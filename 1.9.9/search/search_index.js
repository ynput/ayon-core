var __index = {"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"index.html","title":"AYON Core Addon API Reference","text":"<p>AYON core provides the base building blocks for all other AYON addons and integrations and is responsible for discovery and initialization of other addons.</p> <ul> <li>Some of its key functions include:</li> <li>It is used as the main command line handler in ayon-launcher application.</li> <li>Provides publishing plugins that are available to all AYON integrations.</li> <li>Defines the base classes for new pipeline integrations</li> <li>Provides global hooks</li> <li>Provides universally available loaders and launcher actions</li> <li>Defines pipeline API used by other integrations</li> <li>Provides all graphical tools for artists</li> <li>Defines AYON QT styling</li> <li>A bunch more things</li> </ul> <p>Together with ayon-launcher , they form the base of AYON pipeline and is one of few compulsory addons for AYON pipeline to be useful in a meaningful way.</p> <p>AYON-core is a successor to OpenPype repository (minus all the addons) and still in the process of cleaning up of all references. Please bear with us during this transitional phase.</p>"},{"location":"index.html#development-and-testing-notes","title":"Development and testing notes","text":"<p>There is <code>pyproject.toml</code> file in the root of the repository. This file is used to define the development environment and is used by <code>poetry</code> to create a virtual environment. This virtual environment is used to run tests and to develop the code, to help with linting and formatting. Dependencies defined here are not used in actual addon deployment - for that you need to edit <code>./client/pyproject.toml</code> file. That file will be then processed ayon-dependencies-tool to create dependency package.</p> <p>Right now, this file needs to by synced with dependencies manually, but in the future we plan to automate process of development environment creation.</p>"},{"location":"license.html","title":"License","text":"<pre><code>                             Apache License\n                       Version 2.0, January 2004\n                    http://www.apache.org/licenses/\n</code></pre> <p>TERMS AND CONDITIONS FOR USE, REPRODUCTION, AND DISTRIBUTION</p> <ol> <li> <p>Definitions.</p> <p>\"License\" shall mean the terms and conditions for use, reproduction,   and distribution as defined by Sections 1 through 9 of this document.</p> <p>\"Licensor\" shall mean the copyright owner or entity authorized by   the copyright owner that is granting the License.</p> <p>\"Legal Entity\" shall mean the union of the acting entity and all   other entities that control, are controlled by, or are under common   control with that entity. For the purposes of this definition,   \"control\" means (i) the power, direct or indirect, to cause the   direction or management of such entity, whether by contract or   otherwise, or (ii) ownership of fifty percent (50%) or more of the   outstanding shares, or (iii) beneficial ownership of such entity.</p> <p>\"You\" (or \"Your\") shall mean an individual or Legal Entity   exercising permissions granted by this License.</p> <p>\"Source\" form shall mean the preferred form for making modifications,   including but not limited to software source code, documentation   source, and configuration files.</p> <p>\"Object\" form shall mean any form resulting from mechanical   transformation or translation of a Source form, including but   not limited to compiled object code, generated documentation,   and conversions to other media types.</p> <p>\"Work\" shall mean the work of authorship, whether in Source or   Object form, made available under the License, as indicated by a   copyright notice that is included in or attached to the work   (an example is provided in the Appendix below).</p> <p>\"Derivative Works\" shall mean any work, whether in Source or Object   form, that is based on (or derived from) the Work and for which the   editorial revisions, annotations, elaborations, or other modifications   represent, as a whole, an original work of authorship. For the purposes   of this License, Derivative Works shall not include works that remain   separable from, or merely link (or bind by name) to the interfaces of,   the Work and Derivative Works thereof.</p> <p>\"Contribution\" shall mean any work of authorship, including   the original version of the Work and any modifications or additions   to that Work or Derivative Works thereof, that is intentionally   submitted to Licensor for inclusion in the Work by the copyright owner   or by an individual or Legal Entity authorized to submit on behalf of   the copyright owner. For the purposes of this definition, \"submitted\"   means any form of electronic, verbal, or written communication sent   to the Licensor or its representatives, including but not limited to   communication on electronic mailing lists, source code control systems,   and issue tracking systems that are managed by, or on behalf of, the   Licensor for the purpose of discussing and improving the Work, but   excluding communication that is conspicuously marked or otherwise   designated in writing by the copyright owner as \"Not a Contribution.\"</p> <p>\"Contributor\" shall mean Licensor and any individual or Legal Entity   on behalf of whom a Contribution has been received by Licensor and   subsequently incorporated within the Work.</p> </li> <li> <p>Grant of Copyright License. Subject to the terms and conditions of       this License, each Contributor hereby grants to You a perpetual,       worldwide, non-exclusive, no-charge, royalty-free, irrevocable       copyright license to reproduce, prepare Derivative Works of,       publicly display, publicly perform, sublicense, and distribute the       Work and such Derivative Works in Source or Object form.</p> </li> <li> <p>Grant of Patent License. Subject to the terms and conditions of       this License, each Contributor hereby grants to You a perpetual,       worldwide, non-exclusive, no-charge, royalty-free, irrevocable       (except as stated in this section) patent license to make, have made,       use, offer to sell, sell, import, and otherwise transfer the Work,       where such license applies only to those patent claims licensable       by such Contributor that are necessarily infringed by their       Contribution(s) alone or by combination of their Contribution(s)       with the Work to which such Contribution(s) was submitted. If You       institute patent litigation against any entity (including a       cross-claim or counterclaim in a lawsuit) alleging that the Work       or a Contribution incorporated within the Work constitutes direct       or contributory patent infringement, then any patent licenses       granted to You under this License for that Work shall terminate       as of the date such litigation is filed.</p> </li> <li> <p>Redistribution. You may reproduce and distribute copies of the       Work or Derivative Works thereof in any medium, with or without       modifications, and in Source or Object form, provided that You       meet the following conditions:</p> <p>(a) You must give any other recipients of the Work or       Derivative Works a copy of this License; and</p> <p>(b) You must cause any modified files to carry prominent notices       stating that You changed the files; and</p> <p>(c) You must retain, in the Source form of any Derivative Works       that You distribute, all copyright, patent, trademark, and       attribution notices from the Source form of the Work,       excluding those notices that do not pertain to any part of       the Derivative Works; and</p> <p>(d) If the Work includes a \"NOTICE\" text file as part of its       distribution, then any Derivative Works that You distribute must       include a readable copy of the attribution notices contained       within such NOTICE file, excluding those notices that do not       pertain to any part of the Derivative Works, in at least one       of the following places: within a NOTICE text file distributed       as part of the Derivative Works; within the Source form or       documentation, if provided along with the Derivative Works; or,       within a display generated by the Derivative Works, if and       wherever such third-party notices normally appear. The contents       of the NOTICE file are for informational purposes only and       do not modify the License. You may add Your own attribution       notices within Derivative Works that You distribute, alongside       or as an addendum to the NOTICE text from the Work, provided       that such additional attribution notices cannot be construed       as modifying the License.</p> <p>You may add Your own copyright statement to Your modifications and   may provide additional or different license terms and conditions   for use, reproduction, or distribution of Your modifications, or   for any such Derivative Works as a whole, provided Your use,   reproduction, and distribution of the Work otherwise complies with   the conditions stated in this License.</p> </li> <li> <p>Submission of Contributions. Unless You explicitly state otherwise,       any Contribution intentionally submitted for inclusion in the Work       by You to the Licensor shall be under the terms and conditions of       this License, without any additional terms or conditions.       Notwithstanding the above, nothing herein shall supersede or modify       the terms of any separate license agreement you may have executed       with Licensor regarding such Contributions.</p> </li> <li> <p>Trademarks. This License does not grant permission to use the trade       names, trademarks, service marks, or product names of the Licensor,       except as required for reasonable and customary use in describing the       origin of the Work and reproducing the content of the NOTICE file.</p> </li> <li> <p>Disclaimer of Warranty. Unless required by applicable law or       agreed to in writing, Licensor provides the Work (and each       Contributor provides its Contributions) on an \"AS IS\" BASIS,       WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or       implied, including, without limitation, any warranties or conditions       of TITLE, NON-INFRINGEMENT, MERCHANTABILITY, or FITNESS FOR A       PARTICULAR PURPOSE. You are solely responsible for determining the       appropriateness of using or redistributing the Work and assume any       risks associated with Your exercise of permissions under this License.</p> </li> <li> <p>Limitation of Liability. In no event and under no legal theory,       whether in tort (including negligence), contract, or otherwise,       unless required by applicable law (such as deliberate and grossly       negligent acts) or agreed to in writing, shall any Contributor be       liable to You for damages, including any direct, indirect, special,       incidental, or consequential damages of any character arising as a       result of this License or out of the use or inability to use the       Work (including but not limited to damages for loss of goodwill,       work stoppage, computer failure or malfunction, or any and all       other commercial damages or losses), even if such Contributor       has been advised of the possibility of such damages.</p> </li> <li> <p>Accepting Warranty or Additional Liability. While redistributing       the Work or Derivative Works thereof, You may choose to offer,       and charge a fee for, acceptance of support, warranty, indemnity,       or other liability obligations and/or rights consistent with this       License. However, in accepting such obligations, You may act only       on Your own behalf and on Your sole responsibility, not on behalf       of any other Contributor, and only if You agree to indemnify,       defend, and hold each Contributor harmless for any liability       incurred by, or claims asserted against, such Contributor by reason       of your accepting any such warranty or additional liability.</p> </li> </ol> <p>END OF TERMS AND CONDITIONS</p> <p>APPENDIX: How to apply the Apache License to your work.</p> <pre><code>  To apply the Apache License to your work, attach the following\n  boilerplate notice, with the fields enclosed by brackets \"[]\"\n  replaced with your own identifying information. (Don't include\n  the brackets!)  The text should be enclosed in the appropriate\n  comment syntax for the file format. We also recommend that a\n  file or class name and description of purpose be included on the\n  same \"printed page\" as the copyright notice for easier\n  identification within third-party archives.\n</code></pre> <p>Copyright [yyyy][name of copyright owner]</p> <p>Licensed under the Apache License, Version 2.0 (the \"License\");    you may not use this file except in compliance with the License.    You may obtain a copy of the License at</p> <pre><code>   http://www.apache.org/licenses/LICENSE-2.0\n</code></pre> <p>Unless required by applicable law or agreed to in writing, software    distributed under the License is distributed on an \"AS IS\" BASIS,    WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.    See the License for the specific language governing permissions and    limitations under the License.</p>"},{"location":"ui.html","title":"AYON Core UI (<code>ayon_core.ui</code>)","text":"<p><code>ayon_core.ui</code> is the Qt widget library for AYON, designed to override Qt stylesheets of host applications and preserve our UIs appearance.</p> <p>It provides AYON-styled widgets, data models, and utility components for building consistent desktop tools.</p> <p>Most widgets use variants that can be defined and tweaked in <code>ayon_style.json</code>.</p>"},{"location":"ui.html#overview","title":"Overview","text":"<p><code>ayon_core.ui</code> packages reusable UI building blocks that mirror AYON's visual language while staying native to Qt. It includes:</p> <ul> <li>A custom <code>QStyle</code> implementation (<code>AYONStyle</code>) and style helpers</li> <li>Component widgets (buttons, form controls, containers, cards, filters)</li> <li>Data-driven views and models for paginated tables and lazy trees</li> <li>Async task queue utilities for non-blocking model/view updates</li> </ul>"},{"location":"ui.html#quick-start","title":"Quick Start","text":"<pre><code>from qtpy.QtWidgets import QApplication\n\nfrom ayon_core.ui.style_types import get_ayon_style\nfrom ayon_core.ui.components.buttons import AYButton\nfrom ayon_core.ui.components.container import AYContainer\nfrom ayon_core.ui.components.label import AYLabel\n\napp = QApplication([])\napp.setStyle(get_ayon_style())\n\ncontainer = AYContainer(\n      layout=AYContainer.Layout.VBox,\n      variant=AYContainer.Variants.Low,\n      layout_margin=10,\n      layout_spacing=8,\n)\n\ncontainer.add_widget(\n      AYButton(\"Run\", variant=AYButton.Variants.Filled, icon=\"play_arrow\")\n)\ncontainer.add_widget(\n      AYLabel(\"Ready\", icon=\"check_circle\", icon_color=\"#60c689\")\n)\n\nstyle_widget_and_siblings(container)\ncontainer.show()\napp.exec()\n</code></pre>"},{"location":"ui.html#paginated-model-api","title":"Paginated Model API","text":"<p><code>PaginatedTableModel</code> expects a page fetch callback with this signature:</p> <pre><code>def fetch_page(\n      page: int,\n      page_size: int,\n      sort_key: str | None,\n      descending: bool,\n      parent_id: str | None,\n) -&gt; list[dict]:\n      ...\n</code></pre> <p>Optional batch callback (tree mode child fetches):</p> <pre><code>from ayon_core.ui.components.table_model import BatchFetchRequest\n\n\ndef fetch_page_batch(\n      requests: list[BatchFetchRequest],\n) -&gt; dict[str | None, list[dict]]:\n      ...\n</code></pre> <p>Notes:</p> <ul> <li>Root-level fetch uses <code>fetch_page</code></li> <li>Child-node fetches can be coalesced per event-loop tick using    <code>fetch_page_batch</code></li> <li>Use <code>reset_data()</code> when external context changes (project/folder/filter)</li> </ul>"},{"location":"ui.html#component-catalog","title":"Component Catalog","text":""},{"location":"ui.html#core-widgets","title":"Core Widgets","text":"Module Main classes <code>buttons.py</code> <code>AYButton</code>, <code>AYButtonMenu</code> <code>check_box.py</code> <code>AYCheckBox</code> <code>combo_box.py</code> <code>AYComboBox</code>, <code>AYComboBoxModel</code> <code>line_edit.py</code> <code>AYLineEdit</code> <code>text_edit.py</code> <code>AYTextEdit</code> <code>text_box.py</code> <code>AYTextBox</code>, <code>AYTextEditor</code> <code>label.py</code> <code>AYLabel</code> <code>frame.py</code> <code>AYFrame</code> <code>container.py</code> <code>AYContainer</code> <code>scroll_area.py</code> <code>AYScrollArea</code>, <code>AYScrollBar</code>"},{"location":"ui.html#data-views-and-models","title":"Data Views and Models","text":"Module Main classes <code>table_model.py</code> <code>PaginatedTableModel</code>, <code>TableColumn</code>, <code>BatchFetchRequest</code> <code>table_view.py</code> <code>AYTableView</code>, <code>AYTableHeader</code> <code>table_filter.py</code> <code>AYTableFilter</code>, <code>AYTableFilterProxyModel</code> <code>tree_model.py</code> <code>LazyTreeModel</code>, <code>TreeNode</code> <code>tree_view.py</code> <code>AYTreeView</code> <code>card_view.py</code> <code>AYCardView</code>"},{"location":"ui.html#entity-and-content-widgets","title":"Entity and Content Widgets","text":"Module Main classes <code>entity_card.py</code> <code>AYEntityCard</code> <code>entity_path.py</code> <code>AYEntityPath</code>, <code>AYEntityPathSegment</code> <code>entity_thumbnail.py</code> <code>AYEntityThumbnail</code> <code>comment.py</code> <code>AYComment</code> and related comment widgets <code>comment_completion.py</code> Comment completion helpers <code>gallery_dialog.py</code> <code>AYGalleryDialog</code> <code>user_image.py</code> <code>AYUserImage</code>"},{"location":"ui.html#filtering-tags-and-selection","title":"Filtering, Tags, and Selection","text":"Module Main classes <code>filter.py</code> <code>AYFilter</code>, <code>AYFilterByCategory</code>, <code>FilterItem</code> <code>filterable_list.py</code> <code>AYFilterableList</code> <code>tag.py</code> <code>AYTag</code> <code>tag_selector.py</code> <code>AYTagSelector</code>, <code>TagData</code> <code>slicer.py</code> <code>AYSlicer</code> <code>dropdown.py</code> <code>AYDropdownPopup</code>"},{"location":"ui.html#layout-and-utility-components","title":"Layout and Utility Components","text":"Module Main classes <code>layouts.py</code> <code>AYHBoxLayout</code>, <code>AYVBoxLayout</code>, <code>AYGridLayout</code>, <code>AYFlowLayout</code> <code>task_queue.py</code> <code>AsyncTaskQueue</code>, <code>AsyncTask</code>, queue helpers <code>task_queue_monitor.py</code> <code>AsyncTaskQueueMonitor</code> <code>qss_override.py</code> QSS/style event filter helpers <code>checkbox_handler.py</code> Checkbox event/data helpers <code>screenshot_capture.py</code> Widget screenshot capture helpers"},{"location":"ui.html#project-structure","title":"Project Structure","text":"<pre><code>client/ayon_core/ui/\n\u251c\u2500\u2500 __init__.py\n\u251c\u2500\u2500 style.py\n\u251c\u2500\u2500 variants.py\n\u251c\u2500\u2500 image_cache.py\n\u251c\u2500\u2500 data_models.py\n\u251c\u2500\u2500 components/\n\u2502   \u251c\u2500\u2500 buttons.py\n\u2502   \u251c\u2500\u2500 check_box.py\n\u2502   \u251c\u2500\u2500 combo_box.py\n\u2502   \u251c\u2500\u2500 ...\n\u251c\u2500\u2500 resources/\n\u2502   \u2514\u2500\u2500 ayon_style.json\n\u2514\u2500\u2500 vendor/\n    \u2514\u2500\u2500 qtmaterialsymbols/\n</code></pre>"},{"location":"ui.html#variants-management","title":"Variants Management","text":"<p>The concept of variants is borrowed from AYON's REACT library. A variant is a version of a component with a different appearance. They act like preset stylesheets.</p> <p>Variants are defined in 2 files:</p> <ul> <li><code>ayon_style.json</code> - defines the styles for each variant</li> <li>This file contains:<ul> <li>global settings like the base font or the style's palette.</li> <li>Per base class variants to modify the appearance of the component.</li> </ul> </li> <li><code>variants.py</code> - defines the variants and their properties</li> <li>This file contains enums reflecting all available variants from <code>ayon_style.json</code>.</li> <li>The enums can be automatically updated by running <code>variants.py</code>. The code   in <code>__main__</code> will read <code>ayon_style.json</code> and update the enums if need be. It will also print a summary of the changes.</li> </ul> <p>[!IMPORTANT] Always run <code>variants.py</code> after modifying <code>ayon_style.json</code> to ensure the enums are up to date.</p>"},{"location":"ui.html#migration-from-ayon-ui-qt-addon","title":"Migration from <code>ayon-ui-qt</code> addon","text":"<p>This module was previously shipped as the standalone <code>ayon-ui-qt</code> addon. All imports have moved:</p> <pre><code># Before (standalone addon)\nfrom ayon_core.ui.components.buttons import AYButton\nfrom ayon_core.ui.style_types import get_ayon_style\n\n# After (integrated into core)\nfrom ayon_core.ui.components.buttons import AYButton\nfrom ayon_core.ui.style_types import get_ayon_style\n</code></pre>"},{"location":"ui.html#license","title":"License","text":"<p>Apache-2.0</p>"},{"location":"ui_testing.html","title":"UI Testing","text":"<p>This module uses visual regression testing to verify that UI components render correctly. Tests run headlessly using Qt's offscreen platform, capture widget screenshots, and compare them against stored reference images.</p>"},{"location":"ui_testing.html#prerequisites","title":"Prerequisites","text":"<p>Install the test dependencies (defined in <code>pyproject.toml</code> under <code>[project.optional-dependencies]</code>):</p> <pre><code>pip install -e \".[test]\"\n</code></pre> <p>This installs:</p> Package Purpose <code>pytest</code> Test runner <code>pytest-qt</code> Qt widget lifecycle helpers (<code>qtbot</code>) <code>pytest-regressions[image]</code> Image snapshot comparison"},{"location":"ui_testing.html#running-tests","title":"Running Tests","text":"<p>Run the full UI test suite:</p> <pre><code>pytest tests/client/ayon_core/ui/\n</code></pre> <p>Generate (or regenerate) reference images when adding new tests or intentionally changing component appearance:</p> <pre><code>pytest tests/client/ayon_core/ui/ --force-regen\n</code></pre> <p>When tests fail due to pixel differences, open the visual comparison viewer to inspect results and optionally accept them as new baselines:</p> <pre><code>pytest tests/client/ayon_core/ui/ --show-images\n</code></pre> <p></p>"},{"location":"ui_testing.html#test-structure","title":"Test Structure","text":"<pre><code>tests/client/ayon_core/ui/\n\u251c\u2500\u2500 conftest.py          # Sets QT_QPA_PLATFORM=offscreen before any Qt import\n\u251c\u2500\u2500 test_visual.py       # Auto-discovering runner \u2014 generates parametrized tests\n\u251c\u2500\u2500 visual_utils.py      # capture_widget() helper\n\u251c\u2500\u2500 widget_test.py       # WidgetTest base class\n\u251c\u2500\u2500 components/          # One test_*.py file per component\n\u2502   \u251c\u2500\u2500 test_buttons.py\n\u2502   \u2514\u2500\u2500 test_check_box.py\n\u251c\u2500\u2500 utils/               # Shared test utilities\n\u2502   \u2514\u2500\u2500 composite_widget.py\n\u2514\u2500\u2500 test_visual/         # Reference PNG snapshots (committed to version control)\n    \u251c\u2500\u2500 ButtonTest_00_initial.png\n    \u251c\u2500\u2500 ButtonTest_01_check_all.png\n    \u2514\u2500\u2500 ...\n</code></pre>"},{"location":"ui_testing.html#headless-rendering","title":"Headless rendering","text":"<p><code>conftest.py</code> sets <code>QT_QPA_PLATFORM=offscreen</code> before any Qt import happens. This allows all tests to run without a display server or visible windows.</p>"},{"location":"ui_testing.html#test-runner-test_visualpy","title":"Test runner (<code>test_visual.py</code>)","text":"<p>The runner scans <code>tests/client/ayon_core/ui/components/</code> for any module whose name starts with <code>test_</code>, imports it, and collects all <code>WidgetTest</code> subclasses. For each class it generates two groups of pytest items:</p> <ul> <li><code>test_initial</code> \u2014 snapshot of the widget in its initial state before any steps are applied.</li> <li><code>test_steps</code> \u2014 one parametrized test per step callable, applied cumulatively up to and including the current step.</li> </ul>"},{"location":"ui_testing.html#snapshot-naming","title":"Snapshot naming","text":"<p>Reference images are saved in <code>tests/client/ayon_core/ui/test_visual/</code> with the filename pattern:</p> <pre><code>{ClassName}_{index:02d}_{step_name}.png\n</code></pre> <ul> <li>Index <code>00</code> is always the initial state.</li> <li>Subsequent indices correspond to each step in the order returned by <code>steps()</code>.</li> </ul> <p>Examples:</p> Filename Meaning <code>ButtonTest_00_initial.png</code> <code>ButtonTest</code> initial state <code>ButtonTest_01_check_all.png</code> After <code>check_all</code> step <code>CheckBoxTest_02_uncheck_all.png</code> After second step (<code>uncheck_all</code>)"},{"location":"ui_testing.html#writing-a-new-component-test","title":"Writing a New Component Test","text":"<ol> <li>Create <code>tests/client/ayon_core/ui/components/test_&lt;component&gt;.py</code>.</li> <li>Subclass <code>WidgetTest</code> and implement <code>build()</code>. Optionally implement <code>steps()</code>.</li> </ol> <pre><code># tests/client/ayon_core/ui/components/test_my_widget.py\nfrom qtpy.QtWidgets import QWidget\nfrom widget_test import WidgetTest\nfrom ayon_core.ui.components.my_widget import AYMyWidget\n\n\nclass MyWidgetTest(WidgetTest):\n    size = (600, 400)       # optional: widget dimensions in pixels\n    tolerance = 0.0         # optional: per-pixel diff tolerance (0.0\u20131.0)\n\n    def build(self) -&gt; QWidget:\n        self._widget = AYMyWidget()\n        return self._widget\n\n    def enabled(self) -&gt; None:\n        \"\"\"Step: enable the widget.\"\"\"\n        self._widget.setEnabled(True)\n\n    def disabled(self) -&gt; None:\n        \"\"\"Step: disable the widget.\"\"\"\n        self._widget.setEnabled(False)\n\n    def steps(self):\n        return [self.enabled, self.disabled]\n</code></pre> <ol> <li>Generate reference images for the new test:</li> </ol> <pre><code>pytest tests/client/ayon_core/ui/test_visual.py -k MyWidgetTest --force-regen\n</code></pre> <ol> <li>Commit the generated PNG files from <code>tests/client/ayon_core/ui/test_visual/</code>.</li> </ol>"},{"location":"ui_testing.html#widgettest-api","title":"<code>WidgetTest</code> API","text":"Attribute / Method Description <code>size: tuple[int, int]</code> Widget dimensions applied before the first snapshot. Default: <code>(800, 600)</code>. <code>tolerance: float</code> Per-pixel diff tolerance passed to <code>image_regression.check()</code>. Default: <code>0.0</code>. <code>build() -&gt; QWidget</code> Required. Create and return the widget under test. Store references to sub-widgets you'll mutate in <code>steps()</code> as instance attributes. <code>steps() -&gt; list[Callable]</code> Return an ordered list of callables that mutate widget state. Each callable is called once and followed by a snapshot. Default: <code>[]</code> (initial snapshot only)."},{"location":"ui_testing.html#capture_widget-utility","title":"<code>capture_widget</code> utility","text":"<p><code>visual_utils.capture_widget(widget)</code> renders a widget to PNG bytes using <code>widget.grab()</code>, which works under offscreen rendering without a visible window. The result is passed directly to <code>image_regression.check()</code>.</p>"},{"location":"ui_testing.html#how-snapshot-comparison-works","title":"How Snapshot Comparison Works","text":"<p><code>pytest-regressions</code> stores reference images alongside the test directory. On first run (or with <code>--force-regen</code>), it saves the captured image as the new baseline. On subsequent runs it compares the captured image pixel-by-pixel against the baseline and fails if any pixel differs beyond the configured <code>tolerance</code>.</p> <p>To update a baseline after an intentional visual change:</p> <pre><code>pytest --force-regen -k &lt;TestClassName&gt;\n</code></pre>"},{"location":"ui_testing.html#inspecting-failed-image-comparisons-show-images","title":"Inspecting Failed Image Comparisons (<code>--show-images</code>)","text":"<p>Pass <code>--show-images</code> to open a Qt window after the test run that displays every failed image comparison side-by-side.</p> <pre><code>pytest tests/client/ayon_core/ui/ --show-images\n</code></pre> <p>The viewer is launched in a subprocess without <code>QT_QPA_PLATFORM=offscreen</code> so it renders to a real display. For each failure it shows:</p> <ul> <li>The test name as a heading.</li> <li>A Test / Ref toggle \u2014 click either button to swap between the obtained screenshot and the stored reference.</li> <li>An Accept test button \u2014 copies the obtained image over the reference in <code>tests/client/ayon_core/ui/test_visual/</code>, effectively accepting the new appearance as the new baseline. The button is disabled after clicking to prevent accidental double-acceptance.</li> </ul> <p><code>--show-images</code> has no effect when all tests pass.</p>"},{"location":"ui_testing.html#looking-at-all-reference-images-show-refs","title":"Looking at all reference images (<code>--show-refs</code>)","text":"<p>You can also view all reference images without running tests:</p> <pre><code>python tests/client/ayon_core/ui/visual_utils.py --show-refs\n</code></pre> <p></p> <p>This is useful for verifying that the reference images are correct.</p> <p>The viewer is the same as the one used with <code>--show-images</code>.</p>"},{"location":"autoapi/summary.html","title":"Summary","text":"<ul> <li>client<ul> <li>ayon_core<ul> <li>__main__</li> <li>addon<ul> <li>base</li> <li>click_wrap</li> <li>interfaces</li> <li>ui<ul> <li>process_ready_error</li> </ul> </li> <li>utils</li> </ul> </li> <li>cli</li> <li>hooks<ul> <li>pre_add_last_workfile_arg</li> <li>pre_copy_template_workfile</li> <li>pre_create_extra_workdir_folders</li> <li>pre_filter_farm_environments</li> <li>pre_global_host_data</li> <li>pre_mac_launch</li> <li>pre_new_console_apps</li> <li>pre_ocio_hook</li> <li>pre_remove_launcher_paths</li> </ul> </li> <li>host<ul> <li>abstract</li> <li>constants</li> <li>dirmap</li> <li>host</li> <li>interfaces<ul> <li>exceptions</li> <li>interfaces</li> <li>workfiles</li> </ul> </li> <li>typing</li> </ul> </li> <li>lib<ul> <li>_compatibility</li> <li>attribute_definitions</li> <li>ayon_connection</li> <li>ayon_info</li> <li>cache</li> <li>dateutils</li> <li>env_tools</li> <li>events</li> <li>execute</li> <li>file_transaction</li> <li>icon_definitions</li> <li>local_settings</li> <li>log</li> <li>path_templates</li> <li>path_tools</li> <li>plugin_tools</li> <li>profiles_filtering</li> <li>profiling</li> <li>python_module_tools</li> <li>terminal</li> <li>transcoding</li> <li>vendor_bin_utils</li> </ul> </li> <li>pipeline<ul> <li>actions<ul> <li>inventory</li> <li>launcher</li> <li>loader</li> <li>structures</li> <li>utils</li> </ul> </li> <li>anatomy<ul> <li>anatomy</li> <li>exceptions</li> <li>roots</li> <li>templates</li> </ul> </li> <li>colorspace</li> <li>compatibility</li> <li>constants</li> <li>context_tools</li> <li>create<ul> <li>changes</li> <li>constants</li> <li>context</li> <li>creator_plugins</li> <li>exceptions</li> <li>product_name</li> <li>structures</li> <li>utils</li> </ul> </li> <li>delivery</li> <li>editorial</li> <li>entity_uri</li> <li>farm<ul> <li>patterning</li> <li>pyblish_functions</li> <li>tools</li> </ul> </li> <li>load<ul> <li>plugins</li> <li>utils</li> </ul> </li> <li>plugin_discover</li> <li>project_folders</li> <li>publish<ul> <li>abstract_collect_render</li> <li>abstract_expected_files</li> <li>constants</li> <li>input_versions</li> <li>lib</li> <li>publish_plugins</li> <li>report</li> </ul> </li> <li>schema</li> <li>staging_dir</li> <li>structures</li> <li>tempdir</li> <li>template_data</li> <li>thumbnails</li> <li>traits<ul> <li>color</li> <li>content</li> <li>cryptography</li> <li>lifecycle</li> <li>meta</li> <li>publishing</li> <li>representation</li> <li>temporal</li> <li>three_dimensional</li> <li>trait</li> <li>two_dimensional</li> <li>utils</li> </ul> </li> <li>usdlib</li> <li>version_start</li> <li>workfile<ul> <li>build_workfile</li> <li>lock_workfile</li> <li>path_resolving</li> <li>utils</li> <li>workfile_template_builder</li> </ul> </li> </ul> </li> <li>plugins<ul> <li>actions<ul> <li>open_file_explorer</li> <li>show_in_ayon</li> </ul> </li> <li>inventory<ul> <li>remove_and_load</li> </ul> </li> <li>loader<ul> <li>copy_file</li> <li>create_hero_version</li> <li>delete_old_versions</li> <li>delivery</li> <li>export_otio</li> <li>open_file</li> <li>push_to_project</li> </ul> </li> <li>publish<ul> <li>cleanup</li> <li>cleanup_explicit</li> <li>cleanup_farm</li> <li>collect_addons</li> <li>collect_anatomy_context_data</li> <li>collect_anatomy_instance_data</li> <li>collect_audio</li> <li>collect_cleanup_keys</li> <li>collect_comment</li> <li>collect_context_entities</li> <li>collect_context_label</li> <li>collect_current_ayon_user</li> <li>collect_current_context</li> <li>collect_current_shell_file</li> <li>collect_datetime_data</li> <li>collect_explicit_resolution</li> <li>collect_farm_env_variables</li> <li>collect_farm_target</li> <li>collect_frames_fix</li> <li>collect_from_create_context</li> <li>collect_hierarchy</li> <li>collect_host_name</li> <li>collect_input_representations_to_versions</li> <li>collect_instance_entities</li> <li>collect_machine_name</li> <li>collect_managed_staging_dir</li> <li>collect_otio_frame_ranges</li> <li>collect_otio_review</li> <li>collect_otio_subset_resources</li> <li>collect_rendered_files</li> <li>collect_resources_path</li> <li>collect_scene_loaded_versions</li> <li>collect_scene_version</li> <li>collect_settings</li> <li>collect_shell_workspace</li> <li>collect_source_for_source</li> <li>collect_status</li> <li>collect_time</li> <li>collect_version_tags</li> <li>collect_version_to_list</li> <li>extract_burnin</li> <li>extract_color_transcode</li> <li>extract_colorspace_data</li> <li>extract_hierarchy_to_ayon</li> <li>extract_oiio_postprocess</li> <li>extract_otio_audio_tracks</li> <li>extract_otio_file</li> <li>extract_otio_review</li> <li>extract_otio_trimming_video</li> <li>extract_review</li> <li>extract_review_slate</li> <li>extract_scanline_exr</li> <li>extract_slate_data</li> <li>extract_thumbnail</li> <li>extract_thumbnail_from_source</li> <li>extract_usd_layer_contributions</li> <li>integrate</li> <li>integrate_attach_reviewable</li> <li>integrate_hero_version</li> <li>integrate_hero_version_with_traits</li> <li>integrate_inputlinks</li> <li>integrate_product_group</li> <li>integrate_resources_path</li> <li>integrate_review</li> <li>integrate_thumbnail</li> <li>integrate_traits</li> <li>integrate_version_attrs</li> <li>integrate_versions_to_list</li> <li>preintegrate_thumbnail_representation</li> <li>repair_unicode_strings</li> <li>validate_asset_docs</li> <li>validate_containers</li> <li>validate_file_saved</li> <li>validate_filesequences</li> <li>validate_intent</li> <li>validate_publish_dir</li> <li>validate_resources</li> <li>validate_unique_subsets</li> <li>validate_version</li> </ul> </li> </ul> </li> <li>resources</li> <li>scripts<ul> <li>ocio_wrapper</li> <li>otio_burnin</li> <li>slates<ul> <li>__main__</li> <li>slate_base<ul> <li>api</li> <li>base</li> <li>example</li> <li>font_factory</li> <li>items</li> <li>layer</li> <li>lib</li> <li>main_frame</li> </ul> </li> </ul> </li> </ul> </li> <li>settings<ul> <li>lib</li> </ul> </li> <li>style<ul> <li>color_defs</li> <li>pyqt5_resources</li> <li>pyside2_resources</li> <li>pyside6_resources</li> <li>qrc_resources</li> </ul> </li> <li>tests<ul> <li>conftest</li> <li>plugins<ul> <li>load<ul> <li>test_export_otio</li> </ul> </li> </ul> </li> </ul> </li> <li>tools<ul> <li>attribute_defs<ul> <li>_constants</li> <li>dialog</li> <li>files_widget</li> <li>widgets</li> </ul> </li> <li>common_models<ul> <li>cache</li> <li>hierarchy</li> <li>projects</li> <li>selection</li> <li>settings</li> <li>thumbnails</li> <li>users</li> </ul> </li> <li>console_interpreter<ul> <li>abstract</li> <li>control</li> <li>ui<ul> <li>code_style</li> <li>syntax_highlight</li> <li>utils</li> <li>widgets</li> <li>window</li> </ul> </li> </ul> </li> <li>context_dialog<ul> <li>window</li> </ul> </li> <li>delivery<ul> <li>delivery</li> </ul> </li> <li>experimental_tools<ul> <li>dialog</li> <li>pyblish_debug_stepper</li> <li>tools_def</li> </ul> </li> <li>flickcharm</li> <li>launcher<ul> <li>abstract</li> <li>control</li> <li>models<ul> <li>actions</li> <li>selection</li> <li>workfiles</li> </ul> </li> <li>ui<ul> <li>actions_widget</li> <li>hierarchy_page</li> <li>window</li> <li>workfiles_page</li> </ul> </li> </ul> </li> <li>loader<ul> <li>abstract</li> <li>control</li> <li>models<ul> <li>actions</li> <li>products</li> <li>selection</li> <li>sitesync</li> </ul> </li> <li>ui<ul> <li>_multicombobox</li> <li>actions_utils</li> <li>folders_widget</li> <li>info_widget</li> <li>product_group_dialog</li> <li>products_delegates</li> <li>products_model</li> <li>products_widget</li> <li>repres_widget</li> <li>search_bar</li> <li>tasks_widget</li> <li>window</li> </ul> </li> </ul> </li> <li>publisher<ul> <li>abstract</li> <li>app</li> <li>constants</li> <li>control</li> <li>control_qt</li> <li>models<ul> <li>create</li> <li>publish</li> </ul> </li> <li>publish_report_viewer<ul> <li>constants</li> <li>delegates</li> <li>model</li> <li>widgets</li> <li>window</li> </ul> </li> <li>widgets<ul> <li>border_label_widget</li> <li>card_view_widgets</li> <li>create_context_widgets</li> <li>create_widget</li> <li>folders_dialog</li> <li>help_widget</li> <li>icons</li> <li>list_view_widgets</li> <li>overview_widget</li> <li>precreate_widget</li> <li>product_attributes</li> <li>product_context</li> <li>product_info</li> <li>publish_frame</li> <li>report_page</li> <li>screenshot_widget</li> <li>tabs_widget</li> <li>tasks_model</li> <li>thumbnail_widget</li> <li>utils</li> <li>widgets</li> </ul> </li> <li>window</li> </ul> </li> <li>push_to_project<ul> <li>control</li> <li>main</li> <li>models<ul> <li>integrate</li> <li>selection</li> <li>user_values</li> </ul> </li> <li>ui<ul> <li>window</li> </ul> </li> </ul> </li> <li>resources</li> <li>sceneinventory<ul> <li>control</li> <li>delegates</li> <li>model</li> <li>models<ul> <li>containers</li> <li>sitesync</li> </ul> </li> <li>select_version_dialog</li> <li>switch_dialog<ul> <li>dialog</li> <li>folders_input</li> <li>widgets</li> </ul> </li> <li>view</li> <li>window</li> </ul> </li> <li>stdout_broker<ul> <li>broker</li> <li>window</li> </ul> </li> <li>texture_copy<ul> <li>app</li> </ul> </li> <li>tray<ul> <li>lib</li> <li>structures</li> <li>ui<ul> <li>__main__</li> <li>addons_manager</li> <li>dialogs</li> <li>host_console_listener</li> <li>info_widget</li> <li>tray</li> </ul> </li> <li>webserver<ul> <li>base_routes</li> <li>cors_middleware</li> <li>server</li> </ul> </li> </ul> </li> <li>utils<ul> <li>color_widgets<ul> <li>color_inputs</li> <li>color_picker_widget</li> <li>color_screen_pick</li> <li>color_triangle</li> <li>color_view</li> </ul> </li> <li>constants</li> <li>delegates</li> <li>dialogs</li> <li>error_dialog</li> <li>folders_widget</li> <li>host_tools</li> <li>images</li> <li>layouts</li> <li>lib</li> <li>models</li> <li>multiselection_combobox</li> <li>nice_checkbox</li> <li>overlay_messages</li> <li>projects_widget</li> <li>sliders</li> <li>tasks_widget</li> <li>thumbnail_paint_widget</li> <li>views</li> <li>widgets</li> </ul> </li> <li>workfile_template_build<ul> <li>lib</li> <li>window</li> </ul> </li> <li>workfiles<ul> <li>abstract</li> <li>control</li> <li>lock_dialog</li> <li>models<ul> <li>selection</li> <li>workfiles</li> </ul> </li> <li>widgets<ul> <li>constants</li> <li>files_widget</li> <li>files_widget_published</li> <li>files_widget_workarea</li> <li>save_as_dialog</li> <li>side_panel</li> <li>utils</li> <li>window</li> </ul> </li> </ul> </li> </ul> </li> <li>ui<ul> <li>color_utils</li> <li>components<ul> <li>buttons</li> <li>card_view</li> <li>check_box</li> <li>checkbox_handler</li> <li>combo_box</li> <li>comment</li> <li>comment_completion</li> <li>container</li> <li>dropdown</li> <li>entity_card</li> <li>entity_path</li> <li>entity_thumbnail</li> <li>filter</li> <li>filterable_list</li> <li>frame</li> <li>gallery_dialog</li> <li>label</li> <li>layouts</li> <li>line_edit</li> <li>option_action</li> <li>order</li> <li>page_button</li> <li>qss_override</li> <li>screenshot_capture</li> <li>scroll_area</li> <li>slicer</li> <li>slider</li> <li>style_mixin</li> <li>table_filter</li> <li>table_model</li> <li>table_view</li> <li>tag</li> <li>tag_selector</li> <li>task_queue</li> <li>task_queue_monitor</li> <li>text_box</li> <li>text_edit</li> <li>tree_model</li> <li>tree_view</li> <li>user_image</li> </ul> </li> <li>data_models</li> <li>drawers<ul> <li>_utils</li> <li>button</li> <li>checkbox</li> <li>combobox</li> <li>frame</li> <li>item_view</li> <li>label</li> <li>lineedit</li> <li>menu</li> <li>scroll_area</li> <li>scrollbar</li> <li>table_header</li> <li>tooltip</li> <li>tree_view</li> </ul> </li> <li>image_cache</li> <li>preview_utils<ul> <li>utils</li> </ul> </li> <li>style</li> <li>style_types</li> <li>utils</li> <li>variants</li> </ul> </li> <li>vendor<ul> <li>python<ul> <li>qargparse</li> <li>qtmaterialsymbols<ul> <li>browser</li> <li>exceptions</li> <li>iconic_font</li> <li>resources</li> <li>structures</li> <li>utils</li> <li>version</li> </ul> </li> <li>scriptsmenu<ul> <li>action</li> <li>launchformari</li> <li>launchformaya</li> <li>launchfornuke</li> <li>scriptsmenu</li> <li>version</li> </ul> </li> </ul> </li> </ul> </li> <li>version</li> </ul> </li> </ul> </li> <li>server<ul> <li>settings<ul> <li>conversion</li> <li>main</li> <li>publish_plugins</li> <li>tools</li> </ul> </li> </ul> </li> <li>ui_preview<ul> <li>buttons</li> <li>card_view</li> <li>check_box</li> <li>combo_box</li> <li>comment</li> <li>container</li> <li>entity_card</li> <li>entity_path</li> <li>entity_thumbnail</li> <li>filter</li> <li>frame</li> <li>gallery_dialog</li> <li>label</li> <li>line_edit</li> <li>order</li> <li>page_button</li> <li>slicer</li> <li>slider</li> <li>style</li> <li>table_filter</li> <li>table_model</li> <li>table_view</li> <li>tag</li> <li>tag_selector</li> <li>text_box</li> <li>text_edit</li> <li>tree_view</li> <li>ui_preview_setup</li> <li>user_image</li> </ul> </li> </ul>"},{"location":"autoapi/client/ayon_core/index.html","title":"ayon_core","text":""},{"location":"autoapi/client/ayon_core/__main__.html","title":"__main__","text":"<p>Main entry point for AYON command.</p>"},{"location":"autoapi/client/ayon_core/cli.html","title":"cli","text":"<p>Package for handling AYON command line arguments.</p>"},{"location":"autoapi/client/ayon_core/cli.html#client.ayon_core.cli.addon","title":"<code>addon(ctx)</code>","text":"<p>Addon specific commands created dynamically.</p> <p>These commands are generated dynamically by currently loaded addons.</p> Source code in <code>client/ayon_core/cli.py</code> <pre><code>@main_cli.group(help=\"Run command line arguments of AYON addons\")\n@click.pass_context\ndef addon(ctx):\n    \"\"\"Addon specific commands created dynamically.\n\n    These commands are generated dynamically by currently loaded addons.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/cli.html#client.ayon_core.cli.browser","title":"<code>browser()</code>","text":"<p>Show Browser tool.</p> Source code in <code>client/ayon_core/cli.py</code> <pre><code>@main_cli.command()\ndef browser():\n    \"\"\"Show Browser tool.\"\"\"\n    from ayon_core.tools.loader.ui import LoaderWindow\n    from ayon_core.tools.utils import get_ayon_qt_app\n\n    app = get_ayon_qt_app()\n    browser_window = LoaderWindow()\n    browser_window.setWindowTitle(\"AYON Browser\")\n    browser_window.show()\n    app.exec_()\n</code></pre>"},{"location":"autoapi/client/ayon_core/cli.html#client.ayon_core.cli.contextselection","title":"<code>contextselection(output_path, project, folder, strict)</code>","text":"<p>Show Qt dialog to select context.</p> <p>Context is project name, folder path and task name. The result is stored into json file which path is passed in first argument.</p> Source code in <code>client/ayon_core/cli.py</code> <pre><code>@main_cli.command()\n@click.argument(\"output_path\")\n@click.option(\"--project\", help=\"Define project context\")\n@click.option(\n    \"--folder\", help=\"Define folder in project (project must be set)\")\n@click.option(\n    \"--strict\",\n    is_flag=True,\n    help=\"Full context must be set otherwise dialog can't be closed.\"\n)\ndef contextselection(\n    output_path,\n    project,\n    folder,\n    strict\n):\n    \"\"\"Show Qt dialog to select context.\n\n    Context is project name, folder path and task name. The result is stored\n    into json file which path is passed in first argument.\n    \"\"\"\n    from ayon_core.tools.context_dialog import main\n\n    main(output_path, project, folder, strict)\n</code></pre>"},{"location":"autoapi/client/ayon_core/cli.html#client.ayon_core.cli.create_project_structure","title":"<code>create_project_structure(project)</code>","text":"<p>Create project folder structure as defined in setting <code>ayon+settings://core/project_folder_structure</code></p> <p>Parameters:</p> Name Type Description Default <code>project</code> <code>str</code> <p>The name of the project for which you want to create its additional folder structure.</p> required Source code in <code>client/ayon_core/cli.py</code> <pre><code>@main_cli.command()\n@click.option(\n    \"--project\",\n    type=str,\n    help=\"Project name\",\n    required=True)\ndef create_project_structure(\n    project,\n):\n    \"\"\"Create project folder structure as defined in setting\n    `ayon+settings://core/project_folder_structure`\n\n    Args:\n        project (str): The name of the project for which you\n            want to create its additional folder structure.\n\n    \"\"\"\n\n    from ayon_core.pipeline.project_folders import create_project_folders\n\n    print(f\"&gt;&gt;&gt; Creating project folder structure for project '{project}'.\")\n    create_project_folders(project)\n</code></pre>"},{"location":"autoapi/client/ayon_core/cli.html#client.ayon_core.cli.deliver","title":"<code>deliver(project, event_id, version_ids)</code>","text":"<p>Launch delivery action for selected versions.</p> <p>The version ids can be passed either directly via '--version_ids' (legacy) or via an event using '--event_id'. Exactly one of the two must be provided.</p> <p>Parameters:</p> Name Type Description Default <code>project</code> <code>str</code> <p>The name of the project.</p> required <code>event_id</code> <code>str</code> <p>Id of the event carrying the version ids.</p> required <code>version_ids</code> <code>str</code> <p>Comma separated version ids.</p> required Source code in <code>client/ayon_core/cli.py</code> <pre><code>@main_cli.command()\n@click.option(\n    \"--project\",\n    type=str,\n    help=\"Project name\",\n    required=True)\n@click.option(\n    \"--event_id\",\n    type=str,\n    help=\"Id of the event carrying the version ids\",\n    default=None)\n@click.option(\n    \"--version_ids\",\n    type=str,\n    help=\"Comma separated version ids\",\n    default=None)\ndef deliver(\n    project, event_id, version_ids\n):\n    \"\"\"Launch delivery action for selected versions.\n\n    The version ids can be passed either directly via '--version_ids' (legacy)\n    or via an event using '--event_id'. Exactly one of the two must be\n    provided.\n\n    Args:\n        project (str): The name of the project.\n        event_id (str): Id of the event carrying the version ids.\n        version_ids (str): Comma separated version ids.\n\n    \"\"\"\n\n    print(f\"&gt;&gt;&gt; Launching browser for Delivery action '{project}'.\")\n\n    log = Logger.get_logger(\"delivery\")\n\n    try:\n        from ayon_core.tools.delivery.delivery import DeliveryOptionsDialog\n        # must be here because no module qargparse\n        from ayon_core.tools.utils.lib import get_qt_app\n\n        _app = get_qt_app()\n        list_entity_label = None\n        if event_id:\n            event = ayon_api.get_event(event_id)\n\n            # either version_ids or list_id can be in payload\n            version_ids = event[\"payload\"].get(\"version_ids\")\n            list_id = event[\"payload\"].get(\"list_id\")\n            if not version_ids and not list_id:\n                raise ValueError(\"No version_ids or list_id in payload.\")\n            if list_id:\n                list_entity = ayon_api.get_entity_list_rest(project, list_id)\n                if not list_entity:\n                    raise ValueError(\n                        f\"Entity list '{list_id}' was not found.\")\n                list_entity_label = list_entity[\"label\"]\n                # get all version entities belonging to the list\n                version_ids = [\n                    version[\"entityId\"] for version in list_entity[\"items\"]]\n        elif version_ids:\n            version_ids = version_ids.split(\",\")\n        else:\n            raise ValueError(\"Missing option '--version_ids' or '--event_id'\")\n\n        dialog = DeliveryOptionsDialog(\n            project, version_ids, list_entity_label=list_entity_label, log=log\n        )\n        dialog.exec_()\n    except Exception:\n        log.warning(\"Failed to deliver versions.\", exc_info=True)\n</code></pre>"},{"location":"autoapi/client/ayon_core/cli.html#client.ayon_core.cli.interactive","title":"<code>interactive()</code>","text":"<p>Interactive (Python like) console.</p> <p>Helpful command not only for development to directly work with python interpreter.</p> Warning <p>Executable 'ayon.exe' on Windows won't work.</p> Source code in <code>client/ayon_core/cli.py</code> <pre><code>@main_cli.command()\ndef interactive():\n    \"\"\"Interactive (Python like) console.\n\n    Helpful command not only for development to directly work with python\n    interpreter.\n\n    Warning:\n        Executable 'ayon.exe' on Windows won't work.\n    \"\"\"\n    version = os.environ[\"AYON_VERSION\"]\n    banner = (\n        f\"AYON launcher {version}\\nPython {sys.version} on {sys.platform}\"\n    )\n    code.interact(banner)\n</code></pre>"},{"location":"autoapi/client/ayon_core/cli.html#client.ayon_core.cli.main_cli","title":"<code>main_cli(ctx, *_args, **_kwargs)</code>","text":"<p>AYON is main command serving as entry point to pipeline system.</p> <p>It wraps different commands together.</p> Source code in <code>client/ayon_core/cli.py</code> <pre><code>@click.group(invoke_without_command=True)\n@click.pass_context\n@click.option(\n    \"--use-staging\",\n    is_flag=True,\n    expose_value=False,\n    help=\"use staging variants\")\n@click.option(\n    \"--debug\",\n    is_flag=True,\n    expose_value=False,\n    help=\"Enable debug\")\n@click.option(\n    \"--project\",\n    help=\"Project name\")\n@click.option(\n    \"--verbose\",\n    expose_value=False,\n    help=\"Change AYON log level (debug - critical or 0-50)\")\n@click.option(\n    \"--use-dev\",\n    is_flag=True,\n    expose_value=False,\n    help=\"use dev bundle\")\n@click.option(\n    \"--ayon-login\",\n    is_flag=True,\n    expose_value=False,\n    help=\"Show login dialog (change server or user)\")\ndef main_cli(ctx, *_args, **_kwargs):\n    \"\"\"AYON is main command serving as entry point to pipeline system.\n\n    It wraps different commands together.\n    \"\"\"\n    if ctx.invoked_subcommand is None:\n        # Print help if headless mode is used\n        if os.getenv(\"AYON_HEADLESS_MODE\") == \"1\":\n            print(ctx.get_help())\n            sys.exit(0)\n        else:\n            ctx.params.pop(\"project\")\n            ctx.forward(tray)\n</code></pre>"},{"location":"autoapi/client/ayon_core/cli.html#client.ayon_core.cli.publish","title":"<code>publish(ctx, path, targets)</code>","text":"<p>Start CLI publishing.</p> <p>Publish collects json from path provided as an argument.</p> Source code in <code>client/ayon_core/cli.py</code> <pre><code>@main_cli.command()\n@click.pass_context\n@click.argument(\"path\", required=True)\n@click.option(\"-t\", \"--targets\", help=\"Targets\", default=None,\n              multiple=True)\ndef publish(ctx, path, targets):\n    \"\"\"Start CLI publishing.\n\n    Publish collects json from path provided as an argument.\n\n    \"\"\"\n    from ayon_core.pipeline.publish import main_cli_publish\n\n    main_cli_publish(path, targets, ctx.obj[\"addons_manager\"])\n</code></pre>"},{"location":"autoapi/client/ayon_core/cli.html#client.ayon_core.cli.run","title":"<code>run(script)</code>","text":"<p>Run python script in AYON context.</p> Source code in <code>client/ayon_core/cli.py</code> <pre><code>@main_cli.command(\n    context_settings=dict(\n        ignore_unknown_options=True,\n        allow_extra_args=True))\n@click.argument(\"script\", required=True, type=click.Path(exists=True))\ndef run(script):\n    \"\"\"Run python script in AYON context.\"\"\"\n    import runpy\n\n    if not script:\n        print(\"Error: missing path to script file.\")\n        return\n\n    # Remove first argument if it is the same as AYON executable\n    # - Forward compatibility with future AYON versions.\n    # - Current AYON launcher keeps the arguments with first argument but\n    #     future versions might remove it.\n    first_arg = sys.argv[0]\n    if is_running_from_build():\n        comp_path = os.getenv(\"AYON_EXECUTABLE\")\n    else:\n        comp_path = os.path.join(os.environ[\"AYON_ROOT\"], \"start.py\")\n    # Compare paths and remove first argument if it is the same as AYON\n    if Path(first_arg).resolve() == Path(comp_path).resolve():\n        sys.argv.pop(0)\n\n    # Remove 'run' command from sys.argv\n    sys.argv.remove(\"run\")\n\n    args_string = \" \".join(sys.argv[1:])\n    print(f\"... running: {script} {args_string}\")\n    runpy.run_path(script, run_name=\"__main__\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/cli.html#client.ayon_core.cli.tray","title":"<code>tray(force)</code>","text":"<p>Launch AYON tray.</p> <p>Default action of AYON command is to launch tray widget to control basic aspects of AYON. See documentation for more information.</p> Source code in <code>client/ayon_core/cli.py</code> <pre><code>@main_cli.command()\n@click.option(\n    \"--force\",\n    is_flag=True,\n    help=\"Force to start tray and close any existing one.\")\ndef tray(force):\n    \"\"\"Launch AYON tray.\n\n    Default action of AYON command is to launch tray widget to control basic\n    aspects of AYON. See documentation for more information.\n    \"\"\"\n    from ayon_core.tools.tray import main\n\n    main(force)\n</code></pre>"},{"location":"autoapi/client/ayon_core/cli.html#client.ayon_core.cli.version","title":"<code>version(build)</code>","text":"<p>Print AYON launcher version.</p> Deprecated <p>This function has questionable usage.</p> Source code in <code>client/ayon_core/cli.py</code> <pre><code>@main_cli.command()\n@click.option(\"--build\", help=\"Print only build version\",\n              is_flag=True, default=False)\ndef version(build):\n    \"\"\"Print AYON launcher version.\n\n    Deprecated:\n        This function has questionable usage.\n    \"\"\"\n    print(os.environ[\"AYON_VERSION\"])\n</code></pre>"},{"location":"autoapi/client/ayon_core/version.html","title":"version","text":"<p>Package declaring AYON addon 'core' version.</p>"},{"location":"autoapi/client/ayon_core/addon/index.html","title":"addon","text":"<p>Addons for AYON.</p>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.AYONAddon","title":"<code>AYONAddon</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Base class of AYON addon.</p> <p>Attributes:</p> Name Type Description <code>enabled</code> <code>bool</code> <p>Is addon enabled.</p> <p>Parameters:</p> Name Type Description Default <code>manager</code> <code>AddonsManager</code> <p>Manager object who discovered addon.</p> required <code>settings</code> <code>dict[str, Any]</code> <p>AYON settings.</p> required Source code in <code>client/ayon_core/addon/base.py</code> <pre><code>class AYONAddon(ABC):\n    \"\"\"Base class of AYON addon.\n\n    Attributes:\n        enabled (bool): Is addon enabled.\n\n    Args:\n        manager (AddonsManager): Manager object who discovered addon.\n        settings (dict[str, Any]): AYON settings.\n\n    \"\"\"\n    enabled: bool = True\n    _id = None\n\n    # Temporary variable for 'version' property\n    _missing_version_warned = False\n\n    def __init__(\n        self, manager: AddonsManager, settings: dict[str, Any]\n    ) -&gt; None:\n        self.manager = manager\n\n        self.log = Logger.get_logger(self.name)\n\n        self.initialize(settings)\n\n    @property\n    def id(self) -&gt; str:\n        \"\"\"Random id of addon object.\n\n        Returns:\n            str: Object id.\n\n        \"\"\"\n        if self._id is None:\n            self._id = uuid4()\n        return self._id\n\n    @property\n    @abstractmethod\n    def name(self) -&gt; str:\n        \"\"\"Addon name.\n\n        Returns:\n            str: Addon name.\n\n        \"\"\"\n        pass\n\n    @property\n    def version(self) -&gt; str:\n        \"\"\"Addon version.\n\n        Todo:\n            Should be abstract property (required). Introduced in\n                ayon-core 0.3.3 .\n\n        Returns:\n            str: Addon version as semver compatible string.\n\n        \"\"\"\n        if not self.__class__._missing_version_warned:\n            self.__class__._missing_version_warned = True\n            print(\n                f\"DEV WARNING: Addon '{self.name}' does not have\"\n                f\" defined version.\"\n            )\n        return \"0.0.0\"\n\n    def initialize(self, settings: dict[str, Any]) -&gt; None:\n        \"\"\"Initialization of addon attributes.\n\n        It is not recommended to override __init__ that's why specific method\n        was implemented.\n\n        Args:\n            settings (dict[str, Any]): Settings.\n\n        \"\"\"\n        pass\n\n    def connect_with_addons(self, enabled_addons: list[AYONAddon]) -&gt; None:\n        \"\"\"Connect with other enabled addons.\n\n        Args:\n            enabled_addons (list[AYONAddon]): Addons that are enabled.\n\n        \"\"\"\n        pass\n\n    def ensure_is_process_ready(\n        self, process_context: ProcessContext\n    ) -&gt; None:\n        \"\"\"Make sure addon is prepared for a process.\n\n        This method is called when some action makes sure that addon has set\n        necessary data. For example if user should be logged in\n        and filled credentials in environment variables this method should\n        ask user for credentials.\n\n        Implementation of this method is optional.\n\n        Note:\n            The logic can be similar to logic in tray, but tray does not\n                require to be logged in.\n\n        Args:\n            process_context (ProcessContext): Context of child\n                process.\n\n        \"\"\"\n        pass\n\n    def get_global_environments(self) -&gt; dict[str, str]:\n        \"\"\"Get global environments values of addon.\n\n        Environment variables that can be get only from system settings.\n\n        Returns:\n            dict[str, str]: Environment variables.\n\n        \"\"\"\n        return {}\n\n    def on_host_install(\n        self,\n        host: HostBase,\n        host_name: str,\n        project_name: str,\n    ) -&gt; None:\n        \"\"\"Host was installed which gives option to handle in-host logic.\n\n        It is a good option to register in-host event callbacks which are\n        specific for the addon. The addon is kept in memory for rest of\n        the process.\n\n        Arguments may change in future. E.g. 'host_name' should be possible\n        to receive from 'host' object.\n\n        Args:\n            host (HostBase): Access to installed/registered\n                host object.\n            host_name (str): Name of host.\n            project_name (str): Project name which is main part of host\n                context.\n\n        \"\"\"\n        pass\n\n    def cli(self, addon_click_group: click.Group) -&gt; None:\n        \"\"\"Add commands to click group.\n\n        The best practise is to create click group for whole addon which is\n        used to separate commands.\n\n        Example:\n            class MyPlugin(AYONAddon):\n                ...\n                def cli(self, addon_click_group):\n                    addon_click_group.add_command(cli_main)\n\n\n            @click.group(&lt;addon name&gt;, help=\"&lt;Any help shown in cmd&gt;\")\n            def cli_main():\n                pass\n\n            @cli_main.command()\n            def mycommand():\n                print(\"my_command\")\n\n        Args:\n            addon_click_group (click.Group): Group to which can be added\n                commands.\n\n        \"\"\"\n        pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.AYONAddon.id","title":"<code>id</code>  <code>property</code>","text":"<p>Random id of addon object.</p> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Object id.</p>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.AYONAddon.name","title":"<code>name</code>  <code>abstractmethod</code> <code>property</code>","text":"<p>Addon name.</p> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Addon name.</p>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.AYONAddon.version","title":"<code>version</code>  <code>property</code>","text":"<p>Addon version.</p> Todo <p>Should be abstract property (required). Introduced in     ayon-core 0.3.3 .</p> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Addon version as semver compatible string.</p>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.AYONAddon.cli","title":"<code>cli(addon_click_group)</code>","text":"<p>Add commands to click group.</p> <p>The best practise is to create click group for whole addon which is used to separate commands.</p> Example <p>class MyPlugin(AYONAddon):     ...     def cli(self, addon_click_group):         addon_click_group.add_command(cli_main)</p> <p>@click.group(, help=\"\") def cli_main():     pass <p>@cli_main.command() def mycommand():     print(\"my_command\")</p> <p>Parameters:</p> Name Type Description Default <code>addon_click_group</code> <code>Group</code> <p>Group to which can be added commands.</p> required Source code in <code>client/ayon_core/addon/base.py</code> <pre><code>def cli(self, addon_click_group: click.Group) -&gt; None:\n    \"\"\"Add commands to click group.\n\n    The best practise is to create click group for whole addon which is\n    used to separate commands.\n\n    Example:\n        class MyPlugin(AYONAddon):\n            ...\n            def cli(self, addon_click_group):\n                addon_click_group.add_command(cli_main)\n\n\n        @click.group(&lt;addon name&gt;, help=\"&lt;Any help shown in cmd&gt;\")\n        def cli_main():\n            pass\n\n        @cli_main.command()\n        def mycommand():\n            print(\"my_command\")\n\n    Args:\n        addon_click_group (click.Group): Group to which can be added\n            commands.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.AYONAddon.connect_with_addons","title":"<code>connect_with_addons(enabled_addons)</code>","text":"<p>Connect with other enabled addons.</p> <p>Parameters:</p> Name Type Description Default <code>enabled_addons</code> <code>list[AYONAddon]</code> <p>Addons that are enabled.</p> required Source code in <code>client/ayon_core/addon/base.py</code> <pre><code>def connect_with_addons(self, enabled_addons: list[AYONAddon]) -&gt; None:\n    \"\"\"Connect with other enabled addons.\n\n    Args:\n        enabled_addons (list[AYONAddon]): Addons that are enabled.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.AYONAddon.ensure_is_process_ready","title":"<code>ensure_is_process_ready(process_context)</code>","text":"<p>Make sure addon is prepared for a process.</p> <p>This method is called when some action makes sure that addon has set necessary data. For example if user should be logged in and filled credentials in environment variables this method should ask user for credentials.</p> <p>Implementation of this method is optional.</p> Note <p>The logic can be similar to logic in tray, but tray does not     require to be logged in.</p> <p>Parameters:</p> Name Type Description Default <code>process_context</code> <code>ProcessContext</code> <p>Context of child process.</p> required Source code in <code>client/ayon_core/addon/base.py</code> <pre><code>def ensure_is_process_ready(\n    self, process_context: ProcessContext\n) -&gt; None:\n    \"\"\"Make sure addon is prepared for a process.\n\n    This method is called when some action makes sure that addon has set\n    necessary data. For example if user should be logged in\n    and filled credentials in environment variables this method should\n    ask user for credentials.\n\n    Implementation of this method is optional.\n\n    Note:\n        The logic can be similar to logic in tray, but tray does not\n            require to be logged in.\n\n    Args:\n        process_context (ProcessContext): Context of child\n            process.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.AYONAddon.get_global_environments","title":"<code>get_global_environments()</code>","text":"<p>Get global environments values of addon.</p> <p>Environment variables that can be get only from system settings.</p> <p>Returns:</p> Type Description <code>dict[str, str]</code> <p>dict[str, str]: Environment variables.</p> Source code in <code>client/ayon_core/addon/base.py</code> <pre><code>def get_global_environments(self) -&gt; dict[str, str]:\n    \"\"\"Get global environments values of addon.\n\n    Environment variables that can be get only from system settings.\n\n    Returns:\n        dict[str, str]: Environment variables.\n\n    \"\"\"\n    return {}\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.AYONAddon.initialize","title":"<code>initialize(settings)</code>","text":"<p>Initialization of addon attributes.</p> <p>It is not recommended to override init that's why specific method was implemented.</p> <p>Parameters:</p> Name Type Description Default <code>settings</code> <code>dict[str, Any]</code> <p>Settings.</p> required Source code in <code>client/ayon_core/addon/base.py</code> <pre><code>def initialize(self, settings: dict[str, Any]) -&gt; None:\n    \"\"\"Initialization of addon attributes.\n\n    It is not recommended to override __init__ that's why specific method\n    was implemented.\n\n    Args:\n        settings (dict[str, Any]): Settings.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.AYONAddon.on_host_install","title":"<code>on_host_install(host, host_name, project_name)</code>","text":"<p>Host was installed which gives option to handle in-host logic.</p> <p>It is a good option to register in-host event callbacks which are specific for the addon. The addon is kept in memory for rest of the process.</p> <p>Arguments may change in future. E.g. 'host_name' should be possible to receive from 'host' object.</p> <p>Parameters:</p> Name Type Description Default <code>host</code> <code>HostBase</code> <p>Access to installed/registered host object.</p> required <code>host_name</code> <code>str</code> <p>Name of host.</p> required <code>project_name</code> <code>str</code> <p>Project name which is main part of host context.</p> required Source code in <code>client/ayon_core/addon/base.py</code> <pre><code>def on_host_install(\n    self,\n    host: HostBase,\n    host_name: str,\n    project_name: str,\n) -&gt; None:\n    \"\"\"Host was installed which gives option to handle in-host logic.\n\n    It is a good option to register in-host event callbacks which are\n    specific for the addon. The addon is kept in memory for rest of\n    the process.\n\n    Arguments may change in future. E.g. 'host_name' should be possible\n    to receive from 'host' object.\n\n    Args:\n        host (HostBase): Access to installed/registered\n            host object.\n        host_name (str): Name of host.\n        project_name (str): Project name which is main part of host\n            context.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.AddonsManager","title":"<code>AddonsManager</code>","text":"<p>Manager of addons that helps to load and prepare them to work.</p> <p>Parameters:</p> Name Type Description Default <code>settings</code> <code>dict[str, Any] | None</code> <p>AYON studio settings.</p> <code>None</code> <code>initialize</code> <code>bool</code> <p>Initialize addons on init. True by default.</p> <code>True</code> Source code in <code>client/ayon_core/addon/base.py</code> <pre><code>class AddonsManager:\n    \"\"\"Manager of addons that helps to load and prepare them to work.\n\n    Args:\n        settings (dict[str, Any] | None): AYON studio settings.\n        initialize (bool): Initialize addons on init.\n            True by default.\n\n    \"\"\"\n    # Helper attributes for report\n    _log = None\n\n    def __init__(\n        self,\n        settings: dict[str, Any] | None = None,\n        initialize: bool = True,\n    ) -&gt; None:\n        self._settings = settings\n\n        self._addons: list[AYONAddon] = []\n        self._addons_by_id: dict[str, AYONAddon] = {}\n        self._addons_by_name: dict[str, AYONAddon] = {}\n        self._report_by_name: dict[str, _ReportRow] = {}\n        self._total_row: _ReportRow = _ReportRow(\"Total\", \"(0)\", \"(0)\")\n\n        if initialize:\n            self.initialize_addons()\n            self.connect_addons()\n\n    def __getitem__(self, addon_name: str) -&gt; AYONAddon:\n        return self._addons_by_name[addon_name]\n\n    @property\n    def log(self) -&gt; logging.Logger:\n        if self._log is None:\n            self._log = Logger.get_logger(self.__class__.__name__)\n        return self._log\n\n    def get(\n        self, addon_name: str, default: Any | None = None\n    ) -&gt; AYONAddon | Any:\n        \"\"\"Access addon by name.\n\n        Args:\n            addon_name (str): Name of addon which should be returned.\n            default (Any | None): Default output if addon is not available.\n\n        Returns:\n            AYONAddon | Any: Addon found by name or `default`.\n\n        \"\"\"\n        return self._addons_by_name.get(addon_name, default)\n\n    @property\n    def addons(self) -&gt; list[AYONAddon]:\n        return list(self._addons)\n\n    @property\n    def addons_by_id(self) -&gt; dict[str, AYONAddon]:\n        return dict(self._addons_by_id)\n\n    @property\n    def addons_by_name(self) -&gt; dict[str, AYONAddon]:\n        return dict(self._addons_by_name)\n\n    def get_enabled_addon(\n        self, addon_name: str, default: Any | None = None\n    ) -&gt; AYONAddon | Any:\n        \"\"\"Fast access to enabled addon.\n\n        If addon is available but is not enabled default value is returned.\n\n        Args:\n            addon_name (str): Name of addon which should be returned.\n            default (Any): Default output if addon is not available or is\n                not enabled.\n\n        Returns:\n            Union[AYONAddon, Any]: Enabled addon found by name or None.\n\n        \"\"\"\n        addon = self.get(addon_name)\n        if addon is not None and addon.enabled:\n            return addon\n        return default\n\n    def get_enabled_addons(self) -&gt; list[AYONAddon]:\n        \"\"\"Enabled addons initialized by the manager.\n\n        Returns:\n            list[AYONAddon]: Initialized and enabled addons.\n\n        \"\"\"\n        return [\n            addon\n            for addon in self._addons\n            if addon.enabled\n        ]\n\n    def initialize_addons(self) -&gt; None:\n        \"\"\"Import and initialize addons.\"\"\"\n        # Make sure modules are loaded\n        load_addons()\n\n        self.log.debug(\"*** AYON addons initialization.\")\n\n        # Prepare settings for addons\n        settings = self._settings\n        if settings is None:\n            settings = get_studio_settings()\n\n        time_start = time.time()\n        prev_start_time = time_start\n        addon_classes = []\n        for module in _LoadCache.addon_modules:\n            # Go through globals in `ayon_core.modules`\n            for name in dir(module):\n                modules_item = getattr(module, name, None)\n                # Filter globals that are not classes which inherit from\n                #   AYONAddon\n                if (\n                    not inspect.isclass(modules_item)\n                    or modules_item is AYONAddon\n                    or not issubclass(modules_item, AYONAddon)\n                ):\n                    continue\n\n                # Check if class is abstract (Developing purpose)\n                if inspect.isabstract(modules_item):\n                    # Find abstract attributes by convention on `abc` module\n                    not_implemented = []\n                    for attr_name in dir(modules_item):\n                        attr = getattr(modules_item, attr_name, None)\n                        abs_method = getattr(\n                            attr, \"__isabstractmethod__\", None\n                        )\n                        if attr and abs_method:\n                            not_implemented.append(attr_name)\n\n                    # Log missing implementations\n                    self.log.warning((\n                        \"Skipping abstract Class: {}.\"\n                        \" Missing implementations: {}\"\n                    ).format(name, \", \".join(not_implemented)))\n                    continue\n\n                addon_classes.append(modules_item)\n\n        bundle_info = get_bundle_information()\n        server_version_by_name = {\n            addon.name: addon.version\n            for addon in bundle_info.addons\n        }\n        server_addons = set(server_version_by_name)\n        server_addons.discard(\"core\")\n        self._report_by_name[\"core\"] = _ReportRow(\n            name=\"core\",\n            version=__version__,\n            server_version=server_version_by_name.get(\"core\"),\n        )\n        for addon_cls in addon_classes:\n            name = addon_cls.__name__\n            try:\n                addon = addon_cls(self, settings)\n                server_version = server_version_by_name.get(addon.name)\n                report_row = _ReportRow(\n                    name=addon.name,\n                    version=addon.version,\n                    server_version=server_version,\n                )\n                # Store initialized object\n                self._addons.append(addon)\n                self._addons_by_id[addon.id] = addon\n                self._addons_by_name[addon.name] = addon\n                self._report_by_name[addon.name] = report_row\n                server_addons.discard(addon.name)\n\n                now = time.time()\n                report_row.init_time = now - prev_start_time\n                prev_start_time = now\n\n            except Exception:\n                self.log.warning(\n                    \"Initialization of addon '{}' failed.\".format(name),\n                    exc_info=True\n                )\n\n        self._total_row.version = f\"({len(self._report_by_name)})\"\n        self._total_row.server_version = f\"({len(server_version_by_name)})\"\n\n        for addon_name in server_addons:\n            self._report_by_name[addon_name] = _ReportRow(\n                name=addon_name,\n                version=\"-\",\n                server_version=server_version_by_name[addon_name],\n                server_addon=True,\n            )\n\n        for addon_name in sorted(self._addons_by_name.keys()):\n            addon = self._addons_by_name[addon_name]\n            enabled_str = \"X\" if addon.enabled else \" \"\n            self.log.debug(\n                f\"[{enabled_str}] {addon.name} ({addon.version})\"\n            )\n\n        self._total_row.init_time = time.time() - time_start\n\n    def connect_addons(self) -&gt; None:\n        \"\"\"Trigger connection with other enabled addons.\n\n        Addons should handle their interfaces in `connect_with_addons`.\n        \"\"\"\n        time_start = time.time()\n        prev_start_time = time_start\n        enabled_addons = self.get_enabled_addons()\n        self.log.debug(f\"Has {len(enabled_addons)} enabled addons.\")\n        for addon in enabled_addons:\n            report_row = self._report_by_name[addon.name]\n            try:\n                addon.connect_with_addons(enabled_addons)\n\n            except Exception:\n                self.log.error(\n                    \"BUG: Module failed on connection with other modules.\",\n                    exc_info=True\n                )\n\n            now = time.time()\n            report_row.connect_time = now - prev_start_time\n            prev_start_time = now\n\n        self._total_row.connect_time = time.time() - time_start\n\n    def collect_global_environments(self) -&gt; dict[str, str]:\n        \"\"\"Helper to collect global environment variabled from modules.\n\n        Returns:\n            dict: Global environment variables from enabled modules.\n\n        Raises:\n            AssertionError: Global environment variables must be unique for\n                all modules.\n        \"\"\"\n        module_envs = {}\n        for module in self.get_enabled_addons():\n            # Collect global module's global environments\n            _envs = module.get_global_environments()\n            for key, value in _envs.items():\n                if key in module_envs:\n                    # TODO better error message\n                    raise AssertionError(\n                        \"Duplicated environment key {}\".format(key)\n                    )\n                module_envs[key] = value\n        return module_envs\n\n    def collect_plugin_paths(self) -&gt; dict[str, list[str]]:\n        \"\"\"Helper to collect all plugins from modules inherited IPluginPaths.\n\n        Unknown keys are logged out.\n\n        Deprecated:\n            Use targeted methods 'collect_launcher_action_paths',\n                'collect_create_plugin_paths', 'collect_load_plugin_paths',\n                'collect_publish_plugin_paths' and\n                 'collect_inventory_action_paths' to collect plugin paths.\n\n        Returns:\n            dict: Output is dictionary with keys \"publish\", \"create\", \"load\",\n                \"actions\" and \"inventory\" each containing list of paths.\n\n        \"\"\"\n        warnings.warn(\n            \"Used deprecated method 'collect_plugin_paths'. Please use\"\n            \" targeted methods 'collect_launcher_action_paths',\"\n            \" 'collect_create_plugin_paths', 'collect_load_plugin_paths'\"\n            \" 'collect_publish_plugin_paths' and\"\n            \" 'collect_inventory_action_paths'\",\n            DeprecationWarning,\n            stacklevel=2\n        )\n        # Output structure\n        output = {\n            \"publish\": [],\n            \"create\": [],\n            \"load\": [],\n            \"actions\": [],\n            \"inventory\": []\n        }\n        unknown_keys_by_addon = {}\n        for addon in self.get_enabled_addons():\n            # Skip module that do not inherit from `IPluginPaths`\n            if not isinstance(addon, IPluginPaths):\n                continue\n            plugin_paths = addon.get_plugin_paths()\n            for key, value in plugin_paths.items():\n                # Filter unknown keys\n                if key not in output:\n                    if addon.name not in unknown_keys_by_addon:\n                        unknown_keys_by_addon[addon.name] = []\n                    unknown_keys_by_addon[addon.name].append(key)\n                    continue\n\n                # Skip if value is empty\n                if not value:\n                    continue\n\n                # Convert to list if value is not list\n                if not isinstance(value, (list, tuple, set)):\n                    value = [value]\n                output[key].extend(value)\n\n        # Report unknown keys (Developing purposes)\n        if unknown_keys_by_addon:\n            expected_keys = \", \".join([\n                f'\"{key}\"' for key in output.keys()\n            ])\n            msg_items = []\n            for addon_name, keys in unknown_keys_by_addon.items():\n                joined_keys = \", \".join([f'\"{key}\"' for key in keys])\n                msg_items.append(\n                    f\"Addon: \\\"{addon_name}\\\" - got key {joined_keys}\"\n                )\n            joined_items = \" | \".join(msg_items)\n            self.log.warning(\n                f\"Expected keys from `get_plugin_paths` are {expected_keys}.\"\n                f\" {joined_items}\"\n            )\n        return output\n\n    def _collect_plugin_paths(self, method_name: str, *args, **kwargs):\n        output = []\n        for addon in self.get_enabled_addons():\n            # Skip addon that do not inherit from `IPluginPaths`\n            if not isinstance(addon, IPluginPaths):\n                continue\n\n            paths = None\n            method = getattr(addon, method_name)\n            try:\n                paths = method(*args, **kwargs)\n            except Exception:\n                self.log.warning(\n                    \"Failed to get plugin paths from addon\"\n                    f\" '{addon.name}' using '{method_name}'.\",\n                    exc_info=True\n                )\n\n            if not paths:\n                continue\n\n            if isinstance(paths, str):\n                paths = [paths]\n                self.log.warning(\n                    f\"Addon '{addon.name}' returned invalid output type\"\n                    f\" from '{method_name}'.\"\n                    f\" Got 'str' expected 'list[str]'.\"\n                )\n            output.extend(paths)\n        return output\n\n    def collect_launcher_action_paths(self) -&gt; list[str]:\n        \"\"\"Helper to collect launcher action paths from addons.\n\n        Returns:\n            list: List of paths to launcher actions.\n\n        \"\"\"\n        output = self._collect_plugin_paths(\n            \"get_launcher_action_paths\"\n        )\n        # Add default core actions\n        actions_dir = os.path.join(AYON_CORE_ROOT, \"plugins\", \"actions\")\n        output.insert(0, actions_dir)\n        return output\n\n    def collect_create_plugin_paths(self, host_name: str) -&gt; list[str]:\n        \"\"\"Helper to collect creator plugin paths from addons.\n\n        Args:\n            host_name (str): For which host are creators meant.\n\n        Returns:\n            list[str]: List of creator plugin paths.\n\n        \"\"\"\n        return self._collect_plugin_paths(\n            \"get_create_plugin_paths\",\n            host_name\n        )\n\n    collect_creator_plugin_paths = collect_create_plugin_paths\n\n    def collect_load_plugin_paths(self, host_name: str) -&gt; list[str]:\n        \"\"\"Helper to collect load plugin paths from addons.\n\n        Args:\n            host_name (str): For which host are load plugins meant.\n\n        Returns:\n            list[str]: List of load plugin paths.\n\n        \"\"\"\n        return self._collect_plugin_paths(\n            \"get_load_plugin_paths\",\n            host_name\n        )\n\n    def collect_publish_plugin_paths(self, host_name: str) -&gt; list[str]:\n        \"\"\"Helper to collect load plugin paths from addons.\n\n        Args:\n            host_name (str): For which host are load plugins meant.\n\n        Returns:\n            list[str]: List of pyblish plugin paths.\n\n        \"\"\"\n        return self._collect_plugin_paths(\n            \"get_publish_plugin_paths\",\n            host_name\n        )\n\n    def collect_inventory_action_paths(self, host_name: str) -&gt; list[str]:\n        \"\"\"Helper to collect load plugin paths from addons.\n\n        Args:\n            host_name (str): For which host are load plugins meant.\n\n        Returns:\n            list: List of pyblish plugin paths.\n\n        \"\"\"\n        return self._collect_plugin_paths(\n            \"get_inventory_action_paths\",\n            host_name\n        )\n\n    def get_host_addon(self, host_name: str) -&gt; IHostAddon | None:\n        \"\"\"Find host addon by host name.\n\n        Args:\n            host_name (str): Host name for which is found host addon.\n\n        Returns:\n            IHostAddon | None: Found host addon by name or `None`.\n        \"\"\"\n\n        for addon in self.get_enabled_addons():\n            if (\n                isinstance(addon, IHostAddon)\n                and addon.host_name == host_name\n            ):\n                return addon\n        return None\n\n    def get_host_names(self) -&gt; set[str]:\n        \"\"\"List of available host names based on host addons.\n\n        Returns:\n            set[str]: All available host names based on enabled addons\n                inheriting 'IHostAddon'.\n\n        \"\"\"\n        return {\n            addon.host_name\n            for addon in self.get_enabled_addons()\n            if isinstance(addon, IHostAddon)\n        }\n\n    def print_report(self, include_tray: bool = False) -&gt; None:\n        \"\"\"Print out report of time spent on addons initialization parts.\n\n        Reporting is not automated must be implemented for each initialization\n        part separately. Reports must be stored to `_report` attribute.\n        Print is skipped if `_report` is empty.\n\n        Attribute `_report` is dictionary where key is \"label\" describing\n        the processed part and value is dictionary where key is addon's\n        class name and value is time delta of it's processing.\n\n        \"\"\"\n        sorted_rows = [\n            report_row\n            for report_row in sorted(\n                self._report_by_name.values(),\n                key=lambda item: item.name\n            )\n        ]\n        sorted_rows.append(self._total_row)\n        for report_row in sorted_rows:\n            report_row.sum_total()\n\n        # Add addon names to first column\n        names = [row.name for row in sorted_rows]\n        names.insert(0, \"Addon name\")\n        versions = [row.version or \"N/A\" for row in sorted_rows]\n        versions.insert(0, \"Version\")\n        server_versions = [\n            row.server_version or \"N/A\" for row in sorted_rows\n        ]\n        server_versions.insert(0, \"Server v.\")\n\n        # Prepare columns to calculate their widths\n        cols: list[list[str]] = [names, versions, server_versions]\n        for col_name, attr_name, is_tray_value in (\n            (\"Initialize\", \"init_time\", False),\n            (\"Connect\", \"connect_time\", False),\n            (\"Tray init\", \"tray_init_time\", True),\n            (\"Tray menu\", \"tray_menu_time\", True),\n            (\"Startup\", \"tray_start_time\", True),\n            (\"Total\", \"total_time\", False),\n        ):\n            if is_tray_value and not include_tray:\n                continue\n\n            values = [\n                f\"{getattr(row, attr_name):.3f}\"\n                for row in sorted_rows\n            ]\n            values.insert(0, col_name)\n            cols.append(values)\n\n        col_widths = [\n            max((len(value) for value in col_values))\n            for col_values in cols\n        ]\n\n        # Convert columns to rows for print\n        rows = list(zip(*cols, strict=True))\n        # Top row contains labels\n        top_row = rows.pop(0)\n        # Last row contains totals\n        total_row = rows.pop(-1)\n\n        separator = \"+\".join([w * \"-\" for w in col_widths])\n\n        lines = [separator]\n        lines.append(\"|\".join(\n            value.ljust(col_width)\n            for col_width, value in zip(col_widths, top_row)\n        ))\n        lines.append(separator)\n\n        for row in rows:\n            lines.append(\"|\".join(\n                value.ljust(col_width)\n                for col_width, value in zip(col_widths, row)\n            ))\n\n        lines.append(separator)\n        lines.append(\"|\".join(\n            value.ljust(col_width)\n            for col_width, value in zip(col_widths, total_row)\n        ))\n        lines.append(\"\")\n\n        output = \"\\n\".join(lines)\n        print(output)\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.AddonsManager.collect_create_plugin_paths","title":"<code>collect_create_plugin_paths(host_name)</code>","text":"<p>Helper to collect creator plugin paths from addons.</p> <p>Parameters:</p> Name Type Description Default <code>host_name</code> <code>str</code> <p>For which host are creators meant.</p> required <p>Returns:</p> Type Description <code>list[str]</code> <p>list[str]: List of creator plugin paths.</p> Source code in <code>client/ayon_core/addon/base.py</code> <pre><code>def collect_create_plugin_paths(self, host_name: str) -&gt; list[str]:\n    \"\"\"Helper to collect creator plugin paths from addons.\n\n    Args:\n        host_name (str): For which host are creators meant.\n\n    Returns:\n        list[str]: List of creator plugin paths.\n\n    \"\"\"\n    return self._collect_plugin_paths(\n        \"get_create_plugin_paths\",\n        host_name\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.AddonsManager.collect_global_environments","title":"<code>collect_global_environments()</code>","text":"<p>Helper to collect global environment variabled from modules.</p> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, str]</code> <p>Global environment variables from enabled modules.</p> <p>Raises:</p> Type Description <code>AssertionError</code> <p>Global environment variables must be unique for all modules.</p> Source code in <code>client/ayon_core/addon/base.py</code> <pre><code>def collect_global_environments(self) -&gt; dict[str, str]:\n    \"\"\"Helper to collect global environment variabled from modules.\n\n    Returns:\n        dict: Global environment variables from enabled modules.\n\n    Raises:\n        AssertionError: Global environment variables must be unique for\n            all modules.\n    \"\"\"\n    module_envs = {}\n    for module in self.get_enabled_addons():\n        # Collect global module's global environments\n        _envs = module.get_global_environments()\n        for key, value in _envs.items():\n            if key in module_envs:\n                # TODO better error message\n                raise AssertionError(\n                    \"Duplicated environment key {}\".format(key)\n                )\n            module_envs[key] = value\n    return module_envs\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.AddonsManager.collect_inventory_action_paths","title":"<code>collect_inventory_action_paths(host_name)</code>","text":"<p>Helper to collect load plugin paths from addons.</p> <p>Parameters:</p> Name Type Description Default <code>host_name</code> <code>str</code> <p>For which host are load plugins meant.</p> required <p>Returns:</p> Name Type Description <code>list</code> <code>list[str]</code> <p>List of pyblish plugin paths.</p> Source code in <code>client/ayon_core/addon/base.py</code> <pre><code>def collect_inventory_action_paths(self, host_name: str) -&gt; list[str]:\n    \"\"\"Helper to collect load plugin paths from addons.\n\n    Args:\n        host_name (str): For which host are load plugins meant.\n\n    Returns:\n        list: List of pyblish plugin paths.\n\n    \"\"\"\n    return self._collect_plugin_paths(\n        \"get_inventory_action_paths\",\n        host_name\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.AddonsManager.collect_launcher_action_paths","title":"<code>collect_launcher_action_paths()</code>","text":"<p>Helper to collect launcher action paths from addons.</p> <p>Returns:</p> Name Type Description <code>list</code> <code>list[str]</code> <p>List of paths to launcher actions.</p> Source code in <code>client/ayon_core/addon/base.py</code> <pre><code>def collect_launcher_action_paths(self) -&gt; list[str]:\n    \"\"\"Helper to collect launcher action paths from addons.\n\n    Returns:\n        list: List of paths to launcher actions.\n\n    \"\"\"\n    output = self._collect_plugin_paths(\n        \"get_launcher_action_paths\"\n    )\n    # Add default core actions\n    actions_dir = os.path.join(AYON_CORE_ROOT, \"plugins\", \"actions\")\n    output.insert(0, actions_dir)\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.AddonsManager.collect_load_plugin_paths","title":"<code>collect_load_plugin_paths(host_name)</code>","text":"<p>Helper to collect load plugin paths from addons.</p> <p>Parameters:</p> Name Type Description Default <code>host_name</code> <code>str</code> <p>For which host are load plugins meant.</p> required <p>Returns:</p> Type Description <code>list[str]</code> <p>list[str]: List of load plugin paths.</p> Source code in <code>client/ayon_core/addon/base.py</code> <pre><code>def collect_load_plugin_paths(self, host_name: str) -&gt; list[str]:\n    \"\"\"Helper to collect load plugin paths from addons.\n\n    Args:\n        host_name (str): For which host are load plugins meant.\n\n    Returns:\n        list[str]: List of load plugin paths.\n\n    \"\"\"\n    return self._collect_plugin_paths(\n        \"get_load_plugin_paths\",\n        host_name\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.AddonsManager.collect_plugin_paths","title":"<code>collect_plugin_paths()</code>","text":"<p>Helper to collect all plugins from modules inherited IPluginPaths.</p> <p>Unknown keys are logged out.</p> Deprecated <p>Use targeted methods 'collect_launcher_action_paths',     'collect_create_plugin_paths', 'collect_load_plugin_paths',     'collect_publish_plugin_paths' and      'collect_inventory_action_paths' to collect plugin paths.</p> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, list[str]]</code> <p>Output is dictionary with keys \"publish\", \"create\", \"load\", \"actions\" and \"inventory\" each containing list of paths.</p> Source code in <code>client/ayon_core/addon/base.py</code> <pre><code>def collect_plugin_paths(self) -&gt; dict[str, list[str]]:\n    \"\"\"Helper to collect all plugins from modules inherited IPluginPaths.\n\n    Unknown keys are logged out.\n\n    Deprecated:\n        Use targeted methods 'collect_launcher_action_paths',\n            'collect_create_plugin_paths', 'collect_load_plugin_paths',\n            'collect_publish_plugin_paths' and\n             'collect_inventory_action_paths' to collect plugin paths.\n\n    Returns:\n        dict: Output is dictionary with keys \"publish\", \"create\", \"load\",\n            \"actions\" and \"inventory\" each containing list of paths.\n\n    \"\"\"\n    warnings.warn(\n        \"Used deprecated method 'collect_plugin_paths'. Please use\"\n        \" targeted methods 'collect_launcher_action_paths',\"\n        \" 'collect_create_plugin_paths', 'collect_load_plugin_paths'\"\n        \" 'collect_publish_plugin_paths' and\"\n        \" 'collect_inventory_action_paths'\",\n        DeprecationWarning,\n        stacklevel=2\n    )\n    # Output structure\n    output = {\n        \"publish\": [],\n        \"create\": [],\n        \"load\": [],\n        \"actions\": [],\n        \"inventory\": []\n    }\n    unknown_keys_by_addon = {}\n    for addon in self.get_enabled_addons():\n        # Skip module that do not inherit from `IPluginPaths`\n        if not isinstance(addon, IPluginPaths):\n            continue\n        plugin_paths = addon.get_plugin_paths()\n        for key, value in plugin_paths.items():\n            # Filter unknown keys\n            if key not in output:\n                if addon.name not in unknown_keys_by_addon:\n                    unknown_keys_by_addon[addon.name] = []\n                unknown_keys_by_addon[addon.name].append(key)\n                continue\n\n            # Skip if value is empty\n            if not value:\n                continue\n\n            # Convert to list if value is not list\n            if not isinstance(value, (list, tuple, set)):\n                value = [value]\n            output[key].extend(value)\n\n    # Report unknown keys (Developing purposes)\n    if unknown_keys_by_addon:\n        expected_keys = \", \".join([\n            f'\"{key}\"' for key in output.keys()\n        ])\n        msg_items = []\n        for addon_name, keys in unknown_keys_by_addon.items():\n            joined_keys = \", \".join([f'\"{key}\"' for key in keys])\n            msg_items.append(\n                f\"Addon: \\\"{addon_name}\\\" - got key {joined_keys}\"\n            )\n        joined_items = \" | \".join(msg_items)\n        self.log.warning(\n            f\"Expected keys from `get_plugin_paths` are {expected_keys}.\"\n            f\" {joined_items}\"\n        )\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.AddonsManager.collect_publish_plugin_paths","title":"<code>collect_publish_plugin_paths(host_name)</code>","text":"<p>Helper to collect load plugin paths from addons.</p> <p>Parameters:</p> Name Type Description Default <code>host_name</code> <code>str</code> <p>For which host are load plugins meant.</p> required <p>Returns:</p> Type Description <code>list[str]</code> <p>list[str]: List of pyblish plugin paths.</p> Source code in <code>client/ayon_core/addon/base.py</code> <pre><code>def collect_publish_plugin_paths(self, host_name: str) -&gt; list[str]:\n    \"\"\"Helper to collect load plugin paths from addons.\n\n    Args:\n        host_name (str): For which host are load plugins meant.\n\n    Returns:\n        list[str]: List of pyblish plugin paths.\n\n    \"\"\"\n    return self._collect_plugin_paths(\n        \"get_publish_plugin_paths\",\n        host_name\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.AddonsManager.connect_addons","title":"<code>connect_addons()</code>","text":"<p>Trigger connection with other enabled addons.</p> <p>Addons should handle their interfaces in <code>connect_with_addons</code>.</p> Source code in <code>client/ayon_core/addon/base.py</code> <pre><code>def connect_addons(self) -&gt; None:\n    \"\"\"Trigger connection with other enabled addons.\n\n    Addons should handle their interfaces in `connect_with_addons`.\n    \"\"\"\n    time_start = time.time()\n    prev_start_time = time_start\n    enabled_addons = self.get_enabled_addons()\n    self.log.debug(f\"Has {len(enabled_addons)} enabled addons.\")\n    for addon in enabled_addons:\n        report_row = self._report_by_name[addon.name]\n        try:\n            addon.connect_with_addons(enabled_addons)\n\n        except Exception:\n            self.log.error(\n                \"BUG: Module failed on connection with other modules.\",\n                exc_info=True\n            )\n\n        now = time.time()\n        report_row.connect_time = now - prev_start_time\n        prev_start_time = now\n\n    self._total_row.connect_time = time.time() - time_start\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.AddonsManager.get","title":"<code>get(addon_name, default=None)</code>","text":"<p>Access addon by name.</p> <p>Parameters:</p> Name Type Description Default <code>addon_name</code> <code>str</code> <p>Name of addon which should be returned.</p> required <code>default</code> <code>Any | None</code> <p>Default output if addon is not available.</p> <code>None</code> <p>Returns:</p> Type Description <code>AYONAddon | Any</code> <p>AYONAddon | Any: Addon found by name or <code>default</code>.</p> Source code in <code>client/ayon_core/addon/base.py</code> <pre><code>def get(\n    self, addon_name: str, default: Any | None = None\n) -&gt; AYONAddon | Any:\n    \"\"\"Access addon by name.\n\n    Args:\n        addon_name (str): Name of addon which should be returned.\n        default (Any | None): Default output if addon is not available.\n\n    Returns:\n        AYONAddon | Any: Addon found by name or `default`.\n\n    \"\"\"\n    return self._addons_by_name.get(addon_name, default)\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.AddonsManager.get_enabled_addon","title":"<code>get_enabled_addon(addon_name, default=None)</code>","text":"<p>Fast access to enabled addon.</p> <p>If addon is available but is not enabled default value is returned.</p> <p>Parameters:</p> Name Type Description Default <code>addon_name</code> <code>str</code> <p>Name of addon which should be returned.</p> required <code>default</code> <code>Any</code> <p>Default output if addon is not available or is not enabled.</p> <code>None</code> <p>Returns:</p> Type Description <code>AYONAddon | Any</code> <p>Union[AYONAddon, Any]: Enabled addon found by name or None.</p> Source code in <code>client/ayon_core/addon/base.py</code> <pre><code>def get_enabled_addon(\n    self, addon_name: str, default: Any | None = None\n) -&gt; AYONAddon | Any:\n    \"\"\"Fast access to enabled addon.\n\n    If addon is available but is not enabled default value is returned.\n\n    Args:\n        addon_name (str): Name of addon which should be returned.\n        default (Any): Default output if addon is not available or is\n            not enabled.\n\n    Returns:\n        Union[AYONAddon, Any]: Enabled addon found by name or None.\n\n    \"\"\"\n    addon = self.get(addon_name)\n    if addon is not None and addon.enabled:\n        return addon\n    return default\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.AddonsManager.get_enabled_addons","title":"<code>get_enabled_addons()</code>","text":"<p>Enabled addons initialized by the manager.</p> <p>Returns:</p> Type Description <code>list[AYONAddon]</code> <p>list[AYONAddon]: Initialized and enabled addons.</p> Source code in <code>client/ayon_core/addon/base.py</code> <pre><code>def get_enabled_addons(self) -&gt; list[AYONAddon]:\n    \"\"\"Enabled addons initialized by the manager.\n\n    Returns:\n        list[AYONAddon]: Initialized and enabled addons.\n\n    \"\"\"\n    return [\n        addon\n        for addon in self._addons\n        if addon.enabled\n    ]\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.AddonsManager.get_host_addon","title":"<code>get_host_addon(host_name)</code>","text":"<p>Find host addon by host name.</p> <p>Parameters:</p> Name Type Description Default <code>host_name</code> <code>str</code> <p>Host name for which is found host addon.</p> required <p>Returns:</p> Type Description <code>IHostAddon | None</code> <p>IHostAddon | None: Found host addon by name or <code>None</code>.</p> Source code in <code>client/ayon_core/addon/base.py</code> <pre><code>def get_host_addon(self, host_name: str) -&gt; IHostAddon | None:\n    \"\"\"Find host addon by host name.\n\n    Args:\n        host_name (str): Host name for which is found host addon.\n\n    Returns:\n        IHostAddon | None: Found host addon by name or `None`.\n    \"\"\"\n\n    for addon in self.get_enabled_addons():\n        if (\n            isinstance(addon, IHostAddon)\n            and addon.host_name == host_name\n        ):\n            return addon\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.AddonsManager.get_host_names","title":"<code>get_host_names()</code>","text":"<p>List of available host names based on host addons.</p> <p>Returns:</p> Type Description <code>set[str]</code> <p>set[str]: All available host names based on enabled addons inheriting 'IHostAddon'.</p> Source code in <code>client/ayon_core/addon/base.py</code> <pre><code>def get_host_names(self) -&gt; set[str]:\n    \"\"\"List of available host names based on host addons.\n\n    Returns:\n        set[str]: All available host names based on enabled addons\n            inheriting 'IHostAddon'.\n\n    \"\"\"\n    return {\n        addon.host_name\n        for addon in self.get_enabled_addons()\n        if isinstance(addon, IHostAddon)\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.AddonsManager.initialize_addons","title":"<code>initialize_addons()</code>","text":"<p>Import and initialize addons.</p> Source code in <code>client/ayon_core/addon/base.py</code> <pre><code>def initialize_addons(self) -&gt; None:\n    \"\"\"Import and initialize addons.\"\"\"\n    # Make sure modules are loaded\n    load_addons()\n\n    self.log.debug(\"*** AYON addons initialization.\")\n\n    # Prepare settings for addons\n    settings = self._settings\n    if settings is None:\n        settings = get_studio_settings()\n\n    time_start = time.time()\n    prev_start_time = time_start\n    addon_classes = []\n    for module in _LoadCache.addon_modules:\n        # Go through globals in `ayon_core.modules`\n        for name in dir(module):\n            modules_item = getattr(module, name, None)\n            # Filter globals that are not classes which inherit from\n            #   AYONAddon\n            if (\n                not inspect.isclass(modules_item)\n                or modules_item is AYONAddon\n                or not issubclass(modules_item, AYONAddon)\n            ):\n                continue\n\n            # Check if class is abstract (Developing purpose)\n            if inspect.isabstract(modules_item):\n                # Find abstract attributes by convention on `abc` module\n                not_implemented = []\n                for attr_name in dir(modules_item):\n                    attr = getattr(modules_item, attr_name, None)\n                    abs_method = getattr(\n                        attr, \"__isabstractmethod__\", None\n                    )\n                    if attr and abs_method:\n                        not_implemented.append(attr_name)\n\n                # Log missing implementations\n                self.log.warning((\n                    \"Skipping abstract Class: {}.\"\n                    \" Missing implementations: {}\"\n                ).format(name, \", \".join(not_implemented)))\n                continue\n\n            addon_classes.append(modules_item)\n\n    bundle_info = get_bundle_information()\n    server_version_by_name = {\n        addon.name: addon.version\n        for addon in bundle_info.addons\n    }\n    server_addons = set(server_version_by_name)\n    server_addons.discard(\"core\")\n    self._report_by_name[\"core\"] = _ReportRow(\n        name=\"core\",\n        version=__version__,\n        server_version=server_version_by_name.get(\"core\"),\n    )\n    for addon_cls in addon_classes:\n        name = addon_cls.__name__\n        try:\n            addon = addon_cls(self, settings)\n            server_version = server_version_by_name.get(addon.name)\n            report_row = _ReportRow(\n                name=addon.name,\n                version=addon.version,\n                server_version=server_version,\n            )\n            # Store initialized object\n            self._addons.append(addon)\n            self._addons_by_id[addon.id] = addon\n            self._addons_by_name[addon.name] = addon\n            self._report_by_name[addon.name] = report_row\n            server_addons.discard(addon.name)\n\n            now = time.time()\n            report_row.init_time = now - prev_start_time\n            prev_start_time = now\n\n        except Exception:\n            self.log.warning(\n                \"Initialization of addon '{}' failed.\".format(name),\n                exc_info=True\n            )\n\n    self._total_row.version = f\"({len(self._report_by_name)})\"\n    self._total_row.server_version = f\"({len(server_version_by_name)})\"\n\n    for addon_name in server_addons:\n        self._report_by_name[addon_name] = _ReportRow(\n            name=addon_name,\n            version=\"-\",\n            server_version=server_version_by_name[addon_name],\n            server_addon=True,\n        )\n\n    for addon_name in sorted(self._addons_by_name.keys()):\n        addon = self._addons_by_name[addon_name]\n        enabled_str = \"X\" if addon.enabled else \" \"\n        self.log.debug(\n            f\"[{enabled_str}] {addon.name} ({addon.version})\"\n        )\n\n    self._total_row.init_time = time.time() - time_start\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.AddonsManager.print_report","title":"<code>print_report(include_tray=False)</code>","text":"<p>Print out report of time spent on addons initialization parts.</p> <p>Reporting is not automated must be implemented for each initialization part separately. Reports must be stored to <code>_report</code> attribute. Print is skipped if <code>_report</code> is empty.</p> <p>Attribute <code>_report</code> is dictionary where key is \"label\" describing the processed part and value is dictionary where key is addon's class name and value is time delta of it's processing.</p> Source code in <code>client/ayon_core/addon/base.py</code> <pre><code>def print_report(self, include_tray: bool = False) -&gt; None:\n    \"\"\"Print out report of time spent on addons initialization parts.\n\n    Reporting is not automated must be implemented for each initialization\n    part separately. Reports must be stored to `_report` attribute.\n    Print is skipped if `_report` is empty.\n\n    Attribute `_report` is dictionary where key is \"label\" describing\n    the processed part and value is dictionary where key is addon's\n    class name and value is time delta of it's processing.\n\n    \"\"\"\n    sorted_rows = [\n        report_row\n        for report_row in sorted(\n            self._report_by_name.values(),\n            key=lambda item: item.name\n        )\n    ]\n    sorted_rows.append(self._total_row)\n    for report_row in sorted_rows:\n        report_row.sum_total()\n\n    # Add addon names to first column\n    names = [row.name for row in sorted_rows]\n    names.insert(0, \"Addon name\")\n    versions = [row.version or \"N/A\" for row in sorted_rows]\n    versions.insert(0, \"Version\")\n    server_versions = [\n        row.server_version or \"N/A\" for row in sorted_rows\n    ]\n    server_versions.insert(0, \"Server v.\")\n\n    # Prepare columns to calculate their widths\n    cols: list[list[str]] = [names, versions, server_versions]\n    for col_name, attr_name, is_tray_value in (\n        (\"Initialize\", \"init_time\", False),\n        (\"Connect\", \"connect_time\", False),\n        (\"Tray init\", \"tray_init_time\", True),\n        (\"Tray menu\", \"tray_menu_time\", True),\n        (\"Startup\", \"tray_start_time\", True),\n        (\"Total\", \"total_time\", False),\n    ):\n        if is_tray_value and not include_tray:\n            continue\n\n        values = [\n            f\"{getattr(row, attr_name):.3f}\"\n            for row in sorted_rows\n        ]\n        values.insert(0, col_name)\n        cols.append(values)\n\n    col_widths = [\n        max((len(value) for value in col_values))\n        for col_values in cols\n    ]\n\n    # Convert columns to rows for print\n    rows = list(zip(*cols, strict=True))\n    # Top row contains labels\n    top_row = rows.pop(0)\n    # Last row contains totals\n    total_row = rows.pop(-1)\n\n    separator = \"+\".join([w * \"-\" for w in col_widths])\n\n    lines = [separator]\n    lines.append(\"|\".join(\n        value.ljust(col_width)\n        for col_width, value in zip(col_widths, top_row)\n    ))\n    lines.append(separator)\n\n    for row in rows:\n        lines.append(\"|\".join(\n            value.ljust(col_width)\n            for col_width, value in zip(col_widths, row)\n        ))\n\n    lines.append(separator)\n    lines.append(\"|\".join(\n        value.ljust(col_width)\n        for col_width, value in zip(col_widths, total_row)\n    ))\n    lines.append(\"\")\n\n    output = \"\\n\".join(lines)\n    print(output)\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.IHostAddon","title":"<code>IHostAddon</code>","text":"<p>               Bases: <code>AYONInterface</code></p> <p>Addon which also contain a host implementation.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>class IHostAddon(AYONInterface):\n    \"\"\"Addon which also contain a host implementation.\"\"\"\n\n    @property\n    @abstractmethod\n    def host_name(self) -&gt; str:\n        \"\"\"Name of host which addon represents.\"\"\"\n\n    def get_workfile_extensions(self) -&gt; list[str]:  # noqa: PLR6301\n        \"\"\"Define workfile extensions for host.\n\n        Not all hosts support workfiles thus this is optional implementation.\n\n        Returns:\n            List[str]: Extensions used for workfiles with dot.\n\n        \"\"\"\n        return []\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.IHostAddon.host_name","title":"<code>host_name</code>  <code>abstractmethod</code> <code>property</code>","text":"<p>Name of host which addon represents.</p>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.IHostAddon.get_workfile_extensions","title":"<code>get_workfile_extensions()</code>","text":"<p>Define workfile extensions for host.</p> <p>Not all hosts support workfiles thus this is optional implementation.</p> <p>Returns:</p> Type Description <code>list[str]</code> <p>List[str]: Extensions used for workfiles with dot.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>def get_workfile_extensions(self) -&gt; list[str]:  # noqa: PLR6301\n    \"\"\"Define workfile extensions for host.\n\n    Not all hosts support workfiles thus this is optional implementation.\n\n    Returns:\n        List[str]: Extensions used for workfiles with dot.\n\n    \"\"\"\n    return []\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.IPluginPaths","title":"<code>IPluginPaths</code>","text":"<p>               Bases: <code>AYONInterface</code></p> <p>Addon wants to register plugin paths.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>class IPluginPaths(AYONInterface):\n    \"\"\"Addon wants to register plugin paths.\"\"\"\n\n    def get_plugin_paths(self) -&gt; dict[str, list[str]]:\n        \"\"\"Return plugin paths for addon.\n\n        This method was abstract (required) in the past, so raise the required\n            'core' addon version when 'get_plugin_paths' is removed from\n            addon.\n\n        Deprecated:\n            Please implement specific methods 'get_create_plugin_paths',\n                'get_load_plugin_paths', 'get_inventory_action_paths' and\n                'get_publish_plugin_paths' to return plugin paths.\n\n        Returns:\n            dict[str, list[str]]: Plugin paths for addon.\n\n        \"\"\"\n        return {}\n\n    def _get_plugin_paths_by_type(\n        self, plugin_type: str\n    ) -&gt; list[str]:\n        \"\"\"Get plugin paths by type.\n\n        Args:\n            plugin_type (str): Type of plugin paths to get.\n\n        Returns:\n            list[str]: List of plugin paths.\n\n        \"\"\"\n        paths = self.get_plugin_paths()\n        if not paths or plugin_type not in paths:\n            return []\n\n        paths = paths[plugin_type]\n        if not paths:\n            return []\n\n        if not isinstance(paths, (list, tuple, set)):\n            paths = [paths]\n\n        new_function_name = \"get_launcher_action_paths\"\n        if plugin_type == \"create\":\n            new_function_name = \"get_create_plugin_paths\"\n        elif plugin_type == \"load\":\n            new_function_name = \"get_load_plugin_paths\"\n        elif plugin_type == \"publish\":\n            new_function_name = \"get_publish_plugin_paths\"\n        elif plugin_type == \"inventory\":\n            new_function_name = \"get_inventory_action_paths\"\n\n        warnings.warn(\n            f\"Addon '{self.name}' returns '{plugin_type}' paths using\"\n            \" 'get_plugin_paths' method. Please implement\"\n            f\" '{new_function_name}' instead.\",\n            DeprecationWarning,\n            stacklevel=2\n        )\n        return paths\n\n    def get_launcher_action_paths(self) -&gt; list[str]:\n        \"\"\"Receive launcher actions paths.\n\n        Give addons ability to add launcher actions paths.\n\n        Returns:\n            list[str]: List of launcher action paths.\n\n        \"\"\"\n        return self._get_plugin_paths_by_type(\"actions\")\n\n    def get_create_plugin_paths(self, host_name: str) -&gt; list[str]:\n        \"\"\"Receive create plugin paths.\n\n        Give addons ability to add create plugin paths based on host name.\n\n        Notes:\n            Default implementation uses 'get_plugin_paths' and always return\n                all create plugin paths.\n\n        Args:\n            host_name (str): For which host are the plugins meant.\n\n        Returns:\n            list[str]: List of create plugin paths.\n\n        \"\"\"\n        return self._get_plugin_paths_by_type(\"create\")\n\n    def get_load_plugin_paths(self, host_name: str) -&gt; list[str]:\n        \"\"\"Receive load plugin paths.\n\n        Give addons ability to add load plugin paths based on host name.\n\n        Notes:\n            Default implementation uses 'get_plugin_paths' and always return\n                all load plugin paths.\n\n        Args:\n            host_name (str): For which host are the plugins meant.\n\n        Returns:\n            list[str]: List of load plugin paths.\n\n        \"\"\"\n        return self._get_plugin_paths_by_type(\"load\")\n\n    def get_publish_plugin_paths(self, host_name: str) -&gt; list[str]:\n        \"\"\"Receive publish plugin paths.\n\n        Give addons ability to add publish plugin paths based on host name.\n\n        Notes:\n           Default implementation uses 'get_plugin_paths' and always return\n               all publish plugin paths.\n\n        Args:\n           host_name (str): For which host are the plugins meant.\n\n        Returns:\n            list[str]: List of publish plugin paths.\n\n        \"\"\"\n        return self._get_plugin_paths_by_type(\"publish\")\n\n    def get_inventory_action_paths(self, host_name: str) -&gt; list[str]:\n        \"\"\"Receive inventory action paths.\n\n        Give addons ability to add inventory action plugin paths.\n\n        Notes:\n           Default implementation uses 'get_plugin_paths' and always return\n               all publish plugin paths.\n\n        Args:\n           host_name (str): For which host are the plugins meant.\n\n        Returns:\n            list[str]: List of inventory action plugin paths.\n\n        \"\"\"\n        return self._get_plugin_paths_by_type(\"inventory\")\n\n    def get_loader_action_plugin_paths(\n        self, host_name: Optional[str]\n    ) -&gt; list[str]:\n        \"\"\"Receive loader action plugin paths.\n\n        Args:\n            host_name (Optional[str]): Current host name.\n\n        Returns:\n            list[str]: Paths to loader action plugins.\n\n        \"\"\"\n        return []\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.IPluginPaths.get_create_plugin_paths","title":"<code>get_create_plugin_paths(host_name)</code>","text":"<p>Receive create plugin paths.</p> <p>Give addons ability to add create plugin paths based on host name.</p> Notes <p>Default implementation uses 'get_plugin_paths' and always return     all create plugin paths.</p> <p>Parameters:</p> Name Type Description Default <code>host_name</code> <code>str</code> <p>For which host are the plugins meant.</p> required <p>Returns:</p> Type Description <code>list[str]</code> <p>list[str]: List of create plugin paths.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>def get_create_plugin_paths(self, host_name: str) -&gt; list[str]:\n    \"\"\"Receive create plugin paths.\n\n    Give addons ability to add create plugin paths based on host name.\n\n    Notes:\n        Default implementation uses 'get_plugin_paths' and always return\n            all create plugin paths.\n\n    Args:\n        host_name (str): For which host are the plugins meant.\n\n    Returns:\n        list[str]: List of create plugin paths.\n\n    \"\"\"\n    return self._get_plugin_paths_by_type(\"create\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.IPluginPaths.get_inventory_action_paths","title":"<code>get_inventory_action_paths(host_name)</code>","text":"<p>Receive inventory action paths.</p> <p>Give addons ability to add inventory action plugin paths.</p> Notes <p>Default implementation uses 'get_plugin_paths' and always return     all publish plugin paths.</p> <p>Parameters:</p> Name Type Description Default <code>host_name</code> <code>str</code> <p>For which host are the plugins meant.</p> required <p>Returns:</p> Type Description <code>list[str]</code> <p>list[str]: List of inventory action plugin paths.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>def get_inventory_action_paths(self, host_name: str) -&gt; list[str]:\n    \"\"\"Receive inventory action paths.\n\n    Give addons ability to add inventory action plugin paths.\n\n    Notes:\n       Default implementation uses 'get_plugin_paths' and always return\n           all publish plugin paths.\n\n    Args:\n       host_name (str): For which host are the plugins meant.\n\n    Returns:\n        list[str]: List of inventory action plugin paths.\n\n    \"\"\"\n    return self._get_plugin_paths_by_type(\"inventory\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.IPluginPaths.get_launcher_action_paths","title":"<code>get_launcher_action_paths()</code>","text":"<p>Receive launcher actions paths.</p> <p>Give addons ability to add launcher actions paths.</p> <p>Returns:</p> Type Description <code>list[str]</code> <p>list[str]: List of launcher action paths.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>def get_launcher_action_paths(self) -&gt; list[str]:\n    \"\"\"Receive launcher actions paths.\n\n    Give addons ability to add launcher actions paths.\n\n    Returns:\n        list[str]: List of launcher action paths.\n\n    \"\"\"\n    return self._get_plugin_paths_by_type(\"actions\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.IPluginPaths.get_load_plugin_paths","title":"<code>get_load_plugin_paths(host_name)</code>","text":"<p>Receive load plugin paths.</p> <p>Give addons ability to add load plugin paths based on host name.</p> Notes <p>Default implementation uses 'get_plugin_paths' and always return     all load plugin paths.</p> <p>Parameters:</p> Name Type Description Default <code>host_name</code> <code>str</code> <p>For which host are the plugins meant.</p> required <p>Returns:</p> Type Description <code>list[str]</code> <p>list[str]: List of load plugin paths.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>def get_load_plugin_paths(self, host_name: str) -&gt; list[str]:\n    \"\"\"Receive load plugin paths.\n\n    Give addons ability to add load plugin paths based on host name.\n\n    Notes:\n        Default implementation uses 'get_plugin_paths' and always return\n            all load plugin paths.\n\n    Args:\n        host_name (str): For which host are the plugins meant.\n\n    Returns:\n        list[str]: List of load plugin paths.\n\n    \"\"\"\n    return self._get_plugin_paths_by_type(\"load\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.IPluginPaths.get_loader_action_plugin_paths","title":"<code>get_loader_action_plugin_paths(host_name)</code>","text":"<p>Receive loader action plugin paths.</p> <p>Parameters:</p> Name Type Description Default <code>host_name</code> <code>Optional[str]</code> <p>Current host name.</p> required <p>Returns:</p> Type Description <code>list[str]</code> <p>list[str]: Paths to loader action plugins.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>def get_loader_action_plugin_paths(\n    self, host_name: Optional[str]\n) -&gt; list[str]:\n    \"\"\"Receive loader action plugin paths.\n\n    Args:\n        host_name (Optional[str]): Current host name.\n\n    Returns:\n        list[str]: Paths to loader action plugins.\n\n    \"\"\"\n    return []\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.IPluginPaths.get_plugin_paths","title":"<code>get_plugin_paths()</code>","text":"<p>Return plugin paths for addon.</p> <p>This method was abstract (required) in the past, so raise the required     'core' addon version when 'get_plugin_paths' is removed from     addon.</p> Deprecated <p>Please implement specific methods 'get_create_plugin_paths',     'get_load_plugin_paths', 'get_inventory_action_paths' and     'get_publish_plugin_paths' to return plugin paths.</p> <p>Returns:</p> Type Description <code>dict[str, list[str]]</code> <p>dict[str, list[str]]: Plugin paths for addon.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>def get_plugin_paths(self) -&gt; dict[str, list[str]]:\n    \"\"\"Return plugin paths for addon.\n\n    This method was abstract (required) in the past, so raise the required\n        'core' addon version when 'get_plugin_paths' is removed from\n        addon.\n\n    Deprecated:\n        Please implement specific methods 'get_create_plugin_paths',\n            'get_load_plugin_paths', 'get_inventory_action_paths' and\n            'get_publish_plugin_paths' to return plugin paths.\n\n    Returns:\n        dict[str, list[str]]: Plugin paths for addon.\n\n    \"\"\"\n    return {}\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.IPluginPaths.get_publish_plugin_paths","title":"<code>get_publish_plugin_paths(host_name)</code>","text":"<p>Receive publish plugin paths.</p> <p>Give addons ability to add publish plugin paths based on host name.</p> Notes <p>Default implementation uses 'get_plugin_paths' and always return     all publish plugin paths.</p> <p>Parameters:</p> Name Type Description Default <code>host_name</code> <code>str</code> <p>For which host are the plugins meant.</p> required <p>Returns:</p> Type Description <code>list[str]</code> <p>list[str]: List of publish plugin paths.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>def get_publish_plugin_paths(self, host_name: str) -&gt; list[str]:\n    \"\"\"Receive publish plugin paths.\n\n    Give addons ability to add publish plugin paths based on host name.\n\n    Notes:\n       Default implementation uses 'get_plugin_paths' and always return\n           all publish plugin paths.\n\n    Args:\n       host_name (str): For which host are the plugins meant.\n\n    Returns:\n        list[str]: List of publish plugin paths.\n\n    \"\"\"\n    return self._get_plugin_paths_by_type(\"publish\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.ITraits","title":"<code>ITraits</code>","text":"<p>               Bases: <code>AYONInterface</code></p> <p>Interface for traits.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>class ITraits(AYONInterface):\n    \"\"\"Interface for traits.\"\"\"\n\n    @abstractmethod\n    def get_addon_traits(self) -&gt; list[Type[TraitBase]]:\n        \"\"\"Get trait classes for the addon.\n\n        Returns:\n            list[Type[TraitBase]]: Traits for the addon.\n\n        \"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.ITraits.get_addon_traits","title":"<code>get_addon_traits()</code>  <code>abstractmethod</code>","text":"<p>Get trait classes for the addon.</p> <p>Returns:</p> Type Description <code>list[Type[TraitBase]]</code> <p>list[Type[TraitBase]]: Traits for the addon.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>@abstractmethod\ndef get_addon_traits(self) -&gt; list[Type[TraitBase]]:\n    \"\"\"Get trait classes for the addon.\n\n    Returns:\n        list[Type[TraitBase]]: Traits for the addon.\n\n    \"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.ITrayAction","title":"<code>ITrayAction</code>","text":"<p>               Bases: <code>ITrayAddon</code></p> <p>Implementation of Tray action.</p> <p>Add action to tray menu which will trigger <code>on_action_trigger</code>. It is expected to be used for showing tools.</p> <p>Methods <code>tray_start</code>, <code>tray_exit</code> and <code>connect_with_addons</code> are overridden as it's not expected that action will use them. But it is possible if necessary.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>class ITrayAction(ITrayAddon):\n    \"\"\"Implementation of Tray action.\n\n    Add action to tray menu which will trigger `on_action_trigger`.\n    It is expected to be used for showing tools.\n\n    Methods `tray_start`, `tray_exit` and `connect_with_addons` are overridden\n    as it's not expected that action will use them. But it is possible if\n    necessary.\n    \"\"\"\n\n    admin_action = False\n    _action_item = None\n\n    @property\n    @abstractmethod\n    def label(self) -&gt; str:\n        \"\"\"Service label showed in menu.\"\"\"\n\n    @abstractmethod\n    def on_action_trigger(self) -&gt; None:\n        \"\"\"What happens on actions click.\"\"\"\n\n    def tray_menu(self, tray_menu: QtWidgets.QMenu) -&gt; None:\n        \"\"\"Add action to tray menu.\"\"\"\n        from qtpy import QtWidgets\n\n        if self.admin_action:\n            action = self.add_action_to_admin_submenu(self.label, tray_menu)\n        else:\n            action = QtWidgets.QAction(self.label, tray_menu)\n            tray_menu.addAction(action)\n\n        action.triggered.connect(self.on_action_trigger)\n        self._action_item = action\n\n    def tray_start(self) -&gt; None:  # noqa: PLR6301\n        \"\"\"Start procedure in tray tool.\"\"\"\n        return\n\n    def tray_exit(self) -&gt; None:  # noqa: PLR6301\n        \"\"\"Cleanup method which is executed on tray shutdown.\"\"\"\n        return\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.ITrayAction.label","title":"<code>label</code>  <code>abstractmethod</code> <code>property</code>","text":"<p>Service label showed in menu.</p>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.ITrayAction.on_action_trigger","title":"<code>on_action_trigger()</code>  <code>abstractmethod</code>","text":"<p>What happens on actions click.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>@abstractmethod\ndef on_action_trigger(self) -&gt; None:\n    \"\"\"What happens on actions click.\"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.ITrayAction.tray_exit","title":"<code>tray_exit()</code>","text":"<p>Cleanup method which is executed on tray shutdown.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>def tray_exit(self) -&gt; None:  # noqa: PLR6301\n    \"\"\"Cleanup method which is executed on tray shutdown.\"\"\"\n    return\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.ITrayAction.tray_menu","title":"<code>tray_menu(tray_menu)</code>","text":"<p>Add action to tray menu.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>def tray_menu(self, tray_menu: QtWidgets.QMenu) -&gt; None:\n    \"\"\"Add action to tray menu.\"\"\"\n    from qtpy import QtWidgets\n\n    if self.admin_action:\n        action = self.add_action_to_admin_submenu(self.label, tray_menu)\n    else:\n        action = QtWidgets.QAction(self.label, tray_menu)\n        tray_menu.addAction(action)\n\n    action.triggered.connect(self.on_action_trigger)\n    self._action_item = action\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.ITrayAction.tray_start","title":"<code>tray_start()</code>","text":"<p>Start procedure in tray tool.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>def tray_start(self) -&gt; None:  # noqa: PLR6301\n    \"\"\"Start procedure in tray tool.\"\"\"\n    return\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.ITrayAddon","title":"<code>ITrayAddon</code>","text":"<p>               Bases: <code>AYONInterface</code></p> <p>Addon has special procedures when used in Tray tool.</p> Important <p>The addon. still must be usable if is not used in tray even if it would do nothing.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>class ITrayAddon(AYONInterface):\n    \"\"\"Addon has special procedures when used in Tray tool.\n\n    Important:\n        The addon. still must be usable if is not used in tray even if it\n        would do nothing.\n\n    \"\"\"\n    manager: AddonsManager\n    tray_initialized = False\n    _tray_manager: TrayManager = None\n    _admin_submenu = None\n\n    @abstractmethod\n    def tray_init(self) -&gt; None:\n        \"\"\"Initialization part of tray implementation.\n\n        Triggered between `initialization` and `connect_with_addons`.\n\n        This is where GUIs should be loaded or tray specific parts should be\n        prepared\n\n        \"\"\"\n\n    @abstractmethod\n    def tray_menu(self, tray_menu: QtWidgets.QMenu) -&gt; None:\n        \"\"\"Add addon's action to tray menu.\"\"\"\n\n    @abstractmethod\n    def tray_start(self) -&gt; None:\n        \"\"\"Start procedure in tray tool.\"\"\"\n\n    @abstractmethod\n    def tray_exit(self) -&gt; None:\n        \"\"\"Cleanup method which is executed on tray shutdown.\n\n        This is place where all threads should be shut.\n\n        \"\"\"\n\n    def execute_in_main_thread(self, callback: Callable) -&gt; None:\n        \"\"\"Pushes callback to the queue or process 'callback' on a main thread.\n\n        Some callbacks need to be processed on main thread (menu actions\n        must be added on main thread else they won't get triggered etc.)\n\n        Args:\n            callback (Callable): Function to be executed on main thread\n\n        \"\"\"\n        if not self.tray_initialized:\n            # TODO (Illicit): Called without initialized tray, still\n            #   main thread needed.\n            try:\n                callback()\n\n            except Exception:  # noqa: BLE001\n                self.log.warning(\n                    \"Failed to execute %s callback in main thread\",\n                    str(callback), exc_info=True)\n\n            return\n        self._tray_manager.tray_manager.execute_in_main_thread(callback)\n\n    def show_tray_message(\n            self,\n            title: str,\n            message: str,\n            icon: Optional[QtWidgets.QSystemTrayIcon] = None,\n            msecs: Optional[int] = None) -&gt; None:\n        \"\"\"Show tray message.\n\n        Args:\n            title (str): Title of message.\n            message (str): Content of message.\n            icon (QSystemTrayIcon.MessageIcon): Message's icon. Default is\n                Information icon, may differ by Qt version.\n            msecs (int): Duration of message visibility in milliseconds.\n                Default is 10000 msecs, may differ by Qt version.\n        \"\"\"\n        if self._tray_manager:\n            self._tray_manager.show_tray_message(title, message, icon, msecs)\n\n    def add_doubleclick_callback(self, callback: Callable) -&gt; None:\n        \"\"\"Add callback to be triggered on tray icon double click.\"\"\"\n        if hasattr(self.manager, \"add_doubleclick_callback\"):\n            self.manager.add_doubleclick_callback(self, callback)\n\n    @staticmethod\n    def admin_submenu(tray_menu: QtWidgets.QMenu) -&gt; QtWidgets.QMenu:\n        \"\"\"Get or create admin submenu.\n\n        Returns:\n            QtWidgets.QMenu: Admin submenu.\n\n        \"\"\"\n        if ITrayAddon._admin_submenu is None:\n            from qtpy import QtWidgets\n\n            admin_submenu = QtWidgets.QMenu(\"Admin\", tray_menu)\n            admin_submenu.menuAction().setVisible(False)\n            ITrayAddon._admin_submenu = admin_submenu\n        return ITrayAddon._admin_submenu\n\n    @staticmethod\n    def add_action_to_admin_submenu(\n            label: str, tray_menu: QtWidgets.QMenu) -&gt; QtWidgets.QAction:\n        \"\"\"Add action to admin submenu.\n\n        Args:\n            label (str): Label of action.\n            tray_menu (QtWidgets.QMenu): Tray menu to add action to.\n\n        Returns:\n            QtWidgets.QAction: Action added to admin submenu\n\n        \"\"\"\n        from qtpy import QtWidgets\n\n        menu = ITrayAddon.admin_submenu(tray_menu)\n        action = QtWidgets.QAction(label, menu)\n        menu.addAction(action)\n        if not menu.menuAction().isVisible():\n            menu.menuAction().setVisible(True)\n        return action\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.ITrayAddon.add_action_to_admin_submenu","title":"<code>add_action_to_admin_submenu(label, tray_menu)</code>  <code>staticmethod</code>","text":"<p>Add action to admin submenu.</p> <p>Parameters:</p> Name Type Description Default <code>label</code> <code>str</code> <p>Label of action.</p> required <code>tray_menu</code> <code>QMenu</code> <p>Tray menu to add action to.</p> required <p>Returns:</p> Type Description <code>QAction</code> <p>QtWidgets.QAction: Action added to admin submenu</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>@staticmethod\ndef add_action_to_admin_submenu(\n        label: str, tray_menu: QtWidgets.QMenu) -&gt; QtWidgets.QAction:\n    \"\"\"Add action to admin submenu.\n\n    Args:\n        label (str): Label of action.\n        tray_menu (QtWidgets.QMenu): Tray menu to add action to.\n\n    Returns:\n        QtWidgets.QAction: Action added to admin submenu\n\n    \"\"\"\n    from qtpy import QtWidgets\n\n    menu = ITrayAddon.admin_submenu(tray_menu)\n    action = QtWidgets.QAction(label, menu)\n    menu.addAction(action)\n    if not menu.menuAction().isVisible():\n        menu.menuAction().setVisible(True)\n    return action\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.ITrayAddon.add_doubleclick_callback","title":"<code>add_doubleclick_callback(callback)</code>","text":"<p>Add callback to be triggered on tray icon double click.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>def add_doubleclick_callback(self, callback: Callable) -&gt; None:\n    \"\"\"Add callback to be triggered on tray icon double click.\"\"\"\n    if hasattr(self.manager, \"add_doubleclick_callback\"):\n        self.manager.add_doubleclick_callback(self, callback)\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.ITrayAddon.admin_submenu","title":"<code>admin_submenu(tray_menu)</code>  <code>staticmethod</code>","text":"<p>Get or create admin submenu.</p> <p>Returns:</p> Type Description <code>QMenu</code> <p>QtWidgets.QMenu: Admin submenu.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>@staticmethod\ndef admin_submenu(tray_menu: QtWidgets.QMenu) -&gt; QtWidgets.QMenu:\n    \"\"\"Get or create admin submenu.\n\n    Returns:\n        QtWidgets.QMenu: Admin submenu.\n\n    \"\"\"\n    if ITrayAddon._admin_submenu is None:\n        from qtpy import QtWidgets\n\n        admin_submenu = QtWidgets.QMenu(\"Admin\", tray_menu)\n        admin_submenu.menuAction().setVisible(False)\n        ITrayAddon._admin_submenu = admin_submenu\n    return ITrayAddon._admin_submenu\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.ITrayAddon.execute_in_main_thread","title":"<code>execute_in_main_thread(callback)</code>","text":"<p>Pushes callback to the queue or process 'callback' on a main thread.</p> <p>Some callbacks need to be processed on main thread (menu actions must be added on main thread else they won't get triggered etc.)</p> <p>Parameters:</p> Name Type Description Default <code>callback</code> <code>Callable</code> <p>Function to be executed on main thread</p> required Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>def execute_in_main_thread(self, callback: Callable) -&gt; None:\n    \"\"\"Pushes callback to the queue or process 'callback' on a main thread.\n\n    Some callbacks need to be processed on main thread (menu actions\n    must be added on main thread else they won't get triggered etc.)\n\n    Args:\n        callback (Callable): Function to be executed on main thread\n\n    \"\"\"\n    if not self.tray_initialized:\n        # TODO (Illicit): Called without initialized tray, still\n        #   main thread needed.\n        try:\n            callback()\n\n        except Exception:  # noqa: BLE001\n            self.log.warning(\n                \"Failed to execute %s callback in main thread\",\n                str(callback), exc_info=True)\n\n        return\n    self._tray_manager.tray_manager.execute_in_main_thread(callback)\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.ITrayAddon.show_tray_message","title":"<code>show_tray_message(title, message, icon=None, msecs=None)</code>","text":"<p>Show tray message.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <code>str</code> <p>Title of message.</p> required <code>message</code> <code>str</code> <p>Content of message.</p> required <code>icon</code> <code>MessageIcon</code> <p>Message's icon. Default is Information icon, may differ by Qt version.</p> <code>None</code> <code>msecs</code> <code>int</code> <p>Duration of message visibility in milliseconds. Default is 10000 msecs, may differ by Qt version.</p> <code>None</code> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>def show_tray_message(\n        self,\n        title: str,\n        message: str,\n        icon: Optional[QtWidgets.QSystemTrayIcon] = None,\n        msecs: Optional[int] = None) -&gt; None:\n    \"\"\"Show tray message.\n\n    Args:\n        title (str): Title of message.\n        message (str): Content of message.\n        icon (QSystemTrayIcon.MessageIcon): Message's icon. Default is\n            Information icon, may differ by Qt version.\n        msecs (int): Duration of message visibility in milliseconds.\n            Default is 10000 msecs, may differ by Qt version.\n    \"\"\"\n    if self._tray_manager:\n        self._tray_manager.show_tray_message(title, message, icon, msecs)\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.ITrayAddon.tray_exit","title":"<code>tray_exit()</code>  <code>abstractmethod</code>","text":"<p>Cleanup method which is executed on tray shutdown.</p> <p>This is place where all threads should be shut.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>@abstractmethod\ndef tray_exit(self) -&gt; None:\n    \"\"\"Cleanup method which is executed on tray shutdown.\n\n    This is place where all threads should be shut.\n\n    \"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.ITrayAddon.tray_init","title":"<code>tray_init()</code>  <code>abstractmethod</code>","text":"<p>Initialization part of tray implementation.</p> <p>Triggered between <code>initialization</code> and <code>connect_with_addons</code>.</p> <p>This is where GUIs should be loaded or tray specific parts should be prepared</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>@abstractmethod\ndef tray_init(self) -&gt; None:\n    \"\"\"Initialization part of tray implementation.\n\n    Triggered between `initialization` and `connect_with_addons`.\n\n    This is where GUIs should be loaded or tray specific parts should be\n    prepared\n\n    \"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.ITrayAddon.tray_menu","title":"<code>tray_menu(tray_menu)</code>  <code>abstractmethod</code>","text":"<p>Add addon's action to tray menu.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>@abstractmethod\ndef tray_menu(self, tray_menu: QtWidgets.QMenu) -&gt; None:\n    \"\"\"Add addon's action to tray menu.\"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.ITrayAddon.tray_start","title":"<code>tray_start()</code>  <code>abstractmethod</code>","text":"<p>Start procedure in tray tool.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>@abstractmethod\ndef tray_start(self) -&gt; None:\n    \"\"\"Start procedure in tray tool.\"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.ITrayService","title":"<code>ITrayService</code>","text":"<p>               Bases: <code>ITrayAddon</code></p> <p>Tray service Interface.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>class ITrayService(ITrayAddon):\n    \"\"\"Tray service Interface.\"\"\"\n    # Module's property\n    menu_action: QtWidgets.QAction = None\n\n    # Class properties\n    _services_submenu: QtWidgets.QMenu = None\n    _icon_failed: QtWidgets.QIcon = None\n    _icon_running: QtWidgets.QIcon = None\n    _icon_idle: QtWidgets.QIcon = None\n\n    @property\n    @abstractmethod\n    def label(self) -&gt; str:\n        \"\"\"Service label showed in menu.\"\"\"\n\n    # TODO (Illicit): be able to get any sort of information to show/print\n    # @abstractmethod\n    # def get_service_info(self):\n    #     pass\n\n    @staticmethod\n    def services_submenu(tray_menu: QtWidgets.QMenu) -&gt; QtWidgets.QMenu:\n        \"\"\"Get or create services submenu.\n\n        Returns:\n            QtWidgets.QMenu: Services submenu.\n\n        \"\"\"\n        if ITrayService._services_submenu is None:\n            from qtpy import QtWidgets\n\n            services_submenu = QtWidgets.QMenu(\"Services\", tray_menu)\n            services_submenu.menuAction().setVisible(False)\n            ITrayService._services_submenu = services_submenu\n        return ITrayService._services_submenu\n\n    @staticmethod\n    def add_service_action(action: QtWidgets.QAction) -&gt; None:\n        \"\"\"Add service action to services submenu.\"\"\"\n        ITrayService._services_submenu.addAction(action)\n        if not ITrayService._services_submenu.menuAction().isVisible():\n            ITrayService._services_submenu.menuAction().setVisible(True)\n\n    @staticmethod\n    def _load_service_icons() -&gt; None:\n        \"\"\"Load service icons.\"\"\"\n        from qtpy import QtGui\n\n        ITrayService._failed_icon = QtGui.QIcon(\n            resources.get_resource(\"icons\", \"circle_red.png\")\n        )\n        ITrayService._icon_running = QtGui.QIcon(\n            resources.get_resource(\"icons\", \"circle_green.png\")\n        )\n        ITrayService._icon_idle = QtGui.QIcon(\n            resources.get_resource(\"icons\", \"circle_orange.png\")\n        )\n\n    @staticmethod\n    def get_icon_running() -&gt; QtWidgets.QIcon:\n        \"\"\"Get running icon.\n\n        Returns:\n            QtWidgets.QIcon: Returns \"running\" icon.\n\n        \"\"\"\n        if ITrayService._icon_running is None:\n            ITrayService._load_service_icons()\n        return ITrayService._icon_running\n\n    @staticmethod\n    def get_icon_idle() -&gt; QtWidgets.QIcon:\n        \"\"\"Get idle icon.\n\n        Returns:\n            QtWidgets.QIcon: Returns \"idle\" icon.\n\n        \"\"\"\n        if ITrayService._icon_idle is None:\n            ITrayService._load_service_icons()\n        return ITrayService._icon_idle\n\n    @staticmethod\n    def get_icon_failed() -&gt; QtWidgets.QIcon:\n        \"\"\"Get failed icon.\n\n        Returns:\n            QtWidgets.QIcon: Returns \"failed\" icon.\n\n        \"\"\"\n        if ITrayService._icon_failed is None:\n            ITrayService._load_service_icons()\n        return ITrayService._icon_failed\n\n    def tray_menu(self, tray_menu: QtWidgets.QMenu) -&gt; None:\n        \"\"\"Add service to tray menu.\"\"\"\n        from qtpy import QtWidgets\n\n        action = QtWidgets.QAction(\n            self.label,\n            self.services_submenu(tray_menu)\n        )\n        self.menu_action = action\n\n        self.add_service_action(action)\n\n        self.set_service_running_icon()\n\n    def set_service_running_icon(self) -&gt; None:\n        \"\"\"Change icon of an QAction to green circle.\"\"\"\n        if self.menu_action:\n            self.menu_action.setIcon(self.get_icon_running())\n\n    def set_service_failed_icon(self) -&gt; None:\n        \"\"\"Change icon of an QAction to red circle.\"\"\"\n        if self.menu_action:\n            self.menu_action.setIcon(self.get_icon_failed())\n\n    def set_service_idle_icon(self) -&gt; None:\n        \"\"\"Change icon of an QAction to orange circle.\"\"\"\n        if self.menu_action:\n            self.menu_action.setIcon(self.get_icon_idle())\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.ITrayService.label","title":"<code>label</code>  <code>abstractmethod</code> <code>property</code>","text":"<p>Service label showed in menu.</p>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.ITrayService.add_service_action","title":"<code>add_service_action(action)</code>  <code>staticmethod</code>","text":"<p>Add service action to services submenu.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>@staticmethod\ndef add_service_action(action: QtWidgets.QAction) -&gt; None:\n    \"\"\"Add service action to services submenu.\"\"\"\n    ITrayService._services_submenu.addAction(action)\n    if not ITrayService._services_submenu.menuAction().isVisible():\n        ITrayService._services_submenu.menuAction().setVisible(True)\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.ITrayService.get_icon_failed","title":"<code>get_icon_failed()</code>  <code>staticmethod</code>","text":"<p>Get failed icon.</p> <p>Returns:</p> Type Description <code>QIcon</code> <p>QtWidgets.QIcon: Returns \"failed\" icon.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>@staticmethod\ndef get_icon_failed() -&gt; QtWidgets.QIcon:\n    \"\"\"Get failed icon.\n\n    Returns:\n        QtWidgets.QIcon: Returns \"failed\" icon.\n\n    \"\"\"\n    if ITrayService._icon_failed is None:\n        ITrayService._load_service_icons()\n    return ITrayService._icon_failed\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.ITrayService.get_icon_idle","title":"<code>get_icon_idle()</code>  <code>staticmethod</code>","text":"<p>Get idle icon.</p> <p>Returns:</p> Type Description <code>QIcon</code> <p>QtWidgets.QIcon: Returns \"idle\" icon.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>@staticmethod\ndef get_icon_idle() -&gt; QtWidgets.QIcon:\n    \"\"\"Get idle icon.\n\n    Returns:\n        QtWidgets.QIcon: Returns \"idle\" icon.\n\n    \"\"\"\n    if ITrayService._icon_idle is None:\n        ITrayService._load_service_icons()\n    return ITrayService._icon_idle\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.ITrayService.get_icon_running","title":"<code>get_icon_running()</code>  <code>staticmethod</code>","text":"<p>Get running icon.</p> <p>Returns:</p> Type Description <code>QIcon</code> <p>QtWidgets.QIcon: Returns \"running\" icon.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>@staticmethod\ndef get_icon_running() -&gt; QtWidgets.QIcon:\n    \"\"\"Get running icon.\n\n    Returns:\n        QtWidgets.QIcon: Returns \"running\" icon.\n\n    \"\"\"\n    if ITrayService._icon_running is None:\n        ITrayService._load_service_icons()\n    return ITrayService._icon_running\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.ITrayService.services_submenu","title":"<code>services_submenu(tray_menu)</code>  <code>staticmethod</code>","text":"<p>Get or create services submenu.</p> <p>Returns:</p> Type Description <code>QMenu</code> <p>QtWidgets.QMenu: Services submenu.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>@staticmethod\ndef services_submenu(tray_menu: QtWidgets.QMenu) -&gt; QtWidgets.QMenu:\n    \"\"\"Get or create services submenu.\n\n    Returns:\n        QtWidgets.QMenu: Services submenu.\n\n    \"\"\"\n    if ITrayService._services_submenu is None:\n        from qtpy import QtWidgets\n\n        services_submenu = QtWidgets.QMenu(\"Services\", tray_menu)\n        services_submenu.menuAction().setVisible(False)\n        ITrayService._services_submenu = services_submenu\n    return ITrayService._services_submenu\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.ITrayService.set_service_failed_icon","title":"<code>set_service_failed_icon()</code>","text":"<p>Change icon of an QAction to red circle.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>def set_service_failed_icon(self) -&gt; None:\n    \"\"\"Change icon of an QAction to red circle.\"\"\"\n    if self.menu_action:\n        self.menu_action.setIcon(self.get_icon_failed())\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.ITrayService.set_service_idle_icon","title":"<code>set_service_idle_icon()</code>","text":"<p>Change icon of an QAction to orange circle.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>def set_service_idle_icon(self) -&gt; None:\n    \"\"\"Change icon of an QAction to orange circle.\"\"\"\n    if self.menu_action:\n        self.menu_action.setIcon(self.get_icon_idle())\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.ITrayService.set_service_running_icon","title":"<code>set_service_running_icon()</code>","text":"<p>Change icon of an QAction to green circle.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>def set_service_running_icon(self) -&gt; None:\n    \"\"\"Change icon of an QAction to green circle.\"\"\"\n    if self.menu_action:\n        self.menu_action.setIcon(self.get_icon_running())\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.ITrayService.tray_menu","title":"<code>tray_menu(tray_menu)</code>","text":"<p>Add service to tray menu.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>def tray_menu(self, tray_menu: QtWidgets.QMenu) -&gt; None:\n    \"\"\"Add service to tray menu.\"\"\"\n    from qtpy import QtWidgets\n\n    action = QtWidgets.QAction(\n        self.label,\n        self.services_submenu(tray_menu)\n    )\n    self.menu_action = action\n\n    self.add_service_action(action)\n\n    self.set_service_running_icon()\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.ProcessContext","title":"<code>ProcessContext</code>","text":"<p>Hold context of process that is going to be started.</p> <p>Right now the context is simple, having information about addon that wants     to trigger preparation and possibly project name for which it should     happen.</p> <p>Preparation for process can be required for ayon-core or any other addon.     It can be, change of environment variables, or request login to     a project management.</p> <p>At the moment of creation is 'ProcessContext' only data holder, but that     might change in future if there will be need.</p> <p>Parameters:</p> Name Type Description Default <code>addon_name</code> <code>str</code> <p>Addon name which triggered process.</p> required <code>addon_version</code> <code>str</code> <p>Addon version which triggered process.</p> required <code>project_name</code> <code>str | None</code> <p>Project name. Can be filled in case process is triggered for specific project. Some addons can have different behavior based on project. Value is NOT autofilled.</p> <code>None</code> <code>headless</code> <code>bool | None</code> <p>Is process running in headless mode. Value is filled with value based on state set in AYON launcher.</p> <code>None</code> Source code in <code>client/ayon_core/addon/base.py</code> <pre><code>class ProcessContext:\n    \"\"\"Hold context of process that is going to be started.\n\n    Right now the context is simple, having information about addon that wants\n        to trigger preparation and possibly project name for which it should\n        happen.\n\n    Preparation for process can be required for ayon-core or any other addon.\n        It can be, change of environment variables, or request login to\n        a project management.\n\n    At the moment of creation is 'ProcessContext' only data holder, but that\n        might change in future if there will be need.\n\n    Args:\n        addon_name (str): Addon name which triggered process.\n        addon_version (str): Addon version which triggered process.\n        project_name (str | None): Project name. Can be filled in case\n            process is triggered for specific project. Some addons can have\n            different behavior based on project. Value is NOT autofilled.\n        headless (bool | None): Is process running in headless mode. Value\n            is filled with value based on state set in AYON launcher.\n\n    \"\"\"\n    def __init__(\n        self,\n        addon_name: str,\n        addon_version: str,\n        project_name: str | None = None,\n        headless: bool | None = None,\n        **kwargs,\n    ):\n        if headless is None:\n            headless = is_headless_mode_enabled()\n        self.addon_name: str = addon_name\n        self.addon_version: str = addon_version\n        self.project_name: str | None = project_name\n        self.headless: bool = headless\n\n        if kwargs:\n            unknown_keys = \", \".join([f'\"{key}\"' for key in kwargs.keys()])\n            print(f\"Unknown keys in ProcessContext: {unknown_keys}\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.ProcessPreparationError","title":"<code>ProcessPreparationError</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Exception that can be used when process preparation failed.</p> <p>The message is shown to user (either as UI dialog or printed). If     different error is raised a \"generic\" error message is shown to user     with option to copy error message to clipboard.</p> Source code in <code>client/ayon_core/addon/base.py</code> <pre><code>class ProcessPreparationError(Exception):\n    \"\"\"Exception that can be used when process preparation failed.\n\n    The message is shown to user (either as UI dialog or printed). If\n        different error is raised a \"generic\" error message is shown to user\n        with option to copy error message to clipboard.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.ensure_addons_are_process_context_ready","title":"<code>ensure_addons_are_process_context_ready(process_context, addons_manager=None, exit_on_failure=True)</code>","text":"<p>Ensure all enabled addons are ready to be used in the given context.</p> <p>Call this method only in AYON launcher process and as first thing     to avoid possible clashes with preparation. For example 'QApplication'     should not be created.</p> Todos <p>Run all preparations and allow to \"ignore\" failed preparations.     Right now single addon can block using certain actions.</p> <p>Parameters:</p> Name Type Description Default <code>process_context</code> <code>ProcessContext</code> <p>The context in which the addons should be prepared.</p> required <code>addons_manager</code> <code>Optional[AddonsManager]</code> <p>The addons manager to use. If not provided, a new one will be created.</p> <code>None</code> <code>exit_on_failure</code> <code>bool</code> <p>If True, the process will exit if an error occurs. Defaults to True.</p> <code>True</code> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if all addons are ready, False otherwise.</p> Source code in <code>client/ayon_core/addon/utils.py</code> <pre><code>def ensure_addons_are_process_context_ready(\n    process_context: ProcessContext,\n    addons_manager: Optional[AddonsManager] = None,\n    exit_on_failure: bool = True,\n) -&gt; bool:\n    \"\"\"Ensure all enabled addons are ready to be used in the given context.\n\n    Call this method only in AYON launcher process and as first thing\n        to avoid possible clashes with preparation. For example 'QApplication'\n        should not be created.\n\n    Todos:\n        Run all preparations and allow to \"ignore\" failed preparations.\n            Right now single addon can block using certain actions.\n\n    Args:\n        process_context (ProcessContext): The context in which the\n            addons should be prepared.\n        addons_manager (Optional[AddonsManager]): The addons\n            manager to use. If not provided, a new one will be created.\n        exit_on_failure (bool, optional): If True, the process will exit\n            if an error occurs. Defaults to True.\n\n    Returns:\n        bool: True if all addons are ready, False otherwise.\n\n    \"\"\"\n    if addons_manager is None:\n        addons_manager = AddonsManager()\n\n    message = None\n    failed = False\n    use_detail = False\n    # Wrap the output in StringIO to capture it for details on fail\n    # - but in case stdout was invalid on start of process also store\n    #   the tracebacks\n    tracebacks = []\n    output = StringIO()\n    with contextlib.redirect_stdout(output):\n        with contextlib.redirect_stderr(output):\n            for addon in addons_manager.get_enabled_addons():\n                addon_failed = True\n                try:\n                    addon.ensure_is_process_ready(process_context)\n                    addon_failed = False\n                except ProcessPreparationError as exc:\n                    message = str(exc)\n                    print(f\"Addon preparation failed: '{addon.name}'\")\n                    print(message)\n\n                except BaseException:\n                    use_detail = True\n                    message = \"An unexpected error occurred.\"\n                    formatted_traceback = \"\".join(traceback.format_exception(\n                        *sys.exc_info()\n                    ))\n                    tracebacks.append(formatted_traceback)\n                    print(f\"Addon preparation failed: '{addon.name}'\")\n                    print(message)\n                    # Print the traceback so it is in the stdout\n                    print(formatted_traceback)\n\n                if addon_failed:\n                    failed = True\n                    break\n\n    output_str = output.getvalue()\n    # Print stdout/stderr to console as it was redirected\n    print(output_str)\n    if not failed:\n        if not process_context.headless:\n            _start_tray()\n        return True\n\n    detail = None\n    if use_detail:\n        # In case stdout was not captured, use the tracebacks as detail\n        if not output_str:\n            output_str = \"\\n\".join(tracebacks)\n        detail = output_str\n\n    _handle_error(process_context, message, detail)\n    if exit_on_failure:\n        sys.exit(1)\n    return False\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.ensure_addons_are_process_ready","title":"<code>ensure_addons_are_process_ready(addon_name, addon_version, project_name=None, headless=None, *, addons_manager=None, exit_on_failure=True, **kwargs)</code>","text":"<p>Ensure all enabled addons are ready to be used in the given context.</p> <p>Call this method only in AYON launcher process and as first thing     to avoid possible clashes with preparation. For example 'QApplication'     should not be created.</p> <p>Parameters:</p> Name Type Description Default <code>addon_name</code> <code>str</code> <p>Addon name which triggered process.</p> required <code>addon_version</code> <code>str</code> <p>Addon version which triggered process.</p> required <code>project_name</code> <code>Optional[str]</code> <p>Project name. Can be filled in case process is triggered for specific project. Some addons can have different behavior based on project. Value is NOT autofilled.</p> <code>None</code> <code>headless</code> <code>Optional[bool]</code> <p>Is process running in headless mode. Value is filled with value based on state set in AYON launcher.</p> <code>None</code> <code>addons_manager</code> <code>Optional[AddonsManager]</code> <p>The addons manager to use. If not provided, a new one will be created.</p> <code>None</code> <code>exit_on_failure</code> <code>bool</code> <p>If True, the process will exit if an error occurs. Defaults to True.</p> <code>True</code> <code>kwargs</code> <p>The keyword arguments to pass to the ProcessContext.</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if all addons are ready, False otherwise.</p> Source code in <code>client/ayon_core/addon/utils.py</code> <pre><code>def ensure_addons_are_process_ready(\n    addon_name: str,\n    addon_version: str,\n    project_name: Optional[str] = None,\n    headless: Optional[bool] = None,\n    *,\n    addons_manager: Optional[AddonsManager] = None,\n    exit_on_failure: bool = True,\n    **kwargs,\n) -&gt; bool:\n    \"\"\"Ensure all enabled addons are ready to be used in the given context.\n\n    Call this method only in AYON launcher process and as first thing\n        to avoid possible clashes with preparation. For example 'QApplication'\n        should not be created.\n\n    Args:\n        addon_name (str): Addon name which triggered process.\n        addon_version (str): Addon version which triggered process.\n        project_name (Optional[str]): Project name. Can be filled in case\n            process is triggered for specific project. Some addons can have\n            different behavior based on project. Value is NOT autofilled.\n        headless (Optional[bool]): Is process running in headless mode. Value\n            is filled with value based on state set in AYON launcher.\n        addons_manager (Optional[AddonsManager]): The addons\n            manager to use. If not provided, a new one will be created.\n        exit_on_failure (bool, optional): If True, the process will exit\n            if an error occurs. Defaults to True.\n        kwargs: The keyword arguments to pass to the ProcessContext.\n\n    Returns:\n        bool: True if all addons are ready, False otherwise.\n\n    \"\"\"\n    context: ProcessContext = ProcessContext(\n        addon_name,\n        addon_version,\n        project_name,\n        headless,\n        **kwargs\n    )\n    return ensure_addons_are_process_context_ready(\n        context, addons_manager, exit_on_failure\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/index.html#client.ayon_core.addon.load_addons","title":"<code>load_addons(force=False)</code>","text":"<p>Load AYON addons as python modules.</p> <p>Modules does not load only classes (like in Interfaces) because there must be ability to use inner code of addon and be able to import it from one defined place.</p> <p>With this it is possible to import addon's content from predefined module.</p> <p>Parameters:</p> Name Type Description Default <code>force</code> <code>bool</code> <p>Force to load addons even if are already loaded. This won't update already loaded and used (cached) modules.</p> <code>False</code> Source code in <code>client/ayon_core/addon/base.py</code> <pre><code>def load_addons(force: bool = False) -&gt; None:\n    \"\"\"Load AYON addons as python modules.\n\n    Modules does not load only classes (like in Interfaces) because there must\n    be ability to use inner code of addon and be able to import it from one\n    defined place.\n\n    With this it is possible to import addon's content from predefined module.\n\n    Args:\n        force (bool): Force to load addons even if are already loaded.\n            This won't update already loaded and used (cached) modules.\n\n    \"\"\"\n    _cache_data(force=force)\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/base.html","title":"base","text":"<p>Base class for AYON addons.</p>"},{"location":"autoapi/client/ayon_core/addon/base.html#client.ayon_core.addon.base.AYONAddon","title":"<code>AYONAddon</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Base class of AYON addon.</p> <p>Attributes:</p> Name Type Description <code>enabled</code> <code>bool</code> <p>Is addon enabled.</p> <p>Parameters:</p> Name Type Description Default <code>manager</code> <code>AddonsManager</code> <p>Manager object who discovered addon.</p> required <code>settings</code> <code>dict[str, Any]</code> <p>AYON settings.</p> required Source code in <code>client/ayon_core/addon/base.py</code> <pre><code>class AYONAddon(ABC):\n    \"\"\"Base class of AYON addon.\n\n    Attributes:\n        enabled (bool): Is addon enabled.\n\n    Args:\n        manager (AddonsManager): Manager object who discovered addon.\n        settings (dict[str, Any]): AYON settings.\n\n    \"\"\"\n    enabled: bool = True\n    _id = None\n\n    # Temporary variable for 'version' property\n    _missing_version_warned = False\n\n    def __init__(\n        self, manager: AddonsManager, settings: dict[str, Any]\n    ) -&gt; None:\n        self.manager = manager\n\n        self.log = Logger.get_logger(self.name)\n\n        self.initialize(settings)\n\n    @property\n    def id(self) -&gt; str:\n        \"\"\"Random id of addon object.\n\n        Returns:\n            str: Object id.\n\n        \"\"\"\n        if self._id is None:\n            self._id = uuid4()\n        return self._id\n\n    @property\n    @abstractmethod\n    def name(self) -&gt; str:\n        \"\"\"Addon name.\n\n        Returns:\n            str: Addon name.\n\n        \"\"\"\n        pass\n\n    @property\n    def version(self) -&gt; str:\n        \"\"\"Addon version.\n\n        Todo:\n            Should be abstract property (required). Introduced in\n                ayon-core 0.3.3 .\n\n        Returns:\n            str: Addon version as semver compatible string.\n\n        \"\"\"\n        if not self.__class__._missing_version_warned:\n            self.__class__._missing_version_warned = True\n            print(\n                f\"DEV WARNING: Addon '{self.name}' does not have\"\n                f\" defined version.\"\n            )\n        return \"0.0.0\"\n\n    def initialize(self, settings: dict[str, Any]) -&gt; None:\n        \"\"\"Initialization of addon attributes.\n\n        It is not recommended to override __init__ that's why specific method\n        was implemented.\n\n        Args:\n            settings (dict[str, Any]): Settings.\n\n        \"\"\"\n        pass\n\n    def connect_with_addons(self, enabled_addons: list[AYONAddon]) -&gt; None:\n        \"\"\"Connect with other enabled addons.\n\n        Args:\n            enabled_addons (list[AYONAddon]): Addons that are enabled.\n\n        \"\"\"\n        pass\n\n    def ensure_is_process_ready(\n        self, process_context: ProcessContext\n    ) -&gt; None:\n        \"\"\"Make sure addon is prepared for a process.\n\n        This method is called when some action makes sure that addon has set\n        necessary data. For example if user should be logged in\n        and filled credentials in environment variables this method should\n        ask user for credentials.\n\n        Implementation of this method is optional.\n\n        Note:\n            The logic can be similar to logic in tray, but tray does not\n                require to be logged in.\n\n        Args:\n            process_context (ProcessContext): Context of child\n                process.\n\n        \"\"\"\n        pass\n\n    def get_global_environments(self) -&gt; dict[str, str]:\n        \"\"\"Get global environments values of addon.\n\n        Environment variables that can be get only from system settings.\n\n        Returns:\n            dict[str, str]: Environment variables.\n\n        \"\"\"\n        return {}\n\n    def on_host_install(\n        self,\n        host: HostBase,\n        host_name: str,\n        project_name: str,\n    ) -&gt; None:\n        \"\"\"Host was installed which gives option to handle in-host logic.\n\n        It is a good option to register in-host event callbacks which are\n        specific for the addon. The addon is kept in memory for rest of\n        the process.\n\n        Arguments may change in future. E.g. 'host_name' should be possible\n        to receive from 'host' object.\n\n        Args:\n            host (HostBase): Access to installed/registered\n                host object.\n            host_name (str): Name of host.\n            project_name (str): Project name which is main part of host\n                context.\n\n        \"\"\"\n        pass\n\n    def cli(self, addon_click_group: click.Group) -&gt; None:\n        \"\"\"Add commands to click group.\n\n        The best practise is to create click group for whole addon which is\n        used to separate commands.\n\n        Example:\n            class MyPlugin(AYONAddon):\n                ...\n                def cli(self, addon_click_group):\n                    addon_click_group.add_command(cli_main)\n\n\n            @click.group(&lt;addon name&gt;, help=\"&lt;Any help shown in cmd&gt;\")\n            def cli_main():\n                pass\n\n            @cli_main.command()\n            def mycommand():\n                print(\"my_command\")\n\n        Args:\n            addon_click_group (click.Group): Group to which can be added\n                commands.\n\n        \"\"\"\n        pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/base.html#client.ayon_core.addon.base.AYONAddon.id","title":"<code>id</code>  <code>property</code>","text":"<p>Random id of addon object.</p> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Object id.</p>"},{"location":"autoapi/client/ayon_core/addon/base.html#client.ayon_core.addon.base.AYONAddon.name","title":"<code>name</code>  <code>abstractmethod</code> <code>property</code>","text":"<p>Addon name.</p> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Addon name.</p>"},{"location":"autoapi/client/ayon_core/addon/base.html#client.ayon_core.addon.base.AYONAddon.version","title":"<code>version</code>  <code>property</code>","text":"<p>Addon version.</p> Todo <p>Should be abstract property (required). Introduced in     ayon-core 0.3.3 .</p> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Addon version as semver compatible string.</p>"},{"location":"autoapi/client/ayon_core/addon/base.html#client.ayon_core.addon.base.AYONAddon.cli","title":"<code>cli(addon_click_group)</code>","text":"<p>Add commands to click group.</p> <p>The best practise is to create click group for whole addon which is used to separate commands.</p> Example <p>class MyPlugin(AYONAddon):     ...     def cli(self, addon_click_group):         addon_click_group.add_command(cli_main)</p> <p>@click.group(, help=\"\") def cli_main():     pass <p>@cli_main.command() def mycommand():     print(\"my_command\")</p> <p>Parameters:</p> Name Type Description Default <code>addon_click_group</code> <code>Group</code> <p>Group to which can be added commands.</p> required Source code in <code>client/ayon_core/addon/base.py</code> <pre><code>def cli(self, addon_click_group: click.Group) -&gt; None:\n    \"\"\"Add commands to click group.\n\n    The best practise is to create click group for whole addon which is\n    used to separate commands.\n\n    Example:\n        class MyPlugin(AYONAddon):\n            ...\n            def cli(self, addon_click_group):\n                addon_click_group.add_command(cli_main)\n\n\n        @click.group(&lt;addon name&gt;, help=\"&lt;Any help shown in cmd&gt;\")\n        def cli_main():\n            pass\n\n        @cli_main.command()\n        def mycommand():\n            print(\"my_command\")\n\n    Args:\n        addon_click_group (click.Group): Group to which can be added\n            commands.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/base.html#client.ayon_core.addon.base.AYONAddon.connect_with_addons","title":"<code>connect_with_addons(enabled_addons)</code>","text":"<p>Connect with other enabled addons.</p> <p>Parameters:</p> Name Type Description Default <code>enabled_addons</code> <code>list[AYONAddon]</code> <p>Addons that are enabled.</p> required Source code in <code>client/ayon_core/addon/base.py</code> <pre><code>def connect_with_addons(self, enabled_addons: list[AYONAddon]) -&gt; None:\n    \"\"\"Connect with other enabled addons.\n\n    Args:\n        enabled_addons (list[AYONAddon]): Addons that are enabled.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/base.html#client.ayon_core.addon.base.AYONAddon.ensure_is_process_ready","title":"<code>ensure_is_process_ready(process_context)</code>","text":"<p>Make sure addon is prepared for a process.</p> <p>This method is called when some action makes sure that addon has set necessary data. For example if user should be logged in and filled credentials in environment variables this method should ask user for credentials.</p> <p>Implementation of this method is optional.</p> Note <p>The logic can be similar to logic in tray, but tray does not     require to be logged in.</p> <p>Parameters:</p> Name Type Description Default <code>process_context</code> <code>ProcessContext</code> <p>Context of child process.</p> required Source code in <code>client/ayon_core/addon/base.py</code> <pre><code>def ensure_is_process_ready(\n    self, process_context: ProcessContext\n) -&gt; None:\n    \"\"\"Make sure addon is prepared for a process.\n\n    This method is called when some action makes sure that addon has set\n    necessary data. For example if user should be logged in\n    and filled credentials in environment variables this method should\n    ask user for credentials.\n\n    Implementation of this method is optional.\n\n    Note:\n        The logic can be similar to logic in tray, but tray does not\n            require to be logged in.\n\n    Args:\n        process_context (ProcessContext): Context of child\n            process.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/base.html#client.ayon_core.addon.base.AYONAddon.get_global_environments","title":"<code>get_global_environments()</code>","text":"<p>Get global environments values of addon.</p> <p>Environment variables that can be get only from system settings.</p> <p>Returns:</p> Type Description <code>dict[str, str]</code> <p>dict[str, str]: Environment variables.</p> Source code in <code>client/ayon_core/addon/base.py</code> <pre><code>def get_global_environments(self) -&gt; dict[str, str]:\n    \"\"\"Get global environments values of addon.\n\n    Environment variables that can be get only from system settings.\n\n    Returns:\n        dict[str, str]: Environment variables.\n\n    \"\"\"\n    return {}\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/base.html#client.ayon_core.addon.base.AYONAddon.initialize","title":"<code>initialize(settings)</code>","text":"<p>Initialization of addon attributes.</p> <p>It is not recommended to override init that's why specific method was implemented.</p> <p>Parameters:</p> Name Type Description Default <code>settings</code> <code>dict[str, Any]</code> <p>Settings.</p> required Source code in <code>client/ayon_core/addon/base.py</code> <pre><code>def initialize(self, settings: dict[str, Any]) -&gt; None:\n    \"\"\"Initialization of addon attributes.\n\n    It is not recommended to override __init__ that's why specific method\n    was implemented.\n\n    Args:\n        settings (dict[str, Any]): Settings.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/base.html#client.ayon_core.addon.base.AYONAddon.on_host_install","title":"<code>on_host_install(host, host_name, project_name)</code>","text":"<p>Host was installed which gives option to handle in-host logic.</p> <p>It is a good option to register in-host event callbacks which are specific for the addon. The addon is kept in memory for rest of the process.</p> <p>Arguments may change in future. E.g. 'host_name' should be possible to receive from 'host' object.</p> <p>Parameters:</p> Name Type Description Default <code>host</code> <code>HostBase</code> <p>Access to installed/registered host object.</p> required <code>host_name</code> <code>str</code> <p>Name of host.</p> required <code>project_name</code> <code>str</code> <p>Project name which is main part of host context.</p> required Source code in <code>client/ayon_core/addon/base.py</code> <pre><code>def on_host_install(\n    self,\n    host: HostBase,\n    host_name: str,\n    project_name: str,\n) -&gt; None:\n    \"\"\"Host was installed which gives option to handle in-host logic.\n\n    It is a good option to register in-host event callbacks which are\n    specific for the addon. The addon is kept in memory for rest of\n    the process.\n\n    Arguments may change in future. E.g. 'host_name' should be possible\n    to receive from 'host' object.\n\n    Args:\n        host (HostBase): Access to installed/registered\n            host object.\n        host_name (str): Name of host.\n        project_name (str): Project name which is main part of host\n            context.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/base.html#client.ayon_core.addon.base.AddonsManager","title":"<code>AddonsManager</code>","text":"<p>Manager of addons that helps to load and prepare them to work.</p> <p>Parameters:</p> Name Type Description Default <code>settings</code> <code>dict[str, Any] | None</code> <p>AYON studio settings.</p> <code>None</code> <code>initialize</code> <code>bool</code> <p>Initialize addons on init. True by default.</p> <code>True</code> Source code in <code>client/ayon_core/addon/base.py</code> <pre><code>class AddonsManager:\n    \"\"\"Manager of addons that helps to load and prepare them to work.\n\n    Args:\n        settings (dict[str, Any] | None): AYON studio settings.\n        initialize (bool): Initialize addons on init.\n            True by default.\n\n    \"\"\"\n    # Helper attributes for report\n    _log = None\n\n    def __init__(\n        self,\n        settings: dict[str, Any] | None = None,\n        initialize: bool = True,\n    ) -&gt; None:\n        self._settings = settings\n\n        self._addons: list[AYONAddon] = []\n        self._addons_by_id: dict[str, AYONAddon] = {}\n        self._addons_by_name: dict[str, AYONAddon] = {}\n        self._report_by_name: dict[str, _ReportRow] = {}\n        self._total_row: _ReportRow = _ReportRow(\"Total\", \"(0)\", \"(0)\")\n\n        if initialize:\n            self.initialize_addons()\n            self.connect_addons()\n\n    def __getitem__(self, addon_name: str) -&gt; AYONAddon:\n        return self._addons_by_name[addon_name]\n\n    @property\n    def log(self) -&gt; logging.Logger:\n        if self._log is None:\n            self._log = Logger.get_logger(self.__class__.__name__)\n        return self._log\n\n    def get(\n        self, addon_name: str, default: Any | None = None\n    ) -&gt; AYONAddon | Any:\n        \"\"\"Access addon by name.\n\n        Args:\n            addon_name (str): Name of addon which should be returned.\n            default (Any | None): Default output if addon is not available.\n\n        Returns:\n            AYONAddon | Any: Addon found by name or `default`.\n\n        \"\"\"\n        return self._addons_by_name.get(addon_name, default)\n\n    @property\n    def addons(self) -&gt; list[AYONAddon]:\n        return list(self._addons)\n\n    @property\n    def addons_by_id(self) -&gt; dict[str, AYONAddon]:\n        return dict(self._addons_by_id)\n\n    @property\n    def addons_by_name(self) -&gt; dict[str, AYONAddon]:\n        return dict(self._addons_by_name)\n\n    def get_enabled_addon(\n        self, addon_name: str, default: Any | None = None\n    ) -&gt; AYONAddon | Any:\n        \"\"\"Fast access to enabled addon.\n\n        If addon is available but is not enabled default value is returned.\n\n        Args:\n            addon_name (str): Name of addon which should be returned.\n            default (Any): Default output if addon is not available or is\n                not enabled.\n\n        Returns:\n            Union[AYONAddon, Any]: Enabled addon found by name or None.\n\n        \"\"\"\n        addon = self.get(addon_name)\n        if addon is not None and addon.enabled:\n            return addon\n        return default\n\n    def get_enabled_addons(self) -&gt; list[AYONAddon]:\n        \"\"\"Enabled addons initialized by the manager.\n\n        Returns:\n            list[AYONAddon]: Initialized and enabled addons.\n\n        \"\"\"\n        return [\n            addon\n            for addon in self._addons\n            if addon.enabled\n        ]\n\n    def initialize_addons(self) -&gt; None:\n        \"\"\"Import and initialize addons.\"\"\"\n        # Make sure modules are loaded\n        load_addons()\n\n        self.log.debug(\"*** AYON addons initialization.\")\n\n        # Prepare settings for addons\n        settings = self._settings\n        if settings is None:\n            settings = get_studio_settings()\n\n        time_start = time.time()\n        prev_start_time = time_start\n        addon_classes = []\n        for module in _LoadCache.addon_modules:\n            # Go through globals in `ayon_core.modules`\n            for name in dir(module):\n                modules_item = getattr(module, name, None)\n                # Filter globals that are not classes which inherit from\n                #   AYONAddon\n                if (\n                    not inspect.isclass(modules_item)\n                    or modules_item is AYONAddon\n                    or not issubclass(modules_item, AYONAddon)\n                ):\n                    continue\n\n                # Check if class is abstract (Developing purpose)\n                if inspect.isabstract(modules_item):\n                    # Find abstract attributes by convention on `abc` module\n                    not_implemented = []\n                    for attr_name in dir(modules_item):\n                        attr = getattr(modules_item, attr_name, None)\n                        abs_method = getattr(\n                            attr, \"__isabstractmethod__\", None\n                        )\n                        if attr and abs_method:\n                            not_implemented.append(attr_name)\n\n                    # Log missing implementations\n                    self.log.warning((\n                        \"Skipping abstract Class: {}.\"\n                        \" Missing implementations: {}\"\n                    ).format(name, \", \".join(not_implemented)))\n                    continue\n\n                addon_classes.append(modules_item)\n\n        bundle_info = get_bundle_information()\n        server_version_by_name = {\n            addon.name: addon.version\n            for addon in bundle_info.addons\n        }\n        server_addons = set(server_version_by_name)\n        server_addons.discard(\"core\")\n        self._report_by_name[\"core\"] = _ReportRow(\n            name=\"core\",\n            version=__version__,\n            server_version=server_version_by_name.get(\"core\"),\n        )\n        for addon_cls in addon_classes:\n            name = addon_cls.__name__\n            try:\n                addon = addon_cls(self, settings)\n                server_version = server_version_by_name.get(addon.name)\n                report_row = _ReportRow(\n                    name=addon.name,\n                    version=addon.version,\n                    server_version=server_version,\n                )\n                # Store initialized object\n                self._addons.append(addon)\n                self._addons_by_id[addon.id] = addon\n                self._addons_by_name[addon.name] = addon\n                self._report_by_name[addon.name] = report_row\n                server_addons.discard(addon.name)\n\n                now = time.time()\n                report_row.init_time = now - prev_start_time\n                prev_start_time = now\n\n            except Exception:\n                self.log.warning(\n                    \"Initialization of addon '{}' failed.\".format(name),\n                    exc_info=True\n                )\n\n        self._total_row.version = f\"({len(self._report_by_name)})\"\n        self._total_row.server_version = f\"({len(server_version_by_name)})\"\n\n        for addon_name in server_addons:\n            self._report_by_name[addon_name] = _ReportRow(\n                name=addon_name,\n                version=\"-\",\n                server_version=server_version_by_name[addon_name],\n                server_addon=True,\n            )\n\n        for addon_name in sorted(self._addons_by_name.keys()):\n            addon = self._addons_by_name[addon_name]\n            enabled_str = \"X\" if addon.enabled else \" \"\n            self.log.debug(\n                f\"[{enabled_str}] {addon.name} ({addon.version})\"\n            )\n\n        self._total_row.init_time = time.time() - time_start\n\n    def connect_addons(self) -&gt; None:\n        \"\"\"Trigger connection with other enabled addons.\n\n        Addons should handle their interfaces in `connect_with_addons`.\n        \"\"\"\n        time_start = time.time()\n        prev_start_time = time_start\n        enabled_addons = self.get_enabled_addons()\n        self.log.debug(f\"Has {len(enabled_addons)} enabled addons.\")\n        for addon in enabled_addons:\n            report_row = self._report_by_name[addon.name]\n            try:\n                addon.connect_with_addons(enabled_addons)\n\n            except Exception:\n                self.log.error(\n                    \"BUG: Module failed on connection with other modules.\",\n                    exc_info=True\n                )\n\n            now = time.time()\n            report_row.connect_time = now - prev_start_time\n            prev_start_time = now\n\n        self._total_row.connect_time = time.time() - time_start\n\n    def collect_global_environments(self) -&gt; dict[str, str]:\n        \"\"\"Helper to collect global environment variabled from modules.\n\n        Returns:\n            dict: Global environment variables from enabled modules.\n\n        Raises:\n            AssertionError: Global environment variables must be unique for\n                all modules.\n        \"\"\"\n        module_envs = {}\n        for module in self.get_enabled_addons():\n            # Collect global module's global environments\n            _envs = module.get_global_environments()\n            for key, value in _envs.items():\n                if key in module_envs:\n                    # TODO better error message\n                    raise AssertionError(\n                        \"Duplicated environment key {}\".format(key)\n                    )\n                module_envs[key] = value\n        return module_envs\n\n    def collect_plugin_paths(self) -&gt; dict[str, list[str]]:\n        \"\"\"Helper to collect all plugins from modules inherited IPluginPaths.\n\n        Unknown keys are logged out.\n\n        Deprecated:\n            Use targeted methods 'collect_launcher_action_paths',\n                'collect_create_plugin_paths', 'collect_load_plugin_paths',\n                'collect_publish_plugin_paths' and\n                 'collect_inventory_action_paths' to collect plugin paths.\n\n        Returns:\n            dict: Output is dictionary with keys \"publish\", \"create\", \"load\",\n                \"actions\" and \"inventory\" each containing list of paths.\n\n        \"\"\"\n        warnings.warn(\n            \"Used deprecated method 'collect_plugin_paths'. Please use\"\n            \" targeted methods 'collect_launcher_action_paths',\"\n            \" 'collect_create_plugin_paths', 'collect_load_plugin_paths'\"\n            \" 'collect_publish_plugin_paths' and\"\n            \" 'collect_inventory_action_paths'\",\n            DeprecationWarning,\n            stacklevel=2\n        )\n        # Output structure\n        output = {\n            \"publish\": [],\n            \"create\": [],\n            \"load\": [],\n            \"actions\": [],\n            \"inventory\": []\n        }\n        unknown_keys_by_addon = {}\n        for addon in self.get_enabled_addons():\n            # Skip module that do not inherit from `IPluginPaths`\n            if not isinstance(addon, IPluginPaths):\n                continue\n            plugin_paths = addon.get_plugin_paths()\n            for key, value in plugin_paths.items():\n                # Filter unknown keys\n                if key not in output:\n                    if addon.name not in unknown_keys_by_addon:\n                        unknown_keys_by_addon[addon.name] = []\n                    unknown_keys_by_addon[addon.name].append(key)\n                    continue\n\n                # Skip if value is empty\n                if not value:\n                    continue\n\n                # Convert to list if value is not list\n                if not isinstance(value, (list, tuple, set)):\n                    value = [value]\n                output[key].extend(value)\n\n        # Report unknown keys (Developing purposes)\n        if unknown_keys_by_addon:\n            expected_keys = \", \".join([\n                f'\"{key}\"' for key in output.keys()\n            ])\n            msg_items = []\n            for addon_name, keys in unknown_keys_by_addon.items():\n                joined_keys = \", \".join([f'\"{key}\"' for key in keys])\n                msg_items.append(\n                    f\"Addon: \\\"{addon_name}\\\" - got key {joined_keys}\"\n                )\n            joined_items = \" | \".join(msg_items)\n            self.log.warning(\n                f\"Expected keys from `get_plugin_paths` are {expected_keys}.\"\n                f\" {joined_items}\"\n            )\n        return output\n\n    def _collect_plugin_paths(self, method_name: str, *args, **kwargs):\n        output = []\n        for addon in self.get_enabled_addons():\n            # Skip addon that do not inherit from `IPluginPaths`\n            if not isinstance(addon, IPluginPaths):\n                continue\n\n            paths = None\n            method = getattr(addon, method_name)\n            try:\n                paths = method(*args, **kwargs)\n            except Exception:\n                self.log.warning(\n                    \"Failed to get plugin paths from addon\"\n                    f\" '{addon.name}' using '{method_name}'.\",\n                    exc_info=True\n                )\n\n            if not paths:\n                continue\n\n            if isinstance(paths, str):\n                paths = [paths]\n                self.log.warning(\n                    f\"Addon '{addon.name}' returned invalid output type\"\n                    f\" from '{method_name}'.\"\n                    f\" Got 'str' expected 'list[str]'.\"\n                )\n            output.extend(paths)\n        return output\n\n    def collect_launcher_action_paths(self) -&gt; list[str]:\n        \"\"\"Helper to collect launcher action paths from addons.\n\n        Returns:\n            list: List of paths to launcher actions.\n\n        \"\"\"\n        output = self._collect_plugin_paths(\n            \"get_launcher_action_paths\"\n        )\n        # Add default core actions\n        actions_dir = os.path.join(AYON_CORE_ROOT, \"plugins\", \"actions\")\n        output.insert(0, actions_dir)\n        return output\n\n    def collect_create_plugin_paths(self, host_name: str) -&gt; list[str]:\n        \"\"\"Helper to collect creator plugin paths from addons.\n\n        Args:\n            host_name (str): For which host are creators meant.\n\n        Returns:\n            list[str]: List of creator plugin paths.\n\n        \"\"\"\n        return self._collect_plugin_paths(\n            \"get_create_plugin_paths\",\n            host_name\n        )\n\n    collect_creator_plugin_paths = collect_create_plugin_paths\n\n    def collect_load_plugin_paths(self, host_name: str) -&gt; list[str]:\n        \"\"\"Helper to collect load plugin paths from addons.\n\n        Args:\n            host_name (str): For which host are load plugins meant.\n\n        Returns:\n            list[str]: List of load plugin paths.\n\n        \"\"\"\n        return self._collect_plugin_paths(\n            \"get_load_plugin_paths\",\n            host_name\n        )\n\n    def collect_publish_plugin_paths(self, host_name: str) -&gt; list[str]:\n        \"\"\"Helper to collect load plugin paths from addons.\n\n        Args:\n            host_name (str): For which host are load plugins meant.\n\n        Returns:\n            list[str]: List of pyblish plugin paths.\n\n        \"\"\"\n        return self._collect_plugin_paths(\n            \"get_publish_plugin_paths\",\n            host_name\n        )\n\n    def collect_inventory_action_paths(self, host_name: str) -&gt; list[str]:\n        \"\"\"Helper to collect load plugin paths from addons.\n\n        Args:\n            host_name (str): For which host are load plugins meant.\n\n        Returns:\n            list: List of pyblish plugin paths.\n\n        \"\"\"\n        return self._collect_plugin_paths(\n            \"get_inventory_action_paths\",\n            host_name\n        )\n\n    def get_host_addon(self, host_name: str) -&gt; IHostAddon | None:\n        \"\"\"Find host addon by host name.\n\n        Args:\n            host_name (str): Host name for which is found host addon.\n\n        Returns:\n            IHostAddon | None: Found host addon by name or `None`.\n        \"\"\"\n\n        for addon in self.get_enabled_addons():\n            if (\n                isinstance(addon, IHostAddon)\n                and addon.host_name == host_name\n            ):\n                return addon\n        return None\n\n    def get_host_names(self) -&gt; set[str]:\n        \"\"\"List of available host names based on host addons.\n\n        Returns:\n            set[str]: All available host names based on enabled addons\n                inheriting 'IHostAddon'.\n\n        \"\"\"\n        return {\n            addon.host_name\n            for addon in self.get_enabled_addons()\n            if isinstance(addon, IHostAddon)\n        }\n\n    def print_report(self, include_tray: bool = False) -&gt; None:\n        \"\"\"Print out report of time spent on addons initialization parts.\n\n        Reporting is not automated must be implemented for each initialization\n        part separately. Reports must be stored to `_report` attribute.\n        Print is skipped if `_report` is empty.\n\n        Attribute `_report` is dictionary where key is \"label\" describing\n        the processed part and value is dictionary where key is addon's\n        class name and value is time delta of it's processing.\n\n        \"\"\"\n        sorted_rows = [\n            report_row\n            for report_row in sorted(\n                self._report_by_name.values(),\n                key=lambda item: item.name\n            )\n        ]\n        sorted_rows.append(self._total_row)\n        for report_row in sorted_rows:\n            report_row.sum_total()\n\n        # Add addon names to first column\n        names = [row.name for row in sorted_rows]\n        names.insert(0, \"Addon name\")\n        versions = [row.version or \"N/A\" for row in sorted_rows]\n        versions.insert(0, \"Version\")\n        server_versions = [\n            row.server_version or \"N/A\" for row in sorted_rows\n        ]\n        server_versions.insert(0, \"Server v.\")\n\n        # Prepare columns to calculate their widths\n        cols: list[list[str]] = [names, versions, server_versions]\n        for col_name, attr_name, is_tray_value in (\n            (\"Initialize\", \"init_time\", False),\n            (\"Connect\", \"connect_time\", False),\n            (\"Tray init\", \"tray_init_time\", True),\n            (\"Tray menu\", \"tray_menu_time\", True),\n            (\"Startup\", \"tray_start_time\", True),\n            (\"Total\", \"total_time\", False),\n        ):\n            if is_tray_value and not include_tray:\n                continue\n\n            values = [\n                f\"{getattr(row, attr_name):.3f}\"\n                for row in sorted_rows\n            ]\n            values.insert(0, col_name)\n            cols.append(values)\n\n        col_widths = [\n            max((len(value) for value in col_values))\n            for col_values in cols\n        ]\n\n        # Convert columns to rows for print\n        rows = list(zip(*cols, strict=True))\n        # Top row contains labels\n        top_row = rows.pop(0)\n        # Last row contains totals\n        total_row = rows.pop(-1)\n\n        separator = \"+\".join([w * \"-\" for w in col_widths])\n\n        lines = [separator]\n        lines.append(\"|\".join(\n            value.ljust(col_width)\n            for col_width, value in zip(col_widths, top_row)\n        ))\n        lines.append(separator)\n\n        for row in rows:\n            lines.append(\"|\".join(\n                value.ljust(col_width)\n                for col_width, value in zip(col_widths, row)\n            ))\n\n        lines.append(separator)\n        lines.append(\"|\".join(\n            value.ljust(col_width)\n            for col_width, value in zip(col_widths, total_row)\n        ))\n        lines.append(\"\")\n\n        output = \"\\n\".join(lines)\n        print(output)\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/base.html#client.ayon_core.addon.base.AddonsManager.collect_create_plugin_paths","title":"<code>collect_create_plugin_paths(host_name)</code>","text":"<p>Helper to collect creator plugin paths from addons.</p> <p>Parameters:</p> Name Type Description Default <code>host_name</code> <code>str</code> <p>For which host are creators meant.</p> required <p>Returns:</p> Type Description <code>list[str]</code> <p>list[str]: List of creator plugin paths.</p> Source code in <code>client/ayon_core/addon/base.py</code> <pre><code>def collect_create_plugin_paths(self, host_name: str) -&gt; list[str]:\n    \"\"\"Helper to collect creator plugin paths from addons.\n\n    Args:\n        host_name (str): For which host are creators meant.\n\n    Returns:\n        list[str]: List of creator plugin paths.\n\n    \"\"\"\n    return self._collect_plugin_paths(\n        \"get_create_plugin_paths\",\n        host_name\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/base.html#client.ayon_core.addon.base.AddonsManager.collect_global_environments","title":"<code>collect_global_environments()</code>","text":"<p>Helper to collect global environment variabled from modules.</p> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, str]</code> <p>Global environment variables from enabled modules.</p> <p>Raises:</p> Type Description <code>AssertionError</code> <p>Global environment variables must be unique for all modules.</p> Source code in <code>client/ayon_core/addon/base.py</code> <pre><code>def collect_global_environments(self) -&gt; dict[str, str]:\n    \"\"\"Helper to collect global environment variabled from modules.\n\n    Returns:\n        dict: Global environment variables from enabled modules.\n\n    Raises:\n        AssertionError: Global environment variables must be unique for\n            all modules.\n    \"\"\"\n    module_envs = {}\n    for module in self.get_enabled_addons():\n        # Collect global module's global environments\n        _envs = module.get_global_environments()\n        for key, value in _envs.items():\n            if key in module_envs:\n                # TODO better error message\n                raise AssertionError(\n                    \"Duplicated environment key {}\".format(key)\n                )\n            module_envs[key] = value\n    return module_envs\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/base.html#client.ayon_core.addon.base.AddonsManager.collect_inventory_action_paths","title":"<code>collect_inventory_action_paths(host_name)</code>","text":"<p>Helper to collect load plugin paths from addons.</p> <p>Parameters:</p> Name Type Description Default <code>host_name</code> <code>str</code> <p>For which host are load plugins meant.</p> required <p>Returns:</p> Name Type Description <code>list</code> <code>list[str]</code> <p>List of pyblish plugin paths.</p> Source code in <code>client/ayon_core/addon/base.py</code> <pre><code>def collect_inventory_action_paths(self, host_name: str) -&gt; list[str]:\n    \"\"\"Helper to collect load plugin paths from addons.\n\n    Args:\n        host_name (str): For which host are load plugins meant.\n\n    Returns:\n        list: List of pyblish plugin paths.\n\n    \"\"\"\n    return self._collect_plugin_paths(\n        \"get_inventory_action_paths\",\n        host_name\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/base.html#client.ayon_core.addon.base.AddonsManager.collect_launcher_action_paths","title":"<code>collect_launcher_action_paths()</code>","text":"<p>Helper to collect launcher action paths from addons.</p> <p>Returns:</p> Name Type Description <code>list</code> <code>list[str]</code> <p>List of paths to launcher actions.</p> Source code in <code>client/ayon_core/addon/base.py</code> <pre><code>def collect_launcher_action_paths(self) -&gt; list[str]:\n    \"\"\"Helper to collect launcher action paths from addons.\n\n    Returns:\n        list: List of paths to launcher actions.\n\n    \"\"\"\n    output = self._collect_plugin_paths(\n        \"get_launcher_action_paths\"\n    )\n    # Add default core actions\n    actions_dir = os.path.join(AYON_CORE_ROOT, \"plugins\", \"actions\")\n    output.insert(0, actions_dir)\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/base.html#client.ayon_core.addon.base.AddonsManager.collect_load_plugin_paths","title":"<code>collect_load_plugin_paths(host_name)</code>","text":"<p>Helper to collect load plugin paths from addons.</p> <p>Parameters:</p> Name Type Description Default <code>host_name</code> <code>str</code> <p>For which host are load plugins meant.</p> required <p>Returns:</p> Type Description <code>list[str]</code> <p>list[str]: List of load plugin paths.</p> Source code in <code>client/ayon_core/addon/base.py</code> <pre><code>def collect_load_plugin_paths(self, host_name: str) -&gt; list[str]:\n    \"\"\"Helper to collect load plugin paths from addons.\n\n    Args:\n        host_name (str): For which host are load plugins meant.\n\n    Returns:\n        list[str]: List of load plugin paths.\n\n    \"\"\"\n    return self._collect_plugin_paths(\n        \"get_load_plugin_paths\",\n        host_name\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/base.html#client.ayon_core.addon.base.AddonsManager.collect_plugin_paths","title":"<code>collect_plugin_paths()</code>","text":"<p>Helper to collect all plugins from modules inherited IPluginPaths.</p> <p>Unknown keys are logged out.</p> Deprecated <p>Use targeted methods 'collect_launcher_action_paths',     'collect_create_plugin_paths', 'collect_load_plugin_paths',     'collect_publish_plugin_paths' and      'collect_inventory_action_paths' to collect plugin paths.</p> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, list[str]]</code> <p>Output is dictionary with keys \"publish\", \"create\", \"load\", \"actions\" and \"inventory\" each containing list of paths.</p> Source code in <code>client/ayon_core/addon/base.py</code> <pre><code>def collect_plugin_paths(self) -&gt; dict[str, list[str]]:\n    \"\"\"Helper to collect all plugins from modules inherited IPluginPaths.\n\n    Unknown keys are logged out.\n\n    Deprecated:\n        Use targeted methods 'collect_launcher_action_paths',\n            'collect_create_plugin_paths', 'collect_load_plugin_paths',\n            'collect_publish_plugin_paths' and\n             'collect_inventory_action_paths' to collect plugin paths.\n\n    Returns:\n        dict: Output is dictionary with keys \"publish\", \"create\", \"load\",\n            \"actions\" and \"inventory\" each containing list of paths.\n\n    \"\"\"\n    warnings.warn(\n        \"Used deprecated method 'collect_plugin_paths'. Please use\"\n        \" targeted methods 'collect_launcher_action_paths',\"\n        \" 'collect_create_plugin_paths', 'collect_load_plugin_paths'\"\n        \" 'collect_publish_plugin_paths' and\"\n        \" 'collect_inventory_action_paths'\",\n        DeprecationWarning,\n        stacklevel=2\n    )\n    # Output structure\n    output = {\n        \"publish\": [],\n        \"create\": [],\n        \"load\": [],\n        \"actions\": [],\n        \"inventory\": []\n    }\n    unknown_keys_by_addon = {}\n    for addon in self.get_enabled_addons():\n        # Skip module that do not inherit from `IPluginPaths`\n        if not isinstance(addon, IPluginPaths):\n            continue\n        plugin_paths = addon.get_plugin_paths()\n        for key, value in plugin_paths.items():\n            # Filter unknown keys\n            if key not in output:\n                if addon.name not in unknown_keys_by_addon:\n                    unknown_keys_by_addon[addon.name] = []\n                unknown_keys_by_addon[addon.name].append(key)\n                continue\n\n            # Skip if value is empty\n            if not value:\n                continue\n\n            # Convert to list if value is not list\n            if not isinstance(value, (list, tuple, set)):\n                value = [value]\n            output[key].extend(value)\n\n    # Report unknown keys (Developing purposes)\n    if unknown_keys_by_addon:\n        expected_keys = \", \".join([\n            f'\"{key}\"' for key in output.keys()\n        ])\n        msg_items = []\n        for addon_name, keys in unknown_keys_by_addon.items():\n            joined_keys = \", \".join([f'\"{key}\"' for key in keys])\n            msg_items.append(\n                f\"Addon: \\\"{addon_name}\\\" - got key {joined_keys}\"\n            )\n        joined_items = \" | \".join(msg_items)\n        self.log.warning(\n            f\"Expected keys from `get_plugin_paths` are {expected_keys}.\"\n            f\" {joined_items}\"\n        )\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/base.html#client.ayon_core.addon.base.AddonsManager.collect_publish_plugin_paths","title":"<code>collect_publish_plugin_paths(host_name)</code>","text":"<p>Helper to collect load plugin paths from addons.</p> <p>Parameters:</p> Name Type Description Default <code>host_name</code> <code>str</code> <p>For which host are load plugins meant.</p> required <p>Returns:</p> Type Description <code>list[str]</code> <p>list[str]: List of pyblish plugin paths.</p> Source code in <code>client/ayon_core/addon/base.py</code> <pre><code>def collect_publish_plugin_paths(self, host_name: str) -&gt; list[str]:\n    \"\"\"Helper to collect load plugin paths from addons.\n\n    Args:\n        host_name (str): For which host are load plugins meant.\n\n    Returns:\n        list[str]: List of pyblish plugin paths.\n\n    \"\"\"\n    return self._collect_plugin_paths(\n        \"get_publish_plugin_paths\",\n        host_name\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/base.html#client.ayon_core.addon.base.AddonsManager.connect_addons","title":"<code>connect_addons()</code>","text":"<p>Trigger connection with other enabled addons.</p> <p>Addons should handle their interfaces in <code>connect_with_addons</code>.</p> Source code in <code>client/ayon_core/addon/base.py</code> <pre><code>def connect_addons(self) -&gt; None:\n    \"\"\"Trigger connection with other enabled addons.\n\n    Addons should handle their interfaces in `connect_with_addons`.\n    \"\"\"\n    time_start = time.time()\n    prev_start_time = time_start\n    enabled_addons = self.get_enabled_addons()\n    self.log.debug(f\"Has {len(enabled_addons)} enabled addons.\")\n    for addon in enabled_addons:\n        report_row = self._report_by_name[addon.name]\n        try:\n            addon.connect_with_addons(enabled_addons)\n\n        except Exception:\n            self.log.error(\n                \"BUG: Module failed on connection with other modules.\",\n                exc_info=True\n            )\n\n        now = time.time()\n        report_row.connect_time = now - prev_start_time\n        prev_start_time = now\n\n    self._total_row.connect_time = time.time() - time_start\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/base.html#client.ayon_core.addon.base.AddonsManager.get","title":"<code>get(addon_name, default=None)</code>","text":"<p>Access addon by name.</p> <p>Parameters:</p> Name Type Description Default <code>addon_name</code> <code>str</code> <p>Name of addon which should be returned.</p> required <code>default</code> <code>Any | None</code> <p>Default output if addon is not available.</p> <code>None</code> <p>Returns:</p> Type Description <code>AYONAddon | Any</code> <p>AYONAddon | Any: Addon found by name or <code>default</code>.</p> Source code in <code>client/ayon_core/addon/base.py</code> <pre><code>def get(\n    self, addon_name: str, default: Any | None = None\n) -&gt; AYONAddon | Any:\n    \"\"\"Access addon by name.\n\n    Args:\n        addon_name (str): Name of addon which should be returned.\n        default (Any | None): Default output if addon is not available.\n\n    Returns:\n        AYONAddon | Any: Addon found by name or `default`.\n\n    \"\"\"\n    return self._addons_by_name.get(addon_name, default)\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/base.html#client.ayon_core.addon.base.AddonsManager.get_enabled_addon","title":"<code>get_enabled_addon(addon_name, default=None)</code>","text":"<p>Fast access to enabled addon.</p> <p>If addon is available but is not enabled default value is returned.</p> <p>Parameters:</p> Name Type Description Default <code>addon_name</code> <code>str</code> <p>Name of addon which should be returned.</p> required <code>default</code> <code>Any</code> <p>Default output if addon is not available or is not enabled.</p> <code>None</code> <p>Returns:</p> Type Description <code>AYONAddon | Any</code> <p>Union[AYONAddon, Any]: Enabled addon found by name or None.</p> Source code in <code>client/ayon_core/addon/base.py</code> <pre><code>def get_enabled_addon(\n    self, addon_name: str, default: Any | None = None\n) -&gt; AYONAddon | Any:\n    \"\"\"Fast access to enabled addon.\n\n    If addon is available but is not enabled default value is returned.\n\n    Args:\n        addon_name (str): Name of addon which should be returned.\n        default (Any): Default output if addon is not available or is\n            not enabled.\n\n    Returns:\n        Union[AYONAddon, Any]: Enabled addon found by name or None.\n\n    \"\"\"\n    addon = self.get(addon_name)\n    if addon is not None and addon.enabled:\n        return addon\n    return default\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/base.html#client.ayon_core.addon.base.AddonsManager.get_enabled_addons","title":"<code>get_enabled_addons()</code>","text":"<p>Enabled addons initialized by the manager.</p> <p>Returns:</p> Type Description <code>list[AYONAddon]</code> <p>list[AYONAddon]: Initialized and enabled addons.</p> Source code in <code>client/ayon_core/addon/base.py</code> <pre><code>def get_enabled_addons(self) -&gt; list[AYONAddon]:\n    \"\"\"Enabled addons initialized by the manager.\n\n    Returns:\n        list[AYONAddon]: Initialized and enabled addons.\n\n    \"\"\"\n    return [\n        addon\n        for addon in self._addons\n        if addon.enabled\n    ]\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/base.html#client.ayon_core.addon.base.AddonsManager.get_host_addon","title":"<code>get_host_addon(host_name)</code>","text":"<p>Find host addon by host name.</p> <p>Parameters:</p> Name Type Description Default <code>host_name</code> <code>str</code> <p>Host name for which is found host addon.</p> required <p>Returns:</p> Type Description <code>IHostAddon | None</code> <p>IHostAddon | None: Found host addon by name or <code>None</code>.</p> Source code in <code>client/ayon_core/addon/base.py</code> <pre><code>def get_host_addon(self, host_name: str) -&gt; IHostAddon | None:\n    \"\"\"Find host addon by host name.\n\n    Args:\n        host_name (str): Host name for which is found host addon.\n\n    Returns:\n        IHostAddon | None: Found host addon by name or `None`.\n    \"\"\"\n\n    for addon in self.get_enabled_addons():\n        if (\n            isinstance(addon, IHostAddon)\n            and addon.host_name == host_name\n        ):\n            return addon\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/base.html#client.ayon_core.addon.base.AddonsManager.get_host_names","title":"<code>get_host_names()</code>","text":"<p>List of available host names based on host addons.</p> <p>Returns:</p> Type Description <code>set[str]</code> <p>set[str]: All available host names based on enabled addons inheriting 'IHostAddon'.</p> Source code in <code>client/ayon_core/addon/base.py</code> <pre><code>def get_host_names(self) -&gt; set[str]:\n    \"\"\"List of available host names based on host addons.\n\n    Returns:\n        set[str]: All available host names based on enabled addons\n            inheriting 'IHostAddon'.\n\n    \"\"\"\n    return {\n        addon.host_name\n        for addon in self.get_enabled_addons()\n        if isinstance(addon, IHostAddon)\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/base.html#client.ayon_core.addon.base.AddonsManager.initialize_addons","title":"<code>initialize_addons()</code>","text":"<p>Import and initialize addons.</p> Source code in <code>client/ayon_core/addon/base.py</code> <pre><code>def initialize_addons(self) -&gt; None:\n    \"\"\"Import and initialize addons.\"\"\"\n    # Make sure modules are loaded\n    load_addons()\n\n    self.log.debug(\"*** AYON addons initialization.\")\n\n    # Prepare settings for addons\n    settings = self._settings\n    if settings is None:\n        settings = get_studio_settings()\n\n    time_start = time.time()\n    prev_start_time = time_start\n    addon_classes = []\n    for module in _LoadCache.addon_modules:\n        # Go through globals in `ayon_core.modules`\n        for name in dir(module):\n            modules_item = getattr(module, name, None)\n            # Filter globals that are not classes which inherit from\n            #   AYONAddon\n            if (\n                not inspect.isclass(modules_item)\n                or modules_item is AYONAddon\n                or not issubclass(modules_item, AYONAddon)\n            ):\n                continue\n\n            # Check if class is abstract (Developing purpose)\n            if inspect.isabstract(modules_item):\n                # Find abstract attributes by convention on `abc` module\n                not_implemented = []\n                for attr_name in dir(modules_item):\n                    attr = getattr(modules_item, attr_name, None)\n                    abs_method = getattr(\n                        attr, \"__isabstractmethod__\", None\n                    )\n                    if attr and abs_method:\n                        not_implemented.append(attr_name)\n\n                # Log missing implementations\n                self.log.warning((\n                    \"Skipping abstract Class: {}.\"\n                    \" Missing implementations: {}\"\n                ).format(name, \", \".join(not_implemented)))\n                continue\n\n            addon_classes.append(modules_item)\n\n    bundle_info = get_bundle_information()\n    server_version_by_name = {\n        addon.name: addon.version\n        for addon in bundle_info.addons\n    }\n    server_addons = set(server_version_by_name)\n    server_addons.discard(\"core\")\n    self._report_by_name[\"core\"] = _ReportRow(\n        name=\"core\",\n        version=__version__,\n        server_version=server_version_by_name.get(\"core\"),\n    )\n    for addon_cls in addon_classes:\n        name = addon_cls.__name__\n        try:\n            addon = addon_cls(self, settings)\n            server_version = server_version_by_name.get(addon.name)\n            report_row = _ReportRow(\n                name=addon.name,\n                version=addon.version,\n                server_version=server_version,\n            )\n            # Store initialized object\n            self._addons.append(addon)\n            self._addons_by_id[addon.id] = addon\n            self._addons_by_name[addon.name] = addon\n            self._report_by_name[addon.name] = report_row\n            server_addons.discard(addon.name)\n\n            now = time.time()\n            report_row.init_time = now - prev_start_time\n            prev_start_time = now\n\n        except Exception:\n            self.log.warning(\n                \"Initialization of addon '{}' failed.\".format(name),\n                exc_info=True\n            )\n\n    self._total_row.version = f\"({len(self._report_by_name)})\"\n    self._total_row.server_version = f\"({len(server_version_by_name)})\"\n\n    for addon_name in server_addons:\n        self._report_by_name[addon_name] = _ReportRow(\n            name=addon_name,\n            version=\"-\",\n            server_version=server_version_by_name[addon_name],\n            server_addon=True,\n        )\n\n    for addon_name in sorted(self._addons_by_name.keys()):\n        addon = self._addons_by_name[addon_name]\n        enabled_str = \"X\" if addon.enabled else \" \"\n        self.log.debug(\n            f\"[{enabled_str}] {addon.name} ({addon.version})\"\n        )\n\n    self._total_row.init_time = time.time() - time_start\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/base.html#client.ayon_core.addon.base.AddonsManager.print_report","title":"<code>print_report(include_tray=False)</code>","text":"<p>Print out report of time spent on addons initialization parts.</p> <p>Reporting is not automated must be implemented for each initialization part separately. Reports must be stored to <code>_report</code> attribute. Print is skipped if <code>_report</code> is empty.</p> <p>Attribute <code>_report</code> is dictionary where key is \"label\" describing the processed part and value is dictionary where key is addon's class name and value is time delta of it's processing.</p> Source code in <code>client/ayon_core/addon/base.py</code> <pre><code>def print_report(self, include_tray: bool = False) -&gt; None:\n    \"\"\"Print out report of time spent on addons initialization parts.\n\n    Reporting is not automated must be implemented for each initialization\n    part separately. Reports must be stored to `_report` attribute.\n    Print is skipped if `_report` is empty.\n\n    Attribute `_report` is dictionary where key is \"label\" describing\n    the processed part and value is dictionary where key is addon's\n    class name and value is time delta of it's processing.\n\n    \"\"\"\n    sorted_rows = [\n        report_row\n        for report_row in sorted(\n            self._report_by_name.values(),\n            key=lambda item: item.name\n        )\n    ]\n    sorted_rows.append(self._total_row)\n    for report_row in sorted_rows:\n        report_row.sum_total()\n\n    # Add addon names to first column\n    names = [row.name for row in sorted_rows]\n    names.insert(0, \"Addon name\")\n    versions = [row.version or \"N/A\" for row in sorted_rows]\n    versions.insert(0, \"Version\")\n    server_versions = [\n        row.server_version or \"N/A\" for row in sorted_rows\n    ]\n    server_versions.insert(0, \"Server v.\")\n\n    # Prepare columns to calculate their widths\n    cols: list[list[str]] = [names, versions, server_versions]\n    for col_name, attr_name, is_tray_value in (\n        (\"Initialize\", \"init_time\", False),\n        (\"Connect\", \"connect_time\", False),\n        (\"Tray init\", \"tray_init_time\", True),\n        (\"Tray menu\", \"tray_menu_time\", True),\n        (\"Startup\", \"tray_start_time\", True),\n        (\"Total\", \"total_time\", False),\n    ):\n        if is_tray_value and not include_tray:\n            continue\n\n        values = [\n            f\"{getattr(row, attr_name):.3f}\"\n            for row in sorted_rows\n        ]\n        values.insert(0, col_name)\n        cols.append(values)\n\n    col_widths = [\n        max((len(value) for value in col_values))\n        for col_values in cols\n    ]\n\n    # Convert columns to rows for print\n    rows = list(zip(*cols, strict=True))\n    # Top row contains labels\n    top_row = rows.pop(0)\n    # Last row contains totals\n    total_row = rows.pop(-1)\n\n    separator = \"+\".join([w * \"-\" for w in col_widths])\n\n    lines = [separator]\n    lines.append(\"|\".join(\n        value.ljust(col_width)\n        for col_width, value in zip(col_widths, top_row)\n    ))\n    lines.append(separator)\n\n    for row in rows:\n        lines.append(\"|\".join(\n            value.ljust(col_width)\n            for col_width, value in zip(col_widths, row)\n        ))\n\n    lines.append(separator)\n    lines.append(\"|\".join(\n        value.ljust(col_width)\n        for col_width, value in zip(col_widths, total_row)\n    ))\n    lines.append(\"\")\n\n    output = \"\\n\".join(lines)\n    print(output)\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/base.html#client.ayon_core.addon.base.ProcessContext","title":"<code>ProcessContext</code>","text":"<p>Hold context of process that is going to be started.</p> <p>Right now the context is simple, having information about addon that wants     to trigger preparation and possibly project name for which it should     happen.</p> <p>Preparation for process can be required for ayon-core or any other addon.     It can be, change of environment variables, or request login to     a project management.</p> <p>At the moment of creation is 'ProcessContext' only data holder, but that     might change in future if there will be need.</p> <p>Parameters:</p> Name Type Description Default <code>addon_name</code> <code>str</code> <p>Addon name which triggered process.</p> required <code>addon_version</code> <code>str</code> <p>Addon version which triggered process.</p> required <code>project_name</code> <code>str | None</code> <p>Project name. Can be filled in case process is triggered for specific project. Some addons can have different behavior based on project. Value is NOT autofilled.</p> <code>None</code> <code>headless</code> <code>bool | None</code> <p>Is process running in headless mode. Value is filled with value based on state set in AYON launcher.</p> <code>None</code> Source code in <code>client/ayon_core/addon/base.py</code> <pre><code>class ProcessContext:\n    \"\"\"Hold context of process that is going to be started.\n\n    Right now the context is simple, having information about addon that wants\n        to trigger preparation and possibly project name for which it should\n        happen.\n\n    Preparation for process can be required for ayon-core or any other addon.\n        It can be, change of environment variables, or request login to\n        a project management.\n\n    At the moment of creation is 'ProcessContext' only data holder, but that\n        might change in future if there will be need.\n\n    Args:\n        addon_name (str): Addon name which triggered process.\n        addon_version (str): Addon version which triggered process.\n        project_name (str | None): Project name. Can be filled in case\n            process is triggered for specific project. Some addons can have\n            different behavior based on project. Value is NOT autofilled.\n        headless (bool | None): Is process running in headless mode. Value\n            is filled with value based on state set in AYON launcher.\n\n    \"\"\"\n    def __init__(\n        self,\n        addon_name: str,\n        addon_version: str,\n        project_name: str | None = None,\n        headless: bool | None = None,\n        **kwargs,\n    ):\n        if headless is None:\n            headless = is_headless_mode_enabled()\n        self.addon_name: str = addon_name\n        self.addon_version: str = addon_version\n        self.project_name: str | None = project_name\n        self.headless: bool = headless\n\n        if kwargs:\n            unknown_keys = \", \".join([f'\"{key}\"' for key in kwargs.keys()])\n            print(f\"Unknown keys in ProcessContext: {unknown_keys}\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/base.html#client.ayon_core.addon.base.ProcessPreparationError","title":"<code>ProcessPreparationError</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Exception that can be used when process preparation failed.</p> <p>The message is shown to user (either as UI dialog or printed). If     different error is raised a \"generic\" error message is shown to user     with option to copy error message to clipboard.</p> Source code in <code>client/ayon_core/addon/base.py</code> <pre><code>class ProcessPreparationError(Exception):\n    \"\"\"Exception that can be used when process preparation failed.\n\n    The message is shown to user (either as UI dialog or printed). If\n        different error is raised a \"generic\" error message is shown to user\n        with option to copy error message to clipboard.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/base.html#client.ayon_core.addon.base.load_addons","title":"<code>load_addons(force=False)</code>","text":"<p>Load AYON addons as python modules.</p> <p>Modules does not load only classes (like in Interfaces) because there must be ability to use inner code of addon and be able to import it from one defined place.</p> <p>With this it is possible to import addon's content from predefined module.</p> <p>Parameters:</p> Name Type Description Default <code>force</code> <code>bool</code> <p>Force to load addons even if are already loaded. This won't update already loaded and used (cached) modules.</p> <code>False</code> Source code in <code>client/ayon_core/addon/base.py</code> <pre><code>def load_addons(force: bool = False) -&gt; None:\n    \"\"\"Load AYON addons as python modules.\n\n    Modules does not load only classes (like in Interfaces) because there must\n    be ability to use inner code of addon and be able to import it from one\n    defined place.\n\n    With this it is possible to import addon's content from predefined module.\n\n    Args:\n        force (bool): Force to load addons even if are already loaded.\n            This won't update already loaded and used (cached) modules.\n\n    \"\"\"\n    _cache_data(force=force)\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/click_wrap.html","title":"click_wrap","text":"<p>Simplified wrapper for 'click' python module.</p> <p>Module 'click' is used as main cli handler in AYON. Addons can register their own subcommands with options. This wrapper allows to define commands and options as with 'click', but without any dependency.</p> <p>Why not to use 'click' directly? Version of 'click' used in AYON is not compatible with 'click' version used in some DCCs (e.g. Houdini 20+). And updating 'click' would break other DCCs.</p> <p>How to use it? If you already have cli commands defined in addon, just replace 'click' with 'click_wrap' and it should work and modify your addon's cli method to convert 'click_wrap' object to 'click' object.</p> <p>Before</p> <pre><code>import click\nfrom ayon_core.addon import AYONAddon\n\n\nclass ExampleAddon(AYONAddon):\n    name = \"example\"\n\n    def cli(self, click_group):\n        click_group.add_command(cli_main)\n\n\n@click.group(ExampleAddon.name, help=\"Example addon\")\ndef cli_main():\n    pass\n\n\n@cli_main.command(help=\"Example command\")\n@click.option(\"--arg1\", help=\"Example argument 1\", default=\"default1\")\n@click.option(\"--arg2\", help=\"Example argument 2\", is_flag=True)\ndef mycommand(arg1, arg2):\n    print(arg1, arg2)\n</code></pre> <p>Now</p> <pre><code>from ayon_core import click_wrap\nfrom ayon_core.addon import AYONAddon\n\n\nclass ExampleAddon(AYONAddon):\n    name = \"example\"\n\n    def cli(self, click_group):\n        click_group.add_command(cli_main.to_click_obj())\n\n\n@click_wrap.group(ExampleAddon.name, help=\"Example addon\")\ndef cli_main():\n    pass\n\n\n@cli_main.command(help=\"Example command\")\n@click_wrap.option(\"--arg1\", help=\"Example argument 1\", default=\"default1\")\n@click_wrap.option(\"--arg2\", help=\"Example argument 2\", is_flag=True)\ndef mycommand(arg1, arg2):\n    print(arg1, arg2)\n</code></pre> <p>Added small enhancements: - most of the methods can be used as chained calls - functions/methods 'command' and 'group' can be used in a way that     first argument is callback function and the rest are arguments     for click</p> Example <pre><code>from ayon_core import click_wrap\nfrom ayon_core.addon import AYONAddon\n\n\nclass ExampleAddon(AYONAddon):\n    name = \"example\"\n\n    def cli(self, click_group):\n        # Define main command (name 'example')\n        main = click_wrap.group(\n            self._cli_main, name=self.name, help=\"Example addon\"\n        )\n        # Add subcommand (name 'mycommand')\n        (\n            main.command(\n                self._cli_command, name=\"mycommand\", help=\"Example command\"\n            )\n            .option(\n                \"--arg1\", help=\"Example argument 1\", default=\"default1\"\n            )\n            .option(\n                \"--arg2\", help=\"Example argument 2\", is_flag=True,\n            )\n        )\n        # Convert main command to click object and add it to parent group\n        click_group.add_command(main.to_click_obj())\n\n    def _cli_main(self):\n        pass\n\n    def _cli_command(self, arg1, arg2):\n        print(arg1, arg2)\n</code></pre> <pre><code>ayon addon example mycommand --arg1 value1 --arg2\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/click_wrap.html#client.ayon_core.addon.click_wrap.Command","title":"<code>Command</code>","text":"<p>               Bases: <code>object</code></p> Source code in <code>client/ayon_core/addon/click_wrap.py</code> <pre><code>class Command(object):\n    def __init__(self, func, *args, **kwargs):\n        # Command function\n        self._func = func\n        # Command definition arguments\n        self._args = args\n        # Command definition kwargs\n        self._kwargs = kwargs\n        # Both 'options' and 'arguments' are stored to the same variable\n        #   - keep order of options and arguments\n        self._options = getattr(func, FUNC_ATTR_NAME, [])\n\n    def to_click_obj(self):\n        \"\"\"Converts this object to click object.\n\n        Returns:\n            click.Command: Click command object.\n        \"\"\"\n        return convert_to_click(self)\n\n    # --- Methods for 'convert_to_click' function ---\n    def get_args(self):\n        \"\"\"\n        Returns:\n            tuple: Command definition arguments.\n        \"\"\"\n        return self._args\n\n    def get_kwargs(self):\n        \"\"\"\n        Returns:\n            dict[str, Any]: Command definition kwargs.\n        \"\"\"\n        return self._kwargs\n\n    def get_func(self):\n        \"\"\"\n        Returns:\n            Function: Function to invoke on command trigger.\n        \"\"\"\n        return self._func\n\n    def iter_options(self):\n        \"\"\"\n        Yields:\n            tuple[str, tuple, dict]: Option type name with args and kwargs.\n        \"\"\"\n        for item in self._options:\n            yield item\n    # -----------------------------------------------\n\n    def add_option(self, *args, **kwargs):\n        return self.add_option_by_type(\"option\", *args, **kwargs)\n\n    def add_argument(self, *args, **kwargs):\n        return self.add_option_by_type(\"argument\", *args, **kwargs)\n\n    option = add_option\n    argument = add_argument\n\n    def add_option_by_type(self, option_name, *args, **kwargs):\n        self._options.append((option_name, args, kwargs))\n        return self\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/click_wrap.html#client.ayon_core.addon.click_wrap.Command.get_args","title":"<code>get_args()</code>","text":"<p>Returns:</p> Name Type Description <code>tuple</code> <p>Command definition arguments.</p> Source code in <code>client/ayon_core/addon/click_wrap.py</code> <pre><code>def get_args(self):\n    \"\"\"\n    Returns:\n        tuple: Command definition arguments.\n    \"\"\"\n    return self._args\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/click_wrap.html#client.ayon_core.addon.click_wrap.Command.get_func","title":"<code>get_func()</code>","text":"<p>Returns:</p> Name Type Description <code>Function</code> <p>Function to invoke on command trigger.</p> Source code in <code>client/ayon_core/addon/click_wrap.py</code> <pre><code>def get_func(self):\n    \"\"\"\n    Returns:\n        Function: Function to invoke on command trigger.\n    \"\"\"\n    return self._func\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/click_wrap.html#client.ayon_core.addon.click_wrap.Command.get_kwargs","title":"<code>get_kwargs()</code>","text":"<p>Returns:</p> Type Description <p>dict[str, Any]: Command definition kwargs.</p> Source code in <code>client/ayon_core/addon/click_wrap.py</code> <pre><code>def get_kwargs(self):\n    \"\"\"\n    Returns:\n        dict[str, Any]: Command definition kwargs.\n    \"\"\"\n    return self._kwargs\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/click_wrap.html#client.ayon_core.addon.click_wrap.Command.iter_options","title":"<code>iter_options()</code>","text":"<p>Yields:</p> Type Description <p>tuple[str, tuple, dict]: Option type name with args and kwargs.</p> Source code in <code>client/ayon_core/addon/click_wrap.py</code> <pre><code>def iter_options(self):\n    \"\"\"\n    Yields:\n        tuple[str, tuple, dict]: Option type name with args and kwargs.\n    \"\"\"\n    for item in self._options:\n        yield item\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/click_wrap.html#client.ayon_core.addon.click_wrap.Command.to_click_obj","title":"<code>to_click_obj()</code>","text":"<p>Converts this object to click object.</p> <p>Returns:</p> Type Description <p>click.Command: Click command object.</p> Source code in <code>client/ayon_core/addon/click_wrap.py</code> <pre><code>def to_click_obj(self):\n    \"\"\"Converts this object to click object.\n\n    Returns:\n        click.Command: Click command object.\n    \"\"\"\n    return convert_to_click(self)\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/click_wrap.html#client.ayon_core.addon.click_wrap.Group","title":"<code>Group</code>","text":"<p>               Bases: <code>Command</code></p> Source code in <code>client/ayon_core/addon/click_wrap.py</code> <pre><code>class Group(Command):\n    def __init__(self, func, *args, **kwargs):\n        super(Group, self).__init__(func, *args, **kwargs)\n        # Store sub-groupd and sub-commands to the same variable\n        self._commands = []\n\n    # --- Methods for 'convert_to_click' function ---\n    def iter_commands(self):\n        for command in self._commands:\n            yield command\n    # -----------------------------------------------\n\n    def add_command(self, command):\n        \"\"\"Add prepared command object as child.\n\n        Args:\n            command (Command): Prepared command object.\n        \"\"\"\n        if command not in self._commands:\n            self._commands.append(command)\n\n    def add_group(self, group):\n        \"\"\"Add prepared group object as child.\n\n        Args:\n            group (Group): Prepared group object.\n        \"\"\"\n        if group not in self._commands:\n            self._commands.append(group)\n\n    def command(self, *args, **kwargs):\n        \"\"\"Add child command.\n\n        Returns:\n            Union[Command, Function]: New command object, or wrapper function.\n        \"\"\"\n        return self._add_new(Command, *args, **kwargs)\n\n    def group(self, *args, **kwargs):\n        \"\"\"Add child group.\n\n        Returns:\n            Union[Group, Function]: New group object, or wrapper function.\n        \"\"\"\n        return self._add_new(Group, *args, **kwargs)\n\n    def _add_new(self, target_cls, *args, **kwargs):\n        func = None\n        if args and callable(args[0]):\n            args = list(args)\n            func = args.pop(0)\n            args = tuple(args)\n\n        def decorator(_func):\n            out = target_cls(_func, *args, **kwargs)\n            self._commands.append(out)\n            return out\n\n        if func is not None:\n            return decorator(func)\n        return decorator\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/click_wrap.html#client.ayon_core.addon.click_wrap.Group.add_command","title":"<code>add_command(command)</code>","text":"<p>Add prepared command object as child.</p> <p>Parameters:</p> Name Type Description Default <code>command</code> <code>Command</code> <p>Prepared command object.</p> required Source code in <code>client/ayon_core/addon/click_wrap.py</code> <pre><code>def add_command(self, command):\n    \"\"\"Add prepared command object as child.\n\n    Args:\n        command (Command): Prepared command object.\n    \"\"\"\n    if command not in self._commands:\n        self._commands.append(command)\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/click_wrap.html#client.ayon_core.addon.click_wrap.Group.add_group","title":"<code>add_group(group)</code>","text":"<p>Add prepared group object as child.</p> <p>Parameters:</p> Name Type Description Default <code>group</code> <code>Group</code> <p>Prepared group object.</p> required Source code in <code>client/ayon_core/addon/click_wrap.py</code> <pre><code>def add_group(self, group):\n    \"\"\"Add prepared group object as child.\n\n    Args:\n        group (Group): Prepared group object.\n    \"\"\"\n    if group not in self._commands:\n        self._commands.append(group)\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/click_wrap.html#client.ayon_core.addon.click_wrap.Group.command","title":"<code>command(*args, **kwargs)</code>","text":"<p>Add child command.</p> <p>Returns:</p> Type Description <p>Union[Command, Function]: New command object, or wrapper function.</p> Source code in <code>client/ayon_core/addon/click_wrap.py</code> <pre><code>def command(self, *args, **kwargs):\n    \"\"\"Add child command.\n\n    Returns:\n        Union[Command, Function]: New command object, or wrapper function.\n    \"\"\"\n    return self._add_new(Command, *args, **kwargs)\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/click_wrap.html#client.ayon_core.addon.click_wrap.Group.group","title":"<code>group(*args, **kwargs)</code>","text":"<p>Add child group.</p> <p>Returns:</p> Type Description <p>Union[Group, Function]: New group object, or wrapper function.</p> Source code in <code>client/ayon_core/addon/click_wrap.py</code> <pre><code>def group(self, *args, **kwargs):\n    \"\"\"Add child group.\n\n    Returns:\n        Union[Group, Function]: New group object, or wrapper function.\n    \"\"\"\n    return self._add_new(Group, *args, **kwargs)\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/click_wrap.html#client.ayon_core.addon.click_wrap.convert_to_click","title":"<code>convert_to_click(obj_to_convert)</code>","text":"<p>Convert wrapped object to click object.</p> <p>Parameters:</p> Name Type Description Default <code>obj_to_convert</code> <code>Command</code> <p>Object to convert to click object.</p> required <p>Returns:</p> Type Description <p>click.Command: Click command object.</p> Source code in <code>client/ayon_core/addon/click_wrap.py</code> <pre><code>def convert_to_click(obj_to_convert):\n    \"\"\"Convert wrapped object to click object.\n\n    Args:\n        obj_to_convert (Command): Object to convert to click object.\n\n    Returns:\n        click.Command: Click command object.\n    \"\"\"\n    import click\n\n    commands_queue = collections.deque()\n    commands_queue.append((obj_to_convert, None))\n    top_obj = None\n    while commands_queue:\n        item = commands_queue.popleft()\n        command_obj, parent_obj = item\n        if not isinstance(command_obj, Command):\n            raise TypeError(\n                \"Invalid type '{}' expected 'Command'\".format(\n                    type(command_obj)\n                )\n            )\n\n        if isinstance(command_obj, Group):\n            click_obj = (\n                click.group(\n                    *command_obj.get_args(),\n                    **command_obj.get_kwargs()\n                )(command_obj.get_func())\n            )\n\n        else:\n            click_obj = (\n                click.command(\n                    *command_obj.get_args(),\n                    **command_obj.get_kwargs()\n                )(command_obj.get_func())\n            )\n\n        for item in command_obj.iter_options():\n            option_name, args, kwargs = item\n            if option_name == \"option\":\n                click.option(*args, **kwargs)(click_obj)\n            elif option_name == \"argument\":\n                click.argument(*args, **kwargs)(click_obj)\n            else:\n                raise ValueError(\n                    \"Invalid option name '{}'\".format(option_name)\n                )\n\n        if top_obj is None:\n            top_obj = click_obj\n\n        if parent_obj is not None:\n            parent_obj.add_command(click_obj)\n\n        if isinstance(command_obj, Group):\n            for command in command_obj.iter_commands():\n                commands_queue.append((command, click_obj))\n\n    return top_obj\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/interfaces.html","title":"interfaces","text":"<p>Addon interfaces for AYON.</p>"},{"location":"autoapi/client/ayon_core/addon/interfaces.html#client.ayon_core.addon.interfaces.AYONInterface","title":"<code>AYONInterface</code>","text":"<p>Base class of Interface that can be used as Mixin with abstract parts.</p> <p>This is way how AYON addon can define that contains specific predefined functionality.</p> <p>Child classes of AYONInterface may be used as mixin in different AYON addons which means they have to have implemented methods defined in the interface. By default, interface does not have any abstract parts.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>class AYONInterface(metaclass=_AYONInterfaceMeta):\n    \"\"\"Base class of Interface that can be used as Mixin with abstract parts.\n\n    This is way how AYON addon can define that contains specific predefined\n    functionality.\n\n    Child classes of AYONInterface may be used as mixin in different\n    AYON addons which means they have to have implemented methods defined\n    in the interface. By default, interface does not have any abstract parts.\n    \"\"\"\n\n    log = None\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/interfaces.html#client.ayon_core.addon.interfaces.IHostAddon","title":"<code>IHostAddon</code>","text":"<p>               Bases: <code>AYONInterface</code></p> <p>Addon which also contain a host implementation.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>class IHostAddon(AYONInterface):\n    \"\"\"Addon which also contain a host implementation.\"\"\"\n\n    @property\n    @abstractmethod\n    def host_name(self) -&gt; str:\n        \"\"\"Name of host which addon represents.\"\"\"\n\n    def get_workfile_extensions(self) -&gt; list[str]:  # noqa: PLR6301\n        \"\"\"Define workfile extensions for host.\n\n        Not all hosts support workfiles thus this is optional implementation.\n\n        Returns:\n            List[str]: Extensions used for workfiles with dot.\n\n        \"\"\"\n        return []\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/interfaces.html#client.ayon_core.addon.interfaces.IHostAddon.host_name","title":"<code>host_name</code>  <code>abstractmethod</code> <code>property</code>","text":"<p>Name of host which addon represents.</p>"},{"location":"autoapi/client/ayon_core/addon/interfaces.html#client.ayon_core.addon.interfaces.IHostAddon.get_workfile_extensions","title":"<code>get_workfile_extensions()</code>","text":"<p>Define workfile extensions for host.</p> <p>Not all hosts support workfiles thus this is optional implementation.</p> <p>Returns:</p> Type Description <code>list[str]</code> <p>List[str]: Extensions used for workfiles with dot.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>def get_workfile_extensions(self) -&gt; list[str]:  # noqa: PLR6301\n    \"\"\"Define workfile extensions for host.\n\n    Not all hosts support workfiles thus this is optional implementation.\n\n    Returns:\n        List[str]: Extensions used for workfiles with dot.\n\n    \"\"\"\n    return []\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/interfaces.html#client.ayon_core.addon.interfaces.IPluginPaths","title":"<code>IPluginPaths</code>","text":"<p>               Bases: <code>AYONInterface</code></p> <p>Addon wants to register plugin paths.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>class IPluginPaths(AYONInterface):\n    \"\"\"Addon wants to register plugin paths.\"\"\"\n\n    def get_plugin_paths(self) -&gt; dict[str, list[str]]:\n        \"\"\"Return plugin paths for addon.\n\n        This method was abstract (required) in the past, so raise the required\n            'core' addon version when 'get_plugin_paths' is removed from\n            addon.\n\n        Deprecated:\n            Please implement specific methods 'get_create_plugin_paths',\n                'get_load_plugin_paths', 'get_inventory_action_paths' and\n                'get_publish_plugin_paths' to return plugin paths.\n\n        Returns:\n            dict[str, list[str]]: Plugin paths for addon.\n\n        \"\"\"\n        return {}\n\n    def _get_plugin_paths_by_type(\n        self, plugin_type: str\n    ) -&gt; list[str]:\n        \"\"\"Get plugin paths by type.\n\n        Args:\n            plugin_type (str): Type of plugin paths to get.\n\n        Returns:\n            list[str]: List of plugin paths.\n\n        \"\"\"\n        paths = self.get_plugin_paths()\n        if not paths or plugin_type not in paths:\n            return []\n\n        paths = paths[plugin_type]\n        if not paths:\n            return []\n\n        if not isinstance(paths, (list, tuple, set)):\n            paths = [paths]\n\n        new_function_name = \"get_launcher_action_paths\"\n        if plugin_type == \"create\":\n            new_function_name = \"get_create_plugin_paths\"\n        elif plugin_type == \"load\":\n            new_function_name = \"get_load_plugin_paths\"\n        elif plugin_type == \"publish\":\n            new_function_name = \"get_publish_plugin_paths\"\n        elif plugin_type == \"inventory\":\n            new_function_name = \"get_inventory_action_paths\"\n\n        warnings.warn(\n            f\"Addon '{self.name}' returns '{plugin_type}' paths using\"\n            \" 'get_plugin_paths' method. Please implement\"\n            f\" '{new_function_name}' instead.\",\n            DeprecationWarning,\n            stacklevel=2\n        )\n        return paths\n\n    def get_launcher_action_paths(self) -&gt; list[str]:\n        \"\"\"Receive launcher actions paths.\n\n        Give addons ability to add launcher actions paths.\n\n        Returns:\n            list[str]: List of launcher action paths.\n\n        \"\"\"\n        return self._get_plugin_paths_by_type(\"actions\")\n\n    def get_create_plugin_paths(self, host_name: str) -&gt; list[str]:\n        \"\"\"Receive create plugin paths.\n\n        Give addons ability to add create plugin paths based on host name.\n\n        Notes:\n            Default implementation uses 'get_plugin_paths' and always return\n                all create plugin paths.\n\n        Args:\n            host_name (str): For which host are the plugins meant.\n\n        Returns:\n            list[str]: List of create plugin paths.\n\n        \"\"\"\n        return self._get_plugin_paths_by_type(\"create\")\n\n    def get_load_plugin_paths(self, host_name: str) -&gt; list[str]:\n        \"\"\"Receive load plugin paths.\n\n        Give addons ability to add load plugin paths based on host name.\n\n        Notes:\n            Default implementation uses 'get_plugin_paths' and always return\n                all load plugin paths.\n\n        Args:\n            host_name (str): For which host are the plugins meant.\n\n        Returns:\n            list[str]: List of load plugin paths.\n\n        \"\"\"\n        return self._get_plugin_paths_by_type(\"load\")\n\n    def get_publish_plugin_paths(self, host_name: str) -&gt; list[str]:\n        \"\"\"Receive publish plugin paths.\n\n        Give addons ability to add publish plugin paths based on host name.\n\n        Notes:\n           Default implementation uses 'get_plugin_paths' and always return\n               all publish plugin paths.\n\n        Args:\n           host_name (str): For which host are the plugins meant.\n\n        Returns:\n            list[str]: List of publish plugin paths.\n\n        \"\"\"\n        return self._get_plugin_paths_by_type(\"publish\")\n\n    def get_inventory_action_paths(self, host_name: str) -&gt; list[str]:\n        \"\"\"Receive inventory action paths.\n\n        Give addons ability to add inventory action plugin paths.\n\n        Notes:\n           Default implementation uses 'get_plugin_paths' and always return\n               all publish plugin paths.\n\n        Args:\n           host_name (str): For which host are the plugins meant.\n\n        Returns:\n            list[str]: List of inventory action plugin paths.\n\n        \"\"\"\n        return self._get_plugin_paths_by_type(\"inventory\")\n\n    def get_loader_action_plugin_paths(\n        self, host_name: Optional[str]\n    ) -&gt; list[str]:\n        \"\"\"Receive loader action plugin paths.\n\n        Args:\n            host_name (Optional[str]): Current host name.\n\n        Returns:\n            list[str]: Paths to loader action plugins.\n\n        \"\"\"\n        return []\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/interfaces.html#client.ayon_core.addon.interfaces.IPluginPaths.get_create_plugin_paths","title":"<code>get_create_plugin_paths(host_name)</code>","text":"<p>Receive create plugin paths.</p> <p>Give addons ability to add create plugin paths based on host name.</p> Notes <p>Default implementation uses 'get_plugin_paths' and always return     all create plugin paths.</p> <p>Parameters:</p> Name Type Description Default <code>host_name</code> <code>str</code> <p>For which host are the plugins meant.</p> required <p>Returns:</p> Type Description <code>list[str]</code> <p>list[str]: List of create plugin paths.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>def get_create_plugin_paths(self, host_name: str) -&gt; list[str]:\n    \"\"\"Receive create plugin paths.\n\n    Give addons ability to add create plugin paths based on host name.\n\n    Notes:\n        Default implementation uses 'get_plugin_paths' and always return\n            all create plugin paths.\n\n    Args:\n        host_name (str): For which host are the plugins meant.\n\n    Returns:\n        list[str]: List of create plugin paths.\n\n    \"\"\"\n    return self._get_plugin_paths_by_type(\"create\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/interfaces.html#client.ayon_core.addon.interfaces.IPluginPaths.get_inventory_action_paths","title":"<code>get_inventory_action_paths(host_name)</code>","text":"<p>Receive inventory action paths.</p> <p>Give addons ability to add inventory action plugin paths.</p> Notes <p>Default implementation uses 'get_plugin_paths' and always return     all publish plugin paths.</p> <p>Parameters:</p> Name Type Description Default <code>host_name</code> <code>str</code> <p>For which host are the plugins meant.</p> required <p>Returns:</p> Type Description <code>list[str]</code> <p>list[str]: List of inventory action plugin paths.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>def get_inventory_action_paths(self, host_name: str) -&gt; list[str]:\n    \"\"\"Receive inventory action paths.\n\n    Give addons ability to add inventory action plugin paths.\n\n    Notes:\n       Default implementation uses 'get_plugin_paths' and always return\n           all publish plugin paths.\n\n    Args:\n       host_name (str): For which host are the plugins meant.\n\n    Returns:\n        list[str]: List of inventory action plugin paths.\n\n    \"\"\"\n    return self._get_plugin_paths_by_type(\"inventory\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/interfaces.html#client.ayon_core.addon.interfaces.IPluginPaths.get_launcher_action_paths","title":"<code>get_launcher_action_paths()</code>","text":"<p>Receive launcher actions paths.</p> <p>Give addons ability to add launcher actions paths.</p> <p>Returns:</p> Type Description <code>list[str]</code> <p>list[str]: List of launcher action paths.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>def get_launcher_action_paths(self) -&gt; list[str]:\n    \"\"\"Receive launcher actions paths.\n\n    Give addons ability to add launcher actions paths.\n\n    Returns:\n        list[str]: List of launcher action paths.\n\n    \"\"\"\n    return self._get_plugin_paths_by_type(\"actions\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/interfaces.html#client.ayon_core.addon.interfaces.IPluginPaths.get_load_plugin_paths","title":"<code>get_load_plugin_paths(host_name)</code>","text":"<p>Receive load plugin paths.</p> <p>Give addons ability to add load plugin paths based on host name.</p> Notes <p>Default implementation uses 'get_plugin_paths' and always return     all load plugin paths.</p> <p>Parameters:</p> Name Type Description Default <code>host_name</code> <code>str</code> <p>For which host are the plugins meant.</p> required <p>Returns:</p> Type Description <code>list[str]</code> <p>list[str]: List of load plugin paths.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>def get_load_plugin_paths(self, host_name: str) -&gt; list[str]:\n    \"\"\"Receive load plugin paths.\n\n    Give addons ability to add load plugin paths based on host name.\n\n    Notes:\n        Default implementation uses 'get_plugin_paths' and always return\n            all load plugin paths.\n\n    Args:\n        host_name (str): For which host are the plugins meant.\n\n    Returns:\n        list[str]: List of load plugin paths.\n\n    \"\"\"\n    return self._get_plugin_paths_by_type(\"load\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/interfaces.html#client.ayon_core.addon.interfaces.IPluginPaths.get_loader_action_plugin_paths","title":"<code>get_loader_action_plugin_paths(host_name)</code>","text":"<p>Receive loader action plugin paths.</p> <p>Parameters:</p> Name Type Description Default <code>host_name</code> <code>Optional[str]</code> <p>Current host name.</p> required <p>Returns:</p> Type Description <code>list[str]</code> <p>list[str]: Paths to loader action plugins.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>def get_loader_action_plugin_paths(\n    self, host_name: Optional[str]\n) -&gt; list[str]:\n    \"\"\"Receive loader action plugin paths.\n\n    Args:\n        host_name (Optional[str]): Current host name.\n\n    Returns:\n        list[str]: Paths to loader action plugins.\n\n    \"\"\"\n    return []\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/interfaces.html#client.ayon_core.addon.interfaces.IPluginPaths.get_plugin_paths","title":"<code>get_plugin_paths()</code>","text":"<p>Return plugin paths for addon.</p> <p>This method was abstract (required) in the past, so raise the required     'core' addon version when 'get_plugin_paths' is removed from     addon.</p> Deprecated <p>Please implement specific methods 'get_create_plugin_paths',     'get_load_plugin_paths', 'get_inventory_action_paths' and     'get_publish_plugin_paths' to return plugin paths.</p> <p>Returns:</p> Type Description <code>dict[str, list[str]]</code> <p>dict[str, list[str]]: Plugin paths for addon.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>def get_plugin_paths(self) -&gt; dict[str, list[str]]:\n    \"\"\"Return plugin paths for addon.\n\n    This method was abstract (required) in the past, so raise the required\n        'core' addon version when 'get_plugin_paths' is removed from\n        addon.\n\n    Deprecated:\n        Please implement specific methods 'get_create_plugin_paths',\n            'get_load_plugin_paths', 'get_inventory_action_paths' and\n            'get_publish_plugin_paths' to return plugin paths.\n\n    Returns:\n        dict[str, list[str]]: Plugin paths for addon.\n\n    \"\"\"\n    return {}\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/interfaces.html#client.ayon_core.addon.interfaces.IPluginPaths.get_publish_plugin_paths","title":"<code>get_publish_plugin_paths(host_name)</code>","text":"<p>Receive publish plugin paths.</p> <p>Give addons ability to add publish plugin paths based on host name.</p> Notes <p>Default implementation uses 'get_plugin_paths' and always return     all publish plugin paths.</p> <p>Parameters:</p> Name Type Description Default <code>host_name</code> <code>str</code> <p>For which host are the plugins meant.</p> required <p>Returns:</p> Type Description <code>list[str]</code> <p>list[str]: List of publish plugin paths.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>def get_publish_plugin_paths(self, host_name: str) -&gt; list[str]:\n    \"\"\"Receive publish plugin paths.\n\n    Give addons ability to add publish plugin paths based on host name.\n\n    Notes:\n       Default implementation uses 'get_plugin_paths' and always return\n           all publish plugin paths.\n\n    Args:\n       host_name (str): For which host are the plugins meant.\n\n    Returns:\n        list[str]: List of publish plugin paths.\n\n    \"\"\"\n    return self._get_plugin_paths_by_type(\"publish\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/interfaces.html#client.ayon_core.addon.interfaces.ITraits","title":"<code>ITraits</code>","text":"<p>               Bases: <code>AYONInterface</code></p> <p>Interface for traits.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>class ITraits(AYONInterface):\n    \"\"\"Interface for traits.\"\"\"\n\n    @abstractmethod\n    def get_addon_traits(self) -&gt; list[Type[TraitBase]]:\n        \"\"\"Get trait classes for the addon.\n\n        Returns:\n            list[Type[TraitBase]]: Traits for the addon.\n\n        \"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/interfaces.html#client.ayon_core.addon.interfaces.ITraits.get_addon_traits","title":"<code>get_addon_traits()</code>  <code>abstractmethod</code>","text":"<p>Get trait classes for the addon.</p> <p>Returns:</p> Type Description <code>list[Type[TraitBase]]</code> <p>list[Type[TraitBase]]: Traits for the addon.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>@abstractmethod\ndef get_addon_traits(self) -&gt; list[Type[TraitBase]]:\n    \"\"\"Get trait classes for the addon.\n\n    Returns:\n        list[Type[TraitBase]]: Traits for the addon.\n\n    \"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/interfaces.html#client.ayon_core.addon.interfaces.ITrayAction","title":"<code>ITrayAction</code>","text":"<p>               Bases: <code>ITrayAddon</code></p> <p>Implementation of Tray action.</p> <p>Add action to tray menu which will trigger <code>on_action_trigger</code>. It is expected to be used for showing tools.</p> <p>Methods <code>tray_start</code>, <code>tray_exit</code> and <code>connect_with_addons</code> are overridden as it's not expected that action will use them. But it is possible if necessary.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>class ITrayAction(ITrayAddon):\n    \"\"\"Implementation of Tray action.\n\n    Add action to tray menu which will trigger `on_action_trigger`.\n    It is expected to be used for showing tools.\n\n    Methods `tray_start`, `tray_exit` and `connect_with_addons` are overridden\n    as it's not expected that action will use them. But it is possible if\n    necessary.\n    \"\"\"\n\n    admin_action = False\n    _action_item = None\n\n    @property\n    @abstractmethod\n    def label(self) -&gt; str:\n        \"\"\"Service label showed in menu.\"\"\"\n\n    @abstractmethod\n    def on_action_trigger(self) -&gt; None:\n        \"\"\"What happens on actions click.\"\"\"\n\n    def tray_menu(self, tray_menu: QtWidgets.QMenu) -&gt; None:\n        \"\"\"Add action to tray menu.\"\"\"\n        from qtpy import QtWidgets\n\n        if self.admin_action:\n            action = self.add_action_to_admin_submenu(self.label, tray_menu)\n        else:\n            action = QtWidgets.QAction(self.label, tray_menu)\n            tray_menu.addAction(action)\n\n        action.triggered.connect(self.on_action_trigger)\n        self._action_item = action\n\n    def tray_start(self) -&gt; None:  # noqa: PLR6301\n        \"\"\"Start procedure in tray tool.\"\"\"\n        return\n\n    def tray_exit(self) -&gt; None:  # noqa: PLR6301\n        \"\"\"Cleanup method which is executed on tray shutdown.\"\"\"\n        return\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/interfaces.html#client.ayon_core.addon.interfaces.ITrayAction.label","title":"<code>label</code>  <code>abstractmethod</code> <code>property</code>","text":"<p>Service label showed in menu.</p>"},{"location":"autoapi/client/ayon_core/addon/interfaces.html#client.ayon_core.addon.interfaces.ITrayAction.on_action_trigger","title":"<code>on_action_trigger()</code>  <code>abstractmethod</code>","text":"<p>What happens on actions click.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>@abstractmethod\ndef on_action_trigger(self) -&gt; None:\n    \"\"\"What happens on actions click.\"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/interfaces.html#client.ayon_core.addon.interfaces.ITrayAction.tray_exit","title":"<code>tray_exit()</code>","text":"<p>Cleanup method which is executed on tray shutdown.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>def tray_exit(self) -&gt; None:  # noqa: PLR6301\n    \"\"\"Cleanup method which is executed on tray shutdown.\"\"\"\n    return\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/interfaces.html#client.ayon_core.addon.interfaces.ITrayAction.tray_menu","title":"<code>tray_menu(tray_menu)</code>","text":"<p>Add action to tray menu.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>def tray_menu(self, tray_menu: QtWidgets.QMenu) -&gt; None:\n    \"\"\"Add action to tray menu.\"\"\"\n    from qtpy import QtWidgets\n\n    if self.admin_action:\n        action = self.add_action_to_admin_submenu(self.label, tray_menu)\n    else:\n        action = QtWidgets.QAction(self.label, tray_menu)\n        tray_menu.addAction(action)\n\n    action.triggered.connect(self.on_action_trigger)\n    self._action_item = action\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/interfaces.html#client.ayon_core.addon.interfaces.ITrayAction.tray_start","title":"<code>tray_start()</code>","text":"<p>Start procedure in tray tool.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>def tray_start(self) -&gt; None:  # noqa: PLR6301\n    \"\"\"Start procedure in tray tool.\"\"\"\n    return\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/interfaces.html#client.ayon_core.addon.interfaces.ITrayAddon","title":"<code>ITrayAddon</code>","text":"<p>               Bases: <code>AYONInterface</code></p> <p>Addon has special procedures when used in Tray tool.</p> Important <p>The addon. still must be usable if is not used in tray even if it would do nothing.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>class ITrayAddon(AYONInterface):\n    \"\"\"Addon has special procedures when used in Tray tool.\n\n    Important:\n        The addon. still must be usable if is not used in tray even if it\n        would do nothing.\n\n    \"\"\"\n    manager: AddonsManager\n    tray_initialized = False\n    _tray_manager: TrayManager = None\n    _admin_submenu = None\n\n    @abstractmethod\n    def tray_init(self) -&gt; None:\n        \"\"\"Initialization part of tray implementation.\n\n        Triggered between `initialization` and `connect_with_addons`.\n\n        This is where GUIs should be loaded or tray specific parts should be\n        prepared\n\n        \"\"\"\n\n    @abstractmethod\n    def tray_menu(self, tray_menu: QtWidgets.QMenu) -&gt; None:\n        \"\"\"Add addon's action to tray menu.\"\"\"\n\n    @abstractmethod\n    def tray_start(self) -&gt; None:\n        \"\"\"Start procedure in tray tool.\"\"\"\n\n    @abstractmethod\n    def tray_exit(self) -&gt; None:\n        \"\"\"Cleanup method which is executed on tray shutdown.\n\n        This is place where all threads should be shut.\n\n        \"\"\"\n\n    def execute_in_main_thread(self, callback: Callable) -&gt; None:\n        \"\"\"Pushes callback to the queue or process 'callback' on a main thread.\n\n        Some callbacks need to be processed on main thread (menu actions\n        must be added on main thread else they won't get triggered etc.)\n\n        Args:\n            callback (Callable): Function to be executed on main thread\n\n        \"\"\"\n        if not self.tray_initialized:\n            # TODO (Illicit): Called without initialized tray, still\n            #   main thread needed.\n            try:\n                callback()\n\n            except Exception:  # noqa: BLE001\n                self.log.warning(\n                    \"Failed to execute %s callback in main thread\",\n                    str(callback), exc_info=True)\n\n            return\n        self._tray_manager.tray_manager.execute_in_main_thread(callback)\n\n    def show_tray_message(\n            self,\n            title: str,\n            message: str,\n            icon: Optional[QtWidgets.QSystemTrayIcon] = None,\n            msecs: Optional[int] = None) -&gt; None:\n        \"\"\"Show tray message.\n\n        Args:\n            title (str): Title of message.\n            message (str): Content of message.\n            icon (QSystemTrayIcon.MessageIcon): Message's icon. Default is\n                Information icon, may differ by Qt version.\n            msecs (int): Duration of message visibility in milliseconds.\n                Default is 10000 msecs, may differ by Qt version.\n        \"\"\"\n        if self._tray_manager:\n            self._tray_manager.show_tray_message(title, message, icon, msecs)\n\n    def add_doubleclick_callback(self, callback: Callable) -&gt; None:\n        \"\"\"Add callback to be triggered on tray icon double click.\"\"\"\n        if hasattr(self.manager, \"add_doubleclick_callback\"):\n            self.manager.add_doubleclick_callback(self, callback)\n\n    @staticmethod\n    def admin_submenu(tray_menu: QtWidgets.QMenu) -&gt; QtWidgets.QMenu:\n        \"\"\"Get or create admin submenu.\n\n        Returns:\n            QtWidgets.QMenu: Admin submenu.\n\n        \"\"\"\n        if ITrayAddon._admin_submenu is None:\n            from qtpy import QtWidgets\n\n            admin_submenu = QtWidgets.QMenu(\"Admin\", tray_menu)\n            admin_submenu.menuAction().setVisible(False)\n            ITrayAddon._admin_submenu = admin_submenu\n        return ITrayAddon._admin_submenu\n\n    @staticmethod\n    def add_action_to_admin_submenu(\n            label: str, tray_menu: QtWidgets.QMenu) -&gt; QtWidgets.QAction:\n        \"\"\"Add action to admin submenu.\n\n        Args:\n            label (str): Label of action.\n            tray_menu (QtWidgets.QMenu): Tray menu to add action to.\n\n        Returns:\n            QtWidgets.QAction: Action added to admin submenu\n\n        \"\"\"\n        from qtpy import QtWidgets\n\n        menu = ITrayAddon.admin_submenu(tray_menu)\n        action = QtWidgets.QAction(label, menu)\n        menu.addAction(action)\n        if not menu.menuAction().isVisible():\n            menu.menuAction().setVisible(True)\n        return action\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/interfaces.html#client.ayon_core.addon.interfaces.ITrayAddon.add_action_to_admin_submenu","title":"<code>add_action_to_admin_submenu(label, tray_menu)</code>  <code>staticmethod</code>","text":"<p>Add action to admin submenu.</p> <p>Parameters:</p> Name Type Description Default <code>label</code> <code>str</code> <p>Label of action.</p> required <code>tray_menu</code> <code>QMenu</code> <p>Tray menu to add action to.</p> required <p>Returns:</p> Type Description <code>QAction</code> <p>QtWidgets.QAction: Action added to admin submenu</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>@staticmethod\ndef add_action_to_admin_submenu(\n        label: str, tray_menu: QtWidgets.QMenu) -&gt; QtWidgets.QAction:\n    \"\"\"Add action to admin submenu.\n\n    Args:\n        label (str): Label of action.\n        tray_menu (QtWidgets.QMenu): Tray menu to add action to.\n\n    Returns:\n        QtWidgets.QAction: Action added to admin submenu\n\n    \"\"\"\n    from qtpy import QtWidgets\n\n    menu = ITrayAddon.admin_submenu(tray_menu)\n    action = QtWidgets.QAction(label, menu)\n    menu.addAction(action)\n    if not menu.menuAction().isVisible():\n        menu.menuAction().setVisible(True)\n    return action\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/interfaces.html#client.ayon_core.addon.interfaces.ITrayAddon.add_doubleclick_callback","title":"<code>add_doubleclick_callback(callback)</code>","text":"<p>Add callback to be triggered on tray icon double click.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>def add_doubleclick_callback(self, callback: Callable) -&gt; None:\n    \"\"\"Add callback to be triggered on tray icon double click.\"\"\"\n    if hasattr(self.manager, \"add_doubleclick_callback\"):\n        self.manager.add_doubleclick_callback(self, callback)\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/interfaces.html#client.ayon_core.addon.interfaces.ITrayAddon.admin_submenu","title":"<code>admin_submenu(tray_menu)</code>  <code>staticmethod</code>","text":"<p>Get or create admin submenu.</p> <p>Returns:</p> Type Description <code>QMenu</code> <p>QtWidgets.QMenu: Admin submenu.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>@staticmethod\ndef admin_submenu(tray_menu: QtWidgets.QMenu) -&gt; QtWidgets.QMenu:\n    \"\"\"Get or create admin submenu.\n\n    Returns:\n        QtWidgets.QMenu: Admin submenu.\n\n    \"\"\"\n    if ITrayAddon._admin_submenu is None:\n        from qtpy import QtWidgets\n\n        admin_submenu = QtWidgets.QMenu(\"Admin\", tray_menu)\n        admin_submenu.menuAction().setVisible(False)\n        ITrayAddon._admin_submenu = admin_submenu\n    return ITrayAddon._admin_submenu\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/interfaces.html#client.ayon_core.addon.interfaces.ITrayAddon.execute_in_main_thread","title":"<code>execute_in_main_thread(callback)</code>","text":"<p>Pushes callback to the queue or process 'callback' on a main thread.</p> <p>Some callbacks need to be processed on main thread (menu actions must be added on main thread else they won't get triggered etc.)</p> <p>Parameters:</p> Name Type Description Default <code>callback</code> <code>Callable</code> <p>Function to be executed on main thread</p> required Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>def execute_in_main_thread(self, callback: Callable) -&gt; None:\n    \"\"\"Pushes callback to the queue or process 'callback' on a main thread.\n\n    Some callbacks need to be processed on main thread (menu actions\n    must be added on main thread else they won't get triggered etc.)\n\n    Args:\n        callback (Callable): Function to be executed on main thread\n\n    \"\"\"\n    if not self.tray_initialized:\n        # TODO (Illicit): Called without initialized tray, still\n        #   main thread needed.\n        try:\n            callback()\n\n        except Exception:  # noqa: BLE001\n            self.log.warning(\n                \"Failed to execute %s callback in main thread\",\n                str(callback), exc_info=True)\n\n        return\n    self._tray_manager.tray_manager.execute_in_main_thread(callback)\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/interfaces.html#client.ayon_core.addon.interfaces.ITrayAddon.show_tray_message","title":"<code>show_tray_message(title, message, icon=None, msecs=None)</code>","text":"<p>Show tray message.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <code>str</code> <p>Title of message.</p> required <code>message</code> <code>str</code> <p>Content of message.</p> required <code>icon</code> <code>MessageIcon</code> <p>Message's icon. Default is Information icon, may differ by Qt version.</p> <code>None</code> <code>msecs</code> <code>int</code> <p>Duration of message visibility in milliseconds. Default is 10000 msecs, may differ by Qt version.</p> <code>None</code> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>def show_tray_message(\n        self,\n        title: str,\n        message: str,\n        icon: Optional[QtWidgets.QSystemTrayIcon] = None,\n        msecs: Optional[int] = None) -&gt; None:\n    \"\"\"Show tray message.\n\n    Args:\n        title (str): Title of message.\n        message (str): Content of message.\n        icon (QSystemTrayIcon.MessageIcon): Message's icon. Default is\n            Information icon, may differ by Qt version.\n        msecs (int): Duration of message visibility in milliseconds.\n            Default is 10000 msecs, may differ by Qt version.\n    \"\"\"\n    if self._tray_manager:\n        self._tray_manager.show_tray_message(title, message, icon, msecs)\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/interfaces.html#client.ayon_core.addon.interfaces.ITrayAddon.tray_exit","title":"<code>tray_exit()</code>  <code>abstractmethod</code>","text":"<p>Cleanup method which is executed on tray shutdown.</p> <p>This is place where all threads should be shut.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>@abstractmethod\ndef tray_exit(self) -&gt; None:\n    \"\"\"Cleanup method which is executed on tray shutdown.\n\n    This is place where all threads should be shut.\n\n    \"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/interfaces.html#client.ayon_core.addon.interfaces.ITrayAddon.tray_init","title":"<code>tray_init()</code>  <code>abstractmethod</code>","text":"<p>Initialization part of tray implementation.</p> <p>Triggered between <code>initialization</code> and <code>connect_with_addons</code>.</p> <p>This is where GUIs should be loaded or tray specific parts should be prepared</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>@abstractmethod\ndef tray_init(self) -&gt; None:\n    \"\"\"Initialization part of tray implementation.\n\n    Triggered between `initialization` and `connect_with_addons`.\n\n    This is where GUIs should be loaded or tray specific parts should be\n    prepared\n\n    \"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/interfaces.html#client.ayon_core.addon.interfaces.ITrayAddon.tray_menu","title":"<code>tray_menu(tray_menu)</code>  <code>abstractmethod</code>","text":"<p>Add addon's action to tray menu.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>@abstractmethod\ndef tray_menu(self, tray_menu: QtWidgets.QMenu) -&gt; None:\n    \"\"\"Add addon's action to tray menu.\"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/interfaces.html#client.ayon_core.addon.interfaces.ITrayAddon.tray_start","title":"<code>tray_start()</code>  <code>abstractmethod</code>","text":"<p>Start procedure in tray tool.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>@abstractmethod\ndef tray_start(self) -&gt; None:\n    \"\"\"Start procedure in tray tool.\"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/interfaces.html#client.ayon_core.addon.interfaces.ITrayService","title":"<code>ITrayService</code>","text":"<p>               Bases: <code>ITrayAddon</code></p> <p>Tray service Interface.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>class ITrayService(ITrayAddon):\n    \"\"\"Tray service Interface.\"\"\"\n    # Module's property\n    menu_action: QtWidgets.QAction = None\n\n    # Class properties\n    _services_submenu: QtWidgets.QMenu = None\n    _icon_failed: QtWidgets.QIcon = None\n    _icon_running: QtWidgets.QIcon = None\n    _icon_idle: QtWidgets.QIcon = None\n\n    @property\n    @abstractmethod\n    def label(self) -&gt; str:\n        \"\"\"Service label showed in menu.\"\"\"\n\n    # TODO (Illicit): be able to get any sort of information to show/print\n    # @abstractmethod\n    # def get_service_info(self):\n    #     pass\n\n    @staticmethod\n    def services_submenu(tray_menu: QtWidgets.QMenu) -&gt; QtWidgets.QMenu:\n        \"\"\"Get or create services submenu.\n\n        Returns:\n            QtWidgets.QMenu: Services submenu.\n\n        \"\"\"\n        if ITrayService._services_submenu is None:\n            from qtpy import QtWidgets\n\n            services_submenu = QtWidgets.QMenu(\"Services\", tray_menu)\n            services_submenu.menuAction().setVisible(False)\n            ITrayService._services_submenu = services_submenu\n        return ITrayService._services_submenu\n\n    @staticmethod\n    def add_service_action(action: QtWidgets.QAction) -&gt; None:\n        \"\"\"Add service action to services submenu.\"\"\"\n        ITrayService._services_submenu.addAction(action)\n        if not ITrayService._services_submenu.menuAction().isVisible():\n            ITrayService._services_submenu.menuAction().setVisible(True)\n\n    @staticmethod\n    def _load_service_icons() -&gt; None:\n        \"\"\"Load service icons.\"\"\"\n        from qtpy import QtGui\n\n        ITrayService._failed_icon = QtGui.QIcon(\n            resources.get_resource(\"icons\", \"circle_red.png\")\n        )\n        ITrayService._icon_running = QtGui.QIcon(\n            resources.get_resource(\"icons\", \"circle_green.png\")\n        )\n        ITrayService._icon_idle = QtGui.QIcon(\n            resources.get_resource(\"icons\", \"circle_orange.png\")\n        )\n\n    @staticmethod\n    def get_icon_running() -&gt; QtWidgets.QIcon:\n        \"\"\"Get running icon.\n\n        Returns:\n            QtWidgets.QIcon: Returns \"running\" icon.\n\n        \"\"\"\n        if ITrayService._icon_running is None:\n            ITrayService._load_service_icons()\n        return ITrayService._icon_running\n\n    @staticmethod\n    def get_icon_idle() -&gt; QtWidgets.QIcon:\n        \"\"\"Get idle icon.\n\n        Returns:\n            QtWidgets.QIcon: Returns \"idle\" icon.\n\n        \"\"\"\n        if ITrayService._icon_idle is None:\n            ITrayService._load_service_icons()\n        return ITrayService._icon_idle\n\n    @staticmethod\n    def get_icon_failed() -&gt; QtWidgets.QIcon:\n        \"\"\"Get failed icon.\n\n        Returns:\n            QtWidgets.QIcon: Returns \"failed\" icon.\n\n        \"\"\"\n        if ITrayService._icon_failed is None:\n            ITrayService._load_service_icons()\n        return ITrayService._icon_failed\n\n    def tray_menu(self, tray_menu: QtWidgets.QMenu) -&gt; None:\n        \"\"\"Add service to tray menu.\"\"\"\n        from qtpy import QtWidgets\n\n        action = QtWidgets.QAction(\n            self.label,\n            self.services_submenu(tray_menu)\n        )\n        self.menu_action = action\n\n        self.add_service_action(action)\n\n        self.set_service_running_icon()\n\n    def set_service_running_icon(self) -&gt; None:\n        \"\"\"Change icon of an QAction to green circle.\"\"\"\n        if self.menu_action:\n            self.menu_action.setIcon(self.get_icon_running())\n\n    def set_service_failed_icon(self) -&gt; None:\n        \"\"\"Change icon of an QAction to red circle.\"\"\"\n        if self.menu_action:\n            self.menu_action.setIcon(self.get_icon_failed())\n\n    def set_service_idle_icon(self) -&gt; None:\n        \"\"\"Change icon of an QAction to orange circle.\"\"\"\n        if self.menu_action:\n            self.menu_action.setIcon(self.get_icon_idle())\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/interfaces.html#client.ayon_core.addon.interfaces.ITrayService.label","title":"<code>label</code>  <code>abstractmethod</code> <code>property</code>","text":"<p>Service label showed in menu.</p>"},{"location":"autoapi/client/ayon_core/addon/interfaces.html#client.ayon_core.addon.interfaces.ITrayService.add_service_action","title":"<code>add_service_action(action)</code>  <code>staticmethod</code>","text":"<p>Add service action to services submenu.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>@staticmethod\ndef add_service_action(action: QtWidgets.QAction) -&gt; None:\n    \"\"\"Add service action to services submenu.\"\"\"\n    ITrayService._services_submenu.addAction(action)\n    if not ITrayService._services_submenu.menuAction().isVisible():\n        ITrayService._services_submenu.menuAction().setVisible(True)\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/interfaces.html#client.ayon_core.addon.interfaces.ITrayService.get_icon_failed","title":"<code>get_icon_failed()</code>  <code>staticmethod</code>","text":"<p>Get failed icon.</p> <p>Returns:</p> Type Description <code>QIcon</code> <p>QtWidgets.QIcon: Returns \"failed\" icon.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>@staticmethod\ndef get_icon_failed() -&gt; QtWidgets.QIcon:\n    \"\"\"Get failed icon.\n\n    Returns:\n        QtWidgets.QIcon: Returns \"failed\" icon.\n\n    \"\"\"\n    if ITrayService._icon_failed is None:\n        ITrayService._load_service_icons()\n    return ITrayService._icon_failed\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/interfaces.html#client.ayon_core.addon.interfaces.ITrayService.get_icon_idle","title":"<code>get_icon_idle()</code>  <code>staticmethod</code>","text":"<p>Get idle icon.</p> <p>Returns:</p> Type Description <code>QIcon</code> <p>QtWidgets.QIcon: Returns \"idle\" icon.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>@staticmethod\ndef get_icon_idle() -&gt; QtWidgets.QIcon:\n    \"\"\"Get idle icon.\n\n    Returns:\n        QtWidgets.QIcon: Returns \"idle\" icon.\n\n    \"\"\"\n    if ITrayService._icon_idle is None:\n        ITrayService._load_service_icons()\n    return ITrayService._icon_idle\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/interfaces.html#client.ayon_core.addon.interfaces.ITrayService.get_icon_running","title":"<code>get_icon_running()</code>  <code>staticmethod</code>","text":"<p>Get running icon.</p> <p>Returns:</p> Type Description <code>QIcon</code> <p>QtWidgets.QIcon: Returns \"running\" icon.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>@staticmethod\ndef get_icon_running() -&gt; QtWidgets.QIcon:\n    \"\"\"Get running icon.\n\n    Returns:\n        QtWidgets.QIcon: Returns \"running\" icon.\n\n    \"\"\"\n    if ITrayService._icon_running is None:\n        ITrayService._load_service_icons()\n    return ITrayService._icon_running\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/interfaces.html#client.ayon_core.addon.interfaces.ITrayService.services_submenu","title":"<code>services_submenu(tray_menu)</code>  <code>staticmethod</code>","text":"<p>Get or create services submenu.</p> <p>Returns:</p> Type Description <code>QMenu</code> <p>QtWidgets.QMenu: Services submenu.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>@staticmethod\ndef services_submenu(tray_menu: QtWidgets.QMenu) -&gt; QtWidgets.QMenu:\n    \"\"\"Get or create services submenu.\n\n    Returns:\n        QtWidgets.QMenu: Services submenu.\n\n    \"\"\"\n    if ITrayService._services_submenu is None:\n        from qtpy import QtWidgets\n\n        services_submenu = QtWidgets.QMenu(\"Services\", tray_menu)\n        services_submenu.menuAction().setVisible(False)\n        ITrayService._services_submenu = services_submenu\n    return ITrayService._services_submenu\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/interfaces.html#client.ayon_core.addon.interfaces.ITrayService.set_service_failed_icon","title":"<code>set_service_failed_icon()</code>","text":"<p>Change icon of an QAction to red circle.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>def set_service_failed_icon(self) -&gt; None:\n    \"\"\"Change icon of an QAction to red circle.\"\"\"\n    if self.menu_action:\n        self.menu_action.setIcon(self.get_icon_failed())\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/interfaces.html#client.ayon_core.addon.interfaces.ITrayService.set_service_idle_icon","title":"<code>set_service_idle_icon()</code>","text":"<p>Change icon of an QAction to orange circle.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>def set_service_idle_icon(self) -&gt; None:\n    \"\"\"Change icon of an QAction to orange circle.\"\"\"\n    if self.menu_action:\n        self.menu_action.setIcon(self.get_icon_idle())\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/interfaces.html#client.ayon_core.addon.interfaces.ITrayService.set_service_running_icon","title":"<code>set_service_running_icon()</code>","text":"<p>Change icon of an QAction to green circle.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>def set_service_running_icon(self) -&gt; None:\n    \"\"\"Change icon of an QAction to green circle.\"\"\"\n    if self.menu_action:\n        self.menu_action.setIcon(self.get_icon_running())\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/interfaces.html#client.ayon_core.addon.interfaces.ITrayService.tray_menu","title":"<code>tray_menu(tray_menu)</code>","text":"<p>Add service to tray menu.</p> Source code in <code>client/ayon_core/addon/interfaces.py</code> <pre><code>def tray_menu(self, tray_menu: QtWidgets.QMenu) -&gt; None:\n    \"\"\"Add service to tray menu.\"\"\"\n    from qtpy import QtWidgets\n\n    action = QtWidgets.QAction(\n        self.label,\n        self.services_submenu(tray_menu)\n    )\n    self.menu_action = action\n\n    self.add_service_action(action)\n\n    self.set_service_running_icon()\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/utils.html","title":"utils","text":""},{"location":"autoapi/client/ayon_core/addon/utils.html#client.ayon_core.addon.utils.ensure_addons_are_process_context_ready","title":"<code>ensure_addons_are_process_context_ready(process_context, addons_manager=None, exit_on_failure=True)</code>","text":"<p>Ensure all enabled addons are ready to be used in the given context.</p> <p>Call this method only in AYON launcher process and as first thing     to avoid possible clashes with preparation. For example 'QApplication'     should not be created.</p> Todos <p>Run all preparations and allow to \"ignore\" failed preparations.     Right now single addon can block using certain actions.</p> <p>Parameters:</p> Name Type Description Default <code>process_context</code> <code>ProcessContext</code> <p>The context in which the addons should be prepared.</p> required <code>addons_manager</code> <code>Optional[AddonsManager]</code> <p>The addons manager to use. If not provided, a new one will be created.</p> <code>None</code> <code>exit_on_failure</code> <code>bool</code> <p>If True, the process will exit if an error occurs. Defaults to True.</p> <code>True</code> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if all addons are ready, False otherwise.</p> Source code in <code>client/ayon_core/addon/utils.py</code> <pre><code>def ensure_addons_are_process_context_ready(\n    process_context: ProcessContext,\n    addons_manager: Optional[AddonsManager] = None,\n    exit_on_failure: bool = True,\n) -&gt; bool:\n    \"\"\"Ensure all enabled addons are ready to be used in the given context.\n\n    Call this method only in AYON launcher process and as first thing\n        to avoid possible clashes with preparation. For example 'QApplication'\n        should not be created.\n\n    Todos:\n        Run all preparations and allow to \"ignore\" failed preparations.\n            Right now single addon can block using certain actions.\n\n    Args:\n        process_context (ProcessContext): The context in which the\n            addons should be prepared.\n        addons_manager (Optional[AddonsManager]): The addons\n            manager to use. If not provided, a new one will be created.\n        exit_on_failure (bool, optional): If True, the process will exit\n            if an error occurs. Defaults to True.\n\n    Returns:\n        bool: True if all addons are ready, False otherwise.\n\n    \"\"\"\n    if addons_manager is None:\n        addons_manager = AddonsManager()\n\n    message = None\n    failed = False\n    use_detail = False\n    # Wrap the output in StringIO to capture it for details on fail\n    # - but in case stdout was invalid on start of process also store\n    #   the tracebacks\n    tracebacks = []\n    output = StringIO()\n    with contextlib.redirect_stdout(output):\n        with contextlib.redirect_stderr(output):\n            for addon in addons_manager.get_enabled_addons():\n                addon_failed = True\n                try:\n                    addon.ensure_is_process_ready(process_context)\n                    addon_failed = False\n                except ProcessPreparationError as exc:\n                    message = str(exc)\n                    print(f\"Addon preparation failed: '{addon.name}'\")\n                    print(message)\n\n                except BaseException:\n                    use_detail = True\n                    message = \"An unexpected error occurred.\"\n                    formatted_traceback = \"\".join(traceback.format_exception(\n                        *sys.exc_info()\n                    ))\n                    tracebacks.append(formatted_traceback)\n                    print(f\"Addon preparation failed: '{addon.name}'\")\n                    print(message)\n                    # Print the traceback so it is in the stdout\n                    print(formatted_traceback)\n\n                if addon_failed:\n                    failed = True\n                    break\n\n    output_str = output.getvalue()\n    # Print stdout/stderr to console as it was redirected\n    print(output_str)\n    if not failed:\n        if not process_context.headless:\n            _start_tray()\n        return True\n\n    detail = None\n    if use_detail:\n        # In case stdout was not captured, use the tracebacks as detail\n        if not output_str:\n            output_str = \"\\n\".join(tracebacks)\n        detail = output_str\n\n    _handle_error(process_context, message, detail)\n    if exit_on_failure:\n        sys.exit(1)\n    return False\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/utils.html#client.ayon_core.addon.utils.ensure_addons_are_process_ready","title":"<code>ensure_addons_are_process_ready(addon_name, addon_version, project_name=None, headless=None, *, addons_manager=None, exit_on_failure=True, **kwargs)</code>","text":"<p>Ensure all enabled addons are ready to be used in the given context.</p> <p>Call this method only in AYON launcher process and as first thing     to avoid possible clashes with preparation. For example 'QApplication'     should not be created.</p> <p>Parameters:</p> Name Type Description Default <code>addon_name</code> <code>str</code> <p>Addon name which triggered process.</p> required <code>addon_version</code> <code>str</code> <p>Addon version which triggered process.</p> required <code>project_name</code> <code>Optional[str]</code> <p>Project name. Can be filled in case process is triggered for specific project. Some addons can have different behavior based on project. Value is NOT autofilled.</p> <code>None</code> <code>headless</code> <code>Optional[bool]</code> <p>Is process running in headless mode. Value is filled with value based on state set in AYON launcher.</p> <code>None</code> <code>addons_manager</code> <code>Optional[AddonsManager]</code> <p>The addons manager to use. If not provided, a new one will be created.</p> <code>None</code> <code>exit_on_failure</code> <code>bool</code> <p>If True, the process will exit if an error occurs. Defaults to True.</p> <code>True</code> <code>kwargs</code> <p>The keyword arguments to pass to the ProcessContext.</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if all addons are ready, False otherwise.</p> Source code in <code>client/ayon_core/addon/utils.py</code> <pre><code>def ensure_addons_are_process_ready(\n    addon_name: str,\n    addon_version: str,\n    project_name: Optional[str] = None,\n    headless: Optional[bool] = None,\n    *,\n    addons_manager: Optional[AddonsManager] = None,\n    exit_on_failure: bool = True,\n    **kwargs,\n) -&gt; bool:\n    \"\"\"Ensure all enabled addons are ready to be used in the given context.\n\n    Call this method only in AYON launcher process and as first thing\n        to avoid possible clashes with preparation. For example 'QApplication'\n        should not be created.\n\n    Args:\n        addon_name (str): Addon name which triggered process.\n        addon_version (str): Addon version which triggered process.\n        project_name (Optional[str]): Project name. Can be filled in case\n            process is triggered for specific project. Some addons can have\n            different behavior based on project. Value is NOT autofilled.\n        headless (Optional[bool]): Is process running in headless mode. Value\n            is filled with value based on state set in AYON launcher.\n        addons_manager (Optional[AddonsManager]): The addons\n            manager to use. If not provided, a new one will be created.\n        exit_on_failure (bool, optional): If True, the process will exit\n            if an error occurs. Defaults to True.\n        kwargs: The keyword arguments to pass to the ProcessContext.\n\n    Returns:\n        bool: True if all addons are ready, False otherwise.\n\n    \"\"\"\n    context: ProcessContext = ProcessContext(\n        addon_name,\n        addon_version,\n        project_name,\n        headless,\n        **kwargs\n    )\n    return ensure_addons_are_process_context_ready(\n        context, addons_manager, exit_on_failure\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/addon/ui/index.html","title":"ui","text":""},{"location":"autoapi/client/ayon_core/addon/ui/process_ready_error.html","title":"process_ready_error","text":""},{"location":"autoapi/client/ayon_core/hooks/index.html","title":"hooks","text":""},{"location":"autoapi/client/ayon_core/hooks/pre_add_last_workfile_arg.html","title":"pre_add_last_workfile_arg","text":""},{"location":"autoapi/client/ayon_core/hooks/pre_add_last_workfile_arg.html#client.ayon_core.hooks.pre_add_last_workfile_arg.AddLastWorkfileToLaunchArgs","title":"<code>AddLastWorkfileToLaunchArgs</code>","text":"<p>               Bases: <code>PreLaunchHook</code></p> <p>Add last workfile path to launch arguments.</p> <p>This is not possible to do for all applications the same way. Checks 'start_last_workfile', if set to False, it will not open last workfile. This property is set explicitly in Launcher.</p> Source code in <code>client/ayon_core/hooks/pre_add_last_workfile_arg.py</code> <pre><code>class AddLastWorkfileToLaunchArgs(PreLaunchHook):\n    \"\"\"Add last workfile path to launch arguments.\n\n    This is not possible to do for all applications the same way.\n    Checks 'start_last_workfile', if set to False, it will not open last\n    workfile. This property is set explicitly in Launcher.\n    \"\"\"\n\n    # Execute after workfile template copy\n    order = 10\n    app_groups = {\n        \"3dsmax\", \"adsk_3dsmax\",\n        \"maya\",\n        \"nuke\",\n        \"nukex\",\n        \"hiero\",\n        \"houdini\",\n        \"nukestudio\",\n        \"fusion\",\n        \"blender\",\n        \"photoshop\",\n        \"tvpaint\",\n        \"substancepainter\",\n        \"substancedesigner\",\n        \"aftereffects\",\n        \"wrap\",\n        \"openrv\",\n        \"cinema4d\",\n        \"silhouette\",\n        \"gaffer\",\n        \"loki\",\n        \"marvelousdesigner\",\n    }\n    launch_types = {LaunchTypes.local}\n\n    def execute(self):\n        workfile_path = self.data.get(\"workfile_path\")\n        if not workfile_path:\n            if not self.data.get(\"start_last_workfile\"):\n                self.log.info(\"It is set to not start last workfile on start.\")\n                return\n\n            workfile_path = self.data.get(\"last_workfile_path\")\n            if not workfile_path:\n                self.log.warning(\"Last workfile was not collected.\")\n                return\n\n        if not os.path.exists(workfile_path):\n            self.log.info(\"Current context does not have any workfile yet.\")\n            return\n\n        # Add path to workfile to arguments\n        self.launch_context.launch_args.append(workfile_path)\n</code></pre>"},{"location":"autoapi/client/ayon_core/hooks/pre_copy_template_workfile.html","title":"pre_copy_template_workfile","text":""},{"location":"autoapi/client/ayon_core/hooks/pre_copy_template_workfile.html#client.ayon_core.hooks.pre_copy_template_workfile.CopyTemplateWorkfile","title":"<code>CopyTemplateWorkfile</code>","text":"<p>               Bases: <code>PreLaunchHook</code></p> <p>Copy workfile template.</p> <p>This is not possible to do for all applications the same way.</p> <p>Prelaunch hook works only if last workfile leads to not existing file.     - That is possible only if it's first version.</p> Source code in <code>client/ayon_core/hooks/pre_copy_template_workfile.py</code> <pre><code>class CopyTemplateWorkfile(PreLaunchHook):\n    \"\"\"Copy workfile template.\n\n    This is not possible to do for all applications the same way.\n\n    Prelaunch hook works only if last workfile leads to not existing file.\n        - That is possible only if it's first version.\n    \"\"\"\n\n    # Before `AddLastWorkfileToLaunchArgs`\n    order = 0\n    app_groups = {\"blender\", \"photoshop\", \"tvpaint\", \"aftereffects\",\n                  \"wrap\"}\n    launch_types = {LaunchTypes.local}\n\n    def execute(self):\n        \"\"\"Check if can copy template for context and do it if possible.\n\n        First check if host for current project should create first workfile.\n        Second check is if template is reachable and can be copied.\n\n        Args:\n            last_workfile(str): Path where template will be copied.\n\n        Returns:\n            None: This is a void method.\n        \"\"\"\n\n        last_workfile = self.data.get(\"last_workfile_path\")\n        if not last_workfile:\n            self.log.warning((\n                \"Last workfile was not collected.\"\n                \" Can't add it to launch arguments or determine if should\"\n                \" copy template.\"\n            ))\n            return\n\n        if os.path.exists(last_workfile):\n            self.log.debug(\"Last workfile exists. Skipping {} process.\".format(\n                self.__class__.__name__\n            ))\n            return\n\n        self.log.info(\"Last workfile does not exist.\")\n\n        project_name = self.data[\"project_name\"]\n        folder_path = self.data[\"folder_path\"]\n        task_name = self.data[\"task_name\"]\n        host_name = self.application.host_name\n\n        project_settings = get_project_settings(project_name)\n\n        project_entity = self.data.get(\"project_entity\")\n        folder_entity = self.data.get(\"folder_entity\")\n        task_entity = self.data.get(\"task_entity\")\n        anatomy = self.data.get(\"anatomy\")\n        if project_entity and folder_entity and task_entity:\n            self.log.debug(\"Started filtering of custom template paths.\")\n            template_path = get_custom_workfile_template(\n                project_entity,\n                folder_entity,\n                task_entity,\n                host_name,\n                anatomy,\n                project_settings\n            )\n\n        else:\n            self.log.warning((\n                \"Global data collection probably did not execute.\"\n                \" Using backup solution.\"\n            ))\n            template_path = get_custom_workfile_template_by_string_context(\n                project_name,\n                folder_path,\n                task_name,\n                host_name,\n                anatomy,\n                project_settings\n            )\n\n        if not template_path:\n            self.log.info(\n                \"Registered custom templates didn't match current context.\"\n            )\n            return\n\n        if not os.path.exists(template_path):\n            self.log.warning(\n                \"Couldn't find workfile template file \\\"{}\\\"\".format(\n                    template_path\n                )\n            )\n            return\n\n        self.log.info(\n            f\"Creating workfile from template: \\\"{template_path}\\\"\"\n        )\n\n        # Keep extension of template file\n        _, ext = os.path.splitext(template_path)\n        last_workfile_extless, _ = os.path.splitext(last_workfile)\n        last_workfile = f\"{last_workfile_extless}{ext}\"\n\n        # Copy template workfile to new destination\n        shutil.copy2(\n            os.path.normpath(template_path),\n            os.path.normpath(last_workfile)\n        )\n\n        # TODO Collect all the the information to hook data when last workfile\n        #   is being prepared\n        # NOTE Right now data needed to store workfile entity\n        #   (rootless path, version) are not available and has to be guessed\n        rootless_path = find_workfile_rootless_path(\n            last_workfile,\n            project_name,\n            folder_entity,\n            task_entity,\n            host_name,\n            project_entity=project_entity,\n            project_settings=project_settings,\n            anatomy=anatomy,\n        )\n        template_key = get_workfile_template_key(\n            project_name,\n            task_entity[\"taskType\"],\n            host_name,\n            project_settings=project_settings,\n        )\n        # Find last workfile\n        file_template = anatomy.get_template_item(\n            \"work\", template_key, \"file\"\n        ).template\n        template_data = get_template_data(\n            project_entity,\n            folder_entity,\n            task_entity,\n            host_name,\n            settings=project_settings,\n        )\n        _, version = get_last_workfile_with_version_from_paths(\n            [last_workfile],\n            file_template,\n            template_data,\n            {ext},\n        )\n        save_workfile_info(\n            project_name,\n            task_entity[\"id\"],\n            rootless_path,\n            host_name,\n            version=version,\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/hooks/pre_copy_template_workfile.html#client.ayon_core.hooks.pre_copy_template_workfile.CopyTemplateWorkfile.execute","title":"<code>execute()</code>","text":"<p>Check if can copy template for context and do it if possible.</p> <p>First check if host for current project should create first workfile. Second check is if template is reachable and can be copied.</p> <p>Parameters:</p> Name Type Description Default <code>last_workfile</code> <code>str</code> <p>Path where template will be copied.</p> required <p>Returns:</p> Name Type Description <code>None</code> <p>This is a void method.</p> Source code in <code>client/ayon_core/hooks/pre_copy_template_workfile.py</code> <pre><code>def execute(self):\n    \"\"\"Check if can copy template for context and do it if possible.\n\n    First check if host for current project should create first workfile.\n    Second check is if template is reachable and can be copied.\n\n    Args:\n        last_workfile(str): Path where template will be copied.\n\n    Returns:\n        None: This is a void method.\n    \"\"\"\n\n    last_workfile = self.data.get(\"last_workfile_path\")\n    if not last_workfile:\n        self.log.warning((\n            \"Last workfile was not collected.\"\n            \" Can't add it to launch arguments or determine if should\"\n            \" copy template.\"\n        ))\n        return\n\n    if os.path.exists(last_workfile):\n        self.log.debug(\"Last workfile exists. Skipping {} process.\".format(\n            self.__class__.__name__\n        ))\n        return\n\n    self.log.info(\"Last workfile does not exist.\")\n\n    project_name = self.data[\"project_name\"]\n    folder_path = self.data[\"folder_path\"]\n    task_name = self.data[\"task_name\"]\n    host_name = self.application.host_name\n\n    project_settings = get_project_settings(project_name)\n\n    project_entity = self.data.get(\"project_entity\")\n    folder_entity = self.data.get(\"folder_entity\")\n    task_entity = self.data.get(\"task_entity\")\n    anatomy = self.data.get(\"anatomy\")\n    if project_entity and folder_entity and task_entity:\n        self.log.debug(\"Started filtering of custom template paths.\")\n        template_path = get_custom_workfile_template(\n            project_entity,\n            folder_entity,\n            task_entity,\n            host_name,\n            anatomy,\n            project_settings\n        )\n\n    else:\n        self.log.warning((\n            \"Global data collection probably did not execute.\"\n            \" Using backup solution.\"\n        ))\n        template_path = get_custom_workfile_template_by_string_context(\n            project_name,\n            folder_path,\n            task_name,\n            host_name,\n            anatomy,\n            project_settings\n        )\n\n    if not template_path:\n        self.log.info(\n            \"Registered custom templates didn't match current context.\"\n        )\n        return\n\n    if not os.path.exists(template_path):\n        self.log.warning(\n            \"Couldn't find workfile template file \\\"{}\\\"\".format(\n                template_path\n            )\n        )\n        return\n\n    self.log.info(\n        f\"Creating workfile from template: \\\"{template_path}\\\"\"\n    )\n\n    # Keep extension of template file\n    _, ext = os.path.splitext(template_path)\n    last_workfile_extless, _ = os.path.splitext(last_workfile)\n    last_workfile = f\"{last_workfile_extless}{ext}\"\n\n    # Copy template workfile to new destination\n    shutil.copy2(\n        os.path.normpath(template_path),\n        os.path.normpath(last_workfile)\n    )\n\n    # TODO Collect all the the information to hook data when last workfile\n    #   is being prepared\n    # NOTE Right now data needed to store workfile entity\n    #   (rootless path, version) are not available and has to be guessed\n    rootless_path = find_workfile_rootless_path(\n        last_workfile,\n        project_name,\n        folder_entity,\n        task_entity,\n        host_name,\n        project_entity=project_entity,\n        project_settings=project_settings,\n        anatomy=anatomy,\n    )\n    template_key = get_workfile_template_key(\n        project_name,\n        task_entity[\"taskType\"],\n        host_name,\n        project_settings=project_settings,\n    )\n    # Find last workfile\n    file_template = anatomy.get_template_item(\n        \"work\", template_key, \"file\"\n    ).template\n    template_data = get_template_data(\n        project_entity,\n        folder_entity,\n        task_entity,\n        host_name,\n        settings=project_settings,\n    )\n    _, version = get_last_workfile_with_version_from_paths(\n        [last_workfile],\n        file_template,\n        template_data,\n        {ext},\n    )\n    save_workfile_info(\n        project_name,\n        task_entity[\"id\"],\n        rootless_path,\n        host_name,\n        version=version,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/hooks/pre_create_extra_workdir_folders.html","title":"pre_create_extra_workdir_folders","text":""},{"location":"autoapi/client/ayon_core/hooks/pre_create_extra_workdir_folders.html#client.ayon_core.hooks.pre_create_extra_workdir_folders.CreateWorkdirExtraFolders","title":"<code>CreateWorkdirExtraFolders</code>","text":"<p>               Bases: <code>PreLaunchHook</code></p> <p>Create extra folders for the work directory.</p> <p>Based on setting <code>project_settings/global/tools/Workfiles/extra_folders</code> profile filtering will decide whether extra folders need to be created in the work directory.</p> Source code in <code>client/ayon_core/hooks/pre_create_extra_workdir_folders.py</code> <pre><code>class CreateWorkdirExtraFolders(PreLaunchHook):\n    \"\"\"Create extra folders for the work directory.\n\n    Based on setting `project_settings/global/tools/Workfiles/extra_folders`\n    profile filtering will decide whether extra folders need to be created in\n    the work directory.\n\n    \"\"\"\n\n    # Execute after workfile template copy\n    order = 15\n    launch_types = {LaunchTypes.local}\n\n    def execute(self):\n        if not self.application.is_host:\n            return\n\n        env = self.data.get(\"env\") or {}\n        workdir = env.get(\"AYON_WORKDIR\")\n        if not workdir or not os.path.exists(workdir):\n            return\n\n        host_name = self.application.host_name\n        task_type = self.data[\"task_type\"]\n        task_name = self.data[\"task_name\"]\n        project_name = self.data[\"project_name\"]\n\n        create_workdir_extra_folders(\n            workdir, host_name, task_type, task_name, project_name,\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/hooks/pre_filter_farm_environments.html","title":"pre_filter_farm_environments","text":""},{"location":"autoapi/client/ayon_core/hooks/pre_filter_farm_environments.html#client.ayon_core.hooks.pre_filter_farm_environments.FilterFarmEnvironments","title":"<code>FilterFarmEnvironments</code>","text":"<p>               Bases: <code>PreLaunchHook</code></p> <p>Filter or modify calculated environment variables for farm rendering.</p> <p>This hook must run last, only after all other hooks are finished to get correct environment for launch context.</p> <p>Implemented modifications to self.launch_context.env: - skipping (list) of environment variable keys - removing value in environment variable:     - supports regular expression in pattern</p> Source code in <code>client/ayon_core/hooks/pre_filter_farm_environments.py</code> <pre><code>class FilterFarmEnvironments(PreLaunchHook):\n    \"\"\"Filter or modify calculated environment variables for farm rendering.\n\n    This hook must run last, only after all other hooks are finished to get\n    correct environment for launch context.\n\n    Implemented modifications to self.launch_context.env:\n    - skipping (list) of environment variable keys\n    - removing value in environment variable:\n        - supports regular expression in pattern\n    \"\"\"\n    order = 1000\n\n    launch_types = {LaunchTypes.farm_publish}\n\n    def execute(self):\n        data = self.launch_context.data\n        project_settings = data[\"project_settings\"]\n        filter_env_profiles = (\n            project_settings[\"core\"][\"filter_env_profiles\"])\n\n        if not filter_env_profiles:\n            self.log.debug(\"No profiles found for env var filtering\")\n            return\n\n        task_entity = data[\"task_entity\"]\n\n        filter_data = {\n            \"host_names\": self.host_name,\n            \"task_types\": task_entity[\"taskType\"],\n            \"task_names\": task_entity[\"name\"],\n            \"folder_paths\": data[\"folder_path\"]\n        }\n        matching_profile = filter_profiles(\n            filter_env_profiles, filter_data, logger=self.log\n        )\n        if not matching_profile:\n            self.log.debug(\"No matching profile found for env var filtering \"\n                           f\"for {filter_data}\")\n            return\n\n        self._skip_environment_variables(\n            self.launch_context.env, matching_profile)\n\n        self._modify_environment_variables(\n            self.launch_context.env, matching_profile)\n\n    def _modify_environment_variables(self, calculated_env, matching_profile):\n        \"\"\"Modify environment variable values.\"\"\"\n        for env_item in matching_profile[\"replace_in_environment\"]:\n            key = env_item[\"environment_key\"]\n            value = calculated_env.get(key)\n            if not value:\n                continue\n\n            value = re.sub(value, env_item[\"pattern\"], env_item[\"replacement\"])\n            if value:\n                calculated_env[key] = value\n            else:\n                calculated_env.pop(key)\n\n    def _skip_environment_variables(self, calculated_env, matching_profile):\n        \"\"\"Skips list of environment variable names\"\"\"\n        for skip_env in matching_profile[\"skip_env_keys\"]:\n            self.log.info(f\"Skipping {skip_env}\")\n            calculated_env.pop(skip_env)\n</code></pre>"},{"location":"autoapi/client/ayon_core/hooks/pre_global_host_data.html","title":"pre_global_host_data","text":""},{"location":"autoapi/client/ayon_core/hooks/pre_global_host_data.html#client.ayon_core.hooks.pre_global_host_data.GlobalHostDataHook","title":"<code>GlobalHostDataHook</code>","text":"<p>               Bases: <code>PreLaunchHook</code></p> Source code in <code>client/ayon_core/hooks/pre_global_host_data.py</code> <pre><code>class GlobalHostDataHook(PreLaunchHook):\n    order = -100\n    launch_types = set()\n\n    def execute(self):\n        \"\"\"Prepare global objects to `data` that will be used for sure.\"\"\"\n        self.prepare_global_data()\n\n        if not self.data.get(\"folder_entity\"):\n            return\n\n        app = self.launch_context.application\n        temp_data = EnvironmentPrepData({\n            \"project_name\": self.data[\"project_name\"],\n            \"folder_path\": self.data[\"folder_path\"],\n            \"task_name\": self.data[\"task_name\"],\n\n            \"app\": app,\n\n            \"project_entity\": self.data[\"project_entity\"],\n            \"folder_entity\": self.data.get(\"folder_entity\"),\n            \"task_entity\": self.data.get(\"task_entity\"),\n\n            \"anatomy\": self.data[\"anatomy\"],\n\n            \"env\": self.launch_context.env,\n\n            \"start_last_workfile\": self.data.get(\"start_last_workfile\"),\n            \"last_workfile_path\": self.data.get(\"last_workfile_path\"),\n\n            \"log\": self.log\n        })\n\n        prepare_app_environments(temp_data, self.launch_context.env_group)\n        prepare_context_environments(temp_data)\n\n        temp_data.pop(\"log\")\n\n        self.data.update(temp_data)\n\n    def prepare_global_data(self):\n        \"\"\"Prepare global objects to `data` that will be used for sure.\"\"\"\n        # Mongo documents\n        project_name = self.data.get(\"project_name\")\n        if not project_name:\n            self.log.info(\n                \"Skipping global data preparation.\"\n                \" Key `project_name` was not found in launch context.\"\n            )\n            return\n\n        self.log.debug(\"Project name is set to \\\"{}\\\"\".format(project_name))\n\n        # Project Entity\n        project_entity = get_project(project_name)\n        self.data[\"project_entity\"] = project_entity\n\n        # Anatomy\n        try:\n            self.data[\"anatomy\"] = Anatomy(\n                project_name, project_entity=project_entity\n            )\n        except RootMissingEnv as exc:\n            raise ApplicationLaunchFailed(str(exc))\n\n        folder_path = self.data.get(\"folder_path\")\n        if not folder_path:\n            self.log.warning(\n                \"Folder path is not set. Skipping folder query.\"\n            )\n            return\n\n        folder_entity = get_folder_by_path(project_name, folder_path)\n        if not folder_entity:\n            raise ApplicationLaunchFailed(\n                f\"Folder '{folder_path}' not found\"\n                f\" in project '{project_name}'.\"\n            )\n\n        self.data[\"folder_entity\"] = folder_entity\n\n        task_name = self.data.get(\"task_name\")\n        if not task_name:\n            self.log.warning(\n                \"Task name is not set. Skipping task query.\"\n            )\n            return\n\n        if not folder_entity:\n            return\n\n        task_entity = get_task_by_name(\n            project_name, folder_entity[\"id\"], task_name\n        )\n        if not task_entity:\n            raise ApplicationLaunchFailed(\n                f\"Task '{task_name}' not found\"\n                f\" in '{project_name}' &gt; '{folder_path}'.\"\n            )\n\n        self.data[\"task_entity\"] = task_entity\n</code></pre>"},{"location":"autoapi/client/ayon_core/hooks/pre_global_host_data.html#client.ayon_core.hooks.pre_global_host_data.GlobalHostDataHook.execute","title":"<code>execute()</code>","text":"<p>Prepare global objects to <code>data</code> that will be used for sure.</p> Source code in <code>client/ayon_core/hooks/pre_global_host_data.py</code> <pre><code>def execute(self):\n    \"\"\"Prepare global objects to `data` that will be used for sure.\"\"\"\n    self.prepare_global_data()\n\n    if not self.data.get(\"folder_entity\"):\n        return\n\n    app = self.launch_context.application\n    temp_data = EnvironmentPrepData({\n        \"project_name\": self.data[\"project_name\"],\n        \"folder_path\": self.data[\"folder_path\"],\n        \"task_name\": self.data[\"task_name\"],\n\n        \"app\": app,\n\n        \"project_entity\": self.data[\"project_entity\"],\n        \"folder_entity\": self.data.get(\"folder_entity\"),\n        \"task_entity\": self.data.get(\"task_entity\"),\n\n        \"anatomy\": self.data[\"anatomy\"],\n\n        \"env\": self.launch_context.env,\n\n        \"start_last_workfile\": self.data.get(\"start_last_workfile\"),\n        \"last_workfile_path\": self.data.get(\"last_workfile_path\"),\n\n        \"log\": self.log\n    })\n\n    prepare_app_environments(temp_data, self.launch_context.env_group)\n    prepare_context_environments(temp_data)\n\n    temp_data.pop(\"log\")\n\n    self.data.update(temp_data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/hooks/pre_global_host_data.html#client.ayon_core.hooks.pre_global_host_data.GlobalHostDataHook.prepare_global_data","title":"<code>prepare_global_data()</code>","text":"<p>Prepare global objects to <code>data</code> that will be used for sure.</p> Source code in <code>client/ayon_core/hooks/pre_global_host_data.py</code> <pre><code>def prepare_global_data(self):\n    \"\"\"Prepare global objects to `data` that will be used for sure.\"\"\"\n    # Mongo documents\n    project_name = self.data.get(\"project_name\")\n    if not project_name:\n        self.log.info(\n            \"Skipping global data preparation.\"\n            \" Key `project_name` was not found in launch context.\"\n        )\n        return\n\n    self.log.debug(\"Project name is set to \\\"{}\\\"\".format(project_name))\n\n    # Project Entity\n    project_entity = get_project(project_name)\n    self.data[\"project_entity\"] = project_entity\n\n    # Anatomy\n    try:\n        self.data[\"anatomy\"] = Anatomy(\n            project_name, project_entity=project_entity\n        )\n    except RootMissingEnv as exc:\n        raise ApplicationLaunchFailed(str(exc))\n\n    folder_path = self.data.get(\"folder_path\")\n    if not folder_path:\n        self.log.warning(\n            \"Folder path is not set. Skipping folder query.\"\n        )\n        return\n\n    folder_entity = get_folder_by_path(project_name, folder_path)\n    if not folder_entity:\n        raise ApplicationLaunchFailed(\n            f\"Folder '{folder_path}' not found\"\n            f\" in project '{project_name}'.\"\n        )\n\n    self.data[\"folder_entity\"] = folder_entity\n\n    task_name = self.data.get(\"task_name\")\n    if not task_name:\n        self.log.warning(\n            \"Task name is not set. Skipping task query.\"\n        )\n        return\n\n    if not folder_entity:\n        return\n\n    task_entity = get_task_by_name(\n        project_name, folder_entity[\"id\"], task_name\n    )\n    if not task_entity:\n        raise ApplicationLaunchFailed(\n            f\"Task '{task_name}' not found\"\n            f\" in '{project_name}' &gt; '{folder_path}'.\"\n        )\n\n    self.data[\"task_entity\"] = task_entity\n</code></pre>"},{"location":"autoapi/client/ayon_core/hooks/pre_mac_launch.html","title":"pre_mac_launch","text":""},{"location":"autoapi/client/ayon_core/hooks/pre_mac_launch.html#client.ayon_core.hooks.pre_mac_launch.LaunchWithTerminal","title":"<code>LaunchWithTerminal</code>","text":"<p>               Bases: <code>PreLaunchHook</code></p> <p>Mac specific pre arguments for application.</p> <p>Mac applications should be launched using \"open\" argument which is internal callbacks to open executable. We also add argument \"-a\" to tell it's application open. This is used only for executables ending with \".app\". It is expected that these executables lead to app packages.</p> Source code in <code>client/ayon_core/hooks/pre_mac_launch.py</code> <pre><code>class LaunchWithTerminal(PreLaunchHook):\n    \"\"\"Mac specific pre arguments for application.\n\n    Mac applications should be launched using \"open\" argument which is internal\n    callbacks to open executable. We also add argument \"-a\" to tell it's\n    application open. This is used only for executables ending with \".app\". It\n    is expected that these executables lead to app packages.\n    \"\"\"\n    order = 1000\n\n    platforms = {\"darwin\"}\n    launch_types = {LaunchTypes.local}\n\n    def execute(self):\n        executable = str(self.launch_context.executable)\n        # Skip executables not ending with \".app\" or that are not folder\n        if not executable.endswith(\".app\") or not os.path.isdir(executable):\n            return\n\n        # Check if first argument match executable path\n        # - Few applications are not executed directly but through AYON\n        #   launcher process (Photoshop, AfterEffects, Harmony, ...).\n        #   These should not use `open`.\n        if self.launch_context.launch_args[0] != executable:\n            return\n\n        # Tell `open` to pass arguments if there are any\n        if len(self.launch_context.launch_args) &gt; 1:\n            self.launch_context.launch_args.insert(1, \"--args\")\n        # Prepend open arguments\n        self.launch_context.launch_args.insert(0, [\"open\", \"-na\"])\n</code></pre>"},{"location":"autoapi/client/ayon_core/hooks/pre_new_console_apps.html","title":"pre_new_console_apps","text":""},{"location":"autoapi/client/ayon_core/hooks/pre_new_console_apps.html#client.ayon_core.hooks.pre_new_console_apps.LaunchNewConsoleApps","title":"<code>LaunchNewConsoleApps</code>","text":"<p>               Bases: <code>PreLaunchHook</code></p> <p>Foundry applications have specific way how to launch them.</p> <p>Nuke is executed \"like\" python process so it is required to pass <code>CREATE_NEW_CONSOLE</code> flag on windows to trigger creation of new console. At the same time the newly created console won't create its own stdout and stderr handlers so they should not be redirected to DEVNULL.</p> Source code in <code>client/ayon_core/hooks/pre_new_console_apps.py</code> <pre><code>class LaunchNewConsoleApps(PreLaunchHook):\n    \"\"\"Foundry applications have specific way how to launch them.\n\n    Nuke is executed \"like\" python process so it is required to pass\n    `CREATE_NEW_CONSOLE` flag on windows to trigger creation of new console.\n    At the same time the newly created console won't create its own stdout\n    and stderr handlers so they should not be redirected to DEVNULL.\n    \"\"\"\n\n    # Should be as last hook because must change launch arguments to string\n    order = 1000\n    app_groups = {\n        \"nuke\", \"nukeassist\", \"nukex\", \"hiero\", \"nukestudio\", \"mayapy\"\n    }\n    platforms = {\"windows\"}\n    launch_types = {LaunchTypes.local}\n\n    def execute(self):\n        # Change `creationflags` to CREATE_NEW_CONSOLE\n        # - on Windows some apps will create new window using its console\n        # Set `stdout` and `stderr` to None so new created console does not\n        #   have redirected output to DEVNULL in build\n        self.launch_context.kwargs.update({\n            \"creationflags\": subprocess.CREATE_NEW_CONSOLE,\n            \"stdout\": None,\n            \"stderr\": None\n        })\n</code></pre>"},{"location":"autoapi/client/ayon_core/hooks/pre_ocio_hook.html","title":"pre_ocio_hook","text":""},{"location":"autoapi/client/ayon_core/hooks/pre_ocio_hook.html#client.ayon_core.hooks.pre_ocio_hook.OCIOEnvHook","title":"<code>OCIOEnvHook</code>","text":"<p>               Bases: <code>PreLaunchHook</code></p> <p>Set OCIO environment variable for hosts that use OpenColorIO.</p> Source code in <code>client/ayon_core/hooks/pre_ocio_hook.py</code> <pre><code>class OCIOEnvHook(PreLaunchHook):\n    \"\"\"Set OCIO environment variable for hosts that use OpenColorIO.\"\"\"\n\n    order = 0\n    hosts = {\n        \"substancepainter\",\n        \"substancedesigner\",\n        \"fusion\",\n        \"blender\",\n        \"aftereffects\",\n        \"max\",\n        \"houdini\",\n        \"maya\",\n        \"nuke\",\n        \"hiero\",\n        \"photoshop\",\n        \"resolve\",\n        \"openrv\",\n        \"cinema4d\",\n        \"silhouette\",\n        \"gaffer\",\n        \"loki\",\n    }\n    launch_types = set()\n\n    def execute(self):\n        \"\"\"Hook entry method.\"\"\"\n\n        task_entity = self.data.get(\"task_entity\")\n\n        if not task_entity:\n            self.log.info(\n                \"Skipping OCIO Environment preparation.\"\n                \"Task Entity is not available.\"\n            )\n            return\n\n        folder_entity = self.data[\"folder_entity\"]\n\n        template_data = get_template_data(\n            self.data[\"project_entity\"],\n            folder_entity=folder_entity,\n            task_entity=self.data[\"task_entity\"],\n            host_name=self.host_name,\n            settings=self.data[\"project_settings\"],\n        )\n\n        config_data = get_imageio_config_preset(\n            self.data[\"project_name\"],\n            self.data[\"folder_path\"],\n            self.data[\"task_name\"],\n            self.host_name,\n            self.app_name,\n            anatomy=self.data[\"anatomy\"],\n            project_settings=self.data[\"project_settings\"],\n            template_data=template_data,\n            env=self.launch_context.env,\n            folder_id=folder_entity[\"id\"],\n        )\n\n        if not config_data:\n            self.log.debug(\"OCIO not set or enabled\")\n            return\n\n        ocio_path = config_data[\"path\"]\n\n        if self.host_name in [\"nuke\", \"hiero\"]:\n            ocio_path = ocio_path.replace(\"\\\\\", \"/\")\n\n        self.log.info(\n            f\"Setting OCIO environment to config path: {ocio_path}\")\n\n        self.launch_context.env[\"OCIO\"] = ocio_path\n</code></pre>"},{"location":"autoapi/client/ayon_core/hooks/pre_ocio_hook.html#client.ayon_core.hooks.pre_ocio_hook.OCIOEnvHook.execute","title":"<code>execute()</code>","text":"<p>Hook entry method.</p> Source code in <code>client/ayon_core/hooks/pre_ocio_hook.py</code> <pre><code>def execute(self):\n    \"\"\"Hook entry method.\"\"\"\n\n    task_entity = self.data.get(\"task_entity\")\n\n    if not task_entity:\n        self.log.info(\n            \"Skipping OCIO Environment preparation.\"\n            \"Task Entity is not available.\"\n        )\n        return\n\n    folder_entity = self.data[\"folder_entity\"]\n\n    template_data = get_template_data(\n        self.data[\"project_entity\"],\n        folder_entity=folder_entity,\n        task_entity=self.data[\"task_entity\"],\n        host_name=self.host_name,\n        settings=self.data[\"project_settings\"],\n    )\n\n    config_data = get_imageio_config_preset(\n        self.data[\"project_name\"],\n        self.data[\"folder_path\"],\n        self.data[\"task_name\"],\n        self.host_name,\n        self.app_name,\n        anatomy=self.data[\"anatomy\"],\n        project_settings=self.data[\"project_settings\"],\n        template_data=template_data,\n        env=self.launch_context.env,\n        folder_id=folder_entity[\"id\"],\n    )\n\n    if not config_data:\n        self.log.debug(\"OCIO not set or enabled\")\n        return\n\n    ocio_path = config_data[\"path\"]\n\n    if self.host_name in [\"nuke\", \"hiero\"]:\n        ocio_path = ocio_path.replace(\"\\\\\", \"/\")\n\n    self.log.info(\n        f\"Setting OCIO environment to config path: {ocio_path}\")\n\n    self.launch_context.env[\"OCIO\"] = ocio_path\n</code></pre>"},{"location":"autoapi/client/ayon_core/hooks/pre_remove_launcher_paths.html","title":"pre_remove_launcher_paths","text":"<p>\"Pre launch hook to remove launcher paths from the system.</p>"},{"location":"autoapi/client/ayon_core/hooks/pre_remove_launcher_paths.html#client.ayon_core.hooks.pre_remove_launcher_paths.PreRemoveLauncherPaths","title":"<code>PreRemoveLauncherPaths</code>","text":"<p>               Bases: <code>PreLaunchHook</code></p> <p>Remove launcher paths from the system.</p> <p>This hook is used to remove launcher paths from the system before launching an application. It is used to ensure that the application is launched with the correct environment variables. Especially for Windows, where paths in <code>PATH</code> are used to load DLLs. This is important to avoid conflicts with other applications that may have the same DLLs in their paths.</p> Source code in <code>client/ayon_core/hooks/pre_remove_launcher_paths.py</code> <pre><code>class PreRemoveLauncherPaths(PreLaunchHook):\n    \"\"\"Remove launcher paths from the system.\n\n    This hook is used to remove launcher paths from the system before launching\n    an application. It is used to ensure that the application is launched with\n    the correct environment variables. Especially for Windows, where\n    paths in `PATH` are used to load DLLs. This is important to avoid\n    conflicts with other applications that may have the same DLLs in their\n    paths.\n    \"\"\"\n    order = 1\n\n    def execute(self) -&gt; None:\n        \"\"\"Execute the hook.\"\"\"\n        # Remove launcher paths from the system\n        ayon_root = os.path.normpath(os.environ[\"AYON_ROOT\"])\n\n        paths = [\n            path\n            for path in self.launch_context.env.get(\n                \"PATH\", \"\").split(os.pathsep)\n            if not os.path.normpath(path).startswith(ayon_root)\n        ]\n        self.launch_context.env[\"PATH\"] = os.pathsep.join(paths)\n</code></pre>"},{"location":"autoapi/client/ayon_core/hooks/pre_remove_launcher_paths.html#client.ayon_core.hooks.pre_remove_launcher_paths.PreRemoveLauncherPaths.execute","title":"<code>execute()</code>","text":"<p>Execute the hook.</p> Source code in <code>client/ayon_core/hooks/pre_remove_launcher_paths.py</code> <pre><code>def execute(self) -&gt; None:\n    \"\"\"Execute the hook.\"\"\"\n    # Remove launcher paths from the system\n    ayon_root = os.path.normpath(os.environ[\"AYON_ROOT\"])\n\n    paths = [\n        path\n        for path in self.launch_context.env.get(\n            \"PATH\", \"\").split(os.pathsep)\n        if not os.path.normpath(path).startswith(ayon_root)\n    ]\n    self.launch_context.env[\"PATH\"] = os.pathsep.join(paths)\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html","title":"host","text":""},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.AbstractHost","title":"<code>AbstractHost</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Abstract definition of host implementation.</p> Source code in <code>client/ayon_core/host/abstract.py</code> <pre><code>class AbstractHost(ABC):\n    \"\"\"Abstract definition of host implementation.\"\"\"\n    @property\n    @abstractmethod\n    def log(self) -&gt; logging.Logger:\n        pass\n\n    @property\n    @abstractmethod\n    def name(self) -&gt; str:\n        \"\"\"Host name.\"\"\"\n        pass\n\n    @abstractmethod\n    def get_app_information(self) -&gt; ApplicationInformation:\n        \"\"\"Information about the application where host is running.\n\n        Returns:\n            ApplicationInformation: Application information.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_current_context(self) -&gt; HostContextData:\n        \"\"\"Get the current context of the host.\n\n        Current context is defined by project name, folder path and task name.\n\n        Returns:\n            HostContextData: The current context of the host.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def set_current_context(\n        self,\n        folder_entity: dict[str, Any],\n        task_entity: dict[str, Any],\n        *,\n        reason: ContextChangeReason = ContextChangeReason.undefined,\n        project_entity: Optional[dict[str, Any]] = None,\n        anatomy: Optional[Anatomy] = None,\n    ) -&gt; HostContextData:\n        \"\"\"Change context of the host.\n\n        Args:\n            folder_entity (dict[str, Any]): Folder entity.\n            task_entity (dict[str, Any]): Task entity.\n            reason (ContextChangeReason): Reason for change.\n            project_entity (dict[str, Any]): Project entity.\n            anatomy (Anatomy): Anatomy entity.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_current_project_name(self) -&gt; str:\n        \"\"\"Get the current project name.\n\n        Returns:\n            Optional[str]: The current project name.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_current_folder_path(self) -&gt; Optional[str]:\n        \"\"\"Get the current folder path.\n\n        Returns:\n            Optional[str]: The current folder path.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_current_task_name(self) -&gt; Optional[str]:\n        \"\"\"Get the current task name.\n\n        Returns:\n            Optional[str]: The current task name.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_context_title(self) -&gt; str:\n        \"\"\"Get the context title used in UIs.\"\"\"\n        pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.AbstractHost.name","title":"<code>name</code>  <code>abstractmethod</code> <code>property</code>","text":"<p>Host name.</p>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.AbstractHost.get_app_information","title":"<code>get_app_information()</code>  <code>abstractmethod</code>","text":"<p>Information about the application where host is running.</p> <p>Returns:</p> Name Type Description <code>ApplicationInformation</code> <code>ApplicationInformation</code> <p>Application information.</p> Source code in <code>client/ayon_core/host/abstract.py</code> <pre><code>@abstractmethod\ndef get_app_information(self) -&gt; ApplicationInformation:\n    \"\"\"Information about the application where host is running.\n\n    Returns:\n        ApplicationInformation: Application information.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.AbstractHost.get_context_title","title":"<code>get_context_title()</code>  <code>abstractmethod</code>","text":"<p>Get the context title used in UIs.</p> Source code in <code>client/ayon_core/host/abstract.py</code> <pre><code>@abstractmethod\ndef get_context_title(self) -&gt; str:\n    \"\"\"Get the context title used in UIs.\"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.AbstractHost.get_current_context","title":"<code>get_current_context()</code>  <code>abstractmethod</code>","text":"<p>Get the current context of the host.</p> <p>Current context is defined by project name, folder path and task name.</p> <p>Returns:</p> Name Type Description <code>HostContextData</code> <code>HostContextData</code> <p>The current context of the host.</p> Source code in <code>client/ayon_core/host/abstract.py</code> <pre><code>@abstractmethod\ndef get_current_context(self) -&gt; HostContextData:\n    \"\"\"Get the current context of the host.\n\n    Current context is defined by project name, folder path and task name.\n\n    Returns:\n        HostContextData: The current context of the host.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.AbstractHost.get_current_folder_path","title":"<code>get_current_folder_path()</code>  <code>abstractmethod</code>","text":"<p>Get the current folder path.</p> <p>Returns:</p> Type Description <code>Optional[str]</code> <p>Optional[str]: The current folder path.</p> Source code in <code>client/ayon_core/host/abstract.py</code> <pre><code>@abstractmethod\ndef get_current_folder_path(self) -&gt; Optional[str]:\n    \"\"\"Get the current folder path.\n\n    Returns:\n        Optional[str]: The current folder path.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.AbstractHost.get_current_project_name","title":"<code>get_current_project_name()</code>  <code>abstractmethod</code>","text":"<p>Get the current project name.</p> <p>Returns:</p> Type Description <code>str</code> <p>Optional[str]: The current project name.</p> Source code in <code>client/ayon_core/host/abstract.py</code> <pre><code>@abstractmethod\ndef get_current_project_name(self) -&gt; str:\n    \"\"\"Get the current project name.\n\n    Returns:\n        Optional[str]: The current project name.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.AbstractHost.get_current_task_name","title":"<code>get_current_task_name()</code>  <code>abstractmethod</code>","text":"<p>Get the current task name.</p> <p>Returns:</p> Type Description <code>Optional[str]</code> <p>Optional[str]: The current task name.</p> Source code in <code>client/ayon_core/host/abstract.py</code> <pre><code>@abstractmethod\ndef get_current_task_name(self) -&gt; Optional[str]:\n    \"\"\"Get the current task name.\n\n    Returns:\n        Optional[str]: The current task name.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.AbstractHost.set_current_context","title":"<code>set_current_context(folder_entity, task_entity, *, reason=ContextChangeReason.undefined, project_entity=None, anatomy=None)</code>  <code>abstractmethod</code>","text":"<p>Change context of the host.</p> <p>Parameters:</p> Name Type Description Default <code>folder_entity</code> <code>dict[str, Any]</code> <p>Folder entity.</p> required <code>task_entity</code> <code>dict[str, Any]</code> <p>Task entity.</p> required <code>reason</code> <code>ContextChangeReason</code> <p>Reason for change.</p> <code>undefined</code> <code>project_entity</code> <code>dict[str, Any]</code> <p>Project entity.</p> <code>None</code> <code>anatomy</code> <code>Anatomy</code> <p>Anatomy entity.</p> <code>None</code> Source code in <code>client/ayon_core/host/abstract.py</code> <pre><code>@abstractmethod\ndef set_current_context(\n    self,\n    folder_entity: dict[str, Any],\n    task_entity: dict[str, Any],\n    *,\n    reason: ContextChangeReason = ContextChangeReason.undefined,\n    project_entity: Optional[dict[str, Any]] = None,\n    anatomy: Optional[Anatomy] = None,\n) -&gt; HostContextData:\n    \"\"\"Change context of the host.\n\n    Args:\n        folder_entity (dict[str, Any]): Folder entity.\n        task_entity (dict[str, Any]): Task entity.\n        reason (ContextChangeReason): Reason for change.\n        project_entity (dict[str, Any]): Project entity.\n        anatomy (Anatomy): Anatomy entity.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.ApplicationInformation","title":"<code>ApplicationInformation</code>  <code>dataclass</code>","text":"<p>Application information.</p> <p>Attributes:</p> Name Type Description <code>app_name</code> <code>Optional[str]</code> <p>Application name. e.g. Maya, NukeX, Nuke</p> <code>app_version</code> <code>Optional[str]</code> <p>Application version. e.g. 15.2.1</p> Source code in <code>client/ayon_core/host/abstract.py</code> <pre><code>@dataclass\nclass ApplicationInformation:\n    \"\"\"Application information.\n\n    Attributes:\n        app_name (Optional[str]): Application name. e.g. Maya, NukeX, Nuke\n        app_version (Optional[str]): Application version. e.g. 15.2.1\n\n    \"\"\"\n    app_name: Optional[str] = None\n    app_version: Optional[str] = None\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.ContextChangeReason","title":"<code>ContextChangeReason</code>","text":"<p>               Bases: <code>StrEnum</code></p> <p>Reasons for context change in the host.</p> Source code in <code>client/ayon_core/host/constants.py</code> <pre><code>class ContextChangeReason(StrEnum):\n    \"\"\"Reasons for context change in the host.\"\"\"\n    undefined = \"undefined\"\n    workfile_open = \"workfile.opened\"\n    workfile_save = \"workfile.saved\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.HostBase","title":"<code>HostBase</code>","text":"<p>               Bases: <code>AbstractHost</code></p> <p>Base of host implementation class.</p> <p>Host is pipeline implementation of DCC application. This class should help to identify what must/should/can be implemented for specific functionality.</p> <p>Compared to 'avalon' concept: What was before considered as functions in host implementation folder. The host implementation should primarily care about adding ability of creation (mark products to be published) and optionally about referencing published representations as containers.</p> <p>Host may need extend some functionality like working with workfiles or loading. Not all host implementations may allow that for those purposes can be logic extended with implementing functions for the purpose. There are prepared interfaces to be able identify what must be implemented to be able use that functionality. - current statement is that it is not required to inherit from interfaces     but all of the methods are validated (only their existence!)</p>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.HostBase--installation-of-host-before-avalon-concept","title":"Installation of host before (avalon concept):","text":"<pre><code>from ayon_core.pipeline import install_host\nimport ayon_core.hosts.maya.api as host\n\ninstall_host(host)\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.HostBase--installation-of-host-now","title":"Installation of host now:","text":"<pre><code>from ayon_core.pipeline import install_host\nfrom ayon_core.hosts.maya.api import MayaHost\n\nhost = MayaHost()\ninstall_host(host)\n</code></pre> Todo <ul> <li>move content of 'install_host' as method of this class<ul> <li>register host object</li> <li>install global plugin paths</li> </ul> </li> <li>store registered plugin paths to this object</li> <li>handle current context (project, asset, task)<ul> <li>this must be done in many separated steps</li> </ul> </li> <li>have it's object of host tools instead of using globals</li> </ul> <p>This implementation will probably change over time when more     functionality and responsibility will be added.</p> Source code in <code>client/ayon_core/host/host.py</code> <pre><code>class HostBase(AbstractHost):\n    \"\"\"Base of host implementation class.\n\n    Host is pipeline implementation of DCC application. This class should help\n    to identify what must/should/can be implemented for specific functionality.\n\n    Compared to 'avalon' concept:\n    What was before considered as functions in host implementation folder. The\n    host implementation should primarily care about adding ability of creation\n    (mark products to be published) and optionally about referencing published\n    representations as containers.\n\n    Host may need extend some functionality like working with workfiles\n    or loading. Not all host implementations may allow that for those purposes\n    can be logic extended with implementing functions for the purpose. There\n    are prepared interfaces to be able identify what must be implemented to\n    be able use that functionality.\n    - current statement is that it is not required to inherit from interfaces\n        but all of the methods are validated (only their existence!)\n\n    # Installation of host before (avalon concept):\n    ```python\n    from ayon_core.pipeline import install_host\n    import ayon_core.hosts.maya.api as host\n\n    install_host(host)\n    ```\n\n    # Installation of host now:\n    ```python\n    from ayon_core.pipeline import install_host\n    from ayon_core.hosts.maya.api import MayaHost\n\n    host = MayaHost()\n    install_host(host)\n    ```\n\n    Todo:\n        - move content of 'install_host' as method of this class\n            - register host object\n            - install global plugin paths\n        - store registered plugin paths to this object\n        - handle current context (project, asset, task)\n            - this must be done in many separated steps\n        - have it's object of host tools instead of using globals\n\n    This implementation will probably change over time when more\n        functionality and responsibility will be added.\n    \"\"\"\n\n    _log = None\n\n    def __init__(self):\n        \"\"\"Initialization of host.\n\n        Register DCC callbacks, host specific plugin paths, targets etc.\n        (Part of what 'install' did in 'avalon' concept.)\n\n        Note:\n            At this moment global \"installation\" must happen before host\n            installation. Because of this current limitation it is recommended\n            to implement 'install' method which is triggered after global\n            'install'.\n        \"\"\"\n\n        pass\n\n    def get_app_information(self) -&gt; ApplicationInformation:\n        \"\"\"Running application information.\n\n        Host integration should override this method and return correct\n            information.\n\n        Returns:\n            ApplicationInformation: Application information.\n\n        \"\"\"\n        return ApplicationInformation()\n\n    def install(self):\n        \"\"\"Install host specific functionality.\n\n        This is where should be added menu with tools, registered callbacks\n        and other host integration initialization.\n\n        It is called automatically when 'ayon_core.pipeline.install_host' is\n        triggered.\n\n        \"\"\"\n        pass\n\n    @property\n    def log(self) -&gt; logging.Logger:\n        if self._log is None:\n            self._log = logging.getLogger(self.__class__.__name__)\n        return self._log\n\n    def get_current_project_name(self) -&gt; str:\n        \"\"\"\n        Returns:\n            str: Current project name.\n\n        \"\"\"\n        return os.environ[\"AYON_PROJECT_NAME\"]\n\n    def get_current_folder_path(self) -&gt; Optional[str]:\n        \"\"\"\n        Returns:\n            Optional[str]: Current folder path.\n\n        \"\"\"\n        return os.environ.get(\"AYON_FOLDER_PATH\")\n\n    def get_current_task_name(self) -&gt; Optional[str]:\n        \"\"\"\n        Returns:\n            Optional[str]: Current task name.\n\n        \"\"\"\n        return os.environ.get(\"AYON_TASK_NAME\")\n\n    def get_current_context(self) -&gt; HostContextData:\n        \"\"\"Get current context information.\n\n        This method should be used to get current context of host. Usage of\n        this method can be crucial for host implementations in DCCs where\n        can be opened multiple workfiles at one moment and change of context\n        can't be caught properly.\n\n        Returns:\n            HostContextData: Current context with 'project_name',\n                'folder_path' and 'task_name'.\n\n        \"\"\"\n        return {\n            \"project_name\": self.get_current_project_name(),\n            \"folder_path\": self.get_current_folder_path(),\n            \"task_name\": self.get_current_task_name()\n        }\n\n    def set_current_context(\n        self,\n        folder_entity: dict[str, Any],\n        task_entity: dict[str, Any],\n        *,\n        reason: ContextChangeReason = ContextChangeReason.undefined,\n        project_entity: Optional[dict[str, Any]] = None,\n        anatomy: Optional[Anatomy] = None,\n    ) -&gt; HostContextData:\n        \"\"\"Set current context information.\n\n        This method should be used to set current context of host. Usage of\n        this method can be crucial for host implementations in DCCs where\n        can be opened multiple workfiles at one moment and change of context\n        can't be caught properly.\n\n        Notes:\n            This method should not care about change of workdir and expect any\n                of the arguments.\n\n        Args:\n            folder_entity (Optional[dict[str, Any]]): Folder entity.\n            task_entity (Optional[dict[str, Any]]): Task entity.\n            reason (ContextChangeReason): Reason for context change.\n            project_entity (Optional[dict[str, Any]]): Project entity data.\n            anatomy (Optional[Anatomy]): Anatomy instance for the project.\n\n        Returns:\n            dict[str, Optional[str]]: Context information with project name,\n                folder path and task name.\n\n        \"\"\"\n        from ayon_core.pipeline import Anatomy\n\n        folder_path = folder_entity[\"path\"]\n        task_name = task_entity[\"name\"]\n\n        context = self.get_current_context()\n        # Don't do anything if context did not change\n        if (\n            context[\"folder_path\"] == folder_path\n            and context[\"task_name\"] == task_name\n        ):\n            return context\n\n        project_name = self.get_current_project_name()\n        if project_entity is None:\n            project_entity = ayon_api.get_project(project_name)\n\n        if anatomy is None:\n            anatomy = Anatomy(project_name, project_entity=project_entity)\n\n        context_change_data = ContextChangeData(\n            project_entity,\n            folder_entity,\n            task_entity,\n            reason,\n            anatomy,\n        )\n        self._before_context_change(context_change_data)\n        self._set_current_context(context_change_data)\n        self._after_context_change(context_change_data)\n\n        return self._emit_context_change_event(\n            project_name,\n            folder_path,\n            task_name,\n        )\n\n    def get_context_title(self):\n        \"\"\"Context title shown for UI purposes.\n\n        Should return current context title if possible.\n\n        Note:\n            This method is used only for UI purposes so it is possible to\n                return some logical title for contextless cases.\n            Is not meant for \"Context menu\" label.\n\n        Returns:\n            str: Context title.\n            None: Default title is used based on UI implementation.\n        \"\"\"\n\n        # Use current context to fill the context title\n        current_context = self.get_current_context()\n        project_name = current_context[\"project_name\"]\n        folder_path = current_context[\"folder_path\"]\n        task_name = current_context[\"task_name\"]\n        items = []\n        if project_name:\n            items.append(project_name)\n            if folder_path:\n                items.append(folder_path.lstrip(\"/\"))\n                if task_name:\n                    items.append(task_name)\n        if items:\n            return \"/\".join(items)\n        return None\n\n    @contextlib.contextmanager\n    def maintained_selection(self):\n        \"\"\"Some functionlity will happen but selection should stay same.\n\n        This is DCC specific. Some may not allow to implement this ability\n        that is reason why default implementation is empty context manager.\n\n        Yields:\n            None: Yield when is ready to restore selected at the end.\n        \"\"\"\n\n        try:\n            yield\n        finally:\n            pass\n\n    def _emit_context_change_event(\n        self,\n        project_name: str,\n        folder_path: Optional[str],\n        task_name: Optional[str],\n    ) -&gt; HostContextData:\n        \"\"\"Emit context change event.\n\n        Args:\n            project_name (str): Name of the project.\n            folder_path (Optional[str]): Path of the folder.\n            task_name (Optional[str]): Name of the task.\n\n        Returns:\n            HostContextData: Data send to context change event.\n\n        \"\"\"\n        data: HostContextData = {\n            \"project_name\": project_name,\n            \"folder_path\": folder_path,\n            \"task_name\": task_name,\n        }\n        emit_event(\"taskChanged\", data)\n        return data\n\n    def _set_current_context(\n        self, context_change_data: ContextChangeData\n    ) -&gt; None:\n        \"\"\"Method that changes the context in host.\n\n        Can be overriden for hosts that do need different handling of context\n            than using environment variables.\n\n        Args:\n            context_change_data (ContextChangeData): Context change related\n                data.\n\n        \"\"\"\n        project_name = self.get_current_project_name()\n        folder_path = None\n        task_name = None\n        if context_change_data.folder_entity:\n            folder_path = context_change_data.folder_entity[\"path\"]\n            if context_change_data.task_entity:\n                task_name = context_change_data.task_entity[\"name\"]\n\n        envs = {\n            \"AYON_PROJECT_NAME\": project_name,\n            \"AYON_FOLDER_PATH\": folder_path,\n            \"AYON_TASK_NAME\": task_name,\n        }\n\n        # Update the Session and environments. Pop from environments all\n        #   keys with value set to None.\n        for key, value in envs.items():\n            if value is None:\n                os.environ.pop(key, None)\n            else:\n                os.environ[key] = value\n\n    def _before_context_change(self, context_change_data: ContextChangeData):\n        \"\"\"Before context is changed.\n\n        This method is called before the context is changed in the host.\n\n        Can be overridden to implement host specific logic.\n\n        Args:\n            context_change_data (ContextChangeData): Object with information\n                about context change.\n\n        \"\"\"\n        pass\n\n    def _after_context_change(self, context_change_data: ContextChangeData):\n        \"\"\"After context is changed.\n\n        This method is called after the context is changed in the host.\n\n        Can be overridden to implement host specific logic.\n\n        Args:\n            context_change_data (ContextChangeData): Object with information\n                about context change.\n\n        \"\"\"\n        pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.HostBase.__init__","title":"<code>__init__()</code>","text":"<p>Initialization of host.</p> <p>Register DCC callbacks, host specific plugin paths, targets etc. (Part of what 'install' did in 'avalon' concept.)</p> Note <p>At this moment global \"installation\" must happen before host installation. Because of this current limitation it is recommended to implement 'install' method which is triggered after global 'install'.</p> Source code in <code>client/ayon_core/host/host.py</code> <pre><code>def __init__(self):\n    \"\"\"Initialization of host.\n\n    Register DCC callbacks, host specific plugin paths, targets etc.\n    (Part of what 'install' did in 'avalon' concept.)\n\n    Note:\n        At this moment global \"installation\" must happen before host\n        installation. Because of this current limitation it is recommended\n        to implement 'install' method which is triggered after global\n        'install'.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.HostBase.get_app_information","title":"<code>get_app_information()</code>","text":"<p>Running application information.</p> <p>Host integration should override this method and return correct     information.</p> <p>Returns:</p> Name Type Description <code>ApplicationInformation</code> <code>ApplicationInformation</code> <p>Application information.</p> Source code in <code>client/ayon_core/host/host.py</code> <pre><code>def get_app_information(self) -&gt; ApplicationInformation:\n    \"\"\"Running application information.\n\n    Host integration should override this method and return correct\n        information.\n\n    Returns:\n        ApplicationInformation: Application information.\n\n    \"\"\"\n    return ApplicationInformation()\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.HostBase.get_context_title","title":"<code>get_context_title()</code>","text":"<p>Context title shown for UI purposes.</p> <p>Should return current context title if possible.</p> Note <p>This method is used only for UI purposes so it is possible to     return some logical title for contextless cases. Is not meant for \"Context menu\" label.</p> <p>Returns:</p> Name Type Description <code>str</code> <p>Context title.</p> <code>None</code> <p>Default title is used based on UI implementation.</p> Source code in <code>client/ayon_core/host/host.py</code> <pre><code>def get_context_title(self):\n    \"\"\"Context title shown for UI purposes.\n\n    Should return current context title if possible.\n\n    Note:\n        This method is used only for UI purposes so it is possible to\n            return some logical title for contextless cases.\n        Is not meant for \"Context menu\" label.\n\n    Returns:\n        str: Context title.\n        None: Default title is used based on UI implementation.\n    \"\"\"\n\n    # Use current context to fill the context title\n    current_context = self.get_current_context()\n    project_name = current_context[\"project_name\"]\n    folder_path = current_context[\"folder_path\"]\n    task_name = current_context[\"task_name\"]\n    items = []\n    if project_name:\n        items.append(project_name)\n        if folder_path:\n            items.append(folder_path.lstrip(\"/\"))\n            if task_name:\n                items.append(task_name)\n    if items:\n        return \"/\".join(items)\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.HostBase.get_current_context","title":"<code>get_current_context()</code>","text":"<p>Get current context information.</p> <p>This method should be used to get current context of host. Usage of this method can be crucial for host implementations in DCCs where can be opened multiple workfiles at one moment and change of context can't be caught properly.</p> <p>Returns:</p> Name Type Description <code>HostContextData</code> <code>HostContextData</code> <p>Current context with 'project_name', 'folder_path' and 'task_name'.</p> Source code in <code>client/ayon_core/host/host.py</code> <pre><code>def get_current_context(self) -&gt; HostContextData:\n    \"\"\"Get current context information.\n\n    This method should be used to get current context of host. Usage of\n    this method can be crucial for host implementations in DCCs where\n    can be opened multiple workfiles at one moment and change of context\n    can't be caught properly.\n\n    Returns:\n        HostContextData: Current context with 'project_name',\n            'folder_path' and 'task_name'.\n\n    \"\"\"\n    return {\n        \"project_name\": self.get_current_project_name(),\n        \"folder_path\": self.get_current_folder_path(),\n        \"task_name\": self.get_current_task_name()\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.HostBase.get_current_folder_path","title":"<code>get_current_folder_path()</code>","text":"<p>Returns:</p> Type Description <code>Optional[str]</code> <p>Optional[str]: Current folder path.</p> Source code in <code>client/ayon_core/host/host.py</code> <pre><code>def get_current_folder_path(self) -&gt; Optional[str]:\n    \"\"\"\n    Returns:\n        Optional[str]: Current folder path.\n\n    \"\"\"\n    return os.environ.get(\"AYON_FOLDER_PATH\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.HostBase.get_current_project_name","title":"<code>get_current_project_name()</code>","text":"<p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Current project name.</p> Source code in <code>client/ayon_core/host/host.py</code> <pre><code>def get_current_project_name(self) -&gt; str:\n    \"\"\"\n    Returns:\n        str: Current project name.\n\n    \"\"\"\n    return os.environ[\"AYON_PROJECT_NAME\"]\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.HostBase.get_current_task_name","title":"<code>get_current_task_name()</code>","text":"<p>Returns:</p> Type Description <code>Optional[str]</code> <p>Optional[str]: Current task name.</p> Source code in <code>client/ayon_core/host/host.py</code> <pre><code>def get_current_task_name(self) -&gt; Optional[str]:\n    \"\"\"\n    Returns:\n        Optional[str]: Current task name.\n\n    \"\"\"\n    return os.environ.get(\"AYON_TASK_NAME\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.HostBase.install","title":"<code>install()</code>","text":"<p>Install host specific functionality.</p> <p>This is where should be added menu with tools, registered callbacks and other host integration initialization.</p> <p>It is called automatically when 'ayon_core.pipeline.install_host' is triggered.</p> Source code in <code>client/ayon_core/host/host.py</code> <pre><code>def install(self):\n    \"\"\"Install host specific functionality.\n\n    This is where should be added menu with tools, registered callbacks\n    and other host integration initialization.\n\n    It is called automatically when 'ayon_core.pipeline.install_host' is\n    triggered.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.HostBase.maintained_selection","title":"<code>maintained_selection()</code>","text":"<p>Some functionlity will happen but selection should stay same.</p> <p>This is DCC specific. Some may not allow to implement this ability that is reason why default implementation is empty context manager.</p> <p>Yields:</p> Name Type Description <code>None</code> <p>Yield when is ready to restore selected at the end.</p> Source code in <code>client/ayon_core/host/host.py</code> <pre><code>@contextlib.contextmanager\ndef maintained_selection(self):\n    \"\"\"Some functionlity will happen but selection should stay same.\n\n    This is DCC specific. Some may not allow to implement this ability\n    that is reason why default implementation is empty context manager.\n\n    Yields:\n        None: Yield when is ready to restore selected at the end.\n    \"\"\"\n\n    try:\n        yield\n    finally:\n        pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.HostBase.set_current_context","title":"<code>set_current_context(folder_entity, task_entity, *, reason=ContextChangeReason.undefined, project_entity=None, anatomy=None)</code>","text":"<p>Set current context information.</p> <p>This method should be used to set current context of host. Usage of this method can be crucial for host implementations in DCCs where can be opened multiple workfiles at one moment and change of context can't be caught properly.</p> Notes <p>This method should not care about change of workdir and expect any     of the arguments.</p> <p>Parameters:</p> Name Type Description Default <code>folder_entity</code> <code>Optional[dict[str, Any]]</code> <p>Folder entity.</p> required <code>task_entity</code> <code>Optional[dict[str, Any]]</code> <p>Task entity.</p> required <code>reason</code> <code>ContextChangeReason</code> <p>Reason for context change.</p> <code>undefined</code> <code>project_entity</code> <code>Optional[dict[str, Any]]</code> <p>Project entity data.</p> <code>None</code> <code>anatomy</code> <code>Optional[Anatomy]</code> <p>Anatomy instance for the project.</p> <code>None</code> <p>Returns:</p> Type Description <code>HostContextData</code> <p>dict[str, Optional[str]]: Context information with project name, folder path and task name.</p> Source code in <code>client/ayon_core/host/host.py</code> <pre><code>def set_current_context(\n    self,\n    folder_entity: dict[str, Any],\n    task_entity: dict[str, Any],\n    *,\n    reason: ContextChangeReason = ContextChangeReason.undefined,\n    project_entity: Optional[dict[str, Any]] = None,\n    anatomy: Optional[Anatomy] = None,\n) -&gt; HostContextData:\n    \"\"\"Set current context information.\n\n    This method should be used to set current context of host. Usage of\n    this method can be crucial for host implementations in DCCs where\n    can be opened multiple workfiles at one moment and change of context\n    can't be caught properly.\n\n    Notes:\n        This method should not care about change of workdir and expect any\n            of the arguments.\n\n    Args:\n        folder_entity (Optional[dict[str, Any]]): Folder entity.\n        task_entity (Optional[dict[str, Any]]): Task entity.\n        reason (ContextChangeReason): Reason for context change.\n        project_entity (Optional[dict[str, Any]]): Project entity data.\n        anatomy (Optional[Anatomy]): Anatomy instance for the project.\n\n    Returns:\n        dict[str, Optional[str]]: Context information with project name,\n            folder path and task name.\n\n    \"\"\"\n    from ayon_core.pipeline import Anatomy\n\n    folder_path = folder_entity[\"path\"]\n    task_name = task_entity[\"name\"]\n\n    context = self.get_current_context()\n    # Don't do anything if context did not change\n    if (\n        context[\"folder_path\"] == folder_path\n        and context[\"task_name\"] == task_name\n    ):\n        return context\n\n    project_name = self.get_current_project_name()\n    if project_entity is None:\n        project_entity = ayon_api.get_project(project_name)\n\n    if anatomy is None:\n        anatomy = Anatomy(project_name, project_entity=project_entity)\n\n    context_change_data = ContextChangeData(\n        project_entity,\n        folder_entity,\n        task_entity,\n        reason,\n        anatomy,\n    )\n    self._before_context_change(context_change_data)\n    self._set_current_context(context_change_data)\n    self._after_context_change(context_change_data)\n\n    return self._emit_context_change_event(\n        project_name,\n        folder_path,\n        task_name,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.HostDirmap","title":"<code>HostDirmap</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Abstract class for running dirmap on a workfile in a host.</p> <p>Dirmap is used to translate paths inside of host workfile from one OS to another. (Eg. arstist created workfile on Win, different artists opens same file on Linux.)</p> Expects methods to be implemented inside of host <p>on_dirmap_enabled: run host code for enabling dirmap do_dirmap: run host code to do actual remapping</p> Source code in <code>client/ayon_core/host/dirmap.py</code> <pre><code>class HostDirmap(ABC):\n    \"\"\"Abstract class for running dirmap on a workfile in a host.\n\n    Dirmap is used to translate paths inside of host workfile from one\n    OS to another. (Eg. arstist created workfile on Win, different artists\n    opens same file on Linux.)\n\n    Expects methods to be implemented inside of host:\n        on_dirmap_enabled: run host code for enabling dirmap\n        do_dirmap: run host code to do actual remapping\n    \"\"\"\n\n    def __init__(\n        self,\n        host_name,\n        project_name,\n        project_settings=None,\n        sitesync_addon=None\n    ):\n        self.host_name = host_name\n        self.project_name = project_name\n        self._project_settings = project_settings\n        self._sitesync_addon = sitesync_addon\n        # to limit reinit of Modules\n        self._sitesync_addon_discovered = sitesync_addon is not None\n        self._log = None\n\n    @property\n    def sitesync_addon(self):\n        if not self._sitesync_addon_discovered:\n            self._sitesync_addon_discovered = True\n            manager = AddonsManager()\n            self._sitesync_addon = manager.get(\"sitesync\")\n        return self._sitesync_addon\n\n    @property\n    def project_settings(self):\n        if self._project_settings is None:\n            self._project_settings = get_project_settings(self.project_name)\n        return self._project_settings\n\n    @property\n    def log(self):\n        if self._log is None:\n            self._log = Logger.get_logger(self.__class__.__name__)\n        return self._log\n\n    @abstractmethod\n    def on_enable_dirmap(self):\n        \"\"\"Run host dependent operation for enabling dirmap if necessary.\"\"\"\n        pass\n\n    @abstractmethod\n    def dirmap_routine(self, source_path, destination_path):\n        \"\"\"Run host dependent remapping from source_path to destination_path\"\"\"\n        pass\n\n    def process_dirmap(self, mapping=None):\n        # type: (dict) -&gt; None\n        \"\"\"Go through all paths in Settings and set them using `dirmap`.\n\n            If artists has Site Sync enabled, take dirmap mapping directly from\n            Local Settings when artist is syncing workfile locally.\n\n        \"\"\"\n\n        if not mapping:\n            mapping = self.get_mappings()\n        if not mapping:\n            return\n\n        self.on_enable_dirmap()\n\n        for k, sp in enumerate(mapping[\"source_path\"]):\n            dst = mapping[\"destination_path\"][k]\n            try:\n                # add trailing slash if missing\n                sp = os.path.join(sp, '')\n                dst = os.path.join(dst, '')\n                print(\"{} -&gt; {}\".format(sp, dst))\n                self.dirmap_routine(sp, dst)\n            except IndexError:\n                # missing corresponding destination path\n                self.log.error((\n                    \"invalid dirmap mapping, missing corresponding\"\n                    \" destination directory.\"\n                ))\n                break\n            except RuntimeError:\n                self.log.error(\n                    \"invalid path {} -&gt; {}, mapping not registered\".format(\n                        sp, dst\n                    )\n                )\n                continue\n\n    def get_mappings(self):\n        \"\"\"Get translation from source_path to destination_path.\n\n            It checks if Site Sync is enabled and user chose to use local\n            site, in that case configuration in Local Settings takes precedence\n        \"\"\"\n        mapping_sett = self.project_settings[self.host_name].get(\"dirmap\", {})\n        local_mapping = self._get_local_sync_dirmap()\n        mapping_enabled = mapping_sett.get(\"enabled\") or bool(local_mapping)\n        if not mapping_enabled:\n            return {}\n\n        mapping = (\n            local_mapping\n            or mapping_sett[\"paths\"]\n            or {}\n        )\n\n        if (\n            not mapping\n            or not mapping.get(\"destination_path\")\n            or not mapping.get(\"source_path\")\n        ):\n            return {}\n        self.log.info(\"Processing directory mapping ...\")\n        self.log.info(\"mapping:: {}\".format(mapping))\n        return mapping\n\n    def _get_local_sync_dirmap(self):\n        \"\"\"\n            Returns dirmap if synch to local project is enabled.\n\n            Only valid mapping is from roots of remote site to local site set\n            in Local Settings.\n\n            Returns:\n                dict : { \"source_path\": [XXX], \"destination_path\": [YYYY]}\n        \"\"\"\n        project_name = self.project_name\n\n        sitesync_addon = self.sitesync_addon\n        mapping = {}\n        if (\n            sitesync_addon is None\n            or not sitesync_addon.enabled\n            or not sitesync_addon.is_project_enabled(project_name, True)\n        ):\n            return mapping\n\n        active_site = sitesync_addon.get_local_normalized_site(\n            sitesync_addon.get_active_site(project_name))\n        remote_site = sitesync_addon.get_local_normalized_site(\n            sitesync_addon.get_remote_site(project_name))\n        self.log.debug(\n            \"active {} - remote {}\".format(active_site, remote_site)\n        )\n\n        if active_site == \"local\" and active_site != remote_site:\n            sync_settings = sitesync_addon.get_sync_project_setting(\n                project_name,\n                exclude_locals=False,\n                cached=False)\n\n            # overrides for roots set in `Site Settings`\n            active_roots_overrides = self._get_site_root_overrides(\n                sitesync_addon, project_name, active_site)\n\n            remote_roots_overrides = self._get_site_root_overrides(\n                sitesync_addon, project_name, remote_site)\n\n            current_platform = platform.system().lower()\n            remote_provider = sitesync_addon.get_provider_for_site(\n                project_name, remote_site\n            )\n            # dirmap has sense only with regular disk provider, in the workfile\n            # won't be root on cloud or sftp provider so fallback to studio\n            if remote_provider != \"local_drive\":\n                remote_site = \"studio\"\n            for root_name, active_site_dir in active_roots_overrides.items():\n                remote_site_dir = (\n                    remote_roots_overrides.get(root_name)\n                    or sync_settings[\"sites\"][remote_site][\"root\"][root_name]\n                )\n\n                if isinstance(remote_site_dir, dict):\n                    remote_site_dir = remote_site_dir.get(current_platform)\n\n                if not remote_site_dir:\n                    continue\n\n                if os.path.isdir(active_site_dir):\n                    if \"destination_path\" not in mapping:\n                        mapping[\"destination_path\"] = []\n                    mapping[\"destination_path\"].append(active_site_dir)\n\n                    if \"source_path\" not in mapping:\n                        mapping[\"source_path\"] = []\n                    mapping[\"source_path\"].append(remote_site_dir)\n\n            self.log.debug(\"local sync mapping:: {}\".format(mapping))\n        return mapping\n\n    def _get_site_root_overrides(\n        self, sitesync_addon, project_name, site_name\n    ):\n        \"\"\"Safely handle root overrides.\n\n        SiteSync raises ValueError for non local or studio sites.\n        \"\"\"\n        # TODO: could be removed when `get_site_root_overrides` is not raising\n        #     an Error but just returns {}\n        try:\n            site_roots_overrides = sitesync_addon.get_site_root_overrides(\n                project_name, site_name)\n        except ValueError:\n            site_roots_overrides = {}\n        self.log.debug(\"{} roots overrides {}\".format(\n            site_name, site_roots_overrides))\n\n        return site_roots_overrides\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.HostDirmap.dirmap_routine","title":"<code>dirmap_routine(source_path, destination_path)</code>  <code>abstractmethod</code>","text":"<p>Run host dependent remapping from source_path to destination_path</p> Source code in <code>client/ayon_core/host/dirmap.py</code> <pre><code>@abstractmethod\ndef dirmap_routine(self, source_path, destination_path):\n    \"\"\"Run host dependent remapping from source_path to destination_path\"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.HostDirmap.get_mappings","title":"<code>get_mappings()</code>","text":"<p>Get translation from source_path to destination_path.</p> <p>It checks if Site Sync is enabled and user chose to use local site, in that case configuration in Local Settings takes precedence</p> Source code in <code>client/ayon_core/host/dirmap.py</code> <pre><code>def get_mappings(self):\n    \"\"\"Get translation from source_path to destination_path.\n\n        It checks if Site Sync is enabled and user chose to use local\n        site, in that case configuration in Local Settings takes precedence\n    \"\"\"\n    mapping_sett = self.project_settings[self.host_name].get(\"dirmap\", {})\n    local_mapping = self._get_local_sync_dirmap()\n    mapping_enabled = mapping_sett.get(\"enabled\") or bool(local_mapping)\n    if not mapping_enabled:\n        return {}\n\n    mapping = (\n        local_mapping\n        or mapping_sett[\"paths\"]\n        or {}\n    )\n\n    if (\n        not mapping\n        or not mapping.get(\"destination_path\")\n        or not mapping.get(\"source_path\")\n    ):\n        return {}\n    self.log.info(\"Processing directory mapping ...\")\n    self.log.info(\"mapping:: {}\".format(mapping))\n    return mapping\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.HostDirmap.on_enable_dirmap","title":"<code>on_enable_dirmap()</code>  <code>abstractmethod</code>","text":"<p>Run host dependent operation for enabling dirmap if necessary.</p> Source code in <code>client/ayon_core/host/dirmap.py</code> <pre><code>@abstractmethod\ndef on_enable_dirmap(self):\n    \"\"\"Run host dependent operation for enabling dirmap if necessary.\"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.HostDirmap.process_dirmap","title":"<code>process_dirmap(mapping=None)</code>","text":"<p>Go through all paths in Settings and set them using <code>dirmap</code>.</p> <p>If artists has Site Sync enabled, take dirmap mapping directly from Local Settings when artist is syncing workfile locally.</p> Source code in <code>client/ayon_core/host/dirmap.py</code> <pre><code>def process_dirmap(self, mapping=None):\n    # type: (dict) -&gt; None\n    \"\"\"Go through all paths in Settings and set them using `dirmap`.\n\n        If artists has Site Sync enabled, take dirmap mapping directly from\n        Local Settings when artist is syncing workfile locally.\n\n    \"\"\"\n\n    if not mapping:\n        mapping = self.get_mappings()\n    if not mapping:\n        return\n\n    self.on_enable_dirmap()\n\n    for k, sp in enumerate(mapping[\"source_path\"]):\n        dst = mapping[\"destination_path\"][k]\n        try:\n            # add trailing slash if missing\n            sp = os.path.join(sp, '')\n            dst = os.path.join(dst, '')\n            print(\"{} -&gt; {}\".format(sp, dst))\n            self.dirmap_routine(sp, dst)\n        except IndexError:\n            # missing corresponding destination path\n            self.log.error((\n                \"invalid dirmap mapping, missing corresponding\"\n                \" destination directory.\"\n            ))\n            break\n        except RuntimeError:\n            self.log.error(\n                \"invalid path {} -&gt; {}, mapping not registered\".format(\n                    sp, dst\n                )\n            )\n            continue\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.ILoadHost","title":"<code>ILoadHost</code>","text":"<p>               Bases: <code>AbstractHost</code></p> <p>Implementation requirements to be able use reference of representations.</p> <p>The load plugins can do referencing even without implementation of methods here, but switch and removement of containers would not be possible.</p> Questions <ul> <li>Is list container dependency of host or load plugins?</li> <li>Should this be directly in HostBase?<ul> <li>how to find out if referencing is available?</li> <li>do we need to know that?</li> </ul> </li> </ul> Source code in <code>client/ayon_core/host/interfaces/interfaces.py</code> <pre><code>class ILoadHost(AbstractHost):\n    \"\"\"Implementation requirements to be able use reference of representations.\n\n    The load plugins can do referencing even without implementation of methods\n    here, but switch and removement of containers would not be possible.\n\n    Questions:\n        - Is list container dependency of host or load plugins?\n        - Should this be directly in HostBase?\n            - how to find out if referencing is available?\n            - do we need to know that?\n    \"\"\"\n\n    @staticmethod\n    def get_missing_load_methods(host):\n        \"\"\"Look for missing methods on \"old type\" host implementation.\n\n        Method is used for validation of implemented functions related to\n        loading. Checks only existence of methods.\n\n        Args:\n            Union[ModuleType, AbstractHost]: Object of host where to look for\n                required methods.\n\n        Returns:\n            list[str]: Missing method implementations for loading workflow.\n        \"\"\"\n\n        if isinstance(host, ILoadHost):\n            return []\n\n        required = [\"ls\"]\n        missing = []\n        for name in required:\n            if not hasattr(host, name):\n                missing.append(name)\n        return missing\n\n    @staticmethod\n    def validate_load_methods(host):\n        \"\"\"Validate implemented methods of \"old type\" host for load workflow.\n\n        Args:\n            Union[ModuleType, AbstractHost]: Object of host to validate.\n\n        Raises:\n            MissingMethodsError: If there are missing methods on host\n                implementation.\n        \"\"\"\n        missing = ILoadHost.get_missing_load_methods(host)\n        if missing:\n            raise MissingMethodsError(host, missing)\n\n    @abstractmethod\n    def get_containers(self):\n        \"\"\"Retrieve referenced containers from scene.\n\n        This can be implemented in hosts where referencing can be used.\n\n        Todo:\n            Rename function to something more self explanatory.\n                Suggestion: 'get_containers'\n\n        Returns:\n            list[dict]: Information about loaded containers.\n        \"\"\"\n\n        pass\n\n    # --- Deprecated method names ---\n    def ls(self):\n        \"\"\"Deprecated variant of 'get_containers'.\n\n        Todo:\n            Remove when all usages are replaced.\n        \"\"\"\n\n        return self.get_containers()\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.ILoadHost.get_containers","title":"<code>get_containers()</code>  <code>abstractmethod</code>","text":"<p>Retrieve referenced containers from scene.</p> <p>This can be implemented in hosts where referencing can be used.</p> Todo <p>Rename function to something more self explanatory.     Suggestion: 'get_containers'</p> <p>Returns:</p> Type Description <p>list[dict]: Information about loaded containers.</p> Source code in <code>client/ayon_core/host/interfaces/interfaces.py</code> <pre><code>@abstractmethod\ndef get_containers(self):\n    \"\"\"Retrieve referenced containers from scene.\n\n    This can be implemented in hosts where referencing can be used.\n\n    Todo:\n        Rename function to something more self explanatory.\n            Suggestion: 'get_containers'\n\n    Returns:\n        list[dict]: Information about loaded containers.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.ILoadHost.get_missing_load_methods","title":"<code>get_missing_load_methods(host)</code>  <code>staticmethod</code>","text":"<p>Look for missing methods on \"old type\" host implementation.</p> <p>Method is used for validation of implemented functions related to loading. Checks only existence of methods.</p> <p>Parameters:</p> Name Type Description Default <code>Union[ModuleType, AbstractHost]</code> <p>Object of host where to look for required methods.</p> required <p>Returns:</p> Type Description <p>list[str]: Missing method implementations for loading workflow.</p> Source code in <code>client/ayon_core/host/interfaces/interfaces.py</code> <pre><code>@staticmethod\ndef get_missing_load_methods(host):\n    \"\"\"Look for missing methods on \"old type\" host implementation.\n\n    Method is used for validation of implemented functions related to\n    loading. Checks only existence of methods.\n\n    Args:\n        Union[ModuleType, AbstractHost]: Object of host where to look for\n            required methods.\n\n    Returns:\n        list[str]: Missing method implementations for loading workflow.\n    \"\"\"\n\n    if isinstance(host, ILoadHost):\n        return []\n\n    required = [\"ls\"]\n    missing = []\n    for name in required:\n        if not hasattr(host, name):\n            missing.append(name)\n    return missing\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.ILoadHost.ls","title":"<code>ls()</code>","text":"<p>Deprecated variant of 'get_containers'.</p> Todo <p>Remove when all usages are replaced.</p> Source code in <code>client/ayon_core/host/interfaces/interfaces.py</code> <pre><code>def ls(self):\n    \"\"\"Deprecated variant of 'get_containers'.\n\n    Todo:\n        Remove when all usages are replaced.\n    \"\"\"\n\n    return self.get_containers()\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.ILoadHost.validate_load_methods","title":"<code>validate_load_methods(host)</code>  <code>staticmethod</code>","text":"<p>Validate implemented methods of \"old type\" host for load workflow.</p> <p>Parameters:</p> Name Type Description Default <code>Union[ModuleType, AbstractHost]</code> <p>Object of host to validate.</p> required <p>Raises:</p> Type Description <code>MissingMethodsError</code> <p>If there are missing methods on host implementation.</p> Source code in <code>client/ayon_core/host/interfaces/interfaces.py</code> <pre><code>@staticmethod\ndef validate_load_methods(host):\n    \"\"\"Validate implemented methods of \"old type\" host for load workflow.\n\n    Args:\n        Union[ModuleType, AbstractHost]: Object of host to validate.\n\n    Raises:\n        MissingMethodsError: If there are missing methods on host\n            implementation.\n    \"\"\"\n    missing = ILoadHost.get_missing_load_methods(host)\n    if missing:\n        raise MissingMethodsError(host, missing)\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.INewPublisher","title":"<code>INewPublisher</code>","text":"<p>               Bases: <code>IPublishHost</code></p> <p>Legacy interface replaced by 'IPublishHost'.</p> Deprecated <p>'INewPublisher' is replaced by 'IPublishHost' please change your imports. There is no \"reasonable\" way hot mark these classes as deprecated to show warning of wrong import. Deprecated since 3.14. will be removed in 3.15.</p> Source code in <code>client/ayon_core/host/interfaces/interfaces.py</code> <pre><code>class INewPublisher(IPublishHost):\n    \"\"\"Legacy interface replaced by 'IPublishHost'.\n\n    Deprecated:\n        'INewPublisher' is replaced by 'IPublishHost' please change your\n        imports.\n        There is no \"reasonable\" way hot mark these classes as deprecated\n        to show warning of wrong import. Deprecated since 3.14.* will be\n        removed in 3.15.*\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.IPublishHost","title":"<code>IPublishHost</code>","text":"<p>               Bases: <code>AbstractHost</code></p> <p>Functions related to new creation system in new publisher.</p> <p>New publisher is not storing information only about each created instance but also some global data. At this moment are data related only to context publish plugins but that can extend in future.</p> Source code in <code>client/ayon_core/host/interfaces/interfaces.py</code> <pre><code>class IPublishHost(AbstractHost):\n    \"\"\"Functions related to new creation system in new publisher.\n\n    New publisher is not storing information only about each created instance\n    but also some global data. At this moment are data related only to context\n    publish plugins but that can extend in future.\n    \"\"\"\n\n    @staticmethod\n    def get_missing_publish_methods(host):\n        \"\"\"Look for missing methods on \"old type\" host implementation.\n\n        Method is used for validation of implemented functions related to\n        new publish creation. Checks only existence of methods.\n\n        Args:\n            Union[ModuleType, AbstractHost]: Host module where to look for\n                required methods.\n\n        Returns:\n            list[str]: Missing method implementations for new publisher\n                workflow.\n        \"\"\"\n\n        if isinstance(host, IPublishHost):\n            return []\n\n        required = [\n            \"get_context_data\",\n            \"update_context_data\",\n            \"get_context_title\",\n            \"get_current_context\",\n        ]\n        missing = []\n        for name in required:\n            if not hasattr(host, name):\n                missing.append(name)\n        return missing\n\n    @staticmethod\n    def validate_publish_methods(host):\n        \"\"\"Validate implemented methods of \"old type\" host.\n\n        Args:\n            Union[ModuleType, AbstractHost]: Host module to validate.\n\n        Raises:\n            MissingMethodsError: If there are missing methods on host\n                implementation.\n        \"\"\"\n        missing = IPublishHost.get_missing_publish_methods(host)\n        if missing:\n            raise MissingMethodsError(host, missing)\n\n    @abstractmethod\n    def get_context_data(self):\n        \"\"\"Get global data related to creation-publishing from workfile.\n\n        These data are not related to any created instance but to whole\n        publishing context. Not saving/returning them will cause that each\n        reset of publishing resets all values to default ones.\n\n        Context data can contain information about enabled/disabled publish\n        plugins or other values that can be filled by artist.\n\n        Returns:\n            dict: Context data stored using 'update_context_data'.\n        \"\"\"\n\n        pass\n\n    @abstractmethod\n    def update_context_data(self, data, changes):\n        \"\"\"Store global context data to workfile.\n\n        Called when some values in context data has changed.\n\n        Without storing the values in a way that 'get_context_data' would\n        return them will each reset of publishing cause loose of filled values\n        by artist. Best practice is to store values into workfile, if possible.\n\n        Args:\n            data (dict): New data as are.\n            changes (dict): Only data that has been changed. Each value has\n                tuple with '(&lt;old&gt;, &lt;new&gt;)' value.\n        \"\"\"\n\n        pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.IPublishHost.get_context_data","title":"<code>get_context_data()</code>  <code>abstractmethod</code>","text":"<p>Get global data related to creation-publishing from workfile.</p> <p>These data are not related to any created instance but to whole publishing context. Not saving/returning them will cause that each reset of publishing resets all values to default ones.</p> <p>Context data can contain information about enabled/disabled publish plugins or other values that can be filled by artist.</p> <p>Returns:</p> Name Type Description <code>dict</code> <p>Context data stored using 'update_context_data'.</p> Source code in <code>client/ayon_core/host/interfaces/interfaces.py</code> <pre><code>@abstractmethod\ndef get_context_data(self):\n    \"\"\"Get global data related to creation-publishing from workfile.\n\n    These data are not related to any created instance but to whole\n    publishing context. Not saving/returning them will cause that each\n    reset of publishing resets all values to default ones.\n\n    Context data can contain information about enabled/disabled publish\n    plugins or other values that can be filled by artist.\n\n    Returns:\n        dict: Context data stored using 'update_context_data'.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.IPublishHost.get_missing_publish_methods","title":"<code>get_missing_publish_methods(host)</code>  <code>staticmethod</code>","text":"<p>Look for missing methods on \"old type\" host implementation.</p> <p>Method is used for validation of implemented functions related to new publish creation. Checks only existence of methods.</p> <p>Parameters:</p> Name Type Description Default <code>Union[ModuleType, AbstractHost]</code> <p>Host module where to look for required methods.</p> required <p>Returns:</p> Type Description <p>list[str]: Missing method implementations for new publisher workflow.</p> Source code in <code>client/ayon_core/host/interfaces/interfaces.py</code> <pre><code>@staticmethod\ndef get_missing_publish_methods(host):\n    \"\"\"Look for missing methods on \"old type\" host implementation.\n\n    Method is used for validation of implemented functions related to\n    new publish creation. Checks only existence of methods.\n\n    Args:\n        Union[ModuleType, AbstractHost]: Host module where to look for\n            required methods.\n\n    Returns:\n        list[str]: Missing method implementations for new publisher\n            workflow.\n    \"\"\"\n\n    if isinstance(host, IPublishHost):\n        return []\n\n    required = [\n        \"get_context_data\",\n        \"update_context_data\",\n        \"get_context_title\",\n        \"get_current_context\",\n    ]\n    missing = []\n    for name in required:\n        if not hasattr(host, name):\n            missing.append(name)\n    return missing\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.IPublishHost.update_context_data","title":"<code>update_context_data(data, changes)</code>  <code>abstractmethod</code>","text":"<p>Store global context data to workfile.</p> <p>Called when some values in context data has changed.</p> <p>Without storing the values in a way that 'get_context_data' would return them will each reset of publishing cause loose of filled values by artist. Best practice is to store values into workfile, if possible.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict</code> <p>New data as are.</p> required <code>changes</code> <code>dict</code> <p>Only data that has been changed. Each value has tuple with '(, )' value. required Source code in <code>client/ayon_core/host/interfaces/interfaces.py</code> <pre><code>@abstractmethod\ndef update_context_data(self, data, changes):\n    \"\"\"Store global context data to workfile.\n\n    Called when some values in context data has changed.\n\n    Without storing the values in a way that 'get_context_data' would\n    return them will each reset of publishing cause loose of filled values\n    by artist. Best practice is to store values into workfile, if possible.\n\n    Args:\n        data (dict): New data as are.\n        changes (dict): Only data that has been changed. Each value has\n            tuple with '(&lt;old&gt;, &lt;new&gt;)' value.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.IPublishHost.validate_publish_methods","title":"<code>validate_publish_methods(host)</code>  <code>staticmethod</code>","text":"<p>Validate implemented methods of \"old type\" host.</p> <p>Parameters:</p> Name Type Description Default <code>Union[ModuleType, AbstractHost]</code> <p>Host module to validate.</p> required <p>Raises:</p> Type Description <code>MissingMethodsError</code> <p>If there are missing methods on host implementation.</p> Source code in <code>client/ayon_core/host/interfaces/interfaces.py</code> <pre><code>@staticmethod\ndef validate_publish_methods(host):\n    \"\"\"Validate implemented methods of \"old type\" host.\n\n    Args:\n        Union[ModuleType, AbstractHost]: Host module to validate.\n\n    Raises:\n        MissingMethodsError: If there are missing methods on host\n            implementation.\n    \"\"\"\n    missing = IPublishHost.get_missing_publish_methods(host)\n    if missing:\n        raise MissingMethodsError(host, missing)\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.IWorkfileHost","title":"<code>IWorkfileHost</code>","text":"<p>               Bases: <code>AbstractHost</code></p> <p>Implementation requirements to be able to use workfiles utils and tool.</p> <p>Some of the methods are pre-implemented as they generally do the same in     all host integrations.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>class IWorkfileHost(AbstractHost):\n    \"\"\"Implementation requirements to be able to use workfiles utils and tool.\n\n    Some of the methods are pre-implemented as they generally do the same in\n        all host integrations.\n\n    \"\"\"\n    # Can be changed to False if the host integration needs to change context\n    #   after workfile is opened, useful if the integration stores and\n    #   retrieves context to metadata of the file\n    change_context_before_workfile_open = True\n\n    @abstractmethod\n    def save_workfile(self, dst_path: Optional[str] = None) -&gt; None:\n        \"\"\"Save the currently opened scene.\n\n        Args:\n            dst_path (str): Where the current scene should be saved. Or use\n                the current path if 'None' is passed.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def open_workfile(self, filepath: str) -&gt; None:\n        \"\"\"Open passed filepath in the host.\n\n        Args:\n            filepath (str): Path to workfile.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_current_workfile(self) -&gt; Optional[str]:\n        \"\"\"Retrieve a path to current opened file.\n\n        Returns:\n            Optional[str]: Path to the file which is currently opened. None if\n                nothing is opened or the current workfile is unsaved.\n\n        \"\"\"\n        return None\n\n    def workfile_has_unsaved_changes(self) -&gt; Optional[bool]:\n        \"\"\"Currently opened scene is saved.\n\n        Not all hosts can know if the current scene is saved because the API\n            of DCC does not support it.\n\n        Returns:\n            Optional[bool]: True if scene is saved and False if has unsaved\n                modifications. None if can't tell if workfiles has\n                modifications.\n\n        \"\"\"\n        return None\n\n    def get_workfile_extensions(self) -&gt; list[str]:\n        \"\"\"Extensions that can be used to save the workfile to.\n\n        Notes:\n            Method may not be used if 'list_workfiles' and\n                'list_published_workfiles' are re-implemented with different\n                logic.\n\n        Returns:\n            list[str]: List of extensions that can be used for saving.\n\n        \"\"\"\n        return []\n\n    def save_workfile_with_context(\n        self,\n        filepath: str,\n        folder_entity: dict[str, Any],\n        task_entity: dict[str, Any],\n        *,\n        version: Optional[int] = None,\n        comment: Optional[str] = None,\n        description: Optional[str] = None,\n        prepared_data: Optional[SaveWorkfileOptionalData] = None,\n    ) -&gt; None:\n        \"\"\"Save the current workfile with context.\n\n        Arguments 'rootless_path', 'workfile_entities', 'project_entity'\n            and 'anatomy' can be filled to enhance efficiency if you already\n            have access to the values.\n\n        Argument 'project_settings' is used to calculate 'rootless_path'\n            if it is not provided.\n\n        Notes:\n            Should this method care about context change?\n\n        Args:\n            filepath (str): Where the current scene should be saved.\n            folder_entity (dict[str, Any]): Folder entity.\n            task_entity (dict[str, Any]): Task entity.\n            version (Optional[int]): Version of the workfile. Information\n                for workfile entity. Recommended to fill.\n            comment (Optional[str]): Comment for the workfile.\n                Usually used in the filename template.\n            description (Optional[str]): Artist note for the workfile entity.\n            prepared_data (Optional[SaveWorkfileOptionalData]): Prepared data\n                for speed enhancements.\n\n        \"\"\"\n        project_name = self.get_current_project_name()\n        save_workfile_context = get_save_workfile_context(\n            project_name,\n            filepath,\n            folder_entity,\n            task_entity,\n            host_name=self.name,\n            prepared_data=prepared_data,\n        )\n\n        self._before_workfile_save(save_workfile_context)\n        event_data = self._get_workfile_event_data(\n            project_name,\n            folder_entity,\n            task_entity,\n            filepath,\n        )\n        self._emit_workfile_save_event(event_data, after_save=False)\n\n        workdir = os.path.dirname(filepath)\n        if not os.path.exists(workdir):\n            os.makedirs(workdir, exist_ok=True)\n\n        # Set 'AYON_WORKDIR' environment variable\n        os.environ[\"AYON_WORKDIR\"] = workdir\n\n        self.set_current_context(\n            folder_entity,\n            task_entity,\n            reason=ContextChangeReason.workfile_save,\n            project_entity=save_workfile_context.project_entity,\n            anatomy=save_workfile_context.anatomy,\n        )\n\n        self.save_workfile(filepath)\n\n        self._save_workfile_entity(\n            save_workfile_context,\n            version,\n            comment,\n            description,\n        )\n        self._after_workfile_save(save_workfile_context)\n        self._emit_workfile_save_event(event_data)\n\n    def open_workfile_with_context(\n        self,\n        filepath: str,\n        folder_entity: dict[str, Any],\n        task_entity: dict[str, Any],\n        *,\n        prepared_data: Optional[OpenWorkfileOptionalData] = None,\n    ) -&gt; None:\n        \"\"\"Open passed filepath in the host with context.\n\n        This function should be used to open workfile in different context.\n\n        Notes:\n            Should this method care about context change?\n\n        Args:\n            filepath (str): Path to workfile.\n            folder_entity (dict[str, Any]): Folder id.\n            task_entity (dict[str, Any]): Task id.\n            prepared_data (Optional[WorkfileOptionalData]): Prepared data\n                for speed enhancements.\n\n        \"\"\"\n        context = self.get_current_context()\n        project_name = context[\"project_name\"]\n\n        open_workfile_context = get_open_workfile_context(\n            project_name,\n            filepath,\n            folder_entity,\n            task_entity,\n            prepared_data=prepared_data,\n        )\n\n        workdir = os.path.dirname(filepath)\n        # Set 'AYON_WORKDIR' environment variable\n        os.environ[\"AYON_WORKDIR\"] = workdir\n\n        event_data = self._get_workfile_event_data(\n            project_name, folder_entity, task_entity, filepath\n        )\n        self._before_workfile_open(open_workfile_context)\n        self._emit_workfile_open_event(event_data, after_open=False)\n\n        if self.change_context_before_workfile_open:\n            self.set_current_context(\n                folder_entity,\n                task_entity,\n                reason=ContextChangeReason.workfile_open,\n                project_entity=open_workfile_context.project_entity,\n                anatomy=open_workfile_context.anatomy,\n            )\n\n        self.open_workfile(filepath)\n\n        if not self.change_context_before_workfile_open:\n            self.set_current_context(\n                folder_entity,\n                task_entity,\n                reason=ContextChangeReason.workfile_open,\n                project_entity=open_workfile_context.project_entity,\n                anatomy=open_workfile_context.anatomy,\n            )\n\n        self._after_workfile_open(open_workfile_context)\n        self._emit_workfile_open_event(event_data)\n\n    def list_workfiles(\n        self,\n        project_name: str,\n        folder_entity: dict[str, Any],\n        task_entity: dict[str, Any],\n        *,\n        prepared_data: Optional[ListWorkfilesOptionalData] = None,\n    ) -&gt; list[WorkfileInfo]:\n        \"\"\"List workfiles in the given task.\n\n        The method should also return workfiles that are not available on\n            disk, but are in the AYON database.\n\n        Notes:\n        - Better method name?\n        - This method is pre-implemented as the logic can be shared across\n            95% of host integrations. Ad-hoc implementation to give host\n            integration workfile api functionality.\n\n        Args:\n            project_name (str): Project name.\n            folder_entity (dict[str, Any]): Folder entity.\n            task_entity (dict[str, Any]): Task entity.\n            prepared_data (Optional[ListWorkfilesOptionalData]): Prepared\n                data for speed enhancements.\n\n        Returns:\n            list[WorkfileInfo]: List of workfiles.\n\n        \"\"\"\n        from ayon_core.pipeline.template_data import get_template_data\n        from ayon_core.pipeline.workfile.path_resolving import (\n            get_workdir_with_workdir_data,\n            WorkfileDataParser,\n        )\n\n        extensions = self.get_workfile_extensions()\n        if not extensions:\n            return []\n\n        list_workfiles_context = get_list_workfiles_context(\n            project_name,\n            folder_entity,\n            task_entity,\n            host_name=self.name,\n            prepared_data=prepared_data,\n        )\n\n        workfile_entities_by_path = {}\n        for workfile_entity in list_workfiles_context.workfile_entities:\n            rootless_path = workfile_entity[\"path\"]\n            path = os.path.normpath(\n                list_workfiles_context.anatomy.fill_root(rootless_path)\n            )\n            workfile_entities_by_path[path] = workfile_entity\n\n        workdir_data = get_template_data(\n            list_workfiles_context.project_entity,\n            folder_entity,\n            task_entity,\n            host_name=self.name,\n        )\n        workdir = get_workdir_with_workdir_data(\n            workdir_data,\n            project_name,\n            anatomy=list_workfiles_context.anatomy,\n            template_key=list_workfiles_context.template_key,\n            project_settings=list_workfiles_context.project_settings,\n        )\n\n        file_template = list_workfiles_context.anatomy.get_template_item(\n            \"work\", list_workfiles_context.template_key, \"file\"\n        )\n        rootless_workdir = workdir.rootless\n        if platform.system().lower() == \"windows\":\n            rootless_workdir = rootless_workdir.replace(\"\\\\\", \"/\")\n\n        filenames = []\n        if os.path.exists(workdir):\n            filenames = list(os.listdir(workdir))\n\n        data_parser = WorkfileDataParser(file_template, workdir_data)\n        items = []\n        for filename in filenames:\n            # TODO add 'default' support for folders\n            ext = os.path.splitext(filename)[1].lower()\n            if ext not in extensions:\n                continue\n\n            filepath = os.path.join(workdir, filename)\n\n            rootless_path = f\"{rootless_workdir}/{filename}\"\n            workfile_entity = workfile_entities_by_path.pop(\n                filepath, None\n            )\n            version = comment = None\n            if workfile_entity is not None:\n                _data = workfile_entity[\"data\"]\n                version = _data.get(\"version\")\n                comment = _data.get(\"comment\")\n\n            if version is None:\n                parsed_data = data_parser.parse_data(filename)\n                version = parsed_data.version\n                comment = parsed_data.comment\n\n            item = WorkfileInfo.new(\n                filepath,\n                rootless_path,\n                version=version,\n                comment=comment,\n                available=True,\n                workfile_entity=workfile_entity,\n            )\n            items.append(item)\n\n        for filepath, workfile_entity in workfile_entities_by_path.items():\n            # Workfile entity is not in the filesystem\n            #   but it is in the database\n            rootless_path = workfile_entity[\"path\"]\n            ext = os.path.splitext(rootless_path)[1].lower()\n            if ext not in extensions:\n                continue\n\n            _data = workfile_entity[\"data\"]\n            version = _data.get(\"version\")\n            comment = _data.get(\"comment\")\n            if version is None:\n                filename = os.path.basename(rootless_path)\n                parsed_data = data_parser.parse_data(filename)\n                version = parsed_data.version\n                comment = parsed_data.comment\n\n            available = os.path.exists(filepath)\n            items.append(WorkfileInfo.new(\n                filepath,\n                rootless_path,\n                version=version,\n                comment=comment,\n                available=available,\n                workfile_entity=workfile_entity,\n            ))\n\n        return items\n\n    def list_published_workfiles(\n        self,\n        project_name: str,\n        folder_id: str,\n        *,\n        prepared_data: Optional[ListPublishedWorkfilesOptionalData] = None,\n    ) -&gt; list[PublishedWorkfileInfo]:\n        \"\"\"List published workfiles for the given folder.\n\n        The default implementation looks for products with the 'workfile'\n            product type.\n\n        Pre-fetched entities have mandatory fields to be fetched:\n            - Version: 'id', 'author', 'taskId'\n            - Representation: 'id', 'versionId', 'files'\n\n        Args:\n            project_name (str): Project name.\n            folder_id (str): Folder id.\n            prepared_data (Optional[ListPublishedWorkfilesOptionalData]):\n                Prepared data for speed enhancements.\n\n        Returns:\n            list[PublishedWorkfileInfo]: Published workfile information for\n                the given context.\n\n        \"\"\"\n        list_workfiles_context = get_list_published_workfiles_context(\n            project_name,\n            folder_id,\n            prepared_data=prepared_data,\n        )\n        if not list_workfiles_context.repre_entities:\n            return []\n\n        versions_by_id = {\n            version_entity[\"id\"]: version_entity\n            for version_entity in list_workfiles_context.version_entities\n        }\n        extensions = {\n            ext.lstrip(\".\")\n            for ext in self.get_workfile_extensions()\n        }\n        items = []\n        for repre_entity in list_workfiles_context.repre_entities:\n            version_id = repre_entity[\"versionId\"]\n            version_entity = versions_by_id[version_id]\n            task_id = version_entity[\"taskId\"]\n\n            # Filter by extension\n            workfile_path = None\n            for repre_file in repre_entity[\"files\"]:\n                ext = (\n                    os.path.splitext(repre_file[\"name\"])[1]\n                    .lower()\n                    .lstrip(\".\")\n                )\n                if ext in extensions:\n                    workfile_path = repre_file[\"path\"]\n                    break\n\n            if not workfile_path:\n                continue\n\n            try:\n                workfile_path = workfile_path.format(\n                    root=list_workfiles_context.anatomy.roots\n                )\n            except Exception:\n                self.log.warning(\n                    \"Failed to format workfile path.\", exc_info=True\n                )\n\n            is_available = False\n            file_size = file_modified = file_created = None\n            if workfile_path and os.path.exists(workfile_path):\n                filestat = os.stat(workfile_path)\n                is_available = True\n                file_size = filestat.st_size\n                file_created = filestat.st_ctime\n                file_modified = filestat.st_mtime\n\n            workfile_item = PublishedWorkfileInfo.new(\n                project_name,\n                folder_id,\n                task_id,\n                repre_entity,\n                filepath=workfile_path,\n                author=version_entity[\"author\"],\n                available=is_available,\n                file_size=file_size,\n                file_created=file_created,\n                file_modified=file_modified,\n            )\n            items.append(workfile_item)\n\n        return items\n\n    def copy_workfile(\n        self,\n        src_path: str,\n        dst_path: str,\n        folder_entity: dict[str, Any],\n        task_entity: dict[str, Any],\n        *,\n        version: Optional[int] = None,\n        comment: Optional[str] = None,\n        description: Optional[str] = None,\n        open_workfile: bool = True,\n        prepared_data: Optional[CopyWorkfileOptionalData] = None,\n    ) -&gt; None:\n        \"\"\"Save workfile path with target folder and task context.\n\n        It is expected that workfile is saved to the current project, but\n            can be copied from the other project.\n\n        Arguments 'rootless_path', 'workfile_entities', 'project_entity'\n            and 'anatomy' can be filled to enhance efficiency if you already\n            have access to the values.\n\n        Argument 'project_settings' is used to calculate 'rootless_path'\n            if it is not provided.\n\n        Args:\n            src_path (str): Path to the source scene.\n            dst_path (str): Where the scene should be saved.\n            folder_entity (dict[str, Any]): Folder entity.\n            task_entity (dict[str, Any]): Task entity.\n            version (Optional[int]): Version of the workfile. Information\n                for workfile entity. Recommended to fill.\n            comment (Optional[str]): Comment for the workfile.\n            description (Optional[str]): Artist note for the workfile entity.\n            open_workfile (bool): Open workfile when copied.\n            prepared_data (Optional[CopyWorkfileOptionalData]): Prepared data\n                for speed enhancements.\n\n        \"\"\"\n        project_name = self.get_current_project_name()\n        copy_workfile_context: CopyWorkfileContext = get_copy_workfile_context(\n            project_name,\n            src_path,\n            dst_path,\n            folder_entity,\n            task_entity,\n            version=version,\n            comment=comment,\n            description=description,\n            open_workfile=open_workfile,\n            host_name=self.name,\n            prepared_data=prepared_data,\n        )\n        self._copy_workfile(\n            copy_workfile_context,\n            version=version,\n            comment=comment,\n            description=description,\n            open_workfile=open_workfile,\n        )\n\n    def copy_workfile_representation(\n        self,\n        src_project_name: str,\n        src_representation_entity: dict[str, Any],\n        dst_path: str,\n        folder_entity: dict[str, Any],\n        task_entity: dict[str, Any],\n        *,\n        version: Optional[int] = None,\n        comment: Optional[str] = None,\n        description: Optional[str] = None,\n        open_workfile: bool = True,\n        prepared_data: Optional[CopyPublishedWorkfileOptionalData] = None,\n    ) -&gt; None:\n        \"\"\"Copy workfile representation.\n\n        Use representation as a source for the workfile.\n\n        Arguments 'rootless_path', 'workfile_entities', 'project_entity'\n            and 'anatomy' can be filled to enhance efficiency if you already\n            have access to the values.\n\n        Argument 'project_settings' is used to calculate 'rootless_path'\n            if it is not provided.\n\n        Args:\n            src_project_name (str): Project name.\n            src_representation_entity (dict[str, Any]): Representation\n                entity.\n            dst_path (str): Where the scene should be saved.\n            folder_entity (dict[str, Any): Folder entity.\n            task_entity (dict[str, Any]): Task entity.\n            version (Optional[int]): Version of the workfile. Information\n                for workfile entity. Recommended to fill.\n            comment (Optional[str]): Comment for the workfile.\n            description (Optional[str]): Artist note for the workfile entity.\n            open_workfile (bool): Open workfile when copied.\n            prepared_data (Optional[CopyPublishedWorkfileOptionalData]):\n                Prepared data for speed enhancements.\n\n        \"\"\"\n        project_name = self.get_current_project_name()\n        copy_repre_workfile_context: CopyPublishedWorkfileContext = (\n            get_copy_repre_workfile_context(\n                project_name,\n                src_project_name,\n                src_representation_entity,\n                dst_path,\n                folder_entity,\n                task_entity,\n                version=version,\n                comment=comment,\n                description=description,\n                open_workfile=open_workfile,\n                host_name=self.name,\n                prepared_data=prepared_data,\n            )\n        )\n        self._copy_workfile(\n            copy_repre_workfile_context,\n            version=version,\n            comment=comment,\n            description=description,\n            open_workfile=open_workfile,\n        )\n\n    # --- Deprecated method names ---\n    @deprecated(\"Use 'get_workfile_extensions' instead\")\n    def file_extensions(self):\n        \"\"\"Deprecated variant of 'get_workfile_extensions'.\n\n        Todo:\n            Remove when all usages are replaced.\n\n        \"\"\"\n        return self.get_workfile_extensions()\n\n    @deprecated(\"Use 'save_workfile' instead\")\n    def save_file(self, dst_path=None):\n        \"\"\"Deprecated variant of 'save_workfile'.\n\n        Todo:\n            Remove when all usages are replaced\n\n        \"\"\"\n        self.save_workfile(dst_path)\n\n    @deprecated(\"Use 'open_workfile' instead\")\n    def open_file(self, filepath):\n        \"\"\"Deprecated variant of 'open_workfile'.\n\n        Todo:\n            Remove when all usages are replaced.\n\n        \"\"\"\n        return self.open_workfile(filepath)\n\n    @deprecated(\"Use 'get_current_workfile' instead\")\n    def current_file(self):\n        \"\"\"Deprecated variant of 'get_current_workfile'.\n\n        Todo:\n            Remove when all usages are replaced.\n\n        \"\"\"\n        return self.get_current_workfile()\n\n    @deprecated(\"Use 'workfile_has_unsaved_changes' instead\")\n    def has_unsaved_changes(self):\n        \"\"\"Deprecated variant of 'workfile_has_unsaved_changes'.\n\n        Todo:\n            Remove when all usages are replaced.\n\n        \"\"\"\n        return self.workfile_has_unsaved_changes()\n\n    def _copy_workfile(\n        self,\n        copy_workfile_context: CopyWorkfileContext,\n        *,\n        version: Optional[int],\n        comment: Optional[str],\n        description: Optional[str],\n        open_workfile: bool,\n    ) -&gt; None:\n        \"\"\"Save workfile path with target folder and task context.\n\n        It is expected that workfile is saved to the current project, but\n            can be copied from the other project.\n\n        Arguments 'rootless_path', 'workfile_entities', 'project_entity'\n            and 'anatomy' can be filled to enhance efficiency if you already\n            have access to the values.\n\n        Argument 'project_settings' is used to calculate 'rootless_path'\n            if it is not provided.\n\n        Args:\n            copy_workfile_context (CopyWorkfileContext): Prepared data\n                for speed enhancements.\n            version (Optional[int]): Version of the workfile. Information\n                for workfile entity. Recommended to fill.\n            comment (Optional[str]): Comment for the workfile.\n            description (Optional[str]): Artist note for the workfile entity.\n            open_workfile (bool): Open workfile when copied.\n\n        \"\"\"\n        self._before_workfile_copy(copy_workfile_context)\n        event_data = self._get_workfile_event_data(\n            copy_workfile_context.project_name,\n            copy_workfile_context.folder_entity,\n            copy_workfile_context.task_entity,\n            copy_workfile_context.dst_path,\n        )\n        self._emit_workfile_save_event(event_data, after_save=False)\n\n        dst_dir = os.path.dirname(copy_workfile_context.dst_path)\n        if not os.path.exists(dst_dir):\n            os.makedirs(dst_dir, exist_ok=True)\n        shutil.copy(\n            copy_workfile_context.src_path,\n            copy_workfile_context.dst_path\n        )\n\n        self._save_workfile_entity(\n            copy_workfile_context,\n            version,\n            comment,\n            description,\n        )\n        self._after_workfile_copy(copy_workfile_context)\n        self._emit_workfile_save_event(event_data)\n\n        if not open_workfile:\n            return\n\n        self.open_workfile_with_context(\n            copy_workfile_context.dst_path,\n            copy_workfile_context.folder_entity,\n            copy_workfile_context.task_entity,\n        )\n\n    def _save_workfile_entity(\n        self,\n        save_workfile_context: SaveWorkfileContext,\n        version: Optional[int],\n        comment: Optional[str],\n        description: Optional[str],\n    ) -&gt; Optional[dict[str, Any]]:\n        \"\"\"Create of update workfile entity to AYON based on provided data.\n\n        Args:\n            save_workfile_context (SaveWorkfileContext): Save workfile\n                context with all prepared data.\n            version (Optional[int]): Version of the workfile.\n            comment (Optional[str]): Comment for the workfile.\n            description (Optional[str]): Artist note for the workfile entity.\n\n        Returns:\n            Optional[dict[str, Any]]: Workfile entity.\n\n        \"\"\"\n        from ayon_core.pipeline.workfile.utils import (\n            save_workfile_info\n        )\n\n        project_name = self.get_current_project_name()\n        if not description:\n            description = None\n\n        if not comment:\n            comment = None\n\n        rootless_path = save_workfile_context.rootless_path\n        # It is not possible to create workfile infor without rootless path\n        workfile_info = None\n        if not rootless_path:\n            return workfile_info\n\n        if platform.system().lower() == \"windows\":\n            rootless_path = rootless_path.replace(\"\\\\\", \"/\")\n\n        # Get application information\n        app_info = self.get_app_information()\n        data = {}\n        if app_info.app_name:\n            data[\"app_name\"] = app_info.app_name\n        if app_info.app_version:\n            data[\"app_version\"] = app_info.app_version\n\n        # Use app group and app variant from applications addon (if available)\n        app_addon_name = os.environ.get(\"AYON_APP_NAME\")\n        if not app_addon_name:\n            app_addon_name = None\n\n        app_addon_tools_s = os.environ.get(\"AYON_APP_TOOLS\")\n        app_addon_tools = []\n        if app_addon_tools_s:\n            app_addon_tools = app_addon_tools_s.split(\";\")\n\n        data[\"ayon_app_name\"] = app_addon_name\n        data[\"ayon_app_tools\"] = app_addon_tools\n\n        workfile_info = save_workfile_info(\n            project_name,\n            save_workfile_context.task_entity[\"id\"],\n            rootless_path,\n            self.name,\n            version,\n            comment,\n            description,\n            data=data,\n            workfile_entities=save_workfile_context.workfile_entities,\n        )\n        return workfile_info\n\n    def _create_extra_folders(\n        self,\n        folder_entity: dict[str, Any],\n        task_entity: dict[str, Any],\n        workdir: str,\n    ) -&gt; None:\n        \"\"\"Create extra folders in the workdir.\n\n        This method should be called when workfile is saved or copied.\n\n        Args:\n            folder_entity (dict[str, Any]): Folder entity.\n            task_entity (dict[str, Any]): Task entity.\n            workdir (str): Workdir where workfile/s will be stored.\n\n        \"\"\"\n        from ayon_core.pipeline.workfile.path_resolving import (\n            create_workdir_extra_folders\n        )\n\n        project_name = self.get_current_project_name()\n\n        # Create extra folders\n        create_workdir_extra_folders(\n            workdir,\n            self.name,\n            task_entity[\"taskType\"],\n            task_entity[\"name\"],\n            project_name\n        )\n\n    def _get_workfile_event_data(\n        self,\n        project_name: str,\n        folder_entity: dict[str, Any],\n        task_entity: dict[str, Any],\n        filepath: str,\n    ) -&gt; dict[str, Optional[str]]:\n        \"\"\"Prepare workfile event data.\n\n        Args:\n            project_name (str): Name of the project where workfile lives.\n            folder_entity (dict[str, Any]): Folder entity.\n            task_entity (dict[str, Any]): Task entity.\n            filepath (str): Path to the workfile.\n\n        Returns:\n            dict[str, Optional[str]]: Data for workfile event.\n\n        \"\"\"\n        workdir, filename = os.path.split(filepath)\n        return {\n            \"project_name\": project_name,\n            \"folder_id\": folder_entity[\"id\"],\n            \"folder_path\": folder_entity[\"path\"],\n            \"task_id\": task_entity[\"id\"],\n            \"task_name\": task_entity[\"name\"],\n            \"host_name\": self.name,\n            \"filepath\": filepath,\n            \"filename\": filename,\n            \"workdir_path\": workdir,\n        }\n\n    def _before_workfile_open(\n        self, open_workfile_context: OpenWorkfileContext\n    ) -&gt; None:\n        \"\"\"Before workfile is opened.\n\n        This method is called before the workfile is opened in the host.\n\n        Can be overridden to implement host specific logic.\n\n        Args:\n            open_workfile_context (OpenWorkfileContext): Context and path of\n                workfile to open.\n\n        \"\"\"\n        pass\n\n    def _after_workfile_open(\n        self, open_workfile_context: OpenWorkfileContext\n    ) -&gt; None:\n        \"\"\"After workfile is opened.\n\n        This method is called after the workfile is opened in the host.\n\n        Can be overridden to implement host specific logic.\n\n        Args:\n            open_workfile_context (OpenWorkfileContext): Context and path of\n                opened workfile.\n\n        \"\"\"\n        pass\n\n    def _before_workfile_save(\n        self, save_workfile_context: SaveWorkfileContext\n    ) -&gt; None:\n        \"\"\"Before workfile is saved.\n\n        This method is called before the workfile is saved in the host.\n\n        Can be overridden to implement host specific logic.\n\n        Args:\n            save_workfile_context (SaveWorkfileContext): Workfile path with\n                target folder and task context.\n\n        \"\"\"\n        pass\n\n    def _after_workfile_save(\n        self, save_workfile_context: SaveWorkfileContext\n    ) -&gt; None:\n        \"\"\"After workfile is saved.\n\n        This method is called after the workfile is saved in the host.\n\n        Can be overridden to implement host specific logic.\n\n        Args:\n            save_workfile_context (SaveWorkfileContext): Workfile path with\n                target folder and task context.\n\n        \"\"\"\n        workdir = os.path.dirname(save_workfile_context.dst_path)\n        self._create_extra_folders(\n            save_workfile_context.folder_entity,\n            save_workfile_context.task_entity,\n            workdir\n        )\n\n    def _before_workfile_copy(\n        self, copy_workfile_context: CopyWorkfileContext\n    ) -&gt; None:\n        \"\"\"Before workfile is copied.\n\n        This method is called before the workfile is copied by host\n            integration.\n\n        Can be overridden to implement host specific logic.\n\n        Args:\n            copy_workfile_context (CopyWorkfileContext): Source and destination\n                path with context before workfile is copied.\n\n        \"\"\"\n        pass\n\n    def _after_workfile_copy(\n        self, copy_workfile_context: CopyWorkfileContext\n    ) -&gt; None:\n        \"\"\"After workfile is copied.\n\n        This method is called after the workfile is copied by host\n            integration.\n\n        Can be overridden to implement host specific logic.\n\n        Args:\n            copy_workfile_context (CopyWorkfileContext): Source and destination\n                path with context after workfile is copied.\n\n        \"\"\"\n        workdir = os.path.dirname(copy_workfile_context.dst_path)\n        self._create_extra_folders(\n            copy_workfile_context.folder_entity,\n            copy_workfile_context.task_entity,\n            workdir,\n        )\n\n    def _emit_workfile_open_event(\n        self,\n        event_data: dict[str, Optional[str]],\n        after_open: bool = True,\n    ) -&gt; None:\n        \"\"\"Emit workfile save event.\n\n        Emit event before and after workfile is opened.\n\n        This method is not meant to be overridden.\n\n        Other addons can listen to this event and do additional steps.\n\n        Args:\n            event_data (dict[str, Optional[str]]): Prepare event data.\n            after_open (bool): Emit event after workfile is opened.\n\n        \"\"\"\n        topics = []\n        topic_end = \"before\"\n        if after_open:\n            topics.append(\"workfile.opened\")\n            topic_end = \"after\"\n\n        # Keep backwards compatible event topic\n        topics.append(f\"workfile.open.{topic_end}\")\n\n        for topic in topics:\n            emit_event(topic, event_data)\n\n    def _emit_workfile_save_event(\n        self,\n        event_data: dict[str, Optional[str]],\n        after_save: bool = True,\n    ) -&gt; None:\n        \"\"\"Emit workfile save event.\n\n        Emit event before and after workfile is saved or copied.\n\n        This method is not meant to be overridden.\n\n        Other addons can listen to this event and do additional steps.\n\n        Args:\n            event_data (dict[str, Optional[str]]): Prepare event data.\n            after_save (bool): Emit event after workfile is saved.\n\n        \"\"\"\n        topics = []\n        topic_end = \"before\"\n        if after_save:\n            topics.append(\"workfile.saved\")\n            topic_end = \"after\"\n\n        # Keep backwards compatible event topic\n        topics.append(f\"workfile.save.{topic_end}\")\n\n        for topic in topics:\n            emit_event(topic, event_data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.IWorkfileHost.copy_workfile","title":"<code>copy_workfile(src_path, dst_path, folder_entity, task_entity, *, version=None, comment=None, description=None, open_workfile=True, prepared_data=None)</code>","text":"<p>Save workfile path with target folder and task context.</p> <p>It is expected that workfile is saved to the current project, but     can be copied from the other project.</p> <p>Arguments 'rootless_path', 'workfile_entities', 'project_entity'     and 'anatomy' can be filled to enhance efficiency if you already     have access to the values.</p> <p>Argument 'project_settings' is used to calculate 'rootless_path'     if it is not provided.</p> <p>Parameters:</p> Name Type Description Default <code>src_path</code> <code>str</code> <p>Path to the source scene.</p> required <code>dst_path</code> <code>str</code> <p>Where the scene should be saved.</p> required <code>folder_entity</code> <code>dict[str, Any]</code> <p>Folder entity.</p> required <code>task_entity</code> <code>dict[str, Any]</code> <p>Task entity.</p> required <code>version</code> <code>Optional[int]</code> <p>Version of the workfile. Information for workfile entity. Recommended to fill.</p> <code>None</code> <code>comment</code> <code>Optional[str]</code> <p>Comment for the workfile.</p> <code>None</code> <code>description</code> <code>Optional[str]</code> <p>Artist note for the workfile entity.</p> <code>None</code> <code>open_workfile</code> <code>bool</code> <p>Open workfile when copied.</p> <code>True</code> <code>prepared_data</code> <code>Optional[CopyWorkfileOptionalData]</code> <p>Prepared data for speed enhancements.</p> <code>None</code> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>def copy_workfile(\n    self,\n    src_path: str,\n    dst_path: str,\n    folder_entity: dict[str, Any],\n    task_entity: dict[str, Any],\n    *,\n    version: Optional[int] = None,\n    comment: Optional[str] = None,\n    description: Optional[str] = None,\n    open_workfile: bool = True,\n    prepared_data: Optional[CopyWorkfileOptionalData] = None,\n) -&gt; None:\n    \"\"\"Save workfile path with target folder and task context.\n\n    It is expected that workfile is saved to the current project, but\n        can be copied from the other project.\n\n    Arguments 'rootless_path', 'workfile_entities', 'project_entity'\n        and 'anatomy' can be filled to enhance efficiency if you already\n        have access to the values.\n\n    Argument 'project_settings' is used to calculate 'rootless_path'\n        if it is not provided.\n\n    Args:\n        src_path (str): Path to the source scene.\n        dst_path (str): Where the scene should be saved.\n        folder_entity (dict[str, Any]): Folder entity.\n        task_entity (dict[str, Any]): Task entity.\n        version (Optional[int]): Version of the workfile. Information\n            for workfile entity. Recommended to fill.\n        comment (Optional[str]): Comment for the workfile.\n        description (Optional[str]): Artist note for the workfile entity.\n        open_workfile (bool): Open workfile when copied.\n        prepared_data (Optional[CopyWorkfileOptionalData]): Prepared data\n            for speed enhancements.\n\n    \"\"\"\n    project_name = self.get_current_project_name()\n    copy_workfile_context: CopyWorkfileContext = get_copy_workfile_context(\n        project_name,\n        src_path,\n        dst_path,\n        folder_entity,\n        task_entity,\n        version=version,\n        comment=comment,\n        description=description,\n        open_workfile=open_workfile,\n        host_name=self.name,\n        prepared_data=prepared_data,\n    )\n    self._copy_workfile(\n        copy_workfile_context,\n        version=version,\n        comment=comment,\n        description=description,\n        open_workfile=open_workfile,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.IWorkfileHost.copy_workfile_representation","title":"<code>copy_workfile_representation(src_project_name, src_representation_entity, dst_path, folder_entity, task_entity, *, version=None, comment=None, description=None, open_workfile=True, prepared_data=None)</code>","text":"<p>Copy workfile representation.</p> <p>Use representation as a source for the workfile.</p> <p>Arguments 'rootless_path', 'workfile_entities', 'project_entity'     and 'anatomy' can be filled to enhance efficiency if you already     have access to the values.</p> <p>Argument 'project_settings' is used to calculate 'rootless_path'     if it is not provided.</p> <p>Parameters:</p> Name Type Description Default <code>src_project_name</code> <code>str</code> <p>Project name.</p> required <code>src_representation_entity</code> <code>dict[str, Any]</code> <p>Representation entity.</p> required <code>dst_path</code> <code>str</code> <p>Where the scene should be saved.</p> required <code>folder_entity</code> <code>dict[str, Any</code> <p>Folder entity.</p> required <code>task_entity</code> <code>dict[str, Any]</code> <p>Task entity.</p> required <code>version</code> <code>Optional[int]</code> <p>Version of the workfile. Information for workfile entity. Recommended to fill.</p> <code>None</code> <code>comment</code> <code>Optional[str]</code> <p>Comment for the workfile.</p> <code>None</code> <code>description</code> <code>Optional[str]</code> <p>Artist note for the workfile entity.</p> <code>None</code> <code>open_workfile</code> <code>bool</code> <p>Open workfile when copied.</p> <code>True</code> <code>prepared_data</code> <code>Optional[CopyPublishedWorkfileOptionalData]</code> <p>Prepared data for speed enhancements.</p> <code>None</code> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>def copy_workfile_representation(\n    self,\n    src_project_name: str,\n    src_representation_entity: dict[str, Any],\n    dst_path: str,\n    folder_entity: dict[str, Any],\n    task_entity: dict[str, Any],\n    *,\n    version: Optional[int] = None,\n    comment: Optional[str] = None,\n    description: Optional[str] = None,\n    open_workfile: bool = True,\n    prepared_data: Optional[CopyPublishedWorkfileOptionalData] = None,\n) -&gt; None:\n    \"\"\"Copy workfile representation.\n\n    Use representation as a source for the workfile.\n\n    Arguments 'rootless_path', 'workfile_entities', 'project_entity'\n        and 'anatomy' can be filled to enhance efficiency if you already\n        have access to the values.\n\n    Argument 'project_settings' is used to calculate 'rootless_path'\n        if it is not provided.\n\n    Args:\n        src_project_name (str): Project name.\n        src_representation_entity (dict[str, Any]): Representation\n            entity.\n        dst_path (str): Where the scene should be saved.\n        folder_entity (dict[str, Any): Folder entity.\n        task_entity (dict[str, Any]): Task entity.\n        version (Optional[int]): Version of the workfile. Information\n            for workfile entity. Recommended to fill.\n        comment (Optional[str]): Comment for the workfile.\n        description (Optional[str]): Artist note for the workfile entity.\n        open_workfile (bool): Open workfile when copied.\n        prepared_data (Optional[CopyPublishedWorkfileOptionalData]):\n            Prepared data for speed enhancements.\n\n    \"\"\"\n    project_name = self.get_current_project_name()\n    copy_repre_workfile_context: CopyPublishedWorkfileContext = (\n        get_copy_repre_workfile_context(\n            project_name,\n            src_project_name,\n            src_representation_entity,\n            dst_path,\n            folder_entity,\n            task_entity,\n            version=version,\n            comment=comment,\n            description=description,\n            open_workfile=open_workfile,\n            host_name=self.name,\n            prepared_data=prepared_data,\n        )\n    )\n    self._copy_workfile(\n        copy_repre_workfile_context,\n        version=version,\n        comment=comment,\n        description=description,\n        open_workfile=open_workfile,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.IWorkfileHost.current_file","title":"<code>current_file()</code>","text":"<p>Deprecated variant of 'get_current_workfile'.</p> Todo <p>Remove when all usages are replaced.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>@deprecated(\"Use 'get_current_workfile' instead\")\ndef current_file(self):\n    \"\"\"Deprecated variant of 'get_current_workfile'.\n\n    Todo:\n        Remove when all usages are replaced.\n\n    \"\"\"\n    return self.get_current_workfile()\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.IWorkfileHost.file_extensions","title":"<code>file_extensions()</code>","text":"<p>Deprecated variant of 'get_workfile_extensions'.</p> Todo <p>Remove when all usages are replaced.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>@deprecated(\"Use 'get_workfile_extensions' instead\")\ndef file_extensions(self):\n    \"\"\"Deprecated variant of 'get_workfile_extensions'.\n\n    Todo:\n        Remove when all usages are replaced.\n\n    \"\"\"\n    return self.get_workfile_extensions()\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.IWorkfileHost.get_current_workfile","title":"<code>get_current_workfile()</code>  <code>abstractmethod</code>","text":"<p>Retrieve a path to current opened file.</p> <p>Returns:</p> Type Description <code>Optional[str]</code> <p>Optional[str]: Path to the file which is currently opened. None if nothing is opened or the current workfile is unsaved.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>@abstractmethod\ndef get_current_workfile(self) -&gt; Optional[str]:\n    \"\"\"Retrieve a path to current opened file.\n\n    Returns:\n        Optional[str]: Path to the file which is currently opened. None if\n            nothing is opened or the current workfile is unsaved.\n\n    \"\"\"\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.IWorkfileHost.get_workfile_extensions","title":"<code>get_workfile_extensions()</code>","text":"<p>Extensions that can be used to save the workfile to.</p> Notes <p>Method may not be used if 'list_workfiles' and     'list_published_workfiles' are re-implemented with different     logic.</p> <p>Returns:</p> Type Description <code>list[str]</code> <p>list[str]: List of extensions that can be used for saving.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>def get_workfile_extensions(self) -&gt; list[str]:\n    \"\"\"Extensions that can be used to save the workfile to.\n\n    Notes:\n        Method may not be used if 'list_workfiles' and\n            'list_published_workfiles' are re-implemented with different\n            logic.\n\n    Returns:\n        list[str]: List of extensions that can be used for saving.\n\n    \"\"\"\n    return []\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.IWorkfileHost.has_unsaved_changes","title":"<code>has_unsaved_changes()</code>","text":"<p>Deprecated variant of 'workfile_has_unsaved_changes'.</p> Todo <p>Remove when all usages are replaced.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>@deprecated(\"Use 'workfile_has_unsaved_changes' instead\")\ndef has_unsaved_changes(self):\n    \"\"\"Deprecated variant of 'workfile_has_unsaved_changes'.\n\n    Todo:\n        Remove when all usages are replaced.\n\n    \"\"\"\n    return self.workfile_has_unsaved_changes()\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.IWorkfileHost.list_published_workfiles","title":"<code>list_published_workfiles(project_name, folder_id, *, prepared_data=None)</code>","text":"<p>List published workfiles for the given folder.</p> <p>The default implementation looks for products with the 'workfile'     product type.</p> Pre-fetched entities have mandatory fields to be fetched <ul> <li>Version: 'id', 'author', 'taskId'</li> <li>Representation: 'id', 'versionId', 'files'</li> </ul> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>folder_id</code> <code>str</code> <p>Folder id.</p> required <code>prepared_data</code> <code>Optional[ListPublishedWorkfilesOptionalData]</code> <p>Prepared data for speed enhancements.</p> <code>None</code> <p>Returns:</p> Type Description <code>list[PublishedWorkfileInfo]</code> <p>list[PublishedWorkfileInfo]: Published workfile information for the given context.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>def list_published_workfiles(\n    self,\n    project_name: str,\n    folder_id: str,\n    *,\n    prepared_data: Optional[ListPublishedWorkfilesOptionalData] = None,\n) -&gt; list[PublishedWorkfileInfo]:\n    \"\"\"List published workfiles for the given folder.\n\n    The default implementation looks for products with the 'workfile'\n        product type.\n\n    Pre-fetched entities have mandatory fields to be fetched:\n        - Version: 'id', 'author', 'taskId'\n        - Representation: 'id', 'versionId', 'files'\n\n    Args:\n        project_name (str): Project name.\n        folder_id (str): Folder id.\n        prepared_data (Optional[ListPublishedWorkfilesOptionalData]):\n            Prepared data for speed enhancements.\n\n    Returns:\n        list[PublishedWorkfileInfo]: Published workfile information for\n            the given context.\n\n    \"\"\"\n    list_workfiles_context = get_list_published_workfiles_context(\n        project_name,\n        folder_id,\n        prepared_data=prepared_data,\n    )\n    if not list_workfiles_context.repre_entities:\n        return []\n\n    versions_by_id = {\n        version_entity[\"id\"]: version_entity\n        for version_entity in list_workfiles_context.version_entities\n    }\n    extensions = {\n        ext.lstrip(\".\")\n        for ext in self.get_workfile_extensions()\n    }\n    items = []\n    for repre_entity in list_workfiles_context.repre_entities:\n        version_id = repre_entity[\"versionId\"]\n        version_entity = versions_by_id[version_id]\n        task_id = version_entity[\"taskId\"]\n\n        # Filter by extension\n        workfile_path = None\n        for repre_file in repre_entity[\"files\"]:\n            ext = (\n                os.path.splitext(repre_file[\"name\"])[1]\n                .lower()\n                .lstrip(\".\")\n            )\n            if ext in extensions:\n                workfile_path = repre_file[\"path\"]\n                break\n\n        if not workfile_path:\n            continue\n\n        try:\n            workfile_path = workfile_path.format(\n                root=list_workfiles_context.anatomy.roots\n            )\n        except Exception:\n            self.log.warning(\n                \"Failed to format workfile path.\", exc_info=True\n            )\n\n        is_available = False\n        file_size = file_modified = file_created = None\n        if workfile_path and os.path.exists(workfile_path):\n            filestat = os.stat(workfile_path)\n            is_available = True\n            file_size = filestat.st_size\n            file_created = filestat.st_ctime\n            file_modified = filestat.st_mtime\n\n        workfile_item = PublishedWorkfileInfo.new(\n            project_name,\n            folder_id,\n            task_id,\n            repre_entity,\n            filepath=workfile_path,\n            author=version_entity[\"author\"],\n            available=is_available,\n            file_size=file_size,\n            file_created=file_created,\n            file_modified=file_modified,\n        )\n        items.append(workfile_item)\n\n    return items\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.IWorkfileHost.list_workfiles","title":"<code>list_workfiles(project_name, folder_entity, task_entity, *, prepared_data=None)</code>","text":"<p>List workfiles in the given task.</p> <p>The method should also return workfiles that are not available on     disk, but are in the AYON database.</p> <p>Notes: - Better method name? - This method is pre-implemented as the logic can be shared across     95% of host integrations. Ad-hoc implementation to give host     integration workfile api functionality.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>folder_entity</code> <code>dict[str, Any]</code> <p>Folder entity.</p> required <code>task_entity</code> <code>dict[str, Any]</code> <p>Task entity.</p> required <code>prepared_data</code> <code>Optional[ListWorkfilesOptionalData]</code> <p>Prepared data for speed enhancements.</p> <code>None</code> <p>Returns:</p> Type Description <code>list[WorkfileInfo]</code> <p>list[WorkfileInfo]: List of workfiles.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>def list_workfiles(\n    self,\n    project_name: str,\n    folder_entity: dict[str, Any],\n    task_entity: dict[str, Any],\n    *,\n    prepared_data: Optional[ListWorkfilesOptionalData] = None,\n) -&gt; list[WorkfileInfo]:\n    \"\"\"List workfiles in the given task.\n\n    The method should also return workfiles that are not available on\n        disk, but are in the AYON database.\n\n    Notes:\n    - Better method name?\n    - This method is pre-implemented as the logic can be shared across\n        95% of host integrations. Ad-hoc implementation to give host\n        integration workfile api functionality.\n\n    Args:\n        project_name (str): Project name.\n        folder_entity (dict[str, Any]): Folder entity.\n        task_entity (dict[str, Any]): Task entity.\n        prepared_data (Optional[ListWorkfilesOptionalData]): Prepared\n            data for speed enhancements.\n\n    Returns:\n        list[WorkfileInfo]: List of workfiles.\n\n    \"\"\"\n    from ayon_core.pipeline.template_data import get_template_data\n    from ayon_core.pipeline.workfile.path_resolving import (\n        get_workdir_with_workdir_data,\n        WorkfileDataParser,\n    )\n\n    extensions = self.get_workfile_extensions()\n    if not extensions:\n        return []\n\n    list_workfiles_context = get_list_workfiles_context(\n        project_name,\n        folder_entity,\n        task_entity,\n        host_name=self.name,\n        prepared_data=prepared_data,\n    )\n\n    workfile_entities_by_path = {}\n    for workfile_entity in list_workfiles_context.workfile_entities:\n        rootless_path = workfile_entity[\"path\"]\n        path = os.path.normpath(\n            list_workfiles_context.anatomy.fill_root(rootless_path)\n        )\n        workfile_entities_by_path[path] = workfile_entity\n\n    workdir_data = get_template_data(\n        list_workfiles_context.project_entity,\n        folder_entity,\n        task_entity,\n        host_name=self.name,\n    )\n    workdir = get_workdir_with_workdir_data(\n        workdir_data,\n        project_name,\n        anatomy=list_workfiles_context.anatomy,\n        template_key=list_workfiles_context.template_key,\n        project_settings=list_workfiles_context.project_settings,\n    )\n\n    file_template = list_workfiles_context.anatomy.get_template_item(\n        \"work\", list_workfiles_context.template_key, \"file\"\n    )\n    rootless_workdir = workdir.rootless\n    if platform.system().lower() == \"windows\":\n        rootless_workdir = rootless_workdir.replace(\"\\\\\", \"/\")\n\n    filenames = []\n    if os.path.exists(workdir):\n        filenames = list(os.listdir(workdir))\n\n    data_parser = WorkfileDataParser(file_template, workdir_data)\n    items = []\n    for filename in filenames:\n        # TODO add 'default' support for folders\n        ext = os.path.splitext(filename)[1].lower()\n        if ext not in extensions:\n            continue\n\n        filepath = os.path.join(workdir, filename)\n\n        rootless_path = f\"{rootless_workdir}/{filename}\"\n        workfile_entity = workfile_entities_by_path.pop(\n            filepath, None\n        )\n        version = comment = None\n        if workfile_entity is not None:\n            _data = workfile_entity[\"data\"]\n            version = _data.get(\"version\")\n            comment = _data.get(\"comment\")\n\n        if version is None:\n            parsed_data = data_parser.parse_data(filename)\n            version = parsed_data.version\n            comment = parsed_data.comment\n\n        item = WorkfileInfo.new(\n            filepath,\n            rootless_path,\n            version=version,\n            comment=comment,\n            available=True,\n            workfile_entity=workfile_entity,\n        )\n        items.append(item)\n\n    for filepath, workfile_entity in workfile_entities_by_path.items():\n        # Workfile entity is not in the filesystem\n        #   but it is in the database\n        rootless_path = workfile_entity[\"path\"]\n        ext = os.path.splitext(rootless_path)[1].lower()\n        if ext not in extensions:\n            continue\n\n        _data = workfile_entity[\"data\"]\n        version = _data.get(\"version\")\n        comment = _data.get(\"comment\")\n        if version is None:\n            filename = os.path.basename(rootless_path)\n            parsed_data = data_parser.parse_data(filename)\n            version = parsed_data.version\n            comment = parsed_data.comment\n\n        available = os.path.exists(filepath)\n        items.append(WorkfileInfo.new(\n            filepath,\n            rootless_path,\n            version=version,\n            comment=comment,\n            available=available,\n            workfile_entity=workfile_entity,\n        ))\n\n    return items\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.IWorkfileHost.open_file","title":"<code>open_file(filepath)</code>","text":"<p>Deprecated variant of 'open_workfile'.</p> Todo <p>Remove when all usages are replaced.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>@deprecated(\"Use 'open_workfile' instead\")\ndef open_file(self, filepath):\n    \"\"\"Deprecated variant of 'open_workfile'.\n\n    Todo:\n        Remove when all usages are replaced.\n\n    \"\"\"\n    return self.open_workfile(filepath)\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.IWorkfileHost.open_workfile","title":"<code>open_workfile(filepath)</code>  <code>abstractmethod</code>","text":"<p>Open passed filepath in the host.</p> <p>Parameters:</p> Name Type Description Default <code>filepath</code> <code>str</code> <p>Path to workfile.</p> required Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>@abstractmethod\ndef open_workfile(self, filepath: str) -&gt; None:\n    \"\"\"Open passed filepath in the host.\n\n    Args:\n        filepath (str): Path to workfile.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.IWorkfileHost.open_workfile_with_context","title":"<code>open_workfile_with_context(filepath, folder_entity, task_entity, *, prepared_data=None)</code>","text":"<p>Open passed filepath in the host with context.</p> <p>This function should be used to open workfile in different context.</p> Notes <p>Should this method care about context change?</p> <p>Parameters:</p> Name Type Description Default <code>filepath</code> <code>str</code> <p>Path to workfile.</p> required <code>folder_entity</code> <code>dict[str, Any]</code> <p>Folder id.</p> required <code>task_entity</code> <code>dict[str, Any]</code> <p>Task id.</p> required <code>prepared_data</code> <code>Optional[WorkfileOptionalData]</code> <p>Prepared data for speed enhancements.</p> <code>None</code> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>def open_workfile_with_context(\n    self,\n    filepath: str,\n    folder_entity: dict[str, Any],\n    task_entity: dict[str, Any],\n    *,\n    prepared_data: Optional[OpenWorkfileOptionalData] = None,\n) -&gt; None:\n    \"\"\"Open passed filepath in the host with context.\n\n    This function should be used to open workfile in different context.\n\n    Notes:\n        Should this method care about context change?\n\n    Args:\n        filepath (str): Path to workfile.\n        folder_entity (dict[str, Any]): Folder id.\n        task_entity (dict[str, Any]): Task id.\n        prepared_data (Optional[WorkfileOptionalData]): Prepared data\n            for speed enhancements.\n\n    \"\"\"\n    context = self.get_current_context()\n    project_name = context[\"project_name\"]\n\n    open_workfile_context = get_open_workfile_context(\n        project_name,\n        filepath,\n        folder_entity,\n        task_entity,\n        prepared_data=prepared_data,\n    )\n\n    workdir = os.path.dirname(filepath)\n    # Set 'AYON_WORKDIR' environment variable\n    os.environ[\"AYON_WORKDIR\"] = workdir\n\n    event_data = self._get_workfile_event_data(\n        project_name, folder_entity, task_entity, filepath\n    )\n    self._before_workfile_open(open_workfile_context)\n    self._emit_workfile_open_event(event_data, after_open=False)\n\n    if self.change_context_before_workfile_open:\n        self.set_current_context(\n            folder_entity,\n            task_entity,\n            reason=ContextChangeReason.workfile_open,\n            project_entity=open_workfile_context.project_entity,\n            anatomy=open_workfile_context.anatomy,\n        )\n\n    self.open_workfile(filepath)\n\n    if not self.change_context_before_workfile_open:\n        self.set_current_context(\n            folder_entity,\n            task_entity,\n            reason=ContextChangeReason.workfile_open,\n            project_entity=open_workfile_context.project_entity,\n            anatomy=open_workfile_context.anatomy,\n        )\n\n    self._after_workfile_open(open_workfile_context)\n    self._emit_workfile_open_event(event_data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.IWorkfileHost.save_file","title":"<code>save_file(dst_path=None)</code>","text":"<p>Deprecated variant of 'save_workfile'.</p> Todo <p>Remove when all usages are replaced</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>@deprecated(\"Use 'save_workfile' instead\")\ndef save_file(self, dst_path=None):\n    \"\"\"Deprecated variant of 'save_workfile'.\n\n    Todo:\n        Remove when all usages are replaced\n\n    \"\"\"\n    self.save_workfile(dst_path)\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.IWorkfileHost.save_workfile","title":"<code>save_workfile(dst_path=None)</code>  <code>abstractmethod</code>","text":"<p>Save the currently opened scene.</p> <p>Parameters:</p> Name Type Description Default <code>dst_path</code> <code>str</code> <p>Where the current scene should be saved. Or use the current path if 'None' is passed.</p> <code>None</code> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>@abstractmethod\ndef save_workfile(self, dst_path: Optional[str] = None) -&gt; None:\n    \"\"\"Save the currently opened scene.\n\n    Args:\n        dst_path (str): Where the current scene should be saved. Or use\n            the current path if 'None' is passed.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.IWorkfileHost.save_workfile_with_context","title":"<code>save_workfile_with_context(filepath, folder_entity, task_entity, *, version=None, comment=None, description=None, prepared_data=None)</code>","text":"<p>Save the current workfile with context.</p> <p>Arguments 'rootless_path', 'workfile_entities', 'project_entity'     and 'anatomy' can be filled to enhance efficiency if you already     have access to the values.</p> <p>Argument 'project_settings' is used to calculate 'rootless_path'     if it is not provided.</p> Notes <p>Should this method care about context change?</p> <p>Parameters:</p> Name Type Description Default <code>filepath</code> <code>str</code> <p>Where the current scene should be saved.</p> required <code>folder_entity</code> <code>dict[str, Any]</code> <p>Folder entity.</p> required <code>task_entity</code> <code>dict[str, Any]</code> <p>Task entity.</p> required <code>version</code> <code>Optional[int]</code> <p>Version of the workfile. Information for workfile entity. Recommended to fill.</p> <code>None</code> <code>comment</code> <code>Optional[str]</code> <p>Comment for the workfile. Usually used in the filename template.</p> <code>None</code> <code>description</code> <code>Optional[str]</code> <p>Artist note for the workfile entity.</p> <code>None</code> <code>prepared_data</code> <code>Optional[SaveWorkfileOptionalData]</code> <p>Prepared data for speed enhancements.</p> <code>None</code> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>def save_workfile_with_context(\n    self,\n    filepath: str,\n    folder_entity: dict[str, Any],\n    task_entity: dict[str, Any],\n    *,\n    version: Optional[int] = None,\n    comment: Optional[str] = None,\n    description: Optional[str] = None,\n    prepared_data: Optional[SaveWorkfileOptionalData] = None,\n) -&gt; None:\n    \"\"\"Save the current workfile with context.\n\n    Arguments 'rootless_path', 'workfile_entities', 'project_entity'\n        and 'anatomy' can be filled to enhance efficiency if you already\n        have access to the values.\n\n    Argument 'project_settings' is used to calculate 'rootless_path'\n        if it is not provided.\n\n    Notes:\n        Should this method care about context change?\n\n    Args:\n        filepath (str): Where the current scene should be saved.\n        folder_entity (dict[str, Any]): Folder entity.\n        task_entity (dict[str, Any]): Task entity.\n        version (Optional[int]): Version of the workfile. Information\n            for workfile entity. Recommended to fill.\n        comment (Optional[str]): Comment for the workfile.\n            Usually used in the filename template.\n        description (Optional[str]): Artist note for the workfile entity.\n        prepared_data (Optional[SaveWorkfileOptionalData]): Prepared data\n            for speed enhancements.\n\n    \"\"\"\n    project_name = self.get_current_project_name()\n    save_workfile_context = get_save_workfile_context(\n        project_name,\n        filepath,\n        folder_entity,\n        task_entity,\n        host_name=self.name,\n        prepared_data=prepared_data,\n    )\n\n    self._before_workfile_save(save_workfile_context)\n    event_data = self._get_workfile_event_data(\n        project_name,\n        folder_entity,\n        task_entity,\n        filepath,\n    )\n    self._emit_workfile_save_event(event_data, after_save=False)\n\n    workdir = os.path.dirname(filepath)\n    if not os.path.exists(workdir):\n        os.makedirs(workdir, exist_ok=True)\n\n    # Set 'AYON_WORKDIR' environment variable\n    os.environ[\"AYON_WORKDIR\"] = workdir\n\n    self.set_current_context(\n        folder_entity,\n        task_entity,\n        reason=ContextChangeReason.workfile_save,\n        project_entity=save_workfile_context.project_entity,\n        anatomy=save_workfile_context.anatomy,\n    )\n\n    self.save_workfile(filepath)\n\n    self._save_workfile_entity(\n        save_workfile_context,\n        version,\n        comment,\n        description,\n    )\n    self._after_workfile_save(save_workfile_context)\n    self._emit_workfile_save_event(event_data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.IWorkfileHost.workfile_has_unsaved_changes","title":"<code>workfile_has_unsaved_changes()</code>","text":"<p>Currently opened scene is saved.</p> <p>Not all hosts can know if the current scene is saved because the API     of DCC does not support it.</p> <p>Returns:</p> Type Description <code>Optional[bool]</code> <p>Optional[bool]: True if scene is saved and False if has unsaved modifications. None if can't tell if workfiles has modifications.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>def workfile_has_unsaved_changes(self) -&gt; Optional[bool]:\n    \"\"\"Currently opened scene is saved.\n\n    Not all hosts can know if the current scene is saved because the API\n        of DCC does not support it.\n\n    Returns:\n        Optional[bool]: True if scene is saved and False if has unsaved\n            modifications. None if can't tell if workfiles has\n            modifications.\n\n    \"\"\"\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.PublishedWorkfileInfo","title":"<code>PublishedWorkfileInfo</code>  <code>dataclass</code>","text":"<p>Information about published workfile.</p> <p>Host can copy and use the workfile using this information object.</p> <p>Attributes:</p> Name Type Description <code>project_name</code> <code>str</code> <p>Name of the project where workfile lives.</p> <code>folder_id</code> <code>str</code> <p>Folder id under which is workfile stored.</p> <code>task_id</code> <code>Optional[str]</code> <p>Task id under which is workfile stored.</p> <code>representation_id</code> <code>str</code> <p>Representation id of the workfile.</p> <code>filepath</code> <code>str</code> <p>Path to the workfile.</p> <code>created_at</code> <code>float</code> <p>Timestamp when the workfile representation was created.</p> <code>author</code> <code>str</code> <p>Author of the workfile representation.</p> <code>available</code> <code>bool</code> <p>True if workfile is available on the machine.</p> <code>file_size</code> <code>Optional[float]</code> <p>Size of the workfile in bytes.</p> <code>file_created</code> <code>Optional[float]</code> <p>Timestamp when the workfile was created on the filesystem.</p> <code>file_modified</code> <code>Optional[float]</code> <p>Timestamp when the workfile was modified on the filesystem.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>@dataclass\nclass PublishedWorkfileInfo:\n    \"\"\"Information about published workfile.\n\n    Host can copy and use the workfile using this information object.\n\n    Attributes:\n        project_name (str): Name of the project where workfile lives.\n        folder_id (str): Folder id under which is workfile stored.\n        task_id (Optional[str]): Task id under which is workfile stored.\n        representation_id (str): Representation id of the workfile.\n        filepath (str): Path to the workfile.\n        created_at (float): Timestamp when the workfile representation\n            was created.\n        author (str): Author of the workfile representation.\n        available (bool): True if workfile is available on the machine.\n        file_size (Optional[float]): Size of the workfile in bytes.\n        file_created (Optional[float]): Timestamp when the workfile was\n            created on the filesystem.\n        file_modified (Optional[float]): Timestamp when the workfile was\n            modified on the filesystem.\n\n    \"\"\"\n    project_name: str\n    folder_id: str\n    task_id: Optional[str]\n    representation_id: str\n    filepath: str\n    created_at: float\n    author: str\n    available: bool\n    file_size: Optional[float]\n    file_created: Optional[float]\n    file_modified: Optional[float]\n\n    @classmethod\n    def new(\n        cls,\n        project_name: str,\n        folder_id: str,\n        task_id: Optional[str],\n        repre_entity: dict[str, Any],\n        *,\n        filepath: str,\n        author: str,\n        available: bool,\n        file_size: Optional[float],\n        file_modified: Optional[float],\n        file_created: Optional[float],\n    ) -&gt; \"PublishedWorkfileInfo\":\n        created_at = arrow.get(repre_entity[\"createdAt\"]).to(\"local\")\n\n        return cls(\n            project_name=project_name,\n            folder_id=folder_id,\n            task_id=task_id,\n            representation_id=repre_entity[\"id\"],\n            filepath=filepath,\n            created_at=created_at.float_timestamp,\n            author=author,\n            available=available,\n            file_size=file_size,\n            file_created=file_created,\n            file_modified=file_modified,\n        )\n\n    def to_data(self) -&gt; dict[str, Any]:\n        \"\"\"Converts file item to data.\n\n        Returns:\n            dict[str, Any]: Workfile item data.\n\n        \"\"\"\n        return asdict(self)\n\n    @classmethod\n    def from_data(cls, data: dict[str, Any]) -&gt; \"PublishedWorkfileInfo\":\n        \"\"\"Converts data to workfile item.\n\n        Args:\n            data (dict[str, Any]): Workfile item data.\n\n        Returns:\n            PublishedWorkfileInfo: File item.\n\n        \"\"\"\n        return PublishedWorkfileInfo(**data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.PublishedWorkfileInfo.from_data","title":"<code>from_data(data)</code>  <code>classmethod</code>","text":"<p>Converts data to workfile item.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict[str, Any]</code> <p>Workfile item data.</p> required <p>Returns:</p> Name Type Description <code>PublishedWorkfileInfo</code> <code>'PublishedWorkfileInfo'</code> <p>File item.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>@classmethod\ndef from_data(cls, data: dict[str, Any]) -&gt; \"PublishedWorkfileInfo\":\n    \"\"\"Converts data to workfile item.\n\n    Args:\n        data (dict[str, Any]): Workfile item data.\n\n    Returns:\n        PublishedWorkfileInfo: File item.\n\n    \"\"\"\n    return PublishedWorkfileInfo(**data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.PublishedWorkfileInfo.to_data","title":"<code>to_data()</code>","text":"<p>Converts file item to data.</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: Workfile item data.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>def to_data(self) -&gt; dict[str, Any]:\n    \"\"\"Converts file item to data.\n\n    Returns:\n        dict[str, Any]: Workfile item data.\n\n    \"\"\"\n    return asdict(self)\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.WorkfileInfo","title":"<code>WorkfileInfo</code>  <code>dataclass</code>","text":"<p>Information about workfile.</p> <p>Host can open, copy and use the workfile using this information object.</p> <p>Attributes:</p> Name Type Description <code>filepath</code> <code>str</code> <p>Path to the workfile.</p> <code>rootless_path</code> <code>str</code> <p>Path to the workfile without the root. And without backslashes on Windows.</p> <code>version</code> <code>Optional[int]</code> <p>Version of the workfile.</p> <code>comment</code> <code>Optional[str]</code> <p>Comment of the workfile.</p> <code>file_size</code> <code>Optional[float]</code> <p>Size of the workfile in bytes.</p> <code>file_created</code> <code>Optional[float]</code> <p>Timestamp when the workfile was created on the filesystem.</p> <code>file_modified</code> <code>Optional[float]</code> <p>Timestamp when the workfile was modified on the filesystem.</p> <code>workfile_entity_id</code> <code>Optional[str]</code> <p>Workfile entity id. If None then the workfile is not in the database.</p> <code>description</code> <code>str</code> <p>Description of the workfile.</p> <code>created_by</code> <code>Optional[str]</code> <p>User id of the user who created the workfile entity.</p> <code>updated_by</code> <code>Optional[str]</code> <p>User id of the user who updated the workfile entity.</p> <code>available</code> <code>bool</code> <p>True if workfile is available on the machine.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>@dataclass\nclass WorkfileInfo:\n    \"\"\"Information about workfile.\n\n    Host can open, copy and use the workfile using this information object.\n\n    Attributes:\n        filepath (str): Path to the workfile.\n        rootless_path (str): Path to the workfile without the root. And without\n            backslashes on Windows.\n        version (Optional[int]): Version of the workfile.\n        comment (Optional[str]): Comment of the workfile.\n        file_size (Optional[float]): Size of the workfile in bytes.\n        file_created (Optional[float]): Timestamp when the workfile was\n            created on the filesystem.\n        file_modified (Optional[float]): Timestamp when the workfile was\n            modified on the filesystem.\n        workfile_entity_id (Optional[str]): Workfile entity id. If None then\n            the workfile is not in the database.\n        description (str): Description of the workfile.\n        created_by (Optional[str]): User id of the user who created the\n            workfile entity.\n        updated_by (Optional[str]): User id of the user who updated the\n            workfile entity.\n        available (bool): True if workfile is available on the machine.\n\n    \"\"\"\n    filepath: str\n    rootless_path: str\n    version: Optional[int]\n    comment: Optional[str]\n    file_size: Optional[float]\n    file_created: Optional[float]\n    file_modified: Optional[float]\n    workfile_entity_id: Optional[str]\n    description: str\n    created_by: Optional[str]\n    updated_by: Optional[str]\n    available: bool\n\n    @classmethod\n    def new(\n        cls,\n        filepath: str,\n        rootless_path: str,\n        *,\n        version: Optional[int],\n        comment: Optional[str],\n        available: bool,\n        workfile_entity: dict[str, Any],\n    ):\n        file_size = file_modified = file_created = None\n        if filepath and os.path.exists(filepath):\n            filestat = os.stat(filepath)\n            file_size = filestat.st_size\n            file_created = filestat.st_ctime\n            file_modified = filestat.st_mtime\n\n        if workfile_entity is None:\n            workfile_entity = {}\n\n        attrib = {}\n        if workfile_entity:\n            attrib = workfile_entity[\"attrib\"]\n\n        return cls(\n            filepath=filepath,\n            rootless_path=rootless_path,\n            version=version,\n            comment=comment,\n            file_size=file_size,\n            file_created=file_created,\n            file_modified=file_modified,\n            workfile_entity_id=workfile_entity.get(\"id\"),\n            description=attrib.get(\"description\") or \"\",\n            created_by=workfile_entity.get(\"createdBy\"),\n            updated_by=workfile_entity.get(\"updatedBy\"),\n            available=available,\n        )\n\n    def to_data(self) -&gt; dict[str, Any]:\n        \"\"\"Converts file item to data.\n\n        Returns:\n            dict[str, Any]: Workfile item data.\n\n        \"\"\"\n        return asdict(self)\n\n    @classmethod\n    def from_data(cls, data: dict[str, Any]) -&gt; WorkfileInfo:\n        \"\"\"Converts data to workfile item.\n\n        Args:\n            data (dict[str, Any]): Workfile item data.\n\n        Returns:\n            WorkfileInfo: File item.\n\n        \"\"\"\n        return WorkfileInfo(**data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.WorkfileInfo.from_data","title":"<code>from_data(data)</code>  <code>classmethod</code>","text":"<p>Converts data to workfile item.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict[str, Any]</code> <p>Workfile item data.</p> required <p>Returns:</p> Name Type Description <code>WorkfileInfo</code> <code>WorkfileInfo</code> <p>File item.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>@classmethod\ndef from_data(cls, data: dict[str, Any]) -&gt; WorkfileInfo:\n    \"\"\"Converts data to workfile item.\n\n    Args:\n        data (dict[str, Any]): Workfile item data.\n\n    Returns:\n        WorkfileInfo: File item.\n\n    \"\"\"\n    return WorkfileInfo(**data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/index.html#client.ayon_core.host.WorkfileInfo.to_data","title":"<code>to_data()</code>","text":"<p>Converts file item to data.</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: Workfile item data.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>def to_data(self) -&gt; dict[str, Any]:\n    \"\"\"Converts file item to data.\n\n    Returns:\n        dict[str, Any]: Workfile item data.\n\n    \"\"\"\n    return asdict(self)\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/abstract.html","title":"abstract","text":""},{"location":"autoapi/client/ayon_core/host/abstract.html#client.ayon_core.host.abstract.AbstractHost","title":"<code>AbstractHost</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Abstract definition of host implementation.</p> Source code in <code>client/ayon_core/host/abstract.py</code> <pre><code>class AbstractHost(ABC):\n    \"\"\"Abstract definition of host implementation.\"\"\"\n    @property\n    @abstractmethod\n    def log(self) -&gt; logging.Logger:\n        pass\n\n    @property\n    @abstractmethod\n    def name(self) -&gt; str:\n        \"\"\"Host name.\"\"\"\n        pass\n\n    @abstractmethod\n    def get_app_information(self) -&gt; ApplicationInformation:\n        \"\"\"Information about the application where host is running.\n\n        Returns:\n            ApplicationInformation: Application information.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_current_context(self) -&gt; HostContextData:\n        \"\"\"Get the current context of the host.\n\n        Current context is defined by project name, folder path and task name.\n\n        Returns:\n            HostContextData: The current context of the host.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def set_current_context(\n        self,\n        folder_entity: dict[str, Any],\n        task_entity: dict[str, Any],\n        *,\n        reason: ContextChangeReason = ContextChangeReason.undefined,\n        project_entity: Optional[dict[str, Any]] = None,\n        anatomy: Optional[Anatomy] = None,\n    ) -&gt; HostContextData:\n        \"\"\"Change context of the host.\n\n        Args:\n            folder_entity (dict[str, Any]): Folder entity.\n            task_entity (dict[str, Any]): Task entity.\n            reason (ContextChangeReason): Reason for change.\n            project_entity (dict[str, Any]): Project entity.\n            anatomy (Anatomy): Anatomy entity.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_current_project_name(self) -&gt; str:\n        \"\"\"Get the current project name.\n\n        Returns:\n            Optional[str]: The current project name.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_current_folder_path(self) -&gt; Optional[str]:\n        \"\"\"Get the current folder path.\n\n        Returns:\n            Optional[str]: The current folder path.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_current_task_name(self) -&gt; Optional[str]:\n        \"\"\"Get the current task name.\n\n        Returns:\n            Optional[str]: The current task name.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_context_title(self) -&gt; str:\n        \"\"\"Get the context title used in UIs.\"\"\"\n        pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/abstract.html#client.ayon_core.host.abstract.AbstractHost.name","title":"<code>name</code>  <code>abstractmethod</code> <code>property</code>","text":"<p>Host name.</p>"},{"location":"autoapi/client/ayon_core/host/abstract.html#client.ayon_core.host.abstract.AbstractHost.get_app_information","title":"<code>get_app_information()</code>  <code>abstractmethod</code>","text":"<p>Information about the application where host is running.</p> <p>Returns:</p> Name Type Description <code>ApplicationInformation</code> <code>ApplicationInformation</code> <p>Application information.</p> Source code in <code>client/ayon_core/host/abstract.py</code> <pre><code>@abstractmethod\ndef get_app_information(self) -&gt; ApplicationInformation:\n    \"\"\"Information about the application where host is running.\n\n    Returns:\n        ApplicationInformation: Application information.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/abstract.html#client.ayon_core.host.abstract.AbstractHost.get_context_title","title":"<code>get_context_title()</code>  <code>abstractmethod</code>","text":"<p>Get the context title used in UIs.</p> Source code in <code>client/ayon_core/host/abstract.py</code> <pre><code>@abstractmethod\ndef get_context_title(self) -&gt; str:\n    \"\"\"Get the context title used in UIs.\"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/abstract.html#client.ayon_core.host.abstract.AbstractHost.get_current_context","title":"<code>get_current_context()</code>  <code>abstractmethod</code>","text":"<p>Get the current context of the host.</p> <p>Current context is defined by project name, folder path and task name.</p> <p>Returns:</p> Name Type Description <code>HostContextData</code> <code>HostContextData</code> <p>The current context of the host.</p> Source code in <code>client/ayon_core/host/abstract.py</code> <pre><code>@abstractmethod\ndef get_current_context(self) -&gt; HostContextData:\n    \"\"\"Get the current context of the host.\n\n    Current context is defined by project name, folder path and task name.\n\n    Returns:\n        HostContextData: The current context of the host.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/abstract.html#client.ayon_core.host.abstract.AbstractHost.get_current_folder_path","title":"<code>get_current_folder_path()</code>  <code>abstractmethod</code>","text":"<p>Get the current folder path.</p> <p>Returns:</p> Type Description <code>Optional[str]</code> <p>Optional[str]: The current folder path.</p> Source code in <code>client/ayon_core/host/abstract.py</code> <pre><code>@abstractmethod\ndef get_current_folder_path(self) -&gt; Optional[str]:\n    \"\"\"Get the current folder path.\n\n    Returns:\n        Optional[str]: The current folder path.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/abstract.html#client.ayon_core.host.abstract.AbstractHost.get_current_project_name","title":"<code>get_current_project_name()</code>  <code>abstractmethod</code>","text":"<p>Get the current project name.</p> <p>Returns:</p> Type Description <code>str</code> <p>Optional[str]: The current project name.</p> Source code in <code>client/ayon_core/host/abstract.py</code> <pre><code>@abstractmethod\ndef get_current_project_name(self) -&gt; str:\n    \"\"\"Get the current project name.\n\n    Returns:\n        Optional[str]: The current project name.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/abstract.html#client.ayon_core.host.abstract.AbstractHost.get_current_task_name","title":"<code>get_current_task_name()</code>  <code>abstractmethod</code>","text":"<p>Get the current task name.</p> <p>Returns:</p> Type Description <code>Optional[str]</code> <p>Optional[str]: The current task name.</p> Source code in <code>client/ayon_core/host/abstract.py</code> <pre><code>@abstractmethod\ndef get_current_task_name(self) -&gt; Optional[str]:\n    \"\"\"Get the current task name.\n\n    Returns:\n        Optional[str]: The current task name.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/abstract.html#client.ayon_core.host.abstract.AbstractHost.set_current_context","title":"<code>set_current_context(folder_entity, task_entity, *, reason=ContextChangeReason.undefined, project_entity=None, anatomy=None)</code>  <code>abstractmethod</code>","text":"<p>Change context of the host.</p> <p>Parameters:</p> Name Type Description Default <code>folder_entity</code> <code>dict[str, Any]</code> <p>Folder entity.</p> required <code>task_entity</code> <code>dict[str, Any]</code> <p>Task entity.</p> required <code>reason</code> <code>ContextChangeReason</code> <p>Reason for change.</p> <code>undefined</code> <code>project_entity</code> <code>dict[str, Any]</code> <p>Project entity.</p> <code>None</code> <code>anatomy</code> <code>Anatomy</code> <p>Anatomy entity.</p> <code>None</code> Source code in <code>client/ayon_core/host/abstract.py</code> <pre><code>@abstractmethod\ndef set_current_context(\n    self,\n    folder_entity: dict[str, Any],\n    task_entity: dict[str, Any],\n    *,\n    reason: ContextChangeReason = ContextChangeReason.undefined,\n    project_entity: Optional[dict[str, Any]] = None,\n    anatomy: Optional[Anatomy] = None,\n) -&gt; HostContextData:\n    \"\"\"Change context of the host.\n\n    Args:\n        folder_entity (dict[str, Any]): Folder entity.\n        task_entity (dict[str, Any]): Task entity.\n        reason (ContextChangeReason): Reason for change.\n        project_entity (dict[str, Any]): Project entity.\n        anatomy (Anatomy): Anatomy entity.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/abstract.html#client.ayon_core.host.abstract.ApplicationInformation","title":"<code>ApplicationInformation</code>  <code>dataclass</code>","text":"<p>Application information.</p> <p>Attributes:</p> Name Type Description <code>app_name</code> <code>Optional[str]</code> <p>Application name. e.g. Maya, NukeX, Nuke</p> <code>app_version</code> <code>Optional[str]</code> <p>Application version. e.g. 15.2.1</p> Source code in <code>client/ayon_core/host/abstract.py</code> <pre><code>@dataclass\nclass ApplicationInformation:\n    \"\"\"Application information.\n\n    Attributes:\n        app_name (Optional[str]): Application name. e.g. Maya, NukeX, Nuke\n        app_version (Optional[str]): Application version. e.g. 15.2.1\n\n    \"\"\"\n    app_name: Optional[str] = None\n    app_version: Optional[str] = None\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/constants.html","title":"constants","text":""},{"location":"autoapi/client/ayon_core/host/constants.html#client.ayon_core.host.constants.ContextChangeReason","title":"<code>ContextChangeReason</code>","text":"<p>               Bases: <code>StrEnum</code></p> <p>Reasons for context change in the host.</p> Source code in <code>client/ayon_core/host/constants.py</code> <pre><code>class ContextChangeReason(StrEnum):\n    \"\"\"Reasons for context change in the host.\"\"\"\n    undefined = \"undefined\"\n    workfile_open = \"workfile.opened\"\n    workfile_save = \"workfile.saved\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/dirmap.html","title":"dirmap","text":"<p>Dirmap functionality used in host integrations inside DCCs.</p> <p>Idea for current dirmap implementation was used from Maya where is possible to enter source and destination roots and maya will try each found source in referenced file replace with each destination paths. First path which exists is used.</p>"},{"location":"autoapi/client/ayon_core/host/dirmap.html#client.ayon_core.host.dirmap.HostDirmap","title":"<code>HostDirmap</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Abstract class for running dirmap on a workfile in a host.</p> <p>Dirmap is used to translate paths inside of host workfile from one OS to another. (Eg. arstist created workfile on Win, different artists opens same file on Linux.)</p> Expects methods to be implemented inside of host <p>on_dirmap_enabled: run host code for enabling dirmap do_dirmap: run host code to do actual remapping</p> Source code in <code>client/ayon_core/host/dirmap.py</code> <pre><code>class HostDirmap(ABC):\n    \"\"\"Abstract class for running dirmap on a workfile in a host.\n\n    Dirmap is used to translate paths inside of host workfile from one\n    OS to another. (Eg. arstist created workfile on Win, different artists\n    opens same file on Linux.)\n\n    Expects methods to be implemented inside of host:\n        on_dirmap_enabled: run host code for enabling dirmap\n        do_dirmap: run host code to do actual remapping\n    \"\"\"\n\n    def __init__(\n        self,\n        host_name,\n        project_name,\n        project_settings=None,\n        sitesync_addon=None\n    ):\n        self.host_name = host_name\n        self.project_name = project_name\n        self._project_settings = project_settings\n        self._sitesync_addon = sitesync_addon\n        # to limit reinit of Modules\n        self._sitesync_addon_discovered = sitesync_addon is not None\n        self._log = None\n\n    @property\n    def sitesync_addon(self):\n        if not self._sitesync_addon_discovered:\n            self._sitesync_addon_discovered = True\n            manager = AddonsManager()\n            self._sitesync_addon = manager.get(\"sitesync\")\n        return self._sitesync_addon\n\n    @property\n    def project_settings(self):\n        if self._project_settings is None:\n            self._project_settings = get_project_settings(self.project_name)\n        return self._project_settings\n\n    @property\n    def log(self):\n        if self._log is None:\n            self._log = Logger.get_logger(self.__class__.__name__)\n        return self._log\n\n    @abstractmethod\n    def on_enable_dirmap(self):\n        \"\"\"Run host dependent operation for enabling dirmap if necessary.\"\"\"\n        pass\n\n    @abstractmethod\n    def dirmap_routine(self, source_path, destination_path):\n        \"\"\"Run host dependent remapping from source_path to destination_path\"\"\"\n        pass\n\n    def process_dirmap(self, mapping=None):\n        # type: (dict) -&gt; None\n        \"\"\"Go through all paths in Settings and set them using `dirmap`.\n\n            If artists has Site Sync enabled, take dirmap mapping directly from\n            Local Settings when artist is syncing workfile locally.\n\n        \"\"\"\n\n        if not mapping:\n            mapping = self.get_mappings()\n        if not mapping:\n            return\n\n        self.on_enable_dirmap()\n\n        for k, sp in enumerate(mapping[\"source_path\"]):\n            dst = mapping[\"destination_path\"][k]\n            try:\n                # add trailing slash if missing\n                sp = os.path.join(sp, '')\n                dst = os.path.join(dst, '')\n                print(\"{} -&gt; {}\".format(sp, dst))\n                self.dirmap_routine(sp, dst)\n            except IndexError:\n                # missing corresponding destination path\n                self.log.error((\n                    \"invalid dirmap mapping, missing corresponding\"\n                    \" destination directory.\"\n                ))\n                break\n            except RuntimeError:\n                self.log.error(\n                    \"invalid path {} -&gt; {}, mapping not registered\".format(\n                        sp, dst\n                    )\n                )\n                continue\n\n    def get_mappings(self):\n        \"\"\"Get translation from source_path to destination_path.\n\n            It checks if Site Sync is enabled and user chose to use local\n            site, in that case configuration in Local Settings takes precedence\n        \"\"\"\n        mapping_sett = self.project_settings[self.host_name].get(\"dirmap\", {})\n        local_mapping = self._get_local_sync_dirmap()\n        mapping_enabled = mapping_sett.get(\"enabled\") or bool(local_mapping)\n        if not mapping_enabled:\n            return {}\n\n        mapping = (\n            local_mapping\n            or mapping_sett[\"paths\"]\n            or {}\n        )\n\n        if (\n            not mapping\n            or not mapping.get(\"destination_path\")\n            or not mapping.get(\"source_path\")\n        ):\n            return {}\n        self.log.info(\"Processing directory mapping ...\")\n        self.log.info(\"mapping:: {}\".format(mapping))\n        return mapping\n\n    def _get_local_sync_dirmap(self):\n        \"\"\"\n            Returns dirmap if synch to local project is enabled.\n\n            Only valid mapping is from roots of remote site to local site set\n            in Local Settings.\n\n            Returns:\n                dict : { \"source_path\": [XXX], \"destination_path\": [YYYY]}\n        \"\"\"\n        project_name = self.project_name\n\n        sitesync_addon = self.sitesync_addon\n        mapping = {}\n        if (\n            sitesync_addon is None\n            or not sitesync_addon.enabled\n            or not sitesync_addon.is_project_enabled(project_name, True)\n        ):\n            return mapping\n\n        active_site = sitesync_addon.get_local_normalized_site(\n            sitesync_addon.get_active_site(project_name))\n        remote_site = sitesync_addon.get_local_normalized_site(\n            sitesync_addon.get_remote_site(project_name))\n        self.log.debug(\n            \"active {} - remote {}\".format(active_site, remote_site)\n        )\n\n        if active_site == \"local\" and active_site != remote_site:\n            sync_settings = sitesync_addon.get_sync_project_setting(\n                project_name,\n                exclude_locals=False,\n                cached=False)\n\n            # overrides for roots set in `Site Settings`\n            active_roots_overrides = self._get_site_root_overrides(\n                sitesync_addon, project_name, active_site)\n\n            remote_roots_overrides = self._get_site_root_overrides(\n                sitesync_addon, project_name, remote_site)\n\n            current_platform = platform.system().lower()\n            remote_provider = sitesync_addon.get_provider_for_site(\n                project_name, remote_site\n            )\n            # dirmap has sense only with regular disk provider, in the workfile\n            # won't be root on cloud or sftp provider so fallback to studio\n            if remote_provider != \"local_drive\":\n                remote_site = \"studio\"\n            for root_name, active_site_dir in active_roots_overrides.items():\n                remote_site_dir = (\n                    remote_roots_overrides.get(root_name)\n                    or sync_settings[\"sites\"][remote_site][\"root\"][root_name]\n                )\n\n                if isinstance(remote_site_dir, dict):\n                    remote_site_dir = remote_site_dir.get(current_platform)\n\n                if not remote_site_dir:\n                    continue\n\n                if os.path.isdir(active_site_dir):\n                    if \"destination_path\" not in mapping:\n                        mapping[\"destination_path\"] = []\n                    mapping[\"destination_path\"].append(active_site_dir)\n\n                    if \"source_path\" not in mapping:\n                        mapping[\"source_path\"] = []\n                    mapping[\"source_path\"].append(remote_site_dir)\n\n            self.log.debug(\"local sync mapping:: {}\".format(mapping))\n        return mapping\n\n    def _get_site_root_overrides(\n        self, sitesync_addon, project_name, site_name\n    ):\n        \"\"\"Safely handle root overrides.\n\n        SiteSync raises ValueError for non local or studio sites.\n        \"\"\"\n        # TODO: could be removed when `get_site_root_overrides` is not raising\n        #     an Error but just returns {}\n        try:\n            site_roots_overrides = sitesync_addon.get_site_root_overrides(\n                project_name, site_name)\n        except ValueError:\n            site_roots_overrides = {}\n        self.log.debug(\"{} roots overrides {}\".format(\n            site_name, site_roots_overrides))\n\n        return site_roots_overrides\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/dirmap.html#client.ayon_core.host.dirmap.HostDirmap.dirmap_routine","title":"<code>dirmap_routine(source_path, destination_path)</code>  <code>abstractmethod</code>","text":"<p>Run host dependent remapping from source_path to destination_path</p> Source code in <code>client/ayon_core/host/dirmap.py</code> <pre><code>@abstractmethod\ndef dirmap_routine(self, source_path, destination_path):\n    \"\"\"Run host dependent remapping from source_path to destination_path\"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/dirmap.html#client.ayon_core.host.dirmap.HostDirmap.get_mappings","title":"<code>get_mappings()</code>","text":"<p>Get translation from source_path to destination_path.</p> <p>It checks if Site Sync is enabled and user chose to use local site, in that case configuration in Local Settings takes precedence</p> Source code in <code>client/ayon_core/host/dirmap.py</code> <pre><code>def get_mappings(self):\n    \"\"\"Get translation from source_path to destination_path.\n\n        It checks if Site Sync is enabled and user chose to use local\n        site, in that case configuration in Local Settings takes precedence\n    \"\"\"\n    mapping_sett = self.project_settings[self.host_name].get(\"dirmap\", {})\n    local_mapping = self._get_local_sync_dirmap()\n    mapping_enabled = mapping_sett.get(\"enabled\") or bool(local_mapping)\n    if not mapping_enabled:\n        return {}\n\n    mapping = (\n        local_mapping\n        or mapping_sett[\"paths\"]\n        or {}\n    )\n\n    if (\n        not mapping\n        or not mapping.get(\"destination_path\")\n        or not mapping.get(\"source_path\")\n    ):\n        return {}\n    self.log.info(\"Processing directory mapping ...\")\n    self.log.info(\"mapping:: {}\".format(mapping))\n    return mapping\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/dirmap.html#client.ayon_core.host.dirmap.HostDirmap.on_enable_dirmap","title":"<code>on_enable_dirmap()</code>  <code>abstractmethod</code>","text":"<p>Run host dependent operation for enabling dirmap if necessary.</p> Source code in <code>client/ayon_core/host/dirmap.py</code> <pre><code>@abstractmethod\ndef on_enable_dirmap(self):\n    \"\"\"Run host dependent operation for enabling dirmap if necessary.\"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/dirmap.html#client.ayon_core.host.dirmap.HostDirmap.process_dirmap","title":"<code>process_dirmap(mapping=None)</code>","text":"<p>Go through all paths in Settings and set them using <code>dirmap</code>.</p> <p>If artists has Site Sync enabled, take dirmap mapping directly from Local Settings when artist is syncing workfile locally.</p> Source code in <code>client/ayon_core/host/dirmap.py</code> <pre><code>def process_dirmap(self, mapping=None):\n    # type: (dict) -&gt; None\n    \"\"\"Go through all paths in Settings and set them using `dirmap`.\n\n        If artists has Site Sync enabled, take dirmap mapping directly from\n        Local Settings when artist is syncing workfile locally.\n\n    \"\"\"\n\n    if not mapping:\n        mapping = self.get_mappings()\n    if not mapping:\n        return\n\n    self.on_enable_dirmap()\n\n    for k, sp in enumerate(mapping[\"source_path\"]):\n        dst = mapping[\"destination_path\"][k]\n        try:\n            # add trailing slash if missing\n            sp = os.path.join(sp, '')\n            dst = os.path.join(dst, '')\n            print(\"{} -&gt; {}\".format(sp, dst))\n            self.dirmap_routine(sp, dst)\n        except IndexError:\n            # missing corresponding destination path\n            self.log.error((\n                \"invalid dirmap mapping, missing corresponding\"\n                \" destination directory.\"\n            ))\n            break\n        except RuntimeError:\n            self.log.error(\n                \"invalid path {} -&gt; {}, mapping not registered\".format(\n                    sp, dst\n                )\n            )\n            continue\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/host.html","title":"host","text":""},{"location":"autoapi/client/ayon_core/host/host.html#client.ayon_core.host.host.HostBase","title":"<code>HostBase</code>","text":"<p>               Bases: <code>AbstractHost</code></p> <p>Base of host implementation class.</p> <p>Host is pipeline implementation of DCC application. This class should help to identify what must/should/can be implemented for specific functionality.</p> <p>Compared to 'avalon' concept: What was before considered as functions in host implementation folder. The host implementation should primarily care about adding ability of creation (mark products to be published) and optionally about referencing published representations as containers.</p> <p>Host may need extend some functionality like working with workfiles or loading. Not all host implementations may allow that for those purposes can be logic extended with implementing functions for the purpose. There are prepared interfaces to be able identify what must be implemented to be able use that functionality. - current statement is that it is not required to inherit from interfaces     but all of the methods are validated (only their existence!)</p>"},{"location":"autoapi/client/ayon_core/host/host.html#client.ayon_core.host.host.HostBase--installation-of-host-before-avalon-concept","title":"Installation of host before (avalon concept):","text":"<pre><code>from ayon_core.pipeline import install_host\nimport ayon_core.hosts.maya.api as host\n\ninstall_host(host)\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/host.html#client.ayon_core.host.host.HostBase--installation-of-host-now","title":"Installation of host now:","text":"<pre><code>from ayon_core.pipeline import install_host\nfrom ayon_core.hosts.maya.api import MayaHost\n\nhost = MayaHost()\ninstall_host(host)\n</code></pre> Todo <ul> <li>move content of 'install_host' as method of this class<ul> <li>register host object</li> <li>install global plugin paths</li> </ul> </li> <li>store registered plugin paths to this object</li> <li>handle current context (project, asset, task)<ul> <li>this must be done in many separated steps</li> </ul> </li> <li>have it's object of host tools instead of using globals</li> </ul> <p>This implementation will probably change over time when more     functionality and responsibility will be added.</p> Source code in <code>client/ayon_core/host/host.py</code> <pre><code>class HostBase(AbstractHost):\n    \"\"\"Base of host implementation class.\n\n    Host is pipeline implementation of DCC application. This class should help\n    to identify what must/should/can be implemented for specific functionality.\n\n    Compared to 'avalon' concept:\n    What was before considered as functions in host implementation folder. The\n    host implementation should primarily care about adding ability of creation\n    (mark products to be published) and optionally about referencing published\n    representations as containers.\n\n    Host may need extend some functionality like working with workfiles\n    or loading. Not all host implementations may allow that for those purposes\n    can be logic extended with implementing functions for the purpose. There\n    are prepared interfaces to be able identify what must be implemented to\n    be able use that functionality.\n    - current statement is that it is not required to inherit from interfaces\n        but all of the methods are validated (only their existence!)\n\n    # Installation of host before (avalon concept):\n    ```python\n    from ayon_core.pipeline import install_host\n    import ayon_core.hosts.maya.api as host\n\n    install_host(host)\n    ```\n\n    # Installation of host now:\n    ```python\n    from ayon_core.pipeline import install_host\n    from ayon_core.hosts.maya.api import MayaHost\n\n    host = MayaHost()\n    install_host(host)\n    ```\n\n    Todo:\n        - move content of 'install_host' as method of this class\n            - register host object\n            - install global plugin paths\n        - store registered plugin paths to this object\n        - handle current context (project, asset, task)\n            - this must be done in many separated steps\n        - have it's object of host tools instead of using globals\n\n    This implementation will probably change over time when more\n        functionality and responsibility will be added.\n    \"\"\"\n\n    _log = None\n\n    def __init__(self):\n        \"\"\"Initialization of host.\n\n        Register DCC callbacks, host specific plugin paths, targets etc.\n        (Part of what 'install' did in 'avalon' concept.)\n\n        Note:\n            At this moment global \"installation\" must happen before host\n            installation. Because of this current limitation it is recommended\n            to implement 'install' method which is triggered after global\n            'install'.\n        \"\"\"\n\n        pass\n\n    def get_app_information(self) -&gt; ApplicationInformation:\n        \"\"\"Running application information.\n\n        Host integration should override this method and return correct\n            information.\n\n        Returns:\n            ApplicationInformation: Application information.\n\n        \"\"\"\n        return ApplicationInformation()\n\n    def install(self):\n        \"\"\"Install host specific functionality.\n\n        This is where should be added menu with tools, registered callbacks\n        and other host integration initialization.\n\n        It is called automatically when 'ayon_core.pipeline.install_host' is\n        triggered.\n\n        \"\"\"\n        pass\n\n    @property\n    def log(self) -&gt; logging.Logger:\n        if self._log is None:\n            self._log = logging.getLogger(self.__class__.__name__)\n        return self._log\n\n    def get_current_project_name(self) -&gt; str:\n        \"\"\"\n        Returns:\n            str: Current project name.\n\n        \"\"\"\n        return os.environ[\"AYON_PROJECT_NAME\"]\n\n    def get_current_folder_path(self) -&gt; Optional[str]:\n        \"\"\"\n        Returns:\n            Optional[str]: Current folder path.\n\n        \"\"\"\n        return os.environ.get(\"AYON_FOLDER_PATH\")\n\n    def get_current_task_name(self) -&gt; Optional[str]:\n        \"\"\"\n        Returns:\n            Optional[str]: Current task name.\n\n        \"\"\"\n        return os.environ.get(\"AYON_TASK_NAME\")\n\n    def get_current_context(self) -&gt; HostContextData:\n        \"\"\"Get current context information.\n\n        This method should be used to get current context of host. Usage of\n        this method can be crucial for host implementations in DCCs where\n        can be opened multiple workfiles at one moment and change of context\n        can't be caught properly.\n\n        Returns:\n            HostContextData: Current context with 'project_name',\n                'folder_path' and 'task_name'.\n\n        \"\"\"\n        return {\n            \"project_name\": self.get_current_project_name(),\n            \"folder_path\": self.get_current_folder_path(),\n            \"task_name\": self.get_current_task_name()\n        }\n\n    def set_current_context(\n        self,\n        folder_entity: dict[str, Any],\n        task_entity: dict[str, Any],\n        *,\n        reason: ContextChangeReason = ContextChangeReason.undefined,\n        project_entity: Optional[dict[str, Any]] = None,\n        anatomy: Optional[Anatomy] = None,\n    ) -&gt; HostContextData:\n        \"\"\"Set current context information.\n\n        This method should be used to set current context of host. Usage of\n        this method can be crucial for host implementations in DCCs where\n        can be opened multiple workfiles at one moment and change of context\n        can't be caught properly.\n\n        Notes:\n            This method should not care about change of workdir and expect any\n                of the arguments.\n\n        Args:\n            folder_entity (Optional[dict[str, Any]]): Folder entity.\n            task_entity (Optional[dict[str, Any]]): Task entity.\n            reason (ContextChangeReason): Reason for context change.\n            project_entity (Optional[dict[str, Any]]): Project entity data.\n            anatomy (Optional[Anatomy]): Anatomy instance for the project.\n\n        Returns:\n            dict[str, Optional[str]]: Context information with project name,\n                folder path and task name.\n\n        \"\"\"\n        from ayon_core.pipeline import Anatomy\n\n        folder_path = folder_entity[\"path\"]\n        task_name = task_entity[\"name\"]\n\n        context = self.get_current_context()\n        # Don't do anything if context did not change\n        if (\n            context[\"folder_path\"] == folder_path\n            and context[\"task_name\"] == task_name\n        ):\n            return context\n\n        project_name = self.get_current_project_name()\n        if project_entity is None:\n            project_entity = ayon_api.get_project(project_name)\n\n        if anatomy is None:\n            anatomy = Anatomy(project_name, project_entity=project_entity)\n\n        context_change_data = ContextChangeData(\n            project_entity,\n            folder_entity,\n            task_entity,\n            reason,\n            anatomy,\n        )\n        self._before_context_change(context_change_data)\n        self._set_current_context(context_change_data)\n        self._after_context_change(context_change_data)\n\n        return self._emit_context_change_event(\n            project_name,\n            folder_path,\n            task_name,\n        )\n\n    def get_context_title(self):\n        \"\"\"Context title shown for UI purposes.\n\n        Should return current context title if possible.\n\n        Note:\n            This method is used only for UI purposes so it is possible to\n                return some logical title for contextless cases.\n            Is not meant for \"Context menu\" label.\n\n        Returns:\n            str: Context title.\n            None: Default title is used based on UI implementation.\n        \"\"\"\n\n        # Use current context to fill the context title\n        current_context = self.get_current_context()\n        project_name = current_context[\"project_name\"]\n        folder_path = current_context[\"folder_path\"]\n        task_name = current_context[\"task_name\"]\n        items = []\n        if project_name:\n            items.append(project_name)\n            if folder_path:\n                items.append(folder_path.lstrip(\"/\"))\n                if task_name:\n                    items.append(task_name)\n        if items:\n            return \"/\".join(items)\n        return None\n\n    @contextlib.contextmanager\n    def maintained_selection(self):\n        \"\"\"Some functionlity will happen but selection should stay same.\n\n        This is DCC specific. Some may not allow to implement this ability\n        that is reason why default implementation is empty context manager.\n\n        Yields:\n            None: Yield when is ready to restore selected at the end.\n        \"\"\"\n\n        try:\n            yield\n        finally:\n            pass\n\n    def _emit_context_change_event(\n        self,\n        project_name: str,\n        folder_path: Optional[str],\n        task_name: Optional[str],\n    ) -&gt; HostContextData:\n        \"\"\"Emit context change event.\n\n        Args:\n            project_name (str): Name of the project.\n            folder_path (Optional[str]): Path of the folder.\n            task_name (Optional[str]): Name of the task.\n\n        Returns:\n            HostContextData: Data send to context change event.\n\n        \"\"\"\n        data: HostContextData = {\n            \"project_name\": project_name,\n            \"folder_path\": folder_path,\n            \"task_name\": task_name,\n        }\n        emit_event(\"taskChanged\", data)\n        return data\n\n    def _set_current_context(\n        self, context_change_data: ContextChangeData\n    ) -&gt; None:\n        \"\"\"Method that changes the context in host.\n\n        Can be overriden for hosts that do need different handling of context\n            than using environment variables.\n\n        Args:\n            context_change_data (ContextChangeData): Context change related\n                data.\n\n        \"\"\"\n        project_name = self.get_current_project_name()\n        folder_path = None\n        task_name = None\n        if context_change_data.folder_entity:\n            folder_path = context_change_data.folder_entity[\"path\"]\n            if context_change_data.task_entity:\n                task_name = context_change_data.task_entity[\"name\"]\n\n        envs = {\n            \"AYON_PROJECT_NAME\": project_name,\n            \"AYON_FOLDER_PATH\": folder_path,\n            \"AYON_TASK_NAME\": task_name,\n        }\n\n        # Update the Session and environments. Pop from environments all\n        #   keys with value set to None.\n        for key, value in envs.items():\n            if value is None:\n                os.environ.pop(key, None)\n            else:\n                os.environ[key] = value\n\n    def _before_context_change(self, context_change_data: ContextChangeData):\n        \"\"\"Before context is changed.\n\n        This method is called before the context is changed in the host.\n\n        Can be overridden to implement host specific logic.\n\n        Args:\n            context_change_data (ContextChangeData): Object with information\n                about context change.\n\n        \"\"\"\n        pass\n\n    def _after_context_change(self, context_change_data: ContextChangeData):\n        \"\"\"After context is changed.\n\n        This method is called after the context is changed in the host.\n\n        Can be overridden to implement host specific logic.\n\n        Args:\n            context_change_data (ContextChangeData): Object with information\n                about context change.\n\n        \"\"\"\n        pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/host.html#client.ayon_core.host.host.HostBase.__init__","title":"<code>__init__()</code>","text":"<p>Initialization of host.</p> <p>Register DCC callbacks, host specific plugin paths, targets etc. (Part of what 'install' did in 'avalon' concept.)</p> Note <p>At this moment global \"installation\" must happen before host installation. Because of this current limitation it is recommended to implement 'install' method which is triggered after global 'install'.</p> Source code in <code>client/ayon_core/host/host.py</code> <pre><code>def __init__(self):\n    \"\"\"Initialization of host.\n\n    Register DCC callbacks, host specific plugin paths, targets etc.\n    (Part of what 'install' did in 'avalon' concept.)\n\n    Note:\n        At this moment global \"installation\" must happen before host\n        installation. Because of this current limitation it is recommended\n        to implement 'install' method which is triggered after global\n        'install'.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/host.html#client.ayon_core.host.host.HostBase.get_app_information","title":"<code>get_app_information()</code>","text":"<p>Running application information.</p> <p>Host integration should override this method and return correct     information.</p> <p>Returns:</p> Name Type Description <code>ApplicationInformation</code> <code>ApplicationInformation</code> <p>Application information.</p> Source code in <code>client/ayon_core/host/host.py</code> <pre><code>def get_app_information(self) -&gt; ApplicationInformation:\n    \"\"\"Running application information.\n\n    Host integration should override this method and return correct\n        information.\n\n    Returns:\n        ApplicationInformation: Application information.\n\n    \"\"\"\n    return ApplicationInformation()\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/host.html#client.ayon_core.host.host.HostBase.get_context_title","title":"<code>get_context_title()</code>","text":"<p>Context title shown for UI purposes.</p> <p>Should return current context title if possible.</p> Note <p>This method is used only for UI purposes so it is possible to     return some logical title for contextless cases. Is not meant for \"Context menu\" label.</p> <p>Returns:</p> Name Type Description <code>str</code> <p>Context title.</p> <code>None</code> <p>Default title is used based on UI implementation.</p> Source code in <code>client/ayon_core/host/host.py</code> <pre><code>def get_context_title(self):\n    \"\"\"Context title shown for UI purposes.\n\n    Should return current context title if possible.\n\n    Note:\n        This method is used only for UI purposes so it is possible to\n            return some logical title for contextless cases.\n        Is not meant for \"Context menu\" label.\n\n    Returns:\n        str: Context title.\n        None: Default title is used based on UI implementation.\n    \"\"\"\n\n    # Use current context to fill the context title\n    current_context = self.get_current_context()\n    project_name = current_context[\"project_name\"]\n    folder_path = current_context[\"folder_path\"]\n    task_name = current_context[\"task_name\"]\n    items = []\n    if project_name:\n        items.append(project_name)\n        if folder_path:\n            items.append(folder_path.lstrip(\"/\"))\n            if task_name:\n                items.append(task_name)\n    if items:\n        return \"/\".join(items)\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/host.html#client.ayon_core.host.host.HostBase.get_current_context","title":"<code>get_current_context()</code>","text":"<p>Get current context information.</p> <p>This method should be used to get current context of host. Usage of this method can be crucial for host implementations in DCCs where can be opened multiple workfiles at one moment and change of context can't be caught properly.</p> <p>Returns:</p> Name Type Description <code>HostContextData</code> <code>HostContextData</code> <p>Current context with 'project_name', 'folder_path' and 'task_name'.</p> Source code in <code>client/ayon_core/host/host.py</code> <pre><code>def get_current_context(self) -&gt; HostContextData:\n    \"\"\"Get current context information.\n\n    This method should be used to get current context of host. Usage of\n    this method can be crucial for host implementations in DCCs where\n    can be opened multiple workfiles at one moment and change of context\n    can't be caught properly.\n\n    Returns:\n        HostContextData: Current context with 'project_name',\n            'folder_path' and 'task_name'.\n\n    \"\"\"\n    return {\n        \"project_name\": self.get_current_project_name(),\n        \"folder_path\": self.get_current_folder_path(),\n        \"task_name\": self.get_current_task_name()\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/host.html#client.ayon_core.host.host.HostBase.get_current_folder_path","title":"<code>get_current_folder_path()</code>","text":"<p>Returns:</p> Type Description <code>Optional[str]</code> <p>Optional[str]: Current folder path.</p> Source code in <code>client/ayon_core/host/host.py</code> <pre><code>def get_current_folder_path(self) -&gt; Optional[str]:\n    \"\"\"\n    Returns:\n        Optional[str]: Current folder path.\n\n    \"\"\"\n    return os.environ.get(\"AYON_FOLDER_PATH\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/host.html#client.ayon_core.host.host.HostBase.get_current_project_name","title":"<code>get_current_project_name()</code>","text":"<p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Current project name.</p> Source code in <code>client/ayon_core/host/host.py</code> <pre><code>def get_current_project_name(self) -&gt; str:\n    \"\"\"\n    Returns:\n        str: Current project name.\n\n    \"\"\"\n    return os.environ[\"AYON_PROJECT_NAME\"]\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/host.html#client.ayon_core.host.host.HostBase.get_current_task_name","title":"<code>get_current_task_name()</code>","text":"<p>Returns:</p> Type Description <code>Optional[str]</code> <p>Optional[str]: Current task name.</p> Source code in <code>client/ayon_core/host/host.py</code> <pre><code>def get_current_task_name(self) -&gt; Optional[str]:\n    \"\"\"\n    Returns:\n        Optional[str]: Current task name.\n\n    \"\"\"\n    return os.environ.get(\"AYON_TASK_NAME\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/host.html#client.ayon_core.host.host.HostBase.install","title":"<code>install()</code>","text":"<p>Install host specific functionality.</p> <p>This is where should be added menu with tools, registered callbacks and other host integration initialization.</p> <p>It is called automatically when 'ayon_core.pipeline.install_host' is triggered.</p> Source code in <code>client/ayon_core/host/host.py</code> <pre><code>def install(self):\n    \"\"\"Install host specific functionality.\n\n    This is where should be added menu with tools, registered callbacks\n    and other host integration initialization.\n\n    It is called automatically when 'ayon_core.pipeline.install_host' is\n    triggered.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/host.html#client.ayon_core.host.host.HostBase.maintained_selection","title":"<code>maintained_selection()</code>","text":"<p>Some functionlity will happen but selection should stay same.</p> <p>This is DCC specific. Some may not allow to implement this ability that is reason why default implementation is empty context manager.</p> <p>Yields:</p> Name Type Description <code>None</code> <p>Yield when is ready to restore selected at the end.</p> Source code in <code>client/ayon_core/host/host.py</code> <pre><code>@contextlib.contextmanager\ndef maintained_selection(self):\n    \"\"\"Some functionlity will happen but selection should stay same.\n\n    This is DCC specific. Some may not allow to implement this ability\n    that is reason why default implementation is empty context manager.\n\n    Yields:\n        None: Yield when is ready to restore selected at the end.\n    \"\"\"\n\n    try:\n        yield\n    finally:\n        pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/host.html#client.ayon_core.host.host.HostBase.set_current_context","title":"<code>set_current_context(folder_entity, task_entity, *, reason=ContextChangeReason.undefined, project_entity=None, anatomy=None)</code>","text":"<p>Set current context information.</p> <p>This method should be used to set current context of host. Usage of this method can be crucial for host implementations in DCCs where can be opened multiple workfiles at one moment and change of context can't be caught properly.</p> Notes <p>This method should not care about change of workdir and expect any     of the arguments.</p> <p>Parameters:</p> Name Type Description Default <code>folder_entity</code> <code>Optional[dict[str, Any]]</code> <p>Folder entity.</p> required <code>task_entity</code> <code>Optional[dict[str, Any]]</code> <p>Task entity.</p> required <code>reason</code> <code>ContextChangeReason</code> <p>Reason for context change.</p> <code>undefined</code> <code>project_entity</code> <code>Optional[dict[str, Any]]</code> <p>Project entity data.</p> <code>None</code> <code>anatomy</code> <code>Optional[Anatomy]</code> <p>Anatomy instance for the project.</p> <code>None</code> <p>Returns:</p> Type Description <code>HostContextData</code> <p>dict[str, Optional[str]]: Context information with project name, folder path and task name.</p> Source code in <code>client/ayon_core/host/host.py</code> <pre><code>def set_current_context(\n    self,\n    folder_entity: dict[str, Any],\n    task_entity: dict[str, Any],\n    *,\n    reason: ContextChangeReason = ContextChangeReason.undefined,\n    project_entity: Optional[dict[str, Any]] = None,\n    anatomy: Optional[Anatomy] = None,\n) -&gt; HostContextData:\n    \"\"\"Set current context information.\n\n    This method should be used to set current context of host. Usage of\n    this method can be crucial for host implementations in DCCs where\n    can be opened multiple workfiles at one moment and change of context\n    can't be caught properly.\n\n    Notes:\n        This method should not care about change of workdir and expect any\n            of the arguments.\n\n    Args:\n        folder_entity (Optional[dict[str, Any]]): Folder entity.\n        task_entity (Optional[dict[str, Any]]): Task entity.\n        reason (ContextChangeReason): Reason for context change.\n        project_entity (Optional[dict[str, Any]]): Project entity data.\n        anatomy (Optional[Anatomy]): Anatomy instance for the project.\n\n    Returns:\n        dict[str, Optional[str]]: Context information with project name,\n            folder path and task name.\n\n    \"\"\"\n    from ayon_core.pipeline import Anatomy\n\n    folder_path = folder_entity[\"path\"]\n    task_name = task_entity[\"name\"]\n\n    context = self.get_current_context()\n    # Don't do anything if context did not change\n    if (\n        context[\"folder_path\"] == folder_path\n        and context[\"task_name\"] == task_name\n    ):\n        return context\n\n    project_name = self.get_current_project_name()\n    if project_entity is None:\n        project_entity = ayon_api.get_project(project_name)\n\n    if anatomy is None:\n        anatomy = Anatomy(project_name, project_entity=project_entity)\n\n    context_change_data = ContextChangeData(\n        project_entity,\n        folder_entity,\n        task_entity,\n        reason,\n        anatomy,\n    )\n    self._before_context_change(context_change_data)\n    self._set_current_context(context_change_data)\n    self._after_context_change(context_change_data)\n\n    return self._emit_context_change_event(\n        project_name,\n        folder_path,\n        task_name,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/typing.html","title":"typing","text":""},{"location":"autoapi/client/ayon_core/host/interfaces/index.html","title":"interfaces","text":""},{"location":"autoapi/client/ayon_core/host/interfaces/index.html#client.ayon_core.host.interfaces.CopyPublishedWorkfileOptionalData","title":"<code>CopyPublishedWorkfileOptionalData</code>","text":"<p>               Bases: <code>SaveWorkfileOptionalData</code></p> <p>Optional data to copy published workfile.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>class CopyPublishedWorkfileOptionalData(SaveWorkfileOptionalData):\n    \"\"\"Optional data to copy published workfile.\"\"\"\n    data_version = 1\n\n    def __init__(\n        self,\n        project_entity: Optional[dict[str, Any]] = None,\n        anatomy: Optional[\"Anatomy\"] = None,\n        project_settings: Optional[dict[str, Any]] = None,\n        rootless_path: Optional[str] = None,\n        workfile_entities: Optional[list[dict[str, Any]]] = None,\n        src_anatomy: Optional[\"Anatomy\"] = None,\n        src_representation_path: Optional[str] = None,\n        **kwargs\n    ):\n        super().__init__(\n            rootless_path=rootless_path,\n            workfile_entities=workfile_entities,\n            project_entity=project_entity,\n            anatomy=anatomy,\n            project_settings=project_settings,\n            **kwargs\n        )\n        self.src_anatomy = src_anatomy\n        self.src_representation_path = src_representation_path\n\n    def get_source_data(\n        self,\n        current_anatomy: Optional[\"Anatomy\"],\n        project_name: str,\n        representation_entity: dict[str, Any],\n    ) -&gt; tuple[\"Anatomy\", str]:\n        from ayon_core.pipeline import Anatomy\n        from ayon_core.pipeline.load import (\n            get_representation_path_with_anatomy\n        )\n\n        src_anatomy = self.src_anatomy\n\n        if (\n            src_anatomy is None\n            and current_anatomy is not None\n            and current_anatomy.project_name == project_name\n        ):\n            src_anatomy = current_anatomy\n        else:\n            src_anatomy = Anatomy(project_name)\n\n        repre_path = self.src_representation_path\n        if repre_path is None:\n            repre_path = get_representation_path_with_anatomy(\n                representation_entity,\n                src_anatomy,\n            )\n        return src_anatomy, repre_path\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/index.html#client.ayon_core.host.interfaces.CopyWorkfileOptionalData","title":"<code>CopyWorkfileOptionalData</code>","text":"<p>               Bases: <code>SaveWorkfileOptionalData</code></p> <p>Optional data to copy workfile.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>class CopyWorkfileOptionalData(SaveWorkfileOptionalData):\n    \"\"\"Optional data to copy workfile.\"\"\"\n    data_version = 1\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/index.html#client.ayon_core.host.interfaces.ILoadHost","title":"<code>ILoadHost</code>","text":"<p>               Bases: <code>AbstractHost</code></p> <p>Implementation requirements to be able use reference of representations.</p> <p>The load plugins can do referencing even without implementation of methods here, but switch and removement of containers would not be possible.</p> Questions <ul> <li>Is list container dependency of host or load plugins?</li> <li>Should this be directly in HostBase?<ul> <li>how to find out if referencing is available?</li> <li>do we need to know that?</li> </ul> </li> </ul> Source code in <code>client/ayon_core/host/interfaces/interfaces.py</code> <pre><code>class ILoadHost(AbstractHost):\n    \"\"\"Implementation requirements to be able use reference of representations.\n\n    The load plugins can do referencing even without implementation of methods\n    here, but switch and removement of containers would not be possible.\n\n    Questions:\n        - Is list container dependency of host or load plugins?\n        - Should this be directly in HostBase?\n            - how to find out if referencing is available?\n            - do we need to know that?\n    \"\"\"\n\n    @staticmethod\n    def get_missing_load_methods(host):\n        \"\"\"Look for missing methods on \"old type\" host implementation.\n\n        Method is used for validation of implemented functions related to\n        loading. Checks only existence of methods.\n\n        Args:\n            Union[ModuleType, AbstractHost]: Object of host where to look for\n                required methods.\n\n        Returns:\n            list[str]: Missing method implementations for loading workflow.\n        \"\"\"\n\n        if isinstance(host, ILoadHost):\n            return []\n\n        required = [\"ls\"]\n        missing = []\n        for name in required:\n            if not hasattr(host, name):\n                missing.append(name)\n        return missing\n\n    @staticmethod\n    def validate_load_methods(host):\n        \"\"\"Validate implemented methods of \"old type\" host for load workflow.\n\n        Args:\n            Union[ModuleType, AbstractHost]: Object of host to validate.\n\n        Raises:\n            MissingMethodsError: If there are missing methods on host\n                implementation.\n        \"\"\"\n        missing = ILoadHost.get_missing_load_methods(host)\n        if missing:\n            raise MissingMethodsError(host, missing)\n\n    @abstractmethod\n    def get_containers(self):\n        \"\"\"Retrieve referenced containers from scene.\n\n        This can be implemented in hosts where referencing can be used.\n\n        Todo:\n            Rename function to something more self explanatory.\n                Suggestion: 'get_containers'\n\n        Returns:\n            list[dict]: Information about loaded containers.\n        \"\"\"\n\n        pass\n\n    # --- Deprecated method names ---\n    def ls(self):\n        \"\"\"Deprecated variant of 'get_containers'.\n\n        Todo:\n            Remove when all usages are replaced.\n        \"\"\"\n\n        return self.get_containers()\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/index.html#client.ayon_core.host.interfaces.ILoadHost.get_containers","title":"<code>get_containers()</code>  <code>abstractmethod</code>","text":"<p>Retrieve referenced containers from scene.</p> <p>This can be implemented in hosts where referencing can be used.</p> Todo <p>Rename function to something more self explanatory.     Suggestion: 'get_containers'</p> <p>Returns:</p> Type Description <p>list[dict]: Information about loaded containers.</p> Source code in <code>client/ayon_core/host/interfaces/interfaces.py</code> <pre><code>@abstractmethod\ndef get_containers(self):\n    \"\"\"Retrieve referenced containers from scene.\n\n    This can be implemented in hosts where referencing can be used.\n\n    Todo:\n        Rename function to something more self explanatory.\n            Suggestion: 'get_containers'\n\n    Returns:\n        list[dict]: Information about loaded containers.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/index.html#client.ayon_core.host.interfaces.ILoadHost.get_missing_load_methods","title":"<code>get_missing_load_methods(host)</code>  <code>staticmethod</code>","text":"<p>Look for missing methods on \"old type\" host implementation.</p> <p>Method is used for validation of implemented functions related to loading. Checks only existence of methods.</p> <p>Parameters:</p> Name Type Description Default <code>Union[ModuleType, AbstractHost]</code> <p>Object of host where to look for required methods.</p> required <p>Returns:</p> Type Description <p>list[str]: Missing method implementations for loading workflow.</p> Source code in <code>client/ayon_core/host/interfaces/interfaces.py</code> <pre><code>@staticmethod\ndef get_missing_load_methods(host):\n    \"\"\"Look for missing methods on \"old type\" host implementation.\n\n    Method is used for validation of implemented functions related to\n    loading. Checks only existence of methods.\n\n    Args:\n        Union[ModuleType, AbstractHost]: Object of host where to look for\n            required methods.\n\n    Returns:\n        list[str]: Missing method implementations for loading workflow.\n    \"\"\"\n\n    if isinstance(host, ILoadHost):\n        return []\n\n    required = [\"ls\"]\n    missing = []\n    for name in required:\n        if not hasattr(host, name):\n            missing.append(name)\n    return missing\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/index.html#client.ayon_core.host.interfaces.ILoadHost.ls","title":"<code>ls()</code>","text":"<p>Deprecated variant of 'get_containers'.</p> Todo <p>Remove when all usages are replaced.</p> Source code in <code>client/ayon_core/host/interfaces/interfaces.py</code> <pre><code>def ls(self):\n    \"\"\"Deprecated variant of 'get_containers'.\n\n    Todo:\n        Remove when all usages are replaced.\n    \"\"\"\n\n    return self.get_containers()\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/index.html#client.ayon_core.host.interfaces.ILoadHost.validate_load_methods","title":"<code>validate_load_methods(host)</code>  <code>staticmethod</code>","text":"<p>Validate implemented methods of \"old type\" host for load workflow.</p> <p>Parameters:</p> Name Type Description Default <code>Union[ModuleType, AbstractHost]</code> <p>Object of host to validate.</p> required <p>Raises:</p> Type Description <code>MissingMethodsError</code> <p>If there are missing methods on host implementation.</p> Source code in <code>client/ayon_core/host/interfaces/interfaces.py</code> <pre><code>@staticmethod\ndef validate_load_methods(host):\n    \"\"\"Validate implemented methods of \"old type\" host for load workflow.\n\n    Args:\n        Union[ModuleType, AbstractHost]: Object of host to validate.\n\n    Raises:\n        MissingMethodsError: If there are missing methods on host\n            implementation.\n    \"\"\"\n    missing = ILoadHost.get_missing_load_methods(host)\n    if missing:\n        raise MissingMethodsError(host, missing)\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/index.html#client.ayon_core.host.interfaces.INewPublisher","title":"<code>INewPublisher</code>","text":"<p>               Bases: <code>IPublishHost</code></p> <p>Legacy interface replaced by 'IPublishHost'.</p> Deprecated <p>'INewPublisher' is replaced by 'IPublishHost' please change your imports. There is no \"reasonable\" way hot mark these classes as deprecated to show warning of wrong import. Deprecated since 3.14. will be removed in 3.15.</p> Source code in <code>client/ayon_core/host/interfaces/interfaces.py</code> <pre><code>class INewPublisher(IPublishHost):\n    \"\"\"Legacy interface replaced by 'IPublishHost'.\n\n    Deprecated:\n        'INewPublisher' is replaced by 'IPublishHost' please change your\n        imports.\n        There is no \"reasonable\" way hot mark these classes as deprecated\n        to show warning of wrong import. Deprecated since 3.14.* will be\n        removed in 3.15.*\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/index.html#client.ayon_core.host.interfaces.IPublishHost","title":"<code>IPublishHost</code>","text":"<p>               Bases: <code>AbstractHost</code></p> <p>Functions related to new creation system in new publisher.</p> <p>New publisher is not storing information only about each created instance but also some global data. At this moment are data related only to context publish plugins but that can extend in future.</p> Source code in <code>client/ayon_core/host/interfaces/interfaces.py</code> <pre><code>class IPublishHost(AbstractHost):\n    \"\"\"Functions related to new creation system in new publisher.\n\n    New publisher is not storing information only about each created instance\n    but also some global data. At this moment are data related only to context\n    publish plugins but that can extend in future.\n    \"\"\"\n\n    @staticmethod\n    def get_missing_publish_methods(host):\n        \"\"\"Look for missing methods on \"old type\" host implementation.\n\n        Method is used for validation of implemented functions related to\n        new publish creation. Checks only existence of methods.\n\n        Args:\n            Union[ModuleType, AbstractHost]: Host module where to look for\n                required methods.\n\n        Returns:\n            list[str]: Missing method implementations for new publisher\n                workflow.\n        \"\"\"\n\n        if isinstance(host, IPublishHost):\n            return []\n\n        required = [\n            \"get_context_data\",\n            \"update_context_data\",\n            \"get_context_title\",\n            \"get_current_context\",\n        ]\n        missing = []\n        for name in required:\n            if not hasattr(host, name):\n                missing.append(name)\n        return missing\n\n    @staticmethod\n    def validate_publish_methods(host):\n        \"\"\"Validate implemented methods of \"old type\" host.\n\n        Args:\n            Union[ModuleType, AbstractHost]: Host module to validate.\n\n        Raises:\n            MissingMethodsError: If there are missing methods on host\n                implementation.\n        \"\"\"\n        missing = IPublishHost.get_missing_publish_methods(host)\n        if missing:\n            raise MissingMethodsError(host, missing)\n\n    @abstractmethod\n    def get_context_data(self):\n        \"\"\"Get global data related to creation-publishing from workfile.\n\n        These data are not related to any created instance but to whole\n        publishing context. Not saving/returning them will cause that each\n        reset of publishing resets all values to default ones.\n\n        Context data can contain information about enabled/disabled publish\n        plugins or other values that can be filled by artist.\n\n        Returns:\n            dict: Context data stored using 'update_context_data'.\n        \"\"\"\n\n        pass\n\n    @abstractmethod\n    def update_context_data(self, data, changes):\n        \"\"\"Store global context data to workfile.\n\n        Called when some values in context data has changed.\n\n        Without storing the values in a way that 'get_context_data' would\n        return them will each reset of publishing cause loose of filled values\n        by artist. Best practice is to store values into workfile, if possible.\n\n        Args:\n            data (dict): New data as are.\n            changes (dict): Only data that has been changed. Each value has\n                tuple with '(&lt;old&gt;, &lt;new&gt;)' value.\n        \"\"\"\n\n        pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/index.html#client.ayon_core.host.interfaces.IPublishHost.get_context_data","title":"<code>get_context_data()</code>  <code>abstractmethod</code>","text":"<p>Get global data related to creation-publishing from workfile.</p> <p>These data are not related to any created instance but to whole publishing context. Not saving/returning them will cause that each reset of publishing resets all values to default ones.</p> <p>Context data can contain information about enabled/disabled publish plugins or other values that can be filled by artist.</p> <p>Returns:</p> Name Type Description <code>dict</code> <p>Context data stored using 'update_context_data'.</p> Source code in <code>client/ayon_core/host/interfaces/interfaces.py</code> <pre><code>@abstractmethod\ndef get_context_data(self):\n    \"\"\"Get global data related to creation-publishing from workfile.\n\n    These data are not related to any created instance but to whole\n    publishing context. Not saving/returning them will cause that each\n    reset of publishing resets all values to default ones.\n\n    Context data can contain information about enabled/disabled publish\n    plugins or other values that can be filled by artist.\n\n    Returns:\n        dict: Context data stored using 'update_context_data'.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/index.html#client.ayon_core.host.interfaces.IPublishHost.get_missing_publish_methods","title":"<code>get_missing_publish_methods(host)</code>  <code>staticmethod</code>","text":"<p>Look for missing methods on \"old type\" host implementation.</p> <p>Method is used for validation of implemented functions related to new publish creation. Checks only existence of methods.</p> <p>Parameters:</p> Name Type Description Default <code>Union[ModuleType, AbstractHost]</code> <p>Host module where to look for required methods.</p> required <p>Returns:</p> Type Description <p>list[str]: Missing method implementations for new publisher workflow.</p> Source code in <code>client/ayon_core/host/interfaces/interfaces.py</code> <pre><code>@staticmethod\ndef get_missing_publish_methods(host):\n    \"\"\"Look for missing methods on \"old type\" host implementation.\n\n    Method is used for validation of implemented functions related to\n    new publish creation. Checks only existence of methods.\n\n    Args:\n        Union[ModuleType, AbstractHost]: Host module where to look for\n            required methods.\n\n    Returns:\n        list[str]: Missing method implementations for new publisher\n            workflow.\n    \"\"\"\n\n    if isinstance(host, IPublishHost):\n        return []\n\n    required = [\n        \"get_context_data\",\n        \"update_context_data\",\n        \"get_context_title\",\n        \"get_current_context\",\n    ]\n    missing = []\n    for name in required:\n        if not hasattr(host, name):\n            missing.append(name)\n    return missing\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/index.html#client.ayon_core.host.interfaces.IPublishHost.update_context_data","title":"<code>update_context_data(data, changes)</code>  <code>abstractmethod</code>","text":"<p>Store global context data to workfile.</p> <p>Called when some values in context data has changed.</p> <p>Without storing the values in a way that 'get_context_data' would return them will each reset of publishing cause loose of filled values by artist. Best practice is to store values into workfile, if possible.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict</code> <p>New data as are.</p> required <code>changes</code> <code>dict</code> <p>Only data that has been changed. Each value has tuple with '(, )' value. required Source code in <code>client/ayon_core/host/interfaces/interfaces.py</code> <pre><code>@abstractmethod\ndef update_context_data(self, data, changes):\n    \"\"\"Store global context data to workfile.\n\n    Called when some values in context data has changed.\n\n    Without storing the values in a way that 'get_context_data' would\n    return them will each reset of publishing cause loose of filled values\n    by artist. Best practice is to store values into workfile, if possible.\n\n    Args:\n        data (dict): New data as are.\n        changes (dict): Only data that has been changed. Each value has\n            tuple with '(&lt;old&gt;, &lt;new&gt;)' value.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/index.html#client.ayon_core.host.interfaces.IPublishHost.validate_publish_methods","title":"<code>validate_publish_methods(host)</code>  <code>staticmethod</code>","text":"<p>Validate implemented methods of \"old type\" host.</p> <p>Parameters:</p> Name Type Description Default <code>Union[ModuleType, AbstractHost]</code> <p>Host module to validate.</p> required <p>Raises:</p> Type Description <code>MissingMethodsError</code> <p>If there are missing methods on host implementation.</p> Source code in <code>client/ayon_core/host/interfaces/interfaces.py</code> <pre><code>@staticmethod\ndef validate_publish_methods(host):\n    \"\"\"Validate implemented methods of \"old type\" host.\n\n    Args:\n        Union[ModuleType, AbstractHost]: Host module to validate.\n\n    Raises:\n        MissingMethodsError: If there are missing methods on host\n            implementation.\n    \"\"\"\n    missing = IPublishHost.get_missing_publish_methods(host)\n    if missing:\n        raise MissingMethodsError(host, missing)\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/index.html#client.ayon_core.host.interfaces.IWorkfileHost","title":"<code>IWorkfileHost</code>","text":"<p>               Bases: <code>AbstractHost</code></p> <p>Implementation requirements to be able to use workfiles utils and tool.</p> <p>Some of the methods are pre-implemented as they generally do the same in     all host integrations.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>class IWorkfileHost(AbstractHost):\n    \"\"\"Implementation requirements to be able to use workfiles utils and tool.\n\n    Some of the methods are pre-implemented as they generally do the same in\n        all host integrations.\n\n    \"\"\"\n    # Can be changed to False if the host integration needs to change context\n    #   after workfile is opened, useful if the integration stores and\n    #   retrieves context to metadata of the file\n    change_context_before_workfile_open = True\n\n    @abstractmethod\n    def save_workfile(self, dst_path: Optional[str] = None) -&gt; None:\n        \"\"\"Save the currently opened scene.\n\n        Args:\n            dst_path (str): Where the current scene should be saved. Or use\n                the current path if 'None' is passed.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def open_workfile(self, filepath: str) -&gt; None:\n        \"\"\"Open passed filepath in the host.\n\n        Args:\n            filepath (str): Path to workfile.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_current_workfile(self) -&gt; Optional[str]:\n        \"\"\"Retrieve a path to current opened file.\n\n        Returns:\n            Optional[str]: Path to the file which is currently opened. None if\n                nothing is opened or the current workfile is unsaved.\n\n        \"\"\"\n        return None\n\n    def workfile_has_unsaved_changes(self) -&gt; Optional[bool]:\n        \"\"\"Currently opened scene is saved.\n\n        Not all hosts can know if the current scene is saved because the API\n            of DCC does not support it.\n\n        Returns:\n            Optional[bool]: True if scene is saved and False if has unsaved\n                modifications. None if can't tell if workfiles has\n                modifications.\n\n        \"\"\"\n        return None\n\n    def get_workfile_extensions(self) -&gt; list[str]:\n        \"\"\"Extensions that can be used to save the workfile to.\n\n        Notes:\n            Method may not be used if 'list_workfiles' and\n                'list_published_workfiles' are re-implemented with different\n                logic.\n\n        Returns:\n            list[str]: List of extensions that can be used for saving.\n\n        \"\"\"\n        return []\n\n    def save_workfile_with_context(\n        self,\n        filepath: str,\n        folder_entity: dict[str, Any],\n        task_entity: dict[str, Any],\n        *,\n        version: Optional[int] = None,\n        comment: Optional[str] = None,\n        description: Optional[str] = None,\n        prepared_data: Optional[SaveWorkfileOptionalData] = None,\n    ) -&gt; None:\n        \"\"\"Save the current workfile with context.\n\n        Arguments 'rootless_path', 'workfile_entities', 'project_entity'\n            and 'anatomy' can be filled to enhance efficiency if you already\n            have access to the values.\n\n        Argument 'project_settings' is used to calculate 'rootless_path'\n            if it is not provided.\n\n        Notes:\n            Should this method care about context change?\n\n        Args:\n            filepath (str): Where the current scene should be saved.\n            folder_entity (dict[str, Any]): Folder entity.\n            task_entity (dict[str, Any]): Task entity.\n            version (Optional[int]): Version of the workfile. Information\n                for workfile entity. Recommended to fill.\n            comment (Optional[str]): Comment for the workfile.\n                Usually used in the filename template.\n            description (Optional[str]): Artist note for the workfile entity.\n            prepared_data (Optional[SaveWorkfileOptionalData]): Prepared data\n                for speed enhancements.\n\n        \"\"\"\n        project_name = self.get_current_project_name()\n        save_workfile_context = get_save_workfile_context(\n            project_name,\n            filepath,\n            folder_entity,\n            task_entity,\n            host_name=self.name,\n            prepared_data=prepared_data,\n        )\n\n        self._before_workfile_save(save_workfile_context)\n        event_data = self._get_workfile_event_data(\n            project_name,\n            folder_entity,\n            task_entity,\n            filepath,\n        )\n        self._emit_workfile_save_event(event_data, after_save=False)\n\n        workdir = os.path.dirname(filepath)\n        if not os.path.exists(workdir):\n            os.makedirs(workdir, exist_ok=True)\n\n        # Set 'AYON_WORKDIR' environment variable\n        os.environ[\"AYON_WORKDIR\"] = workdir\n\n        self.set_current_context(\n            folder_entity,\n            task_entity,\n            reason=ContextChangeReason.workfile_save,\n            project_entity=save_workfile_context.project_entity,\n            anatomy=save_workfile_context.anatomy,\n        )\n\n        self.save_workfile(filepath)\n\n        self._save_workfile_entity(\n            save_workfile_context,\n            version,\n            comment,\n            description,\n        )\n        self._after_workfile_save(save_workfile_context)\n        self._emit_workfile_save_event(event_data)\n\n    def open_workfile_with_context(\n        self,\n        filepath: str,\n        folder_entity: dict[str, Any],\n        task_entity: dict[str, Any],\n        *,\n        prepared_data: Optional[OpenWorkfileOptionalData] = None,\n    ) -&gt; None:\n        \"\"\"Open passed filepath in the host with context.\n\n        This function should be used to open workfile in different context.\n\n        Notes:\n            Should this method care about context change?\n\n        Args:\n            filepath (str): Path to workfile.\n            folder_entity (dict[str, Any]): Folder id.\n            task_entity (dict[str, Any]): Task id.\n            prepared_data (Optional[WorkfileOptionalData]): Prepared data\n                for speed enhancements.\n\n        \"\"\"\n        context = self.get_current_context()\n        project_name = context[\"project_name\"]\n\n        open_workfile_context = get_open_workfile_context(\n            project_name,\n            filepath,\n            folder_entity,\n            task_entity,\n            prepared_data=prepared_data,\n        )\n\n        workdir = os.path.dirname(filepath)\n        # Set 'AYON_WORKDIR' environment variable\n        os.environ[\"AYON_WORKDIR\"] = workdir\n\n        event_data = self._get_workfile_event_data(\n            project_name, folder_entity, task_entity, filepath\n        )\n        self._before_workfile_open(open_workfile_context)\n        self._emit_workfile_open_event(event_data, after_open=False)\n\n        if self.change_context_before_workfile_open:\n            self.set_current_context(\n                folder_entity,\n                task_entity,\n                reason=ContextChangeReason.workfile_open,\n                project_entity=open_workfile_context.project_entity,\n                anatomy=open_workfile_context.anatomy,\n            )\n\n        self.open_workfile(filepath)\n\n        if not self.change_context_before_workfile_open:\n            self.set_current_context(\n                folder_entity,\n                task_entity,\n                reason=ContextChangeReason.workfile_open,\n                project_entity=open_workfile_context.project_entity,\n                anatomy=open_workfile_context.anatomy,\n            )\n\n        self._after_workfile_open(open_workfile_context)\n        self._emit_workfile_open_event(event_data)\n\n    def list_workfiles(\n        self,\n        project_name: str,\n        folder_entity: dict[str, Any],\n        task_entity: dict[str, Any],\n        *,\n        prepared_data: Optional[ListWorkfilesOptionalData] = None,\n    ) -&gt; list[WorkfileInfo]:\n        \"\"\"List workfiles in the given task.\n\n        The method should also return workfiles that are not available on\n            disk, but are in the AYON database.\n\n        Notes:\n        - Better method name?\n        - This method is pre-implemented as the logic can be shared across\n            95% of host integrations. Ad-hoc implementation to give host\n            integration workfile api functionality.\n\n        Args:\n            project_name (str): Project name.\n            folder_entity (dict[str, Any]): Folder entity.\n            task_entity (dict[str, Any]): Task entity.\n            prepared_data (Optional[ListWorkfilesOptionalData]): Prepared\n                data for speed enhancements.\n\n        Returns:\n            list[WorkfileInfo]: List of workfiles.\n\n        \"\"\"\n        from ayon_core.pipeline.template_data import get_template_data\n        from ayon_core.pipeline.workfile.path_resolving import (\n            get_workdir_with_workdir_data,\n            WorkfileDataParser,\n        )\n\n        extensions = self.get_workfile_extensions()\n        if not extensions:\n            return []\n\n        list_workfiles_context = get_list_workfiles_context(\n            project_name,\n            folder_entity,\n            task_entity,\n            host_name=self.name,\n            prepared_data=prepared_data,\n        )\n\n        workfile_entities_by_path = {}\n        for workfile_entity in list_workfiles_context.workfile_entities:\n            rootless_path = workfile_entity[\"path\"]\n            path = os.path.normpath(\n                list_workfiles_context.anatomy.fill_root(rootless_path)\n            )\n            workfile_entities_by_path[path] = workfile_entity\n\n        workdir_data = get_template_data(\n            list_workfiles_context.project_entity,\n            folder_entity,\n            task_entity,\n            host_name=self.name,\n        )\n        workdir = get_workdir_with_workdir_data(\n            workdir_data,\n            project_name,\n            anatomy=list_workfiles_context.anatomy,\n            template_key=list_workfiles_context.template_key,\n            project_settings=list_workfiles_context.project_settings,\n        )\n\n        file_template = list_workfiles_context.anatomy.get_template_item(\n            \"work\", list_workfiles_context.template_key, \"file\"\n        )\n        rootless_workdir = workdir.rootless\n        if platform.system().lower() == \"windows\":\n            rootless_workdir = rootless_workdir.replace(\"\\\\\", \"/\")\n\n        filenames = []\n        if os.path.exists(workdir):\n            filenames = list(os.listdir(workdir))\n\n        data_parser = WorkfileDataParser(file_template, workdir_data)\n        items = []\n        for filename in filenames:\n            # TODO add 'default' support for folders\n            ext = os.path.splitext(filename)[1].lower()\n            if ext not in extensions:\n                continue\n\n            filepath = os.path.join(workdir, filename)\n\n            rootless_path = f\"{rootless_workdir}/{filename}\"\n            workfile_entity = workfile_entities_by_path.pop(\n                filepath, None\n            )\n            version = comment = None\n            if workfile_entity is not None:\n                _data = workfile_entity[\"data\"]\n                version = _data.get(\"version\")\n                comment = _data.get(\"comment\")\n\n            if version is None:\n                parsed_data = data_parser.parse_data(filename)\n                version = parsed_data.version\n                comment = parsed_data.comment\n\n            item = WorkfileInfo.new(\n                filepath,\n                rootless_path,\n                version=version,\n                comment=comment,\n                available=True,\n                workfile_entity=workfile_entity,\n            )\n            items.append(item)\n\n        for filepath, workfile_entity in workfile_entities_by_path.items():\n            # Workfile entity is not in the filesystem\n            #   but it is in the database\n            rootless_path = workfile_entity[\"path\"]\n            ext = os.path.splitext(rootless_path)[1].lower()\n            if ext not in extensions:\n                continue\n\n            _data = workfile_entity[\"data\"]\n            version = _data.get(\"version\")\n            comment = _data.get(\"comment\")\n            if version is None:\n                filename = os.path.basename(rootless_path)\n                parsed_data = data_parser.parse_data(filename)\n                version = parsed_data.version\n                comment = parsed_data.comment\n\n            available = os.path.exists(filepath)\n            items.append(WorkfileInfo.new(\n                filepath,\n                rootless_path,\n                version=version,\n                comment=comment,\n                available=available,\n                workfile_entity=workfile_entity,\n            ))\n\n        return items\n\n    def list_published_workfiles(\n        self,\n        project_name: str,\n        folder_id: str,\n        *,\n        prepared_data: Optional[ListPublishedWorkfilesOptionalData] = None,\n    ) -&gt; list[PublishedWorkfileInfo]:\n        \"\"\"List published workfiles for the given folder.\n\n        The default implementation looks for products with the 'workfile'\n            product type.\n\n        Pre-fetched entities have mandatory fields to be fetched:\n            - Version: 'id', 'author', 'taskId'\n            - Representation: 'id', 'versionId', 'files'\n\n        Args:\n            project_name (str): Project name.\n            folder_id (str): Folder id.\n            prepared_data (Optional[ListPublishedWorkfilesOptionalData]):\n                Prepared data for speed enhancements.\n\n        Returns:\n            list[PublishedWorkfileInfo]: Published workfile information for\n                the given context.\n\n        \"\"\"\n        list_workfiles_context = get_list_published_workfiles_context(\n            project_name,\n            folder_id,\n            prepared_data=prepared_data,\n        )\n        if not list_workfiles_context.repre_entities:\n            return []\n\n        versions_by_id = {\n            version_entity[\"id\"]: version_entity\n            for version_entity in list_workfiles_context.version_entities\n        }\n        extensions = {\n            ext.lstrip(\".\")\n            for ext in self.get_workfile_extensions()\n        }\n        items = []\n        for repre_entity in list_workfiles_context.repre_entities:\n            version_id = repre_entity[\"versionId\"]\n            version_entity = versions_by_id[version_id]\n            task_id = version_entity[\"taskId\"]\n\n            # Filter by extension\n            workfile_path = None\n            for repre_file in repre_entity[\"files\"]:\n                ext = (\n                    os.path.splitext(repre_file[\"name\"])[1]\n                    .lower()\n                    .lstrip(\".\")\n                )\n                if ext in extensions:\n                    workfile_path = repre_file[\"path\"]\n                    break\n\n            if not workfile_path:\n                continue\n\n            try:\n                workfile_path = workfile_path.format(\n                    root=list_workfiles_context.anatomy.roots\n                )\n            except Exception:\n                self.log.warning(\n                    \"Failed to format workfile path.\", exc_info=True\n                )\n\n            is_available = False\n            file_size = file_modified = file_created = None\n            if workfile_path and os.path.exists(workfile_path):\n                filestat = os.stat(workfile_path)\n                is_available = True\n                file_size = filestat.st_size\n                file_created = filestat.st_ctime\n                file_modified = filestat.st_mtime\n\n            workfile_item = PublishedWorkfileInfo.new(\n                project_name,\n                folder_id,\n                task_id,\n                repre_entity,\n                filepath=workfile_path,\n                author=version_entity[\"author\"],\n                available=is_available,\n                file_size=file_size,\n                file_created=file_created,\n                file_modified=file_modified,\n            )\n            items.append(workfile_item)\n\n        return items\n\n    def copy_workfile(\n        self,\n        src_path: str,\n        dst_path: str,\n        folder_entity: dict[str, Any],\n        task_entity: dict[str, Any],\n        *,\n        version: Optional[int] = None,\n        comment: Optional[str] = None,\n        description: Optional[str] = None,\n        open_workfile: bool = True,\n        prepared_data: Optional[CopyWorkfileOptionalData] = None,\n    ) -&gt; None:\n        \"\"\"Save workfile path with target folder and task context.\n\n        It is expected that workfile is saved to the current project, but\n            can be copied from the other project.\n\n        Arguments 'rootless_path', 'workfile_entities', 'project_entity'\n            and 'anatomy' can be filled to enhance efficiency if you already\n            have access to the values.\n\n        Argument 'project_settings' is used to calculate 'rootless_path'\n            if it is not provided.\n\n        Args:\n            src_path (str): Path to the source scene.\n            dst_path (str): Where the scene should be saved.\n            folder_entity (dict[str, Any]): Folder entity.\n            task_entity (dict[str, Any]): Task entity.\n            version (Optional[int]): Version of the workfile. Information\n                for workfile entity. Recommended to fill.\n            comment (Optional[str]): Comment for the workfile.\n            description (Optional[str]): Artist note for the workfile entity.\n            open_workfile (bool): Open workfile when copied.\n            prepared_data (Optional[CopyWorkfileOptionalData]): Prepared data\n                for speed enhancements.\n\n        \"\"\"\n        project_name = self.get_current_project_name()\n        copy_workfile_context: CopyWorkfileContext = get_copy_workfile_context(\n            project_name,\n            src_path,\n            dst_path,\n            folder_entity,\n            task_entity,\n            version=version,\n            comment=comment,\n            description=description,\n            open_workfile=open_workfile,\n            host_name=self.name,\n            prepared_data=prepared_data,\n        )\n        self._copy_workfile(\n            copy_workfile_context,\n            version=version,\n            comment=comment,\n            description=description,\n            open_workfile=open_workfile,\n        )\n\n    def copy_workfile_representation(\n        self,\n        src_project_name: str,\n        src_representation_entity: dict[str, Any],\n        dst_path: str,\n        folder_entity: dict[str, Any],\n        task_entity: dict[str, Any],\n        *,\n        version: Optional[int] = None,\n        comment: Optional[str] = None,\n        description: Optional[str] = None,\n        open_workfile: bool = True,\n        prepared_data: Optional[CopyPublishedWorkfileOptionalData] = None,\n    ) -&gt; None:\n        \"\"\"Copy workfile representation.\n\n        Use representation as a source for the workfile.\n\n        Arguments 'rootless_path', 'workfile_entities', 'project_entity'\n            and 'anatomy' can be filled to enhance efficiency if you already\n            have access to the values.\n\n        Argument 'project_settings' is used to calculate 'rootless_path'\n            if it is not provided.\n\n        Args:\n            src_project_name (str): Project name.\n            src_representation_entity (dict[str, Any]): Representation\n                entity.\n            dst_path (str): Where the scene should be saved.\n            folder_entity (dict[str, Any): Folder entity.\n            task_entity (dict[str, Any]): Task entity.\n            version (Optional[int]): Version of the workfile. Information\n                for workfile entity. Recommended to fill.\n            comment (Optional[str]): Comment for the workfile.\n            description (Optional[str]): Artist note for the workfile entity.\n            open_workfile (bool): Open workfile when copied.\n            prepared_data (Optional[CopyPublishedWorkfileOptionalData]):\n                Prepared data for speed enhancements.\n\n        \"\"\"\n        project_name = self.get_current_project_name()\n        copy_repre_workfile_context: CopyPublishedWorkfileContext = (\n            get_copy_repre_workfile_context(\n                project_name,\n                src_project_name,\n                src_representation_entity,\n                dst_path,\n                folder_entity,\n                task_entity,\n                version=version,\n                comment=comment,\n                description=description,\n                open_workfile=open_workfile,\n                host_name=self.name,\n                prepared_data=prepared_data,\n            )\n        )\n        self._copy_workfile(\n            copy_repre_workfile_context,\n            version=version,\n            comment=comment,\n            description=description,\n            open_workfile=open_workfile,\n        )\n\n    # --- Deprecated method names ---\n    @deprecated(\"Use 'get_workfile_extensions' instead\")\n    def file_extensions(self):\n        \"\"\"Deprecated variant of 'get_workfile_extensions'.\n\n        Todo:\n            Remove when all usages are replaced.\n\n        \"\"\"\n        return self.get_workfile_extensions()\n\n    @deprecated(\"Use 'save_workfile' instead\")\n    def save_file(self, dst_path=None):\n        \"\"\"Deprecated variant of 'save_workfile'.\n\n        Todo:\n            Remove when all usages are replaced\n\n        \"\"\"\n        self.save_workfile(dst_path)\n\n    @deprecated(\"Use 'open_workfile' instead\")\n    def open_file(self, filepath):\n        \"\"\"Deprecated variant of 'open_workfile'.\n\n        Todo:\n            Remove when all usages are replaced.\n\n        \"\"\"\n        return self.open_workfile(filepath)\n\n    @deprecated(\"Use 'get_current_workfile' instead\")\n    def current_file(self):\n        \"\"\"Deprecated variant of 'get_current_workfile'.\n\n        Todo:\n            Remove when all usages are replaced.\n\n        \"\"\"\n        return self.get_current_workfile()\n\n    @deprecated(\"Use 'workfile_has_unsaved_changes' instead\")\n    def has_unsaved_changes(self):\n        \"\"\"Deprecated variant of 'workfile_has_unsaved_changes'.\n\n        Todo:\n            Remove when all usages are replaced.\n\n        \"\"\"\n        return self.workfile_has_unsaved_changes()\n\n    def _copy_workfile(\n        self,\n        copy_workfile_context: CopyWorkfileContext,\n        *,\n        version: Optional[int],\n        comment: Optional[str],\n        description: Optional[str],\n        open_workfile: bool,\n    ) -&gt; None:\n        \"\"\"Save workfile path with target folder and task context.\n\n        It is expected that workfile is saved to the current project, but\n            can be copied from the other project.\n\n        Arguments 'rootless_path', 'workfile_entities', 'project_entity'\n            and 'anatomy' can be filled to enhance efficiency if you already\n            have access to the values.\n\n        Argument 'project_settings' is used to calculate 'rootless_path'\n            if it is not provided.\n\n        Args:\n            copy_workfile_context (CopyWorkfileContext): Prepared data\n                for speed enhancements.\n            version (Optional[int]): Version of the workfile. Information\n                for workfile entity. Recommended to fill.\n            comment (Optional[str]): Comment for the workfile.\n            description (Optional[str]): Artist note for the workfile entity.\n            open_workfile (bool): Open workfile when copied.\n\n        \"\"\"\n        self._before_workfile_copy(copy_workfile_context)\n        event_data = self._get_workfile_event_data(\n            copy_workfile_context.project_name,\n            copy_workfile_context.folder_entity,\n            copy_workfile_context.task_entity,\n            copy_workfile_context.dst_path,\n        )\n        self._emit_workfile_save_event(event_data, after_save=False)\n\n        dst_dir = os.path.dirname(copy_workfile_context.dst_path)\n        if not os.path.exists(dst_dir):\n            os.makedirs(dst_dir, exist_ok=True)\n        shutil.copy(\n            copy_workfile_context.src_path,\n            copy_workfile_context.dst_path\n        )\n\n        self._save_workfile_entity(\n            copy_workfile_context,\n            version,\n            comment,\n            description,\n        )\n        self._after_workfile_copy(copy_workfile_context)\n        self._emit_workfile_save_event(event_data)\n\n        if not open_workfile:\n            return\n\n        self.open_workfile_with_context(\n            copy_workfile_context.dst_path,\n            copy_workfile_context.folder_entity,\n            copy_workfile_context.task_entity,\n        )\n\n    def _save_workfile_entity(\n        self,\n        save_workfile_context: SaveWorkfileContext,\n        version: Optional[int],\n        comment: Optional[str],\n        description: Optional[str],\n    ) -&gt; Optional[dict[str, Any]]:\n        \"\"\"Create of update workfile entity to AYON based on provided data.\n\n        Args:\n            save_workfile_context (SaveWorkfileContext): Save workfile\n                context with all prepared data.\n            version (Optional[int]): Version of the workfile.\n            comment (Optional[str]): Comment for the workfile.\n            description (Optional[str]): Artist note for the workfile entity.\n\n        Returns:\n            Optional[dict[str, Any]]: Workfile entity.\n\n        \"\"\"\n        from ayon_core.pipeline.workfile.utils import (\n            save_workfile_info\n        )\n\n        project_name = self.get_current_project_name()\n        if not description:\n            description = None\n\n        if not comment:\n            comment = None\n\n        rootless_path = save_workfile_context.rootless_path\n        # It is not possible to create workfile infor without rootless path\n        workfile_info = None\n        if not rootless_path:\n            return workfile_info\n\n        if platform.system().lower() == \"windows\":\n            rootless_path = rootless_path.replace(\"\\\\\", \"/\")\n\n        # Get application information\n        app_info = self.get_app_information()\n        data = {}\n        if app_info.app_name:\n            data[\"app_name\"] = app_info.app_name\n        if app_info.app_version:\n            data[\"app_version\"] = app_info.app_version\n\n        # Use app group and app variant from applications addon (if available)\n        app_addon_name = os.environ.get(\"AYON_APP_NAME\")\n        if not app_addon_name:\n            app_addon_name = None\n\n        app_addon_tools_s = os.environ.get(\"AYON_APP_TOOLS\")\n        app_addon_tools = []\n        if app_addon_tools_s:\n            app_addon_tools = app_addon_tools_s.split(\";\")\n\n        data[\"ayon_app_name\"] = app_addon_name\n        data[\"ayon_app_tools\"] = app_addon_tools\n\n        workfile_info = save_workfile_info(\n            project_name,\n            save_workfile_context.task_entity[\"id\"],\n            rootless_path,\n            self.name,\n            version,\n            comment,\n            description,\n            data=data,\n            workfile_entities=save_workfile_context.workfile_entities,\n        )\n        return workfile_info\n\n    def _create_extra_folders(\n        self,\n        folder_entity: dict[str, Any],\n        task_entity: dict[str, Any],\n        workdir: str,\n    ) -&gt; None:\n        \"\"\"Create extra folders in the workdir.\n\n        This method should be called when workfile is saved or copied.\n\n        Args:\n            folder_entity (dict[str, Any]): Folder entity.\n            task_entity (dict[str, Any]): Task entity.\n            workdir (str): Workdir where workfile/s will be stored.\n\n        \"\"\"\n        from ayon_core.pipeline.workfile.path_resolving import (\n            create_workdir_extra_folders\n        )\n\n        project_name = self.get_current_project_name()\n\n        # Create extra folders\n        create_workdir_extra_folders(\n            workdir,\n            self.name,\n            task_entity[\"taskType\"],\n            task_entity[\"name\"],\n            project_name\n        )\n\n    def _get_workfile_event_data(\n        self,\n        project_name: str,\n        folder_entity: dict[str, Any],\n        task_entity: dict[str, Any],\n        filepath: str,\n    ) -&gt; dict[str, Optional[str]]:\n        \"\"\"Prepare workfile event data.\n\n        Args:\n            project_name (str): Name of the project where workfile lives.\n            folder_entity (dict[str, Any]): Folder entity.\n            task_entity (dict[str, Any]): Task entity.\n            filepath (str): Path to the workfile.\n\n        Returns:\n            dict[str, Optional[str]]: Data for workfile event.\n\n        \"\"\"\n        workdir, filename = os.path.split(filepath)\n        return {\n            \"project_name\": project_name,\n            \"folder_id\": folder_entity[\"id\"],\n            \"folder_path\": folder_entity[\"path\"],\n            \"task_id\": task_entity[\"id\"],\n            \"task_name\": task_entity[\"name\"],\n            \"host_name\": self.name,\n            \"filepath\": filepath,\n            \"filename\": filename,\n            \"workdir_path\": workdir,\n        }\n\n    def _before_workfile_open(\n        self, open_workfile_context: OpenWorkfileContext\n    ) -&gt; None:\n        \"\"\"Before workfile is opened.\n\n        This method is called before the workfile is opened in the host.\n\n        Can be overridden to implement host specific logic.\n\n        Args:\n            open_workfile_context (OpenWorkfileContext): Context and path of\n                workfile to open.\n\n        \"\"\"\n        pass\n\n    def _after_workfile_open(\n        self, open_workfile_context: OpenWorkfileContext\n    ) -&gt; None:\n        \"\"\"After workfile is opened.\n\n        This method is called after the workfile is opened in the host.\n\n        Can be overridden to implement host specific logic.\n\n        Args:\n            open_workfile_context (OpenWorkfileContext): Context and path of\n                opened workfile.\n\n        \"\"\"\n        pass\n\n    def _before_workfile_save(\n        self, save_workfile_context: SaveWorkfileContext\n    ) -&gt; None:\n        \"\"\"Before workfile is saved.\n\n        This method is called before the workfile is saved in the host.\n\n        Can be overridden to implement host specific logic.\n\n        Args:\n            save_workfile_context (SaveWorkfileContext): Workfile path with\n                target folder and task context.\n\n        \"\"\"\n        pass\n\n    def _after_workfile_save(\n        self, save_workfile_context: SaveWorkfileContext\n    ) -&gt; None:\n        \"\"\"After workfile is saved.\n\n        This method is called after the workfile is saved in the host.\n\n        Can be overridden to implement host specific logic.\n\n        Args:\n            save_workfile_context (SaveWorkfileContext): Workfile path with\n                target folder and task context.\n\n        \"\"\"\n        workdir = os.path.dirname(save_workfile_context.dst_path)\n        self._create_extra_folders(\n            save_workfile_context.folder_entity,\n            save_workfile_context.task_entity,\n            workdir\n        )\n\n    def _before_workfile_copy(\n        self, copy_workfile_context: CopyWorkfileContext\n    ) -&gt; None:\n        \"\"\"Before workfile is copied.\n\n        This method is called before the workfile is copied by host\n            integration.\n\n        Can be overridden to implement host specific logic.\n\n        Args:\n            copy_workfile_context (CopyWorkfileContext): Source and destination\n                path with context before workfile is copied.\n\n        \"\"\"\n        pass\n\n    def _after_workfile_copy(\n        self, copy_workfile_context: CopyWorkfileContext\n    ) -&gt; None:\n        \"\"\"After workfile is copied.\n\n        This method is called after the workfile is copied by host\n            integration.\n\n        Can be overridden to implement host specific logic.\n\n        Args:\n            copy_workfile_context (CopyWorkfileContext): Source and destination\n                path with context after workfile is copied.\n\n        \"\"\"\n        workdir = os.path.dirname(copy_workfile_context.dst_path)\n        self._create_extra_folders(\n            copy_workfile_context.folder_entity,\n            copy_workfile_context.task_entity,\n            workdir,\n        )\n\n    def _emit_workfile_open_event(\n        self,\n        event_data: dict[str, Optional[str]],\n        after_open: bool = True,\n    ) -&gt; None:\n        \"\"\"Emit workfile save event.\n\n        Emit event before and after workfile is opened.\n\n        This method is not meant to be overridden.\n\n        Other addons can listen to this event and do additional steps.\n\n        Args:\n            event_data (dict[str, Optional[str]]): Prepare event data.\n            after_open (bool): Emit event after workfile is opened.\n\n        \"\"\"\n        topics = []\n        topic_end = \"before\"\n        if after_open:\n            topics.append(\"workfile.opened\")\n            topic_end = \"after\"\n\n        # Keep backwards compatible event topic\n        topics.append(f\"workfile.open.{topic_end}\")\n\n        for topic in topics:\n            emit_event(topic, event_data)\n\n    def _emit_workfile_save_event(\n        self,\n        event_data: dict[str, Optional[str]],\n        after_save: bool = True,\n    ) -&gt; None:\n        \"\"\"Emit workfile save event.\n\n        Emit event before and after workfile is saved or copied.\n\n        This method is not meant to be overridden.\n\n        Other addons can listen to this event and do additional steps.\n\n        Args:\n            event_data (dict[str, Optional[str]]): Prepare event data.\n            after_save (bool): Emit event after workfile is saved.\n\n        \"\"\"\n        topics = []\n        topic_end = \"before\"\n        if after_save:\n            topics.append(\"workfile.saved\")\n            topic_end = \"after\"\n\n        # Keep backwards compatible event topic\n        topics.append(f\"workfile.save.{topic_end}\")\n\n        for topic in topics:\n            emit_event(topic, event_data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/index.html#client.ayon_core.host.interfaces.IWorkfileHost.copy_workfile","title":"<code>copy_workfile(src_path, dst_path, folder_entity, task_entity, *, version=None, comment=None, description=None, open_workfile=True, prepared_data=None)</code>","text":"<p>Save workfile path with target folder and task context.</p> <p>It is expected that workfile is saved to the current project, but     can be copied from the other project.</p> <p>Arguments 'rootless_path', 'workfile_entities', 'project_entity'     and 'anatomy' can be filled to enhance efficiency if you already     have access to the values.</p> <p>Argument 'project_settings' is used to calculate 'rootless_path'     if it is not provided.</p> <p>Parameters:</p> Name Type Description Default <code>src_path</code> <code>str</code> <p>Path to the source scene.</p> required <code>dst_path</code> <code>str</code> <p>Where the scene should be saved.</p> required <code>folder_entity</code> <code>dict[str, Any]</code> <p>Folder entity.</p> required <code>task_entity</code> <code>dict[str, Any]</code> <p>Task entity.</p> required <code>version</code> <code>Optional[int]</code> <p>Version of the workfile. Information for workfile entity. Recommended to fill.</p> <code>None</code> <code>comment</code> <code>Optional[str]</code> <p>Comment for the workfile.</p> <code>None</code> <code>description</code> <code>Optional[str]</code> <p>Artist note for the workfile entity.</p> <code>None</code> <code>open_workfile</code> <code>bool</code> <p>Open workfile when copied.</p> <code>True</code> <code>prepared_data</code> <code>Optional[CopyWorkfileOptionalData]</code> <p>Prepared data for speed enhancements.</p> <code>None</code> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>def copy_workfile(\n    self,\n    src_path: str,\n    dst_path: str,\n    folder_entity: dict[str, Any],\n    task_entity: dict[str, Any],\n    *,\n    version: Optional[int] = None,\n    comment: Optional[str] = None,\n    description: Optional[str] = None,\n    open_workfile: bool = True,\n    prepared_data: Optional[CopyWorkfileOptionalData] = None,\n) -&gt; None:\n    \"\"\"Save workfile path with target folder and task context.\n\n    It is expected that workfile is saved to the current project, but\n        can be copied from the other project.\n\n    Arguments 'rootless_path', 'workfile_entities', 'project_entity'\n        and 'anatomy' can be filled to enhance efficiency if you already\n        have access to the values.\n\n    Argument 'project_settings' is used to calculate 'rootless_path'\n        if it is not provided.\n\n    Args:\n        src_path (str): Path to the source scene.\n        dst_path (str): Where the scene should be saved.\n        folder_entity (dict[str, Any]): Folder entity.\n        task_entity (dict[str, Any]): Task entity.\n        version (Optional[int]): Version of the workfile. Information\n            for workfile entity. Recommended to fill.\n        comment (Optional[str]): Comment for the workfile.\n        description (Optional[str]): Artist note for the workfile entity.\n        open_workfile (bool): Open workfile when copied.\n        prepared_data (Optional[CopyWorkfileOptionalData]): Prepared data\n            for speed enhancements.\n\n    \"\"\"\n    project_name = self.get_current_project_name()\n    copy_workfile_context: CopyWorkfileContext = get_copy_workfile_context(\n        project_name,\n        src_path,\n        dst_path,\n        folder_entity,\n        task_entity,\n        version=version,\n        comment=comment,\n        description=description,\n        open_workfile=open_workfile,\n        host_name=self.name,\n        prepared_data=prepared_data,\n    )\n    self._copy_workfile(\n        copy_workfile_context,\n        version=version,\n        comment=comment,\n        description=description,\n        open_workfile=open_workfile,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/index.html#client.ayon_core.host.interfaces.IWorkfileHost.copy_workfile_representation","title":"<code>copy_workfile_representation(src_project_name, src_representation_entity, dst_path, folder_entity, task_entity, *, version=None, comment=None, description=None, open_workfile=True, prepared_data=None)</code>","text":"<p>Copy workfile representation.</p> <p>Use representation as a source for the workfile.</p> <p>Arguments 'rootless_path', 'workfile_entities', 'project_entity'     and 'anatomy' can be filled to enhance efficiency if you already     have access to the values.</p> <p>Argument 'project_settings' is used to calculate 'rootless_path'     if it is not provided.</p> <p>Parameters:</p> Name Type Description Default <code>src_project_name</code> <code>str</code> <p>Project name.</p> required <code>src_representation_entity</code> <code>dict[str, Any]</code> <p>Representation entity.</p> required <code>dst_path</code> <code>str</code> <p>Where the scene should be saved.</p> required <code>folder_entity</code> <code>dict[str, Any</code> <p>Folder entity.</p> required <code>task_entity</code> <code>dict[str, Any]</code> <p>Task entity.</p> required <code>version</code> <code>Optional[int]</code> <p>Version of the workfile. Information for workfile entity. Recommended to fill.</p> <code>None</code> <code>comment</code> <code>Optional[str]</code> <p>Comment for the workfile.</p> <code>None</code> <code>description</code> <code>Optional[str]</code> <p>Artist note for the workfile entity.</p> <code>None</code> <code>open_workfile</code> <code>bool</code> <p>Open workfile when copied.</p> <code>True</code> <code>prepared_data</code> <code>Optional[CopyPublishedWorkfileOptionalData]</code> <p>Prepared data for speed enhancements.</p> <code>None</code> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>def copy_workfile_representation(\n    self,\n    src_project_name: str,\n    src_representation_entity: dict[str, Any],\n    dst_path: str,\n    folder_entity: dict[str, Any],\n    task_entity: dict[str, Any],\n    *,\n    version: Optional[int] = None,\n    comment: Optional[str] = None,\n    description: Optional[str] = None,\n    open_workfile: bool = True,\n    prepared_data: Optional[CopyPublishedWorkfileOptionalData] = None,\n) -&gt; None:\n    \"\"\"Copy workfile representation.\n\n    Use representation as a source for the workfile.\n\n    Arguments 'rootless_path', 'workfile_entities', 'project_entity'\n        and 'anatomy' can be filled to enhance efficiency if you already\n        have access to the values.\n\n    Argument 'project_settings' is used to calculate 'rootless_path'\n        if it is not provided.\n\n    Args:\n        src_project_name (str): Project name.\n        src_representation_entity (dict[str, Any]): Representation\n            entity.\n        dst_path (str): Where the scene should be saved.\n        folder_entity (dict[str, Any): Folder entity.\n        task_entity (dict[str, Any]): Task entity.\n        version (Optional[int]): Version of the workfile. Information\n            for workfile entity. Recommended to fill.\n        comment (Optional[str]): Comment for the workfile.\n        description (Optional[str]): Artist note for the workfile entity.\n        open_workfile (bool): Open workfile when copied.\n        prepared_data (Optional[CopyPublishedWorkfileOptionalData]):\n            Prepared data for speed enhancements.\n\n    \"\"\"\n    project_name = self.get_current_project_name()\n    copy_repre_workfile_context: CopyPublishedWorkfileContext = (\n        get_copy_repre_workfile_context(\n            project_name,\n            src_project_name,\n            src_representation_entity,\n            dst_path,\n            folder_entity,\n            task_entity,\n            version=version,\n            comment=comment,\n            description=description,\n            open_workfile=open_workfile,\n            host_name=self.name,\n            prepared_data=prepared_data,\n        )\n    )\n    self._copy_workfile(\n        copy_repre_workfile_context,\n        version=version,\n        comment=comment,\n        description=description,\n        open_workfile=open_workfile,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/index.html#client.ayon_core.host.interfaces.IWorkfileHost.current_file","title":"<code>current_file()</code>","text":"<p>Deprecated variant of 'get_current_workfile'.</p> Todo <p>Remove when all usages are replaced.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>@deprecated(\"Use 'get_current_workfile' instead\")\ndef current_file(self):\n    \"\"\"Deprecated variant of 'get_current_workfile'.\n\n    Todo:\n        Remove when all usages are replaced.\n\n    \"\"\"\n    return self.get_current_workfile()\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/index.html#client.ayon_core.host.interfaces.IWorkfileHost.file_extensions","title":"<code>file_extensions()</code>","text":"<p>Deprecated variant of 'get_workfile_extensions'.</p> Todo <p>Remove when all usages are replaced.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>@deprecated(\"Use 'get_workfile_extensions' instead\")\ndef file_extensions(self):\n    \"\"\"Deprecated variant of 'get_workfile_extensions'.\n\n    Todo:\n        Remove when all usages are replaced.\n\n    \"\"\"\n    return self.get_workfile_extensions()\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/index.html#client.ayon_core.host.interfaces.IWorkfileHost.get_current_workfile","title":"<code>get_current_workfile()</code>  <code>abstractmethod</code>","text":"<p>Retrieve a path to current opened file.</p> <p>Returns:</p> Type Description <code>Optional[str]</code> <p>Optional[str]: Path to the file which is currently opened. None if nothing is opened or the current workfile is unsaved.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>@abstractmethod\ndef get_current_workfile(self) -&gt; Optional[str]:\n    \"\"\"Retrieve a path to current opened file.\n\n    Returns:\n        Optional[str]: Path to the file which is currently opened. None if\n            nothing is opened or the current workfile is unsaved.\n\n    \"\"\"\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/index.html#client.ayon_core.host.interfaces.IWorkfileHost.get_workfile_extensions","title":"<code>get_workfile_extensions()</code>","text":"<p>Extensions that can be used to save the workfile to.</p> Notes <p>Method may not be used if 'list_workfiles' and     'list_published_workfiles' are re-implemented with different     logic.</p> <p>Returns:</p> Type Description <code>list[str]</code> <p>list[str]: List of extensions that can be used for saving.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>def get_workfile_extensions(self) -&gt; list[str]:\n    \"\"\"Extensions that can be used to save the workfile to.\n\n    Notes:\n        Method may not be used if 'list_workfiles' and\n            'list_published_workfiles' are re-implemented with different\n            logic.\n\n    Returns:\n        list[str]: List of extensions that can be used for saving.\n\n    \"\"\"\n    return []\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/index.html#client.ayon_core.host.interfaces.IWorkfileHost.has_unsaved_changes","title":"<code>has_unsaved_changes()</code>","text":"<p>Deprecated variant of 'workfile_has_unsaved_changes'.</p> Todo <p>Remove when all usages are replaced.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>@deprecated(\"Use 'workfile_has_unsaved_changes' instead\")\ndef has_unsaved_changes(self):\n    \"\"\"Deprecated variant of 'workfile_has_unsaved_changes'.\n\n    Todo:\n        Remove when all usages are replaced.\n\n    \"\"\"\n    return self.workfile_has_unsaved_changes()\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/index.html#client.ayon_core.host.interfaces.IWorkfileHost.list_published_workfiles","title":"<code>list_published_workfiles(project_name, folder_id, *, prepared_data=None)</code>","text":"<p>List published workfiles for the given folder.</p> <p>The default implementation looks for products with the 'workfile'     product type.</p> Pre-fetched entities have mandatory fields to be fetched <ul> <li>Version: 'id', 'author', 'taskId'</li> <li>Representation: 'id', 'versionId', 'files'</li> </ul> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>folder_id</code> <code>str</code> <p>Folder id.</p> required <code>prepared_data</code> <code>Optional[ListPublishedWorkfilesOptionalData]</code> <p>Prepared data for speed enhancements.</p> <code>None</code> <p>Returns:</p> Type Description <code>list[PublishedWorkfileInfo]</code> <p>list[PublishedWorkfileInfo]: Published workfile information for the given context.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>def list_published_workfiles(\n    self,\n    project_name: str,\n    folder_id: str,\n    *,\n    prepared_data: Optional[ListPublishedWorkfilesOptionalData] = None,\n) -&gt; list[PublishedWorkfileInfo]:\n    \"\"\"List published workfiles for the given folder.\n\n    The default implementation looks for products with the 'workfile'\n        product type.\n\n    Pre-fetched entities have mandatory fields to be fetched:\n        - Version: 'id', 'author', 'taskId'\n        - Representation: 'id', 'versionId', 'files'\n\n    Args:\n        project_name (str): Project name.\n        folder_id (str): Folder id.\n        prepared_data (Optional[ListPublishedWorkfilesOptionalData]):\n            Prepared data for speed enhancements.\n\n    Returns:\n        list[PublishedWorkfileInfo]: Published workfile information for\n            the given context.\n\n    \"\"\"\n    list_workfiles_context = get_list_published_workfiles_context(\n        project_name,\n        folder_id,\n        prepared_data=prepared_data,\n    )\n    if not list_workfiles_context.repre_entities:\n        return []\n\n    versions_by_id = {\n        version_entity[\"id\"]: version_entity\n        for version_entity in list_workfiles_context.version_entities\n    }\n    extensions = {\n        ext.lstrip(\".\")\n        for ext in self.get_workfile_extensions()\n    }\n    items = []\n    for repre_entity in list_workfiles_context.repre_entities:\n        version_id = repre_entity[\"versionId\"]\n        version_entity = versions_by_id[version_id]\n        task_id = version_entity[\"taskId\"]\n\n        # Filter by extension\n        workfile_path = None\n        for repre_file in repre_entity[\"files\"]:\n            ext = (\n                os.path.splitext(repre_file[\"name\"])[1]\n                .lower()\n                .lstrip(\".\")\n            )\n            if ext in extensions:\n                workfile_path = repre_file[\"path\"]\n                break\n\n        if not workfile_path:\n            continue\n\n        try:\n            workfile_path = workfile_path.format(\n                root=list_workfiles_context.anatomy.roots\n            )\n        except Exception:\n            self.log.warning(\n                \"Failed to format workfile path.\", exc_info=True\n            )\n\n        is_available = False\n        file_size = file_modified = file_created = None\n        if workfile_path and os.path.exists(workfile_path):\n            filestat = os.stat(workfile_path)\n            is_available = True\n            file_size = filestat.st_size\n            file_created = filestat.st_ctime\n            file_modified = filestat.st_mtime\n\n        workfile_item = PublishedWorkfileInfo.new(\n            project_name,\n            folder_id,\n            task_id,\n            repre_entity,\n            filepath=workfile_path,\n            author=version_entity[\"author\"],\n            available=is_available,\n            file_size=file_size,\n            file_created=file_created,\n            file_modified=file_modified,\n        )\n        items.append(workfile_item)\n\n    return items\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/index.html#client.ayon_core.host.interfaces.IWorkfileHost.list_workfiles","title":"<code>list_workfiles(project_name, folder_entity, task_entity, *, prepared_data=None)</code>","text":"<p>List workfiles in the given task.</p> <p>The method should also return workfiles that are not available on     disk, but are in the AYON database.</p> <p>Notes: - Better method name? - This method is pre-implemented as the logic can be shared across     95% of host integrations. Ad-hoc implementation to give host     integration workfile api functionality.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>folder_entity</code> <code>dict[str, Any]</code> <p>Folder entity.</p> required <code>task_entity</code> <code>dict[str, Any]</code> <p>Task entity.</p> required <code>prepared_data</code> <code>Optional[ListWorkfilesOptionalData]</code> <p>Prepared data for speed enhancements.</p> <code>None</code> <p>Returns:</p> Type Description <code>list[WorkfileInfo]</code> <p>list[WorkfileInfo]: List of workfiles.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>def list_workfiles(\n    self,\n    project_name: str,\n    folder_entity: dict[str, Any],\n    task_entity: dict[str, Any],\n    *,\n    prepared_data: Optional[ListWorkfilesOptionalData] = None,\n) -&gt; list[WorkfileInfo]:\n    \"\"\"List workfiles in the given task.\n\n    The method should also return workfiles that are not available on\n        disk, but are in the AYON database.\n\n    Notes:\n    - Better method name?\n    - This method is pre-implemented as the logic can be shared across\n        95% of host integrations. Ad-hoc implementation to give host\n        integration workfile api functionality.\n\n    Args:\n        project_name (str): Project name.\n        folder_entity (dict[str, Any]): Folder entity.\n        task_entity (dict[str, Any]): Task entity.\n        prepared_data (Optional[ListWorkfilesOptionalData]): Prepared\n            data for speed enhancements.\n\n    Returns:\n        list[WorkfileInfo]: List of workfiles.\n\n    \"\"\"\n    from ayon_core.pipeline.template_data import get_template_data\n    from ayon_core.pipeline.workfile.path_resolving import (\n        get_workdir_with_workdir_data,\n        WorkfileDataParser,\n    )\n\n    extensions = self.get_workfile_extensions()\n    if not extensions:\n        return []\n\n    list_workfiles_context = get_list_workfiles_context(\n        project_name,\n        folder_entity,\n        task_entity,\n        host_name=self.name,\n        prepared_data=prepared_data,\n    )\n\n    workfile_entities_by_path = {}\n    for workfile_entity in list_workfiles_context.workfile_entities:\n        rootless_path = workfile_entity[\"path\"]\n        path = os.path.normpath(\n            list_workfiles_context.anatomy.fill_root(rootless_path)\n        )\n        workfile_entities_by_path[path] = workfile_entity\n\n    workdir_data = get_template_data(\n        list_workfiles_context.project_entity,\n        folder_entity,\n        task_entity,\n        host_name=self.name,\n    )\n    workdir = get_workdir_with_workdir_data(\n        workdir_data,\n        project_name,\n        anatomy=list_workfiles_context.anatomy,\n        template_key=list_workfiles_context.template_key,\n        project_settings=list_workfiles_context.project_settings,\n    )\n\n    file_template = list_workfiles_context.anatomy.get_template_item(\n        \"work\", list_workfiles_context.template_key, \"file\"\n    )\n    rootless_workdir = workdir.rootless\n    if platform.system().lower() == \"windows\":\n        rootless_workdir = rootless_workdir.replace(\"\\\\\", \"/\")\n\n    filenames = []\n    if os.path.exists(workdir):\n        filenames = list(os.listdir(workdir))\n\n    data_parser = WorkfileDataParser(file_template, workdir_data)\n    items = []\n    for filename in filenames:\n        # TODO add 'default' support for folders\n        ext = os.path.splitext(filename)[1].lower()\n        if ext not in extensions:\n            continue\n\n        filepath = os.path.join(workdir, filename)\n\n        rootless_path = f\"{rootless_workdir}/{filename}\"\n        workfile_entity = workfile_entities_by_path.pop(\n            filepath, None\n        )\n        version = comment = None\n        if workfile_entity is not None:\n            _data = workfile_entity[\"data\"]\n            version = _data.get(\"version\")\n            comment = _data.get(\"comment\")\n\n        if version is None:\n            parsed_data = data_parser.parse_data(filename)\n            version = parsed_data.version\n            comment = parsed_data.comment\n\n        item = WorkfileInfo.new(\n            filepath,\n            rootless_path,\n            version=version,\n            comment=comment,\n            available=True,\n            workfile_entity=workfile_entity,\n        )\n        items.append(item)\n\n    for filepath, workfile_entity in workfile_entities_by_path.items():\n        # Workfile entity is not in the filesystem\n        #   but it is in the database\n        rootless_path = workfile_entity[\"path\"]\n        ext = os.path.splitext(rootless_path)[1].lower()\n        if ext not in extensions:\n            continue\n\n        _data = workfile_entity[\"data\"]\n        version = _data.get(\"version\")\n        comment = _data.get(\"comment\")\n        if version is None:\n            filename = os.path.basename(rootless_path)\n            parsed_data = data_parser.parse_data(filename)\n            version = parsed_data.version\n            comment = parsed_data.comment\n\n        available = os.path.exists(filepath)\n        items.append(WorkfileInfo.new(\n            filepath,\n            rootless_path,\n            version=version,\n            comment=comment,\n            available=available,\n            workfile_entity=workfile_entity,\n        ))\n\n    return items\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/index.html#client.ayon_core.host.interfaces.IWorkfileHost.open_file","title":"<code>open_file(filepath)</code>","text":"<p>Deprecated variant of 'open_workfile'.</p> Todo <p>Remove when all usages are replaced.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>@deprecated(\"Use 'open_workfile' instead\")\ndef open_file(self, filepath):\n    \"\"\"Deprecated variant of 'open_workfile'.\n\n    Todo:\n        Remove when all usages are replaced.\n\n    \"\"\"\n    return self.open_workfile(filepath)\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/index.html#client.ayon_core.host.interfaces.IWorkfileHost.open_workfile","title":"<code>open_workfile(filepath)</code>  <code>abstractmethod</code>","text":"<p>Open passed filepath in the host.</p> <p>Parameters:</p> Name Type Description Default <code>filepath</code> <code>str</code> <p>Path to workfile.</p> required Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>@abstractmethod\ndef open_workfile(self, filepath: str) -&gt; None:\n    \"\"\"Open passed filepath in the host.\n\n    Args:\n        filepath (str): Path to workfile.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/index.html#client.ayon_core.host.interfaces.IWorkfileHost.open_workfile_with_context","title":"<code>open_workfile_with_context(filepath, folder_entity, task_entity, *, prepared_data=None)</code>","text":"<p>Open passed filepath in the host with context.</p> <p>This function should be used to open workfile in different context.</p> Notes <p>Should this method care about context change?</p> <p>Parameters:</p> Name Type Description Default <code>filepath</code> <code>str</code> <p>Path to workfile.</p> required <code>folder_entity</code> <code>dict[str, Any]</code> <p>Folder id.</p> required <code>task_entity</code> <code>dict[str, Any]</code> <p>Task id.</p> required <code>prepared_data</code> <code>Optional[WorkfileOptionalData]</code> <p>Prepared data for speed enhancements.</p> <code>None</code> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>def open_workfile_with_context(\n    self,\n    filepath: str,\n    folder_entity: dict[str, Any],\n    task_entity: dict[str, Any],\n    *,\n    prepared_data: Optional[OpenWorkfileOptionalData] = None,\n) -&gt; None:\n    \"\"\"Open passed filepath in the host with context.\n\n    This function should be used to open workfile in different context.\n\n    Notes:\n        Should this method care about context change?\n\n    Args:\n        filepath (str): Path to workfile.\n        folder_entity (dict[str, Any]): Folder id.\n        task_entity (dict[str, Any]): Task id.\n        prepared_data (Optional[WorkfileOptionalData]): Prepared data\n            for speed enhancements.\n\n    \"\"\"\n    context = self.get_current_context()\n    project_name = context[\"project_name\"]\n\n    open_workfile_context = get_open_workfile_context(\n        project_name,\n        filepath,\n        folder_entity,\n        task_entity,\n        prepared_data=prepared_data,\n    )\n\n    workdir = os.path.dirname(filepath)\n    # Set 'AYON_WORKDIR' environment variable\n    os.environ[\"AYON_WORKDIR\"] = workdir\n\n    event_data = self._get_workfile_event_data(\n        project_name, folder_entity, task_entity, filepath\n    )\n    self._before_workfile_open(open_workfile_context)\n    self._emit_workfile_open_event(event_data, after_open=False)\n\n    if self.change_context_before_workfile_open:\n        self.set_current_context(\n            folder_entity,\n            task_entity,\n            reason=ContextChangeReason.workfile_open,\n            project_entity=open_workfile_context.project_entity,\n            anatomy=open_workfile_context.anatomy,\n        )\n\n    self.open_workfile(filepath)\n\n    if not self.change_context_before_workfile_open:\n        self.set_current_context(\n            folder_entity,\n            task_entity,\n            reason=ContextChangeReason.workfile_open,\n            project_entity=open_workfile_context.project_entity,\n            anatomy=open_workfile_context.anatomy,\n        )\n\n    self._after_workfile_open(open_workfile_context)\n    self._emit_workfile_open_event(event_data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/index.html#client.ayon_core.host.interfaces.IWorkfileHost.save_file","title":"<code>save_file(dst_path=None)</code>","text":"<p>Deprecated variant of 'save_workfile'.</p> Todo <p>Remove when all usages are replaced</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>@deprecated(\"Use 'save_workfile' instead\")\ndef save_file(self, dst_path=None):\n    \"\"\"Deprecated variant of 'save_workfile'.\n\n    Todo:\n        Remove when all usages are replaced\n\n    \"\"\"\n    self.save_workfile(dst_path)\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/index.html#client.ayon_core.host.interfaces.IWorkfileHost.save_workfile","title":"<code>save_workfile(dst_path=None)</code>  <code>abstractmethod</code>","text":"<p>Save the currently opened scene.</p> <p>Parameters:</p> Name Type Description Default <code>dst_path</code> <code>str</code> <p>Where the current scene should be saved. Or use the current path if 'None' is passed.</p> <code>None</code> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>@abstractmethod\ndef save_workfile(self, dst_path: Optional[str] = None) -&gt; None:\n    \"\"\"Save the currently opened scene.\n\n    Args:\n        dst_path (str): Where the current scene should be saved. Or use\n            the current path if 'None' is passed.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/index.html#client.ayon_core.host.interfaces.IWorkfileHost.save_workfile_with_context","title":"<code>save_workfile_with_context(filepath, folder_entity, task_entity, *, version=None, comment=None, description=None, prepared_data=None)</code>","text":"<p>Save the current workfile with context.</p> <p>Arguments 'rootless_path', 'workfile_entities', 'project_entity'     and 'anatomy' can be filled to enhance efficiency if you already     have access to the values.</p> <p>Argument 'project_settings' is used to calculate 'rootless_path'     if it is not provided.</p> Notes <p>Should this method care about context change?</p> <p>Parameters:</p> Name Type Description Default <code>filepath</code> <code>str</code> <p>Where the current scene should be saved.</p> required <code>folder_entity</code> <code>dict[str, Any]</code> <p>Folder entity.</p> required <code>task_entity</code> <code>dict[str, Any]</code> <p>Task entity.</p> required <code>version</code> <code>Optional[int]</code> <p>Version of the workfile. Information for workfile entity. Recommended to fill.</p> <code>None</code> <code>comment</code> <code>Optional[str]</code> <p>Comment for the workfile. Usually used in the filename template.</p> <code>None</code> <code>description</code> <code>Optional[str]</code> <p>Artist note for the workfile entity.</p> <code>None</code> <code>prepared_data</code> <code>Optional[SaveWorkfileOptionalData]</code> <p>Prepared data for speed enhancements.</p> <code>None</code> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>def save_workfile_with_context(\n    self,\n    filepath: str,\n    folder_entity: dict[str, Any],\n    task_entity: dict[str, Any],\n    *,\n    version: Optional[int] = None,\n    comment: Optional[str] = None,\n    description: Optional[str] = None,\n    prepared_data: Optional[SaveWorkfileOptionalData] = None,\n) -&gt; None:\n    \"\"\"Save the current workfile with context.\n\n    Arguments 'rootless_path', 'workfile_entities', 'project_entity'\n        and 'anatomy' can be filled to enhance efficiency if you already\n        have access to the values.\n\n    Argument 'project_settings' is used to calculate 'rootless_path'\n        if it is not provided.\n\n    Notes:\n        Should this method care about context change?\n\n    Args:\n        filepath (str): Where the current scene should be saved.\n        folder_entity (dict[str, Any]): Folder entity.\n        task_entity (dict[str, Any]): Task entity.\n        version (Optional[int]): Version of the workfile. Information\n            for workfile entity. Recommended to fill.\n        comment (Optional[str]): Comment for the workfile.\n            Usually used in the filename template.\n        description (Optional[str]): Artist note for the workfile entity.\n        prepared_data (Optional[SaveWorkfileOptionalData]): Prepared data\n            for speed enhancements.\n\n    \"\"\"\n    project_name = self.get_current_project_name()\n    save_workfile_context = get_save_workfile_context(\n        project_name,\n        filepath,\n        folder_entity,\n        task_entity,\n        host_name=self.name,\n        prepared_data=prepared_data,\n    )\n\n    self._before_workfile_save(save_workfile_context)\n    event_data = self._get_workfile_event_data(\n        project_name,\n        folder_entity,\n        task_entity,\n        filepath,\n    )\n    self._emit_workfile_save_event(event_data, after_save=False)\n\n    workdir = os.path.dirname(filepath)\n    if not os.path.exists(workdir):\n        os.makedirs(workdir, exist_ok=True)\n\n    # Set 'AYON_WORKDIR' environment variable\n    os.environ[\"AYON_WORKDIR\"] = workdir\n\n    self.set_current_context(\n        folder_entity,\n        task_entity,\n        reason=ContextChangeReason.workfile_save,\n        project_entity=save_workfile_context.project_entity,\n        anatomy=save_workfile_context.anatomy,\n    )\n\n    self.save_workfile(filepath)\n\n    self._save_workfile_entity(\n        save_workfile_context,\n        version,\n        comment,\n        description,\n    )\n    self._after_workfile_save(save_workfile_context)\n    self._emit_workfile_save_event(event_data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/index.html#client.ayon_core.host.interfaces.IWorkfileHost.workfile_has_unsaved_changes","title":"<code>workfile_has_unsaved_changes()</code>","text":"<p>Currently opened scene is saved.</p> <p>Not all hosts can know if the current scene is saved because the API     of DCC does not support it.</p> <p>Returns:</p> Type Description <code>Optional[bool]</code> <p>Optional[bool]: True if scene is saved and False if has unsaved modifications. None if can't tell if workfiles has modifications.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>def workfile_has_unsaved_changes(self) -&gt; Optional[bool]:\n    \"\"\"Currently opened scene is saved.\n\n    Not all hosts can know if the current scene is saved because the API\n        of DCC does not support it.\n\n    Returns:\n        Optional[bool]: True if scene is saved and False if has unsaved\n            modifications. None if can't tell if workfiles has\n            modifications.\n\n    \"\"\"\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/index.html#client.ayon_core.host.interfaces.ListPublishedWorkfilesOptionalData","title":"<code>ListPublishedWorkfilesOptionalData</code>","text":"<p>               Bases: <code>_WorkfileOptionalData</code></p> <p>Optional data to list published workfiles.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>class ListPublishedWorkfilesOptionalData(_WorkfileOptionalData):\n    \"\"\"Optional data to list published workfiles.\"\"\"\n    data_version = 1\n\n    def __init__(\n        self,\n        *,\n        project_entity: Optional[dict[str, Any]] = None,\n        anatomy: Optional[\"Anatomy\"] = None,\n        project_settings: Optional[dict[str, Any]] = None,\n        product_entities: Optional[list[dict[str, Any]]] = None,\n        version_entities: Optional[list[dict[str, Any]]] = None,\n        repre_entities: Optional[list[dict[str, Any]]] = None,\n        **kwargs\n    ):\n        super().__init__(\n            project_entity=project_entity,\n            anatomy=anatomy,\n            project_settings=project_settings,\n            **kwargs\n        )\n\n        self.product_entities = product_entities\n        self.version_entities = version_entities\n        self.repre_entities = repre_entities\n\n    def get_entities(\n        self,\n        project_name: str,\n        folder_id: str,\n    ) -&gt; tuple[\n        list[dict[str, Any]],\n        list[dict[str, Any]],\n        list[dict[str, Any]]\n    ]:\n        product_entities = self.product_entities\n        if product_entities is None:\n            # TODO remove when server &gt;= 1.14.0 is required\n            filter_key = \"product_base_types\"\n            if ayon_api.get_server_version_tuple() &lt; (1, 14, 0):\n                filter_key = \"product_types\"\n            filter_kwargs = {filter_key: {\"workfile\"}}\n            product_entities = list(ayon_api.get_products(\n                project_name,\n                folder_ids={folder_id},\n                **filter_kwargs,\n                fields={\"id\", \"name\"},\n            ))\n\n        version_entities = self.version_entities\n        if version_entities is None:\n            product_ids = {p[\"id\"] for p in product_entities}\n            version_entities = list(ayon_api.get_versions(\n                project_name,\n                product_ids=product_ids,\n                fields={\"id\", \"author\", \"taskId\"},\n            ))\n\n        repre_entities = self.repre_entities\n        if repre_entities is None:\n            version_ids = {v[\"id\"] for v in version_entities}\n            repre_entities = list(ayon_api.get_representations(\n                project_name,\n                version_ids=version_ids,\n            ))\n        return product_entities, version_entities, repre_entities\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/index.html#client.ayon_core.host.interfaces.ListWorkfilesOptionalData","title":"<code>ListWorkfilesOptionalData</code>","text":"<p>               Bases: <code>_WorkfileOptionalData</code></p> <p>Optional data to list workfiles.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>class ListWorkfilesOptionalData(_WorkfileOptionalData):\n    \"\"\"Optional data to list workfiles.\"\"\"\n    data_version = 1\n\n    def __init__(\n        self,\n        *,\n        project_entity: Optional[dict[str, Any]] = None,\n        anatomy: Optional[\"Anatomy\"] = None,\n        project_settings: Optional[dict[str, Any]] = None,\n        template_key: Optional[str] = None,\n        workfile_entities: Optional[list[dict[str, Any]]] = None,\n        **kwargs\n    ):\n        super().__init__(\n            project_entity=project_entity,\n            anatomy=anatomy,\n            project_settings=project_settings,\n            **kwargs\n        )\n        self.template_key = template_key\n        self.workfile_entities = workfile_entities\n\n    def get_template_key(\n        self,\n        project_name: str,\n        task_type: str,\n        host_name: str,\n        project_settings: dict[str, Any],\n    ) -&gt; str:\n        from ayon_core.pipeline.workfile import get_workfile_template_key\n\n        if self.template_key is not None:\n            return self.template_key\n\n        return get_workfile_template_key(\n            project_name=project_name,\n            task_type=task_type,\n            host_name=host_name,\n            project_settings=project_settings,\n        )\n\n    def get_workfile_entities(\n        self, project_name: str, task_id: str\n    ) -&gt; list[dict[str, Any]]:\n        \"\"\"Fill workfile entities if not provided.\"\"\"\n        if self.workfile_entities is not None:\n            return self.workfile_entities\n        return list(ayon_api.get_workfiles_info(\n            project_name, task_ids=[task_id]\n        ))\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/index.html#client.ayon_core.host.interfaces.ListWorkfilesOptionalData.get_workfile_entities","title":"<code>get_workfile_entities(project_name, task_id)</code>","text":"<p>Fill workfile entities if not provided.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>def get_workfile_entities(\n    self, project_name: str, task_id: str\n) -&gt; list[dict[str, Any]]:\n    \"\"\"Fill workfile entities if not provided.\"\"\"\n    if self.workfile_entities is not None:\n        return self.workfile_entities\n    return list(ayon_api.get_workfiles_info(\n        project_name, task_ids=[task_id]\n    ))\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/index.html#client.ayon_core.host.interfaces.MissingMethodsError","title":"<code>MissingMethodsError</code>","text":"<p>               Bases: <code>ValueError</code></p> <p>Exception when host miss some required methods for a specific workflow.</p> <p>Parameters:</p> Name Type Description Default <code>host</code> <code>HostBase</code> <p>Host implementation where are missing methods.</p> required <code>missing_methods</code> <code>list[str]</code> <p>List of missing methods.</p> required Source code in <code>client/ayon_core/host/interfaces/exceptions.py</code> <pre><code>class MissingMethodsError(ValueError):\n    \"\"\"Exception when host miss some required methods for a specific workflow.\n\n    Args:\n        host (HostBase): Host implementation where are missing methods.\n        missing_methods (list[str]): List of missing methods.\n    \"\"\"\n\n    def __init__(self, host, missing_methods):\n        joined_missing = \", \".join(\n            ['\"{}\"'.format(item) for item in missing_methods]\n        )\n        super().__init__(\n            f\"Host \\\"{host.name}\\\" miss methods {joined_missing}\"\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/index.html#client.ayon_core.host.interfaces.OpenWorkfileOptionalData","title":"<code>OpenWorkfileOptionalData</code>","text":"<p>               Bases: <code>_WorkfileOptionalData</code></p> <p>Optional data for opening workfile.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>class OpenWorkfileOptionalData(_WorkfileOptionalData):\n    \"\"\"Optional data for opening workfile.\"\"\"\n    data_version = 1\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/index.html#client.ayon_core.host.interfaces.PublishedWorkfileInfo","title":"<code>PublishedWorkfileInfo</code>  <code>dataclass</code>","text":"<p>Information about published workfile.</p> <p>Host can copy and use the workfile using this information object.</p> <p>Attributes:</p> Name Type Description <code>project_name</code> <code>str</code> <p>Name of the project where workfile lives.</p> <code>folder_id</code> <code>str</code> <p>Folder id under which is workfile stored.</p> <code>task_id</code> <code>Optional[str]</code> <p>Task id under which is workfile stored.</p> <code>representation_id</code> <code>str</code> <p>Representation id of the workfile.</p> <code>filepath</code> <code>str</code> <p>Path to the workfile.</p> <code>created_at</code> <code>float</code> <p>Timestamp when the workfile representation was created.</p> <code>author</code> <code>str</code> <p>Author of the workfile representation.</p> <code>available</code> <code>bool</code> <p>True if workfile is available on the machine.</p> <code>file_size</code> <code>Optional[float]</code> <p>Size of the workfile in bytes.</p> <code>file_created</code> <code>Optional[float]</code> <p>Timestamp when the workfile was created on the filesystem.</p> <code>file_modified</code> <code>Optional[float]</code> <p>Timestamp when the workfile was modified on the filesystem.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>@dataclass\nclass PublishedWorkfileInfo:\n    \"\"\"Information about published workfile.\n\n    Host can copy and use the workfile using this information object.\n\n    Attributes:\n        project_name (str): Name of the project where workfile lives.\n        folder_id (str): Folder id under which is workfile stored.\n        task_id (Optional[str]): Task id under which is workfile stored.\n        representation_id (str): Representation id of the workfile.\n        filepath (str): Path to the workfile.\n        created_at (float): Timestamp when the workfile representation\n            was created.\n        author (str): Author of the workfile representation.\n        available (bool): True if workfile is available on the machine.\n        file_size (Optional[float]): Size of the workfile in bytes.\n        file_created (Optional[float]): Timestamp when the workfile was\n            created on the filesystem.\n        file_modified (Optional[float]): Timestamp when the workfile was\n            modified on the filesystem.\n\n    \"\"\"\n    project_name: str\n    folder_id: str\n    task_id: Optional[str]\n    representation_id: str\n    filepath: str\n    created_at: float\n    author: str\n    available: bool\n    file_size: Optional[float]\n    file_created: Optional[float]\n    file_modified: Optional[float]\n\n    @classmethod\n    def new(\n        cls,\n        project_name: str,\n        folder_id: str,\n        task_id: Optional[str],\n        repre_entity: dict[str, Any],\n        *,\n        filepath: str,\n        author: str,\n        available: bool,\n        file_size: Optional[float],\n        file_modified: Optional[float],\n        file_created: Optional[float],\n    ) -&gt; \"PublishedWorkfileInfo\":\n        created_at = arrow.get(repre_entity[\"createdAt\"]).to(\"local\")\n\n        return cls(\n            project_name=project_name,\n            folder_id=folder_id,\n            task_id=task_id,\n            representation_id=repre_entity[\"id\"],\n            filepath=filepath,\n            created_at=created_at.float_timestamp,\n            author=author,\n            available=available,\n            file_size=file_size,\n            file_created=file_created,\n            file_modified=file_modified,\n        )\n\n    def to_data(self) -&gt; dict[str, Any]:\n        \"\"\"Converts file item to data.\n\n        Returns:\n            dict[str, Any]: Workfile item data.\n\n        \"\"\"\n        return asdict(self)\n\n    @classmethod\n    def from_data(cls, data: dict[str, Any]) -&gt; \"PublishedWorkfileInfo\":\n        \"\"\"Converts data to workfile item.\n\n        Args:\n            data (dict[str, Any]): Workfile item data.\n\n        Returns:\n            PublishedWorkfileInfo: File item.\n\n        \"\"\"\n        return PublishedWorkfileInfo(**data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/index.html#client.ayon_core.host.interfaces.PublishedWorkfileInfo.from_data","title":"<code>from_data(data)</code>  <code>classmethod</code>","text":"<p>Converts data to workfile item.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict[str, Any]</code> <p>Workfile item data.</p> required <p>Returns:</p> Name Type Description <code>PublishedWorkfileInfo</code> <code>'PublishedWorkfileInfo'</code> <p>File item.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>@classmethod\ndef from_data(cls, data: dict[str, Any]) -&gt; \"PublishedWorkfileInfo\":\n    \"\"\"Converts data to workfile item.\n\n    Args:\n        data (dict[str, Any]): Workfile item data.\n\n    Returns:\n        PublishedWorkfileInfo: File item.\n\n    \"\"\"\n    return PublishedWorkfileInfo(**data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/index.html#client.ayon_core.host.interfaces.PublishedWorkfileInfo.to_data","title":"<code>to_data()</code>","text":"<p>Converts file item to data.</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: Workfile item data.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>def to_data(self) -&gt; dict[str, Any]:\n    \"\"\"Converts file item to data.\n\n    Returns:\n        dict[str, Any]: Workfile item data.\n\n    \"\"\"\n    return asdict(self)\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/index.html#client.ayon_core.host.interfaces.SaveWorkfileOptionalData","title":"<code>SaveWorkfileOptionalData</code>","text":"<p>               Bases: <code>_WorkfileOptionalData</code></p> <p>Optional data to save workfile.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>class SaveWorkfileOptionalData(_WorkfileOptionalData):\n    \"\"\"Optional data to save workfile.\"\"\"\n    data_version = 1\n\n    def __init__(\n        self,\n        *,\n        project_entity: Optional[dict[str, Any]] = None,\n        anatomy: Optional[\"Anatomy\"] = None,\n        project_settings: Optional[dict[str, Any]] = None,\n        rootless_path: Optional[str] = None,\n        workfile_entities: Optional[list[dict[str, Any]]] = None,\n        **kwargs\n    ):\n        super().__init__(\n            project_entity=project_entity,\n            anatomy=anatomy,\n            project_settings=project_settings,\n            **kwargs\n        )\n\n        self.rootless_path = rootless_path\n        self.workfile_entities = workfile_entities\n\n    def get_workfile_entities(self, project_name: str, task_id: str):\n        \"\"\"Fill workfile entities if not provided.\"\"\"\n        if self.workfile_entities is not None:\n            return self.workfile_entities\n        return list(ayon_api.get_workfiles_info(\n            project_name, task_ids=[task_id]\n        ))\n\n    def get_rootless_path(\n        self,\n        workfile_path: str,\n        project_name: str,\n        folder_entity: dict[str, Any],\n        task_entity: dict[str, Any],\n        host_name: str,\n        project_entity: dict[str, Any],\n        project_settings: dict[str, Any],\n        anatomy: \"Anatomy\",\n    ):\n        from ayon_core.pipeline.workfile.utils import (\n            find_workfile_rootless_path\n        )\n\n        if self.rootless_path is not None:\n            return self.rootless_path\n\n        return find_workfile_rootless_path(\n            workfile_path,\n            project_name,\n            folder_entity,\n            task_entity,\n            host_name,\n            project_entity=project_entity,\n            project_settings=project_settings,\n            anatomy=anatomy,\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/index.html#client.ayon_core.host.interfaces.SaveWorkfileOptionalData.get_workfile_entities","title":"<code>get_workfile_entities(project_name, task_id)</code>","text":"<p>Fill workfile entities if not provided.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>def get_workfile_entities(self, project_name: str, task_id: str):\n    \"\"\"Fill workfile entities if not provided.\"\"\"\n    if self.workfile_entities is not None:\n        return self.workfile_entities\n    return list(ayon_api.get_workfiles_info(\n        project_name, task_ids=[task_id]\n    ))\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/index.html#client.ayon_core.host.interfaces.WorkfileInfo","title":"<code>WorkfileInfo</code>  <code>dataclass</code>","text":"<p>Information about workfile.</p> <p>Host can open, copy and use the workfile using this information object.</p> <p>Attributes:</p> Name Type Description <code>filepath</code> <code>str</code> <p>Path to the workfile.</p> <code>rootless_path</code> <code>str</code> <p>Path to the workfile without the root. And without backslashes on Windows.</p> <code>version</code> <code>Optional[int]</code> <p>Version of the workfile.</p> <code>comment</code> <code>Optional[str]</code> <p>Comment of the workfile.</p> <code>file_size</code> <code>Optional[float]</code> <p>Size of the workfile in bytes.</p> <code>file_created</code> <code>Optional[float]</code> <p>Timestamp when the workfile was created on the filesystem.</p> <code>file_modified</code> <code>Optional[float]</code> <p>Timestamp when the workfile was modified on the filesystem.</p> <code>workfile_entity_id</code> <code>Optional[str]</code> <p>Workfile entity id. If None then the workfile is not in the database.</p> <code>description</code> <code>str</code> <p>Description of the workfile.</p> <code>created_by</code> <code>Optional[str]</code> <p>User id of the user who created the workfile entity.</p> <code>updated_by</code> <code>Optional[str]</code> <p>User id of the user who updated the workfile entity.</p> <code>available</code> <code>bool</code> <p>True if workfile is available on the machine.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>@dataclass\nclass WorkfileInfo:\n    \"\"\"Information about workfile.\n\n    Host can open, copy and use the workfile using this information object.\n\n    Attributes:\n        filepath (str): Path to the workfile.\n        rootless_path (str): Path to the workfile without the root. And without\n            backslashes on Windows.\n        version (Optional[int]): Version of the workfile.\n        comment (Optional[str]): Comment of the workfile.\n        file_size (Optional[float]): Size of the workfile in bytes.\n        file_created (Optional[float]): Timestamp when the workfile was\n            created on the filesystem.\n        file_modified (Optional[float]): Timestamp when the workfile was\n            modified on the filesystem.\n        workfile_entity_id (Optional[str]): Workfile entity id. If None then\n            the workfile is not in the database.\n        description (str): Description of the workfile.\n        created_by (Optional[str]): User id of the user who created the\n            workfile entity.\n        updated_by (Optional[str]): User id of the user who updated the\n            workfile entity.\n        available (bool): True if workfile is available on the machine.\n\n    \"\"\"\n    filepath: str\n    rootless_path: str\n    version: Optional[int]\n    comment: Optional[str]\n    file_size: Optional[float]\n    file_created: Optional[float]\n    file_modified: Optional[float]\n    workfile_entity_id: Optional[str]\n    description: str\n    created_by: Optional[str]\n    updated_by: Optional[str]\n    available: bool\n\n    @classmethod\n    def new(\n        cls,\n        filepath: str,\n        rootless_path: str,\n        *,\n        version: Optional[int],\n        comment: Optional[str],\n        available: bool,\n        workfile_entity: dict[str, Any],\n    ):\n        file_size = file_modified = file_created = None\n        if filepath and os.path.exists(filepath):\n            filestat = os.stat(filepath)\n            file_size = filestat.st_size\n            file_created = filestat.st_ctime\n            file_modified = filestat.st_mtime\n\n        if workfile_entity is None:\n            workfile_entity = {}\n\n        attrib = {}\n        if workfile_entity:\n            attrib = workfile_entity[\"attrib\"]\n\n        return cls(\n            filepath=filepath,\n            rootless_path=rootless_path,\n            version=version,\n            comment=comment,\n            file_size=file_size,\n            file_created=file_created,\n            file_modified=file_modified,\n            workfile_entity_id=workfile_entity.get(\"id\"),\n            description=attrib.get(\"description\") or \"\",\n            created_by=workfile_entity.get(\"createdBy\"),\n            updated_by=workfile_entity.get(\"updatedBy\"),\n            available=available,\n        )\n\n    def to_data(self) -&gt; dict[str, Any]:\n        \"\"\"Converts file item to data.\n\n        Returns:\n            dict[str, Any]: Workfile item data.\n\n        \"\"\"\n        return asdict(self)\n\n    @classmethod\n    def from_data(cls, data: dict[str, Any]) -&gt; WorkfileInfo:\n        \"\"\"Converts data to workfile item.\n\n        Args:\n            data (dict[str, Any]): Workfile item data.\n\n        Returns:\n            WorkfileInfo: File item.\n\n        \"\"\"\n        return WorkfileInfo(**data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/index.html#client.ayon_core.host.interfaces.WorkfileInfo.from_data","title":"<code>from_data(data)</code>  <code>classmethod</code>","text":"<p>Converts data to workfile item.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict[str, Any]</code> <p>Workfile item data.</p> required <p>Returns:</p> Name Type Description <code>WorkfileInfo</code> <code>WorkfileInfo</code> <p>File item.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>@classmethod\ndef from_data(cls, data: dict[str, Any]) -&gt; WorkfileInfo:\n    \"\"\"Converts data to workfile item.\n\n    Args:\n        data (dict[str, Any]): Workfile item data.\n\n    Returns:\n        WorkfileInfo: File item.\n\n    \"\"\"\n    return WorkfileInfo(**data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/index.html#client.ayon_core.host.interfaces.WorkfileInfo.to_data","title":"<code>to_data()</code>","text":"<p>Converts file item to data.</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: Workfile item data.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>def to_data(self) -&gt; dict[str, Any]:\n    \"\"\"Converts file item to data.\n\n    Returns:\n        dict[str, Any]: Workfile item data.\n\n    \"\"\"\n    return asdict(self)\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/exceptions.html","title":"exceptions","text":""},{"location":"autoapi/client/ayon_core/host/interfaces/exceptions.html#client.ayon_core.host.interfaces.exceptions.MissingMethodsError","title":"<code>MissingMethodsError</code>","text":"<p>               Bases: <code>ValueError</code></p> <p>Exception when host miss some required methods for a specific workflow.</p> <p>Parameters:</p> Name Type Description Default <code>host</code> <code>HostBase</code> <p>Host implementation where are missing methods.</p> required <code>missing_methods</code> <code>list[str]</code> <p>List of missing methods.</p> required Source code in <code>client/ayon_core/host/interfaces/exceptions.py</code> <pre><code>class MissingMethodsError(ValueError):\n    \"\"\"Exception when host miss some required methods for a specific workflow.\n\n    Args:\n        host (HostBase): Host implementation where are missing methods.\n        missing_methods (list[str]): List of missing methods.\n    \"\"\"\n\n    def __init__(self, host, missing_methods):\n        joined_missing = \", \".join(\n            ['\"{}\"'.format(item) for item in missing_methods]\n        )\n        super().__init__(\n            f\"Host \\\"{host.name}\\\" miss methods {joined_missing}\"\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/interfaces.html","title":"interfaces","text":""},{"location":"autoapi/client/ayon_core/host/interfaces/interfaces.html#client.ayon_core.host.interfaces.interfaces.ILoadHost","title":"<code>ILoadHost</code>","text":"<p>               Bases: <code>AbstractHost</code></p> <p>Implementation requirements to be able use reference of representations.</p> <p>The load plugins can do referencing even without implementation of methods here, but switch and removement of containers would not be possible.</p> Questions <ul> <li>Is list container dependency of host or load plugins?</li> <li>Should this be directly in HostBase?<ul> <li>how to find out if referencing is available?</li> <li>do we need to know that?</li> </ul> </li> </ul> Source code in <code>client/ayon_core/host/interfaces/interfaces.py</code> <pre><code>class ILoadHost(AbstractHost):\n    \"\"\"Implementation requirements to be able use reference of representations.\n\n    The load plugins can do referencing even without implementation of methods\n    here, but switch and removement of containers would not be possible.\n\n    Questions:\n        - Is list container dependency of host or load plugins?\n        - Should this be directly in HostBase?\n            - how to find out if referencing is available?\n            - do we need to know that?\n    \"\"\"\n\n    @staticmethod\n    def get_missing_load_methods(host):\n        \"\"\"Look for missing methods on \"old type\" host implementation.\n\n        Method is used for validation of implemented functions related to\n        loading. Checks only existence of methods.\n\n        Args:\n            Union[ModuleType, AbstractHost]: Object of host where to look for\n                required methods.\n\n        Returns:\n            list[str]: Missing method implementations for loading workflow.\n        \"\"\"\n\n        if isinstance(host, ILoadHost):\n            return []\n\n        required = [\"ls\"]\n        missing = []\n        for name in required:\n            if not hasattr(host, name):\n                missing.append(name)\n        return missing\n\n    @staticmethod\n    def validate_load_methods(host):\n        \"\"\"Validate implemented methods of \"old type\" host for load workflow.\n\n        Args:\n            Union[ModuleType, AbstractHost]: Object of host to validate.\n\n        Raises:\n            MissingMethodsError: If there are missing methods on host\n                implementation.\n        \"\"\"\n        missing = ILoadHost.get_missing_load_methods(host)\n        if missing:\n            raise MissingMethodsError(host, missing)\n\n    @abstractmethod\n    def get_containers(self):\n        \"\"\"Retrieve referenced containers from scene.\n\n        This can be implemented in hosts where referencing can be used.\n\n        Todo:\n            Rename function to something more self explanatory.\n                Suggestion: 'get_containers'\n\n        Returns:\n            list[dict]: Information about loaded containers.\n        \"\"\"\n\n        pass\n\n    # --- Deprecated method names ---\n    def ls(self):\n        \"\"\"Deprecated variant of 'get_containers'.\n\n        Todo:\n            Remove when all usages are replaced.\n        \"\"\"\n\n        return self.get_containers()\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/interfaces.html#client.ayon_core.host.interfaces.interfaces.ILoadHost.get_containers","title":"<code>get_containers()</code>  <code>abstractmethod</code>","text":"<p>Retrieve referenced containers from scene.</p> <p>This can be implemented in hosts where referencing can be used.</p> Todo <p>Rename function to something more self explanatory.     Suggestion: 'get_containers'</p> <p>Returns:</p> Type Description <p>list[dict]: Information about loaded containers.</p> Source code in <code>client/ayon_core/host/interfaces/interfaces.py</code> <pre><code>@abstractmethod\ndef get_containers(self):\n    \"\"\"Retrieve referenced containers from scene.\n\n    This can be implemented in hosts where referencing can be used.\n\n    Todo:\n        Rename function to something more self explanatory.\n            Suggestion: 'get_containers'\n\n    Returns:\n        list[dict]: Information about loaded containers.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/interfaces.html#client.ayon_core.host.interfaces.interfaces.ILoadHost.get_missing_load_methods","title":"<code>get_missing_load_methods(host)</code>  <code>staticmethod</code>","text":"<p>Look for missing methods on \"old type\" host implementation.</p> <p>Method is used for validation of implemented functions related to loading. Checks only existence of methods.</p> <p>Parameters:</p> Name Type Description Default <code>Union[ModuleType, AbstractHost]</code> <p>Object of host where to look for required methods.</p> required <p>Returns:</p> Type Description <p>list[str]: Missing method implementations for loading workflow.</p> Source code in <code>client/ayon_core/host/interfaces/interfaces.py</code> <pre><code>@staticmethod\ndef get_missing_load_methods(host):\n    \"\"\"Look for missing methods on \"old type\" host implementation.\n\n    Method is used for validation of implemented functions related to\n    loading. Checks only existence of methods.\n\n    Args:\n        Union[ModuleType, AbstractHost]: Object of host where to look for\n            required methods.\n\n    Returns:\n        list[str]: Missing method implementations for loading workflow.\n    \"\"\"\n\n    if isinstance(host, ILoadHost):\n        return []\n\n    required = [\"ls\"]\n    missing = []\n    for name in required:\n        if not hasattr(host, name):\n            missing.append(name)\n    return missing\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/interfaces.html#client.ayon_core.host.interfaces.interfaces.ILoadHost.ls","title":"<code>ls()</code>","text":"<p>Deprecated variant of 'get_containers'.</p> Todo <p>Remove when all usages are replaced.</p> Source code in <code>client/ayon_core/host/interfaces/interfaces.py</code> <pre><code>def ls(self):\n    \"\"\"Deprecated variant of 'get_containers'.\n\n    Todo:\n        Remove when all usages are replaced.\n    \"\"\"\n\n    return self.get_containers()\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/interfaces.html#client.ayon_core.host.interfaces.interfaces.ILoadHost.validate_load_methods","title":"<code>validate_load_methods(host)</code>  <code>staticmethod</code>","text":"<p>Validate implemented methods of \"old type\" host for load workflow.</p> <p>Parameters:</p> Name Type Description Default <code>Union[ModuleType, AbstractHost]</code> <p>Object of host to validate.</p> required <p>Raises:</p> Type Description <code>MissingMethodsError</code> <p>If there are missing methods on host implementation.</p> Source code in <code>client/ayon_core/host/interfaces/interfaces.py</code> <pre><code>@staticmethod\ndef validate_load_methods(host):\n    \"\"\"Validate implemented methods of \"old type\" host for load workflow.\n\n    Args:\n        Union[ModuleType, AbstractHost]: Object of host to validate.\n\n    Raises:\n        MissingMethodsError: If there are missing methods on host\n            implementation.\n    \"\"\"\n    missing = ILoadHost.get_missing_load_methods(host)\n    if missing:\n        raise MissingMethodsError(host, missing)\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/interfaces.html#client.ayon_core.host.interfaces.interfaces.INewPublisher","title":"<code>INewPublisher</code>","text":"<p>               Bases: <code>IPublishHost</code></p> <p>Legacy interface replaced by 'IPublishHost'.</p> Deprecated <p>'INewPublisher' is replaced by 'IPublishHost' please change your imports. There is no \"reasonable\" way hot mark these classes as deprecated to show warning of wrong import. Deprecated since 3.14. will be removed in 3.15.</p> Source code in <code>client/ayon_core/host/interfaces/interfaces.py</code> <pre><code>class INewPublisher(IPublishHost):\n    \"\"\"Legacy interface replaced by 'IPublishHost'.\n\n    Deprecated:\n        'INewPublisher' is replaced by 'IPublishHost' please change your\n        imports.\n        There is no \"reasonable\" way hot mark these classes as deprecated\n        to show warning of wrong import. Deprecated since 3.14.* will be\n        removed in 3.15.*\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/interfaces.html#client.ayon_core.host.interfaces.interfaces.IPublishHost","title":"<code>IPublishHost</code>","text":"<p>               Bases: <code>AbstractHost</code></p> <p>Functions related to new creation system in new publisher.</p> <p>New publisher is not storing information only about each created instance but also some global data. At this moment are data related only to context publish plugins but that can extend in future.</p> Source code in <code>client/ayon_core/host/interfaces/interfaces.py</code> <pre><code>class IPublishHost(AbstractHost):\n    \"\"\"Functions related to new creation system in new publisher.\n\n    New publisher is not storing information only about each created instance\n    but also some global data. At this moment are data related only to context\n    publish plugins but that can extend in future.\n    \"\"\"\n\n    @staticmethod\n    def get_missing_publish_methods(host):\n        \"\"\"Look for missing methods on \"old type\" host implementation.\n\n        Method is used for validation of implemented functions related to\n        new publish creation. Checks only existence of methods.\n\n        Args:\n            Union[ModuleType, AbstractHost]: Host module where to look for\n                required methods.\n\n        Returns:\n            list[str]: Missing method implementations for new publisher\n                workflow.\n        \"\"\"\n\n        if isinstance(host, IPublishHost):\n            return []\n\n        required = [\n            \"get_context_data\",\n            \"update_context_data\",\n            \"get_context_title\",\n            \"get_current_context\",\n        ]\n        missing = []\n        for name in required:\n            if not hasattr(host, name):\n                missing.append(name)\n        return missing\n\n    @staticmethod\n    def validate_publish_methods(host):\n        \"\"\"Validate implemented methods of \"old type\" host.\n\n        Args:\n            Union[ModuleType, AbstractHost]: Host module to validate.\n\n        Raises:\n            MissingMethodsError: If there are missing methods on host\n                implementation.\n        \"\"\"\n        missing = IPublishHost.get_missing_publish_methods(host)\n        if missing:\n            raise MissingMethodsError(host, missing)\n\n    @abstractmethod\n    def get_context_data(self):\n        \"\"\"Get global data related to creation-publishing from workfile.\n\n        These data are not related to any created instance but to whole\n        publishing context. Not saving/returning them will cause that each\n        reset of publishing resets all values to default ones.\n\n        Context data can contain information about enabled/disabled publish\n        plugins or other values that can be filled by artist.\n\n        Returns:\n            dict: Context data stored using 'update_context_data'.\n        \"\"\"\n\n        pass\n\n    @abstractmethod\n    def update_context_data(self, data, changes):\n        \"\"\"Store global context data to workfile.\n\n        Called when some values in context data has changed.\n\n        Without storing the values in a way that 'get_context_data' would\n        return them will each reset of publishing cause loose of filled values\n        by artist. Best practice is to store values into workfile, if possible.\n\n        Args:\n            data (dict): New data as are.\n            changes (dict): Only data that has been changed. Each value has\n                tuple with '(&lt;old&gt;, &lt;new&gt;)' value.\n        \"\"\"\n\n        pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/interfaces.html#client.ayon_core.host.interfaces.interfaces.IPublishHost.get_context_data","title":"<code>get_context_data()</code>  <code>abstractmethod</code>","text":"<p>Get global data related to creation-publishing from workfile.</p> <p>These data are not related to any created instance but to whole publishing context. Not saving/returning them will cause that each reset of publishing resets all values to default ones.</p> <p>Context data can contain information about enabled/disabled publish plugins or other values that can be filled by artist.</p> <p>Returns:</p> Name Type Description <code>dict</code> <p>Context data stored using 'update_context_data'.</p> Source code in <code>client/ayon_core/host/interfaces/interfaces.py</code> <pre><code>@abstractmethod\ndef get_context_data(self):\n    \"\"\"Get global data related to creation-publishing from workfile.\n\n    These data are not related to any created instance but to whole\n    publishing context. Not saving/returning them will cause that each\n    reset of publishing resets all values to default ones.\n\n    Context data can contain information about enabled/disabled publish\n    plugins or other values that can be filled by artist.\n\n    Returns:\n        dict: Context data stored using 'update_context_data'.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/interfaces.html#client.ayon_core.host.interfaces.interfaces.IPublishHost.get_missing_publish_methods","title":"<code>get_missing_publish_methods(host)</code>  <code>staticmethod</code>","text":"<p>Look for missing methods on \"old type\" host implementation.</p> <p>Method is used for validation of implemented functions related to new publish creation. Checks only existence of methods.</p> <p>Parameters:</p> Name Type Description Default <code>Union[ModuleType, AbstractHost]</code> <p>Host module where to look for required methods.</p> required <p>Returns:</p> Type Description <p>list[str]: Missing method implementations for new publisher workflow.</p> Source code in <code>client/ayon_core/host/interfaces/interfaces.py</code> <pre><code>@staticmethod\ndef get_missing_publish_methods(host):\n    \"\"\"Look for missing methods on \"old type\" host implementation.\n\n    Method is used for validation of implemented functions related to\n    new publish creation. Checks only existence of methods.\n\n    Args:\n        Union[ModuleType, AbstractHost]: Host module where to look for\n            required methods.\n\n    Returns:\n        list[str]: Missing method implementations for new publisher\n            workflow.\n    \"\"\"\n\n    if isinstance(host, IPublishHost):\n        return []\n\n    required = [\n        \"get_context_data\",\n        \"update_context_data\",\n        \"get_context_title\",\n        \"get_current_context\",\n    ]\n    missing = []\n    for name in required:\n        if not hasattr(host, name):\n            missing.append(name)\n    return missing\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/interfaces.html#client.ayon_core.host.interfaces.interfaces.IPublishHost.update_context_data","title":"<code>update_context_data(data, changes)</code>  <code>abstractmethod</code>","text":"<p>Store global context data to workfile.</p> <p>Called when some values in context data has changed.</p> <p>Without storing the values in a way that 'get_context_data' would return them will each reset of publishing cause loose of filled values by artist. Best practice is to store values into workfile, if possible.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict</code> <p>New data as are.</p> required <code>changes</code> <code>dict</code> <p>Only data that has been changed. Each value has tuple with '(, )' value. required Source code in <code>client/ayon_core/host/interfaces/interfaces.py</code> <pre><code>@abstractmethod\ndef update_context_data(self, data, changes):\n    \"\"\"Store global context data to workfile.\n\n    Called when some values in context data has changed.\n\n    Without storing the values in a way that 'get_context_data' would\n    return them will each reset of publishing cause loose of filled values\n    by artist. Best practice is to store values into workfile, if possible.\n\n    Args:\n        data (dict): New data as are.\n        changes (dict): Only data that has been changed. Each value has\n            tuple with '(&lt;old&gt;, &lt;new&gt;)' value.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/interfaces.html#client.ayon_core.host.interfaces.interfaces.IPublishHost.validate_publish_methods","title":"<code>validate_publish_methods(host)</code>  <code>staticmethod</code>","text":"<p>Validate implemented methods of \"old type\" host.</p> <p>Parameters:</p> Name Type Description Default <code>Union[ModuleType, AbstractHost]</code> <p>Host module to validate.</p> required <p>Raises:</p> Type Description <code>MissingMethodsError</code> <p>If there are missing methods on host implementation.</p> Source code in <code>client/ayon_core/host/interfaces/interfaces.py</code> <pre><code>@staticmethod\ndef validate_publish_methods(host):\n    \"\"\"Validate implemented methods of \"old type\" host.\n\n    Args:\n        Union[ModuleType, AbstractHost]: Host module to validate.\n\n    Raises:\n        MissingMethodsError: If there are missing methods on host\n            implementation.\n    \"\"\"\n    missing = IPublishHost.get_missing_publish_methods(host)\n    if missing:\n        raise MissingMethodsError(host, missing)\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/workfiles.html","title":"workfiles","text":""},{"location":"autoapi/client/ayon_core/host/interfaces/workfiles.html#client.ayon_core.host.interfaces.workfiles.CopyPublishedWorkfileOptionalData","title":"<code>CopyPublishedWorkfileOptionalData</code>","text":"<p>               Bases: <code>SaveWorkfileOptionalData</code></p> <p>Optional data to copy published workfile.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>class CopyPublishedWorkfileOptionalData(SaveWorkfileOptionalData):\n    \"\"\"Optional data to copy published workfile.\"\"\"\n    data_version = 1\n\n    def __init__(\n        self,\n        project_entity: Optional[dict[str, Any]] = None,\n        anatomy: Optional[\"Anatomy\"] = None,\n        project_settings: Optional[dict[str, Any]] = None,\n        rootless_path: Optional[str] = None,\n        workfile_entities: Optional[list[dict[str, Any]]] = None,\n        src_anatomy: Optional[\"Anatomy\"] = None,\n        src_representation_path: Optional[str] = None,\n        **kwargs\n    ):\n        super().__init__(\n            rootless_path=rootless_path,\n            workfile_entities=workfile_entities,\n            project_entity=project_entity,\n            anatomy=anatomy,\n            project_settings=project_settings,\n            **kwargs\n        )\n        self.src_anatomy = src_anatomy\n        self.src_representation_path = src_representation_path\n\n    def get_source_data(\n        self,\n        current_anatomy: Optional[\"Anatomy\"],\n        project_name: str,\n        representation_entity: dict[str, Any],\n    ) -&gt; tuple[\"Anatomy\", str]:\n        from ayon_core.pipeline import Anatomy\n        from ayon_core.pipeline.load import (\n            get_representation_path_with_anatomy\n        )\n\n        src_anatomy = self.src_anatomy\n\n        if (\n            src_anatomy is None\n            and current_anatomy is not None\n            and current_anatomy.project_name == project_name\n        ):\n            src_anatomy = current_anatomy\n        else:\n            src_anatomy = Anatomy(project_name)\n\n        repre_path = self.src_representation_path\n        if repre_path is None:\n            repre_path = get_representation_path_with_anatomy(\n                representation_entity,\n                src_anatomy,\n            )\n        return src_anatomy, repre_path\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/workfiles.html#client.ayon_core.host.interfaces.workfiles.CopyWorkfileOptionalData","title":"<code>CopyWorkfileOptionalData</code>","text":"<p>               Bases: <code>SaveWorkfileOptionalData</code></p> <p>Optional data to copy workfile.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>class CopyWorkfileOptionalData(SaveWorkfileOptionalData):\n    \"\"\"Optional data to copy workfile.\"\"\"\n    data_version = 1\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/workfiles.html#client.ayon_core.host.interfaces.workfiles.IWorkfileHost","title":"<code>IWorkfileHost</code>","text":"<p>               Bases: <code>AbstractHost</code></p> <p>Implementation requirements to be able to use workfiles utils and tool.</p> <p>Some of the methods are pre-implemented as they generally do the same in     all host integrations.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>class IWorkfileHost(AbstractHost):\n    \"\"\"Implementation requirements to be able to use workfiles utils and tool.\n\n    Some of the methods are pre-implemented as they generally do the same in\n        all host integrations.\n\n    \"\"\"\n    # Can be changed to False if the host integration needs to change context\n    #   after workfile is opened, useful if the integration stores and\n    #   retrieves context to metadata of the file\n    change_context_before_workfile_open = True\n\n    @abstractmethod\n    def save_workfile(self, dst_path: Optional[str] = None) -&gt; None:\n        \"\"\"Save the currently opened scene.\n\n        Args:\n            dst_path (str): Where the current scene should be saved. Or use\n                the current path if 'None' is passed.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def open_workfile(self, filepath: str) -&gt; None:\n        \"\"\"Open passed filepath in the host.\n\n        Args:\n            filepath (str): Path to workfile.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_current_workfile(self) -&gt; Optional[str]:\n        \"\"\"Retrieve a path to current opened file.\n\n        Returns:\n            Optional[str]: Path to the file which is currently opened. None if\n                nothing is opened or the current workfile is unsaved.\n\n        \"\"\"\n        return None\n\n    def workfile_has_unsaved_changes(self) -&gt; Optional[bool]:\n        \"\"\"Currently opened scene is saved.\n\n        Not all hosts can know if the current scene is saved because the API\n            of DCC does not support it.\n\n        Returns:\n            Optional[bool]: True if scene is saved and False if has unsaved\n                modifications. None if can't tell if workfiles has\n                modifications.\n\n        \"\"\"\n        return None\n\n    def get_workfile_extensions(self) -&gt; list[str]:\n        \"\"\"Extensions that can be used to save the workfile to.\n\n        Notes:\n            Method may not be used if 'list_workfiles' and\n                'list_published_workfiles' are re-implemented with different\n                logic.\n\n        Returns:\n            list[str]: List of extensions that can be used for saving.\n\n        \"\"\"\n        return []\n\n    def save_workfile_with_context(\n        self,\n        filepath: str,\n        folder_entity: dict[str, Any],\n        task_entity: dict[str, Any],\n        *,\n        version: Optional[int] = None,\n        comment: Optional[str] = None,\n        description: Optional[str] = None,\n        prepared_data: Optional[SaveWorkfileOptionalData] = None,\n    ) -&gt; None:\n        \"\"\"Save the current workfile with context.\n\n        Arguments 'rootless_path', 'workfile_entities', 'project_entity'\n            and 'anatomy' can be filled to enhance efficiency if you already\n            have access to the values.\n\n        Argument 'project_settings' is used to calculate 'rootless_path'\n            if it is not provided.\n\n        Notes:\n            Should this method care about context change?\n\n        Args:\n            filepath (str): Where the current scene should be saved.\n            folder_entity (dict[str, Any]): Folder entity.\n            task_entity (dict[str, Any]): Task entity.\n            version (Optional[int]): Version of the workfile. Information\n                for workfile entity. Recommended to fill.\n            comment (Optional[str]): Comment for the workfile.\n                Usually used in the filename template.\n            description (Optional[str]): Artist note for the workfile entity.\n            prepared_data (Optional[SaveWorkfileOptionalData]): Prepared data\n                for speed enhancements.\n\n        \"\"\"\n        project_name = self.get_current_project_name()\n        save_workfile_context = get_save_workfile_context(\n            project_name,\n            filepath,\n            folder_entity,\n            task_entity,\n            host_name=self.name,\n            prepared_data=prepared_data,\n        )\n\n        self._before_workfile_save(save_workfile_context)\n        event_data = self._get_workfile_event_data(\n            project_name,\n            folder_entity,\n            task_entity,\n            filepath,\n        )\n        self._emit_workfile_save_event(event_data, after_save=False)\n\n        workdir = os.path.dirname(filepath)\n        if not os.path.exists(workdir):\n            os.makedirs(workdir, exist_ok=True)\n\n        # Set 'AYON_WORKDIR' environment variable\n        os.environ[\"AYON_WORKDIR\"] = workdir\n\n        self.set_current_context(\n            folder_entity,\n            task_entity,\n            reason=ContextChangeReason.workfile_save,\n            project_entity=save_workfile_context.project_entity,\n            anatomy=save_workfile_context.anatomy,\n        )\n\n        self.save_workfile(filepath)\n\n        self._save_workfile_entity(\n            save_workfile_context,\n            version,\n            comment,\n            description,\n        )\n        self._after_workfile_save(save_workfile_context)\n        self._emit_workfile_save_event(event_data)\n\n    def open_workfile_with_context(\n        self,\n        filepath: str,\n        folder_entity: dict[str, Any],\n        task_entity: dict[str, Any],\n        *,\n        prepared_data: Optional[OpenWorkfileOptionalData] = None,\n    ) -&gt; None:\n        \"\"\"Open passed filepath in the host with context.\n\n        This function should be used to open workfile in different context.\n\n        Notes:\n            Should this method care about context change?\n\n        Args:\n            filepath (str): Path to workfile.\n            folder_entity (dict[str, Any]): Folder id.\n            task_entity (dict[str, Any]): Task id.\n            prepared_data (Optional[WorkfileOptionalData]): Prepared data\n                for speed enhancements.\n\n        \"\"\"\n        context = self.get_current_context()\n        project_name = context[\"project_name\"]\n\n        open_workfile_context = get_open_workfile_context(\n            project_name,\n            filepath,\n            folder_entity,\n            task_entity,\n            prepared_data=prepared_data,\n        )\n\n        workdir = os.path.dirname(filepath)\n        # Set 'AYON_WORKDIR' environment variable\n        os.environ[\"AYON_WORKDIR\"] = workdir\n\n        event_data = self._get_workfile_event_data(\n            project_name, folder_entity, task_entity, filepath\n        )\n        self._before_workfile_open(open_workfile_context)\n        self._emit_workfile_open_event(event_data, after_open=False)\n\n        if self.change_context_before_workfile_open:\n            self.set_current_context(\n                folder_entity,\n                task_entity,\n                reason=ContextChangeReason.workfile_open,\n                project_entity=open_workfile_context.project_entity,\n                anatomy=open_workfile_context.anatomy,\n            )\n\n        self.open_workfile(filepath)\n\n        if not self.change_context_before_workfile_open:\n            self.set_current_context(\n                folder_entity,\n                task_entity,\n                reason=ContextChangeReason.workfile_open,\n                project_entity=open_workfile_context.project_entity,\n                anatomy=open_workfile_context.anatomy,\n            )\n\n        self._after_workfile_open(open_workfile_context)\n        self._emit_workfile_open_event(event_data)\n\n    def list_workfiles(\n        self,\n        project_name: str,\n        folder_entity: dict[str, Any],\n        task_entity: dict[str, Any],\n        *,\n        prepared_data: Optional[ListWorkfilesOptionalData] = None,\n    ) -&gt; list[WorkfileInfo]:\n        \"\"\"List workfiles in the given task.\n\n        The method should also return workfiles that are not available on\n            disk, but are in the AYON database.\n\n        Notes:\n        - Better method name?\n        - This method is pre-implemented as the logic can be shared across\n            95% of host integrations. Ad-hoc implementation to give host\n            integration workfile api functionality.\n\n        Args:\n            project_name (str): Project name.\n            folder_entity (dict[str, Any]): Folder entity.\n            task_entity (dict[str, Any]): Task entity.\n            prepared_data (Optional[ListWorkfilesOptionalData]): Prepared\n                data for speed enhancements.\n\n        Returns:\n            list[WorkfileInfo]: List of workfiles.\n\n        \"\"\"\n        from ayon_core.pipeline.template_data import get_template_data\n        from ayon_core.pipeline.workfile.path_resolving import (\n            get_workdir_with_workdir_data,\n            WorkfileDataParser,\n        )\n\n        extensions = self.get_workfile_extensions()\n        if not extensions:\n            return []\n\n        list_workfiles_context = get_list_workfiles_context(\n            project_name,\n            folder_entity,\n            task_entity,\n            host_name=self.name,\n            prepared_data=prepared_data,\n        )\n\n        workfile_entities_by_path = {}\n        for workfile_entity in list_workfiles_context.workfile_entities:\n            rootless_path = workfile_entity[\"path\"]\n            path = os.path.normpath(\n                list_workfiles_context.anatomy.fill_root(rootless_path)\n            )\n            workfile_entities_by_path[path] = workfile_entity\n\n        workdir_data = get_template_data(\n            list_workfiles_context.project_entity,\n            folder_entity,\n            task_entity,\n            host_name=self.name,\n        )\n        workdir = get_workdir_with_workdir_data(\n            workdir_data,\n            project_name,\n            anatomy=list_workfiles_context.anatomy,\n            template_key=list_workfiles_context.template_key,\n            project_settings=list_workfiles_context.project_settings,\n        )\n\n        file_template = list_workfiles_context.anatomy.get_template_item(\n            \"work\", list_workfiles_context.template_key, \"file\"\n        )\n        rootless_workdir = workdir.rootless\n        if platform.system().lower() == \"windows\":\n            rootless_workdir = rootless_workdir.replace(\"\\\\\", \"/\")\n\n        filenames = []\n        if os.path.exists(workdir):\n            filenames = list(os.listdir(workdir))\n\n        data_parser = WorkfileDataParser(file_template, workdir_data)\n        items = []\n        for filename in filenames:\n            # TODO add 'default' support for folders\n            ext = os.path.splitext(filename)[1].lower()\n            if ext not in extensions:\n                continue\n\n            filepath = os.path.join(workdir, filename)\n\n            rootless_path = f\"{rootless_workdir}/{filename}\"\n            workfile_entity = workfile_entities_by_path.pop(\n                filepath, None\n            )\n            version = comment = None\n            if workfile_entity is not None:\n                _data = workfile_entity[\"data\"]\n                version = _data.get(\"version\")\n                comment = _data.get(\"comment\")\n\n            if version is None:\n                parsed_data = data_parser.parse_data(filename)\n                version = parsed_data.version\n                comment = parsed_data.comment\n\n            item = WorkfileInfo.new(\n                filepath,\n                rootless_path,\n                version=version,\n                comment=comment,\n                available=True,\n                workfile_entity=workfile_entity,\n            )\n            items.append(item)\n\n        for filepath, workfile_entity in workfile_entities_by_path.items():\n            # Workfile entity is not in the filesystem\n            #   but it is in the database\n            rootless_path = workfile_entity[\"path\"]\n            ext = os.path.splitext(rootless_path)[1].lower()\n            if ext not in extensions:\n                continue\n\n            _data = workfile_entity[\"data\"]\n            version = _data.get(\"version\")\n            comment = _data.get(\"comment\")\n            if version is None:\n                filename = os.path.basename(rootless_path)\n                parsed_data = data_parser.parse_data(filename)\n                version = parsed_data.version\n                comment = parsed_data.comment\n\n            available = os.path.exists(filepath)\n            items.append(WorkfileInfo.new(\n                filepath,\n                rootless_path,\n                version=version,\n                comment=comment,\n                available=available,\n                workfile_entity=workfile_entity,\n            ))\n\n        return items\n\n    def list_published_workfiles(\n        self,\n        project_name: str,\n        folder_id: str,\n        *,\n        prepared_data: Optional[ListPublishedWorkfilesOptionalData] = None,\n    ) -&gt; list[PublishedWorkfileInfo]:\n        \"\"\"List published workfiles for the given folder.\n\n        The default implementation looks for products with the 'workfile'\n            product type.\n\n        Pre-fetched entities have mandatory fields to be fetched:\n            - Version: 'id', 'author', 'taskId'\n            - Representation: 'id', 'versionId', 'files'\n\n        Args:\n            project_name (str): Project name.\n            folder_id (str): Folder id.\n            prepared_data (Optional[ListPublishedWorkfilesOptionalData]):\n                Prepared data for speed enhancements.\n\n        Returns:\n            list[PublishedWorkfileInfo]: Published workfile information for\n                the given context.\n\n        \"\"\"\n        list_workfiles_context = get_list_published_workfiles_context(\n            project_name,\n            folder_id,\n            prepared_data=prepared_data,\n        )\n        if not list_workfiles_context.repre_entities:\n            return []\n\n        versions_by_id = {\n            version_entity[\"id\"]: version_entity\n            for version_entity in list_workfiles_context.version_entities\n        }\n        extensions = {\n            ext.lstrip(\".\")\n            for ext in self.get_workfile_extensions()\n        }\n        items = []\n        for repre_entity in list_workfiles_context.repre_entities:\n            version_id = repre_entity[\"versionId\"]\n            version_entity = versions_by_id[version_id]\n            task_id = version_entity[\"taskId\"]\n\n            # Filter by extension\n            workfile_path = None\n            for repre_file in repre_entity[\"files\"]:\n                ext = (\n                    os.path.splitext(repre_file[\"name\"])[1]\n                    .lower()\n                    .lstrip(\".\")\n                )\n                if ext in extensions:\n                    workfile_path = repre_file[\"path\"]\n                    break\n\n            if not workfile_path:\n                continue\n\n            try:\n                workfile_path = workfile_path.format(\n                    root=list_workfiles_context.anatomy.roots\n                )\n            except Exception:\n                self.log.warning(\n                    \"Failed to format workfile path.\", exc_info=True\n                )\n\n            is_available = False\n            file_size = file_modified = file_created = None\n            if workfile_path and os.path.exists(workfile_path):\n                filestat = os.stat(workfile_path)\n                is_available = True\n                file_size = filestat.st_size\n                file_created = filestat.st_ctime\n                file_modified = filestat.st_mtime\n\n            workfile_item = PublishedWorkfileInfo.new(\n                project_name,\n                folder_id,\n                task_id,\n                repre_entity,\n                filepath=workfile_path,\n                author=version_entity[\"author\"],\n                available=is_available,\n                file_size=file_size,\n                file_created=file_created,\n                file_modified=file_modified,\n            )\n            items.append(workfile_item)\n\n        return items\n\n    def copy_workfile(\n        self,\n        src_path: str,\n        dst_path: str,\n        folder_entity: dict[str, Any],\n        task_entity: dict[str, Any],\n        *,\n        version: Optional[int] = None,\n        comment: Optional[str] = None,\n        description: Optional[str] = None,\n        open_workfile: bool = True,\n        prepared_data: Optional[CopyWorkfileOptionalData] = None,\n    ) -&gt; None:\n        \"\"\"Save workfile path with target folder and task context.\n\n        It is expected that workfile is saved to the current project, but\n            can be copied from the other project.\n\n        Arguments 'rootless_path', 'workfile_entities', 'project_entity'\n            and 'anatomy' can be filled to enhance efficiency if you already\n            have access to the values.\n\n        Argument 'project_settings' is used to calculate 'rootless_path'\n            if it is not provided.\n\n        Args:\n            src_path (str): Path to the source scene.\n            dst_path (str): Where the scene should be saved.\n            folder_entity (dict[str, Any]): Folder entity.\n            task_entity (dict[str, Any]): Task entity.\n            version (Optional[int]): Version of the workfile. Information\n                for workfile entity. Recommended to fill.\n            comment (Optional[str]): Comment for the workfile.\n            description (Optional[str]): Artist note for the workfile entity.\n            open_workfile (bool): Open workfile when copied.\n            prepared_data (Optional[CopyWorkfileOptionalData]): Prepared data\n                for speed enhancements.\n\n        \"\"\"\n        project_name = self.get_current_project_name()\n        copy_workfile_context: CopyWorkfileContext = get_copy_workfile_context(\n            project_name,\n            src_path,\n            dst_path,\n            folder_entity,\n            task_entity,\n            version=version,\n            comment=comment,\n            description=description,\n            open_workfile=open_workfile,\n            host_name=self.name,\n            prepared_data=prepared_data,\n        )\n        self._copy_workfile(\n            copy_workfile_context,\n            version=version,\n            comment=comment,\n            description=description,\n            open_workfile=open_workfile,\n        )\n\n    def copy_workfile_representation(\n        self,\n        src_project_name: str,\n        src_representation_entity: dict[str, Any],\n        dst_path: str,\n        folder_entity: dict[str, Any],\n        task_entity: dict[str, Any],\n        *,\n        version: Optional[int] = None,\n        comment: Optional[str] = None,\n        description: Optional[str] = None,\n        open_workfile: bool = True,\n        prepared_data: Optional[CopyPublishedWorkfileOptionalData] = None,\n    ) -&gt; None:\n        \"\"\"Copy workfile representation.\n\n        Use representation as a source for the workfile.\n\n        Arguments 'rootless_path', 'workfile_entities', 'project_entity'\n            and 'anatomy' can be filled to enhance efficiency if you already\n            have access to the values.\n\n        Argument 'project_settings' is used to calculate 'rootless_path'\n            if it is not provided.\n\n        Args:\n            src_project_name (str): Project name.\n            src_representation_entity (dict[str, Any]): Representation\n                entity.\n            dst_path (str): Where the scene should be saved.\n            folder_entity (dict[str, Any): Folder entity.\n            task_entity (dict[str, Any]): Task entity.\n            version (Optional[int]): Version of the workfile. Information\n                for workfile entity. Recommended to fill.\n            comment (Optional[str]): Comment for the workfile.\n            description (Optional[str]): Artist note for the workfile entity.\n            open_workfile (bool): Open workfile when copied.\n            prepared_data (Optional[CopyPublishedWorkfileOptionalData]):\n                Prepared data for speed enhancements.\n\n        \"\"\"\n        project_name = self.get_current_project_name()\n        copy_repre_workfile_context: CopyPublishedWorkfileContext = (\n            get_copy_repre_workfile_context(\n                project_name,\n                src_project_name,\n                src_representation_entity,\n                dst_path,\n                folder_entity,\n                task_entity,\n                version=version,\n                comment=comment,\n                description=description,\n                open_workfile=open_workfile,\n                host_name=self.name,\n                prepared_data=prepared_data,\n            )\n        )\n        self._copy_workfile(\n            copy_repre_workfile_context,\n            version=version,\n            comment=comment,\n            description=description,\n            open_workfile=open_workfile,\n        )\n\n    # --- Deprecated method names ---\n    @deprecated(\"Use 'get_workfile_extensions' instead\")\n    def file_extensions(self):\n        \"\"\"Deprecated variant of 'get_workfile_extensions'.\n\n        Todo:\n            Remove when all usages are replaced.\n\n        \"\"\"\n        return self.get_workfile_extensions()\n\n    @deprecated(\"Use 'save_workfile' instead\")\n    def save_file(self, dst_path=None):\n        \"\"\"Deprecated variant of 'save_workfile'.\n\n        Todo:\n            Remove when all usages are replaced\n\n        \"\"\"\n        self.save_workfile(dst_path)\n\n    @deprecated(\"Use 'open_workfile' instead\")\n    def open_file(self, filepath):\n        \"\"\"Deprecated variant of 'open_workfile'.\n\n        Todo:\n            Remove when all usages are replaced.\n\n        \"\"\"\n        return self.open_workfile(filepath)\n\n    @deprecated(\"Use 'get_current_workfile' instead\")\n    def current_file(self):\n        \"\"\"Deprecated variant of 'get_current_workfile'.\n\n        Todo:\n            Remove when all usages are replaced.\n\n        \"\"\"\n        return self.get_current_workfile()\n\n    @deprecated(\"Use 'workfile_has_unsaved_changes' instead\")\n    def has_unsaved_changes(self):\n        \"\"\"Deprecated variant of 'workfile_has_unsaved_changes'.\n\n        Todo:\n            Remove when all usages are replaced.\n\n        \"\"\"\n        return self.workfile_has_unsaved_changes()\n\n    def _copy_workfile(\n        self,\n        copy_workfile_context: CopyWorkfileContext,\n        *,\n        version: Optional[int],\n        comment: Optional[str],\n        description: Optional[str],\n        open_workfile: bool,\n    ) -&gt; None:\n        \"\"\"Save workfile path with target folder and task context.\n\n        It is expected that workfile is saved to the current project, but\n            can be copied from the other project.\n\n        Arguments 'rootless_path', 'workfile_entities', 'project_entity'\n            and 'anatomy' can be filled to enhance efficiency if you already\n            have access to the values.\n\n        Argument 'project_settings' is used to calculate 'rootless_path'\n            if it is not provided.\n\n        Args:\n            copy_workfile_context (CopyWorkfileContext): Prepared data\n                for speed enhancements.\n            version (Optional[int]): Version of the workfile. Information\n                for workfile entity. Recommended to fill.\n            comment (Optional[str]): Comment for the workfile.\n            description (Optional[str]): Artist note for the workfile entity.\n            open_workfile (bool): Open workfile when copied.\n\n        \"\"\"\n        self._before_workfile_copy(copy_workfile_context)\n        event_data = self._get_workfile_event_data(\n            copy_workfile_context.project_name,\n            copy_workfile_context.folder_entity,\n            copy_workfile_context.task_entity,\n            copy_workfile_context.dst_path,\n        )\n        self._emit_workfile_save_event(event_data, after_save=False)\n\n        dst_dir = os.path.dirname(copy_workfile_context.dst_path)\n        if not os.path.exists(dst_dir):\n            os.makedirs(dst_dir, exist_ok=True)\n        shutil.copy(\n            copy_workfile_context.src_path,\n            copy_workfile_context.dst_path\n        )\n\n        self._save_workfile_entity(\n            copy_workfile_context,\n            version,\n            comment,\n            description,\n        )\n        self._after_workfile_copy(copy_workfile_context)\n        self._emit_workfile_save_event(event_data)\n\n        if not open_workfile:\n            return\n\n        self.open_workfile_with_context(\n            copy_workfile_context.dst_path,\n            copy_workfile_context.folder_entity,\n            copy_workfile_context.task_entity,\n        )\n\n    def _save_workfile_entity(\n        self,\n        save_workfile_context: SaveWorkfileContext,\n        version: Optional[int],\n        comment: Optional[str],\n        description: Optional[str],\n    ) -&gt; Optional[dict[str, Any]]:\n        \"\"\"Create of update workfile entity to AYON based on provided data.\n\n        Args:\n            save_workfile_context (SaveWorkfileContext): Save workfile\n                context with all prepared data.\n            version (Optional[int]): Version of the workfile.\n            comment (Optional[str]): Comment for the workfile.\n            description (Optional[str]): Artist note for the workfile entity.\n\n        Returns:\n            Optional[dict[str, Any]]: Workfile entity.\n\n        \"\"\"\n        from ayon_core.pipeline.workfile.utils import (\n            save_workfile_info\n        )\n\n        project_name = self.get_current_project_name()\n        if not description:\n            description = None\n\n        if not comment:\n            comment = None\n\n        rootless_path = save_workfile_context.rootless_path\n        # It is not possible to create workfile infor without rootless path\n        workfile_info = None\n        if not rootless_path:\n            return workfile_info\n\n        if platform.system().lower() == \"windows\":\n            rootless_path = rootless_path.replace(\"\\\\\", \"/\")\n\n        # Get application information\n        app_info = self.get_app_information()\n        data = {}\n        if app_info.app_name:\n            data[\"app_name\"] = app_info.app_name\n        if app_info.app_version:\n            data[\"app_version\"] = app_info.app_version\n\n        # Use app group and app variant from applications addon (if available)\n        app_addon_name = os.environ.get(\"AYON_APP_NAME\")\n        if not app_addon_name:\n            app_addon_name = None\n\n        app_addon_tools_s = os.environ.get(\"AYON_APP_TOOLS\")\n        app_addon_tools = []\n        if app_addon_tools_s:\n            app_addon_tools = app_addon_tools_s.split(\";\")\n\n        data[\"ayon_app_name\"] = app_addon_name\n        data[\"ayon_app_tools\"] = app_addon_tools\n\n        workfile_info = save_workfile_info(\n            project_name,\n            save_workfile_context.task_entity[\"id\"],\n            rootless_path,\n            self.name,\n            version,\n            comment,\n            description,\n            data=data,\n            workfile_entities=save_workfile_context.workfile_entities,\n        )\n        return workfile_info\n\n    def _create_extra_folders(\n        self,\n        folder_entity: dict[str, Any],\n        task_entity: dict[str, Any],\n        workdir: str,\n    ) -&gt; None:\n        \"\"\"Create extra folders in the workdir.\n\n        This method should be called when workfile is saved or copied.\n\n        Args:\n            folder_entity (dict[str, Any]): Folder entity.\n            task_entity (dict[str, Any]): Task entity.\n            workdir (str): Workdir where workfile/s will be stored.\n\n        \"\"\"\n        from ayon_core.pipeline.workfile.path_resolving import (\n            create_workdir_extra_folders\n        )\n\n        project_name = self.get_current_project_name()\n\n        # Create extra folders\n        create_workdir_extra_folders(\n            workdir,\n            self.name,\n            task_entity[\"taskType\"],\n            task_entity[\"name\"],\n            project_name\n        )\n\n    def _get_workfile_event_data(\n        self,\n        project_name: str,\n        folder_entity: dict[str, Any],\n        task_entity: dict[str, Any],\n        filepath: str,\n    ) -&gt; dict[str, Optional[str]]:\n        \"\"\"Prepare workfile event data.\n\n        Args:\n            project_name (str): Name of the project where workfile lives.\n            folder_entity (dict[str, Any]): Folder entity.\n            task_entity (dict[str, Any]): Task entity.\n            filepath (str): Path to the workfile.\n\n        Returns:\n            dict[str, Optional[str]]: Data for workfile event.\n\n        \"\"\"\n        workdir, filename = os.path.split(filepath)\n        return {\n            \"project_name\": project_name,\n            \"folder_id\": folder_entity[\"id\"],\n            \"folder_path\": folder_entity[\"path\"],\n            \"task_id\": task_entity[\"id\"],\n            \"task_name\": task_entity[\"name\"],\n            \"host_name\": self.name,\n            \"filepath\": filepath,\n            \"filename\": filename,\n            \"workdir_path\": workdir,\n        }\n\n    def _before_workfile_open(\n        self, open_workfile_context: OpenWorkfileContext\n    ) -&gt; None:\n        \"\"\"Before workfile is opened.\n\n        This method is called before the workfile is opened in the host.\n\n        Can be overridden to implement host specific logic.\n\n        Args:\n            open_workfile_context (OpenWorkfileContext): Context and path of\n                workfile to open.\n\n        \"\"\"\n        pass\n\n    def _after_workfile_open(\n        self, open_workfile_context: OpenWorkfileContext\n    ) -&gt; None:\n        \"\"\"After workfile is opened.\n\n        This method is called after the workfile is opened in the host.\n\n        Can be overridden to implement host specific logic.\n\n        Args:\n            open_workfile_context (OpenWorkfileContext): Context and path of\n                opened workfile.\n\n        \"\"\"\n        pass\n\n    def _before_workfile_save(\n        self, save_workfile_context: SaveWorkfileContext\n    ) -&gt; None:\n        \"\"\"Before workfile is saved.\n\n        This method is called before the workfile is saved in the host.\n\n        Can be overridden to implement host specific logic.\n\n        Args:\n            save_workfile_context (SaveWorkfileContext): Workfile path with\n                target folder and task context.\n\n        \"\"\"\n        pass\n\n    def _after_workfile_save(\n        self, save_workfile_context: SaveWorkfileContext\n    ) -&gt; None:\n        \"\"\"After workfile is saved.\n\n        This method is called after the workfile is saved in the host.\n\n        Can be overridden to implement host specific logic.\n\n        Args:\n            save_workfile_context (SaveWorkfileContext): Workfile path with\n                target folder and task context.\n\n        \"\"\"\n        workdir = os.path.dirname(save_workfile_context.dst_path)\n        self._create_extra_folders(\n            save_workfile_context.folder_entity,\n            save_workfile_context.task_entity,\n            workdir\n        )\n\n    def _before_workfile_copy(\n        self, copy_workfile_context: CopyWorkfileContext\n    ) -&gt; None:\n        \"\"\"Before workfile is copied.\n\n        This method is called before the workfile is copied by host\n            integration.\n\n        Can be overridden to implement host specific logic.\n\n        Args:\n            copy_workfile_context (CopyWorkfileContext): Source and destination\n                path with context before workfile is copied.\n\n        \"\"\"\n        pass\n\n    def _after_workfile_copy(\n        self, copy_workfile_context: CopyWorkfileContext\n    ) -&gt; None:\n        \"\"\"After workfile is copied.\n\n        This method is called after the workfile is copied by host\n            integration.\n\n        Can be overridden to implement host specific logic.\n\n        Args:\n            copy_workfile_context (CopyWorkfileContext): Source and destination\n                path with context after workfile is copied.\n\n        \"\"\"\n        workdir = os.path.dirname(copy_workfile_context.dst_path)\n        self._create_extra_folders(\n            copy_workfile_context.folder_entity,\n            copy_workfile_context.task_entity,\n            workdir,\n        )\n\n    def _emit_workfile_open_event(\n        self,\n        event_data: dict[str, Optional[str]],\n        after_open: bool = True,\n    ) -&gt; None:\n        \"\"\"Emit workfile save event.\n\n        Emit event before and after workfile is opened.\n\n        This method is not meant to be overridden.\n\n        Other addons can listen to this event and do additional steps.\n\n        Args:\n            event_data (dict[str, Optional[str]]): Prepare event data.\n            after_open (bool): Emit event after workfile is opened.\n\n        \"\"\"\n        topics = []\n        topic_end = \"before\"\n        if after_open:\n            topics.append(\"workfile.opened\")\n            topic_end = \"after\"\n\n        # Keep backwards compatible event topic\n        topics.append(f\"workfile.open.{topic_end}\")\n\n        for topic in topics:\n            emit_event(topic, event_data)\n\n    def _emit_workfile_save_event(\n        self,\n        event_data: dict[str, Optional[str]],\n        after_save: bool = True,\n    ) -&gt; None:\n        \"\"\"Emit workfile save event.\n\n        Emit event before and after workfile is saved or copied.\n\n        This method is not meant to be overridden.\n\n        Other addons can listen to this event and do additional steps.\n\n        Args:\n            event_data (dict[str, Optional[str]]): Prepare event data.\n            after_save (bool): Emit event after workfile is saved.\n\n        \"\"\"\n        topics = []\n        topic_end = \"before\"\n        if after_save:\n            topics.append(\"workfile.saved\")\n            topic_end = \"after\"\n\n        # Keep backwards compatible event topic\n        topics.append(f\"workfile.save.{topic_end}\")\n\n        for topic in topics:\n            emit_event(topic, event_data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/workfiles.html#client.ayon_core.host.interfaces.workfiles.IWorkfileHost.copy_workfile","title":"<code>copy_workfile(src_path, dst_path, folder_entity, task_entity, *, version=None, comment=None, description=None, open_workfile=True, prepared_data=None)</code>","text":"<p>Save workfile path with target folder and task context.</p> <p>It is expected that workfile is saved to the current project, but     can be copied from the other project.</p> <p>Arguments 'rootless_path', 'workfile_entities', 'project_entity'     and 'anatomy' can be filled to enhance efficiency if you already     have access to the values.</p> <p>Argument 'project_settings' is used to calculate 'rootless_path'     if it is not provided.</p> <p>Parameters:</p> Name Type Description Default <code>src_path</code> <code>str</code> <p>Path to the source scene.</p> required <code>dst_path</code> <code>str</code> <p>Where the scene should be saved.</p> required <code>folder_entity</code> <code>dict[str, Any]</code> <p>Folder entity.</p> required <code>task_entity</code> <code>dict[str, Any]</code> <p>Task entity.</p> required <code>version</code> <code>Optional[int]</code> <p>Version of the workfile. Information for workfile entity. Recommended to fill.</p> <code>None</code> <code>comment</code> <code>Optional[str]</code> <p>Comment for the workfile.</p> <code>None</code> <code>description</code> <code>Optional[str]</code> <p>Artist note for the workfile entity.</p> <code>None</code> <code>open_workfile</code> <code>bool</code> <p>Open workfile when copied.</p> <code>True</code> <code>prepared_data</code> <code>Optional[CopyWorkfileOptionalData]</code> <p>Prepared data for speed enhancements.</p> <code>None</code> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>def copy_workfile(\n    self,\n    src_path: str,\n    dst_path: str,\n    folder_entity: dict[str, Any],\n    task_entity: dict[str, Any],\n    *,\n    version: Optional[int] = None,\n    comment: Optional[str] = None,\n    description: Optional[str] = None,\n    open_workfile: bool = True,\n    prepared_data: Optional[CopyWorkfileOptionalData] = None,\n) -&gt; None:\n    \"\"\"Save workfile path with target folder and task context.\n\n    It is expected that workfile is saved to the current project, but\n        can be copied from the other project.\n\n    Arguments 'rootless_path', 'workfile_entities', 'project_entity'\n        and 'anatomy' can be filled to enhance efficiency if you already\n        have access to the values.\n\n    Argument 'project_settings' is used to calculate 'rootless_path'\n        if it is not provided.\n\n    Args:\n        src_path (str): Path to the source scene.\n        dst_path (str): Where the scene should be saved.\n        folder_entity (dict[str, Any]): Folder entity.\n        task_entity (dict[str, Any]): Task entity.\n        version (Optional[int]): Version of the workfile. Information\n            for workfile entity. Recommended to fill.\n        comment (Optional[str]): Comment for the workfile.\n        description (Optional[str]): Artist note for the workfile entity.\n        open_workfile (bool): Open workfile when copied.\n        prepared_data (Optional[CopyWorkfileOptionalData]): Prepared data\n            for speed enhancements.\n\n    \"\"\"\n    project_name = self.get_current_project_name()\n    copy_workfile_context: CopyWorkfileContext = get_copy_workfile_context(\n        project_name,\n        src_path,\n        dst_path,\n        folder_entity,\n        task_entity,\n        version=version,\n        comment=comment,\n        description=description,\n        open_workfile=open_workfile,\n        host_name=self.name,\n        prepared_data=prepared_data,\n    )\n    self._copy_workfile(\n        copy_workfile_context,\n        version=version,\n        comment=comment,\n        description=description,\n        open_workfile=open_workfile,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/workfiles.html#client.ayon_core.host.interfaces.workfiles.IWorkfileHost.copy_workfile_representation","title":"<code>copy_workfile_representation(src_project_name, src_representation_entity, dst_path, folder_entity, task_entity, *, version=None, comment=None, description=None, open_workfile=True, prepared_data=None)</code>","text":"<p>Copy workfile representation.</p> <p>Use representation as a source for the workfile.</p> <p>Arguments 'rootless_path', 'workfile_entities', 'project_entity'     and 'anatomy' can be filled to enhance efficiency if you already     have access to the values.</p> <p>Argument 'project_settings' is used to calculate 'rootless_path'     if it is not provided.</p> <p>Parameters:</p> Name Type Description Default <code>src_project_name</code> <code>str</code> <p>Project name.</p> required <code>src_representation_entity</code> <code>dict[str, Any]</code> <p>Representation entity.</p> required <code>dst_path</code> <code>str</code> <p>Where the scene should be saved.</p> required <code>folder_entity</code> <code>dict[str, Any</code> <p>Folder entity.</p> required <code>task_entity</code> <code>dict[str, Any]</code> <p>Task entity.</p> required <code>version</code> <code>Optional[int]</code> <p>Version of the workfile. Information for workfile entity. Recommended to fill.</p> <code>None</code> <code>comment</code> <code>Optional[str]</code> <p>Comment for the workfile.</p> <code>None</code> <code>description</code> <code>Optional[str]</code> <p>Artist note for the workfile entity.</p> <code>None</code> <code>open_workfile</code> <code>bool</code> <p>Open workfile when copied.</p> <code>True</code> <code>prepared_data</code> <code>Optional[CopyPublishedWorkfileOptionalData]</code> <p>Prepared data for speed enhancements.</p> <code>None</code> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>def copy_workfile_representation(\n    self,\n    src_project_name: str,\n    src_representation_entity: dict[str, Any],\n    dst_path: str,\n    folder_entity: dict[str, Any],\n    task_entity: dict[str, Any],\n    *,\n    version: Optional[int] = None,\n    comment: Optional[str] = None,\n    description: Optional[str] = None,\n    open_workfile: bool = True,\n    prepared_data: Optional[CopyPublishedWorkfileOptionalData] = None,\n) -&gt; None:\n    \"\"\"Copy workfile representation.\n\n    Use representation as a source for the workfile.\n\n    Arguments 'rootless_path', 'workfile_entities', 'project_entity'\n        and 'anatomy' can be filled to enhance efficiency if you already\n        have access to the values.\n\n    Argument 'project_settings' is used to calculate 'rootless_path'\n        if it is not provided.\n\n    Args:\n        src_project_name (str): Project name.\n        src_representation_entity (dict[str, Any]): Representation\n            entity.\n        dst_path (str): Where the scene should be saved.\n        folder_entity (dict[str, Any): Folder entity.\n        task_entity (dict[str, Any]): Task entity.\n        version (Optional[int]): Version of the workfile. Information\n            for workfile entity. Recommended to fill.\n        comment (Optional[str]): Comment for the workfile.\n        description (Optional[str]): Artist note for the workfile entity.\n        open_workfile (bool): Open workfile when copied.\n        prepared_data (Optional[CopyPublishedWorkfileOptionalData]):\n            Prepared data for speed enhancements.\n\n    \"\"\"\n    project_name = self.get_current_project_name()\n    copy_repre_workfile_context: CopyPublishedWorkfileContext = (\n        get_copy_repre_workfile_context(\n            project_name,\n            src_project_name,\n            src_representation_entity,\n            dst_path,\n            folder_entity,\n            task_entity,\n            version=version,\n            comment=comment,\n            description=description,\n            open_workfile=open_workfile,\n            host_name=self.name,\n            prepared_data=prepared_data,\n        )\n    )\n    self._copy_workfile(\n        copy_repre_workfile_context,\n        version=version,\n        comment=comment,\n        description=description,\n        open_workfile=open_workfile,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/workfiles.html#client.ayon_core.host.interfaces.workfiles.IWorkfileHost.current_file","title":"<code>current_file()</code>","text":"<p>Deprecated variant of 'get_current_workfile'.</p> Todo <p>Remove when all usages are replaced.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>@deprecated(\"Use 'get_current_workfile' instead\")\ndef current_file(self):\n    \"\"\"Deprecated variant of 'get_current_workfile'.\n\n    Todo:\n        Remove when all usages are replaced.\n\n    \"\"\"\n    return self.get_current_workfile()\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/workfiles.html#client.ayon_core.host.interfaces.workfiles.IWorkfileHost.file_extensions","title":"<code>file_extensions()</code>","text":"<p>Deprecated variant of 'get_workfile_extensions'.</p> Todo <p>Remove when all usages are replaced.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>@deprecated(\"Use 'get_workfile_extensions' instead\")\ndef file_extensions(self):\n    \"\"\"Deprecated variant of 'get_workfile_extensions'.\n\n    Todo:\n        Remove when all usages are replaced.\n\n    \"\"\"\n    return self.get_workfile_extensions()\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/workfiles.html#client.ayon_core.host.interfaces.workfiles.IWorkfileHost.get_current_workfile","title":"<code>get_current_workfile()</code>  <code>abstractmethod</code>","text":"<p>Retrieve a path to current opened file.</p> <p>Returns:</p> Type Description <code>Optional[str]</code> <p>Optional[str]: Path to the file which is currently opened. None if nothing is opened or the current workfile is unsaved.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>@abstractmethod\ndef get_current_workfile(self) -&gt; Optional[str]:\n    \"\"\"Retrieve a path to current opened file.\n\n    Returns:\n        Optional[str]: Path to the file which is currently opened. None if\n            nothing is opened or the current workfile is unsaved.\n\n    \"\"\"\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/workfiles.html#client.ayon_core.host.interfaces.workfiles.IWorkfileHost.get_workfile_extensions","title":"<code>get_workfile_extensions()</code>","text":"<p>Extensions that can be used to save the workfile to.</p> Notes <p>Method may not be used if 'list_workfiles' and     'list_published_workfiles' are re-implemented with different     logic.</p> <p>Returns:</p> Type Description <code>list[str]</code> <p>list[str]: List of extensions that can be used for saving.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>def get_workfile_extensions(self) -&gt; list[str]:\n    \"\"\"Extensions that can be used to save the workfile to.\n\n    Notes:\n        Method may not be used if 'list_workfiles' and\n            'list_published_workfiles' are re-implemented with different\n            logic.\n\n    Returns:\n        list[str]: List of extensions that can be used for saving.\n\n    \"\"\"\n    return []\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/workfiles.html#client.ayon_core.host.interfaces.workfiles.IWorkfileHost.has_unsaved_changes","title":"<code>has_unsaved_changes()</code>","text":"<p>Deprecated variant of 'workfile_has_unsaved_changes'.</p> Todo <p>Remove when all usages are replaced.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>@deprecated(\"Use 'workfile_has_unsaved_changes' instead\")\ndef has_unsaved_changes(self):\n    \"\"\"Deprecated variant of 'workfile_has_unsaved_changes'.\n\n    Todo:\n        Remove when all usages are replaced.\n\n    \"\"\"\n    return self.workfile_has_unsaved_changes()\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/workfiles.html#client.ayon_core.host.interfaces.workfiles.IWorkfileHost.list_published_workfiles","title":"<code>list_published_workfiles(project_name, folder_id, *, prepared_data=None)</code>","text":"<p>List published workfiles for the given folder.</p> <p>The default implementation looks for products with the 'workfile'     product type.</p> Pre-fetched entities have mandatory fields to be fetched <ul> <li>Version: 'id', 'author', 'taskId'</li> <li>Representation: 'id', 'versionId', 'files'</li> </ul> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>folder_id</code> <code>str</code> <p>Folder id.</p> required <code>prepared_data</code> <code>Optional[ListPublishedWorkfilesOptionalData]</code> <p>Prepared data for speed enhancements.</p> <code>None</code> <p>Returns:</p> Type Description <code>list[PublishedWorkfileInfo]</code> <p>list[PublishedWorkfileInfo]: Published workfile information for the given context.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>def list_published_workfiles(\n    self,\n    project_name: str,\n    folder_id: str,\n    *,\n    prepared_data: Optional[ListPublishedWorkfilesOptionalData] = None,\n) -&gt; list[PublishedWorkfileInfo]:\n    \"\"\"List published workfiles for the given folder.\n\n    The default implementation looks for products with the 'workfile'\n        product type.\n\n    Pre-fetched entities have mandatory fields to be fetched:\n        - Version: 'id', 'author', 'taskId'\n        - Representation: 'id', 'versionId', 'files'\n\n    Args:\n        project_name (str): Project name.\n        folder_id (str): Folder id.\n        prepared_data (Optional[ListPublishedWorkfilesOptionalData]):\n            Prepared data for speed enhancements.\n\n    Returns:\n        list[PublishedWorkfileInfo]: Published workfile information for\n            the given context.\n\n    \"\"\"\n    list_workfiles_context = get_list_published_workfiles_context(\n        project_name,\n        folder_id,\n        prepared_data=prepared_data,\n    )\n    if not list_workfiles_context.repre_entities:\n        return []\n\n    versions_by_id = {\n        version_entity[\"id\"]: version_entity\n        for version_entity in list_workfiles_context.version_entities\n    }\n    extensions = {\n        ext.lstrip(\".\")\n        for ext in self.get_workfile_extensions()\n    }\n    items = []\n    for repre_entity in list_workfiles_context.repre_entities:\n        version_id = repre_entity[\"versionId\"]\n        version_entity = versions_by_id[version_id]\n        task_id = version_entity[\"taskId\"]\n\n        # Filter by extension\n        workfile_path = None\n        for repre_file in repre_entity[\"files\"]:\n            ext = (\n                os.path.splitext(repre_file[\"name\"])[1]\n                .lower()\n                .lstrip(\".\")\n            )\n            if ext in extensions:\n                workfile_path = repre_file[\"path\"]\n                break\n\n        if not workfile_path:\n            continue\n\n        try:\n            workfile_path = workfile_path.format(\n                root=list_workfiles_context.anatomy.roots\n            )\n        except Exception:\n            self.log.warning(\n                \"Failed to format workfile path.\", exc_info=True\n            )\n\n        is_available = False\n        file_size = file_modified = file_created = None\n        if workfile_path and os.path.exists(workfile_path):\n            filestat = os.stat(workfile_path)\n            is_available = True\n            file_size = filestat.st_size\n            file_created = filestat.st_ctime\n            file_modified = filestat.st_mtime\n\n        workfile_item = PublishedWorkfileInfo.new(\n            project_name,\n            folder_id,\n            task_id,\n            repre_entity,\n            filepath=workfile_path,\n            author=version_entity[\"author\"],\n            available=is_available,\n            file_size=file_size,\n            file_created=file_created,\n            file_modified=file_modified,\n        )\n        items.append(workfile_item)\n\n    return items\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/workfiles.html#client.ayon_core.host.interfaces.workfiles.IWorkfileHost.list_workfiles","title":"<code>list_workfiles(project_name, folder_entity, task_entity, *, prepared_data=None)</code>","text":"<p>List workfiles in the given task.</p> <p>The method should also return workfiles that are not available on     disk, but are in the AYON database.</p> <p>Notes: - Better method name? - This method is pre-implemented as the logic can be shared across     95% of host integrations. Ad-hoc implementation to give host     integration workfile api functionality.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>folder_entity</code> <code>dict[str, Any]</code> <p>Folder entity.</p> required <code>task_entity</code> <code>dict[str, Any]</code> <p>Task entity.</p> required <code>prepared_data</code> <code>Optional[ListWorkfilesOptionalData]</code> <p>Prepared data for speed enhancements.</p> <code>None</code> <p>Returns:</p> Type Description <code>list[WorkfileInfo]</code> <p>list[WorkfileInfo]: List of workfiles.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>def list_workfiles(\n    self,\n    project_name: str,\n    folder_entity: dict[str, Any],\n    task_entity: dict[str, Any],\n    *,\n    prepared_data: Optional[ListWorkfilesOptionalData] = None,\n) -&gt; list[WorkfileInfo]:\n    \"\"\"List workfiles in the given task.\n\n    The method should also return workfiles that are not available on\n        disk, but are in the AYON database.\n\n    Notes:\n    - Better method name?\n    - This method is pre-implemented as the logic can be shared across\n        95% of host integrations. Ad-hoc implementation to give host\n        integration workfile api functionality.\n\n    Args:\n        project_name (str): Project name.\n        folder_entity (dict[str, Any]): Folder entity.\n        task_entity (dict[str, Any]): Task entity.\n        prepared_data (Optional[ListWorkfilesOptionalData]): Prepared\n            data for speed enhancements.\n\n    Returns:\n        list[WorkfileInfo]: List of workfiles.\n\n    \"\"\"\n    from ayon_core.pipeline.template_data import get_template_data\n    from ayon_core.pipeline.workfile.path_resolving import (\n        get_workdir_with_workdir_data,\n        WorkfileDataParser,\n    )\n\n    extensions = self.get_workfile_extensions()\n    if not extensions:\n        return []\n\n    list_workfiles_context = get_list_workfiles_context(\n        project_name,\n        folder_entity,\n        task_entity,\n        host_name=self.name,\n        prepared_data=prepared_data,\n    )\n\n    workfile_entities_by_path = {}\n    for workfile_entity in list_workfiles_context.workfile_entities:\n        rootless_path = workfile_entity[\"path\"]\n        path = os.path.normpath(\n            list_workfiles_context.anatomy.fill_root(rootless_path)\n        )\n        workfile_entities_by_path[path] = workfile_entity\n\n    workdir_data = get_template_data(\n        list_workfiles_context.project_entity,\n        folder_entity,\n        task_entity,\n        host_name=self.name,\n    )\n    workdir = get_workdir_with_workdir_data(\n        workdir_data,\n        project_name,\n        anatomy=list_workfiles_context.anatomy,\n        template_key=list_workfiles_context.template_key,\n        project_settings=list_workfiles_context.project_settings,\n    )\n\n    file_template = list_workfiles_context.anatomy.get_template_item(\n        \"work\", list_workfiles_context.template_key, \"file\"\n    )\n    rootless_workdir = workdir.rootless\n    if platform.system().lower() == \"windows\":\n        rootless_workdir = rootless_workdir.replace(\"\\\\\", \"/\")\n\n    filenames = []\n    if os.path.exists(workdir):\n        filenames = list(os.listdir(workdir))\n\n    data_parser = WorkfileDataParser(file_template, workdir_data)\n    items = []\n    for filename in filenames:\n        # TODO add 'default' support for folders\n        ext = os.path.splitext(filename)[1].lower()\n        if ext not in extensions:\n            continue\n\n        filepath = os.path.join(workdir, filename)\n\n        rootless_path = f\"{rootless_workdir}/{filename}\"\n        workfile_entity = workfile_entities_by_path.pop(\n            filepath, None\n        )\n        version = comment = None\n        if workfile_entity is not None:\n            _data = workfile_entity[\"data\"]\n            version = _data.get(\"version\")\n            comment = _data.get(\"comment\")\n\n        if version is None:\n            parsed_data = data_parser.parse_data(filename)\n            version = parsed_data.version\n            comment = parsed_data.comment\n\n        item = WorkfileInfo.new(\n            filepath,\n            rootless_path,\n            version=version,\n            comment=comment,\n            available=True,\n            workfile_entity=workfile_entity,\n        )\n        items.append(item)\n\n    for filepath, workfile_entity in workfile_entities_by_path.items():\n        # Workfile entity is not in the filesystem\n        #   but it is in the database\n        rootless_path = workfile_entity[\"path\"]\n        ext = os.path.splitext(rootless_path)[1].lower()\n        if ext not in extensions:\n            continue\n\n        _data = workfile_entity[\"data\"]\n        version = _data.get(\"version\")\n        comment = _data.get(\"comment\")\n        if version is None:\n            filename = os.path.basename(rootless_path)\n            parsed_data = data_parser.parse_data(filename)\n            version = parsed_data.version\n            comment = parsed_data.comment\n\n        available = os.path.exists(filepath)\n        items.append(WorkfileInfo.new(\n            filepath,\n            rootless_path,\n            version=version,\n            comment=comment,\n            available=available,\n            workfile_entity=workfile_entity,\n        ))\n\n    return items\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/workfiles.html#client.ayon_core.host.interfaces.workfiles.IWorkfileHost.open_file","title":"<code>open_file(filepath)</code>","text":"<p>Deprecated variant of 'open_workfile'.</p> Todo <p>Remove when all usages are replaced.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>@deprecated(\"Use 'open_workfile' instead\")\ndef open_file(self, filepath):\n    \"\"\"Deprecated variant of 'open_workfile'.\n\n    Todo:\n        Remove when all usages are replaced.\n\n    \"\"\"\n    return self.open_workfile(filepath)\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/workfiles.html#client.ayon_core.host.interfaces.workfiles.IWorkfileHost.open_workfile","title":"<code>open_workfile(filepath)</code>  <code>abstractmethod</code>","text":"<p>Open passed filepath in the host.</p> <p>Parameters:</p> Name Type Description Default <code>filepath</code> <code>str</code> <p>Path to workfile.</p> required Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>@abstractmethod\ndef open_workfile(self, filepath: str) -&gt; None:\n    \"\"\"Open passed filepath in the host.\n\n    Args:\n        filepath (str): Path to workfile.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/workfiles.html#client.ayon_core.host.interfaces.workfiles.IWorkfileHost.open_workfile_with_context","title":"<code>open_workfile_with_context(filepath, folder_entity, task_entity, *, prepared_data=None)</code>","text":"<p>Open passed filepath in the host with context.</p> <p>This function should be used to open workfile in different context.</p> Notes <p>Should this method care about context change?</p> <p>Parameters:</p> Name Type Description Default <code>filepath</code> <code>str</code> <p>Path to workfile.</p> required <code>folder_entity</code> <code>dict[str, Any]</code> <p>Folder id.</p> required <code>task_entity</code> <code>dict[str, Any]</code> <p>Task id.</p> required <code>prepared_data</code> <code>Optional[WorkfileOptionalData]</code> <p>Prepared data for speed enhancements.</p> <code>None</code> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>def open_workfile_with_context(\n    self,\n    filepath: str,\n    folder_entity: dict[str, Any],\n    task_entity: dict[str, Any],\n    *,\n    prepared_data: Optional[OpenWorkfileOptionalData] = None,\n) -&gt; None:\n    \"\"\"Open passed filepath in the host with context.\n\n    This function should be used to open workfile in different context.\n\n    Notes:\n        Should this method care about context change?\n\n    Args:\n        filepath (str): Path to workfile.\n        folder_entity (dict[str, Any]): Folder id.\n        task_entity (dict[str, Any]): Task id.\n        prepared_data (Optional[WorkfileOptionalData]): Prepared data\n            for speed enhancements.\n\n    \"\"\"\n    context = self.get_current_context()\n    project_name = context[\"project_name\"]\n\n    open_workfile_context = get_open_workfile_context(\n        project_name,\n        filepath,\n        folder_entity,\n        task_entity,\n        prepared_data=prepared_data,\n    )\n\n    workdir = os.path.dirname(filepath)\n    # Set 'AYON_WORKDIR' environment variable\n    os.environ[\"AYON_WORKDIR\"] = workdir\n\n    event_data = self._get_workfile_event_data(\n        project_name, folder_entity, task_entity, filepath\n    )\n    self._before_workfile_open(open_workfile_context)\n    self._emit_workfile_open_event(event_data, after_open=False)\n\n    if self.change_context_before_workfile_open:\n        self.set_current_context(\n            folder_entity,\n            task_entity,\n            reason=ContextChangeReason.workfile_open,\n            project_entity=open_workfile_context.project_entity,\n            anatomy=open_workfile_context.anatomy,\n        )\n\n    self.open_workfile(filepath)\n\n    if not self.change_context_before_workfile_open:\n        self.set_current_context(\n            folder_entity,\n            task_entity,\n            reason=ContextChangeReason.workfile_open,\n            project_entity=open_workfile_context.project_entity,\n            anatomy=open_workfile_context.anatomy,\n        )\n\n    self._after_workfile_open(open_workfile_context)\n    self._emit_workfile_open_event(event_data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/workfiles.html#client.ayon_core.host.interfaces.workfiles.IWorkfileHost.save_file","title":"<code>save_file(dst_path=None)</code>","text":"<p>Deprecated variant of 'save_workfile'.</p> Todo <p>Remove when all usages are replaced</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>@deprecated(\"Use 'save_workfile' instead\")\ndef save_file(self, dst_path=None):\n    \"\"\"Deprecated variant of 'save_workfile'.\n\n    Todo:\n        Remove when all usages are replaced\n\n    \"\"\"\n    self.save_workfile(dst_path)\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/workfiles.html#client.ayon_core.host.interfaces.workfiles.IWorkfileHost.save_workfile","title":"<code>save_workfile(dst_path=None)</code>  <code>abstractmethod</code>","text":"<p>Save the currently opened scene.</p> <p>Parameters:</p> Name Type Description Default <code>dst_path</code> <code>str</code> <p>Where the current scene should be saved. Or use the current path if 'None' is passed.</p> <code>None</code> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>@abstractmethod\ndef save_workfile(self, dst_path: Optional[str] = None) -&gt; None:\n    \"\"\"Save the currently opened scene.\n\n    Args:\n        dst_path (str): Where the current scene should be saved. Or use\n            the current path if 'None' is passed.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/workfiles.html#client.ayon_core.host.interfaces.workfiles.IWorkfileHost.save_workfile_with_context","title":"<code>save_workfile_with_context(filepath, folder_entity, task_entity, *, version=None, comment=None, description=None, prepared_data=None)</code>","text":"<p>Save the current workfile with context.</p> <p>Arguments 'rootless_path', 'workfile_entities', 'project_entity'     and 'anatomy' can be filled to enhance efficiency if you already     have access to the values.</p> <p>Argument 'project_settings' is used to calculate 'rootless_path'     if it is not provided.</p> Notes <p>Should this method care about context change?</p> <p>Parameters:</p> Name Type Description Default <code>filepath</code> <code>str</code> <p>Where the current scene should be saved.</p> required <code>folder_entity</code> <code>dict[str, Any]</code> <p>Folder entity.</p> required <code>task_entity</code> <code>dict[str, Any]</code> <p>Task entity.</p> required <code>version</code> <code>Optional[int]</code> <p>Version of the workfile. Information for workfile entity. Recommended to fill.</p> <code>None</code> <code>comment</code> <code>Optional[str]</code> <p>Comment for the workfile. Usually used in the filename template.</p> <code>None</code> <code>description</code> <code>Optional[str]</code> <p>Artist note for the workfile entity.</p> <code>None</code> <code>prepared_data</code> <code>Optional[SaveWorkfileOptionalData]</code> <p>Prepared data for speed enhancements.</p> <code>None</code> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>def save_workfile_with_context(\n    self,\n    filepath: str,\n    folder_entity: dict[str, Any],\n    task_entity: dict[str, Any],\n    *,\n    version: Optional[int] = None,\n    comment: Optional[str] = None,\n    description: Optional[str] = None,\n    prepared_data: Optional[SaveWorkfileOptionalData] = None,\n) -&gt; None:\n    \"\"\"Save the current workfile with context.\n\n    Arguments 'rootless_path', 'workfile_entities', 'project_entity'\n        and 'anatomy' can be filled to enhance efficiency if you already\n        have access to the values.\n\n    Argument 'project_settings' is used to calculate 'rootless_path'\n        if it is not provided.\n\n    Notes:\n        Should this method care about context change?\n\n    Args:\n        filepath (str): Where the current scene should be saved.\n        folder_entity (dict[str, Any]): Folder entity.\n        task_entity (dict[str, Any]): Task entity.\n        version (Optional[int]): Version of the workfile. Information\n            for workfile entity. Recommended to fill.\n        comment (Optional[str]): Comment for the workfile.\n            Usually used in the filename template.\n        description (Optional[str]): Artist note for the workfile entity.\n        prepared_data (Optional[SaveWorkfileOptionalData]): Prepared data\n            for speed enhancements.\n\n    \"\"\"\n    project_name = self.get_current_project_name()\n    save_workfile_context = get_save_workfile_context(\n        project_name,\n        filepath,\n        folder_entity,\n        task_entity,\n        host_name=self.name,\n        prepared_data=prepared_data,\n    )\n\n    self._before_workfile_save(save_workfile_context)\n    event_data = self._get_workfile_event_data(\n        project_name,\n        folder_entity,\n        task_entity,\n        filepath,\n    )\n    self._emit_workfile_save_event(event_data, after_save=False)\n\n    workdir = os.path.dirname(filepath)\n    if not os.path.exists(workdir):\n        os.makedirs(workdir, exist_ok=True)\n\n    # Set 'AYON_WORKDIR' environment variable\n    os.environ[\"AYON_WORKDIR\"] = workdir\n\n    self.set_current_context(\n        folder_entity,\n        task_entity,\n        reason=ContextChangeReason.workfile_save,\n        project_entity=save_workfile_context.project_entity,\n        anatomy=save_workfile_context.anatomy,\n    )\n\n    self.save_workfile(filepath)\n\n    self._save_workfile_entity(\n        save_workfile_context,\n        version,\n        comment,\n        description,\n    )\n    self._after_workfile_save(save_workfile_context)\n    self._emit_workfile_save_event(event_data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/workfiles.html#client.ayon_core.host.interfaces.workfiles.IWorkfileHost.workfile_has_unsaved_changes","title":"<code>workfile_has_unsaved_changes()</code>","text":"<p>Currently opened scene is saved.</p> <p>Not all hosts can know if the current scene is saved because the API     of DCC does not support it.</p> <p>Returns:</p> Type Description <code>Optional[bool]</code> <p>Optional[bool]: True if scene is saved and False if has unsaved modifications. None if can't tell if workfiles has modifications.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>def workfile_has_unsaved_changes(self) -&gt; Optional[bool]:\n    \"\"\"Currently opened scene is saved.\n\n    Not all hosts can know if the current scene is saved because the API\n        of DCC does not support it.\n\n    Returns:\n        Optional[bool]: True if scene is saved and False if has unsaved\n            modifications. None if can't tell if workfiles has\n            modifications.\n\n    \"\"\"\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/workfiles.html#client.ayon_core.host.interfaces.workfiles.ListPublishedWorkfilesOptionalData","title":"<code>ListPublishedWorkfilesOptionalData</code>","text":"<p>               Bases: <code>_WorkfileOptionalData</code></p> <p>Optional data to list published workfiles.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>class ListPublishedWorkfilesOptionalData(_WorkfileOptionalData):\n    \"\"\"Optional data to list published workfiles.\"\"\"\n    data_version = 1\n\n    def __init__(\n        self,\n        *,\n        project_entity: Optional[dict[str, Any]] = None,\n        anatomy: Optional[\"Anatomy\"] = None,\n        project_settings: Optional[dict[str, Any]] = None,\n        product_entities: Optional[list[dict[str, Any]]] = None,\n        version_entities: Optional[list[dict[str, Any]]] = None,\n        repre_entities: Optional[list[dict[str, Any]]] = None,\n        **kwargs\n    ):\n        super().__init__(\n            project_entity=project_entity,\n            anatomy=anatomy,\n            project_settings=project_settings,\n            **kwargs\n        )\n\n        self.product_entities = product_entities\n        self.version_entities = version_entities\n        self.repre_entities = repre_entities\n\n    def get_entities(\n        self,\n        project_name: str,\n        folder_id: str,\n    ) -&gt; tuple[\n        list[dict[str, Any]],\n        list[dict[str, Any]],\n        list[dict[str, Any]]\n    ]:\n        product_entities = self.product_entities\n        if product_entities is None:\n            # TODO remove when server &gt;= 1.14.0 is required\n            filter_key = \"product_base_types\"\n            if ayon_api.get_server_version_tuple() &lt; (1, 14, 0):\n                filter_key = \"product_types\"\n            filter_kwargs = {filter_key: {\"workfile\"}}\n            product_entities = list(ayon_api.get_products(\n                project_name,\n                folder_ids={folder_id},\n                **filter_kwargs,\n                fields={\"id\", \"name\"},\n            ))\n\n        version_entities = self.version_entities\n        if version_entities is None:\n            product_ids = {p[\"id\"] for p in product_entities}\n            version_entities = list(ayon_api.get_versions(\n                project_name,\n                product_ids=product_ids,\n                fields={\"id\", \"author\", \"taskId\"},\n            ))\n\n        repre_entities = self.repre_entities\n        if repre_entities is None:\n            version_ids = {v[\"id\"] for v in version_entities}\n            repre_entities = list(ayon_api.get_representations(\n                project_name,\n                version_ids=version_ids,\n            ))\n        return product_entities, version_entities, repre_entities\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/workfiles.html#client.ayon_core.host.interfaces.workfiles.ListWorkfilesOptionalData","title":"<code>ListWorkfilesOptionalData</code>","text":"<p>               Bases: <code>_WorkfileOptionalData</code></p> <p>Optional data to list workfiles.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>class ListWorkfilesOptionalData(_WorkfileOptionalData):\n    \"\"\"Optional data to list workfiles.\"\"\"\n    data_version = 1\n\n    def __init__(\n        self,\n        *,\n        project_entity: Optional[dict[str, Any]] = None,\n        anatomy: Optional[\"Anatomy\"] = None,\n        project_settings: Optional[dict[str, Any]] = None,\n        template_key: Optional[str] = None,\n        workfile_entities: Optional[list[dict[str, Any]]] = None,\n        **kwargs\n    ):\n        super().__init__(\n            project_entity=project_entity,\n            anatomy=anatomy,\n            project_settings=project_settings,\n            **kwargs\n        )\n        self.template_key = template_key\n        self.workfile_entities = workfile_entities\n\n    def get_template_key(\n        self,\n        project_name: str,\n        task_type: str,\n        host_name: str,\n        project_settings: dict[str, Any],\n    ) -&gt; str:\n        from ayon_core.pipeline.workfile import get_workfile_template_key\n\n        if self.template_key is not None:\n            return self.template_key\n\n        return get_workfile_template_key(\n            project_name=project_name,\n            task_type=task_type,\n            host_name=host_name,\n            project_settings=project_settings,\n        )\n\n    def get_workfile_entities(\n        self, project_name: str, task_id: str\n    ) -&gt; list[dict[str, Any]]:\n        \"\"\"Fill workfile entities if not provided.\"\"\"\n        if self.workfile_entities is not None:\n            return self.workfile_entities\n        return list(ayon_api.get_workfiles_info(\n            project_name, task_ids=[task_id]\n        ))\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/workfiles.html#client.ayon_core.host.interfaces.workfiles.ListWorkfilesOptionalData.get_workfile_entities","title":"<code>get_workfile_entities(project_name, task_id)</code>","text":"<p>Fill workfile entities if not provided.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>def get_workfile_entities(\n    self, project_name: str, task_id: str\n) -&gt; list[dict[str, Any]]:\n    \"\"\"Fill workfile entities if not provided.\"\"\"\n    if self.workfile_entities is not None:\n        return self.workfile_entities\n    return list(ayon_api.get_workfiles_info(\n        project_name, task_ids=[task_id]\n    ))\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/workfiles.html#client.ayon_core.host.interfaces.workfiles.OpenWorkfileOptionalData","title":"<code>OpenWorkfileOptionalData</code>","text":"<p>               Bases: <code>_WorkfileOptionalData</code></p> <p>Optional data for opening workfile.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>class OpenWorkfileOptionalData(_WorkfileOptionalData):\n    \"\"\"Optional data for opening workfile.\"\"\"\n    data_version = 1\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/workfiles.html#client.ayon_core.host.interfaces.workfiles.PublishedWorkfileInfo","title":"<code>PublishedWorkfileInfo</code>  <code>dataclass</code>","text":"<p>Information about published workfile.</p> <p>Host can copy and use the workfile using this information object.</p> <p>Attributes:</p> Name Type Description <code>project_name</code> <code>str</code> <p>Name of the project where workfile lives.</p> <code>folder_id</code> <code>str</code> <p>Folder id under which is workfile stored.</p> <code>task_id</code> <code>Optional[str]</code> <p>Task id under which is workfile stored.</p> <code>representation_id</code> <code>str</code> <p>Representation id of the workfile.</p> <code>filepath</code> <code>str</code> <p>Path to the workfile.</p> <code>created_at</code> <code>float</code> <p>Timestamp when the workfile representation was created.</p> <code>author</code> <code>str</code> <p>Author of the workfile representation.</p> <code>available</code> <code>bool</code> <p>True if workfile is available on the machine.</p> <code>file_size</code> <code>Optional[float]</code> <p>Size of the workfile in bytes.</p> <code>file_created</code> <code>Optional[float]</code> <p>Timestamp when the workfile was created on the filesystem.</p> <code>file_modified</code> <code>Optional[float]</code> <p>Timestamp when the workfile was modified on the filesystem.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>@dataclass\nclass PublishedWorkfileInfo:\n    \"\"\"Information about published workfile.\n\n    Host can copy and use the workfile using this information object.\n\n    Attributes:\n        project_name (str): Name of the project where workfile lives.\n        folder_id (str): Folder id under which is workfile stored.\n        task_id (Optional[str]): Task id under which is workfile stored.\n        representation_id (str): Representation id of the workfile.\n        filepath (str): Path to the workfile.\n        created_at (float): Timestamp when the workfile representation\n            was created.\n        author (str): Author of the workfile representation.\n        available (bool): True if workfile is available on the machine.\n        file_size (Optional[float]): Size of the workfile in bytes.\n        file_created (Optional[float]): Timestamp when the workfile was\n            created on the filesystem.\n        file_modified (Optional[float]): Timestamp when the workfile was\n            modified on the filesystem.\n\n    \"\"\"\n    project_name: str\n    folder_id: str\n    task_id: Optional[str]\n    representation_id: str\n    filepath: str\n    created_at: float\n    author: str\n    available: bool\n    file_size: Optional[float]\n    file_created: Optional[float]\n    file_modified: Optional[float]\n\n    @classmethod\n    def new(\n        cls,\n        project_name: str,\n        folder_id: str,\n        task_id: Optional[str],\n        repre_entity: dict[str, Any],\n        *,\n        filepath: str,\n        author: str,\n        available: bool,\n        file_size: Optional[float],\n        file_modified: Optional[float],\n        file_created: Optional[float],\n    ) -&gt; \"PublishedWorkfileInfo\":\n        created_at = arrow.get(repre_entity[\"createdAt\"]).to(\"local\")\n\n        return cls(\n            project_name=project_name,\n            folder_id=folder_id,\n            task_id=task_id,\n            representation_id=repre_entity[\"id\"],\n            filepath=filepath,\n            created_at=created_at.float_timestamp,\n            author=author,\n            available=available,\n            file_size=file_size,\n            file_created=file_created,\n            file_modified=file_modified,\n        )\n\n    def to_data(self) -&gt; dict[str, Any]:\n        \"\"\"Converts file item to data.\n\n        Returns:\n            dict[str, Any]: Workfile item data.\n\n        \"\"\"\n        return asdict(self)\n\n    @classmethod\n    def from_data(cls, data: dict[str, Any]) -&gt; \"PublishedWorkfileInfo\":\n        \"\"\"Converts data to workfile item.\n\n        Args:\n            data (dict[str, Any]): Workfile item data.\n\n        Returns:\n            PublishedWorkfileInfo: File item.\n\n        \"\"\"\n        return PublishedWorkfileInfo(**data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/workfiles.html#client.ayon_core.host.interfaces.workfiles.PublishedWorkfileInfo.from_data","title":"<code>from_data(data)</code>  <code>classmethod</code>","text":"<p>Converts data to workfile item.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict[str, Any]</code> <p>Workfile item data.</p> required <p>Returns:</p> Name Type Description <code>PublishedWorkfileInfo</code> <code>'PublishedWorkfileInfo'</code> <p>File item.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>@classmethod\ndef from_data(cls, data: dict[str, Any]) -&gt; \"PublishedWorkfileInfo\":\n    \"\"\"Converts data to workfile item.\n\n    Args:\n        data (dict[str, Any]): Workfile item data.\n\n    Returns:\n        PublishedWorkfileInfo: File item.\n\n    \"\"\"\n    return PublishedWorkfileInfo(**data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/workfiles.html#client.ayon_core.host.interfaces.workfiles.PublishedWorkfileInfo.to_data","title":"<code>to_data()</code>","text":"<p>Converts file item to data.</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: Workfile item data.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>def to_data(self) -&gt; dict[str, Any]:\n    \"\"\"Converts file item to data.\n\n    Returns:\n        dict[str, Any]: Workfile item data.\n\n    \"\"\"\n    return asdict(self)\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/workfiles.html#client.ayon_core.host.interfaces.workfiles.SaveWorkfileOptionalData","title":"<code>SaveWorkfileOptionalData</code>","text":"<p>               Bases: <code>_WorkfileOptionalData</code></p> <p>Optional data to save workfile.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>class SaveWorkfileOptionalData(_WorkfileOptionalData):\n    \"\"\"Optional data to save workfile.\"\"\"\n    data_version = 1\n\n    def __init__(\n        self,\n        *,\n        project_entity: Optional[dict[str, Any]] = None,\n        anatomy: Optional[\"Anatomy\"] = None,\n        project_settings: Optional[dict[str, Any]] = None,\n        rootless_path: Optional[str] = None,\n        workfile_entities: Optional[list[dict[str, Any]]] = None,\n        **kwargs\n    ):\n        super().__init__(\n            project_entity=project_entity,\n            anatomy=anatomy,\n            project_settings=project_settings,\n            **kwargs\n        )\n\n        self.rootless_path = rootless_path\n        self.workfile_entities = workfile_entities\n\n    def get_workfile_entities(self, project_name: str, task_id: str):\n        \"\"\"Fill workfile entities if not provided.\"\"\"\n        if self.workfile_entities is not None:\n            return self.workfile_entities\n        return list(ayon_api.get_workfiles_info(\n            project_name, task_ids=[task_id]\n        ))\n\n    def get_rootless_path(\n        self,\n        workfile_path: str,\n        project_name: str,\n        folder_entity: dict[str, Any],\n        task_entity: dict[str, Any],\n        host_name: str,\n        project_entity: dict[str, Any],\n        project_settings: dict[str, Any],\n        anatomy: \"Anatomy\",\n    ):\n        from ayon_core.pipeline.workfile.utils import (\n            find_workfile_rootless_path\n        )\n\n        if self.rootless_path is not None:\n            return self.rootless_path\n\n        return find_workfile_rootless_path(\n            workfile_path,\n            project_name,\n            folder_entity,\n            task_entity,\n            host_name,\n            project_entity=project_entity,\n            project_settings=project_settings,\n            anatomy=anatomy,\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/workfiles.html#client.ayon_core.host.interfaces.workfiles.SaveWorkfileOptionalData.get_workfile_entities","title":"<code>get_workfile_entities(project_name, task_id)</code>","text":"<p>Fill workfile entities if not provided.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>def get_workfile_entities(self, project_name: str, task_id: str):\n    \"\"\"Fill workfile entities if not provided.\"\"\"\n    if self.workfile_entities is not None:\n        return self.workfile_entities\n    return list(ayon_api.get_workfiles_info(\n        project_name, task_ids=[task_id]\n    ))\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/workfiles.html#client.ayon_core.host.interfaces.workfiles.WorkfileInfo","title":"<code>WorkfileInfo</code>  <code>dataclass</code>","text":"<p>Information about workfile.</p> <p>Host can open, copy and use the workfile using this information object.</p> <p>Attributes:</p> Name Type Description <code>filepath</code> <code>str</code> <p>Path to the workfile.</p> <code>rootless_path</code> <code>str</code> <p>Path to the workfile without the root. And without backslashes on Windows.</p> <code>version</code> <code>Optional[int]</code> <p>Version of the workfile.</p> <code>comment</code> <code>Optional[str]</code> <p>Comment of the workfile.</p> <code>file_size</code> <code>Optional[float]</code> <p>Size of the workfile in bytes.</p> <code>file_created</code> <code>Optional[float]</code> <p>Timestamp when the workfile was created on the filesystem.</p> <code>file_modified</code> <code>Optional[float]</code> <p>Timestamp when the workfile was modified on the filesystem.</p> <code>workfile_entity_id</code> <code>Optional[str]</code> <p>Workfile entity id. If None then the workfile is not in the database.</p> <code>description</code> <code>str</code> <p>Description of the workfile.</p> <code>created_by</code> <code>Optional[str]</code> <p>User id of the user who created the workfile entity.</p> <code>updated_by</code> <code>Optional[str]</code> <p>User id of the user who updated the workfile entity.</p> <code>available</code> <code>bool</code> <p>True if workfile is available on the machine.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>@dataclass\nclass WorkfileInfo:\n    \"\"\"Information about workfile.\n\n    Host can open, copy and use the workfile using this information object.\n\n    Attributes:\n        filepath (str): Path to the workfile.\n        rootless_path (str): Path to the workfile without the root. And without\n            backslashes on Windows.\n        version (Optional[int]): Version of the workfile.\n        comment (Optional[str]): Comment of the workfile.\n        file_size (Optional[float]): Size of the workfile in bytes.\n        file_created (Optional[float]): Timestamp when the workfile was\n            created on the filesystem.\n        file_modified (Optional[float]): Timestamp when the workfile was\n            modified on the filesystem.\n        workfile_entity_id (Optional[str]): Workfile entity id. If None then\n            the workfile is not in the database.\n        description (str): Description of the workfile.\n        created_by (Optional[str]): User id of the user who created the\n            workfile entity.\n        updated_by (Optional[str]): User id of the user who updated the\n            workfile entity.\n        available (bool): True if workfile is available on the machine.\n\n    \"\"\"\n    filepath: str\n    rootless_path: str\n    version: Optional[int]\n    comment: Optional[str]\n    file_size: Optional[float]\n    file_created: Optional[float]\n    file_modified: Optional[float]\n    workfile_entity_id: Optional[str]\n    description: str\n    created_by: Optional[str]\n    updated_by: Optional[str]\n    available: bool\n\n    @classmethod\n    def new(\n        cls,\n        filepath: str,\n        rootless_path: str,\n        *,\n        version: Optional[int],\n        comment: Optional[str],\n        available: bool,\n        workfile_entity: dict[str, Any],\n    ):\n        file_size = file_modified = file_created = None\n        if filepath and os.path.exists(filepath):\n            filestat = os.stat(filepath)\n            file_size = filestat.st_size\n            file_created = filestat.st_ctime\n            file_modified = filestat.st_mtime\n\n        if workfile_entity is None:\n            workfile_entity = {}\n\n        attrib = {}\n        if workfile_entity:\n            attrib = workfile_entity[\"attrib\"]\n\n        return cls(\n            filepath=filepath,\n            rootless_path=rootless_path,\n            version=version,\n            comment=comment,\n            file_size=file_size,\n            file_created=file_created,\n            file_modified=file_modified,\n            workfile_entity_id=workfile_entity.get(\"id\"),\n            description=attrib.get(\"description\") or \"\",\n            created_by=workfile_entity.get(\"createdBy\"),\n            updated_by=workfile_entity.get(\"updatedBy\"),\n            available=available,\n        )\n\n    def to_data(self) -&gt; dict[str, Any]:\n        \"\"\"Converts file item to data.\n\n        Returns:\n            dict[str, Any]: Workfile item data.\n\n        \"\"\"\n        return asdict(self)\n\n    @classmethod\n    def from_data(cls, data: dict[str, Any]) -&gt; WorkfileInfo:\n        \"\"\"Converts data to workfile item.\n\n        Args:\n            data (dict[str, Any]): Workfile item data.\n\n        Returns:\n            WorkfileInfo: File item.\n\n        \"\"\"\n        return WorkfileInfo(**data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/workfiles.html#client.ayon_core.host.interfaces.workfiles.WorkfileInfo.from_data","title":"<code>from_data(data)</code>  <code>classmethod</code>","text":"<p>Converts data to workfile item.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict[str, Any]</code> <p>Workfile item data.</p> required <p>Returns:</p> Name Type Description <code>WorkfileInfo</code> <code>WorkfileInfo</code> <p>File item.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>@classmethod\ndef from_data(cls, data: dict[str, Any]) -&gt; WorkfileInfo:\n    \"\"\"Converts data to workfile item.\n\n    Args:\n        data (dict[str, Any]): Workfile item data.\n\n    Returns:\n        WorkfileInfo: File item.\n\n    \"\"\"\n    return WorkfileInfo(**data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/host/interfaces/workfiles.html#client.ayon_core.host.interfaces.workfiles.WorkfileInfo.to_data","title":"<code>to_data()</code>","text":"<p>Converts file item to data.</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: Workfile item data.</p> Source code in <code>client/ayon_core/host/interfaces/workfiles.py</code> <pre><code>def to_data(self) -&gt; dict[str, Any]:\n    \"\"\"Converts file item to data.\n\n    Returns:\n        dict[str, Any]: Workfile item data.\n\n    \"\"\"\n    return asdict(self)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html","title":"lib","text":"<p>AYON lib functions.</p>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.AYONSecureRegistry","title":"<code>AYONSecureRegistry</code>","text":"<p>               Bases: <code>ASettingRegistry</code></p> <p>Store information using keyring.</p> <p>Registry should be used for private data that should be available only for user.</p> <p>All passed registry names will have added prefix <code>AYON/</code> to easier identify which data were created by AYON.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name of registry used as the identifier for data.</p> required Source code in <code>client/ayon_core/lib/local_settings.py</code> <pre><code>class AYONSecureRegistry(ASettingRegistry):\n    \"\"\"Store information using keyring.\n\n    Registry should be used for private data that should be available only for\n    user.\n\n    All passed registry names will have added prefix `AYON/` to easier\n    identify which data were created by AYON.\n\n    Args:\n        name(str): Name of registry used as the identifier for data.\n\n    \"\"\"\n    def __init__(self, name: str) -&gt; None:\n        try:\n            import keyring\n\n        except Exception:\n            raise NotImplementedError(\n                \"Python module `keyring` is not available.\"\n            )\n\n        # hack for cx_freeze and Windows keyring backend\n        if platform.system().lower() == \"windows\":\n            from keyring.backends import Windows\n\n            keyring.set_keyring(Windows.WinVaultKeyring())\n\n        # Force \"AYON\" prefix\n        super().__init__(f\"AYON/{name}\")\n\n    @lru_cache(maxsize=32)\n    def _get_item(\n        self, name: str, default: Any = _PLACEHOLDER\n    ) -&gt; Optional[str]:\n        \"\"\"Get value of sensitive item from the system's keyring.\n\n        See also `Keyring module`_\n\n        Args:\n            name (str): Name of the item.\n            default (Any): Default value if the item is not available.\n\n        Returns:\n            value (str): Value of the item.\n\n        Raises:\n            RegistryItemNotFound: If the item doesn't exist and default\n                is not defined.\n\n        .. _Keyring module:\n            https://github.com/jaraco/keyring\n\n        \"\"\"\n        import keyring\n\n        try:\n            from keyring.errors import PasswordSetError\n        except ImportError:\n            PasswordSetError = _FakeException\n\n        # Capture 'ItemNotFoundException' exception (on linux)\n        try:\n            from secretstorage.exceptions import ItemNotFoundException\n        except ImportError:\n            ItemNotFoundException = _FakeException\n\n        value = None\n        try:\n            value = keyring.get_password(self._name, name)\n        except (ItemNotFoundException, PasswordSetError):\n            pass\n\n        if value is not None:\n            return value\n\n        if default is not _PLACEHOLDER:\n            return default\n\n        raise RegistryItemNotFound(\n            f\"Item {self._name}:{name} not found in keyring.\"\n        )\n\n    def _set_item(self, name: str, value: str) -&gt; None:\n        \"\"\"Set sensitive item into system's keyring.\n\n        This uses `Keyring module`_ to save sensitive stuff into system's\n        keyring.\n\n        Args:\n            name (str): Name of the item.\n            value (str): Value of the item.\n\n        .. _Keyring module:\n            https://github.com/jaraco/keyring\n\n        \"\"\"\n        import keyring\n\n        keyring.set_password(self._name, name, value)\n        self._get_item.cache_clear()\n\n    def _delete_item(self, name: str, ignore_missing: bool = True):\n        \"\"\"Delete value stored in system's keyring.\n\n        See also `Keyring module`_\n\n        Args:\n            name (str): Name of the item to be deleted.\n            ignore_missing (bool): If True, no error is raised if item doesn't\n                exist.\n\n        .. _Keyring module:\n            https://github.com/jaraco/keyring\n\n        \"\"\"\n        import keyring\n\n        try:\n            from keyring.errors import PasswordDeleteError\n        except ImportError:\n            PasswordDeleteError = _FakeException\n\n        try:\n            keyring.delete_password(self._name, name)\n        except PasswordDeleteError:\n            if not ignore_missing:\n                raise RegistryItemNotFound(\n                    f\"Item {self._name}:{name} not found in keyring.\"\n                )\n        self._get_item.cache_clear()\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.AYONSettingsRegistry","title":"<code>AYONSettingsRegistry</code>","text":"<p>               Bases: <code>JSONSettingRegistry</code></p> <p>Class handling AYON general settings registry.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>Optional[str]</code> <p>Name of the registry.</p> <code>None</code> Source code in <code>client/ayon_core/lib/local_settings.py</code> <pre><code>class AYONSettingsRegistry(JSONSettingRegistry):\n    \"\"\"Class handling AYON general settings registry.\n\n    Args:\n        name (Optional[str]): Name of the registry.\n    \"\"\"\n    def __init__(self, name: Optional[str] = None) -&gt; None:\n        if not name:\n            name = \"AYON_settings\"\n            warnings.warn(\n                (\n                    \"Used 'AYONSettingsRegistry' without 'name' argument.\"\n                    \" The argument will be required in future versions.\"\n                ),\n                DeprecationWarning,\n                stacklevel=2,\n            )\n        path = get_launcher_local_dir()\n        super().__init__(name, path)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.AYONUrlIcon","title":"<code>AYONUrlIcon</code>  <code>dataclass</code>","text":"<p>               Bases: <code>IconBase</code></p> <p>Relative url to an image file on AYON server.</p> <p>Instead of using full url use relative path to image file on AYON server. - 'https://studio.ayon.app/addons/1.0.0/public/icon.png' - 'addons/1.0.0/public/icon.png'</p> <p>The main difference from 'UrlIcon' is that this approach also has access     to endpoints that do require authentication.</p> Source code in <code>client/ayon_core/lib/icon_definitions.py</code> <pre><code>@dataclass\nclass AYONUrlIcon(IconBase):\n    \"\"\"Relative url to an image file on AYON server.\n\n    Instead of using full url use relative path to image file on AYON server.\n    - 'https://studio.ayon.app/addons/1.0.0/public/icon.png'\n    - 'addons/1.0.0/public/icon.png'\n\n    The main difference from 'UrlIcon' is that this approach also has access\n        to endpoints that do require authentication.\n\n    \"\"\"\n    type: ClassVar[str] = \"ayon_url\"\n    url: str\n\n    def __post_init__(self):\n        self.url = self.url.lstrip(\"/\")\n\n    def get_unique_id(self) -&gt; str:\n        return f\"{self.type}|{self.url}\"\n\n    def get_content(self) -&gt; bytes:\n        url = f\"{ayon_api.get_base_url()}/{self.url}\"\n        try:\n            stream = io.BytesIO()\n            ayon_api.download_file_to_stream(url, stream)\n            return stream.getvalue()\n        except Exception:\n            log.warning(\n                \"Failed to download image '%s'\", url, exc_info=True\n            )\n            return b\"\"\n\n    def to_data(self) -&gt; dict:\n        return {\n            \"type\": self.type,\n            \"url\": self.url,\n        }\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.AbstractAttrDef","title":"<code>AbstractAttrDef</code>","text":"<p>Abstraction of attribute definition.</p> <p>Each attribute definition must have implemented validation and conversion method.</p> <p>Attribute definition should have ability to return \"default\" value. That can be based on passed data into <code>__init__</code> so is not abstracted to attribute.</p> <p>QUESTION: How to force to set <code>key</code> attribute?</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>Under which key will be attribute value stored.</p> required <code>default</code> <code>Any</code> <p>Default value of an attribute.</p> required <code>label</code> <code>Optional[str]</code> <p>Attribute label.</p> <code>None</code> <code>tooltip</code> <code>Optional[str]</code> <p>Attribute tooltip.</p> <code>None</code> <code>is_label_horizontal</code> <code>Optional[bool]</code> <p>UI specific argument. Specify if label is next to value input or ahead.</p> <code>None</code> <code>visible</code> <code>Optional[bool]</code> <p>Item is shown to user (for UI purposes).</p> <code>None</code> <code>enabled</code> <code>Optional[bool]</code> <p>Item is enabled (for UI purposes).</p> <code>None</code> <code>hidden</code> <code>Optional[bool]</code> <p>DEPRECATED: Use 'visible' instead.</p> <code>None</code> <code>disabled</code> <code>Optional[bool]</code> <p>DEPRECATED: Use 'enabled' instead.</p> <code>None</code> Source code in <code>client/ayon_core/lib/attribute_definitions.py</code> <pre><code>class AbstractAttrDef(metaclass=AbstractAttrDefMeta):\n    \"\"\"Abstraction of attribute definition.\n\n    Each attribute definition must have implemented validation and\n    conversion method.\n\n    Attribute definition should have ability to return \"default\" value. That\n    can be based on passed data into `__init__` so is not abstracted to\n    attribute.\n\n    QUESTION:\n    How to force to set `key` attribute?\n\n    Args:\n        key (str): Under which key will be attribute value stored.\n        default (Any): Default value of an attribute.\n        label (Optional[str]): Attribute label.\n        tooltip (Optional[str]): Attribute tooltip.\n        is_label_horizontal (Optional[bool]): UI specific argument. Specify\n            if label is next to value input or ahead.\n        visible (Optional[bool]): Item is shown to user (for UI purposes).\n        enabled (Optional[bool]): Item is enabled (for UI purposes).\n        hidden (Optional[bool]): DEPRECATED: Use 'visible' instead.\n        disabled (Optional[bool]): DEPRECATED: Use 'enabled' instead.\n\n    \"\"\"\n    type_attributes = []\n\n    is_value_def = True\n\n    def __init__(\n        self,\n        key: str,\n        default: Any,\n        label: Optional[str] = None,\n        tooltip: Optional[str] = None,\n        is_label_horizontal: Optional[bool] = None,\n        visible: Optional[bool] = None,\n        enabled: Optional[bool] = None,\n        hidden: Optional[bool] = None,\n        disabled: Optional[bool] = None,\n    ):\n        if is_label_horizontal is None:\n            is_label_horizontal = True\n\n        enabled = _convert_reversed_attr(\n            enabled, disabled, \"enabled\", \"disabled\", True\n        )\n        visible = _convert_reversed_attr(\n            visible, hidden, \"visible\", \"hidden\", True\n        )\n\n        self.key: str = key\n        self.label: Optional[str] = label\n        self.tooltip: Optional[str] = tooltip\n        self.default: Any = default\n        self.is_label_horizontal: bool = is_label_horizontal\n        self.visible: bool = visible\n        self.enabled: bool = enabled\n        self._id: str = uuid.uuid4().hex\n\n        self.__init__class__ = AbstractAttrDef\n\n    @property\n    def id(self) -&gt; str:\n        return self._id\n\n    def clone(self) -&gt; \"Self\":\n        data = self.serialize()\n        data.pop(\"type\")\n        return self.deserialize(data)\n\n    @property\n    def hidden(self) -&gt; bool:\n        return not self.visible\n\n    @hidden.setter\n    def hidden(self, value: bool):\n        self.visible = not value\n\n    @property\n    def disabled(self) -&gt; bool:\n        return not self.enabled\n\n    @disabled.setter\n    def disabled(self, value: bool):\n        self.enabled = not value\n\n    def __eq__(self, other: Any) -&gt; bool:\n        return self.compare_to_def(other)\n\n    def __ne__(self, other: Any) -&gt; bool:\n        return not self.compare_to_def(other)\n\n    def compare_to_def(\n        self,\n        other: Any,\n        ignore_default: Optional[bool] = False,\n        ignore_enabled: Optional[bool] = False,\n        ignore_visible: Optional[bool] = False,\n        ignore_def_type_compare: Optional[bool] = False,\n    ) -&gt; bool:\n        if not isinstance(other, self.__class__) or self.key != other.key:\n            return False\n        if not ignore_def_type_compare and not self._def_type_compare(other):\n            return False\n        return (\n            (ignore_default or self.default == other.default)\n            and (ignore_visible or self.visible == other.visible)\n            and (ignore_enabled or self.enabled == other.enabled)\n        )\n\n    @abstractmethod\n    def is_value_valid(self, value: Any) -&gt; bool:\n        \"\"\"Check if value is valid.\n\n        This should return False if value is not valid based\n            on definition type.\n\n        Args:\n            value (Any): Value to validate based on definition type.\n\n        Returns:\n            bool: True if value is valid.\n\n        \"\"\"\n        pass\n\n    @property\n    @abstractmethod\n    def type(self) -&gt; str:\n        \"\"\"Attribute definition type also used as identifier of class.\n\n        Returns:\n            str: Type of attribute definition.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def convert_value(self, value: Any) -&gt; Any:\n        \"\"\"Convert value to a valid one.\n\n        Convert passed value to a valid type. Use default if value can't be\n        converted.\n\n        \"\"\"\n        pass\n\n    def serialize(self) -&gt; Dict[str, Any]:\n        \"\"\"Serialize object to data so it's possible to recreate it.\n\n        Returns:\n            Dict[str, Any]: Serialized object that can be passed to\n                'deserialize' method.\n\n        \"\"\"\n        data = {\n            \"type\": self.type,\n            \"key\": self.key,\n            \"label\": self.label,\n            \"tooltip\": self.tooltip,\n            \"default\": self.default,\n            \"is_label_horizontal\": self.is_label_horizontal,\n            \"visible\": self.visible,\n            \"enabled\": self.enabled\n        }\n        for attr in self.type_attributes:\n            data[attr] = getattr(self, attr)\n        return data\n\n    @classmethod\n    def deserialize(cls, data: Dict[str, Any]) -&gt; \"Self\":\n        \"\"\"Recreate object from data.\n\n        Data can be received using 'serialize' method.\n        \"\"\"\n        if \"type\" in data:\n            data = dict(data)\n            data.pop(\"type\")\n\n        return cls(**data)\n\n    def _def_type_compare(self, other: \"Self\") -&gt; bool:\n        return True\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.AbstractAttrDef.type","title":"<code>type</code>  <code>abstractmethod</code> <code>property</code>","text":"<p>Attribute definition type also used as identifier of class.</p> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Type of attribute definition.</p>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.AbstractAttrDef.convert_value","title":"<code>convert_value(value)</code>  <code>abstractmethod</code>","text":"<p>Convert value to a valid one.</p> <p>Convert passed value to a valid type. Use default if value can't be converted.</p> Source code in <code>client/ayon_core/lib/attribute_definitions.py</code> <pre><code>@abstractmethod\ndef convert_value(self, value: Any) -&gt; Any:\n    \"\"\"Convert value to a valid one.\n\n    Convert passed value to a valid type. Use default if value can't be\n    converted.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.AbstractAttrDef.deserialize","title":"<code>deserialize(data)</code>  <code>classmethod</code>","text":"<p>Recreate object from data.</p> <p>Data can be received using 'serialize' method.</p> Source code in <code>client/ayon_core/lib/attribute_definitions.py</code> <pre><code>@classmethod\ndef deserialize(cls, data: Dict[str, Any]) -&gt; \"Self\":\n    \"\"\"Recreate object from data.\n\n    Data can be received using 'serialize' method.\n    \"\"\"\n    if \"type\" in data:\n        data = dict(data)\n        data.pop(\"type\")\n\n    return cls(**data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.AbstractAttrDef.is_value_valid","title":"<code>is_value_valid(value)</code>  <code>abstractmethod</code>","text":"<p>Check if value is valid.</p> <p>This should return False if value is not valid based     on definition type.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>Any</code> <p>Value to validate based on definition type.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if value is valid.</p> Source code in <code>client/ayon_core/lib/attribute_definitions.py</code> <pre><code>@abstractmethod\ndef is_value_valid(self, value: Any) -&gt; bool:\n    \"\"\"Check if value is valid.\n\n    This should return False if value is not valid based\n        on definition type.\n\n    Args:\n        value (Any): Value to validate based on definition type.\n\n    Returns:\n        bool: True if value is valid.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.AbstractAttrDef.serialize","title":"<code>serialize()</code>","text":"<p>Serialize object to data so it's possible to recreate it.</p> <p>Returns:</p> Type Description <code>Dict[str, Any]</code> <p>Dict[str, Any]: Serialized object that can be passed to 'deserialize' method.</p> Source code in <code>client/ayon_core/lib/attribute_definitions.py</code> <pre><code>def serialize(self) -&gt; Dict[str, Any]:\n    \"\"\"Serialize object to data so it's possible to recreate it.\n\n    Returns:\n        Dict[str, Any]: Serialized object that can be passed to\n            'deserialize' method.\n\n    \"\"\"\n    data = {\n        \"type\": self.type,\n        \"key\": self.key,\n        \"label\": self.label,\n        \"tooltip\": self.tooltip,\n        \"default\": self.default,\n        \"is_label_horizontal\": self.is_label_horizontal,\n        \"visible\": self.visible,\n        \"enabled\": self.enabled\n    }\n    for attr in self.type_attributes:\n        data[attr] = getattr(self, attr)\n    return data\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.AwesomeFontIcon","title":"<code>AwesomeFontIcon</code>  <code>dataclass</code>","text":"<p>               Bases: <code>IconBase</code></p> <p>Awesome Font icon.</p> Source code in <code>client/ayon_core/lib/icon_definitions.py</code> <pre><code>@dataclass\nclass AwesomeFontIcon(IconBase):\n    \"\"\"Awesome Font icon.\"\"\"\n    type: ClassVar[str] = \"awesome-font\"\n    name: str\n    color: str = field(default=DEFAULT_WEB_ICON_COLOR)\n\n    def get_unique_id(self) -&gt; str:\n        return f\"{self.type}|{self.name}|{self.color}\"\n\n    def to_data(self) -&gt; dict:\n        return {\n            \"type\": self.type,\n            \"name\": self.name,\n            \"color\": self.color,\n        }\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.BoolDef","title":"<code>BoolDef</code>","text":"<p>               Bases: <code>AbstractAttrDef</code></p> <p>Boolean representation.</p> <p>Parameters:</p> Name Type Description Default <code>default</code> <code>bool</code> <p>Default value. Set to <code>False</code> if not defined.</p> <code>None</code> Source code in <code>client/ayon_core/lib/attribute_definitions.py</code> <pre><code>class BoolDef(AbstractAttrDef):\n    \"\"\"Boolean representation.\n\n    Args:\n        default(bool): Default value. Set to `False` if not defined.\n\n    \"\"\"\n    type = \"bool\"\n\n    def __init__(self, key: str, default: Optional[bool] = None, **kwargs):\n        if default is None:\n            default = False\n        super().__init__(key, default=default, **kwargs)\n\n    def is_value_valid(self, value: Any) -&gt; bool:\n        return isinstance(value, bool)\n\n    def convert_value(self, value: Any) -&gt; bool:\n        if isinstance(value, bool):\n            return value\n        return self.default\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.ButtonDef","title":"<code>ButtonDef</code>","text":"<p>               Bases: <code>UIDef</code></p> <p>Button definition.</p> <p>UI element to allow to trigger a callback.</p> Source code in <code>client/ayon_core/lib/attribute_definitions.py</code> <pre><code>class ButtonDef(UIDef):\n    \"\"\"Button definition.\n\n    UI element to allow to trigger a callback.\n    \"\"\"\n    type = \"button\"\n\n    def __init__(\n        self,\n        key: str,\n        callback: Callable | None,\n        text: str | None = None,\n        icon: IconBase | None = None,\n        **kwargs\n    ):\n        self._callback = callback\n        if not text and not icon:\n            icon = MaterialSymbolsIcon(name=\"left_click\")\n        self.icon = icon\n        self.text = text\n        super().__init__(key=key, **kwargs)\n\n    def get_callback(self) -&gt; Callable | None:\n        return self._callback\n\n    def set_callback(self, callback: Callable) -&gt; None:\n        self._callback = callback\n\n    callback = property(get_callback, set_callback)\n\n    def trigger(self) -&gt; None:\n        self._callback()\n\n    def serialize(self) -&gt; dict[str, Any]:\n        \"\"\"Serialize object to data so it's possible to recreate it.\n\n        Serialization of button definition does not include callback function.\n            Logic after deserialization has to be handled manually.\n\n        \"\"\"\n        data = super().serialize()\n        icon_data = None\n        if self.icon is not None:\n            icon_data = self.icon.to_data()\n        data[\"callback\"] = None\n        data[\"icon\"] = icon_data\n        data[\"text\"] = self.text\n        return data\n\n    @classmethod\n    def deserialize(cls, data: dict[str, Any]) -&gt; \"Self\":\n        \"\"\"Recreate object from data.\n\n        Data can be received using 'serialize' method.\n        \"\"\"\n        icon = data[\"icon\"]\n        if isinstance(icon, dict):\n            data[\"icon\"] = get_icon_def_from_data(icon)\n\n        return cls(**data)\n\n    def clone(self) -&gt; \"Self\":\n        attr_def = super().clone()\n        attr_def.set_callback(self._callback)\n        return attr_def\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.ButtonDef.deserialize","title":"<code>deserialize(data)</code>  <code>classmethod</code>","text":"<p>Recreate object from data.</p> <p>Data can be received using 'serialize' method.</p> Source code in <code>client/ayon_core/lib/attribute_definitions.py</code> <pre><code>@classmethod\ndef deserialize(cls, data: dict[str, Any]) -&gt; \"Self\":\n    \"\"\"Recreate object from data.\n\n    Data can be received using 'serialize' method.\n    \"\"\"\n    icon = data[\"icon\"]\n    if isinstance(icon, dict):\n        data[\"icon\"] = get_icon_def_from_data(icon)\n\n    return cls(**data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.ButtonDef.serialize","title":"<code>serialize()</code>","text":"<p>Serialize object to data so it's possible to recreate it.</p> <p>Serialization of button definition does not include callback function.     Logic after deserialization has to be handled manually.</p> Source code in <code>client/ayon_core/lib/attribute_definitions.py</code> <pre><code>def serialize(self) -&gt; dict[str, Any]:\n    \"\"\"Serialize object to data so it's possible to recreate it.\n\n    Serialization of button definition does not include callback function.\n        Logic after deserialization has to be handled manually.\n\n    \"\"\"\n    data = super().serialize()\n    icon_data = None\n    if self.icon is not None:\n        icon_data = self.icon.to_data()\n    data[\"callback\"] = None\n    data[\"icon\"] = icon_data\n    data[\"text\"] = self.text\n    return data\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.CacheItem","title":"<code>CacheItem</code>","text":"<p>Simple cache item with lifetime and default factory for default value.</p> <p>Default factory should return default value that is used on init     and on reset.</p> <p>Parameters:</p> Name Type Description Default <code>default_factory</code> <code>Optional[callable]</code> <p>Function that returns default value used on init and on reset.</p> <code>None</code> <code>lifetime</code> <code>Optional[int]</code> <p>Lifetime of the cache data in seconds. Default lifetime is 120 seconds.</p> <code>None</code> Source code in <code>client/ayon_core/lib/cache.py</code> <pre><code>class CacheItem:\n    \"\"\"Simple cache item with lifetime and default factory for default value.\n\n    Default factory should return default value that is used on init\n        and on reset.\n\n    Args:\n        default_factory (Optional[callable]): Function that returns default\n            value used on init and on reset.\n        lifetime (Optional[int]): Lifetime of the cache data in seconds.\n            Default lifetime is 120 seconds.\n\n    \"\"\"\n    def __init__(self, default_factory=None, lifetime=None):\n        if lifetime is None:\n            lifetime = 120\n        self._lifetime = lifetime\n        self._last_update = None\n        if default_factory is None:\n            default_factory = _default_factory_func\n        self._default_factory = default_factory\n        self._data = default_factory()\n\n    @property\n    def is_valid(self):\n        \"\"\"Is cache valid to use.\n\n        Return:\n            bool: True if cache is valid, False otherwise.\n\n        \"\"\"\n        if self._last_update is None:\n            return False\n\n        return (time.time() - self._last_update) &lt; self._lifetime\n\n    def set_lifetime(self, lifetime):\n        \"\"\"Change lifetime of cache item.\n\n        Args:\n            lifetime (int): Lifetime of the cache data in seconds.\n        \"\"\"\n\n        self._lifetime = lifetime\n\n    def set_invalid(self):\n        \"\"\"Set cache as invalid.\"\"\"\n\n        self._last_update = None\n\n    def reset(self):\n        \"\"\"Set cache as invalid and reset data.\"\"\"\n\n        self._last_update = None\n        self._data = self._default_factory()\n\n    def get_data(self):\n        \"\"\"Receive cached data.\n\n        Returns:\n            Any: Any data that are cached.\n\n        \"\"\"\n        return self._data\n\n    def update_data(self, data):\n        \"\"\"Update cache data.\n\n        Args:\n            data (Any): Any data that are cached.\n\n        \"\"\"\n        self._data = data\n        self._last_update = time.time()\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.CacheItem.is_valid","title":"<code>is_valid</code>  <code>property</code>","text":"<p>Is cache valid to use.</p> Return <p>bool: True if cache is valid, False otherwise.</p>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.CacheItem.get_data","title":"<code>get_data()</code>","text":"<p>Receive cached data.</p> <p>Returns:</p> Name Type Description <code>Any</code> <p>Any data that are cached.</p> Source code in <code>client/ayon_core/lib/cache.py</code> <pre><code>def get_data(self):\n    \"\"\"Receive cached data.\n\n    Returns:\n        Any: Any data that are cached.\n\n    \"\"\"\n    return self._data\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.CacheItem.reset","title":"<code>reset()</code>","text":"<p>Set cache as invalid and reset data.</p> Source code in <code>client/ayon_core/lib/cache.py</code> <pre><code>def reset(self):\n    \"\"\"Set cache as invalid and reset data.\"\"\"\n\n    self._last_update = None\n    self._data = self._default_factory()\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.CacheItem.set_invalid","title":"<code>set_invalid()</code>","text":"<p>Set cache as invalid.</p> Source code in <code>client/ayon_core/lib/cache.py</code> <pre><code>def set_invalid(self):\n    \"\"\"Set cache as invalid.\"\"\"\n\n    self._last_update = None\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.CacheItem.set_lifetime","title":"<code>set_lifetime(lifetime)</code>","text":"<p>Change lifetime of cache item.</p> <p>Parameters:</p> Name Type Description Default <code>lifetime</code> <code>int</code> <p>Lifetime of the cache data in seconds.</p> required Source code in <code>client/ayon_core/lib/cache.py</code> <pre><code>def set_lifetime(self, lifetime):\n    \"\"\"Change lifetime of cache item.\n\n    Args:\n        lifetime (int): Lifetime of the cache data in seconds.\n    \"\"\"\n\n    self._lifetime = lifetime\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.CacheItem.update_data","title":"<code>update_data(data)</code>","text":"<p>Update cache data.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>Any</code> <p>Any data that are cached.</p> required Source code in <code>client/ayon_core/lib/cache.py</code> <pre><code>def update_data(self, data):\n    \"\"\"Update cache data.\n\n    Args:\n        data (Any): Any data that are cached.\n\n    \"\"\"\n    self._data = data\n    self._last_update = time.time()\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.DefaultKeysDict","title":"<code>DefaultKeysDict</code>","text":"<p>               Bases: <code>dict</code></p> <p>Dictionary that supports the default key to use for str conversion.</p> <p>Is helpful for changes of a key in a template from string to dictionary     for example '{folder}' -&gt; '{folder[name]}'.     &gt;&gt;&gt; data = DefaultKeysDict(     &gt;&gt;&gt;     \"name\",     &gt;&gt;&gt;     {\"folder\": {\"name\": \"FolderName\"}}     &gt;&gt;&gt; )     &gt;&gt;&gt; print(\"{folder[name]}\".format_map(data))     FolderName     &gt;&gt;&gt; print(\"{folder}\".format_map(data))     FolderName</p> <p>Parameters:</p> Name Type Description Default <code>default_key</code> <code>Union[str, Iterable[str]]</code> <p>Default key to use for str conversion. Can also expect multiple keys for more nested dictionary.</p> required Source code in <code>client/ayon_core/lib/path_templates.py</code> <pre><code>class DefaultKeysDict(dict):\n    \"\"\"Dictionary that supports the default key to use for str conversion.\n\n    Is helpful for changes of a key in a template from string to dictionary\n        for example '{folder}' -&gt; '{folder[name]}'.\n        &gt;&gt;&gt; data = DefaultKeysDict(\n        &gt;&gt;&gt;     \"name\",\n        &gt;&gt;&gt;     {\"folder\": {\"name\": \"FolderName\"}}\n        &gt;&gt;&gt; )\n        &gt;&gt;&gt; print(\"{folder[name]}\".format_map(data))\n        FolderName\n        &gt;&gt;&gt; print(\"{folder}\".format_map(data))\n        FolderName\n\n    Args:\n        default_key (Union[str, Iterable[str]]): Default key to use for str\n            conversion. Can also expect multiple keys for more nested\n            dictionary.\n\n    \"\"\"\n    def __init__(\n        self, default_keys: Union[str, Iterable[str]], *args, **kwargs\n    ) -&gt; None:\n        if isinstance(default_keys, str):\n            default_keys = [default_keys]\n        else:\n            default_keys = list(default_keys)\n        if not default_keys:\n            raise ValueError(\n                \"Default key must be set. Got empty default keys.\"\n            )\n\n        self._default_keys = default_keys\n        super().__init__(*args, **kwargs)\n\n    def __str__(self) -&gt; str:\n        return str(self.get_default_value())\n\n    def get_default_keys(self) -&gt; list[str]:\n        return list(self._default_keys)\n\n    def get_default_value(self) -&gt; Any:\n        value = self\n        for key in self._default_keys:\n            value = value[key]\n        return value\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.EnumDef","title":"<code>EnumDef</code>","text":"<p>               Bases: <code>AbstractAttrDef</code></p> <p>Enumeration of items.</p> <p>Enumeration of single item from items. Or list of items if multiselection is enabled.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>Key under which value is stored.</p> required <code>items</code> <code>EnumItemsInputType</code> <p>Items definition that can be converted using 'prepare_enum_items'.</p> required <code>default</code> <code>Optional[Any]</code> <p>Default value. Must be one key(value) from passed items or list of values for multiselection.</p> <code>None</code> <code>multiselection</code> <code>Optional[bool]</code> <p>If True, multiselection is allowed. Output is list of selected items.</p> <code>False</code> <code>placeholder</code> <code>Optional[str]</code> <p>Placeholder for UI purposes, only for multiselection enumeration.</p> <code>None</code> Source code in <code>client/ayon_core/lib/attribute_definitions.py</code> <pre><code>class EnumDef(AbstractAttrDef):\n    \"\"\"Enumeration of items.\n\n    Enumeration of single item from items. Or list of items if multiselection\n    is enabled.\n\n    Args:\n        key (str): Key under which value is stored.\n        items (EnumItemsInputType): Items definition that can be converted\n            using 'prepare_enum_items'.\n        default (Optional[Any]): Default value. Must be one key(value) from\n            passed items or list of values for multiselection.\n        multiselection (Optional[bool]): If True, multiselection is allowed.\n            Output is list of selected items.\n        placeholder (Optional[str]): Placeholder for UI purposes, only for\n            multiselection enumeration.\n\n    \"\"\"\n    type = \"enum\"\n\n    type_attributes = [\n        \"multiselection\",\n        \"placeholder\",\n    ]\n\n    def __init__(\n        self,\n        key: str,\n        items: \"EnumItemsInputType\",\n        default: \"Union[str, List[Any]]\" = None,\n        multiselection: Optional[bool] = False,\n        placeholder: Optional[str] = None,\n        **kwargs\n    ):\n        if multiselection is None:\n            multiselection = False\n\n        if not items and not multiselection:\n            raise ValueError(\n                f\"Empty 'items' value. {self.__class__.__name__} must have\"\n                \" defined values on initialization.\"\n            )\n\n        items = self.prepare_enum_items(items)\n        item_values = [item[\"value\"] for item in items]\n        item_values_set = set(item_values)\n\n        if multiselection:\n            if default is None:\n                default = []\n            default = list(item_values_set.intersection(default))\n\n        elif default not in item_values:\n            default = next(iter(item_values), None)\n\n        super().__init__(key, default=default, **kwargs)\n\n        self.items: List[\"EnumItemDict\"] = items\n        self._item_values: Set[Any] = item_values_set\n        self.multiselection: bool = multiselection\n        self.placeholder: Optional[str] = placeholder\n\n    def convert_value(self, value):\n        if not self.multiselection:\n            if value in self._item_values:\n                return value\n            return self.default\n\n        if value is None:\n            return copy.deepcopy(self.default)\n        return [\n            v\n            for v in value\n            if v in self._item_values\n        ]\n\n    def is_value_valid(self, value: Any) -&gt; bool:\n        \"\"\"Check if item is available in possible values.\"\"\"\n        if isinstance(value, list):\n            if not self.multiselection:\n                return False\n            return all(value in self._item_values for value in value)\n\n        if self.multiselection:\n            return False\n        return value in self._item_values\n\n    def serialize(self):\n        data = super().serialize()\n        data[\"items\"] = copy.deepcopy(self.items)\n        return data\n\n    @staticmethod\n    def prepare_enum_items(\n        items: \"EnumItemsInputType\"\n    ) -&gt; List[\"EnumItemDict\"]:\n        \"\"\"Convert items to unified structure.\n\n        Output is a list where each item is dictionary with 'value'\n        and 'label'.\n\n        ```python\n        # Example output\n        [\n            {\"label\": \"Option 1\", \"value\": 1},\n            {\"label\": \"Option 2\", \"value\": 2},\n            {\"label\": \"Option 3\", \"value\": 3}\n        ]\n        ```\n\n        Args:\n            items (EnumItemsInputType): The items to convert.\n\n        Returns:\n            List[EnumItemDict]: Unified structure of items.\n\n        \"\"\"\n        output = []\n        if isinstance(items, dict):\n            for value, label in items.items():\n                output.append({\"label\": label, \"value\": value})\n\n        elif isinstance(items, (tuple, list, set)):\n            for item in items:\n                if isinstance(item, dict):\n                    # Validate if 'value' is available\n                    if \"value\" not in item:\n                        raise KeyError(\"Item does not contain 'value' key.\")\n\n                    if \"label\" not in item:\n                        item[\"label\"] = str(item[\"value\"])\n                elif isinstance(item, (list, tuple)):\n                    if len(item) == 2:\n                        value, label = item\n                    elif len(item) == 1:\n                        value = item[0]\n                        label = str(value)\n                    else:\n                        raise ValueError((\n                            \"Invalid items count {}.\"\n                            \" Expected 1 or 2. Value: {}\"\n                        ).format(len(item), str(item)))\n\n                    item = {\"label\": label, \"value\": value}\n                else:\n                    item = {\"label\": str(item), \"value\": item}\n                output.append(item)\n\n        else:\n            raise TypeError(\n                \"Unknown type for enum items '{}'\".format(type(items))\n            )\n\n        return output\n\n    def _def_type_compare(self, other: \"EnumDef\") -&gt; bool:\n        return (\n            self.items == other.items\n            and self.multiselection == other.multiselection\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.EnumDef.is_value_valid","title":"<code>is_value_valid(value)</code>","text":"<p>Check if item is available in possible values.</p> Source code in <code>client/ayon_core/lib/attribute_definitions.py</code> <pre><code>def is_value_valid(self, value: Any) -&gt; bool:\n    \"\"\"Check if item is available in possible values.\"\"\"\n    if isinstance(value, list):\n        if not self.multiselection:\n            return False\n        return all(value in self._item_values for value in value)\n\n    if self.multiselection:\n        return False\n    return value in self._item_values\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.EnumDef.prepare_enum_items","title":"<code>prepare_enum_items(items)</code>  <code>staticmethod</code>","text":"<p>Convert items to unified structure.</p> <p>Output is a list where each item is dictionary with 'value' and 'label'.</p> <pre><code># Example output\n[\n    {\"label\": \"Option 1\", \"value\": 1},\n    {\"label\": \"Option 2\", \"value\": 2},\n    {\"label\": \"Option 3\", \"value\": 3}\n]\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>items</code> <code>EnumItemsInputType</code> <p>The items to convert.</p> required <p>Returns:</p> Type Description <code>List['EnumItemDict']</code> <p>List[EnumItemDict]: Unified structure of items.</p> Source code in <code>client/ayon_core/lib/attribute_definitions.py</code> <pre><code>@staticmethod\ndef prepare_enum_items(\n    items: \"EnumItemsInputType\"\n) -&gt; List[\"EnumItemDict\"]:\n    \"\"\"Convert items to unified structure.\n\n    Output is a list where each item is dictionary with 'value'\n    and 'label'.\n\n    ```python\n    # Example output\n    [\n        {\"label\": \"Option 1\", \"value\": 1},\n        {\"label\": \"Option 2\", \"value\": 2},\n        {\"label\": \"Option 3\", \"value\": 3}\n    ]\n    ```\n\n    Args:\n        items (EnumItemsInputType): The items to convert.\n\n    Returns:\n        List[EnumItemDict]: Unified structure of items.\n\n    \"\"\"\n    output = []\n    if isinstance(items, dict):\n        for value, label in items.items():\n            output.append({\"label\": label, \"value\": value})\n\n    elif isinstance(items, (tuple, list, set)):\n        for item in items:\n            if isinstance(item, dict):\n                # Validate if 'value' is available\n                if \"value\" not in item:\n                    raise KeyError(\"Item does not contain 'value' key.\")\n\n                if \"label\" not in item:\n                    item[\"label\"] = str(item[\"value\"])\n            elif isinstance(item, (list, tuple)):\n                if len(item) == 2:\n                    value, label = item\n                elif len(item) == 1:\n                    value = item[0]\n                    label = str(value)\n                else:\n                    raise ValueError((\n                        \"Invalid items count {}.\"\n                        \" Expected 1 or 2. Value: {}\"\n                    ).format(len(item), str(item)))\n\n                item = {\"label\": label, \"value\": value}\n            else:\n                item = {\"label\": str(item), \"value\": item}\n            output.append(item)\n\n    else:\n        raise TypeError(\n            \"Unknown type for enum items '{}'\".format(type(items))\n        )\n\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.FileDef","title":"<code>FileDef</code>","text":"<p>               Bases: <code>AbstractAttrDef</code></p> <p>File definition. It is possible to define filters of allowed file extensions and if supports folders. Args:     single_item(bool): Allow only single path item.     folders(bool): Allow folder paths.     extensions(List[str]): Allow files with extensions. Empty list will         allow all extensions and None will disable files completely.     extensions_label(str): Custom label shown instead of extensions in UI.     default(str, List[str]): Default value.</p> Source code in <code>client/ayon_core/lib/attribute_definitions.py</code> <pre><code>class FileDef(AbstractAttrDef):\n    \"\"\"File definition.\n    It is possible to define filters of allowed file extensions and if supports\n    folders.\n    Args:\n        single_item(bool): Allow only single path item.\n        folders(bool): Allow folder paths.\n        extensions(List[str]): Allow files with extensions. Empty list will\n            allow all extensions and None will disable files completely.\n        extensions_label(str): Custom label shown instead of extensions in UI.\n        default(str, List[str]): Default value.\n    \"\"\"\n\n    type = \"path\"\n    type_attributes = [\n        \"single_item\",\n        \"folders\",\n        \"extensions\",\n        \"allow_sequences\",\n        \"extensions_label\",\n    ]\n\n    def __init__(\n        self,\n        key: str,\n        single_item: Optional[bool] = True,\n        folders: Optional[bool] = None,\n        extensions: Optional[Iterable[str]] = None,\n        allow_sequences: Optional[bool] = True,\n        extensions_label: Optional[str] = None,\n        default: Optional[\"Union[FileDefItemDict, List[str]]\"] = None,\n        **kwargs\n    ):\n        if folders is None and extensions is None:\n            folders = True\n            extensions = []\n\n        if default is None:\n            if single_item:\n                default = FileDefItem.create_empty_item().to_dict()\n            else:\n                default = []\n        else:\n            if single_item:\n                if isinstance(default, dict):\n                    FileDefItem.from_dict(default)\n\n                elif isinstance(default, str):\n                    default = FileDefItem.from_paths(\n                        [default.strip()], allow_sequences\n                    )[0]\n\n                else:\n                    raise TypeError((\n                        \"'default' argument must be 'str' or 'dict' not '{}'\"\n                    ).format(type(default)))\n\n            else:\n                if not isinstance(default, (tuple, list, set)):\n                    raise TypeError((\n                        \"'default' argument must be 'list', 'tuple' or 'set'\"\n                        \", not '{}'\"\n                    ).format(type(default)))\n\n        # Change horizontal label\n        is_label_horizontal = kwargs.get(\"is_label_horizontal\")\n        if is_label_horizontal is None:\n            kwargs[\"is_label_horizontal\"] = False\n\n        self.single_item: bool = single_item\n        self.folders: bool = folders\n        self.extensions: Set[str] = set(extensions)\n        self.allow_sequences: bool = allow_sequences\n        self.extensions_label: Optional[str] = extensions_label\n        super().__init__(key, default=default, **kwargs)\n\n    def __eq__(self, other: Any) -&gt; bool:\n        if not super().__eq__(other):\n            return False\n\n        return (\n            self.single_item == other.single_item\n            and self.folders == other.folders\n            and self.extensions == other.extensions\n            and self.allow_sequences == other.allow_sequences\n        )\n\n    def is_value_valid(self, value: Any) -&gt; bool:\n        if self.single_item:\n            if not isinstance(value, dict):\n                return False\n            try:\n                FileDefItem.from_dict(value)\n                return True\n            except (ValueError, KeyError):\n                return False\n\n        if not isinstance(value, list):\n            return False\n\n        for item in value:\n            if not isinstance(item, dict):\n                return False\n\n            try:\n                FileDefItem.from_dict(item)\n            except (ValueError, KeyError):\n                return False\n        return True\n\n    def convert_value(\n        self, value: Any\n    ) -&gt; \"Union[FileDefItemDict, List[FileDefItemDict]]\":\n        if isinstance(value, (str, dict)):\n            value = [value]\n\n        if isinstance(value, (tuple, list, set)):\n            string_paths = []\n            dict_items = []\n            for item in value:\n                if isinstance(item, str):\n                    string_paths.append(item.strip())\n                elif isinstance(item, dict):\n                    try:\n                        FileDefItem.from_dict(item)\n                        dict_items.append(item)\n                    except (ValueError, KeyError):\n                        pass\n\n            if string_paths:\n                file_items = FileDefItem.from_paths(\n                    string_paths, self.allow_sequences\n                )\n                dict_items.extend([\n                    file_item.to_dict()\n                    for file_item in file_items\n                ])\n\n            if not self.single_item:\n                return dict_items\n\n            if not dict_items:\n                return self.default\n            return dict_items[0]\n\n        if self.single_item:\n            return FileDefItem.create_empty_item().to_dict()\n        return []\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.FileDefItem","title":"<code>FileDefItem</code>","text":"Source code in <code>client/ayon_core/lib/attribute_definitions.py</code> <pre><code>class FileDefItem:\n    def __init__(\n        self,\n        directory: str,\n        filenames: List[str],\n        frames: Optional[List[int]] = None,\n        template: Optional[str] = None,\n    ):\n        self.directory = directory\n\n        self.filenames = []\n        self.is_sequence = False\n        self.template = None\n        self.frames = []\n        self.is_empty = True\n\n        self.set_filenames(filenames, frames, template)\n\n    def __str__(self):\n        return json.dumps(self.to_dict())\n\n    def __repr__(self):\n        if self.is_empty:\n            filename = \"&lt; empty &gt;\"\n        elif self.is_sequence:\n            filename = self.template\n        else:\n            filename = self.filenames[0]\n\n        return \"&lt;{}: \\\"{}\\\"&gt;\".format(\n            self.__class__.__name__,\n            os.path.join(self.directory, filename)\n        )\n\n    @property\n    def label(self) -&gt; Optional[str]:\n        if self.is_empty:\n            return None\n\n        if not self.is_sequence:\n            return self.filenames[0]\n\n        frame_start = self.frames[0]\n        filename_template = os.path.basename(self.template)\n        if len(self.frames) == 1:\n            return \"{} [{}]\".format(filename_template, frame_start)\n\n        frame_end = self.frames[-1]\n        expected_len = (frame_end - frame_start) + 1\n        if expected_len == len(self.frames):\n            return \"{} [{}-{}]\".format(\n                filename_template, frame_start, frame_end\n            )\n\n        ranges = []\n        _frame_start = None\n        _frame_end = None\n        for frame in range(frame_start, frame_end + 1):\n            if frame not in self.frames:\n                add_to_ranges = _frame_start is not None\n            elif _frame_start is None:\n                _frame_start = _frame_end = frame\n                add_to_ranges = frame == frame_end\n            else:\n                _frame_end = frame\n                add_to_ranges = frame == frame_end\n\n            if add_to_ranges:\n                if _frame_start != _frame_end:\n                    _range = \"{}-{}\".format(_frame_start, _frame_end)\n                else:\n                    _range = str(_frame_start)\n                ranges.append(_range)\n                _frame_start = _frame_end = None\n        return \"{} [{}]\".format(\n            filename_template, \",\".join(ranges)\n        )\n\n    def split_sequence(self) -&gt; List[\"Self\"]:\n        if not self.is_sequence:\n            raise ValueError(\"Cannot split single file item\")\n\n        paths = [\n            os.path.join(self.directory, filename)\n            for filename in self.filenames\n        ]\n        return self.from_paths(paths, False)\n\n    @property\n    def ext(self) -&gt; Optional[str]:\n        if self.is_empty:\n            return None\n        _, ext = os.path.splitext(self.filenames[0])\n        if ext:\n            return ext\n        return None\n\n    @property\n    def lower_ext(self) -&gt; Optional[str]:\n        ext = self.ext\n        if ext is not None:\n            return ext.lower()\n        return ext\n\n    @property\n    def is_dir(self) -&gt; bool:\n        if self.is_empty:\n            return False\n\n        # QUESTION a better way how to define folder (in init argument?)\n        if self.ext:\n            return False\n        return True\n\n    def set_directory(self, directory: str):\n        self.directory = directory\n\n    def set_filenames(\n        self,\n        filenames: List[str],\n        frames: Optional[List[int]] = None,\n        template: Optional[str] = None,\n    ):\n        if frames is None:\n            frames = []\n        is_sequence = False\n        if frames:\n            is_sequence = True\n\n        if is_sequence and not template:\n            raise ValueError(\"Missing template for sequence\")\n\n        self.is_empty = len(filenames) == 0\n        self.filenames = filenames\n        self.template = template\n        self.frames = frames\n        self.is_sequence = is_sequence\n\n    @classmethod\n    def create_empty_item(cls) -&gt; \"Self\":\n        return cls(\"\", \"\")\n\n    @classmethod\n    def from_value(\n        cls,\n        value: \"Union[List[FileDefItemDict], FileDefItemDict]\",\n        allow_sequences: bool,\n    ) -&gt; List[\"Self\"]:\n        \"\"\"Convert passed value to FileDefItem objects.\n\n        Returns:\n            list: Created FileDefItem objects.\n\n        \"\"\"\n        # Convert single item to iterable\n        if not isinstance(value, (list, tuple, set)):\n            value = [value]\n\n        output = []\n        str_filepaths = []\n        for item in value:\n            if isinstance(item, dict):\n                item = cls.from_dict(item)\n\n            if isinstance(item, FileDefItem):\n                if not allow_sequences and item.is_sequence:\n                    output.extend(item.split_sequence())\n                else:\n                    output.append(item)\n\n            elif isinstance(item, str):\n                str_filepaths.append(item)\n            else:\n                raise TypeError(\n                    \"Unknown type \\\"{}\\\". Can't convert to {}\".format(\n                        str(type(item)), cls.__name__\n                    )\n                )\n\n        if str_filepaths:\n            output.extend(cls.from_paths(str_filepaths, allow_sequences))\n\n        return output\n\n    @classmethod\n    def from_dict(cls, data: \"FileDefItemDict\") -&gt; \"Self\":\n        return cls(\n            data[\"directory\"],\n            data[\"filenames\"],\n            data.get(\"frames\"),\n            data.get(\"template\")\n        )\n\n    @classmethod\n    def from_paths(\n        cls,\n        paths: List[str],\n        allow_sequences: bool,\n    ) -&gt; List[\"Self\"]:\n        filenames_by_dir = collections.defaultdict(list)\n        for path in paths:\n            normalized = os.path.normpath(path)\n            directory, filename = os.path.split(normalized)\n            filenames_by_dir[directory].append(filename)\n\n        output = []\n        for directory, filenames in filenames_by_dir.items():\n            if allow_sequences:\n                cols, remainders = clique.assemble(filenames)\n            else:\n                cols = []\n                remainders = filenames\n\n            for remainder in remainders:\n                output.append(cls(directory, [remainder]))\n\n            for col in cols:\n                frames = list(col.indexes)\n                paths = [filename for filename in col]\n                template = col.format(\"{head}{padding}{tail}\")\n\n                output.append(cls(\n                    directory, paths, frames, template\n                ))\n\n        return output\n\n    def to_dict(self) -&gt; \"FileDefItemDict\":\n        output = {\n            \"is_sequence\": self.is_sequence,\n            \"directory\": self.directory,\n            \"filenames\": list(self.filenames),\n        }\n        if self.is_sequence:\n            output.update({\n                \"template\": self.template,\n                \"frames\": list(sorted(self.frames)),\n            })\n\n        return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.FileDefItem.from_value","title":"<code>from_value(value, allow_sequences)</code>  <code>classmethod</code>","text":"<p>Convert passed value to FileDefItem objects.</p> <p>Returns:</p> Name Type Description <code>list</code> <code>List['Self']</code> <p>Created FileDefItem objects.</p> Source code in <code>client/ayon_core/lib/attribute_definitions.py</code> <pre><code>@classmethod\ndef from_value(\n    cls,\n    value: \"Union[List[FileDefItemDict], FileDefItemDict]\",\n    allow_sequences: bool,\n) -&gt; List[\"Self\"]:\n    \"\"\"Convert passed value to FileDefItem objects.\n\n    Returns:\n        list: Created FileDefItem objects.\n\n    \"\"\"\n    # Convert single item to iterable\n    if not isinstance(value, (list, tuple, set)):\n        value = [value]\n\n    output = []\n    str_filepaths = []\n    for item in value:\n        if isinstance(item, dict):\n            item = cls.from_dict(item)\n\n        if isinstance(item, FileDefItem):\n            if not allow_sequences and item.is_sequence:\n                output.extend(item.split_sequence())\n            else:\n                output.append(item)\n\n        elif isinstance(item, str):\n            str_filepaths.append(item)\n        else:\n            raise TypeError(\n                \"Unknown type \\\"{}\\\". Can't convert to {}\".format(\n                    str(type(item)), cls.__name__\n                )\n            )\n\n    if str_filepaths:\n        output.extend(cls.from_paths(str_filepaths, allow_sequences))\n\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.FormatObject","title":"<code>FormatObject</code>","text":"<p>Object that can be used for formatting.</p> <p>This is base that is valid for to be used in 'StringTemplate' value.</p> Source code in <code>client/ayon_core/lib/path_templates.py</code> <pre><code>class FormatObject:\n    \"\"\"Object that can be used for formatting.\n\n    This is base that is valid for to be used in 'StringTemplate' value.\n    \"\"\"\n    def __init__(self):\n        self.value = \"\"\n\n    def __format__(self, *args, **kwargs):\n        return self.value.__format__(*args, **kwargs)\n\n    def __str__(self) -&gt; str:\n        return str(self.value)\n\n    def __repr__(self) -&gt; str:\n        return self.__str__()\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.IconBase","title":"<code>IconBase</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Base class for icon definition.</p> <p>The base class is meant to be used for type validation of icon definition     passed to functions.</p> Source code in <code>client/ayon_core/lib/icon_definitions.py</code> <pre><code>class IconBase(ABC):\n    \"\"\"Base class for icon definition.\n\n    The base class is meant to be used for type validation of icon definition\n        passed to functions.\n    \"\"\"\n\n    @property\n    @abstractmethod\n    def type(self) -&gt; str:\n        pass\n\n    @abstractmethod\n    def get_unique_id(self) -&gt; str:\n        pass\n\n    @abstractmethod\n    def to_data(self) -&gt; dict:\n        pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.IniSettingRegistry","title":"<code>IniSettingRegistry</code>","text":"<p>               Bases: <code>ASettingRegistry</code></p> <p>Class using :mod:<code>configparser</code>.</p> <p>This class is using :mod:<code>configparser</code> (ini) files to store items.</p> Source code in <code>client/ayon_core/lib/local_settings.py</code> <pre><code>class IniSettingRegistry(ASettingRegistry):\n    \"\"\"Class using :mod:`configparser`.\n\n    This class is using :mod:`configparser` (ini) files to store items.\n\n    \"\"\"\n\n    def __init__(self, name: str, path: str):\n        super().__init__(name)\n        # get registry file\n        filepath = os.path.join(path, f\"{name}.ini\")\n        os.makedirs(os.path.dirname(filepath), exist_ok=True)\n        if not os.path.exists(filepath):\n            with open(filepath, mode=\"w\") as stream:\n                print(\"# Settings registry\", stream)\n                now = datetime.now().strftime(\"%d/%m/%Y %H:%M:%S\")\n                print(f\"# {now}\", stream)\n\n        self._registry_file = filepath\n\n    @property\n    def filepath(self) -&gt; str:\n        return self._registry_file\n\n    def set_item_section(self, section: str, name: str, value: str) -&gt; None:\n        \"\"\"Set item to specific section of ini registry.\n\n        If section doesn't exists, it is created.\n\n        Args:\n            section (str): Name of section.\n            name (str): Name of the item.\n            value (str): Value of the item.\n\n        \"\"\"\n        value = str(value)\n        config = configparser.ConfigParser()\n\n        config.read(self._registry_file)\n        if not config.has_section(section):\n            config.add_section(section)\n        current = config[section]\n        current[name] = value\n\n        with open(self._registry_file, mode=\"w\") as stream:\n            config.write(stream)\n\n    def _set_item(self, name: str, value: str) -&gt; None:\n        self.set_item_section(\"MAIN\", name, value)\n\n    def set_item(self, name: str, value: str) -&gt; None:\n        \"\"\"Set item to settings ini file.\n\n        This saves item to ``DEFAULT`` section of ini as each item there\n        must reside in some section.\n\n        Args:\n            name (str): Name of the item.\n            value (str): Value of the item.\n\n        \"\"\"\n        # this does the some, overridden just for different docstring.\n        # we cast value to str as ini options values must be strings.\n        super().set_item(name, str(value))\n\n    def get_item(self, name: str, default: Any = _PLACEHOLDER) -&gt; str:\n        \"\"\"Gets item from settings ini file.\n\n        This gets settings from ``DEFAULT`` section of ini file as each item\n        there must reside in some section.\n\n        Args:\n            name (str): Name of the item.\n            default (Any): Default value if item is not available.\n\n        Returns:\n            str: Value of item.\n\n        Raises:\n            RegistryItemNotFound: If value doesn't exist.\n\n        \"\"\"\n        return super().get_item(name, default)\n\n    @lru_cache(maxsize=32)\n    def get_item_from_section(\n        self, section: str, name: str, default: Any = _PLACEHOLDER\n    ) -&gt; Optional[str]:\n        \"\"\"Get item from section of ini file.\n\n        This will read ini file and try to get item value from specified\n        section. If that section or item doesn't exist,\n        :exc:`RegistryItemNotFound` is risen.\n\n        Args:\n            section (str): Name of ini section.\n            name (str): Name of the item.\n\n        Returns:\n            str: Item value.\n\n        Raises:\n            RegistryItemNotFound: If value doesn't exist.\n\n        \"\"\"\n        config = configparser.ConfigParser()\n        config.read(self._registry_file)\n        try:\n            value = config[section][name]\n        except KeyError:\n            if default is not _PLACEHOLDER:\n                return default\n            raise RegistryItemNotFound(\n                f\"Registry doesn't contain value {section}:{name}\"\n            )\n        return value\n\n    def _get_item(\n        self, name: str, default: Any = _PLACEHOLDER\n    ) -&gt; Any:\n        return self.get_item_from_section(\"MAIN\", name, default)\n\n    def delete_item_from_section(\n        self,\n        section: str,\n        name: str,\n        ignore_missing: bool = True,\n    ):\n        \"\"\"Delete item from section in ini file.\n\n        Args:\n            section (str): Section name.\n            name (str): Name of the item.\n            ignore_missing (bool): If True, no error is raised if item doesn't\n                exist.\n\n        Raises:\n            RegistryItemNotFound: If the item doesn't exist.\n\n        \"\"\"\n        self.get_item_from_section.cache_clear()\n        config = configparser.ConfigParser()\n        config.read(self._registry_file)\n        try:\n            _ = config[section][name]\n        except KeyError:\n            if ignore_missing:\n                return\n            raise RegistryItemNotFound(\n                f\"Registry doesn't contain value {section}:{name}\"\n            )\n        config.remove_option(section, name)\n\n        # if section is empty, delete it\n        if len(config[section].keys()) == 0:\n            config.remove_section(section)\n\n        with open(self._registry_file, mode=\"w\") as stream:\n            config.write(stream)\n\n    def _delete_item(self, name: str, ignore_missing: bool):\n        \"\"\"Delete item from default section.\"\"\"\n        self.delete_item_from_section(\"MAIN\", name, ignore_missing)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.IniSettingRegistry.delete_item_from_section","title":"<code>delete_item_from_section(section, name, ignore_missing=True)</code>","text":"<p>Delete item from section in ini file.</p> <p>Parameters:</p> Name Type Description Default <code>section</code> <code>str</code> <p>Section name.</p> required <code>name</code> <code>str</code> <p>Name of the item.</p> required <code>ignore_missing</code> <code>bool</code> <p>If True, no error is raised if item doesn't exist.</p> <code>True</code> <p>Raises:</p> Type Description <code>RegistryItemNotFound</code> <p>If the item doesn't exist.</p> Source code in <code>client/ayon_core/lib/local_settings.py</code> <pre><code>def delete_item_from_section(\n    self,\n    section: str,\n    name: str,\n    ignore_missing: bool = True,\n):\n    \"\"\"Delete item from section in ini file.\n\n    Args:\n        section (str): Section name.\n        name (str): Name of the item.\n        ignore_missing (bool): If True, no error is raised if item doesn't\n            exist.\n\n    Raises:\n        RegistryItemNotFound: If the item doesn't exist.\n\n    \"\"\"\n    self.get_item_from_section.cache_clear()\n    config = configparser.ConfigParser()\n    config.read(self._registry_file)\n    try:\n        _ = config[section][name]\n    except KeyError:\n        if ignore_missing:\n            return\n        raise RegistryItemNotFound(\n            f\"Registry doesn't contain value {section}:{name}\"\n        )\n    config.remove_option(section, name)\n\n    # if section is empty, delete it\n    if len(config[section].keys()) == 0:\n        config.remove_section(section)\n\n    with open(self._registry_file, mode=\"w\") as stream:\n        config.write(stream)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.IniSettingRegistry.get_item","title":"<code>get_item(name, default=_PLACEHOLDER)</code>","text":"<p>Gets item from settings ini file.</p> <p>This gets settings from <code>DEFAULT</code> section of ini file as each item there must reside in some section.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name of the item.</p> required <code>default</code> <code>Any</code> <p>Default value if item is not available.</p> <code>_PLACEHOLDER</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Value of item.</p> <p>Raises:</p> Type Description <code>RegistryItemNotFound</code> <p>If value doesn't exist.</p> Source code in <code>client/ayon_core/lib/local_settings.py</code> <pre><code>def get_item(self, name: str, default: Any = _PLACEHOLDER) -&gt; str:\n    \"\"\"Gets item from settings ini file.\n\n    This gets settings from ``DEFAULT`` section of ini file as each item\n    there must reside in some section.\n\n    Args:\n        name (str): Name of the item.\n        default (Any): Default value if item is not available.\n\n    Returns:\n        str: Value of item.\n\n    Raises:\n        RegistryItemNotFound: If value doesn't exist.\n\n    \"\"\"\n    return super().get_item(name, default)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.IniSettingRegistry.get_item_from_section","title":"<code>get_item_from_section(section, name, default=_PLACEHOLDER)</code>  <code>cached</code>","text":"<p>Get item from section of ini file.</p> <p>This will read ini file and try to get item value from specified section. If that section or item doesn't exist, :exc:<code>RegistryItemNotFound</code> is risen.</p> <p>Parameters:</p> Name Type Description Default <code>section</code> <code>str</code> <p>Name of ini section.</p> required <code>name</code> <code>str</code> <p>Name of the item.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>Optional[str]</code> <p>Item value.</p> <p>Raises:</p> Type Description <code>RegistryItemNotFound</code> <p>If value doesn't exist.</p> Source code in <code>client/ayon_core/lib/local_settings.py</code> <pre><code>@lru_cache(maxsize=32)\ndef get_item_from_section(\n    self, section: str, name: str, default: Any = _PLACEHOLDER\n) -&gt; Optional[str]:\n    \"\"\"Get item from section of ini file.\n\n    This will read ini file and try to get item value from specified\n    section. If that section or item doesn't exist,\n    :exc:`RegistryItemNotFound` is risen.\n\n    Args:\n        section (str): Name of ini section.\n        name (str): Name of the item.\n\n    Returns:\n        str: Item value.\n\n    Raises:\n        RegistryItemNotFound: If value doesn't exist.\n\n    \"\"\"\n    config = configparser.ConfigParser()\n    config.read(self._registry_file)\n    try:\n        value = config[section][name]\n    except KeyError:\n        if default is not _PLACEHOLDER:\n            return default\n        raise RegistryItemNotFound(\n            f\"Registry doesn't contain value {section}:{name}\"\n        )\n    return value\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.IniSettingRegistry.set_item","title":"<code>set_item(name, value)</code>","text":"<p>Set item to settings ini file.</p> <p>This saves item to <code>DEFAULT</code> section of ini as each item there must reside in some section.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name of the item.</p> required <code>value</code> <code>str</code> <p>Value of the item.</p> required Source code in <code>client/ayon_core/lib/local_settings.py</code> <pre><code>def set_item(self, name: str, value: str) -&gt; None:\n    \"\"\"Set item to settings ini file.\n\n    This saves item to ``DEFAULT`` section of ini as each item there\n    must reside in some section.\n\n    Args:\n        name (str): Name of the item.\n        value (str): Value of the item.\n\n    \"\"\"\n    # this does the some, overridden just for different docstring.\n    # we cast value to str as ini options values must be strings.\n    super().set_item(name, str(value))\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.IniSettingRegistry.set_item_section","title":"<code>set_item_section(section, name, value)</code>","text":"<p>Set item to specific section of ini registry.</p> <p>If section doesn't exists, it is created.</p> <p>Parameters:</p> Name Type Description Default <code>section</code> <code>str</code> <p>Name of section.</p> required <code>name</code> <code>str</code> <p>Name of the item.</p> required <code>value</code> <code>str</code> <p>Value of the item.</p> required Source code in <code>client/ayon_core/lib/local_settings.py</code> <pre><code>def set_item_section(self, section: str, name: str, value: str) -&gt; None:\n    \"\"\"Set item to specific section of ini registry.\n\n    If section doesn't exists, it is created.\n\n    Args:\n        section (str): Name of section.\n        name (str): Name of the item.\n        value (str): Value of the item.\n\n    \"\"\"\n    value = str(value)\n    config = configparser.ConfigParser()\n\n    config.read(self._registry_file)\n    if not config.has_section(section):\n        config.add_section(section)\n    current = config[section]\n    current[name] = value\n\n    with open(self._registry_file, mode=\"w\") as stream:\n        config.write(stream)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.JSONSettingRegistry","title":"<code>JSONSettingRegistry</code>","text":"<p>               Bases: <code>ASettingRegistry</code></p> <p>Class using a json file as storage.</p> Source code in <code>client/ayon_core/lib/local_settings.py</code> <pre><code>class JSONSettingRegistry(ASettingRegistry):\n    \"\"\"Class using a json file as storage.\"\"\"\n\n    def __init__(self, name: str, path: str) -&gt; None:\n        super().__init__(name)\n        self._registry_file = os.path.join(path, f\"{name}.json\")\n        now = datetime.now().strftime(\"%d/%m/%Y %H:%M:%S\")\n        header = {\n            \"__metadata__\": {\"generated\": now},\n            \"registry\": {}\n        }\n\n        os.makedirs(os.path.dirname(self._registry_file), exist_ok=True)\n        if not os.path.exists(self._registry_file):\n            with open(self._registry_file, mode=\"w\") as stream:\n                json.dump(header, stream, indent=4)\n\n    @property\n    def filepath(self) -&gt; str:\n        return self._registry_file\n\n    @lru_cache(maxsize=32)\n    def _get_item(\n        self, name: str, default: Any = _PLACEHOLDER\n    ) -&gt; Any:\n        \"\"\"Get item value from registry json.\n\n        Note:\n            See :meth:`ayon_core.lib.JSONSettingRegistry.get_item`\n\n        \"\"\"\n        with open(self._registry_file, mode=\"r\") as cfg:\n            data = json.load(cfg)\n            try:\n                value = data[\"registry\"][name]\n            except KeyError:\n                if default is not _PLACEHOLDER:\n                    return default\n                raise RegistryItemNotFound(\n                    f\"Registry doesn't contain value {name}\"\n                )\n        return value\n\n    def _set_item(self, name: str, value: Any):\n        \"\"\"Set item value to registry json.\n\n        Note:\n            See :meth:`ayon_core.lib.JSONSettingRegistry.set_item`\n\n        \"\"\"\n        with open(self._registry_file, \"r+\") as stream:\n            data = json.load(stream)\n            registry_data = data.setdefault(\"registry\", {})\n            registry_data[name] = value\n            stream.truncate(0)\n            stream.seek(0)\n            json.dump(data, stream, indent=4)\n        self._get_item.cache_clear()\n\n    def _delete_item(self, name: str, ignore_missing: bool):\n        with open(self._registry_file, \"r+\") as stream:\n            data = json.load(stream)\n            registry_data = data.setdefault(\"registry\", {})\n            if name not in registry_data:\n                if ignore_missing:\n                    return\n                raise KeyError(f\"Registry doesn't contain value {name}\")\n            registry_data.pop(name)\n            stream.truncate(0)\n            stream.seek(0)\n            json.dump(data, stream, indent=4)\n        self._get_item.cache_clear()\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.Logger","title":"<code>Logger</code>","text":"Source code in <code>client/ayon_core/lib/log.py</code> <pre><code>class Logger:\n    DFT = '%(levelname)s &gt;&gt;&gt; { %(name)s }: [ %(message)s ] '\n    DBG = \"  - { %(name)s }: [ %(message)s ] \"\n    INF = \"&gt;&gt;&gt; [ %(message)s ] \"\n    WRN = \"*** WRN: &gt;&gt;&gt; { %(name)s }: [ %(message)s ] \"\n    ERR = \"!!! ERR: %(asctime)s &gt;&gt;&gt; { %(name)s }: [ %(message)s ] \"\n    CRI = \"!!! CRI: %(asctime)s &gt;&gt;&gt; { %(name)s }: [ %(message)s ] \"\n\n    FORMAT_FILE = {\n        logging.INFO: INF,\n        logging.DEBUG: DBG,\n        logging.WARNING: WRN,\n        logging.ERROR: ERR,\n        logging.CRITICAL: CRI,\n    }\n\n    # Is static class initialized\n    initialized = False\n    _init_lock = threading.Lock()\n\n    # Logging level - AYON_LOG_LEVEL\n    log_level = None\n\n    # Data same for all record documents\n    process_data = None\n    # Cached process name or ability to set different process name\n    _process_name = None\n\n    @classmethod\n    def get_logger(cls, name=None):\n        if not cls.initialized:\n            cls.initialize()\n\n        logger = logging.getLogger(name or \"__main__\")\n\n        logger.setLevel(cls.log_level)\n\n        add_console_handler = True\n\n        for handler in logger.handlers:\n            if isinstance(handler, LogStreamHandler):\n                add_console_handler = False\n\n        if add_console_handler:\n            logger.addHandler(cls._get_console_handler())\n\n        # Do not propagate logs to root logger\n        logger.propagate = False\n\n        return logger\n\n    @classmethod\n    def _get_console_handler(cls):\n        formatter = LogFormatter(cls.FORMAT_FILE)\n        console_handler = LogStreamHandler()\n\n        console_handler.set_name(\"LogStreamHandler\")\n        console_handler.setFormatter(formatter)\n        return console_handler\n\n    @classmethod\n    def initialize(cls):\n        # TODO update already created loggers on re-initialization\n        if not cls._init_lock.locked():\n            with cls._init_lock:\n                cls._initialize()\n        else:\n            # If lock is locked wait until is finished\n            while cls._init_lock.locked():\n                time.sleep(0.1)\n\n    @classmethod\n    def _initialize(cls):\n        # Change initialization state to prevent runtime changes\n        # if is executed during runtime\n        cls.initialized = False\n\n        # Define what is logging level\n        log_level = os.getenv(\"AYON_LOG_LEVEL\")\n        if not log_level:\n            # Check AYON_DEBUG for debug level\n            op_debug = os.getenv(\"AYON_DEBUG\")\n            if op_debug and int(op_debug) &gt; 0:\n                log_level = 10\n            else:\n                log_level = 20\n        cls.log_level = int(log_level)\n\n        # Mark as initialized\n        cls.initialized = True\n\n    @classmethod\n    def get_process_data(cls):\n        \"\"\"Data about current process which should be same for all records.\n\n        Process data are used for each record sent to mongo database.\n        \"\"\"\n        if cls.process_data is not None:\n            return copy.deepcopy(cls.process_data)\n\n        if not cls.initialized:\n            cls.initialize()\n\n        host_name = socket.gethostname()\n        try:\n            host_ip = socket.gethostbyname(host_name)\n        except socket.gaierror:\n            host_ip = \"127.0.0.1\"\n\n        process_name = cls.get_process_name()\n\n        cls.process_data = {\n            \"hostname\": host_name,\n            \"hostip\": host_ip,\n            \"username\": getpass.getuser(),\n            \"system_name\": platform.system(),\n            \"process_name\": process_name\n        }\n        return copy.deepcopy(cls.process_data)\n\n    @classmethod\n    def set_process_name(cls, process_name):\n        \"\"\"Set process name for mongo logs.\"\"\"\n        # Just change the attribute\n        cls._process_name = process_name\n        # Update process data if are already set\n        if cls.process_data is not None:\n            cls.process_data[\"process_name\"] = process_name\n\n    @classmethod\n    def get_process_name(cls):\n        \"\"\"Process name that is like \"label\" of a process.\n\n        AYON logging can be used from OpenPyppe itself of from hosts.\n        Even in AYON process it's good to know if logs are from tray or\n        from other cli commands. This should help to identify that information.\n        \"\"\"\n        if cls._process_name is not None:\n            return cls._process_name\n\n        # Get process name\n        process_name = os.environ.get(\"AYON_APP_NAME\")\n        if not process_name:\n            try:\n                import psutil\n                process = psutil.Process(os.getpid())\n                process_name = process.name()\n\n            except ImportError:\n                pass\n\n        if not process_name:\n            process_name = os.path.basename(sys.executable)\n\n        cls._process_name = process_name\n        return cls._process_name\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.Logger.get_process_data","title":"<code>get_process_data()</code>  <code>classmethod</code>","text":"<p>Data about current process which should be same for all records.</p> <p>Process data are used for each record sent to mongo database.</p> Source code in <code>client/ayon_core/lib/log.py</code> <pre><code>@classmethod\ndef get_process_data(cls):\n    \"\"\"Data about current process which should be same for all records.\n\n    Process data are used for each record sent to mongo database.\n    \"\"\"\n    if cls.process_data is not None:\n        return copy.deepcopy(cls.process_data)\n\n    if not cls.initialized:\n        cls.initialize()\n\n    host_name = socket.gethostname()\n    try:\n        host_ip = socket.gethostbyname(host_name)\n    except socket.gaierror:\n        host_ip = \"127.0.0.1\"\n\n    process_name = cls.get_process_name()\n\n    cls.process_data = {\n        \"hostname\": host_name,\n        \"hostip\": host_ip,\n        \"username\": getpass.getuser(),\n        \"system_name\": platform.system(),\n        \"process_name\": process_name\n    }\n    return copy.deepcopy(cls.process_data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.Logger.get_process_name","title":"<code>get_process_name()</code>  <code>classmethod</code>","text":"<p>Process name that is like \"label\" of a process.</p> <p>AYON logging can be used from OpenPyppe itself of from hosts. Even in AYON process it's good to know if logs are from tray or from other cli commands. This should help to identify that information.</p> Source code in <code>client/ayon_core/lib/log.py</code> <pre><code>@classmethod\ndef get_process_name(cls):\n    \"\"\"Process name that is like \"label\" of a process.\n\n    AYON logging can be used from OpenPyppe itself of from hosts.\n    Even in AYON process it's good to know if logs are from tray or\n    from other cli commands. This should help to identify that information.\n    \"\"\"\n    if cls._process_name is not None:\n        return cls._process_name\n\n    # Get process name\n    process_name = os.environ.get(\"AYON_APP_NAME\")\n    if not process_name:\n        try:\n            import psutil\n            process = psutil.Process(os.getpid())\n            process_name = process.name()\n\n        except ImportError:\n            pass\n\n    if not process_name:\n        process_name = os.path.basename(sys.executable)\n\n    cls._process_name = process_name\n    return cls._process_name\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.Logger.set_process_name","title":"<code>set_process_name(process_name)</code>  <code>classmethod</code>","text":"<p>Set process name for mongo logs.</p> Source code in <code>client/ayon_core/lib/log.py</code> <pre><code>@classmethod\ndef set_process_name(cls, process_name):\n    \"\"\"Set process name for mongo logs.\"\"\"\n    # Just change the attribute\n    cls._process_name = process_name\n    # Update process data if are already set\n    if cls.process_data is not None:\n        cls.process_data[\"process_name\"] = process_name\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.MaterialSymbolsIcon","title":"<code>MaterialSymbolsIcon</code>  <code>dataclass</code>","text":"<p>               Bases: <code>IconBase</code></p> <p>Material Symbols icon.</p> Source code in <code>client/ayon_core/lib/icon_definitions.py</code> <pre><code>@dataclass\nclass MaterialSymbolsIcon(IconBase):\n    \"\"\"Material Symbols icon.\"\"\"\n    type: ClassVar[str] = \"material-symbols\"\n    name: str\n    color: str = field(default=DEFAULT_WEB_ICON_COLOR)\n\n    def get_unique_id(self) -&gt; str:\n        return f\"{self.type}|{self.name}|{self.color}\"\n\n    def to_data(self) -&gt; dict:\n        return {\n            \"type\": self.type,\n            \"name\": self.name,\n            \"color\": self.color,\n        }\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.NestedCacheItem","title":"<code>NestedCacheItem</code>","text":"<p>Helper for cached items stored in nested structure.</p> Example <p>cache = NestedCacheItem(levels=2, default_factory=lambda: 0) cache[\"a\"][\"b\"].is_valid False cache[\"a\"][\"b\"].get_data() 0 cache[\"a\"][\"b\"] = 1 cache[\"a\"][\"b\"].is_valid True cache[\"a\"][\"b\"].get_data() 1 cache.reset() cache[\"a\"][\"b\"].is_valid False</p> <p>Parameters:</p> Name Type Description Default <code>levels</code> <code>int</code> <p>Number of nested levels where read cache is stored.</p> <code>1</code> <code>default_factory</code> <code>Optional[callable]</code> <p>Function that returns default value used on init and on reset.</p> <code>None</code> <code>lifetime</code> <code>Optional[int]</code> <p>Lifetime of the cache data in seconds. Default value is based on default value of 'CacheItem'.</p> <code>None</code> <code>_init_info</code> <code>Optional[InitInfo]</code> <p>Private argument. Init info for nested cache where created from parent item.</p> <code>None</code> Source code in <code>client/ayon_core/lib/cache.py</code> <pre><code>class NestedCacheItem:\n    \"\"\"Helper for cached items stored in nested structure.\n\n    Example:\n        &gt;&gt;&gt; cache = NestedCacheItem(levels=2, default_factory=lambda: 0)\n        &gt;&gt;&gt; cache[\"a\"][\"b\"].is_valid\n        False\n        &gt;&gt;&gt; cache[\"a\"][\"b\"].get_data()\n        0\n        &gt;&gt;&gt; cache[\"a\"][\"b\"] = 1\n        &gt;&gt;&gt; cache[\"a\"][\"b\"].is_valid\n        True\n        &gt;&gt;&gt; cache[\"a\"][\"b\"].get_data()\n        1\n        &gt;&gt;&gt; cache.reset()\n        &gt;&gt;&gt; cache[\"a\"][\"b\"].is_valid\n        False\n\n    Args:\n        levels (int): Number of nested levels where read cache is stored.\n        default_factory (Optional[callable]): Function that returns default\n            value used on init and on reset.\n        lifetime (Optional[int]): Lifetime of the cache data in seconds.\n            Default value is based on default value of 'CacheItem'.\n        _init_info (Optional[InitInfo]): Private argument. Init info for\n            nested cache where created from parent item.\n\n    \"\"\"\n    def __init__(\n        self, levels=1, default_factory=None, lifetime=None, _init_info=None\n    ):\n        if levels &lt; 1:\n            raise ValueError(\"Nested levels must be greater than 0\")\n        self._data_by_key = {}\n        if _init_info is None:\n            _init_info = InitInfo(default_factory, lifetime)\n        self._init_info = _init_info\n        self._levels = levels\n\n    def __getitem__(self, key):\n        \"\"\"Get cached data.\n\n        Args:\n            key (str): Key of the cache item.\n\n        Returns:\n            Union[NestedCacheItem, CacheItem]: Cache item.\n\n        \"\"\"\n        cache = self._data_by_key.get(key)\n        if cache is None:\n            if self._levels &gt; 1:\n                cache = NestedCacheItem(\n                    levels=self._levels - 1,\n                    _init_info=self._init_info\n                )\n            else:\n                cache = CacheItem(\n                    self._init_info.default_factory,\n                    self._init_info.lifetime\n                )\n            self._data_by_key[key] = cache\n        return cache\n\n    def __setitem__(self, key, value):\n        \"\"\"Update cached data.\n\n        Args:\n            key (str): Key of the cache item.\n            value (Any): Any data that are cached.\n\n        \"\"\"\n        if self._levels &gt; 1:\n            raise AttributeError((\n                \"{} does not support '__setitem__'. Lower nested level by {}\"\n            ).format(self.__class__.__name__, self._levels - 1))\n        cache = self[key]\n        cache.update_data(value)\n\n    def get(self, key):\n        \"\"\"Get cached data.\n\n        Args:\n            key (str): Key of the cache item.\n\n        Returns:\n            Union[NestedCacheItem, CacheItem]: Cache item.\n\n        \"\"\"\n        return self[key]\n\n    def cached_count(self):\n        \"\"\"Amount of cached items.\n\n        Returns:\n            int: Amount of cached items.\n\n        \"\"\"\n        return len(self._data_by_key)\n\n    def clear_key(self, key):\n        \"\"\"Clear cached item by key.\n\n        Args:\n            key (str): Key of the cache item.\n\n        \"\"\"\n        self._data_by_key.pop(key, None)\n\n    def clear_invalid(self):\n        \"\"\"Clear all invalid cache items.\n\n        Note:\n            To clear all cache items use 'reset'.\n\n        \"\"\"\n        changed = {}\n        children_are_nested = self._levels &gt; 1\n        for key, cache in tuple(self._data_by_key.items()):\n            if children_are_nested:\n                output = cache.clear_invalid()\n                if output:\n                    changed[key] = output\n                if not cache.cached_count():\n                    self._data_by_key.pop(key)\n            elif not cache.is_valid:\n                changed[key] = cache.get_data()\n                self._data_by_key.pop(key)\n        return changed\n\n    def reset(self):\n        \"\"\"Reset cache.\n\n        Note:\n            To clear only invalid cache items use 'clear_invalid'.\n\n        \"\"\"\n        self._data_by_key = {}\n\n    def set_lifetime(self, lifetime):\n        \"\"\"Change lifetime of all children cache items.\n\n        Args:\n            lifetime (int): Lifetime of the cache data in seconds.\n\n        \"\"\"\n        self._init_info.lifetime = lifetime\n        for cache in self._data_by_key.values():\n            cache.set_lifetime(lifetime)\n\n    @property\n    def is_valid(self):\n        \"\"\"Raise reasonable error when called on wrong level.\n\n        Raises:\n            AttributeError: If called on nested cache item.\n\n        \"\"\"\n        raise AttributeError((\n            \"{} does not support 'is_valid'. Lower nested level by '{}'\"\n        ).format(self.__class__.__name__, self._levels))\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.NestedCacheItem.is_valid","title":"<code>is_valid</code>  <code>property</code>","text":"<p>Raise reasonable error when called on wrong level.</p> <p>Raises:</p> Type Description <code>AttributeError</code> <p>If called on nested cache item.</p>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.NestedCacheItem.__getitem__","title":"<code>__getitem__(key)</code>","text":"<p>Get cached data.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>Key of the cache item.</p> required <p>Returns:</p> Type Description <p>Union[NestedCacheItem, CacheItem]: Cache item.</p> Source code in <code>client/ayon_core/lib/cache.py</code> <pre><code>def __getitem__(self, key):\n    \"\"\"Get cached data.\n\n    Args:\n        key (str): Key of the cache item.\n\n    Returns:\n        Union[NestedCacheItem, CacheItem]: Cache item.\n\n    \"\"\"\n    cache = self._data_by_key.get(key)\n    if cache is None:\n        if self._levels &gt; 1:\n            cache = NestedCacheItem(\n                levels=self._levels - 1,\n                _init_info=self._init_info\n            )\n        else:\n            cache = CacheItem(\n                self._init_info.default_factory,\n                self._init_info.lifetime\n            )\n        self._data_by_key[key] = cache\n    return cache\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.NestedCacheItem.__setitem__","title":"<code>__setitem__(key, value)</code>","text":"<p>Update cached data.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>Key of the cache item.</p> required <code>value</code> <code>Any</code> <p>Any data that are cached.</p> required Source code in <code>client/ayon_core/lib/cache.py</code> <pre><code>def __setitem__(self, key, value):\n    \"\"\"Update cached data.\n\n    Args:\n        key (str): Key of the cache item.\n        value (Any): Any data that are cached.\n\n    \"\"\"\n    if self._levels &gt; 1:\n        raise AttributeError((\n            \"{} does not support '__setitem__'. Lower nested level by {}\"\n        ).format(self.__class__.__name__, self._levels - 1))\n    cache = self[key]\n    cache.update_data(value)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.NestedCacheItem.cached_count","title":"<code>cached_count()</code>","text":"<p>Amount of cached items.</p> <p>Returns:</p> Name Type Description <code>int</code> <p>Amount of cached items.</p> Source code in <code>client/ayon_core/lib/cache.py</code> <pre><code>def cached_count(self):\n    \"\"\"Amount of cached items.\n\n    Returns:\n        int: Amount of cached items.\n\n    \"\"\"\n    return len(self._data_by_key)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.NestedCacheItem.clear_invalid","title":"<code>clear_invalid()</code>","text":"<p>Clear all invalid cache items.</p> Note <p>To clear all cache items use 'reset'.</p> Source code in <code>client/ayon_core/lib/cache.py</code> <pre><code>def clear_invalid(self):\n    \"\"\"Clear all invalid cache items.\n\n    Note:\n        To clear all cache items use 'reset'.\n\n    \"\"\"\n    changed = {}\n    children_are_nested = self._levels &gt; 1\n    for key, cache in tuple(self._data_by_key.items()):\n        if children_are_nested:\n            output = cache.clear_invalid()\n            if output:\n                changed[key] = output\n            if not cache.cached_count():\n                self._data_by_key.pop(key)\n        elif not cache.is_valid:\n            changed[key] = cache.get_data()\n            self._data_by_key.pop(key)\n    return changed\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.NestedCacheItem.clear_key","title":"<code>clear_key(key)</code>","text":"<p>Clear cached item by key.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>Key of the cache item.</p> required Source code in <code>client/ayon_core/lib/cache.py</code> <pre><code>def clear_key(self, key):\n    \"\"\"Clear cached item by key.\n\n    Args:\n        key (str): Key of the cache item.\n\n    \"\"\"\n    self._data_by_key.pop(key, None)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.NestedCacheItem.get","title":"<code>get(key)</code>","text":"<p>Get cached data.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>Key of the cache item.</p> required <p>Returns:</p> Type Description <p>Union[NestedCacheItem, CacheItem]: Cache item.</p> Source code in <code>client/ayon_core/lib/cache.py</code> <pre><code>def get(self, key):\n    \"\"\"Get cached data.\n\n    Args:\n        key (str): Key of the cache item.\n\n    Returns:\n        Union[NestedCacheItem, CacheItem]: Cache item.\n\n    \"\"\"\n    return self[key]\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.NestedCacheItem.reset","title":"<code>reset()</code>","text":"<p>Reset cache.</p> Note <p>To clear only invalid cache items use 'clear_invalid'.</p> Source code in <code>client/ayon_core/lib/cache.py</code> <pre><code>def reset(self):\n    \"\"\"Reset cache.\n\n    Note:\n        To clear only invalid cache items use 'clear_invalid'.\n\n    \"\"\"\n    self._data_by_key = {}\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.NestedCacheItem.set_lifetime","title":"<code>set_lifetime(lifetime)</code>","text":"<p>Change lifetime of all children cache items.</p> <p>Parameters:</p> Name Type Description Default <code>lifetime</code> <code>int</code> <p>Lifetime of the cache data in seconds.</p> required Source code in <code>client/ayon_core/lib/cache.py</code> <pre><code>def set_lifetime(self, lifetime):\n    \"\"\"Change lifetime of all children cache items.\n\n    Args:\n        lifetime (int): Lifetime of the cache data in seconds.\n\n    \"\"\"\n    self._init_info.lifetime = lifetime\n    for cache in self._data_by_key.values():\n        cache.set_lifetime(lifetime)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.NumberDef","title":"<code>NumberDef</code>","text":"<p>               Bases: <code>AbstractAttrDef</code></p> <p>Number definition.</p> <p>Number can have defined minimum/maximum value and decimal points. Value is integer if decimals are 0.</p> <p>Parameters:</p> Name Type Description Default <code>minimum</code> <code>(int, float)</code> <p>Minimum possible value.</p> <code>None</code> <code>maximum</code> <code>(int, float)</code> <p>Maximum possible value.</p> <code>None</code> <code>decimals</code> <code>int</code> <p>Maximum decimal points of value.</p> <code>None</code> <code>default</code> <code>(int, float)</code> <p>Default value for conversion.</p> <code>None</code> Source code in <code>client/ayon_core/lib/attribute_definitions.py</code> <pre><code>class NumberDef(AbstractAttrDef):\n    \"\"\"Number definition.\n\n    Number can have defined minimum/maximum value and decimal points. Value\n    is integer if decimals are 0.\n\n    Args:\n        minimum(int, float): Minimum possible value.\n        maximum(int, float): Maximum possible value.\n        decimals(int): Maximum decimal points of value.\n        default(int, float): Default value for conversion.\n\n    \"\"\"\n    type = \"number\"\n    type_attributes = [\n        \"minimum\",\n        \"maximum\",\n        \"decimals\"\n    ]\n\n    def __init__(\n        self,\n        key: str,\n        minimum: Optional[IntFloatType] = None,\n        maximum: Optional[IntFloatType] = None,\n        decimals: Optional[int] = None,\n        default: Optional[IntFloatType] = None,\n        **kwargs\n    ):\n        minimum = 0 if minimum is None else minimum\n        maximum = 999999 if maximum is None else maximum\n        # Swap min/max when are passed in opposite order\n        if minimum &gt; maximum:\n            maximum, minimum = minimum, maximum\n\n        if default is None:\n            default = 0\n\n        elif not isinstance(default, (int, float)):\n            raise TypeError((\n                \"'default' argument must be 'int' or 'float', not '{}'\"\n            ).format(type(default)))\n\n        # Fix default value by mim/max values\n        if default &lt; minimum:\n            default = minimum\n\n        elif default &gt; maximum:\n            default = maximum\n\n        super().__init__(key, default=default, **kwargs)\n\n        self.minimum: IntFloatType = minimum\n        self.maximum: IntFloatType = maximum\n        self.decimals: int = 0 if decimals is None else decimals\n\n    def is_value_valid(self, value: Any) -&gt; bool:\n        if self.decimals == 0:\n            if not isinstance(value, int):\n                return False\n        elif not isinstance(value, float):\n            return False\n        if self.minimum &gt; value &gt; self.maximum:\n            return False\n        return True\n\n    def convert_value(self, value: Any) -&gt; IntFloatType:\n        if isinstance(value, str):\n            try:\n                value = float(value)\n            except Exception:\n                pass\n\n        if not isinstance(value, (int, float)):\n            return self.default\n\n        if self.decimals == 0:\n            return int(value)\n        return round(float(value), self.decimals)\n\n    def _def_type_compare(self, other: \"NumberDef\") -&gt; bool:\n        return (\n            self.decimals == other.decimals\n            and self.maximum == other.maximum\n            and self.maximum == other.maximum\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.PathIcon","title":"<code>PathIcon</code>  <code>dataclass</code>","text":"<p>               Bases: <code>IconBase</code></p> <p>Path to image file on disk.</p> Source code in <code>client/ayon_core/lib/icon_definitions.py</code> <pre><code>@dataclass\nclass PathIcon(IconBase):\n    \"\"\"Path to image file on disk.\"\"\"\n    type: ClassVar[str] = \"path\"\n    path: str\n\n    def get_unique_id(self) -&gt; str:\n        return f\"{self.type}|{self.path}\"\n\n    def to_data(self) -&gt; dict:\n        return {\n            \"type\": self.type,\n            \"path\": self.path,\n        }\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.StrEnum","title":"<code>StrEnum</code>","text":"<p>               Bases: <code>str</code>, <code>Enum</code></p> <p>A string-based Enum class that allows for string comparison.</p> Source code in <code>client/ayon_core/lib/_compatibility.py</code> <pre><code>class StrEnum(str, Enum):\n    \"\"\"A string-based Enum class that allows for string comparison.\"\"\"\n\n    def __str__(self) -&gt; str:\n        return self.value\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.StringTemplate","title":"<code>StringTemplate</code>","text":"<p>String that can be formatted.</p> Source code in <code>client/ayon_core/lib/path_templates.py</code> <pre><code>class StringTemplate:\n    \"\"\"String that can be formatted.\"\"\"\n    def __init__(self, template: str):\n        if not isinstance(template, str):\n            raise TypeError(\n                f\"&lt;{self.__class__.__name__}&gt; argument must be a string,\"\n                f\" not {str(type(template))}.\"\n            )\n\n        self._template: str = template\n        parts = []\n        formatter = Formatter()\n\n        for item in formatter.parse(template):\n            literal_text, field_name, format_spec, conversion = item\n            if literal_text:\n                parts.append(literal_text)\n            if field_name:\n                parts.append(\n                    FormattingPart(field_name, format_spec, conversion)\n                )\n\n        new_parts = []\n        for part in parts:\n            if not isinstance(part, str):\n                new_parts.append(part)\n                continue\n\n            substr = \"\"\n            for char in part:\n                if char not in (\"&lt;\", \"&gt;\"):\n                    substr += char\n                else:\n                    if substr:\n                        new_parts.append(substr)\n                    new_parts.append(char)\n                    substr = \"\"\n            if substr:\n                new_parts.append(substr)\n\n        self._parts: list[Union[str, OptionalPart, FormattingPart]] = (\n            self.find_optional_parts(new_parts)\n        )\n\n    def __str__(self) -&gt; str:\n        return self.template\n\n    def __repr__(self) -&gt; str:\n        return f\"&lt;{self.__class__.__name__}&gt; {self.template}\"\n\n    def __contains__(self, other: str) -&gt; bool:\n        return other in self.template\n\n    def replace(self, *args, **kwargs):\n        self._template = self.template.replace(*args, **kwargs)\n        return self\n\n    @property\n    def template(self) -&gt; str:\n        return self._template\n\n    def format(self, data: dict[str, Any]) -&gt; \"TemplateResult\":\n        \"\"\" Figure out with whole formatting.\n\n        Separate advanced keys (*Like '{project[name]}') from string which must\n        be formatted separately in case of missing or incomplete keys in data.\n\n        Args:\n            data (dict): Containing keys to be filled into template.\n\n        Returns:\n            TemplateResult: Filled or partially filled template containing all\n                data needed or missing for filling template.\n\n        \"\"\"\n        result = TemplatePartResult()\n        for part in self._parts:\n            if isinstance(part, str):\n                result.add_output(part)\n            else:\n                part.format(data, result)\n\n        invalid_types = result.invalid_types\n        invalid_types.update(result.invalid_optional_types)\n        invalid_types = result.split_keys_to_subdicts(invalid_types)\n\n        missing_keys = result.missing_keys\n        missing_keys |= result.missing_optional_keys\n\n        solved = result.solved\n        used_values = result.get_clean_used_values()\n\n        return TemplateResult(\n            result.output,\n            self.template,\n            solved,\n            used_values,\n            missing_keys,\n            invalid_types\n        )\n\n    def format_strict(self, data: dict[str, Any]) -&gt; \"TemplateResult\":\n        result = self.format(data)\n        result.validate()\n        return result\n\n    def remove_optional_parts_for_data(\n        self,\n        data: Optional[dict[str, Any]] = None,\n    ) -&gt; str:\n        \"\"\"Remove optional parts from template for data.\n\n        This method does not modify the template object itself.\n\n        Note:\n            At this moment the functionality is not 1:1 with 'format' where\n                lists are supported and the values are validated.\n\n        Args:\n            data (Optional[dict[str, Any]]): Template data for template.\n\n        Returns:\n            str: Template without optional parts that are not available\n                in passed data.\n\n        \"\"\"\n        if data is None:\n            data = {}\n\n        output = \"\"\n        for part in self._parts:\n            if isinstance(part, str):\n                output += part\n            elif isinstance(part, FormattingPart):\n                output += part.template\n            elif isinstance(part, OptionalPart):\n                output += part.remove_optional_parts_for_data(data)\n            else:\n                raise TypeError(\n                    f\"Got invalid type in template parts '{type(part)}'\"\n                )\n        return output\n\n    @classmethod\n    def format_template(\n        cls, template: str, data: dict[str, Any]\n    ) -&gt; \"TemplateResult\":\n        objected_template = cls(template)\n        return objected_template.format(data)\n\n    @classmethod\n    def format_strict_template(\n        cls, template: str, data: dict[str, Any]\n    ) -&gt; \"TemplateResult\":\n        objected_template = cls(template)\n        return objected_template.format_strict(data)\n\n    @staticmethod\n    def find_optional_parts(\n        parts: list[Union[str, FormattingPart]]\n    ) -&gt; list[Union[str, OptionalPart, FormattingPart]]:\n        new_parts = []\n        tmp_parts = {}\n        counted_symb = -1\n        for part in parts:\n            if part == \"&lt;\":\n                counted_symb += 1\n                tmp_parts[counted_symb] = []\n\n            elif part == \"&gt;\":\n                if counted_symb &gt; -1:\n                    parts = tmp_parts.pop(counted_symb)\n                    counted_symb -= 1\n                    # If part contains only single string keep value\n                    #   unchanged\n                    if parts:\n                        # Remove optional start char\n                        parts.pop(0)\n\n                    if not parts:\n                        value = \"&lt;&gt;\"\n                    elif (\n                        len(parts) == 1\n                        and isinstance(parts[0], str)\n                    ):\n                        value = f\"&lt;{parts[0]}&gt;\"\n                    else:\n                        value = OptionalPart(parts)\n\n                    if counted_symb &lt; 0:\n                        out_parts = new_parts\n                    else:\n                        out_parts = tmp_parts[counted_symb]\n                    # Store value\n                    out_parts.append(value)\n                    continue\n\n            if counted_symb &lt; 0:\n                new_parts.append(part)\n            else:\n                tmp_parts[counted_symb].append(part)\n\n        if tmp_parts:\n            for idx in sorted(tmp_parts.keys()):\n                new_parts.extend(tmp_parts[idx])\n        return new_parts\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.StringTemplate.format","title":"<code>format(data)</code>","text":"<p>Figure out with whole formatting.</p> <p>Separate advanced keys (*Like '{project[name]}') from string which must be formatted separately in case of missing or incomplete keys in data.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict</code> <p>Containing keys to be filled into template.</p> required <p>Returns:</p> Name Type Description <code>TemplateResult</code> <code>'TemplateResult'</code> <p>Filled or partially filled template containing all data needed or missing for filling template.</p> Source code in <code>client/ayon_core/lib/path_templates.py</code> <pre><code>def format(self, data: dict[str, Any]) -&gt; \"TemplateResult\":\n    \"\"\" Figure out with whole formatting.\n\n    Separate advanced keys (*Like '{project[name]}') from string which must\n    be formatted separately in case of missing or incomplete keys in data.\n\n    Args:\n        data (dict): Containing keys to be filled into template.\n\n    Returns:\n        TemplateResult: Filled or partially filled template containing all\n            data needed or missing for filling template.\n\n    \"\"\"\n    result = TemplatePartResult()\n    for part in self._parts:\n        if isinstance(part, str):\n            result.add_output(part)\n        else:\n            part.format(data, result)\n\n    invalid_types = result.invalid_types\n    invalid_types.update(result.invalid_optional_types)\n    invalid_types = result.split_keys_to_subdicts(invalid_types)\n\n    missing_keys = result.missing_keys\n    missing_keys |= result.missing_optional_keys\n\n    solved = result.solved\n    used_values = result.get_clean_used_values()\n\n    return TemplateResult(\n        result.output,\n        self.template,\n        solved,\n        used_values,\n        missing_keys,\n        invalid_types\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.StringTemplate.remove_optional_parts_for_data","title":"<code>remove_optional_parts_for_data(data=None)</code>","text":"<p>Remove optional parts from template for data.</p> <p>This method does not modify the template object itself.</p> Note <p>At this moment the functionality is not 1:1 with 'format' where     lists are supported and the values are validated.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>Optional[dict[str, Any]]</code> <p>Template data for template.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Template without optional parts that are not available in passed data.</p> Source code in <code>client/ayon_core/lib/path_templates.py</code> <pre><code>def remove_optional_parts_for_data(\n    self,\n    data: Optional[dict[str, Any]] = None,\n) -&gt; str:\n    \"\"\"Remove optional parts from template for data.\n\n    This method does not modify the template object itself.\n\n    Note:\n        At this moment the functionality is not 1:1 with 'format' where\n            lists are supported and the values are validated.\n\n    Args:\n        data (Optional[dict[str, Any]]): Template data for template.\n\n    Returns:\n        str: Template without optional parts that are not available\n            in passed data.\n\n    \"\"\"\n    if data is None:\n        data = {}\n\n    output = \"\"\n    for part in self._parts:\n        if isinstance(part, str):\n            output += part\n        elif isinstance(part, FormattingPart):\n            output += part.template\n        elif isinstance(part, OptionalPart):\n            output += part.remove_optional_parts_for_data(data)\n        else:\n            raise TypeError(\n                f\"Got invalid type in template parts '{type(part)}'\"\n            )\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.TemplateUnsolved","title":"<code>TemplateUnsolved</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Exception for unsolved template when strict is set to True.</p> Source code in <code>client/ayon_core/lib/path_templates.py</code> <pre><code>class TemplateUnsolved(Exception):\n    \"\"\"Exception for unsolved template when strict is set to True.\"\"\"\n\n    msg = \"Template \\\"{0}\\\" is unsolved.{1}{2}\"\n    invalid_types_msg = \" Keys with invalid data type: `{0}`.\"\n    missing_keys_msg = \" Missing keys: \\\"{0}\\\".\"\n\n    def __init__(self, template, missing_keys, invalid_types):\n        invalid_type_items = []\n        for _key, _type in invalid_types.items():\n            invalid_type_items.append(f\"\\\"{_key}\\\" {str(_type)}\")\n\n        invalid_types_msg = \"\"\n        if invalid_type_items:\n            invalid_types_msg = self.invalid_types_msg.format(\n                \", \".join(invalid_type_items)\n            )\n\n        missing_keys_msg = \"\"\n        if missing_keys:\n            missing_keys_msg = self.missing_keys_msg.format(\n                \", \".join(missing_keys)\n            )\n        super().__init__(\n            self.msg.format(template, missing_keys_msg, invalid_types_msg)\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.TextDef","title":"<code>TextDef</code>","text":"<p>               Bases: <code>AbstractAttrDef</code></p> <p>Text definition.</p> <p>Text can have multiline option so end-line characters are allowed regex validation can be applied placeholder for UI purposes and default value.</p> <p>Regex validation is not part of attribute implementation.</p> <p>Parameters:</p> Name Type Description Default <code>multiline</code> <code>bool</code> <p>Text has single or multiline support.</p> <code>None</code> <code>regex</code> <code>(str, Pattern)</code> <p>Regex validation.</p> <code>None</code> <code>placeholder</code> <code>str</code> <p>UI placeholder for attribute.</p> <code>None</code> <code>default</code> <code>(str, None)</code> <p>Default value. Empty string used when not defined.</p> <code>None</code> Source code in <code>client/ayon_core/lib/attribute_definitions.py</code> <pre><code>class TextDef(AbstractAttrDef):\n    \"\"\"Text definition.\n\n    Text can have multiline option so end-line characters are allowed regex\n    validation can be applied placeholder for UI purposes and default value.\n\n    Regex validation is not part of attribute implementation.\n\n    Args:\n        multiline(bool): Text has single or multiline support.\n        regex(str, re.Pattern): Regex validation.\n        placeholder(str): UI placeholder for attribute.\n        default(str, None): Default value. Empty string used when not defined.\n\n    \"\"\"\n    type = \"text\"\n    type_attributes = [\n        \"multiline\",\n        \"placeholder\",\n    ]\n\n    def __init__(\n        self,\n        key: str,\n        multiline: Optional[bool] = None,\n        regex: Optional[str] = None,\n        placeholder: Optional[str] = None,\n        default: Optional[str] = None,\n        **kwargs\n    ):\n        if default is None:\n            default = \"\"\n\n        super().__init__(key, default=default, **kwargs)\n\n        if multiline is None:\n            multiline = False\n\n        elif not isinstance(default, str):\n            raise TypeError((\n                f\"'default' argument must be a str, not '{type(default)}'\"\n            ))\n\n        if isinstance(regex, str):\n            regex = re.compile(regex)\n\n        self.multiline: bool = multiline\n        self.placeholder: Optional[str] = placeholder\n        self.regex: Optional[\"Pattern\"] = regex\n\n    def is_value_valid(self, value: Any) -&gt; bool:\n        if not isinstance(value, str):\n            return False\n        if self.regex and not self.regex.match(value):\n            return False\n        return True\n\n    def convert_value(self, value: Any) -&gt; str:\n        if isinstance(value, str):\n            return value\n        return self.default\n\n    def serialize(self) -&gt; Dict[str, Any]:\n        data = super().serialize()\n        regex = None\n        if self.regex is not None:\n            regex = self.regex.pattern\n        data[\"regex\"] = regex\n        data[\"multiline\"] = self.multiline\n        data[\"placeholder\"] = self.placeholder\n        return data\n\n    def _def_type_compare(self, other: \"TextDef\") -&gt; bool:\n        return (\n            self.multiline == other.multiline\n            and self.regex == other.regex\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.ToolNotFoundError","title":"<code>ToolNotFoundError</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Raised when tool arguments are not found.</p> Source code in <code>client/ayon_core/lib/vendor_bin_utils.py</code> <pre><code>class ToolNotFoundError(Exception):\n    \"\"\"Raised when tool arguments are not found.\"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.TransparentIcon","title":"<code>TransparentIcon</code>  <code>dataclass</code>","text":"<p>               Bases: <code>IconBase</code></p> <p>Transparent icon.</p> Source code in <code>client/ayon_core/lib/icon_definitions.py</code> <pre><code>@dataclass\nclass TransparentIcon(IconBase):\n    \"\"\"Transparent icon.\"\"\"\n    type: ClassVar[str] = \"transparent\"\n    size: int = 256\n\n    def get_unique_id(self):\n        return f\"{self.type}|{self.size}\"\n\n    def to_data(self) -&gt; dict:\n        return {\n            \"type\": self.type,\n            \"size\": self.size,\n        }\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.UnknownDef","title":"<code>UnknownDef</code>","text":"<p>               Bases: <code>AbstractAttrDef</code></p> <p>Definition is not known because definition is not available.</p> <p>This attribute can be used to keep existing data unchanged but does not have known definition of type.</p> Source code in <code>client/ayon_core/lib/attribute_definitions.py</code> <pre><code>class UnknownDef(AbstractAttrDef):\n    \"\"\"Definition is not known because definition is not available.\n\n    This attribute can be used to keep existing data unchanged but does not\n    have known definition of type.\n\n    \"\"\"\n    type = \"unknown\"\n\n    def __init__(self, key: str, default: Optional[Any] = None, **kwargs):\n        kwargs[\"default\"] = default\n        super().__init__(key, **kwargs)\n\n    def is_value_valid(self, value: Any) -&gt; bool:\n        return True\n\n    def convert_value(self, value: Any) -&gt; Any:\n        return value\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.UrlIcon","title":"<code>UrlIcon</code>  <code>dataclass</code>","text":"<p>               Bases: <code>IconBase</code></p> <p>Url to an image file.</p> Source code in <code>client/ayon_core/lib/icon_definitions.py</code> <pre><code>@dataclass\nclass UrlIcon(IconBase):\n    \"\"\"Url to an image file.\"\"\"\n    type: ClassVar[str] = \"url\"\n    url: str\n\n    def get_unique_id(self) -&gt; str:\n        return f\"{self.type}|{self.url}\"\n\n    def get_content(self) -&gt; bytes | None:\n        try:\n            return urllib.request.urlopen(self.url).read()\n        except Exception:\n            log.warning(\n                \"Failed to download image '%s'\", self.url, exc_info=True\n            )\n            return b\"\"\n\n    def to_data(self) -&gt; dict:\n        return {\n            \"type\": self.type,\n            \"url\": self.url,\n        }\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.classes_from_module","title":"<code>classes_from_module(superclass, module)</code>","text":"<p>Return plug-ins from module</p> <p>Parameters:</p> Name Type Description Default <code>superclass</code> <code>superclass</code> <p>Superclass of subclasses to look for</p> required <code>module</code> <code>ModuleType</code> <p>Imported module where to look for 'superclass' subclasses.</p> required <p>Returns:</p> Type Description <p>List of plug-ins, or empty list if none is found.</p> Source code in <code>client/ayon_core/lib/python_module_tools.py</code> <pre><code>def classes_from_module(superclass, module):\n    \"\"\"Return plug-ins from module\n\n    Arguments:\n        superclass (superclass): Superclass of subclasses to look for\n        module (types.ModuleType): Imported module where to look for\n            'superclass' subclasses.\n\n    Returns:\n        List of plug-ins, or empty list if none is found.\n\n    \"\"\"\n\n    classes = list()\n    for name in dir(module):\n        # It could be anything at this point\n        obj = getattr(module, name)\n        if not inspect.isclass(obj) or obj is superclass:\n            continue\n\n        if issubclass(obj, superclass):\n            classes.append(obj)\n\n    return classes\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.collect_frames","title":"<code>collect_frames(files)</code>","text":"<p>Returns dict of source path and its frame, if from sequence</p> <p>Uses clique as most precise solution, used when anatomy template that created files is not known.</p> <p>Assumption is that frames are separated by '.', negative frames are not allowed.</p> <p>Parameters:</p> Name Type Description Default <code>files</code> <code>list) or (set with single value</code> <p>list of source paths</p> required <p>Returns:</p> Name Type Description <code>dict</code> <p>{'/folder/product_v001.0001.png': '0001', ....}</p> Source code in <code>client/ayon_core/lib/path_tools.py</code> <pre><code>def collect_frames(files):\n    \"\"\"Returns dict of source path and its frame, if from sequence\n\n    Uses clique as most precise solution, used when anatomy template that\n    created files is not known.\n\n    Assumption is that frames are separated by '.', negative frames are not\n    allowed.\n\n    Args:\n        files(list) or (set with single value): list of source paths\n\n    Returns:\n        dict: {'/folder/product_v001.0001.png': '0001', ....}\n    \"\"\"\n\n    # clique.PATTERNS[\"frames\"] supports only `.1001.exr` not `_1001.exr` so\n    # we use a customized pattern.\n    pattern = \"[_.](?P&lt;index&gt;(?P&lt;padding&gt;0*)\\\\d+)\\\\.\\\\D+\\\\d?$\"\n    patterns = [pattern]\n    collections, remainder = clique.assemble(\n        files, minimum_items=1, patterns=patterns)\n\n    sources_and_frames = {}\n    if collections:\n        for collection in collections:\n            src_head = collection.head\n            src_tail = collection.tail\n\n            for index in collection.indexes:\n                src_frame = collection.format(\"{padding}\") % index\n                src_file_name = \"{}{}{}\".format(\n                    src_head, src_frame, src_tail)\n                sources_and_frames[src_file_name] = src_frame\n    else:\n        sources_and_frames[remainder.pop()] = None\n\n    return sources_and_frames\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.compile_list_of_regexes","title":"<code>compile_list_of_regexes(in_list)</code>","text":"<p>Convert strings in entered list to compiled regex objects.</p> Source code in <code>client/ayon_core/lib/profiles_filtering.py</code> <pre><code>def compile_list_of_regexes(in_list):\n    \"\"\"Convert strings in entered list to compiled regex objects.\"\"\"\n    regexes = list()\n    if not in_list:\n        return regexes\n\n    for item in in_list:\n        if not item:\n            continue\n        try:\n            regexes.append(re.compile(item))\n        except TypeError:\n            print((\n                \"Invalid type \\\"{}\\\" value \\\"{}\\\".\"\n                \" Expected string based object. Skipping.\"\n            ).format(str(type(item)), str(item)))\n    return regexes\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.compute_env_variables_structure","title":"<code>compute_env_variables_structure(env, fill_dynamic_keys=True)</code>","text":"<p>Compute the result from recursive dynamic environment.</p> Keys that are not present in the data will remain unformatted as the <p>original keys. So they can be formatted against the current user environment when merging. So {\"A\": \"{key}\"} will remain {key} if not present in the dynamic environment.</p> Source code in <code>client/ayon_core/lib/env_tools.py</code> <pre><code>def compute_env_variables_structure(\n    env: dict[str, str],\n    fill_dynamic_keys: bool = True,\n) -&gt; dict[str, str]:\n    \"\"\"Compute the result from recursive dynamic environment.\n\n    Note: Keys that are not present in the data will remain unformatted as the\n        original keys. So they can be formatted against the current user\n        environment when merging. So {\"A\": \"{key}\"} will remain {key} if not\n        present in the dynamic environment.\n\n    \"\"\"\n    env = env.copy()\n\n    # Collect dependencies\n    dependencies = collections.defaultdict(set)\n    for key, value in env.items():\n        dependent_keys = re.findall(\"{(.+?)}\", value)\n        for dependent_key in dependent_keys:\n            # Ignore reference to itself or key is not in env\n            if dependent_key != key and dependent_key in env:\n                dependencies[key].add(dependent_key)\n\n    ordered, cyclic = _topological_sort(dependencies)\n\n    # Check cycle\n    if cyclic:\n        raise CycleError(f\"A cycle is detected on: {cyclic}\")\n\n    # Format dynamic values\n    for key in reversed(ordered):\n        if key in env:\n            if not isinstance(env[key], str):\n                continue\n            data = env.copy()\n            data.pop(key)    # format without itself\n            env[key] = _partial_format(env[key], data=data)\n\n    # Format dynamic keys\n    if fill_dynamic_keys:\n        formatted = {}\n        for key, value in env.items():\n            if not isinstance(value, str):\n                formatted[key] = value\n                continue\n\n            new_key = _partial_format(key, data=env)\n            if new_key in formatted:\n                raise DynamicKeyClashError(\n                    f\"Key clashes on: {new_key} (source: {key})\"\n                )\n\n            formatted[new_key] = value\n        env = formatted\n\n    return env\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.convert_ffprobe_fps_to_float","title":"<code>convert_ffprobe_fps_to_float(value)</code>","text":"<p>Convert string value of frame rate to float.</p> <p>Copy of 'convert_ffprobe_fps_value' which raises exceptions on invalid value, does not convert value to string and does not return \"Unknown\" string.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>str</code> <p>Value to be converted.</p> required <p>Returns:</p> Name Type Description <code>Float</code> <p>Converted frame rate in float. If divisor in value is '0' then '0.0' is returned.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>Passed value is invalid for conversion.</p> Source code in <code>client/ayon_core/lib/transcoding.py</code> <pre><code>def convert_ffprobe_fps_to_float(value):\n    \"\"\"Convert string value of frame rate to float.\n\n    Copy of 'convert_ffprobe_fps_value' which raises exceptions on invalid\n    value, does not convert value to string and does not return \"Unknown\"\n    string.\n\n    Args:\n        value (str): Value to be converted.\n\n    Returns:\n        Float: Converted frame rate in float. If divisor in value is '0' then\n            '0.0' is returned.\n\n    Raises:\n        ValueError: Passed value is invalid for conversion.\n    \"\"\"\n\n    if not value:\n        raise ValueError(\"Got empty value.\")\n\n    items = value.split(\"/\")\n    if len(items) == 1:\n        return float(items[0])\n\n    if len(items) &gt; 2:\n        raise ValueError((\n            \"FPS expression contains multiple dividers \\\"{}\\\".\"\n        ).format(value))\n\n    dividend = float(items.pop(0))\n    divisor = float(items.pop(0))\n    if divisor == 0.0:\n        return 0.0\n    return dividend / divisor\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.convert_ffprobe_fps_value","title":"<code>convert_ffprobe_fps_value(str_value)</code>","text":"<p>Returns (str) value of fps from ffprobe frame format (120/1)</p> Source code in <code>client/ayon_core/lib/transcoding.py</code> <pre><code>def convert_ffprobe_fps_value(str_value):\n    \"\"\"Returns (str) value of fps from ffprobe frame format (120/1)\"\"\"\n    if str_value == \"0/0\":\n        print(\"WARNING: Source has \\\"r_frame_rate\\\" value set to \\\"0/0\\\".\")\n        return \"Unknown\"\n\n    items = str_value.split(\"/\")\n    if len(items) == 1:\n        fps = float(items[0])\n\n    elif len(items) == 2:\n        fps = float(items[0]) / float(items[1])\n\n    # Check if fps is integer or float number\n    if int(fps) == fps:\n        fps = int(fps)\n\n    return str(fps)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.convert_input_paths_for_ffmpeg","title":"<code>convert_input_paths_for_ffmpeg(input_paths, output_dir, logger=None, *, review_layers=None)</code>","text":"<p>Convert source file to format supported in ffmpeg.</p> <p>Can currently convert only EXRs. The input filepaths should be files with same type. Information about input is loaded only from first found file.</p> <p>Filenames of input files are kept so make sure that output directory is not the same directory as input files have. - This way it can handle gaps and can keep input filenames without handling     frame template</p> <p>Parameters:</p> Name Type Description Default <code>input_paths</code> <code>str</code> <p>Paths that should be converted. It is expected that contains single file or image sequence of same type.</p> required <code>output_dir</code> <code>str</code> <p>Path to directory where output will be rendered. Must not be same as input's directory.</p> required <code>logger</code> <code>Logger</code> <p>Logger used for logging.</p> <code>None</code> <code>review_layers</code> <code>Optional[list[str]]</code> <p>List of reviewable layers.</p> <code>None</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If input filepath has extension not supported by function. Currently, only \".exr\" extension is supported.</p> Source code in <code>client/ayon_core/lib/transcoding.py</code> <pre><code>def convert_input_paths_for_ffmpeg(\n    input_paths: str,\n    output_dir: str,\n    # TODO move 'review_layers' before logger\n    logger: logging.Logger = None,\n    *,\n    review_layers: Optional[list[str]] = None,\n) -&gt; None:\n    \"\"\"Convert source file to format supported in ffmpeg.\n\n    Can currently convert only EXRs. The input filepaths should be files\n    with same type. Information about input is loaded only from first found\n    file.\n\n    Filenames of input files are kept so make sure that output directory\n    is not the same directory as input files have.\n    - This way it can handle gaps and can keep input filenames without handling\n        frame template\n\n    Args:\n        input_paths (str): Paths that should be converted. It is expected that\n            contains single file or image sequence of same type.\n        output_dir (str): Path to directory where output will be rendered.\n            Must not be same as input's directory.\n        logger (logging.Logger): Logger used for logging.\n        review_layers (Optional[list[str]]): List of reviewable layers.\n\n    Raises:\n        ValueError: If input filepath has extension not supported by function.\n            Currently, only \".exr\" extension is supported.\n    \"\"\"\n    if logger is None:\n        logger = logging.getLogger(__name__)\n\n    first_input_path = input_paths[0]\n    ext = os.path.splitext(first_input_path)[1].lower()\n\n    if ext != \".exr\":\n        raise ValueError(\n            \"Function 'convert_input_paths_for_ffmpeg' currently supports\"\n            f\" only \\\".exr\\\" extension. Got \\\"{ext}\\\".\"\n        )\n\n    input_info = get_oiio_info_for_input(first_input_path, logger=logger)\n\n    # Change compression only if source compression is \"dwaa\" or \"dwab\"\n    #   - they're not supported in ffmpeg\n    compression = input_info[\"attribs\"].get(\"compression\")\n    if compression in (\"dwaa\", \"dwab\"):\n        compression = \"none\"\n\n    # Collect channels to export\n    input_arg, channels_arg = get_oiio_input_and_channel_args(\n        input_info, review_layers=review_layers\n    )\n\n    # Find which attributes to strip\n    erase_attributes: list[str] = _get_attributes_to_erase(\n        input_info, logger=logger\n    )\n\n    # clique.PATTERNS[\"frames\"] supports only `.1001.exr` not `_1001.exr` so\n    # we use a customized pattern.\n    pattern = \"[_.](?P&lt;index&gt;(?P&lt;padding&gt;0*)\\\\d+)\\\\.\\\\D+\\\\d?$\"\n    input_collections, input_remainder = clique.assemble(\n        input_paths,\n        patterns=[pattern],\n        assume_padded_when_ambiguous=True,\n    )\n    input_items = list(input_collections)\n    input_items.extend(input_remainder)\n    for input_item in input_items:\n        # Prepare subprocess arguments\n        oiio_cmd = get_oiio_tool_args(\n            \"oiiotool\",\n            # Don't add any additional attributes\n            \"--nosoftwareattrib\",\n        )\n        # Add input compression if available\n        if compression:\n            oiio_cmd.extend([\"--compression\", compression])\n\n        # Convert a sequence of files using a single oiiotool command\n        # using its sequence syntax\n        if isinstance(input_item, clique.Collection):\n            frames = input_item.format(\"{ranges}\")\n            oiio_cmd.extend([\n                \"--framepadding\", str(input_item.padding),\n                \"--frames\", frames,\n                \"--parallel-frames\"\n            ])\n            input_item: str = input_item.format(\"{head}#{tail}\")\n        elif not isinstance(input_item, str):\n            raise TypeError(\n                f\"Input is not a string or Collection: {input_item}\"\n            )\n\n        oiio_cmd.extend([\n            input_arg, input_item,\n            # Tell oiiotool which channels should be put to top stack\n            #   (and output)\n            \"--ch\", channels_arg,\n            # Use first subimage\n            \"--subimage\", \"0\"\n        ])\n\n        for attr_name in erase_attributes:\n            oiio_cmd.extend([\"--eraseattrib\", attr_name])\n\n        # Add last argument - path to output\n        base_filename = os.path.basename(input_item)\n        output_path = os.path.join(output_dir, base_filename)\n        oiio_cmd.extend([\n            \"-o\", output_path\n        ])\n\n        logger.debug(\"Conversion command: {}\".format(\" \".join(oiio_cmd)))\n        run_subprocess(oiio_cmd, logger=logger)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.create_hard_link","title":"<code>create_hard_link(src_path, dst_path)</code>","text":"<p>Create hardlink of file.</p> <p>Parameters:</p> Name Type Description Default <code>src_path</code> <code>str</code> <p>Full path to a file which is used as source for hardlink.</p> required <code>dst_path</code> <code>str</code> <p>Full path to a file where a link of source will be added.</p> required Source code in <code>client/ayon_core/lib/path_tools.py</code> <pre><code>def create_hard_link(src_path, dst_path):\n    \"\"\"Create hardlink of file.\n\n    Args:\n        src_path(str): Full path to a file which is used as source for\n            hardlink.\n        dst_path(str): Full path to a file where a link of source will be\n            added.\n    \"\"\"\n    os.link(src_path, dst_path)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.emit_event","title":"<code>emit_event(topic, data=None, source=None)</code>","text":"<p>Emit event with topic and data.</p> Arg <p>topic(str): Event's topic. data(dict): Event's additional data. Optional. source(str): Who emitted the topic. Optional.</p> <p>Returns:</p> Name Type Description <code>Event</code> <p>Object of event that was emitted.</p> Source code in <code>client/ayon_core/lib/events.py</code> <pre><code>def emit_event(topic, data=None, source=None):\n    \"\"\"Emit event with topic and data.\n\n    Arg:\n        topic(str): Event's topic.\n        data(dict): Event's additional data. Optional.\n        source(str): Who emitted the topic. Optional.\n\n    Returns:\n        Event: Object of event that was emitted.\n    \"\"\"\n\n    return GlobalEventSystem.emit(topic, data, source)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.env_value_to_bool","title":"<code>env_value_to_bool(env_key=None, value=None, default=False)</code>","text":"<p>Convert environment variable value to boolean.</p> <p>Function is based on value of the environemt variable. Value is lowered so function is not case sensitive.</p> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>If value match to one of [\"true\", \"yes\", \"1\"] result if True but if value match to [\"false\", \"no\", \"0\"] result is False else default value is returned.</p> Source code in <code>client/ayon_core/lib/env_tools.py</code> <pre><code>def env_value_to_bool(\n    env_key: Optional[str] = None,\n    value: Optional[str] = None,\n    default: bool = False,\n) -&gt; bool:\n    \"\"\"Convert environment variable value to boolean.\n\n    Function is based on value of the environemt variable. Value is lowered\n    so function is not case sensitive.\n\n    Returns:\n        bool: If value match to one of [\"true\", \"yes\", \"1\"] result if True\n            but if value match to [\"false\", \"no\", \"0\"] result is False else\n            default value is returned.\n\n    \"\"\"\n    if value is None and env_key is None:\n        return default\n\n    if value is None:\n        value = os.environ.get(env_key)\n\n    if value is not None:\n        value = str(value).lower()\n        if value in (\"true\", \"yes\", \"1\", \"on\"):\n            return True\n        elif value in (\"false\", \"no\", \"0\", \"off\"):\n            return False\n    return default\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.filter_profiles","title":"<code>filter_profiles(profiles_data, key_values, keys_order=None, logger=None)</code>","text":"<p>Filter profiles by entered key -&gt; values.</p> <p>Profile if marked with score for each key/value from <code>key_values</code> with points -1, 0 or 1. - if profile contain the key and profile's value contain value from     <code>key_values</code> then profile gets 1 point - if profile does not contain the key or profile's value is empty or     contain \"\" then got 0 point - if profile contain the key, profile's value is not empty and does not     contain \"\" and value from <code>key_values</code> is not available in the value     then got -1 point</p> <p>If profile gets -1 point at any time then is skipped and not used for output. Profile with higher score is returned. If there are multiple profiles with same score then first in order is used (order of profiles matter).</p> <p>Parameters:</p> Name Type Description Default <code>profiles_data</code> <code>list</code> <p>Profile definitions as dictionaries.</p> required <code>key_values</code> <code>dict</code> <p>Mapping of Key &lt;-&gt; Value. Key is checked if is available in profile and if Value is matching it's values.</p> required <code>keys_order</code> <code>(list, tuple)</code> <p>Order of keys from <code>key_values</code> which matters only when multiple profiles have same score.</p> <code>None</code> <code>logger</code> <code>Logger</code> <p>Optionally can be passed different logger.</p> <code>None</code> <p>Returns:</p> Type Description <p>dict/None: Return most matching profile or None if none of profiles match at least one criteria.</p> Source code in <code>client/ayon_core/lib/profiles_filtering.py</code> <pre><code>def filter_profiles(profiles_data, key_values, keys_order=None, logger=None):\n    \"\"\" Filter profiles by entered key -&gt; values.\n\n    Profile if marked with score for each key/value from `key_values` with\n    points -1, 0 or 1.\n    - if profile contain the key and profile's value contain value from\n        `key_values` then profile gets 1 point\n    - if profile does not contain the key or profile's value is empty or\n        contain \"*\" then got 0 point\n    - if profile contain the key, profile's value is not empty and does not\n        contain \"*\" and value from `key_values` is not available in the value\n        then got -1 point\n\n    If profile gets -1 point at any time then is skipped and not used for\n    output. Profile with higher score is returned. If there are multiple\n    profiles with same score then first in order is used (order of profiles\n    matter).\n\n    Args:\n        profiles_data (list): Profile definitions as dictionaries.\n        key_values (dict): Mapping of Key &lt;-&gt; Value. Key is checked if is\n            available in profile and if Value is matching it's values.\n        keys_order (list, tuple): Order of keys from `key_values` which matters\n            only when multiple profiles have same score.\n        logger (logging.Logger): Optionally can be passed different logger.\n\n    Returns:\n        dict/None: Return most matching profile or None if none of profiles\n            match at least one criteria.\n    \"\"\"\n    if not profiles_data:\n        return None\n\n    if not logger:\n        logger = log\n\n    if not keys_order:\n        keys_order = tuple(key_values.keys())\n    else:\n        _keys_order = list(keys_order)\n        # Make all keys from `key_values` are passed\n        for key in key_values.keys():\n            if key not in _keys_order:\n                _keys_order.append(key)\n        keys_order = tuple(_keys_order)\n\n    log_parts = \" | \".join([\n        \"{}: \\\"{}\\\"\".format(*item)\n        for item in key_values.items()\n    ])\n\n    logger.debug(\n        \"Looking for matching profile for: {}\".format(log_parts)\n    )\n\n    matching_profiles = None\n    highest_profile_points = -1\n    # Each profile get 1 point for each matching filter. Profile with most\n    # points is returned. For cases when more than one profile will match\n    # are also stored ordered lists of matching values.\n    for profile in profiles_data:\n        profile_points = 0\n        profile_scores = []\n\n        for key in keys_order:\n            value = key_values[key]\n            match = validate_value_by_regexes(value, profile.get(key))\n            if match == -1:\n                profile_value = profile.get(key) or []\n                logger.debug(\n                    \"\\\"{}\\\" not found in \\\"{}\\\": {}\".format(value, key,\n                                                            profile_value)\n                )\n                profile_points = -1\n                break\n\n            profile_points += match\n            profile_scores.append(bool(match))\n\n        if (\n            profile_points &lt; 0\n            or profile_points &lt; highest_profile_points\n        ):\n            continue\n\n        if profile_points &gt; highest_profile_points:\n            matching_profiles = []\n            highest_profile_points = profile_points\n\n        if profile_points == highest_profile_points:\n            matching_profiles.append((profile, profile_scores))\n\n    if not matching_profiles:\n        logger.debug(\n            \"None of profiles match your setup. {}\".format(log_parts)\n        )\n        return None\n\n    if len(matching_profiles) &gt; 1:\n        logger.debug(\n            \"More than one profile match your setup. {}\".format(log_parts)\n        )\n\n    profile = _profile_exclusion(matching_profiles, logger)\n    if profile:\n        logger.debug(\n            \"Profile selected: {}\".format(profile)\n        )\n    return profile\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.find_executable","title":"<code>find_executable(executable)</code>","text":"<p>Find full path to executable.</p> <p>Also tries additional extensions if passed executable does not contain one.</p> <p>Paths where it is looked for executable is defined by 'PATH' environment variable, 'os.confstr(\"CS_PATH\")' or 'os.defpath'.</p> <p>Parameters:</p> Name Type Description Default <code>executable</code> <code>str</code> <p>Name of executable with or without extension. Can be path to file.</p> required <p>Returns:</p> Type Description <p>Union[str, None]: Full path to executable with extension which was found otherwise None.</p> Source code in <code>client/ayon_core/lib/vendor_bin_utils.py</code> <pre><code>def find_executable(executable):\n    \"\"\"Find full path to executable.\n\n    Also tries additional extensions if passed executable does not contain one.\n\n    Paths where it is looked for executable is defined by 'PATH' environment\n    variable, 'os.confstr(\"CS_PATH\")' or 'os.defpath'.\n\n    Args:\n        executable(str): Name of executable with or without extension. Can be\n            path to file.\n\n    Returns:\n        Union[str, None]: Full path to executable with extension which was\n            found otherwise None.\n    \"\"\"\n\n    # Skip if passed path is file\n    if is_file_executable(executable):\n        return executable\n\n    low_platform = platform.system().lower()\n    _, ext = os.path.splitext(executable)\n\n    # Prepare extensions to check\n    exts = set()\n    if ext:\n        exts.add(ext.lower())\n\n    else:\n        # Add other possible extension variants only if passed executable\n        #   does not have any\n        if low_platform == \"windows\":\n            exts |= {\".exe\", \".ps1\", \".bat\"}\n            for ext in os.getenv(\"PATHEXT\", \"\").split(os.pathsep):\n                exts.add(ext.lower())\n\n        else:\n            exts |= {\".sh\"}\n\n    # Executable is a path but there may be missing extension\n    #   - this can happen primarily on windows where\n    #       e.g. \"ffmpeg\" should be \"ffmpeg.exe\"\n    exe_dir, exe_filename = os.path.split(executable)\n    if exe_dir and os.path.isdir(exe_dir):\n        for filename in os.listdir(exe_dir):\n            filepath = os.path.join(exe_dir, filename)\n            basename, ext = os.path.splitext(filename)\n            if (\n                basename == exe_filename\n                and ext.lower() in exts\n                and is_file_executable(filepath)\n            ):\n                return filepath\n\n    # Get paths where to look for executable\n    path_str = os.environ.get(\"PATH\", None)\n    if path_str is None:\n        if hasattr(os, \"confstr\"):\n            path_str = os.confstr(\"CS_PATH\")\n        elif hasattr(os, \"defpath\"):\n            path_str = os.defpath\n\n    if not path_str:\n        return None\n\n    paths = path_str.split(os.pathsep)\n    for path in paths:\n        if not os.path.isdir(path):\n            continue\n        for filename in os.listdir(path):\n            filepath = os.path.abspath(os.path.join(path, filename))\n            # Filename matches executable exactly\n            if filename == executable and is_file_executable(filepath):\n                return filepath\n\n            basename, ext = os.path.splitext(filename)\n            if (\n                basename == executable\n                and ext.lower() in exts\n                and is_file_executable(filepath)\n            ):\n                return filepath\n\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.format_file_size","title":"<code>format_file_size(file_size, suffix=None)</code>","text":"<p>Returns formatted string with size in appropriate unit.</p> <p>Parameters:</p> Name Type Description Default <code>file_size</code> <code>int</code> <p>Size of file in bytes.</p> required <code>suffix</code> <code>str</code> <p>Suffix for formatted size. Default is 'B' (as bytes).</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <p>Formatted size using proper unit and passed suffix (e.g. 7 MiB).</p> Source code in <code>client/ayon_core/lib/path_tools.py</code> <pre><code>def format_file_size(file_size, suffix=None):\n    \"\"\"Returns formatted string with size in appropriate unit.\n\n    Args:\n        file_size (int): Size of file in bytes.\n        suffix (str): Suffix for formatted size. Default is 'B' (as bytes).\n\n    Returns:\n        str: Formatted size using proper unit and passed suffix (e.g. 7 MiB).\n    \"\"\"\n\n    if suffix is None:\n        suffix = \"B\"\n\n    for unit in [\"\", \"Ki\", \"Mi\", \"Gi\", \"Ti\", \"Pi\", \"Ei\", \"Zi\"]:\n        if abs(file_size) &lt; 1024.0:\n            return \"%3.1f%s%s\" % (file_size, unit, suffix)\n        file_size /= 1024.0\n    return \"%.1f%s%s\" % (file_size, \"Yi\", suffix)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.get_addons_resources_dir","title":"<code>get_addons_resources_dir(addon_name, *args)</code>","text":"<p>Get a directory for storing resources for addons.</p> <p>Some addons might need to store ad-hoc resources that are not part of     addon client package (e.g. because of size). Studio might define     dedicated directory to store them with 'AYON_ADDONS_RESOURCES_DIR'     environment variable. By default, is used 'addons_resources' in     launcher storage (might be shared across platforms).</p> <p>Parameters:</p> Name Type Description Default <code>addon_name</code> <code>str</code> <p>Addon name.</p> required <code>*args</code> <code>str</code> <p>Subfolders in the resources directory.</p> <code>()</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Path to resources directory.</p> Source code in <code>client/ayon_core/lib/local_settings.py</code> <pre><code>def get_addons_resources_dir(addon_name: str, *args) -&gt; str:\n    \"\"\"Get a directory for storing resources for addons.\n\n    Some addons might need to store ad-hoc resources that are not part of\n        addon client package (e.g. because of size). Studio might define\n        dedicated directory to store them with 'AYON_ADDONS_RESOURCES_DIR'\n        environment variable. By default, is used 'addons_resources' in\n        launcher storage (might be shared across platforms).\n\n    Args:\n        addon_name (str): Addon name.\n        *args (str): Subfolders in the resources directory.\n\n    Returns:\n        str: Path to resources directory.\n\n    \"\"\"\n    addons_resources_dir = os.getenv(\"AYON_ADDONS_RESOURCES_DIR\")\n    if not addons_resources_dir:\n        addons_resources_dir = get_launcher_storage_dir(\"addons_resources\")\n\n    return os.path.join(addons_resources_dir, addon_name, *args)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.get_ayon_launcher_args","title":"<code>get_ayon_launcher_args(*args)</code>","text":"<p>Arguments to run AYON launcher process.</p> <p>Arguments for subprocess when need to spawn new AYON launcher process.</p> Reasons <p>AYON launcher started from code has different executable set to     virtual env python and must have path to script as first argument     which is not needed for built application.</p> <p>Parameters:</p> Name Type Description Default <code>*args</code> <code>str</code> <p>Any arguments that will be added after executables.</p> <code>()</code> <p>Returns:</p> Type Description <p>list[str]: List of arguments to run AYON launcher process.</p> Source code in <code>client/ayon_core/lib/execute.py</code> <pre><code>def get_ayon_launcher_args(*args):\n    \"\"\"Arguments to run AYON launcher process.\n\n    Arguments for subprocess when need to spawn new AYON launcher process.\n\n    Reasons:\n        AYON launcher started from code has different executable set to\n            virtual env python and must have path to script as first argument\n            which is not needed for built application.\n\n    Args:\n        *args (str): Any arguments that will be added after executables.\n\n    Returns:\n        list[str]: List of arguments to run AYON launcher process.\n\n    \"\"\"\n    executable = os.environ[\"AYON_EXECUTABLE\"]\n    launch_args = [executable]\n\n    executable_filename = os.path.basename(executable)\n    if \"python\" in executable_filename.lower():\n        filepath = os.path.join(os.environ[\"AYON_ROOT\"], \"start.py\")\n        launch_args.append(filepath)\n\n    if args:\n        launch_args.extend(args)\n\n    return launch_args\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.get_ayon_user_entity","title":"<code>get_ayon_user_entity(username=None)</code>","text":"<p>AYON user entity used for templates and publishing.</p> Note <p>Usually only service and admin users can receive the full user entity.</p> <p>Parameters:</p> Name Type Description Default <code>username</code> <code>Optional[str]</code> <p>Username of the user. If not passed, then the current user in 'ayon_api' is used.</p> <code>None</code> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: User entity.</p> Source code in <code>client/ayon_core/lib/local_settings.py</code> <pre><code>def get_ayon_user_entity(username: Optional[str] = None) -&gt; dict[str, Any]:\n    \"\"\"AYON user entity used for templates and publishing.\n\n    Note:\n        Usually only service and admin users can receive the full user entity.\n\n    Args:\n        username (Optional[str]): Username of the user. If not passed, then\n            the current user in 'ayon_api' is used.\n\n    Returns:\n        dict[str, Any]: User entity.\n\n    \"\"\"\n    service_username = _get_ayon_service_username()\n    # Handle service user handling first\n    if service_username:\n        if username is None:\n            username = service_username\n        cache: CacheItem = _Cache.user_entities_by_name[username]\n        if not cache.is_valid:\n            if username == service_username:\n                user = ayon_api.get_user()\n            else:\n                user = ayon_api.get_user(username)\n            cache.update_data(user)\n        return copy.deepcopy(cache.get_data())\n\n    # Cache current user\n    current_user = None\n    if _Cache.username is None:\n        current_user = ayon_api.get_user()\n        _Cache.username = current_user[\"name\"]\n\n    if username is None:\n        username = _Cache.username\n\n    cache: CacheItem = _Cache.user_entities_by_name[username]\n    if not cache.is_valid:\n        user = None\n        if username == _Cache.username:\n            if current_user is None:\n                current_user = ayon_api.get_user()\n            user = current_user\n\n        if user is None:\n            user = ayon_api.get_user(username)\n        cache.update_data(user)\n\n    return copy.deepcopy(cache.get_data())\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.get_ayon_username","title":"<code>get_ayon_username()</code>","text":"<p>AYON username used for templates and publishing.</p> <p>Uses current ayon api username.</p> <p>Returns:</p> Name Type Description <code>str</code> <p>Username.</p> Source code in <code>client/ayon_core/lib/local_settings.py</code> <pre><code>def get_ayon_username():\n    \"\"\"AYON username used for templates and publishing.\n\n    Uses current ayon api username.\n\n    Returns:\n        str: Username.\n\n    \"\"\"\n    user = get_ayon_user_entity()\n    return user[\"name\"]\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.get_datetime_data","title":"<code>get_datetime_data(datetime_obj=None)</code>","text":"<p>Returns current datetime data as dictionary.</p> <p>Parameters:</p> Name Type Description Default <code>datetime_obj</code> <code>datetime</code> <p>Specific datetime object</p> <code>None</code> <p>Returns:</p> Name Type Description <code>dict</code> <p>prepared date &amp; time data</p> Available keys <p>\"d\" -  in shortest possible way. \"dd\" -  with 2 digits. \"ddd\" -  shortened week day. e.g.: <code>Mon</code>, ... \"dddd\" -  full name of week day. e.g.: <code>Monday</code>, ... \"m\" -  in shortest possible way. e.g.: <code>1</code> if January \"mm\" -  with 2 digits. \"mmm\" -  shortened month name. e.g.: <code>Jan</code>, ... \"mmmm\" -  full month name. e.g.: <code>January</code>, ... \"yy\" -  shortened year. e.g.: <code>19</code>, <code>20</code>, ... \"yyyy\" -  full year. e.g.: <code>2019</code>, <code>2020</code>, ... \"H\" -  shortened hours. \"HH\" -  with 2 digits. \"h\" -  shortened hours. \"hh\" -  with 2 digits. \"ht\" -  AM or PM. \"M\" -  shortened minutes. \"MM\" -  with 2 digits. \"S\" -  shortened seconds. \"SS\" -  with 2 digits. Source code in <code>client/ayon_core/lib/dateutils.py</code> <pre><code>def get_datetime_data(datetime_obj=None):\n    \"\"\"Returns current datetime data as dictionary.\n\n    Args:\n        datetime_obj (datetime): Specific datetime object\n\n    Returns:\n        dict: prepared date &amp; time data\n\n    Available keys:\n        \"d\" - &lt;Day of month number&gt; in shortest possible way.\n        \"dd\" - &lt;Day of month number&gt; with 2 digits.\n        \"ddd\" - &lt;Week day name&gt; shortened week day. e.g.: `Mon`, ...\n        \"dddd\" - &lt;Week day name&gt; full name of week day. e.g.: `Monday`, ...\n        \"m\" - &lt;Month number&gt; in shortest possible way. e.g.: `1` if January\n        \"mm\" - &lt;Month number&gt; with 2 digits.\n        \"mmm\" - &lt;Month name&gt; shortened month name. e.g.: `Jan`, ...\n        \"mmmm\" - &lt;Month name&gt; full month name. e.g.: `January`, ...\n        \"yy\" - &lt;Year number&gt; shortened year. e.g.: `19`, `20`, ...\n        \"yyyy\" - &lt;Year number&gt; full year. e.g.: `2019`, `2020`, ...\n        \"H\" - &lt;Hours number 24-hour&gt; shortened hours.\n        \"HH\" - &lt;Hours number 24-hour&gt; with 2 digits.\n        \"h\" - &lt;Hours number 12-hour&gt; shortened hours.\n        \"hh\" - &lt;Hours number 12-hour&gt; with 2 digits.\n        \"ht\" - &lt;Midday type&gt; AM or PM.\n        \"M\" - &lt;Minutes number&gt; shortened minutes.\n        \"MM\" - &lt;Minutes number&gt; with 2 digits.\n        \"S\" - &lt;Seconds number&gt; shortened seconds.\n        \"SS\" - &lt;Seconds number&gt; with 2 digits.\n    \"\"\"\n\n    if not datetime_obj:\n        datetime_obj = datetime.datetime.now()\n\n    year = datetime_obj.strftime(\"%Y\")\n\n    month = datetime_obj.strftime(\"%m\")\n    month_name_full = datetime_obj.strftime(\"%B\")\n    month_name_short = datetime_obj.strftime(\"%b\")\n    day = datetime_obj.strftime(\"%d\")\n\n    weekday_full = datetime_obj.strftime(\"%A\")\n    weekday_short = datetime_obj.strftime(\"%a\")\n\n    hours = datetime_obj.strftime(\"%H\")\n    hours_midday = datetime_obj.strftime(\"%I\")\n    hour_midday_type = datetime_obj.strftime(\"%p\")\n    minutes = datetime_obj.strftime(\"%M\")\n    seconds = datetime_obj.strftime(\"%S\")\n\n    return {\n        \"d\": str(int(day)),\n        \"dd\": str(day),\n        \"ddd\": weekday_short,\n        \"dddd\": weekday_full,\n        \"m\": str(int(month)),\n        \"mm\": str(month),\n        \"mmm\": month_name_short,\n        \"mmmm\": month_name_full,\n        \"yy\": str(year[2:]),\n        \"yyyy\": str(year),\n        \"H\": str(int(hours)),\n        \"HH\": str(hours),\n        \"h\": str(int(hours_midday)),\n        \"hh\": str(hours_midday),\n        \"ht\": hour_midday_type,\n        \"M\": str(int(minutes)),\n        \"MM\": str(minutes),\n        \"S\": str(int(seconds)),\n        \"SS\": str(seconds),\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.get_ffmpeg_codec_args","title":"<code>get_ffmpeg_codec_args(ffprobe_data, source_ffmpeg_cmd=None, logger=None)</code>","text":"<p>Copy codec from input metadata for output.</p> <p>Parameters:</p> Name Type Description Default <code>ffprobe_data</code> <code>dict</code> <p>Data received from ffprobe.</p> required <code>source_ffmpeg_cmd</code> <code>str</code> <p>Command that created input if available.</p> <code>None</code> Source code in <code>client/ayon_core/lib/transcoding.py</code> <pre><code>def get_ffmpeg_codec_args(ffprobe_data, source_ffmpeg_cmd=None, logger=None):\n    \"\"\"Copy codec from input metadata for output.\n\n    Args:\n        ffprobe_data(dict): Data received from ffprobe.\n        source_ffmpeg_cmd(str): Command that created input if available.\n    \"\"\"\n    if logger is None:\n        logger = logging.getLogger(__name__)\n\n    video_stream = None\n    no_audio_stream = None\n    for stream in ffprobe_data[\"streams\"]:\n        codec_type = stream[\"codec_type\"]\n        if codec_type == \"video\":\n            video_stream = stream\n            break\n        elif no_audio_stream is None and codec_type != \"audio\":\n            no_audio_stream = stream\n\n    if video_stream is None:\n        if no_audio_stream is None:\n            logger.warning(\n                \"Couldn't find stream that is not an audio file.\"\n            )\n            return []\n        logger.info(\n            \"Didn't find video stream. Using first non audio stream.\"\n        )\n        video_stream = no_audio_stream\n\n    codec_name = video_stream.get(\"codec_name\")\n    # Codec \"prores\"\n    if codec_name == \"prores\":\n        return _ffmpeg_prores_codec_args(video_stream, source_ffmpeg_cmd)\n\n    # Codec \"h264\"\n    if codec_name == \"h264\":\n        return _ffmpeg_h264_codec_args(video_stream, source_ffmpeg_cmd)\n\n    # Coded DNxHD\n    if codec_name == \"dnxhd\":\n        return _ffmpeg_dnxhd_codec_args(video_stream, source_ffmpeg_cmd)\n\n    output = []\n    if codec_name:\n        output.extend([\"-codec:v\", codec_name])\n\n    bit_rate = video_stream.get(\"bit_rate\")\n    if bit_rate:\n        output.extend([\"-b:v\", bit_rate])\n\n    pix_fmt = video_stream.get(\"pix_fmt\")\n    if pix_fmt:\n        output.extend([\"-pix_fmt\", pix_fmt])\n\n    output.extend([\"-g\", \"1\"])\n\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.get_ffmpeg_format_args","title":"<code>get_ffmpeg_format_args(ffprobe_data, source_ffmpeg_cmd=None)</code>","text":"<p>Copy format from input metadata for output.</p> <p>Parameters:</p> Name Type Description Default <code>ffprobe_data</code> <code>dict</code> <p>Data received from ffprobe.</p> required <code>source_ffmpeg_cmd</code> <code>str</code> <p>Command that created input if available.</p> <code>None</code> Source code in <code>client/ayon_core/lib/transcoding.py</code> <pre><code>def get_ffmpeg_format_args(ffprobe_data, source_ffmpeg_cmd=None):\n    \"\"\"Copy format from input metadata for output.\n\n    Args:\n        ffprobe_data(dict): Data received from ffprobe.\n        source_ffmpeg_cmd(str): Command that created input if available.\n    \"\"\"\n    input_format = ffprobe_data.get(\"format\") or {}\n    if input_format.get(\"format_name\") == \"mxf\":\n        return _ffmpeg_mxf_format_args(ffprobe_data, source_ffmpeg_cmd)\n    return []\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.get_ffmpeg_tool_args","title":"<code>get_ffmpeg_tool_args(tool_name, *extra_args)</code>","text":"<p>Arguments to launch FFmpeg tool.</p> <p>Parameters:</p> Name Type Description Default <code>tool_name</code> <code>str</code> <p>Tool name 'ffmpeg', 'ffprobe', exc.</p> required <code>*extra_args</code> <code>str</code> <p>Extra arguments to add to after tool arguments.</p> <code>()</code> <p>Returns:</p> Type Description <p>list[str]: List of arguments.</p> Source code in <code>client/ayon_core/lib/vendor_bin_utils.py</code> <pre><code>def get_ffmpeg_tool_args(tool_name, *extra_args):\n    \"\"\"Arguments to launch FFmpeg tool.\n\n    Args:\n        tool_name (str): Tool name 'ffmpeg', 'ffprobe', exc.\n        *extra_args (str): Extra arguments to add to after tool arguments.\n\n    Returns:\n        list[str]: List of arguments.\n    \"\"\"\n\n    extra_args = list(extra_args)\n\n    args = _get_ayon_ffmpeg_tool_args(tool_name)\n    if args:\n        return args + extra_args\n\n    executable_path = get_ffmpeg_tool_path(tool_name)\n    if executable_path:\n        return [executable_path] + extra_args\n    raise ToolNotFoundError(\n        \"FFmpeg '{}' tool not found.\".format(tool_name)\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.get_ffmpeg_tool_path","title":"<code>get_ffmpeg_tool_path(tool='ffmpeg')</code>","text":"<p>Path to vendorized FFmpeg executable.</p> <p>Parameters:</p> Name Type Description Default <code>tool</code> <code>str</code> <p>Tool name 'ffmpeg', 'ffprobe', etc. Default is \"ffmpeg\".</p> <code>'ffmpeg'</code> <p>Returns:</p> Name Type Description <code>str</code> <p>Full path to ffmpeg executable.</p> Source code in <code>client/ayon_core/lib/vendor_bin_utils.py</code> <pre><code>def get_ffmpeg_tool_path(tool=\"ffmpeg\"):\n    \"\"\"Path to vendorized FFmpeg executable.\n\n    Args:\n        tool (str): Tool name 'ffmpeg', 'ffprobe', etc.\n            Default is \"ffmpeg\".\n\n    Returns:\n        str: Full path to ffmpeg executable.\n    \"\"\"\n\n    if CachedToolPaths.is_tool_cached(tool):\n        return CachedToolPaths.get_executable_path(tool)\n\n    args = _get_ayon_ffmpeg_tool_args(tool)\n    if args is not None:\n        if len(args) &gt; 1:\n            raise ValueError(\n                \"AYON ffmpeg arguments consist of multiple arguments.\"\n            )\n        tool_executable_path = args[0]\n        CachedToolPaths.cache_executable_path(tool, tool_executable_path)\n        return tool_executable_path\n\n    custom_paths_str = os.environ.get(\"AYON_FFMPEG_PATHS\") or \"\"\n    tool_executable_path = find_tool_in_custom_paths(\n        custom_paths_str.split(os.pathsep),\n        tool,\n        _ffmpeg_executable_validation\n    )\n\n    # Look to PATH for the tool\n    if not tool_executable_path:\n        from_path = find_executable(tool)\n        if from_path and _ffmpeg_executable_validation(from_path):\n            tool_executable_path = from_path\n\n    CachedToolPaths.cache_executable_path(tool, tool_executable_path)\n    return tool_executable_path\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.get_ffprobe_data","title":"<code>get_ffprobe_data(path_to_file, logger=None)</code>","text":"<p>Load data about entered filepath via ffprobe.</p> <p>Parameters:</p> Name Type Description Default <code>path_to_file</code> <code>str</code> <p>absolute path</p> required <code>logger</code> <code>Logger</code> <p>injected logger, if empty new is created</p> <code>None</code> Source code in <code>client/ayon_core/lib/transcoding.py</code> <pre><code>def get_ffprobe_data(path_to_file, logger=None):\n    \"\"\"Load data about entered filepath via ffprobe.\n\n    Args:\n        path_to_file (str): absolute path\n        logger (logging.Logger): injected logger, if empty new is created\n    \"\"\"\n    if not logger:\n        logger = logging.getLogger(__name__)\n    logger.debug(\n        \"Getting information about input \\\"{}\\\".\".format(path_to_file)\n    )\n    ffprobe_args = get_ffmpeg_tool_args(\"ffprobe\")\n    args = ffprobe_args + [\n        \"-hide_banner\",\n        \"-loglevel\", \"fatal\",\n        \"-show_error\",\n        \"-show_format\",\n        \"-show_streams\",\n        \"-show_programs\",\n        \"-show_chapters\",\n        \"-show_private_data\",\n        \"-print_format\", \"json\",\n        path_to_file\n    ]\n\n    logger.debug(\"FFprobe command: {}\".format(\n        subprocess.list2cmdline(args)\n    ))\n    kwargs = {\n        \"stdout\": subprocess.PIPE,\n        \"stderr\": subprocess.PIPE,\n    }\n    if platform.system().lower() == \"windows\":\n        kwargs[\"creationflags\"] = (\n            subprocess.CREATE_NEW_PROCESS_GROUP\n            | getattr(subprocess, \"DETACHED_PROCESS\", 0)\n            | getattr(subprocess, \"CREATE_NO_WINDOW\", 0)\n        )\n\n    popen = subprocess.Popen(args, **kwargs)\n\n    popen_stdout, popen_stderr = popen.communicate()\n    if popen_stdout:\n        logger.debug(\"FFprobe stdout:\\n{}\".format(\n            popen_stdout.decode(\"utf-8\")\n        ))\n\n    if popen_stderr:\n        logger.warning(\"FFprobe stderr:\\n{}\".format(\n            popen_stderr.decode(\"utf-8\")\n        ))\n\n    return json.loads(popen_stdout)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.get_ffprobe_streams","title":"<code>get_ffprobe_streams(path_to_file, logger=None)</code>","text":"<p>Load streams from entered filepath via ffprobe.</p> <p>Parameters:</p> Name Type Description Default <code>path_to_file</code> <code>str</code> <p>absolute path</p> required <code>logger</code> <code>Logger</code> <p>injected logger, if empty new is created</p> <code>None</code> Source code in <code>client/ayon_core/lib/transcoding.py</code> <pre><code>def get_ffprobe_streams(path_to_file, logger=None):\n    \"\"\"Load streams from entered filepath via ffprobe.\n\n    Args:\n        path_to_file (str): absolute path\n        logger (logging.Logger): injected logger, if empty new is created\n    \"\"\"\n    return get_ffprobe_data(path_to_file, logger)[\"streams\"]\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.get_last_version_from_path","title":"<code>get_last_version_from_path(path_dir, filter)</code>","text":"<p>Find last version of given directory content.</p> <p>Parameters:</p> Name Type Description Default <code>path_dir</code> <code>str</code> <p>directory path</p> required <code>filter</code> <code>list</code> <p>list of strings used as file name filter</p> required <p>Returns:</p> Name Type Description <code>str</code> <p>file name with last version</p> Example <p>last_version_file = get_last_version_from_path(     \"/project/shots/shot01/work\", [\"shot01\", \"compositing\", \"nk\"])</p> Source code in <code>client/ayon_core/lib/path_tools.py</code> <pre><code>def get_last_version_from_path(path_dir, filter):\n    \"\"\"Find last version of given directory content.\n\n    Args:\n        path_dir (str): directory path\n        filter (list): list of strings used as file name filter\n\n    Returns:\n        str: file name with last version\n\n    Example:\n        last_version_file = get_last_version_from_path(\n            \"/project/shots/shot01/work\", [\"shot01\", \"compositing\", \"nk\"])\n    \"\"\"\n\n    assert os.path.isdir(path_dir), \"`path_dir` argument needs to be directory\"\n    assert isinstance(filter, list) and (\n        len(filter) != 0), \"`filter` argument needs to be list and not empty\"\n\n    filtered_files = list()\n\n    # form regex for filtering\n    pattern = r\".*\".join(filter)\n\n    for file in os.listdir(path_dir):\n        if not re.findall(pattern, file):\n            continue\n        filtered_files.append(file)\n\n    if filtered_files:\n        filtered_files.sort()\n        return filtered_files[-1]\n\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.get_launcher_local_dir","title":"<code>get_launcher_local_dir(*subdirs)</code>","text":"<p>Get a local directory for launcher.</p> <p>Local directory is used for storing machine or user-specific data.</p> <p>The location is user-specific.</p> Note <p>This function should be called at least once on the bootstrap.</p> <p>Parameters:</p> Name Type Description Default <code>*subdirs</code> <code>str</code> <p>Subdirectories relative to local dir.</p> <code>()</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Path to local directory.</p> Source code in <code>client/ayon_core/lib/local_settings.py</code> <pre><code>def get_launcher_local_dir(*subdirs: str) -&gt; str:\n    \"\"\"Get a local directory for launcher.\n\n    Local directory is used for storing machine or user-specific data.\n\n    The location is user-specific.\n\n    Note:\n        This function should be called at least once on the bootstrap.\n\n    Args:\n        *subdirs (str): Subdirectories relative to local dir.\n\n    Returns:\n        str: Path to local directory.\n\n    \"\"\"\n    storage_dir = os.getenv(\"AYON_LAUNCHER_LOCAL_DIR\")\n    if not storage_dir:\n        storage_dir = _get_ayon_appdirs()\n\n    return os.path.join(storage_dir, *subdirs)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.get_launcher_storage_dir","title":"<code>get_launcher_storage_dir(*subdirs)</code>","text":"<p>Get a storage directory for launcher.</p> <p>Storage directory is used for storing shims, addons, dependencies, etc.</p> <p>It is not recommended, but the location can be shared across     multiple machines.</p> Note <p>This function should be called at least once on bootstrap.</p> <p>Parameters:</p> Name Type Description Default <code>*subdirs</code> <code>str</code> <p>Subdirectories relative to storage dir.</p> <code>()</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Path to storage directory.</p> Source code in <code>client/ayon_core/lib/local_settings.py</code> <pre><code>def get_launcher_storage_dir(*subdirs: str) -&gt; str:\n    \"\"\"Get a storage directory for launcher.\n\n    Storage directory is used for storing shims, addons, dependencies, etc.\n\n    It is not recommended, but the location can be shared across\n        multiple machines.\n\n    Note:\n        This function should be called at least once on bootstrap.\n\n    Args:\n        *subdirs (str): Subdirectories relative to storage dir.\n\n    Returns:\n        str: Path to storage directory.\n\n    \"\"\"\n    storage_dir = os.getenv(\"AYON_LAUNCHER_STORAGE_DIR\")\n    if not storage_dir:\n        storage_dir = _get_ayon_appdirs()\n\n    return os.path.join(storage_dir, *subdirs)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.get_linux_launcher_args","title":"<code>get_linux_launcher_args(*args)</code>","text":"<p>Path to application mid process executable.</p> <p>This function should be able as arguments are different when used from code and build.</p> <p>It is possible that this function is used in AYON build which does not have yet the new executable. In that case 'None' is returned.</p> Todos <p>Replace by script in scripts for ayon-launcher.</p> <p>Parameters:</p> Name Type Description Default <code>args</code> <code>iterable</code> <p>List of additional arguments added after executable argument.</p> <code>()</code> <p>Returns:</p> Name Type Description <code>list</code> <p>Executables with possible positional argument to script when called from code.</p> Source code in <code>client/ayon_core/lib/execute.py</code> <pre><code>def get_linux_launcher_args(*args):\n    \"\"\"Path to application mid process executable.\n\n    This function should be able as arguments are different when used\n    from code and build.\n\n    It is possible that this function is used in AYON build which does\n    not have yet the new executable. In that case 'None' is returned.\n\n    Todos:\n        Replace by script in scripts for ayon-launcher.\n\n    Args:\n        args (iterable): List of additional arguments added after executable\n            argument.\n\n    Returns:\n        list: Executables with possible positional argument to script when\n            called from code.\n    \"\"\"\n    filename = \"app_launcher\"\n    executable = os.environ[\"AYON_EXECUTABLE\"]\n\n    executable_filename = os.path.basename(executable)\n    if \"python\" in executable_filename.lower():\n        root = os.environ[\"AYON_ROOT\"]\n        script_path = os.path.join(root, \"{}.py\".format(filename))\n        launch_args = [executable, script_path]\n    else:\n        new_executable = os.path.join(\n            os.path.dirname(executable),\n            filename\n        )\n        executable_path = find_executable(new_executable)\n        if executable_path is None:\n            return None\n        launch_args = [executable_path]\n\n    if args:\n        launch_args.extend(args)\n\n    return launch_args\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.get_local_site_id","title":"<code>get_local_site_id()</code>","text":"<p>Get local site identifier.</p> <p>Identifier is created if does not exist yet.</p> Source code in <code>client/ayon_core/lib/local_settings.py</code> <pre><code>def get_local_site_id():\n    \"\"\"Get local site identifier.\n\n    Identifier is created if does not exist yet.\n    \"\"\"\n    # used for background syncing\n    site_id = os.environ.get(\"AYON_SITE_ID\")\n    if site_id:\n        return site_id\n\n    site_id_path = get_launcher_local_dir(\"site_id\")\n    if os.path.exists(site_id_path):\n        with open(site_id_path, \"r\") as stream:\n            site_id = stream.read()\n\n    if site_id:\n        return site_id\n\n    try:\n        from ayon_common.utils import get_local_site_id as _get_local_site_id\n        site_id = _get_local_site_id()\n    except ImportError:\n        raise ValueError(\"Couldn't access local site id\")\n\n    return site_id\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.get_media_mime_type","title":"<code>get_media_mime_type(filepath)</code>","text":"<p>Determine Mime-Type of a file.</p> <p>Parameters:</p> Name Type Description Default <code>filepath</code> <code>str</code> <p>Path to file.</p> required <p>Returns:</p> Type Description <code>Optional[str]</code> <p>Optional[str]: Mime type or None if is unknown mime type.</p> Source code in <code>client/ayon_core/lib/transcoding.py</code> <pre><code>def get_media_mime_type(filepath: str) -&gt; Optional[str]:\n    \"\"\"Determine Mime-Type of a file.\n\n    Args:\n        filepath (str): Path to file.\n\n    Returns:\n        Optional[str]: Mime type or None if is unknown mime type.\n\n    \"\"\"\n    # The implementation is identical or better with ayon_api &gt;=1.1.0,\n    #   which is used in AYON launcher &gt;=1.3.0.\n    # NOTE Remove safe import when AYON launcher &gt;=1.2.0.\n    try:\n        from ayon_api.utils import (\n            get_media_mime_type_for_content as _ayon_api_func\n        )\n    except ImportError:\n        _ayon_api_func = None\n\n    if not filepath or not os.path.exists(filepath):\n        return None\n\n    with open(filepath, \"rb\") as stream:\n        content = stream.read()\n\n    if _ayon_api_func is not None:\n        mime_type = _ayon_api_func(content)\n        if mime_type is not None:\n            return mime_type\n\n    content_len = len(content)\n    # Pre-validation (largest definition check)\n    # - hopefully there cannot be media defined in less than 12 bytes\n    if content_len &lt; 12:\n        return None\n\n    # FTYP\n    if content[4:8] == b\"ftyp\":\n        return _get_media_mime_type_from_ftyp(content)\n\n    # BMP\n    if content[0:2] == b\"BM\":\n        return \"image/bmp\"\n\n    # Tiff\n    if content[0:2] in (b\"MM\", b\"II\"):\n        return \"tiff\"\n\n    # PNG\n    if content[0:4] == b\"\\211PNG\":\n        return \"image/png\"\n\n    # SVG\n    if b'xmlns=\"http://www.w3.org/2000/svg\"' in content:\n        return \"image/svg+xml\"\n\n    # JPEG\n    # - [0:2] is constant b\"\\xff\\xd8\"\n    #   (ref. https://www.file-recovery.com/jpg-signature-format.htm)\n    # - [2:4] Marker identifier b\"\\xff{?}\"\n    #   (ref. https://www.disktuna.com/list-of-jpeg-markers/)\n    # NOTE: File ends with b\"\\xff\\xd9\"\n    if content[0:3] == b\"\\xff\\xd8\\xff\":\n        return \"image/jpeg\"\n\n    # Webp\n    if content[0:4] == b\"RIFF\" and content[8:12] == b\"WEBP\":\n        return \"image/webp\"\n\n    # Gif\n    if content[0:6] in (b\"GIF87a\", b\"GIF89a\"):\n        return \"gif\"\n\n    # Adobe PhotoShop file (8B &gt; Adobe, PS &gt; PhotoShop)\n    if content[0:4] == b\"8BPS\":\n        return \"image/vnd.adobe.photoshop\"\n\n    # Windows ICO &gt; this might be wild guess as multiple files can start\n    #   with this header\n    if content[0:4] == b\"\\x00\\x00\\x01\\x00\":\n        return \"image/x-icon\"\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.get_oiio_tool_args","title":"<code>get_oiio_tool_args(tool_name, *extra_args)</code>","text":"<p>Arguments to launch OpenImageIO tool.</p> <p>Parameters:</p> Name Type Description Default <code>tool_name</code> <code>str</code> <p>Tool name 'oiiotool', 'maketx', etc.</p> required <code>*extra_args</code> <code>str</code> <p>Extra arguments to add to after tool arguments.</p> <code>()</code> <p>Returns:</p> Type Description <p>list[str]: List of arguments.</p> Source code in <code>client/ayon_core/lib/vendor_bin_utils.py</code> <pre><code>def get_oiio_tool_args(tool_name, *extra_args):\n    \"\"\"Arguments to launch OpenImageIO tool.\n\n    Args:\n        tool_name (str): Tool name 'oiiotool', 'maketx', etc.\n        *extra_args (str): Extra arguments to add to after tool arguments.\n\n    Returns:\n        list[str]: List of arguments.\n    \"\"\"\n\n    extra_args = list(extra_args)\n\n    args = _get_ayon_oiio_tool_args(tool_name)\n    if args:\n        return args + extra_args\n\n    path = get_oiio_tools_path(tool_name)\n    if path:\n        return [path] + extra_args\n    raise ToolNotFoundError(\n        \"OIIO '{}' tool not found.\".format(tool_name)\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.get_oiio_tools_path","title":"<code>get_oiio_tools_path(tool='oiiotool')</code>","text":"<p>Path to OpenImageIO tool executables.</p> <p>On Windows it adds .exe extension if missing from tool argument.</p> <p>Parameters:</p> Name Type Description Default <code>tool</code> <code>string</code> <p>Tool name 'oiiotool', 'maketx', etc. Default is \"oiiotool\".</p> <code>'oiiotool'</code> Source code in <code>client/ayon_core/lib/vendor_bin_utils.py</code> <pre><code>def get_oiio_tools_path(tool=\"oiiotool\"):\n    \"\"\"Path to OpenImageIO tool executables.\n\n    On Windows it adds .exe extension if missing from tool argument.\n\n    Args:\n        tool (string): Tool name 'oiiotool', 'maketx', etc.\n            Default is \"oiiotool\".\n    \"\"\"\n\n    if CachedToolPaths.is_tool_cached(tool):\n        return CachedToolPaths.get_executable_path(tool)\n\n    args = _get_ayon_oiio_tool_args(tool)\n    if args:\n        if len(args) &gt; 1:\n            raise ValueError(\n                \"AYON oiio arguments consist of multiple arguments.\"\n            )\n        tool_executable_path = args[0]\n        CachedToolPaths.cache_executable_path(tool, tool_executable_path)\n        return tool_executable_path\n\n    custom_paths_str = os.environ.get(\"AYON_OIIO_PATHS\") or \"\"\n    tool_executable_path = find_tool_in_custom_paths(\n        custom_paths_str.split(os.pathsep),\n        tool,\n        _oiio_executable_validation\n    )\n\n    # Look to PATH for the tool\n    if not tool_executable_path:\n        from_path = find_executable(tool)\n        if from_path and _oiio_executable_validation(from_path):\n            tool_executable_path = from_path\n\n    CachedToolPaths.cache_executable_path(tool, tool_executable_path)\n    return tool_executable_path\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.get_paths_from_environ","title":"<code>get_paths_from_environ(env_key=None, env_value=None, return_first=False)</code>","text":"<p>Return existing paths from specific environment variable.</p> <p>Parameters:</p> Name Type Description Default <code>env_key</code> <code>Optional[str]</code> <p>Environment key where should look for paths.</p> <code>None</code> <code>env_value</code> <code>Optional[str]</code> <p>Value of environment variable. Argument <code>env_key</code> is skipped if this argument is entered.</p> <code>None</code> <code>return_first</code> <code>bool</code> <p>Return first found value or return list of found paths. <code>None</code> or empty list returned if nothing found.</p> <code>False</code> <p>Returns:</p> Type Description <code>Optional[Union[str, list[str]]]</code> <p>Optional[Union[str, list[str]]]: Result of found path/s.</p> Source code in <code>client/ayon_core/lib/env_tools.py</code> <pre><code>def get_paths_from_environ(\n    env_key: Optional[str] = None,\n    env_value: Optional[str] = None,\n    return_first: bool = False,\n) -&gt; Optional[Union[str, list[str]]]:\n    \"\"\"Return existing paths from specific environment variable.\n\n    Args:\n        env_key (Optional[str]): Environment key where should look for paths.\n        env_value (Optional[str]): Value of environment variable.\n            Argument `env_key` is skipped if this argument is entered.\n        return_first (bool): Return first found value or return list of found\n            paths. `None` or empty list returned if nothing found.\n\n    Returns:\n        Optional[Union[str, list[str]]]: Result of found path/s.\n\n    \"\"\"\n    existing_paths = []\n    if not env_key and not env_value:\n        if return_first:\n            return None\n        return existing_paths\n\n    if env_value is None:\n        env_value = os.environ.get(env_key) or \"\"\n\n    path_items = env_value.split(os.pathsep)\n    for path in path_items:\n        # Skip empty string\n        if not path:\n            continue\n        # Normalize path\n        path = os.path.normpath(path)\n        # Check if path exists\n        if os.path.exists(path):\n            # Return path if `return_first` is set to True\n            if return_first:\n                return path\n            # Store path\n            existing_paths.append(path)\n\n    # Return None if none of paths exists\n    if return_first:\n        return None\n    # Return all existing paths from environment variable\n    return existing_paths\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.get_rescaled_command_arguments","title":"<code>get_rescaled_command_arguments(application, input_path, target_width, target_height, *, target_par=None, bg_color=None, review_layers=None, log=None)</code>","text":"<p>Get command arguments for rescaling input to target size.</p> <p>Parameters:</p> Name Type Description Default <code>application</code> <code>str</code> <p>Application for which command should be created. Currently supported are \"ffmpeg\" and \"oiiotool\".</p> required <code>input_path</code> <code>str</code> <p>Path to input file.</p> required <code>target_width</code> <code>int</code> <p>Width of target.</p> required <code>target_height</code> <code>int</code> <p>Height of target.</p> required <code>target_par</code> <code>Optional[float]</code> <p>Pixel aspect ratio of target.</p> <code>None</code> <code>bg_color</code> <code>Optional[list[int]]</code> <p>List of 8bit int values for background color. Should be in range 0 - 255.</p> <code>None</code> <code>log</code> <code>Optional[Logger]</code> <p>Logger used for logging.</p> <code>None</code> <code>review_layers</code> <code>Optional[list[str]]</code> <p>List of reviewable layers.</p> <code>None</code> <p>Returns:</p> Type Description <code>list[str]</code> <p>list[str]: List of command arguments.</p> Source code in <code>client/ayon_core/lib/transcoding.py</code> <pre><code>def get_rescaled_command_arguments(\n    application: str,\n    input_path: str,\n    target_width: int,\n    target_height: int,\n    *,\n    target_par: float = None,\n    bg_color: Optional[list[int]] = None,\n    review_layers: Optional[list[str]] = None,\n    log: Optional[logging.Logger] = None,\n) -&gt; list[str]:\n    \"\"\"Get command arguments for rescaling input to target size.\n\n    Args:\n        application (str): Application for which command should be created.\n            Currently supported are \"ffmpeg\" and \"oiiotool\".\n        input_path (str): Path to input file.\n        target_width (int): Width of target.\n        target_height (int): Height of target.\n        target_par (Optional[float]): Pixel aspect ratio of target.\n        bg_color (Optional[list[int]]): List of 8bit int values for\n            background color. Should be in range 0 - 255.\n        log (Optional[logging.Logger]): Logger used for logging.\n        review_layers (Optional[list[str]]): List of reviewable layers.\n\n    Returns:\n        list[str]: List of command arguments.\n    \"\"\"\n    command_args = []\n    target_par = target_par or 1.0\n    input_par = 1.0\n\n    input_height, input_width, stream_input_par = _get_image_dimensions(\n        application, input_path, log)\n    if stream_input_par:\n        input_par = (\n            float(stream_input_par.split(\":\")[0])\n            / float(stream_input_par.split(\":\")[1])\n        )\n    # recalculating input and target width\n    input_width = int(input_width * input_par)\n    target_width = int(target_width * target_par)\n\n    # calculate aspect ratios\n    target_aspect = float(target_width) / target_height\n    input_aspect = float(input_width) / input_height\n\n    # calculate scale size\n    scale_size = float(input_width) / target_width\n    if input_aspect &lt; target_aspect:\n        scale_size = float(input_height) / target_height\n\n    # calculate rescaled width and height\n    rescaled_width = int(input_width / scale_size)\n    rescaled_height = int(input_height / scale_size)\n\n    # calculate width and height shift\n    rescaled_width_shift = int((target_width - rescaled_width) / 2)\n    rescaled_height_shift = int((target_height - rescaled_height) / 2)\n\n    if application == \"ffmpeg\":\n        # create scale command\n        scale = \"scale={0}:{1}\".format(input_width, input_height)\n        pad = \"pad={0}:{1}:({2}-iw)/2:({3}-ih)/2\".format(\n            target_width,\n            target_height,\n            target_width,\n            target_height\n        )\n        if input_width &gt; target_width or input_height &gt; target_height:\n            scale = \"scale={0}:{1}\".format(rescaled_width, rescaled_height)\n            pad = \"pad={0}:{1}:{2}:{3}\".format(\n                target_width,\n                target_height,\n                rescaled_width_shift,\n                rescaled_height_shift\n            )\n\n        if bg_color:\n            color = convert_color_values(application, bg_color)\n            pad += \":{0}\".format(color)\n        command_args.extend([\"-vf\", \"{0},{1}\".format(scale, pad)])\n\n    elif application == \"oiiotool\":\n        input_info = get_oiio_info_for_input(\n            input_path,\n            verbose=False,\n            logger=log,\n        )\n        # Collect channels to export\n        _, channels_arg = get_oiio_input_and_channel_args(\n            input_info, alpha_default=1.0, review_layers=review_layers\n        )\n\n        command_args.extend([\n            # Tell oiiotool which channels should be put to top stack\n            #   (and output)\n            \"--ch\", channels_arg,\n            # Use first subimage\n            \"--subimage\", \"0\"\n        ])\n\n        if input_par != 1.0:\n            command_args.extend([\"--pixelaspect\", \"1\"])\n\n        width_shift = int((target_width - input_width) / 2)\n        height_shift = int((target_height - input_height) / 2)\n\n        # default resample is not scaling source image\n        resample = [\n            \"--resize\",\n            \"{0}x{1}\".format(input_width, input_height),\n            \"--origin\",\n            \"+{0}+{1}\".format(width_shift, height_shift),\n        ]\n        # scaled source image to target size\n        if input_width &gt; target_width or input_height &gt; target_height:\n            # form resample command\n            resample = [\n                \"--resize:filter=lanczos3\",\n                \"{0}x{1}\".format(rescaled_width, rescaled_height),\n                \"--origin\",\n                \"+{0}+{1}\".format(rescaled_width_shift, rescaled_height_shift),\n            ]\n        command_args.extend(resample)\n\n        fullsize = [\n            \"--fullsize\",\n            \"{0}x{1}\".format(target_width, target_height)\n        ]\n        if bg_color:\n            color = convert_color_values(application, bg_color)\n\n            fullsize.extend([\n                \"--pattern\",\n                \"constant:color={0}\".format(color),\n                \"{0}x{1}\".format(target_width, target_height),\n                \"4\",  # 4 channels\n                \"--over\"\n            ])\n        command_args.extend(fullsize)\n\n    else:\n        raise ValueError(\n            \"\\\"application\\\" input argument should \"\n            \"be either \\\"ffmpeg\\\" or \\\"oiiotool\\\"\"\n        )\n\n    return command_args\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.get_settings_variant","title":"<code>get_settings_variant()</code>","text":"<p>Get AYON settings variant.</p> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Settings variant.</p> Source code in <code>client/ayon_core/lib/ayon_info.py</code> <pre><code>def get_settings_variant() -&gt; str:\n    \"\"\"Get AYON settings variant.\n\n    Returns:\n        str: Settings variant.\n\n    \"\"\"\n    if is_dev_mode_enabled():\n        return os.environ[\"AYON_BUNDLE_NAME\"]\n\n    if is_staging_enabled():\n        return \"staging\"\n\n    return \"production\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.get_timestamp","title":"<code>get_timestamp(datetime_obj=None)</code>","text":"<p>Get standardized timestamp from datetime object.</p> <p>Parameters:</p> Name Type Description Default <code>datetime_obj</code> <code>datetime</code> <p>Object of datetime. Current time is used if not passed.</p> <code>None</code> Source code in <code>client/ayon_core/lib/dateutils.py</code> <pre><code>def get_timestamp(datetime_obj=None):\n    \"\"\"Get standardized timestamp from datetime object.\n\n    Args:\n        datetime_obj (datetime.datetime): Object of datetime. Current time\n            is used if not passed.\n    \"\"\"\n\n    if datetime_obj is None:\n        datetime_obj = datetime.datetime.now()\n    return datetime_obj.strftime(\n        \"%Y%m%dT%H%M%SZ\"\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.get_transcode_temp_directory","title":"<code>get_transcode_temp_directory()</code>","text":"<p>Creates temporary folder for transcoding.</p> <p>Its local, in case of farm it is 'local' to the farm machine.</p> <p>Should be much faster, needs to be cleaned up later.</p> Source code in <code>client/ayon_core/lib/transcoding.py</code> <pre><code>def get_transcode_temp_directory():\n    \"\"\"Creates temporary folder for transcoding.\n\n    Its local, in case of farm it is 'local' to the farm machine.\n\n    Should be much faster, needs to be cleaned up later.\n    \"\"\"\n    return os.path.normpath(\n        tempfile.mkdtemp(prefix=\"op_transcoding_\")\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.get_version_from_path","title":"<code>get_version_from_path(file)</code>","text":"<p>Find version number in file path string.</p> <p>Parameters:</p> Name Type Description Default <code>file</code> <code>str</code> <p>file path</p> required <p>Returns:</p> Name Type Description <code>str</code> <p>version number in string ('001')</p> Source code in <code>client/ayon_core/lib/path_tools.py</code> <pre><code>def get_version_from_path(file):\n    \"\"\"Find version number in file path string.\n\n    Args:\n        file (str): file path\n\n    Returns:\n        str: version number in string ('001')\n    \"\"\"\n\n    pattern = re.compile(r\"[\\._]v([0-9]+)\", re.IGNORECASE)\n    try:\n        return pattern.findall(file)[-1]\n    except IndexError:\n        log.error(\n            \"templates:get_version_from_workfile:\"\n            \"`{}` missing version string.\"\n            \"Example `v004`\".format(file)\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.import_filepath","title":"<code>import_filepath(filepath, module_name=None, sys_module_name=None)</code>","text":"<p>Import python file as python module.</p> <p>Parameters:</p> Name Type Description Default <code>filepath</code> <code>str</code> <p>Path to python file.</p> required <code>module_name</code> <code>str</code> <p>Name of loaded module. Only for Python 3. By default is filled with filename of filepath.</p> <code>None</code> <code>sys_module_name</code> <code>str</code> <p>Name of module in <code>sys.modules</code> where to store loaded module. By default is None so module is not added to <code>sys.modules</code>.</p> <code>None</code> <p>Todo (antirotor): We should add the module to the sys.modules always but     we need to be careful about it and test it properly.</p> Source code in <code>client/ayon_core/lib/python_module_tools.py</code> <pre><code>def import_filepath(\n        filepath: str,\n        module_name: Optional[str] = None,\n        sys_module_name: Optional[str] = None) -&gt; types.ModuleType:\n    \"\"\"Import python file as python module.\n\n    Args:\n        filepath (str): Path to python file.\n        module_name (str): Name of loaded module. Only for Python 3. By default\n            is filled with filename of filepath.\n        sys_module_name (str): Name of module in `sys.modules` where to store\n            loaded module. By default is None so module is not added to\n            `sys.modules`.\n\n    Todo (antirotor): We should add the module to the sys.modules always but\n        we need to be careful about it and test it properly.\n\n    \"\"\"\n    if module_name is None:\n        module_name = os.path.splitext(os.path.basename(filepath))[0]\n\n    # Prepare module object where content of file will be parsed\n    module = types.ModuleType(module_name)\n    module.__file__ = filepath\n\n    # Use loader so module has full specs\n    module_loader = importlib.machinery.SourceFileLoader(\n        module_name, filepath\n    )\n    # only add to sys.modules if requested\n    if sys_module_name:\n        sys.modules[sys_module_name] = module\n    module_loader.exec_module(module)\n    return module\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.import_module_from_dirpath","title":"<code>import_module_from_dirpath(dirpath, folder_name, dst_module_name=None)</code>","text":"<p>Import passed directory as a python module.</p> <p>Imported module can be assigned as a child attribute of already loaded module from <code>sys.modules</code> if has support of <code>setattr</code>. That is not default behavior of python modules so parent module must be a custom module with that ability.</p> <p>It is not possible to reimport already cached module. If you need to reimport module you have to remove it from caches manually.</p> <p>Parameters:</p> Name Type Description Default <code>dirpath</code> <code>str</code> <p>Parent directory path of loaded folder.</p> required <code>folder_name</code> <code>str</code> <p>Folder name which should be imported inside passed directory.</p> required <code>dst_module_name</code> <code>str</code> <p>Parent module name under which can be loaded module added.</p> <code>None</code> Source code in <code>client/ayon_core/lib/python_module_tools.py</code> <pre><code>def import_module_from_dirpath(\n        dirpath, folder_name, dst_module_name=None):\n    \"\"\"Import passed directory as a python module.\n\n    Imported module can be assigned as a child attribute of already loaded\n    module from `sys.modules` if has support of `setattr`. That is not default\n    behavior of python modules so parent module must be a custom module with\n    that ability.\n\n    It is not possible to reimport already cached module. If you need to\n    reimport module you have to remove it from caches manually.\n\n    Args:\n        dirpath (str): Parent directory path of loaded folder.\n        folder_name (str): Folder name which should be imported inside passed\n            directory.\n        dst_module_name (str): Parent module name under which can be loaded\n            module added.\n\n    \"\"\"\n    # Import passed dirpath as python module\n    if dst_module_name:\n        full_module_name = \"{}.{}\".format(dst_module_name, folder_name)\n        dst_module = sys.modules[dst_module_name]\n    else:\n        full_module_name = folder_name\n        dst_module = None\n\n    # Skip import if is already imported\n    if full_module_name in sys.modules:\n        return sys.modules[full_module_name]\n\n    import importlib.util\n    from importlib._bootstrap_external import PathFinder\n\n    # Find loader for passed path and name\n    loader = PathFinder.find_module(full_module_name, [dirpath])\n\n    # Load specs of module\n    spec = importlib.util.spec_from_loader(\n        full_module_name, loader, origin=dirpath\n    )\n\n    # Create module based on specs\n    module = importlib.util.module_from_spec(spec)\n\n    # Store module to destination module and `sys.modules`\n    # WARNING this mus be done before module execution\n    if dst_module is not None:\n        setattr(dst_module, folder_name, module)\n\n    sys.modules[full_module_name] = module\n\n    # Execute module import\n    loader.exec_module(module)\n\n    return module\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.initialize_ayon_connection","title":"<code>initialize_ayon_connection(force=False)</code>","text":"<p>Initialize global AYON api connection.</p> <p>Create global connection in ayon_api module and set site id     and client version. Is silently skipped if already happened.</p> <p>Parameters:</p> Name Type Description Default <code>force</code> <code>Optional[bool]</code> <p>Force reinitialize connection. Defaults to False.</p> <code>False</code> Source code in <code>client/ayon_core/lib/ayon_connection.py</code> <pre><code>def initialize_ayon_connection(force=False):\n    \"\"\"Initialize global AYON api connection.\n\n    Create global connection in ayon_api module and set site id\n        and client version.\n    Is silently skipped if already happened.\n\n    Args:\n        force (Optional[bool]): Force reinitialize connection.\n            Defaults to False.\n\n    \"\"\"\n    if not force and _Cache.initialized:\n        return\n\n    _Cache.initialized = True\n    ayon_api_version = (\n        semver.VersionInfo.parse(ayon_api.__version__).to_tuple()\n    )\n    # TODO remove mokey patching after when AYON api is safely updated\n    fix_before_1_0_2 = ayon_api_version &lt; (1, 0, 2)\n    # Monkey patching to fix 'get_last_version_by_product_name'\n    if fix_before_1_0_2:\n        ayon_api.ServerAPI.get_last_versions = (\n            _new_get_last_versions\n        )\n        ayon_api.ServerAPI.get_last_version_by_product_id = (\n            _new_get_last_version_by_product_id\n        )\n        ayon_api.ServerAPI.get_last_version_by_product_name = (\n            _new_get_last_version_by_product_name\n        )\n\n    site_id = get_local_site_id()\n    version = os.getenv(\"AYON_VERSION\")\n    if ayon_api.is_connection_created():\n        con = ayon_api.get_server_api_connection()\n        # Monkey patching to fix 'get_last_version_by_product_name'\n        if fix_before_1_0_2:\n            def _lvs_wrapper(*args, **kwargs):\n                return _new_get_last_versions(\n                    con, *args, **kwargs\n                )\n\n            def _lv_by_pi_wrapper(*args, **kwargs):\n                return _new_get_last_version_by_product_id(\n                    con, *args, **kwargs\n                )\n\n            def _lv_by_pn_wrapper(*args, **kwargs):\n                return _new_get_last_version_by_product_name(\n                    con, *args, **kwargs\n                )\n            con.get_last_versions = _lvs_wrapper\n            con.get_last_version_by_product_id = _lv_by_pi_wrapper\n            con.get_last_version_by_product_name = _lv_by_pn_wrapper\n        con.set_site_id(site_id)\n        con.set_client_version(version)\n    else:\n        ayon_api.create_connection(site_id, version)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.is_dev_mode_enabled","title":"<code>is_dev_mode_enabled()</code>","text":"<p>Dev mode is enabled in AYON.</p> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if dev mode is enabled.</p> Source code in <code>client/ayon_core/lib/ayon_info.py</code> <pre><code>def is_dev_mode_enabled() -&gt; bool:\n    \"\"\"Dev mode is enabled in AYON.\n\n    Returns:\n        bool: True if dev mode is enabled.\n    \"\"\"\n\n    return os.getenv(\"AYON_USE_DEV\") == \"1\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.is_func_signature_supported","title":"<code>is_func_signature_supported(func, *args, **kwargs)</code>","text":"<p>Check if a function signature supports passed args and kwargs.</p> <p>This check does not actually call the function, just look if function can be called with the arguments.</p> Notes <p>This does NOT check if the function would work with passed arguments     only if they can be passed in. If function have args, *kwargs     in parameters, this will always return 'True'.</p> Example <p>def my_function(my_number): ...     return my_number + 1 ... is_func_signature_supported(my_function, 1) True is_func_signature_supported(my_function, 1, 2) False is_func_signature_supported(my_function, my_number=1) True is_func_signature_supported(my_function, number=1) False is_func_signature_supported(my_function, \"string\") True def my_other_function(args, kwargs): ...     my_function(args, **kwargs) ... is_func_signature_supported( ...     my_other_function, ...     \"string\", ...     1, ...     other=None ... ) True</p> <p>Parameters:</p> Name Type Description Default <code>func</code> <code>Callable</code> <p>A function where the signature should be tested.</p> required <code>*args</code> <code>Any</code> <p>Positional arguments for function signature.</p> <code>()</code> <code>**kwargs</code> <code>Any</code> <p>Keyword arguments for function signature.</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>bool</code> <p>Function can pass in arguments.</p> Source code in <code>client/ayon_core/lib/python_module_tools.py</code> <pre><code>def is_func_signature_supported(func, *args, **kwargs):\n    \"\"\"Check if a function signature supports passed args and kwargs.\n\n    This check does not actually call the function, just look if function can\n    be called with the arguments.\n\n    Notes:\n        This does NOT check if the function would work with passed arguments\n            only if they can be passed in. If function have *args, **kwargs\n            in parameters, this will always return 'True'.\n\n    Example:\n        &gt;&gt;&gt; def my_function(my_number):\n        ...     return my_number + 1\n        ...\n        &gt;&gt;&gt; is_func_signature_supported(my_function, 1)\n        True\n        &gt;&gt;&gt; is_func_signature_supported(my_function, 1, 2)\n        False\n        &gt;&gt;&gt; is_func_signature_supported(my_function, my_number=1)\n        True\n        &gt;&gt;&gt; is_func_signature_supported(my_function, number=1)\n        False\n        &gt;&gt;&gt; is_func_signature_supported(my_function, \"string\")\n        True\n        &gt;&gt;&gt; def my_other_function(*args, **kwargs):\n        ...     my_function(*args, **kwargs)\n        ...\n        &gt;&gt;&gt; is_func_signature_supported(\n        ...     my_other_function,\n        ...     \"string\",\n        ...     1,\n        ...     other=None\n        ... )\n        True\n\n    Args:\n        func (Callable): A function where the signature should be tested.\n        *args (Any): Positional arguments for function signature.\n        **kwargs (Any): Keyword arguments for function signature.\n\n    Returns:\n        bool: Function can pass in arguments.\n\n    \"\"\"\n    sig = inspect.signature(func)\n    try:\n        sig.bind(*args, **kwargs)\n        return True\n    except TypeError:\n        pass\n    return False\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.is_in_ayon_launcher_process","title":"<code>is_in_ayon_launcher_process()</code>","text":"<p>Determine if current process is running from AYON launcher.</p> <p>Returns:</p> Name Type Description <code>bool</code> <p>True if running from AYON launcher.</p> Source code in <code>client/ayon_core/lib/ayon_info.py</code> <pre><code>def is_in_ayon_launcher_process():\n    \"\"\"Determine if current process is running from AYON launcher.\n\n    Returns:\n        bool: True if running from AYON launcher.\n\n    \"\"\"\n    ayon_executable_path = os.path.normpath(os.environ[\"AYON_EXECUTABLE\"])\n    executable_path = os.path.normpath(sys.executable)\n    return ayon_executable_path == executable_path\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.is_in_tests","title":"<code>is_in_tests()</code>","text":"<p>Process is running in automatic tests mode.</p> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if running in tests.</p> Source code in <code>client/ayon_core/lib/ayon_info.py</code> <pre><code>def is_in_tests() -&gt; bool:\n    \"\"\"Process is running in automatic tests mode.\n\n    Returns:\n        bool: True if running in tests.\n\n    \"\"\"\n    return os.environ.get(\"AYON_IN_TESTS\") == \"1\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.is_oiio_supported","title":"<code>is_oiio_supported()</code>","text":"<p>Checks if oiiotool is configured for this platform.</p> <p>Returns:</p> Name Type Description <code>bool</code> <p>OIIO tool executable is available.</p> Source code in <code>client/ayon_core/lib/vendor_bin_utils.py</code> <pre><code>def is_oiio_supported():\n    \"\"\"Checks if oiiotool is configured for this platform.\n\n    Returns:\n        bool: OIIO tool executable is available.\n    \"\"\"\n\n    try:\n        args = get_oiio_tool_args(\"oiiotool\")\n    except ToolNotFoundError:\n        args = None\n    if not args:\n        log.debug(\"OIIOTool is not configured or not present.\")\n        return False\n    return _oiio_executable_validation(args)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.is_running_from_build","title":"<code>is_running_from_build()</code>","text":"<p>Determine if current process is running from build or code.</p> <p>Returns:</p> Name Type Description <code>bool</code> <p>True if running from build.</p> Source code in <code>client/ayon_core/lib/ayon_info.py</code> <pre><code>def is_running_from_build():\n    \"\"\"Determine if current process is running from build or code.\n\n    Returns:\n        bool: True if running from build.\n\n    \"\"\"\n    executable_path = os.environ[\"AYON_EXECUTABLE\"]\n    executable_filename = os.path.basename(executable_path)\n    if \"python\" in executable_filename.lower():\n        return False\n    return True\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.is_using_ayon_console","title":"<code>is_using_ayon_console()</code>","text":"<p>AYON launcher console executable is used.</p> <p>This function make sense only on Windows platform. For other platforms always returns True. True is also returned if process is running from code.</p> <p>AYON launcher on windows has 2 executable files. First 'ayon_console.exe' works as 'python.exe' executable, the second 'ayon.exe' works as 'pythonw.exe' executable. The difference is way how stdout/stderr is handled (especially when calling subprocess).</p> <p>Returns:</p> Name Type Description <code>bool</code> <p>True if console executable is used.</p> Source code in <code>client/ayon_core/lib/ayon_info.py</code> <pre><code>def is_using_ayon_console():\n    \"\"\"AYON launcher console executable is used.\n\n    This function make sense only on Windows platform. For other platforms\n    always returns True. True is also returned if process is running from\n    code.\n\n    AYON launcher on windows has 2 executable files. First 'ayon_console.exe'\n    works as 'python.exe' executable, the second 'ayon.exe' works as\n    'pythonw.exe' executable. The difference is way how stdout/stderr is\n    handled (especially when calling subprocess).\n\n    Returns:\n        bool: True if console executable is used.\n\n    \"\"\"\n    if (\n        platform.system().lower() != \"windows\"\n        or is_running_from_build()\n    ):\n        return True\n    executable_path = os.environ[\"AYON_EXECUTABLE\"]\n    executable_filename = os.path.basename(executable_path)\n    return \"ayon_console\" in executable_filename\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.merge_env_variables","title":"<code>merge_env_variables(src_env, dst_env, missing_template=None)</code>","text":"<p>Merge the tools environment with the 'current_env'.</p> <p>This finalizes the join with a current environment by formatting the remainder of dynamic variables with that from the current environment.</p> <p>Remaining missing variables result in an empty value.</p> <p>Parameters:</p> Name Type Description Default <code>src_env</code> <code>dict</code> <p>The dynamic environment</p> required <code>dst_env</code> <code>dict</code> <p>The target environment variables mapping to merge the dynamic environment into.</p> required <code>missing_template</code> <code>str</code> <p>Argument passed to '_partial_format' during merging. <code>None</code> should keep missing keys unchanged.</p> <code>None</code> <p>Returns:</p> Type Description <code>dict[str, str]</code> <p>dict[str, str]: The resulting environment after the merge.</p> Source code in <code>client/ayon_core/lib/env_tools.py</code> <pre><code>def merge_env_variables(\n    src_env: dict[str, str],\n    dst_env: dict[str, str],\n    missing_template: Optional[str] = None,\n) -&gt; dict[str, str]:\n    \"\"\"Merge the tools environment with the 'current_env'.\n\n    This finalizes the join with a current environment by formatting the\n    remainder of dynamic variables with that from the current environment.\n\n    Remaining missing variables result in an empty value.\n\n    Args:\n        src_env (dict): The dynamic environment\n        dst_env (dict): The target environment variables mapping to merge\n            the dynamic environment into.\n        missing_template (str): Argument passed to '_partial_format' during\n            merging. `None` should keep missing keys unchanged.\n\n    Returns:\n        dict[str, str]: The resulting environment after the merge.\n\n    \"\"\"\n    result = dst_env.copy()\n    for key, value in src_env.items():\n        result[key] = _partial_format(\n            str(value), dst_env, missing_template\n        )\n\n    return result\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.modules_from_path","title":"<code>modules_from_path(folder_path)</code>","text":"<p>Get python scripts as modules from a path.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Path to folder containing python scripts.</p> required <p>Returns:</p> Type Description <p>tuple: First list contains successfully imported modules and second list contains tuples of path and exception. Source code in <code>client/ayon_core/lib/python_module_tools.py</code> <pre><code>def modules_from_path(folder_path):\n    \"\"\"Get python scripts as modules from a path.\n\n    Arguments:\n        path (str): Path to folder containing python scripts.\n\n    Returns:\n        tuple&lt;list, list&gt;: First list contains successfully imported modules\n            and second list contains tuples of path and exception.\n    \"\"\"\n    crashed = []\n    modules = []\n    output = (modules, crashed)\n    # Just skip and return empty list if path is not set\n    if not folder_path:\n        return output\n\n    # Do not allow relative imports\n    if folder_path.startswith(\".\"):\n        log.warning((\n            \"BUG: Relative paths are not allowed for security reasons. {}\"\n        ).format(folder_path))\n        return output\n\n    folder_path = os.path.normpath(folder_path)\n\n    if not os.path.isdir(folder_path):\n        log.warning(\"Not a directory path: {}\".format(folder_path))\n        return output\n\n    for filename in os.listdir(folder_path):\n        # Ignore files which start with underscore\n        if filename.startswith(\"_\"):\n            continue\n\n        mod_name, mod_ext = os.path.splitext(filename)\n        if not mod_ext == \".py\":\n            continue\n\n        full_path = os.path.join(folder_path, filename)\n        if not os.path.isfile(full_path):\n            continue\n\n        try:\n            module = import_filepath(full_path, mod_name)\n            modules.append((full_path, module))\n\n        except Exception:\n            crashed.append((full_path, sys.exc_info()))\n            log.warning(\n                \"Failed to load path: \\\"{0}\\\"\".format(full_path),\n                exc_info=True\n            )\n            continue\n\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.path_to_subprocess_arg","title":"<code>path_to_subprocess_arg(path)</code>","text":"<p>Prepare path for subprocess arguments.</p> <p>Returned path can be wrapped with quotes or kept as is.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Path to be converted.</p> required <p>Returns:</p> Name Type Description <code>str</code> <p>Converted path.</p> Source code in <code>client/ayon_core/lib/execute.py</code> <pre><code>def path_to_subprocess_arg(path):\n    \"\"\"Prepare path for subprocess arguments.\n\n    Returned path can be wrapped with quotes or kept as is.\n\n    Args:\n        path (str): Path to be converted.\n\n    Returns:\n        str: Converted path.\n    \"\"\"\n    return subprocess.list2cmdline([path])\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.prepare_template_data","title":"<code>prepare_template_data(fill_pairs)</code>","text":"<p>Prepares formatted data for filling template.</p> <p>It produces multiple variants of keys (key, Key, KEY) to control format of filled template.</p> Example <p>src_data = { ...    \"host\": \"maya\", ... } output = prepare_template_data(src_data) sorted(list(output.items())) # sort &amp; list conversion for tests [('HOST', 'MAYA'), ('Host', 'Maya'), ('host', 'maya')]</p> <p>Parameters:</p> Name Type Description Default <code>fill_pairs</code> <code>Union[dict[str, Any], Iterable[Tuple[str, Any]]]</code> <p>The value that are prepared for template.</p> required <p>Returns:</p> Type Description <p>dict[str, str]: Prepared values for template.</p> Source code in <code>client/ayon_core/lib/plugin_tools.py</code> <pre><code>def prepare_template_data(fill_pairs):\n    \"\"\"Prepares formatted data for filling template.\n\n    It produces multiple variants of keys (key, Key, KEY) to control\n    format of filled template.\n\n    Example:\n        &gt;&gt;&gt; src_data = {\n        ...    \"host\": \"maya\",\n        ... }\n        &gt;&gt;&gt; output = prepare_template_data(src_data)\n        &gt;&gt;&gt; sorted(list(output.items())) # sort &amp; list conversion for tests\n        [('HOST', 'MAYA'), ('Host', 'Maya'), ('host', 'maya')]\n\n    Args:\n        fill_pairs (Union[dict[str, Any], Iterable[Tuple[str, Any]]]): The\n            value that are prepared for template.\n\n    Returns:\n        dict[str, str]: Prepared values for template.\n    \"\"\"\n\n    valid_items = _separate_keys_and_value(fill_pairs)\n    output = {}\n    for item in valid_items:\n        keys, value = item\n        # Convert only string values\n        if isinstance(value, str):\n            upper_value = value.upper()\n            capitalized_value = _capitalize_value(value)\n        else:\n            upper_value = capitalized_value = value\n\n        first_key = keys.pop(0)\n        if not keys:\n            output[first_key] = value\n            output[first_key.upper()] = upper_value\n            output[first_key.capitalize()] = capitalized_value\n            continue\n\n        # Prepare 'normal', 'upper' and 'capitalized' variables\n        normal = output.setdefault(first_key, {})\n        capitalized = output.setdefault(first_key.capitalize(), {})\n        upper = output.setdefault(first_key.upper(), {})\n\n        keys_deque = collections.deque(keys)\n        while keys_deque:\n            key = keys_deque.popleft()\n            upper_key = key\n            if isinstance(key, str):\n                upper_key = key.upper()\n\n            if not keys_deque:\n                # Fill value on last key\n                upper[upper_key] = upper_value\n                capitalized[key] = capitalized_value\n                normal[key] = value\n            else:\n                normal = normal.setdefault(key, {})\n                capitalized = capitalized.setdefault(key, {})\n                upper = upper.setdefault(upper_key, {})\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.recursive_bases_from_class","title":"<code>recursive_bases_from_class(klass)</code>","text":"<p>Extract all bases from entered class.</p> Source code in <code>client/ayon_core/lib/python_module_tools.py</code> <pre><code>def recursive_bases_from_class(klass):\n    \"\"\"Extract all bases from entered class.\"\"\"\n    result = []\n    bases = klass.__bases__\n    result.extend(bases)\n    for base in bases:\n        result.extend(recursive_bases_from_class(base))\n    return result\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.register_event_callback","title":"<code>register_event_callback(topic, callback)</code>","text":"<p>Add callback that will be executed on specific topic.</p> <p>Parameters:</p> Name Type Description Default <code>topic</code> <code>str</code> <p>Topic on which will callback be triggered.</p> required <code>callback</code> <code>function</code> <p>Callback that will be triggered when a topic is triggered. Callback should expect none or 1 argument where <code>Event</code> object is passed.</p> required <p>Returns:</p> Name Type Description <code>EventCallback</code> <p>Object wrapping the callback. It can be used to enable/disable listening to a topic or remove the callback from the topic completely.</p> Source code in <code>client/ayon_core/lib/events.py</code> <pre><code>def register_event_callback(topic, callback):\n    \"\"\"Add callback that will be executed on specific topic.\n\n    Args:\n        topic(str): Topic on which will callback be triggered.\n        callback(function): Callback that will be triggered when a topic\n            is triggered. Callback should expect none or 1 argument where\n            `Event` object is passed.\n\n    Returns:\n        EventCallback: Object wrapping the callback. It can be used to\n            enable/disable listening to a topic or remove the callback from\n            the topic completely.\n    \"\"\"\n\n    return GlobalEventSystem.add_callback(topic, callback)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.run_ayon_launcher_process","title":"<code>run_ayon_launcher_process(*args, add_sys_paths=False, **kwargs)</code>","text":"<p>Execute AYON process with passed arguments and wait.</p> <p>Wrapper for 'run_process' which prepends AYON executable arguments before passed arguments and define environments if are not passed.</p> <p>Values from 'os.environ' are used for environments if are not passed. They are cleaned using 'clean_envs_for_ayon_process' function.</p> <p>Example:</p> <pre><code>run_ayon_launcher_process(\"run\", \"&lt;path to .py script&gt;\")\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>*args</code> <code>str</code> <p>ayon-launcher cli arguments.</p> <code>()</code> <code>add_sys_paths</code> <code>bool</code> <p>Add system paths to PYTHONPATH.</p> <code>False</code> <code>**kwargs</code> <code>Any</code> <p>Keyword arguments for subprocess.Popen.</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Full output of subprocess concatenated stdout and stderr.</p> Source code in <code>client/ayon_core/lib/execute.py</code> <pre><code>def run_ayon_launcher_process(\n    *args, add_sys_paths: bool = False, **kwargs\n) -&gt; str:\n    \"\"\"Execute AYON process with passed arguments and wait.\n\n    Wrapper for 'run_process' which prepends AYON executable arguments\n    before passed arguments and define environments if are not passed.\n\n    Values from 'os.environ' are used for environments if are not passed.\n    They are cleaned using 'clean_envs_for_ayon_process' function.\n\n    Example:\n    ```\n    run_ayon_launcher_process(\"run\", \"&lt;path to .py script&gt;\")\n    ```\n\n    Args:\n        *args (str): ayon-launcher cli arguments.\n        add_sys_paths (bool): Add system paths to PYTHONPATH.\n        **kwargs (Any): Keyword arguments for subprocess.Popen.\n\n    Returns:\n        str: Full output of subprocess concatenated stdout and stderr.\n\n    \"\"\"\n    args = get_ayon_launcher_args(*args)\n    env = _prepare_ayon_launcher_env(add_sys_paths, kwargs)\n    return run_subprocess(args, env=env, **kwargs)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.run_detached_ayon_launcher_process","title":"<code>run_detached_ayon_launcher_process(*args, add_sys_paths=False, **kwargs)</code>","text":"<p>Execute AYON process with passed arguments and wait.</p> <p>Wrapper for 'run_process' which prepends AYON executable arguments before passed arguments and define environments if are not passed.</p> <p>Values from 'os.environ' are used for environments if are not passed. They are cleaned using 'clean_envs_for_ayon_process' function.</p> <p>Example:</p> <pre><code>run_detached_ayon_launcher_process(\"run\", \"&lt;path to .py script&gt;\")\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>*args</code> <code>str</code> <p>ayon-launcher cli arguments.</p> <code>()</code> <code>add_sys_paths</code> <code>bool</code> <p>Add system paths to PYTHONPATH.</p> <code>False</code> <code>**kwargs</code> <code>Any</code> <p>Keyword arguments for subprocess.Popen.</p> <code>{}</code> <p>Returns:</p> Type Description <code>Popen</code> <p>subprocess.Popen: Pointer to launched process but it is possible that launched process is already killed (on linux).</p> Source code in <code>client/ayon_core/lib/execute.py</code> <pre><code>def run_detached_ayon_launcher_process(\n    *args, add_sys_paths: bool = False, **kwargs\n) -&gt; subprocess.Popen:\n    \"\"\"Execute AYON process with passed arguments and wait.\n\n    Wrapper for 'run_process' which prepends AYON executable arguments\n    before passed arguments and define environments if are not passed.\n\n    Values from 'os.environ' are used for environments if are not passed.\n    They are cleaned using 'clean_envs_for_ayon_process' function.\n\n    Example:\n    ```\n    run_detached_ayon_launcher_process(\"run\", \"&lt;path to .py script&gt;\")\n    ```\n\n    Args:\n        *args (str): ayon-launcher cli arguments.\n        add_sys_paths (bool): Add system paths to PYTHONPATH.\n        **kwargs (Any): Keyword arguments for subprocess.Popen.\n\n    Returns:\n        subprocess.Popen: Pointer to launched process but it is possible that\n            launched process is already killed (on linux).\n\n    \"\"\"\n    args = get_ayon_launcher_args(*args)\n    env = _prepare_ayon_launcher_env(add_sys_paths, kwargs)\n    return run_detached_process(args, env=env, **kwargs)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.run_detached_process","title":"<code>run_detached_process(args, **kwargs)</code>","text":"<p>Execute process with passed arguments as separated process.</p> Example <p>run_detached_process(\"run\", \"./path_to.py\")</p> <p>Parameters:</p> Name Type Description Default <code>args</code> <code>Iterable[str]</code> <p>AYON cli arguments.</p> required <code>**kwargs</code> <code>dict</code> <p>Keyword arguments for subprocess.Popen.</p> <code>{}</code> <p>Returns:</p> Type Description <p>subprocess.Popen: Pointer to launched process but it is possible that launched process is already killed (on linux).</p> Source code in <code>client/ayon_core/lib/execute.py</code> <pre><code>def run_detached_process(args, **kwargs):\n    \"\"\"Execute process with passed arguments as separated process.\n\n    Example:\n        &gt;&gt;&gt; run_detached_process(\"run\", \"./path_to.py\")\n\n\n    Args:\n        args (Iterable[str]): AYON cli arguments.\n        **kwargs (dict): Keyword arguments for subprocess.Popen.\n\n    Returns:\n        subprocess.Popen: Pointer to launched process but it is possible that\n            launched process is already killed (on linux).\n\n    \"\"\"\n    env = kwargs.pop(\"env\", None)\n    # Keep env untouched if are passed and not empty\n    if not env:\n        env = os.environ\n\n    # Create copy of passed env\n    kwargs[\"env\"] = {k: v for k, v in env.items()}\n\n    low_platform = platform.system().lower()\n    if low_platform == \"darwin\":\n        executable = args[0]\n        # If it's a macOS app bundle, use open -na\n        if executable.endswith(\".app\") or \"/Contents/MacOS/\" in executable:\n            new_args = [\"open\", \"-na\", args.pop(0), \"--args\"]\n            new_args.extend(args)\n            args = new_args\n        else:\n            kwargs[\"start_new_session\"] = True\n\n    elif low_platform == \"windows\":\n        flags = (\n            subprocess.CREATE_NEW_PROCESS_GROUP\n            | subprocess.DETACHED_PROCESS\n        )\n        kwargs[\"creationflags\"] = flags\n\n        if not sys.stdout:\n            kwargs[\"stdout\"] = subprocess.DEVNULL\n            kwargs[\"stderr\"] = subprocess.DEVNULL\n\n    elif low_platform == \"linux\" and get_linux_launcher_args() is not None:\n        json_data = {\n            \"args\": args,\n            \"env\": kwargs.pop(\"env\")\n        }\n        json_temp = tempfile.NamedTemporaryFile(\n            mode=\"w\", prefix=\"op_app_args\", suffix=\".json\", delete=False\n        )\n        json_temp.close()\n        json_temp_filpath = json_temp.name\n        with open(json_temp_filpath, \"w\") as stream:\n            json.dump(json_data, stream)\n\n        new_args = get_linux_launcher_args()\n        new_args.append(json_temp_filpath)\n\n        # Create mid-process which will launch application\n        process = subprocess.Popen(new_args, **kwargs)\n        # Wait until the process finishes\n        #   - This is important! The process would stay in \"open\" state.\n        process.wait()\n        # Remove the temp file\n        os.remove(json_temp_filpath)\n        # Return process which is already terminated\n        return process\n\n    process = subprocess.Popen(args, **kwargs)\n    return process\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.run_subprocess","title":"<code>run_subprocess(*args, **kwargs)</code>","text":"<p>Convenience method for getting output errors for subprocess.</p> <p>Output logged when process finish.</p> <p>Entered arguments and keyword arguments are passed to subprocess Popen.</p> <p>On windows are 'creationflags' filled with flags that should cause ignore creation of new window.</p> <p>Parameters:</p> Name Type Description Default <code>*args</code> <p>Variable length argument list passed to Popen.</p> <code>()</code> <code>**kwargs </code> <p>Arbitrary keyword arguments passed to Popen. Is possible to pass <code>logging.Logger</code> object under \"logger\" to use custom logger for output.</p> required <p>Returns:</p> Name Type Description <code>str</code> <p>Full output of subprocess concatenated stdout and stderr.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>Exception is raised if process finished with nonzero return code.</p> Source code in <code>client/ayon_core/lib/execute.py</code> <pre><code>def run_subprocess(*args, **kwargs):\n    \"\"\"Convenience method for getting output errors for subprocess.\n\n    Output logged when process finish.\n\n    Entered arguments and keyword arguments are passed to subprocess Popen.\n\n    On windows are 'creationflags' filled with flags that should cause ignore\n    creation of new window.\n\n    Args:\n        *args: Variable length argument list passed to Popen.\n        **kwargs : Arbitrary keyword arguments passed to Popen. Is possible to\n            pass `logging.Logger` object under \"logger\" to use custom logger\n            for output.\n\n    Returns:\n        str: Full output of subprocess concatenated stdout and stderr.\n\n    Raises:\n        RuntimeError: Exception is raised if process finished with nonzero\n            return code.\n\n    \"\"\"\n    # Modify creation flags on windows to hide console window if in UI mode\n    if (\n        platform.system().lower() == \"windows\"\n        and \"creationflags\" not in kwargs\n        # shell=True already tries to hide the console window\n        # and passing these creationflags then shows the window again\n        # so we avoid it for shell=True cases\n        and kwargs.get(\"shell\") is not True\n    ):\n        kwargs[\"creationflags\"] = (\n            subprocess.CREATE_NEW_PROCESS_GROUP\n            | getattr(subprocess, \"DETACHED_PROCESS\", 0)\n            | getattr(subprocess, \"CREATE_NO_WINDOW\", 0)\n        )\n\n    # Escape special characters in certain shells\n    if (\n        kwargs.get(\"shell\") is True\n        and len(args) == 1\n        and isinstance(args[0], str)\n    ):\n        # Escape parentheses for bash\n        if os.getenv(\"SHELL\") in (\"/bin/bash\", \"/bin/sh\"):\n            new_arg = (\n                args[0]\n                .replace(\"(\", \"\\\\(\")\n                .replace(\")\", \"\\\\)\")\n            )\n            args = (new_arg,)\n        # Escape &amp; on Windows in shell with `cmd.exe` using ^&amp;\n        elif (\n            platform.system().lower() == \"windows\"\n            and os.getenv(\"COMSPEC\").endswith(\"cmd.exe\")\n        ):\n            new_arg = args[0].replace(\"&amp;\", \"^&amp;\")\n            args = (new_arg, )\n\n    # Get environments from kwarg or use current process environments if were\n    # not passed.\n    env = kwargs.get(\"env\") or os.environ\n    # Make sure environment contains only strings\n    filtered_env = {str(k): str(v) for k, v in env.items()}\n\n    # Use lib's logger if was not passed with kwargs.\n    logger = kwargs.pop(\"logger\", None)\n    if logger is None:\n        logger = Logger.get_logger(\"run_subprocess\")\n\n    # set overrides\n    kwargs[\"stdout\"] = kwargs.get(\"stdout\", subprocess.PIPE)\n    kwargs[\"stderr\"] = kwargs.get(\"stderr\", subprocess.PIPE)\n    kwargs[\"stdin\"] = kwargs.get(\"stdin\", subprocess.PIPE)\n    kwargs[\"env\"] = filtered_env\n\n    proc = subprocess.Popen(*args, **kwargs)\n\n    full_output = \"\"\n    _stdout, _stderr = proc.communicate()\n    if _stdout:\n        _stdout = _stdout.decode(\"utf-8\", errors=\"backslashreplace\")\n        full_output += _stdout\n        logger.debug(_stdout)\n\n    if _stderr:\n        _stderr = _stderr.decode(\"utf-8\", errors=\"backslashreplace\")\n        # Add additional line break if output already contains stdout\n        if full_output:\n            full_output += \"\\n\"\n        full_output += _stderr\n        logger.info(_stderr)\n\n    if proc.returncode != 0:\n        exc_msg = \"Executing arguments was not successful: \\\"{}\\\"\".format(args)\n        if _stdout:\n            exc_msg += \"\\n\\nOutput:\\n{}\".format(_stdout)\n\n        if _stderr:\n            exc_msg += \"Error:\\n{}\".format(_stderr)\n\n        raise RuntimeError(exc_msg)\n\n    return full_output\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.should_convert_for_ffmpeg","title":"<code>should_convert_for_ffmpeg(src_filepath, *, review_layers=None)</code>","text":"<p>Find out if input should be converted for ffmpeg.</p> <p>Currently cares only about exr inputs and is based on OpenImageIO.</p> <p>Parameters:</p> Name Type Description Default <code>src_filepath</code> <code>str</code> <p>Path to input file.</p> required <code>review_layers</code> <code>Optional[list[str]]</code> <p>List of reviewable layers.</p> <code>None</code> <p>Returns:</p> Type Description <code>Optional[bool]</code> <p>bool/NoneType: True if should be converted, False if should not and None if can't determine.</p> Source code in <code>client/ayon_core/lib/transcoding.py</code> <pre><code>def should_convert_for_ffmpeg(\n    src_filepath: str,\n    *,\n    review_layers: Optional[list[str]] = None,\n) -&gt; Optional[bool]:\n    \"\"\"Find out if input should be converted for ffmpeg.\n\n    Currently cares only about exr inputs and is based on OpenImageIO.\n\n    Args:\n        src_filepath (str): Path to input file.\n        review_layers (Optional[list[str]]): List of reviewable layers.\n\n    Returns:\n        bool/NoneType: True if should be converted, False if should not and\n            None if can't determine.\n    \"\"\"\n    # Care only about exr at this moment\n    ext = os.path.splitext(src_filepath)[-1].lower()\n    if ext != \".exr\":\n        return False\n\n    # Can't determine if should convert or not without oiio_tool\n    if not is_oiio_supported():\n        return None\n\n    # Load info about file from oiio tool\n    input_info = get_oiio_info_for_input(src_filepath)\n    if not input_info:\n        return None\n\n    subimages = input_info.get(\"subimages\")\n    if subimages is not None and subimages &gt; 1:\n        return True\n\n    # Check compression\n    compression = input_info[\"attribs\"].get(\"compression\")\n    if compression in (\"dwaa\", \"dwab\"):\n        return True\n\n    # Check channels\n    channel_names = input_info[\"channelnames\"]\n    review_channels = get_convert_rgb_channels(\n        channel_names,\n        review_layers=review_layers\n    )\n    if review_channels is None:\n        return None\n\n    for attr_value in input_info[\"attribs\"].values():\n        if not isinstance(attr_value, str):\n            continue\n\n        if len(attr_value) &gt; MAX_FFMPEG_STRING_LEN:\n            return True\n\n        for char in NOT_ALLOWED_FFMPEG_CHARS:\n            if char in attr_value:\n                return True\n    return False\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.source_hash","title":"<code>source_hash(filepath, *args)</code>","text":"<p>Generate simple identifier for a source file. This is used to identify whether a source file has previously been processe into the pipeline, e.g. a texture. The hash is based on source filepath, modification time and file size. This is only used to identify whether a specific source file was already published before from the same location with the same modification date. We opt to do it this way as opposed to Avalanch C4 hash as this is much faster and predictable enough for all our production use cases. Args:     filepath (str): The source file path. You can specify additional arguments in the function to allow for specific 'processing' values to be included.</p> Source code in <code>client/ayon_core/lib/plugin_tools.py</code> <pre><code>def source_hash(filepath, *args):\n    \"\"\"Generate simple identifier for a source file.\n    This is used to identify whether a source file has previously been\n    processe into the pipeline, e.g. a texture.\n    The hash is based on source filepath, modification time and file size.\n    This is only used to identify whether a specific source file was already\n    published before from the same location with the same modification date.\n    We opt to do it this way as opposed to Avalanch C4 hash as this is much\n    faster and predictable enough for all our production use cases.\n    Args:\n        filepath (str): The source file path.\n    You can specify additional arguments in the function\n    to allow for specific 'processing' values to be included.\n    \"\"\"\n    # We replace dots with comma because . cannot be a key in a pymongo dict.\n    file_name = os.path.basename(filepath)\n    time = str(os.path.getmtime(filepath))\n    size = str(os.path.getsize(filepath))\n    return \"|\".join([file_name, time, size] + list(args)).replace(\".\", \",\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/index.html#client.ayon_core.lib.version_up","title":"<code>version_up(filepath)</code>","text":"<p>Version up filepath to a new non-existing version.</p> <p>Parses for a version identifier like <code>_v001</code> or <code>.v001</code> When no version present _v001 is appended as suffix.</p> <p>Parameters:</p> Name Type Description Default <code>filepath</code> <code>str</code> <p>full url</p> required <p>Returns:</p> Type Description <code>str</code> <p>filepath with increased version number</p> Source code in <code>client/ayon_core/lib/path_tools.py</code> <pre><code>def version_up(filepath):\n    \"\"\"Version up filepath to a new non-existing version.\n\n    Parses for a version identifier like `_v001` or `.v001`\n    When no version present _v001 is appended as suffix.\n\n    Args:\n        filepath (str): full url\n\n    Returns:\n        (str): filepath with increased version number\n\n    \"\"\"\n    dirname = os.path.dirname(filepath)\n    basename, ext = os.path.splitext(os.path.basename(filepath))\n\n    regex = r\"[._]v\\d+\"\n    matches = re.findall(regex, str(basename), re.IGNORECASE)\n    if not matches:\n        log.info(\"Creating version...\")\n        new_label = \"_v{version:03d}\".format(version=1)\n        new_basename = \"{}{}\".format(basename, new_label)\n    else:\n        label = matches[-1]\n        version = re.search(r\"\\d+\", label).group()\n        padding = len(version)\n\n        new_version = int(version) + 1\n        new_version = '{version:0{padding}d}'.format(version=new_version,\n                                                     padding=padding)\n        new_label = label.replace(version, new_version, 1)\n        new_basename = _rreplace(basename, label, new_label)\n    new_filename = \"{}{}\".format(new_basename, ext)\n    new_filename = os.path.join(dirname, new_filename)\n    new_filename = os.path.normpath(new_filename)\n\n    if new_filename == filepath:\n        raise RuntimeError(\"Created path is the same as current file,\"\n                           \"this is a bug\")\n\n    # We check for version clashes against the current file for any file\n    # that matches completely in name up to the {version} label found. Thus\n    # if source file was test_v001_test.txt we want to also check clashes\n    # against test_v002.txt but do want to preserve the part after the version\n    # label for our new filename\n    clash_basename = new_basename\n    if not clash_basename.endswith(new_label):\n        index = (clash_basename.find(new_label))\n        index += len(new_label)\n        clash_basename = clash_basename[:index]\n\n    for file in os.listdir(dirname):\n        if file.endswith(ext) and file.startswith(clash_basename):\n            log.info(\"Skipping existing version %s\" % new_label)\n            return version_up(new_filename)\n\n    log.info(\"New version %s\" % new_label)\n    return new_filename\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/_compatibility.html","title":"_compatibility","text":""},{"location":"autoapi/client/ayon_core/lib/_compatibility.html#client.ayon_core.lib._compatibility.StrEnum","title":"<code>StrEnum</code>","text":"<p>               Bases: <code>str</code>, <code>Enum</code></p> <p>A string-based Enum class that allows for string comparison.</p> Source code in <code>client/ayon_core/lib/_compatibility.py</code> <pre><code>class StrEnum(str, Enum):\n    \"\"\"A string-based Enum class that allows for string comparison.\"\"\"\n\n    def __str__(self) -&gt; str:\n        return self.value\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/attribute_definitions.html","title":"attribute_definitions","text":""},{"location":"autoapi/client/ayon_core/lib/attribute_definitions.html#client.ayon_core.lib.attribute_definitions.AbstractAttrDef","title":"<code>AbstractAttrDef</code>","text":"<p>Abstraction of attribute definition.</p> <p>Each attribute definition must have implemented validation and conversion method.</p> <p>Attribute definition should have ability to return \"default\" value. That can be based on passed data into <code>__init__</code> so is not abstracted to attribute.</p> <p>QUESTION: How to force to set <code>key</code> attribute?</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>Under which key will be attribute value stored.</p> required <code>default</code> <code>Any</code> <p>Default value of an attribute.</p> required <code>label</code> <code>Optional[str]</code> <p>Attribute label.</p> <code>None</code> <code>tooltip</code> <code>Optional[str]</code> <p>Attribute tooltip.</p> <code>None</code> <code>is_label_horizontal</code> <code>Optional[bool]</code> <p>UI specific argument. Specify if label is next to value input or ahead.</p> <code>None</code> <code>visible</code> <code>Optional[bool]</code> <p>Item is shown to user (for UI purposes).</p> <code>None</code> <code>enabled</code> <code>Optional[bool]</code> <p>Item is enabled (for UI purposes).</p> <code>None</code> <code>hidden</code> <code>Optional[bool]</code> <p>DEPRECATED: Use 'visible' instead.</p> <code>None</code> <code>disabled</code> <code>Optional[bool]</code> <p>DEPRECATED: Use 'enabled' instead.</p> <code>None</code> Source code in <code>client/ayon_core/lib/attribute_definitions.py</code> <pre><code>class AbstractAttrDef(metaclass=AbstractAttrDefMeta):\n    \"\"\"Abstraction of attribute definition.\n\n    Each attribute definition must have implemented validation and\n    conversion method.\n\n    Attribute definition should have ability to return \"default\" value. That\n    can be based on passed data into `__init__` so is not abstracted to\n    attribute.\n\n    QUESTION:\n    How to force to set `key` attribute?\n\n    Args:\n        key (str): Under which key will be attribute value stored.\n        default (Any): Default value of an attribute.\n        label (Optional[str]): Attribute label.\n        tooltip (Optional[str]): Attribute tooltip.\n        is_label_horizontal (Optional[bool]): UI specific argument. Specify\n            if label is next to value input or ahead.\n        visible (Optional[bool]): Item is shown to user (for UI purposes).\n        enabled (Optional[bool]): Item is enabled (for UI purposes).\n        hidden (Optional[bool]): DEPRECATED: Use 'visible' instead.\n        disabled (Optional[bool]): DEPRECATED: Use 'enabled' instead.\n\n    \"\"\"\n    type_attributes = []\n\n    is_value_def = True\n\n    def __init__(\n        self,\n        key: str,\n        default: Any,\n        label: Optional[str] = None,\n        tooltip: Optional[str] = None,\n        is_label_horizontal: Optional[bool] = None,\n        visible: Optional[bool] = None,\n        enabled: Optional[bool] = None,\n        hidden: Optional[bool] = None,\n        disabled: Optional[bool] = None,\n    ):\n        if is_label_horizontal is None:\n            is_label_horizontal = True\n\n        enabled = _convert_reversed_attr(\n            enabled, disabled, \"enabled\", \"disabled\", True\n        )\n        visible = _convert_reversed_attr(\n            visible, hidden, \"visible\", \"hidden\", True\n        )\n\n        self.key: str = key\n        self.label: Optional[str] = label\n        self.tooltip: Optional[str] = tooltip\n        self.default: Any = default\n        self.is_label_horizontal: bool = is_label_horizontal\n        self.visible: bool = visible\n        self.enabled: bool = enabled\n        self._id: str = uuid.uuid4().hex\n\n        self.__init__class__ = AbstractAttrDef\n\n    @property\n    def id(self) -&gt; str:\n        return self._id\n\n    def clone(self) -&gt; \"Self\":\n        data = self.serialize()\n        data.pop(\"type\")\n        return self.deserialize(data)\n\n    @property\n    def hidden(self) -&gt; bool:\n        return not self.visible\n\n    @hidden.setter\n    def hidden(self, value: bool):\n        self.visible = not value\n\n    @property\n    def disabled(self) -&gt; bool:\n        return not self.enabled\n\n    @disabled.setter\n    def disabled(self, value: bool):\n        self.enabled = not value\n\n    def __eq__(self, other: Any) -&gt; bool:\n        return self.compare_to_def(other)\n\n    def __ne__(self, other: Any) -&gt; bool:\n        return not self.compare_to_def(other)\n\n    def compare_to_def(\n        self,\n        other: Any,\n        ignore_default: Optional[bool] = False,\n        ignore_enabled: Optional[bool] = False,\n        ignore_visible: Optional[bool] = False,\n        ignore_def_type_compare: Optional[bool] = False,\n    ) -&gt; bool:\n        if not isinstance(other, self.__class__) or self.key != other.key:\n            return False\n        if not ignore_def_type_compare and not self._def_type_compare(other):\n            return False\n        return (\n            (ignore_default or self.default == other.default)\n            and (ignore_visible or self.visible == other.visible)\n            and (ignore_enabled or self.enabled == other.enabled)\n        )\n\n    @abstractmethod\n    def is_value_valid(self, value: Any) -&gt; bool:\n        \"\"\"Check if value is valid.\n\n        This should return False if value is not valid based\n            on definition type.\n\n        Args:\n            value (Any): Value to validate based on definition type.\n\n        Returns:\n            bool: True if value is valid.\n\n        \"\"\"\n        pass\n\n    @property\n    @abstractmethod\n    def type(self) -&gt; str:\n        \"\"\"Attribute definition type also used as identifier of class.\n\n        Returns:\n            str: Type of attribute definition.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def convert_value(self, value: Any) -&gt; Any:\n        \"\"\"Convert value to a valid one.\n\n        Convert passed value to a valid type. Use default if value can't be\n        converted.\n\n        \"\"\"\n        pass\n\n    def serialize(self) -&gt; Dict[str, Any]:\n        \"\"\"Serialize object to data so it's possible to recreate it.\n\n        Returns:\n            Dict[str, Any]: Serialized object that can be passed to\n                'deserialize' method.\n\n        \"\"\"\n        data = {\n            \"type\": self.type,\n            \"key\": self.key,\n            \"label\": self.label,\n            \"tooltip\": self.tooltip,\n            \"default\": self.default,\n            \"is_label_horizontal\": self.is_label_horizontal,\n            \"visible\": self.visible,\n            \"enabled\": self.enabled\n        }\n        for attr in self.type_attributes:\n            data[attr] = getattr(self, attr)\n        return data\n\n    @classmethod\n    def deserialize(cls, data: Dict[str, Any]) -&gt; \"Self\":\n        \"\"\"Recreate object from data.\n\n        Data can be received using 'serialize' method.\n        \"\"\"\n        if \"type\" in data:\n            data = dict(data)\n            data.pop(\"type\")\n\n        return cls(**data)\n\n    def _def_type_compare(self, other: \"Self\") -&gt; bool:\n        return True\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/attribute_definitions.html#client.ayon_core.lib.attribute_definitions.AbstractAttrDef.type","title":"<code>type</code>  <code>abstractmethod</code> <code>property</code>","text":"<p>Attribute definition type also used as identifier of class.</p> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Type of attribute definition.</p>"},{"location":"autoapi/client/ayon_core/lib/attribute_definitions.html#client.ayon_core.lib.attribute_definitions.AbstractAttrDef.convert_value","title":"<code>convert_value(value)</code>  <code>abstractmethod</code>","text":"<p>Convert value to a valid one.</p> <p>Convert passed value to a valid type. Use default if value can't be converted.</p> Source code in <code>client/ayon_core/lib/attribute_definitions.py</code> <pre><code>@abstractmethod\ndef convert_value(self, value: Any) -&gt; Any:\n    \"\"\"Convert value to a valid one.\n\n    Convert passed value to a valid type. Use default if value can't be\n    converted.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/attribute_definitions.html#client.ayon_core.lib.attribute_definitions.AbstractAttrDef.deserialize","title":"<code>deserialize(data)</code>  <code>classmethod</code>","text":"<p>Recreate object from data.</p> <p>Data can be received using 'serialize' method.</p> Source code in <code>client/ayon_core/lib/attribute_definitions.py</code> <pre><code>@classmethod\ndef deserialize(cls, data: Dict[str, Any]) -&gt; \"Self\":\n    \"\"\"Recreate object from data.\n\n    Data can be received using 'serialize' method.\n    \"\"\"\n    if \"type\" in data:\n        data = dict(data)\n        data.pop(\"type\")\n\n    return cls(**data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/attribute_definitions.html#client.ayon_core.lib.attribute_definitions.AbstractAttrDef.is_value_valid","title":"<code>is_value_valid(value)</code>  <code>abstractmethod</code>","text":"<p>Check if value is valid.</p> <p>This should return False if value is not valid based     on definition type.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>Any</code> <p>Value to validate based on definition type.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if value is valid.</p> Source code in <code>client/ayon_core/lib/attribute_definitions.py</code> <pre><code>@abstractmethod\ndef is_value_valid(self, value: Any) -&gt; bool:\n    \"\"\"Check if value is valid.\n\n    This should return False if value is not valid based\n        on definition type.\n\n    Args:\n        value (Any): Value to validate based on definition type.\n\n    Returns:\n        bool: True if value is valid.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/attribute_definitions.html#client.ayon_core.lib.attribute_definitions.AbstractAttrDef.serialize","title":"<code>serialize()</code>","text":"<p>Serialize object to data so it's possible to recreate it.</p> <p>Returns:</p> Type Description <code>Dict[str, Any]</code> <p>Dict[str, Any]: Serialized object that can be passed to 'deserialize' method.</p> Source code in <code>client/ayon_core/lib/attribute_definitions.py</code> <pre><code>def serialize(self) -&gt; Dict[str, Any]:\n    \"\"\"Serialize object to data so it's possible to recreate it.\n\n    Returns:\n        Dict[str, Any]: Serialized object that can be passed to\n            'deserialize' method.\n\n    \"\"\"\n    data = {\n        \"type\": self.type,\n        \"key\": self.key,\n        \"label\": self.label,\n        \"tooltip\": self.tooltip,\n        \"default\": self.default,\n        \"is_label_horizontal\": self.is_label_horizontal,\n        \"visible\": self.visible,\n        \"enabled\": self.enabled\n    }\n    for attr in self.type_attributes:\n        data[attr] = getattr(self, attr)\n    return data\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/attribute_definitions.html#client.ayon_core.lib.attribute_definitions.AbstractAttrDefMeta","title":"<code>AbstractAttrDefMeta</code>","text":"<p>               Bases: <code>ABCMeta</code></p> <p>Metaclass to validate the existence of 'key' attribute.</p> <p>Each object of <code>AbstractAttrDef</code> must have defined 'key' attribute.</p> Source code in <code>client/ayon_core/lib/attribute_definitions.py</code> <pre><code>class AbstractAttrDefMeta(ABCMeta):\n    \"\"\"Metaclass to validate the existence of 'key' attribute.\n\n    Each object of `AbstractAttrDef` must have defined 'key' attribute.\n\n    \"\"\"\n    def __call__(cls, *args, **kwargs):\n        obj = super(AbstractAttrDefMeta, cls).__call__(*args, **kwargs)\n        init_class = getattr(obj, \"__init__class__\", None)\n        if init_class is not AbstractAttrDef:\n            raise TypeError(\"{} super was not called in __init__.\".format(\n                type(obj)\n            ))\n        return obj\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/attribute_definitions.html#client.ayon_core.lib.attribute_definitions.BoolDef","title":"<code>BoolDef</code>","text":"<p>               Bases: <code>AbstractAttrDef</code></p> <p>Boolean representation.</p> <p>Parameters:</p> Name Type Description Default <code>default</code> <code>bool</code> <p>Default value. Set to <code>False</code> if not defined.</p> <code>None</code> Source code in <code>client/ayon_core/lib/attribute_definitions.py</code> <pre><code>class BoolDef(AbstractAttrDef):\n    \"\"\"Boolean representation.\n\n    Args:\n        default(bool): Default value. Set to `False` if not defined.\n\n    \"\"\"\n    type = \"bool\"\n\n    def __init__(self, key: str, default: Optional[bool] = None, **kwargs):\n        if default is None:\n            default = False\n        super().__init__(key, default=default, **kwargs)\n\n    def is_value_valid(self, value: Any) -&gt; bool:\n        return isinstance(value, bool)\n\n    def convert_value(self, value: Any) -&gt; bool:\n        if isinstance(value, bool):\n            return value\n        return self.default\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/attribute_definitions.html#client.ayon_core.lib.attribute_definitions.ButtonDef","title":"<code>ButtonDef</code>","text":"<p>               Bases: <code>UIDef</code></p> <p>Button definition.</p> <p>UI element to allow to trigger a callback.</p> Source code in <code>client/ayon_core/lib/attribute_definitions.py</code> <pre><code>class ButtonDef(UIDef):\n    \"\"\"Button definition.\n\n    UI element to allow to trigger a callback.\n    \"\"\"\n    type = \"button\"\n\n    def __init__(\n        self,\n        key: str,\n        callback: Callable | None,\n        text: str | None = None,\n        icon: IconBase | None = None,\n        **kwargs\n    ):\n        self._callback = callback\n        if not text and not icon:\n            icon = MaterialSymbolsIcon(name=\"left_click\")\n        self.icon = icon\n        self.text = text\n        super().__init__(key=key, **kwargs)\n\n    def get_callback(self) -&gt; Callable | None:\n        return self._callback\n\n    def set_callback(self, callback: Callable) -&gt; None:\n        self._callback = callback\n\n    callback = property(get_callback, set_callback)\n\n    def trigger(self) -&gt; None:\n        self._callback()\n\n    def serialize(self) -&gt; dict[str, Any]:\n        \"\"\"Serialize object to data so it's possible to recreate it.\n\n        Serialization of button definition does not include callback function.\n            Logic after deserialization has to be handled manually.\n\n        \"\"\"\n        data = super().serialize()\n        icon_data = None\n        if self.icon is not None:\n            icon_data = self.icon.to_data()\n        data[\"callback\"] = None\n        data[\"icon\"] = icon_data\n        data[\"text\"] = self.text\n        return data\n\n    @classmethod\n    def deserialize(cls, data: dict[str, Any]) -&gt; \"Self\":\n        \"\"\"Recreate object from data.\n\n        Data can be received using 'serialize' method.\n        \"\"\"\n        icon = data[\"icon\"]\n        if isinstance(icon, dict):\n            data[\"icon\"] = get_icon_def_from_data(icon)\n\n        return cls(**data)\n\n    def clone(self) -&gt; \"Self\":\n        attr_def = super().clone()\n        attr_def.set_callback(self._callback)\n        return attr_def\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/attribute_definitions.html#client.ayon_core.lib.attribute_definitions.ButtonDef.deserialize","title":"<code>deserialize(data)</code>  <code>classmethod</code>","text":"<p>Recreate object from data.</p> <p>Data can be received using 'serialize' method.</p> Source code in <code>client/ayon_core/lib/attribute_definitions.py</code> <pre><code>@classmethod\ndef deserialize(cls, data: dict[str, Any]) -&gt; \"Self\":\n    \"\"\"Recreate object from data.\n\n    Data can be received using 'serialize' method.\n    \"\"\"\n    icon = data[\"icon\"]\n    if isinstance(icon, dict):\n        data[\"icon\"] = get_icon_def_from_data(icon)\n\n    return cls(**data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/attribute_definitions.html#client.ayon_core.lib.attribute_definitions.ButtonDef.serialize","title":"<code>serialize()</code>","text":"<p>Serialize object to data so it's possible to recreate it.</p> <p>Serialization of button definition does not include callback function.     Logic after deserialization has to be handled manually.</p> Source code in <code>client/ayon_core/lib/attribute_definitions.py</code> <pre><code>def serialize(self) -&gt; dict[str, Any]:\n    \"\"\"Serialize object to data so it's possible to recreate it.\n\n    Serialization of button definition does not include callback function.\n        Logic after deserialization has to be handled manually.\n\n    \"\"\"\n    data = super().serialize()\n    icon_data = None\n    if self.icon is not None:\n        icon_data = self.icon.to_data()\n    data[\"callback\"] = None\n    data[\"icon\"] = icon_data\n    data[\"text\"] = self.text\n    return data\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/attribute_definitions.html#client.ayon_core.lib.attribute_definitions.EnumDef","title":"<code>EnumDef</code>","text":"<p>               Bases: <code>AbstractAttrDef</code></p> <p>Enumeration of items.</p> <p>Enumeration of single item from items. Or list of items if multiselection is enabled.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>Key under which value is stored.</p> required <code>items</code> <code>EnumItemsInputType</code> <p>Items definition that can be converted using 'prepare_enum_items'.</p> required <code>default</code> <code>Optional[Any]</code> <p>Default value. Must be one key(value) from passed items or list of values for multiselection.</p> <code>None</code> <code>multiselection</code> <code>Optional[bool]</code> <p>If True, multiselection is allowed. Output is list of selected items.</p> <code>False</code> <code>placeholder</code> <code>Optional[str]</code> <p>Placeholder for UI purposes, only for multiselection enumeration.</p> <code>None</code> Source code in <code>client/ayon_core/lib/attribute_definitions.py</code> <pre><code>class EnumDef(AbstractAttrDef):\n    \"\"\"Enumeration of items.\n\n    Enumeration of single item from items. Or list of items if multiselection\n    is enabled.\n\n    Args:\n        key (str): Key under which value is stored.\n        items (EnumItemsInputType): Items definition that can be converted\n            using 'prepare_enum_items'.\n        default (Optional[Any]): Default value. Must be one key(value) from\n            passed items or list of values for multiselection.\n        multiselection (Optional[bool]): If True, multiselection is allowed.\n            Output is list of selected items.\n        placeholder (Optional[str]): Placeholder for UI purposes, only for\n            multiselection enumeration.\n\n    \"\"\"\n    type = \"enum\"\n\n    type_attributes = [\n        \"multiselection\",\n        \"placeholder\",\n    ]\n\n    def __init__(\n        self,\n        key: str,\n        items: \"EnumItemsInputType\",\n        default: \"Union[str, List[Any]]\" = None,\n        multiselection: Optional[bool] = False,\n        placeholder: Optional[str] = None,\n        **kwargs\n    ):\n        if multiselection is None:\n            multiselection = False\n\n        if not items and not multiselection:\n            raise ValueError(\n                f\"Empty 'items' value. {self.__class__.__name__} must have\"\n                \" defined values on initialization.\"\n            )\n\n        items = self.prepare_enum_items(items)\n        item_values = [item[\"value\"] for item in items]\n        item_values_set = set(item_values)\n\n        if multiselection:\n            if default is None:\n                default = []\n            default = list(item_values_set.intersection(default))\n\n        elif default not in item_values:\n            default = next(iter(item_values), None)\n\n        super().__init__(key, default=default, **kwargs)\n\n        self.items: List[\"EnumItemDict\"] = items\n        self._item_values: Set[Any] = item_values_set\n        self.multiselection: bool = multiselection\n        self.placeholder: Optional[str] = placeholder\n\n    def convert_value(self, value):\n        if not self.multiselection:\n            if value in self._item_values:\n                return value\n            return self.default\n\n        if value is None:\n            return copy.deepcopy(self.default)\n        return [\n            v\n            for v in value\n            if v in self._item_values\n        ]\n\n    def is_value_valid(self, value: Any) -&gt; bool:\n        \"\"\"Check if item is available in possible values.\"\"\"\n        if isinstance(value, list):\n            if not self.multiselection:\n                return False\n            return all(value in self._item_values for value in value)\n\n        if self.multiselection:\n            return False\n        return value in self._item_values\n\n    def serialize(self):\n        data = super().serialize()\n        data[\"items\"] = copy.deepcopy(self.items)\n        return data\n\n    @staticmethod\n    def prepare_enum_items(\n        items: \"EnumItemsInputType\"\n    ) -&gt; List[\"EnumItemDict\"]:\n        \"\"\"Convert items to unified structure.\n\n        Output is a list where each item is dictionary with 'value'\n        and 'label'.\n\n        ```python\n        # Example output\n        [\n            {\"label\": \"Option 1\", \"value\": 1},\n            {\"label\": \"Option 2\", \"value\": 2},\n            {\"label\": \"Option 3\", \"value\": 3}\n        ]\n        ```\n\n        Args:\n            items (EnumItemsInputType): The items to convert.\n\n        Returns:\n            List[EnumItemDict]: Unified structure of items.\n\n        \"\"\"\n        output = []\n        if isinstance(items, dict):\n            for value, label in items.items():\n                output.append({\"label\": label, \"value\": value})\n\n        elif isinstance(items, (tuple, list, set)):\n            for item in items:\n                if isinstance(item, dict):\n                    # Validate if 'value' is available\n                    if \"value\" not in item:\n                        raise KeyError(\"Item does not contain 'value' key.\")\n\n                    if \"label\" not in item:\n                        item[\"label\"] = str(item[\"value\"])\n                elif isinstance(item, (list, tuple)):\n                    if len(item) == 2:\n                        value, label = item\n                    elif len(item) == 1:\n                        value = item[0]\n                        label = str(value)\n                    else:\n                        raise ValueError((\n                            \"Invalid items count {}.\"\n                            \" Expected 1 or 2. Value: {}\"\n                        ).format(len(item), str(item)))\n\n                    item = {\"label\": label, \"value\": value}\n                else:\n                    item = {\"label\": str(item), \"value\": item}\n                output.append(item)\n\n        else:\n            raise TypeError(\n                \"Unknown type for enum items '{}'\".format(type(items))\n            )\n\n        return output\n\n    def _def_type_compare(self, other: \"EnumDef\") -&gt; bool:\n        return (\n            self.items == other.items\n            and self.multiselection == other.multiselection\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/attribute_definitions.html#client.ayon_core.lib.attribute_definitions.EnumDef.is_value_valid","title":"<code>is_value_valid(value)</code>","text":"<p>Check if item is available in possible values.</p> Source code in <code>client/ayon_core/lib/attribute_definitions.py</code> <pre><code>def is_value_valid(self, value: Any) -&gt; bool:\n    \"\"\"Check if item is available in possible values.\"\"\"\n    if isinstance(value, list):\n        if not self.multiselection:\n            return False\n        return all(value in self._item_values for value in value)\n\n    if self.multiselection:\n        return False\n    return value in self._item_values\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/attribute_definitions.html#client.ayon_core.lib.attribute_definitions.EnumDef.prepare_enum_items","title":"<code>prepare_enum_items(items)</code>  <code>staticmethod</code>","text":"<p>Convert items to unified structure.</p> <p>Output is a list where each item is dictionary with 'value' and 'label'.</p> <pre><code># Example output\n[\n    {\"label\": \"Option 1\", \"value\": 1},\n    {\"label\": \"Option 2\", \"value\": 2},\n    {\"label\": \"Option 3\", \"value\": 3}\n]\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>items</code> <code>EnumItemsInputType</code> <p>The items to convert.</p> required <p>Returns:</p> Type Description <code>List['EnumItemDict']</code> <p>List[EnumItemDict]: Unified structure of items.</p> Source code in <code>client/ayon_core/lib/attribute_definitions.py</code> <pre><code>@staticmethod\ndef prepare_enum_items(\n    items: \"EnumItemsInputType\"\n) -&gt; List[\"EnumItemDict\"]:\n    \"\"\"Convert items to unified structure.\n\n    Output is a list where each item is dictionary with 'value'\n    and 'label'.\n\n    ```python\n    # Example output\n    [\n        {\"label\": \"Option 1\", \"value\": 1},\n        {\"label\": \"Option 2\", \"value\": 2},\n        {\"label\": \"Option 3\", \"value\": 3}\n    ]\n    ```\n\n    Args:\n        items (EnumItemsInputType): The items to convert.\n\n    Returns:\n        List[EnumItemDict]: Unified structure of items.\n\n    \"\"\"\n    output = []\n    if isinstance(items, dict):\n        for value, label in items.items():\n            output.append({\"label\": label, \"value\": value})\n\n    elif isinstance(items, (tuple, list, set)):\n        for item in items:\n            if isinstance(item, dict):\n                # Validate if 'value' is available\n                if \"value\" not in item:\n                    raise KeyError(\"Item does not contain 'value' key.\")\n\n                if \"label\" not in item:\n                    item[\"label\"] = str(item[\"value\"])\n            elif isinstance(item, (list, tuple)):\n                if len(item) == 2:\n                    value, label = item\n                elif len(item) == 1:\n                    value = item[0]\n                    label = str(value)\n                else:\n                    raise ValueError((\n                        \"Invalid items count {}.\"\n                        \" Expected 1 or 2. Value: {}\"\n                    ).format(len(item), str(item)))\n\n                item = {\"label\": label, \"value\": value}\n            else:\n                item = {\"label\": str(item), \"value\": item}\n            output.append(item)\n\n    else:\n        raise TypeError(\n            \"Unknown type for enum items '{}'\".format(type(items))\n        )\n\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/attribute_definitions.html#client.ayon_core.lib.attribute_definitions.FileDef","title":"<code>FileDef</code>","text":"<p>               Bases: <code>AbstractAttrDef</code></p> <p>File definition. It is possible to define filters of allowed file extensions and if supports folders. Args:     single_item(bool): Allow only single path item.     folders(bool): Allow folder paths.     extensions(List[str]): Allow files with extensions. Empty list will         allow all extensions and None will disable files completely.     extensions_label(str): Custom label shown instead of extensions in UI.     default(str, List[str]): Default value.</p> Source code in <code>client/ayon_core/lib/attribute_definitions.py</code> <pre><code>class FileDef(AbstractAttrDef):\n    \"\"\"File definition.\n    It is possible to define filters of allowed file extensions and if supports\n    folders.\n    Args:\n        single_item(bool): Allow only single path item.\n        folders(bool): Allow folder paths.\n        extensions(List[str]): Allow files with extensions. Empty list will\n            allow all extensions and None will disable files completely.\n        extensions_label(str): Custom label shown instead of extensions in UI.\n        default(str, List[str]): Default value.\n    \"\"\"\n\n    type = \"path\"\n    type_attributes = [\n        \"single_item\",\n        \"folders\",\n        \"extensions\",\n        \"allow_sequences\",\n        \"extensions_label\",\n    ]\n\n    def __init__(\n        self,\n        key: str,\n        single_item: Optional[bool] = True,\n        folders: Optional[bool] = None,\n        extensions: Optional[Iterable[str]] = None,\n        allow_sequences: Optional[bool] = True,\n        extensions_label: Optional[str] = None,\n        default: Optional[\"Union[FileDefItemDict, List[str]]\"] = None,\n        **kwargs\n    ):\n        if folders is None and extensions is None:\n            folders = True\n            extensions = []\n\n        if default is None:\n            if single_item:\n                default = FileDefItem.create_empty_item().to_dict()\n            else:\n                default = []\n        else:\n            if single_item:\n                if isinstance(default, dict):\n                    FileDefItem.from_dict(default)\n\n                elif isinstance(default, str):\n                    default = FileDefItem.from_paths(\n                        [default.strip()], allow_sequences\n                    )[0]\n\n                else:\n                    raise TypeError((\n                        \"'default' argument must be 'str' or 'dict' not '{}'\"\n                    ).format(type(default)))\n\n            else:\n                if not isinstance(default, (tuple, list, set)):\n                    raise TypeError((\n                        \"'default' argument must be 'list', 'tuple' or 'set'\"\n                        \", not '{}'\"\n                    ).format(type(default)))\n\n        # Change horizontal label\n        is_label_horizontal = kwargs.get(\"is_label_horizontal\")\n        if is_label_horizontal is None:\n            kwargs[\"is_label_horizontal\"] = False\n\n        self.single_item: bool = single_item\n        self.folders: bool = folders\n        self.extensions: Set[str] = set(extensions)\n        self.allow_sequences: bool = allow_sequences\n        self.extensions_label: Optional[str] = extensions_label\n        super().__init__(key, default=default, **kwargs)\n\n    def __eq__(self, other: Any) -&gt; bool:\n        if not super().__eq__(other):\n            return False\n\n        return (\n            self.single_item == other.single_item\n            and self.folders == other.folders\n            and self.extensions == other.extensions\n            and self.allow_sequences == other.allow_sequences\n        )\n\n    def is_value_valid(self, value: Any) -&gt; bool:\n        if self.single_item:\n            if not isinstance(value, dict):\n                return False\n            try:\n                FileDefItem.from_dict(value)\n                return True\n            except (ValueError, KeyError):\n                return False\n\n        if not isinstance(value, list):\n            return False\n\n        for item in value:\n            if not isinstance(item, dict):\n                return False\n\n            try:\n                FileDefItem.from_dict(item)\n            except (ValueError, KeyError):\n                return False\n        return True\n\n    def convert_value(\n        self, value: Any\n    ) -&gt; \"Union[FileDefItemDict, List[FileDefItemDict]]\":\n        if isinstance(value, (str, dict)):\n            value = [value]\n\n        if isinstance(value, (tuple, list, set)):\n            string_paths = []\n            dict_items = []\n            for item in value:\n                if isinstance(item, str):\n                    string_paths.append(item.strip())\n                elif isinstance(item, dict):\n                    try:\n                        FileDefItem.from_dict(item)\n                        dict_items.append(item)\n                    except (ValueError, KeyError):\n                        pass\n\n            if string_paths:\n                file_items = FileDefItem.from_paths(\n                    string_paths, self.allow_sequences\n                )\n                dict_items.extend([\n                    file_item.to_dict()\n                    for file_item in file_items\n                ])\n\n            if not self.single_item:\n                return dict_items\n\n            if not dict_items:\n                return self.default\n            return dict_items[0]\n\n        if self.single_item:\n            return FileDefItem.create_empty_item().to_dict()\n        return []\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/attribute_definitions.html#client.ayon_core.lib.attribute_definitions.FileDefItem","title":"<code>FileDefItem</code>","text":"Source code in <code>client/ayon_core/lib/attribute_definitions.py</code> <pre><code>class FileDefItem:\n    def __init__(\n        self,\n        directory: str,\n        filenames: List[str],\n        frames: Optional[List[int]] = None,\n        template: Optional[str] = None,\n    ):\n        self.directory = directory\n\n        self.filenames = []\n        self.is_sequence = False\n        self.template = None\n        self.frames = []\n        self.is_empty = True\n\n        self.set_filenames(filenames, frames, template)\n\n    def __str__(self):\n        return json.dumps(self.to_dict())\n\n    def __repr__(self):\n        if self.is_empty:\n            filename = \"&lt; empty &gt;\"\n        elif self.is_sequence:\n            filename = self.template\n        else:\n            filename = self.filenames[0]\n\n        return \"&lt;{}: \\\"{}\\\"&gt;\".format(\n            self.__class__.__name__,\n            os.path.join(self.directory, filename)\n        )\n\n    @property\n    def label(self) -&gt; Optional[str]:\n        if self.is_empty:\n            return None\n\n        if not self.is_sequence:\n            return self.filenames[0]\n\n        frame_start = self.frames[0]\n        filename_template = os.path.basename(self.template)\n        if len(self.frames) == 1:\n            return \"{} [{}]\".format(filename_template, frame_start)\n\n        frame_end = self.frames[-1]\n        expected_len = (frame_end - frame_start) + 1\n        if expected_len == len(self.frames):\n            return \"{} [{}-{}]\".format(\n                filename_template, frame_start, frame_end\n            )\n\n        ranges = []\n        _frame_start = None\n        _frame_end = None\n        for frame in range(frame_start, frame_end + 1):\n            if frame not in self.frames:\n                add_to_ranges = _frame_start is not None\n            elif _frame_start is None:\n                _frame_start = _frame_end = frame\n                add_to_ranges = frame == frame_end\n            else:\n                _frame_end = frame\n                add_to_ranges = frame == frame_end\n\n            if add_to_ranges:\n                if _frame_start != _frame_end:\n                    _range = \"{}-{}\".format(_frame_start, _frame_end)\n                else:\n                    _range = str(_frame_start)\n                ranges.append(_range)\n                _frame_start = _frame_end = None\n        return \"{} [{}]\".format(\n            filename_template, \",\".join(ranges)\n        )\n\n    def split_sequence(self) -&gt; List[\"Self\"]:\n        if not self.is_sequence:\n            raise ValueError(\"Cannot split single file item\")\n\n        paths = [\n            os.path.join(self.directory, filename)\n            for filename in self.filenames\n        ]\n        return self.from_paths(paths, False)\n\n    @property\n    def ext(self) -&gt; Optional[str]:\n        if self.is_empty:\n            return None\n        _, ext = os.path.splitext(self.filenames[0])\n        if ext:\n            return ext\n        return None\n\n    @property\n    def lower_ext(self) -&gt; Optional[str]:\n        ext = self.ext\n        if ext is not None:\n            return ext.lower()\n        return ext\n\n    @property\n    def is_dir(self) -&gt; bool:\n        if self.is_empty:\n            return False\n\n        # QUESTION a better way how to define folder (in init argument?)\n        if self.ext:\n            return False\n        return True\n\n    def set_directory(self, directory: str):\n        self.directory = directory\n\n    def set_filenames(\n        self,\n        filenames: List[str],\n        frames: Optional[List[int]] = None,\n        template: Optional[str] = None,\n    ):\n        if frames is None:\n            frames = []\n        is_sequence = False\n        if frames:\n            is_sequence = True\n\n        if is_sequence and not template:\n            raise ValueError(\"Missing template for sequence\")\n\n        self.is_empty = len(filenames) == 0\n        self.filenames = filenames\n        self.template = template\n        self.frames = frames\n        self.is_sequence = is_sequence\n\n    @classmethod\n    def create_empty_item(cls) -&gt; \"Self\":\n        return cls(\"\", \"\")\n\n    @classmethod\n    def from_value(\n        cls,\n        value: \"Union[List[FileDefItemDict], FileDefItemDict]\",\n        allow_sequences: bool,\n    ) -&gt; List[\"Self\"]:\n        \"\"\"Convert passed value to FileDefItem objects.\n\n        Returns:\n            list: Created FileDefItem objects.\n\n        \"\"\"\n        # Convert single item to iterable\n        if not isinstance(value, (list, tuple, set)):\n            value = [value]\n\n        output = []\n        str_filepaths = []\n        for item in value:\n            if isinstance(item, dict):\n                item = cls.from_dict(item)\n\n            if isinstance(item, FileDefItem):\n                if not allow_sequences and item.is_sequence:\n                    output.extend(item.split_sequence())\n                else:\n                    output.append(item)\n\n            elif isinstance(item, str):\n                str_filepaths.append(item)\n            else:\n                raise TypeError(\n                    \"Unknown type \\\"{}\\\". Can't convert to {}\".format(\n                        str(type(item)), cls.__name__\n                    )\n                )\n\n        if str_filepaths:\n            output.extend(cls.from_paths(str_filepaths, allow_sequences))\n\n        return output\n\n    @classmethod\n    def from_dict(cls, data: \"FileDefItemDict\") -&gt; \"Self\":\n        return cls(\n            data[\"directory\"],\n            data[\"filenames\"],\n            data.get(\"frames\"),\n            data.get(\"template\")\n        )\n\n    @classmethod\n    def from_paths(\n        cls,\n        paths: List[str],\n        allow_sequences: bool,\n    ) -&gt; List[\"Self\"]:\n        filenames_by_dir = collections.defaultdict(list)\n        for path in paths:\n            normalized = os.path.normpath(path)\n            directory, filename = os.path.split(normalized)\n            filenames_by_dir[directory].append(filename)\n\n        output = []\n        for directory, filenames in filenames_by_dir.items():\n            if allow_sequences:\n                cols, remainders = clique.assemble(filenames)\n            else:\n                cols = []\n                remainders = filenames\n\n            for remainder in remainders:\n                output.append(cls(directory, [remainder]))\n\n            for col in cols:\n                frames = list(col.indexes)\n                paths = [filename for filename in col]\n                template = col.format(\"{head}{padding}{tail}\")\n\n                output.append(cls(\n                    directory, paths, frames, template\n                ))\n\n        return output\n\n    def to_dict(self) -&gt; \"FileDefItemDict\":\n        output = {\n            \"is_sequence\": self.is_sequence,\n            \"directory\": self.directory,\n            \"filenames\": list(self.filenames),\n        }\n        if self.is_sequence:\n            output.update({\n                \"template\": self.template,\n                \"frames\": list(sorted(self.frames)),\n            })\n\n        return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/attribute_definitions.html#client.ayon_core.lib.attribute_definitions.FileDefItem.from_value","title":"<code>from_value(value, allow_sequences)</code>  <code>classmethod</code>","text":"<p>Convert passed value to FileDefItem objects.</p> <p>Returns:</p> Name Type Description <code>list</code> <code>List['Self']</code> <p>Created FileDefItem objects.</p> Source code in <code>client/ayon_core/lib/attribute_definitions.py</code> <pre><code>@classmethod\ndef from_value(\n    cls,\n    value: \"Union[List[FileDefItemDict], FileDefItemDict]\",\n    allow_sequences: bool,\n) -&gt; List[\"Self\"]:\n    \"\"\"Convert passed value to FileDefItem objects.\n\n    Returns:\n        list: Created FileDefItem objects.\n\n    \"\"\"\n    # Convert single item to iterable\n    if not isinstance(value, (list, tuple, set)):\n        value = [value]\n\n    output = []\n    str_filepaths = []\n    for item in value:\n        if isinstance(item, dict):\n            item = cls.from_dict(item)\n\n        if isinstance(item, FileDefItem):\n            if not allow_sequences and item.is_sequence:\n                output.extend(item.split_sequence())\n            else:\n                output.append(item)\n\n        elif isinstance(item, str):\n            str_filepaths.append(item)\n        else:\n            raise TypeError(\n                \"Unknown type \\\"{}\\\". Can't convert to {}\".format(\n                    str(type(item)), cls.__name__\n                )\n            )\n\n    if str_filepaths:\n        output.extend(cls.from_paths(str_filepaths, allow_sequences))\n\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/attribute_definitions.html#client.ayon_core.lib.attribute_definitions.HiddenDef","title":"<code>HiddenDef</code>","text":"<p>               Bases: <code>AbstractAttrDef</code></p> <p>Hidden value of Any type.</p> <p>This attribute can be used for UI purposes to pass values related to other attributes (e.g. in multi-page UIs).</p> <p>Keep in mind the value should be possible to parse by json parser.</p> Source code in <code>client/ayon_core/lib/attribute_definitions.py</code> <pre><code>class HiddenDef(AbstractAttrDef):\n    \"\"\"Hidden value of Any type.\n\n    This attribute can be used for UI purposes to pass values related\n    to other attributes (e.g. in multi-page UIs).\n\n    Keep in mind the value should be possible to parse by json parser.\n\n    \"\"\"\n    type = \"hidden\"\n\n    def __init__(self, key: str, default: Optional[Any] = None, **kwargs):\n        kwargs[\"default\"] = default\n        kwargs[\"visible\"] = False\n        super().__init__(key, **kwargs)\n\n    def is_value_valid(self, value: Any) -&gt; bool:\n        return True\n\n    def convert_value(self, value: Any) -&gt; Any:\n        return value\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/attribute_definitions.html#client.ayon_core.lib.attribute_definitions.NumberDef","title":"<code>NumberDef</code>","text":"<p>               Bases: <code>AbstractAttrDef</code></p> <p>Number definition.</p> <p>Number can have defined minimum/maximum value and decimal points. Value is integer if decimals are 0.</p> <p>Parameters:</p> Name Type Description Default <code>minimum</code> <code>(int, float)</code> <p>Minimum possible value.</p> <code>None</code> <code>maximum</code> <code>(int, float)</code> <p>Maximum possible value.</p> <code>None</code> <code>decimals</code> <code>int</code> <p>Maximum decimal points of value.</p> <code>None</code> <code>default</code> <code>(int, float)</code> <p>Default value for conversion.</p> <code>None</code> Source code in <code>client/ayon_core/lib/attribute_definitions.py</code> <pre><code>class NumberDef(AbstractAttrDef):\n    \"\"\"Number definition.\n\n    Number can have defined minimum/maximum value and decimal points. Value\n    is integer if decimals are 0.\n\n    Args:\n        minimum(int, float): Minimum possible value.\n        maximum(int, float): Maximum possible value.\n        decimals(int): Maximum decimal points of value.\n        default(int, float): Default value for conversion.\n\n    \"\"\"\n    type = \"number\"\n    type_attributes = [\n        \"minimum\",\n        \"maximum\",\n        \"decimals\"\n    ]\n\n    def __init__(\n        self,\n        key: str,\n        minimum: Optional[IntFloatType] = None,\n        maximum: Optional[IntFloatType] = None,\n        decimals: Optional[int] = None,\n        default: Optional[IntFloatType] = None,\n        **kwargs\n    ):\n        minimum = 0 if minimum is None else minimum\n        maximum = 999999 if maximum is None else maximum\n        # Swap min/max when are passed in opposite order\n        if minimum &gt; maximum:\n            maximum, minimum = minimum, maximum\n\n        if default is None:\n            default = 0\n\n        elif not isinstance(default, (int, float)):\n            raise TypeError((\n                \"'default' argument must be 'int' or 'float', not '{}'\"\n            ).format(type(default)))\n\n        # Fix default value by mim/max values\n        if default &lt; minimum:\n            default = minimum\n\n        elif default &gt; maximum:\n            default = maximum\n\n        super().__init__(key, default=default, **kwargs)\n\n        self.minimum: IntFloatType = minimum\n        self.maximum: IntFloatType = maximum\n        self.decimals: int = 0 if decimals is None else decimals\n\n    def is_value_valid(self, value: Any) -&gt; bool:\n        if self.decimals == 0:\n            if not isinstance(value, int):\n                return False\n        elif not isinstance(value, float):\n            return False\n        if self.minimum &gt; value &gt; self.maximum:\n            return False\n        return True\n\n    def convert_value(self, value: Any) -&gt; IntFloatType:\n        if isinstance(value, str):\n            try:\n                value = float(value)\n            except Exception:\n                pass\n\n        if not isinstance(value, (int, float)):\n            return self.default\n\n        if self.decimals == 0:\n            return int(value)\n        return round(float(value), self.decimals)\n\n    def _def_type_compare(self, other: \"NumberDef\") -&gt; bool:\n        return (\n            self.decimals == other.decimals\n            and self.maximum == other.maximum\n            and self.maximum == other.maximum\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/attribute_definitions.html#client.ayon_core.lib.attribute_definitions.TextDef","title":"<code>TextDef</code>","text":"<p>               Bases: <code>AbstractAttrDef</code></p> <p>Text definition.</p> <p>Text can have multiline option so end-line characters are allowed regex validation can be applied placeholder for UI purposes and default value.</p> <p>Regex validation is not part of attribute implementation.</p> <p>Parameters:</p> Name Type Description Default <code>multiline</code> <code>bool</code> <p>Text has single or multiline support.</p> <code>None</code> <code>regex</code> <code>(str, Pattern)</code> <p>Regex validation.</p> <code>None</code> <code>placeholder</code> <code>str</code> <p>UI placeholder for attribute.</p> <code>None</code> <code>default</code> <code>(str, None)</code> <p>Default value. Empty string used when not defined.</p> <code>None</code> Source code in <code>client/ayon_core/lib/attribute_definitions.py</code> <pre><code>class TextDef(AbstractAttrDef):\n    \"\"\"Text definition.\n\n    Text can have multiline option so end-line characters are allowed regex\n    validation can be applied placeholder for UI purposes and default value.\n\n    Regex validation is not part of attribute implementation.\n\n    Args:\n        multiline(bool): Text has single or multiline support.\n        regex(str, re.Pattern): Regex validation.\n        placeholder(str): UI placeholder for attribute.\n        default(str, None): Default value. Empty string used when not defined.\n\n    \"\"\"\n    type = \"text\"\n    type_attributes = [\n        \"multiline\",\n        \"placeholder\",\n    ]\n\n    def __init__(\n        self,\n        key: str,\n        multiline: Optional[bool] = None,\n        regex: Optional[str] = None,\n        placeholder: Optional[str] = None,\n        default: Optional[str] = None,\n        **kwargs\n    ):\n        if default is None:\n            default = \"\"\n\n        super().__init__(key, default=default, **kwargs)\n\n        if multiline is None:\n            multiline = False\n\n        elif not isinstance(default, str):\n            raise TypeError((\n                f\"'default' argument must be a str, not '{type(default)}'\"\n            ))\n\n        if isinstance(regex, str):\n            regex = re.compile(regex)\n\n        self.multiline: bool = multiline\n        self.placeholder: Optional[str] = placeholder\n        self.regex: Optional[\"Pattern\"] = regex\n\n    def is_value_valid(self, value: Any) -&gt; bool:\n        if not isinstance(value, str):\n            return False\n        if self.regex and not self.regex.match(value):\n            return False\n        return True\n\n    def convert_value(self, value: Any) -&gt; str:\n        if isinstance(value, str):\n            return value\n        return self.default\n\n    def serialize(self) -&gt; Dict[str, Any]:\n        data = super().serialize()\n        regex = None\n        if self.regex is not None:\n            regex = self.regex.pattern\n        data[\"regex\"] = regex\n        data[\"multiline\"] = self.multiline\n        data[\"placeholder\"] = self.placeholder\n        return data\n\n    def _def_type_compare(self, other: \"TextDef\") -&gt; bool:\n        return (\n            self.multiline == other.multiline\n            and self.regex == other.regex\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/attribute_definitions.html#client.ayon_core.lib.attribute_definitions.UnknownDef","title":"<code>UnknownDef</code>","text":"<p>               Bases: <code>AbstractAttrDef</code></p> <p>Definition is not known because definition is not available.</p> <p>This attribute can be used to keep existing data unchanged but does not have known definition of type.</p> Source code in <code>client/ayon_core/lib/attribute_definitions.py</code> <pre><code>class UnknownDef(AbstractAttrDef):\n    \"\"\"Definition is not known because definition is not available.\n\n    This attribute can be used to keep existing data unchanged but does not\n    have known definition of type.\n\n    \"\"\"\n    type = \"unknown\"\n\n    def __init__(self, key: str, default: Optional[Any] = None, **kwargs):\n        kwargs[\"default\"] = default\n        super().__init__(key, **kwargs)\n\n    def is_value_valid(self, value: Any) -&gt; bool:\n        return True\n\n    def convert_value(self, value: Any) -&gt; Any:\n        return value\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/attribute_definitions.html#client.ayon_core.lib.attribute_definitions.deserialize_attr_def","title":"<code>deserialize_attr_def(attr_def_data)</code>","text":"<p>Deserialize attribute definition from data.</p> <p>Parameters:</p> Name Type Description Default <code>attr_def_data</code> <code>Dict[str, Any]</code> <p>Attribute definition data to deserialize.</p> required Source code in <code>client/ayon_core/lib/attribute_definitions.py</code> <pre><code>def deserialize_attr_def(attr_def_data: Dict[str, Any]) -&gt; AttrDefType:\n    \"\"\"Deserialize attribute definition from data.\n\n    Args:\n        attr_def_data (Dict[str, Any]): Attribute definition data to\n            deserialize.\n\n    \"\"\"\n    attr_type = attr_def_data.pop(\"type\")\n    cls = _attr_defs_by_type[attr_type]\n    return cls.deserialize(attr_def_data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/attribute_definitions.html#client.ayon_core.lib.attribute_definitions.deserialize_attr_defs","title":"<code>deserialize_attr_defs(attr_defs_data)</code>","text":"<p>Deserialize attribute definitions.</p> <p>Parameters:</p> Name Type Description Default <code>List[Dict[str, Any]]</code> <p>List of attribute definitions.</p> required Source code in <code>client/ayon_core/lib/attribute_definitions.py</code> <pre><code>def deserialize_attr_defs(\n    attr_defs_data: List[Dict[str, Any]]\n) -&gt; List[AttrDefType]:\n    \"\"\"Deserialize attribute definitions.\n\n    Args:\n        List[Dict[str, Any]]: List of attribute definitions.\n\n    \"\"\"\n    return [\n        deserialize_attr_def(attr_def_data)\n        for attr_def_data in attr_defs_data\n    ]\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/attribute_definitions.html#client.ayon_core.lib.attribute_definitions.get_attributes_keys","title":"<code>get_attributes_keys(attribute_definitions)</code>","text":"<p>Collect keys from list of attribute definitions.</p> <p>Parameters:</p> Name Type Description Default <code>attribute_definitions</code> <code>List[AttrDefType]</code> <p>Objects of attribute definitions.</p> required <p>Returns:</p> Type Description <code>Set[str]</code> <p>Set[str]: Keys that will be created using passed attribute definitions.</p> Source code in <code>client/ayon_core/lib/attribute_definitions.py</code> <pre><code>def get_attributes_keys(\n    attribute_definitions: List[AttrDefType]\n) -&gt; Set[str]:\n    \"\"\"Collect keys from list of attribute definitions.\n\n    Args:\n        attribute_definitions (List[AttrDefType]): Objects of attribute\n            definitions.\n\n    Returns:\n        Set[str]: Keys that will be created using passed attribute definitions.\n\n    \"\"\"\n    keys = set()\n    if not attribute_definitions:\n        return keys\n\n    for attribute_def in attribute_definitions:\n        if not isinstance(attribute_def, UIDef):\n            keys.add(attribute_def.key)\n    return keys\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/attribute_definitions.html#client.ayon_core.lib.attribute_definitions.get_default_values","title":"<code>get_default_values(attribute_definitions)</code>","text":"<p>Receive default values for attribute definitions.</p> <p>Parameters:</p> Name Type Description Default <code>attribute_definitions</code> <code>List[AttrDefType]</code> <p>Attribute definitions for which default values should be collected.</p> required <p>Returns:</p> Type Description <code>Dict[str, Any]</code> <p>Dict[str, Any]: Default values for passed attribute definitions.</p> Source code in <code>client/ayon_core/lib/attribute_definitions.py</code> <pre><code>def get_default_values(\n    attribute_definitions: List[AttrDefType]\n) -&gt; Dict[str, Any]:\n    \"\"\"Receive default values for attribute definitions.\n\n    Args:\n        attribute_definitions (List[AttrDefType]): Attribute definitions\n            for which default values should be collected.\n\n    Returns:\n        Dict[str, Any]: Default values for passed attribute definitions.\n\n    \"\"\"\n    output = {}\n    if not attribute_definitions:\n        return output\n\n    for attr_def in attribute_definitions:\n        # Skip UI definitions\n        if not isinstance(attr_def, UIDef):\n            output[attr_def.key] = attr_def.default\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/attribute_definitions.html#client.ayon_core.lib.attribute_definitions.register_attr_def_class","title":"<code>register_attr_def_class(cls)</code>","text":"<p>Register attribute definition.</p> <p>Currently registered definitions are used to deserialize data to objects.</p> Attrs <p>cls (AttrDefType): Non-abstract class to be registered with unique     'type' attribute.</p> <p>Raises:</p> Type Description <code>KeyError</code> <p>When type was already registered.</p> Source code in <code>client/ayon_core/lib/attribute_definitions.py</code> <pre><code>def register_attr_def_class(cls: AttrDefType):\n    \"\"\"Register attribute definition.\n\n    Currently registered definitions are used to deserialize data to objects.\n\n    Attrs:\n        cls (AttrDefType): Non-abstract class to be registered with unique\n            'type' attribute.\n\n    Raises:\n        KeyError: When type was already registered.\n\n    \"\"\"\n    if cls.type in _attr_defs_by_type:\n        raise KeyError(\"Type \\\"{}\\\" was already registered\".format(cls.type))\n    _attr_defs_by_type[cls.type] = cls\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/attribute_definitions.html#client.ayon_core.lib.attribute_definitions.serialize_attr_def","title":"<code>serialize_attr_def(attr_def)</code>","text":"<p>Serialize attribute definition to data.</p> <p>Parameters:</p> Name Type Description Default <code>attr_def</code> <code>AttrDefType</code> <p>Attribute definition to serialize.</p> required <p>Returns:</p> Type Description <code>Dict[str, Any]</code> <p>Dict[str, Any]: Serialized data.</p> Source code in <code>client/ayon_core/lib/attribute_definitions.py</code> <pre><code>def serialize_attr_def(attr_def: AttrDefType) -&gt; Dict[str, Any]:\n    \"\"\"Serialize attribute definition to data.\n\n    Args:\n        attr_def (AttrDefType): Attribute definition to serialize.\n\n    Returns:\n        Dict[str, Any]: Serialized data.\n\n    \"\"\"\n    return attr_def.serialize()\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/attribute_definitions.html#client.ayon_core.lib.attribute_definitions.serialize_attr_defs","title":"<code>serialize_attr_defs(attr_defs)</code>","text":"<p>Serialize attribute definitions to data.</p> <p>Parameters:</p> Name Type Description Default <code>attr_defs</code> <code>List[AttrDefType]</code> <p>Attribute definitions to serialize.</p> required <p>Returns:</p> Type Description <code>List[Dict[str, Any]]</code> <p>List[Dict[str, Any]]: Serialized data.</p> Source code in <code>client/ayon_core/lib/attribute_definitions.py</code> <pre><code>def serialize_attr_defs(\n    attr_defs: List[AttrDefType]\n) -&gt; List[Dict[str, Any]]:\n    \"\"\"Serialize attribute definitions to data.\n\n    Args:\n        attr_defs (List[AttrDefType]): Attribute definitions to serialize.\n\n    Returns:\n        List[Dict[str, Any]]: Serialized data.\n\n    \"\"\"\n    return [\n        serialize_attr_def(attr_def)\n        for attr_def in attr_defs\n    ]\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/ayon_connection.html","title":"ayon_connection","text":""},{"location":"autoapi/client/ayon_core/lib/ayon_connection.html#client.ayon_core.lib.ayon_connection.initialize_ayon_connection","title":"<code>initialize_ayon_connection(force=False)</code>","text":"<p>Initialize global AYON api connection.</p> <p>Create global connection in ayon_api module and set site id     and client version. Is silently skipped if already happened.</p> <p>Parameters:</p> Name Type Description Default <code>force</code> <code>Optional[bool]</code> <p>Force reinitialize connection. Defaults to False.</p> <code>False</code> Source code in <code>client/ayon_core/lib/ayon_connection.py</code> <pre><code>def initialize_ayon_connection(force=False):\n    \"\"\"Initialize global AYON api connection.\n\n    Create global connection in ayon_api module and set site id\n        and client version.\n    Is silently skipped if already happened.\n\n    Args:\n        force (Optional[bool]): Force reinitialize connection.\n            Defaults to False.\n\n    \"\"\"\n    if not force and _Cache.initialized:\n        return\n\n    _Cache.initialized = True\n    ayon_api_version = (\n        semver.VersionInfo.parse(ayon_api.__version__).to_tuple()\n    )\n    # TODO remove mokey patching after when AYON api is safely updated\n    fix_before_1_0_2 = ayon_api_version &lt; (1, 0, 2)\n    # Monkey patching to fix 'get_last_version_by_product_name'\n    if fix_before_1_0_2:\n        ayon_api.ServerAPI.get_last_versions = (\n            _new_get_last_versions\n        )\n        ayon_api.ServerAPI.get_last_version_by_product_id = (\n            _new_get_last_version_by_product_id\n        )\n        ayon_api.ServerAPI.get_last_version_by_product_name = (\n            _new_get_last_version_by_product_name\n        )\n\n    site_id = get_local_site_id()\n    version = os.getenv(\"AYON_VERSION\")\n    if ayon_api.is_connection_created():\n        con = ayon_api.get_server_api_connection()\n        # Monkey patching to fix 'get_last_version_by_product_name'\n        if fix_before_1_0_2:\n            def _lvs_wrapper(*args, **kwargs):\n                return _new_get_last_versions(\n                    con, *args, **kwargs\n                )\n\n            def _lv_by_pi_wrapper(*args, **kwargs):\n                return _new_get_last_version_by_product_id(\n                    con, *args, **kwargs\n                )\n\n            def _lv_by_pn_wrapper(*args, **kwargs):\n                return _new_get_last_version_by_product_name(\n                    con, *args, **kwargs\n                )\n            con.get_last_versions = _lvs_wrapper\n            con.get_last_version_by_product_id = _lv_by_pi_wrapper\n            con.get_last_version_by_product_name = _lv_by_pn_wrapper\n        con.set_site_id(site_id)\n        con.set_client_version(version)\n    else:\n        ayon_api.create_connection(site_id, version)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/ayon_info.html","title":"ayon_info","text":""},{"location":"autoapi/client/ayon_core/lib/ayon_info.html#client.ayon_core.lib.ayon_info.extract_ayon_info_to_file","title":"<code>extract_ayon_info_to_file(dirpath, filename=None)</code>","text":"<p>Extract all current info to a file.</p> <p>It is possible to define only directory path. Filename is concatenated with AYON version, workstation site id and timestamp.</p> <p>Parameters:</p> Name Type Description Default <code>dirpath</code> <code>str</code> <p>Path to directory where file will be stored.</p> required <code>filename</code> <code>Optional[str]</code> <p>Filename. If not defined, it is generated.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>filepath</code> <code>str</code> <p>Full path to file where data were extracted.</p> Source code in <code>client/ayon_core/lib/ayon_info.py</code> <pre><code>def extract_ayon_info_to_file(dirpath, filename=None):\n    \"\"\"Extract all current info to a file.\n\n    It is possible to define only directory path. Filename is concatenated\n    with AYON version, workstation site id and timestamp.\n\n    Args:\n        dirpath (str): Path to directory where file will be stored.\n        filename (Optional[str]): Filename. If not defined, it is generated.\n\n    Returns:\n        filepath (str): Full path to file where data were extracted.\n    \"\"\"\n    if not filename:\n        filename = \"{}_{}.json\".format(\n            get_local_site_id(),\n            datetime.datetime.now().strftime(\"%y%m%d%H%M%S\")\n        )\n    filepath = os.path.join(dirpath, filename)\n    data = get_all_current_info()\n    if not os.path.exists(dirpath):\n        os.makedirs(dirpath)\n\n    with open(filepath, \"w\") as file_stream:\n        json.dump(data, file_stream, indent=4)\n    return filepath\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/ayon_info.html#client.ayon_core.lib.ayon_info.get_all_current_info","title":"<code>get_all_current_info()</code>","text":"<p>All information about current process in one dictionary.</p> Source code in <code>client/ayon_core/lib/ayon_info.py</code> <pre><code>def get_all_current_info():\n    \"\"\"All information about current process in one dictionary.\"\"\"\n\n    return {\n        \"workstation\": get_workstation_info(),\n        \"env\": os.environ.copy(),\n        \"ayon\": get_ayon_info(),\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/ayon_info.html#client.ayon_core.lib.ayon_info.get_ayon_launcher_version","title":"<code>get_ayon_launcher_version()</code>","text":"<p>Get AYON launcher version.</p> <p>Returns:</p> Name Type Description <code>str</code> <p>Version string.</p> Source code in <code>client/ayon_core/lib/ayon_info.py</code> <pre><code>def get_ayon_launcher_version():\n    \"\"\"Get AYON launcher version.\n\n    Returns:\n        str: Version string.\n\n    \"\"\"\n    version_filepath = os.path.join(os.environ[\"AYON_ROOT\"], \"version.py\")\n    if not os.path.exists(version_filepath):\n        return None\n    content = {}\n    with open(version_filepath, \"r\") as stream:\n        exec(stream.read(), content)\n    return content[\"__version__\"]\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/ayon_info.html#client.ayon_core.lib.ayon_info.get_settings_variant","title":"<code>get_settings_variant()</code>","text":"<p>Get AYON settings variant.</p> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Settings variant.</p> Source code in <code>client/ayon_core/lib/ayon_info.py</code> <pre><code>def get_settings_variant() -&gt; str:\n    \"\"\"Get AYON settings variant.\n\n    Returns:\n        str: Settings variant.\n\n    \"\"\"\n    if is_dev_mode_enabled():\n        return os.environ[\"AYON_BUNDLE_NAME\"]\n\n    if is_staging_enabled():\n        return \"staging\"\n\n    return \"production\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/ayon_info.html#client.ayon_core.lib.ayon_info.get_workstation_info","title":"<code>get_workstation_info()</code>","text":"<p>Basic information about workstation.</p> Source code in <code>client/ayon_core/lib/ayon_info.py</code> <pre><code>def get_workstation_info():\n    \"\"\"Basic information about workstation.\"\"\"\n    host_name = socket.gethostname()\n    try:\n        host_ip = socket.gethostbyname(host_name)\n    except socket.gaierror:\n        host_ip = \"127.0.0.1\"\n\n    return {\n        \"hostname\": host_name,\n        \"host_ip\": host_ip,\n        \"username\": getpass.getuser(),\n        \"system_name\": platform.system(),\n        \"local_id\": get_local_site_id()\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/ayon_info.html#client.ayon_core.lib.ayon_info.is_dev_mode_enabled","title":"<code>is_dev_mode_enabled()</code>","text":"<p>Dev mode is enabled in AYON.</p> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if dev mode is enabled.</p> Source code in <code>client/ayon_core/lib/ayon_info.py</code> <pre><code>def is_dev_mode_enabled() -&gt; bool:\n    \"\"\"Dev mode is enabled in AYON.\n\n    Returns:\n        bool: True if dev mode is enabled.\n    \"\"\"\n\n    return os.getenv(\"AYON_USE_DEV\") == \"1\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/ayon_info.html#client.ayon_core.lib.ayon_info.is_in_ayon_launcher_process","title":"<code>is_in_ayon_launcher_process()</code>","text":"<p>Determine if current process is running from AYON launcher.</p> <p>Returns:</p> Name Type Description <code>bool</code> <p>True if running from AYON launcher.</p> Source code in <code>client/ayon_core/lib/ayon_info.py</code> <pre><code>def is_in_ayon_launcher_process():\n    \"\"\"Determine if current process is running from AYON launcher.\n\n    Returns:\n        bool: True if running from AYON launcher.\n\n    \"\"\"\n    ayon_executable_path = os.path.normpath(os.environ[\"AYON_EXECUTABLE\"])\n    executable_path = os.path.normpath(sys.executable)\n    return ayon_executable_path == executable_path\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/ayon_info.html#client.ayon_core.lib.ayon_info.is_in_tests","title":"<code>is_in_tests()</code>","text":"<p>Process is running in automatic tests mode.</p> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if running in tests.</p> Source code in <code>client/ayon_core/lib/ayon_info.py</code> <pre><code>def is_in_tests() -&gt; bool:\n    \"\"\"Process is running in automatic tests mode.\n\n    Returns:\n        bool: True if running in tests.\n\n    \"\"\"\n    return os.environ.get(\"AYON_IN_TESTS\") == \"1\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/ayon_info.html#client.ayon_core.lib.ayon_info.is_running_from_build","title":"<code>is_running_from_build()</code>","text":"<p>Determine if current process is running from build or code.</p> <p>Returns:</p> Name Type Description <code>bool</code> <p>True if running from build.</p> Source code in <code>client/ayon_core/lib/ayon_info.py</code> <pre><code>def is_running_from_build():\n    \"\"\"Determine if current process is running from build or code.\n\n    Returns:\n        bool: True if running from build.\n\n    \"\"\"\n    executable_path = os.environ[\"AYON_EXECUTABLE\"]\n    executable_filename = os.path.basename(executable_path)\n    if \"python\" in executable_filename.lower():\n        return False\n    return True\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/ayon_info.html#client.ayon_core.lib.ayon_info.is_using_ayon_console","title":"<code>is_using_ayon_console()</code>","text":"<p>AYON launcher console executable is used.</p> <p>This function make sense only on Windows platform. For other platforms always returns True. True is also returned if process is running from code.</p> <p>AYON launcher on windows has 2 executable files. First 'ayon_console.exe' works as 'python.exe' executable, the second 'ayon.exe' works as 'pythonw.exe' executable. The difference is way how stdout/stderr is handled (especially when calling subprocess).</p> <p>Returns:</p> Name Type Description <code>bool</code> <p>True if console executable is used.</p> Source code in <code>client/ayon_core/lib/ayon_info.py</code> <pre><code>def is_using_ayon_console():\n    \"\"\"AYON launcher console executable is used.\n\n    This function make sense only on Windows platform. For other platforms\n    always returns True. True is also returned if process is running from\n    code.\n\n    AYON launcher on windows has 2 executable files. First 'ayon_console.exe'\n    works as 'python.exe' executable, the second 'ayon.exe' works as\n    'pythonw.exe' executable. The difference is way how stdout/stderr is\n    handled (especially when calling subprocess).\n\n    Returns:\n        bool: True if console executable is used.\n\n    \"\"\"\n    if (\n        platform.system().lower() != \"windows\"\n        or is_running_from_build()\n    ):\n        return True\n    executable_path = os.environ[\"AYON_EXECUTABLE\"]\n    executable_filename = os.path.basename(executable_path)\n    return \"ayon_console\" in executable_filename\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/cache.html","title":"cache","text":""},{"location":"autoapi/client/ayon_core/lib/cache.html#client.ayon_core.lib.cache.CacheItem","title":"<code>CacheItem</code>","text":"<p>Simple cache item with lifetime and default factory for default value.</p> <p>Default factory should return default value that is used on init     and on reset.</p> <p>Parameters:</p> Name Type Description Default <code>default_factory</code> <code>Optional[callable]</code> <p>Function that returns default value used on init and on reset.</p> <code>None</code> <code>lifetime</code> <code>Optional[int]</code> <p>Lifetime of the cache data in seconds. Default lifetime is 120 seconds.</p> <code>None</code> Source code in <code>client/ayon_core/lib/cache.py</code> <pre><code>class CacheItem:\n    \"\"\"Simple cache item with lifetime and default factory for default value.\n\n    Default factory should return default value that is used on init\n        and on reset.\n\n    Args:\n        default_factory (Optional[callable]): Function that returns default\n            value used on init and on reset.\n        lifetime (Optional[int]): Lifetime of the cache data in seconds.\n            Default lifetime is 120 seconds.\n\n    \"\"\"\n    def __init__(self, default_factory=None, lifetime=None):\n        if lifetime is None:\n            lifetime = 120\n        self._lifetime = lifetime\n        self._last_update = None\n        if default_factory is None:\n            default_factory = _default_factory_func\n        self._default_factory = default_factory\n        self._data = default_factory()\n\n    @property\n    def is_valid(self):\n        \"\"\"Is cache valid to use.\n\n        Return:\n            bool: True if cache is valid, False otherwise.\n\n        \"\"\"\n        if self._last_update is None:\n            return False\n\n        return (time.time() - self._last_update) &lt; self._lifetime\n\n    def set_lifetime(self, lifetime):\n        \"\"\"Change lifetime of cache item.\n\n        Args:\n            lifetime (int): Lifetime of the cache data in seconds.\n        \"\"\"\n\n        self._lifetime = lifetime\n\n    def set_invalid(self):\n        \"\"\"Set cache as invalid.\"\"\"\n\n        self._last_update = None\n\n    def reset(self):\n        \"\"\"Set cache as invalid and reset data.\"\"\"\n\n        self._last_update = None\n        self._data = self._default_factory()\n\n    def get_data(self):\n        \"\"\"Receive cached data.\n\n        Returns:\n            Any: Any data that are cached.\n\n        \"\"\"\n        return self._data\n\n    def update_data(self, data):\n        \"\"\"Update cache data.\n\n        Args:\n            data (Any): Any data that are cached.\n\n        \"\"\"\n        self._data = data\n        self._last_update = time.time()\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/cache.html#client.ayon_core.lib.cache.CacheItem.is_valid","title":"<code>is_valid</code>  <code>property</code>","text":"<p>Is cache valid to use.</p> Return <p>bool: True if cache is valid, False otherwise.</p>"},{"location":"autoapi/client/ayon_core/lib/cache.html#client.ayon_core.lib.cache.CacheItem.get_data","title":"<code>get_data()</code>","text":"<p>Receive cached data.</p> <p>Returns:</p> Name Type Description <code>Any</code> <p>Any data that are cached.</p> Source code in <code>client/ayon_core/lib/cache.py</code> <pre><code>def get_data(self):\n    \"\"\"Receive cached data.\n\n    Returns:\n        Any: Any data that are cached.\n\n    \"\"\"\n    return self._data\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/cache.html#client.ayon_core.lib.cache.CacheItem.reset","title":"<code>reset()</code>","text":"<p>Set cache as invalid and reset data.</p> Source code in <code>client/ayon_core/lib/cache.py</code> <pre><code>def reset(self):\n    \"\"\"Set cache as invalid and reset data.\"\"\"\n\n    self._last_update = None\n    self._data = self._default_factory()\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/cache.html#client.ayon_core.lib.cache.CacheItem.set_invalid","title":"<code>set_invalid()</code>","text":"<p>Set cache as invalid.</p> Source code in <code>client/ayon_core/lib/cache.py</code> <pre><code>def set_invalid(self):\n    \"\"\"Set cache as invalid.\"\"\"\n\n    self._last_update = None\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/cache.html#client.ayon_core.lib.cache.CacheItem.set_lifetime","title":"<code>set_lifetime(lifetime)</code>","text":"<p>Change lifetime of cache item.</p> <p>Parameters:</p> Name Type Description Default <code>lifetime</code> <code>int</code> <p>Lifetime of the cache data in seconds.</p> required Source code in <code>client/ayon_core/lib/cache.py</code> <pre><code>def set_lifetime(self, lifetime):\n    \"\"\"Change lifetime of cache item.\n\n    Args:\n        lifetime (int): Lifetime of the cache data in seconds.\n    \"\"\"\n\n    self._lifetime = lifetime\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/cache.html#client.ayon_core.lib.cache.CacheItem.update_data","title":"<code>update_data(data)</code>","text":"<p>Update cache data.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>Any</code> <p>Any data that are cached.</p> required Source code in <code>client/ayon_core/lib/cache.py</code> <pre><code>def update_data(self, data):\n    \"\"\"Update cache data.\n\n    Args:\n        data (Any): Any data that are cached.\n\n    \"\"\"\n    self._data = data\n    self._last_update = time.time()\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/cache.html#client.ayon_core.lib.cache.NestedCacheItem","title":"<code>NestedCacheItem</code>","text":"<p>Helper for cached items stored in nested structure.</p> Example <p>cache = NestedCacheItem(levels=2, default_factory=lambda: 0) cache[\"a\"][\"b\"].is_valid False cache[\"a\"][\"b\"].get_data() 0 cache[\"a\"][\"b\"] = 1 cache[\"a\"][\"b\"].is_valid True cache[\"a\"][\"b\"].get_data() 1 cache.reset() cache[\"a\"][\"b\"].is_valid False</p> <p>Parameters:</p> Name Type Description Default <code>levels</code> <code>int</code> <p>Number of nested levels where read cache is stored.</p> <code>1</code> <code>default_factory</code> <code>Optional[callable]</code> <p>Function that returns default value used on init and on reset.</p> <code>None</code> <code>lifetime</code> <code>Optional[int]</code> <p>Lifetime of the cache data in seconds. Default value is based on default value of 'CacheItem'.</p> <code>None</code> <code>_init_info</code> <code>Optional[InitInfo]</code> <p>Private argument. Init info for nested cache where created from parent item.</p> <code>None</code> Source code in <code>client/ayon_core/lib/cache.py</code> <pre><code>class NestedCacheItem:\n    \"\"\"Helper for cached items stored in nested structure.\n\n    Example:\n        &gt;&gt;&gt; cache = NestedCacheItem(levels=2, default_factory=lambda: 0)\n        &gt;&gt;&gt; cache[\"a\"][\"b\"].is_valid\n        False\n        &gt;&gt;&gt; cache[\"a\"][\"b\"].get_data()\n        0\n        &gt;&gt;&gt; cache[\"a\"][\"b\"] = 1\n        &gt;&gt;&gt; cache[\"a\"][\"b\"].is_valid\n        True\n        &gt;&gt;&gt; cache[\"a\"][\"b\"].get_data()\n        1\n        &gt;&gt;&gt; cache.reset()\n        &gt;&gt;&gt; cache[\"a\"][\"b\"].is_valid\n        False\n\n    Args:\n        levels (int): Number of nested levels where read cache is stored.\n        default_factory (Optional[callable]): Function that returns default\n            value used on init and on reset.\n        lifetime (Optional[int]): Lifetime of the cache data in seconds.\n            Default value is based on default value of 'CacheItem'.\n        _init_info (Optional[InitInfo]): Private argument. Init info for\n            nested cache where created from parent item.\n\n    \"\"\"\n    def __init__(\n        self, levels=1, default_factory=None, lifetime=None, _init_info=None\n    ):\n        if levels &lt; 1:\n            raise ValueError(\"Nested levels must be greater than 0\")\n        self._data_by_key = {}\n        if _init_info is None:\n            _init_info = InitInfo(default_factory, lifetime)\n        self._init_info = _init_info\n        self._levels = levels\n\n    def __getitem__(self, key):\n        \"\"\"Get cached data.\n\n        Args:\n            key (str): Key of the cache item.\n\n        Returns:\n            Union[NestedCacheItem, CacheItem]: Cache item.\n\n        \"\"\"\n        cache = self._data_by_key.get(key)\n        if cache is None:\n            if self._levels &gt; 1:\n                cache = NestedCacheItem(\n                    levels=self._levels - 1,\n                    _init_info=self._init_info\n                )\n            else:\n                cache = CacheItem(\n                    self._init_info.default_factory,\n                    self._init_info.lifetime\n                )\n            self._data_by_key[key] = cache\n        return cache\n\n    def __setitem__(self, key, value):\n        \"\"\"Update cached data.\n\n        Args:\n            key (str): Key of the cache item.\n            value (Any): Any data that are cached.\n\n        \"\"\"\n        if self._levels &gt; 1:\n            raise AttributeError((\n                \"{} does not support '__setitem__'. Lower nested level by {}\"\n            ).format(self.__class__.__name__, self._levels - 1))\n        cache = self[key]\n        cache.update_data(value)\n\n    def get(self, key):\n        \"\"\"Get cached data.\n\n        Args:\n            key (str): Key of the cache item.\n\n        Returns:\n            Union[NestedCacheItem, CacheItem]: Cache item.\n\n        \"\"\"\n        return self[key]\n\n    def cached_count(self):\n        \"\"\"Amount of cached items.\n\n        Returns:\n            int: Amount of cached items.\n\n        \"\"\"\n        return len(self._data_by_key)\n\n    def clear_key(self, key):\n        \"\"\"Clear cached item by key.\n\n        Args:\n            key (str): Key of the cache item.\n\n        \"\"\"\n        self._data_by_key.pop(key, None)\n\n    def clear_invalid(self):\n        \"\"\"Clear all invalid cache items.\n\n        Note:\n            To clear all cache items use 'reset'.\n\n        \"\"\"\n        changed = {}\n        children_are_nested = self._levels &gt; 1\n        for key, cache in tuple(self._data_by_key.items()):\n            if children_are_nested:\n                output = cache.clear_invalid()\n                if output:\n                    changed[key] = output\n                if not cache.cached_count():\n                    self._data_by_key.pop(key)\n            elif not cache.is_valid:\n                changed[key] = cache.get_data()\n                self._data_by_key.pop(key)\n        return changed\n\n    def reset(self):\n        \"\"\"Reset cache.\n\n        Note:\n            To clear only invalid cache items use 'clear_invalid'.\n\n        \"\"\"\n        self._data_by_key = {}\n\n    def set_lifetime(self, lifetime):\n        \"\"\"Change lifetime of all children cache items.\n\n        Args:\n            lifetime (int): Lifetime of the cache data in seconds.\n\n        \"\"\"\n        self._init_info.lifetime = lifetime\n        for cache in self._data_by_key.values():\n            cache.set_lifetime(lifetime)\n\n    @property\n    def is_valid(self):\n        \"\"\"Raise reasonable error when called on wrong level.\n\n        Raises:\n            AttributeError: If called on nested cache item.\n\n        \"\"\"\n        raise AttributeError((\n            \"{} does not support 'is_valid'. Lower nested level by '{}'\"\n        ).format(self.__class__.__name__, self._levels))\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/cache.html#client.ayon_core.lib.cache.NestedCacheItem.is_valid","title":"<code>is_valid</code>  <code>property</code>","text":"<p>Raise reasonable error when called on wrong level.</p> <p>Raises:</p> Type Description <code>AttributeError</code> <p>If called on nested cache item.</p>"},{"location":"autoapi/client/ayon_core/lib/cache.html#client.ayon_core.lib.cache.NestedCacheItem.__getitem__","title":"<code>__getitem__(key)</code>","text":"<p>Get cached data.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>Key of the cache item.</p> required <p>Returns:</p> Type Description <p>Union[NestedCacheItem, CacheItem]: Cache item.</p> Source code in <code>client/ayon_core/lib/cache.py</code> <pre><code>def __getitem__(self, key):\n    \"\"\"Get cached data.\n\n    Args:\n        key (str): Key of the cache item.\n\n    Returns:\n        Union[NestedCacheItem, CacheItem]: Cache item.\n\n    \"\"\"\n    cache = self._data_by_key.get(key)\n    if cache is None:\n        if self._levels &gt; 1:\n            cache = NestedCacheItem(\n                levels=self._levels - 1,\n                _init_info=self._init_info\n            )\n        else:\n            cache = CacheItem(\n                self._init_info.default_factory,\n                self._init_info.lifetime\n            )\n        self._data_by_key[key] = cache\n    return cache\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/cache.html#client.ayon_core.lib.cache.NestedCacheItem.__setitem__","title":"<code>__setitem__(key, value)</code>","text":"<p>Update cached data.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>Key of the cache item.</p> required <code>value</code> <code>Any</code> <p>Any data that are cached.</p> required Source code in <code>client/ayon_core/lib/cache.py</code> <pre><code>def __setitem__(self, key, value):\n    \"\"\"Update cached data.\n\n    Args:\n        key (str): Key of the cache item.\n        value (Any): Any data that are cached.\n\n    \"\"\"\n    if self._levels &gt; 1:\n        raise AttributeError((\n            \"{} does not support '__setitem__'. Lower nested level by {}\"\n        ).format(self.__class__.__name__, self._levels - 1))\n    cache = self[key]\n    cache.update_data(value)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/cache.html#client.ayon_core.lib.cache.NestedCacheItem.cached_count","title":"<code>cached_count()</code>","text":"<p>Amount of cached items.</p> <p>Returns:</p> Name Type Description <code>int</code> <p>Amount of cached items.</p> Source code in <code>client/ayon_core/lib/cache.py</code> <pre><code>def cached_count(self):\n    \"\"\"Amount of cached items.\n\n    Returns:\n        int: Amount of cached items.\n\n    \"\"\"\n    return len(self._data_by_key)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/cache.html#client.ayon_core.lib.cache.NestedCacheItem.clear_invalid","title":"<code>clear_invalid()</code>","text":"<p>Clear all invalid cache items.</p> Note <p>To clear all cache items use 'reset'.</p> Source code in <code>client/ayon_core/lib/cache.py</code> <pre><code>def clear_invalid(self):\n    \"\"\"Clear all invalid cache items.\n\n    Note:\n        To clear all cache items use 'reset'.\n\n    \"\"\"\n    changed = {}\n    children_are_nested = self._levels &gt; 1\n    for key, cache in tuple(self._data_by_key.items()):\n        if children_are_nested:\n            output = cache.clear_invalid()\n            if output:\n                changed[key] = output\n            if not cache.cached_count():\n                self._data_by_key.pop(key)\n        elif not cache.is_valid:\n            changed[key] = cache.get_data()\n            self._data_by_key.pop(key)\n    return changed\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/cache.html#client.ayon_core.lib.cache.NestedCacheItem.clear_key","title":"<code>clear_key(key)</code>","text":"<p>Clear cached item by key.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>Key of the cache item.</p> required Source code in <code>client/ayon_core/lib/cache.py</code> <pre><code>def clear_key(self, key):\n    \"\"\"Clear cached item by key.\n\n    Args:\n        key (str): Key of the cache item.\n\n    \"\"\"\n    self._data_by_key.pop(key, None)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/cache.html#client.ayon_core.lib.cache.NestedCacheItem.get","title":"<code>get(key)</code>","text":"<p>Get cached data.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>Key of the cache item.</p> required <p>Returns:</p> Type Description <p>Union[NestedCacheItem, CacheItem]: Cache item.</p> Source code in <code>client/ayon_core/lib/cache.py</code> <pre><code>def get(self, key):\n    \"\"\"Get cached data.\n\n    Args:\n        key (str): Key of the cache item.\n\n    Returns:\n        Union[NestedCacheItem, CacheItem]: Cache item.\n\n    \"\"\"\n    return self[key]\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/cache.html#client.ayon_core.lib.cache.NestedCacheItem.reset","title":"<code>reset()</code>","text":"<p>Reset cache.</p> Note <p>To clear only invalid cache items use 'clear_invalid'.</p> Source code in <code>client/ayon_core/lib/cache.py</code> <pre><code>def reset(self):\n    \"\"\"Reset cache.\n\n    Note:\n        To clear only invalid cache items use 'clear_invalid'.\n\n    \"\"\"\n    self._data_by_key = {}\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/cache.html#client.ayon_core.lib.cache.NestedCacheItem.set_lifetime","title":"<code>set_lifetime(lifetime)</code>","text":"<p>Change lifetime of all children cache items.</p> <p>Parameters:</p> Name Type Description Default <code>lifetime</code> <code>int</code> <p>Lifetime of the cache data in seconds.</p> required Source code in <code>client/ayon_core/lib/cache.py</code> <pre><code>def set_lifetime(self, lifetime):\n    \"\"\"Change lifetime of all children cache items.\n\n    Args:\n        lifetime (int): Lifetime of the cache data in seconds.\n\n    \"\"\"\n    self._init_info.lifetime = lifetime\n    for cache in self._data_by_key.values():\n        cache.set_lifetime(lifetime)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/dateutils.html","title":"dateutils","text":"<p>Get configuration data.</p>"},{"location":"autoapi/client/ayon_core/lib/dateutils.html#client.ayon_core.lib.dateutils.get_datetime_data","title":"<code>get_datetime_data(datetime_obj=None)</code>","text":"<p>Returns current datetime data as dictionary.</p> <p>Parameters:</p> Name Type Description Default <code>datetime_obj</code> <code>datetime</code> <p>Specific datetime object</p> <code>None</code> <p>Returns:</p> Name Type Description <code>dict</code> <p>prepared date &amp; time data</p> Available keys <p>\"d\" -  in shortest possible way. \"dd\" -  with 2 digits. \"ddd\" -  shortened week day. e.g.: <code>Mon</code>, ... \"dddd\" -  full name of week day. e.g.: <code>Monday</code>, ... \"m\" -  in shortest possible way. e.g.: <code>1</code> if January \"mm\" -  with 2 digits. \"mmm\" -  shortened month name. e.g.: <code>Jan</code>, ... \"mmmm\" -  full month name. e.g.: <code>January</code>, ... \"yy\" -  shortened year. e.g.: <code>19</code>, <code>20</code>, ... \"yyyy\" -  full year. e.g.: <code>2019</code>, <code>2020</code>, ... \"H\" -  shortened hours. \"HH\" -  with 2 digits. \"h\" -  shortened hours. \"hh\" -  with 2 digits. \"ht\" -  AM or PM. \"M\" -  shortened minutes. \"MM\" -  with 2 digits. \"S\" -  shortened seconds. \"SS\" -  with 2 digits. Source code in <code>client/ayon_core/lib/dateutils.py</code> <pre><code>def get_datetime_data(datetime_obj=None):\n    \"\"\"Returns current datetime data as dictionary.\n\n    Args:\n        datetime_obj (datetime): Specific datetime object\n\n    Returns:\n        dict: prepared date &amp; time data\n\n    Available keys:\n        \"d\" - &lt;Day of month number&gt; in shortest possible way.\n        \"dd\" - &lt;Day of month number&gt; with 2 digits.\n        \"ddd\" - &lt;Week day name&gt; shortened week day. e.g.: `Mon`, ...\n        \"dddd\" - &lt;Week day name&gt; full name of week day. e.g.: `Monday`, ...\n        \"m\" - &lt;Month number&gt; in shortest possible way. e.g.: `1` if January\n        \"mm\" - &lt;Month number&gt; with 2 digits.\n        \"mmm\" - &lt;Month name&gt; shortened month name. e.g.: `Jan`, ...\n        \"mmmm\" - &lt;Month name&gt; full month name. e.g.: `January`, ...\n        \"yy\" - &lt;Year number&gt; shortened year. e.g.: `19`, `20`, ...\n        \"yyyy\" - &lt;Year number&gt; full year. e.g.: `2019`, `2020`, ...\n        \"H\" - &lt;Hours number 24-hour&gt; shortened hours.\n        \"HH\" - &lt;Hours number 24-hour&gt; with 2 digits.\n        \"h\" - &lt;Hours number 12-hour&gt; shortened hours.\n        \"hh\" - &lt;Hours number 12-hour&gt; with 2 digits.\n        \"ht\" - &lt;Midday type&gt; AM or PM.\n        \"M\" - &lt;Minutes number&gt; shortened minutes.\n        \"MM\" - &lt;Minutes number&gt; with 2 digits.\n        \"S\" - &lt;Seconds number&gt; shortened seconds.\n        \"SS\" - &lt;Seconds number&gt; with 2 digits.\n    \"\"\"\n\n    if not datetime_obj:\n        datetime_obj = datetime.datetime.now()\n\n    year = datetime_obj.strftime(\"%Y\")\n\n    month = datetime_obj.strftime(\"%m\")\n    month_name_full = datetime_obj.strftime(\"%B\")\n    month_name_short = datetime_obj.strftime(\"%b\")\n    day = datetime_obj.strftime(\"%d\")\n\n    weekday_full = datetime_obj.strftime(\"%A\")\n    weekday_short = datetime_obj.strftime(\"%a\")\n\n    hours = datetime_obj.strftime(\"%H\")\n    hours_midday = datetime_obj.strftime(\"%I\")\n    hour_midday_type = datetime_obj.strftime(\"%p\")\n    minutes = datetime_obj.strftime(\"%M\")\n    seconds = datetime_obj.strftime(\"%S\")\n\n    return {\n        \"d\": str(int(day)),\n        \"dd\": str(day),\n        \"ddd\": weekday_short,\n        \"dddd\": weekday_full,\n        \"m\": str(int(month)),\n        \"mm\": str(month),\n        \"mmm\": month_name_short,\n        \"mmmm\": month_name_full,\n        \"yy\": str(year[2:]),\n        \"yyyy\": str(year),\n        \"H\": str(int(hours)),\n        \"HH\": str(hours),\n        \"h\": str(int(hours_midday)),\n        \"hh\": str(hours_midday),\n        \"ht\": hour_midday_type,\n        \"M\": str(int(minutes)),\n        \"MM\": str(minutes),\n        \"S\": str(int(seconds)),\n        \"SS\": str(seconds),\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/dateutils.html#client.ayon_core.lib.dateutils.get_timestamp","title":"<code>get_timestamp(datetime_obj=None)</code>","text":"<p>Get standardized timestamp from datetime object.</p> <p>Parameters:</p> Name Type Description Default <code>datetime_obj</code> <code>datetime</code> <p>Object of datetime. Current time is used if not passed.</p> <code>None</code> Source code in <code>client/ayon_core/lib/dateutils.py</code> <pre><code>def get_timestamp(datetime_obj=None):\n    \"\"\"Get standardized timestamp from datetime object.\n\n    Args:\n        datetime_obj (datetime.datetime): Object of datetime. Current time\n            is used if not passed.\n    \"\"\"\n\n    if datetime_obj is None:\n        datetime_obj = datetime.datetime.now()\n    return datetime_obj.strftime(\n        \"%Y%m%dT%H%M%SZ\"\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/env_tools.html","title":"env_tools","text":""},{"location":"autoapi/client/ayon_core/lib/env_tools.html#client.ayon_core.lib.env_tools.CycleError","title":"<code>CycleError</code>","text":"<p>               Bases: <code>ValueError</code></p> <p>Raised when a cycle is detected in dynamic env variables compute.</p> Source code in <code>client/ayon_core/lib/env_tools.py</code> <pre><code>class CycleError(ValueError):\n    \"\"\"Raised when a cycle is detected in dynamic env variables compute.\"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/env_tools.html#client.ayon_core.lib.env_tools.DynamicKeyClashError","title":"<code>DynamicKeyClashError</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Raised when dynamic key clashes with an existing key.</p> Source code in <code>client/ayon_core/lib/env_tools.py</code> <pre><code>class DynamicKeyClashError(Exception):\n    \"\"\"Raised when dynamic key clashes with an existing key.\"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/env_tools.html#client.ayon_core.lib.env_tools.compute_env_variables_structure","title":"<code>compute_env_variables_structure(env, fill_dynamic_keys=True)</code>","text":"<p>Compute the result from recursive dynamic environment.</p> Keys that are not present in the data will remain unformatted as the <p>original keys. So they can be formatted against the current user environment when merging. So {\"A\": \"{key}\"} will remain {key} if not present in the dynamic environment.</p> Source code in <code>client/ayon_core/lib/env_tools.py</code> <pre><code>def compute_env_variables_structure(\n    env: dict[str, str],\n    fill_dynamic_keys: bool = True,\n) -&gt; dict[str, str]:\n    \"\"\"Compute the result from recursive dynamic environment.\n\n    Note: Keys that are not present in the data will remain unformatted as the\n        original keys. So they can be formatted against the current user\n        environment when merging. So {\"A\": \"{key}\"} will remain {key} if not\n        present in the dynamic environment.\n\n    \"\"\"\n    env = env.copy()\n\n    # Collect dependencies\n    dependencies = collections.defaultdict(set)\n    for key, value in env.items():\n        dependent_keys = re.findall(\"{(.+?)}\", value)\n        for dependent_key in dependent_keys:\n            # Ignore reference to itself or key is not in env\n            if dependent_key != key and dependent_key in env:\n                dependencies[key].add(dependent_key)\n\n    ordered, cyclic = _topological_sort(dependencies)\n\n    # Check cycle\n    if cyclic:\n        raise CycleError(f\"A cycle is detected on: {cyclic}\")\n\n    # Format dynamic values\n    for key in reversed(ordered):\n        if key in env:\n            if not isinstance(env[key], str):\n                continue\n            data = env.copy()\n            data.pop(key)    # format without itself\n            env[key] = _partial_format(env[key], data=data)\n\n    # Format dynamic keys\n    if fill_dynamic_keys:\n        formatted = {}\n        for key, value in env.items():\n            if not isinstance(value, str):\n                formatted[key] = value\n                continue\n\n            new_key = _partial_format(key, data=env)\n            if new_key in formatted:\n                raise DynamicKeyClashError(\n                    f\"Key clashes on: {new_key} (source: {key})\"\n                )\n\n            formatted[new_key] = value\n        env = formatted\n\n    return env\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/env_tools.html#client.ayon_core.lib.env_tools.env_value_to_bool","title":"<code>env_value_to_bool(env_key=None, value=None, default=False)</code>","text":"<p>Convert environment variable value to boolean.</p> <p>Function is based on value of the environemt variable. Value is lowered so function is not case sensitive.</p> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>If value match to one of [\"true\", \"yes\", \"1\"] result if True but if value match to [\"false\", \"no\", \"0\"] result is False else default value is returned.</p> Source code in <code>client/ayon_core/lib/env_tools.py</code> <pre><code>def env_value_to_bool(\n    env_key: Optional[str] = None,\n    value: Optional[str] = None,\n    default: bool = False,\n) -&gt; bool:\n    \"\"\"Convert environment variable value to boolean.\n\n    Function is based on value of the environemt variable. Value is lowered\n    so function is not case sensitive.\n\n    Returns:\n        bool: If value match to one of [\"true\", \"yes\", \"1\"] result if True\n            but if value match to [\"false\", \"no\", \"0\"] result is False else\n            default value is returned.\n\n    \"\"\"\n    if value is None and env_key is None:\n        return default\n\n    if value is None:\n        value = os.environ.get(env_key)\n\n    if value is not None:\n        value = str(value).lower()\n        if value in (\"true\", \"yes\", \"1\", \"on\"):\n            return True\n        elif value in (\"false\", \"no\", \"0\", \"off\"):\n            return False\n    return default\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/env_tools.html#client.ayon_core.lib.env_tools.get_paths_from_environ","title":"<code>get_paths_from_environ(env_key=None, env_value=None, return_first=False)</code>","text":"<p>Return existing paths from specific environment variable.</p> <p>Parameters:</p> Name Type Description Default <code>env_key</code> <code>Optional[str]</code> <p>Environment key where should look for paths.</p> <code>None</code> <code>env_value</code> <code>Optional[str]</code> <p>Value of environment variable. Argument <code>env_key</code> is skipped if this argument is entered.</p> <code>None</code> <code>return_first</code> <code>bool</code> <p>Return first found value or return list of found paths. <code>None</code> or empty list returned if nothing found.</p> <code>False</code> <p>Returns:</p> Type Description <code>Optional[Union[str, list[str]]]</code> <p>Optional[Union[str, list[str]]]: Result of found path/s.</p> Source code in <code>client/ayon_core/lib/env_tools.py</code> <pre><code>def get_paths_from_environ(\n    env_key: Optional[str] = None,\n    env_value: Optional[str] = None,\n    return_first: bool = False,\n) -&gt; Optional[Union[str, list[str]]]:\n    \"\"\"Return existing paths from specific environment variable.\n\n    Args:\n        env_key (Optional[str]): Environment key where should look for paths.\n        env_value (Optional[str]): Value of environment variable.\n            Argument `env_key` is skipped if this argument is entered.\n        return_first (bool): Return first found value or return list of found\n            paths. `None` or empty list returned if nothing found.\n\n    Returns:\n        Optional[Union[str, list[str]]]: Result of found path/s.\n\n    \"\"\"\n    existing_paths = []\n    if not env_key and not env_value:\n        if return_first:\n            return None\n        return existing_paths\n\n    if env_value is None:\n        env_value = os.environ.get(env_key) or \"\"\n\n    path_items = env_value.split(os.pathsep)\n    for path in path_items:\n        # Skip empty string\n        if not path:\n            continue\n        # Normalize path\n        path = os.path.normpath(path)\n        # Check if path exists\n        if os.path.exists(path):\n            # Return path if `return_first` is set to True\n            if return_first:\n                return path\n            # Store path\n            existing_paths.append(path)\n\n    # Return None if none of paths exists\n    if return_first:\n        return None\n    # Return all existing paths from environment variable\n    return existing_paths\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/env_tools.html#client.ayon_core.lib.env_tools.merge_env_variables","title":"<code>merge_env_variables(src_env, dst_env, missing_template=None)</code>","text":"<p>Merge the tools environment with the 'current_env'.</p> <p>This finalizes the join with a current environment by formatting the remainder of dynamic variables with that from the current environment.</p> <p>Remaining missing variables result in an empty value.</p> <p>Parameters:</p> Name Type Description Default <code>src_env</code> <code>dict</code> <p>The dynamic environment</p> required <code>dst_env</code> <code>dict</code> <p>The target environment variables mapping to merge the dynamic environment into.</p> required <code>missing_template</code> <code>str</code> <p>Argument passed to '_partial_format' during merging. <code>None</code> should keep missing keys unchanged.</p> <code>None</code> <p>Returns:</p> Type Description <code>dict[str, str]</code> <p>dict[str, str]: The resulting environment after the merge.</p> Source code in <code>client/ayon_core/lib/env_tools.py</code> <pre><code>def merge_env_variables(\n    src_env: dict[str, str],\n    dst_env: dict[str, str],\n    missing_template: Optional[str] = None,\n) -&gt; dict[str, str]:\n    \"\"\"Merge the tools environment with the 'current_env'.\n\n    This finalizes the join with a current environment by formatting the\n    remainder of dynamic variables with that from the current environment.\n\n    Remaining missing variables result in an empty value.\n\n    Args:\n        src_env (dict): The dynamic environment\n        dst_env (dict): The target environment variables mapping to merge\n            the dynamic environment into.\n        missing_template (str): Argument passed to '_partial_format' during\n            merging. `None` should keep missing keys unchanged.\n\n    Returns:\n        dict[str, str]: The resulting environment after the merge.\n\n    \"\"\"\n    result = dst_env.copy()\n    for key, value in src_env.items():\n        result[key] = _partial_format(\n            str(value), dst_env, missing_template\n        )\n\n    return result\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/env_tools.html#client.ayon_core.lib.env_tools.parse_env_variables_structure","title":"<code>parse_env_variables_structure(env, platform_name=None)</code>","text":"<p>Parse environment for platform-specific values and paths as lists.</p> <p>Parameters:</p> Name Type Description Default <code>env</code> <code>dict</code> <p>The source environment to read.</p> required <code>platform_name</code> <code>Optional[PlatformName]</code> <p>Name of platform to parse for. Defaults to current platform.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, str]</code> <p>The flattened environment for a platform.</p> Source code in <code>client/ayon_core/lib/env_tools.py</code> <pre><code>def parse_env_variables_structure(\n    env: dict[str, EnvValue],\n    platform_name: Optional[PlatformName] = None\n) -&gt; dict[str, str]:\n    \"\"\"Parse environment for platform-specific values and paths as lists.\n\n    Args:\n        env (dict): The source environment to read.\n        platform_name (Optional[PlatformName]): Name of platform to parse for.\n            Defaults to current platform.\n\n    Returns:\n        dict: The flattened environment for a platform.\n\n    \"\"\"\n    if platform_name is None:\n        platform_name = platform.system().lower()\n\n    # Separator based on OS 'os.pathsep' is ';' on Windows and ':' on Unix\n    sep = \";\" if platform_name == \"windows\" else \":\"\n\n    result = {}\n    for variable, value in env.items():\n        # Platform specific values\n        if isinstance(value, dict):\n            value = value.get(platform_name)\n\n        # Allow to have lists as values in the tool data\n        if isinstance(value, (list, tuple)):\n            value = sep.join(value)\n\n        if not value:\n            continue\n\n        if not isinstance(value, str):\n            raise TypeError(f\"Expected 'str' got '{type(value)}'\")\n\n        result[variable] = value\n\n    return result\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/events.html","title":"events","text":"<p>Events holding data about specific event.</p>"},{"location":"autoapi/client/ayon_core/lib/events.html#client.ayon_core.lib.events.Event","title":"<code>Event</code>","text":"<p>Base event object.</p> <p>Can be used for any event because is not specific. Only required argument is topic which defines why event is happening and may be used for filtering.</p> Arg <p>topic (str): Identifier of event. data (Any): Data specific for event. Dictionary is recommended. source (str): Identifier of source. event_system (EventSystem): Event system in which can be event     triggered.</p> Source code in <code>client/ayon_core/lib/events.py</code> <pre><code>class Event:\n    \"\"\"Base event object.\n\n    Can be used for any event because is not specific. Only required argument\n    is topic which defines why event is happening and may be used for\n    filtering.\n\n    Arg:\n        topic (str): Identifier of event.\n        data (Any): Data specific for event. Dictionary is recommended.\n        source (str): Identifier of source.\n        event_system (EventSystem): Event system in which can be event\n            triggered.\n    \"\"\"\n\n    _data = {}\n\n    def __init__(self, topic, data=None, source=None, event_system=None):\n        self._id = str(uuid4())\n        self._topic = topic\n        if data is None:\n            data = {}\n        self._data = data\n        self._source = source\n        self._event_system = event_system\n\n    def __getitem__(self, key):\n        return self._data[key]\n\n    def get(self, key, *args, **kwargs):\n        return self._data.get(key, *args, **kwargs)\n\n    @property\n    def id(self):\n        return self._id\n\n    @property\n    def source(self):\n        \"\"\"Event's source used for triggering callbacks.\n\n        Returns:\n            Union[str, None]: Source string or None. Source is optional.\n        \"\"\"\n\n        return self._source\n\n    @property\n    def data(self):\n        return self._data\n\n    @property\n    def topic(self):\n        \"\"\"Event's topic used for triggering callbacks.\n\n        Returns:\n            str: Topic string.\n        \"\"\"\n\n        return self._topic\n\n    def emit(self):\n        \"\"\"Emit event and trigger callbacks.\"\"\"\n        if self._event_system is None:\n            raise MissingEventSystem(\n                \"Can't emit event {}. Does not have set event system.\".format(\n                    str(repr(self))\n                )\n            )\n        self._event_system.emit_event(self)\n\n    def to_data(self):\n        \"\"\"Convert Event object to data.\n\n        Returns:\n            Dict[str, Any]: Event data.\n        \"\"\"\n\n        return {\n            \"id\": self.id,\n            \"topic\": self.topic,\n            \"source\": self.source,\n            \"data\": copy.deepcopy(self.data)\n        }\n\n    @classmethod\n    def from_data(cls, event_data, event_system=None):\n        \"\"\"Create event from data.\n\n        Args:\n            event_data (Dict[str, Any]): Event data with defined keys. Can be\n                created using 'to_data' method.\n            event_system (EventSystem): System to which the event belongs.\n\n        Returns:\n            Event: Event with attributes from passed data.\n        \"\"\"\n\n        obj = cls(\n            event_data[\"topic\"],\n            event_data[\"data\"],\n            event_data[\"source\"],\n            event_system\n        )\n        obj._id = event_data[\"id\"]\n        return obj\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/events.html#client.ayon_core.lib.events.Event.source","title":"<code>source</code>  <code>property</code>","text":"<p>Event's source used for triggering callbacks.</p> <p>Returns:</p> Type Description <p>Union[str, None]: Source string or None. Source is optional.</p>"},{"location":"autoapi/client/ayon_core/lib/events.html#client.ayon_core.lib.events.Event.topic","title":"<code>topic</code>  <code>property</code>","text":"<p>Event's topic used for triggering callbacks.</p> <p>Returns:</p> Name Type Description <code>str</code> <p>Topic string.</p>"},{"location":"autoapi/client/ayon_core/lib/events.html#client.ayon_core.lib.events.Event.emit","title":"<code>emit()</code>","text":"<p>Emit event and trigger callbacks.</p> Source code in <code>client/ayon_core/lib/events.py</code> <pre><code>def emit(self):\n    \"\"\"Emit event and trigger callbacks.\"\"\"\n    if self._event_system is None:\n        raise MissingEventSystem(\n            \"Can't emit event {}. Does not have set event system.\".format(\n                str(repr(self))\n            )\n        )\n    self._event_system.emit_event(self)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/events.html#client.ayon_core.lib.events.Event.from_data","title":"<code>from_data(event_data, event_system=None)</code>  <code>classmethod</code>","text":"<p>Create event from data.</p> <p>Parameters:</p> Name Type Description Default <code>event_data</code> <code>Dict[str, Any]</code> <p>Event data with defined keys. Can be created using 'to_data' method.</p> required <code>event_system</code> <code>EventSystem</code> <p>System to which the event belongs.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>Event</code> <p>Event with attributes from passed data.</p> Source code in <code>client/ayon_core/lib/events.py</code> <pre><code>@classmethod\ndef from_data(cls, event_data, event_system=None):\n    \"\"\"Create event from data.\n\n    Args:\n        event_data (Dict[str, Any]): Event data with defined keys. Can be\n            created using 'to_data' method.\n        event_system (EventSystem): System to which the event belongs.\n\n    Returns:\n        Event: Event with attributes from passed data.\n    \"\"\"\n\n    obj = cls(\n        event_data[\"topic\"],\n        event_data[\"data\"],\n        event_data[\"source\"],\n        event_system\n    )\n    obj._id = event_data[\"id\"]\n    return obj\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/events.html#client.ayon_core.lib.events.Event.to_data","title":"<code>to_data()</code>","text":"<p>Convert Event object to data.</p> <p>Returns:</p> Type Description <p>Dict[str, Any]: Event data.</p> Source code in <code>client/ayon_core/lib/events.py</code> <pre><code>def to_data(self):\n    \"\"\"Convert Event object to data.\n\n    Returns:\n        Dict[str, Any]: Event data.\n    \"\"\"\n\n    return {\n        \"id\": self.id,\n        \"topic\": self.topic,\n        \"source\": self.source,\n        \"data\": copy.deepcopy(self.data)\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/events.html#client.ayon_core.lib.events.EventCallback","title":"<code>EventCallback</code>","text":"<p>Callback registered to a topic.</p> <p>The callback function is registered to a topic. Topic is a string which may contain '*' that will be handled as \"any characters\".</p>"},{"location":"autoapi/client/ayon_core/lib/events.html#client.ayon_core.lib.events.EventCallback--examples","title":"Examples:","text":"<ul> <li>\"workfile.save\"   Callback will be triggered if the event topic is                     exactly \"workfile.save\" .</li> <li>\"workfile.*\"      Callback will be triggered an event topic starts with                     \"workfile.\" so \"workfile.save\" and \"workfile.open\"                     will trigger the callback.</li> <li>\"*\"               Callback will listen to all events.</li> </ul> <p>Callback can be function or method. In both cases it should expect one or none arguments. When 1 argument is expected then the processed 'Event' object is passed in.</p> <p>The callbacks are validated against their reference counter, that is     achieved using 'weakref' module. That means that the callback must     be stored in memory somewhere. e.g. lambda functions are not     supported as valid callback.</p> <p>You can use 'weakref_partial' functions. In that case is partial object     stored in the callback object and reference counter is checked for     the wrapped function.</p> <p>Parameters:</p> Name Type Description Default <code>topic</code> <code>str</code> <p>Topic which will be listened.</p> required <code>func</code> <code>Callable</code> <p>Callback to a topic.</p> required <code>order</code> <code>Union[int, None]</code> <p>Order of callback. Lower number means higher priority.</p> required <p>Raises:</p> Type Description <code>TypeError</code> <p>When passed function is not a callable object.</p> Source code in <code>client/ayon_core/lib/events.py</code> <pre><code>class EventCallback:\n    \"\"\"Callback registered to a topic.\n\n    The callback function is registered to a topic. Topic is a string which\n    may contain '*' that will be handled as \"any characters\".\n\n    # Examples:\n    - \"workfile.save\"   Callback will be triggered if the event topic is\n                        exactly \"workfile.save\" .\n    - \"workfile.*\"      Callback will be triggered an event topic starts with\n                        \"workfile.\" so \"workfile.save\" and \"workfile.open\"\n                        will trigger the callback.\n    - \"*\"               Callback will listen to all events.\n\n    Callback can be function or method. In both cases it should expect one\n    or none arguments. When 1 argument is expected then the processed 'Event'\n    object is passed in.\n\n    The callbacks are validated against their reference counter, that is\n        achieved using 'weakref' module. That means that the callback must\n        be stored in memory somewhere. e.g. lambda functions are not\n        supported as valid callback.\n\n    You can use 'weakref_partial' functions. In that case is partial object\n        stored in the callback object and reference counter is checked for\n        the wrapped function.\n\n    Args:\n        topic (str): Topic which will be listened.\n        func (Callable): Callback to a topic.\n        order (Union[int, None]): Order of callback. Lower number means higher\n            priority.\n\n    Raises:\n        TypeError: When passed function is not a callable object.\n    \"\"\"\n\n    def __init__(self, topic, func, order):\n        if not callable(func):\n            raise TypeError((\n                \"Registered callback is not callable. \\\"{}\\\"\"\n            ).format(str(func)))\n\n        self._validate_order(order)\n\n        self._log = None\n        self._topic = topic\n        self._order = order\n        self._enabled = True\n        # Replace '*' with any character regex and escape rest of text\n        #   - when callback is registered for '*' topic it will receive all\n        #       events\n        #   - it is possible to register to a partial topis 'my.event.*'\n        #       - it will receive all matching event topics\n        #           e.g. 'my.event.start' and 'my.event.end'\n        topic_regex_str = \"^{}$\".format(\n            \".+\".join(\n                re.escape(part)\n                for part in topic.split(\"*\")\n            )\n        )\n        topic_regex = re.compile(topic_regex_str)\n        self._topic_regex = topic_regex\n\n        # Callback function prep\n        if isinstance(func, weakref_partial):\n            partial_func = func\n            (name, path) = _get_func_info(func.get_func())\n            func_ref = None\n            expect_args = partial_func.validate_signature(\"fake\")\n            expect_kwargs = partial_func.validate_signature(event=\"fake\")\n\n        else:\n            partial_func = None\n            (name, path) = _get_func_info(func)\n            # Convert callback into references\n            #   - deleted functions won't cause crashes\n            func_ref = _get_func_ref(func)\n\n            # Get expected arguments from function spec\n            # - positional arguments are always preferred\n            expect_args = is_func_signature_supported(func, \"fake\")\n            expect_kwargs = is_func_signature_supported(func, event=\"fake\")\n\n        self._func_ref = func_ref\n        self._partial_func = partial_func\n        self._ref_is_valid = True\n        self._expect_args = expect_args\n        self._expect_kwargs = expect_kwargs\n\n        self._name = name\n        self._path = path\n\n    def __repr__(self):\n        return \"&lt; {} - {} &gt; {}\".format(\n            self.__class__.__name__, self._name, self._path\n        )\n\n    @property\n    def log(self):\n        if self._log is None:\n            self._log = logging.getLogger(self.__class__.__name__)\n        return self._log\n\n    @property\n    def is_ref_valid(self):\n        \"\"\"\n\n        Returns:\n            bool: Is reference to callback valid.\n        \"\"\"\n\n        self._validate_ref()\n        return self._ref_is_valid\n\n    def validate_ref(self):\n        \"\"\"Validate if reference to callback is valid.\n\n        Deprecated:\n            Reference is always live checkd with 'is_ref_valid'.\n        \"\"\"\n\n        # Trigger validate by getting 'is_valid'\n        _ = self.is_ref_valid\n\n    @property\n    def enabled(self):\n        \"\"\"Is callback enabled.\n\n        Returns:\n            bool: Is callback enabled.\n        \"\"\"\n\n        return self._enabled\n\n    def set_enabled(self, enabled):\n        \"\"\"Change if callback is enabled.\n\n        Args:\n            enabled (bool): Change enabled state of the callback.\n        \"\"\"\n\n        self._enabled = enabled\n\n    def deregister(self):\n        \"\"\"Calling this function will cause that callback will be removed.\"\"\"\n\n        self._ref_is_valid = False\n        self._partial_func = None\n        self._func_ref = None\n\n    def get_order(self):\n        \"\"\"Get callback order.\n\n        Returns:\n            Union[int, None]: Callback order.\n        \"\"\"\n\n        return self._order\n\n    def set_order(self, order):\n        \"\"\"Change callback order.\n\n        Args:\n            order (Union[int, None]): Order of callback. Lower number means\n                higher priority.\n        \"\"\"\n\n        self._validate_order(order)\n        self._order = order\n\n    order = property(get_order, set_order)\n\n    def topic_matches(self, topic):\n        \"\"\"Check if event topic matches callback's topic.\n\n        Args:\n            topic (str): Topic name.\n\n        Returns:\n            bool: Topic matches callback's topic.\n        \"\"\"\n\n        return self._topic_regex.match(topic)\n\n    def process_event(self, event):\n        \"\"\"Process event.\n\n        Args:\n            event(Event): Event that was triggered.\n        \"\"\"\n\n        # Skip if callback is not enabled\n        if not self._enabled:\n            return\n\n        # Get reference and skip if is not available\n        callback = self._get_callback()\n        if callback is None:\n            return\n\n        if not self.topic_matches(event.topic):\n            return\n\n        # Try to execute callback\n        try:\n            if self._expect_args:\n                callback(event)\n\n            elif self._expect_kwargs:\n                callback(event=event)\n\n            else:\n                callback()\n\n        except Exception:\n            self.log.warning(\n                \"Failed to execute event callback {}\".format(\n                    str(repr(self))\n                ),\n                exc_info=True\n            )\n\n    def _validate_order(self, order):\n        if isinstance(order, int):\n            return\n\n        raise TypeError(\n            \"Expected type 'int' got '{}'.\".format(str(type(order)))\n        )\n\n    def _get_callback(self):\n        if self._partial_func is not None:\n            return self._partial_func\n\n        if self._func_ref is not None:\n            return self._func_ref()\n        return None\n\n    def _validate_ref(self):\n        if self._ref_is_valid is False:\n            return\n\n        if self._func_ref is not None:\n            self._ref_is_valid = self._func_ref() is not None\n\n        elif self._partial_func is not None:\n            self._ref_is_valid = self._partial_func.is_valid()\n\n        else:\n            self._ref_is_valid = False\n\n        if not self._ref_is_valid:\n            self._func_ref = None\n            self._partial_func = None\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/events.html#client.ayon_core.lib.events.EventCallback.enabled","title":"<code>enabled</code>  <code>property</code>","text":"<p>Is callback enabled.</p> <p>Returns:</p> Name Type Description <code>bool</code> <p>Is callback enabled.</p>"},{"location":"autoapi/client/ayon_core/lib/events.html#client.ayon_core.lib.events.EventCallback.is_ref_valid","title":"<code>is_ref_valid</code>  <code>property</code>","text":"<p>Returns:</p> Name Type Description <code>bool</code> <p>Is reference to callback valid.</p>"},{"location":"autoapi/client/ayon_core/lib/events.html#client.ayon_core.lib.events.EventCallback.deregister","title":"<code>deregister()</code>","text":"<p>Calling this function will cause that callback will be removed.</p> Source code in <code>client/ayon_core/lib/events.py</code> <pre><code>def deregister(self):\n    \"\"\"Calling this function will cause that callback will be removed.\"\"\"\n\n    self._ref_is_valid = False\n    self._partial_func = None\n    self._func_ref = None\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/events.html#client.ayon_core.lib.events.EventCallback.get_order","title":"<code>get_order()</code>","text":"<p>Get callback order.</p> <p>Returns:</p> Type Description <p>Union[int, None]: Callback order.</p> Source code in <code>client/ayon_core/lib/events.py</code> <pre><code>def get_order(self):\n    \"\"\"Get callback order.\n\n    Returns:\n        Union[int, None]: Callback order.\n    \"\"\"\n\n    return self._order\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/events.html#client.ayon_core.lib.events.EventCallback.process_event","title":"<code>process_event(event)</code>","text":"<p>Process event.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <code>Event</code> <p>Event that was triggered.</p> required Source code in <code>client/ayon_core/lib/events.py</code> <pre><code>def process_event(self, event):\n    \"\"\"Process event.\n\n    Args:\n        event(Event): Event that was triggered.\n    \"\"\"\n\n    # Skip if callback is not enabled\n    if not self._enabled:\n        return\n\n    # Get reference and skip if is not available\n    callback = self._get_callback()\n    if callback is None:\n        return\n\n    if not self.topic_matches(event.topic):\n        return\n\n    # Try to execute callback\n    try:\n        if self._expect_args:\n            callback(event)\n\n        elif self._expect_kwargs:\n            callback(event=event)\n\n        else:\n            callback()\n\n    except Exception:\n        self.log.warning(\n            \"Failed to execute event callback {}\".format(\n                str(repr(self))\n            ),\n            exc_info=True\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/events.html#client.ayon_core.lib.events.EventCallback.set_enabled","title":"<code>set_enabled(enabled)</code>","text":"<p>Change if callback is enabled.</p> <p>Parameters:</p> Name Type Description Default <code>enabled</code> <code>bool</code> <p>Change enabled state of the callback.</p> required Source code in <code>client/ayon_core/lib/events.py</code> <pre><code>def set_enabled(self, enabled):\n    \"\"\"Change if callback is enabled.\n\n    Args:\n        enabled (bool): Change enabled state of the callback.\n    \"\"\"\n\n    self._enabled = enabled\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/events.html#client.ayon_core.lib.events.EventCallback.set_order","title":"<code>set_order(order)</code>","text":"<p>Change callback order.</p> <p>Parameters:</p> Name Type Description Default <code>order</code> <code>Union[int, None]</code> <p>Order of callback. Lower number means higher priority.</p> required Source code in <code>client/ayon_core/lib/events.py</code> <pre><code>def set_order(self, order):\n    \"\"\"Change callback order.\n\n    Args:\n        order (Union[int, None]): Order of callback. Lower number means\n            higher priority.\n    \"\"\"\n\n    self._validate_order(order)\n    self._order = order\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/events.html#client.ayon_core.lib.events.EventCallback.topic_matches","title":"<code>topic_matches(topic)</code>","text":"<p>Check if event topic matches callback's topic.</p> <p>Parameters:</p> Name Type Description Default <code>topic</code> <code>str</code> <p>Topic name.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <p>Topic matches callback's topic.</p> Source code in <code>client/ayon_core/lib/events.py</code> <pre><code>def topic_matches(self, topic):\n    \"\"\"Check if event topic matches callback's topic.\n\n    Args:\n        topic (str): Topic name.\n\n    Returns:\n        bool: Topic matches callback's topic.\n    \"\"\"\n\n    return self._topic_regex.match(topic)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/events.html#client.ayon_core.lib.events.EventCallback.validate_ref","title":"<code>validate_ref()</code>","text":"<p>Validate if reference to callback is valid.</p> Deprecated <p>Reference is always live checkd with 'is_ref_valid'.</p> Source code in <code>client/ayon_core/lib/events.py</code> <pre><code>def validate_ref(self):\n    \"\"\"Validate if reference to callback is valid.\n\n    Deprecated:\n        Reference is always live checkd with 'is_ref_valid'.\n    \"\"\"\n\n    # Trigger validate by getting 'is_valid'\n    _ = self.is_ref_valid\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/events.html#client.ayon_core.lib.events.EventSystem","title":"<code>EventSystem</code>","text":"<p>Encapsulate event handling into an object.</p> <p>System wraps registered callbacks and triggered events into single object, so it is possible to create multiple independent systems that have their topics and callbacks.</p> <p>Callbacks are stored by order of their registration, but it is possible to manually define order of callbacks using 'order' argument within 'add_callback'.</p> Source code in <code>client/ayon_core/lib/events.py</code> <pre><code>class EventSystem:\n    \"\"\"Encapsulate event handling into an object.\n\n    System wraps registered callbacks and triggered events into single object,\n    so it is possible to create multiple independent systems that have their\n    topics and callbacks.\n\n    Callbacks are stored by order of their registration, but it is possible to\n    manually define order of callbacks using 'order' argument within\n    'add_callback'.\n    \"\"\"\n\n    default_order = 100\n\n    def __init__(self):\n        self._registered_callbacks = []\n\n    def add_callback(self, topic, callback, order=None):\n        \"\"\"Register callback in event system.\n\n        Args:\n            topic (str): Topic for EventCallback.\n            callback (Union[Callable, weakref_partial]): Function or method\n                that will be called when topic is triggered.\n            order (Optional[int]): Order of callback. Lower number means\n                higher priority.\n\n        Returns:\n            EventCallback: Created callback object which can be used to\n                stop listening.\n        \"\"\"\n\n        if order is None:\n            order = self.default_order\n\n        callback = EventCallback(topic, callback, order)\n        self._registered_callbacks.append(callback)\n        return callback\n\n    def create_event(self, topic, data, source):\n        \"\"\"Create new event which is bound to event system.\n\n        Args:\n            topic (str): Event topic.\n            data (dict): Data related to event.\n            source (str): Source of event.\n\n        Returns:\n            Event: Object of event.\n        \"\"\"\n\n        return Event(topic, data, source, self)\n\n    def emit(self, topic, data, source):\n        \"\"\"Create event based on passed data and emit it.\n\n        This is easiest way how to trigger event in an event system.\n\n        Args:\n            topic (str): Event topic.\n            data (dict): Data related to event.\n            source (str): Source of event.\n\n        Returns:\n            Event: Created and emitted event.\n        \"\"\"\n\n        event = self.create_event(topic, data, source)\n        event.emit()\n        return event\n\n    def emit_event(self, event):\n        \"\"\"Emit event object.\n\n        Args:\n            event (Event): Prepared event with topic and data.\n        \"\"\"\n\n        self._process_event(event)\n\n    def clear_callbacks(self):\n        \"\"\"Clear all registered callbacks.\"\"\"\n        self._registered_callbacks = []\n\n    def _process_event(self, event):\n        \"\"\"Process event topic and trigger callbacks.\n\n        Args:\n            event (Event): Prepared event with topic and data.\n        \"\"\"\n\n        callbacks = tuple(sorted(\n            self._registered_callbacks, key=lambda x: x.order\n        ))\n        for callback in callbacks:\n            callback.process_event(event)\n            if not callback.is_ref_valid:\n                self._registered_callbacks.remove(callback)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/events.html#client.ayon_core.lib.events.EventSystem.add_callback","title":"<code>add_callback(topic, callback, order=None)</code>","text":"<p>Register callback in event system.</p> <p>Parameters:</p> Name Type Description Default <code>topic</code> <code>str</code> <p>Topic for EventCallback.</p> required <code>callback</code> <code>Union[Callable, weakref_partial]</code> <p>Function or method that will be called when topic is triggered.</p> required <code>order</code> <code>Optional[int]</code> <p>Order of callback. Lower number means higher priority.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>EventCallback</code> <p>Created callback object which can be used to stop listening.</p> Source code in <code>client/ayon_core/lib/events.py</code> <pre><code>def add_callback(self, topic, callback, order=None):\n    \"\"\"Register callback in event system.\n\n    Args:\n        topic (str): Topic for EventCallback.\n        callback (Union[Callable, weakref_partial]): Function or method\n            that will be called when topic is triggered.\n        order (Optional[int]): Order of callback. Lower number means\n            higher priority.\n\n    Returns:\n        EventCallback: Created callback object which can be used to\n            stop listening.\n    \"\"\"\n\n    if order is None:\n        order = self.default_order\n\n    callback = EventCallback(topic, callback, order)\n    self._registered_callbacks.append(callback)\n    return callback\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/events.html#client.ayon_core.lib.events.EventSystem.clear_callbacks","title":"<code>clear_callbacks()</code>","text":"<p>Clear all registered callbacks.</p> Source code in <code>client/ayon_core/lib/events.py</code> <pre><code>def clear_callbacks(self):\n    \"\"\"Clear all registered callbacks.\"\"\"\n    self._registered_callbacks = []\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/events.html#client.ayon_core.lib.events.EventSystem.create_event","title":"<code>create_event(topic, data, source)</code>","text":"<p>Create new event which is bound to event system.</p> <p>Parameters:</p> Name Type Description Default <code>topic</code> <code>str</code> <p>Event topic.</p> required <code>data</code> <code>dict</code> <p>Data related to event.</p> required <code>source</code> <code>str</code> <p>Source of event.</p> required <p>Returns:</p> Name Type Description <code>Event</code> <p>Object of event.</p> Source code in <code>client/ayon_core/lib/events.py</code> <pre><code>def create_event(self, topic, data, source):\n    \"\"\"Create new event which is bound to event system.\n\n    Args:\n        topic (str): Event topic.\n        data (dict): Data related to event.\n        source (str): Source of event.\n\n    Returns:\n        Event: Object of event.\n    \"\"\"\n\n    return Event(topic, data, source, self)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/events.html#client.ayon_core.lib.events.EventSystem.emit","title":"<code>emit(topic, data, source)</code>","text":"<p>Create event based on passed data and emit it.</p> <p>This is easiest way how to trigger event in an event system.</p> <p>Parameters:</p> Name Type Description Default <code>topic</code> <code>str</code> <p>Event topic.</p> required <code>data</code> <code>dict</code> <p>Data related to event.</p> required <code>source</code> <code>str</code> <p>Source of event.</p> required <p>Returns:</p> Name Type Description <code>Event</code> <p>Created and emitted event.</p> Source code in <code>client/ayon_core/lib/events.py</code> <pre><code>def emit(self, topic, data, source):\n    \"\"\"Create event based on passed data and emit it.\n\n    This is easiest way how to trigger event in an event system.\n\n    Args:\n        topic (str): Event topic.\n        data (dict): Data related to event.\n        source (str): Source of event.\n\n    Returns:\n        Event: Created and emitted event.\n    \"\"\"\n\n    event = self.create_event(topic, data, source)\n    event.emit()\n    return event\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/events.html#client.ayon_core.lib.events.EventSystem.emit_event","title":"<code>emit_event(event)</code>","text":"<p>Emit event object.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <code>Event</code> <p>Prepared event with topic and data.</p> required Source code in <code>client/ayon_core/lib/events.py</code> <pre><code>def emit_event(self, event):\n    \"\"\"Emit event object.\n\n    Args:\n        event (Event): Prepared event with topic and data.\n    \"\"\"\n\n    self._process_event(event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/events.html#client.ayon_core.lib.events.GlobalEventSystem","title":"<code>GlobalEventSystem</code>","text":"<p>Event system living in global scope of process.</p> <p>This is primarily used in host implementation to trigger events related to DCC changes or changes of context in the host implementation.</p> Source code in <code>client/ayon_core/lib/events.py</code> <pre><code>class GlobalEventSystem:\n    \"\"\"Event system living in global scope of process.\n\n    This is primarily used in host implementation to trigger events\n    related to DCC changes or changes of context in the host implementation.\n    \"\"\"\n\n    _global_event_system = None\n\n    @classmethod\n    def get_global_event_system(cls):\n        if cls._global_event_system is None:\n            cls._global_event_system = EventSystem()\n        return cls._global_event_system\n\n    @classmethod\n    def add_callback(cls, topic, callback):\n        event_system = cls.get_global_event_system()\n        return event_system.add_callback(topic, callback)\n\n    @classmethod\n    def emit(cls, topic, data, source):\n        event_system = cls.get_global_event_system()\n        return event_system.emit(topic, data, source)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/events.html#client.ayon_core.lib.events.QueuedEventSystem","title":"<code>QueuedEventSystem</code>","text":"<p>               Bases: <code>EventSystem</code></p> <p>Events are automatically processed in queue.</p> <p>If callback triggers another event, the event is not processed until all callbacks of previous event are processed.</p> <p>Allows to implement custom event process loop by changing 'auto_execute'.</p> Note <p>This probably should be default behavior of 'EventSystem'. Changing it     now could cause problems in existing code.</p> <p>Parameters:</p> Name Type Description Default <code>auto_execute</code> <code>Optional[bool]</code> <p>If 'True', events are processed automatically. Custom loop calling 'process_next_event' must be implemented when set to 'False'.</p> <code>True</code> Source code in <code>client/ayon_core/lib/events.py</code> <pre><code>class QueuedEventSystem(EventSystem):\n    \"\"\"Events are automatically processed in queue.\n\n    If callback triggers another event, the event is not processed until\n    all callbacks of previous event are processed.\n\n    Allows to implement custom event process loop by changing 'auto_execute'.\n\n    Note:\n        This probably should be default behavior of 'EventSystem'. Changing it\n            now could cause problems in existing code.\n\n    Args:\n        auto_execute (Optional[bool]): If 'True', events are processed\n            automatically. Custom loop calling 'process_next_event'\n            must be implemented when set to 'False'.\n    \"\"\"\n\n    def __init__(self, auto_execute=True):\n        super(QueuedEventSystem, self).__init__()\n        self._event_queue = collections.deque()\n        self._current_event = None\n        self._auto_execute = auto_execute\n\n    def __len__(self):\n        return self.count()\n\n    def count(self):\n        \"\"\"Get number of events in queue.\n\n        Returns:\n            int: Number of events in queue.\n        \"\"\"\n\n        return len(self._event_queue)\n\n    def process_next_event(self):\n        \"\"\"Process next event in queue.\n\n        Should be used only if 'auto_execute' is set to 'False'. Only single\n            event is processed.\n\n        Returns:\n            Union[Event, None]: Processed event.\n        \"\"\"\n\n        if self._current_event is not None:\n            raise ValueError(\"An event is already in progress.\")\n\n        if not self._event_queue:\n            return None\n        event = self._event_queue.popleft()\n        self._current_event = event\n        self._process_event(event)\n        self._current_event = None\n        return event\n\n    def emit_event(self, event):\n        \"\"\"Emit event object.\n\n        Args:\n           event (Event): Prepared event with topic and data.\n        \"\"\"\n\n        if not self._auto_execute or self._current_event is not None:\n            self._event_queue.append(event)\n            return\n\n        self._event_queue.append(event)\n        while self._event_queue:\n            event = self._event_queue.popleft()\n            self._current_event = event\n            self._process_event(event)\n        self._current_event = None\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/events.html#client.ayon_core.lib.events.QueuedEventSystem.count","title":"<code>count()</code>","text":"<p>Get number of events in queue.</p> <p>Returns:</p> Name Type Description <code>int</code> <p>Number of events in queue.</p> Source code in <code>client/ayon_core/lib/events.py</code> <pre><code>def count(self):\n    \"\"\"Get number of events in queue.\n\n    Returns:\n        int: Number of events in queue.\n    \"\"\"\n\n    return len(self._event_queue)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/events.html#client.ayon_core.lib.events.QueuedEventSystem.emit_event","title":"<code>emit_event(event)</code>","text":"<p>Emit event object.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <code>Event</code> <p>Prepared event with topic and data.</p> required Source code in <code>client/ayon_core/lib/events.py</code> <pre><code>def emit_event(self, event):\n    \"\"\"Emit event object.\n\n    Args:\n       event (Event): Prepared event with topic and data.\n    \"\"\"\n\n    if not self._auto_execute or self._current_event is not None:\n        self._event_queue.append(event)\n        return\n\n    self._event_queue.append(event)\n    while self._event_queue:\n        event = self._event_queue.popleft()\n        self._current_event = event\n        self._process_event(event)\n    self._current_event = None\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/events.html#client.ayon_core.lib.events.QueuedEventSystem.process_next_event","title":"<code>process_next_event()</code>","text":"<p>Process next event in queue.</p> <p>Should be used only if 'auto_execute' is set to 'False'. Only single     event is processed.</p> <p>Returns:</p> Type Description <p>Union[Event, None]: Processed event.</p> Source code in <code>client/ayon_core/lib/events.py</code> <pre><code>def process_next_event(self):\n    \"\"\"Process next event in queue.\n\n    Should be used only if 'auto_execute' is set to 'False'. Only single\n        event is processed.\n\n    Returns:\n        Union[Event, None]: Processed event.\n    \"\"\"\n\n    if self._current_event is not None:\n        raise ValueError(\"An event is already in progress.\")\n\n    if not self._event_queue:\n        return None\n    event = self._event_queue.popleft()\n    self._current_event = event\n    self._process_event(event)\n    self._current_event = None\n    return event\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/events.html#client.ayon_core.lib.events.weakref_partial","title":"<code>weakref_partial</code>","text":"<p>Partial function with weak reference to the wrapped function.</p> <p>Can be used as 'functools.partial' but it will store weak reference to     function. That means that the function must be reference counted     to avoid garbage collecting the function itself.</p> <pre><code>When the referenced functions is garbage collected then calling the\nweakref partial (no matter the args/kwargs passed) will do nothing.\nIt will fail silently, returning `None`. The `is_valid()` method can\nbe used to detect whether the reference is still valid.\n</code></pre> <p>Is useful for object methods. In that case the callback is     deregistered when object is destroyed.</p> <p>Parameters:</p> Name Type Description Default <code>func</code> <code>Callable</code> <p>Function to wrap.</p> required <code>*args</code> <p>Arguments passed to the wrapped function.</p> <code>()</code> <code>**kwargs</code> <p>Keyword arguments passed to the wrapped function.</p> <code>{}</code> Source code in <code>client/ayon_core/lib/events.py</code> <pre><code>class weakref_partial:\n    \"\"\"Partial function with weak reference to the wrapped function.\n\n    Can be used as 'functools.partial' but it will store weak reference to\n        function. That means that the function must be reference counted\n        to avoid garbage collecting the function itself.\n\n        When the referenced functions is garbage collected then calling the\n        weakref partial (no matter the args/kwargs passed) will do nothing.\n        It will fail silently, returning `None`. The `is_valid()` method can\n        be used to detect whether the reference is still valid.\n\n    Is useful for object methods. In that case the callback is\n        deregistered when object is destroyed.\n\n    Warnings:\n        Values passed as *args and **kwargs are stored strongly in memory.\n            That may \"keep alive\" objects that should be already destroyed.\n            It is recommended to pass only immutable objects like 'str',\n            'bool', 'int' etc.\n\n    Args:\n        func (Callable): Function to wrap.\n        *args: Arguments passed to the wrapped function.\n        **kwargs: Keyword arguments passed to the wrapped function.\n    \"\"\"\n\n    def __init__(self, func, *args, **kwargs):\n        self._func_ref = _get_func_ref(func)\n        self._args = args\n        self._kwargs = kwargs\n\n    def __call__(self, *args, **kwargs):\n        func = self._func_ref()\n        if func is None:\n            return\n\n        new_args = tuple(list(self._args) + list(args))\n        new_kwargs = dict(self._kwargs)\n        new_kwargs.update(kwargs)\n        return func(*new_args, **new_kwargs)\n\n    def get_func(self):\n        \"\"\"Get wrapped function.\n\n        Returns:\n            Union[Callable, None]: Wrapped function or None if it was\n                destroyed.\n        \"\"\"\n\n        return self._func_ref()\n\n    def is_valid(self):\n        \"\"\"Check if wrapped function is still valid.\n\n        Returns:\n            bool: Is wrapped function still valid.\n        \"\"\"\n\n        return self._func_ref() is not None\n\n    def validate_signature(self, *args, **kwargs):\n        \"\"\"Validate if passed arguments are supported by wrapped function.\n\n        Returns:\n            bool: Are passed arguments supported by wrapped function.\n        \"\"\"\n\n        func = self._func_ref()\n        if func is None:\n            return False\n\n        new_args = tuple(list(self._args) + list(args))\n        new_kwargs = dict(self._kwargs)\n        new_kwargs.update(kwargs)\n        return is_func_signature_supported(\n            func, *new_args, **new_kwargs\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/events.html#client.ayon_core.lib.events.weakref_partial.get_func","title":"<code>get_func()</code>","text":"<p>Get wrapped function.</p> <p>Returns:</p> Type Description <p>Union[Callable, None]: Wrapped function or None if it was destroyed.</p> Source code in <code>client/ayon_core/lib/events.py</code> <pre><code>def get_func(self):\n    \"\"\"Get wrapped function.\n\n    Returns:\n        Union[Callable, None]: Wrapped function or None if it was\n            destroyed.\n    \"\"\"\n\n    return self._func_ref()\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/events.html#client.ayon_core.lib.events.weakref_partial.is_valid","title":"<code>is_valid()</code>","text":"<p>Check if wrapped function is still valid.</p> <p>Returns:</p> Name Type Description <code>bool</code> <p>Is wrapped function still valid.</p> Source code in <code>client/ayon_core/lib/events.py</code> <pre><code>def is_valid(self):\n    \"\"\"Check if wrapped function is still valid.\n\n    Returns:\n        bool: Is wrapped function still valid.\n    \"\"\"\n\n    return self._func_ref() is not None\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/events.html#client.ayon_core.lib.events.weakref_partial.validate_signature","title":"<code>validate_signature(*args, **kwargs)</code>","text":"<p>Validate if passed arguments are supported by wrapped function.</p> <p>Returns:</p> Name Type Description <code>bool</code> <p>Are passed arguments supported by wrapped function.</p> Source code in <code>client/ayon_core/lib/events.py</code> <pre><code>def validate_signature(self, *args, **kwargs):\n    \"\"\"Validate if passed arguments are supported by wrapped function.\n\n    Returns:\n        bool: Are passed arguments supported by wrapped function.\n    \"\"\"\n\n    func = self._func_ref()\n    if func is None:\n        return False\n\n    new_args = tuple(list(self._args) + list(args))\n    new_kwargs = dict(self._kwargs)\n    new_kwargs.update(kwargs)\n    return is_func_signature_supported(\n        func, *new_args, **new_kwargs\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/events.html#client.ayon_core.lib.events.emit_event","title":"<code>emit_event(topic, data=None, source=None)</code>","text":"<p>Emit event with topic and data.</p> Arg <p>topic(str): Event's topic. data(dict): Event's additional data. Optional. source(str): Who emitted the topic. Optional.</p> <p>Returns:</p> Name Type Description <code>Event</code> <p>Object of event that was emitted.</p> Source code in <code>client/ayon_core/lib/events.py</code> <pre><code>def emit_event(topic, data=None, source=None):\n    \"\"\"Emit event with topic and data.\n\n    Arg:\n        topic(str): Event's topic.\n        data(dict): Event's additional data. Optional.\n        source(str): Who emitted the topic. Optional.\n\n    Returns:\n        Event: Object of event that was emitted.\n    \"\"\"\n\n    return GlobalEventSystem.emit(topic, data, source)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/events.html#client.ayon_core.lib.events.register_event_callback","title":"<code>register_event_callback(topic, callback)</code>","text":"<p>Add callback that will be executed on specific topic.</p> <p>Parameters:</p> Name Type Description Default <code>topic</code> <code>str</code> <p>Topic on which will callback be triggered.</p> required <code>callback</code> <code>function</code> <p>Callback that will be triggered when a topic is triggered. Callback should expect none or 1 argument where <code>Event</code> object is passed.</p> required <p>Returns:</p> Name Type Description <code>EventCallback</code> <p>Object wrapping the callback. It can be used to enable/disable listening to a topic or remove the callback from the topic completely.</p> Source code in <code>client/ayon_core/lib/events.py</code> <pre><code>def register_event_callback(topic, callback):\n    \"\"\"Add callback that will be executed on specific topic.\n\n    Args:\n        topic(str): Topic on which will callback be triggered.\n        callback(function): Callback that will be triggered when a topic\n            is triggered. Callback should expect none or 1 argument where\n            `Event` object is passed.\n\n    Returns:\n        EventCallback: Object wrapping the callback. It can be used to\n            enable/disable listening to a topic or remove the callback from\n            the topic completely.\n    \"\"\"\n\n    return GlobalEventSystem.add_callback(topic, callback)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/execute.html","title":"execute","text":""},{"location":"autoapi/client/ayon_core/lib/execute.html#client.ayon_core.lib.execute.clean_envs_for_ayon_process","title":"<code>clean_envs_for_ayon_process(env=None)</code>","text":"<p>Modify environments that may affect ayon-launcher process.</p> <p>Main reason to implement this function is to pop PYTHONPATH which may be affected by in-host environments.</p> <p>Parameters:</p> Name Type Description Default <code>env</code> <code>Optional[dict[str, str]]</code> <p>Environment variables to modify.</p> <code>None</code> <p>Returns:</p> Type Description <p>dict[str, str]: Environment variables for ayon process.</p> Source code in <code>client/ayon_core/lib/execute.py</code> <pre><code>def clean_envs_for_ayon_process(env=None):\n    \"\"\"Modify environments that may affect ayon-launcher process.\n\n    Main reason to implement this function is to pop PYTHONPATH which may be\n    affected by in-host environments.\n\n    Args:\n        env (Optional[dict[str, str]]): Environment variables to modify.\n\n    Returns:\n        dict[str, str]: Environment variables for ayon process.\n\n    \"\"\"\n    if env is None:\n        env = os.environ\n\n    # Exclude some environment variables from a copy of the environment\n    env = env.copy()\n    for key in [\"PYTHONPATH\", \"PYTHONHOME\"]:\n        env.pop(key, None)\n\n    return env\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/execute.html#client.ayon_core.lib.execute.execute","title":"<code>execute(args, silent=False, cwd=None, env=None, shell=None)</code>","text":"<p>Execute command as process.</p> <p>This will execute given command as process, monitor its output and log it appropriately.</p> <p>.. seealso::</p> <pre><code>:mod:`subprocess` module in Python.\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>args</code> <code>list</code> <p>list of arguments passed to process.</p> required <code>silent</code> <code>bool</code> <p>control output of executed process.</p> <code>False</code> <code>cwd</code> <code>str</code> <p>current working directory for process.</p> <code>None</code> <code>env</code> <code>dict</code> <p>environment variables for process.</p> <code>None</code> <code>shell</code> <code>bool</code> <p>use shell to execute, default is no.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>int</code> <p>return code of process</p> Source code in <code>client/ayon_core/lib/execute.py</code> <pre><code>def execute(args, silent=False, cwd=None, env=None, shell=None):\n    \"\"\"Execute command as process.\n\n    This will execute given command as process, monitor its output\n    and log it appropriately.\n\n    .. seealso::\n\n        :mod:`subprocess` module in Python.\n\n    Args:\n        args (list): list of arguments passed to process.\n        silent (bool): control output of executed process.\n        cwd (str): current working directory for process.\n        env (dict): environment variables for process.\n        shell (bool): use shell to execute, default is no.\n\n    Returns:\n        int: return code of process\n\n    \"\"\"\n    log_levels = [\"DEBUG:\", \"INFO:\", \"ERROR:\", \"WARNING:\", \"CRITICAL:\"]\n\n    log = Logger.get_logger(\"execute\")\n    log.info(\"Executing ({})\".format(\" \".join(args)))\n    popen = subprocess.Popen(\n        args,\n        stdout=subprocess.PIPE,\n        stderr=subprocess.STDOUT,\n        universal_newlines=True,\n        bufsize=1,\n        cwd=cwd,\n        env=env or os.environ,\n        shell=shell\n    )\n\n    # Blocks until finished\n    while True:\n        line = popen.stdout.readline()\n        if line == \"\":\n            break\n        if silent:\n            continue\n        line_test = False\n        for test_string in log_levels:\n            if line.startswith(test_string):\n                line_test = True\n                break\n        if not line_test:\n            print(line[:-1])\n\n    log.info(\"Execution is finishing up ...\")\n\n    popen.wait()\n    return popen.returncode\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/execute.html#client.ayon_core.lib.execute.get_ayon_launcher_args","title":"<code>get_ayon_launcher_args(*args)</code>","text":"<p>Arguments to run AYON launcher process.</p> <p>Arguments for subprocess when need to spawn new AYON launcher process.</p> Reasons <p>AYON launcher started from code has different executable set to     virtual env python and must have path to script as first argument     which is not needed for built application.</p> <p>Parameters:</p> Name Type Description Default <code>*args</code> <code>str</code> <p>Any arguments that will be added after executables.</p> <code>()</code> <p>Returns:</p> Type Description <p>list[str]: List of arguments to run AYON launcher process.</p> Source code in <code>client/ayon_core/lib/execute.py</code> <pre><code>def get_ayon_launcher_args(*args):\n    \"\"\"Arguments to run AYON launcher process.\n\n    Arguments for subprocess when need to spawn new AYON launcher process.\n\n    Reasons:\n        AYON launcher started from code has different executable set to\n            virtual env python and must have path to script as first argument\n            which is not needed for built application.\n\n    Args:\n        *args (str): Any arguments that will be added after executables.\n\n    Returns:\n        list[str]: List of arguments to run AYON launcher process.\n\n    \"\"\"\n    executable = os.environ[\"AYON_EXECUTABLE\"]\n    launch_args = [executable]\n\n    executable_filename = os.path.basename(executable)\n    if \"python\" in executable_filename.lower():\n        filepath = os.path.join(os.environ[\"AYON_ROOT\"], \"start.py\")\n        launch_args.append(filepath)\n\n    if args:\n        launch_args.extend(args)\n\n    return launch_args\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/execute.html#client.ayon_core.lib.execute.get_linux_launcher_args","title":"<code>get_linux_launcher_args(*args)</code>","text":"<p>Path to application mid process executable.</p> <p>This function should be able as arguments are different when used from code and build.</p> <p>It is possible that this function is used in AYON build which does not have yet the new executable. In that case 'None' is returned.</p> Todos <p>Replace by script in scripts for ayon-launcher.</p> <p>Parameters:</p> Name Type Description Default <code>args</code> <code>iterable</code> <p>List of additional arguments added after executable argument.</p> <code>()</code> <p>Returns:</p> Name Type Description <code>list</code> <p>Executables with possible positional argument to script when called from code.</p> Source code in <code>client/ayon_core/lib/execute.py</code> <pre><code>def get_linux_launcher_args(*args):\n    \"\"\"Path to application mid process executable.\n\n    This function should be able as arguments are different when used\n    from code and build.\n\n    It is possible that this function is used in AYON build which does\n    not have yet the new executable. In that case 'None' is returned.\n\n    Todos:\n        Replace by script in scripts for ayon-launcher.\n\n    Args:\n        args (iterable): List of additional arguments added after executable\n            argument.\n\n    Returns:\n        list: Executables with possible positional argument to script when\n            called from code.\n    \"\"\"\n    filename = \"app_launcher\"\n    executable = os.environ[\"AYON_EXECUTABLE\"]\n\n    executable_filename = os.path.basename(executable)\n    if \"python\" in executable_filename.lower():\n        root = os.environ[\"AYON_ROOT\"]\n        script_path = os.path.join(root, \"{}.py\".format(filename))\n        launch_args = [executable, script_path]\n    else:\n        new_executable = os.path.join(\n            os.path.dirname(executable),\n            filename\n        )\n        executable_path = find_executable(new_executable)\n        if executable_path is None:\n            return None\n        launch_args = [executable_path]\n\n    if args:\n        launch_args.extend(args)\n\n    return launch_args\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/execute.html#client.ayon_core.lib.execute.path_to_subprocess_arg","title":"<code>path_to_subprocess_arg(path)</code>","text":"<p>Prepare path for subprocess arguments.</p> <p>Returned path can be wrapped with quotes or kept as is.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Path to be converted.</p> required <p>Returns:</p> Name Type Description <code>str</code> <p>Converted path.</p> Source code in <code>client/ayon_core/lib/execute.py</code> <pre><code>def path_to_subprocess_arg(path):\n    \"\"\"Prepare path for subprocess arguments.\n\n    Returned path can be wrapped with quotes or kept as is.\n\n    Args:\n        path (str): Path to be converted.\n\n    Returns:\n        str: Converted path.\n    \"\"\"\n    return subprocess.list2cmdline([path])\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/execute.html#client.ayon_core.lib.execute.run_ayon_launcher_process","title":"<code>run_ayon_launcher_process(*args, add_sys_paths=False, **kwargs)</code>","text":"<p>Execute AYON process with passed arguments and wait.</p> <p>Wrapper for 'run_process' which prepends AYON executable arguments before passed arguments and define environments if are not passed.</p> <p>Values from 'os.environ' are used for environments if are not passed. They are cleaned using 'clean_envs_for_ayon_process' function.</p> <p>Example:</p> <pre><code>run_ayon_launcher_process(\"run\", \"&lt;path to .py script&gt;\")\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>*args</code> <code>str</code> <p>ayon-launcher cli arguments.</p> <code>()</code> <code>add_sys_paths</code> <code>bool</code> <p>Add system paths to PYTHONPATH.</p> <code>False</code> <code>**kwargs</code> <code>Any</code> <p>Keyword arguments for subprocess.Popen.</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Full output of subprocess concatenated stdout and stderr.</p> Source code in <code>client/ayon_core/lib/execute.py</code> <pre><code>def run_ayon_launcher_process(\n    *args, add_sys_paths: bool = False, **kwargs\n) -&gt; str:\n    \"\"\"Execute AYON process with passed arguments and wait.\n\n    Wrapper for 'run_process' which prepends AYON executable arguments\n    before passed arguments and define environments if are not passed.\n\n    Values from 'os.environ' are used for environments if are not passed.\n    They are cleaned using 'clean_envs_for_ayon_process' function.\n\n    Example:\n    ```\n    run_ayon_launcher_process(\"run\", \"&lt;path to .py script&gt;\")\n    ```\n\n    Args:\n        *args (str): ayon-launcher cli arguments.\n        add_sys_paths (bool): Add system paths to PYTHONPATH.\n        **kwargs (Any): Keyword arguments for subprocess.Popen.\n\n    Returns:\n        str: Full output of subprocess concatenated stdout and stderr.\n\n    \"\"\"\n    args = get_ayon_launcher_args(*args)\n    env = _prepare_ayon_launcher_env(add_sys_paths, kwargs)\n    return run_subprocess(args, env=env, **kwargs)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/execute.html#client.ayon_core.lib.execute.run_detached_ayon_launcher_process","title":"<code>run_detached_ayon_launcher_process(*args, add_sys_paths=False, **kwargs)</code>","text":"<p>Execute AYON process with passed arguments and wait.</p> <p>Wrapper for 'run_process' which prepends AYON executable arguments before passed arguments and define environments if are not passed.</p> <p>Values from 'os.environ' are used for environments if are not passed. They are cleaned using 'clean_envs_for_ayon_process' function.</p> <p>Example:</p> <pre><code>run_detached_ayon_launcher_process(\"run\", \"&lt;path to .py script&gt;\")\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>*args</code> <code>str</code> <p>ayon-launcher cli arguments.</p> <code>()</code> <code>add_sys_paths</code> <code>bool</code> <p>Add system paths to PYTHONPATH.</p> <code>False</code> <code>**kwargs</code> <code>Any</code> <p>Keyword arguments for subprocess.Popen.</p> <code>{}</code> <p>Returns:</p> Type Description <code>Popen</code> <p>subprocess.Popen: Pointer to launched process but it is possible that launched process is already killed (on linux).</p> Source code in <code>client/ayon_core/lib/execute.py</code> <pre><code>def run_detached_ayon_launcher_process(\n    *args, add_sys_paths: bool = False, **kwargs\n) -&gt; subprocess.Popen:\n    \"\"\"Execute AYON process with passed arguments and wait.\n\n    Wrapper for 'run_process' which prepends AYON executable arguments\n    before passed arguments and define environments if are not passed.\n\n    Values from 'os.environ' are used for environments if are not passed.\n    They are cleaned using 'clean_envs_for_ayon_process' function.\n\n    Example:\n    ```\n    run_detached_ayon_launcher_process(\"run\", \"&lt;path to .py script&gt;\")\n    ```\n\n    Args:\n        *args (str): ayon-launcher cli arguments.\n        add_sys_paths (bool): Add system paths to PYTHONPATH.\n        **kwargs (Any): Keyword arguments for subprocess.Popen.\n\n    Returns:\n        subprocess.Popen: Pointer to launched process but it is possible that\n            launched process is already killed (on linux).\n\n    \"\"\"\n    args = get_ayon_launcher_args(*args)\n    env = _prepare_ayon_launcher_env(add_sys_paths, kwargs)\n    return run_detached_process(args, env=env, **kwargs)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/execute.html#client.ayon_core.lib.execute.run_detached_process","title":"<code>run_detached_process(args, **kwargs)</code>","text":"<p>Execute process with passed arguments as separated process.</p> Example <p>run_detached_process(\"run\", \"./path_to.py\")</p> <p>Parameters:</p> Name Type Description Default <code>args</code> <code>Iterable[str]</code> <p>AYON cli arguments.</p> required <code>**kwargs</code> <code>dict</code> <p>Keyword arguments for subprocess.Popen.</p> <code>{}</code> <p>Returns:</p> Type Description <p>subprocess.Popen: Pointer to launched process but it is possible that launched process is already killed (on linux).</p> Source code in <code>client/ayon_core/lib/execute.py</code> <pre><code>def run_detached_process(args, **kwargs):\n    \"\"\"Execute process with passed arguments as separated process.\n\n    Example:\n        &gt;&gt;&gt; run_detached_process(\"run\", \"./path_to.py\")\n\n\n    Args:\n        args (Iterable[str]): AYON cli arguments.\n        **kwargs (dict): Keyword arguments for subprocess.Popen.\n\n    Returns:\n        subprocess.Popen: Pointer to launched process but it is possible that\n            launched process is already killed (on linux).\n\n    \"\"\"\n    env = kwargs.pop(\"env\", None)\n    # Keep env untouched if are passed and not empty\n    if not env:\n        env = os.environ\n\n    # Create copy of passed env\n    kwargs[\"env\"] = {k: v for k, v in env.items()}\n\n    low_platform = platform.system().lower()\n    if low_platform == \"darwin\":\n        executable = args[0]\n        # If it's a macOS app bundle, use open -na\n        if executable.endswith(\".app\") or \"/Contents/MacOS/\" in executable:\n            new_args = [\"open\", \"-na\", args.pop(0), \"--args\"]\n            new_args.extend(args)\n            args = new_args\n        else:\n            kwargs[\"start_new_session\"] = True\n\n    elif low_platform == \"windows\":\n        flags = (\n            subprocess.CREATE_NEW_PROCESS_GROUP\n            | subprocess.DETACHED_PROCESS\n        )\n        kwargs[\"creationflags\"] = flags\n\n        if not sys.stdout:\n            kwargs[\"stdout\"] = subprocess.DEVNULL\n            kwargs[\"stderr\"] = subprocess.DEVNULL\n\n    elif low_platform == \"linux\" and get_linux_launcher_args() is not None:\n        json_data = {\n            \"args\": args,\n            \"env\": kwargs.pop(\"env\")\n        }\n        json_temp = tempfile.NamedTemporaryFile(\n            mode=\"w\", prefix=\"op_app_args\", suffix=\".json\", delete=False\n        )\n        json_temp.close()\n        json_temp_filpath = json_temp.name\n        with open(json_temp_filpath, \"w\") as stream:\n            json.dump(json_data, stream)\n\n        new_args = get_linux_launcher_args()\n        new_args.append(json_temp_filpath)\n\n        # Create mid-process which will launch application\n        process = subprocess.Popen(new_args, **kwargs)\n        # Wait until the process finishes\n        #   - This is important! The process would stay in \"open\" state.\n        process.wait()\n        # Remove the temp file\n        os.remove(json_temp_filpath)\n        # Return process which is already terminated\n        return process\n\n    process = subprocess.Popen(args, **kwargs)\n    return process\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/execute.html#client.ayon_core.lib.execute.run_subprocess","title":"<code>run_subprocess(*args, **kwargs)</code>","text":"<p>Convenience method for getting output errors for subprocess.</p> <p>Output logged when process finish.</p> <p>Entered arguments and keyword arguments are passed to subprocess Popen.</p> <p>On windows are 'creationflags' filled with flags that should cause ignore creation of new window.</p> <p>Parameters:</p> Name Type Description Default <code>*args</code> <p>Variable length argument list passed to Popen.</p> <code>()</code> <code>**kwargs </code> <p>Arbitrary keyword arguments passed to Popen. Is possible to pass <code>logging.Logger</code> object under \"logger\" to use custom logger for output.</p> required <p>Returns:</p> Name Type Description <code>str</code> <p>Full output of subprocess concatenated stdout and stderr.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>Exception is raised if process finished with nonzero return code.</p> Source code in <code>client/ayon_core/lib/execute.py</code> <pre><code>def run_subprocess(*args, **kwargs):\n    \"\"\"Convenience method for getting output errors for subprocess.\n\n    Output logged when process finish.\n\n    Entered arguments and keyword arguments are passed to subprocess Popen.\n\n    On windows are 'creationflags' filled with flags that should cause ignore\n    creation of new window.\n\n    Args:\n        *args: Variable length argument list passed to Popen.\n        **kwargs : Arbitrary keyword arguments passed to Popen. Is possible to\n            pass `logging.Logger` object under \"logger\" to use custom logger\n            for output.\n\n    Returns:\n        str: Full output of subprocess concatenated stdout and stderr.\n\n    Raises:\n        RuntimeError: Exception is raised if process finished with nonzero\n            return code.\n\n    \"\"\"\n    # Modify creation flags on windows to hide console window if in UI mode\n    if (\n        platform.system().lower() == \"windows\"\n        and \"creationflags\" not in kwargs\n        # shell=True already tries to hide the console window\n        # and passing these creationflags then shows the window again\n        # so we avoid it for shell=True cases\n        and kwargs.get(\"shell\") is not True\n    ):\n        kwargs[\"creationflags\"] = (\n            subprocess.CREATE_NEW_PROCESS_GROUP\n            | getattr(subprocess, \"DETACHED_PROCESS\", 0)\n            | getattr(subprocess, \"CREATE_NO_WINDOW\", 0)\n        )\n\n    # Escape special characters in certain shells\n    if (\n        kwargs.get(\"shell\") is True\n        and len(args) == 1\n        and isinstance(args[0], str)\n    ):\n        # Escape parentheses for bash\n        if os.getenv(\"SHELL\") in (\"/bin/bash\", \"/bin/sh\"):\n            new_arg = (\n                args[0]\n                .replace(\"(\", \"\\\\(\")\n                .replace(\")\", \"\\\\)\")\n            )\n            args = (new_arg,)\n        # Escape &amp; on Windows in shell with `cmd.exe` using ^&amp;\n        elif (\n            platform.system().lower() == \"windows\"\n            and os.getenv(\"COMSPEC\").endswith(\"cmd.exe\")\n        ):\n            new_arg = args[0].replace(\"&amp;\", \"^&amp;\")\n            args = (new_arg, )\n\n    # Get environments from kwarg or use current process environments if were\n    # not passed.\n    env = kwargs.get(\"env\") or os.environ\n    # Make sure environment contains only strings\n    filtered_env = {str(k): str(v) for k, v in env.items()}\n\n    # Use lib's logger if was not passed with kwargs.\n    logger = kwargs.pop(\"logger\", None)\n    if logger is None:\n        logger = Logger.get_logger(\"run_subprocess\")\n\n    # set overrides\n    kwargs[\"stdout\"] = kwargs.get(\"stdout\", subprocess.PIPE)\n    kwargs[\"stderr\"] = kwargs.get(\"stderr\", subprocess.PIPE)\n    kwargs[\"stdin\"] = kwargs.get(\"stdin\", subprocess.PIPE)\n    kwargs[\"env\"] = filtered_env\n\n    proc = subprocess.Popen(*args, **kwargs)\n\n    full_output = \"\"\n    _stdout, _stderr = proc.communicate()\n    if _stdout:\n        _stdout = _stdout.decode(\"utf-8\", errors=\"backslashreplace\")\n        full_output += _stdout\n        logger.debug(_stdout)\n\n    if _stderr:\n        _stderr = _stderr.decode(\"utf-8\", errors=\"backslashreplace\")\n        # Add additional line break if output already contains stdout\n        if full_output:\n            full_output += \"\\n\"\n        full_output += _stderr\n        logger.info(_stderr)\n\n    if proc.returncode != 0:\n        exc_msg = \"Executing arguments was not successful: \\\"{}\\\"\".format(args)\n        if _stdout:\n            exc_msg += \"\\n\\nOutput:\\n{}\".format(_stdout)\n\n        if _stderr:\n            exc_msg += \"Error:\\n{}\".format(_stderr)\n\n        raise RuntimeError(exc_msg)\n\n    return full_output\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/file_transaction.html","title":"file_transaction","text":""},{"location":"autoapi/client/ayon_core/lib/file_transaction.html#client.ayon_core.lib.file_transaction.DuplicateDestinationError","title":"<code>DuplicateDestinationError</code>","text":"<p>               Bases: <code>ValueError</code></p> <p>Error raised when transfer destination already exists in queue.</p> <p>The error is only raised if <code>allow_queue_replacements</code> is False on the FileTransaction instance and the added file to transfer is of a different src file than the one already detected in the queue.</p> Source code in <code>client/ayon_core/lib/file_transaction.py</code> <pre><code>class DuplicateDestinationError(ValueError):\n    \"\"\"Error raised when transfer destination already exists in queue.\n\n    The error is only raised if `allow_queue_replacements` is False on the\n    FileTransaction instance and the added file to transfer is of a different\n    src file than the one already detected in the queue.\n\n    \"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/file_transaction.html#client.ayon_core.lib.file_transaction.FileTransaction","title":"<code>FileTransaction</code>","text":"<p>File transaction with rollback options.</p> <p>The file transaction is a three-step process.</p> <p>1) Rename any existing files to a \"temporary backup\" during <code>process()</code> 2) Copy the files to final destination during <code>process()</code> 3) Remove any backed up files (*no rollback possible!) during <code>finalize()</code></p> <p>Step 3 is done during <code>finalize()</code>. If not called the .bak files will remain on disk.</p> <p>These steps try to ensure that we don't overwrite half of any existing files e.g. if they are currently in use.</p> Note <p>A regular filesystem is not a transactional file system and even though this implementation tries to produce a 'safe copy' with a potential rollback do keep in mind that it's inherently unsafe due to how filesystem works and a myriad of things could happen during the transaction that break the logic. A file storage could go down, permissions could be changed, other machines could be moving or writing files. A lot can happen.</p> Warning <p>Any folders created during the transfer will not be removed.</p> Source code in <code>client/ayon_core/lib/file_transaction.py</code> <pre><code>class FileTransaction:\n    \"\"\"File transaction with rollback options.\n\n    The file transaction is a three-step process.\n\n    1) Rename any existing files to a \"temporary backup\" during `process()`\n    2) Copy the files to final destination during `process()`\n    3) Remove any backed up files (*no rollback possible!) during `finalize()`\n\n    Step 3 is done during `finalize()`. If not called the .bak files will\n    remain on disk.\n\n    These steps try to ensure that we don't overwrite half of any existing\n    files e.g. if they are currently in use.\n\n    Note:\n        A regular filesystem is *not* a transactional file system and even\n        though this implementation tries to produce a 'safe copy' with a\n        potential rollback do keep in mind that it's inherently unsafe due\n        to how filesystem works and a myriad of things could happen during\n        the transaction that break the logic. A file storage could go down,\n        permissions could be changed, other machines could be moving or writing\n        files. A lot can happen.\n\n    Warning:\n        Any folders created during the transfer will not be removed.\n\n    \"\"\"\n    MODE_COPY = 0\n    MODE_HARDLINK = 1\n\n    def __init__(\n        self,\n        log: logging.Logger | None = None,\n        allow_queue_replacements: bool = False,\n    ) -&gt; None:\n        if log is None:\n            log = logging.getLogger(\"FileTransaction\")\n\n        self.log: logging.Logger = log\n\n        # The transfer queue\n        # todo: make this an actual FIFO queue?\n        self._transfers: dict[str, tuple[str, dict[str, Any]]] = {}\n\n        # Destination file paths that a file was transferred to\n        self._transferred: list[str] = []\n\n        # Backup file location mapping to original locations\n        self._backup_to_original: dict[str, str] = {}\n\n        self._allow_queue_replacements: bool = allow_queue_replacements\n\n    def add(self, src: str, dst: str, mode: int = MODE_COPY) -&gt; None:\n        \"\"\"Add a new file to transfer queue.\n\n        Args:\n            src (str): Source path.\n            dst (str): Destination path.\n            mode (MODE_COPY, MODE_HARDLINK): Transfer mode.\n\n        \"\"\"\n        opts = {\"mode\": mode}\n\n        src = os.path.normpath(os.path.abspath(src))\n        dst = os.path.normpath(os.path.abspath(dst))\n\n        if dst in self._transfers:\n            queued_src = self._transfers[dst][0]\n            if src == queued_src:\n                self.log.debug(\n                    f\"File transfer was already in queue: {src} -&gt; {dst}\"\n                )\n                return\n\n            if not self._allow_queue_replacements:\n                raise DuplicateDestinationError(\n                    \"Transfer to destination is already in queue: \"\n                    f\"{queued_src} -&gt; {dst}. It's not allowed to be\"\n                    f\" replaced by a new transfer from {src}\"\n                )\n\n            self.log.warning(\"File transfer in queue replaced..\")\n            self.log.debug(\n                f\"Removed from queue: {queued_src} -&gt; {dst}\"\n                f\" replaced by {src} -&gt; {dst}\"\n            )\n\n        self._transfers[dst] = (src, opts)\n\n    def process(self) -&gt; None:\n        with ThreadPoolExecutor(max_workers=8) as executor:\n            # Submit backup tasks\n            backup_futures = [\n                executor.submit(self._backup_file, dst, src)\n                for dst, (src, _) in self._transfers.items()\n            ]\n            wait_for_future_errors(\n                executor, backup_futures, logger=self.log\n            )\n\n            # Submit transfer tasks\n            transfer_futures = [\n                executor.submit(self._transfer_file, dst, src, opts)\n                for dst, (src, opts) in self._transfers.items()\n            ]\n            wait_for_future_errors(\n                executor, transfer_futures, logger=self.log\n            )\n\n    def _backup_file(self, dst: str, src: str) -&gt; None:\n        self.log.debug(f\"Checking file destination ... {src} -&gt; {dst}\")\n        path_same = self._same_paths(src, dst)\n        if path_same or not os.path.exists(dst):\n            return\n\n        # Backup original file\n        backup = dst + \".bak\"\n        self._backup_to_original[backup] = dst\n        self.log.debug(f\"Backup existing file: {dst} -&gt; {backup}\")\n        os.rename(dst, backup)\n\n    def _transfer_file(\n        self, dst: str, src: str, opts: dict[str, Any]\n    ) -&gt; None:\n        path_same = self._same_paths(src, dst)\n        if path_same:\n            self.log.debug(\n                f\"Source and destination are same files {src} -&gt; {dst}\")\n            return\n\n        self._create_folder_for_file(dst)\n\n        if opts[\"mode\"] == self.MODE_COPY:\n            self.log.debug(f\"Copying file ... {src} -&gt; {dst}\")\n            copyfile(src, dst)\n        elif opts[\"mode\"] == self.MODE_HARDLINK:\n            self.log.debug(f\"Hardlinking file ... {src} -&gt; {dst}\")\n            create_hard_link(src, dst)\n\n        self._transferred.append(dst)\n\n    def finalize(self) -&gt; None:\n        # Delete any backed up files\n        for backup in self._backup_to_original.keys():\n            try:\n                os.remove(backup)\n            except OSError:\n                self.log.error(\n                    f\"Failed to remove backup file: {backup}\",\n                    exc_info=True)\n\n    def rollback(self) -&gt; Exception | None:\n        errors = 0\n        last_exc = None\n        # Rollback any transferred files\n        for path in self._transferred:\n            try:\n                os.remove(path)\n            except OSError as exc:\n                last_exc = exc\n                errors += 1\n                self.log.error(\n                    f\"Failed to rollback created file: {path}\",\n                    exc_info=True)\n\n        # Rollback the backups\n        for backup, original in self._backup_to_original.items():\n            try:\n                os.rename(backup, original)\n            except OSError as exc:\n                last_exc = exc\n                errors += 1\n                self.log.error(\n                    f\"Failed to restore original file: {backup} -&gt; {original}\",\n                    exc_info=True)\n\n        if errors:\n            self.log.error(\n                f\"{errors} errors occurred during rollback.\",\n                exc_info=True)\n            raise last_exc\n\n    @property\n    def transferred(self) -&gt; list[str]:\n        \"\"\"Return the processed transfers destination paths\"\"\"\n        return list(self._transferred)\n\n    @property\n    def backups(self) -&gt; list[str]:\n        \"\"\"Return the backup file paths\"\"\"\n        return list(self._backup_to_original.keys())\n\n    def _create_folder_for_file(self, path: str) -&gt; None:\n        dirname = os.path.dirname(path)\n        try:\n            os.makedirs(dirname)\n        except OSError as e:\n            if e.errno != errno.EEXIST:\n                self.log.critical(\"An unexpected error occurred.\")\n                raise e\n\n    def _same_paths(self, src: str, dst: str) -&gt; bool:\n        # handles same paths but with C:/project vs c:/project\n        if os.path.exists(src) and os.path.exists(dst):\n            return os.stat(src) == os.stat(dst)\n\n        return src == dst\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/file_transaction.html#client.ayon_core.lib.file_transaction.FileTransaction.backups","title":"<code>backups</code>  <code>property</code>","text":"<p>Return the backup file paths</p>"},{"location":"autoapi/client/ayon_core/lib/file_transaction.html#client.ayon_core.lib.file_transaction.FileTransaction.transferred","title":"<code>transferred</code>  <code>property</code>","text":"<p>Return the processed transfers destination paths</p>"},{"location":"autoapi/client/ayon_core/lib/file_transaction.html#client.ayon_core.lib.file_transaction.FileTransaction.add","title":"<code>add(src, dst, mode=MODE_COPY)</code>","text":"<p>Add a new file to transfer queue.</p> <p>Parameters:</p> Name Type Description Default <code>src</code> <code>str</code> <p>Source path.</p> required <code>dst</code> <code>str</code> <p>Destination path.</p> required <code>mode</code> <code>(MODE_COPY, MODE_HARDLINK)</code> <p>Transfer mode.</p> <code>MODE_COPY</code> Source code in <code>client/ayon_core/lib/file_transaction.py</code> <pre><code>def add(self, src: str, dst: str, mode: int = MODE_COPY) -&gt; None:\n    \"\"\"Add a new file to transfer queue.\n\n    Args:\n        src (str): Source path.\n        dst (str): Destination path.\n        mode (MODE_COPY, MODE_HARDLINK): Transfer mode.\n\n    \"\"\"\n    opts = {\"mode\": mode}\n\n    src = os.path.normpath(os.path.abspath(src))\n    dst = os.path.normpath(os.path.abspath(dst))\n\n    if dst in self._transfers:\n        queued_src = self._transfers[dst][0]\n        if src == queued_src:\n            self.log.debug(\n                f\"File transfer was already in queue: {src} -&gt; {dst}\"\n            )\n            return\n\n        if not self._allow_queue_replacements:\n            raise DuplicateDestinationError(\n                \"Transfer to destination is already in queue: \"\n                f\"{queued_src} -&gt; {dst}. It's not allowed to be\"\n                f\" replaced by a new transfer from {src}\"\n            )\n\n        self.log.warning(\"File transfer in queue replaced..\")\n        self.log.debug(\n            f\"Removed from queue: {queued_src} -&gt; {dst}\"\n            f\" replaced by {src} -&gt; {dst}\"\n        )\n\n    self._transfers[dst] = (src, opts)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/file_transaction.html#client.ayon_core.lib.file_transaction.copyfile","title":"<code>copyfile(src, dst)</code>","text":"<p>Copy a file from src to dst.</p> <p>Defaults to using <code>speedcopy</code> but on certain infrastructure the fstats calls that it does may fail. For those cases, environment variable <code>AYON_COPY_FILE_DISABLE_SPEEDCOPY=1</code> could be set to avoid using speedcopy.</p> <p>Parameters:</p> Name Type Description Default <code>src</code> <code>str</code> <p>Source path.</p> required <code>dst</code> <code>str</code> <p>Destination path.</p> required Source code in <code>client/ayon_core/lib/file_transaction.py</code> <pre><code>def copyfile(src: str, dst: str) -&gt; None:\n    \"\"\"Copy a file from src to dst.\n\n    Defaults to using `speedcopy` but on certain infrastructure the fstats\n    calls that it does may fail. For those cases, environment variable\n    `AYON_COPY_FILE_DISABLE_SPEEDCOPY=1` could be set to avoid using speedcopy.\n\n    Args:\n        src (str): Source path.\n        dst (str): Destination path.\n\n    \"\"\"\n    # NOTE speedcopy has a bug that causes failure on macOS, fixed in 2.2.0\n    # TODO find out if speedcopy is still needed and remove if not.\n    if _IS_MACOS and speedcopy.version.version_info &lt;= (2, 2, 0):\n        shutil.copyfile(src, dst)\n    elif os.getenv(\"AYON_COPY_FILE_DISABLE_SPEEDCOPY\") != \"1\":\n        speedcopy.copyfile(src, dst)\n    else:\n        shutil.copyfile(src, dst)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/file_transaction.html#client.ayon_core.lib.file_transaction.wait_for_future_errors","title":"<code>wait_for_future_errors(executor, futures, logger=None)</code>","text":"<p>For the ThreadPoolExecutor shutdown and cancel futures as soon one of the workers raises an error as they complete.</p> <p>The ThreadPoolExecutor only cancels pending futures on exception but will still complete those that are running - each which also themselves could fail. We log all exceptions but re-raise the last exception only.</p> Source code in <code>client/ayon_core/lib/file_transaction.py</code> <pre><code>def wait_for_future_errors(\n    executor: ThreadPoolExecutor,\n    futures: list[Future],\n    logger: logging.Logger | None = None\n) -&gt; Exception | None:\n    \"\"\"For the ThreadPoolExecutor shutdown and cancel futures as soon one of\n    the workers raises an error as they complete.\n\n    The ThreadPoolExecutor only cancels pending futures on exception but will\n    still complete those that are running - each which also themselves could\n    fail. We log all exceptions but re-raise the last exception only.\n    \"\"\"\n    if logger is None:\n        logger = logging.getLogger(__name__)\n\n    for future in concurrent.futures.as_completed(futures):\n        exception = future.exception()\n        if exception:\n            # As soon as an error occurs, stop executing more futures.\n            # Running workers, however, will still be complete, so we also want\n            # to log those errors if any occurred on them.\n            executor.shutdown(wait=True, cancel_futures=True)\n            break\n    else:\n        # Futures are completed, no exceptions occurred\n        return None\n\n    # An exception occurred in at least one future. Get exceptions from\n    # all futures that are done and ended up failing until that point.\n    exceptions = []\n    for future in futures:\n        if not future.cancelled() and future.done():\n            exception = future.exception()\n            if exception:\n                exceptions.append(exception)\n\n    # Log any exceptions that occurred in all workers\n    for exception in exceptions:\n        logger.error(\"Error occurred in worker\", exc_info=exception)\n\n    # Raise the last exception\n    raise exceptions[-1]\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/icon_definitions.html","title":"icon_definitions","text":"<p>Icon definitions.</p> <p>There are multiple places where icons are defined for UI.</p>"},{"location":"autoapi/client/ayon_core/lib/icon_definitions.html#client.ayon_core.lib.icon_definitions.AYONUrlIcon","title":"<code>AYONUrlIcon</code>  <code>dataclass</code>","text":"<p>               Bases: <code>IconBase</code></p> <p>Relative url to an image file on AYON server.</p> <p>Instead of using full url use relative path to image file on AYON server. - 'https://studio.ayon.app/addons/1.0.0/public/icon.png' - 'addons/1.0.0/public/icon.png'</p> <p>The main difference from 'UrlIcon' is that this approach also has access     to endpoints that do require authentication.</p> Source code in <code>client/ayon_core/lib/icon_definitions.py</code> <pre><code>@dataclass\nclass AYONUrlIcon(IconBase):\n    \"\"\"Relative url to an image file on AYON server.\n\n    Instead of using full url use relative path to image file on AYON server.\n    - 'https://studio.ayon.app/addons/1.0.0/public/icon.png'\n    - 'addons/1.0.0/public/icon.png'\n\n    The main difference from 'UrlIcon' is that this approach also has access\n        to endpoints that do require authentication.\n\n    \"\"\"\n    type: ClassVar[str] = \"ayon_url\"\n    url: str\n\n    def __post_init__(self):\n        self.url = self.url.lstrip(\"/\")\n\n    def get_unique_id(self) -&gt; str:\n        return f\"{self.type}|{self.url}\"\n\n    def get_content(self) -&gt; bytes:\n        url = f\"{ayon_api.get_base_url()}/{self.url}\"\n        try:\n            stream = io.BytesIO()\n            ayon_api.download_file_to_stream(url, stream)\n            return stream.getvalue()\n        except Exception:\n            log.warning(\n                \"Failed to download image '%s'\", url, exc_info=True\n            )\n            return b\"\"\n\n    def to_data(self) -&gt; dict:\n        return {\n            \"type\": self.type,\n            \"url\": self.url,\n        }\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/icon_definitions.html#client.ayon_core.lib.icon_definitions.AwesomeFontIcon","title":"<code>AwesomeFontIcon</code>  <code>dataclass</code>","text":"<p>               Bases: <code>IconBase</code></p> <p>Awesome Font icon.</p> Source code in <code>client/ayon_core/lib/icon_definitions.py</code> <pre><code>@dataclass\nclass AwesomeFontIcon(IconBase):\n    \"\"\"Awesome Font icon.\"\"\"\n    type: ClassVar[str] = \"awesome-font\"\n    name: str\n    color: str = field(default=DEFAULT_WEB_ICON_COLOR)\n\n    def get_unique_id(self) -&gt; str:\n        return f\"{self.type}|{self.name}|{self.color}\"\n\n    def to_data(self) -&gt; dict:\n        return {\n            \"type\": self.type,\n            \"name\": self.name,\n            \"color\": self.color,\n        }\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/icon_definitions.html#client.ayon_core.lib.icon_definitions.IconBase","title":"<code>IconBase</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Base class for icon definition.</p> <p>The base class is meant to be used for type validation of icon definition     passed to functions.</p> Source code in <code>client/ayon_core/lib/icon_definitions.py</code> <pre><code>class IconBase(ABC):\n    \"\"\"Base class for icon definition.\n\n    The base class is meant to be used for type validation of icon definition\n        passed to functions.\n    \"\"\"\n\n    @property\n    @abstractmethod\n    def type(self) -&gt; str:\n        pass\n\n    @abstractmethod\n    def get_unique_id(self) -&gt; str:\n        pass\n\n    @abstractmethod\n    def to_data(self) -&gt; dict:\n        pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/icon_definitions.html#client.ayon_core.lib.icon_definitions.MaterialSymbolsIcon","title":"<code>MaterialSymbolsIcon</code>  <code>dataclass</code>","text":"<p>               Bases: <code>IconBase</code></p> <p>Material Symbols icon.</p> Source code in <code>client/ayon_core/lib/icon_definitions.py</code> <pre><code>@dataclass\nclass MaterialSymbolsIcon(IconBase):\n    \"\"\"Material Symbols icon.\"\"\"\n    type: ClassVar[str] = \"material-symbols\"\n    name: str\n    color: str = field(default=DEFAULT_WEB_ICON_COLOR)\n\n    def get_unique_id(self) -&gt; str:\n        return f\"{self.type}|{self.name}|{self.color}\"\n\n    def to_data(self) -&gt; dict:\n        return {\n            \"type\": self.type,\n            \"name\": self.name,\n            \"color\": self.color,\n        }\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/icon_definitions.html#client.ayon_core.lib.icon_definitions.PathIcon","title":"<code>PathIcon</code>  <code>dataclass</code>","text":"<p>               Bases: <code>IconBase</code></p> <p>Path to image file on disk.</p> Source code in <code>client/ayon_core/lib/icon_definitions.py</code> <pre><code>@dataclass\nclass PathIcon(IconBase):\n    \"\"\"Path to image file on disk.\"\"\"\n    type: ClassVar[str] = \"path\"\n    path: str\n\n    def get_unique_id(self) -&gt; str:\n        return f\"{self.type}|{self.path}\"\n\n    def to_data(self) -&gt; dict:\n        return {\n            \"type\": self.type,\n            \"path\": self.path,\n        }\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/icon_definitions.html#client.ayon_core.lib.icon_definitions.TransparentIcon","title":"<code>TransparentIcon</code>  <code>dataclass</code>","text":"<p>               Bases: <code>IconBase</code></p> <p>Transparent icon.</p> Source code in <code>client/ayon_core/lib/icon_definitions.py</code> <pre><code>@dataclass\nclass TransparentIcon(IconBase):\n    \"\"\"Transparent icon.\"\"\"\n    type: ClassVar[str] = \"transparent\"\n    size: int = 256\n\n    def get_unique_id(self):\n        return f\"{self.type}|{self.size}\"\n\n    def to_data(self) -&gt; dict:\n        return {\n            \"type\": self.type,\n            \"size\": self.size,\n        }\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/icon_definitions.html#client.ayon_core.lib.icon_definitions.UrlIcon","title":"<code>UrlIcon</code>  <code>dataclass</code>","text":"<p>               Bases: <code>IconBase</code></p> <p>Url to an image file.</p> Source code in <code>client/ayon_core/lib/icon_definitions.py</code> <pre><code>@dataclass\nclass UrlIcon(IconBase):\n    \"\"\"Url to an image file.\"\"\"\n    type: ClassVar[str] = \"url\"\n    url: str\n\n    def get_unique_id(self) -&gt; str:\n        return f\"{self.type}|{self.url}\"\n\n    def get_content(self) -&gt; bytes | None:\n        try:\n            return urllib.request.urlopen(self.url).read()\n        except Exception:\n            log.warning(\n                \"Failed to download image '%s'\", self.url, exc_info=True\n            )\n            return b\"\"\n\n    def to_data(self) -&gt; dict:\n        return {\n            \"type\": self.type,\n            \"url\": self.url,\n        }\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/local_settings.html","title":"local_settings","text":"<p>Package to deal with saving and retrieving user specific settings.</p>"},{"location":"autoapi/client/ayon_core/lib/local_settings.html#client.ayon_core.lib.local_settings.ASettingRegistry","title":"<code>ASettingRegistry</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Abstract class defining structure of SettingRegistry class.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name of registry used as the identifier for data.</p> required Source code in <code>client/ayon_core/lib/local_settings.py</code> <pre><code>class ASettingRegistry(ABC):\n    \"\"\"Abstract class defining structure of **SettingRegistry** class.\n\n    Args:\n        name(str): Name of registry used as the identifier for data.\n\n    \"\"\"\n    def __init__(self, name: str) -&gt; None:\n        self._name = name\n\n    @abstractmethod\n    def _get_item(\n        self, name: str, default: Any = _PLACEHOLDER\n    ) -&gt; Any:\n        \"\"\"Get item value from registry.\"\"\"\n\n    @abstractmethod\n    def _set_item(self, name: str, value: Any) -&gt; None:\n        \"\"\"Set item value to registry.\"\"\"\n\n    @abstractmethod\n    def _delete_item(self, name: str, ignore_missing: bool) -&gt; None:\n        \"\"\"Delete item from registry.\"\"\"\n\n    def __getitem__(self, name: str) -&gt; Any:\n        return self._get_item(name)\n\n    def __setitem__(self, name: str, value: Any) -&gt; None:\n        self._set_item(name, value)\n\n    def __delitem__(self, name: str) -&gt; None:\n        self._delete_item(name)\n\n    @property\n    def name(self) -&gt; str:\n        return self._name\n\n    def get_item(\n        self,\n        name: str,\n        default: Any = _PLACEHOLDER,\n    ) -&gt; Any:\n        \"\"\"Get item from settings registry.\n\n        Args:\n            name (str): Name of the item.\n            default (Any): Default value if item is not available.\n\n        Returns:\n            Any: Value of the item.\n\n        Raises:\n            RegistryItemNotFound: If item doesn't exist.\n\n        \"\"\"\n        return self._get_item(name, default)\n\n    def set_item(self, name: str, value: Any) -&gt; None:\n        \"\"\"Set item to settings registry.\n\n        Args:\n            name (str): Name of the item.\n            value (str): Value of the item.\n\n        \"\"\"\n        self._set_item(name, value)\n\n    def delete_item(self, name: str, ignore_missing: bool = True):\n        \"\"\"Delete item from settings registry.\n\n        Args:\n            name (str): Name of the item.\n            ignore_missing (bool): If True, no error is raised if item doesn't\n                exist.\n\n        \"\"\"\n        self._delete_item(name, ignore_missing=ignore_missing)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/local_settings.html#client.ayon_core.lib.local_settings.ASettingRegistry.delete_item","title":"<code>delete_item(name, ignore_missing=True)</code>","text":"<p>Delete item from settings registry.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name of the item.</p> required <code>ignore_missing</code> <code>bool</code> <p>If True, no error is raised if item doesn't exist.</p> <code>True</code> Source code in <code>client/ayon_core/lib/local_settings.py</code> <pre><code>def delete_item(self, name: str, ignore_missing: bool = True):\n    \"\"\"Delete item from settings registry.\n\n    Args:\n        name (str): Name of the item.\n        ignore_missing (bool): If True, no error is raised if item doesn't\n            exist.\n\n    \"\"\"\n    self._delete_item(name, ignore_missing=ignore_missing)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/local_settings.html#client.ayon_core.lib.local_settings.ASettingRegistry.get_item","title":"<code>get_item(name, default=_PLACEHOLDER)</code>","text":"<p>Get item from settings registry.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name of the item.</p> required <code>default</code> <code>Any</code> <p>Default value if item is not available.</p> <code>_PLACEHOLDER</code> <p>Returns:</p> Name Type Description <code>Any</code> <code>Any</code> <p>Value of the item.</p> <p>Raises:</p> Type Description <code>RegistryItemNotFound</code> <p>If item doesn't exist.</p> Source code in <code>client/ayon_core/lib/local_settings.py</code> <pre><code>def get_item(\n    self,\n    name: str,\n    default: Any = _PLACEHOLDER,\n) -&gt; Any:\n    \"\"\"Get item from settings registry.\n\n    Args:\n        name (str): Name of the item.\n        default (Any): Default value if item is not available.\n\n    Returns:\n        Any: Value of the item.\n\n    Raises:\n        RegistryItemNotFound: If item doesn't exist.\n\n    \"\"\"\n    return self._get_item(name, default)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/local_settings.html#client.ayon_core.lib.local_settings.ASettingRegistry.set_item","title":"<code>set_item(name, value)</code>","text":"<p>Set item to settings registry.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name of the item.</p> required <code>value</code> <code>str</code> <p>Value of the item.</p> required Source code in <code>client/ayon_core/lib/local_settings.py</code> <pre><code>def set_item(self, name: str, value: Any) -&gt; None:\n    \"\"\"Set item to settings registry.\n\n    Args:\n        name (str): Name of the item.\n        value (str): Value of the item.\n\n    \"\"\"\n    self._set_item(name, value)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/local_settings.html#client.ayon_core.lib.local_settings.AYONSecureRegistry","title":"<code>AYONSecureRegistry</code>","text":"<p>               Bases: <code>ASettingRegistry</code></p> <p>Store information using keyring.</p> <p>Registry should be used for private data that should be available only for user.</p> <p>All passed registry names will have added prefix <code>AYON/</code> to easier identify which data were created by AYON.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name of registry used as the identifier for data.</p> required Source code in <code>client/ayon_core/lib/local_settings.py</code> <pre><code>class AYONSecureRegistry(ASettingRegistry):\n    \"\"\"Store information using keyring.\n\n    Registry should be used for private data that should be available only for\n    user.\n\n    All passed registry names will have added prefix `AYON/` to easier\n    identify which data were created by AYON.\n\n    Args:\n        name(str): Name of registry used as the identifier for data.\n\n    \"\"\"\n    def __init__(self, name: str) -&gt; None:\n        try:\n            import keyring\n\n        except Exception:\n            raise NotImplementedError(\n                \"Python module `keyring` is not available.\"\n            )\n\n        # hack for cx_freeze and Windows keyring backend\n        if platform.system().lower() == \"windows\":\n            from keyring.backends import Windows\n\n            keyring.set_keyring(Windows.WinVaultKeyring())\n\n        # Force \"AYON\" prefix\n        super().__init__(f\"AYON/{name}\")\n\n    @lru_cache(maxsize=32)\n    def _get_item(\n        self, name: str, default: Any = _PLACEHOLDER\n    ) -&gt; Optional[str]:\n        \"\"\"Get value of sensitive item from the system's keyring.\n\n        See also `Keyring module`_\n\n        Args:\n            name (str): Name of the item.\n            default (Any): Default value if the item is not available.\n\n        Returns:\n            value (str): Value of the item.\n\n        Raises:\n            RegistryItemNotFound: If the item doesn't exist and default\n                is not defined.\n\n        .. _Keyring module:\n            https://github.com/jaraco/keyring\n\n        \"\"\"\n        import keyring\n\n        try:\n            from keyring.errors import PasswordSetError\n        except ImportError:\n            PasswordSetError = _FakeException\n\n        # Capture 'ItemNotFoundException' exception (on linux)\n        try:\n            from secretstorage.exceptions import ItemNotFoundException\n        except ImportError:\n            ItemNotFoundException = _FakeException\n\n        value = None\n        try:\n            value = keyring.get_password(self._name, name)\n        except (ItemNotFoundException, PasswordSetError):\n            pass\n\n        if value is not None:\n            return value\n\n        if default is not _PLACEHOLDER:\n            return default\n\n        raise RegistryItemNotFound(\n            f\"Item {self._name}:{name} not found in keyring.\"\n        )\n\n    def _set_item(self, name: str, value: str) -&gt; None:\n        \"\"\"Set sensitive item into system's keyring.\n\n        This uses `Keyring module`_ to save sensitive stuff into system's\n        keyring.\n\n        Args:\n            name (str): Name of the item.\n            value (str): Value of the item.\n\n        .. _Keyring module:\n            https://github.com/jaraco/keyring\n\n        \"\"\"\n        import keyring\n\n        keyring.set_password(self._name, name, value)\n        self._get_item.cache_clear()\n\n    def _delete_item(self, name: str, ignore_missing: bool = True):\n        \"\"\"Delete value stored in system's keyring.\n\n        See also `Keyring module`_\n\n        Args:\n            name (str): Name of the item to be deleted.\n            ignore_missing (bool): If True, no error is raised if item doesn't\n                exist.\n\n        .. _Keyring module:\n            https://github.com/jaraco/keyring\n\n        \"\"\"\n        import keyring\n\n        try:\n            from keyring.errors import PasswordDeleteError\n        except ImportError:\n            PasswordDeleteError = _FakeException\n\n        try:\n            keyring.delete_password(self._name, name)\n        except PasswordDeleteError:\n            if not ignore_missing:\n                raise RegistryItemNotFound(\n                    f\"Item {self._name}:{name} not found in keyring.\"\n                )\n        self._get_item.cache_clear()\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/local_settings.html#client.ayon_core.lib.local_settings.AYONSettingsRegistry","title":"<code>AYONSettingsRegistry</code>","text":"<p>               Bases: <code>JSONSettingRegistry</code></p> <p>Class handling AYON general settings registry.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>Optional[str]</code> <p>Name of the registry.</p> <code>None</code> Source code in <code>client/ayon_core/lib/local_settings.py</code> <pre><code>class AYONSettingsRegistry(JSONSettingRegistry):\n    \"\"\"Class handling AYON general settings registry.\n\n    Args:\n        name (Optional[str]): Name of the registry.\n    \"\"\"\n    def __init__(self, name: Optional[str] = None) -&gt; None:\n        if not name:\n            name = \"AYON_settings\"\n            warnings.warn(\n                (\n                    \"Used 'AYONSettingsRegistry' without 'name' argument.\"\n                    \" The argument will be required in future versions.\"\n                ),\n                DeprecationWarning,\n                stacklevel=2,\n            )\n        path = get_launcher_local_dir()\n        super().__init__(name, path)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/local_settings.html#client.ayon_core.lib.local_settings.IniSettingRegistry","title":"<code>IniSettingRegistry</code>","text":"<p>               Bases: <code>ASettingRegistry</code></p> <p>Class using :mod:<code>configparser</code>.</p> <p>This class is using :mod:<code>configparser</code> (ini) files to store items.</p> Source code in <code>client/ayon_core/lib/local_settings.py</code> <pre><code>class IniSettingRegistry(ASettingRegistry):\n    \"\"\"Class using :mod:`configparser`.\n\n    This class is using :mod:`configparser` (ini) files to store items.\n\n    \"\"\"\n\n    def __init__(self, name: str, path: str):\n        super().__init__(name)\n        # get registry file\n        filepath = os.path.join(path, f\"{name}.ini\")\n        os.makedirs(os.path.dirname(filepath), exist_ok=True)\n        if not os.path.exists(filepath):\n            with open(filepath, mode=\"w\") as stream:\n                print(\"# Settings registry\", stream)\n                now = datetime.now().strftime(\"%d/%m/%Y %H:%M:%S\")\n                print(f\"# {now}\", stream)\n\n        self._registry_file = filepath\n\n    @property\n    def filepath(self) -&gt; str:\n        return self._registry_file\n\n    def set_item_section(self, section: str, name: str, value: str) -&gt; None:\n        \"\"\"Set item to specific section of ini registry.\n\n        If section doesn't exists, it is created.\n\n        Args:\n            section (str): Name of section.\n            name (str): Name of the item.\n            value (str): Value of the item.\n\n        \"\"\"\n        value = str(value)\n        config = configparser.ConfigParser()\n\n        config.read(self._registry_file)\n        if not config.has_section(section):\n            config.add_section(section)\n        current = config[section]\n        current[name] = value\n\n        with open(self._registry_file, mode=\"w\") as stream:\n            config.write(stream)\n\n    def _set_item(self, name: str, value: str) -&gt; None:\n        self.set_item_section(\"MAIN\", name, value)\n\n    def set_item(self, name: str, value: str) -&gt; None:\n        \"\"\"Set item to settings ini file.\n\n        This saves item to ``DEFAULT`` section of ini as each item there\n        must reside in some section.\n\n        Args:\n            name (str): Name of the item.\n            value (str): Value of the item.\n\n        \"\"\"\n        # this does the some, overridden just for different docstring.\n        # we cast value to str as ini options values must be strings.\n        super().set_item(name, str(value))\n\n    def get_item(self, name: str, default: Any = _PLACEHOLDER) -&gt; str:\n        \"\"\"Gets item from settings ini file.\n\n        This gets settings from ``DEFAULT`` section of ini file as each item\n        there must reside in some section.\n\n        Args:\n            name (str): Name of the item.\n            default (Any): Default value if item is not available.\n\n        Returns:\n            str: Value of item.\n\n        Raises:\n            RegistryItemNotFound: If value doesn't exist.\n\n        \"\"\"\n        return super().get_item(name, default)\n\n    @lru_cache(maxsize=32)\n    def get_item_from_section(\n        self, section: str, name: str, default: Any = _PLACEHOLDER\n    ) -&gt; Optional[str]:\n        \"\"\"Get item from section of ini file.\n\n        This will read ini file and try to get item value from specified\n        section. If that section or item doesn't exist,\n        :exc:`RegistryItemNotFound` is risen.\n\n        Args:\n            section (str): Name of ini section.\n            name (str): Name of the item.\n\n        Returns:\n            str: Item value.\n\n        Raises:\n            RegistryItemNotFound: If value doesn't exist.\n\n        \"\"\"\n        config = configparser.ConfigParser()\n        config.read(self._registry_file)\n        try:\n            value = config[section][name]\n        except KeyError:\n            if default is not _PLACEHOLDER:\n                return default\n            raise RegistryItemNotFound(\n                f\"Registry doesn't contain value {section}:{name}\"\n            )\n        return value\n\n    def _get_item(\n        self, name: str, default: Any = _PLACEHOLDER\n    ) -&gt; Any:\n        return self.get_item_from_section(\"MAIN\", name, default)\n\n    def delete_item_from_section(\n        self,\n        section: str,\n        name: str,\n        ignore_missing: bool = True,\n    ):\n        \"\"\"Delete item from section in ini file.\n\n        Args:\n            section (str): Section name.\n            name (str): Name of the item.\n            ignore_missing (bool): If True, no error is raised if item doesn't\n                exist.\n\n        Raises:\n            RegistryItemNotFound: If the item doesn't exist.\n\n        \"\"\"\n        self.get_item_from_section.cache_clear()\n        config = configparser.ConfigParser()\n        config.read(self._registry_file)\n        try:\n            _ = config[section][name]\n        except KeyError:\n            if ignore_missing:\n                return\n            raise RegistryItemNotFound(\n                f\"Registry doesn't contain value {section}:{name}\"\n            )\n        config.remove_option(section, name)\n\n        # if section is empty, delete it\n        if len(config[section].keys()) == 0:\n            config.remove_section(section)\n\n        with open(self._registry_file, mode=\"w\") as stream:\n            config.write(stream)\n\n    def _delete_item(self, name: str, ignore_missing: bool):\n        \"\"\"Delete item from default section.\"\"\"\n        self.delete_item_from_section(\"MAIN\", name, ignore_missing)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/local_settings.html#client.ayon_core.lib.local_settings.IniSettingRegistry.delete_item_from_section","title":"<code>delete_item_from_section(section, name, ignore_missing=True)</code>","text":"<p>Delete item from section in ini file.</p> <p>Parameters:</p> Name Type Description Default <code>section</code> <code>str</code> <p>Section name.</p> required <code>name</code> <code>str</code> <p>Name of the item.</p> required <code>ignore_missing</code> <code>bool</code> <p>If True, no error is raised if item doesn't exist.</p> <code>True</code> <p>Raises:</p> Type Description <code>RegistryItemNotFound</code> <p>If the item doesn't exist.</p> Source code in <code>client/ayon_core/lib/local_settings.py</code> <pre><code>def delete_item_from_section(\n    self,\n    section: str,\n    name: str,\n    ignore_missing: bool = True,\n):\n    \"\"\"Delete item from section in ini file.\n\n    Args:\n        section (str): Section name.\n        name (str): Name of the item.\n        ignore_missing (bool): If True, no error is raised if item doesn't\n            exist.\n\n    Raises:\n        RegistryItemNotFound: If the item doesn't exist.\n\n    \"\"\"\n    self.get_item_from_section.cache_clear()\n    config = configparser.ConfigParser()\n    config.read(self._registry_file)\n    try:\n        _ = config[section][name]\n    except KeyError:\n        if ignore_missing:\n            return\n        raise RegistryItemNotFound(\n            f\"Registry doesn't contain value {section}:{name}\"\n        )\n    config.remove_option(section, name)\n\n    # if section is empty, delete it\n    if len(config[section].keys()) == 0:\n        config.remove_section(section)\n\n    with open(self._registry_file, mode=\"w\") as stream:\n        config.write(stream)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/local_settings.html#client.ayon_core.lib.local_settings.IniSettingRegistry.get_item","title":"<code>get_item(name, default=_PLACEHOLDER)</code>","text":"<p>Gets item from settings ini file.</p> <p>This gets settings from <code>DEFAULT</code> section of ini file as each item there must reside in some section.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name of the item.</p> required <code>default</code> <code>Any</code> <p>Default value if item is not available.</p> <code>_PLACEHOLDER</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Value of item.</p> <p>Raises:</p> Type Description <code>RegistryItemNotFound</code> <p>If value doesn't exist.</p> Source code in <code>client/ayon_core/lib/local_settings.py</code> <pre><code>def get_item(self, name: str, default: Any = _PLACEHOLDER) -&gt; str:\n    \"\"\"Gets item from settings ini file.\n\n    This gets settings from ``DEFAULT`` section of ini file as each item\n    there must reside in some section.\n\n    Args:\n        name (str): Name of the item.\n        default (Any): Default value if item is not available.\n\n    Returns:\n        str: Value of item.\n\n    Raises:\n        RegistryItemNotFound: If value doesn't exist.\n\n    \"\"\"\n    return super().get_item(name, default)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/local_settings.html#client.ayon_core.lib.local_settings.IniSettingRegistry.get_item_from_section","title":"<code>get_item_from_section(section, name, default=_PLACEHOLDER)</code>  <code>cached</code>","text":"<p>Get item from section of ini file.</p> <p>This will read ini file and try to get item value from specified section. If that section or item doesn't exist, :exc:<code>RegistryItemNotFound</code> is risen.</p> <p>Parameters:</p> Name Type Description Default <code>section</code> <code>str</code> <p>Name of ini section.</p> required <code>name</code> <code>str</code> <p>Name of the item.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>Optional[str]</code> <p>Item value.</p> <p>Raises:</p> Type Description <code>RegistryItemNotFound</code> <p>If value doesn't exist.</p> Source code in <code>client/ayon_core/lib/local_settings.py</code> <pre><code>@lru_cache(maxsize=32)\ndef get_item_from_section(\n    self, section: str, name: str, default: Any = _PLACEHOLDER\n) -&gt; Optional[str]:\n    \"\"\"Get item from section of ini file.\n\n    This will read ini file and try to get item value from specified\n    section. If that section or item doesn't exist,\n    :exc:`RegistryItemNotFound` is risen.\n\n    Args:\n        section (str): Name of ini section.\n        name (str): Name of the item.\n\n    Returns:\n        str: Item value.\n\n    Raises:\n        RegistryItemNotFound: If value doesn't exist.\n\n    \"\"\"\n    config = configparser.ConfigParser()\n    config.read(self._registry_file)\n    try:\n        value = config[section][name]\n    except KeyError:\n        if default is not _PLACEHOLDER:\n            return default\n        raise RegistryItemNotFound(\n            f\"Registry doesn't contain value {section}:{name}\"\n        )\n    return value\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/local_settings.html#client.ayon_core.lib.local_settings.IniSettingRegistry.set_item","title":"<code>set_item(name, value)</code>","text":"<p>Set item to settings ini file.</p> <p>This saves item to <code>DEFAULT</code> section of ini as each item there must reside in some section.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name of the item.</p> required <code>value</code> <code>str</code> <p>Value of the item.</p> required Source code in <code>client/ayon_core/lib/local_settings.py</code> <pre><code>def set_item(self, name: str, value: str) -&gt; None:\n    \"\"\"Set item to settings ini file.\n\n    This saves item to ``DEFAULT`` section of ini as each item there\n    must reside in some section.\n\n    Args:\n        name (str): Name of the item.\n        value (str): Value of the item.\n\n    \"\"\"\n    # this does the some, overridden just for different docstring.\n    # we cast value to str as ini options values must be strings.\n    super().set_item(name, str(value))\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/local_settings.html#client.ayon_core.lib.local_settings.IniSettingRegistry.set_item_section","title":"<code>set_item_section(section, name, value)</code>","text":"<p>Set item to specific section of ini registry.</p> <p>If section doesn't exists, it is created.</p> <p>Parameters:</p> Name Type Description Default <code>section</code> <code>str</code> <p>Name of section.</p> required <code>name</code> <code>str</code> <p>Name of the item.</p> required <code>value</code> <code>str</code> <p>Value of the item.</p> required Source code in <code>client/ayon_core/lib/local_settings.py</code> <pre><code>def set_item_section(self, section: str, name: str, value: str) -&gt; None:\n    \"\"\"Set item to specific section of ini registry.\n\n    If section doesn't exists, it is created.\n\n    Args:\n        section (str): Name of section.\n        name (str): Name of the item.\n        value (str): Value of the item.\n\n    \"\"\"\n    value = str(value)\n    config = configparser.ConfigParser()\n\n    config.read(self._registry_file)\n    if not config.has_section(section):\n        config.add_section(section)\n    current = config[section]\n    current[name] = value\n\n    with open(self._registry_file, mode=\"w\") as stream:\n        config.write(stream)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/local_settings.html#client.ayon_core.lib.local_settings.JSONSettingRegistry","title":"<code>JSONSettingRegistry</code>","text":"<p>               Bases: <code>ASettingRegistry</code></p> <p>Class using a json file as storage.</p> Source code in <code>client/ayon_core/lib/local_settings.py</code> <pre><code>class JSONSettingRegistry(ASettingRegistry):\n    \"\"\"Class using a json file as storage.\"\"\"\n\n    def __init__(self, name: str, path: str) -&gt; None:\n        super().__init__(name)\n        self._registry_file = os.path.join(path, f\"{name}.json\")\n        now = datetime.now().strftime(\"%d/%m/%Y %H:%M:%S\")\n        header = {\n            \"__metadata__\": {\"generated\": now},\n            \"registry\": {}\n        }\n\n        os.makedirs(os.path.dirname(self._registry_file), exist_ok=True)\n        if not os.path.exists(self._registry_file):\n            with open(self._registry_file, mode=\"w\") as stream:\n                json.dump(header, stream, indent=4)\n\n    @property\n    def filepath(self) -&gt; str:\n        return self._registry_file\n\n    @lru_cache(maxsize=32)\n    def _get_item(\n        self, name: str, default: Any = _PLACEHOLDER\n    ) -&gt; Any:\n        \"\"\"Get item value from registry json.\n\n        Note:\n            See :meth:`ayon_core.lib.JSONSettingRegistry.get_item`\n\n        \"\"\"\n        with open(self._registry_file, mode=\"r\") as cfg:\n            data = json.load(cfg)\n            try:\n                value = data[\"registry\"][name]\n            except KeyError:\n                if default is not _PLACEHOLDER:\n                    return default\n                raise RegistryItemNotFound(\n                    f\"Registry doesn't contain value {name}\"\n                )\n        return value\n\n    def _set_item(self, name: str, value: Any):\n        \"\"\"Set item value to registry json.\n\n        Note:\n            See :meth:`ayon_core.lib.JSONSettingRegistry.set_item`\n\n        \"\"\"\n        with open(self._registry_file, \"r+\") as stream:\n            data = json.load(stream)\n            registry_data = data.setdefault(\"registry\", {})\n            registry_data[name] = value\n            stream.truncate(0)\n            stream.seek(0)\n            json.dump(data, stream, indent=4)\n        self._get_item.cache_clear()\n\n    def _delete_item(self, name: str, ignore_missing: bool):\n        with open(self._registry_file, \"r+\") as stream:\n            data = json.load(stream)\n            registry_data = data.setdefault(\"registry\", {})\n            if name not in registry_data:\n                if ignore_missing:\n                    return\n                raise KeyError(f\"Registry doesn't contain value {name}\")\n            registry_data.pop(name)\n            stream.truncate(0)\n            stream.seek(0)\n            json.dump(data, stream, indent=4)\n        self._get_item.cache_clear()\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/local_settings.html#client.ayon_core.lib.local_settings.RegistryItemNotFound","title":"<code>RegistryItemNotFound</code>","text":"<p>               Bases: <code>ValueError</code></p> <p>Raised when the item is not found in the keyring.</p> Source code in <code>client/ayon_core/lib/local_settings.py</code> <pre><code>class RegistryItemNotFound(ValueError):\n    \"\"\"Raised when the item is not found in the keyring.\"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/local_settings.html#client.ayon_core.lib.local_settings.get_addons_resources_dir","title":"<code>get_addons_resources_dir(addon_name, *args)</code>","text":"<p>Get a directory for storing resources for addons.</p> <p>Some addons might need to store ad-hoc resources that are not part of     addon client package (e.g. because of size). Studio might define     dedicated directory to store them with 'AYON_ADDONS_RESOURCES_DIR'     environment variable. By default, is used 'addons_resources' in     launcher storage (might be shared across platforms).</p> <p>Parameters:</p> Name Type Description Default <code>addon_name</code> <code>str</code> <p>Addon name.</p> required <code>*args</code> <code>str</code> <p>Subfolders in the resources directory.</p> <code>()</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Path to resources directory.</p> Source code in <code>client/ayon_core/lib/local_settings.py</code> <pre><code>def get_addons_resources_dir(addon_name: str, *args) -&gt; str:\n    \"\"\"Get a directory for storing resources for addons.\n\n    Some addons might need to store ad-hoc resources that are not part of\n        addon client package (e.g. because of size). Studio might define\n        dedicated directory to store them with 'AYON_ADDONS_RESOURCES_DIR'\n        environment variable. By default, is used 'addons_resources' in\n        launcher storage (might be shared across platforms).\n\n    Args:\n        addon_name (str): Addon name.\n        *args (str): Subfolders in the resources directory.\n\n    Returns:\n        str: Path to resources directory.\n\n    \"\"\"\n    addons_resources_dir = os.getenv(\"AYON_ADDONS_RESOURCES_DIR\")\n    if not addons_resources_dir:\n        addons_resources_dir = get_launcher_storage_dir(\"addons_resources\")\n\n    return os.path.join(addons_resources_dir, addon_name, *args)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/local_settings.html#client.ayon_core.lib.local_settings.get_ayon_appdirs","title":"<code>get_ayon_appdirs(*args)</code>","text":"<p>Local app data directory of AYON client.</p> Deprecated <p>Use 'get_launcher_local_dir' or 'get_launcher_storage_dir' based on     a use-case. Deprecation added 24/08/09 (0.4.4-dev.1).</p> <p>Parameters:</p> Name Type Description Default <code>*args</code> <code>Iterable[str]</code> <p>Subdirectories/files in the local app data dir.</p> <code>()</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Path to directory/file in local app data dir.</p> Source code in <code>client/ayon_core/lib/local_settings.py</code> <pre><code>def get_ayon_appdirs(*args: str) -&gt; str:\n    \"\"\"Local app data directory of AYON client.\n\n    Deprecated:\n        Use 'get_launcher_local_dir' or 'get_launcher_storage_dir' based on\n            a use-case. Deprecation added 24/08/09 (0.4.4-dev.1).\n\n    Args:\n        *args (Iterable[str]): Subdirectories/files in the local app data dir.\n\n    Returns:\n        str: Path to directory/file in local app data dir.\n\n    \"\"\"\n    warnings.warn(\n        (\n            \"Function 'get_ayon_appdirs' is deprecated. Should be replaced\"\n            \" with 'get_launcher_local_dir' or 'get_launcher_storage_dir'\"\n            \" based on use-case.\"\n        ),\n        DeprecationWarning\n    )\n    return _get_ayon_appdirs(*args)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/local_settings.html#client.ayon_core.lib.local_settings.get_ayon_user_entity","title":"<code>get_ayon_user_entity(username=None)</code>","text":"<p>AYON user entity used for templates and publishing.</p> Note <p>Usually only service and admin users can receive the full user entity.</p> <p>Parameters:</p> Name Type Description Default <code>username</code> <code>Optional[str]</code> <p>Username of the user. If not passed, then the current user in 'ayon_api' is used.</p> <code>None</code> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: User entity.</p> Source code in <code>client/ayon_core/lib/local_settings.py</code> <pre><code>def get_ayon_user_entity(username: Optional[str] = None) -&gt; dict[str, Any]:\n    \"\"\"AYON user entity used for templates and publishing.\n\n    Note:\n        Usually only service and admin users can receive the full user entity.\n\n    Args:\n        username (Optional[str]): Username of the user. If not passed, then\n            the current user in 'ayon_api' is used.\n\n    Returns:\n        dict[str, Any]: User entity.\n\n    \"\"\"\n    service_username = _get_ayon_service_username()\n    # Handle service user handling first\n    if service_username:\n        if username is None:\n            username = service_username\n        cache: CacheItem = _Cache.user_entities_by_name[username]\n        if not cache.is_valid:\n            if username == service_username:\n                user = ayon_api.get_user()\n            else:\n                user = ayon_api.get_user(username)\n            cache.update_data(user)\n        return copy.deepcopy(cache.get_data())\n\n    # Cache current user\n    current_user = None\n    if _Cache.username is None:\n        current_user = ayon_api.get_user()\n        _Cache.username = current_user[\"name\"]\n\n    if username is None:\n        username = _Cache.username\n\n    cache: CacheItem = _Cache.user_entities_by_name[username]\n    if not cache.is_valid:\n        user = None\n        if username == _Cache.username:\n            if current_user is None:\n                current_user = ayon_api.get_user()\n            user = current_user\n\n        if user is None:\n            user = ayon_api.get_user(username)\n        cache.update_data(user)\n\n    return copy.deepcopy(cache.get_data())\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/local_settings.html#client.ayon_core.lib.local_settings.get_ayon_username","title":"<code>get_ayon_username()</code>","text":"<p>AYON username used for templates and publishing.</p> <p>Uses current ayon api username.</p> <p>Returns:</p> Name Type Description <code>str</code> <p>Username.</p> Source code in <code>client/ayon_core/lib/local_settings.py</code> <pre><code>def get_ayon_username():\n    \"\"\"AYON username used for templates and publishing.\n\n    Uses current ayon api username.\n\n    Returns:\n        str: Username.\n\n    \"\"\"\n    user = get_ayon_user_entity()\n    return user[\"name\"]\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/local_settings.html#client.ayon_core.lib.local_settings.get_launcher_local_dir","title":"<code>get_launcher_local_dir(*subdirs)</code>","text":"<p>Get a local directory for launcher.</p> <p>Local directory is used for storing machine or user-specific data.</p> <p>The location is user-specific.</p> Note <p>This function should be called at least once on the bootstrap.</p> <p>Parameters:</p> Name Type Description Default <code>*subdirs</code> <code>str</code> <p>Subdirectories relative to local dir.</p> <code>()</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Path to local directory.</p> Source code in <code>client/ayon_core/lib/local_settings.py</code> <pre><code>def get_launcher_local_dir(*subdirs: str) -&gt; str:\n    \"\"\"Get a local directory for launcher.\n\n    Local directory is used for storing machine or user-specific data.\n\n    The location is user-specific.\n\n    Note:\n        This function should be called at least once on the bootstrap.\n\n    Args:\n        *subdirs (str): Subdirectories relative to local dir.\n\n    Returns:\n        str: Path to local directory.\n\n    \"\"\"\n    storage_dir = os.getenv(\"AYON_LAUNCHER_LOCAL_DIR\")\n    if not storage_dir:\n        storage_dir = _get_ayon_appdirs()\n\n    return os.path.join(storage_dir, *subdirs)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/local_settings.html#client.ayon_core.lib.local_settings.get_launcher_storage_dir","title":"<code>get_launcher_storage_dir(*subdirs)</code>","text":"<p>Get a storage directory for launcher.</p> <p>Storage directory is used for storing shims, addons, dependencies, etc.</p> <p>It is not recommended, but the location can be shared across     multiple machines.</p> Note <p>This function should be called at least once on bootstrap.</p> <p>Parameters:</p> Name Type Description Default <code>*subdirs</code> <code>str</code> <p>Subdirectories relative to storage dir.</p> <code>()</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Path to storage directory.</p> Source code in <code>client/ayon_core/lib/local_settings.py</code> <pre><code>def get_launcher_storage_dir(*subdirs: str) -&gt; str:\n    \"\"\"Get a storage directory for launcher.\n\n    Storage directory is used for storing shims, addons, dependencies, etc.\n\n    It is not recommended, but the location can be shared across\n        multiple machines.\n\n    Note:\n        This function should be called at least once on bootstrap.\n\n    Args:\n        *subdirs (str): Subdirectories relative to storage dir.\n\n    Returns:\n        str: Path to storage directory.\n\n    \"\"\"\n    storage_dir = os.getenv(\"AYON_LAUNCHER_STORAGE_DIR\")\n    if not storage_dir:\n        storage_dir = _get_ayon_appdirs()\n\n    return os.path.join(storage_dir, *subdirs)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/local_settings.html#client.ayon_core.lib.local_settings.get_local_site_id","title":"<code>get_local_site_id()</code>","text":"<p>Get local site identifier.</p> <p>Identifier is created if does not exist yet.</p> Source code in <code>client/ayon_core/lib/local_settings.py</code> <pre><code>def get_local_site_id():\n    \"\"\"Get local site identifier.\n\n    Identifier is created if does not exist yet.\n    \"\"\"\n    # used for background syncing\n    site_id = os.environ.get(\"AYON_SITE_ID\")\n    if site_id:\n        return site_id\n\n    site_id_path = get_launcher_local_dir(\"site_id\")\n    if os.path.exists(site_id_path):\n        with open(site_id_path, \"r\") as stream:\n            site_id = stream.read()\n\n    if site_id:\n        return site_id\n\n    try:\n        from ayon_common.utils import get_local_site_id as _get_local_site_id\n        site_id = _get_local_site_id()\n    except ImportError:\n        raise ValueError(\"Couldn't access local site id\")\n\n    return site_id\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/log.html","title":"log","text":""},{"location":"autoapi/client/ayon_core/lib/log.html#client.ayon_core.lib.log.LogStreamHandler","title":"<code>LogStreamHandler</code>","text":"<p>               Bases: <code>StreamHandler</code></p> <p>StreamHandler class.</p> <p>This was originally designed to handle UTF errors in python 2.x hosts, however currently solely remains for backwards compatibility.</p> Source code in <code>client/ayon_core/lib/log.py</code> <pre><code>class LogStreamHandler(logging.StreamHandler):\n    \"\"\"StreamHandler class.\n\n    This was originally designed to handle UTF errors in python 2.x hosts,\n    however currently solely remains for backwards compatibility.\n\n    \"\"\"\n\n    def __init__(self, stream=None):\n        super(LogStreamHandler, self).__init__(stream)\n        self.enabled = True\n\n    def enable(self):\n        \"\"\"Enable StreamHandler\n\n        Make StreamHandler output again\n        \"\"\"\n        self.enabled = True\n\n    def disable(self):\n        \"\"\"Disable StreamHandler\n\n        Used to silence output\n        \"\"\"\n        self.enabled = False\n\n    def emit(self, record):\n        if not self.enabled or self.stream is None:\n            return\n        try:\n            msg = self.format(record)\n            msg = Terminal.log(msg)\n            stream = self.stream\n            stream.write(f\"{msg}\\n\")\n            self.flush()\n        except (KeyboardInterrupt, SystemExit):\n            raise\n\n        except OSError:\n            self.handleError(record)\n\n        except Exception:\n            print(repr(record))\n            self.handleError(record)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/log.html#client.ayon_core.lib.log.LogStreamHandler.disable","title":"<code>disable()</code>","text":"<p>Disable StreamHandler</p> <p>Used to silence output</p> Source code in <code>client/ayon_core/lib/log.py</code> <pre><code>def disable(self):\n    \"\"\"Disable StreamHandler\n\n    Used to silence output\n    \"\"\"\n    self.enabled = False\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/log.html#client.ayon_core.lib.log.LogStreamHandler.enable","title":"<code>enable()</code>","text":"<p>Enable StreamHandler</p> <p>Make StreamHandler output again</p> Source code in <code>client/ayon_core/lib/log.py</code> <pre><code>def enable(self):\n    \"\"\"Enable StreamHandler\n\n    Make StreamHandler output again\n    \"\"\"\n    self.enabled = True\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/log.html#client.ayon_core.lib.log.Logger","title":"<code>Logger</code>","text":"Source code in <code>client/ayon_core/lib/log.py</code> <pre><code>class Logger:\n    DFT = '%(levelname)s &gt;&gt;&gt; { %(name)s }: [ %(message)s ] '\n    DBG = \"  - { %(name)s }: [ %(message)s ] \"\n    INF = \"&gt;&gt;&gt; [ %(message)s ] \"\n    WRN = \"*** WRN: &gt;&gt;&gt; { %(name)s }: [ %(message)s ] \"\n    ERR = \"!!! ERR: %(asctime)s &gt;&gt;&gt; { %(name)s }: [ %(message)s ] \"\n    CRI = \"!!! CRI: %(asctime)s &gt;&gt;&gt; { %(name)s }: [ %(message)s ] \"\n\n    FORMAT_FILE = {\n        logging.INFO: INF,\n        logging.DEBUG: DBG,\n        logging.WARNING: WRN,\n        logging.ERROR: ERR,\n        logging.CRITICAL: CRI,\n    }\n\n    # Is static class initialized\n    initialized = False\n    _init_lock = threading.Lock()\n\n    # Logging level - AYON_LOG_LEVEL\n    log_level = None\n\n    # Data same for all record documents\n    process_data = None\n    # Cached process name or ability to set different process name\n    _process_name = None\n\n    @classmethod\n    def get_logger(cls, name=None):\n        if not cls.initialized:\n            cls.initialize()\n\n        logger = logging.getLogger(name or \"__main__\")\n\n        logger.setLevel(cls.log_level)\n\n        add_console_handler = True\n\n        for handler in logger.handlers:\n            if isinstance(handler, LogStreamHandler):\n                add_console_handler = False\n\n        if add_console_handler:\n            logger.addHandler(cls._get_console_handler())\n\n        # Do not propagate logs to root logger\n        logger.propagate = False\n\n        return logger\n\n    @classmethod\n    def _get_console_handler(cls):\n        formatter = LogFormatter(cls.FORMAT_FILE)\n        console_handler = LogStreamHandler()\n\n        console_handler.set_name(\"LogStreamHandler\")\n        console_handler.setFormatter(formatter)\n        return console_handler\n\n    @classmethod\n    def initialize(cls):\n        # TODO update already created loggers on re-initialization\n        if not cls._init_lock.locked():\n            with cls._init_lock:\n                cls._initialize()\n        else:\n            # If lock is locked wait until is finished\n            while cls._init_lock.locked():\n                time.sleep(0.1)\n\n    @classmethod\n    def _initialize(cls):\n        # Change initialization state to prevent runtime changes\n        # if is executed during runtime\n        cls.initialized = False\n\n        # Define what is logging level\n        log_level = os.getenv(\"AYON_LOG_LEVEL\")\n        if not log_level:\n            # Check AYON_DEBUG for debug level\n            op_debug = os.getenv(\"AYON_DEBUG\")\n            if op_debug and int(op_debug) &gt; 0:\n                log_level = 10\n            else:\n                log_level = 20\n        cls.log_level = int(log_level)\n\n        # Mark as initialized\n        cls.initialized = True\n\n    @classmethod\n    def get_process_data(cls):\n        \"\"\"Data about current process which should be same for all records.\n\n        Process data are used for each record sent to mongo database.\n        \"\"\"\n        if cls.process_data is not None:\n            return copy.deepcopy(cls.process_data)\n\n        if not cls.initialized:\n            cls.initialize()\n\n        host_name = socket.gethostname()\n        try:\n            host_ip = socket.gethostbyname(host_name)\n        except socket.gaierror:\n            host_ip = \"127.0.0.1\"\n\n        process_name = cls.get_process_name()\n\n        cls.process_data = {\n            \"hostname\": host_name,\n            \"hostip\": host_ip,\n            \"username\": getpass.getuser(),\n            \"system_name\": platform.system(),\n            \"process_name\": process_name\n        }\n        return copy.deepcopy(cls.process_data)\n\n    @classmethod\n    def set_process_name(cls, process_name):\n        \"\"\"Set process name for mongo logs.\"\"\"\n        # Just change the attribute\n        cls._process_name = process_name\n        # Update process data if are already set\n        if cls.process_data is not None:\n            cls.process_data[\"process_name\"] = process_name\n\n    @classmethod\n    def get_process_name(cls):\n        \"\"\"Process name that is like \"label\" of a process.\n\n        AYON logging can be used from OpenPyppe itself of from hosts.\n        Even in AYON process it's good to know if logs are from tray or\n        from other cli commands. This should help to identify that information.\n        \"\"\"\n        if cls._process_name is not None:\n            return cls._process_name\n\n        # Get process name\n        process_name = os.environ.get(\"AYON_APP_NAME\")\n        if not process_name:\n            try:\n                import psutil\n                process = psutil.Process(os.getpid())\n                process_name = process.name()\n\n            except ImportError:\n                pass\n\n        if not process_name:\n            process_name = os.path.basename(sys.executable)\n\n        cls._process_name = process_name\n        return cls._process_name\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/log.html#client.ayon_core.lib.log.Logger.get_process_data","title":"<code>get_process_data()</code>  <code>classmethod</code>","text":"<p>Data about current process which should be same for all records.</p> <p>Process data are used for each record sent to mongo database.</p> Source code in <code>client/ayon_core/lib/log.py</code> <pre><code>@classmethod\ndef get_process_data(cls):\n    \"\"\"Data about current process which should be same for all records.\n\n    Process data are used for each record sent to mongo database.\n    \"\"\"\n    if cls.process_data is not None:\n        return copy.deepcopy(cls.process_data)\n\n    if not cls.initialized:\n        cls.initialize()\n\n    host_name = socket.gethostname()\n    try:\n        host_ip = socket.gethostbyname(host_name)\n    except socket.gaierror:\n        host_ip = \"127.0.0.1\"\n\n    process_name = cls.get_process_name()\n\n    cls.process_data = {\n        \"hostname\": host_name,\n        \"hostip\": host_ip,\n        \"username\": getpass.getuser(),\n        \"system_name\": platform.system(),\n        \"process_name\": process_name\n    }\n    return copy.deepcopy(cls.process_data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/log.html#client.ayon_core.lib.log.Logger.get_process_name","title":"<code>get_process_name()</code>  <code>classmethod</code>","text":"<p>Process name that is like \"label\" of a process.</p> <p>AYON logging can be used from OpenPyppe itself of from hosts. Even in AYON process it's good to know if logs are from tray or from other cli commands. This should help to identify that information.</p> Source code in <code>client/ayon_core/lib/log.py</code> <pre><code>@classmethod\ndef get_process_name(cls):\n    \"\"\"Process name that is like \"label\" of a process.\n\n    AYON logging can be used from OpenPyppe itself of from hosts.\n    Even in AYON process it's good to know if logs are from tray or\n    from other cli commands. This should help to identify that information.\n    \"\"\"\n    if cls._process_name is not None:\n        return cls._process_name\n\n    # Get process name\n    process_name = os.environ.get(\"AYON_APP_NAME\")\n    if not process_name:\n        try:\n            import psutil\n            process = psutil.Process(os.getpid())\n            process_name = process.name()\n\n        except ImportError:\n            pass\n\n    if not process_name:\n        process_name = os.path.basename(sys.executable)\n\n    cls._process_name = process_name\n    return cls._process_name\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/log.html#client.ayon_core.lib.log.Logger.set_process_name","title":"<code>set_process_name(process_name)</code>  <code>classmethod</code>","text":"<p>Set process name for mongo logs.</p> Source code in <code>client/ayon_core/lib/log.py</code> <pre><code>@classmethod\ndef set_process_name(cls, process_name):\n    \"\"\"Set process name for mongo logs.\"\"\"\n    # Just change the attribute\n    cls._process_name = process_name\n    # Update process data if are already set\n    if cls.process_data is not None:\n        cls.process_data[\"process_name\"] = process_name\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/path_templates.html","title":"path_templates","text":""},{"location":"autoapi/client/ayon_core/lib/path_templates.html#client.ayon_core.lib.path_templates.DefaultKeysDict","title":"<code>DefaultKeysDict</code>","text":"<p>               Bases: <code>dict</code></p> <p>Dictionary that supports the default key to use for str conversion.</p> <p>Is helpful for changes of a key in a template from string to dictionary     for example '{folder}' -&gt; '{folder[name]}'.     &gt;&gt;&gt; data = DefaultKeysDict(     &gt;&gt;&gt;     \"name\",     &gt;&gt;&gt;     {\"folder\": {\"name\": \"FolderName\"}}     &gt;&gt;&gt; )     &gt;&gt;&gt; print(\"{folder[name]}\".format_map(data))     FolderName     &gt;&gt;&gt; print(\"{folder}\".format_map(data))     FolderName</p> <p>Parameters:</p> Name Type Description Default <code>default_key</code> <code>Union[str, Iterable[str]]</code> <p>Default key to use for str conversion. Can also expect multiple keys for more nested dictionary.</p> required Source code in <code>client/ayon_core/lib/path_templates.py</code> <pre><code>class DefaultKeysDict(dict):\n    \"\"\"Dictionary that supports the default key to use for str conversion.\n\n    Is helpful for changes of a key in a template from string to dictionary\n        for example '{folder}' -&gt; '{folder[name]}'.\n        &gt;&gt;&gt; data = DefaultKeysDict(\n        &gt;&gt;&gt;     \"name\",\n        &gt;&gt;&gt;     {\"folder\": {\"name\": \"FolderName\"}}\n        &gt;&gt;&gt; )\n        &gt;&gt;&gt; print(\"{folder[name]}\".format_map(data))\n        FolderName\n        &gt;&gt;&gt; print(\"{folder}\".format_map(data))\n        FolderName\n\n    Args:\n        default_key (Union[str, Iterable[str]]): Default key to use for str\n            conversion. Can also expect multiple keys for more nested\n            dictionary.\n\n    \"\"\"\n    def __init__(\n        self, default_keys: Union[str, Iterable[str]], *args, **kwargs\n    ) -&gt; None:\n        if isinstance(default_keys, str):\n            default_keys = [default_keys]\n        else:\n            default_keys = list(default_keys)\n        if not default_keys:\n            raise ValueError(\n                \"Default key must be set. Got empty default keys.\"\n            )\n\n        self._default_keys = default_keys\n        super().__init__(*args, **kwargs)\n\n    def __str__(self) -&gt; str:\n        return str(self.get_default_value())\n\n    def get_default_keys(self) -&gt; list[str]:\n        return list(self._default_keys)\n\n    def get_default_value(self) -&gt; Any:\n        value = self\n        for key in self._default_keys:\n            value = value[key]\n        return value\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/path_templates.html#client.ayon_core.lib.path_templates.FormatObject","title":"<code>FormatObject</code>","text":"<p>Object that can be used for formatting.</p> <p>This is base that is valid for to be used in 'StringTemplate' value.</p> Source code in <code>client/ayon_core/lib/path_templates.py</code> <pre><code>class FormatObject:\n    \"\"\"Object that can be used for formatting.\n\n    This is base that is valid for to be used in 'StringTemplate' value.\n    \"\"\"\n    def __init__(self):\n        self.value = \"\"\n\n    def __format__(self, *args, **kwargs):\n        return self.value.__format__(*args, **kwargs)\n\n    def __str__(self) -&gt; str:\n        return str(self.value)\n\n    def __repr__(self) -&gt; str:\n        return self.__str__()\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/path_templates.html#client.ayon_core.lib.path_templates.FormattingPart","title":"<code>FormattingPart</code>","text":"<p>String with formatting template.</p> <p>Containt only single key to format e.g. \"{project[name]}\".</p> <p>Parameters:</p> Name Type Description Default <code>field_name</code> <code>str</code> <p>Name of key.</p> required <code>format_spec</code> <code>str</code> <p>Format specification.</p> required <code>conversion</code> <code>Union[str, None]</code> <p>Conversion type.</p> required Source code in <code>client/ayon_core/lib/path_templates.py</code> <pre><code>class FormattingPart:\n    \"\"\"String with formatting template.\n\n    Containt only single key to format e.g. \"{project[name]}\".\n\n    Args:\n        field_name (str): Name of key.\n        format_spec (str): Format specification.\n        conversion (Union[str, None]): Conversion type.\n\n    \"\"\"\n    def __init__(\n        self,\n        field_name: str,\n        format_spec: str,\n        conversion: Union[str, None],\n    ):\n        format_spec_v = \"\"\n        if format_spec:\n            format_spec_v = f\":{format_spec}\"\n        conversion_v = \"\"\n        if conversion:\n            conversion_v = f\"!{conversion}\"\n\n        self._field_name: str = field_name\n        self._format_spec: str = format_spec_v\n        self._conversion: str = conversion_v\n\n        template_base = f\"{field_name}{format_spec_v}{conversion_v}\"\n        self._template_base: str = template_base\n        self._template: str = f\"{{{template_base}}}\"\n\n    @property\n    def template(self) -&gt; str:\n        return self._template\n\n    def __repr__(self) -&gt; str:\n        return \"&lt;Format:{}&gt;\".format(self._template)\n\n    def __str__(self) -&gt; str:\n        return self._template\n\n    @staticmethod\n    def validate_value_type(value: Any) -&gt; bool:\n        \"\"\"Check if value can be used for formatting of single key.\"\"\"\n        if isinstance(value, (numbers.Number, FormatObject)):\n            return True\n\n        for inh_class in type(value).mro():\n            if inh_class is str:\n                return True\n        return False\n\n    @staticmethod\n    def validate_key_is_matched(key: str) -&gt; bool:\n        \"\"\"Validate that opening has closing at correct place.\n        Future-proof, only square brackets are currently used in keys.\n\n        Example:\n            &gt;&gt;&gt; is_matched(\"[]()()(((([])))\")\n            False\n            &gt;&gt;&gt; is_matched(\"[](){{{[]}}}\")\n            True\n\n        Returns:\n            bool: Openings and closing are valid.\n\n        \"\"\"\n        mapping = dict(zip(\"({[\", \")}]\"))\n        opening = set(mapping.keys())\n        closing = set(mapping.values())\n        queue = []\n\n        for letter in key:\n            if letter in opening:\n                queue.append(mapping[letter])\n            elif letter in closing:\n                if not queue or letter != queue.pop():\n                    return False\n        return not queue\n\n    @staticmethod\n    def keys_to_template_base(keys: list[str]):\n        if not keys:\n            return None\n        # Create copy of keys\n        keys = list(keys)\n        template_base = keys.pop(0)\n        joined_keys = \"\".join([f\"[{key}]\" for key in keys])\n        return f\"{template_base}{joined_keys}\"\n\n    def keys(self) -&gt; tuple[str]:\n        \"\"\"Return keys of the template.\n\n        Returns:\n            tuple[str]: Keys of the template.\n\n        \"\"\"\n        return tuple(SUB_DICT_PATTERN.findall(self._field_name))\n\n    def format(\n        self, data: dict[str, Any], result: TemplatePartResult\n    ) -&gt; TemplatePartResult:\n        \"\"\"Format the formattings string.\n\n        Args:\n            data(dict): Data that should be used for formatting.\n            result(TemplatePartResult): Object where result is stored.\n\n        \"\"\"\n        key = self._template_base\n\n        # ensure key is properly formed [({})] properly closed.\n        if not self.validate_key_is_matched(key):\n            result.add_missing_key(key)\n            result.add_output(self.template)\n            return result\n\n        # check if key expects subdictionary keys (e.g. project[name])\n        key_subdict = self.keys()\n\n        value = data\n        missing_key = False\n        invalid_type = False\n        used_keys = []\n        keys_to_value = None\n        used_value = None\n\n        for sub_key in key_subdict:\n            if isinstance(value, list):\n                if not sub_key.lstrip(\"-\").isdigit():\n                    invalid_type = True\n                    break\n                sub_key = int(sub_key)\n                if sub_key &lt; 0:\n                    sub_key = len(value) + sub_key\n\n                valid = 0 &lt;= sub_key &lt; len(value)\n                if not valid:\n                    used_keys.append(sub_key)\n                    missing_key = True\n                    break\n\n                used_keys.append(sub_key)\n                if keys_to_value is None:\n                    keys_to_value = list(used_keys)\n                    keys_to_value.pop(-1)\n                    used_value = copy.deepcopy(value)\n                value = value[sub_key]\n                continue\n\n            if (\n                value is None\n                or (hasattr(value, \"items\") and sub_key not in value)\n            ):\n                missing_key = True\n                used_keys.append(sub_key)\n                break\n\n            if not hasattr(value, \"items\"):\n                invalid_type = True\n                break\n\n            used_keys.append(sub_key)\n            value = value.get(sub_key)\n\n        field_name = key_subdict[0]\n        if used_keys:\n            field_name = self.keys_to_template_base(used_keys)\n\n        if missing_key or invalid_type:\n            if missing_key:\n                result.add_missing_key(field_name)\n\n            elif invalid_type:\n                result.add_invalid_type(field_name, value)\n\n            result.add_output(self.template)\n            return result\n\n        if isinstance(value, DefaultKeysDict):\n            try:\n                value = value.get_default_value()\n            except KeyError:\n                pass\n\n        if not self.validate_value_type(value):\n            result.add_invalid_type(key, value)\n            result.add_output(self.template)\n            return result\n\n        fill_data = root_fill_data = {}\n        parent_fill_data = None\n        parent_key = None\n        fill_value = data\n        value_filled = False\n        for used_key in used_keys:\n            if isinstance(fill_value, list):\n                parent_fill_data[parent_key] = fill_value\n                value_filled = True\n                break\n            fill_value = fill_value[used_key]\n            parent_fill_data = fill_data\n            fill_data = parent_fill_data.setdefault(used_key, {})\n            parent_key = used_key\n\n        if not value_filled:\n            parent_fill_data[used_keys[-1]] = value\n\n        template = f\"{{{field_name}{self._format_spec}{self._conversion}}}\"\n        formatted_value = template.format(**root_fill_data)\n        used_key = key\n        if keys_to_value is not None:\n            used_key = self.keys_to_template_base(keys_to_value)\n\n        if used_value is None:\n            if isinstance(value, numbers.Number):\n                used_value = value\n            else:\n                used_value = formatted_value\n        result.add_really_used_value(self._field_name, used_value)\n        result.add_used_value(used_key, used_value)\n        result.add_output(formatted_value)\n        return result\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/path_templates.html#client.ayon_core.lib.path_templates.FormattingPart.format","title":"<code>format(data, result)</code>","text":"<p>Format the formattings string.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict</code> <p>Data that should be used for formatting.</p> required <code>result</code> <code>TemplatePartResult</code> <p>Object where result is stored.</p> required Source code in <code>client/ayon_core/lib/path_templates.py</code> <pre><code>def format(\n    self, data: dict[str, Any], result: TemplatePartResult\n) -&gt; TemplatePartResult:\n    \"\"\"Format the formattings string.\n\n    Args:\n        data(dict): Data that should be used for formatting.\n        result(TemplatePartResult): Object where result is stored.\n\n    \"\"\"\n    key = self._template_base\n\n    # ensure key is properly formed [({})] properly closed.\n    if not self.validate_key_is_matched(key):\n        result.add_missing_key(key)\n        result.add_output(self.template)\n        return result\n\n    # check if key expects subdictionary keys (e.g. project[name])\n    key_subdict = self.keys()\n\n    value = data\n    missing_key = False\n    invalid_type = False\n    used_keys = []\n    keys_to_value = None\n    used_value = None\n\n    for sub_key in key_subdict:\n        if isinstance(value, list):\n            if not sub_key.lstrip(\"-\").isdigit():\n                invalid_type = True\n                break\n            sub_key = int(sub_key)\n            if sub_key &lt; 0:\n                sub_key = len(value) + sub_key\n\n            valid = 0 &lt;= sub_key &lt; len(value)\n            if not valid:\n                used_keys.append(sub_key)\n                missing_key = True\n                break\n\n            used_keys.append(sub_key)\n            if keys_to_value is None:\n                keys_to_value = list(used_keys)\n                keys_to_value.pop(-1)\n                used_value = copy.deepcopy(value)\n            value = value[sub_key]\n            continue\n\n        if (\n            value is None\n            or (hasattr(value, \"items\") and sub_key not in value)\n        ):\n            missing_key = True\n            used_keys.append(sub_key)\n            break\n\n        if not hasattr(value, \"items\"):\n            invalid_type = True\n            break\n\n        used_keys.append(sub_key)\n        value = value.get(sub_key)\n\n    field_name = key_subdict[0]\n    if used_keys:\n        field_name = self.keys_to_template_base(used_keys)\n\n    if missing_key or invalid_type:\n        if missing_key:\n            result.add_missing_key(field_name)\n\n        elif invalid_type:\n            result.add_invalid_type(field_name, value)\n\n        result.add_output(self.template)\n        return result\n\n    if isinstance(value, DefaultKeysDict):\n        try:\n            value = value.get_default_value()\n        except KeyError:\n            pass\n\n    if not self.validate_value_type(value):\n        result.add_invalid_type(key, value)\n        result.add_output(self.template)\n        return result\n\n    fill_data = root_fill_data = {}\n    parent_fill_data = None\n    parent_key = None\n    fill_value = data\n    value_filled = False\n    for used_key in used_keys:\n        if isinstance(fill_value, list):\n            parent_fill_data[parent_key] = fill_value\n            value_filled = True\n            break\n        fill_value = fill_value[used_key]\n        parent_fill_data = fill_data\n        fill_data = parent_fill_data.setdefault(used_key, {})\n        parent_key = used_key\n\n    if not value_filled:\n        parent_fill_data[used_keys[-1]] = value\n\n    template = f\"{{{field_name}{self._format_spec}{self._conversion}}}\"\n    formatted_value = template.format(**root_fill_data)\n    used_key = key\n    if keys_to_value is not None:\n        used_key = self.keys_to_template_base(keys_to_value)\n\n    if used_value is None:\n        if isinstance(value, numbers.Number):\n            used_value = value\n        else:\n            used_value = formatted_value\n    result.add_really_used_value(self._field_name, used_value)\n    result.add_used_value(used_key, used_value)\n    result.add_output(formatted_value)\n    return result\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/path_templates.html#client.ayon_core.lib.path_templates.FormattingPart.keys","title":"<code>keys()</code>","text":"<p>Return keys of the template.</p> <p>Returns:</p> Type Description <code>tuple[str]</code> <p>tuple[str]: Keys of the template.</p> Source code in <code>client/ayon_core/lib/path_templates.py</code> <pre><code>def keys(self) -&gt; tuple[str]:\n    \"\"\"Return keys of the template.\n\n    Returns:\n        tuple[str]: Keys of the template.\n\n    \"\"\"\n    return tuple(SUB_DICT_PATTERN.findall(self._field_name))\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/path_templates.html#client.ayon_core.lib.path_templates.FormattingPart.validate_key_is_matched","title":"<code>validate_key_is_matched(key)</code>  <code>staticmethod</code>","text":"<p>Validate that opening has closing at correct place. Future-proof, only square brackets are currently used in keys.</p> Example <p>is_matched(\"()(((([])))\") False is_matched(\"{{{[]}}}\") True</p> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>Openings and closing are valid.</p> Source code in <code>client/ayon_core/lib/path_templates.py</code> <pre><code>@staticmethod\ndef validate_key_is_matched(key: str) -&gt; bool:\n    \"\"\"Validate that opening has closing at correct place.\n    Future-proof, only square brackets are currently used in keys.\n\n    Example:\n        &gt;&gt;&gt; is_matched(\"[]()()(((([])))\")\n        False\n        &gt;&gt;&gt; is_matched(\"[](){{{[]}}}\")\n        True\n\n    Returns:\n        bool: Openings and closing are valid.\n\n    \"\"\"\n    mapping = dict(zip(\"({[\", \")}]\"))\n    opening = set(mapping.keys())\n    closing = set(mapping.values())\n    queue = []\n\n    for letter in key:\n        if letter in opening:\n            queue.append(mapping[letter])\n        elif letter in closing:\n            if not queue or letter != queue.pop():\n                return False\n    return not queue\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/path_templates.html#client.ayon_core.lib.path_templates.FormattingPart.validate_value_type","title":"<code>validate_value_type(value)</code>  <code>staticmethod</code>","text":"<p>Check if value can be used for formatting of single key.</p> Source code in <code>client/ayon_core/lib/path_templates.py</code> <pre><code>@staticmethod\ndef validate_value_type(value: Any) -&gt; bool:\n    \"\"\"Check if value can be used for formatting of single key.\"\"\"\n    if isinstance(value, (numbers.Number, FormatObject)):\n        return True\n\n    for inh_class in type(value).mro():\n        if inh_class is str:\n            return True\n    return False\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/path_templates.html#client.ayon_core.lib.path_templates.OptionalPart","title":"<code>OptionalPart</code>","text":"<p>Template part which contains optional formatting strings.</p> <p>If this part can't be filled the result is empty string.</p> <p>Parameters:</p> Name Type Description Default <code>parts</code> <code>list</code> <p>Parts of template. Can contain 'str', 'OptionalPart' or 'FormattingPart'.</p> required Source code in <code>client/ayon_core/lib/path_templates.py</code> <pre><code>class OptionalPart:\n    \"\"\"Template part which contains optional formatting strings.\n\n    If this part can't be filled the result is empty string.\n\n    Args:\n        parts(list): Parts of template. Can contain 'str', 'OptionalPart' or\n            'FormattingPart'.\n    \"\"\"\n\n    def __init__(\n        self,\n        parts: list[Union[str, OptionalPart, FormattingPart]]\n    ):\n        self._parts: list[Union[str, OptionalPart, FormattingPart]] = parts\n\n    @property\n    def parts(self) -&gt; list[Union[str, OptionalPart, FormattingPart]]:\n        return self._parts\n\n    def remove_optional_parts_for_data(self, data: dict[str, Any]) -&gt; str:\n        if not data:\n            return \"\"\n\n        output = \"\"\n        for part in self._parts:\n            if isinstance(part, str):\n                output += part\n            elif isinstance(part, OptionalPart):\n                output += part.remove_optional_parts_for_data(data)\n\n            elif isinstance(part, FormattingPart):\n                data_v = data\n                for key in part.keys():\n                    if key not in data_v:\n                        return \"\"\n                    try:\n                        data_v = data_v[key]\n                    except (KeyError, IndexError, TypeError):\n                        return \"\"\n                output += part.template\n\n            else:\n                raise TypeError(\n                    f\"Got invalid type in template parts '{type(part)}'\"\n                )\n        return output\n\n    def __str__(self) -&gt; str:\n        joined_parts = \"\".join([str(p) for p in self._parts])\n        return f\"&lt;{joined_parts}&gt;\"\n\n    def __repr__(self) -&gt; str:\n        joined_parts = \"\".join([str(p) for p in self._parts])\n        return f\"&lt;Optional:{joined_parts}&gt;\"\n\n    def format(\n        self,\n        data: dict[str, Any],\n        result: TemplatePartResult,\n    ) -&gt; TemplatePartResult:\n        new_result = TemplatePartResult(True)\n        for part in self._parts:\n            if isinstance(part, str):\n                new_result.add_output(part)\n            else:\n                part.format(data, new_result)\n\n        if new_result.solved:\n            result.add_output(new_result)\n        return result\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/path_templates.html#client.ayon_core.lib.path_templates.StringTemplate","title":"<code>StringTemplate</code>","text":"<p>String that can be formatted.</p> Source code in <code>client/ayon_core/lib/path_templates.py</code> <pre><code>class StringTemplate:\n    \"\"\"String that can be formatted.\"\"\"\n    def __init__(self, template: str):\n        if not isinstance(template, str):\n            raise TypeError(\n                f\"&lt;{self.__class__.__name__}&gt; argument must be a string,\"\n                f\" not {str(type(template))}.\"\n            )\n\n        self._template: str = template\n        parts = []\n        formatter = Formatter()\n\n        for item in formatter.parse(template):\n            literal_text, field_name, format_spec, conversion = item\n            if literal_text:\n                parts.append(literal_text)\n            if field_name:\n                parts.append(\n                    FormattingPart(field_name, format_spec, conversion)\n                )\n\n        new_parts = []\n        for part in parts:\n            if not isinstance(part, str):\n                new_parts.append(part)\n                continue\n\n            substr = \"\"\n            for char in part:\n                if char not in (\"&lt;\", \"&gt;\"):\n                    substr += char\n                else:\n                    if substr:\n                        new_parts.append(substr)\n                    new_parts.append(char)\n                    substr = \"\"\n            if substr:\n                new_parts.append(substr)\n\n        self._parts: list[Union[str, OptionalPart, FormattingPart]] = (\n            self.find_optional_parts(new_parts)\n        )\n\n    def __str__(self) -&gt; str:\n        return self.template\n\n    def __repr__(self) -&gt; str:\n        return f\"&lt;{self.__class__.__name__}&gt; {self.template}\"\n\n    def __contains__(self, other: str) -&gt; bool:\n        return other in self.template\n\n    def replace(self, *args, **kwargs):\n        self._template = self.template.replace(*args, **kwargs)\n        return self\n\n    @property\n    def template(self) -&gt; str:\n        return self._template\n\n    def format(self, data: dict[str, Any]) -&gt; \"TemplateResult\":\n        \"\"\" Figure out with whole formatting.\n\n        Separate advanced keys (*Like '{project[name]}') from string which must\n        be formatted separately in case of missing or incomplete keys in data.\n\n        Args:\n            data (dict): Containing keys to be filled into template.\n\n        Returns:\n            TemplateResult: Filled or partially filled template containing all\n                data needed or missing for filling template.\n\n        \"\"\"\n        result = TemplatePartResult()\n        for part in self._parts:\n            if isinstance(part, str):\n                result.add_output(part)\n            else:\n                part.format(data, result)\n\n        invalid_types = result.invalid_types\n        invalid_types.update(result.invalid_optional_types)\n        invalid_types = result.split_keys_to_subdicts(invalid_types)\n\n        missing_keys = result.missing_keys\n        missing_keys |= result.missing_optional_keys\n\n        solved = result.solved\n        used_values = result.get_clean_used_values()\n\n        return TemplateResult(\n            result.output,\n            self.template,\n            solved,\n            used_values,\n            missing_keys,\n            invalid_types\n        )\n\n    def format_strict(self, data: dict[str, Any]) -&gt; \"TemplateResult\":\n        result = self.format(data)\n        result.validate()\n        return result\n\n    def remove_optional_parts_for_data(\n        self,\n        data: Optional[dict[str, Any]] = None,\n    ) -&gt; str:\n        \"\"\"Remove optional parts from template for data.\n\n        This method does not modify the template object itself.\n\n        Note:\n            At this moment the functionality is not 1:1 with 'format' where\n                lists are supported and the values are validated.\n\n        Args:\n            data (Optional[dict[str, Any]]): Template data for template.\n\n        Returns:\n            str: Template without optional parts that are not available\n                in passed data.\n\n        \"\"\"\n        if data is None:\n            data = {}\n\n        output = \"\"\n        for part in self._parts:\n            if isinstance(part, str):\n                output += part\n            elif isinstance(part, FormattingPart):\n                output += part.template\n            elif isinstance(part, OptionalPart):\n                output += part.remove_optional_parts_for_data(data)\n            else:\n                raise TypeError(\n                    f\"Got invalid type in template parts '{type(part)}'\"\n                )\n        return output\n\n    @classmethod\n    def format_template(\n        cls, template: str, data: dict[str, Any]\n    ) -&gt; \"TemplateResult\":\n        objected_template = cls(template)\n        return objected_template.format(data)\n\n    @classmethod\n    def format_strict_template(\n        cls, template: str, data: dict[str, Any]\n    ) -&gt; \"TemplateResult\":\n        objected_template = cls(template)\n        return objected_template.format_strict(data)\n\n    @staticmethod\n    def find_optional_parts(\n        parts: list[Union[str, FormattingPart]]\n    ) -&gt; list[Union[str, OptionalPart, FormattingPart]]:\n        new_parts = []\n        tmp_parts = {}\n        counted_symb = -1\n        for part in parts:\n            if part == \"&lt;\":\n                counted_symb += 1\n                tmp_parts[counted_symb] = []\n\n            elif part == \"&gt;\":\n                if counted_symb &gt; -1:\n                    parts = tmp_parts.pop(counted_symb)\n                    counted_symb -= 1\n                    # If part contains only single string keep value\n                    #   unchanged\n                    if parts:\n                        # Remove optional start char\n                        parts.pop(0)\n\n                    if not parts:\n                        value = \"&lt;&gt;\"\n                    elif (\n                        len(parts) == 1\n                        and isinstance(parts[0], str)\n                    ):\n                        value = f\"&lt;{parts[0]}&gt;\"\n                    else:\n                        value = OptionalPart(parts)\n\n                    if counted_symb &lt; 0:\n                        out_parts = new_parts\n                    else:\n                        out_parts = tmp_parts[counted_symb]\n                    # Store value\n                    out_parts.append(value)\n                    continue\n\n            if counted_symb &lt; 0:\n                new_parts.append(part)\n            else:\n                tmp_parts[counted_symb].append(part)\n\n        if tmp_parts:\n            for idx in sorted(tmp_parts.keys()):\n                new_parts.extend(tmp_parts[idx])\n        return new_parts\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/path_templates.html#client.ayon_core.lib.path_templates.StringTemplate.format","title":"<code>format(data)</code>","text":"<p>Figure out with whole formatting.</p> <p>Separate advanced keys (*Like '{project[name]}') from string which must be formatted separately in case of missing or incomplete keys in data.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict</code> <p>Containing keys to be filled into template.</p> required <p>Returns:</p> Name Type Description <code>TemplateResult</code> <code>'TemplateResult'</code> <p>Filled or partially filled template containing all data needed or missing for filling template.</p> Source code in <code>client/ayon_core/lib/path_templates.py</code> <pre><code>def format(self, data: dict[str, Any]) -&gt; \"TemplateResult\":\n    \"\"\" Figure out with whole formatting.\n\n    Separate advanced keys (*Like '{project[name]}') from string which must\n    be formatted separately in case of missing or incomplete keys in data.\n\n    Args:\n        data (dict): Containing keys to be filled into template.\n\n    Returns:\n        TemplateResult: Filled or partially filled template containing all\n            data needed or missing for filling template.\n\n    \"\"\"\n    result = TemplatePartResult()\n    for part in self._parts:\n        if isinstance(part, str):\n            result.add_output(part)\n        else:\n            part.format(data, result)\n\n    invalid_types = result.invalid_types\n    invalid_types.update(result.invalid_optional_types)\n    invalid_types = result.split_keys_to_subdicts(invalid_types)\n\n    missing_keys = result.missing_keys\n    missing_keys |= result.missing_optional_keys\n\n    solved = result.solved\n    used_values = result.get_clean_used_values()\n\n    return TemplateResult(\n        result.output,\n        self.template,\n        solved,\n        used_values,\n        missing_keys,\n        invalid_types\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/path_templates.html#client.ayon_core.lib.path_templates.StringTemplate.remove_optional_parts_for_data","title":"<code>remove_optional_parts_for_data(data=None)</code>","text":"<p>Remove optional parts from template for data.</p> <p>This method does not modify the template object itself.</p> Note <p>At this moment the functionality is not 1:1 with 'format' where     lists are supported and the values are validated.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>Optional[dict[str, Any]]</code> <p>Template data for template.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Template without optional parts that are not available in passed data.</p> Source code in <code>client/ayon_core/lib/path_templates.py</code> <pre><code>def remove_optional_parts_for_data(\n    self,\n    data: Optional[dict[str, Any]] = None,\n) -&gt; str:\n    \"\"\"Remove optional parts from template for data.\n\n    This method does not modify the template object itself.\n\n    Note:\n        At this moment the functionality is not 1:1 with 'format' where\n            lists are supported and the values are validated.\n\n    Args:\n        data (Optional[dict[str, Any]]): Template data for template.\n\n    Returns:\n        str: Template without optional parts that are not available\n            in passed data.\n\n    \"\"\"\n    if data is None:\n        data = {}\n\n    output = \"\"\n    for part in self._parts:\n        if isinstance(part, str):\n            output += part\n        elif isinstance(part, FormattingPart):\n            output += part.template\n        elif isinstance(part, OptionalPart):\n            output += part.remove_optional_parts_for_data(data)\n        else:\n            raise TypeError(\n                f\"Got invalid type in template parts '{type(part)}'\"\n            )\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/path_templates.html#client.ayon_core.lib.path_templates.TemplatePartResult","title":"<code>TemplatePartResult</code>","text":"<p>Result to store result of template parts.</p> Source code in <code>client/ayon_core/lib/path_templates.py</code> <pre><code>class TemplatePartResult:\n    \"\"\"Result to store result of template parts.\"\"\"\n    def __init__(self, optional: bool = False):\n        # Missing keys or invalid value types of required keys\n        self._missing_keys: set[str] = set()\n        self._invalid_types: dict[str, Any] = {}\n        # Missing keys or invalid value types of optional keys\n        self._missing_optional_keys: set[str] = set()\n        self._invalid_optional_types: dict[str, Any] = {}\n\n        # Used values stored by key with origin type\n        #   - key without any padding or key modifiers\n        #   - value from filling data\n        #   Example: {\"version\": 1}\n        self._used_values: dict[str, Any] = {}\n        # Used values stored by key with all modifirs\n        #   - value is already formatted string\n        #   Example: {\"version:0&gt;3\": \"001\"}\n        self._really_used_values: dict[str, Any] = {}\n        # Concatenated string output after formatting\n        self._output: str = \"\"\n        # Is this result from optional part\n        # TODO find out why we don't use 'optional' from args\n        self._optional: bool = True\n\n    def add_output(self, other):\n        if isinstance(other, str):\n            self._output += other\n\n        elif isinstance(other, TemplatePartResult):\n            self._output += other.output\n\n            self._missing_keys |= other.missing_keys\n            self._missing_optional_keys |= other.missing_optional_keys\n\n            self._invalid_types.update(other.invalid_types)\n            self._invalid_optional_types.update(other.invalid_optional_types)\n\n            if other.optional and not other.solved:\n                return\n            self._used_values.update(other.used_values)\n            self._really_used_values.update(other.really_used_values)\n\n        else:\n            raise TypeError(\n                f\"Cannot add data from \\\"{type(other)}\\\"\"\n                f\" to \\\"{self.__class__.__name__}\\\"\"\n            )\n\n    @property\n    def solved(self) -&gt; bool:\n        if self.optional:\n            if (\n                len(self.missing_optional_keys) &gt; 0\n                or len(self.invalid_optional_types) &gt; 0\n            ):\n                return False\n        return (\n            len(self.missing_keys) == 0\n            and len(self.invalid_types) == 0\n        )\n\n    @property\n    def optional(self) -&gt; bool:\n        return self._optional\n\n    @property\n    def output(self) -&gt; str:\n        return self._output\n\n    @property\n    def missing_keys(self) -&gt; set[str]:\n        return self._missing_keys\n\n    @property\n    def missing_optional_keys(self) -&gt; set[str]:\n        return self._missing_optional_keys\n\n    @property\n    def invalid_types(self) -&gt; dict[str, Any]:\n        return self._invalid_types\n\n    @property\n    def invalid_optional_types(self) -&gt; dict[str, Any]:\n        return self._invalid_optional_types\n\n    @property\n    def really_used_values(self) -&gt; dict[str, Any]:\n        return self._really_used_values\n\n    @property\n    def realy_used_values(self) -&gt; dict[str, Any]:\n        warnings.warn(\n            \"Property 'realy_used_values' is deprecated.\"\n            \" Use 'really_used_values' instead.\",\n            DeprecationWarning,\n            stacklevel=2,\n        )\n        return self._really_used_values\n\n    @property\n    def used_values(self) -&gt; dict[str, Any]:\n        return self._used_values\n\n    @staticmethod\n    def split_keys_to_subdicts(values: dict[str, Any]) -&gt; dict[str, Any]:\n        output = {}\n        formatter = Formatter()\n        for key, value in values.items():\n            _, field_name, _, _ = next(formatter.parse(f\"{{{key}}}\"))\n            key_subdict = list(SUB_DICT_PATTERN.findall(field_name))\n            data = output\n            last_key = key_subdict.pop(-1)\n            for subkey in key_subdict:\n                if subkey not in data:\n                    data[subkey] = {}\n                data = data[subkey]\n            data[last_key] = value\n        return output\n\n    def get_clean_used_values(self) -&gt; dict[str, Any]:\n        new_used_values = {}\n        for key, value in self.used_values.items():\n            if isinstance(value, FormatObject):\n                value = str(value)\n            new_used_values[key] = value\n\n        return self.split_keys_to_subdicts(new_used_values)\n\n    def add_really_used_value(self, key: str, value: Any):\n        self._really_used_values[key] = value\n\n    def add_realy_used_value(self, key: str, value: Any):\n        warnings.warn(\n            \"Method 'add_realy_used_value' is deprecated.\"\n            \" Use 'add_really_used_value' instead.\",\n            DeprecationWarning,\n            stacklevel=2,\n        )\n        self.add_really_used_value(key, value)\n\n    def add_used_value(self, key: str, value: Any):\n        self._used_values[key] = value\n\n    def add_missing_key(self, key: str):\n        if self._optional:\n            self._missing_optional_keys.add(key)\n        else:\n            self._missing_keys.add(key)\n\n    def add_invalid_type(self, key: str, value: Any):\n        if self._optional:\n            self._invalid_optional_types[key] = type(value)\n        else:\n            self._invalid_types[key] = type(value)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/path_templates.html#client.ayon_core.lib.path_templates.TemplateResult","title":"<code>TemplateResult</code>","text":"<p>               Bases: <code>str</code></p> <p>Result of template format with most of the information in.</p> <p>Parameters:</p> Name Type Description Default <code>used_values</code> <code>dict</code> <p>Dictionary of template filling data with only used keys.</p> required <code>solved</code> <code>bool</code> <p>For check if all required keys were filled.</p> required <code>template</code> <code>str</code> <p>Original template.</p> required <code>missing_keys</code> <code>list[str]</code> <p>Missing keys that were not in the data. Include missing optional keys.</p> required <code>invalid_types</code> <code>dict</code> <p>When key was found in data, but value had not allowed DataType. Allowed data types are <code>numbers</code>, <code>str</code>(<code>basestring</code>) and <code>dict</code>. Dictionary may cause invalid type when value of key in data is dictionary but template expect string of number.</p> required Source code in <code>client/ayon_core/lib/path_templates.py</code> <pre><code>class TemplateResult(str):\n    \"\"\"Result of template format with most of the information in.\n\n    Args:\n        used_values (dict): Dictionary of template filling data with\n            only used keys.\n        solved (bool): For check if all required keys were filled.\n        template (str): Original template.\n        missing_keys (list[str]): Missing keys that were not in the data.\n            Include missing optional keys.\n        invalid_types (dict): When key was found in data, but value had not\n            allowed DataType. Allowed data types are `numbers`,\n            `str`(`basestring`) and `dict`. Dictionary may cause invalid type\n            when value of key in data is dictionary but template expect string\n            of number.\n    \"\"\"\n\n    used_values: dict[str, Any] = None\n    solved: bool = None\n    template: str = None\n    missing_keys: list[str] = None\n    invalid_types: dict[str, Any] = None\n\n    def __new__(\n        cls, filled_template, template, solved,\n        used_values, missing_keys, invalid_types\n    ):\n        new_obj = super(TemplateResult, cls).__new__(cls, filled_template)\n        new_obj.used_values = used_values\n        new_obj.solved = solved\n        new_obj.template = template\n        new_obj.missing_keys = list(set(missing_keys))\n        new_obj.invalid_types = invalid_types\n        return new_obj\n\n    def __copy__(self, *args, **kwargs):\n        return self.copy()\n\n    def __deepcopy__(self, *args, **kwargs):\n        return self.copy()\n\n    def validate(self):\n        if not self.solved:\n            raise TemplateUnsolved(\n                self.template,\n                self.missing_keys,\n                self.invalid_types\n            )\n\n    def copy(self) -&gt; \"TemplateResult\":\n        cls = self.__class__\n        return cls(\n            str(self),\n            self.template,\n            self.solved,\n            self.used_values,\n            self.missing_keys,\n            self.invalid_types\n        )\n\n    def normalized(self) -&gt; \"TemplateResult\":\n        \"\"\"Convert to normalized path.\"\"\"\n\n        cls = self.__class__\n        path = str(self)\n        if _IS_WINDOWS:\n            path = path.replace(\"\\\\\", \"/\")\n        return cls(\n            os.path.normpath(path),\n            self.template,\n            self.solved,\n            self.used_values,\n            self.missing_keys,\n            self.invalid_types\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/path_templates.html#client.ayon_core.lib.path_templates.TemplateResult.normalized","title":"<code>normalized()</code>","text":"<p>Convert to normalized path.</p> Source code in <code>client/ayon_core/lib/path_templates.py</code> <pre><code>def normalized(self) -&gt; \"TemplateResult\":\n    \"\"\"Convert to normalized path.\"\"\"\n\n    cls = self.__class__\n    path = str(self)\n    if _IS_WINDOWS:\n        path = path.replace(\"\\\\\", \"/\")\n    return cls(\n        os.path.normpath(path),\n        self.template,\n        self.solved,\n        self.used_values,\n        self.missing_keys,\n        self.invalid_types\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/path_templates.html#client.ayon_core.lib.path_templates.TemplateUnsolved","title":"<code>TemplateUnsolved</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Exception for unsolved template when strict is set to True.</p> Source code in <code>client/ayon_core/lib/path_templates.py</code> <pre><code>class TemplateUnsolved(Exception):\n    \"\"\"Exception for unsolved template when strict is set to True.\"\"\"\n\n    msg = \"Template \\\"{0}\\\" is unsolved.{1}{2}\"\n    invalid_types_msg = \" Keys with invalid data type: `{0}`.\"\n    missing_keys_msg = \" Missing keys: \\\"{0}\\\".\"\n\n    def __init__(self, template, missing_keys, invalid_types):\n        invalid_type_items = []\n        for _key, _type in invalid_types.items():\n            invalid_type_items.append(f\"\\\"{_key}\\\" {str(_type)}\")\n\n        invalid_types_msg = \"\"\n        if invalid_type_items:\n            invalid_types_msg = self.invalid_types_msg.format(\n                \", \".join(invalid_type_items)\n            )\n\n        missing_keys_msg = \"\"\n        if missing_keys:\n            missing_keys_msg = self.missing_keys_msg.format(\n                \", \".join(missing_keys)\n            )\n        super().__init__(\n            self.msg.format(template, missing_keys_msg, invalid_types_msg)\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/path_tools.html","title":"path_tools","text":""},{"location":"autoapi/client/ayon_core/lib/path_tools.html#client.ayon_core.lib.path_tools.collect_frames","title":"<code>collect_frames(files)</code>","text":"<p>Returns dict of source path and its frame, if from sequence</p> <p>Uses clique as most precise solution, used when anatomy template that created files is not known.</p> <p>Assumption is that frames are separated by '.', negative frames are not allowed.</p> <p>Parameters:</p> Name Type Description Default <code>files</code> <code>list) or (set with single value</code> <p>list of source paths</p> required <p>Returns:</p> Name Type Description <code>dict</code> <p>{'/folder/product_v001.0001.png': '0001', ....}</p> Source code in <code>client/ayon_core/lib/path_tools.py</code> <pre><code>def collect_frames(files):\n    \"\"\"Returns dict of source path and its frame, if from sequence\n\n    Uses clique as most precise solution, used when anatomy template that\n    created files is not known.\n\n    Assumption is that frames are separated by '.', negative frames are not\n    allowed.\n\n    Args:\n        files(list) or (set with single value): list of source paths\n\n    Returns:\n        dict: {'/folder/product_v001.0001.png': '0001', ....}\n    \"\"\"\n\n    # clique.PATTERNS[\"frames\"] supports only `.1001.exr` not `_1001.exr` so\n    # we use a customized pattern.\n    pattern = \"[_.](?P&lt;index&gt;(?P&lt;padding&gt;0*)\\\\d+)\\\\.\\\\D+\\\\d?$\"\n    patterns = [pattern]\n    collections, remainder = clique.assemble(\n        files, minimum_items=1, patterns=patterns)\n\n    sources_and_frames = {}\n    if collections:\n        for collection in collections:\n            src_head = collection.head\n            src_tail = collection.tail\n\n            for index in collection.indexes:\n                src_frame = collection.format(\"{padding}\") % index\n                src_file_name = \"{}{}{}\".format(\n                    src_head, src_frame, src_tail)\n                sources_and_frames[src_file_name] = src_frame\n    else:\n        sources_and_frames[remainder.pop()] = None\n\n    return sources_and_frames\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/path_tools.html#client.ayon_core.lib.path_tools.create_hard_link","title":"<code>create_hard_link(src_path, dst_path)</code>","text":"<p>Create hardlink of file.</p> <p>Parameters:</p> Name Type Description Default <code>src_path</code> <code>str</code> <p>Full path to a file which is used as source for hardlink.</p> required <code>dst_path</code> <code>str</code> <p>Full path to a file where a link of source will be added.</p> required Source code in <code>client/ayon_core/lib/path_tools.py</code> <pre><code>def create_hard_link(src_path, dst_path):\n    \"\"\"Create hardlink of file.\n\n    Args:\n        src_path(str): Full path to a file which is used as source for\n            hardlink.\n        dst_path(str): Full path to a file where a link of source will be\n            added.\n    \"\"\"\n    os.link(src_path, dst_path)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/path_tools.html#client.ayon_core.lib.path_tools.format_file_size","title":"<code>format_file_size(file_size, suffix=None)</code>","text":"<p>Returns formatted string with size in appropriate unit.</p> <p>Parameters:</p> Name Type Description Default <code>file_size</code> <code>int</code> <p>Size of file in bytes.</p> required <code>suffix</code> <code>str</code> <p>Suffix for formatted size. Default is 'B' (as bytes).</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <p>Formatted size using proper unit and passed suffix (e.g. 7 MiB).</p> Source code in <code>client/ayon_core/lib/path_tools.py</code> <pre><code>def format_file_size(file_size, suffix=None):\n    \"\"\"Returns formatted string with size in appropriate unit.\n\n    Args:\n        file_size (int): Size of file in bytes.\n        suffix (str): Suffix for formatted size. Default is 'B' (as bytes).\n\n    Returns:\n        str: Formatted size using proper unit and passed suffix (e.g. 7 MiB).\n    \"\"\"\n\n    if suffix is None:\n        suffix = \"B\"\n\n    for unit in [\"\", \"Ki\", \"Mi\", \"Gi\", \"Ti\", \"Pi\", \"Ei\", \"Zi\"]:\n        if abs(file_size) &lt; 1024.0:\n            return \"%3.1f%s%s\" % (file_size, unit, suffix)\n        file_size /= 1024.0\n    return \"%.1f%s%s\" % (file_size, \"Yi\", suffix)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/path_tools.html#client.ayon_core.lib.path_tools.get_last_version_from_path","title":"<code>get_last_version_from_path(path_dir, filter)</code>","text":"<p>Find last version of given directory content.</p> <p>Parameters:</p> Name Type Description Default <code>path_dir</code> <code>str</code> <p>directory path</p> required <code>filter</code> <code>list</code> <p>list of strings used as file name filter</p> required <p>Returns:</p> Name Type Description <code>str</code> <p>file name with last version</p> Example <p>last_version_file = get_last_version_from_path(     \"/project/shots/shot01/work\", [\"shot01\", \"compositing\", \"nk\"])</p> Source code in <code>client/ayon_core/lib/path_tools.py</code> <pre><code>def get_last_version_from_path(path_dir, filter):\n    \"\"\"Find last version of given directory content.\n\n    Args:\n        path_dir (str): directory path\n        filter (list): list of strings used as file name filter\n\n    Returns:\n        str: file name with last version\n\n    Example:\n        last_version_file = get_last_version_from_path(\n            \"/project/shots/shot01/work\", [\"shot01\", \"compositing\", \"nk\"])\n    \"\"\"\n\n    assert os.path.isdir(path_dir), \"`path_dir` argument needs to be directory\"\n    assert isinstance(filter, list) and (\n        len(filter) != 0), \"`filter` argument needs to be list and not empty\"\n\n    filtered_files = list()\n\n    # form regex for filtering\n    pattern = r\".*\".join(filter)\n\n    for file in os.listdir(path_dir):\n        if not re.findall(pattern, file):\n            continue\n        filtered_files.append(file)\n\n    if filtered_files:\n        filtered_files.sort()\n        return filtered_files[-1]\n\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/path_tools.html#client.ayon_core.lib.path_tools.get_version_from_path","title":"<code>get_version_from_path(file)</code>","text":"<p>Find version number in file path string.</p> <p>Parameters:</p> Name Type Description Default <code>file</code> <code>str</code> <p>file path</p> required <p>Returns:</p> Name Type Description <code>str</code> <p>version number in string ('001')</p> Source code in <code>client/ayon_core/lib/path_tools.py</code> <pre><code>def get_version_from_path(file):\n    \"\"\"Find version number in file path string.\n\n    Args:\n        file (str): file path\n\n    Returns:\n        str: version number in string ('001')\n    \"\"\"\n\n    pattern = re.compile(r\"[\\._]v([0-9]+)\", re.IGNORECASE)\n    try:\n        return pattern.findall(file)[-1]\n    except IndexError:\n        log.error(\n            \"templates:get_version_from_workfile:\"\n            \"`{}` missing version string.\"\n            \"Example `v004`\".format(file)\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/path_tools.html#client.ayon_core.lib.path_tools.version_up","title":"<code>version_up(filepath)</code>","text":"<p>Version up filepath to a new non-existing version.</p> <p>Parses for a version identifier like <code>_v001</code> or <code>.v001</code> When no version present _v001 is appended as suffix.</p> <p>Parameters:</p> Name Type Description Default <code>filepath</code> <code>str</code> <p>full url</p> required <p>Returns:</p> Type Description <code>str</code> <p>filepath with increased version number</p> Source code in <code>client/ayon_core/lib/path_tools.py</code> <pre><code>def version_up(filepath):\n    \"\"\"Version up filepath to a new non-existing version.\n\n    Parses for a version identifier like `_v001` or `.v001`\n    When no version present _v001 is appended as suffix.\n\n    Args:\n        filepath (str): full url\n\n    Returns:\n        (str): filepath with increased version number\n\n    \"\"\"\n    dirname = os.path.dirname(filepath)\n    basename, ext = os.path.splitext(os.path.basename(filepath))\n\n    regex = r\"[._]v\\d+\"\n    matches = re.findall(regex, str(basename), re.IGNORECASE)\n    if not matches:\n        log.info(\"Creating version...\")\n        new_label = \"_v{version:03d}\".format(version=1)\n        new_basename = \"{}{}\".format(basename, new_label)\n    else:\n        label = matches[-1]\n        version = re.search(r\"\\d+\", label).group()\n        padding = len(version)\n\n        new_version = int(version) + 1\n        new_version = '{version:0{padding}d}'.format(version=new_version,\n                                                     padding=padding)\n        new_label = label.replace(version, new_version, 1)\n        new_basename = _rreplace(basename, label, new_label)\n    new_filename = \"{}{}\".format(new_basename, ext)\n    new_filename = os.path.join(dirname, new_filename)\n    new_filename = os.path.normpath(new_filename)\n\n    if new_filename == filepath:\n        raise RuntimeError(\"Created path is the same as current file,\"\n                           \"this is a bug\")\n\n    # We check for version clashes against the current file for any file\n    # that matches completely in name up to the {version} label found. Thus\n    # if source file was test_v001_test.txt we want to also check clashes\n    # against test_v002.txt but do want to preserve the part after the version\n    # label for our new filename\n    clash_basename = new_basename\n    if not clash_basename.endswith(new_label):\n        index = (clash_basename.find(new_label))\n        index += len(new_label)\n        clash_basename = clash_basename[:index]\n\n    for file in os.listdir(dirname):\n        if file.endswith(ext) and file.startswith(clash_basename):\n            log.info(\"Skipping existing version %s\" % new_label)\n            return version_up(new_filename)\n\n    log.info(\"New version %s\" % new_label)\n    return new_filename\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/plugin_tools.html","title":"plugin_tools","text":"<p>AYON plugin tools.</p>"},{"location":"autoapi/client/ayon_core/lib/plugin_tools.html#client.ayon_core.lib.plugin_tools.prepare_template_data","title":"<code>prepare_template_data(fill_pairs)</code>","text":"<p>Prepares formatted data for filling template.</p> <p>It produces multiple variants of keys (key, Key, KEY) to control format of filled template.</p> Example <p>src_data = { ...    \"host\": \"maya\", ... } output = prepare_template_data(src_data) sorted(list(output.items())) # sort &amp; list conversion for tests [('HOST', 'MAYA'), ('Host', 'Maya'), ('host', 'maya')]</p> <p>Parameters:</p> Name Type Description Default <code>fill_pairs</code> <code>Union[dict[str, Any], Iterable[Tuple[str, Any]]]</code> <p>The value that are prepared for template.</p> required <p>Returns:</p> Type Description <p>dict[str, str]: Prepared values for template.</p> Source code in <code>client/ayon_core/lib/plugin_tools.py</code> <pre><code>def prepare_template_data(fill_pairs):\n    \"\"\"Prepares formatted data for filling template.\n\n    It produces multiple variants of keys (key, Key, KEY) to control\n    format of filled template.\n\n    Example:\n        &gt;&gt;&gt; src_data = {\n        ...    \"host\": \"maya\",\n        ... }\n        &gt;&gt;&gt; output = prepare_template_data(src_data)\n        &gt;&gt;&gt; sorted(list(output.items())) # sort &amp; list conversion for tests\n        [('HOST', 'MAYA'), ('Host', 'Maya'), ('host', 'maya')]\n\n    Args:\n        fill_pairs (Union[dict[str, Any], Iterable[Tuple[str, Any]]]): The\n            value that are prepared for template.\n\n    Returns:\n        dict[str, str]: Prepared values for template.\n    \"\"\"\n\n    valid_items = _separate_keys_and_value(fill_pairs)\n    output = {}\n    for item in valid_items:\n        keys, value = item\n        # Convert only string values\n        if isinstance(value, str):\n            upper_value = value.upper()\n            capitalized_value = _capitalize_value(value)\n        else:\n            upper_value = capitalized_value = value\n\n        first_key = keys.pop(0)\n        if not keys:\n            output[first_key] = value\n            output[first_key.upper()] = upper_value\n            output[first_key.capitalize()] = capitalized_value\n            continue\n\n        # Prepare 'normal', 'upper' and 'capitalized' variables\n        normal = output.setdefault(first_key, {})\n        capitalized = output.setdefault(first_key.capitalize(), {})\n        upper = output.setdefault(first_key.upper(), {})\n\n        keys_deque = collections.deque(keys)\n        while keys_deque:\n            key = keys_deque.popleft()\n            upper_key = key\n            if isinstance(key, str):\n                upper_key = key.upper()\n\n            if not keys_deque:\n                # Fill value on last key\n                upper[upper_key] = upper_value\n                capitalized[key] = capitalized_value\n                normal[key] = value\n            else:\n                normal = normal.setdefault(key, {})\n                capitalized = capitalized.setdefault(key, {})\n                upper = upper.setdefault(upper_key, {})\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/plugin_tools.html#client.ayon_core.lib.plugin_tools.source_hash","title":"<code>source_hash(filepath, *args)</code>","text":"<p>Generate simple identifier for a source file. This is used to identify whether a source file has previously been processe into the pipeline, e.g. a texture. The hash is based on source filepath, modification time and file size. This is only used to identify whether a specific source file was already published before from the same location with the same modification date. We opt to do it this way as opposed to Avalanch C4 hash as this is much faster and predictable enough for all our production use cases. Args:     filepath (str): The source file path. You can specify additional arguments in the function to allow for specific 'processing' values to be included.</p> Source code in <code>client/ayon_core/lib/plugin_tools.py</code> <pre><code>def source_hash(filepath, *args):\n    \"\"\"Generate simple identifier for a source file.\n    This is used to identify whether a source file has previously been\n    processe into the pipeline, e.g. a texture.\n    The hash is based on source filepath, modification time and file size.\n    This is only used to identify whether a specific source file was already\n    published before from the same location with the same modification date.\n    We opt to do it this way as opposed to Avalanch C4 hash as this is much\n    faster and predictable enough for all our production use cases.\n    Args:\n        filepath (str): The source file path.\n    You can specify additional arguments in the function\n    to allow for specific 'processing' values to be included.\n    \"\"\"\n    # We replace dots with comma because . cannot be a key in a pymongo dict.\n    file_name = os.path.basename(filepath)\n    time = str(os.path.getmtime(filepath))\n    size = str(os.path.getsize(filepath))\n    return \"|\".join([file_name, time, size] + list(args)).replace(\".\", \",\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/profiles_filtering.html","title":"profiles_filtering","text":""},{"location":"autoapi/client/ayon_core/lib/profiles_filtering.html#client.ayon_core.lib.profiles_filtering.compile_list_of_regexes","title":"<code>compile_list_of_regexes(in_list)</code>","text":"<p>Convert strings in entered list to compiled regex objects.</p> Source code in <code>client/ayon_core/lib/profiles_filtering.py</code> <pre><code>def compile_list_of_regexes(in_list):\n    \"\"\"Convert strings in entered list to compiled regex objects.\"\"\"\n    regexes = list()\n    if not in_list:\n        return regexes\n\n    for item in in_list:\n        if not item:\n            continue\n        try:\n            regexes.append(re.compile(item))\n        except TypeError:\n            print((\n                \"Invalid type \\\"{}\\\" value \\\"{}\\\".\"\n                \" Expected string based object. Skipping.\"\n            ).format(str(type(item)), str(item)))\n    return regexes\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/profiles_filtering.html#client.ayon_core.lib.profiles_filtering.filter_profiles","title":"<code>filter_profiles(profiles_data, key_values, keys_order=None, logger=None)</code>","text":"<p>Filter profiles by entered key -&gt; values.</p> <p>Profile if marked with score for each key/value from <code>key_values</code> with points -1, 0 or 1. - if profile contain the key and profile's value contain value from     <code>key_values</code> then profile gets 1 point - if profile does not contain the key or profile's value is empty or     contain \"\" then got 0 point - if profile contain the key, profile's value is not empty and does not     contain \"\" and value from <code>key_values</code> is not available in the value     then got -1 point</p> <p>If profile gets -1 point at any time then is skipped and not used for output. Profile with higher score is returned. If there are multiple profiles with same score then first in order is used (order of profiles matter).</p> <p>Parameters:</p> Name Type Description Default <code>profiles_data</code> <code>list</code> <p>Profile definitions as dictionaries.</p> required <code>key_values</code> <code>dict</code> <p>Mapping of Key &lt;-&gt; Value. Key is checked if is available in profile and if Value is matching it's values.</p> required <code>keys_order</code> <code>(list, tuple)</code> <p>Order of keys from <code>key_values</code> which matters only when multiple profiles have same score.</p> <code>None</code> <code>logger</code> <code>Logger</code> <p>Optionally can be passed different logger.</p> <code>None</code> <p>Returns:</p> Type Description <p>dict/None: Return most matching profile or None if none of profiles match at least one criteria.</p> Source code in <code>client/ayon_core/lib/profiles_filtering.py</code> <pre><code>def filter_profiles(profiles_data, key_values, keys_order=None, logger=None):\n    \"\"\" Filter profiles by entered key -&gt; values.\n\n    Profile if marked with score for each key/value from `key_values` with\n    points -1, 0 or 1.\n    - if profile contain the key and profile's value contain value from\n        `key_values` then profile gets 1 point\n    - if profile does not contain the key or profile's value is empty or\n        contain \"*\" then got 0 point\n    - if profile contain the key, profile's value is not empty and does not\n        contain \"*\" and value from `key_values` is not available in the value\n        then got -1 point\n\n    If profile gets -1 point at any time then is skipped and not used for\n    output. Profile with higher score is returned. If there are multiple\n    profiles with same score then first in order is used (order of profiles\n    matter).\n\n    Args:\n        profiles_data (list): Profile definitions as dictionaries.\n        key_values (dict): Mapping of Key &lt;-&gt; Value. Key is checked if is\n            available in profile and if Value is matching it's values.\n        keys_order (list, tuple): Order of keys from `key_values` which matters\n            only when multiple profiles have same score.\n        logger (logging.Logger): Optionally can be passed different logger.\n\n    Returns:\n        dict/None: Return most matching profile or None if none of profiles\n            match at least one criteria.\n    \"\"\"\n    if not profiles_data:\n        return None\n\n    if not logger:\n        logger = log\n\n    if not keys_order:\n        keys_order = tuple(key_values.keys())\n    else:\n        _keys_order = list(keys_order)\n        # Make all keys from `key_values` are passed\n        for key in key_values.keys():\n            if key not in _keys_order:\n                _keys_order.append(key)\n        keys_order = tuple(_keys_order)\n\n    log_parts = \" | \".join([\n        \"{}: \\\"{}\\\"\".format(*item)\n        for item in key_values.items()\n    ])\n\n    logger.debug(\n        \"Looking for matching profile for: {}\".format(log_parts)\n    )\n\n    matching_profiles = None\n    highest_profile_points = -1\n    # Each profile get 1 point for each matching filter. Profile with most\n    # points is returned. For cases when more than one profile will match\n    # are also stored ordered lists of matching values.\n    for profile in profiles_data:\n        profile_points = 0\n        profile_scores = []\n\n        for key in keys_order:\n            value = key_values[key]\n            match = validate_value_by_regexes(value, profile.get(key))\n            if match == -1:\n                profile_value = profile.get(key) or []\n                logger.debug(\n                    \"\\\"{}\\\" not found in \\\"{}\\\": {}\".format(value, key,\n                                                            profile_value)\n                )\n                profile_points = -1\n                break\n\n            profile_points += match\n            profile_scores.append(bool(match))\n\n        if (\n            profile_points &lt; 0\n            or profile_points &lt; highest_profile_points\n        ):\n            continue\n\n        if profile_points &gt; highest_profile_points:\n            matching_profiles = []\n            highest_profile_points = profile_points\n\n        if profile_points == highest_profile_points:\n            matching_profiles.append((profile, profile_scores))\n\n    if not matching_profiles:\n        logger.debug(\n            \"None of profiles match your setup. {}\".format(log_parts)\n        )\n        return None\n\n    if len(matching_profiles) &gt; 1:\n        logger.debug(\n            \"More than one profile match your setup. {}\".format(log_parts)\n        )\n\n    profile = _profile_exclusion(matching_profiles, logger)\n    if profile:\n        logger.debug(\n            \"Profile selected: {}\".format(profile)\n        )\n    return profile\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/profiles_filtering.html#client.ayon_core.lib.profiles_filtering.fullmatch","title":"<code>fullmatch(regex, string, flags=0)</code>","text":"<p>Emulate python-3.4 re.fullmatch().</p> Source code in <code>client/ayon_core/lib/profiles_filtering.py</code> <pre><code>def fullmatch(regex, string, flags=0):\n    \"\"\"Emulate python-3.4 re.fullmatch().\"\"\"\n    matched = re.match(regex, string, flags=flags)\n    if matched and matched.span()[1] == len(string):\n        return matched\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/profiles_filtering.html#client.ayon_core.lib.profiles_filtering.validate_value_by_regexes","title":"<code>validate_value_by_regexes(value, in_list)</code>","text":"<p>Validates in any regex from list match entered value.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>str</code> <p>String where regexes is checked.</p> required <code>in_list</code> <code>list</code> <p>List with regexes.</p> required <p>Returns:</p> Name Type Description <code>int</code> <p>Returns <code>0</code> when list is not set, is empty or contain \"*\". Returns <code>1</code> when any regex match value and returns <code>-1</code> when none of regexes match entered value.</p> Source code in <code>client/ayon_core/lib/profiles_filtering.py</code> <pre><code>def validate_value_by_regexes(value, in_list):\n    \"\"\"Validates in any regex from list match entered value.\n\n    Args:\n        value (str): String where regexes is checked.\n        in_list (list): List with regexes.\n\n    Returns:\n        int: Returns `0` when list is not set, is empty or contain \"*\".\n            Returns `1` when any regex match value and returns `-1`\n            when none of regexes match entered value.\n    \"\"\"\n    if not in_list:\n        return 0\n\n    if not isinstance(in_list, (list, tuple, set)):\n        in_list = [in_list]\n\n    if \"*\" in in_list:\n        return 0\n\n    # If value is not set and in list has specific values then resolve value\n    #   as not matching.\n    if not value:\n        return -1\n\n    regexes = compile_list_of_regexes(in_list)\n    for regex in regexes:\n        if hasattr(regex, \"fullmatch\"):\n            result = regex.fullmatch(value)\n        else:\n            result = fullmatch(regex, value)\n        if result:\n            return 1\n    return -1\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/profiling.html","title":"profiling","text":"<p>Provide profiling decorator.</p>"},{"location":"autoapi/client/ayon_core/lib/profiling.html#client.ayon_core.lib.profiling.do_profile","title":"<code>do_profile(fn, to_file=None)</code>","text":"<p>Wraps function in profiler run and print stat after it is done.</p> <p>Parameters:</p> Name Type Description Default <code>to_file</code> <code>str</code> <p>If specified, dumps stats into the file</p> <code>None</code> Source code in <code>client/ayon_core/lib/profiling.py</code> <pre><code>def do_profile(fn, to_file=None):\n    \"\"\"Wraps function in profiler run and print stat after it is done.\n\n    Args:\n        to_file (str, optional): If specified, dumps stats into the file\n        instead of printing.\n\n    \"\"\"\n    if to_file:\n        to_file = to_file.format(pid=os.getpid())\n\n    def profiled(*args, **kwargs):\n        profiler = cProfile.Profile()\n        try:\n            profiler.enable()\n            res = fn(*args, **kwargs)\n            profiler.disable()\n            return res\n        finally:\n            if to_file:\n                profiler.dump_stats(to_file)\n            else:\n                profiler.print_stats()\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/python_module_tools.html","title":"python_module_tools","text":"<p>Tools for working with python modules and classes.</p>"},{"location":"autoapi/client/ayon_core/lib/python_module_tools.html#client.ayon_core.lib.python_module_tools.classes_from_module","title":"<code>classes_from_module(superclass, module)</code>","text":"<p>Return plug-ins from module</p> <p>Parameters:</p> Name Type Description Default <code>superclass</code> <code>superclass</code> <p>Superclass of subclasses to look for</p> required <code>module</code> <code>ModuleType</code> <p>Imported module where to look for 'superclass' subclasses.</p> required <p>Returns:</p> Type Description <p>List of plug-ins, or empty list if none is found.</p> Source code in <code>client/ayon_core/lib/python_module_tools.py</code> <pre><code>def classes_from_module(superclass, module):\n    \"\"\"Return plug-ins from module\n\n    Arguments:\n        superclass (superclass): Superclass of subclasses to look for\n        module (types.ModuleType): Imported module where to look for\n            'superclass' subclasses.\n\n    Returns:\n        List of plug-ins, or empty list if none is found.\n\n    \"\"\"\n\n    classes = list()\n    for name in dir(module):\n        # It could be anything at this point\n        obj = getattr(module, name)\n        if not inspect.isclass(obj) or obj is superclass:\n            continue\n\n        if issubclass(obj, superclass):\n            classes.append(obj)\n\n    return classes\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/python_module_tools.html#client.ayon_core.lib.python_module_tools.import_filepath","title":"<code>import_filepath(filepath, module_name=None, sys_module_name=None)</code>","text":"<p>Import python file as python module.</p> <p>Parameters:</p> Name Type Description Default <code>filepath</code> <code>str</code> <p>Path to python file.</p> required <code>module_name</code> <code>str</code> <p>Name of loaded module. Only for Python 3. By default is filled with filename of filepath.</p> <code>None</code> <code>sys_module_name</code> <code>str</code> <p>Name of module in <code>sys.modules</code> where to store loaded module. By default is None so module is not added to <code>sys.modules</code>.</p> <code>None</code> <p>Todo (antirotor): We should add the module to the sys.modules always but     we need to be careful about it and test it properly.</p> Source code in <code>client/ayon_core/lib/python_module_tools.py</code> <pre><code>def import_filepath(\n        filepath: str,\n        module_name: Optional[str] = None,\n        sys_module_name: Optional[str] = None) -&gt; types.ModuleType:\n    \"\"\"Import python file as python module.\n\n    Args:\n        filepath (str): Path to python file.\n        module_name (str): Name of loaded module. Only for Python 3. By default\n            is filled with filename of filepath.\n        sys_module_name (str): Name of module in `sys.modules` where to store\n            loaded module. By default is None so module is not added to\n            `sys.modules`.\n\n    Todo (antirotor): We should add the module to the sys.modules always but\n        we need to be careful about it and test it properly.\n\n    \"\"\"\n    if module_name is None:\n        module_name = os.path.splitext(os.path.basename(filepath))[0]\n\n    # Prepare module object where content of file will be parsed\n    module = types.ModuleType(module_name)\n    module.__file__ = filepath\n\n    # Use loader so module has full specs\n    module_loader = importlib.machinery.SourceFileLoader(\n        module_name, filepath\n    )\n    # only add to sys.modules if requested\n    if sys_module_name:\n        sys.modules[sys_module_name] = module\n    module_loader.exec_module(module)\n    return module\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/python_module_tools.html#client.ayon_core.lib.python_module_tools.import_module_from_dirpath","title":"<code>import_module_from_dirpath(dirpath, folder_name, dst_module_name=None)</code>","text":"<p>Import passed directory as a python module.</p> <p>Imported module can be assigned as a child attribute of already loaded module from <code>sys.modules</code> if has support of <code>setattr</code>. That is not default behavior of python modules so parent module must be a custom module with that ability.</p> <p>It is not possible to reimport already cached module. If you need to reimport module you have to remove it from caches manually.</p> <p>Parameters:</p> Name Type Description Default <code>dirpath</code> <code>str</code> <p>Parent directory path of loaded folder.</p> required <code>folder_name</code> <code>str</code> <p>Folder name which should be imported inside passed directory.</p> required <code>dst_module_name</code> <code>str</code> <p>Parent module name under which can be loaded module added.</p> <code>None</code> Source code in <code>client/ayon_core/lib/python_module_tools.py</code> <pre><code>def import_module_from_dirpath(\n        dirpath, folder_name, dst_module_name=None):\n    \"\"\"Import passed directory as a python module.\n\n    Imported module can be assigned as a child attribute of already loaded\n    module from `sys.modules` if has support of `setattr`. That is not default\n    behavior of python modules so parent module must be a custom module with\n    that ability.\n\n    It is not possible to reimport already cached module. If you need to\n    reimport module you have to remove it from caches manually.\n\n    Args:\n        dirpath (str): Parent directory path of loaded folder.\n        folder_name (str): Folder name which should be imported inside passed\n            directory.\n        dst_module_name (str): Parent module name under which can be loaded\n            module added.\n\n    \"\"\"\n    # Import passed dirpath as python module\n    if dst_module_name:\n        full_module_name = \"{}.{}\".format(dst_module_name, folder_name)\n        dst_module = sys.modules[dst_module_name]\n    else:\n        full_module_name = folder_name\n        dst_module = None\n\n    # Skip import if is already imported\n    if full_module_name in sys.modules:\n        return sys.modules[full_module_name]\n\n    import importlib.util\n    from importlib._bootstrap_external import PathFinder\n\n    # Find loader for passed path and name\n    loader = PathFinder.find_module(full_module_name, [dirpath])\n\n    # Load specs of module\n    spec = importlib.util.spec_from_loader(\n        full_module_name, loader, origin=dirpath\n    )\n\n    # Create module based on specs\n    module = importlib.util.module_from_spec(spec)\n\n    # Store module to destination module and `sys.modules`\n    # WARNING this mus be done before module execution\n    if dst_module is not None:\n        setattr(dst_module, folder_name, module)\n\n    sys.modules[full_module_name] = module\n\n    # Execute module import\n    loader.exec_module(module)\n\n    return module\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/python_module_tools.html#client.ayon_core.lib.python_module_tools.is_func_signature_supported","title":"<code>is_func_signature_supported(func, *args, **kwargs)</code>","text":"<p>Check if a function signature supports passed args and kwargs.</p> <p>This check does not actually call the function, just look if function can be called with the arguments.</p> Notes <p>This does NOT check if the function would work with passed arguments     only if they can be passed in. If function have args, *kwargs     in parameters, this will always return 'True'.</p> Example <p>def my_function(my_number): ...     return my_number + 1 ... is_func_signature_supported(my_function, 1) True is_func_signature_supported(my_function, 1, 2) False is_func_signature_supported(my_function, my_number=1) True is_func_signature_supported(my_function, number=1) False is_func_signature_supported(my_function, \"string\") True def my_other_function(args, kwargs): ...     my_function(args, **kwargs) ... is_func_signature_supported( ...     my_other_function, ...     \"string\", ...     1, ...     other=None ... ) True</p> <p>Parameters:</p> Name Type Description Default <code>func</code> <code>Callable</code> <p>A function where the signature should be tested.</p> required <code>*args</code> <code>Any</code> <p>Positional arguments for function signature.</p> <code>()</code> <code>**kwargs</code> <code>Any</code> <p>Keyword arguments for function signature.</p> <code>{}</code> <p>Returns:</p> Name Type Description <code>bool</code> <p>Function can pass in arguments.</p> Source code in <code>client/ayon_core/lib/python_module_tools.py</code> <pre><code>def is_func_signature_supported(func, *args, **kwargs):\n    \"\"\"Check if a function signature supports passed args and kwargs.\n\n    This check does not actually call the function, just look if function can\n    be called with the arguments.\n\n    Notes:\n        This does NOT check if the function would work with passed arguments\n            only if they can be passed in. If function have *args, **kwargs\n            in parameters, this will always return 'True'.\n\n    Example:\n        &gt;&gt;&gt; def my_function(my_number):\n        ...     return my_number + 1\n        ...\n        &gt;&gt;&gt; is_func_signature_supported(my_function, 1)\n        True\n        &gt;&gt;&gt; is_func_signature_supported(my_function, 1, 2)\n        False\n        &gt;&gt;&gt; is_func_signature_supported(my_function, my_number=1)\n        True\n        &gt;&gt;&gt; is_func_signature_supported(my_function, number=1)\n        False\n        &gt;&gt;&gt; is_func_signature_supported(my_function, \"string\")\n        True\n        &gt;&gt;&gt; def my_other_function(*args, **kwargs):\n        ...     my_function(*args, **kwargs)\n        ...\n        &gt;&gt;&gt; is_func_signature_supported(\n        ...     my_other_function,\n        ...     \"string\",\n        ...     1,\n        ...     other=None\n        ... )\n        True\n\n    Args:\n        func (Callable): A function where the signature should be tested.\n        *args (Any): Positional arguments for function signature.\n        **kwargs (Any): Keyword arguments for function signature.\n\n    Returns:\n        bool: Function can pass in arguments.\n\n    \"\"\"\n    sig = inspect.signature(func)\n    try:\n        sig.bind(*args, **kwargs)\n        return True\n    except TypeError:\n        pass\n    return False\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/python_module_tools.html#client.ayon_core.lib.python_module_tools.modules_from_path","title":"<code>modules_from_path(folder_path)</code>","text":"<p>Get python scripts as modules from a path.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Path to folder containing python scripts.</p> required <p>Returns:</p> Type Description <p>tuple: First list contains successfully imported modules and second list contains tuples of path and exception. Source code in <code>client/ayon_core/lib/python_module_tools.py</code> <pre><code>def modules_from_path(folder_path):\n    \"\"\"Get python scripts as modules from a path.\n\n    Arguments:\n        path (str): Path to folder containing python scripts.\n\n    Returns:\n        tuple&lt;list, list&gt;: First list contains successfully imported modules\n            and second list contains tuples of path and exception.\n    \"\"\"\n    crashed = []\n    modules = []\n    output = (modules, crashed)\n    # Just skip and return empty list if path is not set\n    if not folder_path:\n        return output\n\n    # Do not allow relative imports\n    if folder_path.startswith(\".\"):\n        log.warning((\n            \"BUG: Relative paths are not allowed for security reasons. {}\"\n        ).format(folder_path))\n        return output\n\n    folder_path = os.path.normpath(folder_path)\n\n    if not os.path.isdir(folder_path):\n        log.warning(\"Not a directory path: {}\".format(folder_path))\n        return output\n\n    for filename in os.listdir(folder_path):\n        # Ignore files which start with underscore\n        if filename.startswith(\"_\"):\n            continue\n\n        mod_name, mod_ext = os.path.splitext(filename)\n        if not mod_ext == \".py\":\n            continue\n\n        full_path = os.path.join(folder_path, filename)\n        if not os.path.isfile(full_path):\n            continue\n\n        try:\n            module = import_filepath(full_path, mod_name)\n            modules.append((full_path, module))\n\n        except Exception:\n            crashed.append((full_path, sys.exc_info()))\n            log.warning(\n                \"Failed to load path: \\\"{0}\\\"\".format(full_path),\n                exc_info=True\n            )\n            continue\n\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/python_module_tools.html#client.ayon_core.lib.python_module_tools.recursive_bases_from_class","title":"<code>recursive_bases_from_class(klass)</code>","text":"<p>Extract all bases from entered class.</p> Source code in <code>client/ayon_core/lib/python_module_tools.py</code> <pre><code>def recursive_bases_from_class(klass):\n    \"\"\"Extract all bases from entered class.\"\"\"\n    result = []\n    bases = klass.__bases__\n    result.extend(bases)\n    for base in bases:\n        result.extend(recursive_bases_from_class(base))\n    return result\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/terminal.html","title":"terminal","text":"<p>Package helping with colorizing and formatting terminal output.</p>"},{"location":"autoapi/client/ayon_core/lib/terminal.html#client.ayon_core.lib.terminal.Terminal","title":"<code>Terminal</code>","text":"<p>Class formatting messages using colorama to specific visual tokens.</p> <p>If :mod:<code>Colorama</code> is not found, it will still work, but without colors.</p> <p>Depends on :mod:<code>Colorama</code> Using AYON_LOG_NO_COLORS environment variable.</p> Source code in <code>client/ayon_core/lib/terminal.py</code> <pre><code>class Terminal:\n    \"\"\"Class formatting messages using colorama to specific visual tokens.\n\n    If :mod:`Colorama` is not found, it will still work, but without colors.\n\n    Depends on :mod:`Colorama`\n    Using **AYON_LOG_NO_COLORS** environment variable.\n    \"\"\"\n\n    # Is Terminal initialized\n    _initialized = False\n    # Thread lock for initialization to avoid race conditions\n    _init_lock = threading.Lock()\n    # Use colorized output\n    use_colors = True\n    # Output message replacements mapping - set on initialization\n    _sdict = {}\n\n    @staticmethod\n    def _initialize():\n        \"\"\"Initialize Terminal class as object.\n\n        First check if colorized output is disabled by environment variable\n        `AYON_LOG_NO_COLORS` value. By default is colorized output turned\n        on.\n\n        Then tries to import python module that do the colors magic and create\n        it's terminal object. Colorized output is not used if import of python\n        module or terminal object creation fails.\n\n        Set `_initialized` attribute to `True` when is done.\n        \"\"\"\n\n        from ayon_core.lib import env_value_to_bool\n\n        log_no_colors = env_value_to_bool(\n            \"AYON_LOG_NO_COLORS\", default=None\n        )\n        if log_no_colors is not None:\n            Terminal.use_colors = not log_no_colors\n\n        if not Terminal.use_colors:\n            Terminal._initialized = True\n            return\n\n        try:\n            # Try to import `blessed` module and create `Terminal` object\n            import blessed\n            term = blessed.Terminal()\n\n        except Exception:\n            # Do not use colors if crashed\n            Terminal.use_colors = False\n            print(\n                \"Module `blessed` failed on import or terminal creation.\"\n                \" AYON terminal won't use colors.\"\n            )\n            Terminal._initialized = True\n            return\n\n        # shortcuts for blessed codes\n        _SB = term.bold\n        _RST = \"\"\n        _LR = term.tomato2\n        _LG = term.aquamarine3\n        _LB = term.turquoise2\n        _LM = term.slateblue2\n        _LY = term.gold\n        _R = term.red\n        _G = term.green\n        _B = term.blue\n        _C = term.cyan\n        _Y = term.yellow\n        _W = term.white\n\n        # dictionary replacing string sequences with colorized one\n        Terminal._sdict = {\n            r\"&gt;&gt;&gt; \": _SB + _LG + r\"&gt;&gt;&gt; \" + _RST,\n            r\"!!!(?!\\sCRI|\\sERR)\": _SB + _R + r\"!!! \" + _RST,\n            r\"\\-\\-\\- \": _SB + _C + r\"--- \" + _RST,\n            r\"\\*\\*\\*(?!\\sWRN)\": _SB + _LY + r\"***\" + _RST,\n            r\"\\*\\*\\* WRN\": _SB + _LY + r\"*** WRN\" + _RST,\n            r\"  \\- \": _SB + _LY + r\"  - \" + _RST,\n            r\"\\[ \": _SB + _LG + r\"[ \" + _RST,\n            r\" \\]\": _SB + _LG + r\" ]\" + _RST,\n            r\"{\": _LG + r\"{\",\n            r\"}\": r\"}\" + _RST,\n            r\"\\(\": _LY + r\"(\",\n            r\"\\)\": r\")\" + _RST,\n            r\"^\\.\\.\\. \": _SB + _LR + r\"... \" + _RST,\n            r\"!!! ERR: \":\n                _SB + _LR + r\"!!! ERR: \" + _RST,\n            r\"!!! CRI: \":\n                _SB + _R + r\"!!! CRI: \" + _RST,\n            r\"(?i)failed\": _SB + _LR + \"FAILED\" + _RST,\n            r\"(?i)error\": _SB + _LR + \"ERROR\" + _RST\n        }\n\n        Terminal._SB = _SB\n        Terminal._RST = _RST\n        Terminal._LR = _LR\n        Terminal._LG = _LG\n        Terminal._LB = _LB\n        Terminal._LM = _LM\n        Terminal._LY = _LY\n        Terminal._R = _R\n        Terminal._G = _G\n        Terminal._B = _B\n        Terminal._C = _C\n        Terminal._Y = _Y\n        Terminal._W = _W\n\n        Terminal._initialized = True\n\n    @staticmethod\n    def _multiple_replace(text, adict):\n        \"\"\"Replace multiple tokens defined in dict.\n\n        Find and replace all occurrences of strings defined in dict is\n        supplied string.\n\n        Args:\n            text (str): string to be searched\n            adict (dict): dictionary with `{'search': 'replace'}`\n\n        Returns:\n            str: string with replaced tokens\n\n        \"\"\"\n        for r, v in adict.items():\n            text = re.sub(r, v, text)\n\n        return text\n\n    @staticmethod\n    def echo(message):\n        \"\"\"Print colorized message to stdout.\n\n        Args:\n            message (str): Message to be colorized.\n            debug (bool):\n\n        Returns:\n            str: Colorized message.\n\n        \"\"\"\n        colorized = Terminal.log(message)\n        print(colorized)\n\n        return colorized\n\n    @staticmethod\n    def log(message):\n        \"\"\"Return color formatted message.\n\n        If environment variable `AYON_LOG_NO_COLORS` is set to\n        whatever value, message will be formatted but not colorized.\n\n        Args:\n            message (str): Message to be colorized.\n\n        Returns:\n            str: Colorized message.\n\n        \"\"\"\n        T = Terminal\n        # Initialize if not yet initialized and use thread lock to avoid race\n        # condition issues\n        if not T._initialized:\n            # Check if lock is already locked to be sure `_initialize` is not\n            # executed multiple times\n            if not T._init_lock.locked():\n                with T._init_lock:\n                    T._initialize()\n            else:\n                # If lock is locked wait until is finished\n                while T._init_lock.locked():\n                    time.sleep(0.1)\n\n        # if we dont want colors, just print raw message\n        if not T.use_colors:\n            return message\n\n        message = re.sub(r'\\[(.*)\\]', '[ ' + T._SB + T._W +\n                         r'\\1' + T._RST + ' ]', message)\n        message = T._multiple_replace(message + T._RST, T._sdict)\n\n        return message\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/terminal.html#client.ayon_core.lib.terminal.Terminal.echo","title":"<code>echo(message)</code>  <code>staticmethod</code>","text":"<p>Print colorized message to stdout.</p> <p>Parameters:</p> Name Type Description Default <code>message</code> <code>str</code> <p>Message to be colorized.</p> required <code>debug</code> <code>bool</code> required <p>Returns:</p> Name Type Description <code>str</code> <p>Colorized message.</p> Source code in <code>client/ayon_core/lib/terminal.py</code> <pre><code>@staticmethod\ndef echo(message):\n    \"\"\"Print colorized message to stdout.\n\n    Args:\n        message (str): Message to be colorized.\n        debug (bool):\n\n    Returns:\n        str: Colorized message.\n\n    \"\"\"\n    colorized = Terminal.log(message)\n    print(colorized)\n\n    return colorized\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/terminal.html#client.ayon_core.lib.terminal.Terminal.log","title":"<code>log(message)</code>  <code>staticmethod</code>","text":"<p>Return color formatted message.</p> <p>If environment variable <code>AYON_LOG_NO_COLORS</code> is set to whatever value, message will be formatted but not colorized.</p> <p>Parameters:</p> Name Type Description Default <code>message</code> <code>str</code> <p>Message to be colorized.</p> required <p>Returns:</p> Name Type Description <code>str</code> <p>Colorized message.</p> Source code in <code>client/ayon_core/lib/terminal.py</code> <pre><code>@staticmethod\ndef log(message):\n    \"\"\"Return color formatted message.\n\n    If environment variable `AYON_LOG_NO_COLORS` is set to\n    whatever value, message will be formatted but not colorized.\n\n    Args:\n        message (str): Message to be colorized.\n\n    Returns:\n        str: Colorized message.\n\n    \"\"\"\n    T = Terminal\n    # Initialize if not yet initialized and use thread lock to avoid race\n    # condition issues\n    if not T._initialized:\n        # Check if lock is already locked to be sure `_initialize` is not\n        # executed multiple times\n        if not T._init_lock.locked():\n            with T._init_lock:\n                T._initialize()\n        else:\n            # If lock is locked wait until is finished\n            while T._init_lock.locked():\n                time.sleep(0.1)\n\n    # if we dont want colors, just print raw message\n    if not T.use_colors:\n        return message\n\n    message = re.sub(r'\\[(.*)\\]', '[ ' + T._SB + T._W +\n                     r'\\1' + T._RST + ' ]', message)\n    message = T._multiple_replace(message + T._RST, T._sdict)\n\n    return message\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/transcoding.html","title":"transcoding","text":""},{"location":"autoapi/client/ayon_core/lib/transcoding.html#client.ayon_core.lib.transcoding.MissingRGBAChannelsError","title":"<code>MissingRGBAChannelsError</code>","text":"<p>               Bases: <code>ValueError</code></p> <p>Raised when we can't find channels to use as RGBA for conversion in input media.</p> <p>This may be other channels than solely RGBA, like Z-channel. The error is raised when no matching 'reviewable' channel was found.</p> Source code in <code>client/ayon_core/lib/transcoding.py</code> <pre><code>class MissingRGBAChannelsError(ValueError):\n    \"\"\"Raised when we can't find channels to use as RGBA for conversion in\n    input media.\n\n    This may be other channels than solely RGBA, like Z-channel. The error is\n    raised when no matching 'reviewable' channel was found.\n    \"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/transcoding.html#client.ayon_core.lib.transcoding.RationalToInt","title":"<code>RationalToInt</code>","text":"<p>Rational value stored as division of 2 integers using string.</p> Source code in <code>client/ayon_core/lib/transcoding.py</code> <pre><code>class RationalToInt:\n    \"\"\"Rational value stored as division of 2 integers using string.\"\"\"\n\n    def __init__(self, string_value):\n        parts = string_value.split(\"/\")\n        top = float(parts[0])\n        bottom = 1.0\n        if len(parts) != 1:\n            bottom = float(parts[1])\n\n        self._value = float(top) / float(bottom)\n        self._string_value = string_value\n\n    @property\n    def value(self):\n        return self._value\n\n    @property\n    def string_value(self):\n        return self._string_value\n\n    def __format__(self, *args, **kwargs):\n        return self._string_value.__format__(*args, **kwargs)\n\n    def __float__(self):\n        return self._value\n\n    def __str__(self):\n        return self._string_value\n\n    def __repr__(self):\n        return \"&lt;{}&gt; {}\".format(self.__class__.__name__, self._string_value)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/transcoding.html#client.ayon_core.lib.transcoding.convert_color_values","title":"<code>convert_color_values(application, color_value)</code>","text":"<p>Get color mapping for ffmpeg and oiiotool.</p> <p>Parameters:</p> Name Type Description Default <code>application</code> <code>str</code> <p>Application for which command should be created.</p> required <code>color_value</code> <code>tuple[int, int, int, float]</code> <p>List of 8bit int values for RGBA.</p> required <p>Returns:</p> Name Type Description <code>str</code> <p>ffmpeg returns hex string, oiiotool is string with floats.</p> Source code in <code>client/ayon_core/lib/transcoding.py</code> <pre><code>def convert_color_values(application, color_value):\n    \"\"\"Get color mapping for ffmpeg and oiiotool.\n\n    Args:\n        application (str): Application for which command should be created.\n        color_value (tuple[int, int, int, float]): List of 8bit int values\n            for RGBA.\n\n    Returns:\n        str: ffmpeg returns hex string, oiiotool is string with floats.\n\n    \"\"\"\n    red, green, blue, alpha = color_value\n\n    if application == \"ffmpeg\":\n        return \"{0:0&gt;2X}{1:0&gt;2X}{2:0&gt;2X}@{3}\".format(\n            red, green, blue, alpha\n        )\n    elif application == \"oiiotool\":\n        red = float(red / 255)\n        green = float(green / 255)\n        blue = float(blue / 255)\n\n        return \"{0:.3f},{1:.3f},{2:.3f},{3:.3f}\".format(\n            red, green, blue, alpha)\n    else:\n        raise ValueError(\n            \"\\\"application\\\" input argument should \"\n            \"be either \\\"ffmpeg\\\" or \\\"oiiotool\\\"\"\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/transcoding.html#client.ayon_core.lib.transcoding.convert_colorspace","title":"<code>convert_colorspace(input_path, output_path, config_path, source_colorspace, target_colorspace=None, view=None, display=None, additional_command_args=None, logger=None)</code>","text":"<p>DEPRECATED function use <code>oiio_color_convert</code> instead</p> <p>Parameters:</p> Name Type Description Default <code>input_path</code> <code>str</code> <p>Path to input file that should be converted.</p> required <code>output_path</code> <code>str</code> <p>Path to output file where result will be stored.</p> required <code>config_path</code> <code>str</code> <p>Path to OCIO config file.</p> required <code>source_colorspace</code> <code>str</code> <p>OCIO valid color space of source files.</p> required <code>target_colorspace</code> <code>str</code> <p>OCIO valid target color space. If filled, 'view' and 'display' must be empty.</p> <code>None</code> <code>view</code> <code>str</code> <p>Name for target viewer space (OCIO valid). Both 'view' and 'display' must be filled (if not 'target_colorspace').</p> <code>None</code> <code>display</code> <code>str</code> <p>Name for target display-referred reference space. Both 'view' and 'display' must be filled (if not 'target_colorspace').</p> <code>None</code> <code>additional_command_args</code> <code>list</code> <p>Additional arguments for oiiotool (like binary depth for .dpx).</p> <code>None</code> <code>logger</code> <code>Logger</code> <p>Logger used for logging.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>None</code> <p>Function returns None.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If parameters are misconfigured.</p> Source code in <code>client/ayon_core/lib/transcoding.py</code> <pre><code>@deprecated(\"oiio_color_convert\")\ndef convert_colorspace(\n    input_path,\n    output_path,\n    config_path,\n    source_colorspace,\n    target_colorspace=None,\n    view=None,\n    display=None,\n    additional_command_args=None,\n    logger=None,\n):\n    \"\"\"DEPRECATED function use `oiio_color_convert` instead\n\n    Args:\n        input_path (str): Path to input file that should be converted.\n        output_path (str): Path to output file where result will be stored.\n        config_path (str): Path to OCIO config file.\n        source_colorspace (str): OCIO valid color space of source files.\n        target_colorspace (str, optional): OCIO valid target color space.\n            If filled, 'view' and 'display' must be empty.\n        view (str, optional): Name for target viewer space (OCIO valid).\n            Both 'view' and 'display' must be filled\n            (if not 'target_colorspace').\n        display (str, optional): Name for target display-referred\n            reference space. Both 'view' and 'display' must be filled\n            (if not 'target_colorspace').\n        additional_command_args (list, optional): Additional arguments\n            for oiiotool (like binary depth for .dpx).\n        logger (logging.Logger, optional): Logger used for logging.\n\n    Returns:\n        None: Function returns None.\n\n    Raises:\n        ValueError: If parameters are misconfigured.\n    \"\"\"\n    return oiio_color_convert(\n        input_path,\n        output_path,\n        config_path,\n        source_colorspace,\n        target_colorspace=target_colorspace,\n        target_display=display,\n        target_view=view,\n        additional_command_args=additional_command_args,\n        logger=logger,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/transcoding.html#client.ayon_core.lib.transcoding.convert_ffprobe_fps_to_float","title":"<code>convert_ffprobe_fps_to_float(value)</code>","text":"<p>Convert string value of frame rate to float.</p> <p>Copy of 'convert_ffprobe_fps_value' which raises exceptions on invalid value, does not convert value to string and does not return \"Unknown\" string.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>str</code> <p>Value to be converted.</p> required <p>Returns:</p> Name Type Description <code>Float</code> <p>Converted frame rate in float. If divisor in value is '0' then '0.0' is returned.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>Passed value is invalid for conversion.</p> Source code in <code>client/ayon_core/lib/transcoding.py</code> <pre><code>def convert_ffprobe_fps_to_float(value):\n    \"\"\"Convert string value of frame rate to float.\n\n    Copy of 'convert_ffprobe_fps_value' which raises exceptions on invalid\n    value, does not convert value to string and does not return \"Unknown\"\n    string.\n\n    Args:\n        value (str): Value to be converted.\n\n    Returns:\n        Float: Converted frame rate in float. If divisor in value is '0' then\n            '0.0' is returned.\n\n    Raises:\n        ValueError: Passed value is invalid for conversion.\n    \"\"\"\n\n    if not value:\n        raise ValueError(\"Got empty value.\")\n\n    items = value.split(\"/\")\n    if len(items) == 1:\n        return float(items[0])\n\n    if len(items) &gt; 2:\n        raise ValueError((\n            \"FPS expression contains multiple dividers \\\"{}\\\".\"\n        ).format(value))\n\n    dividend = float(items.pop(0))\n    divisor = float(items.pop(0))\n    if divisor == 0.0:\n        return 0.0\n    return dividend / divisor\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/transcoding.html#client.ayon_core.lib.transcoding.convert_ffprobe_fps_value","title":"<code>convert_ffprobe_fps_value(str_value)</code>","text":"<p>Returns (str) value of fps from ffprobe frame format (120/1)</p> Source code in <code>client/ayon_core/lib/transcoding.py</code> <pre><code>def convert_ffprobe_fps_value(str_value):\n    \"\"\"Returns (str) value of fps from ffprobe frame format (120/1)\"\"\"\n    if str_value == \"0/0\":\n        print(\"WARNING: Source has \\\"r_frame_rate\\\" value set to \\\"0/0\\\".\")\n        return \"Unknown\"\n\n    items = str_value.split(\"/\")\n    if len(items) == 1:\n        fps = float(items[0])\n\n    elif len(items) == 2:\n        fps = float(items[0]) / float(items[1])\n\n    # Check if fps is integer or float number\n    if int(fps) == fps:\n        fps = int(fps)\n\n    return str(fps)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/transcoding.html#client.ayon_core.lib.transcoding.convert_input_paths_for_ffmpeg","title":"<code>convert_input_paths_for_ffmpeg(input_paths, output_dir, logger=None, *, review_layers=None)</code>","text":"<p>Convert source file to format supported in ffmpeg.</p> <p>Can currently convert only EXRs. The input filepaths should be files with same type. Information about input is loaded only from first found file.</p> <p>Filenames of input files are kept so make sure that output directory is not the same directory as input files have. - This way it can handle gaps and can keep input filenames without handling     frame template</p> <p>Parameters:</p> Name Type Description Default <code>input_paths</code> <code>str</code> <p>Paths that should be converted. It is expected that contains single file or image sequence of same type.</p> required <code>output_dir</code> <code>str</code> <p>Path to directory where output will be rendered. Must not be same as input's directory.</p> required <code>logger</code> <code>Logger</code> <p>Logger used for logging.</p> <code>None</code> <code>review_layers</code> <code>Optional[list[str]]</code> <p>List of reviewable layers.</p> <code>None</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If input filepath has extension not supported by function. Currently, only \".exr\" extension is supported.</p> Source code in <code>client/ayon_core/lib/transcoding.py</code> <pre><code>def convert_input_paths_for_ffmpeg(\n    input_paths: str,\n    output_dir: str,\n    # TODO move 'review_layers' before logger\n    logger: logging.Logger = None,\n    *,\n    review_layers: Optional[list[str]] = None,\n) -&gt; None:\n    \"\"\"Convert source file to format supported in ffmpeg.\n\n    Can currently convert only EXRs. The input filepaths should be files\n    with same type. Information about input is loaded only from first found\n    file.\n\n    Filenames of input files are kept so make sure that output directory\n    is not the same directory as input files have.\n    - This way it can handle gaps and can keep input filenames without handling\n        frame template\n\n    Args:\n        input_paths (str): Paths that should be converted. It is expected that\n            contains single file or image sequence of same type.\n        output_dir (str): Path to directory where output will be rendered.\n            Must not be same as input's directory.\n        logger (logging.Logger): Logger used for logging.\n        review_layers (Optional[list[str]]): List of reviewable layers.\n\n    Raises:\n        ValueError: If input filepath has extension not supported by function.\n            Currently, only \".exr\" extension is supported.\n    \"\"\"\n    if logger is None:\n        logger = logging.getLogger(__name__)\n\n    first_input_path = input_paths[0]\n    ext = os.path.splitext(first_input_path)[1].lower()\n\n    if ext != \".exr\":\n        raise ValueError(\n            \"Function 'convert_input_paths_for_ffmpeg' currently supports\"\n            f\" only \\\".exr\\\" extension. Got \\\"{ext}\\\".\"\n        )\n\n    input_info = get_oiio_info_for_input(first_input_path, logger=logger)\n\n    # Change compression only if source compression is \"dwaa\" or \"dwab\"\n    #   - they're not supported in ffmpeg\n    compression = input_info[\"attribs\"].get(\"compression\")\n    if compression in (\"dwaa\", \"dwab\"):\n        compression = \"none\"\n\n    # Collect channels to export\n    input_arg, channels_arg = get_oiio_input_and_channel_args(\n        input_info, review_layers=review_layers\n    )\n\n    # Find which attributes to strip\n    erase_attributes: list[str] = _get_attributes_to_erase(\n        input_info, logger=logger\n    )\n\n    # clique.PATTERNS[\"frames\"] supports only `.1001.exr` not `_1001.exr` so\n    # we use a customized pattern.\n    pattern = \"[_.](?P&lt;index&gt;(?P&lt;padding&gt;0*)\\\\d+)\\\\.\\\\D+\\\\d?$\"\n    input_collections, input_remainder = clique.assemble(\n        input_paths,\n        patterns=[pattern],\n        assume_padded_when_ambiguous=True,\n    )\n    input_items = list(input_collections)\n    input_items.extend(input_remainder)\n    for input_item in input_items:\n        # Prepare subprocess arguments\n        oiio_cmd = get_oiio_tool_args(\n            \"oiiotool\",\n            # Don't add any additional attributes\n            \"--nosoftwareattrib\",\n        )\n        # Add input compression if available\n        if compression:\n            oiio_cmd.extend([\"--compression\", compression])\n\n        # Convert a sequence of files using a single oiiotool command\n        # using its sequence syntax\n        if isinstance(input_item, clique.Collection):\n            frames = input_item.format(\"{ranges}\")\n            oiio_cmd.extend([\n                \"--framepadding\", str(input_item.padding),\n                \"--frames\", frames,\n                \"--parallel-frames\"\n            ])\n            input_item: str = input_item.format(\"{head}#{tail}\")\n        elif not isinstance(input_item, str):\n            raise TypeError(\n                f\"Input is not a string or Collection: {input_item}\"\n            )\n\n        oiio_cmd.extend([\n            input_arg, input_item,\n            # Tell oiiotool which channels should be put to top stack\n            #   (and output)\n            \"--ch\", channels_arg,\n            # Use first subimage\n            \"--subimage\", \"0\"\n        ])\n\n        for attr_name in erase_attributes:\n            oiio_cmd.extend([\"--eraseattrib\", attr_name])\n\n        # Add last argument - path to output\n        base_filename = os.path.basename(input_item)\n        output_path = os.path.join(output_dir, base_filename)\n        oiio_cmd.extend([\n            \"-o\", output_path\n        ])\n\n        logger.debug(\"Conversion command: {}\".format(\" \".join(oiio_cmd)))\n        run_subprocess(oiio_cmd, logger=logger)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/transcoding.html#client.ayon_core.lib.transcoding.convert_value_by_type_name","title":"<code>convert_value_by_type_name(value_type, value, logger=None)</code>","text":"<p>Convert value to proper type based on type name.</p> <p>In some cases value types have custom python class.</p> Source code in <code>client/ayon_core/lib/transcoding.py</code> <pre><code>def convert_value_by_type_name(value_type, value, logger=None):\n    \"\"\"Convert value to proper type based on type name.\n\n    In some cases value types have custom python class.\n    \"\"\"\n    if logger is None:\n        logger = logging.getLogger(__name__)\n\n    # Simple types\n    if value_type == \"string\":\n        return value\n\n    if value_type == \"int\":\n        return int(value)\n\n    if value_type in (\"float\", \"double\"):\n        return float(value)\n\n    # Vectors will probably have more types\n    if value_type in (\"vec2f\", \"float2\", \"float2d\"):\n        return [float(item) for item in value.split(\",\")]\n\n    # Matrix should be always have square size of element 3x3, 4x4\n    # - are returned as list of lists\n    if value_type in (\"matrix\", \"matrixd\"):\n        output = []\n        current_index = -1\n        parts = value.split(\",\")\n        parts_len = len(parts)\n        if parts_len == 1:\n            divisor = 1\n        elif parts_len == 4:\n            divisor = 2\n        elif parts_len == 9:\n            divisor = 3\n        elif parts_len == 16:\n            divisor = 4\n        else:\n            logger.info(\"Unknown matrix resolution {}. Value: \\\"{}\\\"\".format(\n                parts_len, value\n            ))\n            for part in parts:\n                output.append(float(part))\n            return output\n\n        for idx, item in enumerate(parts):\n            list_index = idx % divisor\n            if list_index &gt; current_index:\n                current_index = list_index\n                output.append([])\n            output[list_index].append(float(item))\n        return output\n\n    if value_type == \"rational2i\":\n        return RationalToInt(value)\n\n    if value_type in (\"vector\", \"vectord\"):\n        parts = [part.strip() for part in value.split(\",\")]\n        output = []\n        for part in parts:\n            if part == \"-nan\":\n                output.append(None)\n                continue\n            try:\n                part = float(part)\n            except ValueError:\n                pass\n            output.append(part)\n        return output\n\n    if value_type == \"timecode\":\n        return value\n\n    # Array of other types is converted to list\n    re_result = ARRAY_TYPE_REGEX.findall(value_type)\n    if re_result:\n        array_type = re_result[0]\n        output = []\n        for item in value.split(\",\"):\n            output.append(\n                convert_value_by_type_name(array_type, item, logger=logger)\n            )\n        return output\n\n    logger.debug((\n        \"Dev note (missing implementation):\"\n        \" Unknown attrib type \\\"{}\\\". Value: {}\"\n    ).format(value_type, value))\n    return value\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/transcoding.html#client.ayon_core.lib.transcoding.deprecated","title":"<code>deprecated(new_destination)</code>","text":"<p>Mark functions as deprecated.</p> <p>It will result in a warning being emitted when the function is used.</p> Source code in <code>client/ayon_core/lib/transcoding.py</code> <pre><code>def deprecated(new_destination):\n    \"\"\"Mark functions as deprecated.\n\n    It will result in a warning being emitted when the function is used.\n    \"\"\"\n\n    func = None\n    if callable(new_destination):\n        func = new_destination\n        new_destination = None\n\n    def _decorator(decorated_func):\n        if new_destination is None:\n            warning_message = (\n                \" Please check content of deprecated function to figure out\"\n                \" possible replacement.\"\n            )\n        else:\n            warning_message = \" Please replace your usage with '{}'.\".format(\n                new_destination\n            )\n\n        @functools.wraps(decorated_func)\n        def wrapper(*args, **kwargs):\n            warnings.simplefilter(\"always\", DeprecationWarning)\n            warnings.warn(\n                (\n                    \"Call to deprecated function '{}'\"\n                    \"\\nFunction was moved or removed.{}\"\n                ).format(decorated_func.__name__, warning_message),\n                category=DeprecationWarning,\n                stacklevel=4\n            )\n            return decorated_func(*args, **kwargs)\n        return wrapper\n\n    if func is None:\n        return _decorator\n    return _decorator(func)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/transcoding.html#client.ayon_core.lib.transcoding.get_convert_rgb_channels","title":"<code>get_convert_rgb_channels(channel_names, *, review_layers=None)</code>","text":"<p>Get first available RGB(A) group from channels info.</p>"},{"location":"autoapi/client/ayon_core/lib/transcoding.html#client.ayon_core.lib.transcoding.get_convert_rgb_channels--examples","title":"Examples","text":"<pre><code># Ideal situation\nchannels_info: [\n    \"R\", \"G\", \"B\", \"A\"\n]\n</code></pre> <p>Result will be <code>(\"R\", \"G\", \"B\", \"A\")</code></p> <pre><code># Not ideal situation\nchannels_info: [\n    \"beauty.red\",\n    \"beauty.green\",\n    \"beauty.blue\",\n    \"depth.Z\"\n]\n</code></pre> <p>Result will be <code>(\"beauty.red\", \"beauty.green\", \"beauty.blue\", None)</code></p> <p>Parameters:</p> Name Type Description Default <code>channel_names</code> <code>list[str]</code> <p>List of channel names.</p> required <code>review_layers</code> <code>Optional[list[str]]</code> <p>List of reviewable layers.</p> <code>None</code> <p>Returns:</p> Type Description <code>Optional[tuple[str, str, str, Optional[str]]]</code> <p>Union[NoneType, tuple[str, str, str, Union[str, None]]]: Tuple of 4 channel names defying channel names for R, G, B, A or None if there is not any layer with RGB combination.</p> Source code in <code>client/ayon_core/lib/transcoding.py</code> <pre><code>def get_convert_rgb_channels(\n    channel_names: list[str],\n    *,\n    review_layers: Optional[list[str]] = None,\n) -&gt; Optional[tuple[str, str, str, Optional[str]]]:\n    \"\"\"Get first available RGB(A) group from channels info.\n\n    ## Examples\n    ```\n    # Ideal situation\n    channels_info: [\n        \"R\", \"G\", \"B\", \"A\"\n    ]\n    ```\n    Result will be `(\"R\", \"G\", \"B\", \"A\")`\n\n    ```\n    # Not ideal situation\n    channels_info: [\n        \"beauty.red\",\n        \"beauty.green\",\n        \"beauty.blue\",\n        \"depth.Z\"\n    ]\n    ```\n    Result will be `(\"beauty.red\", \"beauty.green\", \"beauty.blue\", None)`\n\n    Args:\n        channel_names (list[str]): List of channel names.\n        review_layers (Optional[list[str]]): List of reviewable layers.\n\n    Returns:\n        Union[NoneType, tuple[str, str, str, Union[str, None]]]: Tuple of\n            4 channel names defying channel names for R, G, B, A or None\n            if there is not any layer with RGB combination.\n    \"\"\"\n    channels_info = get_review_info_by_layer_name(\n        channel_names, review_layers=review_layers\n    )\n    for item in channels_info:\n        review_channels = item[\"review_channels\"]\n        return (\n            review_channels[\"R\"],\n            review_channels[\"G\"],\n            review_channels[\"B\"],\n            review_channels[\"A\"]\n        )\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/transcoding.html#client.ayon_core.lib.transcoding.get_ffmpeg_codec_args","title":"<code>get_ffmpeg_codec_args(ffprobe_data, source_ffmpeg_cmd=None, logger=None)</code>","text":"<p>Copy codec from input metadata for output.</p> <p>Parameters:</p> Name Type Description Default <code>ffprobe_data</code> <code>dict</code> <p>Data received from ffprobe.</p> required <code>source_ffmpeg_cmd</code> <code>str</code> <p>Command that created input if available.</p> <code>None</code> Source code in <code>client/ayon_core/lib/transcoding.py</code> <pre><code>def get_ffmpeg_codec_args(ffprobe_data, source_ffmpeg_cmd=None, logger=None):\n    \"\"\"Copy codec from input metadata for output.\n\n    Args:\n        ffprobe_data(dict): Data received from ffprobe.\n        source_ffmpeg_cmd(str): Command that created input if available.\n    \"\"\"\n    if logger is None:\n        logger = logging.getLogger(__name__)\n\n    video_stream = None\n    no_audio_stream = None\n    for stream in ffprobe_data[\"streams\"]:\n        codec_type = stream[\"codec_type\"]\n        if codec_type == \"video\":\n            video_stream = stream\n            break\n        elif no_audio_stream is None and codec_type != \"audio\":\n            no_audio_stream = stream\n\n    if video_stream is None:\n        if no_audio_stream is None:\n            logger.warning(\n                \"Couldn't find stream that is not an audio file.\"\n            )\n            return []\n        logger.info(\n            \"Didn't find video stream. Using first non audio stream.\"\n        )\n        video_stream = no_audio_stream\n\n    codec_name = video_stream.get(\"codec_name\")\n    # Codec \"prores\"\n    if codec_name == \"prores\":\n        return _ffmpeg_prores_codec_args(video_stream, source_ffmpeg_cmd)\n\n    # Codec \"h264\"\n    if codec_name == \"h264\":\n        return _ffmpeg_h264_codec_args(video_stream, source_ffmpeg_cmd)\n\n    # Coded DNxHD\n    if codec_name == \"dnxhd\":\n        return _ffmpeg_dnxhd_codec_args(video_stream, source_ffmpeg_cmd)\n\n    output = []\n    if codec_name:\n        output.extend([\"-codec:v\", codec_name])\n\n    bit_rate = video_stream.get(\"bit_rate\")\n    if bit_rate:\n        output.extend([\"-b:v\", bit_rate])\n\n    pix_fmt = video_stream.get(\"pix_fmt\")\n    if pix_fmt:\n        output.extend([\"-pix_fmt\", pix_fmt])\n\n    output.extend([\"-g\", \"1\"])\n\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/transcoding.html#client.ayon_core.lib.transcoding.get_ffmpeg_format_args","title":"<code>get_ffmpeg_format_args(ffprobe_data, source_ffmpeg_cmd=None)</code>","text":"<p>Copy format from input metadata for output.</p> <p>Parameters:</p> Name Type Description Default <code>ffprobe_data</code> <code>dict</code> <p>Data received from ffprobe.</p> required <code>source_ffmpeg_cmd</code> <code>str</code> <p>Command that created input if available.</p> <code>None</code> Source code in <code>client/ayon_core/lib/transcoding.py</code> <pre><code>def get_ffmpeg_format_args(ffprobe_data, source_ffmpeg_cmd=None):\n    \"\"\"Copy format from input metadata for output.\n\n    Args:\n        ffprobe_data(dict): Data received from ffprobe.\n        source_ffmpeg_cmd(str): Command that created input if available.\n    \"\"\"\n    input_format = ffprobe_data.get(\"format\") or {}\n    if input_format.get(\"format_name\") == \"mxf\":\n        return _ffmpeg_mxf_format_args(ffprobe_data, source_ffmpeg_cmd)\n    return []\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/transcoding.html#client.ayon_core.lib.transcoding.get_ffprobe_data","title":"<code>get_ffprobe_data(path_to_file, logger=None)</code>","text":"<p>Load data about entered filepath via ffprobe.</p> <p>Parameters:</p> Name Type Description Default <code>path_to_file</code> <code>str</code> <p>absolute path</p> required <code>logger</code> <code>Logger</code> <p>injected logger, if empty new is created</p> <code>None</code> Source code in <code>client/ayon_core/lib/transcoding.py</code> <pre><code>def get_ffprobe_data(path_to_file, logger=None):\n    \"\"\"Load data about entered filepath via ffprobe.\n\n    Args:\n        path_to_file (str): absolute path\n        logger (logging.Logger): injected logger, if empty new is created\n    \"\"\"\n    if not logger:\n        logger = logging.getLogger(__name__)\n    logger.debug(\n        \"Getting information about input \\\"{}\\\".\".format(path_to_file)\n    )\n    ffprobe_args = get_ffmpeg_tool_args(\"ffprobe\")\n    args = ffprobe_args + [\n        \"-hide_banner\",\n        \"-loglevel\", \"fatal\",\n        \"-show_error\",\n        \"-show_format\",\n        \"-show_streams\",\n        \"-show_programs\",\n        \"-show_chapters\",\n        \"-show_private_data\",\n        \"-print_format\", \"json\",\n        path_to_file\n    ]\n\n    logger.debug(\"FFprobe command: {}\".format(\n        subprocess.list2cmdline(args)\n    ))\n    kwargs = {\n        \"stdout\": subprocess.PIPE,\n        \"stderr\": subprocess.PIPE,\n    }\n    if platform.system().lower() == \"windows\":\n        kwargs[\"creationflags\"] = (\n            subprocess.CREATE_NEW_PROCESS_GROUP\n            | getattr(subprocess, \"DETACHED_PROCESS\", 0)\n            | getattr(subprocess, \"CREATE_NO_WINDOW\", 0)\n        )\n\n    popen = subprocess.Popen(args, **kwargs)\n\n    popen_stdout, popen_stderr = popen.communicate()\n    if popen_stdout:\n        logger.debug(\"FFprobe stdout:\\n{}\".format(\n            popen_stdout.decode(\"utf-8\")\n        ))\n\n    if popen_stderr:\n        logger.warning(\"FFprobe stderr:\\n{}\".format(\n            popen_stderr.decode(\"utf-8\")\n        ))\n\n    return json.loads(popen_stdout)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/transcoding.html#client.ayon_core.lib.transcoding.get_ffprobe_streams","title":"<code>get_ffprobe_streams(path_to_file, logger=None)</code>","text":"<p>Load streams from entered filepath via ffprobe.</p> <p>Parameters:</p> Name Type Description Default <code>path_to_file</code> <code>str</code> <p>absolute path</p> required <code>logger</code> <code>Logger</code> <p>injected logger, if empty new is created</p> <code>None</code> Source code in <code>client/ayon_core/lib/transcoding.py</code> <pre><code>def get_ffprobe_streams(path_to_file, logger=None):\n    \"\"\"Load streams from entered filepath via ffprobe.\n\n    Args:\n        path_to_file (str): absolute path\n        logger (logging.Logger): injected logger, if empty new is created\n    \"\"\"\n    return get_ffprobe_data(path_to_file, logger)[\"streams\"]\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/transcoding.html#client.ayon_core.lib.transcoding.get_media_mime_type","title":"<code>get_media_mime_type(filepath)</code>","text":"<p>Determine Mime-Type of a file.</p> <p>Parameters:</p> Name Type Description Default <code>filepath</code> <code>str</code> <p>Path to file.</p> required <p>Returns:</p> Type Description <code>Optional[str]</code> <p>Optional[str]: Mime type or None if is unknown mime type.</p> Source code in <code>client/ayon_core/lib/transcoding.py</code> <pre><code>def get_media_mime_type(filepath: str) -&gt; Optional[str]:\n    \"\"\"Determine Mime-Type of a file.\n\n    Args:\n        filepath (str): Path to file.\n\n    Returns:\n        Optional[str]: Mime type or None if is unknown mime type.\n\n    \"\"\"\n    # The implementation is identical or better with ayon_api &gt;=1.1.0,\n    #   which is used in AYON launcher &gt;=1.3.0.\n    # NOTE Remove safe import when AYON launcher &gt;=1.2.0.\n    try:\n        from ayon_api.utils import (\n            get_media_mime_type_for_content as _ayon_api_func\n        )\n    except ImportError:\n        _ayon_api_func = None\n\n    if not filepath or not os.path.exists(filepath):\n        return None\n\n    with open(filepath, \"rb\") as stream:\n        content = stream.read()\n\n    if _ayon_api_func is not None:\n        mime_type = _ayon_api_func(content)\n        if mime_type is not None:\n            return mime_type\n\n    content_len = len(content)\n    # Pre-validation (largest definition check)\n    # - hopefully there cannot be media defined in less than 12 bytes\n    if content_len &lt; 12:\n        return None\n\n    # FTYP\n    if content[4:8] == b\"ftyp\":\n        return _get_media_mime_type_from_ftyp(content)\n\n    # BMP\n    if content[0:2] == b\"BM\":\n        return \"image/bmp\"\n\n    # Tiff\n    if content[0:2] in (b\"MM\", b\"II\"):\n        return \"tiff\"\n\n    # PNG\n    if content[0:4] == b\"\\211PNG\":\n        return \"image/png\"\n\n    # SVG\n    if b'xmlns=\"http://www.w3.org/2000/svg\"' in content:\n        return \"image/svg+xml\"\n\n    # JPEG\n    # - [0:2] is constant b\"\\xff\\xd8\"\n    #   (ref. https://www.file-recovery.com/jpg-signature-format.htm)\n    # - [2:4] Marker identifier b\"\\xff{?}\"\n    #   (ref. https://www.disktuna.com/list-of-jpeg-markers/)\n    # NOTE: File ends with b\"\\xff\\xd9\"\n    if content[0:3] == b\"\\xff\\xd8\\xff\":\n        return \"image/jpeg\"\n\n    # Webp\n    if content[0:4] == b\"RIFF\" and content[8:12] == b\"WEBP\":\n        return \"image/webp\"\n\n    # Gif\n    if content[0:6] in (b\"GIF87a\", b\"GIF89a\"):\n        return \"gif\"\n\n    # Adobe PhotoShop file (8B &gt; Adobe, PS &gt; PhotoShop)\n    if content[0:4] == b\"8BPS\":\n        return \"image/vnd.adobe.photoshop\"\n\n    # Windows ICO &gt; this might be wild guess as multiple files can start\n    #   with this header\n    if content[0:4] == b\"\\x00\\x00\\x01\\x00\":\n        return \"image/x-icon\"\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/transcoding.html#client.ayon_core.lib.transcoding.get_oiio_info_for_input","title":"<code>get_oiio_info_for_input(filepath, *, subimages=False, verbose=True, logger=None)</code>","text":"<p>Call oiiotool to get information about input and return stdout.</p> <p>Parameters:</p> Name Type Description Default <code>filepath</code> <code>str</code> <p>Path to file.</p> required <code>subimages</code> <code>bool</code> <p>include info about subimages in the output.</p> <code>False</code> <code>verbose</code> <code>bool</code> <p>get the full metadata about each input image.</p> <code>True</code> <code>logger</code> <code>Logger</code> <p>Logger used for logging.</p> <code>None</code> <p>Stdout should contain xml format string.</p> Source code in <code>client/ayon_core/lib/transcoding.py</code> <pre><code>def get_oiio_info_for_input(\n    filepath: str,\n    *,\n    subimages: bool = False,\n    verbose: bool = True,\n    logger: logging.Logger = None,\n):\n    \"\"\"Call oiiotool to get information about input and return stdout.\n\n    Args:\n        filepath (str): Path to file.\n        subimages (bool): include info about subimages in the output.\n        verbose (bool): get the full metadata about each input image.\n        logger (logging.Logger): Logger used for logging.\n\n    Stdout should contain xml format string.\n    \"\"\"\n    args = get_oiio_tool_args(\n        \"oiiotool\",\n        \"--info\",\n    )\n    if verbose:\n        args.append(\"-v\")\n    if subimages:\n        args.append(\"-a\")\n\n    args.extend([\"-i:infoformat=xml\", filepath])\n\n    output = run_subprocess(args, logger=logger)\n    output = output.replace(\"\\r\\n\", \"\\n\")\n\n    xml_started = False\n    subimages_lines = []\n    lines = []\n    for line in output.split(\"\\n\"):\n        if not xml_started:\n            if not line.startswith(\"&lt;\"):\n                continue\n            xml_started = True\n\n        if xml_started:\n            lines.append(line)\n            if line == \"&lt;/ImageSpec&gt;\":\n                subimages_lines.append(lines)\n                lines = []\n                xml_started = False\n\n    if not subimages_lines:\n        raise ValueError(\n            \"Failed to read input file \\\"{}\\\".\\nOutput:\\n{}\".format(\n                filepath, output\n            )\n        )\n\n    output = []\n    for subimage_lines in subimages_lines:\n        xml_text = \"\\n\".join(subimage_lines)\n        output.append(parse_oiio_xml_output(xml_text, logger=logger))\n\n    if subimages:\n        return output\n    return output[0]\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/transcoding.html#client.ayon_core.lib.transcoding.get_oiio_input_and_channel_args","title":"<code>get_oiio_input_and_channel_args(oiio_input_info, alpha_default=None, *, review_layers=None)</code>","text":"<p>Get input and channel arguments for oiiotool. Args:     oiio_input_info (dict): Information about input from oiio tool.         Should be output of function 'get_oiio_info_for_input' (can be         called with 'verbose=False').     alpha_default (float, optional): Default value for alpha channel.     review_layers (Optional[list[str]], optional): List of reviewable         layers.</p> <p>Returns:</p> Type Description <code>tuple[str, str]</code> <p>tuple[str, str]: Tuple of input and channel arguments.</p> Source code in <code>client/ayon_core/lib/transcoding.py</code> <pre><code>def get_oiio_input_and_channel_args(\n    oiio_input_info: dict,\n    alpha_default: float = None,\n    *,\n    review_layers: Optional[list[str]] = None,\n) -&gt; tuple[str, str]:\n    \"\"\"Get input and channel arguments for oiiotool.\n    Args:\n        oiio_input_info (dict): Information about input from oiio tool.\n            Should be output of function 'get_oiio_info_for_input' (can be\n            called with 'verbose=False').\n        alpha_default (float, optional): Default value for alpha channel.\n        review_layers (Optional[list[str]], optional): List of reviewable\n            layers.\n\n    Returns:\n        tuple[str, str]: Tuple of input and channel arguments.\n\n    \"\"\"\n    channel_names = oiio_input_info[\"channelnames\"]\n    review_channels = get_convert_rgb_channels(\n        channel_names,\n        review_layers=review_layers\n    )\n\n    if review_channels is None:\n        raise MissingRGBAChannelsError(\n            \"Couldn't find channels that can be used for conversion \"\n            f\"among channels: {channel_names}.\"\n        )\n\n    red, green, blue, alpha = review_channels\n    input_channels = [red, green, blue]\n\n    channels_arg = \"R={0},G={1},B={2}\".format(red, green, blue)\n    if alpha is not None:\n        channels_arg += \",A={}\".format(alpha)\n        input_channels.append(alpha)\n    elif alpha_default:\n        channels_arg += \",A={}\".format(float(alpha_default))\n        input_channels.append(\"A\")\n\n    # Make sure channels are unique, but preserve order to avoid oiiotool crash\n    input_channels_str = \",\".join(list(dict.fromkeys(input_channels)))\n\n    subimages = oiio_input_info.get(\"subimages\")\n    input_arg = \"-i\"\n    if subimages is None or subimages == 1:\n        # Tell oiiotool which channels should be loaded\n        # - other channels are not loaded to memory so helps to avoid memory\n        #       leak issues\n        # - this option is crashing if used on multipart exrs\n        input_arg += \":ch={}\".format(input_channels_str)\n\n    return input_arg, channels_arg\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/transcoding.html#client.ayon_core.lib.transcoding.get_rescaled_command_arguments","title":"<code>get_rescaled_command_arguments(application, input_path, target_width, target_height, *, target_par=None, bg_color=None, review_layers=None, log=None)</code>","text":"<p>Get command arguments for rescaling input to target size.</p> <p>Parameters:</p> Name Type Description Default <code>application</code> <code>str</code> <p>Application for which command should be created. Currently supported are \"ffmpeg\" and \"oiiotool\".</p> required <code>input_path</code> <code>str</code> <p>Path to input file.</p> required <code>target_width</code> <code>int</code> <p>Width of target.</p> required <code>target_height</code> <code>int</code> <p>Height of target.</p> required <code>target_par</code> <code>Optional[float]</code> <p>Pixel aspect ratio of target.</p> <code>None</code> <code>bg_color</code> <code>Optional[list[int]]</code> <p>List of 8bit int values for background color. Should be in range 0 - 255.</p> <code>None</code> <code>log</code> <code>Optional[Logger]</code> <p>Logger used for logging.</p> <code>None</code> <code>review_layers</code> <code>Optional[list[str]]</code> <p>List of reviewable layers.</p> <code>None</code> <p>Returns:</p> Type Description <code>list[str]</code> <p>list[str]: List of command arguments.</p> Source code in <code>client/ayon_core/lib/transcoding.py</code> <pre><code>def get_rescaled_command_arguments(\n    application: str,\n    input_path: str,\n    target_width: int,\n    target_height: int,\n    *,\n    target_par: float = None,\n    bg_color: Optional[list[int]] = None,\n    review_layers: Optional[list[str]] = None,\n    log: Optional[logging.Logger] = None,\n) -&gt; list[str]:\n    \"\"\"Get command arguments for rescaling input to target size.\n\n    Args:\n        application (str): Application for which command should be created.\n            Currently supported are \"ffmpeg\" and \"oiiotool\".\n        input_path (str): Path to input file.\n        target_width (int): Width of target.\n        target_height (int): Height of target.\n        target_par (Optional[float]): Pixel aspect ratio of target.\n        bg_color (Optional[list[int]]): List of 8bit int values for\n            background color. Should be in range 0 - 255.\n        log (Optional[logging.Logger]): Logger used for logging.\n        review_layers (Optional[list[str]]): List of reviewable layers.\n\n    Returns:\n        list[str]: List of command arguments.\n    \"\"\"\n    command_args = []\n    target_par = target_par or 1.0\n    input_par = 1.0\n\n    input_height, input_width, stream_input_par = _get_image_dimensions(\n        application, input_path, log)\n    if stream_input_par:\n        input_par = (\n            float(stream_input_par.split(\":\")[0])\n            / float(stream_input_par.split(\":\")[1])\n        )\n    # recalculating input and target width\n    input_width = int(input_width * input_par)\n    target_width = int(target_width * target_par)\n\n    # calculate aspect ratios\n    target_aspect = float(target_width) / target_height\n    input_aspect = float(input_width) / input_height\n\n    # calculate scale size\n    scale_size = float(input_width) / target_width\n    if input_aspect &lt; target_aspect:\n        scale_size = float(input_height) / target_height\n\n    # calculate rescaled width and height\n    rescaled_width = int(input_width / scale_size)\n    rescaled_height = int(input_height / scale_size)\n\n    # calculate width and height shift\n    rescaled_width_shift = int((target_width - rescaled_width) / 2)\n    rescaled_height_shift = int((target_height - rescaled_height) / 2)\n\n    if application == \"ffmpeg\":\n        # create scale command\n        scale = \"scale={0}:{1}\".format(input_width, input_height)\n        pad = \"pad={0}:{1}:({2}-iw)/2:({3}-ih)/2\".format(\n            target_width,\n            target_height,\n            target_width,\n            target_height\n        )\n        if input_width &gt; target_width or input_height &gt; target_height:\n            scale = \"scale={0}:{1}\".format(rescaled_width, rescaled_height)\n            pad = \"pad={0}:{1}:{2}:{3}\".format(\n                target_width,\n                target_height,\n                rescaled_width_shift,\n                rescaled_height_shift\n            )\n\n        if bg_color:\n            color = convert_color_values(application, bg_color)\n            pad += \":{0}\".format(color)\n        command_args.extend([\"-vf\", \"{0},{1}\".format(scale, pad)])\n\n    elif application == \"oiiotool\":\n        input_info = get_oiio_info_for_input(\n            input_path,\n            verbose=False,\n            logger=log,\n        )\n        # Collect channels to export\n        _, channels_arg = get_oiio_input_and_channel_args(\n            input_info, alpha_default=1.0, review_layers=review_layers\n        )\n\n        command_args.extend([\n            # Tell oiiotool which channels should be put to top stack\n            #   (and output)\n            \"--ch\", channels_arg,\n            # Use first subimage\n            \"--subimage\", \"0\"\n        ])\n\n        if input_par != 1.0:\n            command_args.extend([\"--pixelaspect\", \"1\"])\n\n        width_shift = int((target_width - input_width) / 2)\n        height_shift = int((target_height - input_height) / 2)\n\n        # default resample is not scaling source image\n        resample = [\n            \"--resize\",\n            \"{0}x{1}\".format(input_width, input_height),\n            \"--origin\",\n            \"+{0}+{1}\".format(width_shift, height_shift),\n        ]\n        # scaled source image to target size\n        if input_width &gt; target_width or input_height &gt; target_height:\n            # form resample command\n            resample = [\n                \"--resize:filter=lanczos3\",\n                \"{0}x{1}\".format(rescaled_width, rescaled_height),\n                \"--origin\",\n                \"+{0}+{1}\".format(rescaled_width_shift, rescaled_height_shift),\n            ]\n        command_args.extend(resample)\n\n        fullsize = [\n            \"--fullsize\",\n            \"{0}x{1}\".format(target_width, target_height)\n        ]\n        if bg_color:\n            color = convert_color_values(application, bg_color)\n\n            fullsize.extend([\n                \"--pattern\",\n                \"constant:color={0}\".format(color),\n                \"{0}x{1}\".format(target_width, target_height),\n                \"4\",  # 4 channels\n                \"--over\"\n            ])\n        command_args.extend(fullsize)\n\n    else:\n        raise ValueError(\n            \"\\\"application\\\" input argument should \"\n            \"be either \\\"ffmpeg\\\" or \\\"oiiotool\\\"\"\n        )\n\n    return command_args\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/transcoding.html#client.ayon_core.lib.transcoding.get_review_info_by_layer_name","title":"<code>get_review_info_by_layer_name(channel_names, *, review_layers=None)</code>","text":"<p>Get channels info grouped by layer name.</p> <p>Finds all layers in channel names and returns list of dictionaries with information about channels in layer. Example output (not real world example):     [         {             \"name\": \"Main\",             \"review_channels\": {                 \"R\": \"Main.red\",                 \"G\": \"Main.green\",                 \"B\": \"Main.blue\",                 \"A\": None,             }         },         {             \"name\": \"Composed\",             \"review_channels\": {                 \"R\": \"Composed.R\",                 \"G\": \"Composed.G\",                 \"B\": \"Composed.B\",                 \"A\": \"Composed.A\",             }         },         ...     ]</p> <p>This tries to find suitable outputs good for review purposes, by searching for channel names like RGBA, but also XYZ, Z, N, AR, AG, AB channels.</p> <p>Parameters:</p> Name Type Description Default <code>channel_names</code> <code>list[str]</code> <p>List of channel names.</p> required <code>review_layers</code> <code>Optional[list[str]]</code> <p>List of reviewable layers.</p> <code>None</code> <p>Returns:</p> Type Description <code>list[dict]</code> <p>list[dict]: List of channels information.</p> Source code in <code>client/ayon_core/lib/transcoding.py</code> <pre><code>def get_review_info_by_layer_name(\n    channel_names: list[str],\n    *,\n    review_layers: Optional[list[str]] = None\n) -&gt; list[dict]:\n    \"\"\"Get channels info grouped by layer name.\n\n    Finds all layers in channel names and returns list of dictionaries with\n    information about channels in layer.\n    Example output (not real world example):\n        [\n            {\n                \"name\": \"Main\",\n                \"review_channels\": {\n                    \"R\": \"Main.red\",\n                    \"G\": \"Main.green\",\n                    \"B\": \"Main.blue\",\n                    \"A\": None,\n                }\n            },\n            {\n                \"name\": \"Composed\",\n                \"review_channels\": {\n                    \"R\": \"Composed.R\",\n                    \"G\": \"Composed.G\",\n                    \"B\": \"Composed.B\",\n                    \"A\": \"Composed.A\",\n                }\n            },\n            ...\n        ]\n\n    This tries to find suitable outputs good for review purposes, by\n    searching for channel names like RGBA, but also XYZ, Z, N, AR, AG, AB\n    channels.\n\n    Args:\n        channel_names (list[str]): List of channel names.\n        review_layers (Optional[list[str]]): List of reviewable layers.\n\n    Returns:\n        list[dict]: List of channels information.\n    \"\"\"\n\n    layer_names_order = []\n    channels_by_layer_name = collections.defaultdict(dict)\n    for channel_name in channel_names:\n        layer_name = \"\"\n        last_part = channel_name\n        if \".\" in channel_name:\n            layer_name, last_part = channel_name.rsplit(\".\", 1)\n\n        # R, G, B, A or X, Y, Z, N, AR, AG, AB, RED, GREEN, BLUE, ALPHA\n        channel = last_part.upper()\n        if channel not in {\n            # Detect RGBA channels\n            \"R\", \"G\", \"B\", \"A\",\n            # Support fully written out rgba channel names\n            \"RED\", \"GREEN\", \"BLUE\", \"ALPHA\",\n            # Allow detecting of x, y and z channels, and normal channels\n            \"X\", \"Y\", \"Z\", \"N\",\n            # red, green and blue alpha/opacity, for colored mattes\n            \"AR\", \"AG\", \"AB\"\n        }:\n            continue\n\n        if layer_name not in layer_names_order:\n            layer_names_order.append(layer_name)\n\n        channels_by_layer_name[layer_name][channel] = channel_name\n\n    if review_layers is None:\n        review_layers = []\n\n    def _sort(_layer_name: str) -&gt; int:\n        # Put empty layer or 'rgba' to the beginning of the list\n        # - if input has R, G, B, A channels they should be used for review\n        for idx, layer in enumerate(review_layers):\n            if re.match(layer, _layer_name):\n                return idx - len(review_layers)\n\n        if _layer_name == \"rgba\":\n            return 0\n\n        if _layer_name == \"\":\n            return 1\n\n        channels = channels_by_layer_name[_layer_name]\n        if all(channel in channels for channel in \"RGB\"):\n            return 2\n        return 10\n    layer_names_order.sort(key=_sort)\n\n    output = []\n    for layer_name in layer_names_order:\n        channel_info = channels_by_layer_name[layer_name]\n\n        alpha = channel_info.get(\"A\")\n\n        # RGB channels\n        if all(channel in channel_info for channel in \"RGB\"):\n            rgb = \"R\", \"G\", \"B\"\n\n        # RGB channels using fully written out channel names\n        elif all(\n            channel in channel_info\n            for channel in (\"RED\", \"GREEN\", \"BLUE\")\n        ):\n            rgb = \"RED\", \"GREEN\", \"BLUE\"\n            alpha = channel_info.get(\"ALPHA\")\n\n        # XYZ channels (position pass)\n        elif all(channel in channel_info for channel in \"XYZ\"):\n            rgb = \"X\", \"Y\", \"Z\"\n\n        # Colored mattes (as defined in OpenEXR Channel Name standards)\n        elif all(channel in channel_info for channel in (\"AR\", \"AG\", \"AB\")):\n            rgb = \"AR\", \"AG\", \"AB\"\n\n        # Luminance channel (as defined in OpenEXR Channel Name standards)\n        elif \"Y\" in channel_info:\n            rgb = \"Y\", \"Y\", \"Y\"\n\n        # Has only Z channel (Z-depth layer)\n        elif \"Z\" in channel_info:\n            rgb = \"Z\", \"Z\", \"Z\"\n\n        # Has only A channel (Alpha layer)\n        elif \"A\" in channel_info:\n            rgb = \"A\", \"A\", \"A\"\n            alpha = None\n\n        else:\n            # No reviewable channels found\n            continue\n\n        red = channel_info[rgb[0]]\n        green = channel_info[rgb[1]]\n        blue = channel_info[rgb[2]]\n        output.append({\n            \"name\": layer_name,\n            \"review_channels\": {\n                \"R\": red,\n                \"G\": green,\n                \"B\": blue,\n                \"A\": alpha,\n            }\n        })\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/transcoding.html#client.ayon_core.lib.transcoding.get_review_layer_name","title":"<code>get_review_layer_name(src_filepath, *, review_layers=None)</code>","text":"<p>Find layer name that could be used for review.</p> <p>Parameters:</p> Name Type Description Default <code>src_filepath</code> <code>str</code> <p>Path to input file.</p> required <code>review_layers</code> <code>Optional[list[str]]</code> <p>List of reviewable layers.</p> <code>None</code> <p>Returns:</p> Type Description <code>Optional[str]</code> <p>Union[str, None]: Layer name of None.</p> Source code in <code>client/ayon_core/lib/transcoding.py</code> <pre><code>def get_review_layer_name(\n    src_filepath: str,\n    *,\n    review_layers: Optional[list[str]] = None,\n) -&gt; Optional[str]:\n    \"\"\"Find layer name that could be used for review.\n\n    Args:\n        src_filepath (str): Path to input file.\n        review_layers (Optional[list[str]]): List of reviewable layers.\n\n    Returns:\n        Union[str, None]: Layer name of None.\n    \"\"\"\n\n    ext = os.path.splitext(src_filepath)[-1].lower()\n    if ext != \".exr\":\n        return None\n\n    # Load info about file from oiio tool\n    input_info = get_oiio_info_for_input(\n        src_filepath,\n        verbose=False,\n    )\n    if not input_info:\n        return None\n\n    channel_names = input_info[\"channelnames\"]\n    channels_info = get_review_info_by_layer_name(\n        channel_names, review_layers=review_layers\n    )\n    for item in channels_info:\n        # Layer name can be '', when review channels are 'R', 'G', 'B'\n        #   without layer\n        return item[\"name\"] or None\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/transcoding.html#client.ayon_core.lib.transcoding.get_transcode_temp_directory","title":"<code>get_transcode_temp_directory()</code>","text":"<p>Creates temporary folder for transcoding.</p> <p>Its local, in case of farm it is 'local' to the farm machine.</p> <p>Should be much faster, needs to be cleaned up later.</p> Source code in <code>client/ayon_core/lib/transcoding.py</code> <pre><code>def get_transcode_temp_directory():\n    \"\"\"Creates temporary folder for transcoding.\n\n    Its local, in case of farm it is 'local' to the farm machine.\n\n    Should be much faster, needs to be cleaned up later.\n    \"\"\"\n    return os.path.normpath(\n        tempfile.mkdtemp(prefix=\"op_transcoding_\")\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/transcoding.html#client.ayon_core.lib.transcoding.oiio_color_convert","title":"<code>oiio_color_convert(input_path, output_path, config_path, source_colorspace, source_display=None, source_view=None, target_colorspace=None, target_display=None, target_view=None, additional_command_args=None, frames=None, frame_padding=None, parallel_frames=False, logger=None, *, review_layers=None)</code>","text":"<p>Transcode source file to other with colormanagement.</p> <p>Oiiotool also support additional arguments for transcoding. For more information, see the official documentation: https://openimageio.readthedocs.io/en/latest/oiiotool.html</p> <p>Parameters:</p> Name Type Description Default <code>input_path</code> <code>str</code> <p>Path that should be converted. It is expected that contains single file or image sequence of same type (sequence in format 'file.FRAMESTART-FRAMEEND#.ext', see oiio docs, eg <code>big.1-3#.tif</code> or <code>big.1-3%d.ext</code> with <code>frames</code> argument)</p> required <code>output_path</code> <code>str</code> <p>Path to output filename. (must follow format of 'input_path', eg. single file or  sequence in 'file.FRAMESTART-FRAMEEND#.ext', <code>output.1-3#.tif</code>)</p> required <code>config_path</code> <code>str</code> <p>path to OCIO config file</p> required <code>source_colorspace</code> <code>str</code> <p>ocio valid color space of source files</p> required <code>source_display</code> <code>str</code> <p>name for source display-referred reference space (ocio valid). If provided, source_view must also be provided, and source_colorspace will be ignored</p> <code>None</code> <code>source_view</code> <code>str</code> <p>name for source viewer space (ocio valid) If provided, source_display must also be provided, and source_colorspace will be ignored</p> <code>None</code> <code>target_colorspace</code> <code>str</code> <p>ocio valid target color space         if filled, 'view' and 'display' must be empty</p> <code>None</code> <code>target_display</code> <code>str</code> <p>name for target display-referred reference space (ocio valid) both 'view' and 'display' must be filled (if 'target_colorspace')</p> <code>None</code> <code>target_view</code> <code>str</code> <p>name for target viewer space (ocio valid) both 'view' and 'display' must be filled (if 'target_colorspace')</p> <code>None</code> <code>additional_command_args</code> <code>list</code> <p>arguments for oiiotool (like binary depth for .dpx)</p> <code>None</code> <code>frames</code> <code>Optional[str]</code> <p>Complex frame range to process. This requires input path and output path to use frame token placeholder like <code>#</code> or <code>%d</code>, e.g. file.#.exr</p> <code>None</code> <code>frame_padding</code> <code>Optional[int]</code> <p>Frame padding to use for the input and output when using a sequence filepath.</p> <code>None</code> <code>parallel_frames</code> <code>bool</code> <p>If True, process frames in parallel inside the <code>oiiotool</code> process. Only supported in OIIO 2.5.20.0+.</p> <code>False</code> <code>logger</code> <code>Logger</code> <p>Logger used for logging.</p> <code>None</code> <code>review_layers</code> <code>Optional[list[str]]</code> <p>List of reviewable layers.</p> <code>None</code> <p>Raises:     ValueError: if misconfigured</p> Source code in <code>client/ayon_core/lib/transcoding.py</code> <pre><code>def oiio_color_convert(\n    input_path,\n    output_path,\n    config_path,\n    source_colorspace,\n    source_display=None,\n    source_view=None,\n    target_colorspace=None,\n    target_display=None,\n    target_view=None,\n    additional_command_args=None,\n    frames: Optional[str] = None,\n    frame_padding: Optional[int] = None,\n    parallel_frames: bool = False,\n    logger: Optional[logging.Logger] = None,\n    *,\n    review_layers: Optional[list[str]] = None,\n) -&gt; None:\n    \"\"\"Transcode source file to other with colormanagement.\n\n    Oiiotool also support additional arguments for transcoding.\n    For more information, see the official documentation:\n    https://openimageio.readthedocs.io/en/latest/oiiotool.html\n\n    Args:\n        input_path (str): Path that should be converted. It is expected that\n            contains single file or image sequence of same type\n            (sequence in format 'file.FRAMESTART-FRAMEEND#.ext', see oiio docs,\n            eg `big.1-3#.tif` or `big.1-3%d.ext` with `frames` argument)\n        output_path (str): Path to output filename.\n            (must follow format of 'input_path', eg. single file or\n             sequence in 'file.FRAMESTART-FRAMEEND#.ext', `output.1-3#.tif`)\n        config_path (str): path to OCIO config file\n        source_colorspace (str): ocio valid color space of source files\n        source_display (str, optional): name for source display-referred\n            reference space (ocio valid). If provided, source_view must also be\n            provided, and source_colorspace will be ignored\n        source_view (str, optional): name for source viewer space (ocio valid)\n            If provided, source_display must also be provided, and\n            source_colorspace will be ignored\n        target_colorspace (str): ocio valid target color space\n                    if filled, 'view' and 'display' must be empty\n        target_display (str): name for target display-referred reference space\n            (ocio valid) both 'view' and 'display' must be filled (if\n            'target_colorspace')\n        target_view (str): name for target viewer space (ocio valid)\n            both 'view' and 'display' must be filled (if 'target_colorspace')\n        additional_command_args (list): arguments for oiiotool (like binary\n            depth for .dpx)\n        frames (Optional[str]): Complex frame range to process. This requires\n            input path and output path to use frame token placeholder like\n            `#` or `%d`, e.g. file.#.exr\n        frame_padding (Optional[int]): Frame padding to use for the input and\n            output when using a sequence filepath.\n        parallel_frames (bool): If True, process frames in parallel inside\n            the `oiiotool` process. Only supported in OIIO 2.5.20.0+.\n        logger (logging.Logger): Logger used for logging.\n        review_layers (Optional[list[str]]): List of reviewable layers.\n    Raises:\n        ValueError: if misconfigured\n\n    \"\"\"\n    if logger is None:\n        logger = logging.getLogger(__name__)\n\n    # Get oiioinfo only from first image, otherwise file can't be found\n    first_input_path = input_path\n    if frames:\n        match = re.search(r\"\\d+\", frames)\n        first_frame = match.group() if match else \"0\"\n        first_frame = first_frame.zfill(frame_padding or 0)\n        for token in [\"#\", \"%d\"]:\n            first_input_path = first_input_path.replace(token, first_frame)\n\n    input_info = get_oiio_info_for_input(\n        first_input_path,\n        verbose=False,\n        logger=logger,\n    )\n\n    # Collect channels to export\n    input_arg, channels_arg = get_oiio_input_and_channel_args(\n        input_info, review_layers=review_layers\n    )\n\n    # Prepare subprocess arguments\n    oiio_cmd = get_oiio_tool_args(\n        \"oiiotool\",\n        # Don't add any additional attributes\n        \"--nosoftwareattrib\",\n        \"--colorconfig\", config_path\n    )\n\n    if frames:\n        # If `frames` is specified, then process the input and output\n        # as if it's a sequence of frames (must contain `%04d` as frame\n        # token placeholder in filepaths)\n        oiio_cmd.extend([\n            \"--frames\", frames,\n        ])\n\n    if frame_padding:\n        oiio_cmd.extend([\n            \"--framepadding\", str(frame_padding),\n        ])\n\n    if parallel_frames:\n        oiio_cmd.append(\"--parallel-frames\")\n\n    oiio_cmd.extend([\n        input_arg, input_path,\n        # Tell oiiotool which channels should be put to top stack\n        #   (and output)\n        \"--ch\", channels_arg\n    ])\n\n    # Validate input parameters\n    if target_colorspace and target_view and target_display:\n        raise ValueError(\n            \"Colorspace and both view and display cannot be set together.\"\n            \"Choose colorspace or screen and display\"\n        )\n\n    if not target_colorspace and not target_view and not target_display:\n        raise ValueError(\n            \"Both view and display must be set if target_colorspace is not \"\n            \"provided.\"\n        )\n\n    if (\n        (source_view and not source_display)\n        or (source_display and not source_view)\n    ):\n        raise ValueError(\n            \"Both source_view and source_display must be provided if using \"\n            \"display/view inputs.\"\n        )\n\n    if source_view and source_display and source_colorspace:\n        logger.warning(\n            \"Both source display/view and source_colorspace provided. \"\n            \"Using source display/view pair and ignoring source_colorspace.\"\n        )\n\n    if additional_command_args:\n        oiio_cmd.extend(additional_command_args)\n\n    # Handle the different conversion cases\n    # Source view and display are known\n    if source_view and source_display:\n        color_convert_args = None\n        ocio_display_args = None\n        if target_colorspace:\n            # This is a two-step conversion process since there's no direct\n            # display/view to colorspace command\n            # This could be a config parameter or determined from OCIO config\n            # Use temporary role space 'scene_linear'\n            color_convert_args = (\"scene_linear\", target_colorspace)\n        elif source_display != target_display or source_view != target_view:\n            # Complete display/view pair conversion\n            # - go through a reference space\n            ocio_display_args = (target_display, target_view)\n        else:\n            logger.debug(\n                \"Source and target display/view pairs are identical.\"\n                \" No color conversion needed.\"\n            )\n\n        if color_convert_args or ocio_display_args:\n            # Invert source display/view so that we can go from there to the\n            # target colorspace or display/view\n            oiio_cmd.extend([\n                \"--ociodisplay:inverse=1:subimages=0\",\n                source_display,\n                source_view,\n            ])\n\n        if color_convert_args:\n            # Use colorconvert for colorspace target\n            oiio_cmd.extend([\n                \"--colorconvert:subimages=0\",\n                *color_convert_args\n            ])\n        elif ocio_display_args:\n            # Use ociodisplay for display/view target\n            oiio_cmd.extend([\n                \"--ociodisplay:subimages=0\",\n                *ocio_display_args\n            ])\n\n    elif target_colorspace:\n        # Standard color space to color space conversion\n        oiio_cmd.extend([\n            \"--colorconvert:subimages=0\",\n            source_colorspace,\n            target_colorspace,\n        ])\n    else:\n        # Standard conversion from colorspace to display/view\n        oiio_cmd.extend([\n            \"--iscolorspace\",\n            source_colorspace,\n            \"--ociodisplay:subimages=0\",\n            target_display,\n            target_view,\n        ])\n\n    oiio_cmd.extend([\"-o\", output_path])\n\n    logger.debug(\"Conversion command: {}\".format(\" \".join(oiio_cmd)))\n    run_subprocess(oiio_cmd, logger=logger)\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/transcoding.html#client.ayon_core.lib.transcoding.parse_oiio_xml_output","title":"<code>parse_oiio_xml_output(xml_string, logger=None)</code>","text":"<p>Parse xml output from OIIO info command.</p> Source code in <code>client/ayon_core/lib/transcoding.py</code> <pre><code>def parse_oiio_xml_output(xml_string, logger=None):\n    \"\"\"Parse xml output from OIIO info command.\"\"\"\n    output = {}\n    if not xml_string:\n        return output\n\n    # Fix values with ampresand (lazy fix)\n    # - oiiotool exports invalid xml which ElementTree can't handle\n    #   e.g. \"&amp;#01;\"\n    # WARNING: this will affect even valid character entities. If you need\n    #   those values correctly, this must take care of valid character ranges.\n    #   See https://github.com/pypeclub/OpenPype/pull/2729\n    matches = XML_CHAR_REF_REGEX_HEX.findall(xml_string)\n    for match in matches:\n        new_value = match.replace(\"&amp;\", \"&amp;amp;\")\n        xml_string = xml_string.replace(match, new_value)\n\n    if logger is None:\n        logger = logging.getLogger(\"OIIO-xml-parse\")\n\n    tree = xml.etree.ElementTree.fromstring(xml_string)\n    attribs = {}\n    output[\"attribs\"] = attribs\n    for child in tree:\n        tag_name = child.tag\n        if tag_name == \"attrib\":\n            attrib_def = child.attrib\n            value = convert_value_by_type_name(\n                attrib_def[\"type\"], child.text, logger=logger\n            )\n\n            attribs[attrib_def[\"name\"]] = value\n            continue\n\n        # Channels are stored as tex on each child\n        if tag_name == \"channelnames\":\n            value = []\n            for channel in child:\n                value.append(channel.text)\n\n        # Convert known integer type tags to int\n        elif tag_name in INT_TAGS:\n            value = int(child.text)\n\n        # Keep value of known string tags\n        elif tag_name in STRING_TAGS:\n            value = child.text\n\n        # Keep value as text for unknown tags\n        # - feel free to add more tags\n        else:\n            value = child.text\n            logger.debug((\n                \"Dev note (missing implementation):\"\n                \" Unknown tag \\\"{}\\\". Value \\\"{}\\\"\"\n            ).format(tag_name, value))\n\n        output[child.tag] = value\n\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/transcoding.html#client.ayon_core.lib.transcoding.should_convert_for_ffmpeg","title":"<code>should_convert_for_ffmpeg(src_filepath, *, review_layers=None)</code>","text":"<p>Find out if input should be converted for ffmpeg.</p> <p>Currently cares only about exr inputs and is based on OpenImageIO.</p> <p>Parameters:</p> Name Type Description Default <code>src_filepath</code> <code>str</code> <p>Path to input file.</p> required <code>review_layers</code> <code>Optional[list[str]]</code> <p>List of reviewable layers.</p> <code>None</code> <p>Returns:</p> Type Description <code>Optional[bool]</code> <p>bool/NoneType: True if should be converted, False if should not and None if can't determine.</p> Source code in <code>client/ayon_core/lib/transcoding.py</code> <pre><code>def should_convert_for_ffmpeg(\n    src_filepath: str,\n    *,\n    review_layers: Optional[list[str]] = None,\n) -&gt; Optional[bool]:\n    \"\"\"Find out if input should be converted for ffmpeg.\n\n    Currently cares only about exr inputs and is based on OpenImageIO.\n\n    Args:\n        src_filepath (str): Path to input file.\n        review_layers (Optional[list[str]]): List of reviewable layers.\n\n    Returns:\n        bool/NoneType: True if should be converted, False if should not and\n            None if can't determine.\n    \"\"\"\n    # Care only about exr at this moment\n    ext = os.path.splitext(src_filepath)[-1].lower()\n    if ext != \".exr\":\n        return False\n\n    # Can't determine if should convert or not without oiio_tool\n    if not is_oiio_supported():\n        return None\n\n    # Load info about file from oiio tool\n    input_info = get_oiio_info_for_input(src_filepath)\n    if not input_info:\n        return None\n\n    subimages = input_info.get(\"subimages\")\n    if subimages is not None and subimages &gt; 1:\n        return True\n\n    # Check compression\n    compression = input_info[\"attribs\"].get(\"compression\")\n    if compression in (\"dwaa\", \"dwab\"):\n        return True\n\n    # Check channels\n    channel_names = input_info[\"channelnames\"]\n    review_channels = get_convert_rgb_channels(\n        channel_names,\n        review_layers=review_layers\n    )\n    if review_channels is None:\n        return None\n\n    for attr_value in input_info[\"attribs\"].values():\n        if not isinstance(attr_value, str):\n            continue\n\n        if len(attr_value) &gt; MAX_FFMPEG_STRING_LEN:\n            return True\n\n        for char in NOT_ALLOWED_FFMPEG_CHARS:\n            if char in attr_value:\n                return True\n    return False\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/vendor_bin_utils.html","title":"vendor_bin_utils","text":""},{"location":"autoapi/client/ayon_core/lib/vendor_bin_utils.html#client.ayon_core.lib.vendor_bin_utils.CachedToolPaths","title":"<code>CachedToolPaths</code>","text":"<p>Cache already used and discovered tools and their executables.</p> <p>Discovering path can take some time and can trigger subprocesses so it's better to cache the paths on first get.</p> Source code in <code>client/ayon_core/lib/vendor_bin_utils.py</code> <pre><code>class CachedToolPaths:\n    \"\"\"Cache already used and discovered tools and their executables.\n\n    Discovering path can take some time and can trigger subprocesses so it's\n    better to cache the paths on first get.\n    \"\"\"\n\n    _cached_paths = {}\n\n    @classmethod\n    def is_tool_cached(cls, tool):\n        return tool in cls._cached_paths\n\n    @classmethod\n    def get_executable_path(cls, tool):\n        return cls._cached_paths.get(tool)\n\n    @classmethod\n    def cache_executable_path(cls, tool, path):\n        cls._cached_paths[tool] = path\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/vendor_bin_utils.html#client.ayon_core.lib.vendor_bin_utils.ToolNotFoundError","title":"<code>ToolNotFoundError</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Raised when tool arguments are not found.</p> Source code in <code>client/ayon_core/lib/vendor_bin_utils.py</code> <pre><code>class ToolNotFoundError(Exception):\n    \"\"\"Raised when tool arguments are not found.\"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/vendor_bin_utils.html#client.ayon_core.lib.vendor_bin_utils.find_executable","title":"<code>find_executable(executable)</code>","text":"<p>Find full path to executable.</p> <p>Also tries additional extensions if passed executable does not contain one.</p> <p>Paths where it is looked for executable is defined by 'PATH' environment variable, 'os.confstr(\"CS_PATH\")' or 'os.defpath'.</p> <p>Parameters:</p> Name Type Description Default <code>executable</code> <code>str</code> <p>Name of executable with or without extension. Can be path to file.</p> required <p>Returns:</p> Type Description <p>Union[str, None]: Full path to executable with extension which was found otherwise None.</p> Source code in <code>client/ayon_core/lib/vendor_bin_utils.py</code> <pre><code>def find_executable(executable):\n    \"\"\"Find full path to executable.\n\n    Also tries additional extensions if passed executable does not contain one.\n\n    Paths where it is looked for executable is defined by 'PATH' environment\n    variable, 'os.confstr(\"CS_PATH\")' or 'os.defpath'.\n\n    Args:\n        executable(str): Name of executable with or without extension. Can be\n            path to file.\n\n    Returns:\n        Union[str, None]: Full path to executable with extension which was\n            found otherwise None.\n    \"\"\"\n\n    # Skip if passed path is file\n    if is_file_executable(executable):\n        return executable\n\n    low_platform = platform.system().lower()\n    _, ext = os.path.splitext(executable)\n\n    # Prepare extensions to check\n    exts = set()\n    if ext:\n        exts.add(ext.lower())\n\n    else:\n        # Add other possible extension variants only if passed executable\n        #   does not have any\n        if low_platform == \"windows\":\n            exts |= {\".exe\", \".ps1\", \".bat\"}\n            for ext in os.getenv(\"PATHEXT\", \"\").split(os.pathsep):\n                exts.add(ext.lower())\n\n        else:\n            exts |= {\".sh\"}\n\n    # Executable is a path but there may be missing extension\n    #   - this can happen primarily on windows where\n    #       e.g. \"ffmpeg\" should be \"ffmpeg.exe\"\n    exe_dir, exe_filename = os.path.split(executable)\n    if exe_dir and os.path.isdir(exe_dir):\n        for filename in os.listdir(exe_dir):\n            filepath = os.path.join(exe_dir, filename)\n            basename, ext = os.path.splitext(filename)\n            if (\n                basename == exe_filename\n                and ext.lower() in exts\n                and is_file_executable(filepath)\n            ):\n                return filepath\n\n    # Get paths where to look for executable\n    path_str = os.environ.get(\"PATH\", None)\n    if path_str is None:\n        if hasattr(os, \"confstr\"):\n            path_str = os.confstr(\"CS_PATH\")\n        elif hasattr(os, \"defpath\"):\n            path_str = os.defpath\n\n    if not path_str:\n        return None\n\n    paths = path_str.split(os.pathsep)\n    for path in paths:\n        if not os.path.isdir(path):\n            continue\n        for filename in os.listdir(path):\n            filepath = os.path.abspath(os.path.join(path, filename))\n            # Filename matches executable exactly\n            if filename == executable and is_file_executable(filepath):\n                return filepath\n\n            basename, ext = os.path.splitext(filename)\n            if (\n                basename == executable\n                and ext.lower() in exts\n                and is_file_executable(filepath)\n            ):\n                return filepath\n\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/vendor_bin_utils.html#client.ayon_core.lib.vendor_bin_utils.find_tool_in_custom_paths","title":"<code>find_tool_in_custom_paths(paths, tool, validation_func=None)</code>","text":"<p>Find a tool executable in custom paths.</p> <p>Parameters:</p> Name Type Description Default <code>paths</code> <code>Iterable[str]</code> <p>Iterable of paths where to look for tool.</p> required <code>tool</code> <code>str</code> <p>Name of tool (binary file) to find in passed paths.</p> required <code>validation_func</code> <code>Function</code> <p>Custom validation function of path. Function must expect one argument which is path to executable. If not passed only 'find_executable' is used to be able identify if path is valid.</p> <code>None</code> Reuturns <p>Union[str, None]: Path to validated executable or None if was not     found.</p> Source code in <code>client/ayon_core/lib/vendor_bin_utils.py</code> <pre><code>def find_tool_in_custom_paths(paths, tool, validation_func=None):\n    \"\"\"Find a tool executable in custom paths.\n\n    Args:\n        paths (Iterable[str]): Iterable of paths where to look for tool.\n        tool (str): Name of tool (binary file) to find in passed paths.\n        validation_func (Function): Custom validation function of path.\n            Function must expect one argument which is path to executable.\n            If not passed only 'find_executable' is used to be able identify\n            if path is valid.\n\n    Reuturns:\n        Union[str, None]: Path to validated executable or None if was not\n            found.\n    \"\"\"\n\n    for path in paths:\n        # Skip empty strings\n        if not path:\n            continue\n\n        # Handle cases when path is just an executable\n        #   - it allows to use executable from PATH\n        #   - basename must match 'tool' value (without extension)\n        extless_path, _ext = os.path.splitext(path)\n        if extless_path == tool:\n            executable_path = find_executable(tool)\n            if executable_path and (\n                validation_func is None\n                or validation_func(executable_path)\n            ):\n                return executable_path\n            continue\n\n        # Normalize path because it should be a path and check if exists\n        normalized = os.path.normpath(path)\n        if not os.path.exists(normalized):\n            continue\n\n        # Note: Path can be both file and directory\n\n        # If path is a file validate it\n        if os.path.isfile(normalized):\n            basename, _ext = os.path.splitext(os.path.basename(path))\n            # Check if the filename has actually the sane bane as 'tool'\n            if basename == tool:\n                executable_path = find_executable(normalized)\n                if executable_path and (\n                    validation_func is None\n                    or validation_func(executable_path)\n                ):\n                    return executable_path\n\n        # Check if path is a directory and look for tool inside the dir\n        if os.path.isdir(normalized):\n            executable_path = find_executable(os.path.join(normalized, tool))\n            if executable_path and (\n                validation_func is None\n                or validation_func(executable_path)\n            ):\n                return executable_path\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/vendor_bin_utils.html#client.ayon_core.lib.vendor_bin_utils.get_ffmpeg_tool_args","title":"<code>get_ffmpeg_tool_args(tool_name, *extra_args)</code>","text":"<p>Arguments to launch FFmpeg tool.</p> <p>Parameters:</p> Name Type Description Default <code>tool_name</code> <code>str</code> <p>Tool name 'ffmpeg', 'ffprobe', exc.</p> required <code>*extra_args</code> <code>str</code> <p>Extra arguments to add to after tool arguments.</p> <code>()</code> <p>Returns:</p> Type Description <p>list[str]: List of arguments.</p> Source code in <code>client/ayon_core/lib/vendor_bin_utils.py</code> <pre><code>def get_ffmpeg_tool_args(tool_name, *extra_args):\n    \"\"\"Arguments to launch FFmpeg tool.\n\n    Args:\n        tool_name (str): Tool name 'ffmpeg', 'ffprobe', exc.\n        *extra_args (str): Extra arguments to add to after tool arguments.\n\n    Returns:\n        list[str]: List of arguments.\n    \"\"\"\n\n    extra_args = list(extra_args)\n\n    args = _get_ayon_ffmpeg_tool_args(tool_name)\n    if args:\n        return args + extra_args\n\n    executable_path = get_ffmpeg_tool_path(tool_name)\n    if executable_path:\n        return [executable_path] + extra_args\n    raise ToolNotFoundError(\n        \"FFmpeg '{}' tool not found.\".format(tool_name)\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/vendor_bin_utils.html#client.ayon_core.lib.vendor_bin_utils.get_ffmpeg_tool_path","title":"<code>get_ffmpeg_tool_path(tool='ffmpeg')</code>","text":"<p>Path to vendorized FFmpeg executable.</p> <p>Parameters:</p> Name Type Description Default <code>tool</code> <code>str</code> <p>Tool name 'ffmpeg', 'ffprobe', etc. Default is \"ffmpeg\".</p> <code>'ffmpeg'</code> <p>Returns:</p> Name Type Description <code>str</code> <p>Full path to ffmpeg executable.</p> Source code in <code>client/ayon_core/lib/vendor_bin_utils.py</code> <pre><code>def get_ffmpeg_tool_path(tool=\"ffmpeg\"):\n    \"\"\"Path to vendorized FFmpeg executable.\n\n    Args:\n        tool (str): Tool name 'ffmpeg', 'ffprobe', etc.\n            Default is \"ffmpeg\".\n\n    Returns:\n        str: Full path to ffmpeg executable.\n    \"\"\"\n\n    if CachedToolPaths.is_tool_cached(tool):\n        return CachedToolPaths.get_executable_path(tool)\n\n    args = _get_ayon_ffmpeg_tool_args(tool)\n    if args is not None:\n        if len(args) &gt; 1:\n            raise ValueError(\n                \"AYON ffmpeg arguments consist of multiple arguments.\"\n            )\n        tool_executable_path = args[0]\n        CachedToolPaths.cache_executable_path(tool, tool_executable_path)\n        return tool_executable_path\n\n    custom_paths_str = os.environ.get(\"AYON_FFMPEG_PATHS\") or \"\"\n    tool_executable_path = find_tool_in_custom_paths(\n        custom_paths_str.split(os.pathsep),\n        tool,\n        _ffmpeg_executable_validation\n    )\n\n    # Look to PATH for the tool\n    if not tool_executable_path:\n        from_path = find_executable(tool)\n        if from_path and _ffmpeg_executable_validation(from_path):\n            tool_executable_path = from_path\n\n    CachedToolPaths.cache_executable_path(tool, tool_executable_path)\n    return tool_executable_path\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/vendor_bin_utils.html#client.ayon_core.lib.vendor_bin_utils.get_oiio_tool_args","title":"<code>get_oiio_tool_args(tool_name, *extra_args)</code>","text":"<p>Arguments to launch OpenImageIO tool.</p> <p>Parameters:</p> Name Type Description Default <code>tool_name</code> <code>str</code> <p>Tool name 'oiiotool', 'maketx', etc.</p> required <code>*extra_args</code> <code>str</code> <p>Extra arguments to add to after tool arguments.</p> <code>()</code> <p>Returns:</p> Type Description <p>list[str]: List of arguments.</p> Source code in <code>client/ayon_core/lib/vendor_bin_utils.py</code> <pre><code>def get_oiio_tool_args(tool_name, *extra_args):\n    \"\"\"Arguments to launch OpenImageIO tool.\n\n    Args:\n        tool_name (str): Tool name 'oiiotool', 'maketx', etc.\n        *extra_args (str): Extra arguments to add to after tool arguments.\n\n    Returns:\n        list[str]: List of arguments.\n    \"\"\"\n\n    extra_args = list(extra_args)\n\n    args = _get_ayon_oiio_tool_args(tool_name)\n    if args:\n        return args + extra_args\n\n    path = get_oiio_tools_path(tool_name)\n    if path:\n        return [path] + extra_args\n    raise ToolNotFoundError(\n        \"OIIO '{}' tool not found.\".format(tool_name)\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/vendor_bin_utils.html#client.ayon_core.lib.vendor_bin_utils.get_oiio_tools_path","title":"<code>get_oiio_tools_path(tool='oiiotool')</code>","text":"<p>Path to OpenImageIO tool executables.</p> <p>On Windows it adds .exe extension if missing from tool argument.</p> <p>Parameters:</p> Name Type Description Default <code>tool</code> <code>string</code> <p>Tool name 'oiiotool', 'maketx', etc. Default is \"oiiotool\".</p> <code>'oiiotool'</code> Source code in <code>client/ayon_core/lib/vendor_bin_utils.py</code> <pre><code>def get_oiio_tools_path(tool=\"oiiotool\"):\n    \"\"\"Path to OpenImageIO tool executables.\n\n    On Windows it adds .exe extension if missing from tool argument.\n\n    Args:\n        tool (string): Tool name 'oiiotool', 'maketx', etc.\n            Default is \"oiiotool\".\n    \"\"\"\n\n    if CachedToolPaths.is_tool_cached(tool):\n        return CachedToolPaths.get_executable_path(tool)\n\n    args = _get_ayon_oiio_tool_args(tool)\n    if args:\n        if len(args) &gt; 1:\n            raise ValueError(\n                \"AYON oiio arguments consist of multiple arguments.\"\n            )\n        tool_executable_path = args[0]\n        CachedToolPaths.cache_executable_path(tool, tool_executable_path)\n        return tool_executable_path\n\n    custom_paths_str = os.environ.get(\"AYON_OIIO_PATHS\") or \"\"\n    tool_executable_path = find_tool_in_custom_paths(\n        custom_paths_str.split(os.pathsep),\n        tool,\n        _oiio_executable_validation\n    )\n\n    # Look to PATH for the tool\n    if not tool_executable_path:\n        from_path = find_executable(tool)\n        if from_path and _oiio_executable_validation(from_path):\n            tool_executable_path = from_path\n\n    CachedToolPaths.cache_executable_path(tool, tool_executable_path)\n    return tool_executable_path\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/vendor_bin_utils.html#client.ayon_core.lib.vendor_bin_utils.is_file_executable","title":"<code>is_file_executable(filepath)</code>","text":"<p>Filepath lead to executable file.</p> <p>Parameters:</p> Name Type Description Default <code>filepath</code> <code>str</code> <p>Full path to file.</p> required Source code in <code>client/ayon_core/lib/vendor_bin_utils.py</code> <pre><code>def is_file_executable(filepath):\n    \"\"\"Filepath lead to executable file.\n\n    Args:\n        filepath(str): Full path to file.\n    \"\"\"\n    if not filepath:\n        return False\n\n    if os.path.isfile(filepath):\n        if os.access(filepath, os.X_OK):\n            return True\n\n        log.info(\n            \"Filepath is not available for execution \\\"{}\\\"\".format(filepath)\n        )\n    return False\n</code></pre>"},{"location":"autoapi/client/ayon_core/lib/vendor_bin_utils.html#client.ayon_core.lib.vendor_bin_utils.is_oiio_supported","title":"<code>is_oiio_supported()</code>","text":"<p>Checks if oiiotool is configured for this platform.</p> <p>Returns:</p> Name Type Description <code>bool</code> <p>OIIO tool executable is available.</p> Source code in <code>client/ayon_core/lib/vendor_bin_utils.py</code> <pre><code>def is_oiio_supported():\n    \"\"\"Checks if oiiotool is configured for this platform.\n\n    Returns:\n        bool: OIIO tool executable is available.\n    \"\"\"\n\n    try:\n        args = get_oiio_tool_args(\"oiiotool\")\n    except ToolNotFoundError:\n        args = None\n    if not args:\n        log.debug(\"OIIOTool is not configured or not present.\")\n        return False\n    return _oiio_executable_validation(args)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html","title":"pipeline","text":""},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.AYONPyblishPluginMixin","title":"<code>AYONPyblishPluginMixin</code>","text":"Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>class AYONPyblishPluginMixin:\n    # TODO\n    # executable_in_thread = False\n    #\n    # state_message = None\n    # state_percent = None\n    # _state_change_callbacks = []\n    #\n    # def set_state(self, percent=None, message=None):\n    #     \"\"\"Inner callback of plugin that would help to show in UI state.\n    #\n    #     Plugin have registered callbacks on state change which could trigger\n    #     update message and percent in UI and repaint the change.\n    #\n    #     This part must be optional and should not be used to display errors\n    #     or for logging.\n    #\n    #     Message should be short without details.\n    #\n    #     Args:\n    #         percent(int): Percent of processing in range &lt;1-100&gt;.\n    #         message(str): Message which will be shown to user (if in UI).\n    #     \"\"\"\n    #     if percent is not None:\n    #         self.state_percent = percent\n    #\n    #     if message:\n    #         self.state_message = message\n    #\n    #     for callback in self._state_change_callbacks:\n    #         callback(self)\n\n    @classmethod\n    def register_create_context_callbacks(\n        cls, create_context: \"CreateContext\"\n    ):\n        \"\"\"Register callbacks for create context.\n\n        It is possible to register callbacks listening to changes happened\n        in create context.\n\n        Methods available on create context:\n        - add_instances_added_callback\n        - add_instances_removed_callback\n        - add_value_changed_callback\n        - add_pre_create_attr_defs_change_callback\n        - add_create_attr_defs_change_callback\n        - add_publish_attr_defs_change_callback\n\n        Args:\n            create_context (CreateContext): Create context.\n\n        \"\"\"\n        pass\n\n    @classmethod\n    def get_attribute_defs(cls):\n        \"\"\"Publish attribute definitions.\n\n        Attributes available for all families in plugin's `families` attribute.\n\n        Returns:\n            list[AbstractAttrDef]: Attribute definitions for plugin.\n\n        \"\"\"\n        return []\n\n    @classmethod\n    def get_attr_defs_for_context(cls, create_context: \"CreateContext\"):\n        \"\"\"Publish attribute definitions for context.\n\n        Attributes available for all families in plugin's `families` attribute.\n\n        Args:\n            create_context (CreateContext): Create context.\n\n        Returns:\n            list[AbstractAttrDef]: Attribute definitions for plugin.\n\n        \"\"\"\n        if cls.__instanceEnabled__:\n            return []\n        return cls.get_attribute_defs()\n\n    @classmethod\n    def instance_matches_plugin_families(\n        cls, instance: Optional[\"CreatedInstance\"]\n    ):\n        \"\"\"Check if instance matches families.\n\n        Args:\n            instance (Optional[CreatedInstance]): Instance to check. Or None\n                for context.\n\n        Returns:\n            bool: True if instance matches plugin families.\n\n        \"\"\"\n        if instance is None:\n            return not cls.__instanceEnabled__\n\n        if not cls.__instanceEnabled__:\n            return False\n\n        families = [instance.product_base_type]\n        families.extend(instance.get(\"families\", []))\n        for _ in pyblish.logic.plugins_by_families([cls], families):\n            return True\n        return False\n\n    @classmethod\n    def get_attr_defs_for_instance(\n        cls, create_context: \"CreateContext\", instance: \"CreatedInstance\"\n    ):\n        \"\"\"Publish attribute definitions for an instance.\n\n        Attributes available for all families in plugin's `families` attribute.\n\n        Args:\n            create_context (CreateContext): Create context.\n            instance (CreatedInstance): Instance for which attributes are\n                collected.\n\n        Returns:\n            list[AbstractAttrDef]: Attribute definitions for plugin.\n\n        \"\"\"\n        if not cls.instance_matches_plugin_families(instance):\n            return []\n        return cls.get_attribute_defs()\n\n    @classmethod\n    def convert_attribute_values(\n        cls, create_context: \"CreateContext\", instance: \"CreatedInstance\"\n    ):\n        \"\"\"Convert attribute values for instance.\n\n        Args:\n            create_context (CreateContext): Create context.\n            instance (CreatedInstance): Instance for which attributes are\n                converted.\n\n        \"\"\"\n        return\n\n    @staticmethod\n    def get_attr_values_from_data_for_plugin(plugin, data):\n        \"\"\"Get attribute values for attribute definitions from data.\n\n        Args:\n            plugin (Union[publish.api.Plugin, Type[publish.api.Plugin]]): The\n                plugin for which attributes are extracted.\n            data(dict): Data from instance or context.\n        \"\"\"\n\n        if not inspect.isclass(plugin):\n            plugin = plugin.__class__\n\n        return (\n            data\n            .get(\"publish_attributes\", {})\n            .get(plugin.__name__, {})\n        )\n\n    def get_attr_values_from_data(self, data):\n        \"\"\"Get attribute values for attribute definitions from data.\n\n        Args:\n            data(dict): Data from instance or context.\n        \"\"\"\n\n        return self.get_attr_values_from_data_for_plugin(self.__class__, data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.AYONPyblishPluginMixin.convert_attribute_values","title":"<code>convert_attribute_values(create_context, instance)</code>  <code>classmethod</code>","text":"<p>Convert attribute values for instance.</p> <p>Parameters:</p> Name Type Description Default <code>create_context</code> <code>CreateContext</code> <p>Create context.</p> required <code>instance</code> <code>CreatedInstance</code> <p>Instance for which attributes are converted.</p> required Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>@classmethod\ndef convert_attribute_values(\n    cls, create_context: \"CreateContext\", instance: \"CreatedInstance\"\n):\n    \"\"\"Convert attribute values for instance.\n\n    Args:\n        create_context (CreateContext): Create context.\n        instance (CreatedInstance): Instance for which attributes are\n            converted.\n\n    \"\"\"\n    return\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.AYONPyblishPluginMixin.get_attr_defs_for_context","title":"<code>get_attr_defs_for_context(create_context)</code>  <code>classmethod</code>","text":"<p>Publish attribute definitions for context.</p> <p>Attributes available for all families in plugin's <code>families</code> attribute.</p> <p>Parameters:</p> Name Type Description Default <code>create_context</code> <code>CreateContext</code> <p>Create context.</p> required <p>Returns:</p> Type Description <p>list[AbstractAttrDef]: Attribute definitions for plugin.</p> Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>@classmethod\ndef get_attr_defs_for_context(cls, create_context: \"CreateContext\"):\n    \"\"\"Publish attribute definitions for context.\n\n    Attributes available for all families in plugin's `families` attribute.\n\n    Args:\n        create_context (CreateContext): Create context.\n\n    Returns:\n        list[AbstractAttrDef]: Attribute definitions for plugin.\n\n    \"\"\"\n    if cls.__instanceEnabled__:\n        return []\n    return cls.get_attribute_defs()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.AYONPyblishPluginMixin.get_attr_defs_for_instance","title":"<code>get_attr_defs_for_instance(create_context, instance)</code>  <code>classmethod</code>","text":"<p>Publish attribute definitions for an instance.</p> <p>Attributes available for all families in plugin's <code>families</code> attribute.</p> <p>Parameters:</p> Name Type Description Default <code>create_context</code> <code>CreateContext</code> <p>Create context.</p> required <code>instance</code> <code>CreatedInstance</code> <p>Instance for which attributes are collected.</p> required <p>Returns:</p> Type Description <p>list[AbstractAttrDef]: Attribute definitions for plugin.</p> Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>@classmethod\ndef get_attr_defs_for_instance(\n    cls, create_context: \"CreateContext\", instance: \"CreatedInstance\"\n):\n    \"\"\"Publish attribute definitions for an instance.\n\n    Attributes available for all families in plugin's `families` attribute.\n\n    Args:\n        create_context (CreateContext): Create context.\n        instance (CreatedInstance): Instance for which attributes are\n            collected.\n\n    Returns:\n        list[AbstractAttrDef]: Attribute definitions for plugin.\n\n    \"\"\"\n    if not cls.instance_matches_plugin_families(instance):\n        return []\n    return cls.get_attribute_defs()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.AYONPyblishPluginMixin.get_attr_values_from_data","title":"<code>get_attr_values_from_data(data)</code>","text":"<p>Get attribute values for attribute definitions from data.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict</code> <p>Data from instance or context.</p> required Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>def get_attr_values_from_data(self, data):\n    \"\"\"Get attribute values for attribute definitions from data.\n\n    Args:\n        data(dict): Data from instance or context.\n    \"\"\"\n\n    return self.get_attr_values_from_data_for_plugin(self.__class__, data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.AYONPyblishPluginMixin.get_attr_values_from_data_for_plugin","title":"<code>get_attr_values_from_data_for_plugin(plugin, data)</code>  <code>staticmethod</code>","text":"<p>Get attribute values for attribute definitions from data.</p> <p>Parameters:</p> Name Type Description Default <code>plugin</code> <code>Union[Plugin, Type[Plugin]]</code> <p>The plugin for which attributes are extracted.</p> required <code>data</code> <code>dict</code> <p>Data from instance or context.</p> required Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>@staticmethod\ndef get_attr_values_from_data_for_plugin(plugin, data):\n    \"\"\"Get attribute values for attribute definitions from data.\n\n    Args:\n        plugin (Union[publish.api.Plugin, Type[publish.api.Plugin]]): The\n            plugin for which attributes are extracted.\n        data(dict): Data from instance or context.\n    \"\"\"\n\n    if not inspect.isclass(plugin):\n        plugin = plugin.__class__\n\n    return (\n        data\n        .get(\"publish_attributes\", {})\n        .get(plugin.__name__, {})\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.AYONPyblishPluginMixin.get_attribute_defs","title":"<code>get_attribute_defs()</code>  <code>classmethod</code>","text":"<p>Publish attribute definitions.</p> <p>Attributes available for all families in plugin's <code>families</code> attribute.</p> <p>Returns:</p> Type Description <p>list[AbstractAttrDef]: Attribute definitions for plugin.</p> Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>@classmethod\ndef get_attribute_defs(cls):\n    \"\"\"Publish attribute definitions.\n\n    Attributes available for all families in plugin's `families` attribute.\n\n    Returns:\n        list[AbstractAttrDef]: Attribute definitions for plugin.\n\n    \"\"\"\n    return []\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.AYONPyblishPluginMixin.instance_matches_plugin_families","title":"<code>instance_matches_plugin_families(instance)</code>  <code>classmethod</code>","text":"<p>Check if instance matches families.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>Optional[CreatedInstance]</code> <p>Instance to check. Or None for context.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <p>True if instance matches plugin families.</p> Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>@classmethod\ndef instance_matches_plugin_families(\n    cls, instance: Optional[\"CreatedInstance\"]\n):\n    \"\"\"Check if instance matches families.\n\n    Args:\n        instance (Optional[CreatedInstance]): Instance to check. Or None\n            for context.\n\n    Returns:\n        bool: True if instance matches plugin families.\n\n    \"\"\"\n    if instance is None:\n        return not cls.__instanceEnabled__\n\n    if not cls.__instanceEnabled__:\n        return False\n\n    families = [instance.product_base_type]\n    families.extend(instance.get(\"families\", []))\n    for _ in pyblish.logic.plugins_by_families([cls], families):\n        return True\n    return False\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.AYONPyblishPluginMixin.register_create_context_callbacks","title":"<code>register_create_context_callbacks(create_context)</code>  <code>classmethod</code>","text":"<p>Register callbacks for create context.</p> <p>It is possible to register callbacks listening to changes happened in create context.</p> <p>Methods available on create context: - add_instances_added_callback - add_instances_removed_callback - add_value_changed_callback - add_pre_create_attr_defs_change_callback - add_create_attr_defs_change_callback - add_publish_attr_defs_change_callback</p> <p>Parameters:</p> Name Type Description Default <code>create_context</code> <code>CreateContext</code> <p>Create context.</p> required Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>@classmethod\ndef register_create_context_callbacks(\n    cls, create_context: \"CreateContext\"\n):\n    \"\"\"Register callbacks for create context.\n\n    It is possible to register callbacks listening to changes happened\n    in create context.\n\n    Methods available on create context:\n    - add_instances_added_callback\n    - add_instances_removed_callback\n    - add_value_changed_callback\n    - add_pre_create_attr_defs_change_callback\n    - add_create_attr_defs_change_callback\n    - add_publish_attr_defs_change_callback\n\n    Args:\n        create_context (CreateContext): Create context.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.Anatomy","title":"<code>Anatomy</code>","text":"<p>               Bases: <code>BaseAnatomy</code></p> Source code in <code>client/ayon_core/pipeline/anatomy/anatomy.py</code> <pre><code>class Anatomy(BaseAnatomy):\n    _project_cache = NestedCacheItem(lifetime=10)\n    _sitesync_addon_cache = CacheItem(lifetime=60)\n    _default_site_id_cache = NestedCacheItem(lifetime=60)\n    _root_overrides_cache = NestedCacheItem(2, lifetime=60)\n\n    def __init__(\n        self, project_name=None, site_name=None, project_entity=None\n    ):\n        if not project_name:\n            project_name = os.environ.get(\"AYON_PROJECT_NAME\")\n\n        if not project_name:\n            raise ProjectNotSet((\n                \"Implementation bug: Project name is not set. Anatomy requires\"\n                \" to load data for specific project.\"\n            ))\n\n        if not project_entity:\n            project_entity = self.get_project_entity_from_cache(project_name)\n        root_overrides = self._get_site_root_overrides(\n            project_name, site_name\n        )\n\n        super(Anatomy, self).__init__(project_entity, root_overrides)\n\n    @classmethod\n    def get_project_entity_from_cache(cls, project_name):\n        project_cache = cls._project_cache[project_name]\n        if not project_cache.is_valid:\n            project_cache.update_data(ayon_api.get_project(project_name))\n        return copy.deepcopy(project_cache.get_data())\n\n    @classmethod\n    def get_sitesync_addon(cls):\n        if not cls._sitesync_addon_cache.is_valid:\n            manager = AddonsManager()\n            cls._sitesync_addon_cache.update_data(\n                manager.get_enabled_addon(\"sitesync\")\n            )\n        return cls._sitesync_addon_cache.get_data()\n\n    @classmethod\n    def _get_studio_roots_overrides(cls, project_name):\n        \"\"\"This would return 'studio' site override by local settings.\n\n        Notes:\n            This logic handles local overrides of studio site which may be\n                available even when sync server is not enabled.\n            Handling of 'studio' and 'local' site was separated as preparation\n                for AYON development where that will be received from\n                separated sources.\n\n        Args:\n            project_name (str): Name of project.\n\n        Returns:\n            Union[Dict[str, str], None]): Local root overrides.\n        \"\"\"\n        if not project_name:\n            return None\n        return ayon_api.get_project_root_overrides_by_site_id(\n            project_name, get_local_site_id()\n        )\n\n    @classmethod\n    def _get_site_root_overrides(cls, project_name, site_name):\n        \"\"\"Get root overrides for site.\n\n        Args:\n            project_name (str): Project name for which root overrides should be\n                received.\n            site_name (Union[str, None]): Name of site for which root overrides\n                should be returned.\n        \"\"\"\n\n        # First check if sync server is available and enabled\n        sitesync_addon = cls.get_sitesync_addon()\n        if sitesync_addon is None or not sitesync_addon.enabled:\n            # QUESTION is ok to force 'studio' when site sync is not enabled?\n            site_name = \"studio\"\n\n        elif not site_name:\n            # Use sync server to receive active site name\n            project_cache = cls._default_site_id_cache[project_name]\n            if not project_cache.is_valid:\n                project_cache.update_data(\n                    sitesync_addon.get_active_site_type(project_name)\n                )\n            site_name = project_cache.get_data()\n\n        site_cache = cls._root_overrides_cache[project_name][site_name]\n        if not site_cache.is_valid:\n            if site_name == \"studio\":\n                # Handle studio root overrides without sync server\n                # - studio root overrides can be done even without sync server\n                roots_overrides = cls._get_studio_roots_overrides(\n                    project_name\n                )\n            else:\n                # Ask sync server to get roots overrides\n                roots_overrides = sitesync_addon.get_site_root_overrides(\n                    project_name, site_name\n                )\n            site_cache.update_data(roots_overrides)\n        return site_cache.get_data()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.AutoCreator","title":"<code>AutoCreator</code>","text":"<p>               Bases: <code>BaseCreator</code></p> <p>Creator which is automatically triggered without user interaction.</p> <p>Can be used e.g. for <code>workfile</code>.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>class AutoCreator(BaseCreator):\n    \"\"\"Creator which is automatically triggered without user interaction.\n\n    Can be used e.g. for `workfile`.\n    \"\"\"\n    skip_discovery = True\n\n    def remove_instances(self, instances: list[CreatedInstance]) -&gt; None:\n        \"\"\"Skip removal.\"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.AutoCreator.remove_instances","title":"<code>remove_instances(instances)</code>","text":"<p>Skip removal.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def remove_instances(self, instances: list[CreatedInstance]) -&gt; None:\n    \"\"\"Skip removal.\"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.BaseCreator","title":"<code>BaseCreator</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Plugin that create and modify instance data before publishing process.</p> <p>Single object should be used for multiple instances instead of single instance per one creator object. Do not store temp data or mid-process data to <code>self</code> if it's not Plugin specific.</p> <p>Parameters:</p> Name Type Description Default <code>project_settings</code> <code>dict[str, Any]</code> <p>Project settings.</p> required <code>create_context</code> <code>CreateContext</code> <p>Context which initialized creator.</p> required <code>headless</code> <code>bool</code> <p>Running in headless mode.</p> <code>False</code> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>class BaseCreator(ABC):\n    \"\"\"Plugin that create and modify instance data before publishing process.\n\n    Single object should be used for multiple instances instead of single\n    instance per one creator object. Do not store temp data or mid-process data\n    to `self` if it's not Plugin specific.\n\n    Args:\n        project_settings (dict[str, Any]): Project settings.\n        create_context (CreateContext): Context which initialized creator.\n        headless (bool): Running in headless mode.\n\n    \"\"\"\n    # Attribute 'skip_discovery' is used during discovery phase to skip\n    #   plugins, which can be used to mark base plugins that should not be\n    #   considered as plugins \"to use\". The discovery logic does NOT use\n    #   the attribute value from parent classes. Each base class has to define\n    #   the attribute again.\n    skip_discovery: bool = True\n\n    # Label shown in UI\n    label: str | None = None\n    group_label: str | None = None\n    # Cached group label after first call 'get_group_label'\n    _cached_group_label: str | None = None\n\n    # Order in which will be plugin executed (collect &amp; update instances)\n    #   less == earlier -&gt; Order '90' will be processed before '100'\n    order: int = 100\n\n    # Variable to store logger\n    _log: logging.Logger | None = None\n\n    # Creator is enabled (Probably does not have reason of existence?)\n    enabled: bool = True\n\n    # Creator (and product base type) icon\n    # - may not be used if `get_icon` is reimplemented\n    icon: IconBase | str | None = None\n\n    # Instance attribute definitions that can be changed per instance\n    # - returns list of attribute definitions from\n    #       `ayon_core.lib.attribute_definitions`\n    instance_attr_defs: list[AbstractAttrDef] = []\n\n    # Filtering by host name - can be used to be filtered by host name\n    # - used on all hosts when set to 'None' for Backwards compatibility\n    #   - was added afterwards\n    # QUESTION make this required?\n    host_name: str | None = None\n\n    # Settings auto-apply helpers\n    # Root key in project settings (mandatory for auto-apply to work)\n    settings_category: str | None = None\n    # Name of plugin in create settings &gt; class name is used if not set\n    settings_name: str | None = None\n\n    product_type_items: list[ProductTypeItem] = []\n\n    def __init__(\n        self,\n        project_settings: dict[str, Any],\n        create_context: CreateContext,\n        headless: bool = False\n    ) -&gt; None:\n        # Reference to CreateContext\n        self.create_context: CreateContext = create_context\n        self.project_settings = project_settings\n\n        # Creator is running in headless mode (without UI elements)\n        # - we may use UI inside processing this attribute should be checked\n        self.headless: bool = headless\n\n        self.apply_settings(project_settings)\n        self.register_callbacks()\n\n    @staticmethod\n    def _get_settings_values(\n        project_settings: dict[str, Any],\n        category_name: str,\n        plugin_name: str\n    ) -&gt; dict[str, Any] | None:\n        \"\"\"Helper method to get settings values.\n\n        Args:\n            project_settings (dict[str, Any]): Project settings.\n            category_name (str): Category of settings.\n            plugin_name (str): Name of settings.\n\n        Returns:\n            dict[str, Any] | None: Settings values or None.\n\n        \"\"\"\n        settings = project_settings.get(category_name)\n        if not settings:\n            return None\n\n        create_settings = settings.get(\"create\")\n        if not create_settings:\n            return None\n\n        return create_settings.get(plugin_name)\n\n    def apply_settings(self, project_settings: dict[str, Any]) -&gt; None:\n        \"\"\"Method called on initialization of plugin to apply settings.\n\n        Default implementation tries to auto-apply settings values if are\n            in expected hierarchy.\n\n        Data hierarchy to auto-apply settings:\n            \u251c\u2500 {self.settings_category}                 - Root key in settings\n            \u2502 \u2514\u2500 \"create\"                               - Hardcoded key\n            \u2502   \u2514\u2500 {self.settings_name} | {class name}  - Name of plugin\n            \u2502     \u251c\u2500 ... attribute values...            - Attribute/value pair\n\n        It is mandatory to define 'settings_category' attribute. Attribute\n        'settings_name' is optional and class name is used if is not defined.\n\n        Example data:\n            ProjectSettings {\n                \"maya\": {                    # self.settings_category\n                    \"create\": {              # Hardcoded key\n                        \"CreateAnimation\": { # self.settings_name / class name\n                            \"enabled\": True, # --- Attributes to set ---\n                            \"optional\": True,#\n                            \"active\": True,  #\n                            \"fps\": 25,       # -------------------------\n                        },\n                        ...\n                    },\n                    ...\n                },\n                ...\n            }\n\n        Args:\n            project_settings (dict[str, Any]): Project settings.\n        \"\"\"\n\n        settings_category = self.settings_category\n        if not settings_category:\n            return\n\n        cls_name = self.__class__.__name__\n        settings_name = self.settings_name or cls_name\n\n        settings = self._get_settings_values(\n            project_settings, settings_category, settings_name\n        )\n        if settings is None:\n            self.log.debug(f\"No settings found for {cls_name}\")\n            return\n\n        for key, value in settings.items():\n            # Log out attributes that are not defined on plugin object\n            # - those may be potential dangerous typos in settings\n            if not hasattr(self, key):\n                self.log.debug(\n                    \"Applying settings to unknown attribute '%s' on '%s'.\",\n                    key, cls_name\n                )\n            setattr(self, key, value)\n\n        self.product_type_items = self._convert_product_type_items(\n            self.product_type_items\n        )\n\n    def _convert_product_type_items(\n        self, product_type_items: list\n    ) -&gt; list[ProductTypeItem]:\n        \"\"\"Helper method to convert product type items from settings.\"\"\"\n        if not product_type_items:\n            return []\n\n        first_item = product_type_items[0]\n        if isinstance(first_item, ProductTypeItem):\n            return product_type_items\n\n        if not isinstance(first_item, dict):\n            self.log.warning(\n                f\"Invalid product type item. Expected 'dict' or\"\n                f\" 'ProductTypeItem', got '{type(first_item)}'.\"\n            )\n            return []\n\n        try:\n            return [\n                ProductTypeItem.from_data(item)\n                for item in self.product_type_items\n            ]\n        except Exception:\n            self.log.warning(\n                \"Failed to convert product type items\"\n                \" to ProductTypeItem instances\"\n            )\n            return []\n\n    def register_callbacks(self) -&gt; None:\n        \"\"\"Register callbacks for creator.\n\n        Default implementation does nothing. It can be overridden to register\n        callbacks for creator.\n        \"\"\"\n\n    @property\n    def identifier(self) -&gt; str:\n        \"\"\"Identifier of creator (must be unique).\n\n        Default implementation returns plugin's product base type,\n        or falls back to product type if product base type is not set.\n\n        \"\"\"\n        identifier = self.product_base_type\n        if not identifier:\n            identifier = self.product_type\n        return identifier\n\n    @property\n    @abstractmethod\n    def product_type(self) -&gt; str:\n        \"\"\"Family that plugin represents.\n\n        DEPRECATED - use 'product_base_type' instead. This is kept for\n            backwards compatibility and should be removed when\n            'product_base_type' is required.\n\n        \"\"\"\n\n    @property\n    def product_base_type(self) -&gt; str | None:\n        \"\"\"Product base type that plugin represents.\n\n        Todo (antirotor): This should be required in future - it\n            should be made abstract then.\n\n        Returns:\n            str | None: Product base type that plugin represents.\n                If not set, it is assumed that the creator plugin is obsolete\n                and does not support product base type.\n\n        \"\"\"\n        return None\n\n    @property\n    def project_name(self) -&gt; str:\n        \"\"\"Current project name.\n\n        Returns:\n            str: Name of a project.\n\n        \"\"\"\n        return self.create_context.project_name\n\n    @property\n    def project_anatomy(self) -&gt; Anatomy:\n        \"\"\"Current project anatomy.\n\n        Returns:\n            Anatomy: Project anatomy object.\n\n        \"\"\"\n        return self.create_context.project_anatomy\n\n    @property\n    def host(self) -&gt; IPublishHost:\n        return self.create_context.host\n\n    def get_group_label(self) -&gt; str:\n        \"\"\"Group label under which are instances grouped in UI.\n\n        Default implementation use attributes in this order:\n            - 'group_label' -&gt; 'label' -&gt; 'identifier'\n\n        Keep in mind that 'identifier' uses 'product_base_type' by default.\n\n        Returns:\n            str: Group label that can be used for grouping of instances in UI.\n                Group label can be overridden by the instance itself.\n\n        \"\"\"\n        if self._cached_group_label is None:\n            label = self.identifier\n            if self.group_label:\n                label = self.group_label\n            elif self.label:\n                label = self.label\n            self._cached_group_label = label\n        return self._cached_group_label\n\n    @property\n    def log(self) -&gt; logging.Logger:\n        \"\"\"Logger of the plugin.\n\n        Returns:\n            logging.Logger: Logger with name of the plugin.\n\n        \"\"\"\n        if self._log is None:\n            self._log = Logger.get_logger(self.__class__.__name__)\n        return self._log\n\n    def _create_instance(\n        self,\n        product_name: str,\n        data: dict[str, Any],\n        product_type: str | None = None,\n        product_base_type: str | None = None\n    ) -&gt; CreatedInstance:\n        \"\"\"Create instance and add instance to context.\n\n        Args:\n            product_name (str): Product name.\n            data (dict[str, Any]): Instance data.\n            product_type (str | None): Product type, object attribute\n                'product_type' is used if not passed.\n            product_base_type (str | None): Product base type, object\n                attribute 'product_base_type' is used if not passed.\n\n        Returns:\n            CreatedInstance: Created instance.\n\n        \"\"\"\n        if not product_base_type:\n            product_base_type = self.product_base_type\n\n        if not product_base_type:\n            product_base_type = self.product_type\n\n        if product_type is None:\n            product_type = product_base_type\n\n        instance = CreatedInstance(\n            product_type=product_type,\n            product_name=product_name,\n            data=data,\n            creator=self,\n            product_base_type=product_base_type,\n        )\n        self._add_instance_to_context(instance)\n        return instance\n\n    def _add_instance_to_context(\n        self, instance: CreatedInstance\n    ) -&gt; None:\n        \"\"\"Helper method to add instance to create context.\n\n        Instances should be stored to DCC workfile metadata to be able reload\n        them and also stored to CreateContext in which is creator plugin\n        existing at the moment to be able use it without refresh of\n        CreateContext.\n\n        Args:\n            instance (CreatedInstance): New created instance.\n\n        \"\"\"\n        self.create_context.creator_adds_instance(instance)\n\n    def _remove_instance_from_context(\n        self, instance: CreatedInstance\n    ) -&gt; None:\n        \"\"\"Helper method to remove instance from create context.\n\n        Instances must be removed from DCC workfile metadat aand from create\n        context in which plugin is existing at the moment of removal to\n        propagate the change without restarting create context.\n\n        Args:\n            instance (CreatedInstance): Instance which should be removed.\n\n        \"\"\"\n        self.create_context.creator_removed_instance(instance)\n\n    @abstractmethod\n    def create(self):\n        \"\"\"Trigger creation logic usually to create new instance/s.\"\"\"\n\n    @abstractmethod\n    def collect_instances(self) -&gt; None:\n        \"\"\"Collect existing instances related to this creator plugin.\n\n        The implementation differs on host abilities. The creator has to\n        collect metadata about instance and create 'CreatedInstance' object\n        which should be added to 'CreateContext'.\n\n        Example:\n        ```python\n        def collect_instances(self):\n            # Getting existing instances is different per host implementation\n            for instance_data in pipeline.list_instances():\n                # Process only instances that were created by this creator\n                creator_id = instance_data.get(\"creator_identifier\")\n                if creator_id == self.identifier:\n                    # Create instance object from existing data\n                    instance = CreatedInstance.from_existing(\n                        instance_data, self\n                    )\n                    # Add instance to create context\n                    self._add_instance_to_context(instance)\n        ```\n        \"\"\"\n\n    @abstractmethod\n    def update_instances(\n        self, update_list: list[UpdateData]\n    ) -&gt; None:\n        \"\"\"Store changes of existing instances so they can be recollected.\n\n        Args:\n            update_list (list[UpdateData]): Gets list of tuples. Each item\n                contain changed instance and it's changes.\n        \"\"\"\n\n    @abstractmethod\n    def remove_instances(\n        self, instances: list[CreatedInstance]\n    ) -&gt; None:\n        \"\"\"Method called on instance removal.\n\n        Can also remove instance metadata from context but should return\n        'True' if did so.\n\n        Args:\n            instances (list[CreatedInstance]): Instance objects which should be\n                removed.\n        \"\"\"\n\n    def get_icon(self) -&gt; IconBase | str | None:\n        \"\"\"Icon of creator (product base type).\n\n        Can return path to image file or awesome icon name.\n        \"\"\"\n        return self.icon\n\n    def get_dynamic_data(\n        self,\n        project_name: str,\n        folder_entity: dict[str, Any] | None,\n        task_entity: dict[str, Any] | None,\n        variant: str,\n        host_name: str,\n        instance: CreatedInstance | None = None,\n        project_entity: dict[str, Any] | None = None,\n        product_type: str | None = None,\n    ) -&gt; dict[str, Any]:\n        \"\"\"Dynamic data for product name filling.\n\n        These may be dynamically created based on current context of workfile.\n\n        Default implementation will always return empty dictionary.\n\n        \"\"\"\n        return {}\n\n    def get_product_name(\n        self,\n        project_name: str,\n        folder_entity: dict[str, Any],\n        task_entity: dict[str, Any] | None,\n        variant: str,\n        host_name: str | None = None,\n        instance: CreatedInstance | None = None,\n        project_entity: dict[str, Any] | None = None,\n        product_type: str | None = None,\n    ) -&gt; str:\n        \"\"\"Return product name for passed context.\n\n        Method is also called on product name update. In that case origin\n        instance is passed in.\n\n        Args:\n            project_name (str): Project name.\n            folder_entity (dict): Folder entity.\n            task_entity (dict | None): Task entity.\n            variant (str): Product name variant. In most of cases user input.\n            host_name (str | None): Which host creates product. Defaults\n                to host name on create context.\n            instance (CreatedInstance | None): Object of 'CreatedInstance'\n                for which is product name updated. Passed only on product name\n                update.\n            project_entity (dict[str, Any] | None): Project entity.\n            product_type (str | None): Product type.\n\n        Returns:\n            str: Product name for passed context.\n\n        \"\"\"\n        if host_name is None:\n            host_name = self.create_context.host_name\n\n        # Backwards compatibility for create plugins that don't implement\n        #   'product_base_type'.\n        # TODO Remove when 'product_base_type' is required\n        product_base_type = self.product_base_type\n        if not product_base_type:\n            product_base_type = self.product_type\n\n        if product_type is None:\n            for product_type_item in self.get_product_type_items():\n                product_type = product_type_item.product_type\n                break\n            else:\n                product_type = product_base_type\n\n        cur_project_name = self.create_context.get_current_project_name()\n        if not project_entity and project_name == cur_project_name:\n            project_entity = self.create_context.get_current_project_entity()\n\n        args = (\n            project_name,\n            folder_entity,\n            task_entity,\n            variant,\n            host_name,\n        )\n        kwargs = dict(\n            instance=instance,\n            project_entity=project_entity,\n            product_type=product_type,\n        )\n        # NOTE 'project_entity' and 'product_type' were added at the same time\n        #   26/01/19\n        if not is_func_signature_supported(\n            self.get_dynamic_data, *args, **kwargs\n        ):\n            kwargs.pop(\"project_entity\")\n            kwargs.pop(\"product_type\")\n\n        dynamic_data = self.get_dynamic_data(*args, **kwargs)\n\n        return get_product_name(\n            project_name,\n            folder_entity=folder_entity,\n            task_entity=task_entity,\n            product_base_type=product_base_type,\n            product_type=product_type,\n            host_name=host_name,\n            variant=variant,\n            dynamic_data=dynamic_data,\n            project_settings=self.project_settings,\n            project_entity=project_entity,\n        )\n\n    def get_instance_attr_defs(self) -&gt; list[AbstractAttrDef]:\n        \"\"\"Plugin attribute definitions.\n\n        Attribute definitions of plugin that hold data about created instance\n        and values are stored to metadata for future usage and for publishing\n        purposes.\n\n        Note:\n            Convert method should be implemented which should care about\n            updating keys/values when plugin attributes change.\n\n        Returns:\n            list[AbstractAttrDef]: Attribute definitions that can be tweaked\n                for created instance.\n\n        \"\"\"\n        return self.instance_attr_defs\n\n    def get_attr_defs_for_instance(\n        self, instance: CreatedInstance\n    ) -&gt; list[AbstractAttrDef]:\n        \"\"\"Get attribute definitions for an instance.\n\n        Args:\n            instance (CreatedInstance): Instance for which to get\n                attribute definitions.\n\n        \"\"\"\n        return self.get_instance_attr_defs()\n\n    @property\n    def collection_shared_data(self) -&gt; dict[str, Any]:\n        \"\"\"Access to shared data that can be used during creator's collection.\n\n        Returns:\n            dict[str, Any]: Shared data.\n\n        Raises:\n            UnavailableSharedData: When called out of collection phase.\n\n        \"\"\"\n        return self.create_context.collection_shared_data\n\n    def set_instance_thumbnail_path(\n        self,\n        instance_id: str,\n        thumbnail_path: str | None = None,\n    ) -&gt; None:\n        \"\"\"Set path to thumbnail for instance.\"\"\"\n\n        self.create_context.thumbnail_paths_by_instance_id[instance_id] = (\n            thumbnail_path\n        )\n\n    def get_next_versions_for_instances(\n        self, instances: list[CreatedInstance]\n    ) -&gt; dict[str, int]:\n        \"\"\"Prepare next versions for instances.\n\n        This is helper method to receive next possible versions for instances.\n        It is using context information on instance to receive them,\n        'folderPath' and 'product'.\n\n        Output will contain version by each instance id.\n\n        Args:\n            instances (list[CreatedInstance]): Instances for which to get next\n                versions.\n\n        Returns:\n            dict[str, int]: Next versions by instance id.\n        \"\"\"\n\n        return get_next_versions_for_instances(\n            self.create_context.project_name, instances\n        )\n\n    def get_product_type_items(self) -&gt; list[ProductTypeItem]:\n        \"\"\"Get product type the Creator can work with.\n\n        By default, it returns `product_type_items` attribute value that\n        can be set by Creator settings. This can be overridden to provide\n        different source.\n\n        Product type items are list of ProductTypeItem that\n        Creator can create. Label is used in UI to show user-friendly name.\n        This dataclass can be easily expanded with data in the future. New\n        fields must have default values to not break existing implementations.\n\n        Returns:\n            list[ProductTypeItem]: List of product type items.\n\n        \"\"\"\n        return self.product_type_items\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.BaseCreator.collection_shared_data","title":"<code>collection_shared_data</code>  <code>property</code>","text":"<p>Access to shared data that can be used during creator's collection.</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: Shared data.</p> <p>Raises:</p> Type Description <code>UnavailableSharedData</code> <p>When called out of collection phase.</p>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.BaseCreator.identifier","title":"<code>identifier</code>  <code>property</code>","text":"<p>Identifier of creator (must be unique).</p> <p>Default implementation returns plugin's product base type, or falls back to product type if product base type is not set.</p>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.BaseCreator.log","title":"<code>log</code>  <code>property</code>","text":"<p>Logger of the plugin.</p> <p>Returns:</p> Type Description <code>Logger</code> <p>logging.Logger: Logger with name of the plugin.</p>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.BaseCreator.product_base_type","title":"<code>product_base_type</code>  <code>property</code>","text":"<p>Product base type that plugin represents.</p> <p>Todo (antirotor): This should be required in future - it     should be made abstract then.</p> <p>Returns:</p> Type Description <code>str | None</code> <p>str | None: Product base type that plugin represents. If not set, it is assumed that the creator plugin is obsolete and does not support product base type.</p>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.BaseCreator.product_type","title":"<code>product_type</code>  <code>abstractmethod</code> <code>property</code>","text":"<p>Family that plugin represents.</p> <p>DEPRECATED - use 'product_base_type' instead. This is kept for     backwards compatibility and should be removed when     'product_base_type' is required.</p>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.BaseCreator.project_anatomy","title":"<code>project_anatomy</code>  <code>property</code>","text":"<p>Current project anatomy.</p> <p>Returns:</p> Name Type Description <code>Anatomy</code> <code>Anatomy</code> <p>Project anatomy object.</p>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.BaseCreator.project_name","title":"<code>project_name</code>  <code>property</code>","text":"<p>Current project name.</p> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Name of a project.</p>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.BaseCreator.apply_settings","title":"<code>apply_settings(project_settings)</code>","text":"<p>Method called on initialization of plugin to apply settings.</p> <p>Default implementation tries to auto-apply settings values if are     in expected hierarchy.</p> Data hierarchy to auto-apply settings <p>\u251c\u2500 {self.settings_category}                 - Root key in settings \u2502 \u2514\u2500 \"create\"                               - Hardcoded key \u2502   \u2514\u2500 {self.settings_name} | {class name}  - Name of plugin \u2502     \u251c\u2500 ... attribute values...            - Attribute/value pair</p> <p>It is mandatory to define 'settings_category' attribute. Attribute 'settings_name' is optional and class name is used if is not defined.</p> Example data <p>ProjectSettings {     \"maya\": {                    # self.settings_category         \"create\": {              # Hardcoded key             \"CreateAnimation\": { # self.settings_name / class name                 \"enabled\": True, # --- Attributes to set ---                 \"optional\": True,#                 \"active\": True,  #                 \"fps\": 25,       # -------------------------             },             ...         },         ...     },     ... }</p> <p>Parameters:</p> Name Type Description Default <code>project_settings</code> <code>dict[str, Any]</code> <p>Project settings.</p> required Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def apply_settings(self, project_settings: dict[str, Any]) -&gt; None:\n    \"\"\"Method called on initialization of plugin to apply settings.\n\n    Default implementation tries to auto-apply settings values if are\n        in expected hierarchy.\n\n    Data hierarchy to auto-apply settings:\n        \u251c\u2500 {self.settings_category}                 - Root key in settings\n        \u2502 \u2514\u2500 \"create\"                               - Hardcoded key\n        \u2502   \u2514\u2500 {self.settings_name} | {class name}  - Name of plugin\n        \u2502     \u251c\u2500 ... attribute values...            - Attribute/value pair\n\n    It is mandatory to define 'settings_category' attribute. Attribute\n    'settings_name' is optional and class name is used if is not defined.\n\n    Example data:\n        ProjectSettings {\n            \"maya\": {                    # self.settings_category\n                \"create\": {              # Hardcoded key\n                    \"CreateAnimation\": { # self.settings_name / class name\n                        \"enabled\": True, # --- Attributes to set ---\n                        \"optional\": True,#\n                        \"active\": True,  #\n                        \"fps\": 25,       # -------------------------\n                    },\n                    ...\n                },\n                ...\n            },\n            ...\n        }\n\n    Args:\n        project_settings (dict[str, Any]): Project settings.\n    \"\"\"\n\n    settings_category = self.settings_category\n    if not settings_category:\n        return\n\n    cls_name = self.__class__.__name__\n    settings_name = self.settings_name or cls_name\n\n    settings = self._get_settings_values(\n        project_settings, settings_category, settings_name\n    )\n    if settings is None:\n        self.log.debug(f\"No settings found for {cls_name}\")\n        return\n\n    for key, value in settings.items():\n        # Log out attributes that are not defined on plugin object\n        # - those may be potential dangerous typos in settings\n        if not hasattr(self, key):\n            self.log.debug(\n                \"Applying settings to unknown attribute '%s' on '%s'.\",\n                key, cls_name\n            )\n        setattr(self, key, value)\n\n    self.product_type_items = self._convert_product_type_items(\n        self.product_type_items\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.BaseCreator.collect_instances","title":"<code>collect_instances()</code>  <code>abstractmethod</code>","text":"<p>Collect existing instances related to this creator plugin.</p> <p>The implementation differs on host abilities. The creator has to collect metadata about instance and create 'CreatedInstance' object which should be added to 'CreateContext'.</p> <p>Example:</p> <pre><code>def collect_instances(self):\n    # Getting existing instances is different per host implementation\n    for instance_data in pipeline.list_instances():\n        # Process only instances that were created by this creator\n        creator_id = instance_data.get(\"creator_identifier\")\n        if creator_id == self.identifier:\n            # Create instance object from existing data\n            instance = CreatedInstance.from_existing(\n                instance_data, self\n            )\n            # Add instance to create context\n            self._add_instance_to_context(instance)\n</code></pre> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>@abstractmethod\ndef collect_instances(self) -&gt; None:\n    \"\"\"Collect existing instances related to this creator plugin.\n\n    The implementation differs on host abilities. The creator has to\n    collect metadata about instance and create 'CreatedInstance' object\n    which should be added to 'CreateContext'.\n\n    Example:\n    ```python\n    def collect_instances(self):\n        # Getting existing instances is different per host implementation\n        for instance_data in pipeline.list_instances():\n            # Process only instances that were created by this creator\n            creator_id = instance_data.get(\"creator_identifier\")\n            if creator_id == self.identifier:\n                # Create instance object from existing data\n                instance = CreatedInstance.from_existing(\n                    instance_data, self\n                )\n                # Add instance to create context\n                self._add_instance_to_context(instance)\n    ```\n    \"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.BaseCreator.create","title":"<code>create()</code>  <code>abstractmethod</code>","text":"<p>Trigger creation logic usually to create new instance/s.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>@abstractmethod\ndef create(self):\n    \"\"\"Trigger creation logic usually to create new instance/s.\"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.BaseCreator.get_attr_defs_for_instance","title":"<code>get_attr_defs_for_instance(instance)</code>","text":"<p>Get attribute definitions for an instance.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>CreatedInstance</code> <p>Instance for which to get attribute definitions.</p> required Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def get_attr_defs_for_instance(\n    self, instance: CreatedInstance\n) -&gt; list[AbstractAttrDef]:\n    \"\"\"Get attribute definitions for an instance.\n\n    Args:\n        instance (CreatedInstance): Instance for which to get\n            attribute definitions.\n\n    \"\"\"\n    return self.get_instance_attr_defs()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.BaseCreator.get_dynamic_data","title":"<code>get_dynamic_data(project_name, folder_entity, task_entity, variant, host_name, instance=None, project_entity=None, product_type=None)</code>","text":"<p>Dynamic data for product name filling.</p> <p>These may be dynamically created based on current context of workfile.</p> <p>Default implementation will always return empty dictionary.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def get_dynamic_data(\n    self,\n    project_name: str,\n    folder_entity: dict[str, Any] | None,\n    task_entity: dict[str, Any] | None,\n    variant: str,\n    host_name: str,\n    instance: CreatedInstance | None = None,\n    project_entity: dict[str, Any] | None = None,\n    product_type: str | None = None,\n) -&gt; dict[str, Any]:\n    \"\"\"Dynamic data for product name filling.\n\n    These may be dynamically created based on current context of workfile.\n\n    Default implementation will always return empty dictionary.\n\n    \"\"\"\n    return {}\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.BaseCreator.get_group_label","title":"<code>get_group_label()</code>","text":"<p>Group label under which are instances grouped in UI.</p> Default implementation use attributes in this order <ul> <li>'group_label' -&gt; 'label' -&gt; 'identifier'</li> </ul> <p>Keep in mind that 'identifier' uses 'product_base_type' by default.</p> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Group label that can be used for grouping of instances in UI. Group label can be overridden by the instance itself.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def get_group_label(self) -&gt; str:\n    \"\"\"Group label under which are instances grouped in UI.\n\n    Default implementation use attributes in this order:\n        - 'group_label' -&gt; 'label' -&gt; 'identifier'\n\n    Keep in mind that 'identifier' uses 'product_base_type' by default.\n\n    Returns:\n        str: Group label that can be used for grouping of instances in UI.\n            Group label can be overridden by the instance itself.\n\n    \"\"\"\n    if self._cached_group_label is None:\n        label = self.identifier\n        if self.group_label:\n            label = self.group_label\n        elif self.label:\n            label = self.label\n        self._cached_group_label = label\n    return self._cached_group_label\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.BaseCreator.get_icon","title":"<code>get_icon()</code>","text":"<p>Icon of creator (product base type).</p> <p>Can return path to image file or awesome icon name.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def get_icon(self) -&gt; IconBase | str | None:\n    \"\"\"Icon of creator (product base type).\n\n    Can return path to image file or awesome icon name.\n    \"\"\"\n    return self.icon\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.BaseCreator.get_instance_attr_defs","title":"<code>get_instance_attr_defs()</code>","text":"<p>Plugin attribute definitions.</p> <p>Attribute definitions of plugin that hold data about created instance and values are stored to metadata for future usage and for publishing purposes.</p> Note <p>Convert method should be implemented which should care about updating keys/values when plugin attributes change.</p> <p>Returns:</p> Type Description <code>list[AbstractAttrDef]</code> <p>list[AbstractAttrDef]: Attribute definitions that can be tweaked for created instance.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def get_instance_attr_defs(self) -&gt; list[AbstractAttrDef]:\n    \"\"\"Plugin attribute definitions.\n\n    Attribute definitions of plugin that hold data about created instance\n    and values are stored to metadata for future usage and for publishing\n    purposes.\n\n    Note:\n        Convert method should be implemented which should care about\n        updating keys/values when plugin attributes change.\n\n    Returns:\n        list[AbstractAttrDef]: Attribute definitions that can be tweaked\n            for created instance.\n\n    \"\"\"\n    return self.instance_attr_defs\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.BaseCreator.get_next_versions_for_instances","title":"<code>get_next_versions_for_instances(instances)</code>","text":"<p>Prepare next versions for instances.</p> <p>This is helper method to receive next possible versions for instances. It is using context information on instance to receive them, 'folderPath' and 'product'.</p> <p>Output will contain version by each instance id.</p> <p>Parameters:</p> Name Type Description Default <code>instances</code> <code>list[CreatedInstance]</code> <p>Instances for which to get next versions.</p> required <p>Returns:</p> Type Description <code>dict[str, int]</code> <p>dict[str, int]: Next versions by instance id.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def get_next_versions_for_instances(\n    self, instances: list[CreatedInstance]\n) -&gt; dict[str, int]:\n    \"\"\"Prepare next versions for instances.\n\n    This is helper method to receive next possible versions for instances.\n    It is using context information on instance to receive them,\n    'folderPath' and 'product'.\n\n    Output will contain version by each instance id.\n\n    Args:\n        instances (list[CreatedInstance]): Instances for which to get next\n            versions.\n\n    Returns:\n        dict[str, int]: Next versions by instance id.\n    \"\"\"\n\n    return get_next_versions_for_instances(\n        self.create_context.project_name, instances\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.BaseCreator.get_product_name","title":"<code>get_product_name(project_name, folder_entity, task_entity, variant, host_name=None, instance=None, project_entity=None, product_type=None)</code>","text":"<p>Return product name for passed context.</p> <p>Method is also called on product name update. In that case origin instance is passed in.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>folder_entity</code> <code>dict</code> <p>Folder entity.</p> required <code>task_entity</code> <code>dict | None</code> <p>Task entity.</p> required <code>variant</code> <code>str</code> <p>Product name variant. In most of cases user input.</p> required <code>host_name</code> <code>str | None</code> <p>Which host creates product. Defaults to host name on create context.</p> <code>None</code> <code>instance</code> <code>CreatedInstance | None</code> <p>Object of 'CreatedInstance' for which is product name updated. Passed only on product name update.</p> <code>None</code> <code>project_entity</code> <code>dict[str, Any] | None</code> <p>Project entity.</p> <code>None</code> <code>product_type</code> <code>str | None</code> <p>Product type.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Product name for passed context.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def get_product_name(\n    self,\n    project_name: str,\n    folder_entity: dict[str, Any],\n    task_entity: dict[str, Any] | None,\n    variant: str,\n    host_name: str | None = None,\n    instance: CreatedInstance | None = None,\n    project_entity: dict[str, Any] | None = None,\n    product_type: str | None = None,\n) -&gt; str:\n    \"\"\"Return product name for passed context.\n\n    Method is also called on product name update. In that case origin\n    instance is passed in.\n\n    Args:\n        project_name (str): Project name.\n        folder_entity (dict): Folder entity.\n        task_entity (dict | None): Task entity.\n        variant (str): Product name variant. In most of cases user input.\n        host_name (str | None): Which host creates product. Defaults\n            to host name on create context.\n        instance (CreatedInstance | None): Object of 'CreatedInstance'\n            for which is product name updated. Passed only on product name\n            update.\n        project_entity (dict[str, Any] | None): Project entity.\n        product_type (str | None): Product type.\n\n    Returns:\n        str: Product name for passed context.\n\n    \"\"\"\n    if host_name is None:\n        host_name = self.create_context.host_name\n\n    # Backwards compatibility for create plugins that don't implement\n    #   'product_base_type'.\n    # TODO Remove when 'product_base_type' is required\n    product_base_type = self.product_base_type\n    if not product_base_type:\n        product_base_type = self.product_type\n\n    if product_type is None:\n        for product_type_item in self.get_product_type_items():\n            product_type = product_type_item.product_type\n            break\n        else:\n            product_type = product_base_type\n\n    cur_project_name = self.create_context.get_current_project_name()\n    if not project_entity and project_name == cur_project_name:\n        project_entity = self.create_context.get_current_project_entity()\n\n    args = (\n        project_name,\n        folder_entity,\n        task_entity,\n        variant,\n        host_name,\n    )\n    kwargs = dict(\n        instance=instance,\n        project_entity=project_entity,\n        product_type=product_type,\n    )\n    # NOTE 'project_entity' and 'product_type' were added at the same time\n    #   26/01/19\n    if not is_func_signature_supported(\n        self.get_dynamic_data, *args, **kwargs\n    ):\n        kwargs.pop(\"project_entity\")\n        kwargs.pop(\"product_type\")\n\n    dynamic_data = self.get_dynamic_data(*args, **kwargs)\n\n    return get_product_name(\n        project_name,\n        folder_entity=folder_entity,\n        task_entity=task_entity,\n        product_base_type=product_base_type,\n        product_type=product_type,\n        host_name=host_name,\n        variant=variant,\n        dynamic_data=dynamic_data,\n        project_settings=self.project_settings,\n        project_entity=project_entity,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.BaseCreator.get_product_type_items","title":"<code>get_product_type_items()</code>","text":"<p>Get product type the Creator can work with.</p> <p>By default, it returns <code>product_type_items</code> attribute value that can be set by Creator settings. This can be overridden to provide different source.</p> <p>Product type items are list of ProductTypeItem that Creator can create. Label is used in UI to show user-friendly name. This dataclass can be easily expanded with data in the future. New fields must have default values to not break existing implementations.</p> <p>Returns:</p> Type Description <code>list[ProductTypeItem]</code> <p>list[ProductTypeItem]: List of product type items.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def get_product_type_items(self) -&gt; list[ProductTypeItem]:\n    \"\"\"Get product type the Creator can work with.\n\n    By default, it returns `product_type_items` attribute value that\n    can be set by Creator settings. This can be overridden to provide\n    different source.\n\n    Product type items are list of ProductTypeItem that\n    Creator can create. Label is used in UI to show user-friendly name.\n    This dataclass can be easily expanded with data in the future. New\n    fields must have default values to not break existing implementations.\n\n    Returns:\n        list[ProductTypeItem]: List of product type items.\n\n    \"\"\"\n    return self.product_type_items\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.BaseCreator.register_callbacks","title":"<code>register_callbacks()</code>","text":"<p>Register callbacks for creator.</p> <p>Default implementation does nothing. It can be overridden to register callbacks for creator.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def register_callbacks(self) -&gt; None:\n    \"\"\"Register callbacks for creator.\n\n    Default implementation does nothing. It can be overridden to register\n    callbacks for creator.\n    \"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.BaseCreator.remove_instances","title":"<code>remove_instances(instances)</code>  <code>abstractmethod</code>","text":"<p>Method called on instance removal.</p> <p>Can also remove instance metadata from context but should return 'True' if did so.</p> <p>Parameters:</p> Name Type Description Default <code>instances</code> <code>list[CreatedInstance]</code> <p>Instance objects which should be removed.</p> required Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>@abstractmethod\ndef remove_instances(\n    self, instances: list[CreatedInstance]\n) -&gt; None:\n    \"\"\"Method called on instance removal.\n\n    Can also remove instance metadata from context but should return\n    'True' if did so.\n\n    Args:\n        instances (list[CreatedInstance]): Instance objects which should be\n            removed.\n    \"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.BaseCreator.set_instance_thumbnail_path","title":"<code>set_instance_thumbnail_path(instance_id, thumbnail_path=None)</code>","text":"<p>Set path to thumbnail for instance.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def set_instance_thumbnail_path(\n    self,\n    instance_id: str,\n    thumbnail_path: str | None = None,\n) -&gt; None:\n    \"\"\"Set path to thumbnail for instance.\"\"\"\n\n    self.create_context.thumbnail_paths_by_instance_id[instance_id] = (\n        thumbnail_path\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.BaseCreator.update_instances","title":"<code>update_instances(update_list)</code>  <code>abstractmethod</code>","text":"<p>Store changes of existing instances so they can be recollected.</p> <p>Parameters:</p> Name Type Description Default <code>update_list</code> <code>list[UpdateData]</code> <p>Gets list of tuples. Each item contain changed instance and it's changes.</p> required Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>@abstractmethod\ndef update_instances(\n    self, update_list: list[UpdateData]\n) -&gt; None:\n    \"\"\"Store changes of existing instances so they can be recollected.\n\n    Args:\n        update_list (list[UpdateData]): Gets list of tuples. Each item\n            contain changed instance and it's changes.\n    \"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.CreatedInstance","title":"<code>CreatedInstance</code>","text":"<p>Instance entity with data that will be stored to workfile.</p> <p>I think <code>data</code> must be required argument containing all minimum information about instance like \"folderPath\" and \"task\" and all data used for filling product name as creators may have custom data for product name filling.</p> Notes <p>Object have 2 possible initialization. One using 'creator' object which     is recommended for api usage. Second by passing information about     creator.</p> <p>Parameters:</p> Name Type Description Default <code>product_type</code> <code>str</code> <p>Product type that will be created.</p> required <code>product_name</code> <code>str</code> <p>Name of product that will be created.</p> required <code>data</code> <code>Dict[str, Any]</code> <p>Data used for filling product name or override data from already existing instance.</p> required <code>creator</code> <code>BaseCreator</code> <p>Creator responsible for instance.</p> required <code>product_base_type</code> <code>Optional[str]</code> <p>Product base type that will be created. If not provided then product base type is taken from creator plugin. If creator does not have product base type then deprecation warning is raised.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>class CreatedInstance:\n    \"\"\"Instance entity with data that will be stored to workfile.\n\n    I think `data` must be required argument containing all minimum information\n    about instance like \"folderPath\" and \"task\" and all data used for filling\n    product name as creators may have custom data for product name filling.\n\n    Notes:\n        Object have 2 possible initialization. One using 'creator' object which\n            is recommended for api usage. Second by passing information about\n            creator.\n\n    Args:\n        product_type (str): Product type that will be created.\n        product_name (str): Name of product that will be created.\n        data (Dict[str, Any]): Data used for filling product name or override\n            data from already existing instance.\n        creator (BaseCreator): Creator responsible for instance.\n        product_base_type (Optional[str]): Product base type that will be\n            created. If not provided then product base type is taken from\n            creator plugin. If creator does not have product base type then\n            deprecation warning is raised.\n    \"\"\"\n\n    # Keys that can't be changed or removed from data after loading using\n    #   creator.\n    # - 'creator_attributes' and 'publish_attributes' can change values of\n    #   their individual children but not on their own\n    __immutable_keys = (\n        \"id\",\n        \"instance_id\",\n        \"productType\",\n        \"productBaseType\",\n        \"creator_identifier\",\n        \"creator_attributes\",\n        \"publish_attributes\"\n    )\n    # Keys that can be changed, but should not be removed from instance\n    __required_keys = {\n        \"folderPath\": None,\n        \"task\": None,\n        \"productName\": None,\n        \"active\": True,\n    }\n\n    def __init__(\n        self,\n        product_type: str,\n        product_name: str,\n        data: Dict[str, Any],\n        creator: \"BaseCreator\",\n        transient_data: Optional[Dict[str, Any]] = None,\n        product_base_type: Optional[str] = None\n    ):\n        \"\"\"Initialize CreatedInstance.\"\"\"\n        # fallback to product type for backward compatibility\n        if not product_base_type:\n            product_base_type = creator.product_base_type or product_type\n\n        self._creator = creator\n        creator_identifier = creator.identifier\n        group_label = creator.get_group_label()\n        creator_label = creator.label\n\n        self._creator_label = creator_label\n        self._group_label = group_label or creator_identifier\n\n        # Instance members may have actions on them\n        # TODO implement members logic\n        self._members = []\n\n        # Data that can be used for lifetime of object\n        if transient_data is None:\n            transient_data = {}\n        self._transient_data = transient_data\n        self._is_mandatory: bool = False\n        self._parent_instance_id: Optional[str] = None\n        self._parent_flags: int = 0\n\n        # Create a copy of passed data to avoid changing them on the fly\n        data = copy.deepcopy(data or {})\n\n        # Pop dictionary values that will be converted to objects to be able\n        #   catch changes\n        orig_creator_attributes = data.pop(\"creator_attributes\", None) or {}\n        orig_publish_attributes = data.pop(\"publish_attributes\", None) or {}\n\n        # Store original value of passed data\n        self._orig_data = copy.deepcopy(data)\n\n        # Pop 'productType' and 'productName' to prevent unexpected changes\n        data.pop(\"productType\", None)\n        data.pop(\"productName\", None)\n        # Backwards compatibility with OpenPype instances\n        data.pop(\"family\", None)\n        data.pop(\"subset\", None)\n\n        asset_name = data.pop(\"asset\", None)\n        if \"folderPath\" not in data:\n            data[\"folderPath\"] = asset_name\n\n        # QUESTION Does it make sense to have data stored as ordered dict?\n        self._data = collections.OrderedDict()\n        # QUESTION Do we need this \"id\" information on instance?\n        item_id = data.get(\"id\")\n        # TODO use only 'AYON_INSTANCE_ID' when all hosts support it\n        if item_id not in {AYON_INSTANCE_ID, AVALON_INSTANCE_ID}:\n            item_id = AYON_INSTANCE_ID\n        self._data[\"id\"] = item_id\n        self._data[\"productType\"] = product_type\n        self._data[\"productName\"] = product_name\n\n        self._data[\"productBaseType\"] = product_base_type\n\n        self._data[\"active\"] = data.get(\"active\", True)\n        self._data[\"creator_identifier\"] = creator_identifier\n\n        # Pop from source data all keys that are defined in `_data` before\n        #   this moment and through their values away\n        # - they should be the same and if are not then should not change\n        #   already set values\n        for key in self._data.keys():\n            if key in data:\n                data.pop(key)\n\n        self._data[\"variant\"] = self._data.get(\"variant\") or \"\"\n        # Stored creator specific attribute values\n        # {key: value}\n        creator_values = copy.deepcopy(orig_creator_attributes)\n\n        self._data[\"creator_attributes\"] = creator_values\n\n        # Stored publish specific attribute values\n        # {&lt;plugin name&gt;: {key: value}}\n        self._data[\"publish_attributes\"] = PublishAttributes(\n            self, orig_publish_attributes\n        )\n        if data:\n            self._data.update(data)\n\n        for key, default in self.__required_keys.items():\n            self._data.setdefault(key, default)\n\n        if not self._data.get(\"instance_id\"):\n            self._data[\"instance_id\"] = str(uuid4())\n\n        creator_attr_defs = creator.get_attr_defs_for_instance(self)\n        self.set_create_attr_defs(\n            creator_attr_defs, creator_values\n        )\n\n    def __str__(self):\n        return (\n            \"&lt;CreatedInstance {product[name]}\"\n            \" ({product[type]}[{creator_identifier}])&gt; {data}\"\n        ).format(\n            creator_identifier=self.creator_identifier,\n            product={\"name\": self.product_name, \"type\": self.product_type},\n            data=str(self._data)\n        )\n\n    # --- Dictionary like methods ---\n    def __getitem__(self, key):\n        return self._data[key]\n\n    def __contains__(self, key):\n        return key in self._data\n\n    def __setitem__(self, key, value):\n        # Validate immutable keys\n        if key in self.__immutable_keys:\n            if value == self._data.get(key):\n                return\n            # Raise exception if key is immutable and value has changed\n            raise ImmutableKeyError(key)\n\n        if key in self._data and self._data[key] == value:\n            return\n\n        if self.is_mandatory and key == \"active\" and value is not True:\n            raise ImmutableKeyError(\n                key,\n                \"Instance is mandatory and can't be disabled.\"\n            )\n\n        self._data[key] = value\n        self._create_context.instance_values_changed(\n            self.id, {key: value}\n        )\n\n    def get(self, key, default=None):\n        return self._data.get(key, default)\n\n    def pop(self, key, *args, **kwargs):\n        # Raise exception if is trying to pop key which is immutable\n        if key in self.__immutable_keys:\n            raise ImmutableKeyError(key)\n\n        has_key = key in self._data\n        output = self._data.pop(key, *args, **kwargs)\n        if has_key:\n            if key in self.__required_keys:\n                self._data[key] = self.__required_keys[key]\n            self._create_context.instance_values_changed(\n                self.id, {key: None}\n            )\n        return output\n\n    def keys(self):\n        return self._data.keys()\n\n    def values(self):\n        return self._data.values()\n\n    def items(self):\n        return self._data.items()\n    # ------\n\n    @property\n    def product_base_type(self) -&gt; str:\n        return self._data[\"productBaseType\"]\n\n    @property\n    def product_type(self) -&gt; str:\n        return self._data[\"productType\"]\n\n    @property\n    def product_name(self) -&gt; str:\n        return self._data[\"productName\"]\n\n    @property\n    def label(self) -&gt; str:\n        label = self._data.get(\"label\")\n        if not label:\n            label = self.product_name\n        return label\n\n    @property\n    def group_label(self) -&gt; str:\n        label = self._data.get(\"group\")\n        if label:\n            return label\n        return self._group_label\n\n    @property\n    def origin_data(self):\n        output = copy.deepcopy(self._orig_data)\n        output[\"creator_attributes\"] = self.creator_attributes.origin_data\n        output[\"publish_attributes\"] = self.publish_attributes.origin_data\n        return output\n\n    @property\n    def creator_identifier(self):\n        return self._data[\"creator_identifier\"]\n\n    @property\n    def creator_label(self):\n        return self._creator.label or self.creator_identifier\n\n    @property\n    def id(self):\n        \"\"\"Instance identifier.\n\n        Returns:\n            str: UUID of instance.\n        \"\"\"\n\n        return self._data[\"instance_id\"]\n\n    @property\n    def data(self):\n        \"\"\"Legacy access to data.\n\n        Access to data is needed to modify values.\n\n        Returns:\n            CreatedInstance: Object can be used as dictionary but with\n                validations of immutable keys.\n        \"\"\"\n\n        return self\n\n    @property\n    def transient_data(self):\n        \"\"\"Data stored for lifetime of instance object.\n\n        These data are not stored to scene and will be lost on object\n        deletion.\n\n        Can be used to store objects. In some host implementations is not\n        possible to reference to object in scene with some unique identifier\n        (e.g. node in Fusion.). In that case it is handy to store the object\n        here. Should be used that way only if instance data are stored on the\n        node itself.\n\n        Returns:\n            Dict[str, Any]: Dictionary object where you can store data related\n                to instance for lifetime of instance object.\n        \"\"\"\n\n        return self._transient_data\n\n    @property\n    def is_mandatory(self) -&gt; bool:\n        \"\"\"Check if instance is mandatory.\n\n        Returns:\n            bool: True if instance is mandatory, False otherwise.\n\n        \"\"\"\n        return self._is_mandatory\n\n    def set_mandatory(self, value: bool) -&gt; None:\n        \"\"\"Set instance as mandatory or not.\n\n        Mandatory instance can't be disabled in UI.\n\n        Args:\n            value (bool): True if instance should be mandatory, False\n                otherwise.\n\n        \"\"\"\n        if value is self._is_mandatory:\n            return\n        self._is_mandatory = value\n        if value is True:\n            self[\"active\"] = True\n        self._create_context.instance_requirement_changed(self.id)\n\n    @property\n    def parent_instance_id(self) -&gt; Optional[str]:\n        return self._parent_instance_id\n\n    @property\n    def parent_flags(self) -&gt; int:\n        return self._parent_flags\n\n    def set_parent(\n        self, instance_id: Optional[str], flags: int\n    ) -&gt; None:\n        \"\"\"Set parent instance id and parenting flags.\n\n        Args:\n            instance_id (Optional[str]): Parent instance id.\n            flags (int): Parenting flags.\n\n        \"\"\"\n        changed = False\n        if instance_id != self._parent_instance_id:\n            changed = True\n            self._parent_instance_id = instance_id\n\n        if flags is None:\n            flags = 0\n\n        if self._parent_flags != flags:\n            self._parent_flags = flags\n            changed = True\n\n        if changed:\n            self._create_context.instance_parent_changed(self.id)\n\n    def changes(self):\n        \"\"\"Calculate and return changes.\"\"\"\n\n        return TrackChangesItem(self.origin_data, self.data_to_store())\n\n    def mark_as_stored(self):\n        \"\"\"Should be called when instance data are stored.\n\n        Origin data are replaced by current data so changes are cleared.\n        \"\"\"\n\n        orig_keys = set(self._orig_data.keys())\n        for key, value in self._data.items():\n            orig_keys.discard(key)\n            if key in (\"creator_attributes\", \"publish_attributes\"):\n                continue\n            self._orig_data[key] = copy.deepcopy(value)\n\n        for key in orig_keys:\n            self._orig_data.pop(key)\n\n        self.creator_attributes.mark_as_stored()\n        self.publish_attributes.mark_as_stored()\n\n    @property\n    def creator_attributes(self) -&gt; CreatorAttributeValues:\n        return self._data[\"creator_attributes\"]\n\n    @property\n    def creator_attribute_defs(self) -&gt; list[AbstractAttrDef]:\n        \"\"\"Attribute definitions defined by creator plugin.\n\n        Returns:\n              list[AbstractAttrDef]: Attribute definitions.\n\n        \"\"\"\n        return self.creator_attributes.attr_defs\n\n    @property\n    def publish_attributes(self) -&gt; PublishAttributes:\n        return self._data[\"publish_attributes\"]\n\n    @property\n    def has_promised_context(self) -&gt; bool:\n        \"\"\"Get context data that are promised to be set by creator.\n\n        Returns:\n            bool: Has context that won't bo validated. Artist can't change\n                value when set to True.\n\n        \"\"\"\n        return self._transient_data.get(\"has_promised_context\", False)\n\n    def data_to_store(self):\n        \"\"\"Collect data that contain json parsable types.\n\n        It is possible to recreate the instance using these data.\n\n        Todos:\n            We probably don't need OrderedDict. When data are loaded they\n                are not ordered anymore.\n\n        Returns:\n            OrderedDict: Ordered dictionary with instance data.\n        \"\"\"\n\n        output = collections.OrderedDict()\n        for key, value in self._data.items():\n            if key in (\"creator_attributes\", \"publish_attributes\"):\n                continue\n            output[key] = value\n\n        if isinstance(self.creator_attributes, AttributeValues):\n            creator_attributes = self.creator_attributes.data_to_store()\n        else:\n            creator_attributes = copy.deepcopy(self.creator_attributes)\n        output[\"creator_attributes\"] = creator_attributes\n        output[\"publish_attributes\"] = self.publish_attributes.data_to_store()\n\n        return output\n\n    def set_create_attr_defs(self, attr_defs, value=None):\n        \"\"\"Create plugin updates create attribute definitions.\n\n        Method called by create plugin when attribute definitions should\n            be changed.\n\n        Args:\n            attr_defs (List[AbstractAttrDef]): Attribute definitions.\n            value (Optional[Dict[str, Any]]): Values of attribute definitions.\n                Current values are used if not passed in.\n\n        \"\"\"\n        if value is None:\n            value = self._data[\"creator_attributes\"]\n\n        if isinstance(value, AttributeValues):\n            value = value.data_to_store()\n\n        if isinstance(self._data[\"creator_attributes\"], AttributeValues):\n            origin_data = self._data[\"creator_attributes\"].origin_data\n        else:\n            origin_data = copy.deepcopy(self._data[\"creator_attributes\"])\n        self._data[\"creator_attributes\"] = CreatorAttributeValues(\n            self,\n            \"creator_attributes\",\n            attr_defs,\n            value,\n            origin_data\n        )\n        self._create_context.instance_create_attr_defs_changed(self.id)\n\n    @classmethod\n    def from_existing(\n        cls,\n        instance_data: Dict[str, Any],\n        creator: \"BaseCreator\",\n        transient_data: Optional[Dict[str, Any]] = None,\n    ) -&gt; \"CreatedInstance\":\n        \"\"\"Convert instance data from workfile to CreatedInstance.\n\n        Args:\n            instance_data (Dict[str, Any]): Data in a structure ready for\n                'CreatedInstance' object.\n            creator (BaseCreator): Creator plugin which is creating the\n                instance of for which the instance belongs.\n            transient_data (Optional[dict[str, Any]]): Instance transient\n                data.\n\n        Returns:\n            CreatedInstance: Instance object.\n\n        \"\"\"\n        instance_data = copy.deepcopy(instance_data)\n\n        product_type = instance_data.get(\"productType\")\n        product_base_type = (\n            instance_data.get(\"productBaseType\")\n            or product_type\n            or instance_data.get(\"family\")\n            or creator.product_base_type\n        )\n        if product_type is None:\n            product_type = product_base_type\n\n        product_name = instance_data.get(\"productName\")\n        if product_name is None:\n            product_name = instance_data.get(\"subset\")\n\n        return cls(\n            product_base_type=product_base_type,\n            product_type=product_type,\n            product_name=product_name,\n            data=instance_data,\n            creator=creator,\n            transient_data=transient_data,\n        )\n\n    def attribute_value_changed(self, key, changes):\n        \"\"\"A value changed.\n\n        Args:\n            key (str): Key of attribute values.\n            changes (Dict[str, Any]): Changes in values.\n\n        \"\"\"\n        self._create_context.instance_values_changed(self.id, {key: changes})\n\n    def set_publish_plugin_attr_defs(self, plugin_name, attr_defs):\n        \"\"\"Set attribute definitions for publish plugin.\n\n        Args:\n            plugin_name(str): Name of publish plugin.\n            attr_defs(List[AbstractAttrDef]): Attribute definitions.\n\n        \"\"\"\n        self.publish_attributes.set_publish_plugin_attr_defs(\n            plugin_name, attr_defs\n        )\n        self._create_context.instance_publish_attr_defs_changed(\n            self.id, plugin_name\n        )\n\n    def publish_attribute_value_changed(self, plugin_name, value):\n        \"\"\"Method called from PublishAttributes.\n\n        Args:\n            plugin_name (str): Plugin name.\n            value (Dict[str, Any]): Changes in values for the plugin.\n\n        \"\"\"\n        self._create_context.instance_values_changed(\n            self.id,\n            {\n                \"publish_attributes\": {\n                    plugin_name: value,\n                },\n            },\n        )\n\n    def add_members(self, members):\n        \"\"\"Currently unused method.\"\"\"\n\n        for member in members:\n            if member not in self._members:\n                self._members.append(member)\n\n    @property\n    def _create_context(self):\n        \"\"\"Get create context.\n\n        Returns:\n            CreateContext: Context object which wraps object.\n\n        \"\"\"\n        return self._creator.create_context\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.CreatedInstance.creator_attribute_defs","title":"<code>creator_attribute_defs</code>  <code>property</code>","text":"<p>Attribute definitions defined by creator plugin.</p> <p>Returns:</p> Type Description <code>list[AbstractAttrDef]</code> <p>list[AbstractAttrDef]: Attribute definitions.</p>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.CreatedInstance.data","title":"<code>data</code>  <code>property</code>","text":"<p>Legacy access to data.</p> <p>Access to data is needed to modify values.</p> <p>Returns:</p> Name Type Description <code>CreatedInstance</code> <p>Object can be used as dictionary but with validations of immutable keys.</p>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.CreatedInstance.has_promised_context","title":"<code>has_promised_context</code>  <code>property</code>","text":"<p>Get context data that are promised to be set by creator.</p> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>Has context that won't bo validated. Artist can't change value when set to True.</p>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.CreatedInstance.id","title":"<code>id</code>  <code>property</code>","text":"<p>Instance identifier.</p> <p>Returns:</p> Name Type Description <code>str</code> <p>UUID of instance.</p>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.CreatedInstance.is_mandatory","title":"<code>is_mandatory</code>  <code>property</code>","text":"<p>Check if instance is mandatory.</p> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if instance is mandatory, False otherwise.</p>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.CreatedInstance.transient_data","title":"<code>transient_data</code>  <code>property</code>","text":"<p>Data stored for lifetime of instance object.</p> <p>These data are not stored to scene and will be lost on object deletion.</p> <p>Can be used to store objects. In some host implementations is not possible to reference to object in scene with some unique identifier (e.g. node in Fusion.). In that case it is handy to store the object here. Should be used that way only if instance data are stored on the node itself.</p> <p>Returns:</p> Type Description <p>Dict[str, Any]: Dictionary object where you can store data related to instance for lifetime of instance object.</p>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.CreatedInstance.__init__","title":"<code>__init__(product_type, product_name, data, creator, transient_data=None, product_base_type=None)</code>","text":"<p>Initialize CreatedInstance.</p> Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>def __init__(\n    self,\n    product_type: str,\n    product_name: str,\n    data: Dict[str, Any],\n    creator: \"BaseCreator\",\n    transient_data: Optional[Dict[str, Any]] = None,\n    product_base_type: Optional[str] = None\n):\n    \"\"\"Initialize CreatedInstance.\"\"\"\n    # fallback to product type for backward compatibility\n    if not product_base_type:\n        product_base_type = creator.product_base_type or product_type\n\n    self._creator = creator\n    creator_identifier = creator.identifier\n    group_label = creator.get_group_label()\n    creator_label = creator.label\n\n    self._creator_label = creator_label\n    self._group_label = group_label or creator_identifier\n\n    # Instance members may have actions on them\n    # TODO implement members logic\n    self._members = []\n\n    # Data that can be used for lifetime of object\n    if transient_data is None:\n        transient_data = {}\n    self._transient_data = transient_data\n    self._is_mandatory: bool = False\n    self._parent_instance_id: Optional[str] = None\n    self._parent_flags: int = 0\n\n    # Create a copy of passed data to avoid changing them on the fly\n    data = copy.deepcopy(data or {})\n\n    # Pop dictionary values that will be converted to objects to be able\n    #   catch changes\n    orig_creator_attributes = data.pop(\"creator_attributes\", None) or {}\n    orig_publish_attributes = data.pop(\"publish_attributes\", None) or {}\n\n    # Store original value of passed data\n    self._orig_data = copy.deepcopy(data)\n\n    # Pop 'productType' and 'productName' to prevent unexpected changes\n    data.pop(\"productType\", None)\n    data.pop(\"productName\", None)\n    # Backwards compatibility with OpenPype instances\n    data.pop(\"family\", None)\n    data.pop(\"subset\", None)\n\n    asset_name = data.pop(\"asset\", None)\n    if \"folderPath\" not in data:\n        data[\"folderPath\"] = asset_name\n\n    # QUESTION Does it make sense to have data stored as ordered dict?\n    self._data = collections.OrderedDict()\n    # QUESTION Do we need this \"id\" information on instance?\n    item_id = data.get(\"id\")\n    # TODO use only 'AYON_INSTANCE_ID' when all hosts support it\n    if item_id not in {AYON_INSTANCE_ID, AVALON_INSTANCE_ID}:\n        item_id = AYON_INSTANCE_ID\n    self._data[\"id\"] = item_id\n    self._data[\"productType\"] = product_type\n    self._data[\"productName\"] = product_name\n\n    self._data[\"productBaseType\"] = product_base_type\n\n    self._data[\"active\"] = data.get(\"active\", True)\n    self._data[\"creator_identifier\"] = creator_identifier\n\n    # Pop from source data all keys that are defined in `_data` before\n    #   this moment and through their values away\n    # - they should be the same and if are not then should not change\n    #   already set values\n    for key in self._data.keys():\n        if key in data:\n            data.pop(key)\n\n    self._data[\"variant\"] = self._data.get(\"variant\") or \"\"\n    # Stored creator specific attribute values\n    # {key: value}\n    creator_values = copy.deepcopy(orig_creator_attributes)\n\n    self._data[\"creator_attributes\"] = creator_values\n\n    # Stored publish specific attribute values\n    # {&lt;plugin name&gt;: {key: value}}\n    self._data[\"publish_attributes\"] = PublishAttributes(\n        self, orig_publish_attributes\n    )\n    if data:\n        self._data.update(data)\n\n    for key, default in self.__required_keys.items():\n        self._data.setdefault(key, default)\n\n    if not self._data.get(\"instance_id\"):\n        self._data[\"instance_id\"] = str(uuid4())\n\n    creator_attr_defs = creator.get_attr_defs_for_instance(self)\n    self.set_create_attr_defs(\n        creator_attr_defs, creator_values\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.CreatedInstance.add_members","title":"<code>add_members(members)</code>","text":"<p>Currently unused method.</p> Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>def add_members(self, members):\n    \"\"\"Currently unused method.\"\"\"\n\n    for member in members:\n        if member not in self._members:\n            self._members.append(member)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.CreatedInstance.attribute_value_changed","title":"<code>attribute_value_changed(key, changes)</code>","text":"<p>A value changed.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>Key of attribute values.</p> required <code>changes</code> <code>Dict[str, Any]</code> <p>Changes in values.</p> required Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>def attribute_value_changed(self, key, changes):\n    \"\"\"A value changed.\n\n    Args:\n        key (str): Key of attribute values.\n        changes (Dict[str, Any]): Changes in values.\n\n    \"\"\"\n    self._create_context.instance_values_changed(self.id, {key: changes})\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.CreatedInstance.changes","title":"<code>changes()</code>","text":"<p>Calculate and return changes.</p> Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>def changes(self):\n    \"\"\"Calculate and return changes.\"\"\"\n\n    return TrackChangesItem(self.origin_data, self.data_to_store())\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.CreatedInstance.data_to_store","title":"<code>data_to_store()</code>","text":"<p>Collect data that contain json parsable types.</p> <p>It is possible to recreate the instance using these data.</p> Todos <p>We probably don't need OrderedDict. When data are loaded they     are not ordered anymore.</p> <p>Returns:</p> Name Type Description <code>OrderedDict</code> <p>Ordered dictionary with instance data.</p> Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>def data_to_store(self):\n    \"\"\"Collect data that contain json parsable types.\n\n    It is possible to recreate the instance using these data.\n\n    Todos:\n        We probably don't need OrderedDict. When data are loaded they\n            are not ordered anymore.\n\n    Returns:\n        OrderedDict: Ordered dictionary with instance data.\n    \"\"\"\n\n    output = collections.OrderedDict()\n    for key, value in self._data.items():\n        if key in (\"creator_attributes\", \"publish_attributes\"):\n            continue\n        output[key] = value\n\n    if isinstance(self.creator_attributes, AttributeValues):\n        creator_attributes = self.creator_attributes.data_to_store()\n    else:\n        creator_attributes = copy.deepcopy(self.creator_attributes)\n    output[\"creator_attributes\"] = creator_attributes\n    output[\"publish_attributes\"] = self.publish_attributes.data_to_store()\n\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.CreatedInstance.from_existing","title":"<code>from_existing(instance_data, creator, transient_data=None)</code>  <code>classmethod</code>","text":"<p>Convert instance data from workfile to CreatedInstance.</p> <p>Parameters:</p> Name Type Description Default <code>instance_data</code> <code>Dict[str, Any]</code> <p>Data in a structure ready for 'CreatedInstance' object.</p> required <code>creator</code> <code>BaseCreator</code> <p>Creator plugin which is creating the instance of for which the instance belongs.</p> required <code>transient_data</code> <code>Optional[dict[str, Any]]</code> <p>Instance transient data.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>CreatedInstance</code> <code>'CreatedInstance'</code> <p>Instance object.</p> Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>@classmethod\ndef from_existing(\n    cls,\n    instance_data: Dict[str, Any],\n    creator: \"BaseCreator\",\n    transient_data: Optional[Dict[str, Any]] = None,\n) -&gt; \"CreatedInstance\":\n    \"\"\"Convert instance data from workfile to CreatedInstance.\n\n    Args:\n        instance_data (Dict[str, Any]): Data in a structure ready for\n            'CreatedInstance' object.\n        creator (BaseCreator): Creator plugin which is creating the\n            instance of for which the instance belongs.\n        transient_data (Optional[dict[str, Any]]): Instance transient\n            data.\n\n    Returns:\n        CreatedInstance: Instance object.\n\n    \"\"\"\n    instance_data = copy.deepcopy(instance_data)\n\n    product_type = instance_data.get(\"productType\")\n    product_base_type = (\n        instance_data.get(\"productBaseType\")\n        or product_type\n        or instance_data.get(\"family\")\n        or creator.product_base_type\n    )\n    if product_type is None:\n        product_type = product_base_type\n\n    product_name = instance_data.get(\"productName\")\n    if product_name is None:\n        product_name = instance_data.get(\"subset\")\n\n    return cls(\n        product_base_type=product_base_type,\n        product_type=product_type,\n        product_name=product_name,\n        data=instance_data,\n        creator=creator,\n        transient_data=transient_data,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.CreatedInstance.mark_as_stored","title":"<code>mark_as_stored()</code>","text":"<p>Should be called when instance data are stored.</p> <p>Origin data are replaced by current data so changes are cleared.</p> Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>def mark_as_stored(self):\n    \"\"\"Should be called when instance data are stored.\n\n    Origin data are replaced by current data so changes are cleared.\n    \"\"\"\n\n    orig_keys = set(self._orig_data.keys())\n    for key, value in self._data.items():\n        orig_keys.discard(key)\n        if key in (\"creator_attributes\", \"publish_attributes\"):\n            continue\n        self._orig_data[key] = copy.deepcopy(value)\n\n    for key in orig_keys:\n        self._orig_data.pop(key)\n\n    self.creator_attributes.mark_as_stored()\n    self.publish_attributes.mark_as_stored()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.CreatedInstance.publish_attribute_value_changed","title":"<code>publish_attribute_value_changed(plugin_name, value)</code>","text":"<p>Method called from PublishAttributes.</p> <p>Parameters:</p> Name Type Description Default <code>plugin_name</code> <code>str</code> <p>Plugin name.</p> required <code>value</code> <code>Dict[str, Any]</code> <p>Changes in values for the plugin.</p> required Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>def publish_attribute_value_changed(self, plugin_name, value):\n    \"\"\"Method called from PublishAttributes.\n\n    Args:\n        plugin_name (str): Plugin name.\n        value (Dict[str, Any]): Changes in values for the plugin.\n\n    \"\"\"\n    self._create_context.instance_values_changed(\n        self.id,\n        {\n            \"publish_attributes\": {\n                plugin_name: value,\n            },\n        },\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.CreatedInstance.set_create_attr_defs","title":"<code>set_create_attr_defs(attr_defs, value=None)</code>","text":"<p>Create plugin updates create attribute definitions.</p> <p>Method called by create plugin when attribute definitions should     be changed.</p> <p>Parameters:</p> Name Type Description Default <code>attr_defs</code> <code>List[AbstractAttrDef]</code> <p>Attribute definitions.</p> required <code>value</code> <code>Optional[Dict[str, Any]]</code> <p>Values of attribute definitions. Current values are used if not passed in.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>def set_create_attr_defs(self, attr_defs, value=None):\n    \"\"\"Create plugin updates create attribute definitions.\n\n    Method called by create plugin when attribute definitions should\n        be changed.\n\n    Args:\n        attr_defs (List[AbstractAttrDef]): Attribute definitions.\n        value (Optional[Dict[str, Any]]): Values of attribute definitions.\n            Current values are used if not passed in.\n\n    \"\"\"\n    if value is None:\n        value = self._data[\"creator_attributes\"]\n\n    if isinstance(value, AttributeValues):\n        value = value.data_to_store()\n\n    if isinstance(self._data[\"creator_attributes\"], AttributeValues):\n        origin_data = self._data[\"creator_attributes\"].origin_data\n    else:\n        origin_data = copy.deepcopy(self._data[\"creator_attributes\"])\n    self._data[\"creator_attributes\"] = CreatorAttributeValues(\n        self,\n        \"creator_attributes\",\n        attr_defs,\n        value,\n        origin_data\n    )\n    self._create_context.instance_create_attr_defs_changed(self.id)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.CreatedInstance.set_mandatory","title":"<code>set_mandatory(value)</code>","text":"<p>Set instance as mandatory or not.</p> <p>Mandatory instance can't be disabled in UI.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>bool</code> <p>True if instance should be mandatory, False otherwise.</p> required Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>def set_mandatory(self, value: bool) -&gt; None:\n    \"\"\"Set instance as mandatory or not.\n\n    Mandatory instance can't be disabled in UI.\n\n    Args:\n        value (bool): True if instance should be mandatory, False\n            otherwise.\n\n    \"\"\"\n    if value is self._is_mandatory:\n        return\n    self._is_mandatory = value\n    if value is True:\n        self[\"active\"] = True\n    self._create_context.instance_requirement_changed(self.id)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.CreatedInstance.set_parent","title":"<code>set_parent(instance_id, flags)</code>","text":"<p>Set parent instance id and parenting flags.</p> <p>Parameters:</p> Name Type Description Default <code>instance_id</code> <code>Optional[str]</code> <p>Parent instance id.</p> required <code>flags</code> <code>int</code> <p>Parenting flags.</p> required Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>def set_parent(\n    self, instance_id: Optional[str], flags: int\n) -&gt; None:\n    \"\"\"Set parent instance id and parenting flags.\n\n    Args:\n        instance_id (Optional[str]): Parent instance id.\n        flags (int): Parenting flags.\n\n    \"\"\"\n    changed = False\n    if instance_id != self._parent_instance_id:\n        changed = True\n        self._parent_instance_id = instance_id\n\n    if flags is None:\n        flags = 0\n\n    if self._parent_flags != flags:\n        self._parent_flags = flags\n        changed = True\n\n    if changed:\n        self._create_context.instance_parent_changed(self.id)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.CreatedInstance.set_publish_plugin_attr_defs","title":"<code>set_publish_plugin_attr_defs(plugin_name, attr_defs)</code>","text":"<p>Set attribute definitions for publish plugin.</p> <p>Parameters:</p> Name Type Description Default <code>plugin_name</code> <code>str</code> <p>Name of publish plugin.</p> required <code>attr_defs</code> <code>List[AbstractAttrDef]</code> <p>Attribute definitions.</p> required Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>def set_publish_plugin_attr_defs(self, plugin_name, attr_defs):\n    \"\"\"Set attribute definitions for publish plugin.\n\n    Args:\n        plugin_name(str): Name of publish plugin.\n        attr_defs(List[AbstractAttrDef]): Attribute definitions.\n\n    \"\"\"\n    self.publish_attributes.set_publish_plugin_attr_defs(\n        plugin_name, attr_defs\n    )\n    self._create_context.instance_publish_attr_defs_changed(\n        self.id, plugin_name\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.Creator","title":"<code>Creator</code>","text":"<p>               Bases: <code>BaseCreator</code></p> <p>Creator that has more information for artist to show in UI.</p> <p>Creation requires prepared product name and instance data.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>class Creator(BaseCreator):\n    \"\"\"Creator that has more information for artist to show in UI.\n\n    Creation requires prepared product name and instance data.\n    \"\"\"\n    skip_discovery = True\n    # GUI Purposes\n    # - default_variants may not be used if `get_default_variants`\n    #   is overridden\n    default_variants: list[str] = []\n\n    # Default variant used in 'get_default_variant'\n    _default_variant: str | None = None\n\n    # Short description of product base type\n    # - may not be used if `get_description` is overridden\n    description: str | None = None\n\n    # Detailed description of product base type for artists\n    # - may not be used if `get_detail_description` is overridden\n    detailed_description: str | None = None\n\n    # It does make sense to change context on creation\n    # - in some cases it may confuse artists because it would not be used\n    #      e.g. for buld creators\n    create_allow_context_change: bool = True\n    # A thumbnail can be passed in precreate attributes\n    # - if is set to True is should expect that a thumbnail path under key\n    #   PRE_CREATE_THUMBNAIL_KEY can be sent in data with precreate data\n    # - is disabled by default because the feature was added in later stages\n    #   and creators who would not expect PRE_CREATE_THUMBNAIL_KEY could\n    #   cause issues with instance data\n    create_allow_thumbnail: bool = False\n\n    # Precreate attribute definitions showed before creation\n    # - similar to instance attribute definitions\n    pre_create_attr_defs: list[AbstractAttrDef] = []\n\n    def __init__(self, *args, **kwargs):\n        cls = self.__class__\n\n        # Fix backwards compatibility for plugins which override\n        #   'default_variant' attribute directly\n        if not isinstance(cls.default_variant, property):\n            # Move value from 'default_variant' to '_default_variant'\n            self._default_variant = self.default_variant\n            # Create property 'default_variant' on the class\n            cls.default_variant = property(\n                cls._get_default_variant_wrap,\n                cls._set_default_variant_wrap\n            )\n        super().__init__(*args, **kwargs)\n\n    @property\n    def show_order(self) -&gt; int:\n        \"\"\"Order in which is creator shown in UI.\n\n        Returns:\n            int: Order in which is creator shown (less == earlier). By default\n                is using Creator's 'order' or processing.\n        \"\"\"\n        return self.order\n\n    @abstractmethod\n    def create(\n        self,\n        product_name: str,\n        instance_data: dict[str, Any],\n        pre_create_data: dict[str, Any],\n    ) -&gt; Any:\n        \"\"\"Create new instance and store it.\n\n        Ideally should be stored to workfile using host implementation.\n\n        Args:\n            product_name (str): Product name of created instance.\n            instance_data (dict[str, Any]): Base data for instance.\n            pre_create_data (dict[str, Any]): Data based on pre creation\n                attributes. Those may affect how creator works.\n        \"\"\"\n\n    def get_description(self):\n        \"\"\"Short description of product base type and plugin.\n\n        Returns:\n            str: Short description of product base type.\n\n        \"\"\"\n        return self.description\n\n    def get_detail_description(self) -&gt; str | None:\n        \"\"\"Description of product base type and plugin.\n\n        Can be detailed with markdown or html tags.\n\n        Returns:\n            str: Detailed description of product base type for artist.\n\n        \"\"\"\n        return self.detailed_description\n\n    def get_default_variants(self) -&gt; list[str]:\n        \"\"\"Default variant values for UI tooltips.\n\n        Replacement of `default_variants` attribute. Using method gives\n        ability to have some \"logic\" other than attribute values.\n\n        By default, returns `default_variants` value.\n\n        Returns:\n            list[str]: Whisper variants for user input.\n\n        \"\"\"\n        return copy.deepcopy(self.default_variants)\n\n    def get_default_variant(\n        self, only_explicit: bool = False\n    ) -&gt; str | None:\n        \"\"\"Default variant value that will be used to prefill variant input.\n\n        This is for user input and value may not be content of result from\n        `get_default_variants`.\n\n        Note:\n            This method does not allow to have empty string as\n                default variant.\n\n        Args:\n            only_explicit (bool): If True, only explicit default\n                variant from '_default_variant' will be returned.\n\n        Returns:\n            str | None: Variant value.\n\n        \"\"\"\n        if only_explicit or self._default_variant:\n            return self._default_variant\n\n        for variant in self.get_default_variants():\n            return variant\n        return DEFAULT_VARIANT_VALUE\n\n    def _get_default_variant_wrap(self) -&gt; str:\n        \"\"\"Default variant value that will be used to prefill variant input.\n\n        Wrapper for 'get_default_variant'.\n\n        Notes:\n            This method is wrapper for 'get_default_variant'\n                for 'default_variant' property, so creator can override\n                the method.\n\n        Returns:\n            str: Variant value.\n\n        \"\"\"\n        return self.get_default_variant()\n\n    def _set_default_variant_wrap(self, variant: str) -&gt; None:\n        \"\"\"Set default variant value.\n\n        This method is needed for automated settings overrides which are\n        changing attributes based on keys in settings.\n\n        Args:\n            variant (str): New default variant value.\n\n        \"\"\"\n        self._default_variant = variant\n\n    default_variant: str = property(\n        _get_default_variant_wrap,\n        _set_default_variant_wrap\n    )\n\n    def get_pre_create_attr_defs(self) -&gt; list[AbstractAttrDef]:\n        \"\"\"Plugin attribute definitions needed for creation.\n        Attribute definitions of plugin that define how creation will work.\n        Values of these definitions are passed to `create` method.\n\n        Note:\n            Convert method should be implemented which should care about\n            updating keys/values when plugin attributes change.\n\n        Returns:\n            list[AbstractAttrDef]: Attribute definitions that can be tweaked\n                for created instance.\n\n        \"\"\"\n        return self.pre_create_attr_defs\n\n    def get_staging_dir(\n        self, instance: CreatedInstance\n    ) -&gt; StagingDir | None:\n        \"\"\"Return the staging dir and persistence from instance.\n\n        Args:\n            instance (CreatedInstance): Instance for which should be staging\n                dir gathered.\n\n        Returns:\n            StagingDir | None: Staging dir path and persistence or None.\n\n        \"\"\"\n        create_ctx = self.create_context\n        product_name = instance.get(\"productName\")\n        product_base_type = instance.get(\"productBaseType\")\n        product_type = instance.get(\"productType\")\n        folder_path = instance.get(\"folderPath\")\n\n        if not product_base_type:\n            product_base_type = product_type\n\n        # this can only work if product name and folder path are available\n        if not product_name or not folder_path:\n            return None\n\n        publish_settings = self.project_settings[\"core\"][\"publish\"]\n        follow_workfile_version = (\n            publish_settings\n            [\"CollectAnatomyInstanceData\"]\n            [\"follow_workfile_version\"]\n        )\n        follow_version_hosts = (\n            publish_settings\n            [\"CollectSceneVersion\"]\n            [\"hosts\"]\n        )\n\n        current_host = create_ctx.host.name\n        follow_workfile_version = (\n            follow_workfile_version and\n            current_host in follow_version_hosts\n        )\n\n        # Gather version number provided from the instance.\n        current_workfile = create_ctx.get_current_workfile_path()\n        version = instance.get(\"version\")\n\n        # If follow workfile, gather version from workfile path.\n        if version is None and follow_workfile_version and current_workfile:\n            workfile_version = get_version_from_path(current_workfile)\n            if workfile_version is not None:\n                version = int(workfile_version)\n\n        # Fill-up version with next version available.\n        if version is None:\n            versions = self.get_next_versions_for_instances(\n                [instance]\n            )\n            version, = tuple(versions.values())\n\n        template_data = {\"version\": version}\n\n        staging_dir_info = get_staging_dir_info(\n            create_ctx.get_current_project_entity(),\n            create_ctx.get_folder_entity(folder_path),\n            create_ctx.get_task_entity(folder_path, instance.get(\"task\")),\n            product_base_type=product_base_type,\n            product_type=product_type,\n            product_name=product_name,\n            host_name=create_ctx.host_name,\n            anatomy=create_ctx.get_current_project_anatomy(),\n            project_settings=create_ctx.get_current_project_settings(),\n            always_return_path=False,\n            logger=self.log,\n            template_data=template_data,\n        )\n\n        return staging_dir_info or None\n\n    def apply_staging_dir(\n        self, instance: CreatedInstance\n    ) -&gt; str | None:\n        \"\"\"Apply staging dir with persistence to instance's transient data.\n\n        Method is called on instance creation and on instance update.\n\n        Args:\n            instance (CreatedInstance): Instance for which should be staging\n                dir applied.\n\n        Returns:\n            str | None: Staging dir path or None if not applied.\n\n        \"\"\"\n        staging_dir_info = self.get_staging_dir(instance)\n        if staging_dir_info is None:\n            return None\n\n        # path might be already created by get_staging_dir_info\n        staging_dir_path = staging_dir_info.directory\n        os.makedirs(staging_dir_path, exist_ok=True)\n\n        instance.transient_data.update({\n            \"stagingDir\": staging_dir_path,\n            \"stagingDir_persistent\": staging_dir_info.is_persistent,\n            \"stagingDir_is_custom\": staging_dir_info.is_custom,\n        })\n\n        self.log.info(f\"Applied staging dir to instance: {staging_dir_path}\")\n\n        return staging_dir_path\n\n    def _pre_create_attr_defs_changed(self) -&gt; None:\n        \"\"\"Called when pre-create attribute definitions change.\n\n        Create plugin can call this method when knows that\n            'get_pre_create_attr_defs' should be called again.\n        \"\"\"\n        self.create_context.create_plugin_pre_create_attr_defs_changed(\n            self.identifier\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.Creator.show_order","title":"<code>show_order</code>  <code>property</code>","text":"<p>Order in which is creator shown in UI.</p> <p>Returns:</p> Name Type Description <code>int</code> <code>int</code> <p>Order in which is creator shown (less == earlier). By default is using Creator's 'order' or processing.</p>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.Creator.apply_staging_dir","title":"<code>apply_staging_dir(instance)</code>","text":"<p>Apply staging dir with persistence to instance's transient data.</p> <p>Method is called on instance creation and on instance update.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>CreatedInstance</code> <p>Instance for which should be staging dir applied.</p> required <p>Returns:</p> Type Description <code>str | None</code> <p>str | None: Staging dir path or None if not applied.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def apply_staging_dir(\n    self, instance: CreatedInstance\n) -&gt; str | None:\n    \"\"\"Apply staging dir with persistence to instance's transient data.\n\n    Method is called on instance creation and on instance update.\n\n    Args:\n        instance (CreatedInstance): Instance for which should be staging\n            dir applied.\n\n    Returns:\n        str | None: Staging dir path or None if not applied.\n\n    \"\"\"\n    staging_dir_info = self.get_staging_dir(instance)\n    if staging_dir_info is None:\n        return None\n\n    # path might be already created by get_staging_dir_info\n    staging_dir_path = staging_dir_info.directory\n    os.makedirs(staging_dir_path, exist_ok=True)\n\n    instance.transient_data.update({\n        \"stagingDir\": staging_dir_path,\n        \"stagingDir_persistent\": staging_dir_info.is_persistent,\n        \"stagingDir_is_custom\": staging_dir_info.is_custom,\n    })\n\n    self.log.info(f\"Applied staging dir to instance: {staging_dir_path}\")\n\n    return staging_dir_path\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.Creator.create","title":"<code>create(product_name, instance_data, pre_create_data)</code>  <code>abstractmethod</code>","text":"<p>Create new instance and store it.</p> <p>Ideally should be stored to workfile using host implementation.</p> <p>Parameters:</p> Name Type Description Default <code>product_name</code> <code>str</code> <p>Product name of created instance.</p> required <code>instance_data</code> <code>dict[str, Any]</code> <p>Base data for instance.</p> required <code>pre_create_data</code> <code>dict[str, Any]</code> <p>Data based on pre creation attributes. Those may affect how creator works.</p> required Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>@abstractmethod\ndef create(\n    self,\n    product_name: str,\n    instance_data: dict[str, Any],\n    pre_create_data: dict[str, Any],\n) -&gt; Any:\n    \"\"\"Create new instance and store it.\n\n    Ideally should be stored to workfile using host implementation.\n\n    Args:\n        product_name (str): Product name of created instance.\n        instance_data (dict[str, Any]): Base data for instance.\n        pre_create_data (dict[str, Any]): Data based on pre creation\n            attributes. Those may affect how creator works.\n    \"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.Creator.get_default_variant","title":"<code>get_default_variant(only_explicit=False)</code>","text":"<p>Default variant value that will be used to prefill variant input.</p> <p>This is for user input and value may not be content of result from <code>get_default_variants</code>.</p> Note <p>This method does not allow to have empty string as     default variant.</p> <p>Parameters:</p> Name Type Description Default <code>only_explicit</code> <code>bool</code> <p>If True, only explicit default variant from '_default_variant' will be returned.</p> <code>False</code> <p>Returns:</p> Type Description <code>str | None</code> <p>str | None: Variant value.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def get_default_variant(\n    self, only_explicit: bool = False\n) -&gt; str | None:\n    \"\"\"Default variant value that will be used to prefill variant input.\n\n    This is for user input and value may not be content of result from\n    `get_default_variants`.\n\n    Note:\n        This method does not allow to have empty string as\n            default variant.\n\n    Args:\n        only_explicit (bool): If True, only explicit default\n            variant from '_default_variant' will be returned.\n\n    Returns:\n        str | None: Variant value.\n\n    \"\"\"\n    if only_explicit or self._default_variant:\n        return self._default_variant\n\n    for variant in self.get_default_variants():\n        return variant\n    return DEFAULT_VARIANT_VALUE\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.Creator.get_default_variants","title":"<code>get_default_variants()</code>","text":"<p>Default variant values for UI tooltips.</p> <p>Replacement of <code>default_variants</code> attribute. Using method gives ability to have some \"logic\" other than attribute values.</p> <p>By default, returns <code>default_variants</code> value.</p> <p>Returns:</p> Type Description <code>list[str]</code> <p>list[str]: Whisper variants for user input.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def get_default_variants(self) -&gt; list[str]:\n    \"\"\"Default variant values for UI tooltips.\n\n    Replacement of `default_variants` attribute. Using method gives\n    ability to have some \"logic\" other than attribute values.\n\n    By default, returns `default_variants` value.\n\n    Returns:\n        list[str]: Whisper variants for user input.\n\n    \"\"\"\n    return copy.deepcopy(self.default_variants)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.Creator.get_description","title":"<code>get_description()</code>","text":"<p>Short description of product base type and plugin.</p> <p>Returns:</p> Name Type Description <code>str</code> <p>Short description of product base type.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def get_description(self):\n    \"\"\"Short description of product base type and plugin.\n\n    Returns:\n        str: Short description of product base type.\n\n    \"\"\"\n    return self.description\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.Creator.get_detail_description","title":"<code>get_detail_description()</code>","text":"<p>Description of product base type and plugin.</p> <p>Can be detailed with markdown or html tags.</p> <p>Returns:</p> Name Type Description <code>str</code> <code>str | None</code> <p>Detailed description of product base type for artist.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def get_detail_description(self) -&gt; str | None:\n    \"\"\"Description of product base type and plugin.\n\n    Can be detailed with markdown or html tags.\n\n    Returns:\n        str: Detailed description of product base type for artist.\n\n    \"\"\"\n    return self.detailed_description\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.Creator.get_pre_create_attr_defs","title":"<code>get_pre_create_attr_defs()</code>","text":"<p>Plugin attribute definitions needed for creation. Attribute definitions of plugin that define how creation will work. Values of these definitions are passed to <code>create</code> method.</p> Note <p>Convert method should be implemented which should care about updating keys/values when plugin attributes change.</p> <p>Returns:</p> Type Description <code>list[AbstractAttrDef]</code> <p>list[AbstractAttrDef]: Attribute definitions that can be tweaked for created instance.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def get_pre_create_attr_defs(self) -&gt; list[AbstractAttrDef]:\n    \"\"\"Plugin attribute definitions needed for creation.\n    Attribute definitions of plugin that define how creation will work.\n    Values of these definitions are passed to `create` method.\n\n    Note:\n        Convert method should be implemented which should care about\n        updating keys/values when plugin attributes change.\n\n    Returns:\n        list[AbstractAttrDef]: Attribute definitions that can be tweaked\n            for created instance.\n\n    \"\"\"\n    return self.pre_create_attr_defs\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.Creator.get_staging_dir","title":"<code>get_staging_dir(instance)</code>","text":"<p>Return the staging dir and persistence from instance.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>CreatedInstance</code> <p>Instance for which should be staging dir gathered.</p> required <p>Returns:</p> Type Description <code>StagingDir | None</code> <p>StagingDir | None: Staging dir path and persistence or None.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def get_staging_dir(\n    self, instance: CreatedInstance\n) -&gt; StagingDir | None:\n    \"\"\"Return the staging dir and persistence from instance.\n\n    Args:\n        instance (CreatedInstance): Instance for which should be staging\n            dir gathered.\n\n    Returns:\n        StagingDir | None: Staging dir path and persistence or None.\n\n    \"\"\"\n    create_ctx = self.create_context\n    product_name = instance.get(\"productName\")\n    product_base_type = instance.get(\"productBaseType\")\n    product_type = instance.get(\"productType\")\n    folder_path = instance.get(\"folderPath\")\n\n    if not product_base_type:\n        product_base_type = product_type\n\n    # this can only work if product name and folder path are available\n    if not product_name or not folder_path:\n        return None\n\n    publish_settings = self.project_settings[\"core\"][\"publish\"]\n    follow_workfile_version = (\n        publish_settings\n        [\"CollectAnatomyInstanceData\"]\n        [\"follow_workfile_version\"]\n    )\n    follow_version_hosts = (\n        publish_settings\n        [\"CollectSceneVersion\"]\n        [\"hosts\"]\n    )\n\n    current_host = create_ctx.host.name\n    follow_workfile_version = (\n        follow_workfile_version and\n        current_host in follow_version_hosts\n    )\n\n    # Gather version number provided from the instance.\n    current_workfile = create_ctx.get_current_workfile_path()\n    version = instance.get(\"version\")\n\n    # If follow workfile, gather version from workfile path.\n    if version is None and follow_workfile_version and current_workfile:\n        workfile_version = get_version_from_path(current_workfile)\n        if workfile_version is not None:\n            version = int(workfile_version)\n\n    # Fill-up version with next version available.\n    if version is None:\n        versions = self.get_next_versions_for_instances(\n            [instance]\n        )\n        version, = tuple(versions.values())\n\n    template_data = {\"version\": version}\n\n    staging_dir_info = get_staging_dir_info(\n        create_ctx.get_current_project_entity(),\n        create_ctx.get_folder_entity(folder_path),\n        create_ctx.get_task_entity(folder_path, instance.get(\"task\")),\n        product_base_type=product_base_type,\n        product_type=product_type,\n        product_name=product_name,\n        host_name=create_ctx.host_name,\n        anatomy=create_ctx.get_current_project_anatomy(),\n        project_settings=create_ctx.get_current_project_settings(),\n        always_return_path=False,\n        logger=self.log,\n        template_data=template_data,\n    )\n\n    return staging_dir_info or None\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.CreatorError","title":"<code>CreatorError</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Should be raised when creator failed because of known issue.</p> <p>Message of error should be artist friendly.</p> Source code in <code>client/ayon_core/pipeline/create/exceptions.py</code> <pre><code>class CreatorError(Exception):\n    \"\"\"Should be raised when creator failed because of known issue.\n\n    Message of error should be artist friendly.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.IncompatibleLoaderError","title":"<code>IncompatibleLoaderError</code>","text":"<p>               Bases: <code>ValueError</code></p> <p>Error when Loader is incompatible with a representation.</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>class IncompatibleLoaderError(ValueError):\n    \"\"\"Error when Loader is incompatible with a representation.\"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.InventoryAction","title":"<code>InventoryAction</code>","text":"<p>A custom action for the scene inventory tool</p> <p>If registered the action will be visible in the Right Mouse Button menu under the submenu \"Actions\".</p> Source code in <code>client/ayon_core/pipeline/actions/inventory.py</code> <pre><code>class InventoryAction:\n    \"\"\"A custom action for the scene inventory tool\n\n    If registered the action will be visible in the Right Mouse Button menu\n    under the submenu \"Actions\".\n\n    \"\"\"\n\n    label = None\n    icon = None\n    color = None\n    order = 0\n\n    log = logging.getLogger(\"InventoryAction\")\n    log.propagate = True\n\n    @staticmethod\n    def is_compatible(container):\n        \"\"\"Override function in a custom class\n\n        This method is specifically used to ensure the action can operate on\n        the container.\n\n        Args:\n            container(dict): the data of a loaded asset, see host.ls()\n\n        Returns:\n            bool\n        \"\"\"\n        return bool(container.get(\"objectName\"))\n\n    def process(self, containers):\n        \"\"\"Override function in a custom class\n\n        This method will receive all containers even those which are\n        incompatible. It is advised to create a small filter along the lines\n        of this example:\n\n        valid_containers = filter(self.is_compatible(c) for c in containers)\n\n        The return value will need to be a True-ish value to trigger\n        the data_changed signal in order to refresh the view.\n\n        You can return a list of container names to trigger GUI to select\n        treeview items.\n\n        You can return a dict to carry extra GUI options. For example:\n            {\n                \"objectNames\": [container names...],\n                \"options\": {\"mode\": \"toggle\",\n                            \"clear\": False}\n            }\n        Currently workable GUI options are:\n            - clear (bool): Clear current selection before selecting by action.\n                            Default `True`.\n            - mode (str): selection mode, use one of these:\n                          \"select\", \"deselect\", \"toggle\". Default is \"select\".\n\n        Args:\n            containers (list): list of dictionaries\n\n        Return:\n            bool, list or dict\n\n        \"\"\"\n        return True\n\n    @classmethod\n    def filepath_from_context(cls, context):\n        return get_representation_path_from_context(context)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.InventoryAction.is_compatible","title":"<code>is_compatible(container)</code>  <code>staticmethod</code>","text":"<p>Override function in a custom class</p> <p>This method is specifically used to ensure the action can operate on the container.</p> <p>Parameters:</p> Name Type Description Default <code>container</code> <code>dict</code> <p>the data of a loaded asset, see host.ls()</p> required <p>Returns:</p> Type Description <p>bool</p> Source code in <code>client/ayon_core/pipeline/actions/inventory.py</code> <pre><code>@staticmethod\ndef is_compatible(container):\n    \"\"\"Override function in a custom class\n\n    This method is specifically used to ensure the action can operate on\n    the container.\n\n    Args:\n        container(dict): the data of a loaded asset, see host.ls()\n\n    Returns:\n        bool\n    \"\"\"\n    return bool(container.get(\"objectName\"))\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.InventoryAction.process","title":"<code>process(containers)</code>","text":"<p>Override function in a custom class</p> <p>This method will receive all containers even those which are incompatible. It is advised to create a small filter along the lines of this example:</p> <p>valid_containers = filter(self.is_compatible(c) for c in containers)</p> <p>The return value will need to be a True-ish value to trigger the data_changed signal in order to refresh the view.</p> <p>You can return a list of container names to trigger GUI to select treeview items.</p> <p>You can return a dict to carry extra GUI options. For example:     {         \"objectNames\": [container names...],         \"options\": {\"mode\": \"toggle\",                     \"clear\": False}     } Currently workable GUI options are:     - clear (bool): Clear current selection before selecting by action.                     Default <code>True</code>.     - mode (str): selection mode, use one of these:                   \"select\", \"deselect\", \"toggle\". Default is \"select\".</p> <p>Parameters:</p> Name Type Description Default <code>containers</code> <code>list</code> <p>list of dictionaries</p> required Return <p>bool, list or dict</p> Source code in <code>client/ayon_core/pipeline/actions/inventory.py</code> <pre><code>def process(self, containers):\n    \"\"\"Override function in a custom class\n\n    This method will receive all containers even those which are\n    incompatible. It is advised to create a small filter along the lines\n    of this example:\n\n    valid_containers = filter(self.is_compatible(c) for c in containers)\n\n    The return value will need to be a True-ish value to trigger\n    the data_changed signal in order to refresh the view.\n\n    You can return a list of container names to trigger GUI to select\n    treeview items.\n\n    You can return a dict to carry extra GUI options. For example:\n        {\n            \"objectNames\": [container names...],\n            \"options\": {\"mode\": \"toggle\",\n                        \"clear\": False}\n        }\n    Currently workable GUI options are:\n        - clear (bool): Clear current selection before selecting by action.\n                        Default `True`.\n        - mode (str): selection mode, use one of these:\n                      \"select\", \"deselect\", \"toggle\". Default is \"select\".\n\n    Args:\n        containers (list): list of dictionaries\n\n    Return:\n        bool, list or dict\n\n    \"\"\"\n    return True\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.KnownPublishError","title":"<code>KnownPublishError</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Publishing crashed because of known error.</p> <p>Artist can't affect source of the error.</p> Deprecated <p>Please use <code>PublishError</code> instead. Marked as deprecated 24/09/02.</p> Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>class KnownPublishError(Exception):\n    \"\"\"Publishing crashed because of known error.\n\n    Artist can't affect source of the error.\n\n    Deprecated:\n        Please use `PublishError` instead. Marked as deprecated 24/09/02.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.LauncherAction","title":"<code>LauncherAction</code>","text":"<p>               Bases: <code>object</code></p> <p>A custom action available</p> Source code in <code>client/ayon_core/pipeline/actions/launcher.py</code> <pre><code>class LauncherAction(object):\n    \"\"\"A custom action available\"\"\"\n    name = None\n    label = None\n    icon = None\n    color = None\n    order = 0\n\n    log = logging.getLogger(\"LauncherAction\")\n    log.propagate = True\n\n    def is_compatible(self, selection):\n        \"\"\"Return whether the class is compatible with the Session.\n\n        Args:\n            selection (LauncherActionSelection): Data with selection.\n\n        \"\"\"\n        return True\n\n    def process(self, selection, **kwargs):\n        \"\"\"Process the action.\n\n        Args:\n            selection (LauncherActionSelection): Data with selection.\n            **kwargs: Additional arguments.\n\n        \"\"\"\n        pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.LauncherAction.is_compatible","title":"<code>is_compatible(selection)</code>","text":"<p>Return whether the class is compatible with the Session.</p> <p>Parameters:</p> Name Type Description Default <code>selection</code> <code>LauncherActionSelection</code> <p>Data with selection.</p> required Source code in <code>client/ayon_core/pipeline/actions/launcher.py</code> <pre><code>def is_compatible(self, selection):\n    \"\"\"Return whether the class is compatible with the Session.\n\n    Args:\n        selection (LauncherActionSelection): Data with selection.\n\n    \"\"\"\n    return True\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.LauncherAction.process","title":"<code>process(selection, **kwargs)</code>","text":"<p>Process the action.</p> <p>Parameters:</p> Name Type Description Default <code>selection</code> <code>LauncherActionSelection</code> <p>Data with selection.</p> required <code>**kwargs</code> <p>Additional arguments.</p> <code>{}</code> Source code in <code>client/ayon_core/pipeline/actions/launcher.py</code> <pre><code>def process(self, selection, **kwargs):\n    \"\"\"Process the action.\n\n    Args:\n        selection (LauncherActionSelection): Data with selection.\n        **kwargs: Additional arguments.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.LoaderPlugin","title":"<code>LoaderPlugin</code>","text":"<p>               Bases: <code>list</code></p> <p>Load representation into host application</p> Source code in <code>client/ayon_core/pipeline/load/plugins.py</code> <pre><code>class LoaderPlugin(list):\n    \"\"\"Load representation into host application\"\"\"\n\n    # Attribute 'skip_discovery' is used during discovery phase to skip\n    #   plugins, which can be used to mark base plugins that should not be\n    #   considered as plugins \"to use\". The discovery logic does NOT use\n    #   the attribute value from parent classes. Each base class has to define\n    #   the attribute again.\n    skip_discovery = True\n\n    product_types: set[str] = set()\n    product_base_types: Optional[set[str]] = None\n    representations = set()\n    extensions = {\"*\"}\n    order = 0\n    is_multiple_contexts_compatible = False\n    enabled = True\n\n    options = []\n\n    log = Logger.get_logger(\"ProductLoader\")\n\n    @classmethod\n    def apply_settings(cls, project_settings):\n        host_name = os.environ.get(\"AYON_HOST_NAME\")\n        plugin_type = \"load\"\n        plugin_type_settings = (\n            project_settings\n            .get(host_name, {})\n            .get(plugin_type, {})\n        )\n        global_type_settings = (\n            project_settings\n            .get(\"core\", {})\n            .get(plugin_type, {})\n        )\n        if not global_type_settings and not plugin_type_settings:\n            return\n\n        plugin_name = cls.__name__\n\n        plugin_settings = None\n        # Look for plugin settings in host specific settings\n        if plugin_name in plugin_type_settings:\n            plugin_settings = plugin_type_settings[plugin_name]\n\n        # Look for plugin settings in global settings\n        elif plugin_name in global_type_settings:\n            plugin_settings = global_type_settings[plugin_name]\n\n        if not plugin_settings:\n            return\n\n        print(f\"&gt;&gt;&gt; We have preset for {plugin_name}\")\n        for option, value in plugin_settings.items():\n            if option == \"enabled\" and value is False:\n                print(\"  - is disabled by preset\")\n            else:\n                print(f\"  - setting `{option}`: `{value}`\")\n            setattr(cls, option, value)\n\n    @classmethod\n    def has_valid_extension(cls, repre_entity):\n        \"\"\"Has representation document valid extension for loader.\n\n        Args:\n            repre_entity (dict[str, Any]): Representation entity.\n\n        Returns:\n             bool: Representation has valid extension\n        \"\"\"\n        if \"*\" in cls.extensions:\n            return True\n\n        # Get representation main file extension from 'context'\n        repre_context = repre_entity.get(\"context\") or {}\n        ext = repre_context.get(\"ext\")\n        if not ext:\n            # Legacy way how to get extensions\n            path = repre_entity.get(\"attrib\", {}).get(\"path\")\n            if not path:\n                cls.log.info(\n                    \"Representation doesn't have known source of extension\"\n                    \" information.\"\n                )\n                return False\n\n            cls.log.debug(\"Using legacy source of extension from path.\")\n            ext = os.path.splitext(path)[-1].lstrip(\".\")\n\n        # If representation does not have extension then can't be valid\n        if not ext:\n            return False\n\n        valid_extensions_low = {ext.lower() for ext in cls.extensions}\n        return ext.lower() in valid_extensions_low\n\n    @classmethod\n    def is_compatible_loader(cls, context):\n        \"\"\"Return whether a loader is compatible with a context.\n\n        On override make sure it is overridden as class or static method.\n\n        This checks the product type and the representation for the given\n        loader plugin.\n\n        Args:\n            context (dict[str, Any]): Documents of context for which should\n                be loader used.\n\n        Returns:\n            bool: Is loader compatible for context.\n        \"\"\"\n\n        plugin_repre_names = cls.get_representations()\n\n        # If the product base type isn't defined on the loader plugin,\n        # then we will use the product types.\n        plugin_product_filter = cls.product_base_types\n        if plugin_product_filter is None:\n            plugin_product_filter = cls.product_types\n\n        if plugin_product_filter:\n            plugin_product_filter = set(plugin_product_filter)\n\n        repre_entity = context.get(\"representation\")\n        product_entity = context[\"product\"]\n\n        # If no representation names, product types or extensions are defined\n        # then loader is not compatible with any context.\n        if (\n            not plugin_repre_names\n            or not plugin_product_filter\n            or not cls.extensions\n        ):\n            return False\n\n        # If no representation entity is provided then loader is not\n        # compatible with context.\n        if not repre_entity:\n            return False\n\n        # Check the compatibility with the representation names.\n        plugin_repre_names = set(plugin_repre_names)\n        if (\n            \"*\" not in plugin_repre_names\n            and repre_entity[\"name\"] not in plugin_repre_names\n        ):\n            return False\n\n        # Check the compatibility with the extension of the representation.\n        if not cls.has_valid_extension(repre_entity):\n            return False\n\n        product_type = product_entity.get(\"productType\")\n        product_base_type = product_entity.get(\"productBaseType\")\n\n        # Use product base type if defined, otherwise use product type.\n        product_filter = product_base_type\n        # If there is no product base type defined in the product entity,\n        # then we will use the product type.\n        if product_filter is None:\n            product_filter = product_type\n\n        # If wildcard is used in product types or base types,\n        # then we will consider the loader compatible with any product type.\n        if \"*\" in plugin_product_filter:\n            return True\n\n        # compatibility with legacy loader\n        if cls.product_base_types is None and product_base_type:\n            cls.log.error(\n                f\"Loader {cls.__name__} is doesn't specify \"\n                \"`product_base_types` but product entity has \"\n                f\"`productBaseType` defined as `{product_base_type}`. \"\n            )\n\n        return product_filter in plugin_product_filter\n\n    @classmethod\n    def get_representations(cls):\n        \"\"\"Representation names with which is plugin compatible.\n\n        Empty set makes the plugin incompatible with any representation. To\n            allow compatibility with all representations use '{\"*\"}'.\n\n        Returns:\n            set[str]: Names with which is plugin compatible.\n\n        \"\"\"\n        return cls.representations\n\n    @classmethod\n    def filepath_from_context(cls, context):\n        return get_representation_path_from_context(context)\n\n    def load(self, context, name=None, namespace=None, options=None):\n        \"\"\"Load asset via database\n\n        Arguments:\n            context (dict): Full parenthood of representation to load\n            name (str, optional): Use pre-defined name\n            namespace (str, optional): Use pre-defined namespace\n            options (dict, optional): Additional settings dictionary\n\n        \"\"\"\n        raise NotImplementedError(\"Loader.load() must be \"\n                                  \"implemented by subclass\")\n\n    def update(self, container, context):\n        \"\"\"Update `container` to `representation`\n\n        Args:\n            container (avalon-core:container-1.0): Container to update,\n                from `host.ls()`.\n            context (dict): Update the container to this representation.\n\n        \"\"\"\n        raise NotImplementedError(\"Loader.update() must be \"\n                                  \"implemented by subclass\")\n\n    def remove(self, container):\n        \"\"\"Remove a container\n\n        Arguments:\n            container (avalon-core:container-1.0): Container to remove,\n                from `host.ls()`.\n\n        Returns:\n            bool: Whether the container was deleted\n\n        \"\"\"\n        raise NotImplementedError(\"Loader.remove() must be \"\n                                  \"implemented by subclass\")\n\n    @classmethod\n    def get_options(cls, contexts):\n        \"\"\"Returns static (cls) options or could collect from 'contexts'.\n\n        Args:\n            contexts (list): of repre or product contexts\n        Returns:\n            (list)\n        \"\"\"\n        return cls.options or []\n\n    @classmethod\n    def get_representation_name_aliases(cls, representation_name: str):\n        \"\"\"Return representation names to which switching is allowed from\n        the input representation name, like an alias replacement of the input\n        `representation_name`.\n\n        For example, to allow an automated switch on update from representation\n        `ma` to `mb` or `abc`, then when `representation_name` is `ma` return:\n            [\"mb\", \"abc\"]\n\n        The order of the names in the returned representation names is\n        important, because the first one existing under the new version will\n        be chosen.\n\n        Returns:\n            List[str]: Representation names switching to is allowed on update\n              if the input representation name is not found on the new version.\n        \"\"\"\n        return []\n\n    def __repr__(self):\n        # Avoid printing as a list (due to inheritance), but print like object.\n        return object.__repr__(self)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.LoaderPlugin.get_options","title":"<code>get_options(contexts)</code>  <code>classmethod</code>","text":"<p>Returns static (cls) options or could collect from 'contexts'.</p> <p>Parameters:</p> Name Type Description Default <code>contexts</code> <code>list</code> <p>of repre or product contexts</p> required <p>Returns:     (list)</p> Source code in <code>client/ayon_core/pipeline/load/plugins.py</code> <pre><code>@classmethod\ndef get_options(cls, contexts):\n    \"\"\"Returns static (cls) options or could collect from 'contexts'.\n\n    Args:\n        contexts (list): of repre or product contexts\n    Returns:\n        (list)\n    \"\"\"\n    return cls.options or []\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.LoaderPlugin.get_representation_name_aliases","title":"<code>get_representation_name_aliases(representation_name)</code>  <code>classmethod</code>","text":"<p>Return representation names to which switching is allowed from the input representation name, like an alias replacement of the input <code>representation_name</code>.</p> <p>For example, to allow an automated switch on update from representation <code>ma</code> to <code>mb</code> or <code>abc</code>, then when <code>representation_name</code> is <code>ma</code> return:     [\"mb\", \"abc\"]</p> <p>The order of the names in the returned representation names is important, because the first one existing under the new version will be chosen.</p> <p>Returns:</p> Type Description <p>List[str]: Representation names switching to is allowed on update if the input representation name is not found on the new version.</p> Source code in <code>client/ayon_core/pipeline/load/plugins.py</code> <pre><code>@classmethod\ndef get_representation_name_aliases(cls, representation_name: str):\n    \"\"\"Return representation names to which switching is allowed from\n    the input representation name, like an alias replacement of the input\n    `representation_name`.\n\n    For example, to allow an automated switch on update from representation\n    `ma` to `mb` or `abc`, then when `representation_name` is `ma` return:\n        [\"mb\", \"abc\"]\n\n    The order of the names in the returned representation names is\n    important, because the first one existing under the new version will\n    be chosen.\n\n    Returns:\n        List[str]: Representation names switching to is allowed on update\n          if the input representation name is not found on the new version.\n    \"\"\"\n    return []\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.LoaderPlugin.get_representations","title":"<code>get_representations()</code>  <code>classmethod</code>","text":"<p>Representation names with which is plugin compatible.</p> <p>Empty set makes the plugin incompatible with any representation. To     allow compatibility with all representations use '{\"*\"}'.</p> <p>Returns:</p> Type Description <p>set[str]: Names with which is plugin compatible.</p> Source code in <code>client/ayon_core/pipeline/load/plugins.py</code> <pre><code>@classmethod\ndef get_representations(cls):\n    \"\"\"Representation names with which is plugin compatible.\n\n    Empty set makes the plugin incompatible with any representation. To\n        allow compatibility with all representations use '{\"*\"}'.\n\n    Returns:\n        set[str]: Names with which is plugin compatible.\n\n    \"\"\"\n    return cls.representations\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.LoaderPlugin.has_valid_extension","title":"<code>has_valid_extension(repre_entity)</code>  <code>classmethod</code>","text":"<p>Has representation document valid extension for loader.</p> <p>Parameters:</p> Name Type Description Default <code>repre_entity</code> <code>dict[str, Any]</code> <p>Representation entity.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <p>Representation has valid extension</p> Source code in <code>client/ayon_core/pipeline/load/plugins.py</code> <pre><code>@classmethod\ndef has_valid_extension(cls, repre_entity):\n    \"\"\"Has representation document valid extension for loader.\n\n    Args:\n        repre_entity (dict[str, Any]): Representation entity.\n\n    Returns:\n         bool: Representation has valid extension\n    \"\"\"\n    if \"*\" in cls.extensions:\n        return True\n\n    # Get representation main file extension from 'context'\n    repre_context = repre_entity.get(\"context\") or {}\n    ext = repre_context.get(\"ext\")\n    if not ext:\n        # Legacy way how to get extensions\n        path = repre_entity.get(\"attrib\", {}).get(\"path\")\n        if not path:\n            cls.log.info(\n                \"Representation doesn't have known source of extension\"\n                \" information.\"\n            )\n            return False\n\n        cls.log.debug(\"Using legacy source of extension from path.\")\n        ext = os.path.splitext(path)[-1].lstrip(\".\")\n\n    # If representation does not have extension then can't be valid\n    if not ext:\n        return False\n\n    valid_extensions_low = {ext.lower() for ext in cls.extensions}\n    return ext.lower() in valid_extensions_low\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.LoaderPlugin.is_compatible_loader","title":"<code>is_compatible_loader(context)</code>  <code>classmethod</code>","text":"<p>Return whether a loader is compatible with a context.</p> <p>On override make sure it is overridden as class or static method.</p> <p>This checks the product type and the representation for the given loader plugin.</p> <p>Parameters:</p> Name Type Description Default <code>context</code> <code>dict[str, Any]</code> <p>Documents of context for which should be loader used.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <p>Is loader compatible for context.</p> Source code in <code>client/ayon_core/pipeline/load/plugins.py</code> <pre><code>@classmethod\ndef is_compatible_loader(cls, context):\n    \"\"\"Return whether a loader is compatible with a context.\n\n    On override make sure it is overridden as class or static method.\n\n    This checks the product type and the representation for the given\n    loader plugin.\n\n    Args:\n        context (dict[str, Any]): Documents of context for which should\n            be loader used.\n\n    Returns:\n        bool: Is loader compatible for context.\n    \"\"\"\n\n    plugin_repre_names = cls.get_representations()\n\n    # If the product base type isn't defined on the loader plugin,\n    # then we will use the product types.\n    plugin_product_filter = cls.product_base_types\n    if plugin_product_filter is None:\n        plugin_product_filter = cls.product_types\n\n    if plugin_product_filter:\n        plugin_product_filter = set(plugin_product_filter)\n\n    repre_entity = context.get(\"representation\")\n    product_entity = context[\"product\"]\n\n    # If no representation names, product types or extensions are defined\n    # then loader is not compatible with any context.\n    if (\n        not plugin_repre_names\n        or not plugin_product_filter\n        or not cls.extensions\n    ):\n        return False\n\n    # If no representation entity is provided then loader is not\n    # compatible with context.\n    if not repre_entity:\n        return False\n\n    # Check the compatibility with the representation names.\n    plugin_repre_names = set(plugin_repre_names)\n    if (\n        \"*\" not in plugin_repre_names\n        and repre_entity[\"name\"] not in plugin_repre_names\n    ):\n        return False\n\n    # Check the compatibility with the extension of the representation.\n    if not cls.has_valid_extension(repre_entity):\n        return False\n\n    product_type = product_entity.get(\"productType\")\n    product_base_type = product_entity.get(\"productBaseType\")\n\n    # Use product base type if defined, otherwise use product type.\n    product_filter = product_base_type\n    # If there is no product base type defined in the product entity,\n    # then we will use the product type.\n    if product_filter is None:\n        product_filter = product_type\n\n    # If wildcard is used in product types or base types,\n    # then we will consider the loader compatible with any product type.\n    if \"*\" in plugin_product_filter:\n        return True\n\n    # compatibility with legacy loader\n    if cls.product_base_types is None and product_base_type:\n        cls.log.error(\n            f\"Loader {cls.__name__} is doesn't specify \"\n            \"`product_base_types` but product entity has \"\n            f\"`productBaseType` defined as `{product_base_type}`. \"\n        )\n\n    return product_filter in plugin_product_filter\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.LoaderPlugin.load","title":"<code>load(context, name=None, namespace=None, options=None)</code>","text":"<p>Load asset via database</p> <p>Parameters:</p> Name Type Description Default <code>context</code> <code>dict</code> <p>Full parenthood of representation to load</p> required <code>name</code> <code>str</code> <p>Use pre-defined name</p> <code>None</code> <code>namespace</code> <code>str</code> <p>Use pre-defined namespace</p> <code>None</code> <code>options</code> <code>dict</code> <p>Additional settings dictionary</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/load/plugins.py</code> <pre><code>def load(self, context, name=None, namespace=None, options=None):\n    \"\"\"Load asset via database\n\n    Arguments:\n        context (dict): Full parenthood of representation to load\n        name (str, optional): Use pre-defined name\n        namespace (str, optional): Use pre-defined namespace\n        options (dict, optional): Additional settings dictionary\n\n    \"\"\"\n    raise NotImplementedError(\"Loader.load() must be \"\n                              \"implemented by subclass\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.LoaderPlugin.remove","title":"<code>remove(container)</code>","text":"<p>Remove a container</p> <p>Parameters:</p> Name Type Description Default <code>container (avalon-core</code> <p>container-1.0): Container to remove, from <code>host.ls()</code>.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <p>Whether the container was deleted</p> Source code in <code>client/ayon_core/pipeline/load/plugins.py</code> <pre><code>def remove(self, container):\n    \"\"\"Remove a container\n\n    Arguments:\n        container (avalon-core:container-1.0): Container to remove,\n            from `host.ls()`.\n\n    Returns:\n        bool: Whether the container was deleted\n\n    \"\"\"\n    raise NotImplementedError(\"Loader.remove() must be \"\n                              \"implemented by subclass\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.LoaderPlugin.update","title":"<code>update(container, context)</code>","text":"<p>Update <code>container</code> to <code>representation</code></p> <p>Parameters:</p> Name Type Description Default <code>container (avalon-core</code> <p>container-1.0): Container to update, from <code>host.ls()</code>.</p> required <code>context</code> <code>dict</code> <p>Update the container to this representation.</p> required Source code in <code>client/ayon_core/pipeline/load/plugins.py</code> <pre><code>def update(self, container, context):\n    \"\"\"Update `container` to `representation`\n\n    Args:\n        container (avalon-core:container-1.0): Container to update,\n            from `host.ls()`.\n        context (dict): Update the container to this representation.\n\n    \"\"\"\n    raise NotImplementedError(\"Loader.update() must be \"\n                              \"implemented by subclass\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.OptionalPyblishPluginMixin","title":"<code>OptionalPyblishPluginMixin</code>","text":"<p>               Bases: <code>AYONPyblishPluginMixin</code></p> <p>Prepare mixin for optional plugins.</p> <p>Defined active attribute definition prepared for published and prepares method which will check if is active or not.</p> <pre><code>class ValidateScene(\n    pyblish.api.InstancePlugin, OptionalPyblishPluginMixin\n):\n    def process(self, instance):\n        # Skip the instance if is not active by data on the instance\n        if not self.is_active(instance.data):\n            return\n</code></pre> Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>class OptionalPyblishPluginMixin(AYONPyblishPluginMixin):\n    \"\"\"Prepare mixin for optional plugins.\n\n    Defined active attribute definition prepared for published and\n    prepares method which will check if is active or not.\n\n    ```\n    class ValidateScene(\n        pyblish.api.InstancePlugin, OptionalPyblishPluginMixin\n    ):\n        def process(self, instance):\n            # Skip the instance if is not active by data on the instance\n            if not self.is_active(instance.data):\n                return\n    ```\n    \"\"\"\n\n    # Allow exposing tooltip from class with `optional_tooltip` attribute\n    optional_tooltip: Optional[str] = None\n\n    @classmethod\n    def get_attribute_defs(cls):\n        \"\"\"Attribute definitions based on plugin's optional attribute.\"\"\"\n\n        # Empty list if plugin is not optional\n        if not getattr(cls, \"optional\", None):\n            return []\n\n        # Get active value from class as default value\n        active = getattr(cls, \"active\", True)\n        # Return boolean stored under 'active' key with label of the class name\n        label = cls.label or cls.__name__\n\n        return [\n            BoolDef(\n                \"active\",\n                default=active,\n                label=label,\n                tooltip=cls.optional_tooltip,\n            )\n        ]\n\n    def is_active(self, data):\n        \"\"\"Check if plugins is active for instance/context based on their data.\n\n        Args:\n            data(dict): Data from instance or context.\n        \"\"\"\n        # Skip if is not optional and return True\n        if not getattr(self, \"optional\", None):\n            return True\n        attr_values = self.get_attr_values_from_data(data)\n        active = attr_values.get(\"active\")\n        if active is None:\n            active = getattr(self, \"active\", True)\n        return active\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.OptionalPyblishPluginMixin.get_attribute_defs","title":"<code>get_attribute_defs()</code>  <code>classmethod</code>","text":"<p>Attribute definitions based on plugin's optional attribute.</p> Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>@classmethod\ndef get_attribute_defs(cls):\n    \"\"\"Attribute definitions based on plugin's optional attribute.\"\"\"\n\n    # Empty list if plugin is not optional\n    if not getattr(cls, \"optional\", None):\n        return []\n\n    # Get active value from class as default value\n    active = getattr(cls, \"active\", True)\n    # Return boolean stored under 'active' key with label of the class name\n    label = cls.label or cls.__name__\n\n    return [\n        BoolDef(\n            \"active\",\n            default=active,\n            label=label,\n            tooltip=cls.optional_tooltip,\n        )\n    ]\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.OptionalPyblishPluginMixin.is_active","title":"<code>is_active(data)</code>","text":"<p>Check if plugins is active for instance/context based on their data.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict</code> <p>Data from instance or context.</p> required Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>def is_active(self, data):\n    \"\"\"Check if plugins is active for instance/context based on their data.\n\n    Args:\n        data(dict): Data from instance or context.\n    \"\"\"\n    # Skip if is not optional and return True\n    if not getattr(self, \"optional\", None):\n        return True\n    attr_values = self.get_attr_values_from_data(data)\n    active = attr_values.get(\"active\")\n    if active is None:\n        active = getattr(self, \"active\", True)\n    return active\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.ProductLoaderPlugin","title":"<code>ProductLoaderPlugin</code>","text":"<p>               Bases: <code>LoaderPlugin</code></p> <p>Load product into host application Arguments:     context (dict): avalon-core:context-1.0     name (str, optional): Use pre-defined name     namespace (str, optional): Use pre-defined namespace</p> Source code in <code>client/ayon_core/pipeline/load/plugins.py</code> <pre><code>class ProductLoaderPlugin(LoaderPlugin):\n    \"\"\"Load product into host application\n    Arguments:\n        context (dict): avalon-core:context-1.0\n        name (str, optional): Use pre-defined name\n        namespace (str, optional): Use pre-defined namespace\n    \"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.PublishError","title":"<code>PublishError</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Publishing crashed because of known error.</p> <p>Message will be shown in UI for artist.</p> <p>Parameters:</p> Name Type Description Default <code>message</code> <code>str</code> <p>Message of error. Short explanation an issue.</p> required <code>title</code> <code>Optional[str]</code> <p>Title showed in UI.</p> <code>None</code> <code>description</code> <code>Optional[str]</code> <p>Detailed description of an error. It is possible to use Markdown syntax.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>class PublishError(Exception):\n    \"\"\"Publishing crashed because of known error.\n\n    Message will be shown in UI for artist.\n\n    Args:\n        message (str): Message of error. Short explanation an issue.\n        title (Optional[str]): Title showed in UI.\n        description (Optional[str]): Detailed description of an error.\n            It is possible to use Markdown syntax.\n\n    \"\"\"\n    def __init__(self, message, title=None, description=None, detail=None):\n        self.message = message\n        self.title = title\n        self.description = description or message\n        self.detail = detail\n        super().__init__(message)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.PublishValidationError","title":"<code>PublishValidationError</code>","text":"<p>               Bases: <code>PublishError</code></p> <p>Validation error happened during publishing.</p> <p>This exception should be used when validation publishing failed.</p> <p>Publishing does not stop during validation order if this     exception is raised.</p> <p>Has additional UI specific attributes that may be handy for artist.</p> <p>Argument 'title' is used to group errors.</p> Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>class PublishValidationError(PublishError):\n    \"\"\"Validation error happened during publishing.\n\n    This exception should be used when validation publishing failed.\n\n    Publishing does not stop during validation order if this\n        exception is raised.\n\n    Has additional UI specific attributes that may be handy for artist.\n\n    Argument 'title' is used to group errors.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.PublishXmlValidationError","title":"<code>PublishXmlValidationError</code>","text":"<p>               Bases: <code>PublishValidationError</code></p> <p>Raise an error from a dedicated xml file.</p> <p>Can be useful to have one xml file with different possible messages that     helps to avoid flood code with dedicated artist messages.</p> XML files should live relative to the plugin file location <p>'{plugin dir}/help/some_plugin.xml'.</p> <p>Parameters:</p> Name Type Description Default <code>plugin</code> <code>Plugin</code> <p>Plugin that raised an error. Is used to get path to xml file.</p> required <code>message</code> <code>str</code> <p>Exception message, can be technical, is used for console output.</p> required <code>key</code> <code>Optional[str]</code> <p>XML file can contain multiple error messages, key is used to get one of them. By default is used 'main'.</p> <code>None</code> <code>formatting_data</code> <code>Optional[dict[str, Any]</code> <p>Error message can have variables to fill.</p> <code>None</code> <code>help_filename</code> <code>Optional[str]</code> <p>Name of xml file with messages. By default, is used filename where plugin lives with .xml extension.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>class PublishXmlValidationError(PublishValidationError):\n    \"\"\"Raise an error from a dedicated xml file.\n\n    Can be useful to have one xml file with different possible messages that\n        helps to avoid flood code with dedicated artist messages.\n\n    XML files should live relative to the plugin file location:\n        '{plugin dir}/help/some_plugin.xml'.\n\n    Args:\n        plugin (pyblish.api.Plugin): Plugin that raised an error. Is used\n            to get path to xml file.\n        message (str): Exception message, can be technical, is used for\n            console output.\n        key (Optional[str]): XML file can contain multiple error messages, key\n            is used to get one of them. By default is used 'main'.\n        formatting_data (Optional[dict[str, Any]): Error message can have\n            variables to fill.\n        help_filename (Optional[str]): Name of xml file with messages. By\n            default, is used filename where plugin lives with .xml extension.\n\n    \"\"\"\n    def __init__(\n        self,\n        plugin: pyblish.api.Plugin,\n        message: str,\n        key: Optional[str] = None,\n        formatting_data: Optional[dict[str, Any]] = None,\n        help_filename: Optional[str] = None,\n    ) -&gt; None:\n        if key is None:\n            key = \"main\"\n\n        if not formatting_data:\n            formatting_data = {}\n        result = load_help_content_from_plugin(plugin, help_filename)\n        content_obj = result[\"errors\"][key]\n        description = content_obj.description.format(**formatting_data)\n        detail = content_obj.detail\n        if detail:\n            detail = detail.format(**formatting_data)\n        super().__init__(\n            message,\n            content_obj.title,\n            description,\n            detail\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.get_current_host_name","title":"<code>get_current_host_name()</code>","text":"<p>Current host name.</p> <p>Function is based on currently registered host integration or environment variable 'AYON_HOST_NAME'.</p> <p>Returns:</p> Type Description <p>Union[str, None]: Name of host integration in current process or None.</p> Source code in <code>client/ayon_core/pipeline/context_tools.py</code> <pre><code>def get_current_host_name():\n    \"\"\"Current host name.\n\n    Function is based on currently registered host integration or environment\n    variable 'AYON_HOST_NAME'.\n\n    Returns:\n        Union[str, None]: Name of host integration in current process or None.\n    \"\"\"\n\n    host = registered_host()\n    if isinstance(host, AbstractHost):\n        return host.name\n    return os.environ.get(\"AYON_HOST_NAME\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.get_global_context","title":"<code>get_global_context()</code>","text":"<p>Global context defined in environment variables.</p> <p>Values here may not reflect current context of host integration. The function can be used on startup before a host is registered.</p> <p>Use 'get_current_context' to make sure you'll get current host integration context info.</p> <p>Example::</p> <pre><code>{\n    \"project_name\": \"Commercial\",\n    \"folder_path\": \"Bunny\",\n    \"task_name\": \"Animation\",\n}\n</code></pre> <p>Returns:</p> Type Description <p>dict[str, Union[str, None]]: Context defined with environment variables.</p> Source code in <code>client/ayon_core/pipeline/context_tools.py</code> <pre><code>def get_global_context():\n    \"\"\"Global context defined in environment variables.\n\n    Values here may not reflect current context of host integration. The\n    function can be used on startup before a host is registered.\n\n    Use 'get_current_context' to make sure you'll get current host integration\n    context info.\n\n    Example::\n\n        {\n            \"project_name\": \"Commercial\",\n            \"folder_path\": \"Bunny\",\n            \"task_name\": \"Animation\",\n        }\n\n    Returns:\n        dict[str, Union[str, None]]: Context defined with environment\n            variables.\n    \"\"\"\n\n    return {\n        \"project_name\": os.environ.get(\"AYON_PROJECT_NAME\"),\n        \"folder_path\": os.environ.get(\"AYON_FOLDER_PATH\"),\n        \"task_name\": os.environ.get(\"AYON_TASK_NAME\"),\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.get_process_id","title":"<code>get_process_id()</code>","text":"<p>Fake process id created on demand using uuid.</p> <p>Can be used to create process specific folders in temp directory.</p> <p>Returns:</p> Name Type Description <code>str</code> <p>Process id.</p> Source code in <code>client/ayon_core/pipeline/context_tools.py</code> <pre><code>def get_process_id():\n    \"\"\"Fake process id created on demand using uuid.\n\n    Can be used to create process specific folders in temp directory.\n\n    Returns:\n        str: Process id.\n    \"\"\"\n\n    global _process_id\n    if _process_id is None:\n        _process_id = str(uuid.uuid4())\n    return _process_id\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.get_repres_contexts","title":"<code>get_repres_contexts(representation_ids, project_name=None)</code>","text":"<p>Return parenthood context for representation.</p> <p>Parameters:</p> Name Type Description Default <code>representation_ids</code> <code>list</code> <p>The representation ids.</p> required <code>project_name</code> <code>Optional[str]</code> <p>Project name.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>dict</code> <p>The full representation context by representation id. keys are repre_id, value is dictionary with entities of folder, product, version and representation.</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>def get_repres_contexts(representation_ids, project_name=None):\n    \"\"\"Return parenthood context for representation.\n\n    Args:\n        representation_ids (list): The representation ids.\n        project_name (Optional[str]): Project name.\n\n    Returns:\n        dict: The full representation context by representation id.\n            keys are repre_id, value is dictionary with entities of\n            folder, product, version and representation.\n    \"\"\"\n    from ayon_core.pipeline import get_current_project_name\n\n    if not representation_ids:\n        return {}\n\n    if not project_name:\n        project_name = get_current_project_name()\n\n    repre_entities = ayon_api.get_representations(\n        project_name, representation_ids\n    )\n\n    return get_representation_contexts(project_name, repre_entities)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.get_representation_context","title":"<code>get_representation_context(project_name, representation)</code>","text":"<p>Return parenthood context for representation.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>representation</code> <code>Union[dict[str, Any], str]</code> <p>Representation entity or representation id.</p> required <p>Returns:</p> Type Description <p>dict[str, dict[str, Any]]: The full representation context.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>When representation is invalid or parents were not found.</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>def get_representation_context(project_name, representation):\n    \"\"\"Return parenthood context for representation.\n\n    Args:\n        project_name (str): Project name.\n        representation (Union[dict[str, Any], str]): Representation entity\n            or representation id.\n\n    Returns:\n        dict[str, dict[str, Any]]: The full representation context.\n\n    Raises:\n        ValueError: When representation is invalid or parents were not found.\n\n    \"\"\"\n    if not representation:\n        raise ValueError(\n            \"Invalid argument value {}\".format(str(representation))\n        )\n\n    if isinstance(representation, dict):\n        repre_entity = representation\n        repre_id = repre_entity[\"id\"]\n        context = get_representation_contexts(\n            project_name, [repre_entity]\n        )[repre_id]\n    else:\n        repre_id = representation\n        context = get_representation_contexts_by_ids(\n            project_name, {repre_id}\n        )[repre_id]\n\n    missing_entities = []\n    for key, value in context.items():\n        if value is None:\n            missing_entities.append(key)\n\n    if missing_entities:\n        raise ValueError(\n            \"Not able to receive representation parent types: {}\".format(\n                \", \".join(missing_entities)\n            )\n        )\n\n    return context\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.get_representation_path","title":"<code>get_representation_path(project_name, repre_entity, *, anatomy=None, project_entity=None)</code>","text":"<pre><code>get_representation_path(representation: dict[str, Any], root: Optional[dict[str, Any]] = None) -&gt; TemplateResult\n</code></pre><pre><code>get_representation_path(project_name: str, repre_entity: dict[str, Any], *, anatomy: Optional[Anatomy] = None, project_entity: Optional[dict[str, Any]] = None) -&gt; TemplateResult\n</code></pre> <p>Get filled representation path.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>repre_entity</code> <code>dict[str, Any]</code> <p>Representation entity.</p> required <code>anatomy</code> <code>Optional[Anatomy]</code> <p>Project anatomy.</p> <code>None</code> <code>project_entity</code> <code>Optional[dict[str, Any]</code> <p>Project entity. Is used to initialize Anatomy and is not needed if 'anatomy' is passed in.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>TemplateResult</code> <code>TemplateResult</code> <p>Resolved path to representation.</p> <p>Raises:</p> Type Description <code>InvalidRepresentationContext</code> <p>When representation data are probably invalid or not available.</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>@_backwards_compatibility_repre_path\ndef get_representation_path(\n    project_name: str,\n    repre_entity: dict[str, Any],\n    *,\n    anatomy: Optional[Anatomy] = None,\n    project_entity: Optional[dict[str, Any]] = None,\n) -&gt; TemplateResult:\n    \"\"\"Get filled representation path.\n\n    Args:\n        project_name (str): Project name.\n        repre_entity (dict[str, Any]): Representation entity.\n        anatomy (Optional[Anatomy]): Project anatomy.\n        project_entity (Optional[dict[str, Any]): Project entity. Is used to\n            initialize Anatomy and is not needed if 'anatomy' is passed in.\n\n    Returns:\n        TemplateResult: Resolved path to representation.\n\n    Raises:\n        InvalidRepresentationContext: When representation data are probably\n            invalid or not available.\n\n    \"\"\"\n    if anatomy is None:\n        anatomy = Anatomy(project_name, project_entity=project_entity)\n\n    try:\n        template = repre_entity[\"attrib\"][\"template\"]\n\n    except KeyError as exc:\n        raise InvalidRepresentationContext(\n            \"Failed to receive template from representation entity.\"\n        ) from exc\n\n    try:\n        context = copy.deepcopy(repre_entity[\"context\"])\n        _fix_representation_context_compatibility(context)\n        context[\"root\"] = anatomy.roots\n\n        path = StringTemplate.format_strict_template(template, context)\n\n    except TemplateUnsolved as exc:\n        raise InvalidRepresentationContext(\n            \"Failed to resolve representation template with available data.\"\n        ) from exc\n\n    return path.normalized()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.get_staging_dir_info","title":"<code>get_staging_dir_info(project_entity, folder_entity, task_entity, product_type, product_name, host_name, *, anatomy=None, project_settings=None, template_data=None, always_return_path=True, force_tmp_dir=False, logger=None, prefix=None, suffix=None, username=None, product_base_type=None)</code>","text":"<p>Get staging dir info data.</p> <p>If <code>force_temp</code> is set, staging dir will be created as tempdir. If <code>always_get_some_dir</code> is set, staging dir will be created as tempdir if no staging dir profile is found. If <code>prefix</code> or <code>suffix</code> is not set, default values will be used.</p> <p>Parameters:</p> Name Type Description Default <code>project_entity</code> <code>dict[str, Any]</code> <p>Project entity.</p> required <code>folder_entity</code> <code>Optional[dict[str, Any]]</code> <p>Folder entity.</p> required <code>task_entity</code> <code>Optional[dict[str, Any]]</code> <p>Task entity.</p> required <code>product_type</code> <code>str</code> <p>Product type.</p> required <code>product_name</code> <code>str</code> <p>Name of product.</p> required <code>host_name</code> <code>str</code> <p>Name of host.</p> required <code>anatomy</code> <code>Optional[Anatomy]</code> <p>Anatomy object.</p> <code>None</code> <code>project_settings</code> <code>Optional[dict[str, Any]]</code> <p>Prepared project settings.</p> <code>None</code> <code>template_data</code> <code>Optional[dict[str, Any]]</code> <p>Additional data for formatting staging dir template.</p> <code>None</code> <code>always_return_path</code> <code>Optional[bool]</code> <p>If True, staging dir will be created as tempdir if no staging dir profile is found. Input value False will return None if no staging dir profile is found.</p> <code>True</code> <code>force_tmp_dir</code> <code>Optional[bool]</code> <p>If True, staging dir will be created as tempdir.</p> <code>False</code> <code>logger</code> <code>Optional[Logger]</code> <p>Logger instance.</p> <code>None</code> <code>suffix</code> <code>Optional[str]</code> <p>Optional suffix for staging dir name.</p> <code>None</code> <code>username</code> <code>Optional[str]</code> <p>AYON Username.</p> <code>None</code> <code>product_base_type</code> <code>str</code> <p>Product base type.</p> <code>None</code> <p>Returns:</p> Type Description <code>Optional[StagingDir]</code> <p>Optional[StagingDir]: Staging dir info data</p> Source code in <code>client/ayon_core/pipeline/staging_dir.py</code> <pre><code>@_get_staging_dir_info_wrap\ndef get_staging_dir_info(\n    project_entity: dict[str, Any],\n    folder_entity: Optional[dict[str, Any]],\n    task_entity: Optional[dict[str, Any]],\n    product_type: str,\n    product_name: str,\n    host_name: str,\n    *,\n    anatomy: Optional[Anatomy] = None,\n    project_settings: Optional[dict[str, Any]] = None,\n    template_data: Optional[dict[str, Any]] = None,\n    always_return_path: bool = True,\n    force_tmp_dir: bool = False,\n    logger: Optional[logging.Logger] = None,\n    prefix: Optional[str] = None,\n    suffix: Optional[str] = None,\n    username: Optional[str] = None,\n    product_base_type: Optional[str] = None,\n) -&gt; Optional[StagingDir]:\n    \"\"\"Get staging dir info data.\n\n    If `force_temp` is set, staging dir will be created as tempdir.\n    If `always_get_some_dir` is set, staging dir will be created as tempdir if\n    no staging dir profile is found.\n    If `prefix` or `suffix` is not set, default values will be used.\n\n    Arguments:\n        project_entity (dict[str, Any]): Project entity.\n        folder_entity (Optional[dict[str, Any]]): Folder entity.\n        task_entity (Optional[dict[str, Any]]): Task entity.\n        product_type (str): Product type.\n        product_name (str): Name of product.\n        host_name (str): Name of host.\n        anatomy (Optional[Anatomy]): Anatomy object.\n        project_settings (Optional[dict[str, Any]]): Prepared project settings.\n        template_data (Optional[dict[str, Any]]): Additional data for\n            formatting staging dir template.\n        always_return_path (Optional[bool]): If True, staging dir will be\n            created as tempdir if no staging dir profile is found. Input value\n            False will return None if no staging dir profile is found.\n        force_tmp_dir (Optional[bool]): If True, staging dir will be created as\n            tempdir.\n        logger (Optional[logging.Logger]): Logger instance.\n        prefix (Optional[str]) Optional prefix for staging dir name.\n        suffix (Optional[str]): Optional suffix for staging dir name.\n        username (Optional[str]): AYON Username.\n        product_base_type (str): Product base type.\n\n    Returns:\n        Optional[StagingDir]: Staging dir info data\n\n    \"\"\"\n    if logger is None:\n        logger = Logger.get_logger(\"get_staging_dir_info\")\n\n    if anatomy is None:\n        anatomy = Anatomy(\n            project_entity[\"name\"], project_entity=project_entity\n        )\n\n    if not product_base_type:\n        msg = (\n            \"Missing 'product_base_type' in 'get_staging_dir_info'.\"\n            \" Will be soon required.\"\n        )\n        logger.warning(msg)\n        warnings.warn(msg, DeprecationWarning, stacklevel=2)\n        product_base_type = product_type\n\n    if force_tmp_dir:\n        return StagingDir(\n            get_temp_dir(\n                project_name=project_entity[\"name\"],\n                anatomy=anatomy,\n                prefix=prefix,\n                suffix=suffix,\n            ),\n            is_persistent=False,\n            is_custom=False\n        )\n\n    # making few queries to database\n    ctx_data = get_template_data(\n        project_entity,\n        folder_entity,\n        task_entity,\n        host_name,\n        settings=project_settings,\n        username=username\n    )\n\n    # add additional data\n    ctx_data[\"product\"] = {\n        \"name\": product_name,\n        \"type\": product_type,\n        \"basetype\": product_base_type,\n    }\n\n    # add additional template formatting data\n    if template_data:\n        ctx_data.update(template_data)\n\n    task_name = task_type = None\n    if task_entity:\n        task_name = task_entity[\"name\"]\n        task_type = task_entity[\"taskType\"]\n\n    # get staging dir config\n    staging_dir_config = get_staging_dir_config(\n        project_entity[\"name\"],\n        task_type,\n        task_name,\n        product_base_type=product_base_type,\n        product_type=product_base_type,\n        product_name=product_name,\n        host_name=host_name,\n        project_settings=project_settings,\n        anatomy=anatomy,\n        logger=logger,\n    )\n\n    if staging_dir_config:\n        dir_template = staging_dir_config[\"template\"][\"directory\"]\n        return StagingDir(\n            dir_template.format_strict(ctx_data),\n            is_persistent=staging_dir_config[\"persistence\"],\n            is_custom=True\n        )\n\n    # no config found but force an output\n    if always_return_path:\n        return StagingDir(\n            get_temp_dir(\n                project_name=project_entity[\"name\"],\n                anatomy=anatomy,\n                prefix=prefix,\n                suffix=suffix,\n            ),\n            is_persistent=False,\n            is_custom=False\n        )\n\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.get_temp_dir","title":"<code>get_temp_dir(project_name, anatomy=None, prefix=None, suffix=None, use_local_temp=False)</code>","text":"<p>Get temporary dir path.</p> <p>If <code>use_local_temp</code> is set, tempdir will be created in local tempdir. If <code>anatomy</code> is not set, default anatomy will be used. If <code>prefix</code> or <code>suffix</code> is not set, default values will be used.</p> <p>It also supports <code>AYON_TMPDIR</code>, so studio can define own temp shared repository per project or even per more granular context. Template formatting is supported also with optional keys. Folder is created in case it doesn't exists.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Name of project.</p> required <code>anatomy</code> <code>Optional[Anatomy]</code> <p>Project Anatomy object.</p> <code>None</code> <code>suffix</code> <code>Optional[str]</code> <p>Suffix for tempdir.</p> <code>None</code> <code>prefix</code> <code>Optional[str]</code> <p>Prefix for tempdir.</p> <code>None</code> <code>use_local_temp</code> <code>Optional[bool]</code> <p>If True, temp dir will be created in local tempdir.</p> <code>False</code> <p>Returns:</p> Name Type Description <code>str</code> <p>Path to staging dir of instance.</p> Source code in <code>client/ayon_core/pipeline/tempdir.py</code> <pre><code>def get_temp_dir(\n    project_name, anatomy=None, prefix=None, suffix=None, use_local_temp=False\n):\n    \"\"\"Get temporary dir path.\n\n    If `use_local_temp` is set, tempdir will be created in local tempdir.\n    If `anatomy` is not set, default anatomy will be used.\n    If `prefix` or `suffix` is not set, default values will be used.\n\n    It also supports `AYON_TMPDIR`, so studio can define own temp\n    shared repository per project or even per more granular context.\n    Template formatting is supported also with optional keys. Folder is\n    created in case it doesn't exists.\n\n    Args:\n        project_name (str): Name of project.\n        anatomy (Optional[Anatomy]): Project Anatomy object.\n        suffix (Optional[str]): Suffix for tempdir.\n        prefix (Optional[str]): Prefix for tempdir.\n        use_local_temp (Optional[bool]): If True, temp dir will be created in\n            local tempdir.\n\n    Returns:\n        str: Path to staging dir of instance.\n\n    \"\"\"\n    if prefix is None:\n        prefix = \"ay_tmp_\"\n    suffix = suffix or \"\"\n\n    if use_local_temp:\n        return _create_local_staging_dir(prefix, suffix)\n\n    # make sure anatomy is set\n    if not anatomy:\n        anatomy = Anatomy(project_name)\n\n    # get customized tempdir path from `OPENPYPE_TMPDIR` env var\n    custom_temp_dir = _create_custom_tempdir(anatomy.project_name, anatomy)\n\n    return _create_local_staging_dir(prefix, suffix, dirpath=custom_temp_dir)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.install_ayon_plugins","title":"<code>install_ayon_plugins(project_name=None, host_name=None)</code>","text":"<p>Install AYON core plugins and make sure the core is initialized.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>Optional[str]</code> <p>Name of project to install plugins for.</p> <code>None</code> <code>host_name</code> <code>Optional[str]</code> <p>Name of host to install plugins for.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/context_tools.py</code> <pre><code>def install_ayon_plugins(project_name=None, host_name=None):\n    \"\"\"Install AYON core plugins and make sure the core is initialized.\n\n    Args:\n        project_name (Optional[str]): Name of project to install plugins for.\n        host_name (Optional[str]): Name of host to install plugins for.\n\n    \"\"\"\n    # Make sure global AYON connection has set site id and version\n    # - this is necessary if 'install_host' is not called\n    initialize_ayon_connection()\n    # Make sure addons are loaded\n    load_addons()\n\n    log.info(\"Registering global plug-ins..\")\n    pyblish.api.register_plugin_path(PUBLISH_PATH)\n    pyblish.api.register_discovery_filter(filter_pyblish_plugins)\n    register_inventory_action_path(INVENTORY_PATH)\n\n    if host_name is None:\n        host_name = get_current_host_name()\n\n    addons_manager = _get_addons_manager()\n    publish_plugin_dirs = addons_manager.collect_publish_plugin_paths(\n        host_name)\n    for path in publish_plugin_dirs:\n        pyblish.api.register_plugin_path(path)\n\n    create_plugin_paths = addons_manager.collect_create_plugin_paths(\n        host_name)\n    for path in create_plugin_paths:\n        register_creator_plugin_path(path)\n\n    load_plugin_paths = addons_manager.collect_load_plugin_paths(\n        host_name)\n    for path in load_plugin_paths:\n        register_loader_plugin_path(path)\n\n    inventory_action_paths = addons_manager.collect_inventory_action_paths(\n        host_name)\n    for path in inventory_action_paths:\n        register_inventory_action_path(path)\n\n    if project_name is None:\n        project_name = os.environ.get(\"AYON_PROJECT_NAME\")\n\n    # Register studio specific plugins\n    if project_name:\n        anatomy = Anatomy(project_name)\n        anatomy.set_root_environments()\n        register_root(anatomy.roots)\n\n        project_settings = get_project_settings(project_name)\n        platform_name = platform.system().lower()\n        project_plugins = (\n            project_settings\n            [\"core\"]\n            [\"project_plugins\"]\n            .get(platform_name)\n        ) or []\n        for path in project_plugins:\n            try:\n                path = str(path.format(**os.environ))\n            except KeyError:\n                pass\n\n            if not path or not os.path.exists(path):\n                continue\n\n            pyblish.api.register_plugin_path(path)\n            register_loader_plugin_path(path)\n            register_creator_plugin_path(path)\n            register_inventory_action_path(path)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.install_host","title":"<code>install_host(host)</code>","text":"<p>Install <code>host</code> into the running Python session.</p> <p>Parameters:</p> Name Type Description Default <code>host</code> <code>AbstractHost</code> <p>A host interface object.</p> required Source code in <code>client/ayon_core/pipeline/context_tools.py</code> <pre><code>def install_host(host: AbstractHost) -&gt; None:\n    \"\"\"Install `host` into the running Python session.\n\n    Args:\n        host (AbstractHost): A host interface object.\n\n    \"\"\"\n    if not isinstance(host, AbstractHost):\n        log.error(\n            f\"Host must be a subclass of 'AbstractHost', got '{type(host)}'.\"\n        )\n\n    global _is_installed\n\n    _is_installed = True\n\n    # Make sure global AYON connection has set site id and version\n    initialize_ayon_connection()\n\n    addons_manager = _get_addons_manager()\n\n    project_name = os.getenv(\"AYON_PROJECT_NAME\")\n    # WARNING: This might be an issue\n    #   - commented out because 'traypublisher' does not have set project\n    # if not project_name:\n    #     raise ValueError(\n    #         \"AYON_PROJECT_NAME is missing in environment variables.\"\n    #     )\n\n    log.info(\"Activating {}..\".format(project_name))\n\n    # Optional host install function\n    if hasattr(host, \"install\"):\n        host.install()\n\n    register_host(host)\n\n    def modified_emit(obj, record):\n        \"\"\"Method replacing `emit` in Pyblish's MessageHandler.\"\"\"\n        try:\n            record.msg = record.getMessage()\n        except Exception:\n            record.msg = str(record.msg)\n        obj.records.append(record)\n\n    MessageHandler.emit = modified_emit\n\n    if os.environ.get(\"AYON_REMOTE_PUBLISH\"):\n        # target \"farm\" == rendering on farm, expects AYON_PUBLISH_DATA\n        # target \"remote\" == remote execution, installs host\n        print(\"Registering pyblish target: remote\")\n        pyblish.api.register_target(\"remote\")\n    else:\n        pyblish.api.register_target(\"local\")\n\n    if is_in_tests():\n        print(\"Registering pyblish target: automated\")\n        pyblish.api.register_target(\"automated\")\n\n    host_name = os.environ.get(\"AYON_HOST_NAME\")\n\n    # Give option to handle host installation\n    for addon in addons_manager.get_enabled_addons():\n        addon.on_host_install(host, host_name, project_name)\n\n    install_ayon_plugins(project_name, host_name)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.is_installed","title":"<code>is_installed()</code>","text":"<p>Return state of installation</p> <p>Returns:</p> Type Description <p>True if installed, False otherwise</p> Source code in <code>client/ayon_core/pipeline/context_tools.py</code> <pre><code>def is_installed():\n    \"\"\"Return state of installation\n\n    Returns:\n        True if installed, False otherwise\n\n    \"\"\"\n\n    return _is_installed\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.is_product_base_type_supported","title":"<code>is_product_base_type_supported()</code>","text":"<p>Check support for product base types.</p> <p>This function checks if the current pipeline supports product base types. Once this feature is implemented, it will return True. This should be used in places where some kind of backward compatibility is needed to avoid breaking existing functionality that relies on the current behavior.</p> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if product base types are supported, False otherwise.</p> Source code in <code>client/ayon_core/pipeline/compatibility.py</code> <pre><code>def is_product_base_type_supported() -&gt; bool:\n    \"\"\"Check support for product base types.\n\n    This function checks if the current pipeline supports product base types.\n    Once this feature is implemented, it will return True. This should be used\n    in places where some kind of backward compatibility is needed to avoid\n    breaking existing functionality that relies on the current behavior.\n\n    Returns:\n        bool: True if product base types are supported, False otherwise.\n\n    \"\"\"\n\n    if not hasattr(ayon_api, \"is_product_base_type_supported\"):\n        return False\n    return ayon_api.is_product_base_type_supported()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.load_container","title":"<code>load_container(Loader, representation, namespace=None, name=None, options=None, **kwargs)</code>","text":"<p>Use Loader to load a representation.</p> <p>Parameters:</p> Name Type Description Default <code>Loader</code> <code>Loader</code> <p>The loader class to trigger.</p> required <code>representation</code> <code>str or dict</code> <p>The representation id or full representation as returned by the database.</p> required <code>namespace</code> <code>(str, Optional)</code> <p>The namespace to assign. Defaults to None.</p> <code>None</code> <code>name</code> <code>(str, Optional)</code> <p>The name to assign. Defaults to product name.</p> <code>None</code> <code>options</code> <code>(dict, Optional)</code> <p>Additional options to pass on to the loader.</p> <code>None</code> <p>Returns:</p> Type Description <p>The return of the <code>loader.load()</code> method.</p> <p>Raises:</p> Type Description <code>IncompatibleLoaderError</code> <p>When the loader is not compatible with the representation.</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>def load_container(\n    Loader, representation, namespace=None, name=None, options=None, **kwargs\n):\n    \"\"\"Use Loader to load a representation.\n\n    Args:\n        Loader (Loader): The loader class to trigger.\n        representation (str or dict): The representation id\n            or full representation as returned by the database.\n        namespace (str, Optional): The namespace to assign. Defaults to None.\n        name (str, Optional): The name to assign. Defaults to product name.\n        options (dict, Optional): Additional options to pass on to the loader.\n\n    Returns:\n        The return of the `loader.load()` method.\n\n    Raises:\n        IncompatibleLoaderError: When the loader is not compatible with\n            the representation.\n\n    \"\"\"\n    from ayon_core.pipeline import get_current_project_name\n\n    context = get_representation_context(\n        get_current_project_name(), representation\n    )\n    return load_with_repre_context(\n        Loader,\n        context,\n        namespace=namespace,\n        name=name,\n        options=options,\n        **kwargs\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.loaders_from_representation","title":"<code>loaders_from_representation(loaders, representation)</code>","text":"<p>Return all compatible loaders for a representation.</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>def loaders_from_representation(loaders, representation):\n    \"\"\"Return all compatible loaders for a representation.\"\"\"\n    from ayon_core.pipeline import get_current_project_name\n\n    project_name = get_current_project_name()\n    context = get_representation_context(\n        project_name, representation\n    )\n    return loaders_from_repre_context(loaders, context)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.register_host","title":"<code>register_host(host)</code>","text":"<p>Register a new host for the current process</p> <p>Parameters:</p> Name Type Description Default <code>host</code> <code>ModuleType</code> <p>A module implementing the Host API interface. See the Host API documentation for details on what is required, or browse the source code.</p> required Source code in <code>client/ayon_core/pipeline/context_tools.py</code> <pre><code>def register_host(host):\n    \"\"\"Register a new host for the current process\n\n    Arguments:\n        host (ModuleType): A module implementing the\n            Host API interface. See the Host API\n            documentation for details on what is\n            required, or browse the source code.\n\n    \"\"\"\n\n    _registered_host[\"_\"] = host\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.register_root","title":"<code>register_root(path)</code>","text":"<p>DEPRECATED Register currently active root.</p> Source code in <code>client/ayon_core/pipeline/context_tools.py</code> <pre><code>def register_root(path):\n    \"\"\"DEPRECATED Register currently active root.\"\"\"\n    log.info(\"Registering root: %s\" % path)\n    _registered_root[\"_\"] = path\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.registered_host","title":"<code>registered_host()</code>","text":"<p>Return currently registered host</p> Source code in <code>client/ayon_core/pipeline/context_tools.py</code> <pre><code>def registered_host():\n    \"\"\"Return currently registered host\"\"\"\n    return _registered_host[\"_\"]\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.registered_root","title":"<code>registered_root()</code>","text":"<p>Return registered roots from current project anatomy.</p> <p>Consider this does return roots only for current project and current     platforms, only if host was installer using 'install_host'.</p> Deprecated <p>Please use project 'Anatomy' to get roots. This function is still used     at current core functions of load logic, but that will change     in future and this function will be removed eventually. Using this     function at new places can cause problems in the future.</p> <p>Returns:</p> Type Description <p>dict[str, str]: Root paths.</p> Source code in <code>client/ayon_core/pipeline/context_tools.py</code> <pre><code>def registered_root():\n    \"\"\"Return registered roots from current project anatomy.\n\n    Consider this does return roots only for current project and current\n        platforms, only if host was installer using 'install_host'.\n\n    Deprecated:\n        Please use project 'Anatomy' to get roots. This function is still used\n            at current core functions of load logic, but that will change\n            in future and this function will be removed eventually. Using this\n            function at new places can cause problems in the future.\n\n    Returns:\n        dict[str, str]: Root paths.\n\n    \"\"\"\n    warnings.warn(\n        \"Used deprecated function 'registered_root'. Please use 'Anatomy'\"\n        \" to get roots.\",\n        DeprecationWarning,\n        stacklevel=2,\n    )\n    return _registered_root[\"_\"]\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.remove_container","title":"<code>remove_container(container)</code>","text":"<p>Remove a container</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>def remove_container(container):\n    \"\"\"Remove a container\"\"\"\n\n    Loader = _get_container_loader(container)\n    if not Loader:\n        raise LoaderNotFoundError(\n            \"Can't remove container because loader '{}' was not found.\"\n            .format(container.get(\"loader\"))\n        )\n\n    return Loader().remove(container)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.switch_container","title":"<code>switch_container(container, representation, loader_plugin=None)</code>","text":"<p>Switch a container to representation</p> <p>Parameters:</p> Name Type Description Default <code>container</code> <code>dict</code> <p>container information</p> required <code>representation</code> <code>dict</code> <p>representation entity</p> required <p>Returns:</p> Type Description <p>return from function call</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>def switch_container(\n    container,\n    representation,\n    loader_plugin=None,\n):\n    \"\"\"Switch a container to representation\n\n    Args:\n        container (dict): container information\n        representation (dict): representation entity\n        loader_plugin (LoaderPlugin)\n\n    Returns:\n        return from function call\n    \"\"\"\n    from ayon_core.pipeline import get_current_project_name\n\n    # Get the Loader for this container\n    if loader_plugin is None:\n        loader_plugin = _get_container_loader(container)\n\n    if not loader_plugin:\n        raise LoaderNotFoundError(\n            \"Can't switch container because loader '{}' was not found.\"\n            .format(container.get(\"loader\"))\n        )\n\n    if not hasattr(loader_plugin, \"switch\"):\n        # Backwards compatibility (classes without switch support\n        # might be better to just have \"switch\" raise NotImplementedError\n        # on the base class of Loader\\\n        raise LoaderSwitchNotImplementedError(\n            \"Loader {} does not support 'switch'\".format(loader_plugin.label)\n        )\n\n    # Get the new representation to switch to\n    project_name = container.get(\"project_name\")\n    if project_name is None:\n        project_name = get_current_project_name()\n\n    context = get_representation_context(\n        project_name, representation[\"id\"]\n    )\n    if not is_compatible_loader(loader_plugin, context):\n        raise IncompatibleLoaderError(\n            \"Loader {} is incompatible with {}\".format(\n                loader_plugin.__name__, context[\"product\"][\"name\"]\n            )\n        )\n\n    loader = loader_plugin(context)\n\n    return loader.switch(container, context)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.uninstall_host","title":"<code>uninstall_host()</code>","text":"<p>Undo all of what <code>install()</code> did</p> Source code in <code>client/ayon_core/pipeline/context_tools.py</code> <pre><code>def uninstall_host():\n    \"\"\"Undo all of what `install()` did\"\"\"\n    host = registered_host()\n\n    try:\n        host.uninstall()\n    except AttributeError:\n        pass\n\n    log.info(\"Deregistering global plug-ins..\")\n    pyblish.api.deregister_plugin_path(PUBLISH_PATH)\n    pyblish.api.deregister_discovery_filter(filter_pyblish_plugins)\n    deregister_inventory_action_path(INVENTORY_PATH)\n    log.info(\"Global plug-ins unregistered\")\n\n    deregister_host()\n\n    log.info(\"Successfully uninstalled AYON!\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/index.html#client.ayon_core.pipeline.update_container","title":"<code>update_container(container, version=-1)</code>","text":"<p>Update a container</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>def update_container(container, version=-1):\n    \"\"\"Update a container\"\"\"\n    from ayon_core.pipeline import get_current_project_name\n\n    # Compute the different version from 'representation'\n    project_name = container.get(\"project_name\")\n    if project_name is None:\n        project_name = get_current_project_name()\n    repre_id = container[\"representation\"]\n    if not _is_valid_representation_id(repre_id):\n        raise ValueError(\n            f\"Got container with invalid representation id '{repre_id}'\"\n        )\n    current_representation = ayon_api.get_representation_by_id(\n        project_name, repre_id\n    )\n\n    assert current_representation is not None, \"This is a bug\"\n\n    current_version_id = current_representation[\"versionId\"]\n    current_version = ayon_api.get_version_by_id(\n        project_name, current_version_id, fields={\"productId\"}\n    )\n    if isinstance(version, HeroVersionType):\n        new_version = ayon_api.get_hero_version_by_product_id(\n            project_name, current_version[\"productId\"]\n        )\n    elif version == -1:\n        new_version = ayon_api.get_last_version_by_product_id(\n            project_name, current_version[\"productId\"]\n        )\n\n    else:\n        new_version = ayon_api.get_version_by_name(\n            project_name, version, current_version[\"productId\"]\n        )\n\n    if new_version is None:\n        raise ValueError(\"Failed to find matching version\")\n\n    product_entity = ayon_api.get_product_by_id(\n        project_name, current_version[\"productId\"]\n    )\n    folder_entity = ayon_api.get_folder_by_id(\n        project_name, product_entity[\"folderId\"]\n    )\n\n    # Run update on the Loader for this container\n    Loader = _get_container_loader(container)\n    if not Loader:\n        raise LoaderNotFoundError(\n            \"Can't update container because loader '{}' was not found.\"\n            .format(container.get(\"loader\"))\n        )\n\n    repre_name = current_representation[\"name\"]\n    new_representation = ayon_api.get_representation_by_name(\n        project_name, repre_name, new_version[\"id\"]\n    )\n    if new_representation is None:\n        # The representation name is not found in the new version.\n        # Allow updating to a 'matching' representation if the loader\n        # has defined compatible update conversions\n        repre_name_aliases = Loader.get_representation_name_aliases(repre_name)\n        if repre_name_aliases:\n            representations = ayon_api.get_representations(\n                project_name,\n                representation_names=repre_name_aliases,\n                version_ids=[new_version[\"id\"]])\n            representations_by_name = {\n                repre[\"name\"]: repre for repre in representations\n            }\n            for name in repre_name_aliases:\n                if name in representations_by_name:\n                    new_representation = representations_by_name[name]\n                    break\n\n        if new_representation is None:\n            raise ValueError(\n                \"Representation '{}' wasn't found on requested version\".format(\n                    repre_name\n                )\n            )\n\n    project_entity = ayon_api.get_project(project_name)\n    context = {\n        \"project\": project_entity,\n        \"folder\": folder_entity,\n        \"product\": product_entity,\n        \"version\": new_version,\n        \"representation\": new_representation,\n    }\n    path = get_representation_path_from_context(context)\n    if not path or not os.path.exists(path):\n        raise ValueError(\"Path {} doesn't exist\".format(path))\n\n    return Loader().update(container, context)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/colorspace.html","title":"colorspace","text":""},{"location":"autoapi/client/ayon_core/pipeline/colorspace.html#client.ayon_core.pipeline.colorspace.ConfigData","title":"<code>ConfigData</code>  <code>dataclass</code>","text":"<p>OCIO Config to use in a certain context.</p> <p>When enabled and no path/template are set, it will be considered invalid and will error on OCIO path not found. Enabled must be False to explicitly allow OCIO to be disabled.</p> Source code in <code>client/ayon_core/pipeline/colorspace.py</code> <pre><code>@dataclass\nclass ConfigData:\n    \"\"\"OCIO Config to use in a certain context.\n\n    When enabled and no path/template are set, it will be considered invalid\n    and will error on OCIO path not found. Enabled must be False to explicitly\n    allow OCIO to be disabled.\"\"\"\n    path: str = \"\"\n    template: str = \"\"\n    enabled: bool = True\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/colorspace.html#client.ayon_core.pipeline.colorspace.compatibility_check_config_version","title":"<code>compatibility_check_config_version(config_path, major=1, minor=None)</code>","text":"<p>Making sure PyOpenColorIO config version is compatible</p> Source code in <code>client/ayon_core/pipeline/colorspace.py</code> <pre><code>def compatibility_check_config_version(config_path, major=1, minor=None):\n    \"\"\"Making sure PyOpenColorIO config version is compatible\"\"\"\n\n    version_data = get_config_version_data(config_path)\n\n    # check major version\n    if version_data[\"major\"] != major:\n        return False\n\n    # check minor version\n    if minor is not None and version_data[\"minor\"] != minor:\n        return False\n\n    return True\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/colorspace.html#client.ayon_core.pipeline.colorspace.convert_colorspace_enumerator_item","title":"<code>convert_colorspace_enumerator_item(colorspace_enum_item, config_items)</code>","text":"<p>Convert colorspace enumerator item to dictionary</p> <p>Parameters:</p> Name Type Description Default <code>colorspace_enum_item</code> <code>str</code> <p>Colorspace and family in couple.</p> required <code>config_items</code> <code>dict[str, dict]</code> <p>Colorspace data.</p> required <p>Returns:</p> Name Type Description <code>dict</code> <p>colorspace data</p> Source code in <code>client/ayon_core/pipeline/colorspace.py</code> <pre><code>def convert_colorspace_enumerator_item(\n    colorspace_enum_item,\n    config_items\n):\n    \"\"\"Convert colorspace enumerator item to dictionary\n\n    Args:\n        colorspace_enum_item (str): Colorspace and family in couple.\n        config_items (dict[str,dict]): Colorspace data.\n\n    Returns:\n        dict: colorspace data\n\n    \"\"\"\n    if \"::\" not in colorspace_enum_item:\n        return None\n\n    # split string with `::` separator and set first as key and second as value\n    item_type, item_name = colorspace_enum_item.split(\"::\")\n\n    item_data = None\n    if item_type == \"aliases\":\n        # loop through all colorspaces and find matching alias\n        for name, _data in config_items.get(\"colorspaces\", {}).items():\n            if item_name in _data.get(\"aliases\", []):\n                item_data = deepcopy(_data)\n                item_data.update({\n                    \"name\": name,\n                    \"type\": \"colorspace\"\n                })\n                break\n    else:\n        # find matching colorspace item found in labeled_colorspaces\n        item_data = config_items.get(item_type, {}).get(item_name)\n        if item_data:\n            item_data = deepcopy(item_data)\n            item_data.update({\n                \"name\": item_name,\n                \"type\": item_type\n            })\n\n    # raise exception if item is not found\n    if not item_data:\n        message_config_keys = \", \".join(\n            \"'{}':{}\".format(\n                key,\n                set(config_items.get(key, {}).keys())\n            ) for key in config_items.keys()\n        )\n        raise KeyError(\n            \"Missing colorspace item '{}' in config data: [{}]\".format(\n                colorspace_enum_item, message_config_keys\n            )\n        )\n\n    return item_data\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/colorspace.html#client.ayon_core.pipeline.colorspace.deprecated","title":"<code>deprecated(new_destination)</code>","text":"<p>Mark functions as deprecated.</p> <p>It will result in a warning being emitted when the function is used.</p> Source code in <code>client/ayon_core/pipeline/colorspace.py</code> <pre><code>def deprecated(new_destination):\n    \"\"\"Mark functions as deprecated.\n\n    It will result in a warning being emitted when the function is used.\n    \"\"\"\n\n    func = None\n    if callable(new_destination):\n        func = new_destination\n        new_destination = None\n\n    def _decorator(decorated_func):\n        if new_destination is None:\n            warning_message = (\n                \" Please check content of deprecated function to figure out\"\n                \" possible replacement.\"\n            )\n        else:\n            warning_message = \" Please replace your usage with '{}'.\".format(\n                new_destination\n            )\n\n        @functools.wraps(decorated_func)\n        def wrapper(*args, **kwargs):\n            warnings.simplefilter(\"always\", DeprecationWarning)\n            warnings.warn(\n                (\n                    \"Call to deprecated function '{}'\"\n                    \"\\nFunction was moved or removed.{}\"\n                ).format(decorated_func.__name__, warning_message),\n                category=DeprecationWarning,\n                stacklevel=4\n            )\n            return decorated_func(*args, **kwargs)\n        return wrapper\n\n    if func is None:\n        return _decorator\n    return _decorator(func)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/colorspace.html#client.ayon_core.pipeline.colorspace.get_colorspace_name_from_filepath","title":"<code>get_colorspace_name_from_filepath(filepath, host_name, project_name, config_data, file_rules=None, project_settings=None, validate=True)</code>","text":"<p>Get colorspace name from filepath</p> <p>Parameters:</p> Name Type Description Default <code>filepath</code> <code>str</code> <p>Path string, file rule pattern is tested on it.</p> required <code>host_name</code> <code>str</code> <p>Host name.</p> required <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>config_data</code> <code>dict</code> <p>Config path and template in dict.</p> required <code>file_rules</code> <code>Optional[dict]</code> <p>File rule data from settings.</p> <code>None</code> <code>project_settings</code> <code>Optional[dict]</code> <p>Project settings.</p> <code>None</code> <code>validate</code> <code>Optional[bool]</code> <p>should resulting colorspace be validated with config file? Defaults to True.</p> <code>True</code> <p>Returns:</p> Type Description <p>Union[str, None]: name of colorspace</p> Source code in <code>client/ayon_core/pipeline/colorspace.py</code> <pre><code>def get_colorspace_name_from_filepath(\n    filepath,\n    host_name,\n    project_name,\n    config_data,\n    file_rules=None,\n    project_settings=None,\n    validate=True\n):\n    \"\"\"Get colorspace name from filepath\n\n    Args:\n        filepath (str): Path string, file rule pattern is tested on it.\n        host_name (str): Host name.\n        project_name (str): Project name.\n        config_data (dict): Config path and template in dict.\n        file_rules (Optional[dict]): File rule data from settings.\n        project_settings (Optional[dict]): Project settings.\n        validate (Optional[bool]): should resulting colorspace be validated\n            with config file? Defaults to True.\n\n    Returns:\n        Union[str, None]: name of colorspace\n\n    \"\"\"\n    if not config_data:\n        # in case global or host color management is not enabled\n        return None\n\n    if file_rules is None:\n        if project_settings is None:\n            project_settings = get_project_settings(project_name)\n        file_rules = get_imageio_file_rules(\n            project_name, host_name, project_settings\n        )\n\n    # use ImageIO file rules\n    colorspace_name = get_imageio_file_rules_colorspace_from_filepath(\n        filepath,\n        host_name,\n        project_name,\n        config_data=config_data,\n        file_rules=file_rules,\n        project_settings=project_settings\n    )\n\n    # try to get colorspace from OCIO v2 file rules\n    if (\n        not colorspace_name\n        and compatibility_check_config_version(config_data[\"path\"], major=2)\n    ):\n        colorspace_name = get_config_file_rules_colorspace_from_filepath(\n            config_data[\"path\"], filepath)\n\n    # use parse colorspace from filepath as fallback\n    colorspace_name = colorspace_name or parse_colorspace_from_filepath(\n        filepath, config_path=config_data[\"path\"]\n    )\n\n    if not colorspace_name:\n        log.info(\"No imageio file rule matched input path: '{}'\".format(\n            filepath\n        ))\n        return None\n\n    # validate matching colorspace with config\n    if validate:\n        validate_imageio_colorspace_in_config(\n            config_data[\"path\"], colorspace_name\n        )\n\n    return colorspace_name\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/colorspace.html#client.ayon_core.pipeline.colorspace.get_colorspace_settings_from_publish_context","title":"<code>get_colorspace_settings_from_publish_context(context_data)</code>","text":"<p>Returns solved settings for the host context.</p> <p>Parameters:</p> Name Type Description Default <code>context_data</code> <code>data</code> <p>publishing context data</p> required <p>Returns:</p> Type Description <p>tuple | bool: config, file rules or None</p> Source code in <code>client/ayon_core/pipeline/colorspace.py</code> <pre><code>def get_colorspace_settings_from_publish_context(context_data):\n    \"\"\"Returns solved settings for the host context.\n\n    Args:\n        context_data (publish.Context.data): publishing context data\n\n    Returns:\n        tuple | bool: config, file rules or None\n\n    \"\"\"\n    if \"imageioSettings\" in context_data and context_data[\"imageioSettings\"]:\n        return context_data[\"imageioSettings\"]\n\n    project_name = context_data[\"projectName\"]\n    folder_path = context_data[\"folderPath\"]\n    task_name = context_data[\"task\"]\n    host_name = context_data[\"hostName\"]\n    app_name = context_data.get(\"appName\")\n    anatomy = context_data[\"anatomy\"]\n    template_data = context_data[\"anatomyData\"]\n    project_settings = context_data[\"project_settings\"]\n    folder_id = None\n    folder_entity = context_data.get(\"folderEntity\")\n    if folder_entity:\n        folder_id = folder_entity[\"id\"]\n\n    config_data = get_imageio_config_preset(\n        project_name,\n        folder_path,\n        task_name,\n        host_name,\n        app_name,\n        anatomy=anatomy,\n        project_settings=project_settings,\n        template_data=template_data,\n        folder_id=folder_id,\n    )\n\n    # caching invalid state, so it's not recalculated all the time\n    file_rules = None\n    if config_data:\n        file_rules = get_imageio_file_rules(\n            project_name,\n            host_name,\n            project_settings=project_settings\n        )\n\n    # caching settings for future instance processing\n    context_data[\"imageioSettings\"] = (config_data, file_rules)\n\n    return config_data, file_rules\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/colorspace.html#client.ayon_core.pipeline.colorspace.get_colorspaces_enumerator_items","title":"<code>get_colorspaces_enumerator_items(config_items, include_aliases=False, include_looks=False, include_roles=False, include_display_views=False)</code>","text":"<p>Get all colorspace data with labels</p> <p>Wrapper function for aggregating all names and its families. Families can be used for building menu and submenus in gui.</p> <p>Parameters:</p> Name Type Description Default <code>config_items</code> <code>dict[str, dict]</code> <p>Colorspace data coming from <code>get_ocio_config_colorspaces</code> function.</p> required <code>include_aliases</code> <code>bool</code> <p>Include aliases in result.</p> <code>False</code> <code>include_looks</code> <code>bool</code> <p>Include looks in result.</p> <code>False</code> <code>include_roles</code> <code>bool</code> <p>Include roles in result.</p> <code>False</code> <code>include_display_views</code> <code>bool</code> <p>Include display views in result.</p> <code>False</code> <p>Returns:</p> Type Description <p>list[tuple[str, str]]: Colorspace and family in couples.</p> Source code in <code>client/ayon_core/pipeline/colorspace.py</code> <pre><code>def get_colorspaces_enumerator_items(\n    config_items: dict[str, Any],\n    include_aliases: bool = False,\n    include_looks: bool = False,\n    include_roles: bool = False,\n    include_display_views: bool = False\n):\n    \"\"\"Get all colorspace data with labels\n\n    Wrapper function for aggregating all names and its families.\n    Families can be used for building menu and submenus in gui.\n\n    Args:\n        config_items (dict[str,dict]): Colorspace data coming from\n            `get_ocio_config_colorspaces` function.\n        include_aliases (bool): Include aliases in result.\n        include_looks (bool): Include looks in result.\n        include_roles (bool): Include roles in result.\n        include_display_views (bool): Include display views\n            in result.\n\n    Returns:\n        list[tuple[str, str]]: Colorspace and family in couples.\n\n    \"\"\"\n    aliases = set()\n    colorspaces = set()\n    looks = set()\n    roles = set()\n    display_views = set()\n    for items_type, colorspace_items in config_items.items():\n        if items_type == \"colorspaces\":\n            for color_name, color_data in colorspace_items.items():\n                if color_data.get(\"aliases\"):\n                    aliases.update({\n                        (\n                            \"aliases::{}\".format(alias_name),\n                            \"[alias] {} ({})\".format(alias_name, color_name)\n                        )\n                        for alias_name in color_data[\"aliases\"]\n                    })\n                colorspaces.add((\n                    \"{}::{}\".format(items_type, color_name),\n                    \"[colorspace] {}\".format(color_name)\n                ))\n\n        elif items_type == \"looks\":\n            looks.update({\n                (\n                    \"{}::{}\".format(items_type, name),\n                    \"[look] {} ({})\".format(name, role_data[\"process_space\"])\n                )\n                for name, role_data in colorspace_items.items()\n            })\n\n        elif items_type == \"displays_views\":\n            display_views.update({\n                (\n                    \"{}::{}\".format(items_type, name),\n                    \"[view (display)] {}\".format(name)\n                )\n                for name, _ in colorspace_items.items()\n            })\n\n        elif items_type == \"roles\":\n            roles.update({\n                (\n                    \"{}::{}\".format(items_type, name),\n                    \"[role] {} ({})\".format(name, role_data[\"colorspace\"])\n                )\n                for name, role_data in colorspace_items.items()\n            })\n\n    def _sort_key_getter(item):\n        \"\"\"Use colorspace for sorting.\n\n        Args:\n            item (tuple[str, str]): Item with colorspace and label.\n\n        Returns:\n            str: Colorspace.\n\n        \"\"\"\n        return item[0]\n\n    labeled_colorspaces = []\n    if include_roles:\n        labeled_colorspaces.extend(\n            sorted(roles, key=_sort_key_getter)\n        )\n\n    # Add colorspaces after roles, so it is not first in menu\n    labeled_colorspaces.extend(\n        sorted(colorspaces, key=_sort_key_getter)\n    )\n\n    if include_aliases:\n        labeled_colorspaces.extend(\n            sorted(aliases, key=_sort_key_getter)\n        )\n\n    if include_looks:\n        labeled_colorspaces.extend(\n            sorted(looks, key=_sort_key_getter)\n        )\n\n    if include_display_views:\n        labeled_colorspaces.extend(\n            sorted(display_views, key=_sort_key_getter)\n        )\n\n    return labeled_colorspaces\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/colorspace.html#client.ayon_core.pipeline.colorspace.get_config_file_rules_colorspace_from_filepath","title":"<code>get_config_file_rules_colorspace_from_filepath(config_path, filepath)</code>","text":"<p>Get colorspace from file path wrapper.</p> <p>Wrapper function for getting colorspace from file path with use of OCIO v2 file-rules.</p> <p>Parameters:</p> Name Type Description Default <code>config_path</code> <code>str</code> <p>path leading to config.ocio file</p> required <code>filepath</code> <code>str</code> <p>path leading to a file</p> required <p>Returns:</p> Type Description <p>Union[str, None]: matching colorspace name</p> Source code in <code>client/ayon_core/pipeline/colorspace.py</code> <pre><code>def get_config_file_rules_colorspace_from_filepath(config_path, filepath):\n    \"\"\"Get colorspace from file path wrapper.\n\n    Wrapper function for getting colorspace from file path\n    with use of OCIO v2 file-rules.\n\n    Args:\n        config_path (str): path leading to config.ocio file\n        filepath (str): path leading to a file\n\n    Returns:\n        Union[str, None]: matching colorspace name\n\n    \"\"\"\n    if has_compatible_ocio_package():\n        result_data = _get_config_file_rules_colorspace_from_filepath(\n            config_path, filepath\n        )\n    else:\n        result_data = _get_wrapped_with_subprocess(\n            \"get_config_file_rules_colorspace_from_filepath\",\n            config_path=config_path,\n            filepath=filepath\n        )\n\n    if result_data:\n        return result_data[0]\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/colorspace.html#client.ayon_core.pipeline.colorspace.get_config_version_data","title":"<code>get_config_version_data(config_path)</code>","text":"<p>Return major and minor version info.</p> <p>Parameters:</p> Name Type Description Default <code>config_path</code> <code>str</code> <p>path string leading to config.ocio</p> required <p>Raises:</p> Type Description <code>IOError</code> <p>Input config does not exist.</p> <p>Returns:</p> Name Type Description <code>dict</code> <p>minor and major keys with values</p> Source code in <code>client/ayon_core/pipeline/colorspace.py</code> <pre><code>def get_config_version_data(config_path):\n    \"\"\"Return major and minor version info.\n\n    Args:\n        config_path (str): path string leading to config.ocio\n\n    Raises:\n        IOError: Input config does not exist.\n\n    Returns:\n        dict: minor and major keys with values\n\n    \"\"\"\n    if config_path not in CachedData.config_version_data:\n        if has_compatible_ocio_package():\n            version_data = _get_config_version_data(config_path)\n        else:\n            version_data = _get_wrapped_with_subprocess(\n                \"get_config_version_data\",\n                config_path=config_path\n            )\n        CachedData.config_version_data[config_path] = version_data\n\n    return deepcopy(CachedData.config_version_data[config_path])\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/colorspace.html#client.ayon_core.pipeline.colorspace.get_current_context_imageio_config_preset","title":"<code>get_current_context_imageio_config_preset(anatomy=None, project_settings=None, template_data=None, env=None, app_name=None)</code>","text":"<p>Get ImageIO config preset for current context.</p> <p>Parameters:</p> Name Type Description Default <code>anatomy</code> <code>Anatomy | None</code> <p>Current project anatomy.</p> <code>None</code> <code>project_settings</code> <code>dict[str, Any] | NOne</code> <p>Current project settings.</p> <code>None</code> <code>template_data</code> <code>dict[str, Any] | None</code> <p>Prepared template data for current context.</p> <code>None</code> <code>env</code> <code>dict[str, str] | None</code> <p>Custom environment variable values.</p> <code>None</code> <code>app_name</code> <code>str | None</code> <p>Application name.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>dict</code> <p>ImageIO config preset.</p> Source code in <code>client/ayon_core/pipeline/colorspace.py</code> <pre><code>def get_current_context_imageio_config_preset(\n    anatomy: Anatomy | None = None,\n    project_settings: dict | None = None,\n    template_data: dict | None = None,\n    env: dict[str, str] | None = None,\n    app_name: str | None = None,\n):\n    \"\"\"Get ImageIO config preset for current context.\n\n    Args:\n        anatomy (Anatomy | None): Current project anatomy.\n        project_settings (dict[str, Any] | NOne): Current project settings.\n        template_data (dict[str, Any] | None): Prepared template data\n            for current context.\n        env (dict[str, str] | None): Custom environment variable values.\n        app_name (str | None): Application name.\n\n    Returns:\n        dict: ImageIO config preset.\n\n    \"\"\"\n    from .context_tools import get_current_context, get_current_host_name\n\n    context = get_current_context()\n    host_name = get_current_host_name()\n    if not app_name:\n        app_name = os.getenv(\"AYON_APP_NAME\")\n    return get_imageio_config_preset(\n        context[\"project_name\"],\n        context[\"folder_path\"],\n        context[\"task_name\"],\n        host_name,\n        app_name,\n        anatomy=anatomy,\n        project_settings=project_settings,\n        template_data=template_data,\n        env=env,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/colorspace.html#client.ayon_core.pipeline.colorspace.get_display_view_colorspace_name","title":"<code>get_display_view_colorspace_name(config_path, display, view)</code>","text":"<p>Returns the colorspace attribute of the (display, view) pair.</p> <p>Parameters:</p> Name Type Description Default <code>config_path</code> <code>str</code> <p>path string leading to config.ocio</p> required <code>display</code> <code>str</code> <p>display name e.g. \"ACES\"</p> required <code>view</code> <code>str</code> <p>view name e.g. \"sRGB\"</p> required <p>Returns:</p> Name Type Description <code>str</code> <p>View color space name. e.g. \"Output - sRGB\"</p> Source code in <code>client/ayon_core/pipeline/colorspace.py</code> <pre><code>def get_display_view_colorspace_name(config_path, display, view):\n    \"\"\"Returns the colorspace attribute of the (display, view) pair.\n\n    Args:\n        config_path (str): path string leading to config.ocio\n        display (str): display name e.g. \"ACES\"\n        view (str): view name e.g. \"sRGB\"\n\n    Returns:\n        str: View color space name. e.g. \"Output - sRGB\"\n\n    \"\"\"\n    if has_compatible_ocio_package():\n        return _get_display_view_colorspace_name(\n            config_path, display, view\n        )\n    return _get_wrapped_with_subprocess(\n        \"get_display_view_colorspace_name\",\n        config_path=config_path,\n        display=display,\n        view=view\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/colorspace.html#client.ayon_core.pipeline.colorspace.get_imageio_config","title":"<code>get_imageio_config(project_name, host_name, project_settings=None, anatomy_data=None, anatomy=None, env=None)</code>","text":"<p>Returns config data from settings</p> <p>Config path is formatted in <code>path</code> key and original settings input is saved into <code>template</code> key.</p> Deprecated <p>Deprecated since '0.3.1' . Use <code>get_imageio_config_preset</code> instead.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>project name</p> required <code>host_name</code> <code>str</code> <p>host name</p> required <code>project_settings</code> <code>Optional[dict]</code> <p>Project settings.</p> <code>None</code> <code>anatomy_data</code> <code>Optional[dict]</code> <p>anatomy formatting data.</p> <code>None</code> <code>anatomy</code> <code>Optional[Anatomy]</code> <p>Anatomy object.</p> <code>None</code> <code>env</code> <code>Optional[dict]</code> <p>Environment variables.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>dict</code> <p>config path data or empty dict</p> Source code in <code>client/ayon_core/pipeline/colorspace.py</code> <pre><code>@deprecated(\"get_imageio_config_preset\")\ndef get_imageio_config(\n    project_name: str,\n    host_name: str,\n    project_settings: dict | None = None,\n    anatomy_data: dict | None = None,\n    anatomy: Anatomy | None = None,\n    env: dict[str, str] | None = None,\n):\n    \"\"\"Returns config data from settings\n\n    Config path is formatted in `path` key\n    and original settings input is saved into `template` key.\n\n    Deprecated:\n        Deprecated since '0.3.1' . Use `get_imageio_config_preset` instead.\n\n    Args:\n        project_name (str): project name\n        host_name (str): host name\n        project_settings (Optional[dict]): Project settings.\n        anatomy_data (Optional[dict]): anatomy formatting data.\n        anatomy (Optional[Anatomy]): Anatomy object.\n        env (Optional[dict]): Environment variables.\n\n    Returns:\n        dict: config path data or empty dict\n\n    \"\"\"\n    if not anatomy_data:\n        from .context_tools import get_current_context_template_data\n        anatomy_data = get_current_context_template_data()\n\n    app_name = None\n    if env is not None:\n        app_name = env.get(\"AYON_APP_NAME\")\n\n    if not app_name:\n        app_name = os.getenv(\"AYON_APP_NAME\")\n\n    task_name = anatomy_data.get(\"task\", {}).get(\"name\")\n    folder_path = anatomy_data.get(\"folder\", {}).get(\"path\")\n    return get_imageio_config_preset(\n        project_name,\n        folder_path,\n        task_name,\n        host_name,\n        app_name,\n        anatomy=anatomy,\n        project_settings=project_settings,\n        template_data=anatomy_data,\n        env=env,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/colorspace.html#client.ayon_core.pipeline.colorspace.get_imageio_config_preset","title":"<code>get_imageio_config_preset(project_name, folder_path, task_name, host_name, app_name=None, *, anatomy=None, project_settings=None, template_data=None, env=None, folder_id=None)</code>","text":"<p>Returns config data from settings</p> <p>Output contains 'path' key and 'template' key holds its template.</p> <p>Template data can be prepared with 'get_template_data'.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>folder_path</code> <code>str</code> <p>Folder path.</p> required <code>task_name</code> <code>str</code> <p>Task name.</p> required <code>host_name</code> <code>str</code> <p>Host name.</p> required <code>app_name</code> <code>str</code> <p>Application name.</p> <code>None</code> <code>anatomy</code> <code>Optional[Anatomy]</code> <p>Project anatomy object.</p> <code>None</code> <code>project_settings</code> <code>Optional[dict]</code> <p>Project settings.</p> <code>None</code> <code>template_data</code> <code>Optional[dict]</code> <p>Template data used for template formatting.</p> <code>None</code> <code>env</code> <code>Optional[dict]</code> <p>Environment variables. Environments are used for template formatting too. Values from 'os.environ' are used when not provided.</p> <code>None</code> <code>folder_id</code> <code>Optional[str]</code> <p>Folder id. Is used only when config path is received from published representation. Is autofilled when not provided.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>dict</code> <p>config path data or empty dict</p> Source code in <code>client/ayon_core/pipeline/colorspace.py</code> <pre><code>def get_imageio_config_preset(\n    project_name: str,\n    folder_path: str,\n    task_name: str,\n    host_name: str,\n    app_name: str | None = None,\n    *,\n    anatomy: Anatomy | None = None,\n    project_settings: dict | None = None,\n    template_data: dict | None = None,\n    env: dict | None = None,\n    folder_id: str | None = None,\n):\n    \"\"\"Returns config data from settings\n\n    Output contains 'path' key and 'template' key holds its template.\n\n    Template data can be prepared with 'get_template_data'.\n\n    Args:\n        project_name (str): Project name.\n        folder_path (str): Folder path.\n        task_name (str): Task name.\n        host_name (str): Host name.\n        app_name (str): Application name.\n        anatomy (Optional[Anatomy]): Project anatomy object.\n        project_settings (Optional[dict]): Project settings.\n        template_data (Optional[dict]): Template data used for\n            template formatting.\n        env (Optional[dict]): Environment variables. Environments are used\n            for template formatting too. Values from 'os.environ' are used\n            when not provided.\n        folder_id (Optional[str]): Folder id. Is used only when config path\n            is received from published representation. Is autofilled when\n            not provided.\n\n    Returns:\n        dict: config path data or empty dict\n\n    \"\"\"\n    if not project_settings:\n        project_settings = get_project_settings(project_name)\n\n    # Get colorspace settings\n    imageio_global, imageio_host = _get_imageio_settings(\n        project_settings, host_name\n    )\n    # Global color management must be enabled to be able to use host settings\n    if not imageio_global[\"activate_global_color_management\"]:\n        log.info(\"Colorspace management is disabled globally.\")\n        return {}\n\n    # Host 'ocio_config' is optional\n    host_ocio_config = imageio_host.get(\"ocio_config\") or {}\n    # TODO remove\n    #  - backward compatibility when host settings had only 'enabled' flag\n    #      the flag was split into 'activate_global_color_management'\n    #      and 'override_global_config'\n    host_ocio_config_enabled = host_ocio_config.get(\"enabled\", False)\n\n    # Check if host settings group is having 'activate_host_color_management'\n    # - if it does not have activation key then default it to True so it uses\n    #       global settings\n    activate_host_color_management = imageio_host.get(\n        \"activate_host_color_management\"\n    )\n    if activate_host_color_management is None:\n        activate_host_color_management = host_ocio_config_enabled\n\n    if not activate_host_color_management:\n        # if host settings are disabled return False because\n        # it is expected that no colorspace management is needed\n        log.info(\n            f\"Colorspace management for host '{host_name}' is disabled.\"\n        )\n        return {}\n\n    project_entity = None\n    if anatomy is None:\n        project_entity = ayon_api.get_project(project_name)\n        anatomy = Anatomy(project_name, project_entity=project_entity)\n\n    if env is None:\n        env = dict(os.environ.items())\n\n    if template_data:\n        template_data = deepcopy(template_data)\n    else:\n        if not project_entity:\n            project_entity = ayon_api.get_project(project_name)\n\n        folder_entity = task_entity = None\n        if folder_path:\n            folder_entity = ayon_api.get_folder_by_path(\n                project_name, folder_path\n            )\n            folder_id = folder_entity[\"id\"]\n\n        if folder_id and task_name:\n            task_entity = ayon_api.get_task_by_name(\n                project_name, folder_id, task_name\n            )\n        template_data = get_template_data(\n            project_entity,\n            folder_entity,\n            task_entity,\n            host_name,\n            project_settings,\n        )\n\n    # Add project roots to anatomy data\n    template_data[\"root\"] = anatomy.roots\n    template_data[\"platform\"] = platform.system().lower()\n\n    # Add environment variables to template data\n    template_data.update(env)\n\n    # Get config path from core or host settings\n    #  - based on override flag in host settings\n    # TODO: in future rewrite this to be more explicit\n    override_global_config = host_ocio_config.get(\"override_global_config\")\n    if override_global_config is None:\n        override_global_config = host_ocio_config_enabled\n\n    if not override_global_config:\n        config_data = _get_global_config_data(\n            project_name,\n            host_name,\n            app_name,\n            anatomy=anatomy,\n            template_data=template_data,\n            imageio_global=imageio_global,\n            folder_id=folder_id,\n            log=log,\n        )\n    else:\n        config_data = _get_host_config_data(\n            host_ocio_config[\"filepath\"], template_data\n        )\n\n    if not config_data.enabled:\n        return {}  # OCIO management disabled\n\n    if not config_data.path:\n        raise FileExistsError(\n            \"No OCIO config found in settings. It is\"\n            \" either missing or there is typo in path inputs\"\n        )\n\n    return {\n        \"path\": config_data.path,\n        \"template\": config_data.template,\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/colorspace.html#client.ayon_core.pipeline.colorspace.get_imageio_file_rules","title":"<code>get_imageio_file_rules(project_name, host_name, project_settings=None)</code>","text":"<p>Get ImageIO File rules from project settings</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>host_name</code> <code>str</code> <p>Host name.</p> required <code>project_settings</code> <code>Optional[dict]</code> <p>Project settings.</p> <code>None</code> <p>Returns:</p> Type Description <p>list[dict[str, Any]]: file rules data</p> Source code in <code>client/ayon_core/pipeline/colorspace.py</code> <pre><code>def get_imageio_file_rules(project_name, host_name, project_settings=None):\n    \"\"\"Get ImageIO File rules from project settings\n\n    Args:\n        project_name (str): Project name.\n        host_name (str): Host name.\n        project_settings (Optional[dict]): Project settings.\n\n    Returns:\n        list[dict[str, Any]]: file rules data\n\n    \"\"\"\n    if not project_settings:\n        project_settings = get_project_settings(project_name)\n\n    imageio_global, imageio_host = _get_imageio_settings(\n        project_settings, host_name)\n\n    # host is optional, some might not have any settings\n    frules_host = imageio_host.get(\"file_rules\", {})\n\n    # compile file rules dictionary\n    activate_host_rules = frules_host.get(\"activate_host_rules\")\n    if activate_host_rules is None:\n        # TODO: remove this in future - backward compatibility\n        activate_host_rules = frules_host.get(\"enabled\", False)\n\n    if activate_host_rules:\n        return frules_host[\"rules\"]\n\n    # get file rules from global and host_name\n    frules_global = imageio_global[\"file_rules\"]\n    activate_global_rules = (\n        frules_global.get(\"activate_global_file_rules\", False)\n        # TODO: remove this in future - backward compatibility\n        or frules_global.get(\"enabled\")\n    )\n\n    if not activate_global_rules:\n        log.info(\n            \"Colorspace global file rules are disabled.\"\n        )\n        return []\n\n    return frules_global[\"rules\"]\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/colorspace.html#client.ayon_core.pipeline.colorspace.get_imageio_file_rules_colorspace_from_filepath","title":"<code>get_imageio_file_rules_colorspace_from_filepath(filepath, host_name, project_name, config_data, file_rules=None, project_settings=None)</code>","text":"<p>Get colorspace name from filepath</p> <p>ImageIO Settings file rules are tested for matching rule.</p> <p>Parameters:</p> Name Type Description Default <code>filepath</code> <code>str</code> <p>Path string, file rule pattern is tested on it.</p> required <code>host_name</code> <code>str</code> <p>Host name.</p> required <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>config_data</code> <code>dict</code> <p>Config path and template in dict.</p> required <code>file_rules</code> <code>Optional[dict]</code> <p>File rule data from settings.</p> <code>None</code> <code>project_settings</code> <code>Optional[dict]</code> <p>Project settings.</p> <code>None</code> <p>Returns:</p> Type Description <p>Union[str, None]: Name of colorspace.</p> Source code in <code>client/ayon_core/pipeline/colorspace.py</code> <pre><code>def get_imageio_file_rules_colorspace_from_filepath(\n    filepath,\n    host_name,\n    project_name,\n    config_data,\n    file_rules=None,\n    project_settings=None\n):\n    \"\"\"Get colorspace name from filepath\n\n    ImageIO Settings file rules are tested for matching rule.\n\n    Args:\n        filepath (str): Path string, file rule pattern is tested on it.\n        host_name (str): Host name.\n        project_name (str): Project name.\n        config_data (dict): Config path and template in dict.\n        file_rules (Optional[dict]): File rule data from settings.\n        project_settings (Optional[dict]): Project settings.\n\n    Returns:\n        Union[str, None]: Name of colorspace.\n\n    \"\"\"\n    if not config_data:\n        # in case global or host color management is not enabled\n        return None\n\n    if file_rules is None:\n        if project_settings is None:\n            project_settings = get_project_settings(project_name)\n        file_rules = get_imageio_file_rules(\n            project_name, host_name, project_settings\n        )\n\n    # match file rule from path\n    colorspace_name = None\n    for file_rule in file_rules:\n        pattern = file_rule[\"pattern\"]\n        extension = file_rule[\"ext\"]\n        ext_match = re.match(\n            r\".*(?=.{})\".format(extension), filepath\n        )\n        file_match = re.search(\n            pattern, filepath\n        )\n\n        if ext_match and file_match:\n            colorspace_name = file_rule[\"colorspace\"]\n\n    return colorspace_name\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/colorspace.html#client.ayon_core.pipeline.colorspace.get_ocio_config_colorspaces","title":"<code>get_ocio_config_colorspaces(config_path)</code>","text":"<p>Get all colorspace data</p> <p>Wrapper function for aggregating all names and its families. Families can be used for building menu and submenus in gui.</p> <p>Parameters:</p> Name Type Description Default <code>config_path</code> <code>str</code> <p>path leading to config.ocio file</p> required <p>Returns:</p> Name Type Description <code>dict</code> <p>colorspace and family in couple</p> Source code in <code>client/ayon_core/pipeline/colorspace.py</code> <pre><code>def get_ocio_config_colorspaces(config_path):\n    \"\"\"Get all colorspace data\n\n    Wrapper function for aggregating all names and its families.\n    Families can be used for building menu and submenus in gui.\n\n    Args:\n        config_path (str): path leading to config.ocio file\n\n    Returns:\n        dict: colorspace and family in couple\n\n    \"\"\"\n    if config_path not in CachedData.ocio_config_colorspaces:\n        if has_compatible_ocio_package():\n            config_colorspaces = _get_ocio_config_colorspaces(config_path)\n        else:\n            config_colorspaces = _get_wrapped_with_subprocess(\n                \"get_ocio_config_colorspaces\",\n                config_path=config_path\n            )\n        CachedData.ocio_config_colorspaces[config_path] = config_colorspaces\n\n    return deepcopy(CachedData.ocio_config_colorspaces[config_path])\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/colorspace.html#client.ayon_core.pipeline.colorspace.get_ocio_config_script_path","title":"<code>get_ocio_config_script_path()</code>","text":"<p>Get path to ocio wrapper script</p> <p>Returns:</p> Name Type Description <code>str</code> <p>path string</p> Source code in <code>client/ayon_core/pipeline/colorspace.py</code> <pre><code>def get_ocio_config_script_path():\n    \"\"\"Get path to ocio wrapper script\n\n    Returns:\n        str: path string\n\n    \"\"\"\n    return os.path.join(\n        os.path.normpath(AYON_CORE_ROOT),\n        \"scripts\",\n        \"ocio_wrapper.py\"\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/colorspace.html#client.ayon_core.pipeline.colorspace.get_ocio_config_views","title":"<code>get_ocio_config_views(config_path)</code>","text":"<p>Get all viewer data</p> <p>Wrapper function for aggregating all display and related viewers. Key can be used for building gui menu with submenus.</p> <p>Parameters:</p> Name Type Description Default <code>config_path</code> <code>str</code> <p>path leading to config.ocio file</p> required <p>Returns:</p> Name Type Description <code>dict</code> <p><code>display/viewer</code> and viewer data</p> Source code in <code>client/ayon_core/pipeline/colorspace.py</code> <pre><code>def get_ocio_config_views(config_path):\n    \"\"\"Get all viewer data\n\n    Wrapper function for aggregating all display and related viewers.\n    Key can be used for building gui menu with submenus.\n\n    Args:\n        config_path (str): path leading to config.ocio file\n\n    Returns:\n        dict: `display/viewer` and viewer data\n\n    \"\"\"\n    if has_compatible_ocio_package():\n        return _get_ocio_config_views(config_path)\n\n    return _get_wrapped_with_subprocess(\n        \"get_ocio_config_views\",\n        config_path=config_path\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/colorspace.html#client.ayon_core.pipeline.colorspace.get_remapped_colorspace_from_native","title":"<code>get_remapped_colorspace_from_native(host_native_colorspace_name, host_name, imageio_host_settings)</code>","text":"<p>Return ocio colorspace name remapped from host native used name.</p> <p>Parameters:</p> Name Type Description Default <code>host_native_colorspace_name</code> <code>str</code> <p>host native colorspace name</p> required <code>host_name</code> <code>str</code> <p>Host name.</p> required <code>imageio_host_settings</code> <code>dict[str, Any]</code> <p>ImageIO host settings.</p> required <p>Returns:</p> Type Description <p>Union[str, None]: Ocio colorspace name defined in remapping or None.</p> Source code in <code>client/ayon_core/pipeline/colorspace.py</code> <pre><code>def get_remapped_colorspace_from_native(\n    host_native_colorspace_name, host_name, imageio_host_settings\n):\n    \"\"\"Return ocio colorspace name remapped from host native used name.\n\n    Args:\n        host_native_colorspace_name (str): host native colorspace name\n        host_name (str): Host name.\n        imageio_host_settings (dict[str, Any]): ImageIO host settings.\n\n    Returns:\n        Union[str, None]: Ocio colorspace name defined in remapping or None.\n\n    \"\"\"\n    host_mapping = CachedData.remapping.setdefault(host_name, {})\n    if \"from_native\" not in host_mapping:\n        remapping_rules = imageio_host_settings[\"remapping\"][\"rules\"]\n        host_mapping[\"from_native\"] = {\n            rule[\"host_native_name\"]: rule[\"ocio_name\"]\n            for rule in remapping_rules\n        }\n\n    return host_mapping[\"from_native\"].get(host_native_colorspace_name)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/colorspace.html#client.ayon_core.pipeline.colorspace.get_remapped_colorspace_to_native","title":"<code>get_remapped_colorspace_to_native(ocio_colorspace_name, host_name, imageio_host_settings)</code>","text":"<p>Return native colorspace name.</p> <p>Parameters:</p> Name Type Description Default <code>ocio_colorspace_name</code> <code>str | None</code> <p>OCIO colorspace name.</p> required <code>host_name</code> <code>str</code> <p>Host name.</p> required <code>imageio_host_settings</code> <code>dict[str, Any]</code> <p>ImageIO host settings.</p> required <p>Returns:</p> Type Description <p>Union[str, None]: native colorspace name defined in remapping or None</p> Source code in <code>client/ayon_core/pipeline/colorspace.py</code> <pre><code>def get_remapped_colorspace_to_native(\n    ocio_colorspace_name, host_name, imageio_host_settings\n):\n    \"\"\"Return native colorspace name.\n\n    Args:\n        ocio_colorspace_name (str | None): OCIO colorspace name.\n        host_name (str): Host name.\n        imageio_host_settings (dict[str, Any]): ImageIO host settings.\n\n    Returns:\n        Union[str, None]: native colorspace name defined in remapping or None\n    \"\"\"\n\n    host_mapping = CachedData.remapping.setdefault(host_name, {})\n    if \"to_native\" not in host_mapping:\n        remapping_rules = imageio_host_settings[\"remapping\"][\"rules\"]\n        host_mapping[\"to_native\"] = {\n            rule[\"ocio_name\"]: rule[\"host_native_name\"]\n            for rule in remapping_rules\n        }\n\n    return host_mapping[\"to_native\"].get(ocio_colorspace_name)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/colorspace.html#client.ayon_core.pipeline.colorspace.get_representation_ocio_config_path","title":"<code>get_representation_ocio_config_path(publish_repre, anatomy, logger=None)</code>","text":"<p>Return the OCIO config path from representation's colorspace data.</p> <p>If not colorspace data is found, then <code>None</code> is returned.</p> <p>If the colorspace data  <code>path</code> data does not point to an existing file, then the <code>template</code> will be filled with root paths by Anatomy to try and find a local equivalent of the filepath. If that's also not found, then <code>None</code> is returned.</p> Source code in <code>client/ayon_core/pipeline/colorspace.py</code> <pre><code>def get_representation_ocio_config_path(\n    publish_repre: dict[str, Any],\n    anatomy: Anatomy,\n    logger: Optional[logging.Logger] = None,\n) -&gt; Optional[str]:\n    \"\"\"Return the OCIO config path from representation's colorspace data.\n\n    If not colorspace data is found, then `None` is returned.\n\n    If the colorspace data  `path` data does not point to an existing file,\n    then the `template` will be filled with root paths by Anatomy to try and\n    find a local equivalent of the filepath. If that's also not found,\n    then `None` is returned.\n\n    \"\"\"\n    if logger is None:\n        logger = log\n    colorspace_data = publish_repre.get(\"colorspaceData\") or {}\n    config = colorspace_data.get(\"config\")\n    if not config:\n        logger.debug(\n            \"No OCIO Config file for representation,\"\n            \" because it has no colorspace data.\"\n        )\n        return None\n\n    config_template: str = config.get(\"template\", \"\")\n    config_path: str = config.get(\"path\", \"\")\n    if not config_path and not config_template:\n        logger.warning(\n            \"OCIO Config file is not set for representation.\"\n        )\n        return None\n\n    # First we check the lightest route and just see if path is a valid file.\n    # When it is not we'll assume that the path is e.g. from another machine\n    # or another platform and may need to be computed from the template\n    if config_path:\n        if os.path.isfile(config_path):\n            return config_path\n        logger.debug(\n            \"OCIO Config file for representation not found at:\"\n            f\" {config_path}\"\n        )\n\n    if not config_template:\n        return None\n\n    # Fill template with environment variables\n    # - use StringTemplate to ignore unfilled values so anatomy can try\n    config_template = str(StringTemplate.format_template(\n        config_template, os.environ.copy()\n    ))\n    config_path = anatomy.fill_root(config_template)\n    if os.path.isfile(config_path):\n        return config_path\n\n    logger.warning(\n        \"OCIO Config file for representation not found at:\"\n        f\" {config_path}\"\n    )\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/colorspace.html#client.ayon_core.pipeline.colorspace.has_compatible_ocio_package","title":"<code>has_compatible_ocio_package()</code>","text":"<p>Current process has available compatible 'PyOpenColorIO'.</p> <p>Returns:</p> Name Type Description <code>bool</code> <p>True if compatible package is available.</p> Source code in <code>client/ayon_core/pipeline/colorspace.py</code> <pre><code>def has_compatible_ocio_package():\n    \"\"\"Current process has available compatible 'PyOpenColorIO'.\n\n    Returns:\n        bool: True if compatible package is available.\n\n    \"\"\"\n    if CachedData.has_compatible_ocio_package is not None:\n        return CachedData.has_compatible_ocio_package\n\n    is_compatible = False\n    try:\n        import PyOpenColorIO\n\n        # Check if PyOpenColorIO is compatible\n        # - version 2.0.0 or higher is required\n        # NOTE version 1 does not have '__version__' attribute\n        if hasattr(PyOpenColorIO, \"__version__\"):\n            version_parts = PyOpenColorIO.__version__.split(\".\")\n            major = int(version_parts[0])\n            is_compatible = (major, ) &gt;= (2, )\n    except ImportError:\n        pass\n\n    CachedData.has_compatible_ocio_package = is_compatible\n    # compatible\n    return CachedData.has_compatible_ocio_package\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/colorspace.html#client.ayon_core.pipeline.colorspace.parse_colorspace_from_filepath","title":"<code>parse_colorspace_from_filepath(filepath, colorspaces=None, config_path=None)</code>","text":"<p>Parse colorspace name from filepath</p> <p>An input path can have colorspace name used as part of name or as folder name.</p> Example <p>config_path = \"path/to/config.ocio\" colorspaces = get_ocio_config_colorspaces(config_path) colorspace = parse_colorspace_from_filepath(         \"path/to/file/acescg/file.exr\",         colorspaces=colorspaces     ) print(colorspace) acescg</p> <p>Parameters:</p> Name Type Description Default <code>filepath</code> <code>str</code> <p>path string</p> required <code>colorspaces</code> <code>Optional[dict[str]]</code> <p>list of colorspaces</p> <code>None</code> <code>config_path</code> <code>Optional[str]</code> <p>path to config.ocio file</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <p>name of colorspace</p> Source code in <code>client/ayon_core/pipeline/colorspace.py</code> <pre><code>def parse_colorspace_from_filepath(\n    filepath, colorspaces=None, config_path=None\n):\n    \"\"\"Parse colorspace name from filepath\n\n    An input path can have colorspace name used as part of name\n    or as folder name.\n\n    Example:\n        &gt;&gt;&gt; config_path = \"path/to/config.ocio\"\n        &gt;&gt;&gt; colorspaces = get_ocio_config_colorspaces(config_path)\n        &gt;&gt;&gt; colorspace = parse_colorspace_from_filepath(\n                \"path/to/file/acescg/file.exr\",\n                colorspaces=colorspaces\n            )\n        &gt;&gt;&gt; print(colorspace)\n        acescg\n\n    Args:\n        filepath (str): path string\n        colorspaces (Optional[dict[str]]): list of colorspaces\n        config_path (Optional[str]): path to config.ocio file\n\n    Returns:\n        str: name of colorspace\n    \"\"\"\n    def _get_colorspace_match_regex(colorspaces):\n        \"\"\"Return a regex pattern\n\n        Allows to search a colorspace match in a filename\n\n        Args:\n            colorspaces (list): List of colorspace names\n\n        Returns:\n            re.Pattern: regex pattern\n        \"\"\"\n        pattern = \"|\".join(\n            # Allow to match spaces also as underscores because the\n            # integrator replaces spaces with underscores in filenames\n            re.escape(colorspace)\n            # Sort by longest first so the regex matches longer matches\n            # over smaller matches, e.g. matching 'Output - sRGB' over 'sRGB'\n            for colorspace in sorted(colorspaces, key=len, reverse=True)\n        )\n        return re.compile(pattern)\n\n    if not colorspaces and not config_path:\n        raise ValueError(\n            \"Must provide `config_path` if `colorspaces` is not provided.\"\n        )\n\n    colorspaces = (\n        colorspaces\n        or get_ocio_config_colorspaces(config_path)[\"colorspaces\"]\n    )\n    underscored_colorspaces = {\n        key.replace(\" \", \"_\"): key for key in colorspaces\n        if \" \" in key\n    }\n\n    # match colorspace from  filepath\n    regex_pattern = _get_colorspace_match_regex(\n        list(colorspaces) + list(underscored_colorspaces))\n    match = regex_pattern.search(filepath)\n    colorspace = match.group(0) if match else None\n\n    if colorspace in underscored_colorspaces:\n        return underscored_colorspaces[colorspace]\n\n    if colorspace:\n        return colorspace\n\n    log.info(\"No matching colorspace in config '{}' for path: '{}'\".format(\n        config_path, filepath\n    ))\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/colorspace.html#client.ayon_core.pipeline.colorspace.set_colorspace_data_to_representation","title":"<code>set_colorspace_data_to_representation(representation, context_data, colorspace=None, log=None)</code>","text":"<p>Sets colorspace data to representation.</p> Example <pre><code>{\n    # for other publish plugins and loaders\n    \"colorspace\": \"linear\",\n    \"config\": {\n        # for future references in case need\n        \"path\": \"/abs/path/to/config.ocio\",\n        # for other plugins within remote publish cases\n        \"template\": \"{project[root]}/path/to/config.ocio\"\n    }\n}\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>representation</code> <code>dict</code> <p>publishing representation</p> required <code>context_data</code> <code>data</code> <p>publishing context data</p> required <code>colorspace</code> <code>Optional[str]</code> <p>Colorspace name.</p> <code>None</code> <code>log</code> <code>Optional[Logger]</code> <p>logger instance.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/colorspace.py</code> <pre><code>def set_colorspace_data_to_representation(\n    representation,\n    context_data,\n    colorspace=None,\n    log=None\n):\n    \"\"\"Sets colorspace data to representation.\n\n    Example:\n        ```\n        {\n            # for other publish plugins and loaders\n            \"colorspace\": \"linear\",\n            \"config\": {\n                # for future references in case need\n                \"path\": \"/abs/path/to/config.ocio\",\n                # for other plugins within remote publish cases\n                \"template\": \"{project[root]}/path/to/config.ocio\"\n            }\n        }\n        ```\n\n    Args:\n        representation (dict): publishing representation\n        context_data (publish.Context.data): publishing context data\n        colorspace (Optional[str]): Colorspace name.\n        log (Optional[logging.Logger]): logger instance.\n\n    \"\"\"\n    log = log or Logger.get_logger(__name__)\n\n    file_ext = representation[\"ext\"]\n\n    # check if `file_ext` in lower case is in CachedData.allowed_exts\n    if file_ext.lstrip(\".\").lower() not in CachedData.allowed_exts:\n        log.debug(\n            \"Extension '{}' is not in allowed extensions.\".format(file_ext)\n        )\n        return\n\n    # get colorspace settings\n    config_data, file_rules = get_colorspace_settings_from_publish_context(\n        context_data)\n\n    # in case host color management is not enabled\n    if not config_data:\n        log.warning(\"Host's colorspace management is disabled.\")\n        return\n\n    log.debug(\"Config data is: `{}`\".format(config_data))\n\n    project_name = context_data[\"projectName\"]\n    host_name = context_data[\"hostName\"]\n    project_settings = context_data[\"project_settings\"]\n\n    # get one filename\n    filename = representation[\"files\"]\n    if isinstance(filename, list):\n        filename = filename[0]\n\n    # get matching colorspace from rules\n    if colorspace is None:\n        colorspace = get_imageio_file_rules_colorspace_from_filepath(\n            filename,\n            host_name,\n            project_name,\n            config_data=config_data,\n            file_rules=file_rules,\n            project_settings=project_settings\n        )\n\n    # infuse data to representation\n    if colorspace:\n        colorspace_data = {\n            \"colorspace\": colorspace,\n            \"config\": config_data\n        }\n\n        # update data key\n        representation[\"colorspaceData\"] = colorspace_data\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/colorspace.html#client.ayon_core.pipeline.colorspace.validate_imageio_colorspace_in_config","title":"<code>validate_imageio_colorspace_in_config(config_path, colorspace_name)</code>","text":"<p>Validator making sure colorspace name is used in config.ocio</p> <p>Parameters:</p> Name Type Description Default <code>config_path</code> <code>str</code> <p>path leading to config.ocio file</p> required <code>colorspace_name</code> <code>str</code> <p>tested colorspace name</p> required <p>Raises:</p> Type Description <code>KeyError</code> <p>missing colorspace name</p> <p>Returns:</p> Name Type Description <code>bool</code> <p>True if exists</p> Source code in <code>client/ayon_core/pipeline/colorspace.py</code> <pre><code>def validate_imageio_colorspace_in_config(config_path, colorspace_name):\n    \"\"\"Validator making sure colorspace name is used in config.ocio\n\n    Args:\n        config_path (str): path leading to config.ocio file\n        colorspace_name (str): tested colorspace name\n\n    Raises:\n        KeyError: missing colorspace name\n\n    Returns:\n        bool: True if exists\n\n    \"\"\"\n    colorspaces = get_ocio_config_colorspaces(config_path)[\"colorspaces\"]\n    if colorspace_name not in colorspaces:\n        raise KeyError(\n            \"Missing colorspace '{}' in config file '{}'\".format(\n                colorspace_name, config_path)\n        )\n    return True\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/compatibility.html","title":"compatibility","text":"<p>Package to handle compatibility checks for pipeline components.</p>"},{"location":"autoapi/client/ayon_core/pipeline/compatibility.html#client.ayon_core.pipeline.compatibility.is_product_base_type_supported","title":"<code>is_product_base_type_supported()</code>","text":"<p>Check support for product base types.</p> <p>This function checks if the current pipeline supports product base types. Once this feature is implemented, it will return True. This should be used in places where some kind of backward compatibility is needed to avoid breaking existing functionality that relies on the current behavior.</p> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if product base types are supported, False otherwise.</p> Source code in <code>client/ayon_core/pipeline/compatibility.py</code> <pre><code>def is_product_base_type_supported() -&gt; bool:\n    \"\"\"Check support for product base types.\n\n    This function checks if the current pipeline supports product base types.\n    Once this feature is implemented, it will return True. This should be used\n    in places where some kind of backward compatibility is needed to avoid\n    breaking existing functionality that relies on the current behavior.\n\n    Returns:\n        bool: True if product base types are supported, False otherwise.\n\n    \"\"\"\n\n    if not hasattr(ayon_api, \"is_product_base_type_supported\"):\n        return False\n    return ayon_api.is_product_base_type_supported()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/constants.html","title":"constants","text":""},{"location":"autoapi/client/ayon_core/pipeline/context_tools.html","title":"context_tools","text":"<p>Core pipeline functionality</p>"},{"location":"autoapi/client/ayon_core/pipeline/context_tools.html#client.ayon_core.pipeline.context_tools.change_current_context","title":"<code>change_current_context(folder_entity, task_entity, *, template_key=_PLACEHOLDER, reason=None, project_entity=None, anatomy=None)</code>","text":"<p>Update active Session to a new task work area.</p> <p>This updates the live Session to a different task under a folder.</p> Notes <ul> <li>This function does a lot of things related to workfiles which     extends arguments options a lot.</li> <li>We might want to implement 'set_current_context' on host integration     instead. But <code>AYON_WORKDIR</code>, which is related to 'IWorkfileHost',     would not be available in that case which might break some     logic.</li> </ul> <p>Parameters:</p> Name Type Description Default <code>folder_entity</code> <code>Dict[str, Any]</code> <p>Folder entity to set.</p> required <code>task_entity</code> <code>Dict[str, Any]</code> <p>Task entity to set.</p> required <code>template_key</code> <code>Optional[str]</code> <p>DEPRECATED: Prepared template key to be used for workfile template in Anatomy.</p> <code>_PLACEHOLDER</code> <code>reason</code> <code>Optional[str]</code> <p>Reason for changing context.</p> <code>None</code> <code>anatomy</code> <code>Optional[Anatomy]</code> <p>Anatomy object used for workdir calculation.</p> <code>None</code> <code>project_entity</code> <code>Optional[dict[str, Any]]</code> <p>Project entity used for workdir calculation.</p> <code>None</code> <p>Returns:</p> Type Description <code>dict[str, str]</code> <p>dict[str, str]: New context data.</p> Source code in <code>client/ayon_core/pipeline/context_tools.py</code> <pre><code>def change_current_context(\n    folder_entity: dict[str, Any],\n    task_entity: dict[str, Any],\n    *,\n    template_key: Optional[str] = _PLACEHOLDER,\n    reason: Optional[str] = None,\n    project_entity: Optional[dict[str, Any]] = None,\n    anatomy: Optional[Anatomy] = None,\n) -&gt; dict[str, str]:\n    \"\"\"Update active Session to a new task work area.\n\n    This updates the live Session to a different task under a folder.\n\n    Notes:\n        * This function does a lot of things related to workfiles which\n            extends arguments options a lot.\n        * We might want to implement 'set_current_context' on host integration\n            instead. But `AYON_WORKDIR`, which is related to 'IWorkfileHost',\n            would not be available in that case which might break some\n            logic.\n\n    Args:\n        folder_entity (Dict[str, Any]): Folder entity to set.\n        task_entity (Dict[str, Any]): Task entity to set.\n        template_key (Optional[str]): DEPRECATED: Prepared template key to\n            be used for workfile template in Anatomy.\n        reason (Optional[str]): Reason for changing context.\n        anatomy (Optional[Anatomy]): Anatomy object used for workdir\n            calculation.\n        project_entity (Optional[dict[str, Any]]): Project entity used for\n            workdir calculation.\n\n    Returns:\n        dict[str, str]: New context data.\n\n    \"\"\"\n    if template_key is not _PLACEHOLDER:\n        warnings.warn(\n            (\n                \"Used deprecated argument 'template_key' in\"\n                \" 'change_current_context'.\"\n                \" It is not necessary to pass it in anymore.\"\n            ),\n            DeprecationWarning,\n            stacklevel=2,\n        )\n\n    host = registered_host()\n    return host.set_current_context(\n        folder_entity,\n        task_entity,\n        reason=reason,\n        project_entity=project_entity,\n        anatomy=anatomy,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/context_tools.html#client.ayon_core.pipeline.context_tools.get_current_context_custom_workfile_template","title":"<code>get_current_context_custom_workfile_template(project_settings=None)</code>","text":"<p>Filter and fill workfile template profiles by current context.</p> <p>This function can be used only inside host where current context is set.</p> <p>Parameters:</p> Name Type Description Default <code>project_settings</code> <code>Optional[dict[str, Any]]</code> <p>Project settings</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <p>Path to template or None if none of profiles match current context. (Existence of formatted path is not validated.)</p> Source code in <code>client/ayon_core/pipeline/context_tools.py</code> <pre><code>def get_current_context_custom_workfile_template(project_settings=None):\n    \"\"\"Filter and fill workfile template profiles by current context.\n\n    This function can be used only inside host where current context is set.\n\n    Args:\n        project_settings (Optional[dict[str, Any]]): Project settings\n\n    Returns:\n        str: Path to template or None if none of profiles match current\n            context. (Existence of formatted path is not validated.)\n\n    \"\"\"\n    context = get_current_context()\n    return get_custom_workfile_template_by_string_context(\n        context[\"project_name\"],\n        context[\"folder_path\"],\n        context[\"task_name\"],\n        get_current_host_name(),\n        project_settings=project_settings\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/context_tools.html#client.ayon_core.pipeline.context_tools.get_current_context_template_data","title":"<code>get_current_context_template_data(settings=None)</code>","text":"<p>Prepare template data for current context.</p> <p>Parameters:</p> Name Type Description Default <code>settings</code> <code>Optional[Dict[str, Any]]</code> <p>Prepared studio or project settings.</p> <code>None</code> <p>Returns:</p> Type Description <p>Dict[str, Any] Template data for current context.</p> Source code in <code>client/ayon_core/pipeline/context_tools.py</code> <pre><code>def get_current_context_template_data(settings=None):\n    \"\"\"Prepare template data for current context.\n\n    Args:\n        settings (Optional[Dict[str, Any]]): Prepared studio or\n            project settings.\n\n    Returns:\n        Dict[str, Any] Template data for current context.\n    \"\"\"\n\n    context = get_current_context()\n    project_name = context[\"project_name\"]\n    folder_path = context[\"folder_path\"]\n    task_name = context[\"task_name\"]\n    host_name = get_current_host_name()\n\n    return get_template_data_with_names(\n        project_name, folder_path, task_name, host_name, settings\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/context_tools.html#client.ayon_core.pipeline.context_tools.get_current_folder_entity","title":"<code>get_current_folder_entity(fields=None)</code>","text":"<p>Helper function to get folder entity based on current context.</p> <p>This function should be called only in process where host is installed.</p> <p>Folder is based on current context project name and folder path.</p> <p>Parameters:</p> Name Type Description Default <code>fields</code> <code>Optional[Iterable[str]]</code> <p>Limit returned data of folder entity to specific keys.</p> <code>None</code> <p>Returns:</p> Type Description <p>Union[dict[str, Any], None]: Folder entity or None.</p> Source code in <code>client/ayon_core/pipeline/context_tools.py</code> <pre><code>def get_current_folder_entity(fields=None):\n    \"\"\"Helper function to get folder entity based on current context.\n\n    This function should be called only in process where host is installed.\n\n    Folder is based on current context project name and folder path.\n\n    Args:\n        fields (Optional[Iterable[str]]): Limit returned data of folder entity\n            to specific keys.\n\n    Returns:\n        Union[dict[str, Any], None]: Folder entity or None.\n\n    \"\"\"\n    context = get_current_context()\n    project_name = context[\"project_name\"]\n    folder_path = context[\"folder_path\"]\n\n    # Skip if is not set even on context\n    if not project_name or not folder_path:\n        return None\n    return ayon_api.get_folder_by_path(\n        project_name, folder_path, fields=fields\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/context_tools.html#client.ayon_core.pipeline.context_tools.get_current_host_name","title":"<code>get_current_host_name()</code>","text":"<p>Current host name.</p> <p>Function is based on currently registered host integration or environment variable 'AYON_HOST_NAME'.</p> <p>Returns:</p> Type Description <p>Union[str, None]: Name of host integration in current process or None.</p> Source code in <code>client/ayon_core/pipeline/context_tools.py</code> <pre><code>def get_current_host_name():\n    \"\"\"Current host name.\n\n    Function is based on currently registered host integration or environment\n    variable 'AYON_HOST_NAME'.\n\n    Returns:\n        Union[str, None]: Name of host integration in current process or None.\n    \"\"\"\n\n    host = registered_host()\n    if isinstance(host, AbstractHost):\n        return host.name\n    return os.environ.get(\"AYON_HOST_NAME\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/context_tools.html#client.ayon_core.pipeline.context_tools.get_current_project_entity","title":"<code>get_current_project_entity(fields=None)</code>","text":"<p>Helper function to get project document based on global Session.</p> <p>This function should be called only in process where host is installed.</p> <p>Parameters:</p> Name Type Description Default <code>fields</code> <code>Optional[Iterable[str]]</code> <p>Limit returned data of project entity.</p> <code>None</code> <p>Returns:</p> Type Description <p>Union[dict[str, Any], None]: Project entity of current project or None.</p> Source code in <code>client/ayon_core/pipeline/context_tools.py</code> <pre><code>def get_current_project_entity(fields=None):\n    \"\"\"Helper function to get project document based on global Session.\n\n    This function should be called only in process where host is installed.\n\n    Args:\n        fields (Optional[Iterable[str]]): Limit returned data of project\n            entity.\n\n    Returns:\n        Union[dict[str, Any], None]: Project entity of current project or None.\n\n    \"\"\"\n    project_name = get_current_project_name()\n    return ayon_api.get_project(project_name, fields=fields)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/context_tools.html#client.ayon_core.pipeline.context_tools.get_current_project_settings","title":"<code>get_current_project_settings()</code>","text":"<p>Project settings for the current context project.</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: Project settings for the current context project.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If current project is not set.</p> Source code in <code>client/ayon_core/pipeline/context_tools.py</code> <pre><code>def get_current_project_settings() -&gt; dict[str, Any]:\n    \"\"\"Project settings for the current context project.\n\n    Returns:\n        dict[str, Any]: Project settings for the current context project.\n\n    Raises:\n        ValueError: If current project is not set.\n\n    \"\"\"\n    project_name = get_current_project_name()\n    if not project_name:\n        raise ValueError(\n            \"Current project is not set. Can't get project settings.\"\n        )\n    return get_project_settings(project_name)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/context_tools.html#client.ayon_core.pipeline.context_tools.get_current_task_entity","title":"<code>get_current_task_entity(fields=None)</code>","text":"<p>Helper function to get task entity based on current context.</p> <p>This function should be called only in process where host is installed.</p> <p>Task is based on current context project name, folder path     and task name.</p> <p>Parameters:</p> Name Type Description Default <code>fields</code> <code>Optional[Iterable[str]]</code> <p>Limit returned data of task entity to specific keys.</p> <code>None</code> <p>Returns:</p> Type Description <p>Union[dict[str, Any], None]: Task entity or None.</p> Source code in <code>client/ayon_core/pipeline/context_tools.py</code> <pre><code>def get_current_task_entity(fields=None):\n    \"\"\"Helper function to get task entity based on current context.\n\n    This function should be called only in process where host is installed.\n\n    Task is based on current context project name, folder path\n        and task name.\n\n    Args:\n        fields (Optional[Iterable[str]]): Limit returned data of task entity\n            to specific keys.\n\n    Returns:\n        Union[dict[str, Any], None]: Task entity or None.\n\n    \"\"\"\n    context = get_current_context()\n    project_name = context[\"project_name\"]\n    folder_path = context[\"folder_path\"]\n    task_name = context[\"task_name\"]\n\n    # Skip if is not set even on context\n    if not project_name or not folder_path or not task_name:\n        return None\n    folder_entity = ayon_api.get_folder_by_path(\n        project_name, folder_path, fields={\"id\"}\n    )\n    if not folder_entity:\n        return None\n    return ayon_api.get_task_by_name(\n        project_name, folder_entity[\"id\"], task_name, fields=fields\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/context_tools.html#client.ayon_core.pipeline.context_tools.get_global_context","title":"<code>get_global_context()</code>","text":"<p>Global context defined in environment variables.</p> <p>Values here may not reflect current context of host integration. The function can be used on startup before a host is registered.</p> <p>Use 'get_current_context' to make sure you'll get current host integration context info.</p> <p>Example::</p> <pre><code>{\n    \"project_name\": \"Commercial\",\n    \"folder_path\": \"Bunny\",\n    \"task_name\": \"Animation\",\n}\n</code></pre> <p>Returns:</p> Type Description <p>dict[str, Union[str, None]]: Context defined with environment variables.</p> Source code in <code>client/ayon_core/pipeline/context_tools.py</code> <pre><code>def get_global_context():\n    \"\"\"Global context defined in environment variables.\n\n    Values here may not reflect current context of host integration. The\n    function can be used on startup before a host is registered.\n\n    Use 'get_current_context' to make sure you'll get current host integration\n    context info.\n\n    Example::\n\n        {\n            \"project_name\": \"Commercial\",\n            \"folder_path\": \"Bunny\",\n            \"task_name\": \"Animation\",\n        }\n\n    Returns:\n        dict[str, Union[str, None]]: Context defined with environment\n            variables.\n    \"\"\"\n\n    return {\n        \"project_name\": os.environ.get(\"AYON_PROJECT_NAME\"),\n        \"folder_path\": os.environ.get(\"AYON_FOLDER_PATH\"),\n        \"task_name\": os.environ.get(\"AYON_TASK_NAME\"),\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/context_tools.html#client.ayon_core.pipeline.context_tools.get_process_id","title":"<code>get_process_id()</code>","text":"<p>Fake process id created on demand using uuid.</p> <p>Can be used to create process specific folders in temp directory.</p> <p>Returns:</p> Name Type Description <code>str</code> <p>Process id.</p> Source code in <code>client/ayon_core/pipeline/context_tools.py</code> <pre><code>def get_process_id():\n    \"\"\"Fake process id created on demand using uuid.\n\n    Can be used to create process specific folders in temp directory.\n\n    Returns:\n        str: Process id.\n    \"\"\"\n\n    global _process_id\n    if _process_id is None:\n        _process_id = str(uuid.uuid4())\n    return _process_id\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/context_tools.html#client.ayon_core.pipeline.context_tools.install_ayon_plugins","title":"<code>install_ayon_plugins(project_name=None, host_name=None)</code>","text":"<p>Install AYON core plugins and make sure the core is initialized.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>Optional[str]</code> <p>Name of project to install plugins for.</p> <code>None</code> <code>host_name</code> <code>Optional[str]</code> <p>Name of host to install plugins for.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/context_tools.py</code> <pre><code>def install_ayon_plugins(project_name=None, host_name=None):\n    \"\"\"Install AYON core plugins and make sure the core is initialized.\n\n    Args:\n        project_name (Optional[str]): Name of project to install plugins for.\n        host_name (Optional[str]): Name of host to install plugins for.\n\n    \"\"\"\n    # Make sure global AYON connection has set site id and version\n    # - this is necessary if 'install_host' is not called\n    initialize_ayon_connection()\n    # Make sure addons are loaded\n    load_addons()\n\n    log.info(\"Registering global plug-ins..\")\n    pyblish.api.register_plugin_path(PUBLISH_PATH)\n    pyblish.api.register_discovery_filter(filter_pyblish_plugins)\n    register_inventory_action_path(INVENTORY_PATH)\n\n    if host_name is None:\n        host_name = get_current_host_name()\n\n    addons_manager = _get_addons_manager()\n    publish_plugin_dirs = addons_manager.collect_publish_plugin_paths(\n        host_name)\n    for path in publish_plugin_dirs:\n        pyblish.api.register_plugin_path(path)\n\n    create_plugin_paths = addons_manager.collect_create_plugin_paths(\n        host_name)\n    for path in create_plugin_paths:\n        register_creator_plugin_path(path)\n\n    load_plugin_paths = addons_manager.collect_load_plugin_paths(\n        host_name)\n    for path in load_plugin_paths:\n        register_loader_plugin_path(path)\n\n    inventory_action_paths = addons_manager.collect_inventory_action_paths(\n        host_name)\n    for path in inventory_action_paths:\n        register_inventory_action_path(path)\n\n    if project_name is None:\n        project_name = os.environ.get(\"AYON_PROJECT_NAME\")\n\n    # Register studio specific plugins\n    if project_name:\n        anatomy = Anatomy(project_name)\n        anatomy.set_root_environments()\n        register_root(anatomy.roots)\n\n        project_settings = get_project_settings(project_name)\n        platform_name = platform.system().lower()\n        project_plugins = (\n            project_settings\n            [\"core\"]\n            [\"project_plugins\"]\n            .get(platform_name)\n        ) or []\n        for path in project_plugins:\n            try:\n                path = str(path.format(**os.environ))\n            except KeyError:\n                pass\n\n            if not path or not os.path.exists(path):\n                continue\n\n            pyblish.api.register_plugin_path(path)\n            register_loader_plugin_path(path)\n            register_creator_plugin_path(path)\n            register_inventory_action_path(path)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/context_tools.html#client.ayon_core.pipeline.context_tools.install_host","title":"<code>install_host(host)</code>","text":"<p>Install <code>host</code> into the running Python session.</p> <p>Parameters:</p> Name Type Description Default <code>host</code> <code>AbstractHost</code> <p>A host interface object.</p> required Source code in <code>client/ayon_core/pipeline/context_tools.py</code> <pre><code>def install_host(host: AbstractHost) -&gt; None:\n    \"\"\"Install `host` into the running Python session.\n\n    Args:\n        host (AbstractHost): A host interface object.\n\n    \"\"\"\n    if not isinstance(host, AbstractHost):\n        log.error(\n            f\"Host must be a subclass of 'AbstractHost', got '{type(host)}'.\"\n        )\n\n    global _is_installed\n\n    _is_installed = True\n\n    # Make sure global AYON connection has set site id and version\n    initialize_ayon_connection()\n\n    addons_manager = _get_addons_manager()\n\n    project_name = os.getenv(\"AYON_PROJECT_NAME\")\n    # WARNING: This might be an issue\n    #   - commented out because 'traypublisher' does not have set project\n    # if not project_name:\n    #     raise ValueError(\n    #         \"AYON_PROJECT_NAME is missing in environment variables.\"\n    #     )\n\n    log.info(\"Activating {}..\".format(project_name))\n\n    # Optional host install function\n    if hasattr(host, \"install\"):\n        host.install()\n\n    register_host(host)\n\n    def modified_emit(obj, record):\n        \"\"\"Method replacing `emit` in Pyblish's MessageHandler.\"\"\"\n        try:\n            record.msg = record.getMessage()\n        except Exception:\n            record.msg = str(record.msg)\n        obj.records.append(record)\n\n    MessageHandler.emit = modified_emit\n\n    if os.environ.get(\"AYON_REMOTE_PUBLISH\"):\n        # target \"farm\" == rendering on farm, expects AYON_PUBLISH_DATA\n        # target \"remote\" == remote execution, installs host\n        print(\"Registering pyblish target: remote\")\n        pyblish.api.register_target(\"remote\")\n    else:\n        pyblish.api.register_target(\"local\")\n\n    if is_in_tests():\n        print(\"Registering pyblish target: automated\")\n        pyblish.api.register_target(\"automated\")\n\n    host_name = os.environ.get(\"AYON_HOST_NAME\")\n\n    # Give option to handle host installation\n    for addon in addons_manager.get_enabled_addons():\n        addon.on_host_install(host, host_name, project_name)\n\n    install_ayon_plugins(project_name, host_name)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/context_tools.html#client.ayon_core.pipeline.context_tools.is_installed","title":"<code>is_installed()</code>","text":"<p>Return state of installation</p> <p>Returns:</p> Type Description <p>True if installed, False otherwise</p> Source code in <code>client/ayon_core/pipeline/context_tools.py</code> <pre><code>def is_installed():\n    \"\"\"Return state of installation\n\n    Returns:\n        True if installed, False otherwise\n\n    \"\"\"\n\n    return _is_installed\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/context_tools.html#client.ayon_core.pipeline.context_tools.is_representation_from_latest","title":"<code>is_representation_from_latest(representation)</code>","text":"<p>Return whether the representation is from latest version</p> <p>Parameters:</p> Name Type Description Default <code>representation</code> <code>dict</code> <p>The representation document from the database.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <p>Whether the representation is of latest version.</p> Source code in <code>client/ayon_core/pipeline/context_tools.py</code> <pre><code>def is_representation_from_latest(representation):\n    \"\"\"Return whether the representation is from latest version\n\n    Args:\n        representation (dict): The representation document from the database.\n\n    Returns:\n        bool: Whether the representation is of latest version.\n    \"\"\"\n\n    project_name = get_current_project_name()\n    return ayon_api.version_is_latest(\n        project_name, representation[\"versionId\"]\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/context_tools.html#client.ayon_core.pipeline.context_tools.register_host","title":"<code>register_host(host)</code>","text":"<p>Register a new host for the current process</p> <p>Parameters:</p> Name Type Description Default <code>host</code> <code>ModuleType</code> <p>A module implementing the Host API interface. See the Host API documentation for details on what is required, or browse the source code.</p> required Source code in <code>client/ayon_core/pipeline/context_tools.py</code> <pre><code>def register_host(host):\n    \"\"\"Register a new host for the current process\n\n    Arguments:\n        host (ModuleType): A module implementing the\n            Host API interface. See the Host API\n            documentation for details on what is\n            required, or browse the source code.\n\n    \"\"\"\n\n    _registered_host[\"_\"] = host\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/context_tools.html#client.ayon_core.pipeline.context_tools.register_root","title":"<code>register_root(path)</code>","text":"<p>DEPRECATED Register currently active root.</p> Source code in <code>client/ayon_core/pipeline/context_tools.py</code> <pre><code>def register_root(path):\n    \"\"\"DEPRECATED Register currently active root.\"\"\"\n    log.info(\"Registering root: %s\" % path)\n    _registered_root[\"_\"] = path\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/context_tools.html#client.ayon_core.pipeline.context_tools.registered_host","title":"<code>registered_host()</code>","text":"<p>Return currently registered host</p> Source code in <code>client/ayon_core/pipeline/context_tools.py</code> <pre><code>def registered_host():\n    \"\"\"Return currently registered host\"\"\"\n    return _registered_host[\"_\"]\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/context_tools.html#client.ayon_core.pipeline.context_tools.registered_root","title":"<code>registered_root()</code>","text":"<p>Return registered roots from current project anatomy.</p> <p>Consider this does return roots only for current project and current     platforms, only if host was installer using 'install_host'.</p> Deprecated <p>Please use project 'Anatomy' to get roots. This function is still used     at current core functions of load logic, but that will change     in future and this function will be removed eventually. Using this     function at new places can cause problems in the future.</p> <p>Returns:</p> Type Description <p>dict[str, str]: Root paths.</p> Source code in <code>client/ayon_core/pipeline/context_tools.py</code> <pre><code>def registered_root():\n    \"\"\"Return registered roots from current project anatomy.\n\n    Consider this does return roots only for current project and current\n        platforms, only if host was installer using 'install_host'.\n\n    Deprecated:\n        Please use project 'Anatomy' to get roots. This function is still used\n            at current core functions of load logic, but that will change\n            in future and this function will be removed eventually. Using this\n            function at new places can cause problems in the future.\n\n    Returns:\n        dict[str, str]: Root paths.\n\n    \"\"\"\n    warnings.warn(\n        \"Used deprecated function 'registered_root'. Please use 'Anatomy'\"\n        \" to get roots.\",\n        DeprecationWarning,\n        stacklevel=2,\n    )\n    return _registered_root[\"_\"]\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/context_tools.html#client.ayon_core.pipeline.context_tools.uninstall_host","title":"<code>uninstall_host()</code>","text":"<p>Undo all of what <code>install()</code> did</p> Source code in <code>client/ayon_core/pipeline/context_tools.py</code> <pre><code>def uninstall_host():\n    \"\"\"Undo all of what `install()` did\"\"\"\n    host = registered_host()\n\n    try:\n        host.uninstall()\n    except AttributeError:\n        pass\n\n    log.info(\"Deregistering global plug-ins..\")\n    pyblish.api.deregister_plugin_path(PUBLISH_PATH)\n    pyblish.api.deregister_discovery_filter(filter_pyblish_plugins)\n    deregister_inventory_action_path(INVENTORY_PATH)\n    log.info(\"Global plug-ins unregistered\")\n\n    deregister_host()\n\n    log.info(\"Successfully uninstalled AYON!\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/context_tools.html#client.ayon_core.pipeline.context_tools.version_up_current_workfile","title":"<code>version_up_current_workfile()</code>","text":"<p>DEPRECATED Function to increment and save workfile.</p> <p>Please use 'save_next_version' from 'ayon_core.pipeline.workfile' instead.</p> Source code in <code>client/ayon_core/pipeline/context_tools.py</code> <pre><code>def version_up_current_workfile():\n    \"\"\"DEPRECATED Function to increment and save workfile.\n\n    Please use 'save_next_version' from 'ayon_core.pipeline.workfile' instead.\n\n    \"\"\"\n    warnings.warn(\n        \"Used deprecated 'version_up_current_workfile' please use\"\n        \" 'save_next_version' from 'ayon_core.pipeline.workfile' instead.\",\n        DeprecationWarning,\n        stacklevel=2,\n    )\n    from ayon_core.pipeline.workfile import save_next_version\n    save_next_version()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/delivery.html","title":"delivery","text":"<p>Functions useful for delivery of published representations.</p>"},{"location":"autoapi/client/ayon_core/pipeline/delivery.html#client.ayon_core.pipeline.delivery.check_destination_path","title":"<code>check_destination_path(repre_id, anatomy, anatomy_data, datetime_data, template_name)</code>","text":"<p>Try to create destination path based on 'template_name'.</p> <p>In the case that path cannot be filled, template contains unmatched keys, provide error message to filter out repre later.</p> <p>Parameters:</p> Name Type Description Default <code>repre_id</code> <code>str</code> <p>Representation id.</p> required <code>anatomy</code> <code>Anatomy</code> <p>Project anatomy.</p> required <code>anatomy_data</code> <code>dict</code> <p>Template data to fill anatomy templates.</p> required <code>datetime_data</code> <code>dict</code> <p>Values with actual date.</p> required <code>template_name</code> <code>str</code> <p>Name of template which should be used from anatomy templates.</p> required <p>Returns:     Dict[str, List[str]]: Report of happened errors. Key is message title         value is detailed information.</p> Source code in <code>client/ayon_core/pipeline/delivery.py</code> <pre><code>def check_destination_path(\n    repre_id,\n    anatomy,\n    anatomy_data,\n    datetime_data,\n    template_name\n):\n    \"\"\" Try to create destination path based on 'template_name'.\n\n    In the case that path cannot be filled, template contains unmatched\n    keys, provide error message to filter out repre later.\n\n    Args:\n        repre_id (str): Representation id.\n        anatomy (Anatomy): Project anatomy.\n        anatomy_data (dict): Template data to fill anatomy templates.\n        datetime_data (dict): Values with actual date.\n        template_name (str): Name of template which should be used from anatomy\n            templates.\n    Returns:\n        Dict[str, List[str]]: Report of happened errors. Key is message title\n            value is detailed information.\n    \"\"\"\n\n    anatomy_data.update(datetime_data)\n    path_template = anatomy.get_template_item(\n        \"delivery\", template_name, \"path\"\n    )\n    dest_path = path_template.format(anatomy_data)\n    report_items = collections.defaultdict(list)\n\n    if not dest_path.solved:\n        msg = (\n            \"Missing keys in Representation's context\"\n            \" for anatomy template \\\"{}\\\".\"\n        ).format(template_name)\n\n        sub_msg = (\n            \"Representation: {}&lt;br&gt;\"\n        ).format(repre_id)\n\n        if dest_path.missing_keys:\n            keys = \", \".join(dest_path.missing_keys)\n            sub_msg += (\n                \"- Missing keys: \\\"{}\\\"&lt;br&gt;\"\n            ).format(keys)\n\n        if dest_path.invalid_types:\n            items = []\n            for key, value in dest_path.invalid_types.items():\n                items.append(\"\\\"{}\\\" {}\".format(key, str(value)))\n\n            keys = \", \".join(items)\n            sub_msg += (\n                \"- Invalid value DataType: \\\"{}\\\"&lt;br&gt;\"\n            ).format(keys)\n\n        report_items[msg].append(sub_msg)\n\n    return report_items\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/delivery.html#client.ayon_core.pipeline.delivery.deliver_sequence","title":"<code>deliver_sequence(src_path, repre, anatomy, template_name, anatomy_data, format_dict, report_items, log, has_renumbered_frame=False, new_frame_start=0)</code>","text":"<p>For Pype2(mainly - works in 3 too) where representation might not     contain files.</p> <pre><code>Uses listing physical files (not 'files' on repre as a)might not be\n present, b)might not be reliable for representation and copying them.\n\n TODO Should be refactored when files are sufficient to drive all\n representations.\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>src_path</code> <code>str</code> <p>path of source representation file</p> required <code>repre</code> <code>dict</code> <p>full representation</p> required <code>template_name</code> <code>string</code> <p>user selected delivery template name</p> required <code>anatomy_data</code> <code>dict</code> <p>data from repre to fill anatomy with</p> required <code>format_dict</code> <code>dict</code> <p>root dictionary with names and values</p> required <code>report_items</code> <code>defaultdict</code> <p>to return error messages</p> required <code>log</code> <code>Logger</code> <p>for log printing</p> required <p>Returns:</p> Type Description <p>(collections.defaultdict, int)</p> Source code in <code>client/ayon_core/pipeline/delivery.py</code> <pre><code>def deliver_sequence(\n    src_path,\n    repre,\n    anatomy,\n    template_name,\n    anatomy_data,\n    format_dict,\n    report_items,\n    log,\n    has_renumbered_frame=False,\n    new_frame_start=0\n):\n    \"\"\" For Pype2(mainly - works in 3 too) where representation might not\n        contain files.\n\n        Uses listing physical files (not 'files' on repre as a)might not be\n         present, b)might not be reliable for representation and copying them.\n\n         TODO Should be refactored when files are sufficient to drive all\n         representations.\n\n    Args:\n        src_path(str): path of source representation file\n        repre (dict): full representation\n        anatomy (Anatomy)\n        template_name (string): user selected delivery template name\n        anatomy_data (dict): data from repre to fill anatomy with\n        format_dict (dict): root dictionary with names and values\n        report_items (collections.defaultdict): to return error messages\n        log (logging.Logger): for log printing\n\n    Returns:\n        (collections.defaultdict, int)\n    \"\"\"\n\n    src_path = os.path.normpath(src_path.replace(\"\\\\\", \"/\"))\n\n    def hash_path_exist(myPath):\n        res = myPath.replace('#', '*')\n        glob_search_results = glob.glob(res)\n        if len(glob_search_results) &gt; 0:\n            return True\n        return False\n\n    if not hash_path_exist(src_path):\n        msg = \"{} doesn't exist for {}\".format(\n            src_path, repre[\"id\"])\n        report_items[\"Source file was not found\"].append(msg)\n        return report_items, 0\n\n    delivery_template = anatomy.get_template_item(\n        \"delivery\", template_name, \"path\", default=None\n    )\n    if delivery_template is None:\n        msg = (\n            \"Delivery template \\\"{}\\\" in anatomy of project \\\"{}\\\"\"\n            \" was not found\"\n        ).format(template_name, anatomy.project_name)\n        report_items[\"\"].append(msg)\n        return report_items, 0\n\n    # Check if 'frame' key is available in template which is required\n    #   for sequence delivery\n    if \"{frame\" not in delivery_template.template:\n        msg = (\n            \"Delivery template \\\"{}\\\" in anatomy of project \\\"{}\\\"\"\n            \"does not contain '{{frame}}' key to fill. Delivery of sequence\"\n            \" can't be processed.\"\n        ).format(template_name, anatomy.project_name)\n        report_items[\"\"].append(msg)\n        return report_items, 0\n\n    dir_path, _file_name = os.path.split(str(src_path))\n\n    context = repre[\"context\"]\n    ext = context.get(\"ext\", context.get(\"representation\"))\n\n    if not ext:\n        msg = \"Source extension not found, cannot find collection\"\n        report_items[msg].append(src_path)\n        log.warning(\"{} &lt;{}&gt;\".format(msg, context))\n        return report_items, 0\n\n    ext = \".\" + ext\n    # context.representation could be .psd\n    ext = ext.replace(\"..\", \".\")\n\n    src_collections, _remainder = clique.assemble(os.listdir(dir_path))\n    src_collection = None\n    for col in src_collections:\n        if col.tail != ext:\n            continue\n\n        src_collection = col\n        break\n\n    if src_collection is None:\n        msg = \"Source collection of files was not found\"\n        report_items[msg].append(src_path)\n        log.warning(\"{} &lt;{}&gt;\".format(msg, src_path))\n        return report_items, 0\n\n    frame_indicator = \"@####@\"\n\n    anatomy_data = copy.deepcopy(anatomy_data)\n    anatomy_data[\"frame\"] = frame_indicator\n    if format_dict:\n        anatomy_data[\"root\"] = format_dict[\"root\"]\n    delivery_path = delivery_template.format_strict(anatomy_data)\n\n    delivery_path = os.path.normpath(delivery_path.replace(\"\\\\\", \"/\"))\n    delivery_folder = os.path.dirname(delivery_path)\n    dst_head, dst_tail = delivery_path.split(frame_indicator)\n    dst_padding = src_collection.padding\n    dst_collection = clique.Collection(\n        head=dst_head,\n        tail=dst_tail,\n        padding=dst_padding\n    )\n\n    if not os.path.exists(delivery_folder):\n        os.makedirs(delivery_folder)\n\n    src_head = src_collection.head\n    src_tail = src_collection.tail\n    uploaded = 0\n    first_frame = min(src_collection.indexes)\n    for index in src_collection.indexes:\n        src_padding = src_collection.format(\"{padding}\") % index\n        src_file_name = \"{}{}{}\".format(src_head, src_padding, src_tail)\n        src = os.path.normpath(\n            os.path.join(dir_path, src_file_name)\n        )\n        dst_index = index\n        if has_renumbered_frame:\n            # Calculate offset between first frame and current frame\n            # - '0' for first frame\n            offset = new_frame_start - first_frame\n            # Add offset to new frame start\n            dst_index = index + offset\n            if dst_index &lt; 0:\n                msg = \"Renumber frame has a smaller number than original frame\"     # noqa\n                report_items[msg].append(src_file_name)\n                log.warning(\"{} &lt;{}&gt;\".format(msg, context))\n                return report_items, 0\n        dst_padding = dst_collection.format(\"{padding}\") % dst_index\n        dst = \"{}{}{}\".format(dst_head, dst_padding, dst_tail)\n        log.debug(\"Copying single: {} -&gt; {}\".format(src, dst))\n        _copy_file(src, dst)\n\n        uploaded += 1\n\n    return report_items, uploaded\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/delivery.html#client.ayon_core.pipeline.delivery.deliver_single_file","title":"<code>deliver_single_file(src_path, repre, anatomy, template_name, anatomy_data, format_dict, report_items, log)</code>","text":"<p>Copy single file to calculated path based on template</p> <p>Parameters:</p> Name Type Description Default <code>src_path</code> <code>str</code> <p>path of source representation file</p> required <code>repre</code> <code>dict</code> <p>full repre, used only in deliver_sequence, here only as to share same signature</p> required <code>template_name</code> <code>string</code> <p>user selected delivery template name</p> required <code>anatomy_data</code> <code>dict</code> <p>data from repre to fill anatomy with</p> required <code>format_dict</code> <code>dict</code> <p>root dictionary with names and values</p> required <code>report_items</code> <code>defaultdict</code> <p>to return error messages</p> required <code>log</code> <code>Logger</code> <p>for log printing</p> required <p>Returns:</p> Type Description <p>(collections.defaultdict, int)</p> Source code in <code>client/ayon_core/pipeline/delivery.py</code> <pre><code>def deliver_single_file(\n    src_path,\n    repre,\n    anatomy,\n    template_name,\n    anatomy_data,\n    format_dict,\n    report_items,\n    log\n):\n    \"\"\"Copy single file to calculated path based on template\n\n    Args:\n        src_path(str): path of source representation file\n        repre (dict): full repre, used only in deliver_sequence, here only\n            as to share same signature\n        anatomy (Anatomy)\n        template_name (string): user selected delivery template name\n        anatomy_data (dict): data from repre to fill anatomy with\n        format_dict (dict): root dictionary with names and values\n        report_items (collections.defaultdict): to return error messages\n        log (logging.Logger): for log printing\n\n    Returns:\n        (collections.defaultdict, int)\n    \"\"\"\n\n    # Make sure path is valid for all platforms\n    src_path = os.path.normpath(src_path.replace(\"\\\\\", \"/\"))\n\n    if not os.path.exists(src_path):\n        msg = \"{} doesn't exist for {}\".format(src_path, repre[\"id\"])\n        report_items[\"Source file was not found\"].append(msg)\n        return report_items, 0\n\n    if format_dict:\n        anatomy_data = copy.deepcopy(anatomy_data)\n        anatomy_data[\"root\"] = format_dict[\"root\"]\n    template_obj = anatomy.get_template_item(\n        \"delivery\", template_name, \"path\"\n    )\n    delivery_path = template_obj.format_strict(anatomy_data)\n\n    # Backwards compatibility when extension contained `.`\n    delivery_path = delivery_path.replace(\"..\", \".\")\n    # Make sure path is valid for all platforms\n    delivery_path = os.path.normpath(delivery_path.replace(\"\\\\\", \"/\"))\n    # Remove newlines from the end of the string to avoid OSError during copy\n    delivery_path = delivery_path.rstrip()\n\n    delivery_folder = os.path.dirname(delivery_path)\n    if not os.path.exists(delivery_folder):\n        os.makedirs(delivery_folder)\n\n    log.debug(\"Copying single: {} -&gt; {}\".format(src_path, delivery_path))\n    _copy_file(src_path, delivery_path)\n\n    return report_items, 1\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/delivery.html#client.ayon_core.pipeline.delivery.get_format_dict","title":"<code>get_format_dict(anatomy, location_path)</code>","text":"<p>Returns replaced root values from user provider value.</p> <p>Parameters:</p> Name Type Description Default <code>anatomy</code> <code>Anatomy</code> <p>Project anatomy.</p> required <code>location_path</code> <code>str</code> <p>User provided value.</p> required <p>Returns:</p> Type Description <code>dict</code> <p>Prepared data for formatting of a template.</p> Source code in <code>client/ayon_core/pipeline/delivery.py</code> <pre><code>def get_format_dict(anatomy, location_path):\n    \"\"\"Returns replaced root values from user provider value.\n\n    Args:\n        anatomy (Anatomy): Project anatomy.\n        location_path (str): User provided value.\n\n    Returns:\n        (dict): Prepared data for formatting of a template.\n    \"\"\"\n\n    format_dict = {}\n    if not location_path:\n        return format_dict\n\n    location_path = location_path.replace(\"\\\\\", \"/\")\n    root_names = anatomy.root_names_from_templates(\n        anatomy.templates[\"delivery\"]\n    )\n    format_dict[\"root\"] = {}\n    for name in root_names:\n        format_dict[\"root\"][name] = location_path\n    return format_dict\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/editorial.html","title":"editorial","text":""},{"location":"autoapi/client/ayon_core/pipeline/editorial.html#client.ayon_core.pipeline.editorial.convert_to_padded_path","title":"<code>convert_to_padded_path(path, padding)</code>","text":"<p>Return correct padding in sequence string</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>path url or simple file name</p> required <code>padding</code> <code>int</code> <p>number of padding</p> required <p>Returns:</p> Name Type Description <code>type</code> <p>string with reformatted path</p> Example <p>convert_to_padded_path(\"plate.%d.exr\") &gt; plate.%04d.exr</p> Source code in <code>client/ayon_core/pipeline/editorial.py</code> <pre><code>def convert_to_padded_path(path, padding):\n    \"\"\"\n    Return correct padding in sequence string\n\n    Args:\n        path (str): path url or simple file name\n        padding (int): number of padding\n\n    Returns:\n        type: string with reformatted path\n\n    Example:\n        convert_to_padded_path(\"plate.%d.exr\") &gt; plate.%04d.exr\n\n    \"\"\"\n    if \"%d\" in path:\n        path = re.sub(\"%d\", \"%0{padding}d\".format(padding=padding), path)\n    return path\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/editorial.html#client.ayon_core.pipeline.editorial.frames_to_seconds","title":"<code>frames_to_seconds(frames, framerate)</code>","text":"<p>Returning seconds.</p> <p>Parameters:</p> Name Type Description Default <code>frames</code> <code>int</code> <p>frame</p> required <code>framerate</code> <code>float</code> <p>frame rate</p> required <p>Returns:</p> Name Type Description <code>float</code> <p>second value</p> Source code in <code>client/ayon_core/pipeline/editorial.py</code> <pre><code>def frames_to_seconds(frames, framerate):\n    \"\"\"\n    Returning seconds.\n\n    Args:\n        frames (int): frame\n        framerate (float): frame rate\n\n    Returns:\n        float: second value\n    \"\"\"\n\n    rt = _ot.from_frames(frames, framerate)\n    return _ot.to_seconds(rt)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/editorial.html#client.ayon_core.pipeline.editorial.is_clip_from_media_sequence","title":"<code>is_clip_from_media_sequence(otio_clip)</code>","text":"<p>Parameters:</p> Name Type Description Default <code>otio_clip</code> <code>Clip</code> <p>The OTIO clip to check.</p> required <p>Returns:</p> Type Description <p>bool. Is the provided clip from an input media sequence ?</p> Source code in <code>client/ayon_core/pipeline/editorial.py</code> <pre><code>def is_clip_from_media_sequence(otio_clip):\n    \"\"\"\n    Args:\n        otio_clip (otio.schema.Clip): The OTIO clip to check.\n\n    Returns:\n        bool. Is the provided clip from an input media sequence ?\n    \"\"\"\n    media_ref = otio_clip.media_reference\n    metadata = media_ref.metadata\n\n    # OpenTimelineIO 0.13 and newer\n    is_input_sequence = (\n        hasattr(otio.schema, \"ImageSequenceReference\") and\n        isinstance(media_ref, otio.schema.ImageSequenceReference)\n    )\n\n    # OpenTimelineIO 0.12 and older\n    is_input_sequence_legacy = bool(metadata.get(\"padding\"))\n\n    return is_input_sequence or is_input_sequence_legacy\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/editorial.html#client.ayon_core.pipeline.editorial.make_sequence_collection","title":"<code>make_sequence_collection(path, otio_range, metadata)</code>","text":"<p>Make collection from path otio range and otio metadata.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>path to image sequence with <code>%d</code></p> required <code>otio_range</code> <code>TimeRange</code> <p>range to be used</p> required <code>metadata</code> <code>dict</code> <p>data where padding value can be found</p> required <p>Returns:</p> Name Type Description <code>list</code> <p>dir_path (str): path to sequence, collection object</p> Source code in <code>client/ayon_core/pipeline/editorial.py</code> <pre><code>def make_sequence_collection(path, otio_range, metadata):\n    \"\"\"\n    Make collection from path otio range and otio metadata.\n\n    Args:\n        path (str): path to image sequence with `%d`\n        otio_range (otio._ot._ot.TimeRange): range to be used\n        metadata (dict): data where padding value can be found\n\n    Returns:\n        list: dir_path (str): path to sequence, collection object\n\n    \"\"\"\n    if \"%\" not in path:\n        return None\n    file_name = os.path.basename(path)\n    dir_path = os.path.dirname(path)\n    head = file_name.split(\"%\")[0]\n    tail = os.path.splitext(file_name)[-1]\n    first, last = otio_range_to_frame_range(otio_range)\n    collection = clique.Collection(\n        head=head, tail=tail, padding=metadata[\"padding\"])\n    collection.indexes.update([i for i in range(first, last)])\n    return dir_path, collection\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/editorial.html#client.ayon_core.pipeline.editorial.range_from_frames","title":"<code>range_from_frames(start, duration, fps)</code>","text":"<p>Returns otio time range.</p> <p>Parameters:</p> Name Type Description Default <code>start</code> <code>int</code> <p>frame start</p> required <code>duration</code> <code>int</code> <p>frame duration</p> required <code>fps</code> <code>float</code> <p>frame range</p> required <p>Returns:</p> Type Description <p>otio._ot._ot.TimeRange: created range</p> Source code in <code>client/ayon_core/pipeline/editorial.py</code> <pre><code>def range_from_frames(start, duration, fps):\n    \"\"\"\n    Returns otio time range.\n\n    Args:\n        start (int): frame start\n        duration (int): frame duration\n        fps (float): frame range\n\n    Returns:\n        otio._ot._ot.TimeRange: created range\n\n    \"\"\"\n    return _ot.TimeRange(\n        _ot.RationalTime(start, fps),\n        _ot.RationalTime(duration, fps)\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/editorial.html#client.ayon_core.pipeline.editorial.remap_range_on_file_sequence","title":"<code>remap_range_on_file_sequence(otio_clip, otio_range)</code>","text":"<p>Remap the provided range on a file sequence clip.</p> <p>Parameters:</p> Name Type Description Default <code>otio_clip</code> <code>Clip</code> <p>The OTIO clip to check.</p> required <code>otio_range</code> <code>TimeRange</code> <p>The trim range to apply.</p> required <p>Returns:</p> Name Type Description <code>tuple</code> <code>(int, int)</code> <p>The remapped range as discrete frame number.</p> Source code in <code>client/ayon_core/pipeline/editorial.py</code> <pre><code>def remap_range_on_file_sequence(otio_clip, otio_range):\n    \"\"\" Remap the provided range on a file sequence clip.\n\n    Args:\n        otio_clip (otio.schema.Clip): The OTIO clip to check.\n        otio_range (otio.schema.TimeRange): The trim range to apply.\n\n    Returns:\n        tuple(int, int): The remapped range as discrete frame number.\n\n    Raises:\n        ValueError. When the otio_clip or provided range is invalid.\n    \"\"\"\n    if not is_clip_from_media_sequence(otio_clip):\n        raise ValueError(f\"Cannot map on non-file sequence clip {otio_clip}.\")\n\n    media_ref = otio_clip.media_reference\n    available_range = otio_clip.available_range()\n    available_range_rate = available_range.start_time.rate\n\n    # Backward-compatibility for Hiero OTIO exporter.\n    # NTSC compatibility might introduce floating rates, when these are\n    # not exactly the same (23.976 vs 23.976024627685547)\n    # this will cause precision issue in computation.\n    # Currently round to 2 decimals for comparison,\n    # but this should always rescale after that.\n    rounded_av_rate = round(available_range_rate, 2)\n    rounded_range_rate = round(otio_range.start_time.rate, 2)\n\n    if rounded_av_rate != rounded_range_rate:\n        raise ValueError(\"Inconsistent range between clip and provided clip\")\n\n    source_range = otio_clip.source_range\n    media_in = available_range.start_time\n    available_range_start_frame = (\n        available_range.start_time.to_frames()\n    )\n\n    # Temporary.\n    # Some AYON custom OTIO exporter were implemented with relative\n    # source range for image sequence. Following code maintain\n    # backward-compatibility by adjusting media_in\n    # while we are updating those.\n    conformed_src_in = source_range.start_time.rescaled_to(\n        available_range_rate\n    )\n    if (\n        is_clip_from_media_sequence(otio_clip)\n        and available_range_start_frame == media_ref.start_frame\n\n        # source range should be included in available range from media\n        # using round instead of conformed_src_in.to_frames() to avoid\n        # any precision issue with frame rate.\n        and round(conformed_src_in.value) &lt; media_ref.start_frame\n    ):\n        media_in = otio.opentime.RationalTime(\n            0, rate=available_range_rate\n        )\n\n    src_offset_in = otio_range.start_time - media_in\n    frame_in = otio.opentime.RationalTime.from_frames(\n        media_ref.start_frame + src_offset_in.to_frames(),\n        rate=available_range_rate,\n    ).to_frames()\n\n    # e.g.:\n    # duration = 10 frames at 24fps\n    # if frame_in = 1001 then\n    # frame_out = 1010\n    offset_duration = max(0, otio_range.duration.to_frames() - 1)\n\n    frame_out = otio.opentime.RationalTime.from_frames(\n        frame_in + offset_duration,\n        rate=available_range_rate,\n    ).to_frames()\n\n    return frame_in, frame_out\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/editorial.html#client.ayon_core.pipeline.editorial.trim_media_range","title":"<code>trim_media_range(media_range, source_range)</code>","text":"<p>Trim input media range with clip source range.</p> <p>Parameters:</p> Name Type Description Default <code>media_range</code> <code>TimeRange</code> <p>available range of media</p> required <code>source_range</code> <code>TimeRange</code> <p>clip required range</p> required <p>Returns:</p> Type Description <p>otio._ot._ot.TimeRange: trimmed media range</p> Source code in <code>client/ayon_core/pipeline/editorial.py</code> <pre><code>def trim_media_range(media_range, source_range):\n    \"\"\"\n    Trim input media range with clip source range.\n\n    Args:\n        media_range (otio._ot._ot.TimeRange): available range of media\n        source_range (otio._ot._ot.TimeRange): clip required range\n\n    Returns:\n        otio._ot._ot.TimeRange: trimmed media range\n\n    \"\"\"\n    rw_media_start = _ot.RationalTime(\n        source_range.start_time.value,\n        media_range.start_time.rate\n    )\n    rw_media_duration = _ot.RationalTime(\n        source_range.duration.value,\n        media_range.duration.rate\n    )\n    return _ot.TimeRange(\n        rw_media_start, rw_media_duration)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/entity_uri.html","title":"entity_uri","text":""},{"location":"autoapi/client/ayon_core/pipeline/entity_uri.html#client.ayon_core.pipeline.entity_uri.construct_ayon_entity_uri","title":"<code>construct_ayon_entity_uri(project_name, folder_path, product, version, representation_name)</code>","text":"<p>Construct AYON entity URI from its components</p> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>AYON Entity URI to query entity path.</p> Source code in <code>client/ayon_core/pipeline/entity_uri.py</code> <pre><code>def construct_ayon_entity_uri(\n        project_name: str,\n        folder_path: str,\n        product: str,\n        version: Union[int, str],\n        representation_name: str\n) -&gt; str:\n    \"\"\"Construct AYON entity URI from its components\n\n    Returns:\n        str: AYON Entity URI to query entity path.\n    \"\"\"\n    if isinstance(version, int) and version &lt; 0:\n        version = \"hero\"\n    if not (isinstance(version, int) or version in {\"latest\", \"hero\"}):\n        raise ValueError(\n            \"Version must either be integer, 'latest' or 'hero'. \"\n            \"Got: {}\".format(version)\n        )\n    return (\n        \"ayon://{project}/{folder_path}?product={product}&amp;version={version}\"\n        \"&amp;representation={representation}\".format(\n            project=project_name,\n            folder_path=folder_path,\n            product=product,\n            version=version,\n            representation=representation_name\n        )\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/entity_uri.html#client.ayon_core.pipeline.entity_uri.parse_ayon_entity_uri","title":"<code>parse_ayon_entity_uri(uri)</code>","text":"<p>Parse AYON entity URI into individual components.</p> URI specification <p>ayon+entity://{project}/{folder}?product={product}     &amp;version={version}     &amp;representation={representation}</p> <p>URI example:     ayon+entity://test/hero?product=modelMain&amp;version=2&amp;representation=usd</p> <p>However - if the netloc is <code>ayon://</code> it will by default also resolve as <code>ayon+entity://</code> on AYON server, thus we need to support both. The shorter <code>ayon://</code> is preferred for user readability.</p> <p>Example:</p> <p>parse_ayon_entity_uri(     \"ayon://test/char/villain?product=modelMain&amp;version=2&amp;representation=usd\" ) {'project': 'test', 'folderPath': '/char/villain',  'product': 'modelMain', 'version': 1,  'representation': 'usd'} parse_ayon_entity_uri(     \"ayon+entity://project/folder?product=renderMain&amp;version=3&amp;representation=exr\" ) {'project': 'project', 'folderPath': '/folder',  'product': 'renderMain', 'version': 3,  'representation': 'exr'}</p> <p>Returns:</p> Type Description <code>Optional[dict]</code> <p>dict[str, Union[str, int]]: The individual key with their values as found in the ayon entity URI.</p> Source code in <code>client/ayon_core/pipeline/entity_uri.py</code> <pre><code>def parse_ayon_entity_uri(uri: str) -&gt; Optional[dict]:\n    \"\"\"Parse AYON entity URI into individual components.\n\n    URI specification:\n        ayon+entity://{project}/{folder}?product={product}\n            &amp;version={version}\n            &amp;representation={representation}\n    URI example:\n        ayon+entity://test/hero?product=modelMain&amp;version=2&amp;representation=usd\n\n    However - if the netloc is `ayon://` it will by default also resolve as\n    `ayon+entity://` on AYON server, thus we need to support both. The shorter\n    `ayon://` is preferred for user readability.\n\n    Example:\n    &gt;&gt;&gt; parse_ayon_entity_uri(\n    &gt;&gt;&gt;     \"ayon://test/char/villain?product=modelMain&amp;version=2&amp;representation=usd\"\n    &gt;&gt;&gt; )\n    {'project': 'test', 'folderPath': '/char/villain',\n     'product': 'modelMain', 'version': 1,\n     'representation': 'usd'}\n    &gt;&gt;&gt; parse_ayon_entity_uri(\n    &gt;&gt;&gt;     \"ayon+entity://project/folder?product=renderMain&amp;version=3&amp;representation=exr\"\n    &gt;&gt;&gt; )\n    {'project': 'project', 'folderPath': '/folder',\n     'product': 'renderMain', 'version': 3,\n     'representation': 'exr'}\n\n    Returns:\n        dict[str, Union[str, int]]: The individual key with their values as\n            found in the ayon entity URI.\n\n    \"\"\"  # noqa: E501\n\n    if not (uri.startswith(\"ayon+entity://\") or uri.startswith(\"ayon://\")):\n        return {}\n\n    parsed = urlparse(uri)\n    if parsed.scheme not in {\"ayon+entity\", \"ayon\"}:\n        return {}\n\n    result = {\n        \"project\": parsed.netloc,\n        \"folderPath\": \"/\" + parsed.path.strip(\"/\")\n    }\n    query = parse_qs(parsed.query)\n    for key in [\"product\", \"version\", \"representation\"]:\n        if key in query:\n            result[key] = query[key][0]\n\n    # Convert version to integer if it is a digit\n    version = result.get(\"version\")\n    if version is not None and version.isdigit():\n        result[\"version\"] = int(version)\n\n    return result\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/plugin_discover.html","title":"plugin_discover","text":""},{"location":"autoapi/client/ayon_core/pipeline/plugin_discover.html#client.ayon_core.pipeline.plugin_discover.DiscoverResult","title":"<code>DiscoverResult</code>","text":"<p>Result of Plug-ins discovery of a single superclass type.</p> <p>Stores discovered, duplicated, ignored and abstract plugins and file paths which crashed on execution of file.</p> Source code in <code>client/ayon_core/pipeline/plugin_discover.py</code> <pre><code>class DiscoverResult:\n    \"\"\"Result of Plug-ins discovery of a single superclass type.\n\n    Stores discovered, duplicated, ignored and abstract plugins and file paths\n    which crashed on execution of file.\n    \"\"\"\n\n    def __init__(self, superclass):\n        self.superclass = superclass\n        self.plugins = []\n        self.crashed_file_paths = {}\n        self.duplicated_plugins = []\n        self.abstract_plugins = []\n        self.ignored_plugins = set()\n        # Store loaded modules to keep them in memory\n        self._modules = set()\n\n    def __iter__(self):\n        for plugin in self.plugins:\n            yield plugin\n\n    def __getitem__(self, item):\n        return self.plugins[item]\n\n    def __setitem__(self, item, value):\n        self.plugins[item] = value\n\n    def add_module(self, module):\n        \"\"\"Add dynamically loaded python module to keep it in memory.\"\"\"\n        self._modules.add(module)\n\n    def get_report(self, only_errors=True, exc_info=True, full_report=False):\n        lines = []\n        if not only_errors:\n            # Successfully discovered plugins\n            if self.plugins or full_report:\n                lines.append(\n                    \"*** Discovered {} plugins\".format(len(self.plugins))\n                )\n                for cls in self.plugins:\n                    lines.append(\"- {}\".format(cls.__name__))\n\n            # Plugin that were defined to be ignored\n            if self.ignored_plugins or full_report:\n                lines.append(\"*** Ignored plugins {}\".format(len(\n                    self.ignored_plugins\n                )))\n                for cls in self.ignored_plugins:\n                    lines.append(\"- {}\".format(cls.__name__))\n\n        # Abstract classes\n        if self.abstract_plugins or full_report:\n            lines.append(\"*** Discovered {} abstract plugins\".format(len(\n                self.abstract_plugins\n            )))\n            for cls in self.abstract_plugins:\n                lines.append(\"- {}\".format(cls.__name__))\n\n        # Abstract classes\n        if self.duplicated_plugins or full_report:\n            lines.append(\"*** There were {} duplicated plugins\".format(len(\n                self.duplicated_plugins\n            )))\n            for cls in self.duplicated_plugins:\n                lines.append(\"- {}\".format(cls.__name__))\n\n        if self.crashed_file_paths or full_report:\n            lines.append(\"*** Failed to load {} files\".format(len(\n                self.crashed_file_paths\n            )))\n            for path, exc_info_args in self.crashed_file_paths.items():\n                lines.append(\"- {}\".format(path))\n                if exc_info:\n                    lines.append(10 * \"*\")\n                    lines.extend(traceback.format_exception(*exc_info_args))\n                    lines.append(10 * \"*\")\n\n        return \"\\n\".join(lines)\n\n    def log_report(self, only_errors=True, exc_info=True):\n        report = self.get_report(only_errors, exc_info)\n        if report:\n            log.info(report)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/plugin_discover.html#client.ayon_core.pipeline.plugin_discover.DiscoverResult.add_module","title":"<code>add_module(module)</code>","text":"<p>Add dynamically loaded python module to keep it in memory.</p> Source code in <code>client/ayon_core/pipeline/plugin_discover.py</code> <pre><code>def add_module(self, module):\n    \"\"\"Add dynamically loaded python module to keep it in memory.\"\"\"\n    self._modules.add(module)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/plugin_discover.html#client.ayon_core.pipeline.plugin_discover.PluginDiscoverContext","title":"<code>PluginDiscoverContext</code>","text":"<p>               Bases: <code>object</code></p> <p>Store and discover registered types nad registered paths to types.</p> <p>Keeps in memory all registered types and their paths. Paths are dynamically loaded on discover so different discover calls won't return the same class objects even if were loaded from same file.</p> Source code in <code>client/ayon_core/pipeline/plugin_discover.py</code> <pre><code>class PluginDiscoverContext(object):\n    \"\"\"Store and discover registered types nad registered paths to types.\n\n    Keeps in memory all registered types and their paths. Paths are dynamically\n    loaded on discover so different discover calls won't return the same\n    class objects even if were loaded from same file.\n    \"\"\"\n\n    def __init__(self):\n        self._registered_plugins = {}\n        self._registered_plugin_paths = {}\n        self._last_discovered_plugins = {}\n        # Store the last result to memory\n        self._last_discovered_results = {}\n\n    def get_last_discovered_plugins(self, superclass):\n        \"\"\"Access last discovered plugin by a subperclass.\n\n        Returns:\n            None: When superclass was not discovered yet.\n            list: Lastly discovered plugins of the superclass.\n        \"\"\"\n\n        return self._last_discovered_plugins.get(superclass)\n\n    def discover(\n        self,\n        superclass,\n        allow_duplicates=True,\n        ignore_classes=None,\n        return_report=False\n    ):\n        \"\"\"Find and return subclasses of `superclass`\n\n        Args:\n            superclass (type): Class which determines discovered subclasses.\n            allow_duplicates (bool): Validate class name duplications.\n            ignore_classes (list): List of classes that will be ignored\n                and not added to result.\n            return_report (bool): Output will be full report if set to 'True'.\n\n        Returns:\n            Union[DiscoverResult, list[Any]]: Object holding successfully\n                discovered plugins, ignored plugins, plugins with missing\n                abstract implementation and duplicated plugin.\n\n        \"\"\"\n        registered_classes = self._registered_plugins.get(superclass) or []\n        registered_paths = self._registered_plugin_paths.get(superclass) or []\n        result = discover_plugins(\n            superclass,\n            paths=registered_paths,\n            classes=registered_classes,\n            ignored_classes=ignore_classes,\n            allow_duplicates=allow_duplicates,\n        )\n\n        # Store in memory last result to keep in memory loaded modules\n        self._last_discovered_results[superclass] = result\n        self._last_discovered_plugins[superclass] = list(\n            result.plugins\n        )\n        result.log_report()\n        if return_report:\n            return result\n        return result.plugins\n\n    def register_plugin(self, superclass, cls):\n        \"\"\"Register a directory containing plug-ins of type `superclass`\n\n        Arguments:\n            superclass (type): Superclass of plug-in\n            cls (object): Subclass of `superclass`\n        \"\"\"\n\n        if superclass not in self._registered_plugins:\n            self._registered_plugins[superclass] = list()\n\n        if cls not in self._registered_plugins[superclass]:\n            self._registered_plugins[superclass].append(cls)\n\n    def register_plugin_path(self, superclass, path):\n        \"\"\"Register a directory of one or more plug-ins\n\n        Arguments:\n            superclass (type): Superclass of plug-ins to look for during\n                discovery\n            path (str): Absolute path to directory in which to discover\n                plug-ins\n        \"\"\"\n\n        if superclass not in self._registered_plugin_paths:\n            self._registered_plugin_paths[superclass] = list()\n\n        path = os.path.normpath(path)\n        if path not in self._registered_plugin_paths[superclass]:\n            self._registered_plugin_paths[superclass].append(path)\n\n    def registered_plugin_paths(self):\n        \"\"\"Return all currently registered plug-in paths\"\"\"\n        # Return shallow copy so we the original data can't be changed\n        return {\n            superclass: paths[:]\n            for superclass, paths in self._registered_plugin_paths.items()\n        }\n\n    def deregister_plugin(self, superclass, plugin):\n        \"\"\"Opposite of `register_plugin()`\"\"\"\n        if superclass in self._registered_plugins:\n            self._registered_plugins[superclass].remove(plugin)\n\n    def deregister_plugin_path(self, superclass, path):\n        \"\"\"Opposite of `register_plugin_path()`\"\"\"\n        self._registered_plugin_paths[superclass].remove(path)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/plugin_discover.html#client.ayon_core.pipeline.plugin_discover.PluginDiscoverContext.deregister_plugin","title":"<code>deregister_plugin(superclass, plugin)</code>","text":"<p>Opposite of <code>register_plugin()</code></p> Source code in <code>client/ayon_core/pipeline/plugin_discover.py</code> <pre><code>def deregister_plugin(self, superclass, plugin):\n    \"\"\"Opposite of `register_plugin()`\"\"\"\n    if superclass in self._registered_plugins:\n        self._registered_plugins[superclass].remove(plugin)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/plugin_discover.html#client.ayon_core.pipeline.plugin_discover.PluginDiscoverContext.deregister_plugin_path","title":"<code>deregister_plugin_path(superclass, path)</code>","text":"<p>Opposite of <code>register_plugin_path()</code></p> Source code in <code>client/ayon_core/pipeline/plugin_discover.py</code> <pre><code>def deregister_plugin_path(self, superclass, path):\n    \"\"\"Opposite of `register_plugin_path()`\"\"\"\n    self._registered_plugin_paths[superclass].remove(path)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/plugin_discover.html#client.ayon_core.pipeline.plugin_discover.PluginDiscoverContext.discover","title":"<code>discover(superclass, allow_duplicates=True, ignore_classes=None, return_report=False)</code>","text":"<p>Find and return subclasses of <code>superclass</code></p> <p>Parameters:</p> Name Type Description Default <code>superclass</code> <code>type</code> <p>Class which determines discovered subclasses.</p> required <code>allow_duplicates</code> <code>bool</code> <p>Validate class name duplications.</p> <code>True</code> <code>ignore_classes</code> <code>list</code> <p>List of classes that will be ignored and not added to result.</p> <code>None</code> <code>return_report</code> <code>bool</code> <p>Output will be full report if set to 'True'.</p> <code>False</code> <p>Returns:</p> Type Description <p>Union[DiscoverResult, list[Any]]: Object holding successfully discovered plugins, ignored plugins, plugins with missing abstract implementation and duplicated plugin.</p> Source code in <code>client/ayon_core/pipeline/plugin_discover.py</code> <pre><code>def discover(\n    self,\n    superclass,\n    allow_duplicates=True,\n    ignore_classes=None,\n    return_report=False\n):\n    \"\"\"Find and return subclasses of `superclass`\n\n    Args:\n        superclass (type): Class which determines discovered subclasses.\n        allow_duplicates (bool): Validate class name duplications.\n        ignore_classes (list): List of classes that will be ignored\n            and not added to result.\n        return_report (bool): Output will be full report if set to 'True'.\n\n    Returns:\n        Union[DiscoverResult, list[Any]]: Object holding successfully\n            discovered plugins, ignored plugins, plugins with missing\n            abstract implementation and duplicated plugin.\n\n    \"\"\"\n    registered_classes = self._registered_plugins.get(superclass) or []\n    registered_paths = self._registered_plugin_paths.get(superclass) or []\n    result = discover_plugins(\n        superclass,\n        paths=registered_paths,\n        classes=registered_classes,\n        ignored_classes=ignore_classes,\n        allow_duplicates=allow_duplicates,\n    )\n\n    # Store in memory last result to keep in memory loaded modules\n    self._last_discovered_results[superclass] = result\n    self._last_discovered_plugins[superclass] = list(\n        result.plugins\n    )\n    result.log_report()\n    if return_report:\n        return result\n    return result.plugins\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/plugin_discover.html#client.ayon_core.pipeline.plugin_discover.PluginDiscoverContext.get_last_discovered_plugins","title":"<code>get_last_discovered_plugins(superclass)</code>","text":"<p>Access last discovered plugin by a subperclass.</p> <p>Returns:</p> Name Type Description <code>None</code> <p>When superclass was not discovered yet.</p> <code>list</code> <p>Lastly discovered plugins of the superclass.</p> Source code in <code>client/ayon_core/pipeline/plugin_discover.py</code> <pre><code>def get_last_discovered_plugins(self, superclass):\n    \"\"\"Access last discovered plugin by a subperclass.\n\n    Returns:\n        None: When superclass was not discovered yet.\n        list: Lastly discovered plugins of the superclass.\n    \"\"\"\n\n    return self._last_discovered_plugins.get(superclass)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/plugin_discover.html#client.ayon_core.pipeline.plugin_discover.PluginDiscoverContext.register_plugin","title":"<code>register_plugin(superclass, cls)</code>","text":"<p>Register a directory containing plug-ins of type <code>superclass</code></p> <p>Parameters:</p> Name Type Description Default <code>superclass</code> <code>type</code> <p>Superclass of plug-in</p> required <code>cls</code> <code>object</code> <p>Subclass of <code>superclass</code></p> required Source code in <code>client/ayon_core/pipeline/plugin_discover.py</code> <pre><code>def register_plugin(self, superclass, cls):\n    \"\"\"Register a directory containing plug-ins of type `superclass`\n\n    Arguments:\n        superclass (type): Superclass of plug-in\n        cls (object): Subclass of `superclass`\n    \"\"\"\n\n    if superclass not in self._registered_plugins:\n        self._registered_plugins[superclass] = list()\n\n    if cls not in self._registered_plugins[superclass]:\n        self._registered_plugins[superclass].append(cls)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/plugin_discover.html#client.ayon_core.pipeline.plugin_discover.PluginDiscoverContext.register_plugin_path","title":"<code>register_plugin_path(superclass, path)</code>","text":"<p>Register a directory of one or more plug-ins</p> <p>Parameters:</p> Name Type Description Default <code>superclass</code> <code>type</code> <p>Superclass of plug-ins to look for during discovery</p> required <code>path</code> <code>str</code> <p>Absolute path to directory in which to discover plug-ins</p> required Source code in <code>client/ayon_core/pipeline/plugin_discover.py</code> <pre><code>def register_plugin_path(self, superclass, path):\n    \"\"\"Register a directory of one or more plug-ins\n\n    Arguments:\n        superclass (type): Superclass of plug-ins to look for during\n            discovery\n        path (str): Absolute path to directory in which to discover\n            plug-ins\n    \"\"\"\n\n    if superclass not in self._registered_plugin_paths:\n        self._registered_plugin_paths[superclass] = list()\n\n    path = os.path.normpath(path)\n    if path not in self._registered_plugin_paths[superclass]:\n        self._registered_plugin_paths[superclass].append(path)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/plugin_discover.html#client.ayon_core.pipeline.plugin_discover.PluginDiscoverContext.registered_plugin_paths","title":"<code>registered_plugin_paths()</code>","text":"<p>Return all currently registered plug-in paths</p> Source code in <code>client/ayon_core/pipeline/plugin_discover.py</code> <pre><code>def registered_plugin_paths(self):\n    \"\"\"Return all currently registered plug-in paths\"\"\"\n    # Return shallow copy so we the original data can't be changed\n    return {\n        superclass: paths[:]\n        for superclass, paths in self._registered_plugin_paths.items()\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/plugin_discover.html#client.ayon_core.pipeline.plugin_discover.discover","title":"<code>discover(superclass, allow_duplicates=True, ignore_classes=None, return_report=False)</code>","text":"<p>Find and return subclasses of <code>superclass</code></p> <p>Parameters:</p> Name Type Description Default <code>superclass</code> <code>type</code> <p>Class which determines discovered subclasses.</p> required <code>allow_duplicates</code> <code>bool</code> <p>Validate class name duplications.</p> <code>True</code> <code>ignore_classes</code> <code>list</code> <p>List of classes that will be ignored and not added to result.</p> <code>None</code> <code>return_report</code> <code>bool</code> <p>Output will be full report if set to 'True'.</p> <code>False</code> <p>Returns:</p> Type Description <p>Union[DiscoverResult, list[Any]]: Object holding successfully discovered plugins, ignored plugins, plugins with missing abstract implementation and duplicated plugin.</p> Source code in <code>client/ayon_core/pipeline/plugin_discover.py</code> <pre><code>def discover(\n    superclass,\n    allow_duplicates=True,\n    ignore_classes=None,\n    return_report=False\n):\n    \"\"\"Find and return subclasses of `superclass`\n\n    Args:\n        superclass (type): Class which determines discovered subclasses.\n        allow_duplicates (bool): Validate class name duplications.\n        ignore_classes (list): List of classes that will be ignored\n            and not added to result.\n        return_report (bool): Output will be full report if set to 'True'.\n\n    Returns:\n        Union[DiscoverResult, list[Any]]: Object holding successfully\n            discovered plugins, ignored plugins, plugins with missing\n            abstract implementation and duplicated plugin.\n    \"\"\"\n\n    context = _GlobalDiscover.get_context()\n    return context.discover(\n        superclass,\n        allow_duplicates,\n        ignore_classes,\n        return_report\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/plugin_discover.html#client.ayon_core.pipeline.plugin_discover.discover_plugins","title":"<code>discover_plugins(base_class, paths=None, classes=None, ignored_classes=None, allow_duplicates=True)</code>","text":"<p>Find and return subclasses of <code>superclass</code></p> <p>Parameters:</p> Name Type Description Default <code>base_class</code> <code>type</code> <p>Class which determines discovered subclasses.</p> required <code>paths</code> <code>Optional[list[str]]</code> <p>List of paths to look for plug-ins.</p> <code>None</code> <code>classes</code> <code>Optional[list[str]]</code> <p>List of classes to filter.</p> <code>None</code> <code>ignored_classes</code> <code>list[type]</code> <p>List of classes that won't be added to the output plugins.</p> <code>None</code> <code>allow_duplicates</code> <code>bool</code> <p>Validate class name duplications.</p> <code>True</code> <p>Returns:</p> Name Type Description <code>DiscoverResult</code> <p>Object holding successfully discovered plugins, ignored plugins, plugins with missing abstract implementation and duplicated plugin.</p> Source code in <code>client/ayon_core/pipeline/plugin_discover.py</code> <pre><code>def discover_plugins(\n    base_class: type,\n    paths: Optional[list[str]] = None,\n    classes: Optional[list[type]] = None,\n    ignored_classes: Optional[list[type]] = None,\n    allow_duplicates: bool = True,\n):\n    \"\"\"Find and return subclasses of `superclass`\n\n    Args:\n        base_class (type): Class which determines discovered subclasses.\n        paths (Optional[list[str]]): List of paths to look for plug-ins.\n        classes (Optional[list[str]]): List of classes to filter.\n        ignored_classes (list[type]): List of classes that won't be added to\n            the output plugins.\n        allow_duplicates (bool): Validate class name duplications.\n\n    Returns:\n        DiscoverResult: Object holding successfully\n            discovered plugins, ignored plugins, plugins with missing\n            abstract implementation and duplicated plugin.\n\n    \"\"\"\n    ignored_classes = ignored_classes or []\n    paths = paths or []\n    classes = classes or []\n\n    result = DiscoverResult(base_class)\n\n    all_plugins = list(classes)\n\n    for path in paths:\n        modules, crashed = modules_from_path(path)\n        for (filepath, exc_info) in crashed:\n            result.crashed_file_paths[filepath] = exc_info\n\n        for item in modules:\n            filepath, module = item\n            result.add_module(module)\n            for cls in classes_from_module(base_class, module):\n                if cls is base_class:\n                    continue\n                # Class has defined 'skip_discovery = True'\n                skip_discovery = cls.__dict__.get(\"skip_discovery\")\n                if skip_discovery is True:\n                    continue\n                all_plugins.append(cls)\n\n    if base_class not in ignored_classes:\n        ignored_classes.append(base_class)\n\n    plugin_names = set()\n    for cls in all_plugins:\n        if cls in ignored_classes:\n            result.ignored_plugins.add(cls)\n            continue\n\n        if inspect.isabstract(cls):\n            result.abstract_plugins.append(cls)\n            continue\n\n        if not allow_duplicates:\n            class_name = cls.__name__\n            if class_name in plugin_names:\n                result.duplicated_plugins.append(cls)\n                continue\n            plugin_names.add(class_name)\n        result.plugins.append(cls)\n    return result\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/project_folders.html","title":"project_folders","text":""},{"location":"autoapi/client/ayon_core/pipeline/staging_dir.html","title":"staging_dir","text":""},{"location":"autoapi/client/ayon_core/pipeline/staging_dir.html#client.ayon_core.pipeline.staging_dir.get_staging_dir_config","title":"<code>get_staging_dir_config(project_name, task_type, task_name, product_type, product_name, host_name, *, project_settings=None, anatomy=None, logger=None, product_base_type=None)</code>","text":"<p>Get matching staging dir profile.</p> <p>Parameters:</p> Name Type Description Default <code>host_name</code> <code>str</code> <p>Name of host.</p> required <code>project_name</code> <code>str</code> <p>Name of project.</p> required <code>task_type</code> <code>Optional[str]</code> <p>Type of task.</p> required <code>task_name</code> <code>Optional[str]</code> <p>Name of task.</p> required <code>product_type</code> <code>str</code> <p>Product type.</p> required <code>product_name</code> <code>str</code> <p>Name of product.</p> required <code>project_settings</code> <code>dict[str, Any]</code> <p>Prepared project settings.</p> <code>None</code> <code>product_base_type</code> <code>Optional[str]</code> <p>Product base type.</p> <code>None</code> <p>Returns:</p> Type Description <code>Optional[dict[str, Any]]</code> <p>dict or None: Data with directory template and is_persistent or None</p> Source code in <code>client/ayon_core/pipeline/staging_dir.py</code> <pre><code>@_get_staging_dir_config_wrap\ndef get_staging_dir_config(\n    project_name: str,\n    task_type: Optional[str],\n    task_name: Optional[str],\n    product_type: str,\n    product_name: str,\n    host_name: str,\n    *,\n    project_settings: Optional[dict[str, Any]] = None,\n    anatomy: Optional[Anatomy] = None,\n    logger: Optional[logging.Logger] = None,\n    product_base_type: Optional[str] = None,\n) -&gt; Optional[dict[str, Any]]:\n    \"\"\"Get matching staging dir profile.\n\n    Args:\n        host_name (str): Name of host.\n        project_name (str): Name of project.\n        task_type (Optional[str]): Type of task.\n        task_name (Optional[str]): Name of task.\n        product_type (str): Product type.\n        product_name (str): Name of product.\n        project_settings(dict[str, Any]): Prepared project settings.\n        anatomy (dict[str, Any])\n        logger (Optional[logging.Logger])\n        product_base_type (Optional[str]): Product base type.\n\n    Returns:\n        dict or None: Data with directory template and is_persistent or None\n\n    Raises:\n        KeyError - if misconfigured template should be used\n\n    \"\"\"\n    settings = project_settings or get_project_settings(project_name)\n\n    staging_dir_profiles = settings[\"core\"][\"tools\"][\"publish\"][\n        \"custom_staging_dir_profiles\"\n    ]\n\n    if not staging_dir_profiles:\n        return None\n\n    if not logger:\n        logger = Logger.get_logger(\"get_staging_dir_config\")\n\n    if not product_base_type:\n        msg = (\n            \"Missing 'product_base_type' in 'get_staging_dir_config'.\"\n            \" Will soon be required.\"\n        )\n        logger.warning(msg)\n        warnings.warn(msg, DeprecationWarning, stacklevel=2)\n        product_base_type = product_type\n\n    filtering_criteria = {\n        \"host_names\": host_name,\n        \"task_types\": task_type,\n        \"task_names\": task_name,\n        \"product_base_types\": product_base_type,\n        \"product_names\": product_name,\n    }\n    profile = filter_profiles(\n        staging_dir_profiles,\n        filtering_criteria,\n        logger=logger,\n    )\n\n    if not profile or not profile[\"active\"]:\n        return None\n\n    if not anatomy:\n        anatomy = Anatomy(project_name)\n\n    # get template from template name\n    template_name = profile[\"template_name\"]\n\n    template = anatomy.get_template_item(\"staging\", template_name)\n\n    if not template:\n        # template should always be found either from anatomy or from profile\n        raise KeyError(\n            f\"Staging template '{template_name}' was not found.\"\n            \"Check project anatomy or settings at: \"\n            \"'ayon+settings://core/tools/publish/custom_staging_dir_profiles'\"\n        )\n\n    data_persistence = profile[\"custom_staging_dir_persistent\"]\n\n    return {\"template\": template, \"persistence\": data_persistence}\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/staging_dir.html#client.ayon_core.pipeline.staging_dir.get_staging_dir_info","title":"<code>get_staging_dir_info(project_entity, folder_entity, task_entity, product_type, product_name, host_name, *, anatomy=None, project_settings=None, template_data=None, always_return_path=True, force_tmp_dir=False, logger=None, prefix=None, suffix=None, username=None, product_base_type=None)</code>","text":"<p>Get staging dir info data.</p> <p>If <code>force_temp</code> is set, staging dir will be created as tempdir. If <code>always_get_some_dir</code> is set, staging dir will be created as tempdir if no staging dir profile is found. If <code>prefix</code> or <code>suffix</code> is not set, default values will be used.</p> <p>Parameters:</p> Name Type Description Default <code>project_entity</code> <code>dict[str, Any]</code> <p>Project entity.</p> required <code>folder_entity</code> <code>Optional[dict[str, Any]]</code> <p>Folder entity.</p> required <code>task_entity</code> <code>Optional[dict[str, Any]]</code> <p>Task entity.</p> required <code>product_type</code> <code>str</code> <p>Product type.</p> required <code>product_name</code> <code>str</code> <p>Name of product.</p> required <code>host_name</code> <code>str</code> <p>Name of host.</p> required <code>anatomy</code> <code>Optional[Anatomy]</code> <p>Anatomy object.</p> <code>None</code> <code>project_settings</code> <code>Optional[dict[str, Any]]</code> <p>Prepared project settings.</p> <code>None</code> <code>template_data</code> <code>Optional[dict[str, Any]]</code> <p>Additional data for formatting staging dir template.</p> <code>None</code> <code>always_return_path</code> <code>Optional[bool]</code> <p>If True, staging dir will be created as tempdir if no staging dir profile is found. Input value False will return None if no staging dir profile is found.</p> <code>True</code> <code>force_tmp_dir</code> <code>Optional[bool]</code> <p>If True, staging dir will be created as tempdir.</p> <code>False</code> <code>logger</code> <code>Optional[Logger]</code> <p>Logger instance.</p> <code>None</code> <code>suffix</code> <code>Optional[str]</code> <p>Optional suffix for staging dir name.</p> <code>None</code> <code>username</code> <code>Optional[str]</code> <p>AYON Username.</p> <code>None</code> <code>product_base_type</code> <code>str</code> <p>Product base type.</p> <code>None</code> <p>Returns:</p> Type Description <code>Optional[StagingDir]</code> <p>Optional[StagingDir]: Staging dir info data</p> Source code in <code>client/ayon_core/pipeline/staging_dir.py</code> <pre><code>@_get_staging_dir_info_wrap\ndef get_staging_dir_info(\n    project_entity: dict[str, Any],\n    folder_entity: Optional[dict[str, Any]],\n    task_entity: Optional[dict[str, Any]],\n    product_type: str,\n    product_name: str,\n    host_name: str,\n    *,\n    anatomy: Optional[Anatomy] = None,\n    project_settings: Optional[dict[str, Any]] = None,\n    template_data: Optional[dict[str, Any]] = None,\n    always_return_path: bool = True,\n    force_tmp_dir: bool = False,\n    logger: Optional[logging.Logger] = None,\n    prefix: Optional[str] = None,\n    suffix: Optional[str] = None,\n    username: Optional[str] = None,\n    product_base_type: Optional[str] = None,\n) -&gt; Optional[StagingDir]:\n    \"\"\"Get staging dir info data.\n\n    If `force_temp` is set, staging dir will be created as tempdir.\n    If `always_get_some_dir` is set, staging dir will be created as tempdir if\n    no staging dir profile is found.\n    If `prefix` or `suffix` is not set, default values will be used.\n\n    Arguments:\n        project_entity (dict[str, Any]): Project entity.\n        folder_entity (Optional[dict[str, Any]]): Folder entity.\n        task_entity (Optional[dict[str, Any]]): Task entity.\n        product_type (str): Product type.\n        product_name (str): Name of product.\n        host_name (str): Name of host.\n        anatomy (Optional[Anatomy]): Anatomy object.\n        project_settings (Optional[dict[str, Any]]): Prepared project settings.\n        template_data (Optional[dict[str, Any]]): Additional data for\n            formatting staging dir template.\n        always_return_path (Optional[bool]): If True, staging dir will be\n            created as tempdir if no staging dir profile is found. Input value\n            False will return None if no staging dir profile is found.\n        force_tmp_dir (Optional[bool]): If True, staging dir will be created as\n            tempdir.\n        logger (Optional[logging.Logger]): Logger instance.\n        prefix (Optional[str]) Optional prefix for staging dir name.\n        suffix (Optional[str]): Optional suffix for staging dir name.\n        username (Optional[str]): AYON Username.\n        product_base_type (str): Product base type.\n\n    Returns:\n        Optional[StagingDir]: Staging dir info data\n\n    \"\"\"\n    if logger is None:\n        logger = Logger.get_logger(\"get_staging_dir_info\")\n\n    if anatomy is None:\n        anatomy = Anatomy(\n            project_entity[\"name\"], project_entity=project_entity\n        )\n\n    if not product_base_type:\n        msg = (\n            \"Missing 'product_base_type' in 'get_staging_dir_info'.\"\n            \" Will be soon required.\"\n        )\n        logger.warning(msg)\n        warnings.warn(msg, DeprecationWarning, stacklevel=2)\n        product_base_type = product_type\n\n    if force_tmp_dir:\n        return StagingDir(\n            get_temp_dir(\n                project_name=project_entity[\"name\"],\n                anatomy=anatomy,\n                prefix=prefix,\n                suffix=suffix,\n            ),\n            is_persistent=False,\n            is_custom=False\n        )\n\n    # making few queries to database\n    ctx_data = get_template_data(\n        project_entity,\n        folder_entity,\n        task_entity,\n        host_name,\n        settings=project_settings,\n        username=username\n    )\n\n    # add additional data\n    ctx_data[\"product\"] = {\n        \"name\": product_name,\n        \"type\": product_type,\n        \"basetype\": product_base_type,\n    }\n\n    # add additional template formatting data\n    if template_data:\n        ctx_data.update(template_data)\n\n    task_name = task_type = None\n    if task_entity:\n        task_name = task_entity[\"name\"]\n        task_type = task_entity[\"taskType\"]\n\n    # get staging dir config\n    staging_dir_config = get_staging_dir_config(\n        project_entity[\"name\"],\n        task_type,\n        task_name,\n        product_base_type=product_base_type,\n        product_type=product_base_type,\n        product_name=product_name,\n        host_name=host_name,\n        project_settings=project_settings,\n        anatomy=anatomy,\n        logger=logger,\n    )\n\n    if staging_dir_config:\n        dir_template = staging_dir_config[\"template\"][\"directory\"]\n        return StagingDir(\n            dir_template.format_strict(ctx_data),\n            is_persistent=staging_dir_config[\"persistence\"],\n            is_custom=True\n        )\n\n    # no config found but force an output\n    if always_return_path:\n        return StagingDir(\n            get_temp_dir(\n                project_name=project_entity[\"name\"],\n                anatomy=anatomy,\n                prefix=prefix,\n                suffix=suffix,\n            ),\n            is_persistent=False,\n            is_custom=False\n        )\n\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/structures.html","title":"structures","text":""},{"location":"autoapi/client/ayon_core/pipeline/structures.html#client.ayon_core.pipeline.structures.ListConfig","title":"<code>ListConfig</code>  <code>dataclass</code>","text":"<p>Define a list.</p> Source code in <code>client/ayon_core/pipeline/structures.py</code> <pre><code>@dataclass\nclass ListConfig:\n    \"\"\"Define a list.\"\"\"\n    label: str\n    list_type: ListType = \"generic\"\n    list_folders: list[ListConfigFolder] = field(default_factory=list)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/tempdir.html","title":"tempdir","text":"<p>Temporary folder operations</p>"},{"location":"autoapi/client/ayon_core/pipeline/tempdir.html#client.ayon_core.pipeline.tempdir.create_custom_tempdir","title":"<code>create_custom_tempdir(project_name, anatomy=None)</code>","text":"<p>Backward compatibility deprecated since 2024/12/09.</p> Source code in <code>client/ayon_core/pipeline/tempdir.py</code> <pre><code>def create_custom_tempdir(project_name, anatomy=None):\n    \"\"\"Backward compatibility deprecated since 2024/12/09.\n    \"\"\"\n    warnings.warn(\n        \"Used deprecated 'create_custom_tempdir' \"\n        \"use 'ayon_core.pipeline.tempdir.get_temp_dir' instead.\",\n        DeprecationWarning,\n    )\n\n    if anatomy is None:\n        anatomy = Anatomy(project_name)\n\n    return _create_custom_tempdir(project_name, anatomy)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/tempdir.html#client.ayon_core.pipeline.tempdir.get_temp_dir","title":"<code>get_temp_dir(project_name, anatomy=None, prefix=None, suffix=None, use_local_temp=False)</code>","text":"<p>Get temporary dir path.</p> <p>If <code>use_local_temp</code> is set, tempdir will be created in local tempdir. If <code>anatomy</code> is not set, default anatomy will be used. If <code>prefix</code> or <code>suffix</code> is not set, default values will be used.</p> <p>It also supports <code>AYON_TMPDIR</code>, so studio can define own temp shared repository per project or even per more granular context. Template formatting is supported also with optional keys. Folder is created in case it doesn't exists.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Name of project.</p> required <code>anatomy</code> <code>Optional[Anatomy]</code> <p>Project Anatomy object.</p> <code>None</code> <code>suffix</code> <code>Optional[str]</code> <p>Suffix for tempdir.</p> <code>None</code> <code>prefix</code> <code>Optional[str]</code> <p>Prefix for tempdir.</p> <code>None</code> <code>use_local_temp</code> <code>Optional[bool]</code> <p>If True, temp dir will be created in local tempdir.</p> <code>False</code> <p>Returns:</p> Name Type Description <code>str</code> <p>Path to staging dir of instance.</p> Source code in <code>client/ayon_core/pipeline/tempdir.py</code> <pre><code>def get_temp_dir(\n    project_name, anatomy=None, prefix=None, suffix=None, use_local_temp=False\n):\n    \"\"\"Get temporary dir path.\n\n    If `use_local_temp` is set, tempdir will be created in local tempdir.\n    If `anatomy` is not set, default anatomy will be used.\n    If `prefix` or `suffix` is not set, default values will be used.\n\n    It also supports `AYON_TMPDIR`, so studio can define own temp\n    shared repository per project or even per more granular context.\n    Template formatting is supported also with optional keys. Folder is\n    created in case it doesn't exists.\n\n    Args:\n        project_name (str): Name of project.\n        anatomy (Optional[Anatomy]): Project Anatomy object.\n        suffix (Optional[str]): Suffix for tempdir.\n        prefix (Optional[str]): Prefix for tempdir.\n        use_local_temp (Optional[bool]): If True, temp dir will be created in\n            local tempdir.\n\n    Returns:\n        str: Path to staging dir of instance.\n\n    \"\"\"\n    if prefix is None:\n        prefix = \"ay_tmp_\"\n    suffix = suffix or \"\"\n\n    if use_local_temp:\n        return _create_local_staging_dir(prefix, suffix)\n\n    # make sure anatomy is set\n    if not anatomy:\n        anatomy = Anatomy(project_name)\n\n    # get customized tempdir path from `OPENPYPE_TMPDIR` env var\n    custom_temp_dir = _create_custom_tempdir(anatomy.project_name, anatomy)\n\n    return _create_local_staging_dir(prefix, suffix, dirpath=custom_temp_dir)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/template_data.html","title":"template_data","text":""},{"location":"autoapi/client/ayon_core/pipeline/template_data.html#client.ayon_core.pipeline.template_data.get_folder_template_data","title":"<code>get_folder_template_data(folder_entity, project_name)</code>","text":"<p>Extract data from folder entity that are used in templates.</p> <p>Output dictionary contains keys: - 'folder'      - dictionary with 'name' key filled with folder name - 'hierarchy'   - parent folder names joined with '/' - 'parent'      - direct parent name, project name used if is under                   project</p> Required entity fields <p>Folder: 'path', 'folderType'</p> <p>Parameters:</p> Name Type Description Default <code>folder_entity</code> <code>Dict[str, Any]</code> <p>Folder entity.</p> required <code>project_name</code> <code>str</code> <p>Is used for 'parent' key if folder entity does not have any.</p> required <p>Returns:</p> Type Description <p>Dict[str, str]: Data that are based on folder entity and can be used in templates.</p> Source code in <code>client/ayon_core/pipeline/template_data.py</code> <pre><code>def get_folder_template_data(folder_entity, project_name):\n    \"\"\"Extract data from folder entity that are used in templates.\n\n    Output dictionary contains keys:\n    - 'folder'      - dictionary with 'name' key filled with folder name\n    - 'hierarchy'   - parent folder names joined with '/'\n    - 'parent'      - direct parent name, project name used if is under\n                      project\n\n    Required entity fields:\n        Folder: 'path', 'folderType'\n\n    Args:\n        folder_entity (Dict[str, Any]): Folder entity.\n        project_name (str): Is used for 'parent' key if folder entity\n            does not have any.\n\n    Returns:\n        Dict[str, str]: Data that are based on folder entity and can be used\n            in templates.\n    \"\"\"\n\n    path = folder_entity[\"path\"]\n    # Remove empty string from the beginning and split by '/'\n    parents = path.lstrip(\"/\").split(\"/\")\n    # Remove last part which is folder name\n    folder_name = parents.pop(-1)\n    hierarchy = \"/\".join(parents)\n    if parents:\n        parent_name = parents[-1]\n    else:\n        parent_name = project_name\n\n    return {\n        \"folder\": {\n            \"name\": folder_name,\n            \"type\": folder_entity[\"folderType\"],\n            \"path\": path,\n            \"parents\": parents,\n        },\n        \"hierarchy\": hierarchy,\n        \"parent\": parent_name\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/template_data.html#client.ayon_core.pipeline.template_data.get_general_template_data","title":"<code>get_general_template_data(settings=None, username=None, user_entity=None)</code>","text":"<p>General template data based on system settings or machine.</p> <p>Output contains formatting keys: - 'studio[name]'      - Studio name filled from system settings - 'studio[code]'      - Studio code filled from system settings - 'user[name]'        - User's name - 'user[attrib][...]' - User's attributes - 'user[data][...]'   - User's data</p> <p>Parameters:</p> Name Type Description Default <code>settings</code> <code>Dict[str, Any]</code> <p>Studio or project settings.</p> <code>None</code> <code>username</code> <code>Optional[str]</code> <p>AYON Username.</p> <code>None</code> <code>user_entity</code> <code>Optional[dict[str, Any]]</code> <p>User entity.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/template_data.py</code> <pre><code>def get_general_template_data(\n    settings: Optional[dict[str, Any]] = None,\n    username: Optional[str] = None,\n    user_entity: Optional[dict[str, Any]] = None,\n):\n    \"\"\"General template data based on system settings or machine.\n\n    Output contains formatting keys:\n    - 'studio[name]'      - Studio name filled from system settings\n    - 'studio[code]'      - Studio code filled from system settings\n    - 'user[name]'        - User's name\n    - 'user[attrib][...]' - User's attributes\n    - 'user[data][...]'   - User's data\n\n    Args:\n        settings (Dict[str, Any]): Studio or project settings.\n        username (Optional[str]): AYON Username.\n        user_entity (Optional[dict[str, Any]]): User entity.\n\n    \"\"\"\n    if not settings:\n        settings = get_studio_settings()\n\n    if user_entity is None:\n        user_entity = get_ayon_user_entity(username)\n\n    # Use dictionary with default value for backwards compatibility\n    # - we did support '{user}' now it should be '{user[name]}'\n    user_data = DefaultKeysDict(\n        \"name\",\n        {\n            \"name\": user_entity[\"name\"],\n            \"attrib\": user_entity[\"attrib\"],\n            \"data\": user_entity[\"data\"],\n        }\n    )\n\n    core_settings = settings[\"core\"]\n    return {\n        \"studio\": {\n            \"name\": core_settings[\"studio_name\"],\n            \"code\": core_settings[\"studio_code\"]\n        },\n        \"user\": user_data,\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/template_data.html#client.ayon_core.pipeline.template_data.get_project_template_data","title":"<code>get_project_template_data(project_entity=None, project_name=None)</code>","text":"<p>Extract data from project document that are used in templates.</p> <p>Project document must have 'name' and 'code'.</p> <p>One of 'project_name' or 'project_entity' must be passed. With prepared project document is function much faster because don't have to query.</p> <p>Output contains formatting keys: - 'project[name]'   - Project name - 'project[code]'   - Project code</p> <p>Parameters:</p> Name Type Description Default <code>project_entity</code> <code>Dict[str, Any]</code> <p>Queried project entity.</p> <code>None</code> <code>project_name</code> <code>str</code> <p>Name of project.</p> <code>None</code> <p>Returns:</p> Type Description <p>Dict[str, Dict[str, str]]: Template data based on project document.</p> Source code in <code>client/ayon_core/pipeline/template_data.py</code> <pre><code>def get_project_template_data(project_entity=None, project_name=None):\n    \"\"\"Extract data from project document that are used in templates.\n\n    Project document must have 'name' and 'code'.\n\n    One of 'project_name' or 'project_entity' must be passed. With prepared\n    project document is function much faster because don't have to query.\n\n    Output contains formatting keys:\n    - 'project[name]'   - Project name\n    - 'project[code]'   - Project code\n\n    Args:\n        project_entity (Dict[str, Any]): Queried project entity.\n        project_name (str): Name of project.\n\n    Returns:\n        Dict[str, Dict[str, str]]: Template data based on project document.\n    \"\"\"\n\n    if not project_name:\n        project_name = project_entity[\"name\"]\n\n    elif not project_entity:\n        project_entity = ayon_api.get_project(project_name, fields=[\"code\"])\n\n    project_code = project_entity[\"code\"]\n    return {\n        \"project\": {\n            \"name\": project_name,\n            \"code\": project_code\n        }\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/template_data.html#client.ayon_core.pipeline.template_data.get_task_template_data","title":"<code>get_task_template_data(project_entity, task_entity)</code>","text":"<p>Prepare task template data.</p> Required document fields <p>Project: 'tasksTypes' Task: 'type'</p> <p>Parameters:</p> Name Type Description Default <code>project_entity</code> <code>Dict[str, Any]</code> <p>Project entity.</p> required <code>task_entity</code> <code>Dict[str, Any]</code> <p>Task entity.</p> required <p>Returns:</p> Type Description <p>Dict[str, Dict[str, str]]: Template data</p> Source code in <code>client/ayon_core/pipeline/template_data.py</code> <pre><code>def get_task_template_data(project_entity, task_entity):\n    \"\"\"Prepare task template data.\n\n    Required document fields:\n        Project: 'tasksTypes'\n        Task: 'type'\n\n    Args:\n        project_entity (Dict[str, Any]): Project entity.\n        task_entity (Dict[str, Any]): Task entity.\n\n    Returns:\n        Dict[str, Dict[str, str]]: Template data\n\n    \"\"\"\n    project_task_types = project_entity[\"taskTypes\"]\n    task_types_by_name = {task[\"name\"]: task for task in project_task_types}\n    task_type = task_entity[\"taskType\"]\n    task_code = task_types_by_name.get(task_type, {}).get(\"shortName\")\n\n    return {\n        \"task\": {\n            \"name\": task_entity[\"name\"],\n            \"type\": task_type,\n            \"short\": task_code,\n        }\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/template_data.html#client.ayon_core.pipeline.template_data.get_template_data","title":"<code>get_template_data(project_entity, folder_entity=None, task_entity=None, host_name=None, settings=None, username=None, user_entity=None)</code>","text":"<p>Prepare data for templates filling from entered documents and info.</p> <p>This function does not \"auto fill\" any values except system settings and it's on purpose.</p> <p>Universal function to receive template data from passed arguments. Only required argument is project document all other arguments are optional and their values won't be added to template data if are not passed.</p> Required document fields <p>Project: 'name', 'code', 'taskTypes.name' Folder: 'name', 'path' Task: 'type'</p> <p>Parameters:</p> Name Type Description Default <code>project_entity</code> <code>Dict[str, Any]</code> <p>Project entity.</p> required <code>folder_entity</code> <code>Optional[Dict[str, Any]]</code> <p>Folder entity.</p> <code>None</code> <code>task_entity</code> <code>Optional[Dict[str, Any]</code> <p>Task entity.</p> <code>None</code> <code>host_name</code> <code>Optional[str]</code> <p>Used to fill '{app}' key.</p> <code>None</code> <code>settings</code> <code>Union[Dict, None]</code> <p>Prepared studio or project settings. They're queried if not passed (may be slower).</p> <code>None</code> <code>username</code> <code>Optional[str]</code> <p>DEPRECATED AYON Username.</p> <code>None</code> <code>user_entity</code> <code>Optional[dict[str, Any]</code> <p>AYON user entity.</p> <code>None</code> <p>Returns:</p> Type Description <p>Dict[str, Any]: Data prepared for filling workdir template.</p> Source code in <code>client/ayon_core/pipeline/template_data.py</code> <pre><code>def get_template_data(\n    project_entity,\n    folder_entity=None,\n    task_entity=None,\n    host_name=None,\n    settings=None,\n    username=None,\n    user_entity=None,\n):\n    \"\"\"Prepare data for templates filling from entered documents and info.\n\n    This function does not \"auto fill\" any values except system settings and\n    it's on purpose.\n\n    Universal function to receive template data from passed arguments. Only\n    required argument is project document all other arguments are optional\n    and their values won't be added to template data if are not passed.\n\n    Required document fields:\n        Project: 'name', 'code', 'taskTypes.name'\n        Folder: 'name', 'path'\n        Task: 'type'\n\n    Args:\n        project_entity (Dict[str, Any]): Project entity.\n        folder_entity (Optional[Dict[str, Any]]): Folder entity.\n        task_entity (Optional[Dict[str, Any]): Task entity.\n        host_name (Optional[str]): Used to fill '{app}' key.\n        settings (Union[Dict, None]): Prepared studio or project settings.\n            They're queried if not passed (may be slower).\n        username (Optional[str]): DEPRECATED AYON Username.\n        user_entity (Optional[dict[str, Any]): AYON user entity.\n\n    Returns:\n        Dict[str, Any]: Data prepared for filling workdir template.\n    \"\"\"\n\n    template_data = get_general_template_data(\n        settings,\n        username=username,\n        user_entity=user_entity,\n    )\n    template_data.update(get_project_template_data(project_entity))\n    if folder_entity:\n        template_data.update(get_folder_template_data(\n            folder_entity, project_entity[\"name\"]\n        ))\n        if task_entity:\n            template_data.update(get_task_template_data(\n                project_entity, task_entity\n            ))\n\n    if host_name:\n        template_data[\"app\"] = host_name\n        template_data[\"host\"] = {\n            \"name\": host_name,\n        }\n\n    return template_data\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/template_data.html#client.ayon_core.pipeline.template_data.get_template_data_with_names","title":"<code>get_template_data_with_names(project_name, folder_path=None, task_name=None, host_name=None, settings=None)</code>","text":"<p>Prepare data for templates filling from entered entity names and info.</p> <p>Copy of 'get_template_data' but based on entity names instead of documents. Only difference is that documents are queried.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>folder_path</code> <code>Optional[str]</code> <p>Folder path.</p> <code>None</code> <code>task_name</code> <code>Optional[str]</code> <p>Task name.</p> <code>None</code> <code>host_name</code> <code>Optional[str]</code> <p>Used to fill '{app}' key. because workdir template may contain <code>{app}</code> key.</p> <code>None</code> <code>settings</code> <code>Optional[Dict]</code> <p>Prepared studio or project settings. They're queried if not passed.</p> <code>None</code> <p>Returns:</p> Type Description <p>Dict[str, Any]: Data prepared for filling workdir template.</p> Source code in <code>client/ayon_core/pipeline/template_data.py</code> <pre><code>def get_template_data_with_names(\n    project_name,\n    folder_path=None,\n    task_name=None,\n    host_name=None,\n    settings=None\n):\n    \"\"\"Prepare data for templates filling from entered entity names and info.\n\n    Copy of 'get_template_data' but based on entity names instead of documents.\n    Only difference is that documents are queried.\n\n    Args:\n        project_name (str): Project name.\n        folder_path (Optional[str]): Folder path.\n        task_name (Optional[str]): Task name.\n        host_name (Optional[str]):Used to fill '{app}' key.\n            because workdir template may contain `{app}` key.\n        settings (Optional[Dict]): Prepared studio or project settings.\n            They're queried if not passed.\n\n    Returns:\n        Dict[str, Any]: Data prepared for filling workdir template.\n    \"\"\"\n\n    project_entity = ayon_api.get_project(project_name)\n    folder_entity = None\n    task_entity = None\n    if folder_path:\n        folder_entity = ayon_api.get_folder_by_path(\n            project_name,\n            folder_path,\n            fields={\"id\", \"path\", \"folderType\"}\n        )\n        if task_name and folder_entity:\n            task_entity = ayon_api.get_task_by_name(\n                project_name, folder_entity[\"id\"], task_name\n            )\n    return get_template_data(\n        project_entity, folder_entity, task_entity, host_name, settings\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/thumbnails.html","title":"thumbnails","text":""},{"location":"autoapi/client/ayon_core/pipeline/thumbnails.html#client.ayon_core.pipeline.thumbnails.ThumbnailsCache","title":"<code>ThumbnailsCache</code>","text":"<p>Cache of thumbnails on local storage.</p> <p>Thumbnails are cached to appdirs to predefined directory. Each project has own subfolder with thumbnails -&gt; that's because each project has own thumbnail id validation and file names are thumbnail ids with matching extension. Extensions are predefined (.png and .jpeg).</p> <p>Cache has cleanup mechanism which is triggered on initialized by default.</p> <p>The cleanup has 2 levels: 1. soft cleanup which remove all files that are older then 'days_alive' 2. max size cleanup which remove all files until the thumbnails folder     contains less then 'max_filesize'     - this is time consuming so it's not triggered automatically</p> <p>Parameters:</p> Name Type Description Default <code>cleanup</code> <code>bool</code> <p>Trigger soft cleanup (Cleanup expired thumbnails).</p> <code>True</code> Source code in <code>client/ayon_core/pipeline/thumbnails.py</code> <pre><code>class ThumbnailsCache:\n    \"\"\"Cache of thumbnails on local storage.\n\n    Thumbnails are cached to appdirs to predefined directory. Each project has\n    own subfolder with thumbnails -&gt; that's because each project has own\n    thumbnail id validation and file names are thumbnail ids with matching\n    extension. Extensions are predefined (.png and .jpeg).\n\n    Cache has cleanup mechanism which is triggered on initialized by default.\n\n    The cleanup has 2 levels:\n    1. soft cleanup which remove all files that are older then 'days_alive'\n    2. max size cleanup which remove all files until the thumbnails folder\n        contains less then 'max_filesize'\n        - this is time consuming so it's not triggered automatically\n\n    Args:\n        cleanup (bool): Trigger soft cleanup (Cleanup expired thumbnails).\n    \"\"\"\n\n    # Lifetime of thumbnails (in seconds)\n    # - default 3 days\n    days_alive = 3\n    # Max size of thumbnail directory (in bytes)\n    # - default 2 Gb\n    max_filesize = 2 * 1024 * 1024 * 1024\n\n    def __init__(self, cleanup=True):\n        self._thumbnails_dir = None\n        self._days_alive_secs = self.days_alive * 24 * 60 * 60\n        if cleanup:\n            self.cleanup()\n\n    def get_thumbnails_dir(self):\n        \"\"\"Root directory where thumbnails are stored.\n\n        Returns:\n            str: Path to thumbnails root.\n        \"\"\"\n\n        if self._thumbnails_dir is None:\n            self._thumbnails_dir = get_launcher_local_dir(\"thumbnails\")\n        return self._thumbnails_dir\n\n    thumbnails_dir = property(get_thumbnails_dir)\n\n    def get_thumbnails_dir_file_info(self):\n        \"\"\"Get information about all files in thumbnails directory.\n\n        Returns:\n            List[FileInfo]: List of file information about all files.\n        \"\"\"\n\n        thumbnails_dir = self.thumbnails_dir\n        files_info = []\n        if not os.path.exists(thumbnails_dir):\n            return files_info\n\n        for root, _, filenames in os.walk(thumbnails_dir):\n            for filename in filenames:\n                path = os.path.join(root, filename)\n                files_info.append(FileInfo(\n                    path, os.path.getsize(path), os.path.getmtime(path)\n                ))\n        return files_info\n\n    def get_thumbnails_dir_size(self, files_info=None):\n        \"\"\"Got full size of thumbnail directory.\n\n        Args:\n            files_info (List[FileInfo]): Prepared file information about\n                files in thumbnail directory.\n\n        Returns:\n            int: File size of all files in thumbnail directory.\n        \"\"\"\n\n        if files_info is None:\n            files_info = self.get_thumbnails_dir_file_info()\n\n        if not files_info:\n            return 0\n\n        return sum(\n            file_info.size\n            for file_info in files_info\n        )\n\n    def cleanup(self, check_max_size=False):\n        \"\"\"Cleanup thumbnails directory.\n\n        Args:\n            check_max_size (bool): Also cleanup files to match max size of\n                thumbnails directory.\n        \"\"\"\n\n        thumbnails_dir = self.get_thumbnails_dir()\n        # Skip if thumbnails dir does not exist yet\n        if not os.path.exists(thumbnails_dir):\n            return\n\n        self._soft_cleanup(thumbnails_dir)\n        if check_max_size:\n            self._max_size_cleanup(thumbnails_dir)\n\n    def _soft_cleanup(self, thumbnails_dir):\n        current_time = time.time()\n        for root, _, filenames in os.walk(thumbnails_dir):\n            for filename in filenames:\n                path = os.path.join(root, filename)\n                modification_time = os.path.getmtime(path)\n                if current_time - modification_time &gt; self._days_alive_secs:\n                    os.remove(path)\n\n    def _max_size_cleanup(self, thumbnails_dir):\n        files_info = self.get_thumbnails_dir_file_info()\n        size = self.get_thumbnails_dir_size(files_info)\n        if size &lt; self.max_filesize:\n            return\n\n        sorted_file_info = collections.deque(\n            sorted(files_info, key=lambda item: item.modification_time)\n        )\n        diff = size - self.max_filesize\n        while diff &gt; 0:\n            if not sorted_file_info:\n                break\n\n            file_info = sorted_file_info.popleft()\n            diff -= file_info.size\n            os.remove(file_info.path)\n\n    def get_thumbnail_filepath(self, project_name, thumbnail_id):\n        \"\"\"Get thumbnail by thumbnail id.\n\n        Args:\n            project_name (str): Name of project.\n            thumbnail_id (str): Thumbnail id.\n\n        Returns:\n            Union[str, None]: Path to thumbnail image or None if thumbnail\n                is not cached yet.\n        \"\"\"\n\n        if not thumbnail_id:\n            return None\n\n        for ext in (\n            \".png\",\n            \".jpeg\",\n        ):\n            filepath = os.path.join(\n                self.thumbnails_dir, project_name, thumbnail_id + ext\n            )\n            if os.path.exists(filepath):\n                return filepath\n        return None\n\n    def get_project_dir(self, project_name):\n        \"\"\"Path to root directory for specific project.\n\n        Args:\n            project_name (str): Name of project for which root directory path\n                should be returned.\n\n        Returns:\n            str: Path to root of project's thumbnails.\n        \"\"\"\n\n        return os.path.join(self.thumbnails_dir, project_name)\n\n    def make_sure_project_dir_exists(self, project_name):\n        project_dir = self.get_project_dir(project_name)\n        if not os.path.exists(project_dir):\n            os.makedirs(project_dir)\n        return project_dir\n\n    def store_thumbnail(self, project_name, thumbnail_id, content, mime_type):\n        \"\"\"Store thumbnail to cache folder.\n\n        Args:\n            project_name (str): Project where the thumbnail belong to.\n            thumbnail_id (str): Thumbnail id.\n            content (bytes): Byte content of thumbnail file.\n            mime_type (str): Type of content.\n\n        Returns:\n            str: Path to cached thumbnail image file.\n        \"\"\"\n\n        if mime_type == \"image/png\":\n            ext = \".png\"\n        elif mime_type == \"image/jpeg\":\n            ext = \".jpeg\"\n        else:\n            raise ValueError(\n                \"Unknown mime type for thumbnail \\\"{}\\\"\".format(mime_type))\n\n        project_dir = self.make_sure_project_dir_exists(project_name)\n        thumbnail_path = os.path.join(project_dir, thumbnail_id + ext)\n        with open(thumbnail_path, \"wb\") as stream:\n            stream.write(content)\n\n        current_time = time.time()\n        os.utime(thumbnail_path, (current_time, current_time))\n\n        return thumbnail_path\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/thumbnails.html#client.ayon_core.pipeline.thumbnails.ThumbnailsCache.cleanup","title":"<code>cleanup(check_max_size=False)</code>","text":"<p>Cleanup thumbnails directory.</p> <p>Parameters:</p> Name Type Description Default <code>check_max_size</code> <code>bool</code> <p>Also cleanup files to match max size of thumbnails directory.</p> <code>False</code> Source code in <code>client/ayon_core/pipeline/thumbnails.py</code> <pre><code>def cleanup(self, check_max_size=False):\n    \"\"\"Cleanup thumbnails directory.\n\n    Args:\n        check_max_size (bool): Also cleanup files to match max size of\n            thumbnails directory.\n    \"\"\"\n\n    thumbnails_dir = self.get_thumbnails_dir()\n    # Skip if thumbnails dir does not exist yet\n    if not os.path.exists(thumbnails_dir):\n        return\n\n    self._soft_cleanup(thumbnails_dir)\n    if check_max_size:\n        self._max_size_cleanup(thumbnails_dir)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/thumbnails.html#client.ayon_core.pipeline.thumbnails.ThumbnailsCache.get_project_dir","title":"<code>get_project_dir(project_name)</code>","text":"<p>Path to root directory for specific project.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Name of project for which root directory path should be returned.</p> required <p>Returns:</p> Name Type Description <code>str</code> <p>Path to root of project's thumbnails.</p> Source code in <code>client/ayon_core/pipeline/thumbnails.py</code> <pre><code>def get_project_dir(self, project_name):\n    \"\"\"Path to root directory for specific project.\n\n    Args:\n        project_name (str): Name of project for which root directory path\n            should be returned.\n\n    Returns:\n        str: Path to root of project's thumbnails.\n    \"\"\"\n\n    return os.path.join(self.thumbnails_dir, project_name)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/thumbnails.html#client.ayon_core.pipeline.thumbnails.ThumbnailsCache.get_thumbnail_filepath","title":"<code>get_thumbnail_filepath(project_name, thumbnail_id)</code>","text":"<p>Get thumbnail by thumbnail id.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Name of project.</p> required <code>thumbnail_id</code> <code>str</code> <p>Thumbnail id.</p> required <p>Returns:</p> Type Description <p>Union[str, None]: Path to thumbnail image or None if thumbnail is not cached yet.</p> Source code in <code>client/ayon_core/pipeline/thumbnails.py</code> <pre><code>def get_thumbnail_filepath(self, project_name, thumbnail_id):\n    \"\"\"Get thumbnail by thumbnail id.\n\n    Args:\n        project_name (str): Name of project.\n        thumbnail_id (str): Thumbnail id.\n\n    Returns:\n        Union[str, None]: Path to thumbnail image or None if thumbnail\n            is not cached yet.\n    \"\"\"\n\n    if not thumbnail_id:\n        return None\n\n    for ext in (\n        \".png\",\n        \".jpeg\",\n    ):\n        filepath = os.path.join(\n            self.thumbnails_dir, project_name, thumbnail_id + ext\n        )\n        if os.path.exists(filepath):\n            return filepath\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/thumbnails.html#client.ayon_core.pipeline.thumbnails.ThumbnailsCache.get_thumbnails_dir","title":"<code>get_thumbnails_dir()</code>","text":"<p>Root directory where thumbnails are stored.</p> <p>Returns:</p> Name Type Description <code>str</code> <p>Path to thumbnails root.</p> Source code in <code>client/ayon_core/pipeline/thumbnails.py</code> <pre><code>def get_thumbnails_dir(self):\n    \"\"\"Root directory where thumbnails are stored.\n\n    Returns:\n        str: Path to thumbnails root.\n    \"\"\"\n\n    if self._thumbnails_dir is None:\n        self._thumbnails_dir = get_launcher_local_dir(\"thumbnails\")\n    return self._thumbnails_dir\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/thumbnails.html#client.ayon_core.pipeline.thumbnails.ThumbnailsCache.get_thumbnails_dir_file_info","title":"<code>get_thumbnails_dir_file_info()</code>","text":"<p>Get information about all files in thumbnails directory.</p> <p>Returns:</p> Type Description <p>List[FileInfo]: List of file information about all files.</p> Source code in <code>client/ayon_core/pipeline/thumbnails.py</code> <pre><code>def get_thumbnails_dir_file_info(self):\n    \"\"\"Get information about all files in thumbnails directory.\n\n    Returns:\n        List[FileInfo]: List of file information about all files.\n    \"\"\"\n\n    thumbnails_dir = self.thumbnails_dir\n    files_info = []\n    if not os.path.exists(thumbnails_dir):\n        return files_info\n\n    for root, _, filenames in os.walk(thumbnails_dir):\n        for filename in filenames:\n            path = os.path.join(root, filename)\n            files_info.append(FileInfo(\n                path, os.path.getsize(path), os.path.getmtime(path)\n            ))\n    return files_info\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/thumbnails.html#client.ayon_core.pipeline.thumbnails.ThumbnailsCache.get_thumbnails_dir_size","title":"<code>get_thumbnails_dir_size(files_info=None)</code>","text":"<p>Got full size of thumbnail directory.</p> <p>Parameters:</p> Name Type Description Default <code>files_info</code> <code>List[FileInfo]</code> <p>Prepared file information about files in thumbnail directory.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>int</code> <p>File size of all files in thumbnail directory.</p> Source code in <code>client/ayon_core/pipeline/thumbnails.py</code> <pre><code>def get_thumbnails_dir_size(self, files_info=None):\n    \"\"\"Got full size of thumbnail directory.\n\n    Args:\n        files_info (List[FileInfo]): Prepared file information about\n            files in thumbnail directory.\n\n    Returns:\n        int: File size of all files in thumbnail directory.\n    \"\"\"\n\n    if files_info is None:\n        files_info = self.get_thumbnails_dir_file_info()\n\n    if not files_info:\n        return 0\n\n    return sum(\n        file_info.size\n        for file_info in files_info\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/thumbnails.html#client.ayon_core.pipeline.thumbnails.ThumbnailsCache.store_thumbnail","title":"<code>store_thumbnail(project_name, thumbnail_id, content, mime_type)</code>","text":"<p>Store thumbnail to cache folder.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project where the thumbnail belong to.</p> required <code>thumbnail_id</code> <code>str</code> <p>Thumbnail id.</p> required <code>content</code> <code>bytes</code> <p>Byte content of thumbnail file.</p> required <code>mime_type</code> <code>str</code> <p>Type of content.</p> required <p>Returns:</p> Name Type Description <code>str</code> <p>Path to cached thumbnail image file.</p> Source code in <code>client/ayon_core/pipeline/thumbnails.py</code> <pre><code>def store_thumbnail(self, project_name, thumbnail_id, content, mime_type):\n    \"\"\"Store thumbnail to cache folder.\n\n    Args:\n        project_name (str): Project where the thumbnail belong to.\n        thumbnail_id (str): Thumbnail id.\n        content (bytes): Byte content of thumbnail file.\n        mime_type (str): Type of content.\n\n    Returns:\n        str: Path to cached thumbnail image file.\n    \"\"\"\n\n    if mime_type == \"image/png\":\n        ext = \".png\"\n    elif mime_type == \"image/jpeg\":\n        ext = \".jpeg\"\n    else:\n        raise ValueError(\n            \"Unknown mime type for thumbnail \\\"{}\\\"\".format(mime_type))\n\n    project_dir = self.make_sure_project_dir_exists(project_name)\n    thumbnail_path = os.path.join(project_dir, thumbnail_id + ext)\n    with open(thumbnail_path, \"wb\") as stream:\n        stream.write(content)\n\n    current_time = time.time()\n    os.utime(thumbnail_path, (current_time, current_time))\n\n    return thumbnail_path\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/thumbnails.html#client.ayon_core.pipeline.thumbnails.get_thumbnail_path","title":"<code>get_thumbnail_path(project_name, entity_type, entity_id, thumbnail_id)</code>","text":"<p>Get path to thumbnail image.</p> <p>Thumbnail is cached by thumbnail id but is received using entity type and     entity id.</p> Notes <p>Function 'get_thumbnail_by_id' can't be used because does not work     for artists. The endpoint can't validate artist permissions.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project where thumbnail belongs to.</p> required <code>entity_type</code> <code>str</code> <p>Entity type \"folder\", \"task\", \"version\" and \"workfile\".</p> required <code>entity_id</code> <code>str</code> <p>Entity id.</p> required <code>thumbnail_id</code> <code>Union[str, None]</code> <p>Thumbnail id.</p> required <p>Returns:</p> Type Description <p>Union[str, None]: Path to thumbnail image or None if thumbnail id is not valid or thumbnail was not possible to receive.</p> Source code in <code>client/ayon_core/pipeline/thumbnails.py</code> <pre><code>def get_thumbnail_path(\n    project_name: str,\n    entity_type: str,\n    entity_id: str,\n    thumbnail_id: str\n):\n    \"\"\"Get path to thumbnail image.\n\n    Thumbnail is cached by thumbnail id but is received using entity type and\n        entity id.\n\n    Notes:\n        Function 'get_thumbnail_by_id' can't be used because does not work\n            for artists. The endpoint can't validate artist permissions.\n\n    Args:\n        project_name (str): Project where thumbnail belongs to.\n        entity_type (str): Entity type \"folder\", \"task\", \"version\"\n            and \"workfile\".\n        entity_id (str): Entity id.\n        thumbnail_id (Union[str, None]): Thumbnail id.\n\n    Returns:\n        Union[str, None]: Path to thumbnail image or None if thumbnail\n            id is not valid or thumbnail was not possible to receive.\n\n    \"\"\"\n    if not thumbnail_id:\n        return None\n\n    filepath = _CacheItems.thumbnails_cache.get_thumbnail_filepath(\n        project_name, thumbnail_id\n    )\n    if filepath is not None:\n        return filepath\n\n    # 'ayon_api' had a bug, public function\n    #   'get_thumbnail_by_id' did not return output of\n    #   'ServerAPI' method.\n    con = ayon_api.get_server_api_connection()\n    result = con.get_thumbnail(project_name, entity_type, entity_id)\n\n    if result is not None and result.is_valid:\n        return _CacheItems.thumbnails_cache.store_thumbnail(\n            project_name,\n            thumbnail_id,\n            result.content,\n            result.content_type\n        )\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/usdlib.html","title":"usdlib","text":""},{"location":"autoapi/client/ayon_core/pipeline/usdlib.html#client.ayon_core.pipeline.usdlib.Layer","title":"<code>Layer</code>  <code>dataclass</code>","text":"Source code in <code>client/ayon_core/pipeline/usdlib.py</code> <pre><code>@dataclasses.dataclass\nclass Layer:\n    layer: Sdf.Layer\n    path: str\n    # Allow to anchor a layer to another so that when the layer would be\n    # exported it'd write itself out relative to its anchor\n    anchor: 'Layer' = None\n\n    @property\n    def identifier(self):\n        return self.layer.identifier\n\n    def get_full_path(self):\n        \"\"\"Return full path relative to the anchor layer\"\"\"\n        if not os.path.isabs(self.path) and self.anchor:\n            anchor_path = self.anchor.get_full_path()\n            root = os.path.dirname(anchor_path)\n            return os.path.normpath(os.path.join(root, self.path))\n        return self.path\n\n    def export(self, path=None, args=None):\n        \"\"\"Save the layer\"\"\"\n        if path is None:\n            path = self.get_full_path()\n\n        if args is None:\n            args = self.layer.GetFileFormatArguments()\n\n        self.layer.Export(path, args=args)\n\n    @classmethod\n    def create_anonymous(cls, path, tag=\"LOP\", anchor=None):\n        \"\"\"Create an anonymous layer instance.\n\n        Arguments:\n            path (str): The layer's filepath.\n            tag (Optional[str]): The tag to give to the anonymous layer.\n                This defaults to 'LOP' because Houdini requires that tag for\n                its in-memory layers that it will be able to manage. In other\n                integrations no similar requirements have been found so it was\n                deemed a 'safe' default for now.\n            anchor (Optional[Layer]): Another layer to relatively anchor to.\n        \"\"\"\n        sdf_layer = Sdf.Layer.CreateAnonymous(tag)\n        return cls(layer=sdf_layer, path=path, anchor=anchor)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/usdlib.html#client.ayon_core.pipeline.usdlib.Layer.create_anonymous","title":"<code>create_anonymous(path, tag='LOP', anchor=None)</code>  <code>classmethod</code>","text":"<p>Create an anonymous layer instance.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>The layer's filepath.</p> required <code>tag</code> <code>Optional[str]</code> <p>The tag to give to the anonymous layer. This defaults to 'LOP' because Houdini requires that tag for its in-memory layers that it will be able to manage. In other integrations no similar requirements have been found so it was deemed a 'safe' default for now.</p> <code>'LOP'</code> <code>anchor</code> <code>Optional[Layer]</code> <p>Another layer to relatively anchor to.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/usdlib.py</code> <pre><code>@classmethod\ndef create_anonymous(cls, path, tag=\"LOP\", anchor=None):\n    \"\"\"Create an anonymous layer instance.\n\n    Arguments:\n        path (str): The layer's filepath.\n        tag (Optional[str]): The tag to give to the anonymous layer.\n            This defaults to 'LOP' because Houdini requires that tag for\n            its in-memory layers that it will be able to manage. In other\n            integrations no similar requirements have been found so it was\n            deemed a 'safe' default for now.\n        anchor (Optional[Layer]): Another layer to relatively anchor to.\n    \"\"\"\n    sdf_layer = Sdf.Layer.CreateAnonymous(tag)\n    return cls(layer=sdf_layer, path=path, anchor=anchor)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/usdlib.html#client.ayon_core.pipeline.usdlib.Layer.export","title":"<code>export(path=None, args=None)</code>","text":"<p>Save the layer</p> Source code in <code>client/ayon_core/pipeline/usdlib.py</code> <pre><code>def export(self, path=None, args=None):\n    \"\"\"Save the layer\"\"\"\n    if path is None:\n        path = self.get_full_path()\n\n    if args is None:\n        args = self.layer.GetFileFormatArguments()\n\n    self.layer.Export(path, args=args)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/usdlib.html#client.ayon_core.pipeline.usdlib.Layer.get_full_path","title":"<code>get_full_path()</code>","text":"<p>Return full path relative to the anchor layer</p> Source code in <code>client/ayon_core/pipeline/usdlib.py</code> <pre><code>def get_full_path(self):\n    \"\"\"Return full path relative to the anchor layer\"\"\"\n    if not os.path.isabs(self.path) and self.anchor:\n        anchor_path = self.anchor.get_full_path()\n        root = os.path.dirname(anchor_path)\n        return os.path.normpath(os.path.join(root, self.path))\n    return self.path\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/usdlib.html#client.ayon_core.pipeline.usdlib.add_ordered_reference","title":"<code>add_ordered_reference(layer, prim_path, reference, order)</code>","text":"<p>Add reference alongside other ordered references.</p> <p>Parameters:</p> Name Type Description Default <code>layer</code> <code>Layer</code> <p>Layer to operate in.</p> required <code>prim_path</code> <code>Path</code> <p>Prim path to reference into. This may include variant selections to reference into a prim inside the variant selection.</p> required <code>reference</code> <code>Reference</code> <p>Reference to add.</p> required <code>order</code> <code>int</code> <p>Order.</p> required <p>Returns:</p> Type Description <p>Sdf.PrimSpec: The prim spec for the prim path.</p> Source code in <code>client/ayon_core/pipeline/usdlib.py</code> <pre><code>def add_ordered_reference(\n        layer,\n        prim_path,\n        reference,\n        order\n):\n    \"\"\"Add reference alongside other ordered references.\n\n    Args:\n        layer (Sdf.Layer): Layer to operate in.\n        prim_path (Sdf.Path): Prim path to reference into.\n            This may include variant selections to reference into a prim\n            inside the variant selection.\n        reference (Sdf.Reference): Reference to add.\n        order  (int): Order.\n\n    Returns:\n        Sdf.PrimSpec: The prim spec for the prim path.\n\n    \"\"\"\n    assert isinstance(order, int), \"order must be integer\"\n\n    # Sdf.Reference is immutable, see: `pxr/usd/sdf/wrapReference.cpp`\n    # A Sdf.Reference can't be edited in Python so we create a new entry\n    # matching the original with the extra data entry added.\n    custom_data = reference.customData\n    custom_data[\"ayon_order\"] = order\n    reference = Sdf.Reference(\n        assetPath=reference.assetPath,\n        primPath=reference.primPath,\n        layerOffset=reference.layerOffset,\n        customData=custom_data\n    )\n\n    # TODO: inherit type from outside of variants if it has it\n    prim_spec = get_or_define_prim_spec(layer, prim_path, \"Xform\")\n\n    # Insert new entry at correct order\n    entries = list(prim_spec.referenceList.prependedItems)\n\n    if not entries:\n        prim_spec.referenceList.prependedItems.append(reference)\n        return prim_spec\n\n    for index, existing_ref in enumerate(entries):\n        existing_order = existing_ref.customData.get(\"order\")\n        if existing_order is not None and existing_order &lt; order:\n            log.debug(\n                f\"Inserting new reference at {index}: {reference}\"\n            )\n            entries.insert(index, reference)\n            break\n    else:\n        prim_spec.referenceList.prependedItems.append(reference)\n        return prim_spec\n\n    prim_spec.referenceList.prependedItems[:] = entries\n    return prim_spec\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/usdlib.html#client.ayon_core.pipeline.usdlib.add_ordered_sublayer","title":"<code>add_ordered_sublayer(layer, contribution_path, layer_id, order=None, add_sdf_arguments_metadata=True)</code>","text":"<p>Add sublayer paths in the Sdf.Layer at given \"orders\"</p> <p>USD does not provide a way to set metadata per sublayer entry, but we can 'sneak it in' by adding it as part of the file url after :SDF_FORMAT_ARGS: There they will then just be unused args that we can parse later again to access our data.</p> <p>A higher order will appear earlier in the subLayerPaths as a stronger opinion. An unordered layer (<code>order=None</code>) will be stronger than any ordered opinion and thus will be inserted at the start of the list.</p> <p>Parameters:</p> Name Type Description Default <code>layer</code> <code>Layer</code> <p>Layer to add sublayers in.</p> required <code>contribution_path</code> <code>str</code> <p>Path/URI to add.</p> required <code>layer_id</code> <code>str</code> <p>Token that if found for an existing layer it will replace that layer.</p> required <code>order</code> <code>Any[int, None]</code> <p>Order to place the contribution in the sublayers. When <code>None</code> no ordering is considered nor will ordering metadata be written if <code>add_sdf_arguments_metadata</code> is False.</p> <code>None</code> <code>add_sdf_arguments_metadata</code> <code>bool</code> <p>Add metadata into the filepath to store the <code>layer_id</code> and <code>order</code> so ordering can be maintained in the future as intended.</p> <code>True</code> <p>Returns:</p> Name Type Description <code>str</code> <p>The resulting contribution path (which maybe include the sdf format args metadata if enabled)</p> Source code in <code>client/ayon_core/pipeline/usdlib.py</code> <pre><code>def add_ordered_sublayer(layer, contribution_path, layer_id, order=None,\n                         add_sdf_arguments_metadata=True):\n    \"\"\"Add sublayer paths in the Sdf.Layer at given \"orders\"\n\n    USD does not provide a way to set metadata per sublayer entry, but we can\n    'sneak it in' by adding it as part of the file url after :SDF_FORMAT_ARGS:\n    There they will then just be unused args that we can parse later again\n    to access our data.\n\n    A higher order will appear earlier in the subLayerPaths as a stronger\n    opinion. An unordered layer (`order=None`) will be stronger than any\n    ordered opinion and thus will be inserted at the start of the list.\n\n    Args:\n        layer (Sdf.Layer): Layer to add sublayers in.\n        contribution_path (str): Path/URI to add.\n        layer_id (str): Token that if found for an existing layer it will\n            replace that layer.\n        order (Any[int, None]): Order to place the contribution in\n            the sublayers. When `None` no ordering is considered nor will\n            ordering metadata be written if `add_sdf_arguments_metadata` is\n            False.\n        add_sdf_arguments_metadata (bool): Add metadata into the filepath\n            to store the `layer_id` and `order` so ordering can be maintained\n            in the future as intended.\n\n    Returns:\n        str: The resulting contribution path (which maybe include the\n            sdf format args metadata if enabled)\n\n    \"\"\"\n    # Add the order with the contribution path so that for future\n    # contributions we can again use it to magically fit into the\n    # ordering. We put this in the path because sublayer paths do\n    # not allow customData to be stored.\n    def _format_path(path, layer_id, order):\n        # TODO: Avoid this hack to store 'order' and 'layer' metadata\n        #   for sublayers; in USD sublayers can't hold customdata\n        if not add_sdf_arguments_metadata:\n            return path\n        data = {\"layer_id\": str(layer_id)}\n        if order is not None:\n            data[\"order\"] = str(order)\n        return Sdf.Layer.CreateIdentifier(path, data)\n\n    # If the layer was already in the layers, then replace it\n    for index, existing_path in enumerate(layer.subLayerPaths):\n        args = get_sdf_format_args(existing_path)\n        existing_layer_id = args.get(\"layer_id\")\n        if existing_layer_id == layer_id:\n            existing_layer = layer.subLayerPaths[index]\n            existing_order = args.get(\"order\")\n            existing_order = int(existing_order) if existing_order else None\n            if order is not None and order != existing_order:\n                # We need to move the layer, so we will remove this index\n                # and then re-insert it below at the right order\n                log.debug(f\"Removing existing layer: {existing_layer}\")\n                del layer.subLayerPaths[index]\n                break\n\n            # Put it in the same position where it was before when swapping\n            # it with the original, also take over its order metadata\n            contribution_path = _format_path(contribution_path,\n                                             order=existing_order,\n                                             layer_id=layer_id)\n            log.debug(\n                f\"Replacing existing layer: {existing_layer} \"\n                f\"-&gt; {contribution_path}\"\n            )\n            layer.subLayerPaths[index] = contribution_path\n            return contribution_path\n\n    contribution_path = _format_path(contribution_path,\n                                     order=order,\n                                     layer_id=layer_id)\n\n    # If an order is defined and other layers are ordered than place it before\n    # the first order where existing order is lower\n    if order is not None:\n        for index, existing_path in enumerate(layer.subLayerPaths):\n            args = get_sdf_format_args(existing_path)\n            existing_order = args.get(\"order\")\n            if existing_order is not None and int(existing_order) &lt; order:\n                log.debug(\n                    f\"Inserting new layer at {index}: {contribution_path}\"\n                )\n                layer.subLayerPaths.insert(index, contribution_path)\n                return\n        # Weakest ordered opinion\n        layer.subLayerPaths.append(contribution_path)\n        return contribution_path\n\n    # If no paths found with an order to put it next to\n    # then put the sublayer at the end\n    log.debug(f\"Appending new layer: {contribution_path}\")\n    layer.subLayerPaths.insert(0, contribution_path)\n    return contribution_path\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/usdlib.html#client.ayon_core.pipeline.usdlib.add_variant_references_to_layer","title":"<code>add_variant_references_to_layer(variants, variantset, default_variant=None, variant_prim='/root', reference_prim=None, set_default_variant=True, as_payload=False, skip_variant_on_single_file=False, layer=None)</code>","text":"<p>Add or set a prim's variants to reference specified paths in the layer.</p> Note <p>This does not clear any of the other opinions than replacing <code>prim.referenceList.prependedItems</code> with the new reference. If <code>as_payload=True</code> then this only does it for payloads and leaves references as they were in-tact.</p> Note <p>If <code>skip_variant_on_single_file=True</code> it does not check if any other variants do exist; it only checks whether you are currently adding more than one since it'd be hard to find out whether previously this was also skipped and should now if you're adding a new one suddenly also be its original 'variant'. As such it's recommended to keep this disabled unless you know you're not updating the file later into the same variant set.</p> <p>Examples:</p> <p>layer = add_variant_references_to_layer(\"model.usd\",     variants=[         (\"main\", \"main.usd\"),         (\"damaged\", \"damaged.usd\"),         (\"twisted\", \"twisted.usd\")     ],     variantset=\"model\") layer.Export(\"model.usd\", args={\"format\": \"usda\"})</p> <p>Parameters:</p> Name Type Description Default <code>variants</code> <code>List[List[str, str]</code> <p>List of two-tuples of variant name to the filepath that should be referenced in for that variant.</p> required <code>variantset</code> <code>str</code> <p>Name of the variant set</p> required <code>default_variant</code> <code>str</code> <p>Default variant to set. If not provided the first variant will be used.</p> <code>None</code> <code>variant_prim</code> <code>str</code> <p>Variant prim?</p> <code>'/root'</code> <code>reference_prim</code> <code>str</code> <p>Path to the reference prim where to add the references and variant sets.</p> <code>None</code> <code>set_default_variant</code> <code>bool</code> <p>Whether to set the default variant. When False no default variant will be set, even if a value was provided to <code>default_variant</code></p> <code>True</code> <code>as_payload</code> <code>bool</code> <p>When enabled, instead of referencing use payloads</p> <code>False</code> <code>skip_variant_on_single_file</code> <code>bool</code> <p>If this is enabled and only a single variant is provided then do not create the variant set but just reference that single file.</p> <code>False</code> <code>layer</code> <code>Optional[Layer]</code> <p>When provided operate on this layer, otherwise create an anonymous layer in memory.</p> <code>None</code> <p>Returns:</p> Type Description <p>Sdf.Layer: The layer with the added references inside the variants.</p> Source code in <code>client/ayon_core/pipeline/usdlib.py</code> <pre><code>def add_variant_references_to_layer(\n    variants,\n    variantset,\n    default_variant=None,\n    variant_prim=\"/root\",\n    reference_prim=None,\n    set_default_variant=True,\n    as_payload=False,\n    skip_variant_on_single_file=False,\n    layer=None\n):\n    \"\"\"Add or set a prim's variants to reference specified paths in the layer.\n\n    Note:\n        This does not clear any of the other opinions than replacing\n        `prim.referenceList.prependedItems` with the new reference.\n        If `as_payload=True` then this only does it for payloads and leaves\n        references as they were in-tact.\n\n    Note:\n        If `skip_variant_on_single_file=True` it does *not* check if any\n        other variants do exist; it only checks whether you are currently\n        adding more than one since it'd be hard to find out whether previously\n        this was also skipped and should now if you're adding a new one\n        suddenly also be its original 'variant'. As such it's recommended to\n        keep this disabled unless you know you're not updating the file later\n        into the same variant set.\n\n    Examples:\n    &gt;&gt;&gt; layer = add_variant_references_to_layer(\"model.usd\",\n    &gt;&gt;&gt;     variants=[\n    &gt;&gt;&gt;         (\"main\", \"main.usd\"),\n    &gt;&gt;&gt;         (\"damaged\", \"damaged.usd\"),\n    &gt;&gt;&gt;         (\"twisted\", \"twisted.usd\")\n    &gt;&gt;&gt;     ],\n    &gt;&gt;&gt;     variantset=\"model\")\n    &gt;&gt;&gt; layer.Export(\"model.usd\", args={\"format\": \"usda\"})\n\n    Arguments:\n        variants (List[List[str, str]): List of two-tuples of variant name to\n            the filepath that should be referenced in for that variant.\n        variantset (str): Name of the variant set\n        default_variant (str): Default variant to set. If not provided\n            the first variant will be used.\n        variant_prim (str): Variant prim?\n        reference_prim (str): Path to the reference prim where to add the\n            references and variant sets.\n        set_default_variant (bool): Whether to set the default variant.\n            When False no default variant will be set, even if a value\n            was provided to `default_variant`\n        as_payload (bool): When enabled, instead of referencing use payloads\n        skip_variant_on_single_file (bool): If this is enabled and only\n            a single variant is provided then do not create the variant set\n            but just reference that single file.\n        layer (Optional[Sdf.Layer]): When provided operate on this layer,\n            otherwise create an anonymous layer in memory.\n\n    Returns:\n        Sdf.Layer: The layer with the added references inside the variants.\n\n    \"\"\"\n    if layer is None:\n        layer = Sdf.Layer.CreateAnonymous()\n        set_layer_defaults(layer, default_prim=variant_prim.strip(\"/\"))\n\n    prim_path_to_get_variants = Sdf.Path(variant_prim)\n    root_prim = get_or_define_prim_spec(layer, variant_prim, \"Xform\")\n\n    # TODO: Define why there's a need for separate variant_prim and\n    #   reference_prim attribute. When should they differ? Does it even work?\n    if not reference_prim:\n        reference_prim = root_prim\n    else:\n        reference_prim = get_or_define_prim_spec(layer, reference_prim,\n                                                 \"Xform\")\n\n    assert variants, \"Must have variants, got: %s\" % variants\n\n    if skip_variant_on_single_file and len(variants) == 1:\n        # Reference directly, no variants\n        variant_path = variants[0][1]\n        if as_payload:\n            # Payload\n            reference_prim.payloadList.prependedItems.append(\n                Sdf.Payload(variant_path)\n            )\n        else:\n            # Reference\n            reference_prim.referenceList.prependedItems.append(\n                Sdf.Reference(variant_path)\n            )\n\n        log.debug(\"Creating without variants due to single file only.\")\n        log.debug(\"Path: %s\", variant_path)\n\n    else:\n        # Variants\n        for variant, variant_filepath in variants:\n            if default_variant is None:\n                default_variant = variant\n\n            set_variant_reference(layer,\n                                  prim_path=prim_path_to_get_variants,\n                                  variant_selections=[[variantset, variant]],\n                                  path=variant_filepath,\n                                  as_payload=as_payload)\n\n        if set_default_variant and default_variant is not None:\n            # Set default variant selection\n            root_prim.variantSelections[variantset] = default_variant\n\n    return layer\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/usdlib.html#client.ayon_core.pipeline.usdlib.create_asset","title":"<code>create_asset(filepath, asset_name, reference_layers=None, kind=Kind.Tokens.component, define_class=True)</code>","text":"<p>Creates and saves a prepared asset stage layer.</p> <p>Creates an asset file that consists of a top level asset prim, asset info  and references in the provided <code>reference_layers</code>.</p> <p>Returns:</p> Name Type Description <code>list</code> <p>Created layers</p> Source code in <code>client/ayon_core/pipeline/usdlib.py</code> <pre><code>def create_asset(\n        filepath,\n        asset_name,\n        reference_layers=None,\n        kind=Kind.Tokens.component,\n        define_class=True\n):\n    \"\"\"Creates and saves a prepared asset stage layer.\n\n    Creates an asset file that consists of a top level asset prim, asset info\n     and references in the provided `reference_layers`.\n\n    Returns:\n        list: Created layers\n\n    \"\"\"\n    # Also see create_asset.py in PixarAnimationStudios/USD endToEnd example\n\n    sdf_layer = Sdf.Layer.CreateAnonymous()\n    layer = Layer(layer=sdf_layer, path=filepath)\n\n    created_layers = setup_asset_layer(\n        layer=sdf_layer,\n        asset_name=asset_name,\n        reference_layers=reference_layers,\n        kind=kind,\n        define_class=define_class,\n        set_payload_path=True\n    )\n    for created_layer in created_layers:\n        created_layer.anchor = layer\n        created_layer.export()\n\n    # Make the layer ascii - good for readability, plus the file is small\n    log.debug(\"Creating asset at %s\", filepath)\n    layer.export(args={\"format\": \"usda\"})\n\n    return [layer] + created_layers\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/usdlib.html#client.ayon_core.pipeline.usdlib.create_shot","title":"<code>create_shot(filepath, layers, create_layers=False)</code>","text":"<p>Create a shot with separate layers for departments.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; create_shot(\"/path/to/shot.usd\",\n&gt;&gt;&gt;             layers=[\"lighting.usd\", \"fx.usd\", \"animation.usd\"])\n\"/path/to/shot.usd\"\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>filepath</code> <code>str</code> <p>Filepath where the asset.usd file will be saved.</p> required <code>layers</code> <code>list</code> <p>When provided this will be added verbatim in the subLayerPaths layers. When the provided layer paths do not exist they are generated using Sdf.Layer.CreateNew</p> required <code>create_layers</code> <code>bool</code> <p>Whether to create the stub layers on disk if they do not exist yet.</p> <code>False</code> <p>Returns:</p> Name Type Description <code>str</code> <p>The saved shot file path</p> Source code in <code>client/ayon_core/pipeline/usdlib.py</code> <pre><code>def create_shot(filepath, layers, create_layers=False):\n    \"\"\"Create a shot with separate layers for departments.\n\n    Examples:\n        &gt;&gt;&gt; create_shot(\"/path/to/shot.usd\",\n        &gt;&gt;&gt;             layers=[\"lighting.usd\", \"fx.usd\", \"animation.usd\"])\n        \"/path/to/shot.usd\"\n\n    Args:\n        filepath (str): Filepath where the asset.usd file will be saved.\n        layers (list): When provided this will be added verbatim in the\n            subLayerPaths layers. When the provided layer paths do not exist\n            they are generated using Sdf.Layer.CreateNew\n        create_layers (bool): Whether to create the stub layers on disk if\n            they do not exist yet.\n\n    Returns:\n        str: The saved shot file path\n\n    \"\"\"\n    # Also see create_shot.py in PixarAnimationStudios/USD endToEnd example\n    root_layer = Sdf.Layer.CreateAnonymous()\n\n    created_layers = [root_layer]\n    for layer_path in layers:\n        if create_layers and not os.path.exists(layer_path):\n            # We use the Sdf API here to quickly create layers.  Also, we're\n            # using it as a way to author the subLayerPaths as there is no\n            # way to do that directly in the Usd API.\n            layer_folder = os.path.dirname(layer_path)\n            if not os.path.exists(layer_folder):\n                os.makedirs(layer_folder)\n\n            new_layer = Sdf.Layer.CreateNew(layer_path)\n            created_layers.append(new_layer)\n\n        root_layer.subLayerPaths.append(layer_path)\n\n    set_layer_defaults(root_layer)\n    log.debug(\"Creating shot at %s\" % filepath)\n    root_layer.Export(filepath, args={\"format\": \"usda\"})\n\n    return created_layers\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/usdlib.html#client.ayon_core.pipeline.usdlib.get_or_define_prim_spec","title":"<code>get_or_define_prim_spec(layer, prim_path, type_name)</code>","text":"<p>Get or create a PrimSpec in the layer.</p> Note <p>This creates a Sdf.PrimSpec with Sdf.SpecifierDef but if the PrimSpec already exists this will not force it to be a Sdf.SpecifierDef and it may remain what it was, e.g. Sdf.SpecifierOver</p> <p>Parameters:</p> Name Type Description Default <code>layer</code> <code>Layer</code> <p>The layer to create it in.</p> required <code>prim_path</code> <code>Any[str, Path]</code> <p>Prim path to create.</p> required <code>type_name</code> <code>str</code> <p>Type name for the PrimSpec. This will only be set if the prim does not exist in the layer yet. It does not update type for an existing prim.</p> required <p>Returns:</p> Type Description <p>Sdf.PrimSpec: The PrimSpec in the layer for the given prim path.</p> Source code in <code>client/ayon_core/pipeline/usdlib.py</code> <pre><code>def get_or_define_prim_spec(layer, prim_path, type_name):\n    \"\"\"Get or create a PrimSpec in the layer.\n\n    Note:\n        This creates a Sdf.PrimSpec with Sdf.SpecifierDef but if the PrimSpec\n        already exists this will not force it to be a Sdf.SpecifierDef and\n        it may remain what it was, e.g. Sdf.SpecifierOver\n\n    Args:\n        layer (Sdf.Layer): The layer to create it in.\n        prim_path (Any[str, Sdf.Path]): Prim path to create.\n        type_name (str): Type name for the PrimSpec.\n            This will only be set if the prim does not exist in the layer\n            yet. It does not update type for an existing prim.\n\n    Returns:\n        Sdf.PrimSpec: The PrimSpec in the layer for the given prim path.\n\n    \"\"\"\n    prim_spec = layer.GetPrimAtPath(prim_path)\n    if prim_spec:\n        return prim_spec\n\n    prim_spec = Sdf.CreatePrimInLayer(layer, prim_path)\n    prim_spec.specifier = Sdf.SpecifierDef\n    prim_spec.typeName = type_name\n    return prim_spec\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/usdlib.html#client.ayon_core.pipeline.usdlib.get_sdf_format_args","title":"<code>get_sdf_format_args(path)</code>","text":"<p>Return SDF_FORMAT_ARGS parsed to <code>dict</code></p> Source code in <code>client/ayon_core/pipeline/usdlib.py</code> <pre><code>def get_sdf_format_args(path):\n    \"\"\"Return SDF_FORMAT_ARGS parsed to `dict`\"\"\"\n    _raw_path, data = Sdf.Layer.SplitIdentifier(path)\n    return data\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/usdlib.html#client.ayon_core.pipeline.usdlib.get_standard_default_prim_name","title":"<code>get_standard_default_prim_name(folder_path)</code>","text":"<p>Return the AYON-specified default prim name for a folder path.</p> <p>This is used e.g. for the default prim in AYON USD Contribution workflows.</p> Source code in <code>client/ayon_core/pipeline/usdlib.py</code> <pre><code>def get_standard_default_prim_name(folder_path: str) -&gt; str:\n    \"\"\"Return the AYON-specified default prim name for a folder path.\n\n    This is used e.g. for the default prim in AYON USD Contribution workflows.\n    \"\"\"\n    folder_name: str = folder_path.rsplit(\"/\", 1)[-1]\n\n    # Prim names are not allowed to start with a digit in USD. Authoring them\n    # would mean generating essentially garbage data and may result in\n    # unexpected behavior in certain USD or DCC versions, like failure to\n    # refresh in usdview or crashes in Houdini 21.\n    if folder_name and folder_name[0].isdigit():\n        folder_name = f\"_{folder_name}\"\n\n    return folder_name\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/usdlib.html#client.ayon_core.pipeline.usdlib.set_layer_defaults","title":"<code>set_layer_defaults(layer, up_axis=UsdGeom.Tokens.y, meters_per_unit=1.0, default_prim=None)</code>","text":"<p>Set some default metadata for the SdfLayer.</p> <p>Parameters:</p> Name Type Description Default <code>layer</code> <code>Layer</code> <p>The layer to set default for via Sdf API.</p> required <code>meters_per_unit</code> <code>float</code> <p>Meters per unit</p> <code>1.0</code> <code>default_prim (Optional[str]</code> <p>Default prim name</p> required Source code in <code>client/ayon_core/pipeline/usdlib.py</code> <pre><code>def set_layer_defaults(layer,\n                       up_axis=UsdGeom.Tokens.y,\n                       meters_per_unit=1.0,\n                       default_prim=None):\n    \"\"\"Set some default metadata for the SdfLayer.\n\n    Arguments:\n        layer (Sdf.Layer): The layer to set default for via Sdf API.\n        up_axis (UsdGeom.Token); Which axis is the up-axis\n        meters_per_unit (float): Meters per unit\n        default_prim (Optional[str]: Default prim name\n\n    \"\"\"\n    # Set default prim\n    if default_prim is not None:\n        layer.defaultPrim = default_prim\n\n    # Let viewing applications know how to orient a free camera properly\n    # Similar to: UsdGeom.SetStageUpAxis(stage, UsdGeom.Tokens.y)\n    layer.pseudoRoot.SetInfo(UsdGeom.Tokens.upAxis, up_axis)\n\n    # Set meters per unit\n    layer.pseudoRoot.SetInfo(UsdGeom.Tokens.metersPerUnit,\n                             float(meters_per_unit))\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/usdlib.html#client.ayon_core.pipeline.usdlib.set_variant_reference","title":"<code>set_variant_reference(sdf_layer, prim_path, variant_selections, path, as_payload=False, append=True)</code>","text":"<p>Get or define variant selection at prim path and add a reference</p> <p>If the Variant Prim already exists the prepended references are replaced with a reference to <code>path</code>, it is overridden.</p> <p>Parameters:</p> Name Type Description Default <code>sdf_layer</code> <code>Layer</code> <p>Layer to operate in.</p> required <code>prim_path</code> <code>Any[str, Path]</code> <p>Prim path to add variant to.</p> required <code>variant_selections</code> <code>List[List[str, str]]</code> <p>A list of variant set names and variant names to get the prim spec in.</p> required <code>path</code> <code>str</code> <p>Path to reference or payload</p> required <code>as_payload</code> <code>bool</code> <p>When enabled it will generate a payload instead of a reference. Defaults to False.</p> <code>False</code> <code>append</code> <code>bool</code> <p>When enabled it will append the reference of payload to prepended items, otherwise it will replace it.</p> <code>True</code> <p>Returns:</p> Type Description <p>Sdf.PrimSpec: The prim spec for the prim path at the given variant selection.</p> Source code in <code>client/ayon_core/pipeline/usdlib.py</code> <pre><code>def set_variant_reference(\n        sdf_layer,\n        prim_path,\n        variant_selections,\n        path,\n        as_payload=False,\n        append=True\n):\n    \"\"\"Get or define variant selection at prim path and add a reference\n\n    If the Variant Prim already exists the prepended references are replaced\n    with a reference to `path`, it is overridden.\n\n    Args:\n        sdf_layer (Sdf.Layer): Layer to operate in.\n        prim_path (Any[str, Sdf.Path]): Prim path to add variant to.\n        variant_selections (List[List[str, str]]): A list of variant set names\n            and variant names to get the prim spec in.\n        path (str): Path to reference or payload\n        as_payload (bool): When enabled it will generate a payload instead of\n            a reference. Defaults to False.\n        append (bool): When enabled it will append the reference of payload\n            to prepended items, otherwise it will replace it.\n\n    Returns:\n        Sdf.PrimSpec: The prim spec for the prim path at the given\n            variant selection.\n\n    \"\"\"\n    prim_path = Sdf.Path(prim_path)\n    # TODO: inherit type from outside of variants if it has it\n    get_or_define_prim_spec(sdf_layer, prim_path, \"Xform\")\n    variant_prim_path = variant_nested_prim_path(prim_path, variant_selections)\n    variant_prim = get_or_define_prim_spec(sdf_layer,\n                                           variant_prim_path,\n                                           \"Xform\")\n    # Replace the prepended references or payloads\n    if as_payload:\n        # Payload\n        if append:\n            variant_prim.payloadList.prependedItems.append(\n                Sdf.Payload(assetPath=path)\n            )\n        else:\n            variant_prim.payloadList.prependedItems[:] = [\n                Sdf.Payload(assetPath=path)\n            ]\n    else:\n        # Reference\n        if append:\n            variant_prim.referenceList.prependedItems.append(\n                Sdf.Reference(assetPath=path)\n            )\n        else:\n            variant_prim.referenceList.prependedItems[:] = [\n                Sdf.Reference(assetPath=path)\n            ]\n\n    return variant_prim\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/usdlib.html#client.ayon_core.pipeline.usdlib.setup_asset_layer","title":"<code>setup_asset_layer(layer, asset_name, reference_layers=None, kind=Kind.Tokens.component, define_class=True, force_add_payload=False, set_payload_path=False)</code>","text":"<p>Adds an asset prim to the layer with the <code>reference_layers</code> added as references for e.g. geometry and shading.</p> <p>The referenced layers will be moved into a separate <code>./payload.usd</code> file that the asset file uses to allow deferred loading of the heavier geometrical data. An example would be:</p> <p>asset.usd      &lt;-- out filepath   payload.usd  &lt;-- always automatically added in-between     look.usd   &lt;-- reference layer 0 from <code>reference_layers</code> argument     model.usd  &lt;-- reference layer 1 from <code>reference_layers</code> argument</p> <p>If <code>define_class</code> is enabled then a <code>/__class__/{asset_name}</code> class definition will be created that the root asset inherits from</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; create_asset(\"/path/to/asset.usd\",\n&gt;&gt;&gt;              asset_name=\"test\",\n&gt;&gt;&gt;              reference_layers=[\"./model.usd\", \"./look.usd\"])\n</code></pre> <p>Returns:</p> Type Description <p>List[Tuple[Sdf.Layer, str]]: List of created layers with their preferred output save paths.</p> <p>Parameters:</p> Name Type Description Default <code>layer</code> <code>Layer</code> <p>Layer to set up the asset structure for.</p> required <code>asset_name</code> <code>str</code> <p>The name for the Asset identifier and default prim.</p> required <code>reference_layers</code> <code>list</code> <p>USD Files to reference in the asset. Note that the bottom layer (first file, like a model) would be last in the list. The strongest layer will be the first index.</p> <code>None</code> <code>kind</code> <code>Kind</code> <p>A USD Kind for the root asset.</p> <code>component</code> <code>define_class</code> <p>Define a <code>/__class__/{asset_name}</code> class which the root asset prim will inherit from.</p> <code>True</code> <code>force_add_payload</code> <code>bool</code> <p>Generate payload layer even if no reference paths are set - thus generating an enmpty layer.</p> <code>False</code> <code>set_payload_path</code> <code>bool</code> <p>Whether to directly set the payload asset path to <code>./payload.usd</code> or not Defaults to True.</p> <code>False</code> Source code in <code>client/ayon_core/pipeline/usdlib.py</code> <pre><code>def setup_asset_layer(\n        layer,\n        asset_name,\n        reference_layers=None,\n        kind=Kind.Tokens.component,\n        define_class=True,\n        force_add_payload=False,\n        set_payload_path=False\n):\n    \"\"\"\n    Adds an asset prim to the layer with the `reference_layers` added as\n    references for e.g. geometry and shading.\n\n    The referenced layers will be moved into a separate `./payload.usd` file\n    that the asset file uses to allow deferred loading of the heavier\n    geometrical data. An example would be:\n\n    asset.usd      &lt;-- out filepath\n      payload.usd  &lt;-- always automatically added in-between\n        look.usd   &lt;-- reference layer 0 from `reference_layers` argument\n        model.usd  &lt;-- reference layer 1 from `reference_layers` argument\n\n    If `define_class` is enabled then a `/__class__/{asset_name}` class\n    definition will be created that the root asset inherits from\n\n    Examples:\n        &gt;&gt;&gt; create_asset(\"/path/to/asset.usd\",\n        &gt;&gt;&gt;              asset_name=\"test\",\n        &gt;&gt;&gt;              reference_layers=[\"./model.usd\", \"./look.usd\"])\n\n    Returns:\n        List[Tuple[Sdf.Layer, str]]: List of created layers with their\n            preferred output save paths.\n\n    Args:\n        layer (Sdf.Layer): Layer to set up the asset structure for.\n        asset_name (str): The name for the Asset identifier and default prim.\n        reference_layers (list): USD Files to reference in the asset.\n            Note that the bottom layer (first file, like a model) would\n            be last in the list. The strongest layer will be the first\n            index.\n        kind (pxr.Kind): A USD Kind for the root asset.\n        define_class: Define a `/__class__/{asset_name}` class which the\n            root asset prim will inherit from.\n        force_add_payload (bool): Generate payload layer even if no\n            reference paths are set - thus generating an enmpty layer.\n        set_payload_path (bool): Whether to directly set the payload asset\n            path to `./payload.usd` or not Defaults to True.\n\n    \"\"\"\n    # Define root prim for the asset and make it the default for the stage.\n    prim_name = asset_name\n\n    if define_class:\n        class_prim = Sdf.PrimSpec(\n            layer.pseudoRoot,\n            \"__class__\",\n            Sdf.SpecifierClass,\n        )\n        Sdf.PrimSpec(\n            class_prim,\n            prim_name,\n            Sdf.SpecifierClass,\n        )\n\n    asset_prim = Sdf.PrimSpec(\n        layer.pseudoRoot,\n        prim_name,\n        Sdf.SpecifierDef,\n        \"Xform\"\n    )\n\n    if define_class:\n        asset_prim.inheritPathList.prependedItems[:] = [\n            \"/__class__/{}\".format(prim_name)\n        ]\n\n    # Define Kind\n    # Usually we will \"loft up\" the kind authored into the exported geometry\n    # layer rather than re-stamping here; we'll leave that for a later\n    # tutorial, and just be explicit here.\n    asset_prim.kind = kind\n\n    # Set asset info\n    asset_prim.assetInfo[\"name\"] = asset_name\n    asset_prim.assetInfo[\"identifier\"] = \"%s/%s.usd\" % (asset_name, asset_name)\n\n    # asset.assetInfo[\"version\"] = asset_version\n    set_layer_defaults(layer, default_prim=asset_name)\n\n    created_layers = []\n\n    # Add references to the  asset prim\n    if force_add_payload or reference_layers:\n        # Create a relative payload file to filepath through which we sublayer\n        # the heavier payloads\n        # Prefix with `LOP` just so so that if Houdini ROP were to save\n        # the nodes it's capable of exporting with explicit save path\n        payload_layer = Sdf.Layer.CreateAnonymous(\"LOP\",\n                                                  args={\"format\": \"usda\"})\n        set_layer_defaults(payload_layer, default_prim=asset_name)\n        created_layers.append(Layer(layer=payload_layer,\n                                    path=\"./payload.usd\"))\n\n        # Add payload\n        if set_payload_path:\n            payload_identifier = \"./payload.usd\"\n        else:\n            payload_identifier = payload_layer.identifier\n\n        asset_prim.payloadList.prependedItems[:] = [\n            Sdf.Payload(assetPath=payload_identifier)\n        ]\n\n        # Add sublayers to the payload layer\n        # Note: Sublayering is tricky because it requires that the sublayers\n        #   actually define the path at defaultPrim otherwise the payload\n        #   reference will not find the defaultPrim and turn up empty.\n        if reference_layers:\n            for ref_layer in reference_layers:\n                payload_layer.subLayerPaths.append(ref_layer)\n\n    return created_layers\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/usdlib.html#client.ayon_core.pipeline.usdlib.variant_nested_prim_path","title":"<code>variant_nested_prim_path(prim_path, variant_selections)</code>","text":"<p>Return the Sdf.Path for a nested variant selection at prim path.</p> <p>Examples:</p> <p>prim_path = Sdf.Path(\"/asset\") variant_spec = variant_nested_prim_path(     prim_path,     variant_selections=[[\"model\", \"main\"], [\"look\", \"main\"]] ) variant_spec.path</p> <p>Parameters:</p> Name Type Description Default <code>prim_path</code> <code>PrimPath</code> <p>The prim path to create the spec in</p> required <code>variant_selections</code> <code>List[List[str, str]]</code> <p>A list of variant set names and variant names to get the prim spec in.</p> required <p>Returns:</p> Type Description <p>Sdf.Path: The variant prim path</p> Source code in <code>client/ayon_core/pipeline/usdlib.py</code> <pre><code>def variant_nested_prim_path(prim_path, variant_selections):\n    \"\"\"Return the Sdf.Path for a nested variant selection at prim path.\n\n    Examples:\n    &gt;&gt;&gt; prim_path = Sdf.Path(\"/asset\")\n    &gt;&gt;&gt; variant_spec = variant_nested_prim_path(\n    &gt;&gt;&gt;     prim_path,\n    &gt;&gt;&gt;     variant_selections=[[\"model\", \"main\"], [\"look\", \"main\"]]\n    &gt;&gt;&gt; )\n    &gt;&gt;&gt; variant_spec.path\n\n    Args:\n        prim_path (Sdf.PrimPath): The prim path to create the spec in\n        variant_selections (List[List[str, str]]): A list of variant set names\n            and variant names to get the prim spec in.\n\n    Returns:\n        Sdf.Path: The variant prim path\n\n    \"\"\"\n    variant_prim_path = Sdf.Path(prim_path)\n    for variant_set_name, variant_name in variant_selections:\n        variant_prim_path = variant_prim_path.AppendVariantSelection(\n            variant_set_name, variant_name)\n    return variant_prim_path\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/version_start.html","title":"version_start","text":""},{"location":"autoapi/client/ayon_core/pipeline/version_start.html#client.ayon_core.pipeline.version_start.get_versioning_start","title":"<code>get_versioning_start(project_name, host_name, task_name=None, task_type=None, product_base_type=None, product_name=None, *, project_settings=None)</code>","text":"<p>Get anatomy versioning start</p> Source code in <code>client/ayon_core/pipeline/version_start.py</code> <pre><code>@_get_versioning_start_wrap\ndef get_versioning_start(\n    project_name: str,\n    host_name: str,\n    task_name: Optional[str] = None,\n    task_type: Optional[str] = None,\n    product_base_type: Optional[str] = None,\n    product_name: Optional[str] = None,\n    *,\n    project_settings: Optional[dict[str, Any]] = None,\n) -&gt; int:\n    \"\"\"Get anatomy versioning start\"\"\"\n    if not project_settings:\n        project_settings = get_project_settings(project_name)\n\n    version_start = 1\n    settings = project_settings[\"core\"]\n    profiles = settings[\"version_start_category\"][\"profiles\"]\n\n    if not profiles:\n        return version_start\n\n    filtering_criteria = {\n        \"host_names\": host_name,\n        \"product_base_types\": product_base_type,\n        \"product_names\": product_name,\n        \"task_names\": task_name,\n        \"task_types\": task_type,\n    }\n    profile = filter_profiles(profiles, filtering_criteria)\n\n    if profile is None:\n        return version_start\n\n    return profile[\"version_start\"]\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/index.html","title":"actions","text":""},{"location":"autoapi/client/ayon_core/pipeline/actions/index.html#client.ayon_core.pipeline.actions.ActionForm","title":"<code>ActionForm</code>  <code>dataclass</code>","text":"<p>Form for loader action.</p> <p>If an action needs to collect information from a user before or during of     the action execution, it can return a response with a form. When the     form is submitted, a new execution of the action is triggered.</p> <p>It is also possible to just show a label message without the submit     button to make sure the user has seen the message.</p> <p>Attributes:</p> Name Type Description <code>title</code> <code>str</code> <p>Title of the form -&gt; title of the window.</p> <code>fields</code> <code>list[AbstractAttrDef]</code> <p>Fields of the form.</p> <code>submit_label</code> <code>Optional[str]</code> <p>Label of the submit button. Is hidden if is set to None.</p> <code>submit_icon</code> <code>Optional[dict[str, Any]]</code> <p>Icon definition of the submit button.</p> <code>cancel_label</code> <code>Optional[str]</code> <p>Label of the cancel button. Is hidden if is set to None. User can still close the window tho.</p> <code>cancel_icon</code> <code>Optional[dict[str, Any]]</code> <p>Icon definition of the cancel button.</p> Source code in <code>client/ayon_core/pipeline/actions/structures.py</code> <pre><code>@dataclass\nclass ActionForm:\n    \"\"\"Form for loader action.\n\n    If an action needs to collect information from a user before or during of\n        the action execution, it can return a response with a form. When the\n        form is submitted, a new execution of the action is triggered.\n\n    It is also possible to just show a label message without the submit\n        button to make sure the user has seen the message.\n\n    Attributes:\n        title (str): Title of the form -&gt; title of the window.\n        fields (list[AbstractAttrDef]): Fields of the form.\n        submit_label (Optional[str]): Label of the submit button. Is hidden\n            if is set to None.\n        submit_icon (Optional[dict[str, Any]]): Icon definition of the submit\n            button.\n        cancel_label (Optional[str]): Label of the cancel button. Is hidden\n            if is set to None. User can still close the window tho.\n        cancel_icon (Optional[dict[str, Any]]): Icon definition of the cancel\n            button.\n\n    \"\"\"\n    title: str\n    fields: list[AbstractAttrDef]\n    submit_label: Optional[str] = \"Submit\"\n    submit_icon: IconBase | dict[str, Any] | None = None\n    cancel_label: Optional[str] = \"Cancel\"\n    cancel_icon: IconBase | dict[str, Any] | None = None\n\n    def to_json_data(self) -&gt; dict[str, Any]:\n        fields = self.fields\n        if fields is not None:\n            fields = serialize_attr_defs(fields)\n        return {\n            \"title\": self.title,\n            \"fields\": fields,\n            \"submit_label\": self.submit_label,\n            \"submit_icon\": self.submit_icon,\n            \"cancel_label\": self.cancel_label,\n            \"cancel_icon\": self.cancel_icon,\n        }\n\n    @classmethod\n    def from_json_data(cls, data: dict[str, Any]) -&gt; \"ActionForm\":\n        fields = data[\"fields\"]\n        if fields is not None:\n            data[\"fields\"] = deserialize_attr_defs(fields)\n        return cls(**data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/index.html#client.ayon_core.pipeline.actions.InventoryAction","title":"<code>InventoryAction</code>","text":"<p>A custom action for the scene inventory tool</p> <p>If registered the action will be visible in the Right Mouse Button menu under the submenu \"Actions\".</p> Source code in <code>client/ayon_core/pipeline/actions/inventory.py</code> <pre><code>class InventoryAction:\n    \"\"\"A custom action for the scene inventory tool\n\n    If registered the action will be visible in the Right Mouse Button menu\n    under the submenu \"Actions\".\n\n    \"\"\"\n\n    label = None\n    icon = None\n    color = None\n    order = 0\n\n    log = logging.getLogger(\"InventoryAction\")\n    log.propagate = True\n\n    @staticmethod\n    def is_compatible(container):\n        \"\"\"Override function in a custom class\n\n        This method is specifically used to ensure the action can operate on\n        the container.\n\n        Args:\n            container(dict): the data of a loaded asset, see host.ls()\n\n        Returns:\n            bool\n        \"\"\"\n        return bool(container.get(\"objectName\"))\n\n    def process(self, containers):\n        \"\"\"Override function in a custom class\n\n        This method will receive all containers even those which are\n        incompatible. It is advised to create a small filter along the lines\n        of this example:\n\n        valid_containers = filter(self.is_compatible(c) for c in containers)\n\n        The return value will need to be a True-ish value to trigger\n        the data_changed signal in order to refresh the view.\n\n        You can return a list of container names to trigger GUI to select\n        treeview items.\n\n        You can return a dict to carry extra GUI options. For example:\n            {\n                \"objectNames\": [container names...],\n                \"options\": {\"mode\": \"toggle\",\n                            \"clear\": False}\n            }\n        Currently workable GUI options are:\n            - clear (bool): Clear current selection before selecting by action.\n                            Default `True`.\n            - mode (str): selection mode, use one of these:\n                          \"select\", \"deselect\", \"toggle\". Default is \"select\".\n\n        Args:\n            containers (list): list of dictionaries\n\n        Return:\n            bool, list or dict\n\n        \"\"\"\n        return True\n\n    @classmethod\n    def filepath_from_context(cls, context):\n        return get_representation_path_from_context(context)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/index.html#client.ayon_core.pipeline.actions.InventoryAction.is_compatible","title":"<code>is_compatible(container)</code>  <code>staticmethod</code>","text":"<p>Override function in a custom class</p> <p>This method is specifically used to ensure the action can operate on the container.</p> <p>Parameters:</p> Name Type Description Default <code>container</code> <code>dict</code> <p>the data of a loaded asset, see host.ls()</p> required <p>Returns:</p> Type Description <p>bool</p> Source code in <code>client/ayon_core/pipeline/actions/inventory.py</code> <pre><code>@staticmethod\ndef is_compatible(container):\n    \"\"\"Override function in a custom class\n\n    This method is specifically used to ensure the action can operate on\n    the container.\n\n    Args:\n        container(dict): the data of a loaded asset, see host.ls()\n\n    Returns:\n        bool\n    \"\"\"\n    return bool(container.get(\"objectName\"))\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/index.html#client.ayon_core.pipeline.actions.InventoryAction.process","title":"<code>process(containers)</code>","text":"<p>Override function in a custom class</p> <p>This method will receive all containers even those which are incompatible. It is advised to create a small filter along the lines of this example:</p> <p>valid_containers = filter(self.is_compatible(c) for c in containers)</p> <p>The return value will need to be a True-ish value to trigger the data_changed signal in order to refresh the view.</p> <p>You can return a list of container names to trigger GUI to select treeview items.</p> <p>You can return a dict to carry extra GUI options. For example:     {         \"objectNames\": [container names...],         \"options\": {\"mode\": \"toggle\",                     \"clear\": False}     } Currently workable GUI options are:     - clear (bool): Clear current selection before selecting by action.                     Default <code>True</code>.     - mode (str): selection mode, use one of these:                   \"select\", \"deselect\", \"toggle\". Default is \"select\".</p> <p>Parameters:</p> Name Type Description Default <code>containers</code> <code>list</code> <p>list of dictionaries</p> required Return <p>bool, list or dict</p> Source code in <code>client/ayon_core/pipeline/actions/inventory.py</code> <pre><code>def process(self, containers):\n    \"\"\"Override function in a custom class\n\n    This method will receive all containers even those which are\n    incompatible. It is advised to create a small filter along the lines\n    of this example:\n\n    valid_containers = filter(self.is_compatible(c) for c in containers)\n\n    The return value will need to be a True-ish value to trigger\n    the data_changed signal in order to refresh the view.\n\n    You can return a list of container names to trigger GUI to select\n    treeview items.\n\n    You can return a dict to carry extra GUI options. For example:\n        {\n            \"objectNames\": [container names...],\n            \"options\": {\"mode\": \"toggle\",\n                        \"clear\": False}\n        }\n    Currently workable GUI options are:\n        - clear (bool): Clear current selection before selecting by action.\n                        Default `True`.\n        - mode (str): selection mode, use one of these:\n                      \"select\", \"deselect\", \"toggle\". Default is \"select\".\n\n    Args:\n        containers (list): list of dictionaries\n\n    Return:\n        bool, list or dict\n\n    \"\"\"\n    return True\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/index.html#client.ayon_core.pipeline.actions.LauncherAction","title":"<code>LauncherAction</code>","text":"<p>               Bases: <code>object</code></p> <p>A custom action available</p> Source code in <code>client/ayon_core/pipeline/actions/launcher.py</code> <pre><code>class LauncherAction(object):\n    \"\"\"A custom action available\"\"\"\n    name = None\n    label = None\n    icon = None\n    color = None\n    order = 0\n\n    log = logging.getLogger(\"LauncherAction\")\n    log.propagate = True\n\n    def is_compatible(self, selection):\n        \"\"\"Return whether the class is compatible with the Session.\n\n        Args:\n            selection (LauncherActionSelection): Data with selection.\n\n        \"\"\"\n        return True\n\n    def process(self, selection, **kwargs):\n        \"\"\"Process the action.\n\n        Args:\n            selection (LauncherActionSelection): Data with selection.\n            **kwargs: Additional arguments.\n\n        \"\"\"\n        pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/index.html#client.ayon_core.pipeline.actions.LauncherAction.is_compatible","title":"<code>is_compatible(selection)</code>","text":"<p>Return whether the class is compatible with the Session.</p> <p>Parameters:</p> Name Type Description Default <code>selection</code> <code>LauncherActionSelection</code> <p>Data with selection.</p> required Source code in <code>client/ayon_core/pipeline/actions/launcher.py</code> <pre><code>def is_compatible(self, selection):\n    \"\"\"Return whether the class is compatible with the Session.\n\n    Args:\n        selection (LauncherActionSelection): Data with selection.\n\n    \"\"\"\n    return True\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/index.html#client.ayon_core.pipeline.actions.LauncherAction.process","title":"<code>process(selection, **kwargs)</code>","text":"<p>Process the action.</p> <p>Parameters:</p> Name Type Description Default <code>selection</code> <code>LauncherActionSelection</code> <p>Data with selection.</p> required <code>**kwargs</code> <p>Additional arguments.</p> <code>{}</code> Source code in <code>client/ayon_core/pipeline/actions/launcher.py</code> <pre><code>def process(self, selection, **kwargs):\n    \"\"\"Process the action.\n\n    Args:\n        selection (LauncherActionSelection): Data with selection.\n        **kwargs: Additional arguments.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/index.html#client.ayon_core.pipeline.actions.LauncherActionSelection","title":"<code>LauncherActionSelection</code>","text":"<p>Object helper to pass selection to actions.</p> <p>Object support backwards compatibility for 'session' from OpenPype where environment variable keys were used to define selection.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Selected project name.</p> required <code>folder_id</code> <code>str</code> <p>Selected folder id.</p> required <code>task_id</code> <code>str</code> <p>Selected task id.</p> required <code>folder_path</code> <code>Optional[str]</code> <p>Selected folder path.</p> <code>None</code> <code>task_name</code> <code>Optional[str]</code> <p>Selected task name.</p> <code>None</code> <code>project_entity</code> <code>Optional[dict[str, Any]]</code> <p>Project entity.</p> <code>None</code> <code>folder_entity</code> <code>Optional[dict[str, Any]]</code> <p>Folder entity.</p> <code>None</code> <code>task_entity</code> <code>Optional[dict[str, Any]]</code> <p>Task entity.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/actions/launcher.py</code> <pre><code>class LauncherActionSelection:\n    \"\"\"Object helper to pass selection to actions.\n\n    Object support backwards compatibility for 'session' from OpenPype where\n    environment variable keys were used to define selection.\n\n    Args:\n        project_name (str): Selected project name.\n        folder_id (str): Selected folder id.\n        task_id (str): Selected task id.\n        folder_path (Optional[str]): Selected folder path.\n        task_name (Optional[str]): Selected task name.\n        project_entity (Optional[dict[str, Any]]): Project entity.\n        folder_entity (Optional[dict[str, Any]]): Folder entity.\n        task_entity (Optional[dict[str, Any]]): Task entity.\n\n    \"\"\"\n    def __init__(\n        self,\n        project_name,\n        folder_id,\n        task_id,\n        workfile_id,\n        folder_path=None,\n        task_name=None,\n        project_entity=None,\n        folder_entity=None,\n        task_entity=None,\n        workfile_entity=None,\n        project_settings=None,\n    ):\n        self._project_name = project_name\n        self._folder_id = folder_id\n        self._task_id = task_id\n        self._workfile_id = workfile_id\n\n        self._folder_path = folder_path\n        self._task_name = task_name\n\n        self._project_entity = project_entity\n        self._folder_entity = folder_entity\n        self._task_entity = task_entity\n        self._workfile_entity = workfile_entity\n\n        self._project_settings = project_settings\n\n    def __getitem__(self, key):\n        warnings.warn(\n            (\n                \"Using deprecated access to selection data. Please use\"\n                \" attributes and methods\"\n                \" defined by 'LauncherActionSelection'.\"\n            ),\n            category=DeprecationWarning\n        )\n        if key == \"AYON_PROJECT_NAME\":\n            return self.project_name\n        if key == \"AYON_FOLDER_PATH\":\n            return self.folder_path\n        if key == \"AYON_TASK_NAME\":\n            return self.task_name\n        raise KeyError(f\"Key: {key} not found\")\n\n    def __iter__(self):\n        for key in self.keys():\n            yield key\n\n    def __contains__(self, key):\n        warnings.warn(\n            (\n                \"Using deprecated access to selection data. Please use\"\n                \" attributes and methods\"\n                \" defined by 'LauncherActionSelection'.\"\n            ),\n            category=DeprecationWarning\n        )\n        # Fake missing keys check for backwards compatibility\n        if key == \"AYON_PROJECT_NAME\":\n            return self._project_name is not None\n        if key == \"AYON_FOLDER_PATH\":\n            return self._folder_id is not None\n        if key == \"AYON_TASK_NAME\":\n            return self._task_id is not None\n        return False\n\n    def get(self, key, default=None):\n        \"\"\"\n\n        Deprecated:\n            Added for backwards compatibility with older actions.\n\n        \"\"\"\n        warnings.warn(\n            (\n                \"Using deprecated access to selection data. Please use\"\n                \" attributes and methods\"\n                \" defined by 'LauncherActionSelection'.\"\n            ),\n            category=DeprecationWarning\n        )\n        try:\n            return self[key]\n        except KeyError:\n            return default\n\n    def items(self):\n        \"\"\"\n\n        Deprecated:\n            Added for backwards compatibility with older actions.\n\n        \"\"\"\n        for key, value in (\n            (\"AYON_PROJECT_NAME\", self.project_name),\n            (\"AYON_FOLDER_PATH\", self.folder_path),\n            (\"AYON_TASK_NAME\", self.task_name),\n        ):\n            if value is not None:\n                yield (key, value)\n\n    def keys(self):\n        \"\"\"\n\n        Deprecated:\n            Added for backwards compatibility with older actions.\n\n        \"\"\"\n        for key, _ in self.items():\n            yield key\n\n    def values(self):\n        \"\"\"\n\n        Deprecated:\n            Added for backwards compatibility with older actions.\n\n        \"\"\"\n        for _, value in self.items():\n            yield value\n\n    def get_project_name(self):\n        \"\"\"Selected project name.\n\n        Returns:\n            Union[str, None]: Selected project name.\n\n        \"\"\"\n        return self._project_name\n\n    def get_folder_id(self):\n        \"\"\"Selected folder id.\n\n        Returns:\n            Union[str, None]: Selected folder id.\n\n        \"\"\"\n        return self._folder_id\n\n    def get_folder_path(self):\n        \"\"\"Selected folder path.\n\n        Returns:\n            Union[str, None]: Selected folder path.\n\n        \"\"\"\n        if self._folder_id is None:\n            return None\n        if self._folder_path is None:\n            self._folder_path = self.folder_entity[\"path\"]\n        return self._folder_path\n\n    def get_task_id(self):\n        \"\"\"Selected task id.\n\n        Returns:\n            Union[str, None]: Selected task id.\n\n        \"\"\"\n        return self._task_id\n\n    def get_task_name(self):\n        \"\"\"Selected task name.\n\n        Returns:\n            Union[str, None]: Selected task name.\n\n        \"\"\"\n        if self._task_id is None:\n            return None\n        if self._task_name is None:\n            self._task_name = self.task_entity[\"name\"]\n        return self._task_name\n\n    def get_workfile_id(self):\n        \"\"\"Selected workfile id.\n\n        Returns:\n            Union[str, None]: Selected workfile id.\n\n        \"\"\"\n        return self._workfile_id\n\n    def get_project_entity(self):\n        \"\"\"Project entity for the selection.\n\n        Returns:\n            Union[dict[str, Any], None]: Project entity.\n\n        \"\"\"\n        if self._project_name is None:\n            return None\n        if self._project_entity is None:\n            self._project_entity = ayon_api.get_project(self._project_name)\n        return self._project_entity\n\n    def get_folder_entity(self):\n        \"\"\"Folder entity for the selection.\n\n        Returns:\n            Union[dict[str, Any], None]: Folder entity.\n\n        \"\"\"\n        if self._project_name is None or self._folder_id is None:\n            return None\n        if self._folder_entity is None:\n            self._folder_entity = ayon_api.get_folder_by_id(\n                self._project_name, self._folder_id\n            )\n        return self._folder_entity\n\n    def get_task_entity(self):\n        \"\"\"Task entity for the selection.\n\n        Returns:\n            Union[dict[str, Any], None]: Task entity.\n\n        \"\"\"\n        if (\n            self._project_name is None\n            or self._task_id is None\n        ):\n            return None\n        if self._task_entity is None:\n            self._task_entity = ayon_api.get_task_by_id(\n                self._project_name, self._task_id\n            )\n        return self._task_entity\n\n    def get_workfile_entity(self):\n        \"\"\"Workfile entity for the selection.\n\n        Returns:\n            Union[dict[str, Any], None]: Workfile entity.\n\n        \"\"\"\n        if (\n            self._project_name is None\n            or self._workfile_id is None\n        ):\n            return None\n        if self._workfile_entity is None:\n            self._workfile_entity = ayon_api.get_workfile_info_by_id(\n                self._project_name, self._workfile_id\n            )\n        return self._workfile_entity\n\n    def get_project_settings(self):\n        \"\"\"Project settings for the selection.\n\n        Returns:\n            dict[str, Any]: Project settings or studio settings if\n                project is not selected.\n\n        \"\"\"\n        if self._project_settings is None:\n            if self._project_name is None:\n                settings = get_studio_settings()\n            else:\n                settings = get_project_settings(self._project_name)\n            self._project_settings = settings\n        return self._project_settings\n\n    @property\n    def is_project_selected(self):\n        \"\"\"Return whether a project is selected.\n\n        Returns:\n            bool: Whether a project is selected.\n\n        \"\"\"\n        return self._project_name is not None\n\n    @property\n    def is_folder_selected(self):\n        \"\"\"Return whether a folder is selected.\n\n        Returns:\n            bool: Whether a folder is selected.\n\n        \"\"\"\n        return self._folder_id is not None\n\n    @property\n    def is_task_selected(self):\n        \"\"\"Return whether a task is selected.\n\n        Returns:\n            bool: Whether a task is selected.\n\n        \"\"\"\n        return self._task_id is not None\n\n    @property\n    def is_workfile_selected(self):\n        \"\"\"Return whether a task is selected.\n\n        Returns:\n            bool: Whether a task is selected.\n\n        \"\"\"\n        return self._workfile_id is not None\n\n    project_name = property(get_project_name)\n    folder_id = property(get_folder_id)\n    task_id = property(get_task_id)\n    workfile_id = property(get_workfile_id)\n    folder_path = property(get_folder_path)\n    task_name = property(get_task_name)\n\n    project_entity = property(get_project_entity)\n    folder_entity = property(get_folder_entity)\n    task_entity = property(get_task_entity)\n    workfile_entity = property(get_workfile_entity)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/index.html#client.ayon_core.pipeline.actions.LauncherActionSelection.is_folder_selected","title":"<code>is_folder_selected</code>  <code>property</code>","text":"<p>Return whether a folder is selected.</p> <p>Returns:</p> Name Type Description <code>bool</code> <p>Whether a folder is selected.</p>"},{"location":"autoapi/client/ayon_core/pipeline/actions/index.html#client.ayon_core.pipeline.actions.LauncherActionSelection.is_project_selected","title":"<code>is_project_selected</code>  <code>property</code>","text":"<p>Return whether a project is selected.</p> <p>Returns:</p> Name Type Description <code>bool</code> <p>Whether a project is selected.</p>"},{"location":"autoapi/client/ayon_core/pipeline/actions/index.html#client.ayon_core.pipeline.actions.LauncherActionSelection.is_task_selected","title":"<code>is_task_selected</code>  <code>property</code>","text":"<p>Return whether a task is selected.</p> <p>Returns:</p> Name Type Description <code>bool</code> <p>Whether a task is selected.</p>"},{"location":"autoapi/client/ayon_core/pipeline/actions/index.html#client.ayon_core.pipeline.actions.LauncherActionSelection.is_workfile_selected","title":"<code>is_workfile_selected</code>  <code>property</code>","text":"<p>Return whether a task is selected.</p> <p>Returns:</p> Name Type Description <code>bool</code> <p>Whether a task is selected.</p>"},{"location":"autoapi/client/ayon_core/pipeline/actions/index.html#client.ayon_core.pipeline.actions.LauncherActionSelection.get","title":"<code>get(key, default=None)</code>","text":"Deprecated <p>Added for backwards compatibility with older actions.</p> Source code in <code>client/ayon_core/pipeline/actions/launcher.py</code> <pre><code>def get(self, key, default=None):\n    \"\"\"\n\n    Deprecated:\n        Added for backwards compatibility with older actions.\n\n    \"\"\"\n    warnings.warn(\n        (\n            \"Using deprecated access to selection data. Please use\"\n            \" attributes and methods\"\n            \" defined by 'LauncherActionSelection'.\"\n        ),\n        category=DeprecationWarning\n    )\n    try:\n        return self[key]\n    except KeyError:\n        return default\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/index.html#client.ayon_core.pipeline.actions.LauncherActionSelection.get_folder_entity","title":"<code>get_folder_entity()</code>","text":"<p>Folder entity for the selection.</p> <p>Returns:</p> Type Description <p>Union[dict[str, Any], None]: Folder entity.</p> Source code in <code>client/ayon_core/pipeline/actions/launcher.py</code> <pre><code>def get_folder_entity(self):\n    \"\"\"Folder entity for the selection.\n\n    Returns:\n        Union[dict[str, Any], None]: Folder entity.\n\n    \"\"\"\n    if self._project_name is None or self._folder_id is None:\n        return None\n    if self._folder_entity is None:\n        self._folder_entity = ayon_api.get_folder_by_id(\n            self._project_name, self._folder_id\n        )\n    return self._folder_entity\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/index.html#client.ayon_core.pipeline.actions.LauncherActionSelection.get_folder_id","title":"<code>get_folder_id()</code>","text":"<p>Selected folder id.</p> <p>Returns:</p> Type Description <p>Union[str, None]: Selected folder id.</p> Source code in <code>client/ayon_core/pipeline/actions/launcher.py</code> <pre><code>def get_folder_id(self):\n    \"\"\"Selected folder id.\n\n    Returns:\n        Union[str, None]: Selected folder id.\n\n    \"\"\"\n    return self._folder_id\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/index.html#client.ayon_core.pipeline.actions.LauncherActionSelection.get_folder_path","title":"<code>get_folder_path()</code>","text":"<p>Selected folder path.</p> <p>Returns:</p> Type Description <p>Union[str, None]: Selected folder path.</p> Source code in <code>client/ayon_core/pipeline/actions/launcher.py</code> <pre><code>def get_folder_path(self):\n    \"\"\"Selected folder path.\n\n    Returns:\n        Union[str, None]: Selected folder path.\n\n    \"\"\"\n    if self._folder_id is None:\n        return None\n    if self._folder_path is None:\n        self._folder_path = self.folder_entity[\"path\"]\n    return self._folder_path\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/index.html#client.ayon_core.pipeline.actions.LauncherActionSelection.get_project_entity","title":"<code>get_project_entity()</code>","text":"<p>Project entity for the selection.</p> <p>Returns:</p> Type Description <p>Union[dict[str, Any], None]: Project entity.</p> Source code in <code>client/ayon_core/pipeline/actions/launcher.py</code> <pre><code>def get_project_entity(self):\n    \"\"\"Project entity for the selection.\n\n    Returns:\n        Union[dict[str, Any], None]: Project entity.\n\n    \"\"\"\n    if self._project_name is None:\n        return None\n    if self._project_entity is None:\n        self._project_entity = ayon_api.get_project(self._project_name)\n    return self._project_entity\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/index.html#client.ayon_core.pipeline.actions.LauncherActionSelection.get_project_name","title":"<code>get_project_name()</code>","text":"<p>Selected project name.</p> <p>Returns:</p> Type Description <p>Union[str, None]: Selected project name.</p> Source code in <code>client/ayon_core/pipeline/actions/launcher.py</code> <pre><code>def get_project_name(self):\n    \"\"\"Selected project name.\n\n    Returns:\n        Union[str, None]: Selected project name.\n\n    \"\"\"\n    return self._project_name\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/index.html#client.ayon_core.pipeline.actions.LauncherActionSelection.get_project_settings","title":"<code>get_project_settings()</code>","text":"<p>Project settings for the selection.</p> <p>Returns:</p> Type Description <p>dict[str, Any]: Project settings or studio settings if project is not selected.</p> Source code in <code>client/ayon_core/pipeline/actions/launcher.py</code> <pre><code>def get_project_settings(self):\n    \"\"\"Project settings for the selection.\n\n    Returns:\n        dict[str, Any]: Project settings or studio settings if\n            project is not selected.\n\n    \"\"\"\n    if self._project_settings is None:\n        if self._project_name is None:\n            settings = get_studio_settings()\n        else:\n            settings = get_project_settings(self._project_name)\n        self._project_settings = settings\n    return self._project_settings\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/index.html#client.ayon_core.pipeline.actions.LauncherActionSelection.get_task_entity","title":"<code>get_task_entity()</code>","text":"<p>Task entity for the selection.</p> <p>Returns:</p> Type Description <p>Union[dict[str, Any], None]: Task entity.</p> Source code in <code>client/ayon_core/pipeline/actions/launcher.py</code> <pre><code>def get_task_entity(self):\n    \"\"\"Task entity for the selection.\n\n    Returns:\n        Union[dict[str, Any], None]: Task entity.\n\n    \"\"\"\n    if (\n        self._project_name is None\n        or self._task_id is None\n    ):\n        return None\n    if self._task_entity is None:\n        self._task_entity = ayon_api.get_task_by_id(\n            self._project_name, self._task_id\n        )\n    return self._task_entity\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/index.html#client.ayon_core.pipeline.actions.LauncherActionSelection.get_task_id","title":"<code>get_task_id()</code>","text":"<p>Selected task id.</p> <p>Returns:</p> Type Description <p>Union[str, None]: Selected task id.</p> Source code in <code>client/ayon_core/pipeline/actions/launcher.py</code> <pre><code>def get_task_id(self):\n    \"\"\"Selected task id.\n\n    Returns:\n        Union[str, None]: Selected task id.\n\n    \"\"\"\n    return self._task_id\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/index.html#client.ayon_core.pipeline.actions.LauncherActionSelection.get_task_name","title":"<code>get_task_name()</code>","text":"<p>Selected task name.</p> <p>Returns:</p> Type Description <p>Union[str, None]: Selected task name.</p> Source code in <code>client/ayon_core/pipeline/actions/launcher.py</code> <pre><code>def get_task_name(self):\n    \"\"\"Selected task name.\n\n    Returns:\n        Union[str, None]: Selected task name.\n\n    \"\"\"\n    if self._task_id is None:\n        return None\n    if self._task_name is None:\n        self._task_name = self.task_entity[\"name\"]\n    return self._task_name\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/index.html#client.ayon_core.pipeline.actions.LauncherActionSelection.get_workfile_entity","title":"<code>get_workfile_entity()</code>","text":"<p>Workfile entity for the selection.</p> <p>Returns:</p> Type Description <p>Union[dict[str, Any], None]: Workfile entity.</p> Source code in <code>client/ayon_core/pipeline/actions/launcher.py</code> <pre><code>def get_workfile_entity(self):\n    \"\"\"Workfile entity for the selection.\n\n    Returns:\n        Union[dict[str, Any], None]: Workfile entity.\n\n    \"\"\"\n    if (\n        self._project_name is None\n        or self._workfile_id is None\n    ):\n        return None\n    if self._workfile_entity is None:\n        self._workfile_entity = ayon_api.get_workfile_info_by_id(\n            self._project_name, self._workfile_id\n        )\n    return self._workfile_entity\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/index.html#client.ayon_core.pipeline.actions.LauncherActionSelection.get_workfile_id","title":"<code>get_workfile_id()</code>","text":"<p>Selected workfile id.</p> <p>Returns:</p> Type Description <p>Union[str, None]: Selected workfile id.</p> Source code in <code>client/ayon_core/pipeline/actions/launcher.py</code> <pre><code>def get_workfile_id(self):\n    \"\"\"Selected workfile id.\n\n    Returns:\n        Union[str, None]: Selected workfile id.\n\n    \"\"\"\n    return self._workfile_id\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/index.html#client.ayon_core.pipeline.actions.LauncherActionSelection.items","title":"<code>items()</code>","text":"Deprecated <p>Added for backwards compatibility with older actions.</p> Source code in <code>client/ayon_core/pipeline/actions/launcher.py</code> <pre><code>def items(self):\n    \"\"\"\n\n    Deprecated:\n        Added for backwards compatibility with older actions.\n\n    \"\"\"\n    for key, value in (\n        (\"AYON_PROJECT_NAME\", self.project_name),\n        (\"AYON_FOLDER_PATH\", self.folder_path),\n        (\"AYON_TASK_NAME\", self.task_name),\n    ):\n        if value is not None:\n            yield (key, value)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/index.html#client.ayon_core.pipeline.actions.LauncherActionSelection.keys","title":"<code>keys()</code>","text":"Deprecated <p>Added for backwards compatibility with older actions.</p> Source code in <code>client/ayon_core/pipeline/actions/launcher.py</code> <pre><code>def keys(self):\n    \"\"\"\n\n    Deprecated:\n        Added for backwards compatibility with older actions.\n\n    \"\"\"\n    for key, _ in self.items():\n        yield key\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/index.html#client.ayon_core.pipeline.actions.LauncherActionSelection.values","title":"<code>values()</code>","text":"Deprecated <p>Added for backwards compatibility with older actions.</p> Source code in <code>client/ayon_core/pipeline/actions/launcher.py</code> <pre><code>def values(self):\n    \"\"\"\n\n    Deprecated:\n        Added for backwards compatibility with older actions.\n\n    \"\"\"\n    for _, value in self.items():\n        yield value\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/index.html#client.ayon_core.pipeline.actions.LoaderActionItem","title":"<code>LoaderActionItem</code>  <code>dataclass</code>","text":"<p>Item of loader action.</p> <p>Action plugins return these items as possible actions to run for a given     context.</p> <p>Because the action item can be related to a specific entity     and not the whole selection, they also have to define the entity type     and ids to be executed on.</p> <p>Attributes:</p> Name Type Description <code>label</code> <code>str</code> <p>Text shown in UI.</p> <code>order</code> <code>int</code> <p>Order of the action in UI.</p> <code>group_label</code> <code>Optional[str]</code> <p>Label of the group to which the action belongs.</p> <code>icon</code> <code>Optional[dict[str, Any]</code> <p>Icon definition.</p> <code>data</code> <code>Optional[DataType]</code> <p>Action item data.</p> <code>identifier</code> <code>Optional[str]</code> <p>Identifier of the plugin which created the action item. Is filled automatically. Is not changed if is filled -&gt; can lead to different plugin.</p> Source code in <code>client/ayon_core/pipeline/actions/loader.py</code> <pre><code>@dataclass\nclass LoaderActionItem:\n    \"\"\"Item of loader action.\n\n    Action plugins return these items as possible actions to run for a given\n        context.\n\n    Because the action item can be related to a specific entity\n        and not the whole selection, they also have to define the entity type\n        and ids to be executed on.\n\n    Attributes:\n        label (str): Text shown in UI.\n        order (int): Order of the action in UI.\n        group_label (Optional[str]): Label of the group to which the action\n            belongs.\n        icon (Optional[dict[str, Any]): Icon definition.\n        data (Optional[DataType]): Action item data.\n        identifier (Optional[str]): Identifier of the plugin which\n            created the action item. Is filled automatically. Is not changed\n            if is filled -&gt; can lead to different plugin.\n\n    \"\"\"\n    label: str\n    order: int = 0\n    group_label: Optional[str] = None\n    icon: IconBase | dict[str, Any] | None = None\n    data: Optional[DataType] = None\n    # Is filled automatically\n    identifier: str = None\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/index.html#client.ayon_core.pipeline.actions.LoaderActionPlugin","title":"<code>LoaderActionPlugin</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Plugin for loader actions.</p> <p>Plugin is responsible for getting action items and executing actions.</p> Source code in <code>client/ayon_core/pipeline/actions/loader.py</code> <pre><code>class LoaderActionPlugin(ABC):\n    \"\"\"Plugin for loader actions.\n\n    Plugin is responsible for getting action items and executing actions.\n\n    \"\"\"\n    _log: Optional[logging.Logger] = None\n    enabled: bool = True\n    skip_discovery: bool = True\n\n    def __init__(self, context: \"LoaderActionsContext\") -&gt; None:\n        self._context = context\n        self.apply_settings(context.get_studio_settings())\n\n    def apply_settings(self, studio_settings: dict[str, Any]) -&gt; None:\n        \"\"\"Apply studio settings to the plugin.\n\n        Args:\n            studio_settings (dict[str, Any]): Studio settings.\n\n        \"\"\"\n        pass\n\n    @property\n    def log(self) -&gt; logging.Logger:\n        if self._log is None:\n            self._log = Logger.get_logger(self.__class__.__name__)\n        return self._log\n\n    @property\n    def identifier(self) -&gt; str:\n        \"\"\"Identifier of the plugin.\n\n        Returns:\n            str: Plugin identifier.\n\n        \"\"\"\n        return self.__class__.__name__\n\n    @property\n    def host_name(self) -&gt; Optional[str]:\n        \"\"\"Name of the current host.\"\"\"\n        return self._context.get_host_name()\n\n    @abstractmethod\n    def get_action_items(\n        self, selection: LoaderActionSelection\n    ) -&gt; list[LoaderActionItem]:\n        \"\"\"Action items for the selection.\n\n        Args:\n            selection (LoaderActionSelection): Selection.\n\n        Returns:\n            list[LoaderActionItem]: Action items.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def execute_action(\n        self,\n        selection: LoaderActionSelection,\n        data: Optional[DataType],\n        form_values: dict[str, Any],\n    ) -&gt; Optional[LoaderActionResult]:\n        \"\"\"Execute an action.\n\n        Args:\n            selection (LoaderActionSelection): Selection wrapper. Can be used\n                to get entities or get context of original selection.\n            data (Optional[DataType]): Additional action item data.\n            form_values (dict[str, Any]): Attribute values.\n\n        Returns:\n            Optional[LoaderActionResult]: Result of the action execution.\n\n        \"\"\"\n        pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/index.html#client.ayon_core.pipeline.actions.LoaderActionPlugin.host_name","title":"<code>host_name</code>  <code>property</code>","text":"<p>Name of the current host.</p>"},{"location":"autoapi/client/ayon_core/pipeline/actions/index.html#client.ayon_core.pipeline.actions.LoaderActionPlugin.identifier","title":"<code>identifier</code>  <code>property</code>","text":"<p>Identifier of the plugin.</p> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Plugin identifier.</p>"},{"location":"autoapi/client/ayon_core/pipeline/actions/index.html#client.ayon_core.pipeline.actions.LoaderActionPlugin.apply_settings","title":"<code>apply_settings(studio_settings)</code>","text":"<p>Apply studio settings to the plugin.</p> <p>Parameters:</p> Name Type Description Default <code>studio_settings</code> <code>dict[str, Any]</code> <p>Studio settings.</p> required Source code in <code>client/ayon_core/pipeline/actions/loader.py</code> <pre><code>def apply_settings(self, studio_settings: dict[str, Any]) -&gt; None:\n    \"\"\"Apply studio settings to the plugin.\n\n    Args:\n        studio_settings (dict[str, Any]): Studio settings.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/index.html#client.ayon_core.pipeline.actions.LoaderActionPlugin.execute_action","title":"<code>execute_action(selection, data, form_values)</code>  <code>abstractmethod</code>","text":"<p>Execute an action.</p> <p>Parameters:</p> Name Type Description Default <code>selection</code> <code>LoaderActionSelection</code> <p>Selection wrapper. Can be used to get entities or get context of original selection.</p> required <code>data</code> <code>Optional[DataType]</code> <p>Additional action item data.</p> required <code>form_values</code> <code>dict[str, Any]</code> <p>Attribute values.</p> required <p>Returns:</p> Type Description <code>Optional[LoaderActionResult]</code> <p>Optional[LoaderActionResult]: Result of the action execution.</p> Source code in <code>client/ayon_core/pipeline/actions/loader.py</code> <pre><code>@abstractmethod\ndef execute_action(\n    self,\n    selection: LoaderActionSelection,\n    data: Optional[DataType],\n    form_values: dict[str, Any],\n) -&gt; Optional[LoaderActionResult]:\n    \"\"\"Execute an action.\n\n    Args:\n        selection (LoaderActionSelection): Selection wrapper. Can be used\n            to get entities or get context of original selection.\n        data (Optional[DataType]): Additional action item data.\n        form_values (dict[str, Any]): Attribute values.\n\n    Returns:\n        Optional[LoaderActionResult]: Result of the action execution.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/index.html#client.ayon_core.pipeline.actions.LoaderActionPlugin.get_action_items","title":"<code>get_action_items(selection)</code>  <code>abstractmethod</code>","text":"<p>Action items for the selection.</p> <p>Parameters:</p> Name Type Description Default <code>selection</code> <code>LoaderActionSelection</code> <p>Selection.</p> required <p>Returns:</p> Type Description <code>list[LoaderActionItem]</code> <p>list[LoaderActionItem]: Action items.</p> Source code in <code>client/ayon_core/pipeline/actions/loader.py</code> <pre><code>@abstractmethod\ndef get_action_items(\n    self, selection: LoaderActionSelection\n) -&gt; list[LoaderActionItem]:\n    \"\"\"Action items for the selection.\n\n    Args:\n        selection (LoaderActionSelection): Selection.\n\n    Returns:\n        list[LoaderActionItem]: Action items.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/index.html#client.ayon_core.pipeline.actions.LoaderActionResult","title":"<code>LoaderActionResult</code>  <code>dataclass</code>","text":"<p>Result of loader action execution.</p> <p>Attributes:</p> Name Type Description <code>message</code> <code>Optional[str]</code> <p>Message to show in UI.</p> <code>success</code> <code>bool</code> <p>If the action was successful. Affects color of the message.</p> <code>form</code> <code>Optional[ActionForm]</code> <p>Form to show in UI.</p> <code>form_values</code> <code>Optional[dict[str, Any]]</code> <p>Values for the form. Can be used if the same form is re-shown e.g. because a user forgot to fill a required field.</p> Source code in <code>client/ayon_core/pipeline/actions/loader.py</code> <pre><code>@dataclass\nclass LoaderActionResult:\n    \"\"\"Result of loader action execution.\n\n    Attributes:\n        message (Optional[str]): Message to show in UI.\n        success (bool): If the action was successful. Affects color of\n            the message.\n        form (Optional[ActionForm]): Form to show in UI.\n        form_values (Optional[dict[str, Any]]): Values for the form. Can be\n            used if the same form is re-shown e.g. because a user forgot to\n            fill a required field.\n\n    \"\"\"\n    message: Optional[str] = None\n    success: bool = True\n    form: Optional[ActionForm] = None\n    form_values: Optional[dict[str, Any]] = None\n\n    def to_json_data(self) -&gt; dict[str, Any]:\n        form = self.form\n        if form is not None:\n            form = form.to_json_data()\n        return {\n            \"message\": self.message,\n            \"success\": self.success,\n            \"form\": form,\n            \"form_values\": self.form_values,\n        }\n\n    @classmethod\n    def from_json_data(cls, data: dict[str, Any]) -&gt; \"LoaderActionResult\":\n        form = data[\"form\"]\n        if form is not None:\n            data[\"form\"] = ActionForm.from_json_data(form)\n        return LoaderActionResult(**data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/index.html#client.ayon_core.pipeline.actions.LoaderActionSelection","title":"<code>LoaderActionSelection</code>","text":"<p>Selection of entities for loader actions.</p> <p>Selection tells action plugins what exactly is selected in the tool and     which ids.</p> <p>Contains entity cache which can be used to get entities by their ids. Or     to get project settings and anatomy.</p> Source code in <code>client/ayon_core/pipeline/actions/loader.py</code> <pre><code>class LoaderActionSelection:\n    \"\"\"Selection of entities for loader actions.\n\n    Selection tells action plugins what exactly is selected in the tool and\n        which ids.\n\n    Contains entity cache which can be used to get entities by their ids. Or\n        to get project settings and anatomy.\n\n    \"\"\"\n    def __init__(\n        self,\n        project_name: str,\n        selected_ids: set[str],\n        selected_type: LoaderSelectedType,\n        *,\n        project_anatomy: Optional[Anatomy] = None,\n        project_settings: Optional[dict[str, Any]] = None,\n        entities_cache: Optional[SelectionEntitiesCache] = None,\n    ):\n        self._project_name = project_name\n        self._selected_ids = selected_ids\n        self._selected_type = selected_type\n\n        self._project_anatomy = project_anatomy\n        self._project_settings = project_settings\n\n        if entities_cache is None:\n            entities_cache = SelectionEntitiesCache(project_name)\n        self._entities_cache = entities_cache\n\n    def get_entities_cache(self) -&gt; SelectionEntitiesCache:\n        return self._entities_cache\n\n    def get_project_name(self) -&gt; str:\n        return self._project_name\n\n    def get_selected_ids(self) -&gt; set[str]:\n        return set(self._selected_ids)\n\n    def get_selected_type(self) -&gt; str:\n        return self._selected_type\n\n    def get_project_settings(self) -&gt; dict[str, Any]:\n        if self._project_settings is None:\n            self._project_settings = get_project_settings(self._project_name)\n        return copy.deepcopy(self._project_settings)\n\n    def get_project_anatomy(self) -&gt; Anatomy:\n        if self._project_anatomy is None:\n            self._project_anatomy = Anatomy(\n                self._project_name,\n                project_entity=self.get_entities_cache().get_project(),\n            )\n        return self._project_anatomy\n\n    project_name = property(get_project_name)\n    selected_ids = property(get_selected_ids)\n    selected_type = property(get_selected_type)\n    project_settings = property(get_project_settings)\n    project_anatomy = property(get_project_anatomy)\n    entities = property(get_entities_cache)\n\n    # --- Helper methods ---\n    def versions_selected(self) -&gt; bool:\n        \"\"\"Selected entity type is version.\n\n        Returns:\n            bool: True if selected entity type is version.\n\n        \"\"\"\n        return self._selected_type == LoaderSelectedType.version\n\n    def representations_selected(self) -&gt; bool:\n        \"\"\"Selected entity type is representation.\n\n        Returns:\n            bool: True if selected entity type is representation.\n\n        \"\"\"\n        return self._selected_type == LoaderSelectedType.representation\n\n    def get_selected_version_entities(self) -&gt; list[dict[str, Any]]:\n        \"\"\"Retrieve selected version entities.\n\n        An empty list is returned if 'version' is not the selected\n            entity type.\n\n        Returns:\n            list[dict[str, Any]]: List of selected version entities.\n\n        \"\"\"\n        if self.versions_selected():\n            return self.entities.get_versions(self.selected_ids)\n        return []\n\n    def get_selected_representation_entities(self) -&gt; list[dict[str, Any]]:\n        \"\"\"Retrieve selected representation entities.\n\n        An empty list is returned if 'representation' is not the selected\n            entity type.\n\n        Returns:\n            list[dict[str, Any]]: List of selected representation entities.\n\n        \"\"\"\n        if self.representations_selected():\n            return self.entities.get_representations(self.selected_ids)\n        return []\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/index.html#client.ayon_core.pipeline.actions.LoaderActionSelection.get_selected_representation_entities","title":"<code>get_selected_representation_entities()</code>","text":"<p>Retrieve selected representation entities.</p> <p>An empty list is returned if 'representation' is not the selected     entity type.</p> <p>Returns:</p> Type Description <code>list[dict[str, Any]]</code> <p>list[dict[str, Any]]: List of selected representation entities.</p> Source code in <code>client/ayon_core/pipeline/actions/loader.py</code> <pre><code>def get_selected_representation_entities(self) -&gt; list[dict[str, Any]]:\n    \"\"\"Retrieve selected representation entities.\n\n    An empty list is returned if 'representation' is not the selected\n        entity type.\n\n    Returns:\n        list[dict[str, Any]]: List of selected representation entities.\n\n    \"\"\"\n    if self.representations_selected():\n        return self.entities.get_representations(self.selected_ids)\n    return []\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/index.html#client.ayon_core.pipeline.actions.LoaderActionSelection.get_selected_version_entities","title":"<code>get_selected_version_entities()</code>","text":"<p>Retrieve selected version entities.</p> <p>An empty list is returned if 'version' is not the selected     entity type.</p> <p>Returns:</p> Type Description <code>list[dict[str, Any]]</code> <p>list[dict[str, Any]]: List of selected version entities.</p> Source code in <code>client/ayon_core/pipeline/actions/loader.py</code> <pre><code>def get_selected_version_entities(self) -&gt; list[dict[str, Any]]:\n    \"\"\"Retrieve selected version entities.\n\n    An empty list is returned if 'version' is not the selected\n        entity type.\n\n    Returns:\n        list[dict[str, Any]]: List of selected version entities.\n\n    \"\"\"\n    if self.versions_selected():\n        return self.entities.get_versions(self.selected_ids)\n    return []\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/index.html#client.ayon_core.pipeline.actions.LoaderActionSelection.representations_selected","title":"<code>representations_selected()</code>","text":"<p>Selected entity type is representation.</p> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if selected entity type is representation.</p> Source code in <code>client/ayon_core/pipeline/actions/loader.py</code> <pre><code>def representations_selected(self) -&gt; bool:\n    \"\"\"Selected entity type is representation.\n\n    Returns:\n        bool: True if selected entity type is representation.\n\n    \"\"\"\n    return self._selected_type == LoaderSelectedType.representation\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/index.html#client.ayon_core.pipeline.actions.LoaderActionSelection.versions_selected","title":"<code>versions_selected()</code>","text":"<p>Selected entity type is version.</p> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if selected entity type is version.</p> Source code in <code>client/ayon_core/pipeline/actions/loader.py</code> <pre><code>def versions_selected(self) -&gt; bool:\n    \"\"\"Selected entity type is version.\n\n    Returns:\n        bool: True if selected entity type is version.\n\n    \"\"\"\n    return self._selected_type == LoaderSelectedType.version\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/index.html#client.ayon_core.pipeline.actions.LoaderActionsContext","title":"<code>LoaderActionsContext</code>","text":"<p>Wrapper for loader actions and their logic.</p> <p>Takes care about the public api of loader actions and internal logic like     discovery and initialization of plugins.</p> Source code in <code>client/ayon_core/pipeline/actions/loader.py</code> <pre><code>class LoaderActionsContext:\n    \"\"\"Wrapper for loader actions and their logic.\n\n    Takes care about the public api of loader actions and internal logic like\n        discovery and initialization of plugins.\n\n    \"\"\"\n    def __init__(\n        self,\n        studio_settings: Optional[dict[str, Any]] = None,\n        addons_manager: Optional[AddonsManager] = None,\n        host: Optional[AbstractHost] = _PLACEHOLDER,\n    ) -&gt; None:\n        self._log = Logger.get_logger(self.__class__.__name__)\n\n        self._addons_manager = addons_manager\n        self._host = host\n\n        # Attributes that are re-cached on reset\n        self._studio_settings = studio_settings\n        self._plugins = None\n\n    def reset(\n        self, studio_settings: Optional[dict[str, Any]] = None\n    ) -&gt; None:\n        \"\"\"Reset context cache.\n\n        Reset plugins and studio settings to reload them.\n\n        Notes:\n             Does not reset the cache of AddonsManger because there should not\n                be a reason to do so.\n\n        \"\"\"\n        self._studio_settings = studio_settings\n        self._plugins = None\n\n    def get_addons_manager(self) -&gt; AddonsManager:\n        if self._addons_manager is None:\n            self._addons_manager = AddonsManager(\n                settings=self.get_studio_settings()\n            )\n        return self._addons_manager\n\n    def get_host(self) -&gt; Optional[AbstractHost]:\n        \"\"\"Get current host integration.\n\n        Returns:\n            Optional[AbstractHost]: Host integration. Can be None if host\n                integration is not registered -&gt; probably not used in the\n                host integration process.\n\n        \"\"\"\n        if self._host is _PLACEHOLDER:\n            from ayon_core.pipeline import registered_host\n\n            self._host = registered_host()\n        return self._host\n\n    def get_host_name(self) -&gt; Optional[str]:\n        host = self.get_host()\n        if host is None:\n            return None\n        return host.name\n\n    def get_studio_settings(self) -&gt; dict[str, Any]:\n        if self._studio_settings is None:\n            self._studio_settings = get_studio_settings()\n        return copy.deepcopy(self._studio_settings)\n\n    def get_action_items(\n        self, selection: LoaderActionSelection\n    ) -&gt; list[LoaderActionItem]:\n        \"\"\"Collect action items from all plugins for given selection.\n\n        Args:\n            selection (LoaderActionSelection): Selection wrapper.\n\n        \"\"\"\n        output = []\n        for plugin_id, plugin in self._get_plugins().items():\n            try:\n                for action_item in plugin.get_action_items(selection):\n                    if action_item.identifier is None:\n                        action_item.identifier = plugin_id\n                    output.append(action_item)\n\n            except Exception:\n                self._log.warning(\n                    \"Failed to get action items for\"\n                    f\" plugin '{plugin.identifier}'\",\n                    exc_info=True,\n                )\n        return output\n\n    def execute_action(\n        self,\n        identifier: str,\n        selection: LoaderActionSelection,\n        data: Optional[DataType],\n        form_values: dict[str, Any],\n    ) -&gt; Optional[LoaderActionResult]:\n        \"\"\"Trigger action execution.\n\n        Args:\n            identifier (str): Identifier of the plugin.\n            selection (LoaderActionSelection): Selection wrapper. Can be used\n                to get what is selected in UI and to get access to entity\n                cache.\n            data (Optional[DataType]): Additional action item data.\n            form_values (dict[str, Any]): Form values related to action.\n                Usually filled if action returned response with form.\n\n        \"\"\"\n        plugins_by_id = self._get_plugins()\n        plugin = plugins_by_id[identifier]\n        return plugin.execute_action(\n            selection,\n            data,\n            form_values,\n        )\n\n    def _get_plugins(self) -&gt; dict[str, LoaderActionPlugin]:\n        if self._plugins is None:\n            host_name = self.get_host_name()\n            addons_manager = self.get_addons_manager()\n            all_paths = [\n                os.path.join(AYON_CORE_ROOT, \"plugins\", \"loader\")\n            ]\n            for addon in addons_manager.addons:\n                if not isinstance(addon, IPluginPaths):\n                    continue\n\n                try:\n                    if is_func_signature_supported(\n                        addon.get_loader_action_plugin_paths,\n                        host_name\n                    ):\n                        paths = addon.get_loader_action_plugin_paths(\n                            host_name\n                        )\n                    else:\n                        paths = addon.get_loader_action_plugin_paths()\n                except Exception:\n                    self._log.warning(\n                        \"Failed to get plugin paths for addon\",\n                        exc_info=True\n                    )\n                    continue\n\n                if paths:\n                    all_paths.extend(paths)\n\n            result = discover_plugins(LoaderActionPlugin, all_paths)\n            result.log_report()\n            plugins = {}\n            for cls in result.plugins:\n                try:\n                    plugin = cls(self)\n                    if not plugin.enabled:\n                        continue\n\n                    plugin_id = plugin.identifier\n                    if plugin_id not in plugins:\n                        plugins[plugin_id] = plugin\n                        continue\n\n                    self._log.warning(\n                        f\"Duplicated plugins identifier found '{plugin_id}'.\"\n                    )\n\n                except Exception:\n                    self._log.warning(\n                        f\"Failed to initialize plugin '{cls.__name__}'\",\n                        exc_info=True,\n                    )\n            self._plugins = plugins\n        return self._plugins\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/index.html#client.ayon_core.pipeline.actions.LoaderActionsContext.execute_action","title":"<code>execute_action(identifier, selection, data, form_values)</code>","text":"<p>Trigger action execution.</p> <p>Parameters:</p> Name Type Description Default <code>identifier</code> <code>str</code> <p>Identifier of the plugin.</p> required <code>selection</code> <code>LoaderActionSelection</code> <p>Selection wrapper. Can be used to get what is selected in UI and to get access to entity cache.</p> required <code>data</code> <code>Optional[DataType]</code> <p>Additional action item data.</p> required <code>form_values</code> <code>dict[str, Any]</code> <p>Form values related to action. Usually filled if action returned response with form.</p> required Source code in <code>client/ayon_core/pipeline/actions/loader.py</code> <pre><code>def execute_action(\n    self,\n    identifier: str,\n    selection: LoaderActionSelection,\n    data: Optional[DataType],\n    form_values: dict[str, Any],\n) -&gt; Optional[LoaderActionResult]:\n    \"\"\"Trigger action execution.\n\n    Args:\n        identifier (str): Identifier of the plugin.\n        selection (LoaderActionSelection): Selection wrapper. Can be used\n            to get what is selected in UI and to get access to entity\n            cache.\n        data (Optional[DataType]): Additional action item data.\n        form_values (dict[str, Any]): Form values related to action.\n            Usually filled if action returned response with form.\n\n    \"\"\"\n    plugins_by_id = self._get_plugins()\n    plugin = plugins_by_id[identifier]\n    return plugin.execute_action(\n        selection,\n        data,\n        form_values,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/index.html#client.ayon_core.pipeline.actions.LoaderActionsContext.get_action_items","title":"<code>get_action_items(selection)</code>","text":"<p>Collect action items from all plugins for given selection.</p> <p>Parameters:</p> Name Type Description Default <code>selection</code> <code>LoaderActionSelection</code> <p>Selection wrapper.</p> required Source code in <code>client/ayon_core/pipeline/actions/loader.py</code> <pre><code>def get_action_items(\n    self, selection: LoaderActionSelection\n) -&gt; list[LoaderActionItem]:\n    \"\"\"Collect action items from all plugins for given selection.\n\n    Args:\n        selection (LoaderActionSelection): Selection wrapper.\n\n    \"\"\"\n    output = []\n    for plugin_id, plugin in self._get_plugins().items():\n        try:\n            for action_item in plugin.get_action_items(selection):\n                if action_item.identifier is None:\n                    action_item.identifier = plugin_id\n                output.append(action_item)\n\n        except Exception:\n            self._log.warning(\n                \"Failed to get action items for\"\n                f\" plugin '{plugin.identifier}'\",\n                exc_info=True,\n            )\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/index.html#client.ayon_core.pipeline.actions.LoaderActionsContext.get_host","title":"<code>get_host()</code>","text":"<p>Get current host integration.</p> <p>Returns:</p> Type Description <code>Optional[AbstractHost]</code> <p>Optional[AbstractHost]: Host integration. Can be None if host integration is not registered -&gt; probably not used in the host integration process.</p> Source code in <code>client/ayon_core/pipeline/actions/loader.py</code> <pre><code>def get_host(self) -&gt; Optional[AbstractHost]:\n    \"\"\"Get current host integration.\n\n    Returns:\n        Optional[AbstractHost]: Host integration. Can be None if host\n            integration is not registered -&gt; probably not used in the\n            host integration process.\n\n    \"\"\"\n    if self._host is _PLACEHOLDER:\n        from ayon_core.pipeline import registered_host\n\n        self._host = registered_host()\n    return self._host\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/index.html#client.ayon_core.pipeline.actions.LoaderActionsContext.reset","title":"<code>reset(studio_settings=None)</code>","text":"<p>Reset context cache.</p> <p>Reset plugins and studio settings to reload them.</p> Notes <p>Does not reset the cache of AddonsManger because there should not    be a reason to do so.</p> Source code in <code>client/ayon_core/pipeline/actions/loader.py</code> <pre><code>def reset(\n    self, studio_settings: Optional[dict[str, Any]] = None\n) -&gt; None:\n    \"\"\"Reset context cache.\n\n    Reset plugins and studio settings to reload them.\n\n    Notes:\n         Does not reset the cache of AddonsManger because there should not\n            be a reason to do so.\n\n    \"\"\"\n    self._studio_settings = studio_settings\n    self._plugins = None\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/index.html#client.ayon_core.pipeline.actions.LoaderSelectedType","title":"<code>LoaderSelectedType</code>","text":"<p>               Bases: <code>StrEnum</code></p> <p>Selected entity type.</p> Source code in <code>client/ayon_core/pipeline/actions/loader.py</code> <pre><code>class LoaderSelectedType(StrEnum):\n    \"\"\"Selected entity type.\"\"\"\n    # folder = \"folder\"\n    # task = \"task\"\n    version = \"version\"\n    representation = \"representation\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/index.html#client.ayon_core.pipeline.actions.LoaderSimpleActionPlugin","title":"<code>LoaderSimpleActionPlugin</code>","text":"<p>               Bases: <code>LoaderActionPlugin</code></p> <p>Simple action plugin.</p> <p>This action will show exactly one action item defined by attributes     on the class.</p> <p>Attributes:</p> Name Type Description <code>label</code> <code>Optional[str]</code> <p>Label of the action item.</p> <code>order</code> <code>int</code> <p>Order of the action item.</p> <code>group_label</code> <code>Optional[str]</code> <p>Label of the group to which the action belongs.</p> <code>icon</code> <code>Optional[dict[str, Any]]</code> <p>Icon definition shown next to label.</p> Source code in <code>client/ayon_core/pipeline/actions/loader.py</code> <pre><code>class LoaderSimpleActionPlugin(LoaderActionPlugin):\n    \"\"\"Simple action plugin.\n\n    This action will show exactly one action item defined by attributes\n        on the class.\n\n    Attributes:\n        label: Label of the action item.\n        order: Order of the action item.\n        group_label: Label of the group to which the action belongs.\n        icon: Icon definition shown next to label.\n\n    \"\"\"\n\n    label: Optional[str] = None\n    order: int = 0\n    group_label: Optional[str] = None\n    icon: Optional[dict[str, Any]] = None\n    skip_discovery: bool = True\n\n    @abstractmethod\n    def is_compatible(self, selection: LoaderActionSelection) -&gt; bool:\n        \"\"\"Check if plugin is compatible with selection.\n\n        Args:\n            selection (LoaderActionSelection): Selection information.\n\n        Returns:\n            bool: True if plugin is compatible with selection.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def execute_simple_action(\n        self,\n        selection: LoaderActionSelection,\n        form_values: dict[str, Any],\n    ) -&gt; Optional[LoaderActionResult]:\n        \"\"\"Process action based on selection.\n\n        Args:\n            selection (LoaderActionSelection): Selection information.\n            form_values (dict[str, Any]): Values from a form if there are any.\n\n        Returns:\n            Optional[LoaderActionResult]: Result of the action.\n\n        \"\"\"\n        pass\n\n    def get_action_items(\n        self, selection: LoaderActionSelection\n    ) -&gt; list[LoaderActionItem]:\n        if self.is_compatible(selection):\n            label = self.label or self.__class__.__name__\n            return [\n                LoaderActionItem(\n                    label=label,\n                    order=self.order,\n                    group_label=self.group_label,\n                    icon=self.icon,\n                )\n            ]\n        return []\n\n    def execute_action(\n        self,\n        selection: LoaderActionSelection,\n        data: Optional[DataType],\n        form_values: dict[str, Any],\n    ) -&gt; Optional[LoaderActionResult]:\n        return self.execute_simple_action(selection, form_values)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/index.html#client.ayon_core.pipeline.actions.LoaderSimpleActionPlugin.execute_simple_action","title":"<code>execute_simple_action(selection, form_values)</code>  <code>abstractmethod</code>","text":"<p>Process action based on selection.</p> <p>Parameters:</p> Name Type Description Default <code>selection</code> <code>LoaderActionSelection</code> <p>Selection information.</p> required <code>form_values</code> <code>dict[str, Any]</code> <p>Values from a form if there are any.</p> required <p>Returns:</p> Type Description <code>Optional[LoaderActionResult]</code> <p>Optional[LoaderActionResult]: Result of the action.</p> Source code in <code>client/ayon_core/pipeline/actions/loader.py</code> <pre><code>@abstractmethod\ndef execute_simple_action(\n    self,\n    selection: LoaderActionSelection,\n    form_values: dict[str, Any],\n) -&gt; Optional[LoaderActionResult]:\n    \"\"\"Process action based on selection.\n\n    Args:\n        selection (LoaderActionSelection): Selection information.\n        form_values (dict[str, Any]): Values from a form if there are any.\n\n    Returns:\n        Optional[LoaderActionResult]: Result of the action.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/index.html#client.ayon_core.pipeline.actions.LoaderSimpleActionPlugin.is_compatible","title":"<code>is_compatible(selection)</code>  <code>abstractmethod</code>","text":"<p>Check if plugin is compatible with selection.</p> <p>Parameters:</p> Name Type Description Default <code>selection</code> <code>LoaderActionSelection</code> <p>Selection information.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if plugin is compatible with selection.</p> Source code in <code>client/ayon_core/pipeline/actions/loader.py</code> <pre><code>@abstractmethod\ndef is_compatible(self, selection: LoaderActionSelection) -&gt; bool:\n    \"\"\"Check if plugin is compatible with selection.\n\n    Args:\n        selection (LoaderActionSelection): Selection information.\n\n    Returns:\n        bool: True if plugin is compatible with selection.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/index.html#client.ayon_core.pipeline.actions.SelectionEntitiesCache","title":"<code>SelectionEntitiesCache</code>","text":"<p>Cache of entities used as helper in the selection wrapper.</p> <p>It is possible to get entities based on ids with helper methods to get     entities, their parents or their children's entities.</p> <p>The goal is to avoid multiple API calls for the same entity in multiple     action plugins.</p> <p>The cache is based on the selected project. Entities are fetched     if are not in cache yet.</p> Source code in <code>client/ayon_core/pipeline/actions/loader.py</code> <pre><code>class SelectionEntitiesCache:\n    \"\"\"Cache of entities used as helper in the selection wrapper.\n\n    It is possible to get entities based on ids with helper methods to get\n        entities, their parents or their children's entities.\n\n    The goal is to avoid multiple API calls for the same entity in multiple\n        action plugins.\n\n    The cache is based on the selected project. Entities are fetched\n        if are not in cache yet.\n    \"\"\"\n    def __init__(\n        self,\n        project_name: str,\n        project_entity: Optional[dict[str, Any]] = None,\n        folders_by_id: Optional[dict[str, dict[str, Any]]] = None,\n        tasks_by_id: Optional[dict[str, dict[str, Any]]] = None,\n        products_by_id: Optional[dict[str, dict[str, Any]]] = None,\n        versions_by_id: Optional[dict[str, dict[str, Any]]] = None,\n        representations_by_id: Optional[dict[str, dict[str, Any]]] = None,\n        task_ids_by_folder_id: Optional[dict[str, set[str]]] = None,\n        product_ids_by_folder_id: Optional[dict[str, set[str]]] = None,\n        version_ids_by_product_id: Optional[dict[str, set[str]]] = None,\n        representation_ids_by_version_id: Optional[dict[str, set[str]]] = None,\n    ):\n        self._project_name = project_name\n        self._project_entity = project_entity\n        self._folders_by_id = folders_by_id or {}\n        self._tasks_by_id = tasks_by_id or {}\n        self._products_by_id = products_by_id or {}\n        self._versions_by_id = versions_by_id or {}\n        self._representations_by_id = representations_by_id or {}\n\n        self._task_ids_by_folder_id = task_ids_by_folder_id or {}\n        self._product_ids_by_folder_id = product_ids_by_folder_id or {}\n        self._version_ids_by_product_id = version_ids_by_product_id or {}\n        self._representation_ids_by_version_id = (\n            representation_ids_by_version_id or {}\n        )\n\n    def get_project(self) -&gt; dict[str, Any]:\n        \"\"\"Get project entity\"\"\"\n        if self._project_entity is None:\n            self._project_entity = ayon_api.get_project(self._project_name)\n        return copy.deepcopy(self._project_entity)\n\n    def get_folders(\n        self, folder_ids: set[str]\n    ) -&gt; list[dict[str, Any]]:\n        return self._get_entities(\n            folder_ids,\n            self._folders_by_id,\n            \"folder_ids\",\n            ayon_api.get_folders,\n        )\n\n    def get_tasks(\n        self, task_ids: set[str]\n    ) -&gt; list[dict[str, Any]]:\n        return self._get_entities(\n            task_ids,\n            self._tasks_by_id,\n            \"task_ids\",\n            ayon_api.get_tasks,\n        )\n\n    def get_products(\n        self, product_ids: set[str]\n    ) -&gt; list[dict[str, Any]]:\n        return self._get_entities(\n            product_ids,\n            self._products_by_id,\n            \"product_ids\",\n            ayon_api.get_products,\n        )\n\n    def get_versions(\n        self, version_ids: set[str]\n    ) -&gt; list[dict[str, Any]]:\n        return self._get_entities(\n            version_ids,\n            self._versions_by_id,\n            \"version_ids\",\n            ayon_api.get_versions,\n        )\n\n    def get_representations(\n        self, representation_ids: set[str]\n    ) -&gt; list[dict[str, Any]]:\n        return self._get_entities(\n            representation_ids,\n            self._representations_by_id,\n            \"representation_ids\",\n            ayon_api.get_representations,\n        )\n\n    def get_folders_tasks(\n        self, folder_ids: set[str]\n    ) -&gt; list[dict[str, Any]]:\n        task_ids = self._fill_parent_children_ids(\n            folder_ids,\n            \"folderId\",\n            \"folder_ids\",\n            self._task_ids_by_folder_id,\n            ayon_api.get_tasks,\n        )\n        return self.get_tasks(task_ids)\n\n    def get_folders_products(\n        self, folder_ids: set[str]\n    ) -&gt; list[dict[str, Any]]:\n        product_ids = self._get_folders_products_ids(folder_ids)\n        return self.get_products(product_ids)\n\n    def get_tasks_versions(\n        self, task_ids: set[str]\n    ) -&gt; list[dict[str, Any]]:\n        folder_ids = {\n            task[\"folderId\"]\n            for task in self.get_tasks(task_ids)\n        }\n        product_ids = self._get_folders_products_ids(folder_ids)\n        output = []\n        for version in self.get_products_versions(product_ids):\n            task_id = version[\"taskId\"]\n            if task_id in task_ids:\n                output.append(version)\n        return output\n\n    def get_products_versions(\n        self, product_ids: set[str]\n    ) -&gt; list[dict[str, Any]]:\n        version_ids = self._fill_parent_children_ids(\n            product_ids,\n            \"productId\",\n            \"product_ids\",\n            self._version_ids_by_product_id,\n            ayon_api.get_versions,\n        )\n        return self.get_versions(version_ids)\n\n    def get_versions_representations(\n        self, version_ids: set[str]\n    ) -&gt; list[dict[str, Any]]:\n        repre_ids = self._fill_parent_children_ids(\n            version_ids,\n            \"versionId\",\n            \"version_ids\",\n            self._representation_ids_by_version_id,\n            ayon_api.get_representations,\n        )\n        return self.get_representations(repre_ids)\n\n    def get_tasks_folders(self, task_ids: set[str]) -&gt; list[dict[str, Any]]:\n        folder_ids = {\n            task[\"folderId\"]\n            for task in self.get_tasks(task_ids)\n        }\n        return self.get_folders(folder_ids)\n\n    def get_products_folders(\n        self, product_ids: set[str]\n    ) -&gt; list[dict[str, Any]]:\n        folder_ids = {\n            product[\"folderId\"]\n            for product in self.get_products(product_ids)\n        }\n        return self.get_folders(folder_ids)\n\n    def get_versions_products(\n        self, version_ids: set[str]\n    ) -&gt; list[dict[str, Any]]:\n        product_ids = {\n            version[\"productId\"]\n            for version in self.get_versions(version_ids)\n        }\n        return self.get_products(product_ids)\n\n    def get_versions_tasks(\n        self, version_ids: set[str]\n    ) -&gt; list[dict[str, Any]]:\n        task_ids = {\n            version[\"taskId\"]\n            for version in self.get_versions(version_ids)\n            if version[\"taskId\"]\n        }\n        return self.get_tasks(task_ids)\n\n    def get_representations_versions(\n        self, representation_ids: set[str]\n    ) -&gt; list[dict[str, Any]]:\n        version_ids = {\n            repre[\"versionId\"]\n            for repre in self.get_representations(representation_ids)\n        }\n        return self.get_versions(version_ids)\n\n    def _get_folders_products_ids(self, folder_ids: set[str]) -&gt; set[str]:\n        return self._fill_parent_children_ids(\n            folder_ids,\n            \"folderId\",\n            \"folder_ids\",\n            self._product_ids_by_folder_id,\n            ayon_api.get_products,\n        )\n\n    def _fill_parent_children_ids(\n        self,\n        entity_ids: set[str],\n        parent_key: str,\n        filter_attr: str,\n        parent_mapping: dict[str, set[str]],\n        getter: Callable,\n    ) -&gt; set[str]:\n        if not entity_ids:\n            return set()\n        children_ids = set()\n        missing_ids = set()\n        for entity_id in entity_ids:\n            _children_ids = parent_mapping.get(entity_id)\n            if _children_ids is None:\n                missing_ids.add(entity_id)\n            else:\n                children_ids.update(_children_ids)\n        if missing_ids:\n            entities_by_parent_id = collections.defaultdict(set)\n            for entity in getter(\n                self._project_name,\n                fields={\"id\", parent_key},\n                **{filter_attr: missing_ids},\n            ):\n                child_id = entity[\"id\"]\n                children_ids.add(child_id)\n                entities_by_parent_id[entity[parent_key]].add(child_id)\n\n            for entity_id in missing_ids:\n                parent_mapping[entity_id] = entities_by_parent_id[entity_id]\n\n        return children_ids\n\n    def _get_entities(\n        self,\n        entity_ids: set[str],\n        cache_var: dict[str, Any],\n        filter_arg: str,\n        getter: Callable,\n    ) -&gt; list[dict[str, Any]]:\n        if not entity_ids:\n            return []\n\n        output = []\n        missing_ids: set[str] = set()\n        for entity_id in entity_ids:\n            entity = cache_var.get(entity_id)\n            if entity_id not in cache_var:\n                missing_ids.add(entity_id)\n                cache_var[entity_id] = None\n            elif entity:\n                output.append(entity)\n\n        if missing_ids:\n            for entity in getter(\n                self._project_name,\n                **{filter_arg: missing_ids}\n            ):\n                output.append(entity)\n                cache_var[entity[\"id\"]] = entity\n        return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/index.html#client.ayon_core.pipeline.actions.SelectionEntitiesCache.get_project","title":"<code>get_project()</code>","text":"<p>Get project entity</p> Source code in <code>client/ayon_core/pipeline/actions/loader.py</code> <pre><code>def get_project(self) -&gt; dict[str, Any]:\n    \"\"\"Get project entity\"\"\"\n    if self._project_entity is None:\n        self._project_entity = ayon_api.get_project(self._project_name)\n    return copy.deepcopy(self._project_entity)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/index.html#client.ayon_core.pipeline.actions.webaction_fields_to_attribute_defs","title":"<code>webaction_fields_to_attribute_defs(fields)</code>","text":"<p>Helper function to convert fields definition from webactions form.</p> <p>Convert form fields to attribute definitions to be able to display them     using attribute definitions.</p> <p>Parameters:</p> Name Type Description Default <code>fields</code> <code>list[dict[str, Any]]</code> <p>Fields from webaction form.</p> required <p>Returns:</p> Type Description <code>list[AbstractAttrDef]</code> <p>list[AbstractAttrDef]: Converted attribute definitions.</p> Source code in <code>client/ayon_core/pipeline/actions/utils.py</code> <pre><code>def webaction_fields_to_attribute_defs(\n    fields: list[dict[str, Any]]\n) -&gt; list[AbstractAttrDef]:\n    \"\"\"Helper function to convert fields definition from webactions form.\n\n    Convert form fields to attribute definitions to be able to display them\n        using attribute definitions.\n\n    Args:\n        fields (list[dict[str, Any]]): Fields from webaction form.\n\n    Returns:\n        list[AbstractAttrDef]: Converted attribute definitions.\n\n    \"\"\"\n    attr_defs = []\n    for field in fields:\n        field_type = field[\"type\"]\n        attr_def = None\n        if field_type == \"label\":\n            label = field.get(\"value\")\n            if label is None:\n                label = field.get(\"text\")\n            attr_def = UILabelDef(\n                label, key=uuid.uuid4().hex\n            )\n        elif field_type == \"boolean\":\n            value = field[\"value\"]\n            if isinstance(value, str):\n                value = value.lower() == \"true\"\n\n            attr_def = BoolDef(\n                field[\"name\"],\n                default=value,\n                label=field.get(\"label\"),\n            )\n        elif field_type == \"text\":\n            attr_def = TextDef(\n                field[\"name\"],\n                default=field.get(\"value\"),\n                label=field.get(\"label\"),\n                placeholder=field.get(\"placeholder\"),\n                multiline=field.get(\"multiline\", False),\n                regex=field.get(\"regex\"),\n                # syntax=field[\"syntax\"],\n            )\n        elif field_type in (\"integer\", \"float\"):\n            value = field.get(\"value\")\n            if isinstance(value, str):\n                if field_type == \"integer\":\n                    value = int(value)\n                else:\n                    value = float(value)\n            attr_def = NumberDef(\n                field[\"name\"],\n                default=value,\n                label=field.get(\"label\"),\n                decimals=0 if field_type == \"integer\" else 5,\n                # placeholder=field.get(\"placeholder\"),\n                minimum=field.get(\"min\"),\n                maximum=field.get(\"max\"),\n            )\n        elif field_type in (\"select\", \"multiselect\"):\n            attr_def = EnumDef(\n                field[\"name\"],\n                items=field[\"options\"],\n                default=field.get(\"value\"),\n                label=field.get(\"label\"),\n                multiselection=field_type == \"multiselect\",\n            )\n        elif field_type == \"hidden\":\n            attr_def = HiddenDef(\n                field[\"name\"],\n                default=field.get(\"value\"),\n            )\n\n        if attr_def is None:\n            print(f\"Unknown config field type: {field_type}\")\n            attr_def = UILabelDef(\n                f\"Unknown field type '{field_type}\",\n                key=uuid.uuid4().hex\n            )\n        attr_defs.append(attr_def)\n    return attr_defs\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/inventory.html","title":"inventory","text":""},{"location":"autoapi/client/ayon_core/pipeline/actions/inventory.html#client.ayon_core.pipeline.actions.inventory.InventoryAction","title":"<code>InventoryAction</code>","text":"<p>A custom action for the scene inventory tool</p> <p>If registered the action will be visible in the Right Mouse Button menu under the submenu \"Actions\".</p> Source code in <code>client/ayon_core/pipeline/actions/inventory.py</code> <pre><code>class InventoryAction:\n    \"\"\"A custom action for the scene inventory tool\n\n    If registered the action will be visible in the Right Mouse Button menu\n    under the submenu \"Actions\".\n\n    \"\"\"\n\n    label = None\n    icon = None\n    color = None\n    order = 0\n\n    log = logging.getLogger(\"InventoryAction\")\n    log.propagate = True\n\n    @staticmethod\n    def is_compatible(container):\n        \"\"\"Override function in a custom class\n\n        This method is specifically used to ensure the action can operate on\n        the container.\n\n        Args:\n            container(dict): the data of a loaded asset, see host.ls()\n\n        Returns:\n            bool\n        \"\"\"\n        return bool(container.get(\"objectName\"))\n\n    def process(self, containers):\n        \"\"\"Override function in a custom class\n\n        This method will receive all containers even those which are\n        incompatible. It is advised to create a small filter along the lines\n        of this example:\n\n        valid_containers = filter(self.is_compatible(c) for c in containers)\n\n        The return value will need to be a True-ish value to trigger\n        the data_changed signal in order to refresh the view.\n\n        You can return a list of container names to trigger GUI to select\n        treeview items.\n\n        You can return a dict to carry extra GUI options. For example:\n            {\n                \"objectNames\": [container names...],\n                \"options\": {\"mode\": \"toggle\",\n                            \"clear\": False}\n            }\n        Currently workable GUI options are:\n            - clear (bool): Clear current selection before selecting by action.\n                            Default `True`.\n            - mode (str): selection mode, use one of these:\n                          \"select\", \"deselect\", \"toggle\". Default is \"select\".\n\n        Args:\n            containers (list): list of dictionaries\n\n        Return:\n            bool, list or dict\n\n        \"\"\"\n        return True\n\n    @classmethod\n    def filepath_from_context(cls, context):\n        return get_representation_path_from_context(context)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/inventory.html#client.ayon_core.pipeline.actions.inventory.InventoryAction.is_compatible","title":"<code>is_compatible(container)</code>  <code>staticmethod</code>","text":"<p>Override function in a custom class</p> <p>This method is specifically used to ensure the action can operate on the container.</p> <p>Parameters:</p> Name Type Description Default <code>container</code> <code>dict</code> <p>the data of a loaded asset, see host.ls()</p> required <p>Returns:</p> Type Description <p>bool</p> Source code in <code>client/ayon_core/pipeline/actions/inventory.py</code> <pre><code>@staticmethod\ndef is_compatible(container):\n    \"\"\"Override function in a custom class\n\n    This method is specifically used to ensure the action can operate on\n    the container.\n\n    Args:\n        container(dict): the data of a loaded asset, see host.ls()\n\n    Returns:\n        bool\n    \"\"\"\n    return bool(container.get(\"objectName\"))\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/inventory.html#client.ayon_core.pipeline.actions.inventory.InventoryAction.process","title":"<code>process(containers)</code>","text":"<p>Override function in a custom class</p> <p>This method will receive all containers even those which are incompatible. It is advised to create a small filter along the lines of this example:</p> <p>valid_containers = filter(self.is_compatible(c) for c in containers)</p> <p>The return value will need to be a True-ish value to trigger the data_changed signal in order to refresh the view.</p> <p>You can return a list of container names to trigger GUI to select treeview items.</p> <p>You can return a dict to carry extra GUI options. For example:     {         \"objectNames\": [container names...],         \"options\": {\"mode\": \"toggle\",                     \"clear\": False}     } Currently workable GUI options are:     - clear (bool): Clear current selection before selecting by action.                     Default <code>True</code>.     - mode (str): selection mode, use one of these:                   \"select\", \"deselect\", \"toggle\". Default is \"select\".</p> <p>Parameters:</p> Name Type Description Default <code>containers</code> <code>list</code> <p>list of dictionaries</p> required Return <p>bool, list or dict</p> Source code in <code>client/ayon_core/pipeline/actions/inventory.py</code> <pre><code>def process(self, containers):\n    \"\"\"Override function in a custom class\n\n    This method will receive all containers even those which are\n    incompatible. It is advised to create a small filter along the lines\n    of this example:\n\n    valid_containers = filter(self.is_compatible(c) for c in containers)\n\n    The return value will need to be a True-ish value to trigger\n    the data_changed signal in order to refresh the view.\n\n    You can return a list of container names to trigger GUI to select\n    treeview items.\n\n    You can return a dict to carry extra GUI options. For example:\n        {\n            \"objectNames\": [container names...],\n            \"options\": {\"mode\": \"toggle\",\n                        \"clear\": False}\n        }\n    Currently workable GUI options are:\n        - clear (bool): Clear current selection before selecting by action.\n                        Default `True`.\n        - mode (str): selection mode, use one of these:\n                      \"select\", \"deselect\", \"toggle\". Default is \"select\".\n\n    Args:\n        containers (list): list of dictionaries\n\n    Return:\n        bool, list or dict\n\n    \"\"\"\n    return True\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/launcher.html","title":"launcher","text":""},{"location":"autoapi/client/ayon_core/pipeline/actions/launcher.html#client.ayon_core.pipeline.actions.launcher.LauncherAction","title":"<code>LauncherAction</code>","text":"<p>               Bases: <code>object</code></p> <p>A custom action available</p> Source code in <code>client/ayon_core/pipeline/actions/launcher.py</code> <pre><code>class LauncherAction(object):\n    \"\"\"A custom action available\"\"\"\n    name = None\n    label = None\n    icon = None\n    color = None\n    order = 0\n\n    log = logging.getLogger(\"LauncherAction\")\n    log.propagate = True\n\n    def is_compatible(self, selection):\n        \"\"\"Return whether the class is compatible with the Session.\n\n        Args:\n            selection (LauncherActionSelection): Data with selection.\n\n        \"\"\"\n        return True\n\n    def process(self, selection, **kwargs):\n        \"\"\"Process the action.\n\n        Args:\n            selection (LauncherActionSelection): Data with selection.\n            **kwargs: Additional arguments.\n\n        \"\"\"\n        pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/launcher.html#client.ayon_core.pipeline.actions.launcher.LauncherAction.is_compatible","title":"<code>is_compatible(selection)</code>","text":"<p>Return whether the class is compatible with the Session.</p> <p>Parameters:</p> Name Type Description Default <code>selection</code> <code>LauncherActionSelection</code> <p>Data with selection.</p> required Source code in <code>client/ayon_core/pipeline/actions/launcher.py</code> <pre><code>def is_compatible(self, selection):\n    \"\"\"Return whether the class is compatible with the Session.\n\n    Args:\n        selection (LauncherActionSelection): Data with selection.\n\n    \"\"\"\n    return True\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/launcher.html#client.ayon_core.pipeline.actions.launcher.LauncherAction.process","title":"<code>process(selection, **kwargs)</code>","text":"<p>Process the action.</p> <p>Parameters:</p> Name Type Description Default <code>selection</code> <code>LauncherActionSelection</code> <p>Data with selection.</p> required <code>**kwargs</code> <p>Additional arguments.</p> <code>{}</code> Source code in <code>client/ayon_core/pipeline/actions/launcher.py</code> <pre><code>def process(self, selection, **kwargs):\n    \"\"\"Process the action.\n\n    Args:\n        selection (LauncherActionSelection): Data with selection.\n        **kwargs: Additional arguments.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/launcher.html#client.ayon_core.pipeline.actions.launcher.LauncherActionSelection","title":"<code>LauncherActionSelection</code>","text":"<p>Object helper to pass selection to actions.</p> <p>Object support backwards compatibility for 'session' from OpenPype where environment variable keys were used to define selection.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Selected project name.</p> required <code>folder_id</code> <code>str</code> <p>Selected folder id.</p> required <code>task_id</code> <code>str</code> <p>Selected task id.</p> required <code>folder_path</code> <code>Optional[str]</code> <p>Selected folder path.</p> <code>None</code> <code>task_name</code> <code>Optional[str]</code> <p>Selected task name.</p> <code>None</code> <code>project_entity</code> <code>Optional[dict[str, Any]]</code> <p>Project entity.</p> <code>None</code> <code>folder_entity</code> <code>Optional[dict[str, Any]]</code> <p>Folder entity.</p> <code>None</code> <code>task_entity</code> <code>Optional[dict[str, Any]]</code> <p>Task entity.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/actions/launcher.py</code> <pre><code>class LauncherActionSelection:\n    \"\"\"Object helper to pass selection to actions.\n\n    Object support backwards compatibility for 'session' from OpenPype where\n    environment variable keys were used to define selection.\n\n    Args:\n        project_name (str): Selected project name.\n        folder_id (str): Selected folder id.\n        task_id (str): Selected task id.\n        folder_path (Optional[str]): Selected folder path.\n        task_name (Optional[str]): Selected task name.\n        project_entity (Optional[dict[str, Any]]): Project entity.\n        folder_entity (Optional[dict[str, Any]]): Folder entity.\n        task_entity (Optional[dict[str, Any]]): Task entity.\n\n    \"\"\"\n    def __init__(\n        self,\n        project_name,\n        folder_id,\n        task_id,\n        workfile_id,\n        folder_path=None,\n        task_name=None,\n        project_entity=None,\n        folder_entity=None,\n        task_entity=None,\n        workfile_entity=None,\n        project_settings=None,\n    ):\n        self._project_name = project_name\n        self._folder_id = folder_id\n        self._task_id = task_id\n        self._workfile_id = workfile_id\n\n        self._folder_path = folder_path\n        self._task_name = task_name\n\n        self._project_entity = project_entity\n        self._folder_entity = folder_entity\n        self._task_entity = task_entity\n        self._workfile_entity = workfile_entity\n\n        self._project_settings = project_settings\n\n    def __getitem__(self, key):\n        warnings.warn(\n            (\n                \"Using deprecated access to selection data. Please use\"\n                \" attributes and methods\"\n                \" defined by 'LauncherActionSelection'.\"\n            ),\n            category=DeprecationWarning\n        )\n        if key == \"AYON_PROJECT_NAME\":\n            return self.project_name\n        if key == \"AYON_FOLDER_PATH\":\n            return self.folder_path\n        if key == \"AYON_TASK_NAME\":\n            return self.task_name\n        raise KeyError(f\"Key: {key} not found\")\n\n    def __iter__(self):\n        for key in self.keys():\n            yield key\n\n    def __contains__(self, key):\n        warnings.warn(\n            (\n                \"Using deprecated access to selection data. Please use\"\n                \" attributes and methods\"\n                \" defined by 'LauncherActionSelection'.\"\n            ),\n            category=DeprecationWarning\n        )\n        # Fake missing keys check for backwards compatibility\n        if key == \"AYON_PROJECT_NAME\":\n            return self._project_name is not None\n        if key == \"AYON_FOLDER_PATH\":\n            return self._folder_id is not None\n        if key == \"AYON_TASK_NAME\":\n            return self._task_id is not None\n        return False\n\n    def get(self, key, default=None):\n        \"\"\"\n\n        Deprecated:\n            Added for backwards compatibility with older actions.\n\n        \"\"\"\n        warnings.warn(\n            (\n                \"Using deprecated access to selection data. Please use\"\n                \" attributes and methods\"\n                \" defined by 'LauncherActionSelection'.\"\n            ),\n            category=DeprecationWarning\n        )\n        try:\n            return self[key]\n        except KeyError:\n            return default\n\n    def items(self):\n        \"\"\"\n\n        Deprecated:\n            Added for backwards compatibility with older actions.\n\n        \"\"\"\n        for key, value in (\n            (\"AYON_PROJECT_NAME\", self.project_name),\n            (\"AYON_FOLDER_PATH\", self.folder_path),\n            (\"AYON_TASK_NAME\", self.task_name),\n        ):\n            if value is not None:\n                yield (key, value)\n\n    def keys(self):\n        \"\"\"\n\n        Deprecated:\n            Added for backwards compatibility with older actions.\n\n        \"\"\"\n        for key, _ in self.items():\n            yield key\n\n    def values(self):\n        \"\"\"\n\n        Deprecated:\n            Added for backwards compatibility with older actions.\n\n        \"\"\"\n        for _, value in self.items():\n            yield value\n\n    def get_project_name(self):\n        \"\"\"Selected project name.\n\n        Returns:\n            Union[str, None]: Selected project name.\n\n        \"\"\"\n        return self._project_name\n\n    def get_folder_id(self):\n        \"\"\"Selected folder id.\n\n        Returns:\n            Union[str, None]: Selected folder id.\n\n        \"\"\"\n        return self._folder_id\n\n    def get_folder_path(self):\n        \"\"\"Selected folder path.\n\n        Returns:\n            Union[str, None]: Selected folder path.\n\n        \"\"\"\n        if self._folder_id is None:\n            return None\n        if self._folder_path is None:\n            self._folder_path = self.folder_entity[\"path\"]\n        return self._folder_path\n\n    def get_task_id(self):\n        \"\"\"Selected task id.\n\n        Returns:\n            Union[str, None]: Selected task id.\n\n        \"\"\"\n        return self._task_id\n\n    def get_task_name(self):\n        \"\"\"Selected task name.\n\n        Returns:\n            Union[str, None]: Selected task name.\n\n        \"\"\"\n        if self._task_id is None:\n            return None\n        if self._task_name is None:\n            self._task_name = self.task_entity[\"name\"]\n        return self._task_name\n\n    def get_workfile_id(self):\n        \"\"\"Selected workfile id.\n\n        Returns:\n            Union[str, None]: Selected workfile id.\n\n        \"\"\"\n        return self._workfile_id\n\n    def get_project_entity(self):\n        \"\"\"Project entity for the selection.\n\n        Returns:\n            Union[dict[str, Any], None]: Project entity.\n\n        \"\"\"\n        if self._project_name is None:\n            return None\n        if self._project_entity is None:\n            self._project_entity = ayon_api.get_project(self._project_name)\n        return self._project_entity\n\n    def get_folder_entity(self):\n        \"\"\"Folder entity for the selection.\n\n        Returns:\n            Union[dict[str, Any], None]: Folder entity.\n\n        \"\"\"\n        if self._project_name is None or self._folder_id is None:\n            return None\n        if self._folder_entity is None:\n            self._folder_entity = ayon_api.get_folder_by_id(\n                self._project_name, self._folder_id\n            )\n        return self._folder_entity\n\n    def get_task_entity(self):\n        \"\"\"Task entity for the selection.\n\n        Returns:\n            Union[dict[str, Any], None]: Task entity.\n\n        \"\"\"\n        if (\n            self._project_name is None\n            or self._task_id is None\n        ):\n            return None\n        if self._task_entity is None:\n            self._task_entity = ayon_api.get_task_by_id(\n                self._project_name, self._task_id\n            )\n        return self._task_entity\n\n    def get_workfile_entity(self):\n        \"\"\"Workfile entity for the selection.\n\n        Returns:\n            Union[dict[str, Any], None]: Workfile entity.\n\n        \"\"\"\n        if (\n            self._project_name is None\n            or self._workfile_id is None\n        ):\n            return None\n        if self._workfile_entity is None:\n            self._workfile_entity = ayon_api.get_workfile_info_by_id(\n                self._project_name, self._workfile_id\n            )\n        return self._workfile_entity\n\n    def get_project_settings(self):\n        \"\"\"Project settings for the selection.\n\n        Returns:\n            dict[str, Any]: Project settings or studio settings if\n                project is not selected.\n\n        \"\"\"\n        if self._project_settings is None:\n            if self._project_name is None:\n                settings = get_studio_settings()\n            else:\n                settings = get_project_settings(self._project_name)\n            self._project_settings = settings\n        return self._project_settings\n\n    @property\n    def is_project_selected(self):\n        \"\"\"Return whether a project is selected.\n\n        Returns:\n            bool: Whether a project is selected.\n\n        \"\"\"\n        return self._project_name is not None\n\n    @property\n    def is_folder_selected(self):\n        \"\"\"Return whether a folder is selected.\n\n        Returns:\n            bool: Whether a folder is selected.\n\n        \"\"\"\n        return self._folder_id is not None\n\n    @property\n    def is_task_selected(self):\n        \"\"\"Return whether a task is selected.\n\n        Returns:\n            bool: Whether a task is selected.\n\n        \"\"\"\n        return self._task_id is not None\n\n    @property\n    def is_workfile_selected(self):\n        \"\"\"Return whether a task is selected.\n\n        Returns:\n            bool: Whether a task is selected.\n\n        \"\"\"\n        return self._workfile_id is not None\n\n    project_name = property(get_project_name)\n    folder_id = property(get_folder_id)\n    task_id = property(get_task_id)\n    workfile_id = property(get_workfile_id)\n    folder_path = property(get_folder_path)\n    task_name = property(get_task_name)\n\n    project_entity = property(get_project_entity)\n    folder_entity = property(get_folder_entity)\n    task_entity = property(get_task_entity)\n    workfile_entity = property(get_workfile_entity)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/launcher.html#client.ayon_core.pipeline.actions.launcher.LauncherActionSelection.is_folder_selected","title":"<code>is_folder_selected</code>  <code>property</code>","text":"<p>Return whether a folder is selected.</p> <p>Returns:</p> Name Type Description <code>bool</code> <p>Whether a folder is selected.</p>"},{"location":"autoapi/client/ayon_core/pipeline/actions/launcher.html#client.ayon_core.pipeline.actions.launcher.LauncherActionSelection.is_project_selected","title":"<code>is_project_selected</code>  <code>property</code>","text":"<p>Return whether a project is selected.</p> <p>Returns:</p> Name Type Description <code>bool</code> <p>Whether a project is selected.</p>"},{"location":"autoapi/client/ayon_core/pipeline/actions/launcher.html#client.ayon_core.pipeline.actions.launcher.LauncherActionSelection.is_task_selected","title":"<code>is_task_selected</code>  <code>property</code>","text":"<p>Return whether a task is selected.</p> <p>Returns:</p> Name Type Description <code>bool</code> <p>Whether a task is selected.</p>"},{"location":"autoapi/client/ayon_core/pipeline/actions/launcher.html#client.ayon_core.pipeline.actions.launcher.LauncherActionSelection.is_workfile_selected","title":"<code>is_workfile_selected</code>  <code>property</code>","text":"<p>Return whether a task is selected.</p> <p>Returns:</p> Name Type Description <code>bool</code> <p>Whether a task is selected.</p>"},{"location":"autoapi/client/ayon_core/pipeline/actions/launcher.html#client.ayon_core.pipeline.actions.launcher.LauncherActionSelection.get","title":"<code>get(key, default=None)</code>","text":"Deprecated <p>Added for backwards compatibility with older actions.</p> Source code in <code>client/ayon_core/pipeline/actions/launcher.py</code> <pre><code>def get(self, key, default=None):\n    \"\"\"\n\n    Deprecated:\n        Added for backwards compatibility with older actions.\n\n    \"\"\"\n    warnings.warn(\n        (\n            \"Using deprecated access to selection data. Please use\"\n            \" attributes and methods\"\n            \" defined by 'LauncherActionSelection'.\"\n        ),\n        category=DeprecationWarning\n    )\n    try:\n        return self[key]\n    except KeyError:\n        return default\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/launcher.html#client.ayon_core.pipeline.actions.launcher.LauncherActionSelection.get_folder_entity","title":"<code>get_folder_entity()</code>","text":"<p>Folder entity for the selection.</p> <p>Returns:</p> Type Description <p>Union[dict[str, Any], None]: Folder entity.</p> Source code in <code>client/ayon_core/pipeline/actions/launcher.py</code> <pre><code>def get_folder_entity(self):\n    \"\"\"Folder entity for the selection.\n\n    Returns:\n        Union[dict[str, Any], None]: Folder entity.\n\n    \"\"\"\n    if self._project_name is None or self._folder_id is None:\n        return None\n    if self._folder_entity is None:\n        self._folder_entity = ayon_api.get_folder_by_id(\n            self._project_name, self._folder_id\n        )\n    return self._folder_entity\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/launcher.html#client.ayon_core.pipeline.actions.launcher.LauncherActionSelection.get_folder_id","title":"<code>get_folder_id()</code>","text":"<p>Selected folder id.</p> <p>Returns:</p> Type Description <p>Union[str, None]: Selected folder id.</p> Source code in <code>client/ayon_core/pipeline/actions/launcher.py</code> <pre><code>def get_folder_id(self):\n    \"\"\"Selected folder id.\n\n    Returns:\n        Union[str, None]: Selected folder id.\n\n    \"\"\"\n    return self._folder_id\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/launcher.html#client.ayon_core.pipeline.actions.launcher.LauncherActionSelection.get_folder_path","title":"<code>get_folder_path()</code>","text":"<p>Selected folder path.</p> <p>Returns:</p> Type Description <p>Union[str, None]: Selected folder path.</p> Source code in <code>client/ayon_core/pipeline/actions/launcher.py</code> <pre><code>def get_folder_path(self):\n    \"\"\"Selected folder path.\n\n    Returns:\n        Union[str, None]: Selected folder path.\n\n    \"\"\"\n    if self._folder_id is None:\n        return None\n    if self._folder_path is None:\n        self._folder_path = self.folder_entity[\"path\"]\n    return self._folder_path\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/launcher.html#client.ayon_core.pipeline.actions.launcher.LauncherActionSelection.get_project_entity","title":"<code>get_project_entity()</code>","text":"<p>Project entity for the selection.</p> <p>Returns:</p> Type Description <p>Union[dict[str, Any], None]: Project entity.</p> Source code in <code>client/ayon_core/pipeline/actions/launcher.py</code> <pre><code>def get_project_entity(self):\n    \"\"\"Project entity for the selection.\n\n    Returns:\n        Union[dict[str, Any], None]: Project entity.\n\n    \"\"\"\n    if self._project_name is None:\n        return None\n    if self._project_entity is None:\n        self._project_entity = ayon_api.get_project(self._project_name)\n    return self._project_entity\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/launcher.html#client.ayon_core.pipeline.actions.launcher.LauncherActionSelection.get_project_name","title":"<code>get_project_name()</code>","text":"<p>Selected project name.</p> <p>Returns:</p> Type Description <p>Union[str, None]: Selected project name.</p> Source code in <code>client/ayon_core/pipeline/actions/launcher.py</code> <pre><code>def get_project_name(self):\n    \"\"\"Selected project name.\n\n    Returns:\n        Union[str, None]: Selected project name.\n\n    \"\"\"\n    return self._project_name\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/launcher.html#client.ayon_core.pipeline.actions.launcher.LauncherActionSelection.get_project_settings","title":"<code>get_project_settings()</code>","text":"<p>Project settings for the selection.</p> <p>Returns:</p> Type Description <p>dict[str, Any]: Project settings or studio settings if project is not selected.</p> Source code in <code>client/ayon_core/pipeline/actions/launcher.py</code> <pre><code>def get_project_settings(self):\n    \"\"\"Project settings for the selection.\n\n    Returns:\n        dict[str, Any]: Project settings or studio settings if\n            project is not selected.\n\n    \"\"\"\n    if self._project_settings is None:\n        if self._project_name is None:\n            settings = get_studio_settings()\n        else:\n            settings = get_project_settings(self._project_name)\n        self._project_settings = settings\n    return self._project_settings\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/launcher.html#client.ayon_core.pipeline.actions.launcher.LauncherActionSelection.get_task_entity","title":"<code>get_task_entity()</code>","text":"<p>Task entity for the selection.</p> <p>Returns:</p> Type Description <p>Union[dict[str, Any], None]: Task entity.</p> Source code in <code>client/ayon_core/pipeline/actions/launcher.py</code> <pre><code>def get_task_entity(self):\n    \"\"\"Task entity for the selection.\n\n    Returns:\n        Union[dict[str, Any], None]: Task entity.\n\n    \"\"\"\n    if (\n        self._project_name is None\n        or self._task_id is None\n    ):\n        return None\n    if self._task_entity is None:\n        self._task_entity = ayon_api.get_task_by_id(\n            self._project_name, self._task_id\n        )\n    return self._task_entity\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/launcher.html#client.ayon_core.pipeline.actions.launcher.LauncherActionSelection.get_task_id","title":"<code>get_task_id()</code>","text":"<p>Selected task id.</p> <p>Returns:</p> Type Description <p>Union[str, None]: Selected task id.</p> Source code in <code>client/ayon_core/pipeline/actions/launcher.py</code> <pre><code>def get_task_id(self):\n    \"\"\"Selected task id.\n\n    Returns:\n        Union[str, None]: Selected task id.\n\n    \"\"\"\n    return self._task_id\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/launcher.html#client.ayon_core.pipeline.actions.launcher.LauncherActionSelection.get_task_name","title":"<code>get_task_name()</code>","text":"<p>Selected task name.</p> <p>Returns:</p> Type Description <p>Union[str, None]: Selected task name.</p> Source code in <code>client/ayon_core/pipeline/actions/launcher.py</code> <pre><code>def get_task_name(self):\n    \"\"\"Selected task name.\n\n    Returns:\n        Union[str, None]: Selected task name.\n\n    \"\"\"\n    if self._task_id is None:\n        return None\n    if self._task_name is None:\n        self._task_name = self.task_entity[\"name\"]\n    return self._task_name\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/launcher.html#client.ayon_core.pipeline.actions.launcher.LauncherActionSelection.get_workfile_entity","title":"<code>get_workfile_entity()</code>","text":"<p>Workfile entity for the selection.</p> <p>Returns:</p> Type Description <p>Union[dict[str, Any], None]: Workfile entity.</p> Source code in <code>client/ayon_core/pipeline/actions/launcher.py</code> <pre><code>def get_workfile_entity(self):\n    \"\"\"Workfile entity for the selection.\n\n    Returns:\n        Union[dict[str, Any], None]: Workfile entity.\n\n    \"\"\"\n    if (\n        self._project_name is None\n        or self._workfile_id is None\n    ):\n        return None\n    if self._workfile_entity is None:\n        self._workfile_entity = ayon_api.get_workfile_info_by_id(\n            self._project_name, self._workfile_id\n        )\n    return self._workfile_entity\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/launcher.html#client.ayon_core.pipeline.actions.launcher.LauncherActionSelection.get_workfile_id","title":"<code>get_workfile_id()</code>","text":"<p>Selected workfile id.</p> <p>Returns:</p> Type Description <p>Union[str, None]: Selected workfile id.</p> Source code in <code>client/ayon_core/pipeline/actions/launcher.py</code> <pre><code>def get_workfile_id(self):\n    \"\"\"Selected workfile id.\n\n    Returns:\n        Union[str, None]: Selected workfile id.\n\n    \"\"\"\n    return self._workfile_id\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/launcher.html#client.ayon_core.pipeline.actions.launcher.LauncherActionSelection.items","title":"<code>items()</code>","text":"Deprecated <p>Added for backwards compatibility with older actions.</p> Source code in <code>client/ayon_core/pipeline/actions/launcher.py</code> <pre><code>def items(self):\n    \"\"\"\n\n    Deprecated:\n        Added for backwards compatibility with older actions.\n\n    \"\"\"\n    for key, value in (\n        (\"AYON_PROJECT_NAME\", self.project_name),\n        (\"AYON_FOLDER_PATH\", self.folder_path),\n        (\"AYON_TASK_NAME\", self.task_name),\n    ):\n        if value is not None:\n            yield (key, value)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/launcher.html#client.ayon_core.pipeline.actions.launcher.LauncherActionSelection.keys","title":"<code>keys()</code>","text":"Deprecated <p>Added for backwards compatibility with older actions.</p> Source code in <code>client/ayon_core/pipeline/actions/launcher.py</code> <pre><code>def keys(self):\n    \"\"\"\n\n    Deprecated:\n        Added for backwards compatibility with older actions.\n\n    \"\"\"\n    for key, _ in self.items():\n        yield key\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/launcher.html#client.ayon_core.pipeline.actions.launcher.LauncherActionSelection.values","title":"<code>values()</code>","text":"Deprecated <p>Added for backwards compatibility with older actions.</p> Source code in <code>client/ayon_core/pipeline/actions/launcher.py</code> <pre><code>def values(self):\n    \"\"\"\n\n    Deprecated:\n        Added for backwards compatibility with older actions.\n\n    \"\"\"\n    for _, value in self.items():\n        yield value\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/loader.html","title":"loader","text":"<p>API for actions for loader tool.</p> <p>Even though the api is meant for the loader tool, the api should be possible     to use in a standalone way out of the loader tool.</p> <p>To use add actions, make sure your addon does inherit from     'IPluginPaths' and implements 'get_loader_action_plugin_paths' which     returns paths to python files with loader actions.</p> <p>The plugin is used to collect available actions for the given context and to     execute them. Selection is defined with 'LoaderActionSelection' object     that also contains a cache of entities and project anatomy.</p> <p>Implementing 'get_action_items' allows the plugin to define what actions     are shown and available for the selection. Because for a single selection     can be shown multiple actions with the same action identifier, the action     items also have 'data' attribute which can be used to store additional     data for the action (they have to be json-serializable).</p> <p>The action is triggered by calling the 'execute_action' method. Which takes     the action identifier, the selection, the additional data from the action     item and form values from the form if any.</p> <p>Using 'LoaderActionResult' as the output of 'execute_action' can trigger to     show a message in UI or to show an additional form ('ActionForm')     which would retrigger the action with the values from the form on     submitting. That allows handling of multistep actions.</p> <p>It is also recommended that the plugin does override the 'identifier'     attribute. The identifier has to be unique across all plugins.     Class name is used by default.</p> The selection wrapper currently supports the following types of entity types <ul> <li>version</li> <li>representation</li> </ul> <p>It is planned to add 'folder' and 'task' selection in the future.</p> It is possible to trigger 'execute_action' without ever calling <p>'get_action_items', that can be handy in automations.</p> <p>The whole logic is wrapped into 'LoaderActionsContext'. It takes care of     the discovery of plugins and wraps the collection and execution of     action items. Method 'execute_action' on context also requires plugin     identifier.</p> <p>The flow of the logic is (in the loader tool):     1. User selects entities in the UI.     2. Right-click the selected entities.     3. Use 'LoaderActionsContext' to collect items using 'get_action_items'.     4. Show a menu (with submenus) in the UI.     5. If a user selects an action, the action is triggered using         'execute_action'.     5a. If the action returns 'LoaderActionResult', show a 'message' if it is         filled and show a form dialog if 'form' is filled.     5b. If the user submitted the form, trigger the action again with the         values from the form and repeat from 5a.</p>"},{"location":"autoapi/client/ayon_core/pipeline/actions/loader.html#client.ayon_core.pipeline.actions.loader.LoaderActionItem","title":"<code>LoaderActionItem</code>  <code>dataclass</code>","text":"<p>Item of loader action.</p> <p>Action plugins return these items as possible actions to run for a given     context.</p> <p>Because the action item can be related to a specific entity     and not the whole selection, they also have to define the entity type     and ids to be executed on.</p> <p>Attributes:</p> Name Type Description <code>label</code> <code>str</code> <p>Text shown in UI.</p> <code>order</code> <code>int</code> <p>Order of the action in UI.</p> <code>group_label</code> <code>Optional[str]</code> <p>Label of the group to which the action belongs.</p> <code>icon</code> <code>Optional[dict[str, Any]</code> <p>Icon definition.</p> <code>data</code> <code>Optional[DataType]</code> <p>Action item data.</p> <code>identifier</code> <code>Optional[str]</code> <p>Identifier of the plugin which created the action item. Is filled automatically. Is not changed if is filled -&gt; can lead to different plugin.</p> Source code in <code>client/ayon_core/pipeline/actions/loader.py</code> <pre><code>@dataclass\nclass LoaderActionItem:\n    \"\"\"Item of loader action.\n\n    Action plugins return these items as possible actions to run for a given\n        context.\n\n    Because the action item can be related to a specific entity\n        and not the whole selection, they also have to define the entity type\n        and ids to be executed on.\n\n    Attributes:\n        label (str): Text shown in UI.\n        order (int): Order of the action in UI.\n        group_label (Optional[str]): Label of the group to which the action\n            belongs.\n        icon (Optional[dict[str, Any]): Icon definition.\n        data (Optional[DataType]): Action item data.\n        identifier (Optional[str]): Identifier of the plugin which\n            created the action item. Is filled automatically. Is not changed\n            if is filled -&gt; can lead to different plugin.\n\n    \"\"\"\n    label: str\n    order: int = 0\n    group_label: Optional[str] = None\n    icon: IconBase | dict[str, Any] | None = None\n    data: Optional[DataType] = None\n    # Is filled automatically\n    identifier: str = None\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/loader.html#client.ayon_core.pipeline.actions.loader.LoaderActionPlugin","title":"<code>LoaderActionPlugin</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Plugin for loader actions.</p> <p>Plugin is responsible for getting action items and executing actions.</p> Source code in <code>client/ayon_core/pipeline/actions/loader.py</code> <pre><code>class LoaderActionPlugin(ABC):\n    \"\"\"Plugin for loader actions.\n\n    Plugin is responsible for getting action items and executing actions.\n\n    \"\"\"\n    _log: Optional[logging.Logger] = None\n    enabled: bool = True\n    skip_discovery: bool = True\n\n    def __init__(self, context: \"LoaderActionsContext\") -&gt; None:\n        self._context = context\n        self.apply_settings(context.get_studio_settings())\n\n    def apply_settings(self, studio_settings: dict[str, Any]) -&gt; None:\n        \"\"\"Apply studio settings to the plugin.\n\n        Args:\n            studio_settings (dict[str, Any]): Studio settings.\n\n        \"\"\"\n        pass\n\n    @property\n    def log(self) -&gt; logging.Logger:\n        if self._log is None:\n            self._log = Logger.get_logger(self.__class__.__name__)\n        return self._log\n\n    @property\n    def identifier(self) -&gt; str:\n        \"\"\"Identifier of the plugin.\n\n        Returns:\n            str: Plugin identifier.\n\n        \"\"\"\n        return self.__class__.__name__\n\n    @property\n    def host_name(self) -&gt; Optional[str]:\n        \"\"\"Name of the current host.\"\"\"\n        return self._context.get_host_name()\n\n    @abstractmethod\n    def get_action_items(\n        self, selection: LoaderActionSelection\n    ) -&gt; list[LoaderActionItem]:\n        \"\"\"Action items for the selection.\n\n        Args:\n            selection (LoaderActionSelection): Selection.\n\n        Returns:\n            list[LoaderActionItem]: Action items.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def execute_action(\n        self,\n        selection: LoaderActionSelection,\n        data: Optional[DataType],\n        form_values: dict[str, Any],\n    ) -&gt; Optional[LoaderActionResult]:\n        \"\"\"Execute an action.\n\n        Args:\n            selection (LoaderActionSelection): Selection wrapper. Can be used\n                to get entities or get context of original selection.\n            data (Optional[DataType]): Additional action item data.\n            form_values (dict[str, Any]): Attribute values.\n\n        Returns:\n            Optional[LoaderActionResult]: Result of the action execution.\n\n        \"\"\"\n        pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/loader.html#client.ayon_core.pipeline.actions.loader.LoaderActionPlugin.host_name","title":"<code>host_name</code>  <code>property</code>","text":"<p>Name of the current host.</p>"},{"location":"autoapi/client/ayon_core/pipeline/actions/loader.html#client.ayon_core.pipeline.actions.loader.LoaderActionPlugin.identifier","title":"<code>identifier</code>  <code>property</code>","text":"<p>Identifier of the plugin.</p> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Plugin identifier.</p>"},{"location":"autoapi/client/ayon_core/pipeline/actions/loader.html#client.ayon_core.pipeline.actions.loader.LoaderActionPlugin.apply_settings","title":"<code>apply_settings(studio_settings)</code>","text":"<p>Apply studio settings to the plugin.</p> <p>Parameters:</p> Name Type Description Default <code>studio_settings</code> <code>dict[str, Any]</code> <p>Studio settings.</p> required Source code in <code>client/ayon_core/pipeline/actions/loader.py</code> <pre><code>def apply_settings(self, studio_settings: dict[str, Any]) -&gt; None:\n    \"\"\"Apply studio settings to the plugin.\n\n    Args:\n        studio_settings (dict[str, Any]): Studio settings.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/loader.html#client.ayon_core.pipeline.actions.loader.LoaderActionPlugin.execute_action","title":"<code>execute_action(selection, data, form_values)</code>  <code>abstractmethod</code>","text":"<p>Execute an action.</p> <p>Parameters:</p> Name Type Description Default <code>selection</code> <code>LoaderActionSelection</code> <p>Selection wrapper. Can be used to get entities or get context of original selection.</p> required <code>data</code> <code>Optional[DataType]</code> <p>Additional action item data.</p> required <code>form_values</code> <code>dict[str, Any]</code> <p>Attribute values.</p> required <p>Returns:</p> Type Description <code>Optional[LoaderActionResult]</code> <p>Optional[LoaderActionResult]: Result of the action execution.</p> Source code in <code>client/ayon_core/pipeline/actions/loader.py</code> <pre><code>@abstractmethod\ndef execute_action(\n    self,\n    selection: LoaderActionSelection,\n    data: Optional[DataType],\n    form_values: dict[str, Any],\n) -&gt; Optional[LoaderActionResult]:\n    \"\"\"Execute an action.\n\n    Args:\n        selection (LoaderActionSelection): Selection wrapper. Can be used\n            to get entities or get context of original selection.\n        data (Optional[DataType]): Additional action item data.\n        form_values (dict[str, Any]): Attribute values.\n\n    Returns:\n        Optional[LoaderActionResult]: Result of the action execution.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/loader.html#client.ayon_core.pipeline.actions.loader.LoaderActionPlugin.get_action_items","title":"<code>get_action_items(selection)</code>  <code>abstractmethod</code>","text":"<p>Action items for the selection.</p> <p>Parameters:</p> Name Type Description Default <code>selection</code> <code>LoaderActionSelection</code> <p>Selection.</p> required <p>Returns:</p> Type Description <code>list[LoaderActionItem]</code> <p>list[LoaderActionItem]: Action items.</p> Source code in <code>client/ayon_core/pipeline/actions/loader.py</code> <pre><code>@abstractmethod\ndef get_action_items(\n    self, selection: LoaderActionSelection\n) -&gt; list[LoaderActionItem]:\n    \"\"\"Action items for the selection.\n\n    Args:\n        selection (LoaderActionSelection): Selection.\n\n    Returns:\n        list[LoaderActionItem]: Action items.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/loader.html#client.ayon_core.pipeline.actions.loader.LoaderActionResult","title":"<code>LoaderActionResult</code>  <code>dataclass</code>","text":"<p>Result of loader action execution.</p> <p>Attributes:</p> Name Type Description <code>message</code> <code>Optional[str]</code> <p>Message to show in UI.</p> <code>success</code> <code>bool</code> <p>If the action was successful. Affects color of the message.</p> <code>form</code> <code>Optional[ActionForm]</code> <p>Form to show in UI.</p> <code>form_values</code> <code>Optional[dict[str, Any]]</code> <p>Values for the form. Can be used if the same form is re-shown e.g. because a user forgot to fill a required field.</p> Source code in <code>client/ayon_core/pipeline/actions/loader.py</code> <pre><code>@dataclass\nclass LoaderActionResult:\n    \"\"\"Result of loader action execution.\n\n    Attributes:\n        message (Optional[str]): Message to show in UI.\n        success (bool): If the action was successful. Affects color of\n            the message.\n        form (Optional[ActionForm]): Form to show in UI.\n        form_values (Optional[dict[str, Any]]): Values for the form. Can be\n            used if the same form is re-shown e.g. because a user forgot to\n            fill a required field.\n\n    \"\"\"\n    message: Optional[str] = None\n    success: bool = True\n    form: Optional[ActionForm] = None\n    form_values: Optional[dict[str, Any]] = None\n\n    def to_json_data(self) -&gt; dict[str, Any]:\n        form = self.form\n        if form is not None:\n            form = form.to_json_data()\n        return {\n            \"message\": self.message,\n            \"success\": self.success,\n            \"form\": form,\n            \"form_values\": self.form_values,\n        }\n\n    @classmethod\n    def from_json_data(cls, data: dict[str, Any]) -&gt; \"LoaderActionResult\":\n        form = data[\"form\"]\n        if form is not None:\n            data[\"form\"] = ActionForm.from_json_data(form)\n        return LoaderActionResult(**data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/loader.html#client.ayon_core.pipeline.actions.loader.LoaderActionSelection","title":"<code>LoaderActionSelection</code>","text":"<p>Selection of entities for loader actions.</p> <p>Selection tells action plugins what exactly is selected in the tool and     which ids.</p> <p>Contains entity cache which can be used to get entities by their ids. Or     to get project settings and anatomy.</p> Source code in <code>client/ayon_core/pipeline/actions/loader.py</code> <pre><code>class LoaderActionSelection:\n    \"\"\"Selection of entities for loader actions.\n\n    Selection tells action plugins what exactly is selected in the tool and\n        which ids.\n\n    Contains entity cache which can be used to get entities by their ids. Or\n        to get project settings and anatomy.\n\n    \"\"\"\n    def __init__(\n        self,\n        project_name: str,\n        selected_ids: set[str],\n        selected_type: LoaderSelectedType,\n        *,\n        project_anatomy: Optional[Anatomy] = None,\n        project_settings: Optional[dict[str, Any]] = None,\n        entities_cache: Optional[SelectionEntitiesCache] = None,\n    ):\n        self._project_name = project_name\n        self._selected_ids = selected_ids\n        self._selected_type = selected_type\n\n        self._project_anatomy = project_anatomy\n        self._project_settings = project_settings\n\n        if entities_cache is None:\n            entities_cache = SelectionEntitiesCache(project_name)\n        self._entities_cache = entities_cache\n\n    def get_entities_cache(self) -&gt; SelectionEntitiesCache:\n        return self._entities_cache\n\n    def get_project_name(self) -&gt; str:\n        return self._project_name\n\n    def get_selected_ids(self) -&gt; set[str]:\n        return set(self._selected_ids)\n\n    def get_selected_type(self) -&gt; str:\n        return self._selected_type\n\n    def get_project_settings(self) -&gt; dict[str, Any]:\n        if self._project_settings is None:\n            self._project_settings = get_project_settings(self._project_name)\n        return copy.deepcopy(self._project_settings)\n\n    def get_project_anatomy(self) -&gt; Anatomy:\n        if self._project_anatomy is None:\n            self._project_anatomy = Anatomy(\n                self._project_name,\n                project_entity=self.get_entities_cache().get_project(),\n            )\n        return self._project_anatomy\n\n    project_name = property(get_project_name)\n    selected_ids = property(get_selected_ids)\n    selected_type = property(get_selected_type)\n    project_settings = property(get_project_settings)\n    project_anatomy = property(get_project_anatomy)\n    entities = property(get_entities_cache)\n\n    # --- Helper methods ---\n    def versions_selected(self) -&gt; bool:\n        \"\"\"Selected entity type is version.\n\n        Returns:\n            bool: True if selected entity type is version.\n\n        \"\"\"\n        return self._selected_type == LoaderSelectedType.version\n\n    def representations_selected(self) -&gt; bool:\n        \"\"\"Selected entity type is representation.\n\n        Returns:\n            bool: True if selected entity type is representation.\n\n        \"\"\"\n        return self._selected_type == LoaderSelectedType.representation\n\n    def get_selected_version_entities(self) -&gt; list[dict[str, Any]]:\n        \"\"\"Retrieve selected version entities.\n\n        An empty list is returned if 'version' is not the selected\n            entity type.\n\n        Returns:\n            list[dict[str, Any]]: List of selected version entities.\n\n        \"\"\"\n        if self.versions_selected():\n            return self.entities.get_versions(self.selected_ids)\n        return []\n\n    def get_selected_representation_entities(self) -&gt; list[dict[str, Any]]:\n        \"\"\"Retrieve selected representation entities.\n\n        An empty list is returned if 'representation' is not the selected\n            entity type.\n\n        Returns:\n            list[dict[str, Any]]: List of selected representation entities.\n\n        \"\"\"\n        if self.representations_selected():\n            return self.entities.get_representations(self.selected_ids)\n        return []\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/loader.html#client.ayon_core.pipeline.actions.loader.LoaderActionSelection.get_selected_representation_entities","title":"<code>get_selected_representation_entities()</code>","text":"<p>Retrieve selected representation entities.</p> <p>An empty list is returned if 'representation' is not the selected     entity type.</p> <p>Returns:</p> Type Description <code>list[dict[str, Any]]</code> <p>list[dict[str, Any]]: List of selected representation entities.</p> Source code in <code>client/ayon_core/pipeline/actions/loader.py</code> <pre><code>def get_selected_representation_entities(self) -&gt; list[dict[str, Any]]:\n    \"\"\"Retrieve selected representation entities.\n\n    An empty list is returned if 'representation' is not the selected\n        entity type.\n\n    Returns:\n        list[dict[str, Any]]: List of selected representation entities.\n\n    \"\"\"\n    if self.representations_selected():\n        return self.entities.get_representations(self.selected_ids)\n    return []\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/loader.html#client.ayon_core.pipeline.actions.loader.LoaderActionSelection.get_selected_version_entities","title":"<code>get_selected_version_entities()</code>","text":"<p>Retrieve selected version entities.</p> <p>An empty list is returned if 'version' is not the selected     entity type.</p> <p>Returns:</p> Type Description <code>list[dict[str, Any]]</code> <p>list[dict[str, Any]]: List of selected version entities.</p> Source code in <code>client/ayon_core/pipeline/actions/loader.py</code> <pre><code>def get_selected_version_entities(self) -&gt; list[dict[str, Any]]:\n    \"\"\"Retrieve selected version entities.\n\n    An empty list is returned if 'version' is not the selected\n        entity type.\n\n    Returns:\n        list[dict[str, Any]]: List of selected version entities.\n\n    \"\"\"\n    if self.versions_selected():\n        return self.entities.get_versions(self.selected_ids)\n    return []\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/loader.html#client.ayon_core.pipeline.actions.loader.LoaderActionSelection.representations_selected","title":"<code>representations_selected()</code>","text":"<p>Selected entity type is representation.</p> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if selected entity type is representation.</p> Source code in <code>client/ayon_core/pipeline/actions/loader.py</code> <pre><code>def representations_selected(self) -&gt; bool:\n    \"\"\"Selected entity type is representation.\n\n    Returns:\n        bool: True if selected entity type is representation.\n\n    \"\"\"\n    return self._selected_type == LoaderSelectedType.representation\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/loader.html#client.ayon_core.pipeline.actions.loader.LoaderActionSelection.versions_selected","title":"<code>versions_selected()</code>","text":"<p>Selected entity type is version.</p> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if selected entity type is version.</p> Source code in <code>client/ayon_core/pipeline/actions/loader.py</code> <pre><code>def versions_selected(self) -&gt; bool:\n    \"\"\"Selected entity type is version.\n\n    Returns:\n        bool: True if selected entity type is version.\n\n    \"\"\"\n    return self._selected_type == LoaderSelectedType.version\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/loader.html#client.ayon_core.pipeline.actions.loader.LoaderActionsContext","title":"<code>LoaderActionsContext</code>","text":"<p>Wrapper for loader actions and their logic.</p> <p>Takes care about the public api of loader actions and internal logic like     discovery and initialization of plugins.</p> Source code in <code>client/ayon_core/pipeline/actions/loader.py</code> <pre><code>class LoaderActionsContext:\n    \"\"\"Wrapper for loader actions and their logic.\n\n    Takes care about the public api of loader actions and internal logic like\n        discovery and initialization of plugins.\n\n    \"\"\"\n    def __init__(\n        self,\n        studio_settings: Optional[dict[str, Any]] = None,\n        addons_manager: Optional[AddonsManager] = None,\n        host: Optional[AbstractHost] = _PLACEHOLDER,\n    ) -&gt; None:\n        self._log = Logger.get_logger(self.__class__.__name__)\n\n        self._addons_manager = addons_manager\n        self._host = host\n\n        # Attributes that are re-cached on reset\n        self._studio_settings = studio_settings\n        self._plugins = None\n\n    def reset(\n        self, studio_settings: Optional[dict[str, Any]] = None\n    ) -&gt; None:\n        \"\"\"Reset context cache.\n\n        Reset plugins and studio settings to reload them.\n\n        Notes:\n             Does not reset the cache of AddonsManger because there should not\n                be a reason to do so.\n\n        \"\"\"\n        self._studio_settings = studio_settings\n        self._plugins = None\n\n    def get_addons_manager(self) -&gt; AddonsManager:\n        if self._addons_manager is None:\n            self._addons_manager = AddonsManager(\n                settings=self.get_studio_settings()\n            )\n        return self._addons_manager\n\n    def get_host(self) -&gt; Optional[AbstractHost]:\n        \"\"\"Get current host integration.\n\n        Returns:\n            Optional[AbstractHost]: Host integration. Can be None if host\n                integration is not registered -&gt; probably not used in the\n                host integration process.\n\n        \"\"\"\n        if self._host is _PLACEHOLDER:\n            from ayon_core.pipeline import registered_host\n\n            self._host = registered_host()\n        return self._host\n\n    def get_host_name(self) -&gt; Optional[str]:\n        host = self.get_host()\n        if host is None:\n            return None\n        return host.name\n\n    def get_studio_settings(self) -&gt; dict[str, Any]:\n        if self._studio_settings is None:\n            self._studio_settings = get_studio_settings()\n        return copy.deepcopy(self._studio_settings)\n\n    def get_action_items(\n        self, selection: LoaderActionSelection\n    ) -&gt; list[LoaderActionItem]:\n        \"\"\"Collect action items from all plugins for given selection.\n\n        Args:\n            selection (LoaderActionSelection): Selection wrapper.\n\n        \"\"\"\n        output = []\n        for plugin_id, plugin in self._get_plugins().items():\n            try:\n                for action_item in plugin.get_action_items(selection):\n                    if action_item.identifier is None:\n                        action_item.identifier = plugin_id\n                    output.append(action_item)\n\n            except Exception:\n                self._log.warning(\n                    \"Failed to get action items for\"\n                    f\" plugin '{plugin.identifier}'\",\n                    exc_info=True,\n                )\n        return output\n\n    def execute_action(\n        self,\n        identifier: str,\n        selection: LoaderActionSelection,\n        data: Optional[DataType],\n        form_values: dict[str, Any],\n    ) -&gt; Optional[LoaderActionResult]:\n        \"\"\"Trigger action execution.\n\n        Args:\n            identifier (str): Identifier of the plugin.\n            selection (LoaderActionSelection): Selection wrapper. Can be used\n                to get what is selected in UI and to get access to entity\n                cache.\n            data (Optional[DataType]): Additional action item data.\n            form_values (dict[str, Any]): Form values related to action.\n                Usually filled if action returned response with form.\n\n        \"\"\"\n        plugins_by_id = self._get_plugins()\n        plugin = plugins_by_id[identifier]\n        return plugin.execute_action(\n            selection,\n            data,\n            form_values,\n        )\n\n    def _get_plugins(self) -&gt; dict[str, LoaderActionPlugin]:\n        if self._plugins is None:\n            host_name = self.get_host_name()\n            addons_manager = self.get_addons_manager()\n            all_paths = [\n                os.path.join(AYON_CORE_ROOT, \"plugins\", \"loader\")\n            ]\n            for addon in addons_manager.addons:\n                if not isinstance(addon, IPluginPaths):\n                    continue\n\n                try:\n                    if is_func_signature_supported(\n                        addon.get_loader_action_plugin_paths,\n                        host_name\n                    ):\n                        paths = addon.get_loader_action_plugin_paths(\n                            host_name\n                        )\n                    else:\n                        paths = addon.get_loader_action_plugin_paths()\n                except Exception:\n                    self._log.warning(\n                        \"Failed to get plugin paths for addon\",\n                        exc_info=True\n                    )\n                    continue\n\n                if paths:\n                    all_paths.extend(paths)\n\n            result = discover_plugins(LoaderActionPlugin, all_paths)\n            result.log_report()\n            plugins = {}\n            for cls in result.plugins:\n                try:\n                    plugin = cls(self)\n                    if not plugin.enabled:\n                        continue\n\n                    plugin_id = plugin.identifier\n                    if plugin_id not in plugins:\n                        plugins[plugin_id] = plugin\n                        continue\n\n                    self._log.warning(\n                        f\"Duplicated plugins identifier found '{plugin_id}'.\"\n                    )\n\n                except Exception:\n                    self._log.warning(\n                        f\"Failed to initialize plugin '{cls.__name__}'\",\n                        exc_info=True,\n                    )\n            self._plugins = plugins\n        return self._plugins\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/loader.html#client.ayon_core.pipeline.actions.loader.LoaderActionsContext.execute_action","title":"<code>execute_action(identifier, selection, data, form_values)</code>","text":"<p>Trigger action execution.</p> <p>Parameters:</p> Name Type Description Default <code>identifier</code> <code>str</code> <p>Identifier of the plugin.</p> required <code>selection</code> <code>LoaderActionSelection</code> <p>Selection wrapper. Can be used to get what is selected in UI and to get access to entity cache.</p> required <code>data</code> <code>Optional[DataType]</code> <p>Additional action item data.</p> required <code>form_values</code> <code>dict[str, Any]</code> <p>Form values related to action. Usually filled if action returned response with form.</p> required Source code in <code>client/ayon_core/pipeline/actions/loader.py</code> <pre><code>def execute_action(\n    self,\n    identifier: str,\n    selection: LoaderActionSelection,\n    data: Optional[DataType],\n    form_values: dict[str, Any],\n) -&gt; Optional[LoaderActionResult]:\n    \"\"\"Trigger action execution.\n\n    Args:\n        identifier (str): Identifier of the plugin.\n        selection (LoaderActionSelection): Selection wrapper. Can be used\n            to get what is selected in UI and to get access to entity\n            cache.\n        data (Optional[DataType]): Additional action item data.\n        form_values (dict[str, Any]): Form values related to action.\n            Usually filled if action returned response with form.\n\n    \"\"\"\n    plugins_by_id = self._get_plugins()\n    plugin = plugins_by_id[identifier]\n    return plugin.execute_action(\n        selection,\n        data,\n        form_values,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/loader.html#client.ayon_core.pipeline.actions.loader.LoaderActionsContext.get_action_items","title":"<code>get_action_items(selection)</code>","text":"<p>Collect action items from all plugins for given selection.</p> <p>Parameters:</p> Name Type Description Default <code>selection</code> <code>LoaderActionSelection</code> <p>Selection wrapper.</p> required Source code in <code>client/ayon_core/pipeline/actions/loader.py</code> <pre><code>def get_action_items(\n    self, selection: LoaderActionSelection\n) -&gt; list[LoaderActionItem]:\n    \"\"\"Collect action items from all plugins for given selection.\n\n    Args:\n        selection (LoaderActionSelection): Selection wrapper.\n\n    \"\"\"\n    output = []\n    for plugin_id, plugin in self._get_plugins().items():\n        try:\n            for action_item in plugin.get_action_items(selection):\n                if action_item.identifier is None:\n                    action_item.identifier = plugin_id\n                output.append(action_item)\n\n        except Exception:\n            self._log.warning(\n                \"Failed to get action items for\"\n                f\" plugin '{plugin.identifier}'\",\n                exc_info=True,\n            )\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/loader.html#client.ayon_core.pipeline.actions.loader.LoaderActionsContext.get_host","title":"<code>get_host()</code>","text":"<p>Get current host integration.</p> <p>Returns:</p> Type Description <code>Optional[AbstractHost]</code> <p>Optional[AbstractHost]: Host integration. Can be None if host integration is not registered -&gt; probably not used in the host integration process.</p> Source code in <code>client/ayon_core/pipeline/actions/loader.py</code> <pre><code>def get_host(self) -&gt; Optional[AbstractHost]:\n    \"\"\"Get current host integration.\n\n    Returns:\n        Optional[AbstractHost]: Host integration. Can be None if host\n            integration is not registered -&gt; probably not used in the\n            host integration process.\n\n    \"\"\"\n    if self._host is _PLACEHOLDER:\n        from ayon_core.pipeline import registered_host\n\n        self._host = registered_host()\n    return self._host\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/loader.html#client.ayon_core.pipeline.actions.loader.LoaderActionsContext.reset","title":"<code>reset(studio_settings=None)</code>","text":"<p>Reset context cache.</p> <p>Reset plugins and studio settings to reload them.</p> Notes <p>Does not reset the cache of AddonsManger because there should not    be a reason to do so.</p> Source code in <code>client/ayon_core/pipeline/actions/loader.py</code> <pre><code>def reset(\n    self, studio_settings: Optional[dict[str, Any]] = None\n) -&gt; None:\n    \"\"\"Reset context cache.\n\n    Reset plugins and studio settings to reload them.\n\n    Notes:\n         Does not reset the cache of AddonsManger because there should not\n            be a reason to do so.\n\n    \"\"\"\n    self._studio_settings = studio_settings\n    self._plugins = None\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/loader.html#client.ayon_core.pipeline.actions.loader.LoaderSelectedType","title":"<code>LoaderSelectedType</code>","text":"<p>               Bases: <code>StrEnum</code></p> <p>Selected entity type.</p> Source code in <code>client/ayon_core/pipeline/actions/loader.py</code> <pre><code>class LoaderSelectedType(StrEnum):\n    \"\"\"Selected entity type.\"\"\"\n    # folder = \"folder\"\n    # task = \"task\"\n    version = \"version\"\n    representation = \"representation\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/loader.html#client.ayon_core.pipeline.actions.loader.LoaderSimpleActionPlugin","title":"<code>LoaderSimpleActionPlugin</code>","text":"<p>               Bases: <code>LoaderActionPlugin</code></p> <p>Simple action plugin.</p> <p>This action will show exactly one action item defined by attributes     on the class.</p> <p>Attributes:</p> Name Type Description <code>label</code> <code>Optional[str]</code> <p>Label of the action item.</p> <code>order</code> <code>int</code> <p>Order of the action item.</p> <code>group_label</code> <code>Optional[str]</code> <p>Label of the group to which the action belongs.</p> <code>icon</code> <code>Optional[dict[str, Any]]</code> <p>Icon definition shown next to label.</p> Source code in <code>client/ayon_core/pipeline/actions/loader.py</code> <pre><code>class LoaderSimpleActionPlugin(LoaderActionPlugin):\n    \"\"\"Simple action plugin.\n\n    This action will show exactly one action item defined by attributes\n        on the class.\n\n    Attributes:\n        label: Label of the action item.\n        order: Order of the action item.\n        group_label: Label of the group to which the action belongs.\n        icon: Icon definition shown next to label.\n\n    \"\"\"\n\n    label: Optional[str] = None\n    order: int = 0\n    group_label: Optional[str] = None\n    icon: Optional[dict[str, Any]] = None\n    skip_discovery: bool = True\n\n    @abstractmethod\n    def is_compatible(self, selection: LoaderActionSelection) -&gt; bool:\n        \"\"\"Check if plugin is compatible with selection.\n\n        Args:\n            selection (LoaderActionSelection): Selection information.\n\n        Returns:\n            bool: True if plugin is compatible with selection.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def execute_simple_action(\n        self,\n        selection: LoaderActionSelection,\n        form_values: dict[str, Any],\n    ) -&gt; Optional[LoaderActionResult]:\n        \"\"\"Process action based on selection.\n\n        Args:\n            selection (LoaderActionSelection): Selection information.\n            form_values (dict[str, Any]): Values from a form if there are any.\n\n        Returns:\n            Optional[LoaderActionResult]: Result of the action.\n\n        \"\"\"\n        pass\n\n    def get_action_items(\n        self, selection: LoaderActionSelection\n    ) -&gt; list[LoaderActionItem]:\n        if self.is_compatible(selection):\n            label = self.label or self.__class__.__name__\n            return [\n                LoaderActionItem(\n                    label=label,\n                    order=self.order,\n                    group_label=self.group_label,\n                    icon=self.icon,\n                )\n            ]\n        return []\n\n    def execute_action(\n        self,\n        selection: LoaderActionSelection,\n        data: Optional[DataType],\n        form_values: dict[str, Any],\n    ) -&gt; Optional[LoaderActionResult]:\n        return self.execute_simple_action(selection, form_values)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/loader.html#client.ayon_core.pipeline.actions.loader.LoaderSimpleActionPlugin.execute_simple_action","title":"<code>execute_simple_action(selection, form_values)</code>  <code>abstractmethod</code>","text":"<p>Process action based on selection.</p> <p>Parameters:</p> Name Type Description Default <code>selection</code> <code>LoaderActionSelection</code> <p>Selection information.</p> required <code>form_values</code> <code>dict[str, Any]</code> <p>Values from a form if there are any.</p> required <p>Returns:</p> Type Description <code>Optional[LoaderActionResult]</code> <p>Optional[LoaderActionResult]: Result of the action.</p> Source code in <code>client/ayon_core/pipeline/actions/loader.py</code> <pre><code>@abstractmethod\ndef execute_simple_action(\n    self,\n    selection: LoaderActionSelection,\n    form_values: dict[str, Any],\n) -&gt; Optional[LoaderActionResult]:\n    \"\"\"Process action based on selection.\n\n    Args:\n        selection (LoaderActionSelection): Selection information.\n        form_values (dict[str, Any]): Values from a form if there are any.\n\n    Returns:\n        Optional[LoaderActionResult]: Result of the action.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/loader.html#client.ayon_core.pipeline.actions.loader.LoaderSimpleActionPlugin.is_compatible","title":"<code>is_compatible(selection)</code>  <code>abstractmethod</code>","text":"<p>Check if plugin is compatible with selection.</p> <p>Parameters:</p> Name Type Description Default <code>selection</code> <code>LoaderActionSelection</code> <p>Selection information.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if plugin is compatible with selection.</p> Source code in <code>client/ayon_core/pipeline/actions/loader.py</code> <pre><code>@abstractmethod\ndef is_compatible(self, selection: LoaderActionSelection) -&gt; bool:\n    \"\"\"Check if plugin is compatible with selection.\n\n    Args:\n        selection (LoaderActionSelection): Selection information.\n\n    Returns:\n        bool: True if plugin is compatible with selection.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/loader.html#client.ayon_core.pipeline.actions.loader.SelectionEntitiesCache","title":"<code>SelectionEntitiesCache</code>","text":"<p>Cache of entities used as helper in the selection wrapper.</p> <p>It is possible to get entities based on ids with helper methods to get     entities, their parents or their children's entities.</p> <p>The goal is to avoid multiple API calls for the same entity in multiple     action plugins.</p> <p>The cache is based on the selected project. Entities are fetched     if are not in cache yet.</p> Source code in <code>client/ayon_core/pipeline/actions/loader.py</code> <pre><code>class SelectionEntitiesCache:\n    \"\"\"Cache of entities used as helper in the selection wrapper.\n\n    It is possible to get entities based on ids with helper methods to get\n        entities, their parents or their children's entities.\n\n    The goal is to avoid multiple API calls for the same entity in multiple\n        action plugins.\n\n    The cache is based on the selected project. Entities are fetched\n        if are not in cache yet.\n    \"\"\"\n    def __init__(\n        self,\n        project_name: str,\n        project_entity: Optional[dict[str, Any]] = None,\n        folders_by_id: Optional[dict[str, dict[str, Any]]] = None,\n        tasks_by_id: Optional[dict[str, dict[str, Any]]] = None,\n        products_by_id: Optional[dict[str, dict[str, Any]]] = None,\n        versions_by_id: Optional[dict[str, dict[str, Any]]] = None,\n        representations_by_id: Optional[dict[str, dict[str, Any]]] = None,\n        task_ids_by_folder_id: Optional[dict[str, set[str]]] = None,\n        product_ids_by_folder_id: Optional[dict[str, set[str]]] = None,\n        version_ids_by_product_id: Optional[dict[str, set[str]]] = None,\n        representation_ids_by_version_id: Optional[dict[str, set[str]]] = None,\n    ):\n        self._project_name = project_name\n        self._project_entity = project_entity\n        self._folders_by_id = folders_by_id or {}\n        self._tasks_by_id = tasks_by_id or {}\n        self._products_by_id = products_by_id or {}\n        self._versions_by_id = versions_by_id or {}\n        self._representations_by_id = representations_by_id or {}\n\n        self._task_ids_by_folder_id = task_ids_by_folder_id or {}\n        self._product_ids_by_folder_id = product_ids_by_folder_id or {}\n        self._version_ids_by_product_id = version_ids_by_product_id or {}\n        self._representation_ids_by_version_id = (\n            representation_ids_by_version_id or {}\n        )\n\n    def get_project(self) -&gt; dict[str, Any]:\n        \"\"\"Get project entity\"\"\"\n        if self._project_entity is None:\n            self._project_entity = ayon_api.get_project(self._project_name)\n        return copy.deepcopy(self._project_entity)\n\n    def get_folders(\n        self, folder_ids: set[str]\n    ) -&gt; list[dict[str, Any]]:\n        return self._get_entities(\n            folder_ids,\n            self._folders_by_id,\n            \"folder_ids\",\n            ayon_api.get_folders,\n        )\n\n    def get_tasks(\n        self, task_ids: set[str]\n    ) -&gt; list[dict[str, Any]]:\n        return self._get_entities(\n            task_ids,\n            self._tasks_by_id,\n            \"task_ids\",\n            ayon_api.get_tasks,\n        )\n\n    def get_products(\n        self, product_ids: set[str]\n    ) -&gt; list[dict[str, Any]]:\n        return self._get_entities(\n            product_ids,\n            self._products_by_id,\n            \"product_ids\",\n            ayon_api.get_products,\n        )\n\n    def get_versions(\n        self, version_ids: set[str]\n    ) -&gt; list[dict[str, Any]]:\n        return self._get_entities(\n            version_ids,\n            self._versions_by_id,\n            \"version_ids\",\n            ayon_api.get_versions,\n        )\n\n    def get_representations(\n        self, representation_ids: set[str]\n    ) -&gt; list[dict[str, Any]]:\n        return self._get_entities(\n            representation_ids,\n            self._representations_by_id,\n            \"representation_ids\",\n            ayon_api.get_representations,\n        )\n\n    def get_folders_tasks(\n        self, folder_ids: set[str]\n    ) -&gt; list[dict[str, Any]]:\n        task_ids = self._fill_parent_children_ids(\n            folder_ids,\n            \"folderId\",\n            \"folder_ids\",\n            self._task_ids_by_folder_id,\n            ayon_api.get_tasks,\n        )\n        return self.get_tasks(task_ids)\n\n    def get_folders_products(\n        self, folder_ids: set[str]\n    ) -&gt; list[dict[str, Any]]:\n        product_ids = self._get_folders_products_ids(folder_ids)\n        return self.get_products(product_ids)\n\n    def get_tasks_versions(\n        self, task_ids: set[str]\n    ) -&gt; list[dict[str, Any]]:\n        folder_ids = {\n            task[\"folderId\"]\n            for task in self.get_tasks(task_ids)\n        }\n        product_ids = self._get_folders_products_ids(folder_ids)\n        output = []\n        for version in self.get_products_versions(product_ids):\n            task_id = version[\"taskId\"]\n            if task_id in task_ids:\n                output.append(version)\n        return output\n\n    def get_products_versions(\n        self, product_ids: set[str]\n    ) -&gt; list[dict[str, Any]]:\n        version_ids = self._fill_parent_children_ids(\n            product_ids,\n            \"productId\",\n            \"product_ids\",\n            self._version_ids_by_product_id,\n            ayon_api.get_versions,\n        )\n        return self.get_versions(version_ids)\n\n    def get_versions_representations(\n        self, version_ids: set[str]\n    ) -&gt; list[dict[str, Any]]:\n        repre_ids = self._fill_parent_children_ids(\n            version_ids,\n            \"versionId\",\n            \"version_ids\",\n            self._representation_ids_by_version_id,\n            ayon_api.get_representations,\n        )\n        return self.get_representations(repre_ids)\n\n    def get_tasks_folders(self, task_ids: set[str]) -&gt; list[dict[str, Any]]:\n        folder_ids = {\n            task[\"folderId\"]\n            for task in self.get_tasks(task_ids)\n        }\n        return self.get_folders(folder_ids)\n\n    def get_products_folders(\n        self, product_ids: set[str]\n    ) -&gt; list[dict[str, Any]]:\n        folder_ids = {\n            product[\"folderId\"]\n            for product in self.get_products(product_ids)\n        }\n        return self.get_folders(folder_ids)\n\n    def get_versions_products(\n        self, version_ids: set[str]\n    ) -&gt; list[dict[str, Any]]:\n        product_ids = {\n            version[\"productId\"]\n            for version in self.get_versions(version_ids)\n        }\n        return self.get_products(product_ids)\n\n    def get_versions_tasks(\n        self, version_ids: set[str]\n    ) -&gt; list[dict[str, Any]]:\n        task_ids = {\n            version[\"taskId\"]\n            for version in self.get_versions(version_ids)\n            if version[\"taskId\"]\n        }\n        return self.get_tasks(task_ids)\n\n    def get_representations_versions(\n        self, representation_ids: set[str]\n    ) -&gt; list[dict[str, Any]]:\n        version_ids = {\n            repre[\"versionId\"]\n            for repre in self.get_representations(representation_ids)\n        }\n        return self.get_versions(version_ids)\n\n    def _get_folders_products_ids(self, folder_ids: set[str]) -&gt; set[str]:\n        return self._fill_parent_children_ids(\n            folder_ids,\n            \"folderId\",\n            \"folder_ids\",\n            self._product_ids_by_folder_id,\n            ayon_api.get_products,\n        )\n\n    def _fill_parent_children_ids(\n        self,\n        entity_ids: set[str],\n        parent_key: str,\n        filter_attr: str,\n        parent_mapping: dict[str, set[str]],\n        getter: Callable,\n    ) -&gt; set[str]:\n        if not entity_ids:\n            return set()\n        children_ids = set()\n        missing_ids = set()\n        for entity_id in entity_ids:\n            _children_ids = parent_mapping.get(entity_id)\n            if _children_ids is None:\n                missing_ids.add(entity_id)\n            else:\n                children_ids.update(_children_ids)\n        if missing_ids:\n            entities_by_parent_id = collections.defaultdict(set)\n            for entity in getter(\n                self._project_name,\n                fields={\"id\", parent_key},\n                **{filter_attr: missing_ids},\n            ):\n                child_id = entity[\"id\"]\n                children_ids.add(child_id)\n                entities_by_parent_id[entity[parent_key]].add(child_id)\n\n            for entity_id in missing_ids:\n                parent_mapping[entity_id] = entities_by_parent_id[entity_id]\n\n        return children_ids\n\n    def _get_entities(\n        self,\n        entity_ids: set[str],\n        cache_var: dict[str, Any],\n        filter_arg: str,\n        getter: Callable,\n    ) -&gt; list[dict[str, Any]]:\n        if not entity_ids:\n            return []\n\n        output = []\n        missing_ids: set[str] = set()\n        for entity_id in entity_ids:\n            entity = cache_var.get(entity_id)\n            if entity_id not in cache_var:\n                missing_ids.add(entity_id)\n                cache_var[entity_id] = None\n            elif entity:\n                output.append(entity)\n\n        if missing_ids:\n            for entity in getter(\n                self._project_name,\n                **{filter_arg: missing_ids}\n            ):\n                output.append(entity)\n                cache_var[entity[\"id\"]] = entity\n        return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/loader.html#client.ayon_core.pipeline.actions.loader.SelectionEntitiesCache.get_project","title":"<code>get_project()</code>","text":"<p>Get project entity</p> Source code in <code>client/ayon_core/pipeline/actions/loader.py</code> <pre><code>def get_project(self) -&gt; dict[str, Any]:\n    \"\"\"Get project entity\"\"\"\n    if self._project_entity is None:\n        self._project_entity = ayon_api.get_project(self._project_name)\n    return copy.deepcopy(self._project_entity)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/structures.html","title":"structures","text":""},{"location":"autoapi/client/ayon_core/pipeline/actions/structures.html#client.ayon_core.pipeline.actions.structures.ActionForm","title":"<code>ActionForm</code>  <code>dataclass</code>","text":"<p>Form for loader action.</p> <p>If an action needs to collect information from a user before or during of     the action execution, it can return a response with a form. When the     form is submitted, a new execution of the action is triggered.</p> <p>It is also possible to just show a label message without the submit     button to make sure the user has seen the message.</p> <p>Attributes:</p> Name Type Description <code>title</code> <code>str</code> <p>Title of the form -&gt; title of the window.</p> <code>fields</code> <code>list[AbstractAttrDef]</code> <p>Fields of the form.</p> <code>submit_label</code> <code>Optional[str]</code> <p>Label of the submit button. Is hidden if is set to None.</p> <code>submit_icon</code> <code>Optional[dict[str, Any]]</code> <p>Icon definition of the submit button.</p> <code>cancel_label</code> <code>Optional[str]</code> <p>Label of the cancel button. Is hidden if is set to None. User can still close the window tho.</p> <code>cancel_icon</code> <code>Optional[dict[str, Any]]</code> <p>Icon definition of the cancel button.</p> Source code in <code>client/ayon_core/pipeline/actions/structures.py</code> <pre><code>@dataclass\nclass ActionForm:\n    \"\"\"Form for loader action.\n\n    If an action needs to collect information from a user before or during of\n        the action execution, it can return a response with a form. When the\n        form is submitted, a new execution of the action is triggered.\n\n    It is also possible to just show a label message without the submit\n        button to make sure the user has seen the message.\n\n    Attributes:\n        title (str): Title of the form -&gt; title of the window.\n        fields (list[AbstractAttrDef]): Fields of the form.\n        submit_label (Optional[str]): Label of the submit button. Is hidden\n            if is set to None.\n        submit_icon (Optional[dict[str, Any]]): Icon definition of the submit\n            button.\n        cancel_label (Optional[str]): Label of the cancel button. Is hidden\n            if is set to None. User can still close the window tho.\n        cancel_icon (Optional[dict[str, Any]]): Icon definition of the cancel\n            button.\n\n    \"\"\"\n    title: str\n    fields: list[AbstractAttrDef]\n    submit_label: Optional[str] = \"Submit\"\n    submit_icon: IconBase | dict[str, Any] | None = None\n    cancel_label: Optional[str] = \"Cancel\"\n    cancel_icon: IconBase | dict[str, Any] | None = None\n\n    def to_json_data(self) -&gt; dict[str, Any]:\n        fields = self.fields\n        if fields is not None:\n            fields = serialize_attr_defs(fields)\n        return {\n            \"title\": self.title,\n            \"fields\": fields,\n            \"submit_label\": self.submit_label,\n            \"submit_icon\": self.submit_icon,\n            \"cancel_label\": self.cancel_label,\n            \"cancel_icon\": self.cancel_icon,\n        }\n\n    @classmethod\n    def from_json_data(cls, data: dict[str, Any]) -&gt; \"ActionForm\":\n        fields = data[\"fields\"]\n        if fields is not None:\n            data[\"fields\"] = deserialize_attr_defs(fields)\n        return cls(**data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/actions/utils.html","title":"utils","text":""},{"location":"autoapi/client/ayon_core/pipeline/actions/utils.html#client.ayon_core.pipeline.actions.utils.webaction_fields_to_attribute_defs","title":"<code>webaction_fields_to_attribute_defs(fields)</code>","text":"<p>Helper function to convert fields definition from webactions form.</p> <p>Convert form fields to attribute definitions to be able to display them     using attribute definitions.</p> <p>Parameters:</p> Name Type Description Default <code>fields</code> <code>list[dict[str, Any]]</code> <p>Fields from webaction form.</p> required <p>Returns:</p> Type Description <code>list[AbstractAttrDef]</code> <p>list[AbstractAttrDef]: Converted attribute definitions.</p> Source code in <code>client/ayon_core/pipeline/actions/utils.py</code> <pre><code>def webaction_fields_to_attribute_defs(\n    fields: list[dict[str, Any]]\n) -&gt; list[AbstractAttrDef]:\n    \"\"\"Helper function to convert fields definition from webactions form.\n\n    Convert form fields to attribute definitions to be able to display them\n        using attribute definitions.\n\n    Args:\n        fields (list[dict[str, Any]]): Fields from webaction form.\n\n    Returns:\n        list[AbstractAttrDef]: Converted attribute definitions.\n\n    \"\"\"\n    attr_defs = []\n    for field in fields:\n        field_type = field[\"type\"]\n        attr_def = None\n        if field_type == \"label\":\n            label = field.get(\"value\")\n            if label is None:\n                label = field.get(\"text\")\n            attr_def = UILabelDef(\n                label, key=uuid.uuid4().hex\n            )\n        elif field_type == \"boolean\":\n            value = field[\"value\"]\n            if isinstance(value, str):\n                value = value.lower() == \"true\"\n\n            attr_def = BoolDef(\n                field[\"name\"],\n                default=value,\n                label=field.get(\"label\"),\n            )\n        elif field_type == \"text\":\n            attr_def = TextDef(\n                field[\"name\"],\n                default=field.get(\"value\"),\n                label=field.get(\"label\"),\n                placeholder=field.get(\"placeholder\"),\n                multiline=field.get(\"multiline\", False),\n                regex=field.get(\"regex\"),\n                # syntax=field[\"syntax\"],\n            )\n        elif field_type in (\"integer\", \"float\"):\n            value = field.get(\"value\")\n            if isinstance(value, str):\n                if field_type == \"integer\":\n                    value = int(value)\n                else:\n                    value = float(value)\n            attr_def = NumberDef(\n                field[\"name\"],\n                default=value,\n                label=field.get(\"label\"),\n                decimals=0 if field_type == \"integer\" else 5,\n                # placeholder=field.get(\"placeholder\"),\n                minimum=field.get(\"min\"),\n                maximum=field.get(\"max\"),\n            )\n        elif field_type in (\"select\", \"multiselect\"):\n            attr_def = EnumDef(\n                field[\"name\"],\n                items=field[\"options\"],\n                default=field.get(\"value\"),\n                label=field.get(\"label\"),\n                multiselection=field_type == \"multiselect\",\n            )\n        elif field_type == \"hidden\":\n            attr_def = HiddenDef(\n                field[\"name\"],\n                default=field.get(\"value\"),\n            )\n\n        if attr_def is None:\n            print(f\"Unknown config field type: {field_type}\")\n            attr_def = UILabelDef(\n                f\"Unknown field type '{field_type}\",\n                key=uuid.uuid4().hex\n            )\n        attr_defs.append(attr_def)\n    return attr_defs\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/index.html","title":"anatomy","text":""},{"location":"autoapi/client/ayon_core/pipeline/anatomy/index.html#client.ayon_core.pipeline.anatomy.Anatomy","title":"<code>Anatomy</code>","text":"<p>               Bases: <code>BaseAnatomy</code></p> Source code in <code>client/ayon_core/pipeline/anatomy/anatomy.py</code> <pre><code>class Anatomy(BaseAnatomy):\n    _project_cache = NestedCacheItem(lifetime=10)\n    _sitesync_addon_cache = CacheItem(lifetime=60)\n    _default_site_id_cache = NestedCacheItem(lifetime=60)\n    _root_overrides_cache = NestedCacheItem(2, lifetime=60)\n\n    def __init__(\n        self, project_name=None, site_name=None, project_entity=None\n    ):\n        if not project_name:\n            project_name = os.environ.get(\"AYON_PROJECT_NAME\")\n\n        if not project_name:\n            raise ProjectNotSet((\n                \"Implementation bug: Project name is not set. Anatomy requires\"\n                \" to load data for specific project.\"\n            ))\n\n        if not project_entity:\n            project_entity = self.get_project_entity_from_cache(project_name)\n        root_overrides = self._get_site_root_overrides(\n            project_name, site_name\n        )\n\n        super(Anatomy, self).__init__(project_entity, root_overrides)\n\n    @classmethod\n    def get_project_entity_from_cache(cls, project_name):\n        project_cache = cls._project_cache[project_name]\n        if not project_cache.is_valid:\n            project_cache.update_data(ayon_api.get_project(project_name))\n        return copy.deepcopy(project_cache.get_data())\n\n    @classmethod\n    def get_sitesync_addon(cls):\n        if not cls._sitesync_addon_cache.is_valid:\n            manager = AddonsManager()\n            cls._sitesync_addon_cache.update_data(\n                manager.get_enabled_addon(\"sitesync\")\n            )\n        return cls._sitesync_addon_cache.get_data()\n\n    @classmethod\n    def _get_studio_roots_overrides(cls, project_name):\n        \"\"\"This would return 'studio' site override by local settings.\n\n        Notes:\n            This logic handles local overrides of studio site which may be\n                available even when sync server is not enabled.\n            Handling of 'studio' and 'local' site was separated as preparation\n                for AYON development where that will be received from\n                separated sources.\n\n        Args:\n            project_name (str): Name of project.\n\n        Returns:\n            Union[Dict[str, str], None]): Local root overrides.\n        \"\"\"\n        if not project_name:\n            return None\n        return ayon_api.get_project_root_overrides_by_site_id(\n            project_name, get_local_site_id()\n        )\n\n    @classmethod\n    def _get_site_root_overrides(cls, project_name, site_name):\n        \"\"\"Get root overrides for site.\n\n        Args:\n            project_name (str): Project name for which root overrides should be\n                received.\n            site_name (Union[str, None]): Name of site for which root overrides\n                should be returned.\n        \"\"\"\n\n        # First check if sync server is available and enabled\n        sitesync_addon = cls.get_sitesync_addon()\n        if sitesync_addon is None or not sitesync_addon.enabled:\n            # QUESTION is ok to force 'studio' when site sync is not enabled?\n            site_name = \"studio\"\n\n        elif not site_name:\n            # Use sync server to receive active site name\n            project_cache = cls._default_site_id_cache[project_name]\n            if not project_cache.is_valid:\n                project_cache.update_data(\n                    sitesync_addon.get_active_site_type(project_name)\n                )\n            site_name = project_cache.get_data()\n\n        site_cache = cls._root_overrides_cache[project_name][site_name]\n        if not site_cache.is_valid:\n            if site_name == \"studio\":\n                # Handle studio root overrides without sync server\n                # - studio root overrides can be done even without sync server\n                roots_overrides = cls._get_studio_roots_overrides(\n                    project_name\n                )\n            else:\n                # Ask sync server to get roots overrides\n                roots_overrides = sitesync_addon.get_site_root_overrides(\n                    project_name, site_name\n                )\n            site_cache.update_data(roots_overrides)\n        return site_cache.get_data()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/index.html#client.ayon_core.pipeline.anatomy.AnatomyRoot","title":"<code>AnatomyRoot</code>","text":"<p>               Bases: <code>FormatObject</code></p> <p>Represents one item or roots.</p> <p>Holds raw data of root item specification. Raw data contain value for each platform, but current platform value is used when object is used for formatting of template.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <code>AnatomyRoots</code> <p>Parent object.</p> required <code>root_raw_data</code> <code>dict</code> <p>Dictionary containing root values by platform names. [\"windows\", \"linux\" and \"darwin\"]</p> required <code>name</code> <code>str</code> <p>Root name which is representing. Used with multi root setup otherwise None value is expected.</p> required Source code in <code>client/ayon_core/pipeline/anatomy/roots.py</code> <pre><code>class AnatomyRoot(FormatObject):\n    \"\"\"Represents one item or roots.\n\n    Holds raw data of root item specification. Raw data contain value\n    for each platform, but current platform value is used when object\n    is used for formatting of template.\n\n    Args:\n        parent (AnatomyRoots): Parent object.\n        root_raw_data (dict): Dictionary containing root values by platform\n            names. [\"windows\", \"linux\" and \"darwin\"]\n        name (str): Root name which is representing. Used with\n            multi root setup otherwise None value is expected.\n    \"\"\"\n    def __init__(self, parent, root_raw_data, name):\n        super().__init__()\n        self._log = None\n        lowered_platform_keys = {\n            key.lower(): value\n            for key, value in root_raw_data.items()\n        }\n        self.raw_data = lowered_platform_keys\n        self.cleaned_data = self._clean_roots(lowered_platform_keys)\n        self.name = name\n        self.parent = parent\n\n        self.available_platforms = set(lowered_platform_keys.keys())\n\n        current_platform = platform.system().lower()\n        # WARNING: Using environment variables in roots is not considered\n        #   as production safe. Some features may not work as expected, for\n        #   example USD resolver or site sync.\n        try:\n            self.value = lowered_platform_keys[current_platform].format_map(\n                os.environ\n            )\n        except KeyError:\n            result = StringTemplate(self.value).format(os.environ.copy())\n            is_are = \"is\" if len(result.missing_keys) == 1 else \"are\"\n            missing_keys = \", \".join(result.missing_keys)\n            raise RootMissingEnv(\n                f\"Root \\\"{name}\\\" requires environment variable/s\"\n                f\" {missing_keys} which {is_are} not available.\"\n            )\n\n        self.clean_value = self._clean_root(self.value)\n\n    def __format__(self, *args, **kwargs):\n        return self.value.__format__(*args, **kwargs)\n\n    def __str__(self):\n        return str(self.value)\n\n    def __repr__(self):\n        return self.__str__()\n\n    def __getitem__(self, key):\n        if isinstance(key, numbers.Number):\n            return self.value[key]\n\n        additional_info = \"\"\n        if self.parent and self.parent.project_name:\n            additional_info += \" for project \\\"{}\\\"\".format(\n                self.parent.project_name\n            )\n\n        raise KeyError(\n            \"Root key \\\"{}\\\" is missing{}.\".format(\n                key, additional_info\n            )\n        )\n\n    @property\n    def log(self):\n        if self._log is None:\n            self._log = Logger.get_logger(self.__class__.__name__)\n        return self._log\n\n    @property\n    def full_key(self):\n        \"\"\"Full key value for dictionary formatting in template.\n\n        Returns:\n            str: Return full replacement key for formatting. This helps when\n                multiple roots are set. In that case e.g. `\"root[work]\"` is\n                returned.\n\n        \"\"\"\n        return \"root[{}]\".format(self.name)\n\n    @staticmethod\n    def _clean_path(path):\n        \"\"\"Just replace backslashes with forward slashes.\n\n        Args:\n            path (str): Path which should be cleaned.\n\n        Returns:\n            str: Cleaned path with forward slashes.\n\n        \"\"\"\n        return str(path).replace(\"\\\\\", \"/\")\n\n    def _clean_root(self, root):\n        \"\"\"Clean root value.\n\n        Args:\n            root (str): Root value which should be cleaned.\n\n        Returns:\n            str: Cleaned root value.\n\n        \"\"\"\n        return self._clean_path(root).rstrip(\"/\")\n\n    def _clean_roots(self, raw_data):\n        \"\"\"Clean all values of raw root item values.\"\"\"\n        return {\n            key: self._clean_root(value)\n            for key, value in raw_data.items()\n        }\n\n    def path_remapper(self, path, dst_platform=None, src_platform=None):\n        \"\"\"Remap path for specific platform.\n\n        Args:\n            path (str): Source path which need to be remapped.\n            dst_platform (str, optional): Specify destination platform\n                for which remapping should happen.\n            src_platform (str, optional): Specify source platform. This is\n                recommended to not use and keep unset until you really want\n                to use specific platform.\n\n        Returns:\n            Union[str, None]: When path does not contain known root then\n                None is returned else returns remapped path with\n                \"{root[&lt;name&gt;]}\".\n\n        \"\"\"\n        cleaned_path = self._clean_path(path)\n        if dst_platform:\n            dst_root_clean = self.cleaned_data.get(dst_platform)\n            if not dst_root_clean:\n                self.log.warning(\n                    \"Root \\\"{}\\\" miss platform \\\"{}\\\" definition.\".format(\n                        self.full_key, dst_platform\n                    )\n                )\n                return None\n\n            if cleaned_path.startswith(dst_root_clean):\n                return cleaned_path\n\n        if src_platform:\n            src_root_clean = self.cleaned_data.get(src_platform)\n            if src_root_clean is None:\n                self.log.warning(\n                    \"Root \\\"{}\\\" miss platform \\\"{}\\\" definition.\".format(\n                        self.full_key, src_platform\n                    )\n                )\n                return None\n\n            if not cleaned_path.startswith(src_root_clean):\n                return None\n\n            subpath = cleaned_path[len(src_root_clean):]\n            if dst_platform:\n                # `dst_root_clean` is used from upper condition\n                return dst_root_clean + subpath\n            return self.clean_value + subpath\n\n        result, template = self.find_root_template_from_path(path)\n        if not result:\n            return None\n\n        if dst_platform:\n            fill_data = {self.name: dst_root_clean}\n        else:\n            fill_data = {self.name: self.value}\n\n        return template.format(**{\"root\": fill_data})\n\n    def find_root_template_from_path(self, path):\n        \"\"\"Replaces known root value with formattable key in path.\n\n        All platform values are checked for this replacement.\n\n        Args:\n            path (str): Path where root value should be found.\n\n        Returns:\n            tuple: Tuple contain 2 values: `success` (bool) and `path` (str).\n                When success it True then path should contain replaced root\n                value with formattable key.\n\n        Example:\n            When input path is::\n                \"C:/windows/path/root/projects/my_project/file.ext\"\n\n            And raw data of item looks like::\n                {\n                    \"windows\": \"C:/windows/path/root\",\n                    \"linux\": \"/mount/root\"\n                }\n\n            Output will be::\n                (True, \"{root}/projects/my_project/file.ext\")\n\n            If any of raw data value wouldn't match path's root output is::\n                (False, \"C:/windows/path/root/projects/my_project/file.ext\")\n        \"\"\"\n        result = False\n        output = str(path)\n\n        mod_path = self._clean_path(path)\n        for root_os, root_path in self.cleaned_data.items():\n            # Skip empty paths\n            if not root_path:\n                continue\n\n            _mod_path = mod_path  # reset to original cleaned value\n            if root_os == \"windows\":\n                root_path = root_path.lower()\n                _mod_path = _mod_path.lower()\n\n            if _mod_path.startswith(root_path):\n                result = True\n                replacement = \"{\" + self.full_key + \"}\"\n                output = replacement + mod_path[len(root_path):]\n                break\n\n        return (result, output)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/index.html#client.ayon_core.pipeline.anatomy.AnatomyRoot.full_key","title":"<code>full_key</code>  <code>property</code>","text":"<p>Full key value for dictionary formatting in template.</p> <p>Returns:</p> Name Type Description <code>str</code> <p>Return full replacement key for formatting. This helps when multiple roots are set. In that case e.g. <code>\"root[work]\"</code> is returned.</p>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/index.html#client.ayon_core.pipeline.anatomy.AnatomyRoot.find_root_template_from_path","title":"<code>find_root_template_from_path(path)</code>","text":"<p>Replaces known root value with formattable key in path.</p> <p>All platform values are checked for this replacement.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Path where root value should be found.</p> required <p>Returns:</p> Name Type Description <code>tuple</code> <p>Tuple contain 2 values: <code>success</code> (bool) and <code>path</code> (str). When success it True then path should contain replaced root value with formattable key.</p> Example <p>When input path is::     \"C:/windows/path/root/projects/my_project/file.ext\"</p> <p>And raw data of item looks like::     {         \"windows\": \"C:/windows/path/root\",         \"linux\": \"/mount/root\"     }</p> <p>Output will be::     (True, \"{root}/projects/my_project/file.ext\")</p> <p>If any of raw data value wouldn't match path's root output is::     (False, \"C:/windows/path/root/projects/my_project/file.ext\")</p> Source code in <code>client/ayon_core/pipeline/anatomy/roots.py</code> <pre><code>def find_root_template_from_path(self, path):\n    \"\"\"Replaces known root value with formattable key in path.\n\n    All platform values are checked for this replacement.\n\n    Args:\n        path (str): Path where root value should be found.\n\n    Returns:\n        tuple: Tuple contain 2 values: `success` (bool) and `path` (str).\n            When success it True then path should contain replaced root\n            value with formattable key.\n\n    Example:\n        When input path is::\n            \"C:/windows/path/root/projects/my_project/file.ext\"\n\n        And raw data of item looks like::\n            {\n                \"windows\": \"C:/windows/path/root\",\n                \"linux\": \"/mount/root\"\n            }\n\n        Output will be::\n            (True, \"{root}/projects/my_project/file.ext\")\n\n        If any of raw data value wouldn't match path's root output is::\n            (False, \"C:/windows/path/root/projects/my_project/file.ext\")\n    \"\"\"\n    result = False\n    output = str(path)\n\n    mod_path = self._clean_path(path)\n    for root_os, root_path in self.cleaned_data.items():\n        # Skip empty paths\n        if not root_path:\n            continue\n\n        _mod_path = mod_path  # reset to original cleaned value\n        if root_os == \"windows\":\n            root_path = root_path.lower()\n            _mod_path = _mod_path.lower()\n\n        if _mod_path.startswith(root_path):\n            result = True\n            replacement = \"{\" + self.full_key + \"}\"\n            output = replacement + mod_path[len(root_path):]\n            break\n\n    return (result, output)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/index.html#client.ayon_core.pipeline.anatomy.AnatomyRoot.path_remapper","title":"<code>path_remapper(path, dst_platform=None, src_platform=None)</code>","text":"<p>Remap path for specific platform.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Source path which need to be remapped.</p> required <code>dst_platform</code> <code>str</code> <p>Specify destination platform for which remapping should happen.</p> <code>None</code> <code>src_platform</code> <code>str</code> <p>Specify source platform. This is recommended to not use and keep unset until you really want to use specific platform.</p> <code>None</code> <p>Returns:</p> Type Description <p>Union[str, None]: When path does not contain known root then None is returned else returns remapped path with \"{root[]}\". Source code in <code>client/ayon_core/pipeline/anatomy/roots.py</code> <pre><code>def path_remapper(self, path, dst_platform=None, src_platform=None):\n    \"\"\"Remap path for specific platform.\n\n    Args:\n        path (str): Source path which need to be remapped.\n        dst_platform (str, optional): Specify destination platform\n            for which remapping should happen.\n        src_platform (str, optional): Specify source platform. This is\n            recommended to not use and keep unset until you really want\n            to use specific platform.\n\n    Returns:\n        Union[str, None]: When path does not contain known root then\n            None is returned else returns remapped path with\n            \"{root[&lt;name&gt;]}\".\n\n    \"\"\"\n    cleaned_path = self._clean_path(path)\n    if dst_platform:\n        dst_root_clean = self.cleaned_data.get(dst_platform)\n        if not dst_root_clean:\n            self.log.warning(\n                \"Root \\\"{}\\\" miss platform \\\"{}\\\" definition.\".format(\n                    self.full_key, dst_platform\n                )\n            )\n            return None\n\n        if cleaned_path.startswith(dst_root_clean):\n            return cleaned_path\n\n    if src_platform:\n        src_root_clean = self.cleaned_data.get(src_platform)\n        if src_root_clean is None:\n            self.log.warning(\n                \"Root \\\"{}\\\" miss platform \\\"{}\\\" definition.\".format(\n                    self.full_key, src_platform\n                )\n            )\n            return None\n\n        if not cleaned_path.startswith(src_root_clean):\n            return None\n\n        subpath = cleaned_path[len(src_root_clean):]\n        if dst_platform:\n            # `dst_root_clean` is used from upper condition\n            return dst_root_clean + subpath\n        return self.clean_value + subpath\n\n    result, template = self.find_root_template_from_path(path)\n    if not result:\n        return None\n\n    if dst_platform:\n        fill_data = {self.name: dst_root_clean}\n    else:\n        fill_data = {self.name: self.value}\n\n    return template.format(**{\"root\": fill_data})\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/index.html#client.ayon_core.pipeline.anatomy.AnatomyRoots","title":"<code>AnatomyRoots</code>","text":"<p>Object which should be used for formatting \"root\" key in templates.</p> <p>Parameters:</p> Name Type Description Default <code>anatomy</code> <code>Anatomy</code> <p>Anatomy object created for a specific project.</p> required Source code in <code>client/ayon_core/pipeline/anatomy/roots.py</code> <pre><code>class AnatomyRoots:\n    \"\"\"Object which should be used for formatting \"root\" key in templates.\n\n    Args:\n        anatomy (Anatomy): Anatomy object created for a specific project.\n    \"\"\"\n\n    env_prefix = \"AYON_PROJECT_ROOT\"\n\n    def __init__(self, anatomy):\n        self._log = None\n        self._anatomy = anatomy\n        self._loaded_project = None\n        self._roots = None\n\n    def __format__(self, *args, **kwargs):\n        return self.roots.__format__(*args, **kwargs)\n\n    def __getitem__(self, key):\n        return self.roots[key]\n\n    @property\n    def log(self):\n        if self._log is None:\n            self._log = Logger.get_logger(self.__class__.__name__)\n        return self._log\n\n    @property\n    def anatomy(self):\n        \"\"\"Parent Anatomy object.\n\n        Returns:\n            Anatomy: Parent anatomy object.\n\n        \"\"\"\n        return self._anatomy\n\n    def reset(self):\n        \"\"\"Reset current roots value.\"\"\"\n        self._roots = None\n\n    def path_remapper(\n        self, path, dst_platform=None, src_platform=None, roots=None\n    ):\n        \"\"\"Remap path for specific platform.\n\n        Args:\n            path (str): Source path which need to be remapped.\n            dst_platform (Optional[str]): Specify destination platform\n                for which remapping should happen.\n            src_platform (Optional[str]): Specify source platform. This is\n                recommended to not use and keep unset until you really want\n                to use specific platform.\n            roots (Optional[Union[dict, AnatomyRoot])): It is possible to remap\n                path with different roots then instance where method was\n                called has.\n\n        Returns:\n            Union[str, None]: When path does not contain known root then\n                None is returned else returns remapped path with \"{root}\"\n                or \"{root[&lt;name&gt;]}\".\n\n        \"\"\"\n        if roots is None:\n            roots = self.roots\n\n        if roots is None:\n            raise ValueError(\"Roots are not set. Can't find path.\")\n\n        if \"{root\" in path:\n            path = path.format(**{\"root\": roots})\n            # If `dst_platform` is not specified then return else continue.\n            if not dst_platform:\n                return path\n\n        if isinstance(roots, AnatomyRoot):\n            return roots.path_remapper(path, dst_platform, src_platform)\n\n        for _root in roots.values():\n            result = self.path_remapper(\n                path, dst_platform, src_platform, _root\n            )\n            if result is not None:\n                return result\n\n    def find_root_template_from_path(self, path, roots=None):\n        \"\"\"Find root value in entered path and replace it with formatting key.\n\n        Args:\n            path (str): Source path where root will be searched.\n            roots (Optional[Union[AnatomyRoots, dict]): It is possible to use\n                different roots than instance where method was triggered has.\n\n        Returns:\n            tuple: Output contains tuple with bool representing success as\n                first value and path with or without replaced root with\n                formatting key as second value.\n\n        Raises:\n            ValueError: When roots are not entered and can't be loaded.\n        \"\"\"\n        if roots is None:\n            self.log.debug(\n                \"Looking for matching root in path \\\"{}\\\".\".format(path)\n            )\n            roots = self.roots\n\n        if roots is None:\n            raise ValueError(\"Roots are not set. Can't find path.\")\n\n        if isinstance(roots, AnatomyRoot):\n            return roots.find_root_template_from_path(path)\n\n        for root_name, _root in roots.items():\n            success, result = self.find_root_template_from_path(path, _root)\n            if success:\n                self.log.debug(\n                    \"Found match in root \\\"{}\\\".\".format(root_name)\n                )\n                return success, result\n\n        self.log.warning(\"No matching root was found in current setting.\")\n        return (False, path)\n\n    def set_root_environments(self):\n        \"\"\"Set root environments for current project.\"\"\"\n        for key, value in self.root_environments().items():\n            os.environ[key] = value\n\n    def root_environments(self):\n        \"\"\"Use root keys to create unique keys for environment variables.\n\n        Concatenates prefix \"AYON_PROJECT_ROOT_\" with root keys to create\n        unique keys.\n\n        Returns:\n            dict: Result is `{(str): (str)}` dicitonary where key represents\n                unique key concatenated by keys and value is root value of\n                current platform root.\n\n        Example:\n            With raw root values::\n                \"work\": {\n                    \"windows\": \"P:/projects/work\",\n                    \"linux\": \"/mnt/share/projects/work\",\n                    \"darwin\": \"/darwin/path/work\"\n                },\n                \"publish\": {\n                    \"windows\": \"P:/projects/publish\",\n                    \"linux\": \"/mnt/share/projects/publish\",\n                    \"darwin\": \"/darwin/path/publish\"\n                }\n\n            Result on windows platform::\n                {\n                    \"AYON_PROJECT_ROOT_WORK\": \"P:/projects/work\",\n                    \"AYON_PROJECT_ROOT_PUBLISH\": \"P:/projects/publish\"\n                }\n\n        \"\"\"\n        return self._root_environments()\n\n    def all_root_paths(self, roots=None):\n        \"\"\"Return all paths for all roots of all platforms.\"\"\"\n        if roots is None:\n            roots = self.roots\n\n        output = []\n        if isinstance(roots, AnatomyRoot):\n            for value in roots.raw_data.values():\n                output.append(value)\n            return output\n\n        for _roots in roots.values():\n            output.extend(self.all_root_paths(_roots))\n        return output\n\n    def _root_environments(self, keys=None, roots=None):\n        if not keys:\n            keys = []\n        if roots is None:\n            roots = self.roots\n\n        if isinstance(roots, AnatomyRoot):\n            key_items = [self.env_prefix]\n            for _key in keys:\n                key_items.append(_key.upper())\n\n            key = \"_\".join(key_items)\n            # Make sure key and value does not contain unicode\n            #   - can happen in Python 2 hosts\n            return {str(key): str(roots.value)}\n\n        output = {}\n        for _key, _value in roots.items():\n            _keys = list(keys)\n            _keys.append(_key)\n            output.update(self._root_environments(_keys, _value))\n        return output\n\n    def root_environmets_fill_data(self, template=None):\n        \"\"\"Environment variable values in dictionary for rootless path.\n\n        Args:\n            template (str): Template for environment variable key fill.\n                By default is set to `\"${}\"`.\n        \"\"\"\n        if template is None:\n            template = \"${}\"\n        return self._root_environmets_fill_data(template)\n\n    def _root_environmets_fill_data(self, template, keys=None, roots=None):\n        if keys is None and roots is None:\n            return {\n                \"root\": self._root_environmets_fill_data(\n                    template, [], self.roots\n                )\n            }\n\n        if isinstance(roots, AnatomyRoot):\n            key_items = [AnatomyRoots.env_prefix]\n            for _key in keys:\n                key_items.append(_key.upper())\n            key = \"_\".join(key_items)\n            return template.format(key)\n\n        output = {}\n        for key, value in roots.items():\n            _keys = list(keys)\n            _keys.append(key)\n            output[key] = self._root_environmets_fill_data(\n                template, _keys, value\n            )\n        return output\n\n    @property\n    def project_name(self):\n        \"\"\"Current project name which will be used for loading root values.\n\n        Returns:\n            str: Project name.\n        \"\"\"\n        return self._anatomy.project_name\n\n    @property\n    def roots(self):\n        \"\"\"Property for filling \"root\" key in templates.\n\n        This property returns roots for current project or default root values.\n\n        Warning:\n            Default roots value may cause issues when project use different\n            roots settings. That may happen when project use multiroot\n            templates but default roots miss their keys.\n\n        \"\"\"\n        if self.project_name != self._loaded_project:\n            self._roots = None\n\n        if self._roots is None:\n            self._roots = self._discover()\n            self._loaded_project = self.project_name\n        return self._roots\n\n    def _discover(self):\n        \"\"\" Loads current project's roots or default.\n\n        Default roots are loaded if project override's does not contain roots.\n\n        Returns:\n            `AnatomyRoot` or `dict` with multiple `AnatomyRoot`s when multiroot\n            setting is used.\n        \"\"\"\n\n        return self._parse_dict(self._anatomy[\"roots\"], self)\n\n    @staticmethod\n    def _parse_dict(data, parent):\n        \"\"\"Parse roots raw data into dictionary with AnatomyRoots.\n\n        Converting raw roots data to `AnatomyRoot` helps to handle platform\n            keys.\n\n        Args:\n            data (dict): Should contain raw roots data to be parsed.\n            parent (AnatomyRoots): Parent object set as parent\n                for ``AnatomyRoot``.\n\n        Returns:\n            dict[str, AnatomyRoot]: Root items by name.\n\n        \"\"\"\n        output = {}\n        for root_name, root_values in data.items():\n            output[root_name] = AnatomyRoot(\n                parent, root_values, root_name\n            )\n        return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/index.html#client.ayon_core.pipeline.anatomy.AnatomyRoots.anatomy","title":"<code>anatomy</code>  <code>property</code>","text":"<p>Parent Anatomy object.</p> <p>Returns:</p> Name Type Description <code>Anatomy</code> <p>Parent anatomy object.</p>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/index.html#client.ayon_core.pipeline.anatomy.AnatomyRoots.project_name","title":"<code>project_name</code>  <code>property</code>","text":"<p>Current project name which will be used for loading root values.</p> <p>Returns:</p> Name Type Description <code>str</code> <p>Project name.</p>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/index.html#client.ayon_core.pipeline.anatomy.AnatomyRoots.roots","title":"<code>roots</code>  <code>property</code>","text":"<p>Property for filling \"root\" key in templates.</p> <p>This property returns roots for current project or default root values.</p> Warning <p>Default roots value may cause issues when project use different roots settings. That may happen when project use multiroot templates but default roots miss their keys.</p>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/index.html#client.ayon_core.pipeline.anatomy.AnatomyRoots.all_root_paths","title":"<code>all_root_paths(roots=None)</code>","text":"<p>Return all paths for all roots of all platforms.</p> Source code in <code>client/ayon_core/pipeline/anatomy/roots.py</code> <pre><code>def all_root_paths(self, roots=None):\n    \"\"\"Return all paths for all roots of all platforms.\"\"\"\n    if roots is None:\n        roots = self.roots\n\n    output = []\n    if isinstance(roots, AnatomyRoot):\n        for value in roots.raw_data.values():\n            output.append(value)\n        return output\n\n    for _roots in roots.values():\n        output.extend(self.all_root_paths(_roots))\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/index.html#client.ayon_core.pipeline.anatomy.AnatomyRoots.find_root_template_from_path","title":"<code>find_root_template_from_path(path, roots=None)</code>","text":"<p>Find root value in entered path and replace it with formatting key.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Source path where root will be searched.</p> required <code>roots</code> <code>Optional[Union[AnatomyRoots, dict]</code> <p>It is possible to use different roots than instance where method was triggered has.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>tuple</code> <p>Output contains tuple with bool representing success as first value and path with or without replaced root with formatting key as second value.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>When roots are not entered and can't be loaded.</p> Source code in <code>client/ayon_core/pipeline/anatomy/roots.py</code> <pre><code>def find_root_template_from_path(self, path, roots=None):\n    \"\"\"Find root value in entered path and replace it with formatting key.\n\n    Args:\n        path (str): Source path where root will be searched.\n        roots (Optional[Union[AnatomyRoots, dict]): It is possible to use\n            different roots than instance where method was triggered has.\n\n    Returns:\n        tuple: Output contains tuple with bool representing success as\n            first value and path with or without replaced root with\n            formatting key as second value.\n\n    Raises:\n        ValueError: When roots are not entered and can't be loaded.\n    \"\"\"\n    if roots is None:\n        self.log.debug(\n            \"Looking for matching root in path \\\"{}\\\".\".format(path)\n        )\n        roots = self.roots\n\n    if roots is None:\n        raise ValueError(\"Roots are not set. Can't find path.\")\n\n    if isinstance(roots, AnatomyRoot):\n        return roots.find_root_template_from_path(path)\n\n    for root_name, _root in roots.items():\n        success, result = self.find_root_template_from_path(path, _root)\n        if success:\n            self.log.debug(\n                \"Found match in root \\\"{}\\\".\".format(root_name)\n            )\n            return success, result\n\n    self.log.warning(\"No matching root was found in current setting.\")\n    return (False, path)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/index.html#client.ayon_core.pipeline.anatomy.AnatomyRoots.path_remapper","title":"<code>path_remapper(path, dst_platform=None, src_platform=None, roots=None)</code>","text":"<p>Remap path for specific platform.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Source path which need to be remapped.</p> required <code>dst_platform</code> <code>Optional[str]</code> <p>Specify destination platform for which remapping should happen.</p> <code>None</code> <code>src_platform</code> <code>Optional[str]</code> <p>Specify source platform. This is recommended to not use and keep unset until you really want to use specific platform.</p> <code>None</code> <code>roots</code> <code>Optional[Union[dict, AnatomyRoot])</code> <p>It is possible to remap path with different roots then instance where method was called has.</p> <code>None</code> <p>Returns:</p> Type Description <p>Union[str, None]: When path does not contain known root then None is returned else returns remapped path with \"{root}\" or \"{root[]}\". Source code in <code>client/ayon_core/pipeline/anatomy/roots.py</code> <pre><code>def path_remapper(\n    self, path, dst_platform=None, src_platform=None, roots=None\n):\n    \"\"\"Remap path for specific platform.\n\n    Args:\n        path (str): Source path which need to be remapped.\n        dst_platform (Optional[str]): Specify destination platform\n            for which remapping should happen.\n        src_platform (Optional[str]): Specify source platform. This is\n            recommended to not use and keep unset until you really want\n            to use specific platform.\n        roots (Optional[Union[dict, AnatomyRoot])): It is possible to remap\n            path with different roots then instance where method was\n            called has.\n\n    Returns:\n        Union[str, None]: When path does not contain known root then\n            None is returned else returns remapped path with \"{root}\"\n            or \"{root[&lt;name&gt;]}\".\n\n    \"\"\"\n    if roots is None:\n        roots = self.roots\n\n    if roots is None:\n        raise ValueError(\"Roots are not set. Can't find path.\")\n\n    if \"{root\" in path:\n        path = path.format(**{\"root\": roots})\n        # If `dst_platform` is not specified then return else continue.\n        if not dst_platform:\n            return path\n\n    if isinstance(roots, AnatomyRoot):\n        return roots.path_remapper(path, dst_platform, src_platform)\n\n    for _root in roots.values():\n        result = self.path_remapper(\n            path, dst_platform, src_platform, _root\n        )\n        if result is not None:\n            return result\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/index.html#client.ayon_core.pipeline.anatomy.AnatomyRoots.reset","title":"<code>reset()</code>","text":"<p>Reset current roots value.</p> Source code in <code>client/ayon_core/pipeline/anatomy/roots.py</code> <pre><code>def reset(self):\n    \"\"\"Reset current roots value.\"\"\"\n    self._roots = None\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/index.html#client.ayon_core.pipeline.anatomy.AnatomyRoots.root_environments","title":"<code>root_environments()</code>","text":"<p>Use root keys to create unique keys for environment variables.</p> <p>Concatenates prefix \"AYON_PROJECT_ROOT_\" with root keys to create unique keys.</p> <p>Returns:</p> Name Type Description <code>dict</code> <p>Result is <code>{(str): (str)}</code> dicitonary where key represents unique key concatenated by keys and value is root value of current platform root.</p> Example <p>With raw root values::     \"work\": {         \"windows\": \"P:/projects/work\",         \"linux\": \"/mnt/share/projects/work\",         \"darwin\": \"/darwin/path/work\"     },     \"publish\": {         \"windows\": \"P:/projects/publish\",         \"linux\": \"/mnt/share/projects/publish\",         \"darwin\": \"/darwin/path/publish\"     }</p> <p>Result on windows platform::     {         \"AYON_PROJECT_ROOT_WORK\": \"P:/projects/work\",         \"AYON_PROJECT_ROOT_PUBLISH\": \"P:/projects/publish\"     }</p> Source code in <code>client/ayon_core/pipeline/anatomy/roots.py</code> <pre><code>def root_environments(self):\n    \"\"\"Use root keys to create unique keys for environment variables.\n\n    Concatenates prefix \"AYON_PROJECT_ROOT_\" with root keys to create\n    unique keys.\n\n    Returns:\n        dict: Result is `{(str): (str)}` dicitonary where key represents\n            unique key concatenated by keys and value is root value of\n            current platform root.\n\n    Example:\n        With raw root values::\n            \"work\": {\n                \"windows\": \"P:/projects/work\",\n                \"linux\": \"/mnt/share/projects/work\",\n                \"darwin\": \"/darwin/path/work\"\n            },\n            \"publish\": {\n                \"windows\": \"P:/projects/publish\",\n                \"linux\": \"/mnt/share/projects/publish\",\n                \"darwin\": \"/darwin/path/publish\"\n            }\n\n        Result on windows platform::\n            {\n                \"AYON_PROJECT_ROOT_WORK\": \"P:/projects/work\",\n                \"AYON_PROJECT_ROOT_PUBLISH\": \"P:/projects/publish\"\n            }\n\n    \"\"\"\n    return self._root_environments()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/index.html#client.ayon_core.pipeline.anatomy.AnatomyRoots.root_environmets_fill_data","title":"<code>root_environmets_fill_data(template=None)</code>","text":"<p>Environment variable values in dictionary for rootless path.</p> <p>Parameters:</p> Name Type Description Default <code>template</code> <code>str</code> <p>Template for environment variable key fill. By default is set to <code>\"${}\"</code>.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/anatomy/roots.py</code> <pre><code>def root_environmets_fill_data(self, template=None):\n    \"\"\"Environment variable values in dictionary for rootless path.\n\n    Args:\n        template (str): Template for environment variable key fill.\n            By default is set to `\"${}\"`.\n    \"\"\"\n    if template is None:\n        template = \"${}\"\n    return self._root_environmets_fill_data(template)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/index.html#client.ayon_core.pipeline.anatomy.AnatomyRoots.set_root_environments","title":"<code>set_root_environments()</code>","text":"<p>Set root environments for current project.</p> Source code in <code>client/ayon_core/pipeline/anatomy/roots.py</code> <pre><code>def set_root_environments(self):\n    \"\"\"Set root environments for current project.\"\"\"\n    for key, value in self.root_environments().items():\n        os.environ[key] = value\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/index.html#client.ayon_core.pipeline.anatomy.AnatomyStringTemplate","title":"<code>AnatomyStringTemplate</code>","text":"<p>               Bases: <code>StringTemplate</code></p> <p>String template which has access to anatomy.</p> <p>Parameters:</p> Name Type Description Default <code>anatomy_templates</code> <code>AnatomyTemplates</code> <p>Anatomy templates object.</p> required <code>template</code> <code>str</code> <p>Template string.</p> required Source code in <code>client/ayon_core/pipeline/anatomy/templates.py</code> <pre><code>class AnatomyStringTemplate(StringTemplate):\n    \"\"\"String template which has access to anatomy.\n\n    Args:\n        anatomy_templates (AnatomyTemplates): Anatomy templates object.\n        template (str): Template string.\n    \"\"\"\n\n    def __init__(self, anatomy_templates, template):\n        self.anatomy_templates = anatomy_templates\n        super(AnatomyStringTemplate, self).__init__(template)\n\n    def format(self, data):\n        \"\"\"Format template and add 'root' key to data if not available.\n\n        Args:\n            data (dict[str, Any]): Formatting data for template.\n\n        Returns:\n            AnatomyTemplateResult: Formatting result.\n        \"\"\"\n\n        anatomy_templates = self.anatomy_templates\n        if not data.get(\"root\"):\n            data = copy.deepcopy(data)\n            data[\"root\"] = anatomy_templates.anatomy.roots\n        result = StringTemplate.format(self, data)\n        rootless_path = anatomy_templates.get_rootless_path_from_result(\n            result\n        )\n        return AnatomyTemplateResult(result, rootless_path)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/index.html#client.ayon_core.pipeline.anatomy.AnatomyStringTemplate.format","title":"<code>format(data)</code>","text":"<p>Format template and add 'root' key to data if not available.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict[str, Any]</code> <p>Formatting data for template.</p> required <p>Returns:</p> Name Type Description <code>AnatomyTemplateResult</code> <p>Formatting result.</p> Source code in <code>client/ayon_core/pipeline/anatomy/templates.py</code> <pre><code>def format(self, data):\n    \"\"\"Format template and add 'root' key to data if not available.\n\n    Args:\n        data (dict[str, Any]): Formatting data for template.\n\n    Returns:\n        AnatomyTemplateResult: Formatting result.\n    \"\"\"\n\n    anatomy_templates = self.anatomy_templates\n    if not data.get(\"root\"):\n        data = copy.deepcopy(data)\n        data[\"root\"] = anatomy_templates.anatomy.roots\n    result = StringTemplate.format(self, data)\n    rootless_path = anatomy_templates.get_rootless_path_from_result(\n        result\n    )\n    return AnatomyTemplateResult(result, rootless_path)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/index.html#client.ayon_core.pipeline.anatomy.AnatomyTemplateResult","title":"<code>AnatomyTemplateResult</code>","text":"<p>               Bases: <code>TemplateResult</code></p> Source code in <code>client/ayon_core/pipeline/anatomy/templates.py</code> <pre><code>class AnatomyTemplateResult(TemplateResult):\n    rootless = None\n\n    def __new__(cls, result, rootless_path):\n        new_obj = super(AnatomyTemplateResult, cls).__new__(\n            cls,\n            str(result),\n            result.template,\n            result.solved,\n            result.used_values,\n            result.missing_keys,\n            result.invalid_types\n        )\n        new_obj.rootless = rootless_path\n        return new_obj\n\n    def validate(self):\n        if not self.solved:\n            raise AnatomyTemplateUnsolved(\n                self.template,\n                self.missing_keys,\n                self.invalid_types\n            )\n\n    def copy(self):\n        tmp = TemplateResult(\n            str(self),\n            self.template,\n            self.solved,\n            self.used_values,\n            self.missing_keys,\n            self.invalid_types\n        )\n        return self.__class__(tmp, self.rootless)\n\n    def normalized(self):\n        \"\"\"Convert to normalized path.\"\"\"\n\n        tmp = TemplateResult(\n            os.path.normpath(self),\n            self.template,\n            self.solved,\n            self.used_values,\n            self.missing_keys,\n            self.invalid_types\n        )\n        return self.__class__(tmp, self.rootless)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/index.html#client.ayon_core.pipeline.anatomy.AnatomyTemplateResult.normalized","title":"<code>normalized()</code>","text":"<p>Convert to normalized path.</p> Source code in <code>client/ayon_core/pipeline/anatomy/templates.py</code> <pre><code>def normalized(self):\n    \"\"\"Convert to normalized path.\"\"\"\n\n    tmp = TemplateResult(\n        os.path.normpath(self),\n        self.template,\n        self.solved,\n        self.used_values,\n        self.missing_keys,\n        self.invalid_types\n    )\n    return self.__class__(tmp, self.rootless)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/index.html#client.ayon_core.pipeline.anatomy.AnatomyTemplateUnsolved","title":"<code>AnatomyTemplateUnsolved</code>","text":"<p>               Bases: <code>TemplateUnsolved</code></p> <p>Exception for unsolved template when strict is set to True.</p> Source code in <code>client/ayon_core/pipeline/anatomy/exceptions.py</code> <pre><code>class AnatomyTemplateUnsolved(TemplateUnsolved):\n    \"\"\"Exception for unsolved template when strict is set to True.\"\"\"\n\n    msg = \"Anatomy template \\\"{0}\\\" is unsolved.{1}{2}\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/index.html#client.ayon_core.pipeline.anatomy.ProjectNotSet","title":"<code>ProjectNotSet</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Exception raised when is created Anatomy without project name.</p> Source code in <code>client/ayon_core/pipeline/anatomy/exceptions.py</code> <pre><code>class ProjectNotSet(Exception):\n    \"\"\"Exception raised when is created Anatomy without project name.\"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/index.html#client.ayon_core.pipeline.anatomy.RootCombinationError","title":"<code>RootCombinationError</code>","text":"<p>               Bases: <code>Exception</code></p> <p>This exception is raised when templates has combined root types.</p> Source code in <code>client/ayon_core/pipeline/anatomy/exceptions.py</code> <pre><code>class RootCombinationError(Exception):\n    \"\"\"This exception is raised when templates has combined root types.\"\"\"\n\n    def __init__(self, roots):\n        joined_roots = \", \".join(\n            [\"\\\"{}\\\"\".format(_root) for _root in roots]\n        )\n        # TODO better error message\n        msg = (\n            \"Combination of root with and\"\n            \" without root name in AnatomyTemplates. {}\"\n        ).format(joined_roots)\n\n        super(RootCombinationError, self).__init__(msg)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/index.html#client.ayon_core.pipeline.anatomy.RootMissingEnv","title":"<code>RootMissingEnv</code>","text":"<p>               Bases: <code>KeyError</code></p> <p>Raised when root requires environment variables which is not filled.</p> Source code in <code>client/ayon_core/pipeline/anatomy/exceptions.py</code> <pre><code>class RootMissingEnv(KeyError):\n    \"\"\"Raised when root requires environment variables which is not filled.\"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/index.html#client.ayon_core.pipeline.anatomy.TemplateMissingKey","title":"<code>TemplateMissingKey</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Exception for cases when key does not exist in template.</p> Source code in <code>client/ayon_core/pipeline/anatomy/exceptions.py</code> <pre><code>class TemplateMissingKey(Exception):\n    \"\"\"Exception for cases when key does not exist in template.\"\"\"\n\n    msg = \"Template key '{}' was not found.\"\n\n    def __init__(self, parents):\n        parent_join = \"\".join([\"[\\\"{0}\\\"]\".format(key) for key in parents])\n        super(TemplateMissingKey, self).__init__(\n            self.msg.format(parent_join)\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/anatomy.html","title":"anatomy","text":""},{"location":"autoapi/client/ayon_core/pipeline/anatomy/anatomy.html#client.ayon_core.pipeline.anatomy.anatomy.Anatomy","title":"<code>Anatomy</code>","text":"<p>               Bases: <code>BaseAnatomy</code></p> Source code in <code>client/ayon_core/pipeline/anatomy/anatomy.py</code> <pre><code>class Anatomy(BaseAnatomy):\n    _project_cache = NestedCacheItem(lifetime=10)\n    _sitesync_addon_cache = CacheItem(lifetime=60)\n    _default_site_id_cache = NestedCacheItem(lifetime=60)\n    _root_overrides_cache = NestedCacheItem(2, lifetime=60)\n\n    def __init__(\n        self, project_name=None, site_name=None, project_entity=None\n    ):\n        if not project_name:\n            project_name = os.environ.get(\"AYON_PROJECT_NAME\")\n\n        if not project_name:\n            raise ProjectNotSet((\n                \"Implementation bug: Project name is not set. Anatomy requires\"\n                \" to load data for specific project.\"\n            ))\n\n        if not project_entity:\n            project_entity = self.get_project_entity_from_cache(project_name)\n        root_overrides = self._get_site_root_overrides(\n            project_name, site_name\n        )\n\n        super(Anatomy, self).__init__(project_entity, root_overrides)\n\n    @classmethod\n    def get_project_entity_from_cache(cls, project_name):\n        project_cache = cls._project_cache[project_name]\n        if not project_cache.is_valid:\n            project_cache.update_data(ayon_api.get_project(project_name))\n        return copy.deepcopy(project_cache.get_data())\n\n    @classmethod\n    def get_sitesync_addon(cls):\n        if not cls._sitesync_addon_cache.is_valid:\n            manager = AddonsManager()\n            cls._sitesync_addon_cache.update_data(\n                manager.get_enabled_addon(\"sitesync\")\n            )\n        return cls._sitesync_addon_cache.get_data()\n\n    @classmethod\n    def _get_studio_roots_overrides(cls, project_name):\n        \"\"\"This would return 'studio' site override by local settings.\n\n        Notes:\n            This logic handles local overrides of studio site which may be\n                available even when sync server is not enabled.\n            Handling of 'studio' and 'local' site was separated as preparation\n                for AYON development where that will be received from\n                separated sources.\n\n        Args:\n            project_name (str): Name of project.\n\n        Returns:\n            Union[Dict[str, str], None]): Local root overrides.\n        \"\"\"\n        if not project_name:\n            return None\n        return ayon_api.get_project_root_overrides_by_site_id(\n            project_name, get_local_site_id()\n        )\n\n    @classmethod\n    def _get_site_root_overrides(cls, project_name, site_name):\n        \"\"\"Get root overrides for site.\n\n        Args:\n            project_name (str): Project name for which root overrides should be\n                received.\n            site_name (Union[str, None]): Name of site for which root overrides\n                should be returned.\n        \"\"\"\n\n        # First check if sync server is available and enabled\n        sitesync_addon = cls.get_sitesync_addon()\n        if sitesync_addon is None or not sitesync_addon.enabled:\n            # QUESTION is ok to force 'studio' when site sync is not enabled?\n            site_name = \"studio\"\n\n        elif not site_name:\n            # Use sync server to receive active site name\n            project_cache = cls._default_site_id_cache[project_name]\n            if not project_cache.is_valid:\n                project_cache.update_data(\n                    sitesync_addon.get_active_site_type(project_name)\n                )\n            site_name = project_cache.get_data()\n\n        site_cache = cls._root_overrides_cache[project_name][site_name]\n        if not site_cache.is_valid:\n            if site_name == \"studio\":\n                # Handle studio root overrides without sync server\n                # - studio root overrides can be done even without sync server\n                roots_overrides = cls._get_studio_roots_overrides(\n                    project_name\n                )\n            else:\n                # Ask sync server to get roots overrides\n                roots_overrides = sitesync_addon.get_site_root_overrides(\n                    project_name, site_name\n                )\n            site_cache.update_data(roots_overrides)\n        return site_cache.get_data()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/anatomy.html#client.ayon_core.pipeline.anatomy.anatomy.BaseAnatomy","title":"<code>BaseAnatomy</code>","text":"<p>               Bases: <code>object</code></p> <p>Anatomy module helps to keep project settings.</p> <p>Wraps key project specifications, AnatomyTemplates and AnatomyRoots.</p> Source code in <code>client/ayon_core/pipeline/anatomy/anatomy.py</code> <pre><code>class BaseAnatomy(object):\n    \"\"\"Anatomy module helps to keep project settings.\n\n    Wraps key project specifications, AnatomyTemplates and AnatomyRoots.\n    \"\"\"\n    root_key_regex = re.compile(r\"{(root?[^}]+)}\")\n    root_name_regex = re.compile(r\"root\\[([^]]+)\\]\")\n\n    def __init__(self, project_entity, root_overrides=None):\n        self._project_name = project_entity[\"name\"]\n        self._project_code = project_entity[\"code\"]\n\n        self._data = self._prepare_anatomy_data(\n            project_entity, root_overrides\n        )\n        self._templates_obj = AnatomyTemplates(self)\n        self._roots_obj = AnatomyRoots(self)\n\n    # Anatomy used as dictionary\n    # - implemented only getters returning copy\n    def __getitem__(self, key):\n        return copy.deepcopy(self._data[key])\n\n    def get(self, key, default=None):\n        if key not in self._data:\n            return default\n        return copy.deepcopy(self._data[key])\n\n    def keys(self):\n        return copy.deepcopy(self._data).keys()\n\n    def values(self):\n        return copy.deepcopy(self._data).values()\n\n    def items(self):\n        return copy.deepcopy(self._data).items()\n\n    @property\n    def project_name(self):\n        \"\"\"Project name for which is anatomy prepared.\n\n        Returns:\n            str: Project name.\n\n        \"\"\"\n        return self._project_name\n\n    @property\n    def project_code(self):\n        \"\"\"Project name for which is anatomy prepared.\n\n        Returns:\n            str: Project code.\n\n        \"\"\"\n        return self._project_code\n\n    def _prepare_anatomy_data(self, project_entity, root_overrides):\n        \"\"\"Prepare anatomy data for further processing.\n\n        Method added to replace `{task}` with `{task[name]}` in templates.\n        \"\"\"\n\n        anatomy_data = self._project_entity_to_anatomy_data(project_entity)\n\n        self._apply_local_settings_on_anatomy_data(\n            anatomy_data,\n            root_overrides\n        )\n\n        return anatomy_data\n\n    @property\n    def templates(self):\n        \"\"\"Wrap property `templates` of Anatomy's AnatomyTemplates instance.\"\"\"\n        return self._templates_obj.templates\n\n    @property\n    def templates_obj(self):\n        \"\"\"Return `AnatomyTemplates` object of current Anatomy instance.\"\"\"\n        return self._templates_obj\n\n    def get_template_item(\n            self,\n            category_name: str,\n            template_name: str,\n            subkey: str | None = None,\n            default: Any = NOT_SET,\n    ) -&gt; TemplateItem | Any:\n        \"\"\"Get template item from category.\n\n        Args:\n            category_name (str): Category name.\n            template_name (str): Template name.\n            subkey (Optional[str]): Subkey name.\n            default (Any): Default value.\n\n        Returns:\n            Any: Template item, subkey value as AnatomyStringTemplate or None.\n\n        \"\"\"\n        return self._templates_obj.get_template_item(\n            category_name, template_name, subkey, default\n        )\n\n    def format(self, *args, **kwargs):\n        \"\"\"Wrap `format` method of Anatomy's `templates_obj`.\"\"\"\n        return self._templates_obj.format(*args, **kwargs)\n\n    def format_all(self, *args, **kwargs):\n        \"\"\"Wrap `format_all` method of Anatomy's `templates_obj`.\n\n        Deprecated:\n            Use ``format`` method with ``strict=False`` instead.\n\n        \"\"\"\n        return self._templates_obj.format_all(*args, **kwargs)\n\n    @property\n    def roots(self):\n        \"\"\"Wrap `roots` property of Anatomy's `roots_obj`.\"\"\"\n        return self._roots_obj.roots\n\n    @property\n    def roots_obj(self):\n        \"\"\"Roots wrapper object.\n\n        Returns:\n            AnatomyRoots: Roots wrapper.\n\n        \"\"\"\n        return self._roots_obj\n\n    def root_environments(self):\n        \"\"\"Return AYON_PROJECT_ROOT_* environments for current project.\"\"\"\n        return self._roots_obj.root_environments()\n\n    def root_environmets_fill_data(self, template=None):\n        \"\"\"Environment variable values in dictionary for rootless path.\n\n        Args:\n            template (str): Template for environment variable key fill.\n                By default is set to `\"${}\"`.\n        \"\"\"\n        return self.roots_obj.root_environmets_fill_data(template)\n\n    def find_root_template_from_path(self, *args, **kwargs):\n        \"\"\"Wrapper for AnatomyRoots `find_root_template_from_path`.\"\"\"\n        return self.roots_obj.find_root_template_from_path(*args, **kwargs)\n\n    def path_remapper(self, *args, **kwargs):\n        \"\"\"Wrapper for AnatomyRoots `path_remapper`.\"\"\"\n        return self.roots_obj.path_remapper(*args, **kwargs)\n\n    def all_root_paths(self):\n        \"\"\"Wrapper for AnatomyRoots `all_root_paths`.\"\"\"\n        return self.roots_obj.all_root_paths()\n\n    def set_root_environments(self):\n        \"\"\"Set AYON_PROJECT_ROOT_* environments for current project.\"\"\"\n        self._roots_obj.set_root_environments()\n\n    def root_names(self):\n        \"\"\"Return root names for current project.\"\"\"\n        return self.root_names_from_templates(self.templates)\n\n    def _root_keys_from_templates(self, data):\n        \"\"\"Extract root key from templates in data.\n\n        Args:\n            data (dict): Data that may contain templates as string.\n\n        Return:\n            set: Set of all root names from templates as strings.\n\n        Output example: `{\"root[work]\", \"root[publish]\"}`\n        \"\"\"\n\n        output = set()\n        keys_queue = collections.deque()\n        keys_queue.append(data)\n        while keys_queue:\n            queue_data = keys_queue.popleft()\n            if isinstance(queue_data, StringTemplate):\n                queue_data = queue_data.template\n\n            if isinstance(queue_data, dict):\n                for value in queue_data.values():\n                    keys_queue.append(value)\n\n            elif isinstance(queue_data, str):\n                for group in re.findall(self.root_key_regex, queue_data):\n                    output.add(group)\n\n        return output\n\n    def root_value_for_template(self, template):\n        \"\"\"Returns value of root key from template.\"\"\"\n        if isinstance(template, StringTemplate):\n            template = template.template\n        root_templates = []\n        for group in re.findall(self.root_key_regex, template):\n            root_templates.append(\"{\" + group + \"}\")\n\n        if not root_templates:\n            return None\n\n        return root_templates[0].format(**{\"root\": self.roots})\n\n    def root_names_from_templates(self, templates):\n        \"\"\"Extract root names form anatomy templates.\n\n        Returns None if values in templates contain only \"{root}\".\n        Empty list is returned if there is no \"root\" in templates.\n        Else returns all root names from templates in list.\n\n        RootCombinationError is raised when templates contain both root types,\n        basic \"{root}\" and with root name specification \"{root[work]}\".\n\n        Args:\n            templates (dict): Anatomy templates where roots are not filled.\n\n        Return:\n            list/None: List of all root names from templates as strings when\n            multiroot setup is used, otherwise None is returned.\n        \"\"\"\n        roots = list(self._root_keys_from_templates(templates))\n        # Return empty list if no roots found in templates\n        if not roots:\n            return roots\n\n        # Raise exception when root keys have roots with and without root name.\n        # Invalid output example: [\"root\", \"root[project]\", \"root[render]\"]\n        if len(roots) &gt; 1 and \"root\" in roots:\n            raise RootCombinationError(roots)\n\n        # Return None if \"root\" without root name in templates\n        if len(roots) == 1 and roots[0] == \"root\":\n            return None\n\n        names = set()\n        for root in roots:\n            for group in re.findall(self.root_name_regex, root):\n                names.add(group)\n        return list(names)\n\n    def fill_root(self, template_path):\n        \"\"\"Fill template path where is only \"root\" key unfilled.\n\n        Args:\n            template_path (str): Path with \"root\" key in.\n                Example path: \"{root}/projects/MyProject/Shot01/Lighting/...\"\n\n        Return:\n            str: formatted path\n        \"\"\"\n        # NOTE does not care if there are different keys than \"root\"\n        return template_path.format(**{\"root\": self.roots})\n\n    @classmethod\n    def fill_root_with_path(cls, rootless_path, root_path):\n        \"\"\"Fill path without filled \"root\" key with passed path.\n\n        This is helper to fill root with different directory path than anatomy\n        has defined no matter if is single or multiroot.\n\n        Output path is same as input path if `rootless_path` does not contain\n        unfilled root key.\n\n        Args:\n            rootless_path (str): Path without filled \"root\" key. Example:\n                \"{root[work]}/MyProject/...\"\n            root_path (str): What should replace root key in `rootless_path`.\n\n        Returns:\n            str: Path with filled root.\n        \"\"\"\n        output = str(rootless_path)\n        for group in re.findall(cls.root_key_regex, rootless_path):\n            replacement = \"{\" + group + \"}\"\n            output = output.replace(replacement, root_path)\n\n        return output\n\n    def replace_root_with_env_key(self, filepath, template=None):\n        \"\"\"Replace root of path with environment key.\n\n        # Example:\n        ## Project with roots:\n        ```\n        {\n            \"nas\": {\n                \"windows\": P:/projects\",\n                ...\n            }\n            ...\n        }\n        ```\n\n        ## Entered filepath\n        \"P:/projects/project/folder/task/animation_v001.ma\"\n\n        ## Entered template\n        \"&lt;{}&gt;\"\n\n        ## Output\n        \"&lt;AYON_PROJECT_ROOT_NAS&gt;/project/folder/task/animation_v001.ma\"\n\n        Args:\n            filepath (str): Full file path where root should be replaced.\n            template (str): Optional template for environment key. Must\n                have one index format key.\n                Default value if not entered: \"${}\"\n\n        Returns:\n            str: Path where root is replaced with environment root key.\n\n        Raise:\n            ValueError: When project's roots were not found in entered path.\n        \"\"\"\n        success, rootless_path = self.find_root_template_from_path(filepath)\n        if not success:\n            raise ValueError(\n                \"{}: Project's roots were not found in path: {}\".format(\n                    self.project_name, filepath\n                )\n            )\n\n        data = self.root_environmets_fill_data(template)\n        return rootless_path.format(**data)\n\n    def _project_entity_to_anatomy_data(self, project_entity):\n        \"\"\"Convert project document to anatomy data.\n\n        Probably should fill missing keys and values.\n        \"\"\"\n\n        output = copy.deepcopy(project_entity[\"config\"])\n        # TODO remove AYON convertion\n        task_types = copy.deepcopy(project_entity[\"taskTypes\"])\n        new_task_types = {}\n        for task_type in task_types:\n            name = task_type[\"name\"]\n            new_task_types[name] = task_type\n        output[\"tasks\"] = new_task_types\n        output[\"attributes\"] = copy.deepcopy(project_entity[\"attrib\"])\n\n        return output\n\n    def _apply_local_settings_on_anatomy_data(\n        self, anatomy_data, root_overrides\n    ):\n        \"\"\"Apply local settings on anatomy data.\n\n        ATM local settings can modify project roots. Project name is required\n        as local settings have data stored data by project's name.\n\n        Local settings override root values in this order:\n        1.) Check if local settings contain overrides for default project and\n            apply it's values on roots if there are any.\n        2.) If passed `project_name` is not None then check project specific\n            overrides in local settings for the project and apply it's value on\n            roots if there are any.\n\n        NOTE: Root values of default project from local settings are always\n        applied if are set.\n\n        Args:\n            anatomy_data (dict): Data for anatomy.\n            root_overrides (dict): Data of local settings.\n        \"\"\"\n\n        # Skip processing if roots for current active site are not available in\n        #   local settings\n        if not root_overrides:\n            return\n\n        current_platform = platform.system().lower()\n\n        root_data = anatomy_data[\"roots\"]\n        for root_name, path in root_overrides.items():\n            if root_name not in root_data:\n                continue\n            anatomy_data[\"roots\"][root_name][current_platform] = (\n                path\n            )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/anatomy.html#client.ayon_core.pipeline.anatomy.anatomy.BaseAnatomy.project_code","title":"<code>project_code</code>  <code>property</code>","text":"<p>Project name for which is anatomy prepared.</p> <p>Returns:</p> Name Type Description <code>str</code> <p>Project code.</p>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/anatomy.html#client.ayon_core.pipeline.anatomy.anatomy.BaseAnatomy.project_name","title":"<code>project_name</code>  <code>property</code>","text":"<p>Project name for which is anatomy prepared.</p> <p>Returns:</p> Name Type Description <code>str</code> <p>Project name.</p>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/anatomy.html#client.ayon_core.pipeline.anatomy.anatomy.BaseAnatomy.roots","title":"<code>roots</code>  <code>property</code>","text":"<p>Wrap <code>roots</code> property of Anatomy's <code>roots_obj</code>.</p>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/anatomy.html#client.ayon_core.pipeline.anatomy.anatomy.BaseAnatomy.roots_obj","title":"<code>roots_obj</code>  <code>property</code>","text":"<p>Roots wrapper object.</p> <p>Returns:</p> Name Type Description <code>AnatomyRoots</code> <p>Roots wrapper.</p>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/anatomy.html#client.ayon_core.pipeline.anatomy.anatomy.BaseAnatomy.templates","title":"<code>templates</code>  <code>property</code>","text":"<p>Wrap property <code>templates</code> of Anatomy's AnatomyTemplates instance.</p>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/anatomy.html#client.ayon_core.pipeline.anatomy.anatomy.BaseAnatomy.templates_obj","title":"<code>templates_obj</code>  <code>property</code>","text":"<p>Return <code>AnatomyTemplates</code> object of current Anatomy instance.</p>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/anatomy.html#client.ayon_core.pipeline.anatomy.anatomy.BaseAnatomy.all_root_paths","title":"<code>all_root_paths()</code>","text":"<p>Wrapper for AnatomyRoots <code>all_root_paths</code>.</p> Source code in <code>client/ayon_core/pipeline/anatomy/anatomy.py</code> <pre><code>def all_root_paths(self):\n    \"\"\"Wrapper for AnatomyRoots `all_root_paths`.\"\"\"\n    return self.roots_obj.all_root_paths()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/anatomy.html#client.ayon_core.pipeline.anatomy.anatomy.BaseAnatomy.fill_root","title":"<code>fill_root(template_path)</code>","text":"<p>Fill template path where is only \"root\" key unfilled.</p> <p>Parameters:</p> Name Type Description Default <code>template_path</code> <code>str</code> <p>Path with \"root\" key in. Example path: \"{root}/projects/MyProject/Shot01/Lighting/...\"</p> required Return <p>str: formatted path</p> Source code in <code>client/ayon_core/pipeline/anatomy/anatomy.py</code> <pre><code>def fill_root(self, template_path):\n    \"\"\"Fill template path where is only \"root\" key unfilled.\n\n    Args:\n        template_path (str): Path with \"root\" key in.\n            Example path: \"{root}/projects/MyProject/Shot01/Lighting/...\"\n\n    Return:\n        str: formatted path\n    \"\"\"\n    # NOTE does not care if there are different keys than \"root\"\n    return template_path.format(**{\"root\": self.roots})\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/anatomy.html#client.ayon_core.pipeline.anatomy.anatomy.BaseAnatomy.fill_root_with_path","title":"<code>fill_root_with_path(rootless_path, root_path)</code>  <code>classmethod</code>","text":"<p>Fill path without filled \"root\" key with passed path.</p> <p>This is helper to fill root with different directory path than anatomy has defined no matter if is single or multiroot.</p> <p>Output path is same as input path if <code>rootless_path</code> does not contain unfilled root key.</p> <p>Parameters:</p> Name Type Description Default <code>rootless_path</code> <code>str</code> <p>Path without filled \"root\" key. Example: \"{root[work]}/MyProject/...\"</p> required <code>root_path</code> <code>str</code> <p>What should replace root key in <code>rootless_path</code>.</p> required <p>Returns:</p> Name Type Description <code>str</code> <p>Path with filled root.</p> Source code in <code>client/ayon_core/pipeline/anatomy/anatomy.py</code> <pre><code>@classmethod\ndef fill_root_with_path(cls, rootless_path, root_path):\n    \"\"\"Fill path without filled \"root\" key with passed path.\n\n    This is helper to fill root with different directory path than anatomy\n    has defined no matter if is single or multiroot.\n\n    Output path is same as input path if `rootless_path` does not contain\n    unfilled root key.\n\n    Args:\n        rootless_path (str): Path without filled \"root\" key. Example:\n            \"{root[work]}/MyProject/...\"\n        root_path (str): What should replace root key in `rootless_path`.\n\n    Returns:\n        str: Path with filled root.\n    \"\"\"\n    output = str(rootless_path)\n    for group in re.findall(cls.root_key_regex, rootless_path):\n        replacement = \"{\" + group + \"}\"\n        output = output.replace(replacement, root_path)\n\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/anatomy.html#client.ayon_core.pipeline.anatomy.anatomy.BaseAnatomy.find_root_template_from_path","title":"<code>find_root_template_from_path(*args, **kwargs)</code>","text":"<p>Wrapper for AnatomyRoots <code>find_root_template_from_path</code>.</p> Source code in <code>client/ayon_core/pipeline/anatomy/anatomy.py</code> <pre><code>def find_root_template_from_path(self, *args, **kwargs):\n    \"\"\"Wrapper for AnatomyRoots `find_root_template_from_path`.\"\"\"\n    return self.roots_obj.find_root_template_from_path(*args, **kwargs)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/anatomy.html#client.ayon_core.pipeline.anatomy.anatomy.BaseAnatomy.format","title":"<code>format(*args, **kwargs)</code>","text":"<p>Wrap <code>format</code> method of Anatomy's <code>templates_obj</code>.</p> Source code in <code>client/ayon_core/pipeline/anatomy/anatomy.py</code> <pre><code>def format(self, *args, **kwargs):\n    \"\"\"Wrap `format` method of Anatomy's `templates_obj`.\"\"\"\n    return self._templates_obj.format(*args, **kwargs)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/anatomy.html#client.ayon_core.pipeline.anatomy.anatomy.BaseAnatomy.format_all","title":"<code>format_all(*args, **kwargs)</code>","text":"<p>Wrap <code>format_all</code> method of Anatomy's <code>templates_obj</code>.</p> Deprecated <p>Use <code>format</code> method with <code>strict=False</code> instead.</p> Source code in <code>client/ayon_core/pipeline/anatomy/anatomy.py</code> <pre><code>def format_all(self, *args, **kwargs):\n    \"\"\"Wrap `format_all` method of Anatomy's `templates_obj`.\n\n    Deprecated:\n        Use ``format`` method with ``strict=False`` instead.\n\n    \"\"\"\n    return self._templates_obj.format_all(*args, **kwargs)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/anatomy.html#client.ayon_core.pipeline.anatomy.anatomy.BaseAnatomy.get_template_item","title":"<code>get_template_item(category_name, template_name, subkey=None, default=NOT_SET)</code>","text":"<p>Get template item from category.</p> <p>Parameters:</p> Name Type Description Default <code>category_name</code> <code>str</code> <p>Category name.</p> required <code>template_name</code> <code>str</code> <p>Template name.</p> required <code>subkey</code> <code>Optional[str]</code> <p>Subkey name.</p> <code>None</code> <code>default</code> <code>Any</code> <p>Default value.</p> <code>NOT_SET</code> <p>Returns:</p> Name Type Description <code>Any</code> <code>TemplateItem | Any</code> <p>Template item, subkey value as AnatomyStringTemplate or None.</p> Source code in <code>client/ayon_core/pipeline/anatomy/anatomy.py</code> <pre><code>def get_template_item(\n        self,\n        category_name: str,\n        template_name: str,\n        subkey: str | None = None,\n        default: Any = NOT_SET,\n) -&gt; TemplateItem | Any:\n    \"\"\"Get template item from category.\n\n    Args:\n        category_name (str): Category name.\n        template_name (str): Template name.\n        subkey (Optional[str]): Subkey name.\n        default (Any): Default value.\n\n    Returns:\n        Any: Template item, subkey value as AnatomyStringTemplate or None.\n\n    \"\"\"\n    return self._templates_obj.get_template_item(\n        category_name, template_name, subkey, default\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/anatomy.html#client.ayon_core.pipeline.anatomy.anatomy.BaseAnatomy.path_remapper","title":"<code>path_remapper(*args, **kwargs)</code>","text":"<p>Wrapper for AnatomyRoots <code>path_remapper</code>.</p> Source code in <code>client/ayon_core/pipeline/anatomy/anatomy.py</code> <pre><code>def path_remapper(self, *args, **kwargs):\n    \"\"\"Wrapper for AnatomyRoots `path_remapper`.\"\"\"\n    return self.roots_obj.path_remapper(*args, **kwargs)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/anatomy.html#client.ayon_core.pipeline.anatomy.anatomy.BaseAnatomy.replace_root_with_env_key","title":"<code>replace_root_with_env_key(filepath, template=None)</code>","text":"<p>Replace root of path with environment key.</p>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/anatomy.html#client.ayon_core.pipeline.anatomy.anatomy.BaseAnatomy.replace_root_with_env_key--example","title":"Example:","text":""},{"location":"autoapi/client/ayon_core/pipeline/anatomy/anatomy.html#client.ayon_core.pipeline.anatomy.anatomy.BaseAnatomy.replace_root_with_env_key--project-with-roots","title":"Project with roots:","text":"<pre><code>{\n    \"nas\": {\n        \"windows\": P:/projects\",\n        ...\n    }\n    ...\n}\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/anatomy.html#client.ayon_core.pipeline.anatomy.anatomy.BaseAnatomy.replace_root_with_env_key--entered-filepath","title":"Entered filepath","text":"<p>\"P:/projects/project/folder/task/animation_v001.ma\"</p>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/anatomy.html#client.ayon_core.pipeline.anatomy.anatomy.BaseAnatomy.replace_root_with_env_key--entered-template","title":"Entered template","text":"<p>\"&lt;{}&gt;\"</p>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/anatomy.html#client.ayon_core.pipeline.anatomy.anatomy.BaseAnatomy.replace_root_with_env_key--output","title":"Output","text":"<p>\"/project/folder/task/animation_v001.ma\" <p>Parameters:</p> Name Type Description Default <code>filepath</code> <code>str</code> <p>Full file path where root should be replaced.</p> required <code>template</code> <code>str</code> <p>Optional template for environment key. Must have one index format key. Default value if not entered: \"${}\"</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <p>Path where root is replaced with environment root key.</p> Raise <p>ValueError: When project's roots were not found in entered path.</p> Source code in <code>client/ayon_core/pipeline/anatomy/anatomy.py</code> <pre><code>def replace_root_with_env_key(self, filepath, template=None):\n    \"\"\"Replace root of path with environment key.\n\n    # Example:\n    ## Project with roots:\n    ```\n    {\n        \"nas\": {\n            \"windows\": P:/projects\",\n            ...\n        }\n        ...\n    }\n    ```\n\n    ## Entered filepath\n    \"P:/projects/project/folder/task/animation_v001.ma\"\n\n    ## Entered template\n    \"&lt;{}&gt;\"\n\n    ## Output\n    \"&lt;AYON_PROJECT_ROOT_NAS&gt;/project/folder/task/animation_v001.ma\"\n\n    Args:\n        filepath (str): Full file path where root should be replaced.\n        template (str): Optional template for environment key. Must\n            have one index format key.\n            Default value if not entered: \"${}\"\n\n    Returns:\n        str: Path where root is replaced with environment root key.\n\n    Raise:\n        ValueError: When project's roots were not found in entered path.\n    \"\"\"\n    success, rootless_path = self.find_root_template_from_path(filepath)\n    if not success:\n        raise ValueError(\n            \"{}: Project's roots were not found in path: {}\".format(\n                self.project_name, filepath\n            )\n        )\n\n    data = self.root_environmets_fill_data(template)\n    return rootless_path.format(**data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/anatomy.html#client.ayon_core.pipeline.anatomy.anatomy.BaseAnatomy.root_environments","title":"<code>root_environments()</code>","text":"<p>Return AYON_PROJECT_ROOT_* environments for current project.</p> Source code in <code>client/ayon_core/pipeline/anatomy/anatomy.py</code> <pre><code>def root_environments(self):\n    \"\"\"Return AYON_PROJECT_ROOT_* environments for current project.\"\"\"\n    return self._roots_obj.root_environments()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/anatomy.html#client.ayon_core.pipeline.anatomy.anatomy.BaseAnatomy.root_environmets_fill_data","title":"<code>root_environmets_fill_data(template=None)</code>","text":"<p>Environment variable values in dictionary for rootless path.</p> <p>Parameters:</p> Name Type Description Default <code>template</code> <code>str</code> <p>Template for environment variable key fill. By default is set to <code>\"${}\"</code>.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/anatomy/anatomy.py</code> <pre><code>def root_environmets_fill_data(self, template=None):\n    \"\"\"Environment variable values in dictionary for rootless path.\n\n    Args:\n        template (str): Template for environment variable key fill.\n            By default is set to `\"${}\"`.\n    \"\"\"\n    return self.roots_obj.root_environmets_fill_data(template)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/anatomy.html#client.ayon_core.pipeline.anatomy.anatomy.BaseAnatomy.root_names","title":"<code>root_names()</code>","text":"<p>Return root names for current project.</p> Source code in <code>client/ayon_core/pipeline/anatomy/anatomy.py</code> <pre><code>def root_names(self):\n    \"\"\"Return root names for current project.\"\"\"\n    return self.root_names_from_templates(self.templates)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/anatomy.html#client.ayon_core.pipeline.anatomy.anatomy.BaseAnatomy.root_names_from_templates","title":"<code>root_names_from_templates(templates)</code>","text":"<p>Extract root names form anatomy templates.</p> <p>Returns None if values in templates contain only \"{root}\". Empty list is returned if there is no \"root\" in templates. Else returns all root names from templates in list.</p> <p>RootCombinationError is raised when templates contain both root types, basic \"{root}\" and with root name specification \"{root[work]}\".</p> <p>Parameters:</p> Name Type Description Default <code>templates</code> <code>dict</code> <p>Anatomy templates where roots are not filled.</p> required Return <p>list/None: List of all root names from templates as strings when multiroot setup is used, otherwise None is returned.</p> Source code in <code>client/ayon_core/pipeline/anatomy/anatomy.py</code> <pre><code>def root_names_from_templates(self, templates):\n    \"\"\"Extract root names form anatomy templates.\n\n    Returns None if values in templates contain only \"{root}\".\n    Empty list is returned if there is no \"root\" in templates.\n    Else returns all root names from templates in list.\n\n    RootCombinationError is raised when templates contain both root types,\n    basic \"{root}\" and with root name specification \"{root[work]}\".\n\n    Args:\n        templates (dict): Anatomy templates where roots are not filled.\n\n    Return:\n        list/None: List of all root names from templates as strings when\n        multiroot setup is used, otherwise None is returned.\n    \"\"\"\n    roots = list(self._root_keys_from_templates(templates))\n    # Return empty list if no roots found in templates\n    if not roots:\n        return roots\n\n    # Raise exception when root keys have roots with and without root name.\n    # Invalid output example: [\"root\", \"root[project]\", \"root[render]\"]\n    if len(roots) &gt; 1 and \"root\" in roots:\n        raise RootCombinationError(roots)\n\n    # Return None if \"root\" without root name in templates\n    if len(roots) == 1 and roots[0] == \"root\":\n        return None\n\n    names = set()\n    for root in roots:\n        for group in re.findall(self.root_name_regex, root):\n            names.add(group)\n    return list(names)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/anatomy.html#client.ayon_core.pipeline.anatomy.anatomy.BaseAnatomy.root_value_for_template","title":"<code>root_value_for_template(template)</code>","text":"<p>Returns value of root key from template.</p> Source code in <code>client/ayon_core/pipeline/anatomy/anatomy.py</code> <pre><code>def root_value_for_template(self, template):\n    \"\"\"Returns value of root key from template.\"\"\"\n    if isinstance(template, StringTemplate):\n        template = template.template\n    root_templates = []\n    for group in re.findall(self.root_key_regex, template):\n        root_templates.append(\"{\" + group + \"}\")\n\n    if not root_templates:\n        return None\n\n    return root_templates[0].format(**{\"root\": self.roots})\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/anatomy.html#client.ayon_core.pipeline.anatomy.anatomy.BaseAnatomy.set_root_environments","title":"<code>set_root_environments()</code>","text":"<p>Set AYON_PROJECT_ROOT_* environments for current project.</p> Source code in <code>client/ayon_core/pipeline/anatomy/anatomy.py</code> <pre><code>def set_root_environments(self):\n    \"\"\"Set AYON_PROJECT_ROOT_* environments for current project.\"\"\"\n    self._roots_obj.set_root_environments()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/exceptions.html","title":"exceptions","text":""},{"location":"autoapi/client/ayon_core/pipeline/anatomy/exceptions.html#client.ayon_core.pipeline.anatomy.exceptions.AnatomyTemplateUnsolved","title":"<code>AnatomyTemplateUnsolved</code>","text":"<p>               Bases: <code>TemplateUnsolved</code></p> <p>Exception for unsolved template when strict is set to True.</p> Source code in <code>client/ayon_core/pipeline/anatomy/exceptions.py</code> <pre><code>class AnatomyTemplateUnsolved(TemplateUnsolved):\n    \"\"\"Exception for unsolved template when strict is set to True.\"\"\"\n\n    msg = \"Anatomy template \\\"{0}\\\" is unsolved.{1}{2}\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/exceptions.html#client.ayon_core.pipeline.anatomy.exceptions.ProjectNotSet","title":"<code>ProjectNotSet</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Exception raised when is created Anatomy without project name.</p> Source code in <code>client/ayon_core/pipeline/anatomy/exceptions.py</code> <pre><code>class ProjectNotSet(Exception):\n    \"\"\"Exception raised when is created Anatomy without project name.\"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/exceptions.html#client.ayon_core.pipeline.anatomy.exceptions.RootCombinationError","title":"<code>RootCombinationError</code>","text":"<p>               Bases: <code>Exception</code></p> <p>This exception is raised when templates has combined root types.</p> Source code in <code>client/ayon_core/pipeline/anatomy/exceptions.py</code> <pre><code>class RootCombinationError(Exception):\n    \"\"\"This exception is raised when templates has combined root types.\"\"\"\n\n    def __init__(self, roots):\n        joined_roots = \", \".join(\n            [\"\\\"{}\\\"\".format(_root) for _root in roots]\n        )\n        # TODO better error message\n        msg = (\n            \"Combination of root with and\"\n            \" without root name in AnatomyTemplates. {}\"\n        ).format(joined_roots)\n\n        super(RootCombinationError, self).__init__(msg)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/exceptions.html#client.ayon_core.pipeline.anatomy.exceptions.RootMissingEnv","title":"<code>RootMissingEnv</code>","text":"<p>               Bases: <code>KeyError</code></p> <p>Raised when root requires environment variables which is not filled.</p> Source code in <code>client/ayon_core/pipeline/anatomy/exceptions.py</code> <pre><code>class RootMissingEnv(KeyError):\n    \"\"\"Raised when root requires environment variables which is not filled.\"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/exceptions.html#client.ayon_core.pipeline.anatomy.exceptions.TemplateMissingKey","title":"<code>TemplateMissingKey</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Exception for cases when key does not exist in template.</p> Source code in <code>client/ayon_core/pipeline/anatomy/exceptions.py</code> <pre><code>class TemplateMissingKey(Exception):\n    \"\"\"Exception for cases when key does not exist in template.\"\"\"\n\n    msg = \"Template key '{}' was not found.\"\n\n    def __init__(self, parents):\n        parent_join = \"\".join([\"[\\\"{0}\\\"]\".format(key) for key in parents])\n        super(TemplateMissingKey, self).__init__(\n            self.msg.format(parent_join)\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/roots.html","title":"roots","text":""},{"location":"autoapi/client/ayon_core/pipeline/anatomy/roots.html#client.ayon_core.pipeline.anatomy.roots.AnatomyRoot","title":"<code>AnatomyRoot</code>","text":"<p>               Bases: <code>FormatObject</code></p> <p>Represents one item or roots.</p> <p>Holds raw data of root item specification. Raw data contain value for each platform, but current platform value is used when object is used for formatting of template.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <code>AnatomyRoots</code> <p>Parent object.</p> required <code>root_raw_data</code> <code>dict</code> <p>Dictionary containing root values by platform names. [\"windows\", \"linux\" and \"darwin\"]</p> required <code>name</code> <code>str</code> <p>Root name which is representing. Used with multi root setup otherwise None value is expected.</p> required Source code in <code>client/ayon_core/pipeline/anatomy/roots.py</code> <pre><code>class AnatomyRoot(FormatObject):\n    \"\"\"Represents one item or roots.\n\n    Holds raw data of root item specification. Raw data contain value\n    for each platform, but current platform value is used when object\n    is used for formatting of template.\n\n    Args:\n        parent (AnatomyRoots): Parent object.\n        root_raw_data (dict): Dictionary containing root values by platform\n            names. [\"windows\", \"linux\" and \"darwin\"]\n        name (str): Root name which is representing. Used with\n            multi root setup otherwise None value is expected.\n    \"\"\"\n    def __init__(self, parent, root_raw_data, name):\n        super().__init__()\n        self._log = None\n        lowered_platform_keys = {\n            key.lower(): value\n            for key, value in root_raw_data.items()\n        }\n        self.raw_data = lowered_platform_keys\n        self.cleaned_data = self._clean_roots(lowered_platform_keys)\n        self.name = name\n        self.parent = parent\n\n        self.available_platforms = set(lowered_platform_keys.keys())\n\n        current_platform = platform.system().lower()\n        # WARNING: Using environment variables in roots is not considered\n        #   as production safe. Some features may not work as expected, for\n        #   example USD resolver or site sync.\n        try:\n            self.value = lowered_platform_keys[current_platform].format_map(\n                os.environ\n            )\n        except KeyError:\n            result = StringTemplate(self.value).format(os.environ.copy())\n            is_are = \"is\" if len(result.missing_keys) == 1 else \"are\"\n            missing_keys = \", \".join(result.missing_keys)\n            raise RootMissingEnv(\n                f\"Root \\\"{name}\\\" requires environment variable/s\"\n                f\" {missing_keys} which {is_are} not available.\"\n            )\n\n        self.clean_value = self._clean_root(self.value)\n\n    def __format__(self, *args, **kwargs):\n        return self.value.__format__(*args, **kwargs)\n\n    def __str__(self):\n        return str(self.value)\n\n    def __repr__(self):\n        return self.__str__()\n\n    def __getitem__(self, key):\n        if isinstance(key, numbers.Number):\n            return self.value[key]\n\n        additional_info = \"\"\n        if self.parent and self.parent.project_name:\n            additional_info += \" for project \\\"{}\\\"\".format(\n                self.parent.project_name\n            )\n\n        raise KeyError(\n            \"Root key \\\"{}\\\" is missing{}.\".format(\n                key, additional_info\n            )\n        )\n\n    @property\n    def log(self):\n        if self._log is None:\n            self._log = Logger.get_logger(self.__class__.__name__)\n        return self._log\n\n    @property\n    def full_key(self):\n        \"\"\"Full key value for dictionary formatting in template.\n\n        Returns:\n            str: Return full replacement key for formatting. This helps when\n                multiple roots are set. In that case e.g. `\"root[work]\"` is\n                returned.\n\n        \"\"\"\n        return \"root[{}]\".format(self.name)\n\n    @staticmethod\n    def _clean_path(path):\n        \"\"\"Just replace backslashes with forward slashes.\n\n        Args:\n            path (str): Path which should be cleaned.\n\n        Returns:\n            str: Cleaned path with forward slashes.\n\n        \"\"\"\n        return str(path).replace(\"\\\\\", \"/\")\n\n    def _clean_root(self, root):\n        \"\"\"Clean root value.\n\n        Args:\n            root (str): Root value which should be cleaned.\n\n        Returns:\n            str: Cleaned root value.\n\n        \"\"\"\n        return self._clean_path(root).rstrip(\"/\")\n\n    def _clean_roots(self, raw_data):\n        \"\"\"Clean all values of raw root item values.\"\"\"\n        return {\n            key: self._clean_root(value)\n            for key, value in raw_data.items()\n        }\n\n    def path_remapper(self, path, dst_platform=None, src_platform=None):\n        \"\"\"Remap path for specific platform.\n\n        Args:\n            path (str): Source path which need to be remapped.\n            dst_platform (str, optional): Specify destination platform\n                for which remapping should happen.\n            src_platform (str, optional): Specify source platform. This is\n                recommended to not use and keep unset until you really want\n                to use specific platform.\n\n        Returns:\n            Union[str, None]: When path does not contain known root then\n                None is returned else returns remapped path with\n                \"{root[&lt;name&gt;]}\".\n\n        \"\"\"\n        cleaned_path = self._clean_path(path)\n        if dst_platform:\n            dst_root_clean = self.cleaned_data.get(dst_platform)\n            if not dst_root_clean:\n                self.log.warning(\n                    \"Root \\\"{}\\\" miss platform \\\"{}\\\" definition.\".format(\n                        self.full_key, dst_platform\n                    )\n                )\n                return None\n\n            if cleaned_path.startswith(dst_root_clean):\n                return cleaned_path\n\n        if src_platform:\n            src_root_clean = self.cleaned_data.get(src_platform)\n            if src_root_clean is None:\n                self.log.warning(\n                    \"Root \\\"{}\\\" miss platform \\\"{}\\\" definition.\".format(\n                        self.full_key, src_platform\n                    )\n                )\n                return None\n\n            if not cleaned_path.startswith(src_root_clean):\n                return None\n\n            subpath = cleaned_path[len(src_root_clean):]\n            if dst_platform:\n                # `dst_root_clean` is used from upper condition\n                return dst_root_clean + subpath\n            return self.clean_value + subpath\n\n        result, template = self.find_root_template_from_path(path)\n        if not result:\n            return None\n\n        if dst_platform:\n            fill_data = {self.name: dst_root_clean}\n        else:\n            fill_data = {self.name: self.value}\n\n        return template.format(**{\"root\": fill_data})\n\n    def find_root_template_from_path(self, path):\n        \"\"\"Replaces known root value with formattable key in path.\n\n        All platform values are checked for this replacement.\n\n        Args:\n            path (str): Path where root value should be found.\n\n        Returns:\n            tuple: Tuple contain 2 values: `success` (bool) and `path` (str).\n                When success it True then path should contain replaced root\n                value with formattable key.\n\n        Example:\n            When input path is::\n                \"C:/windows/path/root/projects/my_project/file.ext\"\n\n            And raw data of item looks like::\n                {\n                    \"windows\": \"C:/windows/path/root\",\n                    \"linux\": \"/mount/root\"\n                }\n\n            Output will be::\n                (True, \"{root}/projects/my_project/file.ext\")\n\n            If any of raw data value wouldn't match path's root output is::\n                (False, \"C:/windows/path/root/projects/my_project/file.ext\")\n        \"\"\"\n        result = False\n        output = str(path)\n\n        mod_path = self._clean_path(path)\n        for root_os, root_path in self.cleaned_data.items():\n            # Skip empty paths\n            if not root_path:\n                continue\n\n            _mod_path = mod_path  # reset to original cleaned value\n            if root_os == \"windows\":\n                root_path = root_path.lower()\n                _mod_path = _mod_path.lower()\n\n            if _mod_path.startswith(root_path):\n                result = True\n                replacement = \"{\" + self.full_key + \"}\"\n                output = replacement + mod_path[len(root_path):]\n                break\n\n        return (result, output)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/roots.html#client.ayon_core.pipeline.anatomy.roots.AnatomyRoot.full_key","title":"<code>full_key</code>  <code>property</code>","text":"<p>Full key value for dictionary formatting in template.</p> <p>Returns:</p> Name Type Description <code>str</code> <p>Return full replacement key for formatting. This helps when multiple roots are set. In that case e.g. <code>\"root[work]\"</code> is returned.</p>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/roots.html#client.ayon_core.pipeline.anatomy.roots.AnatomyRoot.find_root_template_from_path","title":"<code>find_root_template_from_path(path)</code>","text":"<p>Replaces known root value with formattable key in path.</p> <p>All platform values are checked for this replacement.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Path where root value should be found.</p> required <p>Returns:</p> Name Type Description <code>tuple</code> <p>Tuple contain 2 values: <code>success</code> (bool) and <code>path</code> (str). When success it True then path should contain replaced root value with formattable key.</p> Example <p>When input path is::     \"C:/windows/path/root/projects/my_project/file.ext\"</p> <p>And raw data of item looks like::     {         \"windows\": \"C:/windows/path/root\",         \"linux\": \"/mount/root\"     }</p> <p>Output will be::     (True, \"{root}/projects/my_project/file.ext\")</p> <p>If any of raw data value wouldn't match path's root output is::     (False, \"C:/windows/path/root/projects/my_project/file.ext\")</p> Source code in <code>client/ayon_core/pipeline/anatomy/roots.py</code> <pre><code>def find_root_template_from_path(self, path):\n    \"\"\"Replaces known root value with formattable key in path.\n\n    All platform values are checked for this replacement.\n\n    Args:\n        path (str): Path where root value should be found.\n\n    Returns:\n        tuple: Tuple contain 2 values: `success` (bool) and `path` (str).\n            When success it True then path should contain replaced root\n            value with formattable key.\n\n    Example:\n        When input path is::\n            \"C:/windows/path/root/projects/my_project/file.ext\"\n\n        And raw data of item looks like::\n            {\n                \"windows\": \"C:/windows/path/root\",\n                \"linux\": \"/mount/root\"\n            }\n\n        Output will be::\n            (True, \"{root}/projects/my_project/file.ext\")\n\n        If any of raw data value wouldn't match path's root output is::\n            (False, \"C:/windows/path/root/projects/my_project/file.ext\")\n    \"\"\"\n    result = False\n    output = str(path)\n\n    mod_path = self._clean_path(path)\n    for root_os, root_path in self.cleaned_data.items():\n        # Skip empty paths\n        if not root_path:\n            continue\n\n        _mod_path = mod_path  # reset to original cleaned value\n        if root_os == \"windows\":\n            root_path = root_path.lower()\n            _mod_path = _mod_path.lower()\n\n        if _mod_path.startswith(root_path):\n            result = True\n            replacement = \"{\" + self.full_key + \"}\"\n            output = replacement + mod_path[len(root_path):]\n            break\n\n    return (result, output)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/roots.html#client.ayon_core.pipeline.anatomy.roots.AnatomyRoot.path_remapper","title":"<code>path_remapper(path, dst_platform=None, src_platform=None)</code>","text":"<p>Remap path for specific platform.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Source path which need to be remapped.</p> required <code>dst_platform</code> <code>str</code> <p>Specify destination platform for which remapping should happen.</p> <code>None</code> <code>src_platform</code> <code>str</code> <p>Specify source platform. This is recommended to not use and keep unset until you really want to use specific platform.</p> <code>None</code> <p>Returns:</p> Type Description <p>Union[str, None]: When path does not contain known root then None is returned else returns remapped path with \"{root[]}\". Source code in <code>client/ayon_core/pipeline/anatomy/roots.py</code> <pre><code>def path_remapper(self, path, dst_platform=None, src_platform=None):\n    \"\"\"Remap path for specific platform.\n\n    Args:\n        path (str): Source path which need to be remapped.\n        dst_platform (str, optional): Specify destination platform\n            for which remapping should happen.\n        src_platform (str, optional): Specify source platform. This is\n            recommended to not use and keep unset until you really want\n            to use specific platform.\n\n    Returns:\n        Union[str, None]: When path does not contain known root then\n            None is returned else returns remapped path with\n            \"{root[&lt;name&gt;]}\".\n\n    \"\"\"\n    cleaned_path = self._clean_path(path)\n    if dst_platform:\n        dst_root_clean = self.cleaned_data.get(dst_platform)\n        if not dst_root_clean:\n            self.log.warning(\n                \"Root \\\"{}\\\" miss platform \\\"{}\\\" definition.\".format(\n                    self.full_key, dst_platform\n                )\n            )\n            return None\n\n        if cleaned_path.startswith(dst_root_clean):\n            return cleaned_path\n\n    if src_platform:\n        src_root_clean = self.cleaned_data.get(src_platform)\n        if src_root_clean is None:\n            self.log.warning(\n                \"Root \\\"{}\\\" miss platform \\\"{}\\\" definition.\".format(\n                    self.full_key, src_platform\n                )\n            )\n            return None\n\n        if not cleaned_path.startswith(src_root_clean):\n            return None\n\n        subpath = cleaned_path[len(src_root_clean):]\n        if dst_platform:\n            # `dst_root_clean` is used from upper condition\n            return dst_root_clean + subpath\n        return self.clean_value + subpath\n\n    result, template = self.find_root_template_from_path(path)\n    if not result:\n        return None\n\n    if dst_platform:\n        fill_data = {self.name: dst_root_clean}\n    else:\n        fill_data = {self.name: self.value}\n\n    return template.format(**{\"root\": fill_data})\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/roots.html#client.ayon_core.pipeline.anatomy.roots.AnatomyRoots","title":"<code>AnatomyRoots</code>","text":"<p>Object which should be used for formatting \"root\" key in templates.</p> <p>Parameters:</p> Name Type Description Default <code>anatomy</code> <code>Anatomy</code> <p>Anatomy object created for a specific project.</p> required Source code in <code>client/ayon_core/pipeline/anatomy/roots.py</code> <pre><code>class AnatomyRoots:\n    \"\"\"Object which should be used for formatting \"root\" key in templates.\n\n    Args:\n        anatomy (Anatomy): Anatomy object created for a specific project.\n    \"\"\"\n\n    env_prefix = \"AYON_PROJECT_ROOT\"\n\n    def __init__(self, anatomy):\n        self._log = None\n        self._anatomy = anatomy\n        self._loaded_project = None\n        self._roots = None\n\n    def __format__(self, *args, **kwargs):\n        return self.roots.__format__(*args, **kwargs)\n\n    def __getitem__(self, key):\n        return self.roots[key]\n\n    @property\n    def log(self):\n        if self._log is None:\n            self._log = Logger.get_logger(self.__class__.__name__)\n        return self._log\n\n    @property\n    def anatomy(self):\n        \"\"\"Parent Anatomy object.\n\n        Returns:\n            Anatomy: Parent anatomy object.\n\n        \"\"\"\n        return self._anatomy\n\n    def reset(self):\n        \"\"\"Reset current roots value.\"\"\"\n        self._roots = None\n\n    def path_remapper(\n        self, path, dst_platform=None, src_platform=None, roots=None\n    ):\n        \"\"\"Remap path for specific platform.\n\n        Args:\n            path (str): Source path which need to be remapped.\n            dst_platform (Optional[str]): Specify destination platform\n                for which remapping should happen.\n            src_platform (Optional[str]): Specify source platform. This is\n                recommended to not use and keep unset until you really want\n                to use specific platform.\n            roots (Optional[Union[dict, AnatomyRoot])): It is possible to remap\n                path with different roots then instance where method was\n                called has.\n\n        Returns:\n            Union[str, None]: When path does not contain known root then\n                None is returned else returns remapped path with \"{root}\"\n                or \"{root[&lt;name&gt;]}\".\n\n        \"\"\"\n        if roots is None:\n            roots = self.roots\n\n        if roots is None:\n            raise ValueError(\"Roots are not set. Can't find path.\")\n\n        if \"{root\" in path:\n            path = path.format(**{\"root\": roots})\n            # If `dst_platform` is not specified then return else continue.\n            if not dst_platform:\n                return path\n\n        if isinstance(roots, AnatomyRoot):\n            return roots.path_remapper(path, dst_platform, src_platform)\n\n        for _root in roots.values():\n            result = self.path_remapper(\n                path, dst_platform, src_platform, _root\n            )\n            if result is not None:\n                return result\n\n    def find_root_template_from_path(self, path, roots=None):\n        \"\"\"Find root value in entered path and replace it with formatting key.\n\n        Args:\n            path (str): Source path where root will be searched.\n            roots (Optional[Union[AnatomyRoots, dict]): It is possible to use\n                different roots than instance where method was triggered has.\n\n        Returns:\n            tuple: Output contains tuple with bool representing success as\n                first value and path with or without replaced root with\n                formatting key as second value.\n\n        Raises:\n            ValueError: When roots are not entered and can't be loaded.\n        \"\"\"\n        if roots is None:\n            self.log.debug(\n                \"Looking for matching root in path \\\"{}\\\".\".format(path)\n            )\n            roots = self.roots\n\n        if roots is None:\n            raise ValueError(\"Roots are not set. Can't find path.\")\n\n        if isinstance(roots, AnatomyRoot):\n            return roots.find_root_template_from_path(path)\n\n        for root_name, _root in roots.items():\n            success, result = self.find_root_template_from_path(path, _root)\n            if success:\n                self.log.debug(\n                    \"Found match in root \\\"{}\\\".\".format(root_name)\n                )\n                return success, result\n\n        self.log.warning(\"No matching root was found in current setting.\")\n        return (False, path)\n\n    def set_root_environments(self):\n        \"\"\"Set root environments for current project.\"\"\"\n        for key, value in self.root_environments().items():\n            os.environ[key] = value\n\n    def root_environments(self):\n        \"\"\"Use root keys to create unique keys for environment variables.\n\n        Concatenates prefix \"AYON_PROJECT_ROOT_\" with root keys to create\n        unique keys.\n\n        Returns:\n            dict: Result is `{(str): (str)}` dicitonary where key represents\n                unique key concatenated by keys and value is root value of\n                current platform root.\n\n        Example:\n            With raw root values::\n                \"work\": {\n                    \"windows\": \"P:/projects/work\",\n                    \"linux\": \"/mnt/share/projects/work\",\n                    \"darwin\": \"/darwin/path/work\"\n                },\n                \"publish\": {\n                    \"windows\": \"P:/projects/publish\",\n                    \"linux\": \"/mnt/share/projects/publish\",\n                    \"darwin\": \"/darwin/path/publish\"\n                }\n\n            Result on windows platform::\n                {\n                    \"AYON_PROJECT_ROOT_WORK\": \"P:/projects/work\",\n                    \"AYON_PROJECT_ROOT_PUBLISH\": \"P:/projects/publish\"\n                }\n\n        \"\"\"\n        return self._root_environments()\n\n    def all_root_paths(self, roots=None):\n        \"\"\"Return all paths for all roots of all platforms.\"\"\"\n        if roots is None:\n            roots = self.roots\n\n        output = []\n        if isinstance(roots, AnatomyRoot):\n            for value in roots.raw_data.values():\n                output.append(value)\n            return output\n\n        for _roots in roots.values():\n            output.extend(self.all_root_paths(_roots))\n        return output\n\n    def _root_environments(self, keys=None, roots=None):\n        if not keys:\n            keys = []\n        if roots is None:\n            roots = self.roots\n\n        if isinstance(roots, AnatomyRoot):\n            key_items = [self.env_prefix]\n            for _key in keys:\n                key_items.append(_key.upper())\n\n            key = \"_\".join(key_items)\n            # Make sure key and value does not contain unicode\n            #   - can happen in Python 2 hosts\n            return {str(key): str(roots.value)}\n\n        output = {}\n        for _key, _value in roots.items():\n            _keys = list(keys)\n            _keys.append(_key)\n            output.update(self._root_environments(_keys, _value))\n        return output\n\n    def root_environmets_fill_data(self, template=None):\n        \"\"\"Environment variable values in dictionary for rootless path.\n\n        Args:\n            template (str): Template for environment variable key fill.\n                By default is set to `\"${}\"`.\n        \"\"\"\n        if template is None:\n            template = \"${}\"\n        return self._root_environmets_fill_data(template)\n\n    def _root_environmets_fill_data(self, template, keys=None, roots=None):\n        if keys is None and roots is None:\n            return {\n                \"root\": self._root_environmets_fill_data(\n                    template, [], self.roots\n                )\n            }\n\n        if isinstance(roots, AnatomyRoot):\n            key_items = [AnatomyRoots.env_prefix]\n            for _key in keys:\n                key_items.append(_key.upper())\n            key = \"_\".join(key_items)\n            return template.format(key)\n\n        output = {}\n        for key, value in roots.items():\n            _keys = list(keys)\n            _keys.append(key)\n            output[key] = self._root_environmets_fill_data(\n                template, _keys, value\n            )\n        return output\n\n    @property\n    def project_name(self):\n        \"\"\"Current project name which will be used for loading root values.\n\n        Returns:\n            str: Project name.\n        \"\"\"\n        return self._anatomy.project_name\n\n    @property\n    def roots(self):\n        \"\"\"Property for filling \"root\" key in templates.\n\n        This property returns roots for current project or default root values.\n\n        Warning:\n            Default roots value may cause issues when project use different\n            roots settings. That may happen when project use multiroot\n            templates but default roots miss their keys.\n\n        \"\"\"\n        if self.project_name != self._loaded_project:\n            self._roots = None\n\n        if self._roots is None:\n            self._roots = self._discover()\n            self._loaded_project = self.project_name\n        return self._roots\n\n    def _discover(self):\n        \"\"\" Loads current project's roots or default.\n\n        Default roots are loaded if project override's does not contain roots.\n\n        Returns:\n            `AnatomyRoot` or `dict` with multiple `AnatomyRoot`s when multiroot\n            setting is used.\n        \"\"\"\n\n        return self._parse_dict(self._anatomy[\"roots\"], self)\n\n    @staticmethod\n    def _parse_dict(data, parent):\n        \"\"\"Parse roots raw data into dictionary with AnatomyRoots.\n\n        Converting raw roots data to `AnatomyRoot` helps to handle platform\n            keys.\n\n        Args:\n            data (dict): Should contain raw roots data to be parsed.\n            parent (AnatomyRoots): Parent object set as parent\n                for ``AnatomyRoot``.\n\n        Returns:\n            dict[str, AnatomyRoot]: Root items by name.\n\n        \"\"\"\n        output = {}\n        for root_name, root_values in data.items():\n            output[root_name] = AnatomyRoot(\n                parent, root_values, root_name\n            )\n        return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/roots.html#client.ayon_core.pipeline.anatomy.roots.AnatomyRoots.anatomy","title":"<code>anatomy</code>  <code>property</code>","text":"<p>Parent Anatomy object.</p> <p>Returns:</p> Name Type Description <code>Anatomy</code> <p>Parent anatomy object.</p>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/roots.html#client.ayon_core.pipeline.anatomy.roots.AnatomyRoots.project_name","title":"<code>project_name</code>  <code>property</code>","text":"<p>Current project name which will be used for loading root values.</p> <p>Returns:</p> Name Type Description <code>str</code> <p>Project name.</p>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/roots.html#client.ayon_core.pipeline.anatomy.roots.AnatomyRoots.roots","title":"<code>roots</code>  <code>property</code>","text":"<p>Property for filling \"root\" key in templates.</p> <p>This property returns roots for current project or default root values.</p> Warning <p>Default roots value may cause issues when project use different roots settings. That may happen when project use multiroot templates but default roots miss their keys.</p>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/roots.html#client.ayon_core.pipeline.anatomy.roots.AnatomyRoots.all_root_paths","title":"<code>all_root_paths(roots=None)</code>","text":"<p>Return all paths for all roots of all platforms.</p> Source code in <code>client/ayon_core/pipeline/anatomy/roots.py</code> <pre><code>def all_root_paths(self, roots=None):\n    \"\"\"Return all paths for all roots of all platforms.\"\"\"\n    if roots is None:\n        roots = self.roots\n\n    output = []\n    if isinstance(roots, AnatomyRoot):\n        for value in roots.raw_data.values():\n            output.append(value)\n        return output\n\n    for _roots in roots.values():\n        output.extend(self.all_root_paths(_roots))\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/roots.html#client.ayon_core.pipeline.anatomy.roots.AnatomyRoots.find_root_template_from_path","title":"<code>find_root_template_from_path(path, roots=None)</code>","text":"<p>Find root value in entered path and replace it with formatting key.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Source path where root will be searched.</p> required <code>roots</code> <code>Optional[Union[AnatomyRoots, dict]</code> <p>It is possible to use different roots than instance where method was triggered has.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>tuple</code> <p>Output contains tuple with bool representing success as first value and path with or without replaced root with formatting key as second value.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>When roots are not entered and can't be loaded.</p> Source code in <code>client/ayon_core/pipeline/anatomy/roots.py</code> <pre><code>def find_root_template_from_path(self, path, roots=None):\n    \"\"\"Find root value in entered path and replace it with formatting key.\n\n    Args:\n        path (str): Source path where root will be searched.\n        roots (Optional[Union[AnatomyRoots, dict]): It is possible to use\n            different roots than instance where method was triggered has.\n\n    Returns:\n        tuple: Output contains tuple with bool representing success as\n            first value and path with or without replaced root with\n            formatting key as second value.\n\n    Raises:\n        ValueError: When roots are not entered and can't be loaded.\n    \"\"\"\n    if roots is None:\n        self.log.debug(\n            \"Looking for matching root in path \\\"{}\\\".\".format(path)\n        )\n        roots = self.roots\n\n    if roots is None:\n        raise ValueError(\"Roots are not set. Can't find path.\")\n\n    if isinstance(roots, AnatomyRoot):\n        return roots.find_root_template_from_path(path)\n\n    for root_name, _root in roots.items():\n        success, result = self.find_root_template_from_path(path, _root)\n        if success:\n            self.log.debug(\n                \"Found match in root \\\"{}\\\".\".format(root_name)\n            )\n            return success, result\n\n    self.log.warning(\"No matching root was found in current setting.\")\n    return (False, path)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/roots.html#client.ayon_core.pipeline.anatomy.roots.AnatomyRoots.path_remapper","title":"<code>path_remapper(path, dst_platform=None, src_platform=None, roots=None)</code>","text":"<p>Remap path for specific platform.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Source path which need to be remapped.</p> required <code>dst_platform</code> <code>Optional[str]</code> <p>Specify destination platform for which remapping should happen.</p> <code>None</code> <code>src_platform</code> <code>Optional[str]</code> <p>Specify source platform. This is recommended to not use and keep unset until you really want to use specific platform.</p> <code>None</code> <code>roots</code> <code>Optional[Union[dict, AnatomyRoot])</code> <p>It is possible to remap path with different roots then instance where method was called has.</p> <code>None</code> <p>Returns:</p> Type Description <p>Union[str, None]: When path does not contain known root then None is returned else returns remapped path with \"{root}\" or \"{root[]}\". Source code in <code>client/ayon_core/pipeline/anatomy/roots.py</code> <pre><code>def path_remapper(\n    self, path, dst_platform=None, src_platform=None, roots=None\n):\n    \"\"\"Remap path for specific platform.\n\n    Args:\n        path (str): Source path which need to be remapped.\n        dst_platform (Optional[str]): Specify destination platform\n            for which remapping should happen.\n        src_platform (Optional[str]): Specify source platform. This is\n            recommended to not use and keep unset until you really want\n            to use specific platform.\n        roots (Optional[Union[dict, AnatomyRoot])): It is possible to remap\n            path with different roots then instance where method was\n            called has.\n\n    Returns:\n        Union[str, None]: When path does not contain known root then\n            None is returned else returns remapped path with \"{root}\"\n            or \"{root[&lt;name&gt;]}\".\n\n    \"\"\"\n    if roots is None:\n        roots = self.roots\n\n    if roots is None:\n        raise ValueError(\"Roots are not set. Can't find path.\")\n\n    if \"{root\" in path:\n        path = path.format(**{\"root\": roots})\n        # If `dst_platform` is not specified then return else continue.\n        if not dst_platform:\n            return path\n\n    if isinstance(roots, AnatomyRoot):\n        return roots.path_remapper(path, dst_platform, src_platform)\n\n    for _root in roots.values():\n        result = self.path_remapper(\n            path, dst_platform, src_platform, _root\n        )\n        if result is not None:\n            return result\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/roots.html#client.ayon_core.pipeline.anatomy.roots.AnatomyRoots.reset","title":"<code>reset()</code>","text":"<p>Reset current roots value.</p> Source code in <code>client/ayon_core/pipeline/anatomy/roots.py</code> <pre><code>def reset(self):\n    \"\"\"Reset current roots value.\"\"\"\n    self._roots = None\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/roots.html#client.ayon_core.pipeline.anatomy.roots.AnatomyRoots.root_environments","title":"<code>root_environments()</code>","text":"<p>Use root keys to create unique keys for environment variables.</p> <p>Concatenates prefix \"AYON_PROJECT_ROOT_\" with root keys to create unique keys.</p> <p>Returns:</p> Name Type Description <code>dict</code> <p>Result is <code>{(str): (str)}</code> dicitonary where key represents unique key concatenated by keys and value is root value of current platform root.</p> Example <p>With raw root values::     \"work\": {         \"windows\": \"P:/projects/work\",         \"linux\": \"/mnt/share/projects/work\",         \"darwin\": \"/darwin/path/work\"     },     \"publish\": {         \"windows\": \"P:/projects/publish\",         \"linux\": \"/mnt/share/projects/publish\",         \"darwin\": \"/darwin/path/publish\"     }</p> <p>Result on windows platform::     {         \"AYON_PROJECT_ROOT_WORK\": \"P:/projects/work\",         \"AYON_PROJECT_ROOT_PUBLISH\": \"P:/projects/publish\"     }</p> Source code in <code>client/ayon_core/pipeline/anatomy/roots.py</code> <pre><code>def root_environments(self):\n    \"\"\"Use root keys to create unique keys for environment variables.\n\n    Concatenates prefix \"AYON_PROJECT_ROOT_\" with root keys to create\n    unique keys.\n\n    Returns:\n        dict: Result is `{(str): (str)}` dicitonary where key represents\n            unique key concatenated by keys and value is root value of\n            current platform root.\n\n    Example:\n        With raw root values::\n            \"work\": {\n                \"windows\": \"P:/projects/work\",\n                \"linux\": \"/mnt/share/projects/work\",\n                \"darwin\": \"/darwin/path/work\"\n            },\n            \"publish\": {\n                \"windows\": \"P:/projects/publish\",\n                \"linux\": \"/mnt/share/projects/publish\",\n                \"darwin\": \"/darwin/path/publish\"\n            }\n\n        Result on windows platform::\n            {\n                \"AYON_PROJECT_ROOT_WORK\": \"P:/projects/work\",\n                \"AYON_PROJECT_ROOT_PUBLISH\": \"P:/projects/publish\"\n            }\n\n    \"\"\"\n    return self._root_environments()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/roots.html#client.ayon_core.pipeline.anatomy.roots.AnatomyRoots.root_environmets_fill_data","title":"<code>root_environmets_fill_data(template=None)</code>","text":"<p>Environment variable values in dictionary for rootless path.</p> <p>Parameters:</p> Name Type Description Default <code>template</code> <code>str</code> <p>Template for environment variable key fill. By default is set to <code>\"${}\"</code>.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/anatomy/roots.py</code> <pre><code>def root_environmets_fill_data(self, template=None):\n    \"\"\"Environment variable values in dictionary for rootless path.\n\n    Args:\n        template (str): Template for environment variable key fill.\n            By default is set to `\"${}\"`.\n    \"\"\"\n    if template is None:\n        template = \"${}\"\n    return self._root_environmets_fill_data(template)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/roots.html#client.ayon_core.pipeline.anatomy.roots.AnatomyRoots.set_root_environments","title":"<code>set_root_environments()</code>","text":"<p>Set root environments for current project.</p> Source code in <code>client/ayon_core/pipeline/anatomy/roots.py</code> <pre><code>def set_root_environments(self):\n    \"\"\"Set root environments for current project.\"\"\"\n    for key, value in self.root_environments().items():\n        os.environ[key] = value\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/roots.html#client.ayon_core.pipeline.anatomy.roots.RootItem","title":"<code>RootItem</code>","text":"<p>               Bases: <code>AnatomyRoot</code></p> <p>DEPRECATED: Use 'AnatomyRoot' instead.</p> <p>Backwards compatibility added 2026/05/21.</p> Source code in <code>client/ayon_core/pipeline/anatomy/roots.py</code> <pre><code>class RootItem(AnatomyRoot):\n    \"\"\"DEPRECATED: Use 'AnatomyRoot' instead.\n\n    Backwards compatibility added 2026/05/21.\n    \"\"\"\n    def __init__(self, *args, **kwargs):\n        super().__init__(*args, **kwargs)\n        warnings.warn(\n            \"RootItem is deprecated. Use AnatomyRoot instead.\",\n            DeprecationWarning,\n            stacklevel=2,\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/templates.html","title":"templates","text":""},{"location":"autoapi/client/ayon_core/pipeline/anatomy/templates.html#client.ayon_core.pipeline.anatomy.templates.AnatomyStringTemplate","title":"<code>AnatomyStringTemplate</code>","text":"<p>               Bases: <code>StringTemplate</code></p> <p>String template which has access to anatomy.</p> <p>Parameters:</p> Name Type Description Default <code>anatomy_templates</code> <code>AnatomyTemplates</code> <p>Anatomy templates object.</p> required <code>template</code> <code>str</code> <p>Template string.</p> required Source code in <code>client/ayon_core/pipeline/anatomy/templates.py</code> <pre><code>class AnatomyStringTemplate(StringTemplate):\n    \"\"\"String template which has access to anatomy.\n\n    Args:\n        anatomy_templates (AnatomyTemplates): Anatomy templates object.\n        template (str): Template string.\n    \"\"\"\n\n    def __init__(self, anatomy_templates, template):\n        self.anatomy_templates = anatomy_templates\n        super(AnatomyStringTemplate, self).__init__(template)\n\n    def format(self, data):\n        \"\"\"Format template and add 'root' key to data if not available.\n\n        Args:\n            data (dict[str, Any]): Formatting data for template.\n\n        Returns:\n            AnatomyTemplateResult: Formatting result.\n        \"\"\"\n\n        anatomy_templates = self.anatomy_templates\n        if not data.get(\"root\"):\n            data = copy.deepcopy(data)\n            data[\"root\"] = anatomy_templates.anatomy.roots\n        result = StringTemplate.format(self, data)\n        rootless_path = anatomy_templates.get_rootless_path_from_result(\n            result\n        )\n        return AnatomyTemplateResult(result, rootless_path)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/templates.html#client.ayon_core.pipeline.anatomy.templates.AnatomyStringTemplate.format","title":"<code>format(data)</code>","text":"<p>Format template and add 'root' key to data if not available.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict[str, Any]</code> <p>Formatting data for template.</p> required <p>Returns:</p> Name Type Description <code>AnatomyTemplateResult</code> <p>Formatting result.</p> Source code in <code>client/ayon_core/pipeline/anatomy/templates.py</code> <pre><code>def format(self, data):\n    \"\"\"Format template and add 'root' key to data if not available.\n\n    Args:\n        data (dict[str, Any]): Formatting data for template.\n\n    Returns:\n        AnatomyTemplateResult: Formatting result.\n    \"\"\"\n\n    anatomy_templates = self.anatomy_templates\n    if not data.get(\"root\"):\n        data = copy.deepcopy(data)\n        data[\"root\"] = anatomy_templates.anatomy.roots\n    result = StringTemplate.format(self, data)\n    rootless_path = anatomy_templates.get_rootless_path_from_result(\n        result\n    )\n    return AnatomyTemplateResult(result, rootless_path)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/templates.html#client.ayon_core.pipeline.anatomy.templates.AnatomyTemplateResult","title":"<code>AnatomyTemplateResult</code>","text":"<p>               Bases: <code>TemplateResult</code></p> Source code in <code>client/ayon_core/pipeline/anatomy/templates.py</code> <pre><code>class AnatomyTemplateResult(TemplateResult):\n    rootless = None\n\n    def __new__(cls, result, rootless_path):\n        new_obj = super(AnatomyTemplateResult, cls).__new__(\n            cls,\n            str(result),\n            result.template,\n            result.solved,\n            result.used_values,\n            result.missing_keys,\n            result.invalid_types\n        )\n        new_obj.rootless = rootless_path\n        return new_obj\n\n    def validate(self):\n        if not self.solved:\n            raise AnatomyTemplateUnsolved(\n                self.template,\n                self.missing_keys,\n                self.invalid_types\n            )\n\n    def copy(self):\n        tmp = TemplateResult(\n            str(self),\n            self.template,\n            self.solved,\n            self.used_values,\n            self.missing_keys,\n            self.invalid_types\n        )\n        return self.__class__(tmp, self.rootless)\n\n    def normalized(self):\n        \"\"\"Convert to normalized path.\"\"\"\n\n        tmp = TemplateResult(\n            os.path.normpath(self),\n            self.template,\n            self.solved,\n            self.used_values,\n            self.missing_keys,\n            self.invalid_types\n        )\n        return self.__class__(tmp, self.rootless)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/templates.html#client.ayon_core.pipeline.anatomy.templates.AnatomyTemplateResult.normalized","title":"<code>normalized()</code>","text":"<p>Convert to normalized path.</p> Source code in <code>client/ayon_core/pipeline/anatomy/templates.py</code> <pre><code>def normalized(self):\n    \"\"\"Convert to normalized path.\"\"\"\n\n    tmp = TemplateResult(\n        os.path.normpath(self),\n        self.template,\n        self.solved,\n        self.used_values,\n        self.missing_keys,\n        self.invalid_types\n    )\n    return self.__class__(tmp, self.rootless)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/templates.html#client.ayon_core.pipeline.anatomy.templates.AnatomyTemplates","title":"<code>AnatomyTemplates</code>","text":"Source code in <code>client/ayon_core/pipeline/anatomy/templates.py</code> <pre><code>class AnatomyTemplates:\n    inner_key_pattern = re.compile(r\"(\\{@.*?[^{}0]*\\})\")\n    inner_key_name_pattern = re.compile(r\"\\{@(.*?[^{}0]*)\\}\")\n\n    def __init__(self, anatomy):\n        self._anatomy = anatomy\n\n        self._loaded_project = None\n        self._raw_templates = None\n        self._templates = None\n        self._objected_templates = None\n\n    def __getitem__(self, key):\n        self._validate_discovery()\n        return self._objected_templates[key]\n\n    def get(self, key, default=None):\n        self._validate_discovery()\n        return self._objected_templates.get(key, default)\n\n    def keys(self):\n        return self._objected_templates.keys()\n\n    def reset(self):\n        self._raw_templates = None\n        self._templates = None\n        self._objected_templates = None\n\n    @property\n    def anatomy(self):\n        \"\"\"Anatomy instance.\n\n        Returns:\n            Anatomy: Anatomy instance.\n\n        \"\"\"\n        return self._anatomy\n\n    @property\n    def project_name(self):\n        \"\"\"Project name.\n\n        Returns:\n            Union[str, None]: Project name if set, otherwise None.\n\n        \"\"\"\n        return self._anatomy.project_name\n\n    @property\n    def roots(self):\n        \"\"\"Anatomy roots object.\n\n        Returns:\n            dict[str, AnatomyRoot]: Anatomy roots data.\n\n        \"\"\"\n        return self._anatomy.roots\n\n    @property\n    def templates(self):\n        \"\"\"Templates data.\n\n        Templates data with replaced common data.\n\n        Returns:\n            dict[str, Any]: Templates data.\n\n        \"\"\"\n        self._validate_discovery()\n        return self._templates\n\n    @property\n    def frame_padding(self):\n        \"\"\"Default frame padding.\n\n        Returns:\n            int: Frame padding used by default in templates.\n\n        \"\"\"\n        self._validate_discovery()\n        return self[\"frame_padding\"]\n\n    @property\n    def version_padding(self):\n        \"\"\"Default version padding.\n\n        Returns:\n            int: Version padding used by default in templates.\n\n        \"\"\"\n        self._validate_discovery()\n        return self[\"version_padding\"]\n\n    @classmethod\n    def get_rootless_path_from_result(cls, result):\n        \"\"\"Calculate rootless path from formatting result.\n\n        Args:\n            result (TemplateResult): Result of StringTemplate formatting.\n\n        Returns:\n            str: Rootless path if result contains one of anatomy roots.\n        \"\"\"\n\n        used_values = result.used_values\n        missing_keys = result.missing_keys\n        template = result.template\n        invalid_types = result.invalid_types\n        if (\n            \"root\" not in used_values\n            or \"root\" in missing_keys\n            or \"{root\" not in template\n        ):\n            return\n\n        for invalid_type in invalid_types:\n            if \"root\" in invalid_type:\n                return\n\n        root_keys = cls._dict_to_subkeys_list({\"root\": used_values[\"root\"]})\n        if not root_keys:\n            return\n\n        output = str(result)\n        for used_root_keys in root_keys:\n            if not used_root_keys:\n                continue\n\n            used_value = used_values\n            root_key = None\n            for key in used_root_keys:\n                used_value = used_value[key]\n                if root_key is None:\n                    root_key = key\n                else:\n                    root_key += \"[{}]\".format(key)\n\n            root_key = \"{\" + root_key + \"}\"\n            output = output.replace(str(used_value), root_key)\n\n        # Make sure rootless path is with forward slashes\n        if _IS_WINDOWS:\n            output.replace(\"\\\\\", \"/\")\n\n        # Make sure there are no double slashes\n        while \"//\" in output:\n            output = output.replace(\"//\", \"/\")\n\n        return output\n\n    def format(self, data, strict=True):\n        \"\"\"Fill all templates based on entered data.\n\n        Args:\n            data (dict[str, Any]): Fill data used for template formatting.\n            strict (Optional[bool]): Raise exception is accessed value is\n                not fully filled.\n\n        Returns:\n            TemplatesResultDict: Output `TemplateResult` have `strict`\n                attribute set to False so accessing unfilled keys in templates\n                won't raise any exceptions.\n\n        \"\"\"\n        self._validate_discovery()\n        copy_data = copy.deepcopy(data)\n        roots = self._anatomy.roots\n        if roots:\n            copy_data[\"root\"] = roots\n\n        return self._solve_dict(copy_data, strict)\n\n    def format_all(self, in_data):\n        \"\"\"Fill all templates based on entered data.\n\n        Deprecated:\n            Use `format` method with `strict=False` instead.\n\n        Args:\n            in_data (dict): Containing keys to be filled into template.\n\n        Returns:\n            TemplatesResultDict: Output `TemplateResult` have `strict`\n                attribute set to False so accessing unfilled keys in templates\n                won't raise any exceptions.\n\n        \"\"\"\n        return self.format(in_data, strict=False)\n\n    def get_template_item(\n        self,\n        category_name: str,\n        template_name: str,\n        subkey: str | None = None,\n        default: Any = NOT_SET\n    ) -&gt; Any:\n        \"\"\"Get template item from category.\n\n        Args:\n            category_name (str): Category name.\n            template_name (str): Template name.\n            subkey (str | None): Subkey name.\n            default (Any): Default value if template is not found.\n\n        Returns:\n            Any: Template item or subkey value.\n\n        Raises:\n            KeyError: When any passed key is not available. Raise of error\n                does not happen if 'default' is filled.\n\n        \"\"\"\n        self._validate_discovery()\n        category = self.get(category_name)\n        if category is None:\n            if default is not NOT_SET:\n                return default\n            raise KeyError(\"Category '{}' not found.\".format(category_name))\n\n        template_item = category.get(template_name)\n        if template_item is None:\n            if default is not NOT_SET:\n                return default\n            raise KeyError(\n                \"Template '{}' not found in category '{}'.\".format(\n                    template_name, category_name\n                )\n            )\n\n        if subkey is None:\n            return template_item\n\n        item = template_item.get(subkey)\n        if item is not None:\n            return item\n\n        if default is not NOT_SET:\n            return default\n        raise KeyError(\n            \"Subkey '{}' not found in '{}/{}'.\".format(\n                subkey, category_name, template_name\n            )\n        )\n\n    def _solve_dict(self, data, strict):\n        \"\"\" Solves templates with entered data.\n\n        Args:\n            data (dict): Containing keys to be filled into template.\n\n        Returns:\n            dict: With `TemplateResult` in values containing filled or\n                partially filled templates.\n\n        \"\"\"\n        output = {}\n        for key, value in self._objected_templates.items():\n            if isinstance(value, TemplateCategory):\n                value = value.format(data, strict)\n            elif isinstance(value, AnatomyStringTemplate):\n                value = value.format(data)\n            output[key] = value\n        return TemplatesResultDict(output, strict=strict)\n\n    def _validate_discovery(self):\n        \"\"\"Validate if templates are discovered and loaded for anatomy project.\n\n        When project changes the cached data are reset and discovered again.\n        \"\"\"\n        if self.project_name != self._loaded_project:\n            self.reset()\n\n        if self._templates is None:\n            self._discover()\n            self._loaded_project = self.project_name\n\n    def _create_objected_templates(self, templates):\n        \"\"\"Create objected templates from templates data.\n\n        Args:\n            templates (dict[str, Any]): Templates data from project entity.\n\n        Returns:\n            dict[str, Any]: Values are cnmverted to template objects.\n\n        \"\"\"\n        objected_templates = {}\n        for category_name, category_value in copy.deepcopy(templates).items():\n            if isinstance(category_value, dict):\n                category_value = TemplateCategory(\n                    self, category_name, category_value\n                )\n            elif isinstance(category_value, str):\n                category_value = AnatomyStringTemplate(self, category_value)\n            objected_templates[category_name] = category_value\n        return objected_templates\n\n    def _discover(self):\n        \"\"\"Load and cache templates from project entity.\"\"\"\n        if self.project_name is None:\n            raise ProjectNotSet(\"Anatomy project is not set.\")\n\n        templates = self.anatomy[\"templates\"]\n        self._raw_templates = copy.deepcopy(templates)\n\n        templates = copy.deepcopy(templates)\n        # Make sure all the keys are available\n        for key in (\n            \"publish\",\n            \"hero\",\n            \"work\",\n            \"delivery\",\n            \"staging\",\n            \"others\",\n        ):\n            templates.setdefault(key, {})\n\n        solved_templates = self._solve_template_inner_links(templates)\n        self._templates = solved_templates\n        self._objected_templates = self._create_objected_templates(\n            solved_templates\n        )\n\n    @classmethod\n    def _replace_inner_keys(cls, matches, value, key_values, key):\n        \"\"\"Replacement of inner keys in template values.\"\"\"\n        for match in matches:\n            anatomy_sub_keys = (\n                cls.inner_key_name_pattern.findall(match)\n            )\n            if key in anatomy_sub_keys:\n                raise ValueError((\n                    \"Unsolvable recursion in inner keys, \"\n                    \"key: \\\"{}\\\" is in his own value.\"\n                    \" Can't determine source, please check Anatomy templates.\"\n                ).format(key))\n\n            for anatomy_sub_key in anatomy_sub_keys:\n                replace_value = key_values.get(anatomy_sub_key)\n                if replace_value is None:\n                    raise KeyError((\n                        \"Anatomy templates can't be filled.\"\n                        \" Anatomy key `{0}` has\"\n                        \" invalid inner key `{1}`.\"\n                    ).format(key, anatomy_sub_key))\n\n                if not (\n                    isinstance(replace_value, numbers.Number)\n                    or isinstance(replace_value, str)\n                ):\n                    raise ValueError((\n                        \"Anatomy templates can't be filled.\"\n                        \" Anatomy key `{0}` has\"\n                        \" invalid inner key `{1}`\"\n                        \" with value `{2}`.\"\n                    ).format(key, anatomy_sub_key, str(replace_value)))\n\n                value = value.replace(match, str(replace_value))\n\n        return value\n\n    @classmethod\n    def _prepare_inner_keys(cls, key_values):\n        \"\"\"Check values of inner keys.\n\n        Check if inner key exist in template group and has valid value.\n        It is also required to avoid infinite loop with unsolvable recursion\n        when first inner key's value refers to second inner key's value where\n        first is used.\n        \"\"\"\n        keys_to_solve = set(key_values.keys())\n        while True:\n            found = False\n            for key in tuple(keys_to_solve):\n                value = key_values[key]\n\n                if isinstance(value, str):\n                    matches = cls.inner_key_pattern.findall(value)\n                    if not matches:\n                        keys_to_solve.remove(key)\n                        continue\n\n                    found = True\n                    key_values[key] = cls._replace_inner_keys(\n                        matches, value, key_values, key\n                    )\n                    continue\n\n                elif not isinstance(value, dict):\n                    keys_to_solve.remove(key)\n                    continue\n\n                subdict_found = False\n                for _key, _value in tuple(value.items()):\n                    matches = cls.inner_key_pattern.findall(_value)\n                    if not matches:\n                        continue\n\n                    subdict_found = True\n                    found = True\n                    key_values[key][_key] = cls._replace_inner_keys(\n                        matches, _value, key_values,\n                        \"{}.{}\".format(key, _key)\n                    )\n\n                if not subdict_found:\n                    keys_to_solve.remove(key)\n\n            if not found:\n                break\n\n        return key_values\n\n    @classmethod\n    def _solve_template_inner_links(cls, templates):\n        \"\"\"Solve templates inner keys identified by \"{@*}\".\n\n        Process is split into 2 parts.\n        First is collecting all global keys (keys in top hierarchy where value\n        is not dictionary). All global keys are set for all group keys (keys\n        in top hierarchy where value is dictionary). Value of a key is not\n        overridden in group if already contain value for the key.\n\n        In second part all keys with \"at\" symbol in value are replaced with\n        value of the key afterward \"at\" symbol from the group.\n\n        Args:\n            templates (dict): Raw templates data.\n\n        Example:\n            templates::\n                key_1: \"value_1\",\n                key_2: \"{@key_1}/{filling_key}\"\n\n                group_1:\n                    key_3: \"value_3/{@key_2}\"\n\n                group_2:\n                    key_2\": \"value_2\"\n                    key_4\": \"value_4/{@key_2}\"\n\n            output::\n                key_1: \"value_1\"\n                key_2: \"value_1/{filling_key}\"\n\n                group_1: {\n                    key_1: \"value_1\"\n                    key_2: \"value_1/{filling_key}\"\n                    key_3: \"value_3/value_1/{filling_key}\"\n\n                group_2: {\n                    key_1: \"value_1\"\n                    key_2: \"value_2\"\n                    key_4: \"value_3/value_2\"\n\n        Returns:\n            dict[str, Any]: Solved templates data.\n\n        \"\"\"\n        default_key_values = templates.pop(\"common\", {})\n        output = {}\n        for category_name, category_value in templates.items():\n            new_category_value = {}\n            for key, value in category_value.items():\n                key_values = copy.deepcopy(default_key_values)\n                key_values.update(value)\n                new_category_value[key] = cls._prepare_inner_keys(key_values)\n            output[category_name] = new_category_value\n\n        default_keys_by_subkeys = cls._prepare_inner_keys(default_key_values)\n        for key, value in default_keys_by_subkeys.items():\n            output[key] = value\n\n        return output\n\n    @classmethod\n    def _dict_to_subkeys_list(cls, subdict):\n        \"\"\"Convert dictionary to list of subkeys.\n\n        Example::\n\n            _dict_to_subkeys_list({\n                \"root\": {\n                    \"work\": \"path/to/work\",\n                    \"publish\": \"path/to/publish\"\n                }\n            })\n            [\n                [\"root\", \"work\"],\n                [\"root\", \"publish\"]\n            ]\n\n\n        Args:\n            dict[str, Any]: Dictionary to be converted.\n\n        Returns:\n            list[list[str]]: List of subkeys.\n\n        \"\"\"\n        output = []\n        subkey_queue = collections.deque()\n        subkey_queue.append((subdict, []))\n        while subkey_queue:\n            queue_item = subkey_queue.popleft()\n            data, pre_keys = queue_item\n            for key, value in data.items():\n                result = list(pre_keys)\n                result.append(key)\n                if isinstance(value, dict):\n                    subkey_queue.append((value, result))\n                else:\n                    output.append(result)\n        return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/templates.html#client.ayon_core.pipeline.anatomy.templates.AnatomyTemplates.anatomy","title":"<code>anatomy</code>  <code>property</code>","text":"<p>Anatomy instance.</p> <p>Returns:</p> Name Type Description <code>Anatomy</code> <p>Anatomy instance.</p>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/templates.html#client.ayon_core.pipeline.anatomy.templates.AnatomyTemplates.frame_padding","title":"<code>frame_padding</code>  <code>property</code>","text":"<p>Default frame padding.</p> <p>Returns:</p> Name Type Description <code>int</code> <p>Frame padding used by default in templates.</p>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/templates.html#client.ayon_core.pipeline.anatomy.templates.AnatomyTemplates.project_name","title":"<code>project_name</code>  <code>property</code>","text":"<p>Project name.</p> <p>Returns:</p> Type Description <p>Union[str, None]: Project name if set, otherwise None.</p>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/templates.html#client.ayon_core.pipeline.anatomy.templates.AnatomyTemplates.roots","title":"<code>roots</code>  <code>property</code>","text":"<p>Anatomy roots object.</p> <p>Returns:</p> Type Description <p>dict[str, AnatomyRoot]: Anatomy roots data.</p>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/templates.html#client.ayon_core.pipeline.anatomy.templates.AnatomyTemplates.templates","title":"<code>templates</code>  <code>property</code>","text":"<p>Templates data.</p> <p>Templates data with replaced common data.</p> <p>Returns:</p> Type Description <p>dict[str, Any]: Templates data.</p>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/templates.html#client.ayon_core.pipeline.anatomy.templates.AnatomyTemplates.version_padding","title":"<code>version_padding</code>  <code>property</code>","text":"<p>Default version padding.</p> <p>Returns:</p> Name Type Description <code>int</code> <p>Version padding used by default in templates.</p>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/templates.html#client.ayon_core.pipeline.anatomy.templates.AnatomyTemplates.format","title":"<code>format(data, strict=True)</code>","text":"<p>Fill all templates based on entered data.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict[str, Any]</code> <p>Fill data used for template formatting.</p> required <code>strict</code> <code>Optional[bool]</code> <p>Raise exception is accessed value is not fully filled.</p> <code>True</code> <p>Returns:</p> Name Type Description <code>TemplatesResultDict</code> <p>Output <code>TemplateResult</code> have <code>strict</code> attribute set to False so accessing unfilled keys in templates won't raise any exceptions.</p> Source code in <code>client/ayon_core/pipeline/anatomy/templates.py</code> <pre><code>def format(self, data, strict=True):\n    \"\"\"Fill all templates based on entered data.\n\n    Args:\n        data (dict[str, Any]): Fill data used for template formatting.\n        strict (Optional[bool]): Raise exception is accessed value is\n            not fully filled.\n\n    Returns:\n        TemplatesResultDict: Output `TemplateResult` have `strict`\n            attribute set to False so accessing unfilled keys in templates\n            won't raise any exceptions.\n\n    \"\"\"\n    self._validate_discovery()\n    copy_data = copy.deepcopy(data)\n    roots = self._anatomy.roots\n    if roots:\n        copy_data[\"root\"] = roots\n\n    return self._solve_dict(copy_data, strict)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/templates.html#client.ayon_core.pipeline.anatomy.templates.AnatomyTemplates.format_all","title":"<code>format_all(in_data)</code>","text":"<p>Fill all templates based on entered data.</p> Deprecated <p>Use <code>format</code> method with <code>strict=False</code> instead.</p> <p>Parameters:</p> Name Type Description Default <code>in_data</code> <code>dict</code> <p>Containing keys to be filled into template.</p> required <p>Returns:</p> Name Type Description <code>TemplatesResultDict</code> <p>Output <code>TemplateResult</code> have <code>strict</code> attribute set to False so accessing unfilled keys in templates won't raise any exceptions.</p> Source code in <code>client/ayon_core/pipeline/anatomy/templates.py</code> <pre><code>def format_all(self, in_data):\n    \"\"\"Fill all templates based on entered data.\n\n    Deprecated:\n        Use `format` method with `strict=False` instead.\n\n    Args:\n        in_data (dict): Containing keys to be filled into template.\n\n    Returns:\n        TemplatesResultDict: Output `TemplateResult` have `strict`\n            attribute set to False so accessing unfilled keys in templates\n            won't raise any exceptions.\n\n    \"\"\"\n    return self.format(in_data, strict=False)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/templates.html#client.ayon_core.pipeline.anatomy.templates.AnatomyTemplates.get_rootless_path_from_result","title":"<code>get_rootless_path_from_result(result)</code>  <code>classmethod</code>","text":"<p>Calculate rootless path from formatting result.</p> <p>Parameters:</p> Name Type Description Default <code>result</code> <code>TemplateResult</code> <p>Result of StringTemplate formatting.</p> required <p>Returns:</p> Name Type Description <code>str</code> <p>Rootless path if result contains one of anatomy roots.</p> Source code in <code>client/ayon_core/pipeline/anatomy/templates.py</code> <pre><code>@classmethod\ndef get_rootless_path_from_result(cls, result):\n    \"\"\"Calculate rootless path from formatting result.\n\n    Args:\n        result (TemplateResult): Result of StringTemplate formatting.\n\n    Returns:\n        str: Rootless path if result contains one of anatomy roots.\n    \"\"\"\n\n    used_values = result.used_values\n    missing_keys = result.missing_keys\n    template = result.template\n    invalid_types = result.invalid_types\n    if (\n        \"root\" not in used_values\n        or \"root\" in missing_keys\n        or \"{root\" not in template\n    ):\n        return\n\n    for invalid_type in invalid_types:\n        if \"root\" in invalid_type:\n            return\n\n    root_keys = cls._dict_to_subkeys_list({\"root\": used_values[\"root\"]})\n    if not root_keys:\n        return\n\n    output = str(result)\n    for used_root_keys in root_keys:\n        if not used_root_keys:\n            continue\n\n        used_value = used_values\n        root_key = None\n        for key in used_root_keys:\n            used_value = used_value[key]\n            if root_key is None:\n                root_key = key\n            else:\n                root_key += \"[{}]\".format(key)\n\n        root_key = \"{\" + root_key + \"}\"\n        output = output.replace(str(used_value), root_key)\n\n    # Make sure rootless path is with forward slashes\n    if _IS_WINDOWS:\n        output.replace(\"\\\\\", \"/\")\n\n    # Make sure there are no double slashes\n    while \"//\" in output:\n        output = output.replace(\"//\", \"/\")\n\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/templates.html#client.ayon_core.pipeline.anatomy.templates.AnatomyTemplates.get_template_item","title":"<code>get_template_item(category_name, template_name, subkey=None, default=NOT_SET)</code>","text":"<p>Get template item from category.</p> <p>Parameters:</p> Name Type Description Default <code>category_name</code> <code>str</code> <p>Category name.</p> required <code>template_name</code> <code>str</code> <p>Template name.</p> required <code>subkey</code> <code>str | None</code> <p>Subkey name.</p> <code>None</code> <code>default</code> <code>Any</code> <p>Default value if template is not found.</p> <code>NOT_SET</code> <p>Returns:</p> Name Type Description <code>Any</code> <code>Any</code> <p>Template item or subkey value.</p> <p>Raises:</p> Type Description <code>KeyError</code> <p>When any passed key is not available. Raise of error does not happen if 'default' is filled.</p> Source code in <code>client/ayon_core/pipeline/anatomy/templates.py</code> <pre><code>def get_template_item(\n    self,\n    category_name: str,\n    template_name: str,\n    subkey: str | None = None,\n    default: Any = NOT_SET\n) -&gt; Any:\n    \"\"\"Get template item from category.\n\n    Args:\n        category_name (str): Category name.\n        template_name (str): Template name.\n        subkey (str | None): Subkey name.\n        default (Any): Default value if template is not found.\n\n    Returns:\n        Any: Template item or subkey value.\n\n    Raises:\n        KeyError: When any passed key is not available. Raise of error\n            does not happen if 'default' is filled.\n\n    \"\"\"\n    self._validate_discovery()\n    category = self.get(category_name)\n    if category is None:\n        if default is not NOT_SET:\n            return default\n        raise KeyError(\"Category '{}' not found.\".format(category_name))\n\n    template_item = category.get(template_name)\n    if template_item is None:\n        if default is not NOT_SET:\n            return default\n        raise KeyError(\n            \"Template '{}' not found in category '{}'.\".format(\n                template_name, category_name\n            )\n        )\n\n    if subkey is None:\n        return template_item\n\n    item = template_item.get(subkey)\n    if item is not None:\n        return item\n\n    if default is not NOT_SET:\n        return default\n    raise KeyError(\n        \"Subkey '{}' not found in '{}/{}'.\".format(\n            subkey, category_name, template_name\n        )\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/templates.html#client.ayon_core.pipeline.anatomy.templates.TemplateCategory","title":"<code>TemplateCategory</code>","text":"<p>Template category.</p> <p>Template category groups template items for specific usage. Categories     available at the moment are 'work', 'publish', 'hero', 'delivery',     'staging' and 'others'.</p> <p>Parameters:</p> Name Type Description Default <code>anatomy_templates</code> <code>AnatomyTemplates</code> <p>Anatomy templates object.</p> required <code>category_name</code> <code>str</code> <p>Category name.</p> required <code>category_data</code> <code>dict[str, Any]</code> <p>Category data.</p> required Source code in <code>client/ayon_core/pipeline/anatomy/templates.py</code> <pre><code>class TemplateCategory:\n    \"\"\"Template category.\n\n    Template category groups template items for specific usage. Categories\n        available at the moment are 'work', 'publish', 'hero', 'delivery',\n        'staging' and 'others'.\n\n    Args:\n        anatomy_templates (AnatomyTemplates): Anatomy templates object.\n        category_name (str): Category name.\n        category_data (dict[str, Any]): Category data.\n\n    \"\"\"\n    def __init__(self, anatomy_templates, category_name, category_data):\n        for key, value in category_data.items():\n            if isinstance(value, dict):\n                value = TemplateItem(anatomy_templates, value)\n            elif isinstance(value, str):\n                value = AnatomyStringTemplate(anatomy_templates, value)\n            category_data[key] = value\n        self._name = category_name\n        self._name_prefix = \"{}_\".format(category_name)\n        self._category_data = category_data\n\n    def __getitem__(self, key):\n        new_key = self._convert_getter_key(key)\n        return self._category_data[new_key]\n\n    def get(self, key, default=None):\n        new_key = self._convert_getter_key(key)\n        return self._category_data.get(new_key, default)\n\n    @property\n    def name(self):\n        \"\"\"Category name.\n\n        Returns:\n            str: Category name.\n\n        \"\"\"\n        return self._name\n\n    def format(self, data, strict=True):\n        output = {}\n        for key, value in self._category_data.items():\n            if isinstance(value, TemplateItem):\n                value = value.format(data, strict)\n            elif isinstance(value, AnatomyStringTemplate):\n                value = value.format(data)\n\n            if isinstance(value, TemplatesResultDict):\n                value.key = key\n            output[key] = value\n        return TemplatesResultDict(output, key=self.name, strict=strict)\n\n    def _convert_getter_key(self, key):\n        \"\"\"Convert key for backwards compatibility.\n\n        OpenPype compatible settings did contain template keys prefixed by\n        category name e.g. 'publish_render' which should be just 'render'.\n\n        This method keeps the backwards compatibility but only if the key\n        starts with the category name prefix and the key is available in\n        roots.\n\n        Args:\n            key (str): Key to be converted.\n\n        Returns:\n            str: Converted string.\n\n        \"\"\"\n        if key in self._category_data:\n            return key\n\n        # Use default when the key is the category name\n        if key == self._name:\n            return \"default\"\n\n        # Remove prefix if is key prefixed\n        if key.startswith(self._name_prefix):\n            new_key = key[len(self._name_prefix):]\n            if new_key in self._category_data:\n                return new_key\n        return key\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/templates.html#client.ayon_core.pipeline.anatomy.templates.TemplateCategory.name","title":"<code>name</code>  <code>property</code>","text":"<p>Category name.</p> <p>Returns:</p> Name Type Description <code>str</code> <p>Category name.</p>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/templates.html#client.ayon_core.pipeline.anatomy.templates.TemplateItem","title":"<code>TemplateItem</code>","text":"<p>Template item under template category.</p> <p>This item data usually contains 'file' and 'directory' by anatomy     definition, enhanced by common data ('frame_padding',     'version_padding'). It adds 'path' key which is combination of     'file' and 'directory' values.</p> <p>Parameters:</p> Name Type Description Default <code>anatomy_templates</code> <code>AnatomyTemplates</code> <p>Anatomy templates object.</p> required <code>template_data</code> <code>dict[str, Any]</code> <p>Templates data.</p> required Source code in <code>client/ayon_core/pipeline/anatomy/templates.py</code> <pre><code>class TemplateItem:\n    \"\"\"Template item under template category.\n\n    This item data usually contains 'file' and 'directory' by anatomy\n        definition, enhanced by common data ('frame_padding',\n        'version_padding'). It adds 'path' key which is combination of\n        'file' and 'directory' values.\n\n    Args:\n        anatomy_templates (AnatomyTemplates): Anatomy templates object.\n        template_data (dict[str, Any]): Templates data.\n\n    \"\"\"\n    def __init__(self, anatomy_templates, template_data):\n        template_data = copy.deepcopy(template_data)\n\n        # Backwards compatibility for 'folder'\n        # TODO remove when deprecation not needed anymore\n        if (\n            \"folder\" not in template_data\n            and \"directory\" in template_data\n        ):\n            template_data[\"folder\"] = template_data[\"directory\"]\n\n        # Add 'path' key\n        if (\n            \"path\" not in template_data\n            and \"file\" in template_data\n            and \"directory\" in template_data\n        ):\n            template_data[\"path\"] = \"/\".join(\n                (template_data[\"directory\"], template_data[\"file\"])\n            )\n\n        for key, value in template_data.items():\n            if isinstance(value, str):\n                value = AnatomyStringTemplate(anatomy_templates, value)\n            template_data[key] = value\n\n        self._template_data = template_data\n        self._anatomy_templates = anatomy_templates\n\n    def __getitem__(self, key):\n        return self._template_data[key]\n\n    def get(self, key, default=None):\n        return self._template_data.get(key, default)\n\n    def format(self, data, strict=True):\n        output = {}\n        for key, value in self._template_data.items():\n            if isinstance(value, AnatomyStringTemplate):\n                value = value.format(data)\n            output[key] = value\n        return TemplatesResultDict(output, strict=strict)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/templates.html#client.ayon_core.pipeline.anatomy.templates.TemplatesResultDict","title":"<code>TemplatesResultDict</code>","text":"<p>               Bases: <code>dict</code></p> <p>Holds and wrap 'AnatomyTemplateResult' for easy bug report.</p> <p>Dictionary like object which holds 'AnatomyTemplateResult' in the same data structure as base dictionary of anatomy templates. It can raise</p> Source code in <code>client/ayon_core/pipeline/anatomy/templates.py</code> <pre><code>class TemplatesResultDict(dict):\n    \"\"\"Holds and wrap 'AnatomyTemplateResult' for easy bug report.\n\n    Dictionary like object which holds 'AnatomyTemplateResult' in the same\n    data structure as base dictionary of anatomy templates. It can raise\n\n    \"\"\"\n\n    def __init__(self, in_data, key=None, parent=None, strict=None):\n        super(TemplatesResultDict, self).__init__()\n        for _key, _value in in_data.items():\n            if isinstance(_value, TemplatesResultDict):\n                _value.parent = self\n            elif isinstance(_value, dict):\n                _value = self.__class__(_value, _key, self)\n            self[_key] = _value\n\n        if strict is None and parent is None:\n            strict = True\n\n        self.key = key\n        self.parent = parent\n        self._is_strict = strict\n\n    def __getitem__(self, key):\n        if key not in self.keys():\n            hier = self.get_hierarchy()\n            hier.append(key)\n            raise TemplateMissingKey(hier)\n\n        value = super(TemplatesResultDict, self).__getitem__(key)\n        if isinstance(value, self.__class__):\n            return value\n\n        # Raise exception when expected solved templates and it is not.\n        if self.is_strict and hasattr(value, \"validate\"):\n            value.validate()\n        return value\n\n    def get_is_strict(self):\n        return self._is_strict\n\n    def set_is_strict(self, is_strict):\n        if is_strict is None and self.parent is None:\n            is_strict = True\n        self._is_strict = is_strict\n        for child in self.values():\n            if isinstance(child, self.__class__):\n                child.set_is_strict(is_strict)\n            elif isinstance(child, AnatomyTemplateResult):\n                child.strict = is_strict\n\n    strict = property(get_is_strict, set_is_strict)\n    is_strict = property(get_is_strict, set_is_strict)\n\n    def get_hierarchy(self):\n        \"\"\"Return dictionary keys one by one to root parent.\"\"\"\n        if self.key is None:\n            return []\n\n        if self.parent is None:\n            return [self.key]\n\n        par_hier = list(self.parent.get_hierarchy())\n        par_hier.append(self.key)\n        return par_hier\n\n    @property\n    def missing_keys(self):\n        \"\"\"Return missing keys of all children templates.\"\"\"\n        missing_keys = set()\n        for value in self.values():\n            missing_keys |= value.missing_keys\n        return missing_keys\n\n    @property\n    def invalid_types(self):\n        \"\"\"Return invalid types of all children templates.\"\"\"\n        invalid_types = {}\n        for value in self.values():\n            invalid_types = _merge_dict(invalid_types, value.invalid_types)\n        return invalid_types\n\n    @property\n    def used_values(self):\n        \"\"\"Return used values for all children templates.\"\"\"\n        used_values = {}\n        for value in self.values():\n            used_values = _merge_dict(used_values, value.used_values)\n        return used_values\n\n    def get_solved(self):\n        \"\"\"Get only solved key from templates.\"\"\"\n        result = {}\n        for key, value in self.items():\n            if isinstance(value, self.__class__):\n                value = value.get_solved()\n                if not value:\n                    continue\n                result[key] = value\n\n            elif (\n                not hasattr(value, \"solved\") or\n                value.solved\n            ):\n                result[key] = value\n        return self.__class__(result, key=self.key, parent=self.parent)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/templates.html#client.ayon_core.pipeline.anatomy.templates.TemplatesResultDict.invalid_types","title":"<code>invalid_types</code>  <code>property</code>","text":"<p>Return invalid types of all children templates.</p>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/templates.html#client.ayon_core.pipeline.anatomy.templates.TemplatesResultDict.missing_keys","title":"<code>missing_keys</code>  <code>property</code>","text":"<p>Return missing keys of all children templates.</p>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/templates.html#client.ayon_core.pipeline.anatomy.templates.TemplatesResultDict.used_values","title":"<code>used_values</code>  <code>property</code>","text":"<p>Return used values for all children templates.</p>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/templates.html#client.ayon_core.pipeline.anatomy.templates.TemplatesResultDict.get_hierarchy","title":"<code>get_hierarchy()</code>","text":"<p>Return dictionary keys one by one to root parent.</p> Source code in <code>client/ayon_core/pipeline/anatomy/templates.py</code> <pre><code>def get_hierarchy(self):\n    \"\"\"Return dictionary keys one by one to root parent.\"\"\"\n    if self.key is None:\n        return []\n\n    if self.parent is None:\n        return [self.key]\n\n    par_hier = list(self.parent.get_hierarchy())\n    par_hier.append(self.key)\n    return par_hier\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/anatomy/templates.html#client.ayon_core.pipeline.anatomy.templates.TemplatesResultDict.get_solved","title":"<code>get_solved()</code>","text":"<p>Get only solved key from templates.</p> Source code in <code>client/ayon_core/pipeline/anatomy/templates.py</code> <pre><code>def get_solved(self):\n    \"\"\"Get only solved key from templates.\"\"\"\n    result = {}\n    for key, value in self.items():\n        if isinstance(value, self.__class__):\n            value = value.get_solved()\n            if not value:\n                continue\n            result[key] = value\n\n        elif (\n            not hasattr(value, \"solved\") or\n            value.solved\n        ):\n            result[key] = value\n    return self.__class__(result, key=self.key, parent=self.parent)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html","title":"create","text":""},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.AttributeValues","title":"<code>AttributeValues</code>","text":"<p>Container which keep values of Attribute definitions.</p> <p>Goal is to have one object which hold values of attribute definitions for single instance.</p> <p>Has dictionary like methods. Not all of them are allowed all the time.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <code>Union[CreatedInstance, PublishAttributes]</code> <p>Parent object.</p> required <code>key</code> <code>str</code> <p>Key of attribute values.</p> required <code>attr_defs</code> <code>List[AbstractAttrDef]</code> <p>Definitions of value type and properties.</p> required <code>values</code> <code>dict</code> <p>Values after possible conversion.</p> required <code>origin_data</code> <code>dict</code> <p>Values loaded from host before conversion.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>class AttributeValues:\n    \"\"\"Container which keep values of Attribute definitions.\n\n    Goal is to have one object which hold values of attribute definitions for\n    single instance.\n\n    Has dictionary like methods. Not all of them are allowed all the time.\n\n    Args:\n        parent (Union[CreatedInstance, PublishAttributes]): Parent object.\n        key (str): Key of attribute values.\n        attr_defs (List[AbstractAttrDef]): Definitions of value type\n            and properties.\n        values (dict): Values after possible conversion.\n        origin_data (dict): Values loaded from host before conversion.\n\n    \"\"\"\n    def __init__(self, parent, key, attr_defs, values, origin_data=None):\n        self._parent = parent\n        self._key = key\n        if origin_data is None:\n            origin_data = copy.deepcopy(values)\n        self._origin_data = origin_data\n\n        attr_defs_by_key = {}\n        button_defs_by_key = {}\n        for attr_def in attr_defs:\n            if attr_def.is_value_def:\n                attr_defs_by_key[attr_def.key] = attr_def\n            elif isinstance(attr_def, ButtonDef):\n                button_defs_by_key[attr_def.key] = attr_def\n\n        for key, value in values.items():\n            if key not in attr_defs_by_key:\n                new_def = UnknownDef(key, label=key, default=value)\n                attr_defs.append(new_def)\n                attr_defs_by_key[key] = new_def\n\n        self._attr_defs = attr_defs\n        self._attr_defs_by_key = attr_defs_by_key\n        self._button_defs_by_key = button_defs_by_key\n\n        self._data = {}\n        for attr_def in attr_defs:\n            value = values.get(attr_def.key)\n            if value is None:\n                continue\n            converted_value = attr_def.convert_value(value)\n            # QUESTION Could we just use converted value all the time?\n            if converted_value == value:\n                self._data[attr_def.key] = value\n\n    def __setitem__(self, key, value):\n        if key not in self._attr_defs_by_key:\n            raise KeyError(\"Key \\\"{}\\\" was not found.\".format(key))\n\n        self.update({key: value})\n\n    def __getitem__(self, key):\n        if key not in self._attr_defs_by_key:\n            return self._data[key]\n        return self._data.get(key, self._attr_defs_by_key[key].default)\n\n    def __contains__(self, key):\n        return key in self._attr_defs_by_key\n\n    def __iter__(self):\n        for key in self._attr_defs_by_key:\n            yield key\n\n    def get(self, key, default=None):\n        if key in self._attr_defs_by_key:\n            return self[key]\n        return default\n\n    def keys(self):\n        return self._attr_defs_by_key.keys()\n\n    def values(self):\n        for key in self._attr_defs_by_key.keys():\n            yield self._data.get(key)\n\n    def items(self):\n        for key in self._attr_defs_by_key.keys():\n            yield key, self._data.get(key)\n\n    def get_attr_def(self, key, default=None):\n        button_def = self._button_defs_by_key.get(key)\n        if button_def is not None:\n            return button_def\n        return self._attr_defs_by_key.get(key, default)\n\n    def update(self, value):\n        changes = self._update(value)\n        if changes:\n            self._parent.attribute_value_changed(self._key, changes)\n\n    def pop(self, key, default=None):\n        value, changes = self._pop(key, default)\n        if changes:\n            self._parent.attribute_value_changed(self._key, changes)\n        return value\n\n    def set_value(self, value):\n        pop_keys = set(value.keys()) - set(self._data.keys())\n        changes = self._update(value)\n        for key in pop_keys:\n            _, key_changes = self._pop(key, None)\n            changes.update(key_changes)\n\n        if changes:\n            self._parent.attribute_value_changed(self._key, changes)\n\n    def reset_values(self):\n        self._data = {}\n\n    def mark_as_stored(self):\n        self._origin_data = copy.deepcopy(self._data)\n\n    @property\n    def attr_defs(self):\n        \"\"\"Pointer to attribute definitions.\n\n        Returns:\n            List[AbstractAttrDef]: Attribute definitions.\n        \"\"\"\n\n        return list(self._attr_defs)\n\n    @property\n    def origin_data(self):\n        return copy.deepcopy(self._origin_data)\n\n    def data_to_store(self):\n        \"\"\"Create new dictionary with data to store.\n\n        Returns:\n            Dict[str, Any]: Attribute values that should be stored.\n        \"\"\"\n\n        output = {}\n        for key in self._data:\n            output[key] = self[key]\n\n        for key, attr_def in self._attr_defs_by_key.items():\n            if key not in output:\n                output[key] = attr_def.default\n        return output\n\n    def get_serialized_attr_defs(self):\n        \"\"\"Serialize attribute definitions to json serializable types.\n\n        Returns:\n            List[Dict[str, Any]]: Serialized attribute definitions.\n        \"\"\"\n\n        return serialize_attr_defs(self._attr_defs)\n\n    def _update(self, value):\n        changes = {}\n        for key, key_value in dict(value).items():\n            if key in self._data and self._data.get(key) == key_value:\n                continue\n            self._data[key] = key_value\n            changes[key] = key_value\n        return changes\n\n    def _pop(self, key, default):\n        has_key = key in self._data\n        value = self._data.pop(key, default)\n        # Remove attribute definition if is 'UnknownDef'\n        # - gives option to get rid of unknown values\n        attr_def = self._attr_defs_by_key.get(key)\n        changes = {}\n        if isinstance(attr_def, UnknownDef):\n            self._attr_defs_by_key.pop(key)\n            self._attr_defs.remove(attr_def)\n        elif has_key:\n            changes[key] = None\n        return value, changes\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.AttributeValues.attr_defs","title":"<code>attr_defs</code>  <code>property</code>","text":"<p>Pointer to attribute definitions.</p> <p>Returns:</p> Type Description <p>List[AbstractAttrDef]: Attribute definitions.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.AttributeValues.data_to_store","title":"<code>data_to_store()</code>","text":"<p>Create new dictionary with data to store.</p> <p>Returns:</p> Type Description <p>Dict[str, Any]: Attribute values that should be stored.</p> Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>def data_to_store(self):\n    \"\"\"Create new dictionary with data to store.\n\n    Returns:\n        Dict[str, Any]: Attribute values that should be stored.\n    \"\"\"\n\n    output = {}\n    for key in self._data:\n        output[key] = self[key]\n\n    for key, attr_def in self._attr_defs_by_key.items():\n        if key not in output:\n            output[key] = attr_def.default\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.AttributeValues.get_serialized_attr_defs","title":"<code>get_serialized_attr_defs()</code>","text":"<p>Serialize attribute definitions to json serializable types.</p> <p>Returns:</p> Type Description <p>List[Dict[str, Any]]: Serialized attribute definitions.</p> Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>def get_serialized_attr_defs(self):\n    \"\"\"Serialize attribute definitions to json serializable types.\n\n    Returns:\n        List[Dict[str, Any]]: Serialized attribute definitions.\n    \"\"\"\n\n    return serialize_attr_defs(self._attr_defs)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.AutoCreator","title":"<code>AutoCreator</code>","text":"<p>               Bases: <code>BaseCreator</code></p> <p>Creator which is automatically triggered without user interaction.</p> <p>Can be used e.g. for <code>workfile</code>.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>class AutoCreator(BaseCreator):\n    \"\"\"Creator which is automatically triggered without user interaction.\n\n    Can be used e.g. for `workfile`.\n    \"\"\"\n    skip_discovery = True\n\n    def remove_instances(self, instances: list[CreatedInstance]) -&gt; None:\n        \"\"\"Skip removal.\"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.AutoCreator.remove_instances","title":"<code>remove_instances(instances)</code>","text":"<p>Skip removal.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def remove_instances(self, instances: list[CreatedInstance]) -&gt; None:\n    \"\"\"Skip removal.\"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.BaseCreator","title":"<code>BaseCreator</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Plugin that create and modify instance data before publishing process.</p> <p>Single object should be used for multiple instances instead of single instance per one creator object. Do not store temp data or mid-process data to <code>self</code> if it's not Plugin specific.</p> <p>Parameters:</p> Name Type Description Default <code>project_settings</code> <code>dict[str, Any]</code> <p>Project settings.</p> required <code>create_context</code> <code>CreateContext</code> <p>Context which initialized creator.</p> required <code>headless</code> <code>bool</code> <p>Running in headless mode.</p> <code>False</code> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>class BaseCreator(ABC):\n    \"\"\"Plugin that create and modify instance data before publishing process.\n\n    Single object should be used for multiple instances instead of single\n    instance per one creator object. Do not store temp data or mid-process data\n    to `self` if it's not Plugin specific.\n\n    Args:\n        project_settings (dict[str, Any]): Project settings.\n        create_context (CreateContext): Context which initialized creator.\n        headless (bool): Running in headless mode.\n\n    \"\"\"\n    # Attribute 'skip_discovery' is used during discovery phase to skip\n    #   plugins, which can be used to mark base plugins that should not be\n    #   considered as plugins \"to use\". The discovery logic does NOT use\n    #   the attribute value from parent classes. Each base class has to define\n    #   the attribute again.\n    skip_discovery: bool = True\n\n    # Label shown in UI\n    label: str | None = None\n    group_label: str | None = None\n    # Cached group label after first call 'get_group_label'\n    _cached_group_label: str | None = None\n\n    # Order in which will be plugin executed (collect &amp; update instances)\n    #   less == earlier -&gt; Order '90' will be processed before '100'\n    order: int = 100\n\n    # Variable to store logger\n    _log: logging.Logger | None = None\n\n    # Creator is enabled (Probably does not have reason of existence?)\n    enabled: bool = True\n\n    # Creator (and product base type) icon\n    # - may not be used if `get_icon` is reimplemented\n    icon: IconBase | str | None = None\n\n    # Instance attribute definitions that can be changed per instance\n    # - returns list of attribute definitions from\n    #       `ayon_core.lib.attribute_definitions`\n    instance_attr_defs: list[AbstractAttrDef] = []\n\n    # Filtering by host name - can be used to be filtered by host name\n    # - used on all hosts when set to 'None' for Backwards compatibility\n    #   - was added afterwards\n    # QUESTION make this required?\n    host_name: str | None = None\n\n    # Settings auto-apply helpers\n    # Root key in project settings (mandatory for auto-apply to work)\n    settings_category: str | None = None\n    # Name of plugin in create settings &gt; class name is used if not set\n    settings_name: str | None = None\n\n    product_type_items: list[ProductTypeItem] = []\n\n    def __init__(\n        self,\n        project_settings: dict[str, Any],\n        create_context: CreateContext,\n        headless: bool = False\n    ) -&gt; None:\n        # Reference to CreateContext\n        self.create_context: CreateContext = create_context\n        self.project_settings = project_settings\n\n        # Creator is running in headless mode (without UI elements)\n        # - we may use UI inside processing this attribute should be checked\n        self.headless: bool = headless\n\n        self.apply_settings(project_settings)\n        self.register_callbacks()\n\n    @staticmethod\n    def _get_settings_values(\n        project_settings: dict[str, Any],\n        category_name: str,\n        plugin_name: str\n    ) -&gt; dict[str, Any] | None:\n        \"\"\"Helper method to get settings values.\n\n        Args:\n            project_settings (dict[str, Any]): Project settings.\n            category_name (str): Category of settings.\n            plugin_name (str): Name of settings.\n\n        Returns:\n            dict[str, Any] | None: Settings values or None.\n\n        \"\"\"\n        settings = project_settings.get(category_name)\n        if not settings:\n            return None\n\n        create_settings = settings.get(\"create\")\n        if not create_settings:\n            return None\n\n        return create_settings.get(plugin_name)\n\n    def apply_settings(self, project_settings: dict[str, Any]) -&gt; None:\n        \"\"\"Method called on initialization of plugin to apply settings.\n\n        Default implementation tries to auto-apply settings values if are\n            in expected hierarchy.\n\n        Data hierarchy to auto-apply settings:\n            \u251c\u2500 {self.settings_category}                 - Root key in settings\n            \u2502 \u2514\u2500 \"create\"                               - Hardcoded key\n            \u2502   \u2514\u2500 {self.settings_name} | {class name}  - Name of plugin\n            \u2502     \u251c\u2500 ... attribute values...            - Attribute/value pair\n\n        It is mandatory to define 'settings_category' attribute. Attribute\n        'settings_name' is optional and class name is used if is not defined.\n\n        Example data:\n            ProjectSettings {\n                \"maya\": {                    # self.settings_category\n                    \"create\": {              # Hardcoded key\n                        \"CreateAnimation\": { # self.settings_name / class name\n                            \"enabled\": True, # --- Attributes to set ---\n                            \"optional\": True,#\n                            \"active\": True,  #\n                            \"fps\": 25,       # -------------------------\n                        },\n                        ...\n                    },\n                    ...\n                },\n                ...\n            }\n\n        Args:\n            project_settings (dict[str, Any]): Project settings.\n        \"\"\"\n\n        settings_category = self.settings_category\n        if not settings_category:\n            return\n\n        cls_name = self.__class__.__name__\n        settings_name = self.settings_name or cls_name\n\n        settings = self._get_settings_values(\n            project_settings, settings_category, settings_name\n        )\n        if settings is None:\n            self.log.debug(f\"No settings found for {cls_name}\")\n            return\n\n        for key, value in settings.items():\n            # Log out attributes that are not defined on plugin object\n            # - those may be potential dangerous typos in settings\n            if not hasattr(self, key):\n                self.log.debug(\n                    \"Applying settings to unknown attribute '%s' on '%s'.\",\n                    key, cls_name\n                )\n            setattr(self, key, value)\n\n        self.product_type_items = self._convert_product_type_items(\n            self.product_type_items\n        )\n\n    def _convert_product_type_items(\n        self, product_type_items: list\n    ) -&gt; list[ProductTypeItem]:\n        \"\"\"Helper method to convert product type items from settings.\"\"\"\n        if not product_type_items:\n            return []\n\n        first_item = product_type_items[0]\n        if isinstance(first_item, ProductTypeItem):\n            return product_type_items\n\n        if not isinstance(first_item, dict):\n            self.log.warning(\n                f\"Invalid product type item. Expected 'dict' or\"\n                f\" 'ProductTypeItem', got '{type(first_item)}'.\"\n            )\n            return []\n\n        try:\n            return [\n                ProductTypeItem.from_data(item)\n                for item in self.product_type_items\n            ]\n        except Exception:\n            self.log.warning(\n                \"Failed to convert product type items\"\n                \" to ProductTypeItem instances\"\n            )\n            return []\n\n    def register_callbacks(self) -&gt; None:\n        \"\"\"Register callbacks for creator.\n\n        Default implementation does nothing. It can be overridden to register\n        callbacks for creator.\n        \"\"\"\n\n    @property\n    def identifier(self) -&gt; str:\n        \"\"\"Identifier of creator (must be unique).\n\n        Default implementation returns plugin's product base type,\n        or falls back to product type if product base type is not set.\n\n        \"\"\"\n        identifier = self.product_base_type\n        if not identifier:\n            identifier = self.product_type\n        return identifier\n\n    @property\n    @abstractmethod\n    def product_type(self) -&gt; str:\n        \"\"\"Family that plugin represents.\n\n        DEPRECATED - use 'product_base_type' instead. This is kept for\n            backwards compatibility and should be removed when\n            'product_base_type' is required.\n\n        \"\"\"\n\n    @property\n    def product_base_type(self) -&gt; str | None:\n        \"\"\"Product base type that plugin represents.\n\n        Todo (antirotor): This should be required in future - it\n            should be made abstract then.\n\n        Returns:\n            str | None: Product base type that plugin represents.\n                If not set, it is assumed that the creator plugin is obsolete\n                and does not support product base type.\n\n        \"\"\"\n        return None\n\n    @property\n    def project_name(self) -&gt; str:\n        \"\"\"Current project name.\n\n        Returns:\n            str: Name of a project.\n\n        \"\"\"\n        return self.create_context.project_name\n\n    @property\n    def project_anatomy(self) -&gt; Anatomy:\n        \"\"\"Current project anatomy.\n\n        Returns:\n            Anatomy: Project anatomy object.\n\n        \"\"\"\n        return self.create_context.project_anatomy\n\n    @property\n    def host(self) -&gt; IPublishHost:\n        return self.create_context.host\n\n    def get_group_label(self) -&gt; str:\n        \"\"\"Group label under which are instances grouped in UI.\n\n        Default implementation use attributes in this order:\n            - 'group_label' -&gt; 'label' -&gt; 'identifier'\n\n        Keep in mind that 'identifier' uses 'product_base_type' by default.\n\n        Returns:\n            str: Group label that can be used for grouping of instances in UI.\n                Group label can be overridden by the instance itself.\n\n        \"\"\"\n        if self._cached_group_label is None:\n            label = self.identifier\n            if self.group_label:\n                label = self.group_label\n            elif self.label:\n                label = self.label\n            self._cached_group_label = label\n        return self._cached_group_label\n\n    @property\n    def log(self) -&gt; logging.Logger:\n        \"\"\"Logger of the plugin.\n\n        Returns:\n            logging.Logger: Logger with name of the plugin.\n\n        \"\"\"\n        if self._log is None:\n            self._log = Logger.get_logger(self.__class__.__name__)\n        return self._log\n\n    def _create_instance(\n        self,\n        product_name: str,\n        data: dict[str, Any],\n        product_type: str | None = None,\n        product_base_type: str | None = None\n    ) -&gt; CreatedInstance:\n        \"\"\"Create instance and add instance to context.\n\n        Args:\n            product_name (str): Product name.\n            data (dict[str, Any]): Instance data.\n            product_type (str | None): Product type, object attribute\n                'product_type' is used if not passed.\n            product_base_type (str | None): Product base type, object\n                attribute 'product_base_type' is used if not passed.\n\n        Returns:\n            CreatedInstance: Created instance.\n\n        \"\"\"\n        if not product_base_type:\n            product_base_type = self.product_base_type\n\n        if not product_base_type:\n            product_base_type = self.product_type\n\n        if product_type is None:\n            product_type = product_base_type\n\n        instance = CreatedInstance(\n            product_type=product_type,\n            product_name=product_name,\n            data=data,\n            creator=self,\n            product_base_type=product_base_type,\n        )\n        self._add_instance_to_context(instance)\n        return instance\n\n    def _add_instance_to_context(\n        self, instance: CreatedInstance\n    ) -&gt; None:\n        \"\"\"Helper method to add instance to create context.\n\n        Instances should be stored to DCC workfile metadata to be able reload\n        them and also stored to CreateContext in which is creator plugin\n        existing at the moment to be able use it without refresh of\n        CreateContext.\n\n        Args:\n            instance (CreatedInstance): New created instance.\n\n        \"\"\"\n        self.create_context.creator_adds_instance(instance)\n\n    def _remove_instance_from_context(\n        self, instance: CreatedInstance\n    ) -&gt; None:\n        \"\"\"Helper method to remove instance from create context.\n\n        Instances must be removed from DCC workfile metadat aand from create\n        context in which plugin is existing at the moment of removal to\n        propagate the change without restarting create context.\n\n        Args:\n            instance (CreatedInstance): Instance which should be removed.\n\n        \"\"\"\n        self.create_context.creator_removed_instance(instance)\n\n    @abstractmethod\n    def create(self):\n        \"\"\"Trigger creation logic usually to create new instance/s.\"\"\"\n\n    @abstractmethod\n    def collect_instances(self) -&gt; None:\n        \"\"\"Collect existing instances related to this creator plugin.\n\n        The implementation differs on host abilities. The creator has to\n        collect metadata about instance and create 'CreatedInstance' object\n        which should be added to 'CreateContext'.\n\n        Example:\n        ```python\n        def collect_instances(self):\n            # Getting existing instances is different per host implementation\n            for instance_data in pipeline.list_instances():\n                # Process only instances that were created by this creator\n                creator_id = instance_data.get(\"creator_identifier\")\n                if creator_id == self.identifier:\n                    # Create instance object from existing data\n                    instance = CreatedInstance.from_existing(\n                        instance_data, self\n                    )\n                    # Add instance to create context\n                    self._add_instance_to_context(instance)\n        ```\n        \"\"\"\n\n    @abstractmethod\n    def update_instances(\n        self, update_list: list[UpdateData]\n    ) -&gt; None:\n        \"\"\"Store changes of existing instances so they can be recollected.\n\n        Args:\n            update_list (list[UpdateData]): Gets list of tuples. Each item\n                contain changed instance and it's changes.\n        \"\"\"\n\n    @abstractmethod\n    def remove_instances(\n        self, instances: list[CreatedInstance]\n    ) -&gt; None:\n        \"\"\"Method called on instance removal.\n\n        Can also remove instance metadata from context but should return\n        'True' if did so.\n\n        Args:\n            instances (list[CreatedInstance]): Instance objects which should be\n                removed.\n        \"\"\"\n\n    def get_icon(self) -&gt; IconBase | str | None:\n        \"\"\"Icon of creator (product base type).\n\n        Can return path to image file or awesome icon name.\n        \"\"\"\n        return self.icon\n\n    def get_dynamic_data(\n        self,\n        project_name: str,\n        folder_entity: dict[str, Any] | None,\n        task_entity: dict[str, Any] | None,\n        variant: str,\n        host_name: str,\n        instance: CreatedInstance | None = None,\n        project_entity: dict[str, Any] | None = None,\n        product_type: str | None = None,\n    ) -&gt; dict[str, Any]:\n        \"\"\"Dynamic data for product name filling.\n\n        These may be dynamically created based on current context of workfile.\n\n        Default implementation will always return empty dictionary.\n\n        \"\"\"\n        return {}\n\n    def get_product_name(\n        self,\n        project_name: str,\n        folder_entity: dict[str, Any],\n        task_entity: dict[str, Any] | None,\n        variant: str,\n        host_name: str | None = None,\n        instance: CreatedInstance | None = None,\n        project_entity: dict[str, Any] | None = None,\n        product_type: str | None = None,\n    ) -&gt; str:\n        \"\"\"Return product name for passed context.\n\n        Method is also called on product name update. In that case origin\n        instance is passed in.\n\n        Args:\n            project_name (str): Project name.\n            folder_entity (dict): Folder entity.\n            task_entity (dict | None): Task entity.\n            variant (str): Product name variant. In most of cases user input.\n            host_name (str | None): Which host creates product. Defaults\n                to host name on create context.\n            instance (CreatedInstance | None): Object of 'CreatedInstance'\n                for which is product name updated. Passed only on product name\n                update.\n            project_entity (dict[str, Any] | None): Project entity.\n            product_type (str | None): Product type.\n\n        Returns:\n            str: Product name for passed context.\n\n        \"\"\"\n        if host_name is None:\n            host_name = self.create_context.host_name\n\n        # Backwards compatibility for create plugins that don't implement\n        #   'product_base_type'.\n        # TODO Remove when 'product_base_type' is required\n        product_base_type = self.product_base_type\n        if not product_base_type:\n            product_base_type = self.product_type\n\n        if product_type is None:\n            for product_type_item in self.get_product_type_items():\n                product_type = product_type_item.product_type\n                break\n            else:\n                product_type = product_base_type\n\n        cur_project_name = self.create_context.get_current_project_name()\n        if not project_entity and project_name == cur_project_name:\n            project_entity = self.create_context.get_current_project_entity()\n\n        args = (\n            project_name,\n            folder_entity,\n            task_entity,\n            variant,\n            host_name,\n        )\n        kwargs = dict(\n            instance=instance,\n            project_entity=project_entity,\n            product_type=product_type,\n        )\n        # NOTE 'project_entity' and 'product_type' were added at the same time\n        #   26/01/19\n        if not is_func_signature_supported(\n            self.get_dynamic_data, *args, **kwargs\n        ):\n            kwargs.pop(\"project_entity\")\n            kwargs.pop(\"product_type\")\n\n        dynamic_data = self.get_dynamic_data(*args, **kwargs)\n\n        return get_product_name(\n            project_name,\n            folder_entity=folder_entity,\n            task_entity=task_entity,\n            product_base_type=product_base_type,\n            product_type=product_type,\n            host_name=host_name,\n            variant=variant,\n            dynamic_data=dynamic_data,\n            project_settings=self.project_settings,\n            project_entity=project_entity,\n        )\n\n    def get_instance_attr_defs(self) -&gt; list[AbstractAttrDef]:\n        \"\"\"Plugin attribute definitions.\n\n        Attribute definitions of plugin that hold data about created instance\n        and values are stored to metadata for future usage and for publishing\n        purposes.\n\n        Note:\n            Convert method should be implemented which should care about\n            updating keys/values when plugin attributes change.\n\n        Returns:\n            list[AbstractAttrDef]: Attribute definitions that can be tweaked\n                for created instance.\n\n        \"\"\"\n        return self.instance_attr_defs\n\n    def get_attr_defs_for_instance(\n        self, instance: CreatedInstance\n    ) -&gt; list[AbstractAttrDef]:\n        \"\"\"Get attribute definitions for an instance.\n\n        Args:\n            instance (CreatedInstance): Instance for which to get\n                attribute definitions.\n\n        \"\"\"\n        return self.get_instance_attr_defs()\n\n    @property\n    def collection_shared_data(self) -&gt; dict[str, Any]:\n        \"\"\"Access to shared data that can be used during creator's collection.\n\n        Returns:\n            dict[str, Any]: Shared data.\n\n        Raises:\n            UnavailableSharedData: When called out of collection phase.\n\n        \"\"\"\n        return self.create_context.collection_shared_data\n\n    def set_instance_thumbnail_path(\n        self,\n        instance_id: str,\n        thumbnail_path: str | None = None,\n    ) -&gt; None:\n        \"\"\"Set path to thumbnail for instance.\"\"\"\n\n        self.create_context.thumbnail_paths_by_instance_id[instance_id] = (\n            thumbnail_path\n        )\n\n    def get_next_versions_for_instances(\n        self, instances: list[CreatedInstance]\n    ) -&gt; dict[str, int]:\n        \"\"\"Prepare next versions for instances.\n\n        This is helper method to receive next possible versions for instances.\n        It is using context information on instance to receive them,\n        'folderPath' and 'product'.\n\n        Output will contain version by each instance id.\n\n        Args:\n            instances (list[CreatedInstance]): Instances for which to get next\n                versions.\n\n        Returns:\n            dict[str, int]: Next versions by instance id.\n        \"\"\"\n\n        return get_next_versions_for_instances(\n            self.create_context.project_name, instances\n        )\n\n    def get_product_type_items(self) -&gt; list[ProductTypeItem]:\n        \"\"\"Get product type the Creator can work with.\n\n        By default, it returns `product_type_items` attribute value that\n        can be set by Creator settings. This can be overridden to provide\n        different source.\n\n        Product type items are list of ProductTypeItem that\n        Creator can create. Label is used in UI to show user-friendly name.\n        This dataclass can be easily expanded with data in the future. New\n        fields must have default values to not break existing implementations.\n\n        Returns:\n            list[ProductTypeItem]: List of product type items.\n\n        \"\"\"\n        return self.product_type_items\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.BaseCreator.collection_shared_data","title":"<code>collection_shared_data</code>  <code>property</code>","text":"<p>Access to shared data that can be used during creator's collection.</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: Shared data.</p> <p>Raises:</p> Type Description <code>UnavailableSharedData</code> <p>When called out of collection phase.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.BaseCreator.identifier","title":"<code>identifier</code>  <code>property</code>","text":"<p>Identifier of creator (must be unique).</p> <p>Default implementation returns plugin's product base type, or falls back to product type if product base type is not set.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.BaseCreator.log","title":"<code>log</code>  <code>property</code>","text":"<p>Logger of the plugin.</p> <p>Returns:</p> Type Description <code>Logger</code> <p>logging.Logger: Logger with name of the plugin.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.BaseCreator.product_base_type","title":"<code>product_base_type</code>  <code>property</code>","text":"<p>Product base type that plugin represents.</p> <p>Todo (antirotor): This should be required in future - it     should be made abstract then.</p> <p>Returns:</p> Type Description <code>str | None</code> <p>str | None: Product base type that plugin represents. If not set, it is assumed that the creator plugin is obsolete and does not support product base type.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.BaseCreator.product_type","title":"<code>product_type</code>  <code>abstractmethod</code> <code>property</code>","text":"<p>Family that plugin represents.</p> <p>DEPRECATED - use 'product_base_type' instead. This is kept for     backwards compatibility and should be removed when     'product_base_type' is required.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.BaseCreator.project_anatomy","title":"<code>project_anatomy</code>  <code>property</code>","text":"<p>Current project anatomy.</p> <p>Returns:</p> Name Type Description <code>Anatomy</code> <code>Anatomy</code> <p>Project anatomy object.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.BaseCreator.project_name","title":"<code>project_name</code>  <code>property</code>","text":"<p>Current project name.</p> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Name of a project.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.BaseCreator.apply_settings","title":"<code>apply_settings(project_settings)</code>","text":"<p>Method called on initialization of plugin to apply settings.</p> <p>Default implementation tries to auto-apply settings values if are     in expected hierarchy.</p> Data hierarchy to auto-apply settings <p>\u251c\u2500 {self.settings_category}                 - Root key in settings \u2502 \u2514\u2500 \"create\"                               - Hardcoded key \u2502   \u2514\u2500 {self.settings_name} | {class name}  - Name of plugin \u2502     \u251c\u2500 ... attribute values...            - Attribute/value pair</p> <p>It is mandatory to define 'settings_category' attribute. Attribute 'settings_name' is optional and class name is used if is not defined.</p> Example data <p>ProjectSettings {     \"maya\": {                    # self.settings_category         \"create\": {              # Hardcoded key             \"CreateAnimation\": { # self.settings_name / class name                 \"enabled\": True, # --- Attributes to set ---                 \"optional\": True,#                 \"active\": True,  #                 \"fps\": 25,       # -------------------------             },             ...         },         ...     },     ... }</p> <p>Parameters:</p> Name Type Description Default <code>project_settings</code> <code>dict[str, Any]</code> <p>Project settings.</p> required Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def apply_settings(self, project_settings: dict[str, Any]) -&gt; None:\n    \"\"\"Method called on initialization of plugin to apply settings.\n\n    Default implementation tries to auto-apply settings values if are\n        in expected hierarchy.\n\n    Data hierarchy to auto-apply settings:\n        \u251c\u2500 {self.settings_category}                 - Root key in settings\n        \u2502 \u2514\u2500 \"create\"                               - Hardcoded key\n        \u2502   \u2514\u2500 {self.settings_name} | {class name}  - Name of plugin\n        \u2502     \u251c\u2500 ... attribute values...            - Attribute/value pair\n\n    It is mandatory to define 'settings_category' attribute. Attribute\n    'settings_name' is optional and class name is used if is not defined.\n\n    Example data:\n        ProjectSettings {\n            \"maya\": {                    # self.settings_category\n                \"create\": {              # Hardcoded key\n                    \"CreateAnimation\": { # self.settings_name / class name\n                        \"enabled\": True, # --- Attributes to set ---\n                        \"optional\": True,#\n                        \"active\": True,  #\n                        \"fps\": 25,       # -------------------------\n                    },\n                    ...\n                },\n                ...\n            },\n            ...\n        }\n\n    Args:\n        project_settings (dict[str, Any]): Project settings.\n    \"\"\"\n\n    settings_category = self.settings_category\n    if not settings_category:\n        return\n\n    cls_name = self.__class__.__name__\n    settings_name = self.settings_name or cls_name\n\n    settings = self._get_settings_values(\n        project_settings, settings_category, settings_name\n    )\n    if settings is None:\n        self.log.debug(f\"No settings found for {cls_name}\")\n        return\n\n    for key, value in settings.items():\n        # Log out attributes that are not defined on plugin object\n        # - those may be potential dangerous typos in settings\n        if not hasattr(self, key):\n            self.log.debug(\n                \"Applying settings to unknown attribute '%s' on '%s'.\",\n                key, cls_name\n            )\n        setattr(self, key, value)\n\n    self.product_type_items = self._convert_product_type_items(\n        self.product_type_items\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.BaseCreator.collect_instances","title":"<code>collect_instances()</code>  <code>abstractmethod</code>","text":"<p>Collect existing instances related to this creator plugin.</p> <p>The implementation differs on host abilities. The creator has to collect metadata about instance and create 'CreatedInstance' object which should be added to 'CreateContext'.</p> <p>Example:</p> <pre><code>def collect_instances(self):\n    # Getting existing instances is different per host implementation\n    for instance_data in pipeline.list_instances():\n        # Process only instances that were created by this creator\n        creator_id = instance_data.get(\"creator_identifier\")\n        if creator_id == self.identifier:\n            # Create instance object from existing data\n            instance = CreatedInstance.from_existing(\n                instance_data, self\n            )\n            # Add instance to create context\n            self._add_instance_to_context(instance)\n</code></pre> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>@abstractmethod\ndef collect_instances(self) -&gt; None:\n    \"\"\"Collect existing instances related to this creator plugin.\n\n    The implementation differs on host abilities. The creator has to\n    collect metadata about instance and create 'CreatedInstance' object\n    which should be added to 'CreateContext'.\n\n    Example:\n    ```python\n    def collect_instances(self):\n        # Getting existing instances is different per host implementation\n        for instance_data in pipeline.list_instances():\n            # Process only instances that were created by this creator\n            creator_id = instance_data.get(\"creator_identifier\")\n            if creator_id == self.identifier:\n                # Create instance object from existing data\n                instance = CreatedInstance.from_existing(\n                    instance_data, self\n                )\n                # Add instance to create context\n                self._add_instance_to_context(instance)\n    ```\n    \"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.BaseCreator.create","title":"<code>create()</code>  <code>abstractmethod</code>","text":"<p>Trigger creation logic usually to create new instance/s.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>@abstractmethod\ndef create(self):\n    \"\"\"Trigger creation logic usually to create new instance/s.\"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.BaseCreator.get_attr_defs_for_instance","title":"<code>get_attr_defs_for_instance(instance)</code>","text":"<p>Get attribute definitions for an instance.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>CreatedInstance</code> <p>Instance for which to get attribute definitions.</p> required Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def get_attr_defs_for_instance(\n    self, instance: CreatedInstance\n) -&gt; list[AbstractAttrDef]:\n    \"\"\"Get attribute definitions for an instance.\n\n    Args:\n        instance (CreatedInstance): Instance for which to get\n            attribute definitions.\n\n    \"\"\"\n    return self.get_instance_attr_defs()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.BaseCreator.get_dynamic_data","title":"<code>get_dynamic_data(project_name, folder_entity, task_entity, variant, host_name, instance=None, project_entity=None, product_type=None)</code>","text":"<p>Dynamic data for product name filling.</p> <p>These may be dynamically created based on current context of workfile.</p> <p>Default implementation will always return empty dictionary.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def get_dynamic_data(\n    self,\n    project_name: str,\n    folder_entity: dict[str, Any] | None,\n    task_entity: dict[str, Any] | None,\n    variant: str,\n    host_name: str,\n    instance: CreatedInstance | None = None,\n    project_entity: dict[str, Any] | None = None,\n    product_type: str | None = None,\n) -&gt; dict[str, Any]:\n    \"\"\"Dynamic data for product name filling.\n\n    These may be dynamically created based on current context of workfile.\n\n    Default implementation will always return empty dictionary.\n\n    \"\"\"\n    return {}\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.BaseCreator.get_group_label","title":"<code>get_group_label()</code>","text":"<p>Group label under which are instances grouped in UI.</p> Default implementation use attributes in this order <ul> <li>'group_label' -&gt; 'label' -&gt; 'identifier'</li> </ul> <p>Keep in mind that 'identifier' uses 'product_base_type' by default.</p> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Group label that can be used for grouping of instances in UI. Group label can be overridden by the instance itself.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def get_group_label(self) -&gt; str:\n    \"\"\"Group label under which are instances grouped in UI.\n\n    Default implementation use attributes in this order:\n        - 'group_label' -&gt; 'label' -&gt; 'identifier'\n\n    Keep in mind that 'identifier' uses 'product_base_type' by default.\n\n    Returns:\n        str: Group label that can be used for grouping of instances in UI.\n            Group label can be overridden by the instance itself.\n\n    \"\"\"\n    if self._cached_group_label is None:\n        label = self.identifier\n        if self.group_label:\n            label = self.group_label\n        elif self.label:\n            label = self.label\n        self._cached_group_label = label\n    return self._cached_group_label\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.BaseCreator.get_icon","title":"<code>get_icon()</code>","text":"<p>Icon of creator (product base type).</p> <p>Can return path to image file or awesome icon name.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def get_icon(self) -&gt; IconBase | str | None:\n    \"\"\"Icon of creator (product base type).\n\n    Can return path to image file or awesome icon name.\n    \"\"\"\n    return self.icon\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.BaseCreator.get_instance_attr_defs","title":"<code>get_instance_attr_defs()</code>","text":"<p>Plugin attribute definitions.</p> <p>Attribute definitions of plugin that hold data about created instance and values are stored to metadata for future usage and for publishing purposes.</p> Note <p>Convert method should be implemented which should care about updating keys/values when plugin attributes change.</p> <p>Returns:</p> Type Description <code>list[AbstractAttrDef]</code> <p>list[AbstractAttrDef]: Attribute definitions that can be tweaked for created instance.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def get_instance_attr_defs(self) -&gt; list[AbstractAttrDef]:\n    \"\"\"Plugin attribute definitions.\n\n    Attribute definitions of plugin that hold data about created instance\n    and values are stored to metadata for future usage and for publishing\n    purposes.\n\n    Note:\n        Convert method should be implemented which should care about\n        updating keys/values when plugin attributes change.\n\n    Returns:\n        list[AbstractAttrDef]: Attribute definitions that can be tweaked\n            for created instance.\n\n    \"\"\"\n    return self.instance_attr_defs\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.BaseCreator.get_next_versions_for_instances","title":"<code>get_next_versions_for_instances(instances)</code>","text":"<p>Prepare next versions for instances.</p> <p>This is helper method to receive next possible versions for instances. It is using context information on instance to receive them, 'folderPath' and 'product'.</p> <p>Output will contain version by each instance id.</p> <p>Parameters:</p> Name Type Description Default <code>instances</code> <code>list[CreatedInstance]</code> <p>Instances for which to get next versions.</p> required <p>Returns:</p> Type Description <code>dict[str, int]</code> <p>dict[str, int]: Next versions by instance id.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def get_next_versions_for_instances(\n    self, instances: list[CreatedInstance]\n) -&gt; dict[str, int]:\n    \"\"\"Prepare next versions for instances.\n\n    This is helper method to receive next possible versions for instances.\n    It is using context information on instance to receive them,\n    'folderPath' and 'product'.\n\n    Output will contain version by each instance id.\n\n    Args:\n        instances (list[CreatedInstance]): Instances for which to get next\n            versions.\n\n    Returns:\n        dict[str, int]: Next versions by instance id.\n    \"\"\"\n\n    return get_next_versions_for_instances(\n        self.create_context.project_name, instances\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.BaseCreator.get_product_name","title":"<code>get_product_name(project_name, folder_entity, task_entity, variant, host_name=None, instance=None, project_entity=None, product_type=None)</code>","text":"<p>Return product name for passed context.</p> <p>Method is also called on product name update. In that case origin instance is passed in.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>folder_entity</code> <code>dict</code> <p>Folder entity.</p> required <code>task_entity</code> <code>dict | None</code> <p>Task entity.</p> required <code>variant</code> <code>str</code> <p>Product name variant. In most of cases user input.</p> required <code>host_name</code> <code>str | None</code> <p>Which host creates product. Defaults to host name on create context.</p> <code>None</code> <code>instance</code> <code>CreatedInstance | None</code> <p>Object of 'CreatedInstance' for which is product name updated. Passed only on product name update.</p> <code>None</code> <code>project_entity</code> <code>dict[str, Any] | None</code> <p>Project entity.</p> <code>None</code> <code>product_type</code> <code>str | None</code> <p>Product type.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Product name for passed context.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def get_product_name(\n    self,\n    project_name: str,\n    folder_entity: dict[str, Any],\n    task_entity: dict[str, Any] | None,\n    variant: str,\n    host_name: str | None = None,\n    instance: CreatedInstance | None = None,\n    project_entity: dict[str, Any] | None = None,\n    product_type: str | None = None,\n) -&gt; str:\n    \"\"\"Return product name for passed context.\n\n    Method is also called on product name update. In that case origin\n    instance is passed in.\n\n    Args:\n        project_name (str): Project name.\n        folder_entity (dict): Folder entity.\n        task_entity (dict | None): Task entity.\n        variant (str): Product name variant. In most of cases user input.\n        host_name (str | None): Which host creates product. Defaults\n            to host name on create context.\n        instance (CreatedInstance | None): Object of 'CreatedInstance'\n            for which is product name updated. Passed only on product name\n            update.\n        project_entity (dict[str, Any] | None): Project entity.\n        product_type (str | None): Product type.\n\n    Returns:\n        str: Product name for passed context.\n\n    \"\"\"\n    if host_name is None:\n        host_name = self.create_context.host_name\n\n    # Backwards compatibility for create plugins that don't implement\n    #   'product_base_type'.\n    # TODO Remove when 'product_base_type' is required\n    product_base_type = self.product_base_type\n    if not product_base_type:\n        product_base_type = self.product_type\n\n    if product_type is None:\n        for product_type_item in self.get_product_type_items():\n            product_type = product_type_item.product_type\n            break\n        else:\n            product_type = product_base_type\n\n    cur_project_name = self.create_context.get_current_project_name()\n    if not project_entity and project_name == cur_project_name:\n        project_entity = self.create_context.get_current_project_entity()\n\n    args = (\n        project_name,\n        folder_entity,\n        task_entity,\n        variant,\n        host_name,\n    )\n    kwargs = dict(\n        instance=instance,\n        project_entity=project_entity,\n        product_type=product_type,\n    )\n    # NOTE 'project_entity' and 'product_type' were added at the same time\n    #   26/01/19\n    if not is_func_signature_supported(\n        self.get_dynamic_data, *args, **kwargs\n    ):\n        kwargs.pop(\"project_entity\")\n        kwargs.pop(\"product_type\")\n\n    dynamic_data = self.get_dynamic_data(*args, **kwargs)\n\n    return get_product_name(\n        project_name,\n        folder_entity=folder_entity,\n        task_entity=task_entity,\n        product_base_type=product_base_type,\n        product_type=product_type,\n        host_name=host_name,\n        variant=variant,\n        dynamic_data=dynamic_data,\n        project_settings=self.project_settings,\n        project_entity=project_entity,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.BaseCreator.get_product_type_items","title":"<code>get_product_type_items()</code>","text":"<p>Get product type the Creator can work with.</p> <p>By default, it returns <code>product_type_items</code> attribute value that can be set by Creator settings. This can be overridden to provide different source.</p> <p>Product type items are list of ProductTypeItem that Creator can create. Label is used in UI to show user-friendly name. This dataclass can be easily expanded with data in the future. New fields must have default values to not break existing implementations.</p> <p>Returns:</p> Type Description <code>list[ProductTypeItem]</code> <p>list[ProductTypeItem]: List of product type items.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def get_product_type_items(self) -&gt; list[ProductTypeItem]:\n    \"\"\"Get product type the Creator can work with.\n\n    By default, it returns `product_type_items` attribute value that\n    can be set by Creator settings. This can be overridden to provide\n    different source.\n\n    Product type items are list of ProductTypeItem that\n    Creator can create. Label is used in UI to show user-friendly name.\n    This dataclass can be easily expanded with data in the future. New\n    fields must have default values to not break existing implementations.\n\n    Returns:\n        list[ProductTypeItem]: List of product type items.\n\n    \"\"\"\n    return self.product_type_items\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.BaseCreator.register_callbacks","title":"<code>register_callbacks()</code>","text":"<p>Register callbacks for creator.</p> <p>Default implementation does nothing. It can be overridden to register callbacks for creator.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def register_callbacks(self) -&gt; None:\n    \"\"\"Register callbacks for creator.\n\n    Default implementation does nothing. It can be overridden to register\n    callbacks for creator.\n    \"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.BaseCreator.remove_instances","title":"<code>remove_instances(instances)</code>  <code>abstractmethod</code>","text":"<p>Method called on instance removal.</p> <p>Can also remove instance metadata from context but should return 'True' if did so.</p> <p>Parameters:</p> Name Type Description Default <code>instances</code> <code>list[CreatedInstance]</code> <p>Instance objects which should be removed.</p> required Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>@abstractmethod\ndef remove_instances(\n    self, instances: list[CreatedInstance]\n) -&gt; None:\n    \"\"\"Method called on instance removal.\n\n    Can also remove instance metadata from context but should return\n    'True' if did so.\n\n    Args:\n        instances (list[CreatedInstance]): Instance objects which should be\n            removed.\n    \"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.BaseCreator.set_instance_thumbnail_path","title":"<code>set_instance_thumbnail_path(instance_id, thumbnail_path=None)</code>","text":"<p>Set path to thumbnail for instance.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def set_instance_thumbnail_path(\n    self,\n    instance_id: str,\n    thumbnail_path: str | None = None,\n) -&gt; None:\n    \"\"\"Set path to thumbnail for instance.\"\"\"\n\n    self.create_context.thumbnail_paths_by_instance_id[instance_id] = (\n        thumbnail_path\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.BaseCreator.update_instances","title":"<code>update_instances(update_list)</code>  <code>abstractmethod</code>","text":"<p>Store changes of existing instances so they can be recollected.</p> <p>Parameters:</p> Name Type Description Default <code>update_list</code> <code>list[UpdateData]</code> <p>Gets list of tuples. Each item contain changed instance and it's changes.</p> required Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>@abstractmethod\ndef update_instances(\n    self, update_list: list[UpdateData]\n) -&gt; None:\n    \"\"\"Store changes of existing instances so they can be recollected.\n\n    Args:\n        update_list (list[UpdateData]): Gets list of tuples. Each item\n            contain changed instance and it's changes.\n    \"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.ButtonCallbackInfo","title":"<code>ButtonCallbackInfo</code>  <code>dataclass</code>","text":"<p>Button callback info passed to button definition callback.</p> Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>@dataclass\nclass ButtonCallbackInfo:\n    \"\"\"Button callback info passed to button definition callback.\"\"\"\n    create_context: \"CreateContext\"\n    instance_ids: list[str | None]\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.ConvertorItem","title":"<code>ConvertorItem</code>","text":"<p>Item representing convertor plugin.</p> <p>Parameters:</p> Name Type Description Default <code>identifier</code> <code>str</code> <p>Identifier of convertor.</p> required <code>label</code> <code>str</code> <p>Label which will be shown in UI.</p> required Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>class ConvertorItem:\n    \"\"\"Item representing convertor plugin.\n\n    Args:\n        identifier (str): Identifier of convertor.\n        label (str): Label which will be shown in UI.\n    \"\"\"\n\n    def __init__(self, identifier, label):\n        self._id = str(uuid4())\n        self.identifier = identifier\n        self.label = label\n\n    @property\n    def id(self):\n        return self._id\n\n    def to_data(self):\n        return {\n            \"id\": self.id,\n            \"identifier\": self.identifier,\n            \"label\": self.label\n        }\n\n    @classmethod\n    def from_data(cls, data):\n        obj = cls(data[\"identifier\"], data[\"label\"])\n        obj._id = data[\"id\"]\n        return obj\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext","title":"<code>CreateContext</code>","text":"<p>Context of instance creation.</p> <p>Context itself also can store data related to whole creation (workfile). - those are mainly for Context publish plugins</p> <p>Parameters:</p> Name Type Description Default <code>host</code> <code>IPublishHost</code> <p>Host implementation which handles implementation and global metadata.</p> required <code>headless</code> <code>bool</code> <p>Context is created out of UI (Current not used).</p> <code>False</code> <code>reset</code> <code>bool</code> <p>Reset context on initialization.</p> <code>True</code> <code>discover_publish_plugins</code> <code>bool</code> <p>Discover publish plugins during reset phase.</p> <code>True</code> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>class CreateContext:\n    \"\"\"Context of instance creation.\n\n    Context itself also can store data related to whole creation (workfile).\n    - those are mainly for Context publish plugins\n\n    Args:\n        host (IPublishHost): Host implementation which handles implementation\n            and global metadata.\n        headless (bool): Context is created out of UI (Current not used).\n        reset (bool): Reset context on initialization.\n        discover_publish_plugins (bool): Discover publish plugins during reset\n            phase.\n    \"\"\"\n\n    def __init__(\n        self,\n        host: IPublishHost,\n        headless: bool = False,\n        reset: bool = True,\n        discover_publish_plugins: bool = True,\n    ):\n        self.host = host\n\n        # Prepare attribute for logger (Created on demand in `log` property)\n        self._log = None\n        self._event_hub = QueuedEventSystem()\n\n        # Publish context plugins attributes and it's values\n        self._publish_attributes = PublishAttributes(self, {})\n        self._original_context_data = {}\n\n        # Validate host implementation\n        # - defines if context is capable of handling context data\n        host_is_valid = True\n        missing_methods = self.get_host_misssing_methods(host)\n        if missing_methods:\n            host_is_valid = False\n            joined_methods = \", \".join(\n                ['\"{}\"'.format(name) for name in missing_methods]\n            )\n            self.log.warning((\n                \"Host miss required methods to be able use creation.\"\n                \" Missing methods: {}\"\n            ).format(joined_methods))\n\n        self._current_project_name = None\n        self._current_folder_path = None\n        self._current_task_name = None\n        self._current_workfile_path = None\n        self._current_project_settings = None\n\n        self._current_project_entity = _NOT_SET\n        self._current_folder_entity = _NOT_SET\n        self._current_task_entity = _NOT_SET\n        self._current_task_type = _NOT_SET\n\n        self._current_project_anatomy = None\n\n        self._host_is_valid = host_is_valid\n        # Currently unused variable\n        self.headless = headless\n\n        # Instances by their ID\n        self._instances_by_id = {}\n\n        self.creator_discover_result = None\n        self.convertor_discover_result = None\n        # Discovered creators\n        self.creators: dict[str, \"BaseCreator\"] = {}\n        # Prepare categories of creators\n        self.autocreators = {}\n        # Manual creators\n        self.manual_creators = {}\n        # Creators that are disabled\n        self.disabled_creators = {}\n\n        self.convertors_plugins = {}\n        self.convertor_items_by_id = {}\n\n        self.publish_discover_result: Optional[DiscoverResult] = None\n        self.publish_plugins_mismatch_targets = []\n        self.publish_plugins = []\n        self.plugins_with_defs = []\n\n        # Helpers for validating context of collected instances\n        #   - they can be validation for multiple instances at one time\n        #       using context manager which will trigger validation\n        #       after leaving of last context manager scope\n        self._bulk_info = {\n            # Added instances\n            \"add\": BulkInfo(),\n            # Removed instances\n            \"remove\": BulkInfo(),\n            # Change values of instances or create context\n            \"change\": BulkInfo(),\n            # Pre create attribute definitions changed\n            \"pre_create_attrs_change\": BulkInfo(),\n            # Create attribute definitions changed\n            \"create_attrs_change\": BulkInfo(),\n            # Publish attribute definitions changed\n            \"publish_attrs_change\": BulkInfo(),\n            # Instance requirement changed\n            # - right now used only for 'mandatory' but can be extended\n            #   in future\n            \"requirement_change\": BulkInfo(),\n            # Instance parent changed\n            \"parent_change\": BulkInfo(),\n        }\n        self._bulk_order = []\n\n        # Shared data across creators during collection phase\n        self._collection_shared_data = None\n\n        # Entities cache\n        self._folder_entities_by_path = {}\n        self._task_entities_by_id = {}\n        self._task_ids_by_folder_path = {}\n        self._task_names_by_folder_path = {}\n\n        self.thumbnail_paths_by_instance_id = {}\n\n        # Trigger reset if was enabled\n        if reset:\n            self.reset(discover_publish_plugins)\n\n    @property\n    def instances(self) -&gt; Iterable[\"CreatedInstance\"]:\n        return self._instances_by_id.values()\n\n    @property\n    def instances_by_id(self) -&gt; dict[str, \"CreatedInstance\"]:\n        return self._instances_by_id\n\n    @property\n    def publish_attributes(self) -&gt; PublishAttributes:\n        \"\"\"Access to global publish attributes.\"\"\"\n        return self._publish_attributes\n\n    def get_instance_by_id(\n        self, instance_id: str\n    ) -&gt; Optional[\"CreatedInstance\"]:\n        \"\"\"Receive instance by id.\n\n        Args:\n            instance_id (str): Instance id.\n\n        Returns:\n            Optional[CreatedInstance]: Instance or None if instance with\n                given id is not available.\n\n        \"\"\"\n        return self._instances_by_id.get(instance_id)\n\n    def get_sorted_creators(\n        self, identifiers: Optional[Iterable[str]] = None\n    ) -&gt; list[\"BaseCreator\"]:\n        \"\"\"Sorted creators by 'order' attribute.\n\n        Args:\n            identifiers (Optional[Iterable[str]]): Filter creators by\n                identifiers. All creators are returned if 'None' is passed.\n\n        Returns:\n            list[BaseCreator]: Sorted creator plugins by 'order' value.\n\n        \"\"\"\n        if identifiers is not None:\n            identifiers = set(identifiers)\n            creators = [\n                creator\n                for identifier, creator in self.creators.items()\n                if identifier in identifiers\n            ]\n        else:\n            creators = self.creators.values()\n\n        return sorted(\n            creators, key=lambda creator: creator.order\n        )\n\n    @property\n    def sorted_creators(self) -&gt; list[\"BaseCreator\"]:\n        \"\"\"Sorted creators by 'order' attribute.\n\n        Returns:\n            list[BaseCreator]: Sorted creator plugins by 'order' value.\n        \"\"\"\n\n        return self.get_sorted_creators()\n\n    @property\n    def sorted_autocreators(self) -&gt; list[\"AutoCreator\"]:\n        \"\"\"Sorted auto-creators by 'order' attribute.\n\n        Returns:\n            list[AutoCreator]: Sorted plugins by 'order' value.\n        \"\"\"\n\n        return sorted(\n            self.autocreators.values(), key=lambda creator: creator.order\n        )\n\n    @classmethod\n    def get_host_misssing_methods(cls, host):\n        \"\"\"Collect missing methods from host.\n\n        Args:\n            host(ModuleType): Host implementaion.\n        \"\"\"\n\n        missing = set(\n            IPublishHost.get_missing_publish_methods(host)\n        )\n        return missing\n\n    @property\n    def host_is_valid(self):\n        \"\"\"Is host valid for creation.\"\"\"\n        return self._host_is_valid\n\n    @property\n    def host_name(self) -&gt; str:\n        if hasattr(self.host, \"name\"):\n            return self.host.name\n        return os.environ[\"AYON_HOST_NAME\"]\n\n    def get_current_project_name(self) -&gt; str:\n        \"\"\"Project name which was used as current context on context reset.\n\n        Returns:\n            Optional[str]: Project name.\n\n        \"\"\"\n        return self._current_project_name\n\n    def get_current_folder_path(self) -&gt; Optional[str]:\n        \"\"\"Folder path which was used as current context on context reset.\n\n        Returns:\n            Optional[str]: Folder path.\n\n        \"\"\"\n        return self._current_folder_path\n\n    def get_current_task_name(self) -&gt; Optional[str]:\n        \"\"\"Task name which was used as current context on context reset.\n\n        Returns:\n            Optional[str]: Task name.\n\n        \"\"\"\n        return self._current_task_name\n\n    def get_current_task_type(self) -&gt; Optional[str]:\n        \"\"\"Task type which was used as current context on context reset.\n\n        Returns:\n            Optional[str]: Task type.\n\n        \"\"\"\n        if self._current_task_type is _NOT_SET:\n            task_type = None\n            task_entity = self.get_current_task_entity()\n            if task_entity:\n                task_type = task_entity[\"taskType\"]\n            self._current_task_type = task_type\n        return self._current_task_type\n\n    def get_current_project_entity(self) -&gt; Optional[dict[str, Any]]:\n        \"\"\"Project entity for current context project.\n\n        Returns:\n            Optional[dict[str, Any]]: Folder entity.\n\n        \"\"\"\n        if self._current_project_entity is not _NOT_SET:\n            return copy.deepcopy(self._current_project_entity)\n        project_entity = None\n        project_name = self.get_current_project_name()\n        if project_name:\n            project_entity = ayon_api.get_project(project_name)\n        self._current_project_entity = project_entity\n        return copy.deepcopy(self._current_project_entity)\n\n    def get_current_folder_entity(self) -&gt; Optional[dict[str, Any]]:\n        \"\"\"Folder entity for current context folder.\n\n        Returns:\n            Optional[dict[str, Any]]: Folder entity.\n\n        \"\"\"\n        if self._current_folder_entity is not _NOT_SET:\n            return copy.deepcopy(self._current_folder_entity)\n\n        folder_path = self.get_current_folder_path()\n        self._current_folder_entity = self.get_folder_entity(folder_path)\n        return copy.deepcopy(self._current_folder_entity)\n\n    def get_current_task_entity(self) -&gt; Optional[dict[str, Any]]:\n        \"\"\"Task entity for current context task.\n\n        Returns:\n            Optional[dict[str, Any]]: Task entity.\n\n        \"\"\"\n        if self._current_task_entity is not _NOT_SET:\n            return copy.deepcopy(self._current_task_entity)\n\n        folder_path = self.get_current_folder_path()\n        task_name = self.get_current_task_name()\n        self._current_task_entity = self.get_task_entity(\n            folder_path, task_name\n        )\n        return copy.deepcopy(self._current_task_entity)\n\n    def get_current_workfile_path(self) -&gt; Optional[str]:\n        \"\"\"Workfile path which was opened on context reset.\n\n        Returns:\n            Optional[str]: Workfile path.\n\n        \"\"\"\n        return self._current_workfile_path\n\n    def get_current_project_anatomy(self) -&gt; Anatomy:\n        \"\"\"Project anatomy for current project.\n\n        Returns:\n            Anatomy: Anatomy object ready to be used.\n        \"\"\"\n\n        if self._current_project_anatomy is None:\n            self._current_project_anatomy = Anatomy(\n                self._current_project_name)\n        return self._current_project_anatomy\n\n    def get_current_project_settings(self) -&gt; dict[str, Any]:\n        if self._current_project_settings is None:\n            self._current_project_settings = get_project_settings(\n                self.get_current_project_name())\n        return self._current_project_settings\n\n    def get_template_data(\n        self, folder_path: Optional[str], task_name: Optional[str]\n    ) -&gt; dict[str, Any]:\n        \"\"\"Prepare template data for given context.\n\n        Method is using cached entities and settings to prepare template data.\n\n        Args:\n            folder_path (Optional[str]): Folder path.\n            task_name (Optional[str]): Task name.\n\n        Returns:\n            dict[str, Any]: Template data.\n\n        \"\"\"\n        project_entity = self.get_current_project_entity()\n        folder_entity = task_entity = None\n        if folder_path:\n            folder_entity = self.get_folder_entity(folder_path)\n            if task_name and folder_entity:\n                task_entity = self.get_task_entity(folder_path, task_name)\n\n        return get_template_data(\n            project_entity,\n            folder_entity,\n            task_entity,\n            host_name=self.host_name,\n            settings=self.get_current_project_settings(),\n        )\n\n    @property\n    def context_has_changed(self) -&gt; bool:\n        \"\"\"Host context has changed.\n\n        As context is used project, folder, task name and workfile path if\n        host does support workfiles.\n\n        Returns:\n            bool: Context changed.\n\n        \"\"\"\n        project_name, folder_path, task_name, workfile_path = (\n            self._get_current_host_context()\n        )\n        return (\n            self._current_project_name != project_name\n            or self._current_folder_path != folder_path\n            or self._current_task_name != task_name\n            or self._current_workfile_path != workfile_path\n        )\n\n    project_name: str = property(get_current_project_name)\n    project_anatomy: Anatomy = property(get_current_project_anatomy)\n\n    @property\n    def log(self) -&gt; logging.Logger:\n        \"\"\"Dynamic access to logger.\"\"\"\n        if self._log is None:\n            self._log = logging.getLogger(self.__class__.__name__)\n        return self._log\n\n    def reset(self, discover_publish_plugins: bool = True) -&gt; None:\n        \"\"\"Reset context with all plugins and instances.\n\n        All changes will be lost if were not saved explicitely.\n        \"\"\"\n\n        self.reset_preparation()\n\n        self.reset_current_context()\n        self.reset_plugins(discover_publish_plugins)\n        self.reset_context_data()\n\n        with self.bulk_add_instances():\n            self.reset_instances()\n            self.find_convertor_items()\n            self.execute_autocreators()\n\n        self.reset_finalization()\n\n    def refresh_thumbnails(self) -&gt; None:\n        \"\"\"Cleanup thumbnail paths.\n\n        Remove all thumbnail filepaths that are empty or lead to files which\n        does not exist or of instances that are not available anymore.\n        \"\"\"\n\n        invalid = set()\n        for instance_id, path in self.thumbnail_paths_by_instance_id.items():\n            instance_available = True\n            if instance_id is not None:\n                instance_available = instance_id in self._instances_by_id\n\n            if (\n                not instance_available\n                or not path\n                or not os.path.exists(path)\n            ):\n                invalid.add(instance_id)\n\n        for instance_id in invalid:\n            self.thumbnail_paths_by_instance_id.pop(instance_id)\n\n    def reset_preparation(self) -&gt; None:\n        \"\"\"Prepare attributes that must be prepared/cleaned before reset.\"\"\"\n\n        # Give ability to store shared data for collection phase\n        self._collection_shared_data = {}\n\n        self._folder_entities_by_path = {}\n        self._task_entities_by_id = {}\n\n        self._task_ids_by_folder_path = {}\n        self._task_names_by_folder_path = {}\n\n        self._event_hub.clear_callbacks()\n\n    def reset_finalization(self) -&gt; None:\n        \"\"\"Cleanup of attributes after reset.\"\"\"\n\n        # Stop access to collection shared data\n        self._collection_shared_data = None\n        self.refresh_thumbnails()\n\n    def _get_current_host_context(\n        self\n    ) -&gt; tuple[str, Optional[str], Optional[str], Optional[str]]:\n        project_name = folder_path = task_name = workfile_path = None\n        if hasattr(self.host, \"get_current_context\"):\n            host_context = self.host.get_current_context()\n            if host_context:\n                project_name = host_context.get(\"project_name\")\n                folder_path = host_context.get(\"folder_path\")\n                task_name = host_context.get(\"task_name\")\n\n        if isinstance(self.host, IWorkfileHost):\n            workfile_path = self.host.get_current_workfile()\n\n        return project_name, folder_path, task_name, workfile_path\n\n    def reset_current_context(self) -&gt; None:\n        \"\"\"Refresh current context.\n\n        Reset is based on optional host implementation of `get_current_context`\n        function.\n\n        Some hosts have ability to change context file without using workfiles\n        tool but that change is not propagated to 'os.environ'.\n\n        Todos:\n            UI: Current context should be also checked on save - compare\n                initial values vs. current values.\n            Related to UI checks: Current workfile can be also considered\n                as current context information as that's where the metadata\n                are stored. We should store the workfile (if is available) too.\n        \"\"\"\n\n        project_name, folder_path, task_name, workfile_path = (\n            self._get_current_host_context()\n        )\n\n        self._current_project_name = project_name\n        self._current_folder_path = folder_path\n        self._current_task_name = task_name\n        self._current_workfile_path = workfile_path\n\n        self._current_project_entity = _NOT_SET\n        self._current_folder_entity = _NOT_SET\n        self._current_task_entity = _NOT_SET\n        self._current_task_type = _NOT_SET\n\n        self._current_project_anatomy = None\n        self._current_project_settings = None\n\n    def reset_plugins(self, discover_publish_plugins: bool = True) -&gt; None:\n        \"\"\"Reload plugins.\n\n        Reloads creators from preregistered paths and can load publish plugins\n        if it's enabled on context.\n        \"\"\"\n\n        self._reset_publish_plugins(discover_publish_plugins)\n        self._reset_creator_plugins()\n        self._reset_convertor_plugins()\n\n    def _reset_publish_plugins(self, discover_publish_plugins: bool) -&gt; None:\n        from ayon_core.pipeline import AYONPyblishPluginMixin\n        from ayon_core.pipeline.publish import (\n            publish_plugins_discover\n        )\n\n        discover_result = DiscoverResult(pyblish.api.Plugin)\n        plugins_with_defs = []\n        plugins_by_targets = []\n        plugins_mismatch_targets = []\n        if discover_publish_plugins:\n            discover_result = publish_plugins_discover()\n            publish_plugins = discover_result.plugins\n\n            targets = set(pyblish.logic.registered_targets())\n            targets.add(\"default\")\n            plugins_by_targets = pyblish.logic.plugins_by_targets(\n                publish_plugins, list(targets)\n            )\n\n            # Collect plugins that can have attribute definitions\n            for plugin in publish_plugins:\n                if AYONPyblishPluginMixin in inspect.getmro(plugin):\n                    plugins_with_defs.append(plugin)\n\n            plugins_mismatch_targets = [\n                plugin\n                for plugin in publish_plugins\n                if plugin not in plugins_by_targets\n            ]\n\n        # Register create context callbacks\n        for plugin in plugins_with_defs:\n            if not inspect.ismethod(plugin.register_create_context_callbacks):\n                self.log.warning(\n                    f\"Plugin {plugin.__name__} does not have\"\n                    f\" 'register_create_context_callbacks'\"\n                    f\" defined as class method.\"\n                )\n                continue\n            try:\n                plugin.register_create_context_callbacks(self)\n            except Exception:\n                self.log.error(\n                    f\"Failed to register callbacks for plugin\"\n                    f\" {plugin.__name__}.\",\n                    exc_info=True\n                )\n\n        self.publish_plugins_mismatch_targets = plugins_mismatch_targets\n        self.publish_discover_result = discover_result\n        self.publish_plugins = plugins_by_targets\n        self.plugins_with_defs = plugins_with_defs\n\n    def _reset_creator_plugins(self) -&gt; None:\n        # Prepare settings\n        project_settings = self.get_current_project_settings()\n\n        # Discover and prepare creators\n        creators = {}\n        disabled_creators = {}\n        autocreators = {}\n        manual_creators = {}\n        report = discover_creator_plugins(return_report=True)\n        self.creator_discover_result = report\n        for creator_class in report.abstract_plugins:\n            self.log.debug(\n                \"Skipping abstract Creator '%s'\",\n                str(creator_class)\n            )\n\n        for creator_class in report.plugins:\n            creator_identifier = creator_class.identifier\n            if creator_identifier in creators:\n                self.log.warning(\n                    \"Duplicate Creator identifier: '%s'. Using first Creator \"\n                    \"and skipping: %s\", creator_identifier, creator_class\n                )\n                continue\n\n            # Filter by host name\n            if (\n                creator_class.host_name\n                and creator_class.host_name != self.host_name\n            ):\n                self.log.info(\n                    (\n                        'Creator\\'s host name \"{}\"'\n                        ' is not supported for current host \"{}\"'\n                    ).format(creator_class.host_name, self.host_name)\n                )\n                continue\n\n            # TODO report initialization error\n            try:\n                creator = creator_class(project_settings, self, self.headless)\n            except Exception:\n                self.log.error(\n                    f\"Failed to initialize plugin: {creator_class}\",\n                    exc_info=True\n                )\n                continue\n\n            if not creator.product_base_type:\n                message = (\n                    f\"Provided creator {creator!r} doesn't have \"\n                    \"product base type attribute defined. This will be \"\n                    \"required in future.\"\n                )\n                warn(\n                    message,\n                    DeprecationWarning,\n                    stacklevel=2\n                )\n                self.log.warning(message)\n\n            if not creator.enabled:\n                disabled_creators[creator_identifier] = creator\n                continue\n            creators[creator_identifier] = creator\n            if isinstance(creator, AutoCreator):\n                autocreators[creator_identifier] = creator\n            elif isinstance(creator, Creator):\n                manual_creators[creator_identifier] = creator\n\n        self.autocreators = autocreators\n        self.manual_creators = manual_creators\n\n        self.creators = creators\n        self.disabled_creators = disabled_creators\n\n    def _reset_convertor_plugins(self) -&gt; None:\n        convertors_plugins = {}\n        report = discover_convertor_plugins(return_report=True)\n        self.convertor_discover_result = report\n        for convertor_class in report.plugins:\n            if inspect.isabstract(convertor_class):\n                self.log.info(\n                    \"Skipping abstract Creator {}\".format(str(convertor_class))\n                )\n                continue\n\n            convertor_identifier = convertor_class.identifier\n            if convertor_identifier in convertors_plugins:\n                self.log.warning((\n                    \"Duplicated Converter identifier. \"\n                    \"Using first and skipping following\"\n                ))\n                continue\n\n            convertors_plugins[convertor_identifier] = convertor_class(self)\n\n        self.convertors_plugins = convertors_plugins\n\n    def reset_context_data(self) -&gt; None:\n        \"\"\"Reload context data using host implementation.\n\n        These data are not related to any instance but may be needed for whole\n        publishing.\n        \"\"\"\n        if not self.host_is_valid:\n            self._original_context_data = {}\n            self._publish_attributes = PublishAttributes(self, {})\n            return\n\n        original_data = self.host.get_context_data() or {}\n        self._original_context_data = copy.deepcopy(original_data)\n\n        publish_attributes = original_data.get(\"publish_attributes\") or {}\n\n        self._publish_attributes = PublishAttributes(\n            self, publish_attributes\n        )\n\n        for plugin in self.plugins_with_defs:\n            if is_func_signature_supported(\n                plugin.convert_attribute_values, self, None\n            ):\n                plugin.convert_attribute_values(self, None)\n\n            elif not plugin.__instanceEnabled__:\n                output = plugin.convert_attribute_values(publish_attributes)\n                if output:\n                    publish_attributes.update(output)\n\n        for plugin in self.plugins_with_defs:\n            try:\n                attr_defs = plugin.get_attr_defs_for_context(self)\n            except Exception:\n                self.log.error(\n                    \"Failed to get attribute definitions\"\n                    f\" from plugin '{plugin.__name__}'.\",\n                    exc_info=True\n                )\n                continue\n\n            if not attr_defs:\n                continue\n            self._publish_attributes.set_publish_plugin_attr_defs(\n                plugin.__name__, attr_defs\n            )\n\n    def trigger_pre_create_button_callback(\n        self, identifier: str, button_name: str\n    ) -&gt; None:\n        \"\"\"Trigger pre-create button callback.\n\n        Args:\n            identifier (str): Creator identifier.\n            button_name (str): Button name.\n\n        Raises:\n            ValueError if create plugin was not found, does not support\n                pre-create attributes, or button was not found.\n\n        \"\"\"\n        create_plugin = self.creators.get(identifier)\n        if create_plugin is None:\n            return\n\n        if not hasattr(create_plugin, \"get_pre_create_attr_defs\"):\n            return\n\n        pre_create_attributes = create_plugin.get_pre_create_attr_defs()\n        for attr_def in pre_create_attributes:\n            if attr_def.key == button_name:\n                attr_def.trigger()\n                return\n\n    def trigger_create_button_callback(\n        self,\n        button_name: str,\n        instance_ids: list[str],\n        *,\n        identifier: str | None = None,\n    ) -&gt; None:\n        \"\"\"Trigger pre-create button callback.\n\n        Args:\n            button_name (str): Button name.\n            instance_ids (list[str]): List of instance ids for which\n                the button will be triggered. The button will be triggered\n                only for instances that have the button.\n            identifier (str | None): Creator identifier. If None,\n                the button will be triggered for all creators.\n\n        \"\"\"\n        info_by_identifier = {}\n        for instance_id in instance_ids:\n            instance = self.instances_by_id.get(instance_id)\n            if instance is None:\n                continue\n\n            plugin_id = instance.creator_identifier\n            if identifier is not None and plugin_id != identifier:\n                continue\n\n            attr_def = instance.creator_attributes.get_attr_def(button_name)\n            if not isinstance(attr_def, ButtonDef):\n                continue\n\n            info = info_by_identifier.get(plugin_id)\n            if info is None:\n                info = {\n                    \"attr_def\": attr_def,\n                    \"instance_ids\": []\n                }\n                info_by_identifier[plugin_id] = info\n\n            info[\"instance_ids\"].append(instance_id)\n\n        for info in info_by_identifier.values():\n            attr_def = info[\"attr_def\"]\n\n            callback_info = ButtonCallbackInfo(\n                create_context=self,\n                instance_ids=info[\"instance_ids\"],\n            )\n            callback = attr_def.get_callback()\n            if is_func_signature_supported(callback, callback_info):\n                callback(callback_info)\n            else:\n                callback()\n\n    def trigger_publish_button_callback(\n        self,\n        plugin_name: str,\n        button_name: str,\n        instance_ids: list[str | None],\n    ) -&gt; None:\n        \"\"\"Trigger the publish plugin button callback.\n\n        Args:\n            plugin_name (str): Plugin name.\n            button_name (str): Button name.\n            instance_ids (list[str | None]): List of instance ids for which\n                the button will be triggered. The button will be triggered\n                only for instances that have the button. 'None' is used for\n                context selection.\n\n        \"\"\"\n        filtered_instance_ids = []\n        attr_def = None\n        for instance_id in instance_ids:\n            if instance_id is None:\n                publish_attributes = self.publish_attributes\n            else:\n                instance = self.get_instance_by_id(instance_id)\n                if instance is None:\n                    continue\n                publish_attributes = instance.publish_attributes\n\n            plugin_attributes = publish_attributes.get(plugin_name)\n            if not plugin_attributes:\n                continue\n\n            _attr_def = plugin_attributes.get_attr_def(button_name)\n            if not isinstance(_attr_def, ButtonDef):\n                continue\n\n            if attr_def is None:\n                attr_def = _attr_def\n\n            filtered_instance_ids.append(instance_id)\n\n        if attr_def is None:\n            return\n\n        info = ButtonCallbackInfo(\n            create_context=self,\n            instance_ids=filtered_instance_ids,\n        )\n        callback = attr_def.get_callback()\n        if is_func_signature_supported(callback, info):\n            callback(info)\n        else:\n            callback()\n\n    def add_instances_added_callback(\n        self, callback: Callable\n    ) -&gt; \"EventCallback\":\n        \"\"\"Register callback for added instances.\n\n        Event is triggered when instances are already available in context\n            and have set create/publish attribute definitions.\n\n        Data structure of event:\n\n            ```python\n            {\n                \"instances\": [CreatedInstance, ...],\n                \"create_context\": CreateContext\n            }\n            ```\n\n        Args:\n            callback (Callable): Callback function that will be called when\n                instances are added to context.\n\n        Returns:\n            EventCallback: Created callback object which can be used to\n                stop listening.\n\n        \"\"\"\n        return self._event_hub.add_callback(INSTANCE_ADDED_TOPIC, callback)\n\n    def add_instances_removed_callback(\n        self, callback: Callable\n    ) -&gt; \"EventCallback\":\n        \"\"\"Register callback for removed instances.\n\n        Event is triggered when instances are already removed from context.\n\n        Data structure of event:\n\n            ```python\n            {\n                \"instances\": [CreatedInstance, ...],\n                \"create_context\": CreateContext\n            }\n            ```\n\n        Args:\n            callback (Callable): Callback function that will be called when\n                instances are removed from context.\n\n        Returns:\n            EventCallback: Created callback object which can be used to\n                stop listening.\n\n        \"\"\"\n        return self._event_hub.add_callback(INSTANCE_REMOVED_TOPIC, callback)\n\n    def add_value_changed_callback(\n        self, callback: Callable\n    ) -&gt; \"EventCallback\":\n        \"\"\"Register callback to listen value changes.\n\n        Event is triggered when any value changes on any instance or\n            context data.\n\n        Data structure of event:\n\n            ```python\n            {\n                \"changes\": [\n                    {\n                        \"instance\": CreatedInstance,\n                        \"changes\": {\n                            \"folderPath\": \"/new/folder/path\",\n                            \"creator_attributes\": {\n                                \"attr_1\": \"value_1\"\n                            }\n                        }\n                    }\n                ],\n                \"create_context\": CreateContext\n            }\n            ```\n\n        Args:\n            callback (Callable): Callback function that will be called when\n                value changed.\n\n        Returns:\n            EventCallback: Created callback object which can be used to\n                stop listening.\n\n        \"\"\"\n        return self._event_hub.add_callback(VALUE_CHANGED_TOPIC, callback)\n\n    def add_pre_create_attr_defs_change_callback(\n        self, callback: Callable\n    ) -&gt; \"EventCallback\":\n        \"\"\"Register callback to listen pre-create attribute changes.\n\n        Create plugin can trigger refresh of pre-create attributes. Usage of\n            this event is mainly for publisher UI.\n\n        Data structure of event:\n\n            ```python\n            {\n                \"identifiers\": [\"create_plugin_identifier\"],\n                \"create_context\": CreateContext\n            }\n            ```\n\n        Args:\n            callback (Callable): Callback function that will be called when\n                pre-create attributes should be refreshed.\n\n        Returns:\n            EventCallback: Created callback object which can be used to\n                stop listening.\n\n        \"\"\"\n        return self._event_hub.add_callback(\n            PRE_CREATE_ATTR_DEFS_CHANGED_TOPIC, callback\n        )\n\n    def add_create_attr_defs_change_callback(\n        self, callback: Callable\n    ) -&gt; \"EventCallback\":\n        \"\"\"Register callback to listen create attribute changes.\n\n        Create plugin changed attribute definitions of instance.\n\n        Data structure of event:\n\n            ```python\n            {\n                \"instances\": [CreatedInstance, ...],\n                \"create_context\": CreateContext\n            }\n            ```\n\n        Args:\n            callback (Callable): Callback function that will be called when\n                create attributes changed.\n\n        Returns:\n            EventCallback: Created callback object which can be used to\n                stop listening.\n\n        \"\"\"\n        return self._event_hub.add_callback(\n            CREATE_ATTR_DEFS_CHANGED_TOPIC, callback\n        )\n\n    def add_publish_attr_defs_change_callback(\n        self, callback: Callable\n    ) -&gt; \"EventCallback\":\n        \"\"\"Register callback to listen publish attribute changes.\n\n        Publish plugin changed attribute definitions of instance of context.\n\n        Data structure of event:\n\n            ```python\n            {\n                \"instance_changes\": {\n                    None: {\n                        \"instance\": None,\n                        \"plugin_names\": {\"PluginA\"},\n                    }\n                    \"&lt;instance_id&gt;\": {\n                        \"instance\": CreatedInstance,\n                        \"plugin_names\": {\"PluginB\", \"PluginC\"},\n                    }\n                },\n                \"create_context\": CreateContext\n            }\n            ```\n\n        Args:\n            callback (Callable): Callback function that will be called when\n                publish attributes changed.\n\n        Returns:\n            EventCallback: Created callback object which can be used to\n                stop listening.\n\n        \"\"\"\n        return self._event_hub.add_callback(\n            PUBLISH_ATTR_DEFS_CHANGED_TOPIC, callback\n        )\n\n    def add_instance_requirement_change_callback(\n        self, callback: Callable\n    ) -&gt; \"EventCallback\":\n        \"\"\"Register callback to listen to instance requirement changes.\n\n        Instance changed requirement of active state.\n\n        Data structure of event:\n\n            ```python\n            {\n                \"instances\": [CreatedInstance, ...],\n                \"create_context\": CreateContext\n            }\n            ```\n\n        Args:\n            callback (Callable): Callback function that will be called when\n                instance requirement changed.\n\n        Returns:\n            EventCallback: Created callback object which can be used to\n                stop listening.\n\n        \"\"\"\n        return self._event_hub.add_callback(\n            INSTANCE_REQUIREMENT_CHANGED_TOPIC, callback\n        )\n\n    def add_instance_parent_change_callback(\n        self, callback: Callable\n    ) -&gt; \"EventCallback\":\n        \"\"\"Register callback to listen to instance parent changes.\n\n        Instance changed parent or parent flags.\n\n        Data structure of event:\n\n            ```python\n            {\n                \"instances\": [CreatedInstance, ...],\n                \"create_context\": CreateContext\n            }\n            ```\n\n        Args:\n            callback (Callable): Callback function that will be called when\n                instance requirement changed.\n\n        Returns:\n            EventCallback: Created callback object which can be used to\n                stop listening.\n\n        \"\"\"\n        return self._event_hub.add_callback(\n            INSTANCE_PARENT_CHANGED_TOPIC, callback\n        )\n\n    def context_data_to_store(self) -&gt; dict[str, Any]:\n        \"\"\"Data that should be stored by host function.\n\n        The same data should be returned on loading.\n        \"\"\"\n        return {\n            \"publish_attributes\": self._publish_attributes.data_to_store()\n        }\n\n    def context_data_changes(self) -&gt; TrackChangesItem:\n        \"\"\"Changes of attributes.\"\"\"\n\n        return TrackChangesItem(\n            self._original_context_data, self.context_data_to_store()\n        )\n\n    def set_context_publish_plugin_attr_defs(\n        self, plugin_name: str, attr_defs: list[\"AbstractAttrDef\"]\n    ) -&gt; None:\n        \"\"\"Set attribute definitions for CreateContext publish plugin.\n\n        Args:\n            plugin_name(str): Name of publish plugin.\n            attr_defs(list[AbstractAttrDef]): Attribute definitions.\n\n        \"\"\"\n        self.publish_attributes.set_publish_plugin_attr_defs(\n            plugin_name, attr_defs\n        )\n        self.instance_publish_attr_defs_changed(\n            None, plugin_name\n        )\n\n    def creator_adds_instance(self, instance: \"CreatedInstance\") -&gt; None:\n        \"\"\"Creator adds new instance to context.\n\n        Instances should be added only from creators.\n\n        Args:\n            instance(CreatedInstance): Instance with prepared data from\n                creator.\n\n        TODO: Rename method to more suit.\n        \"\"\"\n        # Add instance to instances list\n        if instance.id in self._instances_by_id:\n            self.log.warning((\n                \"Instance with id {} is already added to context.\"\n            ).format(instance.id))\n            return\n\n        self._instances_by_id[instance.id] = instance\n\n        # Add instance to be validated inside 'bulk_add_instances'\n        #   context manager if is inside bulk\n        with self.bulk_add_instances() as bulk_info:\n            bulk_info.append(instance)\n\n    def _get_creator_in_create(self, identifier: str) -&gt; \"BaseCreator\":\n        \"\"\"Creator by identifier with unified error.\n\n        Helper method to get creator by identifier with same error when creator\n        is not available.\n\n        Args:\n            identifier (str): Identifier of creator plugin.\n\n        Returns:\n            BaseCreator: Creator found by identifier.\n\n        Raises:\n            CreatorError: When identifier is not known.\n        \"\"\"\n\n        creator = self.creators.get(identifier)\n        # Fake CreatorError (Could be maybe specific exception?)\n        if creator is None:\n            raise CreatorError(\n                \"Creator {} was not found\".format(identifier)\n            )\n        return creator\n\n    def create(\n        self,\n        creator_identifier: str,\n        variant: str,\n        folder_entity: Optional[dict[str, Any]] = None,\n        task_entity: Optional[dict[str, Any]] = None,\n        pre_create_data: Optional[dict[str, Any]] = None,\n        active: Optional[bool] = None,\n        product_type: Optional[str] = None,\n    ) -&gt; Any:\n        \"\"\"Trigger create of plugins with standartized arguments.\n\n        Arguments 'folder_entity' and 'task_name' use current context as\n        default values. If only 'task_entity' is provided it will be\n        overridden by task name from current context. If 'task_name' is not\n        provided when 'folder_entity' is, it is considered that task name is\n        not specified, which can lead to error if product name template\n        requires task name.\n\n        Args:\n            creator_identifier (str): Identifier of creator plugin.\n            variant (str): Variant used for product name.\n            folder_entity (dict[str, Any]): Folder entity which define context\n                of creation (possible context of created instance/s).\n            task_entity (dict[str, Any]): Task entity.\n            pre_create_data (dict[str, Any]): Pre-create attribute values.\n            active (Optional[bool]): Whether the created instance defaults\n                to be active or not.\n            product_type (str): Specific product type to use.\n\n        Returns:\n            Any: Output of triggered creator's 'create' method.\n\n        Raises:\n            CreatorError: If creator was not found or folder is empty.\n\n        \"\"\"\n        creator = self._get_creator_in_create(creator_identifier)\n\n        project_name = self.project_name\n        if folder_entity is None:\n            folder_path = self.get_current_folder_path()\n            folder_entity = ayon_api.get_folder_by_path(\n                project_name, folder_path\n            )\n            if folder_entity is None:\n                raise CreatorError(f\"Folder '{folder_path}' was not found\")\n\n        if task_entity is None:\n            current_task_name = self.get_current_task_name()\n            if current_task_name:\n                task_entity = ayon_api.get_task_by_name(\n                    project_name, folder_entity[\"id\"], current_task_name\n                )\n\n        # Handle not passed product type\n        if product_type is None:\n            for product_type_item in creator.get_product_type_items():\n                product_type = product_type_item.product_type\n                break\n            else:\n                product_type = creator.product_base_type\n\n        if pre_create_data is None:\n            pre_create_data = {}\n\n        precreate_attr_defs = []\n        # Hidden creators do not have or need the pre-create attributes.\n        if isinstance(creator, Creator):\n            precreate_attr_defs = creator.get_pre_create_attr_defs()\n\n        # Create default values of precreate data\n        _pre_create_data = get_default_values(precreate_attr_defs)\n        # Update passed precreate data to default values\n        # TODO validate types\n        _pre_create_data.update(pre_create_data)\n\n        project_entity = self.get_current_project_entity()\n        args = (\n            project_name,\n            folder_entity,\n            task_entity,\n            variant,\n            self.host_name,\n        )\n        kwargs = {\n            # Backwards compatibility for 'project_entity' argument (24/07/08)\n            \"project_entity\": project_entity,\n            # Backwards compatibility for 'product_type' argument (26/01/19)\n            \"product_type\": product_type,\n        }\n        for kwarg in (\"product_type\", \"project_entity\"):\n            if not is_func_signature_supported(\n                creator.get_product_name, *args, **kwargs\n            ):\n                kwargs.pop(kwarg)\n\n        product_name = creator.get_product_name(*args, **kwargs)\n\n        instance_data = {\n            \"folderPath\": folder_entity[\"path\"],\n            \"task\": task_entity[\"name\"] if task_entity else None,\n            \"productType\": product_type,\n            # Add product base type if supported. Fallback to\n            #   older attribute 'product_type'\n            \"productBaseType\": (\n                creator.product_base_type or creator.product_type),\n            \"variant\": variant\n        }\n\n        if active is not None:\n            if not isinstance(active, bool):\n                self.log.warning(\n                    \"CreateContext.create 'active' argument is not a bool. \"\n                    f\"Converting {active} {type(active)} to bool.\")\n                active = bool(active)\n            instance_data[\"active\"] = active\n\n        with self.bulk_add_instances():\n            return creator.create(\n                product_name,\n                instance_data,\n                _pre_create_data\n            )\n\n    def create_with_unified_error(\n        self, identifier: str, *args, **kwargs\n    ) -&gt; Any:\n        \"\"\"Trigger create but raise only one error if anything fails.\n\n        Added to raise unified exception. Capture any possible issues and\n        reraise it with unified information.\n\n        Args:\n            identifier (str): Identifier of creator.\n            *args: Arguments for create method.\n            **kwargs: Keyword argument for create method.\n\n        Raises:\n            CreatorsCreateFailed: When creation fails due to any possible\n                reason. If anything goes wrong this is only possible exception\n                the method should raise.\n\n        \"\"\"\n        result, fail_info = self._create_with_unified_error(\n            identifier, None, *args, **kwargs\n        )\n        if fail_info is not None:\n            raise CreatorsCreateFailed([fail_info])\n        return result\n\n    def creator_removed_instance(self, instance: \"CreatedInstance\") -&gt; None:\n        \"\"\"When creator removes instance context should be acknowledged.\n\n        If creator removes instance context should know about it to avoid\n        possible issues in the session.\n\n        Args:\n            instance (CreatedInstance): Object of instance which was removed\n                from scene metadata.\n        \"\"\"\n\n        self._remove_instances([instance])\n\n    def add_convertor_item(\n        self, convertor_identifier: str, label: str\n    ) -&gt; None:\n        self.convertor_items_by_id[convertor_identifier] = ConvertorItem(\n            convertor_identifier, label\n        )\n\n    def remove_convertor_item(self, convertor_identifier: str) -&gt; None:\n        self.convertor_items_by_id.pop(convertor_identifier, None)\n\n    @contextmanager\n    def bulk_add_instances(self, sender: Optional[str] = None):\n        with self._bulk_context(\"add\", sender) as bulk_info:\n            yield bulk_info\n\n    @contextmanager\n    def bulk_instances_collection(self, sender: Optional[str] = None):\n        \"\"\"DEPRECATED use 'bulk_add_instances' instead.\"\"\"\n        # TODO add warning\n        with self.bulk_add_instances(sender) as bulk_info:\n            yield bulk_info\n\n    @contextmanager\n    def bulk_remove_instances(self, sender: Optional[str] = None):\n        with self._bulk_context(\"remove\", sender) as bulk_info:\n            yield bulk_info\n\n    @contextmanager\n    def bulk_value_changes(self, sender: Optional[str] = None):\n        with self._bulk_context(\"change\", sender) as bulk_info:\n            yield bulk_info\n\n    @contextmanager\n    def bulk_pre_create_attr_defs_change(self, sender: Optional[str] = None):\n        with self._bulk_context(\n            \"pre_create_attrs_change\", sender\n        ) as bulk_info:\n            yield bulk_info\n\n    @contextmanager\n    def bulk_create_attr_defs_change(self, sender: Optional[str] = None):\n        with self._bulk_context(\n            \"create_attrs_change\", sender\n        ) as bulk_info:\n            yield bulk_info\n\n    @contextmanager\n    def bulk_instance_requirement_change(self, sender: Optional[str] = None):\n        with self._bulk_context(\n            \"requirement_change\", sender\n        ) as bulk_info:\n            yield bulk_info\n\n    @contextmanager\n    def bulk_instance_parent_change(self, sender: Optional[str] = None):\n        with self._bulk_context(\n            \"parent_change\", sender\n        ) as bulk_info:\n            yield bulk_info\n\n    @contextmanager\n    def bulk_publish_attr_defs_change(self, sender: Optional[str] = None):\n        with self._bulk_context(\"publish_attrs_change\", sender) as bulk_info:\n            yield bulk_info\n\n    # --- instance change callbacks ---\n    def create_plugin_pre_create_attr_defs_changed(\n        self, identifier: str\n    ) -&gt; None:\n        \"\"\"Create plugin pre-create attributes changed.\n\n        Triggered by 'Creator'.\n\n        Args:\n            identifier (str): Create plugin identifier.\n\n        \"\"\"\n        with self.bulk_pre_create_attr_defs_change() as bulk_item:\n            bulk_item.append(identifier)\n\n    def instance_create_attr_defs_changed(self, instance_id: str) -&gt; None:\n        \"\"\"Instance attribute definitions changed.\n\n        Triggered by instance 'CreatorAttributeValues' on instance.\n\n        Args:\n            instance_id (str): Instance id.\n\n        \"\"\"\n        if self._is_instance_events_ready(instance_id):\n            with self.bulk_create_attr_defs_change() as bulk_item:\n                bulk_item.append(instance_id)\n\n    def instance_publish_attr_defs_changed(\n        self, instance_id: Optional[str], plugin_name: str\n    ) -&gt; None:\n        \"\"\"Instance attribute definitions changed.\n\n        Triggered by instance 'PublishAttributeValues' on instance.\n\n        Args:\n            instance_id (Optional[str]): Instance id or None for context.\n            plugin_name (str): Plugin name which attribute definitions were\n                changed.\n\n        \"\"\"\n        if self._is_instance_events_ready(instance_id):\n            with self.bulk_publish_attr_defs_change() as bulk_item:\n                bulk_item.append((instance_id, plugin_name))\n\n    def instance_values_changed(\n        self, instance_id: Optional[str], new_values: dict[str, Any]\n    ) -&gt; None:\n        \"\"\"Instance value changed.\n\n        Triggered by `CreatedInstance, 'CreatorAttributeValues'\n            or 'PublishAttributeValues' on instance.\n\n        Args:\n            instance_id (Optional[str]): Instance id or None for context.\n            new_values (dict[str, Any]): Changed values.\n\n        \"\"\"\n        if self._is_instance_events_ready(instance_id):\n            with self.bulk_value_changes() as bulk_item:\n                bulk_item.append((instance_id, new_values))\n\n    def instance_requirement_changed(self, instance_id: str) -&gt; None:\n        \"\"\"Instance requirement changed.\n\n        Triggered by `CreatedInstance`.\n\n        Args:\n            instance_id (Optional[str]): Instance id.\n\n        \"\"\"\n        if self._is_instance_events_ready(instance_id):\n            with self.bulk_instance_requirement_change() as bulk_item:\n                bulk_item.append(instance_id)\n\n    def instance_parent_changed(self, instance_id: str) -&gt; None:\n        \"\"\"Instance parent changed.\n\n        Triggered by `CreatedInstance`.\n\n        Args:\n            instance_id (Optional[str]): Instance id.\n\n        \"\"\"\n        if self._is_instance_events_ready(instance_id):\n            with self.bulk_instance_parent_change() as bulk_item:\n                bulk_item.append(instance_id)\n\n    # --- context change callbacks ---\n    def publish_attribute_value_changed(\n        self, plugin_name: str, value: dict[str, Any]\n    ) -&gt; None:\n        \"\"\"Context publish attribute values changed.\n\n        Triggered by instance 'PublishAttributeValues' on context.\n\n        Args:\n            plugin_name (str): Plugin name which changed value.\n            value (dict[str, Any]): Changed values.\n\n        \"\"\"\n        self.instance_values_changed(\n            None,\n            {\n                \"publish_attributes\": {\n                    plugin_name: value,\n                },\n            },\n        )\n\n    def reset_instances(self) -&gt; None:\n        \"\"\"Reload instances\"\"\"\n        self._instances_by_id = collections.OrderedDict()\n\n        # Collect instances\n        error_message = \"Collection of instances for creator {} failed. {}\"\n        failed_info = []\n        for creator in self.sorted_creators:\n            label = creator.label\n            identifier = creator.identifier\n            failed = False\n            add_traceback = False\n            exc_info = None\n            try:\n                creator.collect_instances()\n\n            except CreatorError:\n                failed = True\n                exc_info = sys.exc_info()\n                self.log.warning(error_message.format(identifier, exc_info[1]))\n\n            except:  # noqa: E722\n                failed = True\n                add_traceback = True\n                exc_info = sys.exc_info()\n                self.log.warning(\n                    error_message.format(identifier, \"\"),\n                    exc_info=True\n                )\n\n            if failed:\n                failed_info.append(\n                    prepare_failed_creator_operation_info(\n                        identifier, label, exc_info, add_traceback\n                    )\n                )\n\n        if failed_info:\n            raise CreatorsCollectionFailed(failed_info)\n\n    def find_convertor_items(self) -&gt; None:\n        \"\"\"Go through convertor plugins to look for items to convert.\n\n        Raises:\n            ConvertorsFindFailed: When one or more convertors fails during\n                finding.\n        \"\"\"\n\n        self.convertor_items_by_id = {}\n\n        failed_info = []\n        for convertor in self.convertors_plugins.values():\n            try:\n                convertor.find_instances()\n\n            except:  # noqa: E722\n                failed_info.append(\n                    prepare_failed_convertor_operation_info(\n                        convertor.identifier, sys.exc_info()\n                    )\n                )\n                self.log.warning(\n                    \"Failed to find instances of convertor \\\"{}\\\"\".format(\n                        convertor.identifier\n                    ),\n                    exc_info=True\n                )\n\n        if failed_info:\n            raise ConvertorsFindFailed(failed_info)\n\n    def execute_autocreators(self) -&gt; None:\n        \"\"\"Execute discovered AutoCreator plugins.\n\n        Reset instances if any autocreator executed properly.\n        \"\"\"\n\n        failed_info = []\n        for creator in self.sorted_autocreators:\n            identifier = creator.identifier\n            _, fail_info = self._create_with_unified_error(identifier, creator)\n            if fail_info is not None:\n                failed_info.append(fail_info)\n\n        if failed_info:\n            raise CreatorsCreateFailed(failed_info)\n\n    def get_folder_entities(\n        self, folder_paths: Iterable[str]\n    ) -&gt; dict[str, Optional[dict[str, Any]]]:\n        \"\"\"Get folder entities by paths.\n\n        Args:\n            folder_paths (Iterable[str]): Folder paths.\n\n        Returns:\n            dict[str, Optional[dict[str, Any]]]: Folder entities by path.\n\n        \"\"\"\n        output = {\n            folder_path: None\n            for folder_path in folder_paths\n        }\n        remainder_paths = set()\n        for folder_path in output:\n            # Skip invalid folder paths (folder name or empty path)\n            if not folder_path or \"/\" not in folder_path:\n                continue\n\n            if folder_path not in self._folder_entities_by_path:\n                remainder_paths.add(folder_path)\n                continue\n\n            output[folder_path] = self._folder_entities_by_path[folder_path]\n\n        if not remainder_paths:\n            return output\n\n        found_paths = set()\n        for folder_entity in ayon_api.get_folders(\n            self.project_name,\n            folder_paths=remainder_paths,\n        ):\n            folder_path = folder_entity[\"path\"]\n            found_paths.add(folder_path)\n            output[folder_path] = folder_entity\n            self._folder_entities_by_path[folder_path] = folder_entity\n\n        # Cache empty folder entities\n        for path in remainder_paths - found_paths:\n            self._folder_entities_by_path[path] = None\n\n        return output\n\n    def get_task_entities(\n        self,\n        task_names_by_folder_paths: dict[str, set[str]]\n    ) -&gt; dict[str, dict[str, Optional[dict[str, Any]]]]:\n        \"\"\"Get task entities by folder path and task name.\n\n        Entities are cached until reset.\n\n        Args:\n            task_names_by_folder_paths (dict[str, set[str]]): Task names by\n                folder path.\n\n        Returns:\n            dict[str, dict[str, dict[str, Any]]]: Task entities by folder path\n                and task name.\n\n        \"\"\"\n        output = {}\n        for folder_path, task_names in task_names_by_folder_paths.items():\n            if folder_path is None:\n                continue\n            output[folder_path] = {\n                task_name: None\n                for task_name in task_names\n                if task_name is not None\n            }\n\n        missing_folder_paths = set()\n        for folder_path, output_task_entities_by_name in output.items():\n            if not output_task_entities_by_name:\n                continue\n\n            if folder_path not in self._task_ids_by_folder_path:\n                missing_folder_paths.add(folder_path)\n                continue\n\n            all_tasks_filled = True\n            task_ids = self._task_ids_by_folder_path[folder_path]\n            task_entities_by_name = {}\n            for task_id in task_ids:\n                task_entity = self._task_entities_by_id.get(task_id)\n                if task_entity is None:\n                    all_tasks_filled = False\n                    continue\n                task_entities_by_name[task_entity[\"name\"]] = task_entity\n\n            any_missing = False\n            for task_name in set(output_task_entities_by_name):\n                task_entity = task_entities_by_name.get(task_name)\n                if task_entity is None:\n                    any_missing = True\n                    continue\n\n                output_task_entities_by_name[task_name] = task_entity\n\n            if any_missing and not all_tasks_filled:\n                missing_folder_paths.add(folder_path)\n\n        if not missing_folder_paths:\n            return output\n\n        folder_entities_by_path = self.get_folder_entities(\n            missing_folder_paths\n        )\n        folder_path_by_id = {}\n        for folder_path, folder_entity in folder_entities_by_path.items():\n            if folder_entity is not None:\n                folder_path_by_id[folder_entity[\"id\"]] = folder_path\n\n        if not folder_path_by_id:\n            return output\n\n        task_entities_by_parent_id = collections.defaultdict(list)\n        for task_entity in ayon_api.get_tasks(\n            self.project_name,\n            folder_ids=folder_path_by_id.keys()\n        ):\n            folder_id = task_entity[\"folderId\"]\n            task_entities_by_parent_id[folder_id].append(task_entity)\n\n        for folder_id, task_entities in task_entities_by_parent_id.items():\n            folder_path = folder_path_by_id[folder_id]\n            task_ids = set()\n            task_names = set()\n            for task_entity in task_entities:\n                task_id = task_entity[\"id\"]\n                task_name = task_entity[\"name\"]\n                task_ids.add(task_id)\n                task_names.add(task_name)\n                self._task_entities_by_id[task_id] = task_entity\n\n                output[folder_path][task_name] = task_entity\n            self._task_ids_by_folder_path[folder_path] = task_ids\n            self._task_names_by_folder_path[folder_path] = task_names\n\n        return output\n\n    def get_folder_entity(\n        self,\n        folder_path: Optional[str],\n    ) -&gt; Optional[dict[str, Any]]:\n        \"\"\"Get folder entity by path.\n\n        Entities are cached until reset.\n\n        Args:\n            folder_path (Optional[str]): Folder path.\n\n        Returns:\n            Optional[dict[str, Any]]: Folder entity.\n\n        \"\"\"\n        if not folder_path:\n            return None\n        return self.get_folder_entities([folder_path]).get(folder_path)\n\n    def get_task_entity(\n        self,\n        folder_path: Optional[str],\n        task_name: Optional[str],\n    ) -&gt; Optional[dict[str, Any]]:\n        \"\"\"Get task entity by name and folder path.\n\n        Entities are cached until reset.\n\n        Args:\n            folder_path (Optional[str]): Folder path.\n            task_name (Optional[str]): Task name.\n\n        Returns:\n            Optional[dict[str, Any]]: Task entity.\n\n        \"\"\"\n        if not folder_path or not task_name:\n            return None\n\n        output = self.get_task_entities({folder_path: {task_name}})\n        return output.get(folder_path, {}).get(task_name)\n\n    def get_instances_folder_entities(\n        self, instances: Optional[Iterable[\"CreatedInstance\"]] = None\n    ) -&gt; dict[str, Optional[dict[str, Any]]]:\n        if instances is None:\n            instances = self._instances_by_id.values()\n        instances = list(instances)\n        output = {\n            instance.id: None\n            for instance in instances\n        }\n        if not instances:\n            return output\n\n        folder_paths = {\n            instance.get(\"folderPath\")\n            for instance in instances\n        }\n        folder_paths.discard(None)\n        folder_entities_by_path = self.get_folder_entities(folder_paths)\n        for instance in instances:\n            folder_path = instance.get(\"folderPath\")\n            output[instance.id] = folder_entities_by_path.get(folder_path)\n        return output\n\n    def get_instances_task_entities(\n        self, instances: Optional[Iterable[\"CreatedInstance\"]] = None\n    ) -&gt; dict[str, Optional[dict[str, Any]]]:\n        \"\"\"Get task entities for instances.\n\n        Args:\n            instances (Optional[Iterable[CreatedInstance]]): Instances to\n                get task entities. If not provided all instances are used.\n\n        Returns:\n            dict[str, Optional[dict[str, Any]]]: Task entity by instance id.\n\n        \"\"\"\n        if instances is None:\n            instances = self._instances_by_id.values()\n        instances = list(instances)\n\n        output = {\n            instance.id: None\n            for instance in instances\n        }\n        if not instances:\n            return output\n\n        filtered_instances = []\n        task_names_by_folder_path = collections.defaultdict(set)\n        for instance in instances:\n            folder_path = instance.get(\"folderPath\")\n            task_name = instance.get(\"task\")\n            if not folder_path or not task_name:\n                continue\n            filtered_instances.append(instance)\n            task_names_by_folder_path[folder_path].add(task_name)\n\n        task_entities_by_folder_path = self.get_task_entities(\n            task_names_by_folder_path\n        )\n        for instance in filtered_instances:\n            folder_path = instance[\"folderPath\"]\n            task_name = instance[\"task\"]\n            output[instance.id] = (\n                task_entities_by_folder_path[folder_path][task_name]\n            )\n\n        return output\n\n    def get_instances_context_info(\n        self, instances: Optional[Iterable[\"CreatedInstance\"]] = None\n    ) -&gt; dict[str, InstanceContextInfo]:\n        \"\"\"Validate 'folder' and 'task' instance context.\n\n        Args:\n            instances (Optional[Iterable[CreatedInstance]]): Instances to\n                validate. If not provided all instances are validated.\n\n        Returns:\n            dict[str, InstanceContextInfo]: Validation results by instance id.\n\n        \"\"\"\n        # Use all instances from context if 'instances' are not passed\n        if instances is None:\n            instances = self._instances_by_id.values()\n        instances = tuple(instances)\n        info_by_instance_id = {\n            instance.id: InstanceContextInfo(\n                instance.get(\"folderPath\"),\n                instance.get(\"task\"),\n                False,\n                False,\n            )\n            for instance in instances\n        }\n\n        # Skip if instances are empty\n        if not info_by_instance_id:\n            return info_by_instance_id\n\n        project_name = self.project_name\n\n        to_validate = []\n        task_names_by_folder_path = collections.defaultdict(set)\n        for instance in instances:\n            context_info = info_by_instance_id[instance.id]\n            if instance.has_promised_context:\n                context_info.folder_is_valid = True\n                context_info.task_is_valid = True\n                # NOTE missing task type\n                continue\n            # TODO allow context promise\n            folder_path = context_info.folder_path\n            if not folder_path:\n                continue\n\n            if folder_path in self._folder_entities_by_path:\n                folder_entity = self._folder_entities_by_path[folder_path]\n                if folder_entity is None:\n                    continue\n                context_info.folder_is_valid = True\n\n            task_name = context_info.task_name\n            if task_name is not None:\n                tasks_cache = self._task_names_by_folder_path.get(folder_path)\n                if tasks_cache is not None:\n                    context_info.task_is_valid = task_name in tasks_cache\n                    continue\n\n            to_validate.append(instance)\n            task_names_by_folder_path[folder_path].add(task_name)\n\n        if not to_validate:\n            return info_by_instance_id\n\n        # Backwards compatibility for cases where folder name is set instead\n        #   of folder path\n        folder_paths = set()\n        task_names_by_folder_name = {}\n        task_names_by_folder_path_clean = {}\n        for folder_path, task_names in task_names_by_folder_path.items():\n            if folder_path is None:\n                continue\n\n            clean_task_names = {\n                task_name\n                for task_name in task_names\n                if task_name\n            }\n\n            if \"/\" not in folder_path:\n                task_names_by_folder_name[folder_path] = clean_task_names\n                continue\n\n            folder_paths.add(folder_path)\n            if not clean_task_names:\n                continue\n\n            task_names_by_folder_path_clean[folder_path] = clean_task_names\n\n        folder_paths_by_name = collections.defaultdict(list)\n        if task_names_by_folder_name:\n            for folder_entity in ayon_api.get_folders(\n                project_name,\n                folder_names=task_names_by_folder_name.keys(),\n                fields={\"name\", \"path\"}\n            ):\n                folder_name = folder_entity[\"name\"]\n                folder_path = folder_entity[\"path\"]\n                folder_paths_by_name[folder_name].append(folder_path)\n\n        folder_path_by_name = {}\n        for folder_name, paths in folder_paths_by_name.items():\n            if len(paths) != 1:\n                continue\n            path = paths[0]\n            folder_path_by_name[folder_name] = path\n            folder_paths.add(path)\n            clean_task_names = task_names_by_folder_name[folder_name]\n            if not clean_task_names:\n                continue\n            folder_task_names = task_names_by_folder_path_clean.setdefault(\n                path, set()\n            )\n            folder_task_names |= clean_task_names\n\n        folder_entities_by_path = self.get_folder_entities(folder_paths)\n        task_entities_by_folder_path = self.get_task_entities(\n            task_names_by_folder_path_clean\n        )\n\n        for instance in to_validate:\n            folder_path = instance[\"folderPath\"]\n            task_name = instance.get(\"task\")\n            if folder_path and \"/\" not in folder_path:\n                new_folder_path = folder_path_by_name.get(folder_path)\n                if new_folder_path:\n                    folder_path = new_folder_path\n                    instance[\"folderPath\"] = new_folder_path\n\n            folder_entity = folder_entities_by_path.get(folder_path)\n            if not folder_entity:\n                continue\n            context_info = info_by_instance_id[instance.id]\n            context_info.folder_is_valid = True\n\n            if (\n                not task_name\n                or task_name in task_entities_by_folder_path[folder_path]\n            ):\n                context_info.task_is_valid = True\n        return info_by_instance_id\n\n    def save_changes(self) -&gt; None:\n        \"\"\"Save changes. Update all changed values.\"\"\"\n        if not self.host_is_valid:\n            missing_methods = self.get_host_misssing_methods(self.host)\n            raise HostMissRequiredMethod(self.host, missing_methods)\n\n        self._save_context_changes()\n        self._save_instance_changes()\n\n    def _save_context_changes(self) -&gt; None:\n        \"\"\"Save global context values.\"\"\"\n        changes = self.context_data_changes()\n        if changes:\n            data = self.context_data_to_store()\n            self.host.update_context_data(data, changes)\n\n    def _save_instance_changes(self) -&gt; None:\n        \"\"\"Save instance specific values.\"\"\"\n        instances_by_identifier = collections.defaultdict(list)\n        for instance in self._instances_by_id.values():\n            instance_changes = instance.changes()\n            if not instance_changes:\n                continue\n\n            identifier = instance.creator_identifier\n            instances_by_identifier[identifier].append(\n                UpdateData(instance, instance_changes)\n            )\n\n        if not instances_by_identifier:\n            return\n\n        error_message = \"Instances update of creator \\\"{}\\\" failed. {}\"\n        failed_info = []\n\n        for creator in self.get_sorted_creators(\n            instances_by_identifier.keys()\n        ):\n            identifier = creator.identifier\n            update_list = instances_by_identifier[identifier]\n            if not update_list:\n                continue\n\n            label = creator.label\n            failed = False\n            add_traceback = False\n            exc_info = None\n            try:\n                creator.update_instances(update_list)\n\n            except CreatorError:\n                failed = True\n                exc_info = sys.exc_info()\n                self.log.warning(error_message.format(identifier, exc_info[1]))\n\n            except:  # noqa: E722\n                failed = True\n                add_traceback = True\n                exc_info = sys.exc_info()\n                self.log.warning(\n                    error_message.format(identifier, \"\"), exc_info=True)\n\n            if failed:\n                failed_info.append(\n                    prepare_failed_creator_operation_info(\n                        identifier, label, exc_info, add_traceback\n                    )\n                )\n            else:\n                for update_data in update_list:\n                    instance = update_data.instance\n                    instance.mark_as_stored()\n\n        if failed_info:\n            raise CreatorsSaveFailed(failed_info)\n\n    def remove_instances(\n        self,\n        instances: list[\"CreatedInstance\"],\n        sender: Optional[str] = None,\n    ) -&gt; None:\n        \"\"\"Remove instances from context.\n\n        All instances that don't have creator identifier leading to existing\n            creator are just removed from context.\n\n        Args:\n            instances (list[CreatedInstance]): Instances that should be\n                removed. Remove logic is done using creator, which may require\n                to do other cleanup than just remove instance from context.\n            sender (Optional[str]): Sender of the event.\n\n        \"\"\"\n        instance_ids_by_parent_id = collections.defaultdict(set)\n        for instance in self.instances:\n            instance_ids_by_parent_id[instance.parent_instance_id].add(\n                instance.id\n            )\n\n        instances_to_remove = list(instances)\n        ids_to_remove = {\n            instance.id\n            for instance in instances_to_remove\n        }\n        _queue = collections.deque()\n        _queue.extend(instances_to_remove)\n        # Add children with parent lifetime flag\n        while _queue:\n            instance = _queue.popleft()\n            ids_to_remove.add(instance.id)\n            children_ids = instance_ids_by_parent_id[instance.id]\n            for children_id in children_ids:\n                if children_id in ids_to_remove:\n                    continue\n                instance = self._instances_by_id[children_id]\n                if instance.parent_flags &amp; ParentFlags.parent_lifetime:\n                    instances_to_remove.append(instance)\n                    ids_to_remove.add(instance.id)\n                    _queue.append(instance)\n\n        instances_by_identifier = collections.defaultdict(list)\n        for instance in instances_to_remove:\n            identifier = instance.creator_identifier\n            instances_by_identifier[identifier].append(instance)\n\n        # Just remove instances from context if creator is not available\n        missing_creators = set(instances_by_identifier) - set(self.creators)\n        miss_creator_instances = []\n        for identifier in missing_creators:\n            miss_creator_instances.extend(instances_by_identifier[identifier])\n\n        with self.bulk_remove_instances(sender):\n            self._remove_instances(miss_creator_instances, sender)\n\n            error_message = \"Instances removement of creator \\\"{}\\\" failed. {}\"\n            failed_info = []\n            # Remove instances by creator plugin order\n            for creator in self.get_sorted_creators(\n                instances_by_identifier.keys()\n            ):\n                identifier = creator.identifier\n                # Filter instances by current state of 'CreateContext'\n                # - in case instances were already removed as subroutine of\n                #   previous create plugin.\n                creator_instances = [\n                    instance\n                    for instance in instances_by_identifier[identifier]\n                    if instance.id in self._instances_by_id\n                ]\n                if not creator_instances:\n                    continue\n\n                label = creator.label\n                failed = False\n                add_traceback = False\n                exc_info = None\n                try:\n                    creator.remove_instances(creator_instances)\n\n                except CreatorError:\n                    failed = True\n                    exc_info = sys.exc_info()\n                    self.log.warning(\n                        error_message.format(identifier, exc_info[1])\n                    )\n\n                except (KeyboardInterrupt, SystemExit):\n                    raise\n\n                except:  # noqa: E722\n                    failed = True\n                    add_traceback = True\n                    exc_info = sys.exc_info()\n                    self.log.warning(\n                        error_message.format(identifier, \"\"),\n                        exc_info=True\n                    )\n\n                if failed:\n                    failed_info.append(\n                        prepare_failed_creator_operation_info(\n                            identifier, label, exc_info, add_traceback\n                        )\n                    )\n\n        if failed_info:\n            raise CreatorsRemoveFailed(failed_info)\n\n    @property\n    def collection_shared_data(self) -&gt; dict[str, Any]:\n        \"\"\"Access to shared data that can be used during creator's collection.\n\n        Returns:\n            dict[str, Any]: Shared data.\n\n        Raises:\n            UnavailableSharedData: When called out of collection phase.\n        \"\"\"\n\n        if self._collection_shared_data is None:\n            raise UnavailableSharedData(\n                \"Accessed Collection shared data out of collection phase\"\n            )\n        return self._collection_shared_data\n\n    def run_convertor(self, convertor_identifier: str) -&gt; None:\n        \"\"\"Run convertor plugin by identifier.\n\n        Conversion is skipped if convertor is not available.\n\n        Args:\n            convertor_identifier (str): Identifier of convertor.\n        \"\"\"\n\n        convertor = self.convertors_plugins.get(convertor_identifier)\n        if convertor is not None:\n            convertor.convert()\n\n    def run_convertors(self, convertor_identifiers: Iterable[str]) -&gt; None:\n        \"\"\"Run convertor plugins by identifiers.\n\n        Conversion is skipped if convertor is not available. It is recommended\n        to trigger reset after conversion to reload instances.\n\n        Args:\n            convertor_identifiers (Iterable[str]): Identifiers of convertors\n                to run.\n\n        Raises:\n            ConvertorsConversionFailed: When one or more convertors fails.\n        \"\"\"\n\n        failed_info = []\n        for convertor_identifier in convertor_identifiers:\n            try:\n                self.run_convertor(convertor_identifier)\n\n            except:  # noqa: E722\n                failed_info.append(\n                    prepare_failed_convertor_operation_info(\n                        convertor_identifier, sys.exc_info()\n                    )\n                )\n                self.log.warning(\n                    \"Failed to convert instances of convertor \\\"{}\\\"\".format(\n                        convertor_identifier\n                    ),\n                    exc_info=True\n                )\n\n        if failed_info:\n            raise ConvertorsConversionFailed(failed_info)\n\n    def _register_event_callback(\n        self, topic: str, callback: Callable\n    ) -&gt; \"EventCallback\":\n        return self._event_hub.add_callback(topic, callback)\n\n    def _emit_event(\n        self,\n        topic: str,\n        data: Optional[dict[str, Any]] = None,\n        sender: Optional[str] = None,\n    ) -&gt; \"Event\":\n        if data is None:\n            data = {}\n        data.setdefault(\"create_context\", self)\n        return self._event_hub.emit(topic, data, sender)\n\n    def _remove_instances(\n        self,\n        instances: Iterable[CreatedInstance],\n        sender: Optional[str] = None,\n    ) -&gt; None:\n        with self.bulk_remove_instances(sender) as bulk_info:\n            for instance in instances:\n                obj = self._instances_by_id.pop(instance.id, None)\n                if obj is not None:\n                    bulk_info.append(obj)\n\n    def _create_with_unified_error(\n        self,\n        identifier: str,\n        creator: Optional[\"BaseCreator\"],\n        *args,\n        **kwargs\n    ) -&gt; tuple[Optional[Any], Optional[dict[str, Any]]]:\n        error_message = \"Failed to run Creator with identifier \\\"{}\\\". {}\"\n\n        label = None\n        add_traceback = False\n        result = None\n        fail_info = None\n        exc_info = None\n        success = False\n\n        try:\n            # Try to get creator and his label\n            if creator is None:\n                creator = self._get_creator_in_create(identifier)\n            label = getattr(creator, \"label\", label)\n\n            # Run create\n            with self.bulk_add_instances():\n                result = creator.create(*args, **kwargs)\n            success = True\n\n        except CreatorError:\n            exc_info = sys.exc_info()\n            self.log.warning(error_message.format(identifier, exc_info[1]))\n\n        except:  # noqa: E722\n            add_traceback = True\n            exc_info = sys.exc_info()\n            self.log.warning(\n                error_message.format(identifier, \"\"),\n                exc_info=True\n            )\n\n        if not success:\n            fail_info = prepare_failed_creator_operation_info(\n                identifier, label, exc_info, add_traceback\n            )\n        return result, fail_info\n\n    def _is_instance_events_ready(self, instance_id: Optional[str]) -&gt; bool:\n        # Context is ready\n        if instance_id is None:\n            return True\n        # Instance is not in yet in context\n        if instance_id not in self._instances_by_id:\n            return False\n\n        # Instance in 'collect' bulk will be ignored\n        for instance in self._bulk_info[\"add\"].get_data():\n            if instance.id == instance_id:\n                return False\n        return True\n\n    @contextmanager\n    def _bulk_context(self, key: str, sender: Optional[str]):\n        bulk_info = self._bulk_info[key]\n        bulk_info.set_sender(sender)\n\n        bulk_info.increase()\n        if key not in self._bulk_order:\n            self._bulk_order.append(key)\n        try:\n            yield bulk_info\n        finally:\n            bulk_info.decrease()\n            if bulk_info:\n                self._bulk_finished(key)\n\n    def _bulk_finished(self, key: str) -&gt; None:\n        if self._bulk_order[0] != key:\n            return\n\n        self._bulk_order.pop(0)\n        self._bulk_finish(key)\n\n        while self._bulk_order:\n            key = self._bulk_order[0]\n            if not self._bulk_info[key]:\n                break\n            self._bulk_order.pop(0)\n            self._bulk_finish(key)\n\n    def _bulk_finish(self, key: str) -&gt; None:\n        bulk_info = self._bulk_info[key]\n        sender = bulk_info.get_sender()\n        data = bulk_info.pop_data()\n        if key == \"add\":\n            self._bulk_add_instances_finished(data, sender)\n        elif key == \"remove\":\n            self._bulk_remove_instances_finished(data, sender)\n        elif key == \"change\":\n            self._bulk_values_change_finished(data, sender)\n        elif key == \"pre_create_attrs_change\":\n            self._bulk_pre_create_attrs_change_finished(data, sender)\n        elif key == \"create_attrs_change\":\n            self._bulk_create_attrs_change_finished(data, sender)\n        elif key == \"publish_attrs_change\":\n            self._bulk_publish_attrs_change_finished(data, sender)\n        elif key == \"requirement_change\":\n            self._bulk_instance_requirement_change_finished(data, sender)\n        elif key == \"parent_change\":\n            self._bulk_instance_parent_change_finished(data, sender)\n\n    def _bulk_add_instances_finished(\n        self,\n        instances_to_validate: list[\"CreatedInstance\"],\n        sender: Optional[str]\n    ) -&gt; None:\n        if not instances_to_validate:\n            return\n\n        # Set publish attributes before bulk callbacks are triggered\n        for instance in instances_to_validate:\n            publish_attributes = instance.publish_attributes\n            # Prepare publish plugin attributes and set it on instance\n            for plugin in self.plugins_with_defs:\n                try:\n                    if is_func_signature_supported(\n                            plugin.convert_attribute_values, self, instance\n                    ):\n                        plugin.convert_attribute_values(self, instance)\n\n                    elif plugin.__instanceEnabled__:\n                        output = plugin.convert_attribute_values(\n                            publish_attributes\n                        )\n                        if output:\n                            publish_attributes.update(output)\n\n                except Exception:\n                    self.log.error(\n                        \"Failed to convert attribute values of\"\n                        f\" plugin '{plugin.__name__}'\",\n                        exc_info=True\n                    )\n\n            for plugin in self.plugins_with_defs:\n                attr_defs = None\n                try:\n                    attr_defs = plugin.get_attr_defs_for_instance(\n                        self, instance\n                    )\n                except Exception:\n                    self.log.error(\n                        \"Failed to get attribute definitions\"\n                        f\" from plugin '{plugin.__name__}'.\",\n                        exc_info=True\n                    )\n\n                if not attr_defs:\n                    continue\n                instance.set_publish_plugin_attr_defs(\n                    plugin.__name__, attr_defs\n                )\n\n        # Cache folder and task entities for all instances at once\n        self.get_instances_context_info(instances_to_validate)\n\n        self._emit_event(\n            INSTANCE_ADDED_TOPIC,\n            {\n                \"instances\": instances_to_validate,\n            },\n            sender,\n        )\n\n    def _bulk_remove_instances_finished(\n        self,\n        instances_to_remove: list[\"CreatedInstance\"],\n        sender: Optional[str]\n    ) -&gt; None:\n        if not instances_to_remove:\n            return\n\n        self._emit_event(\n            INSTANCE_REMOVED_TOPIC,\n            {\n                \"instances\": instances_to_remove,\n            },\n            sender,\n        )\n\n    def _bulk_values_change_finished(\n        self,\n        changes: list[tuple[Optional[str], dict[str, Any]]],\n        sender: Optional[str],\n    ) -&gt; None:\n        if not changes:\n            return\n        item_data_by_id = {}\n        for item_id, item_changes in changes:\n            item_values = item_data_by_id.setdefault(item_id, {})\n            if \"creator_attributes\" in item_changes:\n                current_value = item_values.setdefault(\n                    \"creator_attributes\", {}\n                )\n                current_value.update(\n                    item_changes.pop(\"creator_attributes\")\n                )\n\n            if \"publish_attributes\" in item_changes:\n                current_publish = item_values.setdefault(\n                    \"publish_attributes\", {}\n                )\n                for plugin_name, plugin_value in item_changes.pop(\n                    \"publish_attributes\"\n                ).items():\n                    if plugin_value is None:\n                        current_publish[plugin_name] = None\n                        continue\n                    plugin_changes = current_publish.setdefault(\n                        plugin_name, {}\n                    )\n                    if plugin_changes is None:\n                        current_publish[plugin_name] = plugin_value\n                    else:\n                        plugin_changes.update(plugin_value)\n\n            item_values.update(item_changes)\n\n        event_changes = []\n        for item_id, item_changes in item_data_by_id.items():\n            instance = self.get_instance_by_id(item_id)\n            event_changes.append({\n                \"instance\": instance,\n                \"changes\": item_changes,\n            })\n\n        event_data = {\n            \"changes\": event_changes,\n        }\n\n        self._emit_event(\n            VALUE_CHANGED_TOPIC,\n            event_data,\n            sender\n        )\n\n    def _bulk_pre_create_attrs_change_finished(\n        self, identifiers: list[str], sender: Optional[str]\n    ) -&gt; None:\n        if not identifiers:\n            return\n        identifiers = list(set(identifiers))\n        self._emit_event(\n            PRE_CREATE_ATTR_DEFS_CHANGED_TOPIC,\n            {\n                \"identifiers\": identifiers,\n            },\n            sender,\n        )\n\n    def _bulk_create_attrs_change_finished(\n        self, instance_ids: list[str], sender: Optional[str]\n    ) -&gt; None:\n        if not instance_ids:\n            return\n\n        instances = [\n            self.get_instance_by_id(instance_id)\n            for instance_id in set(instance_ids)\n        ]\n        self._emit_event(\n            CREATE_ATTR_DEFS_CHANGED_TOPIC,\n            {\n                \"instances\": instances,\n            },\n            sender,\n        )\n\n    def _bulk_publish_attrs_change_finished(\n        self,\n        attr_info: list[tuple[str, Optional[str]]],\n        sender: Optional[str],\n    ) -&gt; None:\n        if not attr_info:\n            return\n\n        instance_changes = {}\n        for instance_id, plugin_name in attr_info:\n            instance_data = instance_changes.setdefault(\n                instance_id,\n                {\n                    \"instance\": None,\n                    \"plugin_names\": set(),\n                }\n            )\n            instance = self.get_instance_by_id(instance_id)\n            instance_data[\"instance\"] = instance\n            instance_data[\"plugin_names\"].add(plugin_name)\n\n        self._emit_event(\n            PUBLISH_ATTR_DEFS_CHANGED_TOPIC,\n            {\"instance_changes\": instance_changes},\n            sender,\n        )\n\n    def _bulk_instance_requirement_change_finished(\n        self,\n        instance_ids: list[str],\n        sender: Optional[str],\n    ) -&gt; None:\n        if not instance_ids:\n            return\n\n        instances = [\n            self.get_instance_by_id(instance_id)\n            for instance_id in set(instance_ids)\n        ]\n\n        self._emit_event(\n            INSTANCE_REQUIREMENT_CHANGED_TOPIC,\n            {\"instances\": instances},\n            sender,\n        )\n\n    def _bulk_instance_parent_change_finished(\n        self,\n        instance_ids: list[str],\n        sender: Optional[str],\n    ):\n        if not instance_ids:\n            return\n\n        instances = [\n            self.get_instance_by_id(instance_id)\n            for instance_id in set(instance_ids)\n        ]\n\n        self._emit_event(\n            INSTANCE_PARENT_CHANGED_TOPIC,\n            {\"instances\": instances},\n            sender,\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.collection_shared_data","title":"<code>collection_shared_data</code>  <code>property</code>","text":"<p>Access to shared data that can be used during creator's collection.</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: Shared data.</p> <p>Raises:</p> Type Description <code>UnavailableSharedData</code> <p>When called out of collection phase.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.context_has_changed","title":"<code>context_has_changed</code>  <code>property</code>","text":"<p>Host context has changed.</p> <p>As context is used project, folder, task name and workfile path if host does support workfiles.</p> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>Context changed.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.host_is_valid","title":"<code>host_is_valid</code>  <code>property</code>","text":"<p>Is host valid for creation.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.log","title":"<code>log</code>  <code>property</code>","text":"<p>Dynamic access to logger.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.publish_attributes","title":"<code>publish_attributes</code>  <code>property</code>","text":"<p>Access to global publish attributes.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.sorted_autocreators","title":"<code>sorted_autocreators</code>  <code>property</code>","text":"<p>Sorted auto-creators by 'order' attribute.</p> <p>Returns:</p> Type Description <code>list['AutoCreator']</code> <p>list[AutoCreator]: Sorted plugins by 'order' value.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.sorted_creators","title":"<code>sorted_creators</code>  <code>property</code>","text":"<p>Sorted creators by 'order' attribute.</p> <p>Returns:</p> Type Description <code>list['BaseCreator']</code> <p>list[BaseCreator]: Sorted creator plugins by 'order' value.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.add_create_attr_defs_change_callback","title":"<code>add_create_attr_defs_change_callback(callback)</code>","text":"<p>Register callback to listen create attribute changes.</p> <p>Create plugin changed attribute definitions of instance.</p> <p>Data structure of event:</p> <pre><code>```python\n{\n    \"instances\": [CreatedInstance, ...],\n    \"create_context\": CreateContext\n}\n```\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>callback</code> <code>Callable</code> <p>Callback function that will be called when create attributes changed.</p> required <p>Returns:</p> Name Type Description <code>EventCallback</code> <code>'EventCallback'</code> <p>Created callback object which can be used to stop listening.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def add_create_attr_defs_change_callback(\n    self, callback: Callable\n) -&gt; \"EventCallback\":\n    \"\"\"Register callback to listen create attribute changes.\n\n    Create plugin changed attribute definitions of instance.\n\n    Data structure of event:\n\n        ```python\n        {\n            \"instances\": [CreatedInstance, ...],\n            \"create_context\": CreateContext\n        }\n        ```\n\n    Args:\n        callback (Callable): Callback function that will be called when\n            create attributes changed.\n\n    Returns:\n        EventCallback: Created callback object which can be used to\n            stop listening.\n\n    \"\"\"\n    return self._event_hub.add_callback(\n        CREATE_ATTR_DEFS_CHANGED_TOPIC, callback\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.add_instance_parent_change_callback","title":"<code>add_instance_parent_change_callback(callback)</code>","text":"<p>Register callback to listen to instance parent changes.</p> <p>Instance changed parent or parent flags.</p> <p>Data structure of event:</p> <pre><code>```python\n{\n    \"instances\": [CreatedInstance, ...],\n    \"create_context\": CreateContext\n}\n```\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>callback</code> <code>Callable</code> <p>Callback function that will be called when instance requirement changed.</p> required <p>Returns:</p> Name Type Description <code>EventCallback</code> <code>'EventCallback'</code> <p>Created callback object which can be used to stop listening.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def add_instance_parent_change_callback(\n    self, callback: Callable\n) -&gt; \"EventCallback\":\n    \"\"\"Register callback to listen to instance parent changes.\n\n    Instance changed parent or parent flags.\n\n    Data structure of event:\n\n        ```python\n        {\n            \"instances\": [CreatedInstance, ...],\n            \"create_context\": CreateContext\n        }\n        ```\n\n    Args:\n        callback (Callable): Callback function that will be called when\n            instance requirement changed.\n\n    Returns:\n        EventCallback: Created callback object which can be used to\n            stop listening.\n\n    \"\"\"\n    return self._event_hub.add_callback(\n        INSTANCE_PARENT_CHANGED_TOPIC, callback\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.add_instance_requirement_change_callback","title":"<code>add_instance_requirement_change_callback(callback)</code>","text":"<p>Register callback to listen to instance requirement changes.</p> <p>Instance changed requirement of active state.</p> <p>Data structure of event:</p> <pre><code>```python\n{\n    \"instances\": [CreatedInstance, ...],\n    \"create_context\": CreateContext\n}\n```\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>callback</code> <code>Callable</code> <p>Callback function that will be called when instance requirement changed.</p> required <p>Returns:</p> Name Type Description <code>EventCallback</code> <code>'EventCallback'</code> <p>Created callback object which can be used to stop listening.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def add_instance_requirement_change_callback(\n    self, callback: Callable\n) -&gt; \"EventCallback\":\n    \"\"\"Register callback to listen to instance requirement changes.\n\n    Instance changed requirement of active state.\n\n    Data structure of event:\n\n        ```python\n        {\n            \"instances\": [CreatedInstance, ...],\n            \"create_context\": CreateContext\n        }\n        ```\n\n    Args:\n        callback (Callable): Callback function that will be called when\n            instance requirement changed.\n\n    Returns:\n        EventCallback: Created callback object which can be used to\n            stop listening.\n\n    \"\"\"\n    return self._event_hub.add_callback(\n        INSTANCE_REQUIREMENT_CHANGED_TOPIC, callback\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.add_instances_added_callback","title":"<code>add_instances_added_callback(callback)</code>","text":"<p>Register callback for added instances.</p> <p>Event is triggered when instances are already available in context     and have set create/publish attribute definitions.</p> <p>Data structure of event:</p> <pre><code>```python\n{\n    \"instances\": [CreatedInstance, ...],\n    \"create_context\": CreateContext\n}\n```\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>callback</code> <code>Callable</code> <p>Callback function that will be called when instances are added to context.</p> required <p>Returns:</p> Name Type Description <code>EventCallback</code> <code>'EventCallback'</code> <p>Created callback object which can be used to stop listening.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def add_instances_added_callback(\n    self, callback: Callable\n) -&gt; \"EventCallback\":\n    \"\"\"Register callback for added instances.\n\n    Event is triggered when instances are already available in context\n        and have set create/publish attribute definitions.\n\n    Data structure of event:\n\n        ```python\n        {\n            \"instances\": [CreatedInstance, ...],\n            \"create_context\": CreateContext\n        }\n        ```\n\n    Args:\n        callback (Callable): Callback function that will be called when\n            instances are added to context.\n\n    Returns:\n        EventCallback: Created callback object which can be used to\n            stop listening.\n\n    \"\"\"\n    return self._event_hub.add_callback(INSTANCE_ADDED_TOPIC, callback)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.add_instances_removed_callback","title":"<code>add_instances_removed_callback(callback)</code>","text":"<p>Register callback for removed instances.</p> <p>Event is triggered when instances are already removed from context.</p> <p>Data structure of event:</p> <pre><code>```python\n{\n    \"instances\": [CreatedInstance, ...],\n    \"create_context\": CreateContext\n}\n```\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>callback</code> <code>Callable</code> <p>Callback function that will be called when instances are removed from context.</p> required <p>Returns:</p> Name Type Description <code>EventCallback</code> <code>'EventCallback'</code> <p>Created callback object which can be used to stop listening.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def add_instances_removed_callback(\n    self, callback: Callable\n) -&gt; \"EventCallback\":\n    \"\"\"Register callback for removed instances.\n\n    Event is triggered when instances are already removed from context.\n\n    Data structure of event:\n\n        ```python\n        {\n            \"instances\": [CreatedInstance, ...],\n            \"create_context\": CreateContext\n        }\n        ```\n\n    Args:\n        callback (Callable): Callback function that will be called when\n            instances are removed from context.\n\n    Returns:\n        EventCallback: Created callback object which can be used to\n            stop listening.\n\n    \"\"\"\n    return self._event_hub.add_callback(INSTANCE_REMOVED_TOPIC, callback)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.add_pre_create_attr_defs_change_callback","title":"<code>add_pre_create_attr_defs_change_callback(callback)</code>","text":"<p>Register callback to listen pre-create attribute changes.</p> <p>Create plugin can trigger refresh of pre-create attributes. Usage of     this event is mainly for publisher UI.</p> <p>Data structure of event:</p> <pre><code>```python\n{\n    \"identifiers\": [\"create_plugin_identifier\"],\n    \"create_context\": CreateContext\n}\n```\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>callback</code> <code>Callable</code> <p>Callback function that will be called when pre-create attributes should be refreshed.</p> required <p>Returns:</p> Name Type Description <code>EventCallback</code> <code>'EventCallback'</code> <p>Created callback object which can be used to stop listening.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def add_pre_create_attr_defs_change_callback(\n    self, callback: Callable\n) -&gt; \"EventCallback\":\n    \"\"\"Register callback to listen pre-create attribute changes.\n\n    Create plugin can trigger refresh of pre-create attributes. Usage of\n        this event is mainly for publisher UI.\n\n    Data structure of event:\n\n        ```python\n        {\n            \"identifiers\": [\"create_plugin_identifier\"],\n            \"create_context\": CreateContext\n        }\n        ```\n\n    Args:\n        callback (Callable): Callback function that will be called when\n            pre-create attributes should be refreshed.\n\n    Returns:\n        EventCallback: Created callback object which can be used to\n            stop listening.\n\n    \"\"\"\n    return self._event_hub.add_callback(\n        PRE_CREATE_ATTR_DEFS_CHANGED_TOPIC, callback\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.add_publish_attr_defs_change_callback","title":"<code>add_publish_attr_defs_change_callback(callback)</code>","text":"<p>Register callback to listen publish attribute changes.</p> <p>Publish plugin changed attribute definitions of instance of context.</p> <p>Data structure of event:</p> <pre><code>```python\n{\n    \"instance_changes\": {\n        None: {\n            \"instance\": None,\n            \"plugin_names\": {\"PluginA\"},\n        }\n        \"&lt;instance_id&gt;\": {\n            \"instance\": CreatedInstance,\n            \"plugin_names\": {\"PluginB\", \"PluginC\"},\n        }\n    },\n    \"create_context\": CreateContext\n}\n```\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>callback</code> <code>Callable</code> <p>Callback function that will be called when publish attributes changed.</p> required <p>Returns:</p> Name Type Description <code>EventCallback</code> <code>'EventCallback'</code> <p>Created callback object which can be used to stop listening.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def add_publish_attr_defs_change_callback(\n    self, callback: Callable\n) -&gt; \"EventCallback\":\n    \"\"\"Register callback to listen publish attribute changes.\n\n    Publish plugin changed attribute definitions of instance of context.\n\n    Data structure of event:\n\n        ```python\n        {\n            \"instance_changes\": {\n                None: {\n                    \"instance\": None,\n                    \"plugin_names\": {\"PluginA\"},\n                }\n                \"&lt;instance_id&gt;\": {\n                    \"instance\": CreatedInstance,\n                    \"plugin_names\": {\"PluginB\", \"PluginC\"},\n                }\n            },\n            \"create_context\": CreateContext\n        }\n        ```\n\n    Args:\n        callback (Callable): Callback function that will be called when\n            publish attributes changed.\n\n    Returns:\n        EventCallback: Created callback object which can be used to\n            stop listening.\n\n    \"\"\"\n    return self._event_hub.add_callback(\n        PUBLISH_ATTR_DEFS_CHANGED_TOPIC, callback\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.add_value_changed_callback","title":"<code>add_value_changed_callback(callback)</code>","text":"<p>Register callback to listen value changes.</p> <p>Event is triggered when any value changes on any instance or     context data.</p> <p>Data structure of event:</p> <pre><code>```python\n{\n    \"changes\": [\n        {\n            \"instance\": CreatedInstance,\n            \"changes\": {\n                \"folderPath\": \"/new/folder/path\",\n                \"creator_attributes\": {\n                    \"attr_1\": \"value_1\"\n                }\n            }\n        }\n    ],\n    \"create_context\": CreateContext\n}\n```\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>callback</code> <code>Callable</code> <p>Callback function that will be called when value changed.</p> required <p>Returns:</p> Name Type Description <code>EventCallback</code> <code>'EventCallback'</code> <p>Created callback object which can be used to stop listening.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def add_value_changed_callback(\n    self, callback: Callable\n) -&gt; \"EventCallback\":\n    \"\"\"Register callback to listen value changes.\n\n    Event is triggered when any value changes on any instance or\n        context data.\n\n    Data structure of event:\n\n        ```python\n        {\n            \"changes\": [\n                {\n                    \"instance\": CreatedInstance,\n                    \"changes\": {\n                        \"folderPath\": \"/new/folder/path\",\n                        \"creator_attributes\": {\n                            \"attr_1\": \"value_1\"\n                        }\n                    }\n                }\n            ],\n            \"create_context\": CreateContext\n        }\n        ```\n\n    Args:\n        callback (Callable): Callback function that will be called when\n            value changed.\n\n    Returns:\n        EventCallback: Created callback object which can be used to\n            stop listening.\n\n    \"\"\"\n    return self._event_hub.add_callback(VALUE_CHANGED_TOPIC, callback)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.bulk_instances_collection","title":"<code>bulk_instances_collection(sender=None)</code>","text":"<p>DEPRECATED use 'bulk_add_instances' instead.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>@contextmanager\ndef bulk_instances_collection(self, sender: Optional[str] = None):\n    \"\"\"DEPRECATED use 'bulk_add_instances' instead.\"\"\"\n    # TODO add warning\n    with self.bulk_add_instances(sender) as bulk_info:\n        yield bulk_info\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.context_data_changes","title":"<code>context_data_changes()</code>","text":"<p>Changes of attributes.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def context_data_changes(self) -&gt; TrackChangesItem:\n    \"\"\"Changes of attributes.\"\"\"\n\n    return TrackChangesItem(\n        self._original_context_data, self.context_data_to_store()\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.context_data_to_store","title":"<code>context_data_to_store()</code>","text":"<p>Data that should be stored by host function.</p> <p>The same data should be returned on loading.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def context_data_to_store(self) -&gt; dict[str, Any]:\n    \"\"\"Data that should be stored by host function.\n\n    The same data should be returned on loading.\n    \"\"\"\n    return {\n        \"publish_attributes\": self._publish_attributes.data_to_store()\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.create","title":"<code>create(creator_identifier, variant, folder_entity=None, task_entity=None, pre_create_data=None, active=None, product_type=None)</code>","text":"<p>Trigger create of plugins with standartized arguments.</p> <p>Arguments 'folder_entity' and 'task_name' use current context as default values. If only 'task_entity' is provided it will be overridden by task name from current context. If 'task_name' is not provided when 'folder_entity' is, it is considered that task name is not specified, which can lead to error if product name template requires task name.</p> <p>Parameters:</p> Name Type Description Default <code>creator_identifier</code> <code>str</code> <p>Identifier of creator plugin.</p> required <code>variant</code> <code>str</code> <p>Variant used for product name.</p> required <code>folder_entity</code> <code>dict[str, Any]</code> <p>Folder entity which define context of creation (possible context of created instance/s).</p> <code>None</code> <code>task_entity</code> <code>dict[str, Any]</code> <p>Task entity.</p> <code>None</code> <code>pre_create_data</code> <code>dict[str, Any]</code> <p>Pre-create attribute values.</p> <code>None</code> <code>active</code> <code>Optional[bool]</code> <p>Whether the created instance defaults to be active or not.</p> <code>None</code> <code>product_type</code> <code>str</code> <p>Specific product type to use.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>Any</code> <code>Any</code> <p>Output of triggered creator's 'create' method.</p> <p>Raises:</p> Type Description <code>CreatorError</code> <p>If creator was not found or folder is empty.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def create(\n    self,\n    creator_identifier: str,\n    variant: str,\n    folder_entity: Optional[dict[str, Any]] = None,\n    task_entity: Optional[dict[str, Any]] = None,\n    pre_create_data: Optional[dict[str, Any]] = None,\n    active: Optional[bool] = None,\n    product_type: Optional[str] = None,\n) -&gt; Any:\n    \"\"\"Trigger create of plugins with standartized arguments.\n\n    Arguments 'folder_entity' and 'task_name' use current context as\n    default values. If only 'task_entity' is provided it will be\n    overridden by task name from current context. If 'task_name' is not\n    provided when 'folder_entity' is, it is considered that task name is\n    not specified, which can lead to error if product name template\n    requires task name.\n\n    Args:\n        creator_identifier (str): Identifier of creator plugin.\n        variant (str): Variant used for product name.\n        folder_entity (dict[str, Any]): Folder entity which define context\n            of creation (possible context of created instance/s).\n        task_entity (dict[str, Any]): Task entity.\n        pre_create_data (dict[str, Any]): Pre-create attribute values.\n        active (Optional[bool]): Whether the created instance defaults\n            to be active or not.\n        product_type (str): Specific product type to use.\n\n    Returns:\n        Any: Output of triggered creator's 'create' method.\n\n    Raises:\n        CreatorError: If creator was not found or folder is empty.\n\n    \"\"\"\n    creator = self._get_creator_in_create(creator_identifier)\n\n    project_name = self.project_name\n    if folder_entity is None:\n        folder_path = self.get_current_folder_path()\n        folder_entity = ayon_api.get_folder_by_path(\n            project_name, folder_path\n        )\n        if folder_entity is None:\n            raise CreatorError(f\"Folder '{folder_path}' was not found\")\n\n    if task_entity is None:\n        current_task_name = self.get_current_task_name()\n        if current_task_name:\n            task_entity = ayon_api.get_task_by_name(\n                project_name, folder_entity[\"id\"], current_task_name\n            )\n\n    # Handle not passed product type\n    if product_type is None:\n        for product_type_item in creator.get_product_type_items():\n            product_type = product_type_item.product_type\n            break\n        else:\n            product_type = creator.product_base_type\n\n    if pre_create_data is None:\n        pre_create_data = {}\n\n    precreate_attr_defs = []\n    # Hidden creators do not have or need the pre-create attributes.\n    if isinstance(creator, Creator):\n        precreate_attr_defs = creator.get_pre_create_attr_defs()\n\n    # Create default values of precreate data\n    _pre_create_data = get_default_values(precreate_attr_defs)\n    # Update passed precreate data to default values\n    # TODO validate types\n    _pre_create_data.update(pre_create_data)\n\n    project_entity = self.get_current_project_entity()\n    args = (\n        project_name,\n        folder_entity,\n        task_entity,\n        variant,\n        self.host_name,\n    )\n    kwargs = {\n        # Backwards compatibility for 'project_entity' argument (24/07/08)\n        \"project_entity\": project_entity,\n        # Backwards compatibility for 'product_type' argument (26/01/19)\n        \"product_type\": product_type,\n    }\n    for kwarg in (\"product_type\", \"project_entity\"):\n        if not is_func_signature_supported(\n            creator.get_product_name, *args, **kwargs\n        ):\n            kwargs.pop(kwarg)\n\n    product_name = creator.get_product_name(*args, **kwargs)\n\n    instance_data = {\n        \"folderPath\": folder_entity[\"path\"],\n        \"task\": task_entity[\"name\"] if task_entity else None,\n        \"productType\": product_type,\n        # Add product base type if supported. Fallback to\n        #   older attribute 'product_type'\n        \"productBaseType\": (\n            creator.product_base_type or creator.product_type),\n        \"variant\": variant\n    }\n\n    if active is not None:\n        if not isinstance(active, bool):\n            self.log.warning(\n                \"CreateContext.create 'active' argument is not a bool. \"\n                f\"Converting {active} {type(active)} to bool.\")\n            active = bool(active)\n        instance_data[\"active\"] = active\n\n    with self.bulk_add_instances():\n        return creator.create(\n            product_name,\n            instance_data,\n            _pre_create_data\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.create_plugin_pre_create_attr_defs_changed","title":"<code>create_plugin_pre_create_attr_defs_changed(identifier)</code>","text":"<p>Create plugin pre-create attributes changed.</p> <p>Triggered by 'Creator'.</p> <p>Parameters:</p> Name Type Description Default <code>identifier</code> <code>str</code> <p>Create plugin identifier.</p> required Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def create_plugin_pre_create_attr_defs_changed(\n    self, identifier: str\n) -&gt; None:\n    \"\"\"Create plugin pre-create attributes changed.\n\n    Triggered by 'Creator'.\n\n    Args:\n        identifier (str): Create plugin identifier.\n\n    \"\"\"\n    with self.bulk_pre_create_attr_defs_change() as bulk_item:\n        bulk_item.append(identifier)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.create_with_unified_error","title":"<code>create_with_unified_error(identifier, *args, **kwargs)</code>","text":"<p>Trigger create but raise only one error if anything fails.</p> <p>Added to raise unified exception. Capture any possible issues and reraise it with unified information.</p> <p>Parameters:</p> Name Type Description Default <code>identifier</code> <code>str</code> <p>Identifier of creator.</p> required <code>*args</code> <p>Arguments for create method.</p> <code>()</code> <code>**kwargs</code> <p>Keyword argument for create method.</p> <code>{}</code> <p>Raises:</p> Type Description <code>CreatorsCreateFailed</code> <p>When creation fails due to any possible reason. If anything goes wrong this is only possible exception the method should raise.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def create_with_unified_error(\n    self, identifier: str, *args, **kwargs\n) -&gt; Any:\n    \"\"\"Trigger create but raise only one error if anything fails.\n\n    Added to raise unified exception. Capture any possible issues and\n    reraise it with unified information.\n\n    Args:\n        identifier (str): Identifier of creator.\n        *args: Arguments for create method.\n        **kwargs: Keyword argument for create method.\n\n    Raises:\n        CreatorsCreateFailed: When creation fails due to any possible\n            reason. If anything goes wrong this is only possible exception\n            the method should raise.\n\n    \"\"\"\n    result, fail_info = self._create_with_unified_error(\n        identifier, None, *args, **kwargs\n    )\n    if fail_info is not None:\n        raise CreatorsCreateFailed([fail_info])\n    return result\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.creator_adds_instance","title":"<code>creator_adds_instance(instance)</code>","text":"<p>Creator adds new instance to context.</p> <p>Instances should be added only from creators.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>CreatedInstance</code> <p>Instance with prepared data from creator.</p> required <p>TODO: Rename method to more suit.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def creator_adds_instance(self, instance: \"CreatedInstance\") -&gt; None:\n    \"\"\"Creator adds new instance to context.\n\n    Instances should be added only from creators.\n\n    Args:\n        instance(CreatedInstance): Instance with prepared data from\n            creator.\n\n    TODO: Rename method to more suit.\n    \"\"\"\n    # Add instance to instances list\n    if instance.id in self._instances_by_id:\n        self.log.warning((\n            \"Instance with id {} is already added to context.\"\n        ).format(instance.id))\n        return\n\n    self._instances_by_id[instance.id] = instance\n\n    # Add instance to be validated inside 'bulk_add_instances'\n    #   context manager if is inside bulk\n    with self.bulk_add_instances() as bulk_info:\n        bulk_info.append(instance)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.creator_removed_instance","title":"<code>creator_removed_instance(instance)</code>","text":"<p>When creator removes instance context should be acknowledged.</p> <p>If creator removes instance context should know about it to avoid possible issues in the session.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>CreatedInstance</code> <p>Object of instance which was removed from scene metadata.</p> required Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def creator_removed_instance(self, instance: \"CreatedInstance\") -&gt; None:\n    \"\"\"When creator removes instance context should be acknowledged.\n\n    If creator removes instance context should know about it to avoid\n    possible issues in the session.\n\n    Args:\n        instance (CreatedInstance): Object of instance which was removed\n            from scene metadata.\n    \"\"\"\n\n    self._remove_instances([instance])\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.execute_autocreators","title":"<code>execute_autocreators()</code>","text":"<p>Execute discovered AutoCreator plugins.</p> <p>Reset instances if any autocreator executed properly.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def execute_autocreators(self) -&gt; None:\n    \"\"\"Execute discovered AutoCreator plugins.\n\n    Reset instances if any autocreator executed properly.\n    \"\"\"\n\n    failed_info = []\n    for creator in self.sorted_autocreators:\n        identifier = creator.identifier\n        _, fail_info = self._create_with_unified_error(identifier, creator)\n        if fail_info is not None:\n            failed_info.append(fail_info)\n\n    if failed_info:\n        raise CreatorsCreateFailed(failed_info)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.find_convertor_items","title":"<code>find_convertor_items()</code>","text":"<p>Go through convertor plugins to look for items to convert.</p> <p>Raises:</p> Type Description <code>ConvertorsFindFailed</code> <p>When one or more convertors fails during finding.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def find_convertor_items(self) -&gt; None:\n    \"\"\"Go through convertor plugins to look for items to convert.\n\n    Raises:\n        ConvertorsFindFailed: When one or more convertors fails during\n            finding.\n    \"\"\"\n\n    self.convertor_items_by_id = {}\n\n    failed_info = []\n    for convertor in self.convertors_plugins.values():\n        try:\n            convertor.find_instances()\n\n        except:  # noqa: E722\n            failed_info.append(\n                prepare_failed_convertor_operation_info(\n                    convertor.identifier, sys.exc_info()\n                )\n            )\n            self.log.warning(\n                \"Failed to find instances of convertor \\\"{}\\\"\".format(\n                    convertor.identifier\n                ),\n                exc_info=True\n            )\n\n    if failed_info:\n        raise ConvertorsFindFailed(failed_info)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.get_current_folder_entity","title":"<code>get_current_folder_entity()</code>","text":"<p>Folder entity for current context folder.</p> <p>Returns:</p> Type Description <code>Optional[dict[str, Any]]</code> <p>Optional[dict[str, Any]]: Folder entity.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def get_current_folder_entity(self) -&gt; Optional[dict[str, Any]]:\n    \"\"\"Folder entity for current context folder.\n\n    Returns:\n        Optional[dict[str, Any]]: Folder entity.\n\n    \"\"\"\n    if self._current_folder_entity is not _NOT_SET:\n        return copy.deepcopy(self._current_folder_entity)\n\n    folder_path = self.get_current_folder_path()\n    self._current_folder_entity = self.get_folder_entity(folder_path)\n    return copy.deepcopy(self._current_folder_entity)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.get_current_folder_path","title":"<code>get_current_folder_path()</code>","text":"<p>Folder path which was used as current context on context reset.</p> <p>Returns:</p> Type Description <code>Optional[str]</code> <p>Optional[str]: Folder path.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def get_current_folder_path(self) -&gt; Optional[str]:\n    \"\"\"Folder path which was used as current context on context reset.\n\n    Returns:\n        Optional[str]: Folder path.\n\n    \"\"\"\n    return self._current_folder_path\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.get_current_project_anatomy","title":"<code>get_current_project_anatomy()</code>","text":"<p>Project anatomy for current project.</p> <p>Returns:</p> Name Type Description <code>Anatomy</code> <code>Anatomy</code> <p>Anatomy object ready to be used.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def get_current_project_anatomy(self) -&gt; Anatomy:\n    \"\"\"Project anatomy for current project.\n\n    Returns:\n        Anatomy: Anatomy object ready to be used.\n    \"\"\"\n\n    if self._current_project_anatomy is None:\n        self._current_project_anatomy = Anatomy(\n            self._current_project_name)\n    return self._current_project_anatomy\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.get_current_project_entity","title":"<code>get_current_project_entity()</code>","text":"<p>Project entity for current context project.</p> <p>Returns:</p> Type Description <code>Optional[dict[str, Any]]</code> <p>Optional[dict[str, Any]]: Folder entity.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def get_current_project_entity(self) -&gt; Optional[dict[str, Any]]:\n    \"\"\"Project entity for current context project.\n\n    Returns:\n        Optional[dict[str, Any]]: Folder entity.\n\n    \"\"\"\n    if self._current_project_entity is not _NOT_SET:\n        return copy.deepcopy(self._current_project_entity)\n    project_entity = None\n    project_name = self.get_current_project_name()\n    if project_name:\n        project_entity = ayon_api.get_project(project_name)\n    self._current_project_entity = project_entity\n    return copy.deepcopy(self._current_project_entity)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.get_current_project_name","title":"<code>get_current_project_name()</code>","text":"<p>Project name which was used as current context on context reset.</p> <p>Returns:</p> Type Description <code>str</code> <p>Optional[str]: Project name.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def get_current_project_name(self) -&gt; str:\n    \"\"\"Project name which was used as current context on context reset.\n\n    Returns:\n        Optional[str]: Project name.\n\n    \"\"\"\n    return self._current_project_name\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.get_current_task_entity","title":"<code>get_current_task_entity()</code>","text":"<p>Task entity for current context task.</p> <p>Returns:</p> Type Description <code>Optional[dict[str, Any]]</code> <p>Optional[dict[str, Any]]: Task entity.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def get_current_task_entity(self) -&gt; Optional[dict[str, Any]]:\n    \"\"\"Task entity for current context task.\n\n    Returns:\n        Optional[dict[str, Any]]: Task entity.\n\n    \"\"\"\n    if self._current_task_entity is not _NOT_SET:\n        return copy.deepcopy(self._current_task_entity)\n\n    folder_path = self.get_current_folder_path()\n    task_name = self.get_current_task_name()\n    self._current_task_entity = self.get_task_entity(\n        folder_path, task_name\n    )\n    return copy.deepcopy(self._current_task_entity)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.get_current_task_name","title":"<code>get_current_task_name()</code>","text":"<p>Task name which was used as current context on context reset.</p> <p>Returns:</p> Type Description <code>Optional[str]</code> <p>Optional[str]: Task name.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def get_current_task_name(self) -&gt; Optional[str]:\n    \"\"\"Task name which was used as current context on context reset.\n\n    Returns:\n        Optional[str]: Task name.\n\n    \"\"\"\n    return self._current_task_name\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.get_current_task_type","title":"<code>get_current_task_type()</code>","text":"<p>Task type which was used as current context on context reset.</p> <p>Returns:</p> Type Description <code>Optional[str]</code> <p>Optional[str]: Task type.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def get_current_task_type(self) -&gt; Optional[str]:\n    \"\"\"Task type which was used as current context on context reset.\n\n    Returns:\n        Optional[str]: Task type.\n\n    \"\"\"\n    if self._current_task_type is _NOT_SET:\n        task_type = None\n        task_entity = self.get_current_task_entity()\n        if task_entity:\n            task_type = task_entity[\"taskType\"]\n        self._current_task_type = task_type\n    return self._current_task_type\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.get_current_workfile_path","title":"<code>get_current_workfile_path()</code>","text":"<p>Workfile path which was opened on context reset.</p> <p>Returns:</p> Type Description <code>Optional[str]</code> <p>Optional[str]: Workfile path.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def get_current_workfile_path(self) -&gt; Optional[str]:\n    \"\"\"Workfile path which was opened on context reset.\n\n    Returns:\n        Optional[str]: Workfile path.\n\n    \"\"\"\n    return self._current_workfile_path\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.get_folder_entities","title":"<code>get_folder_entities(folder_paths)</code>","text":"<p>Get folder entities by paths.</p> <p>Parameters:</p> Name Type Description Default <code>folder_paths</code> <code>Iterable[str]</code> <p>Folder paths.</p> required <p>Returns:</p> Type Description <code>dict[str, Optional[dict[str, Any]]]</code> <p>dict[str, Optional[dict[str, Any]]]: Folder entities by path.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def get_folder_entities(\n    self, folder_paths: Iterable[str]\n) -&gt; dict[str, Optional[dict[str, Any]]]:\n    \"\"\"Get folder entities by paths.\n\n    Args:\n        folder_paths (Iterable[str]): Folder paths.\n\n    Returns:\n        dict[str, Optional[dict[str, Any]]]: Folder entities by path.\n\n    \"\"\"\n    output = {\n        folder_path: None\n        for folder_path in folder_paths\n    }\n    remainder_paths = set()\n    for folder_path in output:\n        # Skip invalid folder paths (folder name or empty path)\n        if not folder_path or \"/\" not in folder_path:\n            continue\n\n        if folder_path not in self._folder_entities_by_path:\n            remainder_paths.add(folder_path)\n            continue\n\n        output[folder_path] = self._folder_entities_by_path[folder_path]\n\n    if not remainder_paths:\n        return output\n\n    found_paths = set()\n    for folder_entity in ayon_api.get_folders(\n        self.project_name,\n        folder_paths=remainder_paths,\n    ):\n        folder_path = folder_entity[\"path\"]\n        found_paths.add(folder_path)\n        output[folder_path] = folder_entity\n        self._folder_entities_by_path[folder_path] = folder_entity\n\n    # Cache empty folder entities\n    for path in remainder_paths - found_paths:\n        self._folder_entities_by_path[path] = None\n\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.get_folder_entity","title":"<code>get_folder_entity(folder_path)</code>","text":"<p>Get folder entity by path.</p> <p>Entities are cached until reset.</p> <p>Parameters:</p> Name Type Description Default <code>folder_path</code> <code>Optional[str]</code> <p>Folder path.</p> required <p>Returns:</p> Type Description <code>Optional[dict[str, Any]]</code> <p>Optional[dict[str, Any]]: Folder entity.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def get_folder_entity(\n    self,\n    folder_path: Optional[str],\n) -&gt; Optional[dict[str, Any]]:\n    \"\"\"Get folder entity by path.\n\n    Entities are cached until reset.\n\n    Args:\n        folder_path (Optional[str]): Folder path.\n\n    Returns:\n        Optional[dict[str, Any]]: Folder entity.\n\n    \"\"\"\n    if not folder_path:\n        return None\n    return self.get_folder_entities([folder_path]).get(folder_path)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.get_host_misssing_methods","title":"<code>get_host_misssing_methods(host)</code>  <code>classmethod</code>","text":"<p>Collect missing methods from host.</p> <p>Parameters:</p> Name Type Description Default <code>host</code> <code>ModuleType</code> <p>Host implementaion.</p> required Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>@classmethod\ndef get_host_misssing_methods(cls, host):\n    \"\"\"Collect missing methods from host.\n\n    Args:\n        host(ModuleType): Host implementaion.\n    \"\"\"\n\n    missing = set(\n        IPublishHost.get_missing_publish_methods(host)\n    )\n    return missing\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.get_instance_by_id","title":"<code>get_instance_by_id(instance_id)</code>","text":"<p>Receive instance by id.</p> <p>Parameters:</p> Name Type Description Default <code>instance_id</code> <code>str</code> <p>Instance id.</p> required <p>Returns:</p> Type Description <code>Optional['CreatedInstance']</code> <p>Optional[CreatedInstance]: Instance or None if instance with given id is not available.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def get_instance_by_id(\n    self, instance_id: str\n) -&gt; Optional[\"CreatedInstance\"]:\n    \"\"\"Receive instance by id.\n\n    Args:\n        instance_id (str): Instance id.\n\n    Returns:\n        Optional[CreatedInstance]: Instance or None if instance with\n            given id is not available.\n\n    \"\"\"\n    return self._instances_by_id.get(instance_id)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.get_instances_context_info","title":"<code>get_instances_context_info(instances=None)</code>","text":"<p>Validate 'folder' and 'task' instance context.</p> <p>Parameters:</p> Name Type Description Default <code>instances</code> <code>Optional[Iterable[CreatedInstance]]</code> <p>Instances to validate. If not provided all instances are validated.</p> <code>None</code> <p>Returns:</p> Type Description <code>dict[str, InstanceContextInfo]</code> <p>dict[str, InstanceContextInfo]: Validation results by instance id.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def get_instances_context_info(\n    self, instances: Optional[Iterable[\"CreatedInstance\"]] = None\n) -&gt; dict[str, InstanceContextInfo]:\n    \"\"\"Validate 'folder' and 'task' instance context.\n\n    Args:\n        instances (Optional[Iterable[CreatedInstance]]): Instances to\n            validate. If not provided all instances are validated.\n\n    Returns:\n        dict[str, InstanceContextInfo]: Validation results by instance id.\n\n    \"\"\"\n    # Use all instances from context if 'instances' are not passed\n    if instances is None:\n        instances = self._instances_by_id.values()\n    instances = tuple(instances)\n    info_by_instance_id = {\n        instance.id: InstanceContextInfo(\n            instance.get(\"folderPath\"),\n            instance.get(\"task\"),\n            False,\n            False,\n        )\n        for instance in instances\n    }\n\n    # Skip if instances are empty\n    if not info_by_instance_id:\n        return info_by_instance_id\n\n    project_name = self.project_name\n\n    to_validate = []\n    task_names_by_folder_path = collections.defaultdict(set)\n    for instance in instances:\n        context_info = info_by_instance_id[instance.id]\n        if instance.has_promised_context:\n            context_info.folder_is_valid = True\n            context_info.task_is_valid = True\n            # NOTE missing task type\n            continue\n        # TODO allow context promise\n        folder_path = context_info.folder_path\n        if not folder_path:\n            continue\n\n        if folder_path in self._folder_entities_by_path:\n            folder_entity = self._folder_entities_by_path[folder_path]\n            if folder_entity is None:\n                continue\n            context_info.folder_is_valid = True\n\n        task_name = context_info.task_name\n        if task_name is not None:\n            tasks_cache = self._task_names_by_folder_path.get(folder_path)\n            if tasks_cache is not None:\n                context_info.task_is_valid = task_name in tasks_cache\n                continue\n\n        to_validate.append(instance)\n        task_names_by_folder_path[folder_path].add(task_name)\n\n    if not to_validate:\n        return info_by_instance_id\n\n    # Backwards compatibility for cases where folder name is set instead\n    #   of folder path\n    folder_paths = set()\n    task_names_by_folder_name = {}\n    task_names_by_folder_path_clean = {}\n    for folder_path, task_names in task_names_by_folder_path.items():\n        if folder_path is None:\n            continue\n\n        clean_task_names = {\n            task_name\n            for task_name in task_names\n            if task_name\n        }\n\n        if \"/\" not in folder_path:\n            task_names_by_folder_name[folder_path] = clean_task_names\n            continue\n\n        folder_paths.add(folder_path)\n        if not clean_task_names:\n            continue\n\n        task_names_by_folder_path_clean[folder_path] = clean_task_names\n\n    folder_paths_by_name = collections.defaultdict(list)\n    if task_names_by_folder_name:\n        for folder_entity in ayon_api.get_folders(\n            project_name,\n            folder_names=task_names_by_folder_name.keys(),\n            fields={\"name\", \"path\"}\n        ):\n            folder_name = folder_entity[\"name\"]\n            folder_path = folder_entity[\"path\"]\n            folder_paths_by_name[folder_name].append(folder_path)\n\n    folder_path_by_name = {}\n    for folder_name, paths in folder_paths_by_name.items():\n        if len(paths) != 1:\n            continue\n        path = paths[0]\n        folder_path_by_name[folder_name] = path\n        folder_paths.add(path)\n        clean_task_names = task_names_by_folder_name[folder_name]\n        if not clean_task_names:\n            continue\n        folder_task_names = task_names_by_folder_path_clean.setdefault(\n            path, set()\n        )\n        folder_task_names |= clean_task_names\n\n    folder_entities_by_path = self.get_folder_entities(folder_paths)\n    task_entities_by_folder_path = self.get_task_entities(\n        task_names_by_folder_path_clean\n    )\n\n    for instance in to_validate:\n        folder_path = instance[\"folderPath\"]\n        task_name = instance.get(\"task\")\n        if folder_path and \"/\" not in folder_path:\n            new_folder_path = folder_path_by_name.get(folder_path)\n            if new_folder_path:\n                folder_path = new_folder_path\n                instance[\"folderPath\"] = new_folder_path\n\n        folder_entity = folder_entities_by_path.get(folder_path)\n        if not folder_entity:\n            continue\n        context_info = info_by_instance_id[instance.id]\n        context_info.folder_is_valid = True\n\n        if (\n            not task_name\n            or task_name in task_entities_by_folder_path[folder_path]\n        ):\n            context_info.task_is_valid = True\n    return info_by_instance_id\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.get_instances_task_entities","title":"<code>get_instances_task_entities(instances=None)</code>","text":"<p>Get task entities for instances.</p> <p>Parameters:</p> Name Type Description Default <code>instances</code> <code>Optional[Iterable[CreatedInstance]]</code> <p>Instances to get task entities. If not provided all instances are used.</p> <code>None</code> <p>Returns:</p> Type Description <code>dict[str, Optional[dict[str, Any]]]</code> <p>dict[str, Optional[dict[str, Any]]]: Task entity by instance id.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def get_instances_task_entities(\n    self, instances: Optional[Iterable[\"CreatedInstance\"]] = None\n) -&gt; dict[str, Optional[dict[str, Any]]]:\n    \"\"\"Get task entities for instances.\n\n    Args:\n        instances (Optional[Iterable[CreatedInstance]]): Instances to\n            get task entities. If not provided all instances are used.\n\n    Returns:\n        dict[str, Optional[dict[str, Any]]]: Task entity by instance id.\n\n    \"\"\"\n    if instances is None:\n        instances = self._instances_by_id.values()\n    instances = list(instances)\n\n    output = {\n        instance.id: None\n        for instance in instances\n    }\n    if not instances:\n        return output\n\n    filtered_instances = []\n    task_names_by_folder_path = collections.defaultdict(set)\n    for instance in instances:\n        folder_path = instance.get(\"folderPath\")\n        task_name = instance.get(\"task\")\n        if not folder_path or not task_name:\n            continue\n        filtered_instances.append(instance)\n        task_names_by_folder_path[folder_path].add(task_name)\n\n    task_entities_by_folder_path = self.get_task_entities(\n        task_names_by_folder_path\n    )\n    for instance in filtered_instances:\n        folder_path = instance[\"folderPath\"]\n        task_name = instance[\"task\"]\n        output[instance.id] = (\n            task_entities_by_folder_path[folder_path][task_name]\n        )\n\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.get_sorted_creators","title":"<code>get_sorted_creators(identifiers=None)</code>","text":"<p>Sorted creators by 'order' attribute.</p> <p>Parameters:</p> Name Type Description Default <code>identifiers</code> <code>Optional[Iterable[str]]</code> <p>Filter creators by identifiers. All creators are returned if 'None' is passed.</p> <code>None</code> <p>Returns:</p> Type Description <code>list['BaseCreator']</code> <p>list[BaseCreator]: Sorted creator plugins by 'order' value.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def get_sorted_creators(\n    self, identifiers: Optional[Iterable[str]] = None\n) -&gt; list[\"BaseCreator\"]:\n    \"\"\"Sorted creators by 'order' attribute.\n\n    Args:\n        identifiers (Optional[Iterable[str]]): Filter creators by\n            identifiers. All creators are returned if 'None' is passed.\n\n    Returns:\n        list[BaseCreator]: Sorted creator plugins by 'order' value.\n\n    \"\"\"\n    if identifiers is not None:\n        identifiers = set(identifiers)\n        creators = [\n            creator\n            for identifier, creator in self.creators.items()\n            if identifier in identifiers\n        ]\n    else:\n        creators = self.creators.values()\n\n    return sorted(\n        creators, key=lambda creator: creator.order\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.get_task_entities","title":"<code>get_task_entities(task_names_by_folder_paths)</code>","text":"<p>Get task entities by folder path and task name.</p> <p>Entities are cached until reset.</p> <p>Parameters:</p> Name Type Description Default <code>task_names_by_folder_paths</code> <code>dict[str, set[str]]</code> <p>Task names by folder path.</p> required <p>Returns:</p> Type Description <code>dict[str, dict[str, Optional[dict[str, Any]]]]</code> <p>dict[str, dict[str, dict[str, Any]]]: Task entities by folder path and task name.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def get_task_entities(\n    self,\n    task_names_by_folder_paths: dict[str, set[str]]\n) -&gt; dict[str, dict[str, Optional[dict[str, Any]]]]:\n    \"\"\"Get task entities by folder path and task name.\n\n    Entities are cached until reset.\n\n    Args:\n        task_names_by_folder_paths (dict[str, set[str]]): Task names by\n            folder path.\n\n    Returns:\n        dict[str, dict[str, dict[str, Any]]]: Task entities by folder path\n            and task name.\n\n    \"\"\"\n    output = {}\n    for folder_path, task_names in task_names_by_folder_paths.items():\n        if folder_path is None:\n            continue\n        output[folder_path] = {\n            task_name: None\n            for task_name in task_names\n            if task_name is not None\n        }\n\n    missing_folder_paths = set()\n    for folder_path, output_task_entities_by_name in output.items():\n        if not output_task_entities_by_name:\n            continue\n\n        if folder_path not in self._task_ids_by_folder_path:\n            missing_folder_paths.add(folder_path)\n            continue\n\n        all_tasks_filled = True\n        task_ids = self._task_ids_by_folder_path[folder_path]\n        task_entities_by_name = {}\n        for task_id in task_ids:\n            task_entity = self._task_entities_by_id.get(task_id)\n            if task_entity is None:\n                all_tasks_filled = False\n                continue\n            task_entities_by_name[task_entity[\"name\"]] = task_entity\n\n        any_missing = False\n        for task_name in set(output_task_entities_by_name):\n            task_entity = task_entities_by_name.get(task_name)\n            if task_entity is None:\n                any_missing = True\n                continue\n\n            output_task_entities_by_name[task_name] = task_entity\n\n        if any_missing and not all_tasks_filled:\n            missing_folder_paths.add(folder_path)\n\n    if not missing_folder_paths:\n        return output\n\n    folder_entities_by_path = self.get_folder_entities(\n        missing_folder_paths\n    )\n    folder_path_by_id = {}\n    for folder_path, folder_entity in folder_entities_by_path.items():\n        if folder_entity is not None:\n            folder_path_by_id[folder_entity[\"id\"]] = folder_path\n\n    if not folder_path_by_id:\n        return output\n\n    task_entities_by_parent_id = collections.defaultdict(list)\n    for task_entity in ayon_api.get_tasks(\n        self.project_name,\n        folder_ids=folder_path_by_id.keys()\n    ):\n        folder_id = task_entity[\"folderId\"]\n        task_entities_by_parent_id[folder_id].append(task_entity)\n\n    for folder_id, task_entities in task_entities_by_parent_id.items():\n        folder_path = folder_path_by_id[folder_id]\n        task_ids = set()\n        task_names = set()\n        for task_entity in task_entities:\n            task_id = task_entity[\"id\"]\n            task_name = task_entity[\"name\"]\n            task_ids.add(task_id)\n            task_names.add(task_name)\n            self._task_entities_by_id[task_id] = task_entity\n\n            output[folder_path][task_name] = task_entity\n        self._task_ids_by_folder_path[folder_path] = task_ids\n        self._task_names_by_folder_path[folder_path] = task_names\n\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.get_task_entity","title":"<code>get_task_entity(folder_path, task_name)</code>","text":"<p>Get task entity by name and folder path.</p> <p>Entities are cached until reset.</p> <p>Parameters:</p> Name Type Description Default <code>folder_path</code> <code>Optional[str]</code> <p>Folder path.</p> required <code>task_name</code> <code>Optional[str]</code> <p>Task name.</p> required <p>Returns:</p> Type Description <code>Optional[dict[str, Any]]</code> <p>Optional[dict[str, Any]]: Task entity.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def get_task_entity(\n    self,\n    folder_path: Optional[str],\n    task_name: Optional[str],\n) -&gt; Optional[dict[str, Any]]:\n    \"\"\"Get task entity by name and folder path.\n\n    Entities are cached until reset.\n\n    Args:\n        folder_path (Optional[str]): Folder path.\n        task_name (Optional[str]): Task name.\n\n    Returns:\n        Optional[dict[str, Any]]: Task entity.\n\n    \"\"\"\n    if not folder_path or not task_name:\n        return None\n\n    output = self.get_task_entities({folder_path: {task_name}})\n    return output.get(folder_path, {}).get(task_name)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.get_template_data","title":"<code>get_template_data(folder_path, task_name)</code>","text":"<p>Prepare template data for given context.</p> <p>Method is using cached entities and settings to prepare template data.</p> <p>Parameters:</p> Name Type Description Default <code>folder_path</code> <code>Optional[str]</code> <p>Folder path.</p> required <code>task_name</code> <code>Optional[str]</code> <p>Task name.</p> required <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: Template data.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def get_template_data(\n    self, folder_path: Optional[str], task_name: Optional[str]\n) -&gt; dict[str, Any]:\n    \"\"\"Prepare template data for given context.\n\n    Method is using cached entities and settings to prepare template data.\n\n    Args:\n        folder_path (Optional[str]): Folder path.\n        task_name (Optional[str]): Task name.\n\n    Returns:\n        dict[str, Any]: Template data.\n\n    \"\"\"\n    project_entity = self.get_current_project_entity()\n    folder_entity = task_entity = None\n    if folder_path:\n        folder_entity = self.get_folder_entity(folder_path)\n        if task_name and folder_entity:\n            task_entity = self.get_task_entity(folder_path, task_name)\n\n    return get_template_data(\n        project_entity,\n        folder_entity,\n        task_entity,\n        host_name=self.host_name,\n        settings=self.get_current_project_settings(),\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.instance_create_attr_defs_changed","title":"<code>instance_create_attr_defs_changed(instance_id)</code>","text":"<p>Instance attribute definitions changed.</p> <p>Triggered by instance 'CreatorAttributeValues' on instance.</p> <p>Parameters:</p> Name Type Description Default <code>instance_id</code> <code>str</code> <p>Instance id.</p> required Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def instance_create_attr_defs_changed(self, instance_id: str) -&gt; None:\n    \"\"\"Instance attribute definitions changed.\n\n    Triggered by instance 'CreatorAttributeValues' on instance.\n\n    Args:\n        instance_id (str): Instance id.\n\n    \"\"\"\n    if self._is_instance_events_ready(instance_id):\n        with self.bulk_create_attr_defs_change() as bulk_item:\n            bulk_item.append(instance_id)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.instance_parent_changed","title":"<code>instance_parent_changed(instance_id)</code>","text":"<p>Instance parent changed.</p> <p>Triggered by <code>CreatedInstance</code>.</p> <p>Parameters:</p> Name Type Description Default <code>instance_id</code> <code>Optional[str]</code> <p>Instance id.</p> required Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def instance_parent_changed(self, instance_id: str) -&gt; None:\n    \"\"\"Instance parent changed.\n\n    Triggered by `CreatedInstance`.\n\n    Args:\n        instance_id (Optional[str]): Instance id.\n\n    \"\"\"\n    if self._is_instance_events_ready(instance_id):\n        with self.bulk_instance_parent_change() as bulk_item:\n            bulk_item.append(instance_id)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.instance_publish_attr_defs_changed","title":"<code>instance_publish_attr_defs_changed(instance_id, plugin_name)</code>","text":"<p>Instance attribute definitions changed.</p> <p>Triggered by instance 'PublishAttributeValues' on instance.</p> <p>Parameters:</p> Name Type Description Default <code>instance_id</code> <code>Optional[str]</code> <p>Instance id or None for context.</p> required <code>plugin_name</code> <code>str</code> <p>Plugin name which attribute definitions were changed.</p> required Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def instance_publish_attr_defs_changed(\n    self, instance_id: Optional[str], plugin_name: str\n) -&gt; None:\n    \"\"\"Instance attribute definitions changed.\n\n    Triggered by instance 'PublishAttributeValues' on instance.\n\n    Args:\n        instance_id (Optional[str]): Instance id or None for context.\n        plugin_name (str): Plugin name which attribute definitions were\n            changed.\n\n    \"\"\"\n    if self._is_instance_events_ready(instance_id):\n        with self.bulk_publish_attr_defs_change() as bulk_item:\n            bulk_item.append((instance_id, plugin_name))\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.instance_requirement_changed","title":"<code>instance_requirement_changed(instance_id)</code>","text":"<p>Instance requirement changed.</p> <p>Triggered by <code>CreatedInstance</code>.</p> <p>Parameters:</p> Name Type Description Default <code>instance_id</code> <code>Optional[str]</code> <p>Instance id.</p> required Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def instance_requirement_changed(self, instance_id: str) -&gt; None:\n    \"\"\"Instance requirement changed.\n\n    Triggered by `CreatedInstance`.\n\n    Args:\n        instance_id (Optional[str]): Instance id.\n\n    \"\"\"\n    if self._is_instance_events_ready(instance_id):\n        with self.bulk_instance_requirement_change() as bulk_item:\n            bulk_item.append(instance_id)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.instance_values_changed","title":"<code>instance_values_changed(instance_id, new_values)</code>","text":"<p>Instance value changed.</p> <p>Triggered by `CreatedInstance, 'CreatorAttributeValues'     or 'PublishAttributeValues' on instance.</p> <p>Parameters:</p> Name Type Description Default <code>instance_id</code> <code>Optional[str]</code> <p>Instance id or None for context.</p> required <code>new_values</code> <code>dict[str, Any]</code> <p>Changed values.</p> required Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def instance_values_changed(\n    self, instance_id: Optional[str], new_values: dict[str, Any]\n) -&gt; None:\n    \"\"\"Instance value changed.\n\n    Triggered by `CreatedInstance, 'CreatorAttributeValues'\n        or 'PublishAttributeValues' on instance.\n\n    Args:\n        instance_id (Optional[str]): Instance id or None for context.\n        new_values (dict[str, Any]): Changed values.\n\n    \"\"\"\n    if self._is_instance_events_ready(instance_id):\n        with self.bulk_value_changes() as bulk_item:\n            bulk_item.append((instance_id, new_values))\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.publish_attribute_value_changed","title":"<code>publish_attribute_value_changed(plugin_name, value)</code>","text":"<p>Context publish attribute values changed.</p> <p>Triggered by instance 'PublishAttributeValues' on context.</p> <p>Parameters:</p> Name Type Description Default <code>plugin_name</code> <code>str</code> <p>Plugin name which changed value.</p> required <code>value</code> <code>dict[str, Any]</code> <p>Changed values.</p> required Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def publish_attribute_value_changed(\n    self, plugin_name: str, value: dict[str, Any]\n) -&gt; None:\n    \"\"\"Context publish attribute values changed.\n\n    Triggered by instance 'PublishAttributeValues' on context.\n\n    Args:\n        plugin_name (str): Plugin name which changed value.\n        value (dict[str, Any]): Changed values.\n\n    \"\"\"\n    self.instance_values_changed(\n        None,\n        {\n            \"publish_attributes\": {\n                plugin_name: value,\n            },\n        },\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.refresh_thumbnails","title":"<code>refresh_thumbnails()</code>","text":"<p>Cleanup thumbnail paths.</p> <p>Remove all thumbnail filepaths that are empty or lead to files which does not exist or of instances that are not available anymore.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def refresh_thumbnails(self) -&gt; None:\n    \"\"\"Cleanup thumbnail paths.\n\n    Remove all thumbnail filepaths that are empty or lead to files which\n    does not exist or of instances that are not available anymore.\n    \"\"\"\n\n    invalid = set()\n    for instance_id, path in self.thumbnail_paths_by_instance_id.items():\n        instance_available = True\n        if instance_id is not None:\n            instance_available = instance_id in self._instances_by_id\n\n        if (\n            not instance_available\n            or not path\n            or not os.path.exists(path)\n        ):\n            invalid.add(instance_id)\n\n    for instance_id in invalid:\n        self.thumbnail_paths_by_instance_id.pop(instance_id)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.remove_instances","title":"<code>remove_instances(instances, sender=None)</code>","text":"<p>Remove instances from context.</p> <p>All instances that don't have creator identifier leading to existing     creator are just removed from context.</p> <p>Parameters:</p> Name Type Description Default <code>instances</code> <code>list[CreatedInstance]</code> <p>Instances that should be removed. Remove logic is done using creator, which may require to do other cleanup than just remove instance from context.</p> required <code>sender</code> <code>Optional[str]</code> <p>Sender of the event.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def remove_instances(\n    self,\n    instances: list[\"CreatedInstance\"],\n    sender: Optional[str] = None,\n) -&gt; None:\n    \"\"\"Remove instances from context.\n\n    All instances that don't have creator identifier leading to existing\n        creator are just removed from context.\n\n    Args:\n        instances (list[CreatedInstance]): Instances that should be\n            removed. Remove logic is done using creator, which may require\n            to do other cleanup than just remove instance from context.\n        sender (Optional[str]): Sender of the event.\n\n    \"\"\"\n    instance_ids_by_parent_id = collections.defaultdict(set)\n    for instance in self.instances:\n        instance_ids_by_parent_id[instance.parent_instance_id].add(\n            instance.id\n        )\n\n    instances_to_remove = list(instances)\n    ids_to_remove = {\n        instance.id\n        for instance in instances_to_remove\n    }\n    _queue = collections.deque()\n    _queue.extend(instances_to_remove)\n    # Add children with parent lifetime flag\n    while _queue:\n        instance = _queue.popleft()\n        ids_to_remove.add(instance.id)\n        children_ids = instance_ids_by_parent_id[instance.id]\n        for children_id in children_ids:\n            if children_id in ids_to_remove:\n                continue\n            instance = self._instances_by_id[children_id]\n            if instance.parent_flags &amp; ParentFlags.parent_lifetime:\n                instances_to_remove.append(instance)\n                ids_to_remove.add(instance.id)\n                _queue.append(instance)\n\n    instances_by_identifier = collections.defaultdict(list)\n    for instance in instances_to_remove:\n        identifier = instance.creator_identifier\n        instances_by_identifier[identifier].append(instance)\n\n    # Just remove instances from context if creator is not available\n    missing_creators = set(instances_by_identifier) - set(self.creators)\n    miss_creator_instances = []\n    for identifier in missing_creators:\n        miss_creator_instances.extend(instances_by_identifier[identifier])\n\n    with self.bulk_remove_instances(sender):\n        self._remove_instances(miss_creator_instances, sender)\n\n        error_message = \"Instances removement of creator \\\"{}\\\" failed. {}\"\n        failed_info = []\n        # Remove instances by creator plugin order\n        for creator in self.get_sorted_creators(\n            instances_by_identifier.keys()\n        ):\n            identifier = creator.identifier\n            # Filter instances by current state of 'CreateContext'\n            # - in case instances were already removed as subroutine of\n            #   previous create plugin.\n            creator_instances = [\n                instance\n                for instance in instances_by_identifier[identifier]\n                if instance.id in self._instances_by_id\n            ]\n            if not creator_instances:\n                continue\n\n            label = creator.label\n            failed = False\n            add_traceback = False\n            exc_info = None\n            try:\n                creator.remove_instances(creator_instances)\n\n            except CreatorError:\n                failed = True\n                exc_info = sys.exc_info()\n                self.log.warning(\n                    error_message.format(identifier, exc_info[1])\n                )\n\n            except (KeyboardInterrupt, SystemExit):\n                raise\n\n            except:  # noqa: E722\n                failed = True\n                add_traceback = True\n                exc_info = sys.exc_info()\n                self.log.warning(\n                    error_message.format(identifier, \"\"),\n                    exc_info=True\n                )\n\n            if failed:\n                failed_info.append(\n                    prepare_failed_creator_operation_info(\n                        identifier, label, exc_info, add_traceback\n                    )\n                )\n\n    if failed_info:\n        raise CreatorsRemoveFailed(failed_info)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.reset","title":"<code>reset(discover_publish_plugins=True)</code>","text":"<p>Reset context with all plugins and instances.</p> <p>All changes will be lost if were not saved explicitely.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def reset(self, discover_publish_plugins: bool = True) -&gt; None:\n    \"\"\"Reset context with all plugins and instances.\n\n    All changes will be lost if were not saved explicitely.\n    \"\"\"\n\n    self.reset_preparation()\n\n    self.reset_current_context()\n    self.reset_plugins(discover_publish_plugins)\n    self.reset_context_data()\n\n    with self.bulk_add_instances():\n        self.reset_instances()\n        self.find_convertor_items()\n        self.execute_autocreators()\n\n    self.reset_finalization()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.reset_context_data","title":"<code>reset_context_data()</code>","text":"<p>Reload context data using host implementation.</p> <p>These data are not related to any instance but may be needed for whole publishing.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def reset_context_data(self) -&gt; None:\n    \"\"\"Reload context data using host implementation.\n\n    These data are not related to any instance but may be needed for whole\n    publishing.\n    \"\"\"\n    if not self.host_is_valid:\n        self._original_context_data = {}\n        self._publish_attributes = PublishAttributes(self, {})\n        return\n\n    original_data = self.host.get_context_data() or {}\n    self._original_context_data = copy.deepcopy(original_data)\n\n    publish_attributes = original_data.get(\"publish_attributes\") or {}\n\n    self._publish_attributes = PublishAttributes(\n        self, publish_attributes\n    )\n\n    for plugin in self.plugins_with_defs:\n        if is_func_signature_supported(\n            plugin.convert_attribute_values, self, None\n        ):\n            plugin.convert_attribute_values(self, None)\n\n        elif not plugin.__instanceEnabled__:\n            output = plugin.convert_attribute_values(publish_attributes)\n            if output:\n                publish_attributes.update(output)\n\n    for plugin in self.plugins_with_defs:\n        try:\n            attr_defs = plugin.get_attr_defs_for_context(self)\n        except Exception:\n            self.log.error(\n                \"Failed to get attribute definitions\"\n                f\" from plugin '{plugin.__name__}'.\",\n                exc_info=True\n            )\n            continue\n\n        if not attr_defs:\n            continue\n        self._publish_attributes.set_publish_plugin_attr_defs(\n            plugin.__name__, attr_defs\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.reset_current_context","title":"<code>reset_current_context()</code>","text":"<p>Refresh current context.</p> <p>Reset is based on optional host implementation of <code>get_current_context</code> function.</p> <p>Some hosts have ability to change context file without using workfiles tool but that change is not propagated to 'os.environ'.</p> Todos <p>UI: Current context should be also checked on save - compare     initial values vs. current values. Related to UI checks: Current workfile can be also considered     as current context information as that's where the metadata     are stored. We should store the workfile (if is available) too.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def reset_current_context(self) -&gt; None:\n    \"\"\"Refresh current context.\n\n    Reset is based on optional host implementation of `get_current_context`\n    function.\n\n    Some hosts have ability to change context file without using workfiles\n    tool but that change is not propagated to 'os.environ'.\n\n    Todos:\n        UI: Current context should be also checked on save - compare\n            initial values vs. current values.\n        Related to UI checks: Current workfile can be also considered\n            as current context information as that's where the metadata\n            are stored. We should store the workfile (if is available) too.\n    \"\"\"\n\n    project_name, folder_path, task_name, workfile_path = (\n        self._get_current_host_context()\n    )\n\n    self._current_project_name = project_name\n    self._current_folder_path = folder_path\n    self._current_task_name = task_name\n    self._current_workfile_path = workfile_path\n\n    self._current_project_entity = _NOT_SET\n    self._current_folder_entity = _NOT_SET\n    self._current_task_entity = _NOT_SET\n    self._current_task_type = _NOT_SET\n\n    self._current_project_anatomy = None\n    self._current_project_settings = None\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.reset_finalization","title":"<code>reset_finalization()</code>","text":"<p>Cleanup of attributes after reset.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def reset_finalization(self) -&gt; None:\n    \"\"\"Cleanup of attributes after reset.\"\"\"\n\n    # Stop access to collection shared data\n    self._collection_shared_data = None\n    self.refresh_thumbnails()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.reset_instances","title":"<code>reset_instances()</code>","text":"<p>Reload instances</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def reset_instances(self) -&gt; None:\n    \"\"\"Reload instances\"\"\"\n    self._instances_by_id = collections.OrderedDict()\n\n    # Collect instances\n    error_message = \"Collection of instances for creator {} failed. {}\"\n    failed_info = []\n    for creator in self.sorted_creators:\n        label = creator.label\n        identifier = creator.identifier\n        failed = False\n        add_traceback = False\n        exc_info = None\n        try:\n            creator.collect_instances()\n\n        except CreatorError:\n            failed = True\n            exc_info = sys.exc_info()\n            self.log.warning(error_message.format(identifier, exc_info[1]))\n\n        except:  # noqa: E722\n            failed = True\n            add_traceback = True\n            exc_info = sys.exc_info()\n            self.log.warning(\n                error_message.format(identifier, \"\"),\n                exc_info=True\n            )\n\n        if failed:\n            failed_info.append(\n                prepare_failed_creator_operation_info(\n                    identifier, label, exc_info, add_traceback\n                )\n            )\n\n    if failed_info:\n        raise CreatorsCollectionFailed(failed_info)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.reset_plugins","title":"<code>reset_plugins(discover_publish_plugins=True)</code>","text":"<p>Reload plugins.</p> <p>Reloads creators from preregistered paths and can load publish plugins if it's enabled on context.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def reset_plugins(self, discover_publish_plugins: bool = True) -&gt; None:\n    \"\"\"Reload plugins.\n\n    Reloads creators from preregistered paths and can load publish plugins\n    if it's enabled on context.\n    \"\"\"\n\n    self._reset_publish_plugins(discover_publish_plugins)\n    self._reset_creator_plugins()\n    self._reset_convertor_plugins()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.reset_preparation","title":"<code>reset_preparation()</code>","text":"<p>Prepare attributes that must be prepared/cleaned before reset.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def reset_preparation(self) -&gt; None:\n    \"\"\"Prepare attributes that must be prepared/cleaned before reset.\"\"\"\n\n    # Give ability to store shared data for collection phase\n    self._collection_shared_data = {}\n\n    self._folder_entities_by_path = {}\n    self._task_entities_by_id = {}\n\n    self._task_ids_by_folder_path = {}\n    self._task_names_by_folder_path = {}\n\n    self._event_hub.clear_callbacks()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.run_convertor","title":"<code>run_convertor(convertor_identifier)</code>","text":"<p>Run convertor plugin by identifier.</p> <p>Conversion is skipped if convertor is not available.</p> <p>Parameters:</p> Name Type Description Default <code>convertor_identifier</code> <code>str</code> <p>Identifier of convertor.</p> required Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def run_convertor(self, convertor_identifier: str) -&gt; None:\n    \"\"\"Run convertor plugin by identifier.\n\n    Conversion is skipped if convertor is not available.\n\n    Args:\n        convertor_identifier (str): Identifier of convertor.\n    \"\"\"\n\n    convertor = self.convertors_plugins.get(convertor_identifier)\n    if convertor is not None:\n        convertor.convert()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.run_convertors","title":"<code>run_convertors(convertor_identifiers)</code>","text":"<p>Run convertor plugins by identifiers.</p> <p>Conversion is skipped if convertor is not available. It is recommended to trigger reset after conversion to reload instances.</p> <p>Parameters:</p> Name Type Description Default <code>convertor_identifiers</code> <code>Iterable[str]</code> <p>Identifiers of convertors to run.</p> required <p>Raises:</p> Type Description <code>ConvertorsConversionFailed</code> <p>When one or more convertors fails.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def run_convertors(self, convertor_identifiers: Iterable[str]) -&gt; None:\n    \"\"\"Run convertor plugins by identifiers.\n\n    Conversion is skipped if convertor is not available. It is recommended\n    to trigger reset after conversion to reload instances.\n\n    Args:\n        convertor_identifiers (Iterable[str]): Identifiers of convertors\n            to run.\n\n    Raises:\n        ConvertorsConversionFailed: When one or more convertors fails.\n    \"\"\"\n\n    failed_info = []\n    for convertor_identifier in convertor_identifiers:\n        try:\n            self.run_convertor(convertor_identifier)\n\n        except:  # noqa: E722\n            failed_info.append(\n                prepare_failed_convertor_operation_info(\n                    convertor_identifier, sys.exc_info()\n                )\n            )\n            self.log.warning(\n                \"Failed to convert instances of convertor \\\"{}\\\"\".format(\n                    convertor_identifier\n                ),\n                exc_info=True\n            )\n\n    if failed_info:\n        raise ConvertorsConversionFailed(failed_info)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.save_changes","title":"<code>save_changes()</code>","text":"<p>Save changes. Update all changed values.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def save_changes(self) -&gt; None:\n    \"\"\"Save changes. Update all changed values.\"\"\"\n    if not self.host_is_valid:\n        missing_methods = self.get_host_misssing_methods(self.host)\n        raise HostMissRequiredMethod(self.host, missing_methods)\n\n    self._save_context_changes()\n    self._save_instance_changes()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.set_context_publish_plugin_attr_defs","title":"<code>set_context_publish_plugin_attr_defs(plugin_name, attr_defs)</code>","text":"<p>Set attribute definitions for CreateContext publish plugin.</p> <p>Parameters:</p> Name Type Description Default <code>plugin_name</code> <code>str</code> <p>Name of publish plugin.</p> required <code>attr_defs</code> <code>list[AbstractAttrDef]</code> <p>Attribute definitions.</p> required Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def set_context_publish_plugin_attr_defs(\n    self, plugin_name: str, attr_defs: list[\"AbstractAttrDef\"]\n) -&gt; None:\n    \"\"\"Set attribute definitions for CreateContext publish plugin.\n\n    Args:\n        plugin_name(str): Name of publish plugin.\n        attr_defs(list[AbstractAttrDef]): Attribute definitions.\n\n    \"\"\"\n    self.publish_attributes.set_publish_plugin_attr_defs(\n        plugin_name, attr_defs\n    )\n    self.instance_publish_attr_defs_changed(\n        None, plugin_name\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.trigger_create_button_callback","title":"<code>trigger_create_button_callback(button_name, instance_ids, *, identifier=None)</code>","text":"<p>Trigger pre-create button callback.</p> <p>Parameters:</p> Name Type Description Default <code>button_name</code> <code>str</code> <p>Button name.</p> required <code>instance_ids</code> <code>list[str]</code> <p>List of instance ids for which the button will be triggered. The button will be triggered only for instances that have the button.</p> required <code>identifier</code> <code>str | None</code> <p>Creator identifier. If None, the button will be triggered for all creators.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def trigger_create_button_callback(\n    self,\n    button_name: str,\n    instance_ids: list[str],\n    *,\n    identifier: str | None = None,\n) -&gt; None:\n    \"\"\"Trigger pre-create button callback.\n\n    Args:\n        button_name (str): Button name.\n        instance_ids (list[str]): List of instance ids for which\n            the button will be triggered. The button will be triggered\n            only for instances that have the button.\n        identifier (str | None): Creator identifier. If None,\n            the button will be triggered for all creators.\n\n    \"\"\"\n    info_by_identifier = {}\n    for instance_id in instance_ids:\n        instance = self.instances_by_id.get(instance_id)\n        if instance is None:\n            continue\n\n        plugin_id = instance.creator_identifier\n        if identifier is not None and plugin_id != identifier:\n            continue\n\n        attr_def = instance.creator_attributes.get_attr_def(button_name)\n        if not isinstance(attr_def, ButtonDef):\n            continue\n\n        info = info_by_identifier.get(plugin_id)\n        if info is None:\n            info = {\n                \"attr_def\": attr_def,\n                \"instance_ids\": []\n            }\n            info_by_identifier[plugin_id] = info\n\n        info[\"instance_ids\"].append(instance_id)\n\n    for info in info_by_identifier.values():\n        attr_def = info[\"attr_def\"]\n\n        callback_info = ButtonCallbackInfo(\n            create_context=self,\n            instance_ids=info[\"instance_ids\"],\n        )\n        callback = attr_def.get_callback()\n        if is_func_signature_supported(callback, callback_info):\n            callback(callback_info)\n        else:\n            callback()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.trigger_pre_create_button_callback","title":"<code>trigger_pre_create_button_callback(identifier, button_name)</code>","text":"<p>Trigger pre-create button callback.</p> <p>Parameters:</p> Name Type Description Default <code>identifier</code> <code>str</code> <p>Creator identifier.</p> required <code>button_name</code> <code>str</code> <p>Button name.</p> required Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def trigger_pre_create_button_callback(\n    self, identifier: str, button_name: str\n) -&gt; None:\n    \"\"\"Trigger pre-create button callback.\n\n    Args:\n        identifier (str): Creator identifier.\n        button_name (str): Button name.\n\n    Raises:\n        ValueError if create plugin was not found, does not support\n            pre-create attributes, or button was not found.\n\n    \"\"\"\n    create_plugin = self.creators.get(identifier)\n    if create_plugin is None:\n        return\n\n    if not hasattr(create_plugin, \"get_pre_create_attr_defs\"):\n        return\n\n    pre_create_attributes = create_plugin.get_pre_create_attr_defs()\n    for attr_def in pre_create_attributes:\n        if attr_def.key == button_name:\n            attr_def.trigger()\n            return\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreateContext.trigger_publish_button_callback","title":"<code>trigger_publish_button_callback(plugin_name, button_name, instance_ids)</code>","text":"<p>Trigger the publish plugin button callback.</p> <p>Parameters:</p> Name Type Description Default <code>plugin_name</code> <code>str</code> <p>Plugin name.</p> required <code>button_name</code> <code>str</code> <p>Button name.</p> required <code>instance_ids</code> <code>list[str | None]</code> <p>List of instance ids for which the button will be triggered. The button will be triggered only for instances that have the button. 'None' is used for context selection.</p> required Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def trigger_publish_button_callback(\n    self,\n    plugin_name: str,\n    button_name: str,\n    instance_ids: list[str | None],\n) -&gt; None:\n    \"\"\"Trigger the publish plugin button callback.\n\n    Args:\n        plugin_name (str): Plugin name.\n        button_name (str): Button name.\n        instance_ids (list[str | None]): List of instance ids for which\n            the button will be triggered. The button will be triggered\n            only for instances that have the button. 'None' is used for\n            context selection.\n\n    \"\"\"\n    filtered_instance_ids = []\n    attr_def = None\n    for instance_id in instance_ids:\n        if instance_id is None:\n            publish_attributes = self.publish_attributes\n        else:\n            instance = self.get_instance_by_id(instance_id)\n            if instance is None:\n                continue\n            publish_attributes = instance.publish_attributes\n\n        plugin_attributes = publish_attributes.get(plugin_name)\n        if not plugin_attributes:\n            continue\n\n        _attr_def = plugin_attributes.get_attr_def(button_name)\n        if not isinstance(_attr_def, ButtonDef):\n            continue\n\n        if attr_def is None:\n            attr_def = _attr_def\n\n        filtered_instance_ids.append(instance_id)\n\n    if attr_def is None:\n        return\n\n    info = ButtonCallbackInfo(\n        create_context=self,\n        instance_ids=filtered_instance_ids,\n    )\n    callback = attr_def.get_callback()\n    if is_func_signature_supported(callback, info):\n        callback(info)\n    else:\n        callback()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreatedInstance","title":"<code>CreatedInstance</code>","text":"<p>Instance entity with data that will be stored to workfile.</p> <p>I think <code>data</code> must be required argument containing all minimum information about instance like \"folderPath\" and \"task\" and all data used for filling product name as creators may have custom data for product name filling.</p> Notes <p>Object have 2 possible initialization. One using 'creator' object which     is recommended for api usage. Second by passing information about     creator.</p> <p>Parameters:</p> Name Type Description Default <code>product_type</code> <code>str</code> <p>Product type that will be created.</p> required <code>product_name</code> <code>str</code> <p>Name of product that will be created.</p> required <code>data</code> <code>Dict[str, Any]</code> <p>Data used for filling product name or override data from already existing instance.</p> required <code>creator</code> <code>BaseCreator</code> <p>Creator responsible for instance.</p> required <code>product_base_type</code> <code>Optional[str]</code> <p>Product base type that will be created. If not provided then product base type is taken from creator plugin. If creator does not have product base type then deprecation warning is raised.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>class CreatedInstance:\n    \"\"\"Instance entity with data that will be stored to workfile.\n\n    I think `data` must be required argument containing all minimum information\n    about instance like \"folderPath\" and \"task\" and all data used for filling\n    product name as creators may have custom data for product name filling.\n\n    Notes:\n        Object have 2 possible initialization. One using 'creator' object which\n            is recommended for api usage. Second by passing information about\n            creator.\n\n    Args:\n        product_type (str): Product type that will be created.\n        product_name (str): Name of product that will be created.\n        data (Dict[str, Any]): Data used for filling product name or override\n            data from already existing instance.\n        creator (BaseCreator): Creator responsible for instance.\n        product_base_type (Optional[str]): Product base type that will be\n            created. If not provided then product base type is taken from\n            creator plugin. If creator does not have product base type then\n            deprecation warning is raised.\n    \"\"\"\n\n    # Keys that can't be changed or removed from data after loading using\n    #   creator.\n    # - 'creator_attributes' and 'publish_attributes' can change values of\n    #   their individual children but not on their own\n    __immutable_keys = (\n        \"id\",\n        \"instance_id\",\n        \"productType\",\n        \"productBaseType\",\n        \"creator_identifier\",\n        \"creator_attributes\",\n        \"publish_attributes\"\n    )\n    # Keys that can be changed, but should not be removed from instance\n    __required_keys = {\n        \"folderPath\": None,\n        \"task\": None,\n        \"productName\": None,\n        \"active\": True,\n    }\n\n    def __init__(\n        self,\n        product_type: str,\n        product_name: str,\n        data: Dict[str, Any],\n        creator: \"BaseCreator\",\n        transient_data: Optional[Dict[str, Any]] = None,\n        product_base_type: Optional[str] = None\n    ):\n        \"\"\"Initialize CreatedInstance.\"\"\"\n        # fallback to product type for backward compatibility\n        if not product_base_type:\n            product_base_type = creator.product_base_type or product_type\n\n        self._creator = creator\n        creator_identifier = creator.identifier\n        group_label = creator.get_group_label()\n        creator_label = creator.label\n\n        self._creator_label = creator_label\n        self._group_label = group_label or creator_identifier\n\n        # Instance members may have actions on them\n        # TODO implement members logic\n        self._members = []\n\n        # Data that can be used for lifetime of object\n        if transient_data is None:\n            transient_data = {}\n        self._transient_data = transient_data\n        self._is_mandatory: bool = False\n        self._parent_instance_id: Optional[str] = None\n        self._parent_flags: int = 0\n\n        # Create a copy of passed data to avoid changing them on the fly\n        data = copy.deepcopy(data or {})\n\n        # Pop dictionary values that will be converted to objects to be able\n        #   catch changes\n        orig_creator_attributes = data.pop(\"creator_attributes\", None) or {}\n        orig_publish_attributes = data.pop(\"publish_attributes\", None) or {}\n\n        # Store original value of passed data\n        self._orig_data = copy.deepcopy(data)\n\n        # Pop 'productType' and 'productName' to prevent unexpected changes\n        data.pop(\"productType\", None)\n        data.pop(\"productName\", None)\n        # Backwards compatibility with OpenPype instances\n        data.pop(\"family\", None)\n        data.pop(\"subset\", None)\n\n        asset_name = data.pop(\"asset\", None)\n        if \"folderPath\" not in data:\n            data[\"folderPath\"] = asset_name\n\n        # QUESTION Does it make sense to have data stored as ordered dict?\n        self._data = collections.OrderedDict()\n        # QUESTION Do we need this \"id\" information on instance?\n        item_id = data.get(\"id\")\n        # TODO use only 'AYON_INSTANCE_ID' when all hosts support it\n        if item_id not in {AYON_INSTANCE_ID, AVALON_INSTANCE_ID}:\n            item_id = AYON_INSTANCE_ID\n        self._data[\"id\"] = item_id\n        self._data[\"productType\"] = product_type\n        self._data[\"productName\"] = product_name\n\n        self._data[\"productBaseType\"] = product_base_type\n\n        self._data[\"active\"] = data.get(\"active\", True)\n        self._data[\"creator_identifier\"] = creator_identifier\n\n        # Pop from source data all keys that are defined in `_data` before\n        #   this moment and through their values away\n        # - they should be the same and if are not then should not change\n        #   already set values\n        for key in self._data.keys():\n            if key in data:\n                data.pop(key)\n\n        self._data[\"variant\"] = self._data.get(\"variant\") or \"\"\n        # Stored creator specific attribute values\n        # {key: value}\n        creator_values = copy.deepcopy(orig_creator_attributes)\n\n        self._data[\"creator_attributes\"] = creator_values\n\n        # Stored publish specific attribute values\n        # {&lt;plugin name&gt;: {key: value}}\n        self._data[\"publish_attributes\"] = PublishAttributes(\n            self, orig_publish_attributes\n        )\n        if data:\n            self._data.update(data)\n\n        for key, default in self.__required_keys.items():\n            self._data.setdefault(key, default)\n\n        if not self._data.get(\"instance_id\"):\n            self._data[\"instance_id\"] = str(uuid4())\n\n        creator_attr_defs = creator.get_attr_defs_for_instance(self)\n        self.set_create_attr_defs(\n            creator_attr_defs, creator_values\n        )\n\n    def __str__(self):\n        return (\n            \"&lt;CreatedInstance {product[name]}\"\n            \" ({product[type]}[{creator_identifier}])&gt; {data}\"\n        ).format(\n            creator_identifier=self.creator_identifier,\n            product={\"name\": self.product_name, \"type\": self.product_type},\n            data=str(self._data)\n        )\n\n    # --- Dictionary like methods ---\n    def __getitem__(self, key):\n        return self._data[key]\n\n    def __contains__(self, key):\n        return key in self._data\n\n    def __setitem__(self, key, value):\n        # Validate immutable keys\n        if key in self.__immutable_keys:\n            if value == self._data.get(key):\n                return\n            # Raise exception if key is immutable and value has changed\n            raise ImmutableKeyError(key)\n\n        if key in self._data and self._data[key] == value:\n            return\n\n        if self.is_mandatory and key == \"active\" and value is not True:\n            raise ImmutableKeyError(\n                key,\n                \"Instance is mandatory and can't be disabled.\"\n            )\n\n        self._data[key] = value\n        self._create_context.instance_values_changed(\n            self.id, {key: value}\n        )\n\n    def get(self, key, default=None):\n        return self._data.get(key, default)\n\n    def pop(self, key, *args, **kwargs):\n        # Raise exception if is trying to pop key which is immutable\n        if key in self.__immutable_keys:\n            raise ImmutableKeyError(key)\n\n        has_key = key in self._data\n        output = self._data.pop(key, *args, **kwargs)\n        if has_key:\n            if key in self.__required_keys:\n                self._data[key] = self.__required_keys[key]\n            self._create_context.instance_values_changed(\n                self.id, {key: None}\n            )\n        return output\n\n    def keys(self):\n        return self._data.keys()\n\n    def values(self):\n        return self._data.values()\n\n    def items(self):\n        return self._data.items()\n    # ------\n\n    @property\n    def product_base_type(self) -&gt; str:\n        return self._data[\"productBaseType\"]\n\n    @property\n    def product_type(self) -&gt; str:\n        return self._data[\"productType\"]\n\n    @property\n    def product_name(self) -&gt; str:\n        return self._data[\"productName\"]\n\n    @property\n    def label(self) -&gt; str:\n        label = self._data.get(\"label\")\n        if not label:\n            label = self.product_name\n        return label\n\n    @property\n    def group_label(self) -&gt; str:\n        label = self._data.get(\"group\")\n        if label:\n            return label\n        return self._group_label\n\n    @property\n    def origin_data(self):\n        output = copy.deepcopy(self._orig_data)\n        output[\"creator_attributes\"] = self.creator_attributes.origin_data\n        output[\"publish_attributes\"] = self.publish_attributes.origin_data\n        return output\n\n    @property\n    def creator_identifier(self):\n        return self._data[\"creator_identifier\"]\n\n    @property\n    def creator_label(self):\n        return self._creator.label or self.creator_identifier\n\n    @property\n    def id(self):\n        \"\"\"Instance identifier.\n\n        Returns:\n            str: UUID of instance.\n        \"\"\"\n\n        return self._data[\"instance_id\"]\n\n    @property\n    def data(self):\n        \"\"\"Legacy access to data.\n\n        Access to data is needed to modify values.\n\n        Returns:\n            CreatedInstance: Object can be used as dictionary but with\n                validations of immutable keys.\n        \"\"\"\n\n        return self\n\n    @property\n    def transient_data(self):\n        \"\"\"Data stored for lifetime of instance object.\n\n        These data are not stored to scene and will be lost on object\n        deletion.\n\n        Can be used to store objects. In some host implementations is not\n        possible to reference to object in scene with some unique identifier\n        (e.g. node in Fusion.). In that case it is handy to store the object\n        here. Should be used that way only if instance data are stored on the\n        node itself.\n\n        Returns:\n            Dict[str, Any]: Dictionary object where you can store data related\n                to instance for lifetime of instance object.\n        \"\"\"\n\n        return self._transient_data\n\n    @property\n    def is_mandatory(self) -&gt; bool:\n        \"\"\"Check if instance is mandatory.\n\n        Returns:\n            bool: True if instance is mandatory, False otherwise.\n\n        \"\"\"\n        return self._is_mandatory\n\n    def set_mandatory(self, value: bool) -&gt; None:\n        \"\"\"Set instance as mandatory or not.\n\n        Mandatory instance can't be disabled in UI.\n\n        Args:\n            value (bool): True if instance should be mandatory, False\n                otherwise.\n\n        \"\"\"\n        if value is self._is_mandatory:\n            return\n        self._is_mandatory = value\n        if value is True:\n            self[\"active\"] = True\n        self._create_context.instance_requirement_changed(self.id)\n\n    @property\n    def parent_instance_id(self) -&gt; Optional[str]:\n        return self._parent_instance_id\n\n    @property\n    def parent_flags(self) -&gt; int:\n        return self._parent_flags\n\n    def set_parent(\n        self, instance_id: Optional[str], flags: int\n    ) -&gt; None:\n        \"\"\"Set parent instance id and parenting flags.\n\n        Args:\n            instance_id (Optional[str]): Parent instance id.\n            flags (int): Parenting flags.\n\n        \"\"\"\n        changed = False\n        if instance_id != self._parent_instance_id:\n            changed = True\n            self._parent_instance_id = instance_id\n\n        if flags is None:\n            flags = 0\n\n        if self._parent_flags != flags:\n            self._parent_flags = flags\n            changed = True\n\n        if changed:\n            self._create_context.instance_parent_changed(self.id)\n\n    def changes(self):\n        \"\"\"Calculate and return changes.\"\"\"\n\n        return TrackChangesItem(self.origin_data, self.data_to_store())\n\n    def mark_as_stored(self):\n        \"\"\"Should be called when instance data are stored.\n\n        Origin data are replaced by current data so changes are cleared.\n        \"\"\"\n\n        orig_keys = set(self._orig_data.keys())\n        for key, value in self._data.items():\n            orig_keys.discard(key)\n            if key in (\"creator_attributes\", \"publish_attributes\"):\n                continue\n            self._orig_data[key] = copy.deepcopy(value)\n\n        for key in orig_keys:\n            self._orig_data.pop(key)\n\n        self.creator_attributes.mark_as_stored()\n        self.publish_attributes.mark_as_stored()\n\n    @property\n    def creator_attributes(self) -&gt; CreatorAttributeValues:\n        return self._data[\"creator_attributes\"]\n\n    @property\n    def creator_attribute_defs(self) -&gt; list[AbstractAttrDef]:\n        \"\"\"Attribute definitions defined by creator plugin.\n\n        Returns:\n              list[AbstractAttrDef]: Attribute definitions.\n\n        \"\"\"\n        return self.creator_attributes.attr_defs\n\n    @property\n    def publish_attributes(self) -&gt; PublishAttributes:\n        return self._data[\"publish_attributes\"]\n\n    @property\n    def has_promised_context(self) -&gt; bool:\n        \"\"\"Get context data that are promised to be set by creator.\n\n        Returns:\n            bool: Has context that won't bo validated. Artist can't change\n                value when set to True.\n\n        \"\"\"\n        return self._transient_data.get(\"has_promised_context\", False)\n\n    def data_to_store(self):\n        \"\"\"Collect data that contain json parsable types.\n\n        It is possible to recreate the instance using these data.\n\n        Todos:\n            We probably don't need OrderedDict. When data are loaded they\n                are not ordered anymore.\n\n        Returns:\n            OrderedDict: Ordered dictionary with instance data.\n        \"\"\"\n\n        output = collections.OrderedDict()\n        for key, value in self._data.items():\n            if key in (\"creator_attributes\", \"publish_attributes\"):\n                continue\n            output[key] = value\n\n        if isinstance(self.creator_attributes, AttributeValues):\n            creator_attributes = self.creator_attributes.data_to_store()\n        else:\n            creator_attributes = copy.deepcopy(self.creator_attributes)\n        output[\"creator_attributes\"] = creator_attributes\n        output[\"publish_attributes\"] = self.publish_attributes.data_to_store()\n\n        return output\n\n    def set_create_attr_defs(self, attr_defs, value=None):\n        \"\"\"Create plugin updates create attribute definitions.\n\n        Method called by create plugin when attribute definitions should\n            be changed.\n\n        Args:\n            attr_defs (List[AbstractAttrDef]): Attribute definitions.\n            value (Optional[Dict[str, Any]]): Values of attribute definitions.\n                Current values are used if not passed in.\n\n        \"\"\"\n        if value is None:\n            value = self._data[\"creator_attributes\"]\n\n        if isinstance(value, AttributeValues):\n            value = value.data_to_store()\n\n        if isinstance(self._data[\"creator_attributes\"], AttributeValues):\n            origin_data = self._data[\"creator_attributes\"].origin_data\n        else:\n            origin_data = copy.deepcopy(self._data[\"creator_attributes\"])\n        self._data[\"creator_attributes\"] = CreatorAttributeValues(\n            self,\n            \"creator_attributes\",\n            attr_defs,\n            value,\n            origin_data\n        )\n        self._create_context.instance_create_attr_defs_changed(self.id)\n\n    @classmethod\n    def from_existing(\n        cls,\n        instance_data: Dict[str, Any],\n        creator: \"BaseCreator\",\n        transient_data: Optional[Dict[str, Any]] = None,\n    ) -&gt; \"CreatedInstance\":\n        \"\"\"Convert instance data from workfile to CreatedInstance.\n\n        Args:\n            instance_data (Dict[str, Any]): Data in a structure ready for\n                'CreatedInstance' object.\n            creator (BaseCreator): Creator plugin which is creating the\n                instance of for which the instance belongs.\n            transient_data (Optional[dict[str, Any]]): Instance transient\n                data.\n\n        Returns:\n            CreatedInstance: Instance object.\n\n        \"\"\"\n        instance_data = copy.deepcopy(instance_data)\n\n        product_type = instance_data.get(\"productType\")\n        product_base_type = (\n            instance_data.get(\"productBaseType\")\n            or product_type\n            or instance_data.get(\"family\")\n            or creator.product_base_type\n        )\n        if product_type is None:\n            product_type = product_base_type\n\n        product_name = instance_data.get(\"productName\")\n        if product_name is None:\n            product_name = instance_data.get(\"subset\")\n\n        return cls(\n            product_base_type=product_base_type,\n            product_type=product_type,\n            product_name=product_name,\n            data=instance_data,\n            creator=creator,\n            transient_data=transient_data,\n        )\n\n    def attribute_value_changed(self, key, changes):\n        \"\"\"A value changed.\n\n        Args:\n            key (str): Key of attribute values.\n            changes (Dict[str, Any]): Changes in values.\n\n        \"\"\"\n        self._create_context.instance_values_changed(self.id, {key: changes})\n\n    def set_publish_plugin_attr_defs(self, plugin_name, attr_defs):\n        \"\"\"Set attribute definitions for publish plugin.\n\n        Args:\n            plugin_name(str): Name of publish plugin.\n            attr_defs(List[AbstractAttrDef]): Attribute definitions.\n\n        \"\"\"\n        self.publish_attributes.set_publish_plugin_attr_defs(\n            plugin_name, attr_defs\n        )\n        self._create_context.instance_publish_attr_defs_changed(\n            self.id, plugin_name\n        )\n\n    def publish_attribute_value_changed(self, plugin_name, value):\n        \"\"\"Method called from PublishAttributes.\n\n        Args:\n            plugin_name (str): Plugin name.\n            value (Dict[str, Any]): Changes in values for the plugin.\n\n        \"\"\"\n        self._create_context.instance_values_changed(\n            self.id,\n            {\n                \"publish_attributes\": {\n                    plugin_name: value,\n                },\n            },\n        )\n\n    def add_members(self, members):\n        \"\"\"Currently unused method.\"\"\"\n\n        for member in members:\n            if member not in self._members:\n                self._members.append(member)\n\n    @property\n    def _create_context(self):\n        \"\"\"Get create context.\n\n        Returns:\n            CreateContext: Context object which wraps object.\n\n        \"\"\"\n        return self._creator.create_context\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreatedInstance.creator_attribute_defs","title":"<code>creator_attribute_defs</code>  <code>property</code>","text":"<p>Attribute definitions defined by creator plugin.</p> <p>Returns:</p> Type Description <code>list[AbstractAttrDef]</code> <p>list[AbstractAttrDef]: Attribute definitions.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreatedInstance.data","title":"<code>data</code>  <code>property</code>","text":"<p>Legacy access to data.</p> <p>Access to data is needed to modify values.</p> <p>Returns:</p> Name Type Description <code>CreatedInstance</code> <p>Object can be used as dictionary but with validations of immutable keys.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreatedInstance.has_promised_context","title":"<code>has_promised_context</code>  <code>property</code>","text":"<p>Get context data that are promised to be set by creator.</p> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>Has context that won't bo validated. Artist can't change value when set to True.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreatedInstance.id","title":"<code>id</code>  <code>property</code>","text":"<p>Instance identifier.</p> <p>Returns:</p> Name Type Description <code>str</code> <p>UUID of instance.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreatedInstance.is_mandatory","title":"<code>is_mandatory</code>  <code>property</code>","text":"<p>Check if instance is mandatory.</p> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if instance is mandatory, False otherwise.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreatedInstance.transient_data","title":"<code>transient_data</code>  <code>property</code>","text":"<p>Data stored for lifetime of instance object.</p> <p>These data are not stored to scene and will be lost on object deletion.</p> <p>Can be used to store objects. In some host implementations is not possible to reference to object in scene with some unique identifier (e.g. node in Fusion.). In that case it is handy to store the object here. Should be used that way only if instance data are stored on the node itself.</p> <p>Returns:</p> Type Description <p>Dict[str, Any]: Dictionary object where you can store data related to instance for lifetime of instance object.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreatedInstance.__init__","title":"<code>__init__(product_type, product_name, data, creator, transient_data=None, product_base_type=None)</code>","text":"<p>Initialize CreatedInstance.</p> Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>def __init__(\n    self,\n    product_type: str,\n    product_name: str,\n    data: Dict[str, Any],\n    creator: \"BaseCreator\",\n    transient_data: Optional[Dict[str, Any]] = None,\n    product_base_type: Optional[str] = None\n):\n    \"\"\"Initialize CreatedInstance.\"\"\"\n    # fallback to product type for backward compatibility\n    if not product_base_type:\n        product_base_type = creator.product_base_type or product_type\n\n    self._creator = creator\n    creator_identifier = creator.identifier\n    group_label = creator.get_group_label()\n    creator_label = creator.label\n\n    self._creator_label = creator_label\n    self._group_label = group_label or creator_identifier\n\n    # Instance members may have actions on them\n    # TODO implement members logic\n    self._members = []\n\n    # Data that can be used for lifetime of object\n    if transient_data is None:\n        transient_data = {}\n    self._transient_data = transient_data\n    self._is_mandatory: bool = False\n    self._parent_instance_id: Optional[str] = None\n    self._parent_flags: int = 0\n\n    # Create a copy of passed data to avoid changing them on the fly\n    data = copy.deepcopy(data or {})\n\n    # Pop dictionary values that will be converted to objects to be able\n    #   catch changes\n    orig_creator_attributes = data.pop(\"creator_attributes\", None) or {}\n    orig_publish_attributes = data.pop(\"publish_attributes\", None) or {}\n\n    # Store original value of passed data\n    self._orig_data = copy.deepcopy(data)\n\n    # Pop 'productType' and 'productName' to prevent unexpected changes\n    data.pop(\"productType\", None)\n    data.pop(\"productName\", None)\n    # Backwards compatibility with OpenPype instances\n    data.pop(\"family\", None)\n    data.pop(\"subset\", None)\n\n    asset_name = data.pop(\"asset\", None)\n    if \"folderPath\" not in data:\n        data[\"folderPath\"] = asset_name\n\n    # QUESTION Does it make sense to have data stored as ordered dict?\n    self._data = collections.OrderedDict()\n    # QUESTION Do we need this \"id\" information on instance?\n    item_id = data.get(\"id\")\n    # TODO use only 'AYON_INSTANCE_ID' when all hosts support it\n    if item_id not in {AYON_INSTANCE_ID, AVALON_INSTANCE_ID}:\n        item_id = AYON_INSTANCE_ID\n    self._data[\"id\"] = item_id\n    self._data[\"productType\"] = product_type\n    self._data[\"productName\"] = product_name\n\n    self._data[\"productBaseType\"] = product_base_type\n\n    self._data[\"active\"] = data.get(\"active\", True)\n    self._data[\"creator_identifier\"] = creator_identifier\n\n    # Pop from source data all keys that are defined in `_data` before\n    #   this moment and through their values away\n    # - they should be the same and if are not then should not change\n    #   already set values\n    for key in self._data.keys():\n        if key in data:\n            data.pop(key)\n\n    self._data[\"variant\"] = self._data.get(\"variant\") or \"\"\n    # Stored creator specific attribute values\n    # {key: value}\n    creator_values = copy.deepcopy(orig_creator_attributes)\n\n    self._data[\"creator_attributes\"] = creator_values\n\n    # Stored publish specific attribute values\n    # {&lt;plugin name&gt;: {key: value}}\n    self._data[\"publish_attributes\"] = PublishAttributes(\n        self, orig_publish_attributes\n    )\n    if data:\n        self._data.update(data)\n\n    for key, default in self.__required_keys.items():\n        self._data.setdefault(key, default)\n\n    if not self._data.get(\"instance_id\"):\n        self._data[\"instance_id\"] = str(uuid4())\n\n    creator_attr_defs = creator.get_attr_defs_for_instance(self)\n    self.set_create_attr_defs(\n        creator_attr_defs, creator_values\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreatedInstance.add_members","title":"<code>add_members(members)</code>","text":"<p>Currently unused method.</p> Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>def add_members(self, members):\n    \"\"\"Currently unused method.\"\"\"\n\n    for member in members:\n        if member not in self._members:\n            self._members.append(member)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreatedInstance.attribute_value_changed","title":"<code>attribute_value_changed(key, changes)</code>","text":"<p>A value changed.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>Key of attribute values.</p> required <code>changes</code> <code>Dict[str, Any]</code> <p>Changes in values.</p> required Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>def attribute_value_changed(self, key, changes):\n    \"\"\"A value changed.\n\n    Args:\n        key (str): Key of attribute values.\n        changes (Dict[str, Any]): Changes in values.\n\n    \"\"\"\n    self._create_context.instance_values_changed(self.id, {key: changes})\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreatedInstance.changes","title":"<code>changes()</code>","text":"<p>Calculate and return changes.</p> Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>def changes(self):\n    \"\"\"Calculate and return changes.\"\"\"\n\n    return TrackChangesItem(self.origin_data, self.data_to_store())\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreatedInstance.data_to_store","title":"<code>data_to_store()</code>","text":"<p>Collect data that contain json parsable types.</p> <p>It is possible to recreate the instance using these data.</p> Todos <p>We probably don't need OrderedDict. When data are loaded they     are not ordered anymore.</p> <p>Returns:</p> Name Type Description <code>OrderedDict</code> <p>Ordered dictionary with instance data.</p> Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>def data_to_store(self):\n    \"\"\"Collect data that contain json parsable types.\n\n    It is possible to recreate the instance using these data.\n\n    Todos:\n        We probably don't need OrderedDict. When data are loaded they\n            are not ordered anymore.\n\n    Returns:\n        OrderedDict: Ordered dictionary with instance data.\n    \"\"\"\n\n    output = collections.OrderedDict()\n    for key, value in self._data.items():\n        if key in (\"creator_attributes\", \"publish_attributes\"):\n            continue\n        output[key] = value\n\n    if isinstance(self.creator_attributes, AttributeValues):\n        creator_attributes = self.creator_attributes.data_to_store()\n    else:\n        creator_attributes = copy.deepcopy(self.creator_attributes)\n    output[\"creator_attributes\"] = creator_attributes\n    output[\"publish_attributes\"] = self.publish_attributes.data_to_store()\n\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreatedInstance.from_existing","title":"<code>from_existing(instance_data, creator, transient_data=None)</code>  <code>classmethod</code>","text":"<p>Convert instance data from workfile to CreatedInstance.</p> <p>Parameters:</p> Name Type Description Default <code>instance_data</code> <code>Dict[str, Any]</code> <p>Data in a structure ready for 'CreatedInstance' object.</p> required <code>creator</code> <code>BaseCreator</code> <p>Creator plugin which is creating the instance of for which the instance belongs.</p> required <code>transient_data</code> <code>Optional[dict[str, Any]]</code> <p>Instance transient data.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>CreatedInstance</code> <code>'CreatedInstance'</code> <p>Instance object.</p> Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>@classmethod\ndef from_existing(\n    cls,\n    instance_data: Dict[str, Any],\n    creator: \"BaseCreator\",\n    transient_data: Optional[Dict[str, Any]] = None,\n) -&gt; \"CreatedInstance\":\n    \"\"\"Convert instance data from workfile to CreatedInstance.\n\n    Args:\n        instance_data (Dict[str, Any]): Data in a structure ready for\n            'CreatedInstance' object.\n        creator (BaseCreator): Creator plugin which is creating the\n            instance of for which the instance belongs.\n        transient_data (Optional[dict[str, Any]]): Instance transient\n            data.\n\n    Returns:\n        CreatedInstance: Instance object.\n\n    \"\"\"\n    instance_data = copy.deepcopy(instance_data)\n\n    product_type = instance_data.get(\"productType\")\n    product_base_type = (\n        instance_data.get(\"productBaseType\")\n        or product_type\n        or instance_data.get(\"family\")\n        or creator.product_base_type\n    )\n    if product_type is None:\n        product_type = product_base_type\n\n    product_name = instance_data.get(\"productName\")\n    if product_name is None:\n        product_name = instance_data.get(\"subset\")\n\n    return cls(\n        product_base_type=product_base_type,\n        product_type=product_type,\n        product_name=product_name,\n        data=instance_data,\n        creator=creator,\n        transient_data=transient_data,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreatedInstance.mark_as_stored","title":"<code>mark_as_stored()</code>","text":"<p>Should be called when instance data are stored.</p> <p>Origin data are replaced by current data so changes are cleared.</p> Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>def mark_as_stored(self):\n    \"\"\"Should be called when instance data are stored.\n\n    Origin data are replaced by current data so changes are cleared.\n    \"\"\"\n\n    orig_keys = set(self._orig_data.keys())\n    for key, value in self._data.items():\n        orig_keys.discard(key)\n        if key in (\"creator_attributes\", \"publish_attributes\"):\n            continue\n        self._orig_data[key] = copy.deepcopy(value)\n\n    for key in orig_keys:\n        self._orig_data.pop(key)\n\n    self.creator_attributes.mark_as_stored()\n    self.publish_attributes.mark_as_stored()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreatedInstance.publish_attribute_value_changed","title":"<code>publish_attribute_value_changed(plugin_name, value)</code>","text":"<p>Method called from PublishAttributes.</p> <p>Parameters:</p> Name Type Description Default <code>plugin_name</code> <code>str</code> <p>Plugin name.</p> required <code>value</code> <code>Dict[str, Any]</code> <p>Changes in values for the plugin.</p> required Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>def publish_attribute_value_changed(self, plugin_name, value):\n    \"\"\"Method called from PublishAttributes.\n\n    Args:\n        plugin_name (str): Plugin name.\n        value (Dict[str, Any]): Changes in values for the plugin.\n\n    \"\"\"\n    self._create_context.instance_values_changed(\n        self.id,\n        {\n            \"publish_attributes\": {\n                plugin_name: value,\n            },\n        },\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreatedInstance.set_create_attr_defs","title":"<code>set_create_attr_defs(attr_defs, value=None)</code>","text":"<p>Create plugin updates create attribute definitions.</p> <p>Method called by create plugin when attribute definitions should     be changed.</p> <p>Parameters:</p> Name Type Description Default <code>attr_defs</code> <code>List[AbstractAttrDef]</code> <p>Attribute definitions.</p> required <code>value</code> <code>Optional[Dict[str, Any]]</code> <p>Values of attribute definitions. Current values are used if not passed in.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>def set_create_attr_defs(self, attr_defs, value=None):\n    \"\"\"Create plugin updates create attribute definitions.\n\n    Method called by create plugin when attribute definitions should\n        be changed.\n\n    Args:\n        attr_defs (List[AbstractAttrDef]): Attribute definitions.\n        value (Optional[Dict[str, Any]]): Values of attribute definitions.\n            Current values are used if not passed in.\n\n    \"\"\"\n    if value is None:\n        value = self._data[\"creator_attributes\"]\n\n    if isinstance(value, AttributeValues):\n        value = value.data_to_store()\n\n    if isinstance(self._data[\"creator_attributes\"], AttributeValues):\n        origin_data = self._data[\"creator_attributes\"].origin_data\n    else:\n        origin_data = copy.deepcopy(self._data[\"creator_attributes\"])\n    self._data[\"creator_attributes\"] = CreatorAttributeValues(\n        self,\n        \"creator_attributes\",\n        attr_defs,\n        value,\n        origin_data\n    )\n    self._create_context.instance_create_attr_defs_changed(self.id)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreatedInstance.set_mandatory","title":"<code>set_mandatory(value)</code>","text":"<p>Set instance as mandatory or not.</p> <p>Mandatory instance can't be disabled in UI.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>bool</code> <p>True if instance should be mandatory, False otherwise.</p> required Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>def set_mandatory(self, value: bool) -&gt; None:\n    \"\"\"Set instance as mandatory or not.\n\n    Mandatory instance can't be disabled in UI.\n\n    Args:\n        value (bool): True if instance should be mandatory, False\n            otherwise.\n\n    \"\"\"\n    if value is self._is_mandatory:\n        return\n    self._is_mandatory = value\n    if value is True:\n        self[\"active\"] = True\n    self._create_context.instance_requirement_changed(self.id)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreatedInstance.set_parent","title":"<code>set_parent(instance_id, flags)</code>","text":"<p>Set parent instance id and parenting flags.</p> <p>Parameters:</p> Name Type Description Default <code>instance_id</code> <code>Optional[str]</code> <p>Parent instance id.</p> required <code>flags</code> <code>int</code> <p>Parenting flags.</p> required Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>def set_parent(\n    self, instance_id: Optional[str], flags: int\n) -&gt; None:\n    \"\"\"Set parent instance id and parenting flags.\n\n    Args:\n        instance_id (Optional[str]): Parent instance id.\n        flags (int): Parenting flags.\n\n    \"\"\"\n    changed = False\n    if instance_id != self._parent_instance_id:\n        changed = True\n        self._parent_instance_id = instance_id\n\n    if flags is None:\n        flags = 0\n\n    if self._parent_flags != flags:\n        self._parent_flags = flags\n        changed = True\n\n    if changed:\n        self._create_context.instance_parent_changed(self.id)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreatedInstance.set_publish_plugin_attr_defs","title":"<code>set_publish_plugin_attr_defs(plugin_name, attr_defs)</code>","text":"<p>Set attribute definitions for publish plugin.</p> <p>Parameters:</p> Name Type Description Default <code>plugin_name</code> <code>str</code> <p>Name of publish plugin.</p> required <code>attr_defs</code> <code>List[AbstractAttrDef]</code> <p>Attribute definitions.</p> required Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>def set_publish_plugin_attr_defs(self, plugin_name, attr_defs):\n    \"\"\"Set attribute definitions for publish plugin.\n\n    Args:\n        plugin_name(str): Name of publish plugin.\n        attr_defs(List[AbstractAttrDef]): Attribute definitions.\n\n    \"\"\"\n    self.publish_attributes.set_publish_plugin_attr_defs(\n        plugin_name, attr_defs\n    )\n    self._create_context.instance_publish_attr_defs_changed(\n        self.id, plugin_name\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.Creator","title":"<code>Creator</code>","text":"<p>               Bases: <code>BaseCreator</code></p> <p>Creator that has more information for artist to show in UI.</p> <p>Creation requires prepared product name and instance data.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>class Creator(BaseCreator):\n    \"\"\"Creator that has more information for artist to show in UI.\n\n    Creation requires prepared product name and instance data.\n    \"\"\"\n    skip_discovery = True\n    # GUI Purposes\n    # - default_variants may not be used if `get_default_variants`\n    #   is overridden\n    default_variants: list[str] = []\n\n    # Default variant used in 'get_default_variant'\n    _default_variant: str | None = None\n\n    # Short description of product base type\n    # - may not be used if `get_description` is overridden\n    description: str | None = None\n\n    # Detailed description of product base type for artists\n    # - may not be used if `get_detail_description` is overridden\n    detailed_description: str | None = None\n\n    # It does make sense to change context on creation\n    # - in some cases it may confuse artists because it would not be used\n    #      e.g. for buld creators\n    create_allow_context_change: bool = True\n    # A thumbnail can be passed in precreate attributes\n    # - if is set to True is should expect that a thumbnail path under key\n    #   PRE_CREATE_THUMBNAIL_KEY can be sent in data with precreate data\n    # - is disabled by default because the feature was added in later stages\n    #   and creators who would not expect PRE_CREATE_THUMBNAIL_KEY could\n    #   cause issues with instance data\n    create_allow_thumbnail: bool = False\n\n    # Precreate attribute definitions showed before creation\n    # - similar to instance attribute definitions\n    pre_create_attr_defs: list[AbstractAttrDef] = []\n\n    def __init__(self, *args, **kwargs):\n        cls = self.__class__\n\n        # Fix backwards compatibility for plugins which override\n        #   'default_variant' attribute directly\n        if not isinstance(cls.default_variant, property):\n            # Move value from 'default_variant' to '_default_variant'\n            self._default_variant = self.default_variant\n            # Create property 'default_variant' on the class\n            cls.default_variant = property(\n                cls._get_default_variant_wrap,\n                cls._set_default_variant_wrap\n            )\n        super().__init__(*args, **kwargs)\n\n    @property\n    def show_order(self) -&gt; int:\n        \"\"\"Order in which is creator shown in UI.\n\n        Returns:\n            int: Order in which is creator shown (less == earlier). By default\n                is using Creator's 'order' or processing.\n        \"\"\"\n        return self.order\n\n    @abstractmethod\n    def create(\n        self,\n        product_name: str,\n        instance_data: dict[str, Any],\n        pre_create_data: dict[str, Any],\n    ) -&gt; Any:\n        \"\"\"Create new instance and store it.\n\n        Ideally should be stored to workfile using host implementation.\n\n        Args:\n            product_name (str): Product name of created instance.\n            instance_data (dict[str, Any]): Base data for instance.\n            pre_create_data (dict[str, Any]): Data based on pre creation\n                attributes. Those may affect how creator works.\n        \"\"\"\n\n    def get_description(self):\n        \"\"\"Short description of product base type and plugin.\n\n        Returns:\n            str: Short description of product base type.\n\n        \"\"\"\n        return self.description\n\n    def get_detail_description(self) -&gt; str | None:\n        \"\"\"Description of product base type and plugin.\n\n        Can be detailed with markdown or html tags.\n\n        Returns:\n            str: Detailed description of product base type for artist.\n\n        \"\"\"\n        return self.detailed_description\n\n    def get_default_variants(self) -&gt; list[str]:\n        \"\"\"Default variant values for UI tooltips.\n\n        Replacement of `default_variants` attribute. Using method gives\n        ability to have some \"logic\" other than attribute values.\n\n        By default, returns `default_variants` value.\n\n        Returns:\n            list[str]: Whisper variants for user input.\n\n        \"\"\"\n        return copy.deepcopy(self.default_variants)\n\n    def get_default_variant(\n        self, only_explicit: bool = False\n    ) -&gt; str | None:\n        \"\"\"Default variant value that will be used to prefill variant input.\n\n        This is for user input and value may not be content of result from\n        `get_default_variants`.\n\n        Note:\n            This method does not allow to have empty string as\n                default variant.\n\n        Args:\n            only_explicit (bool): If True, only explicit default\n                variant from '_default_variant' will be returned.\n\n        Returns:\n            str | None: Variant value.\n\n        \"\"\"\n        if only_explicit or self._default_variant:\n            return self._default_variant\n\n        for variant in self.get_default_variants():\n            return variant\n        return DEFAULT_VARIANT_VALUE\n\n    def _get_default_variant_wrap(self) -&gt; str:\n        \"\"\"Default variant value that will be used to prefill variant input.\n\n        Wrapper for 'get_default_variant'.\n\n        Notes:\n            This method is wrapper for 'get_default_variant'\n                for 'default_variant' property, so creator can override\n                the method.\n\n        Returns:\n            str: Variant value.\n\n        \"\"\"\n        return self.get_default_variant()\n\n    def _set_default_variant_wrap(self, variant: str) -&gt; None:\n        \"\"\"Set default variant value.\n\n        This method is needed for automated settings overrides which are\n        changing attributes based on keys in settings.\n\n        Args:\n            variant (str): New default variant value.\n\n        \"\"\"\n        self._default_variant = variant\n\n    default_variant: str = property(\n        _get_default_variant_wrap,\n        _set_default_variant_wrap\n    )\n\n    def get_pre_create_attr_defs(self) -&gt; list[AbstractAttrDef]:\n        \"\"\"Plugin attribute definitions needed for creation.\n        Attribute definitions of plugin that define how creation will work.\n        Values of these definitions are passed to `create` method.\n\n        Note:\n            Convert method should be implemented which should care about\n            updating keys/values when plugin attributes change.\n\n        Returns:\n            list[AbstractAttrDef]: Attribute definitions that can be tweaked\n                for created instance.\n\n        \"\"\"\n        return self.pre_create_attr_defs\n\n    def get_staging_dir(\n        self, instance: CreatedInstance\n    ) -&gt; StagingDir | None:\n        \"\"\"Return the staging dir and persistence from instance.\n\n        Args:\n            instance (CreatedInstance): Instance for which should be staging\n                dir gathered.\n\n        Returns:\n            StagingDir | None: Staging dir path and persistence or None.\n\n        \"\"\"\n        create_ctx = self.create_context\n        product_name = instance.get(\"productName\")\n        product_base_type = instance.get(\"productBaseType\")\n        product_type = instance.get(\"productType\")\n        folder_path = instance.get(\"folderPath\")\n\n        if not product_base_type:\n            product_base_type = product_type\n\n        # this can only work if product name and folder path are available\n        if not product_name or not folder_path:\n            return None\n\n        publish_settings = self.project_settings[\"core\"][\"publish\"]\n        follow_workfile_version = (\n            publish_settings\n            [\"CollectAnatomyInstanceData\"]\n            [\"follow_workfile_version\"]\n        )\n        follow_version_hosts = (\n            publish_settings\n            [\"CollectSceneVersion\"]\n            [\"hosts\"]\n        )\n\n        current_host = create_ctx.host.name\n        follow_workfile_version = (\n            follow_workfile_version and\n            current_host in follow_version_hosts\n        )\n\n        # Gather version number provided from the instance.\n        current_workfile = create_ctx.get_current_workfile_path()\n        version = instance.get(\"version\")\n\n        # If follow workfile, gather version from workfile path.\n        if version is None and follow_workfile_version and current_workfile:\n            workfile_version = get_version_from_path(current_workfile)\n            if workfile_version is not None:\n                version = int(workfile_version)\n\n        # Fill-up version with next version available.\n        if version is None:\n            versions = self.get_next_versions_for_instances(\n                [instance]\n            )\n            version, = tuple(versions.values())\n\n        template_data = {\"version\": version}\n\n        staging_dir_info = get_staging_dir_info(\n            create_ctx.get_current_project_entity(),\n            create_ctx.get_folder_entity(folder_path),\n            create_ctx.get_task_entity(folder_path, instance.get(\"task\")),\n            product_base_type=product_base_type,\n            product_type=product_type,\n            product_name=product_name,\n            host_name=create_ctx.host_name,\n            anatomy=create_ctx.get_current_project_anatomy(),\n            project_settings=create_ctx.get_current_project_settings(),\n            always_return_path=False,\n            logger=self.log,\n            template_data=template_data,\n        )\n\n        return staging_dir_info or None\n\n    def apply_staging_dir(\n        self, instance: CreatedInstance\n    ) -&gt; str | None:\n        \"\"\"Apply staging dir with persistence to instance's transient data.\n\n        Method is called on instance creation and on instance update.\n\n        Args:\n            instance (CreatedInstance): Instance for which should be staging\n                dir applied.\n\n        Returns:\n            str | None: Staging dir path or None if not applied.\n\n        \"\"\"\n        staging_dir_info = self.get_staging_dir(instance)\n        if staging_dir_info is None:\n            return None\n\n        # path might be already created by get_staging_dir_info\n        staging_dir_path = staging_dir_info.directory\n        os.makedirs(staging_dir_path, exist_ok=True)\n\n        instance.transient_data.update({\n            \"stagingDir\": staging_dir_path,\n            \"stagingDir_persistent\": staging_dir_info.is_persistent,\n            \"stagingDir_is_custom\": staging_dir_info.is_custom,\n        })\n\n        self.log.info(f\"Applied staging dir to instance: {staging_dir_path}\")\n\n        return staging_dir_path\n\n    def _pre_create_attr_defs_changed(self) -&gt; None:\n        \"\"\"Called when pre-create attribute definitions change.\n\n        Create plugin can call this method when knows that\n            'get_pre_create_attr_defs' should be called again.\n        \"\"\"\n        self.create_context.create_plugin_pre_create_attr_defs_changed(\n            self.identifier\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.Creator.show_order","title":"<code>show_order</code>  <code>property</code>","text":"<p>Order in which is creator shown in UI.</p> <p>Returns:</p> Name Type Description <code>int</code> <code>int</code> <p>Order in which is creator shown (less == earlier). By default is using Creator's 'order' or processing.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.Creator.apply_staging_dir","title":"<code>apply_staging_dir(instance)</code>","text":"<p>Apply staging dir with persistence to instance's transient data.</p> <p>Method is called on instance creation and on instance update.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>CreatedInstance</code> <p>Instance for which should be staging dir applied.</p> required <p>Returns:</p> Type Description <code>str | None</code> <p>str | None: Staging dir path or None if not applied.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def apply_staging_dir(\n    self, instance: CreatedInstance\n) -&gt; str | None:\n    \"\"\"Apply staging dir with persistence to instance's transient data.\n\n    Method is called on instance creation and on instance update.\n\n    Args:\n        instance (CreatedInstance): Instance for which should be staging\n            dir applied.\n\n    Returns:\n        str | None: Staging dir path or None if not applied.\n\n    \"\"\"\n    staging_dir_info = self.get_staging_dir(instance)\n    if staging_dir_info is None:\n        return None\n\n    # path might be already created by get_staging_dir_info\n    staging_dir_path = staging_dir_info.directory\n    os.makedirs(staging_dir_path, exist_ok=True)\n\n    instance.transient_data.update({\n        \"stagingDir\": staging_dir_path,\n        \"stagingDir_persistent\": staging_dir_info.is_persistent,\n        \"stagingDir_is_custom\": staging_dir_info.is_custom,\n    })\n\n    self.log.info(f\"Applied staging dir to instance: {staging_dir_path}\")\n\n    return staging_dir_path\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.Creator.create","title":"<code>create(product_name, instance_data, pre_create_data)</code>  <code>abstractmethod</code>","text":"<p>Create new instance and store it.</p> <p>Ideally should be stored to workfile using host implementation.</p> <p>Parameters:</p> Name Type Description Default <code>product_name</code> <code>str</code> <p>Product name of created instance.</p> required <code>instance_data</code> <code>dict[str, Any]</code> <p>Base data for instance.</p> required <code>pre_create_data</code> <code>dict[str, Any]</code> <p>Data based on pre creation attributes. Those may affect how creator works.</p> required Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>@abstractmethod\ndef create(\n    self,\n    product_name: str,\n    instance_data: dict[str, Any],\n    pre_create_data: dict[str, Any],\n) -&gt; Any:\n    \"\"\"Create new instance and store it.\n\n    Ideally should be stored to workfile using host implementation.\n\n    Args:\n        product_name (str): Product name of created instance.\n        instance_data (dict[str, Any]): Base data for instance.\n        pre_create_data (dict[str, Any]): Data based on pre creation\n            attributes. Those may affect how creator works.\n    \"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.Creator.get_default_variant","title":"<code>get_default_variant(only_explicit=False)</code>","text":"<p>Default variant value that will be used to prefill variant input.</p> <p>This is for user input and value may not be content of result from <code>get_default_variants</code>.</p> Note <p>This method does not allow to have empty string as     default variant.</p> <p>Parameters:</p> Name Type Description Default <code>only_explicit</code> <code>bool</code> <p>If True, only explicit default variant from '_default_variant' will be returned.</p> <code>False</code> <p>Returns:</p> Type Description <code>str | None</code> <p>str | None: Variant value.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def get_default_variant(\n    self, only_explicit: bool = False\n) -&gt; str | None:\n    \"\"\"Default variant value that will be used to prefill variant input.\n\n    This is for user input and value may not be content of result from\n    `get_default_variants`.\n\n    Note:\n        This method does not allow to have empty string as\n            default variant.\n\n    Args:\n        only_explicit (bool): If True, only explicit default\n            variant from '_default_variant' will be returned.\n\n    Returns:\n        str | None: Variant value.\n\n    \"\"\"\n    if only_explicit or self._default_variant:\n        return self._default_variant\n\n    for variant in self.get_default_variants():\n        return variant\n    return DEFAULT_VARIANT_VALUE\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.Creator.get_default_variants","title":"<code>get_default_variants()</code>","text":"<p>Default variant values for UI tooltips.</p> <p>Replacement of <code>default_variants</code> attribute. Using method gives ability to have some \"logic\" other than attribute values.</p> <p>By default, returns <code>default_variants</code> value.</p> <p>Returns:</p> Type Description <code>list[str]</code> <p>list[str]: Whisper variants for user input.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def get_default_variants(self) -&gt; list[str]:\n    \"\"\"Default variant values for UI tooltips.\n\n    Replacement of `default_variants` attribute. Using method gives\n    ability to have some \"logic\" other than attribute values.\n\n    By default, returns `default_variants` value.\n\n    Returns:\n        list[str]: Whisper variants for user input.\n\n    \"\"\"\n    return copy.deepcopy(self.default_variants)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.Creator.get_description","title":"<code>get_description()</code>","text":"<p>Short description of product base type and plugin.</p> <p>Returns:</p> Name Type Description <code>str</code> <p>Short description of product base type.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def get_description(self):\n    \"\"\"Short description of product base type and plugin.\n\n    Returns:\n        str: Short description of product base type.\n\n    \"\"\"\n    return self.description\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.Creator.get_detail_description","title":"<code>get_detail_description()</code>","text":"<p>Description of product base type and plugin.</p> <p>Can be detailed with markdown or html tags.</p> <p>Returns:</p> Name Type Description <code>str</code> <code>str | None</code> <p>Detailed description of product base type for artist.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def get_detail_description(self) -&gt; str | None:\n    \"\"\"Description of product base type and plugin.\n\n    Can be detailed with markdown or html tags.\n\n    Returns:\n        str: Detailed description of product base type for artist.\n\n    \"\"\"\n    return self.detailed_description\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.Creator.get_pre_create_attr_defs","title":"<code>get_pre_create_attr_defs()</code>","text":"<p>Plugin attribute definitions needed for creation. Attribute definitions of plugin that define how creation will work. Values of these definitions are passed to <code>create</code> method.</p> Note <p>Convert method should be implemented which should care about updating keys/values when plugin attributes change.</p> <p>Returns:</p> Type Description <code>list[AbstractAttrDef]</code> <p>list[AbstractAttrDef]: Attribute definitions that can be tweaked for created instance.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def get_pre_create_attr_defs(self) -&gt; list[AbstractAttrDef]:\n    \"\"\"Plugin attribute definitions needed for creation.\n    Attribute definitions of plugin that define how creation will work.\n    Values of these definitions are passed to `create` method.\n\n    Note:\n        Convert method should be implemented which should care about\n        updating keys/values when plugin attributes change.\n\n    Returns:\n        list[AbstractAttrDef]: Attribute definitions that can be tweaked\n            for created instance.\n\n    \"\"\"\n    return self.pre_create_attr_defs\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.Creator.get_staging_dir","title":"<code>get_staging_dir(instance)</code>","text":"<p>Return the staging dir and persistence from instance.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>CreatedInstance</code> <p>Instance for which should be staging dir gathered.</p> required <p>Returns:</p> Type Description <code>StagingDir | None</code> <p>StagingDir | None: Staging dir path and persistence or None.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def get_staging_dir(\n    self, instance: CreatedInstance\n) -&gt; StagingDir | None:\n    \"\"\"Return the staging dir and persistence from instance.\n\n    Args:\n        instance (CreatedInstance): Instance for which should be staging\n            dir gathered.\n\n    Returns:\n        StagingDir | None: Staging dir path and persistence or None.\n\n    \"\"\"\n    create_ctx = self.create_context\n    product_name = instance.get(\"productName\")\n    product_base_type = instance.get(\"productBaseType\")\n    product_type = instance.get(\"productType\")\n    folder_path = instance.get(\"folderPath\")\n\n    if not product_base_type:\n        product_base_type = product_type\n\n    # this can only work if product name and folder path are available\n    if not product_name or not folder_path:\n        return None\n\n    publish_settings = self.project_settings[\"core\"][\"publish\"]\n    follow_workfile_version = (\n        publish_settings\n        [\"CollectAnatomyInstanceData\"]\n        [\"follow_workfile_version\"]\n    )\n    follow_version_hosts = (\n        publish_settings\n        [\"CollectSceneVersion\"]\n        [\"hosts\"]\n    )\n\n    current_host = create_ctx.host.name\n    follow_workfile_version = (\n        follow_workfile_version and\n        current_host in follow_version_hosts\n    )\n\n    # Gather version number provided from the instance.\n    current_workfile = create_ctx.get_current_workfile_path()\n    version = instance.get(\"version\")\n\n    # If follow workfile, gather version from workfile path.\n    if version is None and follow_workfile_version and current_workfile:\n        workfile_version = get_version_from_path(current_workfile)\n        if workfile_version is not None:\n            version = int(workfile_version)\n\n    # Fill-up version with next version available.\n    if version is None:\n        versions = self.get_next_versions_for_instances(\n            [instance]\n        )\n        version, = tuple(versions.values())\n\n    template_data = {\"version\": version}\n\n    staging_dir_info = get_staging_dir_info(\n        create_ctx.get_current_project_entity(),\n        create_ctx.get_folder_entity(folder_path),\n        create_ctx.get_task_entity(folder_path, instance.get(\"task\")),\n        product_base_type=product_base_type,\n        product_type=product_type,\n        product_name=product_name,\n        host_name=create_ctx.host_name,\n        anatomy=create_ctx.get_current_project_anatomy(),\n        project_settings=create_ctx.get_current_project_settings(),\n        always_return_path=False,\n        logger=self.log,\n        template_data=template_data,\n    )\n\n    return staging_dir_info or None\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreatorAttributeValues","title":"<code>CreatorAttributeValues</code>","text":"<p>               Bases: <code>AttributeValues</code></p> <p>Creator specific attribute values of an instance.</p> Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>class CreatorAttributeValues(AttributeValues):\n    \"\"\"Creator specific attribute values of an instance.\"\"\"\n\n    @property\n    def instance(self):\n        return self._parent\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreatorError","title":"<code>CreatorError</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Should be raised when creator failed because of known issue.</p> <p>Message of error should be artist friendly.</p> Source code in <code>client/ayon_core/pipeline/create/exceptions.py</code> <pre><code>class CreatorError(Exception):\n    \"\"\"Should be raised when creator failed because of known issue.\n\n    Message of error should be artist friendly.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.CreatorsOperationFailed","title":"<code>CreatorsOperationFailed</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Raised when a creator process crashes in 'CreateContext'.</p> <p>The exception contains information about the creator and error. The data are prepared using 'prepare_failed_creator_operation_info' and can be serialized using json.</p> <p>Usage is for UI purposes which may not have access to exceptions directly and would not have ability to catch exceptions 'per creator'.</p> <p>Parameters:</p> Name Type Description Default <code>msg</code> <code>str</code> <p>General error message.</p> required <code>failed_info</code> <code>list[dict[str, Any]]</code> <p>List of failed creators with exception message and optionally formatted traceback.</p> required Source code in <code>client/ayon_core/pipeline/create/exceptions.py</code> <pre><code>class CreatorsOperationFailed(Exception):\n    \"\"\"Raised when a creator process crashes in 'CreateContext'.\n\n    The exception contains information about the creator and error. The data\n    are prepared using 'prepare_failed_creator_operation_info' and can be\n    serialized using json.\n\n    Usage is for UI purposes which may not have access to exceptions directly\n    and would not have ability to catch exceptions 'per creator'.\n\n    Args:\n        msg (str): General error message.\n        failed_info (list[dict[str, Any]]): List of failed creators with\n            exception message and optionally formatted traceback.\n    \"\"\"\n\n    def __init__(self, msg, failed_info):\n        super().__init__(msg)\n        self.failed_info = failed_info\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.HostMissRequiredMethod","title":"<code>HostMissRequiredMethod</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Host does not have implemented required functions for creation.</p> Source code in <code>client/ayon_core/pipeline/create/exceptions.py</code> <pre><code>class HostMissRequiredMethod(Exception):\n    \"\"\"Host does not have implemented required functions for creation.\"\"\"\n\n    def __init__(self, host, missing_methods):\n        self.missing_methods = missing_methods\n        self.host = host\n        joined_methods = \", \".join(\n            ['\"{}\"'.format(name) for name in missing_methods]\n        )\n        dirpath = os.path.dirname(\n            os.path.normpath(inspect.getsourcefile(host))\n        )\n        dirpath_parts = dirpath.split(os.path.sep)\n        host_name = dirpath_parts.pop(-1)\n        if host_name == \"api\":\n            host_name = dirpath_parts.pop(-1)\n\n        msg = \"Host \\\"{}\\\" does not have implemented method/s {}\".format(\n            host_name, joined_methods\n        )\n        super().__init__(msg)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.ImmutableKeyError","title":"<code>ImmutableKeyError</code>","text":"<p>               Bases: <code>TypeError</code></p> <p>Accessed key is immutable so does not allow changes or removals.</p> Source code in <code>client/ayon_core/pipeline/create/exceptions.py</code> <pre><code>class ImmutableKeyError(TypeError):\n    \"\"\"Accessed key is immutable so does not allow changes or removals.\"\"\"\n\n    def __init__(self, key, msg=None):\n        self.immutable_key = key\n        if not msg:\n            msg = \"Key \\\"{}\\\" is immutable and does not allow changes.\".format(\n                key\n            )\n        super().__init__(msg)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.ProductTypeItem","title":"<code>ProductTypeItem</code>  <code>dataclass</code>","text":"<p>Structure to define product types for a create plugin.</p> <p>Product types can be used as studio definitions of a product base type.</p> <p>It is expected that attributes will be added/removed in the future,     in that regards it is recommended to use pre-defined methods     'new' and 'from_data' that do safely handle those cases and     can contain backwards/forwards compatibility if needed.</p> <p>Attributes:</p> Name Type Description <code>product_type</code> <code>str</code> <p>Name of a product type.</p> <code>label</code> <code>str</code> <p>Product type label shown in Create view.</p> Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>@dataclass\nclass ProductTypeItem:\n    \"\"\"Structure to define product types for a create plugin.\n\n    Product types can be used as studio definitions of a product base type.\n\n    It is expected that attributes will be added/removed in the future,\n        in that regards it is recommended to use pre-defined methods\n        'new' and 'from_data' that do safely handle those cases and\n        can contain backwards/forwards compatibility if needed.\n\n    Attributes:\n        product_type (str): Name of a product type.\n        label (str): Product type label shown in Create view.\n\n    \"\"\"\n    product_type: str\n    label: Optional[str] = None\n\n    @classmethod\n    def new(\n        cls,\n        *,\n        product_type: str,\n        label: str,\n        **unknown,\n    ) -&gt; \"ProductTypeItem\":\n        if unknown:\n            unknown_keys = \", \".join(f\"'{k}'\" for k in unknown.keys())\n            log.info(\n                f\"Unknown keys in ProductTypeItem: {unknown_keys}\"\n            )\n\n        return cls(\n            product_type=product_type,\n            label=label,\n        )\n\n    @classmethod\n    def from_data(cls, data: dict[str, Any]) -&gt; \"ProductTypeItem\":\n        return cls.new(**data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.PublishAttributeValues","title":"<code>PublishAttributeValues</code>","text":"<p>               Bases: <code>AttributeValues</code></p> <p>Publish plugin specific attribute values.</p> <p>Values are for single plugin which can be on <code>CreatedInstance</code> or context values stored on <code>CreateContext</code>.</p> Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>class PublishAttributeValues(AttributeValues):\n    \"\"\"Publish plugin specific attribute values.\n\n    Values are for single plugin which can be on `CreatedInstance`\n    or context values stored on `CreateContext`.\n    \"\"\"\n\n    @property\n    def publish_attributes(self):\n        return self._parent\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.PublishAttributes","title":"<code>PublishAttributes</code>","text":"<p>Wrapper for publish plugin attribute definitions.</p> <p>Cares about handling attribute definitions of multiple publish plugins. Keep information about attribute definitions and their values.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <code>(CreatedInstance, CreateContext)</code> <p>Parent for which will be data stored and from which are data loaded.</p> required <code>origin_data</code> <code>dict</code> <p>Loaded data by plugin class name.</p> required Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>class PublishAttributes:\n    \"\"\"Wrapper for publish plugin attribute definitions.\n\n    Cares about handling attribute definitions of multiple publish plugins.\n    Keep information about attribute definitions and their values.\n\n    Args:\n        parent(CreatedInstance, CreateContext): Parent for which will be\n            data stored and from which are data loaded.\n        origin_data(dict): Loaded data by plugin class name.\n\n    \"\"\"\n    def __init__(self, parent, origin_data):\n        self._parent = parent\n        self._origin_data = copy.deepcopy(origin_data)\n\n        self._data = copy.deepcopy(origin_data)\n\n    def __getitem__(self, key):\n        return self._data[key]\n\n    def __setitem__(self, key, value):\n        \"\"\"Set value for plugin.\n\n        Args:\n            key (str): Plugin name.\n            value (dict[str, Any]): Value to set.\n\n        \"\"\"\n        current_value = self._data.get(key)\n        if isinstance(current_value, PublishAttributeValues):\n            current_value.set_value(value)\n        else:\n            self._data[key] = value\n\n    def __delitem__(self, key):\n        self.pop(key)\n\n    def __contains__(self, key):\n        return key in self._data\n\n    def keys(self):\n        return self._data.keys()\n\n    def values(self):\n        return self._data.values()\n\n    def items(self):\n        return self._data.items()\n\n    def get(self, key, default=None):\n        return self._data.get(key, default)\n\n    def pop(self, key, default=None):\n        \"\"\"Remove or reset value for plugin.\n\n        Plugin values are reset to defaults if plugin is available but\n        data of plugin which was not found are removed.\n\n        Args:\n            key(str): Plugin name.\n            default: Default value if plugin was not found.\n        \"\"\"\n\n        if key not in self._data:\n            return default\n\n        value = self._data[key]\n        if not isinstance(value, AttributeValues):\n            self.attribute_value_changed(key, None)\n            return self._data.pop(key)\n\n        value_item = self._data[key]\n        # Prepare value to return\n        output = value_item.data_to_store()\n        # Reset values\n        value_item.reset_values()\n        self.attribute_value_changed(\n            key, value_item.data_to_store()\n        )\n        return output\n\n    def mark_as_stored(self):\n        self._origin_data = copy.deepcopy(self.data_to_store())\n\n    def data_to_store(self):\n        \"\"\"Convert attribute values to \"data to store\".\"\"\"\n        output = {}\n        for key, attr_value in self._data.items():\n            if isinstance(attr_value, AttributeValues):\n                output[key] = attr_value.data_to_store()\n            else:\n                output[key] = attr_value\n        return output\n\n    @property\n    def origin_data(self):\n        return copy.deepcopy(self._origin_data)\n\n    def attribute_value_changed(self, key, changes):\n        self._parent.publish_attribute_value_changed(key, changes)\n\n    def set_publish_plugin_attr_defs(\n        self,\n        plugin_name: str,\n        attr_defs: List[AbstractAttrDef],\n        value: Optional[Dict[str, Any]] = None\n    ):\n        \"\"\"Set attribute definitions for plugin.\n\n        Args:\n            plugin_name (str): Name of plugin.\n            attr_defs (List[AbstractAttrDef]): Attribute definitions.\n            value (Optional[Dict[str, Any]]): Attribute values.\n\n        \"\"\"\n        # TODO what if 'attr_defs' is 'None'?\n        if value is None:\n            value = self._data.get(plugin_name)\n\n        if value is None:\n            value = {}\n\n        self._data[plugin_name] = PublishAttributeValues(\n            self, plugin_name, attr_defs, value, value\n        )\n\n    def serialize_attributes(self):\n        return {\n            \"attr_defs\": {\n                plugin_name: attrs_value.get_serialized_attr_defs()\n                for plugin_name, attrs_value in self._data.items()\n            },\n        }\n\n    def deserialize_attributes(self, data):\n        attr_defs = deserialize_attr_defs(data[\"attr_defs\"])\n\n        origin_data = self._origin_data\n        data = self._data\n        self._data = {}\n\n        added_keys = set()\n        for plugin_name, attr_defs_data in attr_defs.items():\n            attr_defs = deserialize_attr_defs(attr_defs_data)\n            value = data.get(plugin_name) or {}\n            orig_value = copy.deepcopy(origin_data.get(plugin_name) or {})\n            self._data[plugin_name] = PublishAttributeValues(\n                self, plugin_name, attr_defs, value, orig_value\n            )\n\n        for key, value in data.items():\n            if key not in added_keys:\n                self._data[key] = value\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.PublishAttributes.__setitem__","title":"<code>__setitem__(key, value)</code>","text":"<p>Set value for plugin.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>Plugin name.</p> required <code>value</code> <code>dict[str, Any]</code> <p>Value to set.</p> required Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>def __setitem__(self, key, value):\n    \"\"\"Set value for plugin.\n\n    Args:\n        key (str): Plugin name.\n        value (dict[str, Any]): Value to set.\n\n    \"\"\"\n    current_value = self._data.get(key)\n    if isinstance(current_value, PublishAttributeValues):\n        current_value.set_value(value)\n    else:\n        self._data[key] = value\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.PublishAttributes.data_to_store","title":"<code>data_to_store()</code>","text":"<p>Convert attribute values to \"data to store\".</p> Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>def data_to_store(self):\n    \"\"\"Convert attribute values to \"data to store\".\"\"\"\n    output = {}\n    for key, attr_value in self._data.items():\n        if isinstance(attr_value, AttributeValues):\n            output[key] = attr_value.data_to_store()\n        else:\n            output[key] = attr_value\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.PublishAttributes.pop","title":"<code>pop(key, default=None)</code>","text":"<p>Remove or reset value for plugin.</p> <p>Plugin values are reset to defaults if plugin is available but data of plugin which was not found are removed.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>Plugin name.</p> required <code>default</code> <p>Default value if plugin was not found.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>def pop(self, key, default=None):\n    \"\"\"Remove or reset value for plugin.\n\n    Plugin values are reset to defaults if plugin is available but\n    data of plugin which was not found are removed.\n\n    Args:\n        key(str): Plugin name.\n        default: Default value if plugin was not found.\n    \"\"\"\n\n    if key not in self._data:\n        return default\n\n    value = self._data[key]\n    if not isinstance(value, AttributeValues):\n        self.attribute_value_changed(key, None)\n        return self._data.pop(key)\n\n    value_item = self._data[key]\n    # Prepare value to return\n    output = value_item.data_to_store()\n    # Reset values\n    value_item.reset_values()\n    self.attribute_value_changed(\n        key, value_item.data_to_store()\n    )\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.PublishAttributes.set_publish_plugin_attr_defs","title":"<code>set_publish_plugin_attr_defs(plugin_name, attr_defs, value=None)</code>","text":"<p>Set attribute definitions for plugin.</p> <p>Parameters:</p> Name Type Description Default <code>plugin_name</code> <code>str</code> <p>Name of plugin.</p> required <code>attr_defs</code> <code>List[AbstractAttrDef]</code> <p>Attribute definitions.</p> required <code>value</code> <code>Optional[Dict[str, Any]]</code> <p>Attribute values.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>def set_publish_plugin_attr_defs(\n    self,\n    plugin_name: str,\n    attr_defs: List[AbstractAttrDef],\n    value: Optional[Dict[str, Any]] = None\n):\n    \"\"\"Set attribute definitions for plugin.\n\n    Args:\n        plugin_name (str): Name of plugin.\n        attr_defs (List[AbstractAttrDef]): Attribute definitions.\n        value (Optional[Dict[str, Any]]): Attribute values.\n\n    \"\"\"\n    # TODO what if 'attr_defs' is 'None'?\n    if value is None:\n        value = self._data.get(plugin_name)\n\n    if value is None:\n        value = {}\n\n    self._data[plugin_name] = PublishAttributeValues(\n        self, plugin_name, attr_defs, value, value\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.UnavailableSharedData","title":"<code>UnavailableSharedData</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Shared data are not available at the moment when are accessed.</p> Source code in <code>client/ayon_core/pipeline/create/exceptions.py</code> <pre><code>class UnavailableSharedData(Exception):\n    \"\"\"Shared data are not available at the moment when are accessed.\"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.cache_and_get_instances","title":"<code>cache_and_get_instances(creator, shared_key, list_instances_func)</code>","text":"<p>Common approach to cache instances in shared data.</p> <p>This is helper function which does not handle cases when a 'shared_key' is used for different list instances functions. The same approach of caching instances into 'collection_shared_data' is not required but is so common we've decided to unify it to some degree.</p> <p>Function 'list_instances_func' is called only if 'shared_key' is not available in 'collection_shared_data' on creator.</p> <p>Parameters:</p> Name Type Description Default <code>creator</code> <code>Creator</code> <p>Plugin which would like to get instance data.</p> required <code>shared_key</code> <code>str</code> <p>Key under which output of function will be stored.</p> required <code>list_instances_func</code> <code>Function</code> <p>Function that will return instance data if data were not yet stored under 'shared_key'.</p> required <p>Returns:</p> Type Description <code>dict[str, list[Any]]</code> <p>dict[str, dict[str, Any]]: Cached instances by creator identifier from result of passed function.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def cache_and_get_instances(\n    creator: BaseCreator,\n    shared_key: str,\n    list_instances_func: Callable[[], list[dict[str, Any]]],\n) -&gt; dict[str, list[Any]]:\n    \"\"\"Common approach to cache instances in shared data.\n\n    This is helper function which does not handle cases when a 'shared_key' is\n    used for different list instances functions. The same approach of caching\n    instances into 'collection_shared_data' is not required but is so common\n    we've decided to unify it to some degree.\n\n    Function 'list_instances_func' is called only if 'shared_key' is not\n    available in 'collection_shared_data' on creator.\n\n    Args:\n        creator (Creator): Plugin which would like to get instance data.\n        shared_key (str): Key under which output of function will be stored.\n        list_instances_func (Function): Function that will return instance data\n            if data were not yet stored under 'shared_key'.\n\n    Returns:\n        dict[str, dict[str, Any]]: Cached instances by creator identifier from\n            result of passed function.\n\n    \"\"\"\n    if shared_key not in creator.collection_shared_data:\n        value = collections.defaultdict(list)\n        for instance in list_instances_func():\n            identifier = instance.get(\"creator_identifier\")\n            value[identifier].append(instance)\n        creator.collection_shared_data[shared_key] = value\n    return creator.collection_shared_data[shared_key]\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.get_last_versions_for_instances","title":"<code>get_last_versions_for_instances(project_name, instances, use_value_for_missing=False)</code>","text":"<p>Get last versions for instances by their folder path and product name.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>instances</code> <code>list[CreatedInstance]</code> <p>Instances to get next versions for.</p> required <code>use_value_for_missing</code> <code>Optional[bool]</code> <p>Missing values are replaced with negative value if True. Otherwise None is used. -2 is used for instances without filled folder or product name. -1 is used for missing entities.</p> <code>False</code> <p>Returns:</p> Type Description <p>dict[str, Union[int, None]]: Last versions by instance id.</p> Source code in <code>client/ayon_core/pipeline/create/utils.py</code> <pre><code>def get_last_versions_for_instances(\n    project_name, instances, use_value_for_missing=False\n):\n    \"\"\"Get last versions for instances by their folder path and product name.\n\n    Args:\n        project_name (str): Project name.\n        instances (list[CreatedInstance]): Instances to get next versions for.\n        use_value_for_missing (Optional[bool]): Missing values are replaced\n            with negative value if True. Otherwise None is used. -2 is used\n            for instances without filled folder or product name. -1 is used\n            for missing entities.\n\n    Returns:\n        dict[str, Union[int, None]]: Last versions by instance id.\n    \"\"\"\n\n    output = {\n        instance.id: -1 if use_value_for_missing else None\n        for instance in instances\n    }\n    product_names_by_folder_path = collections.defaultdict(set)\n    instances_by_hierarchy = {}\n    for instance in instances:\n        folder_path = instance.data.get(\"folderPath\")\n        product_name = instance.product_name\n        if not folder_path or not product_name:\n            if use_value_for_missing:\n                output[instance.id] = -2\n            continue\n\n        (\n            instances_by_hierarchy\n            .setdefault(folder_path, {})\n            .setdefault(product_name, [])\n            .append(instance)\n        )\n        product_names_by_folder_path[folder_path].add(product_name)\n\n    product_names = set()\n    for names in product_names_by_folder_path.values():\n        product_names |= names\n\n    if not product_names:\n        return output\n\n    folder_entities = ayon_api.get_folders(\n        project_name,\n        folder_paths=product_names_by_folder_path.keys(),\n        fields={\"id\", \"path\"}\n    )\n    folder_paths_by_id = {\n        folder_entity[\"id\"]: folder_entity[\"path\"]\n        for folder_entity in folder_entities\n    }\n    if not folder_paths_by_id:\n        return output\n\n    product_entities = ayon_api.get_products(\n        project_name,\n        folder_ids=folder_paths_by_id.keys(),\n        product_names=product_names,\n        fields={\"id\", \"name\", \"folderId\"}\n    )\n    product_entities_by_id = {}\n    for product_entity in product_entities:\n        # Filter product entities by names under parent\n        folder_id = product_entity[\"folderId\"]\n        product_name = product_entity[\"name\"]\n        folder_path = folder_paths_by_id[folder_id]\n        if product_name not in product_names_by_folder_path[folder_path]:\n            continue\n        product_entities_by_id[product_entity[\"id\"]] = product_entity\n\n    if not product_entities_by_id:\n        return output\n\n    last_versions_by_product_id = ayon_api.get_last_versions(\n        project_name,\n        product_entities_by_id.keys(),\n        fields={\"version\", \"productId\"}\n    )\n    for product_id, version_entity in last_versions_by_product_id.items():\n        product_entity = product_entities_by_id[product_id]\n        product_name = product_entity[\"name\"]\n        folder_id = product_entity[\"folderId\"]\n        folder_path = folder_paths_by_id[folder_id]\n        _instances = instances_by_hierarchy[folder_path][product_name]\n        for instance in _instances:\n            output[instance.id] = version_entity[\"version\"]\n\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.get_next_versions_for_instances","title":"<code>get_next_versions_for_instances(project_name, instances)</code>","text":"<p>Get next versions for instances by their folder path and product name.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>instances</code> <code>list[CreatedInstance]</code> <p>Instances to get next versions for.</p> required <p>Returns:</p> Type Description <p>dict[str, Union[int, None]]: Next versions by instance id. Version is 'None' if instance has no folder path or product name.</p> Source code in <code>client/ayon_core/pipeline/create/utils.py</code> <pre><code>def get_next_versions_for_instances(project_name, instances):\n    \"\"\"Get next versions for instances by their folder path and product name.\n\n    Args:\n        project_name (str): Project name.\n        instances (list[CreatedInstance]): Instances to get next versions for.\n\n    Returns:\n        dict[str, Union[int, None]]: Next versions by instance id. Version is\n            'None' if instance has no folder path or product name.\n    \"\"\"\n\n    last_versions = get_last_versions_for_instances(\n        project_name, instances, True)\n\n    output = {}\n    for instance_id, version in last_versions.items():\n        if version == -2:\n            output[instance_id] = None\n        elif version == -1:\n            output[instance_id] = 1\n        else:\n            output[instance_id] = version + 1\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.get_product_name","title":"<code>get_product_name(project_name, folder_entity, task_entity, product_base_type, product_type, host_name, variant, *, dynamic_data=None, project_settings=None, project_entity=None, default_template=None, product_base_type_filter=None)</code>","text":"<pre><code>get_product_name(project_name: str, folder_entity: dict[str, Any], task_entity: Optional[dict[str, Any]], product_base_type: str, product_type: str, host_name: str, variant: str, *, dynamic_data: Optional[dict[str, Any]] = None, project_settings: Optional[dict[str, Any]] = None, project_entity: Optional[dict[str, Any]] = None, default_template: Optional[str] = None, product_base_type_filter: Optional[str] = None) -&gt; TemplateResult\n</code></pre><pre><code>get_product_name(project_name, task_name, task_type, host_name, product_type, variant, default_template=None, dynamic_data=None, project_settings=None, product_type_filter=None, project_entity=None) -&gt; TemplateResult\n</code></pre> <p>Calculate product name based on passed context and AYON settings.</p> <p>Product name templates are defined in <code>project_settings/global/tools     /creator/product_name_profiles</code> where are profiles with host name,     product base type, product type, task name and task type filters.</p> <p>If context does not match any profile then <code>DEFAULT_PRODUCT_TEMPLATE</code>     is used as default template.</p> <p>That's main reason why so many arguments are required to calculate product     name.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>folder_entity</code> <code>Optional[dict[str, Any]]</code> <p>Folder entity.</p> required <code>task_entity</code> <code>Optional[dict[str, Any]]</code> <p>Task entity.</p> required <code>host_name</code> <code>str</code> <p>Host name.</p> required <code>product_base_type</code> <code>str</code> <p>Product base type.</p> required <code>product_type</code> <code>str</code> <p>Product type.</p> required <code>variant</code> <code>str</code> <p>In most of the cases it is user input during creation.</p> required <code>dynamic_data</code> <code>Optional[dict[str, Any]]</code> <p>Dynamic data specific for a creator which creates instance.</p> <code>None</code> <code>project_settings</code> <code>Optional[dict[str, Any]]</code> <p>Prepared settings for project. Settings are queried if not passed.</p> <code>None</code> <code>project_entity</code> <code>Optional[dict[str, Any]]</code> <p>Project entity used when task short name is required by template.</p> <code>None</code> <code>default_template</code> <code>Optional[str]</code> <p>Default template if any profile does not match passed context. Constant 'DEFAULT_PRODUCT_TEMPLATE' is used if is not passed.</p> <code>None</code> <code>product_base_type_filter</code> <code>Optional[str]</code> <p>Use different product base type for product template filtering. Value of <code>product_base_type_filter</code> is used when not passed.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>TemplateResult</code> <code>TemplateResult</code> <p>Product name.</p> <p>Raises:</p> Type Description <code>TaskNotSetError</code> <p>If template requires task which is not provided.</p> <code>TemplateFillError</code> <p>If filled template contains placeholder key which is not collected.</p> Source code in <code>client/ayon_core/pipeline/create/product_name.py</code> <pre><code>@_backwards_compatibility_product_name\ndef get_product_name(\n    project_name: str,\n    folder_entity: dict[str, Any],\n    task_entity: Optional[dict[str, Any]],\n    product_base_type: str,\n    product_type: str,\n    host_name: str,\n    variant: str,\n    *,\n    dynamic_data: Optional[dict[str, Any]] = None,\n    project_settings: Optional[dict[str, Any]] = None,\n    project_entity: Optional[dict[str, Any]] = None,\n    default_template: Optional[str] = None,\n    product_base_type_filter: Optional[str] = None,\n) -&gt; TemplateResult:\n    \"\"\"Calculate product name based on passed context and AYON settings.\n\n    Product name templates are defined in `project_settings/global/tools\n        /creator/product_name_profiles` where are profiles with host name,\n        product base type, product type, task name and task type filters.\n\n    If context does not match any profile then `DEFAULT_PRODUCT_TEMPLATE`\n        is used as default template.\n\n    That's main reason why so many arguments are required to calculate product\n        name.\n\n    Args:\n        project_name (str): Project name.\n        folder_entity (Optional[dict[str, Any]]): Folder entity.\n        task_entity (Optional[dict[str, Any]]): Task entity.\n        host_name (str): Host name.\n        product_base_type (str): Product base type.\n        product_type (str): Product type.\n        variant (str): In most of the cases it is user input during creation.\n        dynamic_data (Optional[dict[str, Any]]): Dynamic data specific for\n            a creator which creates instance.\n        project_settings (Optional[dict[str, Any]]): Prepared settings\n            for project. Settings are queried if not passed.\n        project_entity (Optional[dict[str, Any]]): Project entity used when\n            task short name is required by template.\n        default_template (Optional[str]): Default template if any profile does\n            not match passed context. Constant 'DEFAULT_PRODUCT_TEMPLATE'\n            is used if is not passed.\n        product_base_type_filter (Optional[str]): Use different product base\n            type for product template filtering. Value of\n            `product_base_type_filter` is used when not passed.\n\n    Returns:\n        TemplateResult: Product name.\n\n    Raises:\n        TaskNotSetError: If template requires task which is not provided.\n        TemplateFillError: If filled template contains placeholder key which\n            is not collected.\n\n    \"\"\"\n    if not product_base_type:\n        return StringTemplate(\"\").format({})\n\n    if not product_type:\n        product_type = product_base_type\n\n    task_name = task_type = None\n    if task_entity:\n        task_name = task_entity[\"name\"]\n        task_type = task_entity[\"taskType\"]\n\n    template = get_product_name_template(\n        project_name=project_name,\n        product_base_type=product_base_type_filter or product_base_type,\n        product_type=product_type,\n        task_name=task_name,\n        task_type=task_type,\n        host_name=host_name,\n        default_template=default_template,\n        project_settings=project_settings,\n    )\n\n    template_low = template.lower()\n    # Simple check of task name existence for template with {task[name]} in\n    if not task_name and \"{task\" in template_low:\n        raise TaskNotSetError()\n\n    task_value = {\n        \"name\": task_name,\n        \"type\": task_type,\n    }\n    if \"{task}\" in template_low:\n        task_value = task_name\n        # NOTE this is message for TDs and Admins -&gt; not really for users\n        # TODO validate this in settings and not allow it\n        log.warning(\n            \"Found deprecated task key '{task}' in product name template.\"\n            \" Please use '{task[name]}' instead.\"\n        )\n\n    elif \"{task[short]}\" in template_low:\n        if project_entity is None:\n            project_entity = ayon_api.get_project(project_name)\n        task_types_by_name = {\n            task[\"name\"]: task\n            for task in project_entity[\"taskTypes\"]\n        }\n        task_short = task_types_by_name.get(task_type, {}).get(\"shortName\")\n        task_value[\"short\"] = task_short\n\n    fill_pairs = {\n        \"variant\": variant,\n        # TODO We should stop support 'family' key.\n        \"family\": product_base_type,\n        \"task\": task_value,\n        \"product\": {\n            \"type\": product_type,\n            \"basetype\": product_base_type,\n        }\n    }\n    if folder_entity:\n        fill_pairs[\"folder\"] = {\n            \"name\": folder_entity[\"name\"],\n            \"type\": folder_entity[\"folderType\"],\n        }\n        # Folder label was added ad-hoc and might not be passed from create\n        #   plugins that do fake entities\n        folder_label = folder_entity.get(\"label\")\n        if folder_label:\n            fill_pairs[\"folder\"][\"label\"] = folder_label\n\n    if dynamic_data:\n        # Dynamic data may override default values\n        for key, value in dynamic_data.items():\n            fill_pairs[key] = value\n\n    try:\n        return StringTemplate.format_strict_template(\n            template=template,\n            data=prepare_template_data(fill_pairs)\n        )\n    except KeyError as exp:\n        msg = (\n            f\"Value for {exp} key is missing in template '{template}'.\"\n            f\" Available values are {fill_pairs}\"\n        )\n        raise TemplateFillError(msg)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/index.html#client.ayon_core.pipeline.create.get_product_name_template","title":"<code>get_product_name_template(project_name, product_type, task_name, task_type, host_name, default_template=None, project_settings=None, product_base_type=None)</code>","text":"<p>Get product name template based on passed context.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project on which the context lives.</p> required <code>product_type</code> <code>str</code> <p>Product type for which the product name is calculated.</p> required <code>host_name</code> <code>str</code> <p>Name of host in which the product name is calculated.</p> required <code>task_name</code> <code>Optional[str]</code> <p>Name of task in which context the product is created.</p> required <code>task_type</code> <code>Optional[str]</code> <p>Type of task in which context the product is created.</p> required <code>default_template</code> <code>Optional[str]</code> <p>Default template which is used if settings won't find any matching possibility. Constant 'DEFAULT_PRODUCT_TEMPLATE' is used if not defined.</p> <code>None</code> <code>project_settings</code> <code>Optional[dict[str, Any]]</code> <p>Prepared settings for project. Settings are queried if not passed.</p> <code>None</code> <code>product_base_type</code> <code>Optional[str]</code> <p>Base type of product.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Product name template.</p> Source code in <code>client/ayon_core/pipeline/create/product_name.py</code> <pre><code>def get_product_name_template(\n    project_name: str,\n    product_type: str,\n    task_name: Optional[str],\n    task_type: Optional[str],\n    host_name: str,\n    default_template: Optional[str] = None,\n    project_settings: Optional[dict[str, Any]] = None,\n    product_base_type: Optional[str] = None\n) -&gt; str:\n    \"\"\"Get product name template based on passed context.\n\n    Args:\n        project_name (str): Project on which the context lives.\n        product_type (str): Product type for which the product name is\n            calculated.\n        host_name (str): Name of host in which the product name is calculated.\n        task_name (Optional[str]): Name of task in which context the\n            product is created.\n        task_type (Optional[str]): Type of task in which context the\n            product is created.\n        default_template (Optional[str]): Default template which is used if\n            settings won't find any matching possibility. Constant\n            'DEFAULT_PRODUCT_TEMPLATE' is used if not defined.\n        project_settings (Optional[dict[str, Any]]): Prepared settings for\n            project. Settings are queried if not passed.\n        product_base_type (Optional[str]): Base type of product.\n\n    Returns:\n        str: Product name template.\n\n    \"\"\"\n    if project_settings is None:\n        project_settings = get_project_settings(project_name)\n    tools_settings = project_settings[\"core\"][\"tools\"]\n    profiles = tools_settings[\"creator\"][\"product_name_profiles\"]\n    filtering_criteria = {\n        \"product_base_types\": product_base_type or product_type,\n        \"product_types\": product_type,\n        \"host_names\": host_name,\n        \"task_names\": task_name,\n        \"task_types\": task_type,\n    }\n    matching_profile = filter_profiles(profiles, filtering_criteria)\n    template = None\n    if matching_profile:\n        # TODO remove formatting keys replacement\n        template = (\n            matching_profile[\"template\"]\n            .replace(\"{task}\", \"{task[name]}\")\n            .replace(\"{Task}\", \"{Task[name]}\")\n            .replace(\"{TASK}\", \"{TASK[NAME]}\")\n            .replace(\"{family}\", \"{product[type]}\")\n            .replace(\"{Family}\", \"{Product[type]}\")\n            .replace(\"{FAMILY}\", \"{PRODUCT[TYPE]}\")\n            .replace(\"{asset}\", \"{folder[name]}\")\n            .replace(\"{Asset}\", \"{Folder[name]}\")\n            .replace(\"{ASSET}\", \"{FOLDER[NAME]}\")\n        )\n\n    # Make sure template is set (matching may have empty string)\n    if not template:\n        template = default_template or DEFAULT_PRODUCT_TEMPLATE\n    return template\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/changes.html","title":"changes","text":""},{"location":"autoapi/client/ayon_core/pipeline/create/changes.html#client.ayon_core.pipeline.create.changes.TrackChangesItem","title":"<code>TrackChangesItem</code>","text":"<p>Helper object to track changes in data.</p> <p>Has access to full old and new data and will create deep copy of them, so it is not needed to create copy before passed in.</p> <p>Can work as a dictionary if old or new value is a dictionary. In that case received object is another object of 'TrackChangesItem'.</p> <p>Goal is to be able to get old or new value as was or only changed values or get information about removed/changed keys, and all of that on any \"dictionary level\".</p> <pre><code># Example of possible usages\n&gt;&gt;&gt; old_value = {\n...     \"key_1\": \"value_1\",\n...     \"key_2\": {\n...         \"key_sub_1\": 1,\n...         \"key_sub_2\": {\n...             \"enabled\": True\n...         }\n...     },\n...     \"key_3\": \"value_2\"\n... }\n&gt;&gt;&gt; new_value = {\n...     \"key_1\": \"value_1\",\n...     \"key_2\": {\n...         \"key_sub_2\": {\n...             \"enabled\": False\n...         },\n...         \"key_sub_3\": 3\n...     },\n...     \"key_3\": \"value_3\"\n... }\n\n&gt;&gt;&gt; changes = TrackChangesItem(old_value, new_value)\n&gt;&gt;&gt; changes.changed\nTrue\n\n&gt;&gt;&gt; changes[\"key_2\"][\"key_sub_1\"].new_value is None\nTrue\n\n&gt;&gt;&gt; list(sorted(changes.changed_keys))\n['key_2', 'key_3']\n\n&gt;&gt;&gt; changes[\"key_2\"][\"key_sub_2\"][\"enabled\"].changed\nTrue\n\n&gt;&gt;&gt; changes[\"key_2\"].removed_keys\n{'key_sub_1'}\n\n&gt;&gt;&gt; list(sorted(changes[\"key_2\"].available_keys))\n['key_sub_1', 'key_sub_2', 'key_sub_3']\n\n&gt;&gt;&gt; changes.new_value == new_value\nTrue\n\n# Get only changed values\nonly_changed_new_values = {\n    key: changes[key].new_value\n    for key in changes.changed_keys\n}\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>old_value</code> <code>Any</code> <p>Old value.</p> required <code>new_value</code> <code>Any</code> <p>New value.</p> required Source code in <code>client/ayon_core/pipeline/create/changes.py</code> <pre><code>class TrackChangesItem:\n    \"\"\"Helper object to track changes in data.\n\n    Has access to full old and new data and will create deep copy of them,\n    so it is not needed to create copy before passed in.\n\n    Can work as a dictionary if old or new value is a dictionary. In\n    that case received object is another object of 'TrackChangesItem'.\n\n    Goal is to be able to get old or new value as was or only changed values\n    or get information about removed/changed keys, and all of that on\n    any \"dictionary level\".\n\n    ```\n    # Example of possible usages\n    &gt;&gt;&gt; old_value = {\n    ...     \"key_1\": \"value_1\",\n    ...     \"key_2\": {\n    ...         \"key_sub_1\": 1,\n    ...         \"key_sub_2\": {\n    ...             \"enabled\": True\n    ...         }\n    ...     },\n    ...     \"key_3\": \"value_2\"\n    ... }\n    &gt;&gt;&gt; new_value = {\n    ...     \"key_1\": \"value_1\",\n    ...     \"key_2\": {\n    ...         \"key_sub_2\": {\n    ...             \"enabled\": False\n    ...         },\n    ...         \"key_sub_3\": 3\n    ...     },\n    ...     \"key_3\": \"value_3\"\n    ... }\n\n    &gt;&gt;&gt; changes = TrackChangesItem(old_value, new_value)\n    &gt;&gt;&gt; changes.changed\n    True\n\n    &gt;&gt;&gt; changes[\"key_2\"][\"key_sub_1\"].new_value is None\n    True\n\n    &gt;&gt;&gt; list(sorted(changes.changed_keys))\n    ['key_2', 'key_3']\n\n    &gt;&gt;&gt; changes[\"key_2\"][\"key_sub_2\"][\"enabled\"].changed\n    True\n\n    &gt;&gt;&gt; changes[\"key_2\"].removed_keys\n    {'key_sub_1'}\n\n    &gt;&gt;&gt; list(sorted(changes[\"key_2\"].available_keys))\n    ['key_sub_1', 'key_sub_2', 'key_sub_3']\n\n    &gt;&gt;&gt; changes.new_value == new_value\n    True\n\n    # Get only changed values\n    only_changed_new_values = {\n        key: changes[key].new_value\n        for key in changes.changed_keys\n    }\n    ```\n\n    Args:\n        old_value (Any): Old value.\n        new_value (Any): New value.\n    \"\"\"\n\n    def __init__(self, old_value, new_value):\n        self._changed = old_value != new_value\n        # Resolve if value is '_EMPTY_VALUE' after comparison of the values\n        if old_value is _EMPTY_VALUE:\n            old_value = None\n        if new_value is _EMPTY_VALUE:\n            new_value = None\n        self._old_value = copy.deepcopy(old_value)\n        self._new_value = copy.deepcopy(new_value)\n\n        self._old_is_dict = isinstance(old_value, dict)\n        self._new_is_dict = isinstance(new_value, dict)\n\n        self._old_keys = None\n        self._new_keys = None\n        self._available_keys = None\n        self._removed_keys = None\n\n        self._changed_keys = None\n\n        self._sub_items = None\n\n    def __getitem__(self, key):\n        \"\"\"Getter looks into subitems if object is dictionary.\"\"\"\n\n        if self._sub_items is None:\n            self._prepare_sub_items()\n        return self._sub_items[key]\n\n    def __bool__(self):\n        \"\"\"Boolean of object is if old and new value are the same.\"\"\"\n\n        return self._changed\n\n    def get(self, key, default=None):\n        \"\"\"Try to get sub item.\"\"\"\n\n        if self._sub_items is None:\n            self._prepare_sub_items()\n        return self._sub_items.get(key, default)\n\n    @property\n    def old_value(self):\n        \"\"\"Get copy of old value.\n\n        Returns:\n            Any: Whatever old value was.\n        \"\"\"\n\n        return copy.deepcopy(self._old_value)\n\n    @property\n    def new_value(self):\n        \"\"\"Get copy of new value.\n\n        Returns:\n            Any: Whatever new value was.\n        \"\"\"\n\n        return copy.deepcopy(self._new_value)\n\n    @property\n    def changed(self):\n        \"\"\"Value changed.\n\n        Returns:\n            bool: If data changed.\n        \"\"\"\n\n        return self._changed\n\n    @property\n    def is_dict(self):\n        \"\"\"Object can be used as dictionary.\n\n        Returns:\n            bool: When can be used that way.\n        \"\"\"\n\n        return self._old_is_dict or self._new_is_dict\n\n    @property\n    def changes(self):\n        \"\"\"Get changes in raw data.\n\n        This method should be used only if 'is_dict' value is 'True'.\n\n        Returns:\n            Dict[str, Tuple[Any, Any]]: Changes are by key in tuple\n                (&lt;old value&gt;, &lt;new value&gt;). If 'is_dict' is 'False' then\n                output is always empty dictionary.\n        \"\"\"\n\n        output = {}\n        if not self.is_dict:\n            return output\n\n        old_value = self.old_value\n        new_value = self.new_value\n        for key in self.changed_keys:\n            _old = None\n            _new = None\n            if self._old_is_dict:\n                _old = old_value.get(key)\n            if self._new_is_dict:\n                _new = new_value.get(key)\n            output[key] = (_old, _new)\n        return output\n\n    # Methods/properties that can be used when 'is_dict' is 'True'\n    @property\n    def old_keys(self):\n        \"\"\"Keys from old value.\n\n        Empty set is returned if old value is not a dict.\n\n        Returns:\n            Set[str]: Keys from old value.\n        \"\"\"\n\n        if self._old_keys is None:\n            self._prepare_keys()\n        return set(self._old_keys)\n\n    @property\n    def new_keys(self):\n        \"\"\"Keys from new value.\n\n        Empty set is returned if old value is not a dict.\n\n        Returns:\n            Set[str]: Keys from new value.\n        \"\"\"\n\n        if self._new_keys is None:\n            self._prepare_keys()\n        return set(self._new_keys)\n\n    @property\n    def changed_keys(self):\n        \"\"\"Keys that has changed from old to new value.\n\n        Empty set is returned if both old and new value are not a dict.\n\n        Returns:\n            Set[str]: Keys of changed keys.\n        \"\"\"\n\n        if self._changed_keys is None:\n            self._prepare_sub_items()\n        return set(self._changed_keys)\n\n    @property\n    def available_keys(self):\n        \"\"\"All keys that are available in old and new value.\n\n        Empty set is returned if both old and new value are not a dict.\n        Output is Union of 'old_keys' and 'new_keys'.\n\n        Returns:\n            Set[str]: All keys from old and new value.\n        \"\"\"\n\n        if self._available_keys is None:\n            self._prepare_keys()\n        return set(self._available_keys)\n\n    @property\n    def removed_keys(self):\n        \"\"\"Key that are not available in new value but were in old value.\n\n        Returns:\n            Set[str]: All removed keys.\n        \"\"\"\n\n        if self._removed_keys is None:\n            self._prepare_sub_items()\n        return set(self._removed_keys)\n\n    def _prepare_keys(self):\n        old_keys = set()\n        new_keys = set()\n        if self._old_is_dict and self._new_is_dict:\n            old_keys = set(self._old_value.keys())\n            new_keys = set(self._new_value.keys())\n\n        elif self._old_is_dict:\n            old_keys = set(self._old_value.keys())\n\n        elif self._new_is_dict:\n            new_keys = set(self._new_value.keys())\n\n        self._old_keys = old_keys\n        self._new_keys = new_keys\n        self._available_keys = old_keys | new_keys\n        self._removed_keys = old_keys - new_keys\n\n    def _prepare_sub_items(self):\n        sub_items = {}\n        changed_keys = set()\n\n        old_keys = self.old_keys\n        new_keys = self.new_keys\n        new_value = self.new_value\n        old_value = self.old_value\n        if self._old_is_dict and self._new_is_dict:\n            for key in self.available_keys:\n                item = TrackChangesItem(\n                    old_value.get(key), new_value.get(key)\n                )\n                sub_items[key] = item\n                if item.changed or key not in old_keys or key not in new_keys:\n                    changed_keys.add(key)\n\n        elif self._old_is_dict:\n            old_keys = set(old_value.keys())\n            available_keys = set(old_keys)\n            changed_keys = set(available_keys)\n            for key in available_keys:\n                # NOTE Use '_EMPTY_VALUE' because old value could be 'None'\n                #   which would result in \"unchanged\" item\n                sub_items[key] = TrackChangesItem(\n                    old_value.get(key), _EMPTY_VALUE\n                )\n\n        elif self._new_is_dict:\n            new_keys = set(new_value.keys())\n            available_keys = set(new_keys)\n            changed_keys = set(available_keys)\n            for key in available_keys:\n                # NOTE Use '_EMPTY_VALUE' because new value could be 'None'\n                #   which would result in \"unchanged\" item\n                sub_items[key] = TrackChangesItem(\n                    _EMPTY_VALUE, new_value.get(key)\n                )\n\n        self._sub_items = sub_items\n        self._changed_keys = changed_keys\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/changes.html#client.ayon_core.pipeline.create.changes.TrackChangesItem.available_keys","title":"<code>available_keys</code>  <code>property</code>","text":"<p>All keys that are available in old and new value.</p> <p>Empty set is returned if both old and new value are not a dict. Output is Union of 'old_keys' and 'new_keys'.</p> <p>Returns:</p> Type Description <p>Set[str]: All keys from old and new value.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/changes.html#client.ayon_core.pipeline.create.changes.TrackChangesItem.changed","title":"<code>changed</code>  <code>property</code>","text":"<p>Value changed.</p> <p>Returns:</p> Name Type Description <code>bool</code> <p>If data changed.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/changes.html#client.ayon_core.pipeline.create.changes.TrackChangesItem.changed_keys","title":"<code>changed_keys</code>  <code>property</code>","text":"<p>Keys that has changed from old to new value.</p> <p>Empty set is returned if both old and new value are not a dict.</p> <p>Returns:</p> Type Description <p>Set[str]: Keys of changed keys.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/changes.html#client.ayon_core.pipeline.create.changes.TrackChangesItem.changes","title":"<code>changes</code>  <code>property</code>","text":"<p>Get changes in raw data.</p> <p>This method should be used only if 'is_dict' value is 'True'.</p> <p>Returns:</p> Type Description <p>Dict[str, Tuple[Any, Any]]: Changes are by key in tuple (, ). If 'is_dict' is 'False' then output is always empty dictionary."},{"location":"autoapi/client/ayon_core/pipeline/create/changes.html#client.ayon_core.pipeline.create.changes.TrackChangesItem.is_dict","title":"<code>is_dict</code>  <code>property</code>","text":"<p>Object can be used as dictionary.</p> <p>Returns:</p> Name Type Description <code>bool</code> <p>When can be used that way.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/changes.html#client.ayon_core.pipeline.create.changes.TrackChangesItem.new_keys","title":"<code>new_keys</code>  <code>property</code>","text":"<p>Keys from new value.</p> <p>Empty set is returned if old value is not a dict.</p> <p>Returns:</p> Type Description <p>Set[str]: Keys from new value.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/changes.html#client.ayon_core.pipeline.create.changes.TrackChangesItem.new_value","title":"<code>new_value</code>  <code>property</code>","text":"<p>Get copy of new value.</p> <p>Returns:</p> Name Type Description <code>Any</code> <p>Whatever new value was.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/changes.html#client.ayon_core.pipeline.create.changes.TrackChangesItem.old_keys","title":"<code>old_keys</code>  <code>property</code>","text":"<p>Keys from old value.</p> <p>Empty set is returned if old value is not a dict.</p> <p>Returns:</p> Type Description <p>Set[str]: Keys from old value.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/changes.html#client.ayon_core.pipeline.create.changes.TrackChangesItem.old_value","title":"<code>old_value</code>  <code>property</code>","text":"<p>Get copy of old value.</p> <p>Returns:</p> Name Type Description <code>Any</code> <p>Whatever old value was.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/changes.html#client.ayon_core.pipeline.create.changes.TrackChangesItem.removed_keys","title":"<code>removed_keys</code>  <code>property</code>","text":"<p>Key that are not available in new value but were in old value.</p> <p>Returns:</p> Type Description <p>Set[str]: All removed keys.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/changes.html#client.ayon_core.pipeline.create.changes.TrackChangesItem.__bool__","title":"<code>__bool__()</code>","text":"<p>Boolean of object is if old and new value are the same.</p> Source code in <code>client/ayon_core/pipeline/create/changes.py</code> <pre><code>def __bool__(self):\n    \"\"\"Boolean of object is if old and new value are the same.\"\"\"\n\n    return self._changed\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/changes.html#client.ayon_core.pipeline.create.changes.TrackChangesItem.__getitem__","title":"<code>__getitem__(key)</code>","text":"<p>Getter looks into subitems if object is dictionary.</p> Source code in <code>client/ayon_core/pipeline/create/changes.py</code> <pre><code>def __getitem__(self, key):\n    \"\"\"Getter looks into subitems if object is dictionary.\"\"\"\n\n    if self._sub_items is None:\n        self._prepare_sub_items()\n    return self._sub_items[key]\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/changes.html#client.ayon_core.pipeline.create.changes.TrackChangesItem.get","title":"<code>get(key, default=None)</code>","text":"<p>Try to get sub item.</p> Source code in <code>client/ayon_core/pipeline/create/changes.py</code> <pre><code>def get(self, key, default=None):\n    \"\"\"Try to get sub item.\"\"\"\n\n    if self._sub_items is None:\n        self._prepare_sub_items()\n    return self._sub_items.get(key, default)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/constants.html","title":"constants","text":""},{"location":"autoapi/client/ayon_core/pipeline/create/context.html","title":"context","text":""},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.BulkInfo","title":"<code>BulkInfo</code>","text":"Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>class BulkInfo:\n    def __init__(self):\n        self._count = 0\n        self._data = []\n        self._sender = None\n\n    def __bool__(self):\n        return self._count == 0\n\n    def get_sender(self):\n        return self._sender\n\n    def set_sender(self, sender):\n        if sender is not None:\n            self._sender = sender\n\n    def increase(self):\n        self._count += 1\n\n    def decrease(self):\n        self._count -= 1\n\n    def append(self, item):\n        self._data.append(item)\n\n    def get_data(self):\n        \"\"\"Use this method for read-only.\"\"\"\n        return self._data\n\n    def pop_data(self):\n        data = self._data\n        self._data = []\n        self._sender = None\n        return data\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.BulkInfo.get_data","title":"<code>get_data()</code>","text":"<p>Use this method for read-only.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def get_data(self):\n    \"\"\"Use this method for read-only.\"\"\"\n    return self._data\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext","title":"<code>CreateContext</code>","text":"<p>Context of instance creation.</p> <p>Context itself also can store data related to whole creation (workfile). - those are mainly for Context publish plugins</p> <p>Parameters:</p> Name Type Description Default <code>host</code> <code>IPublishHost</code> <p>Host implementation which handles implementation and global metadata.</p> required <code>headless</code> <code>bool</code> <p>Context is created out of UI (Current not used).</p> <code>False</code> <code>reset</code> <code>bool</code> <p>Reset context on initialization.</p> <code>True</code> <code>discover_publish_plugins</code> <code>bool</code> <p>Discover publish plugins during reset phase.</p> <code>True</code> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>class CreateContext:\n    \"\"\"Context of instance creation.\n\n    Context itself also can store data related to whole creation (workfile).\n    - those are mainly for Context publish plugins\n\n    Args:\n        host (IPublishHost): Host implementation which handles implementation\n            and global metadata.\n        headless (bool): Context is created out of UI (Current not used).\n        reset (bool): Reset context on initialization.\n        discover_publish_plugins (bool): Discover publish plugins during reset\n            phase.\n    \"\"\"\n\n    def __init__(\n        self,\n        host: IPublishHost,\n        headless: bool = False,\n        reset: bool = True,\n        discover_publish_plugins: bool = True,\n    ):\n        self.host = host\n\n        # Prepare attribute for logger (Created on demand in `log` property)\n        self._log = None\n        self._event_hub = QueuedEventSystem()\n\n        # Publish context plugins attributes and it's values\n        self._publish_attributes = PublishAttributes(self, {})\n        self._original_context_data = {}\n\n        # Validate host implementation\n        # - defines if context is capable of handling context data\n        host_is_valid = True\n        missing_methods = self.get_host_misssing_methods(host)\n        if missing_methods:\n            host_is_valid = False\n            joined_methods = \", \".join(\n                ['\"{}\"'.format(name) for name in missing_methods]\n            )\n            self.log.warning((\n                \"Host miss required methods to be able use creation.\"\n                \" Missing methods: {}\"\n            ).format(joined_methods))\n\n        self._current_project_name = None\n        self._current_folder_path = None\n        self._current_task_name = None\n        self._current_workfile_path = None\n        self._current_project_settings = None\n\n        self._current_project_entity = _NOT_SET\n        self._current_folder_entity = _NOT_SET\n        self._current_task_entity = _NOT_SET\n        self._current_task_type = _NOT_SET\n\n        self._current_project_anatomy = None\n\n        self._host_is_valid = host_is_valid\n        # Currently unused variable\n        self.headless = headless\n\n        # Instances by their ID\n        self._instances_by_id = {}\n\n        self.creator_discover_result = None\n        self.convertor_discover_result = None\n        # Discovered creators\n        self.creators: dict[str, \"BaseCreator\"] = {}\n        # Prepare categories of creators\n        self.autocreators = {}\n        # Manual creators\n        self.manual_creators = {}\n        # Creators that are disabled\n        self.disabled_creators = {}\n\n        self.convertors_plugins = {}\n        self.convertor_items_by_id = {}\n\n        self.publish_discover_result: Optional[DiscoverResult] = None\n        self.publish_plugins_mismatch_targets = []\n        self.publish_plugins = []\n        self.plugins_with_defs = []\n\n        # Helpers for validating context of collected instances\n        #   - they can be validation for multiple instances at one time\n        #       using context manager which will trigger validation\n        #       after leaving of last context manager scope\n        self._bulk_info = {\n            # Added instances\n            \"add\": BulkInfo(),\n            # Removed instances\n            \"remove\": BulkInfo(),\n            # Change values of instances or create context\n            \"change\": BulkInfo(),\n            # Pre create attribute definitions changed\n            \"pre_create_attrs_change\": BulkInfo(),\n            # Create attribute definitions changed\n            \"create_attrs_change\": BulkInfo(),\n            # Publish attribute definitions changed\n            \"publish_attrs_change\": BulkInfo(),\n            # Instance requirement changed\n            # - right now used only for 'mandatory' but can be extended\n            #   in future\n            \"requirement_change\": BulkInfo(),\n            # Instance parent changed\n            \"parent_change\": BulkInfo(),\n        }\n        self._bulk_order = []\n\n        # Shared data across creators during collection phase\n        self._collection_shared_data = None\n\n        # Entities cache\n        self._folder_entities_by_path = {}\n        self._task_entities_by_id = {}\n        self._task_ids_by_folder_path = {}\n        self._task_names_by_folder_path = {}\n\n        self.thumbnail_paths_by_instance_id = {}\n\n        # Trigger reset if was enabled\n        if reset:\n            self.reset(discover_publish_plugins)\n\n    @property\n    def instances(self) -&gt; Iterable[\"CreatedInstance\"]:\n        return self._instances_by_id.values()\n\n    @property\n    def instances_by_id(self) -&gt; dict[str, \"CreatedInstance\"]:\n        return self._instances_by_id\n\n    @property\n    def publish_attributes(self) -&gt; PublishAttributes:\n        \"\"\"Access to global publish attributes.\"\"\"\n        return self._publish_attributes\n\n    def get_instance_by_id(\n        self, instance_id: str\n    ) -&gt; Optional[\"CreatedInstance\"]:\n        \"\"\"Receive instance by id.\n\n        Args:\n            instance_id (str): Instance id.\n\n        Returns:\n            Optional[CreatedInstance]: Instance or None if instance with\n                given id is not available.\n\n        \"\"\"\n        return self._instances_by_id.get(instance_id)\n\n    def get_sorted_creators(\n        self, identifiers: Optional[Iterable[str]] = None\n    ) -&gt; list[\"BaseCreator\"]:\n        \"\"\"Sorted creators by 'order' attribute.\n\n        Args:\n            identifiers (Optional[Iterable[str]]): Filter creators by\n                identifiers. All creators are returned if 'None' is passed.\n\n        Returns:\n            list[BaseCreator]: Sorted creator plugins by 'order' value.\n\n        \"\"\"\n        if identifiers is not None:\n            identifiers = set(identifiers)\n            creators = [\n                creator\n                for identifier, creator in self.creators.items()\n                if identifier in identifiers\n            ]\n        else:\n            creators = self.creators.values()\n\n        return sorted(\n            creators, key=lambda creator: creator.order\n        )\n\n    @property\n    def sorted_creators(self) -&gt; list[\"BaseCreator\"]:\n        \"\"\"Sorted creators by 'order' attribute.\n\n        Returns:\n            list[BaseCreator]: Sorted creator plugins by 'order' value.\n        \"\"\"\n\n        return self.get_sorted_creators()\n\n    @property\n    def sorted_autocreators(self) -&gt; list[\"AutoCreator\"]:\n        \"\"\"Sorted auto-creators by 'order' attribute.\n\n        Returns:\n            list[AutoCreator]: Sorted plugins by 'order' value.\n        \"\"\"\n\n        return sorted(\n            self.autocreators.values(), key=lambda creator: creator.order\n        )\n\n    @classmethod\n    def get_host_misssing_methods(cls, host):\n        \"\"\"Collect missing methods from host.\n\n        Args:\n            host(ModuleType): Host implementaion.\n        \"\"\"\n\n        missing = set(\n            IPublishHost.get_missing_publish_methods(host)\n        )\n        return missing\n\n    @property\n    def host_is_valid(self):\n        \"\"\"Is host valid for creation.\"\"\"\n        return self._host_is_valid\n\n    @property\n    def host_name(self) -&gt; str:\n        if hasattr(self.host, \"name\"):\n            return self.host.name\n        return os.environ[\"AYON_HOST_NAME\"]\n\n    def get_current_project_name(self) -&gt; str:\n        \"\"\"Project name which was used as current context on context reset.\n\n        Returns:\n            Optional[str]: Project name.\n\n        \"\"\"\n        return self._current_project_name\n\n    def get_current_folder_path(self) -&gt; Optional[str]:\n        \"\"\"Folder path which was used as current context on context reset.\n\n        Returns:\n            Optional[str]: Folder path.\n\n        \"\"\"\n        return self._current_folder_path\n\n    def get_current_task_name(self) -&gt; Optional[str]:\n        \"\"\"Task name which was used as current context on context reset.\n\n        Returns:\n            Optional[str]: Task name.\n\n        \"\"\"\n        return self._current_task_name\n\n    def get_current_task_type(self) -&gt; Optional[str]:\n        \"\"\"Task type which was used as current context on context reset.\n\n        Returns:\n            Optional[str]: Task type.\n\n        \"\"\"\n        if self._current_task_type is _NOT_SET:\n            task_type = None\n            task_entity = self.get_current_task_entity()\n            if task_entity:\n                task_type = task_entity[\"taskType\"]\n            self._current_task_type = task_type\n        return self._current_task_type\n\n    def get_current_project_entity(self) -&gt; Optional[dict[str, Any]]:\n        \"\"\"Project entity for current context project.\n\n        Returns:\n            Optional[dict[str, Any]]: Folder entity.\n\n        \"\"\"\n        if self._current_project_entity is not _NOT_SET:\n            return copy.deepcopy(self._current_project_entity)\n        project_entity = None\n        project_name = self.get_current_project_name()\n        if project_name:\n            project_entity = ayon_api.get_project(project_name)\n        self._current_project_entity = project_entity\n        return copy.deepcopy(self._current_project_entity)\n\n    def get_current_folder_entity(self) -&gt; Optional[dict[str, Any]]:\n        \"\"\"Folder entity for current context folder.\n\n        Returns:\n            Optional[dict[str, Any]]: Folder entity.\n\n        \"\"\"\n        if self._current_folder_entity is not _NOT_SET:\n            return copy.deepcopy(self._current_folder_entity)\n\n        folder_path = self.get_current_folder_path()\n        self._current_folder_entity = self.get_folder_entity(folder_path)\n        return copy.deepcopy(self._current_folder_entity)\n\n    def get_current_task_entity(self) -&gt; Optional[dict[str, Any]]:\n        \"\"\"Task entity for current context task.\n\n        Returns:\n            Optional[dict[str, Any]]: Task entity.\n\n        \"\"\"\n        if self._current_task_entity is not _NOT_SET:\n            return copy.deepcopy(self._current_task_entity)\n\n        folder_path = self.get_current_folder_path()\n        task_name = self.get_current_task_name()\n        self._current_task_entity = self.get_task_entity(\n            folder_path, task_name\n        )\n        return copy.deepcopy(self._current_task_entity)\n\n    def get_current_workfile_path(self) -&gt; Optional[str]:\n        \"\"\"Workfile path which was opened on context reset.\n\n        Returns:\n            Optional[str]: Workfile path.\n\n        \"\"\"\n        return self._current_workfile_path\n\n    def get_current_project_anatomy(self) -&gt; Anatomy:\n        \"\"\"Project anatomy for current project.\n\n        Returns:\n            Anatomy: Anatomy object ready to be used.\n        \"\"\"\n\n        if self._current_project_anatomy is None:\n            self._current_project_anatomy = Anatomy(\n                self._current_project_name)\n        return self._current_project_anatomy\n\n    def get_current_project_settings(self) -&gt; dict[str, Any]:\n        if self._current_project_settings is None:\n            self._current_project_settings = get_project_settings(\n                self.get_current_project_name())\n        return self._current_project_settings\n\n    def get_template_data(\n        self, folder_path: Optional[str], task_name: Optional[str]\n    ) -&gt; dict[str, Any]:\n        \"\"\"Prepare template data for given context.\n\n        Method is using cached entities and settings to prepare template data.\n\n        Args:\n            folder_path (Optional[str]): Folder path.\n            task_name (Optional[str]): Task name.\n\n        Returns:\n            dict[str, Any]: Template data.\n\n        \"\"\"\n        project_entity = self.get_current_project_entity()\n        folder_entity = task_entity = None\n        if folder_path:\n            folder_entity = self.get_folder_entity(folder_path)\n            if task_name and folder_entity:\n                task_entity = self.get_task_entity(folder_path, task_name)\n\n        return get_template_data(\n            project_entity,\n            folder_entity,\n            task_entity,\n            host_name=self.host_name,\n            settings=self.get_current_project_settings(),\n        )\n\n    @property\n    def context_has_changed(self) -&gt; bool:\n        \"\"\"Host context has changed.\n\n        As context is used project, folder, task name and workfile path if\n        host does support workfiles.\n\n        Returns:\n            bool: Context changed.\n\n        \"\"\"\n        project_name, folder_path, task_name, workfile_path = (\n            self._get_current_host_context()\n        )\n        return (\n            self._current_project_name != project_name\n            or self._current_folder_path != folder_path\n            or self._current_task_name != task_name\n            or self._current_workfile_path != workfile_path\n        )\n\n    project_name: str = property(get_current_project_name)\n    project_anatomy: Anatomy = property(get_current_project_anatomy)\n\n    @property\n    def log(self) -&gt; logging.Logger:\n        \"\"\"Dynamic access to logger.\"\"\"\n        if self._log is None:\n            self._log = logging.getLogger(self.__class__.__name__)\n        return self._log\n\n    def reset(self, discover_publish_plugins: bool = True) -&gt; None:\n        \"\"\"Reset context with all plugins and instances.\n\n        All changes will be lost if were not saved explicitely.\n        \"\"\"\n\n        self.reset_preparation()\n\n        self.reset_current_context()\n        self.reset_plugins(discover_publish_plugins)\n        self.reset_context_data()\n\n        with self.bulk_add_instances():\n            self.reset_instances()\n            self.find_convertor_items()\n            self.execute_autocreators()\n\n        self.reset_finalization()\n\n    def refresh_thumbnails(self) -&gt; None:\n        \"\"\"Cleanup thumbnail paths.\n\n        Remove all thumbnail filepaths that are empty or lead to files which\n        does not exist or of instances that are not available anymore.\n        \"\"\"\n\n        invalid = set()\n        for instance_id, path in self.thumbnail_paths_by_instance_id.items():\n            instance_available = True\n            if instance_id is not None:\n                instance_available = instance_id in self._instances_by_id\n\n            if (\n                not instance_available\n                or not path\n                or not os.path.exists(path)\n            ):\n                invalid.add(instance_id)\n\n        for instance_id in invalid:\n            self.thumbnail_paths_by_instance_id.pop(instance_id)\n\n    def reset_preparation(self) -&gt; None:\n        \"\"\"Prepare attributes that must be prepared/cleaned before reset.\"\"\"\n\n        # Give ability to store shared data for collection phase\n        self._collection_shared_data = {}\n\n        self._folder_entities_by_path = {}\n        self._task_entities_by_id = {}\n\n        self._task_ids_by_folder_path = {}\n        self._task_names_by_folder_path = {}\n\n        self._event_hub.clear_callbacks()\n\n    def reset_finalization(self) -&gt; None:\n        \"\"\"Cleanup of attributes after reset.\"\"\"\n\n        # Stop access to collection shared data\n        self._collection_shared_data = None\n        self.refresh_thumbnails()\n\n    def _get_current_host_context(\n        self\n    ) -&gt; tuple[str, Optional[str], Optional[str], Optional[str]]:\n        project_name = folder_path = task_name = workfile_path = None\n        if hasattr(self.host, \"get_current_context\"):\n            host_context = self.host.get_current_context()\n            if host_context:\n                project_name = host_context.get(\"project_name\")\n                folder_path = host_context.get(\"folder_path\")\n                task_name = host_context.get(\"task_name\")\n\n        if isinstance(self.host, IWorkfileHost):\n            workfile_path = self.host.get_current_workfile()\n\n        return project_name, folder_path, task_name, workfile_path\n\n    def reset_current_context(self) -&gt; None:\n        \"\"\"Refresh current context.\n\n        Reset is based on optional host implementation of `get_current_context`\n        function.\n\n        Some hosts have ability to change context file without using workfiles\n        tool but that change is not propagated to 'os.environ'.\n\n        Todos:\n            UI: Current context should be also checked on save - compare\n                initial values vs. current values.\n            Related to UI checks: Current workfile can be also considered\n                as current context information as that's where the metadata\n                are stored. We should store the workfile (if is available) too.\n        \"\"\"\n\n        project_name, folder_path, task_name, workfile_path = (\n            self._get_current_host_context()\n        )\n\n        self._current_project_name = project_name\n        self._current_folder_path = folder_path\n        self._current_task_name = task_name\n        self._current_workfile_path = workfile_path\n\n        self._current_project_entity = _NOT_SET\n        self._current_folder_entity = _NOT_SET\n        self._current_task_entity = _NOT_SET\n        self._current_task_type = _NOT_SET\n\n        self._current_project_anatomy = None\n        self._current_project_settings = None\n\n    def reset_plugins(self, discover_publish_plugins: bool = True) -&gt; None:\n        \"\"\"Reload plugins.\n\n        Reloads creators from preregistered paths and can load publish plugins\n        if it's enabled on context.\n        \"\"\"\n\n        self._reset_publish_plugins(discover_publish_plugins)\n        self._reset_creator_plugins()\n        self._reset_convertor_plugins()\n\n    def _reset_publish_plugins(self, discover_publish_plugins: bool) -&gt; None:\n        from ayon_core.pipeline import AYONPyblishPluginMixin\n        from ayon_core.pipeline.publish import (\n            publish_plugins_discover\n        )\n\n        discover_result = DiscoverResult(pyblish.api.Plugin)\n        plugins_with_defs = []\n        plugins_by_targets = []\n        plugins_mismatch_targets = []\n        if discover_publish_plugins:\n            discover_result = publish_plugins_discover()\n            publish_plugins = discover_result.plugins\n\n            targets = set(pyblish.logic.registered_targets())\n            targets.add(\"default\")\n            plugins_by_targets = pyblish.logic.plugins_by_targets(\n                publish_plugins, list(targets)\n            )\n\n            # Collect plugins that can have attribute definitions\n            for plugin in publish_plugins:\n                if AYONPyblishPluginMixin in inspect.getmro(plugin):\n                    plugins_with_defs.append(plugin)\n\n            plugins_mismatch_targets = [\n                plugin\n                for plugin in publish_plugins\n                if plugin not in plugins_by_targets\n            ]\n\n        # Register create context callbacks\n        for plugin in plugins_with_defs:\n            if not inspect.ismethod(plugin.register_create_context_callbacks):\n                self.log.warning(\n                    f\"Plugin {plugin.__name__} does not have\"\n                    f\" 'register_create_context_callbacks'\"\n                    f\" defined as class method.\"\n                )\n                continue\n            try:\n                plugin.register_create_context_callbacks(self)\n            except Exception:\n                self.log.error(\n                    f\"Failed to register callbacks for plugin\"\n                    f\" {plugin.__name__}.\",\n                    exc_info=True\n                )\n\n        self.publish_plugins_mismatch_targets = plugins_mismatch_targets\n        self.publish_discover_result = discover_result\n        self.publish_plugins = plugins_by_targets\n        self.plugins_with_defs = plugins_with_defs\n\n    def _reset_creator_plugins(self) -&gt; None:\n        # Prepare settings\n        project_settings = self.get_current_project_settings()\n\n        # Discover and prepare creators\n        creators = {}\n        disabled_creators = {}\n        autocreators = {}\n        manual_creators = {}\n        report = discover_creator_plugins(return_report=True)\n        self.creator_discover_result = report\n        for creator_class in report.abstract_plugins:\n            self.log.debug(\n                \"Skipping abstract Creator '%s'\",\n                str(creator_class)\n            )\n\n        for creator_class in report.plugins:\n            creator_identifier = creator_class.identifier\n            if creator_identifier in creators:\n                self.log.warning(\n                    \"Duplicate Creator identifier: '%s'. Using first Creator \"\n                    \"and skipping: %s\", creator_identifier, creator_class\n                )\n                continue\n\n            # Filter by host name\n            if (\n                creator_class.host_name\n                and creator_class.host_name != self.host_name\n            ):\n                self.log.info(\n                    (\n                        'Creator\\'s host name \"{}\"'\n                        ' is not supported for current host \"{}\"'\n                    ).format(creator_class.host_name, self.host_name)\n                )\n                continue\n\n            # TODO report initialization error\n            try:\n                creator = creator_class(project_settings, self, self.headless)\n            except Exception:\n                self.log.error(\n                    f\"Failed to initialize plugin: {creator_class}\",\n                    exc_info=True\n                )\n                continue\n\n            if not creator.product_base_type:\n                message = (\n                    f\"Provided creator {creator!r} doesn't have \"\n                    \"product base type attribute defined. This will be \"\n                    \"required in future.\"\n                )\n                warn(\n                    message,\n                    DeprecationWarning,\n                    stacklevel=2\n                )\n                self.log.warning(message)\n\n            if not creator.enabled:\n                disabled_creators[creator_identifier] = creator\n                continue\n            creators[creator_identifier] = creator\n            if isinstance(creator, AutoCreator):\n                autocreators[creator_identifier] = creator\n            elif isinstance(creator, Creator):\n                manual_creators[creator_identifier] = creator\n\n        self.autocreators = autocreators\n        self.manual_creators = manual_creators\n\n        self.creators = creators\n        self.disabled_creators = disabled_creators\n\n    def _reset_convertor_plugins(self) -&gt; None:\n        convertors_plugins = {}\n        report = discover_convertor_plugins(return_report=True)\n        self.convertor_discover_result = report\n        for convertor_class in report.plugins:\n            if inspect.isabstract(convertor_class):\n                self.log.info(\n                    \"Skipping abstract Creator {}\".format(str(convertor_class))\n                )\n                continue\n\n            convertor_identifier = convertor_class.identifier\n            if convertor_identifier in convertors_plugins:\n                self.log.warning((\n                    \"Duplicated Converter identifier. \"\n                    \"Using first and skipping following\"\n                ))\n                continue\n\n            convertors_plugins[convertor_identifier] = convertor_class(self)\n\n        self.convertors_plugins = convertors_plugins\n\n    def reset_context_data(self) -&gt; None:\n        \"\"\"Reload context data using host implementation.\n\n        These data are not related to any instance but may be needed for whole\n        publishing.\n        \"\"\"\n        if not self.host_is_valid:\n            self._original_context_data = {}\n            self._publish_attributes = PublishAttributes(self, {})\n            return\n\n        original_data = self.host.get_context_data() or {}\n        self._original_context_data = copy.deepcopy(original_data)\n\n        publish_attributes = original_data.get(\"publish_attributes\") or {}\n\n        self._publish_attributes = PublishAttributes(\n            self, publish_attributes\n        )\n\n        for plugin in self.plugins_with_defs:\n            if is_func_signature_supported(\n                plugin.convert_attribute_values, self, None\n            ):\n                plugin.convert_attribute_values(self, None)\n\n            elif not plugin.__instanceEnabled__:\n                output = plugin.convert_attribute_values(publish_attributes)\n                if output:\n                    publish_attributes.update(output)\n\n        for plugin in self.plugins_with_defs:\n            try:\n                attr_defs = plugin.get_attr_defs_for_context(self)\n            except Exception:\n                self.log.error(\n                    \"Failed to get attribute definitions\"\n                    f\" from plugin '{plugin.__name__}'.\",\n                    exc_info=True\n                )\n                continue\n\n            if not attr_defs:\n                continue\n            self._publish_attributes.set_publish_plugin_attr_defs(\n                plugin.__name__, attr_defs\n            )\n\n    def trigger_pre_create_button_callback(\n        self, identifier: str, button_name: str\n    ) -&gt; None:\n        \"\"\"Trigger pre-create button callback.\n\n        Args:\n            identifier (str): Creator identifier.\n            button_name (str): Button name.\n\n        Raises:\n            ValueError if create plugin was not found, does not support\n                pre-create attributes, or button was not found.\n\n        \"\"\"\n        create_plugin = self.creators.get(identifier)\n        if create_plugin is None:\n            return\n\n        if not hasattr(create_plugin, \"get_pre_create_attr_defs\"):\n            return\n\n        pre_create_attributes = create_plugin.get_pre_create_attr_defs()\n        for attr_def in pre_create_attributes:\n            if attr_def.key == button_name:\n                attr_def.trigger()\n                return\n\n    def trigger_create_button_callback(\n        self,\n        button_name: str,\n        instance_ids: list[str],\n        *,\n        identifier: str | None = None,\n    ) -&gt; None:\n        \"\"\"Trigger pre-create button callback.\n\n        Args:\n            button_name (str): Button name.\n            instance_ids (list[str]): List of instance ids for which\n                the button will be triggered. The button will be triggered\n                only for instances that have the button.\n            identifier (str | None): Creator identifier. If None,\n                the button will be triggered for all creators.\n\n        \"\"\"\n        info_by_identifier = {}\n        for instance_id in instance_ids:\n            instance = self.instances_by_id.get(instance_id)\n            if instance is None:\n                continue\n\n            plugin_id = instance.creator_identifier\n            if identifier is not None and plugin_id != identifier:\n                continue\n\n            attr_def = instance.creator_attributes.get_attr_def(button_name)\n            if not isinstance(attr_def, ButtonDef):\n                continue\n\n            info = info_by_identifier.get(plugin_id)\n            if info is None:\n                info = {\n                    \"attr_def\": attr_def,\n                    \"instance_ids\": []\n                }\n                info_by_identifier[plugin_id] = info\n\n            info[\"instance_ids\"].append(instance_id)\n\n        for info in info_by_identifier.values():\n            attr_def = info[\"attr_def\"]\n\n            callback_info = ButtonCallbackInfo(\n                create_context=self,\n                instance_ids=info[\"instance_ids\"],\n            )\n            callback = attr_def.get_callback()\n            if is_func_signature_supported(callback, callback_info):\n                callback(callback_info)\n            else:\n                callback()\n\n    def trigger_publish_button_callback(\n        self,\n        plugin_name: str,\n        button_name: str,\n        instance_ids: list[str | None],\n    ) -&gt; None:\n        \"\"\"Trigger the publish plugin button callback.\n\n        Args:\n            plugin_name (str): Plugin name.\n            button_name (str): Button name.\n            instance_ids (list[str | None]): List of instance ids for which\n                the button will be triggered. The button will be triggered\n                only for instances that have the button. 'None' is used for\n                context selection.\n\n        \"\"\"\n        filtered_instance_ids = []\n        attr_def = None\n        for instance_id in instance_ids:\n            if instance_id is None:\n                publish_attributes = self.publish_attributes\n            else:\n                instance = self.get_instance_by_id(instance_id)\n                if instance is None:\n                    continue\n                publish_attributes = instance.publish_attributes\n\n            plugin_attributes = publish_attributes.get(plugin_name)\n            if not plugin_attributes:\n                continue\n\n            _attr_def = plugin_attributes.get_attr_def(button_name)\n            if not isinstance(_attr_def, ButtonDef):\n                continue\n\n            if attr_def is None:\n                attr_def = _attr_def\n\n            filtered_instance_ids.append(instance_id)\n\n        if attr_def is None:\n            return\n\n        info = ButtonCallbackInfo(\n            create_context=self,\n            instance_ids=filtered_instance_ids,\n        )\n        callback = attr_def.get_callback()\n        if is_func_signature_supported(callback, info):\n            callback(info)\n        else:\n            callback()\n\n    def add_instances_added_callback(\n        self, callback: Callable\n    ) -&gt; \"EventCallback\":\n        \"\"\"Register callback for added instances.\n\n        Event is triggered when instances are already available in context\n            and have set create/publish attribute definitions.\n\n        Data structure of event:\n\n            ```python\n            {\n                \"instances\": [CreatedInstance, ...],\n                \"create_context\": CreateContext\n            }\n            ```\n\n        Args:\n            callback (Callable): Callback function that will be called when\n                instances are added to context.\n\n        Returns:\n            EventCallback: Created callback object which can be used to\n                stop listening.\n\n        \"\"\"\n        return self._event_hub.add_callback(INSTANCE_ADDED_TOPIC, callback)\n\n    def add_instances_removed_callback(\n        self, callback: Callable\n    ) -&gt; \"EventCallback\":\n        \"\"\"Register callback for removed instances.\n\n        Event is triggered when instances are already removed from context.\n\n        Data structure of event:\n\n            ```python\n            {\n                \"instances\": [CreatedInstance, ...],\n                \"create_context\": CreateContext\n            }\n            ```\n\n        Args:\n            callback (Callable): Callback function that will be called when\n                instances are removed from context.\n\n        Returns:\n            EventCallback: Created callback object which can be used to\n                stop listening.\n\n        \"\"\"\n        return self._event_hub.add_callback(INSTANCE_REMOVED_TOPIC, callback)\n\n    def add_value_changed_callback(\n        self, callback: Callable\n    ) -&gt; \"EventCallback\":\n        \"\"\"Register callback to listen value changes.\n\n        Event is triggered when any value changes on any instance or\n            context data.\n\n        Data structure of event:\n\n            ```python\n            {\n                \"changes\": [\n                    {\n                        \"instance\": CreatedInstance,\n                        \"changes\": {\n                            \"folderPath\": \"/new/folder/path\",\n                            \"creator_attributes\": {\n                                \"attr_1\": \"value_1\"\n                            }\n                        }\n                    }\n                ],\n                \"create_context\": CreateContext\n            }\n            ```\n\n        Args:\n            callback (Callable): Callback function that will be called when\n                value changed.\n\n        Returns:\n            EventCallback: Created callback object which can be used to\n                stop listening.\n\n        \"\"\"\n        return self._event_hub.add_callback(VALUE_CHANGED_TOPIC, callback)\n\n    def add_pre_create_attr_defs_change_callback(\n        self, callback: Callable\n    ) -&gt; \"EventCallback\":\n        \"\"\"Register callback to listen pre-create attribute changes.\n\n        Create plugin can trigger refresh of pre-create attributes. Usage of\n            this event is mainly for publisher UI.\n\n        Data structure of event:\n\n            ```python\n            {\n                \"identifiers\": [\"create_plugin_identifier\"],\n                \"create_context\": CreateContext\n            }\n            ```\n\n        Args:\n            callback (Callable): Callback function that will be called when\n                pre-create attributes should be refreshed.\n\n        Returns:\n            EventCallback: Created callback object which can be used to\n                stop listening.\n\n        \"\"\"\n        return self._event_hub.add_callback(\n            PRE_CREATE_ATTR_DEFS_CHANGED_TOPIC, callback\n        )\n\n    def add_create_attr_defs_change_callback(\n        self, callback: Callable\n    ) -&gt; \"EventCallback\":\n        \"\"\"Register callback to listen create attribute changes.\n\n        Create plugin changed attribute definitions of instance.\n\n        Data structure of event:\n\n            ```python\n            {\n                \"instances\": [CreatedInstance, ...],\n                \"create_context\": CreateContext\n            }\n            ```\n\n        Args:\n            callback (Callable): Callback function that will be called when\n                create attributes changed.\n\n        Returns:\n            EventCallback: Created callback object which can be used to\n                stop listening.\n\n        \"\"\"\n        return self._event_hub.add_callback(\n            CREATE_ATTR_DEFS_CHANGED_TOPIC, callback\n        )\n\n    def add_publish_attr_defs_change_callback(\n        self, callback: Callable\n    ) -&gt; \"EventCallback\":\n        \"\"\"Register callback to listen publish attribute changes.\n\n        Publish plugin changed attribute definitions of instance of context.\n\n        Data structure of event:\n\n            ```python\n            {\n                \"instance_changes\": {\n                    None: {\n                        \"instance\": None,\n                        \"plugin_names\": {\"PluginA\"},\n                    }\n                    \"&lt;instance_id&gt;\": {\n                        \"instance\": CreatedInstance,\n                        \"plugin_names\": {\"PluginB\", \"PluginC\"},\n                    }\n                },\n                \"create_context\": CreateContext\n            }\n            ```\n\n        Args:\n            callback (Callable): Callback function that will be called when\n                publish attributes changed.\n\n        Returns:\n            EventCallback: Created callback object which can be used to\n                stop listening.\n\n        \"\"\"\n        return self._event_hub.add_callback(\n            PUBLISH_ATTR_DEFS_CHANGED_TOPIC, callback\n        )\n\n    def add_instance_requirement_change_callback(\n        self, callback: Callable\n    ) -&gt; \"EventCallback\":\n        \"\"\"Register callback to listen to instance requirement changes.\n\n        Instance changed requirement of active state.\n\n        Data structure of event:\n\n            ```python\n            {\n                \"instances\": [CreatedInstance, ...],\n                \"create_context\": CreateContext\n            }\n            ```\n\n        Args:\n            callback (Callable): Callback function that will be called when\n                instance requirement changed.\n\n        Returns:\n            EventCallback: Created callback object which can be used to\n                stop listening.\n\n        \"\"\"\n        return self._event_hub.add_callback(\n            INSTANCE_REQUIREMENT_CHANGED_TOPIC, callback\n        )\n\n    def add_instance_parent_change_callback(\n        self, callback: Callable\n    ) -&gt; \"EventCallback\":\n        \"\"\"Register callback to listen to instance parent changes.\n\n        Instance changed parent or parent flags.\n\n        Data structure of event:\n\n            ```python\n            {\n                \"instances\": [CreatedInstance, ...],\n                \"create_context\": CreateContext\n            }\n            ```\n\n        Args:\n            callback (Callable): Callback function that will be called when\n                instance requirement changed.\n\n        Returns:\n            EventCallback: Created callback object which can be used to\n                stop listening.\n\n        \"\"\"\n        return self._event_hub.add_callback(\n            INSTANCE_PARENT_CHANGED_TOPIC, callback\n        )\n\n    def context_data_to_store(self) -&gt; dict[str, Any]:\n        \"\"\"Data that should be stored by host function.\n\n        The same data should be returned on loading.\n        \"\"\"\n        return {\n            \"publish_attributes\": self._publish_attributes.data_to_store()\n        }\n\n    def context_data_changes(self) -&gt; TrackChangesItem:\n        \"\"\"Changes of attributes.\"\"\"\n\n        return TrackChangesItem(\n            self._original_context_data, self.context_data_to_store()\n        )\n\n    def set_context_publish_plugin_attr_defs(\n        self, plugin_name: str, attr_defs: list[\"AbstractAttrDef\"]\n    ) -&gt; None:\n        \"\"\"Set attribute definitions for CreateContext publish plugin.\n\n        Args:\n            plugin_name(str): Name of publish plugin.\n            attr_defs(list[AbstractAttrDef]): Attribute definitions.\n\n        \"\"\"\n        self.publish_attributes.set_publish_plugin_attr_defs(\n            plugin_name, attr_defs\n        )\n        self.instance_publish_attr_defs_changed(\n            None, plugin_name\n        )\n\n    def creator_adds_instance(self, instance: \"CreatedInstance\") -&gt; None:\n        \"\"\"Creator adds new instance to context.\n\n        Instances should be added only from creators.\n\n        Args:\n            instance(CreatedInstance): Instance with prepared data from\n                creator.\n\n        TODO: Rename method to more suit.\n        \"\"\"\n        # Add instance to instances list\n        if instance.id in self._instances_by_id:\n            self.log.warning((\n                \"Instance with id {} is already added to context.\"\n            ).format(instance.id))\n            return\n\n        self._instances_by_id[instance.id] = instance\n\n        # Add instance to be validated inside 'bulk_add_instances'\n        #   context manager if is inside bulk\n        with self.bulk_add_instances() as bulk_info:\n            bulk_info.append(instance)\n\n    def _get_creator_in_create(self, identifier: str) -&gt; \"BaseCreator\":\n        \"\"\"Creator by identifier with unified error.\n\n        Helper method to get creator by identifier with same error when creator\n        is not available.\n\n        Args:\n            identifier (str): Identifier of creator plugin.\n\n        Returns:\n            BaseCreator: Creator found by identifier.\n\n        Raises:\n            CreatorError: When identifier is not known.\n        \"\"\"\n\n        creator = self.creators.get(identifier)\n        # Fake CreatorError (Could be maybe specific exception?)\n        if creator is None:\n            raise CreatorError(\n                \"Creator {} was not found\".format(identifier)\n            )\n        return creator\n\n    def create(\n        self,\n        creator_identifier: str,\n        variant: str,\n        folder_entity: Optional[dict[str, Any]] = None,\n        task_entity: Optional[dict[str, Any]] = None,\n        pre_create_data: Optional[dict[str, Any]] = None,\n        active: Optional[bool] = None,\n        product_type: Optional[str] = None,\n    ) -&gt; Any:\n        \"\"\"Trigger create of plugins with standartized arguments.\n\n        Arguments 'folder_entity' and 'task_name' use current context as\n        default values. If only 'task_entity' is provided it will be\n        overridden by task name from current context. If 'task_name' is not\n        provided when 'folder_entity' is, it is considered that task name is\n        not specified, which can lead to error if product name template\n        requires task name.\n\n        Args:\n            creator_identifier (str): Identifier of creator plugin.\n            variant (str): Variant used for product name.\n            folder_entity (dict[str, Any]): Folder entity which define context\n                of creation (possible context of created instance/s).\n            task_entity (dict[str, Any]): Task entity.\n            pre_create_data (dict[str, Any]): Pre-create attribute values.\n            active (Optional[bool]): Whether the created instance defaults\n                to be active or not.\n            product_type (str): Specific product type to use.\n\n        Returns:\n            Any: Output of triggered creator's 'create' method.\n\n        Raises:\n            CreatorError: If creator was not found or folder is empty.\n\n        \"\"\"\n        creator = self._get_creator_in_create(creator_identifier)\n\n        project_name = self.project_name\n        if folder_entity is None:\n            folder_path = self.get_current_folder_path()\n            folder_entity = ayon_api.get_folder_by_path(\n                project_name, folder_path\n            )\n            if folder_entity is None:\n                raise CreatorError(f\"Folder '{folder_path}' was not found\")\n\n        if task_entity is None:\n            current_task_name = self.get_current_task_name()\n            if current_task_name:\n                task_entity = ayon_api.get_task_by_name(\n                    project_name, folder_entity[\"id\"], current_task_name\n                )\n\n        # Handle not passed product type\n        if product_type is None:\n            for product_type_item in creator.get_product_type_items():\n                product_type = product_type_item.product_type\n                break\n            else:\n                product_type = creator.product_base_type\n\n        if pre_create_data is None:\n            pre_create_data = {}\n\n        precreate_attr_defs = []\n        # Hidden creators do not have or need the pre-create attributes.\n        if isinstance(creator, Creator):\n            precreate_attr_defs = creator.get_pre_create_attr_defs()\n\n        # Create default values of precreate data\n        _pre_create_data = get_default_values(precreate_attr_defs)\n        # Update passed precreate data to default values\n        # TODO validate types\n        _pre_create_data.update(pre_create_data)\n\n        project_entity = self.get_current_project_entity()\n        args = (\n            project_name,\n            folder_entity,\n            task_entity,\n            variant,\n            self.host_name,\n        )\n        kwargs = {\n            # Backwards compatibility for 'project_entity' argument (24/07/08)\n            \"project_entity\": project_entity,\n            # Backwards compatibility for 'product_type' argument (26/01/19)\n            \"product_type\": product_type,\n        }\n        for kwarg in (\"product_type\", \"project_entity\"):\n            if not is_func_signature_supported(\n                creator.get_product_name, *args, **kwargs\n            ):\n                kwargs.pop(kwarg)\n\n        product_name = creator.get_product_name(*args, **kwargs)\n\n        instance_data = {\n            \"folderPath\": folder_entity[\"path\"],\n            \"task\": task_entity[\"name\"] if task_entity else None,\n            \"productType\": product_type,\n            # Add product base type if supported. Fallback to\n            #   older attribute 'product_type'\n            \"productBaseType\": (\n                creator.product_base_type or creator.product_type),\n            \"variant\": variant\n        }\n\n        if active is not None:\n            if not isinstance(active, bool):\n                self.log.warning(\n                    \"CreateContext.create 'active' argument is not a bool. \"\n                    f\"Converting {active} {type(active)} to bool.\")\n                active = bool(active)\n            instance_data[\"active\"] = active\n\n        with self.bulk_add_instances():\n            return creator.create(\n                product_name,\n                instance_data,\n                _pre_create_data\n            )\n\n    def create_with_unified_error(\n        self, identifier: str, *args, **kwargs\n    ) -&gt; Any:\n        \"\"\"Trigger create but raise only one error if anything fails.\n\n        Added to raise unified exception. Capture any possible issues and\n        reraise it with unified information.\n\n        Args:\n            identifier (str): Identifier of creator.\n            *args: Arguments for create method.\n            **kwargs: Keyword argument for create method.\n\n        Raises:\n            CreatorsCreateFailed: When creation fails due to any possible\n                reason. If anything goes wrong this is only possible exception\n                the method should raise.\n\n        \"\"\"\n        result, fail_info = self._create_with_unified_error(\n            identifier, None, *args, **kwargs\n        )\n        if fail_info is not None:\n            raise CreatorsCreateFailed([fail_info])\n        return result\n\n    def creator_removed_instance(self, instance: \"CreatedInstance\") -&gt; None:\n        \"\"\"When creator removes instance context should be acknowledged.\n\n        If creator removes instance context should know about it to avoid\n        possible issues in the session.\n\n        Args:\n            instance (CreatedInstance): Object of instance which was removed\n                from scene metadata.\n        \"\"\"\n\n        self._remove_instances([instance])\n\n    def add_convertor_item(\n        self, convertor_identifier: str, label: str\n    ) -&gt; None:\n        self.convertor_items_by_id[convertor_identifier] = ConvertorItem(\n            convertor_identifier, label\n        )\n\n    def remove_convertor_item(self, convertor_identifier: str) -&gt; None:\n        self.convertor_items_by_id.pop(convertor_identifier, None)\n\n    @contextmanager\n    def bulk_add_instances(self, sender: Optional[str] = None):\n        with self._bulk_context(\"add\", sender) as bulk_info:\n            yield bulk_info\n\n    @contextmanager\n    def bulk_instances_collection(self, sender: Optional[str] = None):\n        \"\"\"DEPRECATED use 'bulk_add_instances' instead.\"\"\"\n        # TODO add warning\n        with self.bulk_add_instances(sender) as bulk_info:\n            yield bulk_info\n\n    @contextmanager\n    def bulk_remove_instances(self, sender: Optional[str] = None):\n        with self._bulk_context(\"remove\", sender) as bulk_info:\n            yield bulk_info\n\n    @contextmanager\n    def bulk_value_changes(self, sender: Optional[str] = None):\n        with self._bulk_context(\"change\", sender) as bulk_info:\n            yield bulk_info\n\n    @contextmanager\n    def bulk_pre_create_attr_defs_change(self, sender: Optional[str] = None):\n        with self._bulk_context(\n            \"pre_create_attrs_change\", sender\n        ) as bulk_info:\n            yield bulk_info\n\n    @contextmanager\n    def bulk_create_attr_defs_change(self, sender: Optional[str] = None):\n        with self._bulk_context(\n            \"create_attrs_change\", sender\n        ) as bulk_info:\n            yield bulk_info\n\n    @contextmanager\n    def bulk_instance_requirement_change(self, sender: Optional[str] = None):\n        with self._bulk_context(\n            \"requirement_change\", sender\n        ) as bulk_info:\n            yield bulk_info\n\n    @contextmanager\n    def bulk_instance_parent_change(self, sender: Optional[str] = None):\n        with self._bulk_context(\n            \"parent_change\", sender\n        ) as bulk_info:\n            yield bulk_info\n\n    @contextmanager\n    def bulk_publish_attr_defs_change(self, sender: Optional[str] = None):\n        with self._bulk_context(\"publish_attrs_change\", sender) as bulk_info:\n            yield bulk_info\n\n    # --- instance change callbacks ---\n    def create_plugin_pre_create_attr_defs_changed(\n        self, identifier: str\n    ) -&gt; None:\n        \"\"\"Create plugin pre-create attributes changed.\n\n        Triggered by 'Creator'.\n\n        Args:\n            identifier (str): Create plugin identifier.\n\n        \"\"\"\n        with self.bulk_pre_create_attr_defs_change() as bulk_item:\n            bulk_item.append(identifier)\n\n    def instance_create_attr_defs_changed(self, instance_id: str) -&gt; None:\n        \"\"\"Instance attribute definitions changed.\n\n        Triggered by instance 'CreatorAttributeValues' on instance.\n\n        Args:\n            instance_id (str): Instance id.\n\n        \"\"\"\n        if self._is_instance_events_ready(instance_id):\n            with self.bulk_create_attr_defs_change() as bulk_item:\n                bulk_item.append(instance_id)\n\n    def instance_publish_attr_defs_changed(\n        self, instance_id: Optional[str], plugin_name: str\n    ) -&gt; None:\n        \"\"\"Instance attribute definitions changed.\n\n        Triggered by instance 'PublishAttributeValues' on instance.\n\n        Args:\n            instance_id (Optional[str]): Instance id or None for context.\n            plugin_name (str): Plugin name which attribute definitions were\n                changed.\n\n        \"\"\"\n        if self._is_instance_events_ready(instance_id):\n            with self.bulk_publish_attr_defs_change() as bulk_item:\n                bulk_item.append((instance_id, plugin_name))\n\n    def instance_values_changed(\n        self, instance_id: Optional[str], new_values: dict[str, Any]\n    ) -&gt; None:\n        \"\"\"Instance value changed.\n\n        Triggered by `CreatedInstance, 'CreatorAttributeValues'\n            or 'PublishAttributeValues' on instance.\n\n        Args:\n            instance_id (Optional[str]): Instance id or None for context.\n            new_values (dict[str, Any]): Changed values.\n\n        \"\"\"\n        if self._is_instance_events_ready(instance_id):\n            with self.bulk_value_changes() as bulk_item:\n                bulk_item.append((instance_id, new_values))\n\n    def instance_requirement_changed(self, instance_id: str) -&gt; None:\n        \"\"\"Instance requirement changed.\n\n        Triggered by `CreatedInstance`.\n\n        Args:\n            instance_id (Optional[str]): Instance id.\n\n        \"\"\"\n        if self._is_instance_events_ready(instance_id):\n            with self.bulk_instance_requirement_change() as bulk_item:\n                bulk_item.append(instance_id)\n\n    def instance_parent_changed(self, instance_id: str) -&gt; None:\n        \"\"\"Instance parent changed.\n\n        Triggered by `CreatedInstance`.\n\n        Args:\n            instance_id (Optional[str]): Instance id.\n\n        \"\"\"\n        if self._is_instance_events_ready(instance_id):\n            with self.bulk_instance_parent_change() as bulk_item:\n                bulk_item.append(instance_id)\n\n    # --- context change callbacks ---\n    def publish_attribute_value_changed(\n        self, plugin_name: str, value: dict[str, Any]\n    ) -&gt; None:\n        \"\"\"Context publish attribute values changed.\n\n        Triggered by instance 'PublishAttributeValues' on context.\n\n        Args:\n            plugin_name (str): Plugin name which changed value.\n            value (dict[str, Any]): Changed values.\n\n        \"\"\"\n        self.instance_values_changed(\n            None,\n            {\n                \"publish_attributes\": {\n                    plugin_name: value,\n                },\n            },\n        )\n\n    def reset_instances(self) -&gt; None:\n        \"\"\"Reload instances\"\"\"\n        self._instances_by_id = collections.OrderedDict()\n\n        # Collect instances\n        error_message = \"Collection of instances for creator {} failed. {}\"\n        failed_info = []\n        for creator in self.sorted_creators:\n            label = creator.label\n            identifier = creator.identifier\n            failed = False\n            add_traceback = False\n            exc_info = None\n            try:\n                creator.collect_instances()\n\n            except CreatorError:\n                failed = True\n                exc_info = sys.exc_info()\n                self.log.warning(error_message.format(identifier, exc_info[1]))\n\n            except:  # noqa: E722\n                failed = True\n                add_traceback = True\n                exc_info = sys.exc_info()\n                self.log.warning(\n                    error_message.format(identifier, \"\"),\n                    exc_info=True\n                )\n\n            if failed:\n                failed_info.append(\n                    prepare_failed_creator_operation_info(\n                        identifier, label, exc_info, add_traceback\n                    )\n                )\n\n        if failed_info:\n            raise CreatorsCollectionFailed(failed_info)\n\n    def find_convertor_items(self) -&gt; None:\n        \"\"\"Go through convertor plugins to look for items to convert.\n\n        Raises:\n            ConvertorsFindFailed: When one or more convertors fails during\n                finding.\n        \"\"\"\n\n        self.convertor_items_by_id = {}\n\n        failed_info = []\n        for convertor in self.convertors_plugins.values():\n            try:\n                convertor.find_instances()\n\n            except:  # noqa: E722\n                failed_info.append(\n                    prepare_failed_convertor_operation_info(\n                        convertor.identifier, sys.exc_info()\n                    )\n                )\n                self.log.warning(\n                    \"Failed to find instances of convertor \\\"{}\\\"\".format(\n                        convertor.identifier\n                    ),\n                    exc_info=True\n                )\n\n        if failed_info:\n            raise ConvertorsFindFailed(failed_info)\n\n    def execute_autocreators(self) -&gt; None:\n        \"\"\"Execute discovered AutoCreator plugins.\n\n        Reset instances if any autocreator executed properly.\n        \"\"\"\n\n        failed_info = []\n        for creator in self.sorted_autocreators:\n            identifier = creator.identifier\n            _, fail_info = self._create_with_unified_error(identifier, creator)\n            if fail_info is not None:\n                failed_info.append(fail_info)\n\n        if failed_info:\n            raise CreatorsCreateFailed(failed_info)\n\n    def get_folder_entities(\n        self, folder_paths: Iterable[str]\n    ) -&gt; dict[str, Optional[dict[str, Any]]]:\n        \"\"\"Get folder entities by paths.\n\n        Args:\n            folder_paths (Iterable[str]): Folder paths.\n\n        Returns:\n            dict[str, Optional[dict[str, Any]]]: Folder entities by path.\n\n        \"\"\"\n        output = {\n            folder_path: None\n            for folder_path in folder_paths\n        }\n        remainder_paths = set()\n        for folder_path in output:\n            # Skip invalid folder paths (folder name or empty path)\n            if not folder_path or \"/\" not in folder_path:\n                continue\n\n            if folder_path not in self._folder_entities_by_path:\n                remainder_paths.add(folder_path)\n                continue\n\n            output[folder_path] = self._folder_entities_by_path[folder_path]\n\n        if not remainder_paths:\n            return output\n\n        found_paths = set()\n        for folder_entity in ayon_api.get_folders(\n            self.project_name,\n            folder_paths=remainder_paths,\n        ):\n            folder_path = folder_entity[\"path\"]\n            found_paths.add(folder_path)\n            output[folder_path] = folder_entity\n            self._folder_entities_by_path[folder_path] = folder_entity\n\n        # Cache empty folder entities\n        for path in remainder_paths - found_paths:\n            self._folder_entities_by_path[path] = None\n\n        return output\n\n    def get_task_entities(\n        self,\n        task_names_by_folder_paths: dict[str, set[str]]\n    ) -&gt; dict[str, dict[str, Optional[dict[str, Any]]]]:\n        \"\"\"Get task entities by folder path and task name.\n\n        Entities are cached until reset.\n\n        Args:\n            task_names_by_folder_paths (dict[str, set[str]]): Task names by\n                folder path.\n\n        Returns:\n            dict[str, dict[str, dict[str, Any]]]: Task entities by folder path\n                and task name.\n\n        \"\"\"\n        output = {}\n        for folder_path, task_names in task_names_by_folder_paths.items():\n            if folder_path is None:\n                continue\n            output[folder_path] = {\n                task_name: None\n                for task_name in task_names\n                if task_name is not None\n            }\n\n        missing_folder_paths = set()\n        for folder_path, output_task_entities_by_name in output.items():\n            if not output_task_entities_by_name:\n                continue\n\n            if folder_path not in self._task_ids_by_folder_path:\n                missing_folder_paths.add(folder_path)\n                continue\n\n            all_tasks_filled = True\n            task_ids = self._task_ids_by_folder_path[folder_path]\n            task_entities_by_name = {}\n            for task_id in task_ids:\n                task_entity = self._task_entities_by_id.get(task_id)\n                if task_entity is None:\n                    all_tasks_filled = False\n                    continue\n                task_entities_by_name[task_entity[\"name\"]] = task_entity\n\n            any_missing = False\n            for task_name in set(output_task_entities_by_name):\n                task_entity = task_entities_by_name.get(task_name)\n                if task_entity is None:\n                    any_missing = True\n                    continue\n\n                output_task_entities_by_name[task_name] = task_entity\n\n            if any_missing and not all_tasks_filled:\n                missing_folder_paths.add(folder_path)\n\n        if not missing_folder_paths:\n            return output\n\n        folder_entities_by_path = self.get_folder_entities(\n            missing_folder_paths\n        )\n        folder_path_by_id = {}\n        for folder_path, folder_entity in folder_entities_by_path.items():\n            if folder_entity is not None:\n                folder_path_by_id[folder_entity[\"id\"]] = folder_path\n\n        if not folder_path_by_id:\n            return output\n\n        task_entities_by_parent_id = collections.defaultdict(list)\n        for task_entity in ayon_api.get_tasks(\n            self.project_name,\n            folder_ids=folder_path_by_id.keys()\n        ):\n            folder_id = task_entity[\"folderId\"]\n            task_entities_by_parent_id[folder_id].append(task_entity)\n\n        for folder_id, task_entities in task_entities_by_parent_id.items():\n            folder_path = folder_path_by_id[folder_id]\n            task_ids = set()\n            task_names = set()\n            for task_entity in task_entities:\n                task_id = task_entity[\"id\"]\n                task_name = task_entity[\"name\"]\n                task_ids.add(task_id)\n                task_names.add(task_name)\n                self._task_entities_by_id[task_id] = task_entity\n\n                output[folder_path][task_name] = task_entity\n            self._task_ids_by_folder_path[folder_path] = task_ids\n            self._task_names_by_folder_path[folder_path] = task_names\n\n        return output\n\n    def get_folder_entity(\n        self,\n        folder_path: Optional[str],\n    ) -&gt; Optional[dict[str, Any]]:\n        \"\"\"Get folder entity by path.\n\n        Entities are cached until reset.\n\n        Args:\n            folder_path (Optional[str]): Folder path.\n\n        Returns:\n            Optional[dict[str, Any]]: Folder entity.\n\n        \"\"\"\n        if not folder_path:\n            return None\n        return self.get_folder_entities([folder_path]).get(folder_path)\n\n    def get_task_entity(\n        self,\n        folder_path: Optional[str],\n        task_name: Optional[str],\n    ) -&gt; Optional[dict[str, Any]]:\n        \"\"\"Get task entity by name and folder path.\n\n        Entities are cached until reset.\n\n        Args:\n            folder_path (Optional[str]): Folder path.\n            task_name (Optional[str]): Task name.\n\n        Returns:\n            Optional[dict[str, Any]]: Task entity.\n\n        \"\"\"\n        if not folder_path or not task_name:\n            return None\n\n        output = self.get_task_entities({folder_path: {task_name}})\n        return output.get(folder_path, {}).get(task_name)\n\n    def get_instances_folder_entities(\n        self, instances: Optional[Iterable[\"CreatedInstance\"]] = None\n    ) -&gt; dict[str, Optional[dict[str, Any]]]:\n        if instances is None:\n            instances = self._instances_by_id.values()\n        instances = list(instances)\n        output = {\n            instance.id: None\n            for instance in instances\n        }\n        if not instances:\n            return output\n\n        folder_paths = {\n            instance.get(\"folderPath\")\n            for instance in instances\n        }\n        folder_paths.discard(None)\n        folder_entities_by_path = self.get_folder_entities(folder_paths)\n        for instance in instances:\n            folder_path = instance.get(\"folderPath\")\n            output[instance.id] = folder_entities_by_path.get(folder_path)\n        return output\n\n    def get_instances_task_entities(\n        self, instances: Optional[Iterable[\"CreatedInstance\"]] = None\n    ) -&gt; dict[str, Optional[dict[str, Any]]]:\n        \"\"\"Get task entities for instances.\n\n        Args:\n            instances (Optional[Iterable[CreatedInstance]]): Instances to\n                get task entities. If not provided all instances are used.\n\n        Returns:\n            dict[str, Optional[dict[str, Any]]]: Task entity by instance id.\n\n        \"\"\"\n        if instances is None:\n            instances = self._instances_by_id.values()\n        instances = list(instances)\n\n        output = {\n            instance.id: None\n            for instance in instances\n        }\n        if not instances:\n            return output\n\n        filtered_instances = []\n        task_names_by_folder_path = collections.defaultdict(set)\n        for instance in instances:\n            folder_path = instance.get(\"folderPath\")\n            task_name = instance.get(\"task\")\n            if not folder_path or not task_name:\n                continue\n            filtered_instances.append(instance)\n            task_names_by_folder_path[folder_path].add(task_name)\n\n        task_entities_by_folder_path = self.get_task_entities(\n            task_names_by_folder_path\n        )\n        for instance in filtered_instances:\n            folder_path = instance[\"folderPath\"]\n            task_name = instance[\"task\"]\n            output[instance.id] = (\n                task_entities_by_folder_path[folder_path][task_name]\n            )\n\n        return output\n\n    def get_instances_context_info(\n        self, instances: Optional[Iterable[\"CreatedInstance\"]] = None\n    ) -&gt; dict[str, InstanceContextInfo]:\n        \"\"\"Validate 'folder' and 'task' instance context.\n\n        Args:\n            instances (Optional[Iterable[CreatedInstance]]): Instances to\n                validate. If not provided all instances are validated.\n\n        Returns:\n            dict[str, InstanceContextInfo]: Validation results by instance id.\n\n        \"\"\"\n        # Use all instances from context if 'instances' are not passed\n        if instances is None:\n            instances = self._instances_by_id.values()\n        instances = tuple(instances)\n        info_by_instance_id = {\n            instance.id: InstanceContextInfo(\n                instance.get(\"folderPath\"),\n                instance.get(\"task\"),\n                False,\n                False,\n            )\n            for instance in instances\n        }\n\n        # Skip if instances are empty\n        if not info_by_instance_id:\n            return info_by_instance_id\n\n        project_name = self.project_name\n\n        to_validate = []\n        task_names_by_folder_path = collections.defaultdict(set)\n        for instance in instances:\n            context_info = info_by_instance_id[instance.id]\n            if instance.has_promised_context:\n                context_info.folder_is_valid = True\n                context_info.task_is_valid = True\n                # NOTE missing task type\n                continue\n            # TODO allow context promise\n            folder_path = context_info.folder_path\n            if not folder_path:\n                continue\n\n            if folder_path in self._folder_entities_by_path:\n                folder_entity = self._folder_entities_by_path[folder_path]\n                if folder_entity is None:\n                    continue\n                context_info.folder_is_valid = True\n\n            task_name = context_info.task_name\n            if task_name is not None:\n                tasks_cache = self._task_names_by_folder_path.get(folder_path)\n                if tasks_cache is not None:\n                    context_info.task_is_valid = task_name in tasks_cache\n                    continue\n\n            to_validate.append(instance)\n            task_names_by_folder_path[folder_path].add(task_name)\n\n        if not to_validate:\n            return info_by_instance_id\n\n        # Backwards compatibility for cases where folder name is set instead\n        #   of folder path\n        folder_paths = set()\n        task_names_by_folder_name = {}\n        task_names_by_folder_path_clean = {}\n        for folder_path, task_names in task_names_by_folder_path.items():\n            if folder_path is None:\n                continue\n\n            clean_task_names = {\n                task_name\n                for task_name in task_names\n                if task_name\n            }\n\n            if \"/\" not in folder_path:\n                task_names_by_folder_name[folder_path] = clean_task_names\n                continue\n\n            folder_paths.add(folder_path)\n            if not clean_task_names:\n                continue\n\n            task_names_by_folder_path_clean[folder_path] = clean_task_names\n\n        folder_paths_by_name = collections.defaultdict(list)\n        if task_names_by_folder_name:\n            for folder_entity in ayon_api.get_folders(\n                project_name,\n                folder_names=task_names_by_folder_name.keys(),\n                fields={\"name\", \"path\"}\n            ):\n                folder_name = folder_entity[\"name\"]\n                folder_path = folder_entity[\"path\"]\n                folder_paths_by_name[folder_name].append(folder_path)\n\n        folder_path_by_name = {}\n        for folder_name, paths in folder_paths_by_name.items():\n            if len(paths) != 1:\n                continue\n            path = paths[0]\n            folder_path_by_name[folder_name] = path\n            folder_paths.add(path)\n            clean_task_names = task_names_by_folder_name[folder_name]\n            if not clean_task_names:\n                continue\n            folder_task_names = task_names_by_folder_path_clean.setdefault(\n                path, set()\n            )\n            folder_task_names |= clean_task_names\n\n        folder_entities_by_path = self.get_folder_entities(folder_paths)\n        task_entities_by_folder_path = self.get_task_entities(\n            task_names_by_folder_path_clean\n        )\n\n        for instance in to_validate:\n            folder_path = instance[\"folderPath\"]\n            task_name = instance.get(\"task\")\n            if folder_path and \"/\" not in folder_path:\n                new_folder_path = folder_path_by_name.get(folder_path)\n                if new_folder_path:\n                    folder_path = new_folder_path\n                    instance[\"folderPath\"] = new_folder_path\n\n            folder_entity = folder_entities_by_path.get(folder_path)\n            if not folder_entity:\n                continue\n            context_info = info_by_instance_id[instance.id]\n            context_info.folder_is_valid = True\n\n            if (\n                not task_name\n                or task_name in task_entities_by_folder_path[folder_path]\n            ):\n                context_info.task_is_valid = True\n        return info_by_instance_id\n\n    def save_changes(self) -&gt; None:\n        \"\"\"Save changes. Update all changed values.\"\"\"\n        if not self.host_is_valid:\n            missing_methods = self.get_host_misssing_methods(self.host)\n            raise HostMissRequiredMethod(self.host, missing_methods)\n\n        self._save_context_changes()\n        self._save_instance_changes()\n\n    def _save_context_changes(self) -&gt; None:\n        \"\"\"Save global context values.\"\"\"\n        changes = self.context_data_changes()\n        if changes:\n            data = self.context_data_to_store()\n            self.host.update_context_data(data, changes)\n\n    def _save_instance_changes(self) -&gt; None:\n        \"\"\"Save instance specific values.\"\"\"\n        instances_by_identifier = collections.defaultdict(list)\n        for instance in self._instances_by_id.values():\n            instance_changes = instance.changes()\n            if not instance_changes:\n                continue\n\n            identifier = instance.creator_identifier\n            instances_by_identifier[identifier].append(\n                UpdateData(instance, instance_changes)\n            )\n\n        if not instances_by_identifier:\n            return\n\n        error_message = \"Instances update of creator \\\"{}\\\" failed. {}\"\n        failed_info = []\n\n        for creator in self.get_sorted_creators(\n            instances_by_identifier.keys()\n        ):\n            identifier = creator.identifier\n            update_list = instances_by_identifier[identifier]\n            if not update_list:\n                continue\n\n            label = creator.label\n            failed = False\n            add_traceback = False\n            exc_info = None\n            try:\n                creator.update_instances(update_list)\n\n            except CreatorError:\n                failed = True\n                exc_info = sys.exc_info()\n                self.log.warning(error_message.format(identifier, exc_info[1]))\n\n            except:  # noqa: E722\n                failed = True\n                add_traceback = True\n                exc_info = sys.exc_info()\n                self.log.warning(\n                    error_message.format(identifier, \"\"), exc_info=True)\n\n            if failed:\n                failed_info.append(\n                    prepare_failed_creator_operation_info(\n                        identifier, label, exc_info, add_traceback\n                    )\n                )\n            else:\n                for update_data in update_list:\n                    instance = update_data.instance\n                    instance.mark_as_stored()\n\n        if failed_info:\n            raise CreatorsSaveFailed(failed_info)\n\n    def remove_instances(\n        self,\n        instances: list[\"CreatedInstance\"],\n        sender: Optional[str] = None,\n    ) -&gt; None:\n        \"\"\"Remove instances from context.\n\n        All instances that don't have creator identifier leading to existing\n            creator are just removed from context.\n\n        Args:\n            instances (list[CreatedInstance]): Instances that should be\n                removed. Remove logic is done using creator, which may require\n                to do other cleanup than just remove instance from context.\n            sender (Optional[str]): Sender of the event.\n\n        \"\"\"\n        instance_ids_by_parent_id = collections.defaultdict(set)\n        for instance in self.instances:\n            instance_ids_by_parent_id[instance.parent_instance_id].add(\n                instance.id\n            )\n\n        instances_to_remove = list(instances)\n        ids_to_remove = {\n            instance.id\n            for instance in instances_to_remove\n        }\n        _queue = collections.deque()\n        _queue.extend(instances_to_remove)\n        # Add children with parent lifetime flag\n        while _queue:\n            instance = _queue.popleft()\n            ids_to_remove.add(instance.id)\n            children_ids = instance_ids_by_parent_id[instance.id]\n            for children_id in children_ids:\n                if children_id in ids_to_remove:\n                    continue\n                instance = self._instances_by_id[children_id]\n                if instance.parent_flags &amp; ParentFlags.parent_lifetime:\n                    instances_to_remove.append(instance)\n                    ids_to_remove.add(instance.id)\n                    _queue.append(instance)\n\n        instances_by_identifier = collections.defaultdict(list)\n        for instance in instances_to_remove:\n            identifier = instance.creator_identifier\n            instances_by_identifier[identifier].append(instance)\n\n        # Just remove instances from context if creator is not available\n        missing_creators = set(instances_by_identifier) - set(self.creators)\n        miss_creator_instances = []\n        for identifier in missing_creators:\n            miss_creator_instances.extend(instances_by_identifier[identifier])\n\n        with self.bulk_remove_instances(sender):\n            self._remove_instances(miss_creator_instances, sender)\n\n            error_message = \"Instances removement of creator \\\"{}\\\" failed. {}\"\n            failed_info = []\n            # Remove instances by creator plugin order\n            for creator in self.get_sorted_creators(\n                instances_by_identifier.keys()\n            ):\n                identifier = creator.identifier\n                # Filter instances by current state of 'CreateContext'\n                # - in case instances were already removed as subroutine of\n                #   previous create plugin.\n                creator_instances = [\n                    instance\n                    for instance in instances_by_identifier[identifier]\n                    if instance.id in self._instances_by_id\n                ]\n                if not creator_instances:\n                    continue\n\n                label = creator.label\n                failed = False\n                add_traceback = False\n                exc_info = None\n                try:\n                    creator.remove_instances(creator_instances)\n\n                except CreatorError:\n                    failed = True\n                    exc_info = sys.exc_info()\n                    self.log.warning(\n                        error_message.format(identifier, exc_info[1])\n                    )\n\n                except (KeyboardInterrupt, SystemExit):\n                    raise\n\n                except:  # noqa: E722\n                    failed = True\n                    add_traceback = True\n                    exc_info = sys.exc_info()\n                    self.log.warning(\n                        error_message.format(identifier, \"\"),\n                        exc_info=True\n                    )\n\n                if failed:\n                    failed_info.append(\n                        prepare_failed_creator_operation_info(\n                            identifier, label, exc_info, add_traceback\n                        )\n                    )\n\n        if failed_info:\n            raise CreatorsRemoveFailed(failed_info)\n\n    @property\n    def collection_shared_data(self) -&gt; dict[str, Any]:\n        \"\"\"Access to shared data that can be used during creator's collection.\n\n        Returns:\n            dict[str, Any]: Shared data.\n\n        Raises:\n            UnavailableSharedData: When called out of collection phase.\n        \"\"\"\n\n        if self._collection_shared_data is None:\n            raise UnavailableSharedData(\n                \"Accessed Collection shared data out of collection phase\"\n            )\n        return self._collection_shared_data\n\n    def run_convertor(self, convertor_identifier: str) -&gt; None:\n        \"\"\"Run convertor plugin by identifier.\n\n        Conversion is skipped if convertor is not available.\n\n        Args:\n            convertor_identifier (str): Identifier of convertor.\n        \"\"\"\n\n        convertor = self.convertors_plugins.get(convertor_identifier)\n        if convertor is not None:\n            convertor.convert()\n\n    def run_convertors(self, convertor_identifiers: Iterable[str]) -&gt; None:\n        \"\"\"Run convertor plugins by identifiers.\n\n        Conversion is skipped if convertor is not available. It is recommended\n        to trigger reset after conversion to reload instances.\n\n        Args:\n            convertor_identifiers (Iterable[str]): Identifiers of convertors\n                to run.\n\n        Raises:\n            ConvertorsConversionFailed: When one or more convertors fails.\n        \"\"\"\n\n        failed_info = []\n        for convertor_identifier in convertor_identifiers:\n            try:\n                self.run_convertor(convertor_identifier)\n\n            except:  # noqa: E722\n                failed_info.append(\n                    prepare_failed_convertor_operation_info(\n                        convertor_identifier, sys.exc_info()\n                    )\n                )\n                self.log.warning(\n                    \"Failed to convert instances of convertor \\\"{}\\\"\".format(\n                        convertor_identifier\n                    ),\n                    exc_info=True\n                )\n\n        if failed_info:\n            raise ConvertorsConversionFailed(failed_info)\n\n    def _register_event_callback(\n        self, topic: str, callback: Callable\n    ) -&gt; \"EventCallback\":\n        return self._event_hub.add_callback(topic, callback)\n\n    def _emit_event(\n        self,\n        topic: str,\n        data: Optional[dict[str, Any]] = None,\n        sender: Optional[str] = None,\n    ) -&gt; \"Event\":\n        if data is None:\n            data = {}\n        data.setdefault(\"create_context\", self)\n        return self._event_hub.emit(topic, data, sender)\n\n    def _remove_instances(\n        self,\n        instances: Iterable[CreatedInstance],\n        sender: Optional[str] = None,\n    ) -&gt; None:\n        with self.bulk_remove_instances(sender) as bulk_info:\n            for instance in instances:\n                obj = self._instances_by_id.pop(instance.id, None)\n                if obj is not None:\n                    bulk_info.append(obj)\n\n    def _create_with_unified_error(\n        self,\n        identifier: str,\n        creator: Optional[\"BaseCreator\"],\n        *args,\n        **kwargs\n    ) -&gt; tuple[Optional[Any], Optional[dict[str, Any]]]:\n        error_message = \"Failed to run Creator with identifier \\\"{}\\\". {}\"\n\n        label = None\n        add_traceback = False\n        result = None\n        fail_info = None\n        exc_info = None\n        success = False\n\n        try:\n            # Try to get creator and his label\n            if creator is None:\n                creator = self._get_creator_in_create(identifier)\n            label = getattr(creator, \"label\", label)\n\n            # Run create\n            with self.bulk_add_instances():\n                result = creator.create(*args, **kwargs)\n            success = True\n\n        except CreatorError:\n            exc_info = sys.exc_info()\n            self.log.warning(error_message.format(identifier, exc_info[1]))\n\n        except:  # noqa: E722\n            add_traceback = True\n            exc_info = sys.exc_info()\n            self.log.warning(\n                error_message.format(identifier, \"\"),\n                exc_info=True\n            )\n\n        if not success:\n            fail_info = prepare_failed_creator_operation_info(\n                identifier, label, exc_info, add_traceback\n            )\n        return result, fail_info\n\n    def _is_instance_events_ready(self, instance_id: Optional[str]) -&gt; bool:\n        # Context is ready\n        if instance_id is None:\n            return True\n        # Instance is not in yet in context\n        if instance_id not in self._instances_by_id:\n            return False\n\n        # Instance in 'collect' bulk will be ignored\n        for instance in self._bulk_info[\"add\"].get_data():\n            if instance.id == instance_id:\n                return False\n        return True\n\n    @contextmanager\n    def _bulk_context(self, key: str, sender: Optional[str]):\n        bulk_info = self._bulk_info[key]\n        bulk_info.set_sender(sender)\n\n        bulk_info.increase()\n        if key not in self._bulk_order:\n            self._bulk_order.append(key)\n        try:\n            yield bulk_info\n        finally:\n            bulk_info.decrease()\n            if bulk_info:\n                self._bulk_finished(key)\n\n    def _bulk_finished(self, key: str) -&gt; None:\n        if self._bulk_order[0] != key:\n            return\n\n        self._bulk_order.pop(0)\n        self._bulk_finish(key)\n\n        while self._bulk_order:\n            key = self._bulk_order[0]\n            if not self._bulk_info[key]:\n                break\n            self._bulk_order.pop(0)\n            self._bulk_finish(key)\n\n    def _bulk_finish(self, key: str) -&gt; None:\n        bulk_info = self._bulk_info[key]\n        sender = bulk_info.get_sender()\n        data = bulk_info.pop_data()\n        if key == \"add\":\n            self._bulk_add_instances_finished(data, sender)\n        elif key == \"remove\":\n            self._bulk_remove_instances_finished(data, sender)\n        elif key == \"change\":\n            self._bulk_values_change_finished(data, sender)\n        elif key == \"pre_create_attrs_change\":\n            self._bulk_pre_create_attrs_change_finished(data, sender)\n        elif key == \"create_attrs_change\":\n            self._bulk_create_attrs_change_finished(data, sender)\n        elif key == \"publish_attrs_change\":\n            self._bulk_publish_attrs_change_finished(data, sender)\n        elif key == \"requirement_change\":\n            self._bulk_instance_requirement_change_finished(data, sender)\n        elif key == \"parent_change\":\n            self._bulk_instance_parent_change_finished(data, sender)\n\n    def _bulk_add_instances_finished(\n        self,\n        instances_to_validate: list[\"CreatedInstance\"],\n        sender: Optional[str]\n    ) -&gt; None:\n        if not instances_to_validate:\n            return\n\n        # Set publish attributes before bulk callbacks are triggered\n        for instance in instances_to_validate:\n            publish_attributes = instance.publish_attributes\n            # Prepare publish plugin attributes and set it on instance\n            for plugin in self.plugins_with_defs:\n                try:\n                    if is_func_signature_supported(\n                            plugin.convert_attribute_values, self, instance\n                    ):\n                        plugin.convert_attribute_values(self, instance)\n\n                    elif plugin.__instanceEnabled__:\n                        output = plugin.convert_attribute_values(\n                            publish_attributes\n                        )\n                        if output:\n                            publish_attributes.update(output)\n\n                except Exception:\n                    self.log.error(\n                        \"Failed to convert attribute values of\"\n                        f\" plugin '{plugin.__name__}'\",\n                        exc_info=True\n                    )\n\n            for plugin in self.plugins_with_defs:\n                attr_defs = None\n                try:\n                    attr_defs = plugin.get_attr_defs_for_instance(\n                        self, instance\n                    )\n                except Exception:\n                    self.log.error(\n                        \"Failed to get attribute definitions\"\n                        f\" from plugin '{plugin.__name__}'.\",\n                        exc_info=True\n                    )\n\n                if not attr_defs:\n                    continue\n                instance.set_publish_plugin_attr_defs(\n                    plugin.__name__, attr_defs\n                )\n\n        # Cache folder and task entities for all instances at once\n        self.get_instances_context_info(instances_to_validate)\n\n        self._emit_event(\n            INSTANCE_ADDED_TOPIC,\n            {\n                \"instances\": instances_to_validate,\n            },\n            sender,\n        )\n\n    def _bulk_remove_instances_finished(\n        self,\n        instances_to_remove: list[\"CreatedInstance\"],\n        sender: Optional[str]\n    ) -&gt; None:\n        if not instances_to_remove:\n            return\n\n        self._emit_event(\n            INSTANCE_REMOVED_TOPIC,\n            {\n                \"instances\": instances_to_remove,\n            },\n            sender,\n        )\n\n    def _bulk_values_change_finished(\n        self,\n        changes: list[tuple[Optional[str], dict[str, Any]]],\n        sender: Optional[str],\n    ) -&gt; None:\n        if not changes:\n            return\n        item_data_by_id = {}\n        for item_id, item_changes in changes:\n            item_values = item_data_by_id.setdefault(item_id, {})\n            if \"creator_attributes\" in item_changes:\n                current_value = item_values.setdefault(\n                    \"creator_attributes\", {}\n                )\n                current_value.update(\n                    item_changes.pop(\"creator_attributes\")\n                )\n\n            if \"publish_attributes\" in item_changes:\n                current_publish = item_values.setdefault(\n                    \"publish_attributes\", {}\n                )\n                for plugin_name, plugin_value in item_changes.pop(\n                    \"publish_attributes\"\n                ).items():\n                    if plugin_value is None:\n                        current_publish[plugin_name] = None\n                        continue\n                    plugin_changes = current_publish.setdefault(\n                        plugin_name, {}\n                    )\n                    if plugin_changes is None:\n                        current_publish[plugin_name] = plugin_value\n                    else:\n                        plugin_changes.update(plugin_value)\n\n            item_values.update(item_changes)\n\n        event_changes = []\n        for item_id, item_changes in item_data_by_id.items():\n            instance = self.get_instance_by_id(item_id)\n            event_changes.append({\n                \"instance\": instance,\n                \"changes\": item_changes,\n            })\n\n        event_data = {\n            \"changes\": event_changes,\n        }\n\n        self._emit_event(\n            VALUE_CHANGED_TOPIC,\n            event_data,\n            sender\n        )\n\n    def _bulk_pre_create_attrs_change_finished(\n        self, identifiers: list[str], sender: Optional[str]\n    ) -&gt; None:\n        if not identifiers:\n            return\n        identifiers = list(set(identifiers))\n        self._emit_event(\n            PRE_CREATE_ATTR_DEFS_CHANGED_TOPIC,\n            {\n                \"identifiers\": identifiers,\n            },\n            sender,\n        )\n\n    def _bulk_create_attrs_change_finished(\n        self, instance_ids: list[str], sender: Optional[str]\n    ) -&gt; None:\n        if not instance_ids:\n            return\n\n        instances = [\n            self.get_instance_by_id(instance_id)\n            for instance_id in set(instance_ids)\n        ]\n        self._emit_event(\n            CREATE_ATTR_DEFS_CHANGED_TOPIC,\n            {\n                \"instances\": instances,\n            },\n            sender,\n        )\n\n    def _bulk_publish_attrs_change_finished(\n        self,\n        attr_info: list[tuple[str, Optional[str]]],\n        sender: Optional[str],\n    ) -&gt; None:\n        if not attr_info:\n            return\n\n        instance_changes = {}\n        for instance_id, plugin_name in attr_info:\n            instance_data = instance_changes.setdefault(\n                instance_id,\n                {\n                    \"instance\": None,\n                    \"plugin_names\": set(),\n                }\n            )\n            instance = self.get_instance_by_id(instance_id)\n            instance_data[\"instance\"] = instance\n            instance_data[\"plugin_names\"].add(plugin_name)\n\n        self._emit_event(\n            PUBLISH_ATTR_DEFS_CHANGED_TOPIC,\n            {\"instance_changes\": instance_changes},\n            sender,\n        )\n\n    def _bulk_instance_requirement_change_finished(\n        self,\n        instance_ids: list[str],\n        sender: Optional[str],\n    ) -&gt; None:\n        if not instance_ids:\n            return\n\n        instances = [\n            self.get_instance_by_id(instance_id)\n            for instance_id in set(instance_ids)\n        ]\n\n        self._emit_event(\n            INSTANCE_REQUIREMENT_CHANGED_TOPIC,\n            {\"instances\": instances},\n            sender,\n        )\n\n    def _bulk_instance_parent_change_finished(\n        self,\n        instance_ids: list[str],\n        sender: Optional[str],\n    ):\n        if not instance_ids:\n            return\n\n        instances = [\n            self.get_instance_by_id(instance_id)\n            for instance_id in set(instance_ids)\n        ]\n\n        self._emit_event(\n            INSTANCE_PARENT_CHANGED_TOPIC,\n            {\"instances\": instances},\n            sender,\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.collection_shared_data","title":"<code>collection_shared_data</code>  <code>property</code>","text":"<p>Access to shared data that can be used during creator's collection.</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: Shared data.</p> <p>Raises:</p> Type Description <code>UnavailableSharedData</code> <p>When called out of collection phase.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.context_has_changed","title":"<code>context_has_changed</code>  <code>property</code>","text":"<p>Host context has changed.</p> <p>As context is used project, folder, task name and workfile path if host does support workfiles.</p> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>Context changed.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.host_is_valid","title":"<code>host_is_valid</code>  <code>property</code>","text":"<p>Is host valid for creation.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.log","title":"<code>log</code>  <code>property</code>","text":"<p>Dynamic access to logger.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.publish_attributes","title":"<code>publish_attributes</code>  <code>property</code>","text":"<p>Access to global publish attributes.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.sorted_autocreators","title":"<code>sorted_autocreators</code>  <code>property</code>","text":"<p>Sorted auto-creators by 'order' attribute.</p> <p>Returns:</p> Type Description <code>list['AutoCreator']</code> <p>list[AutoCreator]: Sorted plugins by 'order' value.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.sorted_creators","title":"<code>sorted_creators</code>  <code>property</code>","text":"<p>Sorted creators by 'order' attribute.</p> <p>Returns:</p> Type Description <code>list['BaseCreator']</code> <p>list[BaseCreator]: Sorted creator plugins by 'order' value.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.add_create_attr_defs_change_callback","title":"<code>add_create_attr_defs_change_callback(callback)</code>","text":"<p>Register callback to listen create attribute changes.</p> <p>Create plugin changed attribute definitions of instance.</p> <p>Data structure of event:</p> <pre><code>```python\n{\n    \"instances\": [CreatedInstance, ...],\n    \"create_context\": CreateContext\n}\n```\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>callback</code> <code>Callable</code> <p>Callback function that will be called when create attributes changed.</p> required <p>Returns:</p> Name Type Description <code>EventCallback</code> <code>'EventCallback'</code> <p>Created callback object which can be used to stop listening.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def add_create_attr_defs_change_callback(\n    self, callback: Callable\n) -&gt; \"EventCallback\":\n    \"\"\"Register callback to listen create attribute changes.\n\n    Create plugin changed attribute definitions of instance.\n\n    Data structure of event:\n\n        ```python\n        {\n            \"instances\": [CreatedInstance, ...],\n            \"create_context\": CreateContext\n        }\n        ```\n\n    Args:\n        callback (Callable): Callback function that will be called when\n            create attributes changed.\n\n    Returns:\n        EventCallback: Created callback object which can be used to\n            stop listening.\n\n    \"\"\"\n    return self._event_hub.add_callback(\n        CREATE_ATTR_DEFS_CHANGED_TOPIC, callback\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.add_instance_parent_change_callback","title":"<code>add_instance_parent_change_callback(callback)</code>","text":"<p>Register callback to listen to instance parent changes.</p> <p>Instance changed parent or parent flags.</p> <p>Data structure of event:</p> <pre><code>```python\n{\n    \"instances\": [CreatedInstance, ...],\n    \"create_context\": CreateContext\n}\n```\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>callback</code> <code>Callable</code> <p>Callback function that will be called when instance requirement changed.</p> required <p>Returns:</p> Name Type Description <code>EventCallback</code> <code>'EventCallback'</code> <p>Created callback object which can be used to stop listening.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def add_instance_parent_change_callback(\n    self, callback: Callable\n) -&gt; \"EventCallback\":\n    \"\"\"Register callback to listen to instance parent changes.\n\n    Instance changed parent or parent flags.\n\n    Data structure of event:\n\n        ```python\n        {\n            \"instances\": [CreatedInstance, ...],\n            \"create_context\": CreateContext\n        }\n        ```\n\n    Args:\n        callback (Callable): Callback function that will be called when\n            instance requirement changed.\n\n    Returns:\n        EventCallback: Created callback object which can be used to\n            stop listening.\n\n    \"\"\"\n    return self._event_hub.add_callback(\n        INSTANCE_PARENT_CHANGED_TOPIC, callback\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.add_instance_requirement_change_callback","title":"<code>add_instance_requirement_change_callback(callback)</code>","text":"<p>Register callback to listen to instance requirement changes.</p> <p>Instance changed requirement of active state.</p> <p>Data structure of event:</p> <pre><code>```python\n{\n    \"instances\": [CreatedInstance, ...],\n    \"create_context\": CreateContext\n}\n```\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>callback</code> <code>Callable</code> <p>Callback function that will be called when instance requirement changed.</p> required <p>Returns:</p> Name Type Description <code>EventCallback</code> <code>'EventCallback'</code> <p>Created callback object which can be used to stop listening.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def add_instance_requirement_change_callback(\n    self, callback: Callable\n) -&gt; \"EventCallback\":\n    \"\"\"Register callback to listen to instance requirement changes.\n\n    Instance changed requirement of active state.\n\n    Data structure of event:\n\n        ```python\n        {\n            \"instances\": [CreatedInstance, ...],\n            \"create_context\": CreateContext\n        }\n        ```\n\n    Args:\n        callback (Callable): Callback function that will be called when\n            instance requirement changed.\n\n    Returns:\n        EventCallback: Created callback object which can be used to\n            stop listening.\n\n    \"\"\"\n    return self._event_hub.add_callback(\n        INSTANCE_REQUIREMENT_CHANGED_TOPIC, callback\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.add_instances_added_callback","title":"<code>add_instances_added_callback(callback)</code>","text":"<p>Register callback for added instances.</p> <p>Event is triggered when instances are already available in context     and have set create/publish attribute definitions.</p> <p>Data structure of event:</p> <pre><code>```python\n{\n    \"instances\": [CreatedInstance, ...],\n    \"create_context\": CreateContext\n}\n```\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>callback</code> <code>Callable</code> <p>Callback function that will be called when instances are added to context.</p> required <p>Returns:</p> Name Type Description <code>EventCallback</code> <code>'EventCallback'</code> <p>Created callback object which can be used to stop listening.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def add_instances_added_callback(\n    self, callback: Callable\n) -&gt; \"EventCallback\":\n    \"\"\"Register callback for added instances.\n\n    Event is triggered when instances are already available in context\n        and have set create/publish attribute definitions.\n\n    Data structure of event:\n\n        ```python\n        {\n            \"instances\": [CreatedInstance, ...],\n            \"create_context\": CreateContext\n        }\n        ```\n\n    Args:\n        callback (Callable): Callback function that will be called when\n            instances are added to context.\n\n    Returns:\n        EventCallback: Created callback object which can be used to\n            stop listening.\n\n    \"\"\"\n    return self._event_hub.add_callback(INSTANCE_ADDED_TOPIC, callback)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.add_instances_removed_callback","title":"<code>add_instances_removed_callback(callback)</code>","text":"<p>Register callback for removed instances.</p> <p>Event is triggered when instances are already removed from context.</p> <p>Data structure of event:</p> <pre><code>```python\n{\n    \"instances\": [CreatedInstance, ...],\n    \"create_context\": CreateContext\n}\n```\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>callback</code> <code>Callable</code> <p>Callback function that will be called when instances are removed from context.</p> required <p>Returns:</p> Name Type Description <code>EventCallback</code> <code>'EventCallback'</code> <p>Created callback object which can be used to stop listening.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def add_instances_removed_callback(\n    self, callback: Callable\n) -&gt; \"EventCallback\":\n    \"\"\"Register callback for removed instances.\n\n    Event is triggered when instances are already removed from context.\n\n    Data structure of event:\n\n        ```python\n        {\n            \"instances\": [CreatedInstance, ...],\n            \"create_context\": CreateContext\n        }\n        ```\n\n    Args:\n        callback (Callable): Callback function that will be called when\n            instances are removed from context.\n\n    Returns:\n        EventCallback: Created callback object which can be used to\n            stop listening.\n\n    \"\"\"\n    return self._event_hub.add_callback(INSTANCE_REMOVED_TOPIC, callback)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.add_pre_create_attr_defs_change_callback","title":"<code>add_pre_create_attr_defs_change_callback(callback)</code>","text":"<p>Register callback to listen pre-create attribute changes.</p> <p>Create plugin can trigger refresh of pre-create attributes. Usage of     this event is mainly for publisher UI.</p> <p>Data structure of event:</p> <pre><code>```python\n{\n    \"identifiers\": [\"create_plugin_identifier\"],\n    \"create_context\": CreateContext\n}\n```\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>callback</code> <code>Callable</code> <p>Callback function that will be called when pre-create attributes should be refreshed.</p> required <p>Returns:</p> Name Type Description <code>EventCallback</code> <code>'EventCallback'</code> <p>Created callback object which can be used to stop listening.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def add_pre_create_attr_defs_change_callback(\n    self, callback: Callable\n) -&gt; \"EventCallback\":\n    \"\"\"Register callback to listen pre-create attribute changes.\n\n    Create plugin can trigger refresh of pre-create attributes. Usage of\n        this event is mainly for publisher UI.\n\n    Data structure of event:\n\n        ```python\n        {\n            \"identifiers\": [\"create_plugin_identifier\"],\n            \"create_context\": CreateContext\n        }\n        ```\n\n    Args:\n        callback (Callable): Callback function that will be called when\n            pre-create attributes should be refreshed.\n\n    Returns:\n        EventCallback: Created callback object which can be used to\n            stop listening.\n\n    \"\"\"\n    return self._event_hub.add_callback(\n        PRE_CREATE_ATTR_DEFS_CHANGED_TOPIC, callback\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.add_publish_attr_defs_change_callback","title":"<code>add_publish_attr_defs_change_callback(callback)</code>","text":"<p>Register callback to listen publish attribute changes.</p> <p>Publish plugin changed attribute definitions of instance of context.</p> <p>Data structure of event:</p> <pre><code>```python\n{\n    \"instance_changes\": {\n        None: {\n            \"instance\": None,\n            \"plugin_names\": {\"PluginA\"},\n        }\n        \"&lt;instance_id&gt;\": {\n            \"instance\": CreatedInstance,\n            \"plugin_names\": {\"PluginB\", \"PluginC\"},\n        }\n    },\n    \"create_context\": CreateContext\n}\n```\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>callback</code> <code>Callable</code> <p>Callback function that will be called when publish attributes changed.</p> required <p>Returns:</p> Name Type Description <code>EventCallback</code> <code>'EventCallback'</code> <p>Created callback object which can be used to stop listening.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def add_publish_attr_defs_change_callback(\n    self, callback: Callable\n) -&gt; \"EventCallback\":\n    \"\"\"Register callback to listen publish attribute changes.\n\n    Publish plugin changed attribute definitions of instance of context.\n\n    Data structure of event:\n\n        ```python\n        {\n            \"instance_changes\": {\n                None: {\n                    \"instance\": None,\n                    \"plugin_names\": {\"PluginA\"},\n                }\n                \"&lt;instance_id&gt;\": {\n                    \"instance\": CreatedInstance,\n                    \"plugin_names\": {\"PluginB\", \"PluginC\"},\n                }\n            },\n            \"create_context\": CreateContext\n        }\n        ```\n\n    Args:\n        callback (Callable): Callback function that will be called when\n            publish attributes changed.\n\n    Returns:\n        EventCallback: Created callback object which can be used to\n            stop listening.\n\n    \"\"\"\n    return self._event_hub.add_callback(\n        PUBLISH_ATTR_DEFS_CHANGED_TOPIC, callback\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.add_value_changed_callback","title":"<code>add_value_changed_callback(callback)</code>","text":"<p>Register callback to listen value changes.</p> <p>Event is triggered when any value changes on any instance or     context data.</p> <p>Data structure of event:</p> <pre><code>```python\n{\n    \"changes\": [\n        {\n            \"instance\": CreatedInstance,\n            \"changes\": {\n                \"folderPath\": \"/new/folder/path\",\n                \"creator_attributes\": {\n                    \"attr_1\": \"value_1\"\n                }\n            }\n        }\n    ],\n    \"create_context\": CreateContext\n}\n```\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>callback</code> <code>Callable</code> <p>Callback function that will be called when value changed.</p> required <p>Returns:</p> Name Type Description <code>EventCallback</code> <code>'EventCallback'</code> <p>Created callback object which can be used to stop listening.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def add_value_changed_callback(\n    self, callback: Callable\n) -&gt; \"EventCallback\":\n    \"\"\"Register callback to listen value changes.\n\n    Event is triggered when any value changes on any instance or\n        context data.\n\n    Data structure of event:\n\n        ```python\n        {\n            \"changes\": [\n                {\n                    \"instance\": CreatedInstance,\n                    \"changes\": {\n                        \"folderPath\": \"/new/folder/path\",\n                        \"creator_attributes\": {\n                            \"attr_1\": \"value_1\"\n                        }\n                    }\n                }\n            ],\n            \"create_context\": CreateContext\n        }\n        ```\n\n    Args:\n        callback (Callable): Callback function that will be called when\n            value changed.\n\n    Returns:\n        EventCallback: Created callback object which can be used to\n            stop listening.\n\n    \"\"\"\n    return self._event_hub.add_callback(VALUE_CHANGED_TOPIC, callback)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.bulk_instances_collection","title":"<code>bulk_instances_collection(sender=None)</code>","text":"<p>DEPRECATED use 'bulk_add_instances' instead.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>@contextmanager\ndef bulk_instances_collection(self, sender: Optional[str] = None):\n    \"\"\"DEPRECATED use 'bulk_add_instances' instead.\"\"\"\n    # TODO add warning\n    with self.bulk_add_instances(sender) as bulk_info:\n        yield bulk_info\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.context_data_changes","title":"<code>context_data_changes()</code>","text":"<p>Changes of attributes.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def context_data_changes(self) -&gt; TrackChangesItem:\n    \"\"\"Changes of attributes.\"\"\"\n\n    return TrackChangesItem(\n        self._original_context_data, self.context_data_to_store()\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.context_data_to_store","title":"<code>context_data_to_store()</code>","text":"<p>Data that should be stored by host function.</p> <p>The same data should be returned on loading.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def context_data_to_store(self) -&gt; dict[str, Any]:\n    \"\"\"Data that should be stored by host function.\n\n    The same data should be returned on loading.\n    \"\"\"\n    return {\n        \"publish_attributes\": self._publish_attributes.data_to_store()\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.create","title":"<code>create(creator_identifier, variant, folder_entity=None, task_entity=None, pre_create_data=None, active=None, product_type=None)</code>","text":"<p>Trigger create of plugins with standartized arguments.</p> <p>Arguments 'folder_entity' and 'task_name' use current context as default values. If only 'task_entity' is provided it will be overridden by task name from current context. If 'task_name' is not provided when 'folder_entity' is, it is considered that task name is not specified, which can lead to error if product name template requires task name.</p> <p>Parameters:</p> Name Type Description Default <code>creator_identifier</code> <code>str</code> <p>Identifier of creator plugin.</p> required <code>variant</code> <code>str</code> <p>Variant used for product name.</p> required <code>folder_entity</code> <code>dict[str, Any]</code> <p>Folder entity which define context of creation (possible context of created instance/s).</p> <code>None</code> <code>task_entity</code> <code>dict[str, Any]</code> <p>Task entity.</p> <code>None</code> <code>pre_create_data</code> <code>dict[str, Any]</code> <p>Pre-create attribute values.</p> <code>None</code> <code>active</code> <code>Optional[bool]</code> <p>Whether the created instance defaults to be active or not.</p> <code>None</code> <code>product_type</code> <code>str</code> <p>Specific product type to use.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>Any</code> <code>Any</code> <p>Output of triggered creator's 'create' method.</p> <p>Raises:</p> Type Description <code>CreatorError</code> <p>If creator was not found or folder is empty.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def create(\n    self,\n    creator_identifier: str,\n    variant: str,\n    folder_entity: Optional[dict[str, Any]] = None,\n    task_entity: Optional[dict[str, Any]] = None,\n    pre_create_data: Optional[dict[str, Any]] = None,\n    active: Optional[bool] = None,\n    product_type: Optional[str] = None,\n) -&gt; Any:\n    \"\"\"Trigger create of plugins with standartized arguments.\n\n    Arguments 'folder_entity' and 'task_name' use current context as\n    default values. If only 'task_entity' is provided it will be\n    overridden by task name from current context. If 'task_name' is not\n    provided when 'folder_entity' is, it is considered that task name is\n    not specified, which can lead to error if product name template\n    requires task name.\n\n    Args:\n        creator_identifier (str): Identifier of creator plugin.\n        variant (str): Variant used for product name.\n        folder_entity (dict[str, Any]): Folder entity which define context\n            of creation (possible context of created instance/s).\n        task_entity (dict[str, Any]): Task entity.\n        pre_create_data (dict[str, Any]): Pre-create attribute values.\n        active (Optional[bool]): Whether the created instance defaults\n            to be active or not.\n        product_type (str): Specific product type to use.\n\n    Returns:\n        Any: Output of triggered creator's 'create' method.\n\n    Raises:\n        CreatorError: If creator was not found or folder is empty.\n\n    \"\"\"\n    creator = self._get_creator_in_create(creator_identifier)\n\n    project_name = self.project_name\n    if folder_entity is None:\n        folder_path = self.get_current_folder_path()\n        folder_entity = ayon_api.get_folder_by_path(\n            project_name, folder_path\n        )\n        if folder_entity is None:\n            raise CreatorError(f\"Folder '{folder_path}' was not found\")\n\n    if task_entity is None:\n        current_task_name = self.get_current_task_name()\n        if current_task_name:\n            task_entity = ayon_api.get_task_by_name(\n                project_name, folder_entity[\"id\"], current_task_name\n            )\n\n    # Handle not passed product type\n    if product_type is None:\n        for product_type_item in creator.get_product_type_items():\n            product_type = product_type_item.product_type\n            break\n        else:\n            product_type = creator.product_base_type\n\n    if pre_create_data is None:\n        pre_create_data = {}\n\n    precreate_attr_defs = []\n    # Hidden creators do not have or need the pre-create attributes.\n    if isinstance(creator, Creator):\n        precreate_attr_defs = creator.get_pre_create_attr_defs()\n\n    # Create default values of precreate data\n    _pre_create_data = get_default_values(precreate_attr_defs)\n    # Update passed precreate data to default values\n    # TODO validate types\n    _pre_create_data.update(pre_create_data)\n\n    project_entity = self.get_current_project_entity()\n    args = (\n        project_name,\n        folder_entity,\n        task_entity,\n        variant,\n        self.host_name,\n    )\n    kwargs = {\n        # Backwards compatibility for 'project_entity' argument (24/07/08)\n        \"project_entity\": project_entity,\n        # Backwards compatibility for 'product_type' argument (26/01/19)\n        \"product_type\": product_type,\n    }\n    for kwarg in (\"product_type\", \"project_entity\"):\n        if not is_func_signature_supported(\n            creator.get_product_name, *args, **kwargs\n        ):\n            kwargs.pop(kwarg)\n\n    product_name = creator.get_product_name(*args, **kwargs)\n\n    instance_data = {\n        \"folderPath\": folder_entity[\"path\"],\n        \"task\": task_entity[\"name\"] if task_entity else None,\n        \"productType\": product_type,\n        # Add product base type if supported. Fallback to\n        #   older attribute 'product_type'\n        \"productBaseType\": (\n            creator.product_base_type or creator.product_type),\n        \"variant\": variant\n    }\n\n    if active is not None:\n        if not isinstance(active, bool):\n            self.log.warning(\n                \"CreateContext.create 'active' argument is not a bool. \"\n                f\"Converting {active} {type(active)} to bool.\")\n            active = bool(active)\n        instance_data[\"active\"] = active\n\n    with self.bulk_add_instances():\n        return creator.create(\n            product_name,\n            instance_data,\n            _pre_create_data\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.create_plugin_pre_create_attr_defs_changed","title":"<code>create_plugin_pre_create_attr_defs_changed(identifier)</code>","text":"<p>Create plugin pre-create attributes changed.</p> <p>Triggered by 'Creator'.</p> <p>Parameters:</p> Name Type Description Default <code>identifier</code> <code>str</code> <p>Create plugin identifier.</p> required Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def create_plugin_pre_create_attr_defs_changed(\n    self, identifier: str\n) -&gt; None:\n    \"\"\"Create plugin pre-create attributes changed.\n\n    Triggered by 'Creator'.\n\n    Args:\n        identifier (str): Create plugin identifier.\n\n    \"\"\"\n    with self.bulk_pre_create_attr_defs_change() as bulk_item:\n        bulk_item.append(identifier)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.create_with_unified_error","title":"<code>create_with_unified_error(identifier, *args, **kwargs)</code>","text":"<p>Trigger create but raise only one error if anything fails.</p> <p>Added to raise unified exception. Capture any possible issues and reraise it with unified information.</p> <p>Parameters:</p> Name Type Description Default <code>identifier</code> <code>str</code> <p>Identifier of creator.</p> required <code>*args</code> <p>Arguments for create method.</p> <code>()</code> <code>**kwargs</code> <p>Keyword argument for create method.</p> <code>{}</code> <p>Raises:</p> Type Description <code>CreatorsCreateFailed</code> <p>When creation fails due to any possible reason. If anything goes wrong this is only possible exception the method should raise.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def create_with_unified_error(\n    self, identifier: str, *args, **kwargs\n) -&gt; Any:\n    \"\"\"Trigger create but raise only one error if anything fails.\n\n    Added to raise unified exception. Capture any possible issues and\n    reraise it with unified information.\n\n    Args:\n        identifier (str): Identifier of creator.\n        *args: Arguments for create method.\n        **kwargs: Keyword argument for create method.\n\n    Raises:\n        CreatorsCreateFailed: When creation fails due to any possible\n            reason. If anything goes wrong this is only possible exception\n            the method should raise.\n\n    \"\"\"\n    result, fail_info = self._create_with_unified_error(\n        identifier, None, *args, **kwargs\n    )\n    if fail_info is not None:\n        raise CreatorsCreateFailed([fail_info])\n    return result\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.creator_adds_instance","title":"<code>creator_adds_instance(instance)</code>","text":"<p>Creator adds new instance to context.</p> <p>Instances should be added only from creators.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>CreatedInstance</code> <p>Instance with prepared data from creator.</p> required <p>TODO: Rename method to more suit.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def creator_adds_instance(self, instance: \"CreatedInstance\") -&gt; None:\n    \"\"\"Creator adds new instance to context.\n\n    Instances should be added only from creators.\n\n    Args:\n        instance(CreatedInstance): Instance with prepared data from\n            creator.\n\n    TODO: Rename method to more suit.\n    \"\"\"\n    # Add instance to instances list\n    if instance.id in self._instances_by_id:\n        self.log.warning((\n            \"Instance with id {} is already added to context.\"\n        ).format(instance.id))\n        return\n\n    self._instances_by_id[instance.id] = instance\n\n    # Add instance to be validated inside 'bulk_add_instances'\n    #   context manager if is inside bulk\n    with self.bulk_add_instances() as bulk_info:\n        bulk_info.append(instance)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.creator_removed_instance","title":"<code>creator_removed_instance(instance)</code>","text":"<p>When creator removes instance context should be acknowledged.</p> <p>If creator removes instance context should know about it to avoid possible issues in the session.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>CreatedInstance</code> <p>Object of instance which was removed from scene metadata.</p> required Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def creator_removed_instance(self, instance: \"CreatedInstance\") -&gt; None:\n    \"\"\"When creator removes instance context should be acknowledged.\n\n    If creator removes instance context should know about it to avoid\n    possible issues in the session.\n\n    Args:\n        instance (CreatedInstance): Object of instance which was removed\n            from scene metadata.\n    \"\"\"\n\n    self._remove_instances([instance])\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.execute_autocreators","title":"<code>execute_autocreators()</code>","text":"<p>Execute discovered AutoCreator plugins.</p> <p>Reset instances if any autocreator executed properly.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def execute_autocreators(self) -&gt; None:\n    \"\"\"Execute discovered AutoCreator plugins.\n\n    Reset instances if any autocreator executed properly.\n    \"\"\"\n\n    failed_info = []\n    for creator in self.sorted_autocreators:\n        identifier = creator.identifier\n        _, fail_info = self._create_with_unified_error(identifier, creator)\n        if fail_info is not None:\n            failed_info.append(fail_info)\n\n    if failed_info:\n        raise CreatorsCreateFailed(failed_info)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.find_convertor_items","title":"<code>find_convertor_items()</code>","text":"<p>Go through convertor plugins to look for items to convert.</p> <p>Raises:</p> Type Description <code>ConvertorsFindFailed</code> <p>When one or more convertors fails during finding.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def find_convertor_items(self) -&gt; None:\n    \"\"\"Go through convertor plugins to look for items to convert.\n\n    Raises:\n        ConvertorsFindFailed: When one or more convertors fails during\n            finding.\n    \"\"\"\n\n    self.convertor_items_by_id = {}\n\n    failed_info = []\n    for convertor in self.convertors_plugins.values():\n        try:\n            convertor.find_instances()\n\n        except:  # noqa: E722\n            failed_info.append(\n                prepare_failed_convertor_operation_info(\n                    convertor.identifier, sys.exc_info()\n                )\n            )\n            self.log.warning(\n                \"Failed to find instances of convertor \\\"{}\\\"\".format(\n                    convertor.identifier\n                ),\n                exc_info=True\n            )\n\n    if failed_info:\n        raise ConvertorsFindFailed(failed_info)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.get_current_folder_entity","title":"<code>get_current_folder_entity()</code>","text":"<p>Folder entity for current context folder.</p> <p>Returns:</p> Type Description <code>Optional[dict[str, Any]]</code> <p>Optional[dict[str, Any]]: Folder entity.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def get_current_folder_entity(self) -&gt; Optional[dict[str, Any]]:\n    \"\"\"Folder entity for current context folder.\n\n    Returns:\n        Optional[dict[str, Any]]: Folder entity.\n\n    \"\"\"\n    if self._current_folder_entity is not _NOT_SET:\n        return copy.deepcopy(self._current_folder_entity)\n\n    folder_path = self.get_current_folder_path()\n    self._current_folder_entity = self.get_folder_entity(folder_path)\n    return copy.deepcopy(self._current_folder_entity)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.get_current_folder_path","title":"<code>get_current_folder_path()</code>","text":"<p>Folder path which was used as current context on context reset.</p> <p>Returns:</p> Type Description <code>Optional[str]</code> <p>Optional[str]: Folder path.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def get_current_folder_path(self) -&gt; Optional[str]:\n    \"\"\"Folder path which was used as current context on context reset.\n\n    Returns:\n        Optional[str]: Folder path.\n\n    \"\"\"\n    return self._current_folder_path\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.get_current_project_anatomy","title":"<code>get_current_project_anatomy()</code>","text":"<p>Project anatomy for current project.</p> <p>Returns:</p> Name Type Description <code>Anatomy</code> <code>Anatomy</code> <p>Anatomy object ready to be used.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def get_current_project_anatomy(self) -&gt; Anatomy:\n    \"\"\"Project anatomy for current project.\n\n    Returns:\n        Anatomy: Anatomy object ready to be used.\n    \"\"\"\n\n    if self._current_project_anatomy is None:\n        self._current_project_anatomy = Anatomy(\n            self._current_project_name)\n    return self._current_project_anatomy\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.get_current_project_entity","title":"<code>get_current_project_entity()</code>","text":"<p>Project entity for current context project.</p> <p>Returns:</p> Type Description <code>Optional[dict[str, Any]]</code> <p>Optional[dict[str, Any]]: Folder entity.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def get_current_project_entity(self) -&gt; Optional[dict[str, Any]]:\n    \"\"\"Project entity for current context project.\n\n    Returns:\n        Optional[dict[str, Any]]: Folder entity.\n\n    \"\"\"\n    if self._current_project_entity is not _NOT_SET:\n        return copy.deepcopy(self._current_project_entity)\n    project_entity = None\n    project_name = self.get_current_project_name()\n    if project_name:\n        project_entity = ayon_api.get_project(project_name)\n    self._current_project_entity = project_entity\n    return copy.deepcopy(self._current_project_entity)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.get_current_project_name","title":"<code>get_current_project_name()</code>","text":"<p>Project name which was used as current context on context reset.</p> <p>Returns:</p> Type Description <code>str</code> <p>Optional[str]: Project name.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def get_current_project_name(self) -&gt; str:\n    \"\"\"Project name which was used as current context on context reset.\n\n    Returns:\n        Optional[str]: Project name.\n\n    \"\"\"\n    return self._current_project_name\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.get_current_task_entity","title":"<code>get_current_task_entity()</code>","text":"<p>Task entity for current context task.</p> <p>Returns:</p> Type Description <code>Optional[dict[str, Any]]</code> <p>Optional[dict[str, Any]]: Task entity.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def get_current_task_entity(self) -&gt; Optional[dict[str, Any]]:\n    \"\"\"Task entity for current context task.\n\n    Returns:\n        Optional[dict[str, Any]]: Task entity.\n\n    \"\"\"\n    if self._current_task_entity is not _NOT_SET:\n        return copy.deepcopy(self._current_task_entity)\n\n    folder_path = self.get_current_folder_path()\n    task_name = self.get_current_task_name()\n    self._current_task_entity = self.get_task_entity(\n        folder_path, task_name\n    )\n    return copy.deepcopy(self._current_task_entity)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.get_current_task_name","title":"<code>get_current_task_name()</code>","text":"<p>Task name which was used as current context on context reset.</p> <p>Returns:</p> Type Description <code>Optional[str]</code> <p>Optional[str]: Task name.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def get_current_task_name(self) -&gt; Optional[str]:\n    \"\"\"Task name which was used as current context on context reset.\n\n    Returns:\n        Optional[str]: Task name.\n\n    \"\"\"\n    return self._current_task_name\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.get_current_task_type","title":"<code>get_current_task_type()</code>","text":"<p>Task type which was used as current context on context reset.</p> <p>Returns:</p> Type Description <code>Optional[str]</code> <p>Optional[str]: Task type.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def get_current_task_type(self) -&gt; Optional[str]:\n    \"\"\"Task type which was used as current context on context reset.\n\n    Returns:\n        Optional[str]: Task type.\n\n    \"\"\"\n    if self._current_task_type is _NOT_SET:\n        task_type = None\n        task_entity = self.get_current_task_entity()\n        if task_entity:\n            task_type = task_entity[\"taskType\"]\n        self._current_task_type = task_type\n    return self._current_task_type\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.get_current_workfile_path","title":"<code>get_current_workfile_path()</code>","text":"<p>Workfile path which was opened on context reset.</p> <p>Returns:</p> Type Description <code>Optional[str]</code> <p>Optional[str]: Workfile path.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def get_current_workfile_path(self) -&gt; Optional[str]:\n    \"\"\"Workfile path which was opened on context reset.\n\n    Returns:\n        Optional[str]: Workfile path.\n\n    \"\"\"\n    return self._current_workfile_path\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.get_folder_entities","title":"<code>get_folder_entities(folder_paths)</code>","text":"<p>Get folder entities by paths.</p> <p>Parameters:</p> Name Type Description Default <code>folder_paths</code> <code>Iterable[str]</code> <p>Folder paths.</p> required <p>Returns:</p> Type Description <code>dict[str, Optional[dict[str, Any]]]</code> <p>dict[str, Optional[dict[str, Any]]]: Folder entities by path.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def get_folder_entities(\n    self, folder_paths: Iterable[str]\n) -&gt; dict[str, Optional[dict[str, Any]]]:\n    \"\"\"Get folder entities by paths.\n\n    Args:\n        folder_paths (Iterable[str]): Folder paths.\n\n    Returns:\n        dict[str, Optional[dict[str, Any]]]: Folder entities by path.\n\n    \"\"\"\n    output = {\n        folder_path: None\n        for folder_path in folder_paths\n    }\n    remainder_paths = set()\n    for folder_path in output:\n        # Skip invalid folder paths (folder name or empty path)\n        if not folder_path or \"/\" not in folder_path:\n            continue\n\n        if folder_path not in self._folder_entities_by_path:\n            remainder_paths.add(folder_path)\n            continue\n\n        output[folder_path] = self._folder_entities_by_path[folder_path]\n\n    if not remainder_paths:\n        return output\n\n    found_paths = set()\n    for folder_entity in ayon_api.get_folders(\n        self.project_name,\n        folder_paths=remainder_paths,\n    ):\n        folder_path = folder_entity[\"path\"]\n        found_paths.add(folder_path)\n        output[folder_path] = folder_entity\n        self._folder_entities_by_path[folder_path] = folder_entity\n\n    # Cache empty folder entities\n    for path in remainder_paths - found_paths:\n        self._folder_entities_by_path[path] = None\n\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.get_folder_entity","title":"<code>get_folder_entity(folder_path)</code>","text":"<p>Get folder entity by path.</p> <p>Entities are cached until reset.</p> <p>Parameters:</p> Name Type Description Default <code>folder_path</code> <code>Optional[str]</code> <p>Folder path.</p> required <p>Returns:</p> Type Description <code>Optional[dict[str, Any]]</code> <p>Optional[dict[str, Any]]: Folder entity.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def get_folder_entity(\n    self,\n    folder_path: Optional[str],\n) -&gt; Optional[dict[str, Any]]:\n    \"\"\"Get folder entity by path.\n\n    Entities are cached until reset.\n\n    Args:\n        folder_path (Optional[str]): Folder path.\n\n    Returns:\n        Optional[dict[str, Any]]: Folder entity.\n\n    \"\"\"\n    if not folder_path:\n        return None\n    return self.get_folder_entities([folder_path]).get(folder_path)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.get_host_misssing_methods","title":"<code>get_host_misssing_methods(host)</code>  <code>classmethod</code>","text":"<p>Collect missing methods from host.</p> <p>Parameters:</p> Name Type Description Default <code>host</code> <code>ModuleType</code> <p>Host implementaion.</p> required Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>@classmethod\ndef get_host_misssing_methods(cls, host):\n    \"\"\"Collect missing methods from host.\n\n    Args:\n        host(ModuleType): Host implementaion.\n    \"\"\"\n\n    missing = set(\n        IPublishHost.get_missing_publish_methods(host)\n    )\n    return missing\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.get_instance_by_id","title":"<code>get_instance_by_id(instance_id)</code>","text":"<p>Receive instance by id.</p> <p>Parameters:</p> Name Type Description Default <code>instance_id</code> <code>str</code> <p>Instance id.</p> required <p>Returns:</p> Type Description <code>Optional['CreatedInstance']</code> <p>Optional[CreatedInstance]: Instance or None if instance with given id is not available.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def get_instance_by_id(\n    self, instance_id: str\n) -&gt; Optional[\"CreatedInstance\"]:\n    \"\"\"Receive instance by id.\n\n    Args:\n        instance_id (str): Instance id.\n\n    Returns:\n        Optional[CreatedInstance]: Instance or None if instance with\n            given id is not available.\n\n    \"\"\"\n    return self._instances_by_id.get(instance_id)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.get_instances_context_info","title":"<code>get_instances_context_info(instances=None)</code>","text":"<p>Validate 'folder' and 'task' instance context.</p> <p>Parameters:</p> Name Type Description Default <code>instances</code> <code>Optional[Iterable[CreatedInstance]]</code> <p>Instances to validate. If not provided all instances are validated.</p> <code>None</code> <p>Returns:</p> Type Description <code>dict[str, InstanceContextInfo]</code> <p>dict[str, InstanceContextInfo]: Validation results by instance id.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def get_instances_context_info(\n    self, instances: Optional[Iterable[\"CreatedInstance\"]] = None\n) -&gt; dict[str, InstanceContextInfo]:\n    \"\"\"Validate 'folder' and 'task' instance context.\n\n    Args:\n        instances (Optional[Iterable[CreatedInstance]]): Instances to\n            validate. If not provided all instances are validated.\n\n    Returns:\n        dict[str, InstanceContextInfo]: Validation results by instance id.\n\n    \"\"\"\n    # Use all instances from context if 'instances' are not passed\n    if instances is None:\n        instances = self._instances_by_id.values()\n    instances = tuple(instances)\n    info_by_instance_id = {\n        instance.id: InstanceContextInfo(\n            instance.get(\"folderPath\"),\n            instance.get(\"task\"),\n            False,\n            False,\n        )\n        for instance in instances\n    }\n\n    # Skip if instances are empty\n    if not info_by_instance_id:\n        return info_by_instance_id\n\n    project_name = self.project_name\n\n    to_validate = []\n    task_names_by_folder_path = collections.defaultdict(set)\n    for instance in instances:\n        context_info = info_by_instance_id[instance.id]\n        if instance.has_promised_context:\n            context_info.folder_is_valid = True\n            context_info.task_is_valid = True\n            # NOTE missing task type\n            continue\n        # TODO allow context promise\n        folder_path = context_info.folder_path\n        if not folder_path:\n            continue\n\n        if folder_path in self._folder_entities_by_path:\n            folder_entity = self._folder_entities_by_path[folder_path]\n            if folder_entity is None:\n                continue\n            context_info.folder_is_valid = True\n\n        task_name = context_info.task_name\n        if task_name is not None:\n            tasks_cache = self._task_names_by_folder_path.get(folder_path)\n            if tasks_cache is not None:\n                context_info.task_is_valid = task_name in tasks_cache\n                continue\n\n        to_validate.append(instance)\n        task_names_by_folder_path[folder_path].add(task_name)\n\n    if not to_validate:\n        return info_by_instance_id\n\n    # Backwards compatibility for cases where folder name is set instead\n    #   of folder path\n    folder_paths = set()\n    task_names_by_folder_name = {}\n    task_names_by_folder_path_clean = {}\n    for folder_path, task_names in task_names_by_folder_path.items():\n        if folder_path is None:\n            continue\n\n        clean_task_names = {\n            task_name\n            for task_name in task_names\n            if task_name\n        }\n\n        if \"/\" not in folder_path:\n            task_names_by_folder_name[folder_path] = clean_task_names\n            continue\n\n        folder_paths.add(folder_path)\n        if not clean_task_names:\n            continue\n\n        task_names_by_folder_path_clean[folder_path] = clean_task_names\n\n    folder_paths_by_name = collections.defaultdict(list)\n    if task_names_by_folder_name:\n        for folder_entity in ayon_api.get_folders(\n            project_name,\n            folder_names=task_names_by_folder_name.keys(),\n            fields={\"name\", \"path\"}\n        ):\n            folder_name = folder_entity[\"name\"]\n            folder_path = folder_entity[\"path\"]\n            folder_paths_by_name[folder_name].append(folder_path)\n\n    folder_path_by_name = {}\n    for folder_name, paths in folder_paths_by_name.items():\n        if len(paths) != 1:\n            continue\n        path = paths[0]\n        folder_path_by_name[folder_name] = path\n        folder_paths.add(path)\n        clean_task_names = task_names_by_folder_name[folder_name]\n        if not clean_task_names:\n            continue\n        folder_task_names = task_names_by_folder_path_clean.setdefault(\n            path, set()\n        )\n        folder_task_names |= clean_task_names\n\n    folder_entities_by_path = self.get_folder_entities(folder_paths)\n    task_entities_by_folder_path = self.get_task_entities(\n        task_names_by_folder_path_clean\n    )\n\n    for instance in to_validate:\n        folder_path = instance[\"folderPath\"]\n        task_name = instance.get(\"task\")\n        if folder_path and \"/\" not in folder_path:\n            new_folder_path = folder_path_by_name.get(folder_path)\n            if new_folder_path:\n                folder_path = new_folder_path\n                instance[\"folderPath\"] = new_folder_path\n\n        folder_entity = folder_entities_by_path.get(folder_path)\n        if not folder_entity:\n            continue\n        context_info = info_by_instance_id[instance.id]\n        context_info.folder_is_valid = True\n\n        if (\n            not task_name\n            or task_name in task_entities_by_folder_path[folder_path]\n        ):\n            context_info.task_is_valid = True\n    return info_by_instance_id\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.get_instances_task_entities","title":"<code>get_instances_task_entities(instances=None)</code>","text":"<p>Get task entities for instances.</p> <p>Parameters:</p> Name Type Description Default <code>instances</code> <code>Optional[Iterable[CreatedInstance]]</code> <p>Instances to get task entities. If not provided all instances are used.</p> <code>None</code> <p>Returns:</p> Type Description <code>dict[str, Optional[dict[str, Any]]]</code> <p>dict[str, Optional[dict[str, Any]]]: Task entity by instance id.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def get_instances_task_entities(\n    self, instances: Optional[Iterable[\"CreatedInstance\"]] = None\n) -&gt; dict[str, Optional[dict[str, Any]]]:\n    \"\"\"Get task entities for instances.\n\n    Args:\n        instances (Optional[Iterable[CreatedInstance]]): Instances to\n            get task entities. If not provided all instances are used.\n\n    Returns:\n        dict[str, Optional[dict[str, Any]]]: Task entity by instance id.\n\n    \"\"\"\n    if instances is None:\n        instances = self._instances_by_id.values()\n    instances = list(instances)\n\n    output = {\n        instance.id: None\n        for instance in instances\n    }\n    if not instances:\n        return output\n\n    filtered_instances = []\n    task_names_by_folder_path = collections.defaultdict(set)\n    for instance in instances:\n        folder_path = instance.get(\"folderPath\")\n        task_name = instance.get(\"task\")\n        if not folder_path or not task_name:\n            continue\n        filtered_instances.append(instance)\n        task_names_by_folder_path[folder_path].add(task_name)\n\n    task_entities_by_folder_path = self.get_task_entities(\n        task_names_by_folder_path\n    )\n    for instance in filtered_instances:\n        folder_path = instance[\"folderPath\"]\n        task_name = instance[\"task\"]\n        output[instance.id] = (\n            task_entities_by_folder_path[folder_path][task_name]\n        )\n\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.get_sorted_creators","title":"<code>get_sorted_creators(identifiers=None)</code>","text":"<p>Sorted creators by 'order' attribute.</p> <p>Parameters:</p> Name Type Description Default <code>identifiers</code> <code>Optional[Iterable[str]]</code> <p>Filter creators by identifiers. All creators are returned if 'None' is passed.</p> <code>None</code> <p>Returns:</p> Type Description <code>list['BaseCreator']</code> <p>list[BaseCreator]: Sorted creator plugins by 'order' value.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def get_sorted_creators(\n    self, identifiers: Optional[Iterable[str]] = None\n) -&gt; list[\"BaseCreator\"]:\n    \"\"\"Sorted creators by 'order' attribute.\n\n    Args:\n        identifiers (Optional[Iterable[str]]): Filter creators by\n            identifiers. All creators are returned if 'None' is passed.\n\n    Returns:\n        list[BaseCreator]: Sorted creator plugins by 'order' value.\n\n    \"\"\"\n    if identifiers is not None:\n        identifiers = set(identifiers)\n        creators = [\n            creator\n            for identifier, creator in self.creators.items()\n            if identifier in identifiers\n        ]\n    else:\n        creators = self.creators.values()\n\n    return sorted(\n        creators, key=lambda creator: creator.order\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.get_task_entities","title":"<code>get_task_entities(task_names_by_folder_paths)</code>","text":"<p>Get task entities by folder path and task name.</p> <p>Entities are cached until reset.</p> <p>Parameters:</p> Name Type Description Default <code>task_names_by_folder_paths</code> <code>dict[str, set[str]]</code> <p>Task names by folder path.</p> required <p>Returns:</p> Type Description <code>dict[str, dict[str, Optional[dict[str, Any]]]]</code> <p>dict[str, dict[str, dict[str, Any]]]: Task entities by folder path and task name.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def get_task_entities(\n    self,\n    task_names_by_folder_paths: dict[str, set[str]]\n) -&gt; dict[str, dict[str, Optional[dict[str, Any]]]]:\n    \"\"\"Get task entities by folder path and task name.\n\n    Entities are cached until reset.\n\n    Args:\n        task_names_by_folder_paths (dict[str, set[str]]): Task names by\n            folder path.\n\n    Returns:\n        dict[str, dict[str, dict[str, Any]]]: Task entities by folder path\n            and task name.\n\n    \"\"\"\n    output = {}\n    for folder_path, task_names in task_names_by_folder_paths.items():\n        if folder_path is None:\n            continue\n        output[folder_path] = {\n            task_name: None\n            for task_name in task_names\n            if task_name is not None\n        }\n\n    missing_folder_paths = set()\n    for folder_path, output_task_entities_by_name in output.items():\n        if not output_task_entities_by_name:\n            continue\n\n        if folder_path not in self._task_ids_by_folder_path:\n            missing_folder_paths.add(folder_path)\n            continue\n\n        all_tasks_filled = True\n        task_ids = self._task_ids_by_folder_path[folder_path]\n        task_entities_by_name = {}\n        for task_id in task_ids:\n            task_entity = self._task_entities_by_id.get(task_id)\n            if task_entity is None:\n                all_tasks_filled = False\n                continue\n            task_entities_by_name[task_entity[\"name\"]] = task_entity\n\n        any_missing = False\n        for task_name in set(output_task_entities_by_name):\n            task_entity = task_entities_by_name.get(task_name)\n            if task_entity is None:\n                any_missing = True\n                continue\n\n            output_task_entities_by_name[task_name] = task_entity\n\n        if any_missing and not all_tasks_filled:\n            missing_folder_paths.add(folder_path)\n\n    if not missing_folder_paths:\n        return output\n\n    folder_entities_by_path = self.get_folder_entities(\n        missing_folder_paths\n    )\n    folder_path_by_id = {}\n    for folder_path, folder_entity in folder_entities_by_path.items():\n        if folder_entity is not None:\n            folder_path_by_id[folder_entity[\"id\"]] = folder_path\n\n    if not folder_path_by_id:\n        return output\n\n    task_entities_by_parent_id = collections.defaultdict(list)\n    for task_entity in ayon_api.get_tasks(\n        self.project_name,\n        folder_ids=folder_path_by_id.keys()\n    ):\n        folder_id = task_entity[\"folderId\"]\n        task_entities_by_parent_id[folder_id].append(task_entity)\n\n    for folder_id, task_entities in task_entities_by_parent_id.items():\n        folder_path = folder_path_by_id[folder_id]\n        task_ids = set()\n        task_names = set()\n        for task_entity in task_entities:\n            task_id = task_entity[\"id\"]\n            task_name = task_entity[\"name\"]\n            task_ids.add(task_id)\n            task_names.add(task_name)\n            self._task_entities_by_id[task_id] = task_entity\n\n            output[folder_path][task_name] = task_entity\n        self._task_ids_by_folder_path[folder_path] = task_ids\n        self._task_names_by_folder_path[folder_path] = task_names\n\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.get_task_entity","title":"<code>get_task_entity(folder_path, task_name)</code>","text":"<p>Get task entity by name and folder path.</p> <p>Entities are cached until reset.</p> <p>Parameters:</p> Name Type Description Default <code>folder_path</code> <code>Optional[str]</code> <p>Folder path.</p> required <code>task_name</code> <code>Optional[str]</code> <p>Task name.</p> required <p>Returns:</p> Type Description <code>Optional[dict[str, Any]]</code> <p>Optional[dict[str, Any]]: Task entity.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def get_task_entity(\n    self,\n    folder_path: Optional[str],\n    task_name: Optional[str],\n) -&gt; Optional[dict[str, Any]]:\n    \"\"\"Get task entity by name and folder path.\n\n    Entities are cached until reset.\n\n    Args:\n        folder_path (Optional[str]): Folder path.\n        task_name (Optional[str]): Task name.\n\n    Returns:\n        Optional[dict[str, Any]]: Task entity.\n\n    \"\"\"\n    if not folder_path or not task_name:\n        return None\n\n    output = self.get_task_entities({folder_path: {task_name}})\n    return output.get(folder_path, {}).get(task_name)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.get_template_data","title":"<code>get_template_data(folder_path, task_name)</code>","text":"<p>Prepare template data for given context.</p> <p>Method is using cached entities and settings to prepare template data.</p> <p>Parameters:</p> Name Type Description Default <code>folder_path</code> <code>Optional[str]</code> <p>Folder path.</p> required <code>task_name</code> <code>Optional[str]</code> <p>Task name.</p> required <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: Template data.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def get_template_data(\n    self, folder_path: Optional[str], task_name: Optional[str]\n) -&gt; dict[str, Any]:\n    \"\"\"Prepare template data for given context.\n\n    Method is using cached entities and settings to prepare template data.\n\n    Args:\n        folder_path (Optional[str]): Folder path.\n        task_name (Optional[str]): Task name.\n\n    Returns:\n        dict[str, Any]: Template data.\n\n    \"\"\"\n    project_entity = self.get_current_project_entity()\n    folder_entity = task_entity = None\n    if folder_path:\n        folder_entity = self.get_folder_entity(folder_path)\n        if task_name and folder_entity:\n            task_entity = self.get_task_entity(folder_path, task_name)\n\n    return get_template_data(\n        project_entity,\n        folder_entity,\n        task_entity,\n        host_name=self.host_name,\n        settings=self.get_current_project_settings(),\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.instance_create_attr_defs_changed","title":"<code>instance_create_attr_defs_changed(instance_id)</code>","text":"<p>Instance attribute definitions changed.</p> <p>Triggered by instance 'CreatorAttributeValues' on instance.</p> <p>Parameters:</p> Name Type Description Default <code>instance_id</code> <code>str</code> <p>Instance id.</p> required Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def instance_create_attr_defs_changed(self, instance_id: str) -&gt; None:\n    \"\"\"Instance attribute definitions changed.\n\n    Triggered by instance 'CreatorAttributeValues' on instance.\n\n    Args:\n        instance_id (str): Instance id.\n\n    \"\"\"\n    if self._is_instance_events_ready(instance_id):\n        with self.bulk_create_attr_defs_change() as bulk_item:\n            bulk_item.append(instance_id)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.instance_parent_changed","title":"<code>instance_parent_changed(instance_id)</code>","text":"<p>Instance parent changed.</p> <p>Triggered by <code>CreatedInstance</code>.</p> <p>Parameters:</p> Name Type Description Default <code>instance_id</code> <code>Optional[str]</code> <p>Instance id.</p> required Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def instance_parent_changed(self, instance_id: str) -&gt; None:\n    \"\"\"Instance parent changed.\n\n    Triggered by `CreatedInstance`.\n\n    Args:\n        instance_id (Optional[str]): Instance id.\n\n    \"\"\"\n    if self._is_instance_events_ready(instance_id):\n        with self.bulk_instance_parent_change() as bulk_item:\n            bulk_item.append(instance_id)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.instance_publish_attr_defs_changed","title":"<code>instance_publish_attr_defs_changed(instance_id, plugin_name)</code>","text":"<p>Instance attribute definitions changed.</p> <p>Triggered by instance 'PublishAttributeValues' on instance.</p> <p>Parameters:</p> Name Type Description Default <code>instance_id</code> <code>Optional[str]</code> <p>Instance id or None for context.</p> required <code>plugin_name</code> <code>str</code> <p>Plugin name which attribute definitions were changed.</p> required Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def instance_publish_attr_defs_changed(\n    self, instance_id: Optional[str], plugin_name: str\n) -&gt; None:\n    \"\"\"Instance attribute definitions changed.\n\n    Triggered by instance 'PublishAttributeValues' on instance.\n\n    Args:\n        instance_id (Optional[str]): Instance id or None for context.\n        plugin_name (str): Plugin name which attribute definitions were\n            changed.\n\n    \"\"\"\n    if self._is_instance_events_ready(instance_id):\n        with self.bulk_publish_attr_defs_change() as bulk_item:\n            bulk_item.append((instance_id, plugin_name))\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.instance_requirement_changed","title":"<code>instance_requirement_changed(instance_id)</code>","text":"<p>Instance requirement changed.</p> <p>Triggered by <code>CreatedInstance</code>.</p> <p>Parameters:</p> Name Type Description Default <code>instance_id</code> <code>Optional[str]</code> <p>Instance id.</p> required Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def instance_requirement_changed(self, instance_id: str) -&gt; None:\n    \"\"\"Instance requirement changed.\n\n    Triggered by `CreatedInstance`.\n\n    Args:\n        instance_id (Optional[str]): Instance id.\n\n    \"\"\"\n    if self._is_instance_events_ready(instance_id):\n        with self.bulk_instance_requirement_change() as bulk_item:\n            bulk_item.append(instance_id)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.instance_values_changed","title":"<code>instance_values_changed(instance_id, new_values)</code>","text":"<p>Instance value changed.</p> <p>Triggered by `CreatedInstance, 'CreatorAttributeValues'     or 'PublishAttributeValues' on instance.</p> <p>Parameters:</p> Name Type Description Default <code>instance_id</code> <code>Optional[str]</code> <p>Instance id or None for context.</p> required <code>new_values</code> <code>dict[str, Any]</code> <p>Changed values.</p> required Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def instance_values_changed(\n    self, instance_id: Optional[str], new_values: dict[str, Any]\n) -&gt; None:\n    \"\"\"Instance value changed.\n\n    Triggered by `CreatedInstance, 'CreatorAttributeValues'\n        or 'PublishAttributeValues' on instance.\n\n    Args:\n        instance_id (Optional[str]): Instance id or None for context.\n        new_values (dict[str, Any]): Changed values.\n\n    \"\"\"\n    if self._is_instance_events_ready(instance_id):\n        with self.bulk_value_changes() as bulk_item:\n            bulk_item.append((instance_id, new_values))\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.publish_attribute_value_changed","title":"<code>publish_attribute_value_changed(plugin_name, value)</code>","text":"<p>Context publish attribute values changed.</p> <p>Triggered by instance 'PublishAttributeValues' on context.</p> <p>Parameters:</p> Name Type Description Default <code>plugin_name</code> <code>str</code> <p>Plugin name which changed value.</p> required <code>value</code> <code>dict[str, Any]</code> <p>Changed values.</p> required Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def publish_attribute_value_changed(\n    self, plugin_name: str, value: dict[str, Any]\n) -&gt; None:\n    \"\"\"Context publish attribute values changed.\n\n    Triggered by instance 'PublishAttributeValues' on context.\n\n    Args:\n        plugin_name (str): Plugin name which changed value.\n        value (dict[str, Any]): Changed values.\n\n    \"\"\"\n    self.instance_values_changed(\n        None,\n        {\n            \"publish_attributes\": {\n                plugin_name: value,\n            },\n        },\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.refresh_thumbnails","title":"<code>refresh_thumbnails()</code>","text":"<p>Cleanup thumbnail paths.</p> <p>Remove all thumbnail filepaths that are empty or lead to files which does not exist or of instances that are not available anymore.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def refresh_thumbnails(self) -&gt; None:\n    \"\"\"Cleanup thumbnail paths.\n\n    Remove all thumbnail filepaths that are empty or lead to files which\n    does not exist or of instances that are not available anymore.\n    \"\"\"\n\n    invalid = set()\n    for instance_id, path in self.thumbnail_paths_by_instance_id.items():\n        instance_available = True\n        if instance_id is not None:\n            instance_available = instance_id in self._instances_by_id\n\n        if (\n            not instance_available\n            or not path\n            or not os.path.exists(path)\n        ):\n            invalid.add(instance_id)\n\n    for instance_id in invalid:\n        self.thumbnail_paths_by_instance_id.pop(instance_id)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.remove_instances","title":"<code>remove_instances(instances, sender=None)</code>","text":"<p>Remove instances from context.</p> <p>All instances that don't have creator identifier leading to existing     creator are just removed from context.</p> <p>Parameters:</p> Name Type Description Default <code>instances</code> <code>list[CreatedInstance]</code> <p>Instances that should be removed. Remove logic is done using creator, which may require to do other cleanup than just remove instance from context.</p> required <code>sender</code> <code>Optional[str]</code> <p>Sender of the event.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def remove_instances(\n    self,\n    instances: list[\"CreatedInstance\"],\n    sender: Optional[str] = None,\n) -&gt; None:\n    \"\"\"Remove instances from context.\n\n    All instances that don't have creator identifier leading to existing\n        creator are just removed from context.\n\n    Args:\n        instances (list[CreatedInstance]): Instances that should be\n            removed. Remove logic is done using creator, which may require\n            to do other cleanup than just remove instance from context.\n        sender (Optional[str]): Sender of the event.\n\n    \"\"\"\n    instance_ids_by_parent_id = collections.defaultdict(set)\n    for instance in self.instances:\n        instance_ids_by_parent_id[instance.parent_instance_id].add(\n            instance.id\n        )\n\n    instances_to_remove = list(instances)\n    ids_to_remove = {\n        instance.id\n        for instance in instances_to_remove\n    }\n    _queue = collections.deque()\n    _queue.extend(instances_to_remove)\n    # Add children with parent lifetime flag\n    while _queue:\n        instance = _queue.popleft()\n        ids_to_remove.add(instance.id)\n        children_ids = instance_ids_by_parent_id[instance.id]\n        for children_id in children_ids:\n            if children_id in ids_to_remove:\n                continue\n            instance = self._instances_by_id[children_id]\n            if instance.parent_flags &amp; ParentFlags.parent_lifetime:\n                instances_to_remove.append(instance)\n                ids_to_remove.add(instance.id)\n                _queue.append(instance)\n\n    instances_by_identifier = collections.defaultdict(list)\n    for instance in instances_to_remove:\n        identifier = instance.creator_identifier\n        instances_by_identifier[identifier].append(instance)\n\n    # Just remove instances from context if creator is not available\n    missing_creators = set(instances_by_identifier) - set(self.creators)\n    miss_creator_instances = []\n    for identifier in missing_creators:\n        miss_creator_instances.extend(instances_by_identifier[identifier])\n\n    with self.bulk_remove_instances(sender):\n        self._remove_instances(miss_creator_instances, sender)\n\n        error_message = \"Instances removement of creator \\\"{}\\\" failed. {}\"\n        failed_info = []\n        # Remove instances by creator plugin order\n        for creator in self.get_sorted_creators(\n            instances_by_identifier.keys()\n        ):\n            identifier = creator.identifier\n            # Filter instances by current state of 'CreateContext'\n            # - in case instances were already removed as subroutine of\n            #   previous create plugin.\n            creator_instances = [\n                instance\n                for instance in instances_by_identifier[identifier]\n                if instance.id in self._instances_by_id\n            ]\n            if not creator_instances:\n                continue\n\n            label = creator.label\n            failed = False\n            add_traceback = False\n            exc_info = None\n            try:\n                creator.remove_instances(creator_instances)\n\n            except CreatorError:\n                failed = True\n                exc_info = sys.exc_info()\n                self.log.warning(\n                    error_message.format(identifier, exc_info[1])\n                )\n\n            except (KeyboardInterrupt, SystemExit):\n                raise\n\n            except:  # noqa: E722\n                failed = True\n                add_traceback = True\n                exc_info = sys.exc_info()\n                self.log.warning(\n                    error_message.format(identifier, \"\"),\n                    exc_info=True\n                )\n\n            if failed:\n                failed_info.append(\n                    prepare_failed_creator_operation_info(\n                        identifier, label, exc_info, add_traceback\n                    )\n                )\n\n    if failed_info:\n        raise CreatorsRemoveFailed(failed_info)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.reset","title":"<code>reset(discover_publish_plugins=True)</code>","text":"<p>Reset context with all plugins and instances.</p> <p>All changes will be lost if were not saved explicitely.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def reset(self, discover_publish_plugins: bool = True) -&gt; None:\n    \"\"\"Reset context with all plugins and instances.\n\n    All changes will be lost if were not saved explicitely.\n    \"\"\"\n\n    self.reset_preparation()\n\n    self.reset_current_context()\n    self.reset_plugins(discover_publish_plugins)\n    self.reset_context_data()\n\n    with self.bulk_add_instances():\n        self.reset_instances()\n        self.find_convertor_items()\n        self.execute_autocreators()\n\n    self.reset_finalization()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.reset_context_data","title":"<code>reset_context_data()</code>","text":"<p>Reload context data using host implementation.</p> <p>These data are not related to any instance but may be needed for whole publishing.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def reset_context_data(self) -&gt; None:\n    \"\"\"Reload context data using host implementation.\n\n    These data are not related to any instance but may be needed for whole\n    publishing.\n    \"\"\"\n    if not self.host_is_valid:\n        self._original_context_data = {}\n        self._publish_attributes = PublishAttributes(self, {})\n        return\n\n    original_data = self.host.get_context_data() or {}\n    self._original_context_data = copy.deepcopy(original_data)\n\n    publish_attributes = original_data.get(\"publish_attributes\") or {}\n\n    self._publish_attributes = PublishAttributes(\n        self, publish_attributes\n    )\n\n    for plugin in self.plugins_with_defs:\n        if is_func_signature_supported(\n            plugin.convert_attribute_values, self, None\n        ):\n            plugin.convert_attribute_values(self, None)\n\n        elif not plugin.__instanceEnabled__:\n            output = plugin.convert_attribute_values(publish_attributes)\n            if output:\n                publish_attributes.update(output)\n\n    for plugin in self.plugins_with_defs:\n        try:\n            attr_defs = plugin.get_attr_defs_for_context(self)\n        except Exception:\n            self.log.error(\n                \"Failed to get attribute definitions\"\n                f\" from plugin '{plugin.__name__}'.\",\n                exc_info=True\n            )\n            continue\n\n        if not attr_defs:\n            continue\n        self._publish_attributes.set_publish_plugin_attr_defs(\n            plugin.__name__, attr_defs\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.reset_current_context","title":"<code>reset_current_context()</code>","text":"<p>Refresh current context.</p> <p>Reset is based on optional host implementation of <code>get_current_context</code> function.</p> <p>Some hosts have ability to change context file without using workfiles tool but that change is not propagated to 'os.environ'.</p> Todos <p>UI: Current context should be also checked on save - compare     initial values vs. current values. Related to UI checks: Current workfile can be also considered     as current context information as that's where the metadata     are stored. We should store the workfile (if is available) too.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def reset_current_context(self) -&gt; None:\n    \"\"\"Refresh current context.\n\n    Reset is based on optional host implementation of `get_current_context`\n    function.\n\n    Some hosts have ability to change context file without using workfiles\n    tool but that change is not propagated to 'os.environ'.\n\n    Todos:\n        UI: Current context should be also checked on save - compare\n            initial values vs. current values.\n        Related to UI checks: Current workfile can be also considered\n            as current context information as that's where the metadata\n            are stored. We should store the workfile (if is available) too.\n    \"\"\"\n\n    project_name, folder_path, task_name, workfile_path = (\n        self._get_current_host_context()\n    )\n\n    self._current_project_name = project_name\n    self._current_folder_path = folder_path\n    self._current_task_name = task_name\n    self._current_workfile_path = workfile_path\n\n    self._current_project_entity = _NOT_SET\n    self._current_folder_entity = _NOT_SET\n    self._current_task_entity = _NOT_SET\n    self._current_task_type = _NOT_SET\n\n    self._current_project_anatomy = None\n    self._current_project_settings = None\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.reset_finalization","title":"<code>reset_finalization()</code>","text":"<p>Cleanup of attributes after reset.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def reset_finalization(self) -&gt; None:\n    \"\"\"Cleanup of attributes after reset.\"\"\"\n\n    # Stop access to collection shared data\n    self._collection_shared_data = None\n    self.refresh_thumbnails()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.reset_instances","title":"<code>reset_instances()</code>","text":"<p>Reload instances</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def reset_instances(self) -&gt; None:\n    \"\"\"Reload instances\"\"\"\n    self._instances_by_id = collections.OrderedDict()\n\n    # Collect instances\n    error_message = \"Collection of instances for creator {} failed. {}\"\n    failed_info = []\n    for creator in self.sorted_creators:\n        label = creator.label\n        identifier = creator.identifier\n        failed = False\n        add_traceback = False\n        exc_info = None\n        try:\n            creator.collect_instances()\n\n        except CreatorError:\n            failed = True\n            exc_info = sys.exc_info()\n            self.log.warning(error_message.format(identifier, exc_info[1]))\n\n        except:  # noqa: E722\n            failed = True\n            add_traceback = True\n            exc_info = sys.exc_info()\n            self.log.warning(\n                error_message.format(identifier, \"\"),\n                exc_info=True\n            )\n\n        if failed:\n            failed_info.append(\n                prepare_failed_creator_operation_info(\n                    identifier, label, exc_info, add_traceback\n                )\n            )\n\n    if failed_info:\n        raise CreatorsCollectionFailed(failed_info)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.reset_plugins","title":"<code>reset_plugins(discover_publish_plugins=True)</code>","text":"<p>Reload plugins.</p> <p>Reloads creators from preregistered paths and can load publish plugins if it's enabled on context.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def reset_plugins(self, discover_publish_plugins: bool = True) -&gt; None:\n    \"\"\"Reload plugins.\n\n    Reloads creators from preregistered paths and can load publish plugins\n    if it's enabled on context.\n    \"\"\"\n\n    self._reset_publish_plugins(discover_publish_plugins)\n    self._reset_creator_plugins()\n    self._reset_convertor_plugins()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.reset_preparation","title":"<code>reset_preparation()</code>","text":"<p>Prepare attributes that must be prepared/cleaned before reset.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def reset_preparation(self) -&gt; None:\n    \"\"\"Prepare attributes that must be prepared/cleaned before reset.\"\"\"\n\n    # Give ability to store shared data for collection phase\n    self._collection_shared_data = {}\n\n    self._folder_entities_by_path = {}\n    self._task_entities_by_id = {}\n\n    self._task_ids_by_folder_path = {}\n    self._task_names_by_folder_path = {}\n\n    self._event_hub.clear_callbacks()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.run_convertor","title":"<code>run_convertor(convertor_identifier)</code>","text":"<p>Run convertor plugin by identifier.</p> <p>Conversion is skipped if convertor is not available.</p> <p>Parameters:</p> Name Type Description Default <code>convertor_identifier</code> <code>str</code> <p>Identifier of convertor.</p> required Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def run_convertor(self, convertor_identifier: str) -&gt; None:\n    \"\"\"Run convertor plugin by identifier.\n\n    Conversion is skipped if convertor is not available.\n\n    Args:\n        convertor_identifier (str): Identifier of convertor.\n    \"\"\"\n\n    convertor = self.convertors_plugins.get(convertor_identifier)\n    if convertor is not None:\n        convertor.convert()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.run_convertors","title":"<code>run_convertors(convertor_identifiers)</code>","text":"<p>Run convertor plugins by identifiers.</p> <p>Conversion is skipped if convertor is not available. It is recommended to trigger reset after conversion to reload instances.</p> <p>Parameters:</p> Name Type Description Default <code>convertor_identifiers</code> <code>Iterable[str]</code> <p>Identifiers of convertors to run.</p> required <p>Raises:</p> Type Description <code>ConvertorsConversionFailed</code> <p>When one or more convertors fails.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def run_convertors(self, convertor_identifiers: Iterable[str]) -&gt; None:\n    \"\"\"Run convertor plugins by identifiers.\n\n    Conversion is skipped if convertor is not available. It is recommended\n    to trigger reset after conversion to reload instances.\n\n    Args:\n        convertor_identifiers (Iterable[str]): Identifiers of convertors\n            to run.\n\n    Raises:\n        ConvertorsConversionFailed: When one or more convertors fails.\n    \"\"\"\n\n    failed_info = []\n    for convertor_identifier in convertor_identifiers:\n        try:\n            self.run_convertor(convertor_identifier)\n\n        except:  # noqa: E722\n            failed_info.append(\n                prepare_failed_convertor_operation_info(\n                    convertor_identifier, sys.exc_info()\n                )\n            )\n            self.log.warning(\n                \"Failed to convert instances of convertor \\\"{}\\\"\".format(\n                    convertor_identifier\n                ),\n                exc_info=True\n            )\n\n    if failed_info:\n        raise ConvertorsConversionFailed(failed_info)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.save_changes","title":"<code>save_changes()</code>","text":"<p>Save changes. Update all changed values.</p> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def save_changes(self) -&gt; None:\n    \"\"\"Save changes. Update all changed values.\"\"\"\n    if not self.host_is_valid:\n        missing_methods = self.get_host_misssing_methods(self.host)\n        raise HostMissRequiredMethod(self.host, missing_methods)\n\n    self._save_context_changes()\n    self._save_instance_changes()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.set_context_publish_plugin_attr_defs","title":"<code>set_context_publish_plugin_attr_defs(plugin_name, attr_defs)</code>","text":"<p>Set attribute definitions for CreateContext publish plugin.</p> <p>Parameters:</p> Name Type Description Default <code>plugin_name</code> <code>str</code> <p>Name of publish plugin.</p> required <code>attr_defs</code> <code>list[AbstractAttrDef]</code> <p>Attribute definitions.</p> required Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def set_context_publish_plugin_attr_defs(\n    self, plugin_name: str, attr_defs: list[\"AbstractAttrDef\"]\n) -&gt; None:\n    \"\"\"Set attribute definitions for CreateContext publish plugin.\n\n    Args:\n        plugin_name(str): Name of publish plugin.\n        attr_defs(list[AbstractAttrDef]): Attribute definitions.\n\n    \"\"\"\n    self.publish_attributes.set_publish_plugin_attr_defs(\n        plugin_name, attr_defs\n    )\n    self.instance_publish_attr_defs_changed(\n        None, plugin_name\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.trigger_create_button_callback","title":"<code>trigger_create_button_callback(button_name, instance_ids, *, identifier=None)</code>","text":"<p>Trigger pre-create button callback.</p> <p>Parameters:</p> Name Type Description Default <code>button_name</code> <code>str</code> <p>Button name.</p> required <code>instance_ids</code> <code>list[str]</code> <p>List of instance ids for which the button will be triggered. The button will be triggered only for instances that have the button.</p> required <code>identifier</code> <code>str | None</code> <p>Creator identifier. If None, the button will be triggered for all creators.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def trigger_create_button_callback(\n    self,\n    button_name: str,\n    instance_ids: list[str],\n    *,\n    identifier: str | None = None,\n) -&gt; None:\n    \"\"\"Trigger pre-create button callback.\n\n    Args:\n        button_name (str): Button name.\n        instance_ids (list[str]): List of instance ids for which\n            the button will be triggered. The button will be triggered\n            only for instances that have the button.\n        identifier (str | None): Creator identifier. If None,\n            the button will be triggered for all creators.\n\n    \"\"\"\n    info_by_identifier = {}\n    for instance_id in instance_ids:\n        instance = self.instances_by_id.get(instance_id)\n        if instance is None:\n            continue\n\n        plugin_id = instance.creator_identifier\n        if identifier is not None and plugin_id != identifier:\n            continue\n\n        attr_def = instance.creator_attributes.get_attr_def(button_name)\n        if not isinstance(attr_def, ButtonDef):\n            continue\n\n        info = info_by_identifier.get(plugin_id)\n        if info is None:\n            info = {\n                \"attr_def\": attr_def,\n                \"instance_ids\": []\n            }\n            info_by_identifier[plugin_id] = info\n\n        info[\"instance_ids\"].append(instance_id)\n\n    for info in info_by_identifier.values():\n        attr_def = info[\"attr_def\"]\n\n        callback_info = ButtonCallbackInfo(\n            create_context=self,\n            instance_ids=info[\"instance_ids\"],\n        )\n        callback = attr_def.get_callback()\n        if is_func_signature_supported(callback, callback_info):\n            callback(callback_info)\n        else:\n            callback()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.trigger_pre_create_button_callback","title":"<code>trigger_pre_create_button_callback(identifier, button_name)</code>","text":"<p>Trigger pre-create button callback.</p> <p>Parameters:</p> Name Type Description Default <code>identifier</code> <code>str</code> <p>Creator identifier.</p> required <code>button_name</code> <code>str</code> <p>Button name.</p> required Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def trigger_pre_create_button_callback(\n    self, identifier: str, button_name: str\n) -&gt; None:\n    \"\"\"Trigger pre-create button callback.\n\n    Args:\n        identifier (str): Creator identifier.\n        button_name (str): Button name.\n\n    Raises:\n        ValueError if create plugin was not found, does not support\n            pre-create attributes, or button was not found.\n\n    \"\"\"\n    create_plugin = self.creators.get(identifier)\n    if create_plugin is None:\n        return\n\n    if not hasattr(create_plugin, \"get_pre_create_attr_defs\"):\n        return\n\n    pre_create_attributes = create_plugin.get_pre_create_attr_defs()\n    for attr_def in pre_create_attributes:\n        if attr_def.key == button_name:\n            attr_def.trigger()\n            return\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/context.html#client.ayon_core.pipeline.create.context.CreateContext.trigger_publish_button_callback","title":"<code>trigger_publish_button_callback(plugin_name, button_name, instance_ids)</code>","text":"<p>Trigger the publish plugin button callback.</p> <p>Parameters:</p> Name Type Description Default <code>plugin_name</code> <code>str</code> <p>Plugin name.</p> required <code>button_name</code> <code>str</code> <p>Button name.</p> required <code>instance_ids</code> <code>list[str | None]</code> <p>List of instance ids for which the button will be triggered. The button will be triggered only for instances that have the button. 'None' is used for context selection.</p> required Source code in <code>client/ayon_core/pipeline/create/context.py</code> <pre><code>def trigger_publish_button_callback(\n    self,\n    plugin_name: str,\n    button_name: str,\n    instance_ids: list[str | None],\n) -&gt; None:\n    \"\"\"Trigger the publish plugin button callback.\n\n    Args:\n        plugin_name (str): Plugin name.\n        button_name (str): Button name.\n        instance_ids (list[str | None]): List of instance ids for which\n            the button will be triggered. The button will be triggered\n            only for instances that have the button. 'None' is used for\n            context selection.\n\n    \"\"\"\n    filtered_instance_ids = []\n    attr_def = None\n    for instance_id in instance_ids:\n        if instance_id is None:\n            publish_attributes = self.publish_attributes\n        else:\n            instance = self.get_instance_by_id(instance_id)\n            if instance is None:\n                continue\n            publish_attributes = instance.publish_attributes\n\n        plugin_attributes = publish_attributes.get(plugin_name)\n        if not plugin_attributes:\n            continue\n\n        _attr_def = plugin_attributes.get_attr_def(button_name)\n        if not isinstance(_attr_def, ButtonDef):\n            continue\n\n        if attr_def is None:\n            attr_def = _attr_def\n\n        filtered_instance_ids.append(instance_id)\n\n    if attr_def is None:\n        return\n\n    info = ButtonCallbackInfo(\n        create_context=self,\n        instance_ids=filtered_instance_ids,\n    )\n    callback = attr_def.get_callback()\n    if is_func_signature_supported(callback, info):\n        callback(info)\n    else:\n        callback()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/creator_plugins.html","title":"creator_plugins","text":"<p>Creator plugins for the create process.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/creator_plugins.html#client.ayon_core.pipeline.create.creator_plugins.AutoCreator","title":"<code>AutoCreator</code>","text":"<p>               Bases: <code>BaseCreator</code></p> <p>Creator which is automatically triggered without user interaction.</p> <p>Can be used e.g. for <code>workfile</code>.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>class AutoCreator(BaseCreator):\n    \"\"\"Creator which is automatically triggered without user interaction.\n\n    Can be used e.g. for `workfile`.\n    \"\"\"\n    skip_discovery = True\n\n    def remove_instances(self, instances: list[CreatedInstance]) -&gt; None:\n        \"\"\"Skip removal.\"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/creator_plugins.html#client.ayon_core.pipeline.create.creator_plugins.AutoCreator.remove_instances","title":"<code>remove_instances(instances)</code>","text":"<p>Skip removal.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def remove_instances(self, instances: list[CreatedInstance]) -&gt; None:\n    \"\"\"Skip removal.\"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/creator_plugins.html#client.ayon_core.pipeline.create.creator_plugins.BaseCreator","title":"<code>BaseCreator</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Plugin that create and modify instance data before publishing process.</p> <p>Single object should be used for multiple instances instead of single instance per one creator object. Do not store temp data or mid-process data to <code>self</code> if it's not Plugin specific.</p> <p>Parameters:</p> Name Type Description Default <code>project_settings</code> <code>dict[str, Any]</code> <p>Project settings.</p> required <code>create_context</code> <code>CreateContext</code> <p>Context which initialized creator.</p> required <code>headless</code> <code>bool</code> <p>Running in headless mode.</p> <code>False</code> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>class BaseCreator(ABC):\n    \"\"\"Plugin that create and modify instance data before publishing process.\n\n    Single object should be used for multiple instances instead of single\n    instance per one creator object. Do not store temp data or mid-process data\n    to `self` if it's not Plugin specific.\n\n    Args:\n        project_settings (dict[str, Any]): Project settings.\n        create_context (CreateContext): Context which initialized creator.\n        headless (bool): Running in headless mode.\n\n    \"\"\"\n    # Attribute 'skip_discovery' is used during discovery phase to skip\n    #   plugins, which can be used to mark base plugins that should not be\n    #   considered as plugins \"to use\". The discovery logic does NOT use\n    #   the attribute value from parent classes. Each base class has to define\n    #   the attribute again.\n    skip_discovery: bool = True\n\n    # Label shown in UI\n    label: str | None = None\n    group_label: str | None = None\n    # Cached group label after first call 'get_group_label'\n    _cached_group_label: str | None = None\n\n    # Order in which will be plugin executed (collect &amp; update instances)\n    #   less == earlier -&gt; Order '90' will be processed before '100'\n    order: int = 100\n\n    # Variable to store logger\n    _log: logging.Logger | None = None\n\n    # Creator is enabled (Probably does not have reason of existence?)\n    enabled: bool = True\n\n    # Creator (and product base type) icon\n    # - may not be used if `get_icon` is reimplemented\n    icon: IconBase | str | None = None\n\n    # Instance attribute definitions that can be changed per instance\n    # - returns list of attribute definitions from\n    #       `ayon_core.lib.attribute_definitions`\n    instance_attr_defs: list[AbstractAttrDef] = []\n\n    # Filtering by host name - can be used to be filtered by host name\n    # - used on all hosts when set to 'None' for Backwards compatibility\n    #   - was added afterwards\n    # QUESTION make this required?\n    host_name: str | None = None\n\n    # Settings auto-apply helpers\n    # Root key in project settings (mandatory for auto-apply to work)\n    settings_category: str | None = None\n    # Name of plugin in create settings &gt; class name is used if not set\n    settings_name: str | None = None\n\n    product_type_items: list[ProductTypeItem] = []\n\n    def __init__(\n        self,\n        project_settings: dict[str, Any],\n        create_context: CreateContext,\n        headless: bool = False\n    ) -&gt; None:\n        # Reference to CreateContext\n        self.create_context: CreateContext = create_context\n        self.project_settings = project_settings\n\n        # Creator is running in headless mode (without UI elements)\n        # - we may use UI inside processing this attribute should be checked\n        self.headless: bool = headless\n\n        self.apply_settings(project_settings)\n        self.register_callbacks()\n\n    @staticmethod\n    def _get_settings_values(\n        project_settings: dict[str, Any],\n        category_name: str,\n        plugin_name: str\n    ) -&gt; dict[str, Any] | None:\n        \"\"\"Helper method to get settings values.\n\n        Args:\n            project_settings (dict[str, Any]): Project settings.\n            category_name (str): Category of settings.\n            plugin_name (str): Name of settings.\n\n        Returns:\n            dict[str, Any] | None: Settings values or None.\n\n        \"\"\"\n        settings = project_settings.get(category_name)\n        if not settings:\n            return None\n\n        create_settings = settings.get(\"create\")\n        if not create_settings:\n            return None\n\n        return create_settings.get(plugin_name)\n\n    def apply_settings(self, project_settings: dict[str, Any]) -&gt; None:\n        \"\"\"Method called on initialization of plugin to apply settings.\n\n        Default implementation tries to auto-apply settings values if are\n            in expected hierarchy.\n\n        Data hierarchy to auto-apply settings:\n            \u251c\u2500 {self.settings_category}                 - Root key in settings\n            \u2502 \u2514\u2500 \"create\"                               - Hardcoded key\n            \u2502   \u2514\u2500 {self.settings_name} | {class name}  - Name of plugin\n            \u2502     \u251c\u2500 ... attribute values...            - Attribute/value pair\n\n        It is mandatory to define 'settings_category' attribute. Attribute\n        'settings_name' is optional and class name is used if is not defined.\n\n        Example data:\n            ProjectSettings {\n                \"maya\": {                    # self.settings_category\n                    \"create\": {              # Hardcoded key\n                        \"CreateAnimation\": { # self.settings_name / class name\n                            \"enabled\": True, # --- Attributes to set ---\n                            \"optional\": True,#\n                            \"active\": True,  #\n                            \"fps\": 25,       # -------------------------\n                        },\n                        ...\n                    },\n                    ...\n                },\n                ...\n            }\n\n        Args:\n            project_settings (dict[str, Any]): Project settings.\n        \"\"\"\n\n        settings_category = self.settings_category\n        if not settings_category:\n            return\n\n        cls_name = self.__class__.__name__\n        settings_name = self.settings_name or cls_name\n\n        settings = self._get_settings_values(\n            project_settings, settings_category, settings_name\n        )\n        if settings is None:\n            self.log.debug(f\"No settings found for {cls_name}\")\n            return\n\n        for key, value in settings.items():\n            # Log out attributes that are not defined on plugin object\n            # - those may be potential dangerous typos in settings\n            if not hasattr(self, key):\n                self.log.debug(\n                    \"Applying settings to unknown attribute '%s' on '%s'.\",\n                    key, cls_name\n                )\n            setattr(self, key, value)\n\n        self.product_type_items = self._convert_product_type_items(\n            self.product_type_items\n        )\n\n    def _convert_product_type_items(\n        self, product_type_items: list\n    ) -&gt; list[ProductTypeItem]:\n        \"\"\"Helper method to convert product type items from settings.\"\"\"\n        if not product_type_items:\n            return []\n\n        first_item = product_type_items[0]\n        if isinstance(first_item, ProductTypeItem):\n            return product_type_items\n\n        if not isinstance(first_item, dict):\n            self.log.warning(\n                f\"Invalid product type item. Expected 'dict' or\"\n                f\" 'ProductTypeItem', got '{type(first_item)}'.\"\n            )\n            return []\n\n        try:\n            return [\n                ProductTypeItem.from_data(item)\n                for item in self.product_type_items\n            ]\n        except Exception:\n            self.log.warning(\n                \"Failed to convert product type items\"\n                \" to ProductTypeItem instances\"\n            )\n            return []\n\n    def register_callbacks(self) -&gt; None:\n        \"\"\"Register callbacks for creator.\n\n        Default implementation does nothing. It can be overridden to register\n        callbacks for creator.\n        \"\"\"\n\n    @property\n    def identifier(self) -&gt; str:\n        \"\"\"Identifier of creator (must be unique).\n\n        Default implementation returns plugin's product base type,\n        or falls back to product type if product base type is not set.\n\n        \"\"\"\n        identifier = self.product_base_type\n        if not identifier:\n            identifier = self.product_type\n        return identifier\n\n    @property\n    @abstractmethod\n    def product_type(self) -&gt; str:\n        \"\"\"Family that plugin represents.\n\n        DEPRECATED - use 'product_base_type' instead. This is kept for\n            backwards compatibility and should be removed when\n            'product_base_type' is required.\n\n        \"\"\"\n\n    @property\n    def product_base_type(self) -&gt; str | None:\n        \"\"\"Product base type that plugin represents.\n\n        Todo (antirotor): This should be required in future - it\n            should be made abstract then.\n\n        Returns:\n            str | None: Product base type that plugin represents.\n                If not set, it is assumed that the creator plugin is obsolete\n                and does not support product base type.\n\n        \"\"\"\n        return None\n\n    @property\n    def project_name(self) -&gt; str:\n        \"\"\"Current project name.\n\n        Returns:\n            str: Name of a project.\n\n        \"\"\"\n        return self.create_context.project_name\n\n    @property\n    def project_anatomy(self) -&gt; Anatomy:\n        \"\"\"Current project anatomy.\n\n        Returns:\n            Anatomy: Project anatomy object.\n\n        \"\"\"\n        return self.create_context.project_anatomy\n\n    @property\n    def host(self) -&gt; IPublishHost:\n        return self.create_context.host\n\n    def get_group_label(self) -&gt; str:\n        \"\"\"Group label under which are instances grouped in UI.\n\n        Default implementation use attributes in this order:\n            - 'group_label' -&gt; 'label' -&gt; 'identifier'\n\n        Keep in mind that 'identifier' uses 'product_base_type' by default.\n\n        Returns:\n            str: Group label that can be used for grouping of instances in UI.\n                Group label can be overridden by the instance itself.\n\n        \"\"\"\n        if self._cached_group_label is None:\n            label = self.identifier\n            if self.group_label:\n                label = self.group_label\n            elif self.label:\n                label = self.label\n            self._cached_group_label = label\n        return self._cached_group_label\n\n    @property\n    def log(self) -&gt; logging.Logger:\n        \"\"\"Logger of the plugin.\n\n        Returns:\n            logging.Logger: Logger with name of the plugin.\n\n        \"\"\"\n        if self._log is None:\n            self._log = Logger.get_logger(self.__class__.__name__)\n        return self._log\n\n    def _create_instance(\n        self,\n        product_name: str,\n        data: dict[str, Any],\n        product_type: str | None = None,\n        product_base_type: str | None = None\n    ) -&gt; CreatedInstance:\n        \"\"\"Create instance and add instance to context.\n\n        Args:\n            product_name (str): Product name.\n            data (dict[str, Any]): Instance data.\n            product_type (str | None): Product type, object attribute\n                'product_type' is used if not passed.\n            product_base_type (str | None): Product base type, object\n                attribute 'product_base_type' is used if not passed.\n\n        Returns:\n            CreatedInstance: Created instance.\n\n        \"\"\"\n        if not product_base_type:\n            product_base_type = self.product_base_type\n\n        if not product_base_type:\n            product_base_type = self.product_type\n\n        if product_type is None:\n            product_type = product_base_type\n\n        instance = CreatedInstance(\n            product_type=product_type,\n            product_name=product_name,\n            data=data,\n            creator=self,\n            product_base_type=product_base_type,\n        )\n        self._add_instance_to_context(instance)\n        return instance\n\n    def _add_instance_to_context(\n        self, instance: CreatedInstance\n    ) -&gt; None:\n        \"\"\"Helper method to add instance to create context.\n\n        Instances should be stored to DCC workfile metadata to be able reload\n        them and also stored to CreateContext in which is creator plugin\n        existing at the moment to be able use it without refresh of\n        CreateContext.\n\n        Args:\n            instance (CreatedInstance): New created instance.\n\n        \"\"\"\n        self.create_context.creator_adds_instance(instance)\n\n    def _remove_instance_from_context(\n        self, instance: CreatedInstance\n    ) -&gt; None:\n        \"\"\"Helper method to remove instance from create context.\n\n        Instances must be removed from DCC workfile metadat aand from create\n        context in which plugin is existing at the moment of removal to\n        propagate the change without restarting create context.\n\n        Args:\n            instance (CreatedInstance): Instance which should be removed.\n\n        \"\"\"\n        self.create_context.creator_removed_instance(instance)\n\n    @abstractmethod\n    def create(self):\n        \"\"\"Trigger creation logic usually to create new instance/s.\"\"\"\n\n    @abstractmethod\n    def collect_instances(self) -&gt; None:\n        \"\"\"Collect existing instances related to this creator plugin.\n\n        The implementation differs on host abilities. The creator has to\n        collect metadata about instance and create 'CreatedInstance' object\n        which should be added to 'CreateContext'.\n\n        Example:\n        ```python\n        def collect_instances(self):\n            # Getting existing instances is different per host implementation\n            for instance_data in pipeline.list_instances():\n                # Process only instances that were created by this creator\n                creator_id = instance_data.get(\"creator_identifier\")\n                if creator_id == self.identifier:\n                    # Create instance object from existing data\n                    instance = CreatedInstance.from_existing(\n                        instance_data, self\n                    )\n                    # Add instance to create context\n                    self._add_instance_to_context(instance)\n        ```\n        \"\"\"\n\n    @abstractmethod\n    def update_instances(\n        self, update_list: list[UpdateData]\n    ) -&gt; None:\n        \"\"\"Store changes of existing instances so they can be recollected.\n\n        Args:\n            update_list (list[UpdateData]): Gets list of tuples. Each item\n                contain changed instance and it's changes.\n        \"\"\"\n\n    @abstractmethod\n    def remove_instances(\n        self, instances: list[CreatedInstance]\n    ) -&gt; None:\n        \"\"\"Method called on instance removal.\n\n        Can also remove instance metadata from context but should return\n        'True' if did so.\n\n        Args:\n            instances (list[CreatedInstance]): Instance objects which should be\n                removed.\n        \"\"\"\n\n    def get_icon(self) -&gt; IconBase | str | None:\n        \"\"\"Icon of creator (product base type).\n\n        Can return path to image file or awesome icon name.\n        \"\"\"\n        return self.icon\n\n    def get_dynamic_data(\n        self,\n        project_name: str,\n        folder_entity: dict[str, Any] | None,\n        task_entity: dict[str, Any] | None,\n        variant: str,\n        host_name: str,\n        instance: CreatedInstance | None = None,\n        project_entity: dict[str, Any] | None = None,\n        product_type: str | None = None,\n    ) -&gt; dict[str, Any]:\n        \"\"\"Dynamic data for product name filling.\n\n        These may be dynamically created based on current context of workfile.\n\n        Default implementation will always return empty dictionary.\n\n        \"\"\"\n        return {}\n\n    def get_product_name(\n        self,\n        project_name: str,\n        folder_entity: dict[str, Any],\n        task_entity: dict[str, Any] | None,\n        variant: str,\n        host_name: str | None = None,\n        instance: CreatedInstance | None = None,\n        project_entity: dict[str, Any] | None = None,\n        product_type: str | None = None,\n    ) -&gt; str:\n        \"\"\"Return product name for passed context.\n\n        Method is also called on product name update. In that case origin\n        instance is passed in.\n\n        Args:\n            project_name (str): Project name.\n            folder_entity (dict): Folder entity.\n            task_entity (dict | None): Task entity.\n            variant (str): Product name variant. In most of cases user input.\n            host_name (str | None): Which host creates product. Defaults\n                to host name on create context.\n            instance (CreatedInstance | None): Object of 'CreatedInstance'\n                for which is product name updated. Passed only on product name\n                update.\n            project_entity (dict[str, Any] | None): Project entity.\n            product_type (str | None): Product type.\n\n        Returns:\n            str: Product name for passed context.\n\n        \"\"\"\n        if host_name is None:\n            host_name = self.create_context.host_name\n\n        # Backwards compatibility for create plugins that don't implement\n        #   'product_base_type'.\n        # TODO Remove when 'product_base_type' is required\n        product_base_type = self.product_base_type\n        if not product_base_type:\n            product_base_type = self.product_type\n\n        if product_type is None:\n            for product_type_item in self.get_product_type_items():\n                product_type = product_type_item.product_type\n                break\n            else:\n                product_type = product_base_type\n\n        cur_project_name = self.create_context.get_current_project_name()\n        if not project_entity and project_name == cur_project_name:\n            project_entity = self.create_context.get_current_project_entity()\n\n        args = (\n            project_name,\n            folder_entity,\n            task_entity,\n            variant,\n            host_name,\n        )\n        kwargs = dict(\n            instance=instance,\n            project_entity=project_entity,\n            product_type=product_type,\n        )\n        # NOTE 'project_entity' and 'product_type' were added at the same time\n        #   26/01/19\n        if not is_func_signature_supported(\n            self.get_dynamic_data, *args, **kwargs\n        ):\n            kwargs.pop(\"project_entity\")\n            kwargs.pop(\"product_type\")\n\n        dynamic_data = self.get_dynamic_data(*args, **kwargs)\n\n        return get_product_name(\n            project_name,\n            folder_entity=folder_entity,\n            task_entity=task_entity,\n            product_base_type=product_base_type,\n            product_type=product_type,\n            host_name=host_name,\n            variant=variant,\n            dynamic_data=dynamic_data,\n            project_settings=self.project_settings,\n            project_entity=project_entity,\n        )\n\n    def get_instance_attr_defs(self) -&gt; list[AbstractAttrDef]:\n        \"\"\"Plugin attribute definitions.\n\n        Attribute definitions of plugin that hold data about created instance\n        and values are stored to metadata for future usage and for publishing\n        purposes.\n\n        Note:\n            Convert method should be implemented which should care about\n            updating keys/values when plugin attributes change.\n\n        Returns:\n            list[AbstractAttrDef]: Attribute definitions that can be tweaked\n                for created instance.\n\n        \"\"\"\n        return self.instance_attr_defs\n\n    def get_attr_defs_for_instance(\n        self, instance: CreatedInstance\n    ) -&gt; list[AbstractAttrDef]:\n        \"\"\"Get attribute definitions for an instance.\n\n        Args:\n            instance (CreatedInstance): Instance for which to get\n                attribute definitions.\n\n        \"\"\"\n        return self.get_instance_attr_defs()\n\n    @property\n    def collection_shared_data(self) -&gt; dict[str, Any]:\n        \"\"\"Access to shared data that can be used during creator's collection.\n\n        Returns:\n            dict[str, Any]: Shared data.\n\n        Raises:\n            UnavailableSharedData: When called out of collection phase.\n\n        \"\"\"\n        return self.create_context.collection_shared_data\n\n    def set_instance_thumbnail_path(\n        self,\n        instance_id: str,\n        thumbnail_path: str | None = None,\n    ) -&gt; None:\n        \"\"\"Set path to thumbnail for instance.\"\"\"\n\n        self.create_context.thumbnail_paths_by_instance_id[instance_id] = (\n            thumbnail_path\n        )\n\n    def get_next_versions_for_instances(\n        self, instances: list[CreatedInstance]\n    ) -&gt; dict[str, int]:\n        \"\"\"Prepare next versions for instances.\n\n        This is helper method to receive next possible versions for instances.\n        It is using context information on instance to receive them,\n        'folderPath' and 'product'.\n\n        Output will contain version by each instance id.\n\n        Args:\n            instances (list[CreatedInstance]): Instances for which to get next\n                versions.\n\n        Returns:\n            dict[str, int]: Next versions by instance id.\n        \"\"\"\n\n        return get_next_versions_for_instances(\n            self.create_context.project_name, instances\n        )\n\n    def get_product_type_items(self) -&gt; list[ProductTypeItem]:\n        \"\"\"Get product type the Creator can work with.\n\n        By default, it returns `product_type_items` attribute value that\n        can be set by Creator settings. This can be overridden to provide\n        different source.\n\n        Product type items are list of ProductTypeItem that\n        Creator can create. Label is used in UI to show user-friendly name.\n        This dataclass can be easily expanded with data in the future. New\n        fields must have default values to not break existing implementations.\n\n        Returns:\n            list[ProductTypeItem]: List of product type items.\n\n        \"\"\"\n        return self.product_type_items\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/creator_plugins.html#client.ayon_core.pipeline.create.creator_plugins.BaseCreator.collection_shared_data","title":"<code>collection_shared_data</code>  <code>property</code>","text":"<p>Access to shared data that can be used during creator's collection.</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: Shared data.</p> <p>Raises:</p> Type Description <code>UnavailableSharedData</code> <p>When called out of collection phase.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/creator_plugins.html#client.ayon_core.pipeline.create.creator_plugins.BaseCreator.identifier","title":"<code>identifier</code>  <code>property</code>","text":"<p>Identifier of creator (must be unique).</p> <p>Default implementation returns plugin's product base type, or falls back to product type if product base type is not set.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/creator_plugins.html#client.ayon_core.pipeline.create.creator_plugins.BaseCreator.log","title":"<code>log</code>  <code>property</code>","text":"<p>Logger of the plugin.</p> <p>Returns:</p> Type Description <code>Logger</code> <p>logging.Logger: Logger with name of the plugin.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/creator_plugins.html#client.ayon_core.pipeline.create.creator_plugins.BaseCreator.product_base_type","title":"<code>product_base_type</code>  <code>property</code>","text":"<p>Product base type that plugin represents.</p> <p>Todo (antirotor): This should be required in future - it     should be made abstract then.</p> <p>Returns:</p> Type Description <code>str | None</code> <p>str | None: Product base type that plugin represents. If not set, it is assumed that the creator plugin is obsolete and does not support product base type.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/creator_plugins.html#client.ayon_core.pipeline.create.creator_plugins.BaseCreator.product_type","title":"<code>product_type</code>  <code>abstractmethod</code> <code>property</code>","text":"<p>Family that plugin represents.</p> <p>DEPRECATED - use 'product_base_type' instead. This is kept for     backwards compatibility and should be removed when     'product_base_type' is required.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/creator_plugins.html#client.ayon_core.pipeline.create.creator_plugins.BaseCreator.project_anatomy","title":"<code>project_anatomy</code>  <code>property</code>","text":"<p>Current project anatomy.</p> <p>Returns:</p> Name Type Description <code>Anatomy</code> <code>Anatomy</code> <p>Project anatomy object.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/creator_plugins.html#client.ayon_core.pipeline.create.creator_plugins.BaseCreator.project_name","title":"<code>project_name</code>  <code>property</code>","text":"<p>Current project name.</p> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Name of a project.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/creator_plugins.html#client.ayon_core.pipeline.create.creator_plugins.BaseCreator.apply_settings","title":"<code>apply_settings(project_settings)</code>","text":"<p>Method called on initialization of plugin to apply settings.</p> <p>Default implementation tries to auto-apply settings values if are     in expected hierarchy.</p> Data hierarchy to auto-apply settings <p>\u251c\u2500 {self.settings_category}                 - Root key in settings \u2502 \u2514\u2500 \"create\"                               - Hardcoded key \u2502   \u2514\u2500 {self.settings_name} | {class name}  - Name of plugin \u2502     \u251c\u2500 ... attribute values...            - Attribute/value pair</p> <p>It is mandatory to define 'settings_category' attribute. Attribute 'settings_name' is optional and class name is used if is not defined.</p> Example data <p>ProjectSettings {     \"maya\": {                    # self.settings_category         \"create\": {              # Hardcoded key             \"CreateAnimation\": { # self.settings_name / class name                 \"enabled\": True, # --- Attributes to set ---                 \"optional\": True,#                 \"active\": True,  #                 \"fps\": 25,       # -------------------------             },             ...         },         ...     },     ... }</p> <p>Parameters:</p> Name Type Description Default <code>project_settings</code> <code>dict[str, Any]</code> <p>Project settings.</p> required Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def apply_settings(self, project_settings: dict[str, Any]) -&gt; None:\n    \"\"\"Method called on initialization of plugin to apply settings.\n\n    Default implementation tries to auto-apply settings values if are\n        in expected hierarchy.\n\n    Data hierarchy to auto-apply settings:\n        \u251c\u2500 {self.settings_category}                 - Root key in settings\n        \u2502 \u2514\u2500 \"create\"                               - Hardcoded key\n        \u2502   \u2514\u2500 {self.settings_name} | {class name}  - Name of plugin\n        \u2502     \u251c\u2500 ... attribute values...            - Attribute/value pair\n\n    It is mandatory to define 'settings_category' attribute. Attribute\n    'settings_name' is optional and class name is used if is not defined.\n\n    Example data:\n        ProjectSettings {\n            \"maya\": {                    # self.settings_category\n                \"create\": {              # Hardcoded key\n                    \"CreateAnimation\": { # self.settings_name / class name\n                        \"enabled\": True, # --- Attributes to set ---\n                        \"optional\": True,#\n                        \"active\": True,  #\n                        \"fps\": 25,       # -------------------------\n                    },\n                    ...\n                },\n                ...\n            },\n            ...\n        }\n\n    Args:\n        project_settings (dict[str, Any]): Project settings.\n    \"\"\"\n\n    settings_category = self.settings_category\n    if not settings_category:\n        return\n\n    cls_name = self.__class__.__name__\n    settings_name = self.settings_name or cls_name\n\n    settings = self._get_settings_values(\n        project_settings, settings_category, settings_name\n    )\n    if settings is None:\n        self.log.debug(f\"No settings found for {cls_name}\")\n        return\n\n    for key, value in settings.items():\n        # Log out attributes that are not defined on plugin object\n        # - those may be potential dangerous typos in settings\n        if not hasattr(self, key):\n            self.log.debug(\n                \"Applying settings to unknown attribute '%s' on '%s'.\",\n                key, cls_name\n            )\n        setattr(self, key, value)\n\n    self.product_type_items = self._convert_product_type_items(\n        self.product_type_items\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/creator_plugins.html#client.ayon_core.pipeline.create.creator_plugins.BaseCreator.collect_instances","title":"<code>collect_instances()</code>  <code>abstractmethod</code>","text":"<p>Collect existing instances related to this creator plugin.</p> <p>The implementation differs on host abilities. The creator has to collect metadata about instance and create 'CreatedInstance' object which should be added to 'CreateContext'.</p> <p>Example:</p> <pre><code>def collect_instances(self):\n    # Getting existing instances is different per host implementation\n    for instance_data in pipeline.list_instances():\n        # Process only instances that were created by this creator\n        creator_id = instance_data.get(\"creator_identifier\")\n        if creator_id == self.identifier:\n            # Create instance object from existing data\n            instance = CreatedInstance.from_existing(\n                instance_data, self\n            )\n            # Add instance to create context\n            self._add_instance_to_context(instance)\n</code></pre> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>@abstractmethod\ndef collect_instances(self) -&gt; None:\n    \"\"\"Collect existing instances related to this creator plugin.\n\n    The implementation differs on host abilities. The creator has to\n    collect metadata about instance and create 'CreatedInstance' object\n    which should be added to 'CreateContext'.\n\n    Example:\n    ```python\n    def collect_instances(self):\n        # Getting existing instances is different per host implementation\n        for instance_data in pipeline.list_instances():\n            # Process only instances that were created by this creator\n            creator_id = instance_data.get(\"creator_identifier\")\n            if creator_id == self.identifier:\n                # Create instance object from existing data\n                instance = CreatedInstance.from_existing(\n                    instance_data, self\n                )\n                # Add instance to create context\n                self._add_instance_to_context(instance)\n    ```\n    \"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/creator_plugins.html#client.ayon_core.pipeline.create.creator_plugins.BaseCreator.create","title":"<code>create()</code>  <code>abstractmethod</code>","text":"<p>Trigger creation logic usually to create new instance/s.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>@abstractmethod\ndef create(self):\n    \"\"\"Trigger creation logic usually to create new instance/s.\"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/creator_plugins.html#client.ayon_core.pipeline.create.creator_plugins.BaseCreator.get_attr_defs_for_instance","title":"<code>get_attr_defs_for_instance(instance)</code>","text":"<p>Get attribute definitions for an instance.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>CreatedInstance</code> <p>Instance for which to get attribute definitions.</p> required Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def get_attr_defs_for_instance(\n    self, instance: CreatedInstance\n) -&gt; list[AbstractAttrDef]:\n    \"\"\"Get attribute definitions for an instance.\n\n    Args:\n        instance (CreatedInstance): Instance for which to get\n            attribute definitions.\n\n    \"\"\"\n    return self.get_instance_attr_defs()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/creator_plugins.html#client.ayon_core.pipeline.create.creator_plugins.BaseCreator.get_dynamic_data","title":"<code>get_dynamic_data(project_name, folder_entity, task_entity, variant, host_name, instance=None, project_entity=None, product_type=None)</code>","text":"<p>Dynamic data for product name filling.</p> <p>These may be dynamically created based on current context of workfile.</p> <p>Default implementation will always return empty dictionary.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def get_dynamic_data(\n    self,\n    project_name: str,\n    folder_entity: dict[str, Any] | None,\n    task_entity: dict[str, Any] | None,\n    variant: str,\n    host_name: str,\n    instance: CreatedInstance | None = None,\n    project_entity: dict[str, Any] | None = None,\n    product_type: str | None = None,\n) -&gt; dict[str, Any]:\n    \"\"\"Dynamic data for product name filling.\n\n    These may be dynamically created based on current context of workfile.\n\n    Default implementation will always return empty dictionary.\n\n    \"\"\"\n    return {}\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/creator_plugins.html#client.ayon_core.pipeline.create.creator_plugins.BaseCreator.get_group_label","title":"<code>get_group_label()</code>","text":"<p>Group label under which are instances grouped in UI.</p> Default implementation use attributes in this order <ul> <li>'group_label' -&gt; 'label' -&gt; 'identifier'</li> </ul> <p>Keep in mind that 'identifier' uses 'product_base_type' by default.</p> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Group label that can be used for grouping of instances in UI. Group label can be overridden by the instance itself.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def get_group_label(self) -&gt; str:\n    \"\"\"Group label under which are instances grouped in UI.\n\n    Default implementation use attributes in this order:\n        - 'group_label' -&gt; 'label' -&gt; 'identifier'\n\n    Keep in mind that 'identifier' uses 'product_base_type' by default.\n\n    Returns:\n        str: Group label that can be used for grouping of instances in UI.\n            Group label can be overridden by the instance itself.\n\n    \"\"\"\n    if self._cached_group_label is None:\n        label = self.identifier\n        if self.group_label:\n            label = self.group_label\n        elif self.label:\n            label = self.label\n        self._cached_group_label = label\n    return self._cached_group_label\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/creator_plugins.html#client.ayon_core.pipeline.create.creator_plugins.BaseCreator.get_icon","title":"<code>get_icon()</code>","text":"<p>Icon of creator (product base type).</p> <p>Can return path to image file or awesome icon name.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def get_icon(self) -&gt; IconBase | str | None:\n    \"\"\"Icon of creator (product base type).\n\n    Can return path to image file or awesome icon name.\n    \"\"\"\n    return self.icon\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/creator_plugins.html#client.ayon_core.pipeline.create.creator_plugins.BaseCreator.get_instance_attr_defs","title":"<code>get_instance_attr_defs()</code>","text":"<p>Plugin attribute definitions.</p> <p>Attribute definitions of plugin that hold data about created instance and values are stored to metadata for future usage and for publishing purposes.</p> Note <p>Convert method should be implemented which should care about updating keys/values when plugin attributes change.</p> <p>Returns:</p> Type Description <code>list[AbstractAttrDef]</code> <p>list[AbstractAttrDef]: Attribute definitions that can be tweaked for created instance.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def get_instance_attr_defs(self) -&gt; list[AbstractAttrDef]:\n    \"\"\"Plugin attribute definitions.\n\n    Attribute definitions of plugin that hold data about created instance\n    and values are stored to metadata for future usage and for publishing\n    purposes.\n\n    Note:\n        Convert method should be implemented which should care about\n        updating keys/values when plugin attributes change.\n\n    Returns:\n        list[AbstractAttrDef]: Attribute definitions that can be tweaked\n            for created instance.\n\n    \"\"\"\n    return self.instance_attr_defs\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/creator_plugins.html#client.ayon_core.pipeline.create.creator_plugins.BaseCreator.get_next_versions_for_instances","title":"<code>get_next_versions_for_instances(instances)</code>","text":"<p>Prepare next versions for instances.</p> <p>This is helper method to receive next possible versions for instances. It is using context information on instance to receive them, 'folderPath' and 'product'.</p> <p>Output will contain version by each instance id.</p> <p>Parameters:</p> Name Type Description Default <code>instances</code> <code>list[CreatedInstance]</code> <p>Instances for which to get next versions.</p> required <p>Returns:</p> Type Description <code>dict[str, int]</code> <p>dict[str, int]: Next versions by instance id.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def get_next_versions_for_instances(\n    self, instances: list[CreatedInstance]\n) -&gt; dict[str, int]:\n    \"\"\"Prepare next versions for instances.\n\n    This is helper method to receive next possible versions for instances.\n    It is using context information on instance to receive them,\n    'folderPath' and 'product'.\n\n    Output will contain version by each instance id.\n\n    Args:\n        instances (list[CreatedInstance]): Instances for which to get next\n            versions.\n\n    Returns:\n        dict[str, int]: Next versions by instance id.\n    \"\"\"\n\n    return get_next_versions_for_instances(\n        self.create_context.project_name, instances\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/creator_plugins.html#client.ayon_core.pipeline.create.creator_plugins.BaseCreator.get_product_name","title":"<code>get_product_name(project_name, folder_entity, task_entity, variant, host_name=None, instance=None, project_entity=None, product_type=None)</code>","text":"<p>Return product name for passed context.</p> <p>Method is also called on product name update. In that case origin instance is passed in.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>folder_entity</code> <code>dict</code> <p>Folder entity.</p> required <code>task_entity</code> <code>dict | None</code> <p>Task entity.</p> required <code>variant</code> <code>str</code> <p>Product name variant. In most of cases user input.</p> required <code>host_name</code> <code>str | None</code> <p>Which host creates product. Defaults to host name on create context.</p> <code>None</code> <code>instance</code> <code>CreatedInstance | None</code> <p>Object of 'CreatedInstance' for which is product name updated. Passed only on product name update.</p> <code>None</code> <code>project_entity</code> <code>dict[str, Any] | None</code> <p>Project entity.</p> <code>None</code> <code>product_type</code> <code>str | None</code> <p>Product type.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Product name for passed context.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def get_product_name(\n    self,\n    project_name: str,\n    folder_entity: dict[str, Any],\n    task_entity: dict[str, Any] | None,\n    variant: str,\n    host_name: str | None = None,\n    instance: CreatedInstance | None = None,\n    project_entity: dict[str, Any] | None = None,\n    product_type: str | None = None,\n) -&gt; str:\n    \"\"\"Return product name for passed context.\n\n    Method is also called on product name update. In that case origin\n    instance is passed in.\n\n    Args:\n        project_name (str): Project name.\n        folder_entity (dict): Folder entity.\n        task_entity (dict | None): Task entity.\n        variant (str): Product name variant. In most of cases user input.\n        host_name (str | None): Which host creates product. Defaults\n            to host name on create context.\n        instance (CreatedInstance | None): Object of 'CreatedInstance'\n            for which is product name updated. Passed only on product name\n            update.\n        project_entity (dict[str, Any] | None): Project entity.\n        product_type (str | None): Product type.\n\n    Returns:\n        str: Product name for passed context.\n\n    \"\"\"\n    if host_name is None:\n        host_name = self.create_context.host_name\n\n    # Backwards compatibility for create plugins that don't implement\n    #   'product_base_type'.\n    # TODO Remove when 'product_base_type' is required\n    product_base_type = self.product_base_type\n    if not product_base_type:\n        product_base_type = self.product_type\n\n    if product_type is None:\n        for product_type_item in self.get_product_type_items():\n            product_type = product_type_item.product_type\n            break\n        else:\n            product_type = product_base_type\n\n    cur_project_name = self.create_context.get_current_project_name()\n    if not project_entity and project_name == cur_project_name:\n        project_entity = self.create_context.get_current_project_entity()\n\n    args = (\n        project_name,\n        folder_entity,\n        task_entity,\n        variant,\n        host_name,\n    )\n    kwargs = dict(\n        instance=instance,\n        project_entity=project_entity,\n        product_type=product_type,\n    )\n    # NOTE 'project_entity' and 'product_type' were added at the same time\n    #   26/01/19\n    if not is_func_signature_supported(\n        self.get_dynamic_data, *args, **kwargs\n    ):\n        kwargs.pop(\"project_entity\")\n        kwargs.pop(\"product_type\")\n\n    dynamic_data = self.get_dynamic_data(*args, **kwargs)\n\n    return get_product_name(\n        project_name,\n        folder_entity=folder_entity,\n        task_entity=task_entity,\n        product_base_type=product_base_type,\n        product_type=product_type,\n        host_name=host_name,\n        variant=variant,\n        dynamic_data=dynamic_data,\n        project_settings=self.project_settings,\n        project_entity=project_entity,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/creator_plugins.html#client.ayon_core.pipeline.create.creator_plugins.BaseCreator.get_product_type_items","title":"<code>get_product_type_items()</code>","text":"<p>Get product type the Creator can work with.</p> <p>By default, it returns <code>product_type_items</code> attribute value that can be set by Creator settings. This can be overridden to provide different source.</p> <p>Product type items are list of ProductTypeItem that Creator can create. Label is used in UI to show user-friendly name. This dataclass can be easily expanded with data in the future. New fields must have default values to not break existing implementations.</p> <p>Returns:</p> Type Description <code>list[ProductTypeItem]</code> <p>list[ProductTypeItem]: List of product type items.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def get_product_type_items(self) -&gt; list[ProductTypeItem]:\n    \"\"\"Get product type the Creator can work with.\n\n    By default, it returns `product_type_items` attribute value that\n    can be set by Creator settings. This can be overridden to provide\n    different source.\n\n    Product type items are list of ProductTypeItem that\n    Creator can create. Label is used in UI to show user-friendly name.\n    This dataclass can be easily expanded with data in the future. New\n    fields must have default values to not break existing implementations.\n\n    Returns:\n        list[ProductTypeItem]: List of product type items.\n\n    \"\"\"\n    return self.product_type_items\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/creator_plugins.html#client.ayon_core.pipeline.create.creator_plugins.BaseCreator.register_callbacks","title":"<code>register_callbacks()</code>","text":"<p>Register callbacks for creator.</p> <p>Default implementation does nothing. It can be overridden to register callbacks for creator.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def register_callbacks(self) -&gt; None:\n    \"\"\"Register callbacks for creator.\n\n    Default implementation does nothing. It can be overridden to register\n    callbacks for creator.\n    \"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/creator_plugins.html#client.ayon_core.pipeline.create.creator_plugins.BaseCreator.remove_instances","title":"<code>remove_instances(instances)</code>  <code>abstractmethod</code>","text":"<p>Method called on instance removal.</p> <p>Can also remove instance metadata from context but should return 'True' if did so.</p> <p>Parameters:</p> Name Type Description Default <code>instances</code> <code>list[CreatedInstance]</code> <p>Instance objects which should be removed.</p> required Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>@abstractmethod\ndef remove_instances(\n    self, instances: list[CreatedInstance]\n) -&gt; None:\n    \"\"\"Method called on instance removal.\n\n    Can also remove instance metadata from context but should return\n    'True' if did so.\n\n    Args:\n        instances (list[CreatedInstance]): Instance objects which should be\n            removed.\n    \"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/creator_plugins.html#client.ayon_core.pipeline.create.creator_plugins.BaseCreator.set_instance_thumbnail_path","title":"<code>set_instance_thumbnail_path(instance_id, thumbnail_path=None)</code>","text":"<p>Set path to thumbnail for instance.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def set_instance_thumbnail_path(\n    self,\n    instance_id: str,\n    thumbnail_path: str | None = None,\n) -&gt; None:\n    \"\"\"Set path to thumbnail for instance.\"\"\"\n\n    self.create_context.thumbnail_paths_by_instance_id[instance_id] = (\n        thumbnail_path\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/creator_plugins.html#client.ayon_core.pipeline.create.creator_plugins.BaseCreator.update_instances","title":"<code>update_instances(update_list)</code>  <code>abstractmethod</code>","text":"<p>Store changes of existing instances so they can be recollected.</p> <p>Parameters:</p> Name Type Description Default <code>update_list</code> <code>list[UpdateData]</code> <p>Gets list of tuples. Each item contain changed instance and it's changes.</p> required Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>@abstractmethod\ndef update_instances(\n    self, update_list: list[UpdateData]\n) -&gt; None:\n    \"\"\"Store changes of existing instances so they can be recollected.\n\n    Args:\n        update_list (list[UpdateData]): Gets list of tuples. Each item\n            contain changed instance and it's changes.\n    \"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/creator_plugins.html#client.ayon_core.pipeline.create.creator_plugins.Creator","title":"<code>Creator</code>","text":"<p>               Bases: <code>BaseCreator</code></p> <p>Creator that has more information for artist to show in UI.</p> <p>Creation requires prepared product name and instance data.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>class Creator(BaseCreator):\n    \"\"\"Creator that has more information for artist to show in UI.\n\n    Creation requires prepared product name and instance data.\n    \"\"\"\n    skip_discovery = True\n    # GUI Purposes\n    # - default_variants may not be used if `get_default_variants`\n    #   is overridden\n    default_variants: list[str] = []\n\n    # Default variant used in 'get_default_variant'\n    _default_variant: str | None = None\n\n    # Short description of product base type\n    # - may not be used if `get_description` is overridden\n    description: str | None = None\n\n    # Detailed description of product base type for artists\n    # - may not be used if `get_detail_description` is overridden\n    detailed_description: str | None = None\n\n    # It does make sense to change context on creation\n    # - in some cases it may confuse artists because it would not be used\n    #      e.g. for buld creators\n    create_allow_context_change: bool = True\n    # A thumbnail can be passed in precreate attributes\n    # - if is set to True is should expect that a thumbnail path under key\n    #   PRE_CREATE_THUMBNAIL_KEY can be sent in data with precreate data\n    # - is disabled by default because the feature was added in later stages\n    #   and creators who would not expect PRE_CREATE_THUMBNAIL_KEY could\n    #   cause issues with instance data\n    create_allow_thumbnail: bool = False\n\n    # Precreate attribute definitions showed before creation\n    # - similar to instance attribute definitions\n    pre_create_attr_defs: list[AbstractAttrDef] = []\n\n    def __init__(self, *args, **kwargs):\n        cls = self.__class__\n\n        # Fix backwards compatibility for plugins which override\n        #   'default_variant' attribute directly\n        if not isinstance(cls.default_variant, property):\n            # Move value from 'default_variant' to '_default_variant'\n            self._default_variant = self.default_variant\n            # Create property 'default_variant' on the class\n            cls.default_variant = property(\n                cls._get_default_variant_wrap,\n                cls._set_default_variant_wrap\n            )\n        super().__init__(*args, **kwargs)\n\n    @property\n    def show_order(self) -&gt; int:\n        \"\"\"Order in which is creator shown in UI.\n\n        Returns:\n            int: Order in which is creator shown (less == earlier). By default\n                is using Creator's 'order' or processing.\n        \"\"\"\n        return self.order\n\n    @abstractmethod\n    def create(\n        self,\n        product_name: str,\n        instance_data: dict[str, Any],\n        pre_create_data: dict[str, Any],\n    ) -&gt; Any:\n        \"\"\"Create new instance and store it.\n\n        Ideally should be stored to workfile using host implementation.\n\n        Args:\n            product_name (str): Product name of created instance.\n            instance_data (dict[str, Any]): Base data for instance.\n            pre_create_data (dict[str, Any]): Data based on pre creation\n                attributes. Those may affect how creator works.\n        \"\"\"\n\n    def get_description(self):\n        \"\"\"Short description of product base type and plugin.\n\n        Returns:\n            str: Short description of product base type.\n\n        \"\"\"\n        return self.description\n\n    def get_detail_description(self) -&gt; str | None:\n        \"\"\"Description of product base type and plugin.\n\n        Can be detailed with markdown or html tags.\n\n        Returns:\n            str: Detailed description of product base type for artist.\n\n        \"\"\"\n        return self.detailed_description\n\n    def get_default_variants(self) -&gt; list[str]:\n        \"\"\"Default variant values for UI tooltips.\n\n        Replacement of `default_variants` attribute. Using method gives\n        ability to have some \"logic\" other than attribute values.\n\n        By default, returns `default_variants` value.\n\n        Returns:\n            list[str]: Whisper variants for user input.\n\n        \"\"\"\n        return copy.deepcopy(self.default_variants)\n\n    def get_default_variant(\n        self, only_explicit: bool = False\n    ) -&gt; str | None:\n        \"\"\"Default variant value that will be used to prefill variant input.\n\n        This is for user input and value may not be content of result from\n        `get_default_variants`.\n\n        Note:\n            This method does not allow to have empty string as\n                default variant.\n\n        Args:\n            only_explicit (bool): If True, only explicit default\n                variant from '_default_variant' will be returned.\n\n        Returns:\n            str | None: Variant value.\n\n        \"\"\"\n        if only_explicit or self._default_variant:\n            return self._default_variant\n\n        for variant in self.get_default_variants():\n            return variant\n        return DEFAULT_VARIANT_VALUE\n\n    def _get_default_variant_wrap(self) -&gt; str:\n        \"\"\"Default variant value that will be used to prefill variant input.\n\n        Wrapper for 'get_default_variant'.\n\n        Notes:\n            This method is wrapper for 'get_default_variant'\n                for 'default_variant' property, so creator can override\n                the method.\n\n        Returns:\n            str: Variant value.\n\n        \"\"\"\n        return self.get_default_variant()\n\n    def _set_default_variant_wrap(self, variant: str) -&gt; None:\n        \"\"\"Set default variant value.\n\n        This method is needed for automated settings overrides which are\n        changing attributes based on keys in settings.\n\n        Args:\n            variant (str): New default variant value.\n\n        \"\"\"\n        self._default_variant = variant\n\n    default_variant: str = property(\n        _get_default_variant_wrap,\n        _set_default_variant_wrap\n    )\n\n    def get_pre_create_attr_defs(self) -&gt; list[AbstractAttrDef]:\n        \"\"\"Plugin attribute definitions needed for creation.\n        Attribute definitions of plugin that define how creation will work.\n        Values of these definitions are passed to `create` method.\n\n        Note:\n            Convert method should be implemented which should care about\n            updating keys/values when plugin attributes change.\n\n        Returns:\n            list[AbstractAttrDef]: Attribute definitions that can be tweaked\n                for created instance.\n\n        \"\"\"\n        return self.pre_create_attr_defs\n\n    def get_staging_dir(\n        self, instance: CreatedInstance\n    ) -&gt; StagingDir | None:\n        \"\"\"Return the staging dir and persistence from instance.\n\n        Args:\n            instance (CreatedInstance): Instance for which should be staging\n                dir gathered.\n\n        Returns:\n            StagingDir | None: Staging dir path and persistence or None.\n\n        \"\"\"\n        create_ctx = self.create_context\n        product_name = instance.get(\"productName\")\n        product_base_type = instance.get(\"productBaseType\")\n        product_type = instance.get(\"productType\")\n        folder_path = instance.get(\"folderPath\")\n\n        if not product_base_type:\n            product_base_type = product_type\n\n        # this can only work if product name and folder path are available\n        if not product_name or not folder_path:\n            return None\n\n        publish_settings = self.project_settings[\"core\"][\"publish\"]\n        follow_workfile_version = (\n            publish_settings\n            [\"CollectAnatomyInstanceData\"]\n            [\"follow_workfile_version\"]\n        )\n        follow_version_hosts = (\n            publish_settings\n            [\"CollectSceneVersion\"]\n            [\"hosts\"]\n        )\n\n        current_host = create_ctx.host.name\n        follow_workfile_version = (\n            follow_workfile_version and\n            current_host in follow_version_hosts\n        )\n\n        # Gather version number provided from the instance.\n        current_workfile = create_ctx.get_current_workfile_path()\n        version = instance.get(\"version\")\n\n        # If follow workfile, gather version from workfile path.\n        if version is None and follow_workfile_version and current_workfile:\n            workfile_version = get_version_from_path(current_workfile)\n            if workfile_version is not None:\n                version = int(workfile_version)\n\n        # Fill-up version with next version available.\n        if version is None:\n            versions = self.get_next_versions_for_instances(\n                [instance]\n            )\n            version, = tuple(versions.values())\n\n        template_data = {\"version\": version}\n\n        staging_dir_info = get_staging_dir_info(\n            create_ctx.get_current_project_entity(),\n            create_ctx.get_folder_entity(folder_path),\n            create_ctx.get_task_entity(folder_path, instance.get(\"task\")),\n            product_base_type=product_base_type,\n            product_type=product_type,\n            product_name=product_name,\n            host_name=create_ctx.host_name,\n            anatomy=create_ctx.get_current_project_anatomy(),\n            project_settings=create_ctx.get_current_project_settings(),\n            always_return_path=False,\n            logger=self.log,\n            template_data=template_data,\n        )\n\n        return staging_dir_info or None\n\n    def apply_staging_dir(\n        self, instance: CreatedInstance\n    ) -&gt; str | None:\n        \"\"\"Apply staging dir with persistence to instance's transient data.\n\n        Method is called on instance creation and on instance update.\n\n        Args:\n            instance (CreatedInstance): Instance for which should be staging\n                dir applied.\n\n        Returns:\n            str | None: Staging dir path or None if not applied.\n\n        \"\"\"\n        staging_dir_info = self.get_staging_dir(instance)\n        if staging_dir_info is None:\n            return None\n\n        # path might be already created by get_staging_dir_info\n        staging_dir_path = staging_dir_info.directory\n        os.makedirs(staging_dir_path, exist_ok=True)\n\n        instance.transient_data.update({\n            \"stagingDir\": staging_dir_path,\n            \"stagingDir_persistent\": staging_dir_info.is_persistent,\n            \"stagingDir_is_custom\": staging_dir_info.is_custom,\n        })\n\n        self.log.info(f\"Applied staging dir to instance: {staging_dir_path}\")\n\n        return staging_dir_path\n\n    def _pre_create_attr_defs_changed(self) -&gt; None:\n        \"\"\"Called when pre-create attribute definitions change.\n\n        Create plugin can call this method when knows that\n            'get_pre_create_attr_defs' should be called again.\n        \"\"\"\n        self.create_context.create_plugin_pre_create_attr_defs_changed(\n            self.identifier\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/creator_plugins.html#client.ayon_core.pipeline.create.creator_plugins.Creator.show_order","title":"<code>show_order</code>  <code>property</code>","text":"<p>Order in which is creator shown in UI.</p> <p>Returns:</p> Name Type Description <code>int</code> <code>int</code> <p>Order in which is creator shown (less == earlier). By default is using Creator's 'order' or processing.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/creator_plugins.html#client.ayon_core.pipeline.create.creator_plugins.Creator.apply_staging_dir","title":"<code>apply_staging_dir(instance)</code>","text":"<p>Apply staging dir with persistence to instance's transient data.</p> <p>Method is called on instance creation and on instance update.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>CreatedInstance</code> <p>Instance for which should be staging dir applied.</p> required <p>Returns:</p> Type Description <code>str | None</code> <p>str | None: Staging dir path or None if not applied.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def apply_staging_dir(\n    self, instance: CreatedInstance\n) -&gt; str | None:\n    \"\"\"Apply staging dir with persistence to instance's transient data.\n\n    Method is called on instance creation and on instance update.\n\n    Args:\n        instance (CreatedInstance): Instance for which should be staging\n            dir applied.\n\n    Returns:\n        str | None: Staging dir path or None if not applied.\n\n    \"\"\"\n    staging_dir_info = self.get_staging_dir(instance)\n    if staging_dir_info is None:\n        return None\n\n    # path might be already created by get_staging_dir_info\n    staging_dir_path = staging_dir_info.directory\n    os.makedirs(staging_dir_path, exist_ok=True)\n\n    instance.transient_data.update({\n        \"stagingDir\": staging_dir_path,\n        \"stagingDir_persistent\": staging_dir_info.is_persistent,\n        \"stagingDir_is_custom\": staging_dir_info.is_custom,\n    })\n\n    self.log.info(f\"Applied staging dir to instance: {staging_dir_path}\")\n\n    return staging_dir_path\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/creator_plugins.html#client.ayon_core.pipeline.create.creator_plugins.Creator.create","title":"<code>create(product_name, instance_data, pre_create_data)</code>  <code>abstractmethod</code>","text":"<p>Create new instance and store it.</p> <p>Ideally should be stored to workfile using host implementation.</p> <p>Parameters:</p> Name Type Description Default <code>product_name</code> <code>str</code> <p>Product name of created instance.</p> required <code>instance_data</code> <code>dict[str, Any]</code> <p>Base data for instance.</p> required <code>pre_create_data</code> <code>dict[str, Any]</code> <p>Data based on pre creation attributes. Those may affect how creator works.</p> required Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>@abstractmethod\ndef create(\n    self,\n    product_name: str,\n    instance_data: dict[str, Any],\n    pre_create_data: dict[str, Any],\n) -&gt; Any:\n    \"\"\"Create new instance and store it.\n\n    Ideally should be stored to workfile using host implementation.\n\n    Args:\n        product_name (str): Product name of created instance.\n        instance_data (dict[str, Any]): Base data for instance.\n        pre_create_data (dict[str, Any]): Data based on pre creation\n            attributes. Those may affect how creator works.\n    \"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/creator_plugins.html#client.ayon_core.pipeline.create.creator_plugins.Creator.get_default_variant","title":"<code>get_default_variant(only_explicit=False)</code>","text":"<p>Default variant value that will be used to prefill variant input.</p> <p>This is for user input and value may not be content of result from <code>get_default_variants</code>.</p> Note <p>This method does not allow to have empty string as     default variant.</p> <p>Parameters:</p> Name Type Description Default <code>only_explicit</code> <code>bool</code> <p>If True, only explicit default variant from '_default_variant' will be returned.</p> <code>False</code> <p>Returns:</p> Type Description <code>str | None</code> <p>str | None: Variant value.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def get_default_variant(\n    self, only_explicit: bool = False\n) -&gt; str | None:\n    \"\"\"Default variant value that will be used to prefill variant input.\n\n    This is for user input and value may not be content of result from\n    `get_default_variants`.\n\n    Note:\n        This method does not allow to have empty string as\n            default variant.\n\n    Args:\n        only_explicit (bool): If True, only explicit default\n            variant from '_default_variant' will be returned.\n\n    Returns:\n        str | None: Variant value.\n\n    \"\"\"\n    if only_explicit or self._default_variant:\n        return self._default_variant\n\n    for variant in self.get_default_variants():\n        return variant\n    return DEFAULT_VARIANT_VALUE\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/creator_plugins.html#client.ayon_core.pipeline.create.creator_plugins.Creator.get_default_variants","title":"<code>get_default_variants()</code>","text":"<p>Default variant values for UI tooltips.</p> <p>Replacement of <code>default_variants</code> attribute. Using method gives ability to have some \"logic\" other than attribute values.</p> <p>By default, returns <code>default_variants</code> value.</p> <p>Returns:</p> Type Description <code>list[str]</code> <p>list[str]: Whisper variants for user input.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def get_default_variants(self) -&gt; list[str]:\n    \"\"\"Default variant values for UI tooltips.\n\n    Replacement of `default_variants` attribute. Using method gives\n    ability to have some \"logic\" other than attribute values.\n\n    By default, returns `default_variants` value.\n\n    Returns:\n        list[str]: Whisper variants for user input.\n\n    \"\"\"\n    return copy.deepcopy(self.default_variants)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/creator_plugins.html#client.ayon_core.pipeline.create.creator_plugins.Creator.get_description","title":"<code>get_description()</code>","text":"<p>Short description of product base type and plugin.</p> <p>Returns:</p> Name Type Description <code>str</code> <p>Short description of product base type.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def get_description(self):\n    \"\"\"Short description of product base type and plugin.\n\n    Returns:\n        str: Short description of product base type.\n\n    \"\"\"\n    return self.description\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/creator_plugins.html#client.ayon_core.pipeline.create.creator_plugins.Creator.get_detail_description","title":"<code>get_detail_description()</code>","text":"<p>Description of product base type and plugin.</p> <p>Can be detailed with markdown or html tags.</p> <p>Returns:</p> Name Type Description <code>str</code> <code>str | None</code> <p>Detailed description of product base type for artist.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def get_detail_description(self) -&gt; str | None:\n    \"\"\"Description of product base type and plugin.\n\n    Can be detailed with markdown or html tags.\n\n    Returns:\n        str: Detailed description of product base type for artist.\n\n    \"\"\"\n    return self.detailed_description\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/creator_plugins.html#client.ayon_core.pipeline.create.creator_plugins.Creator.get_pre_create_attr_defs","title":"<code>get_pre_create_attr_defs()</code>","text":"<p>Plugin attribute definitions needed for creation. Attribute definitions of plugin that define how creation will work. Values of these definitions are passed to <code>create</code> method.</p> Note <p>Convert method should be implemented which should care about updating keys/values when plugin attributes change.</p> <p>Returns:</p> Type Description <code>list[AbstractAttrDef]</code> <p>list[AbstractAttrDef]: Attribute definitions that can be tweaked for created instance.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def get_pre_create_attr_defs(self) -&gt; list[AbstractAttrDef]:\n    \"\"\"Plugin attribute definitions needed for creation.\n    Attribute definitions of plugin that define how creation will work.\n    Values of these definitions are passed to `create` method.\n\n    Note:\n        Convert method should be implemented which should care about\n        updating keys/values when plugin attributes change.\n\n    Returns:\n        list[AbstractAttrDef]: Attribute definitions that can be tweaked\n            for created instance.\n\n    \"\"\"\n    return self.pre_create_attr_defs\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/creator_plugins.html#client.ayon_core.pipeline.create.creator_plugins.Creator.get_staging_dir","title":"<code>get_staging_dir(instance)</code>","text":"<p>Return the staging dir and persistence from instance.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>CreatedInstance</code> <p>Instance for which should be staging dir gathered.</p> required <p>Returns:</p> Type Description <code>StagingDir | None</code> <p>StagingDir | None: Staging dir path and persistence or None.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def get_staging_dir(\n    self, instance: CreatedInstance\n) -&gt; StagingDir | None:\n    \"\"\"Return the staging dir and persistence from instance.\n\n    Args:\n        instance (CreatedInstance): Instance for which should be staging\n            dir gathered.\n\n    Returns:\n        StagingDir | None: Staging dir path and persistence or None.\n\n    \"\"\"\n    create_ctx = self.create_context\n    product_name = instance.get(\"productName\")\n    product_base_type = instance.get(\"productBaseType\")\n    product_type = instance.get(\"productType\")\n    folder_path = instance.get(\"folderPath\")\n\n    if not product_base_type:\n        product_base_type = product_type\n\n    # this can only work if product name and folder path are available\n    if not product_name or not folder_path:\n        return None\n\n    publish_settings = self.project_settings[\"core\"][\"publish\"]\n    follow_workfile_version = (\n        publish_settings\n        [\"CollectAnatomyInstanceData\"]\n        [\"follow_workfile_version\"]\n    )\n    follow_version_hosts = (\n        publish_settings\n        [\"CollectSceneVersion\"]\n        [\"hosts\"]\n    )\n\n    current_host = create_ctx.host.name\n    follow_workfile_version = (\n        follow_workfile_version and\n        current_host in follow_version_hosts\n    )\n\n    # Gather version number provided from the instance.\n    current_workfile = create_ctx.get_current_workfile_path()\n    version = instance.get(\"version\")\n\n    # If follow workfile, gather version from workfile path.\n    if version is None and follow_workfile_version and current_workfile:\n        workfile_version = get_version_from_path(current_workfile)\n        if workfile_version is not None:\n            version = int(workfile_version)\n\n    # Fill-up version with next version available.\n    if version is None:\n        versions = self.get_next_versions_for_instances(\n            [instance]\n        )\n        version, = tuple(versions.values())\n\n    template_data = {\"version\": version}\n\n    staging_dir_info = get_staging_dir_info(\n        create_ctx.get_current_project_entity(),\n        create_ctx.get_folder_entity(folder_path),\n        create_ctx.get_task_entity(folder_path, instance.get(\"task\")),\n        product_base_type=product_base_type,\n        product_type=product_type,\n        product_name=product_name,\n        host_name=create_ctx.host_name,\n        anatomy=create_ctx.get_current_project_anatomy(),\n        project_settings=create_ctx.get_current_project_settings(),\n        always_return_path=False,\n        logger=self.log,\n        template_data=template_data,\n    )\n\n    return staging_dir_info or None\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/creator_plugins.html#client.ayon_core.pipeline.create.creator_plugins.ProductConvertorPlugin","title":"<code>ProductConvertorPlugin</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Helper for conversion of instances created using legacy creators.</p> <p>Conversion from legacy creators would mean to lose legacy instances, convert them automatically or write a script which must user run. All of these solutions are working but will happen without asking or user must know about them. This plugin can be used to show legacy instances in Publisher and give user ability to run conversion script.</p> <p>Convertor logic should be very simple. Method 'find_instances' is to look for legacy instances in scene and possibly call pre-implemented 'add_convertor_item'.</p> <p>User will have ability to trigger conversion which is executed by calling 'convert' which should call 'remove_convertor_item' when is done.</p> <p>It does make sense to add only one or none legacy item to create context for convertor as it's not possible to choose which instace are converted and which are not.</p> <p>Convertor can use 'collection_shared_data' property like creators. Also can store any information to it's object for conversion purposes.</p> <p>Parameters:</p> Name Type Description Default <code>create_context</code> <code>CreateContext</code> <p>Context which initialized the plugin.</p> required Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>class ProductConvertorPlugin(ABC):\n    \"\"\"Helper for conversion of instances created using legacy creators.\n\n    Conversion from legacy creators would mean to lose legacy instances,\n    convert them automatically or write a script which must user run. All of\n    these solutions are working but will happen without asking or user must\n    know about them. This plugin can be used to show legacy instances in\n    Publisher and give user ability to run conversion script.\n\n    Convertor logic should be very simple. Method 'find_instances' is to\n    look for legacy instances in scene and possibly call pre-implemented\n    'add_convertor_item'.\n\n    User will have ability to trigger conversion which is executed by calling\n    'convert' which should call 'remove_convertor_item' when is done.\n\n    It does make sense to add only one or none legacy item to create context\n    for convertor as it's not possible to choose which instace are converted\n    and which are not.\n\n    Convertor can use 'collection_shared_data' property like creators. Also\n    can store any information to it's object for conversion purposes.\n\n    Args:\n        create_context (CreateContext): Context which initialized the plugin.\n    \"\"\"\n\n    _log = None\n\n    def __init__(self, create_context: CreateContext) -&gt; None:\n        self._create_context = create_context\n\n    @property\n    def log(self) -&gt; logging.Logger:\n        \"\"\"Logger of the plugin.\n\n        Returns:\n            logging.Logger: Logger with name of the plugin.\n\n        \"\"\"\n        if self._log is None:\n            self._log = Logger.get_logger(self.__class__.__name__)\n        return self._log\n\n    @property\n    def host(self) -&gt; IPublishHost:\n        return self._create_context.host\n\n    @property\n    @abstractmethod\n    def identifier(self) -&gt; str:\n        \"\"\"Converted identifier.\n\n        Returns:\n            str: Converted identifier unique for all converters in host.\n\n        \"\"\"\n\n    @abstractmethod\n    def find_instances(self) -&gt; None:\n        \"\"\"Look for legacy instances in the scene.\n\n        Should call 'add_convertor_item' if there is at least one instance to\n        convert.\n\n        \"\"\"\n\n    @abstractmethod\n    def convert(self) -&gt; None:\n        \"\"\"Conversion code.\"\"\"\n\n    @property\n    def create_context(self) -&gt; CreateContext:\n        \"\"\"Quick access to create context.\n\n        Returns:\n            CreateContext: Context which initialized the plugin.\n\n        \"\"\"\n        return self._create_context\n\n    @property\n    def collection_shared_data(self) -&gt; dict[str, Any]:\n        \"\"\"Access to shared data that can be used during 'find_instances'.\n\n        Returns:\n            dict[str, Any]: Shared data.\n\n        Raises:\n            UnavailableSharedData: When called out of collection phase.\n\n        \"\"\"\n        return self._create_context.collection_shared_data\n\n    def add_convertor_item(self, label: str) -&gt; None:\n        \"\"\"Add item to CreateContext.\n\n        Args:\n            label (str): Label of item which will show in UI.\n\n        \"\"\"\n        self._create_context.add_convertor_item(self.identifier, label)\n\n    def remove_convertor_item(self) -&gt; None:\n        \"\"\"Remove legacy item from create context when conversion finished.\"\"\"\n        self._create_context.remove_convertor_item(self.identifier)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/creator_plugins.html#client.ayon_core.pipeline.create.creator_plugins.ProductConvertorPlugin.collection_shared_data","title":"<code>collection_shared_data</code>  <code>property</code>","text":"<p>Access to shared data that can be used during 'find_instances'.</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: Shared data.</p> <p>Raises:</p> Type Description <code>UnavailableSharedData</code> <p>When called out of collection phase.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/creator_plugins.html#client.ayon_core.pipeline.create.creator_plugins.ProductConvertorPlugin.create_context","title":"<code>create_context</code>  <code>property</code>","text":"<p>Quick access to create context.</p> <p>Returns:</p> Name Type Description <code>CreateContext</code> <code>CreateContext</code> <p>Context which initialized the plugin.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/creator_plugins.html#client.ayon_core.pipeline.create.creator_plugins.ProductConvertorPlugin.identifier","title":"<code>identifier</code>  <code>abstractmethod</code> <code>property</code>","text":"<p>Converted identifier.</p> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Converted identifier unique for all converters in host.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/creator_plugins.html#client.ayon_core.pipeline.create.creator_plugins.ProductConvertorPlugin.log","title":"<code>log</code>  <code>property</code>","text":"<p>Logger of the plugin.</p> <p>Returns:</p> Type Description <code>Logger</code> <p>logging.Logger: Logger with name of the plugin.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/creator_plugins.html#client.ayon_core.pipeline.create.creator_plugins.ProductConvertorPlugin.add_convertor_item","title":"<code>add_convertor_item(label)</code>","text":"<p>Add item to CreateContext.</p> <p>Parameters:</p> Name Type Description Default <code>label</code> <code>str</code> <p>Label of item which will show in UI.</p> required Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def add_convertor_item(self, label: str) -&gt; None:\n    \"\"\"Add item to CreateContext.\n\n    Args:\n        label (str): Label of item which will show in UI.\n\n    \"\"\"\n    self._create_context.add_convertor_item(self.identifier, label)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/creator_plugins.html#client.ayon_core.pipeline.create.creator_plugins.ProductConvertorPlugin.convert","title":"<code>convert()</code>  <code>abstractmethod</code>","text":"<p>Conversion code.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>@abstractmethod\ndef convert(self) -&gt; None:\n    \"\"\"Conversion code.\"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/creator_plugins.html#client.ayon_core.pipeline.create.creator_plugins.ProductConvertorPlugin.find_instances","title":"<code>find_instances()</code>  <code>abstractmethod</code>","text":"<p>Look for legacy instances in the scene.</p> <p>Should call 'add_convertor_item' if there is at least one instance to convert.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>@abstractmethod\ndef find_instances(self) -&gt; None:\n    \"\"\"Look for legacy instances in the scene.\n\n    Should call 'add_convertor_item' if there is at least one instance to\n    convert.\n\n    \"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/creator_plugins.html#client.ayon_core.pipeline.create.creator_plugins.ProductConvertorPlugin.remove_convertor_item","title":"<code>remove_convertor_item()</code>","text":"<p>Remove legacy item from create context when conversion finished.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def remove_convertor_item(self) -&gt; None:\n    \"\"\"Remove legacy item from create context when conversion finished.\"\"\"\n    self._create_context.remove_convertor_item(self.identifier)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/creator_plugins.html#client.ayon_core.pipeline.create.creator_plugins.cache_and_get_instances","title":"<code>cache_and_get_instances(creator, shared_key, list_instances_func)</code>","text":"<p>Common approach to cache instances in shared data.</p> <p>This is helper function which does not handle cases when a 'shared_key' is used for different list instances functions. The same approach of caching instances into 'collection_shared_data' is not required but is so common we've decided to unify it to some degree.</p> <p>Function 'list_instances_func' is called only if 'shared_key' is not available in 'collection_shared_data' on creator.</p> <p>Parameters:</p> Name Type Description Default <code>creator</code> <code>Creator</code> <p>Plugin which would like to get instance data.</p> required <code>shared_key</code> <code>str</code> <p>Key under which output of function will be stored.</p> required <code>list_instances_func</code> <code>Function</code> <p>Function that will return instance data if data were not yet stored under 'shared_key'.</p> required <p>Returns:</p> Type Description <code>dict[str, list[Any]]</code> <p>dict[str, dict[str, Any]]: Cached instances by creator identifier from result of passed function.</p> Source code in <code>client/ayon_core/pipeline/create/creator_plugins.py</code> <pre><code>def cache_and_get_instances(\n    creator: BaseCreator,\n    shared_key: str,\n    list_instances_func: Callable[[], list[dict[str, Any]]],\n) -&gt; dict[str, list[Any]]:\n    \"\"\"Common approach to cache instances in shared data.\n\n    This is helper function which does not handle cases when a 'shared_key' is\n    used for different list instances functions. The same approach of caching\n    instances into 'collection_shared_data' is not required but is so common\n    we've decided to unify it to some degree.\n\n    Function 'list_instances_func' is called only if 'shared_key' is not\n    available in 'collection_shared_data' on creator.\n\n    Args:\n        creator (Creator): Plugin which would like to get instance data.\n        shared_key (str): Key under which output of function will be stored.\n        list_instances_func (Function): Function that will return instance data\n            if data were not yet stored under 'shared_key'.\n\n    Returns:\n        dict[str, dict[str, Any]]: Cached instances by creator identifier from\n            result of passed function.\n\n    \"\"\"\n    if shared_key not in creator.collection_shared_data:\n        value = collections.defaultdict(list)\n        for instance in list_instances_func():\n            identifier = instance.get(\"creator_identifier\")\n            value[identifier].append(instance)\n        creator.collection_shared_data[shared_key] = value\n    return creator.collection_shared_data[shared_key]\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/exceptions.html","title":"exceptions","text":""},{"location":"autoapi/client/ayon_core/pipeline/create/exceptions.html#client.ayon_core.pipeline.create.exceptions.CreatorError","title":"<code>CreatorError</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Should be raised when creator failed because of known issue.</p> <p>Message of error should be artist friendly.</p> Source code in <code>client/ayon_core/pipeline/create/exceptions.py</code> <pre><code>class CreatorError(Exception):\n    \"\"\"Should be raised when creator failed because of known issue.\n\n    Message of error should be artist friendly.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/exceptions.html#client.ayon_core.pipeline.create.exceptions.CreatorsOperationFailed","title":"<code>CreatorsOperationFailed</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Raised when a creator process crashes in 'CreateContext'.</p> <p>The exception contains information about the creator and error. The data are prepared using 'prepare_failed_creator_operation_info' and can be serialized using json.</p> <p>Usage is for UI purposes which may not have access to exceptions directly and would not have ability to catch exceptions 'per creator'.</p> <p>Parameters:</p> Name Type Description Default <code>msg</code> <code>str</code> <p>General error message.</p> required <code>failed_info</code> <code>list[dict[str, Any]]</code> <p>List of failed creators with exception message and optionally formatted traceback.</p> required Source code in <code>client/ayon_core/pipeline/create/exceptions.py</code> <pre><code>class CreatorsOperationFailed(Exception):\n    \"\"\"Raised when a creator process crashes in 'CreateContext'.\n\n    The exception contains information about the creator and error. The data\n    are prepared using 'prepare_failed_creator_operation_info' and can be\n    serialized using json.\n\n    Usage is for UI purposes which may not have access to exceptions directly\n    and would not have ability to catch exceptions 'per creator'.\n\n    Args:\n        msg (str): General error message.\n        failed_info (list[dict[str, Any]]): List of failed creators with\n            exception message and optionally formatted traceback.\n    \"\"\"\n\n    def __init__(self, msg, failed_info):\n        super().__init__(msg)\n        self.failed_info = failed_info\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/exceptions.html#client.ayon_core.pipeline.create.exceptions.HostMissRequiredMethod","title":"<code>HostMissRequiredMethod</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Host does not have implemented required functions for creation.</p> Source code in <code>client/ayon_core/pipeline/create/exceptions.py</code> <pre><code>class HostMissRequiredMethod(Exception):\n    \"\"\"Host does not have implemented required functions for creation.\"\"\"\n\n    def __init__(self, host, missing_methods):\n        self.missing_methods = missing_methods\n        self.host = host\n        joined_methods = \", \".join(\n            ['\"{}\"'.format(name) for name in missing_methods]\n        )\n        dirpath = os.path.dirname(\n            os.path.normpath(inspect.getsourcefile(host))\n        )\n        dirpath_parts = dirpath.split(os.path.sep)\n        host_name = dirpath_parts.pop(-1)\n        if host_name == \"api\":\n            host_name = dirpath_parts.pop(-1)\n\n        msg = \"Host \\\"{}\\\" does not have implemented method/s {}\".format(\n            host_name, joined_methods\n        )\n        super().__init__(msg)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/exceptions.html#client.ayon_core.pipeline.create.exceptions.ImmutableKeyError","title":"<code>ImmutableKeyError</code>","text":"<p>               Bases: <code>TypeError</code></p> <p>Accessed key is immutable so does not allow changes or removals.</p> Source code in <code>client/ayon_core/pipeline/create/exceptions.py</code> <pre><code>class ImmutableKeyError(TypeError):\n    \"\"\"Accessed key is immutable so does not allow changes or removals.\"\"\"\n\n    def __init__(self, key, msg=None):\n        self.immutable_key = key\n        if not msg:\n            msg = \"Key \\\"{}\\\" is immutable and does not allow changes.\".format(\n                key\n            )\n        super().__init__(msg)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/exceptions.html#client.ayon_core.pipeline.create.exceptions.UnavailableSharedData","title":"<code>UnavailableSharedData</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Shared data are not available at the moment when are accessed.</p> Source code in <code>client/ayon_core/pipeline/create/exceptions.py</code> <pre><code>class UnavailableSharedData(Exception):\n    \"\"\"Shared data are not available at the moment when are accessed.\"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/product_name.html","title":"product_name","text":"<p>Functions for handling product names.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/product_name.html#client.ayon_core.pipeline.create.product_name.get_product_name","title":"<code>get_product_name(project_name, folder_entity, task_entity, product_base_type, product_type, host_name, variant, *, dynamic_data=None, project_settings=None, project_entity=None, default_template=None, product_base_type_filter=None)</code>","text":"<pre><code>get_product_name(project_name: str, folder_entity: dict[str, Any], task_entity: Optional[dict[str, Any]], product_base_type: str, product_type: str, host_name: str, variant: str, *, dynamic_data: Optional[dict[str, Any]] = None, project_settings: Optional[dict[str, Any]] = None, project_entity: Optional[dict[str, Any]] = None, default_template: Optional[str] = None, product_base_type_filter: Optional[str] = None) -&gt; TemplateResult\n</code></pre><pre><code>get_product_name(project_name, task_name, task_type, host_name, product_type, variant, default_template=None, dynamic_data=None, project_settings=None, product_type_filter=None, project_entity=None) -&gt; TemplateResult\n</code></pre> <p>Calculate product name based on passed context and AYON settings.</p> <p>Product name templates are defined in <code>project_settings/global/tools     /creator/product_name_profiles</code> where are profiles with host name,     product base type, product type, task name and task type filters.</p> <p>If context does not match any profile then <code>DEFAULT_PRODUCT_TEMPLATE</code>     is used as default template.</p> <p>That's main reason why so many arguments are required to calculate product     name.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>folder_entity</code> <code>Optional[dict[str, Any]]</code> <p>Folder entity.</p> required <code>task_entity</code> <code>Optional[dict[str, Any]]</code> <p>Task entity.</p> required <code>host_name</code> <code>str</code> <p>Host name.</p> required <code>product_base_type</code> <code>str</code> <p>Product base type.</p> required <code>product_type</code> <code>str</code> <p>Product type.</p> required <code>variant</code> <code>str</code> <p>In most of the cases it is user input during creation.</p> required <code>dynamic_data</code> <code>Optional[dict[str, Any]]</code> <p>Dynamic data specific for a creator which creates instance.</p> <code>None</code> <code>project_settings</code> <code>Optional[dict[str, Any]]</code> <p>Prepared settings for project. Settings are queried if not passed.</p> <code>None</code> <code>project_entity</code> <code>Optional[dict[str, Any]]</code> <p>Project entity used when task short name is required by template.</p> <code>None</code> <code>default_template</code> <code>Optional[str]</code> <p>Default template if any profile does not match passed context. Constant 'DEFAULT_PRODUCT_TEMPLATE' is used if is not passed.</p> <code>None</code> <code>product_base_type_filter</code> <code>Optional[str]</code> <p>Use different product base type for product template filtering. Value of <code>product_base_type_filter</code> is used when not passed.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>TemplateResult</code> <code>TemplateResult</code> <p>Product name.</p> <p>Raises:</p> Type Description <code>TaskNotSetError</code> <p>If template requires task which is not provided.</p> <code>TemplateFillError</code> <p>If filled template contains placeholder key which is not collected.</p> Source code in <code>client/ayon_core/pipeline/create/product_name.py</code> <pre><code>@_backwards_compatibility_product_name\ndef get_product_name(\n    project_name: str,\n    folder_entity: dict[str, Any],\n    task_entity: Optional[dict[str, Any]],\n    product_base_type: str,\n    product_type: str,\n    host_name: str,\n    variant: str,\n    *,\n    dynamic_data: Optional[dict[str, Any]] = None,\n    project_settings: Optional[dict[str, Any]] = None,\n    project_entity: Optional[dict[str, Any]] = None,\n    default_template: Optional[str] = None,\n    product_base_type_filter: Optional[str] = None,\n) -&gt; TemplateResult:\n    \"\"\"Calculate product name based on passed context and AYON settings.\n\n    Product name templates are defined in `project_settings/global/tools\n        /creator/product_name_profiles` where are profiles with host name,\n        product base type, product type, task name and task type filters.\n\n    If context does not match any profile then `DEFAULT_PRODUCT_TEMPLATE`\n        is used as default template.\n\n    That's main reason why so many arguments are required to calculate product\n        name.\n\n    Args:\n        project_name (str): Project name.\n        folder_entity (Optional[dict[str, Any]]): Folder entity.\n        task_entity (Optional[dict[str, Any]]): Task entity.\n        host_name (str): Host name.\n        product_base_type (str): Product base type.\n        product_type (str): Product type.\n        variant (str): In most of the cases it is user input during creation.\n        dynamic_data (Optional[dict[str, Any]]): Dynamic data specific for\n            a creator which creates instance.\n        project_settings (Optional[dict[str, Any]]): Prepared settings\n            for project. Settings are queried if not passed.\n        project_entity (Optional[dict[str, Any]]): Project entity used when\n            task short name is required by template.\n        default_template (Optional[str]): Default template if any profile does\n            not match passed context. Constant 'DEFAULT_PRODUCT_TEMPLATE'\n            is used if is not passed.\n        product_base_type_filter (Optional[str]): Use different product base\n            type for product template filtering. Value of\n            `product_base_type_filter` is used when not passed.\n\n    Returns:\n        TemplateResult: Product name.\n\n    Raises:\n        TaskNotSetError: If template requires task which is not provided.\n        TemplateFillError: If filled template contains placeholder key which\n            is not collected.\n\n    \"\"\"\n    if not product_base_type:\n        return StringTemplate(\"\").format({})\n\n    if not product_type:\n        product_type = product_base_type\n\n    task_name = task_type = None\n    if task_entity:\n        task_name = task_entity[\"name\"]\n        task_type = task_entity[\"taskType\"]\n\n    template = get_product_name_template(\n        project_name=project_name,\n        product_base_type=product_base_type_filter or product_base_type,\n        product_type=product_type,\n        task_name=task_name,\n        task_type=task_type,\n        host_name=host_name,\n        default_template=default_template,\n        project_settings=project_settings,\n    )\n\n    template_low = template.lower()\n    # Simple check of task name existence for template with {task[name]} in\n    if not task_name and \"{task\" in template_low:\n        raise TaskNotSetError()\n\n    task_value = {\n        \"name\": task_name,\n        \"type\": task_type,\n    }\n    if \"{task}\" in template_low:\n        task_value = task_name\n        # NOTE this is message for TDs and Admins -&gt; not really for users\n        # TODO validate this in settings and not allow it\n        log.warning(\n            \"Found deprecated task key '{task}' in product name template.\"\n            \" Please use '{task[name]}' instead.\"\n        )\n\n    elif \"{task[short]}\" in template_low:\n        if project_entity is None:\n            project_entity = ayon_api.get_project(project_name)\n        task_types_by_name = {\n            task[\"name\"]: task\n            for task in project_entity[\"taskTypes\"]\n        }\n        task_short = task_types_by_name.get(task_type, {}).get(\"shortName\")\n        task_value[\"short\"] = task_short\n\n    fill_pairs = {\n        \"variant\": variant,\n        # TODO We should stop support 'family' key.\n        \"family\": product_base_type,\n        \"task\": task_value,\n        \"product\": {\n            \"type\": product_type,\n            \"basetype\": product_base_type,\n        }\n    }\n    if folder_entity:\n        fill_pairs[\"folder\"] = {\n            \"name\": folder_entity[\"name\"],\n            \"type\": folder_entity[\"folderType\"],\n        }\n        # Folder label was added ad-hoc and might not be passed from create\n        #   plugins that do fake entities\n        folder_label = folder_entity.get(\"label\")\n        if folder_label:\n            fill_pairs[\"folder\"][\"label\"] = folder_label\n\n    if dynamic_data:\n        # Dynamic data may override default values\n        for key, value in dynamic_data.items():\n            fill_pairs[key] = value\n\n    try:\n        return StringTemplate.format_strict_template(\n            template=template,\n            data=prepare_template_data(fill_pairs)\n        )\n    except KeyError as exp:\n        msg = (\n            f\"Value for {exp} key is missing in template '{template}'.\"\n            f\" Available values are {fill_pairs}\"\n        )\n        raise TemplateFillError(msg)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/product_name.html#client.ayon_core.pipeline.create.product_name.get_product_name_template","title":"<code>get_product_name_template(project_name, product_type, task_name, task_type, host_name, default_template=None, project_settings=None, product_base_type=None)</code>","text":"<p>Get product name template based on passed context.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project on which the context lives.</p> required <code>product_type</code> <code>str</code> <p>Product type for which the product name is calculated.</p> required <code>host_name</code> <code>str</code> <p>Name of host in which the product name is calculated.</p> required <code>task_name</code> <code>Optional[str]</code> <p>Name of task in which context the product is created.</p> required <code>task_type</code> <code>Optional[str]</code> <p>Type of task in which context the product is created.</p> required <code>default_template</code> <code>Optional[str]</code> <p>Default template which is used if settings won't find any matching possibility. Constant 'DEFAULT_PRODUCT_TEMPLATE' is used if not defined.</p> <code>None</code> <code>project_settings</code> <code>Optional[dict[str, Any]]</code> <p>Prepared settings for project. Settings are queried if not passed.</p> <code>None</code> <code>product_base_type</code> <code>Optional[str]</code> <p>Base type of product.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Product name template.</p> Source code in <code>client/ayon_core/pipeline/create/product_name.py</code> <pre><code>def get_product_name_template(\n    project_name: str,\n    product_type: str,\n    task_name: Optional[str],\n    task_type: Optional[str],\n    host_name: str,\n    default_template: Optional[str] = None,\n    project_settings: Optional[dict[str, Any]] = None,\n    product_base_type: Optional[str] = None\n) -&gt; str:\n    \"\"\"Get product name template based on passed context.\n\n    Args:\n        project_name (str): Project on which the context lives.\n        product_type (str): Product type for which the product name is\n            calculated.\n        host_name (str): Name of host in which the product name is calculated.\n        task_name (Optional[str]): Name of task in which context the\n            product is created.\n        task_type (Optional[str]): Type of task in which context the\n            product is created.\n        default_template (Optional[str]): Default template which is used if\n            settings won't find any matching possibility. Constant\n            'DEFAULT_PRODUCT_TEMPLATE' is used if not defined.\n        project_settings (Optional[dict[str, Any]]): Prepared settings for\n            project. Settings are queried if not passed.\n        product_base_type (Optional[str]): Base type of product.\n\n    Returns:\n        str: Product name template.\n\n    \"\"\"\n    if project_settings is None:\n        project_settings = get_project_settings(project_name)\n    tools_settings = project_settings[\"core\"][\"tools\"]\n    profiles = tools_settings[\"creator\"][\"product_name_profiles\"]\n    filtering_criteria = {\n        \"product_base_types\": product_base_type or product_type,\n        \"product_types\": product_type,\n        \"host_names\": host_name,\n        \"task_names\": task_name,\n        \"task_types\": task_type,\n    }\n    matching_profile = filter_profiles(profiles, filtering_criteria)\n    template = None\n    if matching_profile:\n        # TODO remove formatting keys replacement\n        template = (\n            matching_profile[\"template\"]\n            .replace(\"{task}\", \"{task[name]}\")\n            .replace(\"{Task}\", \"{Task[name]}\")\n            .replace(\"{TASK}\", \"{TASK[NAME]}\")\n            .replace(\"{family}\", \"{product[type]}\")\n            .replace(\"{Family}\", \"{Product[type]}\")\n            .replace(\"{FAMILY}\", \"{PRODUCT[TYPE]}\")\n            .replace(\"{asset}\", \"{folder[name]}\")\n            .replace(\"{Asset}\", \"{Folder[name]}\")\n            .replace(\"{ASSET}\", \"{FOLDER[NAME]}\")\n        )\n\n    # Make sure template is set (matching may have empty string)\n    if not template:\n        template = default_template or DEFAULT_PRODUCT_TEMPLATE\n    return template\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/structures.html","title":"structures","text":""},{"location":"autoapi/client/ayon_core/pipeline/create/structures.html#client.ayon_core.pipeline.create.structures.AttributeValues","title":"<code>AttributeValues</code>","text":"<p>Container which keep values of Attribute definitions.</p> <p>Goal is to have one object which hold values of attribute definitions for single instance.</p> <p>Has dictionary like methods. Not all of them are allowed all the time.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <code>Union[CreatedInstance, PublishAttributes]</code> <p>Parent object.</p> required <code>key</code> <code>str</code> <p>Key of attribute values.</p> required <code>attr_defs</code> <code>List[AbstractAttrDef]</code> <p>Definitions of value type and properties.</p> required <code>values</code> <code>dict</code> <p>Values after possible conversion.</p> required <code>origin_data</code> <code>dict</code> <p>Values loaded from host before conversion.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>class AttributeValues:\n    \"\"\"Container which keep values of Attribute definitions.\n\n    Goal is to have one object which hold values of attribute definitions for\n    single instance.\n\n    Has dictionary like methods. Not all of them are allowed all the time.\n\n    Args:\n        parent (Union[CreatedInstance, PublishAttributes]): Parent object.\n        key (str): Key of attribute values.\n        attr_defs (List[AbstractAttrDef]): Definitions of value type\n            and properties.\n        values (dict): Values after possible conversion.\n        origin_data (dict): Values loaded from host before conversion.\n\n    \"\"\"\n    def __init__(self, parent, key, attr_defs, values, origin_data=None):\n        self._parent = parent\n        self._key = key\n        if origin_data is None:\n            origin_data = copy.deepcopy(values)\n        self._origin_data = origin_data\n\n        attr_defs_by_key = {}\n        button_defs_by_key = {}\n        for attr_def in attr_defs:\n            if attr_def.is_value_def:\n                attr_defs_by_key[attr_def.key] = attr_def\n            elif isinstance(attr_def, ButtonDef):\n                button_defs_by_key[attr_def.key] = attr_def\n\n        for key, value in values.items():\n            if key not in attr_defs_by_key:\n                new_def = UnknownDef(key, label=key, default=value)\n                attr_defs.append(new_def)\n                attr_defs_by_key[key] = new_def\n\n        self._attr_defs = attr_defs\n        self._attr_defs_by_key = attr_defs_by_key\n        self._button_defs_by_key = button_defs_by_key\n\n        self._data = {}\n        for attr_def in attr_defs:\n            value = values.get(attr_def.key)\n            if value is None:\n                continue\n            converted_value = attr_def.convert_value(value)\n            # QUESTION Could we just use converted value all the time?\n            if converted_value == value:\n                self._data[attr_def.key] = value\n\n    def __setitem__(self, key, value):\n        if key not in self._attr_defs_by_key:\n            raise KeyError(\"Key \\\"{}\\\" was not found.\".format(key))\n\n        self.update({key: value})\n\n    def __getitem__(self, key):\n        if key not in self._attr_defs_by_key:\n            return self._data[key]\n        return self._data.get(key, self._attr_defs_by_key[key].default)\n\n    def __contains__(self, key):\n        return key in self._attr_defs_by_key\n\n    def __iter__(self):\n        for key in self._attr_defs_by_key:\n            yield key\n\n    def get(self, key, default=None):\n        if key in self._attr_defs_by_key:\n            return self[key]\n        return default\n\n    def keys(self):\n        return self._attr_defs_by_key.keys()\n\n    def values(self):\n        for key in self._attr_defs_by_key.keys():\n            yield self._data.get(key)\n\n    def items(self):\n        for key in self._attr_defs_by_key.keys():\n            yield key, self._data.get(key)\n\n    def get_attr_def(self, key, default=None):\n        button_def = self._button_defs_by_key.get(key)\n        if button_def is not None:\n            return button_def\n        return self._attr_defs_by_key.get(key, default)\n\n    def update(self, value):\n        changes = self._update(value)\n        if changes:\n            self._parent.attribute_value_changed(self._key, changes)\n\n    def pop(self, key, default=None):\n        value, changes = self._pop(key, default)\n        if changes:\n            self._parent.attribute_value_changed(self._key, changes)\n        return value\n\n    def set_value(self, value):\n        pop_keys = set(value.keys()) - set(self._data.keys())\n        changes = self._update(value)\n        for key in pop_keys:\n            _, key_changes = self._pop(key, None)\n            changes.update(key_changes)\n\n        if changes:\n            self._parent.attribute_value_changed(self._key, changes)\n\n    def reset_values(self):\n        self._data = {}\n\n    def mark_as_stored(self):\n        self._origin_data = copy.deepcopy(self._data)\n\n    @property\n    def attr_defs(self):\n        \"\"\"Pointer to attribute definitions.\n\n        Returns:\n            List[AbstractAttrDef]: Attribute definitions.\n        \"\"\"\n\n        return list(self._attr_defs)\n\n    @property\n    def origin_data(self):\n        return copy.deepcopy(self._origin_data)\n\n    def data_to_store(self):\n        \"\"\"Create new dictionary with data to store.\n\n        Returns:\n            Dict[str, Any]: Attribute values that should be stored.\n        \"\"\"\n\n        output = {}\n        for key in self._data:\n            output[key] = self[key]\n\n        for key, attr_def in self._attr_defs_by_key.items():\n            if key not in output:\n                output[key] = attr_def.default\n        return output\n\n    def get_serialized_attr_defs(self):\n        \"\"\"Serialize attribute definitions to json serializable types.\n\n        Returns:\n            List[Dict[str, Any]]: Serialized attribute definitions.\n        \"\"\"\n\n        return serialize_attr_defs(self._attr_defs)\n\n    def _update(self, value):\n        changes = {}\n        for key, key_value in dict(value).items():\n            if key in self._data and self._data.get(key) == key_value:\n                continue\n            self._data[key] = key_value\n            changes[key] = key_value\n        return changes\n\n    def _pop(self, key, default):\n        has_key = key in self._data\n        value = self._data.pop(key, default)\n        # Remove attribute definition if is 'UnknownDef'\n        # - gives option to get rid of unknown values\n        attr_def = self._attr_defs_by_key.get(key)\n        changes = {}\n        if isinstance(attr_def, UnknownDef):\n            self._attr_defs_by_key.pop(key)\n            self._attr_defs.remove(attr_def)\n        elif has_key:\n            changes[key] = None\n        return value, changes\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/structures.html#client.ayon_core.pipeline.create.structures.AttributeValues.attr_defs","title":"<code>attr_defs</code>  <code>property</code>","text":"<p>Pointer to attribute definitions.</p> <p>Returns:</p> Type Description <p>List[AbstractAttrDef]: Attribute definitions.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/structures.html#client.ayon_core.pipeline.create.structures.AttributeValues.data_to_store","title":"<code>data_to_store()</code>","text":"<p>Create new dictionary with data to store.</p> <p>Returns:</p> Type Description <p>Dict[str, Any]: Attribute values that should be stored.</p> Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>def data_to_store(self):\n    \"\"\"Create new dictionary with data to store.\n\n    Returns:\n        Dict[str, Any]: Attribute values that should be stored.\n    \"\"\"\n\n    output = {}\n    for key in self._data:\n        output[key] = self[key]\n\n    for key, attr_def in self._attr_defs_by_key.items():\n        if key not in output:\n            output[key] = attr_def.default\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/structures.html#client.ayon_core.pipeline.create.structures.AttributeValues.get_serialized_attr_defs","title":"<code>get_serialized_attr_defs()</code>","text":"<p>Serialize attribute definitions to json serializable types.</p> <p>Returns:</p> Type Description <p>List[Dict[str, Any]]: Serialized attribute definitions.</p> Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>def get_serialized_attr_defs(self):\n    \"\"\"Serialize attribute definitions to json serializable types.\n\n    Returns:\n        List[Dict[str, Any]]: Serialized attribute definitions.\n    \"\"\"\n\n    return serialize_attr_defs(self._attr_defs)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/structures.html#client.ayon_core.pipeline.create.structures.ButtonCallbackInfo","title":"<code>ButtonCallbackInfo</code>  <code>dataclass</code>","text":"<p>Button callback info passed to button definition callback.</p> Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>@dataclass\nclass ButtonCallbackInfo:\n    \"\"\"Button callback info passed to button definition callback.\"\"\"\n    create_context: \"CreateContext\"\n    instance_ids: list[str | None]\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/structures.html#client.ayon_core.pipeline.create.structures.ConvertorItem","title":"<code>ConvertorItem</code>","text":"<p>Item representing convertor plugin.</p> <p>Parameters:</p> Name Type Description Default <code>identifier</code> <code>str</code> <p>Identifier of convertor.</p> required <code>label</code> <code>str</code> <p>Label which will be shown in UI.</p> required Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>class ConvertorItem:\n    \"\"\"Item representing convertor plugin.\n\n    Args:\n        identifier (str): Identifier of convertor.\n        label (str): Label which will be shown in UI.\n    \"\"\"\n\n    def __init__(self, identifier, label):\n        self._id = str(uuid4())\n        self.identifier = identifier\n        self.label = label\n\n    @property\n    def id(self):\n        return self._id\n\n    def to_data(self):\n        return {\n            \"id\": self.id,\n            \"identifier\": self.identifier,\n            \"label\": self.label\n        }\n\n    @classmethod\n    def from_data(cls, data):\n        obj = cls(data[\"identifier\"], data[\"label\"])\n        obj._id = data[\"id\"]\n        return obj\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/structures.html#client.ayon_core.pipeline.create.structures.CreatedInstance","title":"<code>CreatedInstance</code>","text":"<p>Instance entity with data that will be stored to workfile.</p> <p>I think <code>data</code> must be required argument containing all minimum information about instance like \"folderPath\" and \"task\" and all data used for filling product name as creators may have custom data for product name filling.</p> Notes <p>Object have 2 possible initialization. One using 'creator' object which     is recommended for api usage. Second by passing information about     creator.</p> <p>Parameters:</p> Name Type Description Default <code>product_type</code> <code>str</code> <p>Product type that will be created.</p> required <code>product_name</code> <code>str</code> <p>Name of product that will be created.</p> required <code>data</code> <code>Dict[str, Any]</code> <p>Data used for filling product name or override data from already existing instance.</p> required <code>creator</code> <code>BaseCreator</code> <p>Creator responsible for instance.</p> required <code>product_base_type</code> <code>Optional[str]</code> <p>Product base type that will be created. If not provided then product base type is taken from creator plugin. If creator does not have product base type then deprecation warning is raised.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>class CreatedInstance:\n    \"\"\"Instance entity with data that will be stored to workfile.\n\n    I think `data` must be required argument containing all minimum information\n    about instance like \"folderPath\" and \"task\" and all data used for filling\n    product name as creators may have custom data for product name filling.\n\n    Notes:\n        Object have 2 possible initialization. One using 'creator' object which\n            is recommended for api usage. Second by passing information about\n            creator.\n\n    Args:\n        product_type (str): Product type that will be created.\n        product_name (str): Name of product that will be created.\n        data (Dict[str, Any]): Data used for filling product name or override\n            data from already existing instance.\n        creator (BaseCreator): Creator responsible for instance.\n        product_base_type (Optional[str]): Product base type that will be\n            created. If not provided then product base type is taken from\n            creator plugin. If creator does not have product base type then\n            deprecation warning is raised.\n    \"\"\"\n\n    # Keys that can't be changed or removed from data after loading using\n    #   creator.\n    # - 'creator_attributes' and 'publish_attributes' can change values of\n    #   their individual children but not on their own\n    __immutable_keys = (\n        \"id\",\n        \"instance_id\",\n        \"productType\",\n        \"productBaseType\",\n        \"creator_identifier\",\n        \"creator_attributes\",\n        \"publish_attributes\"\n    )\n    # Keys that can be changed, but should not be removed from instance\n    __required_keys = {\n        \"folderPath\": None,\n        \"task\": None,\n        \"productName\": None,\n        \"active\": True,\n    }\n\n    def __init__(\n        self,\n        product_type: str,\n        product_name: str,\n        data: Dict[str, Any],\n        creator: \"BaseCreator\",\n        transient_data: Optional[Dict[str, Any]] = None,\n        product_base_type: Optional[str] = None\n    ):\n        \"\"\"Initialize CreatedInstance.\"\"\"\n        # fallback to product type for backward compatibility\n        if not product_base_type:\n            product_base_type = creator.product_base_type or product_type\n\n        self._creator = creator\n        creator_identifier = creator.identifier\n        group_label = creator.get_group_label()\n        creator_label = creator.label\n\n        self._creator_label = creator_label\n        self._group_label = group_label or creator_identifier\n\n        # Instance members may have actions on them\n        # TODO implement members logic\n        self._members = []\n\n        # Data that can be used for lifetime of object\n        if transient_data is None:\n            transient_data = {}\n        self._transient_data = transient_data\n        self._is_mandatory: bool = False\n        self._parent_instance_id: Optional[str] = None\n        self._parent_flags: int = 0\n\n        # Create a copy of passed data to avoid changing them on the fly\n        data = copy.deepcopy(data or {})\n\n        # Pop dictionary values that will be converted to objects to be able\n        #   catch changes\n        orig_creator_attributes = data.pop(\"creator_attributes\", None) or {}\n        orig_publish_attributes = data.pop(\"publish_attributes\", None) or {}\n\n        # Store original value of passed data\n        self._orig_data = copy.deepcopy(data)\n\n        # Pop 'productType' and 'productName' to prevent unexpected changes\n        data.pop(\"productType\", None)\n        data.pop(\"productName\", None)\n        # Backwards compatibility with OpenPype instances\n        data.pop(\"family\", None)\n        data.pop(\"subset\", None)\n\n        asset_name = data.pop(\"asset\", None)\n        if \"folderPath\" not in data:\n            data[\"folderPath\"] = asset_name\n\n        # QUESTION Does it make sense to have data stored as ordered dict?\n        self._data = collections.OrderedDict()\n        # QUESTION Do we need this \"id\" information on instance?\n        item_id = data.get(\"id\")\n        # TODO use only 'AYON_INSTANCE_ID' when all hosts support it\n        if item_id not in {AYON_INSTANCE_ID, AVALON_INSTANCE_ID}:\n            item_id = AYON_INSTANCE_ID\n        self._data[\"id\"] = item_id\n        self._data[\"productType\"] = product_type\n        self._data[\"productName\"] = product_name\n\n        self._data[\"productBaseType\"] = product_base_type\n\n        self._data[\"active\"] = data.get(\"active\", True)\n        self._data[\"creator_identifier\"] = creator_identifier\n\n        # Pop from source data all keys that are defined in `_data` before\n        #   this moment and through their values away\n        # - they should be the same and if are not then should not change\n        #   already set values\n        for key in self._data.keys():\n            if key in data:\n                data.pop(key)\n\n        self._data[\"variant\"] = self._data.get(\"variant\") or \"\"\n        # Stored creator specific attribute values\n        # {key: value}\n        creator_values = copy.deepcopy(orig_creator_attributes)\n\n        self._data[\"creator_attributes\"] = creator_values\n\n        # Stored publish specific attribute values\n        # {&lt;plugin name&gt;: {key: value}}\n        self._data[\"publish_attributes\"] = PublishAttributes(\n            self, orig_publish_attributes\n        )\n        if data:\n            self._data.update(data)\n\n        for key, default in self.__required_keys.items():\n            self._data.setdefault(key, default)\n\n        if not self._data.get(\"instance_id\"):\n            self._data[\"instance_id\"] = str(uuid4())\n\n        creator_attr_defs = creator.get_attr_defs_for_instance(self)\n        self.set_create_attr_defs(\n            creator_attr_defs, creator_values\n        )\n\n    def __str__(self):\n        return (\n            \"&lt;CreatedInstance {product[name]}\"\n            \" ({product[type]}[{creator_identifier}])&gt; {data}\"\n        ).format(\n            creator_identifier=self.creator_identifier,\n            product={\"name\": self.product_name, \"type\": self.product_type},\n            data=str(self._data)\n        )\n\n    # --- Dictionary like methods ---\n    def __getitem__(self, key):\n        return self._data[key]\n\n    def __contains__(self, key):\n        return key in self._data\n\n    def __setitem__(self, key, value):\n        # Validate immutable keys\n        if key in self.__immutable_keys:\n            if value == self._data.get(key):\n                return\n            # Raise exception if key is immutable and value has changed\n            raise ImmutableKeyError(key)\n\n        if key in self._data and self._data[key] == value:\n            return\n\n        if self.is_mandatory and key == \"active\" and value is not True:\n            raise ImmutableKeyError(\n                key,\n                \"Instance is mandatory and can't be disabled.\"\n            )\n\n        self._data[key] = value\n        self._create_context.instance_values_changed(\n            self.id, {key: value}\n        )\n\n    def get(self, key, default=None):\n        return self._data.get(key, default)\n\n    def pop(self, key, *args, **kwargs):\n        # Raise exception if is trying to pop key which is immutable\n        if key in self.__immutable_keys:\n            raise ImmutableKeyError(key)\n\n        has_key = key in self._data\n        output = self._data.pop(key, *args, **kwargs)\n        if has_key:\n            if key in self.__required_keys:\n                self._data[key] = self.__required_keys[key]\n            self._create_context.instance_values_changed(\n                self.id, {key: None}\n            )\n        return output\n\n    def keys(self):\n        return self._data.keys()\n\n    def values(self):\n        return self._data.values()\n\n    def items(self):\n        return self._data.items()\n    # ------\n\n    @property\n    def product_base_type(self) -&gt; str:\n        return self._data[\"productBaseType\"]\n\n    @property\n    def product_type(self) -&gt; str:\n        return self._data[\"productType\"]\n\n    @property\n    def product_name(self) -&gt; str:\n        return self._data[\"productName\"]\n\n    @property\n    def label(self) -&gt; str:\n        label = self._data.get(\"label\")\n        if not label:\n            label = self.product_name\n        return label\n\n    @property\n    def group_label(self) -&gt; str:\n        label = self._data.get(\"group\")\n        if label:\n            return label\n        return self._group_label\n\n    @property\n    def origin_data(self):\n        output = copy.deepcopy(self._orig_data)\n        output[\"creator_attributes\"] = self.creator_attributes.origin_data\n        output[\"publish_attributes\"] = self.publish_attributes.origin_data\n        return output\n\n    @property\n    def creator_identifier(self):\n        return self._data[\"creator_identifier\"]\n\n    @property\n    def creator_label(self):\n        return self._creator.label or self.creator_identifier\n\n    @property\n    def id(self):\n        \"\"\"Instance identifier.\n\n        Returns:\n            str: UUID of instance.\n        \"\"\"\n\n        return self._data[\"instance_id\"]\n\n    @property\n    def data(self):\n        \"\"\"Legacy access to data.\n\n        Access to data is needed to modify values.\n\n        Returns:\n            CreatedInstance: Object can be used as dictionary but with\n                validations of immutable keys.\n        \"\"\"\n\n        return self\n\n    @property\n    def transient_data(self):\n        \"\"\"Data stored for lifetime of instance object.\n\n        These data are not stored to scene and will be lost on object\n        deletion.\n\n        Can be used to store objects. In some host implementations is not\n        possible to reference to object in scene with some unique identifier\n        (e.g. node in Fusion.). In that case it is handy to store the object\n        here. Should be used that way only if instance data are stored on the\n        node itself.\n\n        Returns:\n            Dict[str, Any]: Dictionary object where you can store data related\n                to instance for lifetime of instance object.\n        \"\"\"\n\n        return self._transient_data\n\n    @property\n    def is_mandatory(self) -&gt; bool:\n        \"\"\"Check if instance is mandatory.\n\n        Returns:\n            bool: True if instance is mandatory, False otherwise.\n\n        \"\"\"\n        return self._is_mandatory\n\n    def set_mandatory(self, value: bool) -&gt; None:\n        \"\"\"Set instance as mandatory or not.\n\n        Mandatory instance can't be disabled in UI.\n\n        Args:\n            value (bool): True if instance should be mandatory, False\n                otherwise.\n\n        \"\"\"\n        if value is self._is_mandatory:\n            return\n        self._is_mandatory = value\n        if value is True:\n            self[\"active\"] = True\n        self._create_context.instance_requirement_changed(self.id)\n\n    @property\n    def parent_instance_id(self) -&gt; Optional[str]:\n        return self._parent_instance_id\n\n    @property\n    def parent_flags(self) -&gt; int:\n        return self._parent_flags\n\n    def set_parent(\n        self, instance_id: Optional[str], flags: int\n    ) -&gt; None:\n        \"\"\"Set parent instance id and parenting flags.\n\n        Args:\n            instance_id (Optional[str]): Parent instance id.\n            flags (int): Parenting flags.\n\n        \"\"\"\n        changed = False\n        if instance_id != self._parent_instance_id:\n            changed = True\n            self._parent_instance_id = instance_id\n\n        if flags is None:\n            flags = 0\n\n        if self._parent_flags != flags:\n            self._parent_flags = flags\n            changed = True\n\n        if changed:\n            self._create_context.instance_parent_changed(self.id)\n\n    def changes(self):\n        \"\"\"Calculate and return changes.\"\"\"\n\n        return TrackChangesItem(self.origin_data, self.data_to_store())\n\n    def mark_as_stored(self):\n        \"\"\"Should be called when instance data are stored.\n\n        Origin data are replaced by current data so changes are cleared.\n        \"\"\"\n\n        orig_keys = set(self._orig_data.keys())\n        for key, value in self._data.items():\n            orig_keys.discard(key)\n            if key in (\"creator_attributes\", \"publish_attributes\"):\n                continue\n            self._orig_data[key] = copy.deepcopy(value)\n\n        for key in orig_keys:\n            self._orig_data.pop(key)\n\n        self.creator_attributes.mark_as_stored()\n        self.publish_attributes.mark_as_stored()\n\n    @property\n    def creator_attributes(self) -&gt; CreatorAttributeValues:\n        return self._data[\"creator_attributes\"]\n\n    @property\n    def creator_attribute_defs(self) -&gt; list[AbstractAttrDef]:\n        \"\"\"Attribute definitions defined by creator plugin.\n\n        Returns:\n              list[AbstractAttrDef]: Attribute definitions.\n\n        \"\"\"\n        return self.creator_attributes.attr_defs\n\n    @property\n    def publish_attributes(self) -&gt; PublishAttributes:\n        return self._data[\"publish_attributes\"]\n\n    @property\n    def has_promised_context(self) -&gt; bool:\n        \"\"\"Get context data that are promised to be set by creator.\n\n        Returns:\n            bool: Has context that won't bo validated. Artist can't change\n                value when set to True.\n\n        \"\"\"\n        return self._transient_data.get(\"has_promised_context\", False)\n\n    def data_to_store(self):\n        \"\"\"Collect data that contain json parsable types.\n\n        It is possible to recreate the instance using these data.\n\n        Todos:\n            We probably don't need OrderedDict. When data are loaded they\n                are not ordered anymore.\n\n        Returns:\n            OrderedDict: Ordered dictionary with instance data.\n        \"\"\"\n\n        output = collections.OrderedDict()\n        for key, value in self._data.items():\n            if key in (\"creator_attributes\", \"publish_attributes\"):\n                continue\n            output[key] = value\n\n        if isinstance(self.creator_attributes, AttributeValues):\n            creator_attributes = self.creator_attributes.data_to_store()\n        else:\n            creator_attributes = copy.deepcopy(self.creator_attributes)\n        output[\"creator_attributes\"] = creator_attributes\n        output[\"publish_attributes\"] = self.publish_attributes.data_to_store()\n\n        return output\n\n    def set_create_attr_defs(self, attr_defs, value=None):\n        \"\"\"Create plugin updates create attribute definitions.\n\n        Method called by create plugin when attribute definitions should\n            be changed.\n\n        Args:\n            attr_defs (List[AbstractAttrDef]): Attribute definitions.\n            value (Optional[Dict[str, Any]]): Values of attribute definitions.\n                Current values are used if not passed in.\n\n        \"\"\"\n        if value is None:\n            value = self._data[\"creator_attributes\"]\n\n        if isinstance(value, AttributeValues):\n            value = value.data_to_store()\n\n        if isinstance(self._data[\"creator_attributes\"], AttributeValues):\n            origin_data = self._data[\"creator_attributes\"].origin_data\n        else:\n            origin_data = copy.deepcopy(self._data[\"creator_attributes\"])\n        self._data[\"creator_attributes\"] = CreatorAttributeValues(\n            self,\n            \"creator_attributes\",\n            attr_defs,\n            value,\n            origin_data\n        )\n        self._create_context.instance_create_attr_defs_changed(self.id)\n\n    @classmethod\n    def from_existing(\n        cls,\n        instance_data: Dict[str, Any],\n        creator: \"BaseCreator\",\n        transient_data: Optional[Dict[str, Any]] = None,\n    ) -&gt; \"CreatedInstance\":\n        \"\"\"Convert instance data from workfile to CreatedInstance.\n\n        Args:\n            instance_data (Dict[str, Any]): Data in a structure ready for\n                'CreatedInstance' object.\n            creator (BaseCreator): Creator plugin which is creating the\n                instance of for which the instance belongs.\n            transient_data (Optional[dict[str, Any]]): Instance transient\n                data.\n\n        Returns:\n            CreatedInstance: Instance object.\n\n        \"\"\"\n        instance_data = copy.deepcopy(instance_data)\n\n        product_type = instance_data.get(\"productType\")\n        product_base_type = (\n            instance_data.get(\"productBaseType\")\n            or product_type\n            or instance_data.get(\"family\")\n            or creator.product_base_type\n        )\n        if product_type is None:\n            product_type = product_base_type\n\n        product_name = instance_data.get(\"productName\")\n        if product_name is None:\n            product_name = instance_data.get(\"subset\")\n\n        return cls(\n            product_base_type=product_base_type,\n            product_type=product_type,\n            product_name=product_name,\n            data=instance_data,\n            creator=creator,\n            transient_data=transient_data,\n        )\n\n    def attribute_value_changed(self, key, changes):\n        \"\"\"A value changed.\n\n        Args:\n            key (str): Key of attribute values.\n            changes (Dict[str, Any]): Changes in values.\n\n        \"\"\"\n        self._create_context.instance_values_changed(self.id, {key: changes})\n\n    def set_publish_plugin_attr_defs(self, plugin_name, attr_defs):\n        \"\"\"Set attribute definitions for publish plugin.\n\n        Args:\n            plugin_name(str): Name of publish plugin.\n            attr_defs(List[AbstractAttrDef]): Attribute definitions.\n\n        \"\"\"\n        self.publish_attributes.set_publish_plugin_attr_defs(\n            plugin_name, attr_defs\n        )\n        self._create_context.instance_publish_attr_defs_changed(\n            self.id, plugin_name\n        )\n\n    def publish_attribute_value_changed(self, plugin_name, value):\n        \"\"\"Method called from PublishAttributes.\n\n        Args:\n            plugin_name (str): Plugin name.\n            value (Dict[str, Any]): Changes in values for the plugin.\n\n        \"\"\"\n        self._create_context.instance_values_changed(\n            self.id,\n            {\n                \"publish_attributes\": {\n                    plugin_name: value,\n                },\n            },\n        )\n\n    def add_members(self, members):\n        \"\"\"Currently unused method.\"\"\"\n\n        for member in members:\n            if member not in self._members:\n                self._members.append(member)\n\n    @property\n    def _create_context(self):\n        \"\"\"Get create context.\n\n        Returns:\n            CreateContext: Context object which wraps object.\n\n        \"\"\"\n        return self._creator.create_context\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/structures.html#client.ayon_core.pipeline.create.structures.CreatedInstance.creator_attribute_defs","title":"<code>creator_attribute_defs</code>  <code>property</code>","text":"<p>Attribute definitions defined by creator plugin.</p> <p>Returns:</p> Type Description <code>list[AbstractAttrDef]</code> <p>list[AbstractAttrDef]: Attribute definitions.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/structures.html#client.ayon_core.pipeline.create.structures.CreatedInstance.data","title":"<code>data</code>  <code>property</code>","text":"<p>Legacy access to data.</p> <p>Access to data is needed to modify values.</p> <p>Returns:</p> Name Type Description <code>CreatedInstance</code> <p>Object can be used as dictionary but with validations of immutable keys.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/structures.html#client.ayon_core.pipeline.create.structures.CreatedInstance.has_promised_context","title":"<code>has_promised_context</code>  <code>property</code>","text":"<p>Get context data that are promised to be set by creator.</p> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>Has context that won't bo validated. Artist can't change value when set to True.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/structures.html#client.ayon_core.pipeline.create.structures.CreatedInstance.id","title":"<code>id</code>  <code>property</code>","text":"<p>Instance identifier.</p> <p>Returns:</p> Name Type Description <code>str</code> <p>UUID of instance.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/structures.html#client.ayon_core.pipeline.create.structures.CreatedInstance.is_mandatory","title":"<code>is_mandatory</code>  <code>property</code>","text":"<p>Check if instance is mandatory.</p> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if instance is mandatory, False otherwise.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/structures.html#client.ayon_core.pipeline.create.structures.CreatedInstance.transient_data","title":"<code>transient_data</code>  <code>property</code>","text":"<p>Data stored for lifetime of instance object.</p> <p>These data are not stored to scene and will be lost on object deletion.</p> <p>Can be used to store objects. In some host implementations is not possible to reference to object in scene with some unique identifier (e.g. node in Fusion.). In that case it is handy to store the object here. Should be used that way only if instance data are stored on the node itself.</p> <p>Returns:</p> Type Description <p>Dict[str, Any]: Dictionary object where you can store data related to instance for lifetime of instance object.</p>"},{"location":"autoapi/client/ayon_core/pipeline/create/structures.html#client.ayon_core.pipeline.create.structures.CreatedInstance.__init__","title":"<code>__init__(product_type, product_name, data, creator, transient_data=None, product_base_type=None)</code>","text":"<p>Initialize CreatedInstance.</p> Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>def __init__(\n    self,\n    product_type: str,\n    product_name: str,\n    data: Dict[str, Any],\n    creator: \"BaseCreator\",\n    transient_data: Optional[Dict[str, Any]] = None,\n    product_base_type: Optional[str] = None\n):\n    \"\"\"Initialize CreatedInstance.\"\"\"\n    # fallback to product type for backward compatibility\n    if not product_base_type:\n        product_base_type = creator.product_base_type or product_type\n\n    self._creator = creator\n    creator_identifier = creator.identifier\n    group_label = creator.get_group_label()\n    creator_label = creator.label\n\n    self._creator_label = creator_label\n    self._group_label = group_label or creator_identifier\n\n    # Instance members may have actions on them\n    # TODO implement members logic\n    self._members = []\n\n    # Data that can be used for lifetime of object\n    if transient_data is None:\n        transient_data = {}\n    self._transient_data = transient_data\n    self._is_mandatory: bool = False\n    self._parent_instance_id: Optional[str] = None\n    self._parent_flags: int = 0\n\n    # Create a copy of passed data to avoid changing them on the fly\n    data = copy.deepcopy(data or {})\n\n    # Pop dictionary values that will be converted to objects to be able\n    #   catch changes\n    orig_creator_attributes = data.pop(\"creator_attributes\", None) or {}\n    orig_publish_attributes = data.pop(\"publish_attributes\", None) or {}\n\n    # Store original value of passed data\n    self._orig_data = copy.deepcopy(data)\n\n    # Pop 'productType' and 'productName' to prevent unexpected changes\n    data.pop(\"productType\", None)\n    data.pop(\"productName\", None)\n    # Backwards compatibility with OpenPype instances\n    data.pop(\"family\", None)\n    data.pop(\"subset\", None)\n\n    asset_name = data.pop(\"asset\", None)\n    if \"folderPath\" not in data:\n        data[\"folderPath\"] = asset_name\n\n    # QUESTION Does it make sense to have data stored as ordered dict?\n    self._data = collections.OrderedDict()\n    # QUESTION Do we need this \"id\" information on instance?\n    item_id = data.get(\"id\")\n    # TODO use only 'AYON_INSTANCE_ID' when all hosts support it\n    if item_id not in {AYON_INSTANCE_ID, AVALON_INSTANCE_ID}:\n        item_id = AYON_INSTANCE_ID\n    self._data[\"id\"] = item_id\n    self._data[\"productType\"] = product_type\n    self._data[\"productName\"] = product_name\n\n    self._data[\"productBaseType\"] = product_base_type\n\n    self._data[\"active\"] = data.get(\"active\", True)\n    self._data[\"creator_identifier\"] = creator_identifier\n\n    # Pop from source data all keys that are defined in `_data` before\n    #   this moment and through their values away\n    # - they should be the same and if are not then should not change\n    #   already set values\n    for key in self._data.keys():\n        if key in data:\n            data.pop(key)\n\n    self._data[\"variant\"] = self._data.get(\"variant\") or \"\"\n    # Stored creator specific attribute values\n    # {key: value}\n    creator_values = copy.deepcopy(orig_creator_attributes)\n\n    self._data[\"creator_attributes\"] = creator_values\n\n    # Stored publish specific attribute values\n    # {&lt;plugin name&gt;: {key: value}}\n    self._data[\"publish_attributes\"] = PublishAttributes(\n        self, orig_publish_attributes\n    )\n    if data:\n        self._data.update(data)\n\n    for key, default in self.__required_keys.items():\n        self._data.setdefault(key, default)\n\n    if not self._data.get(\"instance_id\"):\n        self._data[\"instance_id\"] = str(uuid4())\n\n    creator_attr_defs = creator.get_attr_defs_for_instance(self)\n    self.set_create_attr_defs(\n        creator_attr_defs, creator_values\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/structures.html#client.ayon_core.pipeline.create.structures.CreatedInstance.add_members","title":"<code>add_members(members)</code>","text":"<p>Currently unused method.</p> Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>def add_members(self, members):\n    \"\"\"Currently unused method.\"\"\"\n\n    for member in members:\n        if member not in self._members:\n            self._members.append(member)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/structures.html#client.ayon_core.pipeline.create.structures.CreatedInstance.attribute_value_changed","title":"<code>attribute_value_changed(key, changes)</code>","text":"<p>A value changed.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>Key of attribute values.</p> required <code>changes</code> <code>Dict[str, Any]</code> <p>Changes in values.</p> required Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>def attribute_value_changed(self, key, changes):\n    \"\"\"A value changed.\n\n    Args:\n        key (str): Key of attribute values.\n        changes (Dict[str, Any]): Changes in values.\n\n    \"\"\"\n    self._create_context.instance_values_changed(self.id, {key: changes})\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/structures.html#client.ayon_core.pipeline.create.structures.CreatedInstance.changes","title":"<code>changes()</code>","text":"<p>Calculate and return changes.</p> Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>def changes(self):\n    \"\"\"Calculate and return changes.\"\"\"\n\n    return TrackChangesItem(self.origin_data, self.data_to_store())\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/structures.html#client.ayon_core.pipeline.create.structures.CreatedInstance.data_to_store","title":"<code>data_to_store()</code>","text":"<p>Collect data that contain json parsable types.</p> <p>It is possible to recreate the instance using these data.</p> Todos <p>We probably don't need OrderedDict. When data are loaded they     are not ordered anymore.</p> <p>Returns:</p> Name Type Description <code>OrderedDict</code> <p>Ordered dictionary with instance data.</p> Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>def data_to_store(self):\n    \"\"\"Collect data that contain json parsable types.\n\n    It is possible to recreate the instance using these data.\n\n    Todos:\n        We probably don't need OrderedDict. When data are loaded they\n            are not ordered anymore.\n\n    Returns:\n        OrderedDict: Ordered dictionary with instance data.\n    \"\"\"\n\n    output = collections.OrderedDict()\n    for key, value in self._data.items():\n        if key in (\"creator_attributes\", \"publish_attributes\"):\n            continue\n        output[key] = value\n\n    if isinstance(self.creator_attributes, AttributeValues):\n        creator_attributes = self.creator_attributes.data_to_store()\n    else:\n        creator_attributes = copy.deepcopy(self.creator_attributes)\n    output[\"creator_attributes\"] = creator_attributes\n    output[\"publish_attributes\"] = self.publish_attributes.data_to_store()\n\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/structures.html#client.ayon_core.pipeline.create.structures.CreatedInstance.from_existing","title":"<code>from_existing(instance_data, creator, transient_data=None)</code>  <code>classmethod</code>","text":"<p>Convert instance data from workfile to CreatedInstance.</p> <p>Parameters:</p> Name Type Description Default <code>instance_data</code> <code>Dict[str, Any]</code> <p>Data in a structure ready for 'CreatedInstance' object.</p> required <code>creator</code> <code>BaseCreator</code> <p>Creator plugin which is creating the instance of for which the instance belongs.</p> required <code>transient_data</code> <code>Optional[dict[str, Any]]</code> <p>Instance transient data.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>CreatedInstance</code> <code>'CreatedInstance'</code> <p>Instance object.</p> Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>@classmethod\ndef from_existing(\n    cls,\n    instance_data: Dict[str, Any],\n    creator: \"BaseCreator\",\n    transient_data: Optional[Dict[str, Any]] = None,\n) -&gt; \"CreatedInstance\":\n    \"\"\"Convert instance data from workfile to CreatedInstance.\n\n    Args:\n        instance_data (Dict[str, Any]): Data in a structure ready for\n            'CreatedInstance' object.\n        creator (BaseCreator): Creator plugin which is creating the\n            instance of for which the instance belongs.\n        transient_data (Optional[dict[str, Any]]): Instance transient\n            data.\n\n    Returns:\n        CreatedInstance: Instance object.\n\n    \"\"\"\n    instance_data = copy.deepcopy(instance_data)\n\n    product_type = instance_data.get(\"productType\")\n    product_base_type = (\n        instance_data.get(\"productBaseType\")\n        or product_type\n        or instance_data.get(\"family\")\n        or creator.product_base_type\n    )\n    if product_type is None:\n        product_type = product_base_type\n\n    product_name = instance_data.get(\"productName\")\n    if product_name is None:\n        product_name = instance_data.get(\"subset\")\n\n    return cls(\n        product_base_type=product_base_type,\n        product_type=product_type,\n        product_name=product_name,\n        data=instance_data,\n        creator=creator,\n        transient_data=transient_data,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/structures.html#client.ayon_core.pipeline.create.structures.CreatedInstance.mark_as_stored","title":"<code>mark_as_stored()</code>","text":"<p>Should be called when instance data are stored.</p> <p>Origin data are replaced by current data so changes are cleared.</p> Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>def mark_as_stored(self):\n    \"\"\"Should be called when instance data are stored.\n\n    Origin data are replaced by current data so changes are cleared.\n    \"\"\"\n\n    orig_keys = set(self._orig_data.keys())\n    for key, value in self._data.items():\n        orig_keys.discard(key)\n        if key in (\"creator_attributes\", \"publish_attributes\"):\n            continue\n        self._orig_data[key] = copy.deepcopy(value)\n\n    for key in orig_keys:\n        self._orig_data.pop(key)\n\n    self.creator_attributes.mark_as_stored()\n    self.publish_attributes.mark_as_stored()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/structures.html#client.ayon_core.pipeline.create.structures.CreatedInstance.publish_attribute_value_changed","title":"<code>publish_attribute_value_changed(plugin_name, value)</code>","text":"<p>Method called from PublishAttributes.</p> <p>Parameters:</p> Name Type Description Default <code>plugin_name</code> <code>str</code> <p>Plugin name.</p> required <code>value</code> <code>Dict[str, Any]</code> <p>Changes in values for the plugin.</p> required Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>def publish_attribute_value_changed(self, plugin_name, value):\n    \"\"\"Method called from PublishAttributes.\n\n    Args:\n        plugin_name (str): Plugin name.\n        value (Dict[str, Any]): Changes in values for the plugin.\n\n    \"\"\"\n    self._create_context.instance_values_changed(\n        self.id,\n        {\n            \"publish_attributes\": {\n                plugin_name: value,\n            },\n        },\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/structures.html#client.ayon_core.pipeline.create.structures.CreatedInstance.set_create_attr_defs","title":"<code>set_create_attr_defs(attr_defs, value=None)</code>","text":"<p>Create plugin updates create attribute definitions.</p> <p>Method called by create plugin when attribute definitions should     be changed.</p> <p>Parameters:</p> Name Type Description Default <code>attr_defs</code> <code>List[AbstractAttrDef]</code> <p>Attribute definitions.</p> required <code>value</code> <code>Optional[Dict[str, Any]]</code> <p>Values of attribute definitions. Current values are used if not passed in.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>def set_create_attr_defs(self, attr_defs, value=None):\n    \"\"\"Create plugin updates create attribute definitions.\n\n    Method called by create plugin when attribute definitions should\n        be changed.\n\n    Args:\n        attr_defs (List[AbstractAttrDef]): Attribute definitions.\n        value (Optional[Dict[str, Any]]): Values of attribute definitions.\n            Current values are used if not passed in.\n\n    \"\"\"\n    if value is None:\n        value = self._data[\"creator_attributes\"]\n\n    if isinstance(value, AttributeValues):\n        value = value.data_to_store()\n\n    if isinstance(self._data[\"creator_attributes\"], AttributeValues):\n        origin_data = self._data[\"creator_attributes\"].origin_data\n    else:\n        origin_data = copy.deepcopy(self._data[\"creator_attributes\"])\n    self._data[\"creator_attributes\"] = CreatorAttributeValues(\n        self,\n        \"creator_attributes\",\n        attr_defs,\n        value,\n        origin_data\n    )\n    self._create_context.instance_create_attr_defs_changed(self.id)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/structures.html#client.ayon_core.pipeline.create.structures.CreatedInstance.set_mandatory","title":"<code>set_mandatory(value)</code>","text":"<p>Set instance as mandatory or not.</p> <p>Mandatory instance can't be disabled in UI.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>bool</code> <p>True if instance should be mandatory, False otherwise.</p> required Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>def set_mandatory(self, value: bool) -&gt; None:\n    \"\"\"Set instance as mandatory or not.\n\n    Mandatory instance can't be disabled in UI.\n\n    Args:\n        value (bool): True if instance should be mandatory, False\n            otherwise.\n\n    \"\"\"\n    if value is self._is_mandatory:\n        return\n    self._is_mandatory = value\n    if value is True:\n        self[\"active\"] = True\n    self._create_context.instance_requirement_changed(self.id)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/structures.html#client.ayon_core.pipeline.create.structures.CreatedInstance.set_parent","title":"<code>set_parent(instance_id, flags)</code>","text":"<p>Set parent instance id and parenting flags.</p> <p>Parameters:</p> Name Type Description Default <code>instance_id</code> <code>Optional[str]</code> <p>Parent instance id.</p> required <code>flags</code> <code>int</code> <p>Parenting flags.</p> required Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>def set_parent(\n    self, instance_id: Optional[str], flags: int\n) -&gt; None:\n    \"\"\"Set parent instance id and parenting flags.\n\n    Args:\n        instance_id (Optional[str]): Parent instance id.\n        flags (int): Parenting flags.\n\n    \"\"\"\n    changed = False\n    if instance_id != self._parent_instance_id:\n        changed = True\n        self._parent_instance_id = instance_id\n\n    if flags is None:\n        flags = 0\n\n    if self._parent_flags != flags:\n        self._parent_flags = flags\n        changed = True\n\n    if changed:\n        self._create_context.instance_parent_changed(self.id)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/structures.html#client.ayon_core.pipeline.create.structures.CreatedInstance.set_publish_plugin_attr_defs","title":"<code>set_publish_plugin_attr_defs(plugin_name, attr_defs)</code>","text":"<p>Set attribute definitions for publish plugin.</p> <p>Parameters:</p> Name Type Description Default <code>plugin_name</code> <code>str</code> <p>Name of publish plugin.</p> required <code>attr_defs</code> <code>List[AbstractAttrDef]</code> <p>Attribute definitions.</p> required Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>def set_publish_plugin_attr_defs(self, plugin_name, attr_defs):\n    \"\"\"Set attribute definitions for publish plugin.\n\n    Args:\n        plugin_name(str): Name of publish plugin.\n        attr_defs(List[AbstractAttrDef]): Attribute definitions.\n\n    \"\"\"\n    self.publish_attributes.set_publish_plugin_attr_defs(\n        plugin_name, attr_defs\n    )\n    self._create_context.instance_publish_attr_defs_changed(\n        self.id, plugin_name\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/structures.html#client.ayon_core.pipeline.create.structures.CreatorAttributeValues","title":"<code>CreatorAttributeValues</code>","text":"<p>               Bases: <code>AttributeValues</code></p> <p>Creator specific attribute values of an instance.</p> Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>class CreatorAttributeValues(AttributeValues):\n    \"\"\"Creator specific attribute values of an instance.\"\"\"\n\n    @property\n    def instance(self):\n        return self._parent\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/structures.html#client.ayon_core.pipeline.create.structures.InstanceMember","title":"<code>InstanceMember</code>","text":"<p>Representation of instance member.</p> <p>TODO: Implement and use!</p> Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>class InstanceMember:\n    \"\"\"Representation of instance member.\n\n    TODO:\n    Implement and use!\n    \"\"\"\n\n    def __init__(self, instance, name):\n        self.instance = instance\n\n        instance.add_members(self)\n\n        self.name = name\n        self._actions = []\n\n    def add_action(self, label, callback):\n        self._actions.append({\n            \"label\": label,\n            \"callback\": callback\n        })\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/structures.html#client.ayon_core.pipeline.create.structures.IntEnum","title":"<code>IntEnum</code>","text":"<p>               Bases: <code>int</code>, <code>Enum</code></p> <p>An int-based Enum class that allows for int comparison.</p> Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>class IntEnum(int, Enum):\n    \"\"\"An int-based Enum class that allows for int comparison.\"\"\"\n\n    def __int__(self) -&gt; int:\n        return self.value\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/structures.html#client.ayon_core.pipeline.create.structures.ProductTypeItem","title":"<code>ProductTypeItem</code>  <code>dataclass</code>","text":"<p>Structure to define product types for a create plugin.</p> <p>Product types can be used as studio definitions of a product base type.</p> <p>It is expected that attributes will be added/removed in the future,     in that regards it is recommended to use pre-defined methods     'new' and 'from_data' that do safely handle those cases and     can contain backwards/forwards compatibility if needed.</p> <p>Attributes:</p> Name Type Description <code>product_type</code> <code>str</code> <p>Name of a product type.</p> <code>label</code> <code>str</code> <p>Product type label shown in Create view.</p> Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>@dataclass\nclass ProductTypeItem:\n    \"\"\"Structure to define product types for a create plugin.\n\n    Product types can be used as studio definitions of a product base type.\n\n    It is expected that attributes will be added/removed in the future,\n        in that regards it is recommended to use pre-defined methods\n        'new' and 'from_data' that do safely handle those cases and\n        can contain backwards/forwards compatibility if needed.\n\n    Attributes:\n        product_type (str): Name of a product type.\n        label (str): Product type label shown in Create view.\n\n    \"\"\"\n    product_type: str\n    label: Optional[str] = None\n\n    @classmethod\n    def new(\n        cls,\n        *,\n        product_type: str,\n        label: str,\n        **unknown,\n    ) -&gt; \"ProductTypeItem\":\n        if unknown:\n            unknown_keys = \", \".join(f\"'{k}'\" for k in unknown.keys())\n            log.info(\n                f\"Unknown keys in ProductTypeItem: {unknown_keys}\"\n            )\n\n        return cls(\n            product_type=product_type,\n            label=label,\n        )\n\n    @classmethod\n    def from_data(cls, data: dict[str, Any]) -&gt; \"ProductTypeItem\":\n        return cls.new(**data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/structures.html#client.ayon_core.pipeline.create.structures.PublishAttributeValues","title":"<code>PublishAttributeValues</code>","text":"<p>               Bases: <code>AttributeValues</code></p> <p>Publish plugin specific attribute values.</p> <p>Values are for single plugin which can be on <code>CreatedInstance</code> or context values stored on <code>CreateContext</code>.</p> Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>class PublishAttributeValues(AttributeValues):\n    \"\"\"Publish plugin specific attribute values.\n\n    Values are for single plugin which can be on `CreatedInstance`\n    or context values stored on `CreateContext`.\n    \"\"\"\n\n    @property\n    def publish_attributes(self):\n        return self._parent\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/structures.html#client.ayon_core.pipeline.create.structures.PublishAttributes","title":"<code>PublishAttributes</code>","text":"<p>Wrapper for publish plugin attribute definitions.</p> <p>Cares about handling attribute definitions of multiple publish plugins. Keep information about attribute definitions and their values.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <code>(CreatedInstance, CreateContext)</code> <p>Parent for which will be data stored and from which are data loaded.</p> required <code>origin_data</code> <code>dict</code> <p>Loaded data by plugin class name.</p> required Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>class PublishAttributes:\n    \"\"\"Wrapper for publish plugin attribute definitions.\n\n    Cares about handling attribute definitions of multiple publish plugins.\n    Keep information about attribute definitions and their values.\n\n    Args:\n        parent(CreatedInstance, CreateContext): Parent for which will be\n            data stored and from which are data loaded.\n        origin_data(dict): Loaded data by plugin class name.\n\n    \"\"\"\n    def __init__(self, parent, origin_data):\n        self._parent = parent\n        self._origin_data = copy.deepcopy(origin_data)\n\n        self._data = copy.deepcopy(origin_data)\n\n    def __getitem__(self, key):\n        return self._data[key]\n\n    def __setitem__(self, key, value):\n        \"\"\"Set value for plugin.\n\n        Args:\n            key (str): Plugin name.\n            value (dict[str, Any]): Value to set.\n\n        \"\"\"\n        current_value = self._data.get(key)\n        if isinstance(current_value, PublishAttributeValues):\n            current_value.set_value(value)\n        else:\n            self._data[key] = value\n\n    def __delitem__(self, key):\n        self.pop(key)\n\n    def __contains__(self, key):\n        return key in self._data\n\n    def keys(self):\n        return self._data.keys()\n\n    def values(self):\n        return self._data.values()\n\n    def items(self):\n        return self._data.items()\n\n    def get(self, key, default=None):\n        return self._data.get(key, default)\n\n    def pop(self, key, default=None):\n        \"\"\"Remove or reset value for plugin.\n\n        Plugin values are reset to defaults if plugin is available but\n        data of plugin which was not found are removed.\n\n        Args:\n            key(str): Plugin name.\n            default: Default value if plugin was not found.\n        \"\"\"\n\n        if key not in self._data:\n            return default\n\n        value = self._data[key]\n        if not isinstance(value, AttributeValues):\n            self.attribute_value_changed(key, None)\n            return self._data.pop(key)\n\n        value_item = self._data[key]\n        # Prepare value to return\n        output = value_item.data_to_store()\n        # Reset values\n        value_item.reset_values()\n        self.attribute_value_changed(\n            key, value_item.data_to_store()\n        )\n        return output\n\n    def mark_as_stored(self):\n        self._origin_data = copy.deepcopy(self.data_to_store())\n\n    def data_to_store(self):\n        \"\"\"Convert attribute values to \"data to store\".\"\"\"\n        output = {}\n        for key, attr_value in self._data.items():\n            if isinstance(attr_value, AttributeValues):\n                output[key] = attr_value.data_to_store()\n            else:\n                output[key] = attr_value\n        return output\n\n    @property\n    def origin_data(self):\n        return copy.deepcopy(self._origin_data)\n\n    def attribute_value_changed(self, key, changes):\n        self._parent.publish_attribute_value_changed(key, changes)\n\n    def set_publish_plugin_attr_defs(\n        self,\n        plugin_name: str,\n        attr_defs: List[AbstractAttrDef],\n        value: Optional[Dict[str, Any]] = None\n    ):\n        \"\"\"Set attribute definitions for plugin.\n\n        Args:\n            plugin_name (str): Name of plugin.\n            attr_defs (List[AbstractAttrDef]): Attribute definitions.\n            value (Optional[Dict[str, Any]]): Attribute values.\n\n        \"\"\"\n        # TODO what if 'attr_defs' is 'None'?\n        if value is None:\n            value = self._data.get(plugin_name)\n\n        if value is None:\n            value = {}\n\n        self._data[plugin_name] = PublishAttributeValues(\n            self, plugin_name, attr_defs, value, value\n        )\n\n    def serialize_attributes(self):\n        return {\n            \"attr_defs\": {\n                plugin_name: attrs_value.get_serialized_attr_defs()\n                for plugin_name, attrs_value in self._data.items()\n            },\n        }\n\n    def deserialize_attributes(self, data):\n        attr_defs = deserialize_attr_defs(data[\"attr_defs\"])\n\n        origin_data = self._origin_data\n        data = self._data\n        self._data = {}\n\n        added_keys = set()\n        for plugin_name, attr_defs_data in attr_defs.items():\n            attr_defs = deserialize_attr_defs(attr_defs_data)\n            value = data.get(plugin_name) or {}\n            orig_value = copy.deepcopy(origin_data.get(plugin_name) or {})\n            self._data[plugin_name] = PublishAttributeValues(\n                self, plugin_name, attr_defs, value, orig_value\n            )\n\n        for key, value in data.items():\n            if key not in added_keys:\n                self._data[key] = value\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/structures.html#client.ayon_core.pipeline.create.structures.PublishAttributes.__setitem__","title":"<code>__setitem__(key, value)</code>","text":"<p>Set value for plugin.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>Plugin name.</p> required <code>value</code> <code>dict[str, Any]</code> <p>Value to set.</p> required Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>def __setitem__(self, key, value):\n    \"\"\"Set value for plugin.\n\n    Args:\n        key (str): Plugin name.\n        value (dict[str, Any]): Value to set.\n\n    \"\"\"\n    current_value = self._data.get(key)\n    if isinstance(current_value, PublishAttributeValues):\n        current_value.set_value(value)\n    else:\n        self._data[key] = value\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/structures.html#client.ayon_core.pipeline.create.structures.PublishAttributes.data_to_store","title":"<code>data_to_store()</code>","text":"<p>Convert attribute values to \"data to store\".</p> Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>def data_to_store(self):\n    \"\"\"Convert attribute values to \"data to store\".\"\"\"\n    output = {}\n    for key, attr_value in self._data.items():\n        if isinstance(attr_value, AttributeValues):\n            output[key] = attr_value.data_to_store()\n        else:\n            output[key] = attr_value\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/structures.html#client.ayon_core.pipeline.create.structures.PublishAttributes.pop","title":"<code>pop(key, default=None)</code>","text":"<p>Remove or reset value for plugin.</p> <p>Plugin values are reset to defaults if plugin is available but data of plugin which was not found are removed.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>Plugin name.</p> required <code>default</code> <p>Default value if plugin was not found.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>def pop(self, key, default=None):\n    \"\"\"Remove or reset value for plugin.\n\n    Plugin values are reset to defaults if plugin is available but\n    data of plugin which was not found are removed.\n\n    Args:\n        key(str): Plugin name.\n        default: Default value if plugin was not found.\n    \"\"\"\n\n    if key not in self._data:\n        return default\n\n    value = self._data[key]\n    if not isinstance(value, AttributeValues):\n        self.attribute_value_changed(key, None)\n        return self._data.pop(key)\n\n    value_item = self._data[key]\n    # Prepare value to return\n    output = value_item.data_to_store()\n    # Reset values\n    value_item.reset_values()\n    self.attribute_value_changed(\n        key, value_item.data_to_store()\n    )\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/structures.html#client.ayon_core.pipeline.create.structures.PublishAttributes.set_publish_plugin_attr_defs","title":"<code>set_publish_plugin_attr_defs(plugin_name, attr_defs, value=None)</code>","text":"<p>Set attribute definitions for plugin.</p> <p>Parameters:</p> Name Type Description Default <code>plugin_name</code> <code>str</code> <p>Name of plugin.</p> required <code>attr_defs</code> <code>List[AbstractAttrDef]</code> <p>Attribute definitions.</p> required <code>value</code> <code>Optional[Dict[str, Any]]</code> <p>Attribute values.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/create/structures.py</code> <pre><code>def set_publish_plugin_attr_defs(\n    self,\n    plugin_name: str,\n    attr_defs: List[AbstractAttrDef],\n    value: Optional[Dict[str, Any]] = None\n):\n    \"\"\"Set attribute definitions for plugin.\n\n    Args:\n        plugin_name (str): Name of plugin.\n        attr_defs (List[AbstractAttrDef]): Attribute definitions.\n        value (Optional[Dict[str, Any]]): Attribute values.\n\n    \"\"\"\n    # TODO what if 'attr_defs' is 'None'?\n    if value is None:\n        value = self._data.get(plugin_name)\n\n    if value is None:\n        value = {}\n\n    self._data[plugin_name] = PublishAttributeValues(\n        self, plugin_name, attr_defs, value, value\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/utils.html","title":"utils","text":""},{"location":"autoapi/client/ayon_core/pipeline/create/utils.html#client.ayon_core.pipeline.create.utils.get_last_versions_for_instances","title":"<code>get_last_versions_for_instances(project_name, instances, use_value_for_missing=False)</code>","text":"<p>Get last versions for instances by their folder path and product name.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>instances</code> <code>list[CreatedInstance]</code> <p>Instances to get next versions for.</p> required <code>use_value_for_missing</code> <code>Optional[bool]</code> <p>Missing values are replaced with negative value if True. Otherwise None is used. -2 is used for instances without filled folder or product name. -1 is used for missing entities.</p> <code>False</code> <p>Returns:</p> Type Description <p>dict[str, Union[int, None]]: Last versions by instance id.</p> Source code in <code>client/ayon_core/pipeline/create/utils.py</code> <pre><code>def get_last_versions_for_instances(\n    project_name, instances, use_value_for_missing=False\n):\n    \"\"\"Get last versions for instances by their folder path and product name.\n\n    Args:\n        project_name (str): Project name.\n        instances (list[CreatedInstance]): Instances to get next versions for.\n        use_value_for_missing (Optional[bool]): Missing values are replaced\n            with negative value if True. Otherwise None is used. -2 is used\n            for instances without filled folder or product name. -1 is used\n            for missing entities.\n\n    Returns:\n        dict[str, Union[int, None]]: Last versions by instance id.\n    \"\"\"\n\n    output = {\n        instance.id: -1 if use_value_for_missing else None\n        for instance in instances\n    }\n    product_names_by_folder_path = collections.defaultdict(set)\n    instances_by_hierarchy = {}\n    for instance in instances:\n        folder_path = instance.data.get(\"folderPath\")\n        product_name = instance.product_name\n        if not folder_path or not product_name:\n            if use_value_for_missing:\n                output[instance.id] = -2\n            continue\n\n        (\n            instances_by_hierarchy\n            .setdefault(folder_path, {})\n            .setdefault(product_name, [])\n            .append(instance)\n        )\n        product_names_by_folder_path[folder_path].add(product_name)\n\n    product_names = set()\n    for names in product_names_by_folder_path.values():\n        product_names |= names\n\n    if not product_names:\n        return output\n\n    folder_entities = ayon_api.get_folders(\n        project_name,\n        folder_paths=product_names_by_folder_path.keys(),\n        fields={\"id\", \"path\"}\n    )\n    folder_paths_by_id = {\n        folder_entity[\"id\"]: folder_entity[\"path\"]\n        for folder_entity in folder_entities\n    }\n    if not folder_paths_by_id:\n        return output\n\n    product_entities = ayon_api.get_products(\n        project_name,\n        folder_ids=folder_paths_by_id.keys(),\n        product_names=product_names,\n        fields={\"id\", \"name\", \"folderId\"}\n    )\n    product_entities_by_id = {}\n    for product_entity in product_entities:\n        # Filter product entities by names under parent\n        folder_id = product_entity[\"folderId\"]\n        product_name = product_entity[\"name\"]\n        folder_path = folder_paths_by_id[folder_id]\n        if product_name not in product_names_by_folder_path[folder_path]:\n            continue\n        product_entities_by_id[product_entity[\"id\"]] = product_entity\n\n    if not product_entities_by_id:\n        return output\n\n    last_versions_by_product_id = ayon_api.get_last_versions(\n        project_name,\n        product_entities_by_id.keys(),\n        fields={\"version\", \"productId\"}\n    )\n    for product_id, version_entity in last_versions_by_product_id.items():\n        product_entity = product_entities_by_id[product_id]\n        product_name = product_entity[\"name\"]\n        folder_id = product_entity[\"folderId\"]\n        folder_path = folder_paths_by_id[folder_id]\n        _instances = instances_by_hierarchy[folder_path][product_name]\n        for instance in _instances:\n            output[instance.id] = version_entity[\"version\"]\n\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/create/utils.html#client.ayon_core.pipeline.create.utils.get_next_versions_for_instances","title":"<code>get_next_versions_for_instances(project_name, instances)</code>","text":"<p>Get next versions for instances by their folder path and product name.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>instances</code> <code>list[CreatedInstance]</code> <p>Instances to get next versions for.</p> required <p>Returns:</p> Type Description <p>dict[str, Union[int, None]]: Next versions by instance id. Version is 'None' if instance has no folder path or product name.</p> Source code in <code>client/ayon_core/pipeline/create/utils.py</code> <pre><code>def get_next_versions_for_instances(project_name, instances):\n    \"\"\"Get next versions for instances by their folder path and product name.\n\n    Args:\n        project_name (str): Project name.\n        instances (list[CreatedInstance]): Instances to get next versions for.\n\n    Returns:\n        dict[str, Union[int, None]]: Next versions by instance id. Version is\n            'None' if instance has no folder path or product name.\n    \"\"\"\n\n    last_versions = get_last_versions_for_instances(\n        project_name, instances, True)\n\n    output = {}\n    for instance_id, version in last_versions.items():\n        if version == -2:\n            output[instance_id] = None\n        elif version == -1:\n            output[instance_id] = 1\n        else:\n            output[instance_id] = version + 1\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/farm/index.html","title":"farm","text":""},{"location":"autoapi/client/ayon_core/pipeline/farm/patterning.html","title":"patterning","text":""},{"location":"autoapi/client/ayon_core/pipeline/farm/patterning.html#client.ayon_core.pipeline.farm.patterning.match_aov_pattern","title":"<code>match_aov_pattern(host_name, aov_patterns, render_file_name)</code>","text":"<p>Matching against a <code>AOV</code> pattern in the render files.</p> <p>In order to match the AOV name we must compare against the render filename string that we are grabbing the render filename string  from the collection that we have grabbed from <code>exp_files</code>.</p> <p>Parameters:</p> Name Type Description Default <code>host_name</code> <code>str</code> <p>Host name.</p> required <code>aov_patterns</code> <code>dict</code> <p>AOV patterns from AOV filters.</p> required <code>render_file_name</code> <code>str</code> <p>Incoming file name to match against.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <p>Review state for rendered file (render_file_name).</p> Source code in <code>client/ayon_core/pipeline/farm/patterning.py</code> <pre><code>def match_aov_pattern(host_name, aov_patterns, render_file_name):\n    \"\"\"Matching against a `AOV` pattern in the render files.\n\n    In order to match the AOV name we must compare\n    against the render filename string that we are\n    grabbing the render filename string  from the collection\n    that we have grabbed from `exp_files`.\n\n    Args:\n        host_name (str): Host name.\n        aov_patterns (dict):  AOV patterns from AOV filters.\n        render_file_name (str): Incoming file name to match against.\n\n    Returns:\n        bool: Review state for rendered file (render_file_name).\n    \"\"\"\n    aov_pattern = aov_patterns.get(host_name, [])\n    if not aov_pattern:\n        return False\n    return any(re.match(p, render_file_name) for p in aov_pattern)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/farm/pyblish_functions.html","title":"pyblish_functions","text":""},{"location":"autoapi/client/ayon_core/pipeline/farm/pyblish_functions.html#client.ayon_core.pipeline.farm.pyblish_functions.TimeData","title":"<code>TimeData</code>","text":"<p>               Bases: <code>object</code></p> <p>Structure used to handle time related data.</p> Source code in <code>client/ayon_core/pipeline/farm/pyblish_functions.py</code> <pre><code>@attr.s\nclass TimeData(object):\n    \"\"\"Structure used to handle time related data.\"\"\"\n    start = attr.ib(type=int)\n    end = attr.ib(type=int)\n    fps = attr.ib()\n    step = attr.ib(default=1, type=int)\n    handle_start = attr.ib(default=0, type=int)\n    handle_end = attr.ib(default=0, type=int)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/farm/pyblish_functions.html#client.ayon_core.pipeline.farm.pyblish_functions.attach_instances_to_product","title":"<code>attach_instances_to_product(attach_to, instances)</code>","text":"<p>Attach instance to product.</p> <p>If we are attaching to other products, create copy of existing instances, change data to match its product and replace existing instances with modified data.</p> <p>Parameters:</p> Name Type Description Default <code>attach_to</code> <code>list</code> <p>List of instances to attach to.</p> required <code>instances</code> <code>list</code> <p>List of instances to attach.</p> required <p>Returns:</p> Name Type Description <code>list</code> <p>List of attached instances.</p> Source code in <code>client/ayon_core/pipeline/farm/pyblish_functions.py</code> <pre><code>def attach_instances_to_product(attach_to, instances):\n    \"\"\"Attach instance to product.\n\n    If we are attaching to other products, create copy of existing\n    instances, change data to match its product and replace\n    existing instances with modified data.\n\n    Args:\n        attach_to (list): List of instances to attach to.\n        instances (list): List of instances to attach.\n\n    Returns:\n          list: List of attached instances.\n\n    \"\"\"\n    new_instances = []\n    for attach_instance in attach_to:\n        for i in instances:\n            new_inst = copy.deepcopy(i)\n            new_inst[\"version\"] = attach_instance.get(\"version\")\n            new_inst[\"productName\"] = attach_instance.get(\"productName\")\n            new_inst[\"productType\"] = attach_instance.get(\"productType\")\n            new_inst[\"productBaseType\"] = attach_instance.get(\n                \"productBaseType\"\n            )\n            new_inst[\"family\"] = attach_instance.get(\"family\")\n            new_inst[\"append\"] = True\n            # don't set productGroup if we are attaching\n            new_inst.pop(\"productGroup\")\n            new_instances.append(new_inst)\n    return new_instances\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/farm/pyblish_functions.html#client.ayon_core.pipeline.farm.pyblish_functions.convert_frames_str_to_list","title":"<code>convert_frames_str_to_list(frames)</code>","text":"<p>Convert frames definition string to frames.</p> Handles formats as <p>convert_frames_str_to_list('1001') [1001] convert_frames_str_to_list('1002,1004') [1002, 1004] convert_frames_str_to_list('1003-1005') [1003, 1004, 1005] convert_frames_str_to_list('1001-1021x5') [1001, 1006, 1011, 1016, 1021]</p> <p>Parameters:</p> Name Type Description Default <code>frames</code> <code>str</code> <p>String with frames definition.</p> required <p>Returns:</p> Type Description <code>list[int]</code> <p>list[int]: List of frames.</p> Source code in <code>client/ayon_core/pipeline/farm/pyblish_functions.py</code> <pre><code>def convert_frames_str_to_list(frames: str) -&gt; list[int]:\n    \"\"\"Convert frames definition string to frames.\n\n    Handles formats as:\n        &gt;&gt;&gt; convert_frames_str_to_list('1001')\n        [1001]\n        &gt;&gt;&gt; convert_frames_str_to_list('1002,1004')\n        [1002, 1004]\n        &gt;&gt;&gt; convert_frames_str_to_list('1003-1005')\n        [1003, 1004, 1005]\n        &gt;&gt;&gt; convert_frames_str_to_list('1001-1021x5')\n        [1001, 1006, 1011, 1016, 1021]\n\n    Args:\n        frames (str): String with frames definition.\n\n    Returns:\n        list[int]: List of frames.\n\n    \"\"\"\n    step_pattern = re.compile(r\"(?:step|by|every|x|:)(\\d+)$\")\n\n    output = []\n    step = 1\n    for frame in frames.split(\",\"):\n        if \"-\" in frame:\n            frame_start, frame_end = frame.split(\"-\")\n            match = step_pattern.findall(frame_end)\n            if match:\n                step = int(match[0])\n                frame_end = re.sub(step_pattern, \"\", frame_end)\n\n            output.extend(\n                range(int(frame_start), int(frame_end) + 1, step)\n            )\n        else:\n            output.append(int(frame))\n    output.sort()\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/farm/pyblish_functions.html#client.ayon_core.pipeline.farm.pyblish_functions.copy_extend_frames","title":"<code>copy_extend_frames(instance, representation)</code>","text":"<p>Copy existing frames from latest version.</p> <p>This will copy all existing frames from product's latest version back to render directory and rename them to what renderer is expecting.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>Instance</code> <p>instance to get required data from</p> required <code>representation</code> <code>dict</code> <p>presentation to operate on</p> required Source code in <code>client/ayon_core/pipeline/farm/pyblish_functions.py</code> <pre><code>def copy_extend_frames(instance, representation):\n    \"\"\"Copy existing frames from latest version.\n\n    This will copy all existing frames from product's latest version back\n    to render directory and rename them to what renderer is expecting.\n\n    Arguments:\n        instance (pyblish.plugin.Instance): instance to get required\n            data from\n        representation (dict): presentation to operate on\n\n    \"\"\"\n\n    R_FRAME_NUMBER = re.compile(\n        r\".+\\.(?P&lt;frame&gt;[0-9]+)\\..+\")\n\n    log.info(\"Preparing to copy ...\")\n    start = instance.data.get(\"frameStart\")\n    end = instance.data.get(\"frameEnd\")\n    project_name = instance.context.data[\"project\"]\n    anatomy = instance.context.data[\"anatomy\"]\n\n    folder_entity = ayon_api.get_folder_by_path(\n        project_name, instance.data.get(\"folderPath\")\n    )\n\n    # get latest version of product\n    # this will stop if product wasn't published yet\n\n    version_entity = ayon_api.get_last_version_by_product_name(\n        project_name,\n        instance.data.get(\"productName\"),\n        folder_entity[\"id\"]\n    )\n\n    # get its files based on extension\n    product_resources = get_resources(\n        project_name, version_entity, representation.get(\"ext\")\n    )\n    r_col, _ = clique.assemble(product_resources)\n\n    # if override remove all frames we are expecting to be rendered,\n    # so we'll copy only those missing from current render\n    if instance.data.get(\"overrideExistingFrame\"):\n        for frame in range(start, end + 1):\n            if frame not in r_col.indexes:\n                continue\n            r_col.indexes.remove(frame)\n\n    # now we need to translate published names from representation\n    # back. This is tricky, right now we'll just use same naming\n    # and only switch frame numbers\n    resource_files = []\n    r_filename = os.path.basename(\n        representation.get(\"files\")[0])  # first file\n    op = re.search(R_FRAME_NUMBER, r_filename)\n    pre = r_filename[:op.start(\"frame\")]\n    post = r_filename[op.end(\"frame\"):]\n    assert op is not None, \"padding string wasn't found\"\n    for frame in list(r_col):\n        fn = re.search(R_FRAME_NUMBER, frame)\n        # silencing linter as we need to compare to True, not to\n        # type\n        assert fn is not None, \"padding string wasn't found\"\n        # list of tuples (source, destination)\n        staging = representation.get(\"stagingDir\")\n        staging = anatomy.fill_root(staging)\n        resource_files.append(\n            (frame, os.path.join(\n                staging, \"{}{}{}\".format(pre, fn[\"frame\"], post)))\n        )\n\n    # test if destination dir exists and create it if not\n    output_dir = os.path.dirname(representation.get(\"files\")[0])\n    if not os.path.isdir(output_dir):\n        os.makedirs(output_dir)\n\n    # copy files\n    for source in resource_files:\n        copyfile(source[0], source[1])\n        log.info(\"  &gt; {}\".format(source[1]))\n\n    log.info(\"Finished copying %i files\" % len(resource_files))\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/farm/pyblish_functions.html#client.ayon_core.pipeline.farm.pyblish_functions.create_instances_for_aov","title":"<code>create_instances_for_aov(instance, skeleton, aov_filter, skip_integration_repre_list, do_not_add_review, frames_to_render=None)</code>","text":"<p>Create instances from AOVs.</p> <p>This will create new pyblish.api.Instances by going over expected files defined on original instance.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>Instance</code> <p>Original instance.</p> required <code>skeleton</code> <code>dict</code> <p>Skeleton instance data.</p> required <code>aov_filter</code> <code>dict</code> <p>AOV filter.</p> required <code>skip_integration_repre_list</code> <code>list</code> <p>skip</p> required <code>do_not_add_review</code> <code>bool</code> <p>Explicitly disable reviews</p> required <code>frames_to_render</code> <code>str | None</code> <p>Frames to render.</p> <code>None</code> <p>Returns:</p> Type Description <p>list of pyblish.api.Instance: Instances created from expected files.</p> Source code in <code>client/ayon_core/pipeline/farm/pyblish_functions.py</code> <pre><code>def create_instances_for_aov(\n    instance,\n    skeleton,\n    aov_filter,\n    skip_integration_repre_list,\n    do_not_add_review,\n    frames_to_render=None\n):\n    \"\"\"Create instances from AOVs.\n\n    This will create new pyblish.api.Instances by going over expected\n    files defined on original instance.\n\n    Args:\n        instance (pyblish.api.Instance): Original instance.\n        skeleton (dict): Skeleton instance data.\n        aov_filter (dict): AOV filter.\n        skip_integration_repre_list (list): skip\n        do_not_add_review (bool): Explicitly disable reviews\n        frames_to_render (str | None): Frames to render.\n\n    Returns:\n        list of pyblish.api.Instance: Instances created from\n            expected files.\n\n    \"\"\"\n    # we cannot attach AOVs to other products as we consider every\n    # AOV product of its own.\n\n    # if there are product to attach to and more than one AOV,\n    # we cannot proceed.\n    if (\n        len(instance.data.get(\"attachTo\", [])) &gt; 0\n        and len(instance.data.get(\"expectedFiles\")[0].keys()) != 1\n    ):\n        raise PublishError(\n            \"Attaching multiple AOVs or renderable cameras to\"\n            \" product is not supported yet.\"\n        )\n\n    additional_data = {\n        \"renderProducts\": instance.data[\"renderProducts\"],\n    }\n\n    # Collect color management data if present\n    colorspace_config = instance.data.get(\"colorspaceConfig\")\n    if colorspace_config:\n        additional_data.update({\n            \"colorspaceConfig\": colorspace_config,\n            # Display/View are optional\n            \"display\": instance.data.get(\"sourceDisplay\"),\n            \"view\": instance.data.get(\"sourceView\")\n        })\n\n        # Get templated path from absolute config path.\n        anatomy = instance.context.data[\"anatomy\"]\n        template = find_colorspace_template(colorspace_config, anatomy)\n        additional_data[\"colorspaceTemplate\"] = template or colorspace_config\n\n    # create instances for every AOV we found in expected files.\n    # NOTE: this is done for every AOV and every render camera (if\n    #       there are multiple renderable cameras in scene)\n    return _create_instances_for_aov(\n        instance,\n        skeleton,\n        aov_filter,\n        additional_data,\n        skip_integration_repre_list,\n        do_not_add_review,\n        frames_to_render\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/farm/pyblish_functions.html#client.ayon_core.pipeline.farm.pyblish_functions.create_instances_for_cache","title":"<code>create_instances_for_cache(instance, skeleton)</code>","text":"<p>Create instance for cache.</p> <p>This will create new instance for every AOV it can detect in expected files list.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>Instance</code> <p>Original instance.</p> required <code>skeleton</code> <code>dict</code> <p>Skeleton data for instance (those needed) later by collector.</p> required <p>Returns:</p> Type Description <p>list of instances</p> Throws <p>ValueError:</p> Source code in <code>client/ayon_core/pipeline/farm/pyblish_functions.py</code> <pre><code>def create_instances_for_cache(instance, skeleton):\n    \"\"\"Create instance for cache.\n\n    This will create new instance for every AOV it can detect in expected\n    files list.\n\n    Args:\n        instance (pyblish.api.Instance): Original instance.\n        skeleton (dict): Skeleton data for instance (those needed) later\n            by collector.\n\n\n    Returns:\n        list of instances\n\n    Throws:\n        ValueError:\n\n    \"\"\"\n    anatomy = instance.context.data[\"anatomy\"]\n    product_name = skeleton[\"productName\"]\n    product_type = skeleton[\"productType\"]\n    product_base_type = skeleton.get(\"productBaseType\")\n    if not product_base_type:\n        product_base_type = product_type\n    exp_files = instance.data[\"expectedFiles\"]\n\n    instances = []\n    # go through AOVs in expected files\n    for _, files in exp_files[0].items():\n        cols, rem = clique.assemble(files)\n        # we shouldn't have any reminders. And if we do, it should\n        # be just one item for single frame renders.\n        if not cols and rem:\n            if len(rem) != 1:\n                raise ValueError(\"Found multiple non related files \"\n                                 \"to render, don't know what to do \"\n                                 \"with them.\")\n            col = rem[0]\n            ext = os.path.splitext(col)[1].lstrip(\".\")\n        else:\n            # but we really expect only one collection.\n            # Nothing else make sense.\n            if len(cols) != 1:\n                raise ValueError(\"Only one image sequence type is expected.\")  # noqa: E501\n            ext = cols[0].tail.lstrip(\".\")\n            col = list(cols[0])\n\n        if isinstance(col, (list, tuple)):\n            staging = os.path.dirname(col[0])\n        else:\n            staging = os.path.dirname(col)\n\n        try:\n            staging = remap_source(staging, anatomy)\n        except ValueError as e:\n            log.warning(e)\n\n        new_instance = copy.deepcopy(skeleton)\n\n        log.info(\"Creating data for: {}\".format(product_name))\n        new_instance[\"productName\"] = product_name\n        new_instance[\"productType\"] = product_type\n        new_instance[\"productBaseType\"] = product_base_type\n        # create representation\n        if isinstance(col, (list, tuple)):\n            files = [os.path.basename(f) for f in col]\n        else:\n            files = os.path.basename(col)\n\n        rep = {\n            \"name\": ext,\n            \"ext\": ext,\n            \"files\": files,\n            \"frameStart\": int(skeleton[\"frameStartHandle\"]),\n            \"frameEnd\": int(skeleton[\"frameEndHandle\"]),\n            # If expectedFile are absolute, we need only filenames\n            \"stagingDir\": staging,\n            \"fps\": new_instance.get(\"fps\"),\n            \"tags\": [],\n        }\n\n        new_instance[\"representations\"] = [rep]\n\n        # if extending frames from existing version, copy files from there\n        # into our destination directory\n        if new_instance.get(\"extendFrames\", False):\n            copy_extend_frames(new_instance, rep)\n        instances.append(new_instance)\n        log.debug(\"instances:{}\".format(instances))\n    return instances\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/farm/pyblish_functions.html#client.ayon_core.pipeline.farm.pyblish_functions.create_skeleton_instance","title":"<code>create_skeleton_instance(instance, families_transfer=None, instance_transfer=None)</code>","text":"<p>Create skeleton instance from original instance data.</p> <p>This will create dictionary containing skeleton - common - data used for publishing rendered instances. This skeleton instance is then extended with additional data and serialized to be processed by farm job.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>Instance</code> <p>Original instance to be used as a source of data.</p> required <code>families_transfer</code> <code>list</code> <p>List of family names to transfer from the original instance to the skeleton.</p> <code>None</code> <code>instance_transfer</code> <code>dict</code> <p>Dict with keys as families and values as a list of property names to transfer to the new skeleton.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>dict</code> <p>Dictionary with skeleton instance data.</p> Source code in <code>client/ayon_core/pipeline/farm/pyblish_functions.py</code> <pre><code>def create_skeleton_instance(\n    instance,\n    families_transfer=None,\n    instance_transfer=None,\n):\n    \"\"\"Create skeleton instance from original instance data.\n\n    This will create dictionary containing skeleton\n    - common - data used for publishing rendered instances.\n    This skeleton instance is then extended with additional data\n    and serialized to be processed by farm job.\n\n    Args:\n        instance (pyblish.api.Instance): Original instance to\n            be used as a source of data.\n        families_transfer (list): List of family names to transfer\n            from the original instance to the skeleton.\n        instance_transfer (dict): Dict with keys as families and\n            values as a list of property names to transfer to the\n            new skeleton.\n\n    Returns:\n        dict: Dictionary with skeleton instance data.\n\n    \"\"\"\n    # list of family names to transfer to new family if present\n    if families_transfer is None:\n        families_transfer = []\n\n    if instance_transfer is None:\n        instance_transfer = {}\n\n    context = instance.context\n    data = instance.data.copy()\n    anatomy = instance.context.data[\"anatomy\"]\n\n    # get time related data from instance (or context)\n    time_data = get_time_data_from_instance_or_context(instance)\n\n    if data.get(\"extendFrames\", False):\n        time_data.start, time_data.end = extend_frames(\n            data[\"folderPath\"],\n            data[\"productName\"],\n            time_data.start,\n            time_data.end,\n        )\n\n    source = data.get(\"source\") or context.data.get(\"currentFile\")\n    success, rootless_path = (\n        anatomy.find_root_template_from_path(source)\n    )\n    if success:\n        source = rootless_path\n    else:\n        # `rootless_path` is not set to `source` if none of roots match\n        log.warning((\"Could not find root path for remapping \\\"{}\\\". \"\n                     \"This may cause issues.\").format(source))\n\n    # This is a hack to keep the value of 'productType'.\n    # Because this function does not use product base type from source\n    #   instance and we don't know if product type of the instance was\n    #   customized or not, only way how to guess custom product type is\n    #   to check if is same as product base type.\n    i_product_base_type = instance.data.get(\"productBaseType\")\n    i_product_type = instance.data.get(\"productType\")\n    product_type = None\n    if (\n        i_product_base_type\n        and i_product_base_type != i_product_type\n    ):\n        product_type = i_product_type\n\n    # This is the old way of defining product base type\n    # - hard-coded product base type\n    product_base_type = \"render\"\n    if \"prerender.farm\" in instance.data[\"families\"]:\n        product_base_type = \"prerender\"\n\n    if not product_type:\n        product_type = product_base_type\n\n    families = [product_base_type]\n\n    # pass review to families if marked as review\n    if data.get(\"review\"):\n        families.append(\"review\")\n\n    instance_skeleton_data = {\n        # TODO find out how to define product type\n        # - Right now product base type is hardcoded, from where should be\n        #   product type taken?\n        \"productType\": product_type,\n        \"productBaseType\": product_base_type,\n        \"productName\": data[\"productName\"],\n        \"task\": data[\"task\"],\n        \"families\": families,\n        \"folderPath\": data[\"folderPath\"],\n        \"frameStart\": time_data.start,\n        \"frameEnd\": time_data.end,\n        \"handleStart\": time_data.handle_start,\n        \"handleEnd\": time_data.handle_end,\n        \"frameStartHandle\": time_data.start - time_data.handle_start,\n        \"frameEndHandle\": time_data.end + time_data.handle_end,\n        \"comment\": data.get(\"comment\"),\n        \"fps\": time_data.fps,\n        \"source\": source,\n        \"extendFrames\": data.get(\"extendFrames\"),\n        \"overrideExistingFrame\": data.get(\"overrideExistingFrame\"),\n        \"pixelAspect\": data.get(\"pixelAspect\", 1),\n        \"resolutionWidth\": data.get(\"resolutionWidth\", 1920),\n        \"resolutionHeight\": data.get(\"resolutionHeight\", 1080),\n        \"multipartExr\": data.get(\"multipartExr\", False),\n        \"jobBatchName\": data.get(\"jobBatchName\", \"\"),\n        \"useSequenceForReview\": data.get(\"useSequenceForReview\", True),\n        \"inputVersions\": serialize_input_versions(data.get(\"inputVersions\")),\n        \"colorspace\": data.get(\"colorspace\"),\n        \"hasExplicitFrames\": data.get(\"hasExplicitFrames\", False),\n        \"reuseLastVersion\": data.get(\"reuseLastVersion\", False),\n    }\n\n    # Pass on the OCIO metadata of what the source display and view are\n    # so that the farm can correctly set up color management.\n    if \"sceneDisplay\" in data and \"sceneView\" in data:\n        instance_skeleton_data[\"sceneDisplay\"] = data[\"sceneDisplay\"]\n        instance_skeleton_data[\"sceneView\"] = data[\"sceneView\"]\n    elif \"colorspaceDisplay\" in data and \"colorspaceView\" in data:\n        # Backwards compatibility for sceneDisplay and sceneView\n        instance_skeleton_data[\"colorspaceDisplay\"] = data[\"colorspaceDisplay\"]\n        instance_skeleton_data[\"colorspaceView\"] = data[\"colorspaceView\"]\n    if \"sourceDisplay\" in data and \"sourceView\" in data:\n        instance_skeleton_data[\"sourceDisplay\"] = data[\"sourceDisplay\"]\n        instance_skeleton_data[\"sourceView\"] = data[\"sourceView\"]\n\n    if data.get(\"renderlayer\"):\n        instance_skeleton_data[\"renderlayer\"] = data[\"renderlayer\"]\n\n    if data.get(\"status\"):\n        instance_skeleton_data[\"status\"] = data[\"status\"]\n\n    # skip locking version if we are creating v01\n    instance_version = data.get(\"version\")  # take this if exists\n    if instance_version != 1:\n        instance_skeleton_data[\"version\"] = instance_version\n\n    # transfer specific families from original instance to new render\n    for item in families_transfer:\n        if item in instance.data.get(\"families\", []):\n            instance_skeleton_data[\"families\"] += [item]\n\n    # transfer specific properties from original instance based on\n    # mapping dictionary `instance_transfer`\n    for key, values in instance_transfer.items():\n        if key in instance.data.get(\"families\", []):\n            for v in values:\n                instance_skeleton_data[v] = instance.data.get(v)\n\n    representations = get_transferable_representations(instance)\n    instance_skeleton_data[\"representations\"] = representations\n\n    persistent = instance.data.get(\"stagingDir_persistent\") is True\n    instance_skeleton_data[\"stagingDir_persistent\"] = persistent\n\n    return instance_skeleton_data\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/farm/pyblish_functions.html#client.ayon_core.pipeline.farm.pyblish_functions.create_skeleton_instance_cache","title":"<code>create_skeleton_instance_cache(instance)</code>","text":"<p>Create skeleton instance from original instance data.</p> <p>This will create dictionary containing skeleton - common - data used for publishing rendered instances. This skeleton instance is then extended with additional data and serialized to be processed by farm job.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>Instance</code> <p>Original instance to be used as a source of data.</p> required <p>Returns:</p> Name Type Description <code>dict</code> <p>Dictionary with skeleton instance data.</p> Source code in <code>client/ayon_core/pipeline/farm/pyblish_functions.py</code> <pre><code>def create_skeleton_instance_cache(instance):\n    \"\"\"Create skeleton instance from original instance data.\n\n    This will create dictionary containing skeleton\n    - common - data used for publishing rendered instances.\n    This skeleton instance is then extended with additional data\n    and serialized to be processed by farm job.\n\n    Args:\n        instance (pyblish.api.Instance): Original instance to\n            be used as a source of data.\n\n    Returns:\n        dict: Dictionary with skeleton instance data.\n\n    \"\"\"\n    # list of family names to transfer to new family if present\n\n    context = instance.context\n    data = instance.data.copy()\n    anatomy = instance.context.data[\"anatomy\"]\n\n    # get time related data from instance (or context)\n    time_data = get_time_data_from_instance_or_context(instance)\n\n    if data.get(\"extendFrames\", False):\n        time_data.start, time_data.end = extend_frames(\n            data[\"folderPath\"],\n            data[\"productName\"],\n            time_data.start,\n            time_data.end,\n        )\n\n    source = data.get(\"source\") or context.data.get(\"currentFile\")\n    success, rootless_path = (\n        anatomy.find_root_template_from_path(source)\n    )\n    if success:\n        source = rootless_path\n    else:\n        # `rootless_path` is not set to `source` if none of roots match\n        log.warning((\"Could not find root path for remapping \\\"{}\\\". \"\n                     \"This may cause issues.\").format(source))\n\n    product_type = instance.data[\"productType\"]\n    product_base_type = instance.data.get(\"productBaseType\")\n    if not product_base_type:\n        product_base_type = product_type\n    # Make sure \"render\" is in the families to go through\n    # validating expected and rendered files\n    # during publishing job.\n    families = [\"render\", product_base_type]\n\n    instance_skeleton_data = {\n        \"productName\": data[\"productName\"],\n        \"productType\": product_type,\n        \"productBaseType\": product_base_type,\n        \"family\": product_base_type,\n        \"families\": families,\n        \"folderPath\": data[\"folderPath\"],\n        \"frameStart\": time_data.start,\n        \"frameEnd\": time_data.end,\n        \"handleStart\": time_data.handle_start,\n        \"handleEnd\": time_data.handle_end,\n        \"frameStartHandle\": time_data.start - time_data.handle_start,\n        \"frameEndHandle\": time_data.end + time_data.handle_end,\n        \"comment\": data.get(\"comment\"),\n        \"fps\": time_data.fps,\n        \"source\": source,\n        \"extendFrames\": data.get(\"extendFrames\"),\n        \"overrideExistingFrame\": data.get(\"overrideExistingFrame\"),\n        \"jobBatchName\": data.get(\"jobBatchName\", \"\"),\n        \"inputVersions\": serialize_input_versions(data.get(\"inputVersions\")),\n    }\n\n    # skip locking version if we are creating v01\n    instance_version = data.get(\"version\")  # take this if exists\n    if instance_version != 1:\n        instance_skeleton_data[\"version\"] = instance_version\n\n    representations = get_transferable_representations(instance)\n    instance_skeleton_data[\"representations\"] = representations\n\n    persistent = instance.data.get(\"stagingDir_persistent\") is True\n    instance_skeleton_data[\"stagingDir_persistent\"] = persistent\n\n    return instance_skeleton_data\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/farm/pyblish_functions.html#client.ayon_core.pipeline.farm.pyblish_functions.extend_frames","title":"<code>extend_frames(folder_path, product_name, start, end)</code>","text":"<p>Get latest version of asset nad update frame range.</p> <p>Based on minimum and maximum values.</p> <p>Parameters:</p> Name Type Description Default <code>folder_path</code> <code>str</code> <p>Folder path.</p> required <code>product_name</code> <code>str</code> <p>Product name.</p> required <code>start</code> <code>int</code> <p>Start frame.</p> required <code>end</code> <code>int</code> <p>End frame.</p> required <p>Returns:</p> Type Description <code>(int, int)</code> <p>update frame start/end</p> Source code in <code>client/ayon_core/pipeline/farm/pyblish_functions.py</code> <pre><code>def extend_frames(folder_path, product_name, start, end):\n    \"\"\"Get latest version of asset nad update frame range.\n\n    Based on minimum and maximum values.\n\n    Arguments:\n        folder_path (str): Folder path.\n        product_name (str): Product name.\n        start (int): Start frame.\n        end (int): End frame.\n\n    Returns:\n        (int, int): update frame start/end\n\n    \"\"\"\n    # Frame comparison\n    prev_start = None\n    prev_end = None\n\n    project_name = get_current_project_name()\n    folder_entity = ayon_api.get_folder_by_path(\n        project_name, folder_path, fields={\"id\"}\n    )\n    version_entity = ayon_api.get_last_version_by_product_name(\n        project_name,\n        product_name,\n        folder_entity[\"id\"]\n    )\n\n    # Set prev start / end frames for comparison\n    if not prev_start and not prev_end:\n        prev_start = version_entity[\"attrib\"][\"frameStart\"]\n        prev_end = version_entity[\"attrib\"][\"frameEnd\"]\n\n    updated_start = min(start, prev_start)\n    updated_end = max(end, prev_end)\n\n    return updated_start, updated_end\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/farm/pyblish_functions.html#client.ayon_core.pipeline.farm.pyblish_functions.find_colorspace_template","title":"<code>find_colorspace_template(colorspace_path, anatomy)</code>","text":"<p>Find template for colorspace path.</p> <p>Try to use builtin OCIO if path is relative to it. If not, try to     remap path using anatomy. If that fails, return None.</p> <p>Parameters:</p> Name Type Description Default <code>colorspace_path</code> <code>str</code> <p>Path to colorspace.</p> required <code>anatomy</code> <code>Anatomy</code> <p>Project anatomy object.</p> required <p>Returns:</p> Type Description <code>str | None</code> <p>str | None: Template to use for colorspace path.</p> Source code in <code>client/ayon_core/pipeline/farm/pyblish_functions.py</code> <pre><code>def find_colorspace_template(\n    colorspace_path: str,\n    anatomy: Anatomy,\n) -&gt; str | None:\n    \"\"\"Find template for colorspace path.\n\n    Try to use builtin OCIO if path is relative to it. If not, try to\n        remap path using anatomy. If that fails, return None.\n\n    Args:\n        colorspace_path (str): Path to colorspace.\n        anatomy (Anatomy): Project anatomy object.\n\n    Returns:\n        str | None: Template to use for colorspace path.\n\n    \"\"\"\n    builtin_path = os.getenv(\"BUILTIN_OCIO_ROOT\")\n    if builtin_path:\n        builtin_path = Path(builtin_path).resolve().absolute()\n        path = Path(colorspace_path).resolve().absolute()\n        if path.is_relative_to(builtin_path):\n            relative = str(path.relative_to(builtin_path))\n            if platform.system().lower() == \"windows\":\n                relative = relative.replace(\"\\\\\", \"/\")\n            return f\"{{BUILTIN_OCIO_ROOT}}/{relative}\"\n\n    output = None\n    try:\n        output = remap_source(colorspace_path, anatomy)\n        if platform.system().lower() == \"windows\":\n            output = output.replace(\"\\\\\", \"/\")\n    except ValueError:\n        pass\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/farm/pyblish_functions.html#client.ayon_core.pipeline.farm.pyblish_functions.get_product_name_and_group_from_template","title":"<code>get_product_name_and_group_from_template(project_name, task_entity, product_type, variant, host_name, dynamic_data=None, *, folder_entity=None, product_base_type=None, project_entity=None, project_settings=None)</code>","text":"<p>Get product name and group name from template.</p> This function is/was used only in ayon-houdini which uses/d kwargs <p>for all arguments. When houdini starts to use 'folder_entity' and 'product_base_type' this should change order of arguments.</p> <p>This will get product name and group name from template based on data provided. It is doing similar work as <code>func::_get_legacy_product_name_and_group</code> but using templates.</p> <p>To get group name, template is called without any dynamic data, so (depending on the template itself) it should be product name without aov.</p> Todo <p>Maybe we should introduce templates for the groups themselves.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>task_entity</code> <code>Union[dict[str, Any], None]</code> <p>Task entity.</p> required <code>host_name</code> <code>str</code> <p>Host name.</p> required <code>product_type</code> <code>str</code> <p>Product type.</p> required <code>variant</code> <code>str</code> <p>Variant.</p> required <code>dynamic_data</code> <code>Optional[dict[str, Any]]</code> <p>Dynamic data (aov, renderlayer, camera, ...).</p> <code>None</code> <code>folder_entity</code> <code>Union[dict[str, Any], None]</code> <p>Folder entity.</p> <code>None</code> <code>product_base_type</code> <code>str</code> <p>Product base type.</p> <code>None</code> <code>project_entity</code> <code>Optional[dict[str, Any]]</code> <p>Project entity.</p> <code>None</code> <code>project_settings</code> <code>Optional[dict[str, Any]]</code> <p>Project settings.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>tuple</code> <code>tuple[str, str]</code> <p>product name and group name.</p> Source code in <code>client/ayon_core/pipeline/farm/pyblish_functions.py</code> <pre><code>def get_product_name_and_group_from_template(\n    project_name: str,\n    task_entity: Union[dict[str, Any], None],\n    product_type: str,\n    variant: str,\n    host_name: str,\n    dynamic_data: Optional[dict[str, Any]] = None,\n    *,\n    folder_entity: Union[dict[str, Any], None] = None,\n    product_base_type: str = None,\n    project_entity: Optional[dict[str, Any]] = None,\n    project_settings: Optional[dict[str, Any]] = None,\n) -&gt; tuple[str, str]:\n    \"\"\"Get product name and group name from template.\n\n    NOTE: This function is/was used only in ayon-houdini which uses/d kwargs\n        for all arguments. When houdini starts to use 'folder_entity' and\n        'product_base_type' this should change order of arguments.\n\n    This will get product name and group name from template based on\n    data provided. It is doing similar work as\n    `func::_get_legacy_product_name_and_group` but using templates.\n\n    To get group name, template is called without any dynamic data, so\n    (depending on the template itself) it should be product name without\n    aov.\n\n    Todo:\n        Maybe we should introduce templates for the groups themselves.\n\n    Args:\n        project_name (str): Project name.\n        task_entity (Union[dict[str, Any], None]): Task entity.\n        host_name (str): Host name.\n        product_type (str): Product type.\n        variant (str): Variant.\n        dynamic_data (Optional[dict[str, Any]]): Dynamic data\n            (aov, renderlayer, camera, ...).\n        folder_entity (Union[dict[str, Any], None]): Folder entity.\n        product_base_type (str): Product base type.\n        project_entity (Optional[dict[str, Any]]): Project entity.\n        project_settings (Optional[dict[str, Any]]): Project settings.\n\n    Returns:\n        tuple: product name and group name.\n\n    \"\"\"\n    if not folder_entity and task_entity:\n        folder_entity = None\n        if task_entity:\n            folder_entity = ayon_api.get_folder_by_path(\n                project_name, task_entity[\"folderId\"]\n            )\n\n    if not product_base_type:\n        log.warning(\n            f\"DEPRECATION WARNING: Product base type not provided,\"\n            f\" using product type: {product_type}\"\n        )\n        product_base_type = product_type\n\n    if not project_entity:\n        project_entity = ayon_api.get_project(project_name)\n\n    if not project_settings:\n        project_settings = get_project_settings(project_name)\n\n    # remove 'aov' from data used to format group. See todo comment above\n    # for possible solution.\n    if dynamic_data is None:\n        dynamic_data = {}\n    _dynamic_data = copy.deepcopy(dynamic_data)\n    _dynamic_data.pop(\"aov\", None)\n\n    resulting_group_name = get_product_name(\n        project_name=project_name,\n        folder_entity=folder_entity,\n        task_entity=task_entity,\n        host_name=host_name,\n        product_base_type=product_base_type,\n        product_type=product_type,\n        dynamic_data=_dynamic_data,\n        variant=variant,\n        project_entity=project_entity,\n        project_settings=project_settings,\n    )\n\n    resulting_product_name = get_product_name(\n        project_name=project_name,\n        folder_entity=folder_entity,\n        task_entity=task_entity,\n        host_name=host_name,\n        product_base_type=product_base_type,\n        product_type=product_type,\n        dynamic_data=dynamic_data,\n        variant=variant,\n        project_entity=project_entity,\n        project_settings=project_settings,\n    )\n    return resulting_product_name, resulting_group_name\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/farm/pyblish_functions.html#client.ayon_core.pipeline.farm.pyblish_functions.get_resources","title":"<code>get_resources(project_name, version_entity, extension=None)</code>","text":"<p>Get the files from the specific version.</p> <p>This will return all get all files from representation.</p> Todo <p>This is really weird function, and it's use is highly controversial. First, it will not probably work ar all in final release of AYON, second, the logic isn't sound. It should try to find representation matching the current one - because it is used to pull out files from previous version to be included in this one.</p> <p>.. deprecated:: 3.15.5    This won't work in AYON and even the logic must be refactored.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Name of the project.</p> required <code>version_entity</code> <code>dict</code> <p>Version entity.</p> required <code>extension</code> <code>str</code> <p>extension used to filter representations.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>list</code> <p>of files</p> Source code in <code>client/ayon_core/pipeline/farm/pyblish_functions.py</code> <pre><code>def get_resources(project_name, version_entity, extension=None):\n    \"\"\"Get the files from the specific version.\n\n    This will return all get all files from representation.\n\n    Todo:\n        This is really weird function, and it's use is\n        highly controversial. First, it will not probably work\n        ar all in final release of AYON, second, the logic isn't sound.\n        It should try to find representation matching the current one -\n        because it is used to pull out files from previous version to\n        be included in this one.\n\n    .. deprecated:: 3.15.5\n       This won't work in AYON and even the logic must be refactored.\n\n    Args:\n        project_name (str): Name of the project.\n        version_entity (dict): Version entity.\n        extension (str): extension used to filter\n            representations.\n\n    Returns:\n        list: of files\n\n    \"\"\"\n    warnings.warn((\n        \"This won't work in AYON and even \"\n        \"the logic must be refactored.\"), DeprecationWarning)\n    extensions = []\n    if extension:\n        extensions = [extension]\n\n    # there is a `context_filter` argument that won't probably work in\n    # final release of AYON. SO we'll rather not use it\n    repre_entities = list(ayon_api.get_representations(\n        project_name, version_ids={version_entity[\"id\"]}\n    ))\n\n    filtered = []\n    for repre_entity in repre_entities:\n        if repre_entity[\"context\"][\"ext\"] in extensions:\n            filtered.append(repre_entity)\n\n    representation = filtered[0]\n    directory = get_representation_path(\n        project_name, representation\n    )\n    print(\"Source: \", directory)\n    resources = sorted(\n        [\n            os.path.normpath(os.path.join(directory, file_name))\n            for file_name in os.listdir(directory)\n        ]\n    )\n\n    return resources\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/farm/pyblish_functions.html#client.ayon_core.pipeline.farm.pyblish_functions.get_time_data_from_instance_or_context","title":"<code>get_time_data_from_instance_or_context(instance)</code>","text":"<p>Get time data from instance (or context).</p> <p>If time data is not found on instance, data from context will be used.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>Instance</code> <p>Source instance.</p> required <p>Returns:</p> Name Type Description <code>TimeData</code> <p>dataclass holding time information.</p> Source code in <code>client/ayon_core/pipeline/farm/pyblish_functions.py</code> <pre><code>def get_time_data_from_instance_or_context(instance):\n    \"\"\"Get time data from instance (or context).\n\n    If time data is not found on instance, data from context will be used.\n\n    Args:\n        instance (pyblish.api.Instance): Source instance.\n\n    Returns:\n        TimeData: dataclass holding time information.\n\n    \"\"\"\n    context = instance.context\n    return TimeData(\n        start=instance.data.get(\"frameStart\", context.data.get(\"frameStart\")),\n        end=instance.data.get(\"frameEnd\", context.data.get(\"frameEnd\")),\n        fps=instance.data.get(\"fps\", context.data.get(\"fps\")),\n        step=instance.data.get(\"byFrameStep\", instance.data.get(\"step\", 1)),\n        handle_start=instance.data.get(\n            \"handleStart\", context.data.get(\"handleStart\")\n        ),\n        handle_end=instance.data.get(\n            \"handleEnd\", context.data.get(\"handleEnd\")\n        )\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/farm/pyblish_functions.html#client.ayon_core.pipeline.farm.pyblish_functions.get_transferable_representations","title":"<code>get_transferable_representations(instance)</code>","text":"<p>Transfer representations from original instance.</p> <p>This will get all representations on the original instance that are flagged with <code>publish_on_farm</code> and return them to be included on skeleton instance if needed.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>Instance</code> <p>Original instance to be processed.</p> required Return <p>list of dicts: List of transferable representations.</p> Source code in <code>client/ayon_core/pipeline/farm/pyblish_functions.py</code> <pre><code>def get_transferable_representations(instance):\n    \"\"\"Transfer representations from original instance.\n\n    This will get all representations on the original instance that\n    are flagged with `publish_on_farm` and return them to be included\n    on skeleton instance if needed.\n\n    Args:\n        instance (pyblish.api.Instance): Original instance to be processed.\n\n    Return:\n        list of dicts: List of transferable representations.\n\n    \"\"\"\n    anatomy = instance.context.data[\"anatomy\"]\n    to_transfer = []\n\n    for representation in instance.data.get(\"representations\", []):\n        if \"publish_on_farm\" not in representation.get(\"tags\", []):\n            continue\n\n        trans_rep = representation.copy()\n\n        # remove publish_on_farm from representations tags\n        trans_rep[\"tags\"].remove(\"publish_on_farm\")\n\n        staging_dir = trans_rep.get(\"stagingDir\")\n\n        if staging_dir:\n            try:\n                trans_rep[\"stagingDir\"] = remap_source(staging_dir, anatomy)\n            except ValueError:\n                log.warning(\n                    (\"Could not find root path for remapping \\\"{}\\\". \"\n                     \"This may cause issues on farm.\").format(staging_dir))\n\n        to_transfer.append(trans_rep)\n    return to_transfer\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/farm/pyblish_functions.html#client.ayon_core.pipeline.farm.pyblish_functions.prepare_cache_representations","title":"<code>prepare_cache_representations(skeleton_data, exp_files, anatomy)</code>","text":"<p>Create representations for file sequences.</p> <p>This will return representations of expected files if they are not in hierarchy of aovs. There should be only one sequence of files for most cases, but if not - we create representation from each of them.</p> <p>Parameters:</p> Name Type Description Default <code>skeleton_data</code> <code>dict</code> <p>instance data for which we are              setting representations</p> required <code>exp_files</code> <code>list</code> <p>list of expected files</p> required <p>Returns:     list of representations</p> Source code in <code>client/ayon_core/pipeline/farm/pyblish_functions.py</code> <pre><code>def prepare_cache_representations(skeleton_data, exp_files, anatomy):\n    \"\"\"Create representations for file sequences.\n\n    This will return representations of expected files if they are not\n    in hierarchy of aovs. There should be only one sequence of files for\n    most cases, but if not - we create representation from each of them.\n\n    Arguments:\n        skeleton_data (dict): instance data for which we are\n                         setting representations\n        exp_files (list): list of expected files\n        anatomy (Anatomy)\n    Returns:\n        list of representations\n\n    \"\"\"\n    representations = []\n    collections, _remainders = clique.assemble(exp_files)\n\n    # create representation for every collected sequence\n    for collection in collections:\n        ext = collection.tail.lstrip(\".\")\n\n        staging = os.path.dirname(list(collection)[0])\n        success, rootless_staging_dir = (\n            anatomy.find_root_template_from_path(staging)\n        )\n        if success:\n            staging = rootless_staging_dir\n        else:\n            log.warning((\n                \"Could not find root path for remapping \\\"{}\\\".\"\n                \" This may cause issues on farm.\"\n            ).format(staging))\n\n        frame_start = int(skeleton_data.get(\"frameStartHandle\"))\n        rep = {\n            \"name\": ext,\n            \"ext\": ext,\n            \"files\": [os.path.basename(f) for f in list(collection)],\n            \"frameStart\": frame_start,\n            \"frameEnd\": int(skeleton_data.get(\"frameEndHandle\")),\n            # If expectedFile are absolute, we need only filenames\n            \"stagingDir\": staging,\n            \"fps\": skeleton_data.get(\"fps\")\n        }\n\n        representations.append(rep)\n\n    return representations\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/farm/pyblish_functions.html#client.ayon_core.pipeline.farm.pyblish_functions.prepare_representations","title":"<code>prepare_representations(skeleton_data, exp_files, anatomy, aov_filter, skip_integration_repre_list, do_not_add_review, context, color_managed_plugin, frames_to_render=None)</code>","text":"<p>Create representations for file sequences.</p> <p>This will return representations of expected files if they are not in hierarchy of aovs. There should be only one sequence of files for most cases, but if not - we create representation from each of them.</p> <p>Parameters:</p> Name Type Description Default <code>skeleton_data</code> <code>dict</code> <p>instance data for which we are              setting representations</p> required <code>exp_files</code> <code>list</code> <p>list of expected files</p> required <code>anatomy</code> <code>Anatomy</code> required <code>aov_filter</code> <code>dict</code> <p>add review for specific aov names</p> required <code>skip_integration_repre_list</code> <code>list</code> <p>exclude specific extensions,</p> required <code>do_not_add_review</code> <code>bool</code> <p>explicitly skip review</p> required <code>frames_to_render</code> <code>str | None</code> <p>implicit or explicit range of frames to render this value is sent to Deadline in JobInfo.Frames</p> <code>None</code> <p>Returns:     list of representations</p> Source code in <code>client/ayon_core/pipeline/farm/pyblish_functions.py</code> <pre><code>def prepare_representations(\n    skeleton_data,\n    exp_files,\n    anatomy,\n    aov_filter,\n    skip_integration_repre_list,\n    do_not_add_review,\n    context,\n    color_managed_plugin,\n    frames_to_render=None\n):\n    \"\"\"Create representations for file sequences.\n\n    This will return representations of expected files if they are not\n    in hierarchy of aovs. There should be only one sequence of files for\n    most cases, but if not - we create representation from each of them.\n\n    Arguments:\n        skeleton_data (dict): instance data for which we are\n                         setting representations\n        exp_files (list): list of expected files\n        anatomy (Anatomy):\n        aov_filter (dict): add review for specific aov names\n        skip_integration_repre_list (list): exclude specific extensions,\n        do_not_add_review (bool): explicitly skip review\n        color_managed_plugin (publish.ColormanagedPyblishPluginMixin)\n        frames_to_render (str | None): implicit or explicit range of frames\n            to render this value is sent to Deadline in JobInfo.Frames\n    Returns:\n        list of representations\n\n    \"\"\"\n    representations = []\n    host_name = os.environ.get(\"AYON_HOST_NAME\", \"\")\n    collections, remainders = clique.assemble(exp_files)\n\n    if frames_to_render is not None:\n        frames_to_render = convert_frames_str_to_list(frames_to_render)\n    else:\n        # Backwards compatibility for older logic\n        frame_start = int(skeleton_data.get(\"frameStartHandle\"))\n        frame_end = int(skeleton_data.get(\"frameEndHandle\"))\n        frames_to_render = list(range(frame_start, frame_end + 1))\n\n    # create representation for every collected sequence\n    for collection in collections:\n        ext = collection.tail.lstrip(\".\")\n        preview = False\n        # TODO 'useSequenceForReview' is temporary solution which does\n        #   not work for 100% of cases. We must be able to tell what\n        #   expected files contains more explicitly and from what\n        #   should be review made.\n        # - \"review\" tag is never added when is set to 'False'\n        if skeleton_data[\"useSequenceForReview\"]:\n            # toggle preview on if multipart is on\n            if skeleton_data.get(\"multipartExr\", False):\n                log.debug(\n                    \"Adding preview tag because its multipartExr\"\n                )\n                preview = True\n            else:\n                render_file_name = list(collection)[0]\n                # if filtered aov name is found in filename, toggle it for\n                # preview video rendering\n                preview = match_aov_pattern(\n                    host_name, aov_filter, render_file_name\n                )\n\n        staging = os.path.dirname(list(collection)[0])\n        success, rootless_staging_dir = (\n            anatomy.find_root_template_from_path(staging)\n        )\n        if success:\n            staging = rootless_staging_dir\n        else:\n            log.warning((\n                \"Could not find root path for remapping \\\"{}\\\".\"\n                \" This may cause issues on farm.\"\n            ).format(staging))\n\n        frame_start = frames_to_render[0]\n        frame_end = frames_to_render[-1]\n        if skeleton_data.get(\"slate\"):\n            frame_start -= 1\n            frames_to_render.insert(0, frame_start)\n\n        filenames = [\n            os.path.basename(filepath)\n            for filepath in _get_real_files_to_render(\n                collection, frames_to_render\n            )\n        ]\n        # explicitly disable review by user\n        preview = preview and not do_not_add_review\n        rep = {\n            \"name\": ext,\n            \"ext\": ext,\n            \"files\": filenames,\n            \"stagingDir\": staging,\n            \"frameStart\": frame_start,\n            \"frameEnd\": frame_end,\n            \"fps\": skeleton_data.get(\"fps\"),\n            \"tags\": [\"review\"] if preview else [],\n        }\n\n        # poor man exclusion\n        if ext in skip_integration_repre_list:\n            rep[\"tags\"].append(\"delete\")\n\n        if skeleton_data.get(\"multipartExr\", False):\n            rep[\"tags\"].append(\"multipartExr\")\n\n        # support conversion from tiled to scanline\n        if skeleton_data.get(\"convertToScanline\"):\n            log.info(\"Adding scanline conversion.\")\n            rep[\"tags\"].append(\"toScanline\")\n\n        representations.append(rep)\n\n        if preview:\n            skeleton_data[\"families\"] = _add_review_families(\n                skeleton_data[\"families\"])\n\n    # add remainders as representations\n    for remainder in remainders:\n        ext = remainder.split(\".\")[-1]\n\n        staging = os.path.dirname(remainder)\n        success, rootless_staging_dir = (\n            anatomy.find_root_template_from_path(staging)\n        )\n        if success:\n            staging = rootless_staging_dir\n        else:\n            log.warning((\n                \"Could not find root path for remapping \\\"{}\\\".\"\n                \" This may cause issues on farm.\"\n            ).format(staging))\n\n        rep = {\n            \"name\": ext,\n            \"ext\": ext,\n            \"files\": os.path.basename(remainder),\n            \"stagingDir\": staging,\n        }\n\n        preview = match_aov_pattern(\n            host_name, aov_filter, remainder\n        )\n        preview = preview and not do_not_add_review\n        if preview:\n            rep.update({\n                \"fps\": skeleton_data.get(\"fps\"),\n                \"tags\": [\"review\"]\n            })\n            skeleton_data[\"families\"] = \\\n                _add_review_families(skeleton_data[\"families\"])\n\n        already_there = False\n        for repre in skeleton_data.get(\"representations\", []):\n            # might be added explicitly before by publish_on_farm\n            already_there = repre.get(\"files\") == rep[\"files\"]\n            if already_there:\n                log.debug(\"repre {} already_there\".format(repre))\n                break\n\n        if not already_there:\n            representations.append(rep)\n\n    for rep in representations:\n        # inject colorspace data\n        color_managed_plugin.set_representation_colorspace(\n            rep, context,\n            colorspace=skeleton_data[\"colorspace\"]\n        )\n\n    return representations\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/farm/pyblish_functions.html#client.ayon_core.pipeline.farm.pyblish_functions.remap_source","title":"<code>remap_source(path, anatomy)</code>","text":"<p>Try to remap path to rootless path.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Path to be remapped to rootless.</p> required <code>anatomy</code> <code>Anatomy</code> <p>Anatomy object to handle remapping itself.</p> required <p>Returns:</p> Name Type Description <code>str</code> <p>Remapped path.</p> Throws <p>ValueError: if the root cannot be found.</p> Source code in <code>client/ayon_core/pipeline/farm/pyblish_functions.py</code> <pre><code>def remap_source(path, anatomy):\n    \"\"\"Try to remap path to rootless path.\n\n    Args:\n        path (str): Path to be remapped to rootless.\n        anatomy (Anatomy): Anatomy object to handle remapping\n            itself.\n\n    Returns:\n        str: Remapped path.\n\n    Throws:\n        ValueError: if the root cannot be found.\n\n    \"\"\"\n    success, rootless_path = (\n        anatomy.find_root_template_from_path(path)\n    )\n    if success:\n        return rootless_path\n    raise ValueError(\n        f\"Root from template path cannot be found: {path}\"\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/farm/tools.html","title":"tools","text":""},{"location":"autoapi/client/ayon_core/pipeline/farm/tools.html#client.ayon_core.pipeline.farm.tools.from_published_scene","title":"<code>from_published_scene(instance, replace_in_path=True)</code>","text":"<p>Switch work scene for published scene.</p> <p>If rendering/exporting from published scenes is enabled, this will replace paths from working scene to published scene.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>Instance</code> <p>Instance data to process.</p> required <code>replace_in_path</code> <code>bool</code> <p>if True, it will try to find old scene name in path of expected files and replace it with name of published scene.</p> <code>True</code> <p>Returns:</p> Name Type Description <code>str</code> <p>Published scene path.</p> <code>None</code> <p>if no published scene is found.</p> Note <p>Published scene path is actually determined from project Anatomy as at the time this plugin is running the scene can be still un-published.</p> Source code in <code>client/ayon_core/pipeline/farm/tools.py</code> <pre><code>def from_published_scene(instance, replace_in_path=True):\n    \"\"\"Switch work scene for published scene.\n\n    If rendering/exporting from published scenes is enabled, this will\n    replace paths from working scene to published scene.\n\n    Args:\n        instance (pyblish.api.Instance): Instance data to process.\n        replace_in_path (bool): if True, it will try to find\n            old scene name in path of expected files and replace it\n            with name of published scene.\n\n    Returns:\n        str: Published scene path.\n        None: if no published scene is found.\n\n    Note:\n        Published scene path is actually determined from project Anatomy\n        as at the time this plugin is running the scene can be still\n        un-published.\n\n    \"\"\"\n    workfile_instance = get_published_workfile_instance(instance.context)\n    if workfile_instance is None:\n        return\n\n    # determine published path from Anatomy.\n    template_data = workfile_instance.data.get(\"anatomyData\")\n    rep = workfile_instance.data[\"representations\"][0]\n    template_data[\"representation\"] = rep.get(\"name\")\n    template_data[\"ext\"] = rep.get(\"ext\")\n    template_data[\"comment\"] = None\n\n    anatomy = instance.context.data['anatomy']\n    template_obj = anatomy.get_template_item(\"publish\", \"default\", \"path\")\n    template_filled = template_obj.format_strict(template_data)\n    file_path = os.path.normpath(template_filled)\n\n    if not os.path.exists(file_path):\n        raise\n\n    if not replace_in_path:\n        return file_path\n\n    # now we need to switch scene in expected files\n    # because &lt;scene&gt; token will now point to published\n    # scene file and that might differ from current one\n    def _clean_name(path):\n        return os.path.splitext(os.path.basename(path))[0]\n\n    new_scene = _clean_name(file_path)\n    orig_scene = _clean_name(instance.context.data[\"currentFile\"])\n    expected_files = instance.data.get(\"expectedFiles\")\n\n    if isinstance(expected_files[0], dict):\n        # we have aovs and we need to iterate over them\n        new_exp = {}\n        for aov, files in expected_files[0].items():\n            replaced_files = []\n            for f in files:\n                replaced_files.append(\n                    str(f).replace(orig_scene, new_scene)\n                )\n            new_exp[aov] = replaced_files\n        # [] might be too much here, TODO\n        instance.data[\"expectedFiles\"] = [new_exp]\n    else:\n        new_exp = []\n        for f in expected_files:\n            new_exp.append(\n                str(f).replace(orig_scene, new_scene)\n            )\n        instance.data[\"expectedFiles\"] = new_exp\n\n    metadata_folder = instance.data.get(\"publishRenderMetadataFolder\")\n    if metadata_folder:\n        metadata_folder = metadata_folder.replace(orig_scene,\n                                                  new_scene)\n        instance.data[\"publishRenderMetadataFolder\"] = metadata_folder\n\n    return file_path\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/farm/tools.html#client.ayon_core.pipeline.farm.tools.get_published_workfile_instance","title":"<code>get_published_workfile_instance(context)</code>","text":"<p>Find workfile instance in context</p> Source code in <code>client/ayon_core/pipeline/farm/tools.py</code> <pre><code>def get_published_workfile_instance(context):\n    \"\"\"Find workfile instance in context\"\"\"\n    for i in context:\n        product_base_type = i.data.get(\"productBaseType\")\n        if not product_base_type:\n            product_base_type = i.data[\"productType\"]\n\n        is_workfile = (\n            product_base_type == \"workfile\"\n            or \"workfile\" in i.data.get(\"families\", [])\n\n        )\n        if not is_workfile:\n            continue\n\n        # test if there is instance of workfile waiting\n        # to be published.\n        if i.data[\"publish\"] is not True:\n            continue\n\n        return i\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/index.html","title":"load","text":""},{"location":"autoapi/client/ayon_core/pipeline/load/index.html#client.ayon_core.pipeline.load.IncompatibleLoaderError","title":"<code>IncompatibleLoaderError</code>","text":"<p>               Bases: <code>ValueError</code></p> <p>Error when Loader is incompatible with a representation.</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>class IncompatibleLoaderError(ValueError):\n    \"\"\"Error when Loader is incompatible with a representation.\"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/index.html#client.ayon_core.pipeline.load.InvalidRepresentationContext","title":"<code>InvalidRepresentationContext</code>","text":"<p>               Bases: <code>ValueError</code></p> <p>Representation path can't be received using representation document.</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>class InvalidRepresentationContext(ValueError):\n    \"\"\"Representation path can't be received using representation document.\"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/index.html#client.ayon_core.pipeline.load.LoadError","title":"<code>LoadError</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Known error that happened during loading.</p> <p>A message is shown to user (without traceback). Make sure an artist can understand the problem.</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>class LoadError(Exception):\n    \"\"\"Known error that happened during loading.\n\n    A message is shown to user (without traceback). Make sure an artist can\n    understand the problem.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/index.html#client.ayon_core.pipeline.load.LoaderNotFoundError","title":"<code>LoaderNotFoundError</code>","text":"<p>               Bases: <code>RuntimeError</code></p> <p>Error when Loader plugin for a loader name is not found.</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>class LoaderNotFoundError(RuntimeError):\n    \"\"\"Error when Loader plugin for a loader name is not found.\"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/index.html#client.ayon_core.pipeline.load.LoaderPlugin","title":"<code>LoaderPlugin</code>","text":"<p>               Bases: <code>list</code></p> <p>Load representation into host application</p> Source code in <code>client/ayon_core/pipeline/load/plugins.py</code> <pre><code>class LoaderPlugin(list):\n    \"\"\"Load representation into host application\"\"\"\n\n    # Attribute 'skip_discovery' is used during discovery phase to skip\n    #   plugins, which can be used to mark base plugins that should not be\n    #   considered as plugins \"to use\". The discovery logic does NOT use\n    #   the attribute value from parent classes. Each base class has to define\n    #   the attribute again.\n    skip_discovery = True\n\n    product_types: set[str] = set()\n    product_base_types: Optional[set[str]] = None\n    representations = set()\n    extensions = {\"*\"}\n    order = 0\n    is_multiple_contexts_compatible = False\n    enabled = True\n\n    options = []\n\n    log = Logger.get_logger(\"ProductLoader\")\n\n    @classmethod\n    def apply_settings(cls, project_settings):\n        host_name = os.environ.get(\"AYON_HOST_NAME\")\n        plugin_type = \"load\"\n        plugin_type_settings = (\n            project_settings\n            .get(host_name, {})\n            .get(plugin_type, {})\n        )\n        global_type_settings = (\n            project_settings\n            .get(\"core\", {})\n            .get(plugin_type, {})\n        )\n        if not global_type_settings and not plugin_type_settings:\n            return\n\n        plugin_name = cls.__name__\n\n        plugin_settings = None\n        # Look for plugin settings in host specific settings\n        if plugin_name in plugin_type_settings:\n            plugin_settings = plugin_type_settings[plugin_name]\n\n        # Look for plugin settings in global settings\n        elif plugin_name in global_type_settings:\n            plugin_settings = global_type_settings[plugin_name]\n\n        if not plugin_settings:\n            return\n\n        print(f\"&gt;&gt;&gt; We have preset for {plugin_name}\")\n        for option, value in plugin_settings.items():\n            if option == \"enabled\" and value is False:\n                print(\"  - is disabled by preset\")\n            else:\n                print(f\"  - setting `{option}`: `{value}`\")\n            setattr(cls, option, value)\n\n    @classmethod\n    def has_valid_extension(cls, repre_entity):\n        \"\"\"Has representation document valid extension for loader.\n\n        Args:\n            repre_entity (dict[str, Any]): Representation entity.\n\n        Returns:\n             bool: Representation has valid extension\n        \"\"\"\n        if \"*\" in cls.extensions:\n            return True\n\n        # Get representation main file extension from 'context'\n        repre_context = repre_entity.get(\"context\") or {}\n        ext = repre_context.get(\"ext\")\n        if not ext:\n            # Legacy way how to get extensions\n            path = repre_entity.get(\"attrib\", {}).get(\"path\")\n            if not path:\n                cls.log.info(\n                    \"Representation doesn't have known source of extension\"\n                    \" information.\"\n                )\n                return False\n\n            cls.log.debug(\"Using legacy source of extension from path.\")\n            ext = os.path.splitext(path)[-1].lstrip(\".\")\n\n        # If representation does not have extension then can't be valid\n        if not ext:\n            return False\n\n        valid_extensions_low = {ext.lower() for ext in cls.extensions}\n        return ext.lower() in valid_extensions_low\n\n    @classmethod\n    def is_compatible_loader(cls, context):\n        \"\"\"Return whether a loader is compatible with a context.\n\n        On override make sure it is overridden as class or static method.\n\n        This checks the product type and the representation for the given\n        loader plugin.\n\n        Args:\n            context (dict[str, Any]): Documents of context for which should\n                be loader used.\n\n        Returns:\n            bool: Is loader compatible for context.\n        \"\"\"\n\n        plugin_repre_names = cls.get_representations()\n\n        # If the product base type isn't defined on the loader plugin,\n        # then we will use the product types.\n        plugin_product_filter = cls.product_base_types\n        if plugin_product_filter is None:\n            plugin_product_filter = cls.product_types\n\n        if plugin_product_filter:\n            plugin_product_filter = set(plugin_product_filter)\n\n        repre_entity = context.get(\"representation\")\n        product_entity = context[\"product\"]\n\n        # If no representation names, product types or extensions are defined\n        # then loader is not compatible with any context.\n        if (\n            not plugin_repre_names\n            or not plugin_product_filter\n            or not cls.extensions\n        ):\n            return False\n\n        # If no representation entity is provided then loader is not\n        # compatible with context.\n        if not repre_entity:\n            return False\n\n        # Check the compatibility with the representation names.\n        plugin_repre_names = set(plugin_repre_names)\n        if (\n            \"*\" not in plugin_repre_names\n            and repre_entity[\"name\"] not in plugin_repre_names\n        ):\n            return False\n\n        # Check the compatibility with the extension of the representation.\n        if not cls.has_valid_extension(repre_entity):\n            return False\n\n        product_type = product_entity.get(\"productType\")\n        product_base_type = product_entity.get(\"productBaseType\")\n\n        # Use product base type if defined, otherwise use product type.\n        product_filter = product_base_type\n        # If there is no product base type defined in the product entity,\n        # then we will use the product type.\n        if product_filter is None:\n            product_filter = product_type\n\n        # If wildcard is used in product types or base types,\n        # then we will consider the loader compatible with any product type.\n        if \"*\" in plugin_product_filter:\n            return True\n\n        # compatibility with legacy loader\n        if cls.product_base_types is None and product_base_type:\n            cls.log.error(\n                f\"Loader {cls.__name__} is doesn't specify \"\n                \"`product_base_types` but product entity has \"\n                f\"`productBaseType` defined as `{product_base_type}`. \"\n            )\n\n        return product_filter in plugin_product_filter\n\n    @classmethod\n    def get_representations(cls):\n        \"\"\"Representation names with which is plugin compatible.\n\n        Empty set makes the plugin incompatible with any representation. To\n            allow compatibility with all representations use '{\"*\"}'.\n\n        Returns:\n            set[str]: Names with which is plugin compatible.\n\n        \"\"\"\n        return cls.representations\n\n    @classmethod\n    def filepath_from_context(cls, context):\n        return get_representation_path_from_context(context)\n\n    def load(self, context, name=None, namespace=None, options=None):\n        \"\"\"Load asset via database\n\n        Arguments:\n            context (dict): Full parenthood of representation to load\n            name (str, optional): Use pre-defined name\n            namespace (str, optional): Use pre-defined namespace\n            options (dict, optional): Additional settings dictionary\n\n        \"\"\"\n        raise NotImplementedError(\"Loader.load() must be \"\n                                  \"implemented by subclass\")\n\n    def update(self, container, context):\n        \"\"\"Update `container` to `representation`\n\n        Args:\n            container (avalon-core:container-1.0): Container to update,\n                from `host.ls()`.\n            context (dict): Update the container to this representation.\n\n        \"\"\"\n        raise NotImplementedError(\"Loader.update() must be \"\n                                  \"implemented by subclass\")\n\n    def remove(self, container):\n        \"\"\"Remove a container\n\n        Arguments:\n            container (avalon-core:container-1.0): Container to remove,\n                from `host.ls()`.\n\n        Returns:\n            bool: Whether the container was deleted\n\n        \"\"\"\n        raise NotImplementedError(\"Loader.remove() must be \"\n                                  \"implemented by subclass\")\n\n    @classmethod\n    def get_options(cls, contexts):\n        \"\"\"Returns static (cls) options or could collect from 'contexts'.\n\n        Args:\n            contexts (list): of repre or product contexts\n        Returns:\n            (list)\n        \"\"\"\n        return cls.options or []\n\n    @classmethod\n    def get_representation_name_aliases(cls, representation_name: str):\n        \"\"\"Return representation names to which switching is allowed from\n        the input representation name, like an alias replacement of the input\n        `representation_name`.\n\n        For example, to allow an automated switch on update from representation\n        `ma` to `mb` or `abc`, then when `representation_name` is `ma` return:\n            [\"mb\", \"abc\"]\n\n        The order of the names in the returned representation names is\n        important, because the first one existing under the new version will\n        be chosen.\n\n        Returns:\n            List[str]: Representation names switching to is allowed on update\n              if the input representation name is not found on the new version.\n        \"\"\"\n        return []\n\n    def __repr__(self):\n        # Avoid printing as a list (due to inheritance), but print like object.\n        return object.__repr__(self)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/index.html#client.ayon_core.pipeline.load.LoaderPlugin.get_options","title":"<code>get_options(contexts)</code>  <code>classmethod</code>","text":"<p>Returns static (cls) options or could collect from 'contexts'.</p> <p>Parameters:</p> Name Type Description Default <code>contexts</code> <code>list</code> <p>of repre or product contexts</p> required <p>Returns:     (list)</p> Source code in <code>client/ayon_core/pipeline/load/plugins.py</code> <pre><code>@classmethod\ndef get_options(cls, contexts):\n    \"\"\"Returns static (cls) options or could collect from 'contexts'.\n\n    Args:\n        contexts (list): of repre or product contexts\n    Returns:\n        (list)\n    \"\"\"\n    return cls.options or []\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/index.html#client.ayon_core.pipeline.load.LoaderPlugin.get_representation_name_aliases","title":"<code>get_representation_name_aliases(representation_name)</code>  <code>classmethod</code>","text":"<p>Return representation names to which switching is allowed from the input representation name, like an alias replacement of the input <code>representation_name</code>.</p> <p>For example, to allow an automated switch on update from representation <code>ma</code> to <code>mb</code> or <code>abc</code>, then when <code>representation_name</code> is <code>ma</code> return:     [\"mb\", \"abc\"]</p> <p>The order of the names in the returned representation names is important, because the first one existing under the new version will be chosen.</p> <p>Returns:</p> Type Description <p>List[str]: Representation names switching to is allowed on update if the input representation name is not found on the new version.</p> Source code in <code>client/ayon_core/pipeline/load/plugins.py</code> <pre><code>@classmethod\ndef get_representation_name_aliases(cls, representation_name: str):\n    \"\"\"Return representation names to which switching is allowed from\n    the input representation name, like an alias replacement of the input\n    `representation_name`.\n\n    For example, to allow an automated switch on update from representation\n    `ma` to `mb` or `abc`, then when `representation_name` is `ma` return:\n        [\"mb\", \"abc\"]\n\n    The order of the names in the returned representation names is\n    important, because the first one existing under the new version will\n    be chosen.\n\n    Returns:\n        List[str]: Representation names switching to is allowed on update\n          if the input representation name is not found on the new version.\n    \"\"\"\n    return []\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/index.html#client.ayon_core.pipeline.load.LoaderPlugin.get_representations","title":"<code>get_representations()</code>  <code>classmethod</code>","text":"<p>Representation names with which is plugin compatible.</p> <p>Empty set makes the plugin incompatible with any representation. To     allow compatibility with all representations use '{\"*\"}'.</p> <p>Returns:</p> Type Description <p>set[str]: Names with which is plugin compatible.</p> Source code in <code>client/ayon_core/pipeline/load/plugins.py</code> <pre><code>@classmethod\ndef get_representations(cls):\n    \"\"\"Representation names with which is plugin compatible.\n\n    Empty set makes the plugin incompatible with any representation. To\n        allow compatibility with all representations use '{\"*\"}'.\n\n    Returns:\n        set[str]: Names with which is plugin compatible.\n\n    \"\"\"\n    return cls.representations\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/index.html#client.ayon_core.pipeline.load.LoaderPlugin.has_valid_extension","title":"<code>has_valid_extension(repre_entity)</code>  <code>classmethod</code>","text":"<p>Has representation document valid extension for loader.</p> <p>Parameters:</p> Name Type Description Default <code>repre_entity</code> <code>dict[str, Any]</code> <p>Representation entity.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <p>Representation has valid extension</p> Source code in <code>client/ayon_core/pipeline/load/plugins.py</code> <pre><code>@classmethod\ndef has_valid_extension(cls, repre_entity):\n    \"\"\"Has representation document valid extension for loader.\n\n    Args:\n        repre_entity (dict[str, Any]): Representation entity.\n\n    Returns:\n         bool: Representation has valid extension\n    \"\"\"\n    if \"*\" in cls.extensions:\n        return True\n\n    # Get representation main file extension from 'context'\n    repre_context = repre_entity.get(\"context\") or {}\n    ext = repre_context.get(\"ext\")\n    if not ext:\n        # Legacy way how to get extensions\n        path = repre_entity.get(\"attrib\", {}).get(\"path\")\n        if not path:\n            cls.log.info(\n                \"Representation doesn't have known source of extension\"\n                \" information.\"\n            )\n            return False\n\n        cls.log.debug(\"Using legacy source of extension from path.\")\n        ext = os.path.splitext(path)[-1].lstrip(\".\")\n\n    # If representation does not have extension then can't be valid\n    if not ext:\n        return False\n\n    valid_extensions_low = {ext.lower() for ext in cls.extensions}\n    return ext.lower() in valid_extensions_low\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/index.html#client.ayon_core.pipeline.load.LoaderPlugin.is_compatible_loader","title":"<code>is_compatible_loader(context)</code>  <code>classmethod</code>","text":"<p>Return whether a loader is compatible with a context.</p> <p>On override make sure it is overridden as class or static method.</p> <p>This checks the product type and the representation for the given loader plugin.</p> <p>Parameters:</p> Name Type Description Default <code>context</code> <code>dict[str, Any]</code> <p>Documents of context for which should be loader used.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <p>Is loader compatible for context.</p> Source code in <code>client/ayon_core/pipeline/load/plugins.py</code> <pre><code>@classmethod\ndef is_compatible_loader(cls, context):\n    \"\"\"Return whether a loader is compatible with a context.\n\n    On override make sure it is overridden as class or static method.\n\n    This checks the product type and the representation for the given\n    loader plugin.\n\n    Args:\n        context (dict[str, Any]): Documents of context for which should\n            be loader used.\n\n    Returns:\n        bool: Is loader compatible for context.\n    \"\"\"\n\n    plugin_repre_names = cls.get_representations()\n\n    # If the product base type isn't defined on the loader plugin,\n    # then we will use the product types.\n    plugin_product_filter = cls.product_base_types\n    if plugin_product_filter is None:\n        plugin_product_filter = cls.product_types\n\n    if plugin_product_filter:\n        plugin_product_filter = set(plugin_product_filter)\n\n    repre_entity = context.get(\"representation\")\n    product_entity = context[\"product\"]\n\n    # If no representation names, product types or extensions are defined\n    # then loader is not compatible with any context.\n    if (\n        not plugin_repre_names\n        or not plugin_product_filter\n        or not cls.extensions\n    ):\n        return False\n\n    # If no representation entity is provided then loader is not\n    # compatible with context.\n    if not repre_entity:\n        return False\n\n    # Check the compatibility with the representation names.\n    plugin_repre_names = set(plugin_repre_names)\n    if (\n        \"*\" not in plugin_repre_names\n        and repre_entity[\"name\"] not in plugin_repre_names\n    ):\n        return False\n\n    # Check the compatibility with the extension of the representation.\n    if not cls.has_valid_extension(repre_entity):\n        return False\n\n    product_type = product_entity.get(\"productType\")\n    product_base_type = product_entity.get(\"productBaseType\")\n\n    # Use product base type if defined, otherwise use product type.\n    product_filter = product_base_type\n    # If there is no product base type defined in the product entity,\n    # then we will use the product type.\n    if product_filter is None:\n        product_filter = product_type\n\n    # If wildcard is used in product types or base types,\n    # then we will consider the loader compatible with any product type.\n    if \"*\" in plugin_product_filter:\n        return True\n\n    # compatibility with legacy loader\n    if cls.product_base_types is None and product_base_type:\n        cls.log.error(\n            f\"Loader {cls.__name__} is doesn't specify \"\n            \"`product_base_types` but product entity has \"\n            f\"`productBaseType` defined as `{product_base_type}`. \"\n        )\n\n    return product_filter in plugin_product_filter\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/index.html#client.ayon_core.pipeline.load.LoaderPlugin.load","title":"<code>load(context, name=None, namespace=None, options=None)</code>","text":"<p>Load asset via database</p> <p>Parameters:</p> Name Type Description Default <code>context</code> <code>dict</code> <p>Full parenthood of representation to load</p> required <code>name</code> <code>str</code> <p>Use pre-defined name</p> <code>None</code> <code>namespace</code> <code>str</code> <p>Use pre-defined namespace</p> <code>None</code> <code>options</code> <code>dict</code> <p>Additional settings dictionary</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/load/plugins.py</code> <pre><code>def load(self, context, name=None, namespace=None, options=None):\n    \"\"\"Load asset via database\n\n    Arguments:\n        context (dict): Full parenthood of representation to load\n        name (str, optional): Use pre-defined name\n        namespace (str, optional): Use pre-defined namespace\n        options (dict, optional): Additional settings dictionary\n\n    \"\"\"\n    raise NotImplementedError(\"Loader.load() must be \"\n                              \"implemented by subclass\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/index.html#client.ayon_core.pipeline.load.LoaderPlugin.remove","title":"<code>remove(container)</code>","text":"<p>Remove a container</p> <p>Parameters:</p> Name Type Description Default <code>container (avalon-core</code> <p>container-1.0): Container to remove, from <code>host.ls()</code>.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <p>Whether the container was deleted</p> Source code in <code>client/ayon_core/pipeline/load/plugins.py</code> <pre><code>def remove(self, container):\n    \"\"\"Remove a container\n\n    Arguments:\n        container (avalon-core:container-1.0): Container to remove,\n            from `host.ls()`.\n\n    Returns:\n        bool: Whether the container was deleted\n\n    \"\"\"\n    raise NotImplementedError(\"Loader.remove() must be \"\n                              \"implemented by subclass\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/index.html#client.ayon_core.pipeline.load.LoaderPlugin.update","title":"<code>update(container, context)</code>","text":"<p>Update <code>container</code> to <code>representation</code></p> <p>Parameters:</p> Name Type Description Default <code>container (avalon-core</code> <p>container-1.0): Container to update, from <code>host.ls()</code>.</p> required <code>context</code> <code>dict</code> <p>Update the container to this representation.</p> required Source code in <code>client/ayon_core/pipeline/load/plugins.py</code> <pre><code>def update(self, container, context):\n    \"\"\"Update `container` to `representation`\n\n    Args:\n        container (avalon-core:container-1.0): Container to update,\n            from `host.ls()`.\n        context (dict): Update the container to this representation.\n\n    \"\"\"\n    raise NotImplementedError(\"Loader.update() must be \"\n                              \"implemented by subclass\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/index.html#client.ayon_core.pipeline.load.LoaderSwitchNotImplementedError","title":"<code>LoaderSwitchNotImplementedError</code>","text":"<p>               Bases: <code>NotImplementedError</code></p> <p>Error when <code>switch</code> is used with Loader that has no implementation.</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>class LoaderSwitchNotImplementedError(NotImplementedError):\n    \"\"\"Error when `switch` is used with Loader that has no implementation.\"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/index.html#client.ayon_core.pipeline.load.ProductLoaderPlugin","title":"<code>ProductLoaderPlugin</code>","text":"<p>               Bases: <code>LoaderPlugin</code></p> <p>Load product into host application Arguments:     context (dict): avalon-core:context-1.0     name (str, optional): Use pre-defined name     namespace (str, optional): Use pre-defined namespace</p> Source code in <code>client/ayon_core/pipeline/load/plugins.py</code> <pre><code>class ProductLoaderPlugin(LoaderPlugin):\n    \"\"\"Load product into host application\n    Arguments:\n        context (dict): avalon-core:context-1.0\n        name (str, optional): Use pre-defined name\n        namespace (str, optional): Use pre-defined namespace\n    \"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/index.html#client.ayon_core.pipeline.load.any_outdated_containers","title":"<code>any_outdated_containers(host=None, project_name=None)</code>","text":"<p>Check if there are any outdated containers in scene.</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>def any_outdated_containers(host=None, project_name=None):\n    \"\"\"Check if there are any outdated containers in scene.\"\"\"\n\n    if get_outdated_containers(host, project_name):\n        return True\n    return False\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/index.html#client.ayon_core.pipeline.load.filter_containers","title":"<code>filter_containers(containers, project_name)</code>","text":"<p>Filter containers and split them into 4 categories.</p> <p>Categories are 'latest', 'outdated', 'invalid' and 'not_found'. The 'lastest' containers are from last version, 'outdated' are not, 'invalid' are invalid containers (invalid content) and 'not_found' has some missing entity in database.</p> <p>Parameters:</p> Name Type Description Default <code>containers</code> <code>Iterable[dict]</code> <p>List of containers referenced into scene.</p> required <code>project_name</code> <code>str</code> <p>Current project name.</p> required <p>Returns:</p> Name Type Description <code>ContainersFilterResult</code> <p>Named tuple with 'latest', 'outdated', 'invalid' and 'not_found' containers.</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>def filter_containers(containers, project_name):\n    \"\"\"Filter containers and split them into 4 categories.\n\n    Categories are 'latest', 'outdated', 'invalid' and 'not_found'.\n    The 'lastest' containers are from last version, 'outdated' are not,\n    'invalid' are invalid containers (invalid content) and 'not_found' has\n    some missing entity in database.\n\n    Args:\n        containers (Iterable[dict]): List of containers referenced into scene.\n        project_name (str): Current project name.\n\n    Returns:\n        ContainersFilterResult: Named tuple with 'latest', 'outdated',\n            'invalid' and 'not_found' containers.\n\n    \"\"\"\n    outdated_containers = []\n    uptodate_containers = []\n    not_found_containers = []\n    invalid_containers = []\n    output = ContainersFilterResult(\n        uptodate_containers,\n        outdated_containers,\n        not_found_containers,\n        invalid_containers\n    )\n\n    # Iterate over containers and group them by project name\n    # - filter out containers with invalid representation id\n    # - because 'containers' variable is iterable, this is the only place\n    #   where it should be used.\n    containers_by_project_name = collections.defaultdict(list)\n    for container in containers:\n        if not _is_valid_representation_id(container[\"representation\"]):\n            invalid_containers.append(container)\n            continue\n        container_project = container.get(\"project_name\")\n        if not container_project:\n            container_project = project_name\n        containers_by_project_name[container_project].append(container)\n\n    # Fetch entities to be able to filter the containers\n    # - entities can be fetched per project only\n    for l_project_name, l_containers in containers_by_project_name.items():\n        repre_ids = {\n            container[\"representation\"]\n            for container in l_containers\n        }\n        repre_entities = ayon_api.get_representations(\n            l_project_name,\n            representation_ids=repre_ids,\n            fields={\"id\", \"versionId\"}\n        )\n        version_ids = set()\n        repre_entities_by_id = {}\n        for repre_entity in repre_entities:\n            repre_id = repre_entity[\"id\"]\n            version_id = repre_entity[\"versionId\"]\n            version_ids.add(version_id)\n            repre_entities_by_id[repre_id] = repre_entity\n\n        # Query version docs to get it's product ids\n        # - also query hero version to be able identify if representation\n        #   belongs to existing version\n        version_entities = ayon_api.get_versions(\n            l_project_name,\n            version_ids=version_ids,\n            hero=True,\n            fields={\"id\", \"productId\", \"version\"}\n        )\n        versions_by_id = {}\n        versions_by_product_id = collections.defaultdict(list)\n        hero_version_ids = set()\n        for version_entity in version_entities:\n            version_id = version_entity[\"id\"]\n            # Store versions by their ids\n            versions_by_id[version_id] = version_entity\n            # There's no need to query products for hero versions\n            #   - they are considered as latest?\n            if version_entity[\"version\"] &lt; 0:\n                hero_version_ids.add(version_id)\n                continue\n            product_id = version_entity[\"productId\"]\n            versions_by_product_id[product_id].append(version_entity)\n\n        last_versions = ayon_api.get_last_versions(\n            l_project_name,\n            versions_by_product_id.keys(),\n            fields={\"id\"}\n        )\n\n        # Figure out which versions are outdated\n        outdated_version_ids = set()\n        for product_id, last_version_entity in last_versions.items():\n            for version_entity in versions_by_product_id[product_id]:\n                version_id = version_entity[\"id\"]\n                if version_id in hero_version_ids:\n                    continue\n                if version_id != last_version_entity[\"id\"]:\n                    outdated_version_ids.add(version_id)\n\n        # Based on all collected data figure out which containers are outdated\n        #   - log out if there are missing representation or version entities\n        for container in l_containers:\n            container_name = container[\"objectName\"]\n            repre_id = container[\"representation\"]\n            repre_entity = repre_entities_by_id.get(repre_id)\n            if not repre_entity:\n                log.debug(\n                    f\"Container '{container_name}' has an invalid\"\n                    \" representation. It is missing in the database.\"\n                )\n                not_found_containers.append(container)\n                continue\n\n            version_id = repre_entity[\"versionId\"]\n            if version_id not in versions_by_id:\n                log.debug(\n                    f\"Representation on container '{container_name}' has an\"\n                    \" invalid version. It is missing in the database.\"\n                )\n                not_found_containers.append(container)\n\n            elif version_id in outdated_version_ids:\n                outdated_containers.append(container)\n            else:\n                uptodate_containers.append(container)\n\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/index.html#client.ayon_core.pipeline.load.filter_repre_contexts_by_loader","title":"<code>filter_repre_contexts_by_loader(repre_contexts, loader)</code>","text":"<p>Filter representation contexts for loader.</p> <p>Parameters:</p> Name Type Description Default <code>repre_contexts</code> <code>list[dict[str, Ant]]</code> <p>Representation context.</p> required <code>loader</code> <code>LoaderPlugin</code> <p>Loader plugin to filter contexts for.</p> required <p>Returns:</p> Type Description <p>list[dict[str, Any]]: Filtered representation contexts.</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>def filter_repre_contexts_by_loader(repre_contexts, loader):\n    \"\"\"Filter representation contexts for loader.\n\n    Args:\n        repre_contexts (list[dict[str, Ant]]): Representation context.\n        loader (LoaderPlugin): Loader plugin to filter contexts for.\n\n    Returns:\n        list[dict[str, Any]]: Filtered representation contexts.\n    \"\"\"\n\n    return [\n        repre_context\n        for repre_context in repre_contexts\n        if is_compatible_loader(loader, repre_context)\n    ]\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/index.html#client.ayon_core.pipeline.load.get_loader_identifier","title":"<code>get_loader_identifier(loader)</code>","text":"<p>Loader identifier from loader plugin or object.</p> <p>Identifier should be stored to container for future management.</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>def get_loader_identifier(loader):\n    \"\"\"Loader identifier from loader plugin or object.\n\n    Identifier should be stored to container for future management.\n    \"\"\"\n    if not inspect.isclass(loader):\n        loader = loader.__class__\n    return loader.__name__\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/index.html#client.ayon_core.pipeline.load.get_outdated_containers","title":"<code>get_outdated_containers(host=None, project_name=None, ignore_locked_versions=False)</code>","text":"<p>Collect outdated containers from host scene.</p> <p>Currently registered host and project in global session are used if arguments are not passed.</p> <p>Parameters:</p> Name Type Description Default <code>host</code> <code>Optional[AbstractHost]</code> <p>Host implementation.</p> <code>None</code> <code>project_name</code> <code>Optional[str]</code> <p>Name of project in which context we are.</p> <code>None</code> <code>ignore_locked_versions</code> <code>bool</code> <p>Locked versions are ignored.</p> <code>False</code> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>def get_outdated_containers(\n    host: Optional[AbstractHost] = None,\n    project_name: Optional[str] = None,\n    ignore_locked_versions: bool = False,\n):\n    \"\"\"Collect outdated containers from host scene.\n\n    Currently registered host and project in global session are used if\n    arguments are not passed.\n\n    Args:\n        host (Optional[AbstractHost]): Host implementation.\n        project_name (Optional[str]): Name of project in which context we are.\n        ignore_locked_versions (bool): Locked versions are ignored.\n\n    \"\"\"\n    from ayon_core.pipeline import registered_host, get_current_project_name\n\n    if host is None:\n        host = registered_host()\n\n    if project_name is None:\n        project_name = get_current_project_name()\n\n    if isinstance(host, ILoadHost):\n        containers = host.get_containers()\n    else:\n        containers = host.ls()\n\n    outdated_containers = []\n    for container in filter_containers(containers, project_name).outdated:\n        if (\n            not ignore_locked_versions\n            and container.get(\"version_locked\") is True\n        ):\n            continue\n        outdated_containers.append(container)\n    return outdated_containers\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/index.html#client.ayon_core.pipeline.load.get_product_contexts","title":"<code>get_product_contexts(product_ids, project_name=None)</code>","text":"<p>Return parenthood context for product.</p> <pre><code>Provides context on product granularity - less detail than\n'get_repre_contexts'.\n</code></pre> <p>Args:     product_ids (list): The product ids.     project_name (Optional[str]): Project name. Returns:     dict: The full representation context by representation id.</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>def get_product_contexts(product_ids, project_name=None):\n    \"\"\"Return parenthood context for product.\n\n        Provides context on product granularity - less detail than\n        'get_repre_contexts'.\n    Args:\n        product_ids (list): The product ids.\n        project_name (Optional[str]): Project name.\n    Returns:\n        dict: The full representation context by representation id.\n    \"\"\"\n    from ayon_core.pipeline import get_current_project_name\n\n    contexts = {}\n    if not product_ids:\n        return contexts\n\n    if not project_name:\n        project_name = get_current_project_name()\n    product_entities = ayon_api.get_products(\n        project_name, product_ids=product_ids\n    )\n    product_entities_by_id = {}\n    folder_ids = set()\n    for product_entity in product_entities:\n        product_entities_by_id[product_entity[\"id\"]] = product_entity\n        folder_ids.add(product_entity[\"folderId\"])\n\n    folder_entities_by_id = {\n        folder_entity[\"id\"]: folder_entity\n        for folder_entity in ayon_api.get_folders(\n            project_name, folder_ids=folder_ids\n        )\n    }\n\n    project_entity = ayon_api.get_project(project_name)\n\n    for product_id, product_entity in product_entities_by_id.items():\n        folder_entity = folder_entities_by_id[product_entity[\"folderId\"]]\n        context = {\n            \"project\": project_entity,\n            \"folder\": folder_entity,\n            \"product\": product_entity\n        }\n        contexts[product_id] = context\n\n    return contexts\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/index.html#client.ayon_core.pipeline.load.get_repres_contexts","title":"<code>get_repres_contexts(representation_ids, project_name=None)</code>","text":"<p>Return parenthood context for representation.</p> <p>Parameters:</p> Name Type Description Default <code>representation_ids</code> <code>list</code> <p>The representation ids.</p> required <code>project_name</code> <code>Optional[str]</code> <p>Project name.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>dict</code> <p>The full representation context by representation id. keys are repre_id, value is dictionary with entities of folder, product, version and representation.</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>def get_repres_contexts(representation_ids, project_name=None):\n    \"\"\"Return parenthood context for representation.\n\n    Args:\n        representation_ids (list): The representation ids.\n        project_name (Optional[str]): Project name.\n\n    Returns:\n        dict: The full representation context by representation id.\n            keys are repre_id, value is dictionary with entities of\n            folder, product, version and representation.\n    \"\"\"\n    from ayon_core.pipeline import get_current_project_name\n\n    if not representation_ids:\n        return {}\n\n    if not project_name:\n        project_name = get_current_project_name()\n\n    repre_entities = ayon_api.get_representations(\n        project_name, representation_ids\n    )\n\n    return get_representation_contexts(project_name, repre_entities)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/index.html#client.ayon_core.pipeline.load.get_representation_context","title":"<code>get_representation_context(project_name, representation)</code>","text":"<p>Return parenthood context for representation.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>representation</code> <code>Union[dict[str, Any], str]</code> <p>Representation entity or representation id.</p> required <p>Returns:</p> Type Description <p>dict[str, dict[str, Any]]: The full representation context.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>When representation is invalid or parents were not found.</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>def get_representation_context(project_name, representation):\n    \"\"\"Return parenthood context for representation.\n\n    Args:\n        project_name (str): Project name.\n        representation (Union[dict[str, Any], str]): Representation entity\n            or representation id.\n\n    Returns:\n        dict[str, dict[str, Any]]: The full representation context.\n\n    Raises:\n        ValueError: When representation is invalid or parents were not found.\n\n    \"\"\"\n    if not representation:\n        raise ValueError(\n            \"Invalid argument value {}\".format(str(representation))\n        )\n\n    if isinstance(representation, dict):\n        repre_entity = representation\n        repre_id = repre_entity[\"id\"]\n        context = get_representation_contexts(\n            project_name, [repre_entity]\n        )[repre_id]\n    else:\n        repre_id = representation\n        context = get_representation_contexts_by_ids(\n            project_name, {repre_id}\n        )[repre_id]\n\n    missing_entities = []\n    for key, value in context.items():\n        if value is None:\n            missing_entities.append(key)\n\n    if missing_entities:\n        raise ValueError(\n            \"Not able to receive representation parent types: {}\".format(\n                \", \".join(missing_entities)\n            )\n        )\n\n    return context\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/index.html#client.ayon_core.pipeline.load.get_representation_contexts","title":"<code>get_representation_contexts(project_name, representation_entities)</code>","text":"<p>Parenthood context for representations.</p> <p>Function fills <code>None</code> if any entity was not found or could     not be queried.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>representation_entities</code> <code>Iterable[dict[str, Any]]</code> <p>Representation entities.</p> required <p>Returns:</p> Type Description <p>dict[str, dict[str, Any]]: The full representation context by representation id.</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>def get_representation_contexts(project_name, representation_entities):\n    \"\"\"Parenthood context for representations.\n\n    Function fills ``None`` if any entity was not found or could\n        not be queried.\n\n    Args:\n        project_name (str): Project name.\n        representation_entities (Iterable[dict[str, Any]]): Representation\n            entities.\n\n    Returns:\n        dict[str, dict[str, Any]]: The full representation context by\n            representation id.\n\n    \"\"\"\n    repre_entities_by_id = {\n        repre_entity[\"id\"]: repre_entity\n        for repre_entity in representation_entities\n    }\n\n    if not repre_entities_by_id:\n        return {}\n\n    repre_ids = set(repre_entities_by_id)\n\n    parents_by_repre_id = ayon_api.get_representations_parents(\n        project_name, repre_ids\n    )\n    output = {}\n    for repre_id in repre_ids:\n        repre_entity = repre_entities_by_id[repre_id]\n        (\n            version_entity,\n            product_entity,\n            folder_entity,\n            project_entity\n        ) = parents_by_repre_id[repre_id]\n        output[repre_id] = {\n            \"project\": project_entity,\n            \"folder\": folder_entity,\n            \"product\": product_entity,\n            \"version\": version_entity,\n            \"representation\": repre_entity,\n        }\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/index.html#client.ayon_core.pipeline.load.get_representation_contexts_by_ids","title":"<code>get_representation_contexts_by_ids(project_name, representation_ids)</code>","text":"<p>Parenthood context for representations found by ids.</p> <p>Function fills <code>None</code> if any entity was not found or could     not be queried.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>representation_ids</code> <code>Iterable[str]</code> <p>Representation ids.</p> required <p>Returns:</p> Type Description <p>dict[str, dict[str, Any]]: The full representation context by representation id.</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>def get_representation_contexts_by_ids(project_name, representation_ids):\n    \"\"\"Parenthood context for representations found by ids.\n\n    Function fills ``None`` if any entity was not found or could\n        not be queried.\n\n    Args:\n        project_name (str): Project name.\n        representation_ids (Iterable[str]): Representation ids.\n\n    Returns:\n        dict[str, dict[str, Any]]: The full representation context by\n            representation id.\n\n    \"\"\"\n    repre_ids = set(representation_ids)\n    if not repre_ids:\n        return {}\n\n    # Query representation entities by id\n    repre_entities_by_id = {\n        repre_entity[\"id\"]: repre_entity\n        for repre_entity in ayon_api.get_representations(\n            project_name, repre_ids\n        )\n    }\n    output = get_representation_contexts(\n        project_name, repre_entities_by_id.values()\n    )\n    for repre_id in repre_ids:\n        if repre_id not in output:\n            output[repre_id] = {\n                \"project\": None,\n                \"folder\": None,\n                \"product\": None,\n                \"version\": None,\n                \"representation\": None,\n            }\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/index.html#client.ayon_core.pipeline.load.get_representation_path","title":"<code>get_representation_path(project_name, repre_entity, *, anatomy=None, project_entity=None)</code>","text":"<pre><code>get_representation_path(representation: dict[str, Any], root: Optional[dict[str, Any]] = None) -&gt; TemplateResult\n</code></pre><pre><code>get_representation_path(project_name: str, repre_entity: dict[str, Any], *, anatomy: Optional[Anatomy] = None, project_entity: Optional[dict[str, Any]] = None) -&gt; TemplateResult\n</code></pre> <p>Get filled representation path.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>repre_entity</code> <code>dict[str, Any]</code> <p>Representation entity.</p> required <code>anatomy</code> <code>Optional[Anatomy]</code> <p>Project anatomy.</p> <code>None</code> <code>project_entity</code> <code>Optional[dict[str, Any]</code> <p>Project entity. Is used to initialize Anatomy and is not needed if 'anatomy' is passed in.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>TemplateResult</code> <code>TemplateResult</code> <p>Resolved path to representation.</p> <p>Raises:</p> Type Description <code>InvalidRepresentationContext</code> <p>When representation data are probably invalid or not available.</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>@_backwards_compatibility_repre_path\ndef get_representation_path(\n    project_name: str,\n    repre_entity: dict[str, Any],\n    *,\n    anatomy: Optional[Anatomy] = None,\n    project_entity: Optional[dict[str, Any]] = None,\n) -&gt; TemplateResult:\n    \"\"\"Get filled representation path.\n\n    Args:\n        project_name (str): Project name.\n        repre_entity (dict[str, Any]): Representation entity.\n        anatomy (Optional[Anatomy]): Project anatomy.\n        project_entity (Optional[dict[str, Any]): Project entity. Is used to\n            initialize Anatomy and is not needed if 'anatomy' is passed in.\n\n    Returns:\n        TemplateResult: Resolved path to representation.\n\n    Raises:\n        InvalidRepresentationContext: When representation data are probably\n            invalid or not available.\n\n    \"\"\"\n    if anatomy is None:\n        anatomy = Anatomy(project_name, project_entity=project_entity)\n\n    try:\n        template = repre_entity[\"attrib\"][\"template\"]\n\n    except KeyError as exc:\n        raise InvalidRepresentationContext(\n            \"Failed to receive template from representation entity.\"\n        ) from exc\n\n    try:\n        context = copy.deepcopy(repre_entity[\"context\"])\n        _fix_representation_context_compatibility(context)\n        context[\"root\"] = anatomy.roots\n\n        path = StringTemplate.format_strict_template(template, context)\n\n    except TemplateUnsolved as exc:\n        raise InvalidRepresentationContext(\n            \"Failed to resolve representation template with available data.\"\n        ) from exc\n\n    return path.normalized()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/index.html#client.ayon_core.pipeline.load.get_representation_path_from_context","title":"<code>get_representation_path_from_context(context)</code>","text":"<p>Preparation wrapper using only context as a argument</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>def get_representation_path_from_context(context):\n    \"\"\"Preparation wrapper using only context as a argument\"\"\"\n    from ayon_core.pipeline import get_current_project_name\n\n    representation = context[\"representation\"]\n    project_entity = context.get(\"project\")\n    if project_entity:\n        project_name = project_entity[\"name\"]\n    else:\n        project_name = get_current_project_name()\n    return get_representation_path(\n        project_name,\n        representation,\n        project_entity=project_entity,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/index.html#client.ayon_core.pipeline.load.get_representation_path_with_anatomy","title":"<code>get_representation_path_with_anatomy(repre_entity, anatomy)</code>","text":"<p>Receive representation path using representation document and anatomy.</p> <p>Anatomy is used to replace 'root' key in representation file. Ideally should be used instead of 'get_representation_path' which is based on \"current context\".</p> Future notes <p>We want also be able store resources into representation and I can imagine the result should also contain paths to possible resources.</p> <p>Parameters:</p> Name Type Description Default <code>repre_entity</code> <code>Dict[str, Any]</code> <p>Representation entity.</p> required <code>anatomy</code> <code>Anatomy</code> <p>Project anatomy object.</p> required <p>Returns:</p> Name Type Description <code>TemplateResult</code> <p>Resolved representation path.</p> <p>Raises:</p> Type Description <code>InvalidRepresentationContext</code> <p>When representation data are probably invalid or not available.</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>def get_representation_path_with_anatomy(repre_entity, anatomy):\n    \"\"\"Receive representation path using representation document and anatomy.\n\n    Anatomy is used to replace 'root' key in representation file. Ideally\n    should be used instead of 'get_representation_path' which is based on\n    \"current context\".\n\n    Future notes:\n        We want also be able store resources into representation and I can\n        imagine the result should also contain paths to possible resources.\n\n    Args:\n        repre_entity (Dict[str, Any]): Representation entity.\n        anatomy (Anatomy): Project anatomy object.\n\n    Returns:\n        TemplateResult: Resolved representation path.\n\n    Raises:\n        InvalidRepresentationContext: When representation data are probably\n            invalid or not available.\n\n    \"\"\"\n    return get_representation_path(\n        anatomy.project_name,\n        repre_entity,\n        anatomy=anatomy,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/index.html#client.ayon_core.pipeline.load.is_compatible_loader","title":"<code>is_compatible_loader(Loader, context)</code>","text":"<p>Return whether a loader is compatible with a context.</p> <p>This checks the product type and the representation for the given Loader.</p> <p>Returns:</p> Type Description <p>bool</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>def is_compatible_loader(Loader, context):\n    \"\"\"Return whether a loader is compatible with a context.\n\n    This checks the product type and the representation for the given\n    Loader.\n\n    Returns:\n        bool\n    \"\"\"\n\n    return Loader.is_compatible_loader(context)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/index.html#client.ayon_core.pipeline.load.load_container","title":"<code>load_container(Loader, representation, namespace=None, name=None, options=None, **kwargs)</code>","text":"<p>Use Loader to load a representation.</p> <p>Parameters:</p> Name Type Description Default <code>Loader</code> <code>Loader</code> <p>The loader class to trigger.</p> required <code>representation</code> <code>str or dict</code> <p>The representation id or full representation as returned by the database.</p> required <code>namespace</code> <code>(str, Optional)</code> <p>The namespace to assign. Defaults to None.</p> <code>None</code> <code>name</code> <code>(str, Optional)</code> <p>The name to assign. Defaults to product name.</p> <code>None</code> <code>options</code> <code>(dict, Optional)</code> <p>Additional options to pass on to the loader.</p> <code>None</code> <p>Returns:</p> Type Description <p>The return of the <code>loader.load()</code> method.</p> <p>Raises:</p> Type Description <code>IncompatibleLoaderError</code> <p>When the loader is not compatible with the representation.</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>def load_container(\n    Loader, representation, namespace=None, name=None, options=None, **kwargs\n):\n    \"\"\"Use Loader to load a representation.\n\n    Args:\n        Loader (Loader): The loader class to trigger.\n        representation (str or dict): The representation id\n            or full representation as returned by the database.\n        namespace (str, Optional): The namespace to assign. Defaults to None.\n        name (str, Optional): The name to assign. Defaults to product name.\n        options (dict, Optional): Additional options to pass on to the loader.\n\n    Returns:\n        The return of the `loader.load()` method.\n\n    Raises:\n        IncompatibleLoaderError: When the loader is not compatible with\n            the representation.\n\n    \"\"\"\n    from ayon_core.pipeline import get_current_project_name\n\n    context = get_representation_context(\n        get_current_project_name(), representation\n    )\n    return load_with_repre_context(\n        Loader,\n        context,\n        namespace=namespace,\n        name=name,\n        options=options,\n        **kwargs\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/index.html#client.ayon_core.pipeline.load.loaders_from_repre_context","title":"<code>loaders_from_repre_context(loaders, repre_context)</code>","text":"<p>Return compatible loaders for by representation's context.</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>def loaders_from_repre_context(loaders, repre_context):\n    \"\"\"Return compatible loaders for by representation's context.\"\"\"\n\n    return [\n        loader\n        for loader in loaders\n        if is_compatible_loader(loader, repre_context)\n    ]\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/index.html#client.ayon_core.pipeline.load.loaders_from_representation","title":"<code>loaders_from_representation(loaders, representation)</code>","text":"<p>Return all compatible loaders for a representation.</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>def loaders_from_representation(loaders, representation):\n    \"\"\"Return all compatible loaders for a representation.\"\"\"\n    from ayon_core.pipeline import get_current_project_name\n\n    project_name = get_current_project_name()\n    context = get_representation_context(\n        project_name, representation\n    )\n    return loaders_from_repre_context(loaders, context)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/index.html#client.ayon_core.pipeline.load.remove_container","title":"<code>remove_container(container)</code>","text":"<p>Remove a container</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>def remove_container(container):\n    \"\"\"Remove a container\"\"\"\n\n    Loader = _get_container_loader(container)\n    if not Loader:\n        raise LoaderNotFoundError(\n            \"Can't remove container because loader '{}' was not found.\"\n            .format(container.get(\"loader\"))\n        )\n\n    return Loader().remove(container)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/index.html#client.ayon_core.pipeline.load.switch_container","title":"<code>switch_container(container, representation, loader_plugin=None)</code>","text":"<p>Switch a container to representation</p> <p>Parameters:</p> Name Type Description Default <code>container</code> <code>dict</code> <p>container information</p> required <code>representation</code> <code>dict</code> <p>representation entity</p> required <p>Returns:</p> Type Description <p>return from function call</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>def switch_container(\n    container,\n    representation,\n    loader_plugin=None,\n):\n    \"\"\"Switch a container to representation\n\n    Args:\n        container (dict): container information\n        representation (dict): representation entity\n        loader_plugin (LoaderPlugin)\n\n    Returns:\n        return from function call\n    \"\"\"\n    from ayon_core.pipeline import get_current_project_name\n\n    # Get the Loader for this container\n    if loader_plugin is None:\n        loader_plugin = _get_container_loader(container)\n\n    if not loader_plugin:\n        raise LoaderNotFoundError(\n            \"Can't switch container because loader '{}' was not found.\"\n            .format(container.get(\"loader\"))\n        )\n\n    if not hasattr(loader_plugin, \"switch\"):\n        # Backwards compatibility (classes without switch support\n        # might be better to just have \"switch\" raise NotImplementedError\n        # on the base class of Loader\\\n        raise LoaderSwitchNotImplementedError(\n            \"Loader {} does not support 'switch'\".format(loader_plugin.label)\n        )\n\n    # Get the new representation to switch to\n    project_name = container.get(\"project_name\")\n    if project_name is None:\n        project_name = get_current_project_name()\n\n    context = get_representation_context(\n        project_name, representation[\"id\"]\n    )\n    if not is_compatible_loader(loader_plugin, context):\n        raise IncompatibleLoaderError(\n            \"Loader {} is incompatible with {}\".format(\n                loader_plugin.__name__, context[\"product\"][\"name\"]\n            )\n        )\n\n    loader = loader_plugin(context)\n\n    return loader.switch(container, context)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/index.html#client.ayon_core.pipeline.load.update_container","title":"<code>update_container(container, version=-1)</code>","text":"<p>Update a container</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>def update_container(container, version=-1):\n    \"\"\"Update a container\"\"\"\n    from ayon_core.pipeline import get_current_project_name\n\n    # Compute the different version from 'representation'\n    project_name = container.get(\"project_name\")\n    if project_name is None:\n        project_name = get_current_project_name()\n    repre_id = container[\"representation\"]\n    if not _is_valid_representation_id(repre_id):\n        raise ValueError(\n            f\"Got container with invalid representation id '{repre_id}'\"\n        )\n    current_representation = ayon_api.get_representation_by_id(\n        project_name, repre_id\n    )\n\n    assert current_representation is not None, \"This is a bug\"\n\n    current_version_id = current_representation[\"versionId\"]\n    current_version = ayon_api.get_version_by_id(\n        project_name, current_version_id, fields={\"productId\"}\n    )\n    if isinstance(version, HeroVersionType):\n        new_version = ayon_api.get_hero_version_by_product_id(\n            project_name, current_version[\"productId\"]\n        )\n    elif version == -1:\n        new_version = ayon_api.get_last_version_by_product_id(\n            project_name, current_version[\"productId\"]\n        )\n\n    else:\n        new_version = ayon_api.get_version_by_name(\n            project_name, version, current_version[\"productId\"]\n        )\n\n    if new_version is None:\n        raise ValueError(\"Failed to find matching version\")\n\n    product_entity = ayon_api.get_product_by_id(\n        project_name, current_version[\"productId\"]\n    )\n    folder_entity = ayon_api.get_folder_by_id(\n        project_name, product_entity[\"folderId\"]\n    )\n\n    # Run update on the Loader for this container\n    Loader = _get_container_loader(container)\n    if not Loader:\n        raise LoaderNotFoundError(\n            \"Can't update container because loader '{}' was not found.\"\n            .format(container.get(\"loader\"))\n        )\n\n    repre_name = current_representation[\"name\"]\n    new_representation = ayon_api.get_representation_by_name(\n        project_name, repre_name, new_version[\"id\"]\n    )\n    if new_representation is None:\n        # The representation name is not found in the new version.\n        # Allow updating to a 'matching' representation if the loader\n        # has defined compatible update conversions\n        repre_name_aliases = Loader.get_representation_name_aliases(repre_name)\n        if repre_name_aliases:\n            representations = ayon_api.get_representations(\n                project_name,\n                representation_names=repre_name_aliases,\n                version_ids=[new_version[\"id\"]])\n            representations_by_name = {\n                repre[\"name\"]: repre for repre in representations\n            }\n            for name in repre_name_aliases:\n                if name in representations_by_name:\n                    new_representation = representations_by_name[name]\n                    break\n\n        if new_representation is None:\n            raise ValueError(\n                \"Representation '{}' wasn't found on requested version\".format(\n                    repre_name\n                )\n            )\n\n    project_entity = ayon_api.get_project(project_name)\n    context = {\n        \"project\": project_entity,\n        \"folder\": folder_entity,\n        \"product\": product_entity,\n        \"version\": new_version,\n        \"representation\": new_representation,\n    }\n    path = get_representation_path_from_context(context)\n    if not path or not os.path.exists(path):\n        raise ValueError(\"Path {} doesn't exist\".format(path))\n\n    return Loader().update(container, context)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/plugins.html","title":"plugins","text":"<p>Plugins for loading representations and products into host applications.</p>"},{"location":"autoapi/client/ayon_core/pipeline/load/plugins.html#client.ayon_core.pipeline.load.plugins.LoaderHookPlugin","title":"<code>LoaderHookPlugin</code>","text":"<p>Plugin that runs before and post specific Loader in 'loaders'</p> <p>Should be used as non-invasive method to enrich core loading process. Any studio might want to modify loaded data before or after they are loaded without need to override existing core plugins.</p> <p>The post methods are called after the loader's methods and receive the return value of the loader's method as <code>result</code> argument.</p> Source code in <code>client/ayon_core/pipeline/load/plugins.py</code> <pre><code>class LoaderHookPlugin:\n    \"\"\"Plugin that runs before and post specific Loader in 'loaders'\n\n    Should be used as non-invasive method to enrich core loading process.\n    Any studio might want to modify loaded data before or after\n    they are loaded without need to override existing core plugins.\n\n    The post methods are called after the loader's methods and receive the\n    return value of the loader's method as `result` argument.\n    \"\"\"\n    order = 0\n\n    @classmethod\n    @abstractmethod\n    def is_compatible(cls, Loader: Type[LoaderPlugin]) -&gt; bool:\n        pass\n\n    @abstractmethod\n    def pre_load(\n        self,\n        plugin: LoaderPlugin,\n        context: dict,\n        name: Optional[str],\n        namespace: Optional[str],\n        options: Optional[dict],\n    ):\n        pass\n\n    @abstractmethod\n    def post_load(\n        self,\n        plugin: LoaderPlugin,\n        result: Any,\n        context: dict,\n        name: Optional[str],\n        namespace: Optional[str],\n        options: Optional[dict],\n    ):\n        pass\n\n    @abstractmethod\n    def pre_update(\n        self,\n        plugin: LoaderPlugin,\n        container: dict,  # (ayon:container-3.0)\n        context: dict,\n    ):\n        pass\n\n    @abstractmethod\n    def post_update(\n        self,\n        plugin: LoaderPlugin,\n        result: Any,\n        container: dict,  # (ayon:container-3.0)\n        context: dict,\n    ):\n        pass\n\n    @abstractmethod\n    def pre_remove(\n        self,\n        plugin: LoaderPlugin,\n        container: dict,  # (ayon:container-3.0)\n    ):\n        pass\n\n    @abstractmethod\n    def post_remove(\n        self,\n        plugin: LoaderPlugin,\n        result: Any,\n        container: dict,  # (ayon:container-3.0)\n    ):\n        pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/plugins.html#client.ayon_core.pipeline.load.plugins.LoaderPlugin","title":"<code>LoaderPlugin</code>","text":"<p>               Bases: <code>list</code></p> <p>Load representation into host application</p> Source code in <code>client/ayon_core/pipeline/load/plugins.py</code> <pre><code>class LoaderPlugin(list):\n    \"\"\"Load representation into host application\"\"\"\n\n    # Attribute 'skip_discovery' is used during discovery phase to skip\n    #   plugins, which can be used to mark base plugins that should not be\n    #   considered as plugins \"to use\". The discovery logic does NOT use\n    #   the attribute value from parent classes. Each base class has to define\n    #   the attribute again.\n    skip_discovery = True\n\n    product_types: set[str] = set()\n    product_base_types: Optional[set[str]] = None\n    representations = set()\n    extensions = {\"*\"}\n    order = 0\n    is_multiple_contexts_compatible = False\n    enabled = True\n\n    options = []\n\n    log = Logger.get_logger(\"ProductLoader\")\n\n    @classmethod\n    def apply_settings(cls, project_settings):\n        host_name = os.environ.get(\"AYON_HOST_NAME\")\n        plugin_type = \"load\"\n        plugin_type_settings = (\n            project_settings\n            .get(host_name, {})\n            .get(plugin_type, {})\n        )\n        global_type_settings = (\n            project_settings\n            .get(\"core\", {})\n            .get(plugin_type, {})\n        )\n        if not global_type_settings and not plugin_type_settings:\n            return\n\n        plugin_name = cls.__name__\n\n        plugin_settings = None\n        # Look for plugin settings in host specific settings\n        if plugin_name in plugin_type_settings:\n            plugin_settings = plugin_type_settings[plugin_name]\n\n        # Look for plugin settings in global settings\n        elif plugin_name in global_type_settings:\n            plugin_settings = global_type_settings[plugin_name]\n\n        if not plugin_settings:\n            return\n\n        print(f\"&gt;&gt;&gt; We have preset for {plugin_name}\")\n        for option, value in plugin_settings.items():\n            if option == \"enabled\" and value is False:\n                print(\"  - is disabled by preset\")\n            else:\n                print(f\"  - setting `{option}`: `{value}`\")\n            setattr(cls, option, value)\n\n    @classmethod\n    def has_valid_extension(cls, repre_entity):\n        \"\"\"Has representation document valid extension for loader.\n\n        Args:\n            repre_entity (dict[str, Any]): Representation entity.\n\n        Returns:\n             bool: Representation has valid extension\n        \"\"\"\n        if \"*\" in cls.extensions:\n            return True\n\n        # Get representation main file extension from 'context'\n        repre_context = repre_entity.get(\"context\") or {}\n        ext = repre_context.get(\"ext\")\n        if not ext:\n            # Legacy way how to get extensions\n            path = repre_entity.get(\"attrib\", {}).get(\"path\")\n            if not path:\n                cls.log.info(\n                    \"Representation doesn't have known source of extension\"\n                    \" information.\"\n                )\n                return False\n\n            cls.log.debug(\"Using legacy source of extension from path.\")\n            ext = os.path.splitext(path)[-1].lstrip(\".\")\n\n        # If representation does not have extension then can't be valid\n        if not ext:\n            return False\n\n        valid_extensions_low = {ext.lower() for ext in cls.extensions}\n        return ext.lower() in valid_extensions_low\n\n    @classmethod\n    def is_compatible_loader(cls, context):\n        \"\"\"Return whether a loader is compatible with a context.\n\n        On override make sure it is overridden as class or static method.\n\n        This checks the product type and the representation for the given\n        loader plugin.\n\n        Args:\n            context (dict[str, Any]): Documents of context for which should\n                be loader used.\n\n        Returns:\n            bool: Is loader compatible for context.\n        \"\"\"\n\n        plugin_repre_names = cls.get_representations()\n\n        # If the product base type isn't defined on the loader plugin,\n        # then we will use the product types.\n        plugin_product_filter = cls.product_base_types\n        if plugin_product_filter is None:\n            plugin_product_filter = cls.product_types\n\n        if plugin_product_filter:\n            plugin_product_filter = set(plugin_product_filter)\n\n        repre_entity = context.get(\"representation\")\n        product_entity = context[\"product\"]\n\n        # If no representation names, product types or extensions are defined\n        # then loader is not compatible with any context.\n        if (\n            not plugin_repre_names\n            or not plugin_product_filter\n            or not cls.extensions\n        ):\n            return False\n\n        # If no representation entity is provided then loader is not\n        # compatible with context.\n        if not repre_entity:\n            return False\n\n        # Check the compatibility with the representation names.\n        plugin_repre_names = set(plugin_repre_names)\n        if (\n            \"*\" not in plugin_repre_names\n            and repre_entity[\"name\"] not in plugin_repre_names\n        ):\n            return False\n\n        # Check the compatibility with the extension of the representation.\n        if not cls.has_valid_extension(repre_entity):\n            return False\n\n        product_type = product_entity.get(\"productType\")\n        product_base_type = product_entity.get(\"productBaseType\")\n\n        # Use product base type if defined, otherwise use product type.\n        product_filter = product_base_type\n        # If there is no product base type defined in the product entity,\n        # then we will use the product type.\n        if product_filter is None:\n            product_filter = product_type\n\n        # If wildcard is used in product types or base types,\n        # then we will consider the loader compatible with any product type.\n        if \"*\" in plugin_product_filter:\n            return True\n\n        # compatibility with legacy loader\n        if cls.product_base_types is None and product_base_type:\n            cls.log.error(\n                f\"Loader {cls.__name__} is doesn't specify \"\n                \"`product_base_types` but product entity has \"\n                f\"`productBaseType` defined as `{product_base_type}`. \"\n            )\n\n        return product_filter in plugin_product_filter\n\n    @classmethod\n    def get_representations(cls):\n        \"\"\"Representation names with which is plugin compatible.\n\n        Empty set makes the plugin incompatible with any representation. To\n            allow compatibility with all representations use '{\"*\"}'.\n\n        Returns:\n            set[str]: Names with which is plugin compatible.\n\n        \"\"\"\n        return cls.representations\n\n    @classmethod\n    def filepath_from_context(cls, context):\n        return get_representation_path_from_context(context)\n\n    def load(self, context, name=None, namespace=None, options=None):\n        \"\"\"Load asset via database\n\n        Arguments:\n            context (dict): Full parenthood of representation to load\n            name (str, optional): Use pre-defined name\n            namespace (str, optional): Use pre-defined namespace\n            options (dict, optional): Additional settings dictionary\n\n        \"\"\"\n        raise NotImplementedError(\"Loader.load() must be \"\n                                  \"implemented by subclass\")\n\n    def update(self, container, context):\n        \"\"\"Update `container` to `representation`\n\n        Args:\n            container (avalon-core:container-1.0): Container to update,\n                from `host.ls()`.\n            context (dict): Update the container to this representation.\n\n        \"\"\"\n        raise NotImplementedError(\"Loader.update() must be \"\n                                  \"implemented by subclass\")\n\n    def remove(self, container):\n        \"\"\"Remove a container\n\n        Arguments:\n            container (avalon-core:container-1.0): Container to remove,\n                from `host.ls()`.\n\n        Returns:\n            bool: Whether the container was deleted\n\n        \"\"\"\n        raise NotImplementedError(\"Loader.remove() must be \"\n                                  \"implemented by subclass\")\n\n    @classmethod\n    def get_options(cls, contexts):\n        \"\"\"Returns static (cls) options or could collect from 'contexts'.\n\n        Args:\n            contexts (list): of repre or product contexts\n        Returns:\n            (list)\n        \"\"\"\n        return cls.options or []\n\n    @classmethod\n    def get_representation_name_aliases(cls, representation_name: str):\n        \"\"\"Return representation names to which switching is allowed from\n        the input representation name, like an alias replacement of the input\n        `representation_name`.\n\n        For example, to allow an automated switch on update from representation\n        `ma` to `mb` or `abc`, then when `representation_name` is `ma` return:\n            [\"mb\", \"abc\"]\n\n        The order of the names in the returned representation names is\n        important, because the first one existing under the new version will\n        be chosen.\n\n        Returns:\n            List[str]: Representation names switching to is allowed on update\n              if the input representation name is not found on the new version.\n        \"\"\"\n        return []\n\n    def __repr__(self):\n        # Avoid printing as a list (due to inheritance), but print like object.\n        return object.__repr__(self)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/plugins.html#client.ayon_core.pipeline.load.plugins.LoaderPlugin.get_options","title":"<code>get_options(contexts)</code>  <code>classmethod</code>","text":"<p>Returns static (cls) options or could collect from 'contexts'.</p> <p>Parameters:</p> Name Type Description Default <code>contexts</code> <code>list</code> <p>of repre or product contexts</p> required <p>Returns:     (list)</p> Source code in <code>client/ayon_core/pipeline/load/plugins.py</code> <pre><code>@classmethod\ndef get_options(cls, contexts):\n    \"\"\"Returns static (cls) options or could collect from 'contexts'.\n\n    Args:\n        contexts (list): of repre or product contexts\n    Returns:\n        (list)\n    \"\"\"\n    return cls.options or []\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/plugins.html#client.ayon_core.pipeline.load.plugins.LoaderPlugin.get_representation_name_aliases","title":"<code>get_representation_name_aliases(representation_name)</code>  <code>classmethod</code>","text":"<p>Return representation names to which switching is allowed from the input representation name, like an alias replacement of the input <code>representation_name</code>.</p> <p>For example, to allow an automated switch on update from representation <code>ma</code> to <code>mb</code> or <code>abc</code>, then when <code>representation_name</code> is <code>ma</code> return:     [\"mb\", \"abc\"]</p> <p>The order of the names in the returned representation names is important, because the first one existing under the new version will be chosen.</p> <p>Returns:</p> Type Description <p>List[str]: Representation names switching to is allowed on update if the input representation name is not found on the new version.</p> Source code in <code>client/ayon_core/pipeline/load/plugins.py</code> <pre><code>@classmethod\ndef get_representation_name_aliases(cls, representation_name: str):\n    \"\"\"Return representation names to which switching is allowed from\n    the input representation name, like an alias replacement of the input\n    `representation_name`.\n\n    For example, to allow an automated switch on update from representation\n    `ma` to `mb` or `abc`, then when `representation_name` is `ma` return:\n        [\"mb\", \"abc\"]\n\n    The order of the names in the returned representation names is\n    important, because the first one existing under the new version will\n    be chosen.\n\n    Returns:\n        List[str]: Representation names switching to is allowed on update\n          if the input representation name is not found on the new version.\n    \"\"\"\n    return []\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/plugins.html#client.ayon_core.pipeline.load.plugins.LoaderPlugin.get_representations","title":"<code>get_representations()</code>  <code>classmethod</code>","text":"<p>Representation names with which is plugin compatible.</p> <p>Empty set makes the plugin incompatible with any representation. To     allow compatibility with all representations use '{\"*\"}'.</p> <p>Returns:</p> Type Description <p>set[str]: Names with which is plugin compatible.</p> Source code in <code>client/ayon_core/pipeline/load/plugins.py</code> <pre><code>@classmethod\ndef get_representations(cls):\n    \"\"\"Representation names with which is plugin compatible.\n\n    Empty set makes the plugin incompatible with any representation. To\n        allow compatibility with all representations use '{\"*\"}'.\n\n    Returns:\n        set[str]: Names with which is plugin compatible.\n\n    \"\"\"\n    return cls.representations\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/plugins.html#client.ayon_core.pipeline.load.plugins.LoaderPlugin.has_valid_extension","title":"<code>has_valid_extension(repre_entity)</code>  <code>classmethod</code>","text":"<p>Has representation document valid extension for loader.</p> <p>Parameters:</p> Name Type Description Default <code>repre_entity</code> <code>dict[str, Any]</code> <p>Representation entity.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <p>Representation has valid extension</p> Source code in <code>client/ayon_core/pipeline/load/plugins.py</code> <pre><code>@classmethod\ndef has_valid_extension(cls, repre_entity):\n    \"\"\"Has representation document valid extension for loader.\n\n    Args:\n        repre_entity (dict[str, Any]): Representation entity.\n\n    Returns:\n         bool: Representation has valid extension\n    \"\"\"\n    if \"*\" in cls.extensions:\n        return True\n\n    # Get representation main file extension from 'context'\n    repre_context = repre_entity.get(\"context\") or {}\n    ext = repre_context.get(\"ext\")\n    if not ext:\n        # Legacy way how to get extensions\n        path = repre_entity.get(\"attrib\", {}).get(\"path\")\n        if not path:\n            cls.log.info(\n                \"Representation doesn't have known source of extension\"\n                \" information.\"\n            )\n            return False\n\n        cls.log.debug(\"Using legacy source of extension from path.\")\n        ext = os.path.splitext(path)[-1].lstrip(\".\")\n\n    # If representation does not have extension then can't be valid\n    if not ext:\n        return False\n\n    valid_extensions_low = {ext.lower() for ext in cls.extensions}\n    return ext.lower() in valid_extensions_low\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/plugins.html#client.ayon_core.pipeline.load.plugins.LoaderPlugin.is_compatible_loader","title":"<code>is_compatible_loader(context)</code>  <code>classmethod</code>","text":"<p>Return whether a loader is compatible with a context.</p> <p>On override make sure it is overridden as class or static method.</p> <p>This checks the product type and the representation for the given loader plugin.</p> <p>Parameters:</p> Name Type Description Default <code>context</code> <code>dict[str, Any]</code> <p>Documents of context for which should be loader used.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <p>Is loader compatible for context.</p> Source code in <code>client/ayon_core/pipeline/load/plugins.py</code> <pre><code>@classmethod\ndef is_compatible_loader(cls, context):\n    \"\"\"Return whether a loader is compatible with a context.\n\n    On override make sure it is overridden as class or static method.\n\n    This checks the product type and the representation for the given\n    loader plugin.\n\n    Args:\n        context (dict[str, Any]): Documents of context for which should\n            be loader used.\n\n    Returns:\n        bool: Is loader compatible for context.\n    \"\"\"\n\n    plugin_repre_names = cls.get_representations()\n\n    # If the product base type isn't defined on the loader plugin,\n    # then we will use the product types.\n    plugin_product_filter = cls.product_base_types\n    if plugin_product_filter is None:\n        plugin_product_filter = cls.product_types\n\n    if plugin_product_filter:\n        plugin_product_filter = set(plugin_product_filter)\n\n    repre_entity = context.get(\"representation\")\n    product_entity = context[\"product\"]\n\n    # If no representation names, product types or extensions are defined\n    # then loader is not compatible with any context.\n    if (\n        not plugin_repre_names\n        or not plugin_product_filter\n        or not cls.extensions\n    ):\n        return False\n\n    # If no representation entity is provided then loader is not\n    # compatible with context.\n    if not repre_entity:\n        return False\n\n    # Check the compatibility with the representation names.\n    plugin_repre_names = set(plugin_repre_names)\n    if (\n        \"*\" not in plugin_repre_names\n        and repre_entity[\"name\"] not in plugin_repre_names\n    ):\n        return False\n\n    # Check the compatibility with the extension of the representation.\n    if not cls.has_valid_extension(repre_entity):\n        return False\n\n    product_type = product_entity.get(\"productType\")\n    product_base_type = product_entity.get(\"productBaseType\")\n\n    # Use product base type if defined, otherwise use product type.\n    product_filter = product_base_type\n    # If there is no product base type defined in the product entity,\n    # then we will use the product type.\n    if product_filter is None:\n        product_filter = product_type\n\n    # If wildcard is used in product types or base types,\n    # then we will consider the loader compatible with any product type.\n    if \"*\" in plugin_product_filter:\n        return True\n\n    # compatibility with legacy loader\n    if cls.product_base_types is None and product_base_type:\n        cls.log.error(\n            f\"Loader {cls.__name__} is doesn't specify \"\n            \"`product_base_types` but product entity has \"\n            f\"`productBaseType` defined as `{product_base_type}`. \"\n        )\n\n    return product_filter in plugin_product_filter\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/plugins.html#client.ayon_core.pipeline.load.plugins.LoaderPlugin.load","title":"<code>load(context, name=None, namespace=None, options=None)</code>","text":"<p>Load asset via database</p> <p>Parameters:</p> Name Type Description Default <code>context</code> <code>dict</code> <p>Full parenthood of representation to load</p> required <code>name</code> <code>str</code> <p>Use pre-defined name</p> <code>None</code> <code>namespace</code> <code>str</code> <p>Use pre-defined namespace</p> <code>None</code> <code>options</code> <code>dict</code> <p>Additional settings dictionary</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/load/plugins.py</code> <pre><code>def load(self, context, name=None, namespace=None, options=None):\n    \"\"\"Load asset via database\n\n    Arguments:\n        context (dict): Full parenthood of representation to load\n        name (str, optional): Use pre-defined name\n        namespace (str, optional): Use pre-defined namespace\n        options (dict, optional): Additional settings dictionary\n\n    \"\"\"\n    raise NotImplementedError(\"Loader.load() must be \"\n                              \"implemented by subclass\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/plugins.html#client.ayon_core.pipeline.load.plugins.LoaderPlugin.remove","title":"<code>remove(container)</code>","text":"<p>Remove a container</p> <p>Parameters:</p> Name Type Description Default <code>container (avalon-core</code> <p>container-1.0): Container to remove, from <code>host.ls()</code>.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <p>Whether the container was deleted</p> Source code in <code>client/ayon_core/pipeline/load/plugins.py</code> <pre><code>def remove(self, container):\n    \"\"\"Remove a container\n\n    Arguments:\n        container (avalon-core:container-1.0): Container to remove,\n            from `host.ls()`.\n\n    Returns:\n        bool: Whether the container was deleted\n\n    \"\"\"\n    raise NotImplementedError(\"Loader.remove() must be \"\n                              \"implemented by subclass\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/plugins.html#client.ayon_core.pipeline.load.plugins.LoaderPlugin.update","title":"<code>update(container, context)</code>","text":"<p>Update <code>container</code> to <code>representation</code></p> <p>Parameters:</p> Name Type Description Default <code>container (avalon-core</code> <p>container-1.0): Container to update, from <code>host.ls()</code>.</p> required <code>context</code> <code>dict</code> <p>Update the container to this representation.</p> required Source code in <code>client/ayon_core/pipeline/load/plugins.py</code> <pre><code>def update(self, container, context):\n    \"\"\"Update `container` to `representation`\n\n    Args:\n        container (avalon-core:container-1.0): Container to update,\n            from `host.ls()`.\n        context (dict): Update the container to this representation.\n\n    \"\"\"\n    raise NotImplementedError(\"Loader.update() must be \"\n                              \"implemented by subclass\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/plugins.html#client.ayon_core.pipeline.load.plugins.ProductLoaderPlugin","title":"<code>ProductLoaderPlugin</code>","text":"<p>               Bases: <code>LoaderPlugin</code></p> <p>Load product into host application Arguments:     context (dict): avalon-core:context-1.0     name (str, optional): Use pre-defined name     namespace (str, optional): Use pre-defined namespace</p> Source code in <code>client/ayon_core/pipeline/load/plugins.py</code> <pre><code>class ProductLoaderPlugin(LoaderPlugin):\n    \"\"\"Load product into host application\n    Arguments:\n        context (dict): avalon-core:context-1.0\n        name (str, optional): Use pre-defined name\n        namespace (str, optional): Use pre-defined namespace\n    \"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/plugins.html#client.ayon_core.pipeline.load.plugins.add_hooks_to_loader","title":"<code>add_hooks_to_loader(loader_class, compatible_hooks)</code>","text":"<p>Monkey patch method replacing Loader.load|update|remove methods</p> <p>It wraps applicable loaders with pre/post hooks. Discovery is called only once per loaders discovery.</p> Source code in <code>client/ayon_core/pipeline/load/plugins.py</code> <pre><code>def add_hooks_to_loader(\n    loader_class: LoaderPlugin, compatible_hooks: list[Type[LoaderHookPlugin]]\n) -&gt; None:\n    \"\"\"Monkey patch method replacing Loader.load|update|remove methods\n\n    It wraps applicable loaders with pre/post hooks. Discovery is called only\n    once per loaders discovery.\n    \"\"\"\n    loader_class._load_hooks = compatible_hooks\n\n    def wrap_method(method_name: str):\n        original_method = getattr(loader_class, method_name)\n\n        def wrapped_method(self, *args, **kwargs):\n            # Call pre_&lt;method_name&gt; on all hooks\n            pre_hook_name = f\"pre_{method_name}\"\n\n            hooks: list[LoaderHookPlugin] = []\n            for cls in loader_class._load_hooks:\n                hook = cls()  # Instantiate the hook\n                hooks.append(hook)\n                pre_hook = getattr(hook, pre_hook_name, None)\n                if callable(pre_hook):\n                    pre_hook(self, *args, **kwargs)\n            # Call original method\n            result = original_method(self, *args, **kwargs)\n            # Call post_&lt;method_name&gt; on all hooks\n            post_hook_name = f\"post_{method_name}\"\n            for hook in hooks:\n                post_hook = getattr(hook, post_hook_name, None)\n                if callable(post_hook):\n                    post_hook(self, result, *args, **kwargs)\n\n            return result\n\n        setattr(loader_class, method_name, wrapped_method)\n\n    for method in (\"load\", \"update\", \"remove\"):\n        if hasattr(loader_class, method):\n            wrap_method(method)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/utils.html","title":"utils","text":""},{"location":"autoapi/client/ayon_core/pipeline/load/utils.html#client.ayon_core.pipeline.load.utils.IncompatibleLoaderError","title":"<code>IncompatibleLoaderError</code>","text":"<p>               Bases: <code>ValueError</code></p> <p>Error when Loader is incompatible with a representation.</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>class IncompatibleLoaderError(ValueError):\n    \"\"\"Error when Loader is incompatible with a representation.\"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/utils.html#client.ayon_core.pipeline.load.utils.InvalidRepresentationContext","title":"<code>InvalidRepresentationContext</code>","text":"<p>               Bases: <code>ValueError</code></p> <p>Representation path can't be received using representation document.</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>class InvalidRepresentationContext(ValueError):\n    \"\"\"Representation path can't be received using representation document.\"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/utils.html#client.ayon_core.pipeline.load.utils.LoadError","title":"<code>LoadError</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Known error that happened during loading.</p> <p>A message is shown to user (without traceback). Make sure an artist can understand the problem.</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>class LoadError(Exception):\n    \"\"\"Known error that happened during loading.\n\n    A message is shown to user (without traceback). Make sure an artist can\n    understand the problem.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/utils.html#client.ayon_core.pipeline.load.utils.LoaderNotFoundError","title":"<code>LoaderNotFoundError</code>","text":"<p>               Bases: <code>RuntimeError</code></p> <p>Error when Loader plugin for a loader name is not found.</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>class LoaderNotFoundError(RuntimeError):\n    \"\"\"Error when Loader plugin for a loader name is not found.\"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/utils.html#client.ayon_core.pipeline.load.utils.LoaderSwitchNotImplementedError","title":"<code>LoaderSwitchNotImplementedError</code>","text":"<p>               Bases: <code>NotImplementedError</code></p> <p>Error when <code>switch</code> is used with Loader that has no implementation.</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>class LoaderSwitchNotImplementedError(NotImplementedError):\n    \"\"\"Error when `switch` is used with Loader that has no implementation.\"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/utils.html#client.ayon_core.pipeline.load.utils.any_outdated_containers","title":"<code>any_outdated_containers(host=None, project_name=None)</code>","text":"<p>Check if there are any outdated containers in scene.</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>def any_outdated_containers(host=None, project_name=None):\n    \"\"\"Check if there are any outdated containers in scene.\"\"\"\n\n    if get_outdated_containers(host, project_name):\n        return True\n    return False\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/utils.html#client.ayon_core.pipeline.load.utils.filter_containers","title":"<code>filter_containers(containers, project_name)</code>","text":"<p>Filter containers and split them into 4 categories.</p> <p>Categories are 'latest', 'outdated', 'invalid' and 'not_found'. The 'lastest' containers are from last version, 'outdated' are not, 'invalid' are invalid containers (invalid content) and 'not_found' has some missing entity in database.</p> <p>Parameters:</p> Name Type Description Default <code>containers</code> <code>Iterable[dict]</code> <p>List of containers referenced into scene.</p> required <code>project_name</code> <code>str</code> <p>Current project name.</p> required <p>Returns:</p> Name Type Description <code>ContainersFilterResult</code> <p>Named tuple with 'latest', 'outdated', 'invalid' and 'not_found' containers.</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>def filter_containers(containers, project_name):\n    \"\"\"Filter containers and split them into 4 categories.\n\n    Categories are 'latest', 'outdated', 'invalid' and 'not_found'.\n    The 'lastest' containers are from last version, 'outdated' are not,\n    'invalid' are invalid containers (invalid content) and 'not_found' has\n    some missing entity in database.\n\n    Args:\n        containers (Iterable[dict]): List of containers referenced into scene.\n        project_name (str): Current project name.\n\n    Returns:\n        ContainersFilterResult: Named tuple with 'latest', 'outdated',\n            'invalid' and 'not_found' containers.\n\n    \"\"\"\n    outdated_containers = []\n    uptodate_containers = []\n    not_found_containers = []\n    invalid_containers = []\n    output = ContainersFilterResult(\n        uptodate_containers,\n        outdated_containers,\n        not_found_containers,\n        invalid_containers\n    )\n\n    # Iterate over containers and group them by project name\n    # - filter out containers with invalid representation id\n    # - because 'containers' variable is iterable, this is the only place\n    #   where it should be used.\n    containers_by_project_name = collections.defaultdict(list)\n    for container in containers:\n        if not _is_valid_representation_id(container[\"representation\"]):\n            invalid_containers.append(container)\n            continue\n        container_project = container.get(\"project_name\")\n        if not container_project:\n            container_project = project_name\n        containers_by_project_name[container_project].append(container)\n\n    # Fetch entities to be able to filter the containers\n    # - entities can be fetched per project only\n    for l_project_name, l_containers in containers_by_project_name.items():\n        repre_ids = {\n            container[\"representation\"]\n            for container in l_containers\n        }\n        repre_entities = ayon_api.get_representations(\n            l_project_name,\n            representation_ids=repre_ids,\n            fields={\"id\", \"versionId\"}\n        )\n        version_ids = set()\n        repre_entities_by_id = {}\n        for repre_entity in repre_entities:\n            repre_id = repre_entity[\"id\"]\n            version_id = repre_entity[\"versionId\"]\n            version_ids.add(version_id)\n            repre_entities_by_id[repre_id] = repre_entity\n\n        # Query version docs to get it's product ids\n        # - also query hero version to be able identify if representation\n        #   belongs to existing version\n        version_entities = ayon_api.get_versions(\n            l_project_name,\n            version_ids=version_ids,\n            hero=True,\n            fields={\"id\", \"productId\", \"version\"}\n        )\n        versions_by_id = {}\n        versions_by_product_id = collections.defaultdict(list)\n        hero_version_ids = set()\n        for version_entity in version_entities:\n            version_id = version_entity[\"id\"]\n            # Store versions by their ids\n            versions_by_id[version_id] = version_entity\n            # There's no need to query products for hero versions\n            #   - they are considered as latest?\n            if version_entity[\"version\"] &lt; 0:\n                hero_version_ids.add(version_id)\n                continue\n            product_id = version_entity[\"productId\"]\n            versions_by_product_id[product_id].append(version_entity)\n\n        last_versions = ayon_api.get_last_versions(\n            l_project_name,\n            versions_by_product_id.keys(),\n            fields={\"id\"}\n        )\n\n        # Figure out which versions are outdated\n        outdated_version_ids = set()\n        for product_id, last_version_entity in last_versions.items():\n            for version_entity in versions_by_product_id[product_id]:\n                version_id = version_entity[\"id\"]\n                if version_id in hero_version_ids:\n                    continue\n                if version_id != last_version_entity[\"id\"]:\n                    outdated_version_ids.add(version_id)\n\n        # Based on all collected data figure out which containers are outdated\n        #   - log out if there are missing representation or version entities\n        for container in l_containers:\n            container_name = container[\"objectName\"]\n            repre_id = container[\"representation\"]\n            repre_entity = repre_entities_by_id.get(repre_id)\n            if not repre_entity:\n                log.debug(\n                    f\"Container '{container_name}' has an invalid\"\n                    \" representation. It is missing in the database.\"\n                )\n                not_found_containers.append(container)\n                continue\n\n            version_id = repre_entity[\"versionId\"]\n            if version_id not in versions_by_id:\n                log.debug(\n                    f\"Representation on container '{container_name}' has an\"\n                    \" invalid version. It is missing in the database.\"\n                )\n                not_found_containers.append(container)\n\n            elif version_id in outdated_version_ids:\n                outdated_containers.append(container)\n            else:\n                uptodate_containers.append(container)\n\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/utils.html#client.ayon_core.pipeline.load.utils.filter_repre_contexts_by_loader","title":"<code>filter_repre_contexts_by_loader(repre_contexts, loader)</code>","text":"<p>Filter representation contexts for loader.</p> <p>Parameters:</p> Name Type Description Default <code>repre_contexts</code> <code>list[dict[str, Ant]]</code> <p>Representation context.</p> required <code>loader</code> <code>LoaderPlugin</code> <p>Loader plugin to filter contexts for.</p> required <p>Returns:</p> Type Description <p>list[dict[str, Any]]: Filtered representation contexts.</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>def filter_repre_contexts_by_loader(repre_contexts, loader):\n    \"\"\"Filter representation contexts for loader.\n\n    Args:\n        repre_contexts (list[dict[str, Ant]]): Representation context.\n        loader (LoaderPlugin): Loader plugin to filter contexts for.\n\n    Returns:\n        list[dict[str, Any]]: Filtered representation contexts.\n    \"\"\"\n\n    return [\n        repre_context\n        for repre_context in repre_contexts\n        if is_compatible_loader(loader, repre_context)\n    ]\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/utils.html#client.ayon_core.pipeline.load.utils.get_loader_identifier","title":"<code>get_loader_identifier(loader)</code>","text":"<p>Loader identifier from loader plugin or object.</p> <p>Identifier should be stored to container for future management.</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>def get_loader_identifier(loader):\n    \"\"\"Loader identifier from loader plugin or object.\n\n    Identifier should be stored to container for future management.\n    \"\"\"\n    if not inspect.isclass(loader):\n        loader = loader.__class__\n    return loader.__name__\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/utils.html#client.ayon_core.pipeline.load.utils.get_outdated_containers","title":"<code>get_outdated_containers(host=None, project_name=None, ignore_locked_versions=False)</code>","text":"<p>Collect outdated containers from host scene.</p> <p>Currently registered host and project in global session are used if arguments are not passed.</p> <p>Parameters:</p> Name Type Description Default <code>host</code> <code>Optional[AbstractHost]</code> <p>Host implementation.</p> <code>None</code> <code>project_name</code> <code>Optional[str]</code> <p>Name of project in which context we are.</p> <code>None</code> <code>ignore_locked_versions</code> <code>bool</code> <p>Locked versions are ignored.</p> <code>False</code> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>def get_outdated_containers(\n    host: Optional[AbstractHost] = None,\n    project_name: Optional[str] = None,\n    ignore_locked_versions: bool = False,\n):\n    \"\"\"Collect outdated containers from host scene.\n\n    Currently registered host and project in global session are used if\n    arguments are not passed.\n\n    Args:\n        host (Optional[AbstractHost]): Host implementation.\n        project_name (Optional[str]): Name of project in which context we are.\n        ignore_locked_versions (bool): Locked versions are ignored.\n\n    \"\"\"\n    from ayon_core.pipeline import registered_host, get_current_project_name\n\n    if host is None:\n        host = registered_host()\n\n    if project_name is None:\n        project_name = get_current_project_name()\n\n    if isinstance(host, ILoadHost):\n        containers = host.get_containers()\n    else:\n        containers = host.ls()\n\n    outdated_containers = []\n    for container in filter_containers(containers, project_name).outdated:\n        if (\n            not ignore_locked_versions\n            and container.get(\"version_locked\") is True\n        ):\n            continue\n        outdated_containers.append(container)\n    return outdated_containers\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/utils.html#client.ayon_core.pipeline.load.utils.get_product_contexts","title":"<code>get_product_contexts(product_ids, project_name=None)</code>","text":"<p>Return parenthood context for product.</p> <pre><code>Provides context on product granularity - less detail than\n'get_repre_contexts'.\n</code></pre> <p>Args:     product_ids (list): The product ids.     project_name (Optional[str]): Project name. Returns:     dict: The full representation context by representation id.</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>def get_product_contexts(product_ids, project_name=None):\n    \"\"\"Return parenthood context for product.\n\n        Provides context on product granularity - less detail than\n        'get_repre_contexts'.\n    Args:\n        product_ids (list): The product ids.\n        project_name (Optional[str]): Project name.\n    Returns:\n        dict: The full representation context by representation id.\n    \"\"\"\n    from ayon_core.pipeline import get_current_project_name\n\n    contexts = {}\n    if not product_ids:\n        return contexts\n\n    if not project_name:\n        project_name = get_current_project_name()\n    product_entities = ayon_api.get_products(\n        project_name, product_ids=product_ids\n    )\n    product_entities_by_id = {}\n    folder_ids = set()\n    for product_entity in product_entities:\n        product_entities_by_id[product_entity[\"id\"]] = product_entity\n        folder_ids.add(product_entity[\"folderId\"])\n\n    folder_entities_by_id = {\n        folder_entity[\"id\"]: folder_entity\n        for folder_entity in ayon_api.get_folders(\n            project_name, folder_ids=folder_ids\n        )\n    }\n\n    project_entity = ayon_api.get_project(project_name)\n\n    for product_id, product_entity in product_entities_by_id.items():\n        folder_entity = folder_entities_by_id[product_entity[\"folderId\"]]\n        context = {\n            \"project\": project_entity,\n            \"folder\": folder_entity,\n            \"product\": product_entity\n        }\n        contexts[product_id] = context\n\n    return contexts\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/utils.html#client.ayon_core.pipeline.load.utils.get_repres_contexts","title":"<code>get_repres_contexts(representation_ids, project_name=None)</code>","text":"<p>Return parenthood context for representation.</p> <p>Parameters:</p> Name Type Description Default <code>representation_ids</code> <code>list</code> <p>The representation ids.</p> required <code>project_name</code> <code>Optional[str]</code> <p>Project name.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>dict</code> <p>The full representation context by representation id. keys are repre_id, value is dictionary with entities of folder, product, version and representation.</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>def get_repres_contexts(representation_ids, project_name=None):\n    \"\"\"Return parenthood context for representation.\n\n    Args:\n        representation_ids (list): The representation ids.\n        project_name (Optional[str]): Project name.\n\n    Returns:\n        dict: The full representation context by representation id.\n            keys are repre_id, value is dictionary with entities of\n            folder, product, version and representation.\n    \"\"\"\n    from ayon_core.pipeline import get_current_project_name\n\n    if not representation_ids:\n        return {}\n\n    if not project_name:\n        project_name = get_current_project_name()\n\n    repre_entities = ayon_api.get_representations(\n        project_name, representation_ids\n    )\n\n    return get_representation_contexts(project_name, repre_entities)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/utils.html#client.ayon_core.pipeline.load.utils.get_representation_by_names","title":"<code>get_representation_by_names(project_name, folder_path, product_name, version_name, representation_name)</code>","text":"<p>Get representation entity for folder and product.</p> <p>If version_name is \"hero\" then return the hero version If version_name is \"latest\" then return the latest version Otherwise use version_name as the exact integer version name.</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>def get_representation_by_names(\n    project_name: str,\n    folder_path: str,\n    product_name: str,\n    version_name: Union[int, str],\n    representation_name: str,\n) -&gt; Optional[dict]:\n    \"\"\"Get representation entity for folder and product.\n\n    If version_name is \"hero\" then return the hero version\n    If version_name is \"latest\" then return the latest version\n    Otherwise use version_name as the exact integer version name.\n\n    \"\"\"\n\n    if isinstance(folder_path, dict) and \"name\" in folder_path:\n        # Allow explicitly passing asset document\n        folder_entity = folder_path\n    else:\n        folder_entity = ayon_api.get_folder_by_path(\n            project_name, folder_path, fields=[\"id\"])\n    if not folder_entity:\n        return None\n\n    if isinstance(product_name, dict) and \"name\" in product_name:\n        # Allow explicitly passing product entity document\n        product_entity = product_name\n    else:\n        product_entity = ayon_api.get_product_by_name(\n            project_name,\n            product_name,\n            folder_id=folder_entity[\"id\"],\n            fields=[\"id\"])\n    if not product_entity:\n        return None\n\n    if version_name == \"hero\":\n        version_entity = ayon_api.get_hero_version_by_product_id(\n            project_name, product_id=product_entity[\"id\"])\n    elif version_name == \"latest\":\n        version_entity = ayon_api.get_last_version_by_product_id(\n            project_name, product_id=product_entity[\"id\"])\n    else:\n        version_entity = ayon_api.get_version_by_name(\n            project_name, version_name, product_id=product_entity[\"id\"])\n    if not version_entity:\n        return None\n\n    return ayon_api.get_representation_by_name(\n        project_name, representation_name, version_id=version_entity[\"id\"])\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/utils.html#client.ayon_core.pipeline.load.utils.get_representation_context","title":"<code>get_representation_context(project_name, representation)</code>","text":"<p>Return parenthood context for representation.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>representation</code> <code>Union[dict[str, Any], str]</code> <p>Representation entity or representation id.</p> required <p>Returns:</p> Type Description <p>dict[str, dict[str, Any]]: The full representation context.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>When representation is invalid or parents were not found.</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>def get_representation_context(project_name, representation):\n    \"\"\"Return parenthood context for representation.\n\n    Args:\n        project_name (str): Project name.\n        representation (Union[dict[str, Any], str]): Representation entity\n            or representation id.\n\n    Returns:\n        dict[str, dict[str, Any]]: The full representation context.\n\n    Raises:\n        ValueError: When representation is invalid or parents were not found.\n\n    \"\"\"\n    if not representation:\n        raise ValueError(\n            \"Invalid argument value {}\".format(str(representation))\n        )\n\n    if isinstance(representation, dict):\n        repre_entity = representation\n        repre_id = repre_entity[\"id\"]\n        context = get_representation_contexts(\n            project_name, [repre_entity]\n        )[repre_id]\n    else:\n        repre_id = representation\n        context = get_representation_contexts_by_ids(\n            project_name, {repre_id}\n        )[repre_id]\n\n    missing_entities = []\n    for key, value in context.items():\n        if value is None:\n            missing_entities.append(key)\n\n    if missing_entities:\n        raise ValueError(\n            \"Not able to receive representation parent types: {}\".format(\n                \", \".join(missing_entities)\n            )\n        )\n\n    return context\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/utils.html#client.ayon_core.pipeline.load.utils.get_representation_contexts","title":"<code>get_representation_contexts(project_name, representation_entities)</code>","text":"<p>Parenthood context for representations.</p> <p>Function fills <code>None</code> if any entity was not found or could     not be queried.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>representation_entities</code> <code>Iterable[dict[str, Any]]</code> <p>Representation entities.</p> required <p>Returns:</p> Type Description <p>dict[str, dict[str, Any]]: The full representation context by representation id.</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>def get_representation_contexts(project_name, representation_entities):\n    \"\"\"Parenthood context for representations.\n\n    Function fills ``None`` if any entity was not found or could\n        not be queried.\n\n    Args:\n        project_name (str): Project name.\n        representation_entities (Iterable[dict[str, Any]]): Representation\n            entities.\n\n    Returns:\n        dict[str, dict[str, Any]]: The full representation context by\n            representation id.\n\n    \"\"\"\n    repre_entities_by_id = {\n        repre_entity[\"id\"]: repre_entity\n        for repre_entity in representation_entities\n    }\n\n    if not repre_entities_by_id:\n        return {}\n\n    repre_ids = set(repre_entities_by_id)\n\n    parents_by_repre_id = ayon_api.get_representations_parents(\n        project_name, repre_ids\n    )\n    output = {}\n    for repre_id in repre_ids:\n        repre_entity = repre_entities_by_id[repre_id]\n        (\n            version_entity,\n            product_entity,\n            folder_entity,\n            project_entity\n        ) = parents_by_repre_id[repre_id]\n        output[repre_id] = {\n            \"project\": project_entity,\n            \"folder\": folder_entity,\n            \"product\": product_entity,\n            \"version\": version_entity,\n            \"representation\": repre_entity,\n        }\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/utils.html#client.ayon_core.pipeline.load.utils.get_representation_contexts_by_ids","title":"<code>get_representation_contexts_by_ids(project_name, representation_ids)</code>","text":"<p>Parenthood context for representations found by ids.</p> <p>Function fills <code>None</code> if any entity was not found or could     not be queried.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>representation_ids</code> <code>Iterable[str]</code> <p>Representation ids.</p> required <p>Returns:</p> Type Description <p>dict[str, dict[str, Any]]: The full representation context by representation id.</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>def get_representation_contexts_by_ids(project_name, representation_ids):\n    \"\"\"Parenthood context for representations found by ids.\n\n    Function fills ``None`` if any entity was not found or could\n        not be queried.\n\n    Args:\n        project_name (str): Project name.\n        representation_ids (Iterable[str]): Representation ids.\n\n    Returns:\n        dict[str, dict[str, Any]]: The full representation context by\n            representation id.\n\n    \"\"\"\n    repre_ids = set(representation_ids)\n    if not repre_ids:\n        return {}\n\n    # Query representation entities by id\n    repre_entities_by_id = {\n        repre_entity[\"id\"]: repre_entity\n        for repre_entity in ayon_api.get_representations(\n            project_name, repre_ids\n        )\n    }\n    output = get_representation_contexts(\n        project_name, repre_entities_by_id.values()\n    )\n    for repre_id in repre_ids:\n        if repre_id not in output:\n            output[repre_id] = {\n                \"project\": None,\n                \"folder\": None,\n                \"product\": None,\n                \"version\": None,\n                \"representation\": None,\n            }\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/utils.html#client.ayon_core.pipeline.load.utils.get_representation_path","title":"<code>get_representation_path(project_name, repre_entity, *, anatomy=None, project_entity=None)</code>","text":"<pre><code>get_representation_path(representation: dict[str, Any], root: Optional[dict[str, Any]] = None) -&gt; TemplateResult\n</code></pre><pre><code>get_representation_path(project_name: str, repre_entity: dict[str, Any], *, anatomy: Optional[Anatomy] = None, project_entity: Optional[dict[str, Any]] = None) -&gt; TemplateResult\n</code></pre> <p>Get filled representation path.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>repre_entity</code> <code>dict[str, Any]</code> <p>Representation entity.</p> required <code>anatomy</code> <code>Optional[Anatomy]</code> <p>Project anatomy.</p> <code>None</code> <code>project_entity</code> <code>Optional[dict[str, Any]</code> <p>Project entity. Is used to initialize Anatomy and is not needed if 'anatomy' is passed in.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>TemplateResult</code> <code>TemplateResult</code> <p>Resolved path to representation.</p> <p>Raises:</p> Type Description <code>InvalidRepresentationContext</code> <p>When representation data are probably invalid or not available.</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>@_backwards_compatibility_repre_path\ndef get_representation_path(\n    project_name: str,\n    repre_entity: dict[str, Any],\n    *,\n    anatomy: Optional[Anatomy] = None,\n    project_entity: Optional[dict[str, Any]] = None,\n) -&gt; TemplateResult:\n    \"\"\"Get filled representation path.\n\n    Args:\n        project_name (str): Project name.\n        repre_entity (dict[str, Any]): Representation entity.\n        anatomy (Optional[Anatomy]): Project anatomy.\n        project_entity (Optional[dict[str, Any]): Project entity. Is used to\n            initialize Anatomy and is not needed if 'anatomy' is passed in.\n\n    Returns:\n        TemplateResult: Resolved path to representation.\n\n    Raises:\n        InvalidRepresentationContext: When representation data are probably\n            invalid or not available.\n\n    \"\"\"\n    if anatomy is None:\n        anatomy = Anatomy(project_name, project_entity=project_entity)\n\n    try:\n        template = repre_entity[\"attrib\"][\"template\"]\n\n    except KeyError as exc:\n        raise InvalidRepresentationContext(\n            \"Failed to receive template from representation entity.\"\n        ) from exc\n\n    try:\n        context = copy.deepcopy(repre_entity[\"context\"])\n        _fix_representation_context_compatibility(context)\n        context[\"root\"] = anatomy.roots\n\n        path = StringTemplate.format_strict_template(template, context)\n\n    except TemplateUnsolved as exc:\n        raise InvalidRepresentationContext(\n            \"Failed to resolve representation template with available data.\"\n        ) from exc\n\n    return path.normalized()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/utils.html#client.ayon_core.pipeline.load.utils.get_representation_path_by_names","title":"<code>get_representation_path_by_names(project_name, folder_path, product_name, version_name, representation_name, anatomy=None)</code>","text":"<p>Get (latest) filepath for representation for folder and product.</p> <p>See <code>get_representation_by_names</code> for more details.</p> <p>Returns:</p> Name Type Description <code>str</code> <code>Optional[TemplateResult]</code> <p>The representation path if the representation exists.</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>def get_representation_path_by_names(\n    project_name: str,\n    folder_path: str,\n    product_name: str,\n    version_name: Union[int, str],\n    representation_name: str,\n    anatomy: Optional[Anatomy] = None\n) -&gt; Optional[TemplateResult]:\n    \"\"\"Get (latest) filepath for representation for folder and product.\n\n    See `get_representation_by_names` for more details.\n\n    Returns:\n        str: The representation path if the representation exists.\n\n    \"\"\"\n    representation = get_representation_by_names(\n        project_name,\n        folder_path,\n        product_name,\n        version_name,\n        representation_name\n    )\n    if not representation:\n        return None\n\n    return get_representation_path(\n        project_name,\n        representation,\n        anatomy=anatomy,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/utils.html#client.ayon_core.pipeline.load.utils.get_representation_path_from_context","title":"<code>get_representation_path_from_context(context)</code>","text":"<p>Preparation wrapper using only context as a argument</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>def get_representation_path_from_context(context):\n    \"\"\"Preparation wrapper using only context as a argument\"\"\"\n    from ayon_core.pipeline import get_current_project_name\n\n    representation = context[\"representation\"]\n    project_entity = context.get(\"project\")\n    if project_entity:\n        project_name = project_entity[\"name\"]\n    else:\n        project_name = get_current_project_name()\n    return get_representation_path(\n        project_name,\n        representation,\n        project_entity=project_entity,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/utils.html#client.ayon_core.pipeline.load.utils.get_representation_path_with_anatomy","title":"<code>get_representation_path_with_anatomy(repre_entity, anatomy)</code>","text":"<p>Receive representation path using representation document and anatomy.</p> <p>Anatomy is used to replace 'root' key in representation file. Ideally should be used instead of 'get_representation_path' which is based on \"current context\".</p> Future notes <p>We want also be able store resources into representation and I can imagine the result should also contain paths to possible resources.</p> <p>Parameters:</p> Name Type Description Default <code>repre_entity</code> <code>Dict[str, Any]</code> <p>Representation entity.</p> required <code>anatomy</code> <code>Anatomy</code> <p>Project anatomy object.</p> required <p>Returns:</p> Name Type Description <code>TemplateResult</code> <p>Resolved representation path.</p> <p>Raises:</p> Type Description <code>InvalidRepresentationContext</code> <p>When representation data are probably invalid or not available.</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>def get_representation_path_with_anatomy(repre_entity, anatomy):\n    \"\"\"Receive representation path using representation document and anatomy.\n\n    Anatomy is used to replace 'root' key in representation file. Ideally\n    should be used instead of 'get_representation_path' which is based on\n    \"current context\".\n\n    Future notes:\n        We want also be able store resources into representation and I can\n        imagine the result should also contain paths to possible resources.\n\n    Args:\n        repre_entity (Dict[str, Any]): Representation entity.\n        anatomy (Anatomy): Project anatomy object.\n\n    Returns:\n        TemplateResult: Resolved representation path.\n\n    Raises:\n        InvalidRepresentationContext: When representation data are probably\n            invalid or not available.\n\n    \"\"\"\n    return get_representation_path(\n        anatomy.project_name,\n        repre_entity,\n        anatomy=anatomy,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/utils.html#client.ayon_core.pipeline.load.utils.get_representation_path_with_roots","title":"<code>get_representation_path_with_roots(representation, roots)</code>","text":"<p>Get filename from representation with custom root.</p> <p>Parameters:</p> Name Type Description Default <code>representation</code> <code>dict</code> <p>Representation entity.</p> required <code>roots</code> <code>dict[str, str]</code> <p>Roots to use.</p> required <p>Returns:</p> Type Description <code>Optional[TemplateResult]</code> <p>Optional[TemplateResult]: Resolved representation path.</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>def get_representation_path_with_roots(\n    representation: dict[str, Any],\n    roots: dict[str, str],\n) -&gt; Optional[TemplateResult]:\n    \"\"\"Get filename from representation with custom root.\n\n    Args:\n        representation(dict): Representation entity.\n        roots (dict[str, str]): Roots to use.\n\n\n    Returns:\n        Optional[TemplateResult]: Resolved representation path.\n\n    \"\"\"\n    try:\n        template = representation[\"attrib\"][\"template\"]\n    except KeyError:\n        return None\n\n    try:\n        context = representation[\"context\"]\n\n        _fix_representation_context_compatibility(context)\n\n        context[\"root\"] = roots\n        path = StringTemplate.format_strict_template(\n            template, context\n        )\n    except (TemplateUnsolved, KeyError):\n        # Template references unavailable data\n        return None\n\n    return path.normalized()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/utils.html#client.ayon_core.pipeline.load.utils.is_compatible_loader","title":"<code>is_compatible_loader(Loader, context)</code>","text":"<p>Return whether a loader is compatible with a context.</p> <p>This checks the product type and the representation for the given Loader.</p> <p>Returns:</p> Type Description <p>bool</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>def is_compatible_loader(Loader, context):\n    \"\"\"Return whether a loader is compatible with a context.\n\n    This checks the product type and the representation for the given\n    Loader.\n\n    Returns:\n        bool\n    \"\"\"\n\n    return Loader.is_compatible_loader(context)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/utils.html#client.ayon_core.pipeline.load.utils.load_container","title":"<code>load_container(Loader, representation, namespace=None, name=None, options=None, **kwargs)</code>","text":"<p>Use Loader to load a representation.</p> <p>Parameters:</p> Name Type Description Default <code>Loader</code> <code>Loader</code> <p>The loader class to trigger.</p> required <code>representation</code> <code>str or dict</code> <p>The representation id or full representation as returned by the database.</p> required <code>namespace</code> <code>(str, Optional)</code> <p>The namespace to assign. Defaults to None.</p> <code>None</code> <code>name</code> <code>(str, Optional)</code> <p>The name to assign. Defaults to product name.</p> <code>None</code> <code>options</code> <code>(dict, Optional)</code> <p>Additional options to pass on to the loader.</p> <code>None</code> <p>Returns:</p> Type Description <p>The return of the <code>loader.load()</code> method.</p> <p>Raises:</p> Type Description <code>IncompatibleLoaderError</code> <p>When the loader is not compatible with the representation.</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>def load_container(\n    Loader, representation, namespace=None, name=None, options=None, **kwargs\n):\n    \"\"\"Use Loader to load a representation.\n\n    Args:\n        Loader (Loader): The loader class to trigger.\n        representation (str or dict): The representation id\n            or full representation as returned by the database.\n        namespace (str, Optional): The namespace to assign. Defaults to None.\n        name (str, Optional): The name to assign. Defaults to product name.\n        options (dict, Optional): Additional options to pass on to the loader.\n\n    Returns:\n        The return of the `loader.load()` method.\n\n    Raises:\n        IncompatibleLoaderError: When the loader is not compatible with\n            the representation.\n\n    \"\"\"\n    from ayon_core.pipeline import get_current_project_name\n\n    context = get_representation_context(\n        get_current_project_name(), representation\n    )\n    return load_with_repre_context(\n        Loader,\n        context,\n        namespace=namespace,\n        name=name,\n        options=options,\n        **kwargs\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/utils.html#client.ayon_core.pipeline.load.utils.loaders_from_repre_context","title":"<code>loaders_from_repre_context(loaders, repre_context)</code>","text":"<p>Return compatible loaders for by representation's context.</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>def loaders_from_repre_context(loaders, repre_context):\n    \"\"\"Return compatible loaders for by representation's context.\"\"\"\n\n    return [\n        loader\n        for loader in loaders\n        if is_compatible_loader(loader, repre_context)\n    ]\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/utils.html#client.ayon_core.pipeline.load.utils.loaders_from_representation","title":"<code>loaders_from_representation(loaders, representation)</code>","text":"<p>Return all compatible loaders for a representation.</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>def loaders_from_representation(loaders, representation):\n    \"\"\"Return all compatible loaders for a representation.\"\"\"\n    from ayon_core.pipeline import get_current_project_name\n\n    project_name = get_current_project_name()\n    context = get_representation_context(\n        project_name, representation\n    )\n    return loaders_from_repre_context(loaders, context)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/utils.html#client.ayon_core.pipeline.load.utils.remove_container","title":"<code>remove_container(container)</code>","text":"<p>Remove a container</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>def remove_container(container):\n    \"\"\"Remove a container\"\"\"\n\n    Loader = _get_container_loader(container)\n    if not Loader:\n        raise LoaderNotFoundError(\n            \"Can't remove container because loader '{}' was not found.\"\n            .format(container.get(\"loader\"))\n        )\n\n    return Loader().remove(container)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/utils.html#client.ayon_core.pipeline.load.utils.switch_container","title":"<code>switch_container(container, representation, loader_plugin=None)</code>","text":"<p>Switch a container to representation</p> <p>Parameters:</p> Name Type Description Default <code>container</code> <code>dict</code> <p>container information</p> required <code>representation</code> <code>dict</code> <p>representation entity</p> required <p>Returns:</p> Type Description <p>return from function call</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>def switch_container(\n    container,\n    representation,\n    loader_plugin=None,\n):\n    \"\"\"Switch a container to representation\n\n    Args:\n        container (dict): container information\n        representation (dict): representation entity\n        loader_plugin (LoaderPlugin)\n\n    Returns:\n        return from function call\n    \"\"\"\n    from ayon_core.pipeline import get_current_project_name\n\n    # Get the Loader for this container\n    if loader_plugin is None:\n        loader_plugin = _get_container_loader(container)\n\n    if not loader_plugin:\n        raise LoaderNotFoundError(\n            \"Can't switch container because loader '{}' was not found.\"\n            .format(container.get(\"loader\"))\n        )\n\n    if not hasattr(loader_plugin, \"switch\"):\n        # Backwards compatibility (classes without switch support\n        # might be better to just have \"switch\" raise NotImplementedError\n        # on the base class of Loader\\\n        raise LoaderSwitchNotImplementedError(\n            \"Loader {} does not support 'switch'\".format(loader_plugin.label)\n        )\n\n    # Get the new representation to switch to\n    project_name = container.get(\"project_name\")\n    if project_name is None:\n        project_name = get_current_project_name()\n\n    context = get_representation_context(\n        project_name, representation[\"id\"]\n    )\n    if not is_compatible_loader(loader_plugin, context):\n        raise IncompatibleLoaderError(\n            \"Loader {} is incompatible with {}\".format(\n                loader_plugin.__name__, context[\"product\"][\"name\"]\n            )\n        )\n\n    loader = loader_plugin(context)\n\n    return loader.switch(container, context)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/load/utils.html#client.ayon_core.pipeline.load.utils.update_container","title":"<code>update_container(container, version=-1)</code>","text":"<p>Update a container</p> Source code in <code>client/ayon_core/pipeline/load/utils.py</code> <pre><code>def update_container(container, version=-1):\n    \"\"\"Update a container\"\"\"\n    from ayon_core.pipeline import get_current_project_name\n\n    # Compute the different version from 'representation'\n    project_name = container.get(\"project_name\")\n    if project_name is None:\n        project_name = get_current_project_name()\n    repre_id = container[\"representation\"]\n    if not _is_valid_representation_id(repre_id):\n        raise ValueError(\n            f\"Got container with invalid representation id '{repre_id}'\"\n        )\n    current_representation = ayon_api.get_representation_by_id(\n        project_name, repre_id\n    )\n\n    assert current_representation is not None, \"This is a bug\"\n\n    current_version_id = current_representation[\"versionId\"]\n    current_version = ayon_api.get_version_by_id(\n        project_name, current_version_id, fields={\"productId\"}\n    )\n    if isinstance(version, HeroVersionType):\n        new_version = ayon_api.get_hero_version_by_product_id(\n            project_name, current_version[\"productId\"]\n        )\n    elif version == -1:\n        new_version = ayon_api.get_last_version_by_product_id(\n            project_name, current_version[\"productId\"]\n        )\n\n    else:\n        new_version = ayon_api.get_version_by_name(\n            project_name, version, current_version[\"productId\"]\n        )\n\n    if new_version is None:\n        raise ValueError(\"Failed to find matching version\")\n\n    product_entity = ayon_api.get_product_by_id(\n        project_name, current_version[\"productId\"]\n    )\n    folder_entity = ayon_api.get_folder_by_id(\n        project_name, product_entity[\"folderId\"]\n    )\n\n    # Run update on the Loader for this container\n    Loader = _get_container_loader(container)\n    if not Loader:\n        raise LoaderNotFoundError(\n            \"Can't update container because loader '{}' was not found.\"\n            .format(container.get(\"loader\"))\n        )\n\n    repre_name = current_representation[\"name\"]\n    new_representation = ayon_api.get_representation_by_name(\n        project_name, repre_name, new_version[\"id\"]\n    )\n    if new_representation is None:\n        # The representation name is not found in the new version.\n        # Allow updating to a 'matching' representation if the loader\n        # has defined compatible update conversions\n        repre_name_aliases = Loader.get_representation_name_aliases(repre_name)\n        if repre_name_aliases:\n            representations = ayon_api.get_representations(\n                project_name,\n                representation_names=repre_name_aliases,\n                version_ids=[new_version[\"id\"]])\n            representations_by_name = {\n                repre[\"name\"]: repre for repre in representations\n            }\n            for name in repre_name_aliases:\n                if name in representations_by_name:\n                    new_representation = representations_by_name[name]\n                    break\n\n        if new_representation is None:\n            raise ValueError(\n                \"Representation '{}' wasn't found on requested version\".format(\n                    repre_name\n                )\n            )\n\n    project_entity = ayon_api.get_project(project_name)\n    context = {\n        \"project\": project_entity,\n        \"folder\": folder_entity,\n        \"product\": product_entity,\n        \"version\": new_version,\n        \"representation\": new_representation,\n    }\n    path = get_representation_path_from_context(context)\n    if not path or not os.path.exists(path):\n        raise ValueError(\"Path {} doesn't exist\".format(path))\n\n    return Loader().update(container, context)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html","title":"publish","text":""},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.AYONPyblishPluginMixin","title":"<code>AYONPyblishPluginMixin</code>","text":"Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>class AYONPyblishPluginMixin:\n    # TODO\n    # executable_in_thread = False\n    #\n    # state_message = None\n    # state_percent = None\n    # _state_change_callbacks = []\n    #\n    # def set_state(self, percent=None, message=None):\n    #     \"\"\"Inner callback of plugin that would help to show in UI state.\n    #\n    #     Plugin have registered callbacks on state change which could trigger\n    #     update message and percent in UI and repaint the change.\n    #\n    #     This part must be optional and should not be used to display errors\n    #     or for logging.\n    #\n    #     Message should be short without details.\n    #\n    #     Args:\n    #         percent(int): Percent of processing in range &lt;1-100&gt;.\n    #         message(str): Message which will be shown to user (if in UI).\n    #     \"\"\"\n    #     if percent is not None:\n    #         self.state_percent = percent\n    #\n    #     if message:\n    #         self.state_message = message\n    #\n    #     for callback in self._state_change_callbacks:\n    #         callback(self)\n\n    @classmethod\n    def register_create_context_callbacks(\n        cls, create_context: \"CreateContext\"\n    ):\n        \"\"\"Register callbacks for create context.\n\n        It is possible to register callbacks listening to changes happened\n        in create context.\n\n        Methods available on create context:\n        - add_instances_added_callback\n        - add_instances_removed_callback\n        - add_value_changed_callback\n        - add_pre_create_attr_defs_change_callback\n        - add_create_attr_defs_change_callback\n        - add_publish_attr_defs_change_callback\n\n        Args:\n            create_context (CreateContext): Create context.\n\n        \"\"\"\n        pass\n\n    @classmethod\n    def get_attribute_defs(cls):\n        \"\"\"Publish attribute definitions.\n\n        Attributes available for all families in plugin's `families` attribute.\n\n        Returns:\n            list[AbstractAttrDef]: Attribute definitions for plugin.\n\n        \"\"\"\n        return []\n\n    @classmethod\n    def get_attr_defs_for_context(cls, create_context: \"CreateContext\"):\n        \"\"\"Publish attribute definitions for context.\n\n        Attributes available for all families in plugin's `families` attribute.\n\n        Args:\n            create_context (CreateContext): Create context.\n\n        Returns:\n            list[AbstractAttrDef]: Attribute definitions for plugin.\n\n        \"\"\"\n        if cls.__instanceEnabled__:\n            return []\n        return cls.get_attribute_defs()\n\n    @classmethod\n    def instance_matches_plugin_families(\n        cls, instance: Optional[\"CreatedInstance\"]\n    ):\n        \"\"\"Check if instance matches families.\n\n        Args:\n            instance (Optional[CreatedInstance]): Instance to check. Or None\n                for context.\n\n        Returns:\n            bool: True if instance matches plugin families.\n\n        \"\"\"\n        if instance is None:\n            return not cls.__instanceEnabled__\n\n        if not cls.__instanceEnabled__:\n            return False\n\n        families = [instance.product_base_type]\n        families.extend(instance.get(\"families\", []))\n        for _ in pyblish.logic.plugins_by_families([cls], families):\n            return True\n        return False\n\n    @classmethod\n    def get_attr_defs_for_instance(\n        cls, create_context: \"CreateContext\", instance: \"CreatedInstance\"\n    ):\n        \"\"\"Publish attribute definitions for an instance.\n\n        Attributes available for all families in plugin's `families` attribute.\n\n        Args:\n            create_context (CreateContext): Create context.\n            instance (CreatedInstance): Instance for which attributes are\n                collected.\n\n        Returns:\n            list[AbstractAttrDef]: Attribute definitions for plugin.\n\n        \"\"\"\n        if not cls.instance_matches_plugin_families(instance):\n            return []\n        return cls.get_attribute_defs()\n\n    @classmethod\n    def convert_attribute_values(\n        cls, create_context: \"CreateContext\", instance: \"CreatedInstance\"\n    ):\n        \"\"\"Convert attribute values for instance.\n\n        Args:\n            create_context (CreateContext): Create context.\n            instance (CreatedInstance): Instance for which attributes are\n                converted.\n\n        \"\"\"\n        return\n\n    @staticmethod\n    def get_attr_values_from_data_for_plugin(plugin, data):\n        \"\"\"Get attribute values for attribute definitions from data.\n\n        Args:\n            plugin (Union[publish.api.Plugin, Type[publish.api.Plugin]]): The\n                plugin for which attributes are extracted.\n            data(dict): Data from instance or context.\n        \"\"\"\n\n        if not inspect.isclass(plugin):\n            plugin = plugin.__class__\n\n        return (\n            data\n            .get(\"publish_attributes\", {})\n            .get(plugin.__name__, {})\n        )\n\n    def get_attr_values_from_data(self, data):\n        \"\"\"Get attribute values for attribute definitions from data.\n\n        Args:\n            data(dict): Data from instance or context.\n        \"\"\"\n\n        return self.get_attr_values_from_data_for_plugin(self.__class__, data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.AYONPyblishPluginMixin.convert_attribute_values","title":"<code>convert_attribute_values(create_context, instance)</code>  <code>classmethod</code>","text":"<p>Convert attribute values for instance.</p> <p>Parameters:</p> Name Type Description Default <code>create_context</code> <code>CreateContext</code> <p>Create context.</p> required <code>instance</code> <code>CreatedInstance</code> <p>Instance for which attributes are converted.</p> required Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>@classmethod\ndef convert_attribute_values(\n    cls, create_context: \"CreateContext\", instance: \"CreatedInstance\"\n):\n    \"\"\"Convert attribute values for instance.\n\n    Args:\n        create_context (CreateContext): Create context.\n        instance (CreatedInstance): Instance for which attributes are\n            converted.\n\n    \"\"\"\n    return\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.AYONPyblishPluginMixin.get_attr_defs_for_context","title":"<code>get_attr_defs_for_context(create_context)</code>  <code>classmethod</code>","text":"<p>Publish attribute definitions for context.</p> <p>Attributes available for all families in plugin's <code>families</code> attribute.</p> <p>Parameters:</p> Name Type Description Default <code>create_context</code> <code>CreateContext</code> <p>Create context.</p> required <p>Returns:</p> Type Description <p>list[AbstractAttrDef]: Attribute definitions for plugin.</p> Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>@classmethod\ndef get_attr_defs_for_context(cls, create_context: \"CreateContext\"):\n    \"\"\"Publish attribute definitions for context.\n\n    Attributes available for all families in plugin's `families` attribute.\n\n    Args:\n        create_context (CreateContext): Create context.\n\n    Returns:\n        list[AbstractAttrDef]: Attribute definitions for plugin.\n\n    \"\"\"\n    if cls.__instanceEnabled__:\n        return []\n    return cls.get_attribute_defs()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.AYONPyblishPluginMixin.get_attr_defs_for_instance","title":"<code>get_attr_defs_for_instance(create_context, instance)</code>  <code>classmethod</code>","text":"<p>Publish attribute definitions for an instance.</p> <p>Attributes available for all families in plugin's <code>families</code> attribute.</p> <p>Parameters:</p> Name Type Description Default <code>create_context</code> <code>CreateContext</code> <p>Create context.</p> required <code>instance</code> <code>CreatedInstance</code> <p>Instance for which attributes are collected.</p> required <p>Returns:</p> Type Description <p>list[AbstractAttrDef]: Attribute definitions for plugin.</p> Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>@classmethod\ndef get_attr_defs_for_instance(\n    cls, create_context: \"CreateContext\", instance: \"CreatedInstance\"\n):\n    \"\"\"Publish attribute definitions for an instance.\n\n    Attributes available for all families in plugin's `families` attribute.\n\n    Args:\n        create_context (CreateContext): Create context.\n        instance (CreatedInstance): Instance for which attributes are\n            collected.\n\n    Returns:\n        list[AbstractAttrDef]: Attribute definitions for plugin.\n\n    \"\"\"\n    if not cls.instance_matches_plugin_families(instance):\n        return []\n    return cls.get_attribute_defs()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.AYONPyblishPluginMixin.get_attr_values_from_data","title":"<code>get_attr_values_from_data(data)</code>","text":"<p>Get attribute values for attribute definitions from data.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict</code> <p>Data from instance or context.</p> required Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>def get_attr_values_from_data(self, data):\n    \"\"\"Get attribute values for attribute definitions from data.\n\n    Args:\n        data(dict): Data from instance or context.\n    \"\"\"\n\n    return self.get_attr_values_from_data_for_plugin(self.__class__, data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.AYONPyblishPluginMixin.get_attr_values_from_data_for_plugin","title":"<code>get_attr_values_from_data_for_plugin(plugin, data)</code>  <code>staticmethod</code>","text":"<p>Get attribute values for attribute definitions from data.</p> <p>Parameters:</p> Name Type Description Default <code>plugin</code> <code>Union[Plugin, Type[Plugin]]</code> <p>The plugin for which attributes are extracted.</p> required <code>data</code> <code>dict</code> <p>Data from instance or context.</p> required Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>@staticmethod\ndef get_attr_values_from_data_for_plugin(plugin, data):\n    \"\"\"Get attribute values for attribute definitions from data.\n\n    Args:\n        plugin (Union[publish.api.Plugin, Type[publish.api.Plugin]]): The\n            plugin for which attributes are extracted.\n        data(dict): Data from instance or context.\n    \"\"\"\n\n    if not inspect.isclass(plugin):\n        plugin = plugin.__class__\n\n    return (\n        data\n        .get(\"publish_attributes\", {})\n        .get(plugin.__name__, {})\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.AYONPyblishPluginMixin.get_attribute_defs","title":"<code>get_attribute_defs()</code>  <code>classmethod</code>","text":"<p>Publish attribute definitions.</p> <p>Attributes available for all families in plugin's <code>families</code> attribute.</p> <p>Returns:</p> Type Description <p>list[AbstractAttrDef]: Attribute definitions for plugin.</p> Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>@classmethod\ndef get_attribute_defs(cls):\n    \"\"\"Publish attribute definitions.\n\n    Attributes available for all families in plugin's `families` attribute.\n\n    Returns:\n        list[AbstractAttrDef]: Attribute definitions for plugin.\n\n    \"\"\"\n    return []\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.AYONPyblishPluginMixin.instance_matches_plugin_families","title":"<code>instance_matches_plugin_families(instance)</code>  <code>classmethod</code>","text":"<p>Check if instance matches families.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>Optional[CreatedInstance]</code> <p>Instance to check. Or None for context.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <p>True if instance matches plugin families.</p> Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>@classmethod\ndef instance_matches_plugin_families(\n    cls, instance: Optional[\"CreatedInstance\"]\n):\n    \"\"\"Check if instance matches families.\n\n    Args:\n        instance (Optional[CreatedInstance]): Instance to check. Or None\n            for context.\n\n    Returns:\n        bool: True if instance matches plugin families.\n\n    \"\"\"\n    if instance is None:\n        return not cls.__instanceEnabled__\n\n    if not cls.__instanceEnabled__:\n        return False\n\n    families = [instance.product_base_type]\n    families.extend(instance.get(\"families\", []))\n    for _ in pyblish.logic.plugins_by_families([cls], families):\n        return True\n    return False\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.AYONPyblishPluginMixin.register_create_context_callbacks","title":"<code>register_create_context_callbacks(create_context)</code>  <code>classmethod</code>","text":"<p>Register callbacks for create context.</p> <p>It is possible to register callbacks listening to changes happened in create context.</p> <p>Methods available on create context: - add_instances_added_callback - add_instances_removed_callback - add_value_changed_callback - add_pre_create_attr_defs_change_callback - add_create_attr_defs_change_callback - add_publish_attr_defs_change_callback</p> <p>Parameters:</p> Name Type Description Default <code>create_context</code> <code>CreateContext</code> <p>Create context.</p> required Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>@classmethod\ndef register_create_context_callbacks(\n    cls, create_context: \"CreateContext\"\n):\n    \"\"\"Register callbacks for create context.\n\n    It is possible to register callbacks listening to changes happened\n    in create context.\n\n    Methods available on create context:\n    - add_instances_added_callback\n    - add_instances_removed_callback\n    - add_value_changed_callback\n    - add_pre_create_attr_defs_change_callback\n    - add_create_attr_defs_change_callback\n    - add_publish_attr_defs_change_callback\n\n    Args:\n        create_context (CreateContext): Create context.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.AbstractCollectRender","title":"<code>AbstractCollectRender</code>","text":"<p>               Bases: <code>ContextPlugin</code></p> <p>Gather all publishable render layers from renderSetup.</p> Source code in <code>client/ayon_core/pipeline/publish/abstract_collect_render.py</code> <pre><code>class AbstractCollectRender(\n    pyblish.api.ContextPlugin, metaclass=AbstractMetaContextPlugin\n):\n    \"\"\"Gather all publishable render layers from renderSetup.\"\"\"\n\n    order = pyblish.api.CollectorOrder + 0.01\n    label = \"Collect Render\"\n    sync_workfile_version = False\n\n    def __init__(self, *args, **kwargs):\n        \"\"\"Constructor.\"\"\"\n        super(AbstractCollectRender, self).__init__(*args, **kwargs)\n        self._file_path = None\n        self._context = None\n\n    def process(self, context):\n        \"\"\"Entry point to collector.\"\"\"\n        self._context = context\n        for instance in context:\n            # make sure workfile instance publishing is enabled\n            try:\n                if \"workfile\" in instance.data[\"families\"]:\n                    instance.data[\"publish\"] = True\n                # TODO merge renderFarm and render.farm\n                if (\"renderFarm\" in instance.data[\"families\"] or\n                        \"render.farm\" in instance.data[\"families\"]):\n                    instance.data[\"remove\"] = True\n            except KeyError:\n                # be tolerant if 'families' is missing.\n                pass\n\n        self._file_path = context.data[\"currentFile\"].replace(\"\\\\\", \"/\")\n\n        render_instances = self.get_instances(context)\n        for render_instance in render_instances:\n            exp_files = self.get_expected_files(render_instance)\n            assert exp_files, \"no file names were generated, this is bug\"\n\n            # if we want to attach render to product, check if we have AOV's\n            # in expectedFiles. If so, raise error as we cannot attach AOV\n            # (considered to be product on its own) to another product\n            if render_instance.attachTo:\n                assert isinstance(exp_files, list), (\n                    \"attaching multiple AOVs or renderable cameras to \"\n                    \"product is not supported\"\n                )\n\n            frame_start_render = int(render_instance.frameStart)\n            frame_end_render = int(render_instance.frameEnd)\n            # TODO: Refactor hacky frame range workaround below\n            if (render_instance.ignoreFrameHandleCheck or\n                    int(context.data['frameStartHandle']) == frame_start_render\n                    and int(context.data['frameEndHandle']) == frame_end_render):  # noqa: W503, E501\n                # only for Harmony where frame range cannot be set by DB\n                handle_start = context.data['handleStart']\n                handle_end = context.data['handleEnd']\n                frame_start = context.data['frameStart']\n                frame_end = context.data['frameEnd']\n                frame_start_handle = context.data['frameStartHandle']\n                frame_end_handle = context.data['frameEndHandle']\n            elif (hasattr(render_instance, \"frameStartHandle\")\n                  and hasattr(render_instance, \"frameEndHandle\")):\n                handle_start = int(render_instance.handleStart)\n                handle_end = int(render_instance.handleEnd)\n                frame_start = int(render_instance.frameStart)\n                frame_end = int(render_instance.frameEnd)\n                frame_start_handle = int(render_instance.frameStartHandle)\n                frame_end_handle = int(render_instance.frameEndHandle)\n            else:\n                handle_start = 0\n                handle_end = 0\n                frame_start = frame_start_render\n                frame_end = frame_end_render\n                frame_start_handle = frame_start_render\n                frame_end_handle = frame_end_render\n\n            data = {\n                \"handleStart\": handle_start,\n                \"handleEnd\": handle_end,\n                \"frameStart\": frame_start,\n                \"frameEnd\": frame_end,\n                \"frameStartHandle\": frame_start_handle,\n                \"frameEndHandle\": frame_end_handle,\n                \"byFrameStep\": int(render_instance.frameStep),\n\n                \"author\": context.data[\"user\"],\n                # Add source to allow tracing back to the scene from\n                # which was submitted originally\n                \"expectedFiles\": exp_files,\n            }\n            if self.sync_workfile_version:\n                data[\"version\"] = context.data[\"version\"]\n\n            # add additional data\n            data = self.add_additional_data(data)\n\n            instance = render_instance.source_instance\n            if instance is None:\n                instance = context.create_instance(render_instance.name)\n\n            render_instance_dict = attr.asdict(render_instance)\n            instance.data.update(render_instance_dict)\n            instance.data.update(data)\n\n        self.post_collecting_action()\n\n    @abstractmethod\n    def get_instances(self, context):\n        \"\"\"Get all renderable instances and their data.\n\n        Args:\n            context (pyblish.api.Context): Context object.\n\n        Returns:\n            list of :class:`RenderInstance`: All collected renderable instances\n                (like render layers, write nodes, etc.)\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_expected_files(self, render_instance):\n        \"\"\"Get list of expected files.\n\n        Returns:\n            list: expected files. This can be either simple list of files with\n                their paths, or list of dictionaries, where key is name of AOV\n                for example and value is list of files for that AOV.\n\n        Example::\n\n            ['/path/to/file.001.exr', '/path/to/file.002.exr']\n\n            or as dictionary:\n\n            [\n                {\n                    \"beauty\": ['/path/to/beauty.001.exr', ...],\n                    \"mask\": ['/path/to/mask.001.exr']\n                }\n            ]\n\n        \"\"\"\n        pass\n\n    def add_additional_data(self, data):\n        \"\"\"Add additional data to collected instance.\n\n        This can be overridden by host implementation to add custom\n        additional data.\n\n        \"\"\"\n        return data\n\n    def post_collecting_action(self):\n        \"\"\"Execute some code after collection is done.\n\n        This is useful for example for restoring current render layer.\n\n        \"\"\"\n        pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.AbstractCollectRender.__init__","title":"<code>__init__(*args, **kwargs)</code>","text":"<p>Constructor.</p> Source code in <code>client/ayon_core/pipeline/publish/abstract_collect_render.py</code> <pre><code>def __init__(self, *args, **kwargs):\n    \"\"\"Constructor.\"\"\"\n    super(AbstractCollectRender, self).__init__(*args, **kwargs)\n    self._file_path = None\n    self._context = None\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.AbstractCollectRender.add_additional_data","title":"<code>add_additional_data(data)</code>","text":"<p>Add additional data to collected instance.</p> <p>This can be overridden by host implementation to add custom additional data.</p> Source code in <code>client/ayon_core/pipeline/publish/abstract_collect_render.py</code> <pre><code>def add_additional_data(self, data):\n    \"\"\"Add additional data to collected instance.\n\n    This can be overridden by host implementation to add custom\n    additional data.\n\n    \"\"\"\n    return data\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.AbstractCollectRender.get_expected_files","title":"<code>get_expected_files(render_instance)</code>  <code>abstractmethod</code>","text":"<p>Get list of expected files.</p> <p>Returns:</p> Name Type Description <code>list</code> <p>expected files. This can be either simple list of files with their paths, or list of dictionaries, where key is name of AOV for example and value is list of files for that AOV.</p> <p>Example::</p> <pre><code>['/path/to/file.001.exr', '/path/to/file.002.exr']\n\nor as dictionary:\n\n[\n    {\n        \"beauty\": ['/path/to/beauty.001.exr', ...],\n        \"mask\": ['/path/to/mask.001.exr']\n    }\n]\n</code></pre> Source code in <code>client/ayon_core/pipeline/publish/abstract_collect_render.py</code> <pre><code>@abstractmethod\ndef get_expected_files(self, render_instance):\n    \"\"\"Get list of expected files.\n\n    Returns:\n        list: expected files. This can be either simple list of files with\n            their paths, or list of dictionaries, where key is name of AOV\n            for example and value is list of files for that AOV.\n\n    Example::\n\n        ['/path/to/file.001.exr', '/path/to/file.002.exr']\n\n        or as dictionary:\n\n        [\n            {\n                \"beauty\": ['/path/to/beauty.001.exr', ...],\n                \"mask\": ['/path/to/mask.001.exr']\n            }\n        ]\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.AbstractCollectRender.get_instances","title":"<code>get_instances(context)</code>  <code>abstractmethod</code>","text":"<p>Get all renderable instances and their data.</p> <p>Parameters:</p> Name Type Description Default <code>context</code> <code>Context</code> <p>Context object.</p> required <p>Returns:</p> Type Description <p>list of :class:<code>RenderInstance</code>: All collected renderable instances (like render layers, write nodes, etc.)</p> Source code in <code>client/ayon_core/pipeline/publish/abstract_collect_render.py</code> <pre><code>@abstractmethod\ndef get_instances(self, context):\n    \"\"\"Get all renderable instances and their data.\n\n    Args:\n        context (pyblish.api.Context): Context object.\n\n    Returns:\n        list of :class:`RenderInstance`: All collected renderable instances\n            (like render layers, write nodes, etc.)\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.AbstractCollectRender.post_collecting_action","title":"<code>post_collecting_action()</code>","text":"<p>Execute some code after collection is done.</p> <p>This is useful for example for restoring current render layer.</p> Source code in <code>client/ayon_core/pipeline/publish/abstract_collect_render.py</code> <pre><code>def post_collecting_action(self):\n    \"\"\"Execute some code after collection is done.\n\n    This is useful for example for restoring current render layer.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.AbstractCollectRender.process","title":"<code>process(context)</code>","text":"<p>Entry point to collector.</p> Source code in <code>client/ayon_core/pipeline/publish/abstract_collect_render.py</code> <pre><code>def process(self, context):\n    \"\"\"Entry point to collector.\"\"\"\n    self._context = context\n    for instance in context:\n        # make sure workfile instance publishing is enabled\n        try:\n            if \"workfile\" in instance.data[\"families\"]:\n                instance.data[\"publish\"] = True\n            # TODO merge renderFarm and render.farm\n            if (\"renderFarm\" in instance.data[\"families\"] or\n                    \"render.farm\" in instance.data[\"families\"]):\n                instance.data[\"remove\"] = True\n        except KeyError:\n            # be tolerant if 'families' is missing.\n            pass\n\n    self._file_path = context.data[\"currentFile\"].replace(\"\\\\\", \"/\")\n\n    render_instances = self.get_instances(context)\n    for render_instance in render_instances:\n        exp_files = self.get_expected_files(render_instance)\n        assert exp_files, \"no file names were generated, this is bug\"\n\n        # if we want to attach render to product, check if we have AOV's\n        # in expectedFiles. If so, raise error as we cannot attach AOV\n        # (considered to be product on its own) to another product\n        if render_instance.attachTo:\n            assert isinstance(exp_files, list), (\n                \"attaching multiple AOVs or renderable cameras to \"\n                \"product is not supported\"\n            )\n\n        frame_start_render = int(render_instance.frameStart)\n        frame_end_render = int(render_instance.frameEnd)\n        # TODO: Refactor hacky frame range workaround below\n        if (render_instance.ignoreFrameHandleCheck or\n                int(context.data['frameStartHandle']) == frame_start_render\n                and int(context.data['frameEndHandle']) == frame_end_render):  # noqa: W503, E501\n            # only for Harmony where frame range cannot be set by DB\n            handle_start = context.data['handleStart']\n            handle_end = context.data['handleEnd']\n            frame_start = context.data['frameStart']\n            frame_end = context.data['frameEnd']\n            frame_start_handle = context.data['frameStartHandle']\n            frame_end_handle = context.data['frameEndHandle']\n        elif (hasattr(render_instance, \"frameStartHandle\")\n              and hasattr(render_instance, \"frameEndHandle\")):\n            handle_start = int(render_instance.handleStart)\n            handle_end = int(render_instance.handleEnd)\n            frame_start = int(render_instance.frameStart)\n            frame_end = int(render_instance.frameEnd)\n            frame_start_handle = int(render_instance.frameStartHandle)\n            frame_end_handle = int(render_instance.frameEndHandle)\n        else:\n            handle_start = 0\n            handle_end = 0\n            frame_start = frame_start_render\n            frame_end = frame_end_render\n            frame_start_handle = frame_start_render\n            frame_end_handle = frame_end_render\n\n        data = {\n            \"handleStart\": handle_start,\n            \"handleEnd\": handle_end,\n            \"frameStart\": frame_start,\n            \"frameEnd\": frame_end,\n            \"frameStartHandle\": frame_start_handle,\n            \"frameEndHandle\": frame_end_handle,\n            \"byFrameStep\": int(render_instance.frameStep),\n\n            \"author\": context.data[\"user\"],\n            # Add source to allow tracing back to the scene from\n            # which was submitted originally\n            \"expectedFiles\": exp_files,\n        }\n        if self.sync_workfile_version:\n            data[\"version\"] = context.data[\"version\"]\n\n        # add additional data\n        data = self.add_additional_data(data)\n\n        instance = render_instance.source_instance\n        if instance is None:\n            instance = context.create_instance(render_instance.name)\n\n        render_instance_dict = attr.asdict(render_instance)\n        instance.data.update(render_instance_dict)\n        instance.data.update(data)\n\n    self.post_collecting_action()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.ColormanagedPyblishPluginMixin","title":"<code>ColormanagedPyblishPluginMixin</code>","text":"<p>               Bases: <code>object</code></p> <p>Mixin for colormanaged plugins.</p> <p>This class is used to set colorspace data to a publishing representation. It contains a static method, get_colorspace_settings, which returns config and file rules data for the host context. It also contains a method, set_representation_colorspace, which sets colorspace data to the representation. The allowed file extensions are listed in the allowed_ext variable. The method first checks if the file extension is in the list of allowed extensions. If it is, it then gets the colorspace settings from the host context and gets a matching colorspace from rules. Finally, it infuses this data into the representation.</p> Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>class ColormanagedPyblishPluginMixin(object):\n    \"\"\"Mixin for colormanaged plugins.\n\n    This class is used to set colorspace data to a publishing\n    representation. It contains a static method,\n    get_colorspace_settings, which returns config and\n    file rules data for the host context.\n    It also contains a method, set_representation_colorspace,\n    which sets colorspace data to the representation.\n    The allowed file extensions are listed in the allowed_ext variable.\n    The method first checks if the file extension is in\n    the list of allowed extensions. If it is, it then gets the\n    colorspace settings from the host context and gets a\n    matching colorspace from rules. Finally, it infuses this\n    data into the representation.\n    \"\"\"\n\n    def get_colorspace_settings(self, context):\n        \"\"\"Returns solved settings for the host context.\n\n        Args:\n            context (publish.Context): publishing context\n\n        Returns:\n            tuple | bool: config, file rules or None\n        \"\"\"\n        return get_colorspace_settings_from_publish_context(context.data)\n\n    def set_representation_colorspace(\n        self, representation, context,\n        colorspace=None,\n    ):\n        \"\"\"Sets colorspace data to representation.\n\n        Args:\n            representation (dict): publishing representation\n            context (publish.Context): publishing context\n            colorspace (str, optional): colorspace name. Defaults to None.\n\n        Example:\n            ```\n            {\n                # for other publish plugins and loaders\n                \"colorspace\": \"linear\",\n                \"config\": {\n                    # for future references in case need\n                    \"path\": \"/abs/path/to/config.ocio\",\n                    # for other plugins within remote publish cases\n                    \"template\": \"{project[root]}/path/to/config.ocio\"\n                }\n            }\n            ```\n\n        \"\"\"\n\n        # using cached settings if available\n        set_colorspace_data_to_representation(\n            representation, context.data,\n            colorspace,\n            log=self.log\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.ColormanagedPyblishPluginMixin.get_colorspace_settings","title":"<code>get_colorspace_settings(context)</code>","text":"<p>Returns solved settings for the host context.</p> <p>Parameters:</p> Name Type Description Default <code>context</code> <code>Context</code> <p>publishing context</p> required <p>Returns:</p> Type Description <p>tuple | bool: config, file rules or None</p> Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>def get_colorspace_settings(self, context):\n    \"\"\"Returns solved settings for the host context.\n\n    Args:\n        context (publish.Context): publishing context\n\n    Returns:\n        tuple | bool: config, file rules or None\n    \"\"\"\n    return get_colorspace_settings_from_publish_context(context.data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.ColormanagedPyblishPluginMixin.set_representation_colorspace","title":"<code>set_representation_colorspace(representation, context, colorspace=None)</code>","text":"<p>Sets colorspace data to representation.</p> <p>Parameters:</p> Name Type Description Default <code>representation</code> <code>dict</code> <p>publishing representation</p> required <code>context</code> <code>Context</code> <p>publishing context</p> required <code>colorspace</code> <code>str</code> <p>colorspace name. Defaults to None.</p> <code>None</code> Example <pre><code>{\n    # for other publish plugins and loaders\n    \"colorspace\": \"linear\",\n    \"config\": {\n        # for future references in case need\n        \"path\": \"/abs/path/to/config.ocio\",\n        # for other plugins within remote publish cases\n        \"template\": \"{project[root]}/path/to/config.ocio\"\n    }\n}\n</code></pre> Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>def set_representation_colorspace(\n    self, representation, context,\n    colorspace=None,\n):\n    \"\"\"Sets colorspace data to representation.\n\n    Args:\n        representation (dict): publishing representation\n        context (publish.Context): publishing context\n        colorspace (str, optional): colorspace name. Defaults to None.\n\n    Example:\n        ```\n        {\n            # for other publish plugins and loaders\n            \"colorspace\": \"linear\",\n            \"config\": {\n                # for future references in case need\n                \"path\": \"/abs/path/to/config.ocio\",\n                # for other plugins within remote publish cases\n                \"template\": \"{project[root]}/path/to/config.ocio\"\n            }\n        }\n        ```\n\n    \"\"\"\n\n    # using cached settings if available\n    set_colorspace_data_to_representation(\n        representation, context.data,\n        colorspace,\n        log=self.log\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.ExpectedFiles","title":"<code>ExpectedFiles</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Class grouping functionality for all supported renderers.</p> <p>Attributes:</p> Name Type Description <code>multipart</code> <code>bool</code> <p>Flag if multipart exrs are used.</p> Source code in <code>client/ayon_core/pipeline/publish/abstract_expected_files.py</code> <pre><code>class ExpectedFiles(ABC):\n    \"\"\"Class grouping functionality for all supported renderers.\n\n    Attributes:\n        multipart (bool): Flag if multipart exrs are used.\n\n    \"\"\"\n\n    multipart = False\n\n    @abstractmethod\n    def get(self, render_instance):\n        \"\"\"Get expected files for given renderer and render layer.\n\n        This method should return dictionary of all files we are expecting\n        to be rendered from the host. Usually `render_instance` corresponds\n        to *render layer*. Result can be either flat list with the file\n        paths or it can be list of dictionaries. Each key corresponds to\n        for example AOV name or channel, etc.\n\n        Example::\n\n            ['/path/to/file.001.exr', '/path/to/file.002.exr']\n\n            or as dictionary:\n\n            [\n                {\n                    \"beauty\": ['/path/to/beauty.001.exr', ...],\n                    \"mask\": ['/path/to/mask.001.exr']\n                }\n            ]\n\n\n        Args:\n            render_instance (:class:`RenderInstance`): Data passed from\n                collector to determine files. This should be instance of\n                :class:`abstract_collect_render.RenderInstance`\n\n        Returns:\n            list: Full paths to expected rendered files.\n            list of dict: Path to expected rendered files categorized by\n                AOVs, etc.\n\n        \"\"\"\n        raise NotImplementedError()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.ExpectedFiles.get","title":"<code>get(render_instance)</code>  <code>abstractmethod</code>","text":"<p>Get expected files for given renderer and render layer.</p> <p>This method should return dictionary of all files we are expecting to be rendered from the host. Usually <code>render_instance</code> corresponds to render layer. Result can be either flat list with the file paths or it can be list of dictionaries. Each key corresponds to for example AOV name or channel, etc.</p> <p>Example::</p> <pre><code>['/path/to/file.001.exr', '/path/to/file.002.exr']\n\nor as dictionary:\n\n[\n    {\n        \"beauty\": ['/path/to/beauty.001.exr', ...],\n        \"mask\": ['/path/to/mask.001.exr']\n    }\n]\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>render_instance (</code> <p>class:<code>RenderInstance</code>): Data passed from collector to determine files. This should be instance of :class:<code>abstract_collect_render.RenderInstance</code></p> required <p>Returns:</p> Name Type Description <code>list</code> <p>Full paths to expected rendered files.</p> <p>list of dict: Path to expected rendered files categorized by AOVs, etc.</p> Source code in <code>client/ayon_core/pipeline/publish/abstract_expected_files.py</code> <pre><code>@abstractmethod\ndef get(self, render_instance):\n    \"\"\"Get expected files for given renderer and render layer.\n\n    This method should return dictionary of all files we are expecting\n    to be rendered from the host. Usually `render_instance` corresponds\n    to *render layer*. Result can be either flat list with the file\n    paths or it can be list of dictionaries. Each key corresponds to\n    for example AOV name or channel, etc.\n\n    Example::\n\n        ['/path/to/file.001.exr', '/path/to/file.002.exr']\n\n        or as dictionary:\n\n        [\n            {\n                \"beauty\": ['/path/to/beauty.001.exr', ...],\n                \"mask\": ['/path/to/mask.001.exr']\n            }\n        ]\n\n\n    Args:\n        render_instance (:class:`RenderInstance`): Data passed from\n            collector to determine files. This should be instance of\n            :class:`abstract_collect_render.RenderInstance`\n\n    Returns:\n        list: Full paths to expected rendered files.\n        list of dict: Path to expected rendered files categorized by\n            AOVs, etc.\n\n    \"\"\"\n    raise NotImplementedError()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.Extractor","title":"<code>Extractor</code>","text":"<p>               Bases: <code>InstancePlugin</code></p> <p>Extractor base class.</p> <p>The extractor base class implements a \"staging_dir\" function used to generate a temporary directory for an instance to extract to.</p> <p>This temporary directory is generated through <code>tempfile.mkdtemp()</code></p> Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>class Extractor(pyblish.api.InstancePlugin):\n    \"\"\"Extractor base class.\n\n    The extractor base class implements a \"staging_dir\" function used to\n    generate a temporary directory for an instance to extract to.\n\n    This temporary directory is generated through `tempfile.mkdtemp()`\n\n    \"\"\"\n\n    order = 2.0\n\n    def staging_dir(self, instance):\n        \"\"\"Provide a temporary directory in which to store extracted files\n\n        Upon calling this method the staging directory is stored inside\n        the instance.data['stagingDir']\n        \"\"\"\n\n        return get_instance_staging_dir(instance)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.Extractor.staging_dir","title":"<code>staging_dir(instance)</code>","text":"<p>Provide a temporary directory in which to store extracted files</p> <p>Upon calling this method the staging directory is stored inside the instance.data['stagingDir']</p> Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>def staging_dir(self, instance):\n    \"\"\"Provide a temporary directory in which to store extracted files\n\n    Upon calling this method the staging directory is stored inside\n    the instance.data['stagingDir']\n    \"\"\"\n\n    return get_instance_staging_dir(instance)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.IntegrationTemplateItem","title":"<code>IntegrationTemplateItem</code>","text":"<p>Represents single template item.</p> <p>Template path, template data that was used in the template.</p> <p>Attributes:</p> Name Type Description <code>anatomy</code> <code>Anatomy</code> <p>Anatomy object.</p> <code>template_data</code> <code>dict[str, Any]</code> <p>Template data.</p> <code>template_object</code> <code>TemplateItem</code> <p>Template object</p> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>class IntegrationTemplateItem:\n    \"\"\"Represents single template item.\n\n    Template path, template data that was used in the template.\n\n    Attributes:\n        anatomy (Anatomy): Anatomy object.\n        template_data (dict[str, Any]): Template data.\n        template_object (AnatomyTemplateItem): Template object\n    \"\"\"\n    anatomy: Anatomy\n    template_data: dict[str, Any]\n    template_object: \"AnatomyTemplateItem\"\n\n    def __init__(self,\n        anatomy: \"Anatomy\",\n        template_data: dict[str, Any],\n        template_object: \"AnatomyTemplateItem\"):\n        \"\"\"Initialize TemplateItem.\n\n        Args:\n            anatomy (Anatomy): Anatomy object.\n            template_data (dict[str, Any]): Template data.\n            template_object (AnatomyTemplateItem): Template object.\n\n        \"\"\"\n        self.anatomy = anatomy\n        self.template_data = template_data\n        self.template_object = template_object\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.IntegrationTemplateItem.__init__","title":"<code>__init__(anatomy, template_data, template_object)</code>","text":"<p>Initialize TemplateItem.</p> <p>Parameters:</p> Name Type Description Default <code>anatomy</code> <code>Anatomy</code> <p>Anatomy object.</p> required <code>template_data</code> <code>dict[str, Any]</code> <p>Template data.</p> required <code>template_object</code> <code>TemplateItem</code> <p>Template object.</p> required Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def __init__(self,\n    anatomy: \"Anatomy\",\n    template_data: dict[str, Any],\n    template_object: \"AnatomyTemplateItem\"):\n    \"\"\"Initialize TemplateItem.\n\n    Args:\n        anatomy (Anatomy): Anatomy object.\n        template_data (dict[str, Any]): Template data.\n        template_object (AnatomyTemplateItem): Template object.\n\n    \"\"\"\n    self.anatomy = anatomy\n    self.template_data = template_data\n    self.template_object = template_object\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.KnownPublishError","title":"<code>KnownPublishError</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Publishing crashed because of known error.</p> <p>Artist can't affect source of the error.</p> Deprecated <p>Please use <code>PublishError</code> instead. Marked as deprecated 24/09/02.</p> Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>class KnownPublishError(Exception):\n    \"\"\"Publishing crashed because of known error.\n\n    Artist can't affect source of the error.\n\n    Deprecated:\n        Please use `PublishError` instead. Marked as deprecated 24/09/02.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.OptionalPyblishPluginMixin","title":"<code>OptionalPyblishPluginMixin</code>","text":"<p>               Bases: <code>AYONPyblishPluginMixin</code></p> <p>Prepare mixin for optional plugins.</p> <p>Defined active attribute definition prepared for published and prepares method which will check if is active or not.</p> <pre><code>class ValidateScene(\n    pyblish.api.InstancePlugin, OptionalPyblishPluginMixin\n):\n    def process(self, instance):\n        # Skip the instance if is not active by data on the instance\n        if not self.is_active(instance.data):\n            return\n</code></pre> Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>class OptionalPyblishPluginMixin(AYONPyblishPluginMixin):\n    \"\"\"Prepare mixin for optional plugins.\n\n    Defined active attribute definition prepared for published and\n    prepares method which will check if is active or not.\n\n    ```\n    class ValidateScene(\n        pyblish.api.InstancePlugin, OptionalPyblishPluginMixin\n    ):\n        def process(self, instance):\n            # Skip the instance if is not active by data on the instance\n            if not self.is_active(instance.data):\n                return\n    ```\n    \"\"\"\n\n    # Allow exposing tooltip from class with `optional_tooltip` attribute\n    optional_tooltip: Optional[str] = None\n\n    @classmethod\n    def get_attribute_defs(cls):\n        \"\"\"Attribute definitions based on plugin's optional attribute.\"\"\"\n\n        # Empty list if plugin is not optional\n        if not getattr(cls, \"optional\", None):\n            return []\n\n        # Get active value from class as default value\n        active = getattr(cls, \"active\", True)\n        # Return boolean stored under 'active' key with label of the class name\n        label = cls.label or cls.__name__\n\n        return [\n            BoolDef(\n                \"active\",\n                default=active,\n                label=label,\n                tooltip=cls.optional_tooltip,\n            )\n        ]\n\n    def is_active(self, data):\n        \"\"\"Check if plugins is active for instance/context based on their data.\n\n        Args:\n            data(dict): Data from instance or context.\n        \"\"\"\n        # Skip if is not optional and return True\n        if not getattr(self, \"optional\", None):\n            return True\n        attr_values = self.get_attr_values_from_data(data)\n        active = attr_values.get(\"active\")\n        if active is None:\n            active = getattr(self, \"active\", True)\n        return active\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.OptionalPyblishPluginMixin.get_attribute_defs","title":"<code>get_attribute_defs()</code>  <code>classmethod</code>","text":"<p>Attribute definitions based on plugin's optional attribute.</p> Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>@classmethod\ndef get_attribute_defs(cls):\n    \"\"\"Attribute definitions based on plugin's optional attribute.\"\"\"\n\n    # Empty list if plugin is not optional\n    if not getattr(cls, \"optional\", None):\n        return []\n\n    # Get active value from class as default value\n    active = getattr(cls, \"active\", True)\n    # Return boolean stored under 'active' key with label of the class name\n    label = cls.label or cls.__name__\n\n    return [\n        BoolDef(\n            \"active\",\n            default=active,\n            label=label,\n            tooltip=cls.optional_tooltip,\n        )\n    ]\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.OptionalPyblishPluginMixin.is_active","title":"<code>is_active(data)</code>","text":"<p>Check if plugins is active for instance/context based on their data.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict</code> <p>Data from instance or context.</p> required Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>def is_active(self, data):\n    \"\"\"Check if plugins is active for instance/context based on their data.\n\n    Args:\n        data(dict): Data from instance or context.\n    \"\"\"\n    # Skip if is not optional and return True\n    if not getattr(self, \"optional\", None):\n        return True\n    attr_values = self.get_attr_values_from_data(data)\n    active = attr_values.get(\"active\")\n    if active is None:\n        active = getattr(self, \"active\", True)\n    return active\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.PublishError","title":"<code>PublishError</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Publishing crashed because of known error.</p> <p>Message will be shown in UI for artist.</p> <p>Parameters:</p> Name Type Description Default <code>message</code> <code>str</code> <p>Message of error. Short explanation an issue.</p> required <code>title</code> <code>Optional[str]</code> <p>Title showed in UI.</p> <code>None</code> <code>description</code> <code>Optional[str]</code> <p>Detailed description of an error. It is possible to use Markdown syntax.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>class PublishError(Exception):\n    \"\"\"Publishing crashed because of known error.\n\n    Message will be shown in UI for artist.\n\n    Args:\n        message (str): Message of error. Short explanation an issue.\n        title (Optional[str]): Title showed in UI.\n        description (Optional[str]): Detailed description of an error.\n            It is possible to use Markdown syntax.\n\n    \"\"\"\n    def __init__(self, message, title=None, description=None, detail=None):\n        self.message = message\n        self.title = title\n        self.description = description or message\n        self.detail = detail\n        super().__init__(message)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.PublishValidationError","title":"<code>PublishValidationError</code>","text":"<p>               Bases: <code>PublishError</code></p> <p>Validation error happened during publishing.</p> <p>This exception should be used when validation publishing failed.</p> <p>Publishing does not stop during validation order if this     exception is raised.</p> <p>Has additional UI specific attributes that may be handy for artist.</p> <p>Argument 'title' is used to group errors.</p> Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>class PublishValidationError(PublishError):\n    \"\"\"Validation error happened during publishing.\n\n    This exception should be used when validation publishing failed.\n\n    Publishing does not stop during validation order if this\n        exception is raised.\n\n    Has additional UI specific attributes that may be handy for artist.\n\n    Argument 'title' is used to group errors.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.PublishXmlValidationError","title":"<code>PublishXmlValidationError</code>","text":"<p>               Bases: <code>PublishValidationError</code></p> <p>Raise an error from a dedicated xml file.</p> <p>Can be useful to have one xml file with different possible messages that     helps to avoid flood code with dedicated artist messages.</p> XML files should live relative to the plugin file location <p>'{plugin dir}/help/some_plugin.xml'.</p> <p>Parameters:</p> Name Type Description Default <code>plugin</code> <code>Plugin</code> <p>Plugin that raised an error. Is used to get path to xml file.</p> required <code>message</code> <code>str</code> <p>Exception message, can be technical, is used for console output.</p> required <code>key</code> <code>Optional[str]</code> <p>XML file can contain multiple error messages, key is used to get one of them. By default is used 'main'.</p> <code>None</code> <code>formatting_data</code> <code>Optional[dict[str, Any]</code> <p>Error message can have variables to fill.</p> <code>None</code> <code>help_filename</code> <code>Optional[str]</code> <p>Name of xml file with messages. By default, is used filename where plugin lives with .xml extension.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>class PublishXmlValidationError(PublishValidationError):\n    \"\"\"Raise an error from a dedicated xml file.\n\n    Can be useful to have one xml file with different possible messages that\n        helps to avoid flood code with dedicated artist messages.\n\n    XML files should live relative to the plugin file location:\n        '{plugin dir}/help/some_plugin.xml'.\n\n    Args:\n        plugin (pyblish.api.Plugin): Plugin that raised an error. Is used\n            to get path to xml file.\n        message (str): Exception message, can be technical, is used for\n            console output.\n        key (Optional[str]): XML file can contain multiple error messages, key\n            is used to get one of them. By default is used 'main'.\n        formatting_data (Optional[dict[str, Any]): Error message can have\n            variables to fill.\n        help_filename (Optional[str]): Name of xml file with messages. By\n            default, is used filename where plugin lives with .xml extension.\n\n    \"\"\"\n    def __init__(\n        self,\n        plugin: pyblish.api.Plugin,\n        message: str,\n        key: Optional[str] = None,\n        formatting_data: Optional[dict[str, Any]] = None,\n        help_filename: Optional[str] = None,\n    ) -&gt; None:\n        if key is None:\n            key = \"main\"\n\n        if not formatting_data:\n            formatting_data = {}\n        result = load_help_content_from_plugin(plugin, help_filename)\n        content_obj = result[\"errors\"][key]\n        description = content_obj.description.format(**formatting_data)\n        detail = content_obj.detail\n        if detail:\n            detail = detail.format(**formatting_data)\n        super().__init__(\n            message,\n            content_obj.title,\n            description,\n            detail\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.RenderInstance","title":"<code>RenderInstance</code>","text":"<p>               Bases: <code>object</code></p> <p>Data collected by collectors.</p> <p>This data class later on passed to collected instances. Those attributes are required later on.</p> Source code in <code>client/ayon_core/pipeline/publish/abstract_collect_render.py</code> <pre><code>@attr.s\nclass RenderInstance(object):\n    \"\"\"Data collected by collectors.\n\n    This data class later on passed to collected instances.\n    Those attributes are required later on.\n\n    \"\"\"\n\n    # metadata\n    version = attr.ib()  # instance version\n    time = attr.ib()  # time of instance creation (get_formatted_current_time)\n    source = attr.ib()  # path to source scene file\n    label = attr.ib()  # label to show in GUI\n    family = attr.ib()  # product base type used for pyblish filtering\n    productType = attr.ib()  # product type\n    productName = attr.ib()  # product name\n    folderPath = attr.ib()  # folder path\n    task = attr.ib()  # task name\n    attachTo = attr.ib()  # product name to attach render to\n    setMembers = attr.ib()  # list of nodes/members producing render output\n    publish = attr.ib()  # bool, True to publish instance\n    name = attr.ib()  # instance name\n\n    # format settings\n    resolutionWidth = attr.ib()  # resolution width (1920)\n    resolutionHeight = attr.ib()  # resolution height (1080)\n    pixelAspect = attr.ib()  # pixel aspect (1.0)\n\n    # time settings\n    frameStart = attr.ib()  # start frame\n    frameEnd = attr.ib()  # start end\n    frameStep = attr.ib()  # frame step\n\n    productBaseType = attr.ib(default=None)  # product base type\n\n    handleStart = attr.ib(default=None)  # start frame\n    handleEnd = attr.ib(default=None)  # start frame\n\n    # for software (like Harmony) where frame range cannot be set by DB\n    # handles need to be propagated if exist\n    ignoreFrameHandleCheck = attr.ib(default=False)\n\n    # --------------------\n    # With default values\n    # metadata\n    renderer = attr.ib(default=\"\")  # renderer - can be used in Deadline\n    review = attr.ib(default=None)  # False - explicitly skip review\n    priority = attr.ib(default=50)  # job priority on farm\n\n    # family = attr.ib(default=\"renderlayer\")\n    families = attr.ib(default=[\"renderlayer\"])  # list of families\n    # True if should be rendered on farm, eg not integrate\n    farm = attr.ib(default=False)\n\n    # format settings\n    multipartExr = attr.ib(default=False)  # flag for multipart exrs\n    convertToScanline = attr.ib(default=False)  # flag for exr conversion\n\n    tileRendering = attr.ib(default=False)  # bool: treat render as tiles\n    tilesX = attr.ib(default=0)  # number of tiles in X\n    tilesY = attr.ib(default=0)  # number of tiles in Y\n\n    # submit_publish_job\n    deadlineSubmissionJob = attr.ib(default=None)\n    anatomyData = attr.ib(default=None)\n    outputDir = attr.ib(default=None)\n    context = attr.ib(default=None)\n    deadline = attr.ib(default=None)\n\n    # The source instance the data of this render instance should merge into\n    source_instance = attr.ib(default=None, type=pyblish.api.Instance)\n\n    @frameStart.validator\n    def check_frame_start(self, _, value):\n        \"\"\"Validate if frame start is not larger then end.\"\"\"\n        if value &gt; self.frameEnd:\n            raise ValueError(\"frameStart must be smaller \"\n                             \"or equal then frameEnd\")\n\n    @frameEnd.validator\n    def check_frame_end(self, _, value):\n        \"\"\"Validate if frame end is not less then start.\"\"\"\n        if value &lt; self.frameStart:\n            raise ValueError(\"frameEnd must be smaller \"\n                             \"or equal then frameStart\")\n\n    @tilesX.validator\n    def check_tiles_x(self, _, value):\n        \"\"\"Validate if tile x isn't less then 1.\"\"\"\n        if not self.tileRendering:\n            return\n        if value &lt; 1:\n            raise ValueError(\"tile X size cannot be less then 1\")\n\n        if value == 1 and self.tilesY == 1:\n            raise ValueError(\"both tiles X a Y sizes are set to 1\")\n\n    @tilesY.validator\n    def check_tiles_y(self, _, value):\n        \"\"\"Validate if tile y isn't less then 1.\"\"\"\n        if not self.tileRendering:\n            return\n        if value &lt; 1:\n            raise ValueError(\"tile Y size cannot be less then 1\")\n\n        if value == 1 and self.tilesX == 1:\n            raise ValueError(\"both tiles X a Y sizes are set to 1\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.RenderInstance.check_frame_end","title":"<code>check_frame_end(_, value)</code>","text":"<p>Validate if frame end is not less then start.</p> Source code in <code>client/ayon_core/pipeline/publish/abstract_collect_render.py</code> <pre><code>@frameEnd.validator\ndef check_frame_end(self, _, value):\n    \"\"\"Validate if frame end is not less then start.\"\"\"\n    if value &lt; self.frameStart:\n        raise ValueError(\"frameEnd must be smaller \"\n                         \"or equal then frameStart\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.RenderInstance.check_frame_start","title":"<code>check_frame_start(_, value)</code>","text":"<p>Validate if frame start is not larger then end.</p> Source code in <code>client/ayon_core/pipeline/publish/abstract_collect_render.py</code> <pre><code>@frameStart.validator\ndef check_frame_start(self, _, value):\n    \"\"\"Validate if frame start is not larger then end.\"\"\"\n    if value &gt; self.frameEnd:\n        raise ValueError(\"frameStart must be smaller \"\n                         \"or equal then frameEnd\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.RenderInstance.check_tiles_x","title":"<code>check_tiles_x(_, value)</code>","text":"<p>Validate if tile x isn't less then 1.</p> Source code in <code>client/ayon_core/pipeline/publish/abstract_collect_render.py</code> <pre><code>@tilesX.validator\ndef check_tiles_x(self, _, value):\n    \"\"\"Validate if tile x isn't less then 1.\"\"\"\n    if not self.tileRendering:\n        return\n    if value &lt; 1:\n        raise ValueError(\"tile X size cannot be less then 1\")\n\n    if value == 1 and self.tilesY == 1:\n        raise ValueError(\"both tiles X a Y sizes are set to 1\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.RenderInstance.check_tiles_y","title":"<code>check_tiles_y(_, value)</code>","text":"<p>Validate if tile y isn't less then 1.</p> Source code in <code>client/ayon_core/pipeline/publish/abstract_collect_render.py</code> <pre><code>@tilesY.validator\ndef check_tiles_y(self, _, value):\n    \"\"\"Validate if tile y isn't less then 1.\"\"\"\n    if not self.tileRendering:\n        return\n    if value &lt; 1:\n        raise ValueError(\"tile Y size cannot be less then 1\")\n\n    if value == 1 and self.tilesX == 1:\n        raise ValueError(\"both tiles X a Y sizes are set to 1\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.RepairAction","title":"<code>RepairAction</code>","text":"<p>               Bases: <code>Action</code></p> <p>Repairs the action</p> <p>To process the repairing this requires a static <code>repair(instance)</code> method is available on the plugin.</p> Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>class RepairAction(pyblish.api.Action):\n    \"\"\"Repairs the action\n\n    To process the repairing this requires a static `repair(instance)` method\n    is available on the plugin.\n    \"\"\"\n\n    label = \"Repair\"\n    on = \"failed\"  # This action is only available on a failed plug-in\n    icon = \"wrench\"  # Icon from Awesome Icon\n\n    def process(self, context, plugin):\n        if not hasattr(plugin, \"repair\"):\n            raise RuntimeError(\"Plug-in does not have repair method.\")\n\n        # Get the errored instances\n        self.log.debug(\"Finding failed instances..\")\n        errored_instances = get_errored_instances_from_context(context,\n                                                               plugin=plugin)\n        for instance in errored_instances:\n            self.log.debug(\n                \"Attempting repair for instance: {} ...\".format(instance)\n            )\n            plugin.repair(instance)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.RepairContextAction","title":"<code>RepairContextAction</code>","text":"<p>               Bases: <code>Action</code></p> <p>Repairs the action</p> <p>To process the repairing this requires a static <code>repair(context)</code> method is available on the plugin.</p> Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>class RepairContextAction(pyblish.api.Action):\n    \"\"\"Repairs the action\n\n    To process the repairing this requires a static `repair(context)` method\n    is available on the plugin.\n    \"\"\"\n\n    label = \"Repair\"\n    on = \"failed\"  # This action is only available on a failed plug-in\n    icon = \"wrench\"  # Icon from Awesome Icon\n\n    def process(self, context, plugin):\n        if not hasattr(plugin, \"repair\"):\n            raise RuntimeError(\"Plug-in does not have repair method.\")\n\n        # Get the failed instances\n        self.log.debug(\"Finding failed plug-ins..\")\n        failed_plugins = get_errored_plugins_from_context(context)\n\n        # Apply pyblish.logic to get the instances for the plug-in\n        if plugin in failed_plugins:\n            self.log.debug(\"Attempting repair ...\")\n            plugin.repair(context)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.add_trait_representations","title":"<code>add_trait_representations(instance, representations)</code>","text":"<p>Add trait representations to instance.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>Instance</code> <p>Instance to add trait representations to.</p> required <code>representations</code> <code>list[Representation]</code> <p>List of representation trait based representations to add.</p> required Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def add_trait_representations(\n        instance: pyblish.api.Instance,\n        representations: list[Representation]\n) -&gt; None:\n    \"\"\"Add trait representations to instance.\n\n    Args:\n        instance (pyblish.api.Instance): Instance to add trait\n            representations to.\n        representations (list[Representation]): List of representation\n            trait based representations to add.\n\n    \"\"\"\n    repres = instance.data.setdefault(TRAIT_INSTANCE_KEY, [])\n    repres.extend(representations)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.apply_plugin_settings_automatically","title":"<code>apply_plugin_settings_automatically(plugin, settings, logger=None)</code>","text":"<p>Automatically apply plugin settings to a plugin object.</p> Note <p>This function was created to be able to use it in custom overrides of     'apply_settings' class method.</p> <p>Parameters:</p> Name Type Description Default <code>plugin</code> <code>type[Plugin]</code> <p>Class of a plugin.</p> required <code>settings</code> <code>dict[str, Any]</code> <p>Plugin specific settings.</p> required <code>logger</code> <code>Optional[Logger]</code> <p>Logger to log debug messages about applied settings values.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def apply_plugin_settings_automatically(plugin, settings, logger=None):\n    \"\"\"Automatically apply plugin settings to a plugin object.\n\n    Note:\n        This function was created to be able to use it in custom overrides of\n            'apply_settings' class method.\n\n    Args:\n        plugin (type[pyblish.api.Plugin]): Class of a plugin.\n        settings (dict[str, Any]): Plugin specific settings.\n        logger (Optional[logging.Logger]): Logger to log debug messages about\n            applied settings values.\n    \"\"\"\n\n    for option, value in settings.items():\n        if logger:\n            logger.debug(\"Plugin %s - Attr: %s -&gt; %s\",\n                         plugin.__name__, option, value)\n        setattr(plugin, option, value)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.context_plugin_should_run","title":"<code>context_plugin_should_run(plugin, context)</code>","text":"<p>Return whether the ContextPlugin should run on the given context.</p> <p>This is a helper function to work around a bug pyblish-base#250 Whenever a ContextPlugin sets specific families it will still trigger even when no instances are present that have those families.</p> <p>This actually checks it correctly and returns whether it should run.</p> <p>Parameters:</p> Name Type Description Default <code>plugin</code> <code>Plugin</code> <p>Plugin with filters.</p> required <code>context</code> <code>Context</code> <p>Pyblish context with instances.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <p>Context plugin should run based on valid instances.</p> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def context_plugin_should_run(plugin, context):\n    \"\"\"Return whether the ContextPlugin should run on the given context.\n\n    This is a helper function to work around a bug pyblish-base#250\n    Whenever a ContextPlugin sets specific families it will still trigger even\n    when no instances are present that have those families.\n\n    This actually checks it correctly and returns whether it should run.\n\n    Args:\n        plugin (pyblish.api.Plugin): Plugin with filters.\n        context (pyblish.api.Context): Pyblish context with instances.\n\n    Returns:\n        bool: Context plugin should run based on valid instances.\n    \"\"\"\n\n    for _ in filter_instances_for_context_plugin(plugin, context):\n        return True\n    return False\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.filter_crashed_publish_paths","title":"<code>filter_crashed_publish_paths(project_name, crashed_paths, *, project_settings=None)</code>","text":"<p>Filter crashed paths happened during plugins discovery.</p> <p>Check if plugins discovery has enabled strict mode and filter crashed     paths that happened during discover based on regexes from settings.</p> <p>Publishing should not start if any paths are returned.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name in which context plugins discovery happened.</p> required <code>crashed_paths</code> <code>set[str]</code> <p>Crashed paths from plugins discovery report.</p> required <code>project_settings</code> <code>Optional[dict[str, Any]]</code> <p>Project settings.</p> <code>None</code> <p>Returns:</p> Type Description <code>set[str]</code> <p>set[str]: Filtered crashed paths.</p> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def filter_crashed_publish_paths(\n    project_name: str,\n    crashed_paths: set[str],\n    *,\n    project_settings: Optional[dict[str, Any]] = None,\n) -&gt; set[str]:\n    \"\"\"Filter crashed paths happened during plugins discovery.\n\n    Check if plugins discovery has enabled strict mode and filter crashed\n        paths that happened during discover based on regexes from settings.\n\n    Publishing should not start if any paths are returned.\n\n    Args:\n        project_name (str): Project name in which context plugins discovery\n            happened.\n        crashed_paths (set[str]): Crashed paths from plugins discovery report.\n        project_settings (Optional[dict[str, Any]]): Project settings.\n\n    Returns:\n        set[str]: Filtered crashed paths.\n\n    \"\"\"\n    filtered_paths = set()\n    # Nothing crashed all good...\n    if not crashed_paths:\n        return filtered_paths\n\n    if project_settings is None:\n        project_settings = get_project_settings(project_name)\n\n    discover_validation = (\n        project_settings[\"core\"][\"tools\"][\"publish\"][\"discover_validation\"]\n    )\n    # Strict mode is not enabled.\n    if not discover_validation[\"enabled\"]:\n        return filtered_paths\n\n    regexes = [\n        re.compile(value, re.IGNORECASE)\n        for value in discover_validation[\"ignore_paths\"]\n        if value\n    ]\n    is_windows = platform.system().lower() == \"windows\"\n    # Fitler path with regexes from settings\n    for path in crashed_paths:\n        # Normalize paths to use forward slashes on windows\n        if is_windows:\n            path = path.replace(\"\\\\\", \"/\")\n        is_invalid = True\n        for regex in regexes:\n            if regex.match(path):\n                is_invalid = False\n                break\n\n        if is_invalid:\n            filtered_paths.add(path)\n\n    return filtered_paths\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.filter_instances_for_context_plugin","title":"<code>filter_instances_for_context_plugin(plugin, context)</code>","text":"<p>Filter instances on context by context plugin filters.</p> <p>This is for cases when context plugin need similar filtering like instance plugin have, but for some reason must run on context or should find out if there is at least one instance with a family.</p> <p>Parameters:</p> Name Type Description Default <code>plugin</code> <code>Plugin</code> <p>Plugin with filters.</p> required <code>context</code> <code>Context</code> <p>Pyblish context with instances.</p> required <p>Returns:</p> Type Description <p>Iterator[pyblish.lib.Instance]: Iteration of valid instances.</p> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def filter_instances_for_context_plugin(plugin, context):\n    \"\"\"Filter instances on context by context plugin filters.\n\n    This is for cases when context plugin need similar filtering like instance\n    plugin have, but for some reason must run on context or should find out\n    if there is at least one instance with a family.\n\n    Args:\n        plugin (pyblish.api.Plugin): Plugin with filters.\n        context (pyblish.api.Context): Pyblish context with instances.\n\n    Returns:\n        Iterator[pyblish.lib.Instance]: Iteration of valid instances.\n    \"\"\"\n\n    instances = []\n    plugin_families = set()\n    all_families = False\n    if plugin.families:\n        instances = context\n        plugin_families = set(plugin.families)\n        all_families = \"*\" in plugin_families\n\n    for instance in instances:\n        # Ignore inactive instances\n        if (\n            not instance.data.get(\"publish\", True)\n            or not instance.data.get(\"active\", True)\n        ):\n            continue\n\n        family = instance.data.get(\"family\")\n        families = instance.data.get(\"families\") or []\n        if (\n            all_families\n            or (family and family in plugin_families)\n            or any(f in plugin_families for f in families)\n        ):\n            yield instance\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.get_default_reviewable_layers","title":"<code>get_default_reviewable_layers(project_settings)</code>","text":"<p>Get default reviewable layers from project settings.</p> <p>Parameters:</p> Name Type Description Default <code>project_settings</code> <code>dict</code> <p>Project settings.</p> required <p>Returns:</p> Type Description <code>list[str]</code> <p>list[str]: List of default reviewable layers.</p> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def get_default_reviewable_layers(project_settings: dict) -&gt; list[str]:\n    \"\"\"Get default reviewable layers from project settings.\n\n    Args:\n        project_settings (dict): Project settings.\n\n    Returns:\n        list[str]: List of default reviewable layers.\n    \"\"\"\n    return project_settings[\"core\"][\"reviewable_layers\"][\"review_layers\"]\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.get_errored_instances_from_context","title":"<code>get_errored_instances_from_context(context, plugin=None)</code>","text":"<p>Collect failed instances from pyblish context.</p> <p>Parameters:</p> Name Type Description Default <code>context</code> <code>Context</code> <p>Publish context where we're looking for failed instances.</p> required <code>plugin</code> <code>Plugin</code> <p>If provided then only consider errors related to that plug-in.</p> <code>None</code> <p>Returns:</p> Type Description <p>List[pyblish.lib.Instance]: Instances which failed during processing.</p> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def get_errored_instances_from_context(context, plugin=None):\n    \"\"\"Collect failed instances from pyblish context.\n\n    Args:\n        context (pyblish.api.Context): Publish context where we're looking\n            for failed instances.\n        plugin (pyblish.api.Plugin): If provided then only consider errors\n            related to that plug-in.\n\n    Returns:\n        List[pyblish.lib.Instance]: Instances which failed during processing.\n    \"\"\"\n\n    instances = list()\n    for result in context.data[\"results\"]:\n        if result[\"instance\"] is None:\n            # When instance is None we are on the \"context\" result\n            continue\n\n        if plugin is not None and result.get(\"plugin\") != plugin:\n            continue\n\n        if result[\"error\"]:\n            instances.append(result[\"instance\"])\n\n    return instances\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.get_errored_plugins_from_context","title":"<code>get_errored_plugins_from_context(context)</code>","text":"<p>Collect failed plugins from pyblish context.</p> <p>Parameters:</p> Name Type Description Default <code>context</code> <code>Context</code> <p>Publish context where we're looking for failed plugins.</p> required <p>Returns:</p> Type Description <p>List[pyblish.api.Plugin]: Plugins which failed during processing.</p> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def get_errored_plugins_from_context(context):\n    \"\"\"Collect failed plugins from pyblish context.\n\n    Args:\n        context (pyblish.api.Context): Publish context where we're looking\n            for failed plugins.\n\n    Returns:\n        List[pyblish.api.Plugin]: Plugins which failed during processing.\n    \"\"\"\n\n    plugins = list()\n    results = context.data.get(\"results\", [])\n    for result in results:\n        if result[\"success\"] is True:\n            continue\n        plugins.append(result[\"plugin\"])\n\n    return plugins\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.get_instance_families","title":"<code>get_instance_families(instance)</code>","text":"<p>Get all families of the instance.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>Instance</code> <p>Instance to get families from.</p> required <p>Returns:</p> Type Description <code>list[str]</code> <p>list[str]: List of families.</p> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def get_instance_families(instance: pyblish.api.Instance) -&gt; list[str]:\n    \"\"\"Get all families of the instance.\n\n    Args:\n        instance (pyblish.api.Instance): Instance to get families from.\n\n    Returns:\n        list[str]: List of families.\n\n    \"\"\"\n    family = instance.data.get(\"family\")\n    families = []\n    if family:\n        families.append(family)\n\n    for _family in (instance.data.get(\"families\") or []):\n        if _family not in families:\n            families.append(_family)\n\n    return families\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.get_instance_publish_template","title":"<code>get_instance_publish_template(instance)</code>","text":"<p>Return anatomy template name to use for integration.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>Instance</code> <p>Instance to process.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Anatomy template name</p> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def get_instance_publish_template(instance: pyblish.api.Instance) -&gt; str:\n    \"\"\"Return anatomy template name to use for integration.\n\n    Args:\n        instance (pyblish.api.Instance): Instance to process.\n\n    Returns:\n        str: Anatomy template name\n\n    \"\"\"\n    # Anatomy data is pre-filled by Collectors\n    publish_template = get_publish_template_object(instance)\n    return publish_template[\"path\"].template.replace(\"\\\\\", \"/\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.get_instance_staging_dir","title":"<code>get_instance_staging_dir(instance)</code>","text":"<p>Unified way how staging dir is stored and created on instances.</p> <p>First check if 'stagingDir' is already set in instance data. In case there already is new tempdir will not be created.</p> <p>Returns:</p> Name Type Description <code>str</code> <p>Path to staging dir</p> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def get_instance_staging_dir(instance):\n    \"\"\"Unified way how staging dir is stored and created on instances.\n\n    First check if 'stagingDir' is already set in instance data.\n    In case there already is new tempdir will not be created.\n\n    Returns:\n        str: Path to staging dir\n    \"\"\"\n    staging_dir = instance.data.get(\"stagingDir\")\n\n    if staging_dir:\n        return staging_dir\n\n    anatomy_data = instance.data[\"anatomyData\"]\n    template_data = copy.deepcopy(anatomy_data)\n\n    # context data based variables\n    context = instance.context\n\n    # add current file as workfile name into formatting data\n    current_file = context.data.get(\"currentFile\")\n    if current_file:\n        workfile = os.path.basename(current_file)\n        workfile_name, _ = os.path.splitext(workfile)\n        template_data[\"workfile_name\"] = workfile_name\n\n    product_type = instance.data[\"productType\"]\n    product_base_type = instance.data.get(\"productBaseType\")\n    if not product_base_type:\n        product_base_type = product_type\n    staging_dir_info = get_staging_dir_info(\n        context.data[\"projectEntity\"],\n        instance.data.get(\"folderEntity\"),\n        instance.data.get(\"taskEntity\"),\n        product_base_type=product_base_type,\n        product_type=product_type,\n        product_name=instance.data[\"productName\"],\n        host_name=context.data[\"hostName\"],\n        anatomy=context.data[\"anatomy\"],\n        project_settings=context.data[\"project_settings\"],\n        template_data=template_data,\n        always_return_path=True,\n        username=context.data[\"user\"],\n    )\n\n    staging_dir_path = staging_dir_info.directory\n\n    # path might be already created by get_staging_dir_info\n    os.makedirs(staging_dir_path, exist_ok=True)\n    instance.data.update({\n        \"stagingDir\": staging_dir_path,\n        \"stagingDir_persistent\": staging_dir_info.is_persistent,\n        \"stagingDir_is_custom\": staging_dir_info.is_custom\n    })\n\n    return staging_dir_path\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.get_instance_template_name","title":"<code>get_instance_template_name(instance)</code>","text":"<p>Return anatomy template name to use for integration.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>Instance</code> <p>Instance to process.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Anatomy template name</p> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def get_instance_template_name(instance: pyblish.api.Instance) -&gt; str:\n    \"\"\"Return anatomy template name to use for integration.\n\n    Args:\n        instance (pyblish.api.Instance): Instance to process.\n\n    Returns:\n        str: Anatomy template name\n\n    \"\"\"\n    # Anatomy data is pre-filled by Collectors\n    context = instance.context\n    project_name = context.data[\"projectName\"]\n\n    # Task can be optional in anatomy data\n    host_name = context.data[\"hostName\"]\n    anatomy_data = instance.data[\"anatomyData\"]\n    product_type = instance.data[\"productType\"]\n    product_base_type = instance.data.get(\"productBaseType\")\n    if not product_base_type:\n        product_base_type = product_type\n    task_info = anatomy_data.get(\"task\") or {}\n\n    return get_publish_template_name(\n        project_name,\n        host_name,\n        product_base_type=product_base_type,\n        task_name=task_info.get(\"name\"),\n        task_type=task_info.get(\"type\"),\n        project_settings=context.data[\"project_settings\"],\n        logger=log,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.get_plugin_settings","title":"<code>get_plugin_settings(plugin, project_settings, log, category=None)</code>","text":"<p>Get plugin settings based on host name and plugin name.</p> Note <p>Default implementation of automated settings is passing host name     into 'category'.</p> <p>Parameters:</p> Name Type Description Default <code>plugin</code> <code>Plugin</code> <p>Plugin where settings are applied.</p> required <code>project_settings</code> <code>dict[str, Any]</code> <p>Project settings.</p> required <code>log</code> <code>Logger</code> <p>Logger to log messages.</p> required <code>category</code> <code>Optional[str]</code> <p>Settings category key where to look for plugin settings.</p> <code>None</code> <p>Returns:</p> Type Description <p>dict[str, Any]: Plugin settings {'attribute': 'value'}.</p> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def get_plugin_settings(plugin, project_settings, log, category=None):\n    \"\"\"Get plugin settings based on host name and plugin name.\n\n    Note:\n        Default implementation of automated settings is passing host name\n            into 'category'.\n\n    Args:\n        plugin (pyblish.Plugin): Plugin where settings are applied.\n        project_settings (dict[str, Any]): Project settings.\n        log (logging.Logger): Logger to log messages.\n        category (Optional[str]): Settings category key where to look\n            for plugin settings.\n\n    Returns:\n        dict[str, Any]: Plugin settings {'attribute': 'value'}.\n    \"\"\"\n\n    # Plugin can define settings category by class attribute\n    # - it's impossible to set `settings_category` via settings because\n    #     obviously settings are not applied before it.\n    # - if `settings_category` is set the fallback category method is ignored\n    settings_category = getattr(plugin, \"settings_category\", None)\n    if settings_category:\n        try:\n            category_settings = project_settings[settings_category]\n        except KeyError:\n            log.warning((\n                \"Couldn't find settings category '{}' in project settings\"\n            ).format(settings_category))\n            return {}\n\n        try:\n            return category_settings[\"publish\"][plugin.__name__]\n        except KeyError:\n            return {}\n\n    # Use project settings based on a category name\n    if category:\n        try:\n            output = (\n                project_settings\n                [category]\n                [\"publish\"]\n                [plugin.__name__]\n            )\n            warnings.warn(\n                \"Please fill 'settings_category'\"\n                f\" for plugin '{plugin.__name__}'.\",\n                DeprecationWarning\n            )\n            return output\n        except KeyError:\n            pass\n\n    # Settings category determined from path\n    # - usually path is './&lt;category&gt;/plugins/publish/&lt;plugin file&gt;'\n    # - category can be host name of addon name ('maya', 'deadline', ...)\n    filepath = os.path.normpath(inspect.getfile(plugin))\n\n    split_path = filepath.rsplit(os.path.sep, 5)\n    if len(split_path) &lt; 4:\n        log.debug((\n            \"Plugin path is too short to automatically\"\n            \" extract settings category. {}\"\n        ).format(filepath))\n        return {}\n\n    category_from_file = split_path[-4]\n    plugin_kind = split_path[-2]\n\n    # TODO: change after all plugins are moved one level up\n    if category_from_file == \"ayon_core\":\n        category_from_file = \"core\"\n\n    try:\n        output = (\n            project_settings\n            [category_from_file]\n            [plugin_kind]\n            [plugin.__name__]\n        )\n        warnings.warn(\n            \"Please fill 'settings_category'\"\n            f\" for plugin '{plugin.__name__}'.\",\n            DeprecationWarning\n        )\n        return output\n    except KeyError:\n        pass\n    return {}\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.get_publish_instance_families","title":"<code>get_publish_instance_families(instance)</code>","text":"<p>Get all families of the instance.</p> <p>Look for families under 'productType' and 'families' keys in instance data. Value of 'productType' is used as first family and then all other families in random order.</p> <p>Parameters:</p> Name Type Description Default <code>pyblish.api.Instance</code> <p>Instance to get families from.</p> required <p>Returns:</p> Type Description <p>list[str]: List of families.</p> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def get_publish_instance_families(instance):\n    \"\"\"Get all families of the instance.\n\n    Look for families under 'productType' and 'families' keys in instance data.\n    Value of 'productType' is used as first family and then all other families\n    in random order.\n\n    Args:\n        pyblish.api.Instance: Instance to get families from.\n\n    Returns:\n        list[str]: List of families.\n    \"\"\"\n\n    family = instance.data.get(\"family\")\n    families = set(instance.data.get(\"families\") or [])\n    output = []\n    if family:\n        output.append(family)\n        families.discard(family)\n    output.extend(families)\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.get_publish_instance_label","title":"<code>get_publish_instance_label(instance)</code>","text":"<p>Try to get label from pyblish instance.</p> <p>First are used values in instance data under 'label' and 'name' keys. Then is used string conversion of instance object -&gt; 'instance._name'.</p> Todos <p>Maybe 'productName' key could be used too.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>Instance</code> <p>Pyblish instance.</p> required <p>Returns:</p> Name Type Description <code>str</code> <p>Instance label.</p> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def get_publish_instance_label(instance):\n    \"\"\"Try to get label from pyblish instance.\n\n    First are used values in instance data under 'label' and 'name' keys. Then\n    is used string conversion of instance object -&gt; 'instance._name'.\n\n    Todos:\n        Maybe 'productName' key could be used too.\n\n    Args:\n        instance (pyblish.api.Instance): Pyblish instance.\n\n    Returns:\n        str: Instance label.\n    \"\"\"\n\n    return (\n        instance.data.get(\"label\")\n        or instance.data.get(\"name\")\n        or str(instance)\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.get_publish_repre_path","title":"<code>get_publish_repre_path(instance, repre, only_published=False)</code>","text":"<p>Get representation path that can be used for integration.</p> <p>When 'only_published' is set to true the validation of path is not relevant. In that case we just need what is set in 'published_path' as \"reference\". The reference is not used to get or upload the file but for reference where the file was published.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>Instance</code> <p>Processed instance object. Used for source of staging dir if representation does not have filled it.</p> required <code>repre</code> <code>dict</code> <p>Representation on instance which could be and could not be integrated with main integrator.</p> required <code>only_published</code> <code>bool</code> <p>Care only about published paths and ignore if filepath is not existing anymore.</p> <code>False</code> <p>Returns:</p> Name Type Description <code>str</code> <p>Path to representation file.</p> <code>None</code> <p>Path is not filled or does not exists.</p> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def get_publish_repre_path(instance, repre, only_published=False):\n    \"\"\"Get representation path that can be used for integration.\n\n    When 'only_published' is set to true the validation of path is not\n    relevant. In that case we just need what is set in 'published_path'\n    as \"reference\". The reference is not used to get or upload the file but\n    for reference where the file was published.\n\n    Args:\n        instance (pyblish.Instance): Processed instance object. Used\n            for source of staging dir if representation does not have\n            filled it.\n        repre (dict): Representation on instance which could be and\n            could not be integrated with main integrator.\n        only_published (bool): Care only about published paths and\n            ignore if filepath is not existing anymore.\n\n    Returns:\n        str: Path to representation file.\n        None: Path is not filled or does not exists.\n    \"\"\"\n\n    published_path = repre.get(\"published_path\")\n    if published_path:\n        published_path = os.path.normpath(published_path)\n        if os.path.exists(published_path):\n            return published_path\n\n    if only_published:\n        return published_path\n\n    comp_files = repre[\"files\"]\n    if isinstance(comp_files, (tuple, list, set)):\n        filename = comp_files[0]\n    else:\n        filename = comp_files\n\n    staging_dir = repre.get(\"stagingDir\")\n    if not staging_dir:\n        staging_dir = get_instance_staging_dir(instance)\n\n    # Expand the staging dir path in case it's been stored with the root\n    # template syntax\n    anatomy = instance.context.data[\"anatomy\"]\n    staging_dir = anatomy.fill_root(staging_dir)\n\n    src_path = os.path.normpath(os.path.join(staging_dir, filename))\n    if os.path.exists(src_path):\n        return src_path\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.get_publish_template_name","title":"<code>get_publish_template_name(project_name, host_name, product_base_type, task_name, task_type, *, project_settings=None, hero=False, logger=None)</code>","text":"<p>Get template name which should be used for passed context.</p> <p>Publish templates are filtered by host name, family, task name and task type.</p> <p>Default template which is used at if profiles are not available or profile has empty value is defined by 'DEFAULT_PUBLISH_TEMPLATE' constant.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Name of project where to look for settings.</p> required <code>host_name</code> <code>str</code> <p>Name of host integration.</p> required <code>product_base_type</code> <code>str</code> <p>Product base type for which should be found template.</p> required <code>task_name</code> <code>str</code> <p>Task name on which is instance working.</p> required <code>task_type</code> <code>str</code> <p>Task type on which is instance working.</p> required <code>project_settings</code> <code>Dict[str, Any]</code> <p>Prepared project settings.</p> <code>None</code> <code>hero</code> <code>bool</code> <p>Template is for hero version publishing.</p> <code>False</code> <code>logger</code> <code>Logger</code> <p>Custom logger used for 'filter_profiles' function.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Template name which should be used for integration.</p> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>@_get_publish_template_name_wrap\ndef get_publish_template_name(\n    project_name: str,\n    host_name: str,\n    product_base_type: str,\n    task_name: Union[str, None],\n    task_type: Union[str, None],\n    *,\n    project_settings: Optional[dict] = None,\n    hero: bool = False,\n    logger: Optional[logging.Logger] = None,\n) -&gt; str:\n    \"\"\"Get template name which should be used for passed context.\n\n    Publish templates are filtered by host name, family, task name and\n    task type.\n\n    Default template which is used at if profiles are not available or profile\n    has empty value is defined by 'DEFAULT_PUBLISH_TEMPLATE' constant.\n\n    Args:\n        project_name (str): Name of project where to look for settings.\n        host_name (str): Name of host integration.\n        product_base_type (str): Product base type for which should be\n            found template.\n        task_name (str): Task name on which is instance working.\n        task_type (str): Task type on which is instance working.\n        project_settings (Dict[str, Any]): Prepared project settings.\n        hero (bool): Template is for hero version publishing.\n        logger (logging.Logger): Custom logger used for 'filter_profiles'\n            function.\n\n    Returns:\n        str: Template name which should be used for integration.\n\n    \"\"\"\n    template = None\n    filter_criteria = {\n        \"host_names\": host_name,\n        \"product_base_types\": product_base_type,\n        \"task_names\": task_name,\n        \"task_types\": task_type,\n    }\n    if hero:\n        default_template = DEFAULT_HERO_PUBLISH_TEMPLATE\n        profiles = get_hero_template_name_profiles(\n            project_name, project_settings\n        )\n\n    else:\n        profiles = get_template_name_profiles(\n            project_name, project_settings\n        )\n        default_template = DEFAULT_PUBLISH_TEMPLATE\n\n    profile = filter_profiles(profiles, filter_criteria, logger=logger)\n    if profile:\n        template = profile[\"template_name\"]\n    return template or default_template\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.get_publish_template_object","title":"<code>get_publish_template_object(instance, category_name='publish', template_name=None)</code>","text":"<p>Return anatomy template object to use for integration.</p> <p>Note: What is the actual type of the object?</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>Instance</code> <p>Instance to process.</p> required <code>category_name</code> <code>str</code> <p>Category name of the template to use. Defaults to \"publish\".</p> <code>'publish'</code> <code>template_name</code> <code>str</code> <p>Template name to use. If not provided, it will get the template name from the provided instance.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>AnatomyTemplateItem</code> <code>'AnatomyTemplateItem'</code> <p>Anatomy template object</p> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def get_publish_template_object(\n        instance: pyblish.api.Instance,\n        category_name: str = \"publish\",\n        template_name: Optional[str] = None,\n) -&gt; \"AnatomyTemplateItem\":\n    \"\"\"Return anatomy template object to use for integration.\n\n    Note: What is the actual type of the object?\n\n    Args:\n        instance (pyblish.api.Instance): Instance to process.\n        category_name (str): Category name of the template to use.\n            Defaults to \"publish\".\n        template_name (str, optional): Template name to use.\n            If not provided, it will get the template name from\n            the provided instance.\n\n    Returns:\n        AnatomyTemplateItem: Anatomy template object\n\n    \"\"\"\n    # Anatomy data is pre-filled by Collectors\n    if not template_name:\n        template_name = get_instance_template_name(instance)\n    anatomy: Anatomy = instance.context.data[\"anatomy\"]\n    return anatomy.get_template_item(\n        category_name=category_name,\n        template_name=template_name\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.get_rootless_path","title":"<code>get_rootless_path(anatomy, path)</code>","text":"<p>Get rootless variant of the path.</p> <p>Returns, if possible, a path without an absolute portion from the root (e.g. 'c:\\' or '/opt/..'). This is basically a wrapper for the meth:<code>Anatomy.find_root_template_from_path</code> method that displays a warning if the root path is not found.</p> <p>This information is platform-dependent and shouldn't be captured.  For example::</p> <pre><code> 'c:/projects/MyProject1/Assets/publish...'\n will be transformed to:\n '{root}/MyProject1/Assets...'\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>anatomy</code> <code>Anatomy</code> <p>Project anatomy.</p> required <code>path</code> <code>str</code> <p>Absolute path.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Path where root path is replaced by formatting string.</p> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def get_rootless_path(anatomy: \"Anatomy\", path: str) -&gt; str:\n    r\"\"\"Get rootless variant of the path.\n\n    Returns, if possible, a path without an absolute portion from the root\n    (e.g. 'c:\\' or '/opt/..'). This is basically a wrapper for the\n    meth:`Anatomy.find_root_template_from_path` method that displays\n    a warning if the root path is not found.\n\n     This information is platform-dependent and shouldn't be captured.\n     For example::\n\n         'c:/projects/MyProject1/Assets/publish...'\n         will be transformed to:\n         '{root}/MyProject1/Assets...'\n\n    Args:\n        anatomy (Anatomy): Project anatomy.\n        path (str): Absolute path.\n\n    Returns:\n        str: Path where root path is replaced by formatting string.\n\n    \"\"\"\n    success, rootless_path = anatomy.find_root_template_from_path(path)\n    if success:\n        path = rootless_path\n    else:\n        log.warning((\n            'Could not find root path for remapping \"%s\".'\n            \" This may cause issues on farm.\"\n        ), path)\n    return path\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.get_trait_representations","title":"<code>get_trait_representations(instance)</code>","text":"<p>Get trait representations from instance.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>Instance</code> <p>Instance to get trait representations from.</p> required <p>Returns:</p> Type Description <code>list[Representation]</code> <p>list[Representation]: List of representation names.</p> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def get_trait_representations(\n        instance: pyblish.api.Instance) -&gt; list[Representation]:\n    \"\"\"Get trait representations from instance.\n\n    Args:\n        instance (pyblish.api.Instance): Instance to get trait\n            representations from.\n\n    Returns:\n        list[Representation]: List of representation names.\n\n    \"\"\"\n    return instance.data.get(TRAIT_INSTANCE_KEY, [])\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.get_version_data_from_instance","title":"<code>get_version_data_from_instance(instance)</code>","text":"<p>Get version data from the Instance.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>Instance</code> <p>the current instance being published.</p> required <p>Returns:</p> Name Type Description <code>dict</code> <code>dict</code> <p>the required information for <code>version[\"data\"]</code></p> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def get_version_data_from_instance(\n        instance: pyblish.api.Instance) -&gt; dict:\n    \"\"\"Get version data from the Instance.\n\n    Args:\n        instance (pyblish.api.Instance): the current instance\n            being published.\n\n    Returns:\n        dict: the required information for ``version[\"data\"]``\n\n    \"\"\"\n    context = instance.context\n\n    # create relative source path for DB\n    if \"source\" in instance.data:\n        source = instance.data[\"source\"]\n    else:\n        source = context.data[\"currentFile\"]\n        anatomy = instance.context.data[\"anatomy\"]\n        source = get_rootless_path(anatomy, source)\n    log.debug(\"Source: %s\", source)\n\n    version_data = {\n        \"families\": get_instance_families(instance),\n        \"time\": context.data[\"time\"],\n        \"author\": context.data[\"user\"],\n        \"source\": source,\n        \"comment\": instance.data[\"comment\"],\n        \"machine\": context.data.get(\"machine\"),\n        \"fps\": instance.data.get(\"fps\", context.data.get(\"fps\"))\n    }\n\n    intent_value = context.data.get(\"intent\")\n    if intent_value and isinstance(intent_value, dict):\n        intent_value = intent_value.get(\"value\")\n\n    if intent_value:\n        version_data[\"intent\"] = intent_value\n\n    # Include optional data if present in\n    optionals = [\n        \"frameStart\", \"frameEnd\", \"step\",\n        \"handleEnd\", \"handleStart\", \"sourceHashes\"\n    ]\n    for key in optionals:\n        if key in instance.data:\n            version_data[key] = instance.data[key]\n\n    # Include instance.data[versionData] directly\n    version_data_instance = instance.data.get(\"versionData\")\n    if version_data_instance:\n        version_data.update(version_data_instance)\n\n    return version_data\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.has_trait_representations","title":"<code>has_trait_representations(instance)</code>","text":"<p>Check if instance has trait representation.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>Instance</code> <p>Instance to check.</p> required <p>Returns:</p> Name Type Description <code>True</code> <code>bool</code> <p>Instance has trait representation.</p> <code>False</code> <code>bool</code> <p>Instance does not have trait representation.</p> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def has_trait_representations(\n        instance: pyblish.api.Instance) -&gt; bool:\n    \"\"\"Check if instance has trait representation.\n\n    Args:\n        instance (pyblish.api.Instance): Instance to check.\n\n    Returns:\n        True: Instance has trait representation.\n        False: Instance does not have trait representation.\n\n    \"\"\"\n    return TRAIT_INSTANCE_KEY in instance.data\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.load_help_content_from_filepath","title":"<code>load_help_content_from_filepath(filepath)</code>","text":"<p>Load help content from xml file. Xml file may contain errors and warnings.</p> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def load_help_content_from_filepath(\n    filepath: str\n) -&gt; dict[str, dict[str, HelpContent]]:\n    \"\"\"Load help content from xml file.\n    Xml file may contain errors and warnings.\n    \"\"\"\n    errors = {}\n    warnings = {}\n    output = {\n        \"errors\": errors,\n        \"warnings\": warnings\n    }\n    if not os.path.exists(filepath):\n        return output\n    tree = xml.etree.ElementTree.parse(filepath)\n    root = tree.getroot()\n    for child in root:\n        child_id = child.attrib.get(\"id\")\n        if child_id is None:\n            continue\n\n        # Make sure ID is string\n        child_id = str(child_id)\n\n        title = child.find(\"title\").text\n        description = child.find(\"description\").text\n        detail_node = child.find(\"detail\")\n        detail = None\n        if detail_node is not None:\n            detail = detail_node.text\n        if child.tag == \"error\":\n            errors[child_id] = HelpContent(title, description, detail)\n        elif child.tag == \"warning\":\n            warnings[child_id] = HelpContent(title, description, detail)\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.main_cli_publish","title":"<code>main_cli_publish(path, targets=None, addons_manager=None)</code>","text":"<p>Start headless publishing.</p> <p>Publish use json from passed path argument.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Path to JSON.</p> required <code>targets</code> <code>Optional[List[str]]</code> <p>List of pyblish targets.</p> <code>None</code> <code>addons_manager</code> <code>Optional[AddonsManager]</code> <p>Addons manager instance.</p> <code>None</code> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>When there is no path to process or when executed with list of JSON paths.</p> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def main_cli_publish(\n    path: str,\n    targets: Optional[List[str]] = None,\n    addons_manager: Optional[AddonsManager] = None,\n):\n    \"\"\"Start headless publishing.\n\n    Publish use json from passed path argument.\n\n    Args:\n        path (str): Path to JSON.\n        targets (Optional[List[str]]): List of pyblish targets.\n        addons_manager (Optional[AddonsManager]): Addons manager instance.\n\n    Raises:\n        RuntimeError: When there is no path to process or when executed with\n            list of JSON paths.\n\n    \"\"\"\n    from ayon_core.pipeline import (\n        install_ayon_plugins,\n        get_global_context,\n    )\n\n    # Register target and host\n    if not isinstance(path, str):\n        raise RuntimeError(\"Path to JSON must be a string.\")\n\n    log = Logger.get_logger(\"CLI-publish\")\n\n    # Make public ayon api behave as other user\n    # - this works only if public ayon api is using service user\n    username = os.environ.get(\"AYON_USERNAME\")\n    if username:\n        # NOTE: ayon-python-api does not have public api function to find\n        #   out if is used service user. So we need to have try &gt; except\n        #   block.\n        con = get_server_api_connection()\n        try:\n            con.set_default_service_username(username)\n        except ValueError:\n            pass\n\n    context = get_global_context()\n    project_settings = get_project_settings(context[\"project_name\"])\n\n    install_ayon_plugins()\n\n    if addons_manager is None:\n        addons_manager = AddonsManager(project_settings)\n\n    applications_addon = addons_manager.get_enabled_addon(\"applications\")\n    if applications_addon is not None:\n        env = applications_addon.get_farm_publish_environment_variables(\n            context[\"project_name\"],\n            context[\"folder_path\"],\n            context[\"task_name\"],\n        )\n        os.environ.update(env)\n\n    pyblish.api.register_host(\"shell\")\n\n    if targets:\n        for target in targets:\n            print(f\"setting target: {target}\")\n            pyblish.api.register_target(target)\n    else:\n        pyblish.api.register_target(\"farm\")\n\n    os.environ[\"AYON_PUBLISH_DATA\"] = path\n    os.environ[\"HEADLESS_PUBLISH\"] = 'true'  # to use in app lib\n\n    log.info(\"Running publish ...\")\n\n    discover_result = publish_plugins_discover()\n    print(discover_result.get_report(only_errors=False))\n\n    filtered_crashed_paths = filter_crashed_publish_paths(\n        context[\"project_name\"],\n        set(discover_result.crashed_file_paths),\n        project_settings=project_settings,\n    )\n    if filtered_crashed_paths:\n        joined_paths = \"\\n\".join([\n            f\"- {path}\"\n            for path in filtered_crashed_paths\n        ])\n        log.error(\n            \"Plugin discovery strict mode is enabled.\"\n            \" Crashed plugin paths that prevent from publishing:\"\n            f\"\\n{joined_paths}\"\n        )\n        sys.exit(1)\n\n    publish_plugins = discover_result.plugins\n\n    # Error exit as soon as any error occurs.\n    error_format = \"Failed {plugin.__name__}: {error} -- {error.traceback}\"\n\n    for result in pyblish.util.publish_iter(plugins=publish_plugins):\n        if result[\"error\"]:\n            log.error(error_format.format(**result))\n            sys.exit(1)\n\n    log.info(\"Publish finished.\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.publish_plugins_discover","title":"<code>publish_plugins_discover(paths=None)</code>","text":"<p>Find and return available pyblish plug-ins.</p> <p>Overridden function from <code>pyblish</code> module to be able to collect     crashed files and reason of their crash.</p> <p>Parameters:</p> Name Type Description Default <code>paths</code> <code>list</code> <p>Paths to discover plug-ins from. If no paths are provided, all paths are searched.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def publish_plugins_discover(\n        paths: Optional[list[str]] = None) -&gt; DiscoverResult:\n    \"\"\"Find and return available pyblish plug-ins.\n\n    Overridden function from `pyblish` module to be able to collect\n        crashed files and reason of their crash.\n\n    Arguments:\n        paths (list, optional): Paths to discover plug-ins from.\n            If no paths are provided, all paths are searched.\n    \"\"\"\n\n    # The only difference with `pyblish.api.discover`\n    result = DiscoverResult(pyblish.api.Plugin)\n\n    plugins = {}\n    plugin_names = []\n\n    allow_duplicates = pyblish.plugin.ALLOW_DUPLICATES\n    log = pyblish.plugin.log\n\n    # Include plug-ins from registered paths\n    if not paths:\n        paths = pyblish.plugin.plugin_paths()\n\n    for path in paths:\n        path = os.path.normpath(path)\n        filenames = []\n        if os.path.isdir(path):\n            filenames.extend(\n                name\n                for name in os.listdir(path)\n                if (\n                    os.path.isfile(os.path.join(path, name))\n                    and not name.startswith(\"_\")\n                )\n            )\n        else:\n            filenames.append(os.path.basename(path))\n            path = os.path.dirname(path)\n\n        dirpath_hash = hashlib.md5(path.encode(\"utf-8\")).hexdigest()\n        for filename in filenames:\n            basename, ext = os.path.splitext(filename)\n            if ext.lower() != \".py\":\n                continue\n\n            filepath = os.path.join(path, filename)\n            module_name = f\"{dirpath_hash}.{basename}\"\n            try:\n                module = import_filepath(\n                    filepath, module_name, sys_module_name=module_name\n                )\n\n            except Exception as err:  # noqa: BLE001\n                # we need broad exception to catch all possible errors.\n                result.crashed_file_paths[filepath] = sys.exc_info()\n\n                log.debug('Skipped: \"%s\" (%s)', filepath, err)\n                continue\n\n            for plugin in pyblish.plugin.plugins_from_module(module):\n                # Ignore base plugin classes\n                # NOTE 'pyblish.api.discover' does not ignore them!\n                if (\n                    plugin is pyblish.api.Plugin\n                    or plugin is pyblish.api.ContextPlugin\n                    or plugin is pyblish.api.InstancePlugin\n                ):\n                    continue\n                if not allow_duplicates and plugin.__name__ in plugin_names:\n                    result.duplicated_plugins.append(plugin)\n                    log.debug(\"Duplicate plug-in found: %s\", plugin)\n                    continue\n\n                plugin_names.append(plugin.__name__)\n                plugin.__file__ = module.__file__\n                key = f\"{module.__file__}.{plugin.__name__}\"\n                plugins[key] = plugin\n\n    # Include plug-ins from registration.\n    # Directly registered plug-ins take precedence.\n    for plugin in pyblish.plugin.registered_plugins():\n        if not allow_duplicates and plugin.__name__ in plugin_names:\n            result.duplicated_plugins.append(plugin)\n            log.debug(\"Duplicate plug-in found: %s\", plugin)\n            continue\n\n        plugin_names.append(plugin.__name__)\n\n        plugins[plugin.__name__] = plugin\n\n    plugins = list(plugins.values())\n    pyblish.plugin.sort(plugins)  # In-place\n\n    # In-place user-defined filter\n    for filter_ in pyblish.plugin._registered_plugin_filters:\n        filter_(plugins)\n\n    result.plugins = plugins\n\n    return result\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/index.html#client.ayon_core.pipeline.publish.set_trait_representations","title":"<code>set_trait_representations(instance, representations)</code>","text":"<p>Set trait representations to instance.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>Instance</code> <p>Instance to set trait representations to.</p> required <code>representations</code> <code>list[Representation]</code> <p>List of trait based representations.</p> required Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def set_trait_representations(\n        instance: pyblish.api.Instance,\n        representations: list[Representation]\n) -&gt; None:\n    \"\"\"Set trait representations to instance.\n\n    Args:\n        instance (pyblish.api.Instance): Instance to set trait\n            representations to.\n        representations (list[Representation]): List of trait\n            based representations.\n\n    \"\"\"\n    instance.data[TRAIT_INSTANCE_KEY] = representations\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/abstract_collect_render.html","title":"abstract_collect_render","text":"<p>Collect render template.</p> <p>TODO: use @dataclass when times come.</p>"},{"location":"autoapi/client/ayon_core/pipeline/publish/abstract_collect_render.html#client.ayon_core.pipeline.publish.abstract_collect_render.AbstractCollectRender","title":"<code>AbstractCollectRender</code>","text":"<p>               Bases: <code>ContextPlugin</code></p> <p>Gather all publishable render layers from renderSetup.</p> Source code in <code>client/ayon_core/pipeline/publish/abstract_collect_render.py</code> <pre><code>class AbstractCollectRender(\n    pyblish.api.ContextPlugin, metaclass=AbstractMetaContextPlugin\n):\n    \"\"\"Gather all publishable render layers from renderSetup.\"\"\"\n\n    order = pyblish.api.CollectorOrder + 0.01\n    label = \"Collect Render\"\n    sync_workfile_version = False\n\n    def __init__(self, *args, **kwargs):\n        \"\"\"Constructor.\"\"\"\n        super(AbstractCollectRender, self).__init__(*args, **kwargs)\n        self._file_path = None\n        self._context = None\n\n    def process(self, context):\n        \"\"\"Entry point to collector.\"\"\"\n        self._context = context\n        for instance in context:\n            # make sure workfile instance publishing is enabled\n            try:\n                if \"workfile\" in instance.data[\"families\"]:\n                    instance.data[\"publish\"] = True\n                # TODO merge renderFarm and render.farm\n                if (\"renderFarm\" in instance.data[\"families\"] or\n                        \"render.farm\" in instance.data[\"families\"]):\n                    instance.data[\"remove\"] = True\n            except KeyError:\n                # be tolerant if 'families' is missing.\n                pass\n\n        self._file_path = context.data[\"currentFile\"].replace(\"\\\\\", \"/\")\n\n        render_instances = self.get_instances(context)\n        for render_instance in render_instances:\n            exp_files = self.get_expected_files(render_instance)\n            assert exp_files, \"no file names were generated, this is bug\"\n\n            # if we want to attach render to product, check if we have AOV's\n            # in expectedFiles. If so, raise error as we cannot attach AOV\n            # (considered to be product on its own) to another product\n            if render_instance.attachTo:\n                assert isinstance(exp_files, list), (\n                    \"attaching multiple AOVs or renderable cameras to \"\n                    \"product is not supported\"\n                )\n\n            frame_start_render = int(render_instance.frameStart)\n            frame_end_render = int(render_instance.frameEnd)\n            # TODO: Refactor hacky frame range workaround below\n            if (render_instance.ignoreFrameHandleCheck or\n                    int(context.data['frameStartHandle']) == frame_start_render\n                    and int(context.data['frameEndHandle']) == frame_end_render):  # noqa: W503, E501\n                # only for Harmony where frame range cannot be set by DB\n                handle_start = context.data['handleStart']\n                handle_end = context.data['handleEnd']\n                frame_start = context.data['frameStart']\n                frame_end = context.data['frameEnd']\n                frame_start_handle = context.data['frameStartHandle']\n                frame_end_handle = context.data['frameEndHandle']\n            elif (hasattr(render_instance, \"frameStartHandle\")\n                  and hasattr(render_instance, \"frameEndHandle\")):\n                handle_start = int(render_instance.handleStart)\n                handle_end = int(render_instance.handleEnd)\n                frame_start = int(render_instance.frameStart)\n                frame_end = int(render_instance.frameEnd)\n                frame_start_handle = int(render_instance.frameStartHandle)\n                frame_end_handle = int(render_instance.frameEndHandle)\n            else:\n                handle_start = 0\n                handle_end = 0\n                frame_start = frame_start_render\n                frame_end = frame_end_render\n                frame_start_handle = frame_start_render\n                frame_end_handle = frame_end_render\n\n            data = {\n                \"handleStart\": handle_start,\n                \"handleEnd\": handle_end,\n                \"frameStart\": frame_start,\n                \"frameEnd\": frame_end,\n                \"frameStartHandle\": frame_start_handle,\n                \"frameEndHandle\": frame_end_handle,\n                \"byFrameStep\": int(render_instance.frameStep),\n\n                \"author\": context.data[\"user\"],\n                # Add source to allow tracing back to the scene from\n                # which was submitted originally\n                \"expectedFiles\": exp_files,\n            }\n            if self.sync_workfile_version:\n                data[\"version\"] = context.data[\"version\"]\n\n            # add additional data\n            data = self.add_additional_data(data)\n\n            instance = render_instance.source_instance\n            if instance is None:\n                instance = context.create_instance(render_instance.name)\n\n            render_instance_dict = attr.asdict(render_instance)\n            instance.data.update(render_instance_dict)\n            instance.data.update(data)\n\n        self.post_collecting_action()\n\n    @abstractmethod\n    def get_instances(self, context):\n        \"\"\"Get all renderable instances and their data.\n\n        Args:\n            context (pyblish.api.Context): Context object.\n\n        Returns:\n            list of :class:`RenderInstance`: All collected renderable instances\n                (like render layers, write nodes, etc.)\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_expected_files(self, render_instance):\n        \"\"\"Get list of expected files.\n\n        Returns:\n            list: expected files. This can be either simple list of files with\n                their paths, or list of dictionaries, where key is name of AOV\n                for example and value is list of files for that AOV.\n\n        Example::\n\n            ['/path/to/file.001.exr', '/path/to/file.002.exr']\n\n            or as dictionary:\n\n            [\n                {\n                    \"beauty\": ['/path/to/beauty.001.exr', ...],\n                    \"mask\": ['/path/to/mask.001.exr']\n                }\n            ]\n\n        \"\"\"\n        pass\n\n    def add_additional_data(self, data):\n        \"\"\"Add additional data to collected instance.\n\n        This can be overridden by host implementation to add custom\n        additional data.\n\n        \"\"\"\n        return data\n\n    def post_collecting_action(self):\n        \"\"\"Execute some code after collection is done.\n\n        This is useful for example for restoring current render layer.\n\n        \"\"\"\n        pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/abstract_collect_render.html#client.ayon_core.pipeline.publish.abstract_collect_render.AbstractCollectRender.__init__","title":"<code>__init__(*args, **kwargs)</code>","text":"<p>Constructor.</p> Source code in <code>client/ayon_core/pipeline/publish/abstract_collect_render.py</code> <pre><code>def __init__(self, *args, **kwargs):\n    \"\"\"Constructor.\"\"\"\n    super(AbstractCollectRender, self).__init__(*args, **kwargs)\n    self._file_path = None\n    self._context = None\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/abstract_collect_render.html#client.ayon_core.pipeline.publish.abstract_collect_render.AbstractCollectRender.add_additional_data","title":"<code>add_additional_data(data)</code>","text":"<p>Add additional data to collected instance.</p> <p>This can be overridden by host implementation to add custom additional data.</p> Source code in <code>client/ayon_core/pipeline/publish/abstract_collect_render.py</code> <pre><code>def add_additional_data(self, data):\n    \"\"\"Add additional data to collected instance.\n\n    This can be overridden by host implementation to add custom\n    additional data.\n\n    \"\"\"\n    return data\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/abstract_collect_render.html#client.ayon_core.pipeline.publish.abstract_collect_render.AbstractCollectRender.get_expected_files","title":"<code>get_expected_files(render_instance)</code>  <code>abstractmethod</code>","text":"<p>Get list of expected files.</p> <p>Returns:</p> Name Type Description <code>list</code> <p>expected files. This can be either simple list of files with their paths, or list of dictionaries, where key is name of AOV for example and value is list of files for that AOV.</p> <p>Example::</p> <pre><code>['/path/to/file.001.exr', '/path/to/file.002.exr']\n\nor as dictionary:\n\n[\n    {\n        \"beauty\": ['/path/to/beauty.001.exr', ...],\n        \"mask\": ['/path/to/mask.001.exr']\n    }\n]\n</code></pre> Source code in <code>client/ayon_core/pipeline/publish/abstract_collect_render.py</code> <pre><code>@abstractmethod\ndef get_expected_files(self, render_instance):\n    \"\"\"Get list of expected files.\n\n    Returns:\n        list: expected files. This can be either simple list of files with\n            their paths, or list of dictionaries, where key is name of AOV\n            for example and value is list of files for that AOV.\n\n    Example::\n\n        ['/path/to/file.001.exr', '/path/to/file.002.exr']\n\n        or as dictionary:\n\n        [\n            {\n                \"beauty\": ['/path/to/beauty.001.exr', ...],\n                \"mask\": ['/path/to/mask.001.exr']\n            }\n        ]\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/abstract_collect_render.html#client.ayon_core.pipeline.publish.abstract_collect_render.AbstractCollectRender.get_instances","title":"<code>get_instances(context)</code>  <code>abstractmethod</code>","text":"<p>Get all renderable instances and their data.</p> <p>Parameters:</p> Name Type Description Default <code>context</code> <code>Context</code> <p>Context object.</p> required <p>Returns:</p> Type Description <p>list of :class:<code>RenderInstance</code>: All collected renderable instances (like render layers, write nodes, etc.)</p> Source code in <code>client/ayon_core/pipeline/publish/abstract_collect_render.py</code> <pre><code>@abstractmethod\ndef get_instances(self, context):\n    \"\"\"Get all renderable instances and their data.\n\n    Args:\n        context (pyblish.api.Context): Context object.\n\n    Returns:\n        list of :class:`RenderInstance`: All collected renderable instances\n            (like render layers, write nodes, etc.)\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/abstract_collect_render.html#client.ayon_core.pipeline.publish.abstract_collect_render.AbstractCollectRender.post_collecting_action","title":"<code>post_collecting_action()</code>","text":"<p>Execute some code after collection is done.</p> <p>This is useful for example for restoring current render layer.</p> Source code in <code>client/ayon_core/pipeline/publish/abstract_collect_render.py</code> <pre><code>def post_collecting_action(self):\n    \"\"\"Execute some code after collection is done.\n\n    This is useful for example for restoring current render layer.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/abstract_collect_render.html#client.ayon_core.pipeline.publish.abstract_collect_render.AbstractCollectRender.process","title":"<code>process(context)</code>","text":"<p>Entry point to collector.</p> Source code in <code>client/ayon_core/pipeline/publish/abstract_collect_render.py</code> <pre><code>def process(self, context):\n    \"\"\"Entry point to collector.\"\"\"\n    self._context = context\n    for instance in context:\n        # make sure workfile instance publishing is enabled\n        try:\n            if \"workfile\" in instance.data[\"families\"]:\n                instance.data[\"publish\"] = True\n            # TODO merge renderFarm and render.farm\n            if (\"renderFarm\" in instance.data[\"families\"] or\n                    \"render.farm\" in instance.data[\"families\"]):\n                instance.data[\"remove\"] = True\n        except KeyError:\n            # be tolerant if 'families' is missing.\n            pass\n\n    self._file_path = context.data[\"currentFile\"].replace(\"\\\\\", \"/\")\n\n    render_instances = self.get_instances(context)\n    for render_instance in render_instances:\n        exp_files = self.get_expected_files(render_instance)\n        assert exp_files, \"no file names were generated, this is bug\"\n\n        # if we want to attach render to product, check if we have AOV's\n        # in expectedFiles. If so, raise error as we cannot attach AOV\n        # (considered to be product on its own) to another product\n        if render_instance.attachTo:\n            assert isinstance(exp_files, list), (\n                \"attaching multiple AOVs or renderable cameras to \"\n                \"product is not supported\"\n            )\n\n        frame_start_render = int(render_instance.frameStart)\n        frame_end_render = int(render_instance.frameEnd)\n        # TODO: Refactor hacky frame range workaround below\n        if (render_instance.ignoreFrameHandleCheck or\n                int(context.data['frameStartHandle']) == frame_start_render\n                and int(context.data['frameEndHandle']) == frame_end_render):  # noqa: W503, E501\n            # only for Harmony where frame range cannot be set by DB\n            handle_start = context.data['handleStart']\n            handle_end = context.data['handleEnd']\n            frame_start = context.data['frameStart']\n            frame_end = context.data['frameEnd']\n            frame_start_handle = context.data['frameStartHandle']\n            frame_end_handle = context.data['frameEndHandle']\n        elif (hasattr(render_instance, \"frameStartHandle\")\n              and hasattr(render_instance, \"frameEndHandle\")):\n            handle_start = int(render_instance.handleStart)\n            handle_end = int(render_instance.handleEnd)\n            frame_start = int(render_instance.frameStart)\n            frame_end = int(render_instance.frameEnd)\n            frame_start_handle = int(render_instance.frameStartHandle)\n            frame_end_handle = int(render_instance.frameEndHandle)\n        else:\n            handle_start = 0\n            handle_end = 0\n            frame_start = frame_start_render\n            frame_end = frame_end_render\n            frame_start_handle = frame_start_render\n            frame_end_handle = frame_end_render\n\n        data = {\n            \"handleStart\": handle_start,\n            \"handleEnd\": handle_end,\n            \"frameStart\": frame_start,\n            \"frameEnd\": frame_end,\n            \"frameStartHandle\": frame_start_handle,\n            \"frameEndHandle\": frame_end_handle,\n            \"byFrameStep\": int(render_instance.frameStep),\n\n            \"author\": context.data[\"user\"],\n            # Add source to allow tracing back to the scene from\n            # which was submitted originally\n            \"expectedFiles\": exp_files,\n        }\n        if self.sync_workfile_version:\n            data[\"version\"] = context.data[\"version\"]\n\n        # add additional data\n        data = self.add_additional_data(data)\n\n        instance = render_instance.source_instance\n        if instance is None:\n            instance = context.create_instance(render_instance.name)\n\n        render_instance_dict = attr.asdict(render_instance)\n        instance.data.update(render_instance_dict)\n        instance.data.update(data)\n\n    self.post_collecting_action()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/abstract_collect_render.html#client.ayon_core.pipeline.publish.abstract_collect_render.RenderInstance","title":"<code>RenderInstance</code>","text":"<p>               Bases: <code>object</code></p> <p>Data collected by collectors.</p> <p>This data class later on passed to collected instances. Those attributes are required later on.</p> Source code in <code>client/ayon_core/pipeline/publish/abstract_collect_render.py</code> <pre><code>@attr.s\nclass RenderInstance(object):\n    \"\"\"Data collected by collectors.\n\n    This data class later on passed to collected instances.\n    Those attributes are required later on.\n\n    \"\"\"\n\n    # metadata\n    version = attr.ib()  # instance version\n    time = attr.ib()  # time of instance creation (get_formatted_current_time)\n    source = attr.ib()  # path to source scene file\n    label = attr.ib()  # label to show in GUI\n    family = attr.ib()  # product base type used for pyblish filtering\n    productType = attr.ib()  # product type\n    productName = attr.ib()  # product name\n    folderPath = attr.ib()  # folder path\n    task = attr.ib()  # task name\n    attachTo = attr.ib()  # product name to attach render to\n    setMembers = attr.ib()  # list of nodes/members producing render output\n    publish = attr.ib()  # bool, True to publish instance\n    name = attr.ib()  # instance name\n\n    # format settings\n    resolutionWidth = attr.ib()  # resolution width (1920)\n    resolutionHeight = attr.ib()  # resolution height (1080)\n    pixelAspect = attr.ib()  # pixel aspect (1.0)\n\n    # time settings\n    frameStart = attr.ib()  # start frame\n    frameEnd = attr.ib()  # start end\n    frameStep = attr.ib()  # frame step\n\n    productBaseType = attr.ib(default=None)  # product base type\n\n    handleStart = attr.ib(default=None)  # start frame\n    handleEnd = attr.ib(default=None)  # start frame\n\n    # for software (like Harmony) where frame range cannot be set by DB\n    # handles need to be propagated if exist\n    ignoreFrameHandleCheck = attr.ib(default=False)\n\n    # --------------------\n    # With default values\n    # metadata\n    renderer = attr.ib(default=\"\")  # renderer - can be used in Deadline\n    review = attr.ib(default=None)  # False - explicitly skip review\n    priority = attr.ib(default=50)  # job priority on farm\n\n    # family = attr.ib(default=\"renderlayer\")\n    families = attr.ib(default=[\"renderlayer\"])  # list of families\n    # True if should be rendered on farm, eg not integrate\n    farm = attr.ib(default=False)\n\n    # format settings\n    multipartExr = attr.ib(default=False)  # flag for multipart exrs\n    convertToScanline = attr.ib(default=False)  # flag for exr conversion\n\n    tileRendering = attr.ib(default=False)  # bool: treat render as tiles\n    tilesX = attr.ib(default=0)  # number of tiles in X\n    tilesY = attr.ib(default=0)  # number of tiles in Y\n\n    # submit_publish_job\n    deadlineSubmissionJob = attr.ib(default=None)\n    anatomyData = attr.ib(default=None)\n    outputDir = attr.ib(default=None)\n    context = attr.ib(default=None)\n    deadline = attr.ib(default=None)\n\n    # The source instance the data of this render instance should merge into\n    source_instance = attr.ib(default=None, type=pyblish.api.Instance)\n\n    @frameStart.validator\n    def check_frame_start(self, _, value):\n        \"\"\"Validate if frame start is not larger then end.\"\"\"\n        if value &gt; self.frameEnd:\n            raise ValueError(\"frameStart must be smaller \"\n                             \"or equal then frameEnd\")\n\n    @frameEnd.validator\n    def check_frame_end(self, _, value):\n        \"\"\"Validate if frame end is not less then start.\"\"\"\n        if value &lt; self.frameStart:\n            raise ValueError(\"frameEnd must be smaller \"\n                             \"or equal then frameStart\")\n\n    @tilesX.validator\n    def check_tiles_x(self, _, value):\n        \"\"\"Validate if tile x isn't less then 1.\"\"\"\n        if not self.tileRendering:\n            return\n        if value &lt; 1:\n            raise ValueError(\"tile X size cannot be less then 1\")\n\n        if value == 1 and self.tilesY == 1:\n            raise ValueError(\"both tiles X a Y sizes are set to 1\")\n\n    @tilesY.validator\n    def check_tiles_y(self, _, value):\n        \"\"\"Validate if tile y isn't less then 1.\"\"\"\n        if not self.tileRendering:\n            return\n        if value &lt; 1:\n            raise ValueError(\"tile Y size cannot be less then 1\")\n\n        if value == 1 and self.tilesX == 1:\n            raise ValueError(\"both tiles X a Y sizes are set to 1\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/abstract_collect_render.html#client.ayon_core.pipeline.publish.abstract_collect_render.RenderInstance.check_frame_end","title":"<code>check_frame_end(_, value)</code>","text":"<p>Validate if frame end is not less then start.</p> Source code in <code>client/ayon_core/pipeline/publish/abstract_collect_render.py</code> <pre><code>@frameEnd.validator\ndef check_frame_end(self, _, value):\n    \"\"\"Validate if frame end is not less then start.\"\"\"\n    if value &lt; self.frameStart:\n        raise ValueError(\"frameEnd must be smaller \"\n                         \"or equal then frameStart\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/abstract_collect_render.html#client.ayon_core.pipeline.publish.abstract_collect_render.RenderInstance.check_frame_start","title":"<code>check_frame_start(_, value)</code>","text":"<p>Validate if frame start is not larger then end.</p> Source code in <code>client/ayon_core/pipeline/publish/abstract_collect_render.py</code> <pre><code>@frameStart.validator\ndef check_frame_start(self, _, value):\n    \"\"\"Validate if frame start is not larger then end.\"\"\"\n    if value &gt; self.frameEnd:\n        raise ValueError(\"frameStart must be smaller \"\n                         \"or equal then frameEnd\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/abstract_collect_render.html#client.ayon_core.pipeline.publish.abstract_collect_render.RenderInstance.check_tiles_x","title":"<code>check_tiles_x(_, value)</code>","text":"<p>Validate if tile x isn't less then 1.</p> Source code in <code>client/ayon_core/pipeline/publish/abstract_collect_render.py</code> <pre><code>@tilesX.validator\ndef check_tiles_x(self, _, value):\n    \"\"\"Validate if tile x isn't less then 1.\"\"\"\n    if not self.tileRendering:\n        return\n    if value &lt; 1:\n        raise ValueError(\"tile X size cannot be less then 1\")\n\n    if value == 1 and self.tilesY == 1:\n        raise ValueError(\"both tiles X a Y sizes are set to 1\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/abstract_collect_render.html#client.ayon_core.pipeline.publish.abstract_collect_render.RenderInstance.check_tiles_y","title":"<code>check_tiles_y(_, value)</code>","text":"<p>Validate if tile y isn't less then 1.</p> Source code in <code>client/ayon_core/pipeline/publish/abstract_collect_render.py</code> <pre><code>@tilesY.validator\ndef check_tiles_y(self, _, value):\n    \"\"\"Validate if tile y isn't less then 1.\"\"\"\n    if not self.tileRendering:\n        return\n    if value &lt; 1:\n        raise ValueError(\"tile Y size cannot be less then 1\")\n\n    if value == 1 and self.tilesX == 1:\n        raise ValueError(\"both tiles X a Y sizes are set to 1\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/abstract_expected_files.html","title":"abstract_expected_files","text":"<p>Abstract ExpectedFile class definition.</p>"},{"location":"autoapi/client/ayon_core/pipeline/publish/abstract_expected_files.html#client.ayon_core.pipeline.publish.abstract_expected_files.ExpectedFiles","title":"<code>ExpectedFiles</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Class grouping functionality for all supported renderers.</p> <p>Attributes:</p> Name Type Description <code>multipart</code> <code>bool</code> <p>Flag if multipart exrs are used.</p> Source code in <code>client/ayon_core/pipeline/publish/abstract_expected_files.py</code> <pre><code>class ExpectedFiles(ABC):\n    \"\"\"Class grouping functionality for all supported renderers.\n\n    Attributes:\n        multipart (bool): Flag if multipart exrs are used.\n\n    \"\"\"\n\n    multipart = False\n\n    @abstractmethod\n    def get(self, render_instance):\n        \"\"\"Get expected files for given renderer and render layer.\n\n        This method should return dictionary of all files we are expecting\n        to be rendered from the host. Usually `render_instance` corresponds\n        to *render layer*. Result can be either flat list with the file\n        paths or it can be list of dictionaries. Each key corresponds to\n        for example AOV name or channel, etc.\n\n        Example::\n\n            ['/path/to/file.001.exr', '/path/to/file.002.exr']\n\n            or as dictionary:\n\n            [\n                {\n                    \"beauty\": ['/path/to/beauty.001.exr', ...],\n                    \"mask\": ['/path/to/mask.001.exr']\n                }\n            ]\n\n\n        Args:\n            render_instance (:class:`RenderInstance`): Data passed from\n                collector to determine files. This should be instance of\n                :class:`abstract_collect_render.RenderInstance`\n\n        Returns:\n            list: Full paths to expected rendered files.\n            list of dict: Path to expected rendered files categorized by\n                AOVs, etc.\n\n        \"\"\"\n        raise NotImplementedError()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/abstract_expected_files.html#client.ayon_core.pipeline.publish.abstract_expected_files.ExpectedFiles.get","title":"<code>get(render_instance)</code>  <code>abstractmethod</code>","text":"<p>Get expected files for given renderer and render layer.</p> <p>This method should return dictionary of all files we are expecting to be rendered from the host. Usually <code>render_instance</code> corresponds to render layer. Result can be either flat list with the file paths or it can be list of dictionaries. Each key corresponds to for example AOV name or channel, etc.</p> <p>Example::</p> <pre><code>['/path/to/file.001.exr', '/path/to/file.002.exr']\n\nor as dictionary:\n\n[\n    {\n        \"beauty\": ['/path/to/beauty.001.exr', ...],\n        \"mask\": ['/path/to/mask.001.exr']\n    }\n]\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>render_instance (</code> <p>class:<code>RenderInstance</code>): Data passed from collector to determine files. This should be instance of :class:<code>abstract_collect_render.RenderInstance</code></p> required <p>Returns:</p> Name Type Description <code>list</code> <p>Full paths to expected rendered files.</p> <p>list of dict: Path to expected rendered files categorized by AOVs, etc.</p> Source code in <code>client/ayon_core/pipeline/publish/abstract_expected_files.py</code> <pre><code>@abstractmethod\ndef get(self, render_instance):\n    \"\"\"Get expected files for given renderer and render layer.\n\n    This method should return dictionary of all files we are expecting\n    to be rendered from the host. Usually `render_instance` corresponds\n    to *render layer*. Result can be either flat list with the file\n    paths or it can be list of dictionaries. Each key corresponds to\n    for example AOV name or channel, etc.\n\n    Example::\n\n        ['/path/to/file.001.exr', '/path/to/file.002.exr']\n\n        or as dictionary:\n\n        [\n            {\n                \"beauty\": ['/path/to/beauty.001.exr', ...],\n                \"mask\": ['/path/to/mask.001.exr']\n            }\n        ]\n\n\n    Args:\n        render_instance (:class:`RenderInstance`): Data passed from\n            collector to determine files. This should be instance of\n            :class:`abstract_collect_render.RenderInstance`\n\n    Returns:\n        list: Full paths to expected rendered files.\n        list of dict: Path to expected rendered files categorized by\n            AOVs, etc.\n\n    \"\"\"\n    raise NotImplementedError()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/constants.html","title":"constants","text":""},{"location":"autoapi/client/ayon_core/pipeline/publish/input_versions.html","title":"input_versions","text":""},{"location":"autoapi/client/ayon_core/pipeline/publish/input_versions.html#client.ayon_core.pipeline.publish.input_versions.InputVersion","title":"<code>InputVersion</code>  <code>dataclass</code>","text":"<p>Input version used for dependency links.</p> <p>The version id always points to the active real version. When the source was a hero version, the original hero metadata is stored separately so it can be persisted to farm metadata JSON and link data.</p> Source code in <code>client/ayon_core/pipeline/publish/input_versions.py</code> <pre><code>@dataclass(frozen=True)\nclass InputVersion:\n    \"\"\"Input version used for dependency links.\n\n    The version id always points to the active real version.\n    When the source was a hero version, the original hero metadata is stored\n    separately so it can be persisted to farm metadata JSON and link data.\n    \"\"\"\n\n    version_id: str\n    hero: bool = False\n    hero_version_id: Optional[str] = None\n\n    @property\n    def data(self) -&gt; dict[str, Any]:\n        output = {}\n        if self.hero:\n            output[\"hero\"] = True\n        if self.hero_version_id:\n            output[\"hero_version_id\"] = self.hero_version_id\n        return output\n\n    def to_dict(self) -&gt; SerializedInputVersion:\n        return {\n            \"version_id\": self.version_id,\n            \"data\": self.data,\n        }\n\n    @classmethod\n    def from_value(\n        cls,\n        value: Union[\"InputVersion\", SerializedInputVersion, str]\n    ) -&gt; \"InputVersion\":\n        if isinstance(value, cls):\n            return value\n\n        if isinstance(value, str):\n            return cls(version_id=str(value))\n\n        if isinstance(value, dict):\n            version_id = value.get(\"version_id\")\n            if not version_id:\n                raise ValueError(\n                    \"Serialized input version is missing 'version_id'.\"\n                )\n\n            data: dict = value.get(\"data\") or {}\n            hero: bool = data.get(\"hero\", False)\n            hero_version_id: Optional[str] = data.get(\"hero_version_id\")\n            return cls(\n                version_id=str(version_id),\n                hero=hero,\n                hero_version_id=hero_version_id,\n            )\n\n        raise TypeError(\n            \"Must have InputVersion, str or dict type.\"\n            f\" Got: {value} ({type(value)})\"\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/lib.html","title":"lib","text":"<p>Library functions for publishing.</p>"},{"location":"autoapi/client/ayon_core/pipeline/publish/lib.html#client.ayon_core.pipeline.publish.lib.IntegrationTemplateItem","title":"<code>IntegrationTemplateItem</code>","text":"<p>Represents single template item.</p> <p>Template path, template data that was used in the template.</p> <p>Attributes:</p> Name Type Description <code>anatomy</code> <code>Anatomy</code> <p>Anatomy object.</p> <code>template_data</code> <code>dict[str, Any]</code> <p>Template data.</p> <code>template_object</code> <code>TemplateItem</code> <p>Template object</p> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>class IntegrationTemplateItem:\n    \"\"\"Represents single template item.\n\n    Template path, template data that was used in the template.\n\n    Attributes:\n        anatomy (Anatomy): Anatomy object.\n        template_data (dict[str, Any]): Template data.\n        template_object (AnatomyTemplateItem): Template object\n    \"\"\"\n    anatomy: Anatomy\n    template_data: dict[str, Any]\n    template_object: \"AnatomyTemplateItem\"\n\n    def __init__(self,\n        anatomy: \"Anatomy\",\n        template_data: dict[str, Any],\n        template_object: \"AnatomyTemplateItem\"):\n        \"\"\"Initialize TemplateItem.\n\n        Args:\n            anatomy (Anatomy): Anatomy object.\n            template_data (dict[str, Any]): Template data.\n            template_object (AnatomyTemplateItem): Template object.\n\n        \"\"\"\n        self.anatomy = anatomy\n        self.template_data = template_data\n        self.template_object = template_object\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/lib.html#client.ayon_core.pipeline.publish.lib.IntegrationTemplateItem.__init__","title":"<code>__init__(anatomy, template_data, template_object)</code>","text":"<p>Initialize TemplateItem.</p> <p>Parameters:</p> Name Type Description Default <code>anatomy</code> <code>Anatomy</code> <p>Anatomy object.</p> required <code>template_data</code> <code>dict[str, Any]</code> <p>Template data.</p> required <code>template_object</code> <code>TemplateItem</code> <p>Template object.</p> required Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def __init__(self,\n    anatomy: \"Anatomy\",\n    template_data: dict[str, Any],\n    template_object: \"AnatomyTemplateItem\"):\n    \"\"\"Initialize TemplateItem.\n\n    Args:\n        anatomy (Anatomy): Anatomy object.\n        template_data (dict[str, Any]): Template data.\n        template_object (AnatomyTemplateItem): Template object.\n\n    \"\"\"\n    self.anatomy = anatomy\n    self.template_data = template_data\n    self.template_object = template_object\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/lib.html#client.ayon_core.pipeline.publish.lib.add_repre_files_for_cleanup","title":"<code>add_repre_files_for_cleanup(instance, repre)</code>","text":"<p>Explicitly mark repre files to be deleted.</p> <p>Should be used on intermediate files (eg. review, thumbnails) to be explicitly deleted.</p> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def add_repre_files_for_cleanup(instance, repre):\n    \"\"\" Explicitly mark repre files to be deleted.\n\n    Should be used on intermediate files (eg. review, thumbnails) to be\n    explicitly deleted.\n    \"\"\"\n    files = repre[\"files\"]\n    staging_dir = repre.get(\"stagingDir\")\n\n    # first make sure representation level is not persistent\n    if (\n        not staging_dir\n        or repre.get(\"stagingDir_persistent\")\n    ):\n        return\n\n    # then look into instance level if it's not persistent\n    if instance.data.get(\"stagingDir_persistent\"):\n        return\n\n    if isinstance(files, str):\n        files = [files]\n\n    for file_name in files:\n        expected_file = os.path.join(staging_dir, file_name)\n        instance.context.data[\"cleanupFullPaths\"].append(expected_file)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/lib.html#client.ayon_core.pipeline.publish.lib.add_trait_representations","title":"<code>add_trait_representations(instance, representations)</code>","text":"<p>Add trait representations to instance.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>Instance</code> <p>Instance to add trait representations to.</p> required <code>representations</code> <code>list[Representation]</code> <p>List of representation trait based representations to add.</p> required Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def add_trait_representations(\n        instance: pyblish.api.Instance,\n        representations: list[Representation]\n) -&gt; None:\n    \"\"\"Add trait representations to instance.\n\n    Args:\n        instance (pyblish.api.Instance): Instance to add trait\n            representations to.\n        representations (list[Representation]): List of representation\n            trait based representations to add.\n\n    \"\"\"\n    repres = instance.data.setdefault(TRAIT_INSTANCE_KEY, [])\n    repres.extend(representations)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/lib.html#client.ayon_core.pipeline.publish.lib.apply_plugin_settings_automatically","title":"<code>apply_plugin_settings_automatically(plugin, settings, logger=None)</code>","text":"<p>Automatically apply plugin settings to a plugin object.</p> Note <p>This function was created to be able to use it in custom overrides of     'apply_settings' class method.</p> <p>Parameters:</p> Name Type Description Default <code>plugin</code> <code>type[Plugin]</code> <p>Class of a plugin.</p> required <code>settings</code> <code>dict[str, Any]</code> <p>Plugin specific settings.</p> required <code>logger</code> <code>Optional[Logger]</code> <p>Logger to log debug messages about applied settings values.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def apply_plugin_settings_automatically(plugin, settings, logger=None):\n    \"\"\"Automatically apply plugin settings to a plugin object.\n\n    Note:\n        This function was created to be able to use it in custom overrides of\n            'apply_settings' class method.\n\n    Args:\n        plugin (type[pyblish.api.Plugin]): Class of a plugin.\n        settings (dict[str, Any]): Plugin specific settings.\n        logger (Optional[logging.Logger]): Logger to log debug messages about\n            applied settings values.\n    \"\"\"\n\n    for option, value in settings.items():\n        if logger:\n            logger.debug(\"Plugin %s - Attr: %s -&gt; %s\",\n                         plugin.__name__, option, value)\n        setattr(plugin, option, value)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/lib.html#client.ayon_core.pipeline.publish.lib.context_plugin_should_run","title":"<code>context_plugin_should_run(plugin, context)</code>","text":"<p>Return whether the ContextPlugin should run on the given context.</p> <p>This is a helper function to work around a bug pyblish-base#250 Whenever a ContextPlugin sets specific families it will still trigger even when no instances are present that have those families.</p> <p>This actually checks it correctly and returns whether it should run.</p> <p>Parameters:</p> Name Type Description Default <code>plugin</code> <code>Plugin</code> <p>Plugin with filters.</p> required <code>context</code> <code>Context</code> <p>Pyblish context with instances.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <p>Context plugin should run based on valid instances.</p> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def context_plugin_should_run(plugin, context):\n    \"\"\"Return whether the ContextPlugin should run on the given context.\n\n    This is a helper function to work around a bug pyblish-base#250\n    Whenever a ContextPlugin sets specific families it will still trigger even\n    when no instances are present that have those families.\n\n    This actually checks it correctly and returns whether it should run.\n\n    Args:\n        plugin (pyblish.api.Plugin): Plugin with filters.\n        context (pyblish.api.Context): Pyblish context with instances.\n\n    Returns:\n        bool: Context plugin should run based on valid instances.\n    \"\"\"\n\n    for _ in filter_instances_for_context_plugin(plugin, context):\n        return True\n    return False\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/lib.html#client.ayon_core.pipeline.publish.lib.fill_sequence_gaps_with_previous_version","title":"<code>fill_sequence_gaps_with_previous_version(collection, staging_dir, instance, current_repre_name, start_frame, end_frame)</code>","text":"<p>Tries to replace missing frames from ones from last version</p> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def fill_sequence_gaps_with_previous_version(\n    collection: str,\n    staging_dir: str,\n    instance: pyblish.plugin.Instance,\n    current_repre_name: str,\n    start_frame: int,\n    end_frame: int\n) -&gt; tuple[Optional[dict[str, Any]], Optional[dict[int, str]]]:\n    \"\"\"Tries to replace missing frames from ones from last version\"\"\"\n    used_version_entity, repre_file_paths = _get_last_version_files(\n        instance, current_repre_name\n    )\n    if repre_file_paths is None:\n        # issues in getting last version files\n        return (None, None)\n\n    prev_collection = clique.assemble(\n        repre_file_paths,\n        patterns=[clique.PATTERNS[\"frames\"]],\n        minimum_items=1\n    )[0][0]\n    prev_col_format = prev_collection.format(\"{head}{padding}{tail}\")\n\n    added_files = {}\n    anatomy = instance.context.data[\"anatomy\"]\n    col_format = collection.format(\"{head}{padding}{tail}\")\n    for frame in range(start_frame, end_frame + 1):\n        if frame in collection.indexes:\n            continue\n        hole_fpath = os.path.join(staging_dir, col_format % frame)\n\n        previous_version_path = prev_col_format % frame\n        previous_version_path = anatomy.fill_root(previous_version_path)\n        if not os.path.exists(previous_version_path):\n            log.warning(\n                \"Missing frame should be replaced from \"\n                f\"'{previous_version_path}' but that doesn't exist. \"\n            )\n            return (None, None)\n\n        log.warning(\n            f\"Replacing missing '{hole_fpath}' with \"\n            f\"'{previous_version_path}'\"\n        )\n        copyfile(previous_version_path, hole_fpath)\n        added_files[frame] = hole_fpath\n\n    return (used_version_entity, added_files)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/lib.html#client.ayon_core.pipeline.publish.lib.filter_crashed_publish_paths","title":"<code>filter_crashed_publish_paths(project_name, crashed_paths, *, project_settings=None)</code>","text":"<p>Filter crashed paths happened during plugins discovery.</p> <p>Check if plugins discovery has enabled strict mode and filter crashed     paths that happened during discover based on regexes from settings.</p> <p>Publishing should not start if any paths are returned.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name in which context plugins discovery happened.</p> required <code>crashed_paths</code> <code>set[str]</code> <p>Crashed paths from plugins discovery report.</p> required <code>project_settings</code> <code>Optional[dict[str, Any]]</code> <p>Project settings.</p> <code>None</code> <p>Returns:</p> Type Description <code>set[str]</code> <p>set[str]: Filtered crashed paths.</p> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def filter_crashed_publish_paths(\n    project_name: str,\n    crashed_paths: set[str],\n    *,\n    project_settings: Optional[dict[str, Any]] = None,\n) -&gt; set[str]:\n    \"\"\"Filter crashed paths happened during plugins discovery.\n\n    Check if plugins discovery has enabled strict mode and filter crashed\n        paths that happened during discover based on regexes from settings.\n\n    Publishing should not start if any paths are returned.\n\n    Args:\n        project_name (str): Project name in which context plugins discovery\n            happened.\n        crashed_paths (set[str]): Crashed paths from plugins discovery report.\n        project_settings (Optional[dict[str, Any]]): Project settings.\n\n    Returns:\n        set[str]: Filtered crashed paths.\n\n    \"\"\"\n    filtered_paths = set()\n    # Nothing crashed all good...\n    if not crashed_paths:\n        return filtered_paths\n\n    if project_settings is None:\n        project_settings = get_project_settings(project_name)\n\n    discover_validation = (\n        project_settings[\"core\"][\"tools\"][\"publish\"][\"discover_validation\"]\n    )\n    # Strict mode is not enabled.\n    if not discover_validation[\"enabled\"]:\n        return filtered_paths\n\n    regexes = [\n        re.compile(value, re.IGNORECASE)\n        for value in discover_validation[\"ignore_paths\"]\n        if value\n    ]\n    is_windows = platform.system().lower() == \"windows\"\n    # Fitler path with regexes from settings\n    for path in crashed_paths:\n        # Normalize paths to use forward slashes on windows\n        if is_windows:\n            path = path.replace(\"\\\\\", \"/\")\n        is_invalid = True\n        for regex in regexes:\n            if regex.match(path):\n                is_invalid = False\n                break\n\n        if is_invalid:\n            filtered_paths.add(path)\n\n    return filtered_paths\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/lib.html#client.ayon_core.pipeline.publish.lib.filter_instances_for_context_plugin","title":"<code>filter_instances_for_context_plugin(plugin, context)</code>","text":"<p>Filter instances on context by context plugin filters.</p> <p>This is for cases when context plugin need similar filtering like instance plugin have, but for some reason must run on context or should find out if there is at least one instance with a family.</p> <p>Parameters:</p> Name Type Description Default <code>plugin</code> <code>Plugin</code> <p>Plugin with filters.</p> required <code>context</code> <code>Context</code> <p>Pyblish context with instances.</p> required <p>Returns:</p> Type Description <p>Iterator[pyblish.lib.Instance]: Iteration of valid instances.</p> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def filter_instances_for_context_plugin(plugin, context):\n    \"\"\"Filter instances on context by context plugin filters.\n\n    This is for cases when context plugin need similar filtering like instance\n    plugin have, but for some reason must run on context or should find out\n    if there is at least one instance with a family.\n\n    Args:\n        plugin (pyblish.api.Plugin): Plugin with filters.\n        context (pyblish.api.Context): Pyblish context with instances.\n\n    Returns:\n        Iterator[pyblish.lib.Instance]: Iteration of valid instances.\n    \"\"\"\n\n    instances = []\n    plugin_families = set()\n    all_families = False\n    if plugin.families:\n        instances = context\n        plugin_families = set(plugin.families)\n        all_families = \"*\" in plugin_families\n\n    for instance in instances:\n        # Ignore inactive instances\n        if (\n            not instance.data.get(\"publish\", True)\n            or not instance.data.get(\"active\", True)\n        ):\n            continue\n\n        family = instance.data.get(\"family\")\n        families = instance.data.get(\"families\") or []\n        if (\n            all_families\n            or (family and family in plugin_families)\n            or any(f in plugin_families for f in families)\n        ):\n            yield instance\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/lib.html#client.ayon_core.pipeline.publish.lib.filter_pyblish_plugins","title":"<code>filter_pyblish_plugins(plugins)</code>","text":"<p>Pyblish plugin filter which applies AYON settings.</p> <p>Apply settings on discovered plugins. On plugin with implemented class method 'def apply_settings(cls, project_settings)' is called the method. Default behavior looks for plugin name and current host name to look for</p> <p>Parameters:</p> Name Type Description Default <code>plugins</code> <code>List[Plugin]</code> <p>Discovered plugins on which are applied settings.</p> required Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def filter_pyblish_plugins(plugins):\n    \"\"\"Pyblish plugin filter which applies AYON settings.\n\n    Apply settings on discovered plugins. On plugin with implemented\n    class method 'def apply_settings(cls, project_settings)'\n    is called the method. Default behavior looks for plugin name and current\n    host name to look for\n\n    Args:\n        plugins (List[pyblish.plugin.Plugin]): Discovered plugins on which\n            are applied settings.\n    \"\"\"\n\n    log = Logger.get_logger(\"filter_pyblish_plugins\")\n\n    # TODO: Don't use host from 'pyblish.api' but from defined host by us.\n    #   - kept because on farm is probably used host 'shell' which probably\n    #       affect how settings are applied there\n    host_name = pyblish.api.current_host()\n    project_name = os.environ.get(\"AYON_PROJECT_NAME\")\n\n    project_settings = get_project_settings(project_name)\n\n    # iterate over plugins\n    for plugin in plugins[:]:\n        # Apply settings to plugins\n\n        apply_settings_func = getattr(plugin, \"apply_settings\", None)\n        if apply_settings_func is not None:\n            # Use classmethod 'apply_settings'\n            # - can be used to target settings from custom settings place\n            # - skip default behavior when successful\n            try:\n                plugin.apply_settings(project_settings)\n\n            except Exception:\n                log.warning(\n                    (\n                        \"Failed to apply settings on plugin {}\"\n                    ).format(plugin.__name__),\n                    exc_info=True\n                )\n        else:\n            # Automated\n            plugin_settins = get_plugin_settings(\n                plugin, project_settings, log, host_name\n            )\n            apply_plugin_settings_automatically(plugin, plugin_settins, log)\n\n        # Remove disabled plugins\n        if getattr(plugin, \"enabled\", True) is False:\n            plugins.remove(plugin)\n\n        # Pyblish already operated a filter based on host.\n        # But applying settings might have changed \"hosts\"\n        # value in plugin so re-filter.\n        elif not pyblish.plugin.host_is_compatible(plugin):\n            plugins.remove(plugin)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/lib.html#client.ayon_core.pipeline.publish.lib.get_default_reviewable_layers","title":"<code>get_default_reviewable_layers(project_settings)</code>","text":"<p>Get default reviewable layers from project settings.</p> <p>Parameters:</p> Name Type Description Default <code>project_settings</code> <code>dict</code> <p>Project settings.</p> required <p>Returns:</p> Type Description <code>list[str]</code> <p>list[str]: List of default reviewable layers.</p> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def get_default_reviewable_layers(project_settings: dict) -&gt; list[str]:\n    \"\"\"Get default reviewable layers from project settings.\n\n    Args:\n        project_settings (dict): Project settings.\n\n    Returns:\n        list[str]: List of default reviewable layers.\n    \"\"\"\n    return project_settings[\"core\"][\"reviewable_layers\"][\"review_layers\"]\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/lib.html#client.ayon_core.pipeline.publish.lib.get_errored_instances_from_context","title":"<code>get_errored_instances_from_context(context, plugin=None)</code>","text":"<p>Collect failed instances from pyblish context.</p> <p>Parameters:</p> Name Type Description Default <code>context</code> <code>Context</code> <p>Publish context where we're looking for failed instances.</p> required <code>plugin</code> <code>Plugin</code> <p>If provided then only consider errors related to that plug-in.</p> <code>None</code> <p>Returns:</p> Type Description <p>List[pyblish.lib.Instance]: Instances which failed during processing.</p> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def get_errored_instances_from_context(context, plugin=None):\n    \"\"\"Collect failed instances from pyblish context.\n\n    Args:\n        context (pyblish.api.Context): Publish context where we're looking\n            for failed instances.\n        plugin (pyblish.api.Plugin): If provided then only consider errors\n            related to that plug-in.\n\n    Returns:\n        List[pyblish.lib.Instance]: Instances which failed during processing.\n    \"\"\"\n\n    instances = list()\n    for result in context.data[\"results\"]:\n        if result[\"instance\"] is None:\n            # When instance is None we are on the \"context\" result\n            continue\n\n        if plugin is not None and result.get(\"plugin\") != plugin:\n            continue\n\n        if result[\"error\"]:\n            instances.append(result[\"instance\"])\n\n    return instances\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/lib.html#client.ayon_core.pipeline.publish.lib.get_errored_plugins_from_context","title":"<code>get_errored_plugins_from_context(context)</code>","text":"<p>Collect failed plugins from pyblish context.</p> <p>Parameters:</p> Name Type Description Default <code>context</code> <code>Context</code> <p>Publish context where we're looking for failed plugins.</p> required <p>Returns:</p> Type Description <p>List[pyblish.api.Plugin]: Plugins which failed during processing.</p> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def get_errored_plugins_from_context(context):\n    \"\"\"Collect failed plugins from pyblish context.\n\n    Args:\n        context (pyblish.api.Context): Publish context where we're looking\n            for failed plugins.\n\n    Returns:\n        List[pyblish.api.Plugin]: Plugins which failed during processing.\n    \"\"\"\n\n    plugins = list()\n    results = context.data.get(\"results\", [])\n    for result in results:\n        if result[\"success\"] is True:\n            continue\n        plugins.append(result[\"plugin\"])\n\n    return plugins\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/lib.html#client.ayon_core.pipeline.publish.lib.get_hero_template_name_profiles","title":"<code>get_hero_template_name_profiles(project_name, project_settings=None)</code>","text":"<p>Receive profiles for hero publish template keys.</p> <p>At least one of arguments must be passed.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Name of project where to look for templates.</p> required <code>project_settings</code> <code>Dict[str, Any]</code> <p>Prepared project settings.</p> <code>None</code> <p>Returns:</p> Type Description <code>list[dict[str, Any]]</code> <p>list[dict[str, Any]]: Publish template profiles.</p> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def get_hero_template_name_profiles(\n    project_name: str,\n    project_settings: Optional[dict[str, Any]] = None,\n) -&gt; list[dict[str, Any]]:\n    \"\"\"Receive profiles for hero publish template keys.\n\n    At least one of arguments must be passed.\n\n    Args:\n        project_name (str): Name of project where to look for templates.\n        project_settings (Dict[str, Any]): Prepared project settings.\n\n    Returns:\n        list[dict[str, Any]]: Publish template profiles.\n\n    \"\"\"\n    if not project_name and not project_settings:\n        raise ValueError((\n            \"Both project name and project settings are missing.\"\n            \" At least one must be entered.\"\n        ))\n\n    if not project_settings:\n        project_settings = get_project_settings(project_name)\n\n    return copy.deepcopy(\n        project_settings\n        [\"core\"]\n        [\"tools\"]\n        [\"publish\"]\n        [\"hero_template_name_profiles\"]\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/lib.html#client.ayon_core.pipeline.publish.lib.get_instance_expected_output_path","title":"<code>get_instance_expected_output_path(instance, representation_name, ext, version=None)</code>","text":"<p>Return expected publish filepath for representation in instance</p> <p>This does not validate whether the instance has any representation by the given name, extension and/or version.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>Instance</code> <p>Publish instance</p> required <code>representation_name</code> <code>str</code> <p>Representation name</p> required <code>ext</code> <code>Union[str, None]</code> <p>Extension for the file. When None, the <code>ext</code> will be set to the representation name.</p> required <code>version</code> <code>Optional[int]</code> <p>If provided, force it to format to this particular version.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <p>Resolved path</p> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def get_instance_expected_output_path(\n        instance: pyblish.api.Instance,\n        representation_name: str,\n        ext: Union[str, None],\n        version: Optional[str] = None\n):\n    \"\"\"Return expected publish filepath for representation in instance\n\n    This does not validate whether the instance has any representation by the\n    given name, extension and/or version.\n\n    Arguments:\n        instance (pyblish.api.Instance): Publish instance\n        representation_name (str): Representation name\n        ext (Union[str, None]): Extension for the file.\n            When None, the `ext` will be set to the representation name.\n        version (Optional[int]): If provided, force it to format to this\n            particular version.\n\n    Returns:\n        str: Resolved path\n\n    \"\"\"\n\n    if ext is None:\n        ext = representation_name\n    if version is None:\n        version = instance.data[\"version\"]\n\n    context = instance.context\n    anatomy = context.data[\"anatomy\"]\n\n    template_data = copy.deepcopy(instance.data[\"anatomyData\"])\n    template_data.update({\n        \"ext\": ext,\n        \"representation\": representation_name,\n        \"variant\": instance.data.get(\"variant\"),\n        \"version\": version\n    })\n\n    # Get instance publish template name\n    task_name = task_type = None\n    task_entity = instance.data.get(\"taskEntity\")\n    if task_entity:\n        task_name = task_entity[\"name\"]\n        task_type = task_entity[\"taskType\"]\n\n    product_base_type = instance.data.get(\"productBaseType\")\n    if not product_base_type:\n        product_base_type = instance.data[\"productType\"]\n\n    template_name = get_publish_template_name(\n        project_name=instance.context.data[\"projectName\"],\n        host_name=instance.context.data[\"hostName\"],\n        product_base_type=product_base_type,\n        task_name=task_name,\n        task_type=task_type,\n        project_settings=instance.context.data[\"project_settings\"],\n    )\n\n    path_template_obj: AnatomyStringTemplate = anatomy.get_template_item(\n        \"publish\",\n        template_name\n    )[\"path\"]\n\n    # Define {originalBasename} template key which can be used in publish\n    # template to use to original filename.\n    if \"originalbasename\" in path_template_obj.template.lower():\n        repre = next(\n            (\n                repre for repre in instance.data.get(\"representations\", [])\n                if repre[\"name\"] == representation_name\n            ),\n            None\n        )\n        if not repre:\n            raise ValueError(\n                \"Unable to format 'originalBasename' for representation \"\n                f\"{representation_name} because representation is not found\"\n                \" on instance.\"\n            )\n\n        first_file = repre[\"files\"]\n        if isinstance(first_file, list):\n            first_file = first_file[0]\n\n        basename = os.path.splitext(first_file)[0]\n        template_data[\"originalBasename\"] = basename\n\n    template_filled = path_template_obj.format_strict(template_data)\n    return os.path.normpath(template_filled)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/lib.html#client.ayon_core.pipeline.publish.lib.get_instance_families","title":"<code>get_instance_families(instance)</code>","text":"<p>Get all families of the instance.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>Instance</code> <p>Instance to get families from.</p> required <p>Returns:</p> Type Description <code>list[str]</code> <p>list[str]: List of families.</p> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def get_instance_families(instance: pyblish.api.Instance) -&gt; list[str]:\n    \"\"\"Get all families of the instance.\n\n    Args:\n        instance (pyblish.api.Instance): Instance to get families from.\n\n    Returns:\n        list[str]: List of families.\n\n    \"\"\"\n    family = instance.data.get(\"family\")\n    families = []\n    if family:\n        families.append(family)\n\n    for _family in (instance.data.get(\"families\") or []):\n        if _family not in families:\n            families.append(_family)\n\n    return families\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/lib.html#client.ayon_core.pipeline.publish.lib.get_instance_publish_template","title":"<code>get_instance_publish_template(instance)</code>","text":"<p>Return anatomy template name to use for integration.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>Instance</code> <p>Instance to process.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Anatomy template name</p> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def get_instance_publish_template(instance: pyblish.api.Instance) -&gt; str:\n    \"\"\"Return anatomy template name to use for integration.\n\n    Args:\n        instance (pyblish.api.Instance): Instance to process.\n\n    Returns:\n        str: Anatomy template name\n\n    \"\"\"\n    # Anatomy data is pre-filled by Collectors\n    publish_template = get_publish_template_object(instance)\n    return publish_template[\"path\"].template.replace(\"\\\\\", \"/\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/lib.html#client.ayon_core.pipeline.publish.lib.get_instance_staging_dir","title":"<code>get_instance_staging_dir(instance)</code>","text":"<p>Unified way how staging dir is stored and created on instances.</p> <p>First check if 'stagingDir' is already set in instance data. In case there already is new tempdir will not be created.</p> <p>Returns:</p> Name Type Description <code>str</code> <p>Path to staging dir</p> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def get_instance_staging_dir(instance):\n    \"\"\"Unified way how staging dir is stored and created on instances.\n\n    First check if 'stagingDir' is already set in instance data.\n    In case there already is new tempdir will not be created.\n\n    Returns:\n        str: Path to staging dir\n    \"\"\"\n    staging_dir = instance.data.get(\"stagingDir\")\n\n    if staging_dir:\n        return staging_dir\n\n    anatomy_data = instance.data[\"anatomyData\"]\n    template_data = copy.deepcopy(anatomy_data)\n\n    # context data based variables\n    context = instance.context\n\n    # add current file as workfile name into formatting data\n    current_file = context.data.get(\"currentFile\")\n    if current_file:\n        workfile = os.path.basename(current_file)\n        workfile_name, _ = os.path.splitext(workfile)\n        template_data[\"workfile_name\"] = workfile_name\n\n    product_type = instance.data[\"productType\"]\n    product_base_type = instance.data.get(\"productBaseType\")\n    if not product_base_type:\n        product_base_type = product_type\n    staging_dir_info = get_staging_dir_info(\n        context.data[\"projectEntity\"],\n        instance.data.get(\"folderEntity\"),\n        instance.data.get(\"taskEntity\"),\n        product_base_type=product_base_type,\n        product_type=product_type,\n        product_name=instance.data[\"productName\"],\n        host_name=context.data[\"hostName\"],\n        anatomy=context.data[\"anatomy\"],\n        project_settings=context.data[\"project_settings\"],\n        template_data=template_data,\n        always_return_path=True,\n        username=context.data[\"user\"],\n    )\n\n    staging_dir_path = staging_dir_info.directory\n\n    # path might be already created by get_staging_dir_info\n    os.makedirs(staging_dir_path, exist_ok=True)\n    instance.data.update({\n        \"stagingDir\": staging_dir_path,\n        \"stagingDir_persistent\": staging_dir_info.is_persistent,\n        \"stagingDir_is_custom\": staging_dir_info.is_custom\n    })\n\n    return staging_dir_path\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/lib.html#client.ayon_core.pipeline.publish.lib.get_instance_template_name","title":"<code>get_instance_template_name(instance)</code>","text":"<p>Return anatomy template name to use for integration.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>Instance</code> <p>Instance to process.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Anatomy template name</p> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def get_instance_template_name(instance: pyblish.api.Instance) -&gt; str:\n    \"\"\"Return anatomy template name to use for integration.\n\n    Args:\n        instance (pyblish.api.Instance): Instance to process.\n\n    Returns:\n        str: Anatomy template name\n\n    \"\"\"\n    # Anatomy data is pre-filled by Collectors\n    context = instance.context\n    project_name = context.data[\"projectName\"]\n\n    # Task can be optional in anatomy data\n    host_name = context.data[\"hostName\"]\n    anatomy_data = instance.data[\"anatomyData\"]\n    product_type = instance.data[\"productType\"]\n    product_base_type = instance.data.get(\"productBaseType\")\n    if not product_base_type:\n        product_base_type = product_type\n    task_info = anatomy_data.get(\"task\") or {}\n\n    return get_publish_template_name(\n        project_name,\n        host_name,\n        product_base_type=product_base_type,\n        task_name=task_info.get(\"name\"),\n        task_type=task_info.get(\"type\"),\n        project_settings=context.data[\"project_settings\"],\n        logger=log,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/lib.html#client.ayon_core.pipeline.publish.lib.get_plugin_settings","title":"<code>get_plugin_settings(plugin, project_settings, log, category=None)</code>","text":"<p>Get plugin settings based on host name and plugin name.</p> Note <p>Default implementation of automated settings is passing host name     into 'category'.</p> <p>Parameters:</p> Name Type Description Default <code>plugin</code> <code>Plugin</code> <p>Plugin where settings are applied.</p> required <code>project_settings</code> <code>dict[str, Any]</code> <p>Project settings.</p> required <code>log</code> <code>Logger</code> <p>Logger to log messages.</p> required <code>category</code> <code>Optional[str]</code> <p>Settings category key where to look for plugin settings.</p> <code>None</code> <p>Returns:</p> Type Description <p>dict[str, Any]: Plugin settings {'attribute': 'value'}.</p> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def get_plugin_settings(plugin, project_settings, log, category=None):\n    \"\"\"Get plugin settings based on host name and plugin name.\n\n    Note:\n        Default implementation of automated settings is passing host name\n            into 'category'.\n\n    Args:\n        plugin (pyblish.Plugin): Plugin where settings are applied.\n        project_settings (dict[str, Any]): Project settings.\n        log (logging.Logger): Logger to log messages.\n        category (Optional[str]): Settings category key where to look\n            for plugin settings.\n\n    Returns:\n        dict[str, Any]: Plugin settings {'attribute': 'value'}.\n    \"\"\"\n\n    # Plugin can define settings category by class attribute\n    # - it's impossible to set `settings_category` via settings because\n    #     obviously settings are not applied before it.\n    # - if `settings_category` is set the fallback category method is ignored\n    settings_category = getattr(plugin, \"settings_category\", None)\n    if settings_category:\n        try:\n            category_settings = project_settings[settings_category]\n        except KeyError:\n            log.warning((\n                \"Couldn't find settings category '{}' in project settings\"\n            ).format(settings_category))\n            return {}\n\n        try:\n            return category_settings[\"publish\"][plugin.__name__]\n        except KeyError:\n            return {}\n\n    # Use project settings based on a category name\n    if category:\n        try:\n            output = (\n                project_settings\n                [category]\n                [\"publish\"]\n                [plugin.__name__]\n            )\n            warnings.warn(\n                \"Please fill 'settings_category'\"\n                f\" for plugin '{plugin.__name__}'.\",\n                DeprecationWarning\n            )\n            return output\n        except KeyError:\n            pass\n\n    # Settings category determined from path\n    # - usually path is './&lt;category&gt;/plugins/publish/&lt;plugin file&gt;'\n    # - category can be host name of addon name ('maya', 'deadline', ...)\n    filepath = os.path.normpath(inspect.getfile(plugin))\n\n    split_path = filepath.rsplit(os.path.sep, 5)\n    if len(split_path) &lt; 4:\n        log.debug((\n            \"Plugin path is too short to automatically\"\n            \" extract settings category. {}\"\n        ).format(filepath))\n        return {}\n\n    category_from_file = split_path[-4]\n    plugin_kind = split_path[-2]\n\n    # TODO: change after all plugins are moved one level up\n    if category_from_file == \"ayon_core\":\n        category_from_file = \"core\"\n\n    try:\n        output = (\n            project_settings\n            [category_from_file]\n            [plugin_kind]\n            [plugin.__name__]\n        )\n        warnings.warn(\n            \"Please fill 'settings_category'\"\n            f\" for plugin '{plugin.__name__}'.\",\n            DeprecationWarning\n        )\n        return output\n    except KeyError:\n        pass\n    return {}\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/lib.html#client.ayon_core.pipeline.publish.lib.get_publish_instance_families","title":"<code>get_publish_instance_families(instance)</code>","text":"<p>Get all families of the instance.</p> <p>Look for families under 'productType' and 'families' keys in instance data. Value of 'productType' is used as first family and then all other families in random order.</p> <p>Parameters:</p> Name Type Description Default <code>pyblish.api.Instance</code> <p>Instance to get families from.</p> required <p>Returns:</p> Type Description <p>list[str]: List of families.</p> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def get_publish_instance_families(instance):\n    \"\"\"Get all families of the instance.\n\n    Look for families under 'productType' and 'families' keys in instance data.\n    Value of 'productType' is used as first family and then all other families\n    in random order.\n\n    Args:\n        pyblish.api.Instance: Instance to get families from.\n\n    Returns:\n        list[str]: List of families.\n    \"\"\"\n\n    family = instance.data.get(\"family\")\n    families = set(instance.data.get(\"families\") or [])\n    output = []\n    if family:\n        output.append(family)\n        families.discard(family)\n    output.extend(families)\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/lib.html#client.ayon_core.pipeline.publish.lib.get_publish_instance_label","title":"<code>get_publish_instance_label(instance)</code>","text":"<p>Try to get label from pyblish instance.</p> <p>First are used values in instance data under 'label' and 'name' keys. Then is used string conversion of instance object -&gt; 'instance._name'.</p> Todos <p>Maybe 'productName' key could be used too.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>Instance</code> <p>Pyblish instance.</p> required <p>Returns:</p> Name Type Description <code>str</code> <p>Instance label.</p> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def get_publish_instance_label(instance):\n    \"\"\"Try to get label from pyblish instance.\n\n    First are used values in instance data under 'label' and 'name' keys. Then\n    is used string conversion of instance object -&gt; 'instance._name'.\n\n    Todos:\n        Maybe 'productName' key could be used too.\n\n    Args:\n        instance (pyblish.api.Instance): Pyblish instance.\n\n    Returns:\n        str: Instance label.\n    \"\"\"\n\n    return (\n        instance.data.get(\"label\")\n        or instance.data.get(\"name\")\n        or str(instance)\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/lib.html#client.ayon_core.pipeline.publish.lib.get_publish_repre_path","title":"<code>get_publish_repre_path(instance, repre, only_published=False)</code>","text":"<p>Get representation path that can be used for integration.</p> <p>When 'only_published' is set to true the validation of path is not relevant. In that case we just need what is set in 'published_path' as \"reference\". The reference is not used to get or upload the file but for reference where the file was published.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>Instance</code> <p>Processed instance object. Used for source of staging dir if representation does not have filled it.</p> required <code>repre</code> <code>dict</code> <p>Representation on instance which could be and could not be integrated with main integrator.</p> required <code>only_published</code> <code>bool</code> <p>Care only about published paths and ignore if filepath is not existing anymore.</p> <code>False</code> <p>Returns:</p> Name Type Description <code>str</code> <p>Path to representation file.</p> <code>None</code> <p>Path is not filled or does not exists.</p> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def get_publish_repre_path(instance, repre, only_published=False):\n    \"\"\"Get representation path that can be used for integration.\n\n    When 'only_published' is set to true the validation of path is not\n    relevant. In that case we just need what is set in 'published_path'\n    as \"reference\". The reference is not used to get or upload the file but\n    for reference where the file was published.\n\n    Args:\n        instance (pyblish.Instance): Processed instance object. Used\n            for source of staging dir if representation does not have\n            filled it.\n        repre (dict): Representation on instance which could be and\n            could not be integrated with main integrator.\n        only_published (bool): Care only about published paths and\n            ignore if filepath is not existing anymore.\n\n    Returns:\n        str: Path to representation file.\n        None: Path is not filled or does not exists.\n    \"\"\"\n\n    published_path = repre.get(\"published_path\")\n    if published_path:\n        published_path = os.path.normpath(published_path)\n        if os.path.exists(published_path):\n            return published_path\n\n    if only_published:\n        return published_path\n\n    comp_files = repre[\"files\"]\n    if isinstance(comp_files, (tuple, list, set)):\n        filename = comp_files[0]\n    else:\n        filename = comp_files\n\n    staging_dir = repre.get(\"stagingDir\")\n    if not staging_dir:\n        staging_dir = get_instance_staging_dir(instance)\n\n    # Expand the staging dir path in case it's been stored with the root\n    # template syntax\n    anatomy = instance.context.data[\"anatomy\"]\n    staging_dir = anatomy.fill_root(staging_dir)\n\n    src_path = os.path.normpath(os.path.join(staging_dir, filename))\n    if os.path.exists(src_path):\n        return src_path\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/lib.html#client.ayon_core.pipeline.publish.lib.get_publish_template_name","title":"<code>get_publish_template_name(project_name, host_name, product_base_type, task_name, task_type, *, project_settings=None, hero=False, logger=None)</code>","text":"<p>Get template name which should be used for passed context.</p> <p>Publish templates are filtered by host name, family, task name and task type.</p> <p>Default template which is used at if profiles are not available or profile has empty value is defined by 'DEFAULT_PUBLISH_TEMPLATE' constant.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Name of project where to look for settings.</p> required <code>host_name</code> <code>str</code> <p>Name of host integration.</p> required <code>product_base_type</code> <code>str</code> <p>Product base type for which should be found template.</p> required <code>task_name</code> <code>str</code> <p>Task name on which is instance working.</p> required <code>task_type</code> <code>str</code> <p>Task type on which is instance working.</p> required <code>project_settings</code> <code>Dict[str, Any]</code> <p>Prepared project settings.</p> <code>None</code> <code>hero</code> <code>bool</code> <p>Template is for hero version publishing.</p> <code>False</code> <code>logger</code> <code>Logger</code> <p>Custom logger used for 'filter_profiles' function.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Template name which should be used for integration.</p> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>@_get_publish_template_name_wrap\ndef get_publish_template_name(\n    project_name: str,\n    host_name: str,\n    product_base_type: str,\n    task_name: Union[str, None],\n    task_type: Union[str, None],\n    *,\n    project_settings: Optional[dict] = None,\n    hero: bool = False,\n    logger: Optional[logging.Logger] = None,\n) -&gt; str:\n    \"\"\"Get template name which should be used for passed context.\n\n    Publish templates are filtered by host name, family, task name and\n    task type.\n\n    Default template which is used at if profiles are not available or profile\n    has empty value is defined by 'DEFAULT_PUBLISH_TEMPLATE' constant.\n\n    Args:\n        project_name (str): Name of project where to look for settings.\n        host_name (str): Name of host integration.\n        product_base_type (str): Product base type for which should be\n            found template.\n        task_name (str): Task name on which is instance working.\n        task_type (str): Task type on which is instance working.\n        project_settings (Dict[str, Any]): Prepared project settings.\n        hero (bool): Template is for hero version publishing.\n        logger (logging.Logger): Custom logger used for 'filter_profiles'\n            function.\n\n    Returns:\n        str: Template name which should be used for integration.\n\n    \"\"\"\n    template = None\n    filter_criteria = {\n        \"host_names\": host_name,\n        \"product_base_types\": product_base_type,\n        \"task_names\": task_name,\n        \"task_types\": task_type,\n    }\n    if hero:\n        default_template = DEFAULT_HERO_PUBLISH_TEMPLATE\n        profiles = get_hero_template_name_profiles(\n            project_name, project_settings\n        )\n\n    else:\n        profiles = get_template_name_profiles(\n            project_name, project_settings\n        )\n        default_template = DEFAULT_PUBLISH_TEMPLATE\n\n    profile = filter_profiles(profiles, filter_criteria, logger=logger)\n    if profile:\n        template = profile[\"template_name\"]\n    return template or default_template\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/lib.html#client.ayon_core.pipeline.publish.lib.get_publish_template_object","title":"<code>get_publish_template_object(instance, category_name='publish', template_name=None)</code>","text":"<p>Return anatomy template object to use for integration.</p> <p>Note: What is the actual type of the object?</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>Instance</code> <p>Instance to process.</p> required <code>category_name</code> <code>str</code> <p>Category name of the template to use. Defaults to \"publish\".</p> <code>'publish'</code> <code>template_name</code> <code>str</code> <p>Template name to use. If not provided, it will get the template name from the provided instance.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>AnatomyTemplateItem</code> <code>'AnatomyTemplateItem'</code> <p>Anatomy template object</p> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def get_publish_template_object(\n        instance: pyblish.api.Instance,\n        category_name: str = \"publish\",\n        template_name: Optional[str] = None,\n) -&gt; \"AnatomyTemplateItem\":\n    \"\"\"Return anatomy template object to use for integration.\n\n    Note: What is the actual type of the object?\n\n    Args:\n        instance (pyblish.api.Instance): Instance to process.\n        category_name (str): Category name of the template to use.\n            Defaults to \"publish\".\n        template_name (str, optional): Template name to use.\n            If not provided, it will get the template name from\n            the provided instance.\n\n    Returns:\n        AnatomyTemplateItem: Anatomy template object\n\n    \"\"\"\n    # Anatomy data is pre-filled by Collectors\n    if not template_name:\n        template_name = get_instance_template_name(instance)\n    anatomy: Anatomy = instance.context.data[\"anatomy\"]\n    return anatomy.get_template_item(\n        category_name=category_name,\n        template_name=template_name\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/lib.html#client.ayon_core.pipeline.publish.lib.get_published_workfile_instance","title":"<code>get_published_workfile_instance(context)</code>","text":"<p>Find workfile instance in context</p> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def get_published_workfile_instance(context):\n    \"\"\"Find workfile instance in context\"\"\"\n    for i in context:\n        # test if there is instance of workfile waiting\n        # to be published.\n        if not i.data.get(\"publish\", True):\n            continue\n\n        if not (\n            i.data[\"productType\"] == \"workfile\"\n            # QUESTION Is check in 'families' valid?\n            or \"workfile\" in i.data.get(\"families\", [])\n        ):\n            continue\n\n        return i\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/lib.html#client.ayon_core.pipeline.publish.lib.get_rootless_path","title":"<code>get_rootless_path(anatomy, path)</code>","text":"<p>Get rootless variant of the path.</p> <p>Returns, if possible, a path without an absolute portion from the root (e.g. 'c:\\' or '/opt/..'). This is basically a wrapper for the meth:<code>Anatomy.find_root_template_from_path</code> method that displays a warning if the root path is not found.</p> <p>This information is platform-dependent and shouldn't be captured.  For example::</p> <pre><code> 'c:/projects/MyProject1/Assets/publish...'\n will be transformed to:\n '{root}/MyProject1/Assets...'\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>anatomy</code> <code>Anatomy</code> <p>Project anatomy.</p> required <code>path</code> <code>str</code> <p>Absolute path.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Path where root path is replaced by formatting string.</p> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def get_rootless_path(anatomy: \"Anatomy\", path: str) -&gt; str:\n    r\"\"\"Get rootless variant of the path.\n\n    Returns, if possible, a path without an absolute portion from the root\n    (e.g. 'c:\\' or '/opt/..'). This is basically a wrapper for the\n    meth:`Anatomy.find_root_template_from_path` method that displays\n    a warning if the root path is not found.\n\n     This information is platform-dependent and shouldn't be captured.\n     For example::\n\n         'c:/projects/MyProject1/Assets/publish...'\n         will be transformed to:\n         '{root}/MyProject1/Assets...'\n\n    Args:\n        anatomy (Anatomy): Project anatomy.\n        path (str): Absolute path.\n\n    Returns:\n        str: Path where root path is replaced by formatting string.\n\n    \"\"\"\n    success, rootless_path = anatomy.find_root_template_from_path(path)\n    if success:\n        path = rootless_path\n    else:\n        log.warning((\n            'Could not find root path for remapping \"%s\".'\n            \" This may cause issues on farm.\"\n        ), path)\n    return path\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/lib.html#client.ayon_core.pipeline.publish.lib.get_template_name_profiles","title":"<code>get_template_name_profiles(project_name, project_settings=None)</code>","text":"<p>Receive profiles for publish template keys.</p> <p>At least one of arguments must be passed.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Name of project where to look for templates.</p> required <code>project_settings</code> <code>Dict[str, Any]</code> <p>Prepared project settings.</p> <code>None</code> <p>Returns:</p> Type Description <code>list[dict[str, Any]]</code> <p>list[dict[str, Any]]: Publish template profiles.</p> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def get_template_name_profiles(\n    project_name: str,\n    project_settings: Optional[dict[str, Any]] = None,\n) -&gt; list[dict[str, Any]]:\n    \"\"\"Receive profiles for publish template keys.\n\n    At least one of arguments must be passed.\n\n    Args:\n        project_name (str): Name of project where to look for templates.\n        project_settings (Dict[str, Any]): Prepared project settings.\n\n    Returns:\n        list[dict[str, Any]]: Publish template profiles.\n\n    \"\"\"\n    if not project_name and not project_settings:\n        raise ValueError((\n            \"Both project name and project settings are missing.\"\n            \" At least one must be entered.\"\n        ))\n\n    if not project_settings:\n        project_settings = get_project_settings(project_name)\n\n    return copy.deepcopy(\n        project_settings\n        [\"core\"]\n        [\"tools\"]\n        [\"publish\"]\n        [\"template_name_profiles\"]\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/lib.html#client.ayon_core.pipeline.publish.lib.get_trait_representations","title":"<code>get_trait_representations(instance)</code>","text":"<p>Get trait representations from instance.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>Instance</code> <p>Instance to get trait representations from.</p> required <p>Returns:</p> Type Description <code>list[Representation]</code> <p>list[Representation]: List of representation names.</p> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def get_trait_representations(\n        instance: pyblish.api.Instance) -&gt; list[Representation]:\n    \"\"\"Get trait representations from instance.\n\n    Args:\n        instance (pyblish.api.Instance): Instance to get trait\n            representations from.\n\n    Returns:\n        list[Representation]: List of representation names.\n\n    \"\"\"\n    return instance.data.get(TRAIT_INSTANCE_KEY, [])\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/lib.html#client.ayon_core.pipeline.publish.lib.get_version_data_from_instance","title":"<code>get_version_data_from_instance(instance)</code>","text":"<p>Get version data from the Instance.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>Instance</code> <p>the current instance being published.</p> required <p>Returns:</p> Name Type Description <code>dict</code> <code>dict</code> <p>the required information for <code>version[\"data\"]</code></p> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def get_version_data_from_instance(\n        instance: pyblish.api.Instance) -&gt; dict:\n    \"\"\"Get version data from the Instance.\n\n    Args:\n        instance (pyblish.api.Instance): the current instance\n            being published.\n\n    Returns:\n        dict: the required information for ``version[\"data\"]``\n\n    \"\"\"\n    context = instance.context\n\n    # create relative source path for DB\n    if \"source\" in instance.data:\n        source = instance.data[\"source\"]\n    else:\n        source = context.data[\"currentFile\"]\n        anatomy = instance.context.data[\"anatomy\"]\n        source = get_rootless_path(anatomy, source)\n    log.debug(\"Source: %s\", source)\n\n    version_data = {\n        \"families\": get_instance_families(instance),\n        \"time\": context.data[\"time\"],\n        \"author\": context.data[\"user\"],\n        \"source\": source,\n        \"comment\": instance.data[\"comment\"],\n        \"machine\": context.data.get(\"machine\"),\n        \"fps\": instance.data.get(\"fps\", context.data.get(\"fps\"))\n    }\n\n    intent_value = context.data.get(\"intent\")\n    if intent_value and isinstance(intent_value, dict):\n        intent_value = intent_value.get(\"value\")\n\n    if intent_value:\n        version_data[\"intent\"] = intent_value\n\n    # Include optional data if present in\n    optionals = [\n        \"frameStart\", \"frameEnd\", \"step\",\n        \"handleEnd\", \"handleStart\", \"sourceHashes\"\n    ]\n    for key in optionals:\n        if key in instance.data:\n            version_data[key] = instance.data[key]\n\n    # Include instance.data[versionData] directly\n    version_data_instance = instance.data.get(\"versionData\")\n    if version_data_instance:\n        version_data.update(version_data_instance)\n\n    return version_data\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/lib.html#client.ayon_core.pipeline.publish.lib.has_trait_representations","title":"<code>has_trait_representations(instance)</code>","text":"<p>Check if instance has trait representation.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>Instance</code> <p>Instance to check.</p> required <p>Returns:</p> Name Type Description <code>True</code> <code>bool</code> <p>Instance has trait representation.</p> <code>False</code> <code>bool</code> <p>Instance does not have trait representation.</p> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def has_trait_representations(\n        instance: pyblish.api.Instance) -&gt; bool:\n    \"\"\"Check if instance has trait representation.\n\n    Args:\n        instance (pyblish.api.Instance): Instance to check.\n\n    Returns:\n        True: Instance has trait representation.\n        False: Instance does not have trait representation.\n\n    \"\"\"\n    return TRAIT_INSTANCE_KEY in instance.data\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/lib.html#client.ayon_core.pipeline.publish.lib.load_help_content_from_filepath","title":"<code>load_help_content_from_filepath(filepath)</code>","text":"<p>Load help content from xml file. Xml file may contain errors and warnings.</p> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def load_help_content_from_filepath(\n    filepath: str\n) -&gt; dict[str, dict[str, HelpContent]]:\n    \"\"\"Load help content from xml file.\n    Xml file may contain errors and warnings.\n    \"\"\"\n    errors = {}\n    warnings = {}\n    output = {\n        \"errors\": errors,\n        \"warnings\": warnings\n    }\n    if not os.path.exists(filepath):\n        return output\n    tree = xml.etree.ElementTree.parse(filepath)\n    root = tree.getroot()\n    for child in root:\n        child_id = child.attrib.get(\"id\")\n        if child_id is None:\n            continue\n\n        # Make sure ID is string\n        child_id = str(child_id)\n\n        title = child.find(\"title\").text\n        description = child.find(\"description\").text\n        detail_node = child.find(\"detail\")\n        detail = None\n        if detail_node is not None:\n            detail = detail_node.text\n        if child.tag == \"error\":\n            errors[child_id] = HelpContent(title, description, detail)\n        elif child.tag == \"warning\":\n            warnings[child_id] = HelpContent(title, description, detail)\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/lib.html#client.ayon_core.pipeline.publish.lib.main_cli_publish","title":"<code>main_cli_publish(path, targets=None, addons_manager=None)</code>","text":"<p>Start headless publishing.</p> <p>Publish use json from passed path argument.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Path to JSON.</p> required <code>targets</code> <code>Optional[List[str]]</code> <p>List of pyblish targets.</p> <code>None</code> <code>addons_manager</code> <code>Optional[AddonsManager]</code> <p>Addons manager instance.</p> <code>None</code> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>When there is no path to process or when executed with list of JSON paths.</p> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def main_cli_publish(\n    path: str,\n    targets: Optional[List[str]] = None,\n    addons_manager: Optional[AddonsManager] = None,\n):\n    \"\"\"Start headless publishing.\n\n    Publish use json from passed path argument.\n\n    Args:\n        path (str): Path to JSON.\n        targets (Optional[List[str]]): List of pyblish targets.\n        addons_manager (Optional[AddonsManager]): Addons manager instance.\n\n    Raises:\n        RuntimeError: When there is no path to process or when executed with\n            list of JSON paths.\n\n    \"\"\"\n    from ayon_core.pipeline import (\n        install_ayon_plugins,\n        get_global_context,\n    )\n\n    # Register target and host\n    if not isinstance(path, str):\n        raise RuntimeError(\"Path to JSON must be a string.\")\n\n    log = Logger.get_logger(\"CLI-publish\")\n\n    # Make public ayon api behave as other user\n    # - this works only if public ayon api is using service user\n    username = os.environ.get(\"AYON_USERNAME\")\n    if username:\n        # NOTE: ayon-python-api does not have public api function to find\n        #   out if is used service user. So we need to have try &gt; except\n        #   block.\n        con = get_server_api_connection()\n        try:\n            con.set_default_service_username(username)\n        except ValueError:\n            pass\n\n    context = get_global_context()\n    project_settings = get_project_settings(context[\"project_name\"])\n\n    install_ayon_plugins()\n\n    if addons_manager is None:\n        addons_manager = AddonsManager(project_settings)\n\n    applications_addon = addons_manager.get_enabled_addon(\"applications\")\n    if applications_addon is not None:\n        env = applications_addon.get_farm_publish_environment_variables(\n            context[\"project_name\"],\n            context[\"folder_path\"],\n            context[\"task_name\"],\n        )\n        os.environ.update(env)\n\n    pyblish.api.register_host(\"shell\")\n\n    if targets:\n        for target in targets:\n            print(f\"setting target: {target}\")\n            pyblish.api.register_target(target)\n    else:\n        pyblish.api.register_target(\"farm\")\n\n    os.environ[\"AYON_PUBLISH_DATA\"] = path\n    os.environ[\"HEADLESS_PUBLISH\"] = 'true'  # to use in app lib\n\n    log.info(\"Running publish ...\")\n\n    discover_result = publish_plugins_discover()\n    print(discover_result.get_report(only_errors=False))\n\n    filtered_crashed_paths = filter_crashed_publish_paths(\n        context[\"project_name\"],\n        set(discover_result.crashed_file_paths),\n        project_settings=project_settings,\n    )\n    if filtered_crashed_paths:\n        joined_paths = \"\\n\".join([\n            f\"- {path}\"\n            for path in filtered_crashed_paths\n        ])\n        log.error(\n            \"Plugin discovery strict mode is enabled.\"\n            \" Crashed plugin paths that prevent from publishing:\"\n            f\"\\n{joined_paths}\"\n        )\n        sys.exit(1)\n\n    publish_plugins = discover_result.plugins\n\n    # Error exit as soon as any error occurs.\n    error_format = \"Failed {plugin.__name__}: {error} -- {error.traceback}\"\n\n    for result in pyblish.util.publish_iter(plugins=publish_plugins):\n        if result[\"error\"]:\n            log.error(error_format.format(**result))\n            sys.exit(1)\n\n    log.info(\"Publish finished.\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/lib.html#client.ayon_core.pipeline.publish.lib.publish_plugins_discover","title":"<code>publish_plugins_discover(paths=None)</code>","text":"<p>Find and return available pyblish plug-ins.</p> <p>Overridden function from <code>pyblish</code> module to be able to collect     crashed files and reason of their crash.</p> <p>Parameters:</p> Name Type Description Default <code>paths</code> <code>list</code> <p>Paths to discover plug-ins from. If no paths are provided, all paths are searched.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def publish_plugins_discover(\n        paths: Optional[list[str]] = None) -&gt; DiscoverResult:\n    \"\"\"Find and return available pyblish plug-ins.\n\n    Overridden function from `pyblish` module to be able to collect\n        crashed files and reason of their crash.\n\n    Arguments:\n        paths (list, optional): Paths to discover plug-ins from.\n            If no paths are provided, all paths are searched.\n    \"\"\"\n\n    # The only difference with `pyblish.api.discover`\n    result = DiscoverResult(pyblish.api.Plugin)\n\n    plugins = {}\n    plugin_names = []\n\n    allow_duplicates = pyblish.plugin.ALLOW_DUPLICATES\n    log = pyblish.plugin.log\n\n    # Include plug-ins from registered paths\n    if not paths:\n        paths = pyblish.plugin.plugin_paths()\n\n    for path in paths:\n        path = os.path.normpath(path)\n        filenames = []\n        if os.path.isdir(path):\n            filenames.extend(\n                name\n                for name in os.listdir(path)\n                if (\n                    os.path.isfile(os.path.join(path, name))\n                    and not name.startswith(\"_\")\n                )\n            )\n        else:\n            filenames.append(os.path.basename(path))\n            path = os.path.dirname(path)\n\n        dirpath_hash = hashlib.md5(path.encode(\"utf-8\")).hexdigest()\n        for filename in filenames:\n            basename, ext = os.path.splitext(filename)\n            if ext.lower() != \".py\":\n                continue\n\n            filepath = os.path.join(path, filename)\n            module_name = f\"{dirpath_hash}.{basename}\"\n            try:\n                module = import_filepath(\n                    filepath, module_name, sys_module_name=module_name\n                )\n\n            except Exception as err:  # noqa: BLE001\n                # we need broad exception to catch all possible errors.\n                result.crashed_file_paths[filepath] = sys.exc_info()\n\n                log.debug('Skipped: \"%s\" (%s)', filepath, err)\n                continue\n\n            for plugin in pyblish.plugin.plugins_from_module(module):\n                # Ignore base plugin classes\n                # NOTE 'pyblish.api.discover' does not ignore them!\n                if (\n                    plugin is pyblish.api.Plugin\n                    or plugin is pyblish.api.ContextPlugin\n                    or plugin is pyblish.api.InstancePlugin\n                ):\n                    continue\n                if not allow_duplicates and plugin.__name__ in plugin_names:\n                    result.duplicated_plugins.append(plugin)\n                    log.debug(\"Duplicate plug-in found: %s\", plugin)\n                    continue\n\n                plugin_names.append(plugin.__name__)\n                plugin.__file__ = module.__file__\n                key = f\"{module.__file__}.{plugin.__name__}\"\n                plugins[key] = plugin\n\n    # Include plug-ins from registration.\n    # Directly registered plug-ins take precedence.\n    for plugin in pyblish.plugin.registered_plugins():\n        if not allow_duplicates and plugin.__name__ in plugin_names:\n            result.duplicated_plugins.append(plugin)\n            log.debug(\"Duplicate plug-in found: %s\", plugin)\n            continue\n\n        plugin_names.append(plugin.__name__)\n\n        plugins[plugin.__name__] = plugin\n\n    plugins = list(plugins.values())\n    pyblish.plugin.sort(plugins)  # In-place\n\n    # In-place user-defined filter\n    for filter_ in pyblish.plugin._registered_plugin_filters:\n        filter_(plugins)\n\n    result.plugins = plugins\n\n    return result\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/lib.html#client.ayon_core.pipeline.publish.lib.replace_with_published_scene_path","title":"<code>replace_with_published_scene_path(instance, replace_in_path=True)</code>","text":"<p>Switch work scene path for published scene. If rendering/exporting from published scenes is enabled, this will replace paths from working scene to published scene. This only works if publish contains workfile instance! Args:     instance (pyblish.api.Instance): Pyblish instance.     replace_in_path (bool): if True, it will try to find         old scene name in path of expected files and replace it         with name of published scene. Returns:     str: Published scene path.     None: if no published scene is found. Note:     Published scene path is actually determined from project Anatomy     as at the time this plugin is running scene can still not be     published.</p> Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def replace_with_published_scene_path(instance, replace_in_path=True):\n    \"\"\"Switch work scene path for published scene.\n    If rendering/exporting from published scenes is enabled, this will\n    replace paths from working scene to published scene.\n    This only works if publish contains workfile instance!\n    Args:\n        instance (pyblish.api.Instance): Pyblish instance.\n        replace_in_path (bool): if True, it will try to find\n            old scene name in path of expected files and replace it\n            with name of published scene.\n    Returns:\n        str: Published scene path.\n        None: if no published scene is found.\n    Note:\n        Published scene path is actually determined from project Anatomy\n        as at the time this plugin is running scene can still not be\n        published.\n    \"\"\"\n    log = Logger.get_logger(\"published_workfile\")\n    workfile_instance = get_published_workfile_instance(instance.context)\n    if workfile_instance is None:\n        return\n\n    # determine published path from Anatomy.\n    template_data = copy.deepcopy(workfile_instance.data[\"anatomyData\"])\n    rep = workfile_instance.data[\"representations\"][0]\n    template_data[\"representation\"] = rep.get(\"name\")\n    template_data[\"ext\"] = rep.get(\"ext\")\n    template_data[\"comment\"] = None\n\n    anatomy = instance.context.data[\"anatomy\"]\n    project_name = anatomy.project_name\n    task_name = task_type = None\n    task_entity = instance.data.get(\"taskEntity\")\n    if task_entity:\n        task_name = task_entity[\"name\"]\n        task_type = task_entity[\"taskType\"]\n\n    project_settings = instance.context.data[\"project_settings\"]\n    product_base_type = workfile_instance.data.get(\"productBaseType\")\n    if not product_base_type:\n        product_base_type = workfile_instance.data[\"productType\"]\n\n    template_name = get_publish_template_name(\n        project_name=project_name,\n        host_name=instance.context.data[\"hostName\"],\n        product_base_type=product_base_type,\n        task_name=task_name,\n        task_type=task_type,\n        project_settings=project_settings,\n    )\n    template = anatomy.get_template_item(\"publish\", template_name, \"path\")\n    template_filled = template.format_strict(template_data)\n    file_path = os.path.normpath(template_filled)\n\n    log.info(\"Using published scene for render {}\".format(file_path))\n\n    if not os.path.exists(file_path):\n        log.error(\"published scene does not exist!\")\n        raise\n\n    if not replace_in_path:\n        return file_path\n\n    # now we need to switch scene in expected files\n    # because &lt;scene&gt; token will now point to published\n    # scene file and that might differ from current one\n    def _clean_name(path):\n        return os.path.splitext(os.path.basename(path))[0]\n\n    new_scene = _clean_name(file_path)\n    orig_scene = _clean_name(instance.context.data[\"currentFile\"])\n    expected_files = instance.data.get(\"expectedFiles\")\n\n    if isinstance(expected_files[0], dict):\n        # we have aovs and we need to iterate over them\n        new_exp = {}\n        for aov, files in expected_files[0].items():\n            replaced_files = []\n            for f in files:\n                replaced_files.append(\n                    str(f).replace(orig_scene, new_scene)\n                )\n            new_exp[aov] = replaced_files\n        # [] might be too much here, TODO\n        instance.data[\"expectedFiles\"] = [new_exp]\n    else:\n        new_exp = []\n        for f in expected_files:\n            new_exp.append(\n                str(f).replace(orig_scene, new_scene)\n            )\n        instance.data[\"expectedFiles\"] = new_exp\n\n    metadata_folder = instance.data.get(\"publishRenderMetadataFolder\")\n    if metadata_folder:\n        metadata_folder = metadata_folder.replace(orig_scene,\n                                                  new_scene)\n        instance.data[\"publishRenderMetadataFolder\"] = metadata_folder\n\n    log.info(\"Scene name was switched {} -&gt; {}\".format(\n        orig_scene, new_scene\n    ))\n\n    return file_path\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/lib.html#client.ayon_core.pipeline.publish.lib.set_trait_representations","title":"<code>set_trait_representations(instance, representations)</code>","text":"<p>Set trait representations to instance.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>Instance</code> <p>Instance to set trait representations to.</p> required <code>representations</code> <code>list[Representation]</code> <p>List of trait based representations.</p> required Source code in <code>client/ayon_core/pipeline/publish/lib.py</code> <pre><code>def set_trait_representations(\n        instance: pyblish.api.Instance,\n        representations: list[Representation]\n) -&gt; None:\n    \"\"\"Set trait representations to instance.\n\n    Args:\n        instance (pyblish.api.Instance): Instance to set trait\n            representations to.\n        representations (list[Representation]): List of trait\n            based representations.\n\n    \"\"\"\n    instance.data[TRAIT_INSTANCE_KEY] = representations\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/publish_plugins.html","title":"publish_plugins","text":""},{"location":"autoapi/client/ayon_core/pipeline/publish/publish_plugins.html#client.ayon_core.pipeline.publish.publish_plugins.AYONPyblishPluginMixin","title":"<code>AYONPyblishPluginMixin</code>","text":"Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>class AYONPyblishPluginMixin:\n    # TODO\n    # executable_in_thread = False\n    #\n    # state_message = None\n    # state_percent = None\n    # _state_change_callbacks = []\n    #\n    # def set_state(self, percent=None, message=None):\n    #     \"\"\"Inner callback of plugin that would help to show in UI state.\n    #\n    #     Plugin have registered callbacks on state change which could trigger\n    #     update message and percent in UI and repaint the change.\n    #\n    #     This part must be optional and should not be used to display errors\n    #     or for logging.\n    #\n    #     Message should be short without details.\n    #\n    #     Args:\n    #         percent(int): Percent of processing in range &lt;1-100&gt;.\n    #         message(str): Message which will be shown to user (if in UI).\n    #     \"\"\"\n    #     if percent is not None:\n    #         self.state_percent = percent\n    #\n    #     if message:\n    #         self.state_message = message\n    #\n    #     for callback in self._state_change_callbacks:\n    #         callback(self)\n\n    @classmethod\n    def register_create_context_callbacks(\n        cls, create_context: \"CreateContext\"\n    ):\n        \"\"\"Register callbacks for create context.\n\n        It is possible to register callbacks listening to changes happened\n        in create context.\n\n        Methods available on create context:\n        - add_instances_added_callback\n        - add_instances_removed_callback\n        - add_value_changed_callback\n        - add_pre_create_attr_defs_change_callback\n        - add_create_attr_defs_change_callback\n        - add_publish_attr_defs_change_callback\n\n        Args:\n            create_context (CreateContext): Create context.\n\n        \"\"\"\n        pass\n\n    @classmethod\n    def get_attribute_defs(cls):\n        \"\"\"Publish attribute definitions.\n\n        Attributes available for all families in plugin's `families` attribute.\n\n        Returns:\n            list[AbstractAttrDef]: Attribute definitions for plugin.\n\n        \"\"\"\n        return []\n\n    @classmethod\n    def get_attr_defs_for_context(cls, create_context: \"CreateContext\"):\n        \"\"\"Publish attribute definitions for context.\n\n        Attributes available for all families in plugin's `families` attribute.\n\n        Args:\n            create_context (CreateContext): Create context.\n\n        Returns:\n            list[AbstractAttrDef]: Attribute definitions for plugin.\n\n        \"\"\"\n        if cls.__instanceEnabled__:\n            return []\n        return cls.get_attribute_defs()\n\n    @classmethod\n    def instance_matches_plugin_families(\n        cls, instance: Optional[\"CreatedInstance\"]\n    ):\n        \"\"\"Check if instance matches families.\n\n        Args:\n            instance (Optional[CreatedInstance]): Instance to check. Or None\n                for context.\n\n        Returns:\n            bool: True if instance matches plugin families.\n\n        \"\"\"\n        if instance is None:\n            return not cls.__instanceEnabled__\n\n        if not cls.__instanceEnabled__:\n            return False\n\n        families = [instance.product_base_type]\n        families.extend(instance.get(\"families\", []))\n        for _ in pyblish.logic.plugins_by_families([cls], families):\n            return True\n        return False\n\n    @classmethod\n    def get_attr_defs_for_instance(\n        cls, create_context: \"CreateContext\", instance: \"CreatedInstance\"\n    ):\n        \"\"\"Publish attribute definitions for an instance.\n\n        Attributes available for all families in plugin's `families` attribute.\n\n        Args:\n            create_context (CreateContext): Create context.\n            instance (CreatedInstance): Instance for which attributes are\n                collected.\n\n        Returns:\n            list[AbstractAttrDef]: Attribute definitions for plugin.\n\n        \"\"\"\n        if not cls.instance_matches_plugin_families(instance):\n            return []\n        return cls.get_attribute_defs()\n\n    @classmethod\n    def convert_attribute_values(\n        cls, create_context: \"CreateContext\", instance: \"CreatedInstance\"\n    ):\n        \"\"\"Convert attribute values for instance.\n\n        Args:\n            create_context (CreateContext): Create context.\n            instance (CreatedInstance): Instance for which attributes are\n                converted.\n\n        \"\"\"\n        return\n\n    @staticmethod\n    def get_attr_values_from_data_for_plugin(plugin, data):\n        \"\"\"Get attribute values for attribute definitions from data.\n\n        Args:\n            plugin (Union[publish.api.Plugin, Type[publish.api.Plugin]]): The\n                plugin for which attributes are extracted.\n            data(dict): Data from instance or context.\n        \"\"\"\n\n        if not inspect.isclass(plugin):\n            plugin = plugin.__class__\n\n        return (\n            data\n            .get(\"publish_attributes\", {})\n            .get(plugin.__name__, {})\n        )\n\n    def get_attr_values_from_data(self, data):\n        \"\"\"Get attribute values for attribute definitions from data.\n\n        Args:\n            data(dict): Data from instance or context.\n        \"\"\"\n\n        return self.get_attr_values_from_data_for_plugin(self.__class__, data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/publish_plugins.html#client.ayon_core.pipeline.publish.publish_plugins.AYONPyblishPluginMixin.convert_attribute_values","title":"<code>convert_attribute_values(create_context, instance)</code>  <code>classmethod</code>","text":"<p>Convert attribute values for instance.</p> <p>Parameters:</p> Name Type Description Default <code>create_context</code> <code>CreateContext</code> <p>Create context.</p> required <code>instance</code> <code>CreatedInstance</code> <p>Instance for which attributes are converted.</p> required Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>@classmethod\ndef convert_attribute_values(\n    cls, create_context: \"CreateContext\", instance: \"CreatedInstance\"\n):\n    \"\"\"Convert attribute values for instance.\n\n    Args:\n        create_context (CreateContext): Create context.\n        instance (CreatedInstance): Instance for which attributes are\n            converted.\n\n    \"\"\"\n    return\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/publish_plugins.html#client.ayon_core.pipeline.publish.publish_plugins.AYONPyblishPluginMixin.get_attr_defs_for_context","title":"<code>get_attr_defs_for_context(create_context)</code>  <code>classmethod</code>","text":"<p>Publish attribute definitions for context.</p> <p>Attributes available for all families in plugin's <code>families</code> attribute.</p> <p>Parameters:</p> Name Type Description Default <code>create_context</code> <code>CreateContext</code> <p>Create context.</p> required <p>Returns:</p> Type Description <p>list[AbstractAttrDef]: Attribute definitions for plugin.</p> Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>@classmethod\ndef get_attr_defs_for_context(cls, create_context: \"CreateContext\"):\n    \"\"\"Publish attribute definitions for context.\n\n    Attributes available for all families in plugin's `families` attribute.\n\n    Args:\n        create_context (CreateContext): Create context.\n\n    Returns:\n        list[AbstractAttrDef]: Attribute definitions for plugin.\n\n    \"\"\"\n    if cls.__instanceEnabled__:\n        return []\n    return cls.get_attribute_defs()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/publish_plugins.html#client.ayon_core.pipeline.publish.publish_plugins.AYONPyblishPluginMixin.get_attr_defs_for_instance","title":"<code>get_attr_defs_for_instance(create_context, instance)</code>  <code>classmethod</code>","text":"<p>Publish attribute definitions for an instance.</p> <p>Attributes available for all families in plugin's <code>families</code> attribute.</p> <p>Parameters:</p> Name Type Description Default <code>create_context</code> <code>CreateContext</code> <p>Create context.</p> required <code>instance</code> <code>CreatedInstance</code> <p>Instance for which attributes are collected.</p> required <p>Returns:</p> Type Description <p>list[AbstractAttrDef]: Attribute definitions for plugin.</p> Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>@classmethod\ndef get_attr_defs_for_instance(\n    cls, create_context: \"CreateContext\", instance: \"CreatedInstance\"\n):\n    \"\"\"Publish attribute definitions for an instance.\n\n    Attributes available for all families in plugin's `families` attribute.\n\n    Args:\n        create_context (CreateContext): Create context.\n        instance (CreatedInstance): Instance for which attributes are\n            collected.\n\n    Returns:\n        list[AbstractAttrDef]: Attribute definitions for plugin.\n\n    \"\"\"\n    if not cls.instance_matches_plugin_families(instance):\n        return []\n    return cls.get_attribute_defs()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/publish_plugins.html#client.ayon_core.pipeline.publish.publish_plugins.AYONPyblishPluginMixin.get_attr_values_from_data","title":"<code>get_attr_values_from_data(data)</code>","text":"<p>Get attribute values for attribute definitions from data.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict</code> <p>Data from instance or context.</p> required Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>def get_attr_values_from_data(self, data):\n    \"\"\"Get attribute values for attribute definitions from data.\n\n    Args:\n        data(dict): Data from instance or context.\n    \"\"\"\n\n    return self.get_attr_values_from_data_for_plugin(self.__class__, data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/publish_plugins.html#client.ayon_core.pipeline.publish.publish_plugins.AYONPyblishPluginMixin.get_attr_values_from_data_for_plugin","title":"<code>get_attr_values_from_data_for_plugin(plugin, data)</code>  <code>staticmethod</code>","text":"<p>Get attribute values for attribute definitions from data.</p> <p>Parameters:</p> Name Type Description Default <code>plugin</code> <code>Union[Plugin, Type[Plugin]]</code> <p>The plugin for which attributes are extracted.</p> required <code>data</code> <code>dict</code> <p>Data from instance or context.</p> required Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>@staticmethod\ndef get_attr_values_from_data_for_plugin(plugin, data):\n    \"\"\"Get attribute values for attribute definitions from data.\n\n    Args:\n        plugin (Union[publish.api.Plugin, Type[publish.api.Plugin]]): The\n            plugin for which attributes are extracted.\n        data(dict): Data from instance or context.\n    \"\"\"\n\n    if not inspect.isclass(plugin):\n        plugin = plugin.__class__\n\n    return (\n        data\n        .get(\"publish_attributes\", {})\n        .get(plugin.__name__, {})\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/publish_plugins.html#client.ayon_core.pipeline.publish.publish_plugins.AYONPyblishPluginMixin.get_attribute_defs","title":"<code>get_attribute_defs()</code>  <code>classmethod</code>","text":"<p>Publish attribute definitions.</p> <p>Attributes available for all families in plugin's <code>families</code> attribute.</p> <p>Returns:</p> Type Description <p>list[AbstractAttrDef]: Attribute definitions for plugin.</p> Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>@classmethod\ndef get_attribute_defs(cls):\n    \"\"\"Publish attribute definitions.\n\n    Attributes available for all families in plugin's `families` attribute.\n\n    Returns:\n        list[AbstractAttrDef]: Attribute definitions for plugin.\n\n    \"\"\"\n    return []\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/publish_plugins.html#client.ayon_core.pipeline.publish.publish_plugins.AYONPyblishPluginMixin.instance_matches_plugin_families","title":"<code>instance_matches_plugin_families(instance)</code>  <code>classmethod</code>","text":"<p>Check if instance matches families.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>Optional[CreatedInstance]</code> <p>Instance to check. Or None for context.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <p>True if instance matches plugin families.</p> Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>@classmethod\ndef instance_matches_plugin_families(\n    cls, instance: Optional[\"CreatedInstance\"]\n):\n    \"\"\"Check if instance matches families.\n\n    Args:\n        instance (Optional[CreatedInstance]): Instance to check. Or None\n            for context.\n\n    Returns:\n        bool: True if instance matches plugin families.\n\n    \"\"\"\n    if instance is None:\n        return not cls.__instanceEnabled__\n\n    if not cls.__instanceEnabled__:\n        return False\n\n    families = [instance.product_base_type]\n    families.extend(instance.get(\"families\", []))\n    for _ in pyblish.logic.plugins_by_families([cls], families):\n        return True\n    return False\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/publish_plugins.html#client.ayon_core.pipeline.publish.publish_plugins.AYONPyblishPluginMixin.register_create_context_callbacks","title":"<code>register_create_context_callbacks(create_context)</code>  <code>classmethod</code>","text":"<p>Register callbacks for create context.</p> <p>It is possible to register callbacks listening to changes happened in create context.</p> <p>Methods available on create context: - add_instances_added_callback - add_instances_removed_callback - add_value_changed_callback - add_pre_create_attr_defs_change_callback - add_create_attr_defs_change_callback - add_publish_attr_defs_change_callback</p> <p>Parameters:</p> Name Type Description Default <code>create_context</code> <code>CreateContext</code> <p>Create context.</p> required Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>@classmethod\ndef register_create_context_callbacks(\n    cls, create_context: \"CreateContext\"\n):\n    \"\"\"Register callbacks for create context.\n\n    It is possible to register callbacks listening to changes happened\n    in create context.\n\n    Methods available on create context:\n    - add_instances_added_callback\n    - add_instances_removed_callback\n    - add_value_changed_callback\n    - add_pre_create_attr_defs_change_callback\n    - add_create_attr_defs_change_callback\n    - add_publish_attr_defs_change_callback\n\n    Args:\n        create_context (CreateContext): Create context.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/publish_plugins.html#client.ayon_core.pipeline.publish.publish_plugins.ColormanagedPyblishPluginMixin","title":"<code>ColormanagedPyblishPluginMixin</code>","text":"<p>               Bases: <code>object</code></p> <p>Mixin for colormanaged plugins.</p> <p>This class is used to set colorspace data to a publishing representation. It contains a static method, get_colorspace_settings, which returns config and file rules data for the host context. It also contains a method, set_representation_colorspace, which sets colorspace data to the representation. The allowed file extensions are listed in the allowed_ext variable. The method first checks if the file extension is in the list of allowed extensions. If it is, it then gets the colorspace settings from the host context and gets a matching colorspace from rules. Finally, it infuses this data into the representation.</p> Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>class ColormanagedPyblishPluginMixin(object):\n    \"\"\"Mixin for colormanaged plugins.\n\n    This class is used to set colorspace data to a publishing\n    representation. It contains a static method,\n    get_colorspace_settings, which returns config and\n    file rules data for the host context.\n    It also contains a method, set_representation_colorspace,\n    which sets colorspace data to the representation.\n    The allowed file extensions are listed in the allowed_ext variable.\n    The method first checks if the file extension is in\n    the list of allowed extensions. If it is, it then gets the\n    colorspace settings from the host context and gets a\n    matching colorspace from rules. Finally, it infuses this\n    data into the representation.\n    \"\"\"\n\n    def get_colorspace_settings(self, context):\n        \"\"\"Returns solved settings for the host context.\n\n        Args:\n            context (publish.Context): publishing context\n\n        Returns:\n            tuple | bool: config, file rules or None\n        \"\"\"\n        return get_colorspace_settings_from_publish_context(context.data)\n\n    def set_representation_colorspace(\n        self, representation, context,\n        colorspace=None,\n    ):\n        \"\"\"Sets colorspace data to representation.\n\n        Args:\n            representation (dict): publishing representation\n            context (publish.Context): publishing context\n            colorspace (str, optional): colorspace name. Defaults to None.\n\n        Example:\n            ```\n            {\n                # for other publish plugins and loaders\n                \"colorspace\": \"linear\",\n                \"config\": {\n                    # for future references in case need\n                    \"path\": \"/abs/path/to/config.ocio\",\n                    # for other plugins within remote publish cases\n                    \"template\": \"{project[root]}/path/to/config.ocio\"\n                }\n            }\n            ```\n\n        \"\"\"\n\n        # using cached settings if available\n        set_colorspace_data_to_representation(\n            representation, context.data,\n            colorspace,\n            log=self.log\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/publish_plugins.html#client.ayon_core.pipeline.publish.publish_plugins.ColormanagedPyblishPluginMixin.get_colorspace_settings","title":"<code>get_colorspace_settings(context)</code>","text":"<p>Returns solved settings for the host context.</p> <p>Parameters:</p> Name Type Description Default <code>context</code> <code>Context</code> <p>publishing context</p> required <p>Returns:</p> Type Description <p>tuple | bool: config, file rules or None</p> Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>def get_colorspace_settings(self, context):\n    \"\"\"Returns solved settings for the host context.\n\n    Args:\n        context (publish.Context): publishing context\n\n    Returns:\n        tuple | bool: config, file rules or None\n    \"\"\"\n    return get_colorspace_settings_from_publish_context(context.data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/publish_plugins.html#client.ayon_core.pipeline.publish.publish_plugins.ColormanagedPyblishPluginMixin.set_representation_colorspace","title":"<code>set_representation_colorspace(representation, context, colorspace=None)</code>","text":"<p>Sets colorspace data to representation.</p> <p>Parameters:</p> Name Type Description Default <code>representation</code> <code>dict</code> <p>publishing representation</p> required <code>context</code> <code>Context</code> <p>publishing context</p> required <code>colorspace</code> <code>str</code> <p>colorspace name. Defaults to None.</p> <code>None</code> Example <pre><code>{\n    # for other publish plugins and loaders\n    \"colorspace\": \"linear\",\n    \"config\": {\n        # for future references in case need\n        \"path\": \"/abs/path/to/config.ocio\",\n        # for other plugins within remote publish cases\n        \"template\": \"{project[root]}/path/to/config.ocio\"\n    }\n}\n</code></pre> Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>def set_representation_colorspace(\n    self, representation, context,\n    colorspace=None,\n):\n    \"\"\"Sets colorspace data to representation.\n\n    Args:\n        representation (dict): publishing representation\n        context (publish.Context): publishing context\n        colorspace (str, optional): colorspace name. Defaults to None.\n\n    Example:\n        ```\n        {\n            # for other publish plugins and loaders\n            \"colorspace\": \"linear\",\n            \"config\": {\n                # for future references in case need\n                \"path\": \"/abs/path/to/config.ocio\",\n                # for other plugins within remote publish cases\n                \"template\": \"{project[root]}/path/to/config.ocio\"\n            }\n        }\n        ```\n\n    \"\"\"\n\n    # using cached settings if available\n    set_colorspace_data_to_representation(\n        representation, context.data,\n        colorspace,\n        log=self.log\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/publish_plugins.html#client.ayon_core.pipeline.publish.publish_plugins.Extractor","title":"<code>Extractor</code>","text":"<p>               Bases: <code>InstancePlugin</code></p> <p>Extractor base class.</p> <p>The extractor base class implements a \"staging_dir\" function used to generate a temporary directory for an instance to extract to.</p> <p>This temporary directory is generated through <code>tempfile.mkdtemp()</code></p> Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>class Extractor(pyblish.api.InstancePlugin):\n    \"\"\"Extractor base class.\n\n    The extractor base class implements a \"staging_dir\" function used to\n    generate a temporary directory for an instance to extract to.\n\n    This temporary directory is generated through `tempfile.mkdtemp()`\n\n    \"\"\"\n\n    order = 2.0\n\n    def staging_dir(self, instance):\n        \"\"\"Provide a temporary directory in which to store extracted files\n\n        Upon calling this method the staging directory is stored inside\n        the instance.data['stagingDir']\n        \"\"\"\n\n        return get_instance_staging_dir(instance)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/publish_plugins.html#client.ayon_core.pipeline.publish.publish_plugins.Extractor.staging_dir","title":"<code>staging_dir(instance)</code>","text":"<p>Provide a temporary directory in which to store extracted files</p> <p>Upon calling this method the staging directory is stored inside the instance.data['stagingDir']</p> Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>def staging_dir(self, instance):\n    \"\"\"Provide a temporary directory in which to store extracted files\n\n    Upon calling this method the staging directory is stored inside\n    the instance.data['stagingDir']\n    \"\"\"\n\n    return get_instance_staging_dir(instance)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/publish_plugins.html#client.ayon_core.pipeline.publish.publish_plugins.KnownPublishError","title":"<code>KnownPublishError</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Publishing crashed because of known error.</p> <p>Artist can't affect source of the error.</p> Deprecated <p>Please use <code>PublishError</code> instead. Marked as deprecated 24/09/02.</p> Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>class KnownPublishError(Exception):\n    \"\"\"Publishing crashed because of known error.\n\n    Artist can't affect source of the error.\n\n    Deprecated:\n        Please use `PublishError` instead. Marked as deprecated 24/09/02.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/publish_plugins.html#client.ayon_core.pipeline.publish.publish_plugins.OptionalPyblishPluginMixin","title":"<code>OptionalPyblishPluginMixin</code>","text":"<p>               Bases: <code>AYONPyblishPluginMixin</code></p> <p>Prepare mixin for optional plugins.</p> <p>Defined active attribute definition prepared for published and prepares method which will check if is active or not.</p> <pre><code>class ValidateScene(\n    pyblish.api.InstancePlugin, OptionalPyblishPluginMixin\n):\n    def process(self, instance):\n        # Skip the instance if is not active by data on the instance\n        if not self.is_active(instance.data):\n            return\n</code></pre> Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>class OptionalPyblishPluginMixin(AYONPyblishPluginMixin):\n    \"\"\"Prepare mixin for optional plugins.\n\n    Defined active attribute definition prepared for published and\n    prepares method which will check if is active or not.\n\n    ```\n    class ValidateScene(\n        pyblish.api.InstancePlugin, OptionalPyblishPluginMixin\n    ):\n        def process(self, instance):\n            # Skip the instance if is not active by data on the instance\n            if not self.is_active(instance.data):\n                return\n    ```\n    \"\"\"\n\n    # Allow exposing tooltip from class with `optional_tooltip` attribute\n    optional_tooltip: Optional[str] = None\n\n    @classmethod\n    def get_attribute_defs(cls):\n        \"\"\"Attribute definitions based on plugin's optional attribute.\"\"\"\n\n        # Empty list if plugin is not optional\n        if not getattr(cls, \"optional\", None):\n            return []\n\n        # Get active value from class as default value\n        active = getattr(cls, \"active\", True)\n        # Return boolean stored under 'active' key with label of the class name\n        label = cls.label or cls.__name__\n\n        return [\n            BoolDef(\n                \"active\",\n                default=active,\n                label=label,\n                tooltip=cls.optional_tooltip,\n            )\n        ]\n\n    def is_active(self, data):\n        \"\"\"Check if plugins is active for instance/context based on their data.\n\n        Args:\n            data(dict): Data from instance or context.\n        \"\"\"\n        # Skip if is not optional and return True\n        if not getattr(self, \"optional\", None):\n            return True\n        attr_values = self.get_attr_values_from_data(data)\n        active = attr_values.get(\"active\")\n        if active is None:\n            active = getattr(self, \"active\", True)\n        return active\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/publish_plugins.html#client.ayon_core.pipeline.publish.publish_plugins.OptionalPyblishPluginMixin.get_attribute_defs","title":"<code>get_attribute_defs()</code>  <code>classmethod</code>","text":"<p>Attribute definitions based on plugin's optional attribute.</p> Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>@classmethod\ndef get_attribute_defs(cls):\n    \"\"\"Attribute definitions based on plugin's optional attribute.\"\"\"\n\n    # Empty list if plugin is not optional\n    if not getattr(cls, \"optional\", None):\n        return []\n\n    # Get active value from class as default value\n    active = getattr(cls, \"active\", True)\n    # Return boolean stored under 'active' key with label of the class name\n    label = cls.label or cls.__name__\n\n    return [\n        BoolDef(\n            \"active\",\n            default=active,\n            label=label,\n            tooltip=cls.optional_tooltip,\n        )\n    ]\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/publish_plugins.html#client.ayon_core.pipeline.publish.publish_plugins.OptionalPyblishPluginMixin.is_active","title":"<code>is_active(data)</code>","text":"<p>Check if plugins is active for instance/context based on their data.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict</code> <p>Data from instance or context.</p> required Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>def is_active(self, data):\n    \"\"\"Check if plugins is active for instance/context based on their data.\n\n    Args:\n        data(dict): Data from instance or context.\n    \"\"\"\n    # Skip if is not optional and return True\n    if not getattr(self, \"optional\", None):\n        return True\n    attr_values = self.get_attr_values_from_data(data)\n    active = attr_values.get(\"active\")\n    if active is None:\n        active = getattr(self, \"active\", True)\n    return active\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/publish_plugins.html#client.ayon_core.pipeline.publish.publish_plugins.PublishError","title":"<code>PublishError</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Publishing crashed because of known error.</p> <p>Message will be shown in UI for artist.</p> <p>Parameters:</p> Name Type Description Default <code>message</code> <code>str</code> <p>Message of error. Short explanation an issue.</p> required <code>title</code> <code>Optional[str]</code> <p>Title showed in UI.</p> <code>None</code> <code>description</code> <code>Optional[str]</code> <p>Detailed description of an error. It is possible to use Markdown syntax.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>class PublishError(Exception):\n    \"\"\"Publishing crashed because of known error.\n\n    Message will be shown in UI for artist.\n\n    Args:\n        message (str): Message of error. Short explanation an issue.\n        title (Optional[str]): Title showed in UI.\n        description (Optional[str]): Detailed description of an error.\n            It is possible to use Markdown syntax.\n\n    \"\"\"\n    def __init__(self, message, title=None, description=None, detail=None):\n        self.message = message\n        self.title = title\n        self.description = description or message\n        self.detail = detail\n        super().__init__(message)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/publish_plugins.html#client.ayon_core.pipeline.publish.publish_plugins.PublishValidationError","title":"<code>PublishValidationError</code>","text":"<p>               Bases: <code>PublishError</code></p> <p>Validation error happened during publishing.</p> <p>This exception should be used when validation publishing failed.</p> <p>Publishing does not stop during validation order if this     exception is raised.</p> <p>Has additional UI specific attributes that may be handy for artist.</p> <p>Argument 'title' is used to group errors.</p> Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>class PublishValidationError(PublishError):\n    \"\"\"Validation error happened during publishing.\n\n    This exception should be used when validation publishing failed.\n\n    Publishing does not stop during validation order if this\n        exception is raised.\n\n    Has additional UI specific attributes that may be handy for artist.\n\n    Argument 'title' is used to group errors.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/publish_plugins.html#client.ayon_core.pipeline.publish.publish_plugins.PublishXmlValidationError","title":"<code>PublishXmlValidationError</code>","text":"<p>               Bases: <code>PublishValidationError</code></p> <p>Raise an error from a dedicated xml file.</p> <p>Can be useful to have one xml file with different possible messages that     helps to avoid flood code with dedicated artist messages.</p> XML files should live relative to the plugin file location <p>'{plugin dir}/help/some_plugin.xml'.</p> <p>Parameters:</p> Name Type Description Default <code>plugin</code> <code>Plugin</code> <p>Plugin that raised an error. Is used to get path to xml file.</p> required <code>message</code> <code>str</code> <p>Exception message, can be technical, is used for console output.</p> required <code>key</code> <code>Optional[str]</code> <p>XML file can contain multiple error messages, key is used to get one of them. By default is used 'main'.</p> <code>None</code> <code>formatting_data</code> <code>Optional[dict[str, Any]</code> <p>Error message can have variables to fill.</p> <code>None</code> <code>help_filename</code> <code>Optional[str]</code> <p>Name of xml file with messages. By default, is used filename where plugin lives with .xml extension.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>class PublishXmlValidationError(PublishValidationError):\n    \"\"\"Raise an error from a dedicated xml file.\n\n    Can be useful to have one xml file with different possible messages that\n        helps to avoid flood code with dedicated artist messages.\n\n    XML files should live relative to the plugin file location:\n        '{plugin dir}/help/some_plugin.xml'.\n\n    Args:\n        plugin (pyblish.api.Plugin): Plugin that raised an error. Is used\n            to get path to xml file.\n        message (str): Exception message, can be technical, is used for\n            console output.\n        key (Optional[str]): XML file can contain multiple error messages, key\n            is used to get one of them. By default is used 'main'.\n        formatting_data (Optional[dict[str, Any]): Error message can have\n            variables to fill.\n        help_filename (Optional[str]): Name of xml file with messages. By\n            default, is used filename where plugin lives with .xml extension.\n\n    \"\"\"\n    def __init__(\n        self,\n        plugin: pyblish.api.Plugin,\n        message: str,\n        key: Optional[str] = None,\n        formatting_data: Optional[dict[str, Any]] = None,\n        help_filename: Optional[str] = None,\n    ) -&gt; None:\n        if key is None:\n            key = \"main\"\n\n        if not formatting_data:\n            formatting_data = {}\n        result = load_help_content_from_plugin(plugin, help_filename)\n        content_obj = result[\"errors\"][key]\n        description = content_obj.description.format(**formatting_data)\n        detail = content_obj.detail\n        if detail:\n            detail = detail.format(**formatting_data)\n        super().__init__(\n            message,\n            content_obj.title,\n            description,\n            detail\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/publish_plugins.html#client.ayon_core.pipeline.publish.publish_plugins.RepairAction","title":"<code>RepairAction</code>","text":"<p>               Bases: <code>Action</code></p> <p>Repairs the action</p> <p>To process the repairing this requires a static <code>repair(instance)</code> method is available on the plugin.</p> Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>class RepairAction(pyblish.api.Action):\n    \"\"\"Repairs the action\n\n    To process the repairing this requires a static `repair(instance)` method\n    is available on the plugin.\n    \"\"\"\n\n    label = \"Repair\"\n    on = \"failed\"  # This action is only available on a failed plug-in\n    icon = \"wrench\"  # Icon from Awesome Icon\n\n    def process(self, context, plugin):\n        if not hasattr(plugin, \"repair\"):\n            raise RuntimeError(\"Plug-in does not have repair method.\")\n\n        # Get the errored instances\n        self.log.debug(\"Finding failed instances..\")\n        errored_instances = get_errored_instances_from_context(context,\n                                                               plugin=plugin)\n        for instance in errored_instances:\n            self.log.debug(\n                \"Attempting repair for instance: {} ...\".format(instance)\n            )\n            plugin.repair(instance)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/publish_plugins.html#client.ayon_core.pipeline.publish.publish_plugins.RepairContextAction","title":"<code>RepairContextAction</code>","text":"<p>               Bases: <code>Action</code></p> <p>Repairs the action</p> <p>To process the repairing this requires a static <code>repair(context)</code> method is available on the plugin.</p> Source code in <code>client/ayon_core/pipeline/publish/publish_plugins.py</code> <pre><code>class RepairContextAction(pyblish.api.Action):\n    \"\"\"Repairs the action\n\n    To process the repairing this requires a static `repair(context)` method\n    is available on the plugin.\n    \"\"\"\n\n    label = \"Repair\"\n    on = \"failed\"  # This action is only available on a failed plug-in\n    icon = \"wrench\"  # Icon from Awesome Icon\n\n    def process(self, context, plugin):\n        if not hasattr(plugin, \"repair\"):\n            raise RuntimeError(\"Plug-in does not have repair method.\")\n\n        # Get the failed instances\n        self.log.debug(\"Finding failed plug-ins..\")\n        failed_plugins = get_errored_plugins_from_context(context)\n\n        # Apply pyblish.logic to get the instances for the plug-in\n        if plugin in failed_plugins:\n            self.log.debug(\"Attempting repair ...\")\n            plugin.repair(context)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/report.html","title":"report","text":""},{"location":"autoapi/client/ayon_core/pipeline/publish/report.html#client.ayon_core.pipeline.publish.report.PublishPluginReportInfo","title":"<code>PublishPluginReportInfo</code>  <code>dataclass</code>","text":"<p>Information about single plugin in publish process.</p> Source code in <code>client/ayon_core/pipeline/publish/report.py</code> <pre><code>@dataclass\nclass PublishPluginReportInfo:\n    \"\"\"Information about single plugin in publish process.\"\"\"\n    id: str\n    name: str\n    label: str\n    order: float\n    filepath: str\n    docstring: str | None\n    plugin_type: Literal[\"instance\", \"context\"]\n    families: list[str]\n    targets: list[str]\n    skipped: bool = False\n    passed: bool = False\n    process_reports: list[PublishProcessReport] = field(\n        default_factory=list\n    )\n    actions_reports: list[PublishPluginActionReport] = field(\n        default_factory=list\n    )\n\n    def to_data(self, current_plugin_id: str | None = None) -&gt; dict[str, Any]:\n        passed = self.passed\n        if current_plugin_id == self.id:\n            passed = True\n        return {\n            \"id\": self.id,\n            \"name\": self.name,\n            \"label\": self.label,\n            \"order\": self.order,\n            \"filepath\": self.filepath,\n            \"docstring\": self.docstring,\n            \"plugin_type\": self.plugin_type,\n            \"families\": self.families,\n            \"targets\": self.targets,\n            \"instances_data\": [r.to_data() for r in self.process_reports],\n            \"actions_data\": [r.to_data() for r in self.actions_reports],\n            \"skipped\": self.skipped,\n            \"passed\": passed,\n        }\n\n    @classmethod\n    def from_data(cls, data: dict[str, Any]) -&gt; PublishPluginReportInfo:\n        return cls(\n            id=data[\"id\"],\n            name=data[\"name\"],\n            label=data[\"label\"],\n            order=data[\"order\"],\n            filepath=data[\"filepath\"],\n            docstring=data.get(\"docstring\"),\n            plugin_type=data[\"plugin_type\"],\n            families=data[\"families\"],\n            targets=data[\"targets\"],\n            skipped=data.get(\"skipped\", False),\n            passed=data.get(\"passed\", False),\n            process_reports=[\n                PublishProcessReport.from_data(r)\n                for r in data.get(\"instances_data\", [])\n            ],\n            actions_reports=[\n                PublishPluginActionReport.from_data(action_data)\n                for action_data in data[\"actions_data\"]\n            ],\n        )\n\n    @classmethod\n    def from_plugin(\n        cls, plugin: pyblish.api.Plugin\n    ) -&gt; PublishPluginReportInfo:\n        label = None\n        if hasattr(plugin, \"label\"):\n            label = plugin.label\n\n        if not label:\n            label = plugin.__name__\n\n        plugin_type = \"instance\" if plugin.__instanceEnabled__ else \"context\"\n        # Get docstring\n        # NOTE we do care only about docstring from the plugin so we can't\n        #   use 'inspect.getdoc' which also looks for docstring in parent\n        #   classes.\n        docstring = getattr(plugin, \"__doc__\", None)\n        if docstring:\n            docstring = inspect.cleandoc(docstring)\n        return cls(\n            id=plugin.id,\n            name=plugin.__name__,\n            label=label,\n            order=plugin.order,\n            filepath=inspect.getfile(plugin),\n            docstring=docstring,\n            plugin_type=plugin_type,\n            families=list(plugin.families),\n            targets=list(plugin.targets),\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/report.html#client.ayon_core.pipeline.publish.report.PublishReportMaker","title":"<code>PublishReportMaker</code>","text":"<p>Report for single publishing process.</p> <p>Report keeps current state of publishing and currently processed plugin.</p> Source code in <code>client/ayon_core/pipeline/publish/report.py</code> <pre><code>class PublishReportMaker:\n    \"\"\"Report for single publishing process.\n\n    Report keeps current state of publishing and currently processed plugin.\n    \"\"\"\n    def __init__(\n        self,\n        publish_plugins: list[pyblish.api.Plugin] | None = None,\n        crashed_filepaths: dict[str, str] | None = None,\n        blocking_crashed_paths: set[str] | None = None,\n    ) -&gt; None:\n        if publish_plugins is None:\n            publish_plugins = []\n\n        if crashed_filepaths is None:\n            crashed_filepaths = {}\n\n        if blocking_crashed_paths is None:\n            blocking_crashed_paths = set()\n\n        # Make sure plugins are sorted\n        # TODO try to comment this to find out if it is necessary...\n        publish_plugins.sort(key=lambda p: p.order)\n\n        # Track information if instances have been updated since\n        #   last report generation.\n        self._instances_updated: bool = False\n\n        # Current plugin id which is being processed.\n        self._current_plugin_id: str | None = None\n\n        self._plugin_info_by_id: dict[str, PublishPluginReportInfo] = {}\n        self._all_instances_by_id: dict[str, pyblish.api.Instance] = {}\n        self._report = PublishReport(\n            crashed_filepaths=crashed_filepaths,\n            blocking_crashed_paths=list(blocking_crashed_paths),\n        )\n        self._prepare_publish_plugin_items(publish_plugins)\n\n    @property\n    def report_version(self) -&gt; str:\n        return self._report.version\n\n    def get_current_plugin_id(self) -&gt; str | None:\n        return self._current_plugin_id\n\n    current_plugin_id = property(get_current_plugin_id)\n\n    def reset(\n        self,\n        publish_plugins: list[pyblish.api.Plugin] | None = None,\n        crashed_filepaths: dict[str, str] | None = None,\n        blocking_crashed_paths: set[str] | None = None,\n    ) -&gt; None:\n        \"\"\"Reset report and clear all data.\"\"\"\n        if publish_plugins is None:\n            publish_plugins = []\n\n        if crashed_filepaths is None:\n            crashed_filepaths = {}\n\n        if blocking_crashed_paths is None:\n            blocking_crashed_paths = set()\n\n        self._instances_updated = False\n        self._current_plugin_id = None\n        self._plugin_info_by_id = {}\n        self._all_instances_by_id = {}\n\n        self._report = PublishReport(\n            crashed_filepaths=crashed_filepaths,\n            blocking_crashed_paths=list(blocking_crashed_paths),\n        )\n\n        self._prepare_publish_plugin_items(publish_plugins)\n\n    def reset_with_discover_results(\n        self,\n        creator_discover_result: DiscoverResult,\n        convertor_discover_result: DiscoverResult,\n        publish_discover_result: DiscoverResult,\n        blocking_crashed_paths: set[str],\n    ) -&gt; None:\n        \"\"\"Reset report and set discover results.\"\"\"\n\n        crashed_filepaths = {}\n        for report in (\n            creator_discover_result,\n            convertor_discover_result,\n            publish_discover_result,\n        ):\n            items = report.crashed_file_paths.items()\n            for filepath, exc_info in items:\n                crashed_filepaths[filepath] = \"\".join(\n                    traceback.format_exception(*exc_info)\n                )\n\n        self.reset(\n            publish_discover_result.plugins,\n            crashed_filepaths,\n            blocking_crashed_paths,\n        )\n\n    def add_plugin_iter(\n        self, plugin_id: str, context: pyblish.api.Context\n    ) -&gt; None:\n        \"\"\"Add report about single iteration of plugin.\"\"\"\n        self.update_publish_instances(context)\n        context_label = context.data.get(\"label\")\n        if context_label:\n            self._report.context.label = context_label\n\n        self._current_plugin_id = plugin_id\n\n    def set_plugin_passed(self, plugin_id: str) -&gt; None:\n        plugin_info = self._plugin_info_by_id[plugin_id]\n        plugin_info.passed = True\n\n    def set_plugin_skipped(self, plugin_id: str) -&gt; None:\n        \"\"\"Set that current plugin has been skipped.\"\"\"\n        plugin_info = self._plugin_info_by_id[plugin_id]\n        plugin_info.skipped = True\n\n    def add_result(self, plugin_id: str, result: dict[str, Any]) -&gt; None:\n        \"\"\"Handle result of one plugin and it's instance.\"\"\"\n        plugin_info = self._plugin_info_by_id[plugin_id]\n        plugin_info.process_reports.append(\n            PublishProcessReport.from_result(result)\n        )\n\n    def add_action_result(\n        self, action: pyblish.api.Action, result: dict[str, Any]\n    ) -&gt; None:\n        \"\"\"Add result of single action.\"\"\"\n        plugin = result[\"plugin\"]\n\n        plugin_info = self._plugin_info_by_id[plugin.id]\n\n        action_name = action.__name__\n        action_label = action.label or action_name\n        plugin_info.actions_reports.append(PublishPluginActionReport(\n            success=result[\"success\"],\n            name=action_name,\n            label=action_label,\n            logs=ReportLog.log_items_from_result(result),\n        ))\n\n    def get_report(self) -&gt; PublishReport:\n        self._update_instances()\n        return self._report\n\n    def get_report_data(self) -&gt; dict[str, Any]:\n        self._update_instances()\n        return self._report.to_data(\n            current_plugin_id=self._current_plugin_id,\n        )\n\n    def update_publish_instances(\n        self, context: pyblish.api.Context\n    ) -&gt; None:\n        \"\"\"Report data with all details of current state.\"\"\"\n        for instance in context:\n            if instance.id not in self._all_instances_by_id:\n                self._instances_updated = False\n                self._all_instances_by_id[instance.id] = instance\n\n    def _update_instances(self) -&gt; None:\n        if self._instances_updated:\n            return\n\n        self._report.instances_by_id = {\n            instance.id: PublishInstanceInfo.from_instance(instance)\n            for instance in self._all_instances_by_id.values()\n        }\n        self._instances_updated = True\n\n    def _prepare_publish_plugin_items(\n        self, publish_plugins: list[pyblish.api.Plugin]\n    ) -&gt; None:\n        for plugin in publish_plugins:\n            if plugin.id in self._plugin_info_by_id:\n                # A plugin would be processed more than once.\n                #   What can cause it:\n                #   - there is a bug in iteration logic\n                #   - plugin class is imported into multiple files\n                #       - this can happen even with base classes from\n                #            'pyblish'\n                raise ValueError(\n                    f\"Plugin '{plugin}' is already stored\"\n                )\n\n            plugin_info = PublishPluginReportInfo.from_plugin(plugin)\n            self._plugin_info_by_id[plugin.id] = plugin_info\n            self._report.plugins_info.append(plugin_info)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/report.html#client.ayon_core.pipeline.publish.report.PublishReportMaker.add_action_result","title":"<code>add_action_result(action, result)</code>","text":"<p>Add result of single action.</p> Source code in <code>client/ayon_core/pipeline/publish/report.py</code> <pre><code>def add_action_result(\n    self, action: pyblish.api.Action, result: dict[str, Any]\n) -&gt; None:\n    \"\"\"Add result of single action.\"\"\"\n    plugin = result[\"plugin\"]\n\n    plugin_info = self._plugin_info_by_id[plugin.id]\n\n    action_name = action.__name__\n    action_label = action.label or action_name\n    plugin_info.actions_reports.append(PublishPluginActionReport(\n        success=result[\"success\"],\n        name=action_name,\n        label=action_label,\n        logs=ReportLog.log_items_from_result(result),\n    ))\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/report.html#client.ayon_core.pipeline.publish.report.PublishReportMaker.add_plugin_iter","title":"<code>add_plugin_iter(plugin_id, context)</code>","text":"<p>Add report about single iteration of plugin.</p> Source code in <code>client/ayon_core/pipeline/publish/report.py</code> <pre><code>def add_plugin_iter(\n    self, plugin_id: str, context: pyblish.api.Context\n) -&gt; None:\n    \"\"\"Add report about single iteration of plugin.\"\"\"\n    self.update_publish_instances(context)\n    context_label = context.data.get(\"label\")\n    if context_label:\n        self._report.context.label = context_label\n\n    self._current_plugin_id = plugin_id\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/report.html#client.ayon_core.pipeline.publish.report.PublishReportMaker.add_result","title":"<code>add_result(plugin_id, result)</code>","text":"<p>Handle result of one plugin and it's instance.</p> Source code in <code>client/ayon_core/pipeline/publish/report.py</code> <pre><code>def add_result(self, plugin_id: str, result: dict[str, Any]) -&gt; None:\n    \"\"\"Handle result of one plugin and it's instance.\"\"\"\n    plugin_info = self._plugin_info_by_id[plugin_id]\n    plugin_info.process_reports.append(\n        PublishProcessReport.from_result(result)\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/report.html#client.ayon_core.pipeline.publish.report.PublishReportMaker.reset","title":"<code>reset(publish_plugins=None, crashed_filepaths=None, blocking_crashed_paths=None)</code>","text":"<p>Reset report and clear all data.</p> Source code in <code>client/ayon_core/pipeline/publish/report.py</code> <pre><code>def reset(\n    self,\n    publish_plugins: list[pyblish.api.Plugin] | None = None,\n    crashed_filepaths: dict[str, str] | None = None,\n    blocking_crashed_paths: set[str] | None = None,\n) -&gt; None:\n    \"\"\"Reset report and clear all data.\"\"\"\n    if publish_plugins is None:\n        publish_plugins = []\n\n    if crashed_filepaths is None:\n        crashed_filepaths = {}\n\n    if blocking_crashed_paths is None:\n        blocking_crashed_paths = set()\n\n    self._instances_updated = False\n    self._current_plugin_id = None\n    self._plugin_info_by_id = {}\n    self._all_instances_by_id = {}\n\n    self._report = PublishReport(\n        crashed_filepaths=crashed_filepaths,\n        blocking_crashed_paths=list(blocking_crashed_paths),\n    )\n\n    self._prepare_publish_plugin_items(publish_plugins)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/report.html#client.ayon_core.pipeline.publish.report.PublishReportMaker.reset_with_discover_results","title":"<code>reset_with_discover_results(creator_discover_result, convertor_discover_result, publish_discover_result, blocking_crashed_paths)</code>","text":"<p>Reset report and set discover results.</p> Source code in <code>client/ayon_core/pipeline/publish/report.py</code> <pre><code>def reset_with_discover_results(\n    self,\n    creator_discover_result: DiscoverResult,\n    convertor_discover_result: DiscoverResult,\n    publish_discover_result: DiscoverResult,\n    blocking_crashed_paths: set[str],\n) -&gt; None:\n    \"\"\"Reset report and set discover results.\"\"\"\n\n    crashed_filepaths = {}\n    for report in (\n        creator_discover_result,\n        convertor_discover_result,\n        publish_discover_result,\n    ):\n        items = report.crashed_file_paths.items()\n        for filepath, exc_info in items:\n            crashed_filepaths[filepath] = \"\".join(\n                traceback.format_exception(*exc_info)\n            )\n\n    self.reset(\n        publish_discover_result.plugins,\n        crashed_filepaths,\n        blocking_crashed_paths,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/report.html#client.ayon_core.pipeline.publish.report.PublishReportMaker.set_plugin_skipped","title":"<code>set_plugin_skipped(plugin_id)</code>","text":"<p>Set that current plugin has been skipped.</p> Source code in <code>client/ayon_core/pipeline/publish/report.py</code> <pre><code>def set_plugin_skipped(self, plugin_id: str) -&gt; None:\n    \"\"\"Set that current plugin has been skipped.\"\"\"\n    plugin_info = self._plugin_info_by_id[plugin_id]\n    plugin_info.skipped = True\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/publish/report.html#client.ayon_core.pipeline.publish.report.PublishReportMaker.update_publish_instances","title":"<code>update_publish_instances(context)</code>","text":"<p>Report data with all details of current state.</p> Source code in <code>client/ayon_core/pipeline/publish/report.py</code> <pre><code>def update_publish_instances(\n    self, context: pyblish.api.Context\n) -&gt; None:\n    \"\"\"Report data with all details of current state.\"\"\"\n    for instance in context:\n        if instance.id not in self._all_instances_by_id:\n            self._instances_updated = False\n            self._all_instances_by_id[instance.id] = instance\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/schema/index.html","title":"schema","text":"<p>Wrapper around :mod:<code>jsonschema</code></p> <p>Schemas are implicitly loaded from the /schema directory of this project.</p> <p>Attributes:</p> Name Type Description <code>_cache</code> <p>Cache of previously loaded schemas</p> Resources <p>http://json-schema.org/ http://json-schema.org/latest/json-schema-core.html http://spacetelescope.github.io/understanding-json-schema/index.html</p>"},{"location":"autoapi/client/ayon_core/pipeline/schema/index.html#client.ayon_core.pipeline.schema.validate","title":"<code>validate(data, schema=None)</code>","text":"<p>Validate <code>data</code> with <code>schema</code></p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict</code> <p>JSON-compatible data</p> required <code>schema</code> <code>str</code> <p>DEPRECATED Name of schema. Now included in the data.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/schema/__init__.py</code> <pre><code>def validate(data, schema=None):\n    \"\"\"Validate `data` with `schema`\n\n    Arguments:\n        data (dict): JSON-compatible data\n        schema (str): DEPRECATED Name of schema. Now included in the data.\n\n    Raises:\n        ValidationError on invalid schema\n\n    \"\"\"\n    if not _CACHED:\n        _precache()\n\n    _root, schema = data[\"schema\"].rsplit(\":\", 1)\n\n    if isinstance(schema, str):\n        schema = _cache[schema + \".json\"]\n\n    resolver = jsonschema.RefResolver(\n        \"\",\n        None,\n        store=_cache,\n        cache_remote=True\n    )\n\n    jsonschema.validate(data,\n                        schema,\n                        types={\"array\": (list, tuple)},\n                        resolver=resolver)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html","title":"traits","text":"<p>Trait classes for the pipeline.</p>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Bundle","title":"<code>Bundle</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>Bundle trait model.</p> <p>This model list of independent Representation traits that are bundled together. This is useful for representing a collection of sub-entities that are part of a single entity. You can easily reconstruct representations from the bundle.</p> <p>Example::</p> <pre><code>    Bundle(\n        items=[\n            [\n                MimeType(mime_type=\"image/jpeg\"),\n                FileLocation(file_path=\"/path/to/file.jpg\")\n            ],\n            [\n\n                MimeType(mime_type=\"image/png\"),\n                FileLocation(file_path=\"/path/to/file.png\")\n            ]\n        ]\n    )\n</code></pre> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Trait name.</p> <code>description</code> <code>str</code> <p>Trait description.</p> <code>id</code> <code>str</code> <p>id should be a namespaced trait name with version</p> <code>items</code> <code>list[list[TraitBase]]</code> <p>List of representations.</p> <code>persistent</code> <code>bool</code> <p>Should the mime type be stored with the representation on the server or not.</p> Source code in <code>client/ayon_core/pipeline/traits/content.py</code> <pre><code>@dataclass\nclass Bundle(TraitBase):\n    \"\"\"Bundle trait model.\n\n    This model list of independent Representation traits\n    that are bundled together. This is useful for representing\n    a collection of sub-entities that are part of a single\n    entity. You can easily reconstruct representations from\n    the bundle.\n\n    Example::\n\n            Bundle(\n                items=[\n                    [\n                        MimeType(mime_type=\"image/jpeg\"),\n                        FileLocation(file_path=\"/path/to/file.jpg\")\n                    ],\n                    [\n\n                        MimeType(mime_type=\"image/png\"),\n                        FileLocation(file_path=\"/path/to/file.png\")\n                    ]\n                ]\n            )\n\n    Attributes:\n        name (str): Trait name.\n        description (str): Trait description.\n        id (str): id should be a namespaced trait name with version\n        items (list[list[TraitBase]]): List of representations.\n        persistent (bool): Should the mime type be stored with the\n            representation on the server or not.\n    \"\"\"\n\n    name: ClassVar[str] = \"Bundle\"\n    description: ClassVar[str] = \"Bundle Trait\"\n    id: ClassVar[str] = \"ayon.content.Bundle.v1\"\n    persistent: ClassVar[bool] = True\n    items: list[list[TraitBase]]\n\n    def to_representations(self) -&gt; Generator[Representation]:\n        \"\"\"Convert a bundle to representations.\n\n        Yields:\n            Representation: Representation of the bundle.\n\n        \"\"\"\n        for idx, item in enumerate(self.items):\n            yield Representation(name=f\"{self.name} {idx}\", traits=item)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Bundle.to_representations","title":"<code>to_representations()</code>","text":"<p>Convert a bundle to representations.</p> <p>Yields:</p> Name Type Description <code>Representation</code> <code>Generator[Representation]</code> <p>Representation of the bundle.</p> Source code in <code>client/ayon_core/pipeline/traits/content.py</code> <pre><code>def to_representations(self) -&gt; Generator[Representation]:\n    \"\"\"Convert a bundle to representations.\n\n    Yields:\n        Representation: Representation of the bundle.\n\n    \"\"\"\n    for idx, item in enumerate(self.items):\n        yield Representation(name=f\"{self.name} {idx}\", traits=item)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.ColorManaged","title":"<code>ColorManaged</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>Color managed trait.</p> <p>Holds color management information. Can be used with Image-related traits to define color space and config.</p> <p>Sync with OpenAssetIO MediaCreation Traits.</p> <p>Attributes:</p> Name Type Description <code>color_space</code> <code>str</code> <p>An OCIO colorspace name available in the \"current\" OCIO context.</p> <code>config</code> <code>str</code> <p>An OCIO config name defining color space.</p> Source code in <code>client/ayon_core/pipeline/traits/color.py</code> <pre><code>@dataclass\nclass ColorManaged(TraitBase):\n    \"\"\"Color managed trait.\n\n    Holds color management information. Can be used with Image-related\n    traits to define color space and config.\n\n    Sync with OpenAssetIO MediaCreation Traits.\n\n    Attributes:\n        color_space (str): An OCIO colorspace name available\n            in the \"current\" OCIO context.\n        config (str): An OCIO config name defining color space.\n    \"\"\"\n\n    id: ClassVar[str] = \"ayon.color.ColorManaged.v1\"\n    name: ClassVar[str] = \"ColorManaged\"\n    color_space: str\n    description: ClassVar[str] = \"Color Managed trait.\"\n    persistent: ClassVar[bool] = True\n    config: Optional[str] = None\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Compressed","title":"<code>Compressed</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>Compressed trait model.</p> <p>This trait can hold information about compressed content. What type of compression is used.</p> <p>Example::</p> <pre><code>Compressed(\"gzip\")\n</code></pre> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Trait name.</p> <code>description</code> <code>str</code> <p>Trait description.</p> <code>id</code> <code>str</code> <p>id should be a namespaced trait name with version</p> <code>compression_type</code> <code>str</code> <p>Compression type.</p> <code>persistent</code> <code>bool</code> <p>Should the mime type be stored with the representation on the server or not.</p> Source code in <code>client/ayon_core/pipeline/traits/content.py</code> <pre><code>@dataclass\nclass Compressed(TraitBase):\n    \"\"\"Compressed trait model.\n\n    This trait can hold information about compressed content. What type\n    of compression is used.\n\n    Example::\n\n        Compressed(\"gzip\")\n\n    Attributes:\n        name (str): Trait name.\n        description (str): Trait description.\n        id (str): id should be a namespaced trait name with version\n        compression_type (str): Compression type.\n        persistent (bool): Should the mime type be stored with the\n            representation on the server or not.\n    \"\"\"\n\n    name: ClassVar[str] = \"Compressed\"\n    description: ClassVar[str] = \"Compressed Trait\"\n    id: ClassVar[str] = \"ayon.content.Compressed.v1\"\n    persistent: ClassVar[bool] = True\n    compression_type: str\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Deep","title":"<code>Deep</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>Deep trait model.</p> <p>Type trait model for deep EXR images.</p> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Trait name.</p> <code>description</code> <code>str</code> <p>Trait description.</p> <code>id</code> <code>str</code> <p>id should be a namespaced trait name with a version</p> Source code in <code>client/ayon_core/pipeline/traits/two_dimensional.py</code> <pre><code>@dataclass\nclass Deep(TraitBase):\n    \"\"\"Deep trait model.\n\n    Type trait model for deep EXR images.\n\n    Attributes:\n        name (str): Trait name.\n        description (str): Trait description.\n        id (str): id should be a namespaced trait name with a version\n    \"\"\"\n\n    name: ClassVar[str] = \"Deep\"\n    description: ClassVar[str] = \"Deep Trait Model\"\n    id: ClassVar[str] = \"ayon.2d.Deep.v1\"\n    persistent: ClassVar[bool] = True\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.DigitallySigned","title":"<code>DigitallySigned</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>Digitally signed trait.</p> <p>This type trait means that the data is digitally signed.</p> <p>Attributes:</p> Name Type Description <code>signature</code> <code>str</code> <p>Digital signature.</p> Source code in <code>client/ayon_core/pipeline/traits/cryptography.py</code> <pre><code>@dataclass\nclass DigitallySigned(TraitBase):\n    \"\"\"Digitally signed trait.\n\n    This type trait means that the data is digitally signed.\n\n    Attributes:\n        signature (str): Digital signature.\n    \"\"\"\n\n    id: ClassVar[str] = \"ayon.cryptography.DigitallySigned.v1\"\n    name: ClassVar[str] = \"DigitallySigned\"\n    description: ClassVar[str] = \"Digitally signed trait.\"\n    persistent: ClassVar[bool] = True\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.FileLocation","title":"<code>FileLocation</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>FileLocation trait model.</p> <p>This model represents a file path. It is a specialization of the LocatableContent trait. It is adding optional file size and file hash for easy access to file information.</p> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Trait name.</p> <code>description</code> <code>str</code> <p>Trait description.</p> <code>id</code> <code>str</code> <p>id should be a namespaced trait name with version</p> <code>file_path</code> <code>str</code> <p>File path.</p> <code>file_size</code> <code>Optional[int]</code> <p>File size in bytes.</p> <code>file_hash</code> <code>Optional[str]</code> <p>File hash.</p> <code>persistent</code> <code>bool</code> <p>Should the mime type be stored with the representation on the server or not.</p> Source code in <code>client/ayon_core/pipeline/traits/content.py</code> <pre><code>@dataclass\nclass FileLocation(TraitBase):\n    \"\"\"FileLocation trait model.\n\n    This model represents a file path. It is a specialization of the\n    LocatableContent trait. It is adding optional file size and file hash\n    for easy access to file information.\n\n    Attributes:\n        name (str): Trait name.\n        description (str): Trait description.\n        id (str): id should be a namespaced trait name with version\n        file_path (str): File path.\n        file_size (Optional[int]): File size in bytes.\n        file_hash (Optional[str]): File hash.\n        persistent (bool): Should the mime type be stored with the\n            representation on the server or not.\n    \"\"\"\n\n    name: ClassVar[str] = \"FileLocation\"\n    description: ClassVar[str] = \"FileLocation Trait Model\"\n    id: ClassVar[str] = \"ayon.content.FileLocation.v1\"\n    persistent: ClassVar[bool] = True\n    file_path: Path\n    file_size: Optional[int] = None\n    file_hash: Optional[str] = None\n\n    def validate_trait(self, representation: Representation) -&gt; None:\n        \"\"\"Validate the trait.\n\n        This method validates the trait against others in the representation.\n        In particular, it checks that the FileLocations trait is not present.\n\n        Args:\n            representation (Representation): Representation to validate.\n\n        Raises:\n            TraitValidationError: If the trait is invalid within the\n                representation.\n\n        \"\"\"\n        super().validate_trait(representation)\n        if representation.contains_trait(FileLocations):\n            msg = (\n                \"Representation contains a file location. It can not contain \"\n                \"both `FileLocation` and `FileLocations`.\"\n            )\n            raise TraitValidationError(self.name, msg)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.FileLocation.validate_trait","title":"<code>validate_trait(representation)</code>","text":"<p>Validate the trait.</p> <p>This method validates the trait against others in the representation. In particular, it checks that the FileLocations trait is not present.</p> <p>Parameters:</p> Name Type Description Default <code>representation</code> <code>Representation</code> <p>Representation to validate.</p> required <p>Raises:</p> Type Description <code>TraitValidationError</code> <p>If the trait is invalid within the representation.</p> Source code in <code>client/ayon_core/pipeline/traits/content.py</code> <pre><code>def validate_trait(self, representation: Representation) -&gt; None:\n    \"\"\"Validate the trait.\n\n    This method validates the trait against others in the representation.\n    In particular, it checks that the FileLocations trait is not present.\n\n    Args:\n        representation (Representation): Representation to validate.\n\n    Raises:\n        TraitValidationError: If the trait is invalid within the\n            representation.\n\n    \"\"\"\n    super().validate_trait(representation)\n    if representation.contains_trait(FileLocations):\n        msg = (\n            \"Representation contains a file location. It can not contain \"\n            \"both `FileLocation` and `FileLocations`.\"\n        )\n        raise TraitValidationError(self.name, msg)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.FileLocations","title":"<code>FileLocations</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>FileLocations trait model.</p> <p>This model defines list of FileLocation traits. It can be used for file sequences or more complex hierarchies.</p> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Trait name.</p> <code>description</code> <code>str</code> <p>Trait description.</p> <code>id</code> <code>str</code> <p>id should be a namespaced trait name with version</p> <code>file_paths</code> <code>list of FileLocation</code> <p>File locations.</p> <code>persistent</code> <code>bool</code> <p>Should the mime type be stored with the representation on the server or not.</p> Source code in <code>client/ayon_core/pipeline/traits/content.py</code> <pre><code>@dataclass\nclass FileLocations(TraitBase):\n    \"\"\"FileLocations trait model.\n\n    This model defines list of FileLocation traits. It can be\n    used for file sequences or more complex hierarchies.\n\n    Attributes:\n        name (str): Trait name.\n        description (str): Trait description.\n        id (str): id should be a namespaced trait name with version\n        file_paths (list of FileLocation): File locations.\n        persistent (bool): Should the mime type be stored with the\n            representation on the server or not.\n    \"\"\"\n\n    name: ClassVar[str] = \"FileLocations\"\n    description: ClassVar[str] = \"FileLocations Trait Model\"\n    id: ClassVar[str] = \"ayon.content.FileLocations.v1\"\n    persistent: ClassVar[bool] = True\n    file_paths: list[FileLocation]\n\n    def get_files(self) -&gt; Generator[Path, None, None]:\n        \"\"\"Get all file paths from the trait.\n\n        This method will return all file paths from the trait.\n\n        Yields:\n            Path: List of file paths.\n\n        \"\"\"\n        for file_location in self.file_paths:\n            yield file_location.file_path\n\n    def get_common_root(self) -&gt; Path:\n        \"\"\"Get a meaningful common root directory for all file paths.\n\n        Returns:\n            Path: Common root directory.\n\n        Raises:\n            ValueError: If file locations do not share a meaningful root.\n\n        \"\"\"\n        file_paths = [\n            file_path if isinstance(file_path, Path) else Path(file_path)\n            for file_path in self.get_files()\n        ]\n        if not file_paths:\n            raise ValueError(\"No file locations defined.\")\n\n        try:\n            common_root = Path(\n                os.path.commonpath(\n                    [str(file_path.parent) for file_path in file_paths]\n                )\n            )\n        except ValueError as exc:\n            raise ValueError(\n                \"File locations must share a common root directory.\"\n            ) from exc\n\n        if str(common_root) in {\"\", \".\"}:\n            raise ValueError(\n                \"File locations must share a meaningful common root directory.\"\n            )\n\n        if common_root.anchor and common_root == Path(common_root.anchor):\n            raise ValueError(\n                \"File locations must share a common root deeper than the \"\n                \"filesystem root.\"\n            )\n\n        return common_root\n\n    def get_file_location_for_frame(\n            self,\n            frame: int,\n            sequence_trait: Optional[Sequence] = None,\n        ) -&gt; Optional[FileLocation]:\n        \"\"\"Get a file location for a frame.\n\n        This method will return the file location for a given frame. If the\n        frame is not found in the file paths, it will return None.\n\n        Args:\n            frame (int): Frame to get the file location for.\n            sequence_trait (Sequence): Sequence trait to get the\n                frame range specs from.\n\n        Returns:\n            Optional[FileLocation]: File location for the frame.\n\n        \"\"\"\n        frame_regex = re.compile(r\"\\.(?P&lt;index&gt;(?P&lt;padding&gt;0*)\\d+)\\.\\D+\\d?$\")\n        if sequence_trait and sequence_trait.frame_regex:\n            frame_regex = sequence_trait.get_frame_pattern()\n\n        for location in self.file_paths:\n            result = re.search(frame_regex, location.file_path.name)\n            if result:\n                frame_index = int(result.group(\"index\"))\n                if frame_index == frame:\n                    return location\n        return None\n\n    def validate_trait(self, representation: Representation) -&gt; None:\n        \"\"\"Validate the trait.\n\n        This method validates the trait against others in the representation.\n        In particular, it checks that the sequence trait is present, and if\n        so, it will compare the frame range to the file paths.\n\n        Args:\n            representation (Representation): Representation to validate.\n\n        Raises:\n            TraitValidationError: If the trait is invalid within the\n                representation.\n\n        \"\"\"\n        super().validate_trait(representation)\n        if len(self.file_paths) == 0:\n            # If there are no file paths, we can't validate\n            msg = \"No file locations defined (empty list)\"\n            raise TraitValidationError(self.name, msg)\n        if representation.contains_trait(FrameRanged):\n            self._validate_frame_range(representation)\n        if not representation.contains_trait(Sequence) \\\n                and not representation.contains_trait(UDIM):\n            try:\n                self.get_common_root()\n            except ValueError as exc:\n                msg = (\n                    \"Multiple file locations defined, but no Sequence or \"\n                    \"UDIM trait defined. Files must share a meaningful \"\n                    \"common root so their relative hierarchy can be \"\n                    \"preserved.\"\n                )\n                raise TraitValidationError(self.name, msg) from exc\n        if representation.contains_trait(FileLocation):\n            msg = (\n                \"Representation contains a file location. It can not contain \"\n                \"both `FileLocation` and `FileLocations`.\"\n            )\n            raise TraitValidationError(self.name, msg)\n\n    def _validate_frame_range(self, representation: Representation) -&gt; None:\n        \"\"\"Validate the frame range against the file paths.\n\n        If the representation contains a FrameRanged trait, this method will\n        validate the frame range against the file paths. If the frame range\n        does not match the file paths, the trait is invalid. It takes into\n        account the Handles and Sequence traits.\n\n        Args:\n            representation (Representation): Representation to validate.\n\n        Raises:\n            TraitValidationError: If the trait is invalid within the\n                representation.\n\n        \"\"\"\n        tmp_frame_ranged: FrameRanged = self.get_sequence_from_files(\n                    [f.file_path for f in self.file_paths])\n\n        frames_from_spec: list[int] = []\n        with contextlib.suppress(MissingTraitError):\n            sequence: Sequence = representation.get_trait(Sequence)\n            frame_regex = sequence.get_frame_pattern()\n            if sequence.frame_spec:\n                frames_from_spec = sequence.get_frame_list(\n                    self, frame_regex)\n\n        frame_start_with_handles, frame_end_with_handles = \\\n            self._get_frame_info_with_handles(representation, frames_from_spec)\n\n        if frame_start_with_handles \\\n                and tmp_frame_ranged.frame_start != frame_start_with_handles:\n            # If the detected frame range does not match the combined\n            # FrameRanged and Handles trait, the\n            # trait is invalid.\n            msg = (\n                f\"Frame range defined by {self.name} \"\n                f\"({tmp_frame_ranged.frame_start}-\"\n                f\"{tmp_frame_ranged.frame_end}) \"\n                \"in files does not match \"\n                \"frame range \"\n                f\"({frame_start_with_handles}-\"\n                f\"{frame_end_with_handles}) defined in FrameRanged trait.\"\n            )\n\n            raise TraitValidationError(self.name, msg)\n\n        if frames_from_spec:\n            if len(frames_from_spec) != len(self.file_paths):\n                # If the number of file paths does not match the frame range,\n                # the trait is invalid\n                msg = (\n                    f\"Number of file locations ({len(self.file_paths)}) \"\n                    \"does not match frame range defined by frame spec \"\n                    \"on Sequence trait: \"\n                    f\"({len(frames_from_spec)})\"\n                )\n                raise TraitValidationError(self.name, msg)\n            # if there is a frame spec on the Sequence trait,\n            # we should not validate the frame range from the files.\n            # the rest is validated by Sequence validators.\n            return\n\n        length_with_handles: int = (\n            frame_end_with_handles - frame_start_with_handles + 1\n        )\n\n        if len(self.file_paths) != length_with_handles:\n            # If the number of file paths does not match the frame range,\n            # the trait is invalid\n            msg = (\n                f\"Number of file locations ({len(self.file_paths)}) \"\n                \"does not match frame range \"\n                f\"({length_with_handles})\"\n            )\n            raise TraitValidationError(self.name, msg)\n\n        frame_ranged: FrameRanged = representation.get_trait(FrameRanged)\n\n        if frame_start_with_handles != tmp_frame_ranged.frame_start or \\\n                frame_end_with_handles != tmp_frame_ranged.frame_end:\n            # If the frame range does not match the FrameRanged trait, the\n            # trait is invalid. Note that we don't check the frame rate\n            # because it is not stored in the file paths and is not\n            # determined by `get_sequence_from_files`.\n            msg = (\n                \"Frame range \"\n                f\"({frame_ranged.frame_start}-{frame_ranged.frame_end}) \"\n                \"in sequence trait does not match \"\n                \"frame range \"\n                f\"({tmp_frame_ranged.frame_start}-\"\n                f\"{tmp_frame_ranged.frame_end}) \"\n            )\n            raise TraitValidationError(self.name, msg)\n\n    @staticmethod\n    def _get_frame_info_with_handles(\n            representation: Representation,\n            frames_from_spec: list[int]) -&gt; tuple[int, int]:\n        \"\"\"Get the frame range with handles from the representation.\n\n        This will return frame start and frame end with handles calculated\n        in if there actually is the Handles trait in the representation.\n\n        Args:\n            representation (Representation): Representation to get the frame\n                range from.\n            frames_from_spec (list[int]): List of frames from the frame spec.\n                This list is modified in place to take into\n                account the handles.\n\n        Mutates:\n            frames_from_spec: List of frames from the frame spec.\n\n        Returns:\n            tuple[int, int]: Start and end frame with handles.\n\n        \"\"\"\n        frame_start = frame_end = 0\n        frame_start_handle = frame_end_handle = 0\n        # If there is no sequence trait, we can't validate it\n        if frames_from_spec and representation.contains_trait(FrameRanged):\n            # if there is no FrameRanged trait (but really there should be)\n            # we can use the frame range from the frame spec\n            frame_start = min(frames_from_spec)\n            frame_end = max(frames_from_spec)\n\n        # Handle the frame range\n        with contextlib.suppress(MissingTraitError):\n            frame_start = representation.get_trait(FrameRanged).frame_start\n            frame_end = representation.get_trait(FrameRanged).frame_end\n\n        # Handle the handles :P\n        with contextlib.suppress(MissingTraitError):\n            handles: Handles = representation.get_trait(Handles)\n            if not handles.inclusive:\n                # if handless are exclusive, we need to adjust the frame range\n                frame_start_handle = handles.frame_start_handle or 0\n                frame_end_handle = handles.frame_end_handle or 0\n                if frames_from_spec:\n                    frames_from_spec.extend(\n                        range(frame_start - frame_start_handle, frame_start)\n                    )\n                    frames_from_spec.extend(\n                        range(frame_end + 1, frame_end_handle + frame_end + 1)\n                    )\n\n        frame_start_with_handles = frame_start - frame_start_handle\n        frame_end_with_handles = frame_end + frame_end_handle\n\n        return frame_start_with_handles, frame_end_with_handles\n\n    @staticmethod\n    def get_sequence_from_files(paths: list[Path]) -&gt; FrameRanged:\n        \"\"\"Get the original frame range from files.\n\n        Note that this cannot guess frame rate, so it's set to 25.\n        This will also fail on paths that cannot be assembled into\n        one collection without any reminders.\n\n        Args:\n            paths (list[Path]): List of file paths.\n\n        Returns:\n            FrameRanged: FrameRanged trait.\n\n        Raises:\n            ValueError: If paths cannot be assembled into one collection\n\n        \"\"\"\n        cols, rems = assemble(\n            [path.name for path in paths],\n            minimum_items=1)\n        if rems:\n            msg = (\n                \"Assembled paths have singular file(s) and \"\n                \"sequences at the same time. This is not supported. \"\n            )\n            raise ValueError(msg)\n\n        if len(cols) != 1:\n            msg = f\"More than one collection found ({len(cols)}).\"\n            raise ValueError(msg)\n        col = cols[0]\n\n        sorted_frames = sorted(col.indexes)\n        # First frame used for end value\n        first_frame = sorted_frames[0]\n        last_frame = sorted_frames[-1]\n        # Use padding from a collection of the last frame lengths as string\n        # padding = max(col.padding, len(str(last_frame)))\n\n        return FrameRanged(\n            frame_start=first_frame, frame_end=last_frame,\n            frames_per_second=\"25.0\"\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.FileLocations.get_common_root","title":"<code>get_common_root()</code>","text":"<p>Get a meaningful common root directory for all file paths.</p> <p>Returns:</p> Name Type Description <code>Path</code> <code>Path</code> <p>Common root directory.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If file locations do not share a meaningful root.</p> Source code in <code>client/ayon_core/pipeline/traits/content.py</code> <pre><code>def get_common_root(self) -&gt; Path:\n    \"\"\"Get a meaningful common root directory for all file paths.\n\n    Returns:\n        Path: Common root directory.\n\n    Raises:\n        ValueError: If file locations do not share a meaningful root.\n\n    \"\"\"\n    file_paths = [\n        file_path if isinstance(file_path, Path) else Path(file_path)\n        for file_path in self.get_files()\n    ]\n    if not file_paths:\n        raise ValueError(\"No file locations defined.\")\n\n    try:\n        common_root = Path(\n            os.path.commonpath(\n                [str(file_path.parent) for file_path in file_paths]\n            )\n        )\n    except ValueError as exc:\n        raise ValueError(\n            \"File locations must share a common root directory.\"\n        ) from exc\n\n    if str(common_root) in {\"\", \".\"}:\n        raise ValueError(\n            \"File locations must share a meaningful common root directory.\"\n        )\n\n    if common_root.anchor and common_root == Path(common_root.anchor):\n        raise ValueError(\n            \"File locations must share a common root deeper than the \"\n            \"filesystem root.\"\n        )\n\n    return common_root\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.FileLocations.get_file_location_for_frame","title":"<code>get_file_location_for_frame(frame, sequence_trait=None)</code>","text":"<p>Get a file location for a frame.</p> <p>This method will return the file location for a given frame. If the frame is not found in the file paths, it will return None.</p> <p>Parameters:</p> Name Type Description Default <code>frame</code> <code>int</code> <p>Frame to get the file location for.</p> required <code>sequence_trait</code> <code>Sequence</code> <p>Sequence trait to get the frame range specs from.</p> <code>None</code> <p>Returns:</p> Type Description <code>Optional[FileLocation]</code> <p>Optional[FileLocation]: File location for the frame.</p> Source code in <code>client/ayon_core/pipeline/traits/content.py</code> <pre><code>def get_file_location_for_frame(\n        self,\n        frame: int,\n        sequence_trait: Optional[Sequence] = None,\n    ) -&gt; Optional[FileLocation]:\n    \"\"\"Get a file location for a frame.\n\n    This method will return the file location for a given frame. If the\n    frame is not found in the file paths, it will return None.\n\n    Args:\n        frame (int): Frame to get the file location for.\n        sequence_trait (Sequence): Sequence trait to get the\n            frame range specs from.\n\n    Returns:\n        Optional[FileLocation]: File location for the frame.\n\n    \"\"\"\n    frame_regex = re.compile(r\"\\.(?P&lt;index&gt;(?P&lt;padding&gt;0*)\\d+)\\.\\D+\\d?$\")\n    if sequence_trait and sequence_trait.frame_regex:\n        frame_regex = sequence_trait.get_frame_pattern()\n\n    for location in self.file_paths:\n        result = re.search(frame_regex, location.file_path.name)\n        if result:\n            frame_index = int(result.group(\"index\"))\n            if frame_index == frame:\n                return location\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.FileLocations.get_files","title":"<code>get_files()</code>","text":"<p>Get all file paths from the trait.</p> <p>This method will return all file paths from the trait.</p> <p>Yields:</p> Name Type Description <code>Path</code> <code>Path</code> <p>List of file paths.</p> Source code in <code>client/ayon_core/pipeline/traits/content.py</code> <pre><code>def get_files(self) -&gt; Generator[Path, None, None]:\n    \"\"\"Get all file paths from the trait.\n\n    This method will return all file paths from the trait.\n\n    Yields:\n        Path: List of file paths.\n\n    \"\"\"\n    for file_location in self.file_paths:\n        yield file_location.file_path\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.FileLocations.get_sequence_from_files","title":"<code>get_sequence_from_files(paths)</code>  <code>staticmethod</code>","text":"<p>Get the original frame range from files.</p> <p>Note that this cannot guess frame rate, so it's set to 25. This will also fail on paths that cannot be assembled into one collection without any reminders.</p> <p>Parameters:</p> Name Type Description Default <code>paths</code> <code>list[Path]</code> <p>List of file paths.</p> required <p>Returns:</p> Name Type Description <code>FrameRanged</code> <code>FrameRanged</code> <p>FrameRanged trait.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If paths cannot be assembled into one collection</p> Source code in <code>client/ayon_core/pipeline/traits/content.py</code> <pre><code>@staticmethod\ndef get_sequence_from_files(paths: list[Path]) -&gt; FrameRanged:\n    \"\"\"Get the original frame range from files.\n\n    Note that this cannot guess frame rate, so it's set to 25.\n    This will also fail on paths that cannot be assembled into\n    one collection without any reminders.\n\n    Args:\n        paths (list[Path]): List of file paths.\n\n    Returns:\n        FrameRanged: FrameRanged trait.\n\n    Raises:\n        ValueError: If paths cannot be assembled into one collection\n\n    \"\"\"\n    cols, rems = assemble(\n        [path.name for path in paths],\n        minimum_items=1)\n    if rems:\n        msg = (\n            \"Assembled paths have singular file(s) and \"\n            \"sequences at the same time. This is not supported. \"\n        )\n        raise ValueError(msg)\n\n    if len(cols) != 1:\n        msg = f\"More than one collection found ({len(cols)}).\"\n        raise ValueError(msg)\n    col = cols[0]\n\n    sorted_frames = sorted(col.indexes)\n    # First frame used for end value\n    first_frame = sorted_frames[0]\n    last_frame = sorted_frames[-1]\n    # Use padding from a collection of the last frame lengths as string\n    # padding = max(col.padding, len(str(last_frame)))\n\n    return FrameRanged(\n        frame_start=first_frame, frame_end=last_frame,\n        frames_per_second=\"25.0\"\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.FileLocations.validate_trait","title":"<code>validate_trait(representation)</code>","text":"<p>Validate the trait.</p> <p>This method validates the trait against others in the representation. In particular, it checks that the sequence trait is present, and if so, it will compare the frame range to the file paths.</p> <p>Parameters:</p> Name Type Description Default <code>representation</code> <code>Representation</code> <p>Representation to validate.</p> required <p>Raises:</p> Type Description <code>TraitValidationError</code> <p>If the trait is invalid within the representation.</p> Source code in <code>client/ayon_core/pipeline/traits/content.py</code> <pre><code>def validate_trait(self, representation: Representation) -&gt; None:\n    \"\"\"Validate the trait.\n\n    This method validates the trait against others in the representation.\n    In particular, it checks that the sequence trait is present, and if\n    so, it will compare the frame range to the file paths.\n\n    Args:\n        representation (Representation): Representation to validate.\n\n    Raises:\n        TraitValidationError: If the trait is invalid within the\n            representation.\n\n    \"\"\"\n    super().validate_trait(representation)\n    if len(self.file_paths) == 0:\n        # If there are no file paths, we can't validate\n        msg = \"No file locations defined (empty list)\"\n        raise TraitValidationError(self.name, msg)\n    if representation.contains_trait(FrameRanged):\n        self._validate_frame_range(representation)\n    if not representation.contains_trait(Sequence) \\\n            and not representation.contains_trait(UDIM):\n        try:\n            self.get_common_root()\n        except ValueError as exc:\n            msg = (\n                \"Multiple file locations defined, but no Sequence or \"\n                \"UDIM trait defined. Files must share a meaningful \"\n                \"common root so their relative hierarchy can be \"\n                \"preserved.\"\n            )\n            raise TraitValidationError(self.name, msg) from exc\n    if representation.contains_trait(FileLocation):\n        msg = (\n            \"Representation contains a file location. It can not contain \"\n            \"both `FileLocation` and `FileLocations`.\"\n        )\n        raise TraitValidationError(self.name, msg)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Fragment","title":"<code>Fragment</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>Fragment trait model.</p> <p>This model represents a fragment trait. A fragment is a part of a larger entity that is represented by another representation.</p> <p>Example::</p> <pre><code>main_representation = Representation(name=\"parent\",\n    traits=[],\n)\nfragment_representation = Representation(\n    name=\"fragment\",\n    traits=[\n        Fragment(parent=main_representation.id),\n    ]\n)\n</code></pre> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Trait name.</p> <code>description</code> <code>str</code> <p>Trait description.</p> <code>id</code> <code>str</code> <p>id should be namespaced trait name with version</p> <code>parent</code> <code>str</code> <p>Parent representation id.</p> <code>persistent</code> <code>bool</code> <p>Should the mime type be stored with the representation on the server or not.</p> Source code in <code>client/ayon_core/pipeline/traits/content.py</code> <pre><code>@dataclass\nclass Fragment(TraitBase):\n    \"\"\"Fragment trait model.\n\n    This model represents a fragment trait. A fragment is a part of\n    a larger entity that is represented by another representation.\n\n    Example::\n\n        main_representation = Representation(name=\"parent\",\n            traits=[],\n        )\n        fragment_representation = Representation(\n            name=\"fragment\",\n            traits=[\n                Fragment(parent=main_representation.id),\n            ]\n        )\n\n    Attributes:\n        name (str): Trait name.\n        description (str): Trait description.\n        id (str): id should be namespaced trait name with version\n        parent (str): Parent representation id.\n        persistent (bool): Should the mime type be stored with the\n            representation on the server or not.\n    \"\"\"\n\n    name: ClassVar[str] = \"Fragment\"\n    description: ClassVar[str] = \"Fragment Trait\"\n    id: ClassVar[str] = \"ayon.content.Fragment.v1\"\n    persistent: ClassVar[bool] = True\n    parent: str\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.FrameRanged","title":"<code>FrameRanged</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>Frame ranged trait model.</p> <p>Model representing a frame-ranged trait.</p> <p>Sync with OpenAssetIO MediaCreation Traits. For compatibility with OpenAssetIO, we'll need to handle different names of attributes:</p> <pre><code>* frame_start -&gt; start_frame\n* frame_end -&gt; end_frame\n...\n</code></pre> frames_per_second is a string to allow various precision <p>formats. FPS is a floating point number, but it can be also represented as a fraction (e.g. \"30000/1001\") or as a decimal or even as an irrational number. We need to support all these formats. To work with FPS, we'll need some helper function to convert FPS to Decimal from string.</p> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Trait name.</p> <code>description</code> <code>str</code> <p>Trait description.</p> <code>id</code> <code>str</code> <p>id should be a namespaced trait name with a version</p> <code>frame_start</code> <code>int</code> <p>Frame start.</p> <code>frame_end</code> <code>int</code> <p>Frame end.</p> <code>frame_in</code> <code>int</code> <p>Frame in.</p> <code>frame_out</code> <code>int</code> <p>Frame out.</p> <code>frames_per_second</code> <code>str</code> <p>Frames per second.</p> <code>step</code> <code>int</code> <p>Step.</p> Source code in <code>client/ayon_core/pipeline/traits/temporal.py</code> <pre><code>@dataclass\nclass FrameRanged(TraitBase):\n    \"\"\"Frame ranged trait model.\n\n    Model representing a frame-ranged trait.\n\n    Sync with OpenAssetIO MediaCreation Traits. For compatibility with\n    OpenAssetIO, we'll need to handle different names of attributes:\n\n        * frame_start -&gt; start_frame\n        * frame_end -&gt; end_frame\n        ...\n\n    Note: frames_per_second is a string to allow various precision\n        formats. FPS is a floating point number, but it can be also\n        represented as a fraction (e.g. \"30000/1001\") or as a decimal\n        or even as an irrational number. We need to support all these\n        formats. To work with FPS, we'll need some helper function\n        to convert FPS to Decimal from string.\n\n    Attributes:\n        name (str): Trait name.\n        description (str): Trait description.\n        id (str): id should be a namespaced trait name with a version\n        frame_start (int): Frame start.\n        frame_end (int): Frame end.\n        frame_in (int): Frame in.\n        frame_out (int): Frame out.\n        frames_per_second (str): Frames per second.\n        step (int): Step.\n    \"\"\"\n\n    name: ClassVar[str] = \"FrameRanged\"\n    description: ClassVar[str] = \"Frame Ranged Trait\"\n    id: ClassVar[str] = \"ayon.time.FrameRanged.v1\"\n    persistent: ClassVar[bool] = True\n    frame_start: int\n    frame_end: int\n    frame_in: Optional[int] = None\n    frame_out: Optional[int] = None\n    frames_per_second: str = None\n    step: Optional[int] = None\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.GapPolicy","title":"<code>GapPolicy</code>","text":"<p>               Bases: <code>Enum</code></p> <p>Gap policy enumeration.</p> <p>This type defines how to handle gaps in a sequence.</p> <p>Attributes:</p> Name Type Description <code>forbidden</code> <code>int</code> <p>Gaps are forbidden.</p> <code>missing</code> <code>int</code> <p>Gaps are interpreted as missing frames.</p> <code>hold</code> <code>int</code> <p>Gaps are interpreted as hold frames (last existing frames).</p> <code>black</code> <code>int</code> <p>Gaps are interpreted as black frames.</p> Source code in <code>client/ayon_core/pipeline/traits/temporal.py</code> <pre><code>class GapPolicy(Enum):\n    \"\"\"Gap policy enumeration.\n\n    This type defines how to handle gaps in a sequence.\n\n    Attributes:\n        forbidden (int): Gaps are forbidden.\n        missing (int): Gaps are interpreted as missing frames.\n        hold (int): Gaps are interpreted as hold frames (last existing frames).\n        black (int): Gaps are interpreted as black frames.\n    \"\"\"\n\n    forbidden = auto()\n    missing = auto()\n    hold = auto()\n    black = auto()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Geometry","title":"<code>Geometry</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>Geometry type trait model.</p> <p>Type trait for geometry data.</p> <p>Sync with OpenAssetIO MediaCreation Traits.</p> Source code in <code>client/ayon_core/pipeline/traits/three_dimensional.py</code> <pre><code>@dataclass\nclass Geometry(TraitBase):\n    \"\"\"Geometry type trait model.\n\n    Type trait for geometry data.\n\n    Sync with OpenAssetIO MediaCreation Traits.\n    \"\"\"\n\n    id: ClassVar[str] = \"ayon.3d.Geometry.v1\"\n    name: ClassVar[str] = \"Geometry\"\n    description: ClassVar[str] = \"Geometry trait model.\"\n    persistent: ClassVar[bool] = True\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Handles","title":"<code>Handles</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>Handles trait model.</p> <p>Handles define the range of frames that are included or excluded from the sequence.</p> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Trait name.</p> <code>description</code> <code>str</code> <p>Trait description.</p> <code>id</code> <code>str</code> <p>id should be a namespaced trait name with a version</p> <code>inclusive</code> <code>bool</code> <p>Handles are inclusive.</p> <code>frame_start_handle</code> <code>int</code> <p>Frame start handle.</p> <code>frame_end_handle</code> <code>int</code> <p>Frame end handle.</p> Source code in <code>client/ayon_core/pipeline/traits/temporal.py</code> <pre><code>@dataclass\nclass Handles(TraitBase):\n    \"\"\"Handles trait model.\n\n    Handles define the range of frames that are included or excluded\n    from the sequence.\n\n    Attributes:\n        name (str): Trait name.\n        description (str): Trait description.\n        id (str): id should be a namespaced trait name with a version\n        inclusive (bool): Handles are inclusive.\n        frame_start_handle (int): Frame start handle.\n        frame_end_handle (int): Frame end handle.\n    \"\"\"\n\n    name: ClassVar[str] = \"Handles\"\n    description: ClassVar[str] = \"Handles Trait\"\n    id: ClassVar[str] = \"ayon.time.Handles.v1\"\n    persistent: ClassVar[bool] = True\n    inclusive: Optional[bool] = False\n    frame_start_handle: Optional[int] = None\n    frame_end_handle: Optional[int] = None\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.IESProfile","title":"<code>IESProfile</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>IES profile (IES-LM-64) type trait model.</p> <p>Sync with OpenAssetIO MediaCreation Traits.</p> Source code in <code>client/ayon_core/pipeline/traits/three_dimensional.py</code> <pre><code>@dataclass\nclass IESProfile(TraitBase):\n    \"\"\"IES profile (IES-LM-64) type trait model.\n\n    Sync with OpenAssetIO MediaCreation Traits.\n    \"\"\"\n\n    id: ClassVar[str] = \"ayon.3d.IESProfile.v1\"\n    name: ClassVar[str] = \"IESProfile\"\n    description: ClassVar[str] = \"IES profile trait model.\"\n    persistent: ClassVar[bool] = True\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Image","title":"<code>Image</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>Image trait model.</p> <p>Type trait model for image.</p> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Trait name.</p> <code>description</code> <code>str</code> <p>Trait description.</p> <code>id</code> <code>str</code> <p>id should be a namespaced trait name with version</p> Source code in <code>client/ayon_core/pipeline/traits/two_dimensional.py</code> <pre><code>@dataclass\nclass Image(TraitBase):\n    \"\"\"Image trait model.\n\n    Type trait model for image.\n\n    Attributes:\n        name (str): Trait name.\n        description (str): Trait description.\n        id (str): id should be a namespaced trait name with version\n    \"\"\"\n\n    name: ClassVar[str] = \"Image\"\n    description: ClassVar[str] = \"Image Trait\"\n    id: ClassVar[str] = \"ayon.2d.Image.v1\"\n    persistent: ClassVar[bool] = True\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.IntendedUse","title":"<code>IntendedUse</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>Intended use of the representation.</p> <p>This trait describes the intended use of the representation. It can be used in cases where the other traits are not enough to describe the intended use. For example, a txt file with tracking points can be used as a corner pin in After Effect but not in Nuke.</p> <p>Attributes:</p> Name Type Description <code>use</code> <code>str</code> <p>Intended use description.</p> Source code in <code>client/ayon_core/pipeline/traits/meta.py</code> <pre><code>@dataclass\nclass IntendedUse(TraitBase):\n    \"\"\"Intended use of the representation.\n\n    This trait describes the intended use of the representation. It\n    can be used in cases where the other traits are not enough to\n    describe the intended use. For example, a txt file with tracking\n    points can be used as a corner pin in After Effect but not in Nuke.\n\n    Attributes:\n        use (str): Intended use description.\n\n    \"\"\"\n    name: ClassVar[str] = \"IntendedUse\"\n    description: ClassVar[str] = \"Intended Use Trait Model\"\n    id: ClassVar[str] = \"ayon.meta.IntendedUse.v1\"\n    persistent: ClassVar[bool] = True\n    use: str\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.KeepOriginalLocation","title":"<code>KeepOriginalLocation</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>Keep files in its original location.</p> Note <p>This is not a persistent trait.</p> Source code in <code>client/ayon_core/pipeline/traits/meta.py</code> <pre><code>@dataclass\nclass KeepOriginalLocation(TraitBase):\n    \"\"\"Keep files in its original location.\n\n    Note:\n        This is not a persistent trait.\n\n    \"\"\"\n    name: ClassVar[str] = \"KeepOriginalLocation\"\n    description: ClassVar[str] = \"Keep Original Location Trait Model\"\n    id: ClassVar[str] = \"ayon.meta.KeepOriginalLocation.v1\"\n    persistent: ClassVar[bool] = False\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Lighting","title":"<code>Lighting</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>Lighting trait model.</p> <p>Type trait for lighting data.</p> <p>Sync with OpenAssetIO MediaCreation Traits.</p> Source code in <code>client/ayon_core/pipeline/traits/three_dimensional.py</code> <pre><code>@dataclass\nclass Lighting(TraitBase):\n    \"\"\"Lighting trait model.\n\n    Type trait for lighting data.\n\n    Sync with OpenAssetIO MediaCreation Traits.\n    \"\"\"\n\n    id: ClassVar[str] = \"ayon.3d.Lighting.v1\"\n    name: ClassVar[str] = \"Lighting\"\n    description: ClassVar[str] = \"Lighting trait model.\"\n    persistent: ClassVar[bool] = True\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.LocatableContent","title":"<code>LocatableContent</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>LocatableContent trait model.</p> <p>This model represents a locatable content trait. Locatable content is content that has a location. It doesn't have to be a file - it could be a URL or some other location.</p> <p>Sync with OpenAssetIO MediaCreation Traits.</p> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Trait name.</p> <code>description</code> <code>str</code> <p>Trait description.</p> <code>id</code> <code>str</code> <p>id should be a namespaced trait name with version</p> <code>location</code> <code>str</code> <p>Location.</p> <code>is_templated</code> <code>Optional[bool]</code> <p>Is the location templated? Default is None.</p> <code>persistent</code> <code>bool</code> <p>Should the mime type be stored with the representation on the server or not.</p> Source code in <code>client/ayon_core/pipeline/traits/content.py</code> <pre><code>@dataclass\nclass LocatableContent(TraitBase):\n    \"\"\"LocatableContent trait model.\n\n    This model represents a locatable content trait. Locatable content\n    is content that has a location. It doesn't have to be a file - it could\n    be a URL or some other location.\n\n    Sync with OpenAssetIO MediaCreation Traits.\n\n    Attributes:\n        name (str): Trait name.\n        description (str): Trait description.\n        id (str): id should be a namespaced trait name with version\n        location (str): Location.\n        is_templated (Optional[bool]): Is the location templated?\n            Default is None.\n        persistent (bool): Should the mime type be stored with the\n            representation on the server or not.\n    \"\"\"\n\n    name: ClassVar[str] = \"LocatableContent\"\n    description: ClassVar[str] = \"LocatableContent Trait Model\"\n    id: ClassVar[str] = \"ayon.content.LocatableContent.v1\"\n    persistent: ClassVar[bool] = True\n    location: str\n    is_templated: Optional[bool] = None\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.MimeType","title":"<code>MimeType</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>MimeType trait model.</p> <p>This model represents a mime type trait. For example, image/jpeg. It is used to describe the type of content in a representation regardless of the file extension.</p> <p>For more information, see RFC 2046 and RFC 4288 (and related RFCs).</p> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Trait name.</p> <code>description</code> <code>str</code> <p>Trait description.</p> <code>id</code> <code>str</code> <p>id should be a namespaced trait name with version</p> <code>mime_type</code> <code>str</code> <p>Mime type like image/jpeg.</p> <code>persistent</code> <code>bool</code> <p>Should the mime type be stored with the representation on the server or not.</p> Source code in <code>client/ayon_core/pipeline/traits/content.py</code> <pre><code>@dataclass\nclass MimeType(TraitBase):\n    \"\"\"MimeType trait model.\n\n    This model represents a mime type trait. For example, image/jpeg.\n    It is used to describe the type of content in a representation regardless\n    of the file extension.\n\n    For more information, see RFC 2046 and RFC 4288 (and related RFCs).\n\n    Attributes:\n        name (str): Trait name.\n        description (str): Trait description.\n        id (str): id should be a namespaced trait name with version\n        mime_type (str): Mime type like image/jpeg.\n        persistent (bool): Should the mime type be stored with the\n            representation on the server or not.\n    \"\"\"\n\n    name: ClassVar[str] = \"MimeType\"\n    description: ClassVar[str] = \"MimeType Trait Model\"\n    id: ClassVar[str] = \"ayon.content.MimeType.v1\"\n    persistent: ClassVar[bool] = True\n    mime_type: str\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.MissingTraitError","title":"<code>MissingTraitError</code>","text":"<p>               Bases: <code>TypeError</code></p> <p>Missing trait error exception.</p> <p>This exception is raised when the trait is missing.</p> Source code in <code>client/ayon_core/pipeline/traits/trait.py</code> <pre><code>class MissingTraitError(TypeError):\n    \"\"\"Missing trait error exception.\n\n    This exception is raised when the trait is missing.\n    \"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Overscan","title":"<code>Overscan</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>Overscan trait model.</p> <p>This model represents an overscan (or underscan) trait. Defines the extra pixels around the image.</p> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Trait name.</p> <code>description</code> <code>str</code> <p>Trait description.</p> <code>id</code> <code>str</code> <p>id should be a namespaced trait name with a version</p> <code>left</code> <code>int</code> <p>Left overscan/underscan.</p> <code>right</code> <code>int</code> <p>Right overscan/underscan.</p> <code>top</code> <code>int</code> <p>Top overscan/underscan.</p> <code>bottom</code> <code>int</code> <p>Bottom overscan/underscan.</p> Source code in <code>client/ayon_core/pipeline/traits/two_dimensional.py</code> <pre><code>@dataclass\nclass Overscan(TraitBase):\n    \"\"\"Overscan trait model.\n\n    This model represents an overscan (or underscan) trait. Defines the\n    extra pixels around the image.\n\n    Attributes:\n        name (str): Trait name.\n        description (str): Trait description.\n        id (str): id should be a namespaced trait name with a version\n        left (int): Left overscan/underscan.\n        right (int): Right overscan/underscan.\n        top (int): Top overscan/underscan.\n        bottom (int): Bottom overscan/underscan.\n    \"\"\"\n\n    name: ClassVar[str] = \"Overscan\"\n    description: ClassVar[str] = \"Overscan Trait\"\n    id: ClassVar[str] = \"ayon.2d.Overscan.v1\"\n    persistent: ClassVar[bool] = True\n    left: int\n    right: int\n    top: int\n    bottom: int\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.PGPSigned","title":"<code>PGPSigned</code>  <code>dataclass</code>","text":"<p>               Bases: <code>DigitallySigned</code></p> <p>PGP signed trait.</p> <p>This trait holds PGP (RFC-4880) signed data.</p> <p>Attributes:</p> Name Type Description <code>signed_data</code> <code>str</code> <p>Signed data.</p> <code>clear_text</code> <code>str</code> <p>Clear text.</p> Source code in <code>client/ayon_core/pipeline/traits/cryptography.py</code> <pre><code>@dataclass\nclass PGPSigned(DigitallySigned):\n    \"\"\"PGP signed trait.\n\n    This trait holds PGP (RFC-4880) signed data.\n\n    Attributes:\n        signed_data (str): Signed data.\n        clear_text (str): Clear text.\n    \"\"\"\n\n    id: ClassVar[str] = \"ayon.cryptography.PGPSigned.v1\"\n    name: ClassVar[str] = \"PGPSigned\"\n    description: ClassVar[str] = \"PGP signed trait.\"\n    persistent: ClassVar[bool] = True\n    signed_data: str\n    clear_text: Optional[str] = None\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Persistent","title":"<code>Persistent</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>Persistent trait model.</p> <p>Persistent trait is opposite to transient trait. It marks representation as persistent. Such representations are persisted in the system (e.g. in the database).</p> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Trait name.</p> <code>description</code> <code>str</code> <p>Trait description.</p> <code>id</code> <code>str</code> <p>id should be a namespaced trait name with the version</p> Source code in <code>client/ayon_core/pipeline/traits/lifecycle.py</code> <pre><code>@dataclass\nclass Persistent(TraitBase):\n    \"\"\"Persistent trait model.\n\n    Persistent trait is opposite to transient trait. It marks representation\n    as persistent. Such representations are persisted in the system (e.g. in\n    the database).\n\n    Attributes:\n        name (str): Trait name.\n        description (str): Trait description.\n        id (str): id should be a namespaced trait name with the version\n    \"\"\"\n\n    name: ClassVar[str] = \"Persistent\"\n    description: ClassVar[str] = \"Persistent Trait Model\"\n    id: ClassVar[str] = \"ayon.lifecycle.Persistent.v1\"\n    # Note that this affects the persistence of the trait itself, not\n    # the representation. This is a class variable, so it is shared\n    # among all instances of the class.\n    persistent: bool = True\n\n    def validate_trait(self, representation) -&gt; None:  # noqa: ANN001\n        \"\"\"Validate representation is not Transient.\n\n        Args:\n            representation (Representation): Representation model.\n\n        Raises:\n            TraitValidationError: If representation is marked\n                as both Persistent and Transient.\n\n        \"\"\"\n        if representation.contains_trait(Transient):\n            msg = \"Representation is marked as both Persistent and Transient.\"\n            raise TraitValidationError(self.name, msg)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Persistent.validate_trait","title":"<code>validate_trait(representation)</code>","text":"<p>Validate representation is not Transient.</p> <p>Parameters:</p> Name Type Description Default <code>representation</code> <code>Representation</code> <p>Representation model.</p> required <p>Raises:</p> Type Description <code>TraitValidationError</code> <p>If representation is marked as both Persistent and Transient.</p> Source code in <code>client/ayon_core/pipeline/traits/lifecycle.py</code> <pre><code>def validate_trait(self, representation) -&gt; None:  # noqa: ANN001\n    \"\"\"Validate representation is not Transient.\n\n    Args:\n        representation (Representation): Representation model.\n\n    Raises:\n        TraitValidationError: If representation is marked\n            as both Persistent and Transient.\n\n    \"\"\"\n    if representation.contains_trait(Transient):\n        msg = \"Representation is marked as both Persistent and Transient.\"\n        raise TraitValidationError(self.name, msg)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.PixelBased","title":"<code>PixelBased</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>PixelBased trait model.</p> <p>The pixel-related trait for image data.</p> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Trait name.</p> <code>description</code> <code>str</code> <p>Trait description.</p> <code>id</code> <code>str</code> <p>id should be a namespaced trait name with a version</p> <code>display_window_width</code> <code>int</code> <p>Width of the image display window.</p> <code>display_window_height</code> <code>int</code> <p>Height of the image display window.</p> <code>pixel_aspect_ratio</code> <code>float</code> <p>Pixel aspect ratio.</p> Source code in <code>client/ayon_core/pipeline/traits/two_dimensional.py</code> <pre><code>@dataclass\nclass PixelBased(TraitBase):\n    \"\"\"PixelBased trait model.\n\n    The pixel-related trait for image data.\n\n    Attributes:\n        name (str): Trait name.\n        description (str): Trait description.\n        id (str): id should be a namespaced trait name with a version\n        display_window_width (int): Width of the image display window.\n        display_window_height (int): Height of the image display window.\n        pixel_aspect_ratio (float): Pixel aspect ratio.\n    \"\"\"\n\n    name: ClassVar[str] = \"PixelBased\"\n    description: ClassVar[str] = \"PixelBased Trait Model\"\n    id: ClassVar[str] = \"ayon.2d.PixelBased.v1\"\n    persistent: ClassVar[bool] = True\n    display_window_width: int\n    display_window_height: int\n    pixel_aspect_ratio: float\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Planar","title":"<code>Planar</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>Planar trait model.</p> <p>This model represents an Image with planar configuration.</p> Todo <ul> <li>(antirotor): Is this really a planar configuration? As with     bit planes and everything? If it serves as differentiator for     Deep images, should it be named differently? Like Raster?</li> </ul> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Trait name.</p> <code>description</code> <code>str</code> <p>Trait description.</p> <code>id</code> <code>str</code> <p>id should be namespaced trait name with version</p> <code>planar_configuration</code> <code>str</code> <p>Planar configuration.</p> Source code in <code>client/ayon_core/pipeline/traits/two_dimensional.py</code> <pre><code>@dataclass\nclass Planar(TraitBase):\n    \"\"\"Planar trait model.\n\n    This model represents an Image with planar configuration.\n\n    Todo:\n        * (antirotor): Is this really a planar configuration? As with\n            bit planes and everything? If it serves as differentiator for\n            Deep images, should it be named differently? Like Raster?\n\n    Attributes:\n        name (str): Trait name.\n        description (str): Trait description.\n        id (str): id should be namespaced trait name with version\n        planar_configuration (str): Planar configuration.\n    \"\"\"\n\n    name: ClassVar[str] = \"Planar\"\n    description: ClassVar[str] = \"Planar Trait Model\"\n    id: ClassVar[str] = \"ayon.2d.Planar.v1\"\n    persistent: ClassVar[bool] = True\n    planar_configuration: str\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Representation","title":"<code>Representation</code>","text":"<p>               Bases: <code>Generic[T]</code></p> <p>Representation of products.</p> <p>Representation defines a collection of individual properties that describe the specific \"form\" of the product. A trait represents a set of properties therefore, the Representation is a collection of traits.</p> <p>It holds methods to add, remove, get, and check for the existence of a trait in the representation.</p> Note <p><code>PLR0904</code> is the rule for checking the number of public methods in a class.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Representation name. Must be unique within instance.</p> required <code>representation_id</code> <code>str</code> <p>Representation ID.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>class Representation(Generic[T]):  # noqa: PLR0904\n    \"\"\"Representation of products.\n\n    Representation defines a collection of individual properties that describe\n    the specific \"form\" of the product. A trait represents a set of\n    properties therefore, the Representation is a collection of traits.\n\n    It holds methods to add, remove, get, and check for the existence of a\n    trait in the representation.\n\n    Note:\n        `PLR0904` is the rule for checking the number of public methods\n        in a class.\n\n    Arguments:\n        name (str): Representation name. Must be unique within instance.\n        representation_id (str): Representation ID.\n    \"\"\"\n\n    _data: dict[str, T]\n    name: str\n    representation_id: str\n\n    def __hash__(self):\n        \"\"\"Return hash of the representation ID.\"\"\"\n        return hash(self.representation_id)\n\n    def __getitem__(self, key: str) -&gt; T:\n        \"\"\"Get the trait by ID.\n\n        Args:\n            key (str): Trait ID.\n\n        Returns:\n            TraitBase: Trait instance.\n\n        \"\"\"\n        return self.get_trait_by_id(key)\n\n    def __setitem__(self, key: str, value: T) -&gt; None:\n        \"\"\"Set the trait by ID.\n\n        Args:\n            key (str): Trait ID.\n            value (TraitBase): Trait instance.\n\n        \"\"\"\n        with contextlib.suppress(KeyError):\n            self._data.pop(key)\n\n        self.add_trait(value)\n\n    def __delitem__(self, key: str) -&gt; None:\n        \"\"\"Remove the trait by ID.\n\n        Args:\n            key (str): Trait ID.\n\n\n        \"\"\"\n        self.remove_trait_by_id(key)\n\n    def __contains__(self, key: str) -&gt; bool:\n        \"\"\"Check if the trait exists by ID.\n\n        Args:\n            key (str): Trait ID.\n\n        Returns:\n            bool: True if the trait exists, False otherwise.\n\n        \"\"\"\n        return self.contains_trait_by_id(key)\n\n    def __iter__(self):\n        \"\"\"Return the trait ID iterator.\"\"\"\n        return iter(self._data)\n\n    def __str__(self):\n        \"\"\"Return the representation name.\"\"\"\n        return self.name\n\n    def items(self) -&gt; ItemsView[str, T]:\n        \"\"\"Return the traits as items.\"\"\"\n        return ItemsView(self._data)\n\n    def add_trait(self, trait: T, *, exists_ok: bool = False) -&gt; None:\n        \"\"\"Add a trait to the Representation.\n\n        Args:\n            trait (TraitBase): Trait to add.\n            exists_ok (bool, optional): If True, do not raise an error if the\n                trait already exists. Defaults to False.\n\n        Raises:\n            ValueError: If the trait ID is not provided, or the trait already\n                exists.\n\n        \"\"\"\n        if not hasattr(trait, \"id\"):\n            error_msg = f\"Invalid trait {trait} - ID is required.\"\n            raise ValueError(error_msg)\n        if trait.id in self._data and not exists_ok:\n            error_msg = f\"Trait with ID {trait.id} already exists.\"\n            raise ValueError(error_msg)\n        self._data[trait.id] = trait\n\n    def add_traits(\n            self, traits: list[T], *, exists_ok: bool = False) -&gt; None:\n        \"\"\"Add a list of traits to the Representation.\n\n        Args:\n            traits (list[TraitBase]): List of traits to add.\n            exists_ok (bool, optional): If True, do not raise an error if the\n                trait already exists. Defaults to False.\n\n        \"\"\"\n        for trait in traits:\n            self.add_trait(trait, exists_ok=exists_ok)\n\n    def remove_trait(self, trait: Type[TraitBase]) -&gt; None:\n        \"\"\"Remove a trait from the data.\n\n        Args:\n            trait (TraitBase, optional): Trait class.\n\n        Raises:\n            ValueError: If the trait is not found.\n\n        \"\"\"\n        try:\n            self._data.pop(str(trait.id))\n        except KeyError as e:\n            error_msg = f\"Trait with ID {trait.id} not found.\"\n            raise ValueError(error_msg) from e\n\n    def remove_trait_by_id(self, trait_id: str) -&gt; None:\n        \"\"\"Remove a trait from the data by its ID.\n\n        Args:\n            trait_id (str): Trait ID.\n\n        Raises:\n            ValueError: If the trait is not found.\n\n        \"\"\"\n        try:\n            self._data.pop(trait_id)\n        except KeyError as e:\n            error_msg = f\"Trait with ID {trait_id} not found.\"\n            raise ValueError(error_msg) from e\n\n    def remove_traits(self, traits: list[Type[T]]) -&gt; None:\n        \"\"\"Remove a list of traits from the Representation.\n\n        If no trait IDs or traits are provided, all traits will be removed.\n\n        Args:\n            traits (list[TraitBase]): List of trait classes.\n\n        \"\"\"\n        if not traits:\n            self._data = {}\n            return\n\n        for trait in traits:\n            self.remove_trait(trait)\n\n    def remove_traits_by_id(self, trait_ids: list[str]) -&gt; None:\n        \"\"\"Remove a list of traits from the Representation by their ID.\n\n        If no trait IDs or traits are provided, all traits will be removed.\n\n        Args:\n            trait_ids (list[str], optional): List of trait IDs.\n\n        \"\"\"\n        for trait_id in trait_ids:\n            self.remove_trait_by_id(trait_id)\n\n    def has_traits(self) -&gt; bool:\n        \"\"\"Check if the Representation has any traits.\n\n        Returns:\n            bool: True if the Representation has any traits, False otherwise.\n\n        \"\"\"\n        return bool(self._data)\n\n    def contains_trait(self, trait: Type[T]) -&gt; bool:\n        \"\"\"Check if the trait exists in the Representation.\n\n        Args:\n            trait (TraitBase): Trait class.\n\n        Returns:\n            bool: True if the trait exists, False otherwise.\n\n        \"\"\"\n        return bool(self._data.get(str(trait.id)))\n\n    def contains_trait_by_id(self, trait_id: str) -&gt; bool:\n        \"\"\"Check if the trait exists using trait id.\n\n        Args:\n            trait_id (str): Trait ID.\n\n        Returns:\n            bool: True if the trait exists, False otherwise.\n\n        \"\"\"\n        return bool(self._data.get(trait_id))\n\n    def contains_traits(self, traits: list[Type[T]]) -&gt; bool:\n        \"\"\"Check if the traits exist.\n\n        Args:\n            traits (list[TraitBase], optional): List of trait classes.\n\n        Returns:\n            bool: True if all traits exist, False otherwise.\n\n        \"\"\"\n        return all(self.contains_trait(trait=trait) for trait in traits)\n\n    def contains_traits_by_id(self, trait_ids: list[str]) -&gt; bool:\n        \"\"\"Check if the traits exist by id.\n\n        If no trait IDs or traits are provided, it will check if the\n        representation has any traits.\n\n        Args:\n            trait_ids (list[str]): List of trait IDs.\n\n        Returns:\n            bool: True if all traits exist, False otherwise.\n\n        \"\"\"\n        return all(\n            self.contains_trait_by_id(trait_id) for trait_id in trait_ids\n        )\n\n    def get_trait(self, trait: Type[T]) -&gt; T:\n        \"\"\"Get a trait from the representation.\n\n        Args:\n            trait (TraitBase, optional): Trait class.\n\n        Returns:\n            TraitBase: Trait instance.\n\n        Raises:\n            MissingTraitError: If the trait is not found.\n\n        \"\"\"\n        try:\n            return self._data[str(trait.id)]\n        except KeyError as e:\n            msg = f\"Trait with ID {trait.id} not found.\"\n            raise MissingTraitError(msg) from e\n\n    def get_trait_by_id(self, trait_id: str) -&gt; T:\n        # sourcery skip: use-named-expression\n        \"\"\"Get a trait from the representation by id.\n\n        Args:\n            trait_id (str): Trait ID.\n\n        Returns:\n            TraitBase: Trait instance.\n\n        Raises:\n            MissingTraitError: If the trait is not found.\n\n        \"\"\"\n        version = _get_version_from_id(trait_id)\n        if version:\n            try:\n                return self._data[trait_id]\n            except KeyError as e:\n                msg = f\"Trait with ID {trait_id} not found.\"\n                raise MissingTraitError(msg) from e\n\n        result = next(\n            (\n                self._data.get(trait_id)\n                for trait_id in self._data\n                if trait_id.startswith(trait_id)\n            ),\n            None,\n        )\n        if result is None:\n            msg = f\"Trait with ID {trait_id} not found.\"\n            raise MissingTraitError(msg)\n        return result\n\n    def get_traits(self,\n                     traits: Optional[list[Type[T]]] = None\n     ) -&gt; dict[str, T]:\n        \"\"\"Get a list of traits from the representation.\n\n        If no trait IDs or traits are provided, all traits will be returned.\n\n        Args:\n            traits (list[TraitBase], optional): List of trait classes.\n\n        Returns:\n            dict: Dictionary of traits.\n\n        \"\"\"\n        result: dict[str, T] = {}\n        if not traits:\n            for trait_id in self._data:\n                result[trait_id] = self.get_trait_by_id(trait_id=trait_id)\n            return result\n\n        for trait in traits:\n            result[str(trait.id)] = self.get_trait(trait=trait)\n        return result\n\n    def get_traits_by_ids(self, trait_ids: list[str]) -&gt; dict[str, T]:\n        \"\"\"Get a list of traits from the representation by their id.\n\n        If no trait IDs or traits are provided, all traits will be returned.\n\n        Args:\n            trait_ids (list[str]): List of trait IDs.\n\n        Returns:\n            dict: Dictionary of traits.\n\n        \"\"\"\n        return {\n            trait_id: self.get_trait_by_id(trait_id)\n            for trait_id in trait_ids\n        }\n\n    def traits_as_dict(self) -&gt; dict:\n        \"\"\"Return the traits from Representation data as a dictionary.\n\n        Returns:\n            dict: Traits data dictionary.\n\n        \"\"\"\n        return {\n            trait_id: trait.as_dict()\n            for trait_id, trait in self._data.items()\n            if trait and trait_id\n        }\n\n    def __len__(self):\n        \"\"\"Return the length of the data.\"\"\"\n        return len(self._data)\n\n    def __init__(\n            self,\n            name: str,\n            representation_id: Optional[str] = None,\n            traits: Optional[list[T]] = None):\n        \"\"\"Initialize the data.\n\n        Args:\n            name (str): Representation name. Must be unique within instance.\n            representation_id (str, optional): Representation ID.\n            traits (list[TraitBase], optional): List of traits.\n\n        \"\"\"\n        self.name = name\n        self.representation_id = representation_id or uuid.uuid4().hex\n        self._data = {}\n        if traits:\n            for trait in traits:\n                self.add_trait(trait)\n\n    @staticmethod\n    def _get_version_from_id(trait_id: str) -&gt; Union[int, None]:\n        # sourcery skip: use-named-expression\n        \"\"\"Check if the trait has a version specified.\n\n        Args:\n            trait_id (str): Trait ID.\n\n        Returns:\n            int: Trait version.\n            None: If the trait id does not have a version.\n\n        \"\"\"\n        version_regex = r\"v(\\d+)$\"\n        match = re.search(version_regex, trait_id)\n        return int(match[1]) if match else None\n\n    def __eq__(self, other: object) -&gt; bool:  # noqa: PLR0911\n        \"\"\"Check if the representation is equal to another.\n\n        Args:\n            other (Representation): Representation to compare.\n\n        Returns:\n            bool: True if the representations are equal, False otherwise.\n\n        \"\"\"\n        if not isinstance(other, Representation):\n            return False\n\n        if self.representation_id != other.representation_id:\n            return False\n\n        if self.name != other.name:\n            return False\n\n        # number of traits\n        if len(self) != len(other):\n            return False\n\n        for trait_id, trait in self._data.items():\n            if trait_id not in other._data:\n                return False\n            if trait != other._data[trait_id]:\n                return False\n\n        return True\n\n    @classmethod\n    def get_trait_class_by_trait_id(cls, trait_id: str) -&gt; Optional[Type[T]]:\n        \"\"\"Get the trait class for the given trait ID.\n\n        Args:\n            trait_id (str): Trait ID.\n\n        Returns:\n            type[TraitBase]: Trait class.\n            None: If the trait class is not found.\n\n        Raises:\n            IncompatibleTraitVersionError: If the trait version is incompatible\n                with the current version of the trait.\n            UpgradableTraitError: If the trait version is upgradable to a newer\n                version.\n\n        Note:\n            `ignore[return-value]` can be removed in future versions of Python\n            where the Generics support is better.\n\n        \"\"\"\n        # Iteratively collect all subclasses\n        trait_classes = set()\n        to_process = [TraitBase]\n\n        while to_process:\n            current = to_process.pop()\n            subclasses = current.__subclasses__()\n            trait_classes.update(subclasses)\n            to_process.extend(subclasses)\n\n        # 1. Search for the exact match\n        for trait_class in trait_classes:\n            if getattr(trait_class, \"id\", None) == trait_id:\n                return trait_class  # type: ignore[return-value]\n\n        # 2. Search for fuzzy matches (same base ID, different version)\n        req_version = _get_version_from_id(trait_id)\n        # Determine base ID (e.g., 'ayon.trait' from 'ayon.trait.v1')\n        base_id = trait_id\n        if req_version is not None:\n            base_id = re.sub(r\"\\.v\\d+$\", \"\", trait_id)\n\n        candidates = []\n        for trait_class in trait_classes:\n            t_id = getattr(trait_class, \"id\", \"\")\n            if not t_id:\n                continue\n\n            t_ver = _get_version_from_id(t_id)\n            t_base = t_id\n            if t_ver is not None:\n                t_base = re.sub(r\"\\.v\\d+$\", \"\", t_id)\n\n            if t_base == base_id:\n                candidates.append(\n                    (trait_class, t_ver or 0)\n                )\n\n        if not candidates:\n            return None  # type: ignore[return-value]\n\n        # Find the highest version among candidates\n        found_trait, found_version = max(candidates, key=lambda x: x[1])\n\n        if req_version is None:\n            return found_trait  # type: ignore[return-value]\n\n        if found_version == 0:\n            msg = (\n                f\"Trait {found_trait.id} found with no version, \"\n                \"but requested version is specified.\"\n            )\n            raise IncompatibleTraitVersionError(msg)\n\n        if req_version &gt; found_version:\n            error_msg = (\n                f\"Requested trait version {req_version} is \"\n                f\"higher than the found trait version {found_version}.\"\n            )\n            raise IncompatibleTraitVersionError(error_msg)\n\n        if req_version &lt; found_version:\n            if hasattr(found_trait, \"upgrade\"):\n                error_msg = (\n                    \"Requested trait version \"\n                    f\"{req_version} is lower \"\n                    f\"than the found trait version {found_version}.\"\n                )\n                error: UpgradableTraitError = UpgradableTraitError(error_msg)\n                error.trait = found_trait\n                raise error\n            # If the explicit version was requested, matches failed,\n            # and no upgrade path exists, we return None (effectively\n            # \"Not Found\" for thatversion)\n            return None  # type: ignore[return-value]\n\n        return found_trait  # type: ignore[return-value]\n\n    @classmethod\n    def from_dict(\n            cls: Type[Representation],\n            name: str,\n            representation_id: Optional[str] = None,\n            trait_data: Optional[dict] = None) -&gt; Representation:\n        \"\"\"Create a representation from a dictionary.\n\n        Args:\n            name (str): Representation name.\n            representation_id (str, optional): Representation ID.\n            trait_data (dict): Representation data. Dictionary with keys\n                as trait ids and values as trait data. Example::\n\n                    {\n                        \"ayon.2d.PixelBased.v1\": {\n                            \"display_window_width\": 1920,\n                            \"display_window_height\": 1080\n                        },\n                        \"ayon.2d.Planar.v1\": {\n                            \"channels\": 3\n                        }\n                    }\n\n        Returns:\n            Representation: Representation instance.\n\n        Raises:\n            ValueError: If the trait model with ID is not found.\n            TypeError: If the trait data is not a dictionary.\n            IncompatibleTraitVersionError: If the trait version is incompatible\n\n        \"\"\"\n        if not trait_data:\n            trait_data = {}\n        traits = []\n        for trait_id, value in trait_data.items():\n            if not isinstance(value, dict):\n                msg = (\n                    f\"Invalid trait data for trait ID {trait_id}. \"\n                    \"Trait data must be a dictionary.\"\n                )\n                raise TypeError(msg)\n\n            try:\n                trait_class = cls.get_trait_class_by_trait_id(trait_id)\n            except UpgradableTraitError as e:\n                # we found a newer version of trait, we will upgrade the data\n                if hasattr(e.trait, \"upgrade\"):\n                    traits.append(e.trait.upgrade(value))\n                else:\n                    msg = (\n                        f\"Newer version of trait {e.trait.id} found \"\n                        f\"for requested {trait_id} but without \"\n                        \"upgrade method.\"\n                    )\n                    raise IncompatibleTraitVersionError(msg) from e\n            else:\n                if not trait_class:\n                    error_msg = f\"Trait model with ID {trait_id} not found.\"\n                    raise ValueError(error_msg)\n\n                traits.append(trait_class(**value))\n\n        return cls(\n            name=name, representation_id=representation_id, traits=traits)\n\n    def validate(self) -&gt; None:\n        \"\"\"Validate the representation.\n\n        This method will validate all the traits in the representation.\n\n        Raises:\n            TraitValidationError: If the trait is invalid within representation\n\n        \"\"\"\n        errors = []\n        for trait in self._data.values():\n            # we do this in the loop to catch all the errors\n            try:\n                trait.validate_trait(self)\n            except TraitValidationError as e:  # noqa: PERF203\n                errors.append(str(e))\n        if errors:\n            msg = \"\\n\".join(errors)\n            scope = self.name\n            raise TraitValidationError(scope, msg)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Representation.__contains__","title":"<code>__contains__(key)</code>","text":"<p>Check if the trait exists by ID.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>Trait ID.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if the trait exists, False otherwise.</p> Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>def __contains__(self, key: str) -&gt; bool:\n    \"\"\"Check if the trait exists by ID.\n\n    Args:\n        key (str): Trait ID.\n\n    Returns:\n        bool: True if the trait exists, False otherwise.\n\n    \"\"\"\n    return self.contains_trait_by_id(key)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Representation.__delitem__","title":"<code>__delitem__(key)</code>","text":"<p>Remove the trait by ID.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>Trait ID.</p> required Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>def __delitem__(self, key: str) -&gt; None:\n    \"\"\"Remove the trait by ID.\n\n    Args:\n        key (str): Trait ID.\n\n\n    \"\"\"\n    self.remove_trait_by_id(key)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Representation.__eq__","title":"<code>__eq__(other)</code>","text":"<p>Check if the representation is equal to another.</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>Representation</code> <p>Representation to compare.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if the representations are equal, False otherwise.</p> Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>def __eq__(self, other: object) -&gt; bool:  # noqa: PLR0911\n    \"\"\"Check if the representation is equal to another.\n\n    Args:\n        other (Representation): Representation to compare.\n\n    Returns:\n        bool: True if the representations are equal, False otherwise.\n\n    \"\"\"\n    if not isinstance(other, Representation):\n        return False\n\n    if self.representation_id != other.representation_id:\n        return False\n\n    if self.name != other.name:\n        return False\n\n    # number of traits\n    if len(self) != len(other):\n        return False\n\n    for trait_id, trait in self._data.items():\n        if trait_id not in other._data:\n            return False\n        if trait != other._data[trait_id]:\n            return False\n\n    return True\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Representation.__getitem__","title":"<code>__getitem__(key)</code>","text":"<p>Get the trait by ID.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>Trait ID.</p> required <p>Returns:</p> Name Type Description <code>TraitBase</code> <code>T</code> <p>Trait instance.</p> Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>def __getitem__(self, key: str) -&gt; T:\n    \"\"\"Get the trait by ID.\n\n    Args:\n        key (str): Trait ID.\n\n    Returns:\n        TraitBase: Trait instance.\n\n    \"\"\"\n    return self.get_trait_by_id(key)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Representation.__hash__","title":"<code>__hash__()</code>","text":"<p>Return hash of the representation ID.</p> Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>def __hash__(self):\n    \"\"\"Return hash of the representation ID.\"\"\"\n    return hash(self.representation_id)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Representation.__init__","title":"<code>__init__(name, representation_id=None, traits=None)</code>","text":"<p>Initialize the data.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Representation name. Must be unique within instance.</p> required <code>representation_id</code> <code>str</code> <p>Representation ID.</p> <code>None</code> <code>traits</code> <code>list[TraitBase]</code> <p>List of traits.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>def __init__(\n        self,\n        name: str,\n        representation_id: Optional[str] = None,\n        traits: Optional[list[T]] = None):\n    \"\"\"Initialize the data.\n\n    Args:\n        name (str): Representation name. Must be unique within instance.\n        representation_id (str, optional): Representation ID.\n        traits (list[TraitBase], optional): List of traits.\n\n    \"\"\"\n    self.name = name\n    self.representation_id = representation_id or uuid.uuid4().hex\n    self._data = {}\n    if traits:\n        for trait in traits:\n            self.add_trait(trait)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Representation.__iter__","title":"<code>__iter__()</code>","text":"<p>Return the trait ID iterator.</p> Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>def __iter__(self):\n    \"\"\"Return the trait ID iterator.\"\"\"\n    return iter(self._data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Representation.__len__","title":"<code>__len__()</code>","text":"<p>Return the length of the data.</p> Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>def __len__(self):\n    \"\"\"Return the length of the data.\"\"\"\n    return len(self._data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Representation.__setitem__","title":"<code>__setitem__(key, value)</code>","text":"<p>Set the trait by ID.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>Trait ID.</p> required <code>value</code> <code>TraitBase</code> <p>Trait instance.</p> required Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>def __setitem__(self, key: str, value: T) -&gt; None:\n    \"\"\"Set the trait by ID.\n\n    Args:\n        key (str): Trait ID.\n        value (TraitBase): Trait instance.\n\n    \"\"\"\n    with contextlib.suppress(KeyError):\n        self._data.pop(key)\n\n    self.add_trait(value)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Representation.__str__","title":"<code>__str__()</code>","text":"<p>Return the representation name.</p> Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>def __str__(self):\n    \"\"\"Return the representation name.\"\"\"\n    return self.name\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Representation.add_trait","title":"<code>add_trait(trait, *, exists_ok=False)</code>","text":"<p>Add a trait to the Representation.</p> <p>Parameters:</p> Name Type Description Default <code>trait</code> <code>TraitBase</code> <p>Trait to add.</p> required <code>exists_ok</code> <code>bool</code> <p>If True, do not raise an error if the trait already exists. Defaults to False.</p> <code>False</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the trait ID is not provided, or the trait already exists.</p> Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>def add_trait(self, trait: T, *, exists_ok: bool = False) -&gt; None:\n    \"\"\"Add a trait to the Representation.\n\n    Args:\n        trait (TraitBase): Trait to add.\n        exists_ok (bool, optional): If True, do not raise an error if the\n            trait already exists. Defaults to False.\n\n    Raises:\n        ValueError: If the trait ID is not provided, or the trait already\n            exists.\n\n    \"\"\"\n    if not hasattr(trait, \"id\"):\n        error_msg = f\"Invalid trait {trait} - ID is required.\"\n        raise ValueError(error_msg)\n    if trait.id in self._data and not exists_ok:\n        error_msg = f\"Trait with ID {trait.id} already exists.\"\n        raise ValueError(error_msg)\n    self._data[trait.id] = trait\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Representation.add_traits","title":"<code>add_traits(traits, *, exists_ok=False)</code>","text":"<p>Add a list of traits to the Representation.</p> <p>Parameters:</p> Name Type Description Default <code>traits</code> <code>list[TraitBase]</code> <p>List of traits to add.</p> required <code>exists_ok</code> <code>bool</code> <p>If True, do not raise an error if the trait already exists. Defaults to False.</p> <code>False</code> Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>def add_traits(\n        self, traits: list[T], *, exists_ok: bool = False) -&gt; None:\n    \"\"\"Add a list of traits to the Representation.\n\n    Args:\n        traits (list[TraitBase]): List of traits to add.\n        exists_ok (bool, optional): If True, do not raise an error if the\n            trait already exists. Defaults to False.\n\n    \"\"\"\n    for trait in traits:\n        self.add_trait(trait, exists_ok=exists_ok)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Representation.contains_trait","title":"<code>contains_trait(trait)</code>","text":"<p>Check if the trait exists in the Representation.</p> <p>Parameters:</p> Name Type Description Default <code>trait</code> <code>TraitBase</code> <p>Trait class.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if the trait exists, False otherwise.</p> Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>def contains_trait(self, trait: Type[T]) -&gt; bool:\n    \"\"\"Check if the trait exists in the Representation.\n\n    Args:\n        trait (TraitBase): Trait class.\n\n    Returns:\n        bool: True if the trait exists, False otherwise.\n\n    \"\"\"\n    return bool(self._data.get(str(trait.id)))\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Representation.contains_trait_by_id","title":"<code>contains_trait_by_id(trait_id)</code>","text":"<p>Check if the trait exists using trait id.</p> <p>Parameters:</p> Name Type Description Default <code>trait_id</code> <code>str</code> <p>Trait ID.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if the trait exists, False otherwise.</p> Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>def contains_trait_by_id(self, trait_id: str) -&gt; bool:\n    \"\"\"Check if the trait exists using trait id.\n\n    Args:\n        trait_id (str): Trait ID.\n\n    Returns:\n        bool: True if the trait exists, False otherwise.\n\n    \"\"\"\n    return bool(self._data.get(trait_id))\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Representation.contains_traits","title":"<code>contains_traits(traits)</code>","text":"<p>Check if the traits exist.</p> <p>Parameters:</p> Name Type Description Default <code>traits</code> <code>list[TraitBase]</code> <p>List of trait classes.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if all traits exist, False otherwise.</p> Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>def contains_traits(self, traits: list[Type[T]]) -&gt; bool:\n    \"\"\"Check if the traits exist.\n\n    Args:\n        traits (list[TraitBase], optional): List of trait classes.\n\n    Returns:\n        bool: True if all traits exist, False otherwise.\n\n    \"\"\"\n    return all(self.contains_trait(trait=trait) for trait in traits)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Representation.contains_traits_by_id","title":"<code>contains_traits_by_id(trait_ids)</code>","text":"<p>Check if the traits exist by id.</p> <p>If no trait IDs or traits are provided, it will check if the representation has any traits.</p> <p>Parameters:</p> Name Type Description Default <code>trait_ids</code> <code>list[str]</code> <p>List of trait IDs.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if all traits exist, False otherwise.</p> Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>def contains_traits_by_id(self, trait_ids: list[str]) -&gt; bool:\n    \"\"\"Check if the traits exist by id.\n\n    If no trait IDs or traits are provided, it will check if the\n    representation has any traits.\n\n    Args:\n        trait_ids (list[str]): List of trait IDs.\n\n    Returns:\n        bool: True if all traits exist, False otherwise.\n\n    \"\"\"\n    return all(\n        self.contains_trait_by_id(trait_id) for trait_id in trait_ids\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Representation.from_dict","title":"<code>from_dict(name, representation_id=None, trait_data=None)</code>  <code>classmethod</code>","text":"<p>Create a representation from a dictionary.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Representation name.</p> required <code>representation_id</code> <code>str</code> <p>Representation ID.</p> <code>None</code> <code>trait_data</code> <code>dict</code> <p>Representation data. Dictionary with keys as trait ids and values as trait data. Example::</p> <pre><code>{\n    \"ayon.2d.PixelBased.v1\": {\n        \"display_window_width\": 1920,\n        \"display_window_height\": 1080\n    },\n    \"ayon.2d.Planar.v1\": {\n        \"channels\": 3\n    }\n}\n</code></pre> <code>None</code> <p>Returns:</p> Name Type Description <code>Representation</code> <code>Representation</code> <p>Representation instance.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the trait model with ID is not found.</p> <code>TypeError</code> <p>If the trait data is not a dictionary.</p> <code>IncompatibleTraitVersionError</code> <p>If the trait version is incompatible</p> Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>@classmethod\ndef from_dict(\n        cls: Type[Representation],\n        name: str,\n        representation_id: Optional[str] = None,\n        trait_data: Optional[dict] = None) -&gt; Representation:\n    \"\"\"Create a representation from a dictionary.\n\n    Args:\n        name (str): Representation name.\n        representation_id (str, optional): Representation ID.\n        trait_data (dict): Representation data. Dictionary with keys\n            as trait ids and values as trait data. Example::\n\n                {\n                    \"ayon.2d.PixelBased.v1\": {\n                        \"display_window_width\": 1920,\n                        \"display_window_height\": 1080\n                    },\n                    \"ayon.2d.Planar.v1\": {\n                        \"channels\": 3\n                    }\n                }\n\n    Returns:\n        Representation: Representation instance.\n\n    Raises:\n        ValueError: If the trait model with ID is not found.\n        TypeError: If the trait data is not a dictionary.\n        IncompatibleTraitVersionError: If the trait version is incompatible\n\n    \"\"\"\n    if not trait_data:\n        trait_data = {}\n    traits = []\n    for trait_id, value in trait_data.items():\n        if not isinstance(value, dict):\n            msg = (\n                f\"Invalid trait data for trait ID {trait_id}. \"\n                \"Trait data must be a dictionary.\"\n            )\n            raise TypeError(msg)\n\n        try:\n            trait_class = cls.get_trait_class_by_trait_id(trait_id)\n        except UpgradableTraitError as e:\n            # we found a newer version of trait, we will upgrade the data\n            if hasattr(e.trait, \"upgrade\"):\n                traits.append(e.trait.upgrade(value))\n            else:\n                msg = (\n                    f\"Newer version of trait {e.trait.id} found \"\n                    f\"for requested {trait_id} but without \"\n                    \"upgrade method.\"\n                )\n                raise IncompatibleTraitVersionError(msg) from e\n        else:\n            if not trait_class:\n                error_msg = f\"Trait model with ID {trait_id} not found.\"\n                raise ValueError(error_msg)\n\n            traits.append(trait_class(**value))\n\n    return cls(\n        name=name, representation_id=representation_id, traits=traits)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Representation.get_trait","title":"<code>get_trait(trait)</code>","text":"<p>Get a trait from the representation.</p> <p>Parameters:</p> Name Type Description Default <code>trait</code> <code>TraitBase</code> <p>Trait class.</p> required <p>Returns:</p> Name Type Description <code>TraitBase</code> <code>T</code> <p>Trait instance.</p> <p>Raises:</p> Type Description <code>MissingTraitError</code> <p>If the trait is not found.</p> Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>def get_trait(self, trait: Type[T]) -&gt; T:\n    \"\"\"Get a trait from the representation.\n\n    Args:\n        trait (TraitBase, optional): Trait class.\n\n    Returns:\n        TraitBase: Trait instance.\n\n    Raises:\n        MissingTraitError: If the trait is not found.\n\n    \"\"\"\n    try:\n        return self._data[str(trait.id)]\n    except KeyError as e:\n        msg = f\"Trait with ID {trait.id} not found.\"\n        raise MissingTraitError(msg) from e\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Representation.get_trait_by_id","title":"<code>get_trait_by_id(trait_id)</code>","text":"<p>Get a trait from the representation by id.</p> <p>Parameters:</p> Name Type Description Default <code>trait_id</code> <code>str</code> <p>Trait ID.</p> required <p>Returns:</p> Name Type Description <code>TraitBase</code> <code>T</code> <p>Trait instance.</p> <p>Raises:</p> Type Description <code>MissingTraitError</code> <p>If the trait is not found.</p> Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>def get_trait_by_id(self, trait_id: str) -&gt; T:\n    # sourcery skip: use-named-expression\n    \"\"\"Get a trait from the representation by id.\n\n    Args:\n        trait_id (str): Trait ID.\n\n    Returns:\n        TraitBase: Trait instance.\n\n    Raises:\n        MissingTraitError: If the trait is not found.\n\n    \"\"\"\n    version = _get_version_from_id(trait_id)\n    if version:\n        try:\n            return self._data[trait_id]\n        except KeyError as e:\n            msg = f\"Trait with ID {trait_id} not found.\"\n            raise MissingTraitError(msg) from e\n\n    result = next(\n        (\n            self._data.get(trait_id)\n            for trait_id in self._data\n            if trait_id.startswith(trait_id)\n        ),\n        None,\n    )\n    if result is None:\n        msg = f\"Trait with ID {trait_id} not found.\"\n        raise MissingTraitError(msg)\n    return result\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Representation.get_trait_class_by_trait_id","title":"<code>get_trait_class_by_trait_id(trait_id)</code>  <code>classmethod</code>","text":"<p>Get the trait class for the given trait ID.</p> <p>Parameters:</p> Name Type Description Default <code>trait_id</code> <code>str</code> <p>Trait ID.</p> required <p>Returns:</p> Name Type Description <code>Optional[Type[T]]</code> <p>type[TraitBase]: Trait class.</p> <code>None</code> <code>Optional[Type[T]]</code> <p>If the trait class is not found.</p> <p>Raises:</p> Type Description <code>IncompatibleTraitVersionError</code> <p>If the trait version is incompatible with the current version of the trait.</p> <code>UpgradableTraitError</code> <p>If the trait version is upgradable to a newer version.</p> Note <p><code>ignore[return-value]</code> can be removed in future versions of Python where the Generics support is better.</p> Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>@classmethod\ndef get_trait_class_by_trait_id(cls, trait_id: str) -&gt; Optional[Type[T]]:\n    \"\"\"Get the trait class for the given trait ID.\n\n    Args:\n        trait_id (str): Trait ID.\n\n    Returns:\n        type[TraitBase]: Trait class.\n        None: If the trait class is not found.\n\n    Raises:\n        IncompatibleTraitVersionError: If the trait version is incompatible\n            with the current version of the trait.\n        UpgradableTraitError: If the trait version is upgradable to a newer\n            version.\n\n    Note:\n        `ignore[return-value]` can be removed in future versions of Python\n        where the Generics support is better.\n\n    \"\"\"\n    # Iteratively collect all subclasses\n    trait_classes = set()\n    to_process = [TraitBase]\n\n    while to_process:\n        current = to_process.pop()\n        subclasses = current.__subclasses__()\n        trait_classes.update(subclasses)\n        to_process.extend(subclasses)\n\n    # 1. Search for the exact match\n    for trait_class in trait_classes:\n        if getattr(trait_class, \"id\", None) == trait_id:\n            return trait_class  # type: ignore[return-value]\n\n    # 2. Search for fuzzy matches (same base ID, different version)\n    req_version = _get_version_from_id(trait_id)\n    # Determine base ID (e.g., 'ayon.trait' from 'ayon.trait.v1')\n    base_id = trait_id\n    if req_version is not None:\n        base_id = re.sub(r\"\\.v\\d+$\", \"\", trait_id)\n\n    candidates = []\n    for trait_class in trait_classes:\n        t_id = getattr(trait_class, \"id\", \"\")\n        if not t_id:\n            continue\n\n        t_ver = _get_version_from_id(t_id)\n        t_base = t_id\n        if t_ver is not None:\n            t_base = re.sub(r\"\\.v\\d+$\", \"\", t_id)\n\n        if t_base == base_id:\n            candidates.append(\n                (trait_class, t_ver or 0)\n            )\n\n    if not candidates:\n        return None  # type: ignore[return-value]\n\n    # Find the highest version among candidates\n    found_trait, found_version = max(candidates, key=lambda x: x[1])\n\n    if req_version is None:\n        return found_trait  # type: ignore[return-value]\n\n    if found_version == 0:\n        msg = (\n            f\"Trait {found_trait.id} found with no version, \"\n            \"but requested version is specified.\"\n        )\n        raise IncompatibleTraitVersionError(msg)\n\n    if req_version &gt; found_version:\n        error_msg = (\n            f\"Requested trait version {req_version} is \"\n            f\"higher than the found trait version {found_version}.\"\n        )\n        raise IncompatibleTraitVersionError(error_msg)\n\n    if req_version &lt; found_version:\n        if hasattr(found_trait, \"upgrade\"):\n            error_msg = (\n                \"Requested trait version \"\n                f\"{req_version} is lower \"\n                f\"than the found trait version {found_version}.\"\n            )\n            error: UpgradableTraitError = UpgradableTraitError(error_msg)\n            error.trait = found_trait\n            raise error\n        # If the explicit version was requested, matches failed,\n        # and no upgrade path exists, we return None (effectively\n        # \"Not Found\" for thatversion)\n        return None  # type: ignore[return-value]\n\n    return found_trait  # type: ignore[return-value]\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Representation.get_traits","title":"<code>get_traits(traits=None)</code>","text":"<p>Get a list of traits from the representation.</p> <p>If no trait IDs or traits are provided, all traits will be returned.</p> <p>Parameters:</p> Name Type Description Default <code>traits</code> <code>list[TraitBase]</code> <p>List of trait classes.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, T]</code> <p>Dictionary of traits.</p> Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>def get_traits(self,\n                 traits: Optional[list[Type[T]]] = None\n ) -&gt; dict[str, T]:\n    \"\"\"Get a list of traits from the representation.\n\n    If no trait IDs or traits are provided, all traits will be returned.\n\n    Args:\n        traits (list[TraitBase], optional): List of trait classes.\n\n    Returns:\n        dict: Dictionary of traits.\n\n    \"\"\"\n    result: dict[str, T] = {}\n    if not traits:\n        for trait_id in self._data:\n            result[trait_id] = self.get_trait_by_id(trait_id=trait_id)\n        return result\n\n    for trait in traits:\n        result[str(trait.id)] = self.get_trait(trait=trait)\n    return result\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Representation.get_traits_by_ids","title":"<code>get_traits_by_ids(trait_ids)</code>","text":"<p>Get a list of traits from the representation by their id.</p> <p>If no trait IDs or traits are provided, all traits will be returned.</p> <p>Parameters:</p> Name Type Description Default <code>trait_ids</code> <code>list[str]</code> <p>List of trait IDs.</p> required <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, T]</code> <p>Dictionary of traits.</p> Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>def get_traits_by_ids(self, trait_ids: list[str]) -&gt; dict[str, T]:\n    \"\"\"Get a list of traits from the representation by their id.\n\n    If no trait IDs or traits are provided, all traits will be returned.\n\n    Args:\n        trait_ids (list[str]): List of trait IDs.\n\n    Returns:\n        dict: Dictionary of traits.\n\n    \"\"\"\n    return {\n        trait_id: self.get_trait_by_id(trait_id)\n        for trait_id in trait_ids\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Representation.has_traits","title":"<code>has_traits()</code>","text":"<p>Check if the Representation has any traits.</p> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if the Representation has any traits, False otherwise.</p> Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>def has_traits(self) -&gt; bool:\n    \"\"\"Check if the Representation has any traits.\n\n    Returns:\n        bool: True if the Representation has any traits, False otherwise.\n\n    \"\"\"\n    return bool(self._data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Representation.items","title":"<code>items()</code>","text":"<p>Return the traits as items.</p> Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>def items(self) -&gt; ItemsView[str, T]:\n    \"\"\"Return the traits as items.\"\"\"\n    return ItemsView(self._data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Representation.remove_trait","title":"<code>remove_trait(trait)</code>","text":"<p>Remove a trait from the data.</p> <p>Parameters:</p> Name Type Description Default <code>trait</code> <code>TraitBase</code> <p>Trait class.</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>If the trait is not found.</p> Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>def remove_trait(self, trait: Type[TraitBase]) -&gt; None:\n    \"\"\"Remove a trait from the data.\n\n    Args:\n        trait (TraitBase, optional): Trait class.\n\n    Raises:\n        ValueError: If the trait is not found.\n\n    \"\"\"\n    try:\n        self._data.pop(str(trait.id))\n    except KeyError as e:\n        error_msg = f\"Trait with ID {trait.id} not found.\"\n        raise ValueError(error_msg) from e\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Representation.remove_trait_by_id","title":"<code>remove_trait_by_id(trait_id)</code>","text":"<p>Remove a trait from the data by its ID.</p> <p>Parameters:</p> Name Type Description Default <code>trait_id</code> <code>str</code> <p>Trait ID.</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>If the trait is not found.</p> Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>def remove_trait_by_id(self, trait_id: str) -&gt; None:\n    \"\"\"Remove a trait from the data by its ID.\n\n    Args:\n        trait_id (str): Trait ID.\n\n    Raises:\n        ValueError: If the trait is not found.\n\n    \"\"\"\n    try:\n        self._data.pop(trait_id)\n    except KeyError as e:\n        error_msg = f\"Trait with ID {trait_id} not found.\"\n        raise ValueError(error_msg) from e\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Representation.remove_traits","title":"<code>remove_traits(traits)</code>","text":"<p>Remove a list of traits from the Representation.</p> <p>If no trait IDs or traits are provided, all traits will be removed.</p> <p>Parameters:</p> Name Type Description Default <code>traits</code> <code>list[TraitBase]</code> <p>List of trait classes.</p> required Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>def remove_traits(self, traits: list[Type[T]]) -&gt; None:\n    \"\"\"Remove a list of traits from the Representation.\n\n    If no trait IDs or traits are provided, all traits will be removed.\n\n    Args:\n        traits (list[TraitBase]): List of trait classes.\n\n    \"\"\"\n    if not traits:\n        self._data = {}\n        return\n\n    for trait in traits:\n        self.remove_trait(trait)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Representation.remove_traits_by_id","title":"<code>remove_traits_by_id(trait_ids)</code>","text":"<p>Remove a list of traits from the Representation by their ID.</p> <p>If no trait IDs or traits are provided, all traits will be removed.</p> <p>Parameters:</p> Name Type Description Default <code>trait_ids</code> <code>list[str]</code> <p>List of trait IDs.</p> required Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>def remove_traits_by_id(self, trait_ids: list[str]) -&gt; None:\n    \"\"\"Remove a list of traits from the Representation by their ID.\n\n    If no trait IDs or traits are provided, all traits will be removed.\n\n    Args:\n        trait_ids (list[str], optional): List of trait IDs.\n\n    \"\"\"\n    for trait_id in trait_ids:\n        self.remove_trait_by_id(trait_id)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Representation.traits_as_dict","title":"<code>traits_as_dict()</code>","text":"<p>Return the traits from Representation data as a dictionary.</p> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict</code> <p>Traits data dictionary.</p> Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>def traits_as_dict(self) -&gt; dict:\n    \"\"\"Return the traits from Representation data as a dictionary.\n\n    Returns:\n        dict: Traits data dictionary.\n\n    \"\"\"\n    return {\n        trait_id: trait.as_dict()\n        for trait_id, trait in self._data.items()\n        if trait and trait_id\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Representation.validate","title":"<code>validate()</code>","text":"<p>Validate the representation.</p> <p>This method will validate all the traits in the representation.</p> <p>Raises:</p> Type Description <code>TraitValidationError</code> <p>If the trait is invalid within representation</p> Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>def validate(self) -&gt; None:\n    \"\"\"Validate the representation.\n\n    This method will validate all the traits in the representation.\n\n    Raises:\n        TraitValidationError: If the trait is invalid within representation\n\n    \"\"\"\n    errors = []\n    for trait in self._data.values():\n        # we do this in the loop to catch all the errors\n        try:\n            trait.validate_trait(self)\n        except TraitValidationError as e:  # noqa: PERF203\n            errors.append(str(e))\n    if errors:\n        msg = \"\\n\".join(errors)\n        scope = self.name\n        raise TraitValidationError(scope, msg)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.RootlessLocation","title":"<code>RootlessLocation</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>RootlessLocation trait model.</p> <p>RootlessLocation trait is a trait that represents a file path that is without a specific root. To get the absolute path, the root needs to be resolved by AYON. Rootless path can be used on multiple platforms.</p> <p>Example::</p> <pre><code>RootlessLocation(\n    rootless_path=\"{root[work]}/project/asset/asset.jpg\"\n)\n</code></pre> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Trait name.</p> <code>description</code> <code>str</code> <p>Trait description.</p> <code>id</code> <code>str</code> <p>id should be a namespaced trait name with version</p> <code>rootless_path</code> <code>str</code> <p>Rootless path.</p> <code>persistent</code> <code>bool</code> <p>Should the mime type be stored with the representation on the server or not.</p> Source code in <code>client/ayon_core/pipeline/traits/content.py</code> <pre><code>@dataclass\nclass RootlessLocation(TraitBase):\n    \"\"\"RootlessLocation trait model.\n\n    RootlessLocation trait is a trait that represents a file path that is\n    without a specific root. To get the absolute path, the root needs to be\n    resolved by AYON. Rootless path can be used on multiple platforms.\n\n    Example::\n\n        RootlessLocation(\n            rootless_path=\"{root[work]}/project/asset/asset.jpg\"\n        )\n\n    Attributes:\n        name (str): Trait name.\n        description (str): Trait description.\n        id (str): id should be a namespaced trait name with version\n        rootless_path (str): Rootless path.\n        persistent (bool): Should the mime type be stored with the\n            representation on the server or not.\n    \"\"\"\n\n    name: ClassVar[str] = \"RootlessLocation\"\n    description: ClassVar[str] = \"RootlessLocation Trait Model\"\n    id: ClassVar[str] = \"ayon.content.RootlessLocation.v1\"\n    persistent: ClassVar[bool] = True\n    rootless_path: str\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.SMPTETimecode","title":"<code>SMPTETimecode</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>SMPTE Timecode trait model.</p> <p>Attributes:</p> Name Type Description <code>timecode</code> <code>str</code> <p>SMPTE Timecode HH:MM:SS:FF</p> Source code in <code>client/ayon_core/pipeline/traits/temporal.py</code> <pre><code>@dataclass\nclass SMPTETimecode(TraitBase):\n    \"\"\"SMPTE Timecode trait model.\n\n    Attributes:\n        timecode (str): SMPTE Timecode HH:MM:SS:FF\n    \"\"\"\n\n    name: ClassVar[str] = \"Timecode\"\n    description: ClassVar[str] = \"SMPTE Timecode Trait\"\n    id: ClassVar[str] = \"ayon.time.SMPTETimecode.v1\"\n    persistent: ClassVar[bool] = True\n    timecode: str\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Sequence","title":"<code>Sequence</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>Sequence trait model.</p> <p>This model represents a sequence trait. Based on the FrameRanged trait and Handles, adding support for gaps policy, frame padding and frame list specification. Regex is used to match frame numbers.</p> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Trait name.</p> <code>description</code> <code>str</code> <p>Trait description.</p> <code>id</code> <code>str</code> <p>id should be a namespaced trait name with a version</p> <code>gaps_policy</code> <code>GapPolicy</code> <p>Gaps policy - how to handle gaps in sequence.</p> <code>frame_padding</code> <code>int</code> <p>Frame padding.</p> <code>frame_regex</code> <code>str</code> <p>Frame regex - regular expression to match frame numbers. Must include 'index' named group and 'padding' named group.</p> <code>frame_spec</code> <code>str</code> <p>Frame list specification of frames. This takes string like \"1-10,20-30,40-50\" etc.</p> Source code in <code>client/ayon_core/pipeline/traits/temporal.py</code> <pre><code>@dataclass\nclass Sequence(TraitBase):\n    \"\"\"Sequence trait model.\n\n    This model represents a sequence trait. Based on the FrameRanged trait\n    and Handles, adding support for gaps policy, frame padding and frame\n    list specification. Regex is used to match frame numbers.\n\n    Attributes:\n        name (str): Trait name.\n        description (str): Trait description.\n        id (str): id should be a namespaced trait name with a version\n        gaps_policy (GapPolicy): Gaps policy - how to handle gaps in\n            sequence.\n        frame_padding (int): Frame padding.\n        frame_regex (str): Frame regex - regular expression to match\n            frame numbers. Must include 'index' named group and 'padding'\n            named group.\n        frame_spec (str): Frame list specification of frames. This takes\n            string like \"1-10,20-30,40-50\" etc.\n    \"\"\"\n\n    name: ClassVar[str] = \"Sequence\"\n    description: ClassVar[str] = \"Sequence Trait Model\"\n    id: ClassVar[str] = \"ayon.time.Sequence.v1\"\n    persistent: ClassVar[bool] = True\n    frame_padding: int\n    gaps_policy: Optional[GapPolicy] = GapPolicy.forbidden\n    frame_regex: Optional[Pattern] = None\n    frame_spec: Optional[str] = None\n\n    @classmethod\n    def validate_frame_regex(\n        cls, v: Optional[Pattern]\n    ) -&gt; Optional[Pattern]:\n        \"\"\"Validate frame regex.\n\n        Frame regex must have index and padding named groups.\n\n        Returns:\n            Optional[Pattern]: Compiled regex pattern.\n\n        Raises:\n            ValueError: If frame regex does not include 'index' and 'padding'\n\n        \"\"\"\n        if v is None:\n            return v\n        if v and any(s not in v.pattern for s in [\"?P&lt;index&gt;\", \"?P&lt;padding&gt;\"]):\n            msg = \"Frame regex must include 'index' and `padding named groups\"\n            raise ValueError(msg)\n        return v\n\n    def validate_trait(self, representation: Representation) -&gt; None:\n        \"\"\"Validate the trait.\"\"\"\n        super().validate_trait(representation)\n\n        # if there is a FileLocations trait, run validation\n        # on it as well\n\n        with contextlib.suppress(MissingTraitError):\n            self._validate_file_locations(representation)\n\n    def _validate_file_locations(self, representation: Representation) -&gt; None:\n        \"\"\"Validate file locations trait.\n\n        If along with the Sequence trait, there is a FileLocations trait,\n        then we need to validate if the file locations match the frame\n        list specification.\n\n        Args:\n            representation (Representation): Representation instance.\n\n        \"\"\"\n        from .content import FileLocations\n        file_locs: FileLocations = representation.get_trait(\n            FileLocations)\n        # Validate if the file locations on representation\n        # match the frame list (if any).\n        # We need to extend the expected frames with Handles.\n        frame_start = None\n        frame_end = None\n        handles_frame_start = None\n        handles_frame_end = None\n        with contextlib.suppress(MissingTraitError):\n            handles: Handles = representation.get_trait(Handles)\n            # if handles are inclusive, they should be already\n            #  accounted for in the FrameRaged frame spec\n            if not handles.inclusive:\n                handles_frame_start = handles.frame_start_handle\n                handles_frame_end = handles.frame_end_handle\n        with contextlib.suppress(MissingTraitError):\n            frame_ranged: FrameRanged = representation.get_trait(\n                FrameRanged)\n            frame_start = frame_ranged.frame_start\n            frame_end = frame_ranged.frame_end\n        if self.frame_spec is not None:\n            self.validate_frame_list(\n                file_locs,\n                frame_start,\n                frame_end,\n                handles_frame_start,\n                handles_frame_end)\n\n        self.validate_frame_padding(file_locs)\n\n    def validate_frame_list(\n            self,\n            file_locations: FileLocations,\n            frame_start: Optional[int] = None,\n            frame_end: Optional[int] = None,\n            handles_frame_start: Optional[int] = None,\n            handles_frame_end: Optional[int] = None) -&gt; None:\n        \"\"\"Validate a frame list.\n\n        This will take FileLocations trait and validate if the\n        file locations match the frame list specification.\n\n        For example, if the frame list is \"1-10,20-30,40-50\", then\n        the frame numbers in the file locations should match\n        these frames.\n\n        It will skip the validation if the frame list is not provided.\n\n        Args:\n            file_locations (FileLocations): File locations trait.\n            frame_start (Optional[int]): Frame start.\n            frame_end (Optional[int]): Frame end.\n            handles_frame_start (Optional[int]): Frame start handle.\n            handles_frame_end (Optional[int]): Frame end handle.\n\n        Raises:\n            TraitValidationError: If the frame list does not match\n                the expected frames.\n\n        \"\"\"\n        if self.frame_spec is None:\n            return\n\n        frames: list[int] = []\n        if self.frame_regex:\n            frames = self.get_frame_list(\n                    file_locations, self.frame_regex)\n        else:\n            frames = self.get_frame_list(\n                    file_locations)\n\n        expected_frames = self.list_spec_to_frames(self.frame_spec)\n        if frame_start is None or frame_end is None:\n            if min(expected_frames) != frame_start:\n                msg = (\n                    \"Frame start does not match the expected frame start. \"\n                    f\"Expected: {frame_start}, Found: {min(expected_frames)}\"\n                )\n                raise TraitValidationError(self.name, msg)\n\n            if max(expected_frames) != frame_end:\n                msg = (\n                    \"Frame end does not match the expected frame end. \"\n                    f\"Expected: {frame_end}, Found: {max(expected_frames)}\"\n                )\n                raise TraitValidationError(self.name, msg)\n\n        # we need to extend the expected frames with Handles\n        if handles_frame_start is not None:\n            expected_frames.extend(\n                range(\n                    min(frames) - handles_frame_start, min(frames) + 1))\n\n        if handles_frame_end is not None:\n            expected_frames.extend(\n                range(\n                    max(frames), max(frames) + handles_frame_end + 1))\n\n        if set(frames) != set(expected_frames):\n            msg = (\n                \"Frame list does not match the expected frames. \"\n                f\"Expected: {expected_frames}, Found: {frames}\"\n            )\n            raise TraitValidationError(self.name, msg)\n\n    def validate_frame_padding(\n            self, file_locations: FileLocations) -&gt; None:\n        \"\"\"Validate frame padding.\n\n        This will take FileLocations trait and validate if the\n        frame padding matches the expected frame padding.\n\n        Args:\n            file_locations (FileLocations): File locations trait.\n\n        Raises:\n            TraitValidationError: If frame padding does not match\n                the expected frame padding.\n\n        \"\"\"\n        expected_padding = self.get_frame_padding(file_locations)\n        if self.frame_padding != expected_padding:\n            msg = (\n                \"Frame padding does not match the expected frame padding. \"\n                f\"Expected: {expected_padding}, Found: {self.frame_padding}\"\n            )\n            raise TraitValidationError(self.name, msg)\n\n    @staticmethod\n    def list_spec_to_frames(list_spec: str) -&gt; list[int]:\n        \"\"\"Convert list specification to frames.\n\n        Returns:\n            list[int]: List of frame numbers.\n\n        Raises:\n            ValueError: If invalid frame number in the list.\n\n        \"\"\"\n        frames = []\n        segments = list_spec.split(\",\")\n        for segment in segments:\n            ranges = segment.split(\"-\")\n            if len(ranges) == 1:\n                if not ranges[0].isdigit():\n                    msg = (\n                        \"Invalid frame number \"\n                        f\"in the list: {ranges[0]}\"\n                    )\n                    raise ValueError(msg)\n                frames.append(int(ranges[0]))\n                continue\n            start, end = segment.split(\"-\")\n            frames.extend(range(int(start), int(end) + 1))\n        return frames\n\n    @staticmethod\n    def _get_collection(\n        file_locations: FileLocations,\n        regex: Optional[Pattern] = None) -&gt; clique.Collection:\n        r\"\"\"Get the collection from file locations.\n\n        Args:\n            file_locations (FileLocations): File locations trait.\n            regex (Optional[Pattern]): Regular expression to match\n                frame numbers. This is passed to ``clique.assemble()``.\n                Default clique pattern is::\n\n                    \\.(?P&lt;index&gt;(?P&lt;padding&gt;0*)\\d+)\\.\\D+\\d?$\n\n        Returns:\n            clique.Collection: Collection instance.\n\n        Raises:\n            ValueError: If zero or multiple of collections are found.\n\n        \"\"\"\n        patterns = [regex] if regex else None\n        files: list[str] = [\n            file.file_path.as_posix()\n            for file in file_locations.file_paths\n        ]\n        src_collections, _ = clique.assemble(files, patterns=patterns)\n        if len(src_collections) != 1:\n            msg = (\n                f\"Zero or multiple collections found: {len(src_collections)} \"\n                \"expected 1\"\n            )\n            raise ValueError(msg)\n        return src_collections[0]\n\n    @staticmethod\n    def get_frame_padding(file_locations: FileLocations) -&gt; int:\n        \"\"\"Get frame padding.\n\n        Returns:\n            int: Frame padding.\n\n        \"\"\"\n        src_collection = Sequence._get_collection(file_locations)\n        padding = src_collection.padding\n        # sometimes Clique doesn't get the padding right, so\n        # we need to calculate it manually\n        if padding == 0:\n            padding = len(str(max(src_collection.indexes)))\n\n        return padding\n\n    @staticmethod\n    def get_frame_list(\n            file_locations: FileLocations,\n            regex: Optional[Pattern] = None,\n        ) -&gt; list[int]:\n        r\"\"\"Get the frame list.\n\n        Args:\n            file_locations (FileLocations): File locations trait.\n            regex (Optional[Pattern]): Regular expression to match\n                frame numbers. This is passed to ``clique.assemble()``.\n                Default clique pattern is::\n\n                    \\.(?P&lt;index&gt;(?P&lt;padding&gt;0*)\\d+)\\.\\D+\\d?$\n\n        Returns:\n            list[int]: List of frame numbers.\n\n        \"\"\"\n        src_collection = Sequence._get_collection(file_locations, regex)\n        return list(src_collection.indexes)\n\n    def get_frame_pattern(self) -&gt; Pattern:\n        \"\"\"Get frame regex as a pattern.\n\n        If the regex is a string, it will compile it to the pattern.\n\n        Returns:\n            Pattern: Compiled regex pattern.\n\n        \"\"\"\n        if self.frame_regex:\n            if isinstance(self.frame_regex, str):\n                return re.compile(self.frame_regex)\n            return self.frame_regex\n        return re.compile(\n            r\"\\.(?P&lt;index&gt;(?P&lt;padding&gt;0*)\\d+)\\.\\D+\\d?$\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Sequence.get_frame_list","title":"<code>get_frame_list(file_locations, regex=None)</code>  <code>staticmethod</code>","text":"<p>Get the frame list.</p> <p>Parameters:</p> Name Type Description Default <code>file_locations</code> <code>FileLocations</code> <p>File locations trait.</p> required <code>regex</code> <code>Optional[Pattern]</code> <p>Regular expression to match frame numbers. This is passed to <code>clique.assemble()</code>. Default clique pattern is::</p> <pre><code>\\.(?P&lt;index&gt;(?P&lt;padding&gt;0*)\\d+)\\.\\D+\\d?$\n</code></pre> <code>None</code> <p>Returns:</p> Type Description <code>list[int]</code> <p>list[int]: List of frame numbers.</p> Source code in <code>client/ayon_core/pipeline/traits/temporal.py</code> <pre><code>@staticmethod\ndef get_frame_list(\n        file_locations: FileLocations,\n        regex: Optional[Pattern] = None,\n    ) -&gt; list[int]:\n    r\"\"\"Get the frame list.\n\n    Args:\n        file_locations (FileLocations): File locations trait.\n        regex (Optional[Pattern]): Regular expression to match\n            frame numbers. This is passed to ``clique.assemble()``.\n            Default clique pattern is::\n\n                \\.(?P&lt;index&gt;(?P&lt;padding&gt;0*)\\d+)\\.\\D+\\d?$\n\n    Returns:\n        list[int]: List of frame numbers.\n\n    \"\"\"\n    src_collection = Sequence._get_collection(file_locations, regex)\n    return list(src_collection.indexes)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Sequence.get_frame_padding","title":"<code>get_frame_padding(file_locations)</code>  <code>staticmethod</code>","text":"<p>Get frame padding.</p> <p>Returns:</p> Name Type Description <code>int</code> <code>int</code> <p>Frame padding.</p> Source code in <code>client/ayon_core/pipeline/traits/temporal.py</code> <pre><code>@staticmethod\ndef get_frame_padding(file_locations: FileLocations) -&gt; int:\n    \"\"\"Get frame padding.\n\n    Returns:\n        int: Frame padding.\n\n    \"\"\"\n    src_collection = Sequence._get_collection(file_locations)\n    padding = src_collection.padding\n    # sometimes Clique doesn't get the padding right, so\n    # we need to calculate it manually\n    if padding == 0:\n        padding = len(str(max(src_collection.indexes)))\n\n    return padding\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Sequence.get_frame_pattern","title":"<code>get_frame_pattern()</code>","text":"<p>Get frame regex as a pattern.</p> <p>If the regex is a string, it will compile it to the pattern.</p> <p>Returns:</p> Name Type Description <code>Pattern</code> <code>Pattern</code> <p>Compiled regex pattern.</p> Source code in <code>client/ayon_core/pipeline/traits/temporal.py</code> <pre><code>def get_frame_pattern(self) -&gt; Pattern:\n    \"\"\"Get frame regex as a pattern.\n\n    If the regex is a string, it will compile it to the pattern.\n\n    Returns:\n        Pattern: Compiled regex pattern.\n\n    \"\"\"\n    if self.frame_regex:\n        if isinstance(self.frame_regex, str):\n            return re.compile(self.frame_regex)\n        return self.frame_regex\n    return re.compile(\n        r\"\\.(?P&lt;index&gt;(?P&lt;padding&gt;0*)\\d+)\\.\\D+\\d?$\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Sequence.list_spec_to_frames","title":"<code>list_spec_to_frames(list_spec)</code>  <code>staticmethod</code>","text":"<p>Convert list specification to frames.</p> <p>Returns:</p> Type Description <code>list[int]</code> <p>list[int]: List of frame numbers.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If invalid frame number in the list.</p> Source code in <code>client/ayon_core/pipeline/traits/temporal.py</code> <pre><code>@staticmethod\ndef list_spec_to_frames(list_spec: str) -&gt; list[int]:\n    \"\"\"Convert list specification to frames.\n\n    Returns:\n        list[int]: List of frame numbers.\n\n    Raises:\n        ValueError: If invalid frame number in the list.\n\n    \"\"\"\n    frames = []\n    segments = list_spec.split(\",\")\n    for segment in segments:\n        ranges = segment.split(\"-\")\n        if len(ranges) == 1:\n            if not ranges[0].isdigit():\n                msg = (\n                    \"Invalid frame number \"\n                    f\"in the list: {ranges[0]}\"\n                )\n                raise ValueError(msg)\n            frames.append(int(ranges[0]))\n            continue\n        start, end = segment.split(\"-\")\n        frames.extend(range(int(start), int(end) + 1))\n    return frames\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Sequence.validate_frame_list","title":"<code>validate_frame_list(file_locations, frame_start=None, frame_end=None, handles_frame_start=None, handles_frame_end=None)</code>","text":"<p>Validate a frame list.</p> <p>This will take FileLocations trait and validate if the file locations match the frame list specification.</p> <p>For example, if the frame list is \"1-10,20-30,40-50\", then the frame numbers in the file locations should match these frames.</p> <p>It will skip the validation if the frame list is not provided.</p> <p>Parameters:</p> Name Type Description Default <code>file_locations</code> <code>FileLocations</code> <p>File locations trait.</p> required <code>frame_start</code> <code>Optional[int]</code> <p>Frame start.</p> <code>None</code> <code>frame_end</code> <code>Optional[int]</code> <p>Frame end.</p> <code>None</code> <code>handles_frame_start</code> <code>Optional[int]</code> <p>Frame start handle.</p> <code>None</code> <code>handles_frame_end</code> <code>Optional[int]</code> <p>Frame end handle.</p> <code>None</code> <p>Raises:</p> Type Description <code>TraitValidationError</code> <p>If the frame list does not match the expected frames.</p> Source code in <code>client/ayon_core/pipeline/traits/temporal.py</code> <pre><code>def validate_frame_list(\n        self,\n        file_locations: FileLocations,\n        frame_start: Optional[int] = None,\n        frame_end: Optional[int] = None,\n        handles_frame_start: Optional[int] = None,\n        handles_frame_end: Optional[int] = None) -&gt; None:\n    \"\"\"Validate a frame list.\n\n    This will take FileLocations trait and validate if the\n    file locations match the frame list specification.\n\n    For example, if the frame list is \"1-10,20-30,40-50\", then\n    the frame numbers in the file locations should match\n    these frames.\n\n    It will skip the validation if the frame list is not provided.\n\n    Args:\n        file_locations (FileLocations): File locations trait.\n        frame_start (Optional[int]): Frame start.\n        frame_end (Optional[int]): Frame end.\n        handles_frame_start (Optional[int]): Frame start handle.\n        handles_frame_end (Optional[int]): Frame end handle.\n\n    Raises:\n        TraitValidationError: If the frame list does not match\n            the expected frames.\n\n    \"\"\"\n    if self.frame_spec is None:\n        return\n\n    frames: list[int] = []\n    if self.frame_regex:\n        frames = self.get_frame_list(\n                file_locations, self.frame_regex)\n    else:\n        frames = self.get_frame_list(\n                file_locations)\n\n    expected_frames = self.list_spec_to_frames(self.frame_spec)\n    if frame_start is None or frame_end is None:\n        if min(expected_frames) != frame_start:\n            msg = (\n                \"Frame start does not match the expected frame start. \"\n                f\"Expected: {frame_start}, Found: {min(expected_frames)}\"\n            )\n            raise TraitValidationError(self.name, msg)\n\n        if max(expected_frames) != frame_end:\n            msg = (\n                \"Frame end does not match the expected frame end. \"\n                f\"Expected: {frame_end}, Found: {max(expected_frames)}\"\n            )\n            raise TraitValidationError(self.name, msg)\n\n    # we need to extend the expected frames with Handles\n    if handles_frame_start is not None:\n        expected_frames.extend(\n            range(\n                min(frames) - handles_frame_start, min(frames) + 1))\n\n    if handles_frame_end is not None:\n        expected_frames.extend(\n            range(\n                max(frames), max(frames) + handles_frame_end + 1))\n\n    if set(frames) != set(expected_frames):\n        msg = (\n            \"Frame list does not match the expected frames. \"\n            f\"Expected: {expected_frames}, Found: {frames}\"\n        )\n        raise TraitValidationError(self.name, msg)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Sequence.validate_frame_padding","title":"<code>validate_frame_padding(file_locations)</code>","text":"<p>Validate frame padding.</p> <p>This will take FileLocations trait and validate if the frame padding matches the expected frame padding.</p> <p>Parameters:</p> Name Type Description Default <code>file_locations</code> <code>FileLocations</code> <p>File locations trait.</p> required <p>Raises:</p> Type Description <code>TraitValidationError</code> <p>If frame padding does not match the expected frame padding.</p> Source code in <code>client/ayon_core/pipeline/traits/temporal.py</code> <pre><code>def validate_frame_padding(\n        self, file_locations: FileLocations) -&gt; None:\n    \"\"\"Validate frame padding.\n\n    This will take FileLocations trait and validate if the\n    frame padding matches the expected frame padding.\n\n    Args:\n        file_locations (FileLocations): File locations trait.\n\n    Raises:\n        TraitValidationError: If frame padding does not match\n            the expected frame padding.\n\n    \"\"\"\n    expected_padding = self.get_frame_padding(file_locations)\n    if self.frame_padding != expected_padding:\n        msg = (\n            \"Frame padding does not match the expected frame padding. \"\n            f\"Expected: {expected_padding}, Found: {self.frame_padding}\"\n        )\n        raise TraitValidationError(self.name, msg)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Sequence.validate_frame_regex","title":"<code>validate_frame_regex(v)</code>  <code>classmethod</code>","text":"<p>Validate frame regex.</p> <p>Frame regex must have index and padding named groups.</p> <p>Returns:</p> Type Description <code>Optional[Pattern]</code> <p>Optional[Pattern]: Compiled regex pattern.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If frame regex does not include 'index' and 'padding'</p> Source code in <code>client/ayon_core/pipeline/traits/temporal.py</code> <pre><code>@classmethod\ndef validate_frame_regex(\n    cls, v: Optional[Pattern]\n) -&gt; Optional[Pattern]:\n    \"\"\"Validate frame regex.\n\n    Frame regex must have index and padding named groups.\n\n    Returns:\n        Optional[Pattern]: Compiled regex pattern.\n\n    Raises:\n        ValueError: If frame regex does not include 'index' and 'padding'\n\n    \"\"\"\n    if v is None:\n        return v\n    if v and any(s not in v.pattern for s in [\"?P&lt;index&gt;\", \"?P&lt;padding&gt;\"]):\n        msg = \"Frame regex must include 'index' and `padding named groups\"\n        raise ValueError(msg)\n    return v\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Sequence.validate_trait","title":"<code>validate_trait(representation)</code>","text":"<p>Validate the trait.</p> Source code in <code>client/ayon_core/pipeline/traits/temporal.py</code> <pre><code>def validate_trait(self, representation: Representation) -&gt; None:\n    \"\"\"Validate the trait.\"\"\"\n    super().validate_trait(representation)\n\n    # if there is a FileLocations trait, run validation\n    # on it as well\n\n    with contextlib.suppress(MissingTraitError):\n        self._validate_file_locations(representation)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Shader","title":"<code>Shader</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>Shader trait model.</p> <p>Type trait for shader data.</p> <p>Sync with OpenAssetIO MediaCreation Traits.</p> Source code in <code>client/ayon_core/pipeline/traits/three_dimensional.py</code> <pre><code>@dataclass\nclass Shader(TraitBase):\n    \"\"\"Shader trait model.\n\n    Type trait for shader data.\n\n    Sync with OpenAssetIO MediaCreation Traits.\n    \"\"\"\n\n    id: ClassVar[str] = \"ayon.3d.Shader.v1\"\n    name: ClassVar[str] = \"Shader\"\n    description: ClassVar[str] = \"Shader trait model.\"\n    persistent: ClassVar[bool] = True\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.SourceApplication","title":"<code>SourceApplication</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>Metadata about the source (producing) application.</p> <p>This can be useful in cases where this information is needed, but it cannot be determined from other means - like .txt files used for various motion tracking applications that must be interpreted by the loader.</p> <p>Note that this is not really connected to any logic in ayon-applications addon.</p> <p>Attributes:</p> Name Type Description <code>application</code> <code>str</code> <p>Application name.</p> <code>variant</code> <code>str</code> <p>Application variant.</p> <code>version</code> <code>str</code> <p>Application version.</p> <code>platform</code> <code>str</code> <p>Platform name (Windows, darwin, etc.).</p> <code>host_name</code> <code>str</code> <p>AYON host name if applicable.</p> Source code in <code>client/ayon_core/pipeline/traits/meta.py</code> <pre><code>@dataclass\nclass SourceApplication(TraitBase):\n    \"\"\"Metadata about the source (producing) application.\n\n    This can be useful in cases where this information is\n    needed, but it cannot be determined from other means - like\n    .txt files used for various motion tracking applications that\n    must be interpreted by the loader.\n\n    Note that this is not really connected to any logic in\n    ayon-applications addon.\n\n    Attributes:\n        application (str): Application name.\n        variant (str): Application variant.\n        version (str): Application version.\n        platform (str): Platform name (Windows, darwin, etc.).\n        host_name (str): AYON host name if applicable.\n    \"\"\"\n\n    name: ClassVar[str] = \"SourceApplication\"\n    description: ClassVar[str] = \"Source Application Trait Model\"\n    id: ClassVar[str] = \"ayon.meta.SourceApplication.v1\"\n    persistent: ClassVar[bool] = True\n    application: str\n    variant: Optional[str] = None\n    version: Optional[str] = None\n    platform: Optional[str] = None\n    host_name: Optional[str] = None\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Spatial","title":"<code>Spatial</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>Spatial trait model.</p> <p>Trait describing spatial information. Up axis valid strings are \"Y\", \"Z\", \"X\". Handedness valid strings are \"left\", \"right\". Meters per unit is a float value.</p> <p>Example::</p> <pre><code>    Spatial(up_axis=\"Y\", handedness=\"right\", meters_per_unit=1.0)\n</code></pre> Todo <ul> <li>Add value validation for up_axis and handedness.</li> </ul> <p>Attributes:</p> Name Type Description <code>up_axis</code> <code>str</code> <p>Up axis.</p> <code>handedness</code> <code>str</code> <p>Handedness.</p> <code>meters_per_unit</code> <code>float</code> <p>Meters per unit.</p> Source code in <code>client/ayon_core/pipeline/traits/three_dimensional.py</code> <pre><code>@dataclass\nclass Spatial(TraitBase):\n    \"\"\"Spatial trait model.\n\n    Trait describing spatial information. Up axis valid strings are\n    \"Y\", \"Z\", \"X\". Handedness valid strings are \"left\", \"right\". Meters per\n    unit is a float value.\n\n    Example::\n\n            Spatial(up_axis=\"Y\", handedness=\"right\", meters_per_unit=1.0)\n\n    Todo:\n        * Add value validation for up_axis and handedness.\n\n    Attributes:\n        up_axis (str): Up axis.\n        handedness (str): Handedness.\n        meters_per_unit (float): Meters per unit.\n    \"\"\"\n\n    id: ClassVar[str] = \"ayon.3d.Spatial.v1\"\n    name: ClassVar[str] = \"Spatial\"\n    description: ClassVar[str] = \"Spatial trait model.\"\n    persistent: ClassVar[bool] = True\n    up_axis: str\n    handedness: str\n    meters_per_unit: float\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Static","title":"<code>Static</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>Static time trait.</p> <p>Used to define static time (single frame).</p> Source code in <code>client/ayon_core/pipeline/traits/temporal.py</code> <pre><code>@dataclass\nclass Static(TraitBase):\n    \"\"\"Static time trait.\n\n    Used to define static time (single frame).\n    \"\"\"\n\n    name: ClassVar[str] = \"Static\"\n    description: ClassVar[str] = \"Static Time Trait\"\n    id: ClassVar[str] = \"ayon.time.Static.v1\"\n    persistent: ClassVar[bool] = True\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Tagged","title":"<code>Tagged</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>Tagged trait model.</p> <p>This trait can hold a list of tags.</p> <p>Example::</p> <pre><code>Tagged(tags=[\"tag1\", \"tag2\"])\n</code></pre> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Trait name.</p> <code>description</code> <code>str</code> <p>Trait description.</p> <code>id</code> <code>str</code> <p>id should be a namespaced trait name with version</p> <code>tags</code> <code>List[str]</code> <p>Tags.</p> Source code in <code>client/ayon_core/pipeline/traits/meta.py</code> <pre><code>@dataclass\nclass Tagged(TraitBase):\n    \"\"\"Tagged trait model.\n\n    This trait can hold a list of tags.\n\n    Example::\n\n        Tagged(tags=[\"tag1\", \"tag2\"])\n\n    Attributes:\n        name (str): Trait name.\n        description (str): Trait description.\n        id (str): id should be a namespaced trait name with version\n        tags (List[str]): Tags.\n    \"\"\"\n\n    name: ClassVar[str] = \"Tagged\"\n    description: ClassVar[str] = \"Tagged Trait Model\"\n    id: ClassVar[str] = \"ayon.meta.Tagged.v1\"\n    persistent: ClassVar[bool] = True\n    tags: List[str]\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.TemplatePath","title":"<code>TemplatePath</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>TemplatePath trait model.</p> <p>This model represents a template path with formatting data. Template path can be an Anatomy template and data is used to format it.</p> <p>Example::</p> <pre><code>TemplatePath(template=\"path/{key}/file\", data={\"key\": \"to\"})\n</code></pre> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Trait name.</p> <code>description</code> <code>str</code> <p>Trait description.</p> <code>id</code> <code>str</code> <p>id should be a namespaced trait name with version</p> <code>template</code> <code>str</code> <p>Template path.</p> <code>data</code> <code>dict[str]</code> <p>Formatting data.</p> Source code in <code>client/ayon_core/pipeline/traits/meta.py</code> <pre><code>@dataclass\nclass TemplatePath(TraitBase):\n    \"\"\"TemplatePath trait model.\n\n    This model represents a template path with formatting data.\n    Template path can be an Anatomy template and data is used to format it.\n\n    Example::\n\n        TemplatePath(template=\"path/{key}/file\", data={\"key\": \"to\"})\n\n    Attributes:\n        name (str): Trait name.\n        description (str): Trait description.\n        id (str): id should be a namespaced trait name with version\n        template (str): Template path.\n        data (dict[str]): Formatting data.\n    \"\"\"\n\n    name: ClassVar[str] = \"TemplatePath\"\n    description: ClassVar[str] = \"Template Path Trait Model\"\n    id: ClassVar[str] = \"ayon.meta.TemplatePath.v1\"\n    persistent: ClassVar[bool] = True\n    template: str\n    data: dict\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.TraitBase","title":"<code>TraitBase</code>  <code>dataclass</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Base trait model.</p> <p>This model must be used as a base for all trait models. <code>id</code>, <code>name</code>, and <code>description</code> are abstract attributes that must be implemented in the derived classes.</p> Source code in <code>client/ayon_core/pipeline/traits/trait.py</code> <pre><code>@dataclass\nclass TraitBase(ABC):\n    \"\"\"Base trait model.\n\n    This model must be used as a base for all trait models.\n    ``id``, ``name``, and ``description`` are abstract attributes that must be\n    implemented in the derived classes.\n    \"\"\"\n\n    @property\n    @abstractmethod\n    def id(self) -&gt; str:\n        \"\"\"Abstract attribute for ID.\"\"\"\n        ...\n\n    @property\n    @abstractmethod\n    def name(self) -&gt; str:\n        \"\"\"Abstract attribute for name.\"\"\"\n        ...\n\n    @property\n    @abstractmethod\n    def description(self) -&gt; str:\n        \"\"\"Abstract attribute for description.\"\"\"\n        ...\n\n    def validate_trait(self, representation: Representation) -&gt; None:  # noqa: PLR6301\n        \"\"\"Validate the trait.\n\n        This method should be implemented in the derived classes to validate\n        the trait data. It can be used by traits to validate against other\n        traits in the representation.\n\n        Args:\n            representation (Representation): Representation instance.\n\n        \"\"\"\n        return\n\n    @classmethod\n    def get_version(cls) -&gt; Optional[int]:\n        # sourcery skip: use-named-expression\n        \"\"\"Get a trait version from ID.\n\n        This assumes Trait ID ends with `.v{version}`. If not, it will\n        return None.\n\n        Returns:\n            Optional[int]: Trait version\n\n        \"\"\"\n        version_regex = r\"v(\\d+)$\"\n        match = re.search(version_regex, str(cls.id))\n        return int(match[1]) if match else None\n\n    @classmethod\n    def get_versionless_id(cls) -&gt; str:\n        \"\"\"Get a trait ID without a version.\n\n        Returns:\n            str: Trait ID without a version.\n\n        \"\"\"\n        return re.sub(r\"\\.v\\d+$\", \"\", str(cls.id))\n\n    def as_dict(self) -&gt; dict:\n        \"\"\"Return a trait as a dictionary.\n\n        Returns:\n            dict: Trait as dictionary.\n\n        \"\"\"\n        return asdict(self)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.TraitBase.description","title":"<code>description</code>  <code>abstractmethod</code> <code>property</code>","text":"<p>Abstract attribute for description.</p>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.TraitBase.id","title":"<code>id</code>  <code>abstractmethod</code> <code>property</code>","text":"<p>Abstract attribute for ID.</p>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.TraitBase.name","title":"<code>name</code>  <code>abstractmethod</code> <code>property</code>","text":"<p>Abstract attribute for name.</p>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.TraitBase.as_dict","title":"<code>as_dict()</code>","text":"<p>Return a trait as a dictionary.</p> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict</code> <p>Trait as dictionary.</p> Source code in <code>client/ayon_core/pipeline/traits/trait.py</code> <pre><code>def as_dict(self) -&gt; dict:\n    \"\"\"Return a trait as a dictionary.\n\n    Returns:\n        dict: Trait as dictionary.\n\n    \"\"\"\n    return asdict(self)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.TraitBase.get_version","title":"<code>get_version()</code>  <code>classmethod</code>","text":"<p>Get a trait version from ID.</p> <p>This assumes Trait ID ends with <code>.v{version}</code>. If not, it will return None.</p> <p>Returns:</p> Type Description <code>Optional[int]</code> <p>Optional[int]: Trait version</p> Source code in <code>client/ayon_core/pipeline/traits/trait.py</code> <pre><code>@classmethod\ndef get_version(cls) -&gt; Optional[int]:\n    # sourcery skip: use-named-expression\n    \"\"\"Get a trait version from ID.\n\n    This assumes Trait ID ends with `.v{version}`. If not, it will\n    return None.\n\n    Returns:\n        Optional[int]: Trait version\n\n    \"\"\"\n    version_regex = r\"v(\\d+)$\"\n    match = re.search(version_regex, str(cls.id))\n    return int(match[1]) if match else None\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.TraitBase.get_versionless_id","title":"<code>get_versionless_id()</code>  <code>classmethod</code>","text":"<p>Get a trait ID without a version.</p> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Trait ID without a version.</p> Source code in <code>client/ayon_core/pipeline/traits/trait.py</code> <pre><code>@classmethod\ndef get_versionless_id(cls) -&gt; str:\n    \"\"\"Get a trait ID without a version.\n\n    Returns:\n        str: Trait ID without a version.\n\n    \"\"\"\n    return re.sub(r\"\\.v\\d+$\", \"\", str(cls.id))\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.TraitBase.validate_trait","title":"<code>validate_trait(representation)</code>","text":"<p>Validate the trait.</p> <p>This method should be implemented in the derived classes to validate the trait data. It can be used by traits to validate against other traits in the representation.</p> <p>Parameters:</p> Name Type Description Default <code>representation</code> <code>Representation</code> <p>Representation instance.</p> required Source code in <code>client/ayon_core/pipeline/traits/trait.py</code> <pre><code>def validate_trait(self, representation: Representation) -&gt; None:  # noqa: PLR6301\n    \"\"\"Validate the trait.\n\n    This method should be implemented in the derived classes to validate\n    the trait data. It can be used by traits to validate against other\n    traits in the representation.\n\n    Args:\n        representation (Representation): Representation instance.\n\n    \"\"\"\n    return\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.TraitValidationError","title":"<code>TraitValidationError</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Trait validation error exception.</p> <p>This exception is raised when the trait validation fails.</p> Source code in <code>client/ayon_core/pipeline/traits/trait.py</code> <pre><code>class TraitValidationError(Exception):\n    \"\"\"Trait validation error exception.\n\n    This exception is raised when the trait validation fails.\n    \"\"\"\n\n    def __init__(self, scope: str, message: str):\n        \"\"\"Initialize the exception.\n\n        We could determine the scope from the stack in the future,\n        provided the scope is always Trait name.\n\n        Args:\n            scope (str): Scope of the error.\n            message (str): Error message.\n\n        \"\"\"\n        super().__init__(f\"{scope}: {message}\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.TraitValidationError.__init__","title":"<code>__init__(scope, message)</code>","text":"<p>Initialize the exception.</p> <p>We could determine the scope from the stack in the future, provided the scope is always Trait name.</p> <p>Parameters:</p> Name Type Description Default <code>scope</code> <code>str</code> <p>Scope of the error.</p> required <code>message</code> <code>str</code> <p>Error message.</p> required Source code in <code>client/ayon_core/pipeline/traits/trait.py</code> <pre><code>def __init__(self, scope: str, message: str):\n    \"\"\"Initialize the exception.\n\n    We could determine the scope from the stack in the future,\n    provided the scope is always Trait name.\n\n    Args:\n        scope (str): Scope of the error.\n        message (str): Error message.\n\n    \"\"\"\n    super().__init__(f\"{scope}: {message}\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.TransferItem","title":"<code>TransferItem</code>","text":"<p>Represents a single transfer item.</p> <p>Source file path, destination file path, template that was used to construct the destination path, template data that was used in the template, size of the file, checksum of the file.</p> <p>Attributes:</p> Name Type Description <code>source</code> <code>Path</code> <p>Source file path.</p> <code>destination</code> <code>Path</code> <p>Destination file path.</p> <code>size</code> <code>int</code> <p>Size of the file.</p> <code>checksum</code> <code>str</code> <p>Checksum of the file.</p> <code>template</code> <code>str</code> <p>Template path.</p> <code>template_data</code> <code>dict[str, Any]</code> <p>Template data.</p> <code>representation</code> <code>Representation</code> <p>Reference to representation</p> <code>related_trait</code> <code>FileLocation</code> <p>Reference to the trait that this transfer is related to. This is used to update the trait with the new file path after the transfer is done.</p> Source code in <code>client/ayon_core/pipeline/traits/publishing.py</code> <pre><code>class TransferItem:\n    \"\"\"Represents a single transfer item.\n\n    Source file path, destination file path, template that was used to\n    construct the destination path, template data that was used in the\n    template, size of the file, checksum of the file.\n\n    Attributes:\n        source (Path): Source file path.\n        destination (Path): Destination file path.\n        size (int): Size of the file.\n        checksum (str): Checksum of the file.\n        template (str): Template path.\n        template_data (dict[str, Any]): Template data.\n        representation (Representation): Reference to representation\n        related_trait (FileLocation): Reference to the trait that this\n            transfer is related to. This is used to update the trait with\n            the new file path after the transfer is done.\n\n    \"\"\"\n    source: Path\n    destination: Path\n    size: int\n    checksum: str\n    template: str\n    template_data: dict[str, Any]\n    representation: Representation\n    related_trait: FileLocation\n\n    def __init__(self,\n        source: Path,\n        destination: Path,\n        size: int,\n        checksum: str,\n        template: str,\n        template_data: dict[str, Any],\n        representation: Representation,\n        related_trait: FileLocation):\n\n        self.source = source\n        self.destination = destination\n        self.size = size\n        self.checksum = checksum\n        self.template = template\n        self.template_data = template_data\n        self.representation = representation\n        self.related_trait = related_trait\n\n    @staticmethod\n    def get_file_size(file_path: Path) -&gt; int:\n        \"\"\"Get the size of the file.\n\n        Args:\n            file_path (Path): File path.\n\n        Returns:\n            int: Size of the file.\n\n        \"\"\"\n        return file_path.stat().st_size\n\n    @staticmethod\n    def get_file_checksum(file_path: Path) -&gt; str:\n        \"\"\"Get checksum of the file.\n\n        Args:\n            file_path (Path): File path.\n\n        Returns:\n            str: Checksum of the file.\n\n        \"\"\"\n        return hashlib.sha256(\n            file_path.read_bytes()\n        ).hexdigest()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.TransferItem.get_file_checksum","title":"<code>get_file_checksum(file_path)</code>  <code>staticmethod</code>","text":"<p>Get checksum of the file.</p> <p>Parameters:</p> Name Type Description Default <code>file_path</code> <code>Path</code> <p>File path.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Checksum of the file.</p> Source code in <code>client/ayon_core/pipeline/traits/publishing.py</code> <pre><code>@staticmethod\ndef get_file_checksum(file_path: Path) -&gt; str:\n    \"\"\"Get checksum of the file.\n\n    Args:\n        file_path (Path): File path.\n\n    Returns:\n        str: Checksum of the file.\n\n    \"\"\"\n    return hashlib.sha256(\n        file_path.read_bytes()\n    ).hexdigest()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.TransferItem.get_file_size","title":"<code>get_file_size(file_path)</code>  <code>staticmethod</code>","text":"<p>Get the size of the file.</p> <p>Parameters:</p> Name Type Description Default <code>file_path</code> <code>Path</code> <p>File path.</p> required <p>Returns:</p> Name Type Description <code>int</code> <code>int</code> <p>Size of the file.</p> Source code in <code>client/ayon_core/pipeline/traits/publishing.py</code> <pre><code>@staticmethod\ndef get_file_size(file_path: Path) -&gt; int:\n    \"\"\"Get the size of the file.\n\n    Args:\n        file_path (Path): File path.\n\n    Returns:\n        int: Size of the file.\n\n    \"\"\"\n    return file_path.stat().st_size\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Transient","title":"<code>Transient</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>Transient trait model.</p> <p>Transient trait marks representation as transient. Such representations are not persisted in the system.</p> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Trait name.</p> <code>description</code> <code>str</code> <p>Trait description.</p> <code>id</code> <code>str</code> <p>id should be a namespaced trait name with the version</p> Source code in <code>client/ayon_core/pipeline/traits/lifecycle.py</code> <pre><code>@dataclass\nclass Transient(TraitBase):\n    \"\"\"Transient trait model.\n\n    Transient trait marks representation as transient. Such representations\n    are not persisted in the system.\n\n    Attributes:\n        name (str): Trait name.\n        description (str): Trait description.\n        id (str): id should be a namespaced trait name with the version\n    \"\"\"\n\n    name: ClassVar[str] = \"Transient\"\n    description: ClassVar[str] = \"Transient Trait Model\"\n    id: ClassVar[str] = \"ayon.lifecycle.Transient.v1\"\n    persistent: ClassVar[bool] = True  # see note in Persistent\n\n    def validate_trait(self, representation) -&gt; None:  # noqa: ANN001\n        \"\"\"Validate representation is not Persistent.\n\n        Args:\n            representation (Representation): Representation model.\n\n        Raises:\n            TraitValidationError: If representation is marked as both\n                Persistent and Transient.\n\n        \"\"\"\n        if representation.contains_trait(Persistent):\n            msg = \"Representation is marked as both Persistent and Transient.\"\n            raise TraitValidationError(self.name, msg)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Transient.validate_trait","title":"<code>validate_trait(representation)</code>","text":"<p>Validate representation is not Persistent.</p> <p>Parameters:</p> Name Type Description Default <code>representation</code> <code>Representation</code> <p>Representation model.</p> required <p>Raises:</p> Type Description <code>TraitValidationError</code> <p>If representation is marked as both Persistent and Transient.</p> Source code in <code>client/ayon_core/pipeline/traits/lifecycle.py</code> <pre><code>def validate_trait(self, representation) -&gt; None:  # noqa: ANN001\n    \"\"\"Validate representation is not Persistent.\n\n    Args:\n        representation (Representation): Representation model.\n\n    Raises:\n        TraitValidationError: If representation is marked as both\n            Persistent and Transient.\n\n    \"\"\"\n    if representation.contains_trait(Persistent):\n        msg = \"Representation is marked as both Persistent and Transient.\"\n        raise TraitValidationError(self.name, msg)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.UDIM","title":"<code>UDIM</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>UDIM trait model.</p> <p>This model represents a UDIM trait.</p> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Trait name.</p> <code>description</code> <code>str</code> <p>Trait description.</p> <code>id</code> <code>str</code> <p>id should be namespaced trait name with version</p> <code>udim</code> <code>int</code> <p>UDIM value.</p> <code>udim_regex</code> <code>str</code> <p>UDIM regex.</p> Source code in <code>client/ayon_core/pipeline/traits/two_dimensional.py</code> <pre><code>@dataclass\nclass UDIM(TraitBase):\n    \"\"\"UDIM trait model.\n\n    This model represents a UDIM trait.\n\n    Attributes:\n        name (str): Trait name.\n        description (str): Trait description.\n        id (str): id should be namespaced trait name with version\n        udim (int): UDIM value.\n        udim_regex (str): UDIM regex.\n    \"\"\"\n\n    name: ClassVar[str] = \"UDIM\"\n    description: ClassVar[str] = \"UDIM Trait\"\n    id: ClassVar[str] = \"ayon.2d.UDIM.v1\"\n    persistent: ClassVar[bool] = True\n    udim: list[int]\n    udim_regex: Optional[str] = r\"(?:\\.|_)(?P&lt;udim&gt;\\d+)\\.\\D+\\d?$\"\n\n    # Field validator for udim_regex - this works in the pydantic model v2\n    # but not with the pure data classes.\n    @classmethod\n    def validate_frame_regex(cls, v: Optional[str]) -&gt; Optional[str]:\n        \"\"\"Validate udim regex.\n\n        Returns:\n            Optional[str]: UDIM regex.\n\n        Raises:\n            ValueError: UDIM regex must include 'udim' named group.\n\n        \"\"\"\n        if v is not None and \"?P&lt;udim&gt;\" not in v:\n            msg = \"UDIM regex must include 'udim' named group\"\n            raise ValueError(msg)\n        return v\n\n    def get_file_location_for_udim(\n            self,\n            file_locations: FileLocations,\n            udim: int,\n        ) -&gt; Optional[FileLocation]:\n        \"\"\"Get file location for UDIM.\n\n        Args:\n            file_locations (FileLocations): File locations.\n            udim (int): UDIM value.\n\n        Returns:\n            Optional[FileLocation]: File location.\n\n        \"\"\"\n        if not self.udim_regex:\n            return None\n        pattern = re.compile(self.udim_regex)\n        for location in file_locations.file_paths:\n            result = re.search(pattern, location.file_path.name)\n            if result:\n                udim_index = int(result.group(\"udim\"))\n                if udim_index == udim:\n                    return location\n        return None\n\n    def get_udim_from_file_location(\n            self, file_location: FileLocation) -&gt; Optional[int]:\n        \"\"\"Get UDIM from the file location.\n\n        Args:\n            file_location (FileLocation): File location.\n\n        Returns:\n            Optional[int]: UDIM value.\n\n        \"\"\"\n        if not self.udim_regex:\n            return None\n        pattern = re.compile(self.udim_regex)\n        result = re.search(pattern, file_location.file_path.name)\n        if result:\n            return int(result.group(\"udim\"))\n        return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.UDIM.get_file_location_for_udim","title":"<code>get_file_location_for_udim(file_locations, udim)</code>","text":"<p>Get file location for UDIM.</p> <p>Parameters:</p> Name Type Description Default <code>file_locations</code> <code>FileLocations</code> <p>File locations.</p> required <code>udim</code> <code>int</code> <p>UDIM value.</p> required <p>Returns:</p> Type Description <code>Optional[FileLocation]</code> <p>Optional[FileLocation]: File location.</p> Source code in <code>client/ayon_core/pipeline/traits/two_dimensional.py</code> <pre><code>def get_file_location_for_udim(\n        self,\n        file_locations: FileLocations,\n        udim: int,\n    ) -&gt; Optional[FileLocation]:\n    \"\"\"Get file location for UDIM.\n\n    Args:\n        file_locations (FileLocations): File locations.\n        udim (int): UDIM value.\n\n    Returns:\n        Optional[FileLocation]: File location.\n\n    \"\"\"\n    if not self.udim_regex:\n        return None\n    pattern = re.compile(self.udim_regex)\n    for location in file_locations.file_paths:\n        result = re.search(pattern, location.file_path.name)\n        if result:\n            udim_index = int(result.group(\"udim\"))\n            if udim_index == udim:\n                return location\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.UDIM.get_udim_from_file_location","title":"<code>get_udim_from_file_location(file_location)</code>","text":"<p>Get UDIM from the file location.</p> <p>Parameters:</p> Name Type Description Default <code>file_location</code> <code>FileLocation</code> <p>File location.</p> required <p>Returns:</p> Type Description <code>Optional[int]</code> <p>Optional[int]: UDIM value.</p> Source code in <code>client/ayon_core/pipeline/traits/two_dimensional.py</code> <pre><code>def get_udim_from_file_location(\n        self, file_location: FileLocation) -&gt; Optional[int]:\n    \"\"\"Get UDIM from the file location.\n\n    Args:\n        file_location (FileLocation): File location.\n\n    Returns:\n        Optional[int]: UDIM value.\n\n    \"\"\"\n    if not self.udim_regex:\n        return None\n    pattern = re.compile(self.udim_regex)\n    result = re.search(pattern, file_location.file_path.name)\n    if result:\n        return int(result.group(\"udim\"))\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.UDIM.validate_frame_regex","title":"<code>validate_frame_regex(v)</code>  <code>classmethod</code>","text":"<p>Validate udim regex.</p> <p>Returns:</p> Type Description <code>Optional[str]</code> <p>Optional[str]: UDIM regex.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>UDIM regex must include 'udim' named group.</p> Source code in <code>client/ayon_core/pipeline/traits/two_dimensional.py</code> <pre><code>@classmethod\ndef validate_frame_regex(cls, v: Optional[str]) -&gt; Optional[str]:\n    \"\"\"Validate udim regex.\n\n    Returns:\n        Optional[str]: UDIM regex.\n\n    Raises:\n        ValueError: UDIM regex must include 'udim' named group.\n\n    \"\"\"\n    if v is not None and \"?P&lt;udim&gt;\" not in v:\n        msg = \"UDIM regex must include 'udim' named group\"\n        raise ValueError(msg)\n    return v\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.Variant","title":"<code>Variant</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>Variant trait model.</p> <p>This model represents a variant of the representation.</p> <p>Example::</p> <pre><code>Variant(variant=\"high\")\nVariant(variant=\"prores444)\n</code></pre> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Trait name.</p> <code>description</code> <code>str</code> <p>Trait description.</p> <code>id</code> <code>str</code> <p>id should be a namespaced trait name with version</p> <code>variant</code> <code>str</code> <p>Variant name.</p> Source code in <code>client/ayon_core/pipeline/traits/meta.py</code> <pre><code>@dataclass\nclass Variant(TraitBase):\n    \"\"\"Variant trait model.\n\n    This model represents a variant of the representation.\n\n    Example::\n\n        Variant(variant=\"high\")\n        Variant(variant=\"prores444)\n\n    Attributes:\n        name (str): Trait name.\n        description (str): Trait description.\n        id (str): id should be a namespaced trait name with version\n        variant (str): Variant name.\n    \"\"\"\n\n    name: ClassVar[str] = \"Variant\"\n    description: ClassVar[str] = \"Variant Trait Model\"\n    id: ClassVar[str] = \"ayon.meta.Variant.v1\"\n    persistent: ClassVar[bool] = True\n    variant: str\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.get_legacy_files_for_representation","title":"<code>get_legacy_files_for_representation(transfer_items, representation, anatomy)</code>","text":"<p>Get legacy files for a given representation.</p> <p>This expects the file to exist - it must run after the transfer is done.</p> <p>This is used to prepare file information for tools working with legacy file data on representation. Like site-sync, etc.</p> <p>Parameters:</p> Name Type Description Default <code>transfer_items</code> <code>list[TransferItem]</code> <p>List of transfer items.</p> required <code>representation</code> <code>Representation</code> <p>Representation to get files for.</p> required <code>anatomy</code> <code>Anatomy</code> <p>Anatomy to use for preparing file info.</p> required <p>Returns:</p> Name Type Description <code>list</code> <code>list[dict[str, str]]</code> <p>List of legacy files.</p> Source code in <code>client/ayon_core/pipeline/traits/publishing.py</code> <pre><code>def get_legacy_files_for_representation(\n        transfer_items: list[TransferItem],\n        representation: Representation,\n        anatomy: \"Anatomy\",\n    ) -&gt; list[dict[str, str]]:\n    \"\"\"Get legacy files for a given representation.\n\n    This expects the file to exist - it must run after the transfer\n    is done.\n\n    This is used to prepare file information for tools working with legacy\n    file data on representation. Like site-sync, etc.\n\n    Args:\n        transfer_items (list[TransferItem]): List of transfer items.\n        representation (Representation): Representation to get files for.\n        anatomy (Anatomy): Anatomy to use for preparing file info.\n\n    Returns:\n        list: List of legacy files.\n\n    \"\"\"\n    selected: list[TransferItem] = []\n    selected.extend(\n        item\n        for item in transfer_items\n        if item.representation == representation\n    )\n    files: list[dict[str, str]] = []\n    files.extend(\n        _prepare_file_info(item.destination, anatomy)\n        for item in selected\n    )\n    return files\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.get_template_data_from_representation","title":"<code>get_template_data_from_representation(representation, instance)</code>","text":"<p>Get template data from representation.</p> <p>Using representation traits and data on instance prepare data for formatting template.</p> <p>Parameters:</p> Name Type Description Default <code>representation</code> <code>Representation</code> <p>Representation to process.</p> required <code>instance</code> <code>Instance</code> <p>Instance to process.</p> required <p>Returns:</p> Name Type Description <code>dict</code> <code>dict</code> <p>Template data.</p> Source code in <code>client/ayon_core/pipeline/traits/publishing.py</code> <pre><code>def get_template_data_from_representation(\n        representation: Representation,\n        instance: pyblish.api.Instance) -&gt; dict:\n    \"\"\"Get template data from representation.\n\n    Using representation traits and data on instance\n    prepare data for formatting template.\n\n    Args:\n        representation (Representation): Representation to process.\n        instance (pyblish.api.Instance): Instance to process.\n\n    Returns:\n        dict: Template data.\n\n    \"\"\"\n    template_data = copy.deepcopy(instance.data[\"anatomyData\"])\n    template_data[\"representation\"] = representation.name\n    template_data[\"version\"] = instance.data[\"version\"]\n    # template_data[\"hierarchy\"] = instance.data[\"hierarchy\"]\n\n    # add colorspace data to template data\n    if representation.contains_trait(ColorManaged):\n        colorspace_data: ColorManaged = representation.get_trait(\n            ColorManaged)\n\n        template_data[\"colorspace\"] = {\n            \"colorspace\": colorspace_data.color_space,\n            \"config\": colorspace_data.config\n        }\n\n    # add explicit list of traits properties to template data\n    # there must be some better way to handle this.\n\n    with contextlib.suppress(MissingTraitError):\n        # resolution from PixelBased trait\n        template_data[\"resolution_width\"] = representation.get_trait(\n            PixelBased).display_window_width\n        template_data[\"resolution_height\"] = representation.get_trait(\n            PixelBased).display_window_height\n\n    with contextlib.suppress(MissingTraitError):\n        # get fps from representation traits\n        template_data[\"fps\"] = representation.get_trait(\n            FrameRanged).frames_per_second\n\n    with contextlib.suppress(MissingTraitError):\n        file_path = representation.get_trait(FileLocation).file_path\n        if isinstance(file_path, str):\n            file_path = Path(file_path)\n        template_data[\"ext\"] = file_path.suffix.lstrip(\".\")\n    if not template_data.get(\"ext\"):\n        with contextlib.suppress(MissingTraitError):\n            # Try FileLocations trait if FileLocation ext is empty\n            file_locations_trait = representation.get_trait(\n                FileLocations)\n            if file_locations_trait.file_paths:\n                first_file_loc = file_locations_trait.file_paths[0]\n                file_path = first_file_loc.file_path\n                if isinstance(file_path, str):\n                    file_path = Path(file_path)\n                template_data[\"ext\"] = (file_path.suffix.lstrip(\".\"))\n        # Note: handle \"output\" and \"originalBasename\"\n    return template_data\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.get_transfers_from_representations","title":"<code>get_transfers_from_representations(instance, template, representations)</code>","text":"<p>Get transfers from representations.</p> <p>This method will go through all representations and prepare transfers based on the traits they contain. First it will validate the representation, and then it will prepare template data for the representation. It specifically handles FileLocations, FileLocation, Bundle, Sequence and UDIM traits.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>Instance</code> <p>Instance to process.</p> required <code>template</code> <code>AnatomyStringTemplate</code> <p>Template to use for formatting destination paths.</p> required <code>representations</code> <code>list[Representation]</code> <p>List of representations.</p> required <p>Returns:</p> Type Description <code>list[TransferItem]</code> <p>list[TransferItem]: List of transfers.</p> <p>Raises:</p> Type Description <code>PublishError</code> <p>If representation is invalid.</p> Source code in <code>client/ayon_core/pipeline/traits/publishing.py</code> <pre><code>def get_transfers_from_representations(\n        instance: pyblish.api.Instance,\n        template: AnatomyTemplateItem,\n        representations: list[Representation]\n) -&gt; list[TransferItem]:\n    \"\"\"Get transfers from representations.\n\n    This method will go through all representations and prepare transfers\n    based on the traits they contain. First it will validate the\n    representation, and then it will prepare template data for the\n    representation. It specifically handles FileLocations, FileLocation,\n    Bundle, Sequence and UDIM traits.\n\n    Args:\n        instance (pyblish.api.Instance): Instance to process.\n        template (AnatomyStringTemplate): Template to use for formatting\n            destination paths.\n        representations (list[Representation]): List of representations.\n\n    Returns:\n        list[TransferItem]: List of transfers.\n\n    Raises:\n        PublishError: If representation is invalid.\n\n    \"\"\"\n    instance_template_data: dict[str, str] = {}\n    transfers: list[TransferItem] = []\n    # prepare template and data to format it\n    for representation in representations:\n\n        # validate representation first, this will go through all traits\n        # and check if they are valid\n        try:\n            representation.validate()\n        except TraitValidationError as e:\n            msg = f\"Representation '{representation.name}' is invalid: {e}\"\n            raise PublishError(msg) from e\n\n        template_data = get_template_data_from_representation(\n            representation, instance)\n        # add instance based template data\n\n        template_data.update(instance_template_data)\n\n        # treat Variant as `output` in template data\n        with contextlib.suppress(MissingTraitError):\n            template_data[\"output\"] = (\n                representation.get_trait(Variant).variant\n            )\n\n        template_item = IntegrationTemplateItem(\n            anatomy=instance.context.data[\"anatomy\"],\n            template_data=copy.deepcopy(template_data),\n            template_object=template\n        )\n\n        if representation.contains_trait(FileLocations):\n            # If representation has FileLocations trait (list of files)\n            # it can be a Sequence, UDIM tile set, or a group of related\n            # files that share a common root and preserve their hierarchy.\n            # Note: we do not support yet frame sequence of multiple UDIM\n            # tiles in the same representation.\n            get_transfers_from_file_locations(\n                representation, template_item, transfers\n            )\n        elif representation.contains_trait(FileLocation):\n            # This is just a single file representation\n            get_transfers_from_file_location(\n                representation, template_item, transfers\n            )\n\n        elif representation.contains_trait(Bundle):\n            # Bundle groups multiple \"sub-representations\" together.\n            # It has a list of lists with traits, some might be\n            # FileLocations,but some might be \"file-less\" representations\n            # or even other bundles.\n            get_transfers_from_bundle(\n                representation, template_item, transfers\n            )\n    return transfers\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/index.html#client.ayon_core.pipeline.traits.replace_paths_in_representation","title":"<code>replace_paths_in_representation(representation, transfers)</code>","text":"<p>Replace paths in representation traits based on transfers.</p> <p>This is used to update the traits with the new file paths after the transfer is done.</p> <p>Parameters:</p> Name Type Description Default <code>representation</code> <code>Representation</code> <p>Representation to update.</p> required <code>transfers</code> <code>list[TransferItem]</code> <p>List of transfer items.</p> required Mutates <p>representation (Representation): Representation with updated paths.</p> Source code in <code>client/ayon_core/pipeline/traits/publishing.py</code> <pre><code>def replace_paths_in_representation(\n        representation: Representation,\n        transfers: list[TransferItem]\n) -&gt; None:\n    \"\"\"Replace paths in representation traits based on transfers.\n\n    This is used to update the traits with the new file paths after the\n    transfer is done.\n\n    Args:\n        representation (Representation): Representation to update.\n        transfers (list[TransferItem]): List of transfer items.\n\n    Mutates:\n        representation (Representation): Representation with updated paths.\n\n    \"\"\"\n    for transfer in transfers:\n        if transfer.representation == representation:\n            if representation.contains_trait(FileLocation):\n                f_trait: FileLocation = representation.get_trait(\n                    FileLocation)\n                path_in_trait = f_trait.file_path\n                if path_in_trait == transfer.source:\n                    f_trait.file_path = transfer.destination\n            if representation.contains_trait(FileLocations):\n                fl_trait: FileLocations = representation.get_trait(\n                    FileLocations)\n                for idx, file_loc in enumerate(fl_trait.file_paths):\n                    path_in_trait = file_loc.file_path\n                    if path_in_trait == transfer.source:\n                        fl_trait.file_paths[idx].file_path = (\n                            transfer.destination\n                        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/color.html","title":"color","text":"<p>Color-management-related traits.</p>"},{"location":"autoapi/client/ayon_core/pipeline/traits/color.html#client.ayon_core.pipeline.traits.color.ColorManaged","title":"<code>ColorManaged</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>Color managed trait.</p> <p>Holds color management information. Can be used with Image-related traits to define color space and config.</p> <p>Sync with OpenAssetIO MediaCreation Traits.</p> <p>Attributes:</p> Name Type Description <code>color_space</code> <code>str</code> <p>An OCIO colorspace name available in the \"current\" OCIO context.</p> <code>config</code> <code>str</code> <p>An OCIO config name defining color space.</p> Source code in <code>client/ayon_core/pipeline/traits/color.py</code> <pre><code>@dataclass\nclass ColorManaged(TraitBase):\n    \"\"\"Color managed trait.\n\n    Holds color management information. Can be used with Image-related\n    traits to define color space and config.\n\n    Sync with OpenAssetIO MediaCreation Traits.\n\n    Attributes:\n        color_space (str): An OCIO colorspace name available\n            in the \"current\" OCIO context.\n        config (str): An OCIO config name defining color space.\n    \"\"\"\n\n    id: ClassVar[str] = \"ayon.color.ColorManaged.v1\"\n    name: ClassVar[str] = \"ColorManaged\"\n    color_space: str\n    description: ClassVar[str] = \"Color Managed trait.\"\n    persistent: ClassVar[bool] = True\n    config: Optional[str] = None\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/content.html","title":"content","text":"<p>Content traits for the pipeline.</p>"},{"location":"autoapi/client/ayon_core/pipeline/traits/content.html#client.ayon_core.pipeline.traits.content.Bundle","title":"<code>Bundle</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>Bundle trait model.</p> <p>This model list of independent Representation traits that are bundled together. This is useful for representing a collection of sub-entities that are part of a single entity. You can easily reconstruct representations from the bundle.</p> <p>Example::</p> <pre><code>    Bundle(\n        items=[\n            [\n                MimeType(mime_type=\"image/jpeg\"),\n                FileLocation(file_path=\"/path/to/file.jpg\")\n            ],\n            [\n\n                MimeType(mime_type=\"image/png\"),\n                FileLocation(file_path=\"/path/to/file.png\")\n            ]\n        ]\n    )\n</code></pre> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Trait name.</p> <code>description</code> <code>str</code> <p>Trait description.</p> <code>id</code> <code>str</code> <p>id should be a namespaced trait name with version</p> <code>items</code> <code>list[list[TraitBase]]</code> <p>List of representations.</p> <code>persistent</code> <code>bool</code> <p>Should the mime type be stored with the representation on the server or not.</p> Source code in <code>client/ayon_core/pipeline/traits/content.py</code> <pre><code>@dataclass\nclass Bundle(TraitBase):\n    \"\"\"Bundle trait model.\n\n    This model list of independent Representation traits\n    that are bundled together. This is useful for representing\n    a collection of sub-entities that are part of a single\n    entity. You can easily reconstruct representations from\n    the bundle.\n\n    Example::\n\n            Bundle(\n                items=[\n                    [\n                        MimeType(mime_type=\"image/jpeg\"),\n                        FileLocation(file_path=\"/path/to/file.jpg\")\n                    ],\n                    [\n\n                        MimeType(mime_type=\"image/png\"),\n                        FileLocation(file_path=\"/path/to/file.png\")\n                    ]\n                ]\n            )\n\n    Attributes:\n        name (str): Trait name.\n        description (str): Trait description.\n        id (str): id should be a namespaced trait name with version\n        items (list[list[TraitBase]]): List of representations.\n        persistent (bool): Should the mime type be stored with the\n            representation on the server or not.\n    \"\"\"\n\n    name: ClassVar[str] = \"Bundle\"\n    description: ClassVar[str] = \"Bundle Trait\"\n    id: ClassVar[str] = \"ayon.content.Bundle.v1\"\n    persistent: ClassVar[bool] = True\n    items: list[list[TraitBase]]\n\n    def to_representations(self) -&gt; Generator[Representation]:\n        \"\"\"Convert a bundle to representations.\n\n        Yields:\n            Representation: Representation of the bundle.\n\n        \"\"\"\n        for idx, item in enumerate(self.items):\n            yield Representation(name=f\"{self.name} {idx}\", traits=item)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/content.html#client.ayon_core.pipeline.traits.content.Bundle.to_representations","title":"<code>to_representations()</code>","text":"<p>Convert a bundle to representations.</p> <p>Yields:</p> Name Type Description <code>Representation</code> <code>Generator[Representation]</code> <p>Representation of the bundle.</p> Source code in <code>client/ayon_core/pipeline/traits/content.py</code> <pre><code>def to_representations(self) -&gt; Generator[Representation]:\n    \"\"\"Convert a bundle to representations.\n\n    Yields:\n        Representation: Representation of the bundle.\n\n    \"\"\"\n    for idx, item in enumerate(self.items):\n        yield Representation(name=f\"{self.name} {idx}\", traits=item)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/content.html#client.ayon_core.pipeline.traits.content.Compressed","title":"<code>Compressed</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>Compressed trait model.</p> <p>This trait can hold information about compressed content. What type of compression is used.</p> <p>Example::</p> <pre><code>Compressed(\"gzip\")\n</code></pre> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Trait name.</p> <code>description</code> <code>str</code> <p>Trait description.</p> <code>id</code> <code>str</code> <p>id should be a namespaced trait name with version</p> <code>compression_type</code> <code>str</code> <p>Compression type.</p> <code>persistent</code> <code>bool</code> <p>Should the mime type be stored with the representation on the server or not.</p> Source code in <code>client/ayon_core/pipeline/traits/content.py</code> <pre><code>@dataclass\nclass Compressed(TraitBase):\n    \"\"\"Compressed trait model.\n\n    This trait can hold information about compressed content. What type\n    of compression is used.\n\n    Example::\n\n        Compressed(\"gzip\")\n\n    Attributes:\n        name (str): Trait name.\n        description (str): Trait description.\n        id (str): id should be a namespaced trait name with version\n        compression_type (str): Compression type.\n        persistent (bool): Should the mime type be stored with the\n            representation on the server or not.\n    \"\"\"\n\n    name: ClassVar[str] = \"Compressed\"\n    description: ClassVar[str] = \"Compressed Trait\"\n    id: ClassVar[str] = \"ayon.content.Compressed.v1\"\n    persistent: ClassVar[bool] = True\n    compression_type: str\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/content.html#client.ayon_core.pipeline.traits.content.FileLocation","title":"<code>FileLocation</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>FileLocation trait model.</p> <p>This model represents a file path. It is a specialization of the LocatableContent trait. It is adding optional file size and file hash for easy access to file information.</p> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Trait name.</p> <code>description</code> <code>str</code> <p>Trait description.</p> <code>id</code> <code>str</code> <p>id should be a namespaced trait name with version</p> <code>file_path</code> <code>str</code> <p>File path.</p> <code>file_size</code> <code>Optional[int]</code> <p>File size in bytes.</p> <code>file_hash</code> <code>Optional[str]</code> <p>File hash.</p> <code>persistent</code> <code>bool</code> <p>Should the mime type be stored with the representation on the server or not.</p> Source code in <code>client/ayon_core/pipeline/traits/content.py</code> <pre><code>@dataclass\nclass FileLocation(TraitBase):\n    \"\"\"FileLocation trait model.\n\n    This model represents a file path. It is a specialization of the\n    LocatableContent trait. It is adding optional file size and file hash\n    for easy access to file information.\n\n    Attributes:\n        name (str): Trait name.\n        description (str): Trait description.\n        id (str): id should be a namespaced trait name with version\n        file_path (str): File path.\n        file_size (Optional[int]): File size in bytes.\n        file_hash (Optional[str]): File hash.\n        persistent (bool): Should the mime type be stored with the\n            representation on the server or not.\n    \"\"\"\n\n    name: ClassVar[str] = \"FileLocation\"\n    description: ClassVar[str] = \"FileLocation Trait Model\"\n    id: ClassVar[str] = \"ayon.content.FileLocation.v1\"\n    persistent: ClassVar[bool] = True\n    file_path: Path\n    file_size: Optional[int] = None\n    file_hash: Optional[str] = None\n\n    def validate_trait(self, representation: Representation) -&gt; None:\n        \"\"\"Validate the trait.\n\n        This method validates the trait against others in the representation.\n        In particular, it checks that the FileLocations trait is not present.\n\n        Args:\n            representation (Representation): Representation to validate.\n\n        Raises:\n            TraitValidationError: If the trait is invalid within the\n                representation.\n\n        \"\"\"\n        super().validate_trait(representation)\n        if representation.contains_trait(FileLocations):\n            msg = (\n                \"Representation contains a file location. It can not contain \"\n                \"both `FileLocation` and `FileLocations`.\"\n            )\n            raise TraitValidationError(self.name, msg)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/content.html#client.ayon_core.pipeline.traits.content.FileLocation.validate_trait","title":"<code>validate_trait(representation)</code>","text":"<p>Validate the trait.</p> <p>This method validates the trait against others in the representation. In particular, it checks that the FileLocations trait is not present.</p> <p>Parameters:</p> Name Type Description Default <code>representation</code> <code>Representation</code> <p>Representation to validate.</p> required <p>Raises:</p> Type Description <code>TraitValidationError</code> <p>If the trait is invalid within the representation.</p> Source code in <code>client/ayon_core/pipeline/traits/content.py</code> <pre><code>def validate_trait(self, representation: Representation) -&gt; None:\n    \"\"\"Validate the trait.\n\n    This method validates the trait against others in the representation.\n    In particular, it checks that the FileLocations trait is not present.\n\n    Args:\n        representation (Representation): Representation to validate.\n\n    Raises:\n        TraitValidationError: If the trait is invalid within the\n            representation.\n\n    \"\"\"\n    super().validate_trait(representation)\n    if representation.contains_trait(FileLocations):\n        msg = (\n            \"Representation contains a file location. It can not contain \"\n            \"both `FileLocation` and `FileLocations`.\"\n        )\n        raise TraitValidationError(self.name, msg)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/content.html#client.ayon_core.pipeline.traits.content.FileLocations","title":"<code>FileLocations</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>FileLocations trait model.</p> <p>This model defines list of FileLocation traits. It can be used for file sequences or more complex hierarchies.</p> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Trait name.</p> <code>description</code> <code>str</code> <p>Trait description.</p> <code>id</code> <code>str</code> <p>id should be a namespaced trait name with version</p> <code>file_paths</code> <code>list of FileLocation</code> <p>File locations.</p> <code>persistent</code> <code>bool</code> <p>Should the mime type be stored with the representation on the server or not.</p> Source code in <code>client/ayon_core/pipeline/traits/content.py</code> <pre><code>@dataclass\nclass FileLocations(TraitBase):\n    \"\"\"FileLocations trait model.\n\n    This model defines list of FileLocation traits. It can be\n    used for file sequences or more complex hierarchies.\n\n    Attributes:\n        name (str): Trait name.\n        description (str): Trait description.\n        id (str): id should be a namespaced trait name with version\n        file_paths (list of FileLocation): File locations.\n        persistent (bool): Should the mime type be stored with the\n            representation on the server or not.\n    \"\"\"\n\n    name: ClassVar[str] = \"FileLocations\"\n    description: ClassVar[str] = \"FileLocations Trait Model\"\n    id: ClassVar[str] = \"ayon.content.FileLocations.v1\"\n    persistent: ClassVar[bool] = True\n    file_paths: list[FileLocation]\n\n    def get_files(self) -&gt; Generator[Path, None, None]:\n        \"\"\"Get all file paths from the trait.\n\n        This method will return all file paths from the trait.\n\n        Yields:\n            Path: List of file paths.\n\n        \"\"\"\n        for file_location in self.file_paths:\n            yield file_location.file_path\n\n    def get_common_root(self) -&gt; Path:\n        \"\"\"Get a meaningful common root directory for all file paths.\n\n        Returns:\n            Path: Common root directory.\n\n        Raises:\n            ValueError: If file locations do not share a meaningful root.\n\n        \"\"\"\n        file_paths = [\n            file_path if isinstance(file_path, Path) else Path(file_path)\n            for file_path in self.get_files()\n        ]\n        if not file_paths:\n            raise ValueError(\"No file locations defined.\")\n\n        try:\n            common_root = Path(\n                os.path.commonpath(\n                    [str(file_path.parent) for file_path in file_paths]\n                )\n            )\n        except ValueError as exc:\n            raise ValueError(\n                \"File locations must share a common root directory.\"\n            ) from exc\n\n        if str(common_root) in {\"\", \".\"}:\n            raise ValueError(\n                \"File locations must share a meaningful common root directory.\"\n            )\n\n        if common_root.anchor and common_root == Path(common_root.anchor):\n            raise ValueError(\n                \"File locations must share a common root deeper than the \"\n                \"filesystem root.\"\n            )\n\n        return common_root\n\n    def get_file_location_for_frame(\n            self,\n            frame: int,\n            sequence_trait: Optional[Sequence] = None,\n        ) -&gt; Optional[FileLocation]:\n        \"\"\"Get a file location for a frame.\n\n        This method will return the file location for a given frame. If the\n        frame is not found in the file paths, it will return None.\n\n        Args:\n            frame (int): Frame to get the file location for.\n            sequence_trait (Sequence): Sequence trait to get the\n                frame range specs from.\n\n        Returns:\n            Optional[FileLocation]: File location for the frame.\n\n        \"\"\"\n        frame_regex = re.compile(r\"\\.(?P&lt;index&gt;(?P&lt;padding&gt;0*)\\d+)\\.\\D+\\d?$\")\n        if sequence_trait and sequence_trait.frame_regex:\n            frame_regex = sequence_trait.get_frame_pattern()\n\n        for location in self.file_paths:\n            result = re.search(frame_regex, location.file_path.name)\n            if result:\n                frame_index = int(result.group(\"index\"))\n                if frame_index == frame:\n                    return location\n        return None\n\n    def validate_trait(self, representation: Representation) -&gt; None:\n        \"\"\"Validate the trait.\n\n        This method validates the trait against others in the representation.\n        In particular, it checks that the sequence trait is present, and if\n        so, it will compare the frame range to the file paths.\n\n        Args:\n            representation (Representation): Representation to validate.\n\n        Raises:\n            TraitValidationError: If the trait is invalid within the\n                representation.\n\n        \"\"\"\n        super().validate_trait(representation)\n        if len(self.file_paths) == 0:\n            # If there are no file paths, we can't validate\n            msg = \"No file locations defined (empty list)\"\n            raise TraitValidationError(self.name, msg)\n        if representation.contains_trait(FrameRanged):\n            self._validate_frame_range(representation)\n        if not representation.contains_trait(Sequence) \\\n                and not representation.contains_trait(UDIM):\n            try:\n                self.get_common_root()\n            except ValueError as exc:\n                msg = (\n                    \"Multiple file locations defined, but no Sequence or \"\n                    \"UDIM trait defined. Files must share a meaningful \"\n                    \"common root so their relative hierarchy can be \"\n                    \"preserved.\"\n                )\n                raise TraitValidationError(self.name, msg) from exc\n        if representation.contains_trait(FileLocation):\n            msg = (\n                \"Representation contains a file location. It can not contain \"\n                \"both `FileLocation` and `FileLocations`.\"\n            )\n            raise TraitValidationError(self.name, msg)\n\n    def _validate_frame_range(self, representation: Representation) -&gt; None:\n        \"\"\"Validate the frame range against the file paths.\n\n        If the representation contains a FrameRanged trait, this method will\n        validate the frame range against the file paths. If the frame range\n        does not match the file paths, the trait is invalid. It takes into\n        account the Handles and Sequence traits.\n\n        Args:\n            representation (Representation): Representation to validate.\n\n        Raises:\n            TraitValidationError: If the trait is invalid within the\n                representation.\n\n        \"\"\"\n        tmp_frame_ranged: FrameRanged = self.get_sequence_from_files(\n                    [f.file_path for f in self.file_paths])\n\n        frames_from_spec: list[int] = []\n        with contextlib.suppress(MissingTraitError):\n            sequence: Sequence = representation.get_trait(Sequence)\n            frame_regex = sequence.get_frame_pattern()\n            if sequence.frame_spec:\n                frames_from_spec = sequence.get_frame_list(\n                    self, frame_regex)\n\n        frame_start_with_handles, frame_end_with_handles = \\\n            self._get_frame_info_with_handles(representation, frames_from_spec)\n\n        if frame_start_with_handles \\\n                and tmp_frame_ranged.frame_start != frame_start_with_handles:\n            # If the detected frame range does not match the combined\n            # FrameRanged and Handles trait, the\n            # trait is invalid.\n            msg = (\n                f\"Frame range defined by {self.name} \"\n                f\"({tmp_frame_ranged.frame_start}-\"\n                f\"{tmp_frame_ranged.frame_end}) \"\n                \"in files does not match \"\n                \"frame range \"\n                f\"({frame_start_with_handles}-\"\n                f\"{frame_end_with_handles}) defined in FrameRanged trait.\"\n            )\n\n            raise TraitValidationError(self.name, msg)\n\n        if frames_from_spec:\n            if len(frames_from_spec) != len(self.file_paths):\n                # If the number of file paths does not match the frame range,\n                # the trait is invalid\n                msg = (\n                    f\"Number of file locations ({len(self.file_paths)}) \"\n                    \"does not match frame range defined by frame spec \"\n                    \"on Sequence trait: \"\n                    f\"({len(frames_from_spec)})\"\n                )\n                raise TraitValidationError(self.name, msg)\n            # if there is a frame spec on the Sequence trait,\n            # we should not validate the frame range from the files.\n            # the rest is validated by Sequence validators.\n            return\n\n        length_with_handles: int = (\n            frame_end_with_handles - frame_start_with_handles + 1\n        )\n\n        if len(self.file_paths) != length_with_handles:\n            # If the number of file paths does not match the frame range,\n            # the trait is invalid\n            msg = (\n                f\"Number of file locations ({len(self.file_paths)}) \"\n                \"does not match frame range \"\n                f\"({length_with_handles})\"\n            )\n            raise TraitValidationError(self.name, msg)\n\n        frame_ranged: FrameRanged = representation.get_trait(FrameRanged)\n\n        if frame_start_with_handles != tmp_frame_ranged.frame_start or \\\n                frame_end_with_handles != tmp_frame_ranged.frame_end:\n            # If the frame range does not match the FrameRanged trait, the\n            # trait is invalid. Note that we don't check the frame rate\n            # because it is not stored in the file paths and is not\n            # determined by `get_sequence_from_files`.\n            msg = (\n                \"Frame range \"\n                f\"({frame_ranged.frame_start}-{frame_ranged.frame_end}) \"\n                \"in sequence trait does not match \"\n                \"frame range \"\n                f\"({tmp_frame_ranged.frame_start}-\"\n                f\"{tmp_frame_ranged.frame_end}) \"\n            )\n            raise TraitValidationError(self.name, msg)\n\n    @staticmethod\n    def _get_frame_info_with_handles(\n            representation: Representation,\n            frames_from_spec: list[int]) -&gt; tuple[int, int]:\n        \"\"\"Get the frame range with handles from the representation.\n\n        This will return frame start and frame end with handles calculated\n        in if there actually is the Handles trait in the representation.\n\n        Args:\n            representation (Representation): Representation to get the frame\n                range from.\n            frames_from_spec (list[int]): List of frames from the frame spec.\n                This list is modified in place to take into\n                account the handles.\n\n        Mutates:\n            frames_from_spec: List of frames from the frame spec.\n\n        Returns:\n            tuple[int, int]: Start and end frame with handles.\n\n        \"\"\"\n        frame_start = frame_end = 0\n        frame_start_handle = frame_end_handle = 0\n        # If there is no sequence trait, we can't validate it\n        if frames_from_spec and representation.contains_trait(FrameRanged):\n            # if there is no FrameRanged trait (but really there should be)\n            # we can use the frame range from the frame spec\n            frame_start = min(frames_from_spec)\n            frame_end = max(frames_from_spec)\n\n        # Handle the frame range\n        with contextlib.suppress(MissingTraitError):\n            frame_start = representation.get_trait(FrameRanged).frame_start\n            frame_end = representation.get_trait(FrameRanged).frame_end\n\n        # Handle the handles :P\n        with contextlib.suppress(MissingTraitError):\n            handles: Handles = representation.get_trait(Handles)\n            if not handles.inclusive:\n                # if handless are exclusive, we need to adjust the frame range\n                frame_start_handle = handles.frame_start_handle or 0\n                frame_end_handle = handles.frame_end_handle or 0\n                if frames_from_spec:\n                    frames_from_spec.extend(\n                        range(frame_start - frame_start_handle, frame_start)\n                    )\n                    frames_from_spec.extend(\n                        range(frame_end + 1, frame_end_handle + frame_end + 1)\n                    )\n\n        frame_start_with_handles = frame_start - frame_start_handle\n        frame_end_with_handles = frame_end + frame_end_handle\n\n        return frame_start_with_handles, frame_end_with_handles\n\n    @staticmethod\n    def get_sequence_from_files(paths: list[Path]) -&gt; FrameRanged:\n        \"\"\"Get the original frame range from files.\n\n        Note that this cannot guess frame rate, so it's set to 25.\n        This will also fail on paths that cannot be assembled into\n        one collection without any reminders.\n\n        Args:\n            paths (list[Path]): List of file paths.\n\n        Returns:\n            FrameRanged: FrameRanged trait.\n\n        Raises:\n            ValueError: If paths cannot be assembled into one collection\n\n        \"\"\"\n        cols, rems = assemble(\n            [path.name for path in paths],\n            minimum_items=1)\n        if rems:\n            msg = (\n                \"Assembled paths have singular file(s) and \"\n                \"sequences at the same time. This is not supported. \"\n            )\n            raise ValueError(msg)\n\n        if len(cols) != 1:\n            msg = f\"More than one collection found ({len(cols)}).\"\n            raise ValueError(msg)\n        col = cols[0]\n\n        sorted_frames = sorted(col.indexes)\n        # First frame used for end value\n        first_frame = sorted_frames[0]\n        last_frame = sorted_frames[-1]\n        # Use padding from a collection of the last frame lengths as string\n        # padding = max(col.padding, len(str(last_frame)))\n\n        return FrameRanged(\n            frame_start=first_frame, frame_end=last_frame,\n            frames_per_second=\"25.0\"\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/content.html#client.ayon_core.pipeline.traits.content.FileLocations.get_common_root","title":"<code>get_common_root()</code>","text":"<p>Get a meaningful common root directory for all file paths.</p> <p>Returns:</p> Name Type Description <code>Path</code> <code>Path</code> <p>Common root directory.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If file locations do not share a meaningful root.</p> Source code in <code>client/ayon_core/pipeline/traits/content.py</code> <pre><code>def get_common_root(self) -&gt; Path:\n    \"\"\"Get a meaningful common root directory for all file paths.\n\n    Returns:\n        Path: Common root directory.\n\n    Raises:\n        ValueError: If file locations do not share a meaningful root.\n\n    \"\"\"\n    file_paths = [\n        file_path if isinstance(file_path, Path) else Path(file_path)\n        for file_path in self.get_files()\n    ]\n    if not file_paths:\n        raise ValueError(\"No file locations defined.\")\n\n    try:\n        common_root = Path(\n            os.path.commonpath(\n                [str(file_path.parent) for file_path in file_paths]\n            )\n        )\n    except ValueError as exc:\n        raise ValueError(\n            \"File locations must share a common root directory.\"\n        ) from exc\n\n    if str(common_root) in {\"\", \".\"}:\n        raise ValueError(\n            \"File locations must share a meaningful common root directory.\"\n        )\n\n    if common_root.anchor and common_root == Path(common_root.anchor):\n        raise ValueError(\n            \"File locations must share a common root deeper than the \"\n            \"filesystem root.\"\n        )\n\n    return common_root\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/content.html#client.ayon_core.pipeline.traits.content.FileLocations.get_file_location_for_frame","title":"<code>get_file_location_for_frame(frame, sequence_trait=None)</code>","text":"<p>Get a file location for a frame.</p> <p>This method will return the file location for a given frame. If the frame is not found in the file paths, it will return None.</p> <p>Parameters:</p> Name Type Description Default <code>frame</code> <code>int</code> <p>Frame to get the file location for.</p> required <code>sequence_trait</code> <code>Sequence</code> <p>Sequence trait to get the frame range specs from.</p> <code>None</code> <p>Returns:</p> Type Description <code>Optional[FileLocation]</code> <p>Optional[FileLocation]: File location for the frame.</p> Source code in <code>client/ayon_core/pipeline/traits/content.py</code> <pre><code>def get_file_location_for_frame(\n        self,\n        frame: int,\n        sequence_trait: Optional[Sequence] = None,\n    ) -&gt; Optional[FileLocation]:\n    \"\"\"Get a file location for a frame.\n\n    This method will return the file location for a given frame. If the\n    frame is not found in the file paths, it will return None.\n\n    Args:\n        frame (int): Frame to get the file location for.\n        sequence_trait (Sequence): Sequence trait to get the\n            frame range specs from.\n\n    Returns:\n        Optional[FileLocation]: File location for the frame.\n\n    \"\"\"\n    frame_regex = re.compile(r\"\\.(?P&lt;index&gt;(?P&lt;padding&gt;0*)\\d+)\\.\\D+\\d?$\")\n    if sequence_trait and sequence_trait.frame_regex:\n        frame_regex = sequence_trait.get_frame_pattern()\n\n    for location in self.file_paths:\n        result = re.search(frame_regex, location.file_path.name)\n        if result:\n            frame_index = int(result.group(\"index\"))\n            if frame_index == frame:\n                return location\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/content.html#client.ayon_core.pipeline.traits.content.FileLocations.get_files","title":"<code>get_files()</code>","text":"<p>Get all file paths from the trait.</p> <p>This method will return all file paths from the trait.</p> <p>Yields:</p> Name Type Description <code>Path</code> <code>Path</code> <p>List of file paths.</p> Source code in <code>client/ayon_core/pipeline/traits/content.py</code> <pre><code>def get_files(self) -&gt; Generator[Path, None, None]:\n    \"\"\"Get all file paths from the trait.\n\n    This method will return all file paths from the trait.\n\n    Yields:\n        Path: List of file paths.\n\n    \"\"\"\n    for file_location in self.file_paths:\n        yield file_location.file_path\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/content.html#client.ayon_core.pipeline.traits.content.FileLocations.get_sequence_from_files","title":"<code>get_sequence_from_files(paths)</code>  <code>staticmethod</code>","text":"<p>Get the original frame range from files.</p> <p>Note that this cannot guess frame rate, so it's set to 25. This will also fail on paths that cannot be assembled into one collection without any reminders.</p> <p>Parameters:</p> Name Type Description Default <code>paths</code> <code>list[Path]</code> <p>List of file paths.</p> required <p>Returns:</p> Name Type Description <code>FrameRanged</code> <code>FrameRanged</code> <p>FrameRanged trait.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If paths cannot be assembled into one collection</p> Source code in <code>client/ayon_core/pipeline/traits/content.py</code> <pre><code>@staticmethod\ndef get_sequence_from_files(paths: list[Path]) -&gt; FrameRanged:\n    \"\"\"Get the original frame range from files.\n\n    Note that this cannot guess frame rate, so it's set to 25.\n    This will also fail on paths that cannot be assembled into\n    one collection without any reminders.\n\n    Args:\n        paths (list[Path]): List of file paths.\n\n    Returns:\n        FrameRanged: FrameRanged trait.\n\n    Raises:\n        ValueError: If paths cannot be assembled into one collection\n\n    \"\"\"\n    cols, rems = assemble(\n        [path.name for path in paths],\n        minimum_items=1)\n    if rems:\n        msg = (\n            \"Assembled paths have singular file(s) and \"\n            \"sequences at the same time. This is not supported. \"\n        )\n        raise ValueError(msg)\n\n    if len(cols) != 1:\n        msg = f\"More than one collection found ({len(cols)}).\"\n        raise ValueError(msg)\n    col = cols[0]\n\n    sorted_frames = sorted(col.indexes)\n    # First frame used for end value\n    first_frame = sorted_frames[0]\n    last_frame = sorted_frames[-1]\n    # Use padding from a collection of the last frame lengths as string\n    # padding = max(col.padding, len(str(last_frame)))\n\n    return FrameRanged(\n        frame_start=first_frame, frame_end=last_frame,\n        frames_per_second=\"25.0\"\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/content.html#client.ayon_core.pipeline.traits.content.FileLocations.validate_trait","title":"<code>validate_trait(representation)</code>","text":"<p>Validate the trait.</p> <p>This method validates the trait against others in the representation. In particular, it checks that the sequence trait is present, and if so, it will compare the frame range to the file paths.</p> <p>Parameters:</p> Name Type Description Default <code>representation</code> <code>Representation</code> <p>Representation to validate.</p> required <p>Raises:</p> Type Description <code>TraitValidationError</code> <p>If the trait is invalid within the representation.</p> Source code in <code>client/ayon_core/pipeline/traits/content.py</code> <pre><code>def validate_trait(self, representation: Representation) -&gt; None:\n    \"\"\"Validate the trait.\n\n    This method validates the trait against others in the representation.\n    In particular, it checks that the sequence trait is present, and if\n    so, it will compare the frame range to the file paths.\n\n    Args:\n        representation (Representation): Representation to validate.\n\n    Raises:\n        TraitValidationError: If the trait is invalid within the\n            representation.\n\n    \"\"\"\n    super().validate_trait(representation)\n    if len(self.file_paths) == 0:\n        # If there are no file paths, we can't validate\n        msg = \"No file locations defined (empty list)\"\n        raise TraitValidationError(self.name, msg)\n    if representation.contains_trait(FrameRanged):\n        self._validate_frame_range(representation)\n    if not representation.contains_trait(Sequence) \\\n            and not representation.contains_trait(UDIM):\n        try:\n            self.get_common_root()\n        except ValueError as exc:\n            msg = (\n                \"Multiple file locations defined, but no Sequence or \"\n                \"UDIM trait defined. Files must share a meaningful \"\n                \"common root so their relative hierarchy can be \"\n                \"preserved.\"\n            )\n            raise TraitValidationError(self.name, msg) from exc\n    if representation.contains_trait(FileLocation):\n        msg = (\n            \"Representation contains a file location. It can not contain \"\n            \"both `FileLocation` and `FileLocations`.\"\n        )\n        raise TraitValidationError(self.name, msg)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/content.html#client.ayon_core.pipeline.traits.content.Fragment","title":"<code>Fragment</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>Fragment trait model.</p> <p>This model represents a fragment trait. A fragment is a part of a larger entity that is represented by another representation.</p> <p>Example::</p> <pre><code>main_representation = Representation(name=\"parent\",\n    traits=[],\n)\nfragment_representation = Representation(\n    name=\"fragment\",\n    traits=[\n        Fragment(parent=main_representation.id),\n    ]\n)\n</code></pre> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Trait name.</p> <code>description</code> <code>str</code> <p>Trait description.</p> <code>id</code> <code>str</code> <p>id should be namespaced trait name with version</p> <code>parent</code> <code>str</code> <p>Parent representation id.</p> <code>persistent</code> <code>bool</code> <p>Should the mime type be stored with the representation on the server or not.</p> Source code in <code>client/ayon_core/pipeline/traits/content.py</code> <pre><code>@dataclass\nclass Fragment(TraitBase):\n    \"\"\"Fragment trait model.\n\n    This model represents a fragment trait. A fragment is a part of\n    a larger entity that is represented by another representation.\n\n    Example::\n\n        main_representation = Representation(name=\"parent\",\n            traits=[],\n        )\n        fragment_representation = Representation(\n            name=\"fragment\",\n            traits=[\n                Fragment(parent=main_representation.id),\n            ]\n        )\n\n    Attributes:\n        name (str): Trait name.\n        description (str): Trait description.\n        id (str): id should be namespaced trait name with version\n        parent (str): Parent representation id.\n        persistent (bool): Should the mime type be stored with the\n            representation on the server or not.\n    \"\"\"\n\n    name: ClassVar[str] = \"Fragment\"\n    description: ClassVar[str] = \"Fragment Trait\"\n    id: ClassVar[str] = \"ayon.content.Fragment.v1\"\n    persistent: ClassVar[bool] = True\n    parent: str\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/content.html#client.ayon_core.pipeline.traits.content.LocatableContent","title":"<code>LocatableContent</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>LocatableContent trait model.</p> <p>This model represents a locatable content trait. Locatable content is content that has a location. It doesn't have to be a file - it could be a URL or some other location.</p> <p>Sync with OpenAssetIO MediaCreation Traits.</p> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Trait name.</p> <code>description</code> <code>str</code> <p>Trait description.</p> <code>id</code> <code>str</code> <p>id should be a namespaced trait name with version</p> <code>location</code> <code>str</code> <p>Location.</p> <code>is_templated</code> <code>Optional[bool]</code> <p>Is the location templated? Default is None.</p> <code>persistent</code> <code>bool</code> <p>Should the mime type be stored with the representation on the server or not.</p> Source code in <code>client/ayon_core/pipeline/traits/content.py</code> <pre><code>@dataclass\nclass LocatableContent(TraitBase):\n    \"\"\"LocatableContent trait model.\n\n    This model represents a locatable content trait. Locatable content\n    is content that has a location. It doesn't have to be a file - it could\n    be a URL or some other location.\n\n    Sync with OpenAssetIO MediaCreation Traits.\n\n    Attributes:\n        name (str): Trait name.\n        description (str): Trait description.\n        id (str): id should be a namespaced trait name with version\n        location (str): Location.\n        is_templated (Optional[bool]): Is the location templated?\n            Default is None.\n        persistent (bool): Should the mime type be stored with the\n            representation on the server or not.\n    \"\"\"\n\n    name: ClassVar[str] = \"LocatableContent\"\n    description: ClassVar[str] = \"LocatableContent Trait Model\"\n    id: ClassVar[str] = \"ayon.content.LocatableContent.v1\"\n    persistent: ClassVar[bool] = True\n    location: str\n    is_templated: Optional[bool] = None\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/content.html#client.ayon_core.pipeline.traits.content.MimeType","title":"<code>MimeType</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>MimeType trait model.</p> <p>This model represents a mime type trait. For example, image/jpeg. It is used to describe the type of content in a representation regardless of the file extension.</p> <p>For more information, see RFC 2046 and RFC 4288 (and related RFCs).</p> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Trait name.</p> <code>description</code> <code>str</code> <p>Trait description.</p> <code>id</code> <code>str</code> <p>id should be a namespaced trait name with version</p> <code>mime_type</code> <code>str</code> <p>Mime type like image/jpeg.</p> <code>persistent</code> <code>bool</code> <p>Should the mime type be stored with the representation on the server or not.</p> Source code in <code>client/ayon_core/pipeline/traits/content.py</code> <pre><code>@dataclass\nclass MimeType(TraitBase):\n    \"\"\"MimeType trait model.\n\n    This model represents a mime type trait. For example, image/jpeg.\n    It is used to describe the type of content in a representation regardless\n    of the file extension.\n\n    For more information, see RFC 2046 and RFC 4288 (and related RFCs).\n\n    Attributes:\n        name (str): Trait name.\n        description (str): Trait description.\n        id (str): id should be a namespaced trait name with version\n        mime_type (str): Mime type like image/jpeg.\n        persistent (bool): Should the mime type be stored with the\n            representation on the server or not.\n    \"\"\"\n\n    name: ClassVar[str] = \"MimeType\"\n    description: ClassVar[str] = \"MimeType Trait Model\"\n    id: ClassVar[str] = \"ayon.content.MimeType.v1\"\n    persistent: ClassVar[bool] = True\n    mime_type: str\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/content.html#client.ayon_core.pipeline.traits.content.OriginalFilename","title":"<code>OriginalFilename</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>OriginalFilename trait model.</p> <p>This trait indicates that the representation file name(s) are kept - ie they are not renamed using templates</p> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Trait name.</p> <code>description</code> <code>str</code> <p>Trait description.</p> <code>id</code> <code>str</code> <p>id should be namespaced trait name with version</p> <code>persistent</code> <code>bool</code> <p>Should the mime type be stored with the representation on the server or not.</p> Source code in <code>client/ayon_core/pipeline/traits/content.py</code> <pre><code>@dataclass\nclass OriginalFilename(TraitBase):\n    \"\"\"OriginalFilename trait model.\n\n    This trait indicates that the representation file name(s) are kept -\n    ie they are not renamed using templates\n\n    Attributes:\n        name (str): Trait name.\n        description (str): Trait description.\n        id (str): id should be namespaced trait name with version\n        persistent (bool): Should the mime type be stored with the\n            representation on the server or not.\n    \"\"\"\n\n    name: ClassVar[str] = \"OriginalFilename\"\n    description: ClassVar[str] = \"OriginalFilename Trait\"\n    id: ClassVar[str] = \"ayon.content.OriginalFilename.v1\"\n    persistent: ClassVar[bool] = True\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/content.html#client.ayon_core.pipeline.traits.content.RootlessLocation","title":"<code>RootlessLocation</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>RootlessLocation trait model.</p> <p>RootlessLocation trait is a trait that represents a file path that is without a specific root. To get the absolute path, the root needs to be resolved by AYON. Rootless path can be used on multiple platforms.</p> <p>Example::</p> <pre><code>RootlessLocation(\n    rootless_path=\"{root[work]}/project/asset/asset.jpg\"\n)\n</code></pre> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Trait name.</p> <code>description</code> <code>str</code> <p>Trait description.</p> <code>id</code> <code>str</code> <p>id should be a namespaced trait name with version</p> <code>rootless_path</code> <code>str</code> <p>Rootless path.</p> <code>persistent</code> <code>bool</code> <p>Should the mime type be stored with the representation on the server or not.</p> Source code in <code>client/ayon_core/pipeline/traits/content.py</code> <pre><code>@dataclass\nclass RootlessLocation(TraitBase):\n    \"\"\"RootlessLocation trait model.\n\n    RootlessLocation trait is a trait that represents a file path that is\n    without a specific root. To get the absolute path, the root needs to be\n    resolved by AYON. Rootless path can be used on multiple platforms.\n\n    Example::\n\n        RootlessLocation(\n            rootless_path=\"{root[work]}/project/asset/asset.jpg\"\n        )\n\n    Attributes:\n        name (str): Trait name.\n        description (str): Trait description.\n        id (str): id should be a namespaced trait name with version\n        rootless_path (str): Rootless path.\n        persistent (bool): Should the mime type be stored with the\n            representation on the server or not.\n    \"\"\"\n\n    name: ClassVar[str] = \"RootlessLocation\"\n    description: ClassVar[str] = \"RootlessLocation Trait Model\"\n    id: ClassVar[str] = \"ayon.content.RootlessLocation.v1\"\n    persistent: ClassVar[bool] = True\n    rootless_path: str\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/cryptography.html","title":"cryptography","text":"<p>Cryptography traits.</p>"},{"location":"autoapi/client/ayon_core/pipeline/traits/cryptography.html#client.ayon_core.pipeline.traits.cryptography.DigitallySigned","title":"<code>DigitallySigned</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>Digitally signed trait.</p> <p>This type trait means that the data is digitally signed.</p> <p>Attributes:</p> Name Type Description <code>signature</code> <code>str</code> <p>Digital signature.</p> Source code in <code>client/ayon_core/pipeline/traits/cryptography.py</code> <pre><code>@dataclass\nclass DigitallySigned(TraitBase):\n    \"\"\"Digitally signed trait.\n\n    This type trait means that the data is digitally signed.\n\n    Attributes:\n        signature (str): Digital signature.\n    \"\"\"\n\n    id: ClassVar[str] = \"ayon.cryptography.DigitallySigned.v1\"\n    name: ClassVar[str] = \"DigitallySigned\"\n    description: ClassVar[str] = \"Digitally signed trait.\"\n    persistent: ClassVar[bool] = True\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/cryptography.html#client.ayon_core.pipeline.traits.cryptography.PGPSigned","title":"<code>PGPSigned</code>  <code>dataclass</code>","text":"<p>               Bases: <code>DigitallySigned</code></p> <p>PGP signed trait.</p> <p>This trait holds PGP (RFC-4880) signed data.</p> <p>Attributes:</p> Name Type Description <code>signed_data</code> <code>str</code> <p>Signed data.</p> <code>clear_text</code> <code>str</code> <p>Clear text.</p> Source code in <code>client/ayon_core/pipeline/traits/cryptography.py</code> <pre><code>@dataclass\nclass PGPSigned(DigitallySigned):\n    \"\"\"PGP signed trait.\n\n    This trait holds PGP (RFC-4880) signed data.\n\n    Attributes:\n        signed_data (str): Signed data.\n        clear_text (str): Clear text.\n    \"\"\"\n\n    id: ClassVar[str] = \"ayon.cryptography.PGPSigned.v1\"\n    name: ClassVar[str] = \"PGPSigned\"\n    description: ClassVar[str] = \"PGP signed trait.\"\n    persistent: ClassVar[bool] = True\n    signed_data: str\n    clear_text: Optional[str] = None\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/lifecycle.html","title":"lifecycle","text":"<p>Lifecycle traits.</p>"},{"location":"autoapi/client/ayon_core/pipeline/traits/lifecycle.html#client.ayon_core.pipeline.traits.lifecycle.Persistent","title":"<code>Persistent</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>Persistent trait model.</p> <p>Persistent trait is opposite to transient trait. It marks representation as persistent. Such representations are persisted in the system (e.g. in the database).</p> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Trait name.</p> <code>description</code> <code>str</code> <p>Trait description.</p> <code>id</code> <code>str</code> <p>id should be a namespaced trait name with the version</p> Source code in <code>client/ayon_core/pipeline/traits/lifecycle.py</code> <pre><code>@dataclass\nclass Persistent(TraitBase):\n    \"\"\"Persistent trait model.\n\n    Persistent trait is opposite to transient trait. It marks representation\n    as persistent. Such representations are persisted in the system (e.g. in\n    the database).\n\n    Attributes:\n        name (str): Trait name.\n        description (str): Trait description.\n        id (str): id should be a namespaced trait name with the version\n    \"\"\"\n\n    name: ClassVar[str] = \"Persistent\"\n    description: ClassVar[str] = \"Persistent Trait Model\"\n    id: ClassVar[str] = \"ayon.lifecycle.Persistent.v1\"\n    # Note that this affects the persistence of the trait itself, not\n    # the representation. This is a class variable, so it is shared\n    # among all instances of the class.\n    persistent: bool = True\n\n    def validate_trait(self, representation) -&gt; None:  # noqa: ANN001\n        \"\"\"Validate representation is not Transient.\n\n        Args:\n            representation (Representation): Representation model.\n\n        Raises:\n            TraitValidationError: If representation is marked\n                as both Persistent and Transient.\n\n        \"\"\"\n        if representation.contains_trait(Transient):\n            msg = \"Representation is marked as both Persistent and Transient.\"\n            raise TraitValidationError(self.name, msg)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/lifecycle.html#client.ayon_core.pipeline.traits.lifecycle.Persistent.validate_trait","title":"<code>validate_trait(representation)</code>","text":"<p>Validate representation is not Transient.</p> <p>Parameters:</p> Name Type Description Default <code>representation</code> <code>Representation</code> <p>Representation model.</p> required <p>Raises:</p> Type Description <code>TraitValidationError</code> <p>If representation is marked as both Persistent and Transient.</p> Source code in <code>client/ayon_core/pipeline/traits/lifecycle.py</code> <pre><code>def validate_trait(self, representation) -&gt; None:  # noqa: ANN001\n    \"\"\"Validate representation is not Transient.\n\n    Args:\n        representation (Representation): Representation model.\n\n    Raises:\n        TraitValidationError: If representation is marked\n            as both Persistent and Transient.\n\n    \"\"\"\n    if representation.contains_trait(Transient):\n        msg = \"Representation is marked as both Persistent and Transient.\"\n        raise TraitValidationError(self.name, msg)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/lifecycle.html#client.ayon_core.pipeline.traits.lifecycle.Transient","title":"<code>Transient</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>Transient trait model.</p> <p>Transient trait marks representation as transient. Such representations are not persisted in the system.</p> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Trait name.</p> <code>description</code> <code>str</code> <p>Trait description.</p> <code>id</code> <code>str</code> <p>id should be a namespaced trait name with the version</p> Source code in <code>client/ayon_core/pipeline/traits/lifecycle.py</code> <pre><code>@dataclass\nclass Transient(TraitBase):\n    \"\"\"Transient trait model.\n\n    Transient trait marks representation as transient. Such representations\n    are not persisted in the system.\n\n    Attributes:\n        name (str): Trait name.\n        description (str): Trait description.\n        id (str): id should be a namespaced trait name with the version\n    \"\"\"\n\n    name: ClassVar[str] = \"Transient\"\n    description: ClassVar[str] = \"Transient Trait Model\"\n    id: ClassVar[str] = \"ayon.lifecycle.Transient.v1\"\n    persistent: ClassVar[bool] = True  # see note in Persistent\n\n    def validate_trait(self, representation) -&gt; None:  # noqa: ANN001\n        \"\"\"Validate representation is not Persistent.\n\n        Args:\n            representation (Representation): Representation model.\n\n        Raises:\n            TraitValidationError: If representation is marked as both\n                Persistent and Transient.\n\n        \"\"\"\n        if representation.contains_trait(Persistent):\n            msg = \"Representation is marked as both Persistent and Transient.\"\n            raise TraitValidationError(self.name, msg)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/lifecycle.html#client.ayon_core.pipeline.traits.lifecycle.Transient.validate_trait","title":"<code>validate_trait(representation)</code>","text":"<p>Validate representation is not Persistent.</p> <p>Parameters:</p> Name Type Description Default <code>representation</code> <code>Representation</code> <p>Representation model.</p> required <p>Raises:</p> Type Description <code>TraitValidationError</code> <p>If representation is marked as both Persistent and Transient.</p> Source code in <code>client/ayon_core/pipeline/traits/lifecycle.py</code> <pre><code>def validate_trait(self, representation) -&gt; None:  # noqa: ANN001\n    \"\"\"Validate representation is not Persistent.\n\n    Args:\n        representation (Representation): Representation model.\n\n    Raises:\n        TraitValidationError: If representation is marked as both\n            Persistent and Transient.\n\n    \"\"\"\n    if representation.contains_trait(Persistent):\n        msg = \"Representation is marked as both Persistent and Transient.\"\n        raise TraitValidationError(self.name, msg)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/meta.html","title":"meta","text":"<p>Metadata traits.</p>"},{"location":"autoapi/client/ayon_core/pipeline/traits/meta.html#client.ayon_core.pipeline.traits.meta.IntendedUse","title":"<code>IntendedUse</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>Intended use of the representation.</p> <p>This trait describes the intended use of the representation. It can be used in cases where the other traits are not enough to describe the intended use. For example, a txt file with tracking points can be used as a corner pin in After Effect but not in Nuke.</p> <p>Attributes:</p> Name Type Description <code>use</code> <code>str</code> <p>Intended use description.</p> Source code in <code>client/ayon_core/pipeline/traits/meta.py</code> <pre><code>@dataclass\nclass IntendedUse(TraitBase):\n    \"\"\"Intended use of the representation.\n\n    This trait describes the intended use of the representation. It\n    can be used in cases where the other traits are not enough to\n    describe the intended use. For example, a txt file with tracking\n    points can be used as a corner pin in After Effect but not in Nuke.\n\n    Attributes:\n        use (str): Intended use description.\n\n    \"\"\"\n    name: ClassVar[str] = \"IntendedUse\"\n    description: ClassVar[str] = \"Intended Use Trait Model\"\n    id: ClassVar[str] = \"ayon.meta.IntendedUse.v1\"\n    persistent: ClassVar[bool] = True\n    use: str\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/meta.html#client.ayon_core.pipeline.traits.meta.KeepOriginalLocation","title":"<code>KeepOriginalLocation</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>Keep files in its original location.</p> Note <p>This is not a persistent trait.</p> Source code in <code>client/ayon_core/pipeline/traits/meta.py</code> <pre><code>@dataclass\nclass KeepOriginalLocation(TraitBase):\n    \"\"\"Keep files in its original location.\n\n    Note:\n        This is not a persistent trait.\n\n    \"\"\"\n    name: ClassVar[str] = \"KeepOriginalLocation\"\n    description: ClassVar[str] = \"Keep Original Location Trait Model\"\n    id: ClassVar[str] = \"ayon.meta.KeepOriginalLocation.v1\"\n    persistent: ClassVar[bool] = False\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/meta.html#client.ayon_core.pipeline.traits.meta.KeepOriginalName","title":"<code>KeepOriginalName</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>Keep files in its original name.</p> Note <p>This is not a persistent trait.</p> Source code in <code>client/ayon_core/pipeline/traits/meta.py</code> <pre><code>@dataclass\nclass KeepOriginalName(TraitBase):\n    \"\"\"Keep files in its original name.\n\n    Note:\n        This is not a persistent trait.\n    \"\"\"\n\n    name: ClassVar[str] = \"KeepOriginalName\"\n    description: ClassVar[str] = \"Keep Original Name Trait Model\"\n    id: ClassVar[str] = \"ayon.meta.KeepOriginalName.v1\"\n    persistent: ClassVar[bool] = False\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/meta.html#client.ayon_core.pipeline.traits.meta.SourceApplication","title":"<code>SourceApplication</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>Metadata about the source (producing) application.</p> <p>This can be useful in cases where this information is needed, but it cannot be determined from other means - like .txt files used for various motion tracking applications that must be interpreted by the loader.</p> <p>Note that this is not really connected to any logic in ayon-applications addon.</p> <p>Attributes:</p> Name Type Description <code>application</code> <code>str</code> <p>Application name.</p> <code>variant</code> <code>str</code> <p>Application variant.</p> <code>version</code> <code>str</code> <p>Application version.</p> <code>platform</code> <code>str</code> <p>Platform name (Windows, darwin, etc.).</p> <code>host_name</code> <code>str</code> <p>AYON host name if applicable.</p> Source code in <code>client/ayon_core/pipeline/traits/meta.py</code> <pre><code>@dataclass\nclass SourceApplication(TraitBase):\n    \"\"\"Metadata about the source (producing) application.\n\n    This can be useful in cases where this information is\n    needed, but it cannot be determined from other means - like\n    .txt files used for various motion tracking applications that\n    must be interpreted by the loader.\n\n    Note that this is not really connected to any logic in\n    ayon-applications addon.\n\n    Attributes:\n        application (str): Application name.\n        variant (str): Application variant.\n        version (str): Application version.\n        platform (str): Platform name (Windows, darwin, etc.).\n        host_name (str): AYON host name if applicable.\n    \"\"\"\n\n    name: ClassVar[str] = \"SourceApplication\"\n    description: ClassVar[str] = \"Source Application Trait Model\"\n    id: ClassVar[str] = \"ayon.meta.SourceApplication.v1\"\n    persistent: ClassVar[bool] = True\n    application: str\n    variant: Optional[str] = None\n    version: Optional[str] = None\n    platform: Optional[str] = None\n    host_name: Optional[str] = None\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/meta.html#client.ayon_core.pipeline.traits.meta.Tagged","title":"<code>Tagged</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>Tagged trait model.</p> <p>This trait can hold a list of tags.</p> <p>Example::</p> <pre><code>Tagged(tags=[\"tag1\", \"tag2\"])\n</code></pre> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Trait name.</p> <code>description</code> <code>str</code> <p>Trait description.</p> <code>id</code> <code>str</code> <p>id should be a namespaced trait name with version</p> <code>tags</code> <code>List[str]</code> <p>Tags.</p> Source code in <code>client/ayon_core/pipeline/traits/meta.py</code> <pre><code>@dataclass\nclass Tagged(TraitBase):\n    \"\"\"Tagged trait model.\n\n    This trait can hold a list of tags.\n\n    Example::\n\n        Tagged(tags=[\"tag1\", \"tag2\"])\n\n    Attributes:\n        name (str): Trait name.\n        description (str): Trait description.\n        id (str): id should be a namespaced trait name with version\n        tags (List[str]): Tags.\n    \"\"\"\n\n    name: ClassVar[str] = \"Tagged\"\n    description: ClassVar[str] = \"Tagged Trait Model\"\n    id: ClassVar[str] = \"ayon.meta.Tagged.v1\"\n    persistent: ClassVar[bool] = True\n    tags: List[str]\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/meta.html#client.ayon_core.pipeline.traits.meta.TemplatePath","title":"<code>TemplatePath</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>TemplatePath trait model.</p> <p>This model represents a template path with formatting data. Template path can be an Anatomy template and data is used to format it.</p> <p>Example::</p> <pre><code>TemplatePath(template=\"path/{key}/file\", data={\"key\": \"to\"})\n</code></pre> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Trait name.</p> <code>description</code> <code>str</code> <p>Trait description.</p> <code>id</code> <code>str</code> <p>id should be a namespaced trait name with version</p> <code>template</code> <code>str</code> <p>Template path.</p> <code>data</code> <code>dict[str]</code> <p>Formatting data.</p> Source code in <code>client/ayon_core/pipeline/traits/meta.py</code> <pre><code>@dataclass\nclass TemplatePath(TraitBase):\n    \"\"\"TemplatePath trait model.\n\n    This model represents a template path with formatting data.\n    Template path can be an Anatomy template and data is used to format it.\n\n    Example::\n\n        TemplatePath(template=\"path/{key}/file\", data={\"key\": \"to\"})\n\n    Attributes:\n        name (str): Trait name.\n        description (str): Trait description.\n        id (str): id should be a namespaced trait name with version\n        template (str): Template path.\n        data (dict[str]): Formatting data.\n    \"\"\"\n\n    name: ClassVar[str] = \"TemplatePath\"\n    description: ClassVar[str] = \"Template Path Trait Model\"\n    id: ClassVar[str] = \"ayon.meta.TemplatePath.v1\"\n    persistent: ClassVar[bool] = True\n    template: str\n    data: dict\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/meta.html#client.ayon_core.pipeline.traits.meta.Variant","title":"<code>Variant</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>Variant trait model.</p> <p>This model represents a variant of the representation.</p> <p>Example::</p> <pre><code>Variant(variant=\"high\")\nVariant(variant=\"prores444)\n</code></pre> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Trait name.</p> <code>description</code> <code>str</code> <p>Trait description.</p> <code>id</code> <code>str</code> <p>id should be a namespaced trait name with version</p> <code>variant</code> <code>str</code> <p>Variant name.</p> Source code in <code>client/ayon_core/pipeline/traits/meta.py</code> <pre><code>@dataclass\nclass Variant(TraitBase):\n    \"\"\"Variant trait model.\n\n    This model represents a variant of the representation.\n\n    Example::\n\n        Variant(variant=\"high\")\n        Variant(variant=\"prores444)\n\n    Attributes:\n        name (str): Trait name.\n        description (str): Trait description.\n        id (str): id should be a namespaced trait name with version\n        variant (str): Variant name.\n    \"\"\"\n\n    name: ClassVar[str] = \"Variant\"\n    description: ClassVar[str] = \"Variant Trait Model\"\n    id: ClassVar[str] = \"ayon.meta.Variant.v1\"\n    persistent: ClassVar[bool] = True\n    variant: str\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/publishing.html","title":"publishing","text":"<p>Publishing related methods for traits.</p>"},{"location":"autoapi/client/ayon_core/pipeline/traits/publishing.html#client.ayon_core.pipeline.traits.publishing.TransferItem","title":"<code>TransferItem</code>","text":"<p>Represents a single transfer item.</p> <p>Source file path, destination file path, template that was used to construct the destination path, template data that was used in the template, size of the file, checksum of the file.</p> <p>Attributes:</p> Name Type Description <code>source</code> <code>Path</code> <p>Source file path.</p> <code>destination</code> <code>Path</code> <p>Destination file path.</p> <code>size</code> <code>int</code> <p>Size of the file.</p> <code>checksum</code> <code>str</code> <p>Checksum of the file.</p> <code>template</code> <code>str</code> <p>Template path.</p> <code>template_data</code> <code>dict[str, Any]</code> <p>Template data.</p> <code>representation</code> <code>Representation</code> <p>Reference to representation</p> <code>related_trait</code> <code>FileLocation</code> <p>Reference to the trait that this transfer is related to. This is used to update the trait with the new file path after the transfer is done.</p> Source code in <code>client/ayon_core/pipeline/traits/publishing.py</code> <pre><code>class TransferItem:\n    \"\"\"Represents a single transfer item.\n\n    Source file path, destination file path, template that was used to\n    construct the destination path, template data that was used in the\n    template, size of the file, checksum of the file.\n\n    Attributes:\n        source (Path): Source file path.\n        destination (Path): Destination file path.\n        size (int): Size of the file.\n        checksum (str): Checksum of the file.\n        template (str): Template path.\n        template_data (dict[str, Any]): Template data.\n        representation (Representation): Reference to representation\n        related_trait (FileLocation): Reference to the trait that this\n            transfer is related to. This is used to update the trait with\n            the new file path after the transfer is done.\n\n    \"\"\"\n    source: Path\n    destination: Path\n    size: int\n    checksum: str\n    template: str\n    template_data: dict[str, Any]\n    representation: Representation\n    related_trait: FileLocation\n\n    def __init__(self,\n        source: Path,\n        destination: Path,\n        size: int,\n        checksum: str,\n        template: str,\n        template_data: dict[str, Any],\n        representation: Representation,\n        related_trait: FileLocation):\n\n        self.source = source\n        self.destination = destination\n        self.size = size\n        self.checksum = checksum\n        self.template = template\n        self.template_data = template_data\n        self.representation = representation\n        self.related_trait = related_trait\n\n    @staticmethod\n    def get_file_size(file_path: Path) -&gt; int:\n        \"\"\"Get the size of the file.\n\n        Args:\n            file_path (Path): File path.\n\n        Returns:\n            int: Size of the file.\n\n        \"\"\"\n        return file_path.stat().st_size\n\n    @staticmethod\n    def get_file_checksum(file_path: Path) -&gt; str:\n        \"\"\"Get checksum of the file.\n\n        Args:\n            file_path (Path): File path.\n\n        Returns:\n            str: Checksum of the file.\n\n        \"\"\"\n        return hashlib.sha256(\n            file_path.read_bytes()\n        ).hexdigest()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/publishing.html#client.ayon_core.pipeline.traits.publishing.TransferItem.get_file_checksum","title":"<code>get_file_checksum(file_path)</code>  <code>staticmethod</code>","text":"<p>Get checksum of the file.</p> <p>Parameters:</p> Name Type Description Default <code>file_path</code> <code>Path</code> <p>File path.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Checksum of the file.</p> Source code in <code>client/ayon_core/pipeline/traits/publishing.py</code> <pre><code>@staticmethod\ndef get_file_checksum(file_path: Path) -&gt; str:\n    \"\"\"Get checksum of the file.\n\n    Args:\n        file_path (Path): File path.\n\n    Returns:\n        str: Checksum of the file.\n\n    \"\"\"\n    return hashlib.sha256(\n        file_path.read_bytes()\n    ).hexdigest()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/publishing.html#client.ayon_core.pipeline.traits.publishing.TransferItem.get_file_size","title":"<code>get_file_size(file_path)</code>  <code>staticmethod</code>","text":"<p>Get the size of the file.</p> <p>Parameters:</p> Name Type Description Default <code>file_path</code> <code>Path</code> <p>File path.</p> required <p>Returns:</p> Name Type Description <code>int</code> <code>int</code> <p>Size of the file.</p> Source code in <code>client/ayon_core/pipeline/traits/publishing.py</code> <pre><code>@staticmethod\ndef get_file_size(file_path: Path) -&gt; int:\n    \"\"\"Get the size of the file.\n\n    Args:\n        file_path (Path): File path.\n\n    Returns:\n        int: Size of the file.\n\n    \"\"\"\n    return file_path.stat().st_size\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/publishing.html#client.ayon_core.pipeline.traits.publishing.get_legacy_files_for_representation","title":"<code>get_legacy_files_for_representation(transfer_items, representation, anatomy)</code>","text":"<p>Get legacy files for a given representation.</p> <p>This expects the file to exist - it must run after the transfer is done.</p> <p>This is used to prepare file information for tools working with legacy file data on representation. Like site-sync, etc.</p> <p>Parameters:</p> Name Type Description Default <code>transfer_items</code> <code>list[TransferItem]</code> <p>List of transfer items.</p> required <code>representation</code> <code>Representation</code> <p>Representation to get files for.</p> required <code>anatomy</code> <code>Anatomy</code> <p>Anatomy to use for preparing file info.</p> required <p>Returns:</p> Name Type Description <code>list</code> <code>list[dict[str, str]]</code> <p>List of legacy files.</p> Source code in <code>client/ayon_core/pipeline/traits/publishing.py</code> <pre><code>def get_legacy_files_for_representation(\n        transfer_items: list[TransferItem],\n        representation: Representation,\n        anatomy: \"Anatomy\",\n    ) -&gt; list[dict[str, str]]:\n    \"\"\"Get legacy files for a given representation.\n\n    This expects the file to exist - it must run after the transfer\n    is done.\n\n    This is used to prepare file information for tools working with legacy\n    file data on representation. Like site-sync, etc.\n\n    Args:\n        transfer_items (list[TransferItem]): List of transfer items.\n        representation (Representation): Representation to get files for.\n        anatomy (Anatomy): Anatomy to use for preparing file info.\n\n    Returns:\n        list: List of legacy files.\n\n    \"\"\"\n    selected: list[TransferItem] = []\n    selected.extend(\n        item\n        for item in transfer_items\n        if item.representation == representation\n    )\n    files: list[dict[str, str]] = []\n    files.extend(\n        _prepare_file_info(item.destination, anatomy)\n        for item in selected\n    )\n    return files\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/publishing.html#client.ayon_core.pipeline.traits.publishing.get_template_data_from_representation","title":"<code>get_template_data_from_representation(representation, instance)</code>","text":"<p>Get template data from representation.</p> <p>Using representation traits and data on instance prepare data for formatting template.</p> <p>Parameters:</p> Name Type Description Default <code>representation</code> <code>Representation</code> <p>Representation to process.</p> required <code>instance</code> <code>Instance</code> <p>Instance to process.</p> required <p>Returns:</p> Name Type Description <code>dict</code> <code>dict</code> <p>Template data.</p> Source code in <code>client/ayon_core/pipeline/traits/publishing.py</code> <pre><code>def get_template_data_from_representation(\n        representation: Representation,\n        instance: pyblish.api.Instance) -&gt; dict:\n    \"\"\"Get template data from representation.\n\n    Using representation traits and data on instance\n    prepare data for formatting template.\n\n    Args:\n        representation (Representation): Representation to process.\n        instance (pyblish.api.Instance): Instance to process.\n\n    Returns:\n        dict: Template data.\n\n    \"\"\"\n    template_data = copy.deepcopy(instance.data[\"anatomyData\"])\n    template_data[\"representation\"] = representation.name\n    template_data[\"version\"] = instance.data[\"version\"]\n    # template_data[\"hierarchy\"] = instance.data[\"hierarchy\"]\n\n    # add colorspace data to template data\n    if representation.contains_trait(ColorManaged):\n        colorspace_data: ColorManaged = representation.get_trait(\n            ColorManaged)\n\n        template_data[\"colorspace\"] = {\n            \"colorspace\": colorspace_data.color_space,\n            \"config\": colorspace_data.config\n        }\n\n    # add explicit list of traits properties to template data\n    # there must be some better way to handle this.\n\n    with contextlib.suppress(MissingTraitError):\n        # resolution from PixelBased trait\n        template_data[\"resolution_width\"] = representation.get_trait(\n            PixelBased).display_window_width\n        template_data[\"resolution_height\"] = representation.get_trait(\n            PixelBased).display_window_height\n\n    with contextlib.suppress(MissingTraitError):\n        # get fps from representation traits\n        template_data[\"fps\"] = representation.get_trait(\n            FrameRanged).frames_per_second\n\n    with contextlib.suppress(MissingTraitError):\n        file_path = representation.get_trait(FileLocation).file_path\n        if isinstance(file_path, str):\n            file_path = Path(file_path)\n        template_data[\"ext\"] = file_path.suffix.lstrip(\".\")\n    if not template_data.get(\"ext\"):\n        with contextlib.suppress(MissingTraitError):\n            # Try FileLocations trait if FileLocation ext is empty\n            file_locations_trait = representation.get_trait(\n                FileLocations)\n            if file_locations_trait.file_paths:\n                first_file_loc = file_locations_trait.file_paths[0]\n                file_path = first_file_loc.file_path\n                if isinstance(file_path, str):\n                    file_path = Path(file_path)\n                template_data[\"ext\"] = (file_path.suffix.lstrip(\".\"))\n        # Note: handle \"output\" and \"originalBasename\"\n    return template_data\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/publishing.html#client.ayon_core.pipeline.traits.publishing.get_transfers_from_bundle","title":"<code>get_transfers_from_bundle(representation, template_item, transfers)</code>","text":"<p>Get transfers from Bundle trait.</p> <p>This will be called recursively for each sub-representation in the bundle that is a Bundle itself.</p> <p>Parameters:</p> Name Type Description Default <code>representation</code> <code>Representation</code> <p>Representation to process.</p> required <code>template_item</code> <code>IntegrationTemplateItem</code> <p>Template item.</p> required <code>transfers</code> <code>list</code> <p>List of transfers.</p> required Mutates <p>transfers (list): List of transfers. template_item (TemplateItem): Template item.</p> Source code in <code>client/ayon_core/pipeline/traits/publishing.py</code> <pre><code>def get_transfers_from_bundle(\n        representation: Representation,\n        template_item: IntegrationTemplateItem,\n        transfers: list[TransferItem]\n) -&gt; None:\n    \"\"\"Get transfers from Bundle trait.\n\n    This will be called recursively for each sub-representation in the\n    bundle that is a Bundle itself.\n\n    Args:\n        representation (Representation): Representation to process.\n        template_item (IntegrationTemplateItem): Template item.\n        transfers (list): List of transfers.\n\n    Mutates:\n        transfers (list): List of transfers.\n        template_item (TemplateItem): Template item.\n\n    \"\"\"\n    bundle: Bundle = representation.get_trait(Bundle)\n    for idx, sub_representation_traits in enumerate(bundle.items):\n        sub_representation = Representation(\n            name=f\"{representation.name}_{idx}\",\n            traits=sub_representation_traits)\n        # sub presentation transient:\n        sub_representation.add_trait(Transient())\n        if sub_representation.contains_trait(FileLocations):\n            get_transfers_from_file_locations(\n                sub_representation, template_item, transfers\n            )\n        elif sub_representation.contains_trait(FileLocation):\n            get_transfers_from_file_location(\n                sub_representation, template_item, transfers\n            )\n        elif sub_representation.contains_trait(Bundle):\n            get_transfers_from_bundle(\n                sub_representation, template_item, transfers\n            )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/publishing.html#client.ayon_core.pipeline.traits.publishing.get_transfers_from_file_location","title":"<code>get_transfers_from_file_location(representation, template_item, transfers)</code>","text":"<p>Get transfers from FileLocation trait.</p> <p>Parameters:</p> Name Type Description Default <code>representation</code> <code>Representation</code> <p>Representation to process.</p> required <code>template_item</code> <code>IntegrationTemplateItem</code> <p>Template item.</p> required <code>transfers</code> <code>list</code> <p>List of transfers.</p> required Mutates <p>transfers (list): List of transfers. template_item (TemplateItem): Template item.</p> Source code in <code>client/ayon_core/pipeline/traits/publishing.py</code> <pre><code>def get_transfers_from_file_location(\n        representation: Representation,\n        template_item: IntegrationTemplateItem,\n        transfers: list[TransferItem]\n) -&gt; None:\n    \"\"\"Get transfers from FileLocation trait.\n\n    Args:\n        representation (Representation): Representation to process.\n        template_item (IntegrationTemplateItem): Template item.\n        transfers (list): List of transfers.\n\n    Mutates:\n        transfers (list): List of transfers.\n        template_item (TemplateItem): Template item.\n\n    \"\"\"\n    path_template_object: \"AnatomyStringTemplate\" = (\n        template_item.template_object[\"path\"]\n    )\n    file_path = representation.get_trait(FileLocation).file_path\n    if isinstance(file_path, str):\n        file_path = Path(file_path)\n\n    template_item.template_data[\"ext\"] = (\n        file_path.suffix.lstrip(\".\")\n    )\n    template_item.template_data.pop(\"frame\", None)\n    with contextlib.suppress(MissingTraitError):\n        udim = representation.get_trait(UDIM)\n        template_item.template_data[\"udim\"] = udim.udim[0]\n\n    template_filled = path_template_object.format_strict(\n        template_item.template_data\n    )\n\n    # add used values to the template data\n    used_values: dict = template_filled.used_values\n    template_item.template_data.update(used_values)\n\n    file_loc: FileLocation = representation.get_trait(FileLocation)\n    file_path = file_loc.file_path\n    if isinstance(file_path, str):\n        file_path = Path(file_path)\n\n    transfers.append(\n        TransferItem(\n            source=file_path,\n            destination=Path(template_filled),\n            size=file_loc.file_size or TransferItem.get_file_size(\n                file_path),\n            checksum=file_loc.file_hash or TransferItem.get_file_checksum(\n                file_path),\n            template=template_item.template_object[\"path\"],\n            template_data=template_item.template_data,\n            representation=representation,\n            related_trait=file_loc\n        )\n    )\n    # add template path and the data to resolve it\n    # remove template if already exists\n    with contextlib.suppress(ValueError):\n        representation.remove_trait(TemplatePath)\n\n    representation.add_trait(TemplatePath(\n        template=template_item.template_object[\"path\"],\n        data=template_item.template_data\n    ))\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/publishing.html#client.ayon_core.pipeline.traits.publishing.get_transfers_from_file_locations","title":"<code>get_transfers_from_file_locations(representation, template_item, transfers)</code>","text":"<p>Get transfers from FileLocations trait.</p> <p>Parameters:</p> Name Type Description Default <code>representation</code> <code>Representation</code> <p>Representation to process.</p> required <code>template_item</code> <code>IntegrationTemplateItem</code> <p>Template item.</p> required <code>transfers</code> <code>list</code> <p>List of transfers.</p> required Mutates <p>transfers (list): List of transfers. template_item (TemplateItem): Template item.</p> Source code in <code>client/ayon_core/pipeline/traits/publishing.py</code> <pre><code>def get_transfers_from_file_locations(\n        representation: Representation,\n        template_item: IntegrationTemplateItem,\n        transfers: list[TransferItem]) -&gt; None:\n    \"\"\"Get transfers from FileLocations trait.\n\n    Args:\n        representation (Representation): Representation to process.\n        template_item (IntegrationTemplateItem): Template item.\n        transfers (list): List of transfers.\n\n    Mutates:\n        transfers (list): List of transfers.\n        template_item (TemplateItem): Template item.\n\n    \"\"\"\n    if representation.contains_trait(Sequence):\n        get_transfers_from_sequence(\n            representation, template_item, transfers\n        )\n\n    elif representation.contains_trait(UDIM) and \\\n            not representation.contains_trait(Sequence):\n        # handle UDIM not in sequence\n        get_transfers_from_udim(\n            representation, template_item, transfers\n        )\n\n    else:\n        get_transfers_from_file_locations_common_root(\n            representation, template_item, transfers\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/publishing.html#client.ayon_core.pipeline.traits.publishing.get_transfers_from_file_locations_common_root","title":"<code>get_transfers_from_file_locations_common_root(representation, template_item, transfers)</code>","text":"<p>Get transfers from FileLocations trait preserving relative hierarchy.</p> <p>Parameters:</p> Name Type Description Default <code>representation</code> <code>Representation</code> <p>Representation to process.</p> required <code>template_item</code> <code>IntegrationTemplateItem</code> <p>Template item.</p> required <code>transfers</code> <code>list</code> <p>List of transfers.</p> required Mutates <p>transfers (list): List of transfers. template_item (TemplateItem): Template item.</p> Source code in <code>client/ayon_core/pipeline/traits/publishing.py</code> <pre><code>def get_transfers_from_file_locations_common_root(\n        representation: Representation,\n        template_item: IntegrationTemplateItem,\n        transfers: list[TransferItem]\n) -&gt; None:\n    \"\"\"Get transfers from FileLocations trait preserving relative hierarchy.\n\n    Args:\n        representation (Representation): Representation to process.\n        template_item (IntegrationTemplateItem): Template item.\n        transfers (list): List of transfers.\n\n    Mutates:\n        transfers (list): List of transfers.\n        template_item (TemplateItem): Template item.\n\n    \"\"\"\n    file_locations_trait = representation.get_trait(FileLocations)\n    if not file_locations_trait.file_paths:\n        return\n\n    try:\n        common_root = file_locations_trait.get_common_root()\n    except ValueError as exc:\n        raise PublishError(\n            f\"Could not determine common root for representation \"\n            f\"'{representation.name}'\"\n        ) from exc\n\n    path_template_object = template_item.template_object[\"path\"]\n    template_filled = path_template_object.format_strict(\n        template_item.template_data\n    )\n\n    used_values = template_filled.used_values\n    template_item.template_data.update(used_values)\n\n    destination_root = Path(template_filled)\n    if destination_root.suffix:\n        destination_root = destination_root.parent\n\n    for file_loc in file_locations_trait.file_paths:\n        source = file_loc.file_path\n        if isinstance(source, str):\n            source = Path(source)\n\n        relative_path = source.relative_to(common_root)\n        destination = destination_root / relative_path\n\n        transfers.append(\n            TransferItem(\n                source=source,\n                destination=destination,\n                size=file_loc.file_size or TransferItem.get_file_size(source),\n                checksum=(\n                    file_loc.file_hash\n                    or TransferItem.get_file_checksum(source)\n                ),\n                template=template_item.template_object[\"path\"],\n                template_data=template_item.template_data,\n                representation=representation,\n                related_trait=file_loc\n            )\n        )\n\n    if not representation.contains_trait(TemplatePath):\n        representation.add_trait(\n            TemplatePath(\n                template=template_item.template_object[\"path\"],\n                data=template_item.template_data,\n            )\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/publishing.html#client.ayon_core.pipeline.traits.publishing.get_transfers_from_representations","title":"<code>get_transfers_from_representations(instance, template, representations)</code>","text":"<p>Get transfers from representations.</p> <p>This method will go through all representations and prepare transfers based on the traits they contain. First it will validate the representation, and then it will prepare template data for the representation. It specifically handles FileLocations, FileLocation, Bundle, Sequence and UDIM traits.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>Instance</code> <p>Instance to process.</p> required <code>template</code> <code>AnatomyStringTemplate</code> <p>Template to use for formatting destination paths.</p> required <code>representations</code> <code>list[Representation]</code> <p>List of representations.</p> required <p>Returns:</p> Type Description <code>list[TransferItem]</code> <p>list[TransferItem]: List of transfers.</p> <p>Raises:</p> Type Description <code>PublishError</code> <p>If representation is invalid.</p> Source code in <code>client/ayon_core/pipeline/traits/publishing.py</code> <pre><code>def get_transfers_from_representations(\n        instance: pyblish.api.Instance,\n        template: AnatomyTemplateItem,\n        representations: list[Representation]\n) -&gt; list[TransferItem]:\n    \"\"\"Get transfers from representations.\n\n    This method will go through all representations and prepare transfers\n    based on the traits they contain. First it will validate the\n    representation, and then it will prepare template data for the\n    representation. It specifically handles FileLocations, FileLocation,\n    Bundle, Sequence and UDIM traits.\n\n    Args:\n        instance (pyblish.api.Instance): Instance to process.\n        template (AnatomyStringTemplate): Template to use for formatting\n            destination paths.\n        representations (list[Representation]): List of representations.\n\n    Returns:\n        list[TransferItem]: List of transfers.\n\n    Raises:\n        PublishError: If representation is invalid.\n\n    \"\"\"\n    instance_template_data: dict[str, str] = {}\n    transfers: list[TransferItem] = []\n    # prepare template and data to format it\n    for representation in representations:\n\n        # validate representation first, this will go through all traits\n        # and check if they are valid\n        try:\n            representation.validate()\n        except TraitValidationError as e:\n            msg = f\"Representation '{representation.name}' is invalid: {e}\"\n            raise PublishError(msg) from e\n\n        template_data = get_template_data_from_representation(\n            representation, instance)\n        # add instance based template data\n\n        template_data.update(instance_template_data)\n\n        # treat Variant as `output` in template data\n        with contextlib.suppress(MissingTraitError):\n            template_data[\"output\"] = (\n                representation.get_trait(Variant).variant\n            )\n\n        template_item = IntegrationTemplateItem(\n            anatomy=instance.context.data[\"anatomy\"],\n            template_data=copy.deepcopy(template_data),\n            template_object=template\n        )\n\n        if representation.contains_trait(FileLocations):\n            # If representation has FileLocations trait (list of files)\n            # it can be a Sequence, UDIM tile set, or a group of related\n            # files that share a common root and preserve their hierarchy.\n            # Note: we do not support yet frame sequence of multiple UDIM\n            # tiles in the same representation.\n            get_transfers_from_file_locations(\n                representation, template_item, transfers\n            )\n        elif representation.contains_trait(FileLocation):\n            # This is just a single file representation\n            get_transfers_from_file_location(\n                representation, template_item, transfers\n            )\n\n        elif representation.contains_trait(Bundle):\n            # Bundle groups multiple \"sub-representations\" together.\n            # It has a list of lists with traits, some might be\n            # FileLocations,but some might be \"file-less\" representations\n            # or even other bundles.\n            get_transfers_from_bundle(\n                representation, template_item, transfers\n            )\n    return transfers\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/publishing.html#client.ayon_core.pipeline.traits.publishing.get_transfers_from_sequence","title":"<code>get_transfers_from_sequence(representation, template_item, transfers)</code>","text":"<p>Get transfers from Sequence trait.</p> <p>Parameters:</p> Name Type Description Default <code>representation</code> <code>Representation</code> <p>Representation to process.</p> required <code>template_item</code> <code>IntegrationTemplateItem</code> <p>Template item.</p> required <code>transfers</code> <code>list</code> <p>List of transfers.</p> required Mutates <p>transfers (list): List of transfers. template_item (TemplateItem): Template item.</p> Source code in <code>client/ayon_core/pipeline/traits/publishing.py</code> <pre><code>def get_transfers_from_sequence(\n        representation: Representation,\n        template_item: IntegrationTemplateItem,\n        transfers: list[TransferItem]\n) -&gt; None:\n    \"\"\"Get transfers from Sequence trait.\n\n    Args:\n        representation (Representation): Representation to process.\n        template_item (IntegrationTemplateItem): Template item.\n        transfers (list): List of transfers.\n\n    Mutates:\n        transfers (list): List of transfers.\n        template_item (TemplateItem): Template item.\n\n    \"\"\"\n    sequence: Sequence = representation.get_trait(Sequence)\n    path_template_object: AnatomyStringTemplate = (\n        template_item.template_object[\"path\"]\n    )\n    frames: list[int] = sequence.get_frame_list(\n        representation.get_trait(FileLocations),\n        regex=sequence.frame_regex)\n\n    # Go through all frames in the sequence and\n    # find their corresponding file locations, then\n    # format their template and add them to transfers.\n    for frame in frames:\n        file_loc: FileLocation = representation.get_trait(\n            FileLocations).get_file_location_for_frame(\n            frame, sequence)\n\n        template_item.template_data[\"frame\"] = frame\n        template_item.template_data[\"ext\"] = (\n            file_loc.file_path.suffix.lstrip(\".\"))\n        template_filled = path_template_object.format_strict(\n                template_item.template_data\n        )\n\n        # add used values to the template data\n        used_values: dict = template_filled.used_values\n        template_item.template_data.update(used_values)\n\n        transfers.append(\n            TransferItem(\n                source=file_loc.file_path,\n                destination=Path(template_filled),\n                size=file_loc.file_size or TransferItem.get_file_size(\n                    file_loc.file_path),\n                checksum=file_loc.file_hash or TransferItem.get_file_checksum(\n                    file_loc.file_path),\n                template=template_item.template_object[\"path\"],\n                template_data=template_item.template_data,\n                representation=representation,\n                related_trait=file_loc\n            )\n        )\n\n    # add template path and the data to resolve it\n    if not representation.contains_trait(TemplatePath):\n        representation.add_trait(TemplatePath(\n            template=template_item.template_object[\"path\"],\n            data=template_item.template_data\n        ))\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/publishing.html#client.ayon_core.pipeline.traits.publishing.get_transfers_from_udim","title":"<code>get_transfers_from_udim(representation, template_item, transfers)</code>","text":"<p>Get transfers from UDIM trait.</p> <p>Parameters:</p> Name Type Description Default <code>representation</code> <code>Representation</code> <p>Representation to process.</p> required <code>template_item</code> <code>IntegrationTemplateItem</code> <p>Template item.</p> required <code>transfers</code> <code>list</code> <p>List of transfers.</p> required Mutates <p>transfers (list): List of transfers. template_item (TemplateItem): Template item.</p> Source code in <code>client/ayon_core/pipeline/traits/publishing.py</code> <pre><code>def get_transfers_from_udim(\n        representation: Representation,\n        template_item: IntegrationTemplateItem,\n        transfers: list[TransferItem]\n) -&gt; None:\n    \"\"\"Get transfers from UDIM trait.\n\n    Args:\n        representation (Representation): Representation to process.\n        template_item (IntegrationTemplateItem): Template item.\n        transfers (list): List of transfers.\n\n    Mutates:\n        transfers (list): List of transfers.\n        template_item (TemplateItem): Template item.\n\n    \"\"\"\n    udim: UDIM = representation.get_trait(UDIM)\n    path_template_object: \"AnatomyStringTemplate\" = (\n        template_item.template_object[\"path\"]\n    )\n    for file_loc in representation.get_trait(\n            FileLocations).file_paths:\n        template_item.template_data[\"udim\"] = (\n            udim.get_udim_from_file_location(file_loc)\n        )\n\n        template_filled = path_template_object.format_strict(\n            template_item.template_data\n        )\n\n        # add used values to the template data\n        used_values: dict = template_filled.used_values\n        template_item.template_data.update(used_values)\n\n        transfers.append(\n            TransferItem(\n                source=file_loc.file_path,\n                destination=Path(template_filled),\n                size=file_loc.file_size or TransferItem.get_file_size(\n                    file_loc.file_path),\n                checksum=file_loc.file_hash or TransferItem.get_file_checksum(\n                    file_loc.file_path),\n                template=template_item.template_object[\"path\"],\n                template_data=template_item.template_data,\n                representation=representation,\n                related_trait=file_loc\n            )\n        )\n    # add template path and the data to resolve it\n    representation.add_trait(TemplatePath(\n        template=template_item.template_object[\"path\"],\n        data=template_item.template_data\n    ))\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/publishing.html#client.ayon_core.pipeline.traits.publishing.replace_paths_in_representation","title":"<code>replace_paths_in_representation(representation, transfers)</code>","text":"<p>Replace paths in representation traits based on transfers.</p> <p>This is used to update the traits with the new file paths after the transfer is done.</p> <p>Parameters:</p> Name Type Description Default <code>representation</code> <code>Representation</code> <p>Representation to update.</p> required <code>transfers</code> <code>list[TransferItem]</code> <p>List of transfer items.</p> required Mutates <p>representation (Representation): Representation with updated paths.</p> Source code in <code>client/ayon_core/pipeline/traits/publishing.py</code> <pre><code>def replace_paths_in_representation(\n        representation: Representation,\n        transfers: list[TransferItem]\n) -&gt; None:\n    \"\"\"Replace paths in representation traits based on transfers.\n\n    This is used to update the traits with the new file paths after the\n    transfer is done.\n\n    Args:\n        representation (Representation): Representation to update.\n        transfers (list[TransferItem]): List of transfer items.\n\n    Mutates:\n        representation (Representation): Representation with updated paths.\n\n    \"\"\"\n    for transfer in transfers:\n        if transfer.representation == representation:\n            if representation.contains_trait(FileLocation):\n                f_trait: FileLocation = representation.get_trait(\n                    FileLocation)\n                path_in_trait = f_trait.file_path\n                if path_in_trait == transfer.source:\n                    f_trait.file_path = transfer.destination\n            if representation.contains_trait(FileLocations):\n                fl_trait: FileLocations = representation.get_trait(\n                    FileLocations)\n                for idx, file_loc in enumerate(fl_trait.file_paths):\n                    path_in_trait = file_loc.file_path\n                    if path_in_trait == transfer.source:\n                        fl_trait.file_paths[idx].file_path = (\n                            transfer.destination\n                        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/representation.html","title":"representation","text":"<p>Defines the base trait model and representation.</p>"},{"location":"autoapi/client/ayon_core/pipeline/traits/representation.html#client.ayon_core.pipeline.traits.representation.Representation","title":"<code>Representation</code>","text":"<p>               Bases: <code>Generic[T]</code></p> <p>Representation of products.</p> <p>Representation defines a collection of individual properties that describe the specific \"form\" of the product. A trait represents a set of properties therefore, the Representation is a collection of traits.</p> <p>It holds methods to add, remove, get, and check for the existence of a trait in the representation.</p> Note <p><code>PLR0904</code> is the rule for checking the number of public methods in a class.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Representation name. Must be unique within instance.</p> required <code>representation_id</code> <code>str</code> <p>Representation ID.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>class Representation(Generic[T]):  # noqa: PLR0904\n    \"\"\"Representation of products.\n\n    Representation defines a collection of individual properties that describe\n    the specific \"form\" of the product. A trait represents a set of\n    properties therefore, the Representation is a collection of traits.\n\n    It holds methods to add, remove, get, and check for the existence of a\n    trait in the representation.\n\n    Note:\n        `PLR0904` is the rule for checking the number of public methods\n        in a class.\n\n    Arguments:\n        name (str): Representation name. Must be unique within instance.\n        representation_id (str): Representation ID.\n    \"\"\"\n\n    _data: dict[str, T]\n    name: str\n    representation_id: str\n\n    def __hash__(self):\n        \"\"\"Return hash of the representation ID.\"\"\"\n        return hash(self.representation_id)\n\n    def __getitem__(self, key: str) -&gt; T:\n        \"\"\"Get the trait by ID.\n\n        Args:\n            key (str): Trait ID.\n\n        Returns:\n            TraitBase: Trait instance.\n\n        \"\"\"\n        return self.get_trait_by_id(key)\n\n    def __setitem__(self, key: str, value: T) -&gt; None:\n        \"\"\"Set the trait by ID.\n\n        Args:\n            key (str): Trait ID.\n            value (TraitBase): Trait instance.\n\n        \"\"\"\n        with contextlib.suppress(KeyError):\n            self._data.pop(key)\n\n        self.add_trait(value)\n\n    def __delitem__(self, key: str) -&gt; None:\n        \"\"\"Remove the trait by ID.\n\n        Args:\n            key (str): Trait ID.\n\n\n        \"\"\"\n        self.remove_trait_by_id(key)\n\n    def __contains__(self, key: str) -&gt; bool:\n        \"\"\"Check if the trait exists by ID.\n\n        Args:\n            key (str): Trait ID.\n\n        Returns:\n            bool: True if the trait exists, False otherwise.\n\n        \"\"\"\n        return self.contains_trait_by_id(key)\n\n    def __iter__(self):\n        \"\"\"Return the trait ID iterator.\"\"\"\n        return iter(self._data)\n\n    def __str__(self):\n        \"\"\"Return the representation name.\"\"\"\n        return self.name\n\n    def items(self) -&gt; ItemsView[str, T]:\n        \"\"\"Return the traits as items.\"\"\"\n        return ItemsView(self._data)\n\n    def add_trait(self, trait: T, *, exists_ok: bool = False) -&gt; None:\n        \"\"\"Add a trait to the Representation.\n\n        Args:\n            trait (TraitBase): Trait to add.\n            exists_ok (bool, optional): If True, do not raise an error if the\n                trait already exists. Defaults to False.\n\n        Raises:\n            ValueError: If the trait ID is not provided, or the trait already\n                exists.\n\n        \"\"\"\n        if not hasattr(trait, \"id\"):\n            error_msg = f\"Invalid trait {trait} - ID is required.\"\n            raise ValueError(error_msg)\n        if trait.id in self._data and not exists_ok:\n            error_msg = f\"Trait with ID {trait.id} already exists.\"\n            raise ValueError(error_msg)\n        self._data[trait.id] = trait\n\n    def add_traits(\n            self, traits: list[T], *, exists_ok: bool = False) -&gt; None:\n        \"\"\"Add a list of traits to the Representation.\n\n        Args:\n            traits (list[TraitBase]): List of traits to add.\n            exists_ok (bool, optional): If True, do not raise an error if the\n                trait already exists. Defaults to False.\n\n        \"\"\"\n        for trait in traits:\n            self.add_trait(trait, exists_ok=exists_ok)\n\n    def remove_trait(self, trait: Type[TraitBase]) -&gt; None:\n        \"\"\"Remove a trait from the data.\n\n        Args:\n            trait (TraitBase, optional): Trait class.\n\n        Raises:\n            ValueError: If the trait is not found.\n\n        \"\"\"\n        try:\n            self._data.pop(str(trait.id))\n        except KeyError as e:\n            error_msg = f\"Trait with ID {trait.id} not found.\"\n            raise ValueError(error_msg) from e\n\n    def remove_trait_by_id(self, trait_id: str) -&gt; None:\n        \"\"\"Remove a trait from the data by its ID.\n\n        Args:\n            trait_id (str): Trait ID.\n\n        Raises:\n            ValueError: If the trait is not found.\n\n        \"\"\"\n        try:\n            self._data.pop(trait_id)\n        except KeyError as e:\n            error_msg = f\"Trait with ID {trait_id} not found.\"\n            raise ValueError(error_msg) from e\n\n    def remove_traits(self, traits: list[Type[T]]) -&gt; None:\n        \"\"\"Remove a list of traits from the Representation.\n\n        If no trait IDs or traits are provided, all traits will be removed.\n\n        Args:\n            traits (list[TraitBase]): List of trait classes.\n\n        \"\"\"\n        if not traits:\n            self._data = {}\n            return\n\n        for trait in traits:\n            self.remove_trait(trait)\n\n    def remove_traits_by_id(self, trait_ids: list[str]) -&gt; None:\n        \"\"\"Remove a list of traits from the Representation by their ID.\n\n        If no trait IDs or traits are provided, all traits will be removed.\n\n        Args:\n            trait_ids (list[str], optional): List of trait IDs.\n\n        \"\"\"\n        for trait_id in trait_ids:\n            self.remove_trait_by_id(trait_id)\n\n    def has_traits(self) -&gt; bool:\n        \"\"\"Check if the Representation has any traits.\n\n        Returns:\n            bool: True if the Representation has any traits, False otherwise.\n\n        \"\"\"\n        return bool(self._data)\n\n    def contains_trait(self, trait: Type[T]) -&gt; bool:\n        \"\"\"Check if the trait exists in the Representation.\n\n        Args:\n            trait (TraitBase): Trait class.\n\n        Returns:\n            bool: True if the trait exists, False otherwise.\n\n        \"\"\"\n        return bool(self._data.get(str(trait.id)))\n\n    def contains_trait_by_id(self, trait_id: str) -&gt; bool:\n        \"\"\"Check if the trait exists using trait id.\n\n        Args:\n            trait_id (str): Trait ID.\n\n        Returns:\n            bool: True if the trait exists, False otherwise.\n\n        \"\"\"\n        return bool(self._data.get(trait_id))\n\n    def contains_traits(self, traits: list[Type[T]]) -&gt; bool:\n        \"\"\"Check if the traits exist.\n\n        Args:\n            traits (list[TraitBase], optional): List of trait classes.\n\n        Returns:\n            bool: True if all traits exist, False otherwise.\n\n        \"\"\"\n        return all(self.contains_trait(trait=trait) for trait in traits)\n\n    def contains_traits_by_id(self, trait_ids: list[str]) -&gt; bool:\n        \"\"\"Check if the traits exist by id.\n\n        If no trait IDs or traits are provided, it will check if the\n        representation has any traits.\n\n        Args:\n            trait_ids (list[str]): List of trait IDs.\n\n        Returns:\n            bool: True if all traits exist, False otherwise.\n\n        \"\"\"\n        return all(\n            self.contains_trait_by_id(trait_id) for trait_id in trait_ids\n        )\n\n    def get_trait(self, trait: Type[T]) -&gt; T:\n        \"\"\"Get a trait from the representation.\n\n        Args:\n            trait (TraitBase, optional): Trait class.\n\n        Returns:\n            TraitBase: Trait instance.\n\n        Raises:\n            MissingTraitError: If the trait is not found.\n\n        \"\"\"\n        try:\n            return self._data[str(trait.id)]\n        except KeyError as e:\n            msg = f\"Trait with ID {trait.id} not found.\"\n            raise MissingTraitError(msg) from e\n\n    def get_trait_by_id(self, trait_id: str) -&gt; T:\n        # sourcery skip: use-named-expression\n        \"\"\"Get a trait from the representation by id.\n\n        Args:\n            trait_id (str): Trait ID.\n\n        Returns:\n            TraitBase: Trait instance.\n\n        Raises:\n            MissingTraitError: If the trait is not found.\n\n        \"\"\"\n        version = _get_version_from_id(trait_id)\n        if version:\n            try:\n                return self._data[trait_id]\n            except KeyError as e:\n                msg = f\"Trait with ID {trait_id} not found.\"\n                raise MissingTraitError(msg) from e\n\n        result = next(\n            (\n                self._data.get(trait_id)\n                for trait_id in self._data\n                if trait_id.startswith(trait_id)\n            ),\n            None,\n        )\n        if result is None:\n            msg = f\"Trait with ID {trait_id} not found.\"\n            raise MissingTraitError(msg)\n        return result\n\n    def get_traits(self,\n                     traits: Optional[list[Type[T]]] = None\n     ) -&gt; dict[str, T]:\n        \"\"\"Get a list of traits from the representation.\n\n        If no trait IDs or traits are provided, all traits will be returned.\n\n        Args:\n            traits (list[TraitBase], optional): List of trait classes.\n\n        Returns:\n            dict: Dictionary of traits.\n\n        \"\"\"\n        result: dict[str, T] = {}\n        if not traits:\n            for trait_id in self._data:\n                result[trait_id] = self.get_trait_by_id(trait_id=trait_id)\n            return result\n\n        for trait in traits:\n            result[str(trait.id)] = self.get_trait(trait=trait)\n        return result\n\n    def get_traits_by_ids(self, trait_ids: list[str]) -&gt; dict[str, T]:\n        \"\"\"Get a list of traits from the representation by their id.\n\n        If no trait IDs or traits are provided, all traits will be returned.\n\n        Args:\n            trait_ids (list[str]): List of trait IDs.\n\n        Returns:\n            dict: Dictionary of traits.\n\n        \"\"\"\n        return {\n            trait_id: self.get_trait_by_id(trait_id)\n            for trait_id in trait_ids\n        }\n\n    def traits_as_dict(self) -&gt; dict:\n        \"\"\"Return the traits from Representation data as a dictionary.\n\n        Returns:\n            dict: Traits data dictionary.\n\n        \"\"\"\n        return {\n            trait_id: trait.as_dict()\n            for trait_id, trait in self._data.items()\n            if trait and trait_id\n        }\n\n    def __len__(self):\n        \"\"\"Return the length of the data.\"\"\"\n        return len(self._data)\n\n    def __init__(\n            self,\n            name: str,\n            representation_id: Optional[str] = None,\n            traits: Optional[list[T]] = None):\n        \"\"\"Initialize the data.\n\n        Args:\n            name (str): Representation name. Must be unique within instance.\n            representation_id (str, optional): Representation ID.\n            traits (list[TraitBase], optional): List of traits.\n\n        \"\"\"\n        self.name = name\n        self.representation_id = representation_id or uuid.uuid4().hex\n        self._data = {}\n        if traits:\n            for trait in traits:\n                self.add_trait(trait)\n\n    @staticmethod\n    def _get_version_from_id(trait_id: str) -&gt; Union[int, None]:\n        # sourcery skip: use-named-expression\n        \"\"\"Check if the trait has a version specified.\n\n        Args:\n            trait_id (str): Trait ID.\n\n        Returns:\n            int: Trait version.\n            None: If the trait id does not have a version.\n\n        \"\"\"\n        version_regex = r\"v(\\d+)$\"\n        match = re.search(version_regex, trait_id)\n        return int(match[1]) if match else None\n\n    def __eq__(self, other: object) -&gt; bool:  # noqa: PLR0911\n        \"\"\"Check if the representation is equal to another.\n\n        Args:\n            other (Representation): Representation to compare.\n\n        Returns:\n            bool: True if the representations are equal, False otherwise.\n\n        \"\"\"\n        if not isinstance(other, Representation):\n            return False\n\n        if self.representation_id != other.representation_id:\n            return False\n\n        if self.name != other.name:\n            return False\n\n        # number of traits\n        if len(self) != len(other):\n            return False\n\n        for trait_id, trait in self._data.items():\n            if trait_id not in other._data:\n                return False\n            if trait != other._data[trait_id]:\n                return False\n\n        return True\n\n    @classmethod\n    def get_trait_class_by_trait_id(cls, trait_id: str) -&gt; Optional[Type[T]]:\n        \"\"\"Get the trait class for the given trait ID.\n\n        Args:\n            trait_id (str): Trait ID.\n\n        Returns:\n            type[TraitBase]: Trait class.\n            None: If the trait class is not found.\n\n        Raises:\n            IncompatibleTraitVersionError: If the trait version is incompatible\n                with the current version of the trait.\n            UpgradableTraitError: If the trait version is upgradable to a newer\n                version.\n\n        Note:\n            `ignore[return-value]` can be removed in future versions of Python\n            where the Generics support is better.\n\n        \"\"\"\n        # Iteratively collect all subclasses\n        trait_classes = set()\n        to_process = [TraitBase]\n\n        while to_process:\n            current = to_process.pop()\n            subclasses = current.__subclasses__()\n            trait_classes.update(subclasses)\n            to_process.extend(subclasses)\n\n        # 1. Search for the exact match\n        for trait_class in trait_classes:\n            if getattr(trait_class, \"id\", None) == trait_id:\n                return trait_class  # type: ignore[return-value]\n\n        # 2. Search for fuzzy matches (same base ID, different version)\n        req_version = _get_version_from_id(trait_id)\n        # Determine base ID (e.g., 'ayon.trait' from 'ayon.trait.v1')\n        base_id = trait_id\n        if req_version is not None:\n            base_id = re.sub(r\"\\.v\\d+$\", \"\", trait_id)\n\n        candidates = []\n        for trait_class in trait_classes:\n            t_id = getattr(trait_class, \"id\", \"\")\n            if not t_id:\n                continue\n\n            t_ver = _get_version_from_id(t_id)\n            t_base = t_id\n            if t_ver is not None:\n                t_base = re.sub(r\"\\.v\\d+$\", \"\", t_id)\n\n            if t_base == base_id:\n                candidates.append(\n                    (trait_class, t_ver or 0)\n                )\n\n        if not candidates:\n            return None  # type: ignore[return-value]\n\n        # Find the highest version among candidates\n        found_trait, found_version = max(candidates, key=lambda x: x[1])\n\n        if req_version is None:\n            return found_trait  # type: ignore[return-value]\n\n        if found_version == 0:\n            msg = (\n                f\"Trait {found_trait.id} found with no version, \"\n                \"but requested version is specified.\"\n            )\n            raise IncompatibleTraitVersionError(msg)\n\n        if req_version &gt; found_version:\n            error_msg = (\n                f\"Requested trait version {req_version} is \"\n                f\"higher than the found trait version {found_version}.\"\n            )\n            raise IncompatibleTraitVersionError(error_msg)\n\n        if req_version &lt; found_version:\n            if hasattr(found_trait, \"upgrade\"):\n                error_msg = (\n                    \"Requested trait version \"\n                    f\"{req_version} is lower \"\n                    f\"than the found trait version {found_version}.\"\n                )\n                error: UpgradableTraitError = UpgradableTraitError(error_msg)\n                error.trait = found_trait\n                raise error\n            # If the explicit version was requested, matches failed,\n            # and no upgrade path exists, we return None (effectively\n            # \"Not Found\" for thatversion)\n            return None  # type: ignore[return-value]\n\n        return found_trait  # type: ignore[return-value]\n\n    @classmethod\n    def from_dict(\n            cls: Type[Representation],\n            name: str,\n            representation_id: Optional[str] = None,\n            trait_data: Optional[dict] = None) -&gt; Representation:\n        \"\"\"Create a representation from a dictionary.\n\n        Args:\n            name (str): Representation name.\n            representation_id (str, optional): Representation ID.\n            trait_data (dict): Representation data. Dictionary with keys\n                as trait ids and values as trait data. Example::\n\n                    {\n                        \"ayon.2d.PixelBased.v1\": {\n                            \"display_window_width\": 1920,\n                            \"display_window_height\": 1080\n                        },\n                        \"ayon.2d.Planar.v1\": {\n                            \"channels\": 3\n                        }\n                    }\n\n        Returns:\n            Representation: Representation instance.\n\n        Raises:\n            ValueError: If the trait model with ID is not found.\n            TypeError: If the trait data is not a dictionary.\n            IncompatibleTraitVersionError: If the trait version is incompatible\n\n        \"\"\"\n        if not trait_data:\n            trait_data = {}\n        traits = []\n        for trait_id, value in trait_data.items():\n            if not isinstance(value, dict):\n                msg = (\n                    f\"Invalid trait data for trait ID {trait_id}. \"\n                    \"Trait data must be a dictionary.\"\n                )\n                raise TypeError(msg)\n\n            try:\n                trait_class = cls.get_trait_class_by_trait_id(trait_id)\n            except UpgradableTraitError as e:\n                # we found a newer version of trait, we will upgrade the data\n                if hasattr(e.trait, \"upgrade\"):\n                    traits.append(e.trait.upgrade(value))\n                else:\n                    msg = (\n                        f\"Newer version of trait {e.trait.id} found \"\n                        f\"for requested {trait_id} but without \"\n                        \"upgrade method.\"\n                    )\n                    raise IncompatibleTraitVersionError(msg) from e\n            else:\n                if not trait_class:\n                    error_msg = f\"Trait model with ID {trait_id} not found.\"\n                    raise ValueError(error_msg)\n\n                traits.append(trait_class(**value))\n\n        return cls(\n            name=name, representation_id=representation_id, traits=traits)\n\n    def validate(self) -&gt; None:\n        \"\"\"Validate the representation.\n\n        This method will validate all the traits in the representation.\n\n        Raises:\n            TraitValidationError: If the trait is invalid within representation\n\n        \"\"\"\n        errors = []\n        for trait in self._data.values():\n            # we do this in the loop to catch all the errors\n            try:\n                trait.validate_trait(self)\n            except TraitValidationError as e:  # noqa: PERF203\n                errors.append(str(e))\n        if errors:\n            msg = \"\\n\".join(errors)\n            scope = self.name\n            raise TraitValidationError(scope, msg)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/representation.html#client.ayon_core.pipeline.traits.representation.Representation.__contains__","title":"<code>__contains__(key)</code>","text":"<p>Check if the trait exists by ID.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>Trait ID.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if the trait exists, False otherwise.</p> Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>def __contains__(self, key: str) -&gt; bool:\n    \"\"\"Check if the trait exists by ID.\n\n    Args:\n        key (str): Trait ID.\n\n    Returns:\n        bool: True if the trait exists, False otherwise.\n\n    \"\"\"\n    return self.contains_trait_by_id(key)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/representation.html#client.ayon_core.pipeline.traits.representation.Representation.__delitem__","title":"<code>__delitem__(key)</code>","text":"<p>Remove the trait by ID.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>Trait ID.</p> required Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>def __delitem__(self, key: str) -&gt; None:\n    \"\"\"Remove the trait by ID.\n\n    Args:\n        key (str): Trait ID.\n\n\n    \"\"\"\n    self.remove_trait_by_id(key)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/representation.html#client.ayon_core.pipeline.traits.representation.Representation.__eq__","title":"<code>__eq__(other)</code>","text":"<p>Check if the representation is equal to another.</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>Representation</code> <p>Representation to compare.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if the representations are equal, False otherwise.</p> Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>def __eq__(self, other: object) -&gt; bool:  # noqa: PLR0911\n    \"\"\"Check if the representation is equal to another.\n\n    Args:\n        other (Representation): Representation to compare.\n\n    Returns:\n        bool: True if the representations are equal, False otherwise.\n\n    \"\"\"\n    if not isinstance(other, Representation):\n        return False\n\n    if self.representation_id != other.representation_id:\n        return False\n\n    if self.name != other.name:\n        return False\n\n    # number of traits\n    if len(self) != len(other):\n        return False\n\n    for trait_id, trait in self._data.items():\n        if trait_id not in other._data:\n            return False\n        if trait != other._data[trait_id]:\n            return False\n\n    return True\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/representation.html#client.ayon_core.pipeline.traits.representation.Representation.__getitem__","title":"<code>__getitem__(key)</code>","text":"<p>Get the trait by ID.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>Trait ID.</p> required <p>Returns:</p> Name Type Description <code>TraitBase</code> <code>T</code> <p>Trait instance.</p> Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>def __getitem__(self, key: str) -&gt; T:\n    \"\"\"Get the trait by ID.\n\n    Args:\n        key (str): Trait ID.\n\n    Returns:\n        TraitBase: Trait instance.\n\n    \"\"\"\n    return self.get_trait_by_id(key)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/representation.html#client.ayon_core.pipeline.traits.representation.Representation.__hash__","title":"<code>__hash__()</code>","text":"<p>Return hash of the representation ID.</p> Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>def __hash__(self):\n    \"\"\"Return hash of the representation ID.\"\"\"\n    return hash(self.representation_id)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/representation.html#client.ayon_core.pipeline.traits.representation.Representation.__init__","title":"<code>__init__(name, representation_id=None, traits=None)</code>","text":"<p>Initialize the data.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Representation name. Must be unique within instance.</p> required <code>representation_id</code> <code>str</code> <p>Representation ID.</p> <code>None</code> <code>traits</code> <code>list[TraitBase]</code> <p>List of traits.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>def __init__(\n        self,\n        name: str,\n        representation_id: Optional[str] = None,\n        traits: Optional[list[T]] = None):\n    \"\"\"Initialize the data.\n\n    Args:\n        name (str): Representation name. Must be unique within instance.\n        representation_id (str, optional): Representation ID.\n        traits (list[TraitBase], optional): List of traits.\n\n    \"\"\"\n    self.name = name\n    self.representation_id = representation_id or uuid.uuid4().hex\n    self._data = {}\n    if traits:\n        for trait in traits:\n            self.add_trait(trait)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/representation.html#client.ayon_core.pipeline.traits.representation.Representation.__iter__","title":"<code>__iter__()</code>","text":"<p>Return the trait ID iterator.</p> Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>def __iter__(self):\n    \"\"\"Return the trait ID iterator.\"\"\"\n    return iter(self._data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/representation.html#client.ayon_core.pipeline.traits.representation.Representation.__len__","title":"<code>__len__()</code>","text":"<p>Return the length of the data.</p> Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>def __len__(self):\n    \"\"\"Return the length of the data.\"\"\"\n    return len(self._data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/representation.html#client.ayon_core.pipeline.traits.representation.Representation.__setitem__","title":"<code>__setitem__(key, value)</code>","text":"<p>Set the trait by ID.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>Trait ID.</p> required <code>value</code> <code>TraitBase</code> <p>Trait instance.</p> required Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>def __setitem__(self, key: str, value: T) -&gt; None:\n    \"\"\"Set the trait by ID.\n\n    Args:\n        key (str): Trait ID.\n        value (TraitBase): Trait instance.\n\n    \"\"\"\n    with contextlib.suppress(KeyError):\n        self._data.pop(key)\n\n    self.add_trait(value)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/representation.html#client.ayon_core.pipeline.traits.representation.Representation.__str__","title":"<code>__str__()</code>","text":"<p>Return the representation name.</p> Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>def __str__(self):\n    \"\"\"Return the representation name.\"\"\"\n    return self.name\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/representation.html#client.ayon_core.pipeline.traits.representation.Representation.add_trait","title":"<code>add_trait(trait, *, exists_ok=False)</code>","text":"<p>Add a trait to the Representation.</p> <p>Parameters:</p> Name Type Description Default <code>trait</code> <code>TraitBase</code> <p>Trait to add.</p> required <code>exists_ok</code> <code>bool</code> <p>If True, do not raise an error if the trait already exists. Defaults to False.</p> <code>False</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the trait ID is not provided, or the trait already exists.</p> Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>def add_trait(self, trait: T, *, exists_ok: bool = False) -&gt; None:\n    \"\"\"Add a trait to the Representation.\n\n    Args:\n        trait (TraitBase): Trait to add.\n        exists_ok (bool, optional): If True, do not raise an error if the\n            trait already exists. Defaults to False.\n\n    Raises:\n        ValueError: If the trait ID is not provided, or the trait already\n            exists.\n\n    \"\"\"\n    if not hasattr(trait, \"id\"):\n        error_msg = f\"Invalid trait {trait} - ID is required.\"\n        raise ValueError(error_msg)\n    if trait.id in self._data and not exists_ok:\n        error_msg = f\"Trait with ID {trait.id} already exists.\"\n        raise ValueError(error_msg)\n    self._data[trait.id] = trait\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/representation.html#client.ayon_core.pipeline.traits.representation.Representation.add_traits","title":"<code>add_traits(traits, *, exists_ok=False)</code>","text":"<p>Add a list of traits to the Representation.</p> <p>Parameters:</p> Name Type Description Default <code>traits</code> <code>list[TraitBase]</code> <p>List of traits to add.</p> required <code>exists_ok</code> <code>bool</code> <p>If True, do not raise an error if the trait already exists. Defaults to False.</p> <code>False</code> Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>def add_traits(\n        self, traits: list[T], *, exists_ok: bool = False) -&gt; None:\n    \"\"\"Add a list of traits to the Representation.\n\n    Args:\n        traits (list[TraitBase]): List of traits to add.\n        exists_ok (bool, optional): If True, do not raise an error if the\n            trait already exists. Defaults to False.\n\n    \"\"\"\n    for trait in traits:\n        self.add_trait(trait, exists_ok=exists_ok)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/representation.html#client.ayon_core.pipeline.traits.representation.Representation.contains_trait","title":"<code>contains_trait(trait)</code>","text":"<p>Check if the trait exists in the Representation.</p> <p>Parameters:</p> Name Type Description Default <code>trait</code> <code>TraitBase</code> <p>Trait class.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if the trait exists, False otherwise.</p> Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>def contains_trait(self, trait: Type[T]) -&gt; bool:\n    \"\"\"Check if the trait exists in the Representation.\n\n    Args:\n        trait (TraitBase): Trait class.\n\n    Returns:\n        bool: True if the trait exists, False otherwise.\n\n    \"\"\"\n    return bool(self._data.get(str(trait.id)))\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/representation.html#client.ayon_core.pipeline.traits.representation.Representation.contains_trait_by_id","title":"<code>contains_trait_by_id(trait_id)</code>","text":"<p>Check if the trait exists using trait id.</p> <p>Parameters:</p> Name Type Description Default <code>trait_id</code> <code>str</code> <p>Trait ID.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if the trait exists, False otherwise.</p> Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>def contains_trait_by_id(self, trait_id: str) -&gt; bool:\n    \"\"\"Check if the trait exists using trait id.\n\n    Args:\n        trait_id (str): Trait ID.\n\n    Returns:\n        bool: True if the trait exists, False otherwise.\n\n    \"\"\"\n    return bool(self._data.get(trait_id))\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/representation.html#client.ayon_core.pipeline.traits.representation.Representation.contains_traits","title":"<code>contains_traits(traits)</code>","text":"<p>Check if the traits exist.</p> <p>Parameters:</p> Name Type Description Default <code>traits</code> <code>list[TraitBase]</code> <p>List of trait classes.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if all traits exist, False otherwise.</p> Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>def contains_traits(self, traits: list[Type[T]]) -&gt; bool:\n    \"\"\"Check if the traits exist.\n\n    Args:\n        traits (list[TraitBase], optional): List of trait classes.\n\n    Returns:\n        bool: True if all traits exist, False otherwise.\n\n    \"\"\"\n    return all(self.contains_trait(trait=trait) for trait in traits)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/representation.html#client.ayon_core.pipeline.traits.representation.Representation.contains_traits_by_id","title":"<code>contains_traits_by_id(trait_ids)</code>","text":"<p>Check if the traits exist by id.</p> <p>If no trait IDs or traits are provided, it will check if the representation has any traits.</p> <p>Parameters:</p> Name Type Description Default <code>trait_ids</code> <code>list[str]</code> <p>List of trait IDs.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if all traits exist, False otherwise.</p> Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>def contains_traits_by_id(self, trait_ids: list[str]) -&gt; bool:\n    \"\"\"Check if the traits exist by id.\n\n    If no trait IDs or traits are provided, it will check if the\n    representation has any traits.\n\n    Args:\n        trait_ids (list[str]): List of trait IDs.\n\n    Returns:\n        bool: True if all traits exist, False otherwise.\n\n    \"\"\"\n    return all(\n        self.contains_trait_by_id(trait_id) for trait_id in trait_ids\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/representation.html#client.ayon_core.pipeline.traits.representation.Representation.from_dict","title":"<code>from_dict(name, representation_id=None, trait_data=None)</code>  <code>classmethod</code>","text":"<p>Create a representation from a dictionary.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Representation name.</p> required <code>representation_id</code> <code>str</code> <p>Representation ID.</p> <code>None</code> <code>trait_data</code> <code>dict</code> <p>Representation data. Dictionary with keys as trait ids and values as trait data. Example::</p> <pre><code>{\n    \"ayon.2d.PixelBased.v1\": {\n        \"display_window_width\": 1920,\n        \"display_window_height\": 1080\n    },\n    \"ayon.2d.Planar.v1\": {\n        \"channels\": 3\n    }\n}\n</code></pre> <code>None</code> <p>Returns:</p> Name Type Description <code>Representation</code> <code>Representation</code> <p>Representation instance.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the trait model with ID is not found.</p> <code>TypeError</code> <p>If the trait data is not a dictionary.</p> <code>IncompatibleTraitVersionError</code> <p>If the trait version is incompatible</p> Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>@classmethod\ndef from_dict(\n        cls: Type[Representation],\n        name: str,\n        representation_id: Optional[str] = None,\n        trait_data: Optional[dict] = None) -&gt; Representation:\n    \"\"\"Create a representation from a dictionary.\n\n    Args:\n        name (str): Representation name.\n        representation_id (str, optional): Representation ID.\n        trait_data (dict): Representation data. Dictionary with keys\n            as trait ids and values as trait data. Example::\n\n                {\n                    \"ayon.2d.PixelBased.v1\": {\n                        \"display_window_width\": 1920,\n                        \"display_window_height\": 1080\n                    },\n                    \"ayon.2d.Planar.v1\": {\n                        \"channels\": 3\n                    }\n                }\n\n    Returns:\n        Representation: Representation instance.\n\n    Raises:\n        ValueError: If the trait model with ID is not found.\n        TypeError: If the trait data is not a dictionary.\n        IncompatibleTraitVersionError: If the trait version is incompatible\n\n    \"\"\"\n    if not trait_data:\n        trait_data = {}\n    traits = []\n    for trait_id, value in trait_data.items():\n        if not isinstance(value, dict):\n            msg = (\n                f\"Invalid trait data for trait ID {trait_id}. \"\n                \"Trait data must be a dictionary.\"\n            )\n            raise TypeError(msg)\n\n        try:\n            trait_class = cls.get_trait_class_by_trait_id(trait_id)\n        except UpgradableTraitError as e:\n            # we found a newer version of trait, we will upgrade the data\n            if hasattr(e.trait, \"upgrade\"):\n                traits.append(e.trait.upgrade(value))\n            else:\n                msg = (\n                    f\"Newer version of trait {e.trait.id} found \"\n                    f\"for requested {trait_id} but without \"\n                    \"upgrade method.\"\n                )\n                raise IncompatibleTraitVersionError(msg) from e\n        else:\n            if not trait_class:\n                error_msg = f\"Trait model with ID {trait_id} not found.\"\n                raise ValueError(error_msg)\n\n            traits.append(trait_class(**value))\n\n    return cls(\n        name=name, representation_id=representation_id, traits=traits)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/representation.html#client.ayon_core.pipeline.traits.representation.Representation.get_trait","title":"<code>get_trait(trait)</code>","text":"<p>Get a trait from the representation.</p> <p>Parameters:</p> Name Type Description Default <code>trait</code> <code>TraitBase</code> <p>Trait class.</p> required <p>Returns:</p> Name Type Description <code>TraitBase</code> <code>T</code> <p>Trait instance.</p> <p>Raises:</p> Type Description <code>MissingTraitError</code> <p>If the trait is not found.</p> Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>def get_trait(self, trait: Type[T]) -&gt; T:\n    \"\"\"Get a trait from the representation.\n\n    Args:\n        trait (TraitBase, optional): Trait class.\n\n    Returns:\n        TraitBase: Trait instance.\n\n    Raises:\n        MissingTraitError: If the trait is not found.\n\n    \"\"\"\n    try:\n        return self._data[str(trait.id)]\n    except KeyError as e:\n        msg = f\"Trait with ID {trait.id} not found.\"\n        raise MissingTraitError(msg) from e\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/representation.html#client.ayon_core.pipeline.traits.representation.Representation.get_trait_by_id","title":"<code>get_trait_by_id(trait_id)</code>","text":"<p>Get a trait from the representation by id.</p> <p>Parameters:</p> Name Type Description Default <code>trait_id</code> <code>str</code> <p>Trait ID.</p> required <p>Returns:</p> Name Type Description <code>TraitBase</code> <code>T</code> <p>Trait instance.</p> <p>Raises:</p> Type Description <code>MissingTraitError</code> <p>If the trait is not found.</p> Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>def get_trait_by_id(self, trait_id: str) -&gt; T:\n    # sourcery skip: use-named-expression\n    \"\"\"Get a trait from the representation by id.\n\n    Args:\n        trait_id (str): Trait ID.\n\n    Returns:\n        TraitBase: Trait instance.\n\n    Raises:\n        MissingTraitError: If the trait is not found.\n\n    \"\"\"\n    version = _get_version_from_id(trait_id)\n    if version:\n        try:\n            return self._data[trait_id]\n        except KeyError as e:\n            msg = f\"Trait with ID {trait_id} not found.\"\n            raise MissingTraitError(msg) from e\n\n    result = next(\n        (\n            self._data.get(trait_id)\n            for trait_id in self._data\n            if trait_id.startswith(trait_id)\n        ),\n        None,\n    )\n    if result is None:\n        msg = f\"Trait with ID {trait_id} not found.\"\n        raise MissingTraitError(msg)\n    return result\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/representation.html#client.ayon_core.pipeline.traits.representation.Representation.get_trait_class_by_trait_id","title":"<code>get_trait_class_by_trait_id(trait_id)</code>  <code>classmethod</code>","text":"<p>Get the trait class for the given trait ID.</p> <p>Parameters:</p> Name Type Description Default <code>trait_id</code> <code>str</code> <p>Trait ID.</p> required <p>Returns:</p> Name Type Description <code>Optional[Type[T]]</code> <p>type[TraitBase]: Trait class.</p> <code>None</code> <code>Optional[Type[T]]</code> <p>If the trait class is not found.</p> <p>Raises:</p> Type Description <code>IncompatibleTraitVersionError</code> <p>If the trait version is incompatible with the current version of the trait.</p> <code>UpgradableTraitError</code> <p>If the trait version is upgradable to a newer version.</p> Note <p><code>ignore[return-value]</code> can be removed in future versions of Python where the Generics support is better.</p> Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>@classmethod\ndef get_trait_class_by_trait_id(cls, trait_id: str) -&gt; Optional[Type[T]]:\n    \"\"\"Get the trait class for the given trait ID.\n\n    Args:\n        trait_id (str): Trait ID.\n\n    Returns:\n        type[TraitBase]: Trait class.\n        None: If the trait class is not found.\n\n    Raises:\n        IncompatibleTraitVersionError: If the trait version is incompatible\n            with the current version of the trait.\n        UpgradableTraitError: If the trait version is upgradable to a newer\n            version.\n\n    Note:\n        `ignore[return-value]` can be removed in future versions of Python\n        where the Generics support is better.\n\n    \"\"\"\n    # Iteratively collect all subclasses\n    trait_classes = set()\n    to_process = [TraitBase]\n\n    while to_process:\n        current = to_process.pop()\n        subclasses = current.__subclasses__()\n        trait_classes.update(subclasses)\n        to_process.extend(subclasses)\n\n    # 1. Search for the exact match\n    for trait_class in trait_classes:\n        if getattr(trait_class, \"id\", None) == trait_id:\n            return trait_class  # type: ignore[return-value]\n\n    # 2. Search for fuzzy matches (same base ID, different version)\n    req_version = _get_version_from_id(trait_id)\n    # Determine base ID (e.g., 'ayon.trait' from 'ayon.trait.v1')\n    base_id = trait_id\n    if req_version is not None:\n        base_id = re.sub(r\"\\.v\\d+$\", \"\", trait_id)\n\n    candidates = []\n    for trait_class in trait_classes:\n        t_id = getattr(trait_class, \"id\", \"\")\n        if not t_id:\n            continue\n\n        t_ver = _get_version_from_id(t_id)\n        t_base = t_id\n        if t_ver is not None:\n            t_base = re.sub(r\"\\.v\\d+$\", \"\", t_id)\n\n        if t_base == base_id:\n            candidates.append(\n                (trait_class, t_ver or 0)\n            )\n\n    if not candidates:\n        return None  # type: ignore[return-value]\n\n    # Find the highest version among candidates\n    found_trait, found_version = max(candidates, key=lambda x: x[1])\n\n    if req_version is None:\n        return found_trait  # type: ignore[return-value]\n\n    if found_version == 0:\n        msg = (\n            f\"Trait {found_trait.id} found with no version, \"\n            \"but requested version is specified.\"\n        )\n        raise IncompatibleTraitVersionError(msg)\n\n    if req_version &gt; found_version:\n        error_msg = (\n            f\"Requested trait version {req_version} is \"\n            f\"higher than the found trait version {found_version}.\"\n        )\n        raise IncompatibleTraitVersionError(error_msg)\n\n    if req_version &lt; found_version:\n        if hasattr(found_trait, \"upgrade\"):\n            error_msg = (\n                \"Requested trait version \"\n                f\"{req_version} is lower \"\n                f\"than the found trait version {found_version}.\"\n            )\n            error: UpgradableTraitError = UpgradableTraitError(error_msg)\n            error.trait = found_trait\n            raise error\n        # If the explicit version was requested, matches failed,\n        # and no upgrade path exists, we return None (effectively\n        # \"Not Found\" for thatversion)\n        return None  # type: ignore[return-value]\n\n    return found_trait  # type: ignore[return-value]\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/representation.html#client.ayon_core.pipeline.traits.representation.Representation.get_traits","title":"<code>get_traits(traits=None)</code>","text":"<p>Get a list of traits from the representation.</p> <p>If no trait IDs or traits are provided, all traits will be returned.</p> <p>Parameters:</p> Name Type Description Default <code>traits</code> <code>list[TraitBase]</code> <p>List of trait classes.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, T]</code> <p>Dictionary of traits.</p> Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>def get_traits(self,\n                 traits: Optional[list[Type[T]]] = None\n ) -&gt; dict[str, T]:\n    \"\"\"Get a list of traits from the representation.\n\n    If no trait IDs or traits are provided, all traits will be returned.\n\n    Args:\n        traits (list[TraitBase], optional): List of trait classes.\n\n    Returns:\n        dict: Dictionary of traits.\n\n    \"\"\"\n    result: dict[str, T] = {}\n    if not traits:\n        for trait_id in self._data:\n            result[trait_id] = self.get_trait_by_id(trait_id=trait_id)\n        return result\n\n    for trait in traits:\n        result[str(trait.id)] = self.get_trait(trait=trait)\n    return result\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/representation.html#client.ayon_core.pipeline.traits.representation.Representation.get_traits_by_ids","title":"<code>get_traits_by_ids(trait_ids)</code>","text":"<p>Get a list of traits from the representation by their id.</p> <p>If no trait IDs or traits are provided, all traits will be returned.</p> <p>Parameters:</p> Name Type Description Default <code>trait_ids</code> <code>list[str]</code> <p>List of trait IDs.</p> required <p>Returns:</p> Name Type Description <code>dict</code> <code>dict[str, T]</code> <p>Dictionary of traits.</p> Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>def get_traits_by_ids(self, trait_ids: list[str]) -&gt; dict[str, T]:\n    \"\"\"Get a list of traits from the representation by their id.\n\n    If no trait IDs or traits are provided, all traits will be returned.\n\n    Args:\n        trait_ids (list[str]): List of trait IDs.\n\n    Returns:\n        dict: Dictionary of traits.\n\n    \"\"\"\n    return {\n        trait_id: self.get_trait_by_id(trait_id)\n        for trait_id in trait_ids\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/representation.html#client.ayon_core.pipeline.traits.representation.Representation.has_traits","title":"<code>has_traits()</code>","text":"<p>Check if the Representation has any traits.</p> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if the Representation has any traits, False otherwise.</p> Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>def has_traits(self) -&gt; bool:\n    \"\"\"Check if the Representation has any traits.\n\n    Returns:\n        bool: True if the Representation has any traits, False otherwise.\n\n    \"\"\"\n    return bool(self._data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/representation.html#client.ayon_core.pipeline.traits.representation.Representation.items","title":"<code>items()</code>","text":"<p>Return the traits as items.</p> Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>def items(self) -&gt; ItemsView[str, T]:\n    \"\"\"Return the traits as items.\"\"\"\n    return ItemsView(self._data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/representation.html#client.ayon_core.pipeline.traits.representation.Representation.remove_trait","title":"<code>remove_trait(trait)</code>","text":"<p>Remove a trait from the data.</p> <p>Parameters:</p> Name Type Description Default <code>trait</code> <code>TraitBase</code> <p>Trait class.</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>If the trait is not found.</p> Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>def remove_trait(self, trait: Type[TraitBase]) -&gt; None:\n    \"\"\"Remove a trait from the data.\n\n    Args:\n        trait (TraitBase, optional): Trait class.\n\n    Raises:\n        ValueError: If the trait is not found.\n\n    \"\"\"\n    try:\n        self._data.pop(str(trait.id))\n    except KeyError as e:\n        error_msg = f\"Trait with ID {trait.id} not found.\"\n        raise ValueError(error_msg) from e\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/representation.html#client.ayon_core.pipeline.traits.representation.Representation.remove_trait_by_id","title":"<code>remove_trait_by_id(trait_id)</code>","text":"<p>Remove a trait from the data by its ID.</p> <p>Parameters:</p> Name Type Description Default <code>trait_id</code> <code>str</code> <p>Trait ID.</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>If the trait is not found.</p> Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>def remove_trait_by_id(self, trait_id: str) -&gt; None:\n    \"\"\"Remove a trait from the data by its ID.\n\n    Args:\n        trait_id (str): Trait ID.\n\n    Raises:\n        ValueError: If the trait is not found.\n\n    \"\"\"\n    try:\n        self._data.pop(trait_id)\n    except KeyError as e:\n        error_msg = f\"Trait with ID {trait_id} not found.\"\n        raise ValueError(error_msg) from e\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/representation.html#client.ayon_core.pipeline.traits.representation.Representation.remove_traits","title":"<code>remove_traits(traits)</code>","text":"<p>Remove a list of traits from the Representation.</p> <p>If no trait IDs or traits are provided, all traits will be removed.</p> <p>Parameters:</p> Name Type Description Default <code>traits</code> <code>list[TraitBase]</code> <p>List of trait classes.</p> required Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>def remove_traits(self, traits: list[Type[T]]) -&gt; None:\n    \"\"\"Remove a list of traits from the Representation.\n\n    If no trait IDs or traits are provided, all traits will be removed.\n\n    Args:\n        traits (list[TraitBase]): List of trait classes.\n\n    \"\"\"\n    if not traits:\n        self._data = {}\n        return\n\n    for trait in traits:\n        self.remove_trait(trait)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/representation.html#client.ayon_core.pipeline.traits.representation.Representation.remove_traits_by_id","title":"<code>remove_traits_by_id(trait_ids)</code>","text":"<p>Remove a list of traits from the Representation by their ID.</p> <p>If no trait IDs or traits are provided, all traits will be removed.</p> <p>Parameters:</p> Name Type Description Default <code>trait_ids</code> <code>list[str]</code> <p>List of trait IDs.</p> required Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>def remove_traits_by_id(self, trait_ids: list[str]) -&gt; None:\n    \"\"\"Remove a list of traits from the Representation by their ID.\n\n    If no trait IDs or traits are provided, all traits will be removed.\n\n    Args:\n        trait_ids (list[str], optional): List of trait IDs.\n\n    \"\"\"\n    for trait_id in trait_ids:\n        self.remove_trait_by_id(trait_id)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/representation.html#client.ayon_core.pipeline.traits.representation.Representation.traits_as_dict","title":"<code>traits_as_dict()</code>","text":"<p>Return the traits from Representation data as a dictionary.</p> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict</code> <p>Traits data dictionary.</p> Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>def traits_as_dict(self) -&gt; dict:\n    \"\"\"Return the traits from Representation data as a dictionary.\n\n    Returns:\n        dict: Traits data dictionary.\n\n    \"\"\"\n    return {\n        trait_id: trait.as_dict()\n        for trait_id, trait in self._data.items()\n        if trait and trait_id\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/representation.html#client.ayon_core.pipeline.traits.representation.Representation.validate","title":"<code>validate()</code>","text":"<p>Validate the representation.</p> <p>This method will validate all the traits in the representation.</p> <p>Raises:</p> Type Description <code>TraitValidationError</code> <p>If the trait is invalid within representation</p> Source code in <code>client/ayon_core/pipeline/traits/representation.py</code> <pre><code>def validate(self) -&gt; None:\n    \"\"\"Validate the representation.\n\n    This method will validate all the traits in the representation.\n\n    Raises:\n        TraitValidationError: If the trait is invalid within representation\n\n    \"\"\"\n    errors = []\n    for trait in self._data.values():\n        # we do this in the loop to catch all the errors\n        try:\n            trait.validate_trait(self)\n        except TraitValidationError as e:  # noqa: PERF203\n            errors.append(str(e))\n    if errors:\n        msg = \"\\n\".join(errors)\n        scope = self.name\n        raise TraitValidationError(scope, msg)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/temporal.html","title":"temporal","text":"<p>Temporal (time related) traits.</p>"},{"location":"autoapi/client/ayon_core/pipeline/traits/temporal.html#client.ayon_core.pipeline.traits.temporal.FrameRanged","title":"<code>FrameRanged</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>Frame ranged trait model.</p> <p>Model representing a frame-ranged trait.</p> <p>Sync with OpenAssetIO MediaCreation Traits. For compatibility with OpenAssetIO, we'll need to handle different names of attributes:</p> <pre><code>* frame_start -&gt; start_frame\n* frame_end -&gt; end_frame\n...\n</code></pre> frames_per_second is a string to allow various precision <p>formats. FPS is a floating point number, but it can be also represented as a fraction (e.g. \"30000/1001\") or as a decimal or even as an irrational number. We need to support all these formats. To work with FPS, we'll need some helper function to convert FPS to Decimal from string.</p> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Trait name.</p> <code>description</code> <code>str</code> <p>Trait description.</p> <code>id</code> <code>str</code> <p>id should be a namespaced trait name with a version</p> <code>frame_start</code> <code>int</code> <p>Frame start.</p> <code>frame_end</code> <code>int</code> <p>Frame end.</p> <code>frame_in</code> <code>int</code> <p>Frame in.</p> <code>frame_out</code> <code>int</code> <p>Frame out.</p> <code>frames_per_second</code> <code>str</code> <p>Frames per second.</p> <code>step</code> <code>int</code> <p>Step.</p> Source code in <code>client/ayon_core/pipeline/traits/temporal.py</code> <pre><code>@dataclass\nclass FrameRanged(TraitBase):\n    \"\"\"Frame ranged trait model.\n\n    Model representing a frame-ranged trait.\n\n    Sync with OpenAssetIO MediaCreation Traits. For compatibility with\n    OpenAssetIO, we'll need to handle different names of attributes:\n\n        * frame_start -&gt; start_frame\n        * frame_end -&gt; end_frame\n        ...\n\n    Note: frames_per_second is a string to allow various precision\n        formats. FPS is a floating point number, but it can be also\n        represented as a fraction (e.g. \"30000/1001\") or as a decimal\n        or even as an irrational number. We need to support all these\n        formats. To work with FPS, we'll need some helper function\n        to convert FPS to Decimal from string.\n\n    Attributes:\n        name (str): Trait name.\n        description (str): Trait description.\n        id (str): id should be a namespaced trait name with a version\n        frame_start (int): Frame start.\n        frame_end (int): Frame end.\n        frame_in (int): Frame in.\n        frame_out (int): Frame out.\n        frames_per_second (str): Frames per second.\n        step (int): Step.\n    \"\"\"\n\n    name: ClassVar[str] = \"FrameRanged\"\n    description: ClassVar[str] = \"Frame Ranged Trait\"\n    id: ClassVar[str] = \"ayon.time.FrameRanged.v1\"\n    persistent: ClassVar[bool] = True\n    frame_start: int\n    frame_end: int\n    frame_in: Optional[int] = None\n    frame_out: Optional[int] = None\n    frames_per_second: str = None\n    step: Optional[int] = None\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/temporal.html#client.ayon_core.pipeline.traits.temporal.GapPolicy","title":"<code>GapPolicy</code>","text":"<p>               Bases: <code>Enum</code></p> <p>Gap policy enumeration.</p> <p>This type defines how to handle gaps in a sequence.</p> <p>Attributes:</p> Name Type Description <code>forbidden</code> <code>int</code> <p>Gaps are forbidden.</p> <code>missing</code> <code>int</code> <p>Gaps are interpreted as missing frames.</p> <code>hold</code> <code>int</code> <p>Gaps are interpreted as hold frames (last existing frames).</p> <code>black</code> <code>int</code> <p>Gaps are interpreted as black frames.</p> Source code in <code>client/ayon_core/pipeline/traits/temporal.py</code> <pre><code>class GapPolicy(Enum):\n    \"\"\"Gap policy enumeration.\n\n    This type defines how to handle gaps in a sequence.\n\n    Attributes:\n        forbidden (int): Gaps are forbidden.\n        missing (int): Gaps are interpreted as missing frames.\n        hold (int): Gaps are interpreted as hold frames (last existing frames).\n        black (int): Gaps are interpreted as black frames.\n    \"\"\"\n\n    forbidden = auto()\n    missing = auto()\n    hold = auto()\n    black = auto()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/temporal.html#client.ayon_core.pipeline.traits.temporal.Handles","title":"<code>Handles</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>Handles trait model.</p> <p>Handles define the range of frames that are included or excluded from the sequence.</p> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Trait name.</p> <code>description</code> <code>str</code> <p>Trait description.</p> <code>id</code> <code>str</code> <p>id should be a namespaced trait name with a version</p> <code>inclusive</code> <code>bool</code> <p>Handles are inclusive.</p> <code>frame_start_handle</code> <code>int</code> <p>Frame start handle.</p> <code>frame_end_handle</code> <code>int</code> <p>Frame end handle.</p> Source code in <code>client/ayon_core/pipeline/traits/temporal.py</code> <pre><code>@dataclass\nclass Handles(TraitBase):\n    \"\"\"Handles trait model.\n\n    Handles define the range of frames that are included or excluded\n    from the sequence.\n\n    Attributes:\n        name (str): Trait name.\n        description (str): Trait description.\n        id (str): id should be a namespaced trait name with a version\n        inclusive (bool): Handles are inclusive.\n        frame_start_handle (int): Frame start handle.\n        frame_end_handle (int): Frame end handle.\n    \"\"\"\n\n    name: ClassVar[str] = \"Handles\"\n    description: ClassVar[str] = \"Handles Trait\"\n    id: ClassVar[str] = \"ayon.time.Handles.v1\"\n    persistent: ClassVar[bool] = True\n    inclusive: Optional[bool] = False\n    frame_start_handle: Optional[int] = None\n    frame_end_handle: Optional[int] = None\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/temporal.html#client.ayon_core.pipeline.traits.temporal.SMPTETimecode","title":"<code>SMPTETimecode</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>SMPTE Timecode trait model.</p> <p>Attributes:</p> Name Type Description <code>timecode</code> <code>str</code> <p>SMPTE Timecode HH:MM:SS:FF</p> Source code in <code>client/ayon_core/pipeline/traits/temporal.py</code> <pre><code>@dataclass\nclass SMPTETimecode(TraitBase):\n    \"\"\"SMPTE Timecode trait model.\n\n    Attributes:\n        timecode (str): SMPTE Timecode HH:MM:SS:FF\n    \"\"\"\n\n    name: ClassVar[str] = \"Timecode\"\n    description: ClassVar[str] = \"SMPTE Timecode Trait\"\n    id: ClassVar[str] = \"ayon.time.SMPTETimecode.v1\"\n    persistent: ClassVar[bool] = True\n    timecode: str\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/temporal.html#client.ayon_core.pipeline.traits.temporal.Sequence","title":"<code>Sequence</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>Sequence trait model.</p> <p>This model represents a sequence trait. Based on the FrameRanged trait and Handles, adding support for gaps policy, frame padding and frame list specification. Regex is used to match frame numbers.</p> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Trait name.</p> <code>description</code> <code>str</code> <p>Trait description.</p> <code>id</code> <code>str</code> <p>id should be a namespaced trait name with a version</p> <code>gaps_policy</code> <code>GapPolicy</code> <p>Gaps policy - how to handle gaps in sequence.</p> <code>frame_padding</code> <code>int</code> <p>Frame padding.</p> <code>frame_regex</code> <code>str</code> <p>Frame regex - regular expression to match frame numbers. Must include 'index' named group and 'padding' named group.</p> <code>frame_spec</code> <code>str</code> <p>Frame list specification of frames. This takes string like \"1-10,20-30,40-50\" etc.</p> Source code in <code>client/ayon_core/pipeline/traits/temporal.py</code> <pre><code>@dataclass\nclass Sequence(TraitBase):\n    \"\"\"Sequence trait model.\n\n    This model represents a sequence trait. Based on the FrameRanged trait\n    and Handles, adding support for gaps policy, frame padding and frame\n    list specification. Regex is used to match frame numbers.\n\n    Attributes:\n        name (str): Trait name.\n        description (str): Trait description.\n        id (str): id should be a namespaced trait name with a version\n        gaps_policy (GapPolicy): Gaps policy - how to handle gaps in\n            sequence.\n        frame_padding (int): Frame padding.\n        frame_regex (str): Frame regex - regular expression to match\n            frame numbers. Must include 'index' named group and 'padding'\n            named group.\n        frame_spec (str): Frame list specification of frames. This takes\n            string like \"1-10,20-30,40-50\" etc.\n    \"\"\"\n\n    name: ClassVar[str] = \"Sequence\"\n    description: ClassVar[str] = \"Sequence Trait Model\"\n    id: ClassVar[str] = \"ayon.time.Sequence.v1\"\n    persistent: ClassVar[bool] = True\n    frame_padding: int\n    gaps_policy: Optional[GapPolicy] = GapPolicy.forbidden\n    frame_regex: Optional[Pattern] = None\n    frame_spec: Optional[str] = None\n\n    @classmethod\n    def validate_frame_regex(\n        cls, v: Optional[Pattern]\n    ) -&gt; Optional[Pattern]:\n        \"\"\"Validate frame regex.\n\n        Frame regex must have index and padding named groups.\n\n        Returns:\n            Optional[Pattern]: Compiled regex pattern.\n\n        Raises:\n            ValueError: If frame regex does not include 'index' and 'padding'\n\n        \"\"\"\n        if v is None:\n            return v\n        if v and any(s not in v.pattern for s in [\"?P&lt;index&gt;\", \"?P&lt;padding&gt;\"]):\n            msg = \"Frame regex must include 'index' and `padding named groups\"\n            raise ValueError(msg)\n        return v\n\n    def validate_trait(self, representation: Representation) -&gt; None:\n        \"\"\"Validate the trait.\"\"\"\n        super().validate_trait(representation)\n\n        # if there is a FileLocations trait, run validation\n        # on it as well\n\n        with contextlib.suppress(MissingTraitError):\n            self._validate_file_locations(representation)\n\n    def _validate_file_locations(self, representation: Representation) -&gt; None:\n        \"\"\"Validate file locations trait.\n\n        If along with the Sequence trait, there is a FileLocations trait,\n        then we need to validate if the file locations match the frame\n        list specification.\n\n        Args:\n            representation (Representation): Representation instance.\n\n        \"\"\"\n        from .content import FileLocations\n        file_locs: FileLocations = representation.get_trait(\n            FileLocations)\n        # Validate if the file locations on representation\n        # match the frame list (if any).\n        # We need to extend the expected frames with Handles.\n        frame_start = None\n        frame_end = None\n        handles_frame_start = None\n        handles_frame_end = None\n        with contextlib.suppress(MissingTraitError):\n            handles: Handles = representation.get_trait(Handles)\n            # if handles are inclusive, they should be already\n            #  accounted for in the FrameRaged frame spec\n            if not handles.inclusive:\n                handles_frame_start = handles.frame_start_handle\n                handles_frame_end = handles.frame_end_handle\n        with contextlib.suppress(MissingTraitError):\n            frame_ranged: FrameRanged = representation.get_trait(\n                FrameRanged)\n            frame_start = frame_ranged.frame_start\n            frame_end = frame_ranged.frame_end\n        if self.frame_spec is not None:\n            self.validate_frame_list(\n                file_locs,\n                frame_start,\n                frame_end,\n                handles_frame_start,\n                handles_frame_end)\n\n        self.validate_frame_padding(file_locs)\n\n    def validate_frame_list(\n            self,\n            file_locations: FileLocations,\n            frame_start: Optional[int] = None,\n            frame_end: Optional[int] = None,\n            handles_frame_start: Optional[int] = None,\n            handles_frame_end: Optional[int] = None) -&gt; None:\n        \"\"\"Validate a frame list.\n\n        This will take FileLocations trait and validate if the\n        file locations match the frame list specification.\n\n        For example, if the frame list is \"1-10,20-30,40-50\", then\n        the frame numbers in the file locations should match\n        these frames.\n\n        It will skip the validation if the frame list is not provided.\n\n        Args:\n            file_locations (FileLocations): File locations trait.\n            frame_start (Optional[int]): Frame start.\n            frame_end (Optional[int]): Frame end.\n            handles_frame_start (Optional[int]): Frame start handle.\n            handles_frame_end (Optional[int]): Frame end handle.\n\n        Raises:\n            TraitValidationError: If the frame list does not match\n                the expected frames.\n\n        \"\"\"\n        if self.frame_spec is None:\n            return\n\n        frames: list[int] = []\n        if self.frame_regex:\n            frames = self.get_frame_list(\n                    file_locations, self.frame_regex)\n        else:\n            frames = self.get_frame_list(\n                    file_locations)\n\n        expected_frames = self.list_spec_to_frames(self.frame_spec)\n        if frame_start is None or frame_end is None:\n            if min(expected_frames) != frame_start:\n                msg = (\n                    \"Frame start does not match the expected frame start. \"\n                    f\"Expected: {frame_start}, Found: {min(expected_frames)}\"\n                )\n                raise TraitValidationError(self.name, msg)\n\n            if max(expected_frames) != frame_end:\n                msg = (\n                    \"Frame end does not match the expected frame end. \"\n                    f\"Expected: {frame_end}, Found: {max(expected_frames)}\"\n                )\n                raise TraitValidationError(self.name, msg)\n\n        # we need to extend the expected frames with Handles\n        if handles_frame_start is not None:\n            expected_frames.extend(\n                range(\n                    min(frames) - handles_frame_start, min(frames) + 1))\n\n        if handles_frame_end is not None:\n            expected_frames.extend(\n                range(\n                    max(frames), max(frames) + handles_frame_end + 1))\n\n        if set(frames) != set(expected_frames):\n            msg = (\n                \"Frame list does not match the expected frames. \"\n                f\"Expected: {expected_frames}, Found: {frames}\"\n            )\n            raise TraitValidationError(self.name, msg)\n\n    def validate_frame_padding(\n            self, file_locations: FileLocations) -&gt; None:\n        \"\"\"Validate frame padding.\n\n        This will take FileLocations trait and validate if the\n        frame padding matches the expected frame padding.\n\n        Args:\n            file_locations (FileLocations): File locations trait.\n\n        Raises:\n            TraitValidationError: If frame padding does not match\n                the expected frame padding.\n\n        \"\"\"\n        expected_padding = self.get_frame_padding(file_locations)\n        if self.frame_padding != expected_padding:\n            msg = (\n                \"Frame padding does not match the expected frame padding. \"\n                f\"Expected: {expected_padding}, Found: {self.frame_padding}\"\n            )\n            raise TraitValidationError(self.name, msg)\n\n    @staticmethod\n    def list_spec_to_frames(list_spec: str) -&gt; list[int]:\n        \"\"\"Convert list specification to frames.\n\n        Returns:\n            list[int]: List of frame numbers.\n\n        Raises:\n            ValueError: If invalid frame number in the list.\n\n        \"\"\"\n        frames = []\n        segments = list_spec.split(\",\")\n        for segment in segments:\n            ranges = segment.split(\"-\")\n            if len(ranges) == 1:\n                if not ranges[0].isdigit():\n                    msg = (\n                        \"Invalid frame number \"\n                        f\"in the list: {ranges[0]}\"\n                    )\n                    raise ValueError(msg)\n                frames.append(int(ranges[0]))\n                continue\n            start, end = segment.split(\"-\")\n            frames.extend(range(int(start), int(end) + 1))\n        return frames\n\n    @staticmethod\n    def _get_collection(\n        file_locations: FileLocations,\n        regex: Optional[Pattern] = None) -&gt; clique.Collection:\n        r\"\"\"Get the collection from file locations.\n\n        Args:\n            file_locations (FileLocations): File locations trait.\n            regex (Optional[Pattern]): Regular expression to match\n                frame numbers. This is passed to ``clique.assemble()``.\n                Default clique pattern is::\n\n                    \\.(?P&lt;index&gt;(?P&lt;padding&gt;0*)\\d+)\\.\\D+\\d?$\n\n        Returns:\n            clique.Collection: Collection instance.\n\n        Raises:\n            ValueError: If zero or multiple of collections are found.\n\n        \"\"\"\n        patterns = [regex] if regex else None\n        files: list[str] = [\n            file.file_path.as_posix()\n            for file in file_locations.file_paths\n        ]\n        src_collections, _ = clique.assemble(files, patterns=patterns)\n        if len(src_collections) != 1:\n            msg = (\n                f\"Zero or multiple collections found: {len(src_collections)} \"\n                \"expected 1\"\n            )\n            raise ValueError(msg)\n        return src_collections[0]\n\n    @staticmethod\n    def get_frame_padding(file_locations: FileLocations) -&gt; int:\n        \"\"\"Get frame padding.\n\n        Returns:\n            int: Frame padding.\n\n        \"\"\"\n        src_collection = Sequence._get_collection(file_locations)\n        padding = src_collection.padding\n        # sometimes Clique doesn't get the padding right, so\n        # we need to calculate it manually\n        if padding == 0:\n            padding = len(str(max(src_collection.indexes)))\n\n        return padding\n\n    @staticmethod\n    def get_frame_list(\n            file_locations: FileLocations,\n            regex: Optional[Pattern] = None,\n        ) -&gt; list[int]:\n        r\"\"\"Get the frame list.\n\n        Args:\n            file_locations (FileLocations): File locations trait.\n            regex (Optional[Pattern]): Regular expression to match\n                frame numbers. This is passed to ``clique.assemble()``.\n                Default clique pattern is::\n\n                    \\.(?P&lt;index&gt;(?P&lt;padding&gt;0*)\\d+)\\.\\D+\\d?$\n\n        Returns:\n            list[int]: List of frame numbers.\n\n        \"\"\"\n        src_collection = Sequence._get_collection(file_locations, regex)\n        return list(src_collection.indexes)\n\n    def get_frame_pattern(self) -&gt; Pattern:\n        \"\"\"Get frame regex as a pattern.\n\n        If the regex is a string, it will compile it to the pattern.\n\n        Returns:\n            Pattern: Compiled regex pattern.\n\n        \"\"\"\n        if self.frame_regex:\n            if isinstance(self.frame_regex, str):\n                return re.compile(self.frame_regex)\n            return self.frame_regex\n        return re.compile(\n            r\"\\.(?P&lt;index&gt;(?P&lt;padding&gt;0*)\\d+)\\.\\D+\\d?$\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/temporal.html#client.ayon_core.pipeline.traits.temporal.Sequence.get_frame_list","title":"<code>get_frame_list(file_locations, regex=None)</code>  <code>staticmethod</code>","text":"<p>Get the frame list.</p> <p>Parameters:</p> Name Type Description Default <code>file_locations</code> <code>FileLocations</code> <p>File locations trait.</p> required <code>regex</code> <code>Optional[Pattern]</code> <p>Regular expression to match frame numbers. This is passed to <code>clique.assemble()</code>. Default clique pattern is::</p> <pre><code>\\.(?P&lt;index&gt;(?P&lt;padding&gt;0*)\\d+)\\.\\D+\\d?$\n</code></pre> <code>None</code> <p>Returns:</p> Type Description <code>list[int]</code> <p>list[int]: List of frame numbers.</p> Source code in <code>client/ayon_core/pipeline/traits/temporal.py</code> <pre><code>@staticmethod\ndef get_frame_list(\n        file_locations: FileLocations,\n        regex: Optional[Pattern] = None,\n    ) -&gt; list[int]:\n    r\"\"\"Get the frame list.\n\n    Args:\n        file_locations (FileLocations): File locations trait.\n        regex (Optional[Pattern]): Regular expression to match\n            frame numbers. This is passed to ``clique.assemble()``.\n            Default clique pattern is::\n\n                \\.(?P&lt;index&gt;(?P&lt;padding&gt;0*)\\d+)\\.\\D+\\d?$\n\n    Returns:\n        list[int]: List of frame numbers.\n\n    \"\"\"\n    src_collection = Sequence._get_collection(file_locations, regex)\n    return list(src_collection.indexes)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/temporal.html#client.ayon_core.pipeline.traits.temporal.Sequence.get_frame_padding","title":"<code>get_frame_padding(file_locations)</code>  <code>staticmethod</code>","text":"<p>Get frame padding.</p> <p>Returns:</p> Name Type Description <code>int</code> <code>int</code> <p>Frame padding.</p> Source code in <code>client/ayon_core/pipeline/traits/temporal.py</code> <pre><code>@staticmethod\ndef get_frame_padding(file_locations: FileLocations) -&gt; int:\n    \"\"\"Get frame padding.\n\n    Returns:\n        int: Frame padding.\n\n    \"\"\"\n    src_collection = Sequence._get_collection(file_locations)\n    padding = src_collection.padding\n    # sometimes Clique doesn't get the padding right, so\n    # we need to calculate it manually\n    if padding == 0:\n        padding = len(str(max(src_collection.indexes)))\n\n    return padding\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/temporal.html#client.ayon_core.pipeline.traits.temporal.Sequence.get_frame_pattern","title":"<code>get_frame_pattern()</code>","text":"<p>Get frame regex as a pattern.</p> <p>If the regex is a string, it will compile it to the pattern.</p> <p>Returns:</p> Name Type Description <code>Pattern</code> <code>Pattern</code> <p>Compiled regex pattern.</p> Source code in <code>client/ayon_core/pipeline/traits/temporal.py</code> <pre><code>def get_frame_pattern(self) -&gt; Pattern:\n    \"\"\"Get frame regex as a pattern.\n\n    If the regex is a string, it will compile it to the pattern.\n\n    Returns:\n        Pattern: Compiled regex pattern.\n\n    \"\"\"\n    if self.frame_regex:\n        if isinstance(self.frame_regex, str):\n            return re.compile(self.frame_regex)\n        return self.frame_regex\n    return re.compile(\n        r\"\\.(?P&lt;index&gt;(?P&lt;padding&gt;0*)\\d+)\\.\\D+\\d?$\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/temporal.html#client.ayon_core.pipeline.traits.temporal.Sequence.list_spec_to_frames","title":"<code>list_spec_to_frames(list_spec)</code>  <code>staticmethod</code>","text":"<p>Convert list specification to frames.</p> <p>Returns:</p> Type Description <code>list[int]</code> <p>list[int]: List of frame numbers.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If invalid frame number in the list.</p> Source code in <code>client/ayon_core/pipeline/traits/temporal.py</code> <pre><code>@staticmethod\ndef list_spec_to_frames(list_spec: str) -&gt; list[int]:\n    \"\"\"Convert list specification to frames.\n\n    Returns:\n        list[int]: List of frame numbers.\n\n    Raises:\n        ValueError: If invalid frame number in the list.\n\n    \"\"\"\n    frames = []\n    segments = list_spec.split(\",\")\n    for segment in segments:\n        ranges = segment.split(\"-\")\n        if len(ranges) == 1:\n            if not ranges[0].isdigit():\n                msg = (\n                    \"Invalid frame number \"\n                    f\"in the list: {ranges[0]}\"\n                )\n                raise ValueError(msg)\n            frames.append(int(ranges[0]))\n            continue\n        start, end = segment.split(\"-\")\n        frames.extend(range(int(start), int(end) + 1))\n    return frames\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/temporal.html#client.ayon_core.pipeline.traits.temporal.Sequence.validate_frame_list","title":"<code>validate_frame_list(file_locations, frame_start=None, frame_end=None, handles_frame_start=None, handles_frame_end=None)</code>","text":"<p>Validate a frame list.</p> <p>This will take FileLocations trait and validate if the file locations match the frame list specification.</p> <p>For example, if the frame list is \"1-10,20-30,40-50\", then the frame numbers in the file locations should match these frames.</p> <p>It will skip the validation if the frame list is not provided.</p> <p>Parameters:</p> Name Type Description Default <code>file_locations</code> <code>FileLocations</code> <p>File locations trait.</p> required <code>frame_start</code> <code>Optional[int]</code> <p>Frame start.</p> <code>None</code> <code>frame_end</code> <code>Optional[int]</code> <p>Frame end.</p> <code>None</code> <code>handles_frame_start</code> <code>Optional[int]</code> <p>Frame start handle.</p> <code>None</code> <code>handles_frame_end</code> <code>Optional[int]</code> <p>Frame end handle.</p> <code>None</code> <p>Raises:</p> Type Description <code>TraitValidationError</code> <p>If the frame list does not match the expected frames.</p> Source code in <code>client/ayon_core/pipeline/traits/temporal.py</code> <pre><code>def validate_frame_list(\n        self,\n        file_locations: FileLocations,\n        frame_start: Optional[int] = None,\n        frame_end: Optional[int] = None,\n        handles_frame_start: Optional[int] = None,\n        handles_frame_end: Optional[int] = None) -&gt; None:\n    \"\"\"Validate a frame list.\n\n    This will take FileLocations trait and validate if the\n    file locations match the frame list specification.\n\n    For example, if the frame list is \"1-10,20-30,40-50\", then\n    the frame numbers in the file locations should match\n    these frames.\n\n    It will skip the validation if the frame list is not provided.\n\n    Args:\n        file_locations (FileLocations): File locations trait.\n        frame_start (Optional[int]): Frame start.\n        frame_end (Optional[int]): Frame end.\n        handles_frame_start (Optional[int]): Frame start handle.\n        handles_frame_end (Optional[int]): Frame end handle.\n\n    Raises:\n        TraitValidationError: If the frame list does not match\n            the expected frames.\n\n    \"\"\"\n    if self.frame_spec is None:\n        return\n\n    frames: list[int] = []\n    if self.frame_regex:\n        frames = self.get_frame_list(\n                file_locations, self.frame_regex)\n    else:\n        frames = self.get_frame_list(\n                file_locations)\n\n    expected_frames = self.list_spec_to_frames(self.frame_spec)\n    if frame_start is None or frame_end is None:\n        if min(expected_frames) != frame_start:\n            msg = (\n                \"Frame start does not match the expected frame start. \"\n                f\"Expected: {frame_start}, Found: {min(expected_frames)}\"\n            )\n            raise TraitValidationError(self.name, msg)\n\n        if max(expected_frames) != frame_end:\n            msg = (\n                \"Frame end does not match the expected frame end. \"\n                f\"Expected: {frame_end}, Found: {max(expected_frames)}\"\n            )\n            raise TraitValidationError(self.name, msg)\n\n    # we need to extend the expected frames with Handles\n    if handles_frame_start is not None:\n        expected_frames.extend(\n            range(\n                min(frames) - handles_frame_start, min(frames) + 1))\n\n    if handles_frame_end is not None:\n        expected_frames.extend(\n            range(\n                max(frames), max(frames) + handles_frame_end + 1))\n\n    if set(frames) != set(expected_frames):\n        msg = (\n            \"Frame list does not match the expected frames. \"\n            f\"Expected: {expected_frames}, Found: {frames}\"\n        )\n        raise TraitValidationError(self.name, msg)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/temporal.html#client.ayon_core.pipeline.traits.temporal.Sequence.validate_frame_padding","title":"<code>validate_frame_padding(file_locations)</code>","text":"<p>Validate frame padding.</p> <p>This will take FileLocations trait and validate if the frame padding matches the expected frame padding.</p> <p>Parameters:</p> Name Type Description Default <code>file_locations</code> <code>FileLocations</code> <p>File locations trait.</p> required <p>Raises:</p> Type Description <code>TraitValidationError</code> <p>If frame padding does not match the expected frame padding.</p> Source code in <code>client/ayon_core/pipeline/traits/temporal.py</code> <pre><code>def validate_frame_padding(\n        self, file_locations: FileLocations) -&gt; None:\n    \"\"\"Validate frame padding.\n\n    This will take FileLocations trait and validate if the\n    frame padding matches the expected frame padding.\n\n    Args:\n        file_locations (FileLocations): File locations trait.\n\n    Raises:\n        TraitValidationError: If frame padding does not match\n            the expected frame padding.\n\n    \"\"\"\n    expected_padding = self.get_frame_padding(file_locations)\n    if self.frame_padding != expected_padding:\n        msg = (\n            \"Frame padding does not match the expected frame padding. \"\n            f\"Expected: {expected_padding}, Found: {self.frame_padding}\"\n        )\n        raise TraitValidationError(self.name, msg)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/temporal.html#client.ayon_core.pipeline.traits.temporal.Sequence.validate_frame_regex","title":"<code>validate_frame_regex(v)</code>  <code>classmethod</code>","text":"<p>Validate frame regex.</p> <p>Frame regex must have index and padding named groups.</p> <p>Returns:</p> Type Description <code>Optional[Pattern]</code> <p>Optional[Pattern]: Compiled regex pattern.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If frame regex does not include 'index' and 'padding'</p> Source code in <code>client/ayon_core/pipeline/traits/temporal.py</code> <pre><code>@classmethod\ndef validate_frame_regex(\n    cls, v: Optional[Pattern]\n) -&gt; Optional[Pattern]:\n    \"\"\"Validate frame regex.\n\n    Frame regex must have index and padding named groups.\n\n    Returns:\n        Optional[Pattern]: Compiled regex pattern.\n\n    Raises:\n        ValueError: If frame regex does not include 'index' and 'padding'\n\n    \"\"\"\n    if v is None:\n        return v\n    if v and any(s not in v.pattern for s in [\"?P&lt;index&gt;\", \"?P&lt;padding&gt;\"]):\n        msg = \"Frame regex must include 'index' and `padding named groups\"\n        raise ValueError(msg)\n    return v\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/temporal.html#client.ayon_core.pipeline.traits.temporal.Sequence.validate_trait","title":"<code>validate_trait(representation)</code>","text":"<p>Validate the trait.</p> Source code in <code>client/ayon_core/pipeline/traits/temporal.py</code> <pre><code>def validate_trait(self, representation: Representation) -&gt; None:\n    \"\"\"Validate the trait.\"\"\"\n    super().validate_trait(representation)\n\n    # if there is a FileLocations trait, run validation\n    # on it as well\n\n    with contextlib.suppress(MissingTraitError):\n        self._validate_file_locations(representation)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/temporal.html#client.ayon_core.pipeline.traits.temporal.Static","title":"<code>Static</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>Static time trait.</p> <p>Used to define static time (single frame).</p> Source code in <code>client/ayon_core/pipeline/traits/temporal.py</code> <pre><code>@dataclass\nclass Static(TraitBase):\n    \"\"\"Static time trait.\n\n    Used to define static time (single frame).\n    \"\"\"\n\n    name: ClassVar[str] = \"Static\"\n    description: ClassVar[str] = \"Static Time Trait\"\n    id: ClassVar[str] = \"ayon.time.Static.v1\"\n    persistent: ClassVar[bool] = True\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/three_dimensional.html","title":"three_dimensional","text":"<p>3D traits.</p>"},{"location":"autoapi/client/ayon_core/pipeline/traits/three_dimensional.html#client.ayon_core.pipeline.traits.three_dimensional.Geometry","title":"<code>Geometry</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>Geometry type trait model.</p> <p>Type trait for geometry data.</p> <p>Sync with OpenAssetIO MediaCreation Traits.</p> Source code in <code>client/ayon_core/pipeline/traits/three_dimensional.py</code> <pre><code>@dataclass\nclass Geometry(TraitBase):\n    \"\"\"Geometry type trait model.\n\n    Type trait for geometry data.\n\n    Sync with OpenAssetIO MediaCreation Traits.\n    \"\"\"\n\n    id: ClassVar[str] = \"ayon.3d.Geometry.v1\"\n    name: ClassVar[str] = \"Geometry\"\n    description: ClassVar[str] = \"Geometry trait model.\"\n    persistent: ClassVar[bool] = True\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/three_dimensional.html#client.ayon_core.pipeline.traits.three_dimensional.IESProfile","title":"<code>IESProfile</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>IES profile (IES-LM-64) type trait model.</p> <p>Sync with OpenAssetIO MediaCreation Traits.</p> Source code in <code>client/ayon_core/pipeline/traits/three_dimensional.py</code> <pre><code>@dataclass\nclass IESProfile(TraitBase):\n    \"\"\"IES profile (IES-LM-64) type trait model.\n\n    Sync with OpenAssetIO MediaCreation Traits.\n    \"\"\"\n\n    id: ClassVar[str] = \"ayon.3d.IESProfile.v1\"\n    name: ClassVar[str] = \"IESProfile\"\n    description: ClassVar[str] = \"IES profile trait model.\"\n    persistent: ClassVar[bool] = True\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/three_dimensional.html#client.ayon_core.pipeline.traits.three_dimensional.Lighting","title":"<code>Lighting</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>Lighting trait model.</p> <p>Type trait for lighting data.</p> <p>Sync with OpenAssetIO MediaCreation Traits.</p> Source code in <code>client/ayon_core/pipeline/traits/three_dimensional.py</code> <pre><code>@dataclass\nclass Lighting(TraitBase):\n    \"\"\"Lighting trait model.\n\n    Type trait for lighting data.\n\n    Sync with OpenAssetIO MediaCreation Traits.\n    \"\"\"\n\n    id: ClassVar[str] = \"ayon.3d.Lighting.v1\"\n    name: ClassVar[str] = \"Lighting\"\n    description: ClassVar[str] = \"Lighting trait model.\"\n    persistent: ClassVar[bool] = True\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/three_dimensional.html#client.ayon_core.pipeline.traits.three_dimensional.Shader","title":"<code>Shader</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>Shader trait model.</p> <p>Type trait for shader data.</p> <p>Sync with OpenAssetIO MediaCreation Traits.</p> Source code in <code>client/ayon_core/pipeline/traits/three_dimensional.py</code> <pre><code>@dataclass\nclass Shader(TraitBase):\n    \"\"\"Shader trait model.\n\n    Type trait for shader data.\n\n    Sync with OpenAssetIO MediaCreation Traits.\n    \"\"\"\n\n    id: ClassVar[str] = \"ayon.3d.Shader.v1\"\n    name: ClassVar[str] = \"Shader\"\n    description: ClassVar[str] = \"Shader trait model.\"\n    persistent: ClassVar[bool] = True\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/three_dimensional.html#client.ayon_core.pipeline.traits.three_dimensional.Spatial","title":"<code>Spatial</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>Spatial trait model.</p> <p>Trait describing spatial information. Up axis valid strings are \"Y\", \"Z\", \"X\". Handedness valid strings are \"left\", \"right\". Meters per unit is a float value.</p> <p>Example::</p> <pre><code>    Spatial(up_axis=\"Y\", handedness=\"right\", meters_per_unit=1.0)\n</code></pre> Todo <ul> <li>Add value validation for up_axis and handedness.</li> </ul> <p>Attributes:</p> Name Type Description <code>up_axis</code> <code>str</code> <p>Up axis.</p> <code>handedness</code> <code>str</code> <p>Handedness.</p> <code>meters_per_unit</code> <code>float</code> <p>Meters per unit.</p> Source code in <code>client/ayon_core/pipeline/traits/three_dimensional.py</code> <pre><code>@dataclass\nclass Spatial(TraitBase):\n    \"\"\"Spatial trait model.\n\n    Trait describing spatial information. Up axis valid strings are\n    \"Y\", \"Z\", \"X\". Handedness valid strings are \"left\", \"right\". Meters per\n    unit is a float value.\n\n    Example::\n\n            Spatial(up_axis=\"Y\", handedness=\"right\", meters_per_unit=1.0)\n\n    Todo:\n        * Add value validation for up_axis and handedness.\n\n    Attributes:\n        up_axis (str): Up axis.\n        handedness (str): Handedness.\n        meters_per_unit (float): Meters per unit.\n    \"\"\"\n\n    id: ClassVar[str] = \"ayon.3d.Spatial.v1\"\n    name: ClassVar[str] = \"Spatial\"\n    description: ClassVar[str] = \"Spatial trait model.\"\n    persistent: ClassVar[bool] = True\n    up_axis: str\n    handedness: str\n    meters_per_unit: float\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/trait.html","title":"trait","text":"<p>Defines the base trait model and representation.</p>"},{"location":"autoapi/client/ayon_core/pipeline/traits/trait.html#client.ayon_core.pipeline.traits.trait.IncompatibleTraitVersionError","title":"<code>IncompatibleTraitVersionError</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Incompatible trait version exception.</p> <p>This exception is raised when the trait version is incompatible with the current version of the trait.</p> Source code in <code>client/ayon_core/pipeline/traits/trait.py</code> <pre><code>class IncompatibleTraitVersionError(Exception):\n    \"\"\"Incompatible trait version exception.\n\n    This exception is raised when the trait version is incompatible with the\n    current version of the trait.\n    \"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/trait.html#client.ayon_core.pipeline.traits.trait.LooseMatchingTraitError","title":"<code>LooseMatchingTraitError</code>","text":"<p>               Bases: <code>Exception</code>, <code>Generic[T]</code></p> <p>Loose matching trait exception.</p> <p>This exception is raised when the trait is found with a loose matching criteria.</p> Source code in <code>client/ayon_core/pipeline/traits/trait.py</code> <pre><code>class LooseMatchingTraitError(Exception, Generic[T]):\n    \"\"\"Loose matching trait exception.\n\n    This exception is raised when the trait is found with a loose matching\n    criteria.\n    \"\"\"\n\n    found_trait: T\n    expected_id: str\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/trait.html#client.ayon_core.pipeline.traits.trait.MissingTraitError","title":"<code>MissingTraitError</code>","text":"<p>               Bases: <code>TypeError</code></p> <p>Missing trait error exception.</p> <p>This exception is raised when the trait is missing.</p> Source code in <code>client/ayon_core/pipeline/traits/trait.py</code> <pre><code>class MissingTraitError(TypeError):\n    \"\"\"Missing trait error exception.\n\n    This exception is raised when the trait is missing.\n    \"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/trait.html#client.ayon_core.pipeline.traits.trait.TraitBase","title":"<code>TraitBase</code>  <code>dataclass</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Base trait model.</p> <p>This model must be used as a base for all trait models. <code>id</code>, <code>name</code>, and <code>description</code> are abstract attributes that must be implemented in the derived classes.</p> Source code in <code>client/ayon_core/pipeline/traits/trait.py</code> <pre><code>@dataclass\nclass TraitBase(ABC):\n    \"\"\"Base trait model.\n\n    This model must be used as a base for all trait models.\n    ``id``, ``name``, and ``description`` are abstract attributes that must be\n    implemented in the derived classes.\n    \"\"\"\n\n    @property\n    @abstractmethod\n    def id(self) -&gt; str:\n        \"\"\"Abstract attribute for ID.\"\"\"\n        ...\n\n    @property\n    @abstractmethod\n    def name(self) -&gt; str:\n        \"\"\"Abstract attribute for name.\"\"\"\n        ...\n\n    @property\n    @abstractmethod\n    def description(self) -&gt; str:\n        \"\"\"Abstract attribute for description.\"\"\"\n        ...\n\n    def validate_trait(self, representation: Representation) -&gt; None:  # noqa: PLR6301\n        \"\"\"Validate the trait.\n\n        This method should be implemented in the derived classes to validate\n        the trait data. It can be used by traits to validate against other\n        traits in the representation.\n\n        Args:\n            representation (Representation): Representation instance.\n\n        \"\"\"\n        return\n\n    @classmethod\n    def get_version(cls) -&gt; Optional[int]:\n        # sourcery skip: use-named-expression\n        \"\"\"Get a trait version from ID.\n\n        This assumes Trait ID ends with `.v{version}`. If not, it will\n        return None.\n\n        Returns:\n            Optional[int]: Trait version\n\n        \"\"\"\n        version_regex = r\"v(\\d+)$\"\n        match = re.search(version_regex, str(cls.id))\n        return int(match[1]) if match else None\n\n    @classmethod\n    def get_versionless_id(cls) -&gt; str:\n        \"\"\"Get a trait ID without a version.\n\n        Returns:\n            str: Trait ID without a version.\n\n        \"\"\"\n        return re.sub(r\"\\.v\\d+$\", \"\", str(cls.id))\n\n    def as_dict(self) -&gt; dict:\n        \"\"\"Return a trait as a dictionary.\n\n        Returns:\n            dict: Trait as dictionary.\n\n        \"\"\"\n        return asdict(self)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/trait.html#client.ayon_core.pipeline.traits.trait.TraitBase.description","title":"<code>description</code>  <code>abstractmethod</code> <code>property</code>","text":"<p>Abstract attribute for description.</p>"},{"location":"autoapi/client/ayon_core/pipeline/traits/trait.html#client.ayon_core.pipeline.traits.trait.TraitBase.id","title":"<code>id</code>  <code>abstractmethod</code> <code>property</code>","text":"<p>Abstract attribute for ID.</p>"},{"location":"autoapi/client/ayon_core/pipeline/traits/trait.html#client.ayon_core.pipeline.traits.trait.TraitBase.name","title":"<code>name</code>  <code>abstractmethod</code> <code>property</code>","text":"<p>Abstract attribute for name.</p>"},{"location":"autoapi/client/ayon_core/pipeline/traits/trait.html#client.ayon_core.pipeline.traits.trait.TraitBase.as_dict","title":"<code>as_dict()</code>","text":"<p>Return a trait as a dictionary.</p> <p>Returns:</p> Name Type Description <code>dict</code> <code>dict</code> <p>Trait as dictionary.</p> Source code in <code>client/ayon_core/pipeline/traits/trait.py</code> <pre><code>def as_dict(self) -&gt; dict:\n    \"\"\"Return a trait as a dictionary.\n\n    Returns:\n        dict: Trait as dictionary.\n\n    \"\"\"\n    return asdict(self)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/trait.html#client.ayon_core.pipeline.traits.trait.TraitBase.get_version","title":"<code>get_version()</code>  <code>classmethod</code>","text":"<p>Get a trait version from ID.</p> <p>This assumes Trait ID ends with <code>.v{version}</code>. If not, it will return None.</p> <p>Returns:</p> Type Description <code>Optional[int]</code> <p>Optional[int]: Trait version</p> Source code in <code>client/ayon_core/pipeline/traits/trait.py</code> <pre><code>@classmethod\ndef get_version(cls) -&gt; Optional[int]:\n    # sourcery skip: use-named-expression\n    \"\"\"Get a trait version from ID.\n\n    This assumes Trait ID ends with `.v{version}`. If not, it will\n    return None.\n\n    Returns:\n        Optional[int]: Trait version\n\n    \"\"\"\n    version_regex = r\"v(\\d+)$\"\n    match = re.search(version_regex, str(cls.id))\n    return int(match[1]) if match else None\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/trait.html#client.ayon_core.pipeline.traits.trait.TraitBase.get_versionless_id","title":"<code>get_versionless_id()</code>  <code>classmethod</code>","text":"<p>Get a trait ID without a version.</p> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Trait ID without a version.</p> Source code in <code>client/ayon_core/pipeline/traits/trait.py</code> <pre><code>@classmethod\ndef get_versionless_id(cls) -&gt; str:\n    \"\"\"Get a trait ID without a version.\n\n    Returns:\n        str: Trait ID without a version.\n\n    \"\"\"\n    return re.sub(r\"\\.v\\d+$\", \"\", str(cls.id))\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/trait.html#client.ayon_core.pipeline.traits.trait.TraitBase.validate_trait","title":"<code>validate_trait(representation)</code>","text":"<p>Validate the trait.</p> <p>This method should be implemented in the derived classes to validate the trait data. It can be used by traits to validate against other traits in the representation.</p> <p>Parameters:</p> Name Type Description Default <code>representation</code> <code>Representation</code> <p>Representation instance.</p> required Source code in <code>client/ayon_core/pipeline/traits/trait.py</code> <pre><code>def validate_trait(self, representation: Representation) -&gt; None:  # noqa: PLR6301\n    \"\"\"Validate the trait.\n\n    This method should be implemented in the derived classes to validate\n    the trait data. It can be used by traits to validate against other\n    traits in the representation.\n\n    Args:\n        representation (Representation): Representation instance.\n\n    \"\"\"\n    return\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/trait.html#client.ayon_core.pipeline.traits.trait.TraitValidationError","title":"<code>TraitValidationError</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Trait validation error exception.</p> <p>This exception is raised when the trait validation fails.</p> Source code in <code>client/ayon_core/pipeline/traits/trait.py</code> <pre><code>class TraitValidationError(Exception):\n    \"\"\"Trait validation error exception.\n\n    This exception is raised when the trait validation fails.\n    \"\"\"\n\n    def __init__(self, scope: str, message: str):\n        \"\"\"Initialize the exception.\n\n        We could determine the scope from the stack in the future,\n        provided the scope is always Trait name.\n\n        Args:\n            scope (str): Scope of the error.\n            message (str): Error message.\n\n        \"\"\"\n        super().__init__(f\"{scope}: {message}\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/trait.html#client.ayon_core.pipeline.traits.trait.TraitValidationError.__init__","title":"<code>__init__(scope, message)</code>","text":"<p>Initialize the exception.</p> <p>We could determine the scope from the stack in the future, provided the scope is always Trait name.</p> <p>Parameters:</p> Name Type Description Default <code>scope</code> <code>str</code> <p>Scope of the error.</p> required <code>message</code> <code>str</code> <p>Error message.</p> required Source code in <code>client/ayon_core/pipeline/traits/trait.py</code> <pre><code>def __init__(self, scope: str, message: str):\n    \"\"\"Initialize the exception.\n\n    We could determine the scope from the stack in the future,\n    provided the scope is always Trait name.\n\n    Args:\n        scope (str): Scope of the error.\n        message (str): Error message.\n\n    \"\"\"\n    super().__init__(f\"{scope}: {message}\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/trait.html#client.ayon_core.pipeline.traits.trait.UpgradableTraitError","title":"<code>UpgradableTraitError</code>","text":"<p>               Bases: <code>Exception</code>, <code>Generic[T]</code></p> <p>Upgradable trait version exception.</p> <p>This exception is raised when the trait can upgrade existing data meant for older versions of the trait. It must implement an <code>upgrade</code> method that will take old trait data as an argument to handle the upgrade.</p> Source code in <code>client/ayon_core/pipeline/traits/trait.py</code> <pre><code>class UpgradableTraitError(Exception, Generic[T]):\n    \"\"\"Upgradable trait version exception.\n\n    This exception is raised when the trait can upgrade existing data\n    meant for older versions of the trait. It must implement an `upgrade`\n    method that will take old trait data as an argument to handle the upgrade.\n    \"\"\"\n\n    trait: T\n    old_data: dict\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/two_dimensional.html","title":"two_dimensional","text":"<p>Two-dimensional image traits.</p>"},{"location":"autoapi/client/ayon_core/pipeline/traits/two_dimensional.html#client.ayon_core.pipeline.traits.two_dimensional.Deep","title":"<code>Deep</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>Deep trait model.</p> <p>Type trait model for deep EXR images.</p> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Trait name.</p> <code>description</code> <code>str</code> <p>Trait description.</p> <code>id</code> <code>str</code> <p>id should be a namespaced trait name with a version</p> Source code in <code>client/ayon_core/pipeline/traits/two_dimensional.py</code> <pre><code>@dataclass\nclass Deep(TraitBase):\n    \"\"\"Deep trait model.\n\n    Type trait model for deep EXR images.\n\n    Attributes:\n        name (str): Trait name.\n        description (str): Trait description.\n        id (str): id should be a namespaced trait name with a version\n    \"\"\"\n\n    name: ClassVar[str] = \"Deep\"\n    description: ClassVar[str] = \"Deep Trait Model\"\n    id: ClassVar[str] = \"ayon.2d.Deep.v1\"\n    persistent: ClassVar[bool] = True\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/two_dimensional.html#client.ayon_core.pipeline.traits.two_dimensional.Image","title":"<code>Image</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>Image trait model.</p> <p>Type trait model for image.</p> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Trait name.</p> <code>description</code> <code>str</code> <p>Trait description.</p> <code>id</code> <code>str</code> <p>id should be a namespaced trait name with version</p> Source code in <code>client/ayon_core/pipeline/traits/two_dimensional.py</code> <pre><code>@dataclass\nclass Image(TraitBase):\n    \"\"\"Image trait model.\n\n    Type trait model for image.\n\n    Attributes:\n        name (str): Trait name.\n        description (str): Trait description.\n        id (str): id should be a namespaced trait name with version\n    \"\"\"\n\n    name: ClassVar[str] = \"Image\"\n    description: ClassVar[str] = \"Image Trait\"\n    id: ClassVar[str] = \"ayon.2d.Image.v1\"\n    persistent: ClassVar[bool] = True\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/two_dimensional.html#client.ayon_core.pipeline.traits.two_dimensional.Overscan","title":"<code>Overscan</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>Overscan trait model.</p> <p>This model represents an overscan (or underscan) trait. Defines the extra pixels around the image.</p> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Trait name.</p> <code>description</code> <code>str</code> <p>Trait description.</p> <code>id</code> <code>str</code> <p>id should be a namespaced trait name with a version</p> <code>left</code> <code>int</code> <p>Left overscan/underscan.</p> <code>right</code> <code>int</code> <p>Right overscan/underscan.</p> <code>top</code> <code>int</code> <p>Top overscan/underscan.</p> <code>bottom</code> <code>int</code> <p>Bottom overscan/underscan.</p> Source code in <code>client/ayon_core/pipeline/traits/two_dimensional.py</code> <pre><code>@dataclass\nclass Overscan(TraitBase):\n    \"\"\"Overscan trait model.\n\n    This model represents an overscan (or underscan) trait. Defines the\n    extra pixels around the image.\n\n    Attributes:\n        name (str): Trait name.\n        description (str): Trait description.\n        id (str): id should be a namespaced trait name with a version\n        left (int): Left overscan/underscan.\n        right (int): Right overscan/underscan.\n        top (int): Top overscan/underscan.\n        bottom (int): Bottom overscan/underscan.\n    \"\"\"\n\n    name: ClassVar[str] = \"Overscan\"\n    description: ClassVar[str] = \"Overscan Trait\"\n    id: ClassVar[str] = \"ayon.2d.Overscan.v1\"\n    persistent: ClassVar[bool] = True\n    left: int\n    right: int\n    top: int\n    bottom: int\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/two_dimensional.html#client.ayon_core.pipeline.traits.two_dimensional.PixelBased","title":"<code>PixelBased</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>PixelBased trait model.</p> <p>The pixel-related trait for image data.</p> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Trait name.</p> <code>description</code> <code>str</code> <p>Trait description.</p> <code>id</code> <code>str</code> <p>id should be a namespaced trait name with a version</p> <code>display_window_width</code> <code>int</code> <p>Width of the image display window.</p> <code>display_window_height</code> <code>int</code> <p>Height of the image display window.</p> <code>pixel_aspect_ratio</code> <code>float</code> <p>Pixel aspect ratio.</p> Source code in <code>client/ayon_core/pipeline/traits/two_dimensional.py</code> <pre><code>@dataclass\nclass PixelBased(TraitBase):\n    \"\"\"PixelBased trait model.\n\n    The pixel-related trait for image data.\n\n    Attributes:\n        name (str): Trait name.\n        description (str): Trait description.\n        id (str): id should be a namespaced trait name with a version\n        display_window_width (int): Width of the image display window.\n        display_window_height (int): Height of the image display window.\n        pixel_aspect_ratio (float): Pixel aspect ratio.\n    \"\"\"\n\n    name: ClassVar[str] = \"PixelBased\"\n    description: ClassVar[str] = \"PixelBased Trait Model\"\n    id: ClassVar[str] = \"ayon.2d.PixelBased.v1\"\n    persistent: ClassVar[bool] = True\n    display_window_width: int\n    display_window_height: int\n    pixel_aspect_ratio: float\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/two_dimensional.html#client.ayon_core.pipeline.traits.two_dimensional.Planar","title":"<code>Planar</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>Planar trait model.</p> <p>This model represents an Image with planar configuration.</p> Todo <ul> <li>(antirotor): Is this really a planar configuration? As with     bit planes and everything? If it serves as differentiator for     Deep images, should it be named differently? Like Raster?</li> </ul> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Trait name.</p> <code>description</code> <code>str</code> <p>Trait description.</p> <code>id</code> <code>str</code> <p>id should be namespaced trait name with version</p> <code>planar_configuration</code> <code>str</code> <p>Planar configuration.</p> Source code in <code>client/ayon_core/pipeline/traits/two_dimensional.py</code> <pre><code>@dataclass\nclass Planar(TraitBase):\n    \"\"\"Planar trait model.\n\n    This model represents an Image with planar configuration.\n\n    Todo:\n        * (antirotor): Is this really a planar configuration? As with\n            bit planes and everything? If it serves as differentiator for\n            Deep images, should it be named differently? Like Raster?\n\n    Attributes:\n        name (str): Trait name.\n        description (str): Trait description.\n        id (str): id should be namespaced trait name with version\n        planar_configuration (str): Planar configuration.\n    \"\"\"\n\n    name: ClassVar[str] = \"Planar\"\n    description: ClassVar[str] = \"Planar Trait Model\"\n    id: ClassVar[str] = \"ayon.2d.Planar.v1\"\n    persistent: ClassVar[bool] = True\n    planar_configuration: str\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/two_dimensional.html#client.ayon_core.pipeline.traits.two_dimensional.UDIM","title":"<code>UDIM</code>  <code>dataclass</code>","text":"<p>               Bases: <code>TraitBase</code></p> <p>UDIM trait model.</p> <p>This model represents a UDIM trait.</p> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Trait name.</p> <code>description</code> <code>str</code> <p>Trait description.</p> <code>id</code> <code>str</code> <p>id should be namespaced trait name with version</p> <code>udim</code> <code>int</code> <p>UDIM value.</p> <code>udim_regex</code> <code>str</code> <p>UDIM regex.</p> Source code in <code>client/ayon_core/pipeline/traits/two_dimensional.py</code> <pre><code>@dataclass\nclass UDIM(TraitBase):\n    \"\"\"UDIM trait model.\n\n    This model represents a UDIM trait.\n\n    Attributes:\n        name (str): Trait name.\n        description (str): Trait description.\n        id (str): id should be namespaced trait name with version\n        udim (int): UDIM value.\n        udim_regex (str): UDIM regex.\n    \"\"\"\n\n    name: ClassVar[str] = \"UDIM\"\n    description: ClassVar[str] = \"UDIM Trait\"\n    id: ClassVar[str] = \"ayon.2d.UDIM.v1\"\n    persistent: ClassVar[bool] = True\n    udim: list[int]\n    udim_regex: Optional[str] = r\"(?:\\.|_)(?P&lt;udim&gt;\\d+)\\.\\D+\\d?$\"\n\n    # Field validator for udim_regex - this works in the pydantic model v2\n    # but not with the pure data classes.\n    @classmethod\n    def validate_frame_regex(cls, v: Optional[str]) -&gt; Optional[str]:\n        \"\"\"Validate udim regex.\n\n        Returns:\n            Optional[str]: UDIM regex.\n\n        Raises:\n            ValueError: UDIM regex must include 'udim' named group.\n\n        \"\"\"\n        if v is not None and \"?P&lt;udim&gt;\" not in v:\n            msg = \"UDIM regex must include 'udim' named group\"\n            raise ValueError(msg)\n        return v\n\n    def get_file_location_for_udim(\n            self,\n            file_locations: FileLocations,\n            udim: int,\n        ) -&gt; Optional[FileLocation]:\n        \"\"\"Get file location for UDIM.\n\n        Args:\n            file_locations (FileLocations): File locations.\n            udim (int): UDIM value.\n\n        Returns:\n            Optional[FileLocation]: File location.\n\n        \"\"\"\n        if not self.udim_regex:\n            return None\n        pattern = re.compile(self.udim_regex)\n        for location in file_locations.file_paths:\n            result = re.search(pattern, location.file_path.name)\n            if result:\n                udim_index = int(result.group(\"udim\"))\n                if udim_index == udim:\n                    return location\n        return None\n\n    def get_udim_from_file_location(\n            self, file_location: FileLocation) -&gt; Optional[int]:\n        \"\"\"Get UDIM from the file location.\n\n        Args:\n            file_location (FileLocation): File location.\n\n        Returns:\n            Optional[int]: UDIM value.\n\n        \"\"\"\n        if not self.udim_regex:\n            return None\n        pattern = re.compile(self.udim_regex)\n        result = re.search(pattern, file_location.file_path.name)\n        if result:\n            return int(result.group(\"udim\"))\n        return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/two_dimensional.html#client.ayon_core.pipeline.traits.two_dimensional.UDIM.get_file_location_for_udim","title":"<code>get_file_location_for_udim(file_locations, udim)</code>","text":"<p>Get file location for UDIM.</p> <p>Parameters:</p> Name Type Description Default <code>file_locations</code> <code>FileLocations</code> <p>File locations.</p> required <code>udim</code> <code>int</code> <p>UDIM value.</p> required <p>Returns:</p> Type Description <code>Optional[FileLocation]</code> <p>Optional[FileLocation]: File location.</p> Source code in <code>client/ayon_core/pipeline/traits/two_dimensional.py</code> <pre><code>def get_file_location_for_udim(\n        self,\n        file_locations: FileLocations,\n        udim: int,\n    ) -&gt; Optional[FileLocation]:\n    \"\"\"Get file location for UDIM.\n\n    Args:\n        file_locations (FileLocations): File locations.\n        udim (int): UDIM value.\n\n    Returns:\n        Optional[FileLocation]: File location.\n\n    \"\"\"\n    if not self.udim_regex:\n        return None\n    pattern = re.compile(self.udim_regex)\n    for location in file_locations.file_paths:\n        result = re.search(pattern, location.file_path.name)\n        if result:\n            udim_index = int(result.group(\"udim\"))\n            if udim_index == udim:\n                return location\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/two_dimensional.html#client.ayon_core.pipeline.traits.two_dimensional.UDIM.get_udim_from_file_location","title":"<code>get_udim_from_file_location(file_location)</code>","text":"<p>Get UDIM from the file location.</p> <p>Parameters:</p> Name Type Description Default <code>file_location</code> <code>FileLocation</code> <p>File location.</p> required <p>Returns:</p> Type Description <code>Optional[int]</code> <p>Optional[int]: UDIM value.</p> Source code in <code>client/ayon_core/pipeline/traits/two_dimensional.py</code> <pre><code>def get_udim_from_file_location(\n        self, file_location: FileLocation) -&gt; Optional[int]:\n    \"\"\"Get UDIM from the file location.\n\n    Args:\n        file_location (FileLocation): File location.\n\n    Returns:\n        Optional[int]: UDIM value.\n\n    \"\"\"\n    if not self.udim_regex:\n        return None\n    pattern = re.compile(self.udim_regex)\n    result = re.search(pattern, file_location.file_path.name)\n    if result:\n        return int(result.group(\"udim\"))\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/two_dimensional.html#client.ayon_core.pipeline.traits.two_dimensional.UDIM.validate_frame_regex","title":"<code>validate_frame_regex(v)</code>  <code>classmethod</code>","text":"<p>Validate udim regex.</p> <p>Returns:</p> Type Description <code>Optional[str]</code> <p>Optional[str]: UDIM regex.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>UDIM regex must include 'udim' named group.</p> Source code in <code>client/ayon_core/pipeline/traits/two_dimensional.py</code> <pre><code>@classmethod\ndef validate_frame_regex(cls, v: Optional[str]) -&gt; Optional[str]:\n    \"\"\"Validate udim regex.\n\n    Returns:\n        Optional[str]: UDIM regex.\n\n    Raises:\n        ValueError: UDIM regex must include 'udim' named group.\n\n    \"\"\"\n    if v is not None and \"?P&lt;udim&gt;\" not in v:\n        msg = \"UDIM regex must include 'udim' named group\"\n        raise ValueError(msg)\n    return v\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/traits/utils.html","title":"utils","text":"<p>Utility functions for traits.</p>"},{"location":"autoapi/client/ayon_core/pipeline/traits/utils.html#client.ayon_core.pipeline.traits.utils.get_available_traits","title":"<code>get_available_traits(addons_manager=None)</code>","text":"<p>Get available traits from active addons.</p> <p>Parameters:</p> Name Type Description Default <code>addons_manager</code> <code>Optional[AddonsManager]</code> <p>Addons manager instance. If not provided, a new one will be created. Within pyblish plugins, you can use an already collected instance of AddonsManager from context <code>context.data[\"ayonAddonsManager\"]</code>.</p> <code>None</code> <p>Returns:</p> Type Description <code>Optional[list[TraitBase]]</code> <p>list[TraitBase]: List of available traits.</p> Source code in <code>client/ayon_core/pipeline/traits/utils.py</code> <pre><code>def get_available_traits(\n        addons_manager: Optional[AddonsManager] = None\n) -&gt; Optional[list[TraitBase]]:\n    \"\"\"Get available traits from active addons.\n\n    Args:\n        addons_manager (Optional[AddonsManager]): Addons manager instance.\n            If not provided, a new one will be created. Within pyblish\n            plugins, you can use an already collected instance of\n            AddonsManager from context `context.data[\"ayonAddonsManager\"]`.\n\n    Returns:\n        list[TraitBase]: List of available traits.\n\n    \"\"\"\n    if addons_manager is None:\n        # Create a new instance of AddonsManager\n        addons_manager = AddonsManager()\n\n    # Get active addons\n    enabled_addons = addons_manager.get_enabled_addons()\n    traits = []\n    for addon in enabled_addons:\n        if not issubclass(type(addon), ITraits):\n            # Skip addons not providing traits\n            continue\n        # Get traits from addon\n        addon_traits = addon.get_addon_traits()\n        if addon_traits:\n            # Add traits to a list\n            for trait in addon_traits:\n                if trait not in traits:\n                    traits.append(trait)\n\n    return traits\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/index.html","title":"workfile","text":""},{"location":"autoapi/client/ayon_core/pipeline/workfile/index.html#client.ayon_core.pipeline.workfile.BuildWorkfile","title":"<code>BuildWorkfile</code>","text":"<p>Wrapper for build workfile process.</p> <p>Load representations for current context by build presets. Build presets are host related, since each host has it's loaders.</p> Source code in <code>client/ayon_core/pipeline/workfile/build_workfile.py</code> <pre><code>class BuildWorkfile:\n    \"\"\"Wrapper for build workfile process.\n\n    Load representations for current context by build presets. Build presets\n    are host related, since each host has it's loaders.\n    \"\"\"\n\n    addon_name = None\n\n    def __init__(self):\n        self.build_presets = []\n        self.log = Logger.get_logger(self.__class__.__name__)\n        self._host = None\n        self._current_context = None\n        self._current_project_settings = None\n        self._current_project_entity = None\n        self._current_folder_entity = None\n        self._current_task_entity = None\n\n    def get_host(self) -&gt; AbstractHost:\n        from ayon_core.pipeline import registered_host\n\n        if self._host is None:\n            self._host = registered_host()\n        return self._host\n\n    def get_host_name(self) -&gt; str:\n        return self.host.name\n\n    def get_current_context(self) -&gt; dict[str, str]:\n        if self._current_context is None:\n            host = self.get_host()\n            self._current_context = host.get_current_context()\n        return self._current_context\n\n    def get_current_project_name(self) -&gt; str:\n        return self.get_current_context()[\"project_name\"]\n\n    def get_current_project_settings(self) -&gt; dict[str, Any]:\n        if self._current_project_settings is None:\n            self._current_project_settings = get_project_settings(\n                self.get_current_project_name()\n            )\n        return self._current_project_settings\n\n    def get_current_project_entity(self) -&gt; Optional[dict[str, Any]]:\n        if self._current_project_entity is None:\n            context = self.get_current_context()\n            project_name = context[\"project_name\"]\n            if not project_name:\n                return None\n            self._current_project_entity = ayon_api.get_project(project_name)\n        return self._current_project_entity\n\n    def get_current_folder_entity(self) -&gt; Optional[dict[str, Any]]:\n        if self._current_folder_entity is None:\n            context = self.get_current_context()\n            project_name = context[\"project_name\"]\n            folder_path = context[\"folder_path\"]\n            if not project_name or not folder_path:\n                return None\n            self._current_folder_entity = ayon_api.get_folder_by_path(\n                project_name,\n                folder_path,\n            )\n        return self._current_folder_entity\n\n    def get_current_task_entity(self) -&gt; Optional[dict[str, Any]]:\n        if self._current_task_entity is None:\n            context = self.get_current_context()\n            project_name = context[\"project_name\"]\n            task_name = context[\"task_name\"]\n            if not project_name or not task_name:\n                return None\n            folder_entity = self.get_current_folder_entity()\n            if not folder_entity:\n                return None\n            self._current_task_entity = ayon_api.get_task_by_name(\n                context[\"project_name\"],\n                folder_id=folder_entity[\"id\"],\n                task_name=context[\"task_name\"],\n            )\n        return self._current_task_entity\n\n    host = property(get_host)\n    host_name = property(get_host_name)\n    current_project_name = property(get_current_project_name)\n    current_project_settings = property(get_current_project_settings)\n    current_project_entity = property(get_current_project_entity)\n    current_folder_entity = property(get_current_folder_entity)\n    current_task_entity = property(get_current_task_entity)\n\n    @staticmethod\n    def map_products_by_base_type(product_entities):\n        products_by_base_type = collections.defaultdict(list)\n        for product_entity in product_entities:\n            product_base_type = product_entity.get(\"productBaseType\")\n            if not product_base_type:\n                product_base_type = product_entity[\"productType\"]\n            products_by_base_type[product_base_type].append(product_entity)\n        return products_by_base_type\n\n    def process(self):\n        \"\"\"Main method of this wrapper.\n\n        Building of workfile is triggered and is possible to implement\n        post processing of loaded containers if necessary.\n\n        Returns:\n            list[dict[str, Any]]: Loaded containers during build.\n\n        \"\"\"\n        self.apply_settings()\n        return self.build_workfile()\n\n    def apply_settings(self) -&gt; None:\n        project_entity = self.current_project_entity\n        folder_entity = self.current_folder_entity\n        task_entity = self.current_task_entity\n        if not project_entity or not folder_entity or not task_entity:\n            return\n\n        # Load workfile presets for task\n        self.build_presets = self.get_build_presets()\n\n    def get_build_profiles(self) -&gt; list[dict[str, Any]]:\n        project_settings = self.current_project_settings\n        addon_name = self.addon_name\n        if not addon_name:\n            addon_name = self.host_name\n\n        host_settings = project_settings.get(addon_name)\n        if not host_settings:\n            return []\n\n        # Get presets for host\n        wb_settings = host_settings.get(\"workfile_builder\")\n        if not wb_settings:\n            # backward compatibility\n            wb_settings = host_settings.get(\"workfile_build\") or {}\n\n        builder_profiles = wb_settings.get(\"profiles\")\n        return builder_profiles or []\n\n    def get_build_presets(self) -&gt; Optional[dict[str, Any]]:\n        \"\"\" Returns presets to build workfile for task name.\n\n        Presets are loaded for current project received by\n        'get_current_project_name', filtered by registered host\n        and entered task name.\n\n\n        Returns:\n            dict[str, Any]: preset per entered task name\n\n        \"\"\"\n        profiles = self.get_build_profiles()\n        if not profiles:\n            return None\n\n        task_entity = self.current_task_entity\n        task_name = task_type = None\n        if task_entity:\n            task_name = task_entity[\"name\"]\n            task_type = task_entity[\"taskType\"]\n\n        filter_data = {\n            \"task_types\": task_type,\n            \"task_names\": task_name,\n            # Backwards compatibility\n            \"tasks\": task_name,\n        }\n        return filter_profiles(profiles, filter_data)\n\n    def build_workfile(self):\n        \"\"\"Prepares and load containers into workfile.\n\n        Loads latest versions of current and linked folders to workfile by\n        logic stored in Workfile profiles from presets. Profiles are set\n        by host, filtered by current task name and used by families.\n\n        Each product base type can specify representation names and loaders\n        for representations and first available and successful loaded\n        representation is returned as container.\n\n        At the end you'll get list of loaded containers per each folder.\n\n        loaded_containers [{\n            \"folder_entity\": &lt;FolderEntity1&gt;,\n            \"containers\": [&lt;Container1&gt;, &lt;Container2&gt;, ...]\n        }, {\n            \"folder_entity\": &lt;FolderEntity2&gt;,\n            \"containers\": [&lt;Container3&gt;, ...]\n        }, {\n            ...\n        }]\n\n        Returns:\n            list[dict[str, Any]]: Loaded containers during build.\n\n        \"\"\"\n        loaded_containers = []\n\n        current_context = self.get_current_context()\n        project_name = current_context[\"project_name\"]\n        current_folder_path = current_context[\"folder_path\"]\n        current_task_name = current_context[\"task_name\"]\n\n        folder_entity = self.current_folder_entity\n        task_entity = self.current_task_entity\n        # Skip if folder was not found\n        if not folder_entity:\n            self.log.warning(\n                f\"Folder entity '{current_folder_path}' was not found\"\n            )\n            return loaded_containers\n\n        task_path = f\"{current_folder_path}/{current_task_name}\"\n        if not task_entity:\n            self.log.warning(f\"Task entity '{task_path}' was not found\")\n            return loaded_containers\n\n        # Prepare available loaders\n        loaders_by_name = {}\n        for loader in discover_loader_plugins():\n            if not loader.enabled:\n                continue\n            loader_name = loader.__name__\n            if loader_name in loaders_by_name:\n                raise KeyError(f\"Duplicated loader name {loader_name}!\")\n\n            loaders_by_name[loader_name] = loader\n\n        # Skip if there are any loaders\n        if not loaders_by_name:\n            self.log.warning(\"There are no registered loaders.\")\n            return loaded_containers\n\n        # Skip if there are any presets for task\n        if not self.build_presets:\n            self.log.warning(\n                f\"Current task '{task_path}' does not have any\"\n                \" loading preset.\"\n            )\n            return loaded_containers\n\n        # Get presets for loading current folder\n        current_context_profiles = self.build_presets.get(\"current_context\")\n        # Get presets for loading linked folders\n        link_context_profiles = self.build_presets.get(\"linked_folders\")\n        if not link_context_profiles:\n            # TODO remove 'linked_assets' (Marked as deprecated 26/02/05)\n            link_context_profiles = self.build_presets.get(\"linked_assets\")\n        # Skip if both are missing\n        if not current_context_profiles and not link_context_profiles:\n            self.log.warning(\n                f\"Current task '{task_path}' has empty\"\n                \" loading preset.\"\n            )\n            return loaded_containers\n\n        elif not current_context_profiles:\n            self.log.warning(\n                f\"Current task '{task_path}' doesn't have any loading\"\n                \" preset for it's context.\"\n            )\n\n        elif not link_context_profiles:\n            self.log.warning(\n                f\"Current task '{task_path}' doesn't have any\"\n                \"loading preset for it's linked folders.\"\n            )\n\n        # Prepare folders to process by workfile presets\n        folder_entities = []\n        current_folder_id = None\n        if current_context_profiles:\n            # Add current folder entity if preset has current context set\n            folder_entities.append(folder_entity)\n            current_folder_id = folder_entity[\"id\"]\n\n        if link_context_profiles:\n            # Find and append linked folders if preset has set linked mapping\n            linked_folder_entities = self._get_linked_folder_entities(\n                project_name, folder_entity[\"id\"]\n            )\n            if linked_folder_entities:\n                folder_entities.extend(linked_folder_entities)\n\n        # Skip if there are no folders. This can happen if only linked mapping\n        # is set and there are no links for his folder.\n        if not folder_entities:\n            self.log.warning(\n                \"Folder does not have linked folders. Nothing to process.\"\n            )\n            return loaded_containers\n\n        # Prepare entities from database for folders\n        prepared_entities = self._collect_last_version_repres(\n            folder_entities\n        )\n\n        # Load containers by prepared entities and presets\n        # - Current folder containers\n        if current_folder_id and current_folder_id in prepared_entities:\n            current_context_data = prepared_entities.pop(current_folder_id)\n            loaded_data = self.load_containers_by_folder_data(\n                current_context_data,\n                current_context_profiles,\n                loaders_by_name\n            )\n            if loaded_data:\n                loaded_containers.append(loaded_data)\n\n        # - Linked assets container\n        for linked_folder_data in prepared_entities.values():\n            loaded_data = self.load_containers_by_folder_data(\n                linked_folder_data,\n                link_context_profiles,\n                loaders_by_name\n            )\n            if loaded_data:\n                loaded_containers.append(loaded_data)\n\n        # Return list of loaded containers\n        return loaded_containers\n\n    def _filter_build_profiles(self, build_profiles, loaders_by_name):\n        \"\"\" Filter build profiles by loaders and prepare process data.\n\n        Valid profile must have \"loaders\", \"families\" and \"repre_names\" keys\n        with valid values.\n        - \"loaders\" expects list of strings representing possible loaders.\n        - \"product_base_types\" expects list of strings for filtering\n                     by product base type.\n        - \"repre_names\" expects list of strings for filtering by\n                        representation name.\n\n        Lowered \"families\" and \"repre_names\" are prepared for each profile with\n        all required keys.\n\n        Args:\n            build_profiles (Dict[str, Any]): Profiles for building workfile.\n            loaders_by_name (Dict[str, LoaderPlugin]): Available loaders\n                per name.\n\n        Returns:\n            List[Dict[str, Any]]: Filtered and prepared profiles.\n        \"\"\"\n\n        valid_profiles = []\n        for profile in build_profiles:\n            # Check loaders\n            profile_loaders = profile.get(\"loaders\")\n            if not profile_loaders:\n                self.log.warning((\n                    \"Build profile has missing loaders configuration: {0}\"\n                ).format(json.dumps(profile, indent=4)))\n                continue\n\n            # Check if any loader is available\n            loaders_match = False\n            for loader_name in profile_loaders:\n                if loader_name in loaders_by_name:\n                    loaders_match = True\n                    break\n\n            if not loaders_match:\n                self.log.warning((\n                    \"All loaders from Build profile are not available: {0}\"\n                ).format(json.dumps(profile, indent=4)))\n                continue\n\n            # Check product base types\n            profile_product_base_types = profile.get(\"product_base_types\")\n            if not profile_product_base_types:\n                profile_product_base_types = profile.get(\"product_types\")\n\n            if not profile_product_base_types:\n                self.log.warning((\n                    \"Build profile is missing families configuration: {0}\"\n                ).format(json.dumps(profile, indent=4)))\n                continue\n\n            # Check representation names\n            profile_repre_names = profile.get(\"repre_names\")\n            if not profile_repre_names:\n                self.log.warning((\n                    \"Build profile is missing\"\n                    \" representation names filtering: {0}\"\n                ).format(json.dumps(profile, indent=4)))\n                continue\n\n            # Prepare lowered families and representation names\n            profile[\"product_base_types_lowered\"] = [\n                product_base_type.lower()\n                for product_base_type in profile_product_base_types\n            ]\n\n            profile[\"repre_names_lowered\"] = [\n                name.lower() for name in profile_repre_names\n            ]\n\n            valid_profiles.append(profile)\n\n        return valid_profiles\n\n    def _get_linked_folder_entities(\n        self, project_name: str, folder_id: str\n    ) -&gt; list[dict[str, Any]]:\n        \"\"\"Get linked folder entities for entered folder.\n\n        Args:\n            project_name (str): Project name.\n            folder_id (str): Folder id.\n\n        Returns:\n            list[dict[str, Any]]: Linked folder entities.\n\n        \"\"\"\n        links = ayon_api.get_folder_links(\n            project_name, folder_id, link_direction=\"in\"\n        )\n        linked_folder_ids = {\n            link[\"entityId\"]\n            for link in links\n            if link[\"entityType\"] == \"folder\"\n        }\n        if not linked_folder_ids:\n            return []\n        return list(ayon_api.get_folders(\n            project_name, folder_ids=linked_folder_ids\n        ))\n\n    def _prepare_profile_for_products(self, product_entities, profiles):\n        \"\"\"Select profile for each product by it's data.\n\n        Profiles are filtered for each product individually.\n        Profile is filtered by product base type, optionally by name regex and\n        representation names set in profile.\n        It is possible to not find matching profile for product, in that case\n        product is skipped and it is possible that none of products have\n        matching profile.\n\n        Args:\n            product_entities (List[Dict[str, Any]]): product entities.\n            profiles (List[Dict[str, Any]]): Build profiles.\n\n        Returns:\n            Dict[str, Any]: Profile by product id.\n        \"\"\"\n\n        # Prepare products\n        products_by_base_type = self.map_products_by_base_type(\n            product_entities\n        )\n\n        profiles_by_product_id = {}\n        for (\n            product_base_type,\n            product_entities\n        ) in products_by_base_type.items():\n            product_base_type_low = product_base_type.lower()\n            for profile in profiles:\n                # Skip profile if does not contain product base type\n                if product_base_type_low not in (\n                    profile[\"product_base_types_lowered\"]\n                ):\n                    continue\n\n                # Precompile name filters as regexes\n                profile_regexes = profile.get(\"product_name_filters\")\n                if profile_regexes:\n                    _profile_regexes = []\n                    for regex in profile_regexes:\n                        _profile_regexes.append(re.compile(regex))\n                    profile_regexes = _profile_regexes\n\n                # TODO prepare regex compilation\n                for product_entity in product_entities:\n                    # Verify regex filtering (optional)\n                    if profile_regexes:\n                        valid = False\n                        for pattern in profile_regexes:\n                            if re.match(pattern, product_entity[\"name\"]):\n                                valid = True\n                                break\n\n                        if not valid:\n                            continue\n\n                    profiles_by_product_id[product_entity[\"id\"]] = profile\n\n                # break profiles loop on finding the first matching profile\n                break\n        return profiles_by_product_id\n\n    def load_containers_by_folder_data(\n        self, linked_folder_data, build_profiles, loaders_by_name\n    ):\n        \"\"\"Load containers for entered folder entity by Build profiles.\n\n        Args:\n            linked_folder_data (Dict[str, Any]): Prepared data with products,\n                last versions and representations for specific folder.\n            build_profiles (Dict[str, Any]): Build profiles.\n            loaders_by_name (Dict[str, LoaderPlugin]): Available loaders\n                per name.\n\n        Returns:\n            Dict[str, Any]: Output contains folder entity\n                and loaded containers.\n        \"\"\"\n\n        # Make sure all data are not empty\n        if not linked_folder_data or not build_profiles or not loaders_by_name:\n            return\n\n        folder_entity = linked_folder_data[\"folder_entity\"]\n\n        valid_profiles = self._filter_build_profiles(\n            build_profiles, loaders_by_name\n        )\n        if not valid_profiles:\n            self.log.warning(\n                \"There are not valid Workfile profiles. Skipping process.\"\n            )\n            return\n\n        self.log.debug(\"Valid Workfile profiles: {}\".format(valid_profiles))\n\n        products_by_id = {}\n        version_by_product_id = {}\n        repres_by_version_id = {}\n        for product_id, in_data in linked_folder_data[\"products\"].items():\n            product_entity = in_data[\"product_entity\"]\n            products_by_id[product_entity[\"id\"]] = product_entity\n\n            version_data = in_data[\"version\"]\n            version_entity = version_data[\"version_entity\"]\n            version_by_product_id[product_id] = version_entity\n            repres_by_version_id[version_entity[\"id\"]] = (\n                version_data[\"repres\"]\n            )\n\n        if not products_by_id:\n            self.log.warning(\"There are not products for folder {}\".format(\n                folder_entity[\"path\"]\n            ))\n            return\n\n        profiles_by_product_id = self._prepare_profile_for_products(\n            products_by_id.values(), valid_profiles\n        )\n        if not profiles_by_product_id:\n            self.log.warning(\"There are not valid products.\")\n            return\n\n        valid_repres_by_product_id = collections.defaultdict(list)\n        for product_id, profile in profiles_by_product_id.items():\n            profile_repre_names = profile[\"repre_names_lowered\"]\n\n            version_entity = version_by_product_id[product_id]\n            version_id = version_entity[\"id\"]\n            repres = repres_by_version_id[version_id]\n            for repre in repres:\n                repre_name_low = repre[\"name\"].lower()\n                if repre_name_low in profile_repre_names:\n                    valid_repres_by_product_id[product_id].append(repre)\n\n        # DEBUG message\n        msg = \"Valid representations for Folder: `{}`\".format(\n            folder_entity[\"path\"]\n        )\n        for product_id, repres in valid_repres_by_product_id.items():\n            product_entity = products_by_id[product_id]\n            msg += \"\\n# Product Name/ID: `{}`/{}\".format(\n                product_entity[\"name\"], product_id\n            )\n            for repre in repres:\n                msg += \"\\n## Repre name: `{}`\".format(repre[\"name\"])\n\n        self.log.debug(msg)\n\n        containers = self._load_containers(\n            valid_repres_by_product_id,\n            products_by_id,\n            profiles_by_product_id,\n            loaders_by_name,\n        )\n\n        return {\n            \"folder_entity\": folder_entity,\n            \"containers\": containers\n        }\n\n    def _load_containers(\n        self,\n        repres_by_product_id: dict[str, list[dict[str, Any]]],\n        products_by_id: dict[str, dict[str, Any]],\n        profiles_by_product_id: dict[str, list[dict[str, Any]]],\n        loaders_by_name: dict[str, LoaderPlugin],\n    ) -&gt; list[dict[str, Any]]:\n        \"\"\"Real load by collected data happens here.\n\n        Loading of representations per product happens here. Each product can\n        loads one representation. Loading is tried in specific order.\n        Representations are tried to load by names defined in configuration.\n        If product has representation matching representation name each loader\n        is tried to load it until any is successful. If none of them was\n        successful then next representation name is tried.\n        Product process loop ends when any representation is loaded or\n        all matching representations were already tried.\n\n        Args:\n            repres_by_product_id (dict[str, list[dict[str, Any]]]): Available\n                representations mapped by their parent (product) id.\n            products_by_id (dict[str, dict[str, Any]]): Product entities\n                mapped by their id.\n            profiles_by_product_id (dict[str, list[dict[str, Any]]]): Build\n                profiles mapped by product id.\n            loaders_by_name (dict[str, LoaderPlugin]): Available loaders\n                per name.\n\n        Returns:\n            List[Dict[str, Any]]: Objects of loaded containers.\n        \"\"\"\n\n        loaded_containers = []\n\n        # Get product id order from build presets.\n        build_presets = self.build_presets.get(\"current_context\", [])\n        build_presets += self.build_presets.get(\"linked_folders\", [])\n        # TODO remove 'linked_assets' (Marked as deprecated 26/02/05)\n        build_presets += self.build_presets.get(\"linked_assets\", [])\n        product_ids_ordered = []\n        for preset in build_presets:\n            product_base_types = preset.get(\"product_base_types\")\n            if product_base_types is None:\n                product_base_types = preset[\"product_types\"]\n            if not product_base_types:\n                continue\n\n            for product_base_type in product_base_types:\n                for product_id, product_entity in products_by_id.items():\n                    # TODO 'families' is not available on product\n                    families = product_entity[\"data\"].get(\"families\") or []\n                    if product_base_type not in families:\n                        continue\n\n                    product_ids_ordered.append(product_id)\n\n        # Order representations from products.\n        representations_ordered = []\n        representations = []\n        for ordered_product_id in product_ids_ordered:\n            for product_id, repres in repres_by_product_id.items():\n                if repres in representations:\n                    continue\n\n                if ordered_product_id == product_id:\n                    representations_ordered.append((product_id, repres))\n                    representations.append(repres)\n\n        print(\"representations\", representations)\n\n        # Load ordered representations.\n        for product_id, repres in representations_ordered:\n            product_name = products_by_id[product_id][\"name\"]\n\n            profile = profiles_by_product_id[product_id]\n            loaders_last_idx = len(profile[\"loaders\"]) - 1\n            repre_names_last_idx = len(profile[\"repre_names_lowered\"]) - 1\n\n            repre_by_low_name = {\n                repre[\"name\"].lower(): repre for repre in repres\n            }\n\n            is_loaded = False\n            for repre_name_idx, profile_repre_name in enumerate(\n                profile[\"repre_names_lowered\"]\n            ):\n                # Break iteration if representation was already loaded\n                if is_loaded:\n                    break\n\n                repre = repre_by_low_name.get(profile_repre_name)\n                if not repre:\n                    continue\n\n                for loader_idx, loader_name in enumerate(profile[\"loaders\"]):\n                    if is_loaded:\n                        break\n\n                    loader = loaders_by_name.get(loader_name)\n                    if not loader:\n                        continue\n                    try:\n                        container = load_container(\n                            loader,\n                            repre[\"id\"],\n                            name=product_name\n                        )\n                        loaded_containers.append(container)\n                        is_loaded = True\n\n                    except Exception as exc:\n                        if exc == IncompatibleLoaderError:\n                            self.log.info((\n                                \"Loader `{}` is not compatible with\"\n                                \" representation `{}`\"\n                            ).format(loader_name, repre[\"name\"]))\n\n                        else:\n                            self.log.error(\n                                \"Unexpected error happened during loading\",\n                                exc_info=True\n                            )\n\n                        msg = \"Loading failed.\"\n                        if loader_idx &lt; loaders_last_idx:\n                            msg += \" Trying next loader.\"\n                        elif repre_name_idx &lt; repre_names_last_idx:\n                            msg += (\n                                \" Loading of product `{}` was not successful.\"\n                            ).format(product_name)\n                        else:\n                            msg += \" Trying next representation.\"\n                        self.log.info(msg)\n\n        return loaded_containers\n\n    def _collect_last_version_repres(self, folder_entities):\n        \"\"\"Collect products, versions and representations for folder_entities.\n\n        Args:\n            folder_entities (List[Dict[str, Any]]): Folder entities for which\n                want to find data.\n\n        Returns:\n            Dict[str, Any]: collected entities\n\n        Example output:\n        ```\n        {\n            &lt;folder id&gt;: {\n                \"folder_entity\": &lt;dict[str, Any]&gt;,\n                \"products\": {\n                    &lt;product id&gt;: {\n                        \"product_entity\": &lt;dict[str, Any]&gt;,\n                        \"version\": {\n                            \"version_entity\": &lt;VersionEntity&gt;,\n                            \"repres\": [\n                                &lt;RepreEntity1&gt;, &lt;RepreEntity2&gt;, ...\n                            ]\n                        }\n                    },\n                    ...\n                }\n            },\n            ...\n        }\n        output[folder_id][\"products\"][product_id][\"version\"][\"repres\"]\n        ```\n        \"\"\"\n\n        from ayon_core.pipeline.context_tools import get_current_project_name\n\n        output = {}\n        if not folder_entities:\n            return output\n\n        folder_entities_by_id = {\n            folder_entity[\"id\"]: folder_entity\n            for folder_entity in folder_entities\n        }\n\n        project_name = get_current_project_name()\n        product_entities = list(ayon_api.get_products(\n            project_name, folder_ids=folder_entities_by_id.keys()\n        ))\n        product_entities_by_id = {\n            product_entity[\"id\"]: product_entity\n            for product_entity in product_entities\n        }\n\n        last_version_by_product_id = ayon_api.get_last_versions(\n            project_name, product_entities_by_id.keys()\n        )\n        last_version_entities_by_id = {\n            version_entity[\"id\"]: version_entity\n            for version_entity in last_version_by_product_id.values()\n        }\n        repre_entities = ayon_api.get_representations(\n            project_name, version_ids=last_version_entities_by_id.keys()\n        )\n\n        for repre_entity in repre_entities:\n            version_id = repre_entity[\"versionId\"]\n            version_entity = last_version_entities_by_id[version_id]\n\n            product_id = version_entity[\"productId\"]\n            product_entity = product_entities_by_id[product_id]\n\n            folder_id = product_entity[\"folderId\"]\n            folder_entity = folder_entities_by_id[folder_id]\n\n            if folder_id not in output:\n                output[folder_id] = {\n                    \"folder_entity\": folder_entity,\n                    \"products\": {}\n                }\n\n            if product_id not in output[folder_id][\"products\"]:\n                output[folder_id][\"products\"][product_id] = {\n                    \"product_entity\": product_entity,\n                    \"version\": {\n                        \"version_entity\": version_entity,\n                        \"repres\": []\n                    }\n                }\n\n            (\n                output\n                [folder_id]\n                [\"products\"]\n                [product_id]\n                [\"version\"]\n                [\"repres\"]\n            ).append(repre_entity)\n\n        return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/index.html#client.ayon_core.pipeline.workfile.BuildWorkfile.build_workfile","title":"<code>build_workfile()</code>","text":"<p>Prepares and load containers into workfile.</p> <p>Loads latest versions of current and linked folders to workfile by logic stored in Workfile profiles from presets. Profiles are set by host, filtered by current task name and used by families.</p> <p>Each product base type can specify representation names and loaders for representations and first available and successful loaded representation is returned as container.</p> <p>At the end you'll get list of loaded containers per each folder.</p> <p>loaded_containers [{     \"folder_entity\": ,     \"containers\": [, , ...] }, {     \"folder_entity\": ,     \"containers\": [, ...] }, {     ... }] <p>Returns:</p> Type Description <p>list[dict[str, Any]]: Loaded containers during build.</p> Source code in <code>client/ayon_core/pipeline/workfile/build_workfile.py</code> <pre><code>def build_workfile(self):\n    \"\"\"Prepares and load containers into workfile.\n\n    Loads latest versions of current and linked folders to workfile by\n    logic stored in Workfile profiles from presets. Profiles are set\n    by host, filtered by current task name and used by families.\n\n    Each product base type can specify representation names and loaders\n    for representations and first available and successful loaded\n    representation is returned as container.\n\n    At the end you'll get list of loaded containers per each folder.\n\n    loaded_containers [{\n        \"folder_entity\": &lt;FolderEntity1&gt;,\n        \"containers\": [&lt;Container1&gt;, &lt;Container2&gt;, ...]\n    }, {\n        \"folder_entity\": &lt;FolderEntity2&gt;,\n        \"containers\": [&lt;Container3&gt;, ...]\n    }, {\n        ...\n    }]\n\n    Returns:\n        list[dict[str, Any]]: Loaded containers during build.\n\n    \"\"\"\n    loaded_containers = []\n\n    current_context = self.get_current_context()\n    project_name = current_context[\"project_name\"]\n    current_folder_path = current_context[\"folder_path\"]\n    current_task_name = current_context[\"task_name\"]\n\n    folder_entity = self.current_folder_entity\n    task_entity = self.current_task_entity\n    # Skip if folder was not found\n    if not folder_entity:\n        self.log.warning(\n            f\"Folder entity '{current_folder_path}' was not found\"\n        )\n        return loaded_containers\n\n    task_path = f\"{current_folder_path}/{current_task_name}\"\n    if not task_entity:\n        self.log.warning(f\"Task entity '{task_path}' was not found\")\n        return loaded_containers\n\n    # Prepare available loaders\n    loaders_by_name = {}\n    for loader in discover_loader_plugins():\n        if not loader.enabled:\n            continue\n        loader_name = loader.__name__\n        if loader_name in loaders_by_name:\n            raise KeyError(f\"Duplicated loader name {loader_name}!\")\n\n        loaders_by_name[loader_name] = loader\n\n    # Skip if there are any loaders\n    if not loaders_by_name:\n        self.log.warning(\"There are no registered loaders.\")\n        return loaded_containers\n\n    # Skip if there are any presets for task\n    if not self.build_presets:\n        self.log.warning(\n            f\"Current task '{task_path}' does not have any\"\n            \" loading preset.\"\n        )\n        return loaded_containers\n\n    # Get presets for loading current folder\n    current_context_profiles = self.build_presets.get(\"current_context\")\n    # Get presets for loading linked folders\n    link_context_profiles = self.build_presets.get(\"linked_folders\")\n    if not link_context_profiles:\n        # TODO remove 'linked_assets' (Marked as deprecated 26/02/05)\n        link_context_profiles = self.build_presets.get(\"linked_assets\")\n    # Skip if both are missing\n    if not current_context_profiles and not link_context_profiles:\n        self.log.warning(\n            f\"Current task '{task_path}' has empty\"\n            \" loading preset.\"\n        )\n        return loaded_containers\n\n    elif not current_context_profiles:\n        self.log.warning(\n            f\"Current task '{task_path}' doesn't have any loading\"\n            \" preset for it's context.\"\n        )\n\n    elif not link_context_profiles:\n        self.log.warning(\n            f\"Current task '{task_path}' doesn't have any\"\n            \"loading preset for it's linked folders.\"\n        )\n\n    # Prepare folders to process by workfile presets\n    folder_entities = []\n    current_folder_id = None\n    if current_context_profiles:\n        # Add current folder entity if preset has current context set\n        folder_entities.append(folder_entity)\n        current_folder_id = folder_entity[\"id\"]\n\n    if link_context_profiles:\n        # Find and append linked folders if preset has set linked mapping\n        linked_folder_entities = self._get_linked_folder_entities(\n            project_name, folder_entity[\"id\"]\n        )\n        if linked_folder_entities:\n            folder_entities.extend(linked_folder_entities)\n\n    # Skip if there are no folders. This can happen if only linked mapping\n    # is set and there are no links for his folder.\n    if not folder_entities:\n        self.log.warning(\n            \"Folder does not have linked folders. Nothing to process.\"\n        )\n        return loaded_containers\n\n    # Prepare entities from database for folders\n    prepared_entities = self._collect_last_version_repres(\n        folder_entities\n    )\n\n    # Load containers by prepared entities and presets\n    # - Current folder containers\n    if current_folder_id and current_folder_id in prepared_entities:\n        current_context_data = prepared_entities.pop(current_folder_id)\n        loaded_data = self.load_containers_by_folder_data(\n            current_context_data,\n            current_context_profiles,\n            loaders_by_name\n        )\n        if loaded_data:\n            loaded_containers.append(loaded_data)\n\n    # - Linked assets container\n    for linked_folder_data in prepared_entities.values():\n        loaded_data = self.load_containers_by_folder_data(\n            linked_folder_data,\n            link_context_profiles,\n            loaders_by_name\n        )\n        if loaded_data:\n            loaded_containers.append(loaded_data)\n\n    # Return list of loaded containers\n    return loaded_containers\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/index.html#client.ayon_core.pipeline.workfile.BuildWorkfile.get_build_presets","title":"<code>get_build_presets()</code>","text":"<p>Returns presets to build workfile for task name.</p> <p>Presets are loaded for current project received by 'get_current_project_name', filtered by registered host and entered task name.</p> <p>Returns:</p> Type Description <code>Optional[dict[str, Any]]</code> <p>dict[str, Any]: preset per entered task name</p> Source code in <code>client/ayon_core/pipeline/workfile/build_workfile.py</code> <pre><code>def get_build_presets(self) -&gt; Optional[dict[str, Any]]:\n    \"\"\" Returns presets to build workfile for task name.\n\n    Presets are loaded for current project received by\n    'get_current_project_name', filtered by registered host\n    and entered task name.\n\n\n    Returns:\n        dict[str, Any]: preset per entered task name\n\n    \"\"\"\n    profiles = self.get_build_profiles()\n    if not profiles:\n        return None\n\n    task_entity = self.current_task_entity\n    task_name = task_type = None\n    if task_entity:\n        task_name = task_entity[\"name\"]\n        task_type = task_entity[\"taskType\"]\n\n    filter_data = {\n        \"task_types\": task_type,\n        \"task_names\": task_name,\n        # Backwards compatibility\n        \"tasks\": task_name,\n    }\n    return filter_profiles(profiles, filter_data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/index.html#client.ayon_core.pipeline.workfile.BuildWorkfile.load_containers_by_folder_data","title":"<code>load_containers_by_folder_data(linked_folder_data, build_profiles, loaders_by_name)</code>","text":"<p>Load containers for entered folder entity by Build profiles.</p> <p>Parameters:</p> Name Type Description Default <code>linked_folder_data</code> <code>Dict[str, Any]</code> <p>Prepared data with products, last versions and representations for specific folder.</p> required <code>build_profiles</code> <code>Dict[str, Any]</code> <p>Build profiles.</p> required <code>loaders_by_name</code> <code>Dict[str, LoaderPlugin]</code> <p>Available loaders per name.</p> required <p>Returns:</p> Type Description <p>Dict[str, Any]: Output contains folder entity and loaded containers.</p> Source code in <code>client/ayon_core/pipeline/workfile/build_workfile.py</code> <pre><code>def load_containers_by_folder_data(\n    self, linked_folder_data, build_profiles, loaders_by_name\n):\n    \"\"\"Load containers for entered folder entity by Build profiles.\n\n    Args:\n        linked_folder_data (Dict[str, Any]): Prepared data with products,\n            last versions and representations for specific folder.\n        build_profiles (Dict[str, Any]): Build profiles.\n        loaders_by_name (Dict[str, LoaderPlugin]): Available loaders\n            per name.\n\n    Returns:\n        Dict[str, Any]: Output contains folder entity\n            and loaded containers.\n    \"\"\"\n\n    # Make sure all data are not empty\n    if not linked_folder_data or not build_profiles or not loaders_by_name:\n        return\n\n    folder_entity = linked_folder_data[\"folder_entity\"]\n\n    valid_profiles = self._filter_build_profiles(\n        build_profiles, loaders_by_name\n    )\n    if not valid_profiles:\n        self.log.warning(\n            \"There are not valid Workfile profiles. Skipping process.\"\n        )\n        return\n\n    self.log.debug(\"Valid Workfile profiles: {}\".format(valid_profiles))\n\n    products_by_id = {}\n    version_by_product_id = {}\n    repres_by_version_id = {}\n    for product_id, in_data in linked_folder_data[\"products\"].items():\n        product_entity = in_data[\"product_entity\"]\n        products_by_id[product_entity[\"id\"]] = product_entity\n\n        version_data = in_data[\"version\"]\n        version_entity = version_data[\"version_entity\"]\n        version_by_product_id[product_id] = version_entity\n        repres_by_version_id[version_entity[\"id\"]] = (\n            version_data[\"repres\"]\n        )\n\n    if not products_by_id:\n        self.log.warning(\"There are not products for folder {}\".format(\n            folder_entity[\"path\"]\n        ))\n        return\n\n    profiles_by_product_id = self._prepare_profile_for_products(\n        products_by_id.values(), valid_profiles\n    )\n    if not profiles_by_product_id:\n        self.log.warning(\"There are not valid products.\")\n        return\n\n    valid_repres_by_product_id = collections.defaultdict(list)\n    for product_id, profile in profiles_by_product_id.items():\n        profile_repre_names = profile[\"repre_names_lowered\"]\n\n        version_entity = version_by_product_id[product_id]\n        version_id = version_entity[\"id\"]\n        repres = repres_by_version_id[version_id]\n        for repre in repres:\n            repre_name_low = repre[\"name\"].lower()\n            if repre_name_low in profile_repre_names:\n                valid_repres_by_product_id[product_id].append(repre)\n\n    # DEBUG message\n    msg = \"Valid representations for Folder: `{}`\".format(\n        folder_entity[\"path\"]\n    )\n    for product_id, repres in valid_repres_by_product_id.items():\n        product_entity = products_by_id[product_id]\n        msg += \"\\n# Product Name/ID: `{}`/{}\".format(\n            product_entity[\"name\"], product_id\n        )\n        for repre in repres:\n            msg += \"\\n## Repre name: `{}`\".format(repre[\"name\"])\n\n    self.log.debug(msg)\n\n    containers = self._load_containers(\n        valid_repres_by_product_id,\n        products_by_id,\n        profiles_by_product_id,\n        loaders_by_name,\n    )\n\n    return {\n        \"folder_entity\": folder_entity,\n        \"containers\": containers\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/index.html#client.ayon_core.pipeline.workfile.BuildWorkfile.process","title":"<code>process()</code>","text":"<p>Main method of this wrapper.</p> <p>Building of workfile is triggered and is possible to implement post processing of loaded containers if necessary.</p> <p>Returns:</p> Type Description <p>list[dict[str, Any]]: Loaded containers during build.</p> Source code in <code>client/ayon_core/pipeline/workfile/build_workfile.py</code> <pre><code>def process(self):\n    \"\"\"Main method of this wrapper.\n\n    Building of workfile is triggered and is possible to implement\n    post processing of loaded containers if necessary.\n\n    Returns:\n        list[dict[str, Any]]: Loaded containers during build.\n\n    \"\"\"\n    self.apply_settings()\n    return self.build_workfile()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/index.html#client.ayon_core.pipeline.workfile.MissingWorkdirError","title":"<code>MissingWorkdirError</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Raised when accessing a work directory not found on disk.</p> Source code in <code>client/ayon_core/pipeline/workfile/utils.py</code> <pre><code>class MissingWorkdirError(Exception):\n    \"\"\"Raised when accessing a work directory not found on disk.\"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/index.html#client.ayon_core.pipeline.workfile.copy_workfile_to_context","title":"<code>copy_workfile_to_context(src_workfile_path, folder_entity, task_entity, *, version=None, comment=None, description=None, open_workfile=True, prepared_data=None)</code>","text":"<p>Copy workfile to a context.</p> <p>Copy workfile to a specified folder and task. Destination path is     calculated based on passed information.</p> <p>Parameters:</p> Name Type Description Default <code>src_workfile_path</code> <code>str</code> <p>Source workfile path.</p> required <code>folder_entity</code> <code>dict[str, Any]</code> <p>Target folder entity.</p> required <code>task_entity</code> <code>dict[str, Any]</code> <p>Target task entity.</p> required <code>version</code> <code>Optional[int]</code> <p>Workfile version. Use next version if not passed.</p> <code>None</code> <code>comment</code> <code>optional[str]</code> <p>Workfile comment.</p> <code>None</code> <code>description</code> <code>Optional[str]</code> <p>Workfile description.</p> <code>None</code> <code>prepared_data</code> <code>Optional[CopyWorkfileOptionalData]</code> <p>Prepared data for speed enhancements. Rootless path is calculated in this function.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/workfile/utils.py</code> <pre><code>def copy_workfile_to_context(\n    src_workfile_path: str,\n    folder_entity: dict[str, Any],\n    task_entity: dict[str, Any],\n    *,\n    version: Optional[int] = None,\n    comment: Optional[str] = None,\n    description: Optional[str] = None,\n    open_workfile: bool = True,\n    prepared_data: Optional[CopyWorkfileOptionalData] = None,\n) -&gt; None:\n    \"\"\"Copy workfile to a context.\n\n    Copy workfile to a specified folder and task. Destination path is\n        calculated based on passed information.\n\n    Args:\n        src_workfile_path (str): Source workfile path.\n        folder_entity (dict[str, Any]): Target folder entity.\n        task_entity (dict[str, Any]): Target task entity.\n        version (Optional[int]): Workfile version. Use next version if not\n            passed.\n        comment (optional[str]): Workfile comment.\n        description (Optional[str]): Workfile description.\n        prepared_data (Optional[CopyWorkfileOptionalData]): Prepared data\n            for speed enhancements. Rootless path is calculated in this\n            function.\n\n    \"\"\"\n    from ayon_core.pipeline import Anatomy\n    from ayon_core.pipeline.context_tools import registered_host\n\n    host = registered_host()\n    project_name = host.get_current_project_name()\n\n    anatomy = prepared_data.anatomy\n    if anatomy is None:\n        if prepared_data.project_entity is None:\n            prepared_data.project_entity = ayon_api.get_project(\n                project_name\n            )\n        anatomy = Anatomy(\n            project_name, project_entity=prepared_data.project_entity\n        )\n        prepared_data.anatomy = anatomy\n\n    project_settings = prepared_data.project_settings\n    if project_settings is None:\n        project_settings = get_project_settings(project_name)\n        prepared_data.project_settings = project_settings\n\n    if version is None:\n        list_prepared_data = None\n        if prepared_data is not None:\n            list_prepared_data = ListWorkfilesOptionalData(\n                project_entity=prepared_data.project_entity,\n                anatomy=prepared_data.anatomy,\n                project_settings=prepared_data.project_settings,\n                workfile_entities=prepared_data.workfile_entities,\n            )\n\n        workfiles = host.list_workfiles(\n            project_name,\n            folder_entity,\n            task_entity,\n            prepared_data=list_prepared_data\n        )\n        if workfiles:\n            version = max(\n                workfile.version\n                for workfile in workfiles\n            ) + 1\n        else:\n            version = get_versioning_start(\n                project_name,\n                host.name,\n                task_name=task_entity[\"name\"],\n                task_type=task_entity[\"taskType\"],\n                product_base_type=\"workfile\",\n            )\n\n    task_type = task_entity[\"taskType\"]\n    template_key = get_workfile_template_key(\n        project_name,\n        task_type,\n        host.name,\n        project_settings=prepared_data.project_settings\n    )\n\n    template_data = get_template_data(\n        prepared_data.project_entity,\n        folder_entity,\n        task_entity,\n        host.name,\n        prepared_data.project_settings,\n    )\n    template_data[\"version\"] = version\n    if comment:\n        template_data[\"comment\"] = comment\n\n    workfile_extensions = host.get_workfile_extensions()\n    if workfile_extensions:\n        ext = os.path.splitext(src_workfile_path)[1].lstrip(\".\")\n        template_data[\"ext\"] = ext\n\n    workfile_template = anatomy.get_template_item(\n        \"work\", template_key, \"path\"\n    )\n    workfile_path = workfile_template.format_strict(template_data)\n    prepared_data.rootless_path = workfile_path.rootless\n    host.copy_workfile(\n        src_workfile_path,\n        workfile_path,\n        folder_entity,\n        task_entity,\n        version=version,\n        comment=comment,\n        description=description,\n        open_workfile=open_workfile,\n        prepared_data=prepared_data,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/index.html#client.ayon_core.pipeline.workfile.create_workdir_extra_folders","title":"<code>create_workdir_extra_folders(workdir, host_name, task_type, task_name, project_name, project_settings=None)</code>","text":"<p>Create extra folders in work directory based on context.</p> <p>Parameters:</p> Name Type Description Default <code>workdir</code> <code>str</code> <p>Path to workdir where workfiles is stored.</p> required <code>host_name</code> <code>str</code> <p>Name of host implementation.</p> required <code>task_type</code> <code>str</code> <p>Type of task for which extra folders should be created.</p> required <code>task_name</code> <code>str</code> <p>Name of task for which extra folders should be created.</p> required <code>project_name</code> <code>str</code> <p>Name of project on which task is.</p> required <code>project_settings</code> <code>dict</code> <p>Prepared project settings. Are loaded if not passed.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/workfile/path_resolving.py</code> <pre><code>def create_workdir_extra_folders(\n    workdir,\n    host_name,\n    task_type,\n    task_name,\n    project_name,\n    project_settings=None\n):\n    \"\"\"Create extra folders in work directory based on context.\n\n    Args:\n        workdir (str): Path to workdir where workfiles is stored.\n        host_name (str): Name of host implementation.\n        task_type (str): Type of task for which extra folders should be\n            created.\n        task_name (str): Name of task for which extra folders should be\n            created.\n        project_name (str): Name of project on which task is.\n        project_settings (dict): Prepared project settings. Are loaded if not\n            passed.\n    \"\"\"\n\n    # Load project settings if not set\n    if not project_settings:\n        project_settings = get_project_settings(project_name)\n\n    # Load extra folders profiles\n    extra_folders_profiles = (\n        project_settings[\"core\"][\"tools\"][\"Workfiles\"][\"extra_folders\"]\n    )\n    # Skip if are empty\n    if not extra_folders_profiles:\n        return\n\n    # Prepare profiles filters\n    filter_data = {\n        \"task_types\": task_type,\n        \"task_names\": task_name,\n        \"host_names\": host_name,\n    }\n    profile = filter_profiles(extra_folders_profiles, filter_data)\n    if profile is None:\n        return\n\n    for subfolder in profile[\"folders\"]:\n        # Make sure backslashes are converted to forwards slashes\n        #   and does not start with slash\n        subfolder = subfolder.replace(\"\\\\\", \"/\").lstrip(\"/\")\n        # Skip empty strings\n        if not subfolder:\n            continue\n\n        fullpath = os.path.join(workdir, subfolder)\n        if not os.path.exists(fullpath):\n            os.makedirs(fullpath)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/index.html#client.ayon_core.pipeline.workfile.find_workfile_rootless_path","title":"<code>find_workfile_rootless_path(workfile_path, project_name, folder_entity, task_entity, host_name, *, project_entity=None, project_settings=None, anatomy=None)</code>","text":"<p>Find rootless workfile path.</p> Source code in <code>client/ayon_core/pipeline/workfile/utils.py</code> <pre><code>def find_workfile_rootless_path(\n    workfile_path: str,\n    project_name: str,\n    folder_entity: dict[str, Any],\n    task_entity: dict[str, Any],\n    host_name: str,\n    *,\n    project_entity: Optional[dict[str, Any]] = None,\n    project_settings: Optional[dict[str, Any]] = None,\n    anatomy: Optional[\"Anatomy\"] = None,\n) -&gt; str:\n    \"\"\"Find rootless workfile path.\"\"\"\n    if anatomy is None:\n        from ayon_core.pipeline import Anatomy\n\n        anatomy = Anatomy(project_name, project_entity=project_entity)\n\n    task_type = task_entity[\"taskType\"]\n    template_key = get_workfile_template_key(\n        project_name,\n        task_type,\n        host_name,\n        project_settings=project_settings\n    )\n    dir_template = anatomy.get_template_item(\n        \"work\", template_key, \"directory\"\n    )\n    result = dir_template.format({\"root\": anatomy.roots})\n    used_root = result.used_values.get(\"root\")\n    rootless_path = str(workfile_path)\n    if platform.system().lower() == \"windows\":\n        rootless_path = rootless_path.replace(\"\\\\\", \"/\")\n\n    root_key = root_value = None\n    if used_root is not None:\n        root_key, root_value = next(iter(used_root.items()))\n        if platform.system().lower() == \"windows\":\n            root_value = root_value.replace(\"\\\\\", \"/\")\n\n    if root_value and rootless_path.startswith(root_value):\n        rootless_path = rootless_path[len(root_value):].lstrip(\"/\")\n        rootless_path = f\"{{root[{root_key}]}}/{rootless_path}\"\n    else:\n        success, result = anatomy.find_root_template_from_path(rootless_path)\n        if success:\n            rootless_path = result\n    return rootless_path\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/index.html#client.ayon_core.pipeline.workfile.get_comments_from_workfile_paths","title":"<code>get_comments_from_workfile_paths(filepaths, extensions, file_template, template_data, current_filename=None)</code>","text":"<p>DEPRECATED Collect comments from workfile filenames.</p> <p>Based on 'current_filename' is also returned \"current comment\".</p> <p>Parameters:</p> Name Type Description Default <code>filepaths</code> <code>list[str]</code> <p>List of filepaths to parse.</p> required <code>extensions</code> <code>set[str]</code> <p>Set of file extensions.</p> required <code>file_template</code> <code>StringTemplate</code> <p>Workfile file template.</p> required <code>template_data</code> <code>dict[str, Any]</code> <p>Data to fill the template with.</p> required <code>current_filename</code> <code>str</code> <p>Filename to check for the current comment.</p> <code>None</code> <p>Returns:</p> Type Description <code>tuple[list[str], str]</code> <p>tuple[list[str], str]: List of comments and the current comment.</p> Source code in <code>client/ayon_core/pipeline/workfile/path_resolving.py</code> <pre><code>def get_comments_from_workfile_paths(\n    filepaths: list[str],\n    extensions: set[str],\n    file_template: StringTemplate,\n    template_data: dict[str, Any],\n    current_filename: Optional[str] = None,\n) -&gt; tuple[list[str], str]:\n    \"\"\"DEPRECATED Collect comments from workfile filenames.\n\n    Based on 'current_filename' is also returned \"current comment\".\n\n    Args:\n        filepaths (list[str]): List of filepaths to parse.\n        extensions (set[str]): Set of file extensions.\n        file_template (StringTemplate): Workfile file template.\n        template_data (dict[str, Any]): Data to fill the template with.\n        current_filename (str): Filename to check for the current comment.\n\n    Returns:\n        tuple[list[str], str]: List of comments and the current comment.\n\n    \"\"\"\n    warnings.warn(\n        \"Function 'get_comments_from_workfile_paths' is deprecated. Please\"\n        \" use 'parse_dynamic_data_from_workfiles' instead.\",\n        DeprecationWarning,\n        stacklevel=2,\n    )\n    current_comment = \"\"\n    if not filepaths:\n        return [], current_comment\n\n    matcher = CommentMatcher(extensions, file_template, template_data)\n\n    comment_hints = set()\n    for filepath in filepaths:\n        filename = os.path.basename(filepath)\n        comment = matcher.parse_comment(filename)\n        if comment:\n            comment_hints.add(comment)\n            if filename == current_filename:\n                current_comment = comment\n\n    return list(comment_hints), current_comment\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/index.html#client.ayon_core.pipeline.workfile.get_custom_workfile_template","title":"<code>get_custom_workfile_template(project_entity, folder_entity, task_entity, host_name, anatomy=None, project_settings=None)</code>","text":"<p>Filter and fill workfile template profiles by passed context.</p> <p>Custom workfile template can be used as first version of workfiles. Template is a file on a disk which is set in settings. Expected settings structure to have this feature enabled is: project settings |-    |- workfile_builder     |- create_first_version   - a bool which must be set to 'True'     |- custom_templates       - profiles based on task name/type which                                 points to a file which is copied as                                 first workfile <p>It is expected that passed argument are already queried entities of project and folder as parents of processing task name.</p> <p>Parameters:</p> Name Type Description Default <code>project_entity</code> <code>Dict[str, Any]</code> <p>Project entity.</p> required <code>folder_entity</code> <code>Dict[str, Any]</code> <p>Folder entity.</p> required <code>task_entity</code> <code>Dict[str, Any]</code> <p>Task entity.</p> required <code>host_name</code> <code>str</code> <p>Name of host.</p> required <code>anatomy</code> <code>Anatomy</code> <p>Optionally passed anatomy object for passed project name.</p> <code>None</code> <code>project_settings</code> <code>Dict[str, Any]</code> <p>Preloaded project settings.</p> <code>None</code> <p>Returns:</p> Type Description <p>Optional[str]: Path to template or None if none of profiles match current context. Existence of formatted path is not validated.</p> Source code in <code>client/ayon_core/pipeline/workfile/path_resolving.py</code> <pre><code>def get_custom_workfile_template(\n    project_entity,\n    folder_entity,\n    task_entity,\n    host_name,\n    anatomy=None,\n    project_settings=None\n):\n    \"\"\"Filter and fill workfile template profiles by passed context.\n\n    Custom workfile template can be used as first version of workfiles.\n    Template is a file on a disk which is set in settings. Expected settings\n    structure to have this feature enabled is:\n    project settings\n    |- &lt;host name&gt;\n      |- workfile_builder\n        |- create_first_version   - a bool which must be set to 'True'\n        |- custom_templates       - profiles based on task name/type which\n                                    points to a file which is copied as\n                                    first workfile\n\n    It is expected that passed argument are already queried entities of\n    project and folder as parents of processing task name.\n\n    Args:\n        project_entity (Dict[str, Any]): Project entity.\n        folder_entity (Dict[str, Any]): Folder entity.\n        task_entity (Dict[str, Any]): Task entity.\n        host_name (str): Name of host.\n        anatomy (Anatomy): Optionally passed anatomy object for passed project\n            name.\n        project_settings(Dict[str, Any]): Preloaded project settings.\n\n    Returns:\n        Optional[str]: Path to template or None if none of profiles match\n            current context. Existence of formatted path is not validated.\n\n    \"\"\"\n    log = Logger.get_logger(\"CustomWorkfileResolve\")\n\n    project_name = project_entity[\"name\"]\n    if project_settings is None:\n        project_settings = get_project_settings(project_name)\n\n    host_settings = project_settings.get(host_name)\n    if not host_settings:\n        log.info(\"Host \\\"{}\\\" doesn't have settings\".format(host_name))\n        return None\n\n    workfile_builder_settings = host_settings.get(\"workfile_builder\")\n    if not workfile_builder_settings:\n        log.info((\n            \"Seems like old version of settings is used.\"\n            \" Can't access custom templates in host \\\"{}\\\".\"\n        ).format(host_name))\n        return\n\n    if not workfile_builder_settings[\"create_first_version\"]:\n        log.info((\n            \"Project \\\"{}\\\" has turned off to create first workfile for\"\n            \" host \\\"{}\\\"\"\n        ).format(project_name, host_name))\n        return\n\n    # Backwards compatibility\n    template_profiles = workfile_builder_settings.get(\"custom_templates\")\n    if not template_profiles:\n        log.info(\n            \"Custom templates are not filled. Skipping template copy.\"\n        )\n        return\n\n    if anatomy is None:\n        anatomy = Anatomy(project_name)\n\n    # get project, folder, task anatomy context data\n    anatomy_context_data = get_template_data(\n        project_entity, folder_entity, task_entity, host_name\n    )\n    # add root dict\n    anatomy_context_data[\"root\"] = anatomy.roots\n\n    # get task type for the task in context\n    current_task_type = anatomy_context_data[\"task\"][\"type\"]\n\n    # get path from matching profile\n    matching_item = filter_profiles(\n        template_profiles,\n        {\"task_types\": current_task_type}\n    )\n    # when path is available try to format it in case\n    # there are some anatomy template strings\n    if matching_item:\n        # extend anatomy context with os.environ to\n        # also allow formatting against env\n        full_context_data = os.environ.copy()\n        full_context_data.update(anatomy_context_data)\n\n        template = matching_item[\"path\"][platform.system().lower()]\n        return StringTemplate.format_strict_template(\n            template, full_context_data\n        ).normalized()\n\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/index.html#client.ayon_core.pipeline.workfile.get_custom_workfile_template_by_string_context","title":"<code>get_custom_workfile_template_by_string_context(project_name, folder_path, task_name, host_name, anatomy=None, project_settings=None)</code>","text":"<p>Filter and fill workfile template profiles by passed context.</p> <p>Passed context are string representations of project, folder and task. Function will query documents of project and folder to be able to use <code>get_custom_workfile_template</code> for rest of logic.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>folder_path</code> <code>str</code> <p>Folder path.</p> required <code>task_name</code> <code>str</code> <p>Task name.</p> required <code>host_name</code> <code>str</code> <p>Name of host.</p> required <code>anatomy</code> <code>Anatomy</code> <p>Optionally prepared anatomy object for passed project.</p> <code>None</code> <code>project_settings</code> <code>Dict[str, Any]</code> <p>Preloaded project settings.</p> <code>None</code> <p>Returns:</p> Type Description <p>Union[str, None]: Path to template or None if none of profiles match current context. (Existence of formatted path is not validated.)</p> Source code in <code>client/ayon_core/pipeline/workfile/path_resolving.py</code> <pre><code>def get_custom_workfile_template_by_string_context(\n    project_name,\n    folder_path,\n    task_name,\n    host_name,\n    anatomy=None,\n    project_settings=None\n):\n    \"\"\"Filter and fill workfile template profiles by passed context.\n\n    Passed context are string representations of project, folder and task.\n    Function will query documents of project and folder to be able to use\n    `get_custom_workfile_template` for rest of logic.\n\n    Args:\n        project_name (str): Project name.\n        folder_path (str): Folder path.\n        task_name (str): Task name.\n        host_name (str): Name of host.\n        anatomy (Anatomy): Optionally prepared anatomy object for passed\n            project.\n        project_settings (Dict[str, Any]): Preloaded project settings.\n\n    Returns:\n        Union[str, None]: Path to template or None if none of profiles match\n            current context. (Existence of formatted path is not validated.)\n\n    \"\"\"\n\n    project_entity = ayon_api.get_project(project_name)\n    folder_entity = ayon_api.get_folder_by_path(project_name, folder_path)\n    task_entity = ayon_api.get_task_by_name(\n        project_name, folder_entity[\"id\"], task_name\n    )\n\n    return get_custom_workfile_template(\n        project_entity,\n        folder_entity,\n        task_entity,\n        host_name,\n        anatomy,\n        project_settings\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/index.html#client.ayon_core.pipeline.workfile.get_last_workfile","title":"<code>get_last_workfile(workdir, file_template, template_data, extensions, full_path=False)</code>","text":"<p>Return last the workfile filename.</p> <p>Returns first file name/path if there are not workfiles yet.</p> <p>Parameters:</p> Name Type Description Default <code>workdir</code> <code>str</code> <p>Path to dir where workfiles are stored.</p> required <code>file_template</code> <code>str</code> <p>Template of file name.</p> required <code>template_data</code> <code>Dict[str, Any]</code> <p>Data for filling template.</p> required <code>extensions</code> <code>Iterable[str]</code> <p>All allowed file extensions of workfile.</p> required <code>full_path</code> <code>bool</code> <p>Return full path to the file or only filename.</p> <code>False</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Last or first workfile file name or path based on 'full_path' value.</p> Source code in <code>client/ayon_core/pipeline/workfile/path_resolving.py</code> <pre><code>def get_last_workfile(\n    workdir: str,\n    file_template: str,\n    template_data: dict[str, Any],\n    extensions: set[str],\n    full_path: bool = False,\n) -&gt; str:\n    \"\"\"Return last the workfile filename.\n\n    Returns first file name/path if there are not workfiles yet.\n\n    Args:\n        workdir (str): Path to dir where workfiles are stored.\n        file_template (str): Template of file name.\n        template_data (Dict[str, Any]): Data for filling template.\n        extensions (Iterable[str]): All allowed file extensions of workfile.\n        full_path (bool): Return full path to the file or only filename.\n\n    Returns:\n        str: Last or first workfile file name or path based on\n            'full_path' value.\n\n    \"\"\"\n    # TODO (iLLiCiTiT): Remove the argument 'full_path' and return only full\n    #   path. As far as I can tell it is always called with 'full_path' set\n    #   to 'True'.\n    # - it has to be 2 step operation, first warn about having it 'False', and\n    #   then warn about having it filled.\n    if full_path is False:\n        warnings.warn(\n            \"Argument 'full_path' will be removed and will return\"\n            \" only full path in future.\",\n            DeprecationWarning,\n        )\n\n    filepaths, dotted_extensions = _filter_dir_files_by_ext(\n        workdir, extensions\n    )\n    filepath = get_last_workfile_from_paths(\n        filepaths,\n        file_template,\n        template_data,\n        dotted_extensions\n    )\n    if filepath is None:\n        data = copy.deepcopy(template_data)\n        host_name = data.get(\"host\", {}).get(\"name\")\n        if host_name is None:\n            host_name = data[\"app\"]\n        data[\"version\"] = version_start.get_versioning_start(\n            data[\"project\"][\"name\"],\n            host_name,\n            task_name=data[\"task\"][\"name\"],\n            task_type=data[\"task\"][\"type\"],\n            product_base_type=\"workfile\",\n        )\n        data.pop(\"comment\", None)\n        if data.get(\"ext\") is None:\n            data[\"ext\"] = next(iter(extensions), \"\")\n        data[\"ext\"] = data[\"ext\"].lstrip(\".\")\n        filename = StringTemplate.format_strict_template(file_template, data)\n        filepath = os.path.join(workdir, filename)\n\n    if full_path:\n        return os.path.normpath(filepath)\n    return os.path.basename(filepath)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/index.html#client.ayon_core.pipeline.workfile.get_last_workfile_from_paths","title":"<code>get_last_workfile_from_paths(filepaths, file_template, template_data, extensions)</code>","text":"<p>Return the last workfile filename.</p> <p>Returns the file with version 1 if there is not workfile yet.</p> <p>Parameters:</p> Name Type Description Default <code>filepaths</code> <code>list[str]</code> <p>Paths to workfiles.</p> required <code>file_template</code> <code>str</code> <p>Template of file name.</p> required <code>template_data</code> <code>dict[str, Any]</code> <p>Data for filling template.</p> required <code>extensions</code> <code>set[str]</code> <p>All allowed file extensions of workfile.</p> required <p>Returns:</p> Type Description <code>Optional[str]</code> <p>Optional[str]: Last workfile path.</p> Source code in <code>client/ayon_core/pipeline/workfile/path_resolving.py</code> <pre><code>def get_last_workfile_from_paths(\n    filepaths: list[str],\n    file_template: str,\n    template_data: dict[str, Any],\n    extensions: set[str],\n) -&gt; Optional[str]:\n    \"\"\"Return the last workfile filename.\n\n    Returns the file with version 1 if there is not workfile yet.\n\n    Args:\n        filepaths (list[str]): Paths to workfiles.\n        file_template (str): Template of file name.\n        template_data (dict[str, Any]): Data for filling template.\n        extensions (set[str]): All allowed file extensions of workfile.\n\n    Returns:\n        Optional[str]: Last workfile path.\n\n    \"\"\"\n    filepath, _version = get_last_workfile_with_version_from_paths(\n        filepaths, file_template, template_data, extensions\n    )\n    return filepath\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/index.html#client.ayon_core.pipeline.workfile.get_last_workfile_with_version","title":"<code>get_last_workfile_with_version(workdir, file_template, template_data, extensions)</code>","text":"<p>Return last workfile version.</p> <p>Using the workfile template and its filling data to find the most possible last version of workfile which was created for the context.</p> <p>Functionality is fully based on knowing which keys are optional or what values are expected as value.</p> <p>The last modified file is used if more files can be considered as last workfile.</p> <p>Parameters:</p> Name Type Description Default <code>workdir</code> <code>str</code> <p>Path to dir where workfiles are stored.</p> required <code>file_template</code> <code>str</code> <p>Template of file name.</p> required <code>template_data</code> <code>dict[str, Any]</code> <p>Data for filling template.</p> required <code>extensions</code> <code>set[str]</code> <p>All allowed file extensions of workfile.</p> required <p>Returns:</p> Type Description <code>tuple[Optional[str], Optional[int]]</code> <p>tuple[Optional[str], Optional[int]]: Last workfile with version if there is any workfile otherwise None for both.</p> Source code in <code>client/ayon_core/pipeline/workfile/path_resolving.py</code> <pre><code>def get_last_workfile_with_version(\n    workdir: str,\n    file_template: str,\n    template_data: dict[str, Any],\n    extensions: set[str],\n) -&gt; tuple[Optional[str], Optional[int]]:\n    \"\"\"Return last workfile version.\n\n    Using the workfile template and its filling data to find the most possible\n    last version of workfile which was created for the context.\n\n    Functionality is fully based on knowing which keys are optional or what\n    values are expected as value.\n\n    The last modified file is used if more files can be considered as\n    last workfile.\n\n    Args:\n        workdir (str): Path to dir where workfiles are stored.\n        file_template (str): Template of file name.\n        template_data (dict[str, Any]): Data for filling template.\n        extensions (set[str]): All allowed file extensions of workfile.\n\n    Returns:\n        tuple[Optional[str], Optional[int]]: Last workfile with version\n            if there is any workfile otherwise None for both.\n\n    \"\"\"\n    if not os.path.exists(workdir):\n        return None, None\n\n    filepaths, dotted_extensions = _filter_dir_files_by_ext(\n        workdir, extensions\n    )\n\n    return get_last_workfile_with_version_from_paths(\n        filepaths,\n        file_template,\n        template_data,\n        dotted_extensions,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/index.html#client.ayon_core.pipeline.workfile.get_last_workfile_with_version_from_paths","title":"<code>get_last_workfile_with_version_from_paths(filepaths, file_template, template_data, extensions)</code>","text":"<p>Return last workfile version.</p> <p>Using the workfile template and its template data find most possible last version of workfile which was created for the context.</p> <p>Functionality is fully based on knowing which keys are optional or what values are expected as value.</p> <p>The last modified file is used if more files can be considered as last workfile.</p> <p>Parameters:</p> Name Type Description Default <code>filepaths</code> <code>list[str]</code> <p>Workfile paths.</p> required <code>file_template</code> <code>str</code> <p>Template of file name.</p> required <code>template_data</code> <code>Dict[str, Any]</code> <p>Data for filling template.</p> required <code>extensions</code> <code>set[str]</code> <p>All allowed file extensions of workfile.</p> required <p>Returns:</p> Type Description <code>tuple[Optional[str], Optional[int]]</code> <p>tuple[Optional[str], Optional[int]]: Last workfile with version if there is any workfile otherwise None for both.</p> Source code in <code>client/ayon_core/pipeline/workfile/path_resolving.py</code> <pre><code>def get_last_workfile_with_version_from_paths(\n    filepaths: list[str],\n    file_template: str,\n    template_data: dict[str, Any],\n    extensions: set[str],\n) -&gt; tuple[Optional[str], Optional[int]]:\n    \"\"\"Return last workfile version.\n\n    Using the workfile template and its template data find most possible last\n    version of workfile which was created for the context.\n\n    Functionality is fully based on knowing which keys are optional or what\n    values are expected as value.\n\n    The last modified file is used if more files can be considered as\n    last workfile.\n\n    Args:\n        filepaths (list[str]): Workfile paths.\n        file_template (str): Template of file name.\n        template_data (Dict[str, Any]): Data for filling template.\n        extensions (set[str]): All allowed file extensions of workfile.\n\n    Returns:\n        tuple[Optional[str], Optional[int]]: Last workfile with version\n            if there is any workfile otherwise None for both.\n\n    \"\"\"\n    if not filepaths:\n        return None, None\n\n    dotted_extensions = set()\n    for ext in extensions:\n        if not ext.startswith(\".\"):\n            ext = f\".{ext}\"\n        dotted_extensions.add(re.escape(ext))\n\n    # Build template without optionals, version to digits only regex\n    # and comment to any definable value.\n    # Escape extensions dot for regex\n    ext_expression = \"(?:\" + \"|\".join(dotted_extensions) + \")\"\n\n    for pattern, replacement in (\n        # Replace `.{ext}` with `{ext}` so we are sure dot is not at the end\n        (r\"\\.?{ext}\", ext_expression),\n        # Replace optional keys with optional content regex\n        (r\"&lt;.*?&gt;\", r\".*?\"),\n        # Replace `{version}` with group regex\n        (r\"{version.*?}\", r\"([0-9]+)\"),\n        (r\"{comment.*?}\", r\".+?\"),\n    ):\n        file_template = re.sub(pattern, replacement, file_template)\n\n    file_template = StringTemplate.format_strict_template(\n        file_template, template_data\n    )\n\n    # Match with ignore case on Windows due to the Windows\n    # OS not being case-sensitive. This avoids later running\n    # into the error that the file did exist if it existed\n    # with a different upper/lower-case.\n    kwargs = {}\n    if platform.system().lower() == \"windows\":\n        kwargs[\"flags\"] = re.IGNORECASE\n\n    # Get highest version among existing matching files\n    version = None\n    output_filepaths = []\n    for filepath in sorted(filepaths):\n        filename = os.path.basename(filepath)\n        match = re.match(file_template, filename, **kwargs)\n        if not match:\n            continue\n\n        if not match.groups():\n            output_filepaths.append(filename)\n            continue\n\n        file_version = int(match.group(1))\n        if version is None or file_version &gt; version:\n            output_filepaths.clear()\n            version = file_version\n\n        if file_version == version:\n            output_filepaths.append(filepath)\n\n    # Use file modification time to use most recent file if there are\n    #   multiple workfiles with the same version\n    output_filepath = None\n    last_time = None\n    for _output_filepath in output_filepaths:\n        mod_time = None\n        if os.path.exists(_output_filepath):\n            mod_time = os.path.getmtime(_output_filepath)\n        if (\n            last_time is None\n            or (mod_time is not None and last_time &lt; mod_time)\n        ):\n            output_filepath = _output_filepath\n            last_time = mod_time\n\n    return output_filepath, version\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/index.html#client.ayon_core.pipeline.workfile.get_workdir","title":"<code>get_workdir(project_entity, folder_entity, task_entity, host_name, anatomy=None, template_key=None, project_settings=None)</code>","text":"<p>Fill workdir path from entered data and project's anatomy.</p> <p>Parameters:</p> Name Type Description Default <code>project_entity</code> <code>Dict[str, Any]</code> <p>Project entity.</p> required <code>folder_entity</code> <code>Dict[str, Any]</code> <p>Folder entity.</p> required <code>task_entity</code> <code>dict[str, Any]</code> <p>Task entity.</p> required <code>host_name</code> <code>str</code> <p>Host which is used to workdir. This is required because workdir template may contain <code>{app}</code> key. In <code>Session</code> is stored under <code>AYON_HOST_NAME</code> key.</p> required <code>anatomy</code> <code>Anatomy</code> <p>Optional argument. Anatomy object is created using project name from <code>project_entity</code>. It is preferred to pass this argument as initialization of a new Anatomy object may be time-consuming.</p> <code>None</code> <code>template_key</code> <code>str</code> <p>Key of work templates in anatomy templates. Default value is defined in <code>get_workdir_with_workdir_data</code>.</p> <code>None</code> <code>project_settings</code> <code>Dict[str, Any]</code> <p>Prepared project settings for project name. Optional to make processing faster. Ans id used only if 'template_key' is not passed.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>AnatomyTemplateResult</code> <code>'AnatomyTemplateResult'</code> <p>Workdir path.</p> Source code in <code>client/ayon_core/pipeline/workfile/path_resolving.py</code> <pre><code>def get_workdir(\n    project_entity: dict[str, Any],\n    folder_entity: dict[str, Any],\n    task_entity: dict[str, Any],\n    host_name: str,\n    anatomy=None,\n    template_key=None,\n    project_settings=None\n) -&gt; \"AnatomyTemplateResult\":\n    \"\"\"Fill workdir path from entered data and project's anatomy.\n\n    Args:\n        project_entity (Dict[str, Any]): Project entity.\n        folder_entity (Dict[str, Any]): Folder entity.\n        task_entity (dict[str, Any]): Task entity.\n        host_name (str): Host which is used to workdir. This is required\n            because workdir template may contain `{app}` key. In `Session`\n            is stored under `AYON_HOST_NAME` key.\n        anatomy (Anatomy): Optional argument. Anatomy object is created using\n            project name from `project_entity`. It is preferred to pass this\n            argument as initialization of a new Anatomy object may be\n            time-consuming.\n        template_key (str): Key of work templates in anatomy templates. Default\n            value is defined in `get_workdir_with_workdir_data`.\n        project_settings(Dict[str, Any]): Prepared project settings for\n            project name. Optional to make processing faster. Ans id used only\n            if 'template_key' is not passed.\n\n    Returns:\n        AnatomyTemplateResult: Workdir path.\n\n    \"\"\"\n    if not anatomy:\n        anatomy = Anatomy(\n            project_entity[\"name\"], project_entity=project_entity\n        )\n\n    workdir_data = get_template_data(\n        project_entity,\n        folder_entity,\n        task_entity,\n        host_name,\n    )\n    # Output is AnatomyTemplateResult object which contain useful data\n    return get_workdir_with_workdir_data(\n        workdir_data,\n        anatomy.project_name,\n        anatomy,\n        template_key,\n        project_settings\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/index.html#client.ayon_core.pipeline.workfile.get_workdir_with_workdir_data","title":"<code>get_workdir_with_workdir_data(workdir_data, project_name, anatomy=None, template_key=None, project_settings=None)</code>","text":"<p>Fill workdir path from entered data and project's anatomy.</p> <p>It is possible to pass only project's name instead of project's anatomy but one of them must be entered. It is preferred to enter anatomy if is available as initialization of a new Anatomy object may be time consuming.</p> <p>Parameters:</p> Name Type Description Default <code>workdir_data</code> <code>Dict[str, Any]</code> <p>Data to fill workdir template.</p> required <code>project_name</code> <code>str</code> <p>Project's name.</p> required <code>anatomy</code> <code>Anatomy</code> <p>Anatomy object for specific project. Faster processing if is passed.</p> <code>None</code> <code>template_key</code> <code>str</code> <p>Key of work templates in anatomy templates. If not passed <code>get_workfile_template_key_from_context</code> is used to get it.</p> <code>None</code> <code>project_settings</code> <code>Dict[str, Any]</code> <p>Prepared project settings for project name. Optional to make processing faster. Ans id used only if 'template_key' is not passed.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>AnatomyTemplateResult</code> <code>'AnatomyTemplateResult'</code> <p>Workdir path.</p> Source code in <code>client/ayon_core/pipeline/workfile/path_resolving.py</code> <pre><code>def get_workdir_with_workdir_data(\n    workdir_data,\n    project_name,\n    anatomy=None,\n    template_key=None,\n    project_settings=None\n) -&gt; \"AnatomyTemplateResult\":\n    \"\"\"Fill workdir path from entered data and project's anatomy.\n\n    It is possible to pass only project's name instead of project's anatomy but\n    one of them **must** be entered. It is preferred to enter anatomy if is\n    available as initialization of a new Anatomy object may be time consuming.\n\n    Args:\n        workdir_data (Dict[str, Any]): Data to fill workdir template.\n        project_name (str): Project's name.\n        anatomy (Anatomy): Anatomy object for specific project. Faster\n            processing if is passed.\n        template_key (str): Key of work templates in anatomy templates. If not\n            passed `get_workfile_template_key_from_context` is used to get it.\n        project_settings(Dict[str, Any]): Prepared project settings for\n            project name. Optional to make processing faster. Ans id used only\n            if 'template_key' is not passed.\n\n    Returns:\n        AnatomyTemplateResult: Workdir path.\n\n    \"\"\"\n    if not anatomy:\n        anatomy = Anatomy(project_name)\n\n    if not template_key:\n        host_name = workdir_data.get(\"host\", {}).get(\"name\")\n        if host_name is None:\n            host_name = workdir_data[\"app\"]\n        template_key = get_workfile_template_key(\n            workdir_data[\"project\"][\"name\"],\n            workdir_data[\"task\"][\"type\"],\n            host_name,\n            project_settings,\n        )\n\n    template_obj = anatomy.get_template_item(\n        \"work\", template_key, \"directory\"\n    )\n    # Output is AnatomyTemplateResult object which contain useful data\n    output = template_obj.format_strict(workdir_data)\n    if output:\n        return output.normalized()\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/index.html#client.ayon_core.pipeline.workfile.get_workfile_template_key","title":"<code>get_workfile_template_key(project_name, task_type, host_name, project_settings=None)</code>","text":"<p>Workfile template key which should be used to get workfile template.</p> <p>Function is using profiles from project settings to return right template for passed task type and host name.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>task_type</code> <code>str</code> <p>Task type.</p> required <code>host_name</code> <code>str</code> <p>Host name (e.g. \"maya\", \"nuke\", ...)</p> required <code>project_settings</code> <code>Dict[str, Any]</code> <p>Prepared project settings for project name. Optional to make processing faster.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/workfile/path_resolving.py</code> <pre><code>def get_workfile_template_key(\n    project_name, task_type, host_name, project_settings=None\n):\n    \"\"\"Workfile template key which should be used to get workfile template.\n\n    Function is using profiles from project settings to return right template\n    for passed task type and host name.\n\n    Args:\n        project_name(str): Project name.\n        task_type(str): Task type.\n        host_name(str): Host name (e.g. \"maya\", \"nuke\", ...)\n        project_settings(Dict[str, Any]): Prepared project settings for\n            project name. Optional to make processing faster.\n    \"\"\"\n\n    default = \"work\"\n    if not task_type or not host_name:\n        return default\n\n    if not project_settings:\n        project_settings = get_project_settings(project_name)\n\n    try:\n        profiles = (\n            project_settings\n            [\"core\"]\n            [\"tools\"]\n            [\"Workfiles\"]\n            [\"workfile_template_profiles\"]\n        )\n    except Exception:\n        profiles = []\n\n    if not profiles:\n        return default\n\n    profile_filter = {\n        \"task_types\": task_type,\n        \"host_names\": host_name,\n    }\n    profile = filter_profiles(profiles, profile_filter)\n    if profile:\n        return profile[\"workfile_template\"] or default\n    return default\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/index.html#client.ayon_core.pipeline.workfile.get_workfile_template_key_from_context","title":"<code>get_workfile_template_key_from_context(project_name, folder_path, task_name, host_name, project_settings=None)</code>","text":"<p>Helper function to get template key for workfile template.</p> <p>Do the same as <code>get_workfile_template_key</code> but returns value for \"session context\".</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>folder_path</code> <code>str</code> <p>Folder path.</p> required <code>task_name</code> <code>str</code> <p>Task name.</p> required <code>host_name</code> <code>str</code> <p>Host name.</p> required <code>project_settings</code> <code>Dict[str, Any]</code> <p>Project settings for passed 'project_name'. Not required at all but makes function faster.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Workfile template name.</p> Source code in <code>client/ayon_core/pipeline/workfile/path_resolving.py</code> <pre><code>def get_workfile_template_key_from_context(\n    project_name: str,\n    folder_path: str,\n    task_name: str,\n    host_name: str,\n    project_settings: Optional[Dict[str, Any]] = None,\n) -&gt; str:\n    \"\"\"Helper function to get template key for workfile template.\n\n    Do the same as `get_workfile_template_key` but returns value for \"session\n    context\".\n\n    Args:\n        project_name (str): Project name.\n        folder_path (str): Folder path.\n        task_name (str): Task name.\n        host_name (str): Host name.\n        project_settings (Dict[str, Any]): Project settings for passed\n            'project_name'. Not required at all but makes function faster.\n\n    Returns:\n        str: Workfile template name.\n\n    \"\"\"\n    folder_entity = ayon_api.get_folder_by_path(\n        project_name,\n        folder_path,\n        fields={\"id\"},\n    )\n    task_entity = ayon_api.get_task_by_name(\n        project_name,\n        folder_entity[\"id\"],\n        task_name,\n        fields={\"taskType\"},\n    )\n    task_type = task_entity.get(\"taskType\")\n\n    return get_workfile_template_key(\n        project_name, task_type, host_name, project_settings\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/index.html#client.ayon_core.pipeline.workfile.save_current_workfile_to","title":"<code>save_current_workfile_to(workfile_path, folder_path, task_name, *, version=None, comment=None, description=None, prepared_data=None)</code>","text":"<p>Save current workfile to new location or context.</p> <p>Parameters:</p> Name Type Description Default <code>workfile_path</code> <code>str</code> <p>Destination workfile path.</p> required <code>folder_path</code> <code>str</code> <p>Target folder path.</p> required <code>task_name</code> <code>str</code> <p>Target task name.</p> required <code>version</code> <code>Optional[int]</code> <p>Workfile version.</p> <code>None</code> <code>comment</code> <code>optional[str]</code> <p>Workfile comment.</p> <code>None</code> <code>description</code> <code>Optional[str]</code> <p>Workfile description.</p> <code>None</code> <code>prepared_data</code> <code>Optional[SaveWorkfileOptionalData]</code> <p>Prepared data for speed enhancements.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/workfile/utils.py</code> <pre><code>def save_current_workfile_to(\n    workfile_path: str,\n    folder_path: str,\n    task_name: str,\n    *,\n    version: Optional[int] = None,\n    comment: Optional[str] = None,\n    description: Optional[str] = None,\n    prepared_data: Optional[SaveWorkfileOptionalData] = None,\n) -&gt; None:\n    \"\"\"Save current workfile to new location or context.\n\n    Args:\n        workfile_path (str): Destination workfile path.\n        folder_path (str): Target folder path.\n        task_name (str): Target task name.\n        version (Optional[int]): Workfile version.\n        comment (optional[str]): Workfile comment.\n        description (Optional[str]): Workfile description.\n        prepared_data (Optional[SaveWorkfileOptionalData]): Prepared data\n            for speed enhancements.\n\n    \"\"\"\n    from ayon_core.pipeline.context_tools import registered_host\n\n    host = registered_host()\n    context = host.get_current_context()\n    project_name = context[\"project_name\"]\n    folder_entity = ayon_api.get_folder_by_path(\n        project_name, folder_path\n    )\n    task_entity = ayon_api.get_task_by_name(\n        project_name, folder_entity[\"id\"], task_name\n    )\n    host.save_workfile_with_context(\n        workfile_path,\n        folder_entity,\n        task_entity,\n        version=version,\n        comment=comment,\n        description=description,\n        prepared_data=prepared_data,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/index.html#client.ayon_core.pipeline.workfile.save_next_version","title":"<code>save_next_version(version=None, comment=None, description=None, *, prepared_data=None)</code>","text":"<p>Save workfile using current context, version and comment.</p> <p>Helper function to save a workfile using the current context. Last     workfile version + 1 is used if is not passed in.</p> <p>Parameters:</p> Name Type Description Default <code>version</code> <code>Optional[int]</code> <p>Workfile version that will be used. Last version + 1 is used if is not passed in.</p> <code>None</code> <code>comment</code> <code>optional[str]</code> <p>Workfile comment. Pass '\"\"' to clear comment. The current workfile comment is used if it is not passed.</p> <code>None</code> <code>description</code> <code>Optional[str]</code> <p>Workfile description.</p> <code>None</code> <code>prepared_data</code> <code>Optional[SaveWorkfileOptionalData]</code> <p>Prepared data for speed enhancements.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/workfile/utils.py</code> <pre><code>def save_next_version(\n    version: Optional[int] = None,\n    comment: Optional[str] = None,\n    description: Optional[str] = None,\n    *,\n    prepared_data: Optional[SaveWorkfileOptionalData] = None,\n) -&gt; None:\n    \"\"\"Save workfile using current context, version and comment.\n\n    Helper function to save a workfile using the current context. Last\n        workfile version + 1 is used if is not passed in.\n\n    Args:\n        version (Optional[int]): Workfile version that will be used. Last\n            version + 1 is used if is not passed in.\n        comment (optional[str]): Workfile comment. Pass '\"\"' to clear comment.\n            The current workfile comment is used if it is not passed.\n        description (Optional[str]): Workfile description.\n        prepared_data (Optional[SaveWorkfileOptionalData]): Prepared data\n            for speed enhancements.\n\n    \"\"\"\n    from ayon_core.pipeline import Anatomy\n    from ayon_core.pipeline.context_tools import registered_host\n\n    host = registered_host()\n    current_path = host.get_current_workfile()\n    if not current_path:\n        current_path = None\n    else:\n        current_path = os.path.normpath(current_path)\n\n    context = host.get_current_context()\n    project_name = context[\"project_name\"]\n    folder_path = context[\"folder_path\"]\n    task_name = context[\"task_name\"]\n    if prepared_data is None:\n        prepared_data = SaveWorkfileOptionalData()\n\n    project_entity = prepared_data.project_entity\n    anatomy = prepared_data.anatomy\n    project_settings = prepared_data.project_settings\n\n    if project_entity is None:\n        project_entity = ayon_api.get_project(project_name)\n        prepared_data.project_entity = project_entity\n\n    if project_settings is None:\n        project_settings = get_project_settings(project_name)\n        prepared_data.project_settings = project_settings\n\n    if anatomy is None:\n        anatomy = Anatomy(project_name, project_entity=project_entity)\n        prepared_data.anatomy = anatomy\n\n    folder_entity = ayon_api.get_folder_by_path(project_name, folder_path)\n    task_entity = ayon_api.get_task_by_name(\n        project_name, folder_entity[\"id\"], task_name\n    )\n\n    template_key = get_workfile_template_key(\n        project_name,\n        task_entity[\"taskType\"],\n        host.name,\n        project_settings=project_settings\n    )\n    file_template = anatomy.get_template_item(\"work\", template_key, \"file\")\n    template_data = get_template_data(\n        project_entity,\n        folder_entity,\n        task_entity,\n        host.name,\n        project_settings,\n    )\n    workdir = get_workdir(\n        project_entity,\n        folder_entity,\n        task_entity,\n        host.name,\n        anatomy=anatomy,\n        template_key=template_key,\n        project_settings=project_settings,\n    )\n    rootless_dir = workdir.rootless\n    last_workfile = None\n    current_workfile = None\n    if version is None or comment is None:\n        workfiles = host.list_workfiles(\n            project_name, folder_entity, task_entity,\n            prepared_data=ListWorkfilesOptionalData(\n                project_entity=project_entity,\n                anatomy=anatomy,\n                project_settings=project_settings,\n                template_key=template_key,\n            )\n        )\n        for workfile in workfiles:\n            if current_workfile is None and workfile.filepath == current_path:\n                current_workfile = workfile\n\n            if workfile.version is None:\n                continue\n\n            if (\n                last_workfile is None\n                or last_workfile.version &lt; workfile.version\n            ):\n                last_workfile = workfile\n\n    if version is None and last_workfile is not None:\n        version = last_workfile.version + 1\n\n    if version is None:\n        version = get_versioning_start(\n            project_name,\n            host.name,\n            task_name=task_entity[\"name\"],\n            task_type=task_entity[\"taskType\"],\n            product_base_type=\"workfile\",\n        )\n\n    # Re-use comment from the current workfile if is not passed in\n    if comment is None and current_workfile is not None:\n        comment = current_workfile.comment\n\n    template_data[\"version\"] = version\n    if comment:\n        template_data[\"comment\"] = comment\n\n    # Resolve extension\n    # - Don't fill any if the host does not have defined any -&gt; e.g. if host\n    #   uses directory instead of a file.\n    # 1. Use the current file extension.\n    # 2. Use the last known workfile extension.\n    # 3. Use the first extensions from 'get_workfile_extensions'.\n    ext = None\n    workfile_extensions = host.get_workfile_extensions()\n    if workfile_extensions:\n        if current_path:\n            ext = os.path.splitext(current_path)[1]\n        elif last_workfile is not None:\n            ext = os.path.splitext(last_workfile.filepath)[1]\n        else:\n            ext = next(iter(workfile_extensions))\n        ext = ext.lstrip(\".\")\n\n    if ext:\n        template_data[\"ext\"] = ext\n\n    filename = file_template.format_strict(template_data)\n    workfile_path = os.path.join(workdir, filename)\n    rootless_path = f\"{rootless_dir}/{filename}\"\n    if platform.system().lower() == \"windows\":\n        rootless_path = rootless_path.replace(\"\\\\\", \"/\")\n    prepared_data.rootless_path = rootless_path\n\n    host.save_workfile_with_context(\n        workfile_path,\n        folder_entity,\n        task_entity,\n        version=version,\n        comment=comment,\n        description=description,\n        prepared_data=prepared_data,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/index.html#client.ayon_core.pipeline.workfile.save_workfile_info","title":"<code>save_workfile_info(project_name, task_id, rootless_path, host_name, version=None, comment=None, description=None, username=None, data=None, workfile_entities=None)</code>","text":"<p>Save workfile info entity for a workfile path.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>The name of the project.</p> required <code>task_id</code> <code>str</code> <p>Task id under which is workfile created.</p> required <code>rootless_path</code> <code>str</code> <p>Rootless path of the workfile.</p> required <code>host_name</code> <code>str</code> <p>Name of host which is saving the workfile.</p> required <code>version</code> <code>Optional[int]</code> <p>Workfile version.</p> <code>None</code> <code>comment</code> <code>Optional[str]</code> <p>Workfile comment.</p> <code>None</code> <code>description</code> <code>Optional[str]</code> <p>Workfile description.</p> <code>None</code> <code>username</code> <code>Optional[str]</code> <p>Username of user who saves the workfile. If not provided, current user is used.</p> <code>None</code> <code>data</code> <code>Optional[dict[str, Any]]</code> <p>Additional workfile entity data.</p> <code>None</code> <code>workfile_entities</code> <code>Optional[list[dict[str, Any]]]</code> <p>Pre-fetched workfile entities related to task.</p> <code>None</code> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: Workfile info entity.</p> Source code in <code>client/ayon_core/pipeline/workfile/utils.py</code> <pre><code>def save_workfile_info(\n    project_name: str,\n    task_id: str,\n    rootless_path: str,\n    host_name: str,\n    version: Optional[int] = None,\n    comment: Optional[str] = None,\n    description: Optional[str] = None,\n    username: Optional[str] = None,\n    data: Optional[dict[str, Any]] = None,\n    workfile_entities: Optional[list[dict[str, Any]]] = None,\n) -&gt; dict[str, Any]:\n    \"\"\"Save workfile info entity for a workfile path.\n\n    Args:\n        project_name (str): The name of the project.\n        task_id (str): Task id under which is workfile created.\n        rootless_path (str): Rootless path of the workfile.\n        host_name (str): Name of host which is saving the workfile.\n        version (Optional[int]): Workfile version.\n        comment (Optional[str]): Workfile comment.\n        description (Optional[str]): Workfile description.\n        username (Optional[str]): Username of user who saves the workfile.\n            If not provided, current user is used.\n        data (Optional[dict[str, Any]]): Additional workfile entity data.\n        workfile_entities (Optional[list[dict[str, Any]]]): Pre-fetched\n            workfile entities related to task.\n\n    Returns:\n        dict[str, Any]: Workfile info entity.\n\n    \"\"\"\n    if workfile_entities is None:\n        workfile_entities = list(ayon_api.get_workfiles_info(\n            project_name,\n            task_ids=[task_id],\n        ))\n\n    workfile_entity = next(\n        (\n            _ent\n            for _ent in workfile_entities\n            if _ent[\"path\"] == rootless_path\n        ),\n        None\n    )\n\n    if username is None:\n        username = get_ayon_username()\n\n    attrib = {}\n    extension = os.path.splitext(rootless_path)[1]\n    for key, value in (\n        (\"extension\", extension),\n        (\"description\", description),\n    ):\n        if value is not None:\n            attrib[key] = value\n\n    if data is None:\n        data = {}\n\n    if not workfile_entity:\n        return _create_workfile_info_entity(\n            project_name,\n            task_id,\n            host_name,\n            rootless_path,\n            username,\n            version,\n            comment,\n            attrib,\n            data,\n        )\n\n    for key, value in (\n        (\"host_name\", host_name),\n        (\"version\", version),\n        (\"comment\", comment),\n    ):\n        if value is not None:\n            data[key] = value\n\n    changed_data = {}\n    old_data = workfile_entity[\"data\"]\n    for key, value in data.items():\n        if key not in old_data or old_data[key] != value:\n            changed_data[key] = value\n            workfile_entity[\"data\"][key] = value\n\n    changed_attrib = {}\n    old_attrib = workfile_entity[\"attrib\"]\n    for key, value in attrib.items():\n        if key not in old_attrib or old_attrib[key] != value:\n            changed_attrib[key] = value\n            workfile_entity[\"attrib\"][key] = value\n\n    update_data = {}\n    if changed_data:\n        update_data[\"data\"] = changed_data\n\n    if changed_attrib:\n        update_data[\"attrib\"] = changed_attrib\n\n    # Automatically fix 'createdBy' and 'updatedBy' fields\n    # NOTE both fields were not automatically filled by server\n    #   until 1.1.3 release.\n    if workfile_entity.get(\"createdBy\") is None:\n        update_data[\"createdBy\"] = username\n        workfile_entity[\"createdBy\"] = username\n\n    if workfile_entity.get(\"updatedBy\") != username:\n        update_data[\"updatedBy\"] = username\n        workfile_entity[\"updatedBy\"] = username\n\n    if not update_data:\n        return workfile_entity\n\n    session = OperationsSession()\n    session.update_entity(\n        project_name,\n        \"workfile\",\n        workfile_entity[\"id\"],\n        update_data,\n    )\n    session.commit()\n    return workfile_entity\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/index.html#client.ayon_core.pipeline.workfile.save_workfile_with_current_context","title":"<code>save_workfile_with_current_context(workfile_path, *, version=None, comment=None, description=None, prepared_data=None)</code>","text":"<p>Save current workfile to new location using current context.</p> <p>Helper function to save workfile using current context. Calls     'save_current_workfile_to' at the end.</p> <p>Parameters:</p> Name Type Description Default <code>workfile_path</code> <code>str</code> <p>Destination workfile path.</p> required <code>version</code> <code>Optional[int]</code> <p>Workfile version.</p> <code>None</code> <code>comment</code> <code>optional[str]</code> <p>Workfile comment.</p> <code>None</code> <code>description</code> <code>Optional[str]</code> <p>Workfile description.</p> <code>None</code> <code>prepared_data</code> <code>Optional[SaveWorkfileOptionalData]</code> <p>Prepared data for speed enhancements.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/workfile/utils.py</code> <pre><code>def save_workfile_with_current_context(\n    workfile_path: str,\n    *,\n    version: Optional[int] = None,\n    comment: Optional[str] = None,\n    description: Optional[str] = None,\n    prepared_data: Optional[SaveWorkfileOptionalData] = None,\n) -&gt; None:\n    \"\"\"Save current workfile to new location using current context.\n\n    Helper function to save workfile using current context. Calls\n        'save_current_workfile_to' at the end.\n\n    Args:\n        workfile_path (str): Destination workfile path.\n        version (Optional[int]): Workfile version.\n        comment (optional[str]): Workfile comment.\n        description (Optional[str]): Workfile description.\n        prepared_data (Optional[SaveWorkfileOptionalData]): Prepared data\n            for speed enhancements.\n\n    \"\"\"\n    from ayon_core.pipeline.context_tools import registered_host\n\n    host = registered_host()\n    context = host.get_current_context()\n    project_name = context[\"project_name\"]\n    folder_path = context[\"folder_path\"]\n    task_name = context[\"task_name\"]\n    folder_entity = task_entity = None\n    if folder_path:\n        folder_entity = ayon_api.get_folder_by_path(project_name, folder_path)\n        if folder_entity and task_name:\n            task_entity = ayon_api.get_task_by_name(\n                project_name, folder_entity[\"id\"], task_name\n            )\n\n    host.save_workfile_with_context(\n        workfile_path,\n        folder_entity,\n        task_entity,\n        version=version,\n        comment=comment,\n        description=description,\n        prepared_data=prepared_data,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/index.html#client.ayon_core.pipeline.workfile.should_open_workfiles_tool_on_launch","title":"<code>should_open_workfiles_tool_on_launch(project_name, host_name, task_name, task_type, default_output=False, project_settings=None)</code>","text":"<p>Define if host should start workfile tool at host launch.</p> <p>Default output is <code>False</code>. Can be overridden with environment variable <code>AYON_WORKFILE_TOOL_ON_START</code>, valid values without case sensitivity are <code>\"0\", \"1\", \"true\", \"false\", \"yes\", \"no\"</code>.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Name of project.</p> required <code>host_name</code> <code>str</code> <p>Name of host which is launched. In avalon's application context it's value stored in app definition under key <code>\"application_dir\"</code>. Is not case sensitive.</p> required <code>task_name</code> <code>str</code> <p>Name of task which is used for launching the host. Task name is not case sensitive.</p> required <code>task_type</code> <code>str</code> <p>Task type.</p> required <code>default_output</code> <code>Optional[bool]</code> <p>Default output value if no profile is found.</p> <code>False</code> <code>project_settings</code> <code>Optional[dict[str, Any]]</code> <p>Project settings.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>bool</code> <p>True if host should start workfile.</p> Source code in <code>client/ayon_core/pipeline/workfile/utils.py</code> <pre><code>def should_open_workfiles_tool_on_launch(\n    project_name,\n    host_name,\n    task_name,\n    task_type,\n    default_output=False,\n    project_settings=None,\n):\n    \"\"\"Define if host should start workfile tool at host launch.\n\n    Default output is `False`. Can be overridden with environment variable\n    `AYON_WORKFILE_TOOL_ON_START`, valid values without case sensitivity are\n    `\"0\", \"1\", \"true\", \"false\", \"yes\", \"no\"`.\n\n    Args:\n        project_name (str): Name of project.\n        host_name (str): Name of host which is launched. In avalon's\n            application context it's value stored in app definition under\n            key `\"application_dir\"`. Is not case sensitive.\n        task_name (str): Name of task which is used for launching the host.\n            Task name is not case sensitive.\n        task_type (str): Task type.\n        default_output (Optional[bool]): Default output value if no profile\n            is found.\n        project_settings (Optional[dict[str, Any]]): Project settings.\n\n    Returns:\n        bool: True if host should start workfile.\n\n    \"\"\"\n\n    if project_settings is None:\n        project_settings = get_project_settings(project_name)\n    profiles = (\n        project_settings\n        [\"core\"]\n        [\"tools\"]\n        [\"Workfiles\"]\n        [\"open_workfile_tool_on_startup\"]\n    )\n\n    if not profiles:\n        return default_output\n\n    filter_data = {\n        \"task_names\": task_name,\n        \"task_types\": task_type,\n        \"host_names\": host_name,\n    }\n    matching_item = filter_profiles(profiles, filter_data)\n\n    output = None\n    if matching_item:\n        output = matching_item.get(\"enabled\")\n\n    if output is None:\n        return default_output\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/index.html#client.ayon_core.pipeline.workfile.should_use_last_workfile_on_launch","title":"<code>should_use_last_workfile_on_launch(project_name, host_name, task_name, task_type, default_output=False, project_settings=None)</code>","text":"<p>Define if host should start last version workfile if possible.</p> <p>Default output is <code>False</code>. Can be overridden with environment variable <code>AYON_OPEN_LAST_WORKFILE</code>, valid values without case sensitivity are <code>\"0\", \"1\", \"true\", \"false\", \"yes\", \"no\"</code>.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Name of project.</p> required <code>host_name</code> <code>str</code> <p>Name of host which is launched. In avalon's application context it's value stored in app definition under key <code>\"application_dir\"</code>. Is not case sensitive.</p> required <code>task_name</code> <code>str</code> <p>Name of task which is used for launching the host. Task name is not case sensitive.</p> required <code>task_type</code> <code>str</code> <p>Task type.</p> required <code>default_output</code> <code>Optional[bool]</code> <p>Default output value if no profile is found.</p> <code>False</code> <code>project_settings</code> <code>Optional[dict[str, Any]]</code> <p>Project settings.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if host should start workfile.</p> Source code in <code>client/ayon_core/pipeline/workfile/utils.py</code> <pre><code>def should_use_last_workfile_on_launch(\n    project_name: str,\n    host_name: str,\n    task_name: str,\n    task_type: str,\n    default_output: bool = False,\n    project_settings: Optional[dict[str, Any]] = None,\n) -&gt; bool:\n    \"\"\"Define if host should start last version workfile if possible.\n\n    Default output is `False`. Can be overridden with environment variable\n    `AYON_OPEN_LAST_WORKFILE`, valid values without case sensitivity are\n    `\"0\", \"1\", \"true\", \"false\", \"yes\", \"no\"`.\n\n    Args:\n        project_name (str): Name of project.\n        host_name (str): Name of host which is launched. In avalon's\n            application context it's value stored in app definition under\n            key `\"application_dir\"`. Is not case sensitive.\n        task_name (str): Name of task which is used for launching the host.\n            Task name is not case sensitive.\n        task_type (str): Task type.\n        default_output (Optional[bool]): Default output value if no profile\n            is found.\n        project_settings (Optional[dict[str, Any]]): Project settings.\n\n    Returns:\n        bool: True if host should start workfile.\n\n    \"\"\"\n    if project_settings is None:\n        project_settings = get_project_settings(project_name)\n    profiles = (\n        project_settings\n        [\"core\"]\n        [\"tools\"]\n        [\"Workfiles\"]\n        [\"last_workfile_on_startup\"]\n    )\n\n    if not profiles:\n        return default_output\n\n    filter_data = {\n        \"task_names\": task_name,\n        \"task_types\": task_type,\n        \"host_names\": host_name,\n    }\n    matching_item = filter_profiles(profiles, filter_data)\n\n    output = None\n    if matching_item:\n        output = matching_item.get(\"enabled\")\n\n    if output is None:\n        return default_output\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/build_workfile.html","title":"build_workfile","text":"<p>Workfile build based on settings.</p> <p>Workfile builder will do stuff based on project settings. Advantage is that it need only access to settings. Disadvantage is that it is hard to focus build per context and being explicit about loaded content.</p> <p>For more explicit workfile build is recommended 'AbstractTemplateBuilder' from '~/ayon_core/pipeline/workfile/workfile_template_builder'. Which gives more abilities to define how build happens but require more code to achive it.</p>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/build_workfile.html#client.ayon_core.pipeline.workfile.build_workfile.BuildWorkfile","title":"<code>BuildWorkfile</code>","text":"<p>Wrapper for build workfile process.</p> <p>Load representations for current context by build presets. Build presets are host related, since each host has it's loaders.</p> Source code in <code>client/ayon_core/pipeline/workfile/build_workfile.py</code> <pre><code>class BuildWorkfile:\n    \"\"\"Wrapper for build workfile process.\n\n    Load representations for current context by build presets. Build presets\n    are host related, since each host has it's loaders.\n    \"\"\"\n\n    addon_name = None\n\n    def __init__(self):\n        self.build_presets = []\n        self.log = Logger.get_logger(self.__class__.__name__)\n        self._host = None\n        self._current_context = None\n        self._current_project_settings = None\n        self._current_project_entity = None\n        self._current_folder_entity = None\n        self._current_task_entity = None\n\n    def get_host(self) -&gt; AbstractHost:\n        from ayon_core.pipeline import registered_host\n\n        if self._host is None:\n            self._host = registered_host()\n        return self._host\n\n    def get_host_name(self) -&gt; str:\n        return self.host.name\n\n    def get_current_context(self) -&gt; dict[str, str]:\n        if self._current_context is None:\n            host = self.get_host()\n            self._current_context = host.get_current_context()\n        return self._current_context\n\n    def get_current_project_name(self) -&gt; str:\n        return self.get_current_context()[\"project_name\"]\n\n    def get_current_project_settings(self) -&gt; dict[str, Any]:\n        if self._current_project_settings is None:\n            self._current_project_settings = get_project_settings(\n                self.get_current_project_name()\n            )\n        return self._current_project_settings\n\n    def get_current_project_entity(self) -&gt; Optional[dict[str, Any]]:\n        if self._current_project_entity is None:\n            context = self.get_current_context()\n            project_name = context[\"project_name\"]\n            if not project_name:\n                return None\n            self._current_project_entity = ayon_api.get_project(project_name)\n        return self._current_project_entity\n\n    def get_current_folder_entity(self) -&gt; Optional[dict[str, Any]]:\n        if self._current_folder_entity is None:\n            context = self.get_current_context()\n            project_name = context[\"project_name\"]\n            folder_path = context[\"folder_path\"]\n            if not project_name or not folder_path:\n                return None\n            self._current_folder_entity = ayon_api.get_folder_by_path(\n                project_name,\n                folder_path,\n            )\n        return self._current_folder_entity\n\n    def get_current_task_entity(self) -&gt; Optional[dict[str, Any]]:\n        if self._current_task_entity is None:\n            context = self.get_current_context()\n            project_name = context[\"project_name\"]\n            task_name = context[\"task_name\"]\n            if not project_name or not task_name:\n                return None\n            folder_entity = self.get_current_folder_entity()\n            if not folder_entity:\n                return None\n            self._current_task_entity = ayon_api.get_task_by_name(\n                context[\"project_name\"],\n                folder_id=folder_entity[\"id\"],\n                task_name=context[\"task_name\"],\n            )\n        return self._current_task_entity\n\n    host = property(get_host)\n    host_name = property(get_host_name)\n    current_project_name = property(get_current_project_name)\n    current_project_settings = property(get_current_project_settings)\n    current_project_entity = property(get_current_project_entity)\n    current_folder_entity = property(get_current_folder_entity)\n    current_task_entity = property(get_current_task_entity)\n\n    @staticmethod\n    def map_products_by_base_type(product_entities):\n        products_by_base_type = collections.defaultdict(list)\n        for product_entity in product_entities:\n            product_base_type = product_entity.get(\"productBaseType\")\n            if not product_base_type:\n                product_base_type = product_entity[\"productType\"]\n            products_by_base_type[product_base_type].append(product_entity)\n        return products_by_base_type\n\n    def process(self):\n        \"\"\"Main method of this wrapper.\n\n        Building of workfile is triggered and is possible to implement\n        post processing of loaded containers if necessary.\n\n        Returns:\n            list[dict[str, Any]]: Loaded containers during build.\n\n        \"\"\"\n        self.apply_settings()\n        return self.build_workfile()\n\n    def apply_settings(self) -&gt; None:\n        project_entity = self.current_project_entity\n        folder_entity = self.current_folder_entity\n        task_entity = self.current_task_entity\n        if not project_entity or not folder_entity or not task_entity:\n            return\n\n        # Load workfile presets for task\n        self.build_presets = self.get_build_presets()\n\n    def get_build_profiles(self) -&gt; list[dict[str, Any]]:\n        project_settings = self.current_project_settings\n        addon_name = self.addon_name\n        if not addon_name:\n            addon_name = self.host_name\n\n        host_settings = project_settings.get(addon_name)\n        if not host_settings:\n            return []\n\n        # Get presets for host\n        wb_settings = host_settings.get(\"workfile_builder\")\n        if not wb_settings:\n            # backward compatibility\n            wb_settings = host_settings.get(\"workfile_build\") or {}\n\n        builder_profiles = wb_settings.get(\"profiles\")\n        return builder_profiles or []\n\n    def get_build_presets(self) -&gt; Optional[dict[str, Any]]:\n        \"\"\" Returns presets to build workfile for task name.\n\n        Presets are loaded for current project received by\n        'get_current_project_name', filtered by registered host\n        and entered task name.\n\n\n        Returns:\n            dict[str, Any]: preset per entered task name\n\n        \"\"\"\n        profiles = self.get_build_profiles()\n        if not profiles:\n            return None\n\n        task_entity = self.current_task_entity\n        task_name = task_type = None\n        if task_entity:\n            task_name = task_entity[\"name\"]\n            task_type = task_entity[\"taskType\"]\n\n        filter_data = {\n            \"task_types\": task_type,\n            \"task_names\": task_name,\n            # Backwards compatibility\n            \"tasks\": task_name,\n        }\n        return filter_profiles(profiles, filter_data)\n\n    def build_workfile(self):\n        \"\"\"Prepares and load containers into workfile.\n\n        Loads latest versions of current and linked folders to workfile by\n        logic stored in Workfile profiles from presets. Profiles are set\n        by host, filtered by current task name and used by families.\n\n        Each product base type can specify representation names and loaders\n        for representations and first available and successful loaded\n        representation is returned as container.\n\n        At the end you'll get list of loaded containers per each folder.\n\n        loaded_containers [{\n            \"folder_entity\": &lt;FolderEntity1&gt;,\n            \"containers\": [&lt;Container1&gt;, &lt;Container2&gt;, ...]\n        }, {\n            \"folder_entity\": &lt;FolderEntity2&gt;,\n            \"containers\": [&lt;Container3&gt;, ...]\n        }, {\n            ...\n        }]\n\n        Returns:\n            list[dict[str, Any]]: Loaded containers during build.\n\n        \"\"\"\n        loaded_containers = []\n\n        current_context = self.get_current_context()\n        project_name = current_context[\"project_name\"]\n        current_folder_path = current_context[\"folder_path\"]\n        current_task_name = current_context[\"task_name\"]\n\n        folder_entity = self.current_folder_entity\n        task_entity = self.current_task_entity\n        # Skip if folder was not found\n        if not folder_entity:\n            self.log.warning(\n                f\"Folder entity '{current_folder_path}' was not found\"\n            )\n            return loaded_containers\n\n        task_path = f\"{current_folder_path}/{current_task_name}\"\n        if not task_entity:\n            self.log.warning(f\"Task entity '{task_path}' was not found\")\n            return loaded_containers\n\n        # Prepare available loaders\n        loaders_by_name = {}\n        for loader in discover_loader_plugins():\n            if not loader.enabled:\n                continue\n            loader_name = loader.__name__\n            if loader_name in loaders_by_name:\n                raise KeyError(f\"Duplicated loader name {loader_name}!\")\n\n            loaders_by_name[loader_name] = loader\n\n        # Skip if there are any loaders\n        if not loaders_by_name:\n            self.log.warning(\"There are no registered loaders.\")\n            return loaded_containers\n\n        # Skip if there are any presets for task\n        if not self.build_presets:\n            self.log.warning(\n                f\"Current task '{task_path}' does not have any\"\n                \" loading preset.\"\n            )\n            return loaded_containers\n\n        # Get presets for loading current folder\n        current_context_profiles = self.build_presets.get(\"current_context\")\n        # Get presets for loading linked folders\n        link_context_profiles = self.build_presets.get(\"linked_folders\")\n        if not link_context_profiles:\n            # TODO remove 'linked_assets' (Marked as deprecated 26/02/05)\n            link_context_profiles = self.build_presets.get(\"linked_assets\")\n        # Skip if both are missing\n        if not current_context_profiles and not link_context_profiles:\n            self.log.warning(\n                f\"Current task '{task_path}' has empty\"\n                \" loading preset.\"\n            )\n            return loaded_containers\n\n        elif not current_context_profiles:\n            self.log.warning(\n                f\"Current task '{task_path}' doesn't have any loading\"\n                \" preset for it's context.\"\n            )\n\n        elif not link_context_profiles:\n            self.log.warning(\n                f\"Current task '{task_path}' doesn't have any\"\n                \"loading preset for it's linked folders.\"\n            )\n\n        # Prepare folders to process by workfile presets\n        folder_entities = []\n        current_folder_id = None\n        if current_context_profiles:\n            # Add current folder entity if preset has current context set\n            folder_entities.append(folder_entity)\n            current_folder_id = folder_entity[\"id\"]\n\n        if link_context_profiles:\n            # Find and append linked folders if preset has set linked mapping\n            linked_folder_entities = self._get_linked_folder_entities(\n                project_name, folder_entity[\"id\"]\n            )\n            if linked_folder_entities:\n                folder_entities.extend(linked_folder_entities)\n\n        # Skip if there are no folders. This can happen if only linked mapping\n        # is set and there are no links for his folder.\n        if not folder_entities:\n            self.log.warning(\n                \"Folder does not have linked folders. Nothing to process.\"\n            )\n            return loaded_containers\n\n        # Prepare entities from database for folders\n        prepared_entities = self._collect_last_version_repres(\n            folder_entities\n        )\n\n        # Load containers by prepared entities and presets\n        # - Current folder containers\n        if current_folder_id and current_folder_id in prepared_entities:\n            current_context_data = prepared_entities.pop(current_folder_id)\n            loaded_data = self.load_containers_by_folder_data(\n                current_context_data,\n                current_context_profiles,\n                loaders_by_name\n            )\n            if loaded_data:\n                loaded_containers.append(loaded_data)\n\n        # - Linked assets container\n        for linked_folder_data in prepared_entities.values():\n            loaded_data = self.load_containers_by_folder_data(\n                linked_folder_data,\n                link_context_profiles,\n                loaders_by_name\n            )\n            if loaded_data:\n                loaded_containers.append(loaded_data)\n\n        # Return list of loaded containers\n        return loaded_containers\n\n    def _filter_build_profiles(self, build_profiles, loaders_by_name):\n        \"\"\" Filter build profiles by loaders and prepare process data.\n\n        Valid profile must have \"loaders\", \"families\" and \"repre_names\" keys\n        with valid values.\n        - \"loaders\" expects list of strings representing possible loaders.\n        - \"product_base_types\" expects list of strings for filtering\n                     by product base type.\n        - \"repre_names\" expects list of strings for filtering by\n                        representation name.\n\n        Lowered \"families\" and \"repre_names\" are prepared for each profile with\n        all required keys.\n\n        Args:\n            build_profiles (Dict[str, Any]): Profiles for building workfile.\n            loaders_by_name (Dict[str, LoaderPlugin]): Available loaders\n                per name.\n\n        Returns:\n            List[Dict[str, Any]]: Filtered and prepared profiles.\n        \"\"\"\n\n        valid_profiles = []\n        for profile in build_profiles:\n            # Check loaders\n            profile_loaders = profile.get(\"loaders\")\n            if not profile_loaders:\n                self.log.warning((\n                    \"Build profile has missing loaders configuration: {0}\"\n                ).format(json.dumps(profile, indent=4)))\n                continue\n\n            # Check if any loader is available\n            loaders_match = False\n            for loader_name in profile_loaders:\n                if loader_name in loaders_by_name:\n                    loaders_match = True\n                    break\n\n            if not loaders_match:\n                self.log.warning((\n                    \"All loaders from Build profile are not available: {0}\"\n                ).format(json.dumps(profile, indent=4)))\n                continue\n\n            # Check product base types\n            profile_product_base_types = profile.get(\"product_base_types\")\n            if not profile_product_base_types:\n                profile_product_base_types = profile.get(\"product_types\")\n\n            if not profile_product_base_types:\n                self.log.warning((\n                    \"Build profile is missing families configuration: {0}\"\n                ).format(json.dumps(profile, indent=4)))\n                continue\n\n            # Check representation names\n            profile_repre_names = profile.get(\"repre_names\")\n            if not profile_repre_names:\n                self.log.warning((\n                    \"Build profile is missing\"\n                    \" representation names filtering: {0}\"\n                ).format(json.dumps(profile, indent=4)))\n                continue\n\n            # Prepare lowered families and representation names\n            profile[\"product_base_types_lowered\"] = [\n                product_base_type.lower()\n                for product_base_type in profile_product_base_types\n            ]\n\n            profile[\"repre_names_lowered\"] = [\n                name.lower() for name in profile_repre_names\n            ]\n\n            valid_profiles.append(profile)\n\n        return valid_profiles\n\n    def _get_linked_folder_entities(\n        self, project_name: str, folder_id: str\n    ) -&gt; list[dict[str, Any]]:\n        \"\"\"Get linked folder entities for entered folder.\n\n        Args:\n            project_name (str): Project name.\n            folder_id (str): Folder id.\n\n        Returns:\n            list[dict[str, Any]]: Linked folder entities.\n\n        \"\"\"\n        links = ayon_api.get_folder_links(\n            project_name, folder_id, link_direction=\"in\"\n        )\n        linked_folder_ids = {\n            link[\"entityId\"]\n            for link in links\n            if link[\"entityType\"] == \"folder\"\n        }\n        if not linked_folder_ids:\n            return []\n        return list(ayon_api.get_folders(\n            project_name, folder_ids=linked_folder_ids\n        ))\n\n    def _prepare_profile_for_products(self, product_entities, profiles):\n        \"\"\"Select profile for each product by it's data.\n\n        Profiles are filtered for each product individually.\n        Profile is filtered by product base type, optionally by name regex and\n        representation names set in profile.\n        It is possible to not find matching profile for product, in that case\n        product is skipped and it is possible that none of products have\n        matching profile.\n\n        Args:\n            product_entities (List[Dict[str, Any]]): product entities.\n            profiles (List[Dict[str, Any]]): Build profiles.\n\n        Returns:\n            Dict[str, Any]: Profile by product id.\n        \"\"\"\n\n        # Prepare products\n        products_by_base_type = self.map_products_by_base_type(\n            product_entities\n        )\n\n        profiles_by_product_id = {}\n        for (\n            product_base_type,\n            product_entities\n        ) in products_by_base_type.items():\n            product_base_type_low = product_base_type.lower()\n            for profile in profiles:\n                # Skip profile if does not contain product base type\n                if product_base_type_low not in (\n                    profile[\"product_base_types_lowered\"]\n                ):\n                    continue\n\n                # Precompile name filters as regexes\n                profile_regexes = profile.get(\"product_name_filters\")\n                if profile_regexes:\n                    _profile_regexes = []\n                    for regex in profile_regexes:\n                        _profile_regexes.append(re.compile(regex))\n                    profile_regexes = _profile_regexes\n\n                # TODO prepare regex compilation\n                for product_entity in product_entities:\n                    # Verify regex filtering (optional)\n                    if profile_regexes:\n                        valid = False\n                        for pattern in profile_regexes:\n                            if re.match(pattern, product_entity[\"name\"]):\n                                valid = True\n                                break\n\n                        if not valid:\n                            continue\n\n                    profiles_by_product_id[product_entity[\"id\"]] = profile\n\n                # break profiles loop on finding the first matching profile\n                break\n        return profiles_by_product_id\n\n    def load_containers_by_folder_data(\n        self, linked_folder_data, build_profiles, loaders_by_name\n    ):\n        \"\"\"Load containers for entered folder entity by Build profiles.\n\n        Args:\n            linked_folder_data (Dict[str, Any]): Prepared data with products,\n                last versions and representations for specific folder.\n            build_profiles (Dict[str, Any]): Build profiles.\n            loaders_by_name (Dict[str, LoaderPlugin]): Available loaders\n                per name.\n\n        Returns:\n            Dict[str, Any]: Output contains folder entity\n                and loaded containers.\n        \"\"\"\n\n        # Make sure all data are not empty\n        if not linked_folder_data or not build_profiles or not loaders_by_name:\n            return\n\n        folder_entity = linked_folder_data[\"folder_entity\"]\n\n        valid_profiles = self._filter_build_profiles(\n            build_profiles, loaders_by_name\n        )\n        if not valid_profiles:\n            self.log.warning(\n                \"There are not valid Workfile profiles. Skipping process.\"\n            )\n            return\n\n        self.log.debug(\"Valid Workfile profiles: {}\".format(valid_profiles))\n\n        products_by_id = {}\n        version_by_product_id = {}\n        repres_by_version_id = {}\n        for product_id, in_data in linked_folder_data[\"products\"].items():\n            product_entity = in_data[\"product_entity\"]\n            products_by_id[product_entity[\"id\"]] = product_entity\n\n            version_data = in_data[\"version\"]\n            version_entity = version_data[\"version_entity\"]\n            version_by_product_id[product_id] = version_entity\n            repres_by_version_id[version_entity[\"id\"]] = (\n                version_data[\"repres\"]\n            )\n\n        if not products_by_id:\n            self.log.warning(\"There are not products for folder {}\".format(\n                folder_entity[\"path\"]\n            ))\n            return\n\n        profiles_by_product_id = self._prepare_profile_for_products(\n            products_by_id.values(), valid_profiles\n        )\n        if not profiles_by_product_id:\n            self.log.warning(\"There are not valid products.\")\n            return\n\n        valid_repres_by_product_id = collections.defaultdict(list)\n        for product_id, profile in profiles_by_product_id.items():\n            profile_repre_names = profile[\"repre_names_lowered\"]\n\n            version_entity = version_by_product_id[product_id]\n            version_id = version_entity[\"id\"]\n            repres = repres_by_version_id[version_id]\n            for repre in repres:\n                repre_name_low = repre[\"name\"].lower()\n                if repre_name_low in profile_repre_names:\n                    valid_repres_by_product_id[product_id].append(repre)\n\n        # DEBUG message\n        msg = \"Valid representations for Folder: `{}`\".format(\n            folder_entity[\"path\"]\n        )\n        for product_id, repres in valid_repres_by_product_id.items():\n            product_entity = products_by_id[product_id]\n            msg += \"\\n# Product Name/ID: `{}`/{}\".format(\n                product_entity[\"name\"], product_id\n            )\n            for repre in repres:\n                msg += \"\\n## Repre name: `{}`\".format(repre[\"name\"])\n\n        self.log.debug(msg)\n\n        containers = self._load_containers(\n            valid_repres_by_product_id,\n            products_by_id,\n            profiles_by_product_id,\n            loaders_by_name,\n        )\n\n        return {\n            \"folder_entity\": folder_entity,\n            \"containers\": containers\n        }\n\n    def _load_containers(\n        self,\n        repres_by_product_id: dict[str, list[dict[str, Any]]],\n        products_by_id: dict[str, dict[str, Any]],\n        profiles_by_product_id: dict[str, list[dict[str, Any]]],\n        loaders_by_name: dict[str, LoaderPlugin],\n    ) -&gt; list[dict[str, Any]]:\n        \"\"\"Real load by collected data happens here.\n\n        Loading of representations per product happens here. Each product can\n        loads one representation. Loading is tried in specific order.\n        Representations are tried to load by names defined in configuration.\n        If product has representation matching representation name each loader\n        is tried to load it until any is successful. If none of them was\n        successful then next representation name is tried.\n        Product process loop ends when any representation is loaded or\n        all matching representations were already tried.\n\n        Args:\n            repres_by_product_id (dict[str, list[dict[str, Any]]]): Available\n                representations mapped by their parent (product) id.\n            products_by_id (dict[str, dict[str, Any]]): Product entities\n                mapped by their id.\n            profiles_by_product_id (dict[str, list[dict[str, Any]]]): Build\n                profiles mapped by product id.\n            loaders_by_name (dict[str, LoaderPlugin]): Available loaders\n                per name.\n\n        Returns:\n            List[Dict[str, Any]]: Objects of loaded containers.\n        \"\"\"\n\n        loaded_containers = []\n\n        # Get product id order from build presets.\n        build_presets = self.build_presets.get(\"current_context\", [])\n        build_presets += self.build_presets.get(\"linked_folders\", [])\n        # TODO remove 'linked_assets' (Marked as deprecated 26/02/05)\n        build_presets += self.build_presets.get(\"linked_assets\", [])\n        product_ids_ordered = []\n        for preset in build_presets:\n            product_base_types = preset.get(\"product_base_types\")\n            if product_base_types is None:\n                product_base_types = preset[\"product_types\"]\n            if not product_base_types:\n                continue\n\n            for product_base_type in product_base_types:\n                for product_id, product_entity in products_by_id.items():\n                    # TODO 'families' is not available on product\n                    families = product_entity[\"data\"].get(\"families\") or []\n                    if product_base_type not in families:\n                        continue\n\n                    product_ids_ordered.append(product_id)\n\n        # Order representations from products.\n        representations_ordered = []\n        representations = []\n        for ordered_product_id in product_ids_ordered:\n            for product_id, repres in repres_by_product_id.items():\n                if repres in representations:\n                    continue\n\n                if ordered_product_id == product_id:\n                    representations_ordered.append((product_id, repres))\n                    representations.append(repres)\n\n        print(\"representations\", representations)\n\n        # Load ordered representations.\n        for product_id, repres in representations_ordered:\n            product_name = products_by_id[product_id][\"name\"]\n\n            profile = profiles_by_product_id[product_id]\n            loaders_last_idx = len(profile[\"loaders\"]) - 1\n            repre_names_last_idx = len(profile[\"repre_names_lowered\"]) - 1\n\n            repre_by_low_name = {\n                repre[\"name\"].lower(): repre for repre in repres\n            }\n\n            is_loaded = False\n            for repre_name_idx, profile_repre_name in enumerate(\n                profile[\"repre_names_lowered\"]\n            ):\n                # Break iteration if representation was already loaded\n                if is_loaded:\n                    break\n\n                repre = repre_by_low_name.get(profile_repre_name)\n                if not repre:\n                    continue\n\n                for loader_idx, loader_name in enumerate(profile[\"loaders\"]):\n                    if is_loaded:\n                        break\n\n                    loader = loaders_by_name.get(loader_name)\n                    if not loader:\n                        continue\n                    try:\n                        container = load_container(\n                            loader,\n                            repre[\"id\"],\n                            name=product_name\n                        )\n                        loaded_containers.append(container)\n                        is_loaded = True\n\n                    except Exception as exc:\n                        if exc == IncompatibleLoaderError:\n                            self.log.info((\n                                \"Loader `{}` is not compatible with\"\n                                \" representation `{}`\"\n                            ).format(loader_name, repre[\"name\"]))\n\n                        else:\n                            self.log.error(\n                                \"Unexpected error happened during loading\",\n                                exc_info=True\n                            )\n\n                        msg = \"Loading failed.\"\n                        if loader_idx &lt; loaders_last_idx:\n                            msg += \" Trying next loader.\"\n                        elif repre_name_idx &lt; repre_names_last_idx:\n                            msg += (\n                                \" Loading of product `{}` was not successful.\"\n                            ).format(product_name)\n                        else:\n                            msg += \" Trying next representation.\"\n                        self.log.info(msg)\n\n        return loaded_containers\n\n    def _collect_last_version_repres(self, folder_entities):\n        \"\"\"Collect products, versions and representations for folder_entities.\n\n        Args:\n            folder_entities (List[Dict[str, Any]]): Folder entities for which\n                want to find data.\n\n        Returns:\n            Dict[str, Any]: collected entities\n\n        Example output:\n        ```\n        {\n            &lt;folder id&gt;: {\n                \"folder_entity\": &lt;dict[str, Any]&gt;,\n                \"products\": {\n                    &lt;product id&gt;: {\n                        \"product_entity\": &lt;dict[str, Any]&gt;,\n                        \"version\": {\n                            \"version_entity\": &lt;VersionEntity&gt;,\n                            \"repres\": [\n                                &lt;RepreEntity1&gt;, &lt;RepreEntity2&gt;, ...\n                            ]\n                        }\n                    },\n                    ...\n                }\n            },\n            ...\n        }\n        output[folder_id][\"products\"][product_id][\"version\"][\"repres\"]\n        ```\n        \"\"\"\n\n        from ayon_core.pipeline.context_tools import get_current_project_name\n\n        output = {}\n        if not folder_entities:\n            return output\n\n        folder_entities_by_id = {\n            folder_entity[\"id\"]: folder_entity\n            for folder_entity in folder_entities\n        }\n\n        project_name = get_current_project_name()\n        product_entities = list(ayon_api.get_products(\n            project_name, folder_ids=folder_entities_by_id.keys()\n        ))\n        product_entities_by_id = {\n            product_entity[\"id\"]: product_entity\n            for product_entity in product_entities\n        }\n\n        last_version_by_product_id = ayon_api.get_last_versions(\n            project_name, product_entities_by_id.keys()\n        )\n        last_version_entities_by_id = {\n            version_entity[\"id\"]: version_entity\n            for version_entity in last_version_by_product_id.values()\n        }\n        repre_entities = ayon_api.get_representations(\n            project_name, version_ids=last_version_entities_by_id.keys()\n        )\n\n        for repre_entity in repre_entities:\n            version_id = repre_entity[\"versionId\"]\n            version_entity = last_version_entities_by_id[version_id]\n\n            product_id = version_entity[\"productId\"]\n            product_entity = product_entities_by_id[product_id]\n\n            folder_id = product_entity[\"folderId\"]\n            folder_entity = folder_entities_by_id[folder_id]\n\n            if folder_id not in output:\n                output[folder_id] = {\n                    \"folder_entity\": folder_entity,\n                    \"products\": {}\n                }\n\n            if product_id not in output[folder_id][\"products\"]:\n                output[folder_id][\"products\"][product_id] = {\n                    \"product_entity\": product_entity,\n                    \"version\": {\n                        \"version_entity\": version_entity,\n                        \"repres\": []\n                    }\n                }\n\n            (\n                output\n                [folder_id]\n                [\"products\"]\n                [product_id]\n                [\"version\"]\n                [\"repres\"]\n            ).append(repre_entity)\n\n        return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/build_workfile.html#client.ayon_core.pipeline.workfile.build_workfile.BuildWorkfile.build_workfile","title":"<code>build_workfile()</code>","text":"<p>Prepares and load containers into workfile.</p> <p>Loads latest versions of current and linked folders to workfile by logic stored in Workfile profiles from presets. Profiles are set by host, filtered by current task name and used by families.</p> <p>Each product base type can specify representation names and loaders for representations and first available and successful loaded representation is returned as container.</p> <p>At the end you'll get list of loaded containers per each folder.</p> <p>loaded_containers [{     \"folder_entity\": ,     \"containers\": [, , ...] }, {     \"folder_entity\": ,     \"containers\": [, ...] }, {     ... }] <p>Returns:</p> Type Description <p>list[dict[str, Any]]: Loaded containers during build.</p> Source code in <code>client/ayon_core/pipeline/workfile/build_workfile.py</code> <pre><code>def build_workfile(self):\n    \"\"\"Prepares and load containers into workfile.\n\n    Loads latest versions of current and linked folders to workfile by\n    logic stored in Workfile profiles from presets. Profiles are set\n    by host, filtered by current task name and used by families.\n\n    Each product base type can specify representation names and loaders\n    for representations and first available and successful loaded\n    representation is returned as container.\n\n    At the end you'll get list of loaded containers per each folder.\n\n    loaded_containers [{\n        \"folder_entity\": &lt;FolderEntity1&gt;,\n        \"containers\": [&lt;Container1&gt;, &lt;Container2&gt;, ...]\n    }, {\n        \"folder_entity\": &lt;FolderEntity2&gt;,\n        \"containers\": [&lt;Container3&gt;, ...]\n    }, {\n        ...\n    }]\n\n    Returns:\n        list[dict[str, Any]]: Loaded containers during build.\n\n    \"\"\"\n    loaded_containers = []\n\n    current_context = self.get_current_context()\n    project_name = current_context[\"project_name\"]\n    current_folder_path = current_context[\"folder_path\"]\n    current_task_name = current_context[\"task_name\"]\n\n    folder_entity = self.current_folder_entity\n    task_entity = self.current_task_entity\n    # Skip if folder was not found\n    if not folder_entity:\n        self.log.warning(\n            f\"Folder entity '{current_folder_path}' was not found\"\n        )\n        return loaded_containers\n\n    task_path = f\"{current_folder_path}/{current_task_name}\"\n    if not task_entity:\n        self.log.warning(f\"Task entity '{task_path}' was not found\")\n        return loaded_containers\n\n    # Prepare available loaders\n    loaders_by_name = {}\n    for loader in discover_loader_plugins():\n        if not loader.enabled:\n            continue\n        loader_name = loader.__name__\n        if loader_name in loaders_by_name:\n            raise KeyError(f\"Duplicated loader name {loader_name}!\")\n\n        loaders_by_name[loader_name] = loader\n\n    # Skip if there are any loaders\n    if not loaders_by_name:\n        self.log.warning(\"There are no registered loaders.\")\n        return loaded_containers\n\n    # Skip if there are any presets for task\n    if not self.build_presets:\n        self.log.warning(\n            f\"Current task '{task_path}' does not have any\"\n            \" loading preset.\"\n        )\n        return loaded_containers\n\n    # Get presets for loading current folder\n    current_context_profiles = self.build_presets.get(\"current_context\")\n    # Get presets for loading linked folders\n    link_context_profiles = self.build_presets.get(\"linked_folders\")\n    if not link_context_profiles:\n        # TODO remove 'linked_assets' (Marked as deprecated 26/02/05)\n        link_context_profiles = self.build_presets.get(\"linked_assets\")\n    # Skip if both are missing\n    if not current_context_profiles and not link_context_profiles:\n        self.log.warning(\n            f\"Current task '{task_path}' has empty\"\n            \" loading preset.\"\n        )\n        return loaded_containers\n\n    elif not current_context_profiles:\n        self.log.warning(\n            f\"Current task '{task_path}' doesn't have any loading\"\n            \" preset for it's context.\"\n        )\n\n    elif not link_context_profiles:\n        self.log.warning(\n            f\"Current task '{task_path}' doesn't have any\"\n            \"loading preset for it's linked folders.\"\n        )\n\n    # Prepare folders to process by workfile presets\n    folder_entities = []\n    current_folder_id = None\n    if current_context_profiles:\n        # Add current folder entity if preset has current context set\n        folder_entities.append(folder_entity)\n        current_folder_id = folder_entity[\"id\"]\n\n    if link_context_profiles:\n        # Find and append linked folders if preset has set linked mapping\n        linked_folder_entities = self._get_linked_folder_entities(\n            project_name, folder_entity[\"id\"]\n        )\n        if linked_folder_entities:\n            folder_entities.extend(linked_folder_entities)\n\n    # Skip if there are no folders. This can happen if only linked mapping\n    # is set and there are no links for his folder.\n    if not folder_entities:\n        self.log.warning(\n            \"Folder does not have linked folders. Nothing to process.\"\n        )\n        return loaded_containers\n\n    # Prepare entities from database for folders\n    prepared_entities = self._collect_last_version_repres(\n        folder_entities\n    )\n\n    # Load containers by prepared entities and presets\n    # - Current folder containers\n    if current_folder_id and current_folder_id in prepared_entities:\n        current_context_data = prepared_entities.pop(current_folder_id)\n        loaded_data = self.load_containers_by_folder_data(\n            current_context_data,\n            current_context_profiles,\n            loaders_by_name\n        )\n        if loaded_data:\n            loaded_containers.append(loaded_data)\n\n    # - Linked assets container\n    for linked_folder_data in prepared_entities.values():\n        loaded_data = self.load_containers_by_folder_data(\n            linked_folder_data,\n            link_context_profiles,\n            loaders_by_name\n        )\n        if loaded_data:\n            loaded_containers.append(loaded_data)\n\n    # Return list of loaded containers\n    return loaded_containers\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/build_workfile.html#client.ayon_core.pipeline.workfile.build_workfile.BuildWorkfile.get_build_presets","title":"<code>get_build_presets()</code>","text":"<p>Returns presets to build workfile for task name.</p> <p>Presets are loaded for current project received by 'get_current_project_name', filtered by registered host and entered task name.</p> <p>Returns:</p> Type Description <code>Optional[dict[str, Any]]</code> <p>dict[str, Any]: preset per entered task name</p> Source code in <code>client/ayon_core/pipeline/workfile/build_workfile.py</code> <pre><code>def get_build_presets(self) -&gt; Optional[dict[str, Any]]:\n    \"\"\" Returns presets to build workfile for task name.\n\n    Presets are loaded for current project received by\n    'get_current_project_name', filtered by registered host\n    and entered task name.\n\n\n    Returns:\n        dict[str, Any]: preset per entered task name\n\n    \"\"\"\n    profiles = self.get_build_profiles()\n    if not profiles:\n        return None\n\n    task_entity = self.current_task_entity\n    task_name = task_type = None\n    if task_entity:\n        task_name = task_entity[\"name\"]\n        task_type = task_entity[\"taskType\"]\n\n    filter_data = {\n        \"task_types\": task_type,\n        \"task_names\": task_name,\n        # Backwards compatibility\n        \"tasks\": task_name,\n    }\n    return filter_profiles(profiles, filter_data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/build_workfile.html#client.ayon_core.pipeline.workfile.build_workfile.BuildWorkfile.load_containers_by_folder_data","title":"<code>load_containers_by_folder_data(linked_folder_data, build_profiles, loaders_by_name)</code>","text":"<p>Load containers for entered folder entity by Build profiles.</p> <p>Parameters:</p> Name Type Description Default <code>linked_folder_data</code> <code>Dict[str, Any]</code> <p>Prepared data with products, last versions and representations for specific folder.</p> required <code>build_profiles</code> <code>Dict[str, Any]</code> <p>Build profiles.</p> required <code>loaders_by_name</code> <code>Dict[str, LoaderPlugin]</code> <p>Available loaders per name.</p> required <p>Returns:</p> Type Description <p>Dict[str, Any]: Output contains folder entity and loaded containers.</p> Source code in <code>client/ayon_core/pipeline/workfile/build_workfile.py</code> <pre><code>def load_containers_by_folder_data(\n    self, linked_folder_data, build_profiles, loaders_by_name\n):\n    \"\"\"Load containers for entered folder entity by Build profiles.\n\n    Args:\n        linked_folder_data (Dict[str, Any]): Prepared data with products,\n            last versions and representations for specific folder.\n        build_profiles (Dict[str, Any]): Build profiles.\n        loaders_by_name (Dict[str, LoaderPlugin]): Available loaders\n            per name.\n\n    Returns:\n        Dict[str, Any]: Output contains folder entity\n            and loaded containers.\n    \"\"\"\n\n    # Make sure all data are not empty\n    if not linked_folder_data or not build_profiles or not loaders_by_name:\n        return\n\n    folder_entity = linked_folder_data[\"folder_entity\"]\n\n    valid_profiles = self._filter_build_profiles(\n        build_profiles, loaders_by_name\n    )\n    if not valid_profiles:\n        self.log.warning(\n            \"There are not valid Workfile profiles. Skipping process.\"\n        )\n        return\n\n    self.log.debug(\"Valid Workfile profiles: {}\".format(valid_profiles))\n\n    products_by_id = {}\n    version_by_product_id = {}\n    repres_by_version_id = {}\n    for product_id, in_data in linked_folder_data[\"products\"].items():\n        product_entity = in_data[\"product_entity\"]\n        products_by_id[product_entity[\"id\"]] = product_entity\n\n        version_data = in_data[\"version\"]\n        version_entity = version_data[\"version_entity\"]\n        version_by_product_id[product_id] = version_entity\n        repres_by_version_id[version_entity[\"id\"]] = (\n            version_data[\"repres\"]\n        )\n\n    if not products_by_id:\n        self.log.warning(\"There are not products for folder {}\".format(\n            folder_entity[\"path\"]\n        ))\n        return\n\n    profiles_by_product_id = self._prepare_profile_for_products(\n        products_by_id.values(), valid_profiles\n    )\n    if not profiles_by_product_id:\n        self.log.warning(\"There are not valid products.\")\n        return\n\n    valid_repres_by_product_id = collections.defaultdict(list)\n    for product_id, profile in profiles_by_product_id.items():\n        profile_repre_names = profile[\"repre_names_lowered\"]\n\n        version_entity = version_by_product_id[product_id]\n        version_id = version_entity[\"id\"]\n        repres = repres_by_version_id[version_id]\n        for repre in repres:\n            repre_name_low = repre[\"name\"].lower()\n            if repre_name_low in profile_repre_names:\n                valid_repres_by_product_id[product_id].append(repre)\n\n    # DEBUG message\n    msg = \"Valid representations for Folder: `{}`\".format(\n        folder_entity[\"path\"]\n    )\n    for product_id, repres in valid_repres_by_product_id.items():\n        product_entity = products_by_id[product_id]\n        msg += \"\\n# Product Name/ID: `{}`/{}\".format(\n            product_entity[\"name\"], product_id\n        )\n        for repre in repres:\n            msg += \"\\n## Repre name: `{}`\".format(repre[\"name\"])\n\n    self.log.debug(msg)\n\n    containers = self._load_containers(\n        valid_repres_by_product_id,\n        products_by_id,\n        profiles_by_product_id,\n        loaders_by_name,\n    )\n\n    return {\n        \"folder_entity\": folder_entity,\n        \"containers\": containers\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/build_workfile.html#client.ayon_core.pipeline.workfile.build_workfile.BuildWorkfile.process","title":"<code>process()</code>","text":"<p>Main method of this wrapper.</p> <p>Building of workfile is triggered and is possible to implement post processing of loaded containers if necessary.</p> <p>Returns:</p> Type Description <p>list[dict[str, Any]]: Loaded containers during build.</p> Source code in <code>client/ayon_core/pipeline/workfile/build_workfile.py</code> <pre><code>def process(self):\n    \"\"\"Main method of this wrapper.\n\n    Building of workfile is triggered and is possible to implement\n    post processing of loaded containers if necessary.\n\n    Returns:\n        list[dict[str, Any]]: Loaded containers during build.\n\n    \"\"\"\n    self.apply_settings()\n    return self.build_workfile()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/lock_workfile.html","title":"lock_workfile","text":""},{"location":"autoapi/client/ayon_core/pipeline/workfile/path_resolving.html","title":"path_resolving","text":""},{"location":"autoapi/client/ayon_core/pipeline/workfile/path_resolving.html#client.ayon_core.pipeline.workfile.path_resolving.CommentMatcher","title":"<code>CommentMatcher</code>","text":"<p>Use anatomy and work file data to parse comments from filenames.</p> <p>Parameters:</p> Name Type Description Default <code>extensions</code> <code>set[str]</code> <p>Set of extensions.</p> required <code>file_template</code> <code>StringTemplate</code> <p>Workfile file template.</p> required <code>data</code> <code>dict[str, Any]</code> <p>Data to fill the template with.</p> required Source code in <code>client/ayon_core/pipeline/workfile/path_resolving.py</code> <pre><code>class CommentMatcher:\n    \"\"\"Use anatomy and work file data to parse comments from filenames.\n\n    Args:\n        extensions (set[str]): Set of extensions.\n        file_template (StringTemplate): Workfile file template.\n        data (dict[str, Any]): Data to fill the template with.\n\n    \"\"\"\n    def __init__(\n        self,\n        extensions: set[str],\n        file_template: StringTemplate,\n        data: dict[str, Any]\n    ):\n        warnings.warn(\n            \"Class 'CommentMatcher' is deprecated. Please\"\n            \" use 'parse_dynamic_data_from_workfiles' instead.\",\n            DeprecationWarning,\n            stacklevel=2,\n        )\n        self._fname_regex = None\n\n        if \"{comment}\" not in file_template:\n            # Don't look for comment if template doesn't allow it\n            return\n\n        # Create a regex group for extensions\n        any_extension = \"(?:{})\".format(\n            \"|\".join(re.escape(ext.lstrip(\".\")) for ext in extensions)\n        )\n\n        # Use placeholders that will never be in the filename\n        temp_data = copy.deepcopy(data)\n        temp_data[\"comment\"] = \"&lt;&lt;comment&gt;&gt;\"\n        temp_data[\"version\"] = \"&lt;&lt;version&gt;&gt;\"\n        temp_data[\"ext\"] = \"&lt;&lt;ext&gt;&gt;\"\n\n        fname_pattern = re.escape(\n            file_template.format_strict(temp_data)\n        )\n\n        # Replace comment and version with something we can match with regex\n        replacements = (\n            (\"&lt;&lt;comment&gt;&gt;\", r\"(?P&lt;comment&gt;.+)\"),\n            (\"&lt;&lt;version&gt;&gt;\", r\"[0-9]+\"),\n            (\"&lt;&lt;ext&gt;&gt;\", any_extension),\n        )\n        for src, dest in replacements:\n            fname_pattern = fname_pattern.replace(re.escape(src), dest)\n\n        # Match from beginning to end of string to be safe\n        self._fname_regex = re.compile(f\"^{fname_pattern}$\")\n\n    def parse_comment(self, filename: str) -&gt; Optional[str]:\n        \"\"\"Parse the {comment} part from a filename.\"\"\"\n        if self._fname_regex:\n            match = self._fname_regex.match(filename)\n            if match:\n                return match.group(\"comment\")\n        return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/path_resolving.html#client.ayon_core.pipeline.workfile.path_resolving.CommentMatcher.parse_comment","title":"<code>parse_comment(filename)</code>","text":"<p>Parse the {comment} part from a filename.</p> Source code in <code>client/ayon_core/pipeline/workfile/path_resolving.py</code> <pre><code>def parse_comment(self, filename: str) -&gt; Optional[str]:\n    \"\"\"Parse the {comment} part from a filename.\"\"\"\n    if self._fname_regex:\n        match = self._fname_regex.match(filename)\n        if match:\n            return match.group(\"comment\")\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/path_resolving.html#client.ayon_core.pipeline.workfile.path_resolving.WorkfileDataParser","title":"<code>WorkfileDataParser</code>","text":"<p>Parse dynamic data from existing filenames based on template.</p> <p>Parameters:</p> Name Type Description Default <code>file_template</code> <code>str</code> <p>Workfile file template.</p> required <code>data</code> <code>dict[str, Any]</code> <p>Data to fill the template with.</p> required Source code in <code>client/ayon_core/pipeline/workfile/path_resolving.py</code> <pre><code>class WorkfileDataParser:\n    \"\"\"Parse dynamic data from existing filenames based on template.\n\n    Args:\n        file_template (str): Workfile file template.\n        data (dict[str, Any]): Data to fill the template with.\n\n    \"\"\"\n    def __init__(\n        self,\n        file_template: str,\n        data: dict[str, Any],\n    ):\n        data = copy.deepcopy(data)\n        file_template = str(file_template)\n        # Use placeholders that will never be in the filename\n        ext_replacement = \"CIextID\"\n        version_replacement = \"CIversionID\"\n        comment_replacement = \"CIcommentID\"\n        data[\"version\"] = version_replacement\n        data[\"comment\"] = comment_replacement\n        for pattern, replacement in (\n            # Replace `.{ext}` with `{ext}` so we are sure dot is not\n            #   at the end\n            (r\"\\.?{ext}\", ext_replacement),\n        ):\n            file_template = re.sub(pattern, replacement, file_template)\n\n        file_template = StringTemplate(file_template)\n        # Prepare template that does contain 'comment'\n        comment_template = re.escape(str(file_template.format_strict(data)))\n        # Prepare template that does not contain 'comment'\n        # - comment is usually marked as optional and in that case the regex\n        #   to find the comment is different based on the filename\n        #   - if filename contains comment then 'comment_template' will match\n        #   - if filename does not contain comment then 'file_template' will\n        #     match\n        data.pop(\"comment\")\n        file_template = re.escape(str(file_template.format_strict(data)))\n        for src, replacement in (\n            (ext_replacement, r\"(?P&lt;ext&gt;\\..*)\"),\n            (version_replacement, r\"(?P&lt;version&gt;[0-9]+)\"),\n            (comment_replacement, r\"(?P&lt;comment&gt;.+?)\"),\n        ):\n            comment_template = comment_template.replace(src, replacement)\n            file_template = file_template.replace(src, replacement)\n\n        kwargs = {}\n        if platform.system().lower() == \"windows\":\n            kwargs[\"flags\"] = re.IGNORECASE\n\n        # Match from beginning to end of string to be safe\n        self._comment_template = re.compile(f\"^{comment_template}$\", **kwargs)\n        self._file_template = re.compile(f\"^{file_template}$\", **kwargs)\n\n    def parse_data(self, filename: str) -&gt; WorkfileParsedData:\n        \"\"\"Parse the dynamic data from a filename.\"\"\"\n        match = self._comment_template.match(filename)\n        if not match:\n            match = self._file_template.match(filename)\n\n        if not match:\n            return WorkfileParsedData()\n\n        kwargs = match.groupdict()\n        version = kwargs.get(\"version\")\n        if version is not None:\n            kwargs[\"version\"] = int(version)\n        return WorkfileParsedData(**kwargs)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/path_resolving.html#client.ayon_core.pipeline.workfile.path_resolving.WorkfileDataParser.parse_data","title":"<code>parse_data(filename)</code>","text":"<p>Parse the dynamic data from a filename.</p> Source code in <code>client/ayon_core/pipeline/workfile/path_resolving.py</code> <pre><code>def parse_data(self, filename: str) -&gt; WorkfileParsedData:\n    \"\"\"Parse the dynamic data from a filename.\"\"\"\n    match = self._comment_template.match(filename)\n    if not match:\n        match = self._file_template.match(filename)\n\n    if not match:\n        return WorkfileParsedData()\n\n    kwargs = match.groupdict()\n    version = kwargs.get(\"version\")\n    if version is not None:\n        kwargs[\"version\"] = int(version)\n    return WorkfileParsedData(**kwargs)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/path_resolving.html#client.ayon_core.pipeline.workfile.path_resolving.create_workdir_extra_folders","title":"<code>create_workdir_extra_folders(workdir, host_name, task_type, task_name, project_name, project_settings=None)</code>","text":"<p>Create extra folders in work directory based on context.</p> <p>Parameters:</p> Name Type Description Default <code>workdir</code> <code>str</code> <p>Path to workdir where workfiles is stored.</p> required <code>host_name</code> <code>str</code> <p>Name of host implementation.</p> required <code>task_type</code> <code>str</code> <p>Type of task for which extra folders should be created.</p> required <code>task_name</code> <code>str</code> <p>Name of task for which extra folders should be created.</p> required <code>project_name</code> <code>str</code> <p>Name of project on which task is.</p> required <code>project_settings</code> <code>dict</code> <p>Prepared project settings. Are loaded if not passed.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/workfile/path_resolving.py</code> <pre><code>def create_workdir_extra_folders(\n    workdir,\n    host_name,\n    task_type,\n    task_name,\n    project_name,\n    project_settings=None\n):\n    \"\"\"Create extra folders in work directory based on context.\n\n    Args:\n        workdir (str): Path to workdir where workfiles is stored.\n        host_name (str): Name of host implementation.\n        task_type (str): Type of task for which extra folders should be\n            created.\n        task_name (str): Name of task for which extra folders should be\n            created.\n        project_name (str): Name of project on which task is.\n        project_settings (dict): Prepared project settings. Are loaded if not\n            passed.\n    \"\"\"\n\n    # Load project settings if not set\n    if not project_settings:\n        project_settings = get_project_settings(project_name)\n\n    # Load extra folders profiles\n    extra_folders_profiles = (\n        project_settings[\"core\"][\"tools\"][\"Workfiles\"][\"extra_folders\"]\n    )\n    # Skip if are empty\n    if not extra_folders_profiles:\n        return\n\n    # Prepare profiles filters\n    filter_data = {\n        \"task_types\": task_type,\n        \"task_names\": task_name,\n        \"host_names\": host_name,\n    }\n    profile = filter_profiles(extra_folders_profiles, filter_data)\n    if profile is None:\n        return\n\n    for subfolder in profile[\"folders\"]:\n        # Make sure backslashes are converted to forwards slashes\n        #   and does not start with slash\n        subfolder = subfolder.replace(\"\\\\\", \"/\").lstrip(\"/\")\n        # Skip empty strings\n        if not subfolder:\n            continue\n\n        fullpath = os.path.join(workdir, subfolder)\n        if not os.path.exists(fullpath):\n            os.makedirs(fullpath)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/path_resolving.html#client.ayon_core.pipeline.workfile.path_resolving.get_comments_from_workfile_paths","title":"<code>get_comments_from_workfile_paths(filepaths, extensions, file_template, template_data, current_filename=None)</code>","text":"<p>DEPRECATED Collect comments from workfile filenames.</p> <p>Based on 'current_filename' is also returned \"current comment\".</p> <p>Parameters:</p> Name Type Description Default <code>filepaths</code> <code>list[str]</code> <p>List of filepaths to parse.</p> required <code>extensions</code> <code>set[str]</code> <p>Set of file extensions.</p> required <code>file_template</code> <code>StringTemplate</code> <p>Workfile file template.</p> required <code>template_data</code> <code>dict[str, Any]</code> <p>Data to fill the template with.</p> required <code>current_filename</code> <code>str</code> <p>Filename to check for the current comment.</p> <code>None</code> <p>Returns:</p> Type Description <code>tuple[list[str], str]</code> <p>tuple[list[str], str]: List of comments and the current comment.</p> Source code in <code>client/ayon_core/pipeline/workfile/path_resolving.py</code> <pre><code>def get_comments_from_workfile_paths(\n    filepaths: list[str],\n    extensions: set[str],\n    file_template: StringTemplate,\n    template_data: dict[str, Any],\n    current_filename: Optional[str] = None,\n) -&gt; tuple[list[str], str]:\n    \"\"\"DEPRECATED Collect comments from workfile filenames.\n\n    Based on 'current_filename' is also returned \"current comment\".\n\n    Args:\n        filepaths (list[str]): List of filepaths to parse.\n        extensions (set[str]): Set of file extensions.\n        file_template (StringTemplate): Workfile file template.\n        template_data (dict[str, Any]): Data to fill the template with.\n        current_filename (str): Filename to check for the current comment.\n\n    Returns:\n        tuple[list[str], str]: List of comments and the current comment.\n\n    \"\"\"\n    warnings.warn(\n        \"Function 'get_comments_from_workfile_paths' is deprecated. Please\"\n        \" use 'parse_dynamic_data_from_workfiles' instead.\",\n        DeprecationWarning,\n        stacklevel=2,\n    )\n    current_comment = \"\"\n    if not filepaths:\n        return [], current_comment\n\n    matcher = CommentMatcher(extensions, file_template, template_data)\n\n    comment_hints = set()\n    for filepath in filepaths:\n        filename = os.path.basename(filepath)\n        comment = matcher.parse_comment(filename)\n        if comment:\n            comment_hints.add(comment)\n            if filename == current_filename:\n                current_comment = comment\n\n    return list(comment_hints), current_comment\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/path_resolving.html#client.ayon_core.pipeline.workfile.path_resolving.get_custom_workfile_template","title":"<code>get_custom_workfile_template(project_entity, folder_entity, task_entity, host_name, anatomy=None, project_settings=None)</code>","text":"<p>Filter and fill workfile template profiles by passed context.</p> <p>Custom workfile template can be used as first version of workfiles. Template is a file on a disk which is set in settings. Expected settings structure to have this feature enabled is: project settings |-    |- workfile_builder     |- create_first_version   - a bool which must be set to 'True'     |- custom_templates       - profiles based on task name/type which                                 points to a file which is copied as                                 first workfile <p>It is expected that passed argument are already queried entities of project and folder as parents of processing task name.</p> <p>Parameters:</p> Name Type Description Default <code>project_entity</code> <code>Dict[str, Any]</code> <p>Project entity.</p> required <code>folder_entity</code> <code>Dict[str, Any]</code> <p>Folder entity.</p> required <code>task_entity</code> <code>Dict[str, Any]</code> <p>Task entity.</p> required <code>host_name</code> <code>str</code> <p>Name of host.</p> required <code>anatomy</code> <code>Anatomy</code> <p>Optionally passed anatomy object for passed project name.</p> <code>None</code> <code>project_settings</code> <code>Dict[str, Any]</code> <p>Preloaded project settings.</p> <code>None</code> <p>Returns:</p> Type Description <p>Optional[str]: Path to template or None if none of profiles match current context. Existence of formatted path is not validated.</p> Source code in <code>client/ayon_core/pipeline/workfile/path_resolving.py</code> <pre><code>def get_custom_workfile_template(\n    project_entity,\n    folder_entity,\n    task_entity,\n    host_name,\n    anatomy=None,\n    project_settings=None\n):\n    \"\"\"Filter and fill workfile template profiles by passed context.\n\n    Custom workfile template can be used as first version of workfiles.\n    Template is a file on a disk which is set in settings. Expected settings\n    structure to have this feature enabled is:\n    project settings\n    |- &lt;host name&gt;\n      |- workfile_builder\n        |- create_first_version   - a bool which must be set to 'True'\n        |- custom_templates       - profiles based on task name/type which\n                                    points to a file which is copied as\n                                    first workfile\n\n    It is expected that passed argument are already queried entities of\n    project and folder as parents of processing task name.\n\n    Args:\n        project_entity (Dict[str, Any]): Project entity.\n        folder_entity (Dict[str, Any]): Folder entity.\n        task_entity (Dict[str, Any]): Task entity.\n        host_name (str): Name of host.\n        anatomy (Anatomy): Optionally passed anatomy object for passed project\n            name.\n        project_settings(Dict[str, Any]): Preloaded project settings.\n\n    Returns:\n        Optional[str]: Path to template or None if none of profiles match\n            current context. Existence of formatted path is not validated.\n\n    \"\"\"\n    log = Logger.get_logger(\"CustomWorkfileResolve\")\n\n    project_name = project_entity[\"name\"]\n    if project_settings is None:\n        project_settings = get_project_settings(project_name)\n\n    host_settings = project_settings.get(host_name)\n    if not host_settings:\n        log.info(\"Host \\\"{}\\\" doesn't have settings\".format(host_name))\n        return None\n\n    workfile_builder_settings = host_settings.get(\"workfile_builder\")\n    if not workfile_builder_settings:\n        log.info((\n            \"Seems like old version of settings is used.\"\n            \" Can't access custom templates in host \\\"{}\\\".\"\n        ).format(host_name))\n        return\n\n    if not workfile_builder_settings[\"create_first_version\"]:\n        log.info((\n            \"Project \\\"{}\\\" has turned off to create first workfile for\"\n            \" host \\\"{}\\\"\"\n        ).format(project_name, host_name))\n        return\n\n    # Backwards compatibility\n    template_profiles = workfile_builder_settings.get(\"custom_templates\")\n    if not template_profiles:\n        log.info(\n            \"Custom templates are not filled. Skipping template copy.\"\n        )\n        return\n\n    if anatomy is None:\n        anatomy = Anatomy(project_name)\n\n    # get project, folder, task anatomy context data\n    anatomy_context_data = get_template_data(\n        project_entity, folder_entity, task_entity, host_name\n    )\n    # add root dict\n    anatomy_context_data[\"root\"] = anatomy.roots\n\n    # get task type for the task in context\n    current_task_type = anatomy_context_data[\"task\"][\"type\"]\n\n    # get path from matching profile\n    matching_item = filter_profiles(\n        template_profiles,\n        {\"task_types\": current_task_type}\n    )\n    # when path is available try to format it in case\n    # there are some anatomy template strings\n    if matching_item:\n        # extend anatomy context with os.environ to\n        # also allow formatting against env\n        full_context_data = os.environ.copy()\n        full_context_data.update(anatomy_context_data)\n\n        template = matching_item[\"path\"][platform.system().lower()]\n        return StringTemplate.format_strict_template(\n            template, full_context_data\n        ).normalized()\n\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/path_resolving.html#client.ayon_core.pipeline.workfile.path_resolving.get_custom_workfile_template_by_string_context","title":"<code>get_custom_workfile_template_by_string_context(project_name, folder_path, task_name, host_name, anatomy=None, project_settings=None)</code>","text":"<p>Filter and fill workfile template profiles by passed context.</p> <p>Passed context are string representations of project, folder and task. Function will query documents of project and folder to be able to use <code>get_custom_workfile_template</code> for rest of logic.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>folder_path</code> <code>str</code> <p>Folder path.</p> required <code>task_name</code> <code>str</code> <p>Task name.</p> required <code>host_name</code> <code>str</code> <p>Name of host.</p> required <code>anatomy</code> <code>Anatomy</code> <p>Optionally prepared anatomy object for passed project.</p> <code>None</code> <code>project_settings</code> <code>Dict[str, Any]</code> <p>Preloaded project settings.</p> <code>None</code> <p>Returns:</p> Type Description <p>Union[str, None]: Path to template or None if none of profiles match current context. (Existence of formatted path is not validated.)</p> Source code in <code>client/ayon_core/pipeline/workfile/path_resolving.py</code> <pre><code>def get_custom_workfile_template_by_string_context(\n    project_name,\n    folder_path,\n    task_name,\n    host_name,\n    anatomy=None,\n    project_settings=None\n):\n    \"\"\"Filter and fill workfile template profiles by passed context.\n\n    Passed context are string representations of project, folder and task.\n    Function will query documents of project and folder to be able to use\n    `get_custom_workfile_template` for rest of logic.\n\n    Args:\n        project_name (str): Project name.\n        folder_path (str): Folder path.\n        task_name (str): Task name.\n        host_name (str): Name of host.\n        anatomy (Anatomy): Optionally prepared anatomy object for passed\n            project.\n        project_settings (Dict[str, Any]): Preloaded project settings.\n\n    Returns:\n        Union[str, None]: Path to template or None if none of profiles match\n            current context. (Existence of formatted path is not validated.)\n\n    \"\"\"\n\n    project_entity = ayon_api.get_project(project_name)\n    folder_entity = ayon_api.get_folder_by_path(project_name, folder_path)\n    task_entity = ayon_api.get_task_by_name(\n        project_name, folder_entity[\"id\"], task_name\n    )\n\n    return get_custom_workfile_template(\n        project_entity,\n        folder_entity,\n        task_entity,\n        host_name,\n        anatomy,\n        project_settings\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/path_resolving.html#client.ayon_core.pipeline.workfile.path_resolving.get_last_workfile","title":"<code>get_last_workfile(workdir, file_template, template_data, extensions, full_path=False)</code>","text":"<p>Return last the workfile filename.</p> <p>Returns first file name/path if there are not workfiles yet.</p> <p>Parameters:</p> Name Type Description Default <code>workdir</code> <code>str</code> <p>Path to dir where workfiles are stored.</p> required <code>file_template</code> <code>str</code> <p>Template of file name.</p> required <code>template_data</code> <code>Dict[str, Any]</code> <p>Data for filling template.</p> required <code>extensions</code> <code>Iterable[str]</code> <p>All allowed file extensions of workfile.</p> required <code>full_path</code> <code>bool</code> <p>Return full path to the file or only filename.</p> <code>False</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Last or first workfile file name or path based on 'full_path' value.</p> Source code in <code>client/ayon_core/pipeline/workfile/path_resolving.py</code> <pre><code>def get_last_workfile(\n    workdir: str,\n    file_template: str,\n    template_data: dict[str, Any],\n    extensions: set[str],\n    full_path: bool = False,\n) -&gt; str:\n    \"\"\"Return last the workfile filename.\n\n    Returns first file name/path if there are not workfiles yet.\n\n    Args:\n        workdir (str): Path to dir where workfiles are stored.\n        file_template (str): Template of file name.\n        template_data (Dict[str, Any]): Data for filling template.\n        extensions (Iterable[str]): All allowed file extensions of workfile.\n        full_path (bool): Return full path to the file or only filename.\n\n    Returns:\n        str: Last or first workfile file name or path based on\n            'full_path' value.\n\n    \"\"\"\n    # TODO (iLLiCiTiT): Remove the argument 'full_path' and return only full\n    #   path. As far as I can tell it is always called with 'full_path' set\n    #   to 'True'.\n    # - it has to be 2 step operation, first warn about having it 'False', and\n    #   then warn about having it filled.\n    if full_path is False:\n        warnings.warn(\n            \"Argument 'full_path' will be removed and will return\"\n            \" only full path in future.\",\n            DeprecationWarning,\n        )\n\n    filepaths, dotted_extensions = _filter_dir_files_by_ext(\n        workdir, extensions\n    )\n    filepath = get_last_workfile_from_paths(\n        filepaths,\n        file_template,\n        template_data,\n        dotted_extensions\n    )\n    if filepath is None:\n        data = copy.deepcopy(template_data)\n        host_name = data.get(\"host\", {}).get(\"name\")\n        if host_name is None:\n            host_name = data[\"app\"]\n        data[\"version\"] = version_start.get_versioning_start(\n            data[\"project\"][\"name\"],\n            host_name,\n            task_name=data[\"task\"][\"name\"],\n            task_type=data[\"task\"][\"type\"],\n            product_base_type=\"workfile\",\n        )\n        data.pop(\"comment\", None)\n        if data.get(\"ext\") is None:\n            data[\"ext\"] = next(iter(extensions), \"\")\n        data[\"ext\"] = data[\"ext\"].lstrip(\".\")\n        filename = StringTemplate.format_strict_template(file_template, data)\n        filepath = os.path.join(workdir, filename)\n\n    if full_path:\n        return os.path.normpath(filepath)\n    return os.path.basename(filepath)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/path_resolving.html#client.ayon_core.pipeline.workfile.path_resolving.get_last_workfile_from_paths","title":"<code>get_last_workfile_from_paths(filepaths, file_template, template_data, extensions)</code>","text":"<p>Return the last workfile filename.</p> <p>Returns the file with version 1 if there is not workfile yet.</p> <p>Parameters:</p> Name Type Description Default <code>filepaths</code> <code>list[str]</code> <p>Paths to workfiles.</p> required <code>file_template</code> <code>str</code> <p>Template of file name.</p> required <code>template_data</code> <code>dict[str, Any]</code> <p>Data for filling template.</p> required <code>extensions</code> <code>set[str]</code> <p>All allowed file extensions of workfile.</p> required <p>Returns:</p> Type Description <code>Optional[str]</code> <p>Optional[str]: Last workfile path.</p> Source code in <code>client/ayon_core/pipeline/workfile/path_resolving.py</code> <pre><code>def get_last_workfile_from_paths(\n    filepaths: list[str],\n    file_template: str,\n    template_data: dict[str, Any],\n    extensions: set[str],\n) -&gt; Optional[str]:\n    \"\"\"Return the last workfile filename.\n\n    Returns the file with version 1 if there is not workfile yet.\n\n    Args:\n        filepaths (list[str]): Paths to workfiles.\n        file_template (str): Template of file name.\n        template_data (dict[str, Any]): Data for filling template.\n        extensions (set[str]): All allowed file extensions of workfile.\n\n    Returns:\n        Optional[str]: Last workfile path.\n\n    \"\"\"\n    filepath, _version = get_last_workfile_with_version_from_paths(\n        filepaths, file_template, template_data, extensions\n    )\n    return filepath\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/path_resolving.html#client.ayon_core.pipeline.workfile.path_resolving.get_last_workfile_with_version","title":"<code>get_last_workfile_with_version(workdir, file_template, template_data, extensions)</code>","text":"<p>Return last workfile version.</p> <p>Using the workfile template and its filling data to find the most possible last version of workfile which was created for the context.</p> <p>Functionality is fully based on knowing which keys are optional or what values are expected as value.</p> <p>The last modified file is used if more files can be considered as last workfile.</p> <p>Parameters:</p> Name Type Description Default <code>workdir</code> <code>str</code> <p>Path to dir where workfiles are stored.</p> required <code>file_template</code> <code>str</code> <p>Template of file name.</p> required <code>template_data</code> <code>dict[str, Any]</code> <p>Data for filling template.</p> required <code>extensions</code> <code>set[str]</code> <p>All allowed file extensions of workfile.</p> required <p>Returns:</p> Type Description <code>tuple[Optional[str], Optional[int]]</code> <p>tuple[Optional[str], Optional[int]]: Last workfile with version if there is any workfile otherwise None for both.</p> Source code in <code>client/ayon_core/pipeline/workfile/path_resolving.py</code> <pre><code>def get_last_workfile_with_version(\n    workdir: str,\n    file_template: str,\n    template_data: dict[str, Any],\n    extensions: set[str],\n) -&gt; tuple[Optional[str], Optional[int]]:\n    \"\"\"Return last workfile version.\n\n    Using the workfile template and its filling data to find the most possible\n    last version of workfile which was created for the context.\n\n    Functionality is fully based on knowing which keys are optional or what\n    values are expected as value.\n\n    The last modified file is used if more files can be considered as\n    last workfile.\n\n    Args:\n        workdir (str): Path to dir where workfiles are stored.\n        file_template (str): Template of file name.\n        template_data (dict[str, Any]): Data for filling template.\n        extensions (set[str]): All allowed file extensions of workfile.\n\n    Returns:\n        tuple[Optional[str], Optional[int]]: Last workfile with version\n            if there is any workfile otherwise None for both.\n\n    \"\"\"\n    if not os.path.exists(workdir):\n        return None, None\n\n    filepaths, dotted_extensions = _filter_dir_files_by_ext(\n        workdir, extensions\n    )\n\n    return get_last_workfile_with_version_from_paths(\n        filepaths,\n        file_template,\n        template_data,\n        dotted_extensions,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/path_resolving.html#client.ayon_core.pipeline.workfile.path_resolving.get_last_workfile_with_version_from_paths","title":"<code>get_last_workfile_with_version_from_paths(filepaths, file_template, template_data, extensions)</code>","text":"<p>Return last workfile version.</p> <p>Using the workfile template and its template data find most possible last version of workfile which was created for the context.</p> <p>Functionality is fully based on knowing which keys are optional or what values are expected as value.</p> <p>The last modified file is used if more files can be considered as last workfile.</p> <p>Parameters:</p> Name Type Description Default <code>filepaths</code> <code>list[str]</code> <p>Workfile paths.</p> required <code>file_template</code> <code>str</code> <p>Template of file name.</p> required <code>template_data</code> <code>Dict[str, Any]</code> <p>Data for filling template.</p> required <code>extensions</code> <code>set[str]</code> <p>All allowed file extensions of workfile.</p> required <p>Returns:</p> Type Description <code>tuple[Optional[str], Optional[int]]</code> <p>tuple[Optional[str], Optional[int]]: Last workfile with version if there is any workfile otherwise None for both.</p> Source code in <code>client/ayon_core/pipeline/workfile/path_resolving.py</code> <pre><code>def get_last_workfile_with_version_from_paths(\n    filepaths: list[str],\n    file_template: str,\n    template_data: dict[str, Any],\n    extensions: set[str],\n) -&gt; tuple[Optional[str], Optional[int]]:\n    \"\"\"Return last workfile version.\n\n    Using the workfile template and its template data find most possible last\n    version of workfile which was created for the context.\n\n    Functionality is fully based on knowing which keys are optional or what\n    values are expected as value.\n\n    The last modified file is used if more files can be considered as\n    last workfile.\n\n    Args:\n        filepaths (list[str]): Workfile paths.\n        file_template (str): Template of file name.\n        template_data (Dict[str, Any]): Data for filling template.\n        extensions (set[str]): All allowed file extensions of workfile.\n\n    Returns:\n        tuple[Optional[str], Optional[int]]: Last workfile with version\n            if there is any workfile otherwise None for both.\n\n    \"\"\"\n    if not filepaths:\n        return None, None\n\n    dotted_extensions = set()\n    for ext in extensions:\n        if not ext.startswith(\".\"):\n            ext = f\".{ext}\"\n        dotted_extensions.add(re.escape(ext))\n\n    # Build template without optionals, version to digits only regex\n    # and comment to any definable value.\n    # Escape extensions dot for regex\n    ext_expression = \"(?:\" + \"|\".join(dotted_extensions) + \")\"\n\n    for pattern, replacement in (\n        # Replace `.{ext}` with `{ext}` so we are sure dot is not at the end\n        (r\"\\.?{ext}\", ext_expression),\n        # Replace optional keys with optional content regex\n        (r\"&lt;.*?&gt;\", r\".*?\"),\n        # Replace `{version}` with group regex\n        (r\"{version.*?}\", r\"([0-9]+)\"),\n        (r\"{comment.*?}\", r\".+?\"),\n    ):\n        file_template = re.sub(pattern, replacement, file_template)\n\n    file_template = StringTemplate.format_strict_template(\n        file_template, template_data\n    )\n\n    # Match with ignore case on Windows due to the Windows\n    # OS not being case-sensitive. This avoids later running\n    # into the error that the file did exist if it existed\n    # with a different upper/lower-case.\n    kwargs = {}\n    if platform.system().lower() == \"windows\":\n        kwargs[\"flags\"] = re.IGNORECASE\n\n    # Get highest version among existing matching files\n    version = None\n    output_filepaths = []\n    for filepath in sorted(filepaths):\n        filename = os.path.basename(filepath)\n        match = re.match(file_template, filename, **kwargs)\n        if not match:\n            continue\n\n        if not match.groups():\n            output_filepaths.append(filename)\n            continue\n\n        file_version = int(match.group(1))\n        if version is None or file_version &gt; version:\n            output_filepaths.clear()\n            version = file_version\n\n        if file_version == version:\n            output_filepaths.append(filepath)\n\n    # Use file modification time to use most recent file if there are\n    #   multiple workfiles with the same version\n    output_filepath = None\n    last_time = None\n    for _output_filepath in output_filepaths:\n        mod_time = None\n        if os.path.exists(_output_filepath):\n            mod_time = os.path.getmtime(_output_filepath)\n        if (\n            last_time is None\n            or (mod_time is not None and last_time &lt; mod_time)\n        ):\n            output_filepath = _output_filepath\n            last_time = mod_time\n\n    return output_filepath, version\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/path_resolving.html#client.ayon_core.pipeline.workfile.path_resolving.get_workdir","title":"<code>get_workdir(project_entity, folder_entity, task_entity, host_name, anatomy=None, template_key=None, project_settings=None)</code>","text":"<p>Fill workdir path from entered data and project's anatomy.</p> <p>Parameters:</p> Name Type Description Default <code>project_entity</code> <code>Dict[str, Any]</code> <p>Project entity.</p> required <code>folder_entity</code> <code>Dict[str, Any]</code> <p>Folder entity.</p> required <code>task_entity</code> <code>dict[str, Any]</code> <p>Task entity.</p> required <code>host_name</code> <code>str</code> <p>Host which is used to workdir. This is required because workdir template may contain <code>{app}</code> key. In <code>Session</code> is stored under <code>AYON_HOST_NAME</code> key.</p> required <code>anatomy</code> <code>Anatomy</code> <p>Optional argument. Anatomy object is created using project name from <code>project_entity</code>. It is preferred to pass this argument as initialization of a new Anatomy object may be time-consuming.</p> <code>None</code> <code>template_key</code> <code>str</code> <p>Key of work templates in anatomy templates. Default value is defined in <code>get_workdir_with_workdir_data</code>.</p> <code>None</code> <code>project_settings</code> <code>Dict[str, Any]</code> <p>Prepared project settings for project name. Optional to make processing faster. Ans id used only if 'template_key' is not passed.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>AnatomyTemplateResult</code> <code>'AnatomyTemplateResult'</code> <p>Workdir path.</p> Source code in <code>client/ayon_core/pipeline/workfile/path_resolving.py</code> <pre><code>def get_workdir(\n    project_entity: dict[str, Any],\n    folder_entity: dict[str, Any],\n    task_entity: dict[str, Any],\n    host_name: str,\n    anatomy=None,\n    template_key=None,\n    project_settings=None\n) -&gt; \"AnatomyTemplateResult\":\n    \"\"\"Fill workdir path from entered data and project's anatomy.\n\n    Args:\n        project_entity (Dict[str, Any]): Project entity.\n        folder_entity (Dict[str, Any]): Folder entity.\n        task_entity (dict[str, Any]): Task entity.\n        host_name (str): Host which is used to workdir. This is required\n            because workdir template may contain `{app}` key. In `Session`\n            is stored under `AYON_HOST_NAME` key.\n        anatomy (Anatomy): Optional argument. Anatomy object is created using\n            project name from `project_entity`. It is preferred to pass this\n            argument as initialization of a new Anatomy object may be\n            time-consuming.\n        template_key (str): Key of work templates in anatomy templates. Default\n            value is defined in `get_workdir_with_workdir_data`.\n        project_settings(Dict[str, Any]): Prepared project settings for\n            project name. Optional to make processing faster. Ans id used only\n            if 'template_key' is not passed.\n\n    Returns:\n        AnatomyTemplateResult: Workdir path.\n\n    \"\"\"\n    if not anatomy:\n        anatomy = Anatomy(\n            project_entity[\"name\"], project_entity=project_entity\n        )\n\n    workdir_data = get_template_data(\n        project_entity,\n        folder_entity,\n        task_entity,\n        host_name,\n    )\n    # Output is AnatomyTemplateResult object which contain useful data\n    return get_workdir_with_workdir_data(\n        workdir_data,\n        anatomy.project_name,\n        anatomy,\n        template_key,\n        project_settings\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/path_resolving.html#client.ayon_core.pipeline.workfile.path_resolving.get_workdir_with_workdir_data","title":"<code>get_workdir_with_workdir_data(workdir_data, project_name, anatomy=None, template_key=None, project_settings=None)</code>","text":"<p>Fill workdir path from entered data and project's anatomy.</p> <p>It is possible to pass only project's name instead of project's anatomy but one of them must be entered. It is preferred to enter anatomy if is available as initialization of a new Anatomy object may be time consuming.</p> <p>Parameters:</p> Name Type Description Default <code>workdir_data</code> <code>Dict[str, Any]</code> <p>Data to fill workdir template.</p> required <code>project_name</code> <code>str</code> <p>Project's name.</p> required <code>anatomy</code> <code>Anatomy</code> <p>Anatomy object for specific project. Faster processing if is passed.</p> <code>None</code> <code>template_key</code> <code>str</code> <p>Key of work templates in anatomy templates. If not passed <code>get_workfile_template_key_from_context</code> is used to get it.</p> <code>None</code> <code>project_settings</code> <code>Dict[str, Any]</code> <p>Prepared project settings for project name. Optional to make processing faster. Ans id used only if 'template_key' is not passed.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>AnatomyTemplateResult</code> <code>'AnatomyTemplateResult'</code> <p>Workdir path.</p> Source code in <code>client/ayon_core/pipeline/workfile/path_resolving.py</code> <pre><code>def get_workdir_with_workdir_data(\n    workdir_data,\n    project_name,\n    anatomy=None,\n    template_key=None,\n    project_settings=None\n) -&gt; \"AnatomyTemplateResult\":\n    \"\"\"Fill workdir path from entered data and project's anatomy.\n\n    It is possible to pass only project's name instead of project's anatomy but\n    one of them **must** be entered. It is preferred to enter anatomy if is\n    available as initialization of a new Anatomy object may be time consuming.\n\n    Args:\n        workdir_data (Dict[str, Any]): Data to fill workdir template.\n        project_name (str): Project's name.\n        anatomy (Anatomy): Anatomy object for specific project. Faster\n            processing if is passed.\n        template_key (str): Key of work templates in anatomy templates. If not\n            passed `get_workfile_template_key_from_context` is used to get it.\n        project_settings(Dict[str, Any]): Prepared project settings for\n            project name. Optional to make processing faster. Ans id used only\n            if 'template_key' is not passed.\n\n    Returns:\n        AnatomyTemplateResult: Workdir path.\n\n    \"\"\"\n    if not anatomy:\n        anatomy = Anatomy(project_name)\n\n    if not template_key:\n        host_name = workdir_data.get(\"host\", {}).get(\"name\")\n        if host_name is None:\n            host_name = workdir_data[\"app\"]\n        template_key = get_workfile_template_key(\n            workdir_data[\"project\"][\"name\"],\n            workdir_data[\"task\"][\"type\"],\n            host_name,\n            project_settings,\n        )\n\n    template_obj = anatomy.get_template_item(\n        \"work\", template_key, \"directory\"\n    )\n    # Output is AnatomyTemplateResult object which contain useful data\n    output = template_obj.format_strict(workdir_data)\n    if output:\n        return output.normalized()\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/path_resolving.html#client.ayon_core.pipeline.workfile.path_resolving.get_workfile_template_key","title":"<code>get_workfile_template_key(project_name, task_type, host_name, project_settings=None)</code>","text":"<p>Workfile template key which should be used to get workfile template.</p> <p>Function is using profiles from project settings to return right template for passed task type and host name.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>task_type</code> <code>str</code> <p>Task type.</p> required <code>host_name</code> <code>str</code> <p>Host name (e.g. \"maya\", \"nuke\", ...)</p> required <code>project_settings</code> <code>Dict[str, Any]</code> <p>Prepared project settings for project name. Optional to make processing faster.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/workfile/path_resolving.py</code> <pre><code>def get_workfile_template_key(\n    project_name, task_type, host_name, project_settings=None\n):\n    \"\"\"Workfile template key which should be used to get workfile template.\n\n    Function is using profiles from project settings to return right template\n    for passed task type and host name.\n\n    Args:\n        project_name(str): Project name.\n        task_type(str): Task type.\n        host_name(str): Host name (e.g. \"maya\", \"nuke\", ...)\n        project_settings(Dict[str, Any]): Prepared project settings for\n            project name. Optional to make processing faster.\n    \"\"\"\n\n    default = \"work\"\n    if not task_type or not host_name:\n        return default\n\n    if not project_settings:\n        project_settings = get_project_settings(project_name)\n\n    try:\n        profiles = (\n            project_settings\n            [\"core\"]\n            [\"tools\"]\n            [\"Workfiles\"]\n            [\"workfile_template_profiles\"]\n        )\n    except Exception:\n        profiles = []\n\n    if not profiles:\n        return default\n\n    profile_filter = {\n        \"task_types\": task_type,\n        \"host_names\": host_name,\n    }\n    profile = filter_profiles(profiles, profile_filter)\n    if profile:\n        return profile[\"workfile_template\"] or default\n    return default\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/path_resolving.html#client.ayon_core.pipeline.workfile.path_resolving.get_workfile_template_key_from_context","title":"<code>get_workfile_template_key_from_context(project_name, folder_path, task_name, host_name, project_settings=None)</code>","text":"<p>Helper function to get template key for workfile template.</p> <p>Do the same as <code>get_workfile_template_key</code> but returns value for \"session context\".</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>folder_path</code> <code>str</code> <p>Folder path.</p> required <code>task_name</code> <code>str</code> <p>Task name.</p> required <code>host_name</code> <code>str</code> <p>Host name.</p> required <code>project_settings</code> <code>Dict[str, Any]</code> <p>Project settings for passed 'project_name'. Not required at all but makes function faster.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Workfile template name.</p> Source code in <code>client/ayon_core/pipeline/workfile/path_resolving.py</code> <pre><code>def get_workfile_template_key_from_context(\n    project_name: str,\n    folder_path: str,\n    task_name: str,\n    host_name: str,\n    project_settings: Optional[Dict[str, Any]] = None,\n) -&gt; str:\n    \"\"\"Helper function to get template key for workfile template.\n\n    Do the same as `get_workfile_template_key` but returns value for \"session\n    context\".\n\n    Args:\n        project_name (str): Project name.\n        folder_path (str): Folder path.\n        task_name (str): Task name.\n        host_name (str): Host name.\n        project_settings (Dict[str, Any]): Project settings for passed\n            'project_name'. Not required at all but makes function faster.\n\n    Returns:\n        str: Workfile template name.\n\n    \"\"\"\n    folder_entity = ayon_api.get_folder_by_path(\n        project_name,\n        folder_path,\n        fields={\"id\"},\n    )\n    task_entity = ayon_api.get_task_by_name(\n        project_name,\n        folder_entity[\"id\"],\n        task_name,\n        fields={\"taskType\"},\n    )\n    task_type = task_entity.get(\"taskType\")\n\n    return get_workfile_template_key(\n        project_name, task_type, host_name, project_settings\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/path_resolving.html#client.ayon_core.pipeline.workfile.path_resolving.parse_dynamic_data_from_workfile","title":"<code>parse_dynamic_data_from_workfile(filename, file_template, template_data)</code>","text":"<p>Parse dynamic data from a workfile filename.</p> <p>Dynamic data are 'version', 'comment' and 'ext'.</p> <p>Parameters:</p> Name Type Description Default <code>filename</code> <code>str</code> <p>Workfile filename.</p> required <code>file_template</code> <code>str</code> <p>Workfile file template.</p> required <code>template_data</code> <code>dict[str, Any]</code> <p>Data to fill the template with.</p> required <p>Returns:</p> Name Type Description <code>WorkfileParsedData</code> <code>WorkfileParsedData</code> <p>Dynamic data parsed from the filename.</p> Source code in <code>client/ayon_core/pipeline/workfile/path_resolving.py</code> <pre><code>def parse_dynamic_data_from_workfile(\n    filename: str,\n    file_template: str,\n    template_data: dict[str, Any],\n) -&gt; WorkfileParsedData:\n    \"\"\"Parse dynamic data from a workfile filename.\n\n    Dynamic data are 'version', 'comment' and 'ext'.\n\n    Args:\n        filename (str): Workfile filename.\n        file_template (str): Workfile file template.\n        template_data (dict[str, Any]): Data to fill the template with.\n\n    Returns:\n        WorkfileParsedData: Dynamic data parsed from the filename.\n\n    \"\"\"\n    parser = WorkfileDataParser(file_template, template_data)\n    return parser.parse_data(filename)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/path_resolving.html#client.ayon_core.pipeline.workfile.path_resolving.parse_dynamic_data_from_workfiles","title":"<code>parse_dynamic_data_from_workfiles(filenames, file_template, template_data)</code>","text":"<p>Parse dynamic data from a workfiles filenames.</p> <p>Dynamic data are 'version', 'comment' and 'ext'.</p> <p>Parameters:</p> Name Type Description Default <code>filenames</code> <code>list[str]</code> <p>Workfiles filenames.</p> required <code>file_template</code> <code>str</code> <p>Workfile file template.</p> required <code>template_data</code> <code>dict[str, Any]</code> <p>Data to fill the template with.</p> required <p>Returns:</p> Type Description <code>dict[str, WorkfileParsedData]</code> <p>dict[str, WorkfileParsedData]: Dynamic data parsed from the filenames by filename.</p> Source code in <code>client/ayon_core/pipeline/workfile/path_resolving.py</code> <pre><code>def parse_dynamic_data_from_workfiles(\n    filenames: list[str],\n    file_template: str,\n    template_data: dict[str, Any],\n) -&gt; dict[str, WorkfileParsedData]:\n    \"\"\"Parse dynamic data from a workfiles filenames.\n\n    Dynamic data are 'version', 'comment' and 'ext'.\n\n    Args:\n        filenames (list[str]): Workfiles filenames.\n        file_template (str): Workfile file template.\n        template_data (dict[str, Any]): Data to fill the template with.\n\n    Returns:\n        dict[str, WorkfileParsedData]: Dynamic data parsed from the filenames\n            by filename.\n\n    \"\"\"\n    parser = WorkfileDataParser(file_template, template_data)\n    return {\n        filename: parser.parse_data(filename)\n        for filename in filenames\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/utils.html","title":"utils","text":""},{"location":"autoapi/client/ayon_core/pipeline/workfile/utils.html#client.ayon_core.pipeline.workfile.utils.MissingWorkdirError","title":"<code>MissingWorkdirError</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Raised when accessing a work directory not found on disk.</p> Source code in <code>client/ayon_core/pipeline/workfile/utils.py</code> <pre><code>class MissingWorkdirError(Exception):\n    \"\"\"Raised when accessing a work directory not found on disk.\"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/utils.html#client.ayon_core.pipeline.workfile.utils.copy_workfile_to_context","title":"<code>copy_workfile_to_context(src_workfile_path, folder_entity, task_entity, *, version=None, comment=None, description=None, open_workfile=True, prepared_data=None)</code>","text":"<p>Copy workfile to a context.</p> <p>Copy workfile to a specified folder and task. Destination path is     calculated based on passed information.</p> <p>Parameters:</p> Name Type Description Default <code>src_workfile_path</code> <code>str</code> <p>Source workfile path.</p> required <code>folder_entity</code> <code>dict[str, Any]</code> <p>Target folder entity.</p> required <code>task_entity</code> <code>dict[str, Any]</code> <p>Target task entity.</p> required <code>version</code> <code>Optional[int]</code> <p>Workfile version. Use next version if not passed.</p> <code>None</code> <code>comment</code> <code>optional[str]</code> <p>Workfile comment.</p> <code>None</code> <code>description</code> <code>Optional[str]</code> <p>Workfile description.</p> <code>None</code> <code>prepared_data</code> <code>Optional[CopyWorkfileOptionalData]</code> <p>Prepared data for speed enhancements. Rootless path is calculated in this function.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/workfile/utils.py</code> <pre><code>def copy_workfile_to_context(\n    src_workfile_path: str,\n    folder_entity: dict[str, Any],\n    task_entity: dict[str, Any],\n    *,\n    version: Optional[int] = None,\n    comment: Optional[str] = None,\n    description: Optional[str] = None,\n    open_workfile: bool = True,\n    prepared_data: Optional[CopyWorkfileOptionalData] = None,\n) -&gt; None:\n    \"\"\"Copy workfile to a context.\n\n    Copy workfile to a specified folder and task. Destination path is\n        calculated based on passed information.\n\n    Args:\n        src_workfile_path (str): Source workfile path.\n        folder_entity (dict[str, Any]): Target folder entity.\n        task_entity (dict[str, Any]): Target task entity.\n        version (Optional[int]): Workfile version. Use next version if not\n            passed.\n        comment (optional[str]): Workfile comment.\n        description (Optional[str]): Workfile description.\n        prepared_data (Optional[CopyWorkfileOptionalData]): Prepared data\n            for speed enhancements. Rootless path is calculated in this\n            function.\n\n    \"\"\"\n    from ayon_core.pipeline import Anatomy\n    from ayon_core.pipeline.context_tools import registered_host\n\n    host = registered_host()\n    project_name = host.get_current_project_name()\n\n    anatomy = prepared_data.anatomy\n    if anatomy is None:\n        if prepared_data.project_entity is None:\n            prepared_data.project_entity = ayon_api.get_project(\n                project_name\n            )\n        anatomy = Anatomy(\n            project_name, project_entity=prepared_data.project_entity\n        )\n        prepared_data.anatomy = anatomy\n\n    project_settings = prepared_data.project_settings\n    if project_settings is None:\n        project_settings = get_project_settings(project_name)\n        prepared_data.project_settings = project_settings\n\n    if version is None:\n        list_prepared_data = None\n        if prepared_data is not None:\n            list_prepared_data = ListWorkfilesOptionalData(\n                project_entity=prepared_data.project_entity,\n                anatomy=prepared_data.anatomy,\n                project_settings=prepared_data.project_settings,\n                workfile_entities=prepared_data.workfile_entities,\n            )\n\n        workfiles = host.list_workfiles(\n            project_name,\n            folder_entity,\n            task_entity,\n            prepared_data=list_prepared_data\n        )\n        if workfiles:\n            version = max(\n                workfile.version\n                for workfile in workfiles\n            ) + 1\n        else:\n            version = get_versioning_start(\n                project_name,\n                host.name,\n                task_name=task_entity[\"name\"],\n                task_type=task_entity[\"taskType\"],\n                product_base_type=\"workfile\",\n            )\n\n    task_type = task_entity[\"taskType\"]\n    template_key = get_workfile_template_key(\n        project_name,\n        task_type,\n        host.name,\n        project_settings=prepared_data.project_settings\n    )\n\n    template_data = get_template_data(\n        prepared_data.project_entity,\n        folder_entity,\n        task_entity,\n        host.name,\n        prepared_data.project_settings,\n    )\n    template_data[\"version\"] = version\n    if comment:\n        template_data[\"comment\"] = comment\n\n    workfile_extensions = host.get_workfile_extensions()\n    if workfile_extensions:\n        ext = os.path.splitext(src_workfile_path)[1].lstrip(\".\")\n        template_data[\"ext\"] = ext\n\n    workfile_template = anatomy.get_template_item(\n        \"work\", template_key, \"path\"\n    )\n    workfile_path = workfile_template.format_strict(template_data)\n    prepared_data.rootless_path = workfile_path.rootless\n    host.copy_workfile(\n        src_workfile_path,\n        workfile_path,\n        folder_entity,\n        task_entity,\n        version=version,\n        comment=comment,\n        description=description,\n        open_workfile=open_workfile,\n        prepared_data=prepared_data,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/utils.html#client.ayon_core.pipeline.workfile.utils.find_workfile_rootless_path","title":"<code>find_workfile_rootless_path(workfile_path, project_name, folder_entity, task_entity, host_name, *, project_entity=None, project_settings=None, anatomy=None)</code>","text":"<p>Find rootless workfile path.</p> Source code in <code>client/ayon_core/pipeline/workfile/utils.py</code> <pre><code>def find_workfile_rootless_path(\n    workfile_path: str,\n    project_name: str,\n    folder_entity: dict[str, Any],\n    task_entity: dict[str, Any],\n    host_name: str,\n    *,\n    project_entity: Optional[dict[str, Any]] = None,\n    project_settings: Optional[dict[str, Any]] = None,\n    anatomy: Optional[\"Anatomy\"] = None,\n) -&gt; str:\n    \"\"\"Find rootless workfile path.\"\"\"\n    if anatomy is None:\n        from ayon_core.pipeline import Anatomy\n\n        anatomy = Anatomy(project_name, project_entity=project_entity)\n\n    task_type = task_entity[\"taskType\"]\n    template_key = get_workfile_template_key(\n        project_name,\n        task_type,\n        host_name,\n        project_settings=project_settings\n    )\n    dir_template = anatomy.get_template_item(\n        \"work\", template_key, \"directory\"\n    )\n    result = dir_template.format({\"root\": anatomy.roots})\n    used_root = result.used_values.get(\"root\")\n    rootless_path = str(workfile_path)\n    if platform.system().lower() == \"windows\":\n        rootless_path = rootless_path.replace(\"\\\\\", \"/\")\n\n    root_key = root_value = None\n    if used_root is not None:\n        root_key, root_value = next(iter(used_root.items()))\n        if platform.system().lower() == \"windows\":\n            root_value = root_value.replace(\"\\\\\", \"/\")\n\n    if root_value and rootless_path.startswith(root_value):\n        rootless_path = rootless_path[len(root_value):].lstrip(\"/\")\n        rootless_path = f\"{{root[{root_key}]}}/{rootless_path}\"\n    else:\n        success, result = anatomy.find_root_template_from_path(rootless_path)\n        if success:\n            rootless_path = result\n    return rootless_path\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/utils.html#client.ayon_core.pipeline.workfile.utils.get_workfiles_info","title":"<code>get_workfiles_info(workfile_path, project_name, task_id, *, anatomy=None, workfile_entities=None)</code>","text":"<p>Find workfile info entity for a workfile path.</p> <p>Parameters:</p> Name Type Description Default <code>workfile_path</code> <code>str</code> <p>Workfile path.</p> required <code>project_name</code> <code>str</code> <p>The name of the project.</p> required <code>task_id</code> <code>str</code> <p>Task id under which is workfile created.</p> required <code>anatomy</code> <code>Optional[Anatomy]</code> <p>Project anatomy used to get roots.</p> <code>None</code> <code>workfile_entities</code> <code>Optional[list[dict[str, Any]]]</code> <p>Pre-fetched workfile entities related to the task.</p> <code>None</code> <p>Returns:</p> Type Description <code>Optional[dict[str, Any]]</code> <p>Optional[dict[str, Any]]: Workfile info entity if found, otherwise <code>None</code>.</p> Source code in <code>client/ayon_core/pipeline/workfile/utils.py</code> <pre><code>def get_workfiles_info(\n    workfile_path: str,\n    project_name: str,\n    task_id: str,\n    *,\n    anatomy: Optional[\"Anatomy\"] = None,\n    workfile_entities: Optional[list[dict[str, Any]]] = None,\n) -&gt; Optional[dict[str, Any]]:\n    \"\"\"Find workfile info entity for a workfile path.\n\n    Args:\n        workfile_path (str): Workfile path.\n        project_name (str): The name of the project.\n        task_id (str): Task id under which is workfile created.\n        anatomy (Optional[Anatomy]): Project anatomy used to get roots.\n        workfile_entities (Optional[list[dict[str, Any]]]): Pre-fetched\n            workfile entities related to the task.\n\n    Returns:\n        Optional[dict[str, Any]]: Workfile info entity if found, otherwise\n            `None`.\n\n    \"\"\"\n    if anatomy is None:\n        anatomy = Anatomy(project_name)\n\n    if workfile_entities is None:\n        workfile_entities = list(ayon_api.get_workfiles_info(\n            project_name,\n            task_ids=[task_id],\n        ))\n\n    if platform.system().lower() == \"windows\":\n        workfile_path = workfile_path.replace(\"\\\\\", \"/\")\n    workfile_path = workfile_path.lower()\n\n    for workfile_entity in workfile_entities:\n        path = workfile_entity[\"path\"]\n        filled_path = anatomy.fill_root(path)\n        if platform.system().lower() == \"windows\":\n            filled_path = filled_path.replace(\"\\\\\", \"/\")\n        filled_path = filled_path.lower()\n        if filled_path == workfile_path:\n            return workfile_entity\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/utils.html#client.ayon_core.pipeline.workfile.utils.save_current_workfile_to","title":"<code>save_current_workfile_to(workfile_path, folder_path, task_name, *, version=None, comment=None, description=None, prepared_data=None)</code>","text":"<p>Save current workfile to new location or context.</p> <p>Parameters:</p> Name Type Description Default <code>workfile_path</code> <code>str</code> <p>Destination workfile path.</p> required <code>folder_path</code> <code>str</code> <p>Target folder path.</p> required <code>task_name</code> <code>str</code> <p>Target task name.</p> required <code>version</code> <code>Optional[int]</code> <p>Workfile version.</p> <code>None</code> <code>comment</code> <code>optional[str]</code> <p>Workfile comment.</p> <code>None</code> <code>description</code> <code>Optional[str]</code> <p>Workfile description.</p> <code>None</code> <code>prepared_data</code> <code>Optional[SaveWorkfileOptionalData]</code> <p>Prepared data for speed enhancements.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/workfile/utils.py</code> <pre><code>def save_current_workfile_to(\n    workfile_path: str,\n    folder_path: str,\n    task_name: str,\n    *,\n    version: Optional[int] = None,\n    comment: Optional[str] = None,\n    description: Optional[str] = None,\n    prepared_data: Optional[SaveWorkfileOptionalData] = None,\n) -&gt; None:\n    \"\"\"Save current workfile to new location or context.\n\n    Args:\n        workfile_path (str): Destination workfile path.\n        folder_path (str): Target folder path.\n        task_name (str): Target task name.\n        version (Optional[int]): Workfile version.\n        comment (optional[str]): Workfile comment.\n        description (Optional[str]): Workfile description.\n        prepared_data (Optional[SaveWorkfileOptionalData]): Prepared data\n            for speed enhancements.\n\n    \"\"\"\n    from ayon_core.pipeline.context_tools import registered_host\n\n    host = registered_host()\n    context = host.get_current_context()\n    project_name = context[\"project_name\"]\n    folder_entity = ayon_api.get_folder_by_path(\n        project_name, folder_path\n    )\n    task_entity = ayon_api.get_task_by_name(\n        project_name, folder_entity[\"id\"], task_name\n    )\n    host.save_workfile_with_context(\n        workfile_path,\n        folder_entity,\n        task_entity,\n        version=version,\n        comment=comment,\n        description=description,\n        prepared_data=prepared_data,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/utils.html#client.ayon_core.pipeline.workfile.utils.save_next_version","title":"<code>save_next_version(version=None, comment=None, description=None, *, prepared_data=None)</code>","text":"<p>Save workfile using current context, version and comment.</p> <p>Helper function to save a workfile using the current context. Last     workfile version + 1 is used if is not passed in.</p> <p>Parameters:</p> Name Type Description Default <code>version</code> <code>Optional[int]</code> <p>Workfile version that will be used. Last version + 1 is used if is not passed in.</p> <code>None</code> <code>comment</code> <code>optional[str]</code> <p>Workfile comment. Pass '\"\"' to clear comment. The current workfile comment is used if it is not passed.</p> <code>None</code> <code>description</code> <code>Optional[str]</code> <p>Workfile description.</p> <code>None</code> <code>prepared_data</code> <code>Optional[SaveWorkfileOptionalData]</code> <p>Prepared data for speed enhancements.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/workfile/utils.py</code> <pre><code>def save_next_version(\n    version: Optional[int] = None,\n    comment: Optional[str] = None,\n    description: Optional[str] = None,\n    *,\n    prepared_data: Optional[SaveWorkfileOptionalData] = None,\n) -&gt; None:\n    \"\"\"Save workfile using current context, version and comment.\n\n    Helper function to save a workfile using the current context. Last\n        workfile version + 1 is used if is not passed in.\n\n    Args:\n        version (Optional[int]): Workfile version that will be used. Last\n            version + 1 is used if is not passed in.\n        comment (optional[str]): Workfile comment. Pass '\"\"' to clear comment.\n            The current workfile comment is used if it is not passed.\n        description (Optional[str]): Workfile description.\n        prepared_data (Optional[SaveWorkfileOptionalData]): Prepared data\n            for speed enhancements.\n\n    \"\"\"\n    from ayon_core.pipeline import Anatomy\n    from ayon_core.pipeline.context_tools import registered_host\n\n    host = registered_host()\n    current_path = host.get_current_workfile()\n    if not current_path:\n        current_path = None\n    else:\n        current_path = os.path.normpath(current_path)\n\n    context = host.get_current_context()\n    project_name = context[\"project_name\"]\n    folder_path = context[\"folder_path\"]\n    task_name = context[\"task_name\"]\n    if prepared_data is None:\n        prepared_data = SaveWorkfileOptionalData()\n\n    project_entity = prepared_data.project_entity\n    anatomy = prepared_data.anatomy\n    project_settings = prepared_data.project_settings\n\n    if project_entity is None:\n        project_entity = ayon_api.get_project(project_name)\n        prepared_data.project_entity = project_entity\n\n    if project_settings is None:\n        project_settings = get_project_settings(project_name)\n        prepared_data.project_settings = project_settings\n\n    if anatomy is None:\n        anatomy = Anatomy(project_name, project_entity=project_entity)\n        prepared_data.anatomy = anatomy\n\n    folder_entity = ayon_api.get_folder_by_path(project_name, folder_path)\n    task_entity = ayon_api.get_task_by_name(\n        project_name, folder_entity[\"id\"], task_name\n    )\n\n    template_key = get_workfile_template_key(\n        project_name,\n        task_entity[\"taskType\"],\n        host.name,\n        project_settings=project_settings\n    )\n    file_template = anatomy.get_template_item(\"work\", template_key, \"file\")\n    template_data = get_template_data(\n        project_entity,\n        folder_entity,\n        task_entity,\n        host.name,\n        project_settings,\n    )\n    workdir = get_workdir(\n        project_entity,\n        folder_entity,\n        task_entity,\n        host.name,\n        anatomy=anatomy,\n        template_key=template_key,\n        project_settings=project_settings,\n    )\n    rootless_dir = workdir.rootless\n    last_workfile = None\n    current_workfile = None\n    if version is None or comment is None:\n        workfiles = host.list_workfiles(\n            project_name, folder_entity, task_entity,\n            prepared_data=ListWorkfilesOptionalData(\n                project_entity=project_entity,\n                anatomy=anatomy,\n                project_settings=project_settings,\n                template_key=template_key,\n            )\n        )\n        for workfile in workfiles:\n            if current_workfile is None and workfile.filepath == current_path:\n                current_workfile = workfile\n\n            if workfile.version is None:\n                continue\n\n            if (\n                last_workfile is None\n                or last_workfile.version &lt; workfile.version\n            ):\n                last_workfile = workfile\n\n    if version is None and last_workfile is not None:\n        version = last_workfile.version + 1\n\n    if version is None:\n        version = get_versioning_start(\n            project_name,\n            host.name,\n            task_name=task_entity[\"name\"],\n            task_type=task_entity[\"taskType\"],\n            product_base_type=\"workfile\",\n        )\n\n    # Re-use comment from the current workfile if is not passed in\n    if comment is None and current_workfile is not None:\n        comment = current_workfile.comment\n\n    template_data[\"version\"] = version\n    if comment:\n        template_data[\"comment\"] = comment\n\n    # Resolve extension\n    # - Don't fill any if the host does not have defined any -&gt; e.g. if host\n    #   uses directory instead of a file.\n    # 1. Use the current file extension.\n    # 2. Use the last known workfile extension.\n    # 3. Use the first extensions from 'get_workfile_extensions'.\n    ext = None\n    workfile_extensions = host.get_workfile_extensions()\n    if workfile_extensions:\n        if current_path:\n            ext = os.path.splitext(current_path)[1]\n        elif last_workfile is not None:\n            ext = os.path.splitext(last_workfile.filepath)[1]\n        else:\n            ext = next(iter(workfile_extensions))\n        ext = ext.lstrip(\".\")\n\n    if ext:\n        template_data[\"ext\"] = ext\n\n    filename = file_template.format_strict(template_data)\n    workfile_path = os.path.join(workdir, filename)\n    rootless_path = f\"{rootless_dir}/{filename}\"\n    if platform.system().lower() == \"windows\":\n        rootless_path = rootless_path.replace(\"\\\\\", \"/\")\n    prepared_data.rootless_path = rootless_path\n\n    host.save_workfile_with_context(\n        workfile_path,\n        folder_entity,\n        task_entity,\n        version=version,\n        comment=comment,\n        description=description,\n        prepared_data=prepared_data,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/utils.html#client.ayon_core.pipeline.workfile.utils.save_workfile_info","title":"<code>save_workfile_info(project_name, task_id, rootless_path, host_name, version=None, comment=None, description=None, username=None, data=None, workfile_entities=None)</code>","text":"<p>Save workfile info entity for a workfile path.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>The name of the project.</p> required <code>task_id</code> <code>str</code> <p>Task id under which is workfile created.</p> required <code>rootless_path</code> <code>str</code> <p>Rootless path of the workfile.</p> required <code>host_name</code> <code>str</code> <p>Name of host which is saving the workfile.</p> required <code>version</code> <code>Optional[int]</code> <p>Workfile version.</p> <code>None</code> <code>comment</code> <code>Optional[str]</code> <p>Workfile comment.</p> <code>None</code> <code>description</code> <code>Optional[str]</code> <p>Workfile description.</p> <code>None</code> <code>username</code> <code>Optional[str]</code> <p>Username of user who saves the workfile. If not provided, current user is used.</p> <code>None</code> <code>data</code> <code>Optional[dict[str, Any]]</code> <p>Additional workfile entity data.</p> <code>None</code> <code>workfile_entities</code> <code>Optional[list[dict[str, Any]]]</code> <p>Pre-fetched workfile entities related to task.</p> <code>None</code> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: Workfile info entity.</p> Source code in <code>client/ayon_core/pipeline/workfile/utils.py</code> <pre><code>def save_workfile_info(\n    project_name: str,\n    task_id: str,\n    rootless_path: str,\n    host_name: str,\n    version: Optional[int] = None,\n    comment: Optional[str] = None,\n    description: Optional[str] = None,\n    username: Optional[str] = None,\n    data: Optional[dict[str, Any]] = None,\n    workfile_entities: Optional[list[dict[str, Any]]] = None,\n) -&gt; dict[str, Any]:\n    \"\"\"Save workfile info entity for a workfile path.\n\n    Args:\n        project_name (str): The name of the project.\n        task_id (str): Task id under which is workfile created.\n        rootless_path (str): Rootless path of the workfile.\n        host_name (str): Name of host which is saving the workfile.\n        version (Optional[int]): Workfile version.\n        comment (Optional[str]): Workfile comment.\n        description (Optional[str]): Workfile description.\n        username (Optional[str]): Username of user who saves the workfile.\n            If not provided, current user is used.\n        data (Optional[dict[str, Any]]): Additional workfile entity data.\n        workfile_entities (Optional[list[dict[str, Any]]]): Pre-fetched\n            workfile entities related to task.\n\n    Returns:\n        dict[str, Any]: Workfile info entity.\n\n    \"\"\"\n    if workfile_entities is None:\n        workfile_entities = list(ayon_api.get_workfiles_info(\n            project_name,\n            task_ids=[task_id],\n        ))\n\n    workfile_entity = next(\n        (\n            _ent\n            for _ent in workfile_entities\n            if _ent[\"path\"] == rootless_path\n        ),\n        None\n    )\n\n    if username is None:\n        username = get_ayon_username()\n\n    attrib = {}\n    extension = os.path.splitext(rootless_path)[1]\n    for key, value in (\n        (\"extension\", extension),\n        (\"description\", description),\n    ):\n        if value is not None:\n            attrib[key] = value\n\n    if data is None:\n        data = {}\n\n    if not workfile_entity:\n        return _create_workfile_info_entity(\n            project_name,\n            task_id,\n            host_name,\n            rootless_path,\n            username,\n            version,\n            comment,\n            attrib,\n            data,\n        )\n\n    for key, value in (\n        (\"host_name\", host_name),\n        (\"version\", version),\n        (\"comment\", comment),\n    ):\n        if value is not None:\n            data[key] = value\n\n    changed_data = {}\n    old_data = workfile_entity[\"data\"]\n    for key, value in data.items():\n        if key not in old_data or old_data[key] != value:\n            changed_data[key] = value\n            workfile_entity[\"data\"][key] = value\n\n    changed_attrib = {}\n    old_attrib = workfile_entity[\"attrib\"]\n    for key, value in attrib.items():\n        if key not in old_attrib or old_attrib[key] != value:\n            changed_attrib[key] = value\n            workfile_entity[\"attrib\"][key] = value\n\n    update_data = {}\n    if changed_data:\n        update_data[\"data\"] = changed_data\n\n    if changed_attrib:\n        update_data[\"attrib\"] = changed_attrib\n\n    # Automatically fix 'createdBy' and 'updatedBy' fields\n    # NOTE both fields were not automatically filled by server\n    #   until 1.1.3 release.\n    if workfile_entity.get(\"createdBy\") is None:\n        update_data[\"createdBy\"] = username\n        workfile_entity[\"createdBy\"] = username\n\n    if workfile_entity.get(\"updatedBy\") != username:\n        update_data[\"updatedBy\"] = username\n        workfile_entity[\"updatedBy\"] = username\n\n    if not update_data:\n        return workfile_entity\n\n    session = OperationsSession()\n    session.update_entity(\n        project_name,\n        \"workfile\",\n        workfile_entity[\"id\"],\n        update_data,\n    )\n    session.commit()\n    return workfile_entity\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/utils.html#client.ayon_core.pipeline.workfile.utils.save_workfile_with_current_context","title":"<code>save_workfile_with_current_context(workfile_path, *, version=None, comment=None, description=None, prepared_data=None)</code>","text":"<p>Save current workfile to new location using current context.</p> <p>Helper function to save workfile using current context. Calls     'save_current_workfile_to' at the end.</p> <p>Parameters:</p> Name Type Description Default <code>workfile_path</code> <code>str</code> <p>Destination workfile path.</p> required <code>version</code> <code>Optional[int]</code> <p>Workfile version.</p> <code>None</code> <code>comment</code> <code>optional[str]</code> <p>Workfile comment.</p> <code>None</code> <code>description</code> <code>Optional[str]</code> <p>Workfile description.</p> <code>None</code> <code>prepared_data</code> <code>Optional[SaveWorkfileOptionalData]</code> <p>Prepared data for speed enhancements.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/workfile/utils.py</code> <pre><code>def save_workfile_with_current_context(\n    workfile_path: str,\n    *,\n    version: Optional[int] = None,\n    comment: Optional[str] = None,\n    description: Optional[str] = None,\n    prepared_data: Optional[SaveWorkfileOptionalData] = None,\n) -&gt; None:\n    \"\"\"Save current workfile to new location using current context.\n\n    Helper function to save workfile using current context. Calls\n        'save_current_workfile_to' at the end.\n\n    Args:\n        workfile_path (str): Destination workfile path.\n        version (Optional[int]): Workfile version.\n        comment (optional[str]): Workfile comment.\n        description (Optional[str]): Workfile description.\n        prepared_data (Optional[SaveWorkfileOptionalData]): Prepared data\n            for speed enhancements.\n\n    \"\"\"\n    from ayon_core.pipeline.context_tools import registered_host\n\n    host = registered_host()\n    context = host.get_current_context()\n    project_name = context[\"project_name\"]\n    folder_path = context[\"folder_path\"]\n    task_name = context[\"task_name\"]\n    folder_entity = task_entity = None\n    if folder_path:\n        folder_entity = ayon_api.get_folder_by_path(project_name, folder_path)\n        if folder_entity and task_name:\n            task_entity = ayon_api.get_task_by_name(\n                project_name, folder_entity[\"id\"], task_name\n            )\n\n    host.save_workfile_with_context(\n        workfile_path,\n        folder_entity,\n        task_entity,\n        version=version,\n        comment=comment,\n        description=description,\n        prepared_data=prepared_data,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/utils.html#client.ayon_core.pipeline.workfile.utils.should_open_workfiles_tool_on_launch","title":"<code>should_open_workfiles_tool_on_launch(project_name, host_name, task_name, task_type, default_output=False, project_settings=None)</code>","text":"<p>Define if host should start workfile tool at host launch.</p> <p>Default output is <code>False</code>. Can be overridden with environment variable <code>AYON_WORKFILE_TOOL_ON_START</code>, valid values without case sensitivity are <code>\"0\", \"1\", \"true\", \"false\", \"yes\", \"no\"</code>.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Name of project.</p> required <code>host_name</code> <code>str</code> <p>Name of host which is launched. In avalon's application context it's value stored in app definition under key <code>\"application_dir\"</code>. Is not case sensitive.</p> required <code>task_name</code> <code>str</code> <p>Name of task which is used for launching the host. Task name is not case sensitive.</p> required <code>task_type</code> <code>str</code> <p>Task type.</p> required <code>default_output</code> <code>Optional[bool]</code> <p>Default output value if no profile is found.</p> <code>False</code> <code>project_settings</code> <code>Optional[dict[str, Any]]</code> <p>Project settings.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>bool</code> <p>True if host should start workfile.</p> Source code in <code>client/ayon_core/pipeline/workfile/utils.py</code> <pre><code>def should_open_workfiles_tool_on_launch(\n    project_name,\n    host_name,\n    task_name,\n    task_type,\n    default_output=False,\n    project_settings=None,\n):\n    \"\"\"Define if host should start workfile tool at host launch.\n\n    Default output is `False`. Can be overridden with environment variable\n    `AYON_WORKFILE_TOOL_ON_START`, valid values without case sensitivity are\n    `\"0\", \"1\", \"true\", \"false\", \"yes\", \"no\"`.\n\n    Args:\n        project_name (str): Name of project.\n        host_name (str): Name of host which is launched. In avalon's\n            application context it's value stored in app definition under\n            key `\"application_dir\"`. Is not case sensitive.\n        task_name (str): Name of task which is used for launching the host.\n            Task name is not case sensitive.\n        task_type (str): Task type.\n        default_output (Optional[bool]): Default output value if no profile\n            is found.\n        project_settings (Optional[dict[str, Any]]): Project settings.\n\n    Returns:\n        bool: True if host should start workfile.\n\n    \"\"\"\n\n    if project_settings is None:\n        project_settings = get_project_settings(project_name)\n    profiles = (\n        project_settings\n        [\"core\"]\n        [\"tools\"]\n        [\"Workfiles\"]\n        [\"open_workfile_tool_on_startup\"]\n    )\n\n    if not profiles:\n        return default_output\n\n    filter_data = {\n        \"task_names\": task_name,\n        \"task_types\": task_type,\n        \"host_names\": host_name,\n    }\n    matching_item = filter_profiles(profiles, filter_data)\n\n    output = None\n    if matching_item:\n        output = matching_item.get(\"enabled\")\n\n    if output is None:\n        return default_output\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/utils.html#client.ayon_core.pipeline.workfile.utils.should_use_last_workfile_on_launch","title":"<code>should_use_last_workfile_on_launch(project_name, host_name, task_name, task_type, default_output=False, project_settings=None)</code>","text":"<p>Define if host should start last version workfile if possible.</p> <p>Default output is <code>False</code>. Can be overridden with environment variable <code>AYON_OPEN_LAST_WORKFILE</code>, valid values without case sensitivity are <code>\"0\", \"1\", \"true\", \"false\", \"yes\", \"no\"</code>.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Name of project.</p> required <code>host_name</code> <code>str</code> <p>Name of host which is launched. In avalon's application context it's value stored in app definition under key <code>\"application_dir\"</code>. Is not case sensitive.</p> required <code>task_name</code> <code>str</code> <p>Name of task which is used for launching the host. Task name is not case sensitive.</p> required <code>task_type</code> <code>str</code> <p>Task type.</p> required <code>default_output</code> <code>Optional[bool]</code> <p>Default output value if no profile is found.</p> <code>False</code> <code>project_settings</code> <code>Optional[dict[str, Any]]</code> <p>Project settings.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if host should start workfile.</p> Source code in <code>client/ayon_core/pipeline/workfile/utils.py</code> <pre><code>def should_use_last_workfile_on_launch(\n    project_name: str,\n    host_name: str,\n    task_name: str,\n    task_type: str,\n    default_output: bool = False,\n    project_settings: Optional[dict[str, Any]] = None,\n) -&gt; bool:\n    \"\"\"Define if host should start last version workfile if possible.\n\n    Default output is `False`. Can be overridden with environment variable\n    `AYON_OPEN_LAST_WORKFILE`, valid values without case sensitivity are\n    `\"0\", \"1\", \"true\", \"false\", \"yes\", \"no\"`.\n\n    Args:\n        project_name (str): Name of project.\n        host_name (str): Name of host which is launched. In avalon's\n            application context it's value stored in app definition under\n            key `\"application_dir\"`. Is not case sensitive.\n        task_name (str): Name of task which is used for launching the host.\n            Task name is not case sensitive.\n        task_type (str): Task type.\n        default_output (Optional[bool]): Default output value if no profile\n            is found.\n        project_settings (Optional[dict[str, Any]]): Project settings.\n\n    Returns:\n        bool: True if host should start workfile.\n\n    \"\"\"\n    if project_settings is None:\n        project_settings = get_project_settings(project_name)\n    profiles = (\n        project_settings\n        [\"core\"]\n        [\"tools\"]\n        [\"Workfiles\"]\n        [\"last_workfile_on_startup\"]\n    )\n\n    if not profiles:\n        return default_output\n\n    filter_data = {\n        \"task_names\": task_name,\n        \"task_types\": task_type,\n        \"host_names\": host_name,\n    }\n    matching_item = filter_profiles(profiles, filter_data)\n\n    output = None\n    if matching_item:\n        output = matching_item.get(\"enabled\")\n\n    if output is None:\n        return default_output\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html","title":"workfile_template_builder","text":"<p>Workfile build mechanism using workfile templates.</p> <p>Build templates are manually prepared using plugin definitions which create placeholders inside the template which are populated on import.</p> <p>This approach is very explicit to achieve very specific build logic that can be targeted by task types and names.</p> <p>Placeholders are created using placeholder plugins which should care about logic and data of placeholder items. 'PlaceholderItem' is used to keep track about its progress.</p>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.AbstractTemplateBuilder","title":"<code>AbstractTemplateBuilder</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Abstraction of Template Builder.</p> <p>Builder cares about context, shared data, cache, discovery of plugins and trigger logic. Provides public api for host workfile build system.</p> <p>Rest of logic is based on plugins that care about collection and creation of placeholder items.</p> <p>Population of placeholders happens in loops. Each loop will collect all available placeholders, skip already populated, and populate the rest.</p> <p>Builder item has 2 types of shared data. Refresh lifetime which are cleared on refresh and populate lifetime which are cleared after loop of placeholder population.</p> <p>Parameters:</p> Name Type Description Default <code>host</code> <code>Union[AbstractHost, ModuleType]</code> <p>Implementation of host.</p> required Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>class AbstractTemplateBuilder(ABC):\n    \"\"\"Abstraction of Template Builder.\n\n    Builder cares about context, shared data, cache, discovery of plugins\n    and trigger logic. Provides public api for host workfile build system.\n\n    Rest of logic is based on plugins that care about collection and creation\n    of placeholder items.\n\n    Population of placeholders happens in loops. Each loop will collect all\n    available placeholders, skip already populated, and populate the rest.\n\n    Builder item has 2 types of shared data. Refresh lifetime which are cleared\n    on refresh and populate lifetime which are cleared after loop of\n    placeholder population.\n\n    Args:\n        host (Union[AbstractHost, ModuleType]): Implementation of host.\n    \"\"\"\n\n    _log = None\n\n    def __init__(self, host):\n        # Get host name\n        if isinstance(host, AbstractHost):\n            host_name = host.name\n        else:\n            host_name = os.environ.get(\"AYON_HOST_NAME\")\n\n        self._host = host\n        self._host_name = host_name\n\n        # Shared data across placeholder plugins\n        self._shared_data = {}\n        self._shared_populate_data = {}\n\n        # Where created objects of placeholder plugins will be stored\n        self._placeholder_plugins = None\n        self._loaders_by_name = None\n        self._creators_by_name = None\n        self._create_context = None\n\n        self._project_settings = None\n\n        self._current_folder_entity = _NOT_SET\n        self._current_task_entity = _NOT_SET\n        self._linked_folder_entities = _NOT_SET\n\n        self._event_system = EventSystem()\n\n    @property\n    def project_name(self):\n        if isinstance(self._host, AbstractHost):\n            return self._host.get_current_project_name()\n        return os.getenv(\"AYON_PROJECT_NAME\")\n\n    @property\n    def current_folder_path(self):\n        if isinstance(self._host, AbstractHost):\n            return self._host.get_current_folder_path()\n        return os.getenv(\"AYON_FOLDER_PATH\")\n\n    @property\n    def current_task_name(self):\n        if isinstance(self._host, AbstractHost):\n            return self._host.get_current_task_name()\n        return os.getenv(\"AYON_TASK_NAME\")\n\n    def get_current_context(self):\n        if isinstance(self._host, AbstractHost):\n            return self._host.get_current_context()\n        return {\n            \"project_name\": self.project_name,\n            \"folder_path\": self.current_folder_path,\n            \"task_name\": self.current_task_name\n        }\n\n    @property\n    def project_settings(self):\n        if self._project_settings is None:\n            self._project_settings = get_project_settings(self.project_name)\n        return self._project_settings\n\n    @property\n    def current_folder_entity(self):\n        if self._current_folder_entity is _NOT_SET:\n            self._current_folder_entity = get_folder_by_path(\n                self.project_name, self.current_folder_path\n            )\n        return self._current_folder_entity\n\n    @property\n    def current_task_entity(self):\n        if self._current_task_entity is _NOT_SET:\n            task_entity = None\n            folder_entity = self.current_folder_entity\n            if folder_entity:\n                task_entity = get_task_by_name(\n                    self.project_name,\n                    folder_entity[\"id\"],\n                    self.current_task_name\n                )\n            self._current_task_entity = task_entity\n        return self._current_task_entity\n\n    @property\n    def current_task_type(self):\n        task_entity = self.current_task_entity\n        if task_entity:\n            return task_entity[\"taskType\"]\n        return None\n\n    @property\n    def create_context(self):\n        if self._create_context is None:\n            self._create_context = CreateContext(\n                self.host,\n                discover_publish_plugins=False,\n                headless=True\n            )\n        return self._create_context\n\n    def get_placeholder_plugin_classes(self):\n        \"\"\"Get placeholder plugin classes that can be used to build template.\n\n        Default implementation looks for method\n            'get_workfile_build_placeholder_plugins' on host.\n\n        Returns:\n            List[PlaceholderPlugin]: Plugin classes available for host.\n        \"\"\"\n        plugins = []\n\n        # Backwards compatibility\n        if hasattr(self._host, \"get_workfile_build_placeholder_plugins\"):\n            return self._host.get_workfile_build_placeholder_plugins()\n\n        plugins.extend(discover(PlaceholderPlugin))\n        return plugins\n\n    @property\n    def host(self):\n        \"\"\"Access to host implementation.\n\n        Returns:\n            Union[AbstractHost, ModuleType]: Implementation of host.\n        \"\"\"\n\n        return self._host\n\n    @property\n    def host_name(self):\n        \"\"\"Name of 'host' implementation.\n\n        Returns:\n            str: Host's name.\n        \"\"\"\n\n        return self._host_name\n\n    @property\n    def log(self):\n        \"\"\"Dynamically created logger for the plugin.\"\"\"\n\n        if self._log is None:\n            self._log = Logger.get_logger(repr(self))\n        return self._log\n\n    def refresh(self):\n        \"\"\"Reset cached data.\"\"\"\n\n        self._placeholder_plugins = None\n        self._loaders_by_name = None\n        self._creators_by_name = None\n\n        self._current_folder_entity = _NOT_SET\n        self._current_task_entity = _NOT_SET\n        self._linked_folder_entities = _NOT_SET\n\n        self._project_settings = None\n\n        self._event_system = EventSystem()\n\n        self.clear_shared_data()\n        self.clear_shared_populate_data()\n\n    def get_loaders_by_name(self):\n        if self._loaders_by_name is None:\n            self._loaders_by_name = get_loaders_by_name()\n        return self._loaders_by_name\n\n    def get_linked_folder_entities(\n        self,\n        link_type: Optional[str],\n        folder_path_regex: Optional[str],\n    ):\n        if not link_type:\n            return []\n        project_name = self.project_name\n        folder_entity = self.current_folder_entity\n        if not folder_entity:\n            return []\n        links = get_folder_links(\n            project_name,\n            folder_entity[\"id\"], link_types=[link_type], link_direction=\"in\"\n        )\n        linked_folder_ids = {\n            link[\"entityId\"]\n            for link in links\n            if link[\"entityType\"] == \"folder\"\n        }\n\n        return list(get_folders(\n            project_name,\n            folder_path_regex=folder_path_regex,\n            folder_ids=linked_folder_ids,\n        ))\n\n    def _collect_creators(self):\n        self._creators_by_name = {\n            identifier: creator\n            for identifier, creator\n            in self.create_context.manual_creators.items()\n            # Do not list HiddenCreator even though it is a 'manual creator'\n            if not isinstance(creator, HiddenCreator)\n        }\n\n    def get_creators_by_name(self):\n        if self._creators_by_name is None:\n            self._collect_creators()\n\n        return self._creators_by_name\n\n    def get_shared_data(self, key):\n        \"\"\"Receive shared data across plugins and placeholders.\n\n        This can be used to scroll scene only once to look for placeholder\n        items if the storing is unified but each placeholder plugin would have\n        to call it again.\n\n        Args:\n            key (str): Key under which are shared data stored.\n\n        Returns:\n            Union[None, Any]: None if key was not set.\n        \"\"\"\n\n        return self._shared_data.get(key)\n\n    def set_shared_data(self, key, value):\n        \"\"\"Store share data across plugins and placeholders.\n\n        Store data that can be afterwards accessed from any future call. It\n        is good practice to check if the same value is not already stored under\n        different key or if the key is not already used for something else.\n\n        Key should be self-explanatory to content.\n        - wrong: 'folder'\n        - good: 'folder_name'\n\n        Args:\n            key (str): Key under which is key stored.\n            value (Any): Value that should be stored under the key.\n        \"\"\"\n\n        self._shared_data[key] = value\n\n    def clear_shared_data(self):\n        \"\"\"Clear shared data.\n\n        Method only clear shared data to default state.\n        \"\"\"\n\n        self._shared_data = {}\n\n    def clear_shared_populate_data(self):\n        \"\"\"Receive shared data across plugins and placeholders.\n\n        These data are cleared after each loop of populating of template.\n\n        This can be used to scroll scene only once to look for placeholder\n        items if the storing is unified but each placeholder plugin would have\n        to call it again.\n\n        Args:\n            key (str): Key under which are shared data stored.\n\n        Returns:\n            Union[None, Any]: None if key was not set.\n        \"\"\"\n\n        self._shared_populate_data = {}\n\n    def get_shared_populate_data(self, key):\n        \"\"\"Store share populate data across plugins and placeholders.\n\n        These data are cleared after each loop of populating of template.\n\n        Store data that can be afterwards accessed from any future call. It\n        is good practice to check if the same value is not already stored under\n        different key or if the key is not already used for something else.\n\n        Key should be self-explanatory to content.\n        - wrong: 'folder'\n        - good: 'folder_path'\n\n        Args:\n            key (str): Key under which is key stored.\n            value (Any): Value that should be stored under the key.\n        \"\"\"\n\n        return self._shared_populate_data.get(key)\n\n    def set_shared_populate_data(self, key, value):\n        \"\"\"Store share populate data across plugins and placeholders.\n\n        These data are cleared after each loop of populating of template.\n\n        Store data that can be afterwards accessed from any future call. It\n        is good practice to check if the same value is not already stored under\n        different key or if the key is not already used for something else.\n\n        Key should be self-explanatory to content.\n        - wrong: 'folder'\n        - good: 'folder_path'\n\n        Args:\n            key (str): Key under which is key stored.\n            value (Any): Value that should be stored under the key.\n        \"\"\"\n\n        self._shared_populate_data[key] = value\n\n    @property\n    def placeholder_plugins(self):\n        \"\"\"Access to initialized placeholder plugins.\n\n        Returns:\n            List[PlaceholderPlugin]: Initialized plugins available for host.\n        \"\"\"\n\n        if self._placeholder_plugins is None:\n            placeholder_plugins = {}\n            for cls in self.get_placeholder_plugin_classes():\n                try:\n                    plugin = cls(self)\n                    placeholder_plugins[plugin.identifier] = plugin\n\n                except Exception:\n                    self.log.warning(\n                        \"Failed to initialize placeholder plugin {}\".format(\n                            cls.__name__\n                        ),\n                        exc_info=True\n                    )\n\n            self._placeholder_plugins = placeholder_plugins\n        return self._placeholder_plugins\n\n    def create_placeholder(self, plugin_identifier, placeholder_data):\n        \"\"\"Create new placeholder using plugin identifier and data.\n\n        Args:\n            plugin_identifier (str): Identifier of plugin. That's how builder\n                know which plugin should be used.\n            placeholder_data (Dict[str, Any]): Placeholder item data. They\n                should match options required by the plugin.\n\n        Returns:\n            PlaceholderItem: Created placeholder item.\n        \"\"\"\n\n        plugin = self.placeholder_plugins[plugin_identifier]\n        return plugin.create_placeholder(placeholder_data)\n\n    def get_placeholders(self):\n        \"\"\"Collect placeholder items from scene.\n\n        Each placeholder plugin can collect it's placeholders and return them.\n        This method does not use cached values but always go through the scene.\n\n        Returns:\n            List[PlaceholderItem]: Sorted placeholder items.\n        \"\"\"\n\n        placeholders = []\n        for placeholder_plugin in self.placeholder_plugins.values():\n            result = placeholder_plugin.collect_placeholders()\n            if result:\n                placeholders.extend(result)\n\n        return list(sorted(\n            placeholders,\n            key=lambda placeholder: placeholder.order\n        ))\n\n    def _backwards_compatibility_build_template(func):\n        \"\"\"Decorator for backwards compatibility of build_template method.\n\n        This decorator keeps legacy build behavior as default while allowing\n        explicit use of a TemplatePreset for the new behavior.\n\n        If a TemplatePreset is explicitly passed (or provided via\n        ``preset=``), the wrapped method (new implementation) is executed.\n        Otherwise, execution is delegated to ``old_build_template``.\n\n        It supports either a\n        TemplatePreset object or the old parameters (template_path,\n        level_limit,keep_placeholders, create_first_version,\n        workfile_creation_enabled).\n        It checks the type of the first argument and calls the appropriate\n        implementation based on that.\n\n        The function is also marked with an attribute 'has_preset' set to True,\n            which can be used to check if the function is using the new\n            signature or is using the old signature. That should allow addons\n            to adapt to new signature.\n            &gt;&gt;&gt; if getattr(build_template, \"has_preset\", True):\n            &gt;&gt;&gt;     # New signature is used\n            &gt;&gt;&gt;     build_template(preset)\n            &gt;&gt;&gt; else:\n            &gt;&gt;&gt;     # Old signature is used\n            &gt;&gt;&gt;     build_template(template_path, level_limit, ...)\n\n        Args:\n            func (Callable): The original build_template method.\n\n    \"\"\"\n        @wraps(func)\n        def inner(self, *args, **kwargs):\n            if \"preset\" in kwargs:\n                preset = kwargs[\"preset\"]\n                return func(self, preset=preset)\n\n            if (\n                len(args) == 1\n                and isinstance(args[0], TemplatePreset)\n            ):\n                return func(self, args[0])\n\n            # Legacy API fallback.\n            return self.old_build_template(*args, **kwargs)\n\n        setattr(inner, \"has_preset\", True)\n        return inner\n\n    @overload\n    def build_template(self, preset: TemplatePreset | None) -&gt; None:\n        \"\"\"Build the workfile template from a preset.\n\n        Args:\n            preset (TemplatePreset | None): Template preset to use. To use\n                default implementation use 'preset=None'.\n\n        \"\"\"\n        pass\n\n    @overload\n    def build_template(\n        self,\n        template_path=None,\n        level_limit=None,\n        keep_placeholders=None,\n        create_first_version=None,\n        workfile_creation_enabled=False\n    ):\n        \"\"\"Main callback for building workfile from template path.\n\n        Todo:\n            Handle report of populated placeholders from\n                'populate_scene_placeholders' to be shown to a user.\n\n        Args:\n            template_path (str): Path to a template file with placeholders.\n                Template from settings 'get_template_preset' used when not\n                passed.\n            level_limit (int): Limit of populate loops. Related to\n                'populate_scene_placeholders' method.\n            keep_placeholders (bool): Add flag to placeholder data for\n                hosts to decide if they want to remove\n                placeholder after it is used.\n            create_first_version (bool): Create first version of a workfile.\n                 When set to True, this option initiates the saving of the\n                 workfile for an initial version. It will skip saving if\n                 a version already exists.\n            workfile_creation_enabled (bool): Whether the call is part of\n                creating a new workfile.\n                When True, we only build if the current file is not\n                an existing saved workfile but a \"new\" file. Basically when\n                enabled we assume the user tries to load it only into a\n                \"New File\" (unsaved empty workfile).\n                When False, the default value, we assume we explicitly want to\n                build the template in our current scene regardless of current\n                scene state.\n\n        \"\"\"\n        pass\n\n    @_backwards_compatibility_build_template\n    def build_template(\n        self,\n        preset: TemplatePreset | None,\n    ) -&gt; None:\n        \"\"\"Build the workfile template from a preset.\n\n        Args:\n            preset (TemplatePreset | None): Template preset to use. To use\n                default implementation use 'preset=None'.\n\n        \"\"\"\n        if preset is None:\n            preset = self.get_template_preset()\n\n        if not preset.has_valid_path():\n            raise TemplateLoadFailed(\n                f\"Template path '{preset.path}' does not exist.\"\n            )\n\n        self.log.info(f\"Building the workfile template: {preset.path}\")\n        self.import_template(preset.path)\n        self.populate_scene_placeholders(\n            keep_placeholders=preset.keep_placeholder\n        )\n\n    def old_build_template(\n        self,\n        template_path=None,\n        level_limit=None,\n        keep_placeholders=None,\n        create_first_version=None,\n        workfile_creation_enabled=False\n    ):\n        \"\"\"Legacy callback for building workfile from template path.\n        Deprecated, use 'build_template' with 'preset' argument instead.\n\n        Todo:\n            Handle report of populated placeholders from\n                'populate_scene_placeholders' to be shown to a user.\n\n        Args:\n            template_path (str): Path to a template file with placeholders.\n                Template from settings 'get_template_preset' used when not\n                passed.\n            level_limit (int): Limit of populate loops. Related to\n                'populate_scene_placeholders' method.\n            keep_placeholders (bool): Add flag to placeholder data for\n                hosts to decide if they want to remove\n                placeholder after it is used.\n            create_first_version (bool): Create first version of a workfile.\n                 When set to True, this option initiates the saving of the\n                 workfile for an initial version. It will skip saving if\n                 a version already exists.\n            workfile_creation_enabled (bool): Whether the call is part of\n                creating a new workfile.\n                When True, we only build if the current file is not\n                an existing saved workfile but a \"new\" file. Basically when\n                enabled we assume the user tries to load it only into a\n                \"New File\" (unsaved empty workfile).\n                When False, the default value, we assume we explicitly want to\n                build the template in our current scene regardless of current\n                scene state.\n\n        \"\"\"\n        # More accurate variable name\n        # - logic related to workfile creation should be moved out in future\n        explicit_build_requested = not workfile_creation_enabled\n\n        # Get default values if not provided\n        if (\n            template_path is None\n            or keep_placeholders is None\n            or create_first_version is None\n        ):\n            preset = self.get_template_preset()\n            if not preset.has_valid_path():\n                raise TemplateLoadFailed(\n                    f\"Template path '{preset.path}' does not exist.\"\n                )\n            template_path: str = preset.path\n            if keep_placeholders is None:\n                keep_placeholders: bool = preset.keep_placeholder\n            if create_first_version is None:\n                create_first_version: bool = preset.create_first_version\n\n        # Build the template if we are explicitly requesting it or if it's\n        # an unsaved \"new file\".\n        is_new_file = not self.host.get_current_workfile()\n        if is_new_file or explicit_build_requested:\n            self.log.info(f\"Building the workfile template: {template_path}\")\n            self.import_template(template_path)\n            self.populate_scene_placeholders(\n                level_limit, keep_placeholders)\n\n        # Do not consider saving a first workfile version, if this is not set\n        # to be a \"workfile creation\" or `create_first_version` is disabled.\n        if explicit_build_requested or not create_first_version:\n            return\n\n        # If there is no existing workfile, save the first version\n        workfile_path = self.get_workfile_path()\n        if not os.path.exists(workfile_path):\n            self.log.info(\"Saving first workfile: %s\", workfile_path)\n            save_next_version()\n        else:\n            self.log.info(\n                \"A workfile already exists. Skipping save of workfile as \"\n                \"initial version.\")\n\n    def rebuild_template(self):\n        \"\"\"Go through existing placeholders in scene and update them.\n\n        This could not make sense for all plugin types so this is optional\n        logic for plugins.\n\n        Note:\n            Logic is not importing the template again but using placeholders\n                that were already available. We should maybe change the method\n                name.\n\n        Question:\n            Should this also handle subloops as it is possible that another\n                template is loaded during processing?\n        \"\"\"\n\n        if not self.placeholder_plugins:\n            self.log.info(\"There are no placeholder plugins available.\")\n            return\n\n        placeholders = self.get_placeholders()\n        if not placeholders:\n            self.log.info(\"No placeholders were found.\")\n            return\n\n        for placeholder in placeholders:\n            plugin = placeholder.plugin\n            plugin.repopulate_placeholder(placeholder)\n\n        self.clear_shared_populate_data()\n\n    def open_template(self):\n        \"\"\"Open template file with registered host.\"\"\"\n        template_preset = self.get_template_preset()\n        if not template_preset.has_valid_path():\n            raise TemplateLoadFailed(\n                f\"Template path '{template_preset.path}' does not exist.\"\n            )\n        self.host.open_workfile(template_preset.path)\n\n    @abstractmethod\n    def import_template(self, template_path):\n        \"\"\"\n        Import template in current host.\n\n        Should load the content of template into scene so\n        'populate_scene_placeholders' can be started.\n\n        Args:\n            template_path (str): Fullpath for current task and\n                host's template file.\n        \"\"\"\n\n        pass\n\n    def get_workfile_path(self):\n        \"\"\"Return last known workfile path or the first workfile path create.\n\n        Return:\n            str: Last workfile path, or first version to create if none exist.\n        \"\"\"\n        current_path = self.host.get_current_workfile()\n        if current_path:\n            return current_path\n        project_name = self.project_name\n        folder_entity = self.current_folder_entity\n        task_entity = self.current_task_entity\n\n        project_settings = self.project_settings\n        project_entity = ayon_api.get_project(project_name)\n        anatomy = Anatomy(project_name, project_entity=project_entity)\n\n        template_key = get_workfile_template_key(\n            project_name,\n            task_entity[\"taskType\"],\n            self.host_name,\n            project_settings=project_settings,\n        )\n        file_template = anatomy.get_template_item(\"work\", template_key, \"file\")\n        template_data = get_template_data(\n            project_entity,\n            folder_entity,\n            task_entity,\n            self.host_name,\n            settings=project_settings,\n        )\n        workdir = get_workdir(\n            project_entity,\n            folder_entity,\n            task_entity,\n            self.host_name,\n            anatomy=anatomy,\n            template_key=template_key,\n            project_settings=project_settings,\n        )\n        return get_last_workfile(\n            workdir,\n            file_template.template,\n            template_data,\n            set(self.host.get_workfile_extensions() or []),\n            full_path=True,\n        )\n\n    def _prepare_placeholders(self, placeholders):\n        \"\"\"Run preparation part for placeholders on plugins.\n\n        Args:\n            placeholders (List[PlaceholderItem]): Placeholder items that will\n                be processed.\n        \"\"\"\n\n        # Prepare placeholder items by plugin\n        plugins_by_identifier = {}\n        placeholders_by_plugin_id = collections.defaultdict(list)\n        for placeholder in placeholders:\n            plugin = placeholder.plugin\n            identifier = plugin.identifier\n            plugins_by_identifier[identifier] = plugin\n            placeholders_by_plugin_id[identifier].append(placeholder)\n\n        # Plugin should prepare data for passed placeholders\n        for identifier, placeholders in placeholders_by_plugin_id.items():\n            plugin = plugins_by_identifier[identifier]\n            plugin.prepare_placeholders(placeholders)\n\n    def populate_scene_placeholders(\n        self, level_limit=None, keep_placeholders=None\n    ):\n        \"\"\"Find placeholders in scene using plugins and process them.\n\n        This should happen after 'import_template'.\n\n        Collect available placeholders from scene. All of them are processed\n        after that shared data are cleared. Placeholder items are collected\n        again and if there are any new the loop happens again. This is possible\n        to change with defying 'level_limit'.\n\n        Placeholders are marked as processed so they're not re-processed. To\n        identify which placeholders were already processed is used\n        placeholder's 'scene_identifier'.\n\n        Args:\n            level_limit (int): Level of loops that can happen. Default is 1000.\n            keep_placeholders (bool): Add flag to placeholder data for\n                hosts to decide if they want to remove\n                placeholder after it is used.\n        \"\"\"\n\n        if not self.placeholder_plugins:\n            self.log.warning(\"There are no placeholder plugins available.\")\n            return\n\n        placeholders = self.get_placeholders()\n        if not placeholders:\n            self.log.warning(\"No placeholders were found.\")\n            return\n\n        # Avoid infinite loop\n        # - 1000 iterations of placeholders processing must be enough\n        if not level_limit:\n            level_limit = 1000\n\n        placeholder_by_scene_id = {\n            placeholder.scene_identifier: placeholder\n            for placeholder in placeholders\n        }\n        all_processed = len(placeholders) == 0\n        # Counter is checked at the end of a loop so the loop happens at least\n        #   once.\n        iter_counter = 0\n        while not all_processed:\n            filtered_placeholders = []\n            for placeholder in placeholders:\n                if placeholder.finished:\n                    continue\n\n                if placeholder.in_progress:\n                    self.log.warning((\n                        \"Placeholder that should be processed\"\n                        \" is already in progress.\"\n                    ))\n                    continue\n\n                # add flag for keeping placeholders in scene\n                # after they are processed\n                placeholder.data[\"keep_placeholder\"] = keep_placeholders\n\n                filtered_placeholders.append(placeholder)\n\n            self._prepare_placeholders(filtered_placeholders)\n\n            for placeholder in filtered_placeholders:\n                placeholder.set_in_progress()\n                placeholder_plugin = placeholder.plugin\n                try:\n                    placeholder_plugin.populate_placeholder(placeholder)\n\n                except Exception as exc:\n                    self.log.warning(\n                        (\n                            \"Failed to process placeholder {} with plugin {}\"\n                        ).format(\n                            placeholder.scene_identifier,\n                            placeholder_plugin.__class__.__name__\n                        ),\n                        exc_info=True\n                    )\n                    placeholder.set_failed(exc)\n\n                placeholder.set_finished()\n\n            # Trigger on_depth_processed event\n            self.emit_event(\n                topic=\"template.depth_processed\",\n                data={\n                    \"depth\": iter_counter,\n                    \"placeholders_by_scene_id\": placeholder_by_scene_id\n                },\n                source=\"builder\"\n            )\n\n            # Clear shared data before getting new placeholders\n            self.clear_shared_populate_data()\n\n            iter_counter += 1\n            if iter_counter &gt;= level_limit:\n                break\n\n            all_processed = True\n            collected_placeholders = self.get_placeholders()\n            for placeholder in collected_placeholders:\n                identifier = placeholder.scene_identifier\n                if identifier in placeholder_by_scene_id:\n                    continue\n\n                all_processed = False\n                placeholder_by_scene_id[identifier] = placeholder\n                placeholders.append(placeholder)\n\n        # Trigger on_finished event\n        self.emit_event(\n            topic=\"template.finished\",\n            data={\n                \"depth\": iter_counter,\n                \"placeholders_by_scene_id\": placeholder_by_scene_id,\n            },\n            source=\"builder\"\n        )\n\n        self.refresh()\n\n    def _get_build_profiles(self):\n        \"\"\"Get build profiles for workfile build template path.\n\n        Returns:\n            List[Dict[str, Any]]: Profiles for template path resolving.\n        \"\"\"\n\n        return (\n            self.project_settings\n            [self.host_name]\n            [\"templated_workfile_build\"]\n            [\"profiles\"]\n        )\n\n    def trigger_on_app_launch(\n        self, preset: TemplatePreset | None = None\n    ) -&gt; None:\n        \"\"\"\n        Trigger template build on application launch if the preset allows it.\n\n        Args:\n            preset (TemplatePreset | None): The template preset to use for\n                building the workfile template. If not provided, the default\n                template preset will be used.\n\n        \"\"\"\n        if preset is None:\n            preset = self.get_template_preset()\n\n        if not preset.execute_on_app_launch:\n            return\n\n        if not preset.has_valid_path():\n            return\n\n        self.build_template(preset=preset)\n        if preset.create_first_version:\n            workfile_path = self.get_workfile_path()\n            if not os.path.exists(workfile_path):\n                self.log.info(\"Saving first workfile: %s\", workfile_path)\n                save_next_version()\n\n    def trigger_on_new_file(\n        self, preset: TemplatePreset | None = None\n    ) -&gt; None:\n        \"\"\"\n        Trigger template build on new file creation if the preset allows it.\n\n        Args:\n            preset (TemplatePreset | None): The template preset to use for\n                building the workfile template. If not provided, the default\n                template preset will be used.\n\n        \"\"\"\n        if preset is None:\n            preset = self.get_template_preset()\n\n        if not preset.execute_on_new_file:\n            return\n\n        if not preset.has_valid_path():\n            return\n\n        self.build_template(preset=preset)\n        if preset.create_first_version:\n            workfile_path = self.get_workfile_path()\n            if not os.path.exists(workfile_path):\n                self.log.info(\"Saving first workfile: %s\", workfile_path)\n                save_next_version()\n\n    def create_first_workfile_version(\n        self, preset: TemplatePreset | None = None\n    ) -&gt; None:\n        \"\"\"Create first workfile version from template if the preset allows it.\n\n        Args:\n            preset (TemplatePreset | None): The template preset to use for\n                building the workfile template. If not provided, the default\n                template preset will be used.\n\n        \"\"\"\n        if preset is None:\n            preset = self.get_template_preset()\n        if not preset.has_valid_path():\n            return\n\n        self.build_template(preset=preset)\n        if preset.create_first_version:\n            workfile_path = self.get_workfile_path()\n            if not os.path.exists(workfile_path):\n                self.log.info(\"Saving first workfile: %s\", workfile_path)\n                save_next_version()\n\n    def get_template_preset(self) -&gt; TemplatePreset:\n        \"\"\"Unified way how template preset is received using settings.\n\n        Method is dependent on '_get_build_profiles' which should return filter\n        profiles to resolve path to a template. Default implementation looks\n        into host settings:\n        - 'project_settings/{host name}/templated_workfile_build/profiles'\n\n        Returns:\n            TemplatePreset: Template preset object with:\n                - `path`: Resolved template filesystem path.\n                - `keep_placeholder`: Whether placeholders should remain\n                    after processing.\n                - `create_first_version`: Whether first workfile version\n                    should be created in workfile-creation flow.\n                - `execute_on_app_launch`: Whether template should be executed\n                    on application launch.\n                - `execute_on_new_file`: Whether template should be executed\n                    on new file creation.\n                - `profile`: Defensive copy of the matched profile from\n                    project settings.\n\n                Note:\n                    `profile[\"path\"]` contains the unresolved value from\n                    settings, while `path` contains the resolved filesystem\n                    path.\n\n        Raises:\n            TemplateProfileNotFound: When profiles are not filled.\n            TemplateLoadFailed: Profile was found but path is not set.\n            TemplateNotFound: Path was set but file does not exist.\n        \"\"\"\n        task_name = self.current_task_name\n        task_type = self.current_task_type\n        folder_path = self.current_folder_path\n        folder_type = None\n        folder_entity = self.current_folder_entity\n        if folder_entity:\n            folder_type = folder_entity[\"folderType\"]\n\n        filter_data = {\n            \"task_types\": task_type,\n            \"task_names\": task_name,\n            \"folder_types\": folder_type,\n            \"folder_paths\": folder_path,\n        }\n\n        build_profiles = self._get_build_profiles()\n        profile = filter_profiles(\n            build_profiles,\n            filter_data,\n            logger=self.log\n        )\n        if not profile:\n            return TemplatePreset()\n\n        path = profile[\"path\"]\n        # resolve path from ayon entity url\n        resolved_path = self.resolve_template_path(path)\n\n        # switch to remove placeholders after they are used\n        keep_placeholder = profile.get(\"keep_placeholder\")\n        create_first_version = profile.get(\"create_first_version\")\n\n        # backward compatibility, since default is True\n        if keep_placeholder is None:\n            keep_placeholder = True\n        if create_first_version is None:\n            create_first_version = True\n\n        return TemplatePreset(\n            path=resolved_path,\n            keep_placeholder=keep_placeholder,\n            create_first_version=create_first_version,\n            execute_on_app_launch=profile.get(\"execute_on_app_launch\", False),\n            execute_on_new_file=profile.get(\"execute_on_new_file\", False),\n            profile=copy.deepcopy(profile)\n        )\n\n    def resolve_template_path(self, path, fill_data=None) -&gt; str:\n        \"\"\"Resolve the template path.\n\n        By default, this:\n          - Resolves AYON entity URI to a filesystem path\n          - Returns path directly if it exists on disk.\n          - Resolves template keys through anatomy and environment variables.\n\n        This can be overridden in host integrations to perform additional\n        resolving over the template. Like, `hou.text.expandString` in Houdini.\n        It's recommended to still call the super().resolve_template_path()\n        to ensure the basic resolving is done across all integrations.\n\n        Arguments:\n            path (str): The input path.\n            fill_data (dict[str, str]): Deprecated. This is computed inside\n                the method using the current environment and project settings.\n                Used to be the data to use for template formatting.\n\n        Returns:\n            str: The resolved path.\n\n        \"\"\"\n\n        # If the path is an AYON entity URI, then resolve the filepath\n        # through the backend\n        if path.startswith(\"ayon+entity://\") or path.startswith(\"ayon://\"):\n            # This is a special case where the path is an AYON entity URI\n            # We need to resolve it to a filesystem path\n            resolved_path = resolve_entity_uri(path)\n            return resolved_path\n\n        # If the path is set and it's found on disk, return it directly\n        if path and os.path.exists(path):\n            return path\n\n        # We may have path for another platform, like C:/path/to/file\n        # or a path with template keys, like {project[code]} or both.\n        # Try to fill path with environments and anatomy roots\n        project_name = self.project_name\n        anatomy = Anatomy(project_name)\n\n        # Simple check whether the path contains any template keys\n        if \"{\" in path:\n            fill_data = {\n                key: value\n                for key, value in os.environ.items()\n            }\n            fill_data[\"root\"] = anatomy.roots\n            fill_data[\"project\"] = {\n                \"name\": project_name,\n                \"code\": anatomy.project_code,\n            }\n\n            # Format the template using local fill data\n            result = StringTemplate.format_template(path, fill_data)\n            if not result.solved:\n                return path\n\n            path = result.normalized()\n            if os.path.exists(path):\n                return path\n\n        # If the path were set in settings using a Windows path and we\n        # are now on a Linux system, we try to convert the solved path to\n        # the current platform.\n        while True:\n            try:\n                solved_path = anatomy.path_remapper(path)\n            except KeyError as missing_key:\n                raise KeyError(\n                    f\"Could not solve key '{missing_key}'\"\n                    f\" in template path '{path}'\"\n                )\n\n            if solved_path is None:\n                solved_path = path\n            if solved_path == path:\n                break\n            path = solved_path\n\n        solved_path = os.path.normpath(solved_path)\n        return solved_path\n\n    def emit_event(self, topic, data=None, source=None) -&gt; Event:\n        return self._event_system.emit(topic, data, source)\n\n    def add_event_callback(self, topic, callback, order=None):\n        return self._event_system.add_callback(topic, callback, order=order)\n\n    def add_on_finished_callback(\n        self, callback, order=None\n    ) -&gt; EventCallback:\n        return self.add_event_callback(\n            topic=\"template.finished\",\n            callback=callback,\n            order=order\n        )\n\n    def add_on_depth_processed_callback(\n        self, callback, order=None\n    ) -&gt; EventCallback:\n        return self.add_event_callback(\n            topic=\"template.depth_processed\",\n            callback=callback,\n            order=order\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.AbstractTemplateBuilder.host","title":"<code>host</code>  <code>property</code>","text":"<p>Access to host implementation.</p> <p>Returns:</p> Type Description <p>Union[AbstractHost, ModuleType]: Implementation of host.</p>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.AbstractTemplateBuilder.host_name","title":"<code>host_name</code>  <code>property</code>","text":"<p>Name of 'host' implementation.</p> <p>Returns:</p> Name Type Description <code>str</code> <p>Host's name.</p>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.AbstractTemplateBuilder.log","title":"<code>log</code>  <code>property</code>","text":"<p>Dynamically created logger for the plugin.</p>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.AbstractTemplateBuilder.placeholder_plugins","title":"<code>placeholder_plugins</code>  <code>property</code>","text":"<p>Access to initialized placeholder plugins.</p> <p>Returns:</p> Type Description <p>List[PlaceholderPlugin]: Initialized plugins available for host.</p>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.AbstractTemplateBuilder.build_template","title":"<code>build_template(preset)</code>","text":"<pre><code>build_template(preset: TemplatePreset | None) -&gt; None\n</code></pre><pre><code>build_template(template_path=None, level_limit=None, keep_placeholders=None, create_first_version=None, workfile_creation_enabled=False)\n</code></pre> <p>Build the workfile template from a preset.</p> <p>Parameters:</p> Name Type Description Default <code>preset</code> <code>TemplatePreset | None</code> <p>Template preset to use. To use default implementation use 'preset=None'.</p> required Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>@_backwards_compatibility_build_template\ndef build_template(\n    self,\n    preset: TemplatePreset | None,\n) -&gt; None:\n    \"\"\"Build the workfile template from a preset.\n\n    Args:\n        preset (TemplatePreset | None): Template preset to use. To use\n            default implementation use 'preset=None'.\n\n    \"\"\"\n    if preset is None:\n        preset = self.get_template_preset()\n\n    if not preset.has_valid_path():\n        raise TemplateLoadFailed(\n            f\"Template path '{preset.path}' does not exist.\"\n        )\n\n    self.log.info(f\"Building the workfile template: {preset.path}\")\n    self.import_template(preset.path)\n    self.populate_scene_placeholders(\n        keep_placeholders=preset.keep_placeholder\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.AbstractTemplateBuilder.clear_shared_data","title":"<code>clear_shared_data()</code>","text":"<p>Clear shared data.</p> <p>Method only clear shared data to default state.</p> Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>def clear_shared_data(self):\n    \"\"\"Clear shared data.\n\n    Method only clear shared data to default state.\n    \"\"\"\n\n    self._shared_data = {}\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.AbstractTemplateBuilder.clear_shared_populate_data","title":"<code>clear_shared_populate_data()</code>","text":"<p>Receive shared data across plugins and placeholders.</p> <p>These data are cleared after each loop of populating of template.</p> <p>This can be used to scroll scene only once to look for placeholder items if the storing is unified but each placeholder plugin would have to call it again.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>Key under which are shared data stored.</p> required <p>Returns:</p> Type Description <p>Union[None, Any]: None if key was not set.</p> Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>def clear_shared_populate_data(self):\n    \"\"\"Receive shared data across plugins and placeholders.\n\n    These data are cleared after each loop of populating of template.\n\n    This can be used to scroll scene only once to look for placeholder\n    items if the storing is unified but each placeholder plugin would have\n    to call it again.\n\n    Args:\n        key (str): Key under which are shared data stored.\n\n    Returns:\n        Union[None, Any]: None if key was not set.\n    \"\"\"\n\n    self._shared_populate_data = {}\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.AbstractTemplateBuilder.create_first_workfile_version","title":"<code>create_first_workfile_version(preset=None)</code>","text":"<p>Create first workfile version from template if the preset allows it.</p> <p>Parameters:</p> Name Type Description Default <code>preset</code> <code>TemplatePreset | None</code> <p>The template preset to use for building the workfile template. If not provided, the default template preset will be used.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>def create_first_workfile_version(\n    self, preset: TemplatePreset | None = None\n) -&gt; None:\n    \"\"\"Create first workfile version from template if the preset allows it.\n\n    Args:\n        preset (TemplatePreset | None): The template preset to use for\n            building the workfile template. If not provided, the default\n            template preset will be used.\n\n    \"\"\"\n    if preset is None:\n        preset = self.get_template_preset()\n    if not preset.has_valid_path():\n        return\n\n    self.build_template(preset=preset)\n    if preset.create_first_version:\n        workfile_path = self.get_workfile_path()\n        if not os.path.exists(workfile_path):\n            self.log.info(\"Saving first workfile: %s\", workfile_path)\n            save_next_version()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.AbstractTemplateBuilder.create_placeholder","title":"<code>create_placeholder(plugin_identifier, placeholder_data)</code>","text":"<p>Create new placeholder using plugin identifier and data.</p> <p>Parameters:</p> Name Type Description Default <code>plugin_identifier</code> <code>str</code> <p>Identifier of plugin. That's how builder know which plugin should be used.</p> required <code>placeholder_data</code> <code>Dict[str, Any]</code> <p>Placeholder item data. They should match options required by the plugin.</p> required <p>Returns:</p> Name Type Description <code>PlaceholderItem</code> <p>Created placeholder item.</p> Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>def create_placeholder(self, plugin_identifier, placeholder_data):\n    \"\"\"Create new placeholder using plugin identifier and data.\n\n    Args:\n        plugin_identifier (str): Identifier of plugin. That's how builder\n            know which plugin should be used.\n        placeholder_data (Dict[str, Any]): Placeholder item data. They\n            should match options required by the plugin.\n\n    Returns:\n        PlaceholderItem: Created placeholder item.\n    \"\"\"\n\n    plugin = self.placeholder_plugins[plugin_identifier]\n    return plugin.create_placeholder(placeholder_data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.AbstractTemplateBuilder.get_placeholder_plugin_classes","title":"<code>get_placeholder_plugin_classes()</code>","text":"<p>Get placeholder plugin classes that can be used to build template.</p> <p>Default implementation looks for method     'get_workfile_build_placeholder_plugins' on host.</p> <p>Returns:</p> Type Description <p>List[PlaceholderPlugin]: Plugin classes available for host.</p> Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>def get_placeholder_plugin_classes(self):\n    \"\"\"Get placeholder plugin classes that can be used to build template.\n\n    Default implementation looks for method\n        'get_workfile_build_placeholder_plugins' on host.\n\n    Returns:\n        List[PlaceholderPlugin]: Plugin classes available for host.\n    \"\"\"\n    plugins = []\n\n    # Backwards compatibility\n    if hasattr(self._host, \"get_workfile_build_placeholder_plugins\"):\n        return self._host.get_workfile_build_placeholder_plugins()\n\n    plugins.extend(discover(PlaceholderPlugin))\n    return plugins\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.AbstractTemplateBuilder.get_placeholders","title":"<code>get_placeholders()</code>","text":"<p>Collect placeholder items from scene.</p> <p>Each placeholder plugin can collect it's placeholders and return them. This method does not use cached values but always go through the scene.</p> <p>Returns:</p> Type Description <p>List[PlaceholderItem]: Sorted placeholder items.</p> Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>def get_placeholders(self):\n    \"\"\"Collect placeholder items from scene.\n\n    Each placeholder plugin can collect it's placeholders and return them.\n    This method does not use cached values but always go through the scene.\n\n    Returns:\n        List[PlaceholderItem]: Sorted placeholder items.\n    \"\"\"\n\n    placeholders = []\n    for placeholder_plugin in self.placeholder_plugins.values():\n        result = placeholder_plugin.collect_placeholders()\n        if result:\n            placeholders.extend(result)\n\n    return list(sorted(\n        placeholders,\n        key=lambda placeholder: placeholder.order\n    ))\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.AbstractTemplateBuilder.get_shared_data","title":"<code>get_shared_data(key)</code>","text":"<p>Receive shared data across plugins and placeholders.</p> <p>This can be used to scroll scene only once to look for placeholder items if the storing is unified but each placeholder plugin would have to call it again.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>Key under which are shared data stored.</p> required <p>Returns:</p> Type Description <p>Union[None, Any]: None if key was not set.</p> Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>def get_shared_data(self, key):\n    \"\"\"Receive shared data across plugins and placeholders.\n\n    This can be used to scroll scene only once to look for placeholder\n    items if the storing is unified but each placeholder plugin would have\n    to call it again.\n\n    Args:\n        key (str): Key under which are shared data stored.\n\n    Returns:\n        Union[None, Any]: None if key was not set.\n    \"\"\"\n\n    return self._shared_data.get(key)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.AbstractTemplateBuilder.get_shared_populate_data","title":"<code>get_shared_populate_data(key)</code>","text":"<p>Store share populate data across plugins and placeholders.</p> <p>These data are cleared after each loop of populating of template.</p> <p>Store data that can be afterwards accessed from any future call. It is good practice to check if the same value is not already stored under different key or if the key is not already used for something else.</p> <p>Key should be self-explanatory to content. - wrong: 'folder' - good: 'folder_path'</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>Key under which is key stored.</p> required <code>value</code> <code>Any</code> <p>Value that should be stored under the key.</p> required Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>def get_shared_populate_data(self, key):\n    \"\"\"Store share populate data across plugins and placeholders.\n\n    These data are cleared after each loop of populating of template.\n\n    Store data that can be afterwards accessed from any future call. It\n    is good practice to check if the same value is not already stored under\n    different key or if the key is not already used for something else.\n\n    Key should be self-explanatory to content.\n    - wrong: 'folder'\n    - good: 'folder_path'\n\n    Args:\n        key (str): Key under which is key stored.\n        value (Any): Value that should be stored under the key.\n    \"\"\"\n\n    return self._shared_populate_data.get(key)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.AbstractTemplateBuilder.get_template_preset","title":"<code>get_template_preset()</code>","text":"<p>Unified way how template preset is received using settings.</p> <p>Method is dependent on '_get_build_profiles' which should return filter profiles to resolve path to a template. Default implementation looks into host settings: - 'project_settings/{host name}/templated_workfile_build/profiles'</p> <p>Returns:</p> Name Type Description <code>TemplatePreset</code> <code>TemplatePreset</code> <p>Template preset object with: - <code>path</code>: Resolved template filesystem path. - <code>keep_placeholder</code>: Whether placeholders should remain     after processing. - <code>create_first_version</code>: Whether first workfile version     should be created in workfile-creation flow. - <code>execute_on_app_launch</code>: Whether template should be executed     on application launch. - <code>execute_on_new_file</code>: Whether template should be executed     on new file creation. - <code>profile</code>: Defensive copy of the matched profile from     project settings.</p> <p>Note:     <code>profile[\"path\"]</code> contains the unresolved value from     settings, while <code>path</code> contains the resolved filesystem     path.</p> <p>Raises:</p> Type Description <code>TemplateProfileNotFound</code> <p>When profiles are not filled.</p> <code>TemplateLoadFailed</code> <p>Profile was found but path is not set.</p> <code>TemplateNotFound</code> <p>Path was set but file does not exist.</p> Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>def get_template_preset(self) -&gt; TemplatePreset:\n    \"\"\"Unified way how template preset is received using settings.\n\n    Method is dependent on '_get_build_profiles' which should return filter\n    profiles to resolve path to a template. Default implementation looks\n    into host settings:\n    - 'project_settings/{host name}/templated_workfile_build/profiles'\n\n    Returns:\n        TemplatePreset: Template preset object with:\n            - `path`: Resolved template filesystem path.\n            - `keep_placeholder`: Whether placeholders should remain\n                after processing.\n            - `create_first_version`: Whether first workfile version\n                should be created in workfile-creation flow.\n            - `execute_on_app_launch`: Whether template should be executed\n                on application launch.\n            - `execute_on_new_file`: Whether template should be executed\n                on new file creation.\n            - `profile`: Defensive copy of the matched profile from\n                project settings.\n\n            Note:\n                `profile[\"path\"]` contains the unresolved value from\n                settings, while `path` contains the resolved filesystem\n                path.\n\n    Raises:\n        TemplateProfileNotFound: When profiles are not filled.\n        TemplateLoadFailed: Profile was found but path is not set.\n        TemplateNotFound: Path was set but file does not exist.\n    \"\"\"\n    task_name = self.current_task_name\n    task_type = self.current_task_type\n    folder_path = self.current_folder_path\n    folder_type = None\n    folder_entity = self.current_folder_entity\n    if folder_entity:\n        folder_type = folder_entity[\"folderType\"]\n\n    filter_data = {\n        \"task_types\": task_type,\n        \"task_names\": task_name,\n        \"folder_types\": folder_type,\n        \"folder_paths\": folder_path,\n    }\n\n    build_profiles = self._get_build_profiles()\n    profile = filter_profiles(\n        build_profiles,\n        filter_data,\n        logger=self.log\n    )\n    if not profile:\n        return TemplatePreset()\n\n    path = profile[\"path\"]\n    # resolve path from ayon entity url\n    resolved_path = self.resolve_template_path(path)\n\n    # switch to remove placeholders after they are used\n    keep_placeholder = profile.get(\"keep_placeholder\")\n    create_first_version = profile.get(\"create_first_version\")\n\n    # backward compatibility, since default is True\n    if keep_placeholder is None:\n        keep_placeholder = True\n    if create_first_version is None:\n        create_first_version = True\n\n    return TemplatePreset(\n        path=resolved_path,\n        keep_placeholder=keep_placeholder,\n        create_first_version=create_first_version,\n        execute_on_app_launch=profile.get(\"execute_on_app_launch\", False),\n        execute_on_new_file=profile.get(\"execute_on_new_file\", False),\n        profile=copy.deepcopy(profile)\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.AbstractTemplateBuilder.get_workfile_path","title":"<code>get_workfile_path()</code>","text":"<p>Return last known workfile path or the first workfile path create.</p> Return <p>str: Last workfile path, or first version to create if none exist.</p> Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>def get_workfile_path(self):\n    \"\"\"Return last known workfile path or the first workfile path create.\n\n    Return:\n        str: Last workfile path, or first version to create if none exist.\n    \"\"\"\n    current_path = self.host.get_current_workfile()\n    if current_path:\n        return current_path\n    project_name = self.project_name\n    folder_entity = self.current_folder_entity\n    task_entity = self.current_task_entity\n\n    project_settings = self.project_settings\n    project_entity = ayon_api.get_project(project_name)\n    anatomy = Anatomy(project_name, project_entity=project_entity)\n\n    template_key = get_workfile_template_key(\n        project_name,\n        task_entity[\"taskType\"],\n        self.host_name,\n        project_settings=project_settings,\n    )\n    file_template = anatomy.get_template_item(\"work\", template_key, \"file\")\n    template_data = get_template_data(\n        project_entity,\n        folder_entity,\n        task_entity,\n        self.host_name,\n        settings=project_settings,\n    )\n    workdir = get_workdir(\n        project_entity,\n        folder_entity,\n        task_entity,\n        self.host_name,\n        anatomy=anatomy,\n        template_key=template_key,\n        project_settings=project_settings,\n    )\n    return get_last_workfile(\n        workdir,\n        file_template.template,\n        template_data,\n        set(self.host.get_workfile_extensions() or []),\n        full_path=True,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.AbstractTemplateBuilder.import_template","title":"<code>import_template(template_path)</code>  <code>abstractmethod</code>","text":"<p>Import template in current host.</p> <p>Should load the content of template into scene so 'populate_scene_placeholders' can be started.</p> <p>Parameters:</p> Name Type Description Default <code>template_path</code> <code>str</code> <p>Fullpath for current task and host's template file.</p> required Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>@abstractmethod\ndef import_template(self, template_path):\n    \"\"\"\n    Import template in current host.\n\n    Should load the content of template into scene so\n    'populate_scene_placeholders' can be started.\n\n    Args:\n        template_path (str): Fullpath for current task and\n            host's template file.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.AbstractTemplateBuilder.old_build_template","title":"<code>old_build_template(template_path=None, level_limit=None, keep_placeholders=None, create_first_version=None, workfile_creation_enabled=False)</code>","text":"<p>Legacy callback for building workfile from template path. Deprecated, use 'build_template' with 'preset' argument instead.</p> Todo <p>Handle report of populated placeholders from     'populate_scene_placeholders' to be shown to a user.</p> <p>Parameters:</p> Name Type Description Default <code>template_path</code> <code>str</code> <p>Path to a template file with placeholders. Template from settings 'get_template_preset' used when not passed.</p> <code>None</code> <code>level_limit</code> <code>int</code> <p>Limit of populate loops. Related to 'populate_scene_placeholders' method.</p> <code>None</code> <code>keep_placeholders</code> <code>bool</code> <p>Add flag to placeholder data for hosts to decide if they want to remove placeholder after it is used.</p> <code>None</code> <code>create_first_version</code> <code>bool</code> <p>Create first version of a workfile.  When set to True, this option initiates the saving of the  workfile for an initial version. It will skip saving if  a version already exists.</p> <code>None</code> <code>workfile_creation_enabled</code> <code>bool</code> <p>Whether the call is part of creating a new workfile. When True, we only build if the current file is not an existing saved workfile but a \"new\" file. Basically when enabled we assume the user tries to load it only into a \"New File\" (unsaved empty workfile). When False, the default value, we assume we explicitly want to build the template in our current scene regardless of current scene state.</p> <code>False</code> Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>def old_build_template(\n    self,\n    template_path=None,\n    level_limit=None,\n    keep_placeholders=None,\n    create_first_version=None,\n    workfile_creation_enabled=False\n):\n    \"\"\"Legacy callback for building workfile from template path.\n    Deprecated, use 'build_template' with 'preset' argument instead.\n\n    Todo:\n        Handle report of populated placeholders from\n            'populate_scene_placeholders' to be shown to a user.\n\n    Args:\n        template_path (str): Path to a template file with placeholders.\n            Template from settings 'get_template_preset' used when not\n            passed.\n        level_limit (int): Limit of populate loops. Related to\n            'populate_scene_placeholders' method.\n        keep_placeholders (bool): Add flag to placeholder data for\n            hosts to decide if they want to remove\n            placeholder after it is used.\n        create_first_version (bool): Create first version of a workfile.\n             When set to True, this option initiates the saving of the\n             workfile for an initial version. It will skip saving if\n             a version already exists.\n        workfile_creation_enabled (bool): Whether the call is part of\n            creating a new workfile.\n            When True, we only build if the current file is not\n            an existing saved workfile but a \"new\" file. Basically when\n            enabled we assume the user tries to load it only into a\n            \"New File\" (unsaved empty workfile).\n            When False, the default value, we assume we explicitly want to\n            build the template in our current scene regardless of current\n            scene state.\n\n    \"\"\"\n    # More accurate variable name\n    # - logic related to workfile creation should be moved out in future\n    explicit_build_requested = not workfile_creation_enabled\n\n    # Get default values if not provided\n    if (\n        template_path is None\n        or keep_placeholders is None\n        or create_first_version is None\n    ):\n        preset = self.get_template_preset()\n        if not preset.has_valid_path():\n            raise TemplateLoadFailed(\n                f\"Template path '{preset.path}' does not exist.\"\n            )\n        template_path: str = preset.path\n        if keep_placeholders is None:\n            keep_placeholders: bool = preset.keep_placeholder\n        if create_first_version is None:\n            create_first_version: bool = preset.create_first_version\n\n    # Build the template if we are explicitly requesting it or if it's\n    # an unsaved \"new file\".\n    is_new_file = not self.host.get_current_workfile()\n    if is_new_file or explicit_build_requested:\n        self.log.info(f\"Building the workfile template: {template_path}\")\n        self.import_template(template_path)\n        self.populate_scene_placeholders(\n            level_limit, keep_placeholders)\n\n    # Do not consider saving a first workfile version, if this is not set\n    # to be a \"workfile creation\" or `create_first_version` is disabled.\n    if explicit_build_requested or not create_first_version:\n        return\n\n    # If there is no existing workfile, save the first version\n    workfile_path = self.get_workfile_path()\n    if not os.path.exists(workfile_path):\n        self.log.info(\"Saving first workfile: %s\", workfile_path)\n        save_next_version()\n    else:\n        self.log.info(\n            \"A workfile already exists. Skipping save of workfile as \"\n            \"initial version.\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.AbstractTemplateBuilder.open_template","title":"<code>open_template()</code>","text":"<p>Open template file with registered host.</p> Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>def open_template(self):\n    \"\"\"Open template file with registered host.\"\"\"\n    template_preset = self.get_template_preset()\n    if not template_preset.has_valid_path():\n        raise TemplateLoadFailed(\n            f\"Template path '{template_preset.path}' does not exist.\"\n        )\n    self.host.open_workfile(template_preset.path)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.AbstractTemplateBuilder.populate_scene_placeholders","title":"<code>populate_scene_placeholders(level_limit=None, keep_placeholders=None)</code>","text":"<p>Find placeholders in scene using plugins and process them.</p> <p>This should happen after 'import_template'.</p> <p>Collect available placeholders from scene. All of them are processed after that shared data are cleared. Placeholder items are collected again and if there are any new the loop happens again. This is possible to change with defying 'level_limit'.</p> <p>Placeholders are marked as processed so they're not re-processed. To identify which placeholders were already processed is used placeholder's 'scene_identifier'.</p> <p>Parameters:</p> Name Type Description Default <code>level_limit</code> <code>int</code> <p>Level of loops that can happen. Default is 1000.</p> <code>None</code> <code>keep_placeholders</code> <code>bool</code> <p>Add flag to placeholder data for hosts to decide if they want to remove placeholder after it is used.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>def populate_scene_placeholders(\n    self, level_limit=None, keep_placeholders=None\n):\n    \"\"\"Find placeholders in scene using plugins and process them.\n\n    This should happen after 'import_template'.\n\n    Collect available placeholders from scene. All of them are processed\n    after that shared data are cleared. Placeholder items are collected\n    again and if there are any new the loop happens again. This is possible\n    to change with defying 'level_limit'.\n\n    Placeholders are marked as processed so they're not re-processed. To\n    identify which placeholders were already processed is used\n    placeholder's 'scene_identifier'.\n\n    Args:\n        level_limit (int): Level of loops that can happen. Default is 1000.\n        keep_placeholders (bool): Add flag to placeholder data for\n            hosts to decide if they want to remove\n            placeholder after it is used.\n    \"\"\"\n\n    if not self.placeholder_plugins:\n        self.log.warning(\"There are no placeholder plugins available.\")\n        return\n\n    placeholders = self.get_placeholders()\n    if not placeholders:\n        self.log.warning(\"No placeholders were found.\")\n        return\n\n    # Avoid infinite loop\n    # - 1000 iterations of placeholders processing must be enough\n    if not level_limit:\n        level_limit = 1000\n\n    placeholder_by_scene_id = {\n        placeholder.scene_identifier: placeholder\n        for placeholder in placeholders\n    }\n    all_processed = len(placeholders) == 0\n    # Counter is checked at the end of a loop so the loop happens at least\n    #   once.\n    iter_counter = 0\n    while not all_processed:\n        filtered_placeholders = []\n        for placeholder in placeholders:\n            if placeholder.finished:\n                continue\n\n            if placeholder.in_progress:\n                self.log.warning((\n                    \"Placeholder that should be processed\"\n                    \" is already in progress.\"\n                ))\n                continue\n\n            # add flag for keeping placeholders in scene\n            # after they are processed\n            placeholder.data[\"keep_placeholder\"] = keep_placeholders\n\n            filtered_placeholders.append(placeholder)\n\n        self._prepare_placeholders(filtered_placeholders)\n\n        for placeholder in filtered_placeholders:\n            placeholder.set_in_progress()\n            placeholder_plugin = placeholder.plugin\n            try:\n                placeholder_plugin.populate_placeholder(placeholder)\n\n            except Exception as exc:\n                self.log.warning(\n                    (\n                        \"Failed to process placeholder {} with plugin {}\"\n                    ).format(\n                        placeholder.scene_identifier,\n                        placeholder_plugin.__class__.__name__\n                    ),\n                    exc_info=True\n                )\n                placeholder.set_failed(exc)\n\n            placeholder.set_finished()\n\n        # Trigger on_depth_processed event\n        self.emit_event(\n            topic=\"template.depth_processed\",\n            data={\n                \"depth\": iter_counter,\n                \"placeholders_by_scene_id\": placeholder_by_scene_id\n            },\n            source=\"builder\"\n        )\n\n        # Clear shared data before getting new placeholders\n        self.clear_shared_populate_data()\n\n        iter_counter += 1\n        if iter_counter &gt;= level_limit:\n            break\n\n        all_processed = True\n        collected_placeholders = self.get_placeholders()\n        for placeholder in collected_placeholders:\n            identifier = placeholder.scene_identifier\n            if identifier in placeholder_by_scene_id:\n                continue\n\n            all_processed = False\n            placeholder_by_scene_id[identifier] = placeholder\n            placeholders.append(placeholder)\n\n    # Trigger on_finished event\n    self.emit_event(\n        topic=\"template.finished\",\n        data={\n            \"depth\": iter_counter,\n            \"placeholders_by_scene_id\": placeholder_by_scene_id,\n        },\n        source=\"builder\"\n    )\n\n    self.refresh()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.AbstractTemplateBuilder.rebuild_template","title":"<code>rebuild_template()</code>","text":"<p>Go through existing placeholders in scene and update them.</p> <p>This could not make sense for all plugin types so this is optional logic for plugins.</p> Note <p>Logic is not importing the template again but using placeholders     that were already available. We should maybe change the method     name.</p> Question <p>Should this also handle subloops as it is possible that another     template is loaded during processing?</p> Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>def rebuild_template(self):\n    \"\"\"Go through existing placeholders in scene and update them.\n\n    This could not make sense for all plugin types so this is optional\n    logic for plugins.\n\n    Note:\n        Logic is not importing the template again but using placeholders\n            that were already available. We should maybe change the method\n            name.\n\n    Question:\n        Should this also handle subloops as it is possible that another\n            template is loaded during processing?\n    \"\"\"\n\n    if not self.placeholder_plugins:\n        self.log.info(\"There are no placeholder plugins available.\")\n        return\n\n    placeholders = self.get_placeholders()\n    if not placeholders:\n        self.log.info(\"No placeholders were found.\")\n        return\n\n    for placeholder in placeholders:\n        plugin = placeholder.plugin\n        plugin.repopulate_placeholder(placeholder)\n\n    self.clear_shared_populate_data()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.AbstractTemplateBuilder.refresh","title":"<code>refresh()</code>","text":"<p>Reset cached data.</p> Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>def refresh(self):\n    \"\"\"Reset cached data.\"\"\"\n\n    self._placeholder_plugins = None\n    self._loaders_by_name = None\n    self._creators_by_name = None\n\n    self._current_folder_entity = _NOT_SET\n    self._current_task_entity = _NOT_SET\n    self._linked_folder_entities = _NOT_SET\n\n    self._project_settings = None\n\n    self._event_system = EventSystem()\n\n    self.clear_shared_data()\n    self.clear_shared_populate_data()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.AbstractTemplateBuilder.resolve_template_path","title":"<code>resolve_template_path(path, fill_data=None)</code>","text":"<p>Resolve the template path.</p> <p>By default, this:   - Resolves AYON entity URI to a filesystem path   - Returns path directly if it exists on disk.   - Resolves template keys through anatomy and environment variables.</p> <p>This can be overridden in host integrations to perform additional resolving over the template. Like, <code>hou.text.expandString</code> in Houdini. It's recommended to still call the super().resolve_template_path() to ensure the basic resolving is done across all integrations.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>The input path.</p> required <code>fill_data</code> <code>dict[str, str]</code> <p>Deprecated. This is computed inside the method using the current environment and project settings. Used to be the data to use for template formatting.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>The resolved path.</p> Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>def resolve_template_path(self, path, fill_data=None) -&gt; str:\n    \"\"\"Resolve the template path.\n\n    By default, this:\n      - Resolves AYON entity URI to a filesystem path\n      - Returns path directly if it exists on disk.\n      - Resolves template keys through anatomy and environment variables.\n\n    This can be overridden in host integrations to perform additional\n    resolving over the template. Like, `hou.text.expandString` in Houdini.\n    It's recommended to still call the super().resolve_template_path()\n    to ensure the basic resolving is done across all integrations.\n\n    Arguments:\n        path (str): The input path.\n        fill_data (dict[str, str]): Deprecated. This is computed inside\n            the method using the current environment and project settings.\n            Used to be the data to use for template formatting.\n\n    Returns:\n        str: The resolved path.\n\n    \"\"\"\n\n    # If the path is an AYON entity URI, then resolve the filepath\n    # through the backend\n    if path.startswith(\"ayon+entity://\") or path.startswith(\"ayon://\"):\n        # This is a special case where the path is an AYON entity URI\n        # We need to resolve it to a filesystem path\n        resolved_path = resolve_entity_uri(path)\n        return resolved_path\n\n    # If the path is set and it's found on disk, return it directly\n    if path and os.path.exists(path):\n        return path\n\n    # We may have path for another platform, like C:/path/to/file\n    # or a path with template keys, like {project[code]} or both.\n    # Try to fill path with environments and anatomy roots\n    project_name = self.project_name\n    anatomy = Anatomy(project_name)\n\n    # Simple check whether the path contains any template keys\n    if \"{\" in path:\n        fill_data = {\n            key: value\n            for key, value in os.environ.items()\n        }\n        fill_data[\"root\"] = anatomy.roots\n        fill_data[\"project\"] = {\n            \"name\": project_name,\n            \"code\": anatomy.project_code,\n        }\n\n        # Format the template using local fill data\n        result = StringTemplate.format_template(path, fill_data)\n        if not result.solved:\n            return path\n\n        path = result.normalized()\n        if os.path.exists(path):\n            return path\n\n    # If the path were set in settings using a Windows path and we\n    # are now on a Linux system, we try to convert the solved path to\n    # the current platform.\n    while True:\n        try:\n            solved_path = anatomy.path_remapper(path)\n        except KeyError as missing_key:\n            raise KeyError(\n                f\"Could not solve key '{missing_key}'\"\n                f\" in template path '{path}'\"\n            )\n\n        if solved_path is None:\n            solved_path = path\n        if solved_path == path:\n            break\n        path = solved_path\n\n    solved_path = os.path.normpath(solved_path)\n    return solved_path\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.AbstractTemplateBuilder.set_shared_data","title":"<code>set_shared_data(key, value)</code>","text":"<p>Store share data across plugins and placeholders.</p> <p>Store data that can be afterwards accessed from any future call. It is good practice to check if the same value is not already stored under different key or if the key is not already used for something else.</p> <p>Key should be self-explanatory to content. - wrong: 'folder' - good: 'folder_name'</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>Key under which is key stored.</p> required <code>value</code> <code>Any</code> <p>Value that should be stored under the key.</p> required Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>def set_shared_data(self, key, value):\n    \"\"\"Store share data across plugins and placeholders.\n\n    Store data that can be afterwards accessed from any future call. It\n    is good practice to check if the same value is not already stored under\n    different key or if the key is not already used for something else.\n\n    Key should be self-explanatory to content.\n    - wrong: 'folder'\n    - good: 'folder_name'\n\n    Args:\n        key (str): Key under which is key stored.\n        value (Any): Value that should be stored under the key.\n    \"\"\"\n\n    self._shared_data[key] = value\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.AbstractTemplateBuilder.set_shared_populate_data","title":"<code>set_shared_populate_data(key, value)</code>","text":"<p>Store share populate data across plugins and placeholders.</p> <p>These data are cleared after each loop of populating of template.</p> <p>Store data that can be afterwards accessed from any future call. It is good practice to check if the same value is not already stored under different key or if the key is not already used for something else.</p> <p>Key should be self-explanatory to content. - wrong: 'folder' - good: 'folder_path'</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>Key under which is key stored.</p> required <code>value</code> <code>Any</code> <p>Value that should be stored under the key.</p> required Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>def set_shared_populate_data(self, key, value):\n    \"\"\"Store share populate data across plugins and placeholders.\n\n    These data are cleared after each loop of populating of template.\n\n    Store data that can be afterwards accessed from any future call. It\n    is good practice to check if the same value is not already stored under\n    different key or if the key is not already used for something else.\n\n    Key should be self-explanatory to content.\n    - wrong: 'folder'\n    - good: 'folder_path'\n\n    Args:\n        key (str): Key under which is key stored.\n        value (Any): Value that should be stored under the key.\n    \"\"\"\n\n    self._shared_populate_data[key] = value\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.AbstractTemplateBuilder.trigger_on_app_launch","title":"<code>trigger_on_app_launch(preset=None)</code>","text":"<p>Trigger template build on application launch if the preset allows it.</p> <p>Parameters:</p> Name Type Description Default <code>preset</code> <code>TemplatePreset | None</code> <p>The template preset to use for building the workfile template. If not provided, the default template preset will be used.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>def trigger_on_app_launch(\n    self, preset: TemplatePreset | None = None\n) -&gt; None:\n    \"\"\"\n    Trigger template build on application launch if the preset allows it.\n\n    Args:\n        preset (TemplatePreset | None): The template preset to use for\n            building the workfile template. If not provided, the default\n            template preset will be used.\n\n    \"\"\"\n    if preset is None:\n        preset = self.get_template_preset()\n\n    if not preset.execute_on_app_launch:\n        return\n\n    if not preset.has_valid_path():\n        return\n\n    self.build_template(preset=preset)\n    if preset.create_first_version:\n        workfile_path = self.get_workfile_path()\n        if not os.path.exists(workfile_path):\n            self.log.info(\"Saving first workfile: %s\", workfile_path)\n            save_next_version()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.AbstractTemplateBuilder.trigger_on_new_file","title":"<code>trigger_on_new_file(preset=None)</code>","text":"<p>Trigger template build on new file creation if the preset allows it.</p> <p>Parameters:</p> Name Type Description Default <code>preset</code> <code>TemplatePreset | None</code> <p>The template preset to use for building the workfile template. If not provided, the default template preset will be used.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>def trigger_on_new_file(\n    self, preset: TemplatePreset | None = None\n) -&gt; None:\n    \"\"\"\n    Trigger template build on new file creation if the preset allows it.\n\n    Args:\n        preset (TemplatePreset | None): The template preset to use for\n            building the workfile template. If not provided, the default\n            template preset will be used.\n\n    \"\"\"\n    if preset is None:\n        preset = self.get_template_preset()\n\n    if not preset.execute_on_new_file:\n        return\n\n    if not preset.has_valid_path():\n        return\n\n    self.build_template(preset=preset)\n    if preset.create_first_version:\n        workfile_path = self.get_workfile_path()\n        if not os.path.exists(workfile_path):\n            self.log.info(\"Saving first workfile: %s\", workfile_path)\n            save_next_version()\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.CreatePlaceholderItem","title":"<code>CreatePlaceholderItem</code>","text":"<p>               Bases: <code>PlaceholderItem</code></p> <p>PlaceholderItem for plugin which is creating publish instance.</p> <p>Connected to 'PlaceholderCreateMixin'.</p> Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>class CreatePlaceholderItem(PlaceholderItem):\n    \"\"\"PlaceholderItem for plugin which is creating publish instance.\n\n    Connected to 'PlaceholderCreateMixin'.\n    \"\"\"\n\n    def __init__(self, *args, **kwargs):\n        super(CreatePlaceholderItem, self).__init__(*args, **kwargs)\n        self._failed_created_publish_instances = []\n\n    def get_errors(self):\n        if not self._failed_created_publish_instances:\n            return []\n        message = (\n            \"Failed to create {} instance using Creator {}\"\n        ).format(\n            len(self._failed_created_publish_instances),\n            self.data[\"creator\"]\n        )\n        return [message]\n\n    def create_failed(self, creator_data):\n        self._failed_created_publish_instances.append(creator_data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.EntityResolutionError","title":"<code>EntityResolutionError</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Exception raised when entity URI resolution fails.</p> Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>class EntityResolutionError(Exception):\n    \"\"\"Exception raised when entity URI resolution fails.\"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.LoadPlaceholderItem","title":"<code>LoadPlaceholderItem</code>","text":"<p>               Bases: <code>PlaceholderItem</code></p> <p>PlaceholderItem for plugin which is loading representations.</p> <p>Connected to 'PlaceholderLoadMixin'.</p> Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>class LoadPlaceholderItem(PlaceholderItem):\n    \"\"\"PlaceholderItem for plugin which is loading representations.\n\n    Connected to 'PlaceholderLoadMixin'.\n    \"\"\"\n\n    def __init__(self, *args, **kwargs):\n        super(LoadPlaceholderItem, self).__init__(*args, **kwargs)\n        self._failed_representations = []\n\n    def get_errors(self):\n        if not self._failed_representations:\n            return []\n        message = (\n            \"Failed to load {} representations using Loader {}\"\n        ).format(\n            len(self._failed_representations),\n            self.data[\"loader\"]\n        )\n        return [message]\n\n    def load_failed(self, representation):\n        self._failed_representations.append(representation)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.PlaceholderCreateMixin","title":"<code>PlaceholderCreateMixin</code>","text":"<p>               Bases: <code>object</code></p> <p>Mixin prepared for creating placeholder plugins.</p> <p>Implementation prepares options for placeholders with 'get_create_plugin_options'.</p> <p>For placeholder population is implemented 'populate_create_placeholder'.</p> <p>PlaceholderItem can have implemented methods: - 'create_failed' - called when creating of an instance failed - 'create_succeed' - called when creating of an instance succeeded</p> Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>class PlaceholderCreateMixin(object):\n    \"\"\"Mixin prepared for creating placeholder plugins.\n\n    Implementation prepares options for placeholders with\n    'get_create_plugin_options'.\n\n    For placeholder population is implemented 'populate_create_placeholder'.\n\n    PlaceholderItem can have implemented methods:\n    - 'create_failed' - called when creating of an instance failed\n    - 'create_succeed' - called when creating of an instance succeeded\n    \"\"\"\n\n    def get_create_plugin_options(self, options=None):\n        \"\"\"Unified attribute definitions for create placeholder.\n\n        Common function for placeholder plugins used for creating of\n        publishable instances. Use it with 'get_placeholder_options'.\n\n        Args:\n            options (Dict[str, Any]): Already available options which are used\n                as defaults for attributes.\n\n        Returns:\n            List[AbstractAttrDef]: Attribute definitions common for create\n                plugins.\n        \"\"\"\n\n        creators_by_name = self.builder.get_creators_by_name()\n\n        creator_items = [\n            (creator_name, creator.label or creator_name)\n            for creator_name, creator in creators_by_name.items()\n        ]\n\n        creator_items.sort(key=lambda i: i[1])\n        options = options or {}\n        return [\n            attribute_definitions.UISeparatorDef(),\n            attribute_definitions.UILabelDef(\"Main attributes\"),\n            attribute_definitions.UISeparatorDef(),\n\n            attribute_definitions.EnumDef(\n                \"creator\",\n                label=\"Creator\",\n                default=options.get(\"creator\"),\n                items=creator_items,\n                tooltip=(\n                    \"Creator\"\n                    \"\\nDefines what AYON creator will be used to\"\n                    \" create publishable instance.\"\n                    \"\\nUseable creator depends on current host's creator list.\"\n                    \"\\nField is case sensitive.\"\n                )\n            ),\n            attribute_definitions.TextDef(\n                \"create_variant\",\n                label=\"Variant\",\n                default=options.get(\"create_variant\"),\n                placeholder='Main',\n                tooltip=(\n                    \"Creator\"\n                    \"\\nDefines variant name which will be use for \"\n                    \"\\ncompiling of product name.\"\n                )\n            ),\n            attribute_definitions.BoolDef(\n                \"active\",\n                label=\"Active\",\n                default=options.get(\"active\", True),\n                tooltip=(\n                    \"Active\"\n                    \"\\nDefines whether the created instance will default to \"\n                    \"active or not.\"\n                )\n            ),\n            attribute_definitions.UISeparatorDef(),\n            attribute_definitions.NumberDef(\n                \"order\",\n                label=\"Order\",\n                default=options.get(\"order\") or 0,\n                decimals=0,\n                minimum=0,\n                maximum=999,\n                tooltip=(\n                    \"Order\"\n                    \"\\nOrder defines creating instance priority (0 to 999)\"\n                    \"\\nPriority rule is : \\\"lowest is first to load\\\".\"\n                )\n            )\n        ]\n\n    def populate_create_placeholder(self, placeholder, pre_create_data=None):\n        \"\"\"Create placeholder is going to create matching publishabe instance.\n\n        Args:\n            placeholder (PlaceholderItem): Placeholder item with information\n                about requested publishable instance.\n            pre_create_data (dict): dictionary of configuration from Creator\n                configuration in UI\n        \"\"\"\n        creator_name = placeholder.data[\"creator\"]\n        create_variant = placeholder.data[\"create_variant\"]\n        active = placeholder.data.get(\"active\")\n\n        creator_plugin = self.builder.get_creators_by_name()[creator_name]\n\n        # create product name\n        context = self._builder.get_current_context()\n        project_name = context[\"project_name\"]\n        folder_path = context[\"folder_path\"]\n        task_name = context[\"task_name\"]\n        host_name = self.builder.host_name\n\n        folder_entity = get_folder_by_path(project_name, folder_path)\n        if not folder_entity:\n            raise ValueError(\"Current context does not have set folder\")\n        task_entity = get_task_by_name(\n            project_name, folder_entity[\"id\"], task_name\n        )\n\n        product_name = creator_plugin.get_product_name(\n            project_name,\n            folder_entity,\n            task_entity,\n            create_variant,\n            host_name\n        )\n\n        creator_data = {\n            \"creator_name\": creator_name,\n            \"create_variant\": create_variant,\n            \"product_name\": product_name,\n            \"creator_plugin\": creator_plugin\n        }\n\n        self._before_instance_create(placeholder)\n\n        # compile product name from variant\n        try:\n            creator_instance = self.builder.create_context.create(\n                creator_plugin.identifier,\n                create_variant,\n                folder_entity,\n                task_entity,\n                pre_create_data=pre_create_data,\n                active=active\n            )\n\n        except:  # noqa: E722\n            failed = True\n            self.create_failed(placeholder, creator_data)\n\n        else:\n            failed = False\n            self.create_succeed(placeholder, creator_instance)\n\n        self.post_placeholder_process(placeholder, failed)\n\n        if failed:\n            self.log.debug(\n                \"Placeholder cleanup skipped due to failed placeholder \"\n                \"population.\"\n            )\n            return\n\n        if not placeholder.data.get(\"keep_placeholder\", True):\n            self.delete_placeholder(placeholder)\n\n    def create_failed(self, placeholder, creator_data):\n        if hasattr(placeholder, \"create_failed\"):\n            placeholder.create_failed(creator_data)\n\n    def create_succeed(self, placeholder, creator_instance):\n        if hasattr(placeholder, \"create_succeed\"):\n            placeholder.create_succeed(creator_instance)\n\n    def post_placeholder_process(self, placeholder, failed):\n        \"\"\"Cleanup placeholder after load of its corresponding representations.\n\n        Args:\n            placeholder (PlaceholderItem): Item which was just used to load\n                representation.\n            failed (bool): Loading of representation failed.\n        \"\"\"\n        pass\n\n    def delete_placeholder(self, placeholder):\n        \"\"\"Called when all item population is done.\"\"\"\n        self.log.debug(\"Clean up of placeholder is not implemented.\")\n\n    def _before_instance_create(self, placeholder):\n        \"\"\"Can be overridden. Is called before instance is created.\"\"\"\n\n        pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.PlaceholderCreateMixin.delete_placeholder","title":"<code>delete_placeholder(placeholder)</code>","text":"<p>Called when all item population is done.</p> Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>def delete_placeholder(self, placeholder):\n    \"\"\"Called when all item population is done.\"\"\"\n    self.log.debug(\"Clean up of placeholder is not implemented.\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.PlaceholderCreateMixin.get_create_plugin_options","title":"<code>get_create_plugin_options(options=None)</code>","text":"<p>Unified attribute definitions for create placeholder.</p> <p>Common function for placeholder plugins used for creating of publishable instances. Use it with 'get_placeholder_options'.</p> <p>Parameters:</p> Name Type Description Default <code>options</code> <code>Dict[str, Any]</code> <p>Already available options which are used as defaults for attributes.</p> <code>None</code> <p>Returns:</p> Type Description <p>List[AbstractAttrDef]: Attribute definitions common for create plugins.</p> Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>def get_create_plugin_options(self, options=None):\n    \"\"\"Unified attribute definitions for create placeholder.\n\n    Common function for placeholder plugins used for creating of\n    publishable instances. Use it with 'get_placeholder_options'.\n\n    Args:\n        options (Dict[str, Any]): Already available options which are used\n            as defaults for attributes.\n\n    Returns:\n        List[AbstractAttrDef]: Attribute definitions common for create\n            plugins.\n    \"\"\"\n\n    creators_by_name = self.builder.get_creators_by_name()\n\n    creator_items = [\n        (creator_name, creator.label or creator_name)\n        for creator_name, creator in creators_by_name.items()\n    ]\n\n    creator_items.sort(key=lambda i: i[1])\n    options = options or {}\n    return [\n        attribute_definitions.UISeparatorDef(),\n        attribute_definitions.UILabelDef(\"Main attributes\"),\n        attribute_definitions.UISeparatorDef(),\n\n        attribute_definitions.EnumDef(\n            \"creator\",\n            label=\"Creator\",\n            default=options.get(\"creator\"),\n            items=creator_items,\n            tooltip=(\n                \"Creator\"\n                \"\\nDefines what AYON creator will be used to\"\n                \" create publishable instance.\"\n                \"\\nUseable creator depends on current host's creator list.\"\n                \"\\nField is case sensitive.\"\n            )\n        ),\n        attribute_definitions.TextDef(\n            \"create_variant\",\n            label=\"Variant\",\n            default=options.get(\"create_variant\"),\n            placeholder='Main',\n            tooltip=(\n                \"Creator\"\n                \"\\nDefines variant name which will be use for \"\n                \"\\ncompiling of product name.\"\n            )\n        ),\n        attribute_definitions.BoolDef(\n            \"active\",\n            label=\"Active\",\n            default=options.get(\"active\", True),\n            tooltip=(\n                \"Active\"\n                \"\\nDefines whether the created instance will default to \"\n                \"active or not.\"\n            )\n        ),\n        attribute_definitions.UISeparatorDef(),\n        attribute_definitions.NumberDef(\n            \"order\",\n            label=\"Order\",\n            default=options.get(\"order\") or 0,\n            decimals=0,\n            minimum=0,\n            maximum=999,\n            tooltip=(\n                \"Order\"\n                \"\\nOrder defines creating instance priority (0 to 999)\"\n                \"\\nPriority rule is : \\\"lowest is first to load\\\".\"\n            )\n        )\n    ]\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.PlaceholderCreateMixin.populate_create_placeholder","title":"<code>populate_create_placeholder(placeholder, pre_create_data=None)</code>","text":"<p>Create placeholder is going to create matching publishabe instance.</p> <p>Parameters:</p> Name Type Description Default <code>placeholder</code> <code>PlaceholderItem</code> <p>Placeholder item with information about requested publishable instance.</p> required <code>pre_create_data</code> <code>dict</code> <p>dictionary of configuration from Creator configuration in UI</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>def populate_create_placeholder(self, placeholder, pre_create_data=None):\n    \"\"\"Create placeholder is going to create matching publishabe instance.\n\n    Args:\n        placeholder (PlaceholderItem): Placeholder item with information\n            about requested publishable instance.\n        pre_create_data (dict): dictionary of configuration from Creator\n            configuration in UI\n    \"\"\"\n    creator_name = placeholder.data[\"creator\"]\n    create_variant = placeholder.data[\"create_variant\"]\n    active = placeholder.data.get(\"active\")\n\n    creator_plugin = self.builder.get_creators_by_name()[creator_name]\n\n    # create product name\n    context = self._builder.get_current_context()\n    project_name = context[\"project_name\"]\n    folder_path = context[\"folder_path\"]\n    task_name = context[\"task_name\"]\n    host_name = self.builder.host_name\n\n    folder_entity = get_folder_by_path(project_name, folder_path)\n    if not folder_entity:\n        raise ValueError(\"Current context does not have set folder\")\n    task_entity = get_task_by_name(\n        project_name, folder_entity[\"id\"], task_name\n    )\n\n    product_name = creator_plugin.get_product_name(\n        project_name,\n        folder_entity,\n        task_entity,\n        create_variant,\n        host_name\n    )\n\n    creator_data = {\n        \"creator_name\": creator_name,\n        \"create_variant\": create_variant,\n        \"product_name\": product_name,\n        \"creator_plugin\": creator_plugin\n    }\n\n    self._before_instance_create(placeholder)\n\n    # compile product name from variant\n    try:\n        creator_instance = self.builder.create_context.create(\n            creator_plugin.identifier,\n            create_variant,\n            folder_entity,\n            task_entity,\n            pre_create_data=pre_create_data,\n            active=active\n        )\n\n    except:  # noqa: E722\n        failed = True\n        self.create_failed(placeholder, creator_data)\n\n    else:\n        failed = False\n        self.create_succeed(placeholder, creator_instance)\n\n    self.post_placeholder_process(placeholder, failed)\n\n    if failed:\n        self.log.debug(\n            \"Placeholder cleanup skipped due to failed placeholder \"\n            \"population.\"\n        )\n        return\n\n    if not placeholder.data.get(\"keep_placeholder\", True):\n        self.delete_placeholder(placeholder)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.PlaceholderCreateMixin.post_placeholder_process","title":"<code>post_placeholder_process(placeholder, failed)</code>","text":"<p>Cleanup placeholder after load of its corresponding representations.</p> <p>Parameters:</p> Name Type Description Default <code>placeholder</code> <code>PlaceholderItem</code> <p>Item which was just used to load representation.</p> required <code>failed</code> <code>bool</code> <p>Loading of representation failed.</p> required Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>def post_placeholder_process(self, placeholder, failed):\n    \"\"\"Cleanup placeholder after load of its corresponding representations.\n\n    Args:\n        placeholder (PlaceholderItem): Item which was just used to load\n            representation.\n        failed (bool): Loading of representation failed.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.PlaceholderItem","title":"<code>PlaceholderItem</code>","text":"<p>               Bases: <code>object</code></p> <p>Item representing single item in scene that is a placeholder to process.</p> <p>Items are always created and updated by their plugins. Each plugin can use modified class of 'PlaceholderItem' but only to add more options instead of new other.</p> <p>Scene identifier is used to avoid processing of the placeholder item multiple times so must be unique across whole workfile builder.</p> <p>Parameters:</p> Name Type Description Default <code>scene_identifier</code> <code>str</code> <p>Unique scene identifier. If placeholder is created from the same \"node\" it must have same identifier.</p> required <code>data</code> <code>Dict[str, Any]</code> <p>Data related to placeholder. They're defined by plugin.</p> required <code>plugin</code> <code>PlaceholderPlugin</code> <p>Plugin which created the placeholder item.</p> required Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>class PlaceholderItem(object):\n    \"\"\"Item representing single item in scene that is a placeholder to process.\n\n    Items are always created and updated by their plugins. Each plugin can use\n    modified class of 'PlaceholderItem' but only to add more options instead of\n    new other.\n\n    Scene identifier is used to avoid processing of the placeholder item\n    multiple times so must be unique across whole workfile builder.\n\n    Args:\n        scene_identifier (str): Unique scene identifier. If placeholder is\n            created from the same \"node\" it must have same identifier.\n        data (Dict[str, Any]): Data related to placeholder. They're defined\n            by plugin.\n        plugin (PlaceholderPlugin): Plugin which created the placeholder item.\n    \"\"\"\n\n    default_order = 100\n\n    def __init__(self, scene_identifier, data, plugin):\n        self._log = None\n        self._scene_identifier = scene_identifier\n        self._data = data\n        self._plugin = plugin\n\n        # Keep track about state of Placeholder process\n        self._state = 0\n\n        # Error messages to be shown in UI\n        # - all other messages should be logged\n        self._errors = []  # -&gt; List[str]\n\n    @property\n    def plugin(self):\n        \"\"\"Access to plugin which created placeholder.\n\n        Returns:\n            PlaceholderPlugin: Plugin object.\n        \"\"\"\n\n        return self._plugin\n\n    @property\n    def builder(self):\n        \"\"\"Access to builder.\n\n        Returns:\n            AbstractTemplateBuilder: Builder which is the top part of\n                placeholder.\n        \"\"\"\n\n        return self.plugin.builder\n\n    @property\n    def data(self):\n        \"\"\"Placeholder data which can modify how placeholder is processed.\n\n        Possible general keys\n        - order: Can define the order in which is placeholder processed.\n                    Lower == earlier.\n\n        Other keys are defined by placeholder and should validate them on item\n        creation.\n\n        Returns:\n            Dict[str, Any]: Placeholder item data.\n        \"\"\"\n\n        return self._data\n\n    def to_dict(self):\n        \"\"\"Create copy of item's data.\n\n        Returns:\n            Dict[str, Any]: Placeholder data.\n        \"\"\"\n\n        return copy.deepcopy(self.data)\n\n    @property\n    def log(self):\n        if self._log is None:\n            self._log = Logger.get_logger(repr(self))\n        return self._log\n\n    def __repr__(self):\n        return \"&lt; {} {} &gt;\".format(\n            self.__class__.__name__,\n            self._scene_identifier\n        )\n\n    @property\n    def order(self):\n        \"\"\"Order of item processing.\"\"\"\n\n        order = self._data.get(\"order\")\n        if order is None:\n            return self.default_order\n        return order\n\n    @property\n    def scene_identifier(self):\n        return self._scene_identifier\n\n    @property\n    def finished(self):\n        \"\"\"Item was already processed.\"\"\"\n\n        return self._state == 2\n\n    @property\n    def in_progress(self):\n        \"\"\"Processing is in progress.\"\"\"\n\n        return self._state == 1\n\n    def set_in_progress(self):\n        \"\"\"Change to in progress state.\"\"\"\n\n        self._state = 1\n\n    def set_finished(self):\n        \"\"\"Change to finished state.\"\"\"\n\n        self._state = 2\n\n    def set_failed(self, exception):\n        self.add_error(str(exception))\n\n    def add_error(self, error):\n        \"\"\"Set placeholder item as failed and mark it as finished.\"\"\"\n\n        self._errors.append(error)\n\n    def get_errors(self):\n        \"\"\"Exception with which the placeholder process failed.\n\n        Gives ability to access the exception.\n        \"\"\"\n\n        return self._errors\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.PlaceholderItem.builder","title":"<code>builder</code>  <code>property</code>","text":"<p>Access to builder.</p> <p>Returns:</p> Name Type Description <code>AbstractTemplateBuilder</code> <p>Builder which is the top part of placeholder.</p>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.PlaceholderItem.data","title":"<code>data</code>  <code>property</code>","text":"<p>Placeholder data which can modify how placeholder is processed.</p> <p>Possible general keys - order: Can define the order in which is placeholder processed.             Lower == earlier.</p> <p>Other keys are defined by placeholder and should validate them on item creation.</p> <p>Returns:</p> Type Description <p>Dict[str, Any]: Placeholder item data.</p>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.PlaceholderItem.finished","title":"<code>finished</code>  <code>property</code>","text":"<p>Item was already processed.</p>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.PlaceholderItem.in_progress","title":"<code>in_progress</code>  <code>property</code>","text":"<p>Processing is in progress.</p>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.PlaceholderItem.order","title":"<code>order</code>  <code>property</code>","text":"<p>Order of item processing.</p>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.PlaceholderItem.plugin","title":"<code>plugin</code>  <code>property</code>","text":"<p>Access to plugin which created placeholder.</p> <p>Returns:</p> Name Type Description <code>PlaceholderPlugin</code> <p>Plugin object.</p>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.PlaceholderItem.add_error","title":"<code>add_error(error)</code>","text":"<p>Set placeholder item as failed and mark it as finished.</p> Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>def add_error(self, error):\n    \"\"\"Set placeholder item as failed and mark it as finished.\"\"\"\n\n    self._errors.append(error)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.PlaceholderItem.get_errors","title":"<code>get_errors()</code>","text":"<p>Exception with which the placeholder process failed.</p> <p>Gives ability to access the exception.</p> Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>def get_errors(self):\n    \"\"\"Exception with which the placeholder process failed.\n\n    Gives ability to access the exception.\n    \"\"\"\n\n    return self._errors\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.PlaceholderItem.set_finished","title":"<code>set_finished()</code>","text":"<p>Change to finished state.</p> Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>def set_finished(self):\n    \"\"\"Change to finished state.\"\"\"\n\n    self._state = 2\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.PlaceholderItem.set_in_progress","title":"<code>set_in_progress()</code>","text":"<p>Change to in progress state.</p> Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>def set_in_progress(self):\n    \"\"\"Change to in progress state.\"\"\"\n\n    self._state = 1\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.PlaceholderItem.to_dict","title":"<code>to_dict()</code>","text":"<p>Create copy of item's data.</p> <p>Returns:</p> Type Description <p>Dict[str, Any]: Placeholder data.</p> Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>def to_dict(self):\n    \"\"\"Create copy of item's data.\n\n    Returns:\n        Dict[str, Any]: Placeholder data.\n    \"\"\"\n\n    return copy.deepcopy(self.data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.PlaceholderLoadMixin","title":"<code>PlaceholderLoadMixin</code>","text":"<p>               Bases: <code>object</code></p> <p>Mixin prepared for loading placeholder plugins.</p> <p>Implementation prepares options for placeholders with 'get_load_plugin_options'.</p> <p>For placeholder population is implemented 'populate_load_placeholder'.</p> <p>PlaceholderItem can have implemented methods: - 'load_failed' - called when loading of one representation failed - 'load_succeed' - called when loading of one representation succeeded</p> Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>class PlaceholderLoadMixin(object):\n    \"\"\"Mixin prepared for loading placeholder plugins.\n\n    Implementation prepares options for placeholders with\n    'get_load_plugin_options'.\n\n    For placeholder population is implemented 'populate_load_placeholder'.\n\n    PlaceholderItem can have implemented methods:\n    - 'load_failed' - called when loading of one representation failed\n    - 'load_succeed' - called when loading of one representation succeeded\n    \"\"\"\n\n    def get_load_plugin_options(self, options=None):\n        \"\"\"Unified attribute definitions for load placeholder.\n\n        Common function for placeholder plugins used for loading of\n        representations. Use it in 'get_placeholder_options'.\n\n        Args:\n            options (Dict[str, Any]): Already available options which are used\n                as defaults for attributes.\n\n        Returns:\n            List[AbstractAttrDef]: Attribute definitions common for load\n                plugins.\n        \"\"\"\n\n        loaders_by_name = self.builder.get_loaders_by_name()\n        loader_items = [\n            {\"value\": loader_name, \"label\": loader.label or loader_name}\n            for loader_name, loader in loaders_by_name.items()\n        ]\n\n        loader_items = list(sorted(loader_items, key=lambda i: i[\"label\"]))\n        options = options or {}\n\n        # Get product base types from all loaders excluding \"*\"\n        product_base_types = set()\n        for loader in loaders_by_name.values():\n            l_product_base_types = loader.product_base_types\n            if l_product_base_types is None:\n                l_product_base_types = loader.product_types\n            if l_product_base_types:\n                product_base_types.update(l_product_base_types)\n        product_base_types.discard(\"*\")\n\n        # Sort for readability\n        product_base_types = list(sorted(product_base_types))\n\n        builder_type_enum_items = [\n            {\"label\": \"Current folder\", \"value\": \"context_folder\"},\n            {\"label\": \"Linked folders\", \"value\": \"linked_folders\"},\n            {\"label\": \"All folders\", \"value\": \"all_folders\"},\n        ]\n\n        link_types = ayon_api.get_link_types(self.builder.project_name)\n\n        # Filter link types for folder to folder links\n        link_types_enum_items = [\n            {\"label\": link_type[\"name\"], \"value\": link_type[\"linkType\"]}\n            for link_type in link_types\n            if (\n                link_type[\"inputType\"] == \"folder\"\n                and link_type[\"outputType\"] == \"folder\"\n            )\n        ]\n\n        if not link_types_enum_items:\n            link_types_enum_items.append(\n                {\"label\": \"&lt;No link types&gt;\", \"value\": None}\n            )\n\n        build_type_label = \"Folder Builder Type\"\n        build_type_help = (\n            \"Folder Builder Type\\n\"\n            \"\\nBuilder type describe what template loader will look\"\n            \" for.\"\n            \"\\nCurrent Folder: Template loader will look for products\"\n            \" of current context folder (Folder /assets/bob will\"\n            \" find asset)\"\n            \"\\nAll folders: All folders matching the regex will be\"\n            \" used.\"\n        )\n\n        product_base_type = options.get(\"product_base_type\")\n        if product_base_type is None:\n            product_base_type = options.get(\"product_type\")\n            if product_base_type is None:\n                product_base_type = options.get(\"family\")\n\n        return [\n            attribute_definitions.UISeparatorDef(),\n            attribute_definitions.UILabelDef(\"Main attributes\"),\n            attribute_definitions.UISeparatorDef(),\n\n            attribute_definitions.EnumDef(\n                \"builder_type\",\n                label=build_type_label,\n                default=options.get(\"builder_type\"),\n                items=builder_type_enum_items,\n                tooltip=build_type_help\n            ),\n            attribute_definitions.EnumDef(\n                \"link_type\",\n                label=\"Link Type\",\n                default=options.get(\"link_type\"),\n                items=link_types_enum_items,\n                tooltip=(\n                    \"Link Type\\n\"\n                    \"\\nDefines what type of link will be used to\"\n                    \" link the product to the current folder.\"\n                )\n            ),\n            attribute_definitions.EnumDef(\n                \"product_base_type\",\n                label=\"Product base type\",\n                default=product_base_type,\n                items=product_base_types\n            ),\n            attribute_definitions.TextDef(\n                \"representation\",\n                label=\"Representation name\",\n                default=options.get(\"representation\"),\n                placeholder=\"ma, abc, ...\"\n            ),\n            attribute_definitions.EnumDef(\n                \"loader\",\n                label=\"Loader\",\n                default=options.get(\"loader\"),\n                items=loader_items,\n                tooltip=(\n                    \"Loader\"\n                    \"\\nDefines what AYON loader will be used to\"\n                    \" load assets.\"\n                    \"\\nUseable loader depends on current host's loader list.\"\n                    \"\\nField is case sensitive.\"\n                )\n            ),\n            attribute_definitions.TextDef(\n                \"loader_args\",\n                label=\"Loader Arguments\",\n                default=options.get(\"loader_args\"),\n                placeholder='{\"camera\":\"persp\", \"lights\":True}',\n                tooltip=(\n                    \"Loader\"\n                    \"\\nDefines a dictionary of arguments used to load assets.\"\n                    \"\\nUseable arguments depend on current placeholder Loader.\"\n                    \"\\nField should be a valid python dict.\"\n                    \" Anything else will be ignored.\"\n                )\n            ),\n            attribute_definitions.NumberDef(\n                \"order\",\n                label=\"Order\",\n                default=options.get(\"order\") or 0,\n                decimals=0,\n                minimum=0,\n                maximum=999,\n                tooltip=(\n                    \"Order\"\n                    \"\\nOrder defines asset loading priority (0 to 999)\"\n                    \"\\nPriority rule is : \\\"lowest is first to load\\\".\"\n                )\n            ),\n            attribute_definitions.UISeparatorDef(),\n            attribute_definitions.UILabelDef(\"Optional attributes\"),\n            attribute_definitions.UISeparatorDef(),\n            attribute_definitions.TextDef(\n                \"folder_path\",\n                label=\"Folder filter\",\n                default=options.get(\"folder_path\"),\n                placeholder=\"regex filtering by folder path\",\n                tooltip=(\n                    \"Filtering assets by matching\"\n                    \" field regex to folder path\"\n                )\n            ),\n            attribute_definitions.TextDef(\n                \"product_name\",\n                label=\"Product filter\",\n                default=options.get(\"product_name\"),\n                placeholder=\"regex filtering by product name\",\n                tooltip=(\n                    \"Filtering assets by matching\"\n                    \" field regex to product name\"\n                )\n            ),\n        ]\n\n    def parse_loader_args(self, loader_args):\n        \"\"\"Helper function to parse string of loader arguments.\n\n        Empty dictionary is returned if conversion fails.\n\n        Args:\n            loader_args (str): Loader args filled by user.\n\n        Returns:\n            Dict[str, Any]: Parsed arguments used as dictionary.\n        \"\"\"\n\n        if not loader_args:\n            return {}\n\n        try:\n            parsed_args = eval(loader_args)\n            if isinstance(parsed_args, dict):\n                return parsed_args\n\n        except Exception as err:\n            print(\n                \"Error while parsing loader arguments '{}'.\\n{}: {}\\n\\n\"\n                \"Continuing with default arguments. . .\".format(\n                    loader_args, err.__class__.__name__, err))\n\n        return {}\n\n    def _get_representations(self, placeholder):\n        \"\"\"Prepared query of representations based on load options.\n\n        This function is directly connected to options defined in\n        'get_load_plugin_options'.\n\n        Note:\n            This returns all representation documents from all versions of\n                matching product. To filter for last version use\n                '_reduce_last_version_repre_entities'.\n\n        Args:\n            placeholder (PlaceholderItem): Item which should be populated.\n\n        Returns:\n            List[Dict[str, Any]]: Representation documents matching filters\n                from placeholder data.\n        \"\"\"\n\n        # An OpenPype placeholder loaded in AYON\n        if \"asset\" in placeholder.data:\n            return []\n\n        representation_names = None\n        representation_name: str = placeholder.data[\"representation\"]\n        if representation_name:\n            representation_names = [representation_name]\n\n        project_name = self.builder.project_name\n        current_folder_entity = self.builder.current_folder_entity\n\n        folder_path_regex = placeholder.data[\"folder_path\"]\n        product_name_regex_value = placeholder.data[\"product_name\"]\n        product_name_regex = None\n        if product_name_regex_value:\n            product_name_regex = re.compile(product_name_regex_value)\n        product_base_type = placeholder.data.get(\"product_base_type\")\n        if product_base_type is None:\n            product_base_type = placeholder.data.get(\"product_type\")\n            if product_base_type is None:\n                product_base_type = placeholder.data[\"family\"]\n\n        builder_type = placeholder.data[\"builder_type\"]\n        folder_ids = []\n        if builder_type == \"all_folders\":\n            folder_ids = {\n                folder_entity[\"id\"]\n                for folder_entity in get_folders(\n                    project_name,\n                    folder_path_regex=folder_path_regex,\n                    fields={\"id\"}\n                )\n            }\n\n        elif builder_type == \"context_folder\":\n            folder_ids = [current_folder_entity[\"id\"]]\n\n        elif builder_type == \"linked_folders\":\n            # link type from placeholder data or default to \"template\"\n            link_type = placeholder.data.get(\"link_type\", \"template\")\n            # Get all linked folders for the current folder\n            if hasattr(self, \"builder\") and isinstance(\n                    self.builder, AbstractTemplateBuilder):\n                # self.builder: AbstractTemplateBuilder\n                folder_ids = [\n                    linked_folder_entity[\"id\"]\n                    for linked_folder_entity in (\n                        self.builder.get_linked_folder_entities(\n                            link_type=link_type,\n                            folder_path_regex=folder_path_regex\n                        )\n                    )\n                ]\n\n        if not folder_ids:\n            return []\n\n        filter_key = \"product_base_types\"\n        if ayon_api.get_server_version_tuple() &lt; (1, 14, 0):\n            filter_key = \"product_types\"\n        filter_kwargs = {\n            filter_key: {product_base_type},\n        }\n\n        products = list(get_products(\n            project_name,\n            folder_ids=folder_ids,\n            **filter_kwargs,\n            fields={\"id\", \"name\"}\n        ))\n        filtered_product_ids = set()\n        for product in products:\n            if (\n                product_name_regex is None\n                or product_name_regex.match(product[\"name\"])\n            ):\n                filtered_product_ids.add(product[\"id\"])\n\n        if not filtered_product_ids:\n            return []\n\n        version_ids = set(\n            version[\"id\"]\n            for version in get_last_versions(\n                project_name, filtered_product_ids, fields={\"id\"}\n            ).values()\n            # Version may be none if a product has no versions\n            if version is not None\n        )\n        return list(get_representations(\n            project_name,\n            representation_names=representation_names,\n            version_ids=version_ids\n        ))\n\n    def _before_placeholder_load(self, placeholder):\n        \"\"\"Can be overridden. It's called before placeholder representations\n        are loaded.\n        \"\"\"\n\n        pass\n\n    def _before_repre_load(self, placeholder, representation):\n        \"\"\"Can be overridden. It's called before representation is loaded.\"\"\"\n\n        pass\n\n    def _reduce_last_version_repre_entities(self, repre_contexts):\n        \"\"\"Reduce representations to last version.\"\"\"\n\n        version_mapping_by_product_id = {}\n        for repre_context in repre_contexts:\n            product_id = repre_context[\"product\"][\"id\"]\n            version = repre_context[\"version\"][\"version\"]\n            version_mapping = version_mapping_by_product_id.setdefault(\n                product_id, {}\n            )\n            version_mapping.setdefault(version, []).append(repre_context)\n\n        output = []\n        for version_mapping in version_mapping_by_product_id.values():\n            last_version = max(version_mapping.keys())\n            output.extend(version_mapping[last_version])\n        return output\n\n    def populate_load_placeholder(self, placeholder, ignore_repre_ids=None):\n        \"\"\"Load placeholder is going to load matching representations.\n\n        Note:\n            Ignore repre ids is to avoid loading the same representation again\n            on load. But the representation can be loaded with different loader\n            and there could be published new version of matching product for\n            the representation. We should maybe expect containers.\n\n            Also import loaders don't have containers at all...\n\n        Args:\n            placeholder (PlaceholderItem): Placeholder item with information\n                about requested representations.\n            ignore_repre_ids (Iterable[Union[str, ObjectId]]): Representation\n                ids that should be skipped.\n        \"\"\"\n\n        if ignore_repre_ids is None:\n            ignore_repre_ids = set()\n\n        # TODO check loader existence\n        loader_name = placeholder.data[\"loader\"]\n        loader_args = self.parse_loader_args(placeholder.data[\"loader_args\"])\n\n        placeholder_representations = [\n            repre_entity\n            for repre_entity in self._get_representations(placeholder)\n            if repre_entity[\"id\"] not in ignore_repre_ids\n        ]\n\n        repre_load_contexts = get_representation_contexts(\n            self.project_name, placeholder_representations\n        )\n        filtered_repre_contexts = self._reduce_last_version_repre_entities(\n            repre_load_contexts.values()\n        )\n        if not filtered_repre_contexts:\n            self.log.info((\n                \"There's no representation for this placeholder: {}\"\n            ).format(placeholder.scene_identifier))\n            if not placeholder.data.get(\"keep_placeholder\", True):\n                self.delete_placeholder(placeholder)\n            return\n\n        loaders_by_name = self.builder.get_loaders_by_name()\n        self._before_placeholder_load(\n            placeholder\n        )\n\n        failed = False\n        for repre_load_context in filtered_repre_contexts:\n            folder_path = repre_load_context[\"folder\"][\"path\"]\n            product_name = repre_load_context[\"product\"][\"name\"]\n            representation = repre_load_context[\"representation\"]\n            self._before_repre_load(\n                placeholder, representation\n            )\n            self.log.info(\n                \"Loading {} from {} with loader {}\\n\"\n                \"Loader arguments used : {}\".format(\n                    product_name,\n                    folder_path,\n                    loader_name,\n                    placeholder.data[\"loader_args\"],\n                )\n            )\n            try:\n                container = load_with_repre_context(\n                    loaders_by_name[loader_name],\n                    repre_load_context,\n                    options=loader_args\n                )\n\n            except Exception:\n                self.load_failed(placeholder, representation)\n                failed = True\n            else:\n                self.load_succeed(placeholder, container)\n\n        # Run post placeholder process after load of all representations\n        self.post_placeholder_process(placeholder, failed)\n\n        if failed:\n            self.log.debug(\n                \"Placeholder cleanup skipped due to failed placeholder \"\n                \"population.\"\n            )\n            return\n        if not placeholder.data.get(\"keep_placeholder\", True):\n            self.delete_placeholder(placeholder)\n\n    def load_failed(self, placeholder, representation):\n        if hasattr(placeholder, \"load_failed\"):\n            placeholder.load_failed(representation)\n\n    def load_succeed(self, placeholder, container):\n        if hasattr(placeholder, \"load_succeed\"):\n            placeholder.load_succeed(container)\n\n    def post_placeholder_process(self, placeholder, failed):\n        \"\"\"Cleanup placeholder after load of its corresponding representations.\n\n        Args:\n            placeholder (PlaceholderItem): Item which was just used to load\n                representation.\n            failed (bool): Loading of representation failed.\n        \"\"\"\n\n        pass\n\n    def delete_placeholder(self, placeholder):\n        \"\"\"Called when all item population is done.\"\"\"\n        self.log.debug(\"Clean up of placeholder is not implemented.\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.PlaceholderLoadMixin.delete_placeholder","title":"<code>delete_placeholder(placeholder)</code>","text":"<p>Called when all item population is done.</p> Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>def delete_placeholder(self, placeholder):\n    \"\"\"Called when all item population is done.\"\"\"\n    self.log.debug(\"Clean up of placeholder is not implemented.\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.PlaceholderLoadMixin.get_load_plugin_options","title":"<code>get_load_plugin_options(options=None)</code>","text":"<p>Unified attribute definitions for load placeholder.</p> <p>Common function for placeholder plugins used for loading of representations. Use it in 'get_placeholder_options'.</p> <p>Parameters:</p> Name Type Description Default <code>options</code> <code>Dict[str, Any]</code> <p>Already available options which are used as defaults for attributes.</p> <code>None</code> <p>Returns:</p> Type Description <p>List[AbstractAttrDef]: Attribute definitions common for load plugins.</p> Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>def get_load_plugin_options(self, options=None):\n    \"\"\"Unified attribute definitions for load placeholder.\n\n    Common function for placeholder plugins used for loading of\n    representations. Use it in 'get_placeholder_options'.\n\n    Args:\n        options (Dict[str, Any]): Already available options which are used\n            as defaults for attributes.\n\n    Returns:\n        List[AbstractAttrDef]: Attribute definitions common for load\n            plugins.\n    \"\"\"\n\n    loaders_by_name = self.builder.get_loaders_by_name()\n    loader_items = [\n        {\"value\": loader_name, \"label\": loader.label or loader_name}\n        for loader_name, loader in loaders_by_name.items()\n    ]\n\n    loader_items = list(sorted(loader_items, key=lambda i: i[\"label\"]))\n    options = options or {}\n\n    # Get product base types from all loaders excluding \"*\"\n    product_base_types = set()\n    for loader in loaders_by_name.values():\n        l_product_base_types = loader.product_base_types\n        if l_product_base_types is None:\n            l_product_base_types = loader.product_types\n        if l_product_base_types:\n            product_base_types.update(l_product_base_types)\n    product_base_types.discard(\"*\")\n\n    # Sort for readability\n    product_base_types = list(sorted(product_base_types))\n\n    builder_type_enum_items = [\n        {\"label\": \"Current folder\", \"value\": \"context_folder\"},\n        {\"label\": \"Linked folders\", \"value\": \"linked_folders\"},\n        {\"label\": \"All folders\", \"value\": \"all_folders\"},\n    ]\n\n    link_types = ayon_api.get_link_types(self.builder.project_name)\n\n    # Filter link types for folder to folder links\n    link_types_enum_items = [\n        {\"label\": link_type[\"name\"], \"value\": link_type[\"linkType\"]}\n        for link_type in link_types\n        if (\n            link_type[\"inputType\"] == \"folder\"\n            and link_type[\"outputType\"] == \"folder\"\n        )\n    ]\n\n    if not link_types_enum_items:\n        link_types_enum_items.append(\n            {\"label\": \"&lt;No link types&gt;\", \"value\": None}\n        )\n\n    build_type_label = \"Folder Builder Type\"\n    build_type_help = (\n        \"Folder Builder Type\\n\"\n        \"\\nBuilder type describe what template loader will look\"\n        \" for.\"\n        \"\\nCurrent Folder: Template loader will look for products\"\n        \" of current context folder (Folder /assets/bob will\"\n        \" find asset)\"\n        \"\\nAll folders: All folders matching the regex will be\"\n        \" used.\"\n    )\n\n    product_base_type = options.get(\"product_base_type\")\n    if product_base_type is None:\n        product_base_type = options.get(\"product_type\")\n        if product_base_type is None:\n            product_base_type = options.get(\"family\")\n\n    return [\n        attribute_definitions.UISeparatorDef(),\n        attribute_definitions.UILabelDef(\"Main attributes\"),\n        attribute_definitions.UISeparatorDef(),\n\n        attribute_definitions.EnumDef(\n            \"builder_type\",\n            label=build_type_label,\n            default=options.get(\"builder_type\"),\n            items=builder_type_enum_items,\n            tooltip=build_type_help\n        ),\n        attribute_definitions.EnumDef(\n            \"link_type\",\n            label=\"Link Type\",\n            default=options.get(\"link_type\"),\n            items=link_types_enum_items,\n            tooltip=(\n                \"Link Type\\n\"\n                \"\\nDefines what type of link will be used to\"\n                \" link the product to the current folder.\"\n            )\n        ),\n        attribute_definitions.EnumDef(\n            \"product_base_type\",\n            label=\"Product base type\",\n            default=product_base_type,\n            items=product_base_types\n        ),\n        attribute_definitions.TextDef(\n            \"representation\",\n            label=\"Representation name\",\n            default=options.get(\"representation\"),\n            placeholder=\"ma, abc, ...\"\n        ),\n        attribute_definitions.EnumDef(\n            \"loader\",\n            label=\"Loader\",\n            default=options.get(\"loader\"),\n            items=loader_items,\n            tooltip=(\n                \"Loader\"\n                \"\\nDefines what AYON loader will be used to\"\n                \" load assets.\"\n                \"\\nUseable loader depends on current host's loader list.\"\n                \"\\nField is case sensitive.\"\n            )\n        ),\n        attribute_definitions.TextDef(\n            \"loader_args\",\n            label=\"Loader Arguments\",\n            default=options.get(\"loader_args\"),\n            placeholder='{\"camera\":\"persp\", \"lights\":True}',\n            tooltip=(\n                \"Loader\"\n                \"\\nDefines a dictionary of arguments used to load assets.\"\n                \"\\nUseable arguments depend on current placeholder Loader.\"\n                \"\\nField should be a valid python dict.\"\n                \" Anything else will be ignored.\"\n            )\n        ),\n        attribute_definitions.NumberDef(\n            \"order\",\n            label=\"Order\",\n            default=options.get(\"order\") or 0,\n            decimals=0,\n            minimum=0,\n            maximum=999,\n            tooltip=(\n                \"Order\"\n                \"\\nOrder defines asset loading priority (0 to 999)\"\n                \"\\nPriority rule is : \\\"lowest is first to load\\\".\"\n            )\n        ),\n        attribute_definitions.UISeparatorDef(),\n        attribute_definitions.UILabelDef(\"Optional attributes\"),\n        attribute_definitions.UISeparatorDef(),\n        attribute_definitions.TextDef(\n            \"folder_path\",\n            label=\"Folder filter\",\n            default=options.get(\"folder_path\"),\n            placeholder=\"regex filtering by folder path\",\n            tooltip=(\n                \"Filtering assets by matching\"\n                \" field regex to folder path\"\n            )\n        ),\n        attribute_definitions.TextDef(\n            \"product_name\",\n            label=\"Product filter\",\n            default=options.get(\"product_name\"),\n            placeholder=\"regex filtering by product name\",\n            tooltip=(\n                \"Filtering assets by matching\"\n                \" field regex to product name\"\n            )\n        ),\n    ]\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.PlaceholderLoadMixin.parse_loader_args","title":"<code>parse_loader_args(loader_args)</code>","text":"<p>Helper function to parse string of loader arguments.</p> <p>Empty dictionary is returned if conversion fails.</p> <p>Parameters:</p> Name Type Description Default <code>loader_args</code> <code>str</code> <p>Loader args filled by user.</p> required <p>Returns:</p> Type Description <p>Dict[str, Any]: Parsed arguments used as dictionary.</p> Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>def parse_loader_args(self, loader_args):\n    \"\"\"Helper function to parse string of loader arguments.\n\n    Empty dictionary is returned if conversion fails.\n\n    Args:\n        loader_args (str): Loader args filled by user.\n\n    Returns:\n        Dict[str, Any]: Parsed arguments used as dictionary.\n    \"\"\"\n\n    if not loader_args:\n        return {}\n\n    try:\n        parsed_args = eval(loader_args)\n        if isinstance(parsed_args, dict):\n            return parsed_args\n\n    except Exception as err:\n        print(\n            \"Error while parsing loader arguments '{}'.\\n{}: {}\\n\\n\"\n            \"Continuing with default arguments. . .\".format(\n                loader_args, err.__class__.__name__, err))\n\n    return {}\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.PlaceholderLoadMixin.populate_load_placeholder","title":"<code>populate_load_placeholder(placeholder, ignore_repre_ids=None)</code>","text":"<p>Load placeholder is going to load matching representations.</p> Note <p>Ignore repre ids is to avoid loading the same representation again on load. But the representation can be loaded with different loader and there could be published new version of matching product for the representation. We should maybe expect containers.</p> <p>Also import loaders don't have containers at all...</p> <p>Parameters:</p> Name Type Description Default <code>placeholder</code> <code>PlaceholderItem</code> <p>Placeholder item with information about requested representations.</p> required <code>ignore_repre_ids</code> <code>Iterable[Union[str, ObjectId]]</code> <p>Representation ids that should be skipped.</p> <code>None</code> Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>def populate_load_placeholder(self, placeholder, ignore_repre_ids=None):\n    \"\"\"Load placeholder is going to load matching representations.\n\n    Note:\n        Ignore repre ids is to avoid loading the same representation again\n        on load. But the representation can be loaded with different loader\n        and there could be published new version of matching product for\n        the representation. We should maybe expect containers.\n\n        Also import loaders don't have containers at all...\n\n    Args:\n        placeholder (PlaceholderItem): Placeholder item with information\n            about requested representations.\n        ignore_repre_ids (Iterable[Union[str, ObjectId]]): Representation\n            ids that should be skipped.\n    \"\"\"\n\n    if ignore_repre_ids is None:\n        ignore_repre_ids = set()\n\n    # TODO check loader existence\n    loader_name = placeholder.data[\"loader\"]\n    loader_args = self.parse_loader_args(placeholder.data[\"loader_args\"])\n\n    placeholder_representations = [\n        repre_entity\n        for repre_entity in self._get_representations(placeholder)\n        if repre_entity[\"id\"] not in ignore_repre_ids\n    ]\n\n    repre_load_contexts = get_representation_contexts(\n        self.project_name, placeholder_representations\n    )\n    filtered_repre_contexts = self._reduce_last_version_repre_entities(\n        repre_load_contexts.values()\n    )\n    if not filtered_repre_contexts:\n        self.log.info((\n            \"There's no representation for this placeholder: {}\"\n        ).format(placeholder.scene_identifier))\n        if not placeholder.data.get(\"keep_placeholder\", True):\n            self.delete_placeholder(placeholder)\n        return\n\n    loaders_by_name = self.builder.get_loaders_by_name()\n    self._before_placeholder_load(\n        placeholder\n    )\n\n    failed = False\n    for repre_load_context in filtered_repre_contexts:\n        folder_path = repre_load_context[\"folder\"][\"path\"]\n        product_name = repre_load_context[\"product\"][\"name\"]\n        representation = repre_load_context[\"representation\"]\n        self._before_repre_load(\n            placeholder, representation\n        )\n        self.log.info(\n            \"Loading {} from {} with loader {}\\n\"\n            \"Loader arguments used : {}\".format(\n                product_name,\n                folder_path,\n                loader_name,\n                placeholder.data[\"loader_args\"],\n            )\n        )\n        try:\n            container = load_with_repre_context(\n                loaders_by_name[loader_name],\n                repre_load_context,\n                options=loader_args\n            )\n\n        except Exception:\n            self.load_failed(placeholder, representation)\n            failed = True\n        else:\n            self.load_succeed(placeholder, container)\n\n    # Run post placeholder process after load of all representations\n    self.post_placeholder_process(placeholder, failed)\n\n    if failed:\n        self.log.debug(\n            \"Placeholder cleanup skipped due to failed placeholder \"\n            \"population.\"\n        )\n        return\n    if not placeholder.data.get(\"keep_placeholder\", True):\n        self.delete_placeholder(placeholder)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.PlaceholderLoadMixin.post_placeholder_process","title":"<code>post_placeholder_process(placeholder, failed)</code>","text":"<p>Cleanup placeholder after load of its corresponding representations.</p> <p>Parameters:</p> Name Type Description Default <code>placeholder</code> <code>PlaceholderItem</code> <p>Item which was just used to load representation.</p> required <code>failed</code> <code>bool</code> <p>Loading of representation failed.</p> required Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>def post_placeholder_process(self, placeholder, failed):\n    \"\"\"Cleanup placeholder after load of its corresponding representations.\n\n    Args:\n        placeholder (PlaceholderItem): Item which was just used to load\n            representation.\n        failed (bool): Loading of representation failed.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.PlaceholderPlugin","title":"<code>PlaceholderPlugin</code>","text":"<p>               Bases: <code>ABC</code></p> <p>Plugin which care about handling of placeholder items logic.</p> <p>Plugin create and update placeholders in scene and populate them on template import. Populating means that based on placeholder data happens a logic in the scene. Most common logic is to load representation using loaders or to create instances in scene.</p> Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>class PlaceholderPlugin(ABC):\n    \"\"\"Plugin which care about handling of placeholder items logic.\n\n    Plugin create and update placeholders in scene and populate them on\n    template import. Populating means that based on placeholder data happens\n    a logic in the scene. Most common logic is to load representation using\n    loaders or to create instances in scene.\n    \"\"\"\n\n    label = None\n    _log = None\n\n    def __init__(self, builder):\n        self._builder = builder\n\n    @property\n    def builder(self):\n        \"\"\"Access to builder which initialized the plugin.\n\n        Returns:\n            AbstractTemplateBuilder: Loader of template build.\n        \"\"\"\n\n        return self._builder\n\n    @property\n    def project_name(self):\n        return self._builder.project_name\n\n    @property\n    def log(self):\n        \"\"\"Dynamically created logger for the plugin.\"\"\"\n\n        if self._log is None:\n            self._log = Logger.get_logger(repr(self))\n        return self._log\n\n    @property\n    def identifier(self):\n        \"\"\"Identifier which will be stored to placeholder.\n\n        Default implementation uses class name.\n\n        Returns:\n            str: Unique identifier of placeholder plugin.\n        \"\"\"\n\n        return self.__class__.__name__\n\n    @abstractmethod\n    def create_placeholder(self, placeholder_data):\n        \"\"\"Create new placeholder in scene and get it's item.\n\n        It matters on the plugin implementation if placeholder will use\n        selection in scene or create new node.\n\n        Args:\n            placeholder_data (Dict[str, Any]): Data that were created\n                based on attribute definitions from 'get_placeholder_options'.\n\n        Returns:\n            PlaceholderItem: Created placeholder item.\n        \"\"\"\n\n        pass\n\n    @abstractmethod\n    def update_placeholder(self, placeholder_item, placeholder_data):\n        \"\"\"Update placeholder item with new data.\n\n        New data should be propagated to object of placeholder item itself\n        and also into the scene.\n\n        Reason:\n            Some placeholder plugins may require some special way how the\n            updates should be propagated to object.\n\n        Args:\n            placeholder_item (PlaceholderItem): Object of placeholder that\n                should be updated.\n            placeholder_data (Dict[str, Any]): Data related to placeholder.\n                Should match plugin options.\n        \"\"\"\n\n        pass\n\n    @abstractmethod\n    def collect_placeholders(self):\n        \"\"\"Collect placeholders from scene.\n\n        Returns:\n            List[PlaceholderItem]: Placeholder objects.\n        \"\"\"\n\n        pass\n\n    def get_placeholder_options(self, options=None):\n        \"\"\"Placeholder options for data showed.\n\n        Returns:\n            List[AbstractAttrDef]: Attribute definitions of\n                placeholder options.\n        \"\"\"\n\n        return []\n\n    def get_placeholder_keys(self):\n        \"\"\"Get placeholder keys that are stored in scene.\n\n        Returns:\n            Set[str]: Key of placeholder keys that are stored in scene.\n        \"\"\"\n\n        option_keys = get_attributes_keys(self.get_placeholder_options())\n        option_keys.add(\"plugin_identifier\")\n        return option_keys\n\n    def prepare_placeholders(self, placeholders):\n        \"\"\"Preparation part of placeholders.\n\n        Args:\n            placeholders (List[PlaceholderItem]): List of placeholders that\n                will be processed.\n        \"\"\"\n\n        pass\n\n    @abstractmethod\n    def populate_placeholder(self, placeholder):\n        \"\"\"Process single placeholder item.\n\n        Processing of placeholders is defined by their order thus can't be\n        processed in batch.\n\n        Args:\n            placeholder (PlaceholderItem): Placeholder that should be\n                processed.\n        \"\"\"\n\n        pass\n\n    def repopulate_placeholder(self, placeholder):\n        \"\"\"Update scene with current context for passed placeholder.\n\n        Can be used to re-run placeholder logic (if it make sense).\n        \"\"\"\n\n        pass\n\n    def get_plugin_shared_data(self, key):\n        \"\"\"Receive shared data across plugin and placeholders.\n\n        Using shared data from builder but stored under plugin identifier.\n\n        Args:\n            key (str): Key under which are shared data stored.\n\n        Returns:\n            Union[None, Any]: None if key was not set.\n        \"\"\"\n\n        plugin_data = self.builder.get_shared_data(self.identifier)\n        if plugin_data is None:\n            return None\n        return plugin_data.get(key)\n\n    def set_plugin_shared_data(self, key, value):\n        \"\"\"Store share data across plugin and placeholders.\n\n        Using shared data from builder but stored under plugin identifier.\n\n        Key should be self-explanatory to content.\n        - wrong: 'folder'\n        - good: 'folder_path'\n\n        Args:\n            key (str): Key under which is key stored.\n            value (Any): Value that should be stored under the key.\n        \"\"\"\n\n        plugin_data = self.builder.get_shared_data(self.identifier)\n        if plugin_data is None:\n            plugin_data = {}\n        plugin_data[key] = value\n        self.builder.set_shared_data(self.identifier, plugin_data)\n\n    def get_plugin_shared_populate_data(self, key):\n        \"\"\"Receive shared data across plugin and placeholders.\n\n        Using shared populate data from builder but stored under plugin\n        identifier.\n\n        Shared populate data are cleaned up during populate while loop.\n\n        Args:\n            key (str): Key under which are shared data stored.\n\n        Returns:\n            Union[None, Any]: None if key was not set.\n        \"\"\"\n\n        plugin_data = self.builder.get_shared_populate_data(self.identifier)\n        if plugin_data is None:\n            return None\n        return plugin_data.get(key)\n\n    def set_plugin_shared_populate_data(self, key, value):\n        \"\"\"Store share data across plugin and placeholders.\n\n        Using shared data from builder but stored under plugin identifier.\n\n        Key should be self-explanatory to content.\n        - wrong: 'folder'\n        - good: 'folder_path'\n\n        Shared populate data are cleaned up during populate while loop.\n\n        Args:\n            key (str): Key under which is key stored.\n            value (Any): Value that should be stored under the key.\n        \"\"\"\n\n        plugin_data = self.builder.get_shared_populate_data(self.identifier)\n        if plugin_data is None:\n            plugin_data = {}\n        plugin_data[key] = value\n        self.builder.set_shared_populate_data(self.identifier, plugin_data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.PlaceholderPlugin.builder","title":"<code>builder</code>  <code>property</code>","text":"<p>Access to builder which initialized the plugin.</p> <p>Returns:</p> Name Type Description <code>AbstractTemplateBuilder</code> <p>Loader of template build.</p>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.PlaceholderPlugin.identifier","title":"<code>identifier</code>  <code>property</code>","text":"<p>Identifier which will be stored to placeholder.</p> <p>Default implementation uses class name.</p> <p>Returns:</p> Name Type Description <code>str</code> <p>Unique identifier of placeholder plugin.</p>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.PlaceholderPlugin.log","title":"<code>log</code>  <code>property</code>","text":"<p>Dynamically created logger for the plugin.</p>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.PlaceholderPlugin.collect_placeholders","title":"<code>collect_placeholders()</code>  <code>abstractmethod</code>","text":"<p>Collect placeholders from scene.</p> <p>Returns:</p> Type Description <p>List[PlaceholderItem]: Placeholder objects.</p> Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>@abstractmethod\ndef collect_placeholders(self):\n    \"\"\"Collect placeholders from scene.\n\n    Returns:\n        List[PlaceholderItem]: Placeholder objects.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.PlaceholderPlugin.create_placeholder","title":"<code>create_placeholder(placeholder_data)</code>  <code>abstractmethod</code>","text":"<p>Create new placeholder in scene and get it's item.</p> <p>It matters on the plugin implementation if placeholder will use selection in scene or create new node.</p> <p>Parameters:</p> Name Type Description Default <code>placeholder_data</code> <code>Dict[str, Any]</code> <p>Data that were created based on attribute definitions from 'get_placeholder_options'.</p> required <p>Returns:</p> Name Type Description <code>PlaceholderItem</code> <p>Created placeholder item.</p> Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>@abstractmethod\ndef create_placeholder(self, placeholder_data):\n    \"\"\"Create new placeholder in scene and get it's item.\n\n    It matters on the plugin implementation if placeholder will use\n    selection in scene or create new node.\n\n    Args:\n        placeholder_data (Dict[str, Any]): Data that were created\n            based on attribute definitions from 'get_placeholder_options'.\n\n    Returns:\n        PlaceholderItem: Created placeholder item.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.PlaceholderPlugin.get_placeholder_keys","title":"<code>get_placeholder_keys()</code>","text":"<p>Get placeholder keys that are stored in scene.</p> <p>Returns:</p> Type Description <p>Set[str]: Key of placeholder keys that are stored in scene.</p> Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>def get_placeholder_keys(self):\n    \"\"\"Get placeholder keys that are stored in scene.\n\n    Returns:\n        Set[str]: Key of placeholder keys that are stored in scene.\n    \"\"\"\n\n    option_keys = get_attributes_keys(self.get_placeholder_options())\n    option_keys.add(\"plugin_identifier\")\n    return option_keys\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.PlaceholderPlugin.get_placeholder_options","title":"<code>get_placeholder_options(options=None)</code>","text":"<p>Placeholder options for data showed.</p> <p>Returns:</p> Type Description <p>List[AbstractAttrDef]: Attribute definitions of placeholder options.</p> Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>def get_placeholder_options(self, options=None):\n    \"\"\"Placeholder options for data showed.\n\n    Returns:\n        List[AbstractAttrDef]: Attribute definitions of\n            placeholder options.\n    \"\"\"\n\n    return []\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.PlaceholderPlugin.get_plugin_shared_data","title":"<code>get_plugin_shared_data(key)</code>","text":"<p>Receive shared data across plugin and placeholders.</p> <p>Using shared data from builder but stored under plugin identifier.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>Key under which are shared data stored.</p> required <p>Returns:</p> Type Description <p>Union[None, Any]: None if key was not set.</p> Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>def get_plugin_shared_data(self, key):\n    \"\"\"Receive shared data across plugin and placeholders.\n\n    Using shared data from builder but stored under plugin identifier.\n\n    Args:\n        key (str): Key under which are shared data stored.\n\n    Returns:\n        Union[None, Any]: None if key was not set.\n    \"\"\"\n\n    plugin_data = self.builder.get_shared_data(self.identifier)\n    if plugin_data is None:\n        return None\n    return plugin_data.get(key)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.PlaceholderPlugin.get_plugin_shared_populate_data","title":"<code>get_plugin_shared_populate_data(key)</code>","text":"<p>Receive shared data across plugin and placeholders.</p> <p>Using shared populate data from builder but stored under plugin identifier.</p> <p>Shared populate data are cleaned up during populate while loop.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>Key under which are shared data stored.</p> required <p>Returns:</p> Type Description <p>Union[None, Any]: None if key was not set.</p> Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>def get_plugin_shared_populate_data(self, key):\n    \"\"\"Receive shared data across plugin and placeholders.\n\n    Using shared populate data from builder but stored under plugin\n    identifier.\n\n    Shared populate data are cleaned up during populate while loop.\n\n    Args:\n        key (str): Key under which are shared data stored.\n\n    Returns:\n        Union[None, Any]: None if key was not set.\n    \"\"\"\n\n    plugin_data = self.builder.get_shared_populate_data(self.identifier)\n    if plugin_data is None:\n        return None\n    return plugin_data.get(key)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.PlaceholderPlugin.populate_placeholder","title":"<code>populate_placeholder(placeholder)</code>  <code>abstractmethod</code>","text":"<p>Process single placeholder item.</p> <p>Processing of placeholders is defined by their order thus can't be processed in batch.</p> <p>Parameters:</p> Name Type Description Default <code>placeholder</code> <code>PlaceholderItem</code> <p>Placeholder that should be processed.</p> required Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>@abstractmethod\ndef populate_placeholder(self, placeholder):\n    \"\"\"Process single placeholder item.\n\n    Processing of placeholders is defined by their order thus can't be\n    processed in batch.\n\n    Args:\n        placeholder (PlaceholderItem): Placeholder that should be\n            processed.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.PlaceholderPlugin.prepare_placeholders","title":"<code>prepare_placeholders(placeholders)</code>","text":"<p>Preparation part of placeholders.</p> <p>Parameters:</p> Name Type Description Default <code>placeholders</code> <code>List[PlaceholderItem]</code> <p>List of placeholders that will be processed.</p> required Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>def prepare_placeholders(self, placeholders):\n    \"\"\"Preparation part of placeholders.\n\n    Args:\n        placeholders (List[PlaceholderItem]): List of placeholders that\n            will be processed.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.PlaceholderPlugin.repopulate_placeholder","title":"<code>repopulate_placeholder(placeholder)</code>","text":"<p>Update scene with current context for passed placeholder.</p> <p>Can be used to re-run placeholder logic (if it make sense).</p> Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>def repopulate_placeholder(self, placeholder):\n    \"\"\"Update scene with current context for passed placeholder.\n\n    Can be used to re-run placeholder logic (if it make sense).\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.PlaceholderPlugin.set_plugin_shared_data","title":"<code>set_plugin_shared_data(key, value)</code>","text":"<p>Store share data across plugin and placeholders.</p> <p>Using shared data from builder but stored under plugin identifier.</p> <p>Key should be self-explanatory to content. - wrong: 'folder' - good: 'folder_path'</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>Key under which is key stored.</p> required <code>value</code> <code>Any</code> <p>Value that should be stored under the key.</p> required Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>def set_plugin_shared_data(self, key, value):\n    \"\"\"Store share data across plugin and placeholders.\n\n    Using shared data from builder but stored under plugin identifier.\n\n    Key should be self-explanatory to content.\n    - wrong: 'folder'\n    - good: 'folder_path'\n\n    Args:\n        key (str): Key under which is key stored.\n        value (Any): Value that should be stored under the key.\n    \"\"\"\n\n    plugin_data = self.builder.get_shared_data(self.identifier)\n    if plugin_data is None:\n        plugin_data = {}\n    plugin_data[key] = value\n    self.builder.set_shared_data(self.identifier, plugin_data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.PlaceholderPlugin.set_plugin_shared_populate_data","title":"<code>set_plugin_shared_populate_data(key, value)</code>","text":"<p>Store share data across plugin and placeholders.</p> <p>Using shared data from builder but stored under plugin identifier.</p> <p>Key should be self-explanatory to content. - wrong: 'folder' - good: 'folder_path'</p> <p>Shared populate data are cleaned up during populate while loop.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>Key under which is key stored.</p> required <code>value</code> <code>Any</code> <p>Value that should be stored under the key.</p> required Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>def set_plugin_shared_populate_data(self, key, value):\n    \"\"\"Store share data across plugin and placeholders.\n\n    Using shared data from builder but stored under plugin identifier.\n\n    Key should be self-explanatory to content.\n    - wrong: 'folder'\n    - good: 'folder_path'\n\n    Shared populate data are cleaned up during populate while loop.\n\n    Args:\n        key (str): Key under which is key stored.\n        value (Any): Value that should be stored under the key.\n    \"\"\"\n\n    plugin_data = self.builder.get_shared_populate_data(self.identifier)\n    if plugin_data is None:\n        plugin_data = {}\n    plugin_data[key] = value\n    self.builder.set_shared_populate_data(self.identifier, plugin_data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.PlaceholderPlugin.update_placeholder","title":"<code>update_placeholder(placeholder_item, placeholder_data)</code>  <code>abstractmethod</code>","text":"<p>Update placeholder item with new data.</p> <p>New data should be propagated to object of placeholder item itself and also into the scene.</p> Reason <p>Some placeholder plugins may require some special way how the updates should be propagated to object.</p> <p>Parameters:</p> Name Type Description Default <code>placeholder_item</code> <code>PlaceholderItem</code> <p>Object of placeholder that should be updated.</p> required <code>placeholder_data</code> <code>Dict[str, Any]</code> <p>Data related to placeholder. Should match plugin options.</p> required Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>@abstractmethod\ndef update_placeholder(self, placeholder_item, placeholder_data):\n    \"\"\"Update placeholder item with new data.\n\n    New data should be propagated to object of placeholder item itself\n    and also into the scene.\n\n    Reason:\n        Some placeholder plugins may require some special way how the\n        updates should be propagated to object.\n\n    Args:\n        placeholder_item (PlaceholderItem): Object of placeholder that\n            should be updated.\n        placeholder_data (Dict[str, Any]): Data related to placeholder.\n            Should match plugin options.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.TemplateAlreadyImported","title":"<code>TemplateAlreadyImported</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Error raised when Template was already imported by host for this session</p> Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>class TemplateAlreadyImported(Exception):\n    \"\"\"Error raised when Template was already imported by host for\n    this session\"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.TemplateLoadFailed","title":"<code>TemplateLoadFailed</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Error raised whend Template loader was unable to load the template</p> Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>class TemplateLoadFailed(Exception):\n    \"\"\"Error raised whend Template loader was unable to load the template\"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.TemplateNotFound","title":"<code>TemplateNotFound</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Exception raised when template does not exist.</p> Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>class TemplateNotFound(Exception):\n    \"\"\"Exception raised when template does not exist.\"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.TemplatePreset","title":"<code>TemplatePreset</code>  <code>dataclass</code>","text":"<p>Dataclass for template preset information.</p> <p>Attributes:</p> Name Type Description <code>path</code> <code>str</code> <p>Resolved template filesystem path.</p> <code>keep_placeholder</code> <code>bool</code> <p>Whether placeholders should remain after processing.</p> <code>create_first_version</code> <code>bool</code> <p>Whether first workfile version should be created in workfile-creation flow.</p> <code>execute_on_app_launch</code> <code>bool</code> <p>Whether to execute template build on application launch.</p> <code>execute_on_new_file</code> <code>bool</code> <p>Whether to execute template build on new file creation.</p> <code>profile</code> <code>dict</code> <p>Defensive copy of the matched profile from project settings.</p> Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>@dataclass\nclass TemplatePreset:\n    \"\"\"Dataclass for template preset information.\n\n    Attributes:\n        path (str): Resolved template filesystem path.\n        keep_placeholder (bool): Whether placeholders should remain after\n            processing.\n        create_first_version (bool): Whether first workfile version should\n            be created in workfile-creation flow.\n        execute_on_app_launch (bool): Whether to execute template build on\n            application launch.\n        execute_on_new_file (bool): Whether to execute template build on\n            new file creation.\n        profile (dict): Defensive copy of the matched profile from project\n            settings.\n    \"\"\"\n    path: str | None = None\n    keep_placeholder: bool = False\n    create_first_version: bool = True\n    execute_on_app_launch: bool = False\n    execute_on_new_file: bool = False\n    # For integration internal logic\n    profile: dict[str, Any] | None = None\n\n    def has_valid_path(self) -&gt; bool:\n        \"\"\"Check if the template preset has valid path.\n\n        Returns:\n            bool: Preset has set path to an existing file.\n\n        \"\"\"\n        return self.path and os.path.exists(self.path)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.TemplatePreset.has_valid_path","title":"<code>has_valid_path()</code>","text":"<p>Check if the template preset has valid path.</p> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>Preset has set path to an existing file.</p> Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>def has_valid_path(self) -&gt; bool:\n    \"\"\"Check if the template preset has valid path.\n\n    Returns:\n        bool: Preset has set path to an existing file.\n\n    \"\"\"\n    return self.path and os.path.exists(self.path)\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.TemplateProfileNotFound","title":"<code>TemplateProfileNotFound</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Exception raised when current profile doesn't match any template profile</p> Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>class TemplateProfileNotFound(Exception):\n    \"\"\"Exception raised when current profile\n    doesn't match any template profile\"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/pipeline/workfile/workfile_template_builder.html#client.ayon_core.pipeline.workfile.workfile_template_builder.resolve_entity_uri","title":"<code>resolve_entity_uri(entity_uri)</code>","text":"<p>Resolve AYON entity URI to a filesystem path for local system.</p> Source code in <code>client/ayon_core/pipeline/workfile/workfile_template_builder.py</code> <pre><code>def resolve_entity_uri(entity_uri: str) -&gt; str:\n    \"\"\"Resolve AYON entity URI to a filesystem path for local system.\"\"\"\n    response = ayon_api.post(\n        \"resolve\",\n        resolveRoots=True,\n        uris=[entity_uri]\n    )\n    if response.status_code != 200:\n        raise RuntimeError(\n            f\"Unable to resolve AYON entity URI filepath for \"\n            f\"'{entity_uri}': {response.text}\"\n        )\n\n    entities = response.data[0][\"entities\"]\n    if len(entities) != 1:\n        raise EntityResolutionError(\n            f\"Unable to resolve AYON entity URI '{entity_uri}' to a \"\n            f\"single filepath. Received data: {response.data}\"\n        )\n    return entities[0][\"filePath\"]\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/index.html","title":"plugins","text":""},{"location":"autoapi/client/ayon_core/plugins/actions/index.html","title":"actions","text":""},{"location":"autoapi/client/ayon_core/plugins/actions/open_file_explorer.html","title":"open_file_explorer","text":""},{"location":"autoapi/client/ayon_core/plugins/actions/open_file_explorer.html#client.ayon_core.plugins.actions.open_file_explorer.OpenTaskPath","title":"<code>OpenTaskPath</code>","text":"<p>               Bases: <code>LauncherAction</code></p> Source code in <code>client/ayon_core/plugins/actions/open_file_explorer.py</code> <pre><code>class OpenTaskPath(LauncherAction):\n    name = \"open_task_path\"\n    label = \"Explore here\"\n    icon = \"folder-open\"\n    order = 500\n\n    def is_compatible(self, selection):\n        \"\"\"Return whether the action is compatible with the session\"\"\"\n        return selection.is_folder_selected\n\n    def process(self, selection, **kwargs):\n        from qtpy import QtCore, QtWidgets\n\n        path = self._get_workdir(selection)\n        if not path:\n            return\n\n        app = QtWidgets.QApplication.instance()\n        ctrl_pressed = QtCore.Qt.ControlModifier &amp; app.keyboardModifiers()\n        if ctrl_pressed:\n            # Copy path to clipboard\n            self.copy_path_to_clipboard(path)\n        else:\n            self.open_in_explorer(path)\n\n    def _find_first_filled_path(self, path):\n        if not path:\n            return \"\"\n\n        fields = set()\n        for item in Formatter().parse(path):\n            _, field_name, format_spec, conversion = item\n            if not field_name:\n                continue\n            conversion = \"!{}\".format(conversion) if conversion else \"\"\n            format_spec = \":{}\".format(format_spec) if format_spec else \"\"\n            orig_key = \"{{{}{}{}}}\".format(\n                field_name, conversion, format_spec)\n            fields.add(orig_key)\n\n        for field in fields:\n            path = path.split(field, 1)[0]\n        return path\n\n    def _get_workdir(self, selection):\n        data = get_template_data(\n            selection.project_entity,\n            selection.folder_entity,\n            selection.task_entity\n        )\n\n        anatomy = Anatomy(\n            selection.project_name,\n            project_entity=selection.project_entity\n        )\n        workdir = anatomy.get_template_item(\n            \"work\", \"default\", \"folder\"\n        ).format(data)\n\n        # Remove any potential un-formatted parts of the path\n        valid_workdir = self._find_first_filled_path(workdir)\n\n        # Path is not filled at all\n        if not valid_workdir:\n            raise AssertionError(\"Failed to calculate workdir.\")\n\n        # Normalize\n        valid_workdir = os.path.normpath(valid_workdir)\n        if os.path.exists(valid_workdir):\n            return valid_workdir\n\n        data.pop(\"task\", None)\n        workdir = anatomy.get_template_item(\n            \"work\", \"default\", \"folder\"\n        ).format(data)\n        valid_workdir = self._find_first_filled_path(workdir)\n        if valid_workdir:\n            # Normalize\n            valid_workdir = os.path.normpath(valid_workdir)\n            if os.path.exists(valid_workdir):\n                return valid_workdir\n        raise AssertionError(\"Folder does not exist yet.\")\n\n    @staticmethod\n    def open_in_explorer(path):\n        platform_name = platform.system().lower()\n        if platform_name == \"windows\":\n            args = [\"start\", path]\n        elif platform_name == \"darwin\":\n            args = [\"open\", \"-R\", path]\n        elif platform_name == \"linux\":\n            args = [\"xdg-open\", path]\n        else:\n            raise RuntimeError(f\"Unknown platform {platform.system()}\")\n        # Make sure path is converted correctly for 'os.system'\n        os.system(subprocess.list2cmdline(args))\n\n    @staticmethod\n    def copy_path_to_clipboard(path):\n        from qtpy import QtWidgets\n\n        path = path.replace(\"\\\\\", \"/\")\n        print(f\"Copied to clipboard: {path}\")\n        app = QtWidgets.QApplication.instance()\n        assert app, \"Must have running QApplication instance\"\n\n        # Set to Clipboard\n        clipboard = QtWidgets.QApplication.clipboard()\n        clipboard.setText(os.path.normpath(path))\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/actions/open_file_explorer.html#client.ayon_core.plugins.actions.open_file_explorer.OpenTaskPath.is_compatible","title":"<code>is_compatible(selection)</code>","text":"<p>Return whether the action is compatible with the session</p> Source code in <code>client/ayon_core/plugins/actions/open_file_explorer.py</code> <pre><code>def is_compatible(self, selection):\n    \"\"\"Return whether the action is compatible with the session\"\"\"\n    return selection.is_folder_selected\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/actions/show_in_ayon.html","title":"show_in_ayon","text":""},{"location":"autoapi/client/ayon_core/plugins/actions/show_in_ayon.html#client.ayon_core.plugins.actions.show_in_ayon.ShowInAYON","title":"<code>ShowInAYON</code>","text":"<p>               Bases: <code>LauncherAction</code></p> <p>Open AYON browser page to the current context.</p> Source code in <code>client/ayon_core/plugins/actions/show_in_ayon.py</code> <pre><code>class ShowInAYON(LauncherAction):\n    \"\"\"Open AYON browser page to the current context.\"\"\"\n    name = \"showinayon\"\n    label = \"Show in AYON\"\n    icon = get_ayon_icon_filepath()\n    order = 999\n\n    def process(self, selection, **kwargs):\n        url = os.environ[\"AYON_SERVER_URL\"]\n        if selection.is_project_selected:\n            project_name = selection.project_name\n            url += f\"/projects/{project_name}/overview\"\n\n            query = {\n                \"project\": project_name\n            }\n            if selection.is_task_selected:\n                query[\"type\"] = \"task\"\n                query[\"id\"] = selection.get_task_entity()[\"id\"]\n            elif selection.is_folder_selected:\n                query[\"type\"] = \"folder\"\n                query[\"id\"] = selection.get_folder_entity()[\"id\"]\n\n            url += f\"?{urllib.parse.urlencode(query)}\"\n\n        # Open URL in webbrowser\n        self.log.info(f\"Opening URL: {url}\")\n        webbrowser.open_new_tab(url)\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/inventory/index.html","title":"inventory","text":""},{"location":"autoapi/client/ayon_core/plugins/inventory/remove_and_load.html","title":"remove_and_load","text":""},{"location":"autoapi/client/ayon_core/plugins/inventory/remove_and_load.html#client.ayon_core.plugins.inventory.remove_and_load.RemoveAndLoad","title":"<code>RemoveAndLoad</code>","text":"<p>               Bases: <code>InventoryAction</code></p> <p>Delete inventory item and reload it.</p> Source code in <code>client/ayon_core/plugins/inventory/remove_and_load.py</code> <pre><code>class RemoveAndLoad(InventoryAction):\n    \"\"\"Delete inventory item and reload it.\"\"\"\n\n    label = \"Remove and load\"\n    icon = \"refresh\"\n\n    def process(self, containers):\n        project_name = get_current_project_name()\n        loaders_by_name = {\n            get_loader_identifier(plugin): plugin\n            for plugin in discover_loader_plugins(project_name=project_name)\n        }\n        repre_ids = set()\n        for container in containers:\n            # Get loader\n            loader_name = container[\"loader\"]\n            loader = loaders_by_name.get(loader_name, None)\n            if not loader:\n                raise RuntimeError(\n                    \"Failed to get loader '{}', can't remove \"\n                    \"and load container\".format(loader_name)\n                )\n            repre_ids.add(container[\"representation\"])\n\n        repre_entities_by_id = {\n            repre_entity[\"id\"]: repre_entity\n            for repre_entity in ayon_api.get_representations(\n                project_name, representation_ids=repre_ids\n            )\n        }\n        for container in containers:\n            # Get representation\n            repre_id = container[\"representation\"]\n            repre_entity = repre_entities_by_id.get(repre_id)\n            if not repre_entity:\n                self.log.warning(\n                    \"Skipping remove and load because representation id is not\"\n                    \" found in database: '{}'\".format(\n                        repre_id\n                    )\n                )\n                continue\n\n            # Remove container\n            remove_container(container)\n\n            # Load container\n            load_container(loader, repre_entity)\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/loader/index.html","title":"loader","text":""},{"location":"autoapi/client/ayon_core/plugins/loader/copy_file.html","title":"copy_file","text":""},{"location":"autoapi/client/ayon_core/plugins/loader/copy_file.html#client.ayon_core.plugins.loader.copy_file.CopyFileActionPlugin","title":"<code>CopyFileActionPlugin</code>","text":"<p>               Bases: <code>LoaderActionPlugin</code></p> <p>Copy published file path to clipboard</p> Source code in <code>client/ayon_core/plugins/loader/copy_file.py</code> <pre><code>class CopyFileActionPlugin(LoaderActionPlugin):\n    \"\"\"Copy published file path to clipboard\"\"\"\n    identifier = \"core.copy-action\"\n\n    def get_action_items(\n        self, selection: LoaderActionSelection\n    ) -&gt; list[LoaderActionItem]:\n        repres = []\n        if selection.selected_type == \"representation\":\n            repres = selection.entities.get_representations(\n                selection.selected_ids\n            )\n\n        if selection.selected_type == \"version\":\n            repres = selection.entities.get_versions_representations(\n                selection.selected_ids\n            )\n\n        output = []\n        if not repres:\n            return output\n\n        repre_ids_by_name = collections.defaultdict(set)\n        for repre in repres:\n            repre_ids_by_name[repre[\"name\"]].add(repre[\"id\"])\n\n        for repre_name, repre_ids in repre_ids_by_name.items():\n            repre_id = next(iter(repre_ids), None)\n            if not repre_id:\n                continue\n            output.append(\n                LoaderActionItem(\n                    label=repre_name,\n                    order=32,\n                    group_label=\"Copy file path\",\n                    data={\n                        \"representation_id\": repre_id,\n                        \"action\": \"copy-path\",\n                    },\n                    icon=MaterialSymbolsIcon(\n                        \"content_copy\",\n                        color=\"#999999\",\n                    )\n                )\n            )\n            output.append(\n                LoaderActionItem(\n                    label=repre_name,\n                    order=33,\n                    group_label=\"Copy file\",\n                    data={\n                        \"representation_id\": repre_id,\n                        \"action\": \"copy-file\",\n                    },\n                    icon=MaterialSymbolsIcon(\n                        \"file_copy\",\n                        color=\"#999999\",\n                    )\n                )\n            )\n        return output\n\n    def execute_action(\n        self,\n        selection: LoaderActionSelection,\n        data: dict,\n        form_values: dict[str, Any],\n    ) -&gt; Optional[LoaderActionResult]:\n        from qtpy import QtWidgets, QtCore\n\n        action = data[\"action\"]\n        repre_id = data[\"representation_id\"]\n        repre = next(iter(selection.entities.get_representations({repre_id})))\n        path = get_representation_path_with_anatomy(\n            repre, selection.get_project_anatomy()\n        )\n        self.log.info(f\"Added file path to clipboard: {path}\")\n\n        clipboard = QtWidgets.QApplication.clipboard()\n        if not clipboard:\n            return LoaderActionResult(\n                \"Failed to copy file path to clipboard.\",\n                success=False,\n            )\n\n        if action == \"copy-path\":\n            # Set to Clipboard\n            clipboard.setText(os.path.normpath(path))\n\n            return LoaderActionResult(\n                \"Path stored to clipboard...\",\n                success=True,\n            )\n\n        # Build mime data for clipboard\n        data = QtCore.QMimeData()\n        url = QtCore.QUrl.fromLocalFile(path)\n        data.setUrls([url])\n\n        # Set to Clipboard\n        clipboard.setMimeData(data)\n\n        return LoaderActionResult(\n            \"File added to clipboard...\",\n            success=True,\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/loader/create_hero_version.html","title":"create_hero_version","text":"<p>Plugin to create hero version from selected context.</p>"},{"location":"autoapi/client/ayon_core/plugins/loader/create_hero_version.html#client.ayon_core.plugins.loader.create_hero_version.CreateHeroVersion","title":"<code>CreateHeroVersion</code>","text":"<p>               Bases: <code>LoaderActionPlugin</code></p> <p>Create hero version from selected context.</p> Source code in <code>client/ayon_core/plugins/loader/create_hero_version.py</code> <pre><code>class CreateHeroVersion(LoaderActionPlugin):\n    \"\"\"Create hero version from selected context.\"\"\"\n\n    is_multiple_contexts_compatible = False\n    ignored_representation_names: list[str] = []\n    db_representation_context_keys = [\n        \"project\",\n        \"folder\",\n        \"hierarchy\",\n        \"task\",\n        \"product\",\n        \"representation\",\n        \"username\",\n        \"user\",\n        \"output\"\n    ]\n    use_hardlinks = False\n\n    def get_action_items(\n        self, selection: LoaderActionSelection\n    ) -&gt; list[LoaderActionItem]:\n        # Do not show in hosts\n        if self.host_name is not None:\n            return []\n\n        versions = selection.get_selected_version_entities()\n        if not versions:\n            return []\n\n        return [\n            LoaderActionItem(\n                label=\"Create Hero Version\",\n                order=36,\n                icon={\n                    \"type\": \"awesome-font\",\n                    \"name\": \"fa5.star\",\n                    \"color\": \"#ffd700\",\n                }\n            ),\n        ]\n\n    def execute_action(\n        self,\n        selection: LoaderActionSelection,\n        data: dict[str, Any],\n        form_values: dict[str, Any],\n    ) -&gt; Optional[LoaderActionResult]:\n        project_name = selection.project_name\n        project_settings = selection.get_project_settings()\n        project_entity = ayon_api.get_project(project_name)\n        anatomy = selection.get_project_anatomy()\n        version_entities = selection.get_selected_version_entities()\n\n        # Prepare all necessary entities\n        product_ids = set()\n        task_ids = set()\n        version_ids = set()\n        for version in version_entities:\n            product_ids.add(version[\"productId\"])\n            task_ids.add(version[\"taskId\"])\n            version_ids.add(version[\"id\"])\n\n        task_ids.discard(None)\n\n        product_entities_by_id = {\n            product_entity[\"id\"]: product_entity\n            for product_entity in ayon_api.get_products(\n                project_name, product_ids=product_ids\n            )\n        }\n        folder_ids = {\n            product_entity[\"folderId\"]\n            for product_entity in product_entities_by_id.values()\n        }\n        folder_entities_by_id = {\n            folder_entity[\"id\"]: folder_entity\n            for folder_entity in ayon_api.get_folders(\n                project_name, folder_ids=folder_ids\n            )\n        }\n        task_entities_by_id = {\n            task_entity[\"id\"]: task_entity\n            for task_entity in ayon_api.get_tasks(\n                project_name, task_ids=task_ids\n            )\n        }\n\n        hero_version_ids = set()\n        hero_versions_by_product_id = {\n            product_id: None\n            for product_id in product_ids\n        }\n        for hero_version_entity in ayon_api.get_hero_versions(\n            project_name,\n            product_ids=product_ids,\n        ):\n            product_id = hero_version_entity[\"productId\"]\n            hero_versions_by_product_id[product_id] = hero_version_entity\n            hero_version_ids.add(hero_version_entity[\"id\"])\n\n        repres_by_version_id = collections.defaultdict(list)\n        for repre in ayon_api.get_representations(\n            project_name, version_ids=version_ids | hero_version_ids\n        ):\n            version_id = repre[\"versionId\"]\n            repres_by_version_id[version_id].append(repre)\n\n        # Begin the conversion version by version\n        errors = []\n        for version_entity in version_entities:\n            version_id = version_entity[\"id\"]\n            product_id = version_entity[\"productId\"]\n            product_entity = product_entities_by_id[product_id]\n            folder_id = product_entity[\"folderId\"]\n            folder_entity = folder_entities_by_id[folder_id]\n            task_id = version_entity[\"taskId\"]\n            task_entity = task_entities_by_id.get(task_id)\n\n            hero_version_entity = hero_versions_by_product_id[product_id]\n            hero_representations = []\n            if hero_version_entity:\n                hero_version_id = hero_version_entity[\"id\"]\n                hero_representations = repres_by_version_id[hero_version_id]\n\n            template_data = get_template_data(\n                project_entity=project_entity,\n                folder_entity=folder_entity,\n                task_entity=task_entity,\n            )\n\n            product_type = product_entity.get(\"productType\")\n            if not product_type:\n                product_type = product_entity[\"type\"]\n\n            product_base_type = product_entity.get(\"productBaseType\")\n            if not product_base_type:\n                product_base_type = product_type\n\n            template_data[\"product\"] = {\n                \"name\": product_entity[\"name\"],\n                \"type\": product_type,\n                \"basetype\": product_base_type,\n            }\n            template_data[\"version\"] = version_entity[\"version\"]\n\n            src_representations = {}\n            for repre in repres_by_version_id[version_id]:\n                repre_template_data = copy.deepcopy(template_data)\n                ext = repre.get(\"context\", {}).get(\"ext\")\n                if ext:\n                    repre_template_data[\"ext\"] = ext\n\n                src_representations[repre[\"id\"]] = {\n                    \"representation\": repre,\n                    \"published_files\": [\n                        file_info[\"path\"]\n                        for file_info in repre.get(\"files\", [])\n                    ],\n                    \"template_data\": repre_template_data\n                }\n\n            try:\n                self.create_hero_version(\n                    anatomy,\n                    project_settings,\n                    task_entity,\n                    version_entity,\n                    src_representations,\n                    template_data,\n                    hero_version_entity,\n                    hero_representations,\n                )\n            except HeroCreationError as exc:\n                self.log.warning(\n                    f\"Failed to convert version to hero version: {exc}\"\n                )\n                errors.append(str(exc))\n\n            except Exception:\n                self.log.warning(\n                    \"Failed to convert version to hero version.\",\n                    exc_info=True,\n                )\n                errors.append(\"Unexpected error\")\n\n        if not errors:\n            if len(version_entities) == 1:\n                message = \"Hero version created successfully.\"\n            else:\n                message = (\n                    f\"{len(version_entities)} Hero versions\"\n                    \" created successfully.\"\n                )\n            return LoaderActionResult(success=True, message=message)\n\n        if len(version_entities) == 1:\n            message = \"Failed to create hero version\"\n        else:\n            message = (\n                f\"Failed to create {len(errors)}/{len(version_entities)}\"\n                f\" hero versions\"\n            )\n        return LoaderActionResult(\n            success=False,\n            message=f\"{message}:\\n{chr(10).join(errors)}\"\n        )\n\n    def create_hero_version(\n        self,\n        anatomy: Anatomy,\n        project_settings: dict[str, Any],\n        task_entity: dict[str, Any] | None,\n        version_entity: dict[str, Any],\n        src_representations: dict[str, dict],\n        template_data: dict[str, Any],\n        hero_version_entity: dict[str, Any] | None,\n        hero_representations: list[dict[str, Any]],\n    ) -&gt; None:\n        \"\"\"Create hero version from instance data.\n\n        Args:\n            anatomy (Anatomy): Anatomy object for the project.\n            project_settings (dict[str, Any]): Project settings.\n            task_entity (dict[str, Any] | None): Source task entity\n                if there was any.\n            version_entity (dict): Source version entity.\n            src_representations (dict[str, dict]): Representations by id.\n            template_data (dict[str, Any]): Base template data of source\n                context.\n            hero_version_entity (dict[str, Any] | None): Source hero version\n                if there was any.\n            hero_representations (list[dict[str, Any]]): Representations of\n                hero version.\n\n        Raises:\n            HeroCreationError: If any required data is missing or an error\n                occurs ring the hero version creation process.\n\n        \"\"\"\n        if not version_entity:\n            raise HeroCreationError(\"Can't find origin version in database.\")\n\n        if version_entity[\"version\"] == 0:\n            raise HeroCreationError(\"Version 0 cannot have hero version.\")\n\n        if not src_representations:\n            raise HeroCreationError(\"No published representations found.\")\n\n        for repre_id, repre_info in tuple(src_representations.items()):\n            repre = repre_info[\"representation\"]\n            if repre[\"name\"].lower() in self.ignored_representation_names:\n                src_representations.pop(repre_id, None)\n\n        if not src_representations:\n            raise HeroCreationError(\n                \"All published representations were filtered by name.\"\n            )\n\n        project_name = anatomy.project_name\n\n        task_name = task_type = None\n        if task_entity:\n            task_name = task_entity[\"name\"]\n            task_type = task_entity[\"taskType\"]\n\n        product_base_type = template_data[\"product\"][\"basetype\"]\n\n        # TODO how to get host name?\n        host_name = None\n\n        # get the publish directory\n        publish_template_key = get_publish_template_name(\n            project_name,\n            host_name,\n            product_base_type=product_base_type,\n            task_name=task_name,\n            task_type=task_type,\n            project_settings=project_settings,\n            logger=self.log\n        )\n        published_template_obj = anatomy.get_template_item(\n            \"publish\", publish_template_key, \"path\", default=None\n        )\n        if published_template_obj is None:\n            raise HeroCreationError(\n                \"Project anatomy does not have\"\n                f\" publish template key: {publish_template_key}\"\n            )\n\n        hero_template_key = get_publish_template_name(\n            project_name,\n            host_name,\n            product_base_type=product_base_type,\n            task_name=task_name,\n            task_type=task_type,\n            project_settings=project_settings,\n            hero=True,\n        )\n        hero_template = anatomy.get_template_item(\n            \"hero\", hero_template_key, \"path\", default=None\n        )\n        if hero_template is None:\n            raise HeroCreationError(\n                \"Project anatomy does not have\"\n                f\" hero template key: {hero_template_key}\"\n            )\n\n        self.log.info(f\"Hero template: {hero_template.template}\")\n\n        hero_template_obj = anatomy.get_template_item(\n            \"hero\", hero_template_key, \"directory\"\n        )\n        hero_publish_dir = os.path.normpath(\n            hero_template_obj.format_strict(template_data)\n        )\n\n        self.log.info(f\"Hero publish dir: {hero_publish_dir}\")\n\n        all_repre_file_paths = []\n        for repre_info in src_representations.values():\n            for file_path in repre_info[\"published_files\"]:\n                file_path = os.path.normpath(file_path)\n                if file_path not in all_repre_file_paths:\n                    all_repre_file_paths.append(file_path)\n\n        inactive_old_repres_by_name = {}\n        old_repres_by_name = {}\n        for repre in hero_representations:\n            low_name = repre[\"name\"].lower()\n            if repre[\"active\"]:\n                old_repres_by_name[low_name] = repre\n            else:\n                inactive_old_repres_by_name[low_name] = repre\n\n        op_session = OperationsSession()\n        hero_version_id = None\n        if hero_version_entity:\n            hero_version_id = hero_version_entity[\"id\"]\n\n        new_hero_version = new_version_entity(\n            -version_entity[\"version\"],\n            version_entity[\"productId\"],\n            task_id=version_entity[\"taskId\"],\n            data=copy.deepcopy(version_entity[\"data\"]),\n            attribs=copy.deepcopy(version_entity[\"attrib\"]),\n            entity_id=hero_version_id,\n        )\n        if hero_version_entity:\n            update_data = prepare_changes(\n                hero_version_entity,\n                new_hero_version\n            )\n            op_session.update_entity(\n                project_name,\n                \"version\",\n                hero_version_id,\n                update_data\n            )\n        else:\n            op_session.create_entity(\n                project_name,\n                \"version\",\n                new_hero_version\n            )\n\n        old_repres_to_replace = {}\n        for repre_info in src_representations.values():\n            repre = repre_info[\"representation\"]\n            repre_name_low = repre[\"name\"].lower()\n            if repre_name_low in old_repres_by_name:\n                old_repres_to_replace[repre_name_low] = (\n                    old_repres_by_name.pop(repre_name_low)\n                )\n\n        old_repres_to_delete = old_repres_by_name or {}\n        backup_hero_publish_dir = None\n        if os.path.exists(hero_publish_dir):\n            base_backup_dir = f\"{hero_publish_dir}.BACKUP\"\n            max_idx = 10\n            # Find the first available backup directory name\n            for idx in range(max_idx + 1):\n                candidate_backup_dir = base_backup_dir\n                if idx &gt; 0:\n                    candidate_backup_dir = f\"{base_backup_dir}{idx}\"\n                if not os.path.exists(candidate_backup_dir):\n                    backup_hero_publish_dir = candidate_backup_dir\n                    break\n            else:\n                raise HeroCreationError(\n                    f\"Backup folders are fully occupied to max index {max_idx}\"\n                )\n\n            try:\n                os.rename(hero_publish_dir, backup_hero_publish_dir)\n            except PermissionError as e:\n                raise HeroCreationError(\n                    \"Could not create hero version because it is \"\n                    \"not possible to replace current hero files.\"\n                ) from e\n\n        try:\n            src_to_dst_file_paths = []\n            repre_integrate_data = []\n            anatomy_root = {\"root\": anatomy.roots}\n            for repre_info in src_representations.values():\n                published_files = repre_info[\"published_files\"]\n                if len(published_files) == 0:\n                    continue\n                template_data = copy.deepcopy(repre_info[\"template_data\"])\n                template_data.pop(\"version\", None)\n                template_filled = hero_template.format_strict(\n                    template_data\n                )\n                repre_context = template_filled.used_values\n                for key in self.db_representation_context_keys:\n                    value = template_data.get(key)\n                    if value is not None:\n                        repre_context[key] = value\n                repre_entity = copy.deepcopy(repre_info[\"representation\"])\n                repre_entity.pop(\"id\", None)\n                repre_entity[\"versionId\"] = new_hero_version[\"id\"]\n                repre_entity[\"context\"] = repre_context\n                repre_entity[\"attrib\"] = {\n                    \"path\": str(template_filled),\n                    \"template\": hero_template.template\n                }\n                rootless_dst_paths = []\n\n                if len(published_files) == 1:\n                    rootless_dst_paths.append(str(template_filled.rootless))\n                    mapped_published_file = StringTemplate(\n                        published_files[0]\n                    ).format_strict(anatomy_root)\n                    src_to_dst_file_paths.append(\n                        (mapped_published_file, template_filled)\n                    )\n                    self.log.info(\n                        f\"Single published file: {mapped_published_file} -&gt; \"\n                        f\"{template_filled}\"\n                    )\n                else:\n                    collections, remainders = clique.assemble(published_files)\n                    if remainders or not collections or len(collections) &gt; 1:\n                        raise HeroCreationError(\n                            \"Integrity error. Files of published \"\n                            \"representation is combination of frame \"\n                            \"collections and single files.\"\n                        )\n\n                    src_col = collections[0]\n                    frame_splitter = \"_-_FRAME_SPLIT_-_\"\n                    template_data[\"frame\"] = frame_splitter\n                    _template_filled = hero_template.format_strict(\n                        template_data\n                    ).rootless\n                    head, tail = _template_filled.split(frame_splitter)\n                    padding = anatomy.templates_obj.frame_padding\n                    dst_col = clique.Collection(\n                        head=head, padding=padding, tail=tail\n                    )\n                    dst_col.indexes.clear()\n                    dst_col.indexes.update(src_col.indexes)\n                    for src_file, dst_file in zip(src_col, dst_col):\n                        src_file = StringTemplate(src_file).format_strict(\n                            anatomy_root\n                        )\n                        src_to_dst_file_paths.append(\n                            (src_file, dst_file.format(root=anatomy.roots))\n                        )\n                        rootless_dst_paths.append(dst_file)\n                        self.log.info(\n                            f\"Collection published file: {src_file} \"\n                            f\"-&gt; {dst_file}\"\n                        )\n                repre_integrate_data.append(\n                    (repre_entity, rootless_dst_paths)\n                )\n\n            # Copy files\n            with ThreadPoolExecutor(max_workers=8) as executor:\n                futures = [\n                    executor.submit(\n                        self.copy_file,\n                        src_path,\n                        dst_path\n                    )\n                    for src_path, dst_path in src_to_dst_file_paths\n                ]\n                wait_for_future_errors(executor, futures)\n\n            # Update/create representations\n            for repre_entity, dst_paths in repre_integrate_data:\n                repre_files = self.get_files_info(dst_paths, anatomy)\n                repre_entity[\"files\"] = repre_files\n                repre_name_low = repre_entity[\"name\"].lower()\n                if repre_name_low in old_repres_to_replace:\n                    old_repre = old_repres_to_replace.pop(repre_name_low)\n                    repre_entity[\"id\"] = old_repre[\"id\"]\n                    update_data = prepare_changes(old_repre, repre_entity)\n                    op_session.update_entity(\n                        project_name,\n                        \"representation\",\n                        old_repre[\"id\"],\n                        update_data\n                    )\n\n                elif repre_name_low in inactive_old_repres_by_name:\n                    inactive_repre = inactive_old_repres_by_name.pop(\n                        repre_name_low\n                    )\n                    repre_entity[\"id\"] = inactive_repre[\"id\"]\n                    update_data = prepare_changes(inactive_repre, repre_entity)\n                    op_session.update_entity(\n                        project_name,\n                        \"representation\",\n                        inactive_repre[\"id\"],\n                        update_data\n                    )\n\n                else:\n                    op_session.create_entity(\n                        project_name,\n                        \"representation\",\n                        repre_entity\n                    )\n\n            for repre in old_repres_to_delete.values():\n                op_session.update_entity(\n                    project_name,\n                    \"representation\",\n                    repre[\"id\"],\n                    {\"active\": False}\n                )\n\n            op_session.commit()\n\n            if backup_hero_publish_dir is not None and os.path.exists(\n                backup_hero_publish_dir\n            ):\n                shutil.rmtree(backup_hero_publish_dir)\n\n        except Exception:\n            if (\n                backup_hero_publish_dir is not None\n                and os.path.exists(backup_hero_publish_dir)\n            ):\n                if os.path.exists(hero_publish_dir):\n                    shutil.rmtree(hero_publish_dir)\n                os.rename(backup_hero_publish_dir, hero_publish_dir)\n            raise\n\n    def get_files_info(\n        self,\n        filepaths: list[str],\n        anatomy: Anatomy,\n    ) -&gt; list[dict]:\n        \"\"\"Get list of file info dictionaries for given file paths.\n\n        Args:\n            filepaths (list[str]): List of absolute file paths.\n            anatomy (Anatomy): Anatomy object for the project.\n\n        Returns:\n            list[dict]: List of file info dictionaries.\n\n        \"\"\"\n        file_infos = [\n            self.prepare_file_info(filepath, anatomy)\n            for filepath in filepaths\n        ]\n        return file_infos\n\n    def prepare_file_info(self, path: str, anatomy: Anatomy) -&gt; dict:\n        \"\"\"Prepare file info dictionary for given path.\n\n        Args:\n            path (str): Absolute file path.\n            anatomy (Anatomy): Anatomy object for the project.\n\n        Returns:\n            dict: File info dictionary with keys:\n                - id (str): Unique identifier for the file.\n                - name (str): Base name of the file.\n                - path (str): Rootless file path.\n                - size (int): Size of the file in bytes.\n                - hash (str): Hash of the file content.\n                - hash_type (str): Type of the hash used.\n\n        \"\"\"\n        realpath = path.format(root=anatomy.roots)\n        return {\n            \"id\": create_entity_id(),\n            \"name\": os.path.basename(path),\n            \"path\": path,\n            \"size\": os.path.getsize(realpath),\n            \"hash\": source_hash(realpath),\n            \"hash_type\": \"op3\",\n        }\n\n    def copy_file(self, src_path: str, dst_path: str) -&gt; None:\n        \"\"\"Copy file from src to dst with creating directories.\n\n        Args:\n            src_path (str): Source file path.\n            dst_path (str): Destination file path.\n\n        Raises:\n            OSError: If copying or linking fails.\n\n        \"\"\"\n        dirname = os.path.dirname(dst_path)\n        os.makedirs(dirname, exist_ok=True)\n\n        if self.use_hardlinks:\n            try:\n                create_hard_link(src_path, dst_path)\n                return\n            except OSError as exc:\n                if exc.errno not in [errno.EXDEV, errno.EINVAL]:\n                    raise\n        copyfile(src_path, dst_path)\n\n    @staticmethod\n    def current_hero_ents(\n        project_name: str,\n        version_entity: dict[str, Any]\n    ) -&gt; tuple[Any, list[dict[str, Any]]]:\n        hero_version = ayon_api.get_hero_version_by_product_id(\n            project_name, version_entity[\"productId\"]\n        )\n        hero_repres = []\n        if hero_version:\n            hero_repres = list(ayon_api.get_representations(\n                project_name, version_ids={hero_version[\"id\"]}\n            ))\n        return hero_version, hero_repres\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/loader/create_hero_version.html#client.ayon_core.plugins.loader.create_hero_version.CreateHeroVersion.copy_file","title":"<code>copy_file(src_path, dst_path)</code>","text":"<p>Copy file from src to dst with creating directories.</p> <p>Parameters:</p> Name Type Description Default <code>src_path</code> <code>str</code> <p>Source file path.</p> required <code>dst_path</code> <code>str</code> <p>Destination file path.</p> required <p>Raises:</p> Type Description <code>OSError</code> <p>If copying or linking fails.</p> Source code in <code>client/ayon_core/plugins/loader/create_hero_version.py</code> <pre><code>def copy_file(self, src_path: str, dst_path: str) -&gt; None:\n    \"\"\"Copy file from src to dst with creating directories.\n\n    Args:\n        src_path (str): Source file path.\n        dst_path (str): Destination file path.\n\n    Raises:\n        OSError: If copying or linking fails.\n\n    \"\"\"\n    dirname = os.path.dirname(dst_path)\n    os.makedirs(dirname, exist_ok=True)\n\n    if self.use_hardlinks:\n        try:\n            create_hard_link(src_path, dst_path)\n            return\n        except OSError as exc:\n            if exc.errno not in [errno.EXDEV, errno.EINVAL]:\n                raise\n    copyfile(src_path, dst_path)\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/loader/create_hero_version.html#client.ayon_core.plugins.loader.create_hero_version.CreateHeroVersion.create_hero_version","title":"<code>create_hero_version(anatomy, project_settings, task_entity, version_entity, src_representations, template_data, hero_version_entity, hero_representations)</code>","text":"<p>Create hero version from instance data.</p> <p>Parameters:</p> Name Type Description Default <code>anatomy</code> <code>Anatomy</code> <p>Anatomy object for the project.</p> required <code>project_settings</code> <code>dict[str, Any]</code> <p>Project settings.</p> required <code>task_entity</code> <code>dict[str, Any] | None</code> <p>Source task entity if there was any.</p> required <code>version_entity</code> <code>dict</code> <p>Source version entity.</p> required <code>src_representations</code> <code>dict[str, dict]</code> <p>Representations by id.</p> required <code>template_data</code> <code>dict[str, Any]</code> <p>Base template data of source context.</p> required <code>hero_version_entity</code> <code>dict[str, Any] | None</code> <p>Source hero version if there was any.</p> required <code>hero_representations</code> <code>list[dict[str, Any]]</code> <p>Representations of hero version.</p> required <p>Raises:</p> Type Description <code>HeroCreationError</code> <p>If any required data is missing or an error occurs ring the hero version creation process.</p> Source code in <code>client/ayon_core/plugins/loader/create_hero_version.py</code> <pre><code>def create_hero_version(\n    self,\n    anatomy: Anatomy,\n    project_settings: dict[str, Any],\n    task_entity: dict[str, Any] | None,\n    version_entity: dict[str, Any],\n    src_representations: dict[str, dict],\n    template_data: dict[str, Any],\n    hero_version_entity: dict[str, Any] | None,\n    hero_representations: list[dict[str, Any]],\n) -&gt; None:\n    \"\"\"Create hero version from instance data.\n\n    Args:\n        anatomy (Anatomy): Anatomy object for the project.\n        project_settings (dict[str, Any]): Project settings.\n        task_entity (dict[str, Any] | None): Source task entity\n            if there was any.\n        version_entity (dict): Source version entity.\n        src_representations (dict[str, dict]): Representations by id.\n        template_data (dict[str, Any]): Base template data of source\n            context.\n        hero_version_entity (dict[str, Any] | None): Source hero version\n            if there was any.\n        hero_representations (list[dict[str, Any]]): Representations of\n            hero version.\n\n    Raises:\n        HeroCreationError: If any required data is missing or an error\n            occurs ring the hero version creation process.\n\n    \"\"\"\n    if not version_entity:\n        raise HeroCreationError(\"Can't find origin version in database.\")\n\n    if version_entity[\"version\"] == 0:\n        raise HeroCreationError(\"Version 0 cannot have hero version.\")\n\n    if not src_representations:\n        raise HeroCreationError(\"No published representations found.\")\n\n    for repre_id, repre_info in tuple(src_representations.items()):\n        repre = repre_info[\"representation\"]\n        if repre[\"name\"].lower() in self.ignored_representation_names:\n            src_representations.pop(repre_id, None)\n\n    if not src_representations:\n        raise HeroCreationError(\n            \"All published representations were filtered by name.\"\n        )\n\n    project_name = anatomy.project_name\n\n    task_name = task_type = None\n    if task_entity:\n        task_name = task_entity[\"name\"]\n        task_type = task_entity[\"taskType\"]\n\n    product_base_type = template_data[\"product\"][\"basetype\"]\n\n    # TODO how to get host name?\n    host_name = None\n\n    # get the publish directory\n    publish_template_key = get_publish_template_name(\n        project_name,\n        host_name,\n        product_base_type=product_base_type,\n        task_name=task_name,\n        task_type=task_type,\n        project_settings=project_settings,\n        logger=self.log\n    )\n    published_template_obj = anatomy.get_template_item(\n        \"publish\", publish_template_key, \"path\", default=None\n    )\n    if published_template_obj is None:\n        raise HeroCreationError(\n            \"Project anatomy does not have\"\n            f\" publish template key: {publish_template_key}\"\n        )\n\n    hero_template_key = get_publish_template_name(\n        project_name,\n        host_name,\n        product_base_type=product_base_type,\n        task_name=task_name,\n        task_type=task_type,\n        project_settings=project_settings,\n        hero=True,\n    )\n    hero_template = anatomy.get_template_item(\n        \"hero\", hero_template_key, \"path\", default=None\n    )\n    if hero_template is None:\n        raise HeroCreationError(\n            \"Project anatomy does not have\"\n            f\" hero template key: {hero_template_key}\"\n        )\n\n    self.log.info(f\"Hero template: {hero_template.template}\")\n\n    hero_template_obj = anatomy.get_template_item(\n        \"hero\", hero_template_key, \"directory\"\n    )\n    hero_publish_dir = os.path.normpath(\n        hero_template_obj.format_strict(template_data)\n    )\n\n    self.log.info(f\"Hero publish dir: {hero_publish_dir}\")\n\n    all_repre_file_paths = []\n    for repre_info in src_representations.values():\n        for file_path in repre_info[\"published_files\"]:\n            file_path = os.path.normpath(file_path)\n            if file_path not in all_repre_file_paths:\n                all_repre_file_paths.append(file_path)\n\n    inactive_old_repres_by_name = {}\n    old_repres_by_name = {}\n    for repre in hero_representations:\n        low_name = repre[\"name\"].lower()\n        if repre[\"active\"]:\n            old_repres_by_name[low_name] = repre\n        else:\n            inactive_old_repres_by_name[low_name] = repre\n\n    op_session = OperationsSession()\n    hero_version_id = None\n    if hero_version_entity:\n        hero_version_id = hero_version_entity[\"id\"]\n\n    new_hero_version = new_version_entity(\n        -version_entity[\"version\"],\n        version_entity[\"productId\"],\n        task_id=version_entity[\"taskId\"],\n        data=copy.deepcopy(version_entity[\"data\"]),\n        attribs=copy.deepcopy(version_entity[\"attrib\"]),\n        entity_id=hero_version_id,\n    )\n    if hero_version_entity:\n        update_data = prepare_changes(\n            hero_version_entity,\n            new_hero_version\n        )\n        op_session.update_entity(\n            project_name,\n            \"version\",\n            hero_version_id,\n            update_data\n        )\n    else:\n        op_session.create_entity(\n            project_name,\n            \"version\",\n            new_hero_version\n        )\n\n    old_repres_to_replace = {}\n    for repre_info in src_representations.values():\n        repre = repre_info[\"representation\"]\n        repre_name_low = repre[\"name\"].lower()\n        if repre_name_low in old_repres_by_name:\n            old_repres_to_replace[repre_name_low] = (\n                old_repres_by_name.pop(repre_name_low)\n            )\n\n    old_repres_to_delete = old_repres_by_name or {}\n    backup_hero_publish_dir = None\n    if os.path.exists(hero_publish_dir):\n        base_backup_dir = f\"{hero_publish_dir}.BACKUP\"\n        max_idx = 10\n        # Find the first available backup directory name\n        for idx in range(max_idx + 1):\n            candidate_backup_dir = base_backup_dir\n            if idx &gt; 0:\n                candidate_backup_dir = f\"{base_backup_dir}{idx}\"\n            if not os.path.exists(candidate_backup_dir):\n                backup_hero_publish_dir = candidate_backup_dir\n                break\n        else:\n            raise HeroCreationError(\n                f\"Backup folders are fully occupied to max index {max_idx}\"\n            )\n\n        try:\n            os.rename(hero_publish_dir, backup_hero_publish_dir)\n        except PermissionError as e:\n            raise HeroCreationError(\n                \"Could not create hero version because it is \"\n                \"not possible to replace current hero files.\"\n            ) from e\n\n    try:\n        src_to_dst_file_paths = []\n        repre_integrate_data = []\n        anatomy_root = {\"root\": anatomy.roots}\n        for repre_info in src_representations.values():\n            published_files = repre_info[\"published_files\"]\n            if len(published_files) == 0:\n                continue\n            template_data = copy.deepcopy(repre_info[\"template_data\"])\n            template_data.pop(\"version\", None)\n            template_filled = hero_template.format_strict(\n                template_data\n            )\n            repre_context = template_filled.used_values\n            for key in self.db_representation_context_keys:\n                value = template_data.get(key)\n                if value is not None:\n                    repre_context[key] = value\n            repre_entity = copy.deepcopy(repre_info[\"representation\"])\n            repre_entity.pop(\"id\", None)\n            repre_entity[\"versionId\"] = new_hero_version[\"id\"]\n            repre_entity[\"context\"] = repre_context\n            repre_entity[\"attrib\"] = {\n                \"path\": str(template_filled),\n                \"template\": hero_template.template\n            }\n            rootless_dst_paths = []\n\n            if len(published_files) == 1:\n                rootless_dst_paths.append(str(template_filled.rootless))\n                mapped_published_file = StringTemplate(\n                    published_files[0]\n                ).format_strict(anatomy_root)\n                src_to_dst_file_paths.append(\n                    (mapped_published_file, template_filled)\n                )\n                self.log.info(\n                    f\"Single published file: {mapped_published_file} -&gt; \"\n                    f\"{template_filled}\"\n                )\n            else:\n                collections, remainders = clique.assemble(published_files)\n                if remainders or not collections or len(collections) &gt; 1:\n                    raise HeroCreationError(\n                        \"Integrity error. Files of published \"\n                        \"representation is combination of frame \"\n                        \"collections and single files.\"\n                    )\n\n                src_col = collections[0]\n                frame_splitter = \"_-_FRAME_SPLIT_-_\"\n                template_data[\"frame\"] = frame_splitter\n                _template_filled = hero_template.format_strict(\n                    template_data\n                ).rootless\n                head, tail = _template_filled.split(frame_splitter)\n                padding = anatomy.templates_obj.frame_padding\n                dst_col = clique.Collection(\n                    head=head, padding=padding, tail=tail\n                )\n                dst_col.indexes.clear()\n                dst_col.indexes.update(src_col.indexes)\n                for src_file, dst_file in zip(src_col, dst_col):\n                    src_file = StringTemplate(src_file).format_strict(\n                        anatomy_root\n                    )\n                    src_to_dst_file_paths.append(\n                        (src_file, dst_file.format(root=anatomy.roots))\n                    )\n                    rootless_dst_paths.append(dst_file)\n                    self.log.info(\n                        f\"Collection published file: {src_file} \"\n                        f\"-&gt; {dst_file}\"\n                    )\n            repre_integrate_data.append(\n                (repre_entity, rootless_dst_paths)\n            )\n\n        # Copy files\n        with ThreadPoolExecutor(max_workers=8) as executor:\n            futures = [\n                executor.submit(\n                    self.copy_file,\n                    src_path,\n                    dst_path\n                )\n                for src_path, dst_path in src_to_dst_file_paths\n            ]\n            wait_for_future_errors(executor, futures)\n\n        # Update/create representations\n        for repre_entity, dst_paths in repre_integrate_data:\n            repre_files = self.get_files_info(dst_paths, anatomy)\n            repre_entity[\"files\"] = repre_files\n            repre_name_low = repre_entity[\"name\"].lower()\n            if repre_name_low in old_repres_to_replace:\n                old_repre = old_repres_to_replace.pop(repre_name_low)\n                repre_entity[\"id\"] = old_repre[\"id\"]\n                update_data = prepare_changes(old_repre, repre_entity)\n                op_session.update_entity(\n                    project_name,\n                    \"representation\",\n                    old_repre[\"id\"],\n                    update_data\n                )\n\n            elif repre_name_low in inactive_old_repres_by_name:\n                inactive_repre = inactive_old_repres_by_name.pop(\n                    repre_name_low\n                )\n                repre_entity[\"id\"] = inactive_repre[\"id\"]\n                update_data = prepare_changes(inactive_repre, repre_entity)\n                op_session.update_entity(\n                    project_name,\n                    \"representation\",\n                    inactive_repre[\"id\"],\n                    update_data\n                )\n\n            else:\n                op_session.create_entity(\n                    project_name,\n                    \"representation\",\n                    repre_entity\n                )\n\n        for repre in old_repres_to_delete.values():\n            op_session.update_entity(\n                project_name,\n                \"representation\",\n                repre[\"id\"],\n                {\"active\": False}\n            )\n\n        op_session.commit()\n\n        if backup_hero_publish_dir is not None and os.path.exists(\n            backup_hero_publish_dir\n        ):\n            shutil.rmtree(backup_hero_publish_dir)\n\n    except Exception:\n        if (\n            backup_hero_publish_dir is not None\n            and os.path.exists(backup_hero_publish_dir)\n        ):\n            if os.path.exists(hero_publish_dir):\n                shutil.rmtree(hero_publish_dir)\n            os.rename(backup_hero_publish_dir, hero_publish_dir)\n        raise\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/loader/create_hero_version.html#client.ayon_core.plugins.loader.create_hero_version.CreateHeroVersion.get_files_info","title":"<code>get_files_info(filepaths, anatomy)</code>","text":"<p>Get list of file info dictionaries for given file paths.</p> <p>Parameters:</p> Name Type Description Default <code>filepaths</code> <code>list[str]</code> <p>List of absolute file paths.</p> required <code>anatomy</code> <code>Anatomy</code> <p>Anatomy object for the project.</p> required <p>Returns:</p> Type Description <code>list[dict]</code> <p>list[dict]: List of file info dictionaries.</p> Source code in <code>client/ayon_core/plugins/loader/create_hero_version.py</code> <pre><code>def get_files_info(\n    self,\n    filepaths: list[str],\n    anatomy: Anatomy,\n) -&gt; list[dict]:\n    \"\"\"Get list of file info dictionaries for given file paths.\n\n    Args:\n        filepaths (list[str]): List of absolute file paths.\n        anatomy (Anatomy): Anatomy object for the project.\n\n    Returns:\n        list[dict]: List of file info dictionaries.\n\n    \"\"\"\n    file_infos = [\n        self.prepare_file_info(filepath, anatomy)\n        for filepath in filepaths\n    ]\n    return file_infos\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/loader/create_hero_version.html#client.ayon_core.plugins.loader.create_hero_version.CreateHeroVersion.prepare_file_info","title":"<code>prepare_file_info(path, anatomy)</code>","text":"<p>Prepare file info dictionary for given path.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Absolute file path.</p> required <code>anatomy</code> <code>Anatomy</code> <p>Anatomy object for the project.</p> required <p>Returns:</p> Name Type Description <code>dict</code> <code>dict</code> <p>File info dictionary with keys: - id (str): Unique identifier for the file. - name (str): Base name of the file. - path (str): Rootless file path. - size (int): Size of the file in bytes. - hash (str): Hash of the file content. - hash_type (str): Type of the hash used.</p> Source code in <code>client/ayon_core/plugins/loader/create_hero_version.py</code> <pre><code>def prepare_file_info(self, path: str, anatomy: Anatomy) -&gt; dict:\n    \"\"\"Prepare file info dictionary for given path.\n\n    Args:\n        path (str): Absolute file path.\n        anatomy (Anatomy): Anatomy object for the project.\n\n    Returns:\n        dict: File info dictionary with keys:\n            - id (str): Unique identifier for the file.\n            - name (str): Base name of the file.\n            - path (str): Rootless file path.\n            - size (int): Size of the file in bytes.\n            - hash (str): Hash of the file content.\n            - hash_type (str): Type of the hash used.\n\n    \"\"\"\n    realpath = path.format(root=anatomy.roots)\n    return {\n        \"id\": create_entity_id(),\n        \"name\": os.path.basename(path),\n        \"path\": path,\n        \"size\": os.path.getsize(realpath),\n        \"hash\": source_hash(realpath),\n        \"hash_type\": \"op3\",\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/loader/create_hero_version.html#client.ayon_core.plugins.loader.create_hero_version.HeroCreationError","title":"<code>HeroCreationError</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Used for internal error reporting during hero version creation.</p> Source code in <code>client/ayon_core/plugins/loader/create_hero_version.py</code> <pre><code>class HeroCreationError(Exception):\n    \"\"\"Used for internal error reporting during hero version creation.\"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/loader/create_hero_version.html#client.ayon_core.plugins.loader.create_hero_version.prepare_changes","title":"<code>prepare_changes(old_entity, new_entity)</code>","text":"<p>Prepare changes dict for update entity operation.</p> <p>Parameters:</p> Name Type Description Default <code>old_entity</code> <code>dict</code> <p>Existing entity data from database.</p> required <code>new_entity</code> <code>dict</code> <p>New entity data to compare against old.</p> required <p>Returns:</p> Name Type Description <code>dict</code> <code>dict</code> <p>Changes to apply to old entity to make it like new entity.</p> Source code in <code>client/ayon_core/plugins/loader/create_hero_version.py</code> <pre><code>def prepare_changes(old_entity: dict, new_entity: dict) -&gt; dict:\n    \"\"\"Prepare changes dict for update entity operation.\n\n    Args:\n        old_entity (dict): Existing entity data from database.\n        new_entity (dict): New entity data to compare against old.\n\n    Returns:\n        dict: Changes to apply to old entity to make it like new entity.\n\n    \"\"\"\n    changes = {}\n    for key in set(new_entity.keys()):\n        if key == \"attrib\":\n            continue\n        if key in new_entity and new_entity[key] != old_entity.get(key):\n            changes[key] = new_entity[key]\n    attrib_changes = {}\n    if \"attrib\" in new_entity:\n        for key, value in new_entity[\"attrib\"].items():\n            if value != old_entity[\"attrib\"].get(key):\n                attrib_changes[key] = value\n    if attrib_changes:\n        changes[\"attrib\"] = attrib_changes\n    return changes\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/loader/delete_old_versions.html","title":"delete_old_versions","text":""},{"location":"autoapi/client/ayon_core/plugins/loader/delete_old_versions.html#client.ayon_core.plugins.loader.delete_old_versions.DeleteOldVersions","title":"<code>DeleteOldVersions</code>","text":"<p>               Bases: <code>LoaderActionPlugin</code></p> <p>Deletes specific number of old version</p> Source code in <code>client/ayon_core/plugins/loader/delete_old_versions.py</code> <pre><code>class DeleteOldVersions(LoaderActionPlugin):\n    \"\"\"Deletes specific number of old version\"\"\"\n\n    is_multiple_contexts_compatible = True\n    sequence_splitter = \"__sequence_splitter__\"\n\n    requires_confirmation = True\n\n    def get_action_items(\n        self, selection: LoaderActionSelection\n    ) -&gt; list[LoaderActionItem]:\n        # Do not show in hosts\n        if self.host_name is not None:\n            return []\n\n        versions = selection.get_selected_version_entities()\n        if not versions:\n            return []\n\n        product_ids = {\n            version[\"productId\"]\n            for version in versions\n        }\n\n        return [\n            LoaderActionItem(\n                label=\"Delete Versions\",\n                order=35,\n                data={\n                    \"product_ids\": list(product_ids),\n                    \"action\": \"delete-versions\",\n                },\n                icon=MaterialSymbolsIcon(\"delete\", color=\"#d8d8d8\"),\n            ),\n            LoaderActionItem(\n                label=\"Calculate Versions size\",\n                order=34,\n                data={\n                    \"product_ids\": list(product_ids),\n                    \"action\": \"calculate-versions-size\",\n                },\n                icon=MaterialSymbolsIcon(\"auto_delete\", color=\"#d8d8d8\"),\n            )\n        ]\n\n    def execute_action(\n        self,\n        selection: LoaderActionSelection,\n        data: dict[str, Any],\n        form_values: dict[str, Any],\n    ) -&gt; Optional[LoaderActionResult]:\n        step = form_values.get(\"step\")\n        action = data[\"action\"]\n        versions_to_keep = form_values.get(\"versions_to_keep\")\n        remove_publish_folder = form_values.get(\"remove_publish_folder\")\n        if step is None:\n            return self._first_step(\n                action,\n                versions_to_keep,\n                remove_publish_folder,\n            )\n\n        if versions_to_keep is None:\n            versions_to_keep = 2\n        if remove_publish_folder is None:\n            remove_publish_folder = False\n\n        product_ids = data[\"product_ids\"]\n        if step == \"prepare-data\":\n            return self._prepare_data_step(\n                action,\n                versions_to_keep,\n                remove_publish_folder,\n                product_ids,\n                selection,\n            )\n\n        if step == \"delete-versions\":\n            return self._delete_versions_step(\n                selection.project_name, form_values\n            )\n        return None\n\n    def _first_step(\n        self,\n        action: str,\n        versions_to_keep: Optional[int],\n        remove_publish_folder: Optional[bool],\n    ) -&gt; LoaderActionResult:\n        fields: list[AbstractAttrDef] = [\n            TextDef(\n                \"step\",\n                visible=False,\n            ),\n            NumberDef(\n                \"versions_to_keep\",\n                label=\"Versions to keep\",\n                minimum=0,\n                default=2,\n            ),\n        ]\n        if action == \"delete-versions\":\n            fields.append(\n                BoolDef(\n                    \"remove_publish_folder\",\n                    label=\"Remove publish folder\",\n                    default=False,\n                )\n            )\n\n        form_values = {\n            key: value\n            for key, value in (\n                (\"remove_publish_folder\", remove_publish_folder),\n                (\"versions_to_keep\", versions_to_keep),\n            )\n            if value is not None\n        }\n        form_values[\"step\"] = \"prepare-data\"\n        return LoaderActionResult(\n            form=ActionForm(\n                title=\"Delete Old Versions\",\n                fields=fields,\n            ),\n            form_values=form_values\n        )\n\n    def _prepare_data_step(\n        self,\n        action: str,\n        versions_to_keep: int,\n        remove_publish_folder: bool,\n        entity_ids: set[str],\n        selection: LoaderActionSelection,\n    ):\n        versions_by_product_id = collections.defaultdict(list)\n        for version in selection.entities.get_products_versions(entity_ids):\n            # Keep hero version\n            if versions_to_keep != 0 and version[\"version\"] &lt; 0:\n                continue\n            versions_by_product_id[version[\"productId\"]].append(version)\n\n        versions_to_delete = []\n        for product_id, versions in versions_by_product_id.items():\n            if versions_to_keep == 0:\n                versions_to_delete.extend(versions)\n                continue\n\n            if len(versions) &lt;= versions_to_keep:\n                continue\n\n            versions.sort(key=lambda v: v[\"version\"])\n            for _ in range(versions_to_keep):\n                if not versions:\n                    break\n                versions.pop(-1)\n            versions_to_delete.extend(versions)\n\n        self.log.debug(\n            f\"Collected versions to delete ({len(versions_to_delete)})\"\n        )\n\n        version_ids = {\n            version[\"id\"]\n            for version in versions_to_delete\n        }\n        if not version_ids:\n            return LoaderActionResult(\n                message=\"Skipping. Nothing to delete.\",\n                success=False,\n            )\n\n        project = selection.entities.get_project()\n        anatomy = Anatomy(project[\"name\"], project_entity=project)\n\n        repres = selection.entities.get_versions_representations(version_ids)\n\n        self.log.debug(\n            f\"Collected representations to remove ({len(repres)})\"\n        )\n\n        filepaths_by_repre_id = {}\n        repre_ids_by_version_id = {\n            version_id: []\n            for version_id in version_ids\n        }\n        for repre in repres:\n            repre_ids_by_version_id[repre[\"versionId\"]].append(repre[\"id\"])\n            filepaths_by_repre_id[repre[\"id\"]] = [\n                anatomy.fill_root(repre_file[\"path\"])\n                for repre_file in repre[\"files\"]\n            ]\n\n        size = 0\n        for filepaths in filepaths_by_repre_id.values():\n            for filepath in filepaths:\n                if os.path.exists(filepath):\n                    size += os.path.getsize(filepath)\n\n        if action == \"calculate-versions-size\":\n            return LoaderActionResult(\n                message=\"Calculated size\",\n                success=True,\n                form=ActionForm(\n                    title=\"Calculated versions size\",\n                    fields=[\n                        UILabelDef(\n                            f\"Total size of files: {format_file_size(size)}\"\n                        ),\n                    ],\n                    submit_label=None,\n                    cancel_label=\"Close\",\n                ),\n            )\n\n        form, form_values = self._get_delete_form(\n            size,\n            remove_publish_folder,\n            list(version_ids),\n            repre_ids_by_version_id,\n            filepaths_by_repre_id,\n        )\n        return LoaderActionResult(\n            form=form,\n            form_values=form_values\n        )\n\n    def _delete_versions_step(\n        self, project_name: str, form_values: dict[str, Any]\n    ) -&gt; LoaderActionResult:\n        delete_data = json.loads(form_values[\"delete_data\"])\n        remove_publish_folder = form_values[\"remove_publish_folder\"]\n        if form_values[\"delete_value\"].lower() != \"delete\":\n            size = delete_data[\"size\"]\n            form, form_values = self._get_delete_form(\n                size,\n                remove_publish_folder,\n                delete_data[\"version_ids\"],\n                delete_data[\"repre_ids_by_version_id\"],\n                delete_data[\"filepaths_by_repre_id\"],\n                True,\n            )\n            return LoaderActionResult(\n                form=form,\n                form_values=form_values,\n            )\n\n        version_ids = delete_data[\"version_ids\"]\n        repre_ids_by_version_id = delete_data[\"repre_ids_by_version_id\"]\n        filepaths_by_repre_id = delete_data[\"filepaths_by_repre_id\"]\n        op_session = OperationsSession()\n        total_versions = len(version_ids)\n        try:\n            for version_idx, version_id in enumerate(version_ids):\n                self.log.info(\n                    f\"Progressing version {version_idx + 1}/{total_versions}\"\n                )\n                for repre_id in repre_ids_by_version_id[version_id]:\n                    for filepath in filepaths_by_repre_id[repre_id]:\n                        publish_folder = os.path.dirname(filepath)\n                        if remove_publish_folder:\n                            if os.path.exists(publish_folder):\n                                shutil.rmtree(\n                                    publish_folder, ignore_errors=True\n                                )\n                            continue\n\n                        if os.path.exists(filepath):\n                            os.remove(filepath)\n\n                    op_session.delete_entity(\n                        project_name, \"representation\", repre_id\n                    )\n                op_session.delete_entity(\n                    project_name, \"version\", version_id\n                )\n            self.log.info(\"All done\")\n\n        except Exception:\n            self.log.error(\"Failed to delete versions.\", exc_info=True)\n            return LoaderActionResult(\n                message=\"Failed to delete versions.\",\n                success=False,\n            )\n\n        finally:\n            op_session.commit()\n\n        return LoaderActionResult(\n            message=\"Deleted versions\",\n            success=True,\n        )\n\n    def _get_delete_form(\n        self,\n        size: int,\n        remove_publish_folder: bool,\n        version_ids: list[str],\n        repre_ids_by_version_id: dict[str, list[str]],\n        filepaths_by_repre_id: dict[str, list[str]],\n        repeated: bool = False,\n    ) -&gt; tuple[ActionForm, dict[str, Any]]:\n        versions_len = len(repre_ids_by_version_id)\n        fields = [\n            UILabelDef(\n                f\"Going to delete {versions_len} versions&lt;br/&gt;\"\n                f\"- total size of files: {format_file_size(size)}&lt;br/&gt;\"\n            ),\n            UILabelDef(\"Are you sure you want to continue?\"),\n            TextDef(\n                \"delete_value\",\n                placeholder=\"Type 'delete' to confirm...\",\n            ),\n        ]\n        if repeated:\n            fields.append(UILabelDef(\n                \"*Please fill in '**delete**' to confirm deletion.*\"\n            ))\n        fields.extend([\n            TextDef(\n                \"delete_data\",\n                visible=False,\n            ),\n            TextDef(\n                \"step\",\n                visible=False,\n            ),\n            BoolDef(\n                \"remove_publish_folder\",\n                label=\"Remove publish folder\",\n                default=False,\n                visible=False,\n            )\n        ])\n\n        form = ActionForm(\n            title=\"Delete versions\",\n            submit_label=\"Delete\",\n            cancel_label=\"Close\",\n            fields=fields,\n        )\n        form_values = {\n            \"delete_data\": json.dumps({\n                \"size\": size,\n                \"version_ids\": version_ids,\n                \"repre_ids_by_version_id\": repre_ids_by_version_id,\n                \"filepaths_by_repre_id\": filepaths_by_repre_id,\n            }),\n            \"step\": \"delete-versions\",\n            \"remove_publish_folder\": remove_publish_folder,\n        }\n        return form, form_values\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/loader/delivery.html","title":"delivery","text":""},{"location":"autoapi/client/ayon_core/plugins/loader/export_otio.html","title":"export_otio","text":""},{"location":"autoapi/client/ayon_core/plugins/loader/export_otio.html#client.ayon_core.plugins.loader.export_otio.ExportOTIOOptionsDialog","title":"<code>ExportOTIOOptionsDialog</code>","text":"<p>               Bases: <code>QDialog</code></p> <p>Dialog to select template where to deliver selected representations.</p> Source code in <code>client/ayon_core/plugins/loader/export_otio.py</code> <pre><code>class ExportOTIOOptionsDialog(QtWidgets.QDialog):\n    \"\"\"Dialog to select template where to deliver selected representations.\"\"\"\n\n    def __init__(\n        self,\n        project_name,\n        versions_by_id,\n        repre_entities,\n        version_path_by_id,\n        log=None,\n        parent=None\n    ):\n        # Not all hosts have OpenTimelineIO available.\n        self.log = log\n\n        super().__init__(parent=parent)\n\n        self.setWindowTitle(\"AYON - Export OTIO\")\n        icon = QtGui.QIcon(resources.get_ayon_icon_filepath())\n        self.setWindowIcon(icon)\n\n        self.setWindowFlags(\n            QtCore.Qt.WindowStaysOnTopHint\n            | QtCore.Qt.WindowCloseButtonHint\n            | QtCore.Qt.WindowMinimizeButtonHint\n        )\n\n        version_by_representation_id = {\n            repre_entity[\"id\"]: versions_by_id[repre_entity[\"versionId\"]]\n            for repre_entity in repre_entities\n        }\n        representations_by_version_id = {\n            version_id: []\n            for version_id in versions_by_id\n        }\n\n        for repre_entity in repre_entities:\n            representations_by_version_id[repre_entity[\"versionId\"]].append(\n                repre_entity\n            )\n\n        all_representation_names = list(sorted({\n            repo_entity[\"name\"]\n            for repo_entity in repre_entities\n        }))\n\n        input_widget = QtWidgets.QWidget(self)\n        input_layout = QtWidgets.QGridLayout(input_widget)\n        input_layout.setContentsMargins(8, 8, 8, 8)\n\n        row = 0\n        repres_label = QtWidgets.QLabel(\"Representations:\", input_widget)\n        input_layout.addWidget(repres_label, row, 0)\n        repre_name_buttons = []\n        for idx, name in enumerate(all_representation_names):\n            repre_name_btn = QtWidgets.QPushButton(name, input_widget)\n            input_layout.addWidget(\n                repre_name_btn, row, idx + 1,\n                alignment=QtCore.Qt.AlignCenter\n            )\n            repre_name_btn.clicked.connect(self._toggle_all)\n            repre_name_buttons.append(repre_name_btn)\n\n        row += 1\n\n        representation_widgets = defaultdict(list)\n        items = representations_by_version_id.items()\n        for version_id, representations in items:\n            version_path = version_path_by_id[version_id]\n            label_widget = QtWidgets.QLabel(version_path, input_widget)\n            input_layout.addWidget(label_widget, row, 0)\n\n            repres_by_name = {\n                repre_entity[\"name\"]: repre_entity\n                for repre_entity in representations\n            }\n            radio_group = QtWidgets.QButtonGroup(input_widget)\n            for idx, name in enumerate(all_representation_names):\n                if name in repres_by_name:\n                    widget = QtWidgets.QRadioButton(input_widget)\n                    radio_group.addButton(widget)\n                    representation_widgets[name].append(\n                        {\n                            \"widget\": widget,\n                            \"representation\": repres_by_name[name]\n                        }\n                    )\n                else:\n                    widget = QtWidgets.QLabel(\"x\", input_widget)\n\n                input_layout.addWidget(\n                    widget, row, idx + 1, 1, 1,\n                    alignment=QtCore.Qt.AlignCenter\n                )\n\n            row += 1\n\n        export_widget = QtWidgets.QWidget(self)\n\n        options_widget = QtWidgets.QWidget(export_widget)\n\n        uri_label = QtWidgets.QLabel(\"URI paths:\", options_widget)\n        uri_path_format = QtWidgets.QCheckBox(options_widget)\n        uri_path_format.setToolTip(\n            \"Use URI paths (file:///) instead of absolute paths. \"\n            \"This is useful when the OTIO file will be used on Foundry Hiero.\"\n        )\n\n        button_output_path = QtWidgets.QPushButton(\n            \"Output Path:\", options_widget\n        )\n        button_output_path.setToolTip(\n            \"Click to select the output path for the OTIO file.\"\n        )\n\n        line_edit_output_path = QtWidgets.QLineEdit(\n            (Path.home() / f\"{project_name}.otio\").as_posix(),\n            options_widget\n        )\n\n        options_layout = QtWidgets.QHBoxLayout(options_widget)\n        options_layout.setContentsMargins(0, 0, 0, 0)\n        options_layout.addWidget(uri_label)\n        options_layout.addWidget(uri_path_format)\n        options_layout.addWidget(button_output_path)\n        options_layout.addWidget(line_edit_output_path)\n\n        button_export = QtWidgets.QPushButton(\"Export\", export_widget)\n\n        export_layout = QtWidgets.QVBoxLayout(export_widget)\n        export_layout.setContentsMargins(0, 0, 0, 0)\n        export_layout.addWidget(options_widget, 0)\n        export_layout.addWidget(button_export, 0)\n\n        main_layout = QtWidgets.QVBoxLayout(self)\n        main_layout.setContentsMargins(8, 8, 8, 8)\n        main_layout.addWidget(input_widget, 0)\n        main_layout.addStretch(1)\n        # TODO add line spacer?\n        main_layout.addSpacing(30)\n        main_layout.addWidget(export_widget, 0)\n\n        button_export.clicked.connect(self._on_export_click)\n        button_output_path.clicked.connect(self._set_output_path)\n\n        self._project_name = project_name\n        self._version_path_by_id = version_path_by_id\n        self._version_by_representation_id = version_by_representation_id\n        self._representation_widgets = representation_widgets\n        self._repre_name_buttons = repre_name_buttons\n\n        self._uri_path_format = uri_path_format\n        self._button_output_path = button_output_path\n        self._line_edit_output_path = line_edit_output_path\n        self._button_export = button_export\n\n        self._first_show = True\n\n    def showEvent(self, event):\n        super().showEvent(event)\n        if self._first_show:\n            self._first_show = False\n            self.setStyleSheet(style.load_stylesheet())\n\n    def _toggle_all(self):\n        representation_name = self.sender().text()\n        for item in self._representation_widgets[representation_name]:\n            item[\"widget\"].setChecked(True)\n\n    def _set_output_path(self):\n        file_path, _ = QtWidgets.QFileDialog.getSaveFileName(\n            None, \"Save OTIO file.\", \"\", \"OTIO Files (*.otio)\"\n        )\n        if file_path:\n            self._line_edit_output_path.setText(file_path)\n\n    def _on_export_click(self):\n        output_path = self._line_edit_output_path.text()\n        # Validate output path is not empty.\n        if not output_path:\n            show_message_dialog(\n                \"Missing output path\",\n                (\n                    \"Output path is empty. Please enter a path to export the \"\n                    \"OTIO file to.\"\n                ),\n                level=\"critical\",\n                parent=self\n            )\n            return\n\n        # Validate output path ends with .otio.\n        if not output_path.endswith(\".otio\"):\n            show_message_dialog(\n                \"Wrong extension.\",\n                (\n                    \"Output path needs to end with \\\".otio\\\".\"\n                ),\n                level=\"critical\",\n                parent=self\n            )\n            return\n\n        representations = []\n        for name, items in self._representation_widgets.items():\n            for item in items:\n                if item[\"widget\"].isChecked():\n                    representations.append(item[\"representation\"])\n\n        anatomy = Anatomy(self._project_name)\n        clips_data = {}\n        for representation in representations:\n            version = self._version_by_representation_id[\n                representation[\"id\"]\n            ]\n            name = (\n                f'{self._version_path_by_id[version[\"id\"]]}'\n                f'/{representation[\"name\"]}'\n            ).replace(\"/\", \"_\")\n\n            clips_data[name] = {\n                \"representation\": representation,\n                \"anatomy\": anatomy,\n                \"frames\": (\n                    version[\"attrib\"][\"frameEnd\"]\n                    - version[\"attrib\"][\"frameStart\"]\n                ),\n                \"framerate\": version[\"attrib\"][\"fps\"],\n            }\n\n        self.export_otio(clips_data, output_path)\n\n        # Feedback about success.\n        show_message_dialog(\n            \"Success!\",\n            \"Export was successful.\",\n            level=\"info\",\n            parent=self\n        )\n\n        self.close()\n\n    def create_clip(self, name, clip_data, timeline_framerate):\n        representation = clip_data[\"representation\"]\n        anatomy = clip_data[\"anatomy\"]\n        frames = clip_data[\"frames\"]\n        framerate = clip_data[\"framerate\"]\n\n        # Get path to representation with correct frame number\n        repre_path = get_representation_path_with_anatomy(\n            representation, anatomy)\n\n        media_start_frame = clip_start_frame = 0\n        media_framerate = framerate\n        if file_metadata := get_image_info_metadata(\n            repre_path, [\"timecode\", \"duration\", \"framerate\"], self.log\n        ):\n            # get media framerate and convert to float with 3 decimal places\n            media_framerate = file_metadata[\"framerate\"]\n            media_framerate = float(f\"{media_framerate:.4f}\")\n            framerate = float(f\"{timeline_framerate:.4f}\")\n\n            media_start_frame = self.get_timecode_start_frame(\n                media_framerate, file_metadata\n            )\n            clip_start_frame = self.get_timecode_start_frame(\n                timeline_framerate, file_metadata\n            )\n\n            if \"duration\" in file_metadata:\n                frames = int(float(file_metadata[\"duration\"]) * framerate)\n\n        repre_path = Path(repre_path)\n\n        first_frame = representation[\"context\"].get(\"frame\")\n        if first_frame is None:\n            media_range = OTIO.opentime.TimeRange(\n                start_time=OTIO.opentime.RationalTime(\n                    media_start_frame, media_framerate\n                ),\n                duration=OTIO.opentime.RationalTime(\n                    frames, media_framerate),\n            )\n            clip_range = OTIO.opentime.TimeRange(\n                start_time=OTIO.opentime.RationalTime(\n                    clip_start_frame, timeline_framerate\n                ),\n                duration=OTIO.opentime.RationalTime(\n                    frames, timeline_framerate),\n            )\n\n            # Use 'repre_path' as single file\n            media_reference = OTIO.schema.ExternalReference(\n                available_range=media_range,\n                target_url=self.convert_to_uri_or_posix(repre_path),\n            )\n        else:\n            # This is sequence\n            repre_files = [\n                file[\"path\"].format(root=anatomy.roots)\n                for file in representation[\"files\"]\n            ]\n            # Change frame in representation context to get path with frame\n            #   splitter.\n            representation[\"context\"][\"frame\"] = FRAME_SPLITTER\n            frame_repre_path = get_representation_path_with_anatomy(\n                representation, anatomy\n            )\n            frame_repre_path = Path(frame_repre_path)\n            repre_dir, repre_filename = (\n                frame_repre_path.parent, frame_repre_path.name)\n            # Get sequence prefix and suffix\n            file_prefix, file_suffix = repre_filename.split(FRAME_SPLITTER)\n            # Get frame number from path as string to get frame padding\n            frame_str = str(repre_path)[len(file_prefix):][:len(file_suffix)]\n            frame_padding = len(frame_str)\n\n            media_range = OTIO.opentime.TimeRange(\n                start_time=OTIO.opentime.RationalTime(\n                    media_start_frame, media_framerate\n                ),\n                duration=OTIO.opentime.RationalTime(\n                    len(repre_files), media_framerate\n                ),\n            )\n            clip_range = OTIO.opentime.TimeRange(\n                start_time=OTIO.opentime.RationalTime(\n                    clip_start_frame, timeline_framerate\n                ),\n                duration=OTIO.opentime.RationalTime(\n                    len(repre_files), timeline_framerate\n                ),\n            )\n\n            media_reference = OTIO.schema.ImageSequenceReference(\n                available_range=media_range,\n                start_frame=int(first_frame),\n                frame_step=1,\n                rate=framerate,\n                target_url_base=f\"{self.convert_to_uri_or_posix(repre_dir)}/\",\n                name_prefix=file_prefix,\n                name_suffix=file_suffix,\n                frame_zero_padding=frame_padding,\n            )\n\n        return OTIO.schema.Clip(\n            name=name, media_reference=media_reference, source_range=clip_range\n        )\n\n    def convert_to_uri_or_posix(self, path: Path) -&gt; str:\n        \"\"\"Convert path to URI or Posix path.\n\n        Args:\n            path (Path): Path to convert.\n\n        Returns:\n            str: Path as URI or Posix path.\n        \"\"\"\n        if self._uri_path_format.isChecked():\n            return path.as_uri()\n\n        return path.as_posix()\n\n    def get_timecode_start_frame(self, framerate, file_metadata):\n        # use otio to convert timecode into frame number\n        timecode_start_frame = OTIO.opentime.from_timecode(\n            file_metadata[\"timecode\"], framerate)\n        return timecode_start_frame.to_frames()\n\n    def export_otio(self, clips_data, output_path):\n        # first find the highest framerate and set it as default framerate\n        #   for the timeline\n        timeline_framerate = 0\n        for clip_data in clips_data.values():\n            framerate = clip_data[\"framerate\"]\n            if framerate &gt; timeline_framerate:\n                timeline_framerate = framerate\n\n        # reduce decimal places to 3 - otio does not like more\n        timeline_framerate = float(f\"{timeline_framerate:.4f}\")\n\n        # create clips from the representations\n        clips = [\n            self.create_clip(name, clip_data, timeline_framerate)\n            for name, clip_data in clips_data.items()\n        ]\n        timeline = OTIO.schema.timeline_from_clips(clips)\n\n        # set the timeline framerate to the highest framerate\n        timeline.global_start_time = OTIO.opentime.RationalTime(\n            0, timeline_framerate)\n\n        OTIO.adapters.write_to_file(timeline, output_path)\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/loader/export_otio.html#client.ayon_core.plugins.loader.export_otio.ExportOTIOOptionsDialog.convert_to_uri_or_posix","title":"<code>convert_to_uri_or_posix(path)</code>","text":"<p>Convert path to URI or Posix path.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>Path</code> <p>Path to convert.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Path as URI or Posix path.</p> Source code in <code>client/ayon_core/plugins/loader/export_otio.py</code> <pre><code>def convert_to_uri_or_posix(self, path: Path) -&gt; str:\n    \"\"\"Convert path to URI or Posix path.\n\n    Args:\n        path (Path): Path to convert.\n\n    Returns:\n        str: Path as URI or Posix path.\n    \"\"\"\n    if self._uri_path_format.isChecked():\n        return path.as_uri()\n\n    return path.as_posix()\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/loader/export_otio.html#client.ayon_core.plugins.loader.export_otio.get_image_info_metadata","title":"<code>get_image_info_metadata(path_to_file, keys=None, logger=None)</code>","text":"<p>Get flattened metadata from image file</p> <p>With combined approach via FFMPEG and OIIOTool.</p> <p>At first it will try to detect if the image input is supported by OpenImageIO. If it is then it gets the metadata from the image using OpenImageIO. If it is not supported by OpenImageIO then it will try to get the metadata using FFprobe.</p> <p>Parameters:</p> Name Type Description Default <code>path_to_file</code> <code>str</code> <p>Path to image file.</p> required <code>keys</code> <code>list[str]</code> <p>List of keys that should be returned. If None then all keys are returned. Keys are expected all lowercase. Additional keys are: - \"framerate\" - will be created from \"r_frame_rate\" or     \"framespersecond\" and evaluated to float value.</p> <code>None</code> <code>logger</code> <code>Logger</code> <p>Logger used for logging.</p> <code>None</code> Source code in <code>client/ayon_core/plugins/loader/export_otio.py</code> <pre><code>def get_image_info_metadata(\n    path_to_file,\n    keys=None,\n    logger=None,\n):\n    \"\"\"Get flattened metadata from image file\n\n    With combined approach via FFMPEG and OIIOTool.\n\n    At first it will try to detect if the image input is supported by\n    OpenImageIO. If it is then it gets the metadata from the image using\n    OpenImageIO. If it is not supported by OpenImageIO then it will try to\n    get the metadata using FFprobe.\n\n    Args:\n        path_to_file (str): Path to image file.\n        keys (list[str]): List of keys that should be returned. If None then\n            all keys are returned. Keys are expected all lowercase.\n            Additional keys are:\n            - \"framerate\" - will be created from \"r_frame_rate\" or\n                \"framespersecond\" and evaluated to float value.\n        logger (logging.Logger): Logger used for logging.\n    \"\"\"\n    if logger is None:\n        logger = logging.getLogger(__name__)\n\n    def _ffprobe_metadata_conversion(metadata):\n        \"\"\"Convert ffprobe metadata unified format.\"\"\"\n        output = {}\n        for key, val in metadata.items():\n            if key in (\"tags\", \"disposition\"):\n                output.update(val)\n            else:\n                output[key] = val\n        return output\n\n    def _get_video_metadata_from_ffprobe(ffprobe_stream):\n        \"\"\"Extract video metadata from ffprobe stream.\n\n        Args:\n            ffprobe_stream (dict): Stream data obtained from ffprobe.\n\n        Returns:\n            dict: Video metadata extracted from the ffprobe stream.\n        \"\"\"\n        video_stream = None\n        for stream in ffprobe_stream[\"streams\"]:\n            if stream[\"codec_type\"] == \"video\":\n                video_stream = stream\n                break\n        metadata_stream = _ffprobe_metadata_conversion(video_stream)\n        return metadata_stream\n\n    metadata_stream = None\n    ext = os.path.splitext(path_to_file)[-1].lower()\n    if ext not in IMAGE_EXTENSIONS:\n        logger.info(\n            (\n                'File extension \"{}\" is not supported by OpenImageIO.'\n                \" Trying to get metadata using FFprobe.\"\n            ).format(ext)\n        )\n        ffprobe_stream = get_ffprobe_data(path_to_file, logger)\n        if \"streams\" in ffprobe_stream and len(ffprobe_stream[\"streams\"]) &gt; 0:\n            metadata_stream = _get_video_metadata_from_ffprobe(ffprobe_stream)\n\n    if not metadata_stream and is_oiio_supported():\n        oiio_stream = get_oiio_info_for_input(path_to_file, logger=logger)\n        if \"attribs\" in (oiio_stream or {}):\n            metadata_stream = {}\n            for key, val in oiio_stream[\"attribs\"].items():\n                if \"smpte:\" in key.lower():\n                    key = key.replace(\"smpte:\", \"\")\n                metadata_stream[key.lower()] = val\n            for key, val in oiio_stream.items():\n                if key == \"attribs\":\n                    continue\n                metadata_stream[key] = val\n    else:\n        logger.info(\n            (\n                \"OpenImageIO is not supported on this system.\"\n                \" Trying to get metadata using FFprobe.\"\n            )\n        )\n        ffprobe_stream = get_ffprobe_data(path_to_file, logger)\n        if \"streams\" in ffprobe_stream and len(ffprobe_stream[\"streams\"]) &gt; 0:\n            metadata_stream = _get_video_metadata_from_ffprobe(ffprobe_stream)\n\n    if not metadata_stream:\n        logger.warning(\"Failed to get metadata from image file.\")\n        return {}\n\n    if keys is None:\n        return metadata_stream\n\n    # create framerate key from available ffmpeg:r_frame_rate\n    # or oiiotool:framespersecond and evaluate its string expression\n    # value into flaot value\n    if (\n        \"r_frame_rate\" in metadata_stream\n        or \"framespersecond\" in metadata_stream\n    ):\n        rate_info = metadata_stream.get(\"r_frame_rate\")\n        if rate_info is None:\n            rate_info = metadata_stream.get(\"framespersecond\")\n\n        # calculate framerate from string expression\n        if \"/\" in str(rate_info):\n            time, frame = str(rate_info).split(\"/\")\n            rate_info = float(time) / float(frame)\n\n        try:\n            metadata_stream[\"framerate\"] = float(str(rate_info))\n        except Exception as e:\n            logger.warning(\n                \"Failed to evaluate '{}' value to framerate. Error: {}\".format(\n                    rate_info, e\n                )\n            )\n\n    # aggregate all required metadata from prepared metadata stream\n    output = {}\n    for key in keys:\n        for k, v in metadata_stream.items():\n            if key == k:\n                output[key] = v\n                break\n            if isinstance(v, dict) and key in v:\n                output[key] = v[key]\n                break\n\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/loader/open_file.html","title":"open_file","text":""},{"location":"autoapi/client/ayon_core/plugins/loader/open_file.html#client.ayon_core.plugins.loader.open_file.OpenFileAction","title":"<code>OpenFileAction</code>","text":"<p>               Bases: <code>LoaderActionPlugin</code></p> <p>Open Image Sequence or Video with system default</p> Source code in <code>client/ayon_core/plugins/loader/open_file.py</code> <pre><code>class OpenFileAction(LoaderActionPlugin):\n    \"\"\"Open Image Sequence or Video with system default\"\"\"\n    identifier = \"core.open-file\"\n\n    def get_action_items(\n        self, selection: LoaderActionSelection\n    ) -&gt; list[LoaderActionItem]:\n        repres = []\n        if selection.selected_type == \"representation\":\n            repres = selection.entities.get_representations(\n                selection.selected_ids\n            )\n\n        if selection.selected_type == \"version\":\n            repres = selection.entities.get_versions_representations(\n                selection.selected_ids\n            )\n\n        if not repres:\n            return []\n\n        repres_by_ext = collections.defaultdict(list)\n        for repre in repres:\n            repre_context = repre.get(\"context\")\n            if not repre_context:\n                continue\n            ext = repre_context.get(\"ext\")\n            if not ext:\n                path = repre[\"attrib\"].get(\"path\")\n                if path:\n                    ext = os.path.splitext(path)[1]\n\n            if ext:\n                ext = ext.lower()\n                if not ext.startswith(\".\"):\n                    ext = f\".{ext}\"\n                repres_by_ext[ext.lower()].append(repre)\n\n        if not repres_by_ext:\n            return []\n\n        filtered_exts = filter_supported_exts(set(repres_by_ext))\n\n        repre_ids_by_name = collections.defaultdict(set)\n        for ext in filtered_exts:\n            for repre in repres_by_ext[ext]:\n                repre_ids_by_name[repre[\"name\"]].add(repre[\"id\"])\n\n        return [\n            LoaderActionItem(\n                label=repre_name,\n                group_label=\"Open file\",\n                order=30,\n                data={\"representation_ids\": list(repre_ids)},\n                icon=MaterialSymbolsIcon(\n                    \"file_open\",\n                    color=\"#ffffff\",\n                )\n            )\n            for repre_name, repre_ids in repre_ids_by_name.items()\n        ]\n\n    def execute_action(\n        self,\n        selection: LoaderActionSelection,\n        data: dict[str, Any],\n        form_values: dict[str, Any],\n    ) -&gt; Optional[LoaderActionResult]:\n        path = None\n        repre_path = None\n        repre_ids = data[\"representation_ids\"]\n        for repre in selection.entities.get_representations(repre_ids):\n            repre_path = get_representation_path_with_anatomy(\n                repre, selection.get_project_anatomy()\n            )\n            if os.path.exists(repre_path):\n                path = repre_path\n                break\n\n        if path is None:\n            if repre_path is None:\n                return LoaderActionResult(\n                    \"Failed to fill representation path...\",\n                    success=False,\n                )\n            return LoaderActionResult(\n                \"File to open was not found...\",\n                success=False,\n            )\n\n        self.log.info(f\"Opening: {path}\")\n\n        open_file(path)\n\n        return LoaderActionResult(\n            \"File was opened...\",\n            success=True,\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/loader/open_file.html#client.ayon_core.plugins.loader.open_file.open_file","title":"<code>open_file(filepath)</code>","text":"<p>Open file with system default executable</p> Source code in <code>client/ayon_core/plugins/loader/open_file.py</code> <pre><code>def open_file(filepath: str) -&gt; None:\n    \"\"\"Open file with system default executable\"\"\"\n    if sys.platform.startswith(\"darwin\"):\n        subprocess.call((\"open\", filepath))\n    elif os.name == \"nt\":\n        os.startfile(filepath)\n    elif os.name == \"posix\":\n        subprocess.call((\"xdg-open\", filepath))\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/loader/push_to_project.html","title":"push_to_project","text":""},{"location":"autoapi/client/ayon_core/plugins/publish/index.html","title":"publish","text":""},{"location":"autoapi/client/ayon_core/plugins/publish/cleanup.html","title":"cleanup","text":"<p>Cleanup leftover files from publish.</p>"},{"location":"autoapi/client/ayon_core/plugins/publish/cleanup.html#client.ayon_core.plugins.publish.cleanup.CleanUp","title":"<code>CleanUp</code>","text":"<p>               Bases: <code>InstancePlugin</code></p> <p>Cleans up the staging directory after a successful publish.</p> <p>This will also clean published renders and delete their parent directories.</p> Source code in <code>client/ayon_core/plugins/publish/cleanup.py</code> <pre><code>class CleanUp(pyblish.api.InstancePlugin):\n    \"\"\"Cleans up the staging directory after a successful publish.\n\n    This will also clean published renders and delete their parent directories.\n\n    \"\"\"\n\n    order = pyblish.api.IntegratorOrder + 10\n    label = \"Clean Up\"\n    settings_category = \"core\"\n\n    exclude_families = [\"clip\"]\n    optional = True\n    active = True\n\n    # Presets\n    patterns = None  # list of regex patterns\n    remove_temp_renders = True\n\n    def process(self, instance):\n        \"\"\"Plugin entry point.\"\"\"\n        if is_in_tests():\n            # let automatic test process clean up temporary data\n            return\n\n        # If instance has errors, do not clean up\n        for result in instance.context.data[\"results\"]:\n            if result[\"error\"] is not None and result[\"instance\"] is instance:\n                raise PublishError(\n                    \"Result of '{}' instance were not success\".format(\n                        instance.data[\"name\"]\n                    )\n                )\n\n        _skip_cleanup_filepaths = instance.context.data.get(\n            \"skipCleanupFilepaths\"\n        ) or []\n        skip_cleanup_filepaths = set()\n        for path in _skip_cleanup_filepaths:\n            skip_cleanup_filepaths.add(os.path.normpath(path))\n\n        if self.remove_temp_renders:\n            self.log.debug(\"Cleaning renders new...\")\n            self.clean_renders(instance, skip_cleanup_filepaths)\n\n        # TODO: Figure out whether this could be refactored to just a\n        #  product_type in self.exclude_families check.\n        product_base_type = instance.data.get(\"productBaseType\")\n        if not product_base_type:\n            product_base_type = instance.data[\"productType\"]\n\n        if any(\n            product_base_type in exclude_family\n            for exclude_family in self.exclude_families\n        ):\n            self.log.debug(\n                \"Skipping cleanup for instance because product base\"\n                f\" type is excluded from cleanup: {product_base_type}\")\n            return\n\n        temp_root = tempfile.gettempdir()\n        staging_dir = instance.data.get(\"stagingDir\", None)\n\n        if not staging_dir:\n            self.log.debug(\"Skipping cleanup. Staging dir not set \"\n                           \"on instance: {}.\".format(instance))\n            return\n\n        if not os.path.normpath(staging_dir).startswith(temp_root):\n            self.log.debug(\"Skipping cleanup. Staging directory is not in the \"\n                           \"temp folder: %s\" % staging_dir)\n            return\n\n        if not os.path.exists(staging_dir):\n            self.log.debug(\"No staging directory found at: %s\" % staging_dir)\n            return\n\n        if instance.data.get(\"stagingDir_persistent\"):\n            self.log.debug(\n                \"Staging dir {} should be persistent\".format(staging_dir)\n            )\n            return\n\n        self.log.debug(\"Removing staging directory {}\".format(staging_dir))\n        shutil.rmtree(staging_dir)\n\n    def clean_renders(self, instance, skip_cleanup_filepaths):\n        transfers = instance.data.get(\"transfers\", list())\n\n        instance_families = instance.data.get(\"families\", list())\n        instance_product_base_type = instance.data.get(\"productBaseType\")\n        if not instance_product_base_type:\n            instance_product_base_type = instance.data.get(\"productType\")\n        dirnames = []\n        transfers_dirs = []\n\n        for src, dest in transfers:\n            # fix path inconsistency\n            src = os.path.normpath(src)\n            dest = os.path.normpath(dest)\n\n            # add src dir into clearing dir paths (regex patterns)\n            transfers_dirs.append(os.path.dirname(src))\n\n            # add dest dir into clearing dir paths (regex patterns)\n            transfers_dirs.append(os.path.dirname(dest))\n\n            if src in skip_cleanup_filepaths:\n                self.log.debug((\n                    \"Source file is marked to be skipped in cleanup. {}\"\n                ).format(src))\n                continue\n\n            if os.path.normpath(src) == os.path.normpath(dest):\n                continue\n\n            if (\n                instance_product_base_type == \"render\"\n                or \"render\" in instance_families\n            ):\n                self.log.info(\"Removing src: `{}`...\".format(src))\n                try:\n                    os.remove(src)\n                except PermissionError:\n                    self.log.warning(\n                        \"Insufficient permission to delete {}\".format(src)\n                    )\n                    continue\n\n                # add dir for cleanup\n                dirnames.append(os.path.dirname(src))\n\n        # clean by regex patterns\n        # make unique set\n        transfers_dirs = set(transfers_dirs)\n\n        self.log.debug(\"__ transfers_dirs: `{}`\".format(transfers_dirs))\n        self.log.debug(\"__ self.patterns: `{}`\".format(self.patterns))\n        if self.patterns:\n            files = list()\n            # get list of all available content of dirs\n            for _dir in transfers_dirs:\n                if not os.path.exists(_dir):\n                    continue\n                files.extend([\n                    os.path.join(_dir, f)\n                    for f in os.listdir(_dir)])\n\n            self.log.debug(\"__ files: `{}`\".format(files))\n\n            # remove all files which match regex pattern\n            for f in files:\n                if os.path.normpath(f) in skip_cleanup_filepaths:\n                    continue\n\n                for p in self.patterns:\n                    pattern = re.compile(p)\n                    if not pattern.findall(f):\n                        continue\n                    if not os.path.exists(f):\n                        continue\n\n                    self.log.info(\"Removing file by regex: `{}`\".format(f))\n                    os.remove(f)\n\n                    # add dir for cleanup\n                    dirnames.append(os.path.dirname(f))\n\n        # make unique set\n        cleanup_dirs = set(dirnames)\n\n        # clean dirs which are empty\n        for dir in cleanup_dirs:\n            try:\n                os.rmdir(dir)\n            except OSError:\n                # directory is not empty, skipping\n                continue\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/cleanup.html#client.ayon_core.plugins.publish.cleanup.CleanUp.process","title":"<code>process(instance)</code>","text":"<p>Plugin entry point.</p> Source code in <code>client/ayon_core/plugins/publish/cleanup.py</code> <pre><code>def process(self, instance):\n    \"\"\"Plugin entry point.\"\"\"\n    if is_in_tests():\n        # let automatic test process clean up temporary data\n        return\n\n    # If instance has errors, do not clean up\n    for result in instance.context.data[\"results\"]:\n        if result[\"error\"] is not None and result[\"instance\"] is instance:\n            raise PublishError(\n                \"Result of '{}' instance were not success\".format(\n                    instance.data[\"name\"]\n                )\n            )\n\n    _skip_cleanup_filepaths = instance.context.data.get(\n        \"skipCleanupFilepaths\"\n    ) or []\n    skip_cleanup_filepaths = set()\n    for path in _skip_cleanup_filepaths:\n        skip_cleanup_filepaths.add(os.path.normpath(path))\n\n    if self.remove_temp_renders:\n        self.log.debug(\"Cleaning renders new...\")\n        self.clean_renders(instance, skip_cleanup_filepaths)\n\n    # TODO: Figure out whether this could be refactored to just a\n    #  product_type in self.exclude_families check.\n    product_base_type = instance.data.get(\"productBaseType\")\n    if not product_base_type:\n        product_base_type = instance.data[\"productType\"]\n\n    if any(\n        product_base_type in exclude_family\n        for exclude_family in self.exclude_families\n    ):\n        self.log.debug(\n            \"Skipping cleanup for instance because product base\"\n            f\" type is excluded from cleanup: {product_base_type}\")\n        return\n\n    temp_root = tempfile.gettempdir()\n    staging_dir = instance.data.get(\"stagingDir\", None)\n\n    if not staging_dir:\n        self.log.debug(\"Skipping cleanup. Staging dir not set \"\n                       \"on instance: {}.\".format(instance))\n        return\n\n    if not os.path.normpath(staging_dir).startswith(temp_root):\n        self.log.debug(\"Skipping cleanup. Staging directory is not in the \"\n                       \"temp folder: %s\" % staging_dir)\n        return\n\n    if not os.path.exists(staging_dir):\n        self.log.debug(\"No staging directory found at: %s\" % staging_dir)\n        return\n\n    if instance.data.get(\"stagingDir_persistent\"):\n        self.log.debug(\n            \"Staging dir {} should be persistent\".format(staging_dir)\n        )\n        return\n\n    self.log.debug(\"Removing staging directory {}\".format(staging_dir))\n    shutil.rmtree(staging_dir)\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/cleanup_explicit.html","title":"cleanup_explicit","text":"<p>Cleanup files when publishing is done.</p>"},{"location":"autoapi/client/ayon_core/plugins/publish/cleanup_explicit.html#client.ayon_core.plugins.publish.cleanup_explicit.ExplicitCleanUp","title":"<code>ExplicitCleanUp</code>","text":"<p>               Bases: <code>ContextPlugin</code></p> <p>Cleans up the files and folder defined to be deleted.</p> <p>plugin is looking for 2 keys into context data: - <code>cleanupFullPaths</code> - full paths that should be removed not matter if     is path to file or to directory - <code>cleanupEmptyDirs</code> - full paths to directories that should be removed     only if do not contain any file in it but will be removed if contain     sub-folders</p> Source code in <code>client/ayon_core/plugins/publish/cleanup_explicit.py</code> <pre><code>class ExplicitCleanUp(pyblish.api.ContextPlugin):\n    \"\"\"Cleans up the files and folder defined to be deleted.\n\n    plugin is looking for 2 keys into context data:\n    - `cleanupFullPaths` - full paths that should be removed not matter if\n        is path to file or to directory\n    - `cleanupEmptyDirs` - full paths to directories that should be removed\n        only if do not contain any file in it but will be removed if contain\n        sub-folders\n    \"\"\"\n\n    order = pyblish.api.IntegratorOrder + 10\n    label = \"Explicit Clean Up\"\n    optional = True\n    active = True\n\n    def process(self, context):\n        cleanup_full_paths = context.data.get(\"cleanupFullPaths\")\n        cleanup_empty_dirs = context.data.get(\"cleanupEmptyDirs\")\n\n        self._remove_full_paths(cleanup_full_paths)\n        self._remove_empty_dirs(cleanup_empty_dirs)\n\n    def _remove_full_paths(self, full_paths):\n        \"\"\"Remove files and folders from disc.\n\n        Folders are removed with whole content.\n        \"\"\"\n        if not full_paths:\n            self.log.debug(\"No full paths to cleanup were collected.\")\n            return\n\n        # Separate paths into files and directories\n        filepaths = set()\n        dirpaths = set()\n        for path in full_paths:\n            # Skip empty items\n            if not path:\n                continue\n            # Normalize path\n            normalized = os.path.normpath(path)\n            # Check if path exists\n            if not os.path.exists(normalized):\n                continue\n\n            if os.path.isfile(normalized):\n                filepaths.add(normalized)\n            else:\n                dirpaths.add(normalized)\n\n        # Store failed paths with exception\n        failed = []\n        # Store removed filepaths for logging\n        succeeded_files = set()\n        # Remove file by file\n        for filepath in filepaths:\n            try:\n                os.remove(filepath)\n                succeeded_files.add(filepath)\n            except Exception as exc:\n                failed.append((filepath, exc))\n\n        if succeeded_files:\n            self.log.info(\n                \"Removed files:\\n{}\".format(\"\\n\".join(sorted(succeeded_files)))\n            )\n\n        # Delete folders with its content\n        succeeded = set()\n        for dirpath in dirpaths:\n            # Check if directory still exists\n            #   - it is possible that directory was already deleted with\n            #       different dirpath to delete\n            if os.path.exists(dirpath):\n                try:\n                    shutil.rmtree(dirpath)\n                    succeeded.add(dirpath)\n                except Exception:\n                    failed.append(dirpath)\n\n        if succeeded:\n            self.log.info(\n                \"Removed directories:\\n{}\".format(\n                    \"\\n\".join(sorted(succeeded))\n                )\n            )\n\n        # Prepare lines for report of failed removals\n        lines = []\n        for filepath, exc in failed:\n            lines.append(\"{}: {}\".format(filepath, str(exc)))\n\n        if lines:\n            self.log.warning(\n                \"Failed to remove filepaths:\\n{}\".format(\n                    \"\\n\".join(sorted(lines))\n                )\n            )\n\n    def _remove_empty_dirs(self, empty_dirpaths):\n        \"\"\"Remove directories if do not contain any files.\"\"\"\n        if not empty_dirpaths:\n            self.log.debug(\"No empty dirs to cleanup were collected.\")\n            return\n\n        # First filtering of directories and making sure those are\n        #   existing directories\n        filtered_dirpaths = set()\n        for path in empty_dirpaths:\n            if (\n                path\n                and os.path.exists(path)\n                and os.path.isdir(path)\n            ):\n                filtered_dirpaths.add(os.path.normpath(path))\n\n        to_delete_dirpaths = set()\n        to_skip_dirpaths = set()\n        # Check if contain any files (or it's subfolders contain files)\n        for dirpath in filtered_dirpaths:\n            valid = True\n            for _, _, filenames in os.walk(dirpath):\n                if filenames:\n                    valid = False\n                    break\n\n            if valid:\n                to_delete_dirpaths.add(dirpath)\n            else:\n                to_skip_dirpaths.add(dirpath)\n\n        if to_skip_dirpaths:\n            self.log.debug(\n                \"Skipped directories because they contain files:\\n{}\".format(\n                    \"\\n\".join(sorted(to_skip_dirpaths))\n                )\n            )\n\n        # Remove empty directies\n        for dirpath in to_delete_dirpaths:\n            if os.path.exists(dirpath):\n                shutil.rmtree(dirpath)\n\n        if to_delete_dirpaths:\n            self.log.debug(\n                \"Deleted empty directories:\\n{}\".format(\n                    \"\\n\".join(sorted(to_delete_dirpaths))\n                )\n            )\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/cleanup_farm.html","title":"cleanup_farm","text":"<p>Cleanup leftover files from publish.</p>"},{"location":"autoapi/client/ayon_core/plugins/publish/cleanup_farm.html#client.ayon_core.plugins.publish.cleanup_farm.CleanUpFarm","title":"<code>CleanUpFarm</code>","text":"<p>               Bases: <code>ContextPlugin</code></p> <p>Cleans up the staging directory after a successful publish.</p> <p>This will also clean published renders and delete their parent directories.</p> Source code in <code>client/ayon_core/plugins/publish/cleanup_farm.py</code> <pre><code>class CleanUpFarm(pyblish.api.ContextPlugin):\n    \"\"\"Cleans up the staging directory after a successful publish.\n\n    This will also clean published renders and delete their parent directories.\n    \"\"\"\n\n    order = pyblish.api.IntegratorOrder + 11\n    label = \"Clean Up Farm\"\n\n    settings_category = \"core\"\n    enabled = True\n\n    # Keep \"filesequence\" for backwards compatibility of older jobs\n    targets = [\"filesequence\", \"farm\"]\n    allowed_hosts = (\"maya\", )\n\n    def process(self, context):\n        # Get source host from which farm publishing was started\n        src_host_name = context.data[\"hostName\"]\n        self.log.debug(\"Host name from context is {}\".format(src_host_name))\n        # Skip process if is not in list of source hosts in which this\n        #    plugin should run\n        if src_host_name not in self.allowed_hosts:\n            self.log.debug(\n                \"Source host \\\"{}\\\" is not in list of enabled hosts {}.\"\n                \" Skipping\".format(src_host_name, self.allowed_hosts)\n            )\n            return\n\n        self.log.debug(\"Preparing filepaths to remove\")\n        # Collect directories to remove\n        dirpaths_to_remove = set()\n        for instance in context:\n            staging_dir = instance.data.get(\"stagingDir\")\n            if staging_dir and not instance.data.get(\"stagingDir_persistent\"):\n                dirpaths_to_remove.add(os.path.normpath(staging_dir))\n\n            if \"representations\" in instance.data:\n                for repre in instance.data[\"representations\"]:\n                    staging_dir = repre.get(\"stagingDir\")\n                    if staging_dir:\n                        dirpaths_to_remove.add(os.path.normpath(staging_dir))\n\n        if not dirpaths_to_remove:\n            self.log.debug(\"Nothing to remove. Skipping\")\n            return\n\n        self.log.debug(\"Filepaths to remove are:\\n{}\".format(\n            \"\\n\".join([\"- {}\".format(path) for path in dirpaths_to_remove])\n        ))\n\n        # clean dirs which are empty\n        for dirpath in dirpaths_to_remove:\n            if not os.path.exists(dirpath):\n                self.log.debug(\"Skipping not existing directory \\\"{}\\\"\".format(\n                    dirpath\n                ))\n                continue\n\n            self.log.debug(\"Removing directory \\\"{}\\\"\".format(dirpath))\n            try:\n                shutil.rmtree(dirpath)\n            except OSError:\n                self.log.warning(\n                    \"Failed to remove directory \\\"{}\\\"\".format(dirpath),\n                    exc_info=True\n                )\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_addons.html","title":"collect_addons","text":"<p>Collect AYON addons.</p>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_addons.html#client.ayon_core.plugins.publish.collect_addons.AddonInfo","title":"<code>AddonInfo</code>  <code>dataclass</code>","text":"Source code in <code>client/ayon_core/plugins/publish/collect_addons.py</code> <pre><code>@dataclass\nclass AddonInfo:\n    name: str\n    version: str | None\n    server_version: str | None\n    label: str | None = None\n\n    def get_row(\n        self, name_width: int, version_width: int, server_version_width: int\n    ) -&gt; str:\n        \"\"\"Return formatted row for the addon info.\"\"\"\n        version = self.version or \"-\"\n        server_version = self.server_version or \"-\"\n        label = self.label or self.name\n\n        return (\n            f\"{label:&lt;{name_width}}\"\n            f\" | {version:&lt;{version_width}}\"\n            f\" | {server_version:&lt;{server_version_width}}\"\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_addons.html#client.ayon_core.plugins.publish.collect_addons.AddonInfo.get_row","title":"<code>get_row(name_width, version_width, server_version_width)</code>","text":"<p>Return formatted row for the addon info.</p> Source code in <code>client/ayon_core/plugins/publish/collect_addons.py</code> <pre><code>def get_row(\n    self, name_width: int, version_width: int, server_version_width: int\n) -&gt; str:\n    \"\"\"Return formatted row for the addon info.\"\"\"\n    version = self.version or \"-\"\n    server_version = self.server_version or \"-\"\n    label = self.label or self.name\n\n    return (\n        f\"{label:&lt;{name_width}}\"\n        f\" | {version:&lt;{version_width}}\"\n        f\" | {server_version:&lt;{server_version_width}}\"\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_addons.html#client.ayon_core.plugins.publish.collect_addons.CollectAddons","title":"<code>CollectAddons</code>","text":"<p>               Bases: <code>ContextPlugin</code></p> <p>Collect AYON addons.</p> Source code in <code>client/ayon_core/plugins/publish/collect_addons.py</code> <pre><code>class CollectAddons(pyblish.api.ContextPlugin):\n    \"\"\"Collect AYON addons.\"\"\"\n\n    order = pyblish.api.CollectorOrder - 0.5\n    label = \"AYON Addons\"\n\n    def process(self, context):\n        manager = AddonsManager()\n        context.data[\"ayonAddonsManager\"] = manager\n        context.data[\"ayonAddons\"] = manager.addons_by_name\n\n        bundle_info = get_bundle_information()\n        server_version_by_name = {\n            addon.name: addon.version\n            for addon in bundle_info.addons\n        }\n        server_only_addons = set(server_version_by_name)\n\n        _addons = [\n            (addon.name, addon.version or \"N/A\")\n            for addon in manager.addons\n        ]\n        _addons.append((\"core\", __version__))\n\n        items: list[AddonInfo] = []\n        title_name = \"Name\"\n        title_version = \"Version\"\n        title_server_version = \"Server Version\"\n        name_width = len(title_name)\n        version_width = len(title_version)\n        server_version_width = len(title_server_version)\n        for addon_name, addon_version in _addons:\n            server_version = server_version_by_name.get(addon_name)\n            server_only_addons.discard(addon_name)\n            label = addon_name\n            if addon_version != server_version:\n                label = f\"* {addon_name}\"\n            name_width = max(name_width, len(label))\n            version_width = max(version_width, len(addon_version))\n            if server_version is not None:\n                server_version_width = max(\n                    server_version_width, len(server_version)\n                )\n\n            items.append(AddonInfo(\n                addon_name,\n                addon_version,\n                server_version,\n                label,\n            ))\n\n        for addon_name in server_only_addons:\n            server_version = server_version_by_name[addon_name]\n            name_width = max(name_width, len(addon_name))\n            server_version_width = max(\n                server_version_width, len(server_version)\n            )\n            items.append(AddonInfo(\n                addon_name,\n                None,\n                server_version,\n            ))\n\n        items.sort(key=lambda x: x.name)\n\n        title = (\n            f\"{title_name:&lt;{name_width}}\"\n            f\" | {title_version:&lt;{version_width}}\"\n            f\" | {title_server_version:&lt;{server_version_width}}\"\n        )\n        bundle_name = os.getenv(\"AYON_BUNDLE_NAME\")\n        if is_dev_mode_enabled():\n            settings_variant = \"dev\"\n        else:\n            settings_variant = get_settings_variant()\n\n        server_version = ayon_api.get_server_version()\n\n        ayon_info = get_ayon_info()\n        launcher_version = ayon_info[\"ayon_launcher_version\"]\n        launcher_type = ayon_info[\"version_type\"]\n        sep_line = \"+\".join([\n            (name_width + 1) * \"-\",\n            (version_width + 2) * \"-\",\n            (server_version_width + 2) * \"-\"\n        ])\n        lines = [\n            \"Basic AYON information:\",\n            f\"AYON server: {server_version}\",\n            f\"Bundle: {bundle_name} ({settings_variant})\",\n            f\"AYON launcher: {launcher_version} ({launcher_type})\",\n            \"Addons:\",\n            sep_line,\n            title,\n            sep_line,\n        ]\n        lines.extend(\n            item.get_row(name_width, version_width, server_version_width)\n            for item in items\n        )\n        lines.append(sep_line)\n\n        self.log.debug(\"\\n\".join(lines))\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_anatomy_context_data.html","title":"collect_anatomy_context_data","text":"<p>Collect global context Anatomy data.</p> Requires <p>context -&gt; anatomy context -&gt; projectEntity context -&gt; folderEntity context -&gt; taskEntity context -&gt; task context -&gt; username context -&gt; datetimeData</p> Provides <p>context -&gt; anatomyData</p>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_anatomy_context_data.html#client.ayon_core.plugins.publish.collect_anatomy_context_data.CollectAnatomyContextData","title":"<code>CollectAnatomyContextData</code>","text":"<p>               Bases: <code>ContextPlugin</code></p> <p>Collect Anatomy Context data.</p> <p>Example: context.data[\"anatomyData\"] = {     \"project\": {         \"name\": \"MyProject\",         \"code\": \"myproj\"     },     \"asset\": \"AssetName\",     \"hierarchy\": \"path/to/asset\",     \"task\": \"Working\",     \"user\": \"MeDespicable\",     # Duplicated entry     \"username\": \"MeDespicable\",</p> <pre><code># Current host name\n\"app\": \"maya\"\n\n*** OPTIONAL ***\n+ mutliple keys from `datetimeData` (See it's collector)\n</code></pre> <p>}</p> Source code in <code>client/ayon_core/plugins/publish/collect_anatomy_context_data.py</code> <pre><code>class CollectAnatomyContextData(pyblish.api.ContextPlugin):\n    \"\"\"Collect Anatomy Context data.\n\n    Example:\n    context.data[\"anatomyData\"] = {\n        \"project\": {\n            \"name\": \"MyProject\",\n            \"code\": \"myproj\"\n        },\n        \"asset\": \"AssetName\",\n        \"hierarchy\": \"path/to/asset\",\n        \"task\": \"Working\",\n        \"user\": \"MeDespicable\",\n        # Duplicated entry\n        \"username\": \"MeDespicable\",\n\n        # Current host name\n        \"app\": \"maya\"\n\n        *** OPTIONAL ***\n        + mutliple keys from `datetimeData` (See it's collector)\n    }\n    \"\"\"\n\n    order = pyblish.api.CollectorOrder + 0.002\n    label = \"Collect Anatomy Context Data\"\n\n    def process(self, context):\n        host_name = context.data[\"hostName\"]\n        project_settings = context.data[\"project_settings\"]\n        project_entity = context.data[\"projectEntity\"]\n        folder_entity = context.data.get(\"folderEntity\")\n        task_entity = None\n        if folder_entity:\n            task_entity = context.data[\"taskEntity\"]\n\n        username = context.data[\"user\"]\n        user_entity = get_ayon_user_entity(username)\n        anatomy_data = get_template_data(\n            project_entity,\n            folder_entity,\n            task_entity,\n            host_name=host_name,\n            settings=project_settings,\n            user_entity=user_entity,\n        )\n        anatomy_data.update(context.data.get(\"datetimeData\") or {})\n\n        # Backwards compatibility for 'username' key\n        anatomy_data[\"username\"] = username\n\n        # Store\n        context.data[\"anatomyData\"] = anatomy_data\n\n        self.log.debug(\"Global Anatomy Context Data collected:\\n{}\".format(\n            json.dumps(anatomy_data, indent=4)\n        ))\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_anatomy_instance_data.html","title":"collect_anatomy_instance_data","text":"Requires <p>context     -&gt; projectName context     -&gt; projectEntity context     -&gt; anatomyData instance    -&gt; folderPath instance    -&gt; productName instance    -&gt; productBaseType instance    -&gt; productType</p> Optional <p>context     -&gt; folderEntity context     -&gt; taskEntity instance    -&gt; task instance    -&gt; taskEntity instance    -&gt; version instance    -&gt; resolutionWidth instance    -&gt; resolutionHeight instance    -&gt; fps</p> Provides <p>instance    -&gt; projectEntity instance    -&gt; folderEntity instance    -&gt; taskEntity instance    -&gt; anatomyData instance    -&gt; version instance    -&gt; latestVersion</p>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_anatomy_instance_data.html#client.ayon_core.plugins.publish.collect_anatomy_instance_data.CollectAnatomyInstanceData","title":"<code>CollectAnatomyInstanceData</code>","text":"<p>               Bases: <code>ContextPlugin</code></p> <p>Collect Instance specific Anatomy data.</p> <p>Plugin is running for all instances on context even not active instances.</p> Source code in <code>client/ayon_core/plugins/publish/collect_anatomy_instance_data.py</code> <pre><code>class CollectAnatomyInstanceData(pyblish.api.ContextPlugin):\n    \"\"\"Collect Instance specific Anatomy data.\n\n    Plugin is running for all instances on context even not active instances.\n    \"\"\"\n\n    order = pyblish.api.CollectorOrder + 0.49\n    label = \"Collect Anatomy Instance data\"\n\n    settings_category = \"core\"\n\n    follow_workfile_version = False\n\n    def process(self, context):\n        self.log.debug(\"Collecting anatomy data for all instances.\")\n\n        project_name = context.data[\"projectName\"]\n        self.fill_missing_folder_entities(context, project_name)\n        self.fill_missing_task_entities(context, project_name)\n        self.fill_latest_versions(context, project_name)\n        self.fill_anatomy_data(context)\n\n        self.log.debug(\"Anatomy Data collection finished.\")\n\n    def fill_missing_folder_entities(self, context, project_name):\n        self.log.debug(\"Querying folder entities for instances.\")\n\n        context_folder_entity = context.data.get(\"folderEntity\")\n        context_folder_path = None\n        if context_folder_entity:\n            context_folder_path = context_folder_entity[\"path\"]\n\n        instances_missing_folder = collections.defaultdict(list)\n        for instance in context:\n            instance_folder_entity = instance.data.get(\"folderEntity\")\n            _folder_path = instance.data[\"folderPath\"]\n\n            # There is possibility that folderEntity on instance is set\n            if instance_folder_entity:\n                instance_folder_path = instance_folder_entity[\"path\"]\n                if instance_folder_path == _folder_path:\n                    continue\n\n            # Check if folder path is the same as what is in context\n            # - they may be different, e.g. during editorial publishing\n            if context_folder_path and context_folder_path == _folder_path:\n                instance.data[\"folderEntity\"] = context_folder_entity\n\n            else:\n                instances_missing_folder[_folder_path].append(\n                    instance\n                )\n\n        if not instances_missing_folder:\n            self.log.debug(\"All instances already had right folder entity.\")\n            return\n\n        folder_paths = list(instances_missing_folder.keys())\n        self.log.debug(\"Querying folder entities with paths: {}\".format(\n            \", \".join([\"\\\"{}\\\"\".format(path) for path in folder_paths])\n        ))\n\n        folder_entities_by_path = {\n            folder_entity[\"path\"]: folder_entity\n            for folder_entity in ayon_api.get_folders(\n                project_name, folder_paths=folder_paths\n            )\n        }\n\n        not_found_folder_paths = []\n        for folder_path, instances in instances_missing_folder.items():\n            folder_entity = folder_entities_by_path.get(folder_path)\n            if not folder_entity:\n                not_found_folder_paths.append(folder_path)\n                continue\n\n            for _instance in instances:\n                _instance.data[\"folderEntity\"] = folder_entity\n\n        if not_found_folder_paths:\n            joined_folder_paths = \", \".join(\n                [f\"\\\"{path}\\\"\" for path in not_found_folder_paths]\n            )\n            self.log.warning(\n                f\"Not found folder entities with paths {joined_folder_paths}.\"\n            )\n\n    def fill_missing_task_entities(self, context, project_name):\n        self.log.debug(\"Querying task entities for instances.\")\n\n        context_folder_entity = context.data.get(\"folderEntity\")\n        context_folder_id = None\n        if context_folder_entity:\n            context_folder_id = context_folder_entity[\"id\"]\n        context_task_entity = context.data.get(\"taskEntity\")\n        context_task_name = None\n        if context_task_entity:\n            context_task_name = context_task_entity[\"name\"]\n\n        instances_missing_task = {}\n        folder_path_by_id = {}\n        for instance in context:\n            folder_entity = instance.data.get(\"folderEntity\")\n            # Skip if instance does not have filled folder entity\n            if not folder_entity:\n                continue\n            folder_id = folder_entity[\"id\"]\n            folder_path_by_id[folder_id] = folder_entity[\"path\"]\n\n            task_entity = instance.data.get(\"taskEntity\")\n            _task_name = instance.data.get(\"task\")\n\n            # There is possibility that taskEntity on instance is set\n            if task_entity:\n                task_parent_id = task_entity[\"folderId\"]\n                instance_task_name = task_entity[\"name\"]\n                if (\n                    folder_id == task_parent_id\n                    and instance_task_name == _task_name\n                ):\n                    continue\n\n            # Check if folder path is the same as what is in context\n            # - they may be different, e.g. in NukeStudio\n            if (\n                context_folder_id == folder_id\n                and context_task_name == _task_name\n            ):\n                instance.data[\"taskEntity\"] = context_task_entity\n                continue\n\n            _by_folder_id = instances_missing_task.setdefault(folder_id, {})\n            _by_task_name = _by_folder_id.setdefault(_task_name, [])\n            _by_task_name.append(instance)\n\n        if not instances_missing_task:\n            self.log.debug(\"All instances already had right task entity.\")\n            return\n\n        self.log.debug(\"Querying task entities\")\n\n        all_folder_ids = set(instances_missing_task.keys())\n        all_task_names = set()\n        for per_task in instances_missing_task.values():\n            all_task_names |= set(per_task.keys())\n        all_task_names.discard(None)\n\n        task_entities = []\n        if all_task_names:\n            task_entities = ayon_api.get_tasks(\n                project_name,\n                folder_ids=all_folder_ids,\n                task_names=all_task_names\n            )\n        task_entity_by_ids = {}\n        for task_entity in task_entities:\n            folder_id = task_entity[\"folderId\"]\n            task_name = task_entity[\"name\"]\n            _by_folder_id = task_entity_by_ids.setdefault(folder_id, {})\n            _by_folder_id[task_name] = task_entity\n\n        not_found_task_paths = []\n        for folder_id, by_task in instances_missing_task.items():\n            for task_name, instances in by_task.items():\n                task_entity = (\n                    task_entity_by_ids\n                    .get(folder_id, {})\n                    .get(task_name)\n                )\n                if task_name and not task_entity:\n                    folder_path = folder_path_by_id[folder_id]\n                    not_found_task_paths.append(\n                        \"/\".join([folder_path, task_name])\n                    )\n\n                for instance in instances:\n                    instance.data[\"taskEntity\"] = task_entity\n\n        if not_found_task_paths:\n            joined_paths = \", \".join(\n                [\"\\\"{}\\\"\".format(path) for path in not_found_task_paths]\n            )\n            self.log.warning(\n                f\"Not found task entities with paths {joined_paths}.\")\n\n    def fill_latest_versions(self, context, project_name):\n        \"\"\"Try to find latest version for each instance's product name.\n\n        Key \"latestVersion\" is always set to latest version or `None`.\n\n        Args:\n            context (pyblish.Context)\n\n        Returns:\n            None\n\n        \"\"\"\n        self.log.debug(\"Querying latest versions for instances.\")\n\n        hierarchy = {}\n        names_by_folder_ids = collections.defaultdict(set)\n        for instance in context:\n            # Make sure `\"latestVersion\"` key is set\n            latest_version = instance.data.get(\"latestVersion\")\n            instance.data[\"latestVersion\"] = latest_version\n\n            # Skip instances without \"folderEntity\"\n            folder_entity = instance.data.get(\"folderEntity\")\n            if not folder_entity:\n                continue\n\n            # Store folder ids and product names for queries\n            folder_id = folder_entity[\"id\"]\n            product_name = instance.data[\"productName\"]\n\n            # Prepare instance hierarchy for faster filling latest versions\n            if folder_id not in hierarchy:\n                hierarchy[folder_id] = {}\n            if product_name not in hierarchy[folder_id]:\n                hierarchy[folder_id][product_name] = []\n            hierarchy[folder_id][product_name].append(instance)\n            names_by_folder_ids[folder_id].add(product_name)\n\n        product_entities = []\n        if names_by_folder_ids:\n            product_entities = list(ayon_api.get_products(\n                project_name, names_by_folder_ids=names_by_folder_ids\n            ))\n\n        product_ids = {\n            product_entity[\"id\"]\n            for product_entity in product_entities\n        }\n\n        last_versions_by_product_id = ayon_api.get_last_versions(\n            project_name, product_ids, fields={\"version\"}\n        )\n        for product_entity in product_entities:\n            product_id = product_entity[\"id\"]\n            last_version_entity = last_versions_by_product_id.get(product_id)\n            if last_version_entity is None:\n                continue\n\n            last_version = last_version_entity[\"version\"]\n            folder_id = product_entity[\"folderId\"]\n            product_name = product_entity[\"name\"]\n            _instances = hierarchy[folder_id][product_name]\n            for _instance in _instances:\n                _instance.data[\"latestVersion\"] = last_version\n\n    def fill_anatomy_data(self, context):\n        self.log.debug(\"Storing anatomy data to instance data.\")\n\n        project_entity = context.data[\"projectEntity\"]\n        task_types_by_name = {\n            task_type[\"name\"]: task_type\n            for task_type in project_entity[\"taskTypes\"]\n        }\n\n        for instance in context:\n            anatomy_data = copy.deepcopy(context.data[\"anatomyData\"])\n            product_name = instance.data[\"productName\"]\n            product_type = instance.data[\"productType\"]\n            product_base_type = instance.data.get(\"productBaseType\")\n            if not product_base_type:\n                product_base_type = product_type\n            anatomy_data.update({\n                \"product\": {\n                    \"name\": product_name,\n                    \"type\": product_type,\n                    \"basetype\": product_base_type,\n                }\n            })\n\n            self._fill_folder_data(instance, project_entity, anatomy_data)\n            self._fill_task_data(instance, task_types_by_name, anatomy_data)\n\n            # Define version\n            version_number = None\n\n            # Allow an instance to force enable or disable the version\n            # following of the current context\n            use_context_version = self.follow_workfile_version\n            if \"followWorkfileVersion\" in instance.data:\n                use_context_version = instance.data[\"followWorkfileVersion\"]\n\n            if use_context_version:\n                version_number = context.data.get(\"version\")\n\n            # Even if 'follow_workfile_version' is enabled, it may not be set\n            #   because workfile version was not collected to 'context.data'\n            # - that can happen e.g. in 'traypublisher' or other hosts without\n            #   a workfile\n            if version_number is None:\n                version_number = instance.data.get(\"version\")\n\n            # use latest version (+1) if already any exist\n            if version_number is None:\n                latest_version = instance.data[\"latestVersion\"]\n                if latest_version is not None:\n                    version_number = int(latest_version) + 1\n\n            # If version is not specified for instance or context\n            if version_number is None:\n                task_data = anatomy_data.get(\"task\") or {}\n                task_name = task_data.get(\"name\")\n                task_type = task_data.get(\"type\")\n                version_number = get_versioning_start(\n                    context.data[\"projectName\"],\n                    instance.context.data[\"hostName\"],\n                    task_name=task_name,\n                    task_type=task_type,\n                    product_base_type=product_base_type,\n                    product_name=instance.data[\"productName\"],\n                )\n            anatomy_data[\"version\"] = version_number\n\n            # Additional data\n            resolution_width = instance.data.get(\"resolutionWidth\")\n            if resolution_width:\n                anatomy_data[\"resolution_width\"] = resolution_width\n\n            resolution_height = instance.data.get(\"resolutionHeight\")\n            if resolution_height:\n                anatomy_data[\"resolution_height\"] = resolution_height\n\n            pixel_aspect = instance.data.get(\"pixelAspect\")\n            if pixel_aspect:\n                anatomy_data[\"pixel_aspect\"] = float(\n                    \"{:0.2f}\".format(float(pixel_aspect))\n                )\n\n            fps = instance.data.get(\"fps\")\n            if fps:\n                anatomy_data[\"fps\"] = float(\"{:0.2f}\".format(float(fps)))\n\n            # Store anatomy data\n            instance.data[\"projectEntity\"] = project_entity\n            instance.data[\"anatomyData\"] = anatomy_data\n            instance.data[\"version\"] = version_number\n\n            # Log collected data\n            instance_name = instance.data[\"name\"]\n            instance_label = instance.data.get(\"label\")\n            if instance_label:\n                instance_name += \" ({})\".format(instance_label)\n            self.log.debug(\"Anatomy data for instance {}: {}\".format(\n                instance_name,\n                json.dumps(anatomy_data, indent=4)\n            ))\n\n            # make render layer available in anatomy data\n            render_layer = instance.data.get(\"renderlayer\")\n            if render_layer:\n                anatomy_data[\"renderlayer\"] = render_layer\n\n            # make aov name available in anatomy data\n            aov = instance.data.get(\"aov\")\n            if aov:\n                anatomy_data[\"aov\"] = aov\n\n    def _fill_folder_data(self, instance, project_entity, anatomy_data):\n        # QUESTION: should we make sure that all folder data are popped if\n        #   folder data cannot be found?\n        # - 'folder', 'hierarchy', 'parent', 'folder'\n        folder_entity = instance.data.get(\"folderEntity\")\n        if folder_entity:\n            folder_data = get_folder_template_data(\n                folder_entity,\n                project_entity[\"name\"]\n            )\n            anatomy_data.update(folder_data)\n            return\n\n        if (\n            instance.data.get(\"newHierarchyIntegration\")\n            # Backwards compatible (Deprecated since 24/06/06)\n            or instance.data.get(\"newAssetPublishing\")\n        ):\n            folder_path = instance.data[\"folderPath\"]\n            parents = folder_path.lstrip(\"/\").split(\"/\")\n            folder_name = parents.pop(-1)\n\n            parent_name = project_entity[\"name\"]\n            hierarchy = \"\"\n            if parents:\n                parent_name = parents[-1]\n                hierarchy = \"/\".join(parents)\n\n            anatomy_data.update({\n                \"asset\": folder_name,\n                \"hierarchy\": hierarchy,\n                \"parent\": parent_name,\n                \"folder\": {\n                    \"name\": folder_name,\n                    \"path\": instance.data[\"folderPath\"],\n                    # TODO get folder type from hierarchy\n                    #   Using 'Shot' is current default behavior of editorial\n                    #   (or 'newHierarchyIntegration') publishing.\n                    \"type\": \"Shot\",\n                    \"parents\": parents,\n                },\n            })\n\n    def _fill_task_data(self, instance, task_types_by_name, anatomy_data):\n        # QUESTION: should we make sure that all task data are popped if task\n        #   data cannot be resolved?\n        # - 'task'\n\n        # Skip if there is no task\n        task_name = instance.data.get(\"task\")\n        if not task_name:\n            return\n\n        # Find task data based on folder entity\n        task_entity = instance.data.get(\"taskEntity\")\n        task_data = self._get_task_data_from_entity(\n            task_entity, task_types_by_name\n        )\n        if task_data:\n            # Fill task data\n            # - if we're in editorial, make sure the task type is filled\n            new_hierarchy = (\n                instance.data.get(\"newHierarchyIntegration\")\n                # Backwards compatible (Deprecated since 24/06/06)\n                or instance.data.get(\"newAssetPublishing\")\n            )\n            if not new_hierarchy or task_data[\"type\"]:\n                anatomy_data[\"task\"] = task_data\n                return\n\n        # New hierarchy is not created, so we can only skip rest of the logic\n        new_hierarchy = (\n            instance.data.get(\"newHierarchyIntegration\")\n            # Backwards compatible (Deprecated since 24/06/06)\n            or instance.data.get(\"newAssetPublishing\")\n        )\n        if not new_hierarchy:\n            return\n\n        # Try to find task data based on hierarchy context and folder path\n        hierarchy_context = instance.context.data.get(\"hierarchyContext\")\n        folder_path = instance.data.get(\"folderPath\")\n        if not hierarchy_context or not folder_path:\n            return\n\n        project_name = instance.context.data[\"projectName\"]\n        if \"/\" not in folder_path:\n            tasks_info = self._find_tasks_info_in_hierarchy(\n                hierarchy_context, folder_path\n            )\n        else:\n            current_data = hierarchy_context.get(project_name, {})\n            for key in folder_path.split(\"/\"):\n                if key:\n                    current_data = (\n                        current_data\n                        .get(\"children\", {})\n                        .get(key, {})\n                    )\n            tasks_info = current_data.get(\"tasks\", {})\n\n        task_info = tasks_info.get(task_name, {})\n        task_type = task_info.get(\"type\")\n        task_code = (\n            task_types_by_name\n            .get(task_type, {})\n            .get(\"shortName\")\n        )\n        anatomy_data[\"task\"] = {\n            \"name\": task_name,\n            \"type\": task_type,\n            \"short\": task_code\n        }\n\n    def _get_task_data_from_entity(\n        self, task_entity, task_types_by_name\n    ):\n        \"\"\"\n\n        Args:\n            task_entity (Union[dict[str, Any], None]): Task entity.\n            task_types_by_name (dict[str, dict[str, Any]]): Project task\n                types.\n\n        Returns:\n            Union[dict[str, str], None]: Task data or None if not found.\n        \"\"\"\n\n        if not task_entity:\n            return None\n\n        task_type = task_entity[\"taskType\"]\n        task_code = (\n            task_types_by_name\n            .get(task_type, {})\n            .get(\"shortName\")\n        )\n        return {\n            \"name\": task_entity[\"name\"],\n            \"type\": task_type,\n            \"short\": task_code\n        }\n\n    def _find_tasks_info_in_hierarchy(self, hierarchy_context, folder_name):\n        \"\"\"Find tasks info for an asset in editorial hierarchy.\n\n        Args:\n            hierarchy_context (dict[str, Any]): Editorial hierarchy context.\n            folder_name (str): Folder name.\n\n        Returns:\n            dict[str, dict[str, Any]]: Tasks info by name.\n        \"\"\"\n\n        hierarchy_queue = collections.deque()\n        hierarchy_queue.append(copy.deepcopy(hierarchy_context))\n        while hierarchy_queue:\n            item = hierarchy_queue.popleft()\n            if folder_name in item:\n                return item[folder_name].get(\"tasks\") or {}\n\n            for subitem in item.values():\n                hierarchy_queue.extend(subitem.get(\"children\") or [])\n        return {}\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_anatomy_instance_data.html#client.ayon_core.plugins.publish.collect_anatomy_instance_data.CollectAnatomyInstanceData.fill_latest_versions","title":"<code>fill_latest_versions(context, project_name)</code>","text":"<p>Try to find latest version for each instance's product name.</p> <p>Key \"latestVersion\" is always set to latest version or <code>None</code>.</p> <p>Returns:</p> Type Description <p>None</p> Source code in <code>client/ayon_core/plugins/publish/collect_anatomy_instance_data.py</code> <pre><code>def fill_latest_versions(self, context, project_name):\n    \"\"\"Try to find latest version for each instance's product name.\n\n    Key \"latestVersion\" is always set to latest version or `None`.\n\n    Args:\n        context (pyblish.Context)\n\n    Returns:\n        None\n\n    \"\"\"\n    self.log.debug(\"Querying latest versions for instances.\")\n\n    hierarchy = {}\n    names_by_folder_ids = collections.defaultdict(set)\n    for instance in context:\n        # Make sure `\"latestVersion\"` key is set\n        latest_version = instance.data.get(\"latestVersion\")\n        instance.data[\"latestVersion\"] = latest_version\n\n        # Skip instances without \"folderEntity\"\n        folder_entity = instance.data.get(\"folderEntity\")\n        if not folder_entity:\n            continue\n\n        # Store folder ids and product names for queries\n        folder_id = folder_entity[\"id\"]\n        product_name = instance.data[\"productName\"]\n\n        # Prepare instance hierarchy for faster filling latest versions\n        if folder_id not in hierarchy:\n            hierarchy[folder_id] = {}\n        if product_name not in hierarchy[folder_id]:\n            hierarchy[folder_id][product_name] = []\n        hierarchy[folder_id][product_name].append(instance)\n        names_by_folder_ids[folder_id].add(product_name)\n\n    product_entities = []\n    if names_by_folder_ids:\n        product_entities = list(ayon_api.get_products(\n            project_name, names_by_folder_ids=names_by_folder_ids\n        ))\n\n    product_ids = {\n        product_entity[\"id\"]\n        for product_entity in product_entities\n    }\n\n    last_versions_by_product_id = ayon_api.get_last_versions(\n        project_name, product_ids, fields={\"version\"}\n    )\n    for product_entity in product_entities:\n        product_id = product_entity[\"id\"]\n        last_version_entity = last_versions_by_product_id.get(product_id)\n        if last_version_entity is None:\n            continue\n\n        last_version = last_version_entity[\"version\"]\n        folder_id = product_entity[\"folderId\"]\n        product_name = product_entity[\"name\"]\n        _instances = hierarchy[folder_id][product_name]\n        for _instance in _instances:\n            _instance.data[\"latestVersion\"] = last_version\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_audio.html","title":"collect_audio","text":""},{"location":"autoapi/client/ayon_core/plugins/publish/collect_audio.html#client.ayon_core.plugins.publish.collect_audio.CollectAudio","title":"<code>CollectAudio</code>","text":"<p>               Bases: <code>ContextPlugin</code></p> <p>Collect folders's last published audio.</p> The audio product name searched for is defined in <p>project settings &gt; Collect Audio</p> Note <p>The plugin was instance plugin but because of so much queries the     plugin was slowing down whole collection phase a lot thus was     converted to context plugin which requires only 4 queries top.</p> Source code in <code>client/ayon_core/plugins/publish/collect_audio.py</code> <pre><code>class CollectAudio(pyblish.api.ContextPlugin):\n    \"\"\"Collect folders's last published audio.\n\n    The audio product name searched for is defined in:\n        project settings &gt; Collect Audio\n\n    Note:\n        The plugin was instance plugin but because of so much queries the\n            plugin was slowing down whole collection phase a lot thus was\n            converted to context plugin which requires only 4 queries top.\n    \"\"\"\n\n    label = \"Collect Folder Audio\"\n    order = pyblish.api.CollectorOrder + 0.1\n    families = [\"review\"]\n    settings_category = \"core\"\n\n    audio_product_name = \"audioMain\"\n\n    def process(self, context):\n        # Fake filtering by family inside context plugin\n        filtered_instances = []\n        for instance in pyblish.api.instances_by_plugin(\n            context, self.__class__\n        ):\n            # Skip instances that already have audio filled\n            if \"audio\" in instance.data:\n                self.log.debug(\n                    \"Skipping Audio collection. It is already collected\"\n                )\n                continue\n            filtered_instances.append(instance)\n\n        # Skip if none of instances remained\n        if not filtered_instances:\n            return\n\n        # Add audio to instance if exists.\n        instances_by_folder_path = collections.defaultdict(list)\n        for instance in filtered_instances:\n            folder_path = instance.data[\"folderPath\"]\n            instances_by_folder_path[folder_path].append(instance)\n\n        folder_paths = set(instances_by_folder_path.keys())\n        self.log.debug((\n            \"Searching for audio product '{product}' in folders {folders}\"\n        ).format(\n            product=self.audio_product_name,\n            folders=\", \".join([\n                '\"{}\"'.format(folder_path)\n                for folder_path in folder_paths\n            ])\n        ))\n\n        # Query all required documents\n        project_name = context.data[\"projectName\"]\n        anatomy = context.data[\"anatomy\"]\n        repre_entities_by_folder_paths = self.query_representations(\n            project_name, folder_paths)\n\n        for folder_path, instances in instances_by_folder_path.items():\n            repre_entities = repre_entities_by_folder_paths[folder_path]\n            if not repre_entities:\n                continue\n\n            repre_entity = repre_entities[0]\n            repre_path = get_representation_path_with_anatomy(\n                repre_entity, anatomy\n            )\n            for instance in instances:\n                instance.data[\"audio\"] = [{\n                    \"offset\": 0,\n                    \"filename\": repre_path\n                }]\n                self.log.debug(\"Audio Data added to instance ...\")\n\n    def query_representations(self, project_name, folder_paths):\n        \"\"\"Query representations related to audio products for passed folders.\n\n        Args:\n            project_name (str): Project in which we're looking for all\n                entities.\n            folder_paths (Iterable[str]): Folder paths where to look for audio\n                products and their representations.\n\n        Returns:\n            collections.defaultdict[str, List[Dict[Str, Any]]]: Representations\n                related to audio products by folder path.\n\n        \"\"\"\n        output = collections.defaultdict(list)\n        # Skip the queries if audio product name is not defined\n        if not self.audio_product_name:\n            return output\n\n        # Query folder entities\n        folder_entities = ayon_api.get_folders(\n            project_name,\n            folder_paths=folder_paths,\n            fields={\"id\", \"path\"}\n        )\n\n        folder_id_by_path = {\n            folder_entity[\"path\"]: folder_entity[\"id\"]\n            for folder_entity in folder_entities\n        }\n        folder_ids = set(folder_id_by_path.values())\n\n        # Query products with name define by 'audio_product_name' attr\n        # - one or none products with the name should be available on\n        #   an folder\n        product_entities = ayon_api.get_products(\n            project_name,\n            product_names=[self.audio_product_name],\n            folder_ids=folder_ids,\n            fields={\"id\", \"folderId\"}\n        )\n        product_id_by_folder_id = {}\n        for product_entity in product_entities:\n            folder_id = product_entity[\"folderId\"]\n            product_id_by_folder_id[folder_id] = product_entity[\"id\"]\n\n        product_ids = set(product_id_by_folder_id.values())\n        if not product_ids:\n            return output\n\n        # Find all latest versions for the products\n        last_versions_by_product_id = ayon_api.get_last_versions(\n            project_name, product_ids=product_ids, fields={\"id\", \"productId\"}\n        )\n        version_id_by_product_id = {\n            product_id: version_entity[\"id\"]\n            for product_id, version_entity in (\n                last_versions_by_product_id.items()\n            )\n        }\n        version_ids = set(version_id_by_product_id.values())\n        if not version_ids:\n            return output\n\n        # Find representations under latest versions of audio products\n        repre_entities = ayon_api.get_representations(\n            project_name, version_ids=version_ids\n        )\n        repre_entities_by_version_id = collections.defaultdict(list)\n        for repre_entity in repre_entities:\n            version_id = repre_entity[\"versionId\"]\n            repre_entities_by_version_id[version_id].append(repre_entity)\n\n        if not repre_entities_by_version_id:\n            return output\n\n        for folder_path in folder_paths:\n            folder_id = folder_id_by_path.get(folder_path)\n            product_id = product_id_by_folder_id.get(folder_id)\n            version_id = version_id_by_product_id.get(product_id)\n            output[folder_path] = repre_entities_by_version_id[version_id]\n        return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_audio.html#client.ayon_core.plugins.publish.collect_audio.CollectAudio.query_representations","title":"<code>query_representations(project_name, folder_paths)</code>","text":"<p>Query representations related to audio products for passed folders.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project in which we're looking for all entities.</p> required <code>folder_paths</code> <code>Iterable[str]</code> <p>Folder paths where to look for audio products and their representations.</p> required <p>Returns:</p> Type Description <p>collections.defaultdict[str, List[Dict[Str, Any]]]: Representations related to audio products by folder path.</p> Source code in <code>client/ayon_core/plugins/publish/collect_audio.py</code> <pre><code>def query_representations(self, project_name, folder_paths):\n    \"\"\"Query representations related to audio products for passed folders.\n\n    Args:\n        project_name (str): Project in which we're looking for all\n            entities.\n        folder_paths (Iterable[str]): Folder paths where to look for audio\n            products and their representations.\n\n    Returns:\n        collections.defaultdict[str, List[Dict[Str, Any]]]: Representations\n            related to audio products by folder path.\n\n    \"\"\"\n    output = collections.defaultdict(list)\n    # Skip the queries if audio product name is not defined\n    if not self.audio_product_name:\n        return output\n\n    # Query folder entities\n    folder_entities = ayon_api.get_folders(\n        project_name,\n        folder_paths=folder_paths,\n        fields={\"id\", \"path\"}\n    )\n\n    folder_id_by_path = {\n        folder_entity[\"path\"]: folder_entity[\"id\"]\n        for folder_entity in folder_entities\n    }\n    folder_ids = set(folder_id_by_path.values())\n\n    # Query products with name define by 'audio_product_name' attr\n    # - one or none products with the name should be available on\n    #   an folder\n    product_entities = ayon_api.get_products(\n        project_name,\n        product_names=[self.audio_product_name],\n        folder_ids=folder_ids,\n        fields={\"id\", \"folderId\"}\n    )\n    product_id_by_folder_id = {}\n    for product_entity in product_entities:\n        folder_id = product_entity[\"folderId\"]\n        product_id_by_folder_id[folder_id] = product_entity[\"id\"]\n\n    product_ids = set(product_id_by_folder_id.values())\n    if not product_ids:\n        return output\n\n    # Find all latest versions for the products\n    last_versions_by_product_id = ayon_api.get_last_versions(\n        project_name, product_ids=product_ids, fields={\"id\", \"productId\"}\n    )\n    version_id_by_product_id = {\n        product_id: version_entity[\"id\"]\n        for product_id, version_entity in (\n            last_versions_by_product_id.items()\n        )\n    }\n    version_ids = set(version_id_by_product_id.values())\n    if not version_ids:\n        return output\n\n    # Find representations under latest versions of audio products\n    repre_entities = ayon_api.get_representations(\n        project_name, version_ids=version_ids\n    )\n    repre_entities_by_version_id = collections.defaultdict(list)\n    for repre_entity in repre_entities:\n        version_id = repre_entity[\"versionId\"]\n        repre_entities_by_version_id[version_id].append(repre_entity)\n\n    if not repre_entities_by_version_id:\n        return output\n\n    for folder_path in folder_paths:\n        folder_id = folder_id_by_path.get(folder_path)\n        product_id = product_id_by_folder_id.get(folder_id)\n        version_id = version_id_by_product_id.get(product_id)\n        output[folder_path] = repre_entities_by_version_id[version_id]\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_cleanup_keys.html","title":"collect_cleanup_keys","text":"Requires <p>None</p> <p>Provides:     context         - cleanupFullPaths (list)         - cleanupEmptyDirs (list)</p>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_cleanup_keys.html#client.ayon_core.plugins.publish.collect_cleanup_keys.CollectCleanupKeys","title":"<code>CollectCleanupKeys</code>","text":"<p>               Bases: <code>ContextPlugin</code></p> <p>Prepare keys for 'ExplicitCleanUp' plugin.</p> Source code in <code>client/ayon_core/plugins/publish/collect_cleanup_keys.py</code> <pre><code>class CollectCleanupKeys(pyblish.api.ContextPlugin):\n    \"\"\"Prepare keys for 'ExplicitCleanUp' plugin.\"\"\"\n\n    label = \"Collect Cleanup Keys\"\n    order = pyblish.api.CollectorOrder - 0.5\n\n    def process(self, context):\n        context.data[\"cleanupFullPaths\"] = []\n        context.data[\"cleanupEmptyDirs\"] = []\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_comment.html","title":"collect_comment","text":"<p>Collect comment and add option to enter comment per instance.</p> <p>Combination of plugins. One define optional input for instances in Publisher UI (CollectInstanceCommentDef) and second cares that each instance during collection has available \"comment\" key in data (CollectComment).</p> <p>Plugin 'CollectInstanceCommentDef' define \"comment\" attribute which won't be filled with any value if instance does not match families filter or when plugin is disabled.</p> <p>Plugin 'CollectComment' makes sure that each instance in context has available \"comment\" key in data which can be set to 'str' or 'None' if is not set. - In case instance already has filled comment the plugin's logic is skipped - The comment is always set and value should be always 'str' even if is empty</p> <p>Why are separated: - 'CollectInstanceCommentDef' can have specific settings to show comment     attribute only to defined families in publisher UI - 'CollectComment' will run all the time</p> Todos <p>The comment per instance is not sent via farm.</p>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_comment.html#client.ayon_core.plugins.publish.collect_comment.CollectComment","title":"<code>CollectComment</code>","text":"<p>               Bases: <code>ContextPlugin</code>, <code>AYONPyblishPluginMixin</code></p> <p>Collect comment per each instance.</p> <p>Plugin makes sure each instance to publish has set \"comment\" in data so any further plugin can use it directly.</p> Source code in <code>client/ayon_core/plugins/publish/collect_comment.py</code> <pre><code>class CollectComment(\n    pyblish.api.ContextPlugin,\n    AYONPyblishPluginMixin\n):\n    \"\"\"Collect comment per each instance.\n\n    Plugin makes sure each instance to publish has set \"comment\" in data so any\n    further plugin can use it directly.\n    \"\"\"\n\n    label = \"Collect Instance Comment\"\n    # TODO change to CollectorOrder after Pyblish is purged\n    # Pyblish allows modifying comment after collect phase\n    order = pyblish.api.ExtractorOrder - 0.49\n\n    def process(self, context):\n        context_comment = self.cleanup_comment(context.data.get(\"comment\"))\n        # Set it back\n        context.data[\"comment\"] = context_comment\n        for instance in context:\n            instance_label = str(instance)\n            # Check if comment is already set\n            instance_comment = self.cleanup_comment(\n                instance.data.get(\"comment\"))\n\n            # If comment on instance is not set then look for attributes\n            if not instance_comment:\n                attr_values = self.get_attr_values_from_data_for_plugin(\n                    CollectInstanceCommentDef, instance.data\n                )\n                instance_comment = self.cleanup_comment(\n                    attr_values.get(\"comment\")\n                )\n\n            # Use context comment if instance has all options of comment\n            #   empty\n            if not instance_comment:\n                instance_comment = context_comment\n\n            instance.data[\"comment\"] = instance_comment\n            if instance_comment:\n                msg_end = \"has comment set to: \\\"{}\\\"\".format(\n                    instance_comment)\n            else:\n                msg_end = \"does not have set comment\"\n            self.log.debug(\"Instance {} {}\".format(instance_label, msg_end))\n\n    def cleanup_comment(self, comment):\n        \"\"\"Cleanup comment value.\n\n        Args:\n            comment (Union[str, None]): Comment value from data.\n\n        Returns:\n            str: Cleaned comment which is stripped or empty string if input\n                was 'None'.\n        \"\"\"\n\n        if comment:\n            return comment.strip()\n        return \"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_comment.html#client.ayon_core.plugins.publish.collect_comment.CollectComment.cleanup_comment","title":"<code>cleanup_comment(comment)</code>","text":"<p>Cleanup comment value.</p> <p>Parameters:</p> Name Type Description Default <code>comment</code> <code>Union[str, None]</code> <p>Comment value from data.</p> required <p>Returns:</p> Name Type Description <code>str</code> <p>Cleaned comment which is stripped or empty string if input was 'None'.</p> Source code in <code>client/ayon_core/plugins/publish/collect_comment.py</code> <pre><code>def cleanup_comment(self, comment):\n    \"\"\"Cleanup comment value.\n\n    Args:\n        comment (Union[str, None]): Comment value from data.\n\n    Returns:\n        str: Cleaned comment which is stripped or empty string if input\n            was 'None'.\n    \"\"\"\n\n    if comment:\n        return comment.strip()\n    return \"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_context_entities.html","title":"collect_context_entities","text":"<p>Collect Anatomy and global anatomy data.</p> Requires <p>context -&gt; projectName context -&gt; folderPath context -&gt; task</p> Provides <p>context -&gt; projectEntity - Project entity from AYON server. context -&gt; folderEntity - Folder entity from AYON server only if     'folderPath' is set in context data. context -&gt; taskEntity - Task entity from AYON server only if 'folderPath'     and 'task' are set in context data.</p>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_context_entities.html#client.ayon_core.plugins.publish.collect_context_entities.CollectContextEntities","title":"<code>CollectContextEntities</code>","text":"<p>               Bases: <code>ContextPlugin</code></p> <p>Collect entities into Context.</p> Source code in <code>client/ayon_core/plugins/publish/collect_context_entities.py</code> <pre><code>class CollectContextEntities(pyblish.api.ContextPlugin):\n    \"\"\"Collect entities into Context.\"\"\"\n\n    order = pyblish.api.CollectorOrder - 0.45\n    label = \"Collect Context Entities\"\n\n    def process(self, context):\n        project_name = context.data[\"projectName\"]\n        folder_path = context.data[\"folderPath\"]\n        task_name = context.data[\"task\"]\n\n        project_entity = context.data.get(\"projectEntity\")\n        folder_entity = context.data.get(\"folderEntity\")\n        task_entity = context.data.get(\"taskEntity\")\n\n        if not project_entity:\n            project_entity = ayon_api.get_project(project_name)\n            if not project_entity:\n                raise PublishError(f\"Project '{project_name}' was not found.\")\n\n        context.data[\"projectEntity\"] = project_entity\n        context.data[\"anatomy\"] = Anatomy(\n            project_name, project_entity=project_entity\n        )\n\n        self.log.debug(f\"Project entity \\\"{project_entity}\\\"\")\n        self.log.debug(\n            f\"Anatomy object collected for project \\\"{project_name}\\\".\"\n        )\n\n        if not folder_path:\n            self.log.info(\"Context is not set. Can't collect global data.\")\n            return\n\n        if not folder_entity:\n            folder_entity = self._get_folder_entity(project_name, folder_path)\n\n        self.log.debug(f\"Folder entity \\\"{folder_entity}\\\"\")\n\n        if not task_entity:\n            task_entity = self._get_task_entity(\n                project_name, folder_entity, task_name\n            )\n\n        self.log.debug(f\"Task entity \\\"{task_entity}\\\"\")\n\n        context.data[\"folderEntity\"] = folder_entity\n        context.data[\"taskEntity\"] = task_entity\n\n        context_attributes = folder_entity[\"attrib\"]\n        task_type = None\n        if task_entity:\n            context_attributes = task_entity[\"attrib\"]\n            task_type = task_entity[\"taskType\"]\n\n        context.data[\"taskType\"] = task_type\n\n        frame_start = context_attributes.get(\"frameStart\")\n        frame_end = context_attributes.get(\"frameEnd\")\n        if frame_start is None:\n            frame_start = 1\n            self.log.warning(\"Missing frame start. Defaulting to 1.\")\n\n        if frame_end is None:\n            frame_end = 2\n            self.log.warning(\"Missing frame end. Defaulting to 2.\")\n\n        context.data[\"frameStart\"] = frame_start\n        context.data[\"frameEnd\"] = frame_end\n\n        handle_start = context_attributes.get(\"handleStart\") or 0\n        handle_end = context_attributes.get(\"handleEnd\") or 0\n\n        context.data[\"handleStart\"] = int(handle_start)\n        context.data[\"handleEnd\"] = int(handle_end)\n\n        frame_start_h = frame_start - context.data[\"handleStart\"]\n        frame_end_h = frame_end + context.data[\"handleEnd\"]\n        context.data[\"frameStartHandle\"] = frame_start_h\n        context.data[\"frameEndHandle\"] = frame_end_h\n\n        context.data[\"fps\"] = context_attributes[\"fps\"]\n\n    def _get_folder_entity(self, project_name, folder_path):\n        if not folder_path:\n            return None\n        folder_entity = ayon_api.get_folder_by_path(project_name, folder_path)\n        if not folder_entity:\n            raise PublishError(\n                f\"Folder '{folder_path}' was not found\"\n                f\" in project '{project_name}'.\"\n            )\n        return folder_entity\n\n    def _get_task_entity(self, project_name, folder_entity, task_name):\n        if not folder_entity or not task_name:\n            return None\n        task_entity = ayon_api.get_task_by_name(\n            project_name, folder_entity[\"id\"], task_name\n        )\n        if not task_entity:\n            task_path = \"/\".join([folder_entity[\"path\"], task_name])\n            raise PublishError(\n                f\"Task '{task_path}' was not found\"\n                f\" in project '{project_name}'.\"\n            )\n        return task_entity\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_context_label.html","title":"collect_context_label","text":"Optional <p>context     -&gt; hostName (str) context     -&gt; currentFile (str)</p> <p>Provides:     context     -&gt; label (str)</p>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_context_label.html#client.ayon_core.plugins.publish.collect_context_label.CollectContextLabel","title":"<code>CollectContextLabel</code>","text":"<p>               Bases: <code>ContextPlugin</code></p> <p>Labelize context using the registered host and current file</p> Source code in <code>client/ayon_core/plugins/publish/collect_context_label.py</code> <pre><code>class CollectContextLabel(pyblish.api.ContextPlugin):\n    \"\"\"Labelize context using the registered host and current file\"\"\"\n\n    order = pyblish.api.CollectorOrder + 0.25\n    label = \"Context Label\"\n\n    def process(self, context):\n        # Add ability to use custom context label\n        label = context.data.get(\"label\")\n        if label:\n            self.log.debug(\"Context label is already set to \\\"{}\\\"\".format(\n                label\n            ))\n            return\n\n        host_name = context.data.get(\"hostName\")\n        if not host_name:\n            host_name = pyblish.api.registered_hosts()[-1]\n        # Use host name as base for label\n        label = host_name.title()\n\n        # Get scene name from \"currentFile\" and use basename as ending of label\n        path = context.data.get(\"currentFile\")\n        if path:\n            label += \" - {}\".format(os.path.basename(path))\n\n        # Set label\n        context.data[\"label\"] = label\n        self.log.debug(\"Context label is changed to \\\"{}\\\"\".format(\n            label\n        ))\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_current_ayon_user.html","title":"collect_current_ayon_user","text":""},{"location":"autoapi/client/ayon_core/plugins/publish/collect_current_ayon_user.html#client.ayon_core.plugins.publish.collect_current_ayon_user.CollectCurrentAYONUser","title":"<code>CollectCurrentAYONUser</code>","text":"<p>               Bases: <code>ContextPlugin</code></p> <p>Inject the currently logged on user into the Context</p> Source code in <code>client/ayon_core/plugins/publish/collect_current_ayon_user.py</code> <pre><code>class CollectCurrentAYONUser(pyblish.api.ContextPlugin):\n    \"\"\"Inject the currently logged on user into the Context\"\"\"\n\n    # Order must be after default pyblish-base CollectCurrentUser\n    order = pyblish.api.CollectorOrder + 0.001\n    label = \"Collect AYON User\"\n\n    def process(self, context):\n        user = get_ayon_username()\n        context.data[\"user\"] = user\n        self.log.debug(\"Collected user \\\"{}\\\"\".format(user))\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_current_context.html","title":"collect_current_context","text":"Provides <p>context -&gt; projectName (str) context -&gt; folderPath (str) context -&gt; task (str)</p>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_current_context.html#client.ayon_core.plugins.publish.collect_current_context.CollectCurrentContext","title":"<code>CollectCurrentContext</code>","text":"<p>               Bases: <code>ContextPlugin</code></p> <p>Collect project context into publish context data.</p> <p>Plugin does not override any value if is already set.</p> Source code in <code>client/ayon_core/plugins/publish/collect_current_context.py</code> <pre><code>class CollectCurrentContext(pyblish.api.ContextPlugin):\n    \"\"\"Collect project context into publish context data.\n\n    Plugin does not override any value if is already set.\n    \"\"\"\n\n    order = pyblish.api.CollectorOrder - 0.5\n    label = \"Collect Current context\"\n\n    def process(self, context):\n        # Check if values are already set\n        project_name = context.data.get(\"projectName\")\n        folder_path = context.data.get(\"folderPath\")\n        task_name = context.data.get(\"task\")\n\n        current_context = get_current_context()\n        if not project_name:\n            context.data[\"projectName\"] = current_context[\"project_name\"]\n\n        if not folder_path:\n            context.data[\"folderPath\"] = current_context[\"folder_path\"]\n\n        if not task_name:\n            context.data[\"task\"] = current_context[\"task_name\"]\n\n        # QUESTION should we be explicit with keys? (the same on instances)\n        #   - 'task' -&gt; 'taskName'\n\n        self.log.info((\n            \"Collected project context\\n\"\n            \"Project: {project_name}\\n\"\n            \"Folder: {folder_path}\\n\"\n            \"Task: {task_name}\"\n        ).format(\n            project_name=context.data[\"projectName\"],\n            folder_path=context.data[\"folderPath\"],\n            task_name=context.data[\"task\"]\n        ))\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_current_shell_file.html","title":"collect_current_shell_file","text":"Requires <p>None</p> Provides <p>context         -&gt; currentFile (str)</p>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_current_shell_file.html#client.ayon_core.plugins.publish.collect_current_shell_file.CollectCurrentShellFile","title":"<code>CollectCurrentShellFile</code>","text":"<p>               Bases: <code>ContextPlugin</code></p> <p>Inject the current working file into context</p> Source code in <code>client/ayon_core/plugins/publish/collect_current_shell_file.py</code> <pre><code>class CollectCurrentShellFile(pyblish.api.ContextPlugin):\n    \"\"\"Inject the current working file into context\"\"\"\n\n    order = pyblish.api.CollectorOrder - 0.5\n    label = \"Current File\"\n    hosts = [\"shell\"]\n\n    def process(self, context):\n        \"\"\"Inject the current working file\"\"\"\n        context.data[\"currentFile\"] = os.path.join(os.getcwd(), \"&lt;shell&gt;\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_current_shell_file.html#client.ayon_core.plugins.publish.collect_current_shell_file.CollectCurrentShellFile.process","title":"<code>process(context)</code>","text":"<p>Inject the current working file</p> Source code in <code>client/ayon_core/plugins/publish/collect_current_shell_file.py</code> <pre><code>def process(self, context):\n    \"\"\"Inject the current working file\"\"\"\n    context.data[\"currentFile\"] = os.path.join(os.getcwd(), \"&lt;shell&gt;\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_datetime_data.html","title":"collect_datetime_data","text":"<p>These data must be collected only once during publishing process.</p> Provides <p>context -&gt; datetimeData</p>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_explicit_resolution.html","title":"collect_explicit_resolution","text":""},{"location":"autoapi/client/ayon_core/plugins/publish/collect_explicit_resolution.html#client.ayon_core.plugins.publish.collect_explicit_resolution.CollectExplicitResolution","title":"<code>CollectExplicitResolution</code>","text":"<p>               Bases: <code>InstancePlugin</code>, <code>AYONPyblishPluginMixin</code></p> <p>Collect explicit user defined resolution attributes for instances</p> Source code in <code>client/ayon_core/plugins/publish/collect_explicit_resolution.py</code> <pre><code>class CollectExplicitResolution(\n    pyblish.api.InstancePlugin,\n    publish.AYONPyblishPluginMixin,\n):\n    \"\"\"Collect explicit user defined resolution attributes for instances\"\"\"\n\n    label = \"Choose Explicit Resolution\"\n    order = pyblish.api.CollectorOrder - 0.091\n    settings_category = \"core\"\n\n    enabled = False\n\n    default_resolution_item = (None, \"Don't override\")\n    # Settings\n    product_base_types = []\n    options = []\n\n    # caching resoluton items\n    resolution_items = None\n\n    def process(self, instance):\n        \"\"\"Process the instance and collect explicit resolution attributes\"\"\"\n\n        # Get the values from the instance data\n        values = self.get_attr_values_from_data(instance.data)\n        resolution_value = values.get(\"explicit_resolution\", None)\n        if resolution_value is None:\n            return\n\n        # Get the width, height and pixel_aspect from the resolution value\n        resolution_data = self._get_resolution_values(resolution_value)\n\n        # Set the values to the instance data\n        instance.data.update(resolution_data)\n\n    def _get_resolution_values(self, resolution_value):\n        \"\"\"\n        Returns width, height and pixel_aspect from the resolution value\n\n        Arguments:\n            resolution_value (str): resolution value\n\n        Returns:\n            dict: dictionary with width, height and pixel_aspect\n        \"\"\"\n        resolution_items = self._get_resolution_items()\n        # ensure resolution_value is part of expected items\n        item_values = resolution_items.get(resolution_value)\n\n        # if the item is in the cache, get the values from it\n        if item_values:\n            return {\n                \"resolutionWidth\": item_values[\"width\"],\n                \"resolutionHeight\": item_values[\"height\"],\n                \"pixelAspect\": item_values[\"pixel_aspect\"],\n            }\n\n        raise PublishError(\n            f\"Invalid resolution value: {resolution_value} \"\n            f\"expected choices: {resolution_items}\"\n        )\n\n    @classmethod\n    def _get_resolution_items(cls):\n        if cls.resolution_items is None:\n            resolution_items = {}\n            for item in cls.options:\n                item_text = (\n                    f\"{item['width']}x{item['height']} \"\n                    f\"({item['pixel_aspect']})\"\n                )\n                resolution_items[item_text] = item\n\n            cls.resolution_items = resolution_items\n\n        return cls.resolution_items\n\n    @classmethod\n    def get_attr_defs_for_instance(\n        cls, create_context, instance,\n    ):\n        if instance.product_base_type not in cls.product_base_types:\n            return []\n\n        # Get the resolution items\n        resolution_items = cls._get_resolution_items()\n\n        items = [cls.default_resolution_item]\n        # Add all cached resolution items to the dropdown options\n        for item_text in resolution_items:\n            items.append((item_text, item_text))\n\n        return [\n            EnumDef(\n                \"explicit_resolution\",\n                items,\n                default=\"Don't override\",\n                label=\"Force product resolution\",\n            ),\n        ]\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_explicit_resolution.html#client.ayon_core.plugins.publish.collect_explicit_resolution.CollectExplicitResolution.process","title":"<code>process(instance)</code>","text":"<p>Process the instance and collect explicit resolution attributes</p> Source code in <code>client/ayon_core/plugins/publish/collect_explicit_resolution.py</code> <pre><code>def process(self, instance):\n    \"\"\"Process the instance and collect explicit resolution attributes\"\"\"\n\n    # Get the values from the instance data\n    values = self.get_attr_values_from_data(instance.data)\n    resolution_value = values.get(\"explicit_resolution\", None)\n    if resolution_value is None:\n        return\n\n    # Get the width, height and pixel_aspect from the resolution value\n    resolution_data = self._get_resolution_values(resolution_value)\n\n    # Set the values to the instance data\n    instance.data.update(resolution_data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_farm_env_variables.html","title":"collect_farm_env_variables","text":""},{"location":"autoapi/client/ayon_core/plugins/publish/collect_farm_env_variables.html#client.ayon_core.plugins.publish.collect_farm_env_variables.CollectCoreJobEnvVars","title":"<code>CollectCoreJobEnvVars</code>","text":"<p>               Bases: <code>ContextPlugin</code></p> <p>Collect set of environment variables to submit with deadline jobs</p> Source code in <code>client/ayon_core/plugins/publish/collect_farm_env_variables.py</code> <pre><code>class CollectCoreJobEnvVars(pyblish.api.ContextPlugin):\n    \"\"\"Collect set of environment variables to submit with deadline jobs\"\"\"\n    order = pyblish.api.CollectorOrder - 0.45\n    label = \"AYON core Farm Environment Variables\"\n    targets = [\"local\"]\n\n    def process(self, context):\n        env = context.data.setdefault(FARM_JOB_ENV_DATA_KEY, {})\n\n        # Disable colored logs on farm\n        for key, value in (\n            (\"AYON_LOG_NO_COLORS\", \"1\"),\n            (\"AYON_PROJECT_NAME\", context.data[\"projectName\"]),\n            (\"AYON_FOLDER_PATH\", context.data.get(\"folderPath\")),\n            (\"AYON_TASK_NAME\", context.data.get(\"task\")),\n            # NOTE we should use 'context.data[\"user\"]' but that has higher\n            #   order.\n            (\"AYON_USERNAME\", get_ayon_username()),\n            (\"AYON_HOST_NAME\", context.data[\"hostName\"]),\n        ):\n            if value:\n                self.log.debug(f\"Setting job env: {key}: {value}\")\n                env[key] = value\n\n        for key in [\n            \"AYON_BUNDLE_NAME\",\n            \"AYON_STUDIO_BUNDLE_NAME\",\n            \"AYON_USE_STAGING\",\n            \"AYON_IN_TESTS\",\n            # NOTE Not sure why workdir is needed?\n            \"AYON_WORKDIR\",\n        ]:\n            value = os.getenv(key)\n            if value:\n                self.log.debug(f\"Setting job env: {key}: {value}\")\n                env[key] = value\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_farm_target.html","title":"collect_farm_target","text":""},{"location":"autoapi/client/ayon_core/plugins/publish/collect_farm_target.html#client.ayon_core.plugins.publish.collect_farm_target.CollectFarmTarget","title":"<code>CollectFarmTarget</code>","text":"<p>               Bases: <code>InstancePlugin</code></p> <p>Collects the render target for the instance</p> Source code in <code>client/ayon_core/plugins/publish/collect_farm_target.py</code> <pre><code>class CollectFarmTarget(pyblish.api.InstancePlugin):\n    \"\"\"Collects the render target for the instance\n    \"\"\"\n\n    order = pyblish.api.CollectorOrder + 0.499\n    label = \"Collect Farm Target\"\n    targets = [\"local\"]\n\n    def process(self, instance):\n        if not instance.data.get(\"farm\"):\n            return\n\n        addons_manager = instance.context.data.get(\"ayonAddonsManager\")\n\n        farm_renderer_addons = [\"deadline\", \"royalrender\", \"opencue\"]\n        for farm_renderer in farm_renderer_addons:\n            addon = addons_manager.get(farm_renderer)\n            if addon and addon.enabled:\n                farm_name = farm_renderer\n                break\n        else:\n            # No enabled farm render addon found, then report all farm\n            # addons that were searched for yet not found\n            for farm_renderer in farm_renderer_addons:\n                self.log.error(f\"Cannot find AYON addon '{farm_renderer}'.\")\n            raise RuntimeError(\"No AYON renderer addon found.\")\n\n        self.log.debug(\"Collected render target: {0}\".format(farm_name))\n        instance.data[\"toBeRenderedOn\"] = farm_name\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_frames_fix.html","title":"collect_frames_fix","text":""},{"location":"autoapi/client/ayon_core/plugins/publish/collect_frames_fix.html#client.ayon_core.plugins.publish.collect_frames_fix.CollectFramesFixDef","title":"<code>CollectFramesFixDef</code>","text":"<p>               Bases: <code>InstancePlugin</code>, <code>AYONPyblishPluginMixin</code></p> <p>Provides text field to insert frame(s) to be rerendered.</p> <p>Published files of last version of an instance product are collected into instance.data[\"last_version_published_files\"]. All these but frames mentioned in text field will be reused for new version.</p> Source code in <code>client/ayon_core/plugins/publish/collect_frames_fix.py</code> <pre><code>class CollectFramesFixDef(\n    pyblish.api.InstancePlugin,\n    AYONPyblishPluginMixin\n):\n    \"\"\"Provides text field to insert frame(s) to be rerendered.\n\n    Published files of last version of an instance product are collected into\n    instance.data[\"last_version_published_files\"]. All these but frames\n    mentioned in text field will be reused for new version.\n    \"\"\"\n    order = pyblish.api.CollectorOrder + 0.495\n    label = \"Collect Frames to Fix\"\n    targets = [\"local\"]\n    hosts = [\"nuke\"]\n    families = [\"render\", \"prerender\"]\n    settings_category = \"core\"\n\n    rewrite_version_enable = False\n\n    def process(self, instance):\n        attribute_values = self.get_attr_values_from_data(instance.data)\n        frames_to_fix = attribute_values.get(\"frames_to_fix\")\n\n        rewrite_version = attribute_values.get(\"rewrite_version\")\n\n        if not frames_to_fix:\n            return\n\n        instance.data[\"frames_to_fix\"] = frames_to_fix\n\n        product_name = instance.data[\"productName\"]\n        folder_entity = instance.data[\"folderEntity\"]\n\n        project_entity = instance.data[\"projectEntity\"]\n        project_name = project_entity[\"name\"]\n\n        version_entity = ayon_api.get_last_version_by_product_name(\n            project_name,\n            product_name,\n            folder_entity[\"id\"]\n        )\n        if not version_entity:\n            self.log.warning(\n                \"No last version found, re-render not possible\"\n            )\n            return\n\n        representations = ayon_api.get_representations(\n            project_name, version_ids={version_entity[\"id\"]}\n        )\n        published_files = []\n        for repre in representations:\n            # TODO get product type from product entity instead of\n            #   representation 'context' data.\n            repre_context = repre[\"context\"]\n            product_type = repre_context.get(\"product\", {}).get(\"type\")\n            if not product_type:\n                product_type = repre_context.get(\"family\")\n            if product_type not in self.families:\n                continue\n\n            for file_info in repre.get(\"files\"):\n                published_files.append(file_info[\"path\"])\n\n        instance.data[\"last_version_published_files\"] = published_files\n        self.log.debug(\"last_version_published_files::{}\".format(\n            instance.data[\"last_version_published_files\"]))\n\n        if self.rewrite_version_enable and rewrite_version:\n            instance.data[\"version\"] = version_entity[\"version\"]\n            # limits triggering version validator\n            instance.data.pop(\"latestVersion\")\n\n    @classmethod\n    def get_attribute_defs(cls):\n        attributes = [\n            TextDef(\"frames_to_fix\", label=\"Frames to fix\",\n                    placeholder=\"5,10-15\",\n                    regex=\"[0-9,-]+\")\n        ]\n\n        if cls.rewrite_version_enable:\n            attributes.append(\n                BoolDef(\n                    \"rewrite_version\",\n                    label=\"Rewrite latest version\",\n                    default=False\n                )\n            )\n\n        return attributes\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_from_create_context.html","title":"collect_from_create_context","text":"<p>Create instances based on CreateContext.</p>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_from_create_context.html#client.ayon_core.plugins.publish.collect_from_create_context.CollectFromCreateContext","title":"<code>CollectFromCreateContext</code>","text":"<p>               Bases: <code>ContextPlugin</code></p> <p>Collect instances and data from CreateContext from new publishing.</p> Source code in <code>client/ayon_core/plugins/publish/collect_from_create_context.py</code> <pre><code>class CollectFromCreateContext(pyblish.api.ContextPlugin):\n    \"\"\"Collect instances and data from CreateContext from new publishing.\"\"\"\n\n    label = \"Collect From Create Context\"\n    order = pyblish.api.CollectorOrder - 0.5\n\n    def process(self, context):\n        create_context = context.data.get(\"create_context\")\n        if not create_context:\n            host = registered_host()\n            if isinstance(host, IPublishHost):\n                create_context = CreateContext(host)\n\n        if not create_context:\n            return\n\n        thumbnail_paths_by_instance_id = (\n            create_context.thumbnail_paths_by_instance_id\n        )\n        context.data[\"thumbnailSource\"] = (\n            thumbnail_paths_by_instance_id.get(None)\n        )\n\n        project_name = create_context.get_current_project_name()\n        if project_name:\n            context.data[\"projectName\"] = project_name\n\n        # Separate root instances and parented instances\n        instances_by_parent_id = collections.defaultdict(list)\n        root_instances = []\n        for created_instance in create_context.instances:\n            parent_id = created_instance.parent_instance_id\n            if parent_id is None:\n                root_instances.append(created_instance)\n            else:\n                instances_by_parent_id[parent_id].append(created_instance)\n\n        # Traverse instances from top to bottom\n        # - All instances without an existing parent are automatically\n        #   eliminated\n        filtered_instances = []\n        _queue = collections.deque()\n        _queue.append((root_instances, True))\n        while _queue:\n            created_instances, parent_is_active = _queue.popleft()\n            for created_instance in created_instances:\n                is_active = created_instance[\"active\"]\n                # Use a parent's active state if parent flags defines that\n                if (\n                    created_instance.parent_flags &amp; ParentFlags.share_active\n                    and is_active\n                ):\n                    is_active = parent_is_active\n\n                if is_active:\n                    filtered_instances.append(created_instance)\n\n                children = instances_by_parent_id[created_instance.id]\n                if children:\n                    _queue.append((children, is_active))\n\n        for created_instance in filtered_instances:\n            instance_data = created_instance.data_to_store()\n            thumbnail_path = thumbnail_paths_by_instance_id.get(\n                created_instance.id\n            )\n            self.create_instance(\n                context,\n                instance_data,\n                created_instance.transient_data,\n                thumbnail_path\n            )\n\n        # Update global data to context\n        context.data.update(create_context.context_data_to_store())\n        context.data[\"newPublishing\"] = True\n        # Update context data\n        folder_path = create_context.get_current_folder_path()\n        task_name = create_context.get_current_task_name()\n        for key, value in (\n            (\"AYON_PROJECT_NAME\", project_name),\n            (\"AYON_FOLDER_PATH\", folder_path),\n            (\"AYON_TASK_NAME\", task_name)\n        ):\n            if value is None:\n                os.environ.pop(key, None)\n            else:\n                os.environ[key] = value\n\n    def create_instance(\n        self,\n        context,\n        in_data,\n        transient_data,\n        thumbnail_path\n    ):\n        product_name = in_data[\"productName\"]\n        # If instance data already contain families then use it\n        instance_families = in_data.get(\"families\") or []\n        # Add product base type to families\n        product_type = in_data[\"productType\"]\n        product_base_type = in_data.get(\"productBaseType\")\n        if not product_base_type:\n            product_base_type = product_type\n        instance_families.append(product_base_type)\n\n        instance = context.create_instance(product_name)\n        instance.data.update({\n            \"publish\": True,\n            \"label\": in_data.get(\"label\") or product_name,\n            \"name\": product_name,\n            \"folderPath\": in_data[\"folderPath\"],\n            \"task\": in_data[\"task\"],\n            \"productName\": product_name,\n            \"productBaseType\": product_base_type,\n            \"productType\": product_type,\n            \"family\": product_base_type,\n            \"families\": instance_families,\n            \"representations\": [],\n            \"thumbnailSource\": thumbnail_path\n        })\n        for key, value in in_data.items():\n            if key not in instance.data:\n                instance.data[key] = value\n\n        instance.data[\"transientData\"] = transient_data\n\n        self.log.debug(\"collected instance: {}\".format(instance.data))\n        self.log.debug(\"parsing data: {}\".format(in_data))\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_hierarchy.html","title":"collect_hierarchy","text":""},{"location":"autoapi/client/ayon_core/plugins/publish/collect_hierarchy.html#client.ayon_core.plugins.publish.collect_hierarchy.CollectHierarchy","title":"<code>CollectHierarchy</code>","text":"<p>               Bases: <code>ContextPlugin</code>, <code>AYONPyblishPluginMixin</code></p> <p>Collecting hierarchy from <code>parents</code>.</p> <p>present in <code>clip</code> family instances coming from the request json data file</p> <p>It will add <code>hierarchical_context</code> into each instance for integrate plugins to be able to create needed parents for the context if they don't exist yet</p> Source code in <code>client/ayon_core/plugins/publish/collect_hierarchy.py</code> <pre><code>class CollectHierarchy(\n    pyblish.api.ContextPlugin,\n    publish.AYONPyblishPluginMixin,\n):\n    \"\"\"Collecting hierarchy from `parents`.\n\n    present in `clip` family instances coming from the request json data file\n\n    It will add `hierarchical_context` into each instance for integrate\n    plugins to be able to create needed parents for the context if they\n    don't exist yet\n    \"\"\"\n\n    label = \"Collect Hierarchy\"\n    order = pyblish.api.CollectorOrder - 0.076\n    settings_category = \"core\"\n\n    edit_shot_attributes_on_update = True\n    skip_shot_attributes_on_update: list[str] = []\n\n    @classmethod\n    def get_attr_defs_for_context(cls, create_context):\n        return [\n            BoolDef(\n                \"edit_shot_attributes_on_update\",\n                label=\"Edit shot attributes on update\",\n                default=cls.edit_shot_attributes_on_update\n            )\n        ]\n\n    def _get_shot_instances(self, context):\n        \"\"\"Get shot instances from context.\n\n        Args:\n            context (pyblish.api.Context): Context is a list of instances.\n\n        Returns:\n            list[pyblish.api.Instance]: A list of shot instances.\n        \"\"\"\n        shot_instances = []\n        for instance in context:\n            # shot data dict\n            product_base_type = instance.data.get(\"productBaseType\")\n            if not product_base_type:\n                product_base_type = instance.data[\"productType\"]\n            families = instance.data[\"families\"]\n\n            # exclude other families then \"shot\" with intersection\n            if \"shot\" not in (families + [product_base_type]):\n                self.log.debug(\"Skipping not a shot: {}\".format(families))\n                continue\n\n            # Skip if is not a hero track\n            if not instance.data.get(\"heroTrack\"):\n                self.log.debug(\"Skipping not a shot from hero track\")\n                continue\n\n            shot_instances.append(instance)\n\n        return shot_instances\n\n    def get_existing_folder_entities(self, project_name, shot_instances):\n        \"\"\"Get existing folder entities for given shot instances.\n\n        Args:\n            project_name (str): The name of the project.\n            shot_instances (list[pyblish.api.Instance]): A list of shot\n                instances.\n\n        Returns:\n            dict[str, dict]: A dictionary mapping folder paths to existing\n                folder entities.\n        \"\"\"\n        # first we need to get all folder paths from shot instances\n        folder_paths = {\n            instance.data[\"folderPath\"]\n            for instance in shot_instances\n        }\n        # then we get all existing folder entities with one request\n        existing_entities = {\n            folder_entity[\"path\"]: folder_entity\n            for folder_entity in ayon_api.get_folders(\n                project_name, folder_paths=folder_paths, fields={\"path\"})\n        }\n        for folder_path in folder_paths:\n            # add None value to non-existing folder entities\n            existing_entities.setdefault(folder_path, None)\n\n        return existing_entities\n\n    def process(self, context):\n        # get only shot instances from context\n        shot_instances = self._get_shot_instances(context)\n\n        if not shot_instances:\n            return\n\n        # get user input\n        values = self.get_attr_values_from_data(context.data)\n        edit_shot_attributes_on_update = values.get(\n            \"edit_shot_attributes_on_update\", None)\n\n        project_name = context.data[\"projectName\"]\n        final_context = {\n            project_name: {\n                \"entity_type\": \"project\",\n                \"children\": {}\n            },\n        }\n        temp_context = {}\n        existing_entities = self.get_existing_folder_entities(\n            project_name, shot_instances)\n\n        skip_shot_attributes_on_update: set[str] = set(\n            self.skip_shot_attributes_on_update\n        )\n\n        for instance in shot_instances:\n            folder_path = instance.data[\"folderPath\"]\n            self.log.debug(\n                f\"Processing instance: `{folder_path} {instance}` ...\")\n\n            shot_data = {\n                \"entity_type\": \"folder\",\n                # WARNING unless overwritten, default folder type is hardcoded\n                #   to shot\n                \"folder_type\": instance.data.get(\"folder_type\") or \"Shot\",\n                \"tasks\": instance.data.get(\"tasks\") or {},\n                \"comments\": instance.data.get(\"comments\", []),\n            }\n\n            shot_data[\"attributes\"] = {}\n            # we need to check if the shot entity already exists\n            # and if not the attributes needs to be added in case the option\n            # is disabled by settings\n\n            # Add attributes for new shots or update existing shots if enabled\n            folder_entity = existing_entities.get(folder_path)\n            if (\n                (folder_entity and edit_shot_attributes_on_update)\n                or not folder_entity\n            ):\n                for shot_attr in SHOT_ATTRS:\n                    attr_value = instance.data.get(shot_attr)\n                    if attr_value is None:\n                        # Shot attribute might not be defined (e.g. CSV ingest)\n                        self.log.debug(\n                            \"%s shot attribute is not defined for instance.\",\n                            shot_attr\n                        )\n                        continue\n\n                    if shot_attr in skip_shot_attributes_on_update:\n                        self.log.debug(\n                            \"%s shot attribute skipped as the attribute is\"\n                            \" blacklisted for update in settings.\", shot_attr\n                        )\n                        continue\n\n                    shot_data[\"attributes\"][shot_attr] = attr_value\n            else:\n                self.log.debug(\n                    \"Shot attributes will not be updated.\"\n                )\n\n            # Split by '/' for AYON where asset is a path\n            name = folder_path.split(\"/\")[-1]\n            actual = {name: shot_data}\n\n            for parent in reversed(instance.data[\"parents\"]):\n                next_dict = {\n                    parent[\"entity_name\"]: {\n                        \"entity_type\": \"folder\",\n                        \"folder_type\": parent[\"folder_type\"],\n                        \"children\": actual,\n                    }\n                }\n                actual = next_dict\n\n            temp_context = self._update_dict(temp_context, actual)\n\n        # skip if nothing for hierarchy available\n        if not temp_context:\n            return\n\n        final_context[project_name][\"children\"] = temp_context\n\n        # adding hierarchy context to context\n        context.data[\"hierarchyContext\"] = final_context\n        self.log.debug(\"context.data[hierarchyContext] is: {}\".format(\n            context.data[\"hierarchyContext\"]))\n\n    def _update_dict(self, parent_dict, child_dict):\n        \"\"\"Nesting each child into its parent.\n\n        Args:\n            parent_dict (dict): parent dict that should be nested with children\n            child_dict (dict): children dict which should be ingested\n        \"\"\"\n\n        for key in parent_dict:\n            if key in child_dict and isinstance(parent_dict[key], dict):\n                child_dict[key] = self._update_dict(\n                    parent_dict[key], child_dict[key]\n                )\n            else:\n                if parent_dict.get(key) and child_dict.get(key):\n                    continue\n                else:\n                    child_dict[key] = parent_dict[key]\n\n        return child_dict\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_hierarchy.html#client.ayon_core.plugins.publish.collect_hierarchy.CollectHierarchy.get_existing_folder_entities","title":"<code>get_existing_folder_entities(project_name, shot_instances)</code>","text":"<p>Get existing folder entities for given shot instances.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>The name of the project.</p> required <code>shot_instances</code> <code>list[Instance]</code> <p>A list of shot instances.</p> required <p>Returns:</p> Type Description <p>dict[str, dict]: A dictionary mapping folder paths to existing folder entities.</p> Source code in <code>client/ayon_core/plugins/publish/collect_hierarchy.py</code> <pre><code>def get_existing_folder_entities(self, project_name, shot_instances):\n    \"\"\"Get existing folder entities for given shot instances.\n\n    Args:\n        project_name (str): The name of the project.\n        shot_instances (list[pyblish.api.Instance]): A list of shot\n            instances.\n\n    Returns:\n        dict[str, dict]: A dictionary mapping folder paths to existing\n            folder entities.\n    \"\"\"\n    # first we need to get all folder paths from shot instances\n    folder_paths = {\n        instance.data[\"folderPath\"]\n        for instance in shot_instances\n    }\n    # then we get all existing folder entities with one request\n    existing_entities = {\n        folder_entity[\"path\"]: folder_entity\n        for folder_entity in ayon_api.get_folders(\n            project_name, folder_paths=folder_paths, fields={\"path\"})\n    }\n    for folder_path in folder_paths:\n        # add None value to non-existing folder entities\n        existing_entities.setdefault(folder_path, None)\n\n    return existing_entities\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_host_name.html","title":"collect_host_name","text":"Requires <p>None</p> Provides <p>context -&gt; hostName (str)</p>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_host_name.html#client.ayon_core.plugins.publish.collect_host_name.CollectHostName","title":"<code>CollectHostName</code>","text":"<p>               Bases: <code>ContextPlugin</code></p> <p>Collect AYON host name to context.</p> Source code in <code>client/ayon_core/plugins/publish/collect_host_name.py</code> <pre><code>class CollectHostName(pyblish.api.ContextPlugin):\n    \"\"\"Collect AYON host name to context.\"\"\"\n\n    label = \"Collect Host Name\"\n    order = pyblish.api.CollectorOrder - 0.5\n\n    def process(self, context):\n        host_name = context.data.get(\"hostName\")\n        if host_name:\n            return\n\n        # Use AYON_HOST_NAME to get host name if available\n        context.data[\"hostName\"] = os.environ.get(\"AYON_HOST_NAME\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_input_representations_to_versions.html","title":"collect_input_representations_to_versions","text":""},{"location":"autoapi/client/ayon_core/plugins/publish/collect_input_representations_to_versions.html#client.ayon_core.plugins.publish.collect_input_representations_to_versions.CollectInputRepresentationsToVersions","title":"<code>CollectInputRepresentationsToVersions</code>","text":"<p>               Bases: <code>ContextPlugin</code></p> <p>Converts collected input representations to input versions.</p> <p>Any data in <code>instance.data[\"inputRepresentations\"]</code> gets converted into <code>instance.data[\"inputVersions\"]</code> as supported in AYON.</p> Source code in <code>client/ayon_core/plugins/publish/collect_input_representations_to_versions.py</code> <pre><code>class CollectInputRepresentationsToVersions(pyblish.api.ContextPlugin):\n    \"\"\"Converts collected input representations to input versions.\n\n    Any data in `instance.data[\"inputRepresentations\"]` gets converted into\n    `instance.data[\"inputVersions\"]` as supported in AYON.\n\n    \"\"\"\n    # This is a ContextPlugin because then we can query the database only once\n    # for the conversion of representation ids to version ids (optimization)\n    label = \"Input Representations to Versions\"\n    order = pyblish.api.CollectorOrder + 0.499\n    hosts = [\"*\"]\n\n    def process(self, context: pyblish.api.Context):\n        # Query all version ids for representation ids from the database once\n        representation_ids: set[str] = set()\n        for instance in context:\n            inst_repre = instance.data.get(\"inputRepresentations\", [])\n            if inst_repre:\n                representation_ids.update(inst_repre)\n\n        # Ignore representation ids that are not valid\n        representation_ids = {\n            representation_id for representation_id in representation_ids\n            if ayon_api.utils.convert_entity_id(representation_id)\n        }\n        project_name: str = context.data[\"projectName\"]\n        repre_entities: list[dict[str, str]] = list(\n            ayon_api.get_representations(\n                project_name=project_name,\n                representation_ids=representation_ids,\n                fields={\"id\", \"versionId\"}\n            )\n        )\n\n        # Get version input link info for the version ids\n        version_info_by_version_id = version_ids_to_input_versions(\n            project_name=project_name,\n            version_ids={repre[\"versionId\"] for repre in repre_entities},\n            log=self.log\n        )\n        version_info_by_repre_id: dict[str, Optional[InputVersion]] = {}\n        for repre in repre_entities:\n            version_id: str = repre[\"versionId\"]\n            version_info_by_repre_id[repre[\"id\"]] = (\n                version_info_by_version_id.get(version_id)\n            )\n\n        for instance in context:\n            inst_repre_ids: list[str] = instance.data.get(\n                \"inputRepresentations\", []\n            )\n            if not inst_repre_ids:\n                continue\n\n            input_versions: list[InputVersion] = instance.data.setdefault(\n                \"inputVersions\", []\n            )\n            for repre_id in inst_repre_ids:\n                version_info = version_info_by_repre_id.get(repre_id)\n                if version_info:\n                    input_versions.append(version_info)\n                else:\n                    self.log.debug(\n                        \"Representation id {} skipped because its version is \"\n                        \"not found in current project. Likely it is loaded \"\n                        \"from a library project or uses a deleted \"\n                        \"representation or version.\".format(repre_id)\n                    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_instance_entities.html","title":"collect_instance_entities","text":"Requires <p>context  -&gt; projectName context  -&gt; projectEntity instance -&gt; folderPath instance -&gt; task</p> Provides <p>instance -&gt; projectEntity instance -&gt; folderEntity instance -&gt; taskEntity</p>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_instance_entities.html#client.ayon_core.plugins.publish.collect_instance_entities.CollectInstanceEntities","title":"<code>CollectInstanceEntities</code>","text":"<p>               Bases: <code>ContextPlugin</code></p> <p>Collect instance entities based on their context.</p> <p>Plugin is running for all instances on context even not active instances.</p> <p>Logic was separated from CollectInstanceAnatomy data to run the logic     earlier. Instances that don't have set 'folderPath' will be skipped.     If there is a plugin adding or changing entities after this plugin     they should make sure that correct entities are filled. Collect     anatomy instance data plugin can do that but it runs later.</p> Source code in <code>client/ayon_core/plugins/publish/collect_instance_entities.py</code> <pre><code>class CollectInstanceEntities(pyblish.api.ContextPlugin):\n    \"\"\"Collect instance entities based on their context.\n\n    Plugin is running for all instances on context even not active instances.\n\n    Logic was separated from CollectInstanceAnatomy data to run the logic\n        earlier. Instances that don't have set 'folderPath' will be skipped.\n        If there is a plugin adding or changing entities after this plugin\n        they should make sure that correct entities are filled. Collect\n        anatomy instance data plugin can do that but it runs later.\n    \"\"\"\n\n    order = pyblish.api.CollectorOrder - 0.4\n    label = \"Collect Instance entities\"\n\n    def process(self, context):\n        self.log.debug(\"Collecting entities all instances.\")\n        self.fill_project_entity(context)\n        self.fill_folder_entities(context)\n        self.fill_task_entities(context)\n\n    def fill_project_entity(self, context):\n        project_entity = context.data.get(\"projectEntity\")\n        if not project_entity:\n            project_name = context.data[\"projectName\"]\n            project_entity = ayon_api.get_project(project_name)\n            context.data[\"projectEntity\"] = project_entity\n\n        for instance in context:\n            instance.data[\"projectEntity\"] = project_entity\n\n    def fill_folder_entities(self, context):\n        self.log.debug(\"Fetching folder entities for instances.\")\n\n        project_name = context.data[\"projectName\"]\n        context_folder_entity = context.data.get(\"folderEntity\")\n        context_folder_path = None\n        if context_folder_entity:\n            context_folder_path = context_folder_entity[\"path\"]\n\n        instances_missing_folder = collections.defaultdict(list)\n        for instance in context:\n            instance_folder_entity = instance.data.get(\"folderEntity\")\n            instance_folder_path = instance.data.get(\"folderPath\")\n\n            # There is possibility that folderEntity on instance is set\n            if instance_folder_entity:\n                instance_folder_path = instance_folder_entity[\"path\"]\n                if instance_folder_path == instance_folder_path:\n                    continue\n\n            if not instance_folder_path:\n                continue\n\n            # Check if folder path is the same as what is in context\n            # - they may be different, e.g. during editorial publishing\n            if (\n                context_folder_path\n                and context_folder_path == instance_folder_path\n            ):\n                instance.data[\"folderEntity\"] = context_folder_entity\n\n            else:\n                instances_missing_folder[instance_folder_path].append(\n                    instance\n                )\n\n        if not instances_missing_folder:\n            self.log.debug(\"All instances already had right folder entity.\")\n            return\n\n        folder_paths = set(instances_missing_folder.keys())\n        joined_folder_paths = \", \".join(\n            [f\"\\\"{path}\\\"\" for path in folder_paths]\n        )\n        self.log.debug(\n            f\"Fetching folder entities with paths: {joined_folder_paths}\"\n        )\n\n        folder_entities_by_path = {\n            folder_entity[\"path\"]: folder_entity\n            for folder_entity in ayon_api.get_folders(\n                project_name, folder_paths=folder_paths\n            )\n        }\n\n        not_found_folder_paths = set()\n        for folder_path, instances in instances_missing_folder.items():\n            folder_entity = folder_entities_by_path.get(folder_path)\n            if not folder_entity:\n                not_found_folder_paths.add(folder_path)\n                continue\n\n            for instance in instances:\n                instance.data[\"folderEntity\"] = folder_entity\n\n        if not_found_folder_paths:\n            joined_folder_paths = \", \".join(\n                [f\"\\\"{path}\\\"\" for path in not_found_folder_paths]\n            )\n            self.log.warning(\n                f\"Not found folder entities with paths {joined_folder_paths}.\"\n            )\n\n    def fill_task_entities(self, context):\n        self.log.debug(\"Fetching task entities for instances.\")\n        project_name = context.data[\"projectName\"]\n\n        context_folder_entity = context.data.get(\"folderEntity\")\n        context_folder_id = None\n        if context_folder_entity:\n            context_folder_id = context_folder_entity[\"id\"]\n        context_task_entity = context.data.get(\"taskEntity\")\n        context_task_name = None\n        if context_task_entity:\n            context_task_name = context_task_entity[\"name\"]\n\n        instances_missing_task = {}\n        folder_path_by_id = {}\n        for instance in context:\n            task_entity = instance.data.get(\"taskEntity\")\n            # Make sure 'taskEntity' key is available in 'instance.data'\n            if task_entity is None:\n                instance.data[\"taskEntity\"] = None\n\n            # Skip instances without task\n            task_name = instance.data.get(\"task\")\n            if not task_name:\n                continue\n\n            # Skip instances without available folder entity\n            folder_entity = instance.data.get(\"folderEntity\")\n            # Skip if instance does not have filled folder entity\n            if not folder_entity:\n                continue\n\n            folder_id = folder_entity[\"id\"]\n            folder_path_by_id[folder_id] = folder_entity[\"path\"]\n\n            # There is possibility that taskEntity on instance is set\n            if task_entity:\n                task_parent_id = task_entity[\"folderId\"]\n                instance_task_name = task_entity[\"name\"]\n                if (\n                    folder_id == task_parent_id\n                    and instance_task_name == task_name\n                ):\n                    continue\n\n            # Check if folder path is the same as what is in context\n            # - they may be different, e.g. in NukeStudio\n            if (\n                context_folder_id == folder_id\n                and context_task_name == task_name\n            ):\n                instance.data[\"taskEntity\"] = context_task_entity\n                continue\n\n            by_folder_id = instances_missing_task.setdefault(folder_id, {})\n            by_task_name = by_folder_id.setdefault(task_name, [])\n            by_task_name.append(instance)\n\n        if not instances_missing_task:\n            self.log.debug(\"All instances already had right task entity.\")\n            return\n\n        self.log.debug(\"Fetching task entities\")\n\n        all_folder_ids = set(instances_missing_task.keys())\n        all_task_names = set()\n        for per_task in instances_missing_task.values():\n            all_task_names |= set(per_task.keys())\n        all_task_names.discard(None)\n\n        task_entities = []\n        if all_task_names:\n            task_entities = ayon_api.get_tasks(\n                project_name,\n                folder_ids=all_folder_ids,\n                task_names=all_task_names\n            )\n        task_entity_by_ids = {}\n        for task_entity in task_entities:\n            folder_id = task_entity[\"folderId\"]\n            task_name = task_entity[\"name\"]\n            by_folder_id = task_entity_by_ids.setdefault(folder_id, {})\n            by_folder_id[task_name] = task_entity\n\n        not_found_task_paths = []\n        for folder_id, by_task in instances_missing_task.items():\n            for task_name, instances in by_task.items():\n                task_entity = (\n                    task_entity_by_ids\n                    .get(folder_id, {})\n                    .get(task_name)\n                )\n                if task_name and not task_entity:\n                    folder_path = folder_path_by_id[folder_id]\n                    not_found_task_paths.append(\n                        \"/\".join([folder_path, task_name])\n                    )\n\n                for instance in instances:\n                    instance.data[\"taskEntity\"] = task_entity\n\n        if not_found_task_paths:\n            joined_paths = \", \".join(\n                [f'\"{path}\"' for path in not_found_task_paths]\n            )\n            self.log.warning(\n                f\"Not found task entities with paths {joined_paths}.\"\n            )\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_machine_name.html","title":"collect_machine_name","text":"Requires <p>none</p> Provides <p>context     -&gt; machine (str)</p>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_managed_staging_dir.html","title":"collect_managed_staging_dir","text":"Requires <p>anatomy</p> Provides <p>instance.data     -&gt; stagingDir (folder path)                   -&gt; stagingDir_persistent (bool)</p>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_managed_staging_dir.html#client.ayon_core.plugins.publish.collect_managed_staging_dir.CollectManagedStagingDir","title":"<code>CollectManagedStagingDir</code>","text":"<p>               Bases: <code>InstancePlugin</code></p> <p>Apply matching Staging Dir profile to a instance.</p> <p>Apply Staging dir via profiles could be useful in specific use cases where is desirable to have temporary renders in specific, persistent folders, could be on disks optimized for speed for example.</p> <p>It is studio's responsibility to clean up obsolete folders with data.</p> Location of the folder is configured in <p><code>ayon+anatomy://_/templates/staging</code>.</p> <p>Which product type/task type/product is applicable is configured in:     <code>ayon+settings://core/tools/publish/custom_staging_dir_profiles</code></p> Source code in <code>client/ayon_core/plugins/publish/collect_managed_staging_dir.py</code> <pre><code>class CollectManagedStagingDir(pyblish.api.InstancePlugin):\n    \"\"\"Apply matching Staging Dir profile to a instance.\n\n    Apply Staging dir via profiles could be useful in specific use cases\n    where is desirable to have temporary renders in specific,\n    persistent folders, could be on disks optimized for speed for example.\n\n    It is studio's responsibility to clean up obsolete folders with data.\n\n    Location of the folder is configured in:\n        `ayon+anatomy://_/templates/staging`.\n\n    Which product type/task type/product is applicable is configured in:\n        `ayon+settings://core/tools/publish/custom_staging_dir_profiles`\n    \"\"\"\n\n    label = \"Collect Managed Staging Directory\"\n    order = pyblish.api.CollectorOrder + 0.4990\n\n    def process(self, instance: pyblish.api.Instance):\n        \"\"\" Collect the staging data and stores it to the instance.\n\n        Args:\n            instance (object): The instance to inspect.\n        \"\"\"\n        staging_dir_path = get_instance_staging_dir(instance)\n        persistence: bool = instance.data.get(\"stagingDir_persistent\", False)\n\n        self.log.debug(\n            f\"Instance staging dir was set to `{staging_dir_path}` \"\n            f\"and persistence is set to `{persistence}`\"\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_managed_staging_dir.html#client.ayon_core.plugins.publish.collect_managed_staging_dir.CollectManagedStagingDir.process","title":"<code>process(instance)</code>","text":"<p>Collect the staging data and stores it to the instance.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>object</code> <p>The instance to inspect.</p> required Source code in <code>client/ayon_core/plugins/publish/collect_managed_staging_dir.py</code> <pre><code>def process(self, instance: pyblish.api.Instance):\n    \"\"\" Collect the staging data and stores it to the instance.\n\n    Args:\n        instance (object): The instance to inspect.\n    \"\"\"\n    staging_dir_path = get_instance_staging_dir(instance)\n    persistence: bool = instance.data.get(\"stagingDir_persistent\", False)\n\n    self.log.debug(\n        f\"Instance staging dir was set to `{staging_dir_path}` \"\n        f\"and persistence is set to `{persistence}`\"\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_otio_frame_ranges.html","title":"collect_otio_frame_ranges","text":"<p>Plugin for collecting OTIO frame ranges and related timing information.</p> <p>This module contains a unified plugin that handles: - Basic timeline frame ranges - Source media frame ranges - Retimed clip frame ranges</p>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_otio_frame_ranges.html#client.ayon_core.plugins.publish.collect_otio_frame_ranges.CollectOtioRanges","title":"<code>CollectOtioRanges</code>","text":"<p>               Bases: <code>InstancePlugin</code></p> <p>Collect all OTIO-related frame ranges and timing information.</p> <p>This plugin handles collection of: - Basic timeline frame ranges with handles - Source media frame ranges with handles - Retimed clip frame ranges</p> Requires <p>otioClip (otio.schema.Clip): OTIO clip object workfileFrameStart (int): Starting frame of work file</p> Optional <p>shotDurationFromSource (int): Duration from source if retimed</p> Provides <p>frameStart (int): Start frame in timeline frameEnd (int): End frame in timeline clipIn (int): Clip in point clipOut (int): Clip out point clipInH (int): Clip in point with handles clipOutH (int): Clip out point with handles sourceStart (int): Source media start frame sourceEnd (int): Source media end frame sourceStartH (int): Source media start frame with handles sourceEndH (int): Source media end frame with handles</p> Source code in <code>client/ayon_core/plugins/publish/collect_otio_frame_ranges.py</code> <pre><code>class CollectOtioRanges(pyblish.api.InstancePlugin):\n    \"\"\"Collect all OTIO-related frame ranges and timing information.\n\n    This plugin handles collection of:\n    - Basic timeline frame ranges with handles\n    - Source media frame ranges with handles\n    - Retimed clip frame ranges\n\n    Requires:\n        otioClip (otio.schema.Clip): OTIO clip object\n        workfileFrameStart (int): Starting frame of work file\n\n    Optional:\n        shotDurationFromSource (int): Duration from source if retimed\n\n    Provides:\n        frameStart (int): Start frame in timeline\n        frameEnd (int): End frame in timeline\n        clipIn (int): Clip in point\n        clipOut (int): Clip out point\n        clipInH (int): Clip in point with handles\n        clipOutH (int): Clip out point with handles\n        sourceStart (int): Source media start frame\n        sourceEnd (int): Source media end frame\n        sourceStartH (int): Source media start frame with handles\n        sourceEndH (int): Source media end frame with handles\n    \"\"\"\n\n    label = \"Collect OTIO Ranges\"\n    order = pyblish.api.CollectorOrder - 0.08\n    families = [\"shot\", \"clip\"]\n\n    def process(self, instance):\n        \"\"\"Process the instance to collect all frame ranges.\n\n        Args:\n            instance: The instance to process\n        \"\"\"\n        if not validate_otio_clip(instance, self.log):\n            return\n\n        import opentimelineio as otio\n\n        otio_clip = instance.data[\"otioClip\"]\n        if isinstance(\n            otio_clip.media_reference,\n            otio.schema.MissingReference\n        ):\n            self.log.info(\"Clip has no media reference\")\n            return\n\n        # Collect timeline ranges if workfile start frame is available\n        if \"workfileFrameStart\" in instance.data:\n            self._collect_timeline_ranges(instance, otio_clip)\n\n        # Traypublisher Simple or Advanced editorial publishing is\n        # working with otio clips which are having no available range\n        # because they are not having any media references.\n        try:\n            otio_clip.available_range()\n            has_available_range = True\n        except otio._otio.CannotComputeAvailableRangeError:\n            self.log.info(\"Clip has no available range\")\n            has_available_range = False\n\n        # Collect source ranges if clip has available range\n        if has_available_range:\n            self._collect_source_ranges(instance, otio_clip)\n\n        # Handle retimed ranges if source duration is available\n        if \"shotDurationFromSource\" in instance.data:\n            self._collect_retimed_ranges(instance, otio_clip)\n\n    def _collect_timeline_ranges(self, instance, otio_clip):\n        \"\"\"Collect basic timeline frame ranges.\"\"\"\n        from ayon_core.pipeline.editorial import (\n            otio_range_to_frame_range,\n            otio_range_with_handles,\n        )\n\n        workfile_start = instance.data[\"workfileFrameStart\"]\n\n        # Get timeline ranges\n        otio_tl_range = otio_clip.range_in_parent()\n        otio_tl_range_handles = otio_range_with_handles(\n            otio_tl_range,\n            instance\n        )\n\n        # Convert to frames\n        tl_start, tl_end = otio_range_to_frame_range(otio_tl_range)\n        tl_start_h, tl_end_h = otio_range_to_frame_range(otio_tl_range_handles)\n\n        frame_start = workfile_start\n        frame_end = frame_start + otio_tl_range.duration.to_frames() - 1\n\n        data = {\n            \"frameStart\": frame_start,\n            \"frameEnd\": frame_end,\n            \"clipIn\": tl_start,\n            \"clipOut\": tl_end - 1,\n            \"clipInH\": tl_start_h,\n            \"clipOutH\": tl_end_h - 1,\n        }\n        instance.data.update(data)\n        self.log.debug(f\"Added frame ranges: {pformat(data)}\")\n\n    def _collect_source_ranges(self, instance, otio_clip):\n        \"\"\"Collect source media frame ranges.\"\"\"\n        import opentimelineio as otio\n\n        # Get source ranges\n        otio_src_range = otio_clip.source_range\n        otio_available_range = otio_clip.available_range()\n\n        # Backward-compatibility for Hiero OTIO exporter.\n        # NTSC compatibility might introduce floating rates, when these are\n        # not exactly the same (23.976 vs 23.976024627685547)\n        # this will cause precision issue in computation.\n        # Currently round to 2 decimals for comparison,\n        # but this should always rescale after that.\n        rounded_av_rate = round(otio_available_range.start_time.rate, 2)\n        rounded_src_rate = round(otio_src_range.start_time.rate, 2)\n        if rounded_av_rate != rounded_src_rate:\n            conformed_src_in = otio_src_range.start_time.rescaled_to(\n                otio_available_range.start_time.rate\n            )\n            conformed_src_duration = otio_src_range.duration.rescaled_to(\n                otio_available_range.duration.rate\n            )\n            conformed_source_range = otio.opentime.TimeRange(\n                start_time=conformed_src_in,\n                duration=conformed_src_duration\n            )\n        else:\n            conformed_source_range = otio_src_range\n\n        source_start = conformed_source_range.start_time\n        source_end = source_start + conformed_source_range.duration\n        handle_start = otio.opentime.RationalTime(\n            instance.data.get(\"handleStart\", 0),\n            source_start.rate\n        )\n        handle_end = otio.opentime.RationalTime(\n            instance.data.get(\"handleEnd\", 0),\n            source_start.rate\n        )\n        source_start_h = source_start - handle_start\n        source_end_h = source_end + handle_end\n        data = {\n            \"sourceStart\": source_start.to_frames(),\n            \"sourceEnd\": source_end.to_frames() - 1,\n            \"sourceStartH\": source_start_h.to_frames(),\n            \"sourceEndH\": source_end_h.to_frames() - 1,\n        }\n        instance.data.update(data)\n        self.log.debug(f\"Added source ranges: {pformat(data)}\")\n\n    def _collect_retimed_ranges(self, instance, otio_clip):\n        \"\"\"Handle retimed clip frame ranges.\"\"\"\n        from ayon_core.pipeline.editorial import get_media_range_with_retimes\n\n        retimed_attributes = get_media_range_with_retimes(otio_clip, 0, 0)\n        self.log.debug(f\"Retimed attributes: {retimed_attributes}\")\n\n        frame_start = instance.data[\"frameStart\"]\n        media_in = int(retimed_attributes[\"mediaIn\"])\n        media_out = int(retimed_attributes[\"mediaOut\"])\n        frame_end = frame_start + (media_out - media_in)\n\n        data = {\n            \"frameStart\": frame_start,\n            \"frameEnd\": frame_end,\n            \"sourceStart\": media_in,\n            \"sourceEnd\": media_out,\n            \"sourceStartH\": media_in - int(retimed_attributes[\"handleStart\"]),\n            \"sourceEndH\": media_out + int(retimed_attributes[\"handleEnd\"]),\n        }\n\n        instance.data.update(data)\n        self.log.debug(f\"Updated retimed values: {data}\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_otio_frame_ranges.html#client.ayon_core.plugins.publish.collect_otio_frame_ranges.CollectOtioRanges.process","title":"<code>process(instance)</code>","text":"<p>Process the instance to collect all frame ranges.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <p>The instance to process</p> required Source code in <code>client/ayon_core/plugins/publish/collect_otio_frame_ranges.py</code> <pre><code>def process(self, instance):\n    \"\"\"Process the instance to collect all frame ranges.\n\n    Args:\n        instance: The instance to process\n    \"\"\"\n    if not validate_otio_clip(instance, self.log):\n        return\n\n    import opentimelineio as otio\n\n    otio_clip = instance.data[\"otioClip\"]\n    if isinstance(\n        otio_clip.media_reference,\n        otio.schema.MissingReference\n    ):\n        self.log.info(\"Clip has no media reference\")\n        return\n\n    # Collect timeline ranges if workfile start frame is available\n    if \"workfileFrameStart\" in instance.data:\n        self._collect_timeline_ranges(instance, otio_clip)\n\n    # Traypublisher Simple or Advanced editorial publishing is\n    # working with otio clips which are having no available range\n    # because they are not having any media references.\n    try:\n        otio_clip.available_range()\n        has_available_range = True\n    except otio._otio.CannotComputeAvailableRangeError:\n        self.log.info(\"Clip has no available range\")\n        has_available_range = False\n\n    # Collect source ranges if clip has available range\n    if has_available_range:\n        self._collect_source_ranges(instance, otio_clip)\n\n    # Handle retimed ranges if source duration is available\n    if \"shotDurationFromSource\" in instance.data:\n        self._collect_retimed_ranges(instance, otio_clip)\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_otio_frame_ranges.html#client.ayon_core.plugins.publish.collect_otio_frame_ranges.validate_otio_clip","title":"<code>validate_otio_clip(instance, logger)</code>","text":"<p>Validate if instance has required OTIO clip data.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <p>The instance to validate</p> required <code>logger</code> <p>Logger object to use for debug messages</p> required <p>Returns:</p> Name Type Description <code>bool</code> <p>True if valid, False otherwise</p> Source code in <code>client/ayon_core/plugins/publish/collect_otio_frame_ranges.py</code> <pre><code>def validate_otio_clip(instance, logger):\n    \"\"\"Validate if instance has required OTIO clip data.\n\n    Args:\n        instance: The instance to validate\n        logger: Logger object to use for debug messages\n\n    Returns:\n        bool: True if valid, False otherwise\n    \"\"\"\n    if not instance.data.get(\"otioClip\"):\n        logger.debug(\"Skipping collect OTIO range - no clip found.\")\n        return False\n    return True\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_otio_review.html","title":"collect_otio_review","text":"Requires <p>instance -&gt; otioClip context -&gt; otioTimeline</p> Optional <p>instance -&gt; reviewTrack</p> Provides <p>instance -&gt; otioReviewClips instance -&gt; families (adding [\"review\", \"ftrack\"])</p>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_otio_review.html#client.ayon_core.plugins.publish.collect_otio_review.CollectOtioReview","title":"<code>CollectOtioReview</code>","text":"<p>               Bases: <code>InstancePlugin</code></p> <p>Get matching otio track from defined review layer</p> Source code in <code>client/ayon_core/plugins/publish/collect_otio_review.py</code> <pre><code>class CollectOtioReview(pyblish.api.InstancePlugin):\n    \"\"\"Get matching otio track from defined review layer\"\"\"\n\n    label = \"Collect OTIO Review\"\n    order = pyblish.api.CollectorOrder - 0.078\n    families = [\"clip\"]\n    hosts = [\"resolve\", \"hiero\", \"flame\"]\n\n    def process(self, instance):\n        # Not all hosts can import this module.\n        import opentimelineio as otio\n\n        # get basic variables\n        otio_review_clips = []\n        otio_timeline = instance.context.data[\"otioTimeline\"]\n        otio_clip = instance.data[\"otioClip\"]\n        self.log.debug(\"__ otioClip: {}\".format(otio_clip))\n        # optionally get `reviewTrack`\n        review_track_name = instance.data.get(\"reviewTrack\")\n\n        # [clip_media] setting:\n        # Extract current clip source range as reviewable.\n        # Flag review content from otio_clip.\n        if not review_track_name and \"review\" in instance.data[\"families\"]:\n            otio_review_clips = [otio_clip]\n\n        # skip if no review track available\n        elif not review_track_name:\n            return\n\n        # generate range in parent\n        otio_tl_range = otio_clip.range_in_parent()\n\n        # calculate real timeline end needed for the clip\n        clip_frame_end = int(\n            otio_tl_range.start_time.value + otio_tl_range.duration.value)\n\n        # loop all tracks and match with name in `reviewTrack`\n        for track in otio_timeline.tracks:\n\n            # No review track defined, skip the loop\n            if review_track_name is None:\n                break\n\n            # Not current review track, skip it.\n            if review_track_name != track.name:\n                continue\n\n            # process correct track\n            # establish gap\n            otio_gap = None\n\n            # get track parent range\n            track_rip = track.range_in_parent()\n\n            # calculate real track end frame\n            track_frame_end = int(track_rip.end_time_exclusive().value)\n\n            # check if the end of track is not lower then clip requirement\n            if clip_frame_end &gt; track_frame_end:\n                # calculate diference duration\n                gap_duration = clip_frame_end - track_frame_end\n                # create rational time range for gap\n                otio_gap_range = otio.opentime.TimeRange(\n                    start_time=otio.opentime.RationalTime(\n                        float(0),\n                        track_rip.start_time.rate\n                    ),\n                    duration=otio.opentime.RationalTime(\n                        float(gap_duration),\n                        track_rip.start_time.rate\n                    )\n                )\n                # crate gap\n                otio_gap = otio.schema.Gap(source_range=otio_gap_range)\n\n            # trim available clips from devined track as reviewable source\n            otio_review_clips = otio.algorithms.track_trimmed_to_range(\n                track,\n                otio_tl_range\n            )\n            # add gap at the end if track end is shorter then needed\n            if otio_gap:\n                otio_review_clips.append(otio_gap)\n\n        if otio_review_clips:\n            # add review track to instance and change label to reflect it\n            label = instance.data.get(\"label\", instance.data[\"productName\"])\n            instance.data[\"label\"] = label + \" (review)\"\n            instance.data[\"families\"] += [\"review\", \"ftrack\"]\n            instance.data[\"otioReviewClips\"] = otio_review_clips\n\n            self.log.info(\n                \"Creating review track: {}\".format(otio_review_clips))\n\n            # get colorspace from metadata if available\n            # get metadata from first clip with media reference\n            r_otio_cl = next(\n                (\n                    clip\n                    for clip in otio_review_clips\n                    if (\n                        isinstance(clip, otio.schema.Clip)\n                        and clip.media_reference\n                    )\n                ),\n                None\n            )\n            if r_otio_cl is not None:\n                media_ref = r_otio_cl.media_reference\n                media_metadata = media_ref.metadata\n\n                # TODO: we might need some alternative method since\n                #       native OTIO exports do not support ayon metadata\n                review_colorspace = media_metadata.get(\n                    \"ayon.source.colorspace\"\n                )\n                if review_colorspace is None:\n                    # Backwards compatibility for older scenes\n                    review_colorspace = media_metadata.get(\n                        \"openpype.source.colourtransform\"\n                    )\n                if review_colorspace:\n                    instance.data[\"reviewColorspace\"] = review_colorspace\n                    self.log.info(\n                        \"Review colorspace: {}\".format(review_colorspace))\n\n        self.log.debug(\n            \"_ instance.data: {}\".format(pformat(instance.data)))\n        self.log.debug(\n            \"_ families: {}\".format(instance.data[\"families\"]))\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_otio_subset_resources.html","title":"collect_otio_subset_resources","text":"Requires <p>instance -&gt; otio_clip</p> Provides <p>instance -&gt; otioReviewClips</p>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_otio_subset_resources.html#client.ayon_core.plugins.publish.collect_otio_subset_resources.CollectOtioSubsetResources","title":"<code>CollectOtioSubsetResources</code>","text":"<p>               Bases: <code>InstancePlugin</code>, <code>ColormanagedPyblishPluginMixin</code></p> <p>Get Resources for a product version</p> Source code in <code>client/ayon_core/plugins/publish/collect_otio_subset_resources.py</code> <pre><code>class CollectOtioSubsetResources(\n    pyblish.api.InstancePlugin,\n    publish.ColormanagedPyblishPluginMixin\n):\n    \"\"\"Get Resources for a product version\"\"\"\n\n    label = \"Collect OTIO Subset Resources\"\n    order = pyblish.api.CollectorOrder + 0.491\n    families = [\"clip\"]\n    hosts = [\"resolve\", \"hiero\", \"flame\"]\n\n    def process(self, instance):\n        # Not all hosts can import these modules.\n        import opentimelineio as otio\n        from ayon_core.pipeline.editorial import (\n            get_media_range_with_retimes,\n            range_from_frames,\n            make_sequence_collection\n        )\n\n        product_base_type = instance.data.get(\"productBaseType\")\n        if not product_base_type:\n            product_base_type = instance.data[\"productType\"]\n\n        if \"audio\" in product_base_type:\n            return\n\n        if not instance.data.get(\"representations\"):\n            instance.data[\"representations\"] = []\n\n        if not instance.data.get(\"versionData\"):\n            instance.data[\"versionData\"] = {}\n\n        template_name = self.get_template_name(instance)\n        anatomy = instance.context.data[\"anatomy\"]\n        publish_path_template = anatomy.get_template_item(\n            \"publish\", template_name, \"path\"\n        ).template\n        template = os.path.normpath(publish_path_template)\n        self.log.debug(\n            \"&gt;&gt; template: {}\".format(template))\n\n        handle_start = instance.data[\"handleStart\"]\n        handle_end = instance.data[\"handleEnd\"]\n\n        # get basic variables\n        otio_clip = instance.data[\"otioClip\"]\n        if isinstance(\n            otio_clip.media_reference,\n            otio.schema.MissingReference\n        ):\n            self.log.info(\"Clip has no media reference\")\n            return\n\n        otio_available_range = otio_clip.available_range()\n        media_fps = otio_available_range.start_time.rate\n        available_duration = otio_available_range.duration.value\n\n        # get available range trimmed with processed retimes\n        retimed_attributes = get_media_range_with_retimes(\n            otio_clip, handle_start, handle_end)\n        self.log.debug(\n            \"&gt;&gt; retimed_attributes: {}\".format(retimed_attributes))\n\n        # break down into variables as rounded frame numbers\n        #\n        # 0             1               2              3             4\n        # |-------------|---------------|--------------|-------------|\n        #         |_______________media range_______________|\n        #        0.6                                       3.2\n        #\n        #  As rounded frames, media_in = 0 and media_out = 4\n        media_in = math.floor(retimed_attributes[\"mediaIn\"])\n        media_out = math.ceil(retimed_attributes[\"mediaOut\"])\n\n        handle_start = int(retimed_attributes[\"handleStart\"])\n        handle_end = int(retimed_attributes[\"handleEnd\"])\n\n        # set versiondata if any retime\n        version_data = retimed_attributes.get(\"versionData\")\n\n        if version_data:\n            instance.data[\"versionData\"].update(version_data)\n\n        # convert to available frame range with handles\n        a_frame_start_h = media_in - handle_start\n        a_frame_end_h = media_out + handle_end\n\n        # create trimmed otio time range\n        trimmed_media_range_h = range_from_frames(\n            a_frame_start_h, (a_frame_end_h - a_frame_start_h) + 1,\n            media_fps\n        )\n        trimmed_duration = trimmed_media_range_h.duration.value\n\n        self.log.debug(\"trimmed_media_range_h: {}\".format(\n            trimmed_media_range_h))\n        self.log.debug(\"a_frame_start_h: {}\".format(\n            a_frame_start_h))\n        self.log.debug(\"a_frame_end_h: {}\".format(\n            a_frame_end_h))\n\n        # create frame start and end\n        frame_start = instance.data[\"frameStart\"]\n        frame_end = frame_start + (media_out - media_in)\n\n        # Fit start /end frame to media in /out\n        if \"{originalBasename}\" in template:\n            frame_start = media_in\n            frame_end = media_out\n\n        # add to version data start and end range data\n        # for loader plugins to be correctly displayed and loaded\n        instance.data[\"versionData\"].update({\n            \"fps\": media_fps\n        })\n\n        if not instance.data[\"versionData\"].get(\"retime\"):\n            instance.data[\"versionData\"].update({\n                \"frameStart\": frame_start,\n                \"frameEnd\": frame_end,\n                \"handleStart\": handle_start,\n                \"handleEnd\": handle_end,\n            })\n        else:\n            instance.data[\"versionData\"].update({\n                \"frameStart\": frame_start,\n                \"frameEnd\": frame_end\n            })\n\n        # change frame_start and frame_end values\n        # for representation to be correctly renumbered in integrate_new\n        frame_start -= handle_start\n        frame_end += handle_end\n\n        media_ref = otio_clip.media_reference\n        metadata = media_ref.metadata\n\n        is_sequence = None\n        # check in two way if it is sequence\n        if hasattr(otio.schema, \"ImageSequenceReference\"):\n            # for OpenTimelineIO 0.13 and newer\n            if isinstance(\n                media_ref,\n                otio.schema.ImageSequenceReference\n            ):\n                is_sequence = True\n        elif metadata.get(\"padding\"):\n            is_sequence = True\n\n        self.log.info(\n            \"frame_start-frame_end: {}-{}\".format(frame_start, frame_end))\n\n        if is_sequence:\n            # file sequence way\n            if hasattr(media_ref, \"target_url_base\"):\n                self.staging_dir = media_ref.target_url_base\n                head = media_ref.name_prefix\n                tail = media_ref.name_suffix\n                collection = clique.Collection(\n                    head=head,\n                    tail=tail,\n                    padding=media_ref.frame_zero_padding\n                )\n                collection.indexes.update(\n                    list(range(a_frame_start_h, (a_frame_end_h + 1)))\n                )\n\n            else:\n                # in case it is file sequence but not new OTIO schema\n                # `ImageSequenceReference`\n                path = media_ref.target_url\n                collection_data = make_sequence_collection(\n                    path, trimmed_media_range_h, metadata)\n                self.staging_dir, collection = collection_data\n\n            if len(collection.indexes) &gt; 1:\n                self.log.debug(collection)\n                repre = self._create_representation(\n                    frame_start, frame_end, collection=collection)\n            else:\n                filename = tuple(collection)[0]\n                self.log.debug(filename)\n\n                # TODO: discuss this, it erases frame number.\n                repre = self._create_representation(\n                    frame_start, frame_end, file=filename)\n\n        else:\n            _trim = False\n            dirname, filename = os.path.split(media_ref.target_url)\n            self.staging_dir = dirname\n            if trimmed_duration &lt; available_duration:\n                self.log.debug(\"Ready for Trimming\")\n                instance.data[\"families\"].append(\"trim\")\n                instance.data[\"otioTrimmingRange\"] = trimmed_media_range_h\n                _trim = True\n\n            self.log.debug(filename)\n            repre = self._create_representation(\n                frame_start, frame_end, file=filename, trim=_trim)\n\n        instance.data[\"originalDirname\"] = self.staging_dir\n\n        # add representation to instance data\n        if repre:\n            colorspace = instance.data.get(\"colorspace\")\n            # add colorspace data to representation\n            self.set_representation_colorspace(\n                repre, instance.context, colorspace)\n\n            instance.data[\"representations\"].append(repre)\n\n        self.log.debug(instance.data)\n\n    def _create_representation(self, start, end, **kwargs):\n        \"\"\"\n        Creating representation data.\n\n        Args:\n            start (int): start frame\n            end (int): end frame\n            kwargs (dict): optional data\n\n        Returns:\n            dict: representation data\n        \"\"\"\n\n        # create default representation data\n        representation_data = {\n            \"frameStart\": start,\n            \"frameEnd\": end,\n            \"stagingDir\": self.staging_dir,\n            \"tags\": [],\n        }\n\n        if kwargs.get(\"collection\"):\n            collection = kwargs.get(\"collection\")\n            files = list(collection)\n            ext = collection.format(\"{tail}\")\n            representation_data.update({\n                \"name\": ext[1:],\n                \"ext\": ext[1:],\n                \"files\": files,\n                \"frameStart\": start,\n                \"frameEnd\": end,\n            })\n\n        if kwargs.get(\"file\"):\n            file = kwargs.get(\"file\")\n            ext = os.path.splitext(file)[-1]\n            representation_data.update({\n                \"name\": ext[1:],\n                \"ext\": ext[1:],\n                \"files\": file,\n                \"frameStart\": start,\n                \"frameEnd\": end,\n            })\n\n        for tag_name in (\"trim\", \"delete\", \"review\"):\n            if kwargs.get(tag_name) is True:\n                representation_data[\"tags\"].append(tag_name)\n\n        return representation_data\n\n    def get_template_name(self, instance):\n        \"\"\"Return anatomy template name to use for integration\"\"\"\n\n        # Anatomy data is pre-filled by Collectors\n        context = instance.context\n        project_name = context.data[\"projectName\"]\n\n        # Task can be optional in anatomy data\n        host_name = context.data[\"hostName\"]\n\n        product_base_type = instance.data.get(\"productBaseType\")\n        if not product_base_type:\n            product_base_type = instance.data[\"productType\"]\n        anatomy_data = instance.data[\"anatomyData\"]\n        task_info = anatomy_data.get(\"task\") or {}\n\n        return get_publish_template_name(\n            project_name,\n            host_name,\n            product_base_type=product_base_type,\n            task_name=task_info.get(\"name\"),\n            task_type=task_info.get(\"type\"),\n            project_settings=context.data[\"project_settings\"],\n            logger=self.log\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_otio_subset_resources.html#client.ayon_core.plugins.publish.collect_otio_subset_resources.CollectOtioSubsetResources.get_template_name","title":"<code>get_template_name(instance)</code>","text":"<p>Return anatomy template name to use for integration</p> Source code in <code>client/ayon_core/plugins/publish/collect_otio_subset_resources.py</code> <pre><code>def get_template_name(self, instance):\n    \"\"\"Return anatomy template name to use for integration\"\"\"\n\n    # Anatomy data is pre-filled by Collectors\n    context = instance.context\n    project_name = context.data[\"projectName\"]\n\n    # Task can be optional in anatomy data\n    host_name = context.data[\"hostName\"]\n\n    product_base_type = instance.data.get(\"productBaseType\")\n    if not product_base_type:\n        product_base_type = instance.data[\"productType\"]\n    anatomy_data = instance.data[\"anatomyData\"]\n    task_info = anatomy_data.get(\"task\") or {}\n\n    return get_publish_template_name(\n        project_name,\n        host_name,\n        product_base_type=product_base_type,\n        task_name=task_info.get(\"name\"),\n        task_type=task_info.get(\"type\"),\n        project_settings=context.data[\"project_settings\"],\n        logger=self.log\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_rendered_files.html","title":"collect_rendered_files","text":"<p>Loads publishing context from json and continues in publish process.</p> Requires <p>anatomy -&gt; context[\"anatomy\"] *(pyblish.api.CollectorOrder - 0.4)</p> Provides <p>context, instances -&gt; All data from previous publishing process.</p>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_rendered_files.html#client.ayon_core.plugins.publish.collect_rendered_files.CollectRenderedFiles","title":"<code>CollectRenderedFiles</code>","text":"<p>               Bases: <code>ContextPlugin</code></p> <p>This collector will try to find json files in provided <code>AYON_PUBLISH_DATA</code>. Those files MUST share same context.</p> Note <p>We should split this collector and move the part which handle reading     of file and it's context from session data before collect anatomy     and instance creation dependent on anatomy can be done here.</p> Source code in <code>client/ayon_core/plugins/publish/collect_rendered_files.py</code> <pre><code>class CollectRenderedFiles(pyblish.api.ContextPlugin):\n    \"\"\"\n    This collector will try to find json files in provided\n    `AYON_PUBLISH_DATA`. Those files _MUST_ share same context.\n\n    Note:\n        We should split this collector and move the part which handle reading\n            of file and it's context from session data before collect anatomy\n            and instance creation dependent on anatomy can be done here.\n    \"\"\"\n\n    order = pyblish.api.CollectorOrder - 0.2\n    # Keep \"filesequence\" for backwards compatibility of older jobs\n    targets = [\"filesequence\", \"farm\"]\n    label = \"Collect rendered frames\"\n    settings_category = \"core\"\n\n    remove_files = False\n\n    _context = None\n\n    def _load_json(self, path):\n        path = path.strip('\\\"')\n\n        if not os.path.isfile(path):\n            raise FileNotFoundError(\n                f\"Path to json file doesn't exist. \\\"{path}\\\"\")\n\n        data = None\n        with open(path, \"r\") as json_file:\n            try:\n                data = json.load(json_file)\n            except Exception as exc:\n                self.log.error(\n                    \"Error loading json: %s - Exception: %s\", path, exc)\n        return data\n\n    def _fill_staging_dir(self, data_object, anatomy):\n        staging_dir = data_object.get(\"stagingDir\")\n        if staging_dir:\n            data_object[\"stagingDir\"] = anatomy.fill_root(staging_dir)\n            self.log.debug(\"Filling stagingDir with root to: %s\",\n                           data_object[\"stagingDir\"])\n\n    def _process_path(self, data, anatomy):\n        \"\"\"Process data of a single JSON publish metadata file.\n\n        Args:\n            data: The loaded metadata from the JSON file\n            anatomy: Anatomy for the current context\n\n        Returns:\n            bool: Whether any instance of this particular metadata file\n                has a persistent staging dir.\n\n        \"\"\"\n        # validate basic necessary data\n        data_err = \"invalid json file - missing data\"\n        required = [\"user\", \"comment\",\n                    \"job\", \"instances\", \"version\"]\n\n        if any(elem not in data for elem in required):\n            raise ValueError(data_err)\n\n        if \"folderPath\" not in data and \"asset\" not in data:\n            raise ValueError(data_err)\n\n        if \"folderPath\" not in data:\n            data[\"folderPath\"] = data.pop(\"asset\")\n\n        # ftrack credentials are passed as environment variables by Deadline\n        # to publish job, but Muster doesn't pass them.\n        if data.get(\"ftrack\") and not os.environ.get(\"FTRACK_API_USER\"):\n            ftrack = data.get(\"ftrack\")\n            os.environ[\"FTRACK_API_USER\"] = ftrack[\"FTRACK_API_USER\"]\n            os.environ[\"FTRACK_API_KEY\"] = ftrack[\"FTRACK_API_KEY\"]\n            os.environ[\"FTRACK_SERVER\"] = ftrack[\"FTRACK_SERVER\"]\n\n        # now we can just add instances from json file and we are done\n        any_staging_dir_persistent = False\n        for instance_data in data[\"instances\"]:\n            self.log.debug(\"  - processing instance for {}\".format(\n                instance_data.get(\"productName\")))\n            instance = self._context.create_instance(\n                instance_data.get(\"productName\")\n            )\n\n            self._fill_staging_dir(instance_data, anatomy)\n            instance.data.update(instance_data)\n            instance.data[\"inputVersions\"] = deserialize_input_versions(\n                instance_data.get(\"inputVersions\")\n            )\n\n            # stash render job id for later validation\n            instance.data[\"publishJobMetadata\"] = data\n            # TODO remove 'render_job_id' here and rather use\n            #   'publishJobMetadata' where is needed.\n            #   - this is deadline specific\n            instance.data[\"render_job_id\"] = data.get(\"job\", {}).get(\"_id\")\n            staging_dir_persistent = instance.data.get(\n                \"stagingDir_persistent\", False\n            )\n            if staging_dir_persistent:\n                any_staging_dir_persistent = True\n\n            representations = []\n            for repre_data in instance_data.get(\"representations\") or []:\n                self._fill_staging_dir(repre_data, anatomy)\n                representations.append(repre_data)\n\n                if self.remove_files and not staging_dir_persistent:\n                    add_repre_files_for_cleanup(instance, repre_data)\n\n            instance.data[\"representations\"] = representations\n\n            # add audio if in metadata data\n            if data.get(\"audio\"):\n                instance.data.update({\n                    \"audio\": [{\n                        \"filename\": data.get(\"audio\"),\n                        \"offset\": 0\n                    }]\n                })\n                self.log.debug(\n                    f\"Adding audio to instance: {instance.data['audio']}\")\n\n        return any_staging_dir_persistent\n\n    def process(self, context):\n        self._context = context\n\n        publish_data_paths = os.environ.get(\"AYON_PUBLISH_DATA\")\n        if not publish_data_paths:\n            self.log.warning(\n                \"Environment variable `AYON_PUBLISH_DATA` is not set.\")\n            return\n\n        # QUESTION\n        #   Do we support (or want support) multiple files in the variable?\n        #   - what if they have different context?\n        paths = publish_data_paths.split(os.pathsep)\n\n        # Using already collected Anatomy\n        anatomy = context.data[\"anatomy\"]\n        self.log.debug(\"Getting root setting for project \\\"{}\\\"\".format(\n            anatomy.project_name\n        ))\n\n        self.log.debug(\"Anatomy roots: {}\".format(anatomy.roots))\n        try:\n            session_is_set = False\n            for path in paths:\n                path = anatomy.fill_root(path)\n                data = self._load_json(path)\n                assert data, \"failed to load json file\"\n                session_data = data.get(\"session\")\n                if not session_is_set and session_data:\n                    session_is_set = True\n                    self.log.debug(\"Setting session using data from file\")\n                    os.environ.update(session_data)\n\n                staging_dir_persistent = self._process_path(data, anatomy)\n                if self.remove_files and not staging_dir_persistent:\n                    context.data[\"cleanupFullPaths\"].append(path)\n                    context.data[\"cleanupEmptyDirs\"].append(\n                        os.path.dirname(path)\n                    )\n\n            # Remap workdir if it's set\n            workdir = os.getenv(\"AYON_WORKDIR\")\n            remapped_workdir = None\n            if workdir:\n                remapped_workdir = anatomy.roots_obj.path_remapper(\n                    os.getenv(\"AYON_WORKDIR\")\n                )\n            if remapped_workdir:\n                os.environ[\"AYON_WORKDIR\"] = remapped_workdir\n        except Exception as e:\n            self.log.error(e, exc_info=True)\n            raise Exception(\"Error\") from e\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_resources_path.html","title":"collect_resources_path","text":"Requires <p>context     -&gt; anatomy context     -&gt; anatomyData</p> Provides <p>instance    -&gt; publishDir instance    -&gt; resourcesDir</p>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_resources_path.html#client.ayon_core.plugins.publish.collect_resources_path.CollectResourcesPath","title":"<code>CollectResourcesPath</code>","text":"<p>               Bases: <code>InstancePlugin</code></p> <p>Generate directory path where the files and resources will be stored.</p> <p>Collects folder name and file name from files, if exists, for in-situ publishing.</p> Source code in <code>client/ayon_core/plugins/publish/collect_resources_path.py</code> <pre><code>class CollectResourcesPath(pyblish.api.InstancePlugin):\n    \"\"\"Generate directory path where the files and resources will be stored.\n\n    Collects folder name and file name from files, if exists, for in-situ\n    publishing.\n    \"\"\"\n\n    label = \"Collect Resources Path\"\n    order = pyblish.api.CollectorOrder + 0.495\n    families = [\"workfile\",\n                \"pointcache\",\n                \"proxyAbc\",\n                \"camera\",\n                \"animation\",\n                \"model\",\n                \"mayaAscii\",\n                \"mayaScene\",\n                \"setdress\",\n                \"layout\",\n                \"ass\",\n                \"vdbcache\",\n                \"scene\",\n                \"vrayproxy\",\n                \"render\",\n                \"prerender\",\n                \"imagesequence\",\n                \"rendersetup\",\n                \"rig\",\n                \"plate\",\n                \"look\",\n                \"mvLook\",\n                \"yetiRig\",\n                \"yeticache\",\n                \"nukenodes\",\n                \"gizmo\",\n                \"source\",\n                \"matchmove\",\n                \"image\",\n                \"source\",\n                \"assembly\",\n                \"fbx\",\n                \"gltf\",\n                \"textures\",\n                \"action\",\n                \"background\",\n                \"effect\",\n                \"staticMesh\",\n                \"skeletalMesh\",\n                \"xgen\",\n                \"yeticacheUE\",\n                \"tycache\",\n                \"usd\",\n                \"oxrig\",\n                \"sbsar\",\n                \"zfab\",\n                ]\n\n    def process(self, instance):\n        anatomy = instance.context.data[\"anatomy\"]\n\n        template_data = copy.deepcopy(instance.data[\"anatomyData\"])\n\n        # This is for cases of Deprecated anatomy without `folder`\n        # TODO remove when all clients have solved this issue\n        template_data.update({\"frame\": \"FRAME_TEMP\", \"representation\": \"TEMP\"})\n\n        task_name = task_type = None\n        task_entity = instance.data.get(\"taskEntity\")\n        if task_entity:\n            task_name = task_entity[\"name\"]\n            task_type = task_entity[\"taskType\"]\n\n        product_base_type = instance.data.get(\"productBaseType\")\n        if not product_base_type:\n            product_base_type = instance.data[\"productType\"]\n\n        template_name = get_publish_template_name(\n            project_name=instance.context.data[\"projectName\"],\n            host_name=instance.context.data[\"hostName\"],\n            product_base_type=product_base_type,\n            task_name=task_name,\n            task_type=task_type,\n            project_settings=instance.context.data[\"project_settings\"],\n            logger=self.log,\n        )\n\n        publish_template = anatomy.get_template_item(\n            \"publish\", template_name, \"directory\")\n\n        if \"{originalDirname}\" in publish_template:\n            original_directory = instance.data.get(\"originalDirname\")\n            if not original_directory:\n                original_directory = instance.data.get(\"stagingDir\")\n\n            if not original_directory:\n                raise PublishError(\n                    \"Publish template requires 'originalDirname'\"\n                    \" but 'originalDirname' is not set on instance\"\n                    \" and 'stagingDir' is not yet filled.\"\n                )\n\n            template_data[\"originalDirname\"] = original_directory\n\n        publish_folder = os.path.normpath(\n            publish_template.format_strict(template_data)\n        )\n        resources_folder = os.path.join(publish_folder, \"resources\")\n\n        instance.data[\"publishDir\"] = publish_folder\n        instance.data[\"resourcesDir\"] = resources_folder\n\n        self.log.debug(\"publishDir: \\\"{}\\\"\".format(publish_folder))\n        self.log.debug(\"resourcesDir: \\\"{}\\\"\".format(resources_folder))\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_scene_loaded_versions.html","title":"collect_scene_loaded_versions","text":""},{"location":"autoapi/client/ayon_core/plugins/publish/collect_scene_loaded_versions.html#client.ayon_core.plugins.publish.collect_scene_loaded_versions.CollectSceneLoadedVersions","title":"<code>CollectSceneLoadedVersions</code>","text":"<p>               Bases: <code>ContextPlugin</code></p> Source code in <code>client/ayon_core/plugins/publish/collect_scene_loaded_versions.py</code> <pre><code>class CollectSceneLoadedVersions(pyblish.api.ContextPlugin):\n\n    order = pyblish.api.CollectorOrder + 0.0001\n    label = \"Collect Versions Loaded in Scene\"\n\n    def process(self, context):\n        host = registered_host()\n        if host is None:\n            self.log.warning(\"No registered host.\")\n            return\n\n        if not isinstance(host, ILoadHost):\n            host_name = host.name\n            self.log.warning(\n                f\"Host {host_name} does not implement ILoadHost. \"\n                \"Skipping querying of loaded versions in scene.\"\n            )\n            return\n\n        containers = list(host.get_containers())\n        if not containers:\n            # Opt out early if there are no containers\n            self.log.debug(\"No loaded containers found in scene.\")\n            return\n\n        containers = self._filter_invalid_containers(containers)\n\n        repre_ids = {\n            container[\"representation\"]\n            for container in containers\n        }\n\n        # Ignore representation ids that are not valid\n        repre_ids = {\n            representation_id for representation_id in repre_ids\n            if ayon_api.utils.convert_entity_id(representation_id)\n        }\n\n        project_name = context.data[\"projectName\"]\n        repre_entities = ayon_api.get_representations(\n            project_name,\n            representation_ids=repre_ids,\n            fields={\"id\", \"versionId\"}\n        )\n        repre_entities_by_id = {\n            repre_entity[\"id\"]: repre_entity\n            for repre_entity in repre_entities\n        }\n\n        # QUESTION should we add same representation id when loaded multiple\n        #   times?\n        loaded_versions: list[str] = []\n        for con in containers:\n            repre_id = con[\"representation\"]\n            repre_entity = repre_entities_by_id.get(repre_id)\n            if repre_entity is None:\n                self.log.warning((\n                    \"Skipping container,\"\n                    \" did not find representation document. {}\"\n                ).format(str(con)))\n                continue\n\n            # NOTE:\n            # may have more than one representation that are same version\n            version_id: str = repre_entity[\"versionId\"]\n            loaded_versions.append(version_id)\n        self.log.debug(f\"Collected {len(loaded_versions)} loaded versions.\")\n\n        input_versions = version_ids_to_input_versions(\n            project_name=project_name,\n            version_ids=loaded_versions,\n            log=self.log\n        )\n        context.data[\"loadedVersions\"] = input_versions\n\n    def _filter_invalid_containers(\n        self,\n        containers: list[dict[str, Any]]\n    ) -&gt; list[dict[str, Any]]:\n        \"\"\"Filter out invalid containers lacking required keys.\n\n        Skip any invalid containers that lack 'representation' or 'name'\n        keys to avoid KeyError.\n        \"\"\"\n        # Only filter by what's required for this plug-in instead of validating\n        # a full container schema.\n        required_keys = {\"name\", \"representation\"}\n        valid = []\n        for container in containers:\n            missing = [key for key in required_keys if key not in container]\n            if missing:\n                self.log.warning(\n                    \"Skipping invalid container, missing required keys:\"\n                    \" {}. {}\".format(\", \".join(missing), container)\n                )\n                continue\n            valid.append(container)\n\n        return valid\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_scene_version.html","title":"collect_scene_version","text":""},{"location":"autoapi/client/ayon_core/plugins/publish/collect_scene_version.html#client.ayon_core.plugins.publish.collect_scene_version.CollectSceneVersion","title":"<code>CollectSceneVersion</code>","text":"<p>               Bases: <code>ContextPlugin</code></p> <p>Finds version in the filename or passes the one found in the context Arguments: version (int, optional): version number of the publish</p> Source code in <code>client/ayon_core/plugins/publish/collect_scene_version.py</code> <pre><code>class CollectSceneVersion(pyblish.api.ContextPlugin):\n    \"\"\"Finds version in the filename or passes the one found in the context\n        Arguments:\n        version (int, optional): version number of the publish\n    \"\"\"\n\n    order = pyblish.api.CollectorOrder\n    label = \"Collect Scene Version\"\n    # configurable in Settings\n    hosts = [\"*\"]\n    settings_category = \"core\"\n\n    # in some cases of headless publishing (for example webpublisher using PS)\n    # you want to ignore version from name and let integrate use next version\n    skip_hosts_headless_publish = []\n\n    def process(self, context):\n        # tests should be close to regular publish as possible\n        if (\n            os.environ.get(\"HEADLESS_PUBLISH\")\n            and not is_in_tests()\n            and context.data[\"hostName\"] in self.skip_hosts_headless_publish\n        ):\n            self.log.debug(\"Skipping for headless publishing\")\n            return\n\n        if not context.data.get('currentFile'):\n            self.log.error(\"Cannot get current workfile path. \"\n                           \"Make sure your scene is saved.\")\n            return\n\n        filename = os.path.basename(context.data.get('currentFile'))\n\n        if '&lt;shell&gt;' in filename:\n            return\n\n        self.log.debug(\n            \"Collecting scene version from filename: {}\".format(filename)\n        )\n\n        version = get_version_from_path(filename)\n        if version is None:\n            raise PublishError(\n                f\"Unable to retrieve version number from filename: {filename}\"\n            )\n\n        context.data['version'] = int(version)\n        self.log.debug(\n            \"Collected scene version: {}\".format(context.data.get('version'))\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_settings.html","title":"collect_settings","text":""},{"location":"autoapi/client/ayon_core/plugins/publish/collect_settings.html#client.ayon_core.plugins.publish.collect_settings.CollectSettings","title":"<code>CollectSettings</code>","text":"<p>               Bases: <code>ContextPlugin</code></p> <p>Collect Settings and store in the context.</p> Source code in <code>client/ayon_core/plugins/publish/collect_settings.py</code> <pre><code>class CollectSettings(api.ContextPlugin):\n    \"\"\"Collect Settings and store in the context.\"\"\"\n\n    order = api.CollectorOrder - 0.491\n    label = \"Collect Settings\"\n\n    def process(self, context):\n        project_name = context.data[\"projectName\"]\n        self.log.debug(\n            \"Collecting settings for project: {}\".format(project_name)\n        )\n        project_settings = get_project_settings(project_name)\n        context.data[\"project_settings\"] = project_settings\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_shell_workspace.html","title":"collect_shell_workspace","text":""},{"location":"autoapi/client/ayon_core/plugins/publish/collect_shell_workspace.html#client.ayon_core.plugins.publish.collect_shell_workspace.CollectShellWorkspace","title":"<code>CollectShellWorkspace</code>","text":"<p>               Bases: <code>ContextPlugin</code></p> <p>Inject the current workspace into context</p> Source code in <code>client/ayon_core/plugins/publish/collect_shell_workspace.py</code> <pre><code>class CollectShellWorkspace(pyblish.api.ContextPlugin):\n    \"\"\"Inject the current workspace into context\"\"\"\n\n    order = pyblish.api.CollectorOrder - 0.5\n    label = \"Shell Workspace\"\n\n    hosts = [\"shell\"]\n\n    def process(self, context):\n        context.data[\"workspaceDir\"] = os.getcwd()\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_source_for_source.html","title":"collect_source_for_source","text":"Requires <p>instance     -&gt; currentFile instance     -&gt; source</p> Provides <p>instance    -&gt; originalBasename instance    -&gt; originalDirname</p>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_source_for_source.html#client.ayon_core.plugins.publish.collect_source_for_source.CollectSourceForSource","title":"<code>CollectSourceForSource</code>","text":"<p>               Bases: <code>InstancePlugin</code></p> <p>Collects source location of file for instance.</p> <p>Used for 'source' template name which handles in place publishing. For this kind of publishing files are present with correct file name pattern and correct location.</p> Source code in <code>client/ayon_core/plugins/publish/collect_source_for_source.py</code> <pre><code>class CollectSourceForSource(pyblish.api.InstancePlugin):\n    \"\"\"Collects source location of file for instance.\n\n    Used for 'source' template name which handles in place publishing.\n    For this kind of publishing files are present with correct file name\n    pattern and correct location.\n    \"\"\"\n\n    label = \"Collect Source\"\n    order = pyblish.api.CollectorOrder + 0.495\n\n    def process(self, instance):\n        # parse folder name and file name for online and source templates\n        # currentFile comes from hosts workfiles\n        # source comes from Publisher\n        current_file = instance.data.get(\"currentFile\")\n        source = instance.data.get(\"source\")\n        source_file = current_file or source\n        if source_file:\n            self.log.debug(\"Parsing paths for {}\".format(source_file))\n            if not instance.data.get(\"originalBasename\"):\n                instance.data[\"originalBasename\"] = \\\n                    os.path.basename(source_file)\n\n            if not instance.data.get(\"originalDirname\"):\n                instance.data[\"originalDirname\"] = \\\n                    os.path.dirname(source_file)\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_status.html","title":"collect_status","text":""},{"location":"autoapi/client/ayon_core/plugins/publish/collect_status.html#client.ayon_core.plugins.publish.collect_status.CollectStatus","title":"<code>CollectStatus</code>","text":"<p>               Bases: <code>InstancePlugin</code>, <code>AYONPyblishPluginMixin</code></p> <p>Allow to define version status for published version. It is possible to change setttings so user can set status in publisher UI.</p> Source code in <code>client/ayon_core/plugins/publish/collect_status.py</code> <pre><code>class CollectStatus(pyblish.api.InstancePlugin, AYONPyblishPluginMixin):\n    \"\"\"Allow to define version status for published version.\n    It is possible to change setttings so user can set status in publisher UI.\n    \"\"\"\n\n    order = pyblish.api.CollectorOrder + 0.499\n    label = \"Collect Version Status\"\n\n    enabled = False\n    status_profiles: list[dict] = []\n\n    def process(self, instance):\n        if not self.status_profiles:\n            return\n\n        if instance.data.get(\"status\"):\n            # already set so we won't override it\n            return\n        attr_values = self.get_attr_values_from_data(instance.data)\n        status_state = attr_values.get(\"status_state\")\n        if not status_state or status_state == \"dont_use\":\n            return\n\n        if status_state == \"use_status\":\n            status = attr_values.get(\"status\", \"\")\n        elif status_state.startswith(\"status|\"):\n            status = status_state.removeprefix(\"status|\")\n        else:\n            return\n        if status:\n            instance.data[\"status\"] = status\n\n    @classmethod\n    def get_attr_defs_for_instance(\n        cls, create_context: \"CreateContext\", instance: \"CreatedInstance\"\n    ):\n        status_state_attr = TextDef(\n            \"status_state\", visible=False, default=\"dont_use\"\n        )\n        output = [status_state_attr]\n        if not cls.status_profiles:\n            cls._set_instance_state(instance, status_state_attr, \"dont_use\")\n            return output\n\n        project_entity = create_context.get_current_project_entity()\n        statuses = [\n            status[\"name\"]\n            for status in project_entity[\"statuses\"]\n            if \"version\" in status[\"scope\"]\n        ]\n        if not statuses:\n            cls.log.warning(\"No version statuses found in current project.\")\n            cls._set_instance_state(instance, status_state_attr, \"dont_use\")\n            return output\n\n        folder_path = instance.get(\"folderPath\")\n        folder_entity = create_context.get_folder_entity(folder_path)\n        task_entity = None\n        task_name = None\n        task_type = None\n        if folder_entity:\n            task_name = instance.get(\"task\")\n            task_entity = create_context.get_task_entity(\n                folder_path, task_name\n            )\n            if task_entity:\n                task_type = task_entity[\"taskType\"]\n\n        filter_data = {\n            \"host_names\": create_context.host_name,\n            \"task_types\": task_type,\n            \"task_names\": task_name,\n            \"product_base_types\": instance.product_base_type,\n        }\n\n        status_profile = filter_profiles(\n            cls.status_profiles,\n            filter_data,\n            logger=cls.log\n        )\n        default_status = None\n        artist_can_change = True\n        if status_profile:\n            artist_can_change = status_profile[\"artist_can_change\"]\n            default_status = status_profile[\"default_status\"]\n\n        if default_status not in statuses:\n            if status_profile:\n                cls.log.warning(\n                    f\"Default status '{default_status}' is not available\"\n                    f\" on project: {project_entity['name']}\"\n                    f\"Using '{statuses[0]}' instead.\"\n                )\n            default_status = statuses[0]\n\n        if not artist_can_change:\n            cls._set_instance_state(\n                instance, status_state_attr, f\"status|{default_status}\"\n            )\n            cls.log.debug(\n                \"Artist cannot change status based on profile settings.\"\n            )\n            return output\n\n        cls._set_instance_state(instance, status_state_attr, \"use_status\")\n\n        output.append(EnumDef(\n           \"status\",\n            label=\"Version status\",\n            items=statuses,\n            default=default_status,\n        ))\n        return output\n\n    @classmethod\n    def _set_instance_state(\n        cls,\n        instance: \"CreatedInstance\",\n        status_state_attr: \"TextDef\",\n        state: str\n    ) -&gt; None:\n        status_state_attr.default = state\n        plugin_attributes = instance.publish_attributes.get(cls.__name__)\n        if plugin_attributes is None:\n            return\n\n        plugin_attributes[\"status_state\"] = state\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_time.html","title":"collect_time","text":""},{"location":"autoapi/client/ayon_core/plugins/publish/collect_time.html#client.ayon_core.plugins.publish.collect_time.CollectTime","title":"<code>CollectTime</code>","text":"<p>               Bases: <code>ContextPlugin</code></p> <p>Store global time at the time of publish</p> Source code in <code>client/ayon_core/plugins/publish/collect_time.py</code> <pre><code>class CollectTime(pyblish.api.ContextPlugin):\n    \"\"\"Store global time at the time of publish\"\"\"\n\n    label = \"Collect Current Time\"\n    order = pyblish.api.CollectorOrder - 0.499\n\n    def process(self, context):\n        context.data[\"time\"] = get_formatted_current_time()\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_version_tags.html","title":"collect_version_tags","text":""},{"location":"autoapi/client/ayon_core/plugins/publish/collect_version_tags.html#client.ayon_core.plugins.publish.collect_version_tags.CollectVersionTags","title":"<code>CollectVersionTags</code>","text":"<p>               Bases: <code>InstancePlugin</code>, <code>AYONPyblishPluginMixin</code></p> <p>Collect Version Tags</p> <p>Provides a selectable list of tags for the user. Selected tags are stored  in the instance metadata and applied to the Version Entity during  integration.</p> Source code in <code>client/ayon_core/plugins/publish/collect_version_tags.py</code> <pre><code>class CollectVersionTags(\n    pyblish.api.InstancePlugin,\n    AYONPyblishPluginMixin\n):\n    \"\"\"Collect Version Tags\n\n    Provides a selectable list of tags for the user. Selected tags are stored\n     in the instance metadata and applied to the Version Entity during\n     integration.\n    \"\"\"\n\n    order = pyblish.api.CollectorOrder + 0.499\n    label = \"Collect Version Tags\"\n    settings_category = \"core\"\n\n    enabled = False\n\n    def process(self, instance):\n\n        attr_values = self.get_attr_values_from_data(instance.data)\n        version_tags = attr_values.get(\"version_tags\", [])\n\n        if not version_tags:\n            return\n\n        self.log.debug(f\"Adding tags: {version_tags}\")\n        tags = instance.data.get(\"versionTags\")\n        if tags is None:\n            tags = set()\n        elif not isinstance(tags, set):\n            tags = set(tags)\n\n        tags.update(version_tags)\n        instance.data[\"versionTags\"] = tags\n        self.log.debug(f\"Collected version tags: {tags}\")\n\n    @classmethod\n    def get_attr_defs_for_instance(cls, create_context, instance):\n        project_entity = create_context.get_current_project_entity()\n        items = [tag[\"name\"] for tag in project_entity[\"tags\"]]\n        if not items:\n            plugin_attributes = instance.publish_attributes.get(cls.__name__)\n            if plugin_attributes is not None:\n                plugin_attributes.pop(\"version_tags\", None)\n            return []\n\n        return [\n            EnumDef(\n                \"version_tags\",\n                label=\"Version Tags\",\n                multiselection=True,\n                items=items,\n                tooltip=\"Set these tags to versions\",\n            )\n        ]\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/collect_version_to_list.html","title":"collect_version_to_list","text":""},{"location":"autoapi/client/ayon_core/plugins/publish/collect_version_to_list.html#client.ayon_core.plugins.publish.collect_version_to_list.CollectVersionToList","title":"<code>CollectVersionToList</code>","text":"<p>               Bases: <code>InstancePlugin</code></p> <p>Collect Version to List.</p> Source code in <code>client/ayon_core/plugins/publish/collect_version_to_list.py</code> <pre><code>class CollectVersionToList(pyblish.api.InstancePlugin):\n    \"\"\"Collect Version to List.\"\"\"\n\n    order = pyblish.api.CollectorOrder + 0.499\n    label = \"Collect Version to List\"\n    settings_category = \"core\"\n\n    profiles = []\n\n    def process(self, instance):\n        if \"versionLists\" in instance.data:\n            version_lists = instance.data[\"versionLists\"]\n            self.log.debug(f\"Version lists already collected: {version_lists}\")\n            return\n\n        version_lists: list[ListConfig] = []\n        instance.data[\"versionLists\"] = version_lists\n\n        profile = self._get_profile_for_instance(instance)\n        if not profile:\n            self.log.debug(f\"No profile found for instance {instance}\")\n            return\n\n        list_folders = []\n        for item in profile[\"list_folders\"]:\n            scope = []\n            if item[\"scope_def\"] == \"list_type\":\n                scope.append(profile[\"list_type\"])\n\n            list_folders.append(\n                ListConfigFolder(\n                    label=item[\"label\"],\n                    scope=scope,\n                )\n            )\n\n        version_lists.append(\n            ListConfig(\n                label=profile[\"list_name\"],\n                list_type=profile[\"list_type\"],\n                list_folders=list_folders,\n            )\n        )\n        self.log.debug(f\"Collected version lists: {version_lists}\")\n\n    def _get_profile_for_instance(\n        self,\n        instance: pyblish.api.Instance\n    ) -&gt; dict | None:\n        \"\"\"Returns profile for the given instance.\"\"\"\n        host_name = instance.context.data[\"hostName\"]\n        product_base_type = instance.data.get(\"productBaseType\")\n        if not product_base_type:\n            product_base_type = instance.data[\"productType\"]\n        product_name = instance.data[\"productName\"]\n        task_data = instance.data[\"anatomyData\"].get(\"task\", {})\n        task_name = task_data.get(\"name\")\n        task_type = task_data.get(\"type\")\n        filtering_criteria = {\n            \"host_names\": host_name,\n            \"product_base_types\": product_base_type,\n            \"product_names\": product_name,\n            \"task_names\": task_name,\n            \"task_types\": task_type,\n        }\n        profile = filter_profiles(\n            self.profiles,\n            filtering_criteria,\n            logger=self.log\n        )\n\n        if not profile:\n            self.log.debug(\n                \"Skipped instance. None of profiles in presets are for\"\n                f' Host name: \"{host_name}\"'\n                f' | Product base type: \"{product_base_type}\"'\n                f' | Product name: \"{product_name}\"'\n                f' | Task name \"{task_name}\"'\n                f' | Task type \"{task_type}\"'\n            )\n            return None\n\n        return profile\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_burnin.html","title":"extract_burnin","text":""},{"location":"autoapi/client/ayon_core/plugins/publish/extract_burnin.html#client.ayon_core.plugins.publish.extract_burnin.ExtractBurnin","title":"<code>ExtractBurnin</code>","text":"<p>               Bases: <code>Extractor</code></p> <p>Extractor to create video with pre-defined burnins from existing extracted video representation.</p> <p>It will work only on representations having <code>burnin = True</code> or <code>tags</code> including <code>burnin</code></p> Source code in <code>client/ayon_core/plugins/publish/extract_burnin.py</code> <pre><code>class ExtractBurnin(publish.Extractor):\n    \"\"\"\n    Extractor to create video with pre-defined burnins from\n    existing extracted video representation.\n\n    It will work only on representations having `burnin = True` or\n    `tags` including `burnin`\n    \"\"\"\n\n    label = \"Extract burnins\"\n    order = pyblish.api.ExtractorOrder + 0.03\n\n    families = [\"review\", \"burnin\"]\n    settings_category = \"core\"\n\n    optional = True\n\n    positions = [\n        \"top_left\", \"top_centered\", \"top_right\",\n        \"bottom_right\", \"bottom_centered\", \"bottom_left\"\n    ]\n    # Default options for burnins for cases that are not set in presets.\n    default_options = {\n        \"font_size\": 42,\n        \"font_color\": [255, 255, 255, 1.0],\n        \"bg_color\": [0, 0, 0, 0.5],\n        \"bg_padding\": 5,\n        \"x_offset\": 5,\n        \"y_offset\": 5\n    }\n\n    # Configurable by Settings\n    profiles = None\n    options = None\n\n    def process(self, instance):\n        if not self.profiles:\n            self.log.warning(\"No profiles present for create burnin\")\n            return\n\n        if not instance.data.get(\"representations\"):\n            self.log.debug(\n                \"Instance does not have filled representations. Skipping\")\n            return\n\n        self.main_process(instance)\n\n        # Remove only representation tagged with both\n        # tags `delete` and `burnin`\n        for repre in tuple(instance.data[\"representations\"]):\n            if all(x in repre.get(\"tags\", []) for x in ['delete', 'burnin']):\n                self.log.debug(\"Removing representation: {}\".format(repre))\n                instance.data[\"representations\"].remove(repre)\n\n    def _get_burnins_per_representations(self, instance, src_burnin_defs):\n        \"\"\"\n\n        Args:\n            instance (pyblish.api.Instance): Pyblish instance.\n            src_burnin_defs (list): Burnin definitions.\n\n        Returns:\n            list[tuple[dict, list]]: List of tuples containing representation\n                and its burnin definitions.\n\n        \"\"\"\n        self.log.debug(\n            \"Filtering of representations and their burnins starts\"\n        )\n\n        filtered_repres = []\n        repres = instance.data.get(\"representations\") or []\n        for idx, repre in enumerate(repres):\n            self.log.debug(\"repre ({}): `{}`\".format(idx + 1, repre[\"name\"]))\n            if not self.repres_is_valid(repre):\n                continue\n\n            repre_burnin_links = repre.get(\"burnins\", [])\n            self.log.debug(\n                \"repre_burnin_links: {}\".format(repre_burnin_links)\n            )\n\n            burnin_defs = copy.deepcopy(src_burnin_defs)\n\n            # Filter output definition by `burnin` representation key\n            repre_linked_burnins = [\n                burnin_def\n                for burnin_def in burnin_defs\n                if burnin_def[\"name\"] in repre_burnin_links\n            ]\n            self.log.debug(\n                \"repre_linked_burnins: {}\".format(repre_linked_burnins)\n            )\n\n            # if any match then replace burnin defs and follow tag filtering\n            if repre_linked_burnins:\n                burnin_defs = repre_linked_burnins\n\n            # Filter output definition by representation tags (optional)\n            repre_burnin_defs = self.filter_burnins_by_tags(\n                burnin_defs, repre[\"tags\"]\n            )\n            if not repre_burnin_defs:\n                self.log.debug(\n                    \"Skipped representation. All burnin definitions from\"\n                    \" selected profile do not match to representation's\"\n                    \" tags. \\\"{}\\\"\".format(repre[\"tags\"])\n                )\n                continue\n            filtered_repres.append((repre, repre_burnin_defs))\n\n        return filtered_repres\n\n    def main_process(self, instance):\n        host_name = instance.context.data[\"hostName\"]\n        product_base_type = instance.data.get(\"productBaseType\")\n        if not product_base_type:\n            product_base_type = instance.data[\"productType\"]\n        product_name = instance.data[\"productName\"]\n        task_data = instance.data[\"anatomyData\"].get(\"task\", {})\n        task_name = task_data.get(\"name\")\n        task_type = task_data.get(\"type\")\n\n        filtering_criteria = {\n            \"host_names\": host_name,\n            \"product_base_types\": product_base_type,\n            \"product_names\": product_name,\n            \"task_names\": task_name,\n            \"task_types\": task_type,\n        }\n        profile = filter_profiles(\n            self.profiles,\n            filtering_criteria,\n            logger=self.log\n        )\n        if not profile:\n            self.log.debug(\n                \"Skipped instance. None of profiles in presets are for\"\n                f\" Host name: \\\"{host_name}\\\"\"\n                f\" | Product base type: \\\"{product_base_type}\\\"\"\n                f\" | Product name \\\"{product_name}\\\"\"\n                f\" | Task name \\\"{task_name}\\\"\"\n                f\" | Task type \\\"{task_type}\\\"\"\n            )\n            return\n\n        # Pre-filter burnin definitions by instance families\n        burnin_defs = self.filter_burnins_defs(profile, instance)\n        if not burnin_defs:\n            self.log.debug(\n                \"Skipped instance. Burnin definitions are not set for profile\"\n                f\" Host name: \\\"{host_name}\\\"\"\n                f\" | Product base type: \\\"{product_base_type}\\\"\"\n                f\" | Task name \\\"{task_name}\\\"\"\n                f\" | Profile \\\"{profile}\\\"\"\n            )\n            return\n\n        burnins_per_repres = self._get_burnins_per_representations(\n            instance, burnin_defs\n        )\n        if not burnins_per_repres:\n            self.log.debug(\n                \"Skipped instance. No representations found matching a burnin\"\n                \" definition in: %s\", burnin_defs\n            )\n            return\n\n        burnin_options = self._get_burnin_options()\n\n        # Prepare basic data for processing\n        _burnin_data, _temp_data = self.prepare_basic_data(instance)\n\n        anatomy = instance.context.data[\"anatomy\"]\n        scriptpath = self.burnin_script_path()\n        project_settings = instance.context.data[\"project_settings\"]\n        review_layers = get_default_reviewable_layers(project_settings)\n\n        # Args that will execute the script\n        executable_args = [\"run\", scriptpath]\n        for repre, repre_burnin_defs in burnins_per_repres:\n            # Create copy of `_burnin_data` and `_temp_data` for repre.\n            burnin_data = copy.deepcopy(_burnin_data)\n            temp_data = copy.deepcopy(_temp_data)\n\n            # Prepare representation based data.\n            self.prepare_repre_data(instance, repre, burnin_data, temp_data)\n\n            src_repre_staging_dir = repre[\"stagingDir\"]\n            # Should convert representation source files before processing?\n            repre_files = repre[\"files\"]\n            if isinstance(repre_files, (tuple, list)):\n                filename = repre_files[0]\n                src_filepaths = [\n                    os.path.join(src_repre_staging_dir, filename)\n                    for filename in repre_files\n                ]\n            else:\n                filename = repre_files\n                src_filepaths = [os.path.join(src_repre_staging_dir, filename)]\n\n            first_input_path = os.path.join(src_repre_staging_dir, filename)\n            # Determine if representation requires pre conversion for ffmpeg\n            do_convert = should_convert_for_ffmpeg(\n                first_input_path, review_layers=review_layers\n            )\n            # If result is None the requirement of conversion can't be\n            #   determined\n            if do_convert is None:\n                self.log.debug(\n                    \"Can't determine if representation requires conversion.\"\n                    \" Skipped.\"\n                )\n                continue\n\n            # Do conversion if needed\n            #   - change staging dir of source representation\n            #   - must be set back after output definitions processing\n            if do_convert:\n                new_staging_dir = get_temp_dir(\n                    project_name=instance.context.data[\"projectName\"],\n                    use_local_temp=True,\n                )\n                repre[\"stagingDir\"] = new_staging_dir\n\n                convert_input_paths_for_ffmpeg(\n                    src_filepaths,\n                    new_staging_dir,\n                    review_layers=review_layers,\n                    log=self.log,\n                )\n\n            # Add anatomy keys to burnin_data.\n            filled_anatomy = anatomy.format_all(burnin_data)\n            burnin_data[\"anatomy\"] = filled_anatomy.get_solved()\n\n            custom_data = copy.deepcopy(\n                instance.data.get(\"customData\") or {}\n            )\n            # Backwards compatibility (since 2022/04/07)\n            custom_data.update(\n                instance.data.get(\"custom_burnin_data\") or {}\n            )\n\n            # Add context data burnin_data.\n            burnin_data[\"custom\"] = custom_data\n\n            # Add data members.\n            burnin_data.update(instance.data.get(\"burninDataMembers\", {}))\n\n            # Add source camera name to burnin data\n            camera_name = repre.get(\"camera_name\")\n            if camera_name:\n                burnin_data[\"camera_name\"] = camera_name\n\n            first_output = True\n\n            files_to_delete = []\n\n            repre_burnin_options = copy.deepcopy(burnin_options)\n            # Use fps from representation for output in options\n            fps = repre.get(\"fps\")\n            if fps is not None:\n                repre_burnin_options[\"fps\"] = fps\n                # TODO Should we use fps from source representation to fill\n                #  it in review?\n                # burnin_data[\"fps\"] = fps\n\n            for burnin_def in repre_burnin_defs:\n                filename_suffix = burnin_def[\"name\"]\n                new_repre = copy.deepcopy(repre)\n                new_repre[\"stagingDir\"] = src_repre_staging_dir\n\n                # Keep \"ftrackreview\" tag only on first output\n                if first_output:\n                    first_output = False\n                elif \"ftrackreview\" in new_repre[\"tags\"]:\n                    new_repre[\"tags\"].remove(\"ftrackreview\")\n\n                burnin_values = {}\n                for key in self.positions:\n                    value = burnin_def.get(key)\n                    if value:\n                        burnin_values[key] = value\n\n                # Remove \"delete\" tag from new representation\n                if \"delete\" in new_repre[\"tags\"]:\n                    new_repre[\"tags\"].remove(\"delete\")\n\n                if len(repre_burnin_defs) &gt; 1:\n                    # Update name and outputName to be\n                    # able have multiple outputs in case of more burnin presets\n                    # Join previous \"outputName\" with filename suffix\n                    new_name = \"_\".join(\n                        [new_repre[\"outputName\"], filename_suffix]\n                    )\n                    new_repre[\"name\"] = new_name\n                    new_repre[\"outputName\"] = new_name\n\n                # Prepare paths and files for process.\n                self.input_output_paths(\n                    repre, new_repre, temp_data, filename_suffix\n                )\n\n                # Data for burnin script\n                script_data = {\n                    \"input\": temp_data[\"full_input_path\"],\n                    \"output\": temp_data[\"full_output_path\"],\n                    \"burnin_data\": burnin_data,\n                    \"options\": repre_burnin_options,\n                    \"values\": burnin_values,\n                    \"full_input_path\": temp_data[\"full_input_paths\"][0],\n                    \"first_frame\": temp_data[\"first_frame\"],\n                    \"ffmpeg_cmd\": new_repre.get(\"ffmpeg_cmd\", \"\")\n                }\n\n                self.log.debug(\n                    \"script_data: {}\".format(json.dumps(script_data, indent=4))\n                )\n\n                # Dump data to string\n                dumped_script_data = json.dumps(script_data)\n\n                # Store dumped json to temporary file\n                temporary_json_file = tempfile.NamedTemporaryFile(\n                    mode=\"w\", suffix=\".json\", delete=False\n                )\n                temporary_json_file.write(dumped_script_data)\n                temporary_json_file.close()\n                temporary_json_filepath = temporary_json_file.name.replace(\n                    \"\\\\\", \"/\"\n                )\n\n                # Prepare subprocess arguments\n                args = list(executable_args)\n                args.append(temporary_json_filepath)\n                args.append(\"--headless\")\n                self.log.debug(\"Executing: {}\".format(\" \".join(args)))\n\n                # Run burnin script\n                process_kwargs = {\n                    \"logger\": self.log\n                }\n\n                run_ayon_launcher_process(*args, **process_kwargs)\n                # Remove the temporary json\n                os.remove(temporary_json_filepath)\n\n                for filepath in temp_data[\"full_input_paths\"]:\n                    filepath = filepath.replace(\"\\\\\", \"/\")\n                    if filepath not in files_to_delete:\n                        files_to_delete.append(filepath)\n\n                # Add new representation to instance\n                instance.data[\"representations\"].append(new_repre)\n\n                add_repre_files_for_cleanup(instance, new_repre)\n\n            # Cleanup temp staging dir after processing of output definitions\n            if do_convert:\n                temp_dir = repre[\"stagingDir\"]\n                shutil.rmtree(temp_dir)\n                # Set staging dir of source representation back to previous\n                #   value\n                repre[\"stagingDir\"] = src_repre_staging_dir\n\n            # Remove source representation\n            # NOTE we maybe can keep source representation if necessary\n            instance.data[\"representations\"].remove(repre)\n\n            self.log.debug(\"Files to delete: {}\".format(files_to_delete))\n\n            # Delete input files\n            for filepath in files_to_delete:\n                if os.path.exists(filepath):\n                    os.remove(filepath)\n                    self.log.debug(\"Removed: \\\"{}\\\"\".format(filepath))\n\n    def _get_burnin_options(self):\n        \"\"\"Get the burnin options from `ExtractBurnin` settings.\n\n        Returns:\n            dict[str, Any]: Burnin options.\n\n        \"\"\"\n        # Prepare burnin options\n        burnin_options = copy.deepcopy(self.default_options)\n        if self.options:\n            for key, value in self.options.items():\n                if value is not None:\n                    burnin_options[key] = copy.deepcopy(value)\n\n        # Convert colors defined as list of numbers RGBA (0-255)\n        # BG Color\n        bg_color = burnin_options.get(\"bg_color\")\n        if bg_color and isinstance(bg_color, list):\n            bg_red, bg_green, bg_blue, bg_alpha = bg_color\n            bg_color_hex = \"#{0:0&gt;2X}{1:0&gt;2X}{2:0&gt;2X}\".format(\n                bg_red, bg_green, bg_blue\n            )\n            burnin_options[\"bg_opacity\"] = bg_alpha\n            burnin_options[\"bg_color\"] = bg_color_hex\n\n        # FG Color\n        font_color = burnin_options.get(\"font_color\")\n        if font_color and isinstance(font_color, list):\n            fg_red, fg_green, fg_blue, fg_alpha = font_color\n            fg_color_hex = \"#{0:0&gt;2X}{1:0&gt;2X}{2:0&gt;2X}\".format(\n                fg_red, fg_green, fg_blue\n            )\n            burnin_options[\"opacity\"] = fg_alpha\n            burnin_options[\"font_color\"] = fg_color_hex\n\n        # Define font filepath\n        # - font filepath may be defined in settings\n        font_filepath = burnin_options.get(\"font_filepath\")\n        if font_filepath and isinstance(font_filepath, dict):\n            sys_name = platform.system().lower()\n            font_filepath = font_filepath.get(sys_name)\n\n        if font_filepath and isinstance(font_filepath, str):\n            font_filepath = font_filepath.format(**os.environ)\n            if not os.path.exists(font_filepath):\n                font_filepath = None\n\n        # Use default AYON font\n        if not font_filepath:\n            font_filepath = resources.get_liberation_font_path()\n\n        burnin_options[\"font\"] = font_filepath\n\n        return burnin_options\n\n    def prepare_basic_data(self, instance):\n        \"\"\"Pick data from instance for processing and for burnin strings.\n\n        Args:\n            instance (Instance): Currently processed instance.\n\n        Returns:\n            tuple: `(burnin_data, temp_data)` - `burnin_data` contain data for\n                filling burnin strings. `temp_data` are for repre pre-process\n                preparation.\n        \"\"\"\n        self.log.debug(\"Preparing basic data for burnins\")\n        context = instance.context\n\n        version = instance.data.get(\"version\")\n        if version is None:\n            version = context.data.get(\"version\")\n\n        frame_start = instance.data.get(\"frameStart\")\n        if frame_start is None:\n            self.log.warning(\n                \"Key \\\"frameStart\\\" is not set. Setting to \\\"0\\\".\"\n            )\n            frame_start = 0\n        frame_start = int(frame_start)\n\n        frame_end = instance.data.get(\"frameEnd\")\n        if frame_end is None:\n            self.log.warning(\n                \"Key \\\"frameEnd\\\" is not set. Setting to \\\"1\\\".\"\n            )\n            frame_end = 1\n        frame_end = int(frame_end)\n\n        handle_start = instance.data.get(\"handleStart\")\n        if handle_start is None:\n            handle_start = context.data.get(\"handleStart\") or 0\n\n        handle_end = instance.data.get(\"handleEnd\")\n        if handle_end is None:\n            handle_end = context.data.get(\"handleEnd\") or 0\n\n        frame_start_handle = frame_start - handle_start\n        frame_end_handle = frame_end + handle_end\n\n        burnin_data = copy.deepcopy(instance.data[\"anatomyData\"])\n\n        if \"slate.farm\" in instance.data[\"families\"]:\n            frame_start_handle += 1\n\n        burnin_data[\"version\"] = int(version)\n        comment = instance.data[\"comment\"]\n        if comment:\n            burnin_data[\"comment\"] = comment\n\n        intent_label = context.data.get(\"intent\") or \"\"\n        if intent_label and isinstance(intent_label, dict):\n            value = intent_label.get(\"value\")\n            if value:\n                intent_label = intent_label[\"label\"]\n            else:\n                intent_label = \"\"\n\n        burnin_data[\"intent\"] = intent_label\n\n        temp_data = {\n            \"frame_start\": frame_start,\n            \"frame_end\": frame_end,\n            \"frame_start_handle\": frame_start_handle,\n            \"frame_end_handle\": frame_end_handle\n        }\n\n        self.log.debug(\n            \"Basic burnin_data: {}\".format(json.dumps(burnin_data, indent=4))\n        )\n\n        return burnin_data, temp_data\n\n    def repres_is_valid(self, repre):\n        \"\"\"Validation if representation should be processed.\n\n        Args:\n            repre (dict): Representation which should be checked.\n\n        Returns:\n            bool: False if can't be processed else True.\n        \"\"\"\n\n        if \"burnin\" not in (repre.get(\"tags\") or []):\n            self.log.debug((\n                \"Representation \\\"{}\\\" does not have \\\"burnin\\\" tag. Skipped.\"\n            ).format(repre[\"name\"]))\n            return False\n\n        if not repre.get(\"files\"):\n            self.log.warning((\n                \"Representation \\\"{}\\\" have empty files. Skipped.\"\n            ).format(repre[\"name\"]))\n            return False\n        return True\n\n    def filter_burnins_by_tags(self, burnin_defs, tags):\n        \"\"\"Filter burnin definitions by entered representation tags.\n\n        Burnin definitions without tags filter are marked as valid.\n\n        Args:\n            burnin_defs (list): Burnin definitions.\n            tags (list): Tags of processed representation.\n\n        Returns:\n            list: Contain all burnin definitions matching entered tags.\n\n        \"\"\"\n        filtered_burnins = []\n        repre_tags_low = set(tag.lower() for tag in tags)\n        for burnin_def in burnin_defs:\n            valid = True\n            tag_filters = burnin_def[\"filter\"][\"tags\"]\n            if tag_filters:\n                # Check tag filters\n                tag_filters_low = set(tag.lower() for tag in tag_filters)\n\n                valid = bool(repre_tags_low &amp; tag_filters_low)\n\n            if valid:\n                filtered_burnins.append(burnin_def)\n        return filtered_burnins\n\n    def input_output_paths(\n        self, src_repre, new_repre, temp_data, filename_suffix\n    ):\n        \"\"\"Prepare input and output paths for representation.\n\n        Store data to `temp_data` for keys \"full_input_path\" which is full path\n        to source files optionally with sequence formatting,\n        \"full_output_path\" full path to output with optionally with sequence\n        formatting, \"full_input_paths\" list of all source files which will be\n        deleted when burnin script ends, \"repre_files\" list of output\n        filenames.\n\n        Args:\n            new_repre (dict): Currently processed new representation.\n            temp_data (dict): Temp data of representation process.\n            filename_suffix (str): Filename suffix added to inputl filename.\n\n        Returns:\n            None: This is processing method.\n        \"\"\"\n        # TODO we should find better way to know if input is sequence\n        input_filenames = new_repre[\"files\"]\n        is_sequence = False\n        if isinstance(input_filenames, (tuple, list)):\n            if len(input_filenames) &gt; 1:\n                is_sequence = True\n\n        # Sequence must have defined first frame\n        # - not used if input is not a sequence\n        first_frame = None\n        if is_sequence:\n            collections, _ = clique.assemble(input_filenames)\n            if not collections:\n                is_sequence = False\n            else:\n                input_filename = new_repre[\"sequence_file\"]\n                collection = collections[0]\n                indexes = list(collection.indexes)\n                padding = len(str(max(indexes)))\n                head = collection.format(\"{head}\")\n                tail = collection.format(\"{tail}\")\n                output_filename = \"{}%{:0&gt;2}d{}{}\".format(\n                    head, padding, filename_suffix, tail\n                )\n                repre_files = []\n                for idx in indexes:\n                    repre_files.append(output_filename % idx)\n\n                first_frame = min(indexes)\n\n        if not is_sequence:\n            input_filename = input_filenames\n            if isinstance(input_filename, (tuple, list)):\n                input_filename = input_filename[0]\n\n            filepart_start, ext = os.path.splitext(input_filename)\n            dir_path, basename = os.path.split(filepart_start)\n            output_filename = basename + filename_suffix + ext\n            if dir_path:\n                output_filename = os.path.join(dir_path, output_filename)\n\n            repre_files = output_filename\n\n        src_stagingdir = src_repre[\"stagingDir\"]\n        dst_stagingdir = new_repre[\"stagingDir\"]\n        full_input_path = os.path.join(\n            os.path.normpath(src_stagingdir), input_filename\n        ).replace(\"\\\\\", \"/\")\n        full_output_path = os.path.join(\n            os.path.normpath(dst_stagingdir), output_filename\n        ).replace(\"\\\\\", \"/\")\n\n        temp_data[\"full_input_path\"] = full_input_path\n        temp_data[\"full_output_path\"] = full_output_path\n        temp_data[\"first_frame\"] = first_frame\n\n        new_repre[\"files\"] = repre_files\n\n        self.log.debug(\"full_input_path: {}\".format(full_input_path))\n        self.log.debug(\"full_output_path: {}\".format(full_output_path))\n\n        # Prepare full paths to input files and filenames for reprensetation\n        full_input_paths = []\n        if is_sequence:\n            for filename in input_filenames:\n                filepath = os.path.join(\n                    os.path.normpath(src_stagingdir), filename\n                ).replace(\"\\\\\", \"/\")\n                full_input_paths.append(filepath)\n\n        else:\n            full_input_paths.append(full_input_path)\n\n        temp_data[\"full_input_paths\"] = full_input_paths\n\n    def prepare_repre_data(self, instance, repre, burnin_data, temp_data):\n        \"\"\"Prepare data for representation.\n\n        Args:\n            instance (pyblish.api.Instance): Currently processed Instance.\n            repre (dict): Currently processed representation.\n            burnin_data (dict): Copy of basic burnin data based on instance\n                data.\n            temp_data (dict): Copy of basic temp data\n        \"\"\"\n        # Add representation name to burnin data\n        burnin_data[\"representation\"] = repre[\"name\"]\n\n        # no handles switch from profile tags\n        if \"no-handles\" in repre[\"tags\"]:\n            burnin_frame_start = temp_data[\"frame_start\"]\n            burnin_frame_end = temp_data[\"frame_end\"]\n\n        else:\n            burnin_frame_start = temp_data[\"frame_start_handle\"]\n            burnin_frame_end = temp_data[\"frame_end_handle\"]\n\n        burnin_duration = burnin_frame_end - burnin_frame_start + 1\n\n        burnin_data.update({\n            \"frame_start\": burnin_frame_start,\n            \"frame_end\": burnin_frame_end,\n            \"duration\": burnin_duration,\n        })\n        temp_data[\"duration\"] = burnin_duration\n\n        # Add values for slate frames\n        burnin_slate_frame_start = burnin_frame_start\n\n        # Move frame start by 1 frame when slate is used.\n        if (\n            \"slate\" in instance.data[\"families\"]\n            and \"slate-frame\" in repre[\"tags\"]\n        ):\n            burnin_slate_frame_start -= 1\n\n        self.log.debug(\"burnin_slate_frame_start: {}\".format(\n            burnin_slate_frame_start\n        ))\n\n        burnin_data.update({\n            \"slate_frame_start\": burnin_slate_frame_start,\n            \"slate_frame_end\": burnin_frame_end,\n            \"slate_duration\": (\n                burnin_frame_end - burnin_slate_frame_start + 1\n            )\n        })\n\n        # burnin source resolution which might be different than on review\n        repre_source_resolution_width = repre.get(\"source_resolution_width\")\n        repre_source_resolution_height = repre.get(\"source_resolution_height\")\n        if repre_source_resolution_width and repre_source_resolution_height:\n            burnin_data.update({\n                \"source_resolution_width\": repre_source_resolution_width,\n                \"source_resolution_height\": repre_source_resolution_height\n            })\n\n    def filter_burnins_defs(self, profile, instance):\n        \"\"\"Filter outputs by their values from settings.\n\n        Output definitions with at least one value are marked as valid.\n\n        Args:\n            profile (dict): Profile from presets matching current context.\n            instance (pyblish.api.Instance): Publish instance.\n\n        Returns:\n            list[dict[str, Any]]: Contain all valid output definitions.\n\n        \"\"\"\n        filtered_burnin_defs = []\n\n        burnin_defs = profile.get(\"burnins\")\n        if not burnin_defs:\n            return filtered_burnin_defs\n\n        families = self.families_from_instance(instance)\n\n        for orig_burnin_def in burnin_defs:\n            burnin_def = copy.deepcopy(orig_burnin_def)\n            filename_suffix = burnin_def[\"name\"]\n\n            def_filter = burnin_def[\"filter\"]\n            families_filters = def_filter[\"families\"]\n            if not self.families_filter_validation(\n                families, families_filters\n            ):\n                self.log.debug(\n                    f\"Skipped burnin definition \\\"{filename_suffix}\\\".\"\n                    f\" Family filters ({families_filters}) does not match\"\n                    f\" current instance families: {families}\"\n                )\n                continue\n\n            # Burnin values\n            new_burnin_def = {}\n            burnin_values = {}\n            for key, value in tuple(burnin_def.items()):\n                key_low = key.lower()\n                if key_low not in self.positions:\n                    new_burnin_def[key] = value\n                elif value:\n                    burnin_values[key_low] = value\n\n            # Skip processing if burnin values are not set\n            if not burnin_values:\n                self.log.warning((\n                    \"Burnin values for Burnin definition \\\"{}\\\"\"\n                    \" are not filled. Definition will be skipped.\"\n                    \" Origin value: {}\"\n                ).format(filename_suffix, str(orig_burnin_def)))\n                continue\n\n            new_burnin_def.update(burnin_values)\n\n            filtered_burnin_defs.append(new_burnin_def)\n\n            self.log.debug((\n                \"Burnin definition \\\"{}\\\" passed first filtering.\"\n            ).format(filename_suffix))\n\n        return filtered_burnin_defs\n\n    def families_filter_validation(self, families, output_families_filter):\n        \"\"\"Determines if entered families intersect with families filters.\n\n        All family values are lowered to avoid unexpected results.\n        \"\"\"\n\n        families_filter_lower = set(family.lower() for family in\n                                    output_families_filter\n                                    # Exclude empty filter values\n                                    if family)\n        if not families_filter_lower:\n            return True\n        return any(family.lower() in families_filter_lower\n                   for family in families)\n\n    def families_from_instance(self, instance):\n        \"\"\"Return all families of entered instance.\"\"\"\n        families = []\n        family = instance.data.get(\"family\")\n        if family:\n            families.append(family)\n\n        for family in (instance.data.get(\"families\") or tuple()):\n            if family not in families:\n                families.append(family)\n        return families\n\n    def burnin_script_path(self):\n        \"\"\"Return path to python script for burnin processing.\"\"\"\n        scriptpath = os.path.normpath(\n            os.path.join(\n                AYON_CORE_ROOT,\n                \"scripts\",\n                \"otio_burnin.py\"\n            )\n        )\n\n        self.log.debug(\"scriptpath: {}\".format(scriptpath))\n\n        return scriptpath\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_burnin.html#client.ayon_core.plugins.publish.extract_burnin.ExtractBurnin.burnin_script_path","title":"<code>burnin_script_path()</code>","text":"<p>Return path to python script for burnin processing.</p> Source code in <code>client/ayon_core/plugins/publish/extract_burnin.py</code> <pre><code>def burnin_script_path(self):\n    \"\"\"Return path to python script for burnin processing.\"\"\"\n    scriptpath = os.path.normpath(\n        os.path.join(\n            AYON_CORE_ROOT,\n            \"scripts\",\n            \"otio_burnin.py\"\n        )\n    )\n\n    self.log.debug(\"scriptpath: {}\".format(scriptpath))\n\n    return scriptpath\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_burnin.html#client.ayon_core.plugins.publish.extract_burnin.ExtractBurnin.families_filter_validation","title":"<code>families_filter_validation(families, output_families_filter)</code>","text":"<p>Determines if entered families intersect with families filters.</p> <p>All family values are lowered to avoid unexpected results.</p> Source code in <code>client/ayon_core/plugins/publish/extract_burnin.py</code> <pre><code>def families_filter_validation(self, families, output_families_filter):\n    \"\"\"Determines if entered families intersect with families filters.\n\n    All family values are lowered to avoid unexpected results.\n    \"\"\"\n\n    families_filter_lower = set(family.lower() for family in\n                                output_families_filter\n                                # Exclude empty filter values\n                                if family)\n    if not families_filter_lower:\n        return True\n    return any(family.lower() in families_filter_lower\n               for family in families)\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_burnin.html#client.ayon_core.plugins.publish.extract_burnin.ExtractBurnin.families_from_instance","title":"<code>families_from_instance(instance)</code>","text":"<p>Return all families of entered instance.</p> Source code in <code>client/ayon_core/plugins/publish/extract_burnin.py</code> <pre><code>def families_from_instance(self, instance):\n    \"\"\"Return all families of entered instance.\"\"\"\n    families = []\n    family = instance.data.get(\"family\")\n    if family:\n        families.append(family)\n\n    for family in (instance.data.get(\"families\") or tuple()):\n        if family not in families:\n            families.append(family)\n    return families\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_burnin.html#client.ayon_core.plugins.publish.extract_burnin.ExtractBurnin.filter_burnins_by_tags","title":"<code>filter_burnins_by_tags(burnin_defs, tags)</code>","text":"<p>Filter burnin definitions by entered representation tags.</p> <p>Burnin definitions without tags filter are marked as valid.</p> <p>Parameters:</p> Name Type Description Default <code>burnin_defs</code> <code>list</code> <p>Burnin definitions.</p> required <code>tags</code> <code>list</code> <p>Tags of processed representation.</p> required <p>Returns:</p> Name Type Description <code>list</code> <p>Contain all burnin definitions matching entered tags.</p> Source code in <code>client/ayon_core/plugins/publish/extract_burnin.py</code> <pre><code>def filter_burnins_by_tags(self, burnin_defs, tags):\n    \"\"\"Filter burnin definitions by entered representation tags.\n\n    Burnin definitions without tags filter are marked as valid.\n\n    Args:\n        burnin_defs (list): Burnin definitions.\n        tags (list): Tags of processed representation.\n\n    Returns:\n        list: Contain all burnin definitions matching entered tags.\n\n    \"\"\"\n    filtered_burnins = []\n    repre_tags_low = set(tag.lower() for tag in tags)\n    for burnin_def in burnin_defs:\n        valid = True\n        tag_filters = burnin_def[\"filter\"][\"tags\"]\n        if tag_filters:\n            # Check tag filters\n            tag_filters_low = set(tag.lower() for tag in tag_filters)\n\n            valid = bool(repre_tags_low &amp; tag_filters_low)\n\n        if valid:\n            filtered_burnins.append(burnin_def)\n    return filtered_burnins\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_burnin.html#client.ayon_core.plugins.publish.extract_burnin.ExtractBurnin.filter_burnins_defs","title":"<code>filter_burnins_defs(profile, instance)</code>","text":"<p>Filter outputs by their values from settings.</p> <p>Output definitions with at least one value are marked as valid.</p> <p>Parameters:</p> Name Type Description Default <code>profile</code> <code>dict</code> <p>Profile from presets matching current context.</p> required <code>instance</code> <code>Instance</code> <p>Publish instance.</p> required <p>Returns:</p> Type Description <p>list[dict[str, Any]]: Contain all valid output definitions.</p> Source code in <code>client/ayon_core/plugins/publish/extract_burnin.py</code> <pre><code>def filter_burnins_defs(self, profile, instance):\n    \"\"\"Filter outputs by their values from settings.\n\n    Output definitions with at least one value are marked as valid.\n\n    Args:\n        profile (dict): Profile from presets matching current context.\n        instance (pyblish.api.Instance): Publish instance.\n\n    Returns:\n        list[dict[str, Any]]: Contain all valid output definitions.\n\n    \"\"\"\n    filtered_burnin_defs = []\n\n    burnin_defs = profile.get(\"burnins\")\n    if not burnin_defs:\n        return filtered_burnin_defs\n\n    families = self.families_from_instance(instance)\n\n    for orig_burnin_def in burnin_defs:\n        burnin_def = copy.deepcopy(orig_burnin_def)\n        filename_suffix = burnin_def[\"name\"]\n\n        def_filter = burnin_def[\"filter\"]\n        families_filters = def_filter[\"families\"]\n        if not self.families_filter_validation(\n            families, families_filters\n        ):\n            self.log.debug(\n                f\"Skipped burnin definition \\\"{filename_suffix}\\\".\"\n                f\" Family filters ({families_filters}) does not match\"\n                f\" current instance families: {families}\"\n            )\n            continue\n\n        # Burnin values\n        new_burnin_def = {}\n        burnin_values = {}\n        for key, value in tuple(burnin_def.items()):\n            key_low = key.lower()\n            if key_low not in self.positions:\n                new_burnin_def[key] = value\n            elif value:\n                burnin_values[key_low] = value\n\n        # Skip processing if burnin values are not set\n        if not burnin_values:\n            self.log.warning((\n                \"Burnin values for Burnin definition \\\"{}\\\"\"\n                \" are not filled. Definition will be skipped.\"\n                \" Origin value: {}\"\n            ).format(filename_suffix, str(orig_burnin_def)))\n            continue\n\n        new_burnin_def.update(burnin_values)\n\n        filtered_burnin_defs.append(new_burnin_def)\n\n        self.log.debug((\n            \"Burnin definition \\\"{}\\\" passed first filtering.\"\n        ).format(filename_suffix))\n\n    return filtered_burnin_defs\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_burnin.html#client.ayon_core.plugins.publish.extract_burnin.ExtractBurnin.input_output_paths","title":"<code>input_output_paths(src_repre, new_repre, temp_data, filename_suffix)</code>","text":"<p>Prepare input and output paths for representation.</p> <p>Store data to <code>temp_data</code> for keys \"full_input_path\" which is full path to source files optionally with sequence formatting, \"full_output_path\" full path to output with optionally with sequence formatting, \"full_input_paths\" list of all source files which will be deleted when burnin script ends, \"repre_files\" list of output filenames.</p> <p>Parameters:</p> Name Type Description Default <code>new_repre</code> <code>dict</code> <p>Currently processed new representation.</p> required <code>temp_data</code> <code>dict</code> <p>Temp data of representation process.</p> required <code>filename_suffix</code> <code>str</code> <p>Filename suffix added to inputl filename.</p> required <p>Returns:</p> Name Type Description <code>None</code> <p>This is processing method.</p> Source code in <code>client/ayon_core/plugins/publish/extract_burnin.py</code> <pre><code>def input_output_paths(\n    self, src_repre, new_repre, temp_data, filename_suffix\n):\n    \"\"\"Prepare input and output paths for representation.\n\n    Store data to `temp_data` for keys \"full_input_path\" which is full path\n    to source files optionally with sequence formatting,\n    \"full_output_path\" full path to output with optionally with sequence\n    formatting, \"full_input_paths\" list of all source files which will be\n    deleted when burnin script ends, \"repre_files\" list of output\n    filenames.\n\n    Args:\n        new_repre (dict): Currently processed new representation.\n        temp_data (dict): Temp data of representation process.\n        filename_suffix (str): Filename suffix added to inputl filename.\n\n    Returns:\n        None: This is processing method.\n    \"\"\"\n    # TODO we should find better way to know if input is sequence\n    input_filenames = new_repre[\"files\"]\n    is_sequence = False\n    if isinstance(input_filenames, (tuple, list)):\n        if len(input_filenames) &gt; 1:\n            is_sequence = True\n\n    # Sequence must have defined first frame\n    # - not used if input is not a sequence\n    first_frame = None\n    if is_sequence:\n        collections, _ = clique.assemble(input_filenames)\n        if not collections:\n            is_sequence = False\n        else:\n            input_filename = new_repre[\"sequence_file\"]\n            collection = collections[0]\n            indexes = list(collection.indexes)\n            padding = len(str(max(indexes)))\n            head = collection.format(\"{head}\")\n            tail = collection.format(\"{tail}\")\n            output_filename = \"{}%{:0&gt;2}d{}{}\".format(\n                head, padding, filename_suffix, tail\n            )\n            repre_files = []\n            for idx in indexes:\n                repre_files.append(output_filename % idx)\n\n            first_frame = min(indexes)\n\n    if not is_sequence:\n        input_filename = input_filenames\n        if isinstance(input_filename, (tuple, list)):\n            input_filename = input_filename[0]\n\n        filepart_start, ext = os.path.splitext(input_filename)\n        dir_path, basename = os.path.split(filepart_start)\n        output_filename = basename + filename_suffix + ext\n        if dir_path:\n            output_filename = os.path.join(dir_path, output_filename)\n\n        repre_files = output_filename\n\n    src_stagingdir = src_repre[\"stagingDir\"]\n    dst_stagingdir = new_repre[\"stagingDir\"]\n    full_input_path = os.path.join(\n        os.path.normpath(src_stagingdir), input_filename\n    ).replace(\"\\\\\", \"/\")\n    full_output_path = os.path.join(\n        os.path.normpath(dst_stagingdir), output_filename\n    ).replace(\"\\\\\", \"/\")\n\n    temp_data[\"full_input_path\"] = full_input_path\n    temp_data[\"full_output_path\"] = full_output_path\n    temp_data[\"first_frame\"] = first_frame\n\n    new_repre[\"files\"] = repre_files\n\n    self.log.debug(\"full_input_path: {}\".format(full_input_path))\n    self.log.debug(\"full_output_path: {}\".format(full_output_path))\n\n    # Prepare full paths to input files and filenames for reprensetation\n    full_input_paths = []\n    if is_sequence:\n        for filename in input_filenames:\n            filepath = os.path.join(\n                os.path.normpath(src_stagingdir), filename\n            ).replace(\"\\\\\", \"/\")\n            full_input_paths.append(filepath)\n\n    else:\n        full_input_paths.append(full_input_path)\n\n    temp_data[\"full_input_paths\"] = full_input_paths\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_burnin.html#client.ayon_core.plugins.publish.extract_burnin.ExtractBurnin.prepare_basic_data","title":"<code>prepare_basic_data(instance)</code>","text":"<p>Pick data from instance for processing and for burnin strings.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>Instance</code> <p>Currently processed instance.</p> required <p>Returns:</p> Name Type Description <code>tuple</code> <p><code>(burnin_data, temp_data)</code> - <code>burnin_data</code> contain data for filling burnin strings. <code>temp_data</code> are for repre pre-process preparation.</p> Source code in <code>client/ayon_core/plugins/publish/extract_burnin.py</code> <pre><code>def prepare_basic_data(self, instance):\n    \"\"\"Pick data from instance for processing and for burnin strings.\n\n    Args:\n        instance (Instance): Currently processed instance.\n\n    Returns:\n        tuple: `(burnin_data, temp_data)` - `burnin_data` contain data for\n            filling burnin strings. `temp_data` are for repre pre-process\n            preparation.\n    \"\"\"\n    self.log.debug(\"Preparing basic data for burnins\")\n    context = instance.context\n\n    version = instance.data.get(\"version\")\n    if version is None:\n        version = context.data.get(\"version\")\n\n    frame_start = instance.data.get(\"frameStart\")\n    if frame_start is None:\n        self.log.warning(\n            \"Key \\\"frameStart\\\" is not set. Setting to \\\"0\\\".\"\n        )\n        frame_start = 0\n    frame_start = int(frame_start)\n\n    frame_end = instance.data.get(\"frameEnd\")\n    if frame_end is None:\n        self.log.warning(\n            \"Key \\\"frameEnd\\\" is not set. Setting to \\\"1\\\".\"\n        )\n        frame_end = 1\n    frame_end = int(frame_end)\n\n    handle_start = instance.data.get(\"handleStart\")\n    if handle_start is None:\n        handle_start = context.data.get(\"handleStart\") or 0\n\n    handle_end = instance.data.get(\"handleEnd\")\n    if handle_end is None:\n        handle_end = context.data.get(\"handleEnd\") or 0\n\n    frame_start_handle = frame_start - handle_start\n    frame_end_handle = frame_end + handle_end\n\n    burnin_data = copy.deepcopy(instance.data[\"anatomyData\"])\n\n    if \"slate.farm\" in instance.data[\"families\"]:\n        frame_start_handle += 1\n\n    burnin_data[\"version\"] = int(version)\n    comment = instance.data[\"comment\"]\n    if comment:\n        burnin_data[\"comment\"] = comment\n\n    intent_label = context.data.get(\"intent\") or \"\"\n    if intent_label and isinstance(intent_label, dict):\n        value = intent_label.get(\"value\")\n        if value:\n            intent_label = intent_label[\"label\"]\n        else:\n            intent_label = \"\"\n\n    burnin_data[\"intent\"] = intent_label\n\n    temp_data = {\n        \"frame_start\": frame_start,\n        \"frame_end\": frame_end,\n        \"frame_start_handle\": frame_start_handle,\n        \"frame_end_handle\": frame_end_handle\n    }\n\n    self.log.debug(\n        \"Basic burnin_data: {}\".format(json.dumps(burnin_data, indent=4))\n    )\n\n    return burnin_data, temp_data\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_burnin.html#client.ayon_core.plugins.publish.extract_burnin.ExtractBurnin.prepare_repre_data","title":"<code>prepare_repre_data(instance, repre, burnin_data, temp_data)</code>","text":"<p>Prepare data for representation.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>Instance</code> <p>Currently processed Instance.</p> required <code>repre</code> <code>dict</code> <p>Currently processed representation.</p> required <code>burnin_data</code> <code>dict</code> <p>Copy of basic burnin data based on instance data.</p> required <code>temp_data</code> <code>dict</code> <p>Copy of basic temp data</p> required Source code in <code>client/ayon_core/plugins/publish/extract_burnin.py</code> <pre><code>def prepare_repre_data(self, instance, repre, burnin_data, temp_data):\n    \"\"\"Prepare data for representation.\n\n    Args:\n        instance (pyblish.api.Instance): Currently processed Instance.\n        repre (dict): Currently processed representation.\n        burnin_data (dict): Copy of basic burnin data based on instance\n            data.\n        temp_data (dict): Copy of basic temp data\n    \"\"\"\n    # Add representation name to burnin data\n    burnin_data[\"representation\"] = repre[\"name\"]\n\n    # no handles switch from profile tags\n    if \"no-handles\" in repre[\"tags\"]:\n        burnin_frame_start = temp_data[\"frame_start\"]\n        burnin_frame_end = temp_data[\"frame_end\"]\n\n    else:\n        burnin_frame_start = temp_data[\"frame_start_handle\"]\n        burnin_frame_end = temp_data[\"frame_end_handle\"]\n\n    burnin_duration = burnin_frame_end - burnin_frame_start + 1\n\n    burnin_data.update({\n        \"frame_start\": burnin_frame_start,\n        \"frame_end\": burnin_frame_end,\n        \"duration\": burnin_duration,\n    })\n    temp_data[\"duration\"] = burnin_duration\n\n    # Add values for slate frames\n    burnin_slate_frame_start = burnin_frame_start\n\n    # Move frame start by 1 frame when slate is used.\n    if (\n        \"slate\" in instance.data[\"families\"]\n        and \"slate-frame\" in repre[\"tags\"]\n    ):\n        burnin_slate_frame_start -= 1\n\n    self.log.debug(\"burnin_slate_frame_start: {}\".format(\n        burnin_slate_frame_start\n    ))\n\n    burnin_data.update({\n        \"slate_frame_start\": burnin_slate_frame_start,\n        \"slate_frame_end\": burnin_frame_end,\n        \"slate_duration\": (\n            burnin_frame_end - burnin_slate_frame_start + 1\n        )\n    })\n\n    # burnin source resolution which might be different than on review\n    repre_source_resolution_width = repre.get(\"source_resolution_width\")\n    repre_source_resolution_height = repre.get(\"source_resolution_height\")\n    if repre_source_resolution_width and repre_source_resolution_height:\n        burnin_data.update({\n            \"source_resolution_width\": repre_source_resolution_width,\n            \"source_resolution_height\": repre_source_resolution_height\n        })\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_burnin.html#client.ayon_core.plugins.publish.extract_burnin.ExtractBurnin.repres_is_valid","title":"<code>repres_is_valid(repre)</code>","text":"<p>Validation if representation should be processed.</p> <p>Parameters:</p> Name Type Description Default <code>repre</code> <code>dict</code> <p>Representation which should be checked.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <p>False if can't be processed else True.</p> Source code in <code>client/ayon_core/plugins/publish/extract_burnin.py</code> <pre><code>def repres_is_valid(self, repre):\n    \"\"\"Validation if representation should be processed.\n\n    Args:\n        repre (dict): Representation which should be checked.\n\n    Returns:\n        bool: False if can't be processed else True.\n    \"\"\"\n\n    if \"burnin\" not in (repre.get(\"tags\") or []):\n        self.log.debug((\n            \"Representation \\\"{}\\\" does not have \\\"burnin\\\" tag. Skipped.\"\n        ).format(repre[\"name\"]))\n        return False\n\n    if not repre.get(\"files\"):\n        self.log.warning((\n            \"Representation \\\"{}\\\" have empty files. Skipped.\"\n        ).format(repre[\"name\"]))\n        return False\n    return True\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_color_transcode.html","title":"extract_color_transcode","text":""},{"location":"autoapi/client/ayon_core/plugins/publish/extract_color_transcode.html#client.ayon_core.plugins.publish.extract_color_transcode.ExtractOIIOTranscode","title":"<code>ExtractOIIOTranscode</code>","text":"<p>               Bases: <code>Extractor</code></p> <p>Extractor to convert colors from one colorspace to different.</p> <p>Expects \"colorspaceData\" on representation. This dictionary is collected previously and denotes that representation files should be converted. This dict contains source colorspace information, collected by hosts.</p> <p>Target colorspace is selected by profiles in the Settings, based on: - host names - product base types - product names - task types - task names</p> <p>Can produce one or more representations (with different extensions) based on output definition in format:     \"output_name: {         \"extension\": \"png\",         \"colorspace\": \"ACES - ACEScg\",         \"display\": \"\",         \"view\": \"\",         \"tags\": [],         \"custom_tags\": []     }</p> <p>If 'extension' is empty original representation extension is used. 'output_name' will be used as name of new representation. In case of value     'passthrough' name of original representation will be used.</p> <p>'colorspace' denotes target colorspace to be transcoded into. Could be empty if transcoding should be only into display and viewer colorspace. (In that case both 'display' and 'view' must be filled.)</p> Source code in <code>client/ayon_core/plugins/publish/extract_color_transcode.py</code> <pre><code>class ExtractOIIOTranscode(publish.Extractor):\n    \"\"\"\n    Extractor to convert colors from one colorspace to different.\n\n    Expects \"colorspaceData\" on representation. This dictionary is collected\n    previously and denotes that representation files should be converted.\n    This dict contains source colorspace information, collected by hosts.\n\n    Target colorspace is selected by profiles in the Settings, based on:\n    - host names\n    - product base types\n    - product names\n    - task types\n    - task names\n\n    Can produce one or more representations (with different extensions) based\n    on output definition in format:\n        \"output_name: {\n            \"extension\": \"png\",\n            \"colorspace\": \"ACES - ACEScg\",\n            \"display\": \"\",\n            \"view\": \"\",\n            \"tags\": [],\n            \"custom_tags\": []\n        }\n\n    If 'extension' is empty original representation extension is used.\n    'output_name' will be used as name of new representation. In case of value\n        'passthrough' name of original representation will be used.\n\n    'colorspace' denotes target colorspace to be transcoded into. Could be\n    empty if transcoding should be only into display and viewer colorspace.\n    (In that case both 'display' and 'view' must be filled.)\n    \"\"\"\n\n    label = \"Transcode color spaces\"\n    order = pyblish.api.ExtractorOrder + 0.019\n\n    settings_category = \"core\"\n\n    optional = True\n\n    # Supported extensions\n    supported_exts = {\"exr\", \"jpg\", \"jpeg\", \"png\", \"dpx\"}\n\n    # Configurable by Settings\n    profiles = None\n    options = None\n\n    def process(self, instance):\n        if instance.data.get(\"farm\"):\n            self.log.debug(\"Should be processed on farm, skipping.\")\n            return\n\n        if not self.profiles:\n            self.log.debug(\"No profiles present for color transcode\")\n            return\n\n        if \"representations\" not in instance.data:\n            self.log.debug(\"No representations, skipping.\")\n            return\n\n        if not is_oiio_supported():\n            self.log.warning(\"OIIO not supported, no transcoding possible.\")\n            return\n\n        profile = self._get_profile(instance)\n        if not profile:\n            return\n\n        profile_output_defs = profile[\"outputs\"]\n        new_representations = []\n        repres = instance.data[\"representations\"]\n\n        scene_display = instance.data.get(\n            \"sceneDisplay\",\n            # Backward compatibility\n            instance.data.get(\"colorspaceDisplay\")\n        )\n        scene_view = instance.data.get(\n            \"sceneView\",\n            # Backward compatibility\n            instance.data.get(\"colorspaceView\")\n        )\n        project_settings = instance.context.data[\"project_settings\"]\n        review_layers = get_default_reviewable_layers(project_settings)\n        for idx, repre in enumerate(list(repres)):\n            self.log.debug(\"repre ({}): `{}`\".format(idx + 1, repre[\"name\"]))\n            if not self._repre_is_valid(repre, profile):\n                continue\n\n            added_representations = False\n            added_review = False\n\n            colorspace_data = repre[\"colorspaceData\"]\n\n            config_path = get_representation_ocio_config_path(\n                repre,\n                anatomy=instance.context.data[\"anatomy\"],\n                logger=self.log\n            )\n            if not config_path:\n                self.log.debug(\n                    \"Skipping OIIO Color Transcode because no OCIO config\"\n                    \" path found on representation.\"\n                )\n                continue\n\n            source_colorspace = colorspace_data[\"colorspace\"]\n            source_display = colorspace_data.get(\"display\")\n            source_view = colorspace_data.get(\"view\")\n\n            # Get representation files to convert\n            if isinstance(repre[\"files\"], list):\n                repre_files_to_convert = copy.deepcopy(repre[\"files\"])\n            else:\n                repre_files_to_convert = [repre[\"files\"]]\n\n            # Process each output definition\n            for output_def in profile_output_defs:\n                # Local copy to avoid accidental mutable changes\n                files_to_convert = list(repre_files_to_convert)\n\n                output_name = output_def[\"name\"]\n                new_repre = copy.deepcopy(repre)\n\n                original_staging_dir = new_repre[\"stagingDir\"]\n                new_staging_dir = get_temp_dir(\n                    project_name=instance.context.data[\"projectName\"],\n                    use_local_temp=True,\n                )\n                new_repre[\"stagingDir\"] = new_staging_dir\n\n                output_extension = output_def[\"extension\"]\n                output_extension = output_extension.replace('.', '')\n                self._rename_in_representation(new_repre,\n                                               files_to_convert,\n                                               output_name,\n                                               output_extension)\n\n                transcoding_type = output_def[\"transcoding_type\"]\n\n                # Set target colorspace/display/view based on transcoding type\n                target_colorspace = None\n                target_view = None\n                target_display = None\n                if transcoding_type == \"colorspace\":\n                    target_colorspace = output_def[\"colorspace\"]\n                elif transcoding_type == \"display_view\":\n                    display_view = output_def[\"display_view\"]\n                    # If empty values are provided in output definition,\n                    # fallback to scene display/view that is collected from DCC\n                    target_view = display_view[\"view\"] or scene_view\n                    target_display = display_view[\"display\"] or scene_display\n\n                # both could be already collected by DCC,\n                # but could be overwritten when transcoding\n                if target_view:\n                    new_repre[\"colorspaceData\"][\"view\"] = target_view\n                if target_display:\n                    new_repre[\"colorspaceData\"][\"display\"] = target_display\n                if target_colorspace:\n                    new_repre[\"colorspaceData\"][\"colorspace\"] = \\\n                        target_colorspace\n\n                additional_command_args = (output_def[\"oiiotool_args\"]\n                                           [\"additional_command_args\"])\n\n                sequence_files = self._translate_to_sequence(\n                    files_to_convert)\n                self.log.debug(\"Files to convert: {}\".format(sequence_files))\n                missing_rgba_review_channels = False\n                for file_name in sequence_files:\n                    if isinstance(file_name, clique.Collection):\n                        # Support sequences with holes by supplying\n                        # dedicated `--frames` argument to `oiiotool`\n                        # Create `frames` string like \"1001-1002,1004,1010-1012\n                        # Create `filename` string like \"file.#.exr\"\n                        frames = file_name.format(\"{ranges}\").replace(\" \", \"\")\n                        frame_padding = file_name.padding\n                        file_name = file_name.format(\"{head}#{tail}\")\n                        parallel_frames = True\n                    elif isinstance(file_name, str):\n                        # Single file\n                        frames = None\n                        frame_padding = None\n                        parallel_frames = False\n                    else:\n                        raise TypeError(\n                            f\"Unsupported file name type: {type(file_name)}.\"\n                            \" Expected str or clique.Collection.\"\n                        )\n\n                    self.log.debug(\"Transcoding file: `{}`\".format(file_name))\n                    input_path = os.path.join(original_staging_dir, file_name)\n                    output_path = self._get_output_file_path(input_path,\n                                                             new_staging_dir,\n                                                             output_extension)\n                    try:\n                        oiio_color_convert(\n                            input_path=input_path,\n                            output_path=output_path,\n                            config_path=config_path,\n                            source_colorspace=source_colorspace,\n                            target_colorspace=target_colorspace,\n                            target_display=target_display,\n                            target_view=target_view,\n                            source_display=source_display,\n                            source_view=source_view,\n                            additional_command_args=additional_command_args,\n                            frames=frames,\n                            frame_padding=frame_padding,\n                            parallel_frames=parallel_frames,\n                            review_layers=review_layers,\n                            logger=self.log,\n                        )\n                    except MissingRGBAChannelsError as exc:\n                        missing_rgba_review_channels = True\n                        self.log.error(exc)\n                        self.log.error(\n                            \"Skipping OIIO Transcode. Unknown RGBA channels\"\n                            f\" for colorspace conversion in file: {input_path}\"\n                        )\n                        break\n\n                if missing_rgba_review_channels:\n                    # Stop processing this representation\n                    break\n\n                # cleanup temporary transcoded files\n                for file_name in new_repre[\"files\"]:\n                    transcoded_file_path = os.path.join(new_staging_dir,\n                                                        file_name)\n                    instance.context.data[\"cleanupFullPaths\"].append(\n                        transcoded_file_path)\n\n                custom_tags = output_def.get(\"custom_tags\")\n                if custom_tags:\n                    if new_repre.get(\"custom_tags\") is None:\n                        new_repre[\"custom_tags\"] = []\n                    new_repre[\"custom_tags\"].extend(custom_tags)\n\n                # Add additional tags from output definition to representation\n                if new_repre.get(\"tags\") is None:\n                    new_repre[\"tags\"] = []\n                for tag in output_def[\"tags\"]:\n                    if tag not in new_repre[\"tags\"]:\n                        new_repre[\"tags\"].append(tag)\n\n                    if tag == \"review\":\n                        added_review = True\n\n                # If there is only 1 file outputted then convert list to\n                # string, because that'll indicate that it is not a sequence.\n                if len(new_repre[\"files\"]) == 1:\n                    new_repre[\"files\"] = new_repre[\"files\"][0]\n\n                # If the source representation has \"review\" tag, but it's not\n                # part of the output definition tags, then both the\n                # representations will be transcoded in ExtractReview and\n                # their outputs will clash in integration.\n                if \"review\" in repre.get(\"tags\", []):\n                    added_review = True\n\n                new_representations.append(new_repre)\n                added_representations = True\n\n            if added_representations:\n                self._mark_original_repre_for_deletion(\n                    repre, profile, added_review\n                )\n\n            tags = repre.get(\"tags\") or []\n            if \"delete\" in tags and \"thumbnail\" not in tags:\n                instance.data[\"representations\"].remove(repre)\n\n            # In case instance is not flagged for reviewable workflow\n            # by `review` family we have to add it so it can be processed\n            # by ExtractReview plugin\n            if (\n                added_review\n                and \"review\" not in instance.data[\"families\"]\n            ):\n                # TODO: Preferably we do not mess with families\n                #  at this point in processing, but ExtractReview\n                #  currently requires it. And this is the only way\n                #  to have a representation with `review` tag\n                #  actually getting picked up for non-review\n                #  families.\n                instance.data[\"families\"].append(\"review\")\n\n        instance.data[\"representations\"].extend(new_representations)\n\n    def _rename_in_representation(self, new_repre, files_to_convert,\n                                  output_name, output_extension):\n        \"\"\"Replace old extension with new one everywhere in representation.\n\n        Args:\n            new_repre (dict)\n            files_to_convert (list): of filenames from repre[\"files\"],\n                standardized to always list\n            output_name (str): key of output definition from Settings,\n                if \"&lt;passthrough&gt;\" token used, keep original repre name\n            output_extension (str): extension from output definition\n        \"\"\"\n        if output_name != \"passthrough\":\n            new_repre[\"name\"] = output_name\n        if not output_extension:\n            return\n\n        new_repre[\"ext\"] = output_extension\n        new_repre[\"outputName\"] = output_name\n\n        renamed_files = []\n        for file_name in files_to_convert:\n            file_name, _ = os.path.splitext(file_name)\n            file_name = '{}.{}'.format(file_name,\n                                       output_extension)\n            renamed_files.append(file_name)\n        new_repre[\"files\"] = renamed_files\n\n    def _translate_to_sequence(self, files_to_convert):\n        \"\"\"Returns original individual filepaths or list of clique.Collection.\n\n        Uses clique to find frame sequence, and return the collections instead.\n        If sequence not detected in input filenames, it returns original list.\n\n        Args:\n            files_to_convert (list[str]): list of file names\n        Returns:\n            list[str | clique.Collection]: List of\n                filepaths ['fileA.exr', 'fileB.exr']\n                or clique.Collection for a sequence.\n\n        \"\"\"\n        pattern = [clique.PATTERNS[\"frames\"]]\n        collections, _ = clique.assemble(\n            files_to_convert, patterns=pattern,\n            assume_padded_when_ambiguous=True)\n        if collections:\n            if len(collections) &gt; 1:\n                raise ValueError(\n                    \"Too many collections {}\".format(collections))\n\n            return collections\n\n        return files_to_convert\n\n    def _get_output_file_path(self, input_path, output_dir,\n                              output_extension):\n        \"\"\"Create output file name path.\"\"\"\n        file_name = os.path.basename(input_path)\n        file_name, input_extension = os.path.splitext(file_name)\n        if not output_extension:\n            output_extension = input_extension.replace(\".\", \"\")\n        new_file_name = '{}.{}'.format(file_name,\n                                       output_extension)\n        return os.path.join(output_dir, new_file_name)\n\n    def _get_profile(self, instance):\n        \"\"\"Returns profile if and how repre should be color transcoded.\"\"\"\n        host_name = instance.context.data[\"hostName\"]\n        product_base_type = instance.data.get(\"productBaseType\")\n        if not product_base_type:\n            product_base_type = instance.data[\"productType\"]\n        product_name = instance.data[\"productName\"]\n        task_data = instance.data[\"anatomyData\"].get(\"task\", {})\n        task_name = task_data.get(\"name\")\n        task_type = task_data.get(\"type\")\n        filtering_criteria = {\n            \"host_names\": host_name,\n            \"product_base_types\": product_base_type,\n            \"product_names\": product_name,\n            \"task_names\": task_name,\n            \"task_types\": task_type,\n        }\n        profile = filter_profiles(\n            self.profiles,\n            filtering_criteria,\n            logger=self.log\n        )\n\n        if not profile:\n            self.log.debug(\n                \"Skipped instance. None of profiles in presets are for\"\n                f\" Host name: \\\"{host_name}\\\"\"\n                f\" | Product base type: \\\"{product_base_type}\\\"\"\n                f\" | Product name: \\\"{product_name}\\\"\"\n                f\" | Task name \\\"{task_name}\\\"\"\n                f\" | Task type \\\"{task_type}\\\"\"\n            )\n\n        return profile\n\n    def _repre_is_valid(self, repre, profile):\n        \"\"\"Validation if representation should be processed.\n\n        Args:\n            repre (dict): Representation which should be checked.\n\n        Returns:\n            bool: False if can't be processed else True.\n        \"\"\"\n\n        if repre.get(\"ext\") not in self.supported_exts:\n            self.log.debug((\n                \"Representation '{}' has unsupported extension: '{}'. Skipped.\"\n            ).format(repre[\"name\"], repre.get(\"ext\")))\n            return False\n\n        if not repre.get(\"files\"):\n            self.log.debug((\n                \"Representation '{}' has empty files. Skipped.\"\n            ).format(repre[\"name\"]))\n            return False\n\n        if not repre.get(\"colorspaceData\"):\n            self.log.debug(\"Representation '{}' has no colorspace data. \"\n                           \"Skipped.\".format(repre[\"name\"]))\n            return False\n\n        representations_names = profile[\"representation_names\"]\n\n        # make sure that positive will be returned if no representations_names\n        if not representations_names:\n            return True\n\n        repre_name = repre[\"name\"]\n\n        # check if any of representation patterns match in repre_name\n        for r_pattern in representations_names:\n            if re.match(r_pattern, repre_name):\n                return True\n\n        return False\n\n    def _mark_original_repre_for_deletion(self, repre, profile, added_review):\n        \"\"\"If new transcoded representation created, delete old.\"\"\"\n        if not repre.get(\"tags\"):\n            repre[\"tags\"] = []\n\n        delete_original = profile[\"delete_original\"]\n\n        if delete_original:\n            if \"delete\" not in repre[\"tags\"]:\n                repre[\"tags\"].append(\"delete\")\n\n        if added_review and \"review\" in repre[\"tags\"]:\n            repre[\"tags\"].remove(\"review\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_colorspace_data.html","title":"extract_colorspace_data","text":""},{"location":"autoapi/client/ayon_core/plugins/publish/extract_colorspace_data.html#client.ayon_core.plugins.publish.extract_colorspace_data.ExtractColorspaceData","title":"<code>ExtractColorspaceData</code>","text":"<p>               Bases: <code>Extractor</code>, <code>ColormanagedPyblishPluginMixin</code></p> <p>Inject Colorspace data to available representations.</p> <p>Input data: - context.data[colorspace_config_path]:     for anatomy formatting of possible template tokens in config path - context.data[colorspace_config_path]:     for resolving project and host related config.ocio - context.data[colorspace_file_rules]:     for resolving matched file rule from representation file name     and adding it to representation</p> Output data <p>representation[colorspaceData] = {     \"colorspace\": \"linear\",     \"config\": {         \"path\": \"/abs/path/to/config.ocio\",         \"template\": \"{project[root]}/path/to/config.ocio\"     } }</p> Source code in <code>client/ayon_core/plugins/publish/extract_colorspace_data.py</code> <pre><code>class ExtractColorspaceData(publish.Extractor,\n                            publish.ColormanagedPyblishPluginMixin):\n    \"\"\" Inject Colorspace data to available representations.\n\n    Input data:\n    - context.data[colorspace_config_path]:\n        for anatomy formatting of possible template tokens in config path\n    - context.data[colorspace_config_path]:\n        for resolving project and host related config.ocio\n    - context.data[colorspace_file_rules]:\n        for resolving matched file rule from representation file name\n        and adding it to representation\n\n    Output data:\n        representation[colorspaceData] = {\n            \"colorspace\": \"linear\",\n            \"config\": {\n                \"path\": \"/abs/path/to/config.ocio\",\n                \"template\": \"{project[root]}/path/to/config.ocio\"\n            }\n        }\n    \"\"\"\n    label = \"Extract Colorspace data\"\n    order = pyblish.api.ExtractorOrder + 0.49\n\n    def process(self, instance):\n        representations = instance.data.get(\"representations\")\n        if not representations:\n            self.log.debug(\"No representations at instance : `{}`\".format(\n                instance))\n            return\n\n        # get colorspace settings\n        context = instance.context\n\n        # colorspace name could be kept in instance.data\n        colorspace = instance.data.get(\"colorspace\")\n\n        # loop representations\n        for representation in representations:\n            # skip if colorspaceData is already at representation\n            if representation.get(\"colorspaceData\"):\n                continue\n\n            self.set_representation_colorspace(\n                representation, context, colorspace)\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_hierarchy_to_ayon.html","title":"extract_hierarchy_to_ayon","text":""},{"location":"autoapi/client/ayon_core/plugins/publish/extract_hierarchy_to_ayon.html#client.ayon_core.plugins.publish.extract_hierarchy_to_ayon.ExtractHierarchyToAYON","title":"<code>ExtractHierarchyToAYON</code>","text":"<p>               Bases: <code>ContextPlugin</code></p> <p>Create entities in AYON based on collected data.</p> Source code in <code>client/ayon_core/plugins/publish/extract_hierarchy_to_ayon.py</code> <pre><code>class ExtractHierarchyToAYON(pyblish.api.ContextPlugin):\n    \"\"\"Create entities in AYON based on collected data.\"\"\"\n\n    order = pyblish.api.ExtractorOrder - 0.01\n    label = \"Extract Hierarchy To AYON\"\n\n    def process(self, context):\n        if not context.data.get(\"hierarchyContext\"):\n            self.log.debug(\"Skipping ExtractHierarchyToAYON\")\n            return\n\n        project_name = context.data[\"projectName\"]\n        self._create_hierarchy(context, project_name)\n        self._fill_instance_entities(context, project_name)\n\n    def _fill_instance_entities(self, context, project_name):\n        instances_by_folder_path = collections.defaultdict(list)\n        for instance in context:\n            if instance.data.get(\"publish\") is False:\n                continue\n\n            instance_entity = instance.data.get(\"folderEntity\")\n            if instance_entity:\n                continue\n\n            folder_path = instance.data.get(\"folderPath\")\n            instances_by_folder_path[folder_path].append(instance)\n\n        project_entity = context.data[\"projectEntity\"]\n        folder_entities = get_folders(\n            project_name, folder_paths=instances_by_folder_path.keys()\n        )\n        folder_entities_by_path = {\n            folder_entity[\"path\"]: folder_entity\n            for folder_entity in folder_entities\n        }\n        all_task_names = set()\n        folder_ids = set()\n        # Fill folderEntity and prepare data for task entities\n        for folder_path, instances in instances_by_folder_path.items():\n            folder_entity = folder_entities_by_path[folder_path]\n            folder_ids.add(folder_entity[\"id\"])\n            for instance in instances:\n                task_name = instance.data.get(\"task\")\n                all_task_names.add(task_name)\n\n        # Query task entities\n        # Discard 'None' task names\n        all_task_names.discard(None)\n        tasks_by_name_by_folder_id = {\n            folder_id: {} for folder_id in folder_ids\n        }\n        task_entities = []\n        if all_task_names:\n            task_entities = get_tasks(\n                project_name,\n                task_names=all_task_names,\n                folder_ids=folder_ids,\n            )\n        for task_entity in task_entities:\n            task_name = task_entity[\"name\"]\n            folder_id = task_entity[\"folderId\"]\n            tasks_by_name_by_folder_id[folder_id][task_name] = task_entity\n\n        for folder_path, instances in instances_by_folder_path.items():\n            folder_entity = folder_entities_by_path[folder_path]\n            folder_id = folder_entity[\"id\"]\n            folder_data = get_folder_template_data(\n                folder_entity, project_name\n            )\n            task_entities_by_name = tasks_by_name_by_folder_id[folder_id]\n            for instance in instances:\n                task_name = instance.data.get(\"task\")\n                task_entity = task_entities_by_name.get(task_name)\n                template_data = {}\n                if task_entity:\n                    template_data = get_task_template_data(\n                        project_entity, task_entity\n                    )\n                template_data.update(copy.deepcopy(folder_data))\n\n                instance.data[\"anatomyData\"].update(template_data)\n                instance.data[\"folderEntity\"] = folder_entity\n                instance.data[\"taskEntity\"] = task_entity\n\n    def _create_hierarchy(self, context, project_name):\n        hierarchy_context = self._filter_hierarchy(context)\n        if not hierarchy_context:\n            self.log.debug(\"All folders were filtered out\")\n            return\n\n        self.log.debug(\"Hierarchy_context: {}\".format(\n            json.dumps(hierarchy_context, default=_default_json_parse)\n        ))\n\n        entity_hub = EntityHub(project_name)\n        project = entity_hub.project_entity\n        folder_type_name_by_low_name = {\n            folder_type_item[\"name\"].lower(): folder_type_item[\"name\"]\n            for folder_type_item in project.get_folder_types()\n        }\n\n        hierarchy_match_queue = collections.deque()\n        hierarchy_match_queue.append((project, hierarchy_context))\n        while hierarchy_match_queue:\n            item = hierarchy_match_queue.popleft()\n            entity, entity_info = item\n\n            # Update attributes of entities\n            for attr_name, attr_value in entity_info[\"attributes\"].items():\n                if attr_name in entity.attribs:\n                    entity.attribs[attr_name] = attr_value\n\n            # Check if info has any children to sync\n            children_info = entity_info[\"children\"]\n            tasks_info = entity_info[\"tasks\"]\n            if not tasks_info and not children_info:\n                continue\n\n            # Prepare children by lowered name to easily find matching entities\n            children_by_low_name = {\n                child.name.lower(): child\n                for child in entity.children\n            }\n\n            # Create tasks if are not available\n            for task_info in tasks_info:\n                task_label = task_info[\"name\"]\n                task_name = slugify_string(task_label)\n                if task_name == task_label:\n                    task_label = None\n                task_entity = children_by_low_name.get(task_name.lower())\n                # TODO propagate updates of tasks if there are any\n                # TODO check if existing entity have 'task' type\n                if task_entity is None:\n                    task_entity = entity_hub.add_new_task(\n                        task_type=task_info[\"type\"],\n                        # TODO change 'parent_id' to 'folder_id' when ayon api\n                        #   is updated\n                        parent_id=entity.id,\n                        name=task_name\n                    )\n\n                if task_label:\n                    task_entity.label = task_label\n\n            # Create/Update sub-folders\n            for child_info in children_info:\n                child_label = child_info[\"name\"]\n                child_name = slugify_string(child_label)\n                if child_name == child_label:\n                    child_label = None\n                # TODO check if existing entity have 'folder' type\n                child_entity = children_by_low_name.get(child_name.lower())\n                if child_entity is None:\n                    folder_type = folder_type_name_by_low_name.get(\n                        child_info[\"folder_type\"].lower()\n                    )\n                    if folder_type is None:\n                        # TODO add validator for folder type validations\n                        self.log.warning((\n                            \"Couldn't find folder type '{}'\"\n                        ).format(child_info[\"folder_type\"]))\n                        folder_type = \"Folder\"\n\n                    child_entity = entity_hub.add_new_folder(\n                        folder_type=folder_type,\n                        parent_id=entity.id,\n                        name=child_name\n                    )\n\n                if child_label:\n                    child_entity.label = child_label\n\n                # Add folder to queue\n                hierarchy_match_queue.append((child_entity, child_info))\n\n        entity_hub.commit_changes()\n\n    def _filter_hierarchy(self, context):\n        \"\"\"Filter hierarchy context by active folder names.\n\n        Hierarchy context is filtered to folder names on active instances.\n\n        Change hierarchy context to unified structure which suits logic in\n        entity creation.\n\n        Output example:\n            {\n                \"name\": \"MyProject\",\n                \"entity_type\": \"project\",\n                \"attributes\": {},\n                \"tasks\": [],\n                \"children\": [\n                    {\n                        \"name\": \"seq_01\",\n                        \"entity_type\": \"Sequence\",\n                        \"attributes\": {},\n                        \"tasks\": [],\n                        \"children\": [\n                            ...\n                        ]\n                    },\n                    ...\n                ]\n            }\n\n        Todos:\n            Change how active folder are defined (names won't be enough in\n                AYON).\n\n        Args:\n            context (pyblish.api.Context): Pyblish context.\n\n        Returns:\n            dict[str, Any]: Hierarchy structure filtered by folder names.\n        \"\"\"\n\n        # filter only the active publishing instances\n        active_folder_paths = set()\n        for instance in context:\n            if instance.data.get(\"publish\", True) is not False:\n                active_folder_paths.add(instance.data.get(\"folderPath\"))\n\n        active_folder_paths.discard(None)\n\n        if not active_folder_paths:\n            return None\n\n        project_item = None\n        project_children_context = None\n        hierarchy_context = copy.deepcopy(context.data[\"hierarchyContext\"])\n        for key, value in hierarchy_context.items():\n            project_item = copy.deepcopy(value)\n            project_children_context = project_item.pop(\"children\", None)\n            project_item[\"name\"] = key\n            project_item[\"tasks\"] = []\n            project_item[\"attributes\"] = project_item.pop(\n                \"attributes\", {}\n            )\n            project_item[\"children\"] = []\n\n        if not project_children_context:\n            return None\n\n        project_id = uuid.uuid4().hex\n        items_by_id = {project_id: project_item}\n        parent_id_by_item_id = {project_id: None}\n        valid_ids = set()\n\n        hierarchy_queue = collections.deque()\n        hierarchy_queue.append((project_id, \"\", project_children_context))\n        while hierarchy_queue:\n            queue_item = hierarchy_queue.popleft()\n            parent_id, parent_path, children_context = queue_item\n            if not children_context:\n                continue\n\n            for folder_name, folder_info in children_context.items():\n                folder_path = \"{}/{}\".format(parent_path, folder_name)\n                if (\n                    folder_path not in active_folder_paths\n                    and not folder_info.get(\"children\")\n                ):\n                    continue\n\n                item_id = uuid.uuid4().hex\n                new_item = copy.deepcopy(folder_info)\n                new_children_context = new_item.pop(\"children\", None)\n                tasks = new_item.pop(\"tasks\", {})\n\n                new_item[\"name\"] = folder_name\n                new_item[\"children\"] = []\n                task_items = []\n                for task_name, task_info in tasks.items():\n                    task_info[\"name\"] = task_name\n                    task_items.append(task_info)\n                new_item[\"tasks\"] = task_items\n                new_item[\"attributes\"] = new_item.pop(\"attributes\", {})\n\n                items_by_id[item_id] = new_item\n                parent_id_by_item_id[item_id] = parent_id\n\n                if folder_path in active_folder_paths:\n                    valid_ids.add(item_id)\n                hierarchy_queue.append(\n                    (item_id, folder_path, new_children_context)\n                )\n\n        if not valid_ids:\n            return None\n\n        for item_id in set(valid_ids):\n            parent_id = parent_id_by_item_id[item_id]\n            while parent_id is not None and parent_id not in valid_ids:\n                valid_ids.add(parent_id)\n                parent_id = parent_id_by_item_id[parent_id]\n\n        valid_ids.discard(project_id)\n        for item_id in valid_ids:\n            parent_id = parent_id_by_item_id[item_id]\n            item = items_by_id[item_id]\n            parent_item = items_by_id[parent_id]\n            parent_item[\"children\"].append(item)\n\n        if not project_item[\"children\"]:\n            return None\n        return project_item\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_oiio_postprocess.html","title":"extract_oiio_postprocess","text":""},{"location":"autoapi/client/ayon_core/plugins/publish/extract_oiio_postprocess.html#client.ayon_core.plugins.publish.extract_oiio_postprocess.ExtractOIIOPostProcess","title":"<code>ExtractOIIOPostProcess</code>","text":"<p>               Bases: <code>Extractor</code></p> <p>Process representations through <code>oiiotool</code> with profile defined settings so that e.g. color space conversions can be applied or images could be converted to scanline, resized, etc. regardless of colorspace data.</p> Source code in <code>client/ayon_core/plugins/publish/extract_oiio_postprocess.py</code> <pre><code>class ExtractOIIOPostProcess(publish.Extractor):\n    \"\"\"Process representations through `oiiotool` with profile defined\n    settings so that e.g. color space conversions can be applied or images\n    could be converted to scanline, resized, etc. regardless of colorspace\n    data.\n    \"\"\"\n\n    label = \"OIIO Post Process\"\n    # After ExtractOIIOTranscode, before ExtractReview\n    order = pyblish.api.ExtractorOrder + 0.0195\n\n    settings_category = \"core\"\n\n    optional = True\n\n    # Supported extensions\n    supported_exts = {ext.lstrip(\".\") for ext in IMAGE_EXTENSIONS}\n\n    # Configurable by Settings\n    profiles = None\n    options = None\n\n    def process(self, instance):\n        if instance.data.get(\"farm\"):\n            self.log.debug(\"Should be processed on farm, skipping.\")\n            return\n\n        if not self.profiles:\n            self.log.debug(\"No profiles present for OIIO Post Process\")\n            return\n\n        if not instance.data.get(\"representations\"):\n            self.log.debug(\"No representations, skipping.\")\n            return\n\n        if not is_oiio_supported():\n            self.log.warning(\"OIIO not supported, no transcoding possible.\")\n            return\n\n        new_representations = []\n        for idx, repre in enumerate(list(instance.data[\"representations\"])):\n            self.log.debug(\"repre ({}): `{}`\".format(idx + 1, repre[\"name\"]))\n            if not self._repre_is_valid(repre):\n                continue\n\n            # We check profile per representation name and extension because\n            # it's included in the profile check. As such, an instance may have\n            # a different profile applied per representation.\n            profile = self._get_profile(\n                instance,\n                repre\n            )\n            if not profile:\n                continue\n\n            # Get representation files to convert\n            if isinstance(repre[\"files\"], list):\n                repre_files_to_convert = copy.deepcopy(repre[\"files\"])\n            else:\n                repre_files_to_convert = [repre[\"files\"]]\n\n            added_representations = False\n            added_review = False\n\n            # Process each output definition\n            for output_def in profile[\"outputs\"]:\n\n                # Local copy to avoid accidental mutable changes\n                files_to_convert = list(repre_files_to_convert)\n\n                output_name = output_def[\"name\"]\n                new_repre = copy.deepcopy(repre)\n\n                original_staging_dir = new_repre[\"stagingDir\"]\n                new_staging_dir = get_temp_dir(\n                    project_name=instance.context.data[\"projectName\"],\n                    use_local_temp=True,\n                )\n                new_repre[\"stagingDir\"] = new_staging_dir\n\n                output_extension = output_def[\"extension\"]\n                output_extension = output_extension.replace('.', '')\n                self._rename_in_representation(new_repre,\n                                               files_to_convert,\n                                               output_name,\n                                               output_extension)\n\n                sequence_files = self._translate_to_sequence(files_to_convert)\n                self.log.debug(\"Files to convert: {}\".format(sequence_files))\n                for file_name in sequence_files:\n                    if isinstance(file_name, clique.Collection):\n                        # Convert to filepath that can be directly converted\n                        # by oiio like `frame.1001-1025%04d.exr`\n                        file_name: str = file_name.format(\n                            \"{head}{range}{padding}{tail}\"\n                        )\n\n                    self.log.debug(\"Transcoding file: `{}`\".format(file_name))\n                    input_path = os.path.join(original_staging_dir,\n                                              file_name)\n                    output_path = self._get_output_file_path(input_path,\n                                                             new_staging_dir,\n                                                             output_extension)\n\n                    # TODO: Support formatting with dynamic keys from the\n                    #  representation, like e.g. colorspace config, display,\n                    #  view, etc.\n                    input_arguments: list[str] = output_def.get(\n                        \"input_arguments\", []\n                    )\n                    output_arguments: list[str] = output_def.get(\n                        \"output_arguments\", []\n                    )\n\n                    # Prepare subprocess arguments\n                    oiio_cmd = get_oiio_tool_args(\n                        \"oiiotool\",\n                        *input_arguments,\n                        input_path,\n                        *output_arguments,\n                        \"-o\",\n                        output_path\n                    )\n\n                    self.log.debug(\n                        \"Conversion command: {}\".format(\" \".join(oiio_cmd)))\n                    run_subprocess(oiio_cmd, logger=self.log)\n\n                # cleanup temporary transcoded files\n                for file_name in new_repre[\"files\"]:\n                    transcoded_file_path = os.path.join(new_staging_dir,\n                                                        file_name)\n                    instance.context.data[\"cleanupFullPaths\"].append(\n                        transcoded_file_path)\n\n                custom_tags = output_def.get(\"custom_tags\")\n                if custom_tags:\n                    if new_repre.get(\"custom_tags\") is None:\n                        new_repre[\"custom_tags\"] = []\n                    new_repre[\"custom_tags\"].extend(custom_tags)\n\n                # Add additional tags from output definition to representation\n                if new_repre.get(\"tags\") is None:\n                    new_repre[\"tags\"] = []\n                for tag in output_def[\"tags\"]:\n                    if tag not in new_repre[\"tags\"]:\n                        new_repre[\"tags\"].append(tag)\n\n                    if tag == \"review\":\n                        added_review = True\n\n                # If there is only 1 file outputted then convert list to\n                # string, because that'll indicate that it is not a sequence.\n                if len(new_repre[\"files\"]) == 1:\n                    new_repre[\"files\"] = new_repre[\"files\"][0]\n\n                # If the source representation has \"review\" tag, but it's not\n                # part of the output definition tags, then both the\n                # representations will be transcoded in ExtractReview and\n                # their outputs will clash in integration.\n                if \"review\" in repre.get(\"tags\", []):\n                    added_review = True\n\n                new_representations.append(new_repre)\n                added_representations = True\n\n            if added_representations:\n                self._mark_original_repre_for_deletion(\n                    repre, profile, added_review\n                )\n\n            tags = repre.get(\"tags\") or []\n            if \"delete\" in tags and \"thumbnail\" not in tags:\n                instance.data[\"representations\"].remove(repre)\n\n        instance.data[\"representations\"].extend(new_representations)\n\n    def _rename_in_representation(self, new_repre, files_to_convert,\n                                  output_name, output_extension):\n        \"\"\"Replace old extension with new one everywhere in representation.\n\n        Args:\n            new_repre (dict)\n            files_to_convert (list): of filenames from repre[\"files\"],\n                standardized to always list\n            output_name (str): key of output definition from Settings,\n                if \"&lt;passthrough&gt;\" token used, keep original repre name\n            output_extension (str): extension from output definition\n        \"\"\"\n        if output_name != \"passthrough\":\n            new_repre[\"name\"] = output_name\n        if not output_extension:\n            return\n\n        new_repre[\"ext\"] = output_extension\n        new_repre[\"outputName\"] = output_name\n\n        renamed_files = []\n        for file_name in files_to_convert:\n            file_name, _ = os.path.splitext(file_name)\n            file_name = '{}.{}'.format(file_name,\n                                       output_extension)\n            renamed_files.append(file_name)\n        new_repre[\"files\"] = renamed_files\n\n    def _translate_to_sequence(self, files_to_convert):\n        \"\"\"Returns original list or a clique.Collection of a sequence.\n\n        Uses clique to find frame sequence Collection.\n        If sequence not found, it returns original list.\n\n        Args:\n            files_to_convert (list): list of file names\n        Returns:\n            list[str | clique.Collection]: List of filepaths or a list\n                of Collections (usually one, unless there are holes)\n        \"\"\"\n        pattern = [clique.PATTERNS[\"frames\"]]\n        collections, _ = clique.assemble(\n            files_to_convert, patterns=pattern,\n            assume_padded_when_ambiguous=True)\n        if collections:\n            if len(collections) &gt; 1:\n                raise ValueError(\n                    \"Too many collections {}\".format(collections))\n\n            collection = collections[0]\n            # TODO: Technically oiiotool supports holes in the sequence as well\n            #  using the dedicated --frames argument to specify the frames.\n            #  We may want to use that too so conversions of sequences with\n            #  holes will perform faster as well.\n            # Separate the collection so that we have no holes/gaps per\n            # collection.\n            return collection.separate()\n\n        return files_to_convert\n\n    def _get_output_file_path(self, input_path, output_dir,\n                              output_extension):\n        \"\"\"Create output file name path.\"\"\"\n        file_name = os.path.basename(input_path)\n        file_name, input_extension = os.path.splitext(file_name)\n        if not output_extension:\n            output_extension = input_extension.replace(\".\", \"\")\n        new_file_name = '{}.{}'.format(file_name,\n                                       output_extension)\n        return os.path.join(output_dir, new_file_name)\n\n    def _get_profile(\n            self,\n            instance: pyblish.api.Instance,\n            repre: dict\n    ) -&gt; Optional[dict[str, Any]]:\n        \"\"\"Returns profile if it should process this instance.\"\"\"\n        host_name = instance.context.data[\"hostName\"]\n        product_base_type = instance.data.get(\"productBaseType\")\n        if not product_base_type:\n            product_base_type = instance.data[\"productType\"]\n        product_name = instance.data[\"productName\"]\n        task_data = instance.data[\"anatomyData\"].get(\"task\", {})\n        task_name = task_data.get(\"name\")\n        task_type = task_data.get(\"type\")\n        repre_name: str = repre[\"name\"]\n        repre_ext: str = repre[\"ext\"]\n        filtering_criteria = {\n            \"host_names\": host_name,\n            \"product_base_types\": product_base_type,\n            \"product_names\": product_name,\n            \"task_names\": task_name,\n            \"task_types\": task_type,\n            \"representation_names\": repre_name,\n            \"representation_exts\": repre_ext,\n        }\n        profile = filter_profiles(self.profiles, filtering_criteria,\n                                  logger=self.log)\n\n        if not profile:\n            self.log.debug(\n              \"Skipped instance. None of profiles in presets are for\"\n              f\" Host name: \\\"{host_name}\\\"\"\n              f\" | Product types: \\\"{product_base_type}\\\"\"\n              f\" | Product names: \\\"{product_name}\\\"\"\n              f\" | Task name \\\"{task_name}\\\"\"\n              f\" | Task type \\\"{task_type}\\\"\"\n              f\" | Representation: \\\"{repre_name}\\\" (.{repre_ext})\"\n            )\n\n        return profile\n\n    def _repre_is_valid(self, repre: dict) -&gt; bool:\n        \"\"\"Validation if representation should be processed.\n\n        Args:\n            repre (dict): Representation which should be checked.\n\n        Returns:\n            bool: False if can't be processed else True.\n        \"\"\"\n        if repre.get(\"ext\") not in self.supported_exts:\n            self.log.debug((\n                \"Representation '{}' has unsupported extension: '{}'. Skipped.\"\n            ).format(repre[\"name\"], repre.get(\"ext\")))\n            return False\n\n        if not repre.get(\"files\"):\n            self.log.debug((\n                \"Representation '{}' has empty files. Skipped.\"\n            ).format(repre[\"name\"]))\n            return False\n\n        if \"delete\" in repre.get(\"tags\", []):\n            self.log.debug((\n                \"Representation '{}' has 'delete' tag. Skipped.\"\n            ).format(repre[\"name\"]))\n            return False\n\n        return True\n\n    def _mark_original_repre_for_deletion(\n        self,\n        repre: dict,\n        profile: dict,\n        added_review: bool\n    ):\n        \"\"\"If new transcoded representation created, delete old.\"\"\"\n        if not repre.get(\"tags\"):\n            repre[\"tags\"] = []\n\n        delete_original = profile[\"delete_original\"]\n\n        if delete_original:\n            if \"delete\" not in repre[\"tags\"]:\n                repre[\"tags\"].append(\"delete\")\n\n        if added_review and \"review\" in repre[\"tags\"]:\n            repre[\"tags\"].remove(\"review\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_otio_audio_tracks.html","title":"extract_otio_audio_tracks","text":""},{"location":"autoapi/client/ayon_core/plugins/publish/extract_otio_audio_tracks.html#client.ayon_core.plugins.publish.extract_otio_audio_tracks.CollectParentAudioInstanceAttribute","title":"<code>CollectParentAudioInstanceAttribute</code>","text":"<p>               Bases: <code>ContextPlugin</code></p> <p>Collect audio instance attribute</p> Source code in <code>client/ayon_core/plugins/publish/extract_otio_audio_tracks.py</code> <pre><code>class CollectParentAudioInstanceAttribute(pyblish.api.ContextPlugin):\n    \"\"\"Collect audio instance attribute\"\"\"\n\n    order = pyblish.api.CollectorOrder\n    label = \"Collect Audio Instance Attribute\"\n\n    def process(self, context):\n\n        audio_instances = get_audio_instances(context)\n\n        # no need to continue if no audio instances found\n        if not audio_instances:\n            return\n\n        # create mapped instances by parent id\n        instances_by_parent_id = map_instances_by_parent_id(context)\n\n        # distribute audio related attribute\n        for audio_instance in audio_instances:\n            parent_instance_id = audio_instance.data[\"parent_instance_id\"]\n\n            for sibl_instance in instances_by_parent_id[parent_instance_id]:\n                # exclude the same audio instance\n                if sibl_instance.id == audio_instance.id:\n                    continue\n                self.log.info(\n                    \"Adding audio to Sibling instance: \"\n                    f\"{sibl_instance.data['label']}\"\n                )\n                sibl_instance.data[\"audio\"] = None\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_otio_audio_tracks.html#client.ayon_core.plugins.publish.extract_otio_audio_tracks.ExtractOtioAudioTracks","title":"<code>ExtractOtioAudioTracks</code>","text":"<p>               Bases: <code>ContextPlugin</code></p> <p>Extract Audio tracks from OTIO timeline.</p> <p>Process will merge all found audio tracks into one long .wav file at frist stage. Then it will trim it into individual short audio files relative to asset length and add it to each marked instance data representation. This is influenced by instance data audio attribute</p> Source code in <code>client/ayon_core/plugins/publish/extract_otio_audio_tracks.py</code> <pre><code>class ExtractOtioAudioTracks(pyblish.api.ContextPlugin):\n    \"\"\"Extract Audio tracks from OTIO timeline.\n\n    Process will merge all found audio tracks into one long .wav file at frist\n    stage. Then it will trim it into individual short audio files relative to\n    asset length and add it to each marked instance data representation. This\n    is influenced by instance data audio attribute \"\"\"\n\n    order = pyblish.api.ExtractorOrder - 0.44\n    label = \"Extract OTIO Audio Tracks\"\n\n    temp_dir_path = None\n\n    def process(self, context):\n        \"\"\"Convert otio audio track's content to audio representations\n\n        Args:\n            context (pyblish.Context): context of publisher\n        \"\"\"\n        # split the long audio file to peces devided by isntances\n        audio_instances = get_audio_instances(context)\n\n        # no need to continue if no audio instances found\n        if not audio_instances:\n            return\n\n        self.log.debug(\"Audio instances: {}\".format(len(audio_instances)))\n\n        # get sequence\n        otio_timeline = context.data[\"otioTimeline\"]\n\n        # get all audio inputs from otio timeline\n        audio_inputs = self.get_audio_track_items(otio_timeline)\n\n        if not audio_inputs:\n            return\n\n        # Convert all available audio into single file for trimming\n        audio_temp_fpath = self.create_temp_file(\"timeline_audio_track\")\n\n        # create empty audio with longest duration\n        empty = self.create_empty(audio_inputs)\n\n        # add empty to list of audio inputs\n        audio_inputs.insert(0, empty)\n\n        # create cmd\n        self.mix_audio(audio_inputs, audio_temp_fpath)\n\n        # remove empty\n        os.remove(empty[\"mediaPath\"])\n\n        # create mapped instances by parent id\n        instances_by_parent_id = map_instances_by_parent_id(context)\n\n        # cut instance framerange and add to representations\n        self.add_audio_to_instances(\n            audio_temp_fpath, audio_instances, instances_by_parent_id)\n\n        # remove full mixed audio file\n        os.remove(audio_temp_fpath)\n\n    def add_audio_to_instances(\n        self, audio_file, audio_instances, instances_by_parent_id):\n        created_files = []\n        for audio_instance in audio_instances:\n            folder_path = audio_instance.data[\"folderPath\"]\n            file_suffix = folder_path.replace(\"/\", \"-\")\n\n            recycling_file = [f for f in created_files if file_suffix in f]\n            audio_clip = audio_instance.data[\"otioClip\"]\n            audio_range = audio_clip.range_in_parent()\n            duration = audio_range.duration.to_frames()\n\n            # ffmpeg generate new file only if doesn't exists already\n            if not recycling_file:\n                parent_track = audio_clip.parent()\n                parent_track_start = parent_track.range_in_parent().start_time\n                relative_start_time = (\n                    audio_range.start_time - parent_track_start)\n                start_sec = relative_start_time.to_seconds()\n                duration_sec = audio_range.duration.to_seconds()\n\n                # shot related audio file\n                shot_audio_fpath = self.create_temp_file(file_suffix)\n\n                cmd = get_ffmpeg_tool_args(\n                    \"ffmpeg\",\n                    \"-ss\", str(start_sec),\n                    \"-t\", str(duration_sec),\n                    \"-i\", audio_file,\n                    shot_audio_fpath\n                )\n\n                # run subprocess\n                self.log.debug(\"Executing: {}\".format(\" \".join(cmd)))\n                run_subprocess(cmd, logger=self.log)\n\n                # add generated audio file to created files for recycling\n                if shot_audio_fpath not in created_files:\n                    created_files.append(shot_audio_fpath)\n            else:\n                shot_audio_fpath = recycling_file.pop()\n\n            # audio file needs to be published as representation\n            a_product_base_type = audio_instance.data.get(\"productBaseType\")\n            if not a_product_base_type:\n                a_product_base_type = audio_instance.data[\"productType\"]\n\n            if a_product_base_type == \"audio\":\n                # create empty representation attr\n                if \"representations\" not in audio_instance.data:\n                    audio_instance.data[\"representations\"] = []\n                # add to representations\n                audio_instance.data[\"representations\"].append({\n                    \"files\": os.path.basename(shot_audio_fpath),\n                    \"name\": \"wav\",\n                    \"ext\": \"wav\",\n                    \"stagingDir\": os.path.dirname(shot_audio_fpath),\n                    \"frameStart\": 0,\n                    \"frameEnd\": duration\n                })\n\n            # audio file needs to be reviewable too\n            elif \"reviewAudio\" in audio_instance.data.keys():\n                audio_attr = audio_instance.data.get(\"audio\") or []\n                audio_attr.append({\n                    \"filename\": shot_audio_fpath,\n                    \"offset\": 0\n                })\n                audio_instance.data[\"audio\"] = audio_attr\n\n            # Make sure if the audio instance is having siblink instances\n            # which needs audio for reviewable media so it is also added\n            # to its instance data\n            # Retrieve instance data from parent instance shot instance.\n            parent_instance_id = audio_instance.data[\"parent_instance_id\"]\n            for sibl_instance in instances_by_parent_id[parent_instance_id]:\n                # exclude the same audio instance\n                if sibl_instance.id == audio_instance.id:\n                    continue\n                self.log.info(\n                    \"Adding audio to Sibling instance: \"\n                    f\"{sibl_instance.data['label']}\"\n                )\n                audio_attr = sibl_instance.data.get(\"audio\") or []\n                audio_attr.append({\n                    \"filename\": shot_audio_fpath,\n                    \"offset\": 0\n                })\n                sibl_instance.data[\"audio\"] = audio_attr\n\n    def get_audio_track_items(self, otio_timeline):\n        \"\"\"Get all audio clips form OTIO audio tracks\n\n        Args:\n            otio_timeline (otio.schema.timeline): timeline object\n\n        Returns:\n            list: list of audio clip dictionaries\n        \"\"\"\n        # Not all hosts can import this module.\n        import opentimelineio as otio\n        from ayon_core.pipeline.editorial import OTIO_EPSILON\n\n        output = []\n        # go trough all audio tracks\n        for otio_track in otio_timeline.audio_tracks():\n            self.log.debug(\"_\" * 50)\n            playhead = 0\n            for otio_clip in otio_track:\n                self.log.debug(otio_clip)\n                if (isinstance(otio_clip, otio.schema.Clip) and\n                    not otio_clip.media_reference.is_missing_reference):\n                    media_av_start = otio_clip.available_range().start_time\n                    clip_start = otio_clip.source_range.start_time\n                    fps = clip_start.rate\n                    conformed_av_start = media_av_start.rescaled_to(fps)\n\n                    # Avoid rounding issue on media available range.\n                    if clip_start.almost_equal(\n                        conformed_av_start,\n                        OTIO_EPSILON\n                    ):\n                        conformed_av_start = clip_start\n\n                    # ffmpeg ignores embedded tc\n                    start = clip_start - conformed_av_start\n                    duration = otio_clip.source_range.duration\n                    media_path = otio_clip.media_reference.target_url\n                    input = {\n                        \"mediaPath\": media_path,\n                        \"delayFrame\": playhead,\n                        \"startFrame\": start.to_frames(),\n                        \"durationFrame\": duration.to_frames(),\n                        \"delayMilSec\": int(float(playhead / fps) * 1000),\n                        \"startSec\": start.to_seconds(),\n                        \"durationSec\": duration.to_seconds(),\n                        \"fps\": float(fps)\n                    }\n                    if input not in output:\n                        output.append(input)\n                        self.log.debug(\"__ input: {}\".format(input))\n\n                playhead += otio_clip.source_range.duration.value\n\n        return output\n\n    def create_empty(self, inputs):\n        \"\"\"Create an empty audio file used as duration placeholder\n\n        Args:\n            inputs (list): list of audio clip dictionaries\n\n        Returns:\n            dict: audio clip dictionary\n        \"\"\"\n        # temp file\n        empty_fpath = self.create_temp_file(\"empty\")\n\n        # get all end frames\n        end_secs = [(_i[\"delayFrame\"] + _i[\"durationFrame\"]) / _i[\"fps\"]\n                    for _i in inputs]\n        # get the max of end frames\n        max_duration_sec = max(end_secs)\n\n        # create empty cmd\n        cmd = get_ffmpeg_tool_args(\n            \"ffmpeg\",\n            \"-f\", \"lavfi\",\n            \"-i\", \"anullsrc=channel_layout=stereo:sample_rate=48000\",\n            \"-t\", str(max_duration_sec),\n            empty_fpath\n        )\n\n        # generate empty with ffmpeg\n        # run subprocess\n        self.log.debug(\"Executing: {}\".format(\" \".join(cmd)))\n\n        run_subprocess(\n            cmd, logger=self.log\n        )\n\n        # return dict with output\n        return {\n            \"mediaPath\": empty_fpath,\n            \"delayMilSec\": 0,\n            \"startSec\": 0.00,\n            \"durationSec\": max_duration_sec\n        }\n\n    def mix_audio(self, audio_inputs, audio_temp_fpath):\n        \"\"\"Creating multiple input cmd string\n\n        Args:\n            audio_inputs (list): list of input dicts. Order mater.\n\n        Returns:\n            str: the command body\n        \"\"\"\n\n        longest_input = 0\n        for audio_input in audio_inputs:\n            audio_len = audio_input[\"durationSec\"]\n            if audio_len &gt; longest_input:\n                longest_input = audio_len\n\n        # create cmd segments\n        input_args = []\n        filters = []\n        tag_names = []\n        for index, audio_input in enumerate(audio_inputs):\n            input_args.extend([\n                \"-ss\", str(audio_input[\"startSec\"]),\n                \"-t\", str(audio_input[\"durationSec\"]),\n                \"-i\", audio_input[\"mediaPath\"]\n            ])\n\n            # Output tag of a filtered audio input\n            tag_name = \"[r{}]\".format(index)\n            tag_names.append(tag_name)\n            # Delay in audio by delay in item\n            filters.append(\"[{}]adelay={}:all=1{}\".format(\n                index, audio_input[\"delayMilSec\"], tag_name\n            ))\n\n        # Mixing filter\n        #   - dropout transition (when audio will get loader) is set to be\n        #       higher then any input audio item\n        #   - volume is set to number of inputs - each mix adds 1/n volume\n        #       where n is input inder (to get more info read ffmpeg docs and\n        #       send a giftcard to contributor)\n        filters.append(\n            (\n                \"{}amix=inputs={}:duration=first:\"\n                \"dropout_transition={},volume={}[a]\"\n            ).format(\n                \"\".join(tag_names),\n                len(audio_inputs),\n                (longest_input * 1000) + 1000,\n                len(audio_inputs),\n            )\n        )\n\n        # Store filters to a file (separated by ',')\n        #   - this is to avoid \"too long\" command issue in ffmpeg\n        with tempfile.NamedTemporaryFile(\n            delete=False, mode=\"w\", suffix=\".txt\"\n        ) as tmp_file:\n            filters_tmp_filepath = tmp_file.name\n            tmp_file.write(\",\".join(filters))\n\n        args = get_ffmpeg_tool_args(\"ffmpeg\")\n        args.extend(input_args)\n        args.extend([\n            \"-filter_complex_script\", filters_tmp_filepath,\n            \"-map\", \"[a]\"\n        ])\n        args.append(audio_temp_fpath)\n\n        # run subprocess\n        self.log.debug(\"Executing: {}\".format(args))\n        run_subprocess(args, logger=self.log)\n\n        os.remove(filters_tmp_filepath)\n\n    def create_temp_file(self, file_suffix):\n        \"\"\"Create temp wav file\n\n        Args:\n            file_suffix (str): name to be used in file name\n\n        Returns:\n            str: temp fpath\n        \"\"\"\n        extension = \".wav\"\n        # get 8 characters\n        hash = hashlib.md5(str(uuid.uuid4()).encode()).hexdigest()[:8]\n        file_name = f\"{hash}_{file_suffix}{extension}\"\n\n        if not self.temp_dir_path:\n            audio_temp_dir_path = tempfile.mkdtemp(prefix=\"AYON_audio_\")\n            self.temp_dir_path = Path(audio_temp_dir_path)\n            self.temp_dir_path.mkdir(parents=True, exist_ok=True)\n\n        return (self.temp_dir_path / file_name).as_posix()\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_otio_audio_tracks.html#client.ayon_core.plugins.publish.extract_otio_audio_tracks.ExtractOtioAudioTracks.create_empty","title":"<code>create_empty(inputs)</code>","text":"<p>Create an empty audio file used as duration placeholder</p> <p>Parameters:</p> Name Type Description Default <code>inputs</code> <code>list</code> <p>list of audio clip dictionaries</p> required <p>Returns:</p> Name Type Description <code>dict</code> <p>audio clip dictionary</p> Source code in <code>client/ayon_core/plugins/publish/extract_otio_audio_tracks.py</code> <pre><code>def create_empty(self, inputs):\n    \"\"\"Create an empty audio file used as duration placeholder\n\n    Args:\n        inputs (list): list of audio clip dictionaries\n\n    Returns:\n        dict: audio clip dictionary\n    \"\"\"\n    # temp file\n    empty_fpath = self.create_temp_file(\"empty\")\n\n    # get all end frames\n    end_secs = [(_i[\"delayFrame\"] + _i[\"durationFrame\"]) / _i[\"fps\"]\n                for _i in inputs]\n    # get the max of end frames\n    max_duration_sec = max(end_secs)\n\n    # create empty cmd\n    cmd = get_ffmpeg_tool_args(\n        \"ffmpeg\",\n        \"-f\", \"lavfi\",\n        \"-i\", \"anullsrc=channel_layout=stereo:sample_rate=48000\",\n        \"-t\", str(max_duration_sec),\n        empty_fpath\n    )\n\n    # generate empty with ffmpeg\n    # run subprocess\n    self.log.debug(\"Executing: {}\".format(\" \".join(cmd)))\n\n    run_subprocess(\n        cmd, logger=self.log\n    )\n\n    # return dict with output\n    return {\n        \"mediaPath\": empty_fpath,\n        \"delayMilSec\": 0,\n        \"startSec\": 0.00,\n        \"durationSec\": max_duration_sec\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_otio_audio_tracks.html#client.ayon_core.plugins.publish.extract_otio_audio_tracks.ExtractOtioAudioTracks.create_temp_file","title":"<code>create_temp_file(file_suffix)</code>","text":"<p>Create temp wav file</p> <p>Parameters:</p> Name Type Description Default <code>file_suffix</code> <code>str</code> <p>name to be used in file name</p> required <p>Returns:</p> Name Type Description <code>str</code> <p>temp fpath</p> Source code in <code>client/ayon_core/plugins/publish/extract_otio_audio_tracks.py</code> <pre><code>def create_temp_file(self, file_suffix):\n    \"\"\"Create temp wav file\n\n    Args:\n        file_suffix (str): name to be used in file name\n\n    Returns:\n        str: temp fpath\n    \"\"\"\n    extension = \".wav\"\n    # get 8 characters\n    hash = hashlib.md5(str(uuid.uuid4()).encode()).hexdigest()[:8]\n    file_name = f\"{hash}_{file_suffix}{extension}\"\n\n    if not self.temp_dir_path:\n        audio_temp_dir_path = tempfile.mkdtemp(prefix=\"AYON_audio_\")\n        self.temp_dir_path = Path(audio_temp_dir_path)\n        self.temp_dir_path.mkdir(parents=True, exist_ok=True)\n\n    return (self.temp_dir_path / file_name).as_posix()\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_otio_audio_tracks.html#client.ayon_core.plugins.publish.extract_otio_audio_tracks.ExtractOtioAudioTracks.get_audio_track_items","title":"<code>get_audio_track_items(otio_timeline)</code>","text":"<p>Get all audio clips form OTIO audio tracks</p> <p>Parameters:</p> Name Type Description Default <code>otio_timeline</code> <code>timeline</code> <p>timeline object</p> required <p>Returns:</p> Name Type Description <code>list</code> <p>list of audio clip dictionaries</p> Source code in <code>client/ayon_core/plugins/publish/extract_otio_audio_tracks.py</code> <pre><code>def get_audio_track_items(self, otio_timeline):\n    \"\"\"Get all audio clips form OTIO audio tracks\n\n    Args:\n        otio_timeline (otio.schema.timeline): timeline object\n\n    Returns:\n        list: list of audio clip dictionaries\n    \"\"\"\n    # Not all hosts can import this module.\n    import opentimelineio as otio\n    from ayon_core.pipeline.editorial import OTIO_EPSILON\n\n    output = []\n    # go trough all audio tracks\n    for otio_track in otio_timeline.audio_tracks():\n        self.log.debug(\"_\" * 50)\n        playhead = 0\n        for otio_clip in otio_track:\n            self.log.debug(otio_clip)\n            if (isinstance(otio_clip, otio.schema.Clip) and\n                not otio_clip.media_reference.is_missing_reference):\n                media_av_start = otio_clip.available_range().start_time\n                clip_start = otio_clip.source_range.start_time\n                fps = clip_start.rate\n                conformed_av_start = media_av_start.rescaled_to(fps)\n\n                # Avoid rounding issue on media available range.\n                if clip_start.almost_equal(\n                    conformed_av_start,\n                    OTIO_EPSILON\n                ):\n                    conformed_av_start = clip_start\n\n                # ffmpeg ignores embedded tc\n                start = clip_start - conformed_av_start\n                duration = otio_clip.source_range.duration\n                media_path = otio_clip.media_reference.target_url\n                input = {\n                    \"mediaPath\": media_path,\n                    \"delayFrame\": playhead,\n                    \"startFrame\": start.to_frames(),\n                    \"durationFrame\": duration.to_frames(),\n                    \"delayMilSec\": int(float(playhead / fps) * 1000),\n                    \"startSec\": start.to_seconds(),\n                    \"durationSec\": duration.to_seconds(),\n                    \"fps\": float(fps)\n                }\n                if input not in output:\n                    output.append(input)\n                    self.log.debug(\"__ input: {}\".format(input))\n\n            playhead += otio_clip.source_range.duration.value\n\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_otio_audio_tracks.html#client.ayon_core.plugins.publish.extract_otio_audio_tracks.ExtractOtioAudioTracks.mix_audio","title":"<code>mix_audio(audio_inputs, audio_temp_fpath)</code>","text":"<p>Creating multiple input cmd string</p> <p>Parameters:</p> Name Type Description Default <code>audio_inputs</code> <code>list</code> <p>list of input dicts. Order mater.</p> required <p>Returns:</p> Name Type Description <code>str</code> <p>the command body</p> Source code in <code>client/ayon_core/plugins/publish/extract_otio_audio_tracks.py</code> <pre><code>def mix_audio(self, audio_inputs, audio_temp_fpath):\n    \"\"\"Creating multiple input cmd string\n\n    Args:\n        audio_inputs (list): list of input dicts. Order mater.\n\n    Returns:\n        str: the command body\n    \"\"\"\n\n    longest_input = 0\n    for audio_input in audio_inputs:\n        audio_len = audio_input[\"durationSec\"]\n        if audio_len &gt; longest_input:\n            longest_input = audio_len\n\n    # create cmd segments\n    input_args = []\n    filters = []\n    tag_names = []\n    for index, audio_input in enumerate(audio_inputs):\n        input_args.extend([\n            \"-ss\", str(audio_input[\"startSec\"]),\n            \"-t\", str(audio_input[\"durationSec\"]),\n            \"-i\", audio_input[\"mediaPath\"]\n        ])\n\n        # Output tag of a filtered audio input\n        tag_name = \"[r{}]\".format(index)\n        tag_names.append(tag_name)\n        # Delay in audio by delay in item\n        filters.append(\"[{}]adelay={}:all=1{}\".format(\n            index, audio_input[\"delayMilSec\"], tag_name\n        ))\n\n    # Mixing filter\n    #   - dropout transition (when audio will get loader) is set to be\n    #       higher then any input audio item\n    #   - volume is set to number of inputs - each mix adds 1/n volume\n    #       where n is input inder (to get more info read ffmpeg docs and\n    #       send a giftcard to contributor)\n    filters.append(\n        (\n            \"{}amix=inputs={}:duration=first:\"\n            \"dropout_transition={},volume={}[a]\"\n        ).format(\n            \"\".join(tag_names),\n            len(audio_inputs),\n            (longest_input * 1000) + 1000,\n            len(audio_inputs),\n        )\n    )\n\n    # Store filters to a file (separated by ',')\n    #   - this is to avoid \"too long\" command issue in ffmpeg\n    with tempfile.NamedTemporaryFile(\n        delete=False, mode=\"w\", suffix=\".txt\"\n    ) as tmp_file:\n        filters_tmp_filepath = tmp_file.name\n        tmp_file.write(\",\".join(filters))\n\n    args = get_ffmpeg_tool_args(\"ffmpeg\")\n    args.extend(input_args)\n    args.extend([\n        \"-filter_complex_script\", filters_tmp_filepath,\n        \"-map\", \"[a]\"\n    ])\n    args.append(audio_temp_fpath)\n\n    # run subprocess\n    self.log.debug(\"Executing: {}\".format(args))\n    run_subprocess(args, logger=self.log)\n\n    os.remove(filters_tmp_filepath)\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_otio_audio_tracks.html#client.ayon_core.plugins.publish.extract_otio_audio_tracks.ExtractOtioAudioTracks.process","title":"<code>process(context)</code>","text":"<p>Convert otio audio track's content to audio representations</p> <p>Parameters:</p> Name Type Description Default <code>context</code> <code>Context</code> <p>context of publisher</p> required Source code in <code>client/ayon_core/plugins/publish/extract_otio_audio_tracks.py</code> <pre><code>def process(self, context):\n    \"\"\"Convert otio audio track's content to audio representations\n\n    Args:\n        context (pyblish.Context): context of publisher\n    \"\"\"\n    # split the long audio file to peces devided by isntances\n    audio_instances = get_audio_instances(context)\n\n    # no need to continue if no audio instances found\n    if not audio_instances:\n        return\n\n    self.log.debug(\"Audio instances: {}\".format(len(audio_instances)))\n\n    # get sequence\n    otio_timeline = context.data[\"otioTimeline\"]\n\n    # get all audio inputs from otio timeline\n    audio_inputs = self.get_audio_track_items(otio_timeline)\n\n    if not audio_inputs:\n        return\n\n    # Convert all available audio into single file for trimming\n    audio_temp_fpath = self.create_temp_file(\"timeline_audio_track\")\n\n    # create empty audio with longest duration\n    empty = self.create_empty(audio_inputs)\n\n    # add empty to list of audio inputs\n    audio_inputs.insert(0, empty)\n\n    # create cmd\n    self.mix_audio(audio_inputs, audio_temp_fpath)\n\n    # remove empty\n    os.remove(empty[\"mediaPath\"])\n\n    # create mapped instances by parent id\n    instances_by_parent_id = map_instances_by_parent_id(context)\n\n    # cut instance framerange and add to representations\n    self.add_audio_to_instances(\n        audio_temp_fpath, audio_instances, instances_by_parent_id)\n\n    # remove full mixed audio file\n    os.remove(audio_temp_fpath)\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_otio_audio_tracks.html#client.ayon_core.plugins.publish.extract_otio_audio_tracks.get_audio_instances","title":"<code>get_audio_instances(context)</code>","text":"<p>Return only instances which are having audio in families</p> <p>Parameters:</p> Name Type Description Default <code>context</code> <code>context</code> <p>context of publisher</p> required <p>Returns:</p> Name Type Description <code>list</code> <p>list of selected instances</p> Source code in <code>client/ayon_core/plugins/publish/extract_otio_audio_tracks.py</code> <pre><code>def get_audio_instances(context):\n    \"\"\"Return only instances which are having audio in families\n\n    Args:\n        context (pyblish.context): context of publisher\n\n    Returns:\n        list: list of selected instances\n    \"\"\"\n    audio_instances = []\n    for instance in context:\n        if not instance.data.get(\"parent_instance_id\"):\n            continue\n\n        product_base_type = instance.data.get(\"productBaseType\")\n        if not product_base_type:\n            product_base_type = instance.data[\"productType\"]\n        if (\n            product_base_type == \"audio\"\n            or instance.data.get(\"reviewAudio\")\n        ):\n            audio_instances.append(instance)\n    return audio_instances\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_otio_audio_tracks.html#client.ayon_core.plugins.publish.extract_otio_audio_tracks.map_instances_by_parent_id","title":"<code>map_instances_by_parent_id(context)</code>","text":"<p>Create a mapping of instances by their parent id</p> <p>Parameters:</p> Name Type Description Default <code>context</code> <code>context</code> <p>context of publisher</p> required <p>Returns:</p> Name Type Description <code>dict</code> <p>mapping of instances by their parent id</p> Source code in <code>client/ayon_core/plugins/publish/extract_otio_audio_tracks.py</code> <pre><code>def map_instances_by_parent_id(context):\n    \"\"\"Create a mapping of instances by their parent id\n\n    Args:\n        context (pyblish.context): context of publisher\n\n    Returns:\n        dict: mapping of instances by their parent id\n    \"\"\"\n    instances_by_parent_id = collections.defaultdict(list)\n    for instance in context:\n        parent_instance_id = instance.data.get(\"parent_instance_id\")\n        if not parent_instance_id:\n            continue\n        instances_by_parent_id[parent_instance_id].append(instance)\n    return instances_by_parent_id\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_otio_file.html","title":"extract_otio_file","text":""},{"location":"autoapi/client/ayon_core/plugins/publish/extract_otio_file.html#client.ayon_core.plugins.publish.extract_otio_file.ExtractOTIOFile","title":"<code>ExtractOTIOFile</code>","text":"<p>               Bases: <code>Extractor</code></p> <p>Extractor export OTIO file</p> Source code in <code>client/ayon_core/plugins/publish/extract_otio_file.py</code> <pre><code>class ExtractOTIOFile(publish.Extractor):\n    \"\"\"\n    Extractor export OTIO file\n    \"\"\"\n\n    label = \"Extract OTIO file\"\n    order = pyblish.api.ExtractorOrder - 0.45\n    families = [\"workfile\"]\n    hosts = [\"resolve\", \"hiero\", \"traypublisher\"]\n\n    def process(self, instance):\n        # Not all hosts can import this module.\n        import opentimelineio as otio\n\n        if not instance.context.data.get(\"otioTimeline\"):\n            return\n        # create representation data\n        if \"representations\" not in instance.data:\n            instance.data[\"representations\"] = []\n\n        name = instance.data[\"name\"]\n        staging_dir = self.staging_dir(instance)\n\n        otio_timeline = instance.context.data[\"otioTimeline\"]\n        # create otio timeline representation\n        otio_file_name = name + \".otio\"\n        otio_file_path = os.path.join(staging_dir, otio_file_name)\n        otio.adapters.write_to_file(otio_timeline, otio_file_path)\n\n        representation_otio = {\n            'name': \"otio\",\n            'ext': \"otio\",\n            'files': otio_file_name,\n            \"stagingDir\": staging_dir,\n        }\n\n        instance.data[\"representations\"].append(representation_otio)\n\n        self.log.info(\"Added OTIO file representation: {}\".format(\n            representation_otio))\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_otio_review.html","title":"extract_otio_review","text":"Requires <p>instance -&gt; handleStart instance -&gt; handleEnd instance -&gt; otioClip instance -&gt; otioReviewClips</p> Optional <p>instance -&gt; workfileFrameStart instance -&gt; resolutionWidth instance -&gt; resolutionHeight</p> Provides <p>instance -&gt; otioReviewClips</p>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_otio_review.html#client.ayon_core.plugins.publish.extract_otio_review.ExtractOTIOReview","title":"<code>ExtractOTIOReview</code>","text":"<p>               Bases: <code>Extractor</code>, <code>ColormanagedPyblishPluginMixin</code></p> <p>Extract OTIO timeline into one concuted image sequence file.</p> <p>The <code>otioReviewClip</code> is holding trimmed range of clips relative to the <code>otioClip</code>. Handles are added during looping by available list of Gap and clips in the track. Handle start (head) is added before first Gap or Clip and Handle end (tail) is added at the end of last Clip or Gap. In case there is missing source material after the handles addition Gap will be added. At the end all Gaps are converted to black frames and available material is converted to image sequence frames. At the end representation is created and added to the instance.</p> <p>At the moment only image sequence output is supported</p> Source code in <code>client/ayon_core/plugins/publish/extract_otio_review.py</code> <pre><code>class ExtractOTIOReview(\n    publish.Extractor,\n    publish.ColormanagedPyblishPluginMixin\n):\n    \"\"\"\n    Extract OTIO timeline into one concuted image sequence file.\n\n    The `otioReviewClip` is holding trimmed range of clips relative to\n    the `otioClip`. Handles are added during looping by available list\n    of Gap and clips in the track. Handle start (head) is added before\n    first Gap or Clip and Handle end (tail) is added at the end of last\n    Clip or Gap. In case there is missing source material after the\n    handles addition Gap will be added. At the end all Gaps are converted\n    to black frames and available material is converted to image sequence\n    frames. At the end representation is created and added to the instance.\n\n    At the moment only image sequence output is supported\n\n    \"\"\"\n\n    order = api.ExtractorOrder - 0.45\n    label = \"Extract OTIO review\"\n    families = [\"review\"]\n    hosts = [\"resolve\", \"hiero\", \"flame\"]\n\n    # plugin default attributes\n    to_width = 1280\n    to_height = 720\n    output_ext = \".png\"\n\n    def process(self, instance):\n        # Not all hosts can import these modules.\n        import opentimelineio as otio\n        from ayon_core.pipeline.editorial import (\n            make_sequence_collection,\n            remap_range_on_file_sequence,\n            is_clip_from_media_sequence\n        )\n\n        # TODO refactor from using instance variable\n        self.temp_file_head = self._get_folder_name_based_prefix(instance)\n\n        # TODO: convert resulting image sequence to mp4\n\n        # get otio clip and other time info from instance clip\n        otio_review_clips = instance.data.get(\"otioReviewClips\")\n\n        if otio_review_clips is None:\n            self.log.info(f\"Instance `{instance}` has no otioReviewClips\")\n            return\n\n        # TODO: what if handles are different in `versionData`?\n        handle_start = instance.data[\"handleStart\"]\n        handle_end = instance.data[\"handleEnd\"]\n\n        # add plugin wide attributes\n        self.representation_files = []\n        self.used_frames = []\n        self.workfile_start = int(instance.data.get(\n            \"workfileFrameStart\", 1001)) - handle_start\n        # NOTE: padding has to be converted from\n        #       end frame since start could be lower then 1000\n        self.padding = len(str(instance.data.get(\"frameEnd\", 1001)))\n        self.used_frames.append(self.workfile_start)\n        self.to_width = instance.data.get(\n            \"resolutionWidth\") or self.to_width\n        self.to_height = instance.data.get(\n            \"resolutionHeight\") or self.to_height\n\n        # skip instance if no reviewable data available\n        if (\n            len(otio_review_clips) == 1\n            and (\n                not isinstance(otio_review_clips[0], otio.schema.Clip)\n                or otio_review_clips[0].media_reference.is_missing_reference\n            )\n        ):\n            self.log.warning(\n                \"Instance `{}` has nothing to process\".format(instance))\n            return\n        else:\n            self.staging_dir = self.staging_dir(instance)\n            if not instance.data.get(\"representations\"):\n                instance.data[\"representations\"] = list()\n\n        # loop available clips in otio track\n        for index, r_otio_cl in enumerate(otio_review_clips):\n            # QUESTION: what if transition on clip?\n\n            # Clip: compute process range from available media range.\n            src_range = r_otio_cl.source_range\n            if isinstance(r_otio_cl, otio.schema.Clip):\n                # check if resolution is the same as source\n                media_ref = r_otio_cl.media_reference\n                media_metadata = media_ref.metadata\n\n                # get from media reference metadata source\n                # TODO 'openpype' prefix should be removed (added 24/09/03)\n                # NOTE it looks like it is set only in hiero integration\n                res_data = {\"width\": self.to_width, \"height\": self.to_height}\n                for key in res_data:\n                    for meta_prefix in (\"ayon.source\", \"openpype.source\"):\n                        meta_key = f\"{meta_prefix}.{key}\"\n                        value = media_metadata.get(meta_key)\n                        if value is not None:\n                            res_data[key] = value\n                            break\n\n                self.to_width, self.to_height = (\n                    res_data[\"width\"], res_data[\"height\"]\n                )\n                self.log.debug(\n                    \"&gt; self.to_width x self.to_height:\"\n                    f\" {self.to_width} x {self.to_height}\"\n                )\n\n                available_range = r_otio_cl.available_range()\n                available_range_start_frame = (\n                    available_range.start_time.to_frames()\n                )\n                processing_range = None\n                self.actual_fps = available_range.duration.rate\n                start = src_range.start_time.rescaled_to(self.actual_fps)\n                duration = src_range.duration.rescaled_to(self.actual_fps)\n\n                # Temporary.\n                # Some AYON custom OTIO exporter were implemented with\n                # relative source range for image sequence. Following code\n                # maintain backward-compatibility by adjusting available range\n                # while we are updating those.\n                if (\n                    is_clip_from_media_sequence(r_otio_cl)\n                    and available_range_start_frame == media_ref.start_frame\n                    and start.to_frames() &lt; media_ref.start_frame\n                ):\n                    available_range = otio.opentime.TimeRange(\n                        otio.opentime.RationalTime(0, rate=self.actual_fps),\n                        available_range.duration,\n                    )\n\n            # Gap: no media, generate range based on source range\n            else:\n                available_range = processing_range = None\n                self.actual_fps = src_range.duration.rate\n                start = src_range.start_time\n                duration = src_range.duration\n\n            # Create handle offsets.\n            clip_handle_start = otio.opentime.RationalTime(\n                handle_start,\n                rate=self.actual_fps,\n            )\n            clip_handle_end = otio.opentime.RationalTime(\n                handle_end,\n                rate=self.actual_fps,\n            )\n\n            # reframing handles conditions\n            if (len(otio_review_clips) &gt; 1) and (index == 0):\n                # more clips | first clip reframing with handle\n                start -= clip_handle_start\n                duration += clip_handle_start\n            elif len(otio_review_clips) &gt; 1 \\\n                        and (index == len(otio_review_clips) - 1):\n                # more clips | last clip reframing with handle\n                duration += clip_handle_end\n            elif len(otio_review_clips) == 1:\n                # one clip | add both handles\n                start -= clip_handle_start\n                duration += (clip_handle_start + clip_handle_end)\n\n            if available_range:\n                processing_range = self._trim_available_range(\n                    available_range, start, duration)\n\n            # process all track items of the track\n            if isinstance(r_otio_cl, otio.schema.Clip):\n                # process Clip\n                media_ref = r_otio_cl.media_reference\n                metadata = media_ref.metadata\n                is_sequence = is_clip_from_media_sequence(r_otio_cl)\n\n                # File sequence way\n                if is_sequence:\n                    # Remap processing range to input file sequence.\n                    first, last = remap_range_on_file_sequence(\n                        r_otio_cl,\n                        processing_range,\n                    )\n                    input_fps = processing_range.start_time.rate\n\n                    if hasattr(media_ref, \"target_url_base\"):\n                        dirname = media_ref.target_url_base\n                        head = media_ref.name_prefix\n                        tail = media_ref.name_suffix\n                        collection = clique.Collection(\n                            head=head,\n                            tail=tail,\n                            padding=media_ref.frame_zero_padding\n                        )\n                        collection.indexes.update(\n                            [i for i in range(first, (last + 1))])\n                        # render segment\n                        self._render_segment(\n                            sequence=[dirname, collection, input_fps])\n                        # generate used frames\n                        self._generate_used_frames(\n                            len(collection.indexes))\n                    else:\n                        # in case it is file sequence but not new OTIO schema\n                        # `ImageSequenceReference`\n                        path = media_ref.target_url\n                        collection_data = make_sequence_collection(\n                            path, processing_range, metadata)\n                        dir_path, collection = collection_data\n\n                        # render segment\n                        self._render_segment(\n                            sequence=[dir_path, collection, input_fps])\n                        # generate used frames\n                        self._generate_used_frames(\n                            len(collection.indexes))\n\n                # Single video way.\n                # Extraction via FFmpeg.\n                elif hasattr(media_ref, \"target_url\"):\n                    path = media_ref.target_url\n                    # Set extract range from 0 (FFmpeg ignores\n                    #   embedded timecode).\n                    extract_range = otio.opentime.TimeRange(\n                        otio.opentime.RationalTime(\n                            (\n                                processing_range.start_time.value\n                                - available_range.start_time.value\n                            ),\n                            rate=available_range.start_time.rate,\n                        ),\n                        duration=processing_range.duration,\n                    )\n                    # render video file to sequence\n                    self._render_segment(\n                        video=[path, extract_range])\n                    # generate used frames\n                    self._generate_used_frames(\n                        processing_range.duration.value)\n\n            # QUESTION: what if nested track composition is in place?\n            else:\n                # at last process a Gap\n                self._render_segment(gap=duration.to_frames())\n                # generate used frames\n                self._generate_used_frames(duration.to_frames())\n\n        # creating and registering representation\n        representation = self._create_representation(start, duration)\n\n        # add colorspace data to representation\n        if colorspace := instance.data.get(\"reviewColorspace\"):\n            self.set_representation_colorspace(\n                representation, instance.context, colorspace\n            )\n\n        instance.data[\"representations\"].append(representation)\n        self.log.debug(\"Adding representation: {}\".format(representation))\n\n    def _create_representation(self, start, duration):\n        \"\"\"\n        Creating representation data.\n\n        Args:\n            start (int): start frame\n            duration (int): duration frames\n\n        Returns:\n            dict: representation data\n        \"\"\"\n\n        end = start + duration\n\n        # create default representation data\n        representation_data = {\n            \"frameStart\": start,\n            \"frameEnd\": end,\n            \"stagingDir\": self.staging_dir,\n            \"tags\": [\"review\", \"delete\"]\n        }\n\n        collection = clique.Collection(\n            self.temp_file_head,\n            tail=self.output_ext,\n            padding=self.padding,\n            indexes=set(self.used_frames)\n        )\n        start = min(collection.indexes)\n        end = max(collection.indexes)\n\n        files = [f for f in collection]\n        # single frame sequence\n        if len(files) == 1:\n            files = files[0]\n        ext = collection.format(\"{tail}\")\n        representation_data.update({\n            \"name\": ext[1:],\n            \"ext\": ext[1:],\n            \"files\": files,\n            \"frameStart\": start,\n            \"frameEnd\": end,\n        })\n        return representation_data\n\n    def _trim_available_range(self, avl_range, start, duration):\n        \"\"\"\n        Trim available media range to source range.\n\n        If missing media range is detected it will convert it into\n        black frames gaps.\n\n        Args:\n            avl_range (otio.time.TimeRange): media available time range\n            start (otio.time.RationalTime): start\n            duration (otio.time.RationalTime): duration\n\n        Returns:\n            otio.time.TimeRange: trimmed available range\n        \"\"\"\n        # Not all hosts can import these modules.\n        import opentimelineio as otio\n        from ayon_core.pipeline.editorial import (\n            trim_media_range,\n            OTIO_EPSILON,\n        )\n\n        def _round_to_frame(rational_time):\n            \"\"\" Handle rounding duration to frame.\n            \"\"\"\n            # OpentimelineIO &gt;= 0.16.0\n            try:\n                return rational_time.round().to_frames()\n\n            # OpentimelineIO &lt; 0.16.0\n            except AttributeError:\n                return otio.opentime.RationalTime(\n                    round(rational_time.value),\n                    rate=rational_time.rate,\n                ).to_frames()\n\n        avl_start = avl_range.start_time\n\n        # Avoid rounding issue on media available range.\n        if start.almost_equal(\n            avl_start,\n            OTIO_EPSILON\n        ):\n            avl_start = start\n\n        # An additional gap is required before the available\n        # range to conform source start point and head handles.\n        if start &lt; avl_start:\n            gap_duration = avl_start - start\n            start = avl_start\n            duration -= gap_duration\n            gap_duration = _round_to_frame(gap_duration)\n\n            # create gap data to disk\n            self._render_segment(gap=gap_duration)\n            # generate used frames\n            self._generate_used_frames(gap_duration)\n\n        # An additional gap is required after the available\n        # range to conform to source end point + tail handles\n        # (media duration is shorter then clip requirement).\n        end_point = start + duration\n        avl_end_point = avl_range.end_time_exclusive()\n\n        # Avoid rounding issue on media available range.\n        if end_point.almost_equal(\n            avl_end_point,\n            OTIO_EPSILON\n        ):\n            avl_end_point = end_point\n\n        if end_point &gt; avl_end_point:\n            gap_duration = end_point - avl_end_point\n            duration -= gap_duration\n            gap_duration = _round_to_frame(gap_duration)\n\n            # create gap data to disk\n            self._render_segment(\n                gap=gap_duration,\n                end_offset=duration.to_frames()\n            )\n            # generate used frames\n            self._generate_used_frames(\n                gap_duration,\n                end_offset=duration.to_frames()\n            )\n\n        # return correct trimmed range\n        return trim_media_range(\n            avl_range,\n            otio.opentime.TimeRange(\n                start,\n                duration\n            )\n        )\n\n    def _render_segment(self, sequence=None,\n                        video=None, gap=None, end_offset=None):\n        \"\"\"\n        Render segment into image sequence frames.\n\n        Using ffmpeg to convert compatible video and image source\n        to defined image sequence format.\n\n        Args:\n            sequence (list): input dir path string, collection object,\n                fps in list.\n            video (list)[optional]: video_path string, otio_range in list\n            gap (int)[optional]: gap duration\n            end_offset (int)[optional]: offset gap frame start in frames\n\n        Returns:\n            otio.time.TimeRange: trimmed available range\n        \"\"\"\n        # Not all hosts can import this module.\n        from ayon_core.pipeline.editorial import frames_to_seconds\n\n        # create path  and frame start to destination\n        output_path, out_frame_start = self._get_ffmpeg_output()\n\n        if end_offset:\n            out_frame_start += end_offset\n\n        # start command list\n        command = get_ffmpeg_tool_args(\"ffmpeg\")\n\n        input_extension = None\n        # Number of frames the output should contain. Explicitly capping\n        # this on the output side protects against ffmpeg reading past\n        # the intended range, e.g. the image2 demuxer has no \"end frame\"\n        # option and will keep consuming consecutive numbered files on\n        # disk beyond what this segment actually needs.\n        frame_count = None\n        if sequence is not None:\n            input_dir, collection, sequence_fps = sequence\n            in_frame_start = min(collection.indexes)\n            frame_count = len(collection.indexes)\n\n            # converting image sequence to image sequence\n            input_file = collection.format(\"{head}{padding}{tail}\")\n            input_path = os.path.join(input_dir, input_file)\n            input_extension = os.path.splitext(input_path)[-1]\n\n            \"\"\"\n            Form Command for Rendering Sequence Files\n\n            To explicitly set the input frame range and preserve the frame\n            range, avoid silent dropped frames caused by input mismatch\n            with FFmpeg's default rate of 25.0 fps. For more info,\n            refer to the FFmpeg image2 demuxer.\n\n            Implicit:\n                - Input: 100 frames (24fps from metadata)\n                - Demuxer: video 25fps\n                - Output: 98 frames, dropped 2\n\n            Explicit with \"-framerate\":\n                - Input: 100 frames (24fps from metadata)\n                - Demuxer: video 24fps\n                - Output: 100 frames, no dropped frames\n            \"\"\"\n\n            command.extend([\n                \"-start_number\", str(in_frame_start),\n                \"-framerate\", str(sequence_fps),\n                \"-i\", input_path\n            ])\n\n        elif video is not None:\n            video_path, otio_range = video\n            frame_start = otio_range.start_time.value\n            input_fps = otio_range.start_time.rate\n            frame_duration = otio_range.duration.value\n            frame_count = round(frame_duration)\n            sec_start = frames_to_seconds(frame_start, input_fps)\n            sec_duration = frames_to_seconds(\n                frame_duration, input_fps\n            )\n            input_extension = os.path.splitext(video_path)[-1]\n\n            # form command for rendering gap files\n            command.extend([\n                \"-ss\", str(sec_start),\n                \"-t\", str(sec_duration),\n                \"-i\", video_path\n            ])\n\n        elif gap is not None:\n            sec_duration = frames_to_seconds(gap, self.actual_fps)\n\n            # form command for rendering gap files\n            command.extend([\n                \"-t\", str(sec_duration),\n                \"-r\", str(self.actual_fps),\n                \"-f\", \"lavfi\",\n                \"-i\", \"color=c=black:s={}x{}\".format(\n                    self.to_width, self.to_height\n                ),\n                \"-tune\", \"stillimage\"\n            ])\n\n        else:\n            raise PublishError(\"Dev bug: Sequence, video or gap is required.\")\n\n        if video or sequence:\n            command.extend([\n                \"-vf\", f\"scale={self.to_width}:{self.to_height}:flags=lanczos\",\n                \"-compression_level\", \"5\",\n            ])\n\n        if frame_count is not None:\n            command.extend([\n                \"-frames:v\", str(frame_count)\n            ])\n\n        # add output attributes\n        command.extend([\n            \"-start_number\", str(out_frame_start)\n        ])\n\n        # add copying if extensions are matching\n        if (\n            input_extension\n            and self.output_ext == input_extension\n        ):\n            command.extend([\"-c\", \"copy\"])\n        else:\n            # For lossy formats, force re-encode\n            command.extend([\"-pix_fmt\", \"rgba\"])\n\n        # add output path at the end\n        command.append(output_path)\n\n        # execute\n        self.log.debug(\"Executing: {}\".format(\" \".join(command)))\n        output = run_subprocess(\n            command, logger=self.log\n        )\n        self.log.debug(\"Output: {}\".format(output))\n\n    def _generate_used_frames(self, duration, end_offset=None):\n        \"\"\"\n        Generating used frames into plugin argument `used_frames`.\n\n        The argument `used_frames` is used for checking next available\n        frame to start with during rendering sequence segments.\n\n        Args:\n            duration (int): duration of frames needed to be generated\n            end_offset (int)[optional]: in case frames need to be offseted\n\n        \"\"\"\n\n        padding = \"{{:0{}d}}\".format(self.padding)\n\n        if end_offset:\n            new_frames = list()\n            start_frame = self.used_frames[-1]\n            for index in range(end_offset,\n                               (int(end_offset + duration))):\n                seq_number = padding.format(start_frame + index)\n                self.log.debug(\n                    \"index: `{}` | seq_number: `{}`\".format(index, seq_number))\n                new_frames.append(int(seq_number))\n            new_frames += self.used_frames\n            self.used_frames = new_frames\n        else:\n            for _i in range(1, (int(duration) + 1)):\n                if self.used_frames[-1] == self.workfile_start:\n                    seq_number = padding.format(self.used_frames[-1])\n                    self.workfile_start -= 1\n                else:\n                    seq_number = padding.format(self.used_frames[-1] + 1)\n                    self.used_frames.append(int(seq_number))\n\n    def _get_ffmpeg_output(self):\n        \"\"\"\n        Returning ffmpeg output command arguments.\n\n        Returns:\n            str: output_path is path for image sequence output\n            int: out_frame_start is starting sequence frame\n\n        \"\"\"\n        output_file = \"{}{}{}\".format(\n            self.temp_file_head,\n            \"%0{}d\".format(self.padding),\n            self.output_ext\n        )\n        # create path to destination\n        output_path = os.path.join(self.staging_dir, output_file)\n\n        # generate frame start\n        out_frame_start = self.used_frames[-1] + 1\n        if self.used_frames[-1] == self.workfile_start:\n            out_frame_start = self.used_frames[-1]\n\n        return output_path, out_frame_start\n\n    def _get_folder_name_based_prefix(self, instance):\n        \"\"\"Creates 'unique' human readable file prefix to differentiate.\n\n        Multiple instances might share same temp folder, but each instance\n        would be differentiated by asset, eg. folder name.\n\n        It ix expected that there won't be multiple instances for same asset.\n        \"\"\"\n        folder_path = instance.data[\"folderPath\"]\n        folder_name = folder_path.split(\"/\")[-1]\n        folder_path = folder_path.replace(\"/\", \"_\").lstrip(\"_\")\n\n        file_prefix = f\"{folder_path}_{folder_name}.\"\n        self.log.debug(f\"file_prefix::{file_prefix}\")\n\n        return file_prefix\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_otio_trimming_video.html","title":"extract_otio_trimming_video","text":"Requires <p>instance -&gt; otioTrimmingRange instance -&gt; representations</p>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_otio_trimming_video.html#client.ayon_core.plugins.publish.extract_otio_trimming_video.ExtractOTIOTrimmingVideo","title":"<code>ExtractOTIOTrimmingVideo</code>","text":"<p>               Bases: <code>Extractor</code></p> <p>Trimming video file longer then required lenght</p> Source code in <code>client/ayon_core/plugins/publish/extract_otio_trimming_video.py</code> <pre><code>class ExtractOTIOTrimmingVideo(publish.Extractor):\n    \"\"\"\n    Trimming video file longer then required lenght\n\n    \"\"\"\n    order = pyblish.api.ExtractorOrder\n    label = \"Extract OTIO trim longer video\"\n    families = [\"trim\"]\n    hosts = [\"resolve\", \"hiero\", \"flame\"]\n\n    def process(self, instance):\n        self.staging_dir = self.staging_dir(instance)\n        otio_trim_range = instance.data[\"otioTrimmingRange\"]\n        representations = instance.data[\"representations\"]\n        self.log.debug(\"otio_trim_range: {}\".format(otio_trim_range))\n        self.log.debug(\"self.staging_dir: {}\".format(self.staging_dir))\n\n        # get corresponding representation\n        for _repre in representations:\n            if \"trim\" not in _repre.get(\"tags\", []):\n                continue\n\n            input_file = _repre[\"files\"]\n            input_file_path = os.path.normpath(os.path.join(\n                _repre[\"stagingDir\"], input_file\n            ))\n            self.log.debug(\"input_file_path: {}\".format(input_file_path))\n\n            # trim via ffmpeg\n            new_file = self._ffmpeg_trim_seqment(\n                input_file_path, otio_trim_range)\n\n            # prepare new representation data\n            repre_data = deepcopy(_repre)\n            # remove tags as we dont need them\n            repre_data.pop(\"tags\")\n            repre_data[\"stagingDir\"] = self.staging_dir\n            repre_data[\"files\"] = new_file\n\n            # romove `trim` tagged representation\n            representations.remove(_repre)\n            representations.append(repre_data)\n            self.log.debug(repre_data)\n\n        self.log.debug(\"representations: {}\".format(representations))\n\n    def _ffmpeg_trim_seqment(self, input_file_path, otio_range):\n        \"\"\"\n        Trim seqment of video file.\n\n        Using ffmpeg to trim video to desired length.\n\n        Args:\n            input_file_path (str): path string\n            otio_range (opentime.TimeRange): range to trim to\n\n        \"\"\"\n        # start command list\n        command = get_ffmpeg_tool_args(\"ffmpeg\")\n\n        video_path = input_file_path\n        sec_start = otio_range.start_time.to_seconds()\n        sec_duration = otio_range.duration.to_seconds()\n\n        # form command for rendering gap files\n        command.extend([\n            \"-ss\", str(sec_start),\n            \"-t\", str(sec_duration),\n            \"-i\", video_path,\n        ])\n\n        ffprobe_data = get_ffprobe_data(input_file_path, self.log)\n        video_codec_args = get_ffmpeg_codec_args(ffprobe_data)\n\n        # Trim the video by re-encoding the relevant part of it to\n        # the same codec. This is the only way to ensure a precise\n        # output duration.\n        if video_codec_args:\n            command.extend(video_codec_args)\n\n        # Cannot identify video codec, won't be able to re-encode and ensure\n        # precise duration. FFmpeg will cut at keyframes.\n        # https://video.stackexchange.com/questions/16750/\n        # Use '-c copy' which preserve the original video codec.\n        else:\n            self.log.warning(\n                \"FFmpeg could not identify the video codec for %s. \"\n                \"Falling back to '-c copy' which may lead to\"\n                \" duration mismatches.\",\n                input_file_path,\n            )\n            command.extend([\"-c\", \"copy\"])\n\n        # create and append path to destination\n        output_path = self._get_ffmpeg_output(input_file_path)\n        command.append(output_path)\n\n        # execute\n        self.log.debug(\"Executing: {}\".format(\" \".join(command)))\n        output = run_subprocess(\n            command, logger=self.log\n        )\n        self.log.debug(\"Output: {}\".format(output))\n\n        return os.path.basename(output_path)\n\n    def _get_ffmpeg_output(self, file_path):\n        \"\"\"\n        Returning ffmpeg output command arguments.\n\n        Arguments\"\n            file_path (str): path string\n\n        Returns:\n            str: output_path is path\n\n        \"\"\"\n        basename = os.path.basename(file_path)\n        name, ext = os.path.splitext(basename)\n\n        output_file = \"{}_{}{}\".format(\n            name,\n            \"trimmed\",\n            ext\n        )\n        # create path to destination\n        return os.path.join(self.staging_dir, output_file)\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_review.html","title":"extract_review","text":""},{"location":"autoapi/client/ayon_core/plugins/publish/extract_review.html#client.ayon_core.plugins.publish.extract_review.ExtractReview","title":"<code>ExtractReview</code>","text":"<p>               Bases: <code>InstancePlugin</code></p> <p>Extracting Reviewable medias</p> <p>Compulsory attribute of representation is tags list with \"review\", otherwise the representation is ignored.</p> <p>All new representations are created and encoded by ffmpeg following presets found in AYON Settings interface at <code>project_settings/global/publish/ExtractReview/profiles:outputs</code>.</p> Source code in <code>client/ayon_core/plugins/publish/extract_review.py</code> <pre><code>class ExtractReview(pyblish.api.InstancePlugin):\n    \"\"\"Extracting Reviewable medias\n\n    Compulsory attribute of representation is tags list with \"review\",\n    otherwise the representation is ignored.\n\n    All new representations are created and encoded by ffmpeg following\n    presets found in AYON Settings interface at\n    `project_settings/global/publish/ExtractReview/profiles:outputs`.\n    \"\"\"\n\n    label = \"Extract Review\"\n    order = pyblish.api.ExtractorOrder + 0.02\n    families = [\"review\"]\n\n    settings_category = \"core\"\n    # Supported extensions\n    image_exts = {\n        \"exr\", \"jpg\", \"jpeg\", \"png\", \"dpx\", \"tga\", \"tiff\", \"tif\", \"psd\"\n    }\n    video_exts = {\"mov\", \"mp4\"}\n    supported_exts = image_exts | video_exts\n\n    alpha_exts = {\"exr\", \"png\", \"dpx\"}\n\n    # Preset attributes\n    profiles = []\n\n    def process(self, instance):\n        self.log.debug(str(instance.data[\"representations\"]))\n        # Skip review when requested.\n        if not instance.data.get(\"review\", True):\n            return\n\n        orig_representations = tuple(instance.data[\"representations\"])\n\n        # Run processing\n        self.main_process(instance)\n\n        # Make sure cleanup happens and pop representations with \"delete\" tag.\n        for repre in orig_representations:\n            tags = repre.get(\"tags\") or []\n            # Representation is not marked to be deleted\n            if \"delete\" not in tags:\n                continue\n\n            # The representation can be used as thumbnail source\n            if \"thumbnail\" in tags or \"need_thumbnail\" in tags:\n                continue\n\n            self.log.debug(\n                \"Removing representation: {}\".format(repre)\n            )\n            instance.data[\"representations\"].remove(repre)\n\n    def _get_outputs_for_instance(self, instance):\n        host_name = instance.context.data[\"hostName\"]\n        product_base_type = instance.data.get(\"productBaseType\")\n        if not product_base_type:\n            product_base_type = instance.data[\"productType\"]\n        task_type = None\n        task_entity = instance.data.get(\"taskEntity\")\n        if task_entity:\n            task_type = task_entity[\"taskType\"]\n\n        self.log.debug(\n            f\"Host: \\\"{host_name}\\\"\"\n            f\"\\nProduct base type: \\\"{product_base_type}\\\"\"\n            f\"\\nTask type: \\\"{task_type}\\\"\"\n        )\n\n        profile = filter_profiles(\n            self.profiles,\n            {\n                \"host_names\": host_name,\n                \"product_base_types\": product_base_type,\n                \"task_types\": task_type,\n            },\n            logger=self.log)\n        if not profile:\n            self.log.info(\n                \"Skipped instance. None of profiles in presets are for\"\n                f\" Host: \\\"{host_name}\\\"\"\n                f\" | Product base type: \\\"{product_base_type}\\\"\"\n            )\n            return\n\n        self.log.debug(\"Matching profile: \\\"{}\\\"\".format(json.dumps(profile)))\n\n        product_name = instance.data.get(\"productName\")\n        instance_families = self.families_from_instance(instance)\n        filtered_outputs = self.filter_output_defs(\n            profile, product_name, instance_families\n        )\n        if not filtered_outputs:\n            self.log.info((\n                \"Skipped instance. All output definitions from selected\"\n                \" profile do not match instance families \\\"{}\\\" or\"\n                \" product name \\\"{}\\\".\"\n            ).format(str(instance_families), product_name))\n\n        # Store `filename_suffix` to save arguments\n        profile_outputs = []\n        for filename_suffix, definition in filtered_outputs.items():\n            definition[\"filename_suffix\"] = filename_suffix\n            profile_outputs.append(definition)\n\n        return profile_outputs\n\n    def _get_outputs_per_representations(self, instance, profile_outputs):\n        outputs_per_representations = []\n        for repre in instance.data[\"representations\"]:\n            repre_name = str(repre.get(\"name\"))\n            tags = repre.get(\"tags\") or []\n            custom_tags = repre.get(\"custom_tags\")\n            if \"review\" not in tags:\n                self.log.debug((\n                    \"Repre: {} - Didn't find \\\"review\\\" in tags. Skipping\"\n                ).format(repre_name))\n                continue\n\n            if \"thumbnail\" in tags:\n                self.log.debug((\n                    \"Repre: {} - Found \\\"thumbnail\\\" in tags. Skipping\"\n                ).format(repre_name))\n                continue\n\n            if \"passing\" in tags:\n                self.log.debug((\n                    \"Repre: {} - Found \\\"passing\\\" in tags. Skipping\"\n                ).format(repre_name))\n                continue\n\n            input_ext = repre[\"ext\"].lower()\n            if input_ext.startswith(\".\"):\n                input_ext = input_ext[1:]\n\n            if input_ext not in self.supported_exts:\n                self.log.info(\n                    \"Representation has unsupported extension \\\"{}\\\"\".format(\n                        input_ext\n                    )\n                )\n                continue\n\n            # Filter output definition by representation's\n            # custom tags (optional)\n            outputs = self.filter_outputs_by_custom_tags(\n                profile_outputs, custom_tags)\n            if not outputs:\n                self.log.info((\n                    \"Skipped representation. All output definitions from\"\n                    \" selected profile does not match to representation's\"\n                    \" custom tags. \\\"{}\\\"\"\n                ).format(str(custom_tags)))\n                continue\n\n            outputs_per_representations.append((repre, outputs))\n        return outputs_per_representations\n\n    def _single_frame_filter(self, input_filepaths, output_defs):\n        single_frame_image = False\n        if len(input_filepaths) == 1:\n            ext = os.path.splitext(input_filepaths[0])[-1]\n            single_frame_image = ext.lower() in IMAGE_EXTENSIONS\n\n        filtered_defs = []\n        for output_def in output_defs:\n            output_filters = output_def.get(\"filter\") or {}\n            frame_filter = output_filters.get(\"single_frame_filter\")\n            if (\n                (not single_frame_image and frame_filter == \"single_frame\")\n                or (single_frame_image and frame_filter == \"multi_frame\")\n            ):\n                continue\n\n            filtered_defs.append(output_def)\n\n        return filtered_defs\n\n    def main_process(self, instance):\n        instance_label = get_publish_instance_label(instance)\n        self.log.debug(\"Processing instance \\\"{}\\\"\".format(instance_label))\n        profile_outputs = self._get_outputs_for_instance(instance)\n        if not profile_outputs:\n            return\n\n        # Loop through representations\n        outputs_per_repres = self._get_outputs_per_representations(\n            instance, profile_outputs\n        )\n\n        project_settings = instance.context.data[\"project_settings\"]\n        review_layers = get_default_reviewable_layers(project_settings)\n        for repre, output_defs in outputs_per_repres:\n            # Check if input should be preconverted before processing\n            # Store original staging dir (it's value may change)\n            src_repre_staging_dir = repre[\"stagingDir\"]\n            # Receive filepath to first file in representation\n            first_input_path = None\n            input_filepaths = []\n            if not self.input_is_sequence(repre):\n                first_input_path = os.path.join(\n                    src_repre_staging_dir, repre[\"files\"]\n                )\n                input_filepaths.append(first_input_path)\n            else:\n                for filename in repre[\"files\"]:\n                    filepath = os.path.join(\n                        src_repre_staging_dir, filename\n                    )\n                    input_filepaths.append(filepath)\n                    if first_input_path is None:\n                        first_input_path = filepath\n\n            filtered_output_defs = self._single_frame_filter(\n                input_filepaths, output_defs\n            )\n            if not filtered_output_defs:\n                self.log.debug((\n                    \"Repre: {} - All output definitions were filtered\"\n                    \" out by single frame filter. Skipped.\"\n                ).format(repre[\"name\"]))\n                continue\n\n            # Skip if file is not set\n            if first_input_path is None:\n                self.log.warning((\n                    \"Representation \\\"{}\\\" has empty files. Skipped.\"\n                ).format(repre[\"name\"]))\n                continue\n\n            # Determine if representation requires pre conversion for ffmpeg\n            do_convert = should_convert_for_ffmpeg(\n                first_input_path, review_layers=review_layers\n            )\n            # If result is None the requirement of conversion can't be\n            #   determined\n            if do_convert is None:\n                self.log.info((\n                    \"Can't determine if representation requires conversion.\"\n                    \" Skipped.\"\n                ))\n                continue\n\n            layer_name = get_review_layer_name(\n                first_input_path, review_layers=review_layers\n            )\n\n            # Do conversion if needed\n            #   - change staging dir of source representation\n            #   - must be set back after output definitions processing\n            if do_convert:\n                new_staging_dir = get_temp_dir(\n                    project_name=instance.context.data[\"projectName\"],\n                    use_local_temp=True,\n                )\n                repre[\"stagingDir\"] = new_staging_dir\n\n                convert_input_paths_for_ffmpeg(\n                    input_filepaths,\n                    new_staging_dir,\n                    review_layers=review_layers,\n                    logger=self.log,\n                )\n                # The OIIO conversion will remap the RGBA channels just to\n                # `R,G,B,A` so we will pass the intermediate file to FFMPEG\n                # without layer name.\n                layer_name = \"\"\n\n            try:\n                self._render_output_definitions(\n                    instance,\n                    repre,\n                    src_repre_staging_dir,\n                    filtered_output_defs,\n                    layer_name\n                )\n\n            finally:\n                # Make sure temporary staging is cleaned up and representation\n                #   has set origin stagingDir\n                if do_convert:\n                    # Set staging dir of source representation back to previous\n                    #   value\n                    repre[\"stagingDir\"] = src_repre_staging_dir\n                    if os.path.exists(new_staging_dir):\n                        shutil.rmtree(new_staging_dir)\n\n    def _render_output_definitions(\n        self,\n        instance,\n        repre,\n        src_repre_staging_dir,\n        output_definitions,\n        layer_name\n    ):\n        fill_data = copy.deepcopy(instance.data[\"anatomyData\"])\n        for _output_def in output_definitions:\n            output_def = copy.deepcopy(_output_def)\n            # Make sure output definition has \"tags\" key\n            if \"tags\" not in output_def:\n                output_def[\"tags\"] = []\n\n            if \"burnins\" not in output_def:\n                output_def[\"burnins\"] = []\n\n            # Create copy of representation\n            new_repre = copy.deepcopy(repre)\n            new_tags = new_repre.get(\"tags\") or []\n            # Make sure new representation has origin staging dir\n            #   - this is because source representation may change\n            #       it's staging dir because of ffmpeg conversion\n            new_repre[\"stagingDir\"] = src_repre_staging_dir\n\n            # Remove \"delete\" tag from new repre if there is\n            if \"delete\" in new_tags:\n                new_tags.remove(\"delete\")\n\n            if \"need_thumbnail\" in new_tags:\n                new_tags.remove(\"need_thumbnail\")\n\n            # Add additional tags from output definition to representation\n            for tag in output_def[\"tags\"]:\n                if tag not in new_tags:\n                    new_tags.append(tag)\n\n            # Return tags to new representation\n            new_repre[\"tags\"] = new_tags\n\n            # Add burnin link from output definition to representation\n            for burnin in output_def[\"burnins\"]:\n                if burnin not in new_repre.get(\"burnins\", []):\n                    if not new_repre.get(\"burnins\"):\n                        new_repre[\"burnins\"] = []\n                    new_repre[\"burnins\"].append(str(burnin))\n\n            self.log.debug(\n                \"Linked burnins: `{}`\".format(new_repre.get(\"burnins\"))\n            )\n\n            self.log.debug(\n                \"New representation tags: `{}`\".format(\n                    new_repre.get(\"tags\"))\n            )\n\n            temp_data = self.prepare_temp_data(instance, repre, output_def)\n            new_frame_files = {}\n            if temp_data.input_is_sequence:\n                self.log.debug(\"Checking sequence to fill gaps in sequence..\")\n\n                files = temp_data.origin_repre[\"files\"]\n                collections = clique.assemble(\n                    files,\n                )[0]\n                if len(collections) != 1:\n                    raise PublishError(\n                        \"Found multiple collections, expected one.\"\n                        f\" {collections}\"\n                    )\n\n                collection = collections[0]\n\n                fill_missing_frames = _output_def[\"fill_missing_frames\"]\n                if fill_missing_frames == \"closest_existing\":\n                    new_frame_files = self.fill_sequence_gaps_from_existing(\n                        collection=collection,\n                        staging_dir=new_repre[\"stagingDir\"],\n                        start_frame=temp_data.frame_start,\n                        end_frame=temp_data.frame_end,\n                    )\n                elif fill_missing_frames == \"blank\":\n                    new_frame_files = self.fill_sequence_gaps_with_blanks(\n                        collection=collection,\n                        staging_dir=new_repre[\"stagingDir\"],\n                        start_frame=temp_data.frame_start,\n                        end_frame=temp_data.frame_end,\n                        resolution_width=temp_data.resolution_width,\n                        resolution_height=temp_data.resolution_height,\n                        extension=temp_data.input_ext,\n                        temp_data=temp_data,\n                    )\n                elif fill_missing_frames == \"previous_version\":\n                    fill_output = fill_sequence_gaps_with_previous_version(\n                        collection=collection,\n                        staging_dir=new_repre[\"stagingDir\"],\n                        instance=instance,\n                        current_repre_name=repre[\"name\"],\n                        start_frame=temp_data.frame_start,\n                        end_frame=temp_data.frame_end,\n                    )\n                    _, new_frame_files = fill_output\n                    # fallback to original workflow\n                    if new_frame_files is None:\n                        self.log.warning(\n                            \"Falling back to filling from currently \"\n                            \"last rendered.\"\n                        )\n                        new_frame_files = (\n                            self.fill_sequence_gaps_from_existing(\n                            collection=collection,\n                            staging_dir=new_repre[\"stagingDir\"],\n                            start_frame=temp_data.frame_start,\n                            end_frame=temp_data.frame_end,\n                        ))\n                elif fill_missing_frames == \"only_rendered\":\n                    temp_data.explicit_input_paths = [\n                        os.path.join(\n                            new_repre[\"stagingDir\"], file\n                        ).replace(\"\\\\\", \"/\")\n                        for file in files\n                    ]\n                    frame_start = min(collection.indexes)\n                    frame_end = max(collection.indexes)\n                    temp_data.frame_start = frame_start\n                    temp_data.frame_end = frame_end\n\n            temp_data.filled_files = new_frame_files\n\n            # create or update outputName\n            output_name = new_repre.get(\"outputName\", \"\")\n            output_ext = new_repre[\"ext\"]\n            if output_name:\n                output_name += \"_\"\n            output_name += output_def[\"filename_suffix\"]\n            if temp_data.without_handles:\n                output_name += \"_noHandles\"\n\n            # add outputName to anatomy format fill_data\n            fill_data.update({\n                \"output\": output_name,\n                \"ext\": output_ext,\n\n                # By adding `timecode` as data we can use it\n                # in the ffmpeg arguments for `--timecode` so that editorial\n                # like Resolve or Premiere can detect the start frame for e.g.\n                # review output files\n                \"timecode\": frame_to_timecode(\n                    frame=temp_data.frame_start_handle,\n                    fps=float(instance.data[\"fps\"])\n                )\n            })\n\n            try:  # temporary until oiiotool is supported cross platform\n                ffmpeg_args = self._ffmpeg_arguments(\n                    output_def,\n                    instance,\n                    new_repre,\n                    temp_data,\n                    fill_data,\n                    layer_name,\n                )\n            except ZeroDivisionError:\n                # TODO recalculate width and height using OIIO before\n                #   conversion\n                if 'exr' in temp_data.origin_repre[\"ext\"]:\n                    self.log.warning(\n                        (\n                            \"Unsupported compression on input files.\"\n                            \" Skipping!!!\"\n                        ),\n                        exc_info=True\n                    )\n                    return\n                raise NotImplementedError\n\n            subprcs_cmd = \" \".join(ffmpeg_args)\n\n            # run subprocess\n            self.log.debug(\"Executing: {}\".format(subprcs_cmd))\n\n            run_subprocess(subprcs_cmd, shell=True, logger=self.log)\n\n            # delete files added to fill gaps\n            if new_frame_files:\n                for filepath in new_frame_files.values():\n                    os.unlink(filepath)\n\n            for filepath in temp_data.paths_to_remove:\n                os.unlink(filepath)\n\n            new_repre.update({\n                \"fps\": temp_data.fps,\n                \"name\": \"{}_{}\".format(output_name, output_ext),\n                \"outputName\": output_name,\n                \"outputDef\": output_def,\n                \"ffmpeg_cmd\": subprcs_cmd\n            })\n\n            # Force to pop these key if are in new repre\n            new_repre.pop(\"thumbnail\", None)\n            if \"clean_name\" in new_repre.get(\"tags\", []):\n                new_repre.pop(\"outputName\")\n\n            # adding representation\n            self.log.debug(\n                \"Adding new representation: {}\".format(new_repre)\n            )\n            instance.data[\"representations\"].append(new_repre)\n\n            add_repre_files_for_cleanup(instance, new_repre)\n\n    def input_is_sequence(self, repre):\n        \"\"\"Deduce from representation data if input is sequence.\"\"\"\n        # TODO GLOBAL ISSUE - Find better way how to find out if input\n        #  is sequence. Issues (in theory):\n        #   - there may be multiple files ant not be sequence\n        #   - remainders are not checked at all\n        #   - there can be more than one collection\n        return isinstance(repre[\"files\"], (list, tuple))\n\n    def prepare_temp_data(self, instance, repre, output_def) -&gt; TempData:\n        \"\"\"Prepare dictionary with values used across extractor's process.\n\n        All data are collected from instance, context, origin representation\n        and output definition.\n\n        There are few required keys in Instance data: \"frameStart\", \"frameEnd\"\n        and \"fps\".\n\n        Args:\n            instance (Instance): Currently processed instance.\n            repre (dict): Representation from which new representation was\n                copied.\n            output_def (dict): Definition of output of this plugin.\n\n        Returns:\n            TempData: All data which are used across methods during process.\n                Their values should not change during process but new keys\n                with values may be added.\n        \"\"\"\n\n        frame_start = instance.data[\"frameStart\"]\n        frame_end = instance.data[\"frameEnd\"]\n\n        # Try to get handles from instance\n        handle_start = instance.data.get(\"handleStart\")\n        handle_end = instance.data.get(\"handleEnd\")\n        # If even one of handle values is not set on instance use\n        # handles from context\n        if handle_start is None or handle_end is None:\n            handle_start = instance.context.data[\"handleStart\"]\n            handle_end = instance.context.data[\"handleEnd\"]\n\n        frame_start_handle = frame_start - handle_start\n        frame_end_handle = frame_end + handle_end\n\n        # Change output frames when output should be without handles\n        without_handles = bool(\"no-handles\" in output_def[\"tags\"])\n        if without_handles:\n            output_frame_start = frame_start\n            output_frame_end = frame_end\n        else:\n            output_frame_start = frame_start_handle\n            output_frame_end = frame_end_handle\n\n        handles_are_set = handle_start &gt; 0 or handle_end &gt; 0\n\n        with_audio = True\n        if (\n            # Check if has `no-audio` tag\n            \"no-audio\" in output_def[\"tags\"]\n            # Check if instance has ny audio in data\n            or not instance.data.get(\"audio\")\n        ):\n            with_audio = False\n\n        input_is_sequence = self.input_is_sequence(repre)\n        input_allow_bg = False\n        first_sequence_frame = None\n\n        if input_is_sequence and repre[\"files\"]:\n            # Calculate first frame that should be used\n            cols, _ = clique.assemble(repre[\"files\"])\n            input_frames = list(sorted(cols[0].indexes))\n            first_sequence_frame = input_frames[0]\n            # WARNING: This is an issue as we don't know if first frame\n            #   is with or without handles!\n            # - handle start is added but how do not know if we should\n            output_duration = (output_frame_end - output_frame_start) + 1\n            if (\n                without_handles\n                and len(input_frames) - handle_start &gt;= output_duration\n            ):\n                first_sequence_frame += handle_start\n\n            ext = os.path.splitext(repre[\"files\"][0])[1].replace(\".\", \"\")\n            if ext.lower() in self.alpha_exts:\n                input_allow_bg = True\n        else:\n            ext = os.path.splitext(repre[\"files\"])[1].replace(\".\", \"\")\n\n        return TempData(\n            fps=float(instance.data[\"fps\"]),\n            frame_start=frame_start,\n            frame_end=frame_end,\n            handle_start=handle_start,\n            handle_end=handle_end,\n            frame_start_handle=frame_start_handle,\n            frame_end_handle=frame_end_handle,\n            output_frame_start=int(output_frame_start),\n            output_frame_end=int(output_frame_end),\n            pixel_aspect=instance.data.get(\"pixelAspect\", 1),\n            resolution_width=instance.data.get(\"resolutionWidth\"),\n            resolution_height=instance.data.get(\"resolutionHeight\"),\n            origin_repre=repre,\n            input_is_sequence=input_is_sequence,\n            first_sequence_frame=first_sequence_frame,\n            input_allow_bg=input_allow_bg,\n            with_audio=with_audio,\n            without_handles=without_handles,\n            handles_are_set=handles_are_set,\n            input_ext=ext,\n            explicit_input_paths=[],  # absolute paths to rendered files\n            paths_to_remove=[]\n        )\n\n    def _ffmpeg_arguments(\n        self,\n        output_def,\n        instance,\n        new_repre,\n        temp_data,\n        fill_data,\n        layer_name\n    ):\n        \"\"\"Prepares ffmpeg arguments for expected extraction.\n\n        Prepares input and output arguments based on output definition and\n        input files.\n\n        Args:\n            output_def (dict): Currently processed output definition.\n            instance (Instance): Currently processed instance.\n            new_repre (dict): Representation representing output of this\n                process.\n            temp_data (TempData): Base data for successful process.\n        \"\"\"\n\n        # Get FFmpeg arguments from profile presets\n        out_def_ffmpeg_args = output_def.get(\"ffmpeg_args\") or {}\n\n        _ffmpeg_input_args = out_def_ffmpeg_args.get(\"input\") or []\n        _ffmpeg_output_args = out_def_ffmpeg_args.get(\"output\") or []\n        _ffmpeg_video_filters = out_def_ffmpeg_args.get(\"video_filters\") or []\n        _ffmpeg_audio_filters = out_def_ffmpeg_args.get(\"audio_filters\") or []\n\n        # Cleanup empty strings\n        ffmpeg_input_args = [\n            value for value in _ffmpeg_input_args if value.strip()\n        ]\n        ffmpeg_video_filters = [\n            value for value in _ffmpeg_video_filters if value.strip()\n        ]\n        ffmpeg_audio_filters = [\n            value for value in _ffmpeg_audio_filters if value.strip()\n        ]\n\n        ffmpeg_output_args = []\n        for value in _ffmpeg_output_args:\n            value = value.strip()\n            if not value:\n                continue\n            try:\n                value = value.format(**fill_data)\n            except Exception:\n                self.log.warning(\n                    \"Failed to format ffmpeg argument: {}\".format(value),\n                    exc_info=True\n                )\n                pass\n            ffmpeg_output_args.append(value)\n\n        # Prepare input and output filepaths\n        self.input_output_paths(new_repre, output_def, temp_data)\n\n        # Set output frames len to 1 when output is single image\n        if (\n            temp_data.output_ext_is_image\n            and not temp_data.output_is_sequence\n        ):\n            output_frames_len = 1\n\n        else:\n            output_frames_len = (\n                temp_data.output_frame_end\n                - temp_data.output_frame_start\n                + 1\n            )\n\n        duration_seconds = float(output_frames_len / temp_data.fps)\n\n        # Define which layer should be used\n        if layer_name:\n            ffmpeg_input_args.extend([\"-layer\", layer_name])\n\n        explicit_input_paths = temp_data.explicit_input_paths\n        if temp_data.input_is_sequence and not explicit_input_paths:\n            # Set start frame of input sequence (just frame in filename)\n            # - definition of input filepath\n            # - add handle start if output should be without handles\n            start_number = temp_data.first_sequence_frame\n            if temp_data.without_handles and temp_data.handles_are_set:\n                start_number += temp_data.handle_start\n            ffmpeg_input_args.extend([\n                \"-start_number\", str(start_number)\n            ])\n\n            # TODO add fps mapping `{fps: fraction}` ?\n            # - e.g.: {\n            #     \"25\": \"25/1\",\n            #     \"24\": \"24/1\",\n            #     \"23.976\": \"24000/1001\"\n            # }\n            # Add framerate to input when input is sequence\n            ffmpeg_input_args.extend([\n                \"-framerate\", str(temp_data.fps)\n            ])\n            # Add duration of an input sequence if output is video\n            if not temp_data.output_is_sequence:\n                ffmpeg_input_args.extend([\n                    \"-to\", \"{:0.10f}\".format(duration_seconds)\n                ])\n\n        if temp_data.output_is_sequence and not explicit_input_paths:\n            # Set start frame of output sequence (just frame in filename)\n            # - this is definition of an output\n            ffmpeg_output_args.extend([\n                \"-start_number\", str(temp_data.output_frame_start)\n            ])\n\n        # Change output's duration and start point if should not contain\n        # handles\n        if temp_data.without_handles and temp_data.handles_are_set:\n            # Set output duration in seconds\n            ffmpeg_output_args.extend([\n                \"-t\", \"{:0.10}\".format(duration_seconds)\n            ])\n\n            # Add -ss (start offset in seconds) if input is not sequence\n            if not temp_data.input_is_sequence:\n                start_sec = float(temp_data.handle_start) / temp_data.fps\n                # Set start time without handles\n                # - Skip if start sec is 0.0\n                if start_sec &gt; 0.0:\n                    ffmpeg_input_args.extend([\n                        \"-ss\", \"{:0.10f}\".format(start_sec)\n                    ])\n\n        # Set frame range of output when input or output is sequence\n        elif temp_data.output_is_sequence:\n            ffmpeg_output_args.extend([\n                \"-frames:v\", str(output_frames_len)\n            ])\n\n        if not explicit_input_paths:\n            # Add video/image input path\n            ffmpeg_input_args.extend([\n                \"-i\", path_to_subprocess_arg(temp_data.full_input_path)\n            ])\n        else:\n            frame_duration = 1 / temp_data.fps\n\n            explicit_frames_meta = tempfile.NamedTemporaryFile(\n                mode=\"w\", prefix=\"explicit_frames\", suffix=\".txt\", delete=False\n            )\n            explicit_frames_meta.close()\n            explicit_frames_path = explicit_frames_meta.name\n            with open(explicit_frames_path, \"w\") as fp:\n                lines = [\n                    f\"file '{path}'{os.linesep}duration {frame_duration}\"\n                    for path in temp_data.explicit_input_paths\n                ]\n                fp.write(\"\\n\".join(lines))\n            temp_data.paths_to_remove.append(explicit_frames_path)\n\n            # let ffmpeg use only rendered files, might have gaps\n            ffmpeg_input_args.extend([\n                \"-f\", \"concat\",\n                \"-safe\", \"0\",\n                \"-i\", path_to_subprocess_arg(explicit_frames_path),\n                \"-r\", str(temp_data.fps)\n            ])\n\n        # Add audio arguments if there are any. Skipped when output are images.\n        if not temp_data.output_ext_is_image and temp_data.with_audio:\n            audio_in_args, audio_filters, audio_out_args = self.audio_args(\n                instance, temp_data, duration_seconds\n            )\n            ffmpeg_input_args.extend(audio_in_args)\n            ffmpeg_audio_filters.extend(audio_filters)\n            ffmpeg_output_args.extend(audio_out_args)\n\n        res_filters = self.rescaling_filters(temp_data, output_def, new_repre)\n        ffmpeg_video_filters.extend(res_filters)\n\n        ffmpeg_input_args = self.split_ffmpeg_args(ffmpeg_input_args)\n\n        lut_filters = self.lut_filters(new_repre, instance, ffmpeg_input_args)\n        ffmpeg_video_filters.extend(lut_filters)\n\n        bg_alpha = 0.0\n        bg_color = output_def.get(\"bg_color\")\n        if bg_color:\n            bg_red, bg_green, bg_blue, bg_alpha = bg_color\n\n        if bg_alpha &gt; 0.0:\n            if not temp_data.input_allow_bg:\n                self.log.info((\n                    \"Output definition has defined BG color input was\"\n                    \" resolved as does not support adding BG.\"\n                ))\n            else:\n                bg_color_hex = \"#{0:0&gt;2X}{1:0&gt;2X}{2:0&gt;2X}\".format(\n                    bg_red, bg_green, bg_blue\n                )\n                bg_color_str = \"{}@{}\".format(bg_color_hex, bg_alpha)\n\n                self.log.info(\"Applying BG color {}\".format(bg_color_str))\n                color_args = [\n                    \"split=2[bg][fg]\",\n                    \"[bg]drawbox=c={}:replace=1:t=fill[bg]\".format(\n                        bg_color_str\n                    ),\n                    \"[bg][fg]overlay=format=auto\"\n                ]\n                # Prepend bg color change before all video filters\n                # NOTE at the time of creation it is required as video filters\n                #   from settings may affect color of BG\n                #   e.g. `eq` can remove alpha from input\n                for arg in reversed(color_args):\n                    ffmpeg_video_filters.insert(0, arg)\n\n        # Add argument to override output file\n        ffmpeg_output_args.append(\"-y\")\n\n        # NOTE This must be latest added item to output arguments.\n        ffmpeg_output_args.append(\n            path_to_subprocess_arg(temp_data.full_output_path)\n        )\n\n        return self.ffmpeg_full_args(\n            ffmpeg_input_args,\n            ffmpeg_video_filters,\n            ffmpeg_audio_filters,\n            ffmpeg_output_args\n        )\n\n    def split_ffmpeg_args(self, in_args):\n        \"\"\"Makes sure all entered arguments are separated in individual items.\n\n        Split each argument string with \" -\" to identify if string contains\n        one or more arguments.\n        \"\"\"\n        splitted_args = []\n        for arg in in_args:\n            sub_args = arg.split(\" -\")\n            if len(sub_args) == 1:\n                if arg and arg not in splitted_args:\n                    splitted_args.append(arg)\n                continue\n\n            for idx, arg in enumerate(sub_args):\n                if idx != 0:\n                    arg = \"-\" + arg\n\n                if arg and arg not in splitted_args:\n                    splitted_args.append(arg)\n        return splitted_args\n\n    def ffmpeg_full_args(\n        self, input_args, video_filters, audio_filters, output_args\n    ):\n        \"\"\"Post processing of collected FFmpeg arguments.\n\n        Just verify that output arguments does not contain video or audio\n        filters which may cause issues because of duplicated argument entry.\n        Filters found in output arguments are moved to list they belong to.\n\n        Args:\n            input_args (list): All collected ffmpeg arguments with inputs.\n            video_filters (list): All collected video filters.\n            audio_filters (list): All collected audio filters.\n            output_args (list): All collected ffmpeg output arguments with\n                output filepath.\n\n        Returns:\n            list: Containing all arguments ready to run in subprocess.\n        \"\"\"\n        output_args = self.split_ffmpeg_args(output_args)\n\n        video_args_dentifiers = [\"-vf\", \"-filter:v\"]\n        audio_args_dentifiers = [\"-af\", \"-filter:a\"]\n        for arg in tuple(output_args):\n            for identifier in video_args_dentifiers:\n                if arg.startswith(\"{} \".format(identifier)):\n                    output_args.remove(arg)\n                    arg = arg.replace(identifier, \"\").strip()\n                    video_filters.append(arg)\n\n            for identifier in audio_args_dentifiers:\n                if arg.startswith(\"{} \".format(identifier)):\n                    output_args.remove(arg)\n                    arg = arg.replace(identifier, \"\").strip()\n                    audio_filters.append(arg)\n\n        all_args = [\n            subprocess.list2cmdline(get_ffmpeg_tool_args(\"ffmpeg\"))\n        ]\n        all_args.extend(input_args)\n        if video_filters:\n            all_args.append(\"-filter:v\")\n            all_args.append(\"\\\"{}\\\"\".format(\",\".join(video_filters)))\n\n        if audio_filters:\n            all_args.append(\"-filter:a\")\n            all_args.append(\"\\\"{}\\\"\".format(\",\".join(audio_filters)))\n\n        all_args.extend(output_args)\n\n        return all_args\n\n    def fill_sequence_gaps_with_blanks(\n        self,\n        collection: str,\n        staging_dir: str,\n        start_frame: int,\n        end_frame: int,\n        resolution_width: int,\n        resolution_height: int,\n        extension: str,\n        temp_data: TempData\n    ) -&gt; Optional[dict[int, str]]:\n        \"\"\"Fills missing files by blank frame.\"\"\"\n\n        blank_frame_path = None\n\n        added_files = {}\n\n        col_format = collection.format(\"{head}{padding}{tail}\")\n        for frame in range(start_frame, end_frame + 1):\n            if frame in collection.indexes:\n                continue\n            hole_fpath = os.path.join(staging_dir, col_format % frame)\n            if blank_frame_path is None:\n                blank_frame_path = self._create_blank_frame(\n                    staging_dir, extension, resolution_width, resolution_height\n                )\n                temp_data.paths_to_remove.append(blank_frame_path)\n            copyfile(blank_frame_path, hole_fpath)\n            added_files[frame] = hole_fpath\n\n        return added_files\n\n    def _create_blank_frame(\n        self,\n        staging_dir,\n        extension,\n        resolution_width,\n        resolution_height\n    ):\n        blank_frame_path = os.path.join(staging_dir, f\"blank.{extension}\")\n\n        command = get_ffmpeg_tool_args(\n            \"ffmpeg\",\n            \"-f\", \"lavfi\",\n            \"-i\", \"color=c=black:s={}x{}:d=1\".format(\n                resolution_width, resolution_height\n            ),\n            \"-tune\", \"stillimage\",\n            \"-frames:v\", \"1\",\n            blank_frame_path\n        )\n\n        self.log.debug(\"Executing: {}\".format(\" \".join(command)))\n        output = run_subprocess(\n            command, logger=self.log\n        )\n        self.log.debug(\"Output: {}\".format(output))\n\n        return blank_frame_path\n\n    def fill_sequence_gaps_from_existing(\n        self,\n        collection,\n        staging_dir: str,\n        start_frame: int,\n        end_frame: int\n    ) -&gt; dict[int, str]:\n        \"\"\"Fill missing files in sequence by duplicating existing ones.\n\n        This will take nearest frame file and copy it with so as to fill\n        gaps in sequence. Last existing file there is is used to for the\n        hole ahead.\n\n        Args:\n            collection (clique.collection)\n            staging_dir (str): Path to staging directory.\n            start_frame (int): Sequence start (no matter what files are there)\n            end_frame (int): Sequence end (no matter what files are there)\n\n        Returns:\n            dict[int, str] of added files. Those should be cleaned after work\n                is done.\n\n        Raises:\n            PublishError: if more than one collection is obtained.\n\n        \"\"\"\n        # Prepare which hole is filled with what frame\n        #   - the frame is filled only with already existing frames\n        prev_frame = next(iter(collection.indexes))\n        hole_frame_to_nearest = {}\n        for frame in range(int(start_frame), int(end_frame) + 1):\n            if frame in collection.indexes:\n                prev_frame = frame\n            else:\n                # Use previous frame as source for hole\n                hole_frame_to_nearest[frame] = prev_frame\n\n        # Calculate paths\n        added_files = {}\n        col_format = collection.format(\"{head}{padding}{tail}\")\n        for hole_frame, src_frame in hole_frame_to_nearest.items():\n            hole_fpath = os.path.join(staging_dir, col_format % hole_frame)\n            src_fpath = os.path.join(staging_dir, col_format % src_frame)\n            if not os.path.isfile(src_fpath):\n                raise PublishError(\n                    f\"Missing previously detected file: {src_fpath}\"\n                )\n\n            copyfile(src_fpath, hole_fpath)\n            added_files[hole_frame] = hole_fpath\n\n        return added_files\n\n    def input_output_paths(self, new_repre, output_def, temp_data: TempData):\n        \"\"\"Deduce input nad output file paths based on entered data.\n\n        Input may be sequence of images, video file or single image file and\n        same can be said about output, this method helps to find out what\n        their paths are.\n\n        It is validated that output directory exist and creates if not.\n\n        During process are set \"files\", \"stagingDir\", \"ext\" and\n        \"sequence_file\" (if output is sequence) keys to new representation.\n        \"\"\"\n\n        repre = temp_data.origin_repre\n        src_staging_dir = repre[\"stagingDir\"]\n        dst_staging_dir = new_repre[\"stagingDir\"]\n\n        if temp_data.input_is_sequence:\n            collections = clique.assemble(repre[\"files\"])[0]\n            full_input_path = os.path.join(\n                src_staging_dir,\n                collections[0].format(\"{head}{padding}{tail}\")\n            )\n\n            filename = collections[0].format(\"{head}\")\n            if filename.endswith(\".\"):\n                filename = filename[:-1]\n\n            # Make sure to have full path to one input file\n            full_input_path_single_file = os.path.join(\n                src_staging_dir, repre[\"files\"][0]\n            )\n\n        else:\n            full_input_path = os.path.join(\n                src_staging_dir, repre[\"files\"]\n            )\n            filename = os.path.splitext(repre[\"files\"])[0]\n\n            # Make sure to have full path to one input file\n            full_input_path_single_file = full_input_path\n\n        filled_files = temp_data.filled_files\n        if filled_files:\n            first_frame, first_file = next(iter(filled_files.items()))\n            if first_file &lt; full_input_path_single_file:\n                self.log.warning(f\"Using filled frame: '{first_file}'\")\n                full_input_path_single_file = first_file\n                temp_data.first_sequence_frame = first_frame\n\n        filename_suffix = output_def[\"filename_suffix\"]\n\n        output_ext = output_def.get(\"ext\")\n        # Use input extension if output definition do not specify it\n        if output_ext is None:\n            output_ext = os.path.splitext(full_input_path)[1]\n\n        # TODO Define if extension should have dot or not\n        if output_ext.startswith(\".\"):\n            output_ext = output_ext[1:]\n\n        output_ext = output_ext.lower()\n\n        # Store extension to representation\n        new_repre[\"ext\"] = output_ext\n\n        self.log.debug(\"New representation ext: `{}`\".format(output_ext))\n\n        # Output is image file sequence with frames\n        output_ext_is_image = bool(output_ext in self.image_exts)\n        output_is_sequence = bool(\n            output_ext_is_image\n            and \"sequence\" in output_def[\"tags\"]\n        )\n        if output_is_sequence:\n            new_repre_files = []\n            frame_start = temp_data.output_frame_start\n            frame_end = temp_data.output_frame_end\n\n            filename_base = \"{}_{}\".format(filename, filename_suffix)\n            # Temporary template for frame filling. Example output:\n            # \"basename.%04d.exr\" when `frame_end` == 1001\n            repr_file = \"{}.%{:0&gt;2}d.{}\".format(\n                filename_base, len(str(frame_end)), output_ext\n            )\n\n            for frame in range(frame_start, frame_end + 1):\n                new_repre_files.append(repr_file % frame)\n\n            new_repre[\"sequence_file\"] = repr_file\n            full_output_path = os.path.join(\n                dst_staging_dir, filename_base, repr_file\n            )\n\n        else:\n            repr_file = \"{}_{}.{}\".format(\n                filename, filename_suffix, output_ext\n            )\n            full_output_path = os.path.join(dst_staging_dir, repr_file)\n            new_repre_files = repr_file\n\n        # Store files to representation\n        new_repre[\"files\"] = new_repre_files\n\n        # Make sure stagingDire exists\n        dst_staging_dir = os.path.normpath(os.path.dirname(full_output_path))\n        if not os.path.exists(dst_staging_dir):\n            self.log.debug(\"Creating dir: {}\".format(dst_staging_dir))\n            os.makedirs(dst_staging_dir)\n\n        # Store stagingDir to representation\n        new_repre[\"stagingDir\"] = dst_staging_dir\n\n        # Store paths to temp data\n        temp_data.full_input_path = full_input_path\n        temp_data.full_input_path_single_file = full_input_path_single_file\n        temp_data.full_output_path = full_output_path\n\n        # Store information about output\n        temp_data.output_ext_is_image = output_ext_is_image\n        temp_data.output_is_sequence = output_is_sequence\n\n        self.log.debug(\"Input path {}\".format(full_input_path))\n        self.log.debug(\"Output path {}\".format(full_output_path))\n\n    def audio_args(self, instance, temp_data: TempData, duration_seconds):\n        \"\"\"Prepares FFMpeg arguments for audio inputs.\"\"\"\n        audio_in_args = []\n        audio_filters = []\n        audio_out_args = []\n        audio_inputs = instance.data.get(\"audio\")\n        if not audio_inputs:\n            return audio_in_args, audio_filters, audio_out_args\n\n        for audio in audio_inputs:\n            offset_seconds = 0\n            if offset_seconds &gt; 0:\n                audio_in_args.append(\n                    \"-ss {}\".format(offset_seconds)\n                )\n\n            elif offset_seconds &lt; 0:\n                audio_in_args.append(\n                    \"-itsoffset {}\".format(abs(offset_seconds))\n                )\n\n            # Audio duration is offset from `-ss`\n            audio_duration = duration_seconds + offset_seconds\n\n            # Set audio duration\n            audio_in_args.append(\"-to {:0.10f}\".format(audio_duration))\n\n            # Ignore video data from audio input\n            audio_in_args.append(\"-vn\")\n\n            # Add audio input path\n            audio_in_args.append(\"-i {}\".format(\n                path_to_subprocess_arg(audio[\"filename\"])\n            ))\n\n        # NOTE: These were changed from input to output arguments.\n        # NOTE: value in \"-ac\" was hardcoded to 2, changed to audio inputs len.\n        # Need to merge audio if there are more than 1 input.\n        if len(audio_inputs) &gt; 1:\n            audio_out_args.append(\"-filter_complex amerge\")\n            audio_out_args.append(\"-ac {}\".format(len(audio_inputs)))\n\n        return audio_in_args, audio_filters, audio_out_args\n\n    def get_letterbox_filters(\n        self,\n        letter_box_def,\n        output_width,\n        output_height\n    ):\n        output = []\n\n        ratio = letter_box_def[\"ratio\"]\n        fill_color = letter_box_def[\"fill_color\"]\n        f_red, f_green, f_blue, f_alpha = fill_color\n        fill_color_hex = \"{0:0&gt;2X}{1:0&gt;2X}{2:0&gt;2X}\".format(\n            f_red, f_green, f_blue\n        )\n        fill_color_alpha = f_alpha\n\n        line_thickness = letter_box_def[\"line_thickness\"]\n        line_color = letter_box_def[\"line_color\"]\n        l_red, l_green, l_blue, l_alpha = line_color\n        line_color_hex = \"{0:0&gt;2X}{1:0&gt;2X}{2:0&gt;2X}\".format(\n            l_red, l_green, l_blue\n        )\n        line_color_alpha = l_alpha\n\n        # test ratios and define if pillar or letter boxes\n        output_ratio = float(output_width) / float(output_height)\n        self.log.debug(\"Output ratio: {} LetterBox ratio: {}\".format(\n            output_ratio, ratio\n        ))\n        pillar = output_ratio &gt; ratio\n        need_mask = format(output_ratio, \".3f\") != format(ratio, \".3f\")\n        if not need_mask:\n            return []\n\n        if not pillar:\n            if fill_color_alpha &gt; 0:\n                top_box = (\n                    \"drawbox=0:0:{width}\"\n                    \":round(({height}-({width}/{ratio}))/2)\"\n                    \":t=fill:c={color}@{alpha}\"\n                ).format(\n                    width=output_width,\n                    height=output_height,\n                    ratio=ratio,\n                    color=fill_color_hex,\n                    alpha=fill_color_alpha\n                )\n\n                bottom_box = (\n                    \"drawbox=0\"\n                    \":{height}-round(({height}-({width}/{ratio}))/2)\"\n                    \":{width}\"\n                    \":round(({height}-({width}/{ratio}))/2)\"\n                    \":t=fill:c={color}@{alpha}\"\n                ).format(\n                    width=output_width,\n                    height=output_height,\n                    ratio=ratio,\n                    color=fill_color_hex,\n                    alpha=fill_color_alpha\n                )\n                output.extend([top_box, bottom_box])\n\n            if line_color_alpha &gt; 0 and line_thickness &gt; 0:\n                top_line = (\n                    \"drawbox=0\"\n                    \":round(({height}-({width}/{ratio}))/2)-{l_thick}\"\n                    \":{width}:{l_thick}:t=fill:c={l_color}@{l_alpha}\"\n                ).format(\n                    width=output_width,\n                    height=output_height,\n                    ratio=ratio,\n                    l_thick=line_thickness,\n                    l_color=line_color_hex,\n                    l_alpha=line_color_alpha\n                )\n                bottom_line = (\n                    \"drawbox=0\"\n                    \":{height}-round(({height}-({width}/{ratio}))/2)\"\n                    \":{width}:{l_thick}:t=fill:c={l_color}@{l_alpha}\"\n                ).format(\n                    width=output_width,\n                    height=output_height,\n                    ratio=ratio,\n                    l_thick=line_thickness,\n                    l_color=line_color_hex,\n                    l_alpha=line_color_alpha\n                )\n                output.extend([top_line, bottom_line])\n\n        else:\n            if fill_color_alpha &gt; 0:\n                left_box = (\n                    \"drawbox=0:0\"\n                    \":round(({width}-({height}*{ratio}))/2)\"\n                    \":{height}\"\n                    \":t=fill:c={color}@{alpha}\"\n                ).format(\n                    width=output_width,\n                    height=output_height,\n                    ratio=ratio,\n                    color=fill_color_hex,\n                    alpha=fill_color_alpha\n                )\n\n                right_box = (\n                    \"drawbox=\"\n                    \"{width}-round(({width}-({height}*{ratio}))/2)\"\n                    \":0\"\n                    \":round(({width}-({height}*{ratio}))/2)\"\n                    \":{height}\"\n                    \":t=fill:c={color}@{alpha}\"\n                ).format(\n                    width=output_width,\n                    height=output_height,\n                    ratio=ratio,\n                    color=fill_color_hex,\n                    alpha=fill_color_alpha\n                )\n                output.extend([left_box, right_box])\n\n            if line_color_alpha &gt; 0 and line_thickness &gt; 0:\n                left_line = (\n                    \"drawbox=round(({width}-({height}*{ratio}))/2)\"\n                    \":0:{l_thick}:{height}:t=fill:c={l_color}@{l_alpha}\"\n                ).format(\n                    width=output_width,\n                    height=output_height,\n                    ratio=ratio,\n                    l_thick=line_thickness,\n                    l_color=line_color_hex,\n                    l_alpha=line_color_alpha\n                )\n\n                right_line = (\n                    \"drawbox={width}-round(({width}-({height}*{ratio}))/2)\"\n                    \":0:{l_thick}:{height}:t=fill:c={l_color}@{l_alpha}\"\n                ).format(\n                    width=output_width,\n                    height=output_height,\n                    ratio=ratio,\n                    l_thick=line_thickness,\n                    l_color=line_color_hex,\n                    l_alpha=line_color_alpha\n                )\n                output.extend([left_line, right_line])\n\n        return output\n\n    def rescaling_filters(self, temp_data: TempData, output_def, new_repre):\n        \"\"\"Prepare vieo filters based on tags in new representation.\n\n        It is possible to add letterboxes to output video or rescale to\n        different resolution.\n\n        During this preparation \"resolutionWidth\" and \"resolutionHeight\" are\n        set to new representation.\n        \"\"\"\n        filters = []\n\n        # if reformat input video file is already reforamted from upstream\n        reformat_in_baking = (\n            \"reformatted\" in new_repre[\"tags\"]\n            # Backwards compatibility\n            or \"reformated\" in new_repre[\"tags\"]\n        )\n        self.log.debug(\"reformat_in_baking: `{}`\".format(reformat_in_baking))\n\n        # NOTE Skipped using instance's resolution\n        full_input_path_single_file = temp_data.full_input_path_single_file\n        try:\n            streams = get_ffprobe_streams(\n                full_input_path_single_file, self.log\n            )\n        except Exception as exc:\n            raise AssertionError((\n                \"FFprobe couldn't read information about input file: \\\"{}\\\".\"\n                \" Error message: {}\"\n            ).format(full_input_path_single_file, str(exc)))\n\n        # Try to find first stream with defined 'width' and 'height'\n        # - this is to avoid order of streams where audio can be as first\n        # - there may be a better way (checking `codec_type`?)\n        input_width = None\n        input_height = None\n        output_width = None\n        output_height = None\n        for stream in streams:\n            if \"width\" in stream and \"height\" in stream:\n                input_width = int(stream[\"width\"])\n                input_height = int(stream[\"height\"])\n                break\n\n        # Get instance data\n        pixel_aspect = temp_data.pixel_aspect\n        if reformat_in_baking:\n            self.log.debug((\n                \"Using resolution from input. It is already \"\n                \"reformatted from upstream process\"\n            ))\n            pixel_aspect = 1\n            output_width = input_width\n            output_height = input_height\n\n        # Raise exception of any stream didn't define input resolution\n        if input_width is None:\n            raise AssertionError((\n                \"FFprobe couldn't read resolution from input file: \\\"{}\\\"\"\n            ).format(full_input_path_single_file))\n\n        # collect source values to be potentially used in burnins later\n        if \"source_resolution_width\" not in new_repre:\n            new_repre[\"source_resolution_width\"] = input_width\n        if \"source_resolution_height\" not in new_repre:\n            new_repre[\"source_resolution_height\"] = input_height\n\n        # NOTE Setting only one of `width` or `height` is not allowed\n        # - settings value can't have None but has value of 0\n        output_width = output_def[\"width\"] or output_width or None\n        output_height = output_def[\"height\"] or output_height or None\n\n        # Force to use input resolution if output resolution was not defined\n        #   in settings. Resolution from instance is not used when\n        #   'use_input_res' is set to 'True'.\n        use_input_res = False\n\n        # Overscan color\n        overscan_color_value = \"black\"\n        overscan_color = output_def.get(\"overscan_color\")\n        if overscan_color:\n            if len(overscan_color) == 3:\n                bg_red, bg_green, bg_blue = overscan_color\n            else:\n                # Backwards compatibility\n                bg_red, bg_green, bg_blue, _ = overscan_color\n\n            overscan_color_value = \"#{0:0&gt;2X}{1:0&gt;2X}{2:0&gt;2X}\".format(\n                bg_red, bg_green, bg_blue\n            )\n        self.log.debug(\"Overscan color: `{}`\".format(overscan_color_value))\n\n        # Scale input to have proper pixel aspect ratio\n        # - scale width by the pixel aspect ratio\n        scale_pixel_aspect = output_def.get(\"scale_pixel_aspect\", True)\n        if scale_pixel_aspect and pixel_aspect != 1:\n            # Change input width after pixel aspect\n            input_width = int(input_width * pixel_aspect)\n            use_input_res = True\n            filters.append((\n                \"scale={}x{}:flags=lanczos\".format(input_width, input_height)\n            ))\n\n        # Convert overscan value video filters\n        overscan_crop = output_def.get(\"overscan_crop\")\n        overscan = OverscanCrop(\n            input_width, input_height, overscan_crop, overscan_color_value\n        )\n        overscan_crop_filters = overscan.video_filters()\n        # Add overscan filters to filters if are any and modify input\n        #   resolution by it's values\n        if overscan_crop_filters:\n            filters.extend(overscan_crop_filters)\n            # Change input resolution after overscan crop\n            input_width = overscan.width()\n            input_height = overscan.height()\n            use_input_res = True\n\n        # Make sure input width and height is not an odd number\n        input_width_is_odd = bool(input_width % 2 != 0)\n        input_height_is_odd = bool(input_height % 2 != 0)\n        if input_width_is_odd or input_height_is_odd:\n            # Add padding to input and make sure this filter is at first place\n            filters.append(\"pad=width=ceil(iw/2)*2:height=ceil(ih/2)*2\")\n\n            # Change input width or height as first filter will change them\n            if input_width_is_odd:\n                self.log.info((\n                    \"Converting input width from odd to even number. {} -&gt; {}\"\n                ).format(input_width, input_width + 1))\n                input_width += 1\n\n            if input_height_is_odd:\n                self.log.info((\n                    \"Converting input height from odd to even number. {} -&gt; {}\"\n                ).format(input_height, input_height + 1))\n                input_height += 1\n\n        self.log.debug(\"pixel_aspect: `{}`\".format(pixel_aspect))\n        self.log.debug(\"input_width: `{}`\".format(input_width))\n        self.log.debug(\"input_height: `{}`\".format(input_height))\n\n        # Use instance resolution if output definition has not set it\n        #   - use instance resolution only if there were not scale changes\n        #       that may massivelly affect output 'use_input_res'\n        if not use_input_res and output_width is None or output_height is None:\n            output_width = temp_data.resolution_width\n            output_height = temp_data.resolution_height\n\n        # Use source's input resolution instance does not have set it.\n        if output_width is None or output_height is None:\n            self.log.debug(\"Using resolution from input.\")\n            output_width = input_width\n            output_height = input_height\n\n        output_width = int(output_width)\n        output_height = int(output_height)\n\n        # Make sure output width and height is not an odd number\n        # When this can happen:\n        # - if output definition has set width and height with odd number\n        # - `instance.data` contain width and height with odd number\n        if output_width % 2 != 0:\n            self.log.warning((\n                \"Converting output width from odd to even number. {} -&gt; {}\"\n            ).format(output_width, output_width + 1))\n            output_width += 1\n\n        if output_height % 2 != 0:\n            self.log.warning((\n                \"Converting output height from odd to even number. {} -&gt; {}\"\n            ).format(output_height, output_height + 1))\n            output_height += 1\n\n        self.log.debug(\n            \"Output resolution is {}x{}\".format(output_width, output_height)\n        )\n\n        letter_box_def = output_def[\"letter_box\"]\n        letter_box_enabled = letter_box_def[\"enabled\"]\n\n        # Skip processing if resolution is same as input's and letterbox is\n        # not set\n        if (\n            output_width == input_width\n            and output_height == input_height\n            and not letter_box_enabled\n        ):\n            self.log.debug(\n                \"Output resolution is same as input's\"\n                \" and \\\"letter_box\\\" key is not set. Skipping reformat part.\"\n            )\n            new_repre[\"resolutionWidth\"] = input_width\n            new_repre[\"resolutionHeight\"] = input_height\n            return filters\n\n        # scaling none square pixels and 1920 width\n        if input_height != output_height or input_width != output_width:\n            filters.extend([\n                (\n                    \"scale={}x{}\"\n                    \":flags=lanczos\"\n                    \":force_original_aspect_ratio=decrease\"\n                ).format(output_width, output_height),\n                \"pad={}:{}:(ow-iw)/2:(oh-ih)/2:{}\".format(\n                    output_width, output_height,\n                    overscan_color_value\n                ),\n                \"setsar=1\"\n            ])\n\n        # letter_box\n        if letter_box_enabled:\n            filters.extend(\n                self.get_letterbox_filters(\n                    letter_box_def,\n                    output_width,\n                    output_height\n                )\n            )\n\n        new_repre[\"resolutionWidth\"] = output_width\n        new_repre[\"resolutionHeight\"] = output_height\n\n        return filters\n\n    def lut_filters(self, new_repre, instance, input_args):\n        \"\"\"Add lut file to output ffmpeg filters.\"\"\"\n        filters = []\n        # baking lut file application\n        lut_path = instance.data.get(\"lutPath\")\n        if not lut_path or \"bake-lut\" not in new_repre[\"tags\"]:\n            return filters\n\n        # Prepare path for ffmpeg argument\n        lut_path = lut_path.replace(\"\\\\\", \"/\").replace(\":\", \"\\\\:\")\n\n        # Remove gamma from input arguments\n        if \"-gamma\" in input_args:\n            input_args.remove(\"-gamme\")\n\n        # Prepare filters\n        filters.append(\"lut3d=file='{}'\".format(lut_path))\n        # QUESTION hardcoded colormatrix?\n        filters.append(\"colormatrix=bt601:bt709\")\n\n        self.log.info(\"Added Lut to ffmpeg command.\")\n\n        return filters\n\n    def families_from_instance(self, instance):\n        \"\"\"Returns all families of entered instance.\"\"\"\n        families = []\n        family = instance.data.get(\"family\")\n        if family:\n            families.append(family)\n\n        for family in (instance.data.get(\"families\") or tuple()):\n            if family not in families:\n                families.append(family)\n        return families\n\n    def families_filter_validation(self, families, output_families_filter):\n        \"\"\"Determines if entered families intersect with families filters.\n\n        All family values are lowered to avoid unexpected results.\n        \"\"\"\n\n        families_filter_lower = set(family.lower() for family in\n                                    output_families_filter\n                                    # Exclude empty filter values\n                                    if family)\n        if not families_filter_lower:\n            return True\n        return any(family.lower() in families_filter_lower\n                   for family in families)\n\n    def filter_output_defs(self, profile, product_name, families):\n        \"\"\"Return outputs matching input instance families.\n\n        Output definitions without families filter are marked as valid.\n\n        Args:\n            profile (dict): Profile from presets matching current context.\n            families (list): All families of current instance.\n            product_name (str): Product name.\n\n        Returns:\n            dict[str, Any]: Containing all output definitions matching entered\n                families.\n        \"\"\"\n        filtered_outputs = {}\n        outputs = profile.get(\"outputs\")\n        if not outputs:\n            return filtered_outputs\n\n        for output_def in outputs:\n            filename_suffix = output_def[\"name\"]\n            output_filters = output_def.get(\"filter\")\n            # If no filter on output preset, skip filtering and add output\n            # profile for farther processing\n            if not output_filters:\n                filtered_outputs[filename_suffix] = output_def\n                continue\n\n            families_filters = output_filters.get(\"families\")\n            if not self.families_filter_validation(families, families_filters):\n                continue\n\n            # Subsets name filters\n            product_name_filters = [\n                name_filter\n                for name_filter in output_filters.get(\"product_names\", [])\n                # Skip empty strings\n                if name_filter\n            ]\n            if product_name and product_name_filters:\n                match = False\n                for product_name_filter in product_name_filters:\n                    compiled = re.compile(product_name_filter)\n                    if compiled.search(product_name):\n                        match = True\n                        break\n\n                if not match:\n                    continue\n\n            filtered_outputs[filename_suffix] = output_def\n\n        return filtered_outputs\n\n    def filter_outputs_by_custom_tags(self, outputs, custom_tags):\n        \"\"\"Filter output definitions by entered representation custom_tags.\n\n        Output definitions without custom_tags filter are marked as invalid,\n        only in case representation is having any custom_tags defined.\n\n        Args:\n            outputs (list): Contain list of output definitions from presets.\n            custom_tags (list): Custom Tags of processed representation.\n\n        Returns:\n            list: Containing all output definitions matching entered tags.\n        \"\"\"\n\n        filtered_outputs = []\n        repre_c_tags_low = [tag.lower() for tag in (custom_tags or [])]\n        for output_def in outputs:\n            tag_filters = output_def.get(\"filter\", {}).get(\"custom_tags\")\n\n            if not custom_tags and not tag_filters:\n                # Definition is valid if both tags are empty\n                valid = True\n\n            elif not custom_tags or not tag_filters:\n                # Invalid if one is empty\n                valid = False\n\n            else:\n                # Check if output definition tags are in representation tags\n                valid = False\n                # lower all filter tags\n                tag_filters_low = [tag.lower() for tag in tag_filters]\n                # check if any repre tag is not in filter tags\n                for tag in repre_c_tags_low:\n                    if tag in tag_filters_low:\n                        valid = True\n                        break\n\n            if valid:\n                filtered_outputs.append(output_def)\n\n        self.log.debug(\"__ filtered_outputs: {}\".format(\n            [_o[\"filename_suffix\"] for _o in filtered_outputs]\n        ))\n\n        return filtered_outputs\n\n    def add_video_filter_args(self, args, inserting_arg):\n        \"\"\"\n        Fixing video filter arguments to be one long string\n\n        Args:\n            args (list): list of string arguments\n            inserting_arg (str): string argument we want to add\n                                 (without flag `-vf`)\n\n        Returns:\n            str: long joined argument to be added back to list of arguments\n\n        \"\"\"\n        # find all video format settings\n        vf_settings = [p for p in args\n                       for v in [\"-filter:v\", \"-vf\"]\n                       if v in p]\n        self.log.debug(\"_ vf_settings: `{}`\".format(vf_settings))\n\n        # remove them from output args list\n        for p in vf_settings:\n            self.log.debug(\"_ remove p: `{}`\".format(p))\n            args.remove(p)\n            self.log.debug(\"_ args: `{}`\".format(args))\n\n        # strip them from all flags\n        vf_fixed = [p.replace(\"-vf \", \"\").replace(\"-filter:v \", \"\")\n                    for p in vf_settings]\n\n        self.log.debug(\"_ vf_fixed: `{}`\".format(vf_fixed))\n        vf_fixed.insert(0, inserting_arg)\n        self.log.debug(\"_ vf_fixed: `{}`\".format(vf_fixed))\n        # create new video filter setting\n        vf_back = \"-vf \" + \",\".join(vf_fixed)\n\n        return vf_back\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_review.html#client.ayon_core.plugins.publish.extract_review.ExtractReview.add_video_filter_args","title":"<code>add_video_filter_args(args, inserting_arg)</code>","text":"<p>Fixing video filter arguments to be one long string</p> <p>Parameters:</p> Name Type Description Default <code>args</code> <code>list</code> <p>list of string arguments</p> required <code>inserting_arg</code> <code>str</code> <p>string argument we want to add                  (without flag <code>-vf</code>)</p> required <p>Returns:</p> Name Type Description <code>str</code> <p>long joined argument to be added back to list of arguments</p> Source code in <code>client/ayon_core/plugins/publish/extract_review.py</code> <pre><code>def add_video_filter_args(self, args, inserting_arg):\n    \"\"\"\n    Fixing video filter arguments to be one long string\n\n    Args:\n        args (list): list of string arguments\n        inserting_arg (str): string argument we want to add\n                             (without flag `-vf`)\n\n    Returns:\n        str: long joined argument to be added back to list of arguments\n\n    \"\"\"\n    # find all video format settings\n    vf_settings = [p for p in args\n                   for v in [\"-filter:v\", \"-vf\"]\n                   if v in p]\n    self.log.debug(\"_ vf_settings: `{}`\".format(vf_settings))\n\n    # remove them from output args list\n    for p in vf_settings:\n        self.log.debug(\"_ remove p: `{}`\".format(p))\n        args.remove(p)\n        self.log.debug(\"_ args: `{}`\".format(args))\n\n    # strip them from all flags\n    vf_fixed = [p.replace(\"-vf \", \"\").replace(\"-filter:v \", \"\")\n                for p in vf_settings]\n\n    self.log.debug(\"_ vf_fixed: `{}`\".format(vf_fixed))\n    vf_fixed.insert(0, inserting_arg)\n    self.log.debug(\"_ vf_fixed: `{}`\".format(vf_fixed))\n    # create new video filter setting\n    vf_back = \"-vf \" + \",\".join(vf_fixed)\n\n    return vf_back\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_review.html#client.ayon_core.plugins.publish.extract_review.ExtractReview.audio_args","title":"<code>audio_args(instance, temp_data, duration_seconds)</code>","text":"<p>Prepares FFMpeg arguments for audio inputs.</p> Source code in <code>client/ayon_core/plugins/publish/extract_review.py</code> <pre><code>def audio_args(self, instance, temp_data: TempData, duration_seconds):\n    \"\"\"Prepares FFMpeg arguments for audio inputs.\"\"\"\n    audio_in_args = []\n    audio_filters = []\n    audio_out_args = []\n    audio_inputs = instance.data.get(\"audio\")\n    if not audio_inputs:\n        return audio_in_args, audio_filters, audio_out_args\n\n    for audio in audio_inputs:\n        offset_seconds = 0\n        if offset_seconds &gt; 0:\n            audio_in_args.append(\n                \"-ss {}\".format(offset_seconds)\n            )\n\n        elif offset_seconds &lt; 0:\n            audio_in_args.append(\n                \"-itsoffset {}\".format(abs(offset_seconds))\n            )\n\n        # Audio duration is offset from `-ss`\n        audio_duration = duration_seconds + offset_seconds\n\n        # Set audio duration\n        audio_in_args.append(\"-to {:0.10f}\".format(audio_duration))\n\n        # Ignore video data from audio input\n        audio_in_args.append(\"-vn\")\n\n        # Add audio input path\n        audio_in_args.append(\"-i {}\".format(\n            path_to_subprocess_arg(audio[\"filename\"])\n        ))\n\n    # NOTE: These were changed from input to output arguments.\n    # NOTE: value in \"-ac\" was hardcoded to 2, changed to audio inputs len.\n    # Need to merge audio if there are more than 1 input.\n    if len(audio_inputs) &gt; 1:\n        audio_out_args.append(\"-filter_complex amerge\")\n        audio_out_args.append(\"-ac {}\".format(len(audio_inputs)))\n\n    return audio_in_args, audio_filters, audio_out_args\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_review.html#client.ayon_core.plugins.publish.extract_review.ExtractReview.families_filter_validation","title":"<code>families_filter_validation(families, output_families_filter)</code>","text":"<p>Determines if entered families intersect with families filters.</p> <p>All family values are lowered to avoid unexpected results.</p> Source code in <code>client/ayon_core/plugins/publish/extract_review.py</code> <pre><code>def families_filter_validation(self, families, output_families_filter):\n    \"\"\"Determines if entered families intersect with families filters.\n\n    All family values are lowered to avoid unexpected results.\n    \"\"\"\n\n    families_filter_lower = set(family.lower() for family in\n                                output_families_filter\n                                # Exclude empty filter values\n                                if family)\n    if not families_filter_lower:\n        return True\n    return any(family.lower() in families_filter_lower\n               for family in families)\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_review.html#client.ayon_core.plugins.publish.extract_review.ExtractReview.families_from_instance","title":"<code>families_from_instance(instance)</code>","text":"<p>Returns all families of entered instance.</p> Source code in <code>client/ayon_core/plugins/publish/extract_review.py</code> <pre><code>def families_from_instance(self, instance):\n    \"\"\"Returns all families of entered instance.\"\"\"\n    families = []\n    family = instance.data.get(\"family\")\n    if family:\n        families.append(family)\n\n    for family in (instance.data.get(\"families\") or tuple()):\n        if family not in families:\n            families.append(family)\n    return families\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_review.html#client.ayon_core.plugins.publish.extract_review.ExtractReview.ffmpeg_full_args","title":"<code>ffmpeg_full_args(input_args, video_filters, audio_filters, output_args)</code>","text":"<p>Post processing of collected FFmpeg arguments.</p> <p>Just verify that output arguments does not contain video or audio filters which may cause issues because of duplicated argument entry. Filters found in output arguments are moved to list they belong to.</p> <p>Parameters:</p> Name Type Description Default <code>input_args</code> <code>list</code> <p>All collected ffmpeg arguments with inputs.</p> required <code>video_filters</code> <code>list</code> <p>All collected video filters.</p> required <code>audio_filters</code> <code>list</code> <p>All collected audio filters.</p> required <code>output_args</code> <code>list</code> <p>All collected ffmpeg output arguments with output filepath.</p> required <p>Returns:</p> Name Type Description <code>list</code> <p>Containing all arguments ready to run in subprocess.</p> Source code in <code>client/ayon_core/plugins/publish/extract_review.py</code> <pre><code>def ffmpeg_full_args(\n    self, input_args, video_filters, audio_filters, output_args\n):\n    \"\"\"Post processing of collected FFmpeg arguments.\n\n    Just verify that output arguments does not contain video or audio\n    filters which may cause issues because of duplicated argument entry.\n    Filters found in output arguments are moved to list they belong to.\n\n    Args:\n        input_args (list): All collected ffmpeg arguments with inputs.\n        video_filters (list): All collected video filters.\n        audio_filters (list): All collected audio filters.\n        output_args (list): All collected ffmpeg output arguments with\n            output filepath.\n\n    Returns:\n        list: Containing all arguments ready to run in subprocess.\n    \"\"\"\n    output_args = self.split_ffmpeg_args(output_args)\n\n    video_args_dentifiers = [\"-vf\", \"-filter:v\"]\n    audio_args_dentifiers = [\"-af\", \"-filter:a\"]\n    for arg in tuple(output_args):\n        for identifier in video_args_dentifiers:\n            if arg.startswith(\"{} \".format(identifier)):\n                output_args.remove(arg)\n                arg = arg.replace(identifier, \"\").strip()\n                video_filters.append(arg)\n\n        for identifier in audio_args_dentifiers:\n            if arg.startswith(\"{} \".format(identifier)):\n                output_args.remove(arg)\n                arg = arg.replace(identifier, \"\").strip()\n                audio_filters.append(arg)\n\n    all_args = [\n        subprocess.list2cmdline(get_ffmpeg_tool_args(\"ffmpeg\"))\n    ]\n    all_args.extend(input_args)\n    if video_filters:\n        all_args.append(\"-filter:v\")\n        all_args.append(\"\\\"{}\\\"\".format(\",\".join(video_filters)))\n\n    if audio_filters:\n        all_args.append(\"-filter:a\")\n        all_args.append(\"\\\"{}\\\"\".format(\",\".join(audio_filters)))\n\n    all_args.extend(output_args)\n\n    return all_args\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_review.html#client.ayon_core.plugins.publish.extract_review.ExtractReview.fill_sequence_gaps_from_existing","title":"<code>fill_sequence_gaps_from_existing(collection, staging_dir, start_frame, end_frame)</code>","text":"<p>Fill missing files in sequence by duplicating existing ones.</p> <p>This will take nearest frame file and copy it with so as to fill gaps in sequence. Last existing file there is is used to for the hole ahead.</p> <p>Parameters:</p> Name Type Description Default <code>staging_dir</code> <code>str</code> <p>Path to staging directory.</p> required <code>start_frame</code> <code>int</code> <p>Sequence start (no matter what files are there)</p> required <code>end_frame</code> <code>int</code> <p>Sequence end (no matter what files are there)</p> required <p>Returns:</p> Type Description <code>dict[int, str]</code> <p>dict[int, str] of added files. Those should be cleaned after work is done.</p> <p>Raises:</p> Type Description <code>PublishError</code> <p>if more than one collection is obtained.</p> Source code in <code>client/ayon_core/plugins/publish/extract_review.py</code> <pre><code>def fill_sequence_gaps_from_existing(\n    self,\n    collection,\n    staging_dir: str,\n    start_frame: int,\n    end_frame: int\n) -&gt; dict[int, str]:\n    \"\"\"Fill missing files in sequence by duplicating existing ones.\n\n    This will take nearest frame file and copy it with so as to fill\n    gaps in sequence. Last existing file there is is used to for the\n    hole ahead.\n\n    Args:\n        collection (clique.collection)\n        staging_dir (str): Path to staging directory.\n        start_frame (int): Sequence start (no matter what files are there)\n        end_frame (int): Sequence end (no matter what files are there)\n\n    Returns:\n        dict[int, str] of added files. Those should be cleaned after work\n            is done.\n\n    Raises:\n        PublishError: if more than one collection is obtained.\n\n    \"\"\"\n    # Prepare which hole is filled with what frame\n    #   - the frame is filled only with already existing frames\n    prev_frame = next(iter(collection.indexes))\n    hole_frame_to_nearest = {}\n    for frame in range(int(start_frame), int(end_frame) + 1):\n        if frame in collection.indexes:\n            prev_frame = frame\n        else:\n            # Use previous frame as source for hole\n            hole_frame_to_nearest[frame] = prev_frame\n\n    # Calculate paths\n    added_files = {}\n    col_format = collection.format(\"{head}{padding}{tail}\")\n    for hole_frame, src_frame in hole_frame_to_nearest.items():\n        hole_fpath = os.path.join(staging_dir, col_format % hole_frame)\n        src_fpath = os.path.join(staging_dir, col_format % src_frame)\n        if not os.path.isfile(src_fpath):\n            raise PublishError(\n                f\"Missing previously detected file: {src_fpath}\"\n            )\n\n        copyfile(src_fpath, hole_fpath)\n        added_files[hole_frame] = hole_fpath\n\n    return added_files\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_review.html#client.ayon_core.plugins.publish.extract_review.ExtractReview.fill_sequence_gaps_with_blanks","title":"<code>fill_sequence_gaps_with_blanks(collection, staging_dir, start_frame, end_frame, resolution_width, resolution_height, extension, temp_data)</code>","text":"<p>Fills missing files by blank frame.</p> Source code in <code>client/ayon_core/plugins/publish/extract_review.py</code> <pre><code>def fill_sequence_gaps_with_blanks(\n    self,\n    collection: str,\n    staging_dir: str,\n    start_frame: int,\n    end_frame: int,\n    resolution_width: int,\n    resolution_height: int,\n    extension: str,\n    temp_data: TempData\n) -&gt; Optional[dict[int, str]]:\n    \"\"\"Fills missing files by blank frame.\"\"\"\n\n    blank_frame_path = None\n\n    added_files = {}\n\n    col_format = collection.format(\"{head}{padding}{tail}\")\n    for frame in range(start_frame, end_frame + 1):\n        if frame in collection.indexes:\n            continue\n        hole_fpath = os.path.join(staging_dir, col_format % frame)\n        if blank_frame_path is None:\n            blank_frame_path = self._create_blank_frame(\n                staging_dir, extension, resolution_width, resolution_height\n            )\n            temp_data.paths_to_remove.append(blank_frame_path)\n        copyfile(blank_frame_path, hole_fpath)\n        added_files[frame] = hole_fpath\n\n    return added_files\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_review.html#client.ayon_core.plugins.publish.extract_review.ExtractReview.filter_output_defs","title":"<code>filter_output_defs(profile, product_name, families)</code>","text":"<p>Return outputs matching input instance families.</p> <p>Output definitions without families filter are marked as valid.</p> <p>Parameters:</p> Name Type Description Default <code>profile</code> <code>dict</code> <p>Profile from presets matching current context.</p> required <code>families</code> <code>list</code> <p>All families of current instance.</p> required <code>product_name</code> <code>str</code> <p>Product name.</p> required <p>Returns:</p> Type Description <p>dict[str, Any]: Containing all output definitions matching entered families.</p> Source code in <code>client/ayon_core/plugins/publish/extract_review.py</code> <pre><code>def filter_output_defs(self, profile, product_name, families):\n    \"\"\"Return outputs matching input instance families.\n\n    Output definitions without families filter are marked as valid.\n\n    Args:\n        profile (dict): Profile from presets matching current context.\n        families (list): All families of current instance.\n        product_name (str): Product name.\n\n    Returns:\n        dict[str, Any]: Containing all output definitions matching entered\n            families.\n    \"\"\"\n    filtered_outputs = {}\n    outputs = profile.get(\"outputs\")\n    if not outputs:\n        return filtered_outputs\n\n    for output_def in outputs:\n        filename_suffix = output_def[\"name\"]\n        output_filters = output_def.get(\"filter\")\n        # If no filter on output preset, skip filtering and add output\n        # profile for farther processing\n        if not output_filters:\n            filtered_outputs[filename_suffix] = output_def\n            continue\n\n        families_filters = output_filters.get(\"families\")\n        if not self.families_filter_validation(families, families_filters):\n            continue\n\n        # Subsets name filters\n        product_name_filters = [\n            name_filter\n            for name_filter in output_filters.get(\"product_names\", [])\n            # Skip empty strings\n            if name_filter\n        ]\n        if product_name and product_name_filters:\n            match = False\n            for product_name_filter in product_name_filters:\n                compiled = re.compile(product_name_filter)\n                if compiled.search(product_name):\n                    match = True\n                    break\n\n            if not match:\n                continue\n\n        filtered_outputs[filename_suffix] = output_def\n\n    return filtered_outputs\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_review.html#client.ayon_core.plugins.publish.extract_review.ExtractReview.filter_outputs_by_custom_tags","title":"<code>filter_outputs_by_custom_tags(outputs, custom_tags)</code>","text":"<p>Filter output definitions by entered representation custom_tags.</p> <p>Output definitions without custom_tags filter are marked as invalid, only in case representation is having any custom_tags defined.</p> <p>Parameters:</p> Name Type Description Default <code>outputs</code> <code>list</code> <p>Contain list of output definitions from presets.</p> required <code>custom_tags</code> <code>list</code> <p>Custom Tags of processed representation.</p> required <p>Returns:</p> Name Type Description <code>list</code> <p>Containing all output definitions matching entered tags.</p> Source code in <code>client/ayon_core/plugins/publish/extract_review.py</code> <pre><code>def filter_outputs_by_custom_tags(self, outputs, custom_tags):\n    \"\"\"Filter output definitions by entered representation custom_tags.\n\n    Output definitions without custom_tags filter are marked as invalid,\n    only in case representation is having any custom_tags defined.\n\n    Args:\n        outputs (list): Contain list of output definitions from presets.\n        custom_tags (list): Custom Tags of processed representation.\n\n    Returns:\n        list: Containing all output definitions matching entered tags.\n    \"\"\"\n\n    filtered_outputs = []\n    repre_c_tags_low = [tag.lower() for tag in (custom_tags or [])]\n    for output_def in outputs:\n        tag_filters = output_def.get(\"filter\", {}).get(\"custom_tags\")\n\n        if not custom_tags and not tag_filters:\n            # Definition is valid if both tags are empty\n            valid = True\n\n        elif not custom_tags or not tag_filters:\n            # Invalid if one is empty\n            valid = False\n\n        else:\n            # Check if output definition tags are in representation tags\n            valid = False\n            # lower all filter tags\n            tag_filters_low = [tag.lower() for tag in tag_filters]\n            # check if any repre tag is not in filter tags\n            for tag in repre_c_tags_low:\n                if tag in tag_filters_low:\n                    valid = True\n                    break\n\n        if valid:\n            filtered_outputs.append(output_def)\n\n    self.log.debug(\"__ filtered_outputs: {}\".format(\n        [_o[\"filename_suffix\"] for _o in filtered_outputs]\n    ))\n\n    return filtered_outputs\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_review.html#client.ayon_core.plugins.publish.extract_review.ExtractReview.input_is_sequence","title":"<code>input_is_sequence(repre)</code>","text":"<p>Deduce from representation data if input is sequence.</p> Source code in <code>client/ayon_core/plugins/publish/extract_review.py</code> <pre><code>def input_is_sequence(self, repre):\n    \"\"\"Deduce from representation data if input is sequence.\"\"\"\n    # TODO GLOBAL ISSUE - Find better way how to find out if input\n    #  is sequence. Issues (in theory):\n    #   - there may be multiple files ant not be sequence\n    #   - remainders are not checked at all\n    #   - there can be more than one collection\n    return isinstance(repre[\"files\"], (list, tuple))\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_review.html#client.ayon_core.plugins.publish.extract_review.ExtractReview.input_output_paths","title":"<code>input_output_paths(new_repre, output_def, temp_data)</code>","text":"<p>Deduce input nad output file paths based on entered data.</p> <p>Input may be sequence of images, video file or single image file and same can be said about output, this method helps to find out what their paths are.</p> <p>It is validated that output directory exist and creates if not.</p> <p>During process are set \"files\", \"stagingDir\", \"ext\" and \"sequence_file\" (if output is sequence) keys to new representation.</p> Source code in <code>client/ayon_core/plugins/publish/extract_review.py</code> <pre><code>def input_output_paths(self, new_repre, output_def, temp_data: TempData):\n    \"\"\"Deduce input nad output file paths based on entered data.\n\n    Input may be sequence of images, video file or single image file and\n    same can be said about output, this method helps to find out what\n    their paths are.\n\n    It is validated that output directory exist and creates if not.\n\n    During process are set \"files\", \"stagingDir\", \"ext\" and\n    \"sequence_file\" (if output is sequence) keys to new representation.\n    \"\"\"\n\n    repre = temp_data.origin_repre\n    src_staging_dir = repre[\"stagingDir\"]\n    dst_staging_dir = new_repre[\"stagingDir\"]\n\n    if temp_data.input_is_sequence:\n        collections = clique.assemble(repre[\"files\"])[0]\n        full_input_path = os.path.join(\n            src_staging_dir,\n            collections[0].format(\"{head}{padding}{tail}\")\n        )\n\n        filename = collections[0].format(\"{head}\")\n        if filename.endswith(\".\"):\n            filename = filename[:-1]\n\n        # Make sure to have full path to one input file\n        full_input_path_single_file = os.path.join(\n            src_staging_dir, repre[\"files\"][0]\n        )\n\n    else:\n        full_input_path = os.path.join(\n            src_staging_dir, repre[\"files\"]\n        )\n        filename = os.path.splitext(repre[\"files\"])[0]\n\n        # Make sure to have full path to one input file\n        full_input_path_single_file = full_input_path\n\n    filled_files = temp_data.filled_files\n    if filled_files:\n        first_frame, first_file = next(iter(filled_files.items()))\n        if first_file &lt; full_input_path_single_file:\n            self.log.warning(f\"Using filled frame: '{first_file}'\")\n            full_input_path_single_file = first_file\n            temp_data.first_sequence_frame = first_frame\n\n    filename_suffix = output_def[\"filename_suffix\"]\n\n    output_ext = output_def.get(\"ext\")\n    # Use input extension if output definition do not specify it\n    if output_ext is None:\n        output_ext = os.path.splitext(full_input_path)[1]\n\n    # TODO Define if extension should have dot or not\n    if output_ext.startswith(\".\"):\n        output_ext = output_ext[1:]\n\n    output_ext = output_ext.lower()\n\n    # Store extension to representation\n    new_repre[\"ext\"] = output_ext\n\n    self.log.debug(\"New representation ext: `{}`\".format(output_ext))\n\n    # Output is image file sequence with frames\n    output_ext_is_image = bool(output_ext in self.image_exts)\n    output_is_sequence = bool(\n        output_ext_is_image\n        and \"sequence\" in output_def[\"tags\"]\n    )\n    if output_is_sequence:\n        new_repre_files = []\n        frame_start = temp_data.output_frame_start\n        frame_end = temp_data.output_frame_end\n\n        filename_base = \"{}_{}\".format(filename, filename_suffix)\n        # Temporary template for frame filling. Example output:\n        # \"basename.%04d.exr\" when `frame_end` == 1001\n        repr_file = \"{}.%{:0&gt;2}d.{}\".format(\n            filename_base, len(str(frame_end)), output_ext\n        )\n\n        for frame in range(frame_start, frame_end + 1):\n            new_repre_files.append(repr_file % frame)\n\n        new_repre[\"sequence_file\"] = repr_file\n        full_output_path = os.path.join(\n            dst_staging_dir, filename_base, repr_file\n        )\n\n    else:\n        repr_file = \"{}_{}.{}\".format(\n            filename, filename_suffix, output_ext\n        )\n        full_output_path = os.path.join(dst_staging_dir, repr_file)\n        new_repre_files = repr_file\n\n    # Store files to representation\n    new_repre[\"files\"] = new_repre_files\n\n    # Make sure stagingDire exists\n    dst_staging_dir = os.path.normpath(os.path.dirname(full_output_path))\n    if not os.path.exists(dst_staging_dir):\n        self.log.debug(\"Creating dir: {}\".format(dst_staging_dir))\n        os.makedirs(dst_staging_dir)\n\n    # Store stagingDir to representation\n    new_repre[\"stagingDir\"] = dst_staging_dir\n\n    # Store paths to temp data\n    temp_data.full_input_path = full_input_path\n    temp_data.full_input_path_single_file = full_input_path_single_file\n    temp_data.full_output_path = full_output_path\n\n    # Store information about output\n    temp_data.output_ext_is_image = output_ext_is_image\n    temp_data.output_is_sequence = output_is_sequence\n\n    self.log.debug(\"Input path {}\".format(full_input_path))\n    self.log.debug(\"Output path {}\".format(full_output_path))\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_review.html#client.ayon_core.plugins.publish.extract_review.ExtractReview.lut_filters","title":"<code>lut_filters(new_repre, instance, input_args)</code>","text":"<p>Add lut file to output ffmpeg filters.</p> Source code in <code>client/ayon_core/plugins/publish/extract_review.py</code> <pre><code>def lut_filters(self, new_repre, instance, input_args):\n    \"\"\"Add lut file to output ffmpeg filters.\"\"\"\n    filters = []\n    # baking lut file application\n    lut_path = instance.data.get(\"lutPath\")\n    if not lut_path or \"bake-lut\" not in new_repre[\"tags\"]:\n        return filters\n\n    # Prepare path for ffmpeg argument\n    lut_path = lut_path.replace(\"\\\\\", \"/\").replace(\":\", \"\\\\:\")\n\n    # Remove gamma from input arguments\n    if \"-gamma\" in input_args:\n        input_args.remove(\"-gamme\")\n\n    # Prepare filters\n    filters.append(\"lut3d=file='{}'\".format(lut_path))\n    # QUESTION hardcoded colormatrix?\n    filters.append(\"colormatrix=bt601:bt709\")\n\n    self.log.info(\"Added Lut to ffmpeg command.\")\n\n    return filters\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_review.html#client.ayon_core.plugins.publish.extract_review.ExtractReview.prepare_temp_data","title":"<code>prepare_temp_data(instance, repre, output_def)</code>","text":"<p>Prepare dictionary with values used across extractor's process.</p> <p>All data are collected from instance, context, origin representation and output definition.</p> <p>There are few required keys in Instance data: \"frameStart\", \"frameEnd\" and \"fps\".</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>Instance</code> <p>Currently processed instance.</p> required <code>repre</code> <code>dict</code> <p>Representation from which new representation was copied.</p> required <code>output_def</code> <code>dict</code> <p>Definition of output of this plugin.</p> required <p>Returns:</p> Name Type Description <code>TempData</code> <code>TempData</code> <p>All data which are used across methods during process. Their values should not change during process but new keys with values may be added.</p> Source code in <code>client/ayon_core/plugins/publish/extract_review.py</code> <pre><code>def prepare_temp_data(self, instance, repre, output_def) -&gt; TempData:\n    \"\"\"Prepare dictionary with values used across extractor's process.\n\n    All data are collected from instance, context, origin representation\n    and output definition.\n\n    There are few required keys in Instance data: \"frameStart\", \"frameEnd\"\n    and \"fps\".\n\n    Args:\n        instance (Instance): Currently processed instance.\n        repre (dict): Representation from which new representation was\n            copied.\n        output_def (dict): Definition of output of this plugin.\n\n    Returns:\n        TempData: All data which are used across methods during process.\n            Their values should not change during process but new keys\n            with values may be added.\n    \"\"\"\n\n    frame_start = instance.data[\"frameStart\"]\n    frame_end = instance.data[\"frameEnd\"]\n\n    # Try to get handles from instance\n    handle_start = instance.data.get(\"handleStart\")\n    handle_end = instance.data.get(\"handleEnd\")\n    # If even one of handle values is not set on instance use\n    # handles from context\n    if handle_start is None or handle_end is None:\n        handle_start = instance.context.data[\"handleStart\"]\n        handle_end = instance.context.data[\"handleEnd\"]\n\n    frame_start_handle = frame_start - handle_start\n    frame_end_handle = frame_end + handle_end\n\n    # Change output frames when output should be without handles\n    without_handles = bool(\"no-handles\" in output_def[\"tags\"])\n    if without_handles:\n        output_frame_start = frame_start\n        output_frame_end = frame_end\n    else:\n        output_frame_start = frame_start_handle\n        output_frame_end = frame_end_handle\n\n    handles_are_set = handle_start &gt; 0 or handle_end &gt; 0\n\n    with_audio = True\n    if (\n        # Check if has `no-audio` tag\n        \"no-audio\" in output_def[\"tags\"]\n        # Check if instance has ny audio in data\n        or not instance.data.get(\"audio\")\n    ):\n        with_audio = False\n\n    input_is_sequence = self.input_is_sequence(repre)\n    input_allow_bg = False\n    first_sequence_frame = None\n\n    if input_is_sequence and repre[\"files\"]:\n        # Calculate first frame that should be used\n        cols, _ = clique.assemble(repre[\"files\"])\n        input_frames = list(sorted(cols[0].indexes))\n        first_sequence_frame = input_frames[0]\n        # WARNING: This is an issue as we don't know if first frame\n        #   is with or without handles!\n        # - handle start is added but how do not know if we should\n        output_duration = (output_frame_end - output_frame_start) + 1\n        if (\n            without_handles\n            and len(input_frames) - handle_start &gt;= output_duration\n        ):\n            first_sequence_frame += handle_start\n\n        ext = os.path.splitext(repre[\"files\"][0])[1].replace(\".\", \"\")\n        if ext.lower() in self.alpha_exts:\n            input_allow_bg = True\n    else:\n        ext = os.path.splitext(repre[\"files\"])[1].replace(\".\", \"\")\n\n    return TempData(\n        fps=float(instance.data[\"fps\"]),\n        frame_start=frame_start,\n        frame_end=frame_end,\n        handle_start=handle_start,\n        handle_end=handle_end,\n        frame_start_handle=frame_start_handle,\n        frame_end_handle=frame_end_handle,\n        output_frame_start=int(output_frame_start),\n        output_frame_end=int(output_frame_end),\n        pixel_aspect=instance.data.get(\"pixelAspect\", 1),\n        resolution_width=instance.data.get(\"resolutionWidth\"),\n        resolution_height=instance.data.get(\"resolutionHeight\"),\n        origin_repre=repre,\n        input_is_sequence=input_is_sequence,\n        first_sequence_frame=first_sequence_frame,\n        input_allow_bg=input_allow_bg,\n        with_audio=with_audio,\n        without_handles=without_handles,\n        handles_are_set=handles_are_set,\n        input_ext=ext,\n        explicit_input_paths=[],  # absolute paths to rendered files\n        paths_to_remove=[]\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_review.html#client.ayon_core.plugins.publish.extract_review.ExtractReview.rescaling_filters","title":"<code>rescaling_filters(temp_data, output_def, new_repre)</code>","text":"<p>Prepare vieo filters based on tags in new representation.</p> <p>It is possible to add letterboxes to output video or rescale to different resolution.</p> <p>During this preparation \"resolutionWidth\" and \"resolutionHeight\" are set to new representation.</p> Source code in <code>client/ayon_core/plugins/publish/extract_review.py</code> <pre><code>def rescaling_filters(self, temp_data: TempData, output_def, new_repre):\n    \"\"\"Prepare vieo filters based on tags in new representation.\n\n    It is possible to add letterboxes to output video or rescale to\n    different resolution.\n\n    During this preparation \"resolutionWidth\" and \"resolutionHeight\" are\n    set to new representation.\n    \"\"\"\n    filters = []\n\n    # if reformat input video file is already reforamted from upstream\n    reformat_in_baking = (\n        \"reformatted\" in new_repre[\"tags\"]\n        # Backwards compatibility\n        or \"reformated\" in new_repre[\"tags\"]\n    )\n    self.log.debug(\"reformat_in_baking: `{}`\".format(reformat_in_baking))\n\n    # NOTE Skipped using instance's resolution\n    full_input_path_single_file = temp_data.full_input_path_single_file\n    try:\n        streams = get_ffprobe_streams(\n            full_input_path_single_file, self.log\n        )\n    except Exception as exc:\n        raise AssertionError((\n            \"FFprobe couldn't read information about input file: \\\"{}\\\".\"\n            \" Error message: {}\"\n        ).format(full_input_path_single_file, str(exc)))\n\n    # Try to find first stream with defined 'width' and 'height'\n    # - this is to avoid order of streams where audio can be as first\n    # - there may be a better way (checking `codec_type`?)\n    input_width = None\n    input_height = None\n    output_width = None\n    output_height = None\n    for stream in streams:\n        if \"width\" in stream and \"height\" in stream:\n            input_width = int(stream[\"width\"])\n            input_height = int(stream[\"height\"])\n            break\n\n    # Get instance data\n    pixel_aspect = temp_data.pixel_aspect\n    if reformat_in_baking:\n        self.log.debug((\n            \"Using resolution from input. It is already \"\n            \"reformatted from upstream process\"\n        ))\n        pixel_aspect = 1\n        output_width = input_width\n        output_height = input_height\n\n    # Raise exception of any stream didn't define input resolution\n    if input_width is None:\n        raise AssertionError((\n            \"FFprobe couldn't read resolution from input file: \\\"{}\\\"\"\n        ).format(full_input_path_single_file))\n\n    # collect source values to be potentially used in burnins later\n    if \"source_resolution_width\" not in new_repre:\n        new_repre[\"source_resolution_width\"] = input_width\n    if \"source_resolution_height\" not in new_repre:\n        new_repre[\"source_resolution_height\"] = input_height\n\n    # NOTE Setting only one of `width` or `height` is not allowed\n    # - settings value can't have None but has value of 0\n    output_width = output_def[\"width\"] or output_width or None\n    output_height = output_def[\"height\"] or output_height or None\n\n    # Force to use input resolution if output resolution was not defined\n    #   in settings. Resolution from instance is not used when\n    #   'use_input_res' is set to 'True'.\n    use_input_res = False\n\n    # Overscan color\n    overscan_color_value = \"black\"\n    overscan_color = output_def.get(\"overscan_color\")\n    if overscan_color:\n        if len(overscan_color) == 3:\n            bg_red, bg_green, bg_blue = overscan_color\n        else:\n            # Backwards compatibility\n            bg_red, bg_green, bg_blue, _ = overscan_color\n\n        overscan_color_value = \"#{0:0&gt;2X}{1:0&gt;2X}{2:0&gt;2X}\".format(\n            bg_red, bg_green, bg_blue\n        )\n    self.log.debug(\"Overscan color: `{}`\".format(overscan_color_value))\n\n    # Scale input to have proper pixel aspect ratio\n    # - scale width by the pixel aspect ratio\n    scale_pixel_aspect = output_def.get(\"scale_pixel_aspect\", True)\n    if scale_pixel_aspect and pixel_aspect != 1:\n        # Change input width after pixel aspect\n        input_width = int(input_width * pixel_aspect)\n        use_input_res = True\n        filters.append((\n            \"scale={}x{}:flags=lanczos\".format(input_width, input_height)\n        ))\n\n    # Convert overscan value video filters\n    overscan_crop = output_def.get(\"overscan_crop\")\n    overscan = OverscanCrop(\n        input_width, input_height, overscan_crop, overscan_color_value\n    )\n    overscan_crop_filters = overscan.video_filters()\n    # Add overscan filters to filters if are any and modify input\n    #   resolution by it's values\n    if overscan_crop_filters:\n        filters.extend(overscan_crop_filters)\n        # Change input resolution after overscan crop\n        input_width = overscan.width()\n        input_height = overscan.height()\n        use_input_res = True\n\n    # Make sure input width and height is not an odd number\n    input_width_is_odd = bool(input_width % 2 != 0)\n    input_height_is_odd = bool(input_height % 2 != 0)\n    if input_width_is_odd or input_height_is_odd:\n        # Add padding to input and make sure this filter is at first place\n        filters.append(\"pad=width=ceil(iw/2)*2:height=ceil(ih/2)*2\")\n\n        # Change input width or height as first filter will change them\n        if input_width_is_odd:\n            self.log.info((\n                \"Converting input width from odd to even number. {} -&gt; {}\"\n            ).format(input_width, input_width + 1))\n            input_width += 1\n\n        if input_height_is_odd:\n            self.log.info((\n                \"Converting input height from odd to even number. {} -&gt; {}\"\n            ).format(input_height, input_height + 1))\n            input_height += 1\n\n    self.log.debug(\"pixel_aspect: `{}`\".format(pixel_aspect))\n    self.log.debug(\"input_width: `{}`\".format(input_width))\n    self.log.debug(\"input_height: `{}`\".format(input_height))\n\n    # Use instance resolution if output definition has not set it\n    #   - use instance resolution only if there were not scale changes\n    #       that may massivelly affect output 'use_input_res'\n    if not use_input_res and output_width is None or output_height is None:\n        output_width = temp_data.resolution_width\n        output_height = temp_data.resolution_height\n\n    # Use source's input resolution instance does not have set it.\n    if output_width is None or output_height is None:\n        self.log.debug(\"Using resolution from input.\")\n        output_width = input_width\n        output_height = input_height\n\n    output_width = int(output_width)\n    output_height = int(output_height)\n\n    # Make sure output width and height is not an odd number\n    # When this can happen:\n    # - if output definition has set width and height with odd number\n    # - `instance.data` contain width and height with odd number\n    if output_width % 2 != 0:\n        self.log.warning((\n            \"Converting output width from odd to even number. {} -&gt; {}\"\n        ).format(output_width, output_width + 1))\n        output_width += 1\n\n    if output_height % 2 != 0:\n        self.log.warning((\n            \"Converting output height from odd to even number. {} -&gt; {}\"\n        ).format(output_height, output_height + 1))\n        output_height += 1\n\n    self.log.debug(\n        \"Output resolution is {}x{}\".format(output_width, output_height)\n    )\n\n    letter_box_def = output_def[\"letter_box\"]\n    letter_box_enabled = letter_box_def[\"enabled\"]\n\n    # Skip processing if resolution is same as input's and letterbox is\n    # not set\n    if (\n        output_width == input_width\n        and output_height == input_height\n        and not letter_box_enabled\n    ):\n        self.log.debug(\n            \"Output resolution is same as input's\"\n            \" and \\\"letter_box\\\" key is not set. Skipping reformat part.\"\n        )\n        new_repre[\"resolutionWidth\"] = input_width\n        new_repre[\"resolutionHeight\"] = input_height\n        return filters\n\n    # scaling none square pixels and 1920 width\n    if input_height != output_height or input_width != output_width:\n        filters.extend([\n            (\n                \"scale={}x{}\"\n                \":flags=lanczos\"\n                \":force_original_aspect_ratio=decrease\"\n            ).format(output_width, output_height),\n            \"pad={}:{}:(ow-iw)/2:(oh-ih)/2:{}\".format(\n                output_width, output_height,\n                overscan_color_value\n            ),\n            \"setsar=1\"\n        ])\n\n    # letter_box\n    if letter_box_enabled:\n        filters.extend(\n            self.get_letterbox_filters(\n                letter_box_def,\n                output_width,\n                output_height\n            )\n        )\n\n    new_repre[\"resolutionWidth\"] = output_width\n    new_repre[\"resolutionHeight\"] = output_height\n\n    return filters\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_review.html#client.ayon_core.plugins.publish.extract_review.ExtractReview.split_ffmpeg_args","title":"<code>split_ffmpeg_args(in_args)</code>","text":"<p>Makes sure all entered arguments are separated in individual items.</p> <p>Split each argument string with \" -\" to identify if string contains one or more arguments.</p> Source code in <code>client/ayon_core/plugins/publish/extract_review.py</code> <pre><code>def split_ffmpeg_args(self, in_args):\n    \"\"\"Makes sure all entered arguments are separated in individual items.\n\n    Split each argument string with \" -\" to identify if string contains\n    one or more arguments.\n    \"\"\"\n    splitted_args = []\n    for arg in in_args:\n        sub_args = arg.split(\" -\")\n        if len(sub_args) == 1:\n            if arg and arg not in splitted_args:\n                splitted_args.append(arg)\n            continue\n\n        for idx, arg in enumerate(sub_args):\n            if idx != 0:\n                arg = \"-\" + arg\n\n            if arg and arg not in splitted_args:\n                splitted_args.append(arg)\n    return splitted_args\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_review.html#client.ayon_core.plugins.publish.extract_review.OverscanCrop","title":"<code>OverscanCrop</code>","text":"<p>Helper class to read overscan string and calculate output resolution.</p> <p>It is possible to enter single value for both width and height, or two values for width and height. Overscan string may have a few variants. Each variant define output size for input size.</p>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_review.html#client.ayon_core.plugins.publish.extract_review.OverscanCrop--example","title":"Example","text":"<p>For input size: 2200px</p> String Output Description \"\" 2200px Empty string does nothing. \"10%\" 220px Explicit percent size. \"-10%\" 1980px Relative percent size (decrease). \"+10%\" 2420px Relative percent size (increase). \"-10%+\" 2000px Relative percent size to output size. \"300px\" 300px Explicit output size cropped or expanded. \"-300px\" 1900px Relative pixel size (decrease). \"+300px\" 2500px Relative pixel size (increase). \"300\" 300px Value without \"%\" and \"px\" is used as has \"px\". <p>Value without sign (+/-) in is always explicit and value with sign is relative. Output size for \"200px\" and \"+200px\" are not the same. Values \"0\", \"0px\" or \"0%\" are ignored.</p> <p>All values that cause output resolution smaller than 1 pixel are invalid.</p> <p>Value \"-10%+\" is a special case which says that input's resolution is bigger by 10% than expected output.</p> <p>It is possible to combine these variants to define different output for width and height.</p> <p>Resolution: 2000px 1000px</p> String Output \"100px 120px\" 2100px 1120px \"-10% -200px\" 1800px 800px Source code in <code>client/ayon_core/plugins/publish/extract_review.py</code> <pre><code>class OverscanCrop:\n    \"\"\"Helper class to read overscan string and calculate output resolution.\n\n    It is possible to enter single value for both width and height, or\n    two values for width and height. Overscan string may have a few variants.\n    Each variant define output size for input size.\n\n    ### Example\n    For input size: 2200px\n\n    | String   | Output | Description                                     |\n    |----------|--------|-------------------------------------------------|\n    | \"\"       | 2200px | Empty string does nothing.                      |\n    | \"10%\"    | 220px  | Explicit percent size.                          |\n    | \"-10%\"   | 1980px | Relative percent size (decrease).               |\n    | \"+10%\"   | 2420px | Relative percent size (increase).               |\n    | \"-10%+\"  | 2000px | Relative percent size to output size.           |\n    | \"300px\"  | 300px  | Explicit output size cropped or expanded.       |\n    | \"-300px\" | 1900px | Relative pixel size (decrease).                 |\n    | \"+300px\" | 2500px | Relative pixel size (increase).                 |\n    | \"300\"    | 300px  | Value without \"%\" and \"px\" is used as has \"px\". |\n\n    Value without sign (+/-) in is always explicit and value with sign is\n    relative. Output size for \"200px\" and \"+200px\" are not the same.\n    Values \"0\", \"0px\" or \"0%\" are ignored.\n\n    All values that cause output resolution smaller than 1 pixel are invalid.\n\n    Value \"-10%+\" is a special case which says that input's resolution is\n    bigger by 10% than expected output.\n\n    It is possible to combine these variants to define different output for\n    width and height.\n\n    Resolution: 2000px 1000px\n\n    | String        | Output        |\n    |---------------|---------------|\n    | \"100px 120px\" | 2100px 1120px |\n    | \"-10% -200px\" | 1800px 800px  |\n    \"\"\"\n\n    item_regex = re.compile(r\"([\\+\\-])?([0-9]+)(.+)?\")\n    relative_source_regex = re.compile(r\"%([\\+\\-])\")\n\n    def __init__(\n        self, input_width, input_height, string_value, overscan_color=None\n    ):\n        # Make sure that is not None\n        string_value = string_value or \"\"\n\n        self.input_width = input_width\n        self.input_height = input_height\n        self.overscan_color = overscan_color\n\n        width, height = self._convert_string_to_values(string_value)\n        self._width_value = width\n        self._height_value = height\n\n        self._string_value = string_value\n\n    def __str__(self):\n        return \"{}\".format(self._string_value)\n\n    def __repr__(self):\n        return \"&lt;{}&gt;\".format(self.__class__.__name__)\n\n    def width(self):\n        \"\"\"Calculated width.\"\"\"\n        return self._width_value.size_for(self.input_width)\n\n    def height(self):\n        \"\"\"Calculated height.\"\"\"\n        return self._height_value.size_for(self.input_height)\n\n    def video_filters(self):\n        \"\"\"FFmpeg video filters to achieve expected result.\n\n        Filter may be empty, use \"crop\" filter, \"pad\" filter or combination of\n        \"crop\" and \"pad\".\n\n        Returns:\n            list: FFmpeg video filters.\n        \"\"\"\n        # crop=width:height:x:y - explicit start x, y position\n        # crop=width:height     - x, y are related to center by width/height\n        # pad=width:height:x:y  - explicit start x, y position\n        # pad=width:height      - x, y are set to 0 by default\n\n        width = self.width()\n        height = self.height()\n\n        output = []\n        if self.input_width == width and self.input_height == height:\n            return output\n\n        # Make sure resolution has odd numbers\n        if width % 2 == 1:\n            width -= 1\n\n        if height % 2 == 1:\n            height -= 1\n\n        if width &lt;= self.input_width and height &lt;= self.input_height:\n            output.append(\"crop={}:{}\".format(width, height))\n\n        elif width &gt;= self.input_width and height &gt;= self.input_height:\n            output.append(\n                \"pad={}:{}:(iw-ow)/2:(ih-oh)/2:{}\".format(\n                    width, height, self.overscan_color\n                )\n            )\n\n        elif width &gt; self.input_width and height &lt; self.input_height:\n            output.append(\"crop=iw:{}\".format(height))\n            output.append(\"pad={}:ih:(iw-ow)/2:(ih-oh)/2:{}\".format(\n                width, self.overscan_color\n            ))\n\n        elif width &lt; self.input_width and height &gt; self.input_height:\n            output.append(\"crop={}:ih\".format(width))\n            output.append(\"pad=iw:{}:(iw-ow)/2:(ih-oh)/2:{}\".format(\n                height, self.overscan_color\n            ))\n\n        return output\n\n    def _convert_string_to_values(self, orig_string_value):\n        string_value = orig_string_value.strip().lower()\n        if not string_value:\n            return [PixValueRelative(0), PixValueRelative(0)]\n\n        # Replace \"px\" (and spaces before) with single space\n        string_value = re.sub(r\"([ ]+)?px\", \" \", string_value)\n        string_value = re.sub(r\"([ ]+)%\", \"%\", string_value)\n        # Make sure +/- sign at the beginning of string is next to number\n        string_value = re.sub(r\"^([\\+\\-])[ ]+\", r\"\\g&lt;1&gt;\", string_value)\n        # Make sure +/- sign in the middle has zero spaces before number under\n        #   which belongs\n        string_value = re.sub(\n            r\"[ ]([\\+\\-])[ ]+([0-9])\",\n            r\" \\g&lt;1&gt;\\g&lt;2&gt;\",\n            string_value\n        )\n        string_parts = [\n            part\n            for part in string_value.split(\" \")\n            if part\n        ]\n\n        error_msg = \"Invalid string for rescaling \\\"{}\\\"\".format(\n            orig_string_value\n        )\n        if 1 &gt; len(string_parts) &gt; 2:\n            raise ValueError(error_msg)\n\n        output = []\n        for item in string_parts:\n            groups = self.item_regex.findall(item)\n            if not groups:\n                raise ValueError(error_msg)\n\n            relative_sign, value, ending = groups[0]\n            if not relative_sign:\n                if not ending:\n                    output.append(PixValueExplicit(value))\n                else:\n                    output.append(PercentValueExplicit(value))\n            else:\n                source_sign_group = self.relative_source_regex.findall(ending)\n                if not ending:\n                    output.append(PixValueRelative(int(relative_sign + value)))\n\n                elif source_sign_group:\n                    source_sign = source_sign_group[0]\n                    output.append(PercentValueRelativeSource(\n                        float(relative_sign + value), source_sign\n                    ))\n                else:\n                    output.append(\n                        PercentValueRelative(float(relative_sign + value))\n                    )\n\n        if len(output) == 1:\n            width = output.pop(0)\n            height = width.copy()\n        else:\n            width, height = output\n\n        return width, height\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_review.html#client.ayon_core.plugins.publish.extract_review.OverscanCrop.height","title":"<code>height()</code>","text":"<p>Calculated height.</p> Source code in <code>client/ayon_core/plugins/publish/extract_review.py</code> <pre><code>def height(self):\n    \"\"\"Calculated height.\"\"\"\n    return self._height_value.size_for(self.input_height)\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_review.html#client.ayon_core.plugins.publish.extract_review.OverscanCrop.video_filters","title":"<code>video_filters()</code>","text":"<p>FFmpeg video filters to achieve expected result.</p> <p>Filter may be empty, use \"crop\" filter, \"pad\" filter or combination of \"crop\" and \"pad\".</p> <p>Returns:</p> Name Type Description <code>list</code> <p>FFmpeg video filters.</p> Source code in <code>client/ayon_core/plugins/publish/extract_review.py</code> <pre><code>def video_filters(self):\n    \"\"\"FFmpeg video filters to achieve expected result.\n\n    Filter may be empty, use \"crop\" filter, \"pad\" filter or combination of\n    \"crop\" and \"pad\".\n\n    Returns:\n        list: FFmpeg video filters.\n    \"\"\"\n    # crop=width:height:x:y - explicit start x, y position\n    # crop=width:height     - x, y are related to center by width/height\n    # pad=width:height:x:y  - explicit start x, y position\n    # pad=width:height      - x, y are set to 0 by default\n\n    width = self.width()\n    height = self.height()\n\n    output = []\n    if self.input_width == width and self.input_height == height:\n        return output\n\n    # Make sure resolution has odd numbers\n    if width % 2 == 1:\n        width -= 1\n\n    if height % 2 == 1:\n        height -= 1\n\n    if width &lt;= self.input_width and height &lt;= self.input_height:\n        output.append(\"crop={}:{}\".format(width, height))\n\n    elif width &gt;= self.input_width and height &gt;= self.input_height:\n        output.append(\n            \"pad={}:{}:(iw-ow)/2:(ih-oh)/2:{}\".format(\n                width, height, self.overscan_color\n            )\n        )\n\n    elif width &gt; self.input_width and height &lt; self.input_height:\n        output.append(\"crop=iw:{}\".format(height))\n        output.append(\"pad={}:ih:(iw-ow)/2:(ih-oh)/2:{}\".format(\n            width, self.overscan_color\n        ))\n\n    elif width &lt; self.input_width and height &gt; self.input_height:\n        output.append(\"crop={}:ih\".format(width))\n        output.append(\"pad=iw:{}:(iw-ow)/2:(ih-oh)/2:{}\".format(\n            height, self.overscan_color\n        ))\n\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_review.html#client.ayon_core.plugins.publish.extract_review.OverscanCrop.width","title":"<code>width()</code>","text":"<p>Calculated width.</p> Source code in <code>client/ayon_core/plugins/publish/extract_review.py</code> <pre><code>def width(self):\n    \"\"\"Calculated width.\"\"\"\n    return self._width_value.size_for(self.input_width)\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_review.html#client.ayon_core.plugins.publish.extract_review.TempData","title":"<code>TempData</code>","text":"<p>Temporary data used across extractor's process.</p> Source code in <code>client/ayon_core/plugins/publish/extract_review.py</code> <pre><code>class TempData:\n    \"\"\"Temporary data used across extractor's process.\"\"\"\n    def __init__(\n        self,\n        fps: float,\n        frame_start: int,\n        frame_end: int,\n        handle_start: int,\n        handle_end: int,\n        frame_start_handle: int,\n        frame_end_handle: int,\n        output_frame_start: int,\n        output_frame_end: int,\n        pixel_aspect: float,\n        resolution_width: int,\n        resolution_height: int,\n        origin_repre: dict[str, Any],\n        input_is_sequence: bool,\n        first_sequence_frame: int,\n        input_allow_bg: bool,\n        with_audio: bool,\n        without_handles: bool,\n        handles_are_set: bool,\n        input_ext: str,\n        explicit_input_paths: list[str],\n        paths_to_remove: list[str],\n\n        # Set later\n        full_output_path: str = \"\",\n        filled_files: dict[int, str] = None,\n        output_ext_is_image: bool = True,\n        output_is_sequence: bool = True,\n    ):\n        if filled_files is None:\n            filled_files = {}\n        self.fps = fps\n        self.frame_start = frame_start\n        self.frame_end = frame_end\n        self.handle_start = handle_start\n        self.handle_end = handle_end\n        self.frame_start_handle = frame_start_handle\n        self.frame_end_handle = frame_end_handle\n        self.output_frame_start = output_frame_start\n        self.output_frame_end = output_frame_end\n        self.pixel_aspect = pixel_aspect\n        self.resolution_width = resolution_width\n        self.resolution_height = resolution_height\n        self.origin_repre = origin_repre\n        self.input_is_sequence = input_is_sequence\n        self.first_sequence_frame = first_sequence_frame\n        self.input_allow_bg = input_allow_bg\n        self.with_audio = with_audio\n        self.without_handles = without_handles\n        self.handles_are_set = handles_are_set\n        self.input_ext = input_ext\n        self.explicit_input_paths = explicit_input_paths\n        self.paths_to_remove = paths_to_remove\n\n        self.full_output_path = full_output_path\n        self.filled_files = filled_files\n        self.output_ext_is_image = output_ext_is_image\n        self.output_is_sequence = output_is_sequence\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_review.html#client.ayon_core.plugins.publish.extract_review.frame_to_timecode","title":"<code>frame_to_timecode(frame, fps)</code>","text":"<p>Convert a frame number and FPS to editorial timecode (HH:MM:SS:FF).</p> <p>Unlike <code>ayon_core.pipeline.editorial.frames_to_timecode</code> this does not rely on the <code>opentimelineio</code> package, so it can be used across hosts that do not have it available.</p> <p>Parameters:</p> Name Type Description Default <code>frame</code> <code>int</code> <p>The frame number to be converted.</p> required <code>fps</code> <code>float</code> <p>The frames per second of the video.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>The timecode in HH:MM:SS:FF format.</p> Source code in <code>client/ayon_core/plugins/publish/extract_review.py</code> <pre><code>def frame_to_timecode(frame: int, fps: float) -&gt; str:\n    \"\"\"Convert a frame number and FPS to editorial timecode (HH:MM:SS:FF).\n\n    Unlike `ayon_core.pipeline.editorial.frames_to_timecode` this does not\n    rely on the `opentimelineio` package, so it can be used across hosts that\n    do not have it available.\n\n    Args:\n        frame (int): The frame number to be converted.\n        fps (float): The frames per second of the video.\n\n    Returns:\n        str: The timecode in HH:MM:SS:FF format.\n    \"\"\"\n    # Calculate total seconds\n    total_seconds = frame / fps\n\n    # Extract hours, minutes, and seconds\n    hours = int(total_seconds // 3600)\n    minutes = int((total_seconds % 3600) // 60)\n    seconds = int(total_seconds % 60)\n\n    # Adjust for non-integer FPS by rounding the remaining frames appropriately\n    remaining_frames = round((total_seconds - int(total_seconds)) * fps)\n\n    # Format and return the timecode\n    return f\"{hours:02d}:{minutes:02d}:{seconds:02d}:{remaining_frames:02d}\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_review_slate.html","title":"extract_review_slate","text":""},{"location":"autoapi/client/ayon_core/plugins/publish/extract_review_slate.html#client.ayon_core.plugins.publish.extract_review_slate.ExtractReviewSlate","title":"<code>ExtractReviewSlate</code>","text":"<p>               Bases: <code>Extractor</code></p> <p>Will add slate frame at the start of the video files</p> Source code in <code>client/ayon_core/plugins/publish/extract_review_slate.py</code> <pre><code>class ExtractReviewSlate(publish.Extractor):\n    \"\"\"\n    Will add slate frame at the start of the video files\n    \"\"\"\n\n    label = \"Review with Slate frame\"\n    order = pyblish.api.ExtractorOrder + 0.031\n    families = [\"slate\", \"review\"]\n    match = pyblish.api.Subset\n\n    SUFFIX = \"_slate\"\n\n    hosts = [\"nuke\", \"shell\"]\n    optional = True\n\n    def process(self, instance):\n        inst_data = instance.data\n        if \"representations\" not in inst_data:\n            raise RuntimeError(\"Burnin needs already created mov to work on.\")\n\n        # get slates frame from upstream\n        slates_data = inst_data.get(\"slateFrames\")\n        if not slates_data:\n            # make it backward compatible and open for slates generator\n            # premium plugin\n            slates_data = {\n                \"*\": inst_data[\"slateFrame\"]\n            }\n\n        self.log.debug(\"_ slates_data: {}\".format(pformat(slates_data)))\n\n        if \"reviewToWidth\" in inst_data:\n            use_legacy_code = True\n        else:\n            use_legacy_code = False\n\n        pixel_aspect = inst_data.get(\"pixelAspect\", 1)\n        fps = inst_data.get(\"fps\")\n        self.log.debug(\"fps {} \".format(fps))\n\n        for idx, repre in enumerate(inst_data[\"representations\"]):\n            self.log.debug(\"repre ({}): `{}`\".format(idx + 1, repre))\n\n            p_tags = repre.get(\"tags\", [])\n            if \"slate-frame\" not in p_tags:\n                continue\n\n            # get repre file\n            stagingdir = repre[\"stagingDir\"]\n            input_file = \"{0}\".format(repre[\"files\"])\n            input_path = os.path.join(\n                os.path.normpath(stagingdir), repre[\"files\"])\n            self.log.debug(\"__ input_path: {}\".format(input_path))\n\n            streams = get_ffprobe_streams(\n                input_path, self.log\n            )\n            # get slate data\n            slate_path = self._get_slate_path(input_file, slates_data)\n            self.log.debug(\"_ slate_path: {}\".format(slate_path))\n\n            slate_width, slate_height = self._get_slates_resolution(slate_path)\n\n            # Get video metadata\n            (\n                input_width,\n                input_height,\n                input_timecode,\n                input_frame_rate,\n                input_pixel_aspect\n            ) = self._get_video_metadata(streams)\n            if input_pixel_aspect:\n                pixel_aspect = input_pixel_aspect\n\n            # Raise exception of any stream didn't define input resolution\n            if input_width is None:\n                raise PublishError(\n                    \"FFprobe couldn't read resolution from input file:\"\n                    f\" \\\"{input_path}\\\"\"\n                )\n\n            (\n                audio_codec,\n                audio_channels,\n                audio_sample_rate,\n                audio_channel_layout,\n                input_audio\n            ) = self._get_audio_metadata(streams)\n\n            # values are set in ExtractReview\n            if use_legacy_code:\n                to_width = inst_data[\"reviewToWidth\"]\n                to_height = inst_data[\"reviewToHeight\"]\n            else:\n                to_width = input_width\n                to_height = input_height\n\n            self.log.debug(\"to_width: `{}`\".format(to_width))\n            self.log.debug(\"to_height: `{}`\".format(to_height))\n\n            # defining image ratios\n            resolution_ratio = (\n                (float(slate_width) * pixel_aspect) / slate_height\n            )\n            delivery_ratio = float(to_width) / float(to_height)\n            self.log.debug(\"resolution_ratio: `{}`\".format(resolution_ratio))\n            self.log.debug(\"delivery_ratio: `{}`\".format(delivery_ratio))\n\n            # get scale factor\n            scale_factor_by_height = float(to_height) / slate_height\n            scale_factor_by_width = float(to_width) / (\n                slate_width * pixel_aspect\n            )\n\n            # shorten two decimals long float number for testing conditions\n            resolution_ratio_test = float(\"{:0.2f}\".format(resolution_ratio))\n            delivery_ratio_test = float(\"{:0.2f}\".format(delivery_ratio))\n\n            self.log.debug(\"__ scale_factor_by_width: `{}`\".format(\n                scale_factor_by_width\n            ))\n            self.log.debug(\"__ scale_factor_by_height: `{}`\".format(\n                scale_factor_by_height\n            ))\n\n            _remove_at_end = []\n\n            ext = os.path.splitext(input_file)[1]\n            output_file = input_file.replace(ext, \"\") + self.SUFFIX + ext\n\n            _remove_at_end.append(input_path)\n\n            output_path = os.path.join(\n                os.path.normpath(stagingdir), output_file)\n            self.log.debug(\"__ output_path: {}\".format(output_path))\n\n            input_args = []\n            output_args = []\n\n            # preset's input data\n            if use_legacy_code:\n                input_args.extend(repre[\"_profile\"].get('input', []))\n            else:\n                input_args.extend(repre[\"outputDef\"].get('input', []))\n\n            input_args.extend([\n                \"-loop\", \"1\",\n                \"-i\", path_to_subprocess_arg(slate_path),\n                \"-r\", str(input_frame_rate),\n                \"-frames:v\", \"1\",\n            ])\n\n            # add timecode from source to the slate, substract one frame\n            offset_timecode = \"\"\n            if input_timecode:\n                offset_timecode = self._tc_offset(\n                    str(input_timecode),\n                    framerate=fps,\n                    frame_offset=-1\n                )\n                self.log.debug(\"Slate Timecode: `{}`\".format(\n                    offset_timecode\n                ))\n\n            if use_legacy_code:\n                format_args = []\n                codec_args = repre[\"_profile\"].get('codec', [])\n                output_args.extend(codec_args)\n                # preset's output data\n                output_args.extend(repre[\"_profile\"].get('output', []))\n            else:\n                # Codecs are copied from source for whole input\n                format_args, codec_args = self._get_format_codec_args(repre)\n                output_args.extend(format_args)\n                output_args.extend(codec_args)\n\n            # make sure colors are correct\n            output_args.extend([\n                \"-color_primaries\", \"bt709\",\n                \"-color_trc\", \"bt709\",\n                \"-colorspace\", \"bt709\",\n            ])\n\n            # scaling none square pixels and 1920 width\n            if (\n                # Always scale slate if not legacy\n                not use_legacy_code or\n                # Legacy code required reformat tag\n                (use_legacy_code and \"reformat\" in p_tags)\n            ):\n                if resolution_ratio_test &lt; delivery_ratio_test:\n                    self.log.debug(\"lower then delivery\")\n                    width_scale = int(slate_width * scale_factor_by_height)\n                    width_half_pad = int((to_width - width_scale) / 2)\n                    height_scale = to_height\n                    height_half_pad = 0\n                else:\n                    self.log.debug(\"heigher then delivery\")\n                    width_scale = to_width\n                    width_half_pad = 0\n                    height_scale = int(slate_height * scale_factor_by_width)\n                    height_half_pad = int((to_height - height_scale) / 2)\n\n                self.log.debug(\n                    \"__ width_scale: `{}`\".format(width_scale)\n                )\n                self.log.debug(\n                    \"__ width_half_pad: `{}`\".format(width_half_pad)\n                )\n                self.log.debug(\n                    \"__ height_scale: `{}`\".format(height_scale)\n                )\n                self.log.debug(\n                    \"__ height_half_pad: `{}`\".format(height_half_pad)\n                )\n\n                scaling_arg = (\n                    \"scale={0}x{1}:flags=lanczos\"\n                    \":out_color_matrix=bt709\"\n                    \",pad={2}:{3}:{4}:{5}:black\"\n                    \",setsar=1\"\n                    \",fps={6}\"\n                ).format(\n                    width_scale,\n                    height_scale,\n                    to_width,\n                    to_height,\n                    width_half_pad,\n                    height_half_pad,\n                    input_frame_rate\n                )\n\n                vf_back = self.add_video_filter_args(output_args, scaling_arg)\n                # add it to output_args\n                output_args.insert(0, vf_back)\n\n            # overrides output file\n            output_args.append(\"-y\")\n\n            slate_v_path = slate_path.replace(\".png\", ext)\n            output_args.append(\n                path_to_subprocess_arg(slate_v_path)\n            )\n            _remove_at_end.append(slate_v_path)\n\n            slate_args = [\n                subprocess.list2cmdline(get_ffmpeg_tool_args(\"ffmpeg\")),\n                \" \".join(input_args),\n                \" \".join(output_args)\n            ]\n            slate_subprocess_cmd = \" \".join(slate_args)\n\n            # run slate generation subprocess\n            self.log.debug(\n                \"Slate Executing: {}\".format(slate_subprocess_cmd)\n            )\n            run_subprocess(\n                slate_subprocess_cmd, shell=True, logger=self.log\n            )\n\n            # Create slate with silent audio track\n            if input_audio:\n                # silent slate output path\n                slate_silent_path = \"_silent\".join(\n                    os.path.splitext(slate_v_path))\n                _remove_at_end.append(slate_silent_path)\n                self._create_silent_slate(\n                    slate_v_path,\n                    slate_silent_path,\n                    audio_codec,\n                    audio_channels,\n                    audio_sample_rate,\n                    audio_channel_layout,\n                    input_frame_rate\n                )\n\n                # replace slate with silent slate for concat\n                slate_v_path = slate_silent_path\n\n            # concat slate and videos together with concat filter\n            # this will reencode the output\n            if input_audio:\n                fmap = [\n                    \"-filter_complex\",\n                    \"[0:v] [0:a] [1:v] [1:a] concat=n=2:v=1:a=1 [v] [a]\",\n                    \"-map\", '[v]',\n                    \"-map\", '[a]'\n                ]\n            else:\n                fmap = [\n                    \"-filter_complex\",\n                    \"[0:v] [1:v] concat=n=2:v=1:a=0 [v]\",\n                    \"-map\", '[v]'\n                ]\n            concat_args = get_ffmpeg_tool_args(\n                \"ffmpeg\",\n                \"-y\",\n                \"-i\", slate_v_path,\n                \"-i\", input_path,\n            )\n            concat_args.extend(fmap)\n            if offset_timecode:\n                concat_args.extend([\"-timecode\", offset_timecode])\n            # NOTE: Added because of OP Atom demuxers\n            # Add format arguments if there are any\n            # - keep format of output\n            if format_args:\n                concat_args.extend(format_args)\n\n            if codec_args:\n                concat_args.extend(codec_args)\n\n            # Use arguments from ffmpeg preset\n            source_ffmpeg_cmd = repre.get(\"ffmpeg_cmd\")\n            if source_ffmpeg_cmd:\n                copy_args = (\n                    \"-metadata\",\n                    \"-metadata:s:v:0\",\n                    \"-b:v\",\n                    \"-b:a\",\n                )\n                args = source_ffmpeg_cmd.split(\" \")\n                for indx, arg in enumerate(args):\n                    if arg in copy_args:\n                        concat_args.append(arg)\n                        # assumes arg has one parameter\n                        concat_args.append(args[indx + 1])\n\n            # add final output path\n            concat_args.append(output_path)\n\n            # ffmpeg concat subprocess\n            self.log.debug(\n                \"Executing concat filter: {}\".format\n                (\" \".join(concat_args))\n            )\n            run_subprocess(\n                concat_args, logger=self.log\n            )\n\n            self.log.debug(\"__ repre[tags]: {}\".format(repre[\"tags\"]))\n            repre_update = {\n                \"files\": output_file,\n                \"name\": repre[\"name\"],\n                \"tags\": [x for x in repre[\"tags\"] if x != \"delete\"]\n            }\n            inst_data[\"representations\"][idx].update(repre_update)\n            self.log.debug(\n                \"_ representation {}: `{}`\".format(\n                    idx, inst_data[\"representations\"][idx]))\n\n            # removing temp files\n            for f in _remove_at_end:\n                os.remove(f)\n                self.log.debug(\"Removed: `{}`\".format(f))\n\n        # Remove any representations tagged for deletion.\n        for repre in inst_data.get(\"representations\", []):\n            tags = repre.get(\"tags\", [])\n            if \"delete\" not in tags:\n                continue\n            if \"need_thumbnail\" in tags:\n                continue\n            self.log.debug(\"Removing representation: {}\".format(repre))\n            inst_data[\"representations\"].remove(repre)\n\n        self.log.debug(inst_data[\"representations\"])\n\n    def _get_slate_path(self, input_file, slates_data):\n        slate_path = None\n        for sl_n, _slate_path in slates_data.items():\n            if \"*\" in sl_n:\n                slate_path = _slate_path\n                break\n            elif re.search(sl_n, input_file):\n                slate_path = _slate_path\n                break\n\n        if not slate_path:\n            raise AttributeError(\n                \"Missing slates paths: {}\".format(slates_data))\n\n        return slate_path\n\n    def _get_slates_resolution(self, slate_path):\n        slate_streams = get_ffprobe_streams(slate_path, self.log)\n        # Try to find first stream with defined 'width' and 'height'\n        # - this is to avoid order of streams where audio can be as first\n        # - there may be a better way (checking `codec_type`?)+\n        slate_width = None\n        slate_height = None\n        for slate_stream in slate_streams:\n            if \"width\" in slate_stream and \"height\" in slate_stream:\n                slate_width = int(slate_stream[\"width\"])\n                slate_height = int(slate_stream[\"height\"])\n                break\n\n        # Raise exception of any stream didn't define input resolution\n        if slate_width is None:\n            raise AssertionError((\n                \"FFprobe couldn't read resolution from input file: \\\"{}\\\"\"\n            ).format(slate_path))\n\n        return (slate_width, slate_height)\n\n    def _get_video_metadata(self, streams):\n        input_timecode = \"\"\n        input_width = None\n        input_height = None\n        input_frame_rate = None\n        input_pixel_aspect = None\n        for stream in streams:\n            if stream.get(\"codec_type\") != \"video\":\n                continue\n            self.log.debug(\"FFprobe Video: {}\".format(stream))\n\n            if \"width\" not in stream or \"height\" not in stream:\n                continue\n            width = int(stream[\"width\"])\n            height = int(stream[\"height\"])\n            if not width or not height:\n                continue\n\n            # Make sure that width and height are captured even if frame rate\n            #    is not available\n            input_width = width\n            input_height = height\n\n            input_pixel_aspect = stream.get(\"sample_aspect_ratio\")\n            if input_pixel_aspect is not None:\n                try:\n                    input_pixel_aspect = float(\n                        eval(str(input_pixel_aspect).replace(':', '/')))\n                except Exception:\n                    self.log.debug(\n                        \"__Converting pixel aspect to float failed: {}\".format(\n                            input_pixel_aspect))\n\n            tags = stream.get(\"tags\") or {}\n            input_timecode = tags.get(\"timecode\") or \"\"\n\n            input_frame_rate = stream.get(\"r_frame_rate\")\n            if input_frame_rate is not None:\n                break\n        return (\n            input_width,\n            input_height,\n            input_timecode,\n            input_frame_rate,\n            input_pixel_aspect\n        )\n\n    def _get_audio_metadata(self, streams):\n        # Get audio metadata\n        audio_codec = None\n        audio_channels = None\n        audio_sample_rate = None\n        audio_channel_layout = None\n        input_audio = False\n\n        for stream in streams:\n            if stream.get(\"codec_type\") != \"audio\":\n                continue\n            self.log.debug(\"__Ffprobe Audio: {}\".format(stream))\n\n            if all(\n                stream.get(key)\n                for key in (\n                    \"codec_name\",\n                    \"channels\",\n                    \"sample_rate\",\n                    \"channel_layout\",\n                )\n            ):\n                audio_codec = stream[\"codec_name\"]\n                audio_channels = stream[\"channels\"]\n                audio_sample_rate = stream[\"sample_rate\"]\n                audio_channel_layout = stream[\"channel_layout\"]\n                input_audio = True\n                break\n\n        return (\n            audio_codec,\n            audio_channels,\n            audio_sample_rate,\n            audio_channel_layout,\n            input_audio,\n        )\n\n    def _create_silent_slate(\n        self,\n        src_path,\n        dst_path,\n        audio_codec,\n        audio_channels,\n        audio_sample_rate,\n        audio_channel_layout,\n        input_frame_rate\n    ):\n        # Get duration of one frame in micro seconds\n        items = input_frame_rate.split(\"/\")\n        if len(items) == 1:\n            one_frame_duration = 1.0 / float(items[0])\n        elif len(items) == 2:\n            one_frame_duration = float(items[1]) / float(items[0])\n        else:\n            one_frame_duration = None\n\n        if one_frame_duration is None:\n            one_frame_duration = \"40000us\"\n        else:\n            one_frame_duration *= 1000000\n            one_frame_duration = str(int(one_frame_duration)) + \"us\"\n        self.log.debug(\"One frame duration is {}\".format(one_frame_duration))\n\n        slate_silent_args = get_ffmpeg_tool_args(\n            \"ffmpeg\",\n            \"-i\", src_path,\n            \"-f\", \"lavfi\", \"-i\",\n            \"anullsrc=r={}:cl={}:d={}\".format(\n                audio_sample_rate,\n                audio_channel_layout,\n                one_frame_duration\n            ),\n            \"-c:v\", \"copy\",\n            \"-c:a\", audio_codec,\n            \"-map\", \"0:v\",\n            \"-map\", \"1:a\",\n            \"-shortest\",\n            \"-y\",\n            dst_path\n        )\n        # run slate generation subprocess\n        self.log.debug(\"Silent Slate Executing: {}\".format(\n            \" \".join(slate_silent_args)\n        ))\n        run_subprocess(\n            slate_silent_args, logger=self.log\n        )\n\n    def add_video_filter_args(self, args, inserting_arg):\n        \"\"\"\n        Fixing video filter argumets to be one long string\n\n        Args:\n            args (list): list of string arguments\n            inserting_arg (str): string argument we want to add\n                                 (without flag `-vf`)\n\n        Returns:\n            str: long joined argument to be added back to list of arguments\n\n        \"\"\"\n        # find all video format settings\n        vf_settings = [p for p in args\n                       for v in [\"-filter:v\", \"-vf\"]\n                       if v in p]\n        self.log.debug(\"_ vf_settings: `{}`\".format(vf_settings))\n\n        # remove them from output args list\n        for p in vf_settings:\n            self.log.debug(\"_ remove p: `{}`\".format(p))\n            args.remove(p)\n            self.log.debug(\"_ args: `{}`\".format(args))\n\n        # strip them from all flags\n        vf_fixed = [p.replace(\"-vf \", \"\").replace(\"-filter:v \", \"\")\n                    for p in vf_settings]\n\n        self.log.debug(\"_ vf_fixed: `{}`\".format(vf_fixed))\n        vf_fixed.insert(0, inserting_arg)\n        self.log.debug(\"_ vf_fixed: `{}`\".format(vf_fixed))\n        # create new video filter setting\n        vf_back = \"-vf \" + \",\".join(vf_fixed)\n\n        return vf_back\n\n    def _get_format_codec_args(self, repre):\n        \"\"\"Detect possible codec arguments from representation.\"\"\"\n        codec_args = []\n\n        # Get one filename of representation files\n        filename = repre[\"files\"]\n        # If files is list then pick first filename in list\n        if isinstance(filename, (tuple, list)):\n            filename = filename[0]\n        # Get full path to the file\n        full_input_path = os.path.join(repre[\"stagingDir\"], filename)\n\n        try:\n            # Get information about input file via ffprobe tool\n            ffprobe_data = get_ffprobe_data(full_input_path, self.log)\n        except Exception:\n            self.log.warning(\n                \"Could not get codec data from input.\",\n                exc_info=True\n            )\n            return codec_args\n\n        source_ffmpeg_cmd = repre.get(\"ffmpeg_cmd\")\n        format_args = get_ffmpeg_format_args(ffprobe_data, source_ffmpeg_cmd)\n        codec_args = get_ffmpeg_codec_args(\n            ffprobe_data, source_ffmpeg_cmd, logger=self.log\n        )\n\n        return format_args, codec_args\n\n    def _tc_offset(self, timecode, framerate=24.0, frame_offset=-1):\n        \"\"\"Offsets timecode by frame\"\"\"\n        def _seconds(value, framerate):\n            if isinstance(value, str):\n                _zip_ft = zip((3600, 60, 1, 1 / framerate), value.split(':'))\n                _s = sum(f * float(t) for f, t in _zip_ft)\n            elif isinstance(value, (int, float)):\n                _s = value / framerate\n            else:\n                _s = 0\n            return _s\n\n        def _frames(seconds, framerate, frame_offset):\n            _f = seconds * framerate + frame_offset\n            if _f &lt; 0:\n                _f = framerate * 60 * 60 * 24 + _f\n            return _f\n\n        def _timecode(seconds, framerate):\n            return '{h:02d}:{m:02d}:{s:02d}:{f:02d}'.format(\n                h=int(seconds / 3600),\n                m=int(seconds / 60 % 60),\n                s=int(seconds % 60),\n                f=int(round((seconds - int(seconds)) * framerate)))\n        drop = False\n        if ';' in timecode:\n            timecode = timecode.replace(';', ':')\n            drop = True\n        frames = _frames(\n            _seconds(timecode, framerate),\n            framerate,\n            frame_offset\n        )\n        tc = _timecode(_seconds(frames, framerate), framerate)\n        if drop:\n            tc = ';'.join(tc.rsplit(':', 1))\n        return tc\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_review_slate.html#client.ayon_core.plugins.publish.extract_review_slate.ExtractReviewSlate.add_video_filter_args","title":"<code>add_video_filter_args(args, inserting_arg)</code>","text":"<p>Fixing video filter argumets to be one long string</p> <p>Parameters:</p> Name Type Description Default <code>args</code> <code>list</code> <p>list of string arguments</p> required <code>inserting_arg</code> <code>str</code> <p>string argument we want to add                  (without flag <code>-vf</code>)</p> required <p>Returns:</p> Name Type Description <code>str</code> <p>long joined argument to be added back to list of arguments</p> Source code in <code>client/ayon_core/plugins/publish/extract_review_slate.py</code> <pre><code>def add_video_filter_args(self, args, inserting_arg):\n    \"\"\"\n    Fixing video filter argumets to be one long string\n\n    Args:\n        args (list): list of string arguments\n        inserting_arg (str): string argument we want to add\n                             (without flag `-vf`)\n\n    Returns:\n        str: long joined argument to be added back to list of arguments\n\n    \"\"\"\n    # find all video format settings\n    vf_settings = [p for p in args\n                   for v in [\"-filter:v\", \"-vf\"]\n                   if v in p]\n    self.log.debug(\"_ vf_settings: `{}`\".format(vf_settings))\n\n    # remove them from output args list\n    for p in vf_settings:\n        self.log.debug(\"_ remove p: `{}`\".format(p))\n        args.remove(p)\n        self.log.debug(\"_ args: `{}`\".format(args))\n\n    # strip them from all flags\n    vf_fixed = [p.replace(\"-vf \", \"\").replace(\"-filter:v \", \"\")\n                for p in vf_settings]\n\n    self.log.debug(\"_ vf_fixed: `{}`\".format(vf_fixed))\n    vf_fixed.insert(0, inserting_arg)\n    self.log.debug(\"_ vf_fixed: `{}`\".format(vf_fixed))\n    # create new video filter setting\n    vf_back = \"-vf \" + \",\".join(vf_fixed)\n\n    return vf_back\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_scanline_exr.html","title":"extract_scanline_exr","text":"<p>Convert exrs in representation to tiled exrs usin oiio tools.</p>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_scanline_exr.html#client.ayon_core.plugins.publish.extract_scanline_exr.ExtractScanlineExr","title":"<code>ExtractScanlineExr</code>","text":"<p>               Bases: <code>InstancePlugin</code></p> <p>Convert tiled EXRs to scanline using OIIO tool.</p> Source code in <code>client/ayon_core/plugins/publish/extract_scanline_exr.py</code> <pre><code>class ExtractScanlineExr(pyblish.api.InstancePlugin):\n    \"\"\"Convert tiled EXRs to scanline using OIIO tool.\"\"\"\n\n    label = \"Extract Scanline EXR\"\n    hosts = [\"shell\"]\n    order = pyblish.api.ExtractorOrder\n    families = [\"imagesequence\", \"render\", \"render2d\", \"source\"]\n\n    def process(self, instance):\n        \"\"\"Plugin entry point.\"\"\"\n        # get representation and loop them\n        representations = instance.data[\"representations\"]\n\n        representations_new = []\n\n        for repre in representations:\n            self.log.debug(\n                \"Processing representation {}\".format(repre.get(\"name\")))\n            tags = repre.get(\"tags\", [])\n            if \"toScanline\" not in tags:\n                self.log.debug(\" - missing toScanline tag\")\n                continue\n\n            # run only on exrs\n            if repre.get(\"ext\") != \"exr\":\n                self.log.debug(\"- not EXR files\")\n                continue\n\n            if not isinstance(repre['files'], (list, tuple)):\n                input_files = [repre['files']]\n                self.log.debug(\"We have a single frame\")\n            else:\n                input_files = repre['files']\n                self.log.debug(\"We have a sequence\")\n\n            stagingdir = os.path.normpath(repre.get(\"stagingDir\"))\n\n            try:\n                oiio_tool_args = get_oiio_tool_args(\"oiiotool\")\n            except ToolNotFoundError:\n                self.log.error(\"OIIO tool not found.\")\n                raise PublishError(\"OIIO tool not found\")\n\n            for file in input_files:\n\n                original_name = os.path.join(stagingdir, file)\n                temp_name = os.path.join(stagingdir, \"__{}\".format(file))\n                # move original render to temp location\n                shutil.move(original_name, temp_name)\n                oiio_cmd = oiio_tool_args + [\n                    os.path.join(stagingdir, temp_name), \"--scanline\", \"-o\",\n                    os.path.join(stagingdir, original_name)\n                ]\n\n                subprocess_exr = \" \".join(oiio_cmd)\n                self.log.debug(f\"running: {subprocess_exr}\")\n                run_subprocess(subprocess_exr, logger=self.log)\n\n                # raise error if there is no ouptput\n                if not os.path.exists(os.path.join(stagingdir, original_name)):\n                    self.log.error(\n                        (\"File {} was not converted \"\n                         \"by oiio tool!\").format(original_name))\n                    raise AssertionError(\"OIIO tool conversion failed\")\n                else:\n                    try:\n                        os.remove(temp_name)\n                    except OSError as e:\n                        self.log.warning(\"Unable to delete temp file\")\n                        self.log.warning(e)\n\n            repre['name'] = 'exr'\n            try:\n                repre['tags'].remove('toScanline')\n            except ValueError:\n                # no `toScanline` tag present\n                pass\n\n        instance.data[\"representations\"] += representations_new\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_scanline_exr.html#client.ayon_core.plugins.publish.extract_scanline_exr.ExtractScanlineExr.process","title":"<code>process(instance)</code>","text":"<p>Plugin entry point.</p> Source code in <code>client/ayon_core/plugins/publish/extract_scanline_exr.py</code> <pre><code>def process(self, instance):\n    \"\"\"Plugin entry point.\"\"\"\n    # get representation and loop them\n    representations = instance.data[\"representations\"]\n\n    representations_new = []\n\n    for repre in representations:\n        self.log.debug(\n            \"Processing representation {}\".format(repre.get(\"name\")))\n        tags = repre.get(\"tags\", [])\n        if \"toScanline\" not in tags:\n            self.log.debug(\" - missing toScanline tag\")\n            continue\n\n        # run only on exrs\n        if repre.get(\"ext\") != \"exr\":\n            self.log.debug(\"- not EXR files\")\n            continue\n\n        if not isinstance(repre['files'], (list, tuple)):\n            input_files = [repre['files']]\n            self.log.debug(\"We have a single frame\")\n        else:\n            input_files = repre['files']\n            self.log.debug(\"We have a sequence\")\n\n        stagingdir = os.path.normpath(repre.get(\"stagingDir\"))\n\n        try:\n            oiio_tool_args = get_oiio_tool_args(\"oiiotool\")\n        except ToolNotFoundError:\n            self.log.error(\"OIIO tool not found.\")\n            raise PublishError(\"OIIO tool not found\")\n\n        for file in input_files:\n\n            original_name = os.path.join(stagingdir, file)\n            temp_name = os.path.join(stagingdir, \"__{}\".format(file))\n            # move original render to temp location\n            shutil.move(original_name, temp_name)\n            oiio_cmd = oiio_tool_args + [\n                os.path.join(stagingdir, temp_name), \"--scanline\", \"-o\",\n                os.path.join(stagingdir, original_name)\n            ]\n\n            subprocess_exr = \" \".join(oiio_cmd)\n            self.log.debug(f\"running: {subprocess_exr}\")\n            run_subprocess(subprocess_exr, logger=self.log)\n\n            # raise error if there is no ouptput\n            if not os.path.exists(os.path.join(stagingdir, original_name)):\n                self.log.error(\n                    (\"File {} was not converted \"\n                     \"by oiio tool!\").format(original_name))\n                raise AssertionError(\"OIIO tool conversion failed\")\n            else:\n                try:\n                    os.remove(temp_name)\n                except OSError as e:\n                    self.log.warning(\"Unable to delete temp file\")\n                    self.log.warning(e)\n\n        repre['name'] = 'exr'\n        try:\n            repre['tags'].remove('toScanline')\n        except ValueError:\n            # no `toScanline` tag present\n            pass\n\n    instance.data[\"representations\"] += representations_new\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_slate_data.html","title":"extract_slate_data","text":""},{"location":"autoapi/client/ayon_core/plugins/publish/extract_slate_data.html#client.ayon_core.plugins.publish.extract_slate_data.ExtractSlateData","title":"<code>ExtractSlateData</code>","text":"<p>               Bases: <code>Extractor</code></p> <p>Add slate data for integration.</p> Source code in <code>client/ayon_core/plugins/publish/extract_slate_data.py</code> <pre><code>class ExtractSlateData(publish.Extractor):\n    \"\"\"Add slate data for integration.\"\"\"\n\n    label = \"Slate Data\"\n    # Offset from ExtractReviewSlate and ExtractGenerateSlate.\n    order = pyblish.api.ExtractorOrder + 0.49\n    families = [\"slate\", \"review\"]\n    hosts = [\"nuke\", \"shell\"]\n\n    def process(self, instance):\n        for representation in instance.data.get(\"representations\", []):\n            if \"slate-frame\" not in representation.get(\"tags\", []):\n                continue\n\n            data = representation.get(\"data\", {})\n            data[\"slateFrames\"] = 1\n            representation[\"data\"] = data\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_thumbnail.html","title":"extract_thumbnail","text":""},{"location":"autoapi/client/ayon_core/plugins/publish/extract_thumbnail.html#client.ayon_core.plugins.publish.extract_thumbnail.ExtractThumbnail","title":"<code>ExtractThumbnail</code>","text":"<p>               Bases: <code>InstancePlugin</code></p> <p>Create jpg thumbnail from sequence using ffmpeg</p> Source code in <code>client/ayon_core/plugins/publish/extract_thumbnail.py</code> <pre><code>class ExtractThumbnail(pyblish.api.InstancePlugin):\n    \"\"\"Create jpg thumbnail from sequence using ffmpeg\"\"\"\n\n    label = \"Extract Thumbnail\"\n    order = pyblish.api.ExtractorOrder + 0.49\n    families = [\n        \"imagesequence\", \"render\", \"render2d\", \"prerender\",\n        \"source\", \"clip\", \"take\", \"online\", \"image\", \"editorial_pkg\"\n    ]\n    hosts = [\n        \"shell\",\n        \"fusion\",\n        \"resolve\",\n        \"traypublisher\",\n        \"substancepainter\",\n        \"substancedesigner\",\n        \"nuke\",\n        \"aftereffects\",\n        \"photoshop\",\n        \"unreal\",\n        \"houdini\",\n        \"batchdelivery\",\n        \"workflow\",\n    ]\n    settings_category = \"core\"\n    enabled = False\n\n    profiles = []\n\n    def process(self, instance):\n        # run main process\n        self._main_process(instance)\n\n        # Make sure cleanup happens to representations which are having both\n        # tags `delete` and `need_thumbnail`\n        for repre in tuple(instance.data.get(\"representations\", [])):\n            tags = repre.get(\"tags\") or []\n            # skip representations which are going to be published on farm\n            if \"publish_on_farm\" in tags:\n                continue\n            if (\n                \"delete\" in tags\n                and \"need_thumbnail\" in tags\n            ):\n                self.log.debug(\n                    \"Removing representation: {}\".format(repre)\n                )\n                instance.data[\"representations\"].remove(repre)\n\n    def _main_process(self, instance):\n        if not self.profiles:\n            self.log.debug(\"No profiles present for extract review thumbnail.\")\n            return\n        thumbnail_def = self._get_config_from_profile(instance)\n        if not thumbnail_def:\n            return\n\n        product_name = instance.data[\"productName\"]\n        instance_repres = instance.data.get(\"representations\")\n        if not instance_repres:\n            self.log.debug((\n                \"Instance {} does not have representations. Skipping\"\n            ).format(product_name))\n            return\n\n        self.log.debug(\n            \"Processing instance with product name {}\".format(product_name)\n        )\n\n        # Skip if instance have 'review' key in data set to 'False'\n        if not self._is_review_instance(instance):\n            self.log.debug(\"Skipping - no review set on instance.\")\n            return\n\n        # Check if already has thumbnail created\n        if self._already_has_thumbnail(instance_repres):\n            self.log.debug(\"Thumbnail representation already present.\")\n            return\n\n        # skip crypto passes.\n        # TODO: This is just a quick fix and has its own side-effects - it is\n        #       affecting every prouct name with `crypto` in its name.\n        #       This must be solved properly, maybe using tags on\n        #       representation that can be determined much earlier and\n        #       with better precision.\n        if \"crypto\" in product_name.lower():\n            self.log.debug(\"Skipping crypto passes.\")\n            return\n\n        # first check for any explicitly marked representations for thumbnail\n        explicit_repres = self._get_explicit_repres_for_thumbnail(instance)\n        if explicit_repres:\n            filtered_repres = explicit_repres\n        else:\n            filtered_repres = self._get_filtered_repres(instance)\n\n        if not filtered_repres:\n            self.log.info(\n                \"Instance doesn't have representations that can be used \"\n                \"as source for thumbnail. Skipping thumbnail extraction.\"\n            )\n            return\n\n        # Create temp directory for thumbnail\n        # - this is to avoid \"override\" of source file\n        dst_staging = tempfile.mkdtemp(prefix=\"pyblish_tmp_\")\n        self.log.debug(\n            \"Create temp directory {} for thumbnail\".format(dst_staging)\n        )\n        # Store new staging to cleanup paths\n        instance.context.data[\"cleanupFullPaths\"].append(dst_staging)\n\n        oiio_supported = is_oiio_supported()\n        thumbnail_created = False\n        project_settings = instance.context.data[\"project_settings\"]\n        review_layers = get_default_reviewable_layers(project_settings)\n        for repre in filtered_repres:\n            # Reset for each iteration to handle cases where multiple\n            # reviewable thumbnails are needed\n            repre_thumb_created = False\n            repre_files = repre[\"files\"]\n            src_staging = os.path.normpath(repre[\"stagingDir\"])\n            if not isinstance(repre_files, (list, tuple)):\n                # convert any video file to frame so oiio doesn't need to\n                # read video file (it is slow) and also we are having control\n                # over which frame is used for thumbnail\n                # this will also work with ffmpeg fallback conversion in case\n                # oiio is not supported\n                repre_extension = os.path.splitext(repre_files)[1]\n                if repre_extension in VIDEO_EXTENSIONS:\n                    video_file_path = os.path.join(\n                        src_staging, repre_files\n                    )\n                    file_path = self._create_frame_from_video(\n                        video_file_path,\n                        dst_staging,\n                        thumbnail_def\n                    )\n                    if file_path:\n                        src_staging, input_file = os.path.split(file_path)\n                else:\n                    # if it is not video file then just use first file\n                    input_file = repre_files\n            else:\n                repre_files_thumb = copy.deepcopy(repre_files)\n                # exclude first frame if slate in representation tags\n                if \"slate-frame\" in repre.get(\"tags\", []):\n                    repre_files_thumb = repre_files_thumb[1:]\n                file_index = int(\n                    float(len(repre_files_thumb)) * thumbnail_def.duration_split  # noqa: E501\n                )\n                input_file = repre_files[file_index]\n\n            full_input_path = os.path.join(src_staging, input_file)\n            self.log.debug(\"input {}\".format(full_input_path))\n\n            filename = os.path.splitext(input_file)[0]\n            jpeg_file = filename + \"_thumb.jpg\"\n            full_output_path = os.path.join(dst_staging, jpeg_file)\n            colorspace_data = repre.get(\"colorspaceData\")\n\n            # NOTE We should find out what is happening here. Why don't we\n            #   use oiiotool all the time if it is available? Only possible\n            #   reason might be that video files should be converted using\n            #   ffmpeg, but other then that, we should use oiio all the time.\n            #   - We should also probably get rid of the ffmpeg settings...\n\n            # only use OIIO if it is supported and representation has\n            # colorspace data\n            if oiio_supported and colorspace_data:\n                self.log.debug(\n                    \"Trying to convert with OIIO \"\n                    \"with colorspace data: {}\".format(colorspace_data)\n                )\n                # If the input can read by OIIO then use OIIO method for\n                # conversion otherwise use ffmpeg\n                anatomy = instance.context.data[\"anatomy\"]\n                repre_thumb_created = self._create_colorspace_thumbnail(\n                    full_input_path,\n                    full_output_path,\n                    repre,\n                    thumbnail_def,\n                    anatomy=anatomy,\n                    review_layers=review_layers,\n                )\n\n            # Try to use FFMPEG if OIIO is not supported or for cases when\n            #   oiiotool isn't available or representation is not having\n            #   colorspace data\n            if not repre_thumb_created:\n                repre_thumb_created = self._create_thumbnail_ffmpeg(\n                    full_input_path,\n                    full_output_path,\n                    thumbnail_def,\n                    review_layers,\n                )\n\n            # Skip representation and try next one if wasn't created\n            if not repre_thumb_created and oiio_supported:\n                repre_thumb_created = self._create_thumbnail_oiio(\n                    full_input_path,\n                    full_output_path,\n                    thumbnail_def,\n                    review_layers,\n                )\n\n            if not repre_thumb_created:\n                continue\n\n            thumbnail_created = True\n            if len(explicit_repres) &gt; 1:\n                repre_name = \"thumbnail_{}\".format(repre[\"outputName\"])\n            else:\n                repre_name = \"thumbnail\"\n\n            # add thumbnail path to instance data for integrator\n            instance_thumb_path = instance.data.get(\"thumbnailPath\")\n            if (\n                not instance_thumb_path\n                or not os.path.isfile(instance_thumb_path)\n            ):\n                self.log.debug(\n                    \"Adding thumbnail path to instance data: {}\".format(\n                        full_output_path\n                    )\n                )\n                instance.data[\"thumbnailPath\"] = full_output_path\n\n            new_repre_tags = [\"thumbnail\"]\n            # for workflows which needs to have thumbnails published as\n            # separate representations `delete` tag should not be added\n            if not thumbnail_def.integrate_thumbnail:\n                new_repre_tags.append(\"delete\")\n\n            new_repre = {\n                \"name\": repre_name,\n                \"ext\": \"jpg\",\n                \"files\": jpeg_file,\n                \"stagingDir\": dst_staging,\n                \"thumbnail\": True,\n                \"tags\": new_repre_tags,\n                # If source image is jpg then there can be clash when\n                # integrating to making the output name explicit.\n                \"outputName\": \"thumbnail\"\n            }\n\n            # adding representation\n            instance.data[\"representations\"].append(new_repre)\n\n            if explicit_repres:\n                # this key will then align assetVersion ftrack thumbnail sync\n                new_repre[\"outputName\"] = (\n                    repre.get(\"outputName\") or repre[\"name\"])\n                self.log.debug(\n                    \"Adding explicit thumbnail representation: {}\".format(\n                        new_repre))\n            else:\n                self.log.debug(\n                    \"Adding thumbnail representation: {}\".format(new_repre)\n                )\n                # There is no need to create more then one thumbnail\n                break\n\n        if not thumbnail_created:\n            self.log.warning(\"Thumbnail has not been created.\")\n\n    def _is_review_instance(self, instance):\n        # TODO: We should probably handle \"not creating\" of thumbnail\n        #   other way then checking for \"review\" key on instance data?\n        if instance.data.get(\"review\", True):\n            return True\n        return False\n\n    def _already_has_thumbnail(self, repres):\n        for repre in repres:\n            self.log.debug(\"repre {}\".format(repre))\n            if repre[\"name\"] == \"thumbnail\":\n                return True\n        return False\n\n    def _get_explicit_repres_for_thumbnail(self, instance):\n        src_repres = instance.data.get(\"representations\") or []\n        # This is mainly for Nuke where we have multiple representations for\n        #   one instance and representations are tagged for thumbnail.\n        # First check if any of the representations have\n        # `need_thumbnail` in tags and add them to filtered_repres\n        need_thumb_repres = [\n            repre for repre in src_repres\n            if \"need_thumbnail\" in repre.get(\"tags\", [])\n            if \"publish_on_farm\" not in repre.get(\"tags\", [])\n        ]\n        if not need_thumb_repres:\n            return []\n\n        self.log.info(\n            \"Instance has representation with tag `need_thumbnail`. \"\n            \"Using only this representations for thumbnail creation. \"\n        )\n        self.log.debug(\n            \"Representations: {}\".format(need_thumb_repres)\n        )\n        return need_thumb_repres\n\n    def _get_filtered_repres(self, instance):\n        review_repres = []\n        other_repres = []\n        src_repres = instance.data.get(\"representations\") or []\n\n        for repre in src_repres:\n            self.log.debug(repre)\n            tags = repre.get(\"tags\") or []\n\n            if \"publish_on_farm\" in tags:\n                # only process representations with are going\n                # to be published locally\n                continue\n\n            if not repre.get(\"files\"):\n                self.log.debug((\n                    \"Representation \\\"{}\\\" doesn't have files. Skipping\"\n                ).format(repre[\"name\"]))\n                continue\n\n            if \"review\" in tags:\n                review_repres.append(repre)\n            elif self._is_valid_images_repre(repre):\n                other_repres.append(repre)\n\n        return review_repres + other_repres\n\n    def _is_valid_images_repre(self, repre: dict) -&gt; bool:\n        \"\"\"Check if representation contains valid image files\n\n        Args:\n            repre (dict): representation\n\n        Returns:\n            bool: whether the representation has the valid image content\n        \"\"\"\n        # Get first file's extension\n        first_file = repre[\"files\"]\n        if isinstance(first_file, (list, tuple)):\n            first_file = first_file[0]\n\n        ext = os.path.splitext(first_file)[1].lower()\n\n        return ext in IMAGE_EXTENSIONS or ext in VIDEO_EXTENSIONS\n\n    def _create_colorspace_thumbnail(\n        self,\n        src_path: str,\n        dst_path: str,\n        repre: dict,\n        thumbnail_def: ThumbnailDef,\n        anatomy: \"Anatomy\",\n        review_layers: list[str],\n    ) -&gt; bool:\n        \"\"\"Create thumbnail using OIIO tool oiiotool\n\n        Args:\n            src_path (str): path to source file\n            dst_path (str): path to destination file\n            repre (dict): colorspace data from representation\n                keys:\n                    colorspace (str)\n                    config (dict)\n                    display (Optional[str])\n                    view (Optional[str])\n            thumbnail_def (ThumbnailDefinition): Thumbnail definition.\n            anatomy (Anatomy): Current project Anatomy.\n            review_layers (list[str]): List of reviewable layers.\n\n        Returns:\n            bool: Whether a thumbnail has been created.\n        \"\"\"\n\n        ocio_config_path = get_representation_ocio_config_path(\n            repre,\n            anatomy=anatomy,\n            logger=self.log\n        )\n        if not ocio_config_path:\n            self.log.debug(\"Unable to find representation OCIO file.\")\n            return False\n\n        colorspace_data: dict = repre[\"colorspaceData\"]\n\n        self.log.info(f\"Extracting thumbnail {dst_path}\")\n        resolution_arg = self._get_resolution_args(\n            \"oiiotool\", src_path, thumbnail_def, review_layers\n        )\n\n        repre_display = colorspace_data.get(\"display\")\n        repre_view = colorspace_data.get(\"view\")\n        oiio_default_display = None\n        oiio_default_view = None\n        oiio_default_colorspace = None\n        # first look into representation colorspaceData, perhaps it has\n        #   display and view\n        if all([repre_display, repre_view]):\n            self.log.info(\n                \"Using Display &amp; View from \"\n                \"representation: '{} ({})'\".format(\n                    repre_view,\n                    repre_display\n                )\n            )\n        # if representation doesn't have display and view then use\n        #   oiiotool_defaults\n        elif thumbnail_def.oiiotool_defaults:\n            oiiotool_defaults = thumbnail_def.oiiotool_defaults\n            oiio_default_type = oiiotool_defaults[\"type\"]\n            if \"colorspace\" == oiio_default_type:\n                oiio_default_colorspace = oiiotool_defaults[\"colorspace\"]\n            else:\n                display_and_view = oiiotool_defaults[\"display_and_view\"]\n                oiio_default_display = display_and_view[\"display\"]\n                oiio_default_view = display_and_view[\"view\"]\n\n        try:\n            oiio_color_convert(\n                src_path,\n                dst_path,\n                ocio_config_path,\n                colorspace_data[\"colorspace\"],\n                source_display=colorspace_data.get(\"display\"),\n                source_view=colorspace_data.get(\"view\"),\n                target_display=repre_display or oiio_default_display,\n                target_view=repre_view or oiio_default_view,\n                target_colorspace=oiio_default_colorspace,\n                additional_command_args=resolution_arg,\n                review_layers=review_layers,\n                logger=self.log,\n            )\n        except Exception:\n            self.log.warning(\n                \"Failed to create thumbnail using oiiotool\",\n                exc_info=True\n            )\n            return False\n\n        return True\n\n    def _create_thumbnail_oiio(\n        self,\n        src_path: str,\n        dst_path: str,\n        thumbnail_def: ThumbnailDef,\n        review_layers: list[str],\n    ) -&gt; bool:\n        \"\"\"Create thumbnail using OIIO tool\n\n        Args:\n            src_path (str): source file path\n            dst_path (str): destination file path\n            thumbnail_def (ThumbnailDef): Thumbnail definition.\n            review_layers (list[str]): List of reviewable layers.\n\n        Returns:\n            bool: Whether the thumbnail was successfully created.\n\n        \"\"\"\n        self.log.debug(f\"Extracting thumbnail with OIIO: {dst_path}\")\n\n        try:\n            resolution_arg = self._get_resolution_args(\n                \"oiiotool\", src_path, thumbnail_def, review_layers\n            )\n        except RuntimeError:\n            self.log.warning(\n                \"Failed to create thumbnail using oiio\", exc_info=True\n            )\n            return False\n\n        input_info = get_oiio_info_for_input(\n            src_path,\n            logger=self.log,\n            verbose=False,\n        )\n        try:\n            input_arg, channels_arg = get_oiio_input_and_channel_args(\n                input_info, review_layers=review_layers\n            )\n        except MissingRGBAChannelsError:\n            self.log.debug(\n                \"Unable to find relevant reviewable channel for thumbnail \"\n                \"creation\"\n            )\n            return False\n        oiio_cmd = get_oiio_tool_args(\n            \"oiiotool\",\n            input_arg, src_path,\n            # Tell oiiotool which channels should be put to top stack\n            #   (and output)\n            \"--ch\", channels_arg,\n            # Use first subimage\n            \"--subimage\", \"0\"\n        )\n        oiio_cmd.extend(resolution_arg)\n        oiio_cmd.extend((\"-o\", dst_path))\n        self.log.debug(\"Running: {}\".format(\" \".join(oiio_cmd)))\n        try:\n            run_subprocess(oiio_cmd, logger=self.log)\n            return True\n        except Exception:\n            self.log.warning(\n                \"Failed to create thumbnail using oiiotool\",\n                exc_info=True\n            )\n            return False\n\n    def _create_thumbnail_ffmpeg(\n        self,\n        src_path: str,\n        dst_path: str,\n        thumbnail_def: ThumbnailDef,\n        review_layers: list[str],\n) -&gt; bool:\n        \"\"\"Create thumbnail using FFmpeg tool\n\n        Args:\n            src_path (str): source file path\n            dst_path (str): destination file path\n            thumbnail_def (ThumbnailDef): Thumbnail definition.\n            review_layers (list[str]): List of reviewable layers.\n\n        Returns:\n            bool: Whether the thumbnail was successfully created.\n\n        \"\"\"\n        try:\n            resolution_arg = self._get_resolution_args(\n                \"ffmpeg\", src_path, thumbnail_def, review_layers\n            )\n        except RuntimeError:\n            self.log.warning(\n                \"Failed to create thumbnail using ffmpeg\", exc_info=True\n            )\n            return False\n\n        ffmpeg_path_args = get_ffmpeg_tool_args(\"ffmpeg\")\n        ffmpeg_args = thumbnail_def.ffmpeg_args or {}\n\n        jpeg_items = [\n            subprocess.list2cmdline(ffmpeg_path_args)\n        ]\n        # flag for large file sizes\n        max_int = 2147483647\n        jpeg_items.extend([\n            \"-y\",\n            \"-analyzeduration\", str(max_int),\n            \"-probesize\", str(max_int),\n        ])\n        # use same input args like with mov\n        jpeg_items.extend(ffmpeg_args.get(\"input\") or [])\n        # input file\n        jpeg_items.extend([\"-i\", path_to_subprocess_arg(src_path)])\n        # output arguments from presets\n        jpeg_items.extend(ffmpeg_args.get(\"output\") or [])\n        # we just want one frame from movie files\n        jpeg_items.extend([\"-frames:v\", \"1\"])\n\n        if resolution_arg:\n            jpeg_items.extend(resolution_arg)\n\n        # output file\n        jpeg_items.append(path_to_subprocess_arg(dst_path))\n        subprocess_command = \" \".join(jpeg_items)\n\n        try:\n            run_subprocess(\n                subprocess_command, shell=True, logger=self.log\n            )\n            return True\n        except Exception:\n            self.log.warning(\n                \"Failed to create thumbnail using ffmpeg\",\n                exc_info=True\n            )\n            return False\n\n    def _create_frame_from_video(\n        self,\n        video_file_path: str,\n        output_dir: str,\n        thumbnail_def: ThumbnailDef,\n    ) -&gt; Optional[str]:\n        \"\"\"Convert video file to one frame image via ffmpeg\"\"\"\n        # create output file path\n        base_name = os.path.basename(video_file_path)\n        filename = os.path.splitext(base_name)[0]\n        output_thumb_file_path = os.path.join(\n            output_dir, \"{}.exr\".format(filename))\n\n        # Set video input attributes\n        max_int = str(2147483647)\n        video_data = get_ffprobe_data(video_file_path, logger=self.log)\n\n        # Get duration or use a safe default (single frame)\n        duration = 0\n        for stream in video_data[\"streams\"]:\n            if stream.get(\"codec_type\") == \"video\":\n                stream_duration = float(stream.get(\"duration\", 0))\n                if stream_duration &gt; duration:\n                    duration = stream_duration\n\n        # For very short videos, just use the first frame\n        # Calculate seek position safely\n        seek_position = 0.0\n        # Only use timestamp calculation for videos longer than 0.1 seconds\n        if duration &gt; 0.1:\n            seek_position = duration * thumbnail_def.duration_split\n\n        # Build command args\n        cmd_args = []\n        if seek_position &gt; 0.0:\n            cmd_args.extend([\"-ss\", str(seek_position)])\n\n        # Add generic ffmpeg commands\n        cmd_args.extend([\n            \"-i\", video_file_path,\n            \"-analyzeduration\", max_int,\n            \"-probesize\", max_int,\n            \"-y\",\n            \"-frames:v\", \"1\",\n            output_thumb_file_path\n        ])\n\n        # create ffmpeg command\n        cmd = get_ffmpeg_tool_args(\n            \"ffmpeg\",\n            *cmd_args\n        )\n        try:\n            # run subprocess\n            self.log.debug(\"Executing: {}\".format(\" \".join(cmd)))\n            run_subprocess(cmd, logger=self.log)\n\n            # Verify the output file was created\n            if (\n                os.path.exists(output_thumb_file_path)\n                and os.path.getsize(output_thumb_file_path) &gt; 0\n            ):\n                self.log.debug(\n                    \"Thumbnail created: {}\".format(output_thumb_file_path))\n                return output_thumb_file_path\n            self.log.warning(\"Output file was not created or is empty\")\n\n            # Try to create thumbnail without offset\n            # - skip if offset did not happen\n            if \"-ss\" not in cmd_args:\n                return None\n\n            self.log.debug(\"Trying fallback without offset\")\n            # Remove -ss and its value\n            ss_index = cmd_args.index(\"-ss\")\n            cmd_args.pop(ss_index)  # Remove -ss\n            cmd_args.pop(ss_index)  # Remove the timestamp value\n\n            # Create new command and try again\n            cmd = get_ffmpeg_tool_args(\"ffmpeg\", *cmd_args)\n            self.log.debug(\"Fallback command: {}\".format(\" \".join(cmd)))\n            run_subprocess(cmd, logger=self.log)\n\n            if (\n                os.path.exists(output_thumb_file_path)\n                and os.path.getsize(output_thumb_file_path) &gt; 0\n            ):\n                self.log.debug(\"Fallback thumbnail created\")\n                return output_thumb_file_path\n            return None\n        except RuntimeError as error:\n            self.log.warning(\n                \"Failed intermediate thumb source using ffmpeg: {}\".format(\n                    error)\n            )\n            return None\n        finally:\n            # Remove output file if is empty\n            if (\n                os.path.exists(output_thumb_file_path)\n                and os.path.getsize(output_thumb_file_path) == 0\n            ):\n                os.remove(output_thumb_file_path)\n\n    def _get_resolution_args(\n        self,\n        application: str,\n        input_path: str,\n        thumbnail_def: ThumbnailDef,\n        review_layers: list[str],\n    ) -&gt; list:\n        \"\"\"Create command arguments for rescaling.\n\n        Args:\n            application (str): Application name.\n            input_path (str): Input file path.\n            thumbnail_def (ThumbnailDef): Thumbnail definition.\n            review_layers (list[str]): List of reviewable layers.\n\n        Returns:\n            list: List of command arguments for rescaling.\n        \"\"\"\n        # get settings\n        if thumbnail_def.target_size[\"type\"] == \"source\":\n            return []\n\n        resize = thumbnail_def.target_size[\"resize\"]\n        target_width = resize[\"width\"]\n        target_height = resize[\"height\"]\n\n        # form arg string per application\n        return get_rescaled_command_arguments(\n            application,\n            input_path,\n            target_width,\n            target_height,\n            bg_color=thumbnail_def.background_color,\n            review_layers=review_layers,\n            log=self.log,\n        )\n\n    def _get_config_from_profile(\n        self,\n        instance: pyblish.api.Instance\n    ) -&gt; Optional[ThumbnailDef]:\n        \"\"\"Returns profile if and how repre should be color transcoded.\"\"\"\n        host_name = instance.context.data[\"hostName\"]\n        product_base_type = instance.data.get(\"productBaseType\")\n        if not product_base_type:\n            product_base_type = instance.data[\"productType\"]\n        product_name = instance.data[\"productName\"]\n        task_data = instance.data[\"anatomyData\"].get(\"task\", {})\n        task_name = task_data.get(\"name\")\n        task_type = task_data.get(\"type\")\n        filtering_criteria = {\n            \"host_names\": host_name,\n            \"product_base_types\": product_base_type,\n            \"product_names\": product_name,\n            \"task_names\": task_name,\n            \"task_types\": task_type,\n        }\n        profile = filter_profiles(\n            self.profiles,\n            filtering_criteria,\n            logger=self.log\n        )\n\n        if not profile:\n            self.log.debug(\n                \"Skipped instance. None of profiles in presets are for\"\n                f' Host name: \"{host_name}\"'\n                f' | Product base type: \"{product_base_type}\"'\n                f' | Product name: \"{product_name}\"'\n                f' | Task name \"{task_name}\"'\n                f' | Task type \"{task_type}\"'\n            )\n            return None\n\n        return ThumbnailDef.from_dict(profile)\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_thumbnail.html#client.ayon_core.plugins.publish.extract_thumbnail.ThumbnailDef","title":"<code>ThumbnailDef</code>  <code>dataclass</code>","text":"<p>Data class representing the full configuration for selected profile</p> <p>Any change of controllable fields in Settings must propagate here!</p> Source code in <code>client/ayon_core/plugins/publish/extract_thumbnail.py</code> <pre><code>@dataclass\nclass ThumbnailDef:\n    \"\"\"\n    Data class representing the full configuration for selected profile\n\n    Any change of controllable fields in Settings must propagate here!\n    \"\"\"\n    integrate_thumbnail: bool = False\n\n    target_size: Dict[str, Any] = field(\n        default_factory=lambda: {\n            \"type\": \"source\",\n            \"resize\": {\"width\": 1920, \"height\": 1080},\n        }\n    )\n\n    duration_split: float = 0.5\n\n    oiiotool_defaults: Dict[str, str] = field(\n        default_factory=lambda: {\n            \"type\": \"colorspace\",\n            \"colorspace\": \"color_picking\"\n        }\n    )\n\n    ffmpeg_args: Dict[str, List[Any]] = field(\n        default_factory=lambda: {\"input\": [], \"output\": []}\n    )\n\n    # Background color defined as (R, G, B, A) tuple.\n    # Note: Use float for alpha channel (0.0 to 1.0).\n    background_color: Tuple[int, int, int, float] = (0, 0, 0, 0.0)\n\n    @classmethod\n    def from_dict(cls, data: Dict[str, Any]) -&gt; \"ThumbnailDef\":\n        \"\"\"\n        Creates a ThumbnailDef instance from a dictionary, safely ignoring\n        any keys in the dictionary that are not fields in the dataclass.\n\n        Args:\n            data (Dict[str, Any]): The dictionary containing configuration data\n\n        Returns:\n            MediaConfig: A new instance of the dataclass.\n        \"\"\"\n        # Get all field names defined in the dataclass\n        field_names = {f.name for f in fields(cls)}\n\n        # Filter the input dictionary to include only keys matching field names\n        filtered_data = {k: v for k, v in data.items() if k in field_names}\n\n        # Unpack the filtered dictionary into the constructor\n        return cls(**filtered_data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_thumbnail.html#client.ayon_core.plugins.publish.extract_thumbnail.ThumbnailDef.from_dict","title":"<code>from_dict(data)</code>  <code>classmethod</code>","text":"<p>Creates a ThumbnailDef instance from a dictionary, safely ignoring any keys in the dictionary that are not fields in the dataclass.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>Dict[str, Any]</code> <p>The dictionary containing configuration data</p> required <p>Returns:</p> Name Type Description <code>MediaConfig</code> <code>ThumbnailDef</code> <p>A new instance of the dataclass.</p> Source code in <code>client/ayon_core/plugins/publish/extract_thumbnail.py</code> <pre><code>@classmethod\ndef from_dict(cls, data: Dict[str, Any]) -&gt; \"ThumbnailDef\":\n    \"\"\"\n    Creates a ThumbnailDef instance from a dictionary, safely ignoring\n    any keys in the dictionary that are not fields in the dataclass.\n\n    Args:\n        data (Dict[str, Any]): The dictionary containing configuration data\n\n    Returns:\n        MediaConfig: A new instance of the dataclass.\n    \"\"\"\n    # Get all field names defined in the dataclass\n    field_names = {f.name for f in fields(cls)}\n\n    # Filter the input dictionary to include only keys matching field names\n    filtered_data = {k: v for k, v in data.items() if k in field_names}\n\n    # Unpack the filtered dictionary into the constructor\n    return cls(**filtered_data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_thumbnail_from_source.html","title":"extract_thumbnail_from_source","text":"<p>Create instance thumbnail from \"thumbnailSource\" on 'instance.data'.</p> <p>Output is new representation with \"thumbnail\" name on instance. If instance already have such representation the process is skipped.</p> <p>This way a collector can point to a file from which should be thumbnail generated. This is different approach then what global plugin for thumbnails does. The global plugin has specific logic which does not support</p> Todos <p>No size handling. Size of input is used for output thumbnail which can     cause issues.</p>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_thumbnail_from_source.html#client.ayon_core.plugins.publish.extract_thumbnail_from_source.ExtractThumbnailFromSource","title":"<code>ExtractThumbnailFromSource</code>","text":"<p>               Bases: <code>InstancePlugin</code></p> <p>Create jpg thumbnail for instance based on 'thumbnailSource'.</p> <p>Thumbnail source must be a single image or video filepath.</p> Source code in <code>client/ayon_core/plugins/publish/extract_thumbnail_from_source.py</code> <pre><code>class ExtractThumbnailFromSource(pyblish.api.InstancePlugin):\n    \"\"\"Create jpg thumbnail for instance based on 'thumbnailSource'.\n\n    Thumbnail source must be a single image or video filepath.\n    \"\"\"\n\n    label = \"Extract Thumbnail from source\"\n    # Before 'ExtractThumbnail' in global plugins\n    order = pyblish.api.ExtractorOrder - 0.00001\n\n    # Settings\n    target_size = {\n        \"type\": \"resize\",\n        \"resize\": {\"width\": 1920, \"height\": 1080}\n    }\n    background_color = (0, 0, 0, 0.0)\n\n    def process(self, instance: pyblish.api.Instance):\n        self._create_context_thumbnail(instance.context)\n\n        thumbnail_source = instance.data.get(\"thumbnailSource\")\n        if not thumbnail_source:\n            self.log.debug(\"Thumbnail source not filled. Skipping.\")\n            return\n\n        # Check if already has thumbnail created\n        if self._instance_has_thumbnail(instance):\n            self.log.debug(\"Thumbnail representation already present.\")\n            return\n\n        dst_filepath = self._create_thumbnail(\n            instance.context, thumbnail_source,\n        )\n        if not dst_filepath:\n            return\n\n        dst_staging, dst_filename = os.path.split(dst_filepath)\n        new_repre = {\n            \"name\": \"thumbnail\",\n            \"ext\": \"jpg\",\n            \"files\": dst_filename,\n            \"stagingDir\": dst_staging,\n            \"thumbnail\": True,\n            \"tags\": [\"thumbnail\"],\n            \"outputName\": \"thumbnail\",\n        }\n\n        new_repre[\"tags\"].append(\"delete\")\n\n        # adding representation\n        self.log.debug(\n            \"Adding thumbnail representation: {}\".format(new_repre)\n        )\n        instance.data[\"representations\"].append(new_repre)\n        instance.data[\"thumbnailPath\"] = dst_filepath\n\n    def _create_thumbnail(\n        self,\n        context: pyblish.api.Context,\n        thumbnail_source: str,\n    ) -&gt; Optional[str]:\n        if not thumbnail_source:\n            self.log.debug(\"Thumbnail source not filled. Skipping.\")\n            return\n\n        if not os.path.exists(thumbnail_source):\n            self.log.debug((\n                \"Thumbnail source is set but file was not found {}. Skipping.\"\n            ).format(thumbnail_source))\n            return\n\n        # Create temp directory for thumbnail\n        # - this is to avoid \"override\" of source file\n        dst_staging = tempfile.mkdtemp(prefix=\"pyblish_tmp_\")\n        self.log.debug(\n            \"Create temp directory {} for thumbnail\".format(dst_staging)\n        )\n        # Store new staging to cleanup paths\n        context.data[\"cleanupFullPaths\"].append(dst_staging)\n        project_settings = context.data[\"project_settings\"]\n        review_layers = get_default_reviewable_layers(project_settings)\n\n        thumbnail_created = False\n        oiio_supported = is_oiio_supported()\n\n        self.log.debug(\"Thumbnail source: {}\".format(thumbnail_source))\n        src_basename = os.path.basename(thumbnail_source)\n        dst_filename = os.path.splitext(src_basename)[0] + \"_thumb.jpg\"\n        full_output_path = os.path.join(dst_staging, dst_filename)\n\n        if oiio_supported:\n            self.log.debug(\"Trying to convert with OIIO\")\n            # If the input can read by OIIO then use OIIO method for\n            # conversion otherwise use ffmpeg\n            thumbnail_created = self.create_thumbnail_oiio(\n                thumbnail_source, full_output_path, review_layers\n            )\n\n        # Try to use FFMPEG if OIIO is not supported or for cases when\n        #    oiiotool isn't available\n        if not thumbnail_created:\n            if oiio_supported:\n                self.log.info(\n                    \"Converting with FFMPEG because input\"\n                    \" can't be read by OIIO.\"\n                )\n\n            thumbnail_created = self.create_thumbnail_ffmpeg(\n                thumbnail_source, full_output_path, review_layers\n            )\n\n        # Skip representation and try next one if  wasn't created\n        if thumbnail_created:\n            return full_output_path\n\n        self.log.warning(\"Thumbnail has not been created.\")\n\n    def _instance_has_thumbnail(self, instance: pyblish.api.Instance) -&gt; bool:\n        if \"representations\" not in instance.data:\n            self.log.warning(\n                \"Instance does not have 'representations' key filled\"\n            )\n            instance.data[\"representations\"] = []\n\n        for repre in instance.data[\"representations\"]:\n            if repre[\"name\"] == \"thumbnail\":\n                return True\n        return False\n\n    def create_thumbnail_oiio(\n        self,\n        src_path: str,\n        dst_path: str,\n        review_layers: list[str],\n    ) -&gt; bool:\n        self.log.debug(\"Outputting thumbnail with OIIO: {}\".format(dst_path))\n        try:\n            resolution_args = self._get_resolution_args(\n                \"oiiotool\", src_path, review_layers\n            )\n        except Exception:\n            self.log.warning(\"Failed to get resolution args for OIIO.\")\n            return False\n\n        oiio_cmd = get_oiio_tool_args(\"oiiotool\", \"-a\", src_path)\n        if resolution_args:\n            # resize must be before -o\n            oiio_cmd.extend(resolution_args)\n        else:\n            # resize provides own -ch, must be only one\n            oiio_cmd.extend([\"--ch\", \"R,G,B\"])\n\n        oiio_cmd.extend([\"-o\", dst_path])\n        self.log.debug(\"Running: {}\".format(\" \".join(oiio_cmd)))\n        try:\n            run_subprocess(oiio_cmd, logger=self.log)\n            return True\n        except Exception:\n            self.log.warning(\n                \"Failed to create thumbnail using oiiotool\",\n                exc_info=True\n            )\n            return False\n\n    def create_thumbnail_ffmpeg(\n        self,\n        src_path: str,\n        dst_path: str,\n        review_layers: list[str],\n    ) -&gt; bool:\n        try:\n            resolution_args = self._get_resolution_args(\n                \"ffmpeg\", src_path, review_layers\n            )\n        except Exception:\n            self.log.warning(\"Failed to get resolution args for ffmpeg.\")\n            return False\n\n        max_int = str(2147483647)\n        ffmpeg_cmd = get_ffmpeg_tool_args(\n            \"ffmpeg\",\n            \"-y\",\n            \"-analyzeduration\", max_int,\n            \"-probesize\", max_int,\n            \"-i\", src_path,\n            \"-frames:v\", \"1\",\n        )\n\n        ffmpeg_cmd.extend(resolution_args)\n\n        # possible resize must be before output args\n        ffmpeg_cmd.append(dst_path)\n\n        self.log.debug(\"Running: {}\".format(\" \".join(ffmpeg_cmd)))\n        try:\n            run_subprocess(ffmpeg_cmd, logger=self.log)\n            return True\n        except Exception:\n            self.log.warning(\n                \"Failed to create thumbnail using ffmpeg\",\n                exc_info=True\n            )\n            return False\n\n    def _create_context_thumbnail(\n        self,\n        context: pyblish.api.Context,\n    ):\n        if \"thumbnailPath\" in context.data:\n            return\n\n        thumbnail_source = context.data.get(\"thumbnailSource\")\n        context.data[\"thumbnailPath\"] = self._create_thumbnail(\n            context, thumbnail_source\n        )\n\n    def _get_resolution_args(\n        self,\n        application: str,\n        input_path: str,\n        review_layers: list[str],\n    ) -&gt; List[str]:\n        # get settings\n        if self.target_size[\"type\"] == \"source\":\n            return []\n\n        resize = self.target_size[\"resize\"]\n        target_width = resize[\"width\"]\n        target_height = resize[\"height\"]\n\n        # form arg string per application\n        return get_rescaled_command_arguments(\n            application,\n            input_path,\n            target_width,\n            target_height,\n            bg_color=self.background_color,\n            log=self.log,\n            review_layers=review_layers\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_usd_layer_contributions.html","title":"extract_usd_layer_contributions","text":""},{"location":"autoapi/client/ayon_core/plugins/publish/extract_usd_layer_contributions.html#client.ayon_core.plugins.publish.extract_usd_layer_contributions.CollectUSDLayerContributions","title":"<code>CollectUSDLayerContributions</code>","text":"<p>               Bases: <code>InstancePlugin</code>, <code>AYONPyblishPluginMixin</code></p> <p>Collect the USD Layer Contributions and create dependent instances.</p> <p>Our contributions go to the layer</p> <pre><code>Instance representation -&gt; Department Layer -&gt; Asset\n</code></pre> So that for example <p>modelMain --&gt; variant 'main' in model.usd -&gt; asset.usd modelDamaged --&gt; variant 'damaged' in model.usd -&gt; asset.usd</p> Source code in <code>client/ayon_core/plugins/publish/extract_usd_layer_contributions.py</code> <pre><code>class CollectUSDLayerContributions(pyblish.api.InstancePlugin,\n                                   publish.AYONPyblishPluginMixin):\n    \"\"\"Collect the USD Layer Contributions and create dependent instances.\n\n    Our contributions go to the layer\n\n        Instance representation -&gt; Department Layer -&gt; Asset\n\n    So that for example:\n        modelMain --&gt; variant 'main' in model.usd -&gt; asset.usd\n        modelDamaged --&gt; variant 'damaged' in model.usd -&gt; asset.usd\n\n    \"\"\"\n\n    order = pyblish.api.CollectorOrder + 0.35\n    label = \"Collect USD Layer Contributions (Asset/Shot)\"\n    families = [\"usd\"]\n    enabled = True\n    settings_category = \"core\"\n\n    # A contribution defines a contribution into a (department) layer which\n    # will get layered into the target product, usually the asset or shot.\n    # We need to at least know what it targets (e.g. where does it go into) and\n    # in what order (which contribution is stronger?)\n    # Preferably the bootstrapped data (e.g. the Shot) preserves metadata about\n    # the contributions so that we can design a system where custom\n    # contributions outside the predefined orders are possible to be\n    # managed. So that if a particular asset requires an extra contribution\n    # level, you can add it directly from the publisher at that particular\n    # order. Future publishes will then see the existing contribution and will\n    # persist adding it to future bootstraps at that order\n    contribution_layers: Dict[str, Dict[str, int]] = {\n        # asset layers\n        \"asset\": {\n            \"model\": 100,\n            \"assembly\": 150,\n            \"groom\": 175,\n            \"look\": 200,\n            \"rig\": 300,\n        },\n        # shot layers\n        \"shot\": {\n            \"layout\": 200,\n            \"animation\": 300,\n            \"simulation\": 400,\n            \"fx\": 500,\n            \"lighting\": 600,\n        }\n    }\n    # Default profiles to set certain instance attribute defaults based on\n    # profiles in settings\n    profiles: List[Dict[str, Any]] = []\n\n    @classmethod\n    def apply_settings(cls, project_settings):\n        # Override contribution_layers logic to turn data into Dict[str, int]\n        plugin_settings = project_settings[\"core\"][\"publish\"].get(\n            \"CollectUSDLayerContributions\", {}\n        )\n\n        cls.enabled = plugin_settings.get(\"enabled\", cls.enabled)\n\n        # Define contribution layers via settings by their scope\n        contribution_layers = defaultdict(dict)\n        for entry in plugin_settings.get(\"contribution_layers\", []):\n            for scope in entry.get(\"scope\", []):\n                contribution_layers[scope][entry[\"name\"]] = int(entry[\"order\"])\n        if contribution_layers:\n            cls.contribution_layers = dict(contribution_layers)\n        else:\n            cls.log.warning(\n                \"No scoped contribution layers found in settings, falling back\"\n                \" to CollectUSDLayerContributions plug-in defaults...\"\n            )\n\n        cls.profiles = plugin_settings.get(\"profiles\", [])\n\n    def process(self, instance):\n\n        attr_values = self.get_attr_values_from_data(instance.data)\n        if not attr_values.get(\"contribution_enabled\"):\n            return\n\n        instance.data[\"productGroup\"] = (\n            instance.data.get(\"productGroup\") or \"USD Layer\"\n        )\n\n        # Allow formatting in variant set name and variant name\n        data = instance.data.copy()\n        data[\"layer\"] = attr_values[\"contribution_layer\"]\n        for key in [\n            \"contribution_variant_set_name\",\n            \"contribution_variant\"\n        ]:\n            attr_values[key] = attr_values[key].format(**data)\n\n        # Define contribution\n        in_layer_order: int = attr_values.get(\"contribution_in_layer_order\", 0)\n        if attr_values[\"contribution_apply_as_variant\"]:\n            contribution = VariantContribution(\n                instance=instance,\n                layer_id=attr_values[\"contribution_layer\"],\n                target_product=attr_values[\"contribution_target_product\"],\n                variant_set_name=attr_values[\"contribution_variant_set_name\"],\n                variant_name=attr_values[\"contribution_variant\"],\n                variant_is_default=attr_values[\"contribution_variant_is_default\"],  # noqa: E501\n                order=in_layer_order\n            )\n        else:\n            contribution = SublayerContribution(\n                instance=instance,\n                layer_id=attr_values[\"contribution_layer\"],\n                target_product=attr_values[\"contribution_target_product\"],\n                order=in_layer_order\n            )\n\n        asset_product = contribution.target_product\n        layer_product = \"{}_{}\".format(asset_product, contribution.layer_id)\n\n        scope: str = attr_values[\"contribution_target_product_init\"]\n        layer_order: int = (\n            self.contribution_layers[scope][attr_values[\"contribution_layer\"]]\n        )\n        # Layer contribution instance\n        layer_instance = self.get_or_create_instance(\n            product_name=layer_product,\n            variant=contribution.layer_id,\n            source_instance=instance,\n            families=[\"usd\", \"usdLayer\"],\n        )\n        layer_instance.data.setdefault(\"usd_contributions\", []).append(\n            contribution\n        )\n        layer_instance.data[\"usd_layer_id\"] = contribution.layer_id\n        layer_instance.data[\"usd_layer_order\"] = layer_order\n\n        layer_instance.data[\"productGroup\"] = (\n            instance.data.get(\"productGroup\") or \"USD Layer\"\n        )\n\n        # Asset/Shot contribution instance\n        target_instance = self.get_or_create_instance(\n            product_name=asset_product,\n            variant=asset_product,\n            source_instance=layer_instance,\n            families=[\"usd\", \"usdAsset\"],\n        )\n        target_instance.data[\"contribution_target_product_init\"] = attr_values[\n            \"contribution_target_product_init\"\n        ]\n\n        self.log.info(\n            f\"Contributing {instance.data['productName']} to \"\n            f\"{layer_product} -&gt; {asset_product}\"\n        )\n\n    def find_instance(self, context, data, ignore_instance):\n        \"\"\"Return instance in context that has matching `instance.data`.\n\n        If no matching instance is found, then `None` is returned.\n        \"\"\"\n        for instance in context:\n            if instance is ignore_instance:\n                continue\n\n            if all(instance.data.get(key) == value\n                   for key, value in data.items()):\n                return instance\n\n    def get_or_create_instance(self,\n                               product_name,\n                               variant,\n                               source_instance,\n                               families):\n        \"\"\"Get or create the instance matching the product/variant.\n\n        The source instance will be used to do additional matching, like\n        ensuring it's a product for the same asset and task. If the instance\n        already exists in the `context` then the existing one is returned.\n\n        For each source instance this is called the sources will be appended\n        to a `instance.data[\"source_instances\"]` list on the returned instance.\n\n        Arguments:\n            product_name (str): product name\n            variant (str): Variant name\n            source_instance (pyblish.api.Instance): Source instance to\n                be related to for asset, task.\n            families (list): The families required to be set on the instance.\n\n        Returns:\n            pyblish.api.Instance: The resulting instance.\n\n        \"\"\"\n\n        # Potentially the instance already exists due to multiple instances\n        # contributing to the same layer or asset - so we first check for\n        # existence\n        context = source_instance.context\n        task_name = source_instance.data.get(\"task\")\n\n        # Required matching vars\n        data = {\n            \"folderPath\": source_instance.data[\"folderPath\"],\n            \"productName\": product_name,\n            \"variant\": variant,\n            \"families\": families\n        }\n        existing_instance = self.find_instance(context, data,\n                                               ignore_instance=source_instance)\n        if existing_instance:\n            existing_instance.append(source_instance.id)\n            existing_instance.data[\"source_instances\"].append(source_instance)\n\n            # If the instance already exists, we want to ensure the task name\n            # is set if any of the source instances specify it.\n            existing_instance_task_name = existing_instance.data.get(\"task\")\n            if task_name and existing_instance_task_name != task_name:\n                if existing_instance_task_name:\n                    # Log the difference, but do not change task name\n                    # on the existing instance\n                    self.log.debug(\n                        \"Instance has different task name\"\n                        f\" '{existing_instance_task_name}' than source\"\n                        f\" instance '{task_name}' because it already \"\n                        \" inherited the task name from another source \"\n                        \" instance.\"\n                    )\n                else:\n                    # Set the task name\n                    existing_instance.data[\"task\"] = task_name\n\n            return existing_instance\n\n        # Otherwise create the instance\n        product_base_type = \"usd\"\n        new_instance = context.create_instance(name=product_name)\n        new_instance.data.update(data)\n\n        new_instance.data[\"label\"] = (\n            \"{0} ({1})\".format(product_name, new_instance.data[\"folderPath\"])\n        )\n        new_instance.data[\"family\"] = product_base_type\n        new_instance.data[\"productBaseType\"] = product_base_type\n        new_instance.data[\"productType\"] = product_base_type\n        new_instance.data[\"icon\"] = \"link\"\n        new_instance.data[\"comment\"] = \"Automated bootstrap USD file.\"\n        new_instance.append(source_instance.id)\n        new_instance.data[\"source_instances\"] = [source_instance]\n        if task_name:\n            new_instance.data[\"task\"] = task_name\n\n        # The contribution target publishes should never match versioning of\n        # the workfile but should just always increment from their last version\n        # so that there will never be conflicts between contributions from\n        # different departments and scenes.\n        new_instance.data[\"followWorkfileVersion\"] = False\n\n        # Transfer any creator and publish attributes, to ensure any optional\n        # validators that may also apply to these instances will have the\n        # state inherited from its parent\n        for key in (\"creator_attributes\", \"publish_attributes\"):\n            if key in source_instance.data:\n                new_instance.data[key] = copy.deepcopy(\n                    source_instance.data[key]\n                )\n\n        return new_instance\n\n    @classmethod\n    def get_attr_defs_for_instance(cls, create_context, instance):\n        # Filtering of instance, if needed, can be customized\n        if not cls.instance_matches_plugin_families(instance):\n            return []\n\n        # Set default target layer based on product type\n        current_context_task_type = create_context.get_current_task_type()\n        product_base_type = instance.data.get(\"productBaseType\")\n        if not product_base_type:\n            product_base_type = instance.data[\"productType\"]\n        filtering_criteria = {\n            \"product_base_types\": product_base_type,\n            \"task_types\": current_context_task_type,\n            \"task_names\": create_context.get_current_task_name()\n        }\n        profile = filter_profiles(cls.profiles, filtering_criteria)\n        if not profile:\n            profile = {\n                \"contribution_enabled\": True,\n                \"contribution_layer\": None,\n                \"contribution_target_product\": \"usdAsset\",\n                \"contribution_apply_as_variant\": False,\n                \"contribution_variant_set_name\": \"{layer}\",\n                \"contribution_variant\": \"{variant}\",\n                \"contribution_variant_is_default\": False,\n            }\n\n        # Define defaults\n        default_target_product: str = profile[\"contribution_target_product\"]\n        default_init_as: str = (\n            \"asset\"\n            if default_target_product == \"usdAsset\"\n            else \"shot\"\n        )\n        init_as_visible = True\n\n        # Attributes logic\n        publish_attributes = instance[\"publish_attributes\"].get(\n            cls.__name__, {})\n\n        visible = publish_attributes.get(\"contribution_enabled\", True)\n        variant_visible = visible and publish_attributes.get(\n            \"contribution_apply_as_variant\", True)\n        init_as: str = publish_attributes.get(\n            \"contribution_target_product_init\", default_init_as)\n\n        contribution_layers = cls.contribution_layers.get(\n            init_as, {}\n        )\n\n        return [\n            UISeparatorDef(\"usd_container_settings1\"),\n            UILabelDef(label=\"&lt;b&gt;USD Contribution&lt;/b&gt;\"),\n            BoolDef(\"contribution_enabled\",\n                    label=\"Enable\",\n                    tooltip=(\n                        \"When enabled this publish instance will be added \"\n                        \"into a department layer into a target product, \"\n                        \"usually an asset or shot.\\n\"\n                        \"When disabled this publish instance will not be \"\n                        \"added into another USD file and remain as is.\\n\"\n                        \"In both cases the USD data itself is free to have \"\n                        \"references and sublayers of its own.\"\n                    ),\n                    default=profile[\"contribution_enabled\"]),\n            TextDef(\"contribution_target_product\",\n                    label=\"Target product\",\n                    tooltip=(\n                        \"The target product the contribution should be added \"\n                        \"to. Usually this is the asset or shot product.\\nThe \"\n                        \"department layer will be added to this product, and \"\n                        \"the contribution itself will be added to the \"\n                        \"department layer.\"\n                    ),\n                    default=default_target_product,\n                    visible=visible),\n            EnumDef(\"contribution_target_product_init\",\n                    label=\"Initialize as\",\n                    tooltip=(\n                        \"The target product's USD file will be initialized \"\n                        \"based on this type if there's no existing USD of \"\n                        \"that product yet.\\nIf there's already an existing \"\n                        \"product with the name of the 'target product' this \"\n                        \"setting will do nothing.\"\n                    ),\n                    items=[\"asset\", \"shot\"],\n                    default=default_init_as,\n                    visible=visible and init_as_visible),\n\n            # Asset layer, e.g. model.usd, look.usd, rig.usd\n            EnumDef(\"contribution_layer\",\n                    label=\"Add to department layer\",\n                    tooltip=(\n                        \"The layer the contribution should be made to in the \"\n                        \"target product.\\nThe layers have their own \"\n                        \"predefined ordering.\\nA higher order (further down \"\n                        \"the list) will contribute as a stronger opinion.\"\n                    ),\n                    items=list(contribution_layers.keys()),\n                    default=profile[\"contribution_layer\"],\n                    visible=visible),\n            # TODO: We may want to make the visibility of this optional\n            #  based on studio preference, to avoid complexity when not needed\n            NumberDef(\"contribution_in_layer_order\",\n                    label=\"Strength order\",\n                    tooltip=(\n                        \"The contribution inside the department layer will be \"\n                        \"made with this offset applied. A higher number means \"\n                        \"a stronger opinion.\"\n                    ),\n                    default=0,\n                    minimum=-99999,\n                    maximum=99999,\n                    visible=visible),\n            BoolDef(\"contribution_apply_as_variant\",\n                    label=\"Add as variant\",\n                    tooltip=(\n                        \"When enabled the contribution to the department \"\n                        \"layer will be added as a variant where the variant \"\n                        \"on the default root prim will be added as a \"\n                        \"reference.\\nWhen disabled the contribution will be \"\n                        \"appended to as a sublayer to the department layer \"\n                        \"instead.\"\n                    ),\n                    default=profile[\"contribution_apply_as_variant\"],\n                    visible=visible),\n            TextDef(\"contribution_variant_set_name\",\n                    label=\"Variant Set Name\",\n                    default=profile[\"contribution_variant_set_name\"],\n                    visible=variant_visible),\n            TextDef(\"contribution_variant\",\n                    label=\"Variant Name\",\n                    default=profile[\"contribution_variant\"],\n                    visible=variant_visible),\n            BoolDef(\"contribution_variant_is_default\",\n                    label=\"Set as default variant selection\",\n                    tooltip=(\n                        \"Whether to set this instance's variant name as the \"\n                        \"default selected variant name for the variant set.\\n\"\n                        \"It is always expected to be enabled for only one \"\n                        \"variant name in the variant set.\\n\"\n                        \"The behavior is unpredictable if multiple instances \"\n                        \"for the same variant set have this enabled.\"\n                    ),\n                    default=profile[\"contribution_variant_is_default\"],\n                    visible=variant_visible),\n            UISeparatorDef(\"usd_container_settings3\"),\n        ]\n\n    @classmethod\n    def register_create_context_callbacks(cls, create_context):\n        create_context.add_value_changed_callback(cls.on_values_changed)\n\n    @classmethod\n    def on_values_changed(cls, event):\n        \"\"\"Update instance attribute definitions on attribute changes.\"\"\"\n\n        # Update attributes if any of the following plug-in attributes\n        # change:\n        keys = {\n            \"contribution_enabled\",\n            \"contribution_apply_as_variant\",\n            \"contribution_target_product_init\",\n        }\n\n        for instance_change in event[\"changes\"]:\n            instance = instance_change[\"instance\"]\n            if not cls.instance_matches_plugin_families(instance):\n                continue\n            value_changes = instance_change[\"changes\"]\n            plugin_attribute_changes = (\n                value_changes.get(\"publish_attributes\", {})\n                .get(cls.__name__, {}))\n\n            if not any(key in plugin_attribute_changes for key in keys):\n                continue\n\n            # Update the attribute definitions\n            new_attrs = cls.get_attr_defs_for_instance(\n                event[\"create_context\"], instance\n            )\n            instance.set_publish_plugin_attr_defs(cls.__name__, new_attrs)\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_usd_layer_contributions.html#client.ayon_core.plugins.publish.extract_usd_layer_contributions.CollectUSDLayerContributions.find_instance","title":"<code>find_instance(context, data, ignore_instance)</code>","text":"<p>Return instance in context that has matching <code>instance.data</code>.</p> <p>If no matching instance is found, then <code>None</code> is returned.</p> Source code in <code>client/ayon_core/plugins/publish/extract_usd_layer_contributions.py</code> <pre><code>def find_instance(self, context, data, ignore_instance):\n    \"\"\"Return instance in context that has matching `instance.data`.\n\n    If no matching instance is found, then `None` is returned.\n    \"\"\"\n    for instance in context:\n        if instance is ignore_instance:\n            continue\n\n        if all(instance.data.get(key) == value\n               for key, value in data.items()):\n            return instance\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_usd_layer_contributions.html#client.ayon_core.plugins.publish.extract_usd_layer_contributions.CollectUSDLayerContributions.get_or_create_instance","title":"<code>get_or_create_instance(product_name, variant, source_instance, families)</code>","text":"<p>Get or create the instance matching the product/variant.</p> <p>The source instance will be used to do additional matching, like ensuring it's a product for the same asset and task. If the instance already exists in the <code>context</code> then the existing one is returned.</p> <p>For each source instance this is called the sources will be appended to a <code>instance.data[\"source_instances\"]</code> list on the returned instance.</p> <p>Parameters:</p> Name Type Description Default <code>product_name</code> <code>str</code> <p>product name</p> required <code>variant</code> <code>str</code> <p>Variant name</p> required <code>source_instance</code> <code>Instance</code> <p>Source instance to be related to for asset, task.</p> required <code>families</code> <code>list</code> <p>The families required to be set on the instance.</p> required <p>Returns:</p> Type Description <p>pyblish.api.Instance: The resulting instance.</p> Source code in <code>client/ayon_core/plugins/publish/extract_usd_layer_contributions.py</code> <pre><code>def get_or_create_instance(self,\n                           product_name,\n                           variant,\n                           source_instance,\n                           families):\n    \"\"\"Get or create the instance matching the product/variant.\n\n    The source instance will be used to do additional matching, like\n    ensuring it's a product for the same asset and task. If the instance\n    already exists in the `context` then the existing one is returned.\n\n    For each source instance this is called the sources will be appended\n    to a `instance.data[\"source_instances\"]` list on the returned instance.\n\n    Arguments:\n        product_name (str): product name\n        variant (str): Variant name\n        source_instance (pyblish.api.Instance): Source instance to\n            be related to for asset, task.\n        families (list): The families required to be set on the instance.\n\n    Returns:\n        pyblish.api.Instance: The resulting instance.\n\n    \"\"\"\n\n    # Potentially the instance already exists due to multiple instances\n    # contributing to the same layer or asset - so we first check for\n    # existence\n    context = source_instance.context\n    task_name = source_instance.data.get(\"task\")\n\n    # Required matching vars\n    data = {\n        \"folderPath\": source_instance.data[\"folderPath\"],\n        \"productName\": product_name,\n        \"variant\": variant,\n        \"families\": families\n    }\n    existing_instance = self.find_instance(context, data,\n                                           ignore_instance=source_instance)\n    if existing_instance:\n        existing_instance.append(source_instance.id)\n        existing_instance.data[\"source_instances\"].append(source_instance)\n\n        # If the instance already exists, we want to ensure the task name\n        # is set if any of the source instances specify it.\n        existing_instance_task_name = existing_instance.data.get(\"task\")\n        if task_name and existing_instance_task_name != task_name:\n            if existing_instance_task_name:\n                # Log the difference, but do not change task name\n                # on the existing instance\n                self.log.debug(\n                    \"Instance has different task name\"\n                    f\" '{existing_instance_task_name}' than source\"\n                    f\" instance '{task_name}' because it already \"\n                    \" inherited the task name from another source \"\n                    \" instance.\"\n                )\n            else:\n                # Set the task name\n                existing_instance.data[\"task\"] = task_name\n\n        return existing_instance\n\n    # Otherwise create the instance\n    product_base_type = \"usd\"\n    new_instance = context.create_instance(name=product_name)\n    new_instance.data.update(data)\n\n    new_instance.data[\"label\"] = (\n        \"{0} ({1})\".format(product_name, new_instance.data[\"folderPath\"])\n    )\n    new_instance.data[\"family\"] = product_base_type\n    new_instance.data[\"productBaseType\"] = product_base_type\n    new_instance.data[\"productType\"] = product_base_type\n    new_instance.data[\"icon\"] = \"link\"\n    new_instance.data[\"comment\"] = \"Automated bootstrap USD file.\"\n    new_instance.append(source_instance.id)\n    new_instance.data[\"source_instances\"] = [source_instance]\n    if task_name:\n        new_instance.data[\"task\"] = task_name\n\n    # The contribution target publishes should never match versioning of\n    # the workfile but should just always increment from their last version\n    # so that there will never be conflicts between contributions from\n    # different departments and scenes.\n    new_instance.data[\"followWorkfileVersion\"] = False\n\n    # Transfer any creator and publish attributes, to ensure any optional\n    # validators that may also apply to these instances will have the\n    # state inherited from its parent\n    for key in (\"creator_attributes\", \"publish_attributes\"):\n        if key in source_instance.data:\n            new_instance.data[key] = copy.deepcopy(\n                source_instance.data[key]\n            )\n\n    return new_instance\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_usd_layer_contributions.html#client.ayon_core.plugins.publish.extract_usd_layer_contributions.CollectUSDLayerContributions.on_values_changed","title":"<code>on_values_changed(event)</code>  <code>classmethod</code>","text":"<p>Update instance attribute definitions on attribute changes.</p> Source code in <code>client/ayon_core/plugins/publish/extract_usd_layer_contributions.py</code> <pre><code>@classmethod\ndef on_values_changed(cls, event):\n    \"\"\"Update instance attribute definitions on attribute changes.\"\"\"\n\n    # Update attributes if any of the following plug-in attributes\n    # change:\n    keys = {\n        \"contribution_enabled\",\n        \"contribution_apply_as_variant\",\n        \"contribution_target_product_init\",\n    }\n\n    for instance_change in event[\"changes\"]:\n        instance = instance_change[\"instance\"]\n        if not cls.instance_matches_plugin_families(instance):\n            continue\n        value_changes = instance_change[\"changes\"]\n        plugin_attribute_changes = (\n            value_changes.get(\"publish_attributes\", {})\n            .get(cls.__name__, {}))\n\n        if not any(key in plugin_attribute_changes for key in keys):\n            continue\n\n        # Update the attribute definitions\n        new_attrs = cls.get_attr_defs_for_instance(\n            event[\"create_context\"], instance\n        )\n        instance.set_publish_plugin_attr_defs(cls.__name__, new_attrs)\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_usd_layer_contributions.html#client.ayon_core.plugins.publish.extract_usd_layer_contributions.ExtractUSDAssetContribution","title":"<code>ExtractUSDAssetContribution</code>","text":"<p>               Bases: <code>Extractor</code></p> Source code in <code>client/ayon_core/plugins/publish/extract_usd_layer_contributions.py</code> <pre><code>class ExtractUSDAssetContribution(publish.Extractor):\n\n    families = [\"usdAsset\"]\n    label = \"Extract USD Asset/Shot Contributions\"\n    order = ExtractUSDLayerContribution.order + 0.01\n\n    settings_category = \"core\"\n\n    use_ayon_entity_uri = False\n\n    def process(self, instance):\n\n        folder_path = instance.data[\"folderPath\"]\n        product_name = instance.data[\"productName\"]\n        self.log.debug(f\"Building asset: {folder_path} &gt; {product_name}\")\n        asset_name = get_standard_default_prim_name(folder_path)\n\n        # Contribute layers to asset\n        # Use existing asset and add to it, or initialize a new asset layer\n        path = get_last_publish(instance)\n        payload_layer = None\n        if path and BUILD_INTO_LAST_VERSIONS:\n            # If there's a payload file, put it in the payload instead\n            folder = os.path.dirname(path)\n            payload_path = os.path.join(folder, \"payload.usd\")\n            if os.path.exists(payload_path):\n                payload_layer = Sdf.Layer.OpenAsAnonymous(payload_path)\n\n            asset_layer = Sdf.Layer.OpenAsAnonymous(path)\n        else:\n            # If no existing publish of this product exists then we initialize\n            # the layer as either a default asset or shot structure.\n            init_type = instance.data[\"contribution_target_product_init\"]\n            self.log.debug(\"Initializing layer as type: %s\", init_type)\n            asset_layer, payload_layer = self.init_layer(\n                asset_name=asset_name, init_type=init_type\n            )\n\n        # Author timeCodesPerSecond and framesPerSecond if the asset layer\n        # is currently lacking any but our current context does specify an FPS\n        fps = instance.data.get(\"fps\", instance.context.data.get(\"fps\"))\n        if fps is not None:\n            if (\n                not asset_layer.HasTimeCodesPerSecond()\n                    and not asset_layer.HasFramesPerSecond()\n            ):\n                # Author FPS on the asset layer since there is no opinion yet\n                self.log.info(\"Authoring FPS on Asset Layer: %s FPS\", fps)\n                asset_layer.timeCodesPerSecond = fps\n                asset_layer.framesPerSecond = fps\n\n            if fps != asset_layer.timeCodesPerSecond:\n                self.log.warning(\n                    \"Current instance FPS '%s' does not match asset layer \"\n                    \"timecodes per second '%s'\",\n                    fps, asset_layer.timeCodesPerSecond\n                )\n            if fps != asset_layer.framesPerSecond:\n                self.log.warning(\n                    \"Current instance FPS '%s' does not match asset layer \"\n                    \"frames per second '%s'\",\n                    fps, asset_layer.framesPerSecond\n                )\n\n        target_layer = payload_layer if payload_layer else asset_layer\n\n        # Get unique layer instances (remove duplicate entries)\n        processed_ids = set()\n        layer_instances = []\n        for layer_inst in instance.data[\"source_instances\"]:\n            if layer_inst.id in processed_ids:\n                continue\n            layer_instances.append(layer_inst)\n            processed_ids.add(layer_inst.id)\n\n        # Insert the layer in contributions order\n        def sort_by_order(instance):\n            return instance.data[\"usd_layer_order\"]\n\n        for layer_instance in sorted(layer_instances,\n                                     key=sort_by_order,\n                                     reverse=True):\n\n            layer_id = layer_instance.data[\"usd_layer_id\"]\n            order = layer_instance.data[\"usd_layer_order\"]\n\n            path = get_instance_uri_path(instance=layer_instance,\n                                         resolve=not self.use_ayon_entity_uri)\n            add_ordered_sublayer(target_layer,\n                                 contribution_path=path,\n                                 layer_id=layer_id,\n                                 order=order,\n                                 # Add the sdf argument metadata which allows\n                                 # us to later detect whether another path\n                                 # has the same layer id, so we can replace it.\n                                 add_sdf_arguments_metadata=True)\n\n        # Save the file\n        staging_dir = self.staging_dir(instance)\n        filename = f\"{instance.name}.usd\"\n        filepath = os.path.join(staging_dir, filename)\n        asset_layer.Export(filepath, args={\"format\": \"usda\"})\n\n        add_representation(\n            instance,\n            name=\"usd\",\n            files=filename,\n            staging_dir=staging_dir\n        )\n\n        if payload_layer:\n            payload_path = os.path.join(staging_dir, \"payload.usd\")\n            payload_layer.Export(payload_path, args={\"format\": \"usda\"})\n            self.add_relative_file(instance, payload_path)\n\n    def init_layer(self, asset_name: str, init_type: str):\n        \"\"\"Initialize layer if no previous version exists\"\"\"\n\n        if init_type == \"asset\":\n            asset_layer = Sdf.Layer.CreateAnonymous()\n            created_layers = setup_asset_layer(asset_layer, asset_name,\n                                               force_add_payload=True,\n                                               set_payload_path=True)\n            payload_layer = created_layers[0].layer\n            return asset_layer, payload_layer\n\n        elif init_type == \"shot\":\n            shot_layer = Sdf.Layer.CreateAnonymous()\n            set_layer_defaults(shot_layer, default_prim=None)\n            return shot_layer, None\n\n        else:\n            raise ValueError(\n                \"USD Target Product contribution can only initialize \"\n                \"as 'asset' or 'shot', got: '{}'\".format(init_type)\n            )\n\n    def add_relative_file(self, instance, source, staging_dir=None):\n        \"\"\"Add transfer for a relative path form staging to publish dir.\n\n        Unlike files in representations, the file will not be renamed and\n        will be ingested one-to-one into the publish directory.\n\n        Note: This file does not get registered as a representation, because\n          representation files always get renamed by the publish template\n          system. These files get included in the `representation[\"files\"]`\n          info with all the representations of the version - and thus will\n          appear multiple times per version.\n\n        \"\"\"\n        # TODO: It can be nice to force a particular representation no matter\n        #  what to adhere to a certain filename on integration because e.g. a\n        #  particular file format relies on that file named like that or alike\n        #  and still allow regular registering with the database as a file of\n        #  the version. As such we might want to tweak integrator logic?\n        if staging_dir is None:\n            staging_dir = self.staging_dir(instance)\n\n        assert isinstance(staging_dir, str), \"Staging dir must be string\"\n        publish_dir: str = instance.data[\"publishDir\"]\n\n        relative_path = os.path.relpath(source, staging_dir)\n        destination = os.path.join(publish_dir, relative_path)\n        destination = os.path.normpath(destination)\n\n        transfers = instance.data.setdefault(\"transfers\", [])\n        self.log.debug(f\"Adding relative file {source} -&gt; {relative_path}\")\n        transfers.append((source, destination))\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_usd_layer_contributions.html#client.ayon_core.plugins.publish.extract_usd_layer_contributions.ExtractUSDAssetContribution.add_relative_file","title":"<code>add_relative_file(instance, source, staging_dir=None)</code>","text":"<p>Add transfer for a relative path form staging to publish dir.</p> <p>Unlike files in representations, the file will not be renamed and will be ingested one-to-one into the publish directory.</p> This file does not get registered as a representation, because <p>representation files always get renamed by the publish template system. These files get included in the <code>representation[\"files\"]</code> info with all the representations of the version - and thus will appear multiple times per version.</p> Source code in <code>client/ayon_core/plugins/publish/extract_usd_layer_contributions.py</code> <pre><code>def add_relative_file(self, instance, source, staging_dir=None):\n    \"\"\"Add transfer for a relative path form staging to publish dir.\n\n    Unlike files in representations, the file will not be renamed and\n    will be ingested one-to-one into the publish directory.\n\n    Note: This file does not get registered as a representation, because\n      representation files always get renamed by the publish template\n      system. These files get included in the `representation[\"files\"]`\n      info with all the representations of the version - and thus will\n      appear multiple times per version.\n\n    \"\"\"\n    # TODO: It can be nice to force a particular representation no matter\n    #  what to adhere to a certain filename on integration because e.g. a\n    #  particular file format relies on that file named like that or alike\n    #  and still allow regular registering with the database as a file of\n    #  the version. As such we might want to tweak integrator logic?\n    if staging_dir is None:\n        staging_dir = self.staging_dir(instance)\n\n    assert isinstance(staging_dir, str), \"Staging dir must be string\"\n    publish_dir: str = instance.data[\"publishDir\"]\n\n    relative_path = os.path.relpath(source, staging_dir)\n    destination = os.path.join(publish_dir, relative_path)\n    destination = os.path.normpath(destination)\n\n    transfers = instance.data.setdefault(\"transfers\", [])\n    self.log.debug(f\"Adding relative file {source} -&gt; {relative_path}\")\n    transfers.append((source, destination))\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_usd_layer_contributions.html#client.ayon_core.plugins.publish.extract_usd_layer_contributions.ExtractUSDAssetContribution.init_layer","title":"<code>init_layer(asset_name, init_type)</code>","text":"<p>Initialize layer if no previous version exists</p> Source code in <code>client/ayon_core/plugins/publish/extract_usd_layer_contributions.py</code> <pre><code>def init_layer(self, asset_name: str, init_type: str):\n    \"\"\"Initialize layer if no previous version exists\"\"\"\n\n    if init_type == \"asset\":\n        asset_layer = Sdf.Layer.CreateAnonymous()\n        created_layers = setup_asset_layer(asset_layer, asset_name,\n                                           force_add_payload=True,\n                                           set_payload_path=True)\n        payload_layer = created_layers[0].layer\n        return asset_layer, payload_layer\n\n    elif init_type == \"shot\":\n        shot_layer = Sdf.Layer.CreateAnonymous()\n        set_layer_defaults(shot_layer, default_prim=None)\n        return shot_layer, None\n\n    else:\n        raise ValueError(\n            \"USD Target Product contribution can only initialize \"\n            \"as 'asset' or 'shot', got: '{}'\".format(init_type)\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_usd_layer_contributions.html#client.ayon_core.plugins.publish.extract_usd_layer_contributions.SublayerContribution","title":"<code>SublayerContribution</code>  <code>dataclass</code>","text":"<p>               Bases: <code>_BaseContribution</code></p> <p>Sublayer contribution</p> Source code in <code>client/ayon_core/plugins/publish/extract_usd_layer_contributions.py</code> <pre><code>class SublayerContribution(_BaseContribution):\n    \"\"\"Sublayer contribution\"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_usd_layer_contributions.html#client.ayon_core.plugins.publish.extract_usd_layer_contributions.VariantContribution","title":"<code>VariantContribution</code>  <code>dataclass</code>","text":"<p>               Bases: <code>_BaseContribution</code></p> <p>Reference contribution within a Variant Set</p> Source code in <code>client/ayon_core/plugins/publish/extract_usd_layer_contributions.py</code> <pre><code>@dataclasses.dataclass\nclass VariantContribution(_BaseContribution):\n    \"\"\"Reference contribution within a Variant Set\"\"\"\n\n    # Variant\n    variant_set_name: str\n    variant_name: str\n    variant_is_default: bool  # Whether to author variant selection opinion\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_usd_layer_contributions.html#client.ayon_core.plugins.publish.extract_usd_layer_contributions.add_representation","title":"<code>add_representation(instance, name, files, staging_dir, ext=None, output_name=None)</code>","text":"<p>Add a representation to publish and integrate.</p> <p>A representation must exist of either a single file or a single file sequence. It can not contain multiple files.</p> <p>For the integration to succeed the instance must provide the context for asset, frame range, etc. even though the representation can override some parts of it.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>Instance</code> <p>Publish instance</p> required <code>name</code> <code>str</code> <p>The representation name</p> required <code>files</code> <code>str | List[str]</code> <p>List of files or single file of the representation. This should be the filename only.</p> required <code>staging_dir</code> <code>str</code> <p>The directory containing the files.</p> required <code>ext</code> <code>Optional[str]</code> <p>Explicit extension for the output</p> <code>None</code> <code>output_name</code> <code>Optional[str]</code> <p>Output name suffix for the destination file to ensure the file is unique if multiple representations share the same extension.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>dict</code> <p>Representation data for integration.</p> Source code in <code>client/ayon_core/plugins/publish/extract_usd_layer_contributions.py</code> <pre><code>def add_representation(instance, name,\n                       files, staging_dir, ext=None,\n                       output_name=None):\n    \"\"\"Add a representation to publish and integrate.\n\n    A representation must exist of either a single file or a\n    single file sequence. It can *not* contain multiple files.\n\n    For the integration to succeed the instance must provide the context\n    for asset, frame range, etc. even though the representation can\n    override some parts of it.\n\n    Arguments:\n        instance (pyblish.api.Instance): Publish instance\n        name (str): The representation name\n        files (str | List[str]): List of files or single file of the\n            representation. This should be the filename only.\n        staging_dir (str): The directory containing the files.\n        ext (Optional[str]): Explicit extension for the output\n        output_name (Optional[str]): Output name suffix for the\n            destination file to ensure the file is unique if\n            multiple representations share the same extension.\n\n    Returns:\n        dict: Representation data for integration.\n\n    \"\"\"\n    if ext is None:\n        # TODO: Use filename\n        ext = name\n\n    representation = {\n        \"name\": name,\n        \"ext\": ext,\n        \"stagingDir\": staging_dir,\n        \"files\": files\n    }\n    if output_name:\n        representation[\"outputName\"] = output_name\n\n    instance.data.setdefault(\"representations\", []).append(representation)\n    return representation\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_usd_layer_contributions.html#client.ayon_core.plugins.publish.extract_usd_layer_contributions.get_instance_uri_path","title":"<code>get_instance_uri_path(instance, resolve=True)</code>","text":"<p>Return path for instance's usd representation</p> Source code in <code>client/ayon_core/plugins/publish/extract_usd_layer_contributions.py</code> <pre><code>def get_instance_uri_path(\n        instance,\n        resolve=True\n):\n    \"\"\"Return path for instance's usd representation\"\"\"\n    context = instance.context\n    folder_path = instance.data[\"folderPath\"]\n    product_name = instance.data[\"productName\"]\n    project_name = context.data[\"projectName\"]\n    version_name = instance.data[\"version\"]\n\n    # Get the layer's published path\n    path = construct_ayon_entity_uri(\n        project_name=project_name,\n        folder_path=folder_path,\n        product=product_name,\n        version=version_name,\n        representation_name=\"usd\"\n    )\n\n    # Resolve contribution path\n    # TODO: Remove this when Asset Resolver is used\n    if resolve:\n        query = parse_ayon_entity_uri(path)\n        names = {\n            \"project_name\": query[\"project\"],\n            \"folder_path\": query[\"folderPath\"],\n            \"product_name\": query[\"product\"],\n            \"version_name\": query[\"version\"],\n            \"representation_name\": query[\"representation\"],\n        }\n\n        # We want to resolve the paths live from the publishing context\n        path = get_representation_path_in_publish_context(context, **names)\n        if path:\n            return path\n\n        # If for whatever reason we were unable to retrieve from the context\n        # then get the path from an existing database entry\n        path = get_representation_path_by_names(\n            anatomy=context.data[\"anatomy\"],\n            **names\n        )\n        if not path:\n            raise RuntimeError(f\"Unable to resolve publish path for: {names}\")\n\n        # Ensure `None` for now is also a string\n        path = str(path)\n        if platform.system().lower() == \"windows\":\n            path = path.replace(\"\\\\\", \"/\")\n\n    return path\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_usd_layer_contributions.html#client.ayon_core.plugins.publish.extract_usd_layer_contributions.get_last_publish","title":"<code>get_last_publish(instance, representation='usd')</code>","text":"<p>Wrapper to quickly get last representation publish path</p> Source code in <code>client/ayon_core/plugins/publish/extract_usd_layer_contributions.py</code> <pre><code>def get_last_publish(instance, representation=\"usd\"):\n    \"\"\"Wrapper to quickly get last representation publish path\"\"\"\n    return get_representation_path_by_names(\n        project_name=instance.context.data[\"projectName\"],\n        folder_path=instance.data[\"folderPath\"],\n        product_name=instance.data[\"productName\"],\n        version_name=\"latest\",\n        representation_name=representation\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/extract_usd_layer_contributions.html#client.ayon_core.plugins.publish.extract_usd_layer_contributions.get_representation_path_in_publish_context","title":"<code>get_representation_path_in_publish_context(context, project_name, folder_path, product_name, version_name, representation_name)</code>","text":"<p>Return resolved path for product if present in publishing context.</p> <p>Allow resolving 'latest' paths from a publishing context's instances as if they will exist after publishing without them being integrated yet.</p> <p>Use first instance that has same folder path and product name, and contains representation with passed name.</p> <p>Parameters:</p> Name Type Description Default <code>context</code> <code>Context</code> <p>Publishing context.</p> required <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>folder_path</code> <code>str</code> <p>Folder path.</p> required <code>product_name</code> <code>str</code> <p>Product name.</p> required <code>version_name</code> <code>str</code> <p>Version name.</p> required <code>representation_name</code> <code>str</code> <p>Representation name.</p> required <p>Returns:</p> Type Description <p>Union[str, None]: Returns the path if it could be resolved</p> Source code in <code>client/ayon_core/plugins/publish/extract_usd_layer_contributions.py</code> <pre><code>def get_representation_path_in_publish_context(\n    context: pyblish.api.Context,\n    project_name,\n    folder_path,\n    product_name,\n    version_name,\n    representation_name,\n):\n    \"\"\"Return resolved path for product if present in publishing context.\n\n    Allow resolving 'latest' paths from a publishing context's instances\n    as if they will exist after publishing without them being integrated yet.\n\n    Use first instance that has same folder path and product name,\n    and contains representation with passed name.\n\n    Args:\n        context (pyblish.api.Context): Publishing context.\n        project_name (str): Project name.\n        folder_path (str): Folder path.\n        product_name (str): Product name.\n        version_name (str): Version name.\n        representation_name (str): Representation name.\n\n    Returns:\n        Union[str, None]: Returns the path if it could be resolved\n\n    \"\"\"\n    # The AYON publishing logic is set up in such a way that you can not\n    # publish to another project. As such, we know if the project name we're\n    # looking for doesn't match the publishing context it'll not be in there.\n    if context.data[\"projectName\"] != project_name:\n        return\n\n    if version_name == \"hero\":\n        raise NotImplementedError(\n            \"Hero version resolving not implemented from context\"\n        )\n\n    # Search first in publish context to allow resolving latest versions\n    # from e.g. the current publish session if the context is provided\n    specific_version = isinstance(version_name, int)\n    for instance in context:\n        if instance.data.get(\"folderPath\") != folder_path:\n            continue\n\n        if instance.data.get(\"productName\") != product_name:\n            continue\n\n        # Only consider if the instance has a representation by\n        # that name\n        representations = instance.data.get(\"representations\", [])\n        if not any(representation.get(\"name\") == representation_name\n                   for representation in representations):\n            continue\n\n        return get_instance_expected_output_path(\n            instance,\n            representation_name=representation_name,\n            ext=None,\n            version=version_name if specific_version else None\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/integrate.html","title":"integrate","text":""},{"location":"autoapi/client/ayon_core/plugins/publish/integrate.html#client.ayon_core.plugins.publish.integrate.IntegrateAsset","title":"<code>IntegrateAsset</code>","text":"<p>               Bases: <code>InstancePlugin</code></p> <p>Register publish in the database and transfer files to destinations.</p> Steps <p>1) Register the product and version 2) Transfer the representation files to the destination 3) Register the representation</p> Requires <p>instance.data['representations'] - must be a list and each member must be a dictionary with following data:     'files': list of filenames for sequence, string for single file.              Only the filename is allowed, without the folder path.     'stagingDir': \"path/to/folder/with/files\"     'name': representation name (usually the same as extension)     'ext': file extension optional data     \"frameStart\"     \"frameEnd\"     'fps'     \"data\": additional metadata for each representation.</p> Source code in <code>client/ayon_core/plugins/publish/integrate.py</code> <pre><code>class IntegrateAsset(pyblish.api.InstancePlugin):\n    \"\"\"Register publish in the database and transfer files to destinations.\n\n    Steps:\n        1) Register the product and version\n        2) Transfer the representation files to the destination\n        3) Register the representation\n\n    Requires:\n        instance.data['representations'] - must be a list and each member\n        must be a dictionary with following data:\n            'files': list of filenames for sequence, string for single file.\n                     Only the filename is allowed, without the folder path.\n            'stagingDir': \"path/to/folder/with/files\"\n            'name': representation name (usually the same as extension)\n            'ext': file extension\n        optional data\n            \"frameStart\"\n            \"frameEnd\"\n            'fps'\n            \"data\": additional metadata for each representation.\n    \"\"\"\n\n    label = \"Integrate Asset\"\n    order = pyblish.api.IntegratorOrder\n\n    default_template_name = \"publish\"\n\n    # Representation context keys that should always be written to\n    # the database even if not used by the destination template\n    db_representation_context_keys = [\n        \"project\",\n        \"hierarchy\",\n        \"folder\",\n        \"task\",\n        \"product\",\n        \"version\",\n        \"representation\",\n        \"username\",\n        \"output\",\n    ]\n\n    def process(self, instance):\n        # Instance should be integrated on a farm\n        if instance.data.get(\"farm\"):\n            self.log.debug(\n                \"Instance is marked to be processed on farm. Skipping\")\n            return\n\n        # Instance is marked to not get integrated\n        if not instance.data.get(\"integrate\", True):\n            self.log.debug(\"Instance is marked to skip integrating. Skipping\")\n            return\n\n        filtered_repres = self.filter_representations(instance)\n        # Skip instance if there are not representations to integrate\n        #   all representations should not be integrated\n        if not filtered_repres:\n            product_base_type = instance.data.get(\"productBaseType\")\n            if not product_base_type:\n                product_base_type = instance.data[\"productType\"]\n            self.log.info(\n                \"Skipping, there are no representations\"\n                f\" to integrate for instance {product_base_type}\"\n            )\n            return\n\n        file_transactions = FileTransaction(log=self.log,\n                                            # Enforce unique transfers\n                                            allow_queue_replacements=False)\n        try:\n            self.register(instance, file_transactions, filtered_repres)\n        except DuplicateDestinationError as exc:\n            # Raise DuplicateDestinationError as PublishError\n            # and rollback the transactions\n            file_transactions.rollback()\n            raise PublishError(str(exc)).with_traceback(sys.exc_info()[2])\n\n        except Exception as exc:\n            # clean destination\n            # todo: preferably we'd also rollback *any* changes to the database\n            file_transactions.rollback()\n            self.log.critical(\"Error when registering\", exc_info=True)\n            raise exc\n\n        # Finalizing can't rollback safely so no use for moving it to\n        # the try, except.\n        file_transactions.finalize()\n\n    def filter_representations(self, instance):\n        \"\"\"Filter representations to be integrated.\"\"\"\n        repres = instance.data.get(\"representations\")\n        if not repres:\n            return []\n\n        # Validate type of stored representations\n        if not isinstance(repres, (list, tuple)):\n            raise TypeError(\n                \"Instance 'files' must be a list, got: {0} {1}\".format(\n                    str(type(repres)), str(repres)\n                )\n            )\n\n        # Filter representations\n        filtered_repres = []\n        for repre in repres:\n            if \"delete\" in repre.get(\"tags\", []):\n                continue\n            filtered_repres.append(repre)\n\n        return filtered_repres\n\n    def register(self, instance, file_transactions, filtered_repres):\n        project_name = instance.context.data[\"projectName\"]\n\n        instance_stagingdir = instance.data.get(\"stagingDir\")\n        if not instance_stagingdir:\n            self.log.debug((\n                \"{0} is missing reference to staging directory.\"\n                \" Will try to get it from representation.\"\n            ).format(instance))\n\n        else:\n            self.log.debug(\n                \"Establishing staging directory \"\n                \"@ {0}\".format(instance_stagingdir)\n            )\n\n        template_name = self.get_template_name(instance)\n        self.log.debug(f\"Anatomy template name: {template_name}\")\n        anatomy = instance.context.data[\"anatomy\"]\n        publish_template = anatomy.get_template_item(\"publish\", template_name)\n\n        # Prepare preferred root to use for representation files\n        path_template_obj: AnatomyStringTemplate = publish_template[\"path\"]\n        result: AnatomyTemplateResult = path_template_obj.format(\n            {\"root\": anatomy.roots}\n        )\n        root_value = result.used_values.get(\"root\")\n        prefered_root_name = None\n        if root_value:\n            prefered_root_name = next(iter(root_value.keys()))\n\n        op_session = OperationsSession()\n        product_entity = self.prepare_product(\n            instance, op_session, project_name\n        )\n        version_entity = self.prepare_version(\n            instance, op_session, product_entity, project_name\n        )\n        instance.data[\"versionEntity\"] = version_entity\n\n        anatomy = instance.context.data[\"anatomy\"]\n\n        # Get existing representations (if any)\n        existing_repres_by_name = {\n            repre_entity[\"name\"].lower(): repre_entity\n            for repre_entity in get_representations(\n                project_name,\n                version_ids=[version_entity[\"id\"]]\n            )\n        }\n\n        # Prepare all representations\n        prepared_representations = []\n        for repre in filtered_repres:\n            # todo: reduce/simplify what is returned from this function\n            prepared = self.prepare_representation(\n                repre,\n                publish_template,\n                existing_repres_by_name,\n                version_entity,\n                instance_stagingdir,\n                instance)\n\n            for src, dst in prepared[\"transfers\"]:\n                # todo: add support for hardlink transfers\n                file_transactions.add(src, dst)\n\n            prepared_representations.append(prepared)\n\n        # Each instance can also have pre-defined transfers not explicitly\n        # part of a representation - like texture resources used by a\n        # .ma representation. Those destination paths are pre-defined, etc.\n        # todo: should we move or simplify this logic?\n        resource_destinations = set()\n\n        file_copy_modes = [\n            (\"transfers\", FileTransaction.MODE_COPY),\n            (\"hardlinks\", FileTransaction.MODE_HARDLINK)\n        ]\n        for files_type, copy_mode in file_copy_modes:\n            for src, dst in instance.data.get(files_type, []):\n                self._validate_path_in_project_roots(anatomy, dst)\n\n                file_transactions.add(src, dst, mode=copy_mode)\n                resource_destinations.add(os.path.abspath(dst))\n\n        # Bulk write to the database\n        # We write the product and version to the database before the File\n        # Transaction to reduce the chances of another publish trying to\n        # publish to the same version number since that chance can greatly\n        # increase if the file transaction takes a long time.\n        op_session.commit()\n\n        self.log.info((\n            \"Product '{}' version {} written to database..\"\n        ).format(product_entity[\"name\"], version_entity[\"version\"]))\n\n        # Process all file transfers of all integrations now\n        self.log.debug(\"Integrating source files to destination ...\")\n        file_transactions.process()\n        self.log.debug(\n            \"Backed up existing files: {}\".format(file_transactions.backups))\n        self.log.debug(\n            \"Transferred files: {}\".format(file_transactions.transferred))\n        self.log.debug(\"Retrieving Representation Site Sync information ...\")\n\n        # Compute the resource file infos once (files belonging to the\n        # version instance instead of an individual representation) so\n        # we can reuse those file infos per representation\n        resource_file_infos = self.get_files_info(\n            resource_destinations, anatomy, prefered_root_name\n        )\n\n        # Finalize the representations now the published files are integrated\n        # Get 'files' info for representations and its attached resources\n        new_repre_names_low = set()\n        for prepared in prepared_representations:\n            repre_entity = prepared[\"representation\"]\n            repre_update_data = prepared[\"repre_update_data\"]\n            transfers = prepared[\"transfers\"]\n            destinations = [dst for src, dst in transfers]\n\n            repre_files = self.get_files_info(\n                destinations, anatomy, prefered_root_name\n            )\n            # Add the version resource file infos to each representation\n            repre_files += resource_file_infos\n            repre_entity[\"files\"] = repre_files\n\n            # Set up representation for writing to the database. Since\n            # we *might* be overwriting an existing entry if the version\n            # already existed we'll use ReplaceOnce with `upsert=True`\n            if repre_update_data is None:\n                op_session.create_entity(\n                    project_name, \"representation\", repre_entity\n                )\n            else:\n                # Add files to update data\n                repre_update_data[\"files\"] = repre_files\n                op_session.update_entity(\n                    project_name,\n                    \"representation\",\n                    repre_entity[\"id\"],\n                    repre_update_data\n                )\n\n            new_repre_names_low.add(repre_entity[\"name\"].lower())\n\n        # Delete any existing representations that didn't get any new data\n        # if the instance is not set to append mode\n        if not instance.data.get(\"append\", False):\n            for name, existing_repres in existing_repres_by_name.items():\n                if name not in new_repre_names_low:\n                    # We add the exact representation name because `name` is\n                    # lowercase for name matching only and not in the database\n                    op_session.delete_entity(\n                        project_name, \"representation\", existing_repres[\"id\"]\n                    )\n\n        self.log.debug(\"{}\".format(op_session.to_data()))\n        op_session.commit()\n\n        # Backwards compatibility used in hero integration.\n        # todo: can we avoid the need to store this?\n        instance.data[\"published_representations\"] = {\n            p[\"representation\"][\"id\"]: p\n            for p in prepared_representations\n        }\n\n        self.log.info(\n            \"Registered {} representations: {}\".format(\n                len(prepared_representations),\n                \", \".join(p[\"representation\"][\"name\"]\n                          for p in prepared_representations)\n            )\n        )\n\n    def prepare_product(self, instance, op_session, project_name):\n        folder_entity = instance.data[\"folderEntity\"]\n        product_name = instance.data[\"productName\"]\n        product_type = instance.data[\"productType\"]\n        product_base_type = instance.data.get(\"productBaseType\")\n        if not product_base_type:\n            product_base_type = product_type\n\n        self.log.debug(\"Product: {}\".format(product_name))\n\n        # Get existing product if it exists\n        existing_product_entity = get_product_by_name(\n            project_name, product_name, folder_entity[\"id\"]\n        )\n\n        # Define product data\n        attributes = {}\n\n        product_group = instance.data.get(\"productGroup\")\n        if product_group:\n            attributes[\"productGroup\"] = product_group\n        elif existing_product_entity:\n            # Preserve previous product group if new version does not set it\n            product_group = existing_product_entity.get(\"attrib\", {}).get(\n                \"productGroup\"\n            )\n            if product_group is not None:\n                attributes[\"productGroup\"] = product_group\n\n        product_id = None\n        if existing_product_entity:\n            product_id = existing_product_entity[\"id\"]\n\n        new_product_entity_kwargs = {\n            \"name\": product_name,\n            \"folder_id\": folder_entity[\"id\"],\n            \"attribs\": attributes,\n            \"entity_id\": product_id,\n            \"product_base_type\": product_base_type,\n            \"product_type\": product_type,\n        }\n\n        if not is_product_base_type_supported():\n            new_product_entity_kwargs.pop(\"product_base_type\")\n            if (\n                product_base_type is not None\n                and product_base_type != product_type\n            ):\n                self.log.warning((\n                    \"Product base type %s is not supported by the server, \"\n                    \"but it's defined - and it differs from product type %s. \"\n                    \"Using product base type as product type.\"\n                ), product_base_type, product_type)\n\n                new_product_entity_kwargs[\"product_type\"] = (\n                    product_base_type\n                )\n\n        product_entity = new_product_entity(**new_product_entity_kwargs)\n\n        if existing_product_entity is None:\n            # Create a new product\n            self.log.info(\n                \"Product '%s' not found, creating ...\" % product_name\n            )\n            op_session.create_entity(\n                project_name, \"product\", product_entity\n            )\n\n        else:\n            # Update existing product data with new data and set in database.\n            # We also change the found product in-place so we don't need to\n            # re-query the product afterwards\n            update_data = prepare_changes(\n                existing_product_entity, product_entity\n            )\n            op_session.update_entity(\n                project_name,\n                \"product\",\n                product_entity[\"id\"],\n                update_data\n            )\n\n        self.log.debug(\"Prepared product: {}\".format(product_name))\n        return product_entity\n\n    def prepare_version(\n        self, instance, op_session, product_entity, project_name\n    ):\n        version_number = instance.data[\"version\"]\n        task_id = None\n        task_entity = instance.data.get(\"taskEntity\")\n        if task_entity:\n            task_id = task_entity[\"id\"]\n\n        existing_version = get_version_by_name(\n            project_name,\n            version_number,\n            product_entity[\"id\"]\n        )\n        version_id = None\n        if existing_version:\n            version_id = existing_version[\"id\"]\n\n        all_version_data = self.create_version_data(instance)\n        version_data = {}\n        version_attributes = {}\n        attr_defs = self._get_attributes_for_type(instance.context, \"version\")\n        for key, value in all_version_data.items():\n            if key in attr_defs:\n                version_attributes[key] = value\n            else:\n                version_data[key] = value\n\n        host_name = instance.context.data[\"hostName\"]\n        version_data[\"host_name\"] = host_name\n\n        tags = instance.data.get(\"versionTags\")\n        if tags is not None:\n            # Check if tags is an iterable.\n            if not isinstance(tags, (list, tuple, set)):\n                raise PublishError(\n                    \"Tags must be an iterable.\"\n                    f\" Instead got type {type(tags)}\"\n                )\n            # Error if not all are string.\n            if not all(isinstance(tag, str) for tag in tags):\n                raise PublishError(\n                    \"Version tags must be an iterable of strings. \"\n                    f\"Got: {type(tags)}\"\n                )\n            # Force the type to be list for the new_version_entity call.\n            tags = list(tags)\n\n        version_entity = new_version_entity(\n            version_number,\n            product_entity[\"id\"],\n            task_id=task_id,\n            status=instance.data.get(\"status\"),\n            data=version_data,\n            attribs=version_attributes,\n            entity_id=version_id,\n            tags=tags,\n        )\n\n        if existing_version:\n            self.log.debug(\"Updating existing version ...\")\n            update_data = prepare_changes(existing_version, version_entity)\n            op_session.update_entity(\n                project_name,\n                \"version\",\n                version_entity[\"id\"],\n                update_data\n            )\n        else:\n            self.log.debug(\"Creating new version ...\")\n            op_session.create_entity(\n                project_name, \"version\", version_entity\n            )\n\n        self.log.debug(\n            \"Prepared version: v{0:03d}\".format(version_entity[\"version\"])\n        )\n\n        return version_entity\n\n    def _validate_repre_files(self, files, is_sequence_representation):\n        \"\"\"Validate representation files before transfer preparation.\n\n        Check if files contain only filenames instead of full paths and check\n        if sequence don't contain more than one sequence or has remainders.\n\n        Args:\n            files (Union[str, List[str]]): Files from representation.\n            is_sequence_representation (bool): Files are for sequence.\n\n        Raises:\n            PublishError: If validations don't pass.\n\n        \"\"\"\n        if not files:\n            return\n\n        if not is_sequence_representation:\n            files = [files]\n\n        for fname in files:\n            if os.path.isabs(fname):\n                raise PublishError(\n                    f\"Representation file names contains full paths: {fname}\"\n                )\n\n        if not is_sequence_representation:\n            return\n\n        src_collections, remainders = clique.assemble(files)\n        if len(files) &lt; 2 or len(src_collections) != 1 or remainders:\n            raise PublishError((\n                \"Files of representation does not contain proper\"\n                \" sequence files.\\nCollected collections: {}\"\n                \"\\nCollected remainders: {}\"\n            ).format(\n                \", \".join([str(col) for col in src_collections]),\n                \", \".join([str(rem) for rem in remainders])\n            ))\n\n    def prepare_representation(\n        self,\n        repre,\n        publish_template,\n        existing_repres_by_name,\n        version_entity,\n        instance_stagingdir,\n        instance\n    ):\n        # pre-flight validations\n        if repre[\"ext\"].startswith(\".\"):\n            raise PublishError(\n                f\"Extension must not start with a dot '.': {repre['ext']}\"\n            )\n\n        repre_transfers = repre.get(\"transfers\")\n        if repre_transfers:\n            raise PublishError(\n                \"Representation is not allowed to have transfers\"\n                \"data before integration. They are computed in \"\n                f\"the integrator. Got: {repre_transfers}\"\n            )\n\n        # create template data for Anatomy\n        template_data = copy.deepcopy(instance.data[\"anatomyData\"])\n\n        # required representation keys\n        files = repre[\"files\"]\n        template_data[\"representation\"] = repre[\"name\"]\n        template_data[\"ext\"] = repre[\"ext\"]\n\n        # allow overwriting existing version\n        template_data[\"version\"] = version_entity[\"version\"]\n\n        # add template data for colorspaceData\n        if repre.get(\"colorspaceData\"):\n            colorspace = repre[\"colorspaceData\"][\"colorspace\"]\n            # replace spaces with underscores\n            # pipeline.colorspace.parse_colorspace_from_filepath\n            # is checking it with underscores too\n            colorspace = colorspace.replace(\" \", \"_\")\n            template_data[\"colorspace\"] = colorspace\n\n        stagingdir = repre.get(\"stagingDir\")\n        if not stagingdir:\n            # Fall back to instance staging dir if not explicitly\n            # set for representation in the instance\n            self.log.debug((\n                \"Representation uses instance staging dir: {}\"\n            ).format(instance_stagingdir))\n            stagingdir = instance_stagingdir\n\n        if not stagingdir:\n            raise PublishError(\n                f\"No staging directory set for representation: {repre}.\"\n            )\n\n        # optionals\n        # retrieve additional anatomy data from representation if exists\n        for key, anatomy_key in {\n            # Representation Key: Anatomy data key\n            \"resolutionWidth\": \"resolution_width\",\n            \"resolutionHeight\": \"resolution_height\",\n            \"fps\": \"fps\",\n            \"outputName\": \"output\",\n            \"originalBasename\": \"originalBasename\"\n        }.items():\n            # Allow to take value from representation\n            # if not found also consider instance.data\n            value = repre.get(key)\n            if value is None:\n                value = instance.data.get(key)\n\n            if value is not None:\n                template_data[anatomy_key] = value\n\n        anatomy = instance.context.data[\"anatomy\"]\n        path_template_obj = publish_template[\"path\"]\n        template = path_template_obj.template.replace(\"\\\\\", \"/\")\n\n        is_udim = bool(repre.get(\"udim\"))\n\n        # handle publish in place\n        if \"{originalDirname}\" in template:\n            # store as originalDirname only original value without project root\n            # if instance collected originalDirname is present, it should be\n            # used for all represe\n            # from temp to final\n            original_directory = (\n                instance.data.get(\"originalDirname\") or stagingdir)\n            _rootless = self.get_rootless_path(anatomy, original_directory)\n            if _rootless == original_directory:\n                raise PublishError(\n                    f\"Destination path '{original_directory}'\"\n                    f\" must be in project directory.\"\n                )\n            relative_path_start = _rootless.rfind('}') + 2\n            without_root = _rootless[relative_path_start:]\n            template_data[\"originalDirname\"] = without_root\n\n        is_sequence_representation = isinstance(files, (list, tuple))\n        self._validate_repre_files(files, is_sequence_representation)\n\n        # Output variables of conditions below:\n        # - transfers (List[Tuple[str, str]]): src -&gt; dst filepaths to copy\n        # - repre_context (Dict[str, Any]): context data used to fill template\n        # - template_data (Dict[str, Any]): source data used to fill template\n        #   - to add required data to 'repre_context' not used for\n        #       formatting\n\n        # Treat template with 'orignalBasename' in special way\n        if \"{originalBasename}\" in template:\n            # Remove 'frame' from template data\n            template_data.pop(\"frame\", None)\n\n            # Find out first frame string value\n            first_index_padded = None\n            if not is_udim and is_sequence_representation:\n                col = clique.assemble(files)[0][0]\n                sorted_frames = tuple(sorted(col.indexes))\n                # First frame used for end value\n                first_frame = sorted_frames[0]\n                # Get last frame for padding\n                last_frame = sorted_frames[-1]\n                # Use padding from collection of length of last frame as string\n                padding = max(col.padding, len(str(last_frame)))\n                first_index_padded = get_frame_padded(\n                    frame=first_frame,\n                    padding=padding\n                )\n\n            # Convert files to list for single file as remaining part is only\n            #   transfers creation (iteration over files)\n            if not is_sequence_representation:\n                files = [files]\n\n            repre_context = None\n            transfers = []\n            for src_file_name in files:\n                template_data[\"originalBasename\"], _ = os.path.splitext(\n                    src_file_name)\n\n                dst = path_template_obj.format_strict(template_data)\n                src = os.path.join(stagingdir, src_file_name)\n                transfers.append((src, dst))\n                if repre_context is None:\n                    repre_context = dst.used_values\n\n            if not is_udim and first_index_padded is not None:\n                repre_context[\"frame\"] = first_index_padded\n\n        elif is_sequence_representation:\n            # Collection of files (sequence)\n            src_collections, _remainders = clique.assemble(files)\n\n            src_collection = src_collections[0]\n            destination_indexes = list(src_collection.indexes)\n\n            if is_udim:\n                # UDIM should at be four digits\n                destination_padding: int = 4\n            else:\n                # Change padding for frames to match anatomy template\n                destination_padding: int = anatomy.templates_obj.frame_padding\n\n                # If the representation has `frameStart` set it renumbers the\n                # frame indices of the published collection. It will start from\n                # that `frameStart` index instead. Thus if that frame start\n                # differs from the collection we want to shift the destination\n                # frame indices from the source collection.\n                # In case source are published in place we need to\n                # skip renumbering\n                repre_frame_start = repre.get(\"frameStart\")\n                explicit_frames = instance.data.get(\"hasExplicitFrames\", False)\n                if not explicit_frames and repre_frame_start is not None:\n                    index_frame_start = int(repre_frame_start)\n                    # Shift destination sequence to the start frame\n                    destination_indexes = [\n                        index_frame_start + idx\n                        for idx in range(len(destination_indexes))\n                    ]\n\n            # To construct the destination template with anatomy we require\n            # a Frame or UDIM tile set for the template data. We use the first\n            # index of the destination for that because that could've shifted\n            # from the source indexes, etc.\n            first_index_padded = get_frame_padded(\n                frame=destination_indexes[0],\n                padding=destination_padding\n            )\n\n            # Construct destination collection from template\n            repre_context = None\n            dst_filepaths = []\n            for index in destination_indexes:\n                if is_udim:\n                    template_data[\"udim\"] = index\n                else:\n                    template_data[\"frame\"] = index\n                template_filled = path_template_obj.format_strict(\n                    template_data\n                )\n                dst_filepaths.append(template_filled)\n                if repre_context is None:\n                    self.log.debug(\n                        \"Template filled: {}\".format(str(template_filled))\n                    )\n                    repre_context = template_filled.used_values\n\n            # Make sure context contains frame\n            # NOTE: Frame would not be available only if template does not\n            #   contain '{frame}' in template -&gt; Do we want support it?\n            if not is_udim:\n                repre_context[\"frame\"] = first_index_padded\n\n            # store renderlayer in context if it exists\n            # to be later used for example by delivery templates\n            if instance.data.get(\"renderlayer\"):\n                repre_context[\"renderlayer\"] = instance.data[\"renderlayer\"]\n\n            # Update the destination indexes and padding\n            dst_collection = clique.assemble(dst_filepaths)[0][0]\n            dst_collection.padding = destination_padding\n            if len(src_collection.indexes) != len(dst_collection.indexes):\n                raise PublishError(\n                    \"This is a bug. Source sequence frames length\"\n                    \" does not match integration frames length\"\n                )\n\n            # Multiple file transfers\n            transfers = []\n            for src_file_name, dst in zip(src_collection, dst_collection):\n                src = os.path.join(stagingdir, src_file_name)\n                transfers.append((src, dst))\n\n        else:\n            # Single file\n            # Manage anatomy template data\n            template_data.pop(\"frame\", None)\n            if is_udim:\n                template_data[\"udim\"] = repre[\"udim\"][0]\n            # Construct destination filepath from template\n            template_filled = path_template_obj.format_strict(template_data)\n            repre_context = template_filled.used_values\n            dst = os.path.normpath(template_filled)\n\n            # Single file transfer\n            src = os.path.join(stagingdir, files)\n            transfers = [(src, dst)]\n\n        # todo: Are we sure the assumption each representation\n        #       ends up in the same folder is valid?\n        if not instance.data.get(\"publishDir\"):\n            template_obj = publish_template[\"directory\"]\n            template_filled = template_obj.format_strict(template_data)\n            instance.data[\"publishDir\"] = template_filled\n\n        for key in self.db_representation_context_keys:\n            # Also add these values to the context even if not used by the\n            # destination template\n            value = template_data.get(key)\n            if value is not None:\n                repre_context[key] = value\n\n        # Keep only username\n        # NOTE This is to avoid storing all user attributes and data\n        #   to representation\n        if \"user\" not in repre_context:\n            repre_context[\"user\"] = {\n                \"name\": template_data[\"user\"][\"name\"]\n            }\n\n        # Use previous representation's id if there is a name match\n        existing = existing_repres_by_name.get(repre[\"name\"].lower())\n        repre_id = None\n        if existing:\n            repre_id = existing[\"id\"]\n\n        # Store first transferred destination as published path data\n        # - used primarily for reviews that are integrated to custom modules\n        # TODO we should probably store all integrated files\n        #   related to the representation?\n        published_path = transfers[0][1]\n        repre[\"published_path\"] = published_path\n\n        # todo: `repre` is not the actual `representation` entity\n        #       we should simplify/clarify difference between data above\n        #       and the actual representation entity for the database\n        attr_defs = self._get_attributes_for_type(\n            instance.context, \"representation\"\n        )\n        attributes = {\"path\": published_path, \"template\": template}\n        data = {\"context\": repre_context}\n        for key, value in repre.get(\"data\", {}).items():\n            if key in attr_defs:\n                attributes[key] = value\n            else:\n                data[key] = value\n\n        # add colorspace data if any exists on representation\n        if repre.get(\"colorspaceData\"):\n            data[\"colorspaceData\"] = repre[\"colorspaceData\"]\n\n        repre_doc = new_representation_entity(\n            repre[\"name\"],\n            version_entity[\"id\"],\n            # files are filled afterwards\n            [],\n            data=data,\n            attribs=attributes,\n            entity_id=repre_id\n        )\n        update_data = None\n        if repre_id is not None:\n            update_data = prepare_changes(existing, repre_doc)\n\n        return {\n            \"representation\": repre_doc,\n            \"repre_update_data\": update_data,\n            \"anatomy_data\": template_data,\n            \"transfers\": transfers,\n            # todo: avoid the need for 'published_files' used by Integrate Hero\n            # backwards compatibility\n            \"published_files\": [transfer[1] for transfer in transfers]\n        }\n\n    def create_version_data(self, instance):\n        \"\"\"Create the data dictionary for the version\n\n        Args:\n            instance: the current instance being published\n\n        Returns:\n            dict: the required information for version[\"data\"]\n        \"\"\"\n\n        context = instance.context\n\n        # create relative source path for DB\n        if \"source\" in instance.data:\n            source = instance.data[\"source\"]\n        else:\n            source = context.data[\"currentFile\"]\n            anatomy = instance.context.data[\"anatomy\"]\n            source = self.get_rootless_path(anatomy, source)\n        self.log.debug(\"Source: {}\".format(source))\n\n        version_data = {\n            \"families\": get_instance_families(instance),\n            \"time\": context.data[\"time\"],\n            \"author\": context.data[\"user\"],\n            \"source\": source,\n            \"comment\": instance.data[\"comment\"],\n            \"machine\": context.data.get(\"machine\"),\n            \"fps\": instance.data.get(\"fps\", context.data.get(\"fps\"))\n        }\n\n        # todo: preferably we wouldn't need this \"if dict\" etc. logic and\n        #       instead be able to rely what the input value is if it's set.\n        intent_value = context.data.get(\"intent\")\n        if intent_value and isinstance(intent_value, dict):\n            intent_value = intent_value.get(\"value\")\n\n        if intent_value:\n            version_data[\"intent\"] = intent_value\n\n        # Include optional data if present in\n        optionals = [\n            \"frameStart\", \"frameEnd\",\n            \"handleEnd\", \"handleStart\",\n            \"step\",\n            \"resolutionWidth\", \"resolutionHeight\",\n            \"pixelAspect\",\n            \"sourceHashes\"\n        ]\n        for key in optionals:\n            if key in instance.data:\n                version_data[key] = instance.data[key]\n\n        # Include instance.data[versionData] directly\n        version_data_instance = instance.data.get(\"versionData\")\n        if version_data_instance:\n            version_data.update(version_data_instance)\n\n        return version_data\n\n    def get_template_name(self, instance):\n        \"\"\"Return anatomy template name to use for integration\"\"\"\n\n        # Anatomy data is pre-filled by Collectors\n        context = instance.context\n        project_name = context.data[\"projectName\"]\n\n        # Task can be optional in anatomy data\n        host_name = context.data[\"hostName\"]\n        anatomy_data = instance.data[\"anatomyData\"]\n        product_base_type = instance.data.get(\"productBaseType\")\n        if not product_base_type:\n            product_base_type = instance.data[\"productType\"]\n        task_info = anatomy_data.get(\"task\") or {}\n\n        return get_publish_template_name(\n            project_name,\n            host_name,\n            product_base_type=product_base_type,\n            task_name=task_info.get(\"name\"),\n            task_type=task_info.get(\"type\"),\n            project_settings=context.data[\"project_settings\"],\n            logger=self.log,\n        )\n\n    def get_rootless_path(self, anatomy, path):\n        \"\"\"Returns, if possible, path without absolute portion from root\n            (eg. 'c:\\' or '/opt/..')\n\n         This information is platform dependent and shouldn't be captured.\n         Example:\n             'c:/projects/MyProject1/Assets/publish...' &gt;\n             '{root}/MyProject1/Assets...'\n\n        Args:\n            anatomy (Anatomy): Project anatomy.\n            path (str): Absolute path.\n\n        Returns:\n            str: Path where root path is replaced by formatting string.\n\n        \"\"\"\n        success, rootless_path = anatomy.find_root_template_from_path(path)\n        if success:\n            path = rootless_path\n        else:\n            self.log.warning((\n                \"Could not find root path for remapping \\\"{}\\\".\"\n                \" This may cause issues on farm.\"\n            ).format(path))\n        return path\n\n    def get_files_info(\n        self,\n        filepaths: Iterable[str],\n        anatomy: Anatomy,\n        prefered_root_name: str | None,\n    ) -&gt; list[dict[str, Any]]:\n        \"\"\"Prepare 'files' info portion for representations.\n\n        Arguments:\n            filepaths (Iterable[str]): List of transferred file paths.\n            anatomy (Anatomy): Project anatomy.\n            prefered_root_name (str | None): If set, it will be tried as first\n                option for rootless path.\n\n        Returns:\n            list[dict[str, Any]]: Representation 'files' information.\n\n        \"\"\"\n        obj_root = None\n        if prefered_root_name:\n            obj_root = anatomy.roots[prefered_root_name]\n        file_infos = []\n        for filepath in filepaths:\n            file_info = self.prepare_file_info(\n                filepath, anatomy, obj_root\n            )\n            file_infos.append(file_info)\n        return file_infos\n\n    def prepare_file_info(\n        self,\n        path: str,\n        anatomy: Anatomy,\n        root: AnatomyRoot | None,\n    ) -&gt; dict[str, Any]:\n        \"\"\" Prepare information for one file (asset or resource)\n\n        Arguments:\n            path (str): Destination url of published file.\n            anatomy (Anatomy): Project anatomy part from instance.\n            root (AnatomyRoot | None): If set, it will be tried as first\n                option for rootless path.\n\n        Returns:\n            dict[str, Any]: Representation file info dictionary.\n\n        \"\"\"\n        rootless_path = None\n        if root is not None:\n            success, rootless_path = root.find_root_template_from_path(path)\n            if not success:\n                rootless_path = None\n\n        if rootless_path is None:\n            rootless_path = self.get_rootless_path(anatomy, path)\n\n        return {\n            \"id\": create_entity_id(),\n            \"name\": os.path.basename(path),\n            \"path\": rootless_path,\n            \"size\": os.path.getsize(path),\n            \"hash\": source_hash(path),\n            \"hash_type\": \"op3\",\n        }\n\n    def _validate_path_in_project_roots(self, anatomy, file_path):\n        \"\"\"Checks if 'file_path' starts with any of the roots.\n\n        Used to check that published path belongs to project, eg. we are not\n        trying to publish to local only folder.\n        Args:\n            anatomy (Anatomy): Project anatomy.\n            file_path (str): Filepath.\n\n        Raises:\n            PublishError: When failed to find root for the path.\n\n        \"\"\"\n        path = self.get_rootless_path(anatomy, file_path)\n        if not path:\n            raise PublishError(\n                f\"Destination path '{file_path}' must be in project dir\"\n            )\n\n    def _get_attributes_for_type(self, context, entity_type):\n        return self._get_attributes_by_type(context)[entity_type]\n\n    def _get_attributes_by_type(self, context):\n        attributes = context.data.get(\"ayonAttributes\")\n        if attributes is None:\n            attributes = {}\n            for key in (\n                \"project\",\n                \"folder\",\n                \"product\",\n                \"version\",\n                \"representation\",\n            ):\n                attributes[key] = get_attributes_for_type(key)\n            context.data[\"ayonAttributes\"] = attributes\n        return attributes\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/integrate.html#client.ayon_core.plugins.publish.integrate.IntegrateAsset.create_version_data","title":"<code>create_version_data(instance)</code>","text":"<p>Create the data dictionary for the version</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <p>the current instance being published</p> required <p>Returns:</p> Name Type Description <code>dict</code> <p>the required information for version[\"data\"]</p> Source code in <code>client/ayon_core/plugins/publish/integrate.py</code> <pre><code>def create_version_data(self, instance):\n    \"\"\"Create the data dictionary for the version\n\n    Args:\n        instance: the current instance being published\n\n    Returns:\n        dict: the required information for version[\"data\"]\n    \"\"\"\n\n    context = instance.context\n\n    # create relative source path for DB\n    if \"source\" in instance.data:\n        source = instance.data[\"source\"]\n    else:\n        source = context.data[\"currentFile\"]\n        anatomy = instance.context.data[\"anatomy\"]\n        source = self.get_rootless_path(anatomy, source)\n    self.log.debug(\"Source: {}\".format(source))\n\n    version_data = {\n        \"families\": get_instance_families(instance),\n        \"time\": context.data[\"time\"],\n        \"author\": context.data[\"user\"],\n        \"source\": source,\n        \"comment\": instance.data[\"comment\"],\n        \"machine\": context.data.get(\"machine\"),\n        \"fps\": instance.data.get(\"fps\", context.data.get(\"fps\"))\n    }\n\n    # todo: preferably we wouldn't need this \"if dict\" etc. logic and\n    #       instead be able to rely what the input value is if it's set.\n    intent_value = context.data.get(\"intent\")\n    if intent_value and isinstance(intent_value, dict):\n        intent_value = intent_value.get(\"value\")\n\n    if intent_value:\n        version_data[\"intent\"] = intent_value\n\n    # Include optional data if present in\n    optionals = [\n        \"frameStart\", \"frameEnd\",\n        \"handleEnd\", \"handleStart\",\n        \"step\",\n        \"resolutionWidth\", \"resolutionHeight\",\n        \"pixelAspect\",\n        \"sourceHashes\"\n    ]\n    for key in optionals:\n        if key in instance.data:\n            version_data[key] = instance.data[key]\n\n    # Include instance.data[versionData] directly\n    version_data_instance = instance.data.get(\"versionData\")\n    if version_data_instance:\n        version_data.update(version_data_instance)\n\n    return version_data\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/integrate.html#client.ayon_core.plugins.publish.integrate.IntegrateAsset.filter_representations","title":"<code>filter_representations(instance)</code>","text":"<p>Filter representations to be integrated.</p> Source code in <code>client/ayon_core/plugins/publish/integrate.py</code> <pre><code>def filter_representations(self, instance):\n    \"\"\"Filter representations to be integrated.\"\"\"\n    repres = instance.data.get(\"representations\")\n    if not repres:\n        return []\n\n    # Validate type of stored representations\n    if not isinstance(repres, (list, tuple)):\n        raise TypeError(\n            \"Instance 'files' must be a list, got: {0} {1}\".format(\n                str(type(repres)), str(repres)\n            )\n        )\n\n    # Filter representations\n    filtered_repres = []\n    for repre in repres:\n        if \"delete\" in repre.get(\"tags\", []):\n            continue\n        filtered_repres.append(repre)\n\n    return filtered_repres\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/integrate.html#client.ayon_core.plugins.publish.integrate.IntegrateAsset.get_files_info","title":"<code>get_files_info(filepaths, anatomy, prefered_root_name)</code>","text":"<p>Prepare 'files' info portion for representations.</p> <p>Parameters:</p> Name Type Description Default <code>filepaths</code> <code>Iterable[str]</code> <p>List of transferred file paths.</p> required <code>anatomy</code> <code>Anatomy</code> <p>Project anatomy.</p> required <code>prefered_root_name</code> <code>str | None</code> <p>If set, it will be tried as first option for rootless path.</p> required <p>Returns:</p> Type Description <code>list[dict[str, Any]]</code> <p>list[dict[str, Any]]: Representation 'files' information.</p> Source code in <code>client/ayon_core/plugins/publish/integrate.py</code> <pre><code>def get_files_info(\n    self,\n    filepaths: Iterable[str],\n    anatomy: Anatomy,\n    prefered_root_name: str | None,\n) -&gt; list[dict[str, Any]]:\n    \"\"\"Prepare 'files' info portion for representations.\n\n    Arguments:\n        filepaths (Iterable[str]): List of transferred file paths.\n        anatomy (Anatomy): Project anatomy.\n        prefered_root_name (str | None): If set, it will be tried as first\n            option for rootless path.\n\n    Returns:\n        list[dict[str, Any]]: Representation 'files' information.\n\n    \"\"\"\n    obj_root = None\n    if prefered_root_name:\n        obj_root = anatomy.roots[prefered_root_name]\n    file_infos = []\n    for filepath in filepaths:\n        file_info = self.prepare_file_info(\n            filepath, anatomy, obj_root\n        )\n        file_infos.append(file_info)\n    return file_infos\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/integrate.html#client.ayon_core.plugins.publish.integrate.IntegrateAsset.get_rootless_path","title":"<code>get_rootless_path(anatomy, path)</code>","text":"<p>Returns, if possible, path without absolute portion from root     (eg. 'c:' or '/opt/..')</p> <p>This information is platform dependent and shouldn't be captured.  Example:      'c:/projects/MyProject1/Assets/publish...' &gt;      '{root}/MyProject1/Assets...'</p> <p>Parameters:</p> Name Type Description Default <code>anatomy</code> <code>Anatomy</code> <p>Project anatomy.</p> required <code>path</code> <code>str</code> <p>Absolute path.</p> required <p>Returns:</p> Name Type Description <code>str</code> <p>Path where root path is replaced by formatting string.</p> Source code in <code>client/ayon_core/plugins/publish/integrate.py</code> <pre><code>def get_rootless_path(self, anatomy, path):\n    \"\"\"Returns, if possible, path without absolute portion from root\n        (eg. 'c:\\' or '/opt/..')\n\n     This information is platform dependent and shouldn't be captured.\n     Example:\n         'c:/projects/MyProject1/Assets/publish...' &gt;\n         '{root}/MyProject1/Assets...'\n\n    Args:\n        anatomy (Anatomy): Project anatomy.\n        path (str): Absolute path.\n\n    Returns:\n        str: Path where root path is replaced by formatting string.\n\n    \"\"\"\n    success, rootless_path = anatomy.find_root_template_from_path(path)\n    if success:\n        path = rootless_path\n    else:\n        self.log.warning((\n            \"Could not find root path for remapping \\\"{}\\\".\"\n            \" This may cause issues on farm.\"\n        ).format(path))\n    return path\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/integrate.html#client.ayon_core.plugins.publish.integrate.IntegrateAsset.get_template_name","title":"<code>get_template_name(instance)</code>","text":"<p>Return anatomy template name to use for integration</p> Source code in <code>client/ayon_core/plugins/publish/integrate.py</code> <pre><code>def get_template_name(self, instance):\n    \"\"\"Return anatomy template name to use for integration\"\"\"\n\n    # Anatomy data is pre-filled by Collectors\n    context = instance.context\n    project_name = context.data[\"projectName\"]\n\n    # Task can be optional in anatomy data\n    host_name = context.data[\"hostName\"]\n    anatomy_data = instance.data[\"anatomyData\"]\n    product_base_type = instance.data.get(\"productBaseType\")\n    if not product_base_type:\n        product_base_type = instance.data[\"productType\"]\n    task_info = anatomy_data.get(\"task\") or {}\n\n    return get_publish_template_name(\n        project_name,\n        host_name,\n        product_base_type=product_base_type,\n        task_name=task_info.get(\"name\"),\n        task_type=task_info.get(\"type\"),\n        project_settings=context.data[\"project_settings\"],\n        logger=self.log,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/integrate.html#client.ayon_core.plugins.publish.integrate.IntegrateAsset.prepare_file_info","title":"<code>prepare_file_info(path, anatomy, root)</code>","text":"<p>Prepare information for one file (asset or resource)</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Destination url of published file.</p> required <code>anatomy</code> <code>Anatomy</code> <p>Project anatomy part from instance.</p> required <code>root</code> <code>AnatomyRoot | None</code> <p>If set, it will be tried as first option for rootless path.</p> required <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: Representation file info dictionary.</p> Source code in <code>client/ayon_core/plugins/publish/integrate.py</code> <pre><code>def prepare_file_info(\n    self,\n    path: str,\n    anatomy: Anatomy,\n    root: AnatomyRoot | None,\n) -&gt; dict[str, Any]:\n    \"\"\" Prepare information for one file (asset or resource)\n\n    Arguments:\n        path (str): Destination url of published file.\n        anatomy (Anatomy): Project anatomy part from instance.\n        root (AnatomyRoot | None): If set, it will be tried as first\n            option for rootless path.\n\n    Returns:\n        dict[str, Any]: Representation file info dictionary.\n\n    \"\"\"\n    rootless_path = None\n    if root is not None:\n        success, rootless_path = root.find_root_template_from_path(path)\n        if not success:\n            rootless_path = None\n\n    if rootless_path is None:\n        rootless_path = self.get_rootless_path(anatomy, path)\n\n    return {\n        \"id\": create_entity_id(),\n        \"name\": os.path.basename(path),\n        \"path\": rootless_path,\n        \"size\": os.path.getsize(path),\n        \"hash\": source_hash(path),\n        \"hash_type\": \"op3\",\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/integrate.html#client.ayon_core.plugins.publish.integrate.get_frame_padded","title":"<code>get_frame_padded(frame, padding)</code>","text":"<p>Return frame number as string with <code>padding</code> amount of padded zeros</p> Source code in <code>client/ayon_core/plugins/publish/integrate.py</code> <pre><code>def get_frame_padded(frame, padding):\n    \"\"\"Return frame number as string with `padding` amount of padded zeros\"\"\"\n    return \"{frame:0{padding}d}\".format(padding=padding, frame=frame)\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/integrate.html#client.ayon_core.plugins.publish.integrate.get_instance_families","title":"<code>get_instance_families(instance)</code>","text":"<p>Get all families of the instance</p> Source code in <code>client/ayon_core/plugins/publish/integrate.py</code> <pre><code>def get_instance_families(instance):\n    \"\"\"Get all families of the instance\"\"\"\n    # todo: move this to lib?\n    family = instance.data.get(\"family\")\n    families = []\n    if family:\n        families.append(family)\n\n    for _family in (instance.data.get(\"families\") or []):\n        if _family not in families:\n            families.append(_family)\n\n    return families\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/integrate.html#client.ayon_core.plugins.publish.integrate.prepare_changes","title":"<code>prepare_changes(old_entity, new_entity)</code>","text":"<p>Prepare changes for entity update.</p> <p>Parameters:</p> Name Type Description Default <code>old_entity</code> <p>Existing entity.</p> required <code>new_entity</code> <p>New entity.</p> required <p>Returns:</p> Type Description <p>dict[str, Any]: Changes that have new entity.</p> Source code in <code>client/ayon_core/plugins/publish/integrate.py</code> <pre><code>def prepare_changes(old_entity, new_entity):\n    \"\"\"Prepare changes for entity update.\n\n    Args:\n        old_entity: Existing entity.\n        new_entity: New entity.\n\n    Returns:\n        dict[str, Any]: Changes that have new entity.\n\n    \"\"\"\n    changes = {}\n    for key in set(new_entity.keys()):\n        if key == \"attrib\":\n            continue\n\n        if key in new_entity and new_entity[key] != old_entity.get(key):\n            changes[key] = new_entity[key]\n            continue\n\n    attrib_changes = {}\n    if \"attrib\" in new_entity:\n        for key, value in new_entity[\"attrib\"].items():\n            if value != old_entity[\"attrib\"].get(key):\n                attrib_changes[key] = value\n    if attrib_changes:\n        changes[\"attrib\"] = attrib_changes\n    return changes\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/integrate_attach_reviewable.html","title":"integrate_attach_reviewable","text":""},{"location":"autoapi/client/ayon_core/plugins/publish/integrate_attach_reviewable.html#client.ayon_core.plugins.publish.integrate_attach_reviewable.AttachReviewables","title":"<code>AttachReviewables</code>","text":"<p>               Bases: <code>InstancePlugin</code>, <code>OptionalPyblishPluginMixin</code></p> <p>Attach reviewable to other instances</p> <p>This pre-integrator plugin allows instances to be 'attached to' other instances by moving all its representations over to the other instance. Even though this technically could work for any representation the current intent is to use for reviewables only, like e.g. <code>review</code> or <code>render</code> product type.</p> <p>When the reviewable is attached to another instance, the instance itself will not be published as a separate entity. Instead, the representations will be copied/moved to the instances it is attached to.</p> Source code in <code>client/ayon_core/plugins/publish/integrate_attach_reviewable.py</code> <pre><code>class AttachReviewables(\n    pyblish.api.InstancePlugin, OptionalPyblishPluginMixin\n):\n    \"\"\"Attach reviewable to other instances\n\n    This pre-integrator plugin allows instances to be 'attached to' other\n    instances by moving all its representations over to the other instance.\n    Even though this technically could work for any representation the current\n    intent is to use for reviewables only, like e.g. `review` or `render`\n    product type.\n\n    When the reviewable is attached to another instance, the instance itself\n    will not be published as a separate entity. Instead, the representations\n    will be copied/moved to the instances it is attached to.\n    \"\"\"\n\n    families = [\"render\", \"review\"]\n    order = pyblish.api.IntegratorOrder - 0.499\n    label = \"Attach reviewables\"\n\n    settings_category = \"core\"\n\n    def process(self, instance):\n        # TODO: Support farm.\n        #  If instance is being submitted to the farm we should pass through\n        #  the 'attached reviewables' metadata to the farm job\n        # TODO: Reviewable frame range and resolutions\n        #  Because we are attaching the data to another instance, how do we\n        #  correctly propagate the resolution + frame rate to the other\n        #  instance? Do we even need to?\n        # TODO: If this were to attach 'renders' to another instance that would\n        #  mean there wouldn't necessarily be a render publish separate as a\n        #  result. Is that correct expected behavior?\n        attr_values = self.get_attr_values_from_data(instance.data)\n        attach_to = attr_values.get(\"attach\", [])\n        if not attach_to:\n            self.log.debug(\n                \"Reviewable is not set to attach to another instance.\"\n            )\n            return\n\n        attach_instances: List[pyblish.api.Instance] = []\n        for attach_instance_id in attach_to:\n            # Find the `pyblish.api.Instance` matching the `CreatedInstance.id`\n            # in the `attach_to` list\n            attach_instance = next(\n                (\n                    _inst\n                    for _inst in instance.context\n                    if _inst.data.get(\"instance_id\") == attach_instance_id\n                ),\n                None,\n            )\n            if attach_instance is None:\n                continue\n\n            # Skip inactive instances\n            if not attach_instance.data.get(\"active\", True):\n                continue\n\n            # For now do not support attaching to 'farm' instances until we\n            # can pass the 'attaching' on to the farm jobs.\n            if attach_instance.data.get(\"farm\"):\n                self.log.warning(\n                    \"Attaching to farm instances is not supported yet.\"\n                )\n                continue\n\n            attach_instances.append(attach_instance)\n\n        instances_names = \", \".join(\n            instance.name for instance in attach_instances\n        )\n        self.log.info(\n            f\"Attaching reviewable to other instances: {instances_names}\"\n        )\n\n        # Copy the representations of this reviewable instance to the other\n        # instance\n        representations = instance.data.get(\"representations\", [])\n        for attach_instance in attach_instances:\n            self.log.info(f\"Attaching to {attach_instance.name}\")\n            attach_instance.data.setdefault(\"representations\", []).extend(\n                copy.deepcopy(representations)\n            )\n\n        # Delete representations on the reviewable instance itself\n        for repre in representations:\n            self.log.debug(\n                \"Marking representation as deleted because it was \"\n                f\"attached to other instances instead: {repre}\"\n            )\n            repre.setdefault(\"tags\", []).append(\"delete\")\n\n        # Stop integrator from trying to integrate this instance\n        if attach_to:\n            instance.data[\"integrate\"] = False\n\n    @classmethod\n    def get_attr_defs_for_instance(cls, create_context, instance):\n        # TODO: Check if instance is actually a 'reviewable'\n        # Filtering of instance, if needed, can be customized\n        if not cls.instance_matches_plugin_families(instance):\n            return []\n\n        items = []\n        for other_instance in create_context.instances:\n            if other_instance == instance:\n                continue\n\n            # Do not allow attaching to other reviewable instances\n            if other_instance.product_base_type in cls.families:\n                continue\n\n            items.append(\n                {\n                    \"label\": other_instance.label,\n                    \"value\": str(other_instance.id),\n                }\n            )\n\n        return [\n            EnumDef(\n                \"attach\",\n                label=\"Attach reviewable\",\n                multiselection=True,\n                items=items,\n                tooltip=\"Attach this reviewable to another instance\",\n            )\n        ]\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/integrate_hero_version.html","title":"integrate_hero_version","text":""},{"location":"autoapi/client/ayon_core/plugins/publish/integrate_hero_version.html#client.ayon_core.plugins.publish.integrate_hero_version.IntegrateHeroVersion","title":"<code>IntegrateHeroVersion</code>","text":"<p>               Bases: <code>OptionalPyblishPluginMixin</code>, <code>InstancePlugin</code></p> Source code in <code>client/ayon_core/plugins/publish/integrate_hero_version.py</code> <pre><code>class IntegrateHeroVersion(\n    OptionalPyblishPluginMixin, pyblish.api.InstancePlugin\n):\n    label = \"Integrate Hero Version\"\n    # Must happen after IntegrateNew\n    order = pyblish.api.IntegratorOrder + 0.1\n\n    settings_category = \"core\"\n\n    optional = True\n    active = True\n\n    # Families are modified using settings\n    families = [\n        \"model\",\n        \"rig\",\n        \"setdress\",\n        \"look\",\n        \"pointcache\",\n        \"animation\"\n    ]\n\n    # Can specify representation names that will be ignored (lower case)\n    ignored_representation_names = []\n    db_representation_context_keys = [\n        \"project\",\n        \"folder\",\n        \"hierarchy\",\n        \"task\",\n        \"product\",\n        \"representation\",\n        \"username\",\n        \"output\"\n    ]\n    # QUESTION/TODO this process should happen on server if crashed due to\n    # permissions error on files (files were used or user didn't have perms)\n    # *but all other plugins must be successfully completed\n\n    use_hardlinks = False\n\n    def process(self, instance):\n        if not self.is_active(instance.data):\n            return\n\n        self.log.debug(\n            \"--- Integration of Hero version for product `{}` begins.\".format(\n                instance.data[\"productName\"]\n            )\n        )\n        published_repres = instance.data.get(\"published_representations\")\n        if not published_repres:\n            self.log.debug(\n                \"*** There are no published representations on the instance.\"\n            )\n            return\n\n        anatomy = instance.context.data[\"anatomy\"]\n        project_name = anatomy.project_name\n\n        template_key = self._get_template_key(project_name, instance)\n        hero_template = anatomy.get_template_item(\n            \"hero\", template_key, \"path\", default=None\n        )\n\n        if hero_template is None:\n            self.log.warning((\n                \"!!! Anatomy of project \\\"{}\\\" does not have set\"\n                \" \\\"{}\\\" template key!\"\n            ).format(project_name, template_key))\n            return\n\n        self.log.debug(\"`hero` template check was successful. `{}`\".format(\n            hero_template\n        ))\n\n        self.integrate_instance(\n            instance, project_name, template_key, hero_template\n        )\n\n    def integrate_instance(\n        self, instance, project_name, template_key, hero_template\n    ):\n        anatomy = instance.context.data[\"anatomy\"]\n        published_repres = instance.data[\"published_representations\"]\n        hero_publish_dir = self.get_publish_dir(instance, template_key)\n\n        src_version_entity = instance.data.get(\"versionEntity\")\n        filtered_repre_ids = []\n        for repre_id, repre_info in published_repres.items():\n            repre = repre_info[\"representation\"]\n            if repre[\"name\"].lower() in self.ignored_representation_names:\n                self.log.debug(\n                    \"Filtering representation with name: `{}`\".format(\n                        repre[\"name\"].lower()\n                    )\n                )\n                filtered_repre_ids.append(repre_id)\n\n        for repre_id in filtered_repre_ids:\n            published_repres.pop(repre_id, None)\n\n        if not published_repres:\n            self.log.debug(\n                \"*** All published representations were filtered by name.\"\n            )\n            return\n\n        if src_version_entity is None:\n            self.log.debug((\n                \"Published version entity was not sent in representation data.\"\n                \" Querying entity from database.\"\n            ))\n            src_version_entity = self.version_from_representations(\n                project_name, published_repres\n            )\n\n        if not src_version_entity:\n            self.log.warning((\n                \"!!! Can't find origin version in database.\"\n                \" Skipping hero version publish.\"\n            ))\n            return\n\n        if src_version_entity[\"version\"] == 0:\n            self.log.debug(\n                \"Version 0 cannot have hero version. Skipping.\"\n            )\n            return\n\n        all_copied_files = []\n        transfers = instance.data.get(\"transfers\", list())\n        for _src, dst in transfers:\n            dst = os.path.normpath(dst)\n            if dst not in all_copied_files:\n                all_copied_files.append(dst)\n\n        hardlinks = instance.data.get(\"hardlinks\", list())\n        for _src, dst in hardlinks:\n            dst = os.path.normpath(dst)\n            if dst not in all_copied_files:\n                all_copied_files.append(dst)\n\n        all_repre_file_paths = []\n        for repre_info in published_repres.values():\n            published_files = repre_info.get(\"published_files\") or []\n            for file_path in published_files:\n                file_path = os.path.normpath(file_path)\n                if file_path not in all_repre_file_paths:\n                    all_repre_file_paths.append(file_path)\n\n        # TODO this is not best practice of getting resources for publish\n        # WARNING due to this we must remove all files from hero publish dir\n        instance_publish_dir = os.path.normpath(\n            instance.data[\"publishDir\"]\n        )\n        other_file_paths_mapping = []\n        for file_path in all_copied_files:\n            # Check if it is from publishDir\n            if not file_path.startswith(instance_publish_dir):\n                continue\n\n            if file_path in all_repre_file_paths:\n                continue\n\n            dst_filepath = file_path.replace(\n                instance_publish_dir, hero_publish_dir\n            )\n            other_file_paths_mapping.append((file_path, dst_filepath))\n\n        # Current version\n        old_version, old_repres = self.current_hero_ents(\n            project_name, src_version_entity\n        )\n        inactive_old_repres_by_name = {}\n        old_repres_by_name = {}\n        for repre in old_repres:\n            low_name = repre[\"name\"].lower()\n            if repre[\"active\"]:\n                old_repres_by_name[low_name] = repre\n            else:\n                inactive_old_repres_by_name[low_name] = repre\n\n        op_session = OperationsSession()\n\n        entity_id = None\n        if old_version:\n            entity_id = old_version[\"id\"]\n\n        tags = instance.data.get(\"versionTags\")\n        if tags is not None:\n            # Tags contents are checked at earlier step \"IntegrateAsset\"\n            # Force the type to be list for the new_version_entity call.\n            tags = list(tags)\n\n        new_hero_version = new_version_entity(\n            - src_version_entity[\"version\"],\n            src_version_entity[\"productId\"],\n            task_id=src_version_entity.get(\"taskId\"),\n            data=copy.deepcopy(src_version_entity[\"data\"]),\n            attribs=copy.deepcopy(src_version_entity[\"attrib\"]),\n            entity_id=entity_id,\n            tags=tags,\n        )\n\n        if old_version:\n            self.log.debug(\"Replacing old hero version.\")\n            update_data = prepare_changes(\n                old_version, new_hero_version\n            )\n            op_session.update_entity(\n                project_name,\n                \"version\",\n                old_version[\"id\"],\n                update_data\n            )\n        else:\n            self.log.debug(\"Creating first hero version.\")\n            op_session.create_entity(\n                project_name, \"version\", new_hero_version\n            )\n\n        # Store hero entity to 'instance.data'\n        instance.data[\"heroVersionEntity\"] = new_hero_version\n\n        # Separate old representations into `to replace` and `to delete`\n        old_repres_to_replace = {}\n        old_repres_to_delete = {}\n        for repre_info in published_repres.values():\n            repre = repre_info[\"representation\"]\n            repre_name_low = repre[\"name\"].lower()\n            if repre_name_low in old_repres_by_name:\n                old_repres_to_replace[repre_name_low] = (\n                    old_repres_by_name.pop(repre_name_low)\n                )\n\n        if old_repres_by_name:\n            old_repres_to_delete = old_repres_by_name\n\n        backup_hero_publish_dir = None\n        if os.path.exists(hero_publish_dir):\n            backup_hero_publish_dir = hero_publish_dir + \".BACKUP\"\n            max_idx = 10\n            idx = 0\n            _backup_hero_publish_dir = backup_hero_publish_dir\n            while os.path.exists(_backup_hero_publish_dir):\n                self.log.debug((\n                    \"Backup folder already exists.\"\n                    \" Trying to remove \\\"{}\\\"\"\n                ).format(_backup_hero_publish_dir))\n\n                try:\n                    shutil.rmtree(_backup_hero_publish_dir)\n                    backup_hero_publish_dir = _backup_hero_publish_dir\n                    break\n                except Exception:\n                    self.log.info(\n                        \"Could not remove previous backup folder.\"\n                        \" Trying to add index to folder name.\"\n                    )\n\n                _backup_hero_publish_dir = (\n                    backup_hero_publish_dir + str(idx)\n                )\n                if not os.path.exists(_backup_hero_publish_dir):\n                    backup_hero_publish_dir = _backup_hero_publish_dir\n                    break\n\n                if idx &gt; max_idx:\n                    raise AssertionError((\n                        \"Backup folders are fully occupied to max index \\\"{}\\\"\"\n                    ).format(max_idx))\n                    break\n\n                idx += 1\n\n            self.log.debug(\"Backup folder path is \\\"{}\\\"\".format(\n                backup_hero_publish_dir\n            ))\n            try:\n                os.rename(hero_publish_dir, backup_hero_publish_dir)\n            except PermissionError:\n                raise AssertionError((\n                    \"Could not create hero version because it is not\"\n                    \" possible to replace current hero files.\"\n                ))\n\n        try:\n            src_to_dst_file_paths = []\n            repre_integrate_data = []\n            path_template_obj = anatomy.get_template_item(\n                \"hero\", template_key, \"path\"\n            )\n            for repre_info in published_repres.values():\n\n                # Skip if new repre does not have published repre files\n                published_files = repre_info[\"published_files\"]\n                if len(published_files) == 0:\n                    continue\n\n                # Prepare anatomy data\n                anatomy_data = copy.deepcopy(repre_info[\"anatomy_data\"])\n                anatomy_data.pop(\"version\", None)\n\n                # Get filled path to repre context\n                template_filled = path_template_obj.format_strict(\n                    anatomy_data\n                )\n                repre_context = template_filled.used_values\n                for key in self.db_representation_context_keys:\n                    value = anatomy_data.get(key)\n                    if value is not None:\n                        repre_context[key] = value\n\n                # Keep only username\n                # NOTE This is to avoid storing all user attributes and data\n                #   to representation\n                if \"user\" not in repre_context:\n                    repre_context[\"user\"] = {\n                        \"name\": anatomy_data[\"user\"][\"name\"]\n                    }\n\n                # Prepare new repre\n                repre_entity = copy.deepcopy(repre_info[\"representation\"])\n                repre_entity.pop(\"id\", None)\n                repre_entity[\"versionId\"] = new_hero_version[\"id\"]\n                repre_entity[\"context\"] = repre_context\n                repre_entity[\"attrib\"] = {\n                    \"path\": str(template_filled),\n                    \"template\": hero_template.template\n                }\n\n                dst_paths = []\n                is_udim = bool(repre_context.get(\"udim\"))\n                # Prepare paths of source and destination files\n                if len(published_files) == 1:\n                    dst_paths.append(str(template_filled))\n                    src_to_dst_file_paths.append(\n                        (published_files[0], template_filled)\n                    )\n                else:\n                    collections, remainders = clique.assemble(published_files)\n                    if remainders or not collections or len(collections) &gt; 1:\n                        raise Exception((\n                            \"Integrity error. Files of published\"\n                            \" representation is combination of frame\"\n                            \" collections and single files. Collections:\"\n                            \" `{}` Single files: `{}`\"\n                        ).format(str(collections), str(remainders)))\n\n                    src_col = collections[0]\n\n                    # Get head and tail for collection\n                    frame_splitter = \"_-_FRAME_SPLIT_-_\"\n                    if is_udim:\n                        anatomy_data[\"udim\"] = frame_splitter\n                    else:\n                        anatomy_data[\"frame\"] = frame_splitter\n                    _template_filled = path_template_obj.format_strict(\n                        anatomy_data\n                    )\n                    head, tail = _template_filled.split(frame_splitter)\n                    padding = anatomy.templates_obj.frame_padding\n\n                    dst_col = clique.Collection(\n                        head=head, padding=padding, tail=tail\n                    )\n                    dst_col.indexes.clear()\n                    dst_col.indexes.update(src_col.indexes)\n                    for src_file, dst_file in zip(src_col, dst_col):\n                        src_to_dst_file_paths.append(\n                            (src_file, dst_file)\n                        )\n                        dst_paths.append(dst_file)\n\n                repre_integrate_data.append(\n                    (repre_entity, dst_paths)\n                )\n\n            file_transactions = FileTransaction(\n                log=self.log,\n                # Enforce unique transfers\n                allow_queue_replacements=False\n            )\n            mode = FileTransaction.MODE_COPY\n            if self.use_hardlinks:\n                mode = FileTransaction.MODE_HARDLINK\n\n            try:\n                for src_path, dst_path in itertools.chain(\n                    src_to_dst_file_paths,\n                    other_file_paths_mapping\n                ):\n                    file_transactions.add(src_path, dst_path, mode=mode)\n\n                self.log.debug(\"Integrating source files to destination ...\")\n                file_transactions.process()\n\n            except DuplicateDestinationError as exc:\n                # Raise DuplicateDestinationError as PublishError\n                # and rollback the transactions\n                file_transactions.rollback()\n                raise PublishError(str(exc)).with_traceback(sys.exc_info()[2])\n\n            except Exception as exc:\n                # Rollback the transactions\n                file_transactions.rollback()\n                self.log.critical(\"Error when copying files\", exc_info=True)\n                raise exc\n\n            # Finalizing can't rollback safely so no use for moving it to\n            # the try, except.\n            file_transactions.finalize()\n\n            # Update prepared representation etity data with files\n            #   and integrate it to server.\n            # NOTE: This must happen with existing files on disk because of\n            #   file hash.\n            for repre_entity, dst_paths in repre_integrate_data:\n                repre_files = self.get_files_info(dst_paths, anatomy)\n                repre_entity[\"files\"] = repre_files\n\n                repre_name_low = repre_entity[\"name\"].lower()\n                # Replace current representation\n                if repre_name_low in old_repres_to_replace:\n                    old_repre = old_repres_to_replace.pop(repre_name_low)\n\n                    repre_entity[\"id\"] = old_repre[\"id\"]\n                    update_data = prepare_changes(old_repre, repre_entity)\n\n                    op_session.update_entity(\n                        project_name,\n                        \"representation\",\n                        old_repre[\"id\"],\n                        update_data\n                    )\n\n                # Activate representation\n                elif repre_name_low in inactive_old_repres_by_name:\n                    inactive_repre = inactive_old_repres_by_name.pop(\n                        repre_name_low\n                    )\n                    repre_entity[\"id\"] = inactive_repre[\"id\"]\n                    update_data = prepare_changes(\n                        inactive_repre, repre_entity\n                    )\n                    op_session.update_entity(\n                        project_name,\n                        \"representation\",\n                        inactive_repre[\"id\"],\n                        update_data\n                    )\n\n                # Create representation\n                else:\n                    op_session.create_entity(\n                        project_name,\n                        \"representation\",\n                        repre_entity\n                    )\n\n            # Deactivate not replaced representations\n            for repre in old_repres_to_delete.values():\n                op_session.update_entity(\n                    project_name,\n                    \"representation\",\n                    repre[\"id\"],\n                    {\"active\": False}\n                )\n\n            op_session.commit()\n\n            # Remove backuped previous hero\n            if (\n                backup_hero_publish_dir is not None and\n                os.path.exists(backup_hero_publish_dir)\n            ):\n                shutil.rmtree(backup_hero_publish_dir)\n\n        except Exception:\n            if (\n                backup_hero_publish_dir is not None and\n                os.path.exists(backup_hero_publish_dir)\n            ):\n                if os.path.exists(hero_publish_dir):\n                    shutil.rmtree(hero_publish_dir)\n                os.rename(backup_hero_publish_dir, hero_publish_dir)\n            self.log.error((\n                \"!!! Creating of hero version failed.\"\n                \" Previous hero version maybe lost some data!\"\n            ))\n            raise\n\n        self.log.debug((\n            \"--- hero version integration for product `{}`\"\n            \" seems to be successful.\"\n        ).format(\n            instance.data[\"productName\"]\n        ))\n\n    def get_files_info(self, filepaths, anatomy):\n        \"\"\"Prepare 'files' info portion for representations.\n\n        Arguments:\n            filepaths (Iterable[str]): List of transferred file paths.\n            anatomy (Anatomy): Project anatomy.\n\n        Returns:\n            list[dict[str, Any]]: Representation 'files' information.\n\n        \"\"\"\n        file_infos = []\n        for filepath in filepaths:\n            file_info = self.prepare_file_info(filepath, anatomy)\n            file_infos.append(file_info)\n        return file_infos\n\n    def prepare_file_info(self, path, anatomy):\n        \"\"\" Prepare information for one file (asset or resource)\n\n        Arguments:\n            path (str): Destination url of published file.\n            anatomy (Anatomy): Project anatomy part from instance.\n\n        Returns:\n            dict[str, Any]: Representation file info dictionary.\n\n        \"\"\"\n        return {\n            \"id\": create_entity_id(),\n            \"name\": os.path.basename(path),\n            \"path\": self.get_rootless_path(anatomy, path),\n            \"size\": os.path.getsize(path),\n            \"hash\": source_hash(path),\n            \"hash_type\": \"op3\",\n        }\n\n    def get_publish_dir(self, instance, template_key):\n        anatomy = instance.context.data[\"anatomy\"]\n        template_data = copy.deepcopy(instance.data[\"anatomyData\"])\n\n        if \"originalBasename\" in instance.data:\n            template_data.update({\n                \"originalBasename\": instance.data.get(\"originalBasename\")\n            })\n\n        template_obj = anatomy.get_template_item(\n            \"hero\", template_key, \"directory\"\n        )\n        publish_folder = os.path.normpath(\n            template_obj.format_strict(template_data)\n        )\n\n        self.log.debug(\"hero publish dir: \\\"{}\\\"\".format(publish_folder))\n\n        return publish_folder\n\n    def _get_template_key(self, project_name, instance):\n        anatomy_data = instance.data[\"anatomyData\"]\n        task_info = anatomy_data.get(\"task\") or {}\n        host_name = instance.context.data[\"hostName\"]\n        product_base_type = instance.data.get(\"productBaseType\")\n        if not product_base_type:\n            product_base_type = instance.data[\"productType\"]\n\n        return get_publish_template_name(\n            project_name,\n            host_name,\n            product_base_type=product_base_type,\n            task_name=task_info.get(\"name\"),\n            task_type=task_info.get(\"type\"),\n            project_settings=instance.context.data[\"project_settings\"],\n            hero=True,\n            logger=self.log\n        )\n\n    def get_rootless_path(self, anatomy, path):\n        \"\"\"Returns, if possible, path without absolute portion from root\n            (eg. 'c:\\' or '/opt/..')\n\n         This information is platform dependent and shouldn't be captured.\n         Example:\n             'c:/projects/MyProject1/Assets/publish...' &gt;\n             '{root}/MyProject1/Assets...'\n\n        Args:\n            anatomy (Anatomy): Project anatomy.\n            path (str): Absolute path.\n\n        Returns:\n            str: Path where root path is replaced by formatting string.\n\n        \"\"\"\n        success, rootless_path = anatomy.find_root_template_from_path(path)\n        if success:\n            path = rootless_path\n        else:\n            self.log.warning((\n                \"Could not find root path for remapping \\\"{}\\\".\"\n                \" This may cause issues on farm.\"\n            ).format(path))\n        return path\n\n    def version_from_representations(self, project_name, repres):\n        for repre in repres:\n            version = ayon_api.get_version_by_id(\n                project_name, repre[\"versionId\"]\n            )\n            if version:\n                return version\n\n    def current_hero_ents(self, project_name, version):\n        hero_version = ayon_api.get_hero_version_by_product_id(\n            project_name, version[\"productId\"]\n        )\n\n        if not hero_version:\n            return (None, [])\n\n        hero_repres = list(ayon_api.get_representations(\n            project_name, version_ids={hero_version[\"id\"]}\n        ))\n        return (hero_version, hero_repres)\n\n    def _get_name_without_ext(self, value):\n        file_name = os.path.basename(value)\n        file_name, _ = os.path.splitext(file_name)\n        return file_name\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/integrate_hero_version.html#client.ayon_core.plugins.publish.integrate_hero_version.IntegrateHeroVersion.get_files_info","title":"<code>get_files_info(filepaths, anatomy)</code>","text":"<p>Prepare 'files' info portion for representations.</p> <p>Parameters:</p> Name Type Description Default <code>filepaths</code> <code>Iterable[str]</code> <p>List of transferred file paths.</p> required <code>anatomy</code> <code>Anatomy</code> <p>Project anatomy.</p> required <p>Returns:</p> Type Description <p>list[dict[str, Any]]: Representation 'files' information.</p> Source code in <code>client/ayon_core/plugins/publish/integrate_hero_version.py</code> <pre><code>def get_files_info(self, filepaths, anatomy):\n    \"\"\"Prepare 'files' info portion for representations.\n\n    Arguments:\n        filepaths (Iterable[str]): List of transferred file paths.\n        anatomy (Anatomy): Project anatomy.\n\n    Returns:\n        list[dict[str, Any]]: Representation 'files' information.\n\n    \"\"\"\n    file_infos = []\n    for filepath in filepaths:\n        file_info = self.prepare_file_info(filepath, anatomy)\n        file_infos.append(file_info)\n    return file_infos\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/integrate_hero_version.html#client.ayon_core.plugins.publish.integrate_hero_version.IntegrateHeroVersion.get_rootless_path","title":"<code>get_rootless_path(anatomy, path)</code>","text":"<p>Returns, if possible, path without absolute portion from root     (eg. 'c:' or '/opt/..')</p> <p>This information is platform dependent and shouldn't be captured.  Example:      'c:/projects/MyProject1/Assets/publish...' &gt;      '{root}/MyProject1/Assets...'</p> <p>Parameters:</p> Name Type Description Default <code>anatomy</code> <code>Anatomy</code> <p>Project anatomy.</p> required <code>path</code> <code>str</code> <p>Absolute path.</p> required <p>Returns:</p> Name Type Description <code>str</code> <p>Path where root path is replaced by formatting string.</p> Source code in <code>client/ayon_core/plugins/publish/integrate_hero_version.py</code> <pre><code>def get_rootless_path(self, anatomy, path):\n    \"\"\"Returns, if possible, path without absolute portion from root\n        (eg. 'c:\\' or '/opt/..')\n\n     This information is platform dependent and shouldn't be captured.\n     Example:\n         'c:/projects/MyProject1/Assets/publish...' &gt;\n         '{root}/MyProject1/Assets...'\n\n    Args:\n        anatomy (Anatomy): Project anatomy.\n        path (str): Absolute path.\n\n    Returns:\n        str: Path where root path is replaced by formatting string.\n\n    \"\"\"\n    success, rootless_path = anatomy.find_root_template_from_path(path)\n    if success:\n        path = rootless_path\n    else:\n        self.log.warning((\n            \"Could not find root path for remapping \\\"{}\\\".\"\n            \" This may cause issues on farm.\"\n        ).format(path))\n    return path\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/integrate_hero_version.html#client.ayon_core.plugins.publish.integrate_hero_version.IntegrateHeroVersion.prepare_file_info","title":"<code>prepare_file_info(path, anatomy)</code>","text":"<p>Prepare information for one file (asset or resource)</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Destination url of published file.</p> required <code>anatomy</code> <code>Anatomy</code> <p>Project anatomy part from instance.</p> required <p>Returns:</p> Type Description <p>dict[str, Any]: Representation file info dictionary.</p> Source code in <code>client/ayon_core/plugins/publish/integrate_hero_version.py</code> <pre><code>def prepare_file_info(self, path, anatomy):\n    \"\"\" Prepare information for one file (asset or resource)\n\n    Arguments:\n        path (str): Destination url of published file.\n        anatomy (Anatomy): Project anatomy part from instance.\n\n    Returns:\n        dict[str, Any]: Representation file info dictionary.\n\n    \"\"\"\n    return {\n        \"id\": create_entity_id(),\n        \"name\": os.path.basename(path),\n        \"path\": self.get_rootless_path(anatomy, path),\n        \"size\": os.path.getsize(path),\n        \"hash\": source_hash(path),\n        \"hash_type\": \"op3\",\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/integrate_hero_version.html#client.ayon_core.plugins.publish.integrate_hero_version.prepare_changes","title":"<code>prepare_changes(old_entity, new_entity)</code>","text":"<p>Prepare changes for entity update.</p> <p>Parameters:</p> Name Type Description Default <code>old_entity</code> <p>Existing entity.</p> required <code>new_entity</code> <p>New entity.</p> required <p>Returns:</p> Type Description <p>dict[str, Any]: Changes that have new entity.</p> Source code in <code>client/ayon_core/plugins/publish/integrate_hero_version.py</code> <pre><code>def prepare_changes(old_entity, new_entity):\n    \"\"\"Prepare changes for entity update.\n\n    Args:\n        old_entity: Existing entity.\n        new_entity: New entity.\n\n    Returns:\n        dict[str, Any]: Changes that have new entity.\n\n    \"\"\"\n    changes = {}\n    for key in set(new_entity.keys()):\n        if key == \"attrib\":\n            continue\n\n        if key in new_entity and new_entity[key] != old_entity.get(key):\n            changes[key] = new_entity[key]\n            continue\n\n    attrib_changes = {}\n    if \"attrib\" in new_entity:\n        for key, value in new_entity[\"attrib\"].items():\n            if value != old_entity[\"attrib\"].get(key):\n                attrib_changes[key] = value\n    if attrib_changes:\n        changes[\"attrib\"] = attrib_changes\n    return changes\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/integrate_hero_version_with_traits.html","title":"integrate_hero_version_with_traits","text":"<p>Integrate Hero version with representation traits.</p>"},{"location":"autoapi/client/ayon_core/plugins/publish/integrate_hero_version_with_traits.html#client.ayon_core.plugins.publish.integrate_hero_version_with_traits.IntegrateHeroVersionTraits","title":"<code>IntegrateHeroVersionTraits</code>","text":"<p>               Bases: <code>OptionalPyblishPluginMixin</code>, <code>InstancePlugin</code></p> <p>Integrate Hero version with representation traits.</p> Source code in <code>client/ayon_core/plugins/publish/integrate_hero_version_with_traits.py</code> <pre><code>class IntegrateHeroVersionTraits(\n    OptionalPyblishPluginMixin,\n    pyblish.api.InstancePlugin):\n    \"\"\"Integrate Hero version with representation traits.\"\"\"\n\n    order = pyblish.api.IntegratorOrder + 0.1\n    label = \"Integrate Hero version with representation traits\"\n    settings_category = \"core\"\n    optional = True\n    active = True\n\n    # Families are modified using settings\n    families = [\n        \"model\",\n        \"rig\",\n        \"setdress\",\n        \"look\",\n        \"pointcache\",\n        \"animation\"\n    ]\n\n    ignored_representation_names: set[str] = set()\n    log: \"logging.Logger\"\n\n    use_hardlinks = False\n\n    def process(self, instance: pyblish.api.Instance) -&gt; None:\n        \"\"\"Integrate Hero version with representation traits.\n\n        Args:\n            instance (pyblish.api.Instance): The instance to process.\n\n        \"\"\"\n        if not self.is_active(instance.data):\n            return\n\n        anatomy: Anatomy = instance.context.data[\"anatomy\"]\n        project_name = anatomy.project_name\n\n        template_key = self._get_template_key(project_name, instance)\n        hero_template: AnatomyTemplateItem = anatomy.get_template_item(\n            \"hero\", template_key)\n\n        if hero_template is None:\n            self.log.warning(\n                f\"Anatomy of project '{project_name}' does not have set \"\n                f\"'{template_key}' template key!\")\n            return\n\n        hero_publish_dir = self.get_publish_dir(instance, template_key)\n\n        src_version_entity = instance.data.get(\"versionEntity\")\n\n        if src_version_entity is None:\n            msg = (\n                f\"Instance '{instance.name}' does not have 'versionEntity' \"\n                \"data. It has to go first through product integrator.\"\n            )\n            self.log.error(msg)\n            raise PublishError(msg)\n\n        if src_version_entity[\"version\"] == 0:\n            self.log.warning(\"Version 0 cannot have hero version. Skipping.\")\n            return\n\n        # Current hero version data\n        old_version, old_repres = self.current_hero_entities(\n            project_name, src_version_entity\n        )\n\n        # old representations are coming from already existing hero version\n        # new representations are coming from current version that is\n        # being published\n\n        op_session = OperationsSession()\n\n        entity_id = old_version[\"id\"] if old_version else None\n        new_hero_version = new_version_entity(\n            - src_version_entity[\"version\"],\n            src_version_entity[\"productId\"],\n            task_id=src_version_entity.get(\"taskId\"),\n            data=copy.deepcopy(src_version_entity[\"data\"]),\n            attribs=copy.deepcopy(src_version_entity[\"attrib\"]),\n            entity_id=entity_id,\n        )\n        self.log.debug(f\"Prepared hero version entity: {new_hero_version}\")\n\n        if old_version:\n            self.log.debug(\"Replacing old hero version.\")\n            update_data = prepare_changes(\n                old_version, new_hero_version\n            )\n            op_session.update_entity(\n                project_name,\n                \"version\",\n                old_version[\"id\"],\n                update_data\n            )\n        else:\n            self.log.debug(\"Creating first hero version.\")\n            op_session.create_entity(\n                project_name, \"version\", new_hero_version\n            )\n\n        # Store hero entity to 'instance.data'\n        instance.data[\"heroVersionEntity\"] = new_hero_version\n\n        # get published representations with traits for the version\n        repre_entities = list(ayon_api.get_representations(\n            project_name=project_name,\n            version_ids={src_version_entity[\"id\"]}))\n\n        self.log.debug(\n            f\"Found {len(repre_entities)} representations for hero version.\"\n        )\n\n        if not repre_entities:\n            msg = (\n                f\"Version '{src_version_entity['id']}' does not have any \"\n                \"representations. At least one representation with traits \"\n                \"has to be published to create hero version.\"\n            )\n            self.log.error(msg)\n            raise PublishError(msg)\n\n        # Separate old representations into `to replace` and `to delete`\n        inactive_old_repres_by_name = {}\n        old_repres_by_name = {}\n        for repre in old_repres:\n            low_name = repre[\"name\"].lower()\n            if repre[\"active\"]:\n                old_repres_by_name[low_name] = repre\n            else:\n                inactive_old_repres_by_name[low_name] = repre\n\n        old_repres_to_replace = {}\n\n        for repre in repre_entities:\n            repre_name_low = repre[\"name\"].lower()\n            if repre_name_low in old_repres_by_name:\n                old_repres_to_replace[repre_name_low] = (\n                    old_repres_by_name.pop(repre_name_low)\n                )\n\n        # deactivate old representations that are to be replaced\n        if old_repres_to_replace:\n            for repre in old_repres_to_replace.values():\n                if repre[\"active\"]:\n                    op_session.update_representation(\n                        project_name=project_name,\n                        representation_id=repre[\"id\"],\n                        active=False\n                    )\n\n        backup_hero_publish_dir = None\n        if os.path.exists(hero_publish_dir):\n            backup_hero_publish_dir = self._backup_hero_version_dir(\n                hero_publish_dir)\n\n        # prepare new representation entities for hero version\n        new_repre_entities = []\n        representations: list[Representation] = []\n        repre_id_map = {}\n\n        for repre in repre_entities:\n            if (\n                    repre[\"active\"] is False\n                    or repre[\"name\"] in self.ignored_representation_names\n                    or not repre[\"traits\"]\n            ):\n                continue\n\n            representation = Representation.from_dict(\n                name=repre[\"name\"],\n                representation_id=None,\n                trait_data=json.loads(repre[\"traits\"])\n            )\n            repre_id_map[representation.representation_id] = representation\n            representations.append(representation)\n            new_repre = copy.deepcopy(repre)\n            new_repre[\"versionId\"] = new_hero_version[\"id\"]\n            new_repre[\"id\"] = representation.representation_id\n            new_repre[\"traits\"] = representation.traits_as_dict()\n            new_repre_entities.append(new_repre)\n\n        self.log.debug(\n            \"Prepared representations for hero version: %s\",\n            [repre.name for repre in representations]\n        )\n        self.log.debug(f\"Hero template: {hero_template['path']}\")\n\n        transfers = get_transfers_from_representations(\n            instance,\n            template=hero_template,\n            representations=representations)\n\n        self.log.debug(f\"got {len(transfers)} file transfers to \"\n                       \"process for hero version.\")\n        file_transactions = FileTransaction(\n            log=self.log,\n            # Enforce unique transfers\n            allow_queue_replacements=False\n        )\n        mode = FileTransaction.MODE_COPY\n        if self.use_hardlinks:\n            mode = FileTransaction.MODE_HARDLINK\n\n        try:\n            for transfer in transfers:\n                self.log.debug(\n                    \"Transferring file: %s -&gt; %s\",\n                    transfer.source,\n                    transfer.destination\n                )\n                file_transactions.add(\n                    transfer.source.as_posix(),\n                    transfer.destination.as_posix(),\n                    mode=mode,\n                )\n            file_transactions.process()\n        except DuplicateDestinationError as e:\n            msg = (\n                \"Multiple representations are trying to transfer files to \"\n                \"the same destination. This is not allowed because it can \"\n                \"cause conflicts and unintended overwrites. Please check the \"\n                \"representations and their traits to ensure they are unique.\"\n            )\n            file_transactions.rollback()\n            self.log.error(msg)\n            raise PublishError(msg) from e\n        except Exception as e:\n            msg = (\n                \"An error occurred during file transfer for hero version. \"\n                \"Please check the logs for more details.\"\n            )\n            file_transactions.rollback()\n            self.log.error(msg)\n            raise PublishError(msg) from e\n        finally:\n            file_transactions.finalize()\n\n        # create new representation entities and prepare legacy file attrib\n        for new_repre in new_repre_entities:\n            representation = repre_id_map[new_repre[\"id\"]]\n            transfer_items = [\n                transfer\n                for transfer in transfers\n                if getattr(transfer, \"representation\", None) is representation\n            ]\n            files = get_legacy_files_for_representation(\n                transfer_items=transfer_items,\n                representation=representation,\n                anatomy=anatomy,\n            )\n            new_repre[\"files\"] = files\n\n            # replace original paths with the destination\n            # in representation entity\n            replace_paths_in_representation(new_repre, transfers)\n\n            op_session.create_entity(\n                project_name, \"representation\", new_repre\n            )\n        op_session.commit()\n        if (\n                backup_hero_publish_dir is not None and\n                os.path.exists(backup_hero_publish_dir)\n        ):\n            shutil.rmtree(backup_hero_publish_dir)\n\n    def _backup_hero_version_dir(self, hero_publish_dir: str) -&gt; str:\n        \"\"\"Backup current hero version publish directory.\n\n        Args:\n            hero_publish_dir (str): The path to current hero\n                version publish directory.\n        \"\"\"\n        backup_hero_publish_dir = f\"{hero_publish_dir}.BACKUP\"\n        # max backup dirs present\n        max_idx = 10\n        idx = 0\n        _backup_hero_publish_dir = backup_hero_publish_dir\n        while os.path.exists(_backup_hero_publish_dir):\n            self.log.debug(\n                \"Backup folder already exists. \"\n                f'Trying to remove \"{_backup_hero_publish_dir}\"'\n            )\n\n            try:\n                shutil.rmtree(_backup_hero_publish_dir)\n                backup_hero_publish_dir = _backup_hero_publish_dir\n                break\n            except Exception:\n                self.log.info(\n                    \"Could not remove previous backup folder. \"\n                    \"Trying to add index to folder name.\"\n                )\n\n            _backup_hero_publish_dir = (\n                    backup_hero_publish_dir + str(idx)\n            )\n            if not os.path.exists(_backup_hero_publish_dir):\n                backup_hero_publish_dir = _backup_hero_publish_dir\n                break\n\n            if idx &gt; max_idx:\n                msg = (\n                    \"Backup folders are fully occupied \"\n                    f'to max index \"{max_idx}\"'\n                )\n                raise AssertionError(msg)\n            idx += 1\n\n        self.log.debug(f'Backup folder path is \\\"{backup_hero_publish_dir}\\\"')\n        try:\n            os.rename(hero_publish_dir, backup_hero_publish_dir)\n        except PermissionError as e:\n            msg = (\n                \"Could not create hero version because it is not \"\n                \"possible to replace current hero files.\"\n            )\n            raise AssertionError(msg) from e\n\n        return backup_hero_publish_dir\n\n    @staticmethod\n    def version_from_representations(\n            project_name: str,\n            repres: list[Representation]) -&gt; Optional[dict]:\n        \"\"\"Get version entity from representations.\n\n        Args:\n            project_name (str): The name of the project.\n            repres (list[Representation]): List of representations\n                to check.\n\n        Returns:\n            Optional[dict]: The version entity if found, otherwise None.\n\n        \"\"\"\n        for rep in repres:\n            version = ayon_api.get_version_by_id(\n                project_name, rep.representation_id\n            )\n            if version:\n                return version\n\n        return None\n\n    def _get_template_key(\n            self,\n            project_name: str,\n            instance: pyblish.api.Instance) -&gt; str:\n        \"\"\"Get template key for hero template.\n\n        Args:\n            project_name (str): The name of the project.\n            instance (pyblish.api.Instance): The instance to get data from.\n\n        Returns:\n            str: The template key to use for hero template.\n\n        \"\"\"\n        anatomy_data = instance.data[\"anatomyData\"]\n        task_info = anatomy_data.get(\"task\") or {}\n        host_name = instance.context.data[\"hostName\"]\n        product_base_type = (\n                instance.data.get(\"productBaseType\")\n                or instance.data[\"productType\"]\n        )\n\n        return get_publish_template_name(\n            project_name,\n            host_name,\n            product_base_type=product_base_type,\n            task_name=task_info.get(\"name\"),\n            task_type=task_info.get(\"type\"),\n            project_settings=instance.context.data[\"project_settings\"],\n            hero=True,\n            logger=self.log\n        )\n\n    @staticmethod\n    def current_hero_entities(\n            project_name: str,\n            version: dict) -&gt; tuple[Optional[dict], list[dict]]:\n        \"\"\"Get current hero version and representations.\n\n        Args:\n            project_name (str): The name of the project.\n            version (dict): The version entity to find hero version for.\n\n        Returns:\n            tuple[Optional[dict], list[dict]]: The hero version entity and list\n                of its representations. If hero version is not found, returns\n                (None, []).\n\n        \"\"\"\n        hero_version = ayon_api.get_hero_version_by_product_id(\n            project_name, version[\"productId\"]\n        )\n\n        if not hero_version:\n            return None, []\n\n        hero_repres = list(ayon_api.get_representations(\n            project_name, version_ids={hero_version[\"id\"]}\n        ))\n        return hero_version, hero_repres\n\n    def get_publish_dir(\n            self,\n            instance: pyblish.api.Instance,\n            template_key: str) -&gt; str:\n        \"\"\"Get publish directory for hero version.\n\n        Args:\n            instance (pyblish.api.Instance): The instance to get data from.\n            template_key (str): The template key to use for hero template.\n\n        Returns:\n            str: The path to publish directory for hero version.\n\n        \"\"\"\n        anatomy = instance.context.data[\"anatomy\"]\n        template_data = copy.deepcopy(instance.data[\"anatomyData\"])\n\n        if \"originalBasename\" in instance.data:\n            template_data.update({\n                \"originalBasename\": instance.data.get(\"originalBasename\")\n            })\n\n        template_obj: AnatomyStringTemplate = anatomy.get_template_item(\n            \"hero\", template_key, \"directory\"\n        )\n        publish_folder = os.path.normpath(\n            template_obj.format_strict(template_data)\n        )\n\n        self.log.debug(f'hero publish dir: \"{publish_folder}\"')\n\n        return publish_folder\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/integrate_hero_version_with_traits.html#client.ayon_core.plugins.publish.integrate_hero_version_with_traits.IntegrateHeroVersionTraits.current_hero_entities","title":"<code>current_hero_entities(project_name, version)</code>  <code>staticmethod</code>","text":"<p>Get current hero version and representations.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>The name of the project.</p> required <code>version</code> <code>dict</code> <p>The version entity to find hero version for.</p> required <p>Returns:</p> Type Description <code>tuple[Optional[dict], list[dict]]</code> <p>tuple[Optional[dict], list[dict]]: The hero version entity and list of its representations. If hero version is not found, returns (None, []).</p> Source code in <code>client/ayon_core/plugins/publish/integrate_hero_version_with_traits.py</code> <pre><code>@staticmethod\ndef current_hero_entities(\n        project_name: str,\n        version: dict) -&gt; tuple[Optional[dict], list[dict]]:\n    \"\"\"Get current hero version and representations.\n\n    Args:\n        project_name (str): The name of the project.\n        version (dict): The version entity to find hero version for.\n\n    Returns:\n        tuple[Optional[dict], list[dict]]: The hero version entity and list\n            of its representations. If hero version is not found, returns\n            (None, []).\n\n    \"\"\"\n    hero_version = ayon_api.get_hero_version_by_product_id(\n        project_name, version[\"productId\"]\n    )\n\n    if not hero_version:\n        return None, []\n\n    hero_repres = list(ayon_api.get_representations(\n        project_name, version_ids={hero_version[\"id\"]}\n    ))\n    return hero_version, hero_repres\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/integrate_hero_version_with_traits.html#client.ayon_core.plugins.publish.integrate_hero_version_with_traits.IntegrateHeroVersionTraits.get_publish_dir","title":"<code>get_publish_dir(instance, template_key)</code>","text":"<p>Get publish directory for hero version.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>Instance</code> <p>The instance to get data from.</p> required <code>template_key</code> <code>str</code> <p>The template key to use for hero template.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>The path to publish directory for hero version.</p> Source code in <code>client/ayon_core/plugins/publish/integrate_hero_version_with_traits.py</code> <pre><code>def get_publish_dir(\n        self,\n        instance: pyblish.api.Instance,\n        template_key: str) -&gt; str:\n    \"\"\"Get publish directory for hero version.\n\n    Args:\n        instance (pyblish.api.Instance): The instance to get data from.\n        template_key (str): The template key to use for hero template.\n\n    Returns:\n        str: The path to publish directory for hero version.\n\n    \"\"\"\n    anatomy = instance.context.data[\"anatomy\"]\n    template_data = copy.deepcopy(instance.data[\"anatomyData\"])\n\n    if \"originalBasename\" in instance.data:\n        template_data.update({\n            \"originalBasename\": instance.data.get(\"originalBasename\")\n        })\n\n    template_obj: AnatomyStringTemplate = anatomy.get_template_item(\n        \"hero\", template_key, \"directory\"\n    )\n    publish_folder = os.path.normpath(\n        template_obj.format_strict(template_data)\n    )\n\n    self.log.debug(f'hero publish dir: \"{publish_folder}\"')\n\n    return publish_folder\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/integrate_hero_version_with_traits.html#client.ayon_core.plugins.publish.integrate_hero_version_with_traits.IntegrateHeroVersionTraits.process","title":"<code>process(instance)</code>","text":"<p>Integrate Hero version with representation traits.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>Instance</code> <p>The instance to process.</p> required Source code in <code>client/ayon_core/plugins/publish/integrate_hero_version_with_traits.py</code> <pre><code>def process(self, instance: pyblish.api.Instance) -&gt; None:\n    \"\"\"Integrate Hero version with representation traits.\n\n    Args:\n        instance (pyblish.api.Instance): The instance to process.\n\n    \"\"\"\n    if not self.is_active(instance.data):\n        return\n\n    anatomy: Anatomy = instance.context.data[\"anatomy\"]\n    project_name = anatomy.project_name\n\n    template_key = self._get_template_key(project_name, instance)\n    hero_template: AnatomyTemplateItem = anatomy.get_template_item(\n        \"hero\", template_key)\n\n    if hero_template is None:\n        self.log.warning(\n            f\"Anatomy of project '{project_name}' does not have set \"\n            f\"'{template_key}' template key!\")\n        return\n\n    hero_publish_dir = self.get_publish_dir(instance, template_key)\n\n    src_version_entity = instance.data.get(\"versionEntity\")\n\n    if src_version_entity is None:\n        msg = (\n            f\"Instance '{instance.name}' does not have 'versionEntity' \"\n            \"data. It has to go first through product integrator.\"\n        )\n        self.log.error(msg)\n        raise PublishError(msg)\n\n    if src_version_entity[\"version\"] == 0:\n        self.log.warning(\"Version 0 cannot have hero version. Skipping.\")\n        return\n\n    # Current hero version data\n    old_version, old_repres = self.current_hero_entities(\n        project_name, src_version_entity\n    )\n\n    # old representations are coming from already existing hero version\n    # new representations are coming from current version that is\n    # being published\n\n    op_session = OperationsSession()\n\n    entity_id = old_version[\"id\"] if old_version else None\n    new_hero_version = new_version_entity(\n        - src_version_entity[\"version\"],\n        src_version_entity[\"productId\"],\n        task_id=src_version_entity.get(\"taskId\"),\n        data=copy.deepcopy(src_version_entity[\"data\"]),\n        attribs=copy.deepcopy(src_version_entity[\"attrib\"]),\n        entity_id=entity_id,\n    )\n    self.log.debug(f\"Prepared hero version entity: {new_hero_version}\")\n\n    if old_version:\n        self.log.debug(\"Replacing old hero version.\")\n        update_data = prepare_changes(\n            old_version, new_hero_version\n        )\n        op_session.update_entity(\n            project_name,\n            \"version\",\n            old_version[\"id\"],\n            update_data\n        )\n    else:\n        self.log.debug(\"Creating first hero version.\")\n        op_session.create_entity(\n            project_name, \"version\", new_hero_version\n        )\n\n    # Store hero entity to 'instance.data'\n    instance.data[\"heroVersionEntity\"] = new_hero_version\n\n    # get published representations with traits for the version\n    repre_entities = list(ayon_api.get_representations(\n        project_name=project_name,\n        version_ids={src_version_entity[\"id\"]}))\n\n    self.log.debug(\n        f\"Found {len(repre_entities)} representations for hero version.\"\n    )\n\n    if not repre_entities:\n        msg = (\n            f\"Version '{src_version_entity['id']}' does not have any \"\n            \"representations. At least one representation with traits \"\n            \"has to be published to create hero version.\"\n        )\n        self.log.error(msg)\n        raise PublishError(msg)\n\n    # Separate old representations into `to replace` and `to delete`\n    inactive_old_repres_by_name = {}\n    old_repres_by_name = {}\n    for repre in old_repres:\n        low_name = repre[\"name\"].lower()\n        if repre[\"active\"]:\n            old_repres_by_name[low_name] = repre\n        else:\n            inactive_old_repres_by_name[low_name] = repre\n\n    old_repres_to_replace = {}\n\n    for repre in repre_entities:\n        repre_name_low = repre[\"name\"].lower()\n        if repre_name_low in old_repres_by_name:\n            old_repres_to_replace[repre_name_low] = (\n                old_repres_by_name.pop(repre_name_low)\n            )\n\n    # deactivate old representations that are to be replaced\n    if old_repres_to_replace:\n        for repre in old_repres_to_replace.values():\n            if repre[\"active\"]:\n                op_session.update_representation(\n                    project_name=project_name,\n                    representation_id=repre[\"id\"],\n                    active=False\n                )\n\n    backup_hero_publish_dir = None\n    if os.path.exists(hero_publish_dir):\n        backup_hero_publish_dir = self._backup_hero_version_dir(\n            hero_publish_dir)\n\n    # prepare new representation entities for hero version\n    new_repre_entities = []\n    representations: list[Representation] = []\n    repre_id_map = {}\n\n    for repre in repre_entities:\n        if (\n                repre[\"active\"] is False\n                or repre[\"name\"] in self.ignored_representation_names\n                or not repre[\"traits\"]\n        ):\n            continue\n\n        representation = Representation.from_dict(\n            name=repre[\"name\"],\n            representation_id=None,\n            trait_data=json.loads(repre[\"traits\"])\n        )\n        repre_id_map[representation.representation_id] = representation\n        representations.append(representation)\n        new_repre = copy.deepcopy(repre)\n        new_repre[\"versionId\"] = new_hero_version[\"id\"]\n        new_repre[\"id\"] = representation.representation_id\n        new_repre[\"traits\"] = representation.traits_as_dict()\n        new_repre_entities.append(new_repre)\n\n    self.log.debug(\n        \"Prepared representations for hero version: %s\",\n        [repre.name for repre in representations]\n    )\n    self.log.debug(f\"Hero template: {hero_template['path']}\")\n\n    transfers = get_transfers_from_representations(\n        instance,\n        template=hero_template,\n        representations=representations)\n\n    self.log.debug(f\"got {len(transfers)} file transfers to \"\n                   \"process for hero version.\")\n    file_transactions = FileTransaction(\n        log=self.log,\n        # Enforce unique transfers\n        allow_queue_replacements=False\n    )\n    mode = FileTransaction.MODE_COPY\n    if self.use_hardlinks:\n        mode = FileTransaction.MODE_HARDLINK\n\n    try:\n        for transfer in transfers:\n            self.log.debug(\n                \"Transferring file: %s -&gt; %s\",\n                transfer.source,\n                transfer.destination\n            )\n            file_transactions.add(\n                transfer.source.as_posix(),\n                transfer.destination.as_posix(),\n                mode=mode,\n            )\n        file_transactions.process()\n    except DuplicateDestinationError as e:\n        msg = (\n            \"Multiple representations are trying to transfer files to \"\n            \"the same destination. This is not allowed because it can \"\n            \"cause conflicts and unintended overwrites. Please check the \"\n            \"representations and their traits to ensure they are unique.\"\n        )\n        file_transactions.rollback()\n        self.log.error(msg)\n        raise PublishError(msg) from e\n    except Exception as e:\n        msg = (\n            \"An error occurred during file transfer for hero version. \"\n            \"Please check the logs for more details.\"\n        )\n        file_transactions.rollback()\n        self.log.error(msg)\n        raise PublishError(msg) from e\n    finally:\n        file_transactions.finalize()\n\n    # create new representation entities and prepare legacy file attrib\n    for new_repre in new_repre_entities:\n        representation = repre_id_map[new_repre[\"id\"]]\n        transfer_items = [\n            transfer\n            for transfer in transfers\n            if getattr(transfer, \"representation\", None) is representation\n        ]\n        files = get_legacy_files_for_representation(\n            transfer_items=transfer_items,\n            representation=representation,\n            anatomy=anatomy,\n        )\n        new_repre[\"files\"] = files\n\n        # replace original paths with the destination\n        # in representation entity\n        replace_paths_in_representation(new_repre, transfers)\n\n        op_session.create_entity(\n            project_name, \"representation\", new_repre\n        )\n    op_session.commit()\n    if (\n            backup_hero_publish_dir is not None and\n            os.path.exists(backup_hero_publish_dir)\n    ):\n        shutil.rmtree(backup_hero_publish_dir)\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/integrate_hero_version_with_traits.html#client.ayon_core.plugins.publish.integrate_hero_version_with_traits.IntegrateHeroVersionTraits.version_from_representations","title":"<code>version_from_representations(project_name, repres)</code>  <code>staticmethod</code>","text":"<p>Get version entity from representations.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>The name of the project.</p> required <code>repres</code> <code>list[Representation]</code> <p>List of representations to check.</p> required <p>Returns:</p> Type Description <code>Optional[dict]</code> <p>Optional[dict]: The version entity if found, otherwise None.</p> Source code in <code>client/ayon_core/plugins/publish/integrate_hero_version_with_traits.py</code> <pre><code>@staticmethod\ndef version_from_representations(\n        project_name: str,\n        repres: list[Representation]) -&gt; Optional[dict]:\n    \"\"\"Get version entity from representations.\n\n    Args:\n        project_name (str): The name of the project.\n        repres (list[Representation]): List of representations\n            to check.\n\n    Returns:\n        Optional[dict]: The version entity if found, otherwise None.\n\n    \"\"\"\n    for rep in repres:\n        version = ayon_api.get_version_by_id(\n            project_name, rep.representation_id\n        )\n        if version:\n            return version\n\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/integrate_hero_version_with_traits.html#client.ayon_core.plugins.publish.integrate_hero_version_with_traits.prepare_changes","title":"<code>prepare_changes(old_entity, new_entity)</code>","text":"<p>Prepare changes for entity update.</p> <p>Parameters:</p> Name Type Description Default <code>old_entity</code> <p>Existing entity.</p> required <code>new_entity</code> <p>New entity.</p> required <p>Returns:</p> Type Description <p>dict[str, Any]: Changes that have new entity.</p> Source code in <code>client/ayon_core/plugins/publish/integrate_hero_version_with_traits.py</code> <pre><code>def prepare_changes(old_entity, new_entity):\n    \"\"\"Prepare changes for entity update.\n\n    Args:\n        old_entity: Existing entity.\n        new_entity: New entity.\n\n    Returns:\n        dict[str, Any]: Changes that have new entity.\n\n    \"\"\"\n    changes = {}\n    for key in set(new_entity.keys()):\n        if key == \"attrib\":\n            continue\n\n        if key in new_entity and new_entity[key] != old_entity.get(key):\n            changes[key] = new_entity[key]\n            continue\n\n    attrib_changes = {}\n    if \"attrib\" in new_entity:\n        for key, value in new_entity[\"attrib\"].items():\n            if value != old_entity[\"attrib\"].get(key):\n                attrib_changes[key] = value\n    if attrib_changes:\n        changes[\"attrib\"] = attrib_changes\n    return changes\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/integrate_inputlinks.html","title":"integrate_inputlinks","text":""},{"location":"autoapi/client/ayon_core/plugins/publish/integrate_inputlinks.html#client.ayon_core.plugins.publish.integrate_inputlinks.IntegrateInputLinksAYON","title":"<code>IntegrateInputLinksAYON</code>","text":"<p>               Bases: <code>ContextPlugin</code></p> <p>Connecting version level dependency links</p> Handles links <ul> <li>generative - what gets produced from workfile</li> <li>reference - what was loaded into workfile</li> </ul> <p>It expects workfile instance is being published.</p> Source code in <code>client/ayon_core/plugins/publish/integrate_inputlinks.py</code> <pre><code>class IntegrateInputLinksAYON(pyblish.api.ContextPlugin):\n    \"\"\"Connecting version level dependency links\n\n    Handles links:\n        - generative - what gets produced from workfile\n        - reference - what was loaded into workfile\n\n    It expects workfile instance is being published.\n    \"\"\"\n\n    order = pyblish.api.IntegratorOrder + 0.2\n    label = \"Connect Dependency InputLinks AYON\"\n\n    def process(self, context: pyblish.api.Context):\n        \"\"\"Connect dependency links for all instances, globally\n\n        Code steps:\n        - filter instances that integrated version\n            - have \"versionEntity\" entry in data\n        - separate workfile instance within filtered instances\n        - when workfile instance is available:\n            - link all `loadedVersions` as input of the workfile\n            - link workfile as input of all other integrated versions\n        - link version's inputs if it's instance have \"inputVersions\" entry\n        -\n\n        inputVersions:\n            The \"inputVersions\" in instance.data should be a list of\n            version ids (str), which are the dependencies of the publishing\n            instance that should be extracted from working scene by the DCC\n            specific publish plugin.\n        \"\"\"\n\n        workfile_instance, other_instances = self.split_instances(context)\n\n        # Variable where links are stored in submethods\n        new_links_by_type: LinksByType = collections.defaultdict(list)\n\n        self.create_workfile_links(\n            workfile_instance, other_instances, new_links_by_type)\n\n        self.create_generative_links(other_instances, new_links_by_type)\n\n        self.create_links_on_server(context, new_links_by_type)\n\n    def split_instances(\n        self,\n        context: pyblish.api.Context\n    ) -&gt; tuple[Union[pyblish.api.Instance, None], list[pyblish.api.Instance]]:\n        \"\"\"Separates published instances into workfile and other.\n\n        Returns:\n            tuple[\n                pyblish.plugin.Instance | None,\n                list(pyblish.plugin.Instance)\n            ]: workfile instance and list of other instances\n        \"\"\"\n        workfile_instance = None\n        other_instances = []\n\n        for instance in context:\n            # Skip inactive instances\n            if not instance.data.get(\"publish\", True):\n                continue\n\n            if not instance.data.get(\"versionEntity\"):\n                self.log.debug(\n                    \"Instance {} doesn't have version.\".format(instance))\n                continue\n\n            product_base_type = instance.data.get(\"productBaseType\")\n            if not product_base_type:\n                product_base_type = instance.data[\"productType\"]\n\n            if product_base_type == \"workfile\":\n                workfile_instance = instance\n            else:\n                other_instances.append(instance)\n        return workfile_instance, other_instances\n\n    def add_link(\n        self,\n        new_links_by_type: LinksByType,\n        link_type: str,\n        input_id: str,\n        output_id: str,\n        data: dict[str, Any],\n    ):\n        \"\"\"Add dependency link data into temporary variable.\n\n        Args:\n            new_links_by_type (LinksByType): Object where output is stored.\n            link_type (str): Type of link, one of 'reference' or 'generative'\n            input_id (str): Input version id.\n            output_id (str): Output version id.\n            data (dict[str, Any]): Link metadata.\n        \"\"\"\n        new_links_by_type[link_type].append(\n            LinkPayload(\n                input_id=input_id,\n                output_id=output_id,\n                data=data,\n                link_type=link_type,\n            )\n        )\n\n    def create_workfile_links(\n        self,\n        workfile_instance: Optional[pyblish.api.Instance],\n        other_instances: list[pyblish.api.Instance],\n        new_links_by_type: LinksByType,\n    ):\n        \"\"\"Adds links (generative and reference) for workfile.\n\n        Args:\n            workfile_instance (Optional[pyblish.plugin.Instance]): Published\n                workfile.\n            other_instances (list[pyblish.plugin.Instance]): Other published\n                instances\n            new_links_by_type (LinksByType): Dictionary collecting new created\n                links by its type.\n        \"\"\"\n        if workfile_instance is None:\n            self.log.debug(\"No workfile in this publish session.\")\n            return\n\n        workfile_version_id = workfile_instance.data[\"versionEntity\"][\"id\"]\n        # link workfile to all publishing versions\n        for instance in other_instances:\n            self.add_link(\n                new_links_by_type,\n                link_type=\"generative\",\n                input_id=workfile_version_id,\n                output_id=instance.data[\"versionEntity\"][\"id\"],\n                data={}\n            )\n\n        loaded_versions = workfile_instance.context.data.get(\"loadedVersions\")\n        if not loaded_versions:\n            return\n\n        # link all loaded versions in scene into workfile\n        for input_version in loaded_versions:\n            input_version = InputVersion.from_value(input_version)\n            self.add_link(\n                new_links_by_type,\n                link_type=\"reference\",\n                input_id=input_version.version_id,\n                output_id=workfile_version_id,\n                data=input_version.data\n            )\n\n    def create_generative_links(\n        self,\n        other_instances: list[pyblish.api.Instance],\n        new_links_by_type: LinksByType,\n    ):\n        for instance in other_instances:\n            input_versions = instance.data.get(\"inputVersions\")\n            if not input_versions:\n                continue\n\n            version_entity = instance.data[\"versionEntity\"]\n            for input_version in input_versions:\n                input_version = InputVersion.from_value(input_version)\n                self.add_link(\n                    new_links_by_type,\n                    link_type=\"generative\",\n                    input_id=input_version.version_id,\n                    output_id=version_entity[\"id\"],\n                    data=input_version.data,\n                )\n\n    def _get_existing_links(self, project_name, link_type, entity_ids):\n        \"\"\"Find all existing links for given version ids.\n\n        Args:\n            project_name (str): Name of project.\n            link_type (str): Type of link.\n            entity_ids (set[str]): Set of version ids.\n\n        Returns:\n            dict[str, set[str]]: Existing links by version id.\n        \"\"\"\n\n        output = collections.defaultdict(set)\n        if not entity_ids:\n            return output\n\n        existing_in_links = ayon_api.get_versions_links(\n            project_name, entity_ids, [link_type], \"out\"\n        )\n\n        for entity_id, links in existing_in_links.items():\n            if not links:\n                continue\n            for link in links:\n                output[entity_id].add(link[\"entityId\"])\n        return output\n\n    def create_links_on_server(\n        self,\n        context: pyblish.api.Context,\n        new_links: LinksByType,\n    ):\n        \"\"\"Create new links on server.\"\"\"\n        if not new_links:\n            return\n\n        project_name: str = context.data[\"projectName\"]\n\n        # Make sure link types are available on server\n        for link_type in new_links.keys():\n            ayon_api.make_sure_link_type_exists(\n                project_name, link_type, \"version\", \"version\"\n            )\n\n        # Define new links to create\n        links_to_create = []\n        for link_type, link_payloads in new_links.items():\n            # Make sure there are no duplicates of src &gt; dst ids and merge\n            # metadata if multiple links are found.\n            mapping: dict[tuple[str, str], dict] = collections.defaultdict(\n                dict\n            )\n            for link_payload in link_payloads:\n                connection = (link_payload.input_id, link_payload.output_id)\n                mapping[connection].update(link_payload.data)\n\n            in_ids = {payload.input_id for payload in link_payloads}\n            existing_links_by_in_id = self._get_existing_links(\n                project_name, link_type, in_ids\n            )\n\n            for (input_id, output_id), data in mapping.items():\n                existing_links = existing_links_by_in_id[input_id]\n                # Skip creation of link if already exists\n                # NOTE: AYON server does not support\n                #     to have same links\n                if output_id in existing_links:\n                    continue\n\n                links_to_create.append(\n                    LinkPayload(\n                        input_id=input_id,\n                        output_id=output_id,\n                        data=data,\n                        link_type=link_type,\n                    )\n                )\n\n        # Create link themselves\n        self.log.debug(f\"Generating {len(links_to_create)} links\")\n        if ayon_api.get_server_version_tuple() &gt;= (1, 15, 8):\n            create_links_bulk(project_name, links_to_create)\n        else:\n            for link_payload in links_to_create:\n                create_link(\n                    project_name,\n                    link_payload.link_type,\n                    link_payload.input_id,\n                    \"version\",\n                    link_payload.output_id,\n                    \"version\",\n                    data=link_payload.data,\n                )\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/integrate_inputlinks.html#client.ayon_core.plugins.publish.integrate_inputlinks.IntegrateInputLinksAYON.add_link","title":"<code>add_link(new_links_by_type, link_type, input_id, output_id, data)</code>","text":"<p>Add dependency link data into temporary variable.</p> <p>Parameters:</p> Name Type Description Default <code>new_links_by_type</code> <code>LinksByType</code> <p>Object where output is stored.</p> required <code>link_type</code> <code>str</code> <p>Type of link, one of 'reference' or 'generative'</p> required <code>input_id</code> <code>str</code> <p>Input version id.</p> required <code>output_id</code> <code>str</code> <p>Output version id.</p> required <code>data</code> <code>dict[str, Any]</code> <p>Link metadata.</p> required Source code in <code>client/ayon_core/plugins/publish/integrate_inputlinks.py</code> <pre><code>def add_link(\n    self,\n    new_links_by_type: LinksByType,\n    link_type: str,\n    input_id: str,\n    output_id: str,\n    data: dict[str, Any],\n):\n    \"\"\"Add dependency link data into temporary variable.\n\n    Args:\n        new_links_by_type (LinksByType): Object where output is stored.\n        link_type (str): Type of link, one of 'reference' or 'generative'\n        input_id (str): Input version id.\n        output_id (str): Output version id.\n        data (dict[str, Any]): Link metadata.\n    \"\"\"\n    new_links_by_type[link_type].append(\n        LinkPayload(\n            input_id=input_id,\n            output_id=output_id,\n            data=data,\n            link_type=link_type,\n        )\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/integrate_inputlinks.html#client.ayon_core.plugins.publish.integrate_inputlinks.IntegrateInputLinksAYON.create_links_on_server","title":"<code>create_links_on_server(context, new_links)</code>","text":"<p>Create new links on server.</p> Source code in <code>client/ayon_core/plugins/publish/integrate_inputlinks.py</code> <pre><code>def create_links_on_server(\n    self,\n    context: pyblish.api.Context,\n    new_links: LinksByType,\n):\n    \"\"\"Create new links on server.\"\"\"\n    if not new_links:\n        return\n\n    project_name: str = context.data[\"projectName\"]\n\n    # Make sure link types are available on server\n    for link_type in new_links.keys():\n        ayon_api.make_sure_link_type_exists(\n            project_name, link_type, \"version\", \"version\"\n        )\n\n    # Define new links to create\n    links_to_create = []\n    for link_type, link_payloads in new_links.items():\n        # Make sure there are no duplicates of src &gt; dst ids and merge\n        # metadata if multiple links are found.\n        mapping: dict[tuple[str, str], dict] = collections.defaultdict(\n            dict\n        )\n        for link_payload in link_payloads:\n            connection = (link_payload.input_id, link_payload.output_id)\n            mapping[connection].update(link_payload.data)\n\n        in_ids = {payload.input_id for payload in link_payloads}\n        existing_links_by_in_id = self._get_existing_links(\n            project_name, link_type, in_ids\n        )\n\n        for (input_id, output_id), data in mapping.items():\n            existing_links = existing_links_by_in_id[input_id]\n            # Skip creation of link if already exists\n            # NOTE: AYON server does not support\n            #     to have same links\n            if output_id in existing_links:\n                continue\n\n            links_to_create.append(\n                LinkPayload(\n                    input_id=input_id,\n                    output_id=output_id,\n                    data=data,\n                    link_type=link_type,\n                )\n            )\n\n    # Create link themselves\n    self.log.debug(f\"Generating {len(links_to_create)} links\")\n    if ayon_api.get_server_version_tuple() &gt;= (1, 15, 8):\n        create_links_bulk(project_name, links_to_create)\n    else:\n        for link_payload in links_to_create:\n            create_link(\n                project_name,\n                link_payload.link_type,\n                link_payload.input_id,\n                \"version\",\n                link_payload.output_id,\n                \"version\",\n                data=link_payload.data,\n            )\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/integrate_inputlinks.html#client.ayon_core.plugins.publish.integrate_inputlinks.IntegrateInputLinksAYON.create_workfile_links","title":"<code>create_workfile_links(workfile_instance, other_instances, new_links_by_type)</code>","text":"<p>Adds links (generative and reference) for workfile.</p> <p>Parameters:</p> Name Type Description Default <code>workfile_instance</code> <code>Optional[Instance]</code> <p>Published workfile.</p> required <code>other_instances</code> <code>list[Instance]</code> <p>Other published instances</p> required <code>new_links_by_type</code> <code>LinksByType</code> <p>Dictionary collecting new created links by its type.</p> required Source code in <code>client/ayon_core/plugins/publish/integrate_inputlinks.py</code> <pre><code>def create_workfile_links(\n    self,\n    workfile_instance: Optional[pyblish.api.Instance],\n    other_instances: list[pyblish.api.Instance],\n    new_links_by_type: LinksByType,\n):\n    \"\"\"Adds links (generative and reference) for workfile.\n\n    Args:\n        workfile_instance (Optional[pyblish.plugin.Instance]): Published\n            workfile.\n        other_instances (list[pyblish.plugin.Instance]): Other published\n            instances\n        new_links_by_type (LinksByType): Dictionary collecting new created\n            links by its type.\n    \"\"\"\n    if workfile_instance is None:\n        self.log.debug(\"No workfile in this publish session.\")\n        return\n\n    workfile_version_id = workfile_instance.data[\"versionEntity\"][\"id\"]\n    # link workfile to all publishing versions\n    for instance in other_instances:\n        self.add_link(\n            new_links_by_type,\n            link_type=\"generative\",\n            input_id=workfile_version_id,\n            output_id=instance.data[\"versionEntity\"][\"id\"],\n            data={}\n        )\n\n    loaded_versions = workfile_instance.context.data.get(\"loadedVersions\")\n    if not loaded_versions:\n        return\n\n    # link all loaded versions in scene into workfile\n    for input_version in loaded_versions:\n        input_version = InputVersion.from_value(input_version)\n        self.add_link(\n            new_links_by_type,\n            link_type=\"reference\",\n            input_id=input_version.version_id,\n            output_id=workfile_version_id,\n            data=input_version.data\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/integrate_inputlinks.html#client.ayon_core.plugins.publish.integrate_inputlinks.IntegrateInputLinksAYON.process","title":"<code>process(context)</code>","text":"<p>Connect dependency links for all instances, globally</p> <p>Code steps: - filter instances that integrated version     - have \"versionEntity\" entry in data - separate workfile instance within filtered instances - when workfile instance is available:     - link all <code>loadedVersions</code> as input of the workfile     - link workfile as input of all other integrated versions - link version's inputs if it's instance have \"inputVersions\" entry -</p> inputVersions <p>The \"inputVersions\" in instance.data should be a list of version ids (str), which are the dependencies of the publishing instance that should be extracted from working scene by the DCC specific publish plugin.</p> Source code in <code>client/ayon_core/plugins/publish/integrate_inputlinks.py</code> <pre><code>def process(self, context: pyblish.api.Context):\n    \"\"\"Connect dependency links for all instances, globally\n\n    Code steps:\n    - filter instances that integrated version\n        - have \"versionEntity\" entry in data\n    - separate workfile instance within filtered instances\n    - when workfile instance is available:\n        - link all `loadedVersions` as input of the workfile\n        - link workfile as input of all other integrated versions\n    - link version's inputs if it's instance have \"inputVersions\" entry\n    -\n\n    inputVersions:\n        The \"inputVersions\" in instance.data should be a list of\n        version ids (str), which are the dependencies of the publishing\n        instance that should be extracted from working scene by the DCC\n        specific publish plugin.\n    \"\"\"\n\n    workfile_instance, other_instances = self.split_instances(context)\n\n    # Variable where links are stored in submethods\n    new_links_by_type: LinksByType = collections.defaultdict(list)\n\n    self.create_workfile_links(\n        workfile_instance, other_instances, new_links_by_type)\n\n    self.create_generative_links(other_instances, new_links_by_type)\n\n    self.create_links_on_server(context, new_links_by_type)\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/integrate_inputlinks.html#client.ayon_core.plugins.publish.integrate_inputlinks.IntegrateInputLinksAYON.split_instances","title":"<code>split_instances(context)</code>","text":"<p>Separates published instances into workfile and other.</p> <p>Returns:</p> Type Description <code>Union[Instance, None]</code> <p>tuple[ pyblish.plugin.Instance | None, list(pyblish.plugin.Instance)</p> <code>list[Instance]</code> <p>]: workfile instance and list of other instances</p> Source code in <code>client/ayon_core/plugins/publish/integrate_inputlinks.py</code> <pre><code>def split_instances(\n    self,\n    context: pyblish.api.Context\n) -&gt; tuple[Union[pyblish.api.Instance, None], list[pyblish.api.Instance]]:\n    \"\"\"Separates published instances into workfile and other.\n\n    Returns:\n        tuple[\n            pyblish.plugin.Instance | None,\n            list(pyblish.plugin.Instance)\n        ]: workfile instance and list of other instances\n    \"\"\"\n    workfile_instance = None\n    other_instances = []\n\n    for instance in context:\n        # Skip inactive instances\n        if not instance.data.get(\"publish\", True):\n            continue\n\n        if not instance.data.get(\"versionEntity\"):\n            self.log.debug(\n                \"Instance {} doesn't have version.\".format(instance))\n            continue\n\n        product_base_type = instance.data.get(\"productBaseType\")\n        if not product_base_type:\n            product_base_type = instance.data[\"productType\"]\n\n        if product_base_type == \"workfile\":\n            workfile_instance = instance\n        else:\n            other_instances.append(instance)\n    return workfile_instance, other_instances\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/integrate_inputlinks.html#client.ayon_core.plugins.publish.integrate_inputlinks.create_link","title":"<code>create_link(project_name, link_type_name, input_id, input_type, output_id, output_type, data)</code>","text":"<p>Create link in AYON.</p> <p>TODO Replace with 'ayon_api.create_link' when AYON launcher &gt;= 1.5.2     is required by ayon-core.</p> Source code in <code>client/ayon_core/plugins/publish/integrate_inputlinks.py</code> <pre><code>def create_link(\n    project_name: str,\n    link_type_name: str,\n    input_id: str,\n    input_type: str,\n    output_id: str,\n    output_type: str,\n    data: dict,\n):\n    \"\"\"Create link in AYON.\n\n    TODO Replace with 'ayon_api.create_link' when AYON launcher &gt;= 1.5.2\n        is required by ayon-core.\n    \"\"\"\n    full_link_type_name = ayon_api.get_full_link_type_name(\n        link_type_name, input_type, output_type)\n\n    kwargs = {\n        \"input\": input_id,\n        \"output\": output_id,\n        \"linkType\": full_link_type_name,\n    }\n    if data:\n        kwargs[\"data\"] = data\n\n    response = ayon_api.post(\n        f\"projects/{project_name}/links\", **kwargs\n    )\n    response.raise_for_status()\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/integrate_inputlinks.html#client.ayon_core.plugins.publish.integrate_inputlinks.create_links_bulk","title":"<code>create_links_bulk(project_name, link_payloads)</code>","text":"<p>Create multiple links in AYON in a single request.</p> Source code in <code>client/ayon_core/plugins/publish/integrate_inputlinks.py</code> <pre><code>def create_links_bulk(\n    project_name: str,\n    link_payloads: list[LinkPayload],\n):\n    \"\"\"Create multiple links in AYON in a single request.\"\"\"\n    if not link_payloads:\n        return\n\n    links = []\n    for link_payload in link_payloads:\n        full_link_type_name = ayon_api.get_full_link_type_name(\n            link_payload.link_type, \"version\", \"version\"\n        )\n        link = {\n            \"input\": link_payload.input_id,\n            \"output\": link_payload.output_id,\n            \"linkType\": full_link_type_name,\n        }\n        if link_payload.data:\n            link[\"data\"] = link_payload.data\n        links.append(link)\n\n    response = ayon_api.post(\n        f\"projects/{project_name}/links/bulk\",\n        links=links\n    )\n    response.raise_for_status()\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/integrate_product_group.html","title":"integrate_product_group","text":"<p>Produces instance.data[\"productGroup\"] data used during integration.</p> Requires <p>dict -&gt; context[\"anatomyData\"] *(pyblish.api.CollectorOrder + 0.49)</p> Provides <p>instance -&gt; productGroup (str)</p>"},{"location":"autoapi/client/ayon_core/plugins/publish/integrate_product_group.html#client.ayon_core.plugins.publish.integrate_product_group.IntegrateProductGroup","title":"<code>IntegrateProductGroup</code>","text":"<p>               Bases: <code>InstancePlugin</code></p> <p>Integrate Product Group for publish.</p> Source code in <code>client/ayon_core/plugins/publish/integrate_product_group.py</code> <pre><code>class IntegrateProductGroup(pyblish.api.InstancePlugin):\n    \"\"\"Integrate Product Group for publish.\"\"\"\n\n    # Run after CollectAnatomyInstanceData\n    order = pyblish.api.IntegratorOrder - 0.1\n    label = \"Product Group\"\n\n    settings_category = \"core\"\n\n    # Attributes set by settings\n    product_grouping_profiles = None\n\n    def process(self, instance):\n        \"\"\"Look into product group profiles set by settings.\n\n        Attribute 'product_grouping_profiles' is defined by settings.\n        \"\"\"\n\n        # Skip if 'product_grouping_profiles' is empty\n        if not self.product_grouping_profiles:\n            return\n\n        if instance.data.get(\"productGroup\"):\n            # If productGroup is already set then allow that value to remain\n            self.log.debug((\n                \"Skipping collect product group due to existing value: {}\"\n            ).format(instance.data[\"productGroup\"]))\n            return\n\n        # Skip if there is no matching profile\n        filter_criteria = self.get_profile_filter_criteria(instance)\n        profile = filter_profiles(\n            self.product_grouping_profiles,\n            filter_criteria,\n            logger=self.log\n        )\n\n        if not profile:\n            return\n\n        template = profile[\"template\"]\n        product_name = instance.data[\"productName\"]\n        product_type = instance.data[\"productType\"]\n        product_base_type = instance.data.get(\"productBaseType\")\n        if not product_base_type:\n            product_base_type = product_type\n\n        anatomy_data = instance.data[\"anatomyData\"]\n\n        # Task can be optional in anatomy data\n        task = anatomy_data.get(\"task\", {})\n        if \"{task[\" not in template.lower():\n            task = task.get(\"name\")\n\n        fill_pairs = prepare_template_data({\n            \"task\": task,\n            \"host\": instance.context.data[\"hostName\"],\n            \"product\": {\n                \"name\": product_name,\n                \"type\": product_type,\n                \"basetype\": product_base_type,\n            },\n            \"renderlayer\": instance.data.get(\"renderlayer\")\n        })\n\n        filled_template = None\n        try:\n            filled_template = StringTemplate.format_strict_template(\n                template, fill_pairs\n            )\n        except (KeyError, TemplateUnsolved):\n            keys = fill_pairs.keys()\n            self.log.warning((\n                \"Product grouping failed. Only {} are expected in Settings\"\n            ).format(','.join(keys)))\n\n        if filled_template:\n            instance.data[\"productGroup\"] = filled_template\n\n    def get_profile_filter_criteria(self, instance):\n        \"\"\"Return filter criteria for `filter_profiles`\"\"\"\n        # TODO: This logic is used in much more plug-ins in one way or another\n        #       Maybe better suited for lib?\n        # Anatomy data is pre-filled by Collectors\n        anatomy_data = instance.data[\"anatomyData\"]\n\n        # Task can be optional in anatomy data\n        task = anatomy_data.get(\"task\", {})\n\n        # Return filter criteria\n        product_base_type = instance.data.get(\"productBaseType\")\n        if not product_base_type:\n            product_base_type = instance.data[\"productType\"]\n        return {\n            \"product_base_types\": product_base_type,\n            \"host_names\": instance.context.data[\"hostName\"],\n            \"task_names\": task.get(\"name\"),\n            \"task_types\": task.get(\"type\"),\n        }\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/integrate_product_group.html#client.ayon_core.plugins.publish.integrate_product_group.IntegrateProductGroup.get_profile_filter_criteria","title":"<code>get_profile_filter_criteria(instance)</code>","text":"<p>Return filter criteria for <code>filter_profiles</code></p> Source code in <code>client/ayon_core/plugins/publish/integrate_product_group.py</code> <pre><code>def get_profile_filter_criteria(self, instance):\n    \"\"\"Return filter criteria for `filter_profiles`\"\"\"\n    # TODO: This logic is used in much more plug-ins in one way or another\n    #       Maybe better suited for lib?\n    # Anatomy data is pre-filled by Collectors\n    anatomy_data = instance.data[\"anatomyData\"]\n\n    # Task can be optional in anatomy data\n    task = anatomy_data.get(\"task\", {})\n\n    # Return filter criteria\n    product_base_type = instance.data.get(\"productBaseType\")\n    if not product_base_type:\n        product_base_type = instance.data[\"productType\"]\n    return {\n        \"product_base_types\": product_base_type,\n        \"host_names\": instance.context.data[\"hostName\"],\n        \"task_names\": task.get(\"name\"),\n        \"task_types\": task.get(\"type\"),\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/integrate_product_group.html#client.ayon_core.plugins.publish.integrate_product_group.IntegrateProductGroup.process","title":"<code>process(instance)</code>","text":"<p>Look into product group profiles set by settings.</p> <p>Attribute 'product_grouping_profiles' is defined by settings.</p> Source code in <code>client/ayon_core/plugins/publish/integrate_product_group.py</code> <pre><code>def process(self, instance):\n    \"\"\"Look into product group profiles set by settings.\n\n    Attribute 'product_grouping_profiles' is defined by settings.\n    \"\"\"\n\n    # Skip if 'product_grouping_profiles' is empty\n    if not self.product_grouping_profiles:\n        return\n\n    if instance.data.get(\"productGroup\"):\n        # If productGroup is already set then allow that value to remain\n        self.log.debug((\n            \"Skipping collect product group due to existing value: {}\"\n        ).format(instance.data[\"productGroup\"]))\n        return\n\n    # Skip if there is no matching profile\n    filter_criteria = self.get_profile_filter_criteria(instance)\n    profile = filter_profiles(\n        self.product_grouping_profiles,\n        filter_criteria,\n        logger=self.log\n    )\n\n    if not profile:\n        return\n\n    template = profile[\"template\"]\n    product_name = instance.data[\"productName\"]\n    product_type = instance.data[\"productType\"]\n    product_base_type = instance.data.get(\"productBaseType\")\n    if not product_base_type:\n        product_base_type = product_type\n\n    anatomy_data = instance.data[\"anatomyData\"]\n\n    # Task can be optional in anatomy data\n    task = anatomy_data.get(\"task\", {})\n    if \"{task[\" not in template.lower():\n        task = task.get(\"name\")\n\n    fill_pairs = prepare_template_data({\n        \"task\": task,\n        \"host\": instance.context.data[\"hostName\"],\n        \"product\": {\n            \"name\": product_name,\n            \"type\": product_type,\n            \"basetype\": product_base_type,\n        },\n        \"renderlayer\": instance.data.get(\"renderlayer\")\n    })\n\n    filled_template = None\n    try:\n        filled_template = StringTemplate.format_strict_template(\n            template, fill_pairs\n        )\n    except (KeyError, TemplateUnsolved):\n        keys = fill_pairs.keys()\n        self.log.warning((\n            \"Product grouping failed. Only {} are expected in Settings\"\n        ).format(','.join(keys)))\n\n    if filled_template:\n        instance.data[\"productGroup\"] = filled_template\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/integrate_resources_path.html","title":"integrate_resources_path","text":""},{"location":"autoapi/client/ayon_core/plugins/publish/integrate_resources_path.html#client.ayon_core.plugins.publish.integrate_resources_path.IntegrateResourcesPath","title":"<code>IntegrateResourcesPath</code>","text":"<p>               Bases: <code>InstancePlugin</code></p> <p>Generate directory path where the files and resources will be stored</p> Source code in <code>client/ayon_core/plugins/publish/integrate_resources_path.py</code> <pre><code>class IntegrateResourcesPath(pyblish.api.InstancePlugin):\n    \"\"\"Generate directory path where the files and resources will be stored\"\"\"\n\n    label = \"Integrate Resources Path\"\n    order = pyblish.api.IntegratorOrder - 0.05\n    families = [\"clip\", \"projectfile\", \"plate\"]\n\n    def process(self, instance):\n        resources = instance.data.get(\"resources\") or []\n        transfers = instance.data.get(\"transfers\") or []\n\n        if not resources and not transfers:\n            self.log.debug(\n                \"Instance does not have `resources` and `transfers`\"\n            )\n            return\n\n        resources_folder = instance.data[\"resourcesDir\"]\n\n        # Define resource destination and transfers\n        for resource in resources:\n            # Add destination to the resource\n            source_filename = os.path.basename(\n                resource[\"source\"]).replace(\"\\\\\", \"/\")\n            destination = os.path.join(resources_folder, source_filename)\n\n            # Force forward slashes to fix issue with software unable\n            # to work correctly with backslashes in specific scenarios\n            # (e.g. escape characters in PLN-151 V-Ray UDIM)\n            destination = destination.replace(\"\\\\\", \"/\")\n\n            resource['destination'] = destination\n\n            # Collect transfers for the individual files of the resource\n            # e.g. all individual files of a cache or UDIM textures.\n            files = resource['files']\n            for fsrc in files:\n                fname = os.path.basename(fsrc)\n                fdest = os.path.join(\n                    resources_folder, fname\n                ).replace(\"\\\\\", \"/\")\n                transfers.append([fsrc, fdest])\n\n        instance.data[\"resources\"] = resources\n        instance.data[\"transfers\"] = transfers\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/integrate_review.html","title":"integrate_review","text":""},{"location":"autoapi/client/ayon_core/plugins/publish/integrate_thumbnail.html","title":"integrate_thumbnail","text":"<p>Integrate Thumbnails for use in Loaders.</p> <p>This thumbnail is different from 'thumbnail' representation which could be uploaded to Ftrack, or used as any other representation in Loaders to pull into a scene.</p> <p>This one is used only as image describing content of published item and     shows up only in Loader or WebUI.</p> <p>Instance must have 'published_representations' to     be able to integrate thumbnail. Possible sources of thumbnail paths: - instance.data[\"thumbnailPath\"] - representation with 'thumbnail' name in 'published_representations' - context.data[\"thumbnailPath\"]</p> Notes <p>Issue with 'thumbnail' representation is that we most likely don't     want to integrate it as representation. Integrated representation     is polluting Loader and database without real usage. That's why     they usually have 'delete' tag to skip the integration.</p>"},{"location":"autoapi/client/ayon_core/plugins/publish/integrate_thumbnail.html#client.ayon_core.plugins.publish.integrate_thumbnail.IntegrateThumbnailsAYON","title":"<code>IntegrateThumbnailsAYON</code>","text":"<p>               Bases: <code>ContextPlugin</code></p> <p>Integrate Thumbnails for use in Loaders.</p> Source code in <code>client/ayon_core/plugins/publish/integrate_thumbnail.py</code> <pre><code>class IntegrateThumbnailsAYON(pyblish.api.ContextPlugin):\n    \"\"\"Integrate Thumbnails for use in Loaders.\"\"\"\n\n    label = \"Integrate Thumbnails to AYON\"\n    order = pyblish.api.IntegratorOrder + 0.01\n\n    def process(self, context):\n        # Filter instances which can be used for integration\n        filtered_instance_items = self._prepare_instances(context)\n        if not filtered_instance_items:\n            self.log.debug(\n                \"All instances were filtered. Thumbnail integration skipped.\"\n            )\n            return\n\n        project_name = context.data[\"projectName\"]\n\n        # Collect version ids from all filtered instance\n        version_ids = {\n            instance_items.version_id\n            for instance_items in filtered_instance_items\n        }\n        # Query versions\n        version_entities = ayon_api.get_versions(\n            project_name,\n            version_ids=version_ids,\n            hero=True,\n            fields={\"id\", \"version\"}\n        )\n        # Store version by their id (converted to string)\n        version_entities_by_id = {\n            version_entity[\"id\"]: version_entity\n            for version_entity in version_entities\n        }\n        self._integrate_thumbnails(\n            filtered_instance_items,\n            version_entities_by_id,\n            project_name\n        )\n\n    def _prepare_instances(self, context):\n        context_thumbnail_path = context.data.get(\"thumbnailPath\")\n        valid_context_thumbnail = bool(\n            context_thumbnail_path\n            and os.path.exists(context_thumbnail_path)\n        )\n\n        filtered_instances = []\n        anatomy = context.data[\"anatomy\"]\n        for instance in context:\n            instance_label = self._get_instance_label(instance)\n            # Skip instances without published representations\n            # - there is no place where to put the thumbnail\n            published_repres = instance.data.get(\"published_representations\")\n            if not published_repres:\n                self.log.debug((\n                    \"There are no published representations\"\n                    \" on the instance {}.\"\n                ).format(instance_label))\n                continue\n\n            # Find thumbnail path on instance\n            thumbnail_path = (\n                instance.data.get(\"thumbnailPath\")\n                or self._get_instance_thumbnail_path(\n                    published_repres, anatomy\n                )\n            )\n            if thumbnail_path:\n                self.log.debug((\n                    \"Found thumbnail path for instance \\\"{}\\\".\"\n                    \" Thumbnail path: {}\"\n                ).format(instance_label, thumbnail_path))\n\n            elif valid_context_thumbnail:\n                # Use context thumbnail path if is available\n                thumbnail_path = context_thumbnail_path\n                self.log.debug((\n                    \"Using context thumbnail path for instance \\\"{}\\\".\"\n                    \" Thumbnail path: {}\"\n                ).format(instance_label, thumbnail_path))\n\n            # Skip instance if thumbnail path is not available for it\n            if not thumbnail_path:\n                self.log.debug((\n                    \"Skipping thumbnail integration for instance \\\"{}\\\".\"\n                    \" Instance and context\"\n                    \" thumbnail paths are not available.\"\n                ).format(instance_label))\n                continue\n\n            version_id = str(self._get_version_id(published_repres))\n            filtered_instances.append(\n                InstanceFilterResult(instance, thumbnail_path, version_id)\n            )\n        return filtered_instances\n\n    def _get_version_id(self, published_representations):\n        for repre_info in published_representations.values():\n            return repre_info[\"representation\"][\"versionId\"]\n\n    def _get_instance_thumbnail_path(\n        self, published_representations, anatomy\n    ):\n        thumb_repre_entity = None\n        for repre_info in published_representations.values():\n            repre_entity = repre_info[\"representation\"]\n            if \"thumbnail\" in repre_entity[\"name\"].lower():\n                thumb_repre_entity = repre_entity\n                break\n\n        if thumb_repre_entity is None:\n            self.log.debug(\n                \"There is no representation with name \\\"thumbnail\\\"\"\n            )\n            return None\n\n        path = thumb_repre_entity[\"attrib\"][\"path\"]\n        filled_path = anatomy.fill_root(path)\n        if not os.path.exists(filled_path):\n            self.log.warning(\n                \"Thumbnail file cannot be found. Path: {}\".format(filled_path)\n            )\n            return None\n        return os.path.normpath(filled_path)\n\n    def _create_thumbnail(self, project_name: str, src_filepath: str) -&gt; str:\n        \"\"\"Upload thumbnail to AYON and return its id.\"\"\"\n        size = os.path.getsize(src_filepath)\n        self.log.info(\n            f\"Uploading '{src_filepath}' (size: {format_file_size(size)})\"\n        )\n\n        start = time.time()\n        try:\n            thubmnail_id = ayon_api.create_thumbnail(\n                project_name, src_filepath\n            )\n            self.log.debug(f\"Uploaded in {time.time() - start}s.\")\n            return thubmnail_id\n\n        except Exception as exc:\n            last_error = exc\n            self.log.warning(\n                f\"Review upload failed after {time.time() - start}s.\",\n                exc_info=True,\n            )\n\n        # Exhausted retries - raise a user-friendly validation error with help\n        raise PublishXmlValidationError(\n            self,\n            (\n                \"Upload of thumbnail timed out or failed after multiple\"\n                \" attempts. Please try publishing again.\"\n            ),\n            formatting_data={\n                \"upload_type\": \"Thumbnail\",\n                \"file\": src_filepath,\n                \"error\": str(last_error),\n            },\n            help_filename=\"upload_file.xml\",\n        )\n\n    def _integrate_thumbnails(\n        self,\n        filtered_instance_items,\n        version_entities_by_id,\n        project_name\n    ):\n        # Make sure each entity id has defined only one thumbnail id\n        thumbnail_info_by_entity_id = {}\n        for instance_item in filtered_instance_items:\n            instance, thumbnail_path, version_id = instance_item\n            instance_label = self._get_instance_label(instance)\n            version_entity = version_entities_by_id.get(version_id)\n            if not version_entity:\n                self.log.warning((\n                    \"Version entity for instance \\\"{}\\\" was not found.\"\n                ).format(instance_label))\n                continue\n\n            thumbnail_id = self._create_thumbnail(\n                project_name, thumbnail_path\n            )\n\n            # Set thumbnail id for version\n            thumbnail_info_by_entity_id[version_id] = {\n                \"thumbnail_id\": thumbnail_id,\n                \"entity_type\": \"version\",\n            }\n            version_name = version_entity[\"version\"]\n            if version_name &lt; 0:\n                version_name = \"Hero\"\n            self.log.debug(\"Setting thumbnail for version \\\"{}\\\" &lt;{}&gt;\".format(\n                version_name, version_id\n            ))\n\n            folder_id = instance.data[\"folderEntity\"][\"id\"]\n            folder_path = instance.data[\"folderPath\"]\n            thumbnail_info_by_entity_id[folder_id] = {\n                \"thumbnail_id\": thumbnail_id,\n                \"entity_type\": \"folder\",\n            }\n            self.log.debug(\"Setting thumbnail for folder \\\"{}\\\" &lt;{}&gt;\".format(\n                folder_path, version_id\n            ))\n\n        op_session = OperationsSession()\n        for entity_id, thumbnail_info in thumbnail_info_by_entity_id.items():\n            thumbnail_id = thumbnail_info[\"thumbnail_id\"]\n            op_session.update_entity(\n                project_name,\n                thumbnail_info[\"entity_type\"],\n                entity_id,\n                {\"thumbnailId\": thumbnail_id}\n            )\n        op_session.commit()\n\n    def _get_instance_label(self, instance):\n        return (\n            instance.data.get(\"label\")\n            or instance.data.get(\"name\")\n            or \"N/A\"\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/integrate_traits.html","title":"integrate_traits","text":"<p>Integrate representations with traits.</p>"},{"location":"autoapi/client/ayon_core/plugins/publish/integrate_traits.html#client.ayon_core.plugins.publish.integrate_traits.IntegrateTraits","title":"<code>IntegrateTraits</code>","text":"<p>               Bases: <code>InstancePlugin</code></p> <p>Integrate representations with traits.</p> Source code in <code>client/ayon_core/plugins/publish/integrate_traits.py</code> <pre><code>class IntegrateTraits(pyblish.api.InstancePlugin):\n    \"\"\"Integrate representations with traits.\"\"\"\n\n    label = \"Integrate Traits of an Asset\"\n    order = pyblish.api.IntegratorOrder\n    log: \"logging.Logger\"\n\n    def process(self, instance: pyblish.api.Instance) -&gt; None:\n        \"\"\"Integrate representations with traits.\n\n        Args:\n            instance (pyblish.api.Instance): Instance to process.\n\n        \"\"\"\n        # 1) skip farm and integrate ==  False\n\n        if instance.data.get(\"integrate\", True) is False:\n            self.log.debug(f\"Instance '{instance.name}' is marked to skip \"\n                           \"integrating. Skipping\")\n            return\n\n        if instance.data.get(\"farm\"):\n            self.log.debug(\n                f\"Instance '{instance.name}' is marked to be processed on \"\n                \"farm. Skipping\")\n            return\n\n        if not has_trait_representations(instance):\n            self.log.debug(\n                f\"Instance '{instance.name}' has no representations with \"\n                \"traits. Skipping\")\n            return\n\n        # 2) filter representations based on LifeCycle traits\n        set_trait_representations(\n            instance,\n            self.filter_lifecycle(get_trait_representations(instance))\n        )\n\n        representations: list[Representation] = get_trait_representations(\n            instance\n        )\n        if not representations:\n            self.log.debug(\n                f\"Instance '{instance.name}' has no persistent \"\n                \"representations. Skipping\")\n            return\n\n        op_session = OperationsSession()\n\n        product_entity = self.prepare_product(instance, op_session)\n\n        version_entity = self.prepare_version(\n            instance, op_session, product_entity\n        )\n        instance.data[\"versionEntity\"] = version_entity\n\n        template_name = get_instance_template_name(instance)\n        anatomy = instance.context.data[\"anatomy\"]\n        template: Any = anatomy.get_template_item(\"publish\", template_name)\n\n        transfers = get_transfers_from_representations(\n            instance, template, representations)\n\n        # 8) Transfer files\n        file_transactions = FileTransaction(\n            log=self.log,\n            # Enforce unique transfers\n            allow_queue_replacements=False)\n        for transfer in transfers:\n            self.log.debug(\n                \"Transferring file: %s -&gt; %s\",\n                transfer.source,\n                transfer.destination\n            )\n            file_transactions.add(\n                transfer.source.as_posix(),\n                transfer.destination.as_posix(),\n                mode=FileTransaction.MODE_COPY,\n            )\n        file_transactions.process()\n        self.log.debug(\n            \"Transferred files %s\", [file_transactions.transferred])\n\n        # replace original paths with the destination in traits.\n        for transfer in transfers:\n            transfer.related_trait.file_path = transfer.destination\n\n        # 9) Create representation entities\n        for representation in representations:\n            attributes = {\n                \"path\": transfers[0].destination,\n                \"template\": transfers[0].template,\n            }\n\n            data = {\"context\": get_template_data_from_representation(\n                representation, instance)}\n\n            # Original integrator at this moment took all additional data\n            # on the representation and added them into either attribs or data.\n            # This should be avoided - we need to identify anything that\n            # is broken by this and move it to traits. Representation\n            # context in data is already handled by TemplateData trait, so\n            # the line above and any usage should be removed in the future.\n\n            representation_entity = new_representation_entity(\n                representation.name,\n                version_entity[\"id\"],\n                files=get_legacy_files_for_representation(\n                    transfers,\n                    representation,\n                    anatomy=instance.context.data[\"anatomy\"]),\n                attribs=attributes,\n                data=data,\n                tags=[],\n                status=\"\",\n            )\n            # replace original paths with the destination\n            # in representation entity\n            replace_paths_in_representation(representation_entity, transfers)\n\n            # add traits to representation entity\n            representation_entity[\"traits\"] = representation.traits_as_dict()\n            op_session.create_entity(\n                project_name=instance.context.data[\"projectName\"],\n                entity_type=\"representation\",\n                data=prepare_for_json(representation_entity),\n            )\n\n        # 10) Commit the session to AYON\n        self.log.debug(pformat(op_session.to_data()))\n        op_session.commit()\n\n        # 11) Pass the list of published representations to the instance\n        # for further processing in Integrate Hero versions for example.\n        instance.data[\"publishedRepresentationsWithTraits\"] = representations\n\n    @staticmethod\n    def _get_relative_to_root_original_dirname(\n            instance: pyblish.api.Instance) -&gt; str:\n        \"\"\"Get path stripped of root of the original directory name.\n\n        If `originalDirname` or `stagingDir` is set in instance data,\n        this will return it as rootless path. The path must reside\n        within the project directory.\n\n        Returns:\n            str: Relative path to the root of the project directory.\n\n        Raises:\n            PublishError: If the path is not within the project directory.\n\n        \"\"\"\n        original_directory = (\n                instance.data.get(\"originalDirname\") or\n                instance.data.get(\"stagingDir\"))\n        anatomy = instance.context.data[\"anatomy\"]\n\n        rootless = get_rootless_path(anatomy, original_directory)\n        # this check works because _rootless will be the same as\n        # original_directory if the original_directory cannot be transformed\n        # to the rootless path.\n        if rootless == original_directory:\n            msg = (\n                f\"Destination path '{original_directory}' must \"\n                \"be in project directory.\")\n            raise PublishError(msg)\n        # the root is at the beginning - {root[work]}/rest/of/the/path\n        relative_path_start = rootless.rfind(\"}\") + 2\n        return rootless[relative_path_start:]\n\n        # 8) Transfer files\n        # 9) Commit the session to AYON\n        # 10) Finalize represetations - add integrated path Trait etc.\n\n    @staticmethod\n    def filter_lifecycle(\n            representations: list[Representation]\n    ) -&gt; list[Representation]:\n        \"\"\"Filter representations based on LifeCycle traits.\n\n        Args:\n            representations (list): List of representations.\n\n        Returns:\n            list: Filtered representations.\n\n        \"\"\"\n        return [\n            representation\n            for representation in representations\n            if representation.contains_trait(Persistent)\n        ]\n\n    def prepare_product(\n            self,\n            instance: pyblish.api.Instance,\n            op_session: OperationsSession) -&gt; dict:\n        \"\"\"Prepare product for integration.\n\n        Args:\n            instance (pyblish.api.Instance): Instance to process.\n            op_session (OperationsSession): Operations session.\n\n        Returns:\n            dict: Product entity.\n\n        \"\"\"\n        project_name = instance.context.data[\"projectName\"]\n        folder_entity = instance.data[\"folderEntity\"]\n        product_name = instance.data[\"productName\"]\n        product_type = instance.data[\"productType\"]\n        product_base_type = instance.data.get(\"productBaseType\")\n        self.log.debug(\"Product: %s\", product_name)\n\n        # Get existing product if it exists\n        existing_product_entity = get_product_by_name(\n            project_name, product_name, folder_entity[\"id\"]\n        )\n\n        # Define product data\n        data = {\"families\": get_instance_families(instance)}\n        attributes = {}\n\n        product_group = instance.data.get(\"productGroup\")\n        if product_group:\n            attributes[\"productGroup\"] = product_group\n        elif existing_product_entity:\n            # Preserve previous product group if new version does not set it\n            product_group = existing_product_entity.get(\"attrib\", {}).get(\n                \"productGroup\"\n            )\n            if product_group is not None:\n                attributes[\"productGroup\"] = product_group\n\n        product_id = existing_product_entity[\"id\"] if existing_product_entity else None  # noqa: E501\n\n        new_product_entity_kwargs = {\n            \"name\": product_name,\n            \"product_type\": product_type,\n            \"folder_id\": folder_entity[\"id\"],\n            \"data\": data,\n            \"attribs\": attributes,\n            \"entity_id\": product_id,\n            \"product_base_type\": product_base_type,\n        }\n\n        if not is_product_base_type_supported():\n            new_product_entity_kwargs.pop(\"product_base_type\")\n            if (\n                    product_base_type is not None\n                    and product_base_type != product_type):\n                self.log.warning((\n                    \"Product base type %s is not supported by the server, \"\n                    \"but it's defined - and it differs from product type %s. \"\n                    \"Using product base type as product type.\"\n                ), product_base_type, product_type)\n\n                new_product_entity_kwargs[\"product_type\"] = (\n                    product_base_type\n                )\n\n        product_entity = new_product_entity(**new_product_entity_kwargs)\n\n        if existing_product_entity is None:\n            # Create a new product\n            self.log.info(\n                \"Product '%s' not found, creating ...\",\n                product_name\n            )\n            op_session.create_entity(\n                project_name, \"product\", product_entity\n            )\n\n        else:\n            # Update existing product data with new data and set in database.\n            # We also change the found product in-place so we don't need to\n            # re-query the product afterward\n            update_data = get_changed_attributes(\n                existing_product_entity, product_entity\n            )\n            op_session.update_entity(\n                project_name,\n                \"product\",\n                product_entity[\"id\"],\n                update_data\n            )\n\n        self.log.debug(\"Prepared product: %s\", product_name)\n        return product_entity\n\n    def prepare_version(\n            self,\n            instance: pyblish.api.Instance,\n            op_session: OperationsSession,\n            product_entity: dict) -&gt; dict:\n        \"\"\"Prepare version for integration.\n\n        Args:\n            instance (pyblish.api.Instance): Instance to process.\n            op_session (OperationsSession): Operations session.\n            product_entity (dict): Product entity.\n\n        Returns:\n            dict: Version entity.\n\n        \"\"\"\n        project_name = instance.context.data[\"projectName\"]\n        version_number = instance.data[\"version\"]\n        task_entity = instance.data.get(\"taskEntity\")\n        task_id = task_entity[\"id\"] if task_entity else None\n        existing_version = get_version_by_name(\n            project_name,\n            version_number,\n            product_entity[\"id\"]\n        )\n        version_id = existing_version[\"id\"] if existing_version else None\n        all_version_data = get_version_data_from_instance(instance)\n        version_data = {}\n        version_attributes = {}\n        attr_defs = self.get_attributes_for_type(instance.context, \"version\")\n        for key, value in all_version_data.items():\n            if key in attr_defs:\n                version_attributes[key] = value\n            else:\n                version_data[key] = value\n\n        version_entity = new_version_entity(\n            version_number,\n            product_entity[\"id\"],\n            task_id=task_id,\n            status=instance.data.get(\"status\"),\n            data=version_data,\n            attribs=version_attributes,\n            entity_id=version_id,\n        )\n\n        if existing_version:\n            self.log.debug(\"Updating existing version ...\")\n            update_data = get_changed_attributes(\n                existing_version, version_entity)\n            op_session.update_entity(\n                project_name,\n                \"version\",\n                version_entity[\"id\"],\n                update_data\n            )\n        else:\n            self.log.debug(\"Creating new version ...\")\n            op_session.create_entity(\n                project_name, \"version\", version_entity\n            )\n\n        self.log.debug(\n            \"Prepared version: v%s\",\n            \"{:03d}\".format(version_entity[\"version\"])\n        )\n\n        return version_entity\n\n    def get_attributes_for_type(\n            self,\n            context: pyblish.api.Context,\n            entity_type: str) -&gt; dict:\n        \"\"\"Get AYON attributes for the given entity type.\n\n        Args:\n            context (pyblish.api.Context): Context to get attributes from.\n            entity_type (str): Entity type to get attributes for.\n\n        Returns:\n            dict: AYON attributes for the given entity type.\n\n        \"\"\"\n        return self.get_attributes_by_type(context)[entity_type]\n\n    @staticmethod\n    def get_attributes_by_type(\n            context: pyblish.api.Context) -&gt; dict:\n        \"\"\"Gets AYON attributes from the given context.\n\n        Args:\n            context (pyblish.api.Context): Context to get attributes from.\n\n        Returns:\n            dict: AYON attributes.\n\n        \"\"\"\n        attributes = context.data.get(\"ayonAttributes\")\n        if attributes is None:\n            attributes = {\n                key: get_attributes_for_type(key)\n                for key in (\n                    \"project\",\n                    \"folder\",\n                    \"product\",\n                    \"version\",\n                    \"representation\",\n                )\n            }\n            context.data[\"ayonAttributes\"] = attributes\n        return attributes\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/integrate_traits.html#client.ayon_core.plugins.publish.integrate_traits.IntegrateTraits.filter_lifecycle","title":"<code>filter_lifecycle(representations)</code>  <code>staticmethod</code>","text":"<p>Filter representations based on LifeCycle traits.</p> <p>Parameters:</p> Name Type Description Default <code>representations</code> <code>list</code> <p>List of representations.</p> required <p>Returns:</p> Name Type Description <code>list</code> <code>list[Representation]</code> <p>Filtered representations.</p> Source code in <code>client/ayon_core/plugins/publish/integrate_traits.py</code> <pre><code>@staticmethod\ndef filter_lifecycle(\n        representations: list[Representation]\n) -&gt; list[Representation]:\n    \"\"\"Filter representations based on LifeCycle traits.\n\n    Args:\n        representations (list): List of representations.\n\n    Returns:\n        list: Filtered representations.\n\n    \"\"\"\n    return [\n        representation\n        for representation in representations\n        if representation.contains_trait(Persistent)\n    ]\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/integrate_traits.html#client.ayon_core.plugins.publish.integrate_traits.IntegrateTraits.get_attributes_by_type","title":"<code>get_attributes_by_type(context)</code>  <code>staticmethod</code>","text":"<p>Gets AYON attributes from the given context.</p> <p>Parameters:</p> Name Type Description Default <code>context</code> <code>Context</code> <p>Context to get attributes from.</p> required <p>Returns:</p> Name Type Description <code>dict</code> <code>dict</code> <p>AYON attributes.</p> Source code in <code>client/ayon_core/plugins/publish/integrate_traits.py</code> <pre><code>@staticmethod\ndef get_attributes_by_type(\n        context: pyblish.api.Context) -&gt; dict:\n    \"\"\"Gets AYON attributes from the given context.\n\n    Args:\n        context (pyblish.api.Context): Context to get attributes from.\n\n    Returns:\n        dict: AYON attributes.\n\n    \"\"\"\n    attributes = context.data.get(\"ayonAttributes\")\n    if attributes is None:\n        attributes = {\n            key: get_attributes_for_type(key)\n            for key in (\n                \"project\",\n                \"folder\",\n                \"product\",\n                \"version\",\n                \"representation\",\n            )\n        }\n        context.data[\"ayonAttributes\"] = attributes\n    return attributes\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/integrate_traits.html#client.ayon_core.plugins.publish.integrate_traits.IntegrateTraits.get_attributes_for_type","title":"<code>get_attributes_for_type(context, entity_type)</code>","text":"<p>Get AYON attributes for the given entity type.</p> <p>Parameters:</p> Name Type Description Default <code>context</code> <code>Context</code> <p>Context to get attributes from.</p> required <code>entity_type</code> <code>str</code> <p>Entity type to get attributes for.</p> required <p>Returns:</p> Name Type Description <code>dict</code> <code>dict</code> <p>AYON attributes for the given entity type.</p> Source code in <code>client/ayon_core/plugins/publish/integrate_traits.py</code> <pre><code>def get_attributes_for_type(\n        self,\n        context: pyblish.api.Context,\n        entity_type: str) -&gt; dict:\n    \"\"\"Get AYON attributes for the given entity type.\n\n    Args:\n        context (pyblish.api.Context): Context to get attributes from.\n        entity_type (str): Entity type to get attributes for.\n\n    Returns:\n        dict: AYON attributes for the given entity type.\n\n    \"\"\"\n    return self.get_attributes_by_type(context)[entity_type]\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/integrate_traits.html#client.ayon_core.plugins.publish.integrate_traits.IntegrateTraits.prepare_product","title":"<code>prepare_product(instance, op_session)</code>","text":"<p>Prepare product for integration.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>Instance</code> <p>Instance to process.</p> required <code>op_session</code> <code>OperationsSession</code> <p>Operations session.</p> required <p>Returns:</p> Name Type Description <code>dict</code> <code>dict</code> <p>Product entity.</p> Source code in <code>client/ayon_core/plugins/publish/integrate_traits.py</code> <pre><code>def prepare_product(\n        self,\n        instance: pyblish.api.Instance,\n        op_session: OperationsSession) -&gt; dict:\n    \"\"\"Prepare product for integration.\n\n    Args:\n        instance (pyblish.api.Instance): Instance to process.\n        op_session (OperationsSession): Operations session.\n\n    Returns:\n        dict: Product entity.\n\n    \"\"\"\n    project_name = instance.context.data[\"projectName\"]\n    folder_entity = instance.data[\"folderEntity\"]\n    product_name = instance.data[\"productName\"]\n    product_type = instance.data[\"productType\"]\n    product_base_type = instance.data.get(\"productBaseType\")\n    self.log.debug(\"Product: %s\", product_name)\n\n    # Get existing product if it exists\n    existing_product_entity = get_product_by_name(\n        project_name, product_name, folder_entity[\"id\"]\n    )\n\n    # Define product data\n    data = {\"families\": get_instance_families(instance)}\n    attributes = {}\n\n    product_group = instance.data.get(\"productGroup\")\n    if product_group:\n        attributes[\"productGroup\"] = product_group\n    elif existing_product_entity:\n        # Preserve previous product group if new version does not set it\n        product_group = existing_product_entity.get(\"attrib\", {}).get(\n            \"productGroup\"\n        )\n        if product_group is not None:\n            attributes[\"productGroup\"] = product_group\n\n    product_id = existing_product_entity[\"id\"] if existing_product_entity else None  # noqa: E501\n\n    new_product_entity_kwargs = {\n        \"name\": product_name,\n        \"product_type\": product_type,\n        \"folder_id\": folder_entity[\"id\"],\n        \"data\": data,\n        \"attribs\": attributes,\n        \"entity_id\": product_id,\n        \"product_base_type\": product_base_type,\n    }\n\n    if not is_product_base_type_supported():\n        new_product_entity_kwargs.pop(\"product_base_type\")\n        if (\n                product_base_type is not None\n                and product_base_type != product_type):\n            self.log.warning((\n                \"Product base type %s is not supported by the server, \"\n                \"but it's defined - and it differs from product type %s. \"\n                \"Using product base type as product type.\"\n            ), product_base_type, product_type)\n\n            new_product_entity_kwargs[\"product_type\"] = (\n                product_base_type\n            )\n\n    product_entity = new_product_entity(**new_product_entity_kwargs)\n\n    if existing_product_entity is None:\n        # Create a new product\n        self.log.info(\n            \"Product '%s' not found, creating ...\",\n            product_name\n        )\n        op_session.create_entity(\n            project_name, \"product\", product_entity\n        )\n\n    else:\n        # Update existing product data with new data and set in database.\n        # We also change the found product in-place so we don't need to\n        # re-query the product afterward\n        update_data = get_changed_attributes(\n            existing_product_entity, product_entity\n        )\n        op_session.update_entity(\n            project_name,\n            \"product\",\n            product_entity[\"id\"],\n            update_data\n        )\n\n    self.log.debug(\"Prepared product: %s\", product_name)\n    return product_entity\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/integrate_traits.html#client.ayon_core.plugins.publish.integrate_traits.IntegrateTraits.prepare_version","title":"<code>prepare_version(instance, op_session, product_entity)</code>","text":"<p>Prepare version for integration.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>Instance</code> <p>Instance to process.</p> required <code>op_session</code> <code>OperationsSession</code> <p>Operations session.</p> required <code>product_entity</code> <code>dict</code> <p>Product entity.</p> required <p>Returns:</p> Name Type Description <code>dict</code> <code>dict</code> <p>Version entity.</p> Source code in <code>client/ayon_core/plugins/publish/integrate_traits.py</code> <pre><code>def prepare_version(\n        self,\n        instance: pyblish.api.Instance,\n        op_session: OperationsSession,\n        product_entity: dict) -&gt; dict:\n    \"\"\"Prepare version for integration.\n\n    Args:\n        instance (pyblish.api.Instance): Instance to process.\n        op_session (OperationsSession): Operations session.\n        product_entity (dict): Product entity.\n\n    Returns:\n        dict: Version entity.\n\n    \"\"\"\n    project_name = instance.context.data[\"projectName\"]\n    version_number = instance.data[\"version\"]\n    task_entity = instance.data.get(\"taskEntity\")\n    task_id = task_entity[\"id\"] if task_entity else None\n    existing_version = get_version_by_name(\n        project_name,\n        version_number,\n        product_entity[\"id\"]\n    )\n    version_id = existing_version[\"id\"] if existing_version else None\n    all_version_data = get_version_data_from_instance(instance)\n    version_data = {}\n    version_attributes = {}\n    attr_defs = self.get_attributes_for_type(instance.context, \"version\")\n    for key, value in all_version_data.items():\n        if key in attr_defs:\n            version_attributes[key] = value\n        else:\n            version_data[key] = value\n\n    version_entity = new_version_entity(\n        version_number,\n        product_entity[\"id\"],\n        task_id=task_id,\n        status=instance.data.get(\"status\"),\n        data=version_data,\n        attribs=version_attributes,\n        entity_id=version_id,\n    )\n\n    if existing_version:\n        self.log.debug(\"Updating existing version ...\")\n        update_data = get_changed_attributes(\n            existing_version, version_entity)\n        op_session.update_entity(\n            project_name,\n            \"version\",\n            version_entity[\"id\"],\n            update_data\n        )\n    else:\n        self.log.debug(\"Creating new version ...\")\n        op_session.create_entity(\n            project_name, \"version\", version_entity\n        )\n\n    self.log.debug(\n        \"Prepared version: v%s\",\n        \"{:03d}\".format(version_entity[\"version\"])\n    )\n\n    return version_entity\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/integrate_traits.html#client.ayon_core.plugins.publish.integrate_traits.IntegrateTraits.process","title":"<code>process(instance)</code>","text":"<p>Integrate representations with traits.</p> <p>Parameters:</p> Name Type Description Default <code>instance</code> <code>Instance</code> <p>Instance to process.</p> required Source code in <code>client/ayon_core/plugins/publish/integrate_traits.py</code> <pre><code>def process(self, instance: pyblish.api.Instance) -&gt; None:\n    \"\"\"Integrate representations with traits.\n\n    Args:\n        instance (pyblish.api.Instance): Instance to process.\n\n    \"\"\"\n    # 1) skip farm and integrate ==  False\n\n    if instance.data.get(\"integrate\", True) is False:\n        self.log.debug(f\"Instance '{instance.name}' is marked to skip \"\n                       \"integrating. Skipping\")\n        return\n\n    if instance.data.get(\"farm\"):\n        self.log.debug(\n            f\"Instance '{instance.name}' is marked to be processed on \"\n            \"farm. Skipping\")\n        return\n\n    if not has_trait_representations(instance):\n        self.log.debug(\n            f\"Instance '{instance.name}' has no representations with \"\n            \"traits. Skipping\")\n        return\n\n    # 2) filter representations based on LifeCycle traits\n    set_trait_representations(\n        instance,\n        self.filter_lifecycle(get_trait_representations(instance))\n    )\n\n    representations: list[Representation] = get_trait_representations(\n        instance\n    )\n    if not representations:\n        self.log.debug(\n            f\"Instance '{instance.name}' has no persistent \"\n            \"representations. Skipping\")\n        return\n\n    op_session = OperationsSession()\n\n    product_entity = self.prepare_product(instance, op_session)\n\n    version_entity = self.prepare_version(\n        instance, op_session, product_entity\n    )\n    instance.data[\"versionEntity\"] = version_entity\n\n    template_name = get_instance_template_name(instance)\n    anatomy = instance.context.data[\"anatomy\"]\n    template: Any = anatomy.get_template_item(\"publish\", template_name)\n\n    transfers = get_transfers_from_representations(\n        instance, template, representations)\n\n    # 8) Transfer files\n    file_transactions = FileTransaction(\n        log=self.log,\n        # Enforce unique transfers\n        allow_queue_replacements=False)\n    for transfer in transfers:\n        self.log.debug(\n            \"Transferring file: %s -&gt; %s\",\n            transfer.source,\n            transfer.destination\n        )\n        file_transactions.add(\n            transfer.source.as_posix(),\n            transfer.destination.as_posix(),\n            mode=FileTransaction.MODE_COPY,\n        )\n    file_transactions.process()\n    self.log.debug(\n        \"Transferred files %s\", [file_transactions.transferred])\n\n    # replace original paths with the destination in traits.\n    for transfer in transfers:\n        transfer.related_trait.file_path = transfer.destination\n\n    # 9) Create representation entities\n    for representation in representations:\n        attributes = {\n            \"path\": transfers[0].destination,\n            \"template\": transfers[0].template,\n        }\n\n        data = {\"context\": get_template_data_from_representation(\n            representation, instance)}\n\n        # Original integrator at this moment took all additional data\n        # on the representation and added them into either attribs or data.\n        # This should be avoided - we need to identify anything that\n        # is broken by this and move it to traits. Representation\n        # context in data is already handled by TemplateData trait, so\n        # the line above and any usage should be removed in the future.\n\n        representation_entity = new_representation_entity(\n            representation.name,\n            version_entity[\"id\"],\n            files=get_legacy_files_for_representation(\n                transfers,\n                representation,\n                anatomy=instance.context.data[\"anatomy\"]),\n            attribs=attributes,\n            data=data,\n            tags=[],\n            status=\"\",\n        )\n        # replace original paths with the destination\n        # in representation entity\n        replace_paths_in_representation(representation_entity, transfers)\n\n        # add traits to representation entity\n        representation_entity[\"traits\"] = representation.traits_as_dict()\n        op_session.create_entity(\n            project_name=instance.context.data[\"projectName\"],\n            entity_type=\"representation\",\n            data=prepare_for_json(representation_entity),\n        )\n\n    # 10) Commit the session to AYON\n    self.log.debug(pformat(op_session.to_data()))\n    op_session.commit()\n\n    # 11) Pass the list of published representations to the instance\n    # for further processing in Integrate Hero versions for example.\n    instance.data[\"publishedRepresentationsWithTraits\"] = representations\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/integrate_traits.html#client.ayon_core.plugins.publish.integrate_traits.RepresentationEntity","title":"<code>RepresentationEntity</code>","text":"<p>Representation entity data.</p> Source code in <code>client/ayon_core/plugins/publish/integrate_traits.py</code> <pre><code>class RepresentationEntity:\n    \"\"\"Representation entity data.\"\"\"\n    id: str\n    versionId: str  # noqa: N815\n    name: str\n    files: dict[str, Any]\n    attrib: dict[str, Any]\n    data: str\n    tags: list[str]\n    status: str\n\n    def __init__(self,\n        id: str,\n        versionId: str,  # noqa: N815\n        name: str,\n        files: dict[str, Any],\n        attrib: dict[str, Any],\n        data: str,\n        tags: list[str],\n        status: str):\n        \"\"\"Initialize RepresentationEntity.\n\n        Args:\n            id (str): Entity ID.\n            versionId (str): Version ID.\n            name (str): Representation name.\n            files (dict[str, Any]): Files in the representation.\n            attrib (dict[str, Any]): Attributes of the representation.\n            data (str): Data of the representation.\n            tags (list[str]): Tags of the representation.\n            status (str): Status of the representation.\n\n        \"\"\"\n        self.id = id\n        self.versionId = versionId\n        self.name = name\n        self.files = files\n        self.attrib = attrib\n        self.data = data\n        self.tags = tags\n        self.status = status\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/integrate_traits.html#client.ayon_core.plugins.publish.integrate_traits.RepresentationEntity.__init__","title":"<code>__init__(id, versionId, name, files, attrib, data, tags, status)</code>","text":"<p>Initialize RepresentationEntity.</p> <p>Parameters:</p> Name Type Description Default <code>id</code> <code>str</code> <p>Entity ID.</p> required <code>versionId</code> <code>str</code> <p>Version ID.</p> required <code>name</code> <code>str</code> <p>Representation name.</p> required <code>files</code> <code>dict[str, Any]</code> <p>Files in the representation.</p> required <code>attrib</code> <code>dict[str, Any]</code> <p>Attributes of the representation.</p> required <code>data</code> <code>str</code> <p>Data of the representation.</p> required <code>tags</code> <code>list[str]</code> <p>Tags of the representation.</p> required <code>status</code> <code>str</code> <p>Status of the representation.</p> required Source code in <code>client/ayon_core/plugins/publish/integrate_traits.py</code> <pre><code>def __init__(self,\n    id: str,\n    versionId: str,  # noqa: N815\n    name: str,\n    files: dict[str, Any],\n    attrib: dict[str, Any],\n    data: str,\n    tags: list[str],\n    status: str):\n    \"\"\"Initialize RepresentationEntity.\n\n    Args:\n        id (str): Entity ID.\n        versionId (str): Version ID.\n        name (str): Representation name.\n        files (dict[str, Any]): Files in the representation.\n        attrib (dict[str, Any]): Attributes of the representation.\n        data (str): Data of the representation.\n        tags (list[str]): Tags of the representation.\n        status (str): Status of the representation.\n\n    \"\"\"\n    self.id = id\n    self.versionId = versionId\n    self.name = name\n    self.files = files\n    self.attrib = attrib\n    self.data = data\n    self.tags = tags\n    self.status = status\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/integrate_traits.html#client.ayon_core.plugins.publish.integrate_traits.get_changed_attributes","title":"<code>get_changed_attributes(old_entity, new_entity)</code>","text":"<p>Prepare changes for entity update.</p> Todo <p>Move to the library.</p> <p>Parameters:</p> Name Type Description Default <code>old_entity</code> <code>dict[str, Any]</code> <p>Existing entity.</p> required <code>new_entity</code> <code>dict[str, Any]</code> <p>New entity.</p> required <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: Changes that have new entity.</p> Source code in <code>client/ayon_core/plugins/publish/integrate_traits.py</code> <pre><code>def get_changed_attributes(\n        old_entity: dict, new_entity: dict) -&gt; dict[str, Any]:\n    \"\"\"Prepare changes for entity update.\n\n    Todo:\n        Move to the library.\n\n    Args:\n        old_entity (dict[str, Any]): Existing entity.\n        new_entity (dict[str, Any]): New entity.\n\n    Returns:\n        dict[str, Any]: Changes that have new entity.\n\n    \"\"\"\n    changes = {}\n    for key in set(new_entity.keys()):\n        if key == \"attrib\":\n            continue\n\n        if key in new_entity and new_entity[key] != old_entity.get(key):\n            changes[key] = new_entity[key]\n            continue\n\n    attrib_changes = {}\n    if \"attrib\" in new_entity:\n        attrib_changes = {\n            key: value\n            for key, value in new_entity[\"attrib\"].items()\n            if value != old_entity[\"attrib\"].get(key)\n        }\n    if attrib_changes:\n        changes[\"attrib\"] = attrib_changes\n    return changes\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/integrate_traits.html#client.ayon_core.plugins.publish.integrate_traits.prepare_for_json","title":"<code>prepare_for_json(data)</code>","text":"<p>Prepare data for JSON serialization.</p> <p>If there are values that json cannot serialize, this function will convert them to strings.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict[str, Any]</code> <p>Data to prepare.</p> required <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: Prepared data.</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>If the data cannot be converted to JSON.</p> Source code in <code>client/ayon_core/plugins/publish/integrate_traits.py</code> <pre><code>def prepare_for_json(data: dict[str, Any]) -&gt; dict[str, Any]:\n    \"\"\"Prepare data for JSON serialization.\n\n    If there are values that json cannot serialize, this function will\n    convert them to strings.\n\n    Args:\n        data (dict[str, Any]): Data to prepare.\n\n    Returns:\n        dict[str, Any]: Prepared data.\n\n    Raises:\n        TypeError: If the data cannot be converted to JSON.\n\n    \"\"\"\n    prepared = {}\n    for key, value in data.items():\n        if isinstance(value, dict):\n            value = prepare_for_json(value)\n        try:\n            json.dumps(value)\n        except TypeError:\n            value = value.as_posix() if issubclass(\n                value.__class__, Path) else str(value)\n        prepared[key] = value\n    return prepared\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/integrate_version_attrs.html","title":"integrate_version_attrs","text":""},{"location":"autoapi/client/ayon_core/plugins/publish/integrate_version_attrs.html#client.ayon_core.plugins.publish.integrate_version_attrs.IntegrateVersionAttributes","title":"<code>IntegrateVersionAttributes</code>","text":"<p>               Bases: <code>ContextPlugin</code></p> <p>Integrate version attributes from predefined key.</p> <p>Any integration after 'IntegrateAsset' can fill 'versionAttributes' with attribute key &amp; value to be updated on created version.</p> <p>The integration must make sure the attribute is available for the version entity otherwise an error would be raised.</p> <p>Example of 'versionAttributes':     {         \"ftrack_id\": \"0123456789-101112-131415\",         \"syncsketch_id\": \"987654321-012345-678910\"     }</p> Source code in <code>client/ayon_core/plugins/publish/integrate_version_attrs.py</code> <pre><code>class IntegrateVersionAttributes(pyblish.api.ContextPlugin):\n    \"\"\"Integrate version attributes from predefined key.\n\n    Any integration after 'IntegrateAsset' can fill 'versionAttributes' with\n    attribute key &amp; value to be updated on created version.\n\n    The integration must make sure the attribute is available for the version\n    entity otherwise an error would be raised.\n\n    Example of 'versionAttributes':\n        {\n            \"ftrack_id\": \"0123456789-101112-131415\",\n            \"syncsketch_id\": \"987654321-012345-678910\"\n        }\n    \"\"\"\n\n    label = \"Integrate Version Attributes\"\n    order = pyblish.api.IntegratorOrder + 0.5\n\n    def process(self, context):\n        available_attributes = ayon_api.get_attributes_for_type(\"version\")\n        skipped_attributes = set()\n        project_name = context.data[\"projectName\"]\n        op_session = OperationsSession()\n        for instance in context:\n            label = self.get_instance_label(instance)\n            version_entity = instance.data.get(\"versionEntity\")\n            if not version_entity:\n                continue\n\n            current_attributes = version_entity[\"attrib\"]\n            attributes = instance.data.get(\"versionAttributes\")\n            if not attributes:\n                self.log.debug((\n                    \"Skipping instance {} because it does not specify\"\n                    \" version attributes to set.\"\n                ).format(label))\n                continue\n\n            filtered_attributes = {}\n            for attr, value in attributes.items():\n                if attr not in available_attributes:\n                    skipped_attributes.add(attr)\n                elif current_attributes.get(attr) != value:\n                    filtered_attributes[attr] = value\n\n            if not filtered_attributes:\n                self.log.debug((\n                    \"Skipping instance {} because all version attributes were\"\n                    \" filtered out.\"\n                ).format(label))\n                continue\n\n            self.log.debug(\"Updating attributes on version {} to {}\".format(\n                version_entity[\"id\"], str(filtered_attributes)\n            ))\n            op_session.update_entity(\n                project_name,\n                \"version\",\n                version_entity[\"id\"],\n                {\"attrib\": filtered_attributes}\n            )\n\n        if skipped_attributes:\n            self.log.warning((\n                \"Skipped version attributes integration because they're\"\n                \" not available on the server: {}\"\n            ).format(str(skipped_attributes)))\n\n        if len(op_session):\n            op_session.commit()\n            self.log.info(\"Updated version attributes\")\n        else:\n            self.log.debug(\"There are no version attributes to update\")\n\n    @staticmethod\n    def get_instance_label(instance):\n        return (\n            instance.data.get(\"label\")\n            or instance.data.get(\"name\")\n            or instance.data.get(\"productName\")\n            or str(instance)\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/integrate_versions_to_list.html","title":"integrate_versions_to_list","text":"<p>Pyblish context plugin that adds published versions to AYON entity lists.</p> <p>For each instance with <code>versionLists</code> data, creates or updates the named lists (optionally typed as <code>review-session</code>) and organizes them under the requested folder hierarchy on the server.</p>"},{"location":"autoapi/client/ayon_core/plugins/publish/integrate_versions_to_list.html#client.ayon_core.plugins.publish.integrate_versions_to_list.IntegrateVersionToList","title":"<code>IntegrateVersionToList</code>","text":"<p>               Bases: <code>ContextPlugin</code></p> <p>Integrate published versions to a list.</p> <p>This plugin integrates published versions to a list in the server.</p> <p>Note that list names are unique; as such, even if multiple list configs are set to go to different folders but with the same list name - then all versions will be integrated to the same list regardless of what list folder it is in.</p> Source code in <code>client/ayon_core/plugins/publish/integrate_versions_to_list.py</code> <pre><code>class IntegrateVersionToList(pyblish.api.ContextPlugin):\n    \"\"\"Integrate published versions to a list.\n\n    This plugin integrates published versions to a list in the server.\n\n    Note that list names are unique; as such, even if multiple list configs\n    are set to go to different folders but with the same list name - then all\n    versions will be integrated to the same list regardless of what list folder\n    it is in.\n    \"\"\"\n\n    label = \"Integrate Versions to List\"\n    order = pyblish.api.IntegratorOrder + 0.49\n    settings_category = \"core\"\n\n    def process(self, context):\n        list_config_by_list_label: dict[str, ListConfig] = {}\n        version_ids_by_list_label: dict[str, list[str]] = defaultdict(list)\n\n        anatomy: Anatomy = context.data[\"anatomy\"]\n        for instance in context:\n            version_lists: list[ListConfig] = instance.data.get(\n                \"versionLists\", []\n            )\n            if not version_lists:\n                continue\n\n            version_entity = instance.data.get(\"versionEntity\")\n            if not version_entity:\n                continue\n\n            has_webreview = instance.data.get(\"hasWebreview\", False)\n            for list_config in version_lists:\n                # Skip review-session lists if instance does not have review\n                if (\n                    not has_webreview\n                    and list_config.list_type == \"review-session\"\n                ):\n                    continue\n                # Construct the list config with the formatted name and parent\n                # folder names\n                template_data = deepcopy(instance.data[\"anatomyData\"])\n                template_data.update({\n                    \"root\": anatomy.roots,\n                    \"platform\": platform.system().lower(),\n                })\n                try:\n                    list_label: str = str(\n                        StringTemplate.format_strict_template(\n                            list_config.label, template_data\n                        )\n                    )\n                except Exception:\n                    self.log.error(\n                        \"Failed to fill entity list name template: \"\n                        f\"{list_config.label}\",\n                        exc_info=True,\n                    )\n                    continue\n\n                existing_config = list_config_by_list_label.get(list_label)\n                if (\n                    existing_config\n                    and existing_config.list_type != list_config.list_type\n                ):\n                    self.log.error(\n                        \"Conflicting list_type for entity list label \"\n                        f\"'{list_label}': '{existing_config.list_type}' vs \"\n                        f\"'{list_config.list_type}'. Skipping.\"\n                    )\n                    continue\n\n                # Add version to lists mapping\n                version_ids_by_list_label[list_label].append(\n                    version_entity[\"id\"]\n                )\n\n                # Fill label using template data\n                candidate_config = deepcopy(list_config)\n                candidate_config.label = list_label\n                for folder in list_config.list_folders:\n                    folder.label = str(StringTemplate.format_template(\n                        folder.label, template_data\n                    ))\n\n                # Use candadate config\n                if existing_config is None:\n                    list_config_by_list_label[list_label] = candidate_config\n                    continue\n\n                # Compare folders of the existing config and the candidate\n                #   config. Does not affect output but logs a warning if\n                #   they differ.\n                existing_labels = [\n                    folder.label\n                    for folder in existing_config.list_folders\n                ]\n                candidate_labels = [\n                    folder.label\n                    for folder in candidate_config.list_folders\n                ]\n                if existing_labels != candidate_labels:\n                    self.log.warning(\n                        \"Configuration does contain different folders from\"\n                        f\" existing list '{list_label}'. Keeping existing list\"\n                        f\" folders. Existing: {existing_labels}\"\n                        f\" vs Candidate: {candidate_labels}\"\n                    )\n\n        if not list_config_by_list_label:\n            return\n\n        # Get all list entities for the project from server\n        project_name: str = context.data[\"projectName\"]\n        existing_list_entities_by_label: dict[str, dict] = {\n            entity_list[\"label\"]: entity_list\n            for entity_list in ayon_api.get_entity_lists(project_name)\n        }\n        existing_list_folders = get_entity_list_folders(project_name)\n\n        for list_config in list_config_by_list_label.values():\n            version_ids = version_ids_by_list_label[list_config.label]\n            if not version_ids:\n                self.log.debug(f\"No version ids for list: {list_config.label}\")\n                continue\n\n            # If list exists, append to it but ensure it is of correct type\n            existing_list = existing_list_entities_by_label.get(\n                list_config.label\n            )\n            if existing_list:\n                if existing_list[\"entityListType\"] != list_config.list_type:\n                    entity_list_type = existing_list[\"entityListType\"]\n                    self.log.error(\n                        \"Can't add versions to list because another entity\"\n                        f\" list type '{entity_list_type}' with that label\"\n                        f\" already exists: {list_config.label}\"\n                    )\n                    continue\n\n                if existing_list[\"entityType\"] != \"version\":\n                    entity_type = existing_list[\"entityType\"]\n                    self.log.error(\n                        f\"Can't add versions to list because a '{entity_type}'\"\n                        \" list type with that label already exists: \"\n                        f\"{list_config.label}\"\n                    )\n                    continue\n\n                self._append_to_entity_list(\n                    project_name=project_name,\n                    list_entity=existing_list,\n                    version_ids=version_ids,\n                )\n\n            # Else create the new list\n            else:\n                # make sure parent folder exists\n                entity_list_folder_id = self._create_list_folders(\n                    project_name=project_name,\n                    list_config=list_config,\n                    existing_list_folders=existing_list_folders,\n                )\n                # then create list under a parent folder\n                self._create_entity_list(\n                    project_name=project_name,\n                    entity_type=\"version\",\n                    label=list_config.label,\n                    list_type=list_config.list_type,\n                    entity_list_folder_id=entity_list_folder_id,\n                    items=[\n                        {\"entityId\": version_id}\n                        for version_id in version_ids\n                    ],\n                )\n\n    def _create_list_folders(\n        self,\n        project_name: str,\n        list_config: ListConfig,\n        existing_list_folders: list[dict],\n    ) -&gt; str | None:\n        \"\"\"Returns folder id of the last folder in the hierarchy.\"\"\"\n        if not list_config.list_folders:\n            return None\n\n        parent_folder_id = None\n        for list_folder in list_config.list_folders:\n            # make sure to get existing folder id if it exists\n            # and use it instead of creating a new one\n            for existing_list_folder in existing_list_folders:\n                if (\n                    existing_list_folder[\"parentId\"] == parent_folder_id\n                    and existing_list_folder[\"label\"] == list_folder.label\n                ):\n                    # If folder exists, check if scope needs to be updated\n                    #   with the list type.\n                    # NOTE If scope is None or empty list it is scoped for\n                    #   everything. So the list type is added to scope only\n                    #   if scope exists and does not contain the list type.\n                    scope = existing_list_folder[\"data\"].get(\"scope\")\n                    if scope and list_config.list_type not in scope:\n                        scope.append(list_config.list_type)\n                        # --- Temporary bypass ---\n                        # TODO remove when fixed in frontend\n                        # There is an issue with list folder having both\n                        #   'generic' and 'review-session' in scope. The\n                        #   folder is not visible in Lists UI.\n                        scope = []\n                        existing_list_folder[\"data\"][\"scope\"] = scope\n                        # ------------------------\n                        update_entity_list_folder(\n                            project_name,\n                            existing_list_folder[\"id\"],\n                            data={\"scope\": scope},\n                        )\n                    parent_folder_id = existing_list_folder[\"id\"]\n                    break\n            else:\n                # if folder does not exist, create it under the parent folder\n                new_folder_id = create_entity_list_folder(\n                    project_name=project_name,\n                    list_folder=list_folder,\n                    parent_folder_id=parent_folder_id,\n                )\n                # Add new fake folder entity to existing folders\n                existing_list_folders.append({\n                    \"label\": list_folder.label,\n                    \"parentId\": parent_folder_id,\n                    \"id\": new_folder_id,\n                    \"data\": {\n                        \"scope\": list(list_folder.scope),\n                    },\n                })\n                parent_folder_id = new_folder_id\n        return parent_folder_id\n\n    def _append_to_entity_list(\n        self,\n        project_name: str,\n        list_entity: dict,\n        version_ids: list[str],\n    ) -&gt; None:\n        \"\"\"Append version ids to the entity list as items.\"\"\"\n        for entity_id in version_ids:\n            item_id = create_entity_list_item(\n                project_name=project_name,\n                list_id=list_entity[\"id\"],\n                entity_id=entity_id,\n            )\n            self.log.debug(\n                f\"Created entity list item {item_id} for version {entity_id}\"\n                f\" in list {list_entity['label']}\"\n            )\n\n    def _create_entity_list(\n        self,\n        project_name: str,\n        entity_type: str,\n        label: str,\n        items: list[dict],\n        list_type: str,\n        entity_list_folder_id: str | None,\n    ) -&gt; dict:\n        \"\"\"Create a new list parented to the entity list folder.\"\"\"\n        kwargs = {\n            \"id\": create_entity_id(),\n            \"entityType\": entity_type,\n            \"label\": label,\n            \"items\": items,\n            \"entityListType\": list_type,\n        }\n        for key, value in (\n            (\"entityListFolderId\", entity_list_folder_id),\n        ):\n            if value is not None:\n                kwargs[key] = value\n\n        self.log.debug(f\"Creating entity list: {kwargs}\")\n        response = ayon_api.post(\n            f\"projects/{project_name}/lists\",\n            **kwargs\n        )\n        response.raise_for_status()\n        self.log.debug(f\"Entity list created: {response.data}\")\n        return response.data\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/integrate_versions_to_list.html#client.ayon_core.plugins.publish.integrate_versions_to_list.create_entity_list_item","title":"<code>create_entity_list_item(project_name, list_id, entity_id)</code>","text":"<p>Create entity list item.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name where entity list lives.</p> required <code>list_id</code> <code>str</code> <p>Entity list id where item will be added.</p> required <code>entity_id</code> <code>str</code> <p>Id of entity added to the list.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Item id.</p> Source code in <code>client/ayon_core/plugins/publish/integrate_versions_to_list.py</code> <pre><code>def create_entity_list_item(\n    project_name: str,\n    list_id: str,\n    entity_id: str,\n) -&gt; str:\n    \"\"\"Create entity list item.\n\n    Args:\n        project_name (str): Project name where entity list lives.\n        list_id (str): Entity list id where item will be added.\n        entity_id (str): Id of entity added to the list.\n\n    Returns:\n        str: Item id.\n\n    \"\"\"\n    item_id = create_entity_id()\n    kwargs = {\n        \"id\": item_id,\n        \"entityId\": entity_id,\n    }\n\n    response = ayon_api.post(\n        f\"projects/{project_name}/lists/{list_id}/items\",\n        **kwargs\n    )\n    response.raise_for_status()\n    return item_id\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/preintegrate_thumbnail_representation.html","title":"preintegrate_thumbnail_representation","text":"<p>Marks thumbnail representation for integrate to DB or not.</p> <p>Some hosts produce thumbnail representation, most of them do not create them explicitly, but they created during extract phase.</p> <p>In some cases it might be useful to override implicit setting for host/task</p> <p>This plugin needs to run after extract phase, but before integrate.py as thumbnail is part of review family and integrated there.</p> <p>It should be better to control integration of thumbnail in one place than configure it in multiple places on host implementations.</p>"},{"location":"autoapi/client/ayon_core/plugins/publish/preintegrate_thumbnail_representation.html#client.ayon_core.plugins.publish.preintegrate_thumbnail_representation.PreIntegrateThumbnails","title":"<code>PreIntegrateThumbnails</code>","text":"<p>               Bases: <code>InstancePlugin</code></p> <p>Marks thumbnail representation for integrate to DB or not.</p> Source code in <code>client/ayon_core/plugins/publish/preintegrate_thumbnail_representation.py</code> <pre><code>class PreIntegrateThumbnails(pyblish.api.InstancePlugin):\n    \"\"\"Marks thumbnail representation for integrate to DB or not.\"\"\"\n\n    label = \"Override Integrate Thumbnail Representations\"\n    order = pyblish.api.IntegratorOrder - 0.1\n\n    settings_category = \"core\"\n\n    integrate_profiles = []\n\n    def process(self, instance):\n        repres = instance.data.get(\"representations\")\n        if not repres:\n            return\n\n        thumbnail_repres = []\n        for repre in repres:\n            if \"thumbnail\" in repre.get(\"tags\", []):\n                thumbnail_repres.append(repre)\n\n        if not thumbnail_repres:\n            return\n\n        product_base_type = instance.data.get(\"productBaseType\")\n        if not product_base_type:\n            product_base_type = instance.data[\"productType\"]\n        product_name = instance.data[\"productName\"]\n        host_name = instance.context.data[\"hostName\"]\n\n        anatomy_data = instance.data[\"anatomyData\"]\n        task = anatomy_data.get(\"task\", {})\n\n        found_profile = filter_profiles(\n            self.integrate_profiles,\n            {\n                \"host_names\": host_name,\n                \"task_names\": task.get(\"name\"),\n                \"task_types\": task.get(\"type\"),\n                \"product_base_types\": product_base_type,\n                \"product_names\": product_name,\n            },\n            logger=self.log\n        )\n\n        if not found_profile:\n            return\n\n        for thumbnail_repre in thumbnail_repres:\n            thumbnail_repre.setdefault(\"tags\", [])\n\n            if not found_profile[\"integrate_thumbnail\"]:\n                if \"delete\" not in thumbnail_repre[\"tags\"]:\n                    thumbnail_repre[\"tags\"].append(\"delete\")\n            else:\n                if \"delete\" in thumbnail_repre[\"tags\"]:\n                    thumbnail_repre[\"tags\"].remove(\"delete\")\n\n            self.log.debug(\n                \"Thumbnail repre tags {}\".format(thumbnail_repre[\"tags\"]))\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/repair_unicode_strings.html","title":"repair_unicode_strings","text":""},{"location":"autoapi/client/ayon_core/plugins/publish/repair_unicode_strings.html#client.ayon_core.plugins.publish.repair_unicode_strings.RepairUnicodeStrings","title":"<code>RepairUnicodeStrings</code>","text":"<p>               Bases: <code>Collector</code></p> <p>Validate all environment variables are string type.</p> Source code in <code>client/ayon_core/plugins/publish/repair_unicode_strings.py</code> <pre><code>class RepairUnicodeStrings(pyblish.api.Collector):\n    \"\"\"Validate all environment variables are string type.\n\n    \"\"\"\n\n    order = pyblish.api.CollectorOrder\n    label = 'Unicode Strings'\n\n    def process(self, context):\n        for key, value in os.environ.items():\n            os.environ[str(key)] = str(value)\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/validate_asset_docs.html","title":"validate_asset_docs","text":""},{"location":"autoapi/client/ayon_core/plugins/publish/validate_asset_docs.html#client.ayon_core.plugins.publish.validate_asset_docs.ValidateFolderEntities","title":"<code>ValidateFolderEntities</code>","text":"<p>               Bases: <code>InstancePlugin</code></p> <p>Validate existence of folder entity on instances.</p> <p>Without folder entity it is not possible to publish the instance.</p> <p>If context has set folder entity the validation is skipped.</p> <p>Plugin was added because there are cases when context folder is not defined e.g. in tray publisher.</p> Source code in <code>client/ayon_core/plugins/publish/validate_asset_docs.py</code> <pre><code>class ValidateFolderEntities(pyblish.api.InstancePlugin):\n    \"\"\"Validate existence of folder entity on instances.\n\n    Without folder entity it is not possible to publish the instance.\n\n    If context has set folder entity the validation is skipped.\n\n    Plugin was added because there are cases when context folder is not\n    defined e.g. in tray publisher.\n    \"\"\"\n\n    label = \"Validate Folder entities\"\n    order = pyblish.api.ValidatorOrder\n\n    def process(self, instance):\n        context_folder_entity = instance.context.data.get(\"folderEntity\")\n        if context_folder_entity:\n            return\n\n        if instance.data.get(\"folderEntity\"):\n            self.log.debug(\"Instance has set fodler entity in its data.\")\n\n        elif (\n            instance.data.get(\"newHierarchyIntegration\")\n            # Backwards compatible (Deprecated since 24/06/06)\n            or instance.data.get(\"newAssetPublishing\")\n        ):\n            # skip if it is editorial\n            self.log.debug(\"Editorial instance has no need to check...\")\n\n        else:\n            raise PublishValidationError((\n                \"Instance \\\"{}\\\" doesn't have folder entity \"\n                \"set which is needed for publishing.\"\n            ).format(instance.data[\"name\"]))\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/validate_containers.html","title":"validate_containers","text":""},{"location":"autoapi/client/ayon_core/plugins/publish/validate_containers.html#client.ayon_core.plugins.publish.validate_containers.ValidateOutdatedContainers","title":"<code>ValidateOutdatedContainers</code>","text":"<p>               Bases: <code>OptionalPyblishPluginMixin</code>, <code>ContextPlugin</code></p> <p>Containers are must be updated to latest version on publish.</p> Source code in <code>client/ayon_core/plugins/publish/validate_containers.py</code> <pre><code>class ValidateOutdatedContainers(\n    OptionalPyblishPluginMixin,\n    pyblish.api.ContextPlugin\n):\n    \"\"\"Containers are must be updated to latest version on publish.\"\"\"\n\n    label = \"Validate Outdated Containers\"\n    order = pyblish.api.ValidatorOrder\n    settings_category = \"core\"\n\n    optional = True\n    actions = [ShowInventory]\n\n    @classmethod\n    def apply_settings(cls, settings):\n        # Disable plugin if host does not inherit from 'ILoadHost'\n        # - not a host that can load containers\n        host = registered_host()\n        if not isinstance(host, ILoadHost):\n            cls.enabled = False\n            return\n\n        # Disable if no profile is found for the current host\n        profiles = (\n            settings\n            [\"core\"]\n            [\"publish\"]\n            [\"ValidateOutdatedContainers\"]\n            [\"plugin_state_profiles\"]\n        )\n        profile = filter_profiles(\n            profiles, {\"host_names\": get_current_host_name()}\n        )\n        if not profile:\n            cls.enabled = False\n            return\n\n        # Apply settings from profile\n        for attr_name in {\n            \"enabled\",\n            \"optional\",\n            \"active\",\n        }:\n            setattr(cls, attr_name, profile[attr_name])\n\n    def process(self, context):\n        if not self.is_active(context.data):\n            return\n\n        if any_outdated_containers():\n            msg = \"There are outdated containers in the scene.\"\n            raise PublishXmlValidationError(self, msg)\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/validate_file_saved.html","title":"validate_file_saved","text":""},{"location":"autoapi/client/ayon_core/plugins/publish/validate_file_saved.html#client.ayon_core.plugins.publish.validate_file_saved.SaveByVersionUpAction","title":"<code>SaveByVersionUpAction</code>","text":"<p>               Bases: <code>Action</code></p> <p>Save Workfile.</p> Source code in <code>client/ayon_core/plugins/publish/validate_file_saved.py</code> <pre><code>class SaveByVersionUpAction(pyblish.api.Action):\n    \"\"\"Save Workfile.\"\"\"\n    label = \"Save Workfile\"\n    on = \"failed\"\n    icon = \"save\"\n\n    def process(self, context, plugin):\n        save_next_version()\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/validate_file_saved.html#client.ayon_core.plugins.publish.validate_file_saved.ShowWorkfilesAction","title":"<code>ShowWorkfilesAction</code>","text":"<p>               Bases: <code>Action</code></p> <p>Save Workfile.</p> Source code in <code>client/ayon_core/plugins/publish/validate_file_saved.py</code> <pre><code>class ShowWorkfilesAction(pyblish.api.Action):\n    \"\"\"Save Workfile.\"\"\"\n    label = \"Show Workfiles Tool...\"\n    on = \"failed\"\n    icon = \"files-o\"\n\n    def process(self, context, plugin):\n        show_workfiles()\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/validate_file_saved.html#client.ayon_core.plugins.publish.validate_file_saved.ValidateCurrentSaveFile","title":"<code>ValidateCurrentSaveFile</code>","text":"<p>               Bases: <code>ContextPlugin</code></p> <p>File must be saved before publishing</p> <p>This does not validate for unsaved changes. It only validates whether the current context was able to identify any 'currentFile'.</p> Source code in <code>client/ayon_core/plugins/publish/validate_file_saved.py</code> <pre><code>class ValidateCurrentSaveFile(pyblish.api.ContextPlugin):\n    \"\"\"File must be saved before publishing\n\n    This does not validate for unsaved changes. It only validates whether\n    the current context was able to identify any 'currentFile'.\n    \"\"\"\n\n    label = \"Validate File Saved\"\n    order = pyblish.api.ValidatorOrder - 0.1\n    actions = [SaveByVersionUpAction, ShowWorkfilesAction]\n\n    def process(self, context):\n        host = registered_host()\n        if not isinstance(host, IWorkfileHost):\n            self.log.debug(\n                \"Skipping file save validation because host does \"\n                \"not implement workfiles.\"\n            )\n            return\n\n        current_file = context.data[\"currentFile\"]\n        if not current_file:\n            raise PublishValidationError(\n                \"Workfile is not saved. Please save your scene to continue.\",\n                title=\"File not saved\",\n                description=self.get_description())\n\n    def get_description(self):\n        return inspect.cleandoc(\"\"\"\n            ### File not saved\n\n            Your workfile must be saved to continue publishing.\n\n            The **Save Workfile** action will save it for you with the first\n            available workfile version number in your current context.\n        \"\"\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/validate_filesequences.html","title":"validate_filesequences","text":""},{"location":"autoapi/client/ayon_core/plugins/publish/validate_filesequences.html#client.ayon_core.plugins.publish.validate_filesequences.ValidateFileSequences","title":"<code>ValidateFileSequences</code>","text":"<p>               Bases: <code>ContextPlugin</code></p> <p>Validates whether any file sequences were collected.</p> Source code in <code>client/ayon_core/plugins/publish/validate_filesequences.py</code> <pre><code>class ValidateFileSequences(pyblish.api.ContextPlugin):\n    \"\"\"Validates whether any file sequences were collected.\"\"\"\n\n    order = pyblish.api.ValidatorOrder\n    # Keep \"filesequence\" for backwards compatibility of older jobs\n    targets = [\"filesequence\", \"farm\"]\n    label = \"Validate File Sequences\"\n\n    def process(self, context):\n        if not context:\n            raise PublishValidationError(\"Nothing collected.\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/validate_intent.html","title":"validate_intent","text":""},{"location":"autoapi/client/ayon_core/plugins/publish/validate_intent.html#client.ayon_core.plugins.publish.validate_intent.ValidateIntent","title":"<code>ValidateIntent</code>","text":"<p>               Bases: <code>ContextPlugin</code></p> <p>Validate intent of the publish.</p> <p>It is required to fill the intent of this publish. Chech the log for more details</p> Source code in <code>client/ayon_core/plugins/publish/validate_intent.py</code> <pre><code>class ValidateIntent(pyblish.api.ContextPlugin):\n    \"\"\"Validate intent of the publish.\n\n    It is required to fill the intent of this publish. Chech the log\n    for more details\n    \"\"\"\n\n    order = pyblish.api.ValidatorOrder\n\n    label = \"Validate Intent\"\n    settings_category = \"core\"\n\n    enabled = False\n\n    # Can be modified by settings\n    profiles = [{\n        \"host_names\": [],\n        \"task_types\": [],\n        \"task_names\": [],\n        \"validate\": False,\n    }]\n\n    def process(self, context):\n        # Skip if there are no profiles\n        validate = True\n        if self.profiles:\n            # Collect data from context\n            task_name = context.data.get(\"task\")\n            task_type = context.data.get(\"taskType\")\n            host_name = context.data.get(\"hostName\")\n\n            filter_data = {\n                \"host_names\": host_name,\n                \"task_types\": task_type,\n                \"task_names\": task_name,\n            }\n            matching_profile = filter_profiles(\n                self.profiles, filter_data, logger=self.log\n            )\n            if matching_profile:\n                validate = matching_profile[\"validate\"]\n\n        if not validate:\n            self.log.debug((\n                \"Validation of intent was skipped.\"\n                \" Matching profile for current context disabled validation.\"\n            ))\n            return\n\n        intent = context.data.get(\"intent\") or {}\n        self.log.debug(str(intent))\n        intent_value = intent.get(\"value\")\n        if not intent_value:\n            raise PublishValidationError(\n                \"Please make sure that you select the intent of this publish.\"\n            )\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/validate_publish_dir.html","title":"validate_publish_dir","text":""},{"location":"autoapi/client/ayon_core/plugins/publish/validate_publish_dir.html#client.ayon_core.plugins.publish.validate_publish_dir.ValidatePublishDir","title":"<code>ValidatePublishDir</code>","text":"<p>               Bases: <code>InstancePlugin</code></p> <p>Validates if files are being published into a project directory</p> <p>In specific cases ('source' template - in place publishing) source folder of published items is used as a regular <code>publish</code> dir. This validates if it is inside any project dir for the project. (eg. files are not published from local folder, inaccessible for studio')</p> Source code in <code>client/ayon_core/plugins/publish/validate_publish_dir.py</code> <pre><code>class ValidatePublishDir(pyblish.api.InstancePlugin):\n    \"\"\"Validates if files are being published into a project directory\n\n    In specific cases ('source' template - in place publishing) source folder\n    of published items is used as a regular `publish` dir.\n    This validates if it is inside any project dir for the project.\n    (eg. files are not published from local folder, inaccessible for studio')\n\n    \"\"\"\n\n    order = ValidateContentsOrder\n    label = \"Validate publish dir\"\n\n    checked_template_names = [\"source\"]\n    # validate instances might have interim family, needs to be mapped to final\n    product_base_type_mapping = {\n        \"renderLayer\": \"render\",\n        \"renderLocal\": \"render\"\n    }\n\n    def process(self, instance):\n\n        template_name = self._get_template_name_from_instance(instance)\n\n        if template_name not in self.checked_template_names:\n            return\n\n        original_dirname = instance.data.get(\"originalDirname\")\n        if not original_dirname:\n            raise PublishXmlValidationError(\n                self,\n                \"Instance meant for in place publishing.\"\n                \" Its 'originalDirname' must be collected.\"\n                \" Contact AYON developer to modify collector.\"\n            )\n\n        anatomy = instance.context.data[\"anatomy\"]\n\n        # original_dirname must be convertable to rootless path\n        # in other case it is path inside of root folder for the project\n        success, _ = anatomy.find_root_template_from_path(original_dirname)\n        if not success:\n            raise PublishXmlValidationError(\n                plugin=self,\n                message=(\n                    \"Path '{}' not in project folder. Please publish from \"\n                    \"inside of project folder.\".format(original_dirname)\n                ),\n                key=\"not_in_dir\",\n                formatting_data={\"original_dirname\": original_dirname}\n            )\n\n    def _get_template_name_from_instance(self, instance):\n        \"\"\"Find template which will be used during integration.\"\"\"\n        project_name = instance.context.data[\"projectName\"]\n        host_name = instance.context.data[\"hostName\"]\n        product_base_type = instance.data.get(\"productBaseType\")\n        if not product_base_type:\n            product_base_type = instance.data[\"productType\"]\n        mapped_product_base_type = (\n            self.product_base_type_mapping.get(product_base_type)\n            or product_base_type\n        )\n        anatomy_data = instance.data[\"anatomyData\"]\n        task_info = anatomy_data.get(\"task\") or {}\n\n        return get_publish_template_name(\n            project_name,\n            host_name,\n            product_base_type=mapped_product_base_type,\n            task_name=task_info.get(\"name\"),\n            task_type=task_info.get(\"type\"),\n            project_settings=instance.context.data[\"project_settings\"],\n            logger=self.log\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/validate_resources.html","title":"validate_resources","text":""},{"location":"autoapi/client/ayon_core/plugins/publish/validate_resources.html#client.ayon_core.plugins.publish.validate_resources.ValidateResources","title":"<code>ValidateResources</code>","text":"<p>               Bases: <code>InstancePlugin</code></p> <p>Validates mapped resources.</p> <p>These are external files to the current application, for example these could be textures, image planes, cache files or other linked media.</p> <p>A single resource entry MUST contain <code>source</code> and <code>files</code>:     {         \"source\": \"/path/to/file..exr\",         \"files\": ['/path/to/file.1001.exr', '/path/to/file.1002.exr']     } <p>It may contain additional metadata like <code>attribute</code> or <code>node</code> so other publishing plug-ins can detect where the resource was used. The <code>color_space</code> data is also frequently used (e.g. in Maya and Houdini)</p> This validates <ul> <li>The resources are existing files.</li> <li>The resources have correctly collected the data.</li> <li>The resources must be unique to the source filepath so that multiple   source filepaths do not write to the same publish filepath.</li> </ul> Source code in <code>client/ayon_core/plugins/publish/validate_resources.py</code> <pre><code>class ValidateResources(pyblish.api.InstancePlugin):\n    \"\"\"Validates mapped resources.\n\n    These are external files to the current application, for example\n    these could be textures, image planes, cache files or other linked\n    media.\n\n    A single resource entry MUST contain `source` and `files`:\n        {\n            \"source\": \"/path/to/file.&lt;UDIM&gt;.exr\",\n            \"files\": ['/path/to/file.1001.exr', '/path/to/file.1002.exr']\n        }\n\n    It may contain additional metadata like `attribute` or `node` so other\n    publishing plug-ins can detect where the resource was used. The\n    `color_space` data is also frequently used (e.g. in Maya and Houdini)\n\n    This validates:\n        - The resources are existing files.\n        - The resources have correctly collected the data.\n        - The resources must be unique to the source filepath so that multiple\n          source filepaths do not write to the same publish filepath.\n\n    \"\"\"\n\n    order = ValidateContentsOrder\n    label = \"Resources\"\n\n    def process(self, instance):\n\n        resources = instance.data.get(\"resources\", [])\n        if not resources:\n            self.log.debug(\"No resources to validate..\")\n            return\n\n        # Validate the `resources` data structure is valid\n        invalid_data = False\n        for resource in resources:\n            # Required data\n            if \"source\" not in resource:\n                invalid_data = True\n                self.log.error(\"Missing 'source' in resource: %s\", resource)\n            if \"files\" not in resource or not resource[\"files\"]:\n                invalid_data = True\n                self.log.error(\"Missing 'files' in resource: %s\", resource)\n            if not all(os.path.exists(f) for f in resource.get(\"files\", [])):\n                invalid_data = True\n                self.log.error(\n                    \"Resource contains files that do not exist \"\n                    \"on disk: %s\", resource\n                )\n\n        # Ensure unique resource names\n        basenames = defaultdict(set)\n        for resource in resources:\n            files = resource.get(\"files\", [])\n            for filename in files:\n\n                # Use normalized paths in comparison and ignore case\n                # sensitivity\n                filename = os.path.normpath(filename).lower()\n\n                basename = os.path.splitext(os.path.basename(filename))[0]\n                basenames[basename].add(filename)\n\n        invalid_resources = list()\n        for basename, sources in basenames.items():\n            if len(sources) &gt; 1:\n                invalid_resources.extend(sources)\n                self.log.error(\n                    \"Non-unique resource filename: {0}\\n- {1}\".format(\n                        basename,\n                        \"\\n- \".join(sources)\n                    )\n                )\n\n        if invalid_data or invalid_resources:\n            raise PublishValidationError(\n                \"Invalid resources in instance.\",\n                description=self.get_description()\n            )\n\n    def get_description(self):\n        return inspect.cleandoc(\n            \"\"\"### Invalid resources\n\n            Used resources, like textures, must exist on disk and must have\n            unique filenames.\n\n            #### Filenames must be unique\n\n            In most cases this will invalidate  due to using the same filenames\n            from different folders, and as such the file to be transferred is\n            unique but has the same filename. Either rename the source files or\n            make sure to use the same source file if they are intended to\n            be the same file.\n\n            \"\"\"\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/validate_unique_subsets.html","title":"validate_unique_subsets","text":""},{"location":"autoapi/client/ayon_core/plugins/publish/validate_unique_subsets.html#client.ayon_core.plugins.publish.validate_unique_subsets.ValidateProductUniqueness","title":"<code>ValidateProductUniqueness</code>","text":"<p>               Bases: <code>ContextPlugin</code></p> <p>Validate all product names are unique.</p> <p>This only validates whether the instances currently set to publish from the workfile overlap one another for the folder + product they are publishing to.</p> <p>This does not perform any check against existing publishes in the database since it is allowed to publish into existing products resulting in versioning.</p> <p>A product may appear twice to publish from the workfile if one of them is set to publish to another folder than the other.</p> Source code in <code>client/ayon_core/plugins/publish/validate_unique_subsets.py</code> <pre><code>class ValidateProductUniqueness(pyblish.api.ContextPlugin):\n    \"\"\"Validate all product names are unique.\n\n    This only validates whether the instances currently set to publish from\n    the workfile overlap one another for the folder + product they are\n    publishing to.\n\n    This does not perform any check against existing publishes in the database\n    since it is allowed to publish into existing products resulting in\n    versioning.\n\n    A product may appear twice to publish from the workfile if one\n    of them is set to publish to another folder than the other.\n\n    \"\"\"\n\n    label = \"Validate Product Uniqueness\"\n    order = pyblish.api.ValidatorOrder\n    families = [\"*\"]\n\n    def process(self, context):\n\n        # Find instance per (folder,product)\n        instance_per_folder_product = defaultdict(list)\n        for instance in context:\n\n            # Ignore disabled instances\n            if not instance.data.get(\"publish\", True):\n                continue\n\n            # Ignore instances not marked to integrate\n            if not instance.data.get(\"integrate\", True):\n                continue\n\n            # Ignore instance without folder data\n            folder_path = instance.data.get(\"folderPath\")\n            if folder_path is None:\n                self.log.warning(\n                    \"Instance found without `folderPath` data: \"\n                    f\"{instance.name}\"\n                )\n                continue\n\n            # Ignore instance without product data\n            product_name = instance.data.get(\"productName\")\n            if product_name is None:\n                self.log.warning(\n                    \"Instance found without `productName` in data: \"\n                    f\"{instance.name}\"\n                )\n                continue\n\n            version = instance.data.get(\"version\")\n            key = (\n                folder_path,\n                product_name,\n                version\n            )\n            instance_per_folder_product[key].append(instance)\n\n        non_unique = []\n        for (folder_path, product_name, version), instances in (\n            instance_per_folder_product.items()\n        ):\n            label = f\"{folder_path} &gt; {product_name}\"\n\n            # If this has an explicit version, but there is also a non-explicit\n            # version instance then disallow it.\n            versionless_key = (folder_path, product_name, None)\n            if (\n                version is not None\n                and versionless_key in instance_per_folder_product\n            ):\n                non_unique.append(label)\n                self.log.error(\n                    f\"Instance with explicit version {version} found for \"\n                    f\"product {label}, but there is also an instance without \"\n                    f\"explicit version. This is not allowed.\"\n                )\n\n            # A single instance per folder, product is fine\n            if len(instances) &lt; 2:\n                continue\n\n            if version is not None:\n                label += f\" (version {version})\"\n            non_unique.append(label)\n\n        if not non_unique:\n            # All is ok\n            return\n\n        msg = (\n            f\"Instance products {non_unique} are not unique.\"\n            \" Please remove or rename duplicates.\"\n        )\n        formatting_data = {\n            \"non_unique\": \",\".join(non_unique)\n        }\n\n        if non_unique:\n            raise PublishXmlValidationError(self, msg,\n                                            formatting_data=formatting_data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/plugins/publish/validate_version.html","title":"validate_version","text":""},{"location":"autoapi/client/ayon_core/plugins/publish/validate_version.html#client.ayon_core.plugins.publish.validate_version.ValidateVersion","title":"<code>ValidateVersion</code>","text":"<p>               Bases: <code>InstancePlugin</code>, <code>OptionalPyblishPluginMixin</code></p> <p>Validate instance version.</p> <p>AYON does not allow overwriting previously published versions.</p> Source code in <code>client/ayon_core/plugins/publish/validate_version.py</code> <pre><code>class ValidateVersion(pyblish.api.InstancePlugin, OptionalPyblishPluginMixin):\n    \"\"\"Validate instance version.\n\n    AYON does not allow overwriting previously published versions.\n    \"\"\"\n\n    order = pyblish.api.ValidatorOrder\n\n    label = \"Validate Version\"\n    settings_category = \"core\"\n\n    optional = False\n    active = True\n\n    @classmethod\n    def apply_settings(cls, settings):\n        # Disable if no profile is found for the current host\n        profiles = (\n            settings\n            [\"core\"]\n            [\"publish\"]\n            [\"ValidateVersion\"]\n            [\"plugin_state_profiles\"]\n        )\n        profile = filter_profiles(\n            profiles, {\"host_names\": get_current_host_name()}\n        )\n        if not profile:\n            cls.enabled = False\n            return\n\n        # Apply settings from profile\n        for attr_name in {\n            \"enabled\",\n            \"optional\",\n            \"active\",\n        }:\n            setattr(cls, attr_name, profile[attr_name])\n\n    def process(self, instance):\n        if not self.is_active(instance.data):\n            return\n\n        version = instance.data.get(\"version\")\n        latest_version = instance.data.get(\"latestVersion\")\n\n        if latest_version is not None and int(version) &lt;= int(latest_version):\n            # TODO: Remove full non-html version upon drop of old publisher\n            msg = (\n                \"Version '{0}' from instance '{1}' that you are \"\n                \"trying to publish is lower or equal to an existing version \"\n                \"in the database. Version in database: '{2}'.\"\n                \"Please version up your workfile to a higher version number \"\n                \"than: '{2}'.\"\n            ).format(version, instance.data[\"name\"], latest_version)\n\n            msg_html = (\n                \"Version &lt;b&gt;{0}&lt;/b&gt; from instance &lt;b&gt;{1}&lt;/b&gt; that you are \"\n                \"trying to publish is lower or equal to an existing version \"\n                \"in the database. Version in database: &lt;b&gt;{2}&lt;/b&gt;.&lt;br&gt;&lt;br&gt;\"\n                \"Please version up your workfile to a higher version number \"\n                \"than: &lt;b&gt;{2}&lt;/b&gt;.\"\n            ).format(version, instance.data[\"name\"], latest_version)\n            raise PublishValidationError(\n                title=\"Higher version of publish already exists\",\n                message=msg,\n                description=msg_html\n            )\n</code></pre>"},{"location":"autoapi/client/ayon_core/resources/index.html","title":"resources","text":""},{"location":"autoapi/client/ayon_core/resources/index.html#client.ayon_core.resources.get_image_path","title":"<code>get_image_path(*args)</code>","text":"<p>Helper function to get images.</p> <p>Parameters:</p> Name Type Description Default <code>*&lt;str&gt;</code> <p>Filepath part items.</p> required Source code in <code>client/ayon_core/resources/__init__.py</code> <pre><code>def get_image_path(*args):\n    \"\"\"Helper function to get images.\n\n    Args:\n        *&lt;str&gt;: Filepath part items.\n    \"\"\"\n    return get_resource(\"images\", *args)\n</code></pre>"},{"location":"autoapi/client/ayon_core/resources/index.html#client.ayon_core.resources.get_resource","title":"<code>get_resource(*args)</code>","text":"<p>Serves to simple resources access</p> <p>:param args: should contain subfolder names and filename of               resource from resources folder :type args: list</p> Source code in <code>client/ayon_core/resources/__init__.py</code> <pre><code>def get_resource(*args):\n    \"\"\" Serves to simple resources access\n\n    :param *args: should contain *subfolder* names and *filename* of\n                  resource from resources folder\n    :type *args: list\n    \"\"\"\n    return os.path.normpath(os.path.join(RESOURCES_DIR, *args))\n</code></pre>"},{"location":"autoapi/client/ayon_core/scripts/index.html","title":"scripts","text":""},{"location":"autoapi/client/ayon_core/scripts/ocio_wrapper.html","title":"ocio_wrapper","text":"<p>OpenColorIO Wrapper.</p> <p>Receive OpenColorIO information and store it in JSON format for processed that don't have access to OpenColorIO or their version of OpenColorIO is not compatible.</p>"},{"location":"autoapi/client/ayon_core/scripts/otio_burnin.html","title":"otio_burnin","text":""},{"location":"autoapi/client/ayon_core/scripts/otio_burnin.html#client.ayon_core.scripts.otio_burnin.ModifiedBurnins","title":"<code>ModifiedBurnins</code>","text":"<p>               Bases: <code>Burnins</code></p> <p>This is modification of OTIO FFmpeg Burnin adapter. - requires FFmpeg in PATH</p> <p>Offers 6 positions for burnin text. Each can be set with: - static text - frames - timecode</p> <p>Options - dictionary which sets the final look. - Datatypes explanation:  string format must be supported by FFmpeg.     Examples: \"#000000\", \"0x000000\", \"black\"  must be accesible by ffmpeg = name of registered Font in system     or path to font file.     Examples: \"Arial\", \"C:/Windows/Fonts/arial.ttf\" <ul> <li>Possible keys: \"opacity\" - Opacity of text -  \"bg_opacity\" - Opacity of background (box around text) -  \"bg_color\" - Background color -  \"bg_padding\" - Background padding in pixels -  \"x_offset\" - offsets burnin vertically by entered pixels     from border -  \"y_offset\" - offsets burnin horizontally by entered pixels     from border -  <li>x_offset &amp; y_offset should be set at least to same value as bg_padding!! \"font\" - Font Family for text -  \"font_size\" - Font size in pixels -  \"font_color\" - Color of text -  \"frame_offset\" - Default start frame - <ul> <li>required IF start frame is not set when using frames   or timecode burnins</li> </ul> <p>On initializing class can be set General options through     \"options_init\" arg. General options can be overridden when adding burnin.</p> Source code in <code>client/ayon_core/scripts/otio_burnin.py</code> <pre><code>class ModifiedBurnins(ffmpeg_burnins.Burnins):\n    '''\n    This is modification of OTIO FFmpeg Burnin adapter.\n    - requires FFmpeg in PATH\n\n    Offers 6 positions for burnin text. Each can be set with:\n    - static text\n    - frames\n    - timecode\n\n    Options - dictionary which sets the final look.\n    - Datatypes explanation:\n    &lt;color&gt; string format must be supported by FFmpeg.\n        Examples: \"#000000\", \"0x000000\", \"black\"\n    &lt;font&gt; must be accesible by ffmpeg = name of registered Font in system\n        or path to font file.\n        Examples: \"Arial\", \"C:/Windows/Fonts/arial.ttf\"\n\n    - Possible keys:\n    \"opacity\" - Opacity of text - &lt;float, Range:0-1&gt;\n    \"bg_opacity\" - Opacity of background (box around text) - &lt;float, Range:0-1&gt;\n    \"bg_color\" - Background color - &lt;color&gt;\n    \"bg_padding\" - Background padding in pixels - &lt;int&gt;\n    \"x_offset\" - offsets burnin vertically by entered pixels\n        from border - &lt;int&gt;\n    \"y_offset\" - offsets burnin horizontally by entered pixels\n        from border - &lt;int&gt;\n    - x_offset &amp; y_offset should be set at least to same value as bg_padding!!\n    \"font\" - Font Family for text - &lt;font&gt;\n    \"font_size\" - Font size in pixels - &lt;int&gt;\n    \"font_color\" - Color of text - &lt;color&gt;\n    \"frame_offset\" - Default start frame - &lt;int&gt;\n        - required IF start frame is not set when using frames\n          or timecode burnins\n\n    On initializing class can be set General options through\n        \"options_init\" arg.\n    General options can be overridden when adding burnin.\n\n    '''\n    TOP_CENTERED = ffmpeg_burnins.TOP_CENTERED\n    BOTTOM_CENTERED = ffmpeg_burnins.BOTTOM_CENTERED\n    TOP_LEFT = ffmpeg_burnins.TOP_LEFT\n    BOTTOM_LEFT = ffmpeg_burnins.BOTTOM_LEFT\n    TOP_RIGHT = ffmpeg_burnins.TOP_RIGHT\n    BOTTOM_RIGHT = ffmpeg_burnins.BOTTOM_RIGHT\n\n    options_init = {\n        'opacity': 1,\n        'x_offset': 5,\n        'y_offset': 5,\n        'bg_padding': 5,\n        'bg_opacity': 0.5,\n        'font_size': 42\n    }\n\n    def __init__(\n        self, source, ffprobe_data=None, options_init=None, first_frame=None\n    ):\n        if not ffprobe_data:\n            ffprobe_data = _get_ffprobe_data(source)\n\n        # Validate 'streams' before calling super to raise more specific\n        #   error\n        source_streams = ffprobe_data.get(\"streams\")\n        if not source_streams:\n            raise ValueError((\n                \"Input file \\\"{}\\\" does not contain any streams\"\n                \" with image/video content.\"\n            ).format(source))\n\n        self.ffprobe_data = ffprobe_data\n        self.first_frame = first_frame\n        self.input_args = []\n        self.cleanup_paths = []\n\n        super().__init__(source, source_streams)\n\n        if options_init:\n            self.options_init.update(options_init)\n\n    def add_text(\n        self,\n        text,\n        align,\n        frame_start=None,\n        frame_end=None,\n        options=None,\n    ):\n        \"\"\"\n        Adding static text to a filter.\n\n        :param str text: text to apply to the drawtext\n        :param enum align: alignment, must use provided enum flags\n        :param int frame_start: starting frame for burnins current frame\n        :param dict options: recommended to use TextOptions\n        \"\"\"\n        if not text:\n            # do not add empty text, eg.: if it was using an optional part\n            return\n\n        if not options:\n            options = ffmpeg_burnins.TextOptions(**self.options_init)\n\n        options = options.copy()\n        if frame_start is not None:\n            options[\"frame_offset\"] = frame_start\n\n        # `frame_end` is only for meassurements of text position\n        if frame_end is not None:\n            options[\"frame_end\"] = frame_end\n\n        options[\"label\"] = align\n        self._add_burnin(text, align, options, DRAWTEXT)\n\n    def add_timecode(\n        self, align, frame_start=None, frame_end=None, frame_start_tc=None,\n        text=None, options=None\n    ):\n        \"\"\"\n        Convenience method to create the frame number expression.\n\n        :param enum align: alignment, must use provided enum flags\n        :param int frame_start:  starting frame for burnins current frame\n        :param int frame_start_tc:  starting frame for burnins timecode\n        :param str text: text that will be before timecode\n        :param dict options: recommended to use TimeCodeOptions\n        \"\"\"\n        if not options:\n            options = ffmpeg_burnins.TimeCodeOptions(**self.options_init)\n\n        options = options.copy()\n        if frame_start is not None:\n            options[\"frame_offset\"] = frame_start\n\n        # `frame_end` is only for meassurements of text position\n        if frame_end is not None:\n            options[\"frame_end\"] = frame_end\n\n        if not frame_start_tc:\n            frame_start_tc = options[\"frame_offset\"]\n\n        if not text:\n            text = \"\"\n\n        if not options.get(\"fps\"):\n            options[\"fps\"] = self.frame_rate\n\n        if isinstance(frame_start_tc, str):\n            options[\"timecode\"] = frame_start_tc\n        else:\n            options[\"timecode\"] = ffmpeg_burnins._frames_to_timecode(\n                frame_start_tc,\n                self.frame_rate\n            )\n\n        self._add_burnin(text, align, options, TIMECODE)\n\n    def add_per_frame_text(\n        self,\n        text,\n        align,\n        frame_start,\n        frame_end,\n        listed_keys,\n        options=None\n    ):\n        \"\"\"Add text that changes per frame.\n\n        Args:\n            text (str): Template string with unfilled keys that are changed\n                per frame.\n            align (str): Alignment of text.\n            frame_start (int): Starting frame for burnins current frame.\n            frame_end (int): Ending frame for burnins current frame.\n            listed_keys (list): List of keys that are changed per frame.\n            options (Optional[dict]): Options to affect style of burnin.\n        \"\"\"\n\n        if not options:\n            options = ffmpeg_burnins.TimeCodeOptions(**self.options_init)\n\n        options = options.copy()\n        if frame_start is None:\n            frame_start = options[\"frame_offset\"]\n\n        # `frame_end` is only for meassurements of text position\n        if frame_end is None:\n            frame_end = options[\"frame_end\"]\n\n        fps = options.get(\"fps\")\n        if not fps:\n            fps = self.frame_rate\n\n        text_for_size = text\n        if CURRENT_FRAME_SPLITTER in text:\n            expr = self._get_current_frame_expression(frame_start, frame_end)\n            if expr is None:\n                expr = MISSING_KEY_VALUE\n                text_for_size = text_for_size.replace(\n                    CURRENT_FRAME_SPLITTER, MISSING_KEY_VALUE)\n            text = text.replace(CURRENT_FRAME_SPLITTER, expr)\n\n        # Find longest list with values\n        longest_list_len = max(\n            len(item[\"values\"]) for item in listed_keys.values()\n        )\n        # Where to store formatted values per frame by key\n        new_listed_keys = [{} for _ in range(longest_list_len)]\n        # Find the longest value per fill key.\n        #   The longest value is used to determine size of burnin box.\n        longest_value_by_key = {}\n        for key, item in listed_keys.items():\n            values = item[\"values\"]\n            # Fill the missing values from the longest list with the last\n            #   value to make sure all values have same \"frame count\"\n            last_value = values[-1] if values else \"\"\n            for _ in range(longest_list_len - len(values)):\n                values.append(last_value)\n\n            # Prepare dictionary structure for nestes values\n            # - last key is overriden on each frame loop\n            item_keys = list(item[\"keys\"])\n            fill_data = {}\n            sub_value = fill_data\n            last_item_key = item_keys.pop(-1)\n            for item_key in item_keys:\n                sub_value[item_key] = {}\n                sub_value = sub_value[item_key]\n\n            # Fill value per frame\n            key_max_len = 0\n            key_max_value = \"\"\n            for value, new_values in zip(values, new_listed_keys):\n                sub_value[last_item_key] = value\n                try:\n                    value = key.format(**sub_value)\n                except (TypeError, KeyError, ValueError):\n                    value = MISSING_KEY_VALUE\n                new_values[key] = value\n\n                value_len = len(value)\n                if value_len &gt; key_max_len:\n                    key_max_value = value\n                    key_max_len = value_len\n\n            # Store the longest value\n            longest_value_by_key[key] = key_max_value\n\n        # Make sure the longest value of each key is replaced for text size\n        #   calculation\n        for key, value in longest_value_by_key.items():\n            text_for_size = text_for_size.replace(key, value)\n\n        # Create temp file with instructions for each frame of text\n        lines = []\n        for frame, value in enumerate(new_listed_keys):\n            seconds = float(frame) / fps\n            # Escape special character\n            new_text = text\n            for _key, _value in value.items():\n                _value = str(_value)\n                new_text = new_text.replace(_key, str(_value))\n\n            new_text = (\n                str(new_text)\n                .replace(\"\\\\\", \"\\\\\\\\\")\n                .replace(\",\", \"\\\\,\")\n                .replace(\":\", \"\\\\:\")\n            )\n            lines.append(\n                f\"{seconds} drawtext@{align} reinit text='{new_text}';\")\n\n        with tempfile.NamedTemporaryFile(\n            prefix=\"ayon_burnin_\", mode=\"w\", delete=False,\n        ) as temp:\n            path = temp.name\n            temp.write(\"\\n\".join(lines))\n\n        self.cleanup_paths.append(path)\n        self.filters[\"drawtext\"].append(\"sendcmd=f='{}'\".format(\n            path.replace(\"\\\\\", \"/\").replace(\":\", \"\\\\:\")\n        ))\n        self.add_text(text_for_size, align, frame_start, frame_end, options)\n\n    def _get_current_frame_expression(self, frame_start, frame_end):\n        if frame_start is None:\n            return None\n        return (\n            \"%{eif:n+\" + str(frame_start)\n            + \":d:\" + str(len(str(frame_end))) + \"}\"\n        )\n\n    def _add_burnin(self, text, align, options, draw):\n        \"\"\"\n        Generic method for building the filter flags.\n        :param str text: text to apply to the drawtext\n        :param enum align: alignment, must use provided enum flags\n        :param dict options:\n        \"\"\"\n\n        final_text = text\n        text_for_size = text\n        if CURRENT_FRAME_SPLITTER in text:\n            frame_start = options[\"frame_offset\"]\n            frame_end = options.get(\"frame_end\", frame_start)\n            expr = self._get_current_frame_expression(frame_start, frame_end)\n            if expr is not None:\n                max_length = len(str(frame_end))\n                # Use number '8' length times for replacement\n                size_replacement = max_length * \"8\"\n            else:\n                expr = size_replacement = MISSING_KEY_VALUE\n\n            final_text = final_text.replace(\n                CURRENT_FRAME_SPLITTER, expr\n            )\n            text_for_size = text_for_size.replace(\n                CURRENT_FRAME_SPLITTER, size_replacement\n            )\n\n        timecode_text = options.get(\"timecode\") or \"\"\n        text_for_size += timecode_text\n\n        font_path = options.get(\"font\")\n        if not font_path or not os.path.exists(font_path):\n            options[\"font\"] = ffmpeg_burnins.FONT\n\n        drawtext_kwargs = get_drawtext_kwargs(\n            align,\n            self.resolution,\n            text_for_size,\n            options\n        )\n\n        final_text = final_text.replace(\",\", r\"\\,\").replace(':', r'\\:')\n        drawtext_kwargs[\"text\"] = f\"'{final_text}'\"\n\n        args = \":\".join(f\"{k}={v}\" for k, v in drawtext_kwargs.items())\n        drawtext = draw % {\n            **options,\n            \"args\": args,\n            \"label\": align,\n        }\n\n        self.filters[\"drawtext\"].append(drawtext)\n\n    def command(self, output=None, args=None, overwrite=False):\n        \"\"\"\n        Generate the entire FFMPEG command.\n\n        :param str output: output file\n        :param str args: additional FFMPEG arguments\n        :param bool overwrite: overwrite the output if it exists\n        :returns: completed command\n        :rtype: str\n        \"\"\"\n        output = '\"{}\"'.format(output or '')\n        if overwrite:\n            output = '-y {}'.format(output)\n\n        filters = \"\"\n        filter_string = self.filter_string\n        if filter_string:\n            with tempfile.NamedTemporaryFile(\n                prefix=\"ayon_burnin_\", mode=\"w\", delete=False\n            ) as temp:\n                temp.write(filter_string)\n                filters_path = temp.name\n            filters = '-filter_script:v \"{}\"'.format(filters_path)\n            print(\"Filters:\", filter_string)\n            self.cleanup_paths.append(filters_path)\n\n        if self.first_frame is not None:\n            start_number_arg = \"-start_number {}\".format(self.first_frame)\n            self.input_args.append(start_number_arg)\n            if \"start_number\" not in args:\n                if not args:\n                    args = start_number_arg\n                else:\n                    args = \" \".join((start_number_arg, args))\n\n        input_args = \"\"\n        if self.input_args:\n            input_args = \" {}\".format(\" \".join(self.input_args))\n\n        return (FFMPEG % {\n            'input_args': input_args,\n            'input': self.source,\n            'output': output,\n            'args': '%s ' % args if args else '',\n            'filters': filters\n        }).strip()\n\n    def render(self, output, args=None, overwrite=False, **kwargs):\n        \"\"\"\n        Render the media to a specified destination.\n\n        :param str output: output file\n        :param str args: additional FFMPEG arguments\n        :param bool overwrite: overwrite the output if it exists\n        \"\"\"\n        if not overwrite and os.path.exists(output):\n            raise RuntimeError(\"Destination '%s' exists, please \"\n                               \"use overwrite\" % output)\n\n        is_sequence = \"%\" in output\n\n        command = self.command(\n            output=output,\n            args=args,\n            overwrite=overwrite\n        )\n        print(\"Launching command: {}\".format(command))\n\n        use_shell = True\n        try:\n            test_proc = subprocess.Popen(\n                f\"{FFMPEG_EXE_COMMAND} --help\", shell=True\n            )\n            test_proc.wait()\n        except BaseException:\n            use_shell = False\n\n        kwargs = {\n            \"stdout\": subprocess.PIPE,\n            \"stderr\": subprocess.PIPE,\n            \"shell\": use_shell,\n        }\n        proc = subprocess.Popen(command, **kwargs)\n\n        _stdout, _stderr = proc.communicate()\n        if _stdout:\n            print(_stdout.decode(\"utf-8\", errors=\"backslashreplace\"))\n\n        # This will probably never happen as ffmpeg use stdout\n        if _stderr:\n            print(_stderr.decode(\"utf-8\", errors=\"backslashreplace\"))\n\n        if proc.returncode != 0:\n            raise RuntimeError(\n                \"Failed to render '{}': {}'\".format(output, command)\n            )\n        if is_sequence:\n            output = output % kwargs.get(\"duration\")\n\n        if not os.path.exists(output):\n            raise RuntimeError(\n                \"Failed to generate this f*cking file '%s'\" % output\n            )\n\n        for path in self.cleanup_paths:\n            if os.path.exists(path):\n                os.remove(path)\n</code></pre>"},{"location":"autoapi/client/ayon_core/scripts/otio_burnin.html#client.ayon_core.scripts.otio_burnin.ModifiedBurnins.add_per_frame_text","title":"<code>add_per_frame_text(text, align, frame_start, frame_end, listed_keys, options=None)</code>","text":"<p>Add text that changes per frame.</p> <p>Parameters:</p> Name Type Description Default <code>text</code> <code>str</code> <p>Template string with unfilled keys that are changed per frame.</p> required <code>align</code> <code>str</code> <p>Alignment of text.</p> required <code>frame_start</code> <code>int</code> <p>Starting frame for burnins current frame.</p> required <code>frame_end</code> <code>int</code> <p>Ending frame for burnins current frame.</p> required <code>listed_keys</code> <code>list</code> <p>List of keys that are changed per frame.</p> required <code>options</code> <code>Optional[dict]</code> <p>Options to affect style of burnin.</p> <code>None</code> Source code in <code>client/ayon_core/scripts/otio_burnin.py</code> <pre><code>def add_per_frame_text(\n    self,\n    text,\n    align,\n    frame_start,\n    frame_end,\n    listed_keys,\n    options=None\n):\n    \"\"\"Add text that changes per frame.\n\n    Args:\n        text (str): Template string with unfilled keys that are changed\n            per frame.\n        align (str): Alignment of text.\n        frame_start (int): Starting frame for burnins current frame.\n        frame_end (int): Ending frame for burnins current frame.\n        listed_keys (list): List of keys that are changed per frame.\n        options (Optional[dict]): Options to affect style of burnin.\n    \"\"\"\n\n    if not options:\n        options = ffmpeg_burnins.TimeCodeOptions(**self.options_init)\n\n    options = options.copy()\n    if frame_start is None:\n        frame_start = options[\"frame_offset\"]\n\n    # `frame_end` is only for meassurements of text position\n    if frame_end is None:\n        frame_end = options[\"frame_end\"]\n\n    fps = options.get(\"fps\")\n    if not fps:\n        fps = self.frame_rate\n\n    text_for_size = text\n    if CURRENT_FRAME_SPLITTER in text:\n        expr = self._get_current_frame_expression(frame_start, frame_end)\n        if expr is None:\n            expr = MISSING_KEY_VALUE\n            text_for_size = text_for_size.replace(\n                CURRENT_FRAME_SPLITTER, MISSING_KEY_VALUE)\n        text = text.replace(CURRENT_FRAME_SPLITTER, expr)\n\n    # Find longest list with values\n    longest_list_len = max(\n        len(item[\"values\"]) for item in listed_keys.values()\n    )\n    # Where to store formatted values per frame by key\n    new_listed_keys = [{} for _ in range(longest_list_len)]\n    # Find the longest value per fill key.\n    #   The longest value is used to determine size of burnin box.\n    longest_value_by_key = {}\n    for key, item in listed_keys.items():\n        values = item[\"values\"]\n        # Fill the missing values from the longest list with the last\n        #   value to make sure all values have same \"frame count\"\n        last_value = values[-1] if values else \"\"\n        for _ in range(longest_list_len - len(values)):\n            values.append(last_value)\n\n        # Prepare dictionary structure for nestes values\n        # - last key is overriden on each frame loop\n        item_keys = list(item[\"keys\"])\n        fill_data = {}\n        sub_value = fill_data\n        last_item_key = item_keys.pop(-1)\n        for item_key in item_keys:\n            sub_value[item_key] = {}\n            sub_value = sub_value[item_key]\n\n        # Fill value per frame\n        key_max_len = 0\n        key_max_value = \"\"\n        for value, new_values in zip(values, new_listed_keys):\n            sub_value[last_item_key] = value\n            try:\n                value = key.format(**sub_value)\n            except (TypeError, KeyError, ValueError):\n                value = MISSING_KEY_VALUE\n            new_values[key] = value\n\n            value_len = len(value)\n            if value_len &gt; key_max_len:\n                key_max_value = value\n                key_max_len = value_len\n\n        # Store the longest value\n        longest_value_by_key[key] = key_max_value\n\n    # Make sure the longest value of each key is replaced for text size\n    #   calculation\n    for key, value in longest_value_by_key.items():\n        text_for_size = text_for_size.replace(key, value)\n\n    # Create temp file with instructions for each frame of text\n    lines = []\n    for frame, value in enumerate(new_listed_keys):\n        seconds = float(frame) / fps\n        # Escape special character\n        new_text = text\n        for _key, _value in value.items():\n            _value = str(_value)\n            new_text = new_text.replace(_key, str(_value))\n\n        new_text = (\n            str(new_text)\n            .replace(\"\\\\\", \"\\\\\\\\\")\n            .replace(\",\", \"\\\\,\")\n            .replace(\":\", \"\\\\:\")\n        )\n        lines.append(\n            f\"{seconds} drawtext@{align} reinit text='{new_text}';\")\n\n    with tempfile.NamedTemporaryFile(\n        prefix=\"ayon_burnin_\", mode=\"w\", delete=False,\n    ) as temp:\n        path = temp.name\n        temp.write(\"\\n\".join(lines))\n\n    self.cleanup_paths.append(path)\n    self.filters[\"drawtext\"].append(\"sendcmd=f='{}'\".format(\n        path.replace(\"\\\\\", \"/\").replace(\":\", \"\\\\:\")\n    ))\n    self.add_text(text_for_size, align, frame_start, frame_end, options)\n</code></pre>"},{"location":"autoapi/client/ayon_core/scripts/otio_burnin.html#client.ayon_core.scripts.otio_burnin.ModifiedBurnins.add_text","title":"<code>add_text(text, align, frame_start=None, frame_end=None, options=None)</code>","text":"<p>Adding static text to a filter.</p> <p>:param str text: text to apply to the drawtext :param enum align: alignment, must use provided enum flags :param int frame_start: starting frame for burnins current frame :param dict options: recommended to use TextOptions</p> Source code in <code>client/ayon_core/scripts/otio_burnin.py</code> <pre><code>def add_text(\n    self,\n    text,\n    align,\n    frame_start=None,\n    frame_end=None,\n    options=None,\n):\n    \"\"\"\n    Adding static text to a filter.\n\n    :param str text: text to apply to the drawtext\n    :param enum align: alignment, must use provided enum flags\n    :param int frame_start: starting frame for burnins current frame\n    :param dict options: recommended to use TextOptions\n    \"\"\"\n    if not text:\n        # do not add empty text, eg.: if it was using an optional part\n        return\n\n    if not options:\n        options = ffmpeg_burnins.TextOptions(**self.options_init)\n\n    options = options.copy()\n    if frame_start is not None:\n        options[\"frame_offset\"] = frame_start\n\n    # `frame_end` is only for meassurements of text position\n    if frame_end is not None:\n        options[\"frame_end\"] = frame_end\n\n    options[\"label\"] = align\n    self._add_burnin(text, align, options, DRAWTEXT)\n</code></pre>"},{"location":"autoapi/client/ayon_core/scripts/otio_burnin.html#client.ayon_core.scripts.otio_burnin.ModifiedBurnins.add_timecode","title":"<code>add_timecode(align, frame_start=None, frame_end=None, frame_start_tc=None, text=None, options=None)</code>","text":"<p>Convenience method to create the frame number expression.</p> <p>:param enum align: alignment, must use provided enum flags :param int frame_start:  starting frame for burnins current frame :param int frame_start_tc:  starting frame for burnins timecode :param str text: text that will be before timecode :param dict options: recommended to use TimeCodeOptions</p> Source code in <code>client/ayon_core/scripts/otio_burnin.py</code> <pre><code>def add_timecode(\n    self, align, frame_start=None, frame_end=None, frame_start_tc=None,\n    text=None, options=None\n):\n    \"\"\"\n    Convenience method to create the frame number expression.\n\n    :param enum align: alignment, must use provided enum flags\n    :param int frame_start:  starting frame for burnins current frame\n    :param int frame_start_tc:  starting frame for burnins timecode\n    :param str text: text that will be before timecode\n    :param dict options: recommended to use TimeCodeOptions\n    \"\"\"\n    if not options:\n        options = ffmpeg_burnins.TimeCodeOptions(**self.options_init)\n\n    options = options.copy()\n    if frame_start is not None:\n        options[\"frame_offset\"] = frame_start\n\n    # `frame_end` is only for meassurements of text position\n    if frame_end is not None:\n        options[\"frame_end\"] = frame_end\n\n    if not frame_start_tc:\n        frame_start_tc = options[\"frame_offset\"]\n\n    if not text:\n        text = \"\"\n\n    if not options.get(\"fps\"):\n        options[\"fps\"] = self.frame_rate\n\n    if isinstance(frame_start_tc, str):\n        options[\"timecode\"] = frame_start_tc\n    else:\n        options[\"timecode\"] = ffmpeg_burnins._frames_to_timecode(\n            frame_start_tc,\n            self.frame_rate\n        )\n\n    self._add_burnin(text, align, options, TIMECODE)\n</code></pre>"},{"location":"autoapi/client/ayon_core/scripts/otio_burnin.html#client.ayon_core.scripts.otio_burnin.ModifiedBurnins.command","title":"<code>command(output=None, args=None, overwrite=False)</code>","text":"<p>Generate the entire FFMPEG command.</p> <p>:param str output: output file :param str args: additional FFMPEG arguments :param bool overwrite: overwrite the output if it exists :returns: completed command :rtype: str</p> Source code in <code>client/ayon_core/scripts/otio_burnin.py</code> <pre><code>def command(self, output=None, args=None, overwrite=False):\n    \"\"\"\n    Generate the entire FFMPEG command.\n\n    :param str output: output file\n    :param str args: additional FFMPEG arguments\n    :param bool overwrite: overwrite the output if it exists\n    :returns: completed command\n    :rtype: str\n    \"\"\"\n    output = '\"{}\"'.format(output or '')\n    if overwrite:\n        output = '-y {}'.format(output)\n\n    filters = \"\"\n    filter_string = self.filter_string\n    if filter_string:\n        with tempfile.NamedTemporaryFile(\n            prefix=\"ayon_burnin_\", mode=\"w\", delete=False\n        ) as temp:\n            temp.write(filter_string)\n            filters_path = temp.name\n        filters = '-filter_script:v \"{}\"'.format(filters_path)\n        print(\"Filters:\", filter_string)\n        self.cleanup_paths.append(filters_path)\n\n    if self.first_frame is not None:\n        start_number_arg = \"-start_number {}\".format(self.first_frame)\n        self.input_args.append(start_number_arg)\n        if \"start_number\" not in args:\n            if not args:\n                args = start_number_arg\n            else:\n                args = \" \".join((start_number_arg, args))\n\n    input_args = \"\"\n    if self.input_args:\n        input_args = \" {}\".format(\" \".join(self.input_args))\n\n    return (FFMPEG % {\n        'input_args': input_args,\n        'input': self.source,\n        'output': output,\n        'args': '%s ' % args if args else '',\n        'filters': filters\n    }).strip()\n</code></pre>"},{"location":"autoapi/client/ayon_core/scripts/otio_burnin.html#client.ayon_core.scripts.otio_burnin.ModifiedBurnins.render","title":"<code>render(output, args=None, overwrite=False, **kwargs)</code>","text":"<p>Render the media to a specified destination.</p> <p>:param str output: output file :param str args: additional FFMPEG arguments :param bool overwrite: overwrite the output if it exists</p> Source code in <code>client/ayon_core/scripts/otio_burnin.py</code> <pre><code>def render(self, output, args=None, overwrite=False, **kwargs):\n    \"\"\"\n    Render the media to a specified destination.\n\n    :param str output: output file\n    :param str args: additional FFMPEG arguments\n    :param bool overwrite: overwrite the output if it exists\n    \"\"\"\n    if not overwrite and os.path.exists(output):\n        raise RuntimeError(\"Destination '%s' exists, please \"\n                           \"use overwrite\" % output)\n\n    is_sequence = \"%\" in output\n\n    command = self.command(\n        output=output,\n        args=args,\n        overwrite=overwrite\n    )\n    print(\"Launching command: {}\".format(command))\n\n    use_shell = True\n    try:\n        test_proc = subprocess.Popen(\n            f\"{FFMPEG_EXE_COMMAND} --help\", shell=True\n        )\n        test_proc.wait()\n    except BaseException:\n        use_shell = False\n\n    kwargs = {\n        \"stdout\": subprocess.PIPE,\n        \"stderr\": subprocess.PIPE,\n        \"shell\": use_shell,\n    }\n    proc = subprocess.Popen(command, **kwargs)\n\n    _stdout, _stderr = proc.communicate()\n    if _stdout:\n        print(_stdout.decode(\"utf-8\", errors=\"backslashreplace\"))\n\n    # This will probably never happen as ffmpeg use stdout\n    if _stderr:\n        print(_stderr.decode(\"utf-8\", errors=\"backslashreplace\"))\n\n    if proc.returncode != 0:\n        raise RuntimeError(\n            \"Failed to render '{}': {}'\".format(output, command)\n        )\n    if is_sequence:\n        output = output % kwargs.get(\"duration\")\n\n    if not os.path.exists(output):\n        raise RuntimeError(\n            \"Failed to generate this f*cking file '%s'\" % output\n        )\n\n    for path in self.cleanup_paths:\n        if os.path.exists(path):\n            os.remove(path)\n</code></pre>"},{"location":"autoapi/client/ayon_core/scripts/otio_burnin.html#client.ayon_core.scripts.otio_burnin.burnins_from_data","title":"<code>burnins_from_data(input_path, output_path, data, codec_data=None, options=None, burnin_values=None, overwrite=True, full_input_path=None, first_frame=None, source_ffmpeg_cmd=None)</code>","text":"<p>This method adds burnins to video/image file based on presets setting.</p> <p>Extension of output MUST be same as input. (mov -&gt; mov, avi -&gt; avi,...)</p> <p>Parameters:</p> Name Type Description Default <code>input_path</code> <code>str</code> <p>Full path to input file where burnins should be add.</p> required <code>output_path</code> <code>str</code> <p>Full path to output file where output will be rendered.</p> required <code>data</code> <code>dict</code> <p>Data required for burnin settings (more info below).</p> required <code>codec_data</code> <code>list</code> <p>All codec related arguments in list.</p> <code>None</code> <code>options</code> <code>dict</code> <p>Options for burnins.</p> <code>None</code> <code>burnin_values</code> <code>dict</code> <p>Contain positioned values.</p> <code>None</code> <code>overwrite</code> <code>bool</code> <p>Output will be overwritten if already exists, True by default.</p> <code>True</code> <p>Presets must be set separately. Should be dict with 2 keys: - \"options\" - sets look of burnins - colors, opacity,...     (more info: ModifiedBurnins doc)             - OPTIONAL default values are used when not included - \"burnins\" - contains dictionary with burnins settings             - OPTIONAL burnins won't be added (easier is not to use this)     - each key of \"burnins\" represents Alignment,     there are 6 possibilities:         TOP_LEFT        TOP_CENTERED        TOP_RIGHT         BOTTOM_LEFT     BOTTOM_CENTERED     BOTTOM_RIGHT     - value must be string with text you want to burn-in     - text may contain specific formatting keys (exmplained below)</p> <p>Requirement of data keys is based on presets. - \"frame_start\" - is required when \"timecode\" or \"current_frame\" ins keys - \"frame_start_tc\" - when \"timecode\" should start with different frame - keys for static text</p> <p>EXAMPLE: preset = {     \"options\": {OPTIONS FOR LOOK},     \"burnins\": {         \"TOP_LEFT\": \"static_text\",         \"TOP_RIGHT\": \"{shot}\",         \"BOTTOM_LEFT\": \"TC: {timecode}\",         \"BOTTOM_RIGHT\": \"{frame_start}{current_frame}\"     } }</p> <p>For this preset we'll need at least this data: data = {     \"frame_start\": 1001,     \"shot\": \"sh0010\" }</p> <p>When Timecode should start from 1 then data need: data = {     \"frame_start\": 1001,     \"frame_start_tc\": 1,     \"shot\": \"sh0010\" }</p> Source code in <code>client/ayon_core/scripts/otio_burnin.py</code> <pre><code>def burnins_from_data(\n    input_path,\n    output_path,\n    data,\n    codec_data=None,\n    options=None,\n    burnin_values=None,\n    overwrite=True,\n    full_input_path=None,\n    first_frame=None,\n    source_ffmpeg_cmd=None,\n):\n    \"\"\"This method adds burnins to video/image file based on presets setting.\n\n    Extension of output MUST be same as input. (mov -&gt; mov, avi -&gt; avi,...)\n\n    Args:\n        input_path (str): Full path to input file where burnins should be add.\n        output_path (str): Full path to output file where output will be\n            rendered.\n        data (dict): Data required for burnin settings (more info below).\n        codec_data (list): All codec related arguments in list.\n        options (dict): Options for burnins.\n        burnin_values (dict): Contain positioned values.\n        overwrite (bool): Output will be overwritten if already exists,\n            True by default.\n\n    Presets must be set separately. Should be dict with 2 keys:\n    - \"options\" - sets look of burnins - colors, opacity,...\n        (more info: ModifiedBurnins doc)\n                - *OPTIONAL* default values are used when not included\n    - \"burnins\" - contains dictionary with burnins settings\n                - *OPTIONAL* burnins won't be added (easier is not to use this)\n        - each key of \"burnins\" represents Alignment,\n        there are 6 possibilities:\n            TOP_LEFT        TOP_CENTERED        TOP_RIGHT\n            BOTTOM_LEFT     BOTTOM_CENTERED     BOTTOM_RIGHT\n        - value must be string with text you want to burn-in\n        - text may contain specific formatting keys (exmplained below)\n\n    Requirement of *data* keys is based on presets.\n    - \"frame_start\" - is required when \"timecode\" or \"current_frame\" ins keys\n    - \"frame_start_tc\" - when \"timecode\" should start with different frame\n    - *keys for static text*\n\n    EXAMPLE:\n    preset = {\n        \"options\": {*OPTIONS FOR LOOK*},\n        \"burnins\": {\n            \"TOP_LEFT\": \"static_text\",\n            \"TOP_RIGHT\": \"{shot}\",\n            \"BOTTOM_LEFT\": \"TC: {timecode}\",\n            \"BOTTOM_RIGHT\": \"{frame_start}{current_frame}\"\n        }\n    }\n\n    For this preset we'll need at least this data:\n    data = {\n        \"frame_start\": 1001,\n        \"shot\": \"sh0010\"\n    }\n\n    When Timecode should start from 1 then data need:\n    data = {\n        \"frame_start\": 1001,\n        \"frame_start_tc\": 1,\n        \"shot\": \"sh0010\"\n    }\n    \"\"\"\n    ffprobe_data = None\n    if full_input_path:\n        ffprobe_data = _get_ffprobe_data(full_input_path)\n\n    burnin = ModifiedBurnins(input_path, ffprobe_data, options, first_frame)\n\n    frame_start = data.get(\"frame_start\")\n    frame_end = data.get(\"frame_end\")\n    frame_start_tc = data.get('frame_start_tc', frame_start)\n\n    video_stream = None\n    for stream in burnin._streams:\n        if stream.get(\"codec_type\") == \"video\":\n            video_stream = stream\n            break\n\n    if video_stream is None:\n        raise ValueError(\"Source didn't have video stream.\")\n\n    if \"resolution_width\" not in data:\n        data[\"resolution_width\"] = video_stream.get(\n            \"width\", MISSING_KEY_VALUE)\n\n    if \"resolution_height\" not in data:\n        data[\"resolution_height\"] = video_stream.get(\n            \"height\", MISSING_KEY_VALUE)\n\n    r_frame_rate = video_stream.get(\"r_frame_rate\", \"0/0\")\n    if \"fps\" not in data:\n        data[\"fps\"] = convert_ffprobe_fps_value(r_frame_rate)\n\n    # Check frame start and add expression if is available\n    if frame_start is not None:\n        data[CURRENT_FRAME_KEY[1:-1]] = CURRENT_FRAME_SPLITTER\n\n    if frame_start_tc is not None:\n        data[TIMECODE_KEY[1:-1]] = TIMECODE_KEY\n\n    source_timecode = video_stream.get(\"timecode\")\n    if source_timecode is None:\n        source_timecode = video_stream.get(\"tags\", {}).get(\"timecode\")\n\n    # Use \"format\" key from ffprobe data\n    #   - this is used e.g. in mxf extension\n    if source_timecode is None:\n        input_format = burnin.ffprobe_data.get(\"format\") or {}\n        source_timecode = input_format.get(\"timecode\")\n        if source_timecode is None:\n            source_timecode = input_format.get(\"tags\", {}).get(\"timecode\")\n\n    if source_timecode is not None:\n        data[SOURCE_TIMECODE_KEY[1:-1]] = SOURCE_TIMECODE_KEY\n\n    clean_up_paths = []\n    for align_text, value in burnin_values.items():\n        if not value:\n            continue\n\n        if isinstance(value, dict):\n            raise TypeError((\n                \"Expected string, number or list type.\"\n                \" Got: {} - \\\"{}\\\"\"\n                \" (Make sure you have new burnin presets).\"\n            ).format(str(type(value)), str(value)))\n\n        align = None\n        align_text = align_text.strip().lower()\n        if align_text == \"top_left\":\n            align = ModifiedBurnins.TOP_LEFT\n        elif align_text == \"top_centered\":\n            align = ModifiedBurnins.TOP_CENTERED\n        elif align_text == \"top_right\":\n            align = ModifiedBurnins.TOP_RIGHT\n        elif align_text == \"bottom_left\":\n            align = ModifiedBurnins.BOTTOM_LEFT\n        elif align_text == \"bottom_centered\":\n            align = ModifiedBurnins.BOTTOM_CENTERED\n        elif align_text == \"bottom_right\":\n            align = ModifiedBurnins.BOTTOM_RIGHT\n\n        has_timecode = TIMECODE_KEY in value\n        # Replace with missing key value if frame_start_tc is not set\n        if frame_start_tc is None and has_timecode:\n            has_timecode = False\n            print(\n                \"`frame_start` and `frame_start_tc`\"\n                \" are not set in entered data.\"\n            )\n            value = value.replace(TIMECODE_KEY, MISSING_KEY_VALUE)\n\n        has_source_timecode = SOURCE_TIMECODE_KEY in value\n        if source_timecode is None and has_source_timecode:\n            has_source_timecode = False\n            print(\"Source does not have set timecode value.\")\n            value = value.replace(SOURCE_TIMECODE_KEY, MISSING_KEY_VALUE)\n\n        # Remove optional parts for which are data not available\n        value_t = StringTemplate(value)\n        value = value_t.remove_optional_parts_for_data(data)\n\n        # Failsafe for missing keys.\n        fill_values, listed_keys, missing_keys = prepare_fill_values(\n            value, data\n        )\n\n        for key in missing_keys:\n            value = value.replace(key, MISSING_KEY_VALUE)\n\n        if listed_keys:\n            for key, key_value in fill_values.items():\n                if key == CURRENT_FRAME_KEY:\n                    key_value = CURRENT_FRAME_SPLITTER\n                value = value.replace(key, str(key_value))\n            burnin.add_per_frame_text(\n                value, align, frame_start, frame_end, listed_keys\n            )\n            continue\n\n        # Handle timecode differently\n        if has_source_timecode:\n            args = [align, frame_start, frame_end, source_timecode]\n            if not value.startswith(SOURCE_TIMECODE_KEY):\n                value_items = value.split(SOURCE_TIMECODE_KEY)\n                text = value_items[0].format(**data)\n                args.append(text)\n\n            burnin.add_timecode(*args)\n            continue\n\n        if has_timecode:\n            args = [align, frame_start, frame_end, frame_start_tc]\n            if not value.startswith(TIMECODE_KEY):\n                value_items = value.split(TIMECODE_KEY)\n                text = value_items[0].format(**data)\n                args.append(text)\n\n            burnin.add_timecode(*args)\n            continue\n\n        text = value.format(**data)\n\n        burnin.add_text(text, align, frame_start, frame_end)\n\n    ffmpeg_args = []\n    if codec_data:\n        # Use codec definition from method arguments\n        ffmpeg_args = codec_data\n        ffmpeg_args.append(\"-g 1\")\n\n    else:\n        ffmpeg_args.extend(\n            get_ffmpeg_format_args(burnin.ffprobe_data, source_ffmpeg_cmd)\n        )\n        ffmpeg_args.extend(\n            get_ffmpeg_codec_args(burnin.ffprobe_data, source_ffmpeg_cmd)\n        )\n        # Use arguments from source if are available source arguments\n        if source_ffmpeg_cmd:\n            copy_args = (\n                \"-metadata\",\n                \"-metadata:s:v:0\",\n            )\n            args = source_ffmpeg_cmd.split(\" \")\n            for idx, arg in enumerate(args):\n                if arg in copy_args:\n                    ffmpeg_args.extend([arg, args[idx + 1]])\n\n    # Use group one (same as `-intra` argument, which is deprecated)\n    ffmpeg_args_str = \" \".join(ffmpeg_args)\n    burnin.render(\n        output_path, args=ffmpeg_args_str, overwrite=overwrite, **data\n    )\n    for path in clean_up_paths:\n        os.remove(path)\n</code></pre>"},{"location":"autoapi/client/ayon_core/scripts/otio_burnin.html#client.ayon_core.scripts.otio_burnin.get_drawtext_kwargs","title":"<code>get_drawtext_kwargs(align, resolution, text, options)</code>","text":"<p>Returns a dictionary of arguments for the drawtext filter.</p> Source code in <code>client/ayon_core/scripts/otio_burnin.py</code> <pre><code>def get_drawtext_kwargs(align, resolution, text: str, options: dict):\n    \"\"\"Returns a dictionary of arguments for the drawtext filter.\"\"\"\n\n    font_file = options[\"font\"]\n    font_size = options[\"font_size\"]\n\n    font = ImageFont.truetype(font_file, font_size)\n    ascent, descent = get_metrics(font)\n    ffmpeg_font_file = (\n        font_file\n        .replace(\"\\\\\", \"\\\\\\\\\")\n        .replace(\",\", \"\\\\,\")\n        .replace(\":\", \"\\\\:\")\n    )\n    args = {\n        \"fontfile\": f\"'{ffmpeg_font_file}'\",\n        \"fontsize\": font_size,\n        \"fontcolor\": f\"{options['font_color']}@{options['opacity']:.1f}\",\n        \"y_align\": \"font\",\n    }\n\n    # padding\n    pad_l = pad_r = pad_t = pad_b = 0\n    if padding := options.get(\"bg_padding\"):\n        # distance from the top of an uppercase A to the ascend\n        pad_t = max(padding - ascent, 0)\n        pad_b = max(padding - descent, 0)\n        pad_l = pad_r = padding\n\n    # border box\n    if options.get(\"bg_color\") is not None:\n        args[\"box\"] = \"1\"\n        args[\"boxcolor\"] = f\"{options['bg_color']}@{options['bg_opacity']}\"\n        args[\"boxborderw\"] = f\"{pad_t}|{pad_r}|{pad_b}|{pad_l}\"\n\n    # x position\n    x_pos = 0\n    if align in (ffmpeg_burnins.TOP_CENTERED, ffmpeg_burnins.BOTTOM_CENTERED):\n        x_pos = \"w/2-tw/2\"\n\n    elif align in (ffmpeg_burnins.TOP_RIGHT, ffmpeg_burnins.BOTTOM_RIGHT):\n        text_width = get_text_width(text, font_file, font_size)\n        x_offset = options[\"x_offset\"]\n        x_pos = f\"w-{text_width + x_offset + pad_r}\"\n\n    elif align in (ffmpeg_burnins.TOP_LEFT, ffmpeg_burnins.BOTTOM_LEFT):\n        x_pos = options[\"x_offset\"] + pad_l\n\n    # y position\n    y_offset = options[\"y_offset\"]\n    if align in (\n        ffmpeg_burnins.TOP_CENTERED,\n        ffmpeg_burnins.TOP_RIGHT,\n        ffmpeg_burnins.TOP_LEFT\n    ):\n        y_pos = y_offset + pad_t\n    else:\n        y_pos = f\"h-{y_offset + ascent + descent + pad_b}\"\n\n    args[\"x\"] = x_pos\n    args[\"y\"] = y_pos\n\n    return args\n</code></pre>"},{"location":"autoapi/client/ayon_core/scripts/otio_burnin.html#client.ayon_core.scripts.otio_burnin.get_metrics","title":"<code>get_metrics(font)</code>","text":"<p>Return a ascent, descent for a font in pixels.</p> <p>Pillow's <code>font.getmetrics()</code> can under-report extremes for some fonts. To avoid clipping when aligning text, we additionally measure a broad set of printable characters via <code>font.getbbox(..., anchor=\"ms\")</code> and clamp the returned metrics to those observed bounds.</p> <p>Returns:</p> Type Description <code>tuple[float, float]</code> <p>tuple[float, float]: A 2-tuple of <code>(ascent, descent)</code> where both values are positive pixel distances above and below the baseline, respectively, rounded up to whole pixels.</p> Source code in <code>client/ayon_core/scripts/otio_burnin.py</code> <pre><code>def get_metrics(font: ImageFont.FreeTypeFont) -&gt; tuple[float, float]:\n    \"\"\"Return a ascent, descent for a font in pixels.\n\n    Pillow's `font.getmetrics()` can under-report extremes for some fonts.\n    To avoid clipping when aligning text, we additionally measure a broad set\n    of printable characters via `font.getbbox(..., anchor=\"ms\")` and clamp the\n    returned metrics to those observed bounds.\n\n    Returns:\n        tuple[float, float]: A 2-tuple of `(ascent, descent)` where both values\n            are positive pixel distances above and below the baseline,\n            respectively, rounded up to whole pixels.\n\n    \"\"\"\n    # official metrics\n    ascent, descent = font.getmetrics()\n\n    # measured real metrics\n    _, top, _, bottom = font.getbbox(string.printable, anchor=\"ms\")\n\n    ascent = max(ascent, abs(math.ceil(top)))\n    descent = max(descent, abs(math.ceil(bottom)))\n    return ascent, descent\n</code></pre>"},{"location":"autoapi/client/ayon_core/scripts/otio_burnin.html#client.ayon_core.scripts.otio_burnin.prepare_fill_values","title":"<code>prepare_fill_values(burnin_template, data)</code>","text":"<p>Prepare values that will be filled instead of burnin template.</p> <p>Parameters:</p> Name Type Description Default <code>burnin_template</code> <code>str</code> <p>Burnin template string.</p> required <code>data</code> <code>dict[str, Any]</code> <p>Data that will be used to fill template.</p> required <p>Returns:</p> Type Description <p>tuple[dict[str, dict[str, Any]], dict[str, Any], set[str]]: Filled values that can be used as are, listed values that have different value per frame and missing keys that are not present in data.</p> Source code in <code>client/ayon_core/scripts/otio_burnin.py</code> <pre><code>def prepare_fill_values(burnin_template, data):\n    \"\"\"Prepare values that will be filled instead of burnin template.\n\n    Args:\n        burnin_template (str): Burnin template string.\n        data (dict[str, Any]): Data that will be used to fill template.\n\n    Returns:\n        tuple[dict[str, dict[str, Any]], dict[str, Any], set[str]]: Filled\n            values that can be used as are, listed values that have different\n            value per frame and missing keys that are not present in data.\n    \"\"\"\n\n    fill_values = {}\n    listed_keys = {}\n    missing_keys = set()\n\n    for item in Formatter().parse(burnin_template):\n        _, field_name, format_spec, conversion = item\n        if not field_name:\n            continue\n        # Calculate nested keys '{project[name]}' -&gt; ['project', 'name']\n        keys = [key.rstrip(\"]\") for key in field_name.split(\"[\")]\n        # Calculate original full key for replacement\n        conversion = f\"!{conversion}\" if conversion else \"\"\n        format_spec = f\":{format_spec}\" if format_spec else \"\"\n        orig_key = f\"{{{field_name}{conversion}{format_spec}}}\"\n\n        key_value = data\n        try:\n            for key in keys:\n                key_value = key_value[key]\n\n            if isinstance(key_value, list):\n                listed_keys[orig_key] = {\n                    \"values\": key_value,\n                    \"keys\": keys}\n            else:\n                fill_values[orig_key] = orig_key.format(**data)\n        except (KeyError, TypeError):\n            missing_keys.add(orig_key)\n            continue\n    return fill_values, listed_keys, missing_keys\n</code></pre>"},{"location":"autoapi/client/ayon_core/scripts/slates/index.html","title":"slates","text":""},{"location":"autoapi/client/ayon_core/scripts/slates/__main__.html","title":"__main__","text":""},{"location":"autoapi/client/ayon_core/scripts/slates/slate_base/index.html","title":"slate_base","text":""},{"location":"autoapi/client/ayon_core/scripts/slates/slate_base/api.html","title":"api","text":""},{"location":"autoapi/client/ayon_core/scripts/slates/slate_base/api.html#client.ayon_core.scripts.slates.slate_base.api.BaseObj","title":"<code>BaseObj</code>","text":"<p>Base Object for slates.</p> Source code in <code>client/ayon_core/scripts/slates/slate_base/base.py</code> <pre><code>class BaseObj:\n    \"\"\"Base Object for slates.\"\"\"\n\n    obj_type = None\n    available_parents = []\n    all_style_keys = [\n        \"font-family\", \"font-size\", \"font-color\", \"font-bold\", \"font-italic\",\n        \"bg-color\", \"bg-alter-color\",\n        \"alignment-horizontal\", \"alignment-vertical\",\n        \"padding\", \"padding-left\", \"padding-right\",\n        \"padding-top\", \"padding-bottom\",\n        \"margin\", \"margin-left\", \"margin-right\",\n        \"margin-top\", \"margin-bottom\", \"width\", \"height\",\n        \"fill\", \"word-wrap\", \"ellide\", \"max-lines\"\n    ]\n    fill_data_regex = r\"{[^}]+}\"\n\n    def __init__(self, parent, style={}, name=None, pos_x=None, pos_y=None):\n        if not self.obj_type:\n            raise NotImplementedError(\n                \"Class don't have set object type &lt;{}&gt;\".format(\n                    self.__class__.__name__\n                )\n            )\n\n        parent_obj_type = None\n        if parent:\n            parent_obj_type = parent.obj_type\n\n        if parent_obj_type not in self.available_parents:\n            expected_parents = \", \".join(self.available_parents)\n            raise Exception((\n                \"Invalid parent &lt;{}&gt; for &lt;{}&gt;. Expected &lt;{}&gt;\"\n            ).format(\n                parent.__class__.__name__, self.obj_type, expected_parents\n            ))\n\n        self.parent = parent\n        self._style = style\n\n        self.id = uuid4()\n        self.name = name\n        self.items = {}\n\n        self._pos_x = pos_x or 0\n        self._pos_y = pos_y or 0\n\n        log_parts = []\n        module = self.__class__.__module__\n        if module and module != \"__main__\":\n            log_parts.append(module)\n        log_parts.append(self.__class__.__name__)\n        self.log = logging.getLogger(\".\".join(log_parts))\n\n        if parent:\n            parent.add_item(self)\n\n    def fill_data_format(self):\n        return\n\n    @property\n    def fill_data(self):\n        return self.parent.fill_data\n\n    @property\n    def main_style(self):\n        return load_default_style()\n\n    def collect_data(self):\n        return None\n\n    def find_item(self, obj_type=None, name=None):\n        obj_type_fits = False\n        name_fits = False\n        if obj_type is None or self.obj_type == obj_type:\n            obj_type_fits = True\n\n        if name is None or self.name != name:\n            name_fits = True\n\n        output = []\n        if obj_type_fits and name_fits:\n            output.append(self)\n\n        if not self.items:\n            return output\n\n        for item in self.items.values():\n            output.extend(\n                item.find_item(obj_type=obj_type, name=name)\n            )\n        return output\n\n    @property\n    def full_style(self):\n        if self.parent is not None:\n            style = dict(val for val in self.parent.full_style.items())\n        else:\n            style = self.main_style\n\n        for key, value in self._style.items():\n            if key in self.all_style_keys:\n                # TODO which variant is right?\n                style[self.obj_type][key] = value\n                # style[\"*\"][key] = value\n            else:\n                if key not in style:\n                    style[key] = {}\n\n                if isinstance(style[key], dict):\n                    style[key].update(value)\n                else:\n                    style[key] = value\n\n        return style\n\n    def get_style_for_obj_type(self, obj_type, style=None):\n        if not style:\n            style = copy.deepcopy(self.full_style)\n\n        base = style.get(\"*\") or {}\n        obj_specific = style.get(obj_type) or {}\n        name_specific = {}\n        if self.name:\n            name = str(self.name)\n            if not name.startswith(\"#\"):\n                name = \"#\" + name\n            name_specific = style.get(name) or {}\n\n        if obj_type == \"table-item\":\n            col_regex = r\"table-item-col\\[([\\d\\-, ]+)*\\]\"\n            row_regex = r\"table-item-row\\[([\\d\\-, ]+)*\\]\"\n            field_regex = (\n                r\"table-item-field\\[(([ ]+)?\\d+([ ]+)?:([ ]+)?\\d+([ ]+)?)*\\]\"\n            )\n            # STRICT field regex (not allowed spaces)\n            # fild_regex = r\"table-item-field\\[(\\d+:\\d+)*\\]\"\n\n            def get_indexes_from_regex_match(result, field=False):\n                group = result.group(1)\n                indexes = []\n                if field:\n                    return [\n                        int(part.strip()) for part in group.strip().split(\":\")\n                    ]\n\n                parts = group.strip().split(\",\")\n                for part in parts:\n                    part = part.strip()\n                    if \"-\" not in part:\n                        indexes.append(int(part))\n                        continue\n\n                    sub_parts = [\n                        int(sub.strip()) for sub in part.split(\"-\")\n                    ]\n                    if len(sub_parts) != 2:\n                        # TODO logging\n                        self.log.warning(\"Invalid range '{}'\".format(part))\n                        continue\n\n                    for idx in range(sub_parts[0], sub_parts[1] + 1):\n                        indexes.append(idx)\n                return indexes\n\n            for key, value in style.items():\n                if not key.startswith(obj_type):\n                    continue\n\n                result = re.search(col_regex, key)\n                if result:\n                    indexes = get_indexes_from_regex_match(result)\n                    if self.col_idx in indexes:\n                        obj_specific.update(value)\n                    continue\n\n                result = re.search(row_regex, key)\n                if result:\n                    indexes = get_indexes_from_regex_match(result)\n                    if self.row_idx in indexes:\n                        obj_specific.update(value)\n                    continue\n\n                result = re.search(field_regex, key)\n                if result:\n                    row_idx, col_idx = get_indexes_from_regex_match(\n                        result, True\n                    )\n                    if self.col_idx == col_idx and self.row_idx == row_idx:\n                        obj_specific.update(value)\n\n        output = {}\n        output.update(base)\n        output.update(obj_specific)\n        output.update(name_specific)\n\n        return output\n\n    @property\n    def style(self):\n        return self.get_style_for_obj_type(self.obj_type)\n\n    @property\n    def item_pos_x(self):\n        if self.parent.obj_type == \"main_frame\":\n            return int(self._pos_x)\n        return 0\n\n    @property\n    def item_pos_y(self):\n        if self.parent.obj_type == \"main_frame\":\n            return int(self._pos_y)\n        return 0\n\n    @property\n    def content_pos_x(self):\n        pos_x = self.item_pos_x\n        margin = self.style[\"margin\"]\n        margin_left = self.style.get(\"margin-left\") or margin\n\n        pos_x += margin_left\n\n        return pos_x\n\n    @property\n    def content_pos_y(self):\n        pos_y = self.item_pos_y\n        margin = self.style[\"margin\"]\n        margin_top = self.style.get(\"margin-top\") or margin\n        return pos_y + margin_top\n\n    @property\n    def value_pos_x(self):\n        pos_x = int(self.content_pos_x)\n        padding = self.style[\"padding\"]\n        padding_left = self.style.get(\"padding-left\")\n        if padding_left is None:\n            padding_left = padding\n\n        pos_x += padding_left\n\n        return pos_x\n\n    @property\n    def value_pos_y(self):\n        pos_y = int(self.content_pos_y)\n        padding = self.style[\"padding\"]\n        padding_top = self.style.get(\"padding-top\")\n        if padding_top is None:\n            padding_top = padding\n\n        pos_y += padding_top\n\n        return pos_y\n\n    @property\n    def value_pos_start(self):\n        return (self.value_pos_x, self.value_pos_y)\n\n    @property\n    def value_pos_end(self):\n        pos_x, pos_y = self.value_pos_start\n        pos_x += self.width()\n        pos_y += self.height()\n        return (pos_x, pos_y)\n\n    @property\n    def content_pos_start(self):\n        return (self.content_pos_x, self.content_pos_y)\n\n    @property\n    def content_pos_end(self):\n        pos_x, pos_y = self.content_pos_start\n        pos_x += self.content_width()\n        pos_y += self.content_height()\n        return (pos_x, pos_y)\n\n    def value_width(self):\n        raise NotImplementedError(\n            \"Attribute &lt;content_width&gt; is not implemented &lt;{}&gt;\".format(\n                self.__class__.__name__\n            )\n        )\n\n    def value_height(self):\n        raise NotImplementedError(\n            \"Attribute &lt;content_width&gt; is not implemented for &lt;{}&gt;\".format(\n                self.__class__.__name__\n            )\n        )\n\n    def content_width(self):\n        width = self.value_width()\n        padding = self.style[\"padding\"]\n        padding_left = self.style.get(\"padding-left\")\n        if padding_left is None:\n            padding_left = padding\n\n        padding_right = self.style.get(\"padding-right\")\n        if padding_right is None:\n            padding_right = padding\n\n        return width + padding_left + padding_right\n\n    def content_height(self):\n        height = self.value_height()\n        padding = self.style[\"padding\"]\n        padding_top = self.style.get(\"padding-top\")\n        if padding_top is None:\n            padding_top = padding\n\n        padding_bottom = self.style.get(\"padding-bottom\")\n        if padding_bottom is None:\n            padding_bottom = padding\n\n        return height + padding_top + padding_bottom\n\n    def width(self):\n        width = self.content_width()\n\n        margin = self.style[\"margin\"]\n        margin_left = self.style.get(\"margin-left\") or margin\n        margin_right = self.style.get(\"margin-right\") or margin\n\n        return width + margin_left + margin_right\n\n    def height(self):\n        height = self.content_height()\n\n        margin = self.style[\"margin\"]\n        margin_top = self.style.get(\"margin-top\") or margin\n        margin_bottom = self.style.get(\"margin-bottom\") or margin\n\n        return height + margin_bottom + margin_top\n\n    def add_item(self, item):\n        self.items[item.id] = item\n        item.fill_data_format()\n\n    def reset(self):\n        for item in self.items.values():\n            item.reset()\n</code></pre>"},{"location":"autoapi/client/ayon_core/scripts/slates/slate_base/api.html#client.ayon_core.scripts.slates.slate_base.api.create_slates","title":"<code>create_slates(fill_data, slate_name=None, slate_data=None, data_output_json=None)</code>","text":"<p>Implmentation for command line executing.</p> <p>Data for slates are by defaule taken from presets. That requires to enter, <code>slate_name</code>. If <code>slate_data</code> are entered then they are used.</p> <p><code>data_output</code> should be path to json file where data will be collected.</p> Source code in <code>client/ayon_core/scripts/slates/slate_base/lib.py</code> <pre><code>def create_slates(\n    fill_data, slate_name=None, slate_data=None, data_output_json=None\n):\n    \"\"\"Implmentation for command line executing.\n\n    Data for slates are by defaule taken from presets. That requires to enter,\n    `slate_name`. If `slate_data` are entered then they are used.\n\n    `data_output` should be path to json file where data will be collected.\n    \"\"\"\n    if slate_data is None and slate_name is None:\n        raise TypeError(\n            \"`create_slates` expects to enter data for slates or name\"\n            \" of slate preset.\"\n        )\n\n    elif slate_data is None:\n        slate_presets = {}\n        slate_data = slate_presets.get(slate_name)\n        if slate_data is None:\n            raise ValueError(\n                \"Preset name \\\"{}\\\" was not found in slate presets.\".format(\n                    slate_name\n                )\n            )\n\n    missing_keys = []\n    for key in RequiredSlateKeys:\n        if key not in slate_data:\n            missing_keys.append(\"`{}`\".format(key))\n\n    if missing_keys:\n        log.error(\"Slate data of &lt;{}&gt; miss required keys: {}\".format(\n            slate_name, \", \".join(missing_keys)\n        ))\n        return False\n\n    width = slate_data[\"width\"]\n    height = slate_data[\"height\"]\n    dst_path = slate_data[\"destination_path\"]\n    style = slate_data.get(\"style\") or {}\n\n    main = MainFrame(width, height, dst_path, fill_data, style=style)\n\n    load_queue = Queue()\n    for item in slate_data[\"items\"]:\n        load_queue.put((item, main))\n\n    while not load_queue.empty():\n        item_data, parent = load_queue.get()\n\n        item_type = item_data[\"type\"].lower()\n        item_style = item_data.get(\"style\", {})\n        item_name = item_data.get(\"name\")\n\n        pos_x = item_data.get(\"pos_x\")\n        pos_y = item_data.get(\"pos_y\")\n        if parent.obj_type != \"main_frame\":\n            if pos_x or pos_y:\n                # TODO logging\n                log.warning((\n                    \"You have specified `pos_x` and `pos_y` but won't be used.\"\n                    \" Possible only if parent of an item is `main_frame`.\"\n                ))\n            pos_x = None\n            pos_y = None\n\n        kwargs = {\n            \"parent\": parent,\n            \"style\": item_style,\n            \"name\": item_name,\n            \"pos_x\": pos_x,\n            \"pos_y\": pos_y\n        }\n\n        if item_type == \"layer\":\n            direction = item_data.get(\"direction\", 0)\n            item_obj = Layer(direction, **kwargs)\n            for item in item_data.get(\"items\", []):\n                load_queue.put((item, item_obj))\n\n        elif item_type == \"table\":\n            use_alternate_color = item_data.get(\"use_alternate_color\", False)\n            values = item_data.get(\"values\") or []\n            ItemTable(values, use_alternate_color, **kwargs)\n\n        elif item_type == \"image\":\n            path = item_data[\"path\"]\n            ItemImage(path, **kwargs)\n\n        elif item_type == \"rectangle\":\n            ItemRectangle(**kwargs)\n\n        elif item_type == \"placeholder\":\n            path = item_data[\"path\"]\n            ItemPlaceHolder(path, **kwargs)\n\n        else:\n            # TODO logging\n            log.warning(\n                \"Not implemented object type `{}` - skipping\".format(item_type)\n            )\n\n    main.draw()\n    log.debug(\"Slate creation finished\")\n\n    if not data_output_json:\n        return\n\n    if not data_output_json.endswith(\".json\"):\n        raise ValueError(\"Output path must be .json file.\")\n\n    data_output_json_dir = os.path.dirname(data_output_json)\n    if not os.path.exists(data_output_json_dir):\n        log.info(\"Creating folder \\\"{}\\\"\".format(data_output_json_dir))\n        os.makedirs(data_output_json_dir)\n\n    output_data = main.collect_data()\n    with open(data_output_json, \"w\") as json_file:\n        json_file.write(json.dumps(output_data, indent=4))\n\n    log.info(\"Metadata collected in \\\"{}\\\".\".format(data_output_json))\n</code></pre>"},{"location":"autoapi/client/ayon_core/scripts/slates/slate_base/api.html#client.ayon_core.scripts.slates.slate_base.api.example","title":"<code>example()</code>","text":"<p>Example data to demontrate function.</p> <p>It is required to fill \"destination_path\", \"thumbnail_path\" and \"color_bar_path\" in <code>example_fill_data</code> to be able to execute.</p> Source code in <code>client/ayon_core/scripts/slates/slate_base/example.py</code> <pre><code>def example():\n    \"\"\"Example data to demontrate function.\n\n    It is required to fill \"destination_path\", \"thumbnail_path\"\n    and \"color_bar_path\" in `example_fill_data` to be able to execute.\n    \"\"\"\n\n    example_fill_data = {\n        \"destination_path\": \"PATH/TO/OUTPUT/FILE\",\n        \"project\": {\n            \"name\": \"Testing project\"\n        },\n        \"intent\": \"WIP\",\n        \"version_name\": \"seq01_sh0100_compositing_v01\",\n        \"date\": \"2019-08-09\",\n        \"shot_type\": \"2d comp\",\n        \"submission_note\": (\n            \"Lorem ipsum dolor sit amet, consectetuer adipiscing elit.\"\n            \" Aenean commodo ligula eget dolor. Aenean massa.\"\n            \" Cum sociis natoque penatibus et magnis dis parturient montes,\"\n            \" nascetur ridiculus mus. Donec quam felis, ultricies nec,\"\n            \" pellentesque eu, pretium quis, sem. Nulla consequat massa quis\"\n            \" enim. Donec pede justo, fringilla vel,\"\n            \" aliquet nec, vulputate eget, arcu.\"\n        ),\n        \"thumbnail_path\": \"PATH/TO/THUMBNAIL/FILE\",\n        \"color_bar_path\": \"PATH/TO/COLOR/BAR/FILE\",\n        \"vendor\": \"Our Studio\",\n        \"shot_name\": \"sh0100\",\n        \"frame_start\": 1001,\n        \"frame_end\": 1004,\n        \"duration\": 3\n    }\n\n    example_presets = {\"example_HD\": {\n        \"width\": 1920,\n        \"height\": 1080,\n        \"destination_path\": \"{destination_path}\",\n        \"style\": {\n            \"*\": {\n                \"font-family\": \"arial\",\n                \"font-color\": \"#ffffff\",\n                \"font-bold\": False,\n                \"font-italic\": False,\n                \"bg-color\": \"#0077ff\",\n                \"alignment-horizontal\": \"left\",\n                \"alignment-vertical\": \"top\"\n            },\n            \"layer\": {\n                \"padding\": 0,\n                \"margin\": 0\n            },\n            \"rectangle\": {\n                \"padding\": 0,\n                \"margin\": 0,\n                \"bg-color\": \"#E9324B\",\n                \"fill\": True\n            },\n            \"main_frame\": {\n                \"padding\": 0,\n                \"margin\": 0,\n                \"bg-color\": \"#252525\"\n            },\n            \"table\": {\n                \"padding\": 0,\n                \"margin\": 0,\n                \"bg-color\": \"transparent\"\n            },\n            \"table-item\": {\n                \"padding\": 5,\n                \"padding-bottom\": 10,\n                \"margin\": 0,\n                \"bg-color\": \"#212121\",\n                \"bg-alter-color\": \"#272727\",\n                \"font-color\": \"#dcdcdc\",\n                \"font-bold\": False,\n                \"font-italic\": False,\n                \"alignment-horizontal\": \"left\",\n                \"alignment-vertical\": \"top\",\n                \"word-wrap\": False,\n                \"ellide\": True,\n                \"max-lines\": 1\n            },\n            \"table-item-col[0]\": {\n                \"font-size\": 20,\n                \"font-color\": \"#898989\",\n                \"font-bold\": True,\n                \"ellide\": False,\n                \"word-wrap\": True,\n                \"max-lines\": None\n            },\n            \"table-item-col[1]\": {\n                \"font-size\": 40,\n                \"padding-left\": 10\n            },\n            \"#colorbar\": {\n                \"bg-color\": \"#9932CC\"\n            }\n        },\n        \"items\": [{\n            \"type\": \"layer\",\n            \"direction\": 1,\n            \"name\": \"MainLayer\",\n            \"style\": {\n                \"#MainLayer\": {\n                    \"width\": 1094,\n                    \"height\": 1000,\n                    \"margin\": 25,\n                    \"padding\": 0\n                },\n                \"#LeftSide\": {\n                    \"margin-right\": 25\n                }\n            },\n            \"items\": [{\n                \"type\": \"layer\",\n                \"name\": \"LeftSide\",\n                \"items\": [{\n                    \"type\": \"layer\",\n                    \"direction\": 1,\n                    \"style\": {\n                        \"table-item\": {\n                            \"bg-color\": \"transparent\",\n                            \"padding-bottom\": 20\n                        },\n                        \"table-item-col[0]\": {\n                            \"font-size\": 20,\n                            \"font-color\": \"#898989\",\n                            \"alignment-horizontal\": \"right\"\n                        },\n                        \"table-item-col[1]\": {\n                            \"alignment-horizontal\": \"left\",\n                            \"font-bold\": True,\n                            \"font-size\": 40\n                        }\n                    },\n                    \"items\": [{\n                        \"type\": \"table\",\n                        \"values\": [\n                            [\"Show:\", \"{project[name]}\"]\n                        ],\n                        \"style\": {\n                            \"table-item-field[0:0]\": {\n                                \"width\": 150\n                            },\n                            \"table-item-field[0:1]\": {\n                                \"width\": 580\n                            }\n                        }\n                    }, {\n                        \"type\": \"table\",\n                        \"values\": [\n                            [\"Submitting For:\", \"{intent}\"]\n                        ],\n                        \"style\": {\n                            \"table-item-field[0:0]\": {\n                                \"width\": 160\n                            },\n                            \"table-item-field[0:1]\": {\n                                \"width\": 218,\n                                \"alignment-horizontal\": \"right\"\n                            }\n                        }\n                    }]\n                }, {\n                    \"type\": \"rectangle\",\n                    \"style\": {\n                        \"bg-color\": \"#bc1015\",\n                        \"width\": 1108,\n                        \"height\": 5,\n                        \"fill\": True\n                    }\n                }, {\n                    \"type\": \"table\",\n                    \"use_alternate_color\": True,\n                    \"values\": [\n                        [\"Version name:\", \"{version_name}\"],\n                        [\"Date:\", \"{date}\"],\n                        [\"Shot Types:\", \"{shot_type}\"],\n                        [\"Submission Note:\", \"{submission_note}\"]\n                    ],\n                    \"style\": {\n                        \"table-item\": {\n                            \"padding-bottom\": 20\n                        },\n                        \"table-item-field[0:1]\": {\n                            \"font-bold\": True\n                        },\n                        \"table-item-field[3:0]\": {\n                            \"word-wrap\": True,\n                            \"ellide\": True,\n                            \"max-lines\": 4\n                        },\n                        \"table-item-col[0]\": {\n                            \"alignment-horizontal\": \"right\",\n                            \"width\": 150\n                        },\n                        \"table-item-col[1]\": {\n                            \"alignment-horizontal\": \"left\",\n                            \"width\": 958\n                        }\n                    }\n                }]\n            }, {\n                \"type\": \"layer\",\n                \"name\": \"RightSide\",\n                \"items\": [{\n                    \"type\": \"placeholder\",\n                    \"name\": \"thumbnail\",\n                    \"path\": \"{thumbnail_path}\",\n                    \"style\": {\n                        \"width\": 730,\n                        \"height\": 412\n                    }\n                }, {\n                    \"type\": \"placeholder\",\n                    \"name\": \"colorbar\",\n                    \"path\": \"{color_bar_path}\",\n                    \"return_data\": True,\n                    \"style\": {\n                        \"width\": 730,\n                        \"height\": 55\n                    }\n                }, {\n                    \"type\": \"table\",\n                    \"use_alternate_color\": True,\n                    \"values\": [\n                        [\"Vendor:\", \"{vendor}\"],\n                        [\"Shot Name:\", \"{shot_name}\"],\n                        [\"Frames:\", \"{frame_start} - {frame_end} ({duration})\"]\n                    ],\n                    \"style\": {\n                        \"table-item-col[0]\": {\n                            \"alignment-horizontal\": \"left\",\n                            \"width\": 200\n                        },\n                        \"table-item-col[1]\": {\n                            \"alignment-horizontal\": \"right\",\n                            \"width\": 530,\n                            \"font-size\": 30\n                        }\n                    }\n                }]\n            }]\n        }]\n    }}\n\n    api.create_slates(example_fill_data, \"example_HD\", example_presets)\n</code></pre>"},{"location":"autoapi/client/ayon_core/scripts/slates/slate_base/base.html","title":"base","text":""},{"location":"autoapi/client/ayon_core/scripts/slates/slate_base/base.html#client.ayon_core.scripts.slates.slate_base.base.BaseObj","title":"<code>BaseObj</code>","text":"<p>Base Object for slates.</p> Source code in <code>client/ayon_core/scripts/slates/slate_base/base.py</code> <pre><code>class BaseObj:\n    \"\"\"Base Object for slates.\"\"\"\n\n    obj_type = None\n    available_parents = []\n    all_style_keys = [\n        \"font-family\", \"font-size\", \"font-color\", \"font-bold\", \"font-italic\",\n        \"bg-color\", \"bg-alter-color\",\n        \"alignment-horizontal\", \"alignment-vertical\",\n        \"padding\", \"padding-left\", \"padding-right\",\n        \"padding-top\", \"padding-bottom\",\n        \"margin\", \"margin-left\", \"margin-right\",\n        \"margin-top\", \"margin-bottom\", \"width\", \"height\",\n        \"fill\", \"word-wrap\", \"ellide\", \"max-lines\"\n    ]\n    fill_data_regex = r\"{[^}]+}\"\n\n    def __init__(self, parent, style={}, name=None, pos_x=None, pos_y=None):\n        if not self.obj_type:\n            raise NotImplementedError(\n                \"Class don't have set object type &lt;{}&gt;\".format(\n                    self.__class__.__name__\n                )\n            )\n\n        parent_obj_type = None\n        if parent:\n            parent_obj_type = parent.obj_type\n\n        if parent_obj_type not in self.available_parents:\n            expected_parents = \", \".join(self.available_parents)\n            raise Exception((\n                \"Invalid parent &lt;{}&gt; for &lt;{}&gt;. Expected &lt;{}&gt;\"\n            ).format(\n                parent.__class__.__name__, self.obj_type, expected_parents\n            ))\n\n        self.parent = parent\n        self._style = style\n\n        self.id = uuid4()\n        self.name = name\n        self.items = {}\n\n        self._pos_x = pos_x or 0\n        self._pos_y = pos_y or 0\n\n        log_parts = []\n        module = self.__class__.__module__\n        if module and module != \"__main__\":\n            log_parts.append(module)\n        log_parts.append(self.__class__.__name__)\n        self.log = logging.getLogger(\".\".join(log_parts))\n\n        if parent:\n            parent.add_item(self)\n\n    def fill_data_format(self):\n        return\n\n    @property\n    def fill_data(self):\n        return self.parent.fill_data\n\n    @property\n    def main_style(self):\n        return load_default_style()\n\n    def collect_data(self):\n        return None\n\n    def find_item(self, obj_type=None, name=None):\n        obj_type_fits = False\n        name_fits = False\n        if obj_type is None or self.obj_type == obj_type:\n            obj_type_fits = True\n\n        if name is None or self.name != name:\n            name_fits = True\n\n        output = []\n        if obj_type_fits and name_fits:\n            output.append(self)\n\n        if not self.items:\n            return output\n\n        for item in self.items.values():\n            output.extend(\n                item.find_item(obj_type=obj_type, name=name)\n            )\n        return output\n\n    @property\n    def full_style(self):\n        if self.parent is not None:\n            style = dict(val for val in self.parent.full_style.items())\n        else:\n            style = self.main_style\n\n        for key, value in self._style.items():\n            if key in self.all_style_keys:\n                # TODO which variant is right?\n                style[self.obj_type][key] = value\n                # style[\"*\"][key] = value\n            else:\n                if key not in style:\n                    style[key] = {}\n\n                if isinstance(style[key], dict):\n                    style[key].update(value)\n                else:\n                    style[key] = value\n\n        return style\n\n    def get_style_for_obj_type(self, obj_type, style=None):\n        if not style:\n            style = copy.deepcopy(self.full_style)\n\n        base = style.get(\"*\") or {}\n        obj_specific = style.get(obj_type) or {}\n        name_specific = {}\n        if self.name:\n            name = str(self.name)\n            if not name.startswith(\"#\"):\n                name = \"#\" + name\n            name_specific = style.get(name) or {}\n\n        if obj_type == \"table-item\":\n            col_regex = r\"table-item-col\\[([\\d\\-, ]+)*\\]\"\n            row_regex = r\"table-item-row\\[([\\d\\-, ]+)*\\]\"\n            field_regex = (\n                r\"table-item-field\\[(([ ]+)?\\d+([ ]+)?:([ ]+)?\\d+([ ]+)?)*\\]\"\n            )\n            # STRICT field regex (not allowed spaces)\n            # fild_regex = r\"table-item-field\\[(\\d+:\\d+)*\\]\"\n\n            def get_indexes_from_regex_match(result, field=False):\n                group = result.group(1)\n                indexes = []\n                if field:\n                    return [\n                        int(part.strip()) for part in group.strip().split(\":\")\n                    ]\n\n                parts = group.strip().split(\",\")\n                for part in parts:\n                    part = part.strip()\n                    if \"-\" not in part:\n                        indexes.append(int(part))\n                        continue\n\n                    sub_parts = [\n                        int(sub.strip()) for sub in part.split(\"-\")\n                    ]\n                    if len(sub_parts) != 2:\n                        # TODO logging\n                        self.log.warning(\"Invalid range '{}'\".format(part))\n                        continue\n\n                    for idx in range(sub_parts[0], sub_parts[1] + 1):\n                        indexes.append(idx)\n                return indexes\n\n            for key, value in style.items():\n                if not key.startswith(obj_type):\n                    continue\n\n                result = re.search(col_regex, key)\n                if result:\n                    indexes = get_indexes_from_regex_match(result)\n                    if self.col_idx in indexes:\n                        obj_specific.update(value)\n                    continue\n\n                result = re.search(row_regex, key)\n                if result:\n                    indexes = get_indexes_from_regex_match(result)\n                    if self.row_idx in indexes:\n                        obj_specific.update(value)\n                    continue\n\n                result = re.search(field_regex, key)\n                if result:\n                    row_idx, col_idx = get_indexes_from_regex_match(\n                        result, True\n                    )\n                    if self.col_idx == col_idx and self.row_idx == row_idx:\n                        obj_specific.update(value)\n\n        output = {}\n        output.update(base)\n        output.update(obj_specific)\n        output.update(name_specific)\n\n        return output\n\n    @property\n    def style(self):\n        return self.get_style_for_obj_type(self.obj_type)\n\n    @property\n    def item_pos_x(self):\n        if self.parent.obj_type == \"main_frame\":\n            return int(self._pos_x)\n        return 0\n\n    @property\n    def item_pos_y(self):\n        if self.parent.obj_type == \"main_frame\":\n            return int(self._pos_y)\n        return 0\n\n    @property\n    def content_pos_x(self):\n        pos_x = self.item_pos_x\n        margin = self.style[\"margin\"]\n        margin_left = self.style.get(\"margin-left\") or margin\n\n        pos_x += margin_left\n\n        return pos_x\n\n    @property\n    def content_pos_y(self):\n        pos_y = self.item_pos_y\n        margin = self.style[\"margin\"]\n        margin_top = self.style.get(\"margin-top\") or margin\n        return pos_y + margin_top\n\n    @property\n    def value_pos_x(self):\n        pos_x = int(self.content_pos_x)\n        padding = self.style[\"padding\"]\n        padding_left = self.style.get(\"padding-left\")\n        if padding_left is None:\n            padding_left = padding\n\n        pos_x += padding_left\n\n        return pos_x\n\n    @property\n    def value_pos_y(self):\n        pos_y = int(self.content_pos_y)\n        padding = self.style[\"padding\"]\n        padding_top = self.style.get(\"padding-top\")\n        if padding_top is None:\n            padding_top = padding\n\n        pos_y += padding_top\n\n        return pos_y\n\n    @property\n    def value_pos_start(self):\n        return (self.value_pos_x, self.value_pos_y)\n\n    @property\n    def value_pos_end(self):\n        pos_x, pos_y = self.value_pos_start\n        pos_x += self.width()\n        pos_y += self.height()\n        return (pos_x, pos_y)\n\n    @property\n    def content_pos_start(self):\n        return (self.content_pos_x, self.content_pos_y)\n\n    @property\n    def content_pos_end(self):\n        pos_x, pos_y = self.content_pos_start\n        pos_x += self.content_width()\n        pos_y += self.content_height()\n        return (pos_x, pos_y)\n\n    def value_width(self):\n        raise NotImplementedError(\n            \"Attribute &lt;content_width&gt; is not implemented &lt;{}&gt;\".format(\n                self.__class__.__name__\n            )\n        )\n\n    def value_height(self):\n        raise NotImplementedError(\n            \"Attribute &lt;content_width&gt; is not implemented for &lt;{}&gt;\".format(\n                self.__class__.__name__\n            )\n        )\n\n    def content_width(self):\n        width = self.value_width()\n        padding = self.style[\"padding\"]\n        padding_left = self.style.get(\"padding-left\")\n        if padding_left is None:\n            padding_left = padding\n\n        padding_right = self.style.get(\"padding-right\")\n        if padding_right is None:\n            padding_right = padding\n\n        return width + padding_left + padding_right\n\n    def content_height(self):\n        height = self.value_height()\n        padding = self.style[\"padding\"]\n        padding_top = self.style.get(\"padding-top\")\n        if padding_top is None:\n            padding_top = padding\n\n        padding_bottom = self.style.get(\"padding-bottom\")\n        if padding_bottom is None:\n            padding_bottom = padding\n\n        return height + padding_top + padding_bottom\n\n    def width(self):\n        width = self.content_width()\n\n        margin = self.style[\"margin\"]\n        margin_left = self.style.get(\"margin-left\") or margin\n        margin_right = self.style.get(\"margin-right\") or margin\n\n        return width + margin_left + margin_right\n\n    def height(self):\n        height = self.content_height()\n\n        margin = self.style[\"margin\"]\n        margin_top = self.style.get(\"margin-top\") or margin\n        margin_bottom = self.style.get(\"margin-bottom\") or margin\n\n        return height + margin_bottom + margin_top\n\n    def add_item(self, item):\n        self.items[item.id] = item\n        item.fill_data_format()\n\n    def reset(self):\n        for item in self.items.values():\n            item.reset()\n</code></pre>"},{"location":"autoapi/client/ayon_core/scripts/slates/slate_base/example.html","title":"example","text":""},{"location":"autoapi/client/ayon_core/scripts/slates/slate_base/example.html#client.ayon_core.scripts.slates.slate_base.example.example","title":"<code>example()</code>","text":"<p>Example data to demontrate function.</p> <p>It is required to fill \"destination_path\", \"thumbnail_path\" and \"color_bar_path\" in <code>example_fill_data</code> to be able to execute.</p> Source code in <code>client/ayon_core/scripts/slates/slate_base/example.py</code> <pre><code>def example():\n    \"\"\"Example data to demontrate function.\n\n    It is required to fill \"destination_path\", \"thumbnail_path\"\n    and \"color_bar_path\" in `example_fill_data` to be able to execute.\n    \"\"\"\n\n    example_fill_data = {\n        \"destination_path\": \"PATH/TO/OUTPUT/FILE\",\n        \"project\": {\n            \"name\": \"Testing project\"\n        },\n        \"intent\": \"WIP\",\n        \"version_name\": \"seq01_sh0100_compositing_v01\",\n        \"date\": \"2019-08-09\",\n        \"shot_type\": \"2d comp\",\n        \"submission_note\": (\n            \"Lorem ipsum dolor sit amet, consectetuer adipiscing elit.\"\n            \" Aenean commodo ligula eget dolor. Aenean massa.\"\n            \" Cum sociis natoque penatibus et magnis dis parturient montes,\"\n            \" nascetur ridiculus mus. Donec quam felis, ultricies nec,\"\n            \" pellentesque eu, pretium quis, sem. Nulla consequat massa quis\"\n            \" enim. Donec pede justo, fringilla vel,\"\n            \" aliquet nec, vulputate eget, arcu.\"\n        ),\n        \"thumbnail_path\": \"PATH/TO/THUMBNAIL/FILE\",\n        \"color_bar_path\": \"PATH/TO/COLOR/BAR/FILE\",\n        \"vendor\": \"Our Studio\",\n        \"shot_name\": \"sh0100\",\n        \"frame_start\": 1001,\n        \"frame_end\": 1004,\n        \"duration\": 3\n    }\n\n    example_presets = {\"example_HD\": {\n        \"width\": 1920,\n        \"height\": 1080,\n        \"destination_path\": \"{destination_path}\",\n        \"style\": {\n            \"*\": {\n                \"font-family\": \"arial\",\n                \"font-color\": \"#ffffff\",\n                \"font-bold\": False,\n                \"font-italic\": False,\n                \"bg-color\": \"#0077ff\",\n                \"alignment-horizontal\": \"left\",\n                \"alignment-vertical\": \"top\"\n            },\n            \"layer\": {\n                \"padding\": 0,\n                \"margin\": 0\n            },\n            \"rectangle\": {\n                \"padding\": 0,\n                \"margin\": 0,\n                \"bg-color\": \"#E9324B\",\n                \"fill\": True\n            },\n            \"main_frame\": {\n                \"padding\": 0,\n                \"margin\": 0,\n                \"bg-color\": \"#252525\"\n            },\n            \"table\": {\n                \"padding\": 0,\n                \"margin\": 0,\n                \"bg-color\": \"transparent\"\n            },\n            \"table-item\": {\n                \"padding\": 5,\n                \"padding-bottom\": 10,\n                \"margin\": 0,\n                \"bg-color\": \"#212121\",\n                \"bg-alter-color\": \"#272727\",\n                \"font-color\": \"#dcdcdc\",\n                \"font-bold\": False,\n                \"font-italic\": False,\n                \"alignment-horizontal\": \"left\",\n                \"alignment-vertical\": \"top\",\n                \"word-wrap\": False,\n                \"ellide\": True,\n                \"max-lines\": 1\n            },\n            \"table-item-col[0]\": {\n                \"font-size\": 20,\n                \"font-color\": \"#898989\",\n                \"font-bold\": True,\n                \"ellide\": False,\n                \"word-wrap\": True,\n                \"max-lines\": None\n            },\n            \"table-item-col[1]\": {\n                \"font-size\": 40,\n                \"padding-left\": 10\n            },\n            \"#colorbar\": {\n                \"bg-color\": \"#9932CC\"\n            }\n        },\n        \"items\": [{\n            \"type\": \"layer\",\n            \"direction\": 1,\n            \"name\": \"MainLayer\",\n            \"style\": {\n                \"#MainLayer\": {\n                    \"width\": 1094,\n                    \"height\": 1000,\n                    \"margin\": 25,\n                    \"padding\": 0\n                },\n                \"#LeftSide\": {\n                    \"margin-right\": 25\n                }\n            },\n            \"items\": [{\n                \"type\": \"layer\",\n                \"name\": \"LeftSide\",\n                \"items\": [{\n                    \"type\": \"layer\",\n                    \"direction\": 1,\n                    \"style\": {\n                        \"table-item\": {\n                            \"bg-color\": \"transparent\",\n                            \"padding-bottom\": 20\n                        },\n                        \"table-item-col[0]\": {\n                            \"font-size\": 20,\n                            \"font-color\": \"#898989\",\n                            \"alignment-horizontal\": \"right\"\n                        },\n                        \"table-item-col[1]\": {\n                            \"alignment-horizontal\": \"left\",\n                            \"font-bold\": True,\n                            \"font-size\": 40\n                        }\n                    },\n                    \"items\": [{\n                        \"type\": \"table\",\n                        \"values\": [\n                            [\"Show:\", \"{project[name]}\"]\n                        ],\n                        \"style\": {\n                            \"table-item-field[0:0]\": {\n                                \"width\": 150\n                            },\n                            \"table-item-field[0:1]\": {\n                                \"width\": 580\n                            }\n                        }\n                    }, {\n                        \"type\": \"table\",\n                        \"values\": [\n                            [\"Submitting For:\", \"{intent}\"]\n                        ],\n                        \"style\": {\n                            \"table-item-field[0:0]\": {\n                                \"width\": 160\n                            },\n                            \"table-item-field[0:1]\": {\n                                \"width\": 218,\n                                \"alignment-horizontal\": \"right\"\n                            }\n                        }\n                    }]\n                }, {\n                    \"type\": \"rectangle\",\n                    \"style\": {\n                        \"bg-color\": \"#bc1015\",\n                        \"width\": 1108,\n                        \"height\": 5,\n                        \"fill\": True\n                    }\n                }, {\n                    \"type\": \"table\",\n                    \"use_alternate_color\": True,\n                    \"values\": [\n                        [\"Version name:\", \"{version_name}\"],\n                        [\"Date:\", \"{date}\"],\n                        [\"Shot Types:\", \"{shot_type}\"],\n                        [\"Submission Note:\", \"{submission_note}\"]\n                    ],\n                    \"style\": {\n                        \"table-item\": {\n                            \"padding-bottom\": 20\n                        },\n                        \"table-item-field[0:1]\": {\n                            \"font-bold\": True\n                        },\n                        \"table-item-field[3:0]\": {\n                            \"word-wrap\": True,\n                            \"ellide\": True,\n                            \"max-lines\": 4\n                        },\n                        \"table-item-col[0]\": {\n                            \"alignment-horizontal\": \"right\",\n                            \"width\": 150\n                        },\n                        \"table-item-col[1]\": {\n                            \"alignment-horizontal\": \"left\",\n                            \"width\": 958\n                        }\n                    }\n                }]\n            }, {\n                \"type\": \"layer\",\n                \"name\": \"RightSide\",\n                \"items\": [{\n                    \"type\": \"placeholder\",\n                    \"name\": \"thumbnail\",\n                    \"path\": \"{thumbnail_path}\",\n                    \"style\": {\n                        \"width\": 730,\n                        \"height\": 412\n                    }\n                }, {\n                    \"type\": \"placeholder\",\n                    \"name\": \"colorbar\",\n                    \"path\": \"{color_bar_path}\",\n                    \"return_data\": True,\n                    \"style\": {\n                        \"width\": 730,\n                        \"height\": 55\n                    }\n                }, {\n                    \"type\": \"table\",\n                    \"use_alternate_color\": True,\n                    \"values\": [\n                        [\"Vendor:\", \"{vendor}\"],\n                        [\"Shot Name:\", \"{shot_name}\"],\n                        [\"Frames:\", \"{frame_start} - {frame_end} ({duration})\"]\n                    ],\n                    \"style\": {\n                        \"table-item-col[0]\": {\n                            \"alignment-horizontal\": \"left\",\n                            \"width\": 200\n                        },\n                        \"table-item-col[1]\": {\n                            \"alignment-horizontal\": \"right\",\n                            \"width\": 530,\n                            \"font-size\": 30\n                        }\n                    }\n                }]\n            }]\n        }]\n    }}\n\n    api.create_slates(example_fill_data, \"example_HD\", example_presets)\n</code></pre>"},{"location":"autoapi/client/ayon_core/scripts/slates/slate_base/font_factory.html","title":"font_factory","text":""},{"location":"autoapi/client/ayon_core/scripts/slates/slate_base/items.html","title":"items","text":""},{"location":"autoapi/client/ayon_core/scripts/slates/slate_base/layer.html","title":"layer","text":""},{"location":"autoapi/client/ayon_core/scripts/slates/slate_base/lib.html","title":"lib","text":""},{"location":"autoapi/client/ayon_core/scripts/slates/slate_base/lib.html#client.ayon_core.scripts.slates.slate_base.lib.create_slates","title":"<code>create_slates(fill_data, slate_name=None, slate_data=None, data_output_json=None)</code>","text":"<p>Implmentation for command line executing.</p> <p>Data for slates are by defaule taken from presets. That requires to enter, <code>slate_name</code>. If <code>slate_data</code> are entered then they are used.</p> <p><code>data_output</code> should be path to json file where data will be collected.</p> Source code in <code>client/ayon_core/scripts/slates/slate_base/lib.py</code> <pre><code>def create_slates(\n    fill_data, slate_name=None, slate_data=None, data_output_json=None\n):\n    \"\"\"Implmentation for command line executing.\n\n    Data for slates are by defaule taken from presets. That requires to enter,\n    `slate_name`. If `slate_data` are entered then they are used.\n\n    `data_output` should be path to json file where data will be collected.\n    \"\"\"\n    if slate_data is None and slate_name is None:\n        raise TypeError(\n            \"`create_slates` expects to enter data for slates or name\"\n            \" of slate preset.\"\n        )\n\n    elif slate_data is None:\n        slate_presets = {}\n        slate_data = slate_presets.get(slate_name)\n        if slate_data is None:\n            raise ValueError(\n                \"Preset name \\\"{}\\\" was not found in slate presets.\".format(\n                    slate_name\n                )\n            )\n\n    missing_keys = []\n    for key in RequiredSlateKeys:\n        if key not in slate_data:\n            missing_keys.append(\"`{}`\".format(key))\n\n    if missing_keys:\n        log.error(\"Slate data of &lt;{}&gt; miss required keys: {}\".format(\n            slate_name, \", \".join(missing_keys)\n        ))\n        return False\n\n    width = slate_data[\"width\"]\n    height = slate_data[\"height\"]\n    dst_path = slate_data[\"destination_path\"]\n    style = slate_data.get(\"style\") or {}\n\n    main = MainFrame(width, height, dst_path, fill_data, style=style)\n\n    load_queue = Queue()\n    for item in slate_data[\"items\"]:\n        load_queue.put((item, main))\n\n    while not load_queue.empty():\n        item_data, parent = load_queue.get()\n\n        item_type = item_data[\"type\"].lower()\n        item_style = item_data.get(\"style\", {})\n        item_name = item_data.get(\"name\")\n\n        pos_x = item_data.get(\"pos_x\")\n        pos_y = item_data.get(\"pos_y\")\n        if parent.obj_type != \"main_frame\":\n            if pos_x or pos_y:\n                # TODO logging\n                log.warning((\n                    \"You have specified `pos_x` and `pos_y` but won't be used.\"\n                    \" Possible only if parent of an item is `main_frame`.\"\n                ))\n            pos_x = None\n            pos_y = None\n\n        kwargs = {\n            \"parent\": parent,\n            \"style\": item_style,\n            \"name\": item_name,\n            \"pos_x\": pos_x,\n            \"pos_y\": pos_y\n        }\n\n        if item_type == \"layer\":\n            direction = item_data.get(\"direction\", 0)\n            item_obj = Layer(direction, **kwargs)\n            for item in item_data.get(\"items\", []):\n                load_queue.put((item, item_obj))\n\n        elif item_type == \"table\":\n            use_alternate_color = item_data.get(\"use_alternate_color\", False)\n            values = item_data.get(\"values\") or []\n            ItemTable(values, use_alternate_color, **kwargs)\n\n        elif item_type == \"image\":\n            path = item_data[\"path\"]\n            ItemImage(path, **kwargs)\n\n        elif item_type == \"rectangle\":\n            ItemRectangle(**kwargs)\n\n        elif item_type == \"placeholder\":\n            path = item_data[\"path\"]\n            ItemPlaceHolder(path, **kwargs)\n\n        else:\n            # TODO logging\n            log.warning(\n                \"Not implemented object type `{}` - skipping\".format(item_type)\n            )\n\n    main.draw()\n    log.debug(\"Slate creation finished\")\n\n    if not data_output_json:\n        return\n\n    if not data_output_json.endswith(\".json\"):\n        raise ValueError(\"Output path must be .json file.\")\n\n    data_output_json_dir = os.path.dirname(data_output_json)\n    if not os.path.exists(data_output_json_dir):\n        log.info(\"Creating folder \\\"{}\\\"\".format(data_output_json_dir))\n        os.makedirs(data_output_json_dir)\n\n    output_data = main.collect_data()\n    with open(data_output_json, \"w\") as json_file:\n        json_file.write(json.dumps(output_data, indent=4))\n\n    log.info(\"Metadata collected in \\\"{}\\\".\".format(data_output_json))\n</code></pre>"},{"location":"autoapi/client/ayon_core/scripts/slates/slate_base/main_frame.html","title":"main_frame","text":""},{"location":"autoapi/client/ayon_core/settings/index.html","title":"settings","text":""},{"location":"autoapi/client/ayon_core/settings/index.html#client.ayon_core.settings.get_ayon_settings","title":"<code>get_ayon_settings(project_name=None)</code>","text":"<p>AYON studio settings.</p> <p>Raw AYON settings values.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>Optional[str]</code> <p>Project name.</p> <code>None</code> <p>Returns:</p> Type Description <p>dict[str, Any]: AYON settings.</p> Source code in <code>client/ayon_core/settings/lib.py</code> <pre><code>def get_ayon_settings(project_name=None):\n    \"\"\"AYON studio settings.\n\n    Raw AYON settings values.\n\n    Args:\n        project_name (Optional[str]): Project name.\n\n    Returns:\n        dict[str, Any]: AYON settings.\n    \"\"\"\n\n    return _AyonSettingsCache.get_value_by_project(project_name)\n</code></pre>"},{"location":"autoapi/client/ayon_core/settings/index.html#client.ayon_core.settings.get_current_project_settings","title":"<code>get_current_project_settings()</code>","text":"<p>DEPRECATE Project settings for current context project.</p> <p>Function requires access to pipeline context which is in     'ayon_core.pipeline'.</p> <p>Returns:</p> Type Description <p>dict[str, Any]: Project settings for current context project.</p> Source code in <code>client/ayon_core/settings/lib.py</code> <pre><code>def get_current_project_settings():\n    \"\"\"DEPRECATE Project settings for current context project.\n\n    Function requires access to pipeline context which is in\n        'ayon_core.pipeline'.\n\n    Returns:\n        dict[str, Any]: Project settings for current context project.\n\n    \"\"\"\n    warnings.warn(\n        \"Used deprecated function 'get_current_project_settings' in\"\n        \" 'ayon_core.settings'. The function was moved to\"\n        \" 'ayon_core.pipeline.context_tools'.\",\n        DeprecationWarning,\n        stacklevel=2\n    )\n    from ayon_core.pipeline.context_tools import get_current_project_settings\n\n    return get_current_project_settings()\n</code></pre>"},{"location":"autoapi/client/ayon_core/settings/index.html#client.ayon_core.settings.get_general_environments","title":"<code>get_general_environments(studio_settings=None)</code>","text":"<p>General studio environment variables.</p> <p>Parameters:</p> Name Type Description Default <code>studio_settings</code> <code>Optional[dict]</code> <p>Pre-queried studio settings.</p> <code>None</code> <p>Returns:</p> Type Description <p>dict[str, Any]: General studio environment variables.</p> Source code in <code>client/ayon_core/settings/lib.py</code> <pre><code>def get_general_environments(studio_settings=None):\n    \"\"\"General studio environment variables.\n\n    Args:\n        studio_settings (Optional[dict]): Pre-queried studio settings.\n\n    Returns:\n        dict[str, Any]: General studio environment variables.\n\n    \"\"\"\n    if studio_settings is None:\n        studio_settings = get_ayon_settings()\n    return json.loads(studio_settings[\"core\"][\"environments\"])\n</code></pre>"},{"location":"autoapi/client/ayon_core/settings/lib.html","title":"lib","text":""},{"location":"autoapi/client/ayon_core/settings/lib.html#client.ayon_core.settings.lib.get_ayon_settings","title":"<code>get_ayon_settings(project_name=None)</code>","text":"<p>AYON studio settings.</p> <p>Raw AYON settings values.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>Optional[str]</code> <p>Project name.</p> <code>None</code> <p>Returns:</p> Type Description <p>dict[str, Any]: AYON settings.</p> Source code in <code>client/ayon_core/settings/lib.py</code> <pre><code>def get_ayon_settings(project_name=None):\n    \"\"\"AYON studio settings.\n\n    Raw AYON settings values.\n\n    Args:\n        project_name (Optional[str]): Project name.\n\n    Returns:\n        dict[str, Any]: AYON settings.\n    \"\"\"\n\n    return _AyonSettingsCache.get_value_by_project(project_name)\n</code></pre>"},{"location":"autoapi/client/ayon_core/settings/lib.html#client.ayon_core.settings.lib.get_current_project_settings","title":"<code>get_current_project_settings()</code>","text":"<p>DEPRECATE Project settings for current context project.</p> <p>Function requires access to pipeline context which is in     'ayon_core.pipeline'.</p> <p>Returns:</p> Type Description <p>dict[str, Any]: Project settings for current context project.</p> Source code in <code>client/ayon_core/settings/lib.py</code> <pre><code>def get_current_project_settings():\n    \"\"\"DEPRECATE Project settings for current context project.\n\n    Function requires access to pipeline context which is in\n        'ayon_core.pipeline'.\n\n    Returns:\n        dict[str, Any]: Project settings for current context project.\n\n    \"\"\"\n    warnings.warn(\n        \"Used deprecated function 'get_current_project_settings' in\"\n        \" 'ayon_core.settings'. The function was moved to\"\n        \" 'ayon_core.pipeline.context_tools'.\",\n        DeprecationWarning,\n        stacklevel=2\n    )\n    from ayon_core.pipeline.context_tools import get_current_project_settings\n\n    return get_current_project_settings()\n</code></pre>"},{"location":"autoapi/client/ayon_core/settings/lib.html#client.ayon_core.settings.lib.get_general_environments","title":"<code>get_general_environments(studio_settings=None)</code>","text":"<p>General studio environment variables.</p> <p>Parameters:</p> Name Type Description Default <code>studio_settings</code> <code>Optional[dict]</code> <p>Pre-queried studio settings.</p> <code>None</code> <p>Returns:</p> Type Description <p>dict[str, Any]: General studio environment variables.</p> Source code in <code>client/ayon_core/settings/lib.py</code> <pre><code>def get_general_environments(studio_settings=None):\n    \"\"\"General studio environment variables.\n\n    Args:\n        studio_settings (Optional[dict]): Pre-queried studio settings.\n\n    Returns:\n        dict[str, Any]: General studio environment variables.\n\n    \"\"\"\n    if studio_settings is None:\n        studio_settings = get_ayon_settings()\n    return json.loads(studio_settings[\"core\"][\"environments\"])\n</code></pre>"},{"location":"autoapi/client/ayon_core/settings/lib.html#client.ayon_core.settings.lib.get_project_environments","title":"<code>get_project_environments(project_name, project_settings=None)</code>","text":"<p>Project environment variables.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>project_settings</code> <code>Optional[dict]</code> <p>Pre-queried project settings.</p> <code>None</code> <p>Returns:</p> Type Description <p>dict[str, Any]: Project environment variables.</p> Source code in <code>client/ayon_core/settings/lib.py</code> <pre><code>def get_project_environments(project_name, project_settings=None):\n    \"\"\"Project environment variables.\n\n    Args:\n        project_name (str): Project name.\n        project_settings (Optional[dict]): Pre-queried project settings.\n\n    Returns:\n        dict[str, Any]: Project environment variables.\n\n    \"\"\"\n    if project_settings is None:\n        project_settings = get_project_settings(project_name)\n    return json.loads(\n        project_settings[\"core\"][\"project_environments\"]\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/style/index.html","title":"style","text":""},{"location":"autoapi/client/ayon_core/style/index.html#client.ayon_core.style.get_app_icon_path","title":"<code>get_app_icon_path()</code>","text":"<p>Path to AYON icon.</p> Source code in <code>client/ayon_core/style/__init__.py</code> <pre><code>def get_app_icon_path():\n    \"\"\"Path to AYON icon.\"\"\"\n    return resources.get_ayon_icon_filepath()\n</code></pre>"},{"location":"autoapi/client/ayon_core/style/index.html#client.ayon_core.style.get_colors_data","title":"<code>get_colors_data()</code>","text":"<p>Only color data from stylesheet data.</p> Source code in <code>client/ayon_core/style/__init__.py</code> <pre><code>def get_colors_data():\n    \"\"\"Only color data from stylesheet data.\"\"\"\n    if _Cache.colors_data is None:\n        data = _get_colors_raw_data()\n        color_data = data.get(\"color\") or {}\n        _Cache.colors_data = color_data\n    return copy.deepcopy(_Cache.colors_data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/style/index.html#client.ayon_core.style.get_default_entity_icon_color","title":"<code>get_default_entity_icon_color()</code>","text":"<p>Default color of entities icons.</p> <p>Color must be possible to parse using QColor.</p> <p>Returns:</p> Name Type Description <code>str</code> <p>Color as a string.</p> Source code in <code>client/ayon_core/style/__init__.py</code> <pre><code>def get_default_entity_icon_color():\n    \"\"\"Default color of entities icons.\n\n    Color must be possible to parse using QColor.\n\n    Returns:\n        str: Color as a string.\n    \"\"\"\n    if _Cache.default_entity_icon_color is None:\n        color_data = get_colors_data()\n        _Cache.default_entity_icon_color = color_data[\"icon-entity-default\"]\n    return _Cache.default_entity_icon_color\n</code></pre>"},{"location":"autoapi/client/ayon_core/style/index.html#client.ayon_core.style.get_default_tools_icon_color","title":"<code>get_default_tools_icon_color()</code>","text":"<p>Default color used in tool icons.</p> <p>Color must be possible to parse using QColor.</p> <p>Returns:</p> Name Type Description <code>str</code> <p>Color as a string.</p> Source code in <code>client/ayon_core/style/__init__.py</code> <pre><code>def get_default_tools_icon_color():\n    \"\"\"Default color used in tool icons.\n\n    Color must be possible to parse using QColor.\n\n    Returns:\n        str: Color as a string.\n    \"\"\"\n    if _Cache.tools_icon_color is None:\n        color_data = get_colors_data()\n        _Cache.tools_icon_color = color_data[\"icon-tools\"]\n    return _Cache.tools_icon_color\n</code></pre>"},{"location":"autoapi/client/ayon_core/style/index.html#client.ayon_core.style.get_deprecated_entity_font_color","title":"<code>get_deprecated_entity_font_color()</code>","text":"<p>Font color for deprecated entities.</p> <p>Color must be possible to parse using QColor.</p> <p>Returns:</p> Name Type Description <code>str</code> <p>Color as a string.</p> Source code in <code>client/ayon_core/style/__init__.py</code> <pre><code>def get_deprecated_entity_font_color():\n    \"\"\"Font color for deprecated entities.\n\n    Color must be possible to parse using QColor.\n\n    Returns:\n        str: Color as a string.\n    \"\"\"\n    if _Cache.deprecated_entity_font_color is None:\n        color_data = get_colors_data()\n        _Cache.deprecated_entity_font_color = (\n            color_data[\"font-entity-deprecated\"]\n        )\n    return _Cache.deprecated_entity_font_color\n</code></pre>"},{"location":"autoapi/client/ayon_core/style/index.html#client.ayon_core.style.get_disabled_entity_icon_color","title":"<code>get_disabled_entity_icon_color()</code>","text":"<p>Default color of entities icons.</p> <p>TODO: Find more suitable function name.</p> <p>Color must be possible to parse using QColor.</p> <p>Returns:</p> Name Type Description <code>str</code> <p>Color as a string.</p> Source code in <code>client/ayon_core/style/__init__.py</code> <pre><code>def get_disabled_entity_icon_color():\n    \"\"\"Default color of entities icons.\n\n    TODO: Find more suitable function name.\n\n    Color must be possible to parse using QColor.\n\n    Returns:\n        str: Color as a string.\n    \"\"\"\n    if _Cache.disabled_entity_icon_color is None:\n        color_data = get_colors_data()\n        _Cache.disabled_entity_icon_color = color_data[\"icon-entity-disabled\"]\n    return _Cache.disabled_entity_icon_color\n</code></pre>"},{"location":"autoapi/client/ayon_core/style/index.html#client.ayon_core.style.get_objected_colors","title":"<code>get_objected_colors(*keys)</code>","text":"<p>Colors parsed from stylesheet data into color definitions.</p> <p>You can pass multiple arguments to get a key from the data dict's colors. Because this functions returns a deep copy of the cached data this allows a much smaller dataset to be copied and thus result in a faster function. It is however a micro-optimization in the area of 0.001s and smaller.</p> For example <p>get_colors_data()           # copy of full colors dict get_colors_data(\"font\") get_colors_data(\"loader\", \"asset-view\")</p> <p>Parameters:</p> Name Type Description Default <code>*keys</code> <p>Each key argument will return a key nested deeper in the objected colors data.</p> <code>()</code> <p>Returns:</p> Name Type Description <code>Any</code> <p>Parsed color objects by keys in data.</p> Source code in <code>client/ayon_core/style/__init__.py</code> <pre><code>def get_objected_colors(*keys):\n    \"\"\"Colors parsed from stylesheet data into color definitions.\n\n    You can pass multiple arguments to get a key from the data dict's colors.\n    Because this functions returns a deep copy of the cached data this allows\n    a much smaller dataset to be copied and thus result in a faster function.\n    It is however a micro-optimization in the area of 0.001s and smaller.\n\n    For example:\n        &gt;&gt;&gt; get_colors_data()           # copy of full colors dict\n        &gt;&gt;&gt; get_colors_data(\"font\")\n        &gt;&gt;&gt; get_colors_data(\"loader\", \"asset-view\")\n\n    Args:\n        *keys: Each key argument will return a key nested deeper in the\n            objected colors data.\n\n    Returns:\n        Any: Parsed color objects by keys in data.\n    \"\"\"\n    if _Cache.objected_colors is None:\n        colors_data = get_colors_data()\n        output = {}\n        for key, value in colors_data.items():\n            output[key] = _convert_color_values_to_objects(value)\n\n        _Cache.objected_colors = output\n\n    output = _Cache.objected_colors\n    for key in keys:\n        output = output[key]\n    return copy.deepcopy(output)\n</code></pre>"},{"location":"autoapi/client/ayon_core/style/index.html#client.ayon_core.style.load_stylesheet","title":"<code>load_stylesheet()</code>","text":"<p>Load and return AYON Qt stylesheet.</p> Source code in <code>client/ayon_core/style/__init__.py</code> <pre><code>def load_stylesheet():\n    \"\"\"Load and return AYON Qt stylesheet.\"\"\"\n\n    if _Cache.stylesheet is None:\n        _Cache.stylesheet = _load_stylesheet()\n    _load_font()\n    return _Cache.stylesheet\n</code></pre>"},{"location":"autoapi/client/ayon_core/style/color_defs.html","title":"color_defs","text":"<p>Color definitions that can be used to parse strings for stylesheet.</p> <p>Each definition must have available method <code>get_qcolor</code> which should return <code>QtGui.QColor</code> representation of the color.</p>"},{"location":"autoapi/client/ayon_core/style/color_defs.html#client.ayon_core.style.color_defs--todo-create-abstract-class-to-force-this-method-implementation","title":"TODO create abstract class to force this method implementation","text":"<p>Usage: Some colors may be not be used only in stylesheet but is required to use them in code too. To not hardcode these color values into code it is better to use same colors that are available fro stylesheets.</p> <p>It is possible that some colors may not be used in stylesheet at all and thei definition is used only in code.</p>"},{"location":"autoapi/client/ayon_core/style/color_defs.html#client.ayon_core.style.color_defs.HEXColor","title":"<code>HEXColor</code>","text":"<p>Hex color definition.</p> <p>Hex color is defined by '#' and 3 or 6 hex values (0-F).</p> <p>Examples:</p> <p>\"#fff\" \"#f3f3f3\"</p> Source code in <code>client/ayon_core/style/color_defs.py</code> <pre><code>class HEXColor:\n    \"\"\"Hex color definition.\n\n    Hex color is defined by '#' and 3 or 6 hex values (0-F).\n\n    Examples:\n        \"#fff\"\n        \"#f3f3f3\"\n    \"\"\"\n    regex = re.compile(r\"[a-fA-F0-9]{3}(?:[a-fA-F0-9]{3})?$\")\n\n    def __init__(self, color_string):\n        red, green, blue = self.hex_to_rgb(color_string)\n\n        self._color_string = color_string\n        self._red = red\n        self._green = green\n        self._blue = blue\n\n    @property\n    def red(self):\n        return self._red\n\n    @property\n    def green(self):\n        return self._green\n\n    @property\n    def blue(self):\n        return self._blue\n\n    def to_stylesheet_str(self):\n        return self._color_string\n\n    @classmethod\n    def hex_to_rgb(cls, value):\n        \"\"\"Convert hex value to rgb.\"\"\"\n        hex_value = value.lstrip(\"#\")\n        if not cls.regex.match(hex_value):\n            raise ValueError(\"\\\"{}\\\" is not a valid HEX code.\".format(value))\n\n        output = []\n        if len(hex_value) == 3:\n            for char in hex_value:\n                output.append(int(char * 2, 16))\n        else:\n            for idx in range(3):\n                start_idx = idx * 2\n                output.append(int(hex_value[start_idx:start_idx + 2], 16))\n        return output\n\n    def get_qcolor(self):\n        return create_qcolor(self.red, self.green, self.blue)\n</code></pre>"},{"location":"autoapi/client/ayon_core/style/color_defs.html#client.ayon_core.style.color_defs.HEXColor.hex_to_rgb","title":"<code>hex_to_rgb(value)</code>  <code>classmethod</code>","text":"<p>Convert hex value to rgb.</p> Source code in <code>client/ayon_core/style/color_defs.py</code> <pre><code>@classmethod\ndef hex_to_rgb(cls, value):\n    \"\"\"Convert hex value to rgb.\"\"\"\n    hex_value = value.lstrip(\"#\")\n    if not cls.regex.match(hex_value):\n        raise ValueError(\"\\\"{}\\\" is not a valid HEX code.\".format(value))\n\n    output = []\n    if len(hex_value) == 3:\n        for char in hex_value:\n            output.append(int(char * 2, 16))\n    else:\n        for idx in range(3):\n            start_idx = idx * 2\n            output.append(int(hex_value[start_idx:start_idx + 2], 16))\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/style/color_defs.html#client.ayon_core.style.color_defs.HSLAColor","title":"<code>HSLAColor</code>","text":"<p>Color defined by hue, saturation, light and alpha values.</p> <p>Hue is defined as integer in rage 0-360. Saturation and light can be defined as float (0-1 range) or percent value(0-100%). And alpha as float (0-1 range).</p> <p>Examples:</p> <p>\"hsla(27, 0.7, 0.3, 0.5)\" \"hsla(27, 70%, 30%, 0.5)\"</p> Source code in <code>client/ayon_core/style/color_defs.py</code> <pre><code>class HSLAColor:\n    \"\"\"Color defined by hue, saturation, light and alpha values.\n\n    Hue is defined as integer in rage 0-360. Saturation and light can be\n    defined as float (0-1 range) or percent value(0-100%). And alpha\n    as float (0-1 range).\n\n    Examples:\n        \"hsla(27, 0.7, 0.3, 0.5)\"\n        \"hsla(27, 70%, 30%, 0.5)\"\n    \"\"\"\n    def __init__(self, value):\n        modified_color = value.lower().strip()\n        content = modified_color.rstrip(\")\").lstrip(\"hsla(\")\n        hue_str, sat_str, light_str, alpha_str = (\n            item.strip() for item in content.split(\",\")\n        )\n        hue = int(hue_str) % 360\n        if \"%\" in sat_str:\n            sat = float(sat_str.rstrip(\"%\")) / 100\n        else:\n            sat = float(sat_str)\n\n        if \"%\" in light_str:\n            light = float(light_str.rstrip(\"%\")) / 100\n        else:\n            light = float(light_str)\n\n        alpha = float(alpha_str)\n\n        int_validation(hue, 0, 360)\n        float_validation(sat, 0, 1)\n        float_validation(light, 0, 1)\n        float_validation(alpha, 0, 1)\n\n        self._hue = hue\n        self._saturation = sat\n        self._light = light\n        self._alpha = alpha\n\n    @property\n    def hue(self):\n        return self._hue\n\n    @property\n    def saturation(self):\n        return self._saturation\n\n    @property\n    def light(self):\n        return self._light\n\n    @property\n    def alpha(self):\n        return self._alpha\n\n    def get_qcolor(self):\n        color = create_qcolor()\n        color.setHslF(self.hue / 360, self.saturation, self.light, self.alpha)\n        return color\n</code></pre>"},{"location":"autoapi/client/ayon_core/style/color_defs.html#client.ayon_core.style.color_defs.HSLColor","title":"<code>HSLColor</code>","text":"<p>Color defined by hue, saturation and light values.</p> <p>Hue is defined as integer in rage 0-360. Saturation and light can be defined as float or percent value.</p> <p>Examples:</p> <p>\"hsl(27, 0.7, 0.3)\" \"hsl(27, 70%, 30%)\"</p> Source code in <code>client/ayon_core/style/color_defs.py</code> <pre><code>class HSLColor:\n    \"\"\"Color defined by hue, saturation and light values.\n\n    Hue is defined as integer in rage 0-360. Saturation and light can be\n    defined as float or percent value.\n\n    Examples:\n        \"hsl(27, 0.7, 0.3)\"\n        \"hsl(27, 70%, 30%)\"\n    \"\"\"\n    def __init__(self, value):\n        modified_color = value.lower().strip()\n        content = modified_color.rstrip(\")\").lstrip(\"hsl(\")\n        hue_str, sat_str, light_str = (\n            item.strip() for item in content.split(\",\")\n        )\n        hue = int(hue_str) % 360\n        if \"%\" in sat_str:\n            sat = float(sat_str.rstrip(\"%\")) / 100\n        else:\n            sat = float(sat_str)\n\n        if \"%\" in light_str:\n            light = float(light_str.rstrip(\"%\")) / 100\n        else:\n            light = float(light_str)\n\n        int_validation(hue, 0, 360)\n        float_validation(sat, 0, 1)\n        float_validation(light, 0, 1)\n\n        self._hue = hue\n        self._saturation = sat\n        self._light = light\n\n    @property\n    def hue(self):\n        return self._hue\n\n    @property\n    def saturation(self):\n        return self._saturation\n\n    @property\n    def light(self):\n        return self._light\n\n    def get_qcolor(self):\n        color = create_qcolor()\n        color.setHslF(self.hue / 360, self.saturation, self.light)\n        return color\n</code></pre>"},{"location":"autoapi/client/ayon_core/style/color_defs.html#client.ayon_core.style.color_defs.RGBAColor","title":"<code>RGBAColor</code>","text":"<p>Color defined by red green, blue and alpha values.</p> <p>Each color has possible integer range 0-255.</p> <p>Examples:</p> <p>\"rgba(255, 127, 0, 127)\"</p> Source code in <code>client/ayon_core/style/color_defs.py</code> <pre><code>class RGBAColor:\n    \"\"\"Color defined by red green, blue and alpha values.\n\n    Each color has possible integer range 0-255.\n\n    Examples:\n        \"rgba(255, 127, 0, 127)\"\n    \"\"\"\n    def __init__(self, value):\n        modified_color = value.lower().strip()\n        content = modified_color.rstrip(\")\").lstrip(\"rgba(\")\n        red_str, green_str, blue_str, alpha_str = (\n            item.strip() for item in content.split(\",\")\n        )\n        red = int(red_str)\n        green = int(green_str)\n        blue = int(blue_str)\n        if \".\" in alpha_str:\n            alpha = int(float(alpha_str) * 100)\n        else:\n            alpha = int(alpha_str)\n\n        int_validation(red, 0, 255)\n        int_validation(green, 0, 255)\n        int_validation(blue, 0, 255)\n        int_validation(alpha, 0, 255)\n\n        self._red = red\n        self._green = green\n        self._blue = blue\n        self._alpha = alpha\n\n    @property\n    def red(self):\n        return self._red\n\n    @property\n    def green(self):\n        return self._green\n\n    @property\n    def blue(self):\n        return self._blue\n\n    @property\n    def alpha(self):\n        return self._alpha\n\n    def get_qcolor(self):\n        return create_qcolor(self.red, self.green, self.blue, self.alpha)\n</code></pre>"},{"location":"autoapi/client/ayon_core/style/color_defs.html#client.ayon_core.style.color_defs.RGBColor","title":"<code>RGBColor</code>","text":"<p>Color defined by red green and blue values.</p> <p>Each color has possible integer range 0-255.</p> <p>Examples:</p> <p>\"rgb(255, 127, 0)\"</p> Source code in <code>client/ayon_core/style/color_defs.py</code> <pre><code>class RGBColor:\n    \"\"\"Color defined by red green and blue values.\n\n    Each color has possible integer range 0-255.\n\n    Examples:\n        \"rgb(255, 127, 0)\"\n    \"\"\"\n    def __init__(self, value):\n        modified_color = value.lower().strip()\n        content = modified_color.rstrip(\")\").lstrip(\"rgb(\")\n        red_str, green_str, blue_str = (\n            item.strip() for item in content.split(\",\")\n        )\n        red = int(red_str)\n        green = int(green_str)\n        blue = int(blue_str)\n\n        int_validation(red, 0, 255)\n        int_validation(green, 0, 255)\n        int_validation(blue, 0, 255)\n\n        self._red = red\n        self._green = green\n        self._blue = blue\n\n    @property\n    def red(self):\n        return self._red\n\n    @property\n    def green(self):\n        return self._green\n\n    @property\n    def blue(self):\n        return self._blue\n\n    def get_qcolor(self):\n        return create_qcolor(self.red, self.green, self.blue)\n</code></pre>"},{"location":"autoapi/client/ayon_core/style/color_defs.html#client.ayon_core.style.color_defs.UnknownColor","title":"<code>UnknownColor</code>","text":"<p>Color from stylesheet data without known color definition.</p> <p>This is backup for unknown color definitions which may be for example constants or definition not yet defined by class.</p> Source code in <code>client/ayon_core/style/color_defs.py</code> <pre><code>class UnknownColor:\n    \"\"\"Color from stylesheet data without known color definition.\n\n    This is backup for unknown color definitions which may be for example\n    constants or definition not yet defined by class.\n    \"\"\"\n    def __init__(self, value):\n        self.value = value\n\n    def get_qcolor(self):\n        return create_qcolor(self.value)\n</code></pre>"},{"location":"autoapi/client/ayon_core/style/color_defs.html#client.ayon_core.style.color_defs.create_qcolor","title":"<code>create_qcolor(*args)</code>","text":"<p>Create QtGui.QColor object.</p> <p>Parameters:</p> Name Type Description Default <code>*args</code> <code>tuple</code> <p>It is possible to pass initialization arguments for Qcolor.</p> <code>()</code> Source code in <code>client/ayon_core/style/color_defs.py</code> <pre><code>def create_qcolor(*args):\n    \"\"\"Create QtGui.QColor object.\n\n    Args:\n        *args (tuple): It is possible to pass initialization arguments for\n            Qcolor.\n    \"\"\"\n    from qtpy import QtGui\n\n    return QtGui.QColor(*args)\n</code></pre>"},{"location":"autoapi/client/ayon_core/style/color_defs.html#client.ayon_core.style.color_defs.float_validation","title":"<code>float_validation(value, min_value=None, max_value=None)</code>","text":"<p>Validation of float value within range.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>float</code> <p>Validated value.</p> required <code>min_value</code> <code>float</code> <p>Minimum possible value.</p> <code>None</code> <code>max_value</code> <code>float</code> <p>Maximum possible value.</p> <code>None</code> <p>Raises:</p> Type Description <code>TypeError</code> <p>If 'value' is not 'float' type.</p> Source code in <code>client/ayon_core/style/color_defs.py</code> <pre><code>def float_validation(value, min_value=None, max_value=None):\n    \"\"\"Validation of float value within range.\n\n    Args:\n        value (float): Validated value.\n        min_value (float): Minimum possible value.\n        max_value (float): Maximum possible value.\n\n    Raises:\n        TypeError: If 'value' is not 'float' type.\n    \"\"\"\n    if not isinstance(value, float):\n        raise TypeError((\n            \"Invalid type of hue expected 'int' got {}\"\n        ).format(str(type(value))))\n\n    min_max_check(value, min_value, max_value)\n</code></pre>"},{"location":"autoapi/client/ayon_core/style/color_defs.html#client.ayon_core.style.color_defs.int_validation","title":"<code>int_validation(value, min_value=None, max_value=None)</code>","text":"<p>Validation of integer value within range.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>int</code> <p>Validated value.</p> required <code>min_value</code> <code>int</code> <p>Minimum possible value.</p> <code>None</code> <code>max_value</code> <code>int</code> <p>Maximum possible value.</p> <code>None</code> <p>Raises:</p> Type Description <code>TypeError</code> <p>If 'value' is not 'int' type.</p> Source code in <code>client/ayon_core/style/color_defs.py</code> <pre><code>def int_validation(value, min_value=None, max_value=None):\n    \"\"\"Validation of integer value within range.\n\n    Args:\n        value (int): Validated value.\n        min_value (int): Minimum possible value.\n        max_value (int): Maximum possible value.\n\n    Raises:\n        TypeError: If 'value' is not 'int' type.\n    \"\"\"\n    if not isinstance(value, int):\n        raise TypeError((\n            \"Invalid type of hue expected 'int' got {}\"\n        ).format(str(type(value))))\n\n    min_max_check(value, min_value, max_value)\n</code></pre>"},{"location":"autoapi/client/ayon_core/style/color_defs.html#client.ayon_core.style.color_defs.min_max_check","title":"<code>min_max_check(value, min_value, max_value)</code>","text":"<p>Validate number value if is in passed range.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>(int, float)</code> <p>Value which is validated.</p> required <code>min_value</code> <code>(int, float)</code> <p>Minimum possible value. Validation is skipped if passed value is None.</p> required <code>max_value</code> <code>(int, float)</code> <p>Maximum possible value. Validation is skipped if passed value is None.</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>When 'value' is out of specified range.</p> Source code in <code>client/ayon_core/style/color_defs.py</code> <pre><code>def min_max_check(value, min_value, max_value):\n    \"\"\"Validate number value if is in passed range.\n\n    Args:\n        value (int, float): Value which is validated.\n        min_value (int, float): Minimum possible value. Validation is skipped\n            if passed value is None.\n        max_value (int, float): Maximum possible value. Validation is skipped\n            if passed value is None.\n\n    Raises:\n        ValueError: When 'value' is out of specified range.\n    \"\"\"\n    if min_value is not None and value &lt; min_value:\n        raise ValueError(\"Minimum expected value is '{}' got '{}'\".format(\n            min_value, value\n        ))\n\n    if max_value is not None and value &gt; max_value:\n        raise ValueError(\"Maximum expected value is '{}' got '{}'\".format(\n            min_value, value\n        ))\n</code></pre>"},{"location":"autoapi/client/ayon_core/style/color_defs.html#client.ayon_core.style.color_defs.parse_color","title":"<code>parse_color(value)</code>","text":"<p>Parse string value of color to one of objected representation.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>str</code> <p>Color definition usable in stylesheet.</p> required Source code in <code>client/ayon_core/style/color_defs.py</code> <pre><code>def parse_color(value):\n    \"\"\"Parse string value of color to one of objected representation.\n\n    Args:\n        value(str): Color definition usable in stylesheet.\n    \"\"\"\n    modified_value = value.strip().lower()\n    if modified_value.startswith(\"hsla\"):\n        return HSLAColor(value)\n\n    if modified_value.startswith(\"hsl\"):\n        return HSLColor(value)\n\n    if modified_value.startswith(\"#\"):\n        return HEXColor(value)\n\n    if modified_value.startswith(\"rgba\"):\n        return RGBAColor(value)\n\n    if modified_value.startswith(\"rgb\"):\n        return RGBColor(value)\n    return UnknownColor(value)\n</code></pre>"},{"location":"autoapi/client/ayon_core/style/pyqt5_resources.html","title":"pyqt5_resources","text":""},{"location":"autoapi/client/ayon_core/style/pyside2_resources.html","title":"pyside2_resources","text":""},{"location":"autoapi/client/ayon_core/style/pyside6_resources.html","title":"pyside6_resources","text":""},{"location":"autoapi/client/ayon_core/style/qrc_resources.html","title":"qrc_resources","text":""},{"location":"autoapi/client/ayon_core/tests/index.html","title":"tests","text":""},{"location":"autoapi/client/ayon_core/tests/conftest.html","title":"conftest","text":""},{"location":"autoapi/client/ayon_core/tests/plugins/index.html","title":"plugins","text":""},{"location":"autoapi/client/ayon_core/tests/plugins/load/index.html","title":"load","text":""},{"location":"autoapi/client/ayon_core/tests/plugins/load/test_export_otio.html","title":"test_export_otio","text":""},{"location":"autoapi/client/ayon_core/tools/index.html","title":"tools","text":""},{"location":"autoapi/client/ayon_core/tools/flickcharm.html","title":"flickcharm","text":"This based on the flickcharm-python code from <p>https://code.google.com/archive/p/flickcharm-python/</p> Which states <p>This is a Python (PyQt) port of Ariya Hidayat's elegant FlickCharm hack which adds kinetic scrolling to any scrollable Qt widget.</p> <p>Licensed under GNU GPL version 2 or later.</p> <p>It has been altered to fix edge cases where clicks and drags would not propagate correctly under some conditions. It also allows a small \"dead zone\" threshold in which it will still propagate the user pressed click if he or she travelled only very slightly with the cursor.</p>"},{"location":"autoapi/client/ayon_core/tools/flickcharm.html#client.ayon_core.tools.flickcharm.FlickCharm","title":"<code>FlickCharm</code>","text":"<p>               Bases: <code>QObject</code></p> <p>Make scrollable widgets flickable.</p> For example <p>charm = FlickCharm() charm.activateOn(widget)</p> <p>It can <code>activateOn</code> multiple widgets with a single FlickCharm instance. Be aware that the FlickCharm object must be kept around for it not to get garbage collected and losing the flickable behavior.</p> <p>Flick away!</p> Source code in <code>client/ayon_core/tools/flickcharm.py</code> <pre><code>class FlickCharm(QtCore.QObject):\n    \"\"\"Make scrollable widgets flickable.\n\n    For example:\n        charm = FlickCharm()\n        charm.activateOn(widget)\n\n    It can `activateOn` multiple widgets with a single FlickCharm instance.\n    Be aware that the FlickCharm object must be kept around for it not\n    to get garbage collected and losing the flickable behavior.\n\n    Flick away!\n\n    \"\"\"\n\n    def __init__(self, parent=None):\n        super(FlickCharm, self).__init__(parent=parent)\n\n        self.flickData = {}\n        self.ticker = QtCore.QBasicTimer()\n\n        # The flick button to use\n        self.button = QtCore.Qt.LeftButton\n\n        # The time taken per update tick of flicking behavior\n        self.tick_time = 20\n\n        # Allow a item click/press directly when AutoScroll is slower than\n        # this threshold velocity\n        self.click_in_autoscroll_threshold = 10\n\n        # Allow an item click/press to propagate as opposed to scrolling\n        # when the cursor travelled less than this amount of pixels\n        # Note: back &amp; forth motion increases the value too\n        self.travel_threshold = 20\n\n        self.max_speed = 64  # max scroll speed\n        self.drag = 1  # higher drag will stop autoscroll faster\n\n    def activateOn(self, widget):\n        viewport = widget.viewport()\n        viewport.installEventFilter(self)\n        widget.installEventFilter(self)\n        self.flickData[viewport] = FlickData()\n        self.flickData[viewport].widget = widget\n        self.flickData[viewport].state = FlickData.Steady\n\n    def deactivateFrom(self, widget):\n\n        viewport = widget.viewport()\n        viewport.removeEventFilter(self)\n        widget.removeEventFilter(self)\n        self.flickData.pop(viewport)\n\n    def eventFilter(self, obj, event):\n\n        if not obj.isWidgetType():\n            return False\n\n        eventType = event.type()\n        if eventType != QtCore.QEvent.MouseButtonPress and \\\n                eventType != QtCore.QEvent.MouseButtonRelease and \\\n                eventType != QtCore.QEvent.MouseMove:\n            return False\n\n        if event.modifiers() != QtCore.Qt.NoModifier:\n            return False\n\n        if obj not in self.flickData:\n            return False\n\n        data = self.flickData[obj]\n        found, newIgnored = removeAll(data.ignored, event)\n        if found:\n            data.ignored = newIgnored\n            return False\n\n        if data.state == FlickData.Steady:\n            if eventType == QtCore.QEvent.MouseButtonPress:\n                if event.buttons() == self.button:\n                    self._set_press_pos_and_offset(event, data)\n                    data.state = FlickData.Pressed\n                    return True\n\n        elif data.state == FlickData.Pressed:\n            if eventType == QtCore.QEvent.MouseButtonRelease:\n                # User didn't actually scroll but clicked in\n                # the widget. Let the original press and release\n                # event be evaluated on the Widget\n                data.state = FlickData.Steady\n                event1 = QtGui.QMouseEvent(QtCore.QEvent.MouseButtonPress,\n                                           data.pressPos,\n                                           QtCore.Qt.LeftButton,\n                                           QtCore.Qt.LeftButton,\n                                           QtCore.Qt.NoModifier)\n                # Copy the current event\n                event2 = QtGui.QMouseEvent(QtCore.QEvent.MouseButtonRelease,\n                                           event.pos(),\n                                           event.button(),\n                                           event.buttons(),\n                                           event.modifiers())\n                data.ignored.append(event1)\n                data.ignored.append(event2)\n                QtWidgets.QApplication.postEvent(obj, event1)\n                QtWidgets.QApplication.postEvent(obj, event2)\n                return True\n            elif eventType == QtCore.QEvent.MouseMove:\n                data.state = FlickData.ManualScroll\n                data.dragPos = QtGui.QCursor.pos()\n                if not self.ticker.isActive():\n                    self.ticker.start(self.tick_time, self)\n                return True\n\n        elif data.state == FlickData.ManualScroll:\n            if eventType == QtCore.QEvent.MouseMove:\n                pos = event.pos()\n                delta = pos - data.pressPos\n                data.travelled += delta.manhattanLength()\n                setScrollOffset(data.widget, data.offset - delta)\n                return True\n            elif eventType == QtCore.QEvent.MouseButtonRelease:\n\n                if data.travelled &lt;= self.travel_threshold:\n                    # If the user travelled less than the threshold\n                    # don't go into autoscroll mode but assume the user\n                    # intended to click instead\n                    return self._propagate_click(obj, event, data)\n\n                data.state = FlickData.AutoScroll\n                return True\n\n        elif data.state == FlickData.AutoScroll:\n            if eventType == QtCore.QEvent.MouseButtonPress:\n\n                # Allow pressing when auto scroll is already slower than\n                # the click in autoscroll threshold\n                velocity = data.speed.manhattanLength()\n                if velocity &lt;= self.click_in_autoscroll_threshold:\n                    self._set_press_pos_and_offset(event, data)\n                    data.state = FlickData.Pressed\n                else:\n                    data.state = FlickData.Stop\n\n                data.speed = QtCore.QPoint(0, 0)\n                return True\n            elif eventType == QtCore.QEvent.MouseButtonRelease:\n                data.state = FlickData.Steady\n                data.speed = QtCore.QPoint(0, 0)\n                return True\n\n        elif data.state == FlickData.Stop:\n            if eventType == QtCore.QEvent.MouseButtonRelease:\n                data.state = FlickData.Steady\n\n                # If the user had a very limited scroll smaller than the\n                # threshold consider it a regular press and release.\n                if data.travelled &lt; self.travel_threshold:\n                    return self._propagate_click(obj, event, data)\n\n                return True\n            elif eventType == QtCore.QEvent.MouseMove:\n                # Reset the press position and offset to allow us to \"continue\"\n                # the scroll from the new point the user clicked and then held\n                # down to continue scrolling after AutoScroll.\n                self._set_press_pos_and_offset(event, data)\n                data.state = FlickData.ManualScroll\n\n                data.dragPos = QtGui.QCursor.pos()\n                if not self.ticker.isActive():\n                    self.ticker.start(self.tick_time, self)\n                return True\n\n        return False\n\n    def _set_press_pos_and_offset(self, event, data):\n        \"\"\"Store current event position on Press\"\"\"\n        data.state = FlickData.Pressed\n        data.pressPos = copy.copy(event.pos())\n        data.offset = scrollOffset(data.widget)\n        data.travelled = 0\n\n    def _propagate_click(self, obj, event, data):\n        \"\"\"Propagate from Pressed state with MouseButtonRelease event.\n\n        Use only on button release in certain states to propagate a click,\n        for example when the user dragged only a slight distance under the\n        travel threshold.\n\n        \"\"\"\n\n        data.state = FlickData.Pressed\n        data.pressPos = copy.copy(event.pos())\n        data.offset = scrollOffset(data.widget)\n        data.travelled = 0\n        self.eventFilter(obj, event)\n        return True\n\n    def timerEvent(self, event):\n\n        count = 0\n        for data in self.flickData.values():\n            if data.state == FlickData.ManualScroll:\n                count += 1\n                cursorPos = QtGui.QCursor.pos()\n                data.speed = cursorPos - data.dragPos\n                data.dragPos = cursorPos\n            elif data.state == FlickData.AutoScroll:\n                count += 1\n                data.speed = deaccelerate(data.speed,\n                                          a=self.drag,\n                                          maxVal=self.max_speed)\n                p = scrollOffset(data.widget)\n                new_p = p - data.speed\n                setScrollOffset(data.widget, new_p)\n\n                if scrollOffset(data.widget) == p:\n                    # If this scroll resulted in no change on the widget\n                    # we reached the end of the list and set the speed to\n                    # zero.\n                    data.speed = QtCore.QPoint(0, 0)\n\n                if data.speed == QtCore.QPoint(0, 0):\n                    data.state = FlickData.Steady\n\n        if count == 0:\n            self.ticker.stop()\n\n        super(FlickCharm, self).timerEvent(event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/attribute_defs/index.html","title":"attribute_defs","text":""},{"location":"autoapi/client/ayon_core/tools/attribute_defs/index.html#client.ayon_core.tools.attribute_defs.AttributeDefinitionsLabel","title":"<code>AttributeDefinitionsLabel</code>","text":"<p>               Bases: <code>QLabel</code></p> <p>Label related to value attribute definition.</p> <p>Label is used to show attribute definition label and to show if value is overridden.</p> <p>Label can be right-clicked to revert value to default.</p> Source code in <code>client/ayon_core/tools/attribute_defs/widgets.py</code> <pre><code>class AttributeDefinitionsLabel(QtWidgets.QLabel):\n    \"\"\"Label related to value attribute definition.\n\n    Label is used to show attribute definition label and to show if value\n    is overridden.\n\n    Label can be right-clicked to revert value to default.\n    \"\"\"\n    revert_to_default_requested = QtCore.Signal(str)\n\n    def __init__(\n        self,\n        attr_id: str,\n        label: str,\n        parent: QtWidgets.QWidget,\n    ):\n        super().__init__(label, parent)\n\n        self._attr_id = attr_id\n        self._overridden = False\n        self.setContextMenuPolicy(QtCore.Qt.CustomContextMenu)\n\n        self.customContextMenuRequested.connect(self._on_context_menu)\n\n    def set_overridden(self, overridden: bool):\n        if self._overridden == overridden:\n            return\n        self._overridden = overridden\n        set_style_property(\n            self,\n            \"overridden\",\n            \"1\" if overridden else \"\"\n        )\n\n    def _on_context_menu(self, point: QtCore.QPoint):\n        menu = QtWidgets.QMenu(self)\n        action = QtWidgets.QAction(menu)\n        action.setText(REVERT_TO_DEFAULT_LABEL)\n        action.triggered.connect(self._request_revert_to_default)\n        menu.addAction(action)\n        menu.exec_(self.mapToGlobal(point))\n\n    def _request_revert_to_default(self):\n        self.revert_to_default_requested.emit(self._attr_id)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/attribute_defs/index.html#client.ayon_core.tools.attribute_defs.AttributeDefinitionsWidget","title":"<code>AttributeDefinitionsWidget</code>","text":"<p>               Bases: <code>QWidget</code></p> <p>Create widgets for attribute definitions in grid layout.</p> <p>Widget creates input widgets for passed attribute definitions.</p> <p>Widget can't handle multiselection values.</p> Source code in <code>client/ayon_core/tools/attribute_defs/widgets.py</code> <pre><code>class AttributeDefinitionsWidget(QtWidgets.QWidget):\n    \"\"\"Create widgets for attribute definitions in grid layout.\n\n    Widget creates input widgets for passed attribute definitions.\n\n    Widget can't handle multiselection values.\n    \"\"\"\n\n    def __init__(self, attr_defs=None, parent=None):\n        super().__init__(parent)\n\n        self._widgets_by_id = {}\n        self._labels_by_id = {}\n        self._current_keys = set()\n\n        self.set_attr_defs(attr_defs)\n\n    def clear_attr_defs(self):\n        \"\"\"Remove all existing widgets and reset layout if needed.\"\"\"\n        self._widgets_by_id = {}\n        self._labels_by_id = {}\n        self._current_keys = set()\n\n        layout = self.layout()\n        if layout is not None:\n            if layout.count() == 0:\n                return\n\n            while layout.count():\n                item = layout.takeAt(0)\n                widget = item.widget()\n                if widget:\n                    widget.setVisible(False)\n                    widget.deleteLater()\n\n            layout.deleteLater()\n\n        new_layout = QtWidgets.QGridLayout()\n        new_layout.setContentsMargins(0, 0, 0, 0)\n        new_layout.setColumnStretch(0, 0)\n        new_layout.setColumnStretch(1, 1)\n        self.setLayout(new_layout)\n\n    def set_attr_defs(self, attr_defs):\n        \"\"\"Replace current attribute definitions with passed.\"\"\"\n        self.clear_attr_defs()\n        if attr_defs:\n            self.add_attr_defs(attr_defs)\n\n    def add_attr_defs(self, attr_defs):\n        \"\"\"Add attribute definitions to current.\"\"\"\n        layout = self.layout()\n\n        row = 0\n        for attr_def in attr_defs:\n            if attr_def.is_value_def:\n                if attr_def.key in self._current_keys:\n                    raise KeyError(\n                        \"Duplicated key \\\"{}\\\"\".format(attr_def.key))\n\n                self._current_keys.add(attr_def.key)\n            widget = create_widget_for_attr_def(attr_def, self)\n            self._widgets_by_id[attr_def.id] = widget\n\n            if not attr_def.visible:\n                continue\n\n            label = attr_def.label\n            if isinstance(attr_def, UILabelDef):\n                label = None\n\n            col_num = 0\n            if attr_def.is_label_horizontal and (\n                attr_def.is_value_def\n                or isinstance(attr_def, ButtonDef)\n            ):\n                col_num = 1\n            expand_cols = 2 - col_num\n\n            if label:\n                label_widget = AttributeDefinitionsLabel(\n                    attr_def.id, label, self\n                )\n                label_widget.revert_to_default_requested.connect(\n                    self._on_revert_request\n                )\n                self._labels_by_id[attr_def.id] = label_widget\n                tooltip = attr_def.tooltip\n                if tooltip:\n                    label_widget.setToolTip(tooltip)\n                if attr_def.is_label_horizontal:\n                    label_widget.setAlignment(\n                        QtCore.Qt.AlignRight\n                        | QtCore.Qt.AlignVCenter\n                    )\n                layout.addWidget(\n                    label_widget, row, 0, 1, expand_cols\n                )\n                if not attr_def.is_label_horizontal:\n                    row += 1\n\n            if attr_def.is_value_def:\n                widget.value_changed.connect(self._on_value_change)\n\n            layout.addWidget(\n                widget, row, col_num, 1, expand_cols\n            )\n            row += 1\n\n    def set_value(self, value):\n        new_value = copy.deepcopy(value)\n        for widget in self._widgets_by_id.values():\n            attr_def = widget.attr_def\n            if attr_def.key not in new_value:\n                continue\n\n            widget_value = new_value[attr_def.key]\n            if widget_value is None:\n                widget_value = copy.deepcopy(attr_def.default)\n            widget.set_value(widget_value)\n\n    def current_value(self):\n        output = {}\n        for widget in self._widgets_by_id.values():\n            attr_def = widget.attr_def\n            if not isinstance(attr_def, UIDef):\n                output[attr_def.key] = widget.current_value()\n\n        return output\n\n    def _on_revert_request(self, attr_id):\n        widget = self._widgets_by_id.get(attr_id)\n        if widget is not None:\n            widget.set_value(widget.attr_def.default)\n\n    def _on_value_change(self, value, attr_id):\n        widget = self._widgets_by_id.get(attr_id)\n        if widget is None:\n            return\n        label = self._labels_by_id.get(attr_id)\n        if label is not None:\n            label.set_overridden(value != widget.attr_def.default)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/attribute_defs/index.html#client.ayon_core.tools.attribute_defs.AttributeDefinitionsWidget.add_attr_defs","title":"<code>add_attr_defs(attr_defs)</code>","text":"<p>Add attribute definitions to current.</p> Source code in <code>client/ayon_core/tools/attribute_defs/widgets.py</code> <pre><code>def add_attr_defs(self, attr_defs):\n    \"\"\"Add attribute definitions to current.\"\"\"\n    layout = self.layout()\n\n    row = 0\n    for attr_def in attr_defs:\n        if attr_def.is_value_def:\n            if attr_def.key in self._current_keys:\n                raise KeyError(\n                    \"Duplicated key \\\"{}\\\"\".format(attr_def.key))\n\n            self._current_keys.add(attr_def.key)\n        widget = create_widget_for_attr_def(attr_def, self)\n        self._widgets_by_id[attr_def.id] = widget\n\n        if not attr_def.visible:\n            continue\n\n        label = attr_def.label\n        if isinstance(attr_def, UILabelDef):\n            label = None\n\n        col_num = 0\n        if attr_def.is_label_horizontal and (\n            attr_def.is_value_def\n            or isinstance(attr_def, ButtonDef)\n        ):\n            col_num = 1\n        expand_cols = 2 - col_num\n\n        if label:\n            label_widget = AttributeDefinitionsLabel(\n                attr_def.id, label, self\n            )\n            label_widget.revert_to_default_requested.connect(\n                self._on_revert_request\n            )\n            self._labels_by_id[attr_def.id] = label_widget\n            tooltip = attr_def.tooltip\n            if tooltip:\n                label_widget.setToolTip(tooltip)\n            if attr_def.is_label_horizontal:\n                label_widget.setAlignment(\n                    QtCore.Qt.AlignRight\n                    | QtCore.Qt.AlignVCenter\n                )\n            layout.addWidget(\n                label_widget, row, 0, 1, expand_cols\n            )\n            if not attr_def.is_label_horizontal:\n                row += 1\n\n        if attr_def.is_value_def:\n            widget.value_changed.connect(self._on_value_change)\n\n        layout.addWidget(\n            widget, row, col_num, 1, expand_cols\n        )\n        row += 1\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/attribute_defs/index.html#client.ayon_core.tools.attribute_defs.AttributeDefinitionsWidget.clear_attr_defs","title":"<code>clear_attr_defs()</code>","text":"<p>Remove all existing widgets and reset layout if needed.</p> Source code in <code>client/ayon_core/tools/attribute_defs/widgets.py</code> <pre><code>def clear_attr_defs(self):\n    \"\"\"Remove all existing widgets and reset layout if needed.\"\"\"\n    self._widgets_by_id = {}\n    self._labels_by_id = {}\n    self._current_keys = set()\n\n    layout = self.layout()\n    if layout is not None:\n        if layout.count() == 0:\n            return\n\n        while layout.count():\n            item = layout.takeAt(0)\n            widget = item.widget()\n            if widget:\n                widget.setVisible(False)\n                widget.deleteLater()\n\n        layout.deleteLater()\n\n    new_layout = QtWidgets.QGridLayout()\n    new_layout.setContentsMargins(0, 0, 0, 0)\n    new_layout.setColumnStretch(0, 0)\n    new_layout.setColumnStretch(1, 1)\n    self.setLayout(new_layout)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/attribute_defs/index.html#client.ayon_core.tools.attribute_defs.AttributeDefinitionsWidget.set_attr_defs","title":"<code>set_attr_defs(attr_defs)</code>","text":"<p>Replace current attribute definitions with passed.</p> Source code in <code>client/ayon_core/tools/attribute_defs/widgets.py</code> <pre><code>def set_attr_defs(self, attr_defs):\n    \"\"\"Replace current attribute definitions with passed.\"\"\"\n    self.clear_attr_defs()\n    if attr_defs:\n        self.add_attr_defs(attr_defs)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/attribute_defs/_constants.html","title":"_constants","text":""},{"location":"autoapi/client/ayon_core/tools/attribute_defs/dialog.html","title":"dialog","text":""},{"location":"autoapi/client/ayon_core/tools/attribute_defs/files_widget.html","title":"files_widget","text":""},{"location":"autoapi/client/ayon_core/tools/attribute_defs/files_widget.html#client.ayon_core.tools.attribute_defs.files_widget.DropEmpty","title":"<code>DropEmpty</code>","text":"<p>               Bases: <code>QWidget</code></p> Source code in <code>client/ayon_core/tools/attribute_defs/files_widget.py</code> <pre><code>class DropEmpty(QtWidgets.QWidget):\n    _empty_extensions = \"Any file\"\n\n    def __init__(self, single_item, allow_sequences, extensions_label, parent):\n        super(DropEmpty, self).__init__(parent)\n\n        drop_label_widget = QtWidgets.QLabel(\"Drag &amp; Drop files here\", self)\n\n        items_label_widget = SupportLabel(self)\n        items_label_widget.setWordWrap(True)\n\n        layout = QtWidgets.QVBoxLayout(self)\n        layout.setContentsMargins(0, 0, 0, 0)\n        layout.addSpacing(20)\n        layout.addWidget(\n            drop_label_widget, 0, alignment=QtCore.Qt.AlignCenter\n        )\n        layout.addSpacing(30)\n        layout.addStretch(1)\n        layout.addWidget(\n            items_label_widget, 0, alignment=QtCore.Qt.AlignCenter\n        )\n        layout.addSpacing(10)\n\n        for widget in (\n            drop_label_widget,\n            items_label_widget,\n        ):\n            widget.setAlignment(QtCore.Qt.AlignCenter)\n            widget.setAttribute(QtCore.Qt.WA_TranslucentBackground)\n\n        update_size_timer = QtCore.QTimer()\n        update_size_timer.setInterval(10)\n        update_size_timer.setSingleShot(True)\n\n        update_size_timer.timeout.connect(self._on_update_size_timer)\n\n        self._update_size_timer = update_size_timer\n\n        if extensions_label and not extensions_label.startswith(\" \"):\n            extensions_label = \" \" + extensions_label\n\n        self._single_item = single_item\n        self._extensions_label = extensions_label\n        self._allow_sequences = allow_sequences\n        self._allowed_extensions = set()\n        self._allow_folders = None\n\n        self._drop_label_widget = drop_label_widget\n        self._items_label_widget = items_label_widget\n\n        self.set_allow_folders(False)\n\n    def set_extensions(self, extensions):\n        if extensions:\n            extensions = {\n                ext.replace(\".\", \"\")\n                for ext in extensions\n            }\n        if extensions == self._allowed_extensions:\n            return\n        self._allowed_extensions = extensions\n\n        self._update_items_label()\n\n    def set_allow_folders(self, allowed):\n        if self._allow_folders == allowed:\n            return\n\n        self._allow_folders = allowed\n        self._update_items_label()\n\n    def _update_items_label(self):\n        allowed_items = []\n        if self._allow_folders:\n            allowed_items.append(\"folder\")\n\n        if self._allowed_extensions:\n            allowed_items.append(\"file\")\n            if self._allow_sequences:\n                allowed_items.append(\"sequence\")\n\n        if not self._single_item:\n            allowed_items = [item + \"s\" for item in allowed_items]\n\n        if not allowed_items:\n            self._drop_label_widget.setVisible(False)\n            self._items_label_widget.setText(\n                \"It is not allowed to add anything here!\"\n            )\n            return\n\n        self._drop_label_widget.setVisible(True)\n        items_label = \"Multiple \"\n        if self._single_item:\n            items_label = \"Single \"\n\n        if len(allowed_items) == 1:\n            extensions_label = allowed_items[0]\n        elif len(allowed_items) == 2:\n            extensions_label = \" or \".join(allowed_items)\n        else:\n            last_item = allowed_items.pop(-1)\n            new_last_item = \" or \".join([last_item, allowed_items.pop(-1)])\n            allowed_items.append(new_last_item)\n            extensions_label = \", \".join(allowed_items)\n\n        allowed_items_label = extensions_label\n\n        items_label += allowed_items_label\n        label_tooltip = None\n        if self._allowed_extensions:\n            items_label += \" of\\n{}\".format(\n                \", \".join(sorted(self._allowed_extensions))\n            )\n\n        if self._extensions_label:\n            label_tooltip = items_label\n            items_label = self._extensions_label\n\n        if self._items_label_widget.text() == items_label:\n            return\n\n        self._items_label_widget.setToolTip(label_tooltip)\n        self._items_label_widget.setText(items_label)\n        self._update_size_timer.start()\n\n    def resizeEvent(self, event):\n        super(DropEmpty, self).resizeEvent(event)\n        self._update_size_timer.start()\n\n    def _on_update_size_timer(self):\n        \"\"\"Recalculate height of label with extensions.\n\n        Dynamic QLabel with word wrap does not handle properly it's sizeHint\n        calculations on show. This way it is recalculated. It is good practice\n        to trigger this method with small offset using '_update_size_timer'.\n        \"\"\"\n\n        width = self._items_label_widget.width()\n        height = self._items_label_widget.heightForWidth(width)\n        self._items_label_widget.setMinimumHeight(height)\n        self._items_label_widget.updateGeometry()\n\n    def paintEvent(self, event):\n        super(DropEmpty, self).paintEvent(event)\n\n        pen = QtGui.QPen()\n        pen.setBrush(QtCore.Qt.darkGray)\n        pen.setStyle(QtCore.Qt.DashLine)\n        pen.setWidth(1)\n\n        content_margins = self.layout().contentsMargins()\n        rect = self.rect()\n        left_m = content_margins.left() + pen.width()\n        top_m = content_margins.top() + pen.width()\n        new_rect = QtCore.QRect(\n            left_m,\n            top_m,\n            (\n                rect.width()\n                - (left_m + content_margins.right() + pen.width())\n            ),\n            (\n                rect.height()\n                - (top_m + content_margins.bottom() + pen.width())\n            )\n        )\n\n        painter = QtGui.QPainter(self)\n        painter.setRenderHint(QtGui.QPainter.Antialiasing)\n        painter.setPen(pen)\n        painter.drawRect(new_rect)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/attribute_defs/files_widget.html#client.ayon_core.tools.attribute_defs.files_widget.FilesModel","title":"<code>FilesModel</code>","text":"<p>               Bases: <code>QStandardItemModel</code></p> Source code in <code>client/ayon_core/tools/attribute_defs/files_widget.py</code> <pre><code>class FilesModel(QtGui.QStandardItemModel):\n    def __init__(self, single_item, allow_sequences):\n        super(FilesModel, self).__init__()\n\n        self._id = str(uuid.uuid4())\n        self._single_item = single_item\n        self._multivalue = False\n        self._allow_sequences = allow_sequences\n\n        self._items_by_id = {}\n        self._file_items_by_id = {}\n        self._filenames_by_dirpath = collections.defaultdict(set)\n        self._items_by_dirpath = collections.defaultdict(list)\n\n        self.rowsAboutToBeRemoved.connect(self._on_about_to_be_removed)\n        self.rowsInserted.connect(self._on_insert)\n\n    @property\n    def id(self):\n        return self._id\n\n    def _on_about_to_be_removed(self, parent_index, start, end):\n        \"\"\"Make sure that removed items are removed from items mapping.\n\n        Connected with '_on_insert'. When user drag item and drop it to same\n        view the item is actually removed and created again but it happens in\n        inner calls of Qt.\n        \"\"\"\n\n        for row in range(start, end + 1):\n            index = self.index(row, 0, parent_index)\n            item_id = index.data(ITEM_ID_ROLE)\n            if item_id is not None:\n                self._items_by_id.pop(item_id, None)\n\n    def _on_insert(self, parent_index, start, end):\n        \"\"\"Make sure new added items are stored in items mapping.\n\n        Connected to '_on_about_to_be_removed'. Some items are not created\n        using '_create_item' but are recreated using Qt. So the item is not in\n        mapping and if it would it would not lead to same item pointer.\n        \"\"\"\n\n        for row in range(start, end + 1):\n            index = self.index(start, end, parent_index)\n            item_id = index.data(ITEM_ID_ROLE)\n            if item_id not in self._items_by_id:\n                self._items_by_id[item_id] = self.item(row)\n\n    def set_multivalue(self, multivalue):\n        \"\"\"Disable filtering.\"\"\"\n\n        if self._multivalue == multivalue:\n            return\n        self._multivalue = multivalue\n\n    def add_filepaths(self, items):\n        if not items:\n            return\n\n        if self._multivalue:\n            _items = []\n            for item in items:\n                if isinstance(item, (tuple, list, set)):\n                    _items.extend(item)\n                else:\n                    _items.append(item)\n            items = _items\n\n        file_items = FileDefItem.from_value(items, self._allow_sequences)\n        if not file_items:\n            return\n\n        if not self._multivalue and self._single_item:\n            file_items = [file_items[0]]\n            current_ids = list(self._file_items_by_id.keys())\n            if current_ids:\n                self.remove_item_by_ids(current_ids)\n\n        new_model_items = []\n        for file_item in file_items:\n            item_id, model_item = self._create_item(file_item)\n            new_model_items.append(model_item)\n            self._file_items_by_id[item_id] = file_item\n            self._items_by_id[item_id] = model_item\n\n        if new_model_items:\n            roow_item = self.invisibleRootItem()\n            roow_item.appendRows(new_model_items)\n\n    def remove_item_by_ids(self, item_ids):\n        if not item_ids:\n            return\n\n        items = []\n        for item_id in set(item_ids):\n            if item_id not in self._items_by_id:\n                continue\n            item = self._items_by_id.pop(item_id)\n            self._file_items_by_id.pop(item_id)\n            items.append(item)\n\n        if items:\n            for item in items:\n                self.removeRows(item.row(), 1)\n\n    def get_file_item_by_id(self, item_id):\n        return self._file_items_by_id.get(item_id)\n\n    def _create_item(self, file_item):\n        if file_item.is_dir:\n            icon_pixmap = paint_image_with_color(\n                get_image(filename=\"folder.png\"), QtCore.Qt.white\n            )\n        else:\n            icon_pixmap = paint_image_with_color(\n                get_image(filename=\"file.png\"), QtCore.Qt.white\n            )\n\n        item = QtGui.QStandardItem()\n        item_id = str(uuid.uuid4())\n        item.setData(item_id, ITEM_ID_ROLE)\n        item.setData(file_item.label or \"&lt; empty &gt;\", ITEM_LABEL_ROLE)\n        item.setData(file_item.filenames, FILENAMES_ROLE)\n        item.setData(file_item.directory, DIRPATH_ROLE)\n        item.setData(icon_pixmap, ITEM_ICON_ROLE)\n        item.setData(file_item.lower_ext, EXT_ROLE)\n        item.setData(file_item.is_dir, IS_DIR_ROLE)\n        item.setData(file_item.is_sequence, IS_SEQUENCE_ROLE)\n\n        return item_id, item\n\n    def mimeData(self, indexes):\n        item_ids = [\n            index.data(ITEM_ID_ROLE)\n            for index in indexes\n        ]\n\n        item_ids_data = convert_data_to_bytes(item_ids)\n        mime_data = super(FilesModel, self).mimeData(indexes)\n        mime_data.setData(\"files_widget/internal_move\", item_ids_data)\n\n        file_items = []\n        for item_id in item_ids:\n            file_item = self.get_file_item_by_id(item_id)\n            if file_item:\n                file_items.append(file_item.to_dict())\n\n        full_item_data = convert_data_to_bytes({\n            \"items\": file_items,\n            \"id\": self._id\n        })\n        mime_data.setData(\"files_widget/full_data\", full_item_data)\n        return mime_data\n\n    def dropMimeData(self, mime_data, action, row, col, index):\n        item_ids = convert_bytes_to_json(\n            mime_data.data(\"files_widget/internal_move\")\n        )\n        if item_ids is None:\n            return False\n\n        # Find matching item after which will be items moved\n        #   - store item before moved items are removed\n        root = self.invisibleRootItem()\n        if row &gt;= 0:\n            src_item = self.item(row)\n        else:\n            src_item_id = index.data(ITEM_ID_ROLE)\n            src_item = self._items_by_id.get(src_item_id)\n\n        src_row = None\n        if src_item:\n            src_row = src_item.row()\n\n        # Take out items that should be moved\n        items = []\n        for item_id in item_ids:\n            item = self._items_by_id.get(item_id)\n            if item:\n                self.takeRow(item.row())\n                items.append(item)\n\n        # Skip if there are not items that can be moved\n        if not items:\n            return False\n\n        # Calculate row where items should be inserted\n        row_count = root.rowCount()\n        if src_row is None:\n            src_row = row_count\n\n        if src_row &gt; row_count:\n            src_row = row_count\n\n        root.insertRow(src_row, items)\n        return True\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/attribute_defs/files_widget.html#client.ayon_core.tools.attribute_defs.files_widget.FilesModel.set_multivalue","title":"<code>set_multivalue(multivalue)</code>","text":"<p>Disable filtering.</p> Source code in <code>client/ayon_core/tools/attribute_defs/files_widget.py</code> <pre><code>def set_multivalue(self, multivalue):\n    \"\"\"Disable filtering.\"\"\"\n\n    if self._multivalue == multivalue:\n        return\n    self._multivalue = multivalue\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/attribute_defs/files_widget.html#client.ayon_core.tools.attribute_defs.files_widget.FilesProxyModel","title":"<code>FilesProxyModel</code>","text":"<p>               Bases: <code>QSortFilterProxyModel</code></p> Source code in <code>client/ayon_core/tools/attribute_defs/files_widget.py</code> <pre><code>class FilesProxyModel(QtCore.QSortFilterProxyModel):\n    def __init__(self, *args, **kwargs):\n        super(FilesProxyModel, self).__init__(*args, **kwargs)\n        self._allow_folders = False\n        self._allowed_extensions = None\n        self._multivalue = False\n\n    def set_multivalue(self, multivalue):\n        \"\"\"Disable filtering.\"\"\"\n\n        if self._multivalue == multivalue:\n            return\n        self._multivalue = multivalue\n        self.invalidateFilter()\n\n    def set_allow_folders(self, allow=None):\n        if allow is None:\n            allow = not self._allow_folders\n\n        if allow == self._allow_folders:\n            return\n        self._allow_folders = allow\n        self.invalidateFilter()\n\n    def set_allowed_extensions(self, extensions=None):\n        if extensions is not None:\n            _extensions = set()\n            for ext in set(extensions):\n                if not ext.startswith(\".\"):\n                    ext = \".{}\".format(ext)\n                _extensions.add(ext.lower())\n            extensions = _extensions\n\n        if self._allowed_extensions != extensions:\n            self._allowed_extensions = extensions\n            self.invalidateFilter()\n\n    def are_valid_files(self, filepaths):\n        for filepath in filepaths:\n            if os.path.isfile(filepath):\n                _, ext = os.path.splitext(filepath)\n                if ext.lower() in self._allowed_extensions:\n                    return True\n\n            elif self._allow_folders:\n                return True\n        return False\n\n    def filter_valid_files(self, filepaths):\n        filtered_paths = []\n        for filepath in filepaths:\n            if os.path.isfile(filepath):\n                _, ext = os.path.splitext(filepath)\n                if ext.lower() in self._allowed_extensions:\n                    filtered_paths.append(filepath)\n\n            elif self._allow_folders:\n                filtered_paths.append(filepath)\n        return filtered_paths\n\n    def filterAcceptsRow(self, row, parent_index):\n        # Skip filtering if multivalue is set\n        if self._multivalue:\n            return True\n\n        model = self.sourceModel()\n        index = model.index(row, self.filterKeyColumn(), parent_index)\n        # First check if item is folder and if folders are enabled\n        if index.data(IS_DIR_ROLE):\n            if not self._allow_folders:\n                return False\n            return True\n\n        # Check if there are any allowed extensions\n        if self._allowed_extensions is None:\n            return False\n\n        if index.data(EXT_ROLE) not in self._allowed_extensions:\n            return False\n        return True\n\n    def lessThan(self, left, right):\n        left_comparison = left.data(DIRPATH_ROLE)\n        right_comparison = right.data(DIRPATH_ROLE)\n        if left_comparison == right_comparison:\n            left_comparison = left.data(ITEM_LABEL_ROLE)\n            right_comparison = right.data(ITEM_LABEL_ROLE)\n\n        if sorted((left_comparison, right_comparison))[0] == left_comparison:\n            return True\n        return False\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/attribute_defs/files_widget.html#client.ayon_core.tools.attribute_defs.files_widget.FilesProxyModel.set_multivalue","title":"<code>set_multivalue(multivalue)</code>","text":"<p>Disable filtering.</p> Source code in <code>client/ayon_core/tools/attribute_defs/files_widget.py</code> <pre><code>def set_multivalue(self, multivalue):\n    \"\"\"Disable filtering.\"\"\"\n\n    if self._multivalue == multivalue:\n        return\n    self._multivalue = multivalue\n    self.invalidateFilter()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/attribute_defs/files_widget.html#client.ayon_core.tools.attribute_defs.files_widget.FilesView","title":"<code>FilesView</code>","text":"<p>               Bases: <code>QListView</code></p> <p>View showing instances and their groups.</p> Source code in <code>client/ayon_core/tools/attribute_defs/files_widget.py</code> <pre><code>class FilesView(QtWidgets.QListView):\n    \"\"\"View showing instances and their groups.\"\"\"\n\n    remove_requested = QtCore.Signal()\n    context_menu_requested = QtCore.Signal(QtCore.QPoint, bool)\n\n    def __init__(self, *args, **kwargs):\n        super(FilesView, self).__init__(*args, **kwargs)\n\n        self.setEditTriggers(QtWidgets.QAbstractItemView.NoEditTriggers)\n        self.setSelectionMode(\n            QtWidgets.QAbstractItemView.ExtendedSelection\n        )\n        self.setContextMenuPolicy(QtCore.Qt.CustomContextMenu)\n        self.setAcceptDrops(True)\n        self.setDragEnabled(True)\n        self.setDragDropMode(QtWidgets.QAbstractItemView.InternalMove)\n\n        remove_btn = InViewButton(self)\n        pix_enabled = paint_image_with_color(\n            get_image(filename=\"delete.png\"), QtCore.Qt.white\n        )\n        pix_disabled = paint_image_with_color(\n            get_image(filename=\"delete.png\"), QtCore.Qt.gray\n        )\n        icon = QtGui.QIcon(pix_enabled)\n        icon.addPixmap(pix_disabled, QtGui.QIcon.Disabled, QtGui.QIcon.Off)\n        remove_btn.setIcon(icon)\n        remove_btn.setEnabled(False)\n\n        remove_btn.clicked.connect(self._on_remove_clicked)\n        self.customContextMenuRequested.connect(self._on_context_menu_request)\n\n        self._remove_btn = remove_btn\n        self._multivalue = False\n\n    def setSelectionModel(self, *args, **kwargs):\n        \"\"\"Catch selection model set to register signal callback.\n\n        Selection model is not available during initialization.\n        \"\"\"\n\n        super(FilesView, self).setSelectionModel(*args, **kwargs)\n        selection_model = self.selectionModel()\n        selection_model.selectionChanged.connect(self._on_selection_change)\n\n    def set_multivalue(self, multivalue):\n        \"\"\"Disable remove button on multivalue.\"\"\"\n\n        self._multivalue = multivalue\n        self._remove_btn.setVisible(not multivalue)\n\n    def update_remove_btn_visibility(self):\n        model = self.model()\n        visible = False\n        if not self._multivalue and model:\n            visible = model.rowCount() &gt; 0\n        self._remove_btn.setVisible(visible)\n\n    def has_selected_item_ids(self):\n        \"\"\"Is any index selected.\"\"\"\n        for index in self.selectionModel().selectedIndexes():\n            instance_id = index.data(ITEM_ID_ROLE)\n            if instance_id is not None:\n                return True\n        return False\n\n    def get_selected_item_ids(self):\n        \"\"\"Ids of selected instances.\"\"\"\n\n        selected_item_ids = set()\n        for index in self.selectionModel().selectedIndexes():\n            instance_id = index.data(ITEM_ID_ROLE)\n            if instance_id is not None:\n                selected_item_ids.add(instance_id)\n        return selected_item_ids\n\n    def has_selected_sequence(self):\n        for index in self.selectionModel().selectedIndexes():\n            if index.data(IS_SEQUENCE_ROLE):\n                return True\n        return False\n\n    def event(self, event):\n        if event.type() == QtCore.QEvent.KeyPress:\n            if (\n                event.key() == QtCore.Qt.Key_Delete\n                and self.has_selected_item_ids()\n            ):\n                self.remove_requested.emit()\n                return True\n\n        return super(FilesView, self).event(event)\n\n    def _on_context_menu_request(self, pos):\n        index = self.indexAt(pos)\n        point = self.viewport().mapToGlobal(pos)\n        self.context_menu_requested.emit(point, index.isValid())\n\n    def _on_selection_change(self):\n        self._remove_btn.setEnabled(self.has_selected_item_ids())\n\n    def _on_remove_clicked(self):\n        self.remove_requested.emit()\n\n    def _update_remove_btn(self):\n        \"\"\"Position remove button to bottom right.\"\"\"\n\n        viewport = self.viewport()\n        height = viewport.height()\n        pos_x = viewport.width() - self._remove_btn.width() - 5\n        pos_y = height - self._remove_btn.height() - 5\n        self._remove_btn.move(max(0, pos_x), max(0, pos_y))\n\n    def resizeEvent(self, event):\n        super(FilesView, self).resizeEvent(event)\n        self._update_remove_btn()\n\n    def showEvent(self, event):\n        super(FilesView, self).showEvent(event)\n        self._update_remove_btn()\n        self.update_remove_btn_visibility()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/attribute_defs/files_widget.html#client.ayon_core.tools.attribute_defs.files_widget.FilesView.get_selected_item_ids","title":"<code>get_selected_item_ids()</code>","text":"<p>Ids of selected instances.</p> Source code in <code>client/ayon_core/tools/attribute_defs/files_widget.py</code> <pre><code>def get_selected_item_ids(self):\n    \"\"\"Ids of selected instances.\"\"\"\n\n    selected_item_ids = set()\n    for index in self.selectionModel().selectedIndexes():\n        instance_id = index.data(ITEM_ID_ROLE)\n        if instance_id is not None:\n            selected_item_ids.add(instance_id)\n    return selected_item_ids\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/attribute_defs/files_widget.html#client.ayon_core.tools.attribute_defs.files_widget.FilesView.has_selected_item_ids","title":"<code>has_selected_item_ids()</code>","text":"<p>Is any index selected.</p> Source code in <code>client/ayon_core/tools/attribute_defs/files_widget.py</code> <pre><code>def has_selected_item_ids(self):\n    \"\"\"Is any index selected.\"\"\"\n    for index in self.selectionModel().selectedIndexes():\n        instance_id = index.data(ITEM_ID_ROLE)\n        if instance_id is not None:\n            return True\n    return False\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/attribute_defs/files_widget.html#client.ayon_core.tools.attribute_defs.files_widget.FilesView.setSelectionModel","title":"<code>setSelectionModel(*args, **kwargs)</code>","text":"<p>Catch selection model set to register signal callback.</p> <p>Selection model is not available during initialization.</p> Source code in <code>client/ayon_core/tools/attribute_defs/files_widget.py</code> <pre><code>def setSelectionModel(self, *args, **kwargs):\n    \"\"\"Catch selection model set to register signal callback.\n\n    Selection model is not available during initialization.\n    \"\"\"\n\n    super(FilesView, self).setSelectionModel(*args, **kwargs)\n    selection_model = self.selectionModel()\n    selection_model.selectionChanged.connect(self._on_selection_change)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/attribute_defs/files_widget.html#client.ayon_core.tools.attribute_defs.files_widget.FilesView.set_multivalue","title":"<code>set_multivalue(multivalue)</code>","text":"<p>Disable remove button on multivalue.</p> Source code in <code>client/ayon_core/tools/attribute_defs/files_widget.py</code> <pre><code>def set_multivalue(self, multivalue):\n    \"\"\"Disable remove button on multivalue.\"\"\"\n\n    self._multivalue = multivalue\n    self._remove_btn.setVisible(not multivalue)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/attribute_defs/widgets.html","title":"widgets","text":""},{"location":"autoapi/client/ayon_core/tools/attribute_defs/widgets.html#client.ayon_core.tools.attribute_defs.widgets.AttributeDefinitionsLabel","title":"<code>AttributeDefinitionsLabel</code>","text":"<p>               Bases: <code>QLabel</code></p> <p>Label related to value attribute definition.</p> <p>Label is used to show attribute definition label and to show if value is overridden.</p> <p>Label can be right-clicked to revert value to default.</p> Source code in <code>client/ayon_core/tools/attribute_defs/widgets.py</code> <pre><code>class AttributeDefinitionsLabel(QtWidgets.QLabel):\n    \"\"\"Label related to value attribute definition.\n\n    Label is used to show attribute definition label and to show if value\n    is overridden.\n\n    Label can be right-clicked to revert value to default.\n    \"\"\"\n    revert_to_default_requested = QtCore.Signal(str)\n\n    def __init__(\n        self,\n        attr_id: str,\n        label: str,\n        parent: QtWidgets.QWidget,\n    ):\n        super().__init__(label, parent)\n\n        self._attr_id = attr_id\n        self._overridden = False\n        self.setContextMenuPolicy(QtCore.Qt.CustomContextMenu)\n\n        self.customContextMenuRequested.connect(self._on_context_menu)\n\n    def set_overridden(self, overridden: bool):\n        if self._overridden == overridden:\n            return\n        self._overridden = overridden\n        set_style_property(\n            self,\n            \"overridden\",\n            \"1\" if overridden else \"\"\n        )\n\n    def _on_context_menu(self, point: QtCore.QPoint):\n        menu = QtWidgets.QMenu(self)\n        action = QtWidgets.QAction(menu)\n        action.setText(REVERT_TO_DEFAULT_LABEL)\n        action.triggered.connect(self._request_revert_to_default)\n        menu.addAction(action)\n        menu.exec_(self.mapToGlobal(point))\n\n    def _request_revert_to_default(self):\n        self.revert_to_default_requested.emit(self._attr_id)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/attribute_defs/widgets.html#client.ayon_core.tools.attribute_defs.widgets.AttributeDefinitionsWidget","title":"<code>AttributeDefinitionsWidget</code>","text":"<p>               Bases: <code>QWidget</code></p> <p>Create widgets for attribute definitions in grid layout.</p> <p>Widget creates input widgets for passed attribute definitions.</p> <p>Widget can't handle multiselection values.</p> Source code in <code>client/ayon_core/tools/attribute_defs/widgets.py</code> <pre><code>class AttributeDefinitionsWidget(QtWidgets.QWidget):\n    \"\"\"Create widgets for attribute definitions in grid layout.\n\n    Widget creates input widgets for passed attribute definitions.\n\n    Widget can't handle multiselection values.\n    \"\"\"\n\n    def __init__(self, attr_defs=None, parent=None):\n        super().__init__(parent)\n\n        self._widgets_by_id = {}\n        self._labels_by_id = {}\n        self._current_keys = set()\n\n        self.set_attr_defs(attr_defs)\n\n    def clear_attr_defs(self):\n        \"\"\"Remove all existing widgets and reset layout if needed.\"\"\"\n        self._widgets_by_id = {}\n        self._labels_by_id = {}\n        self._current_keys = set()\n\n        layout = self.layout()\n        if layout is not None:\n            if layout.count() == 0:\n                return\n\n            while layout.count():\n                item = layout.takeAt(0)\n                widget = item.widget()\n                if widget:\n                    widget.setVisible(False)\n                    widget.deleteLater()\n\n            layout.deleteLater()\n\n        new_layout = QtWidgets.QGridLayout()\n        new_layout.setContentsMargins(0, 0, 0, 0)\n        new_layout.setColumnStretch(0, 0)\n        new_layout.setColumnStretch(1, 1)\n        self.setLayout(new_layout)\n\n    def set_attr_defs(self, attr_defs):\n        \"\"\"Replace current attribute definitions with passed.\"\"\"\n        self.clear_attr_defs()\n        if attr_defs:\n            self.add_attr_defs(attr_defs)\n\n    def add_attr_defs(self, attr_defs):\n        \"\"\"Add attribute definitions to current.\"\"\"\n        layout = self.layout()\n\n        row = 0\n        for attr_def in attr_defs:\n            if attr_def.is_value_def:\n                if attr_def.key in self._current_keys:\n                    raise KeyError(\n                        \"Duplicated key \\\"{}\\\"\".format(attr_def.key))\n\n                self._current_keys.add(attr_def.key)\n            widget = create_widget_for_attr_def(attr_def, self)\n            self._widgets_by_id[attr_def.id] = widget\n\n            if not attr_def.visible:\n                continue\n\n            label = attr_def.label\n            if isinstance(attr_def, UILabelDef):\n                label = None\n\n            col_num = 0\n            if attr_def.is_label_horizontal and (\n                attr_def.is_value_def\n                or isinstance(attr_def, ButtonDef)\n            ):\n                col_num = 1\n            expand_cols = 2 - col_num\n\n            if label:\n                label_widget = AttributeDefinitionsLabel(\n                    attr_def.id, label, self\n                )\n                label_widget.revert_to_default_requested.connect(\n                    self._on_revert_request\n                )\n                self._labels_by_id[attr_def.id] = label_widget\n                tooltip = attr_def.tooltip\n                if tooltip:\n                    label_widget.setToolTip(tooltip)\n                if attr_def.is_label_horizontal:\n                    label_widget.setAlignment(\n                        QtCore.Qt.AlignRight\n                        | QtCore.Qt.AlignVCenter\n                    )\n                layout.addWidget(\n                    label_widget, row, 0, 1, expand_cols\n                )\n                if not attr_def.is_label_horizontal:\n                    row += 1\n\n            if attr_def.is_value_def:\n                widget.value_changed.connect(self._on_value_change)\n\n            layout.addWidget(\n                widget, row, col_num, 1, expand_cols\n            )\n            row += 1\n\n    def set_value(self, value):\n        new_value = copy.deepcopy(value)\n        for widget in self._widgets_by_id.values():\n            attr_def = widget.attr_def\n            if attr_def.key not in new_value:\n                continue\n\n            widget_value = new_value[attr_def.key]\n            if widget_value is None:\n                widget_value = copy.deepcopy(attr_def.default)\n            widget.set_value(widget_value)\n\n    def current_value(self):\n        output = {}\n        for widget in self._widgets_by_id.values():\n            attr_def = widget.attr_def\n            if not isinstance(attr_def, UIDef):\n                output[attr_def.key] = widget.current_value()\n\n        return output\n\n    def _on_revert_request(self, attr_id):\n        widget = self._widgets_by_id.get(attr_id)\n        if widget is not None:\n            widget.set_value(widget.attr_def.default)\n\n    def _on_value_change(self, value, attr_id):\n        widget = self._widgets_by_id.get(attr_id)\n        if widget is None:\n            return\n        label = self._labels_by_id.get(attr_id)\n        if label is not None:\n            label.set_overridden(value != widget.attr_def.default)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/attribute_defs/widgets.html#client.ayon_core.tools.attribute_defs.widgets.AttributeDefinitionsWidget.add_attr_defs","title":"<code>add_attr_defs(attr_defs)</code>","text":"<p>Add attribute definitions to current.</p> Source code in <code>client/ayon_core/tools/attribute_defs/widgets.py</code> <pre><code>def add_attr_defs(self, attr_defs):\n    \"\"\"Add attribute definitions to current.\"\"\"\n    layout = self.layout()\n\n    row = 0\n    for attr_def in attr_defs:\n        if attr_def.is_value_def:\n            if attr_def.key in self._current_keys:\n                raise KeyError(\n                    \"Duplicated key \\\"{}\\\"\".format(attr_def.key))\n\n            self._current_keys.add(attr_def.key)\n        widget = create_widget_for_attr_def(attr_def, self)\n        self._widgets_by_id[attr_def.id] = widget\n\n        if not attr_def.visible:\n            continue\n\n        label = attr_def.label\n        if isinstance(attr_def, UILabelDef):\n            label = None\n\n        col_num = 0\n        if attr_def.is_label_horizontal and (\n            attr_def.is_value_def\n            or isinstance(attr_def, ButtonDef)\n        ):\n            col_num = 1\n        expand_cols = 2 - col_num\n\n        if label:\n            label_widget = AttributeDefinitionsLabel(\n                attr_def.id, label, self\n            )\n            label_widget.revert_to_default_requested.connect(\n                self._on_revert_request\n            )\n            self._labels_by_id[attr_def.id] = label_widget\n            tooltip = attr_def.tooltip\n            if tooltip:\n                label_widget.setToolTip(tooltip)\n            if attr_def.is_label_horizontal:\n                label_widget.setAlignment(\n                    QtCore.Qt.AlignRight\n                    | QtCore.Qt.AlignVCenter\n                )\n            layout.addWidget(\n                label_widget, row, 0, 1, expand_cols\n            )\n            if not attr_def.is_label_horizontal:\n                row += 1\n\n        if attr_def.is_value_def:\n            widget.value_changed.connect(self._on_value_change)\n\n        layout.addWidget(\n            widget, row, col_num, 1, expand_cols\n        )\n        row += 1\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/attribute_defs/widgets.html#client.ayon_core.tools.attribute_defs.widgets.AttributeDefinitionsWidget.clear_attr_defs","title":"<code>clear_attr_defs()</code>","text":"<p>Remove all existing widgets and reset layout if needed.</p> Source code in <code>client/ayon_core/tools/attribute_defs/widgets.py</code> <pre><code>def clear_attr_defs(self):\n    \"\"\"Remove all existing widgets and reset layout if needed.\"\"\"\n    self._widgets_by_id = {}\n    self._labels_by_id = {}\n    self._current_keys = set()\n\n    layout = self.layout()\n    if layout is not None:\n        if layout.count() == 0:\n            return\n\n        while layout.count():\n            item = layout.takeAt(0)\n            widget = item.widget()\n            if widget:\n                widget.setVisible(False)\n                widget.deleteLater()\n\n        layout.deleteLater()\n\n    new_layout = QtWidgets.QGridLayout()\n    new_layout.setContentsMargins(0, 0, 0, 0)\n    new_layout.setColumnStretch(0, 0)\n    new_layout.setColumnStretch(1, 1)\n    self.setLayout(new_layout)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/attribute_defs/widgets.html#client.ayon_core.tools.attribute_defs.widgets.AttributeDefinitionsWidget.set_attr_defs","title":"<code>set_attr_defs(attr_defs)</code>","text":"<p>Replace current attribute definitions with passed.</p> Source code in <code>client/ayon_core/tools/attribute_defs/widgets.py</code> <pre><code>def set_attr_defs(self, attr_defs):\n    \"\"\"Replace current attribute definitions with passed.\"\"\"\n    self.clear_attr_defs()\n    if attr_defs:\n        self.add_attr_defs(attr_defs)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/index.html","title":"common_models","text":"<p>Backend models that can be used in controllers.</p>"},{"location":"autoapi/client/ayon_core/tools/common_models/index.html#client.ayon_core.tools.common_models.FolderItem","title":"<code>FolderItem</code>","text":"<p>Item representing folder entity on a server.</p> <p>Folder can be a child of another folder or a project.</p> <p>Parameters:</p> Name Type Description Default <code>entity_id</code> <code>str</code> <p>Folder id.</p> required <code>parent_id</code> <code>str | None</code> <p>Parent folder id. If 'None' then project is parent.</p> required <code>name</code> <code>str</code> <p>Name of folder.</p> required <code>path</code> <code>str</code> <p>Folder path.</p> required <code>folder_type</code> <code>str</code> <p>Type of folder.</p> required <code>label</code> <code>str | None</code> <p>Folder label.</p> required Source code in <code>client/ayon_core/tools/common_models/hierarchy.py</code> <pre><code>class FolderItem:\n    \"\"\"Item representing folder entity on a server.\n\n    Folder can be a child of another folder or a project.\n\n    Args:\n        entity_id (str): Folder id.\n        parent_id (str | None): Parent folder id. If 'None' then project\n            is parent.\n        name (str): Name of folder.\n        path (str): Folder path.\n        folder_type (str): Type of folder.\n        label (str | None): Folder label.\n    \"\"\"\n\n    def __init__(\n        self, entity_id, parent_id, name, path, folder_type, label\n    ):\n        self.entity_id = entity_id\n        self.parent_id = parent_id\n        self.name = name\n        self.path = path\n        self.folder_type = folder_type\n        self.label = label or name\n\n    def to_data(self):\n        \"\"\"Converts folder item to data.\n\n        Returns:\n            dict[str, Any]: Folder item data.\n        \"\"\"\n\n        return {\n            \"entity_id\": self.entity_id,\n            \"parent_id\": self.parent_id,\n            \"name\": self.name,\n            \"path\": self.path,\n            \"folder_type\": self.folder_type,\n            \"label\": self.label,\n        }\n\n    @classmethod\n    def from_data(cls, data):\n        \"\"\"Re-creates folder item from data.\n\n        Args:\n            data (dict[str, Any]): Folder item data.\n\n        Returns:\n            FolderItem: Folder item.\n        \"\"\"\n\n        return cls(**data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/index.html#client.ayon_core.tools.common_models.FolderItem.from_data","title":"<code>from_data(data)</code>  <code>classmethod</code>","text":"<p>Re-creates folder item from data.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict[str, Any]</code> <p>Folder item data.</p> required <p>Returns:</p> Name Type Description <code>FolderItem</code> <p>Folder item.</p> Source code in <code>client/ayon_core/tools/common_models/hierarchy.py</code> <pre><code>@classmethod\ndef from_data(cls, data):\n    \"\"\"Re-creates folder item from data.\n\n    Args:\n        data (dict[str, Any]): Folder item data.\n\n    Returns:\n        FolderItem: Folder item.\n    \"\"\"\n\n    return cls(**data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/index.html#client.ayon_core.tools.common_models.FolderItem.to_data","title":"<code>to_data()</code>","text":"<p>Converts folder item to data.</p> <p>Returns:</p> Type Description <p>dict[str, Any]: Folder item data.</p> Source code in <code>client/ayon_core/tools/common_models/hierarchy.py</code> <pre><code>def to_data(self):\n    \"\"\"Converts folder item to data.\n\n    Returns:\n        dict[str, Any]: Folder item data.\n    \"\"\"\n\n    return {\n        \"entity_id\": self.entity_id,\n        \"parent_id\": self.parent_id,\n        \"name\": self.name,\n        \"path\": self.path,\n        \"folder_type\": self.folder_type,\n        \"label\": self.label,\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/index.html#client.ayon_core.tools.common_models.FolderTypeItem","title":"<code>FolderTypeItem</code>","text":"<p>Item representing folder type of project.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Folder type name (\"Shot\").</p> required <code>short</code> <code>str</code> <p>Short folder type name (\"sh\").</p> required <code>icon</code> <code>str</code> <p>Icon name in MaterialIcons (\"fiber_new\").</p> required Source code in <code>client/ayon_core/tools/common_models/projects.py</code> <pre><code>class FolderTypeItem:\n    \"\"\"Item representing folder type of project.\n\n    Args:\n        name (str): Folder type name (\"Shot\").\n        short (str): Short folder type name (\"sh\").\n        icon (str): Icon name in MaterialIcons (\"fiber_new\").\n\n    \"\"\"\n    def __init__(self, name, short, icon):\n        self.name = name\n        self.short = short\n        self.icon = icon\n\n    def to_data(self):\n        return {\n            \"name\": self.name,\n            \"short\": self.short,\n            \"icon\": self.icon,\n        }\n\n    @classmethod\n    def from_data(cls, data):\n        return cls(**data)\n\n    @classmethod\n    def from_project_item(cls, folder_type_data):\n        return cls(\n            name=folder_type_data[\"name\"],\n            short=folder_type_data.get(\"shortName\", \"\"),\n            icon=folder_type_data[\"icon\"],\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/index.html#client.ayon_core.tools.common_models.HierarchyExpectedSelection","title":"<code>HierarchyExpectedSelection</code>","text":"<p>Base skeleton of expected selection model.</p> <p>Expected selection model holds information about which entities should be selected. The order of selection is very important as change of project will affect what folders are available in folders UI and so on. Because of that should expected selection model know what is current entity to select.</p> <p>If any of 'handle_project', 'handle_folder' or 'handle_task' is set to 'False' expected selection data won't contain information about the entity type at all. Also if project is not handled then it is not necessary to call 'expected_project_selected'. Same goes for folder and task.</p> <p>Model is triggering event with 'expected_selection_changed' topic and data &gt; data structure is matching 'get_expected_selection_data' method.</p> Questions <p>Require '_ExampleController' as abstraction?</p> <p>Parameters:</p> Name Type Description Default <code>controller</code> <code>Any</code> <p>Controller object. ('_ExampleController')</p> required <code>handle_project</code> <code>bool</code> <p>Project can be considered as can have expected selection.</p> <code>True</code> <code>handle_folder</code> <code>bool</code> <p>Folder can be considered as can have expected selection.</p> <code>True</code> <code>handle_task</code> <code>bool</code> <p>Task can be considered as can have expected selection.</p> <code>True</code> Source code in <code>client/ayon_core/tools/common_models/selection.py</code> <pre><code>class HierarchyExpectedSelection:\n    \"\"\"Base skeleton of expected selection model.\n\n    Expected selection model holds information about which entities should be\n    selected. The order of selection is very important as change of project\n    will affect what folders are available in folders UI and so on. Because\n    of that should expected selection model know what is current entity\n    to select.\n\n    If any of 'handle_project', 'handle_folder' or 'handle_task' is set to\n    'False' expected selection data won't contain information about the\n    entity type at all. Also if project is not handled then it is not\n    necessary to call 'expected_project_selected'. Same goes for folder and\n    task.\n\n    Model is triggering event with 'expected_selection_changed' topic and\n    data &gt; data structure is matching 'get_expected_selection_data' method.\n\n    Questions:\n        Require '_ExampleController' as abstraction?\n\n    Args:\n        controller (Any): Controller object. ('_ExampleController')\n        handle_project (bool): Project can be considered as can have expected\n            selection.\n        handle_folder (bool): Folder can be considered as can have expected\n            selection.\n        handle_task (bool): Task can be considered as can have expected\n            selection.\n    \"\"\"\n\n    def __init__(\n        self,\n        controller,\n        handle_project=True,\n        handle_folder=True,\n        handle_task=True\n    ):\n        self._project_name = None\n        self._folder_id = None\n        self._task_name = None\n\n        self._project_selected = True\n        self._folder_selected = True\n        self._task_selected = True\n\n        self._controller = controller\n\n        self._handle_project = handle_project\n        self._handle_folder = handle_folder\n        self._handle_task = handle_task\n\n    def set_expected_selection(\n        self,\n        project_name=None,\n        folder_id=None,\n        task_name=None\n    ):\n        \"\"\"Sets expected selection.\n\n        Args:\n            project_name (Optional[str]): Project name.\n            folder_id (Optional[str]): Folder id.\n            task_name (Optional[str]): Task name.\n        \"\"\"\n\n        self._project_name = project_name\n        self._folder_id = folder_id\n        self._task_name = task_name\n\n        self._project_selected = not self._handle_project\n        self._folder_selected = not self._handle_folder\n        self._task_selected = not self._handle_task\n        self._emit_change()\n\n    def get_expected_selection_data(self):\n        project_current = False\n        folder_current = False\n        task_current = False\n        if not self._project_selected:\n            project_current = True\n        elif not self._folder_selected:\n            folder_current = True\n        elif not self._task_selected:\n            task_current = True\n        data = {}\n        if self._handle_project:\n            data[\"project\"] = {\n                \"name\": self._project_name,\n                \"current\": project_current,\n                \"selected\": self._project_selected,\n            }\n        if self._handle_folder:\n            data[\"folder\"] = {\n                \"id\": self._folder_id,\n                \"current\": folder_current,\n                \"selected\": self._folder_selected,\n            }\n        if self._handle_task:\n            data[\"task\"] = {\n                \"name\": self._task_name,\n                \"current\": task_current,\n                \"selected\": self._task_selected,\n            }\n\n        return data\n\n    def is_expected_project_selected(self, project_name):\n        if not self._handle_project:\n            return True\n        return project_name == self._project_name and self._project_selected\n\n    def is_expected_folder_selected(self, folder_id):\n        if not self._handle_folder:\n            return True\n        return folder_id == self._folder_id and self._folder_selected\n\n    def expected_project_selected(self, project_name):\n        \"\"\"UI selected requested project.\n\n        Other entity types can be requested for selection.\n\n        Args:\n            project_name (str): Name of project.\n        \"\"\"\n\n        if project_name != self._project_name:\n            return False\n        self._project_selected = True\n        self._emit_change()\n        return True\n\n    def expected_folder_selected(self, folder_id):\n        \"\"\"UI selected requested folder.\n\n        Other entity types can be requested for selection.\n\n        Args:\n            folder_id (str): Folder id.\n        \"\"\"\n\n        if folder_id != self._folder_id:\n            return False\n        self._folder_selected = True\n        self._emit_change()\n        return True\n\n    def expected_task_selected(self, folder_id, task_name):\n        \"\"\"UI selected requested task.\n\n        Other entity types can be requested for selection.\n\n        Because task name is not unique across project a folder id is also\n        required to confirm the right task has been selected.\n\n        Args:\n            folder_id (str): Folder id.\n            task_name (str): Task name.\n        \"\"\"\n\n        if self._folder_id != folder_id:\n            return False\n\n        if task_name != self._task_name:\n            return False\n        self._task_selected = True\n        self._emit_change()\n        return True\n\n    def _emit_change(self):\n        self._controller.emit_event(\n            \"expected_selection_changed\",\n            self.get_expected_selection_data(),\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/index.html#client.ayon_core.tools.common_models.HierarchyExpectedSelection.expected_folder_selected","title":"<code>expected_folder_selected(folder_id)</code>","text":"<p>UI selected requested folder.</p> <p>Other entity types can be requested for selection.</p> <p>Parameters:</p> Name Type Description Default <code>folder_id</code> <code>str</code> <p>Folder id.</p> required Source code in <code>client/ayon_core/tools/common_models/selection.py</code> <pre><code>def expected_folder_selected(self, folder_id):\n    \"\"\"UI selected requested folder.\n\n    Other entity types can be requested for selection.\n\n    Args:\n        folder_id (str): Folder id.\n    \"\"\"\n\n    if folder_id != self._folder_id:\n        return False\n    self._folder_selected = True\n    self._emit_change()\n    return True\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/index.html#client.ayon_core.tools.common_models.HierarchyExpectedSelection.expected_project_selected","title":"<code>expected_project_selected(project_name)</code>","text":"<p>UI selected requested project.</p> <p>Other entity types can be requested for selection.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Name of project.</p> required Source code in <code>client/ayon_core/tools/common_models/selection.py</code> <pre><code>def expected_project_selected(self, project_name):\n    \"\"\"UI selected requested project.\n\n    Other entity types can be requested for selection.\n\n    Args:\n        project_name (str): Name of project.\n    \"\"\"\n\n    if project_name != self._project_name:\n        return False\n    self._project_selected = True\n    self._emit_change()\n    return True\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/index.html#client.ayon_core.tools.common_models.HierarchyExpectedSelection.expected_task_selected","title":"<code>expected_task_selected(folder_id, task_name)</code>","text":"<p>UI selected requested task.</p> <p>Other entity types can be requested for selection.</p> <p>Because task name is not unique across project a folder id is also required to confirm the right task has been selected.</p> <p>Parameters:</p> Name Type Description Default <code>folder_id</code> <code>str</code> <p>Folder id.</p> required <code>task_name</code> <code>str</code> <p>Task name.</p> required Source code in <code>client/ayon_core/tools/common_models/selection.py</code> <pre><code>def expected_task_selected(self, folder_id, task_name):\n    \"\"\"UI selected requested task.\n\n    Other entity types can be requested for selection.\n\n    Because task name is not unique across project a folder id is also\n    required to confirm the right task has been selected.\n\n    Args:\n        folder_id (str): Folder id.\n        task_name (str): Task name.\n    \"\"\"\n\n    if self._folder_id != folder_id:\n        return False\n\n    if task_name != self._task_name:\n        return False\n    self._task_selected = True\n    self._emit_change()\n    return True\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/index.html#client.ayon_core.tools.common_models.HierarchyExpectedSelection.set_expected_selection","title":"<code>set_expected_selection(project_name=None, folder_id=None, task_name=None)</code>","text":"<p>Sets expected selection.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>Optional[str]</code> <p>Project name.</p> <code>None</code> <code>folder_id</code> <code>Optional[str]</code> <p>Folder id.</p> <code>None</code> <code>task_name</code> <code>Optional[str]</code> <p>Task name.</p> <code>None</code> Source code in <code>client/ayon_core/tools/common_models/selection.py</code> <pre><code>def set_expected_selection(\n    self,\n    project_name=None,\n    folder_id=None,\n    task_name=None\n):\n    \"\"\"Sets expected selection.\n\n    Args:\n        project_name (Optional[str]): Project name.\n        folder_id (Optional[str]): Folder id.\n        task_name (Optional[str]): Task name.\n    \"\"\"\n\n    self._project_name = project_name\n    self._folder_id = folder_id\n    self._task_name = task_name\n\n    self._project_selected = not self._handle_project\n    self._folder_selected = not self._handle_folder\n    self._task_selected = not self._handle_task\n    self._emit_change()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/index.html#client.ayon_core.tools.common_models.HierarchyModel","title":"<code>HierarchyModel</code>","text":"<p>               Bases: <code>object</code></p> <p>Model for project hierarchy items.</p> <p>Hierarchy items are folders and tasks. Folders can have as parent another folder or project. Tasks can have as parent only folder.</p> Source code in <code>client/ayon_core/tools/common_models/hierarchy.py</code> <pre><code>class HierarchyModel(object):\n    \"\"\"Model for project hierarchy items.\n\n    Hierarchy items are folders and tasks. Folders can have as parent another\n    folder or project. Tasks can have as parent only folder.\n    \"\"\"\n    lifetime = 60  # A minute\n\n    def __init__(self, controller):\n        self._tags_by_entity_type = NestedCacheItem(\n            levels=1, default_factory=dict, lifetime=self.lifetime)\n        self._folders_items = NestedCacheItem(\n            levels=1, default_factory=dict, lifetime=self.lifetime)\n        self._folders_by_id = NestedCacheItem(\n            levels=2, default_factory=dict, lifetime=self.lifetime)\n\n        self._task_items = NestedCacheItem(\n            levels=2, default_factory=dict, lifetime=self.lifetime)\n        self._tasks_by_id = NestedCacheItem(\n            levels=2, default_factory=dict, lifetime=self.lifetime)\n\n        self._entity_ids_by_assignee = NestedCacheItem(\n            levels=2, default_factory=dict, lifetime=self.lifetime)\n\n        self._folders_refreshing = set()\n        self._tasks_refreshing = set()\n        self._controller = controller\n\n    def reset(self):\n        self._tags_by_entity_type.reset()\n        self._folders_items.reset()\n        self._folders_by_id.reset()\n\n        self._task_items.reset()\n        self._tasks_by_id.reset()\n\n        self._entity_ids_by_assignee.reset()\n\n    def refresh_project(self, project_name):\n        \"\"\"Force to refresh folder items for a project.\n\n        Args:\n            project_name (str): Name of project to refresh.\n        \"\"\"\n\n        self._refresh_folders_cache(project_name)\n\n    def get_folder_items(self, project_name, sender):\n        \"\"\"Get folder items by project name.\n\n        The folders are cached per project name. If the cache is not valid\n        then the folders are queried from server.\n\n        Args:\n            project_name (str): Name of project where to look for folders.\n            sender (Union[str, None]): Who requested the folder ids.\n\n        Returns:\n            dict[str, FolderItem]: Folder items by id.\n        \"\"\"\n\n        if not self._folders_items[project_name].is_valid:\n            self._refresh_folders_cache(project_name, sender)\n        return self._folders_items[project_name].get_data()\n\n    def get_folder_items_by_id(self, project_name, folder_ids):\n        \"\"\"Get folder items by ids.\n\n        This function will query folders if they are not in cache. But the\n        queried items are not added to cache back.\n\n        Args:\n            project_name (str): Name of project where to look for folders.\n            folder_ids (Iterable[str]): Folder ids.\n\n        Returns:\n            dict[str, Union[FolderItem, None]]: Folder items by id.\n        \"\"\"\n\n        folder_ids = set(folder_ids)\n        if self._folders_items[project_name].is_valid:\n            cache_data = self._folders_items[project_name].get_data()\n            return {\n                folder_id: cache_data.get(folder_id)\n                for folder_id in folder_ids\n            }\n        folders = ayon_api.get_folders(\n            project_name,\n            folder_ids=folder_ids,\n            fields=[\"id\", \"name\", \"label\", \"parentId\", \"path\", \"folderType\"]\n        )\n        # Make sure all folder ids are in output\n        output = {folder_id: None for folder_id in folder_ids}\n        output.update({\n            folder[\"id\"]: _get_folder_item_from_entity(folder)\n            for folder in folders\n        })\n        return output\n\n    def get_folder_items_by_paths(self, project_name, folder_paths):\n        \"\"\"Get folder items by ids.\n\n        This function will query folders if they are not in cache. But the\n        queried items are not added to cache back.\n\n        Args:\n            project_name (str): Name of project where to look for folders.\n            folder_paths (Iterable[str]): Folder paths.\n\n        Returns:\n            dict[str, Union[FolderItem, None]]: Folder items by id.\n        \"\"\"\n\n        folder_paths = set(folder_paths)\n        output = {folder_path: None for folder_path in folder_paths}\n        if not folder_paths:\n            return output\n\n        if self._folders_items[project_name].is_valid:\n            cache_data = self._folders_items[project_name].get_data()\n            for folder_item in cache_data.values():\n                if folder_item.path in folder_paths:\n                    output[folder_item.path] = folder_item\n            return output\n        folders = ayon_api.get_folders(\n            project_name,\n            folder_paths=folder_paths,\n            fields=[\"id\", \"name\", \"label\", \"parentId\", \"path\", \"folderType\"]\n        )\n        # Make sure all folder ids are in output\n        for folder in folders:\n            item = _get_folder_item_from_entity(folder)\n            output[item.path] = item\n        return output\n\n    def get_folder_item(self, project_name, folder_id):\n        \"\"\"Get folder item by id.\n\n        This function will query folder if they is not in cache. But the\n        queried items are not added to cache back.\n\n        Args:\n            project_name (str): Name of project where to look for folders.\n            folder_id (str): Folder id.\n\n        Returns:\n            Union[FolderItem, None]: Folder item.\n        \"\"\"\n        items = self.get_folder_items_by_id(\n            project_name, [folder_id]\n        )\n        return items.get(folder_id)\n\n    def get_folder_item_by_path(self, project_name, folder_path):\n        \"\"\"Get folder item by path.\n\n        This function will query folder if they is not in cache. But the\n        queried items are not added to cache back.\n\n        Args:\n            project_name (str): Name of project where to look for folders.\n            folder_path (str): Folder path.\n\n        Returns:\n            Union[FolderItem, None]: Folder item.\n\n        \"\"\"\n        items = self.get_folder_items_by_paths(\n            project_name, [folder_path]\n        )\n        return items.get(folder_path)\n\n    def get_task_item_by_name(\n        self, project_name, folder_id, task_name, sender\n    ):\n        \"\"\"Get task item by name and folder id.\n\n        Args:\n            project_name (str): Project name.\n            folder_id (str): Folder id.\n            task_name (str): Task name.\n            sender (Union[str, None]): Who requested the task item.\n\n        Returns:\n            Optional[TaskItem]: Task item found by name and folder id.\n\n        \"\"\"\n        for task_item in self.get_task_items(project_name, folder_id, sender):\n            if task_item.name == task_name:\n                return task_item\n        return None\n\n    def get_task_items(self, project_name, folder_id, sender):\n        if not project_name or not folder_id:\n            return []\n\n        task_cache = self._task_items[project_name][folder_id]\n        if not task_cache.is_valid:\n            self._refresh_tasks_cache(project_name, folder_id, sender)\n        return task_cache.get_data()\n\n    def get_folder_entities(self, project_name, folder_ids):\n        \"\"\"Get folder entities by ids.\n\n        Args:\n            project_name (str): Project name.\n            folder_ids (Iterable[str]): Folder ids.\n\n        Returns:\n            dict[str, Any]: Folder entities by id.\n        \"\"\"\n\n        output = {}\n        folder_ids = set(folder_ids)\n        if not project_name or not folder_ids:\n            return output\n\n        folder_ids_to_query = set()\n        for folder_id in folder_ids:\n            cache = self._folders_by_id[project_name][folder_id]\n            if cache.is_valid:\n                output[folder_id] = cache.get_data()\n            elif folder_id:\n                folder_ids_to_query.add(folder_id)\n            else:\n                output[folder_id] = None\n        self._query_folder_entities(project_name, folder_ids_to_query)\n        for folder_id in folder_ids_to_query:\n            cache = self._folders_by_id[project_name][folder_id]\n            output[folder_id] = cache.get_data()\n        return output\n\n    def get_folder_entity(self, project_name, folder_id):\n        output = self.get_folder_entities(project_name, {folder_id})\n        return output[folder_id]\n\n    def get_task_entities(self, project_name, task_ids):\n        output = {}\n        task_ids = set(task_ids)\n        if not project_name or not task_ids:\n            return output\n\n        task_ids_to_query = set()\n        for task_id in task_ids:\n            cache = self._tasks_by_id[project_name][task_id]\n            if cache.is_valid:\n                output[task_id] = cache.get_data()\n            elif task_id:\n                task_ids_to_query.add(task_id)\n            else:\n                output[task_id] = None\n        self._query_task_entities(project_name, task_ids_to_query)\n        for task_id in task_ids_to_query:\n            cache = self._tasks_by_id[project_name][task_id]\n            output[task_id] = cache.get_data()\n        return output\n\n    def get_task_entity(self, project_name, task_id):\n        output = self.get_task_entities(project_name, {task_id})\n        return output[task_id]\n\n    def get_entity_ids_for_assignees(\n        self, project_name: str, assignees: list[str]\n    ):\n        folder_ids = set()\n        task_ids = set()\n        output = {\n            \"folder_ids\": folder_ids,\n            \"task_ids\": task_ids,\n        }\n        assignees = set(assignees)\n        for assignee in tuple(assignees):\n            cache = self._entity_ids_by_assignee[project_name][assignee]\n            if cache.is_valid:\n                assignees.discard(assignee)\n                assignee_data = cache.get_data()\n                folder_ids.update(assignee_data[\"folder_ids\"])\n                task_ids.update(assignee_data[\"task_ids\"])\n\n        if not assignees:\n            return output\n\n        tasks = ayon_api.get_tasks(\n            project_name,\n            assignees_all=assignees,\n            fields={\"id\", \"folderId\", \"assignees\"},\n        )\n        tasks_assignee = {}\n        for task in tasks:\n            folder_ids.add(task[\"folderId\"])\n            task_ids.add(task[\"id\"])\n            for assignee in task[\"assignees\"]:\n                tasks_assignee.setdefault(assignee, []).append(task)\n\n        for assignee, tasks in tasks_assignee.items():\n            cache = self._entity_ids_by_assignee[project_name][assignee]\n            assignee_folder_ids = set()\n            assignee_task_ids = set()\n            assignee_data = {\n                \"folder_ids\": assignee_folder_ids,\n                \"task_ids\": assignee_task_ids,\n            }\n            for task in tasks:\n                assignee_folder_ids.add(task[\"folderId\"])\n                assignee_task_ids.add(task[\"id\"])\n            cache.update_data(assignee_data)\n\n        return output\n\n    def get_available_tags_by_entity_type(\n        self, project_name: str\n    ) -&gt; dict[str, list[str]]:\n        \"\"\"Get available tags for all entity types in a project.\"\"\"\n        cache = self._tags_by_entity_type.get(project_name)\n        if not cache.is_valid:\n            tags = None\n            if project_name:\n                response = ayon_api.get(f\"projects/{project_name}/tags\")\n                if response.status_code == 200:\n                    tags = response.data\n\n            # Fake empty tags\n            if tags is None:\n                tags = {\n                    \"folders\": [],\n                    \"tasks\": [],\n                    \"products\": [],\n                    \"versions\": [],\n                    \"representations\": [],\n                    \"workfiles\": []\n                }\n            cache.update_data(tags)\n        return cache.get_data()\n\n    @contextlib.contextmanager\n    def _folder_refresh_event_manager(self, project_name, sender):\n        self._folders_refreshing.add(project_name)\n        self._controller.emit_event(\n            \"folders.refresh.started\",\n            {\"project_name\": project_name, \"sender\": sender},\n            HIERARCHY_MODEL_SENDER\n        )\n        try:\n            yield\n\n        finally:\n            self._controller.emit_event(\n                \"folders.refresh.finished\",\n                {\"project_name\": project_name, \"sender\": sender},\n                HIERARCHY_MODEL_SENDER\n            )\n            self._folders_refreshing.remove(project_name)\n\n    @contextlib.contextmanager\n    def _task_refresh_event_manager(\n        self, project_name, folder_id, sender\n    ):\n        self._tasks_refreshing.add(folder_id)\n        self._controller.emit_event(\n            \"tasks.refresh.started\",\n            {\n                \"project_name\": project_name,\n                \"folder_id\": folder_id,\n                \"sender\": sender,\n            },\n            HIERARCHY_MODEL_SENDER\n        )\n        try:\n            yield\n\n        finally:\n            self._controller.emit_event(\n                \"tasks.refresh.finished\",\n                {\n                    \"project_name\": project_name,\n                    \"folder_id\": folder_id,\n                    \"sender\": sender,\n                },\n                HIERARCHY_MODEL_SENDER\n            )\n            self._tasks_refreshing.discard(folder_id)\n\n    def _refresh_folders_cache(self, project_name, sender=None):\n        if project_name in self._folders_refreshing:\n            return\n\n        with self._folder_refresh_event_manager(project_name, sender):\n            folder_items = self._query_folders(project_name)\n            self._folders_items[project_name].update_data(folder_items)\n\n    def _query_folders(self, project_name):\n        hierarchy = ayon_api.get_folders_hierarchy(project_name)\n\n        folder_items = {}\n        hierachy_queue = collections.deque(hierarchy[\"hierarchy\"])\n        while hierachy_queue:\n            item = hierachy_queue.popleft()\n            folder_item = _get_folder_item_from_hierarchy_item(item)\n            folder_items[folder_item.entity_id] = folder_item\n            hierachy_queue.extend(item[\"children\"] or [])\n        return folder_items\n\n    def _query_folder_entities(self, project_name, folder_ids):\n        if not project_name or not folder_ids:\n            return\n        project_cache = self._folders_by_id[project_name]\n        folders = ayon_api.get_folders(project_name, folder_ids=folder_ids)\n        for folder in folders:\n            folder_id = folder[\"id\"]\n            project_cache[folder_id].update_data(folder)\n\n    def _query_task_entities(self, project_name, task_ids):\n        if not project_name or not task_ids:\n            return\n\n        project_cache = self._tasks_by_id[project_name]\n        tasks = ayon_api.get_tasks(project_name, task_ids=task_ids)\n        for task in tasks:\n            task_id = task[\"id\"]\n            project_cache[task_id].update_data(task)\n\n    def _refresh_tasks_cache(self, project_name, folder_id, sender=None):\n        if folder_id in self._tasks_refreshing:\n            while folder_id in self._tasks_refreshing:\n                time.sleep(0.01)\n            return\n\n        cache = self._task_items[project_name][folder_id]\n        with self._task_refresh_event_manager(\n            project_name, folder_id, sender\n        ):\n            cache.update_data(self._query_tasks(project_name, folder_id))\n\n    def _query_tasks(self, project_name, folder_id):\n        tasks = list(ayon_api.get_tasks(\n            project_name,\n            folder_ids=[folder_id],\n            fields={\"id\", \"name\", \"label\", \"folderId\", \"type\", \"tags\"}\n        ))\n        task_type_items = self._controller.get_task_type_items(project_name)\n        return _get_task_items_from_tasks(task_type_items, tasks)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/index.html#client.ayon_core.tools.common_models.HierarchyModel.get_available_tags_by_entity_type","title":"<code>get_available_tags_by_entity_type(project_name)</code>","text":"<p>Get available tags for all entity types in a project.</p> Source code in <code>client/ayon_core/tools/common_models/hierarchy.py</code> <pre><code>def get_available_tags_by_entity_type(\n    self, project_name: str\n) -&gt; dict[str, list[str]]:\n    \"\"\"Get available tags for all entity types in a project.\"\"\"\n    cache = self._tags_by_entity_type.get(project_name)\n    if not cache.is_valid:\n        tags = None\n        if project_name:\n            response = ayon_api.get(f\"projects/{project_name}/tags\")\n            if response.status_code == 200:\n                tags = response.data\n\n        # Fake empty tags\n        if tags is None:\n            tags = {\n                \"folders\": [],\n                \"tasks\": [],\n                \"products\": [],\n                \"versions\": [],\n                \"representations\": [],\n                \"workfiles\": []\n            }\n        cache.update_data(tags)\n    return cache.get_data()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/index.html#client.ayon_core.tools.common_models.HierarchyModel.get_folder_entities","title":"<code>get_folder_entities(project_name, folder_ids)</code>","text":"<p>Get folder entities by ids.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>folder_ids</code> <code>Iterable[str]</code> <p>Folder ids.</p> required <p>Returns:</p> Type Description <p>dict[str, Any]: Folder entities by id.</p> Source code in <code>client/ayon_core/tools/common_models/hierarchy.py</code> <pre><code>def get_folder_entities(self, project_name, folder_ids):\n    \"\"\"Get folder entities by ids.\n\n    Args:\n        project_name (str): Project name.\n        folder_ids (Iterable[str]): Folder ids.\n\n    Returns:\n        dict[str, Any]: Folder entities by id.\n    \"\"\"\n\n    output = {}\n    folder_ids = set(folder_ids)\n    if not project_name or not folder_ids:\n        return output\n\n    folder_ids_to_query = set()\n    for folder_id in folder_ids:\n        cache = self._folders_by_id[project_name][folder_id]\n        if cache.is_valid:\n            output[folder_id] = cache.get_data()\n        elif folder_id:\n            folder_ids_to_query.add(folder_id)\n        else:\n            output[folder_id] = None\n    self._query_folder_entities(project_name, folder_ids_to_query)\n    for folder_id in folder_ids_to_query:\n        cache = self._folders_by_id[project_name][folder_id]\n        output[folder_id] = cache.get_data()\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/index.html#client.ayon_core.tools.common_models.HierarchyModel.get_folder_item","title":"<code>get_folder_item(project_name, folder_id)</code>","text":"<p>Get folder item by id.</p> <p>This function will query folder if they is not in cache. But the queried items are not added to cache back.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Name of project where to look for folders.</p> required <code>folder_id</code> <code>str</code> <p>Folder id.</p> required <p>Returns:</p> Type Description <p>Union[FolderItem, None]: Folder item.</p> Source code in <code>client/ayon_core/tools/common_models/hierarchy.py</code> <pre><code>def get_folder_item(self, project_name, folder_id):\n    \"\"\"Get folder item by id.\n\n    This function will query folder if they is not in cache. But the\n    queried items are not added to cache back.\n\n    Args:\n        project_name (str): Name of project where to look for folders.\n        folder_id (str): Folder id.\n\n    Returns:\n        Union[FolderItem, None]: Folder item.\n    \"\"\"\n    items = self.get_folder_items_by_id(\n        project_name, [folder_id]\n    )\n    return items.get(folder_id)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/index.html#client.ayon_core.tools.common_models.HierarchyModel.get_folder_item_by_path","title":"<code>get_folder_item_by_path(project_name, folder_path)</code>","text":"<p>Get folder item by path.</p> <p>This function will query folder if they is not in cache. But the queried items are not added to cache back.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Name of project where to look for folders.</p> required <code>folder_path</code> <code>str</code> <p>Folder path.</p> required <p>Returns:</p> Type Description <p>Union[FolderItem, None]: Folder item.</p> Source code in <code>client/ayon_core/tools/common_models/hierarchy.py</code> <pre><code>def get_folder_item_by_path(self, project_name, folder_path):\n    \"\"\"Get folder item by path.\n\n    This function will query folder if they is not in cache. But the\n    queried items are not added to cache back.\n\n    Args:\n        project_name (str): Name of project where to look for folders.\n        folder_path (str): Folder path.\n\n    Returns:\n        Union[FolderItem, None]: Folder item.\n\n    \"\"\"\n    items = self.get_folder_items_by_paths(\n        project_name, [folder_path]\n    )\n    return items.get(folder_path)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/index.html#client.ayon_core.tools.common_models.HierarchyModel.get_folder_items","title":"<code>get_folder_items(project_name, sender)</code>","text":"<p>Get folder items by project name.</p> <p>The folders are cached per project name. If the cache is not valid then the folders are queried from server.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Name of project where to look for folders.</p> required <code>sender</code> <code>Union[str, None]</code> <p>Who requested the folder ids.</p> required <p>Returns:</p> Type Description <p>dict[str, FolderItem]: Folder items by id.</p> Source code in <code>client/ayon_core/tools/common_models/hierarchy.py</code> <pre><code>def get_folder_items(self, project_name, sender):\n    \"\"\"Get folder items by project name.\n\n    The folders are cached per project name. If the cache is not valid\n    then the folders are queried from server.\n\n    Args:\n        project_name (str): Name of project where to look for folders.\n        sender (Union[str, None]): Who requested the folder ids.\n\n    Returns:\n        dict[str, FolderItem]: Folder items by id.\n    \"\"\"\n\n    if not self._folders_items[project_name].is_valid:\n        self._refresh_folders_cache(project_name, sender)\n    return self._folders_items[project_name].get_data()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/index.html#client.ayon_core.tools.common_models.HierarchyModel.get_folder_items_by_id","title":"<code>get_folder_items_by_id(project_name, folder_ids)</code>","text":"<p>Get folder items by ids.</p> <p>This function will query folders if they are not in cache. But the queried items are not added to cache back.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Name of project where to look for folders.</p> required <code>folder_ids</code> <code>Iterable[str]</code> <p>Folder ids.</p> required <p>Returns:</p> Type Description <p>dict[str, Union[FolderItem, None]]: Folder items by id.</p> Source code in <code>client/ayon_core/tools/common_models/hierarchy.py</code> <pre><code>def get_folder_items_by_id(self, project_name, folder_ids):\n    \"\"\"Get folder items by ids.\n\n    This function will query folders if they are not in cache. But the\n    queried items are not added to cache back.\n\n    Args:\n        project_name (str): Name of project where to look for folders.\n        folder_ids (Iterable[str]): Folder ids.\n\n    Returns:\n        dict[str, Union[FolderItem, None]]: Folder items by id.\n    \"\"\"\n\n    folder_ids = set(folder_ids)\n    if self._folders_items[project_name].is_valid:\n        cache_data = self._folders_items[project_name].get_data()\n        return {\n            folder_id: cache_data.get(folder_id)\n            for folder_id in folder_ids\n        }\n    folders = ayon_api.get_folders(\n        project_name,\n        folder_ids=folder_ids,\n        fields=[\"id\", \"name\", \"label\", \"parentId\", \"path\", \"folderType\"]\n    )\n    # Make sure all folder ids are in output\n    output = {folder_id: None for folder_id in folder_ids}\n    output.update({\n        folder[\"id\"]: _get_folder_item_from_entity(folder)\n        for folder in folders\n    })\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/index.html#client.ayon_core.tools.common_models.HierarchyModel.get_folder_items_by_paths","title":"<code>get_folder_items_by_paths(project_name, folder_paths)</code>","text":"<p>Get folder items by ids.</p> <p>This function will query folders if they are not in cache. But the queried items are not added to cache back.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Name of project where to look for folders.</p> required <code>folder_paths</code> <code>Iterable[str]</code> <p>Folder paths.</p> required <p>Returns:</p> Type Description <p>dict[str, Union[FolderItem, None]]: Folder items by id.</p> Source code in <code>client/ayon_core/tools/common_models/hierarchy.py</code> <pre><code>def get_folder_items_by_paths(self, project_name, folder_paths):\n    \"\"\"Get folder items by ids.\n\n    This function will query folders if they are not in cache. But the\n    queried items are not added to cache back.\n\n    Args:\n        project_name (str): Name of project where to look for folders.\n        folder_paths (Iterable[str]): Folder paths.\n\n    Returns:\n        dict[str, Union[FolderItem, None]]: Folder items by id.\n    \"\"\"\n\n    folder_paths = set(folder_paths)\n    output = {folder_path: None for folder_path in folder_paths}\n    if not folder_paths:\n        return output\n\n    if self._folders_items[project_name].is_valid:\n        cache_data = self._folders_items[project_name].get_data()\n        for folder_item in cache_data.values():\n            if folder_item.path in folder_paths:\n                output[folder_item.path] = folder_item\n        return output\n    folders = ayon_api.get_folders(\n        project_name,\n        folder_paths=folder_paths,\n        fields=[\"id\", \"name\", \"label\", \"parentId\", \"path\", \"folderType\"]\n    )\n    # Make sure all folder ids are in output\n    for folder in folders:\n        item = _get_folder_item_from_entity(folder)\n        output[item.path] = item\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/index.html#client.ayon_core.tools.common_models.HierarchyModel.get_task_item_by_name","title":"<code>get_task_item_by_name(project_name, folder_id, task_name, sender)</code>","text":"<p>Get task item by name and folder id.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>folder_id</code> <code>str</code> <p>Folder id.</p> required <code>task_name</code> <code>str</code> <p>Task name.</p> required <code>sender</code> <code>Union[str, None]</code> <p>Who requested the task item.</p> required <p>Returns:</p> Type Description <p>Optional[TaskItem]: Task item found by name and folder id.</p> Source code in <code>client/ayon_core/tools/common_models/hierarchy.py</code> <pre><code>def get_task_item_by_name(\n    self, project_name, folder_id, task_name, sender\n):\n    \"\"\"Get task item by name and folder id.\n\n    Args:\n        project_name (str): Project name.\n        folder_id (str): Folder id.\n        task_name (str): Task name.\n        sender (Union[str, None]): Who requested the task item.\n\n    Returns:\n        Optional[TaskItem]: Task item found by name and folder id.\n\n    \"\"\"\n    for task_item in self.get_task_items(project_name, folder_id, sender):\n        if task_item.name == task_name:\n            return task_item\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/index.html#client.ayon_core.tools.common_models.HierarchyModel.refresh_project","title":"<code>refresh_project(project_name)</code>","text":"<p>Force to refresh folder items for a project.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Name of project to refresh.</p> required Source code in <code>client/ayon_core/tools/common_models/hierarchy.py</code> <pre><code>def refresh_project(self, project_name):\n    \"\"\"Force to refresh folder items for a project.\n\n    Args:\n        project_name (str): Name of project to refresh.\n    \"\"\"\n\n    self._refresh_folders_cache(project_name)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/index.html#client.ayon_core.tools.common_models.ProjectItem","title":"<code>ProjectItem</code>  <code>dataclass</code>","text":"<p>Item representing folder entity on a server.</p> <p>Folder can be a child of another folder or a project.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Project name.</p> required <code>active</code> <code>Union[str, None]</code> <p>Parent folder id. If 'None' then project is parent.</p> required Source code in <code>client/ayon_core/tools/common_models/projects.py</code> <pre><code>@dataclass\nclass ProjectItem:\n    \"\"\"Item representing folder entity on a server.\n\n    Folder can be a child of another folder or a project.\n\n    Args:\n        name (str): Project name.\n        active (Union[str, None]): Parent folder id. If 'None' then project\n            is parent.\n    \"\"\"\n    name: str\n    active: bool\n    is_library: bool\n    icon: AwesomeFontIcon\n    is_pinned: bool = False\n\n    @classmethod\n    def from_entity(cls, project_entity: dict[str, Any]) -&gt; \"ProjectItem\":\n        \"\"\"Creates folder item from entity.\n\n        Args:\n            project_entity (dict[str, Any]): Project entity.\n\n        Returns:\n            ProjectItem: Project item.\n\n        \"\"\"\n        icon = AwesomeFontIcon(\n            \"fa.book\" if project_entity[\"library\"] else \"fa.map\",\n            color=get_default_entity_icon_color(),\n        )\n        return cls(\n            project_entity[\"name\"],\n            project_entity[\"active\"],\n            project_entity[\"library\"],\n            icon\n        )\n\n    def to_data(self):\n        \"\"\"Converts folder item to data.\n\n        Returns:\n            dict[str, Any]: Folder item data.\n        \"\"\"\n\n        return {\n            \"name\": self.name,\n            \"active\": self.active,\n            \"is_library\": self.is_library,\n            \"icon\": {\n                \"name\": self.icon.name,\n                \"color\": self.icon.color,\n            },\n        }\n\n    @classmethod\n    def from_data(cls, data):\n        \"\"\"Re-creates folder item from data.\n\n        Args:\n            data (dict[str, Any]): Folder item data.\n\n        Returns:\n            FolderItem: Folder item.\n        \"\"\"\n        icon = data[\"icon\"]\n        if isinstance(icon, dict):\n            data[\"icon\"] = AwesomeFontIcon(icon[\"name\"], color=icon[\"color\"])\n        return cls(**data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/index.html#client.ayon_core.tools.common_models.ProjectItem.from_data","title":"<code>from_data(data)</code>  <code>classmethod</code>","text":"<p>Re-creates folder item from data.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict[str, Any]</code> <p>Folder item data.</p> required <p>Returns:</p> Name Type Description <code>FolderItem</code> <p>Folder item.</p> Source code in <code>client/ayon_core/tools/common_models/projects.py</code> <pre><code>@classmethod\ndef from_data(cls, data):\n    \"\"\"Re-creates folder item from data.\n\n    Args:\n        data (dict[str, Any]): Folder item data.\n\n    Returns:\n        FolderItem: Folder item.\n    \"\"\"\n    icon = data[\"icon\"]\n    if isinstance(icon, dict):\n        data[\"icon\"] = AwesomeFontIcon(icon[\"name\"], color=icon[\"color\"])\n    return cls(**data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/index.html#client.ayon_core.tools.common_models.ProjectItem.from_entity","title":"<code>from_entity(project_entity)</code>  <code>classmethod</code>","text":"<p>Creates folder item from entity.</p> <p>Parameters:</p> Name Type Description Default <code>project_entity</code> <code>dict[str, Any]</code> <p>Project entity.</p> required <p>Returns:</p> Name Type Description <code>ProjectItem</code> <code>'ProjectItem'</code> <p>Project item.</p> Source code in <code>client/ayon_core/tools/common_models/projects.py</code> <pre><code>@classmethod\ndef from_entity(cls, project_entity: dict[str, Any]) -&gt; \"ProjectItem\":\n    \"\"\"Creates folder item from entity.\n\n    Args:\n        project_entity (dict[str, Any]): Project entity.\n\n    Returns:\n        ProjectItem: Project item.\n\n    \"\"\"\n    icon = AwesomeFontIcon(\n        \"fa.book\" if project_entity[\"library\"] else \"fa.map\",\n        color=get_default_entity_icon_color(),\n    )\n    return cls(\n        project_entity[\"name\"],\n        project_entity[\"active\"],\n        project_entity[\"library\"],\n        icon\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/index.html#client.ayon_core.tools.common_models.ProjectItem.to_data","title":"<code>to_data()</code>","text":"<p>Converts folder item to data.</p> <p>Returns:</p> Type Description <p>dict[str, Any]: Folder item data.</p> Source code in <code>client/ayon_core/tools/common_models/projects.py</code> <pre><code>def to_data(self):\n    \"\"\"Converts folder item to data.\n\n    Returns:\n        dict[str, Any]: Folder item data.\n    \"\"\"\n\n    return {\n        \"name\": self.name,\n        \"active\": self.active,\n        \"is_library\": self.is_library,\n        \"icon\": {\n            \"name\": self.icon.name,\n            \"color\": self.icon.color,\n        },\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/index.html#client.ayon_core.tools.common_models.ProjectsModel","title":"<code>ProjectsModel</code>","text":"<p>               Bases: <code>object</code></p> Source code in <code>client/ayon_core/tools/common_models/projects.py</code> <pre><code>class ProjectsModel(object):\n    def __init__(self, controller):\n        self._projects_cache = CacheItem(default_factory=list)\n        self._projects_by_name = NestedCacheItem(\n            levels=1, default_factory=list\n        )\n        self._product_type_icons_mapping = NestedCacheItem(\n            levels=1, default_factory=ProductTypeIconMapping\n        )\n        self._project_statuses_cache = {}\n        self._folder_types_cache = {}\n        self._task_types_cache = {}\n\n        self._is_refreshing = False\n        self._controller = controller\n\n    def reset(self):\n        self._project_statuses_cache = {}\n        self._folder_types_cache = {}\n        self._task_types_cache = {}\n        self._projects_cache.reset()\n        self._projects_by_name.reset()\n        self._product_type_icons_mapping.reset()\n\n    def refresh(self):\n        \"\"\"Refresh project items.\n\n        This method will requery list of ProjectItem returned by\n        'get_project_items'.\n\n        To reset all cached items use 'reset' method.\n        \"\"\"\n        self._refresh_projects_cache()\n\n    def get_project_items(self, sender):\n        \"\"\"\n\n        Args:\n            sender (str): Name of sender who asked for items.\n\n        Returns:\n            Union[list[ProjectItem], None]: List of project items, or None\n                if model is refreshing.\n        \"\"\"\n\n        if not self._projects_cache.is_valid:\n            return self._refresh_projects_cache(sender)\n        return self._projects_cache.get_data()\n\n    def get_project_entity(self, project_name):\n        \"\"\"Get project entity.\n\n        Args:\n            project_name (str): Project name.\n\n        Returns:\n            Union[dict[str, Any], None]: Project entity or None if project\n                was not found by name.\n\n        \"\"\"\n        project_cache = self._projects_by_name[project_name]\n        if not project_cache.is_valid:\n            entity = None\n            if project_name:\n                entity = ayon_api.get_rest_project(project_name)\n            project_cache.update_data(entity)\n        return project_cache.get_data()\n\n    def get_project_anatomy_tags(self, project_name: str) -&gt; list[TagItem]:\n        \"\"\"Get project anatomy tags.\n\n        Args:\n            project_name (str): Project name.\n\n        Returns:\n            list[TagItem]: Tag definitions.\n\n        \"\"\"\n        project_entity = self.get_project_entity(project_name)\n        return [\n            TagItem(tag[\"name\"], tag[\"color\"])\n            for tag in project_entity[\"tags\"]\n        ]\n\n    def get_project_status_items(self, project_name, sender):\n        \"\"\"Get project status items.\n\n        Args:\n            project_name (str): Project name.\n            sender (Union[str, None]): Name of sender who asked for items.\n\n        Returns:\n            list[StatusItem]: Status items for project.\n\n        \"\"\"\n        if project_name is None:\n            return []\n\n        statuses_cache = self._project_statuses_cache.get(project_name)\n        if (\n            statuses_cache is not None\n            and not self._projects_cache.is_valid\n        ):\n            statuses_cache = None\n\n        if statuses_cache is None:\n            with self._project_items_refresh_event_manager(\n                sender, project_name, \"statuses\"\n            ):\n                project_entity = self.get_project_entity(project_name)\n                statuses = []\n                if project_entity:\n                    statuses = [\n                        StatusItem.from_project_item(status)\n                        for status in project_entity[\"statuses\"]\n                    ]\n                statuses_cache = statuses\n            self._project_statuses_cache[project_name] = statuses_cache\n        return list(statuses_cache)\n\n    def get_folder_type_items(self, project_name, sender):\n        \"\"\"Get project status items.\n\n        Args:\n            project_name (str): Project name.\n            sender (Union[str, None]): Name of sender who asked for items.\n\n        Returns:\n            list[FolderType]: Folder type items for project.\n\n        \"\"\"\n        return self._get_project_items(\n            project_name,\n            sender,\n            \"folder_types\",\n            self._folder_types_cache,\n            self._folder_type_items_getter,\n        )\n\n    def get_task_type_items(self, project_name, sender):\n        \"\"\"Get project task type items.\n\n        Args:\n            project_name (str): Project name.\n            sender (Union[str, None]): Name of sender who asked for items.\n\n        Returns:\n            list[TaskTypeItem]: Task type items for project.\n\n        \"\"\"\n        return self._get_project_items(\n            project_name,\n            sender,\n            \"task_types\",\n            self._task_types_cache,\n            self._task_type_items_getter,\n        )\n\n    def get_product_type_icons_mapping(\n        self, project_name: Optional[str]\n    ) -&gt; ProductTypeIconMapping:\n        cache = self._product_type_icons_mapping[project_name]\n        if cache.is_valid:\n            return cache.get_data()\n\n        project_entity = self.get_project_entity(project_name)\n        icons_mapping = ProductTypeIconMapping()\n        if project_entity:\n            product_base_types = (\n                project_entity[\"config\"].get(\"productBaseTypes\", {})\n            )\n            icons_mapping = ProductTypeIconMapping(\n                product_base_types.get(\"default\"),\n                product_base_types.get(\"definitions\")\n            )\n\n        cache.update_data(icons_mapping)\n        return icons_mapping\n\n    def _get_project_items(\n        self, project_name, sender, item_type, cache_obj, getter\n    ):\n        if (\n            project_name in cache_obj\n            and (\n                project_name is None\n                or self._projects_by_name[project_name].is_valid\n            )\n        ):\n            return cache_obj[project_name]\n\n        with self._project_items_refresh_event_manager(\n            sender, project_name, item_type\n        ):\n            cache_value = getter(self.get_project_entity(project_name))\n        cache_obj[project_name] = cache_value\n        return cache_value\n\n    @contextlib.contextmanager\n    def _project_refresh_event_manager(self, sender):\n        self._is_refreshing = True\n        self._controller.emit_event(\n            \"projects.refresh.started\",\n            {\"sender\": sender},\n            PROJECTS_MODEL_SENDER\n        )\n        try:\n            yield\n\n        finally:\n            self._controller.emit_event(\n                \"projects.refresh.finished\",\n                {\"sender\": sender},\n                PROJECTS_MODEL_SENDER\n            )\n            self._is_refreshing = False\n\n    @contextlib.contextmanager\n    def _project_items_refresh_event_manager(\n        self, sender, project_name, item_type\n    ):\n        self._controller.emit_event(\n            f\"projects.{item_type}.refresh.started\",\n            {\"sender\": sender, \"project_name\": project_name},\n            PROJECTS_MODEL_SENDER\n        )\n        try:\n            yield\n\n        finally:\n            self._controller.emit_event(\n                f\"projects.{item_type}.refresh.finished\",\n                {\"sender\": sender, \"project_name\": project_name},\n                PROJECTS_MODEL_SENDER\n            )\n\n    def _refresh_projects_cache(self, sender=None):\n        if self._is_refreshing:\n            return None\n\n        with self._project_refresh_event_manager(sender):\n            project_items = self._query_projects()\n            self._projects_cache.update_data(project_items)\n        return self._projects_cache.get_data()\n\n    def _fetch_graphql_projects(self) -&gt; list[dict[str, Any]]:\n        \"\"\"Fetch projects using GraphQl.\n\n        This method was added because ayon_api had a bug in 'get_projects'.\n\n        Returns:\n            list[dict[str, Any]]: List of projects.\n\n        \"\"\"\n        api = ayon_api.get_server_api_connection()\n        query = projects_graphql_query({\"name\", \"active\", \"library\", \"data\"})\n\n        projects = []\n        for parsed_data in query.continuous_query(api):\n            for project in parsed_data[\"projects\"]:\n                project_data = project[\"data\"]\n                if project_data is None:\n                    project[\"data\"] = {}\n                elif isinstance(project_data, str):\n                    project[\"data\"] = json.loads(project_data)\n                projects.append(project)\n        return projects\n\n    def _query_projects(self) -&gt; list[ProjectItem]:\n        projects = self._fetch_graphql_projects()\n\n        user = ayon_api.get_user()\n        pinned_projects = (\n            user\n            .get(\"data\", {})\n            .get(\"frontendPreferences\", {})\n            .get(\"pinnedProjects\")\n        ) or []\n        pinned_projects = set(pinned_projects)\n        project_items = _get_project_items_from_entity(list(projects))\n        for project in project_items:\n            project.is_pinned = project.name in pinned_projects\n        return project_items\n\n    def _status_items_getter(self, project_entity):\n        if not project_entity:\n            return []\n        return [\n            StatusItem.from_project_item(status)\n            for status in project_entity[\"statuses\"]\n        ]\n\n    def _folder_type_items_getter(self, project_entity):\n        if not project_entity:\n            return []\n        return [\n            FolderTypeItem.from_project_item(folder_type)\n            for folder_type in project_entity[\"folderTypes\"]\n        ]\n\n    def _task_type_items_getter(self, project_entity):\n        if not project_entity:\n            return []\n        return [\n            TaskTypeItem.from_project_item(task_type)\n            for task_type in project_entity[\"taskTypes\"]\n        ]\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/index.html#client.ayon_core.tools.common_models.ProjectsModel.get_folder_type_items","title":"<code>get_folder_type_items(project_name, sender)</code>","text":"<p>Get project status items.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>sender</code> <code>Union[str, None]</code> <p>Name of sender who asked for items.</p> required <p>Returns:</p> Type Description <p>list[FolderType]: Folder type items for project.</p> Source code in <code>client/ayon_core/tools/common_models/projects.py</code> <pre><code>def get_folder_type_items(self, project_name, sender):\n    \"\"\"Get project status items.\n\n    Args:\n        project_name (str): Project name.\n        sender (Union[str, None]): Name of sender who asked for items.\n\n    Returns:\n        list[FolderType]: Folder type items for project.\n\n    \"\"\"\n    return self._get_project_items(\n        project_name,\n        sender,\n        \"folder_types\",\n        self._folder_types_cache,\n        self._folder_type_items_getter,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/index.html#client.ayon_core.tools.common_models.ProjectsModel.get_project_anatomy_tags","title":"<code>get_project_anatomy_tags(project_name)</code>","text":"<p>Get project anatomy tags.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <p>Returns:</p> Type Description <code>list[TagItem]</code> <p>list[TagItem]: Tag definitions.</p> Source code in <code>client/ayon_core/tools/common_models/projects.py</code> <pre><code>def get_project_anatomy_tags(self, project_name: str) -&gt; list[TagItem]:\n    \"\"\"Get project anatomy tags.\n\n    Args:\n        project_name (str): Project name.\n\n    Returns:\n        list[TagItem]: Tag definitions.\n\n    \"\"\"\n    project_entity = self.get_project_entity(project_name)\n    return [\n        TagItem(tag[\"name\"], tag[\"color\"])\n        for tag in project_entity[\"tags\"]\n    ]\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/index.html#client.ayon_core.tools.common_models.ProjectsModel.get_project_entity","title":"<code>get_project_entity(project_name)</code>","text":"<p>Get project entity.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <p>Returns:</p> Type Description <p>Union[dict[str, Any], None]: Project entity or None if project was not found by name.</p> Source code in <code>client/ayon_core/tools/common_models/projects.py</code> <pre><code>def get_project_entity(self, project_name):\n    \"\"\"Get project entity.\n\n    Args:\n        project_name (str): Project name.\n\n    Returns:\n        Union[dict[str, Any], None]: Project entity or None if project\n            was not found by name.\n\n    \"\"\"\n    project_cache = self._projects_by_name[project_name]\n    if not project_cache.is_valid:\n        entity = None\n        if project_name:\n            entity = ayon_api.get_rest_project(project_name)\n        project_cache.update_data(entity)\n    return project_cache.get_data()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/index.html#client.ayon_core.tools.common_models.ProjectsModel.get_project_items","title":"<code>get_project_items(sender)</code>","text":"<p>Parameters:</p> Name Type Description Default <code>sender</code> <code>str</code> <p>Name of sender who asked for items.</p> required <p>Returns:</p> Type Description <p>Union[list[ProjectItem], None]: List of project items, or None if model is refreshing.</p> Source code in <code>client/ayon_core/tools/common_models/projects.py</code> <pre><code>def get_project_items(self, sender):\n    \"\"\"\n\n    Args:\n        sender (str): Name of sender who asked for items.\n\n    Returns:\n        Union[list[ProjectItem], None]: List of project items, or None\n            if model is refreshing.\n    \"\"\"\n\n    if not self._projects_cache.is_valid:\n        return self._refresh_projects_cache(sender)\n    return self._projects_cache.get_data()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/index.html#client.ayon_core.tools.common_models.ProjectsModel.get_project_status_items","title":"<code>get_project_status_items(project_name, sender)</code>","text":"<p>Get project status items.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>sender</code> <code>Union[str, None]</code> <p>Name of sender who asked for items.</p> required <p>Returns:</p> Type Description <p>list[StatusItem]: Status items for project.</p> Source code in <code>client/ayon_core/tools/common_models/projects.py</code> <pre><code>def get_project_status_items(self, project_name, sender):\n    \"\"\"Get project status items.\n\n    Args:\n        project_name (str): Project name.\n        sender (Union[str, None]): Name of sender who asked for items.\n\n    Returns:\n        list[StatusItem]: Status items for project.\n\n    \"\"\"\n    if project_name is None:\n        return []\n\n    statuses_cache = self._project_statuses_cache.get(project_name)\n    if (\n        statuses_cache is not None\n        and not self._projects_cache.is_valid\n    ):\n        statuses_cache = None\n\n    if statuses_cache is None:\n        with self._project_items_refresh_event_manager(\n            sender, project_name, \"statuses\"\n        ):\n            project_entity = self.get_project_entity(project_name)\n            statuses = []\n            if project_entity:\n                statuses = [\n                    StatusItem.from_project_item(status)\n                    for status in project_entity[\"statuses\"]\n                ]\n            statuses_cache = statuses\n        self._project_statuses_cache[project_name] = statuses_cache\n    return list(statuses_cache)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/index.html#client.ayon_core.tools.common_models.ProjectsModel.get_task_type_items","title":"<code>get_task_type_items(project_name, sender)</code>","text":"<p>Get project task type items.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>sender</code> <code>Union[str, None]</code> <p>Name of sender who asked for items.</p> required <p>Returns:</p> Type Description <p>list[TaskTypeItem]: Task type items for project.</p> Source code in <code>client/ayon_core/tools/common_models/projects.py</code> <pre><code>def get_task_type_items(self, project_name, sender):\n    \"\"\"Get project task type items.\n\n    Args:\n        project_name (str): Project name.\n        sender (Union[str, None]): Name of sender who asked for items.\n\n    Returns:\n        list[TaskTypeItem]: Task type items for project.\n\n    \"\"\"\n    return self._get_project_items(\n        project_name,\n        sender,\n        \"task_types\",\n        self._task_types_cache,\n        self._task_type_items_getter,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/index.html#client.ayon_core.tools.common_models.ProjectsModel.refresh","title":"<code>refresh()</code>","text":"<p>Refresh project items.</p> <p>This method will requery list of ProjectItem returned by 'get_project_items'.</p> <p>To reset all cached items use 'reset' method.</p> Source code in <code>client/ayon_core/tools/common_models/projects.py</code> <pre><code>def refresh(self):\n    \"\"\"Refresh project items.\n\n    This method will requery list of ProjectItem returned by\n    'get_project_items'.\n\n    To reset all cached items use 'reset' method.\n    \"\"\"\n    self._refresh_projects_cache()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/index.html#client.ayon_core.tools.common_models.StatusItem","title":"<code>StatusItem</code>","text":"<p>Item representing status of project.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Status name (\"Not ready\").</p> required <code>color</code> <code>str</code> <p>Status color in hex (\"#434a56\").</p> required <code>short</code> <code>str</code> <p>Short status name (\"NRD\").</p> required <code>icon</code> <code>str</code> <p>Icon name in MaterialIcons (\"fiber_new\").</p> required <code>state</code> <code>str</code> <p>Status state.</p> required Source code in <code>client/ayon_core/tools/common_models/projects.py</code> <pre><code>class StatusItem:\n    \"\"\"Item representing status of project.\n\n    Args:\n        name (str): Status name (\"Not ready\").\n        color (str): Status color in hex (\"#434a56\").\n        short (str): Short status name (\"NRD\").\n        icon (str): Icon name in MaterialIcons (\"fiber_new\").\n        state (str): Status state.\n\n    \"\"\"\n    def __init__(\n        self,\n        name: str,\n        color: str,\n        short: str,\n        icon: str,\n        state: str\n    ):\n        self.name: str = name\n        self.color: str = color\n        self.short: str = short\n        self.icon: str = icon\n        self.state: str = state\n\n    def to_data(self) -&gt; dict[str, Any]:\n        return {\n            \"name\": self.name,\n            \"color\": self.color,\n            \"short\": self.short,\n            \"icon\": self.icon,\n            \"state\": self.state,\n        }\n\n    @classmethod\n    def from_data(cls, data):\n        return cls(**data)\n\n    @classmethod\n    def from_project_item(cls, status_data):\n        return cls(\n            name=status_data[\"name\"],\n            color=status_data[\"color\"],\n            short=status_data[\"shortName\"],\n            icon=status_data[\"icon\"],\n            state=status_data[\"state\"],\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/index.html#client.ayon_core.tools.common_models.TagItem","title":"<code>TagItem</code>  <code>dataclass</code>","text":"<p>Tag definition set on project anatomy.</p> Source code in <code>client/ayon_core/tools/common_models/projects.py</code> <pre><code>@dataclass\nclass TagItem:\n    \"\"\"Tag definition set on project anatomy.\"\"\"\n    name: str\n    color: str\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/index.html#client.ayon_core.tools.common_models.TaskItem","title":"<code>TaskItem</code>","text":"<p>Task item representing task entity on a server.</p> <p>Task is child of a folder.</p> <p>Task item has label that is used for display in UI. The label is by     default using task name and type.</p> <p>Parameters:</p> Name Type Description Default <code>task_id</code> <code>str</code> <p>Task id.</p> required <code>name</code> <code>str</code> <p>Name of task.</p> required <code>name</code> <code>str | None</code> <p>Task label.</p> required <code>task_type</code> <code>str</code> <p>Type of task.</p> required <code>parent_id</code> <code>str</code> <p>Parent folder id.</p> required Source code in <code>client/ayon_core/tools/common_models/hierarchy.py</code> <pre><code>class TaskItem:\n    \"\"\"Task item representing task entity on a server.\n\n    Task is child of a folder.\n\n    Task item has label that is used for display in UI. The label is by\n        default using task name and type.\n\n    Args:\n        task_id (str): Task id.\n        name (str): Name of task.\n        name (str | None): Task label.\n        task_type (str): Type of task.\n        parent_id (str): Parent folder id.\n    \"\"\"\n\n    def __init__(\n        self,\n        task_id: str,\n        name: str,\n        label: str | None,\n        task_type: str,\n        task_type_order: int,\n        parent_id: str,\n        tags: list[str],\n    ):\n        self.task_id = task_id\n        self.name = name\n        self.label = label\n        self.task_type = task_type\n        self.task_type_order = task_type_order\n        self.parent_id = parent_id\n        self.tags = tags\n\n        self._full_label = None\n\n    @property\n    def id(self):\n        \"\"\"Alias for task_id.\n\n        Returns:\n            str: Task id.\n        \"\"\"\n\n        return self.task_id\n\n    @property\n    def full_label(self):\n        \"\"\"Label of task item for UI.\n\n        Returns:\n            str: Label of task item.\n        \"\"\"\n\n        if self._full_label is None:\n            label = self.label or self.name\n            self._full_label = f\"{label} ({self.task_type})\"\n        return self._full_label\n\n    def to_data(self):\n        \"\"\"Converts task item to data.\n\n        Returns:\n            dict[str, Any]: Task item data.\n        \"\"\"\n\n        return {\n            \"task_id\": self.task_id,\n            \"name\": self.name,\n            \"label\": self.label,\n            \"parent_id\": self.parent_id,\n            \"task_type\": self.task_type,\n            \"task_type_order\": self.task_type_order,\n            \"tags\": self.tags,\n        }\n\n    @classmethod\n    def from_data(cls, data):\n        \"\"\"Re-create task item from data.\n\n        Args:\n            data (dict[str, Any]): Task item data.\n\n        Returns:\n            TaskItem: Task item.\n        \"\"\"\n\n        return cls(**data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/index.html#client.ayon_core.tools.common_models.TaskItem.full_label","title":"<code>full_label</code>  <code>property</code>","text":"<p>Label of task item for UI.</p> <p>Returns:</p> Name Type Description <code>str</code> <p>Label of task item.</p>"},{"location":"autoapi/client/ayon_core/tools/common_models/index.html#client.ayon_core.tools.common_models.TaskItem.id","title":"<code>id</code>  <code>property</code>","text":"<p>Alias for task_id.</p> <p>Returns:</p> Name Type Description <code>str</code> <p>Task id.</p>"},{"location":"autoapi/client/ayon_core/tools/common_models/index.html#client.ayon_core.tools.common_models.TaskItem.from_data","title":"<code>from_data(data)</code>  <code>classmethod</code>","text":"<p>Re-create task item from data.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict[str, Any]</code> <p>Task item data.</p> required <p>Returns:</p> Name Type Description <code>TaskItem</code> <p>Task item.</p> Source code in <code>client/ayon_core/tools/common_models/hierarchy.py</code> <pre><code>@classmethod\ndef from_data(cls, data):\n    \"\"\"Re-create task item from data.\n\n    Args:\n        data (dict[str, Any]): Task item data.\n\n    Returns:\n        TaskItem: Task item.\n    \"\"\"\n\n    return cls(**data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/index.html#client.ayon_core.tools.common_models.TaskItem.to_data","title":"<code>to_data()</code>","text":"<p>Converts task item to data.</p> <p>Returns:</p> Type Description <p>dict[str, Any]: Task item data.</p> Source code in <code>client/ayon_core/tools/common_models/hierarchy.py</code> <pre><code>def to_data(self):\n    \"\"\"Converts task item to data.\n\n    Returns:\n        dict[str, Any]: Task item data.\n    \"\"\"\n\n    return {\n        \"task_id\": self.task_id,\n        \"name\": self.name,\n        \"label\": self.label,\n        \"parent_id\": self.parent_id,\n        \"task_type\": self.task_type,\n        \"task_type_order\": self.task_type_order,\n        \"tags\": self.tags,\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/index.html#client.ayon_core.tools.common_models.TaskTypeItem","title":"<code>TaskTypeItem</code>","text":"<p>Item representing task type of project.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Task type name (\"Shot\").</p> required <code>short</code> <code>str</code> <p>Short task type name (\"sh\").</p> required <code>icon</code> <code>str</code> <p>Icon name in MaterialIcons (\"fiber_new\").</p> required Source code in <code>client/ayon_core/tools/common_models/projects.py</code> <pre><code>class TaskTypeItem:\n    \"\"\"Item representing task type of project.\n\n    Args:\n        name (str): Task type name (\"Shot\").\n        short (str): Short task type name (\"sh\").\n        icon (str): Icon name in MaterialIcons (\"fiber_new\").\n\n    \"\"\"\n    def __init__(\n        self,\n        name: str,\n        short: str,\n        icon: str,\n        color: Optional[str],\n    ):\n        self.name = name\n        self.short = short\n        self.icon = icon\n        self.color = color\n\n    def to_data(self):\n        return {\n            \"name\": self.name,\n            \"short\": self.short,\n            \"icon\": self.icon,\n            \"color\": self.color,\n        }\n\n    @classmethod\n    def from_data(cls, data):\n        return cls(**data)\n\n    @classmethod\n    def from_project_item(cls, task_type_data):\n        return cls(\n            name=task_type_data[\"name\"],\n            short=task_type_data[\"shortName\"],\n            icon=task_type_data[\"icon\"],\n            color=task_type_data.get(\"color\"),\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/index.html#client.ayon_core.tools.common_models.UsersModel","title":"<code>UsersModel</code>","text":"Source code in <code>client/ayon_core/tools/common_models/users.py</code> <pre><code>class UsersModel:\n    def __init__(self, controller):\n        self._current_username = NOT_SET\n        self._controller = controller\n        self._users_cache = NestedCacheItem(default_factory=list)\n\n    def get_current_username(self) -&gt; Optional[str]:\n        if self._current_username is NOT_SET:\n            self._current_username = get_ayon_username()\n        return self._current_username\n\n    def reset(self) -&gt; None:\n        self._users_cache.reset()\n\n    def get_user_items(self, project_name):\n        \"\"\"Get user items.\n\n        Returns:\n            List[UserItem]: List of user items.\n\n        \"\"\"\n        self._invalidate_cache(project_name)\n        return self._users_cache[project_name].get_data()\n\n    def get_user_items_by_name(self, project_name):\n        \"\"\"Get user items by name.\n\n        Implemented as most of cases using this model will need to find\n            user information by username.\n\n        Returns:\n            Dict[str, UserItem]: Dictionary of user items by name.\n\n        \"\"\"\n        return {\n            user_item.username: user_item\n            for user_item in self.get_user_items(project_name)\n        }\n\n    def get_user_item_by_username(self, project_name, username):\n        \"\"\"Get user item by username.\n\n        Args:\n            username (str): Username.\n\n        Returns:\n            Union[UserItem, None]: User item or None if not found.\n\n        \"\"\"\n        self._invalidate_cache(project_name)\n        for user_item in self.get_user_items(project_name):\n            if user_item.username == username:\n                return user_item\n        return None\n\n    def _invalidate_cache(self, project_name):\n        cache = self._users_cache[project_name]\n        if cache.is_valid:\n            return\n\n        if project_name is None:\n            cache.update_data([])\n            return\n\n        self._users_cache[project_name].update_data([\n            UserItem.from_entity_data(user)\n            for user in ayon_api.get_users(project_name=project_name)\n        ])\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/index.html#client.ayon_core.tools.common_models.UsersModel.get_user_item_by_username","title":"<code>get_user_item_by_username(project_name, username)</code>","text":"<p>Get user item by username.</p> <p>Parameters:</p> Name Type Description Default <code>username</code> <code>str</code> <p>Username.</p> required <p>Returns:</p> Type Description <p>Union[UserItem, None]: User item or None if not found.</p> Source code in <code>client/ayon_core/tools/common_models/users.py</code> <pre><code>def get_user_item_by_username(self, project_name, username):\n    \"\"\"Get user item by username.\n\n    Args:\n        username (str): Username.\n\n    Returns:\n        Union[UserItem, None]: User item or None if not found.\n\n    \"\"\"\n    self._invalidate_cache(project_name)\n    for user_item in self.get_user_items(project_name):\n        if user_item.username == username:\n            return user_item\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/index.html#client.ayon_core.tools.common_models.UsersModel.get_user_items","title":"<code>get_user_items(project_name)</code>","text":"<p>Get user items.</p> <p>Returns:</p> Type Description <p>List[UserItem]: List of user items.</p> Source code in <code>client/ayon_core/tools/common_models/users.py</code> <pre><code>def get_user_items(self, project_name):\n    \"\"\"Get user items.\n\n    Returns:\n        List[UserItem]: List of user items.\n\n    \"\"\"\n    self._invalidate_cache(project_name)\n    return self._users_cache[project_name].get_data()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/index.html#client.ayon_core.tools.common_models.UsersModel.get_user_items_by_name","title":"<code>get_user_items_by_name(project_name)</code>","text":"<p>Get user items by name.</p> <p>Implemented as most of cases using this model will need to find     user information by username.</p> <p>Returns:</p> Type Description <p>Dict[str, UserItem]: Dictionary of user items by name.</p> Source code in <code>client/ayon_core/tools/common_models/users.py</code> <pre><code>def get_user_items_by_name(self, project_name):\n    \"\"\"Get user items by name.\n\n    Implemented as most of cases using this model will need to find\n        user information by username.\n\n    Returns:\n        Dict[str, UserItem]: Dictionary of user items by name.\n\n    \"\"\"\n    return {\n        user_item.username: user_item\n        for user_item in self.get_user_items(project_name)\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/cache.html","title":"cache","text":""},{"location":"autoapi/client/ayon_core/tools/common_models/hierarchy.html","title":"hierarchy","text":""},{"location":"autoapi/client/ayon_core/tools/common_models/hierarchy.html#client.ayon_core.tools.common_models.hierarchy.FolderItem","title":"<code>FolderItem</code>","text":"<p>Item representing folder entity on a server.</p> <p>Folder can be a child of another folder or a project.</p> <p>Parameters:</p> Name Type Description Default <code>entity_id</code> <code>str</code> <p>Folder id.</p> required <code>parent_id</code> <code>str | None</code> <p>Parent folder id. If 'None' then project is parent.</p> required <code>name</code> <code>str</code> <p>Name of folder.</p> required <code>path</code> <code>str</code> <p>Folder path.</p> required <code>folder_type</code> <code>str</code> <p>Type of folder.</p> required <code>label</code> <code>str | None</code> <p>Folder label.</p> required Source code in <code>client/ayon_core/tools/common_models/hierarchy.py</code> <pre><code>class FolderItem:\n    \"\"\"Item representing folder entity on a server.\n\n    Folder can be a child of another folder or a project.\n\n    Args:\n        entity_id (str): Folder id.\n        parent_id (str | None): Parent folder id. If 'None' then project\n            is parent.\n        name (str): Name of folder.\n        path (str): Folder path.\n        folder_type (str): Type of folder.\n        label (str | None): Folder label.\n    \"\"\"\n\n    def __init__(\n        self, entity_id, parent_id, name, path, folder_type, label\n    ):\n        self.entity_id = entity_id\n        self.parent_id = parent_id\n        self.name = name\n        self.path = path\n        self.folder_type = folder_type\n        self.label = label or name\n\n    def to_data(self):\n        \"\"\"Converts folder item to data.\n\n        Returns:\n            dict[str, Any]: Folder item data.\n        \"\"\"\n\n        return {\n            \"entity_id\": self.entity_id,\n            \"parent_id\": self.parent_id,\n            \"name\": self.name,\n            \"path\": self.path,\n            \"folder_type\": self.folder_type,\n            \"label\": self.label,\n        }\n\n    @classmethod\n    def from_data(cls, data):\n        \"\"\"Re-creates folder item from data.\n\n        Args:\n            data (dict[str, Any]): Folder item data.\n\n        Returns:\n            FolderItem: Folder item.\n        \"\"\"\n\n        return cls(**data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/hierarchy.html#client.ayon_core.tools.common_models.hierarchy.FolderItem.from_data","title":"<code>from_data(data)</code>  <code>classmethod</code>","text":"<p>Re-creates folder item from data.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict[str, Any]</code> <p>Folder item data.</p> required <p>Returns:</p> Name Type Description <code>FolderItem</code> <p>Folder item.</p> Source code in <code>client/ayon_core/tools/common_models/hierarchy.py</code> <pre><code>@classmethod\ndef from_data(cls, data):\n    \"\"\"Re-creates folder item from data.\n\n    Args:\n        data (dict[str, Any]): Folder item data.\n\n    Returns:\n        FolderItem: Folder item.\n    \"\"\"\n\n    return cls(**data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/hierarchy.html#client.ayon_core.tools.common_models.hierarchy.FolderItem.to_data","title":"<code>to_data()</code>","text":"<p>Converts folder item to data.</p> <p>Returns:</p> Type Description <p>dict[str, Any]: Folder item data.</p> Source code in <code>client/ayon_core/tools/common_models/hierarchy.py</code> <pre><code>def to_data(self):\n    \"\"\"Converts folder item to data.\n\n    Returns:\n        dict[str, Any]: Folder item data.\n    \"\"\"\n\n    return {\n        \"entity_id\": self.entity_id,\n        \"parent_id\": self.parent_id,\n        \"name\": self.name,\n        \"path\": self.path,\n        \"folder_type\": self.folder_type,\n        \"label\": self.label,\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/hierarchy.html#client.ayon_core.tools.common_models.hierarchy.HierarchyModel","title":"<code>HierarchyModel</code>","text":"<p>               Bases: <code>object</code></p> <p>Model for project hierarchy items.</p> <p>Hierarchy items are folders and tasks. Folders can have as parent another folder or project. Tasks can have as parent only folder.</p> Source code in <code>client/ayon_core/tools/common_models/hierarchy.py</code> <pre><code>class HierarchyModel(object):\n    \"\"\"Model for project hierarchy items.\n\n    Hierarchy items are folders and tasks. Folders can have as parent another\n    folder or project. Tasks can have as parent only folder.\n    \"\"\"\n    lifetime = 60  # A minute\n\n    def __init__(self, controller):\n        self._tags_by_entity_type = NestedCacheItem(\n            levels=1, default_factory=dict, lifetime=self.lifetime)\n        self._folders_items = NestedCacheItem(\n            levels=1, default_factory=dict, lifetime=self.lifetime)\n        self._folders_by_id = NestedCacheItem(\n            levels=2, default_factory=dict, lifetime=self.lifetime)\n\n        self._task_items = NestedCacheItem(\n            levels=2, default_factory=dict, lifetime=self.lifetime)\n        self._tasks_by_id = NestedCacheItem(\n            levels=2, default_factory=dict, lifetime=self.lifetime)\n\n        self._entity_ids_by_assignee = NestedCacheItem(\n            levels=2, default_factory=dict, lifetime=self.lifetime)\n\n        self._folders_refreshing = set()\n        self._tasks_refreshing = set()\n        self._controller = controller\n\n    def reset(self):\n        self._tags_by_entity_type.reset()\n        self._folders_items.reset()\n        self._folders_by_id.reset()\n\n        self._task_items.reset()\n        self._tasks_by_id.reset()\n\n        self._entity_ids_by_assignee.reset()\n\n    def refresh_project(self, project_name):\n        \"\"\"Force to refresh folder items for a project.\n\n        Args:\n            project_name (str): Name of project to refresh.\n        \"\"\"\n\n        self._refresh_folders_cache(project_name)\n\n    def get_folder_items(self, project_name, sender):\n        \"\"\"Get folder items by project name.\n\n        The folders are cached per project name. If the cache is not valid\n        then the folders are queried from server.\n\n        Args:\n            project_name (str): Name of project where to look for folders.\n            sender (Union[str, None]): Who requested the folder ids.\n\n        Returns:\n            dict[str, FolderItem]: Folder items by id.\n        \"\"\"\n\n        if not self._folders_items[project_name].is_valid:\n            self._refresh_folders_cache(project_name, sender)\n        return self._folders_items[project_name].get_data()\n\n    def get_folder_items_by_id(self, project_name, folder_ids):\n        \"\"\"Get folder items by ids.\n\n        This function will query folders if they are not in cache. But the\n        queried items are not added to cache back.\n\n        Args:\n            project_name (str): Name of project where to look for folders.\n            folder_ids (Iterable[str]): Folder ids.\n\n        Returns:\n            dict[str, Union[FolderItem, None]]: Folder items by id.\n        \"\"\"\n\n        folder_ids = set(folder_ids)\n        if self._folders_items[project_name].is_valid:\n            cache_data = self._folders_items[project_name].get_data()\n            return {\n                folder_id: cache_data.get(folder_id)\n                for folder_id in folder_ids\n            }\n        folders = ayon_api.get_folders(\n            project_name,\n            folder_ids=folder_ids,\n            fields=[\"id\", \"name\", \"label\", \"parentId\", \"path\", \"folderType\"]\n        )\n        # Make sure all folder ids are in output\n        output = {folder_id: None for folder_id in folder_ids}\n        output.update({\n            folder[\"id\"]: _get_folder_item_from_entity(folder)\n            for folder in folders\n        })\n        return output\n\n    def get_folder_items_by_paths(self, project_name, folder_paths):\n        \"\"\"Get folder items by ids.\n\n        This function will query folders if they are not in cache. But the\n        queried items are not added to cache back.\n\n        Args:\n            project_name (str): Name of project where to look for folders.\n            folder_paths (Iterable[str]): Folder paths.\n\n        Returns:\n            dict[str, Union[FolderItem, None]]: Folder items by id.\n        \"\"\"\n\n        folder_paths = set(folder_paths)\n        output = {folder_path: None for folder_path in folder_paths}\n        if not folder_paths:\n            return output\n\n        if self._folders_items[project_name].is_valid:\n            cache_data = self._folders_items[project_name].get_data()\n            for folder_item in cache_data.values():\n                if folder_item.path in folder_paths:\n                    output[folder_item.path] = folder_item\n            return output\n        folders = ayon_api.get_folders(\n            project_name,\n            folder_paths=folder_paths,\n            fields=[\"id\", \"name\", \"label\", \"parentId\", \"path\", \"folderType\"]\n        )\n        # Make sure all folder ids are in output\n        for folder in folders:\n            item = _get_folder_item_from_entity(folder)\n            output[item.path] = item\n        return output\n\n    def get_folder_item(self, project_name, folder_id):\n        \"\"\"Get folder item by id.\n\n        This function will query folder if they is not in cache. But the\n        queried items are not added to cache back.\n\n        Args:\n            project_name (str): Name of project where to look for folders.\n            folder_id (str): Folder id.\n\n        Returns:\n            Union[FolderItem, None]: Folder item.\n        \"\"\"\n        items = self.get_folder_items_by_id(\n            project_name, [folder_id]\n        )\n        return items.get(folder_id)\n\n    def get_folder_item_by_path(self, project_name, folder_path):\n        \"\"\"Get folder item by path.\n\n        This function will query folder if they is not in cache. But the\n        queried items are not added to cache back.\n\n        Args:\n            project_name (str): Name of project where to look for folders.\n            folder_path (str): Folder path.\n\n        Returns:\n            Union[FolderItem, None]: Folder item.\n\n        \"\"\"\n        items = self.get_folder_items_by_paths(\n            project_name, [folder_path]\n        )\n        return items.get(folder_path)\n\n    def get_task_item_by_name(\n        self, project_name, folder_id, task_name, sender\n    ):\n        \"\"\"Get task item by name and folder id.\n\n        Args:\n            project_name (str): Project name.\n            folder_id (str): Folder id.\n            task_name (str): Task name.\n            sender (Union[str, None]): Who requested the task item.\n\n        Returns:\n            Optional[TaskItem]: Task item found by name and folder id.\n\n        \"\"\"\n        for task_item in self.get_task_items(project_name, folder_id, sender):\n            if task_item.name == task_name:\n                return task_item\n        return None\n\n    def get_task_items(self, project_name, folder_id, sender):\n        if not project_name or not folder_id:\n            return []\n\n        task_cache = self._task_items[project_name][folder_id]\n        if not task_cache.is_valid:\n            self._refresh_tasks_cache(project_name, folder_id, sender)\n        return task_cache.get_data()\n\n    def get_folder_entities(self, project_name, folder_ids):\n        \"\"\"Get folder entities by ids.\n\n        Args:\n            project_name (str): Project name.\n            folder_ids (Iterable[str]): Folder ids.\n\n        Returns:\n            dict[str, Any]: Folder entities by id.\n        \"\"\"\n\n        output = {}\n        folder_ids = set(folder_ids)\n        if not project_name or not folder_ids:\n            return output\n\n        folder_ids_to_query = set()\n        for folder_id in folder_ids:\n            cache = self._folders_by_id[project_name][folder_id]\n            if cache.is_valid:\n                output[folder_id] = cache.get_data()\n            elif folder_id:\n                folder_ids_to_query.add(folder_id)\n            else:\n                output[folder_id] = None\n        self._query_folder_entities(project_name, folder_ids_to_query)\n        for folder_id in folder_ids_to_query:\n            cache = self._folders_by_id[project_name][folder_id]\n            output[folder_id] = cache.get_data()\n        return output\n\n    def get_folder_entity(self, project_name, folder_id):\n        output = self.get_folder_entities(project_name, {folder_id})\n        return output[folder_id]\n\n    def get_task_entities(self, project_name, task_ids):\n        output = {}\n        task_ids = set(task_ids)\n        if not project_name or not task_ids:\n            return output\n\n        task_ids_to_query = set()\n        for task_id in task_ids:\n            cache = self._tasks_by_id[project_name][task_id]\n            if cache.is_valid:\n                output[task_id] = cache.get_data()\n            elif task_id:\n                task_ids_to_query.add(task_id)\n            else:\n                output[task_id] = None\n        self._query_task_entities(project_name, task_ids_to_query)\n        for task_id in task_ids_to_query:\n            cache = self._tasks_by_id[project_name][task_id]\n            output[task_id] = cache.get_data()\n        return output\n\n    def get_task_entity(self, project_name, task_id):\n        output = self.get_task_entities(project_name, {task_id})\n        return output[task_id]\n\n    def get_entity_ids_for_assignees(\n        self, project_name: str, assignees: list[str]\n    ):\n        folder_ids = set()\n        task_ids = set()\n        output = {\n            \"folder_ids\": folder_ids,\n            \"task_ids\": task_ids,\n        }\n        assignees = set(assignees)\n        for assignee in tuple(assignees):\n            cache = self._entity_ids_by_assignee[project_name][assignee]\n            if cache.is_valid:\n                assignees.discard(assignee)\n                assignee_data = cache.get_data()\n                folder_ids.update(assignee_data[\"folder_ids\"])\n                task_ids.update(assignee_data[\"task_ids\"])\n\n        if not assignees:\n            return output\n\n        tasks = ayon_api.get_tasks(\n            project_name,\n            assignees_all=assignees,\n            fields={\"id\", \"folderId\", \"assignees\"},\n        )\n        tasks_assignee = {}\n        for task in tasks:\n            folder_ids.add(task[\"folderId\"])\n            task_ids.add(task[\"id\"])\n            for assignee in task[\"assignees\"]:\n                tasks_assignee.setdefault(assignee, []).append(task)\n\n        for assignee, tasks in tasks_assignee.items():\n            cache = self._entity_ids_by_assignee[project_name][assignee]\n            assignee_folder_ids = set()\n            assignee_task_ids = set()\n            assignee_data = {\n                \"folder_ids\": assignee_folder_ids,\n                \"task_ids\": assignee_task_ids,\n            }\n            for task in tasks:\n                assignee_folder_ids.add(task[\"folderId\"])\n                assignee_task_ids.add(task[\"id\"])\n            cache.update_data(assignee_data)\n\n        return output\n\n    def get_available_tags_by_entity_type(\n        self, project_name: str\n    ) -&gt; dict[str, list[str]]:\n        \"\"\"Get available tags for all entity types in a project.\"\"\"\n        cache = self._tags_by_entity_type.get(project_name)\n        if not cache.is_valid:\n            tags = None\n            if project_name:\n                response = ayon_api.get(f\"projects/{project_name}/tags\")\n                if response.status_code == 200:\n                    tags = response.data\n\n            # Fake empty tags\n            if tags is None:\n                tags = {\n                    \"folders\": [],\n                    \"tasks\": [],\n                    \"products\": [],\n                    \"versions\": [],\n                    \"representations\": [],\n                    \"workfiles\": []\n                }\n            cache.update_data(tags)\n        return cache.get_data()\n\n    @contextlib.contextmanager\n    def _folder_refresh_event_manager(self, project_name, sender):\n        self._folders_refreshing.add(project_name)\n        self._controller.emit_event(\n            \"folders.refresh.started\",\n            {\"project_name\": project_name, \"sender\": sender},\n            HIERARCHY_MODEL_SENDER\n        )\n        try:\n            yield\n\n        finally:\n            self._controller.emit_event(\n                \"folders.refresh.finished\",\n                {\"project_name\": project_name, \"sender\": sender},\n                HIERARCHY_MODEL_SENDER\n            )\n            self._folders_refreshing.remove(project_name)\n\n    @contextlib.contextmanager\n    def _task_refresh_event_manager(\n        self, project_name, folder_id, sender\n    ):\n        self._tasks_refreshing.add(folder_id)\n        self._controller.emit_event(\n            \"tasks.refresh.started\",\n            {\n                \"project_name\": project_name,\n                \"folder_id\": folder_id,\n                \"sender\": sender,\n            },\n            HIERARCHY_MODEL_SENDER\n        )\n        try:\n            yield\n\n        finally:\n            self._controller.emit_event(\n                \"tasks.refresh.finished\",\n                {\n                    \"project_name\": project_name,\n                    \"folder_id\": folder_id,\n                    \"sender\": sender,\n                },\n                HIERARCHY_MODEL_SENDER\n            )\n            self._tasks_refreshing.discard(folder_id)\n\n    def _refresh_folders_cache(self, project_name, sender=None):\n        if project_name in self._folders_refreshing:\n            return\n\n        with self._folder_refresh_event_manager(project_name, sender):\n            folder_items = self._query_folders(project_name)\n            self._folders_items[project_name].update_data(folder_items)\n\n    def _query_folders(self, project_name):\n        hierarchy = ayon_api.get_folders_hierarchy(project_name)\n\n        folder_items = {}\n        hierachy_queue = collections.deque(hierarchy[\"hierarchy\"])\n        while hierachy_queue:\n            item = hierachy_queue.popleft()\n            folder_item = _get_folder_item_from_hierarchy_item(item)\n            folder_items[folder_item.entity_id] = folder_item\n            hierachy_queue.extend(item[\"children\"] or [])\n        return folder_items\n\n    def _query_folder_entities(self, project_name, folder_ids):\n        if not project_name or not folder_ids:\n            return\n        project_cache = self._folders_by_id[project_name]\n        folders = ayon_api.get_folders(project_name, folder_ids=folder_ids)\n        for folder in folders:\n            folder_id = folder[\"id\"]\n            project_cache[folder_id].update_data(folder)\n\n    def _query_task_entities(self, project_name, task_ids):\n        if not project_name or not task_ids:\n            return\n\n        project_cache = self._tasks_by_id[project_name]\n        tasks = ayon_api.get_tasks(project_name, task_ids=task_ids)\n        for task in tasks:\n            task_id = task[\"id\"]\n            project_cache[task_id].update_data(task)\n\n    def _refresh_tasks_cache(self, project_name, folder_id, sender=None):\n        if folder_id in self._tasks_refreshing:\n            while folder_id in self._tasks_refreshing:\n                time.sleep(0.01)\n            return\n\n        cache = self._task_items[project_name][folder_id]\n        with self._task_refresh_event_manager(\n            project_name, folder_id, sender\n        ):\n            cache.update_data(self._query_tasks(project_name, folder_id))\n\n    def _query_tasks(self, project_name, folder_id):\n        tasks = list(ayon_api.get_tasks(\n            project_name,\n            folder_ids=[folder_id],\n            fields={\"id\", \"name\", \"label\", \"folderId\", \"type\", \"tags\"}\n        ))\n        task_type_items = self._controller.get_task_type_items(project_name)\n        return _get_task_items_from_tasks(task_type_items, tasks)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/hierarchy.html#client.ayon_core.tools.common_models.hierarchy.HierarchyModel.get_available_tags_by_entity_type","title":"<code>get_available_tags_by_entity_type(project_name)</code>","text":"<p>Get available tags for all entity types in a project.</p> Source code in <code>client/ayon_core/tools/common_models/hierarchy.py</code> <pre><code>def get_available_tags_by_entity_type(\n    self, project_name: str\n) -&gt; dict[str, list[str]]:\n    \"\"\"Get available tags for all entity types in a project.\"\"\"\n    cache = self._tags_by_entity_type.get(project_name)\n    if not cache.is_valid:\n        tags = None\n        if project_name:\n            response = ayon_api.get(f\"projects/{project_name}/tags\")\n            if response.status_code == 200:\n                tags = response.data\n\n        # Fake empty tags\n        if tags is None:\n            tags = {\n                \"folders\": [],\n                \"tasks\": [],\n                \"products\": [],\n                \"versions\": [],\n                \"representations\": [],\n                \"workfiles\": []\n            }\n        cache.update_data(tags)\n    return cache.get_data()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/hierarchy.html#client.ayon_core.tools.common_models.hierarchy.HierarchyModel.get_folder_entities","title":"<code>get_folder_entities(project_name, folder_ids)</code>","text":"<p>Get folder entities by ids.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>folder_ids</code> <code>Iterable[str]</code> <p>Folder ids.</p> required <p>Returns:</p> Type Description <p>dict[str, Any]: Folder entities by id.</p> Source code in <code>client/ayon_core/tools/common_models/hierarchy.py</code> <pre><code>def get_folder_entities(self, project_name, folder_ids):\n    \"\"\"Get folder entities by ids.\n\n    Args:\n        project_name (str): Project name.\n        folder_ids (Iterable[str]): Folder ids.\n\n    Returns:\n        dict[str, Any]: Folder entities by id.\n    \"\"\"\n\n    output = {}\n    folder_ids = set(folder_ids)\n    if not project_name or not folder_ids:\n        return output\n\n    folder_ids_to_query = set()\n    for folder_id in folder_ids:\n        cache = self._folders_by_id[project_name][folder_id]\n        if cache.is_valid:\n            output[folder_id] = cache.get_data()\n        elif folder_id:\n            folder_ids_to_query.add(folder_id)\n        else:\n            output[folder_id] = None\n    self._query_folder_entities(project_name, folder_ids_to_query)\n    for folder_id in folder_ids_to_query:\n        cache = self._folders_by_id[project_name][folder_id]\n        output[folder_id] = cache.get_data()\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/hierarchy.html#client.ayon_core.tools.common_models.hierarchy.HierarchyModel.get_folder_item","title":"<code>get_folder_item(project_name, folder_id)</code>","text":"<p>Get folder item by id.</p> <p>This function will query folder if they is not in cache. But the queried items are not added to cache back.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Name of project where to look for folders.</p> required <code>folder_id</code> <code>str</code> <p>Folder id.</p> required <p>Returns:</p> Type Description <p>Union[FolderItem, None]: Folder item.</p> Source code in <code>client/ayon_core/tools/common_models/hierarchy.py</code> <pre><code>def get_folder_item(self, project_name, folder_id):\n    \"\"\"Get folder item by id.\n\n    This function will query folder if they is not in cache. But the\n    queried items are not added to cache back.\n\n    Args:\n        project_name (str): Name of project where to look for folders.\n        folder_id (str): Folder id.\n\n    Returns:\n        Union[FolderItem, None]: Folder item.\n    \"\"\"\n    items = self.get_folder_items_by_id(\n        project_name, [folder_id]\n    )\n    return items.get(folder_id)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/hierarchy.html#client.ayon_core.tools.common_models.hierarchy.HierarchyModel.get_folder_item_by_path","title":"<code>get_folder_item_by_path(project_name, folder_path)</code>","text":"<p>Get folder item by path.</p> <p>This function will query folder if they is not in cache. But the queried items are not added to cache back.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Name of project where to look for folders.</p> required <code>folder_path</code> <code>str</code> <p>Folder path.</p> required <p>Returns:</p> Type Description <p>Union[FolderItem, None]: Folder item.</p> Source code in <code>client/ayon_core/tools/common_models/hierarchy.py</code> <pre><code>def get_folder_item_by_path(self, project_name, folder_path):\n    \"\"\"Get folder item by path.\n\n    This function will query folder if they is not in cache. But the\n    queried items are not added to cache back.\n\n    Args:\n        project_name (str): Name of project where to look for folders.\n        folder_path (str): Folder path.\n\n    Returns:\n        Union[FolderItem, None]: Folder item.\n\n    \"\"\"\n    items = self.get_folder_items_by_paths(\n        project_name, [folder_path]\n    )\n    return items.get(folder_path)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/hierarchy.html#client.ayon_core.tools.common_models.hierarchy.HierarchyModel.get_folder_items","title":"<code>get_folder_items(project_name, sender)</code>","text":"<p>Get folder items by project name.</p> <p>The folders are cached per project name. If the cache is not valid then the folders are queried from server.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Name of project where to look for folders.</p> required <code>sender</code> <code>Union[str, None]</code> <p>Who requested the folder ids.</p> required <p>Returns:</p> Type Description <p>dict[str, FolderItem]: Folder items by id.</p> Source code in <code>client/ayon_core/tools/common_models/hierarchy.py</code> <pre><code>def get_folder_items(self, project_name, sender):\n    \"\"\"Get folder items by project name.\n\n    The folders are cached per project name. If the cache is not valid\n    then the folders are queried from server.\n\n    Args:\n        project_name (str): Name of project where to look for folders.\n        sender (Union[str, None]): Who requested the folder ids.\n\n    Returns:\n        dict[str, FolderItem]: Folder items by id.\n    \"\"\"\n\n    if not self._folders_items[project_name].is_valid:\n        self._refresh_folders_cache(project_name, sender)\n    return self._folders_items[project_name].get_data()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/hierarchy.html#client.ayon_core.tools.common_models.hierarchy.HierarchyModel.get_folder_items_by_id","title":"<code>get_folder_items_by_id(project_name, folder_ids)</code>","text":"<p>Get folder items by ids.</p> <p>This function will query folders if they are not in cache. But the queried items are not added to cache back.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Name of project where to look for folders.</p> required <code>folder_ids</code> <code>Iterable[str]</code> <p>Folder ids.</p> required <p>Returns:</p> Type Description <p>dict[str, Union[FolderItem, None]]: Folder items by id.</p> Source code in <code>client/ayon_core/tools/common_models/hierarchy.py</code> <pre><code>def get_folder_items_by_id(self, project_name, folder_ids):\n    \"\"\"Get folder items by ids.\n\n    This function will query folders if they are not in cache. But the\n    queried items are not added to cache back.\n\n    Args:\n        project_name (str): Name of project where to look for folders.\n        folder_ids (Iterable[str]): Folder ids.\n\n    Returns:\n        dict[str, Union[FolderItem, None]]: Folder items by id.\n    \"\"\"\n\n    folder_ids = set(folder_ids)\n    if self._folders_items[project_name].is_valid:\n        cache_data = self._folders_items[project_name].get_data()\n        return {\n            folder_id: cache_data.get(folder_id)\n            for folder_id in folder_ids\n        }\n    folders = ayon_api.get_folders(\n        project_name,\n        folder_ids=folder_ids,\n        fields=[\"id\", \"name\", \"label\", \"parentId\", \"path\", \"folderType\"]\n    )\n    # Make sure all folder ids are in output\n    output = {folder_id: None for folder_id in folder_ids}\n    output.update({\n        folder[\"id\"]: _get_folder_item_from_entity(folder)\n        for folder in folders\n    })\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/hierarchy.html#client.ayon_core.tools.common_models.hierarchy.HierarchyModel.get_folder_items_by_paths","title":"<code>get_folder_items_by_paths(project_name, folder_paths)</code>","text":"<p>Get folder items by ids.</p> <p>This function will query folders if they are not in cache. But the queried items are not added to cache back.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Name of project where to look for folders.</p> required <code>folder_paths</code> <code>Iterable[str]</code> <p>Folder paths.</p> required <p>Returns:</p> Type Description <p>dict[str, Union[FolderItem, None]]: Folder items by id.</p> Source code in <code>client/ayon_core/tools/common_models/hierarchy.py</code> <pre><code>def get_folder_items_by_paths(self, project_name, folder_paths):\n    \"\"\"Get folder items by ids.\n\n    This function will query folders if they are not in cache. But the\n    queried items are not added to cache back.\n\n    Args:\n        project_name (str): Name of project where to look for folders.\n        folder_paths (Iterable[str]): Folder paths.\n\n    Returns:\n        dict[str, Union[FolderItem, None]]: Folder items by id.\n    \"\"\"\n\n    folder_paths = set(folder_paths)\n    output = {folder_path: None for folder_path in folder_paths}\n    if not folder_paths:\n        return output\n\n    if self._folders_items[project_name].is_valid:\n        cache_data = self._folders_items[project_name].get_data()\n        for folder_item in cache_data.values():\n            if folder_item.path in folder_paths:\n                output[folder_item.path] = folder_item\n        return output\n    folders = ayon_api.get_folders(\n        project_name,\n        folder_paths=folder_paths,\n        fields=[\"id\", \"name\", \"label\", \"parentId\", \"path\", \"folderType\"]\n    )\n    # Make sure all folder ids are in output\n    for folder in folders:\n        item = _get_folder_item_from_entity(folder)\n        output[item.path] = item\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/hierarchy.html#client.ayon_core.tools.common_models.hierarchy.HierarchyModel.get_task_item_by_name","title":"<code>get_task_item_by_name(project_name, folder_id, task_name, sender)</code>","text":"<p>Get task item by name and folder id.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>folder_id</code> <code>str</code> <p>Folder id.</p> required <code>task_name</code> <code>str</code> <p>Task name.</p> required <code>sender</code> <code>Union[str, None]</code> <p>Who requested the task item.</p> required <p>Returns:</p> Type Description <p>Optional[TaskItem]: Task item found by name and folder id.</p> Source code in <code>client/ayon_core/tools/common_models/hierarchy.py</code> <pre><code>def get_task_item_by_name(\n    self, project_name, folder_id, task_name, sender\n):\n    \"\"\"Get task item by name and folder id.\n\n    Args:\n        project_name (str): Project name.\n        folder_id (str): Folder id.\n        task_name (str): Task name.\n        sender (Union[str, None]): Who requested the task item.\n\n    Returns:\n        Optional[TaskItem]: Task item found by name and folder id.\n\n    \"\"\"\n    for task_item in self.get_task_items(project_name, folder_id, sender):\n        if task_item.name == task_name:\n            return task_item\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/hierarchy.html#client.ayon_core.tools.common_models.hierarchy.HierarchyModel.refresh_project","title":"<code>refresh_project(project_name)</code>","text":"<p>Force to refresh folder items for a project.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Name of project to refresh.</p> required Source code in <code>client/ayon_core/tools/common_models/hierarchy.py</code> <pre><code>def refresh_project(self, project_name):\n    \"\"\"Force to refresh folder items for a project.\n\n    Args:\n        project_name (str): Name of project to refresh.\n    \"\"\"\n\n    self._refresh_folders_cache(project_name)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/hierarchy.html#client.ayon_core.tools.common_models.hierarchy.TaskItem","title":"<code>TaskItem</code>","text":"<p>Task item representing task entity on a server.</p> <p>Task is child of a folder.</p> <p>Task item has label that is used for display in UI. The label is by     default using task name and type.</p> <p>Parameters:</p> Name Type Description Default <code>task_id</code> <code>str</code> <p>Task id.</p> required <code>name</code> <code>str</code> <p>Name of task.</p> required <code>name</code> <code>str | None</code> <p>Task label.</p> required <code>task_type</code> <code>str</code> <p>Type of task.</p> required <code>parent_id</code> <code>str</code> <p>Parent folder id.</p> required Source code in <code>client/ayon_core/tools/common_models/hierarchy.py</code> <pre><code>class TaskItem:\n    \"\"\"Task item representing task entity on a server.\n\n    Task is child of a folder.\n\n    Task item has label that is used for display in UI. The label is by\n        default using task name and type.\n\n    Args:\n        task_id (str): Task id.\n        name (str): Name of task.\n        name (str | None): Task label.\n        task_type (str): Type of task.\n        parent_id (str): Parent folder id.\n    \"\"\"\n\n    def __init__(\n        self,\n        task_id: str,\n        name: str,\n        label: str | None,\n        task_type: str,\n        task_type_order: int,\n        parent_id: str,\n        tags: list[str],\n    ):\n        self.task_id = task_id\n        self.name = name\n        self.label = label\n        self.task_type = task_type\n        self.task_type_order = task_type_order\n        self.parent_id = parent_id\n        self.tags = tags\n\n        self._full_label = None\n\n    @property\n    def id(self):\n        \"\"\"Alias for task_id.\n\n        Returns:\n            str: Task id.\n        \"\"\"\n\n        return self.task_id\n\n    @property\n    def full_label(self):\n        \"\"\"Label of task item for UI.\n\n        Returns:\n            str: Label of task item.\n        \"\"\"\n\n        if self._full_label is None:\n            label = self.label or self.name\n            self._full_label = f\"{label} ({self.task_type})\"\n        return self._full_label\n\n    def to_data(self):\n        \"\"\"Converts task item to data.\n\n        Returns:\n            dict[str, Any]: Task item data.\n        \"\"\"\n\n        return {\n            \"task_id\": self.task_id,\n            \"name\": self.name,\n            \"label\": self.label,\n            \"parent_id\": self.parent_id,\n            \"task_type\": self.task_type,\n            \"task_type_order\": self.task_type_order,\n            \"tags\": self.tags,\n        }\n\n    @classmethod\n    def from_data(cls, data):\n        \"\"\"Re-create task item from data.\n\n        Args:\n            data (dict[str, Any]): Task item data.\n\n        Returns:\n            TaskItem: Task item.\n        \"\"\"\n\n        return cls(**data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/hierarchy.html#client.ayon_core.tools.common_models.hierarchy.TaskItem.full_label","title":"<code>full_label</code>  <code>property</code>","text":"<p>Label of task item for UI.</p> <p>Returns:</p> Name Type Description <code>str</code> <p>Label of task item.</p>"},{"location":"autoapi/client/ayon_core/tools/common_models/hierarchy.html#client.ayon_core.tools.common_models.hierarchy.TaskItem.id","title":"<code>id</code>  <code>property</code>","text":"<p>Alias for task_id.</p> <p>Returns:</p> Name Type Description <code>str</code> <p>Task id.</p>"},{"location":"autoapi/client/ayon_core/tools/common_models/hierarchy.html#client.ayon_core.tools.common_models.hierarchy.TaskItem.from_data","title":"<code>from_data(data)</code>  <code>classmethod</code>","text":"<p>Re-create task item from data.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict[str, Any]</code> <p>Task item data.</p> required <p>Returns:</p> Name Type Description <code>TaskItem</code> <p>Task item.</p> Source code in <code>client/ayon_core/tools/common_models/hierarchy.py</code> <pre><code>@classmethod\ndef from_data(cls, data):\n    \"\"\"Re-create task item from data.\n\n    Args:\n        data (dict[str, Any]): Task item data.\n\n    Returns:\n        TaskItem: Task item.\n    \"\"\"\n\n    return cls(**data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/hierarchy.html#client.ayon_core.tools.common_models.hierarchy.TaskItem.to_data","title":"<code>to_data()</code>","text":"<p>Converts task item to data.</p> <p>Returns:</p> Type Description <p>dict[str, Any]: Task item data.</p> Source code in <code>client/ayon_core/tools/common_models/hierarchy.py</code> <pre><code>def to_data(self):\n    \"\"\"Converts task item to data.\n\n    Returns:\n        dict[str, Any]: Task item data.\n    \"\"\"\n\n    return {\n        \"task_id\": self.task_id,\n        \"name\": self.name,\n        \"label\": self.label,\n        \"parent_id\": self.parent_id,\n        \"task_type\": self.task_type,\n        \"task_type_order\": self.task_type_order,\n        \"tags\": self.tags,\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/projects.html","title":"projects","text":""},{"location":"autoapi/client/ayon_core/tools/common_models/projects.html#client.ayon_core.tools.common_models.projects.FolderTypeItem","title":"<code>FolderTypeItem</code>","text":"<p>Item representing folder type of project.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Folder type name (\"Shot\").</p> required <code>short</code> <code>str</code> <p>Short folder type name (\"sh\").</p> required <code>icon</code> <code>str</code> <p>Icon name in MaterialIcons (\"fiber_new\").</p> required Source code in <code>client/ayon_core/tools/common_models/projects.py</code> <pre><code>class FolderTypeItem:\n    \"\"\"Item representing folder type of project.\n\n    Args:\n        name (str): Folder type name (\"Shot\").\n        short (str): Short folder type name (\"sh\").\n        icon (str): Icon name in MaterialIcons (\"fiber_new\").\n\n    \"\"\"\n    def __init__(self, name, short, icon):\n        self.name = name\n        self.short = short\n        self.icon = icon\n\n    def to_data(self):\n        return {\n            \"name\": self.name,\n            \"short\": self.short,\n            \"icon\": self.icon,\n        }\n\n    @classmethod\n    def from_data(cls, data):\n        return cls(**data)\n\n    @classmethod\n    def from_project_item(cls, folder_type_data):\n        return cls(\n            name=folder_type_data[\"name\"],\n            short=folder_type_data.get(\"shortName\", \"\"),\n            icon=folder_type_data[\"icon\"],\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/projects.html#client.ayon_core.tools.common_models.projects.ProjectItem","title":"<code>ProjectItem</code>  <code>dataclass</code>","text":"<p>Item representing folder entity on a server.</p> <p>Folder can be a child of another folder or a project.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Project name.</p> required <code>active</code> <code>Union[str, None]</code> <p>Parent folder id. If 'None' then project is parent.</p> required Source code in <code>client/ayon_core/tools/common_models/projects.py</code> <pre><code>@dataclass\nclass ProjectItem:\n    \"\"\"Item representing folder entity on a server.\n\n    Folder can be a child of another folder or a project.\n\n    Args:\n        name (str): Project name.\n        active (Union[str, None]): Parent folder id. If 'None' then project\n            is parent.\n    \"\"\"\n    name: str\n    active: bool\n    is_library: bool\n    icon: AwesomeFontIcon\n    is_pinned: bool = False\n\n    @classmethod\n    def from_entity(cls, project_entity: dict[str, Any]) -&gt; \"ProjectItem\":\n        \"\"\"Creates folder item from entity.\n\n        Args:\n            project_entity (dict[str, Any]): Project entity.\n\n        Returns:\n            ProjectItem: Project item.\n\n        \"\"\"\n        icon = AwesomeFontIcon(\n            \"fa.book\" if project_entity[\"library\"] else \"fa.map\",\n            color=get_default_entity_icon_color(),\n        )\n        return cls(\n            project_entity[\"name\"],\n            project_entity[\"active\"],\n            project_entity[\"library\"],\n            icon\n        )\n\n    def to_data(self):\n        \"\"\"Converts folder item to data.\n\n        Returns:\n            dict[str, Any]: Folder item data.\n        \"\"\"\n\n        return {\n            \"name\": self.name,\n            \"active\": self.active,\n            \"is_library\": self.is_library,\n            \"icon\": {\n                \"name\": self.icon.name,\n                \"color\": self.icon.color,\n            },\n        }\n\n    @classmethod\n    def from_data(cls, data):\n        \"\"\"Re-creates folder item from data.\n\n        Args:\n            data (dict[str, Any]): Folder item data.\n\n        Returns:\n            FolderItem: Folder item.\n        \"\"\"\n        icon = data[\"icon\"]\n        if isinstance(icon, dict):\n            data[\"icon\"] = AwesomeFontIcon(icon[\"name\"], color=icon[\"color\"])\n        return cls(**data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/projects.html#client.ayon_core.tools.common_models.projects.ProjectItem.from_data","title":"<code>from_data(data)</code>  <code>classmethod</code>","text":"<p>Re-creates folder item from data.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict[str, Any]</code> <p>Folder item data.</p> required <p>Returns:</p> Name Type Description <code>FolderItem</code> <p>Folder item.</p> Source code in <code>client/ayon_core/tools/common_models/projects.py</code> <pre><code>@classmethod\ndef from_data(cls, data):\n    \"\"\"Re-creates folder item from data.\n\n    Args:\n        data (dict[str, Any]): Folder item data.\n\n    Returns:\n        FolderItem: Folder item.\n    \"\"\"\n    icon = data[\"icon\"]\n    if isinstance(icon, dict):\n        data[\"icon\"] = AwesomeFontIcon(icon[\"name\"], color=icon[\"color\"])\n    return cls(**data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/projects.html#client.ayon_core.tools.common_models.projects.ProjectItem.from_entity","title":"<code>from_entity(project_entity)</code>  <code>classmethod</code>","text":"<p>Creates folder item from entity.</p> <p>Parameters:</p> Name Type Description Default <code>project_entity</code> <code>dict[str, Any]</code> <p>Project entity.</p> required <p>Returns:</p> Name Type Description <code>ProjectItem</code> <code>'ProjectItem'</code> <p>Project item.</p> Source code in <code>client/ayon_core/tools/common_models/projects.py</code> <pre><code>@classmethod\ndef from_entity(cls, project_entity: dict[str, Any]) -&gt; \"ProjectItem\":\n    \"\"\"Creates folder item from entity.\n\n    Args:\n        project_entity (dict[str, Any]): Project entity.\n\n    Returns:\n        ProjectItem: Project item.\n\n    \"\"\"\n    icon = AwesomeFontIcon(\n        \"fa.book\" if project_entity[\"library\"] else \"fa.map\",\n        color=get_default_entity_icon_color(),\n    )\n    return cls(\n        project_entity[\"name\"],\n        project_entity[\"active\"],\n        project_entity[\"library\"],\n        icon\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/projects.html#client.ayon_core.tools.common_models.projects.ProjectItem.to_data","title":"<code>to_data()</code>","text":"<p>Converts folder item to data.</p> <p>Returns:</p> Type Description <p>dict[str, Any]: Folder item data.</p> Source code in <code>client/ayon_core/tools/common_models/projects.py</code> <pre><code>def to_data(self):\n    \"\"\"Converts folder item to data.\n\n    Returns:\n        dict[str, Any]: Folder item data.\n    \"\"\"\n\n    return {\n        \"name\": self.name,\n        \"active\": self.active,\n        \"is_library\": self.is_library,\n        \"icon\": {\n            \"name\": self.icon.name,\n            \"color\": self.icon.color,\n        },\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/projects.html#client.ayon_core.tools.common_models.projects.ProjectsModel","title":"<code>ProjectsModel</code>","text":"<p>               Bases: <code>object</code></p> Source code in <code>client/ayon_core/tools/common_models/projects.py</code> <pre><code>class ProjectsModel(object):\n    def __init__(self, controller):\n        self._projects_cache = CacheItem(default_factory=list)\n        self._projects_by_name = NestedCacheItem(\n            levels=1, default_factory=list\n        )\n        self._product_type_icons_mapping = NestedCacheItem(\n            levels=1, default_factory=ProductTypeIconMapping\n        )\n        self._project_statuses_cache = {}\n        self._folder_types_cache = {}\n        self._task_types_cache = {}\n\n        self._is_refreshing = False\n        self._controller = controller\n\n    def reset(self):\n        self._project_statuses_cache = {}\n        self._folder_types_cache = {}\n        self._task_types_cache = {}\n        self._projects_cache.reset()\n        self._projects_by_name.reset()\n        self._product_type_icons_mapping.reset()\n\n    def refresh(self):\n        \"\"\"Refresh project items.\n\n        This method will requery list of ProjectItem returned by\n        'get_project_items'.\n\n        To reset all cached items use 'reset' method.\n        \"\"\"\n        self._refresh_projects_cache()\n\n    def get_project_items(self, sender):\n        \"\"\"\n\n        Args:\n            sender (str): Name of sender who asked for items.\n\n        Returns:\n            Union[list[ProjectItem], None]: List of project items, or None\n                if model is refreshing.\n        \"\"\"\n\n        if not self._projects_cache.is_valid:\n            return self._refresh_projects_cache(sender)\n        return self._projects_cache.get_data()\n\n    def get_project_entity(self, project_name):\n        \"\"\"Get project entity.\n\n        Args:\n            project_name (str): Project name.\n\n        Returns:\n            Union[dict[str, Any], None]: Project entity or None if project\n                was not found by name.\n\n        \"\"\"\n        project_cache = self._projects_by_name[project_name]\n        if not project_cache.is_valid:\n            entity = None\n            if project_name:\n                entity = ayon_api.get_rest_project(project_name)\n            project_cache.update_data(entity)\n        return project_cache.get_data()\n\n    def get_project_anatomy_tags(self, project_name: str) -&gt; list[TagItem]:\n        \"\"\"Get project anatomy tags.\n\n        Args:\n            project_name (str): Project name.\n\n        Returns:\n            list[TagItem]: Tag definitions.\n\n        \"\"\"\n        project_entity = self.get_project_entity(project_name)\n        return [\n            TagItem(tag[\"name\"], tag[\"color\"])\n            for tag in project_entity[\"tags\"]\n        ]\n\n    def get_project_status_items(self, project_name, sender):\n        \"\"\"Get project status items.\n\n        Args:\n            project_name (str): Project name.\n            sender (Union[str, None]): Name of sender who asked for items.\n\n        Returns:\n            list[StatusItem]: Status items for project.\n\n        \"\"\"\n        if project_name is None:\n            return []\n\n        statuses_cache = self._project_statuses_cache.get(project_name)\n        if (\n            statuses_cache is not None\n            and not self._projects_cache.is_valid\n        ):\n            statuses_cache = None\n\n        if statuses_cache is None:\n            with self._project_items_refresh_event_manager(\n                sender, project_name, \"statuses\"\n            ):\n                project_entity = self.get_project_entity(project_name)\n                statuses = []\n                if project_entity:\n                    statuses = [\n                        StatusItem.from_project_item(status)\n                        for status in project_entity[\"statuses\"]\n                    ]\n                statuses_cache = statuses\n            self._project_statuses_cache[project_name] = statuses_cache\n        return list(statuses_cache)\n\n    def get_folder_type_items(self, project_name, sender):\n        \"\"\"Get project status items.\n\n        Args:\n            project_name (str): Project name.\n            sender (Union[str, None]): Name of sender who asked for items.\n\n        Returns:\n            list[FolderType]: Folder type items for project.\n\n        \"\"\"\n        return self._get_project_items(\n            project_name,\n            sender,\n            \"folder_types\",\n            self._folder_types_cache,\n            self._folder_type_items_getter,\n        )\n\n    def get_task_type_items(self, project_name, sender):\n        \"\"\"Get project task type items.\n\n        Args:\n            project_name (str): Project name.\n            sender (Union[str, None]): Name of sender who asked for items.\n\n        Returns:\n            list[TaskTypeItem]: Task type items for project.\n\n        \"\"\"\n        return self._get_project_items(\n            project_name,\n            sender,\n            \"task_types\",\n            self._task_types_cache,\n            self._task_type_items_getter,\n        )\n\n    def get_product_type_icons_mapping(\n        self, project_name: Optional[str]\n    ) -&gt; ProductTypeIconMapping:\n        cache = self._product_type_icons_mapping[project_name]\n        if cache.is_valid:\n            return cache.get_data()\n\n        project_entity = self.get_project_entity(project_name)\n        icons_mapping = ProductTypeIconMapping()\n        if project_entity:\n            product_base_types = (\n                project_entity[\"config\"].get(\"productBaseTypes\", {})\n            )\n            icons_mapping = ProductTypeIconMapping(\n                product_base_types.get(\"default\"),\n                product_base_types.get(\"definitions\")\n            )\n\n        cache.update_data(icons_mapping)\n        return icons_mapping\n\n    def _get_project_items(\n        self, project_name, sender, item_type, cache_obj, getter\n    ):\n        if (\n            project_name in cache_obj\n            and (\n                project_name is None\n                or self._projects_by_name[project_name].is_valid\n            )\n        ):\n            return cache_obj[project_name]\n\n        with self._project_items_refresh_event_manager(\n            sender, project_name, item_type\n        ):\n            cache_value = getter(self.get_project_entity(project_name))\n        cache_obj[project_name] = cache_value\n        return cache_value\n\n    @contextlib.contextmanager\n    def _project_refresh_event_manager(self, sender):\n        self._is_refreshing = True\n        self._controller.emit_event(\n            \"projects.refresh.started\",\n            {\"sender\": sender},\n            PROJECTS_MODEL_SENDER\n        )\n        try:\n            yield\n\n        finally:\n            self._controller.emit_event(\n                \"projects.refresh.finished\",\n                {\"sender\": sender},\n                PROJECTS_MODEL_SENDER\n            )\n            self._is_refreshing = False\n\n    @contextlib.contextmanager\n    def _project_items_refresh_event_manager(\n        self, sender, project_name, item_type\n    ):\n        self._controller.emit_event(\n            f\"projects.{item_type}.refresh.started\",\n            {\"sender\": sender, \"project_name\": project_name},\n            PROJECTS_MODEL_SENDER\n        )\n        try:\n            yield\n\n        finally:\n            self._controller.emit_event(\n                f\"projects.{item_type}.refresh.finished\",\n                {\"sender\": sender, \"project_name\": project_name},\n                PROJECTS_MODEL_SENDER\n            )\n\n    def _refresh_projects_cache(self, sender=None):\n        if self._is_refreshing:\n            return None\n\n        with self._project_refresh_event_manager(sender):\n            project_items = self._query_projects()\n            self._projects_cache.update_data(project_items)\n        return self._projects_cache.get_data()\n\n    def _fetch_graphql_projects(self) -&gt; list[dict[str, Any]]:\n        \"\"\"Fetch projects using GraphQl.\n\n        This method was added because ayon_api had a bug in 'get_projects'.\n\n        Returns:\n            list[dict[str, Any]]: List of projects.\n\n        \"\"\"\n        api = ayon_api.get_server_api_connection()\n        query = projects_graphql_query({\"name\", \"active\", \"library\", \"data\"})\n\n        projects = []\n        for parsed_data in query.continuous_query(api):\n            for project in parsed_data[\"projects\"]:\n                project_data = project[\"data\"]\n                if project_data is None:\n                    project[\"data\"] = {}\n                elif isinstance(project_data, str):\n                    project[\"data\"] = json.loads(project_data)\n                projects.append(project)\n        return projects\n\n    def _query_projects(self) -&gt; list[ProjectItem]:\n        projects = self._fetch_graphql_projects()\n\n        user = ayon_api.get_user()\n        pinned_projects = (\n            user\n            .get(\"data\", {})\n            .get(\"frontendPreferences\", {})\n            .get(\"pinnedProjects\")\n        ) or []\n        pinned_projects = set(pinned_projects)\n        project_items = _get_project_items_from_entity(list(projects))\n        for project in project_items:\n            project.is_pinned = project.name in pinned_projects\n        return project_items\n\n    def _status_items_getter(self, project_entity):\n        if not project_entity:\n            return []\n        return [\n            StatusItem.from_project_item(status)\n            for status in project_entity[\"statuses\"]\n        ]\n\n    def _folder_type_items_getter(self, project_entity):\n        if not project_entity:\n            return []\n        return [\n            FolderTypeItem.from_project_item(folder_type)\n            for folder_type in project_entity[\"folderTypes\"]\n        ]\n\n    def _task_type_items_getter(self, project_entity):\n        if not project_entity:\n            return []\n        return [\n            TaskTypeItem.from_project_item(task_type)\n            for task_type in project_entity[\"taskTypes\"]\n        ]\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/projects.html#client.ayon_core.tools.common_models.projects.ProjectsModel.get_folder_type_items","title":"<code>get_folder_type_items(project_name, sender)</code>","text":"<p>Get project status items.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>sender</code> <code>Union[str, None]</code> <p>Name of sender who asked for items.</p> required <p>Returns:</p> Type Description <p>list[FolderType]: Folder type items for project.</p> Source code in <code>client/ayon_core/tools/common_models/projects.py</code> <pre><code>def get_folder_type_items(self, project_name, sender):\n    \"\"\"Get project status items.\n\n    Args:\n        project_name (str): Project name.\n        sender (Union[str, None]): Name of sender who asked for items.\n\n    Returns:\n        list[FolderType]: Folder type items for project.\n\n    \"\"\"\n    return self._get_project_items(\n        project_name,\n        sender,\n        \"folder_types\",\n        self._folder_types_cache,\n        self._folder_type_items_getter,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/projects.html#client.ayon_core.tools.common_models.projects.ProjectsModel.get_project_anatomy_tags","title":"<code>get_project_anatomy_tags(project_name)</code>","text":"<p>Get project anatomy tags.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <p>Returns:</p> Type Description <code>list[TagItem]</code> <p>list[TagItem]: Tag definitions.</p> Source code in <code>client/ayon_core/tools/common_models/projects.py</code> <pre><code>def get_project_anatomy_tags(self, project_name: str) -&gt; list[TagItem]:\n    \"\"\"Get project anatomy tags.\n\n    Args:\n        project_name (str): Project name.\n\n    Returns:\n        list[TagItem]: Tag definitions.\n\n    \"\"\"\n    project_entity = self.get_project_entity(project_name)\n    return [\n        TagItem(tag[\"name\"], tag[\"color\"])\n        for tag in project_entity[\"tags\"]\n    ]\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/projects.html#client.ayon_core.tools.common_models.projects.ProjectsModel.get_project_entity","title":"<code>get_project_entity(project_name)</code>","text":"<p>Get project entity.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <p>Returns:</p> Type Description <p>Union[dict[str, Any], None]: Project entity or None if project was not found by name.</p> Source code in <code>client/ayon_core/tools/common_models/projects.py</code> <pre><code>def get_project_entity(self, project_name):\n    \"\"\"Get project entity.\n\n    Args:\n        project_name (str): Project name.\n\n    Returns:\n        Union[dict[str, Any], None]: Project entity or None if project\n            was not found by name.\n\n    \"\"\"\n    project_cache = self._projects_by_name[project_name]\n    if not project_cache.is_valid:\n        entity = None\n        if project_name:\n            entity = ayon_api.get_rest_project(project_name)\n        project_cache.update_data(entity)\n    return project_cache.get_data()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/projects.html#client.ayon_core.tools.common_models.projects.ProjectsModel.get_project_items","title":"<code>get_project_items(sender)</code>","text":"<p>Parameters:</p> Name Type Description Default <code>sender</code> <code>str</code> <p>Name of sender who asked for items.</p> required <p>Returns:</p> Type Description <p>Union[list[ProjectItem], None]: List of project items, or None if model is refreshing.</p> Source code in <code>client/ayon_core/tools/common_models/projects.py</code> <pre><code>def get_project_items(self, sender):\n    \"\"\"\n\n    Args:\n        sender (str): Name of sender who asked for items.\n\n    Returns:\n        Union[list[ProjectItem], None]: List of project items, or None\n            if model is refreshing.\n    \"\"\"\n\n    if not self._projects_cache.is_valid:\n        return self._refresh_projects_cache(sender)\n    return self._projects_cache.get_data()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/projects.html#client.ayon_core.tools.common_models.projects.ProjectsModel.get_project_status_items","title":"<code>get_project_status_items(project_name, sender)</code>","text":"<p>Get project status items.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>sender</code> <code>Union[str, None]</code> <p>Name of sender who asked for items.</p> required <p>Returns:</p> Type Description <p>list[StatusItem]: Status items for project.</p> Source code in <code>client/ayon_core/tools/common_models/projects.py</code> <pre><code>def get_project_status_items(self, project_name, sender):\n    \"\"\"Get project status items.\n\n    Args:\n        project_name (str): Project name.\n        sender (Union[str, None]): Name of sender who asked for items.\n\n    Returns:\n        list[StatusItem]: Status items for project.\n\n    \"\"\"\n    if project_name is None:\n        return []\n\n    statuses_cache = self._project_statuses_cache.get(project_name)\n    if (\n        statuses_cache is not None\n        and not self._projects_cache.is_valid\n    ):\n        statuses_cache = None\n\n    if statuses_cache is None:\n        with self._project_items_refresh_event_manager(\n            sender, project_name, \"statuses\"\n        ):\n            project_entity = self.get_project_entity(project_name)\n            statuses = []\n            if project_entity:\n                statuses = [\n                    StatusItem.from_project_item(status)\n                    for status in project_entity[\"statuses\"]\n                ]\n            statuses_cache = statuses\n        self._project_statuses_cache[project_name] = statuses_cache\n    return list(statuses_cache)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/projects.html#client.ayon_core.tools.common_models.projects.ProjectsModel.get_task_type_items","title":"<code>get_task_type_items(project_name, sender)</code>","text":"<p>Get project task type items.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>sender</code> <code>Union[str, None]</code> <p>Name of sender who asked for items.</p> required <p>Returns:</p> Type Description <p>list[TaskTypeItem]: Task type items for project.</p> Source code in <code>client/ayon_core/tools/common_models/projects.py</code> <pre><code>def get_task_type_items(self, project_name, sender):\n    \"\"\"Get project task type items.\n\n    Args:\n        project_name (str): Project name.\n        sender (Union[str, None]): Name of sender who asked for items.\n\n    Returns:\n        list[TaskTypeItem]: Task type items for project.\n\n    \"\"\"\n    return self._get_project_items(\n        project_name,\n        sender,\n        \"task_types\",\n        self._task_types_cache,\n        self._task_type_items_getter,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/projects.html#client.ayon_core.tools.common_models.projects.ProjectsModel.refresh","title":"<code>refresh()</code>","text":"<p>Refresh project items.</p> <p>This method will requery list of ProjectItem returned by 'get_project_items'.</p> <p>To reset all cached items use 'reset' method.</p> Source code in <code>client/ayon_core/tools/common_models/projects.py</code> <pre><code>def refresh(self):\n    \"\"\"Refresh project items.\n\n    This method will requery list of ProjectItem returned by\n    'get_project_items'.\n\n    To reset all cached items use 'reset' method.\n    \"\"\"\n    self._refresh_projects_cache()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/projects.html#client.ayon_core.tools.common_models.projects.StatusItem","title":"<code>StatusItem</code>","text":"<p>Item representing status of project.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Status name (\"Not ready\").</p> required <code>color</code> <code>str</code> <p>Status color in hex (\"#434a56\").</p> required <code>short</code> <code>str</code> <p>Short status name (\"NRD\").</p> required <code>icon</code> <code>str</code> <p>Icon name in MaterialIcons (\"fiber_new\").</p> required <code>state</code> <code>str</code> <p>Status state.</p> required Source code in <code>client/ayon_core/tools/common_models/projects.py</code> <pre><code>class StatusItem:\n    \"\"\"Item representing status of project.\n\n    Args:\n        name (str): Status name (\"Not ready\").\n        color (str): Status color in hex (\"#434a56\").\n        short (str): Short status name (\"NRD\").\n        icon (str): Icon name in MaterialIcons (\"fiber_new\").\n        state (str): Status state.\n\n    \"\"\"\n    def __init__(\n        self,\n        name: str,\n        color: str,\n        short: str,\n        icon: str,\n        state: str\n    ):\n        self.name: str = name\n        self.color: str = color\n        self.short: str = short\n        self.icon: str = icon\n        self.state: str = state\n\n    def to_data(self) -&gt; dict[str, Any]:\n        return {\n            \"name\": self.name,\n            \"color\": self.color,\n            \"short\": self.short,\n            \"icon\": self.icon,\n            \"state\": self.state,\n        }\n\n    @classmethod\n    def from_data(cls, data):\n        return cls(**data)\n\n    @classmethod\n    def from_project_item(cls, status_data):\n        return cls(\n            name=status_data[\"name\"],\n            color=status_data[\"color\"],\n            short=status_data[\"shortName\"],\n            icon=status_data[\"icon\"],\n            state=status_data[\"state\"],\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/projects.html#client.ayon_core.tools.common_models.projects.TagItem","title":"<code>TagItem</code>  <code>dataclass</code>","text":"<p>Tag definition set on project anatomy.</p> Source code in <code>client/ayon_core/tools/common_models/projects.py</code> <pre><code>@dataclass\nclass TagItem:\n    \"\"\"Tag definition set on project anatomy.\"\"\"\n    name: str\n    color: str\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/projects.html#client.ayon_core.tools.common_models.projects.TaskTypeItem","title":"<code>TaskTypeItem</code>","text":"<p>Item representing task type of project.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Task type name (\"Shot\").</p> required <code>short</code> <code>str</code> <p>Short task type name (\"sh\").</p> required <code>icon</code> <code>str</code> <p>Icon name in MaterialIcons (\"fiber_new\").</p> required Source code in <code>client/ayon_core/tools/common_models/projects.py</code> <pre><code>class TaskTypeItem:\n    \"\"\"Item representing task type of project.\n\n    Args:\n        name (str): Task type name (\"Shot\").\n        short (str): Short task type name (\"sh\").\n        icon (str): Icon name in MaterialIcons (\"fiber_new\").\n\n    \"\"\"\n    def __init__(\n        self,\n        name: str,\n        short: str,\n        icon: str,\n        color: Optional[str],\n    ):\n        self.name = name\n        self.short = short\n        self.icon = icon\n        self.color = color\n\n    def to_data(self):\n        return {\n            \"name\": self.name,\n            \"short\": self.short,\n            \"icon\": self.icon,\n            \"color\": self.color,\n        }\n\n    @classmethod\n    def from_data(cls, data):\n        return cls(**data)\n\n    @classmethod\n    def from_project_item(cls, task_type_data):\n        return cls(\n            name=task_type_data[\"name\"],\n            short=task_type_data[\"shortName\"],\n            icon=task_type_data[\"icon\"],\n            color=task_type_data.get(\"color\"),\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/selection.html","title":"selection","text":""},{"location":"autoapi/client/ayon_core/tools/common_models/selection.html#client.ayon_core.tools.common_models.selection.HierarchyExpectedSelection","title":"<code>HierarchyExpectedSelection</code>","text":"<p>Base skeleton of expected selection model.</p> <p>Expected selection model holds information about which entities should be selected. The order of selection is very important as change of project will affect what folders are available in folders UI and so on. Because of that should expected selection model know what is current entity to select.</p> <p>If any of 'handle_project', 'handle_folder' or 'handle_task' is set to 'False' expected selection data won't contain information about the entity type at all. Also if project is not handled then it is not necessary to call 'expected_project_selected'. Same goes for folder and task.</p> <p>Model is triggering event with 'expected_selection_changed' topic and data &gt; data structure is matching 'get_expected_selection_data' method.</p> Questions <p>Require '_ExampleController' as abstraction?</p> <p>Parameters:</p> Name Type Description Default <code>controller</code> <code>Any</code> <p>Controller object. ('_ExampleController')</p> required <code>handle_project</code> <code>bool</code> <p>Project can be considered as can have expected selection.</p> <code>True</code> <code>handle_folder</code> <code>bool</code> <p>Folder can be considered as can have expected selection.</p> <code>True</code> <code>handle_task</code> <code>bool</code> <p>Task can be considered as can have expected selection.</p> <code>True</code> Source code in <code>client/ayon_core/tools/common_models/selection.py</code> <pre><code>class HierarchyExpectedSelection:\n    \"\"\"Base skeleton of expected selection model.\n\n    Expected selection model holds information about which entities should be\n    selected. The order of selection is very important as change of project\n    will affect what folders are available in folders UI and so on. Because\n    of that should expected selection model know what is current entity\n    to select.\n\n    If any of 'handle_project', 'handle_folder' or 'handle_task' is set to\n    'False' expected selection data won't contain information about the\n    entity type at all. Also if project is not handled then it is not\n    necessary to call 'expected_project_selected'. Same goes for folder and\n    task.\n\n    Model is triggering event with 'expected_selection_changed' topic and\n    data &gt; data structure is matching 'get_expected_selection_data' method.\n\n    Questions:\n        Require '_ExampleController' as abstraction?\n\n    Args:\n        controller (Any): Controller object. ('_ExampleController')\n        handle_project (bool): Project can be considered as can have expected\n            selection.\n        handle_folder (bool): Folder can be considered as can have expected\n            selection.\n        handle_task (bool): Task can be considered as can have expected\n            selection.\n    \"\"\"\n\n    def __init__(\n        self,\n        controller,\n        handle_project=True,\n        handle_folder=True,\n        handle_task=True\n    ):\n        self._project_name = None\n        self._folder_id = None\n        self._task_name = None\n\n        self._project_selected = True\n        self._folder_selected = True\n        self._task_selected = True\n\n        self._controller = controller\n\n        self._handle_project = handle_project\n        self._handle_folder = handle_folder\n        self._handle_task = handle_task\n\n    def set_expected_selection(\n        self,\n        project_name=None,\n        folder_id=None,\n        task_name=None\n    ):\n        \"\"\"Sets expected selection.\n\n        Args:\n            project_name (Optional[str]): Project name.\n            folder_id (Optional[str]): Folder id.\n            task_name (Optional[str]): Task name.\n        \"\"\"\n\n        self._project_name = project_name\n        self._folder_id = folder_id\n        self._task_name = task_name\n\n        self._project_selected = not self._handle_project\n        self._folder_selected = not self._handle_folder\n        self._task_selected = not self._handle_task\n        self._emit_change()\n\n    def get_expected_selection_data(self):\n        project_current = False\n        folder_current = False\n        task_current = False\n        if not self._project_selected:\n            project_current = True\n        elif not self._folder_selected:\n            folder_current = True\n        elif not self._task_selected:\n            task_current = True\n        data = {}\n        if self._handle_project:\n            data[\"project\"] = {\n                \"name\": self._project_name,\n                \"current\": project_current,\n                \"selected\": self._project_selected,\n            }\n        if self._handle_folder:\n            data[\"folder\"] = {\n                \"id\": self._folder_id,\n                \"current\": folder_current,\n                \"selected\": self._folder_selected,\n            }\n        if self._handle_task:\n            data[\"task\"] = {\n                \"name\": self._task_name,\n                \"current\": task_current,\n                \"selected\": self._task_selected,\n            }\n\n        return data\n\n    def is_expected_project_selected(self, project_name):\n        if not self._handle_project:\n            return True\n        return project_name == self._project_name and self._project_selected\n\n    def is_expected_folder_selected(self, folder_id):\n        if not self._handle_folder:\n            return True\n        return folder_id == self._folder_id and self._folder_selected\n\n    def expected_project_selected(self, project_name):\n        \"\"\"UI selected requested project.\n\n        Other entity types can be requested for selection.\n\n        Args:\n            project_name (str): Name of project.\n        \"\"\"\n\n        if project_name != self._project_name:\n            return False\n        self._project_selected = True\n        self._emit_change()\n        return True\n\n    def expected_folder_selected(self, folder_id):\n        \"\"\"UI selected requested folder.\n\n        Other entity types can be requested for selection.\n\n        Args:\n            folder_id (str): Folder id.\n        \"\"\"\n\n        if folder_id != self._folder_id:\n            return False\n        self._folder_selected = True\n        self._emit_change()\n        return True\n\n    def expected_task_selected(self, folder_id, task_name):\n        \"\"\"UI selected requested task.\n\n        Other entity types can be requested for selection.\n\n        Because task name is not unique across project a folder id is also\n        required to confirm the right task has been selected.\n\n        Args:\n            folder_id (str): Folder id.\n            task_name (str): Task name.\n        \"\"\"\n\n        if self._folder_id != folder_id:\n            return False\n\n        if task_name != self._task_name:\n            return False\n        self._task_selected = True\n        self._emit_change()\n        return True\n\n    def _emit_change(self):\n        self._controller.emit_event(\n            \"expected_selection_changed\",\n            self.get_expected_selection_data(),\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/selection.html#client.ayon_core.tools.common_models.selection.HierarchyExpectedSelection.expected_folder_selected","title":"<code>expected_folder_selected(folder_id)</code>","text":"<p>UI selected requested folder.</p> <p>Other entity types can be requested for selection.</p> <p>Parameters:</p> Name Type Description Default <code>folder_id</code> <code>str</code> <p>Folder id.</p> required Source code in <code>client/ayon_core/tools/common_models/selection.py</code> <pre><code>def expected_folder_selected(self, folder_id):\n    \"\"\"UI selected requested folder.\n\n    Other entity types can be requested for selection.\n\n    Args:\n        folder_id (str): Folder id.\n    \"\"\"\n\n    if folder_id != self._folder_id:\n        return False\n    self._folder_selected = True\n    self._emit_change()\n    return True\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/selection.html#client.ayon_core.tools.common_models.selection.HierarchyExpectedSelection.expected_project_selected","title":"<code>expected_project_selected(project_name)</code>","text":"<p>UI selected requested project.</p> <p>Other entity types can be requested for selection.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Name of project.</p> required Source code in <code>client/ayon_core/tools/common_models/selection.py</code> <pre><code>def expected_project_selected(self, project_name):\n    \"\"\"UI selected requested project.\n\n    Other entity types can be requested for selection.\n\n    Args:\n        project_name (str): Name of project.\n    \"\"\"\n\n    if project_name != self._project_name:\n        return False\n    self._project_selected = True\n    self._emit_change()\n    return True\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/selection.html#client.ayon_core.tools.common_models.selection.HierarchyExpectedSelection.expected_task_selected","title":"<code>expected_task_selected(folder_id, task_name)</code>","text":"<p>UI selected requested task.</p> <p>Other entity types can be requested for selection.</p> <p>Because task name is not unique across project a folder id is also required to confirm the right task has been selected.</p> <p>Parameters:</p> Name Type Description Default <code>folder_id</code> <code>str</code> <p>Folder id.</p> required <code>task_name</code> <code>str</code> <p>Task name.</p> required Source code in <code>client/ayon_core/tools/common_models/selection.py</code> <pre><code>def expected_task_selected(self, folder_id, task_name):\n    \"\"\"UI selected requested task.\n\n    Other entity types can be requested for selection.\n\n    Because task name is not unique across project a folder id is also\n    required to confirm the right task has been selected.\n\n    Args:\n        folder_id (str): Folder id.\n        task_name (str): Task name.\n    \"\"\"\n\n    if self._folder_id != folder_id:\n        return False\n\n    if task_name != self._task_name:\n        return False\n    self._task_selected = True\n    self._emit_change()\n    return True\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/selection.html#client.ayon_core.tools.common_models.selection.HierarchyExpectedSelection.set_expected_selection","title":"<code>set_expected_selection(project_name=None, folder_id=None, task_name=None)</code>","text":"<p>Sets expected selection.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>Optional[str]</code> <p>Project name.</p> <code>None</code> <code>folder_id</code> <code>Optional[str]</code> <p>Folder id.</p> <code>None</code> <code>task_name</code> <code>Optional[str]</code> <p>Task name.</p> <code>None</code> Source code in <code>client/ayon_core/tools/common_models/selection.py</code> <pre><code>def set_expected_selection(\n    self,\n    project_name=None,\n    folder_id=None,\n    task_name=None\n):\n    \"\"\"Sets expected selection.\n\n    Args:\n        project_name (Optional[str]): Project name.\n        folder_id (Optional[str]): Folder id.\n        task_name (Optional[str]): Task name.\n    \"\"\"\n\n    self._project_name = project_name\n    self._folder_id = folder_id\n    self._task_name = task_name\n\n    self._project_selected = not self._handle_project\n    self._folder_selected = not self._handle_folder\n    self._task_selected = not self._handle_task\n    self._emit_change()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/settings.html","title":"settings","text":""},{"location":"autoapi/client/ayon_core/tools/common_models/thumbnails.html","title":"thumbnails","text":""},{"location":"autoapi/client/ayon_core/tools/common_models/users.html","title":"users","text":""},{"location":"autoapi/client/ayon_core/tools/common_models/users.html#client.ayon_core.tools.common_models.users.UsersModel","title":"<code>UsersModel</code>","text":"Source code in <code>client/ayon_core/tools/common_models/users.py</code> <pre><code>class UsersModel:\n    def __init__(self, controller):\n        self._current_username = NOT_SET\n        self._controller = controller\n        self._users_cache = NestedCacheItem(default_factory=list)\n\n    def get_current_username(self) -&gt; Optional[str]:\n        if self._current_username is NOT_SET:\n            self._current_username = get_ayon_username()\n        return self._current_username\n\n    def reset(self) -&gt; None:\n        self._users_cache.reset()\n\n    def get_user_items(self, project_name):\n        \"\"\"Get user items.\n\n        Returns:\n            List[UserItem]: List of user items.\n\n        \"\"\"\n        self._invalidate_cache(project_name)\n        return self._users_cache[project_name].get_data()\n\n    def get_user_items_by_name(self, project_name):\n        \"\"\"Get user items by name.\n\n        Implemented as most of cases using this model will need to find\n            user information by username.\n\n        Returns:\n            Dict[str, UserItem]: Dictionary of user items by name.\n\n        \"\"\"\n        return {\n            user_item.username: user_item\n            for user_item in self.get_user_items(project_name)\n        }\n\n    def get_user_item_by_username(self, project_name, username):\n        \"\"\"Get user item by username.\n\n        Args:\n            username (str): Username.\n\n        Returns:\n            Union[UserItem, None]: User item or None if not found.\n\n        \"\"\"\n        self._invalidate_cache(project_name)\n        for user_item in self.get_user_items(project_name):\n            if user_item.username == username:\n                return user_item\n        return None\n\n    def _invalidate_cache(self, project_name):\n        cache = self._users_cache[project_name]\n        if cache.is_valid:\n            return\n\n        if project_name is None:\n            cache.update_data([])\n            return\n\n        self._users_cache[project_name].update_data([\n            UserItem.from_entity_data(user)\n            for user in ayon_api.get_users(project_name=project_name)\n        ])\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/users.html#client.ayon_core.tools.common_models.users.UsersModel.get_user_item_by_username","title":"<code>get_user_item_by_username(project_name, username)</code>","text":"<p>Get user item by username.</p> <p>Parameters:</p> Name Type Description Default <code>username</code> <code>str</code> <p>Username.</p> required <p>Returns:</p> Type Description <p>Union[UserItem, None]: User item or None if not found.</p> Source code in <code>client/ayon_core/tools/common_models/users.py</code> <pre><code>def get_user_item_by_username(self, project_name, username):\n    \"\"\"Get user item by username.\n\n    Args:\n        username (str): Username.\n\n    Returns:\n        Union[UserItem, None]: User item or None if not found.\n\n    \"\"\"\n    self._invalidate_cache(project_name)\n    for user_item in self.get_user_items(project_name):\n        if user_item.username == username:\n            return user_item\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/users.html#client.ayon_core.tools.common_models.users.UsersModel.get_user_items","title":"<code>get_user_items(project_name)</code>","text":"<p>Get user items.</p> <p>Returns:</p> Type Description <p>List[UserItem]: List of user items.</p> Source code in <code>client/ayon_core/tools/common_models/users.py</code> <pre><code>def get_user_items(self, project_name):\n    \"\"\"Get user items.\n\n    Returns:\n        List[UserItem]: List of user items.\n\n    \"\"\"\n    self._invalidate_cache(project_name)\n    return self._users_cache[project_name].get_data()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/common_models/users.html#client.ayon_core.tools.common_models.users.UsersModel.get_user_items_by_name","title":"<code>get_user_items_by_name(project_name)</code>","text":"<p>Get user items by name.</p> <p>Implemented as most of cases using this model will need to find     user information by username.</p> <p>Returns:</p> Type Description <p>Dict[str, UserItem]: Dictionary of user items by name.</p> Source code in <code>client/ayon_core/tools/common_models/users.py</code> <pre><code>def get_user_items_by_name(self, project_name):\n    \"\"\"Get user items by name.\n\n    Implemented as most of cases using this model will need to find\n        user information by username.\n\n    Returns:\n        Dict[str, UserItem]: Dictionary of user items by name.\n\n    \"\"\"\n    return {\n        user_item.username: user_item\n        for user_item in self.get_user_items(project_name)\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/console_interpreter/index.html","title":"console_interpreter","text":""},{"location":"autoapi/client/ayon_core/tools/console_interpreter/abstract.html","title":"abstract","text":""},{"location":"autoapi/client/ayon_core/tools/console_interpreter/control.html","title":"control","text":""},{"location":"autoapi/client/ayon_core/tools/console_interpreter/ui/index.html","title":"ui","text":""},{"location":"autoapi/client/ayon_core/tools/console_interpreter/ui/code_style.html","title":"code_style","text":""},{"location":"autoapi/client/ayon_core/tools/console_interpreter/ui/code_style.html#client.ayon_core.tools.console_interpreter.ui.code_style.AYONCodeStyle","title":"<code>AYONCodeStyle</code>","text":"<p>               Bases: <code>Style</code></p> <p>A Pygments style similar to GitHub's pretty lights dark theme</p> Source code in <code>client/ayon_core/tools/console_interpreter/ui/code_style.py</code> <pre><code>class AYONCodeStyle(Style):\n    \"\"\"A Pygments style similar to GitHub's pretty lights dark theme\"\"\"\n\n    background_color = None\n    default_style = ''\n\n    styles = {\n        Whitespace: \"#f0f6fc\",\n\n        Comment: pl[\"syntax-comment\"],\n        Comment.Hashbang: pl[\"syntax-comment\"],\n        Comment.Multiline: pl[\"syntax-comment\"],\n        Comment.Preproc: pl[\"syntax-comment\"],\n        Comment.Single: pl[\"syntax-comment\"],\n        Comment.Special: pl[\"syntax-comment\"],\n\n        Generic: \"#f0f6fc\",\n        Generic.Deleted: \"#8b080b\",\n        Generic.Emph: \"#f8f8f2 underline\",\n        Generic.Error: \"#f8f8f2\",\n        Generic.Heading: \"#f8f8f2 bold\",\n        Generic.Inserted: \"#f8f8f2 bold\",\n        Generic.Output: \"#adaeb6\",\n        Generic.Prompt: \"#f8f8f2\",\n        Generic.Strong: \"#f8f8f2\",\n        Generic.Subheading: \"#f8f8f2 bold\",\n        Generic.Traceback: \"#f8f8f2\",\n        Error: \"#f8f8f2\",\n\n        Keyword: pl[\"syntax-keyword\"],\n        Keyword.Constant: pl[\"syntax-constant\"],  # Ex. None\n        Keyword.Declaration: pl[\"syntax-keyword\"],\n        Keyword.Namespace: pl[\"syntax-keyword\"],\n        Keyword.Pseudo: pl[\"syntax-entity\"],\n        Keyword.Reserved: pl[\"syntax-constant\"],\n        Keyword.Type: pl[\"syntax-constant\"],\n\n        Literal: \"#f8f8f2\",\n        Literal.Date: \"#f8f8f2\",\n        Literal.String.Affix: \"#f8f8f2\",\n        Literal.String.Doc: \"#f8f8f2\",\n        Literal.String.Double: \"#f8f8f2\",\n        Literal.String.Interpol: \"#f8f8f2\",\n        Literal.String.Single: \"#f8f8f2\",\n\n        Name: pl[\"syntax-markup-bold\"],\n        Name.Variable: pl[\"syntax-markup-bold\"],\n        Name.Attribute: pl[\"syntax-markup-bold\"],\n        Name.Builtin.Pseudo: pl[\"syntax-markup-bold\"],  # Ex. self\n        Name.Builtin: pl[\"syntax-entity\"],  # Ex. print()\n        Name.Class: pl[\"syntax-variable\"],\n        Name.Constant: pl[\"syntax-constant\"],\n        Name.Decorator: pl[\"syntax-entity\"],\n        Name.Entity: pl[\"syntax-entity\"],\n        Name.Exception: pl[\"syntax-variable\"],\n        Name.Function: pl[\"syntax-entity\"],\n        Name.Function.Magic: pl[\"syntax-entity\"],\n        # Name.Label: \"#8be9fd italic\",\n        Name.Namespace: pl[\"syntax-markup-bold\"],\n        Name.Other: pl[\"syntax-markup-bold\"],\n        # Name.Other: pl[\"syntax-variable\"],\n        # Name.Tag: \"#ff79c6\",\n        Name.Variable.Class: pl[\"syntax-variable\"],\n        Name.Variable.Global: pl[\"syntax-variable\"],\n        Name.Variable.Instance: pl[\"syntax-markup-bold\"],\n        Name.Variable.Magic: pl[\"syntax-markup-bold\"],\n\n        Number: pl[\"syntax-constant\"],\n        Number.Bin: pl[\"syntax-constant\"],\n        Number.Float: pl[\"syntax-constant\"],\n        Number.Hex: pl[\"syntax-constant\"],\n        Number.Integer: pl[\"syntax-constant\"],\n        Number.Integer.Long: pl[\"syntax-constant\"],\n        Number.Oct: pl[\"syntax-constant\"],\n        Operator: pl[\"syntax-constant\"],\n        Operator.Word: pl[\"syntax-constant\"],\n\n        # Other: \"#f8f8f2\",\n        Other.Constant: pl[\"syntax-constant\"],\n        Punctuation: \"#f8f8f2\",\n        Punctuation.Definition.Comment: pl[\"syntax-comment\"],\n        String: pl[\"syntax-string\"],\n        String.Affix: pl[\"syntax-string\"],\n        String.Backtick: pl[\"syntax-string\"],\n        String.Char: pl[\"syntax-string\"],\n        String.Comment: pl[\"syntax-comment\"],\n        String.Doc: pl[\"syntax-string\"],\n        String.Double: pl[\"syntax-string\"],\n        String.Escape: pl[\"syntax-string\"],\n        String.Heredoc: pl[\"syntax-string\"],\n        String.Interpol: pl[\"syntax-string\"],\n        String.Other: pl[\"syntax-string\"],\n        String.Regex: pl[\"syntax-string\"],\n        String.Single: pl[\"syntax-string\"],\n        String.Symbol: pl[\"syntax-string\"],\n        Text: pl[\"syntax-markup-bold\"],\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/console_interpreter/ui/syntax_highlight.html","title":"syntax_highlight","text":""},{"location":"autoapi/client/ayon_core/tools/console_interpreter/ui/syntax_highlight.html#client.ayon_core.tools.console_interpreter.ui.syntax_highlight.PythonSyntaxHighlighter","title":"<code>PythonSyntaxHighlighter</code>","text":"<p>               Bases: <code>QSyntaxHighlighter</code></p> Source code in <code>client/ayon_core/tools/console_interpreter/ui/syntax_highlight.py</code> <pre><code>class PythonSyntaxHighlighter(QtGui.QSyntaxHighlighter):\n    def __init__(self, document, style_name: str = \"\"):\n        self.lexer = pygments.lexers.PythonLexer()\n        self.style = AYONCodeStyle\n\n        # Allow to override style by environment variable\n        override_style_name = os.getenv(\"AYON_CONSOLE_INTERPRETER_STYLE\")\n        if override_style_name:\n\n            try:\n                style = pygments.styles.get_style_by_name(override_style_name)\n            except ClassNotFound:\n                all_styles = \", \".join(pygments.styles.get_all_styles())\n                raise ValueError(\n                    f\"'{override_style_name}' not found. \"\n                    f\"Installed styles: {all_styles}.\"\n                )\n            else:\n                self.style = style\n\n        self.formats = self.style_to_formats(self.style)\n\n        # init after validating and loading the style\n        super().__init__(document)\n\n    def highlightBlock(self, text: str | None) -&gt; None:\n        if text is None:\n            return\n\n        index = 0\n        for token, value in self.lexer.get_tokens(text):\n            length = len(value)\n            if fmt := self.formats.get(token):\n                self.setFormat(index, length, fmt)\n\n            index += length\n\n    def _format_for_token(self, token):\n        if token is None:\n            return None\n        if token in self.formats:\n            return self.formats[token]\n        if token.parent:\n            return self._format_for_token(token.parent)\n        return None\n\n    def style_to_formats(\n        self,\n        style: pygments.styles.Style\n    ) -&gt; dict[pygments.token.Token, QtGui.QTextCharFormat]:\n        \"\"\"Convert a Pygments style to a dictionary of Qt formats.\n\n        Args:\n            style: The Pygments style to convert.\n\n        Returns:\n            token_format = QTextCharFormat for each token in the style.\n\n        \"\"\"\n        formats = {}\n        for token, _ in style.styles.items():\n\n            token_style = style.style_for_token(token)\n            if not token_style:\n                continue\n\n            token_format = QtGui.QTextCharFormat()\n            if color := token_style.get(\"color\"):\n                color = f\"#{color}\"\n                token_format.setForeground(QtGui.QColor(color))\n            if token_style.get(\"bold\"):\n                token_format.setFontWeight(QtGui.QFont.Bold)\n            if token_style.get(\"italic\"):\n                token_format.setFontItalic(True)\n            if token_style.get(\"underline\"):\n                token_format.setFontUnderline(True)\n\n            formats[token] = token_format\n\n        return formats\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/console_interpreter/ui/syntax_highlight.html#client.ayon_core.tools.console_interpreter.ui.syntax_highlight.PythonSyntaxHighlighter.style_to_formats","title":"<code>style_to_formats(style)</code>","text":"<p>Convert a Pygments style to a dictionary of Qt formats.</p> <p>Parameters:</p> Name Type Description Default <code>style</code> <code>Style</code> <p>The Pygments style to convert.</p> required <p>Returns:</p> Type Description <code>dict[Token, QTextCharFormat]</code> <p>token_format = QTextCharFormat for each token in the style.</p> Source code in <code>client/ayon_core/tools/console_interpreter/ui/syntax_highlight.py</code> <pre><code>def style_to_formats(\n    self,\n    style: pygments.styles.Style\n) -&gt; dict[pygments.token.Token, QtGui.QTextCharFormat]:\n    \"\"\"Convert a Pygments style to a dictionary of Qt formats.\n\n    Args:\n        style: The Pygments style to convert.\n\n    Returns:\n        token_format = QTextCharFormat for each token in the style.\n\n    \"\"\"\n    formats = {}\n    for token, _ in style.styles.items():\n\n        token_style = style.style_for_token(token)\n        if not token_style:\n            continue\n\n        token_format = QtGui.QTextCharFormat()\n        if color := token_style.get(\"color\"):\n            color = f\"#{color}\"\n            token_format.setForeground(QtGui.QColor(color))\n        if token_style.get(\"bold\"):\n            token_format.setFontWeight(QtGui.QFont.Bold)\n        if token_style.get(\"italic\"):\n            token_format.setFontItalic(True)\n        if token_style.get(\"underline\"):\n            token_format.setFontUnderline(True)\n\n        formats[token] = token_format\n\n    return formats\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/console_interpreter/ui/utils.html","title":"utils","text":""},{"location":"autoapi/client/ayon_core/tools/console_interpreter/ui/widgets.html","title":"widgets","text":""},{"location":"autoapi/client/ayon_core/tools/console_interpreter/ui/window.html","title":"window","text":""},{"location":"autoapi/client/ayon_core/tools/context_dialog/index.html","title":"context_dialog","text":""},{"location":"autoapi/client/ayon_core/tools/context_dialog/index.html#client.ayon_core.tools.context_dialog.ContextDialog","title":"<code>ContextDialog</code>","text":"<p>               Bases: <code>QDialog</code></p> <p>Dialog to select a context.</p> <p>Context has 3 parts: - Project - Folder - Task</p> <p>It is possible to predefine project and folder. In that case their widgets will have passed preselected values and will be disabled.</p> Source code in <code>client/ayon_core/tools/context_dialog/window.py</code> <pre><code>class ContextDialog(QtWidgets.QDialog):\n    \"\"\"Dialog to select a context.\n\n    Context has 3 parts:\n    - Project\n    - Folder\n    - Task\n\n    It is possible to predefine project and folder. In that case their widgets\n    will have passed preselected values and will be disabled.\n    \"\"\"\n    def __init__(self, controller=None, parent=None):\n        super(ContextDialog, self).__init__(parent)\n\n        self.setWindowTitle(\"Select Context\")\n        self.setWindowIcon(QtGui.QIcon(style.app_icon_path()))\n\n        if controller is None:\n            controller = ContextDialogController()\n\n        # Enable minimize and maximize for app\n        window_flags = QtCore.Qt.Window\n        if not parent:\n            window_flags |= QtCore.Qt.WindowStaysOnTopHint\n        self.setWindowFlags(window_flags)\n        self.setFocusPolicy(QtCore.Qt.StrongFocus)\n\n        # UI initialization\n        main_splitter = QtWidgets.QSplitter(self)\n\n        # Left side widget contains project combobox and folders widget\n        left_side_widget = QtWidgets.QWidget(main_splitter)\n\n        project_combobox = ProjectsCombobox(\n            controller,\n            parent=left_side_widget,\n            handle_expected_selection=True\n        )\n        project_combobox.set_select_item_visible(True)\n\n        # Folders widget\n        folders_widget = FoldersWidget(\n            controller,\n            parent=left_side_widget,\n            handle_expected_selection=True\n        )\n\n        left_side_layout = QtWidgets.QVBoxLayout(left_side_widget)\n        left_side_layout.setContentsMargins(0, 0, 0, 0)\n        left_side_layout.addWidget(project_combobox, 0)\n        left_side_layout.addWidget(folders_widget, 1)\n\n        # Right side of window contains only tasks\n        tasks_widget = TasksWidget(controller, parent=main_splitter)\n\n        # Add widgets to main splitter\n        main_splitter.addWidget(left_side_widget)\n        main_splitter.addWidget(tasks_widget)\n\n        # Set stretch of both sides\n        main_splitter.setStretchFactor(0, 7)\n        main_splitter.setStretchFactor(1, 3)\n\n        # Add confimation button to bottom right\n        ok_btn = QtWidgets.QPushButton(\"OK\", self)\n\n        buttons_layout = QtWidgets.QHBoxLayout()\n        buttons_layout.setContentsMargins(0, 0, 0, 0)\n        buttons_layout.addStretch(1)\n        buttons_layout.addWidget(ok_btn, 0)\n\n        main_layout = QtWidgets.QVBoxLayout(self)\n        main_layout.addWidget(main_splitter, 1)\n        main_layout.addLayout(buttons_layout, 0)\n\n        overlay_widget = InvalidContextOverlay(self)\n        overlay_widget.setVisible(False)\n\n        ok_btn.clicked.connect(self._on_ok_click)\n        project_combobox.refreshed.connect(self._on_projects_refresh)\n        overlay_widget.confirmed.connect(self._on_overlay_confirm)\n\n        controller.register_event_callback(\n            \"selection.project.changed\",\n            self._on_project_selection_change\n        )\n        controller.register_event_callback(\n            \"selection.folder.changed\",\n            self._on_folder_selection_change\n        )\n        controller.register_event_callback(\n            \"selection.task.changed\",\n            self._on_task_selection_change\n        )\n        controller.register_event_callback(\n            \"initial.context.changed\",\n            self._on_init_context_change\n        )\n        controller.register_event_callback(\n            \"strict.changed\",\n            self._on_strict_changed\n        )\n        controller.register_event_callback(\n            \"controller.reset.finished\",\n            self._on_controller_reset\n        )\n        controller.register_event_callback(\n            \"controller.refresh.finished\",\n            self._on_controller_refresh\n        )\n\n        # Set stylehseet and resize window on first show\n        self._first_show = True\n        self._visible = False\n\n        self._controller = controller\n\n        self._project_combobox = project_combobox\n        self._folders_widget = folders_widget\n        self._tasks_widget = tasks_widget\n\n        self._ok_btn = ok_btn\n\n        self._overlay_widget = overlay_widget\n\n        self._apply_strict_changes(self.is_strict())\n\n    def is_strict(self):\n        return self._controller.is_strict()\n\n    def showEvent(self, event):\n        \"\"\"Override show event to do some callbacks.\"\"\"\n        super(ContextDialog, self).showEvent(event)\n        self._visible = True\n\n        if self._first_show:\n            self._first_show = False\n            # Set stylesheet and resize\n            self.setStyleSheet(style.load_stylesheet())\n            self.resize(600, 700)\n            center_window(self)\n        self._controller.refresh()\n\n        initial_context = self._controller.get_initial_context()\n        self._set_init_context(initial_context)\n        self._overlay_widget.resize(self.size())\n\n    def resizeEvent(self, event):\n        super(ContextDialog, self).resizeEvent(event)\n        self._overlay_widget.resize(self.size())\n\n    def closeEvent(self, event):\n        \"\"\"Ignore close event if is in strict state and context is not done.\"\"\"\n        if self.is_strict() and not self._ok_btn.isEnabled():\n            # Allow to close window when initial context is not valid\n            if self._controller.is_initial_context_valid():\n                event.ignore()\n                return\n\n        if self.is_strict():\n            self._confirm_selection()\n        self._visible = False\n        super(ContextDialog, self).closeEvent(event)\n\n    def set_strict(self, enabled):\n        \"\"\"Change strictness of dialog.\"\"\"\n\n        self._controller.set_strict(enabled)\n\n    def refresh(self):\n        \"\"\"Refresh all widget one by one.\"\"\"\n\n        self._controller.reset()\n\n    def get_context(self):\n        \"\"\"Result of dialog.\"\"\"\n        return self._controller.get_selected_context()\n\n    def set_context(self, project_name=None, folder_path=None):\n        \"\"\"Set context which will be used and locked in dialog.\"\"\"\n\n        self._controller.set_initial_context(project_name, folder_path)\n\n    def _on_projects_refresh(self):\n        initial_context = self._controller.get_initial_context()\n        self._controller.set_expected_selection(\n            initial_context[\"project_name\"],\n            initial_context[\"folder_id\"]\n        )\n\n    def _on_overlay_confirm(self):\n        self.close()\n\n    def _on_ok_click(self):\n        # Store values to output\n        self._confirm_selection()\n        # Close dialog\n        self.accept()\n\n    def _confirm_selection(self):\n        self._controller.confirm_selection()\n\n    def _on_project_selection_change(self, event):\n        self._on_selection_change(\n            event[\"project_name\"],\n        )\n\n    def _on_folder_selection_change(self, event):\n        self._on_selection_change(\n            event[\"project_name\"],\n            event[\"folder_id\"],\n        )\n\n    def _on_task_selection_change(self, event):\n        self._on_selection_change(\n            event[\"project_name\"],\n            event[\"folder_id\"],\n            event[\"task_name\"],\n        )\n\n    def _on_selection_change(\n        self, project_name, folder_id=None, task_name=None\n    ):\n        self._validate_strict(project_name, folder_id, task_name)\n\n    def _on_init_context_change(self, event):\n        self._set_init_context(event.data)\n        if self._visible:\n            self._controller.set_expected_selection(\n                event[\"project_name\"], event[\"folder_id\"]\n            )\n\n    def _set_init_context(self, init_context):\n        project_name = init_context[\"project_name\"]\n        if not init_context[\"valid\"]:\n            self._overlay_widget.setVisible(True)\n            self._overlay_widget.set_context(\n                project_name,\n                init_context[\"folder_label\"],\n                init_context[\"project_found\"],\n                init_context[\"folder_found\"],\n                init_context[\"tasks_found\"]\n            )\n            return\n\n        self._overlay_widget.setVisible(False)\n        if project_name:\n            self._project_combobox.setEnabled(False)\n            if init_context[\"folder_id\"]:\n                self._folders_widget.setEnabled(False)\n        else:\n            self._project_combobox.setEnabled(True)\n            self._folders_widget.setEnabled(True)\n\n    def _on_strict_changed(self, event):\n        self._apply_strict_changes(event[\"strict\"])\n\n    def _on_controller_reset(self):\n        self._apply_strict_changes(self.is_strict())\n        self._project_combobox.refresh()\n\n    def _on_controller_refresh(self):\n        self._project_combobox.refresh()\n\n    def _apply_strict_changes(self, is_strict):\n        if not is_strict:\n            if not self._ok_btn.isEnabled():\n                self._ok_btn.setEnabled(True)\n            return\n        context = self._controller.get_selected_context()\n        self._validate_strict(\n            context[\"project_name\"],\n            context[\"folder_id\"],\n            context[\"task_name\"]\n        )\n\n    def _validate_strict(self, project_name, folder_id, task_name):\n        if not self.is_strict():\n            return\n\n        enabled = True\n        if not project_name or not folder_id or not task_name:\n            enabled = False\n        self._ok_btn.setEnabled(enabled)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/context_dialog/index.html#client.ayon_core.tools.context_dialog.ContextDialog.closeEvent","title":"<code>closeEvent(event)</code>","text":"<p>Ignore close event if is in strict state and context is not done.</p> Source code in <code>client/ayon_core/tools/context_dialog/window.py</code> <pre><code>def closeEvent(self, event):\n    \"\"\"Ignore close event if is in strict state and context is not done.\"\"\"\n    if self.is_strict() and not self._ok_btn.isEnabled():\n        # Allow to close window when initial context is not valid\n        if self._controller.is_initial_context_valid():\n            event.ignore()\n            return\n\n    if self.is_strict():\n        self._confirm_selection()\n    self._visible = False\n    super(ContextDialog, self).closeEvent(event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/context_dialog/index.html#client.ayon_core.tools.context_dialog.ContextDialog.get_context","title":"<code>get_context()</code>","text":"<p>Result of dialog.</p> Source code in <code>client/ayon_core/tools/context_dialog/window.py</code> <pre><code>def get_context(self):\n    \"\"\"Result of dialog.\"\"\"\n    return self._controller.get_selected_context()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/context_dialog/index.html#client.ayon_core.tools.context_dialog.ContextDialog.refresh","title":"<code>refresh()</code>","text":"<p>Refresh all widget one by one.</p> Source code in <code>client/ayon_core/tools/context_dialog/window.py</code> <pre><code>def refresh(self):\n    \"\"\"Refresh all widget one by one.\"\"\"\n\n    self._controller.reset()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/context_dialog/index.html#client.ayon_core.tools.context_dialog.ContextDialog.set_context","title":"<code>set_context(project_name=None, folder_path=None)</code>","text":"<p>Set context which will be used and locked in dialog.</p> Source code in <code>client/ayon_core/tools/context_dialog/window.py</code> <pre><code>def set_context(self, project_name=None, folder_path=None):\n    \"\"\"Set context which will be used and locked in dialog.\"\"\"\n\n    self._controller.set_initial_context(project_name, folder_path)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/context_dialog/index.html#client.ayon_core.tools.context_dialog.ContextDialog.set_strict","title":"<code>set_strict(enabled)</code>","text":"<p>Change strictness of dialog.</p> Source code in <code>client/ayon_core/tools/context_dialog/window.py</code> <pre><code>def set_strict(self, enabled):\n    \"\"\"Change strictness of dialog.\"\"\"\n\n    self._controller.set_strict(enabled)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/context_dialog/index.html#client.ayon_core.tools.context_dialog.ContextDialog.showEvent","title":"<code>showEvent(event)</code>","text":"<p>Override show event to do some callbacks.</p> Source code in <code>client/ayon_core/tools/context_dialog/window.py</code> <pre><code>def showEvent(self, event):\n    \"\"\"Override show event to do some callbacks.\"\"\"\n    super(ContextDialog, self).showEvent(event)\n    self._visible = True\n\n    if self._first_show:\n        self._first_show = False\n        # Set stylesheet and resize\n        self.setStyleSheet(style.load_stylesheet())\n        self.resize(600, 700)\n        center_window(self)\n    self._controller.refresh()\n\n    initial_context = self._controller.get_initial_context()\n    self._set_init_context(initial_context)\n    self._overlay_widget.resize(self.size())\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/context_dialog/window.html","title":"window","text":""},{"location":"autoapi/client/ayon_core/tools/context_dialog/window.html#client.ayon_core.tools.context_dialog.window.ContextDialog","title":"<code>ContextDialog</code>","text":"<p>               Bases: <code>QDialog</code></p> <p>Dialog to select a context.</p> <p>Context has 3 parts: - Project - Folder - Task</p> <p>It is possible to predefine project and folder. In that case their widgets will have passed preselected values and will be disabled.</p> Source code in <code>client/ayon_core/tools/context_dialog/window.py</code> <pre><code>class ContextDialog(QtWidgets.QDialog):\n    \"\"\"Dialog to select a context.\n\n    Context has 3 parts:\n    - Project\n    - Folder\n    - Task\n\n    It is possible to predefine project and folder. In that case their widgets\n    will have passed preselected values and will be disabled.\n    \"\"\"\n    def __init__(self, controller=None, parent=None):\n        super(ContextDialog, self).__init__(parent)\n\n        self.setWindowTitle(\"Select Context\")\n        self.setWindowIcon(QtGui.QIcon(style.app_icon_path()))\n\n        if controller is None:\n            controller = ContextDialogController()\n\n        # Enable minimize and maximize for app\n        window_flags = QtCore.Qt.Window\n        if not parent:\n            window_flags |= QtCore.Qt.WindowStaysOnTopHint\n        self.setWindowFlags(window_flags)\n        self.setFocusPolicy(QtCore.Qt.StrongFocus)\n\n        # UI initialization\n        main_splitter = QtWidgets.QSplitter(self)\n\n        # Left side widget contains project combobox and folders widget\n        left_side_widget = QtWidgets.QWidget(main_splitter)\n\n        project_combobox = ProjectsCombobox(\n            controller,\n            parent=left_side_widget,\n            handle_expected_selection=True\n        )\n        project_combobox.set_select_item_visible(True)\n\n        # Folders widget\n        folders_widget = FoldersWidget(\n            controller,\n            parent=left_side_widget,\n            handle_expected_selection=True\n        )\n\n        left_side_layout = QtWidgets.QVBoxLayout(left_side_widget)\n        left_side_layout.setContentsMargins(0, 0, 0, 0)\n        left_side_layout.addWidget(project_combobox, 0)\n        left_side_layout.addWidget(folders_widget, 1)\n\n        # Right side of window contains only tasks\n        tasks_widget = TasksWidget(controller, parent=main_splitter)\n\n        # Add widgets to main splitter\n        main_splitter.addWidget(left_side_widget)\n        main_splitter.addWidget(tasks_widget)\n\n        # Set stretch of both sides\n        main_splitter.setStretchFactor(0, 7)\n        main_splitter.setStretchFactor(1, 3)\n\n        # Add confimation button to bottom right\n        ok_btn = QtWidgets.QPushButton(\"OK\", self)\n\n        buttons_layout = QtWidgets.QHBoxLayout()\n        buttons_layout.setContentsMargins(0, 0, 0, 0)\n        buttons_layout.addStretch(1)\n        buttons_layout.addWidget(ok_btn, 0)\n\n        main_layout = QtWidgets.QVBoxLayout(self)\n        main_layout.addWidget(main_splitter, 1)\n        main_layout.addLayout(buttons_layout, 0)\n\n        overlay_widget = InvalidContextOverlay(self)\n        overlay_widget.setVisible(False)\n\n        ok_btn.clicked.connect(self._on_ok_click)\n        project_combobox.refreshed.connect(self._on_projects_refresh)\n        overlay_widget.confirmed.connect(self._on_overlay_confirm)\n\n        controller.register_event_callback(\n            \"selection.project.changed\",\n            self._on_project_selection_change\n        )\n        controller.register_event_callback(\n            \"selection.folder.changed\",\n            self._on_folder_selection_change\n        )\n        controller.register_event_callback(\n            \"selection.task.changed\",\n            self._on_task_selection_change\n        )\n        controller.register_event_callback(\n            \"initial.context.changed\",\n            self._on_init_context_change\n        )\n        controller.register_event_callback(\n            \"strict.changed\",\n            self._on_strict_changed\n        )\n        controller.register_event_callback(\n            \"controller.reset.finished\",\n            self._on_controller_reset\n        )\n        controller.register_event_callback(\n            \"controller.refresh.finished\",\n            self._on_controller_refresh\n        )\n\n        # Set stylehseet and resize window on first show\n        self._first_show = True\n        self._visible = False\n\n        self._controller = controller\n\n        self._project_combobox = project_combobox\n        self._folders_widget = folders_widget\n        self._tasks_widget = tasks_widget\n\n        self._ok_btn = ok_btn\n\n        self._overlay_widget = overlay_widget\n\n        self._apply_strict_changes(self.is_strict())\n\n    def is_strict(self):\n        return self._controller.is_strict()\n\n    def showEvent(self, event):\n        \"\"\"Override show event to do some callbacks.\"\"\"\n        super(ContextDialog, self).showEvent(event)\n        self._visible = True\n\n        if self._first_show:\n            self._first_show = False\n            # Set stylesheet and resize\n            self.setStyleSheet(style.load_stylesheet())\n            self.resize(600, 700)\n            center_window(self)\n        self._controller.refresh()\n\n        initial_context = self._controller.get_initial_context()\n        self._set_init_context(initial_context)\n        self._overlay_widget.resize(self.size())\n\n    def resizeEvent(self, event):\n        super(ContextDialog, self).resizeEvent(event)\n        self._overlay_widget.resize(self.size())\n\n    def closeEvent(self, event):\n        \"\"\"Ignore close event if is in strict state and context is not done.\"\"\"\n        if self.is_strict() and not self._ok_btn.isEnabled():\n            # Allow to close window when initial context is not valid\n            if self._controller.is_initial_context_valid():\n                event.ignore()\n                return\n\n        if self.is_strict():\n            self._confirm_selection()\n        self._visible = False\n        super(ContextDialog, self).closeEvent(event)\n\n    def set_strict(self, enabled):\n        \"\"\"Change strictness of dialog.\"\"\"\n\n        self._controller.set_strict(enabled)\n\n    def refresh(self):\n        \"\"\"Refresh all widget one by one.\"\"\"\n\n        self._controller.reset()\n\n    def get_context(self):\n        \"\"\"Result of dialog.\"\"\"\n        return self._controller.get_selected_context()\n\n    def set_context(self, project_name=None, folder_path=None):\n        \"\"\"Set context which will be used and locked in dialog.\"\"\"\n\n        self._controller.set_initial_context(project_name, folder_path)\n\n    def _on_projects_refresh(self):\n        initial_context = self._controller.get_initial_context()\n        self._controller.set_expected_selection(\n            initial_context[\"project_name\"],\n            initial_context[\"folder_id\"]\n        )\n\n    def _on_overlay_confirm(self):\n        self.close()\n\n    def _on_ok_click(self):\n        # Store values to output\n        self._confirm_selection()\n        # Close dialog\n        self.accept()\n\n    def _confirm_selection(self):\n        self._controller.confirm_selection()\n\n    def _on_project_selection_change(self, event):\n        self._on_selection_change(\n            event[\"project_name\"],\n        )\n\n    def _on_folder_selection_change(self, event):\n        self._on_selection_change(\n            event[\"project_name\"],\n            event[\"folder_id\"],\n        )\n\n    def _on_task_selection_change(self, event):\n        self._on_selection_change(\n            event[\"project_name\"],\n            event[\"folder_id\"],\n            event[\"task_name\"],\n        )\n\n    def _on_selection_change(\n        self, project_name, folder_id=None, task_name=None\n    ):\n        self._validate_strict(project_name, folder_id, task_name)\n\n    def _on_init_context_change(self, event):\n        self._set_init_context(event.data)\n        if self._visible:\n            self._controller.set_expected_selection(\n                event[\"project_name\"], event[\"folder_id\"]\n            )\n\n    def _set_init_context(self, init_context):\n        project_name = init_context[\"project_name\"]\n        if not init_context[\"valid\"]:\n            self._overlay_widget.setVisible(True)\n            self._overlay_widget.set_context(\n                project_name,\n                init_context[\"folder_label\"],\n                init_context[\"project_found\"],\n                init_context[\"folder_found\"],\n                init_context[\"tasks_found\"]\n            )\n            return\n\n        self._overlay_widget.setVisible(False)\n        if project_name:\n            self._project_combobox.setEnabled(False)\n            if init_context[\"folder_id\"]:\n                self._folders_widget.setEnabled(False)\n        else:\n            self._project_combobox.setEnabled(True)\n            self._folders_widget.setEnabled(True)\n\n    def _on_strict_changed(self, event):\n        self._apply_strict_changes(event[\"strict\"])\n\n    def _on_controller_reset(self):\n        self._apply_strict_changes(self.is_strict())\n        self._project_combobox.refresh()\n\n    def _on_controller_refresh(self):\n        self._project_combobox.refresh()\n\n    def _apply_strict_changes(self, is_strict):\n        if not is_strict:\n            if not self._ok_btn.isEnabled():\n                self._ok_btn.setEnabled(True)\n            return\n        context = self._controller.get_selected_context()\n        self._validate_strict(\n            context[\"project_name\"],\n            context[\"folder_id\"],\n            context[\"task_name\"]\n        )\n\n    def _validate_strict(self, project_name, folder_id, task_name):\n        if not self.is_strict():\n            return\n\n        enabled = True\n        if not project_name or not folder_id or not task_name:\n            enabled = False\n        self._ok_btn.setEnabled(enabled)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/context_dialog/window.html#client.ayon_core.tools.context_dialog.window.ContextDialog.closeEvent","title":"<code>closeEvent(event)</code>","text":"<p>Ignore close event if is in strict state and context is not done.</p> Source code in <code>client/ayon_core/tools/context_dialog/window.py</code> <pre><code>def closeEvent(self, event):\n    \"\"\"Ignore close event if is in strict state and context is not done.\"\"\"\n    if self.is_strict() and not self._ok_btn.isEnabled():\n        # Allow to close window when initial context is not valid\n        if self._controller.is_initial_context_valid():\n            event.ignore()\n            return\n\n    if self.is_strict():\n        self._confirm_selection()\n    self._visible = False\n    super(ContextDialog, self).closeEvent(event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/context_dialog/window.html#client.ayon_core.tools.context_dialog.window.ContextDialog.get_context","title":"<code>get_context()</code>","text":"<p>Result of dialog.</p> Source code in <code>client/ayon_core/tools/context_dialog/window.py</code> <pre><code>def get_context(self):\n    \"\"\"Result of dialog.\"\"\"\n    return self._controller.get_selected_context()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/context_dialog/window.html#client.ayon_core.tools.context_dialog.window.ContextDialog.refresh","title":"<code>refresh()</code>","text":"<p>Refresh all widget one by one.</p> Source code in <code>client/ayon_core/tools/context_dialog/window.py</code> <pre><code>def refresh(self):\n    \"\"\"Refresh all widget one by one.\"\"\"\n\n    self._controller.reset()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/context_dialog/window.html#client.ayon_core.tools.context_dialog.window.ContextDialog.set_context","title":"<code>set_context(project_name=None, folder_path=None)</code>","text":"<p>Set context which will be used and locked in dialog.</p> Source code in <code>client/ayon_core/tools/context_dialog/window.py</code> <pre><code>def set_context(self, project_name=None, folder_path=None):\n    \"\"\"Set context which will be used and locked in dialog.\"\"\"\n\n    self._controller.set_initial_context(project_name, folder_path)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/context_dialog/window.html#client.ayon_core.tools.context_dialog.window.ContextDialog.set_strict","title":"<code>set_strict(enabled)</code>","text":"<p>Change strictness of dialog.</p> Source code in <code>client/ayon_core/tools/context_dialog/window.py</code> <pre><code>def set_strict(self, enabled):\n    \"\"\"Change strictness of dialog.\"\"\"\n\n    self._controller.set_strict(enabled)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/context_dialog/window.html#client.ayon_core.tools.context_dialog.window.ContextDialog.showEvent","title":"<code>showEvent(event)</code>","text":"<p>Override show event to do some callbacks.</p> Source code in <code>client/ayon_core/tools/context_dialog/window.py</code> <pre><code>def showEvent(self, event):\n    \"\"\"Override show event to do some callbacks.\"\"\"\n    super(ContextDialog, self).showEvent(event)\n    self._visible = True\n\n    if self._first_show:\n        self._first_show = False\n        # Set stylesheet and resize\n        self.setStyleSheet(style.load_stylesheet())\n        self.resize(600, 700)\n        center_window(self)\n    self._controller.refresh()\n\n    initial_context = self._controller.get_initial_context()\n    self._set_init_context(initial_context)\n    self._overlay_widget.resize(self.size())\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/context_dialog/window.html#client.ayon_core.tools.context_dialog.window.ContextDialogController","title":"<code>ContextDialogController</code>","text":"Source code in <code>client/ayon_core/tools/context_dialog/window.py</code> <pre><code>class ContextDialogController:\n    def __init__(self):\n        self._event_system = None\n\n        self._settings_model = SettingsModel()\n        self._projects_model = ProjectsModel(self)\n        self._hierarchy_model = HierarchyModel(self)\n        self._selection_model = SelectionModel(self)\n        self._expected_selection = ExpectedSelection(self)\n\n        self._confirmed = False\n        self._is_strict = False\n        self._output_path = None\n\n        self._initial_project_name = None\n        self._initial_folder_id = None\n        self._initial_folder_label = None\n        self._initial_project_found = True\n        self._initial_folder_found = True\n        self._initial_tasks_found = True\n\n    def reset(self):\n        self._emit_event(\"controller.reset.started\")\n\n        self._confirmed = False\n        self._output_path = None\n\n        self._initial_project_name = None\n        self._initial_folder_id = None\n        self._initial_folder_label = None\n        self._initial_project_found = True\n        self._initial_folder_found = True\n        self._initial_tasks_found = True\n\n        self._settings_model.reset()\n        self._projects_model.reset()\n        self._hierarchy_model.reset()\n\n        self._emit_event(\"controller.reset.finished\")\n\n    def refresh(self):\n        self._emit_event(\"controller.refresh.started\")\n\n        self._settings_model.reset()\n        self._projects_model.reset()\n        self._hierarchy_model.reset()\n\n        self._emit_event(\"controller.refresh.finished\")\n\n    # Event handling\n    def emit_event(self, topic, data=None, source=None):\n        \"\"\"Use implemented event system to trigger event.\"\"\"\n\n        if data is None:\n            data = {}\n        self._get_event_system().emit(topic, data, source)\n\n    def register_event_callback(self, topic, callback):\n        self._get_event_system().add_callback(topic, callback)\n\n    def set_output_json_path(self, output_path):\n        self._output_path = output_path\n\n    def is_strict(self):\n        return self._is_strict\n\n    def set_strict(self, enabled):\n        if self._is_strict is enabled:\n            return\n        self._is_strict = enabled\n        self._emit_event(\"strict.changed\", {\"strict\": enabled})\n\n    # Data model functions\n    def get_project_settings(self, project_name: str | None) -&gt; dict:\n        return self._settings_model.get_settings(project_name)\n\n    def get_project_items(self, sender=None):\n        return self._projects_model.get_project_items(sender)\n\n    def get_folder_type_items(self, project_name, sender=None):\n        return self._projects_model.get_folder_type_items(\n            project_name, sender\n        )\n\n    def get_task_type_items(self, project_name, sender=None):\n        return self._projects_model.get_task_type_items(\n            project_name, sender\n        )\n\n    def get_folder_items(self, project_name, sender=None):\n        return self._hierarchy_model.get_folder_items(project_name, sender)\n\n    def get_task_items(self, project_name, folder_id, sender=None):\n        return self._hierarchy_model.get_task_items(\n            project_name, folder_id, sender\n        )\n\n    # Expected selection helpers\n    def set_expected_selection(self, project_name, folder_id):\n        return self._expected_selection.set_expected_selection(\n            project_name, folder_id\n        )\n\n    def get_expected_selection_data(self):\n        return self._expected_selection.get_expected_selection_data()\n\n    def expected_project_selected(self, project_name):\n        self._expected_selection.expected_project_selected(project_name)\n\n    def expected_folder_selected(self, folder_id):\n        self._expected_selection.expected_folder_selected(folder_id)\n\n    # Selection handling\n    def get_selected_project_name(self):\n        return self._selection_model.get_selected_project_name()\n\n    def set_selected_project(self, project_name):\n        self._selection_model.set_selected_project(project_name)\n\n    def get_selected_folder_id(self):\n        return self._selection_model.get_selected_folder_id()\n\n    def set_selected_folder(self, folder_id):\n        self._selection_model.set_selected_folder(folder_id)\n\n    def get_selected_task_name(self):\n        return self._selection_model.get_selected_task_name()\n\n    def get_selected_task_id(self):\n        return self._selection_model.get_selected_task_id()\n\n    def set_selected_task(self, task_id, task_name):\n        self._selection_model.set_selected_task(task_id, task_name)\n\n    def is_initial_context_valid(self):\n        return self._initial_folder_found and self._initial_project_found\n\n    def set_initial_context(self, project_name=None, folder_path=None):\n        result = self._prepare_initial_context(project_name, folder_path)\n\n        self._initial_project_name = project_name\n        self._initial_folder_id = result[\"folder_id\"]\n        self._initial_folder_label = result[\"folder_label\"]\n        self._initial_project_found = result[\"project_found\"]\n        self._initial_folder_found = result[\"folder_found\"]\n        self._initial_tasks_found = result[\"tasks_found\"]\n        self._emit_event(\n            \"initial.context.changed\",\n            self.get_initial_context()\n        )\n\n    def get_initial_context(self):\n        return {\n            \"project_name\": self._initial_project_name,\n            \"folder_id\": self._initial_folder_id,\n            \"folder_label\": self._initial_folder_label,\n            \"project_found\": self._initial_project_found,\n            \"folder_found\": self._initial_folder_found,\n            \"tasks_found\": self._initial_tasks_found,\n            \"valid\": (\n                self._initial_project_found\n                and self._initial_folder_found\n                and self._initial_tasks_found\n            )\n        }\n\n    # Result of this tool\n    def get_selected_context(self):\n        project_name = None\n        folder_id = None\n        task_id = None\n        task_name = None\n        folder_path = None\n        folder_name = None\n        if self._confirmed:\n            project_name = self.get_selected_project_name()\n            folder_id = self.get_selected_folder_id()\n            task_id = self.get_selected_task_id()\n            task_name = self.get_selected_task_name()\n\n        folder_item = None\n        if folder_id:\n            folder_item = self._hierarchy_model.get_folder_item(\n                project_name, folder_id)\n\n        if folder_item:\n            folder_path = folder_item.path\n            folder_name = folder_item.name\n        return {\n            \"project\": project_name,\n            \"project_name\": project_name,\n            \"asset\": folder_name,\n            \"folder_id\": folder_id,\n            \"folder_path\": folder_path,\n            \"task\": task_name,\n            \"task_name\": task_name,\n            \"task_id\": task_id,\n            \"initial_context_valid\": self.is_initial_context_valid(),\n        }\n\n    def confirm_selection(self):\n        self._confirmed = True\n\n    def store_output(self):\n        if not self._output_path:\n            return\n\n        dirpath = os.path.dirname(self._output_path)\n        os.makedirs(dirpath, exist_ok=True)\n        with open(self._output_path, \"w\") as stream:\n            json.dump(self.get_selected_context(), stream, indent=4)\n\n    def _prepare_initial_context(self, project_name, folder_path):\n        project_found = True\n        output = {\n            \"project_found\": project_found,\n            \"folder_id\": None,\n            \"folder_label\": None,\n            \"folder_found\": True,\n            \"tasks_found\": True,\n        }\n        if project_name is None:\n            folder_path = None\n        else:\n            project = ayon_api.get_project(project_name)\n            project_found = project is not None\n        output[\"project_found\"] = project_found\n        if not project_found or not folder_path:\n            return output\n\n        output[\"folder_label\"] = folder_path\n\n        folder_id = None\n        folder_found = False\n        # First try to find by path\n        folder = ayon_api.get_folder_by_path(project_name, folder_path)\n        # Try to find by name if folder was not found by path\n        #   - prevent to query by name if 'folder_path' contains '/'\n        if not folder and \"/\" not in folder_path:\n            folder = next(\n                ayon_api.get_folders(\n                    project_name, folder_names=[folder_path], fields=[\"id\"]),\n                None\n            )\n\n        if folder:\n            folder_id = folder[\"id\"]\n            folder_found = True\n\n        output[\"folder_id\"] = folder_id\n        output[\"folder_found\"] = folder_found\n        if not folder_found:\n            return output\n\n        tasks = list(ayon_api.get_tasks(\n            project_name, folder_ids=[folder_id], fields=[\"id\"]\n        ))\n        output[\"tasks_found\"] = bool(tasks)\n        return output\n\n    def _get_event_system(self):\n        \"\"\"Inner event system for workfiles tool controller.\n\n        Is used for communication with UI. Event system is created on demand.\n\n        Returns:\n            QueuedEventSystem: Event system which can trigger callbacks\n                for topics.\n        \"\"\"\n\n        if self._event_system is None:\n            self._event_system = QueuedEventSystem()\n        return self._event_system\n\n    def _emit_event(self, topic, data=None):\n        self.emit_event(topic, data, \"controller\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/context_dialog/window.html#client.ayon_core.tools.context_dialog.window.ContextDialogController.emit_event","title":"<code>emit_event(topic, data=None, source=None)</code>","text":"<p>Use implemented event system to trigger event.</p> Source code in <code>client/ayon_core/tools/context_dialog/window.py</code> <pre><code>def emit_event(self, topic, data=None, source=None):\n    \"\"\"Use implemented event system to trigger event.\"\"\"\n\n    if data is None:\n        data = {}\n    self._get_event_system().emit(topic, data, source)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/context_dialog/window.html#client.ayon_core.tools.context_dialog.window.SelectionModel","title":"<code>SelectionModel</code>","text":"<p>               Bases: <code>object</code></p> <p>Model handling selection changes.</p> <p>Triggering events: - \"selection.project.changed\" - \"selection.folder.changed\" - \"selection.task.changed\"</p> Source code in <code>client/ayon_core/tools/context_dialog/window.py</code> <pre><code>class SelectionModel(object):\n    \"\"\"Model handling selection changes.\n\n    Triggering events:\n    - \"selection.project.changed\"\n    - \"selection.folder.changed\"\n    - \"selection.task.changed\"\n    \"\"\"\n\n    event_source = \"selection.model\"\n\n    def __init__(self, controller):\n        self._controller = controller\n\n        self._project_name = None\n        self._folder_id = None\n        self._task_id = None\n        self._task_name = None\n\n    def get_selected_project_name(self):\n        return self._project_name\n\n    def set_selected_project(self, project_name):\n        self._project_name = project_name\n        self._controller.emit_event(\n            \"selection.project.changed\",\n            {\"project_name\": project_name},\n            self.event_source\n        )\n\n    def get_selected_folder_id(self):\n        return self._folder_id\n\n    def set_selected_folder(self, folder_id):\n        if folder_id == self._folder_id:\n            return\n        self._folder_id = folder_id\n        self._controller.emit_event(\n            \"selection.folder.changed\",\n            {\n                \"project_name\": self._project_name,\n                \"folder_id\": folder_id,\n            },\n            self.event_source\n        )\n\n    def get_selected_task_name(self):\n        return self._task_name\n\n    def get_selected_task_id(self):\n        return self._task_id\n\n    def set_selected_task(self, task_id, task_name):\n        if task_id == self._task_id:\n            return\n\n        self._task_name = task_name\n        self._task_id = task_id\n        self._controller.emit_event(\n            \"selection.task.changed\",\n            {\n                \"project_name\": self._project_name,\n                \"folder_id\": self._folder_id,\n                \"task_name\": task_name,\n                \"task_id\": task_id,\n            },\n            self.event_source\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/delivery/index.html","title":"delivery","text":""},{"location":"autoapi/client/ayon_core/tools/delivery/index.html#client.ayon_core.tools.delivery.DeliveryOptionsDialog","title":"<code>DeliveryOptionsDialog</code>","text":"<p>               Bases: <code>QDialog</code></p> <p>Dialog to select template where to deliver selected representations.</p> Source code in <code>client/ayon_core/tools/delivery/delivery.py</code> <pre><code>class DeliveryOptionsDialog(QtWidgets.QDialog):\n    \"\"\"Dialog to select template where to deliver selected representations.\"\"\"\n\n    def __init__(\n        self,\n        project_name,\n        version_ids,\n        list_entity_label=None,\n        *,\n        log=None,\n        parent=None,\n    ):\n        super().__init__(parent=parent)\n\n        if log is None:\n            log = logging.getLogger(__name__)\n\n        self.setWindowTitle(f\"Deliver {len(version_ids)} versions\")\n        icon = QtGui.QIcon(resources.get_ayon_icon_filepath())\n        self.setWindowIcon(icon)\n\n        self.setWindowFlags(\n            QtCore.Qt.WindowStaysOnTopHint\n            | QtCore.Qt.WindowCloseButtonHint\n            | QtCore.Qt.WindowMinimizeButtonHint\n        )\n\n        self.setStyleSheet(style.load_stylesheet())\n\n        self.anatomy = Anatomy(project_name)\n        self._representations = None\n        self.log = log\n        self.currently_uploaded = 0\n\n        self._set_representations(project_name, version_ids)\n\n        dropdown = QtWidgets.QComboBox()\n        self.templates = self._get_templates(self.anatomy)\n        for name, _ in self.templates.items():\n            dropdown.addItem(name)\n        if self.templates and platform.system() == \"Darwin\":\n            # fix macos QCombobox Style\n            dropdown.setItemDelegate(QtWidgets.QStyledItemDelegate())\n            # update combo box length to longest entry\n            longest_key = max(self.templates.keys(), key=len)\n            dropdown.setMinimumContentsLength(len(longest_key))\n\n        template_dir_label = QtWidgets.QLabel()\n        template_dir_label.setCursor(QtGui.QCursor(QtCore.Qt.IBeamCursor))\n        template_dir_label.setTextInteractionFlags(\n            QtCore.Qt.TextSelectableByMouse)\n\n        template_file_label = QtWidgets.QLabel()\n        template_file_label.setCursor(QtGui.QCursor(QtCore.Qt.IBeamCursor))\n        template_file_label.setTextInteractionFlags(\n            QtCore.Qt.TextSelectableByMouse)\n\n        renumber_frame = QtWidgets.QCheckBox()\n\n        first_frame_start = QtWidgets.QSpinBox()\n        max_int = (1 &lt;&lt; 32) // 2\n        first_frame_start.setRange(0, max_int - 1)\n\n        root_line_edit = QtWidgets.QLineEdit()\n        list_label_line_edit = QtWidgets.QLineEdit()\n\n        # Collapsible \"Advanced options\" section, hidden by default\n        advanced_toggle = QtWidgets.QToolButton()\n        advanced_toggle.setText(\"Advanced options\")\n        advanced_toggle.setCheckable(True)\n        advanced_toggle.setChecked(False)\n        advanced_toggle.setToolButtonStyle(\n            QtCore.Qt.ToolButtonTextBesideIcon)\n        advanced_toggle.setArrowType(QtCore.Qt.RightArrow)\n\n        advanced_widget = QtWidgets.QWidget(self)\n        advanced_layout = QtWidgets.QFormLayout(advanced_widget)\n        advanced_layout.setContentsMargins(10, 5, 5, 5)\n        advanced_layout.addRow(\"Renumber Frame\", renumber_frame)\n        advanced_layout.addRow(\"Renumber start frame\", first_frame_start)\n        advanced_layout.addRow(\"Root\", root_line_edit)\n        advanced_layout.addRow(\"List label\", list_label_line_edit)\n        # set default value if `list_entity_label` arg is provided\n        if list_entity_label:\n            list_label_line_edit.setText(list_entity_label)\n        advanced_widget.setVisible(False)\n\n        def _on_advanced_toggled(checked):\n            advanced_widget.setVisible(checked)\n            advanced_toggle.setArrowType(\n                QtCore.Qt.DownArrow if checked else QtCore.Qt.RightArrow\n            )\n\n        advanced_toggle.toggled.connect(_on_advanced_toggled)\n\n        btn_select_all = QtWidgets.QPushButton(\"Select All\")\n\n        repre_list = QtWidgets.QListWidget()\n        repre_list.setSelectionMode(\n            QtWidgets.QAbstractItemView.ExtendedSelection)\n        for repre in sorted(self._get_representation_names()):\n            repre_list.addItem(QtWidgets.QListWidgetItem(repre))\n\n        repre_list.itemSelectionChanged.connect(self._update_selected_label)\n        btn_select_all.clicked.connect(repre_list.selectAll)\n\n        repre_widget = QtWidgets.QWidget(self)\n        repre_layout = QtWidgets.QVBoxLayout(repre_widget)\n        repre_layout.setContentsMargins(10, 5, 5, 10)\n        repre_layout.addWidget(btn_select_all)\n        repre_layout.addWidget(repre_list)\n\n        selected_label = QtWidgets.QLabel()\n\n        input_widget = QtWidgets.QWidget(self)\n        input_layout = QtWidgets.QFormLayout(input_widget)\n        input_layout.setContentsMargins(10, 15, 5, 5)\n\n        input_layout.addRow(\"Selected representations\", selected_label)\n        input_layout.addRow(\"Delivery template\", dropdown)\n        input_layout.addRow(\"Directory template\", template_dir_label)\n        input_layout.addRow(\"File template\", template_file_label)\n        input_layout.addRow(\"Representations\", repre_widget)\n        input_layout.addRow(advanced_toggle)\n        input_layout.addRow(advanced_widget)\n\n        btn_delivery = QtWidgets.QPushButton(\"Deliver\")\n        btn_delivery.setEnabled(False)\n\n        progress_bar = QtWidgets.QProgressBar(self)\n        progress_bar.setMinimum = 0\n        progress_bar.setMaximum = 100\n        progress_bar.setVisible(False)\n\n        text_area = QtWidgets.QTextEdit()\n        text_area.setReadOnly(True)\n        text_area.setVisible(False)\n        text_area.setMinimumHeight(100)\n\n        layout = QtWidgets.QVBoxLayout(self)\n\n        layout.addWidget(input_widget)\n        layout.addStretch(1)\n        layout.addWidget(btn_delivery)\n        layout.addWidget(progress_bar)\n        layout.addWidget(text_area)\n\n        self.selected_label = selected_label\n        self.template_dir_label = template_dir_label\n        self.template_file_label = template_file_label\n        self.dropdown = dropdown\n        self.first_frame_start = first_frame_start\n        self.renumber_frame = renumber_frame\n        self.root_line_edit = root_line_edit\n        self.list_label_line_edit = list_label_line_edit\n        self.repre_list = repre_list\n        self.progress_bar = progress_bar\n        self.text_area = text_area\n        self.btn_delivery = btn_delivery\n\n        self.selected_repres_count, self.files_selected, self.size_selected = \\\n            self._get_counts(self._get_selected_repres())\n\n        self._update_selected_label()\n        self._update_template_value()\n\n        btn_delivery.clicked.connect(self.deliver)\n        dropdown.currentIndexChanged.connect(self._update_template_value)\n\n        if not self.dropdown.count():\n            self.text_area.setVisible(True)\n            error_message = (\n                \"No Delivery Templates found!\\n\"\n                \"Add Template in [project_anatomy/templates/delivery]\"\n            )\n            self.text_area.setText(error_message)\n            self.log.error(error_message.replace(\"\\n\", \" \"))\n\n    def deliver(self):\n        \"\"\"Main method to loop through all selected representations\"\"\"\n        self.progress_bar.setVisible(True)\n        self.btn_delivery.setEnabled(False)\n        QtWidgets.QApplication.processEvents()\n\n        report_items = defaultdict(list)\n\n        selected_repres = self._get_selected_repres()\n\n        datetime_data = get_datetime_data()\n        template_name = self.dropdown.currentText()\n        format_dict = get_format_dict(self.anatomy, self.root_line_edit.text())\n        list_label = self.list_label_line_edit.text()\n        renumber_frame = self.renumber_frame.isChecked()\n        frame_offset = self.first_frame_start.value()\n        filtered_repres = []\n        repre_ids = set()\n        for repre in self._representations:\n            if repre[\"name\"] in selected_repres:\n                filtered_repres.append(repre)\n                repre_ids.add(repre[\"id\"])\n\n        template_data_by_repre_id = (\n            get_representations_delivery_template_data(\n                self.anatomy.project_name, repre_ids\n            )\n        )\n        for repre in filtered_repres:\n            repre_path = get_representation_path_with_anatomy(\n                repre, self.anatomy\n            )\n\n            template_data = template_data_by_repre_id[repre[\"id\"]]\n            if list_label:\n                template_data[\"list\"] = {\"label\": list_label}\n\n            # Use temporary placeholder so we don't need to check destination\n            # path per file of the representation\n            template_data[\"publishedFilename\"] = \"&lt;publishedFilename&gt;\"\n            new_report_items = check_destination_path(\n                repre[\"id\"],\n                self.anatomy,\n                template_data,\n                datetime_data,\n                template_name\n            )\n\n            report_items.update(new_report_items)\n            if new_report_items:\n                continue\n\n            args = [\n                repre_path,\n                repre,\n                self.anatomy,\n                template_name,\n                template_data,\n                format_dict,\n                report_items,\n                self.log\n            ]\n\n            # TODO: This will currently incorrectly detect 'resources'\n            #  that are published along with the publish, because those should\n            #  not adhere to the template directly but are ingested in a\n            #  customized way. For example, maya look textures or any publish\n            #  that directly adds files into `instance.data[\"transfers\"]`\n            src_paths = []\n            for repre_file in repre[\"files\"]:\n                src_path = self.anatomy.fill_root(repre_file[\"path\"])\n                src_paths.append(src_path)\n            sources_and_frames = collect_frames(src_paths)\n\n            frames = set(sources_and_frames.values())\n            frames.discard(None)\n            first_frame = None\n            if frames:\n                first_frame = min(frames)\n\n            for src_path, frame in sources_and_frames.items():\n                # Support {publishedFilename} token\n                template_data[\"publishedFilename\"] = os.path.basename(\n                    # Replace backslash to forward slashes so basename\n                    # also resolves to filename only on POSIX\n                    src_path.replace(\"\\\\\", \"/\")\n                )\n                args[0] = src_path\n                # Renumber frames\n                if renumber_frame and frame is not None:\n                    # Calculate offset between\n                    # first frame and current frame\n                    # - '0' for first frame\n                    offset = frame_offset - int(first_frame)\n                    # Add offset to new frame start\n                    dst_frame = int(frame) + offset\n                    if dst_frame &lt; 0:\n                        msg = \"Renumber frame has a smaller number than original frame\"     # noqa\n                        report_items[msg].append(src_path)\n                        self.log.warning(\"{} &lt;{}&gt;\".format(\n                            msg, dst_frame))\n                        continue\n                    frame = dst_frame\n\n                if frame is not None:\n                    if repre[\"context\"].get(\"frame\"):\n                        template_data[\"frame\"] = frame\n                    elif repre[\"context\"].get(\"udim\"):\n                        template_data[\"udim\"] = frame\n                    else:\n                        # Fallback\n                        self.log.warning(\n                            \"Representation context has no frame or udim\"\n                            \" data. Supplying sequence frame to '{frame}'\"\n                            \" formatting data.\"\n                        )\n                        template_data[\"frame\"] = frame\n                new_report_items, uploaded = deliver_single_file(*args)\n                report_items.update(new_report_items)\n                self._update_progress(uploaded)\n\n        self.text_area.setText(self._format_report(report_items))\n        self.text_area.setVisible(True)\n\n    def _get_representation_names(self):\n        \"\"\"Get set of representation names for checkbox filtering.\"\"\"\n        return set([repre[\"name\"] for repre in self._representations])\n\n    def _get_templates(self, anatomy):\n        \"\"\"Adds list of delivery templates from Anatomy to dropdown.\"\"\"\n        templates = {}\n        for template_name, value in anatomy.templates[\"delivery\"].items():\n            directory_template = value[\"directory\"]\n            if not directory_template.startswith(\"{root\"):\n                self.log.warning(\n                    \"Skipping template '%s' because directory template does \"\n                    \"not start with `{root` in value: %s\",\n                    template_name, directory_template\n                )\n                continue\n\n            templates[template_name] = value\n\n        return templates\n\n    def _set_representations(self, project_name, version_ids):\n        repres = list(ayon_api.get_representations(\n            project_name, version_ids=version_ids\n        ))\n\n        self._representations = repres\n\n    def _get_counts(self, selected_repres=None):\n        \"\"\"Returns tuple of number of selected files and their size.\"\"\"\n        files_selected = 0\n        size_selected = 0\n        selected_repres_count = 0\n        for repre in self._representations:\n            if repre[\"name\"] in selected_repres:\n                selected_repres_count += 1\n                files = repre.get(\"files\", [])\n                if not files:  # for repre without files, cannot divide by 0\n                    files_selected += 1\n                    size_selected += 0\n                else:\n                    for repre_file in files:\n                        files_selected += 1\n                        size_selected += repre_file[\"size\"]\n\n        return selected_repres_count, files_selected, size_selected\n\n    def _prepare_label(self):\n        \"\"\"Provides text with no of selected files and their size.\"\"\"\n        label = \"{} ({} files, size {})\".format(\n            self.selected_repres_count,\n            self.files_selected,\n            format_file_size(self.size_selected))\n        return label\n\n    def _get_selected_repres(self):\n        \"\"\"Returns list of representation names selected in the list.\"\"\"\n        return [item.text() for item in self.repre_list.selectedItems()]\n\n    def _update_selected_label(self):\n        \"\"\"Updates label with list of number of selected files.\"\"\"\n        selected_repres = self._get_selected_repres()\n        self.selected_repres_count, self.files_selected, self.size_selected = \\\n            self._get_counts(selected_repres)\n        self.selected_label.setText(self._prepare_label())\n        # update delivery button state if any templates found\n        if self.dropdown.count():\n            self.btn_delivery.setEnabled(bool(selected_repres))\n\n    def _update_template_value(self, _index=None):\n        \"\"\"Sets template value to label after selection in dropdown.\"\"\"\n        name = self.dropdown.currentText()\n        template_value = self.templates.get(name)\n        if template_value:\n            self.template_dir_label.setText(template_value[\"directory\"])\n            self.template_file_label.setText(template_value[\"file\"])\n            self.btn_delivery.setEnabled(bool(self._get_selected_repres()))\n\n    def _update_progress(self, uploaded):\n        \"\"\"Update progress bar after each repre copied.\"\"\"\n        self.currently_uploaded += uploaded\n\n        ratio = self.currently_uploaded / self.files_selected\n        self.progress_bar.setValue(ratio * self.progress_bar.maximum())\n\n    def _format_report(self, report_items):\n        \"\"\"Format final result and error details as html.\"\"\"\n        msg = \"Delivery finished\"\n        if not report_items:\n            msg += \" successfully\"\n        else:\n            msg += \" with errors\"\n        txt = \"&lt;h2&gt;{}&lt;/h2&gt;\".format(msg)\n        for header, data in report_items.items():\n            txt += \"&lt;h3&gt;{}&lt;/h3&gt;\".format(header)\n            for item in data:\n                txt += \"{}&lt;br&gt;\".format(item)\n\n        return txt\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/delivery/index.html#client.ayon_core.tools.delivery.DeliveryOptionsDialog.deliver","title":"<code>deliver()</code>","text":"<p>Main method to loop through all selected representations</p> Source code in <code>client/ayon_core/tools/delivery/delivery.py</code> <pre><code>def deliver(self):\n    \"\"\"Main method to loop through all selected representations\"\"\"\n    self.progress_bar.setVisible(True)\n    self.btn_delivery.setEnabled(False)\n    QtWidgets.QApplication.processEvents()\n\n    report_items = defaultdict(list)\n\n    selected_repres = self._get_selected_repres()\n\n    datetime_data = get_datetime_data()\n    template_name = self.dropdown.currentText()\n    format_dict = get_format_dict(self.anatomy, self.root_line_edit.text())\n    list_label = self.list_label_line_edit.text()\n    renumber_frame = self.renumber_frame.isChecked()\n    frame_offset = self.first_frame_start.value()\n    filtered_repres = []\n    repre_ids = set()\n    for repre in self._representations:\n        if repre[\"name\"] in selected_repres:\n            filtered_repres.append(repre)\n            repre_ids.add(repre[\"id\"])\n\n    template_data_by_repre_id = (\n        get_representations_delivery_template_data(\n            self.anatomy.project_name, repre_ids\n        )\n    )\n    for repre in filtered_repres:\n        repre_path = get_representation_path_with_anatomy(\n            repre, self.anatomy\n        )\n\n        template_data = template_data_by_repre_id[repre[\"id\"]]\n        if list_label:\n            template_data[\"list\"] = {\"label\": list_label}\n\n        # Use temporary placeholder so we don't need to check destination\n        # path per file of the representation\n        template_data[\"publishedFilename\"] = \"&lt;publishedFilename&gt;\"\n        new_report_items = check_destination_path(\n            repre[\"id\"],\n            self.anatomy,\n            template_data,\n            datetime_data,\n            template_name\n        )\n\n        report_items.update(new_report_items)\n        if new_report_items:\n            continue\n\n        args = [\n            repre_path,\n            repre,\n            self.anatomy,\n            template_name,\n            template_data,\n            format_dict,\n            report_items,\n            self.log\n        ]\n\n        # TODO: This will currently incorrectly detect 'resources'\n        #  that are published along with the publish, because those should\n        #  not adhere to the template directly but are ingested in a\n        #  customized way. For example, maya look textures or any publish\n        #  that directly adds files into `instance.data[\"transfers\"]`\n        src_paths = []\n        for repre_file in repre[\"files\"]:\n            src_path = self.anatomy.fill_root(repre_file[\"path\"])\n            src_paths.append(src_path)\n        sources_and_frames = collect_frames(src_paths)\n\n        frames = set(sources_and_frames.values())\n        frames.discard(None)\n        first_frame = None\n        if frames:\n            first_frame = min(frames)\n\n        for src_path, frame in sources_and_frames.items():\n            # Support {publishedFilename} token\n            template_data[\"publishedFilename\"] = os.path.basename(\n                # Replace backslash to forward slashes so basename\n                # also resolves to filename only on POSIX\n                src_path.replace(\"\\\\\", \"/\")\n            )\n            args[0] = src_path\n            # Renumber frames\n            if renumber_frame and frame is not None:\n                # Calculate offset between\n                # first frame and current frame\n                # - '0' for first frame\n                offset = frame_offset - int(first_frame)\n                # Add offset to new frame start\n                dst_frame = int(frame) + offset\n                if dst_frame &lt; 0:\n                    msg = \"Renumber frame has a smaller number than original frame\"     # noqa\n                    report_items[msg].append(src_path)\n                    self.log.warning(\"{} &lt;{}&gt;\".format(\n                        msg, dst_frame))\n                    continue\n                frame = dst_frame\n\n            if frame is not None:\n                if repre[\"context\"].get(\"frame\"):\n                    template_data[\"frame\"] = frame\n                elif repre[\"context\"].get(\"udim\"):\n                    template_data[\"udim\"] = frame\n                else:\n                    # Fallback\n                    self.log.warning(\n                        \"Representation context has no frame or udim\"\n                        \" data. Supplying sequence frame to '{frame}'\"\n                        \" formatting data.\"\n                    )\n                    template_data[\"frame\"] = frame\n            new_report_items, uploaded = deliver_single_file(*args)\n            report_items.update(new_report_items)\n            self._update_progress(uploaded)\n\n    self.text_area.setText(self._format_report(report_items))\n    self.text_area.setVisible(True)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/delivery/delivery.html","title":"delivery","text":""},{"location":"autoapi/client/ayon_core/tools/delivery/delivery.html#client.ayon_core.tools.delivery.delivery.DeliveryOptionsDialog","title":"<code>DeliveryOptionsDialog</code>","text":"<p>               Bases: <code>QDialog</code></p> <p>Dialog to select template where to deliver selected representations.</p> Source code in <code>client/ayon_core/tools/delivery/delivery.py</code> <pre><code>class DeliveryOptionsDialog(QtWidgets.QDialog):\n    \"\"\"Dialog to select template where to deliver selected representations.\"\"\"\n\n    def __init__(\n        self,\n        project_name,\n        version_ids,\n        list_entity_label=None,\n        *,\n        log=None,\n        parent=None,\n    ):\n        super().__init__(parent=parent)\n\n        if log is None:\n            log = logging.getLogger(__name__)\n\n        self.setWindowTitle(f\"Deliver {len(version_ids)} versions\")\n        icon = QtGui.QIcon(resources.get_ayon_icon_filepath())\n        self.setWindowIcon(icon)\n\n        self.setWindowFlags(\n            QtCore.Qt.WindowStaysOnTopHint\n            | QtCore.Qt.WindowCloseButtonHint\n            | QtCore.Qt.WindowMinimizeButtonHint\n        )\n\n        self.setStyleSheet(style.load_stylesheet())\n\n        self.anatomy = Anatomy(project_name)\n        self._representations = None\n        self.log = log\n        self.currently_uploaded = 0\n\n        self._set_representations(project_name, version_ids)\n\n        dropdown = QtWidgets.QComboBox()\n        self.templates = self._get_templates(self.anatomy)\n        for name, _ in self.templates.items():\n            dropdown.addItem(name)\n        if self.templates and platform.system() == \"Darwin\":\n            # fix macos QCombobox Style\n            dropdown.setItemDelegate(QtWidgets.QStyledItemDelegate())\n            # update combo box length to longest entry\n            longest_key = max(self.templates.keys(), key=len)\n            dropdown.setMinimumContentsLength(len(longest_key))\n\n        template_dir_label = QtWidgets.QLabel()\n        template_dir_label.setCursor(QtGui.QCursor(QtCore.Qt.IBeamCursor))\n        template_dir_label.setTextInteractionFlags(\n            QtCore.Qt.TextSelectableByMouse)\n\n        template_file_label = QtWidgets.QLabel()\n        template_file_label.setCursor(QtGui.QCursor(QtCore.Qt.IBeamCursor))\n        template_file_label.setTextInteractionFlags(\n            QtCore.Qt.TextSelectableByMouse)\n\n        renumber_frame = QtWidgets.QCheckBox()\n\n        first_frame_start = QtWidgets.QSpinBox()\n        max_int = (1 &lt;&lt; 32) // 2\n        first_frame_start.setRange(0, max_int - 1)\n\n        root_line_edit = QtWidgets.QLineEdit()\n        list_label_line_edit = QtWidgets.QLineEdit()\n\n        # Collapsible \"Advanced options\" section, hidden by default\n        advanced_toggle = QtWidgets.QToolButton()\n        advanced_toggle.setText(\"Advanced options\")\n        advanced_toggle.setCheckable(True)\n        advanced_toggle.setChecked(False)\n        advanced_toggle.setToolButtonStyle(\n            QtCore.Qt.ToolButtonTextBesideIcon)\n        advanced_toggle.setArrowType(QtCore.Qt.RightArrow)\n\n        advanced_widget = QtWidgets.QWidget(self)\n        advanced_layout = QtWidgets.QFormLayout(advanced_widget)\n        advanced_layout.setContentsMargins(10, 5, 5, 5)\n        advanced_layout.addRow(\"Renumber Frame\", renumber_frame)\n        advanced_layout.addRow(\"Renumber start frame\", first_frame_start)\n        advanced_layout.addRow(\"Root\", root_line_edit)\n        advanced_layout.addRow(\"List label\", list_label_line_edit)\n        # set default value if `list_entity_label` arg is provided\n        if list_entity_label:\n            list_label_line_edit.setText(list_entity_label)\n        advanced_widget.setVisible(False)\n\n        def _on_advanced_toggled(checked):\n            advanced_widget.setVisible(checked)\n            advanced_toggle.setArrowType(\n                QtCore.Qt.DownArrow if checked else QtCore.Qt.RightArrow\n            )\n\n        advanced_toggle.toggled.connect(_on_advanced_toggled)\n\n        btn_select_all = QtWidgets.QPushButton(\"Select All\")\n\n        repre_list = QtWidgets.QListWidget()\n        repre_list.setSelectionMode(\n            QtWidgets.QAbstractItemView.ExtendedSelection)\n        for repre in sorted(self._get_representation_names()):\n            repre_list.addItem(QtWidgets.QListWidgetItem(repre))\n\n        repre_list.itemSelectionChanged.connect(self._update_selected_label)\n        btn_select_all.clicked.connect(repre_list.selectAll)\n\n        repre_widget = QtWidgets.QWidget(self)\n        repre_layout = QtWidgets.QVBoxLayout(repre_widget)\n        repre_layout.setContentsMargins(10, 5, 5, 10)\n        repre_layout.addWidget(btn_select_all)\n        repre_layout.addWidget(repre_list)\n\n        selected_label = QtWidgets.QLabel()\n\n        input_widget = QtWidgets.QWidget(self)\n        input_layout = QtWidgets.QFormLayout(input_widget)\n        input_layout.setContentsMargins(10, 15, 5, 5)\n\n        input_layout.addRow(\"Selected representations\", selected_label)\n        input_layout.addRow(\"Delivery template\", dropdown)\n        input_layout.addRow(\"Directory template\", template_dir_label)\n        input_layout.addRow(\"File template\", template_file_label)\n        input_layout.addRow(\"Representations\", repre_widget)\n        input_layout.addRow(advanced_toggle)\n        input_layout.addRow(advanced_widget)\n\n        btn_delivery = QtWidgets.QPushButton(\"Deliver\")\n        btn_delivery.setEnabled(False)\n\n        progress_bar = QtWidgets.QProgressBar(self)\n        progress_bar.setMinimum = 0\n        progress_bar.setMaximum = 100\n        progress_bar.setVisible(False)\n\n        text_area = QtWidgets.QTextEdit()\n        text_area.setReadOnly(True)\n        text_area.setVisible(False)\n        text_area.setMinimumHeight(100)\n\n        layout = QtWidgets.QVBoxLayout(self)\n\n        layout.addWidget(input_widget)\n        layout.addStretch(1)\n        layout.addWidget(btn_delivery)\n        layout.addWidget(progress_bar)\n        layout.addWidget(text_area)\n\n        self.selected_label = selected_label\n        self.template_dir_label = template_dir_label\n        self.template_file_label = template_file_label\n        self.dropdown = dropdown\n        self.first_frame_start = first_frame_start\n        self.renumber_frame = renumber_frame\n        self.root_line_edit = root_line_edit\n        self.list_label_line_edit = list_label_line_edit\n        self.repre_list = repre_list\n        self.progress_bar = progress_bar\n        self.text_area = text_area\n        self.btn_delivery = btn_delivery\n\n        self.selected_repres_count, self.files_selected, self.size_selected = \\\n            self._get_counts(self._get_selected_repres())\n\n        self._update_selected_label()\n        self._update_template_value()\n\n        btn_delivery.clicked.connect(self.deliver)\n        dropdown.currentIndexChanged.connect(self._update_template_value)\n\n        if not self.dropdown.count():\n            self.text_area.setVisible(True)\n            error_message = (\n                \"No Delivery Templates found!\\n\"\n                \"Add Template in [project_anatomy/templates/delivery]\"\n            )\n            self.text_area.setText(error_message)\n            self.log.error(error_message.replace(\"\\n\", \" \"))\n\n    def deliver(self):\n        \"\"\"Main method to loop through all selected representations\"\"\"\n        self.progress_bar.setVisible(True)\n        self.btn_delivery.setEnabled(False)\n        QtWidgets.QApplication.processEvents()\n\n        report_items = defaultdict(list)\n\n        selected_repres = self._get_selected_repres()\n\n        datetime_data = get_datetime_data()\n        template_name = self.dropdown.currentText()\n        format_dict = get_format_dict(self.anatomy, self.root_line_edit.text())\n        list_label = self.list_label_line_edit.text()\n        renumber_frame = self.renumber_frame.isChecked()\n        frame_offset = self.first_frame_start.value()\n        filtered_repres = []\n        repre_ids = set()\n        for repre in self._representations:\n            if repre[\"name\"] in selected_repres:\n                filtered_repres.append(repre)\n                repre_ids.add(repre[\"id\"])\n\n        template_data_by_repre_id = (\n            get_representations_delivery_template_data(\n                self.anatomy.project_name, repre_ids\n            )\n        )\n        for repre in filtered_repres:\n            repre_path = get_representation_path_with_anatomy(\n                repre, self.anatomy\n            )\n\n            template_data = template_data_by_repre_id[repre[\"id\"]]\n            if list_label:\n                template_data[\"list\"] = {\"label\": list_label}\n\n            # Use temporary placeholder so we don't need to check destination\n            # path per file of the representation\n            template_data[\"publishedFilename\"] = \"&lt;publishedFilename&gt;\"\n            new_report_items = check_destination_path(\n                repre[\"id\"],\n                self.anatomy,\n                template_data,\n                datetime_data,\n                template_name\n            )\n\n            report_items.update(new_report_items)\n            if new_report_items:\n                continue\n\n            args = [\n                repre_path,\n                repre,\n                self.anatomy,\n                template_name,\n                template_data,\n                format_dict,\n                report_items,\n                self.log\n            ]\n\n            # TODO: This will currently incorrectly detect 'resources'\n            #  that are published along with the publish, because those should\n            #  not adhere to the template directly but are ingested in a\n            #  customized way. For example, maya look textures or any publish\n            #  that directly adds files into `instance.data[\"transfers\"]`\n            src_paths = []\n            for repre_file in repre[\"files\"]:\n                src_path = self.anatomy.fill_root(repre_file[\"path\"])\n                src_paths.append(src_path)\n            sources_and_frames = collect_frames(src_paths)\n\n            frames = set(sources_and_frames.values())\n            frames.discard(None)\n            first_frame = None\n            if frames:\n                first_frame = min(frames)\n\n            for src_path, frame in sources_and_frames.items():\n                # Support {publishedFilename} token\n                template_data[\"publishedFilename\"] = os.path.basename(\n                    # Replace backslash to forward slashes so basename\n                    # also resolves to filename only on POSIX\n                    src_path.replace(\"\\\\\", \"/\")\n                )\n                args[0] = src_path\n                # Renumber frames\n                if renumber_frame and frame is not None:\n                    # Calculate offset between\n                    # first frame and current frame\n                    # - '0' for first frame\n                    offset = frame_offset - int(first_frame)\n                    # Add offset to new frame start\n                    dst_frame = int(frame) + offset\n                    if dst_frame &lt; 0:\n                        msg = \"Renumber frame has a smaller number than original frame\"     # noqa\n                        report_items[msg].append(src_path)\n                        self.log.warning(\"{} &lt;{}&gt;\".format(\n                            msg, dst_frame))\n                        continue\n                    frame = dst_frame\n\n                if frame is not None:\n                    if repre[\"context\"].get(\"frame\"):\n                        template_data[\"frame\"] = frame\n                    elif repre[\"context\"].get(\"udim\"):\n                        template_data[\"udim\"] = frame\n                    else:\n                        # Fallback\n                        self.log.warning(\n                            \"Representation context has no frame or udim\"\n                            \" data. Supplying sequence frame to '{frame}'\"\n                            \" formatting data.\"\n                        )\n                        template_data[\"frame\"] = frame\n                new_report_items, uploaded = deliver_single_file(*args)\n                report_items.update(new_report_items)\n                self._update_progress(uploaded)\n\n        self.text_area.setText(self._format_report(report_items))\n        self.text_area.setVisible(True)\n\n    def _get_representation_names(self):\n        \"\"\"Get set of representation names for checkbox filtering.\"\"\"\n        return set([repre[\"name\"] for repre in self._representations])\n\n    def _get_templates(self, anatomy):\n        \"\"\"Adds list of delivery templates from Anatomy to dropdown.\"\"\"\n        templates = {}\n        for template_name, value in anatomy.templates[\"delivery\"].items():\n            directory_template = value[\"directory\"]\n            if not directory_template.startswith(\"{root\"):\n                self.log.warning(\n                    \"Skipping template '%s' because directory template does \"\n                    \"not start with `{root` in value: %s\",\n                    template_name, directory_template\n                )\n                continue\n\n            templates[template_name] = value\n\n        return templates\n\n    def _set_representations(self, project_name, version_ids):\n        repres = list(ayon_api.get_representations(\n            project_name, version_ids=version_ids\n        ))\n\n        self._representations = repres\n\n    def _get_counts(self, selected_repres=None):\n        \"\"\"Returns tuple of number of selected files and their size.\"\"\"\n        files_selected = 0\n        size_selected = 0\n        selected_repres_count = 0\n        for repre in self._representations:\n            if repre[\"name\"] in selected_repres:\n                selected_repres_count += 1\n                files = repre.get(\"files\", [])\n                if not files:  # for repre without files, cannot divide by 0\n                    files_selected += 1\n                    size_selected += 0\n                else:\n                    for repre_file in files:\n                        files_selected += 1\n                        size_selected += repre_file[\"size\"]\n\n        return selected_repres_count, files_selected, size_selected\n\n    def _prepare_label(self):\n        \"\"\"Provides text with no of selected files and their size.\"\"\"\n        label = \"{} ({} files, size {})\".format(\n            self.selected_repres_count,\n            self.files_selected,\n            format_file_size(self.size_selected))\n        return label\n\n    def _get_selected_repres(self):\n        \"\"\"Returns list of representation names selected in the list.\"\"\"\n        return [item.text() for item in self.repre_list.selectedItems()]\n\n    def _update_selected_label(self):\n        \"\"\"Updates label with list of number of selected files.\"\"\"\n        selected_repres = self._get_selected_repres()\n        self.selected_repres_count, self.files_selected, self.size_selected = \\\n            self._get_counts(selected_repres)\n        self.selected_label.setText(self._prepare_label())\n        # update delivery button state if any templates found\n        if self.dropdown.count():\n            self.btn_delivery.setEnabled(bool(selected_repres))\n\n    def _update_template_value(self, _index=None):\n        \"\"\"Sets template value to label after selection in dropdown.\"\"\"\n        name = self.dropdown.currentText()\n        template_value = self.templates.get(name)\n        if template_value:\n            self.template_dir_label.setText(template_value[\"directory\"])\n            self.template_file_label.setText(template_value[\"file\"])\n            self.btn_delivery.setEnabled(bool(self._get_selected_repres()))\n\n    def _update_progress(self, uploaded):\n        \"\"\"Update progress bar after each repre copied.\"\"\"\n        self.currently_uploaded += uploaded\n\n        ratio = self.currently_uploaded / self.files_selected\n        self.progress_bar.setValue(ratio * self.progress_bar.maximum())\n\n    def _format_report(self, report_items):\n        \"\"\"Format final result and error details as html.\"\"\"\n        msg = \"Delivery finished\"\n        if not report_items:\n            msg += \" successfully\"\n        else:\n            msg += \" with errors\"\n        txt = \"&lt;h2&gt;{}&lt;/h2&gt;\".format(msg)\n        for header, data in report_items.items():\n            txt += \"&lt;h3&gt;{}&lt;/h3&gt;\".format(header)\n            for item in data:\n                txt += \"{}&lt;br&gt;\".format(item)\n\n        return txt\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/delivery/delivery.html#client.ayon_core.tools.delivery.delivery.DeliveryOptionsDialog.deliver","title":"<code>deliver()</code>","text":"<p>Main method to loop through all selected representations</p> Source code in <code>client/ayon_core/tools/delivery/delivery.py</code> <pre><code>def deliver(self):\n    \"\"\"Main method to loop through all selected representations\"\"\"\n    self.progress_bar.setVisible(True)\n    self.btn_delivery.setEnabled(False)\n    QtWidgets.QApplication.processEvents()\n\n    report_items = defaultdict(list)\n\n    selected_repres = self._get_selected_repres()\n\n    datetime_data = get_datetime_data()\n    template_name = self.dropdown.currentText()\n    format_dict = get_format_dict(self.anatomy, self.root_line_edit.text())\n    list_label = self.list_label_line_edit.text()\n    renumber_frame = self.renumber_frame.isChecked()\n    frame_offset = self.first_frame_start.value()\n    filtered_repres = []\n    repre_ids = set()\n    for repre in self._representations:\n        if repre[\"name\"] in selected_repres:\n            filtered_repres.append(repre)\n            repre_ids.add(repre[\"id\"])\n\n    template_data_by_repre_id = (\n        get_representations_delivery_template_data(\n            self.anatomy.project_name, repre_ids\n        )\n    )\n    for repre in filtered_repres:\n        repre_path = get_representation_path_with_anatomy(\n            repre, self.anatomy\n        )\n\n        template_data = template_data_by_repre_id[repre[\"id\"]]\n        if list_label:\n            template_data[\"list\"] = {\"label\": list_label}\n\n        # Use temporary placeholder so we don't need to check destination\n        # path per file of the representation\n        template_data[\"publishedFilename\"] = \"&lt;publishedFilename&gt;\"\n        new_report_items = check_destination_path(\n            repre[\"id\"],\n            self.anatomy,\n            template_data,\n            datetime_data,\n            template_name\n        )\n\n        report_items.update(new_report_items)\n        if new_report_items:\n            continue\n\n        args = [\n            repre_path,\n            repre,\n            self.anatomy,\n            template_name,\n            template_data,\n            format_dict,\n            report_items,\n            self.log\n        ]\n\n        # TODO: This will currently incorrectly detect 'resources'\n        #  that are published along with the publish, because those should\n        #  not adhere to the template directly but are ingested in a\n        #  customized way. For example, maya look textures or any publish\n        #  that directly adds files into `instance.data[\"transfers\"]`\n        src_paths = []\n        for repre_file in repre[\"files\"]:\n            src_path = self.anatomy.fill_root(repre_file[\"path\"])\n            src_paths.append(src_path)\n        sources_and_frames = collect_frames(src_paths)\n\n        frames = set(sources_and_frames.values())\n        frames.discard(None)\n        first_frame = None\n        if frames:\n            first_frame = min(frames)\n\n        for src_path, frame in sources_and_frames.items():\n            # Support {publishedFilename} token\n            template_data[\"publishedFilename\"] = os.path.basename(\n                # Replace backslash to forward slashes so basename\n                # also resolves to filename only on POSIX\n                src_path.replace(\"\\\\\", \"/\")\n            )\n            args[0] = src_path\n            # Renumber frames\n            if renumber_frame and frame is not None:\n                # Calculate offset between\n                # first frame and current frame\n                # - '0' for first frame\n                offset = frame_offset - int(first_frame)\n                # Add offset to new frame start\n                dst_frame = int(frame) + offset\n                if dst_frame &lt; 0:\n                    msg = \"Renumber frame has a smaller number than original frame\"     # noqa\n                    report_items[msg].append(src_path)\n                    self.log.warning(\"{} &lt;{}&gt;\".format(\n                        msg, dst_frame))\n                    continue\n                frame = dst_frame\n\n            if frame is not None:\n                if repre[\"context\"].get(\"frame\"):\n                    template_data[\"frame\"] = frame\n                elif repre[\"context\"].get(\"udim\"):\n                    template_data[\"udim\"] = frame\n                else:\n                    # Fallback\n                    self.log.warning(\n                        \"Representation context has no frame or udim\"\n                        \" data. Supplying sequence frame to '{frame}'\"\n                        \" formatting data.\"\n                    )\n                    template_data[\"frame\"] = frame\n            new_report_items, uploaded = deliver_single_file(*args)\n            report_items.update(new_report_items)\n            self._update_progress(uploaded)\n\n    self.text_area.setText(self._format_report(report_items))\n    self.text_area.setVisible(True)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/experimental_tools/index.html","title":"experimental_tools","text":""},{"location":"autoapi/client/ayon_core/tools/experimental_tools/index.html#client.ayon_core.tools.experimental_tools.ExperimentalTools","title":"<code>ExperimentalTools</code>","text":"<p>Wrapper around experimental tools.</p> <p>To add/remove experimental tool just add/remove tool to <code>experimental_tools</code> variable in init function.</p> <p>--- Example tool (callback will just print on click) --- def example_callback(*args):     print(\"Triggered tool\")</p> <p>experimental_tools = [     ExperimentalHostTool(         \"example\",         \"Example experimental tool\",         example_callback,         \"Example tool tooltip.\"     ) ]</p> Source code in <code>client/ayon_core/tools/experimental_tools/tools_def.py</code> <pre><code>class ExperimentalTools:\n    \"\"\"Wrapper around experimental tools.\n\n    To add/remove experimental tool just add/remove tool to\n    `experimental_tools` variable in __init__ function.\n\n    --- Example tool (callback will just print on click) ---\n    def example_callback(*args):\n        print(\"Triggered tool\")\n\n    experimental_tools = [\n        ExperimentalHostTool(\n            \"example\",\n            \"Example experimental tool\",\n            example_callback,\n            \"Example tool tooltip.\"\n        )\n    ]\n    ---\n    \"\"\"\n    def __init__(self, parent_widget=None, refresh=True):\n        # Definition of experimental tools\n        experimental_tools = [\n            ExperimentalHostTool(\n                \"publisher\",\n                \"New publisher\",\n                \"Combined creation and publishing into one tool.\",\n                self._show_publisher,\n                hosts_filter=[\n                    \"celaction\",\n                    \"flame\",\n                    \"harmony\",\n                    \"hiero\",\n                    \"resolve\",\n                ]\n            ),\n            ExperimentalHostTool(\n                \"pyblish_debug_stepper\",\n                \"Pyblish Debug Stepper\",\n                \"Debug Pyblish plugins step by step.\",\n                self._show_pyblish_debugger,\n            )\n        ]\n\n        # Store tools by identifier\n        tools_by_identifier = {}\n        for tool in experimental_tools:\n            if tool.identifier in tools_by_identifier:\n                raise KeyError((\n                    \"Duplicated experimental tool identifier \\\"{}\\\"\"\n                ).format(tool.identifier))\n            tools_by_identifier[tool.identifier] = tool\n\n        self._tools_by_identifier = tools_by_identifier\n        self._tools = experimental_tools\n\n        self._parent_widget = parent_widget\n        self._publisher_tool = None\n\n        if refresh:\n            self.refresh_availability()\n\n    @property\n    def tools(self):\n        \"\"\"Tools in list.\n\n        Returns:\n            list: Tools filtered by host name if filtering was enabled\n                on initialization.\n        \"\"\"\n        return self._tools\n\n    @property\n    def tools_by_identifier(self):\n        \"\"\"Tools by their identifier.\n\n        Returns:\n            dict: Tools by identifier filtered by host name if filtering\n                was enabled on initialization.\n        \"\"\"\n        return self._tools_by_identifier\n\n    def get(self, tool_identifier):\n        \"\"\"Get tool by identifier.\"\"\"\n        return self.tools_by_identifier.get(tool_identifier)\n\n    def get_tools_for_host(self, host_name=None):\n        if not host_name:\n            host_name = os.environ.get(\"AYON_HOST_NAME\")\n        tools = []\n        for tool in self.tools:\n            if (\n                isinstance(tool, ExperimentalHostTool)\n                and tool.is_available_for_host(host_name)\n            ):\n                tools.append(tool)\n        return tools\n\n    def refresh_availability(self):\n        \"\"\"Reload local settings and check if any tool changed ability.\"\"\"\n\n        # NOTE AYON does not have implemented settings for experimental\n        #   tools.\n        local_settings = {}\n        experimental_settings = (\n            local_settings.get(LOCAL_EXPERIMENTAL_KEY)\n        ) or {}\n\n        # Enable the following tools by default.\n        # Because they will always be disabled due\n        #   to the fact their settings don't exist.\n        experimental_settings.update({\n            \"pyblish_debug_stepper\": True,\n        })\n\n        for identifier, experimental_tool in self.tools_by_identifier.items():\n            enabled = experimental_settings.get(identifier, False)\n            experimental_tool.set_enabled(enabled)\n\n    def _show_publisher(self):\n        if self._publisher_tool is None:\n            from ayon_core.tools.publisher.window import PublisherWindow\n\n            self._publisher_tool = PublisherWindow(\n                parent=self._parent_widget\n            )\n\n        self._publisher_tool.show()\n\n    def _show_pyblish_debugger(self):\n        window = DebugUI(parent=self._parent_widget)\n        window.show()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/experimental_tools/index.html#client.ayon_core.tools.experimental_tools.ExperimentalTools.tools","title":"<code>tools</code>  <code>property</code>","text":"<p>Tools in list.</p> <p>Returns:</p> Name Type Description <code>list</code> <p>Tools filtered by host name if filtering was enabled on initialization.</p>"},{"location":"autoapi/client/ayon_core/tools/experimental_tools/index.html#client.ayon_core.tools.experimental_tools.ExperimentalTools.tools_by_identifier","title":"<code>tools_by_identifier</code>  <code>property</code>","text":"<p>Tools by their identifier.</p> <p>Returns:</p> Name Type Description <code>dict</code> <p>Tools by identifier filtered by host name if filtering was enabled on initialization.</p>"},{"location":"autoapi/client/ayon_core/tools/experimental_tools/index.html#client.ayon_core.tools.experimental_tools.ExperimentalTools.get","title":"<code>get(tool_identifier)</code>","text":"<p>Get tool by identifier.</p> Source code in <code>client/ayon_core/tools/experimental_tools/tools_def.py</code> <pre><code>def get(self, tool_identifier):\n    \"\"\"Get tool by identifier.\"\"\"\n    return self.tools_by_identifier.get(tool_identifier)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/experimental_tools/index.html#client.ayon_core.tools.experimental_tools.ExperimentalTools.refresh_availability","title":"<code>refresh_availability()</code>","text":"<p>Reload local settings and check if any tool changed ability.</p> Source code in <code>client/ayon_core/tools/experimental_tools/tools_def.py</code> <pre><code>def refresh_availability(self):\n    \"\"\"Reload local settings and check if any tool changed ability.\"\"\"\n\n    # NOTE AYON does not have implemented settings for experimental\n    #   tools.\n    local_settings = {}\n    experimental_settings = (\n        local_settings.get(LOCAL_EXPERIMENTAL_KEY)\n    ) or {}\n\n    # Enable the following tools by default.\n    # Because they will always be disabled due\n    #   to the fact their settings don't exist.\n    experimental_settings.update({\n        \"pyblish_debug_stepper\": True,\n    })\n\n    for identifier, experimental_tool in self.tools_by_identifier.items():\n        enabled = experimental_settings.get(identifier, False)\n        experimental_tool.set_enabled(enabled)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/experimental_tools/dialog.html","title":"dialog","text":""},{"location":"autoapi/client/ayon_core/tools/experimental_tools/pyblish_debug_stepper.html","title":"pyblish_debug_stepper","text":"<p>Brought from https://gist.github.com/BigRoy/1972822065e38f8fae7521078e44eca2 Code Credits: BigRoy</p> Requirement <p>It requires pyblish version &gt;= 1.8.12</p> How it works <p>This tool makes use of pyblish event <code>pluginProcessed</code> to:     1. Pause the publishing.     2. Collect some info about the plugin.     3. Show that info to the tool's window.     4. Continue publishing on clicking <code>step</code> button.</p> How to use it <ol> <li>Launch the tool from AYON experimental tools window.</li> <li>Launch the publisher tool and click validate.</li> <li>Click Step to run plugins one by one.</li> </ol> Note <p>Pyblish debugger also works when triggering the validation or   publishing from code. Here's an example about validating from code: https://github.com/MustafaJafar/ayon-recipes/blob/main/validate_from_code.py</p>"},{"location":"autoapi/client/ayon_core/tools/experimental_tools/pyblish_debug_stepper.html#client.ayon_core.tools.experimental_tools.pyblish_debug_stepper.failsafe_deepcopy","title":"<code>failsafe_deepcopy(data)</code>","text":"<p>Allow skipping the deepcopy for unsupported types</p> Source code in <code>client/ayon_core/tools/experimental_tools/pyblish_debug_stepper.py</code> <pre><code>def failsafe_deepcopy(data):\n    \"\"\"Allow skipping the deepcopy for unsupported types\"\"\"\n    try:\n        return copy.deepcopy(data)\n    except TypeError:\n        if isinstance(data, dict):\n            return {\n                key: failsafe_deepcopy(value)\n                for key, value in data.items()\n            }\n        elif isinstance(data, list):\n            return data.copy()\n    return data\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/experimental_tools/tools_def.html","title":"tools_def","text":""},{"location":"autoapi/client/ayon_core/tools/experimental_tools/tools_def.html#client.ayon_core.tools.experimental_tools.tools_def.ExperimentalHostTool","title":"<code>ExperimentalHostTool</code>","text":"<p>               Bases: <code>ExperimentalTool</code></p> <p>Definition of experimental tool.</p> <p>Definition is used in local settings and in experimental tools dialog.</p> <p>Parameters:</p> Name Type Description Default <code>identifier</code> <code>str</code> <p>String identifier of tool (unique).</p> required <code>label</code> <code>str</code> <p>Label shown in UI.</p> required <code>callback</code> <code>function</code> <p>Callback for UI button.</p> required <code>tooltip</code> <code>str</code> <p>Tooltip showed on button.</p> required <code>hosts_filter</code> <code>list</code> <p>List of host names for which is tool available. Some tools may not be available in all hosts.</p> <code>None</code> Source code in <code>client/ayon_core/tools/experimental_tools/tools_def.py</code> <pre><code>class ExperimentalHostTool(ExperimentalTool):\n    \"\"\"Definition of experimental tool.\n\n    Definition is used in local settings and in experimental tools dialog.\n\n    Args:\n        identifier (str): String identifier of tool (unique).\n        label (str): Label shown in UI.\n        callback (function): Callback for UI button.\n        tooltip (str): Tooltip showed on button.\n        hosts_filter (list): List of host names for which is tool available.\n            Some tools may not be available in all hosts.\n    \"\"\"\n    def __init__(\n        self, identifier, label, tooltip, callback, hosts_filter=None\n    ):\n        super(ExperimentalHostTool, self).__init__(identifier, label, tooltip)\n        self.callback = callback\n        self.hosts_filter = hosts_filter\n        self._enabled = True\n\n    def is_available_for_host(self, host_name):\n        if self.hosts_filter:\n            return host_name in self.hosts_filter\n        return True\n\n    def execute(self, *args, **kwargs):\n        \"\"\"Trigger registered callback.\"\"\"\n        self.callback(*args, **kwargs)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/experimental_tools/tools_def.html#client.ayon_core.tools.experimental_tools.tools_def.ExperimentalHostTool.execute","title":"<code>execute(*args, **kwargs)</code>","text":"<p>Trigger registered callback.</p> Source code in <code>client/ayon_core/tools/experimental_tools/tools_def.py</code> <pre><code>def execute(self, *args, **kwargs):\n    \"\"\"Trigger registered callback.\"\"\"\n    self.callback(*args, **kwargs)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/experimental_tools/tools_def.html#client.ayon_core.tools.experimental_tools.tools_def.ExperimentalTool","title":"<code>ExperimentalTool</code>","text":"<p>               Bases: <code>object</code></p> <p>Definition of experimental tool.</p> <p>Definition is used in local settings.</p> <p>Parameters:</p> Name Type Description Default <code>identifier</code> <code>str</code> <p>String identifier of tool (unique).</p> required <code>label</code> <code>str</code> <p>Label shown in UI.</p> required Source code in <code>client/ayon_core/tools/experimental_tools/tools_def.py</code> <pre><code>class ExperimentalTool(object):\n    \"\"\"Definition of experimental tool.\n\n    Definition is used in local settings.\n\n    Args:\n        identifier (str): String identifier of tool (unique).\n        label (str): Label shown in UI.\n    \"\"\"\n    def __init__(self, identifier, label, tooltip):\n        self.identifier = identifier\n        self.label = label\n        self.tooltip = tooltip\n        self._enabled = True\n\n    @property\n    def enabled(self):\n        \"\"\"Is tool enabled and button is clickable.\"\"\"\n        return self._enabled\n\n    def set_enabled(self, enabled=True):\n        \"\"\"Change if tool is enabled.\"\"\"\n        self._enabled = enabled\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/experimental_tools/tools_def.html#client.ayon_core.tools.experimental_tools.tools_def.ExperimentalTool.enabled","title":"<code>enabled</code>  <code>property</code>","text":"<p>Is tool enabled and button is clickable.</p>"},{"location":"autoapi/client/ayon_core/tools/experimental_tools/tools_def.html#client.ayon_core.tools.experimental_tools.tools_def.ExperimentalTool.set_enabled","title":"<code>set_enabled(enabled=True)</code>","text":"<p>Change if tool is enabled.</p> Source code in <code>client/ayon_core/tools/experimental_tools/tools_def.py</code> <pre><code>def set_enabled(self, enabled=True):\n    \"\"\"Change if tool is enabled.\"\"\"\n    self._enabled = enabled\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/experimental_tools/tools_def.html#client.ayon_core.tools.experimental_tools.tools_def.ExperimentalTools","title":"<code>ExperimentalTools</code>","text":"<p>Wrapper around experimental tools.</p> <p>To add/remove experimental tool just add/remove tool to <code>experimental_tools</code> variable in init function.</p> <p>--- Example tool (callback will just print on click) --- def example_callback(*args):     print(\"Triggered tool\")</p> <p>experimental_tools = [     ExperimentalHostTool(         \"example\",         \"Example experimental tool\",         example_callback,         \"Example tool tooltip.\"     ) ]</p> Source code in <code>client/ayon_core/tools/experimental_tools/tools_def.py</code> <pre><code>class ExperimentalTools:\n    \"\"\"Wrapper around experimental tools.\n\n    To add/remove experimental tool just add/remove tool to\n    `experimental_tools` variable in __init__ function.\n\n    --- Example tool (callback will just print on click) ---\n    def example_callback(*args):\n        print(\"Triggered tool\")\n\n    experimental_tools = [\n        ExperimentalHostTool(\n            \"example\",\n            \"Example experimental tool\",\n            example_callback,\n            \"Example tool tooltip.\"\n        )\n    ]\n    ---\n    \"\"\"\n    def __init__(self, parent_widget=None, refresh=True):\n        # Definition of experimental tools\n        experimental_tools = [\n            ExperimentalHostTool(\n                \"publisher\",\n                \"New publisher\",\n                \"Combined creation and publishing into one tool.\",\n                self._show_publisher,\n                hosts_filter=[\n                    \"celaction\",\n                    \"flame\",\n                    \"harmony\",\n                    \"hiero\",\n                    \"resolve\",\n                ]\n            ),\n            ExperimentalHostTool(\n                \"pyblish_debug_stepper\",\n                \"Pyblish Debug Stepper\",\n                \"Debug Pyblish plugins step by step.\",\n                self._show_pyblish_debugger,\n            )\n        ]\n\n        # Store tools by identifier\n        tools_by_identifier = {}\n        for tool in experimental_tools:\n            if tool.identifier in tools_by_identifier:\n                raise KeyError((\n                    \"Duplicated experimental tool identifier \\\"{}\\\"\"\n                ).format(tool.identifier))\n            tools_by_identifier[tool.identifier] = tool\n\n        self._tools_by_identifier = tools_by_identifier\n        self._tools = experimental_tools\n\n        self._parent_widget = parent_widget\n        self._publisher_tool = None\n\n        if refresh:\n            self.refresh_availability()\n\n    @property\n    def tools(self):\n        \"\"\"Tools in list.\n\n        Returns:\n            list: Tools filtered by host name if filtering was enabled\n                on initialization.\n        \"\"\"\n        return self._tools\n\n    @property\n    def tools_by_identifier(self):\n        \"\"\"Tools by their identifier.\n\n        Returns:\n            dict: Tools by identifier filtered by host name if filtering\n                was enabled on initialization.\n        \"\"\"\n        return self._tools_by_identifier\n\n    def get(self, tool_identifier):\n        \"\"\"Get tool by identifier.\"\"\"\n        return self.tools_by_identifier.get(tool_identifier)\n\n    def get_tools_for_host(self, host_name=None):\n        if not host_name:\n            host_name = os.environ.get(\"AYON_HOST_NAME\")\n        tools = []\n        for tool in self.tools:\n            if (\n                isinstance(tool, ExperimentalHostTool)\n                and tool.is_available_for_host(host_name)\n            ):\n                tools.append(tool)\n        return tools\n\n    def refresh_availability(self):\n        \"\"\"Reload local settings and check if any tool changed ability.\"\"\"\n\n        # NOTE AYON does not have implemented settings for experimental\n        #   tools.\n        local_settings = {}\n        experimental_settings = (\n            local_settings.get(LOCAL_EXPERIMENTAL_KEY)\n        ) or {}\n\n        # Enable the following tools by default.\n        # Because they will always be disabled due\n        #   to the fact their settings don't exist.\n        experimental_settings.update({\n            \"pyblish_debug_stepper\": True,\n        })\n\n        for identifier, experimental_tool in self.tools_by_identifier.items():\n            enabled = experimental_settings.get(identifier, False)\n            experimental_tool.set_enabled(enabled)\n\n    def _show_publisher(self):\n        if self._publisher_tool is None:\n            from ayon_core.tools.publisher.window import PublisherWindow\n\n            self._publisher_tool = PublisherWindow(\n                parent=self._parent_widget\n            )\n\n        self._publisher_tool.show()\n\n    def _show_pyblish_debugger(self):\n        window = DebugUI(parent=self._parent_widget)\n        window.show()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/experimental_tools/tools_def.html#client.ayon_core.tools.experimental_tools.tools_def.ExperimentalTools.tools","title":"<code>tools</code>  <code>property</code>","text":"<p>Tools in list.</p> <p>Returns:</p> Name Type Description <code>list</code> <p>Tools filtered by host name if filtering was enabled on initialization.</p>"},{"location":"autoapi/client/ayon_core/tools/experimental_tools/tools_def.html#client.ayon_core.tools.experimental_tools.tools_def.ExperimentalTools.tools_by_identifier","title":"<code>tools_by_identifier</code>  <code>property</code>","text":"<p>Tools by their identifier.</p> <p>Returns:</p> Name Type Description <code>dict</code> <p>Tools by identifier filtered by host name if filtering was enabled on initialization.</p>"},{"location":"autoapi/client/ayon_core/tools/experimental_tools/tools_def.html#client.ayon_core.tools.experimental_tools.tools_def.ExperimentalTools.get","title":"<code>get(tool_identifier)</code>","text":"<p>Get tool by identifier.</p> Source code in <code>client/ayon_core/tools/experimental_tools/tools_def.py</code> <pre><code>def get(self, tool_identifier):\n    \"\"\"Get tool by identifier.\"\"\"\n    return self.tools_by_identifier.get(tool_identifier)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/experimental_tools/tools_def.html#client.ayon_core.tools.experimental_tools.tools_def.ExperimentalTools.refresh_availability","title":"<code>refresh_availability()</code>","text":"<p>Reload local settings and check if any tool changed ability.</p> Source code in <code>client/ayon_core/tools/experimental_tools/tools_def.py</code> <pre><code>def refresh_availability(self):\n    \"\"\"Reload local settings and check if any tool changed ability.\"\"\"\n\n    # NOTE AYON does not have implemented settings for experimental\n    #   tools.\n    local_settings = {}\n    experimental_settings = (\n        local_settings.get(LOCAL_EXPERIMENTAL_KEY)\n    ) or {}\n\n    # Enable the following tools by default.\n    # Because they will always be disabled due\n    #   to the fact their settings don't exist.\n    experimental_settings.update({\n        \"pyblish_debug_stepper\": True,\n    })\n\n    for identifier, experimental_tool in self.tools_by_identifier.items():\n        enabled = experimental_settings.get(identifier, False)\n        experimental_tool.set_enabled(enabled)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/index.html","title":"launcher","text":""},{"location":"autoapi/client/ayon_core/tools/launcher/abstract.html","title":"abstract","text":""},{"location":"autoapi/client/ayon_core/tools/launcher/abstract.html#client.ayon_core.tools.launcher.abstract.AbstractLauncherBackend","title":"<code>AbstractLauncherBackend</code>","text":"<p>               Bases: <code>AbstractLauncherCommon</code></p> Source code in <code>client/ayon_core/tools/launcher/abstract.py</code> <pre><code>class AbstractLauncherBackend(AbstractLauncherCommon):\n    @abstractmethod\n    def emit_event(self, topic, data=None, source=None):\n        \"\"\"Emit event.\n\n        Args:\n            topic (str): Event topic used for callbacks filtering.\n            data (Optional[dict[str, Any]]): Event data.\n            source (Optional[str]): Event source.\n        \"\"\"\n\n        pass\n\n    @abstractmethod\n    def get_addons_manager(self) -&gt; AddonsManager:\n        pass\n\n    @abstractmethod\n    def get_project_settings(self, project_name):\n        \"\"\"Project settings for current project.\n\n        Args:\n            project_name (Optional[str]): Project name.\n\n        Returns:\n            dict[str, Any]: Project settings.\n        \"\"\"\n\n        pass\n\n    @abstractmethod\n    def get_project_entity(self, project_name):\n        \"\"\"Get project entity by name.\n\n        Args:\n            project_name (str): Project name.\n\n        Returns:\n            dict[str, Any]: Project entity data.\n        \"\"\"\n\n        pass\n\n    @abstractmethod\n    def get_folder_entity(self, project_name, folder_id):\n        \"\"\"Get folder entity by id.\n\n        Args:\n            project_name (str): Project name.\n            folder_id (str): Folder id.\n\n        Returns:\n            dict[str, Any]: Folder entity data.\n        \"\"\"\n\n        pass\n\n    @abstractmethod\n    def get_task_entity(self, project_name, task_id):\n        \"\"\"Get task entity by id.\n\n        Args:\n            project_name (str): Project name.\n            task_id (str): Task id.\n\n        Returns:\n            dict[str, Any]: Task entity data.\n        \"\"\"\n\n        pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/abstract.html#client.ayon_core.tools.launcher.abstract.AbstractLauncherBackend.emit_event","title":"<code>emit_event(topic, data=None, source=None)</code>  <code>abstractmethod</code>","text":"<p>Emit event.</p> <p>Parameters:</p> Name Type Description Default <code>topic</code> <code>str</code> <p>Event topic used for callbacks filtering.</p> required <code>data</code> <code>Optional[dict[str, Any]]</code> <p>Event data.</p> <code>None</code> <code>source</code> <code>Optional[str]</code> <p>Event source.</p> <code>None</code> Source code in <code>client/ayon_core/tools/launcher/abstract.py</code> <pre><code>@abstractmethod\ndef emit_event(self, topic, data=None, source=None):\n    \"\"\"Emit event.\n\n    Args:\n        topic (str): Event topic used for callbacks filtering.\n        data (Optional[dict[str, Any]]): Event data.\n        source (Optional[str]): Event source.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/abstract.html#client.ayon_core.tools.launcher.abstract.AbstractLauncherBackend.get_folder_entity","title":"<code>get_folder_entity(project_name, folder_id)</code>  <code>abstractmethod</code>","text":"<p>Get folder entity by id.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>folder_id</code> <code>str</code> <p>Folder id.</p> required <p>Returns:</p> Type Description <p>dict[str, Any]: Folder entity data.</p> Source code in <code>client/ayon_core/tools/launcher/abstract.py</code> <pre><code>@abstractmethod\ndef get_folder_entity(self, project_name, folder_id):\n    \"\"\"Get folder entity by id.\n\n    Args:\n        project_name (str): Project name.\n        folder_id (str): Folder id.\n\n    Returns:\n        dict[str, Any]: Folder entity data.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/abstract.html#client.ayon_core.tools.launcher.abstract.AbstractLauncherBackend.get_project_entity","title":"<code>get_project_entity(project_name)</code>  <code>abstractmethod</code>","text":"<p>Get project entity by name.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <p>Returns:</p> Type Description <p>dict[str, Any]: Project entity data.</p> Source code in <code>client/ayon_core/tools/launcher/abstract.py</code> <pre><code>@abstractmethod\ndef get_project_entity(self, project_name):\n    \"\"\"Get project entity by name.\n\n    Args:\n        project_name (str): Project name.\n\n    Returns:\n        dict[str, Any]: Project entity data.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/abstract.html#client.ayon_core.tools.launcher.abstract.AbstractLauncherBackend.get_project_settings","title":"<code>get_project_settings(project_name)</code>  <code>abstractmethod</code>","text":"<p>Project settings for current project.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>Optional[str]</code> <p>Project name.</p> required <p>Returns:</p> Type Description <p>dict[str, Any]: Project settings.</p> Source code in <code>client/ayon_core/tools/launcher/abstract.py</code> <pre><code>@abstractmethod\ndef get_project_settings(self, project_name):\n    \"\"\"Project settings for current project.\n\n    Args:\n        project_name (Optional[str]): Project name.\n\n    Returns:\n        dict[str, Any]: Project settings.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/abstract.html#client.ayon_core.tools.launcher.abstract.AbstractLauncherBackend.get_task_entity","title":"<code>get_task_entity(project_name, task_id)</code>  <code>abstractmethod</code>","text":"<p>Get task entity by id.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>task_id</code> <code>str</code> <p>Task id.</p> required <p>Returns:</p> Type Description <p>dict[str, Any]: Task entity data.</p> Source code in <code>client/ayon_core/tools/launcher/abstract.py</code> <pre><code>@abstractmethod\ndef get_task_entity(self, project_name, task_id):\n    \"\"\"Get task entity by id.\n\n    Args:\n        project_name (str): Project name.\n        task_id (str): Task id.\n\n    Returns:\n        dict[str, Any]: Task entity data.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/abstract.html#client.ayon_core.tools.launcher.abstract.AbstractLauncherCommon","title":"<code>AbstractLauncherCommon</code>","text":"<p>               Bases: <code>ABC</code></p> Source code in <code>client/ayon_core/tools/launcher/abstract.py</code> <pre><code>class AbstractLauncherCommon(ABC):\n    @abstractmethod\n    def register_event_callback(self, topic, callback):\n        \"\"\"Register event callback.\n\n        Listen for events with given topic.\n\n        Args:\n            topic (str): Name of topic.\n            callback (Callable): Callback that will be called when event\n                is triggered.\n        \"\"\"\n\n        pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/abstract.html#client.ayon_core.tools.launcher.abstract.AbstractLauncherCommon.register_event_callback","title":"<code>register_event_callback(topic, callback)</code>  <code>abstractmethod</code>","text":"<p>Register event callback.</p> <p>Listen for events with given topic.</p> <p>Parameters:</p> Name Type Description Default <code>topic</code> <code>str</code> <p>Name of topic.</p> required <code>callback</code> <code>Callable</code> <p>Callback that will be called when event is triggered.</p> required Source code in <code>client/ayon_core/tools/launcher/abstract.py</code> <pre><code>@abstractmethod\ndef register_event_callback(self, topic, callback):\n    \"\"\"Register event callback.\n\n    Listen for events with given topic.\n\n    Args:\n        topic (str): Name of topic.\n        callback (Callable): Callback that will be called when event\n            is triggered.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/abstract.html#client.ayon_core.tools.launcher.abstract.AbstractLauncherFrontEnd","title":"<code>AbstractLauncherFrontEnd</code>","text":"<p>               Bases: <code>AbstractLauncherCommon</code></p> Source code in <code>client/ayon_core/tools/launcher/abstract.py</code> <pre><code>class AbstractLauncherFrontEnd(AbstractLauncherCommon):\n    @abstractmethod\n    def get_grouped_host_names(self) -&gt; list[str | None]:\n        \"\"\"Get list of host names that will group workfiles.\"\"\"\n\n    @abstractmethod\n    def set_grouped_host_names(self, host_names: list[str | None]):\n        \"\"\"Set list of host names that will group workfiles.\"\"\"\n\n    # Entity items for UI\n    @abstractmethod\n    def get_project_items(\n        self, sender: Optional[str] = None\n    ) -&gt; list[ProjectItem]:\n        \"\"\"Project items for all projects.\n\n        This function may trigger events 'projects.refresh.started' and\n        'projects.refresh.finished' which will contain 'sender' value in data.\n        That may help to avoid re-refresh of project items in UI elements.\n\n        Args:\n            sender (str): Who requested folder items.\n\n        Returns:\n            list[ProjectItem]: Minimum possible information needed\n                for visualisation of folder hierarchy.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_folder_type_items(\n        self, project_name: str, sender: Optional[str] = None\n    ) -&gt; list[FolderTypeItem]:\n        \"\"\"Folder type items for a project.\n\n        This function may trigger events with topics\n        'projects.folder_types.refresh.started' and\n        'projects.folder_types.refresh.finished' which will contain 'sender'\n        value in data.\n        That may help to avoid re-refresh of items in UI elements.\n\n        Args:\n            project_name (str): Project name.\n            sender (str): Who requested folder type items.\n\n        Returns:\n            list[FolderTypeItem]: Folder type information.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_task_type_items(\n        self, project_name: str, sender: Optional[str] = None\n    ) -&gt; list[TaskTypeItem]:\n        \"\"\"Task type items for a project.\n\n        This function may trigger events with topics\n        'projects.task_types.refresh.started' and\n        'projects.task_types.refresh.finished' which will contain 'sender'\n        value in data.\n        That may help to avoid re-refresh of items in UI elements.\n\n        Args:\n            project_name (str): Project name.\n            sender (str): Who requested task type items.\n\n        Returns:\n            list[TaskTypeItem]: Task type information.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_folder_items(\n        self, project_name: str, sender: Optional[str] = None\n    ) -&gt; list[FolderItem]:\n        \"\"\"Folder items to visualize project hierarchy.\n\n        This function may trigger events 'folders.refresh.started' and\n        'folders.refresh.finished' which will contain 'sender' value in data.\n        That may help to avoid re-refresh of folder items in UI elements.\n\n        Args:\n            project_name (str): Project name.\n            sender (str): Who requested folder items.\n\n        Returns:\n            list[FolderItem]: Minimum possible information needed\n                for visualisation of folder hierarchy.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_task_items(\n        self, project_name: str, folder_id: str, sender: Optional[str] = None\n    ) -&gt; list[TaskItem]:\n        \"\"\"Task items.\n\n        This function may trigger events 'tasks.refresh.started' and\n        'tasks.refresh.finished' which will contain 'sender' value in data.\n        That may help to avoid re-refresh of task items in UI elements.\n\n        Args:\n            project_name (str): Project name.\n            folder_id (str): Folder ID for which are tasks requested.\n            sender (str): Who requested folder items.\n\n        Returns:\n            list[TaskItem]: Minimum possible information needed\n                for visualisation of tasks.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_selected_project_name(self) -&gt; Optional[str]:\n        \"\"\"Selected project name.\n\n        Returns:\n            Optional[str]: Selected project name.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_selected_folder_id(self) -&gt; Optional[str]:\n        \"\"\"Selected folder id.\n\n        Returns:\n            Optional[str]: Selected folder id.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_selected_task_id(self) -&gt; Optional[str]:\n        \"\"\"Selected task id.\n\n        Returns:\n            Optional[str]: Selected task id.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_selected_task_name(self) -&gt; Optional[str]:\n        \"\"\"Selected task name.\n\n        Returns:\n            Optional[str]: Selected task name.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_selected_context(self) -&gt; dict[str, Optional[str]]:\n        \"\"\"Get whole selected context.\n\n        Example:\n            {\n                \"project_name\": self.get_selected_project_name(),\n                \"folder_id\": self.get_selected_folder_id(),\n                \"task_id\": self.get_selected_task_id(),\n                \"task_name\": self.get_selected_task_name(),\n            }\n\n        Returns:\n            dict[str, Optional[str]]: Selected context.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def set_selected_project(self, project_name: Optional[str]):\n        \"\"\"Change selected folder.\n\n        Args:\n            project_name (Optional[str]): Project nameor None if no project\n                is selected.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def set_selected_folder(self, folder_id: Optional[str]):\n        \"\"\"Change selected folder.\n\n        Args:\n            folder_id (Optional[str]): Folder id or None if no folder\n                is selected.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def set_selected_task(\n        self, task_id: Optional[str], task_name: Optional[str]\n    ):\n        \"\"\"Change selected task.\n\n        Args:\n            task_id (Optional[str]): Task id or None if no task\n                is selected.\n            task_name (Optional[str]): Task name or None if no task\n                is selected.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def set_selected_workfile(self, workfile_id: Optional[str]):\n        \"\"\"Change selected workfile.\n\n        Args:\n            workfile_id (Optional[str]): Workfile id or None.\n\n        \"\"\"\n        pass\n\n    # Actions\n    @abstractmethod\n    def get_action_items(\n        self,\n        project_name: Optional[str],\n        folder_id: Optional[str],\n        task_id: Optional[str],\n        workfile_id: Optional[str],\n    ) -&gt; list[ActionItem]:\n        \"\"\"Get action items for given context.\n\n        Args:\n            project_name (Optional[str]): Project name.\n            folder_id (Optional[str]): Folder id.\n            task_id (Optional[str]): Task id.\n            workfile_id (Optional[str]): Workfile id.\n\n        Returns:\n            list[ActionItem]: List of action items that should be shown\n                for given context.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def trigger_action(\n        self,\n        action_id: str,\n        project_name: Optional[str],\n        folder_id: Optional[str],\n        task_id: Optional[str],\n        workfile_id: Optional[str],\n    ):\n        \"\"\"Trigger action on given context.\n\n        Args:\n            action_id (str): Action identifier.\n            project_name (Optional[str]): Project name.\n            folder_id (Optional[str]): Folder id.\n            task_id (Optional[str]): Task id.\n            workfile_id (Optional[str]): Task id.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def trigger_webaction(\n        self,\n        context: WebactionContext,\n        action_label: str,\n        form_data: Optional[dict[str, Any]] = None,\n    ):\n        \"\"\"Trigger action on the given context.\n\n        Args:\n            context (WebactionContext): Webaction context.\n            action_label (str): Action label.\n            form_data (Optional[dict[str, Any]]): Form values of action.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_action_config_values(\n        self, context: WebactionContext\n    ) -&gt; dict[str, Any]:\n        \"\"\"Get action config values.\n\n        Args:\n            context (WebactionContext): Webaction context.\n\n        Returns:\n            dict[str, Any]: Action config values.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def set_action_config_values(\n        self,\n        context: WebactionContext,\n        values: dict[str, Any],\n    ):\n        \"\"\"Set action config values.\n\n        Args:\n            context (WebactionContext): Webaction context.\n            values (dict[str, Any]): Action config values.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def refresh(self):\n        \"\"\"Refresh everything, models, ui etc.\n\n        Triggers 'controller.refresh.started' event at the beginning and\n        'controller.refresh.finished' at the end.\n        \"\"\"\n\n        pass\n\n    @abstractmethod\n    def refresh_actions(self):\n        \"\"\"Refresh actions and all related data.\n\n        Triggers 'controller.refresh.actions.started' event at the beginning\n        and 'controller.refresh.actions.finished' at the end.\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_my_tasks_entity_ids(\n        self, project_name: str\n    ) -&gt; dict[str, list[str]]:\n        \"\"\"Get entity ids for my tasks.\n\n        Args:\n            project_name (str): Project name.\n\n        Returns:\n            dict[str, list[str]]: Folder and task ids.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_workfile_items(\n        self,\n        project_name: Optional[str],\n        task_id: Optional[str],\n    ) -&gt; list[WorkfileItem]:\n        \"\"\"Get workfile items for a given context.\n\n        Args:\n            project_name (Optional[str]): Project name.\n            task_id (Optional[str]): Task id.\n\n        Returns:\n            list[WorkfileItem]: List of workfile items.\n\n        \"\"\"\n        pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/abstract.html#client.ayon_core.tools.launcher.abstract.AbstractLauncherFrontEnd.get_action_config_values","title":"<code>get_action_config_values(context)</code>  <code>abstractmethod</code>","text":"<p>Get action config values.</p> <p>Parameters:</p> Name Type Description Default <code>context</code> <code>WebactionContext</code> <p>Webaction context.</p> required <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: Action config values.</p> Source code in <code>client/ayon_core/tools/launcher/abstract.py</code> <pre><code>@abstractmethod\ndef get_action_config_values(\n    self, context: WebactionContext\n) -&gt; dict[str, Any]:\n    \"\"\"Get action config values.\n\n    Args:\n        context (WebactionContext): Webaction context.\n\n    Returns:\n        dict[str, Any]: Action config values.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/abstract.html#client.ayon_core.tools.launcher.abstract.AbstractLauncherFrontEnd.get_action_items","title":"<code>get_action_items(project_name, folder_id, task_id, workfile_id)</code>  <code>abstractmethod</code>","text":"<p>Get action items for given context.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>Optional[str]</code> <p>Project name.</p> required <code>folder_id</code> <code>Optional[str]</code> <p>Folder id.</p> required <code>task_id</code> <code>Optional[str]</code> <p>Task id.</p> required <code>workfile_id</code> <code>Optional[str]</code> <p>Workfile id.</p> required <p>Returns:</p> Type Description <code>list[ActionItem]</code> <p>list[ActionItem]: List of action items that should be shown for given context.</p> Source code in <code>client/ayon_core/tools/launcher/abstract.py</code> <pre><code>@abstractmethod\ndef get_action_items(\n    self,\n    project_name: Optional[str],\n    folder_id: Optional[str],\n    task_id: Optional[str],\n    workfile_id: Optional[str],\n) -&gt; list[ActionItem]:\n    \"\"\"Get action items for given context.\n\n    Args:\n        project_name (Optional[str]): Project name.\n        folder_id (Optional[str]): Folder id.\n        task_id (Optional[str]): Task id.\n        workfile_id (Optional[str]): Workfile id.\n\n    Returns:\n        list[ActionItem]: List of action items that should be shown\n            for given context.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/abstract.html#client.ayon_core.tools.launcher.abstract.AbstractLauncherFrontEnd.get_folder_items","title":"<code>get_folder_items(project_name, sender=None)</code>  <code>abstractmethod</code>","text":"<p>Folder items to visualize project hierarchy.</p> <p>This function may trigger events 'folders.refresh.started' and 'folders.refresh.finished' which will contain 'sender' value in data. That may help to avoid re-refresh of folder items in UI elements.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>sender</code> <code>str</code> <p>Who requested folder items.</p> <code>None</code> <p>Returns:</p> Type Description <code>list[FolderItem]</code> <p>list[FolderItem]: Minimum possible information needed for visualisation of folder hierarchy.</p> Source code in <code>client/ayon_core/tools/launcher/abstract.py</code> <pre><code>@abstractmethod\ndef get_folder_items(\n    self, project_name: str, sender: Optional[str] = None\n) -&gt; list[FolderItem]:\n    \"\"\"Folder items to visualize project hierarchy.\n\n    This function may trigger events 'folders.refresh.started' and\n    'folders.refresh.finished' which will contain 'sender' value in data.\n    That may help to avoid re-refresh of folder items in UI elements.\n\n    Args:\n        project_name (str): Project name.\n        sender (str): Who requested folder items.\n\n    Returns:\n        list[FolderItem]: Minimum possible information needed\n            for visualisation of folder hierarchy.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/abstract.html#client.ayon_core.tools.launcher.abstract.AbstractLauncherFrontEnd.get_folder_type_items","title":"<code>get_folder_type_items(project_name, sender=None)</code>  <code>abstractmethod</code>","text":"<p>Folder type items for a project.</p> <p>This function may trigger events with topics 'projects.folder_types.refresh.started' and 'projects.folder_types.refresh.finished' which will contain 'sender' value in data. That may help to avoid re-refresh of items in UI elements.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>sender</code> <code>str</code> <p>Who requested folder type items.</p> <code>None</code> <p>Returns:</p> Type Description <code>list[FolderTypeItem]</code> <p>list[FolderTypeItem]: Folder type information.</p> Source code in <code>client/ayon_core/tools/launcher/abstract.py</code> <pre><code>@abstractmethod\ndef get_folder_type_items(\n    self, project_name: str, sender: Optional[str] = None\n) -&gt; list[FolderTypeItem]:\n    \"\"\"Folder type items for a project.\n\n    This function may trigger events with topics\n    'projects.folder_types.refresh.started' and\n    'projects.folder_types.refresh.finished' which will contain 'sender'\n    value in data.\n    That may help to avoid re-refresh of items in UI elements.\n\n    Args:\n        project_name (str): Project name.\n        sender (str): Who requested folder type items.\n\n    Returns:\n        list[FolderTypeItem]: Folder type information.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/abstract.html#client.ayon_core.tools.launcher.abstract.AbstractLauncherFrontEnd.get_grouped_host_names","title":"<code>get_grouped_host_names()</code>  <code>abstractmethod</code>","text":"<p>Get list of host names that will group workfiles.</p> Source code in <code>client/ayon_core/tools/launcher/abstract.py</code> <pre><code>@abstractmethod\ndef get_grouped_host_names(self) -&gt; list[str | None]:\n    \"\"\"Get list of host names that will group workfiles.\"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/abstract.html#client.ayon_core.tools.launcher.abstract.AbstractLauncherFrontEnd.get_my_tasks_entity_ids","title":"<code>get_my_tasks_entity_ids(project_name)</code>  <code>abstractmethod</code>","text":"<p>Get entity ids for my tasks.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <p>Returns:</p> Type Description <code>dict[str, list[str]]</code> <p>dict[str, list[str]]: Folder and task ids.</p> Source code in <code>client/ayon_core/tools/launcher/abstract.py</code> <pre><code>@abstractmethod\ndef get_my_tasks_entity_ids(\n    self, project_name: str\n) -&gt; dict[str, list[str]]:\n    \"\"\"Get entity ids for my tasks.\n\n    Args:\n        project_name (str): Project name.\n\n    Returns:\n        dict[str, list[str]]: Folder and task ids.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/abstract.html#client.ayon_core.tools.launcher.abstract.AbstractLauncherFrontEnd.get_project_items","title":"<code>get_project_items(sender=None)</code>  <code>abstractmethod</code>","text":"<p>Project items for all projects.</p> <p>This function may trigger events 'projects.refresh.started' and 'projects.refresh.finished' which will contain 'sender' value in data. That may help to avoid re-refresh of project items in UI elements.</p> <p>Parameters:</p> Name Type Description Default <code>sender</code> <code>str</code> <p>Who requested folder items.</p> <code>None</code> <p>Returns:</p> Type Description <code>list[ProjectItem]</code> <p>list[ProjectItem]: Minimum possible information needed for visualisation of folder hierarchy.</p> Source code in <code>client/ayon_core/tools/launcher/abstract.py</code> <pre><code>@abstractmethod\ndef get_project_items(\n    self, sender: Optional[str] = None\n) -&gt; list[ProjectItem]:\n    \"\"\"Project items for all projects.\n\n    This function may trigger events 'projects.refresh.started' and\n    'projects.refresh.finished' which will contain 'sender' value in data.\n    That may help to avoid re-refresh of project items in UI elements.\n\n    Args:\n        sender (str): Who requested folder items.\n\n    Returns:\n        list[ProjectItem]: Minimum possible information needed\n            for visualisation of folder hierarchy.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/abstract.html#client.ayon_core.tools.launcher.abstract.AbstractLauncherFrontEnd.get_selected_context","title":"<code>get_selected_context()</code>  <code>abstractmethod</code>","text":"<p>Get whole selected context.</p> Example <p>{     \"project_name\": self.get_selected_project_name(),     \"folder_id\": self.get_selected_folder_id(),     \"task_id\": self.get_selected_task_id(),     \"task_name\": self.get_selected_task_name(), }</p> <p>Returns:</p> Type Description <code>dict[str, Optional[str]]</code> <p>dict[str, Optional[str]]: Selected context.</p> Source code in <code>client/ayon_core/tools/launcher/abstract.py</code> <pre><code>@abstractmethod\ndef get_selected_context(self) -&gt; dict[str, Optional[str]]:\n    \"\"\"Get whole selected context.\n\n    Example:\n        {\n            \"project_name\": self.get_selected_project_name(),\n            \"folder_id\": self.get_selected_folder_id(),\n            \"task_id\": self.get_selected_task_id(),\n            \"task_name\": self.get_selected_task_name(),\n        }\n\n    Returns:\n        dict[str, Optional[str]]: Selected context.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/abstract.html#client.ayon_core.tools.launcher.abstract.AbstractLauncherFrontEnd.get_selected_folder_id","title":"<code>get_selected_folder_id()</code>  <code>abstractmethod</code>","text":"<p>Selected folder id.</p> <p>Returns:</p> Type Description <code>Optional[str]</code> <p>Optional[str]: Selected folder id.</p> Source code in <code>client/ayon_core/tools/launcher/abstract.py</code> <pre><code>@abstractmethod\ndef get_selected_folder_id(self) -&gt; Optional[str]:\n    \"\"\"Selected folder id.\n\n    Returns:\n        Optional[str]: Selected folder id.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/abstract.html#client.ayon_core.tools.launcher.abstract.AbstractLauncherFrontEnd.get_selected_project_name","title":"<code>get_selected_project_name()</code>  <code>abstractmethod</code>","text":"<p>Selected project name.</p> <p>Returns:</p> Type Description <code>Optional[str]</code> <p>Optional[str]: Selected project name.</p> Source code in <code>client/ayon_core/tools/launcher/abstract.py</code> <pre><code>@abstractmethod\ndef get_selected_project_name(self) -&gt; Optional[str]:\n    \"\"\"Selected project name.\n\n    Returns:\n        Optional[str]: Selected project name.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/abstract.html#client.ayon_core.tools.launcher.abstract.AbstractLauncherFrontEnd.get_selected_task_id","title":"<code>get_selected_task_id()</code>  <code>abstractmethod</code>","text":"<p>Selected task id.</p> <p>Returns:</p> Type Description <code>Optional[str]</code> <p>Optional[str]: Selected task id.</p> Source code in <code>client/ayon_core/tools/launcher/abstract.py</code> <pre><code>@abstractmethod\ndef get_selected_task_id(self) -&gt; Optional[str]:\n    \"\"\"Selected task id.\n\n    Returns:\n        Optional[str]: Selected task id.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/abstract.html#client.ayon_core.tools.launcher.abstract.AbstractLauncherFrontEnd.get_selected_task_name","title":"<code>get_selected_task_name()</code>  <code>abstractmethod</code>","text":"<p>Selected task name.</p> <p>Returns:</p> Type Description <code>Optional[str]</code> <p>Optional[str]: Selected task name.</p> Source code in <code>client/ayon_core/tools/launcher/abstract.py</code> <pre><code>@abstractmethod\ndef get_selected_task_name(self) -&gt; Optional[str]:\n    \"\"\"Selected task name.\n\n    Returns:\n        Optional[str]: Selected task name.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/abstract.html#client.ayon_core.tools.launcher.abstract.AbstractLauncherFrontEnd.get_task_items","title":"<code>get_task_items(project_name, folder_id, sender=None)</code>  <code>abstractmethod</code>","text":"<p>Task items.</p> <p>This function may trigger events 'tasks.refresh.started' and 'tasks.refresh.finished' which will contain 'sender' value in data. That may help to avoid re-refresh of task items in UI elements.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>folder_id</code> <code>str</code> <p>Folder ID for which are tasks requested.</p> required <code>sender</code> <code>str</code> <p>Who requested folder items.</p> <code>None</code> <p>Returns:</p> Type Description <code>list[TaskItem]</code> <p>list[TaskItem]: Minimum possible information needed for visualisation of tasks.</p> Source code in <code>client/ayon_core/tools/launcher/abstract.py</code> <pre><code>@abstractmethod\ndef get_task_items(\n    self, project_name: str, folder_id: str, sender: Optional[str] = None\n) -&gt; list[TaskItem]:\n    \"\"\"Task items.\n\n    This function may trigger events 'tasks.refresh.started' and\n    'tasks.refresh.finished' which will contain 'sender' value in data.\n    That may help to avoid re-refresh of task items in UI elements.\n\n    Args:\n        project_name (str): Project name.\n        folder_id (str): Folder ID for which are tasks requested.\n        sender (str): Who requested folder items.\n\n    Returns:\n        list[TaskItem]: Minimum possible information needed\n            for visualisation of tasks.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/abstract.html#client.ayon_core.tools.launcher.abstract.AbstractLauncherFrontEnd.get_task_type_items","title":"<code>get_task_type_items(project_name, sender=None)</code>  <code>abstractmethod</code>","text":"<p>Task type items for a project.</p> <p>This function may trigger events with topics 'projects.task_types.refresh.started' and 'projects.task_types.refresh.finished' which will contain 'sender' value in data. That may help to avoid re-refresh of items in UI elements.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>sender</code> <code>str</code> <p>Who requested task type items.</p> <code>None</code> <p>Returns:</p> Type Description <code>list[TaskTypeItem]</code> <p>list[TaskTypeItem]: Task type information.</p> Source code in <code>client/ayon_core/tools/launcher/abstract.py</code> <pre><code>@abstractmethod\ndef get_task_type_items(\n    self, project_name: str, sender: Optional[str] = None\n) -&gt; list[TaskTypeItem]:\n    \"\"\"Task type items for a project.\n\n    This function may trigger events with topics\n    'projects.task_types.refresh.started' and\n    'projects.task_types.refresh.finished' which will contain 'sender'\n    value in data.\n    That may help to avoid re-refresh of items in UI elements.\n\n    Args:\n        project_name (str): Project name.\n        sender (str): Who requested task type items.\n\n    Returns:\n        list[TaskTypeItem]: Task type information.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/abstract.html#client.ayon_core.tools.launcher.abstract.AbstractLauncherFrontEnd.get_workfile_items","title":"<code>get_workfile_items(project_name, task_id)</code>  <code>abstractmethod</code>","text":"<p>Get workfile items for a given context.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>Optional[str]</code> <p>Project name.</p> required <code>task_id</code> <code>Optional[str]</code> <p>Task id.</p> required <p>Returns:</p> Type Description <code>list[WorkfileItem]</code> <p>list[WorkfileItem]: List of workfile items.</p> Source code in <code>client/ayon_core/tools/launcher/abstract.py</code> <pre><code>@abstractmethod\ndef get_workfile_items(\n    self,\n    project_name: Optional[str],\n    task_id: Optional[str],\n) -&gt; list[WorkfileItem]:\n    \"\"\"Get workfile items for a given context.\n\n    Args:\n        project_name (Optional[str]): Project name.\n        task_id (Optional[str]): Task id.\n\n    Returns:\n        list[WorkfileItem]: List of workfile items.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/abstract.html#client.ayon_core.tools.launcher.abstract.AbstractLauncherFrontEnd.refresh","title":"<code>refresh()</code>  <code>abstractmethod</code>","text":"<p>Refresh everything, models, ui etc.</p> <p>Triggers 'controller.refresh.started' event at the beginning and 'controller.refresh.finished' at the end.</p> Source code in <code>client/ayon_core/tools/launcher/abstract.py</code> <pre><code>@abstractmethod\ndef refresh(self):\n    \"\"\"Refresh everything, models, ui etc.\n\n    Triggers 'controller.refresh.started' event at the beginning and\n    'controller.refresh.finished' at the end.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/abstract.html#client.ayon_core.tools.launcher.abstract.AbstractLauncherFrontEnd.refresh_actions","title":"<code>refresh_actions()</code>  <code>abstractmethod</code>","text":"<p>Refresh actions and all related data.</p> <p>Triggers 'controller.refresh.actions.started' event at the beginning and 'controller.refresh.actions.finished' at the end.</p> Source code in <code>client/ayon_core/tools/launcher/abstract.py</code> <pre><code>@abstractmethod\ndef refresh_actions(self):\n    \"\"\"Refresh actions and all related data.\n\n    Triggers 'controller.refresh.actions.started' event at the beginning\n    and 'controller.refresh.actions.finished' at the end.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/abstract.html#client.ayon_core.tools.launcher.abstract.AbstractLauncherFrontEnd.set_action_config_values","title":"<code>set_action_config_values(context, values)</code>  <code>abstractmethod</code>","text":"<p>Set action config values.</p> <p>Parameters:</p> Name Type Description Default <code>context</code> <code>WebactionContext</code> <p>Webaction context.</p> required <code>values</code> <code>dict[str, Any]</code> <p>Action config values.</p> required Source code in <code>client/ayon_core/tools/launcher/abstract.py</code> <pre><code>@abstractmethod\ndef set_action_config_values(\n    self,\n    context: WebactionContext,\n    values: dict[str, Any],\n):\n    \"\"\"Set action config values.\n\n    Args:\n        context (WebactionContext): Webaction context.\n        values (dict[str, Any]): Action config values.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/abstract.html#client.ayon_core.tools.launcher.abstract.AbstractLauncherFrontEnd.set_grouped_host_names","title":"<code>set_grouped_host_names(host_names)</code>  <code>abstractmethod</code>","text":"<p>Set list of host names that will group workfiles.</p> Source code in <code>client/ayon_core/tools/launcher/abstract.py</code> <pre><code>@abstractmethod\ndef set_grouped_host_names(self, host_names: list[str | None]):\n    \"\"\"Set list of host names that will group workfiles.\"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/abstract.html#client.ayon_core.tools.launcher.abstract.AbstractLauncherFrontEnd.set_selected_folder","title":"<code>set_selected_folder(folder_id)</code>  <code>abstractmethod</code>","text":"<p>Change selected folder.</p> <p>Parameters:</p> Name Type Description Default <code>folder_id</code> <code>Optional[str]</code> <p>Folder id or None if no folder is selected.</p> required Source code in <code>client/ayon_core/tools/launcher/abstract.py</code> <pre><code>@abstractmethod\ndef set_selected_folder(self, folder_id: Optional[str]):\n    \"\"\"Change selected folder.\n\n    Args:\n        folder_id (Optional[str]): Folder id or None if no folder\n            is selected.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/abstract.html#client.ayon_core.tools.launcher.abstract.AbstractLauncherFrontEnd.set_selected_project","title":"<code>set_selected_project(project_name)</code>  <code>abstractmethod</code>","text":"<p>Change selected folder.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>Optional[str]</code> <p>Project nameor None if no project is selected.</p> required Source code in <code>client/ayon_core/tools/launcher/abstract.py</code> <pre><code>@abstractmethod\ndef set_selected_project(self, project_name: Optional[str]):\n    \"\"\"Change selected folder.\n\n    Args:\n        project_name (Optional[str]): Project nameor None if no project\n            is selected.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/abstract.html#client.ayon_core.tools.launcher.abstract.AbstractLauncherFrontEnd.set_selected_task","title":"<code>set_selected_task(task_id, task_name)</code>  <code>abstractmethod</code>","text":"<p>Change selected task.</p> <p>Parameters:</p> Name Type Description Default <code>task_id</code> <code>Optional[str]</code> <p>Task id or None if no task is selected.</p> required <code>task_name</code> <code>Optional[str]</code> <p>Task name or None if no task is selected.</p> required Source code in <code>client/ayon_core/tools/launcher/abstract.py</code> <pre><code>@abstractmethod\ndef set_selected_task(\n    self, task_id: Optional[str], task_name: Optional[str]\n):\n    \"\"\"Change selected task.\n\n    Args:\n        task_id (Optional[str]): Task id or None if no task\n            is selected.\n        task_name (Optional[str]): Task name or None if no task\n            is selected.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/abstract.html#client.ayon_core.tools.launcher.abstract.AbstractLauncherFrontEnd.set_selected_workfile","title":"<code>set_selected_workfile(workfile_id)</code>  <code>abstractmethod</code>","text":"<p>Change selected workfile.</p> <p>Parameters:</p> Name Type Description Default <code>workfile_id</code> <code>Optional[str]</code> <p>Workfile id or None.</p> required Source code in <code>client/ayon_core/tools/launcher/abstract.py</code> <pre><code>@abstractmethod\ndef set_selected_workfile(self, workfile_id: Optional[str]):\n    \"\"\"Change selected workfile.\n\n    Args:\n        workfile_id (Optional[str]): Workfile id or None.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/abstract.html#client.ayon_core.tools.launcher.abstract.AbstractLauncherFrontEnd.trigger_action","title":"<code>trigger_action(action_id, project_name, folder_id, task_id, workfile_id)</code>  <code>abstractmethod</code>","text":"<p>Trigger action on given context.</p> <p>Parameters:</p> Name Type Description Default <code>action_id</code> <code>str</code> <p>Action identifier.</p> required <code>project_name</code> <code>Optional[str]</code> <p>Project name.</p> required <code>folder_id</code> <code>Optional[str]</code> <p>Folder id.</p> required <code>task_id</code> <code>Optional[str]</code> <p>Task id.</p> required <code>workfile_id</code> <code>Optional[str]</code> <p>Task id.</p> required Source code in <code>client/ayon_core/tools/launcher/abstract.py</code> <pre><code>@abstractmethod\ndef trigger_action(\n    self,\n    action_id: str,\n    project_name: Optional[str],\n    folder_id: Optional[str],\n    task_id: Optional[str],\n    workfile_id: Optional[str],\n):\n    \"\"\"Trigger action on given context.\n\n    Args:\n        action_id (str): Action identifier.\n        project_name (Optional[str]): Project name.\n        folder_id (Optional[str]): Folder id.\n        task_id (Optional[str]): Task id.\n        workfile_id (Optional[str]): Task id.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/abstract.html#client.ayon_core.tools.launcher.abstract.AbstractLauncherFrontEnd.trigger_webaction","title":"<code>trigger_webaction(context, action_label, form_data=None)</code>  <code>abstractmethod</code>","text":"<p>Trigger action on the given context.</p> <p>Parameters:</p> Name Type Description Default <code>context</code> <code>WebactionContext</code> <p>Webaction context.</p> required <code>action_label</code> <code>str</code> <p>Action label.</p> required <code>form_data</code> <code>Optional[dict[str, Any]]</code> <p>Form values of action.</p> <code>None</code> Source code in <code>client/ayon_core/tools/launcher/abstract.py</code> <pre><code>@abstractmethod\ndef trigger_webaction(\n    self,\n    context: WebactionContext,\n    action_label: str,\n    form_data: Optional[dict[str, Any]] = None,\n):\n    \"\"\"Trigger action on the given context.\n\n    Args:\n        context (WebactionContext): Webaction context.\n        action_label (str): Action label.\n        form_data (Optional[dict[str, Any]]): Form values of action.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/abstract.html#client.ayon_core.tools.launcher.abstract.ActionItem","title":"<code>ActionItem</code>  <code>dataclass</code>","text":"<p>Item representing single action to trigger.</p> <p>Attributes:</p> Name Type Description <code>action_type</code> <code>Literal['webaction', 'local']</code> <p>Type of action.</p> <code>identifier</code> <code>str</code> <p>Unique identifier of action item.</p> <code>order</code> <code>int</code> <p>Action ordering.</p> <code>suborder</code> <code>int</code> <p>Internal ordering used for webactions order.</p> <code>label</code> <code>str</code> <p>Action label.</p> <code>variant_label</code> <code>Optional[str]</code> <p>Variant label, full label is concatenated with space. Actions are grouped under single action if it has same 'label' and have set 'variant_label'.</p> <code>full_label</code> <code>str</code> <p>Full label, if not set it is generated from 'label' and 'variant_label'.</p> <code>icon</code> <code>dict[str, str]</code> <p>Icon definition.</p> <code>addon_name</code> <code>Optional[str]</code> <p>Addon name.</p> <code>addon_version</code> <code>Optional[str]</code> <p>Addon version.</p> <code>config_fields</code> <code>list[dict]</code> <p>Config fields for webaction.</p> Source code in <code>client/ayon_core/tools/launcher/abstract.py</code> <pre><code>@dataclass\nclass ActionItem:\n    \"\"\"Item representing single action to trigger.\n\n    Attributes:\n        action_type (Literal[\"webaction\", \"local\"]): Type of action.\n        identifier (str): Unique identifier of action item.\n        order (int): Action ordering.\n        suborder (int): Internal ordering used for webactions order.\n        label (str): Action label.\n        variant_label (Optional[str]): Variant label, full label is\n            concatenated with space. Actions are grouped under single\n            action if it has same 'label' and have set 'variant_label'.\n        full_label (str): Full label, if not set it is generated\n            from 'label' and 'variant_label'.\n        icon (dict[str, str]): Icon definition.\n        addon_name (Optional[str]): Addon name.\n        addon_version (Optional[str]): Addon version.\n        config_fields (list[dict]): Config fields for webaction.\n\n    \"\"\"\n    action_type: str\n    identifier: str\n    order: int\n    suborder: int\n    label: str\n    variant_label: Optional[str]\n    full_label: str\n    icon: Optional[dict[str, str]]\n    config_fields: list[dict]\n    addon_name: Optional[str] = None\n    addon_version: Optional[str] = None\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/abstract.html#client.ayon_core.tools.launcher.abstract.WebactionContext","title":"<code>WebactionContext</code>  <code>dataclass</code>","text":"<p>Context used for methods related to webactions.</p> Source code in <code>client/ayon_core/tools/launcher/abstract.py</code> <pre><code>@dataclass\nclass WebactionContext:\n    \"\"\"Context used for methods related to webactions.\"\"\"\n    identifier: str\n    project_name: str\n    folder_id: str\n    task_id: str\n    workfile_id: str\n    addon_name: str\n    addon_version: str\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/control.html","title":"control","text":""},{"location":"autoapi/client/ayon_core/tools/launcher/control.html#client.ayon_core.tools.launcher.control.BaseLauncherController","title":"<code>BaseLauncherController</code>","text":"<p>               Bases: <code>AbstractLauncherFrontEnd</code>, <code>AbstractLauncherBackend</code></p> Source code in <code>client/ayon_core/tools/launcher/control.py</code> <pre><code>class BaseLauncherController(\n    AbstractLauncherFrontEnd, AbstractLauncherBackend\n):\n    def __init__(self):\n        self._event_system = None\n        self._log = None\n\n        self._launcher_registry = JSONSettingRegistry(\n            \"launcher\",\n            get_launcher_local_dir(\"tools\")\n        )\n\n        self._addons_manager = None\n\n        self._settings_model = SettingsModel()\n        self._selection_model = LauncherSelectionModel(self)\n        self._projects_model = ProjectsModel(self)\n        self._hierarchy_model = HierarchyModel(self)\n        self._actions_model = ActionsModel(self)\n        self._workfiles_model = WorkfilesModel(self)\n        self._users_model = UsersModel(self)\n\n    @property\n    def log(self):\n        if self._log is None:\n            self._log = Logger.get_logger(self.__class__.__name__)\n        return self._log\n\n    @property\n    def event_system(self):\n        \"\"\"Inner event system for launcher tool controller.\n\n        Is used for communication with UI. Event system is created on demand.\n\n        Returns:\n            QueuedEventSystem: Event system which can trigger callbacks\n                for topics.\n        \"\"\"\n\n        if self._event_system is None:\n            self._event_system = QueuedEventSystem()\n        return self._event_system\n\n    # ---------------------------------\n    # Implementation of abstract methods\n    # ---------------------------------\n    # Events system\n    def emit_event(self, topic, data=None, source=None):\n        \"\"\"Use implemented event system to trigger event.\"\"\"\n\n        if data is None:\n            data = {}\n        self.event_system.emit(topic, data, source)\n\n    def register_event_callback(self, topic, callback):\n        self.event_system.add_callback(topic, callback)\n\n    def get_addons_manager(self) -&gt; AddonsManager:\n        if self._addons_manager is None:\n            self._addons_manager = AddonsManager()\n        return self._addons_manager\n\n    def get_grouped_host_names(self) -&gt; list[str | None]:\n        try:\n            value = self._launcher_registry.get_item(\n                \"grouped_hosts\", default=\"[]\"\n            )\n            return json.loads(value)\n        except Exception:\n            # NOTE This is future-guarding in case we'd change the stored data\n            self.log.warning(\"Failed to get grouped hosts\", exc_info=True)\n            return []\n\n    def set_grouped_host_names(self, host_names: list[str | None]):\n        value = json.dumps(host_names)\n        self._launcher_registry.set_item(\"grouped_hosts\", value)\n\n    # Entity items for UI\n    def get_project_items(self, sender=None):\n        return self._projects_model.get_project_items(sender)\n\n    def get_folder_type_items(self, project_name, sender=None):\n        return self._projects_model.get_folder_type_items(\n            project_name, sender\n        )\n\n    def get_task_type_items(self, project_name, sender=None):\n        return self._projects_model.get_task_type_items(\n            project_name, sender\n        )\n\n    def get_folder_items(self, project_name, sender=None):\n        return self._hierarchy_model.get_folder_items(project_name, sender)\n\n    def get_task_items(self, project_name, folder_id, sender=None):\n        return self._hierarchy_model.get_task_items(\n            project_name, folder_id, sender\n        )\n\n    # Project settings for applications actions\n    def get_project_settings(self, project_name):\n        return self._settings_model.get_settings(project_name)\n\n    # Entity for backend\n    def get_project_entity(self, project_name):\n        return self._projects_model.get_project_entity(project_name)\n\n    def get_folder_entity(self, project_name, folder_id):\n        return self._hierarchy_model.get_folder_entity(\n            project_name, folder_id)\n\n    def get_task_entity(self, project_name, task_id):\n        return self._hierarchy_model.get_task_entity(project_name, task_id)\n\n    # Selection methods\n    def get_selected_project_name(self):\n        return self._selection_model.get_selected_project_name()\n\n    def set_selected_project(self, project_name):\n        self._selection_model.set_selected_project(project_name)\n\n    def get_selected_folder_id(self):\n        return self._selection_model.get_selected_folder_id()\n\n    def set_selected_folder(self, folder_id):\n        self._selection_model.set_selected_folder(folder_id)\n\n    def get_selected_task_id(self):\n        return self._selection_model.get_selected_task_id()\n\n    def get_selected_task_name(self):\n        return self._selection_model.get_selected_task_name()\n\n    def set_selected_task(self, task_id, task_name):\n        self._selection_model.set_selected_task(task_id, task_name)\n\n    def set_selected_workfile(self, workfile_id):\n        self._selection_model.set_selected_workfile(workfile_id)\n\n    def get_selected_context(self):\n        return {\n            \"project_name\": self.get_selected_project_name(),\n            \"folder_id\": self.get_selected_folder_id(),\n            \"task_id\": self.get_selected_task_id(),\n            \"task_name\": self.get_selected_task_name(),\n        }\n\n    # Workfiles\n    def get_workfile_items(\n        self,\n        project_name: Optional[str],\n        task_id: Optional[str],\n    ) -&gt; list[WorkfileItem]:\n        return self._workfiles_model.get_workfile_items(\n            project_name,\n            task_id,\n        )\n\n    # Actions\n    def get_action_items(\n        self, project_name, folder_id, task_id, workfile_id\n    ):\n        return self._actions_model.get_action_items(\n            project_name, folder_id, task_id, workfile_id\n        )\n\n    def trigger_action(\n        self,\n        identifier,\n        project_name,\n        folder_id,\n        task_id,\n        workfile_id,\n    ):\n        self._actions_model.trigger_action(\n            identifier,\n            project_name,\n            folder_id,\n            task_id,\n            workfile_id,\n        )\n\n    def trigger_webaction(self, context, action_label, form_data=None):\n        self._actions_model.trigger_webaction(\n            context, action_label, form_data\n        )\n\n    def get_action_config_values(self, context):\n        return self._actions_model.get_action_config_values(context)\n\n    def set_action_config_values(self, context, values):\n        return self._actions_model.set_action_config_values(context, values)\n\n    # General methods\n    def refresh(self):\n        self._emit_event(\"controller.refresh.started\")\n\n        self._settings_model.reset()\n        self._projects_model.reset()\n        self._hierarchy_model.reset()\n        self._users_model.reset()\n\n        self._actions_model.refresh()\n        self._projects_model.refresh()\n\n        self._emit_event(\"controller.refresh.finished\")\n\n    def refresh_actions(self):\n        self._emit_event(\"controller.refresh.actions.started\")\n\n        # Refresh project settings (used for actions discovery)\n        self._settings_model.reset()\n        # Refresh projects - they define applications\n        self._projects_model.reset()\n        # Refresh actions\n        self._actions_model.refresh()\n        # Reset workfiles model\n        self._workfiles_model.reset()\n\n        self._emit_event(\"controller.refresh.actions.finished\")\n\n    def get_my_tasks_entity_ids(\n        self, project_name: str\n    ) -&gt; dict[str, list[str]]:\n        username = self._users_model.get_current_username()\n        assignees = []\n        if username:\n            assignees.append(username)\n        return self._hierarchy_model.get_entity_ids_for_assignees(\n            project_name, assignees\n        )\n\n    def _emit_event(self, topic, data=None):\n        self.emit_event(topic, data, \"controller\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/control.html#client.ayon_core.tools.launcher.control.BaseLauncherController.event_system","title":"<code>event_system</code>  <code>property</code>","text":"<p>Inner event system for launcher tool controller.</p> <p>Is used for communication with UI. Event system is created on demand.</p> <p>Returns:</p> Name Type Description <code>QueuedEventSystem</code> <p>Event system which can trigger callbacks for topics.</p>"},{"location":"autoapi/client/ayon_core/tools/launcher/control.html#client.ayon_core.tools.launcher.control.BaseLauncherController.emit_event","title":"<code>emit_event(topic, data=None, source=None)</code>","text":"<p>Use implemented event system to trigger event.</p> Source code in <code>client/ayon_core/tools/launcher/control.py</code> <pre><code>def emit_event(self, topic, data=None, source=None):\n    \"\"\"Use implemented event system to trigger event.\"\"\"\n\n    if data is None:\n        data = {}\n    self.event_system.emit(topic, data, source)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/models/index.html","title":"models","text":""},{"location":"autoapi/client/ayon_core/tools/launcher/models/index.html#client.ayon_core.tools.launcher.models.ActionsModel","title":"<code>ActionsModel</code>","text":"<p>Actions model.</p> <p>Parameters:</p> Name Type Description Default <code>controller</code> <code>AbstractLauncherBackend</code> <p>Controller instance.</p> required Source code in <code>client/ayon_core/tools/launcher/models/actions.py</code> <pre><code>class ActionsModel:\n    \"\"\"Actions model.\n\n    Args:\n        controller (AbstractLauncherBackend): Controller instance.\n    \"\"\"\n\n    def __init__(self, controller):\n        self._controller = controller\n\n        self._log = None\n\n        self._discovered_actions = None\n        self._actions = None\n        self._action_items = {}\n        self._webaction_items = NestedCacheItem(\n            levels=2, default_factory=list, lifetime=20,\n        )\n\n        self._variant = get_settings_variant()\n\n    @staticmethod\n    def calculate_full_label(label: str, variant_label: Optional[str]) -&gt; str:\n        \"\"\"Calculate full label from label and variant_label.\"\"\"\n        if variant_label:\n            return \" \".join([label, variant_label])\n        return label\n\n    @property\n    def log(self):\n        if self._log is None:\n            self._log = Logger.get_logger(self.__class__.__name__)\n        return self._log\n\n    def refresh(self):\n        self._discovered_actions = None\n        self._actions = None\n        self._action_items = {}\n        self._webaction_items.reset()\n\n        self._controller.emit_event(\"actions.refresh.started\")\n        self._get_action_objects()\n        self._controller.emit_event(\"actions.refresh.finished\")\n\n    def get_action_items(\n        self,\n        project_name: Optional[str],\n        folder_id: Optional[str],\n        task_id: Optional[str],\n        workfile_id: Optional[str],\n    ) -&gt; list[ActionItem]:\n        \"\"\"Get actions for project.\n\n        Args:\n            project_name (Optional[str]): Project name.\n            folder_id (Optional[str]): Folder id.\n            task_id (Optional[str]): Task id.\n            workfile_id (Optional[str]): Workfile id.\n\n        Returns:\n            list[ActionItem]: List of actions.\n\n        \"\"\"\n        selection = self._prepare_selection(\n            project_name, folder_id, task_id, workfile_id\n        )\n        output = []\n        action_items = self._get_action_items(project_name)\n        for identifier, action in self._get_action_objects().items():\n            if action.is_compatible(selection):\n                output.append(action_items[identifier])\n        output.extend(self._get_webactions(selection))\n\n        return output\n\n    def trigger_action(\n        self,\n        identifier,\n        project_name,\n        folder_id,\n        task_id,\n        workfile_id,\n    ):\n        selection = self._prepare_selection(\n            project_name, folder_id, task_id, workfile_id\n        )\n        failed = False\n        error_message = None\n        action_label = identifier\n        action_items = self._get_action_items(project_name)\n        trigger_id = uuid.uuid4().hex\n        try:\n            action = self._actions[identifier]\n            action_item = action_items[identifier]\n            action_label = action_item.full_label\n            self._controller.emit_event(\n                \"action.trigger.started\",\n                {\n                    \"trigger_id\": trigger_id,\n                    \"identifier\": identifier,\n                    \"full_label\": action_label,\n                }\n            )\n\n            action.process(selection)\n        except Exception as exc:\n            self.log.warning(\"Action trigger failed.\", exc_info=True)\n            failed = True\n            error_message = str(exc)\n\n        self._controller.emit_event(\n            \"action.trigger.finished\",\n            {\n                \"trigger_id\": trigger_id,\n                \"identifier\": identifier,\n                \"failed\": failed,\n                \"error_message\": error_message,\n                \"full_label\": action_label,\n            }\n        )\n\n    def trigger_webaction(self, context, action_label, form_data):\n        entity_type = None\n        entity_ids = []\n        identifier = context.identifier\n        folder_id = context.folder_id\n        task_id = context.task_id\n        workfile_id = context.workfile_id\n        project_name = context.project_name\n        addon_name = context.addon_name\n        addon_version = context.addon_version\n\n        if workfile_id:\n            entity_type = \"workfile\"\n            entity_ids.append(workfile_id)\n        elif task_id:\n            entity_type = \"task\"\n            entity_ids.append(task_id)\n        elif folder_id:\n            entity_type = \"folder\"\n            entity_ids.append(folder_id)\n\n        query = {\n            \"addonName\": addon_name,\n            \"addonVersion\": addon_version,\n            \"identifier\": identifier,\n            \"variant\": self._variant,\n        }\n        url = f\"actions/execute?{urlencode(query)}\"\n        request_data = {\n            \"projectName\": project_name,\n            \"entityType\": entity_type,\n            \"entityIds\": entity_ids,\n        }\n        if form_data is not None:\n            request_data[\"formData\"] = form_data\n\n        trigger_id = uuid.uuid4().hex\n        failed = False\n        try:\n            self._controller.emit_event(\n                \"webaction.trigger.started\",\n                {\n                    \"trigger_id\": trigger_id,\n                    \"identifier\": identifier,\n                    \"full_label\": action_label,\n                }\n            )\n\n            response = ayon_api.raw_post(url, json=request_data)\n            response.raise_for_status()\n            handle_response = self._handle_webaction_response(response.data)\n\n        except Exception:\n            failed = True\n            self.log.warning(\"Action trigger failed.\", exc_info=True)\n            handle_response = WebactionResponse(\n                \"unknown\",\n                False,\n                error_message=\"Failed to trigger webaction.\",\n            )\n\n        data = asdict(handle_response)\n        data.update({\n            \"trigger_failed\": failed,\n            \"trigger_id\": trigger_id,\n            \"identifier\": identifier,\n            \"full_label\": action_label,\n            \"project_name\": project_name,\n            \"folder_id\": folder_id,\n            \"task_id\": task_id,\n            \"workfile_id\": workfile_id,\n            \"addon_name\": addon_name,\n            \"addon_version\": addon_version,\n        })\n        self._controller.emit_event(\n            \"webaction.trigger.finished\",\n            data,\n        )\n\n    def get_action_config_values(self, context: WebactionContext):\n        selection = self._prepare_selection(\n            context.project_name,\n            context.folder_id,\n            context.task_id,\n            context.workfile_id,\n        )\n        if not selection.is_project_selected:\n            return {}\n\n        request_data = self._get_webaction_request_data(selection)\n\n        query = {\n            \"addonName\": context.addon_name,\n            \"addonVersion\": context.addon_version,\n            \"identifier\": context.identifier,\n            \"variant\": self._variant,\n        }\n        url = f\"actions/config?{urlencode(query)}\"\n        try:\n            response = ayon_api.post(url, **request_data)\n            response.raise_for_status()\n        except Exception:\n            self.log.warning(\n                \"Failed to collect webaction config values.\",\n                exc_info=True\n            )\n            return {}\n        return response.data\n\n    def set_action_config_values(self, context, values):\n        selection = self._prepare_selection(\n            context.project_name,\n            context.folder_id,\n            context.task_id,\n            context.workfile_id,\n        )\n        if not selection.is_project_selected:\n            return {}\n\n        request_data = self._get_webaction_request_data(selection)\n        request_data[\"value\"] = values\n\n        query = {\n            \"addonName\": context.addon_name,\n            \"addonVersion\": context.addon_version,\n            \"identifier\": context.identifier,\n            \"variant\": self._variant,\n        }\n        url = f\"actions/config?{urlencode(query)}\"\n        try:\n            response = ayon_api.post(url, **request_data)\n            response.raise_for_status()\n        except Exception:\n            self.log.warning(\n                \"Failed to store webaction config values.\",\n                exc_info=True\n            )\n\n    def _prepare_selection(\n        self, project_name, folder_id, task_id, workfile_id\n    ):\n        project_entity = None\n        if project_name:\n            project_entity = self._controller.get_project_entity(project_name)\n        project_settings = self._controller.get_project_settings(project_name)\n        return LauncherActionSelection(\n            project_name,\n            folder_id,\n            task_id,\n            workfile_id,\n            project_entity=project_entity,\n            project_settings=project_settings,\n        )\n\n    def _get_webaction_request_data(self, selection: LauncherActionSelection):\n        entity_type = None\n        entity_id = None\n        entity_subtypes = []\n        if selection.is_workfile_selected:\n            entity_type = \"workfile\"\n            entity_id = selection.workfile_id\n            entity_subtypes = []\n\n        elif selection.is_task_selected:\n            entity_type = \"task\"\n            entity_id = selection.task_entity[\"id\"]\n            entity_subtypes = [selection.task_entity[\"taskType\"]]\n\n        elif selection.is_folder_selected:\n            entity_type = \"folder\"\n            entity_id = selection.folder_entity[\"id\"]\n            entity_subtypes = [selection.folder_entity[\"folderType\"]]\n\n        elif selection.is_project_selected:\n            # Project actions are supported since AYON 1.9.1\n            ma, mi, pa, _, _ = ayon_api.get_server_version_tuple()\n            if (ma, mi, pa) &lt; (1, 9, 1):\n                return None\n            entity_type = \"project\"\n\n        entity_ids = []\n        if entity_id:\n            entity_ids.append(entity_id)\n\n        project_name = selection.project_name\n        return {\n            \"projectName\": project_name,\n            \"entityType\": entity_type,\n            \"entitySubtypes\": entity_subtypes,\n            \"entityIds\": entity_ids,\n        }\n\n    def _get_webactions(self, selection: LauncherActionSelection):\n        request_data = self._get_webaction_request_data(selection)\n        if request_data is None:\n            return []\n\n        project_name = selection.project_name\n        entity_id = None\n        if request_data[\"entityIds\"]:\n            entity_id = request_data[\"entityIds\"][0]\n\n        cache: CacheItem = self._webaction_items[project_name][entity_id]\n        if cache.is_valid:\n            return cache.get_data()\n\n        try:\n            # 'variant' query is supported since AYON backend 1.10.4\n            query = urlencode({\"variant\": self._variant, \"mode\": \"all\"})\n            response = ayon_api.post(\n                f\"actions/list?{query}\", **request_data\n            )\n            response.raise_for_status()\n        except Exception:\n            self.log.warning(\"Failed to collect webactions.\", exc_info=True)\n            return []\n\n        action_items = []\n        for idx, action in enumerate(response.data[\"actions\"]):\n            # NOTE Settings variant may be important for triggering?\n            # - action[\"variant\"]\n            icon = action.get(\"icon\")\n            if icon and icon[\"type\"] == \"url\":\n                if not urlparse(icon[\"url\"]).scheme:\n                    icon[\"type\"] = \"ayon_url\"\n\n            if icon:\n                try:\n                    icon = get_icon_def_from_data(icon)\n                except ValueError:\n                    icon = None\n\n            config_fields = action.get(\"configFields\") or []\n            variant_label = action[\"label\"]\n            group_label = action.get(\"groupLabel\")\n            if not group_label:\n                group_label = variant_label\n                variant_label = None\n\n            full_label = self.calculate_full_label(\n                group_label, variant_label\n            )\n            identifier = action[\"identifier\"]\n            order = action[\"order\"]\n            if order is None:\n                order = 0\n                self.log.warning(\n                    f\"Got webaction without order. Identifier: {identifier}\"\n                )\n\n            action_items.append(ActionItem(\n                action_type=\"webaction\",\n                identifier=action[\"identifier\"],\n                order=order,\n                suborder=idx,\n                label=group_label,\n                variant_label=variant_label,\n                full_label=full_label,\n                icon=icon,\n                addon_name=action[\"addonName\"],\n                addon_version=action[\"addonVersion\"],\n                config_fields=config_fields,\n                # category=action[\"category\"],\n            ))\n\n        cache.update_data(action_items)\n        return cache.get_data()\n\n    def _handle_webaction_response(self, data) -&gt; WebactionResponse:\n        response_type = data[\"type\"]\n        # Backwards compatibility -&gt; 'server' type is not available since\n        #   AYON backend 1.8.3\n        if response_type == \"server\":\n            return WebactionResponse(\n                response_type,\n                False,\n                error_message=\"Please use AYON web UI to run the action.\",\n            )\n\n        payload = data.get(\"payload\") or {}\n\n        download_uri = payload.get(\"extra_download\")\n        if download_uri is not None:\n            # Find out if is relative or absolute URL\n            if not urlparse(download_uri).scheme:\n                ayon_url = ayon_api.get_base_url().rstrip(\"/\")\n                path = download_uri.lstrip(\"/\")\n                download_uri = f\"{ayon_url}/{path}\"\n\n            # Use webbrowser to open file\n            webbrowser.open_new_tab(download_uri)\n\n        response = WebactionResponse(\n            response_type,\n            data[\"success\"],\n            data.get(\"message\"),\n            payload.get(\"extra_clipboard\"),\n        )\n        if response_type == \"simple\":\n            pass\n\n        elif response_type == \"redirect\":\n            # NOTE unused 'newTab' key because we always have to\n            #   open new tab from desktop app.\n            if not webbrowser.open_new_tab(payload[\"uri\"]):\n                payload.error_message = \"Failed to open web browser.\"\n\n        elif response_type == \"form\":\n            submit_icon = payload[\"submit_icon\"] or None\n            cancel_icon = payload[\"cancel_icon\"] or None\n            if submit_icon:\n                submit_icon = MaterialSymbolsIcon(submit_icon)\n\n            if cancel_icon:\n                cancel_icon = MaterialSymbolsIcon(cancel_icon)\n\n            response.form = WebactionForm(\n                fields=payload[\"fields\"],\n                title=payload[\"title\"],\n                submit_label=payload[\"submit_label\"],\n                cancel_label=payload[\"cancel_label\"],\n                submit_icon=submit_icon,\n                cancel_icon=cancel_icon,\n            )\n\n        elif response_type == \"launcher\":\n            # Run AYON launcher process with uri in arguments\n            # NOTE This does pass environment variables of current process\n            #   to the subprocess.\n            # NOTE We could 'take action' directly and use the arguments here\n            if payload is not None:\n                uri = payload[\"uri\"]\n            else:\n                uri = data[\"uri\"]\n\n            # Remove bundles from environment variables\n            env = os.environ.copy()\n            env.pop(\"AYON_BUNDLE_NAME\", None)\n            env.pop(\"AYON_STUDIO_BUNDLE_NAME\", None)\n            env = clean_envs_for_ayon_process(env)\n            run_detached_ayon_launcher_process(uri, env=env)\n\n        elif response_type in (\"query\", \"navigate\"):\n            response.error_message = (\n                \"Please use AYON web UI to run the action.\"\n            )\n\n        else:\n            self.log.warning(\n                f\"Unknown webaction response type '{response_type}'\"\n            )\n            response.error_message = \"Unknown webaction response type.\"\n\n        return response\n\n    def _get_discovered_action_classes(self):\n        if self._discovered_actions is None:\n            # NOTE We don't need to register the paths, but that would\n            #   require to change discovery logic and deprecate all functions\n            #   related to registering and discovering launcher actions.\n            addons_manager = self._controller.get_addons_manager()\n            actions_paths = addons_manager.collect_launcher_action_paths()\n            for path in actions_paths:\n                if path and os.path.exists(path):\n                    register_launcher_action_path(path)\n            self._discovered_actions = (\n                discover_launcher_actions()\n            )\n        return self._discovered_actions\n\n    def _get_action_objects(self):\n        if self._actions is None:\n            actions = {}\n            for cls in self._get_discovered_action_classes():\n                obj = cls()\n                identifier = getattr(obj, \"identifier\", None)\n                if identifier is None:\n                    identifier = cls.__name__\n                actions[identifier] = obj\n            self._actions = actions\n        return self._actions\n\n    def _get_action_items(self, project_name):\n        action_items = self._action_items.get(project_name)\n        if action_items is not None:\n            return action_items\n\n        project_entity = None\n        if project_name:\n            project_entity = self._controller.get_project_entity(project_name)\n        project_settings = self._controller.get_project_settings(project_name)\n\n        action_items = {}\n        for identifier, action in self._get_action_objects().items():\n            # Backwards compatibility from 0.3.3 (24/06/10)\n            # TODO: Remove in future releases\n            if hasattr(action, \"project_settings\"):\n                action.project_entities[project_name] = project_entity\n                action.project_settings[project_name] = project_settings\n\n            label = action.label or identifier\n            variant_label = getattr(action, \"label_variant\", None)\n            full_label = self.calculate_full_label(\n                label, variant_label\n            )\n            icon = get_action_icon(action)\n            order = action.order\n            # Make sure it is not 'None'\n            if order is None:\n                order = 0\n                self.log.warning(\n                    f\"Got action without order. Identifier: {identifier}\"\n                )\n\n            item = ActionItem(\n                action_type=\"local\",\n                identifier=identifier,\n                order=order,\n                suborder=0,\n                label=label,\n                variant_label=variant_label,\n                full_label=full_label,\n                icon=icon,\n                config_fields=[],\n            )\n            action_items[identifier] = item\n        self._action_items[project_name] = action_items\n        return action_items\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/models/index.html#client.ayon_core.tools.launcher.models.ActionsModel.calculate_full_label","title":"<code>calculate_full_label(label, variant_label)</code>  <code>staticmethod</code>","text":"<p>Calculate full label from label and variant_label.</p> Source code in <code>client/ayon_core/tools/launcher/models/actions.py</code> <pre><code>@staticmethod\ndef calculate_full_label(label: str, variant_label: Optional[str]) -&gt; str:\n    \"\"\"Calculate full label from label and variant_label.\"\"\"\n    if variant_label:\n        return \" \".join([label, variant_label])\n    return label\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/models/index.html#client.ayon_core.tools.launcher.models.ActionsModel.get_action_items","title":"<code>get_action_items(project_name, folder_id, task_id, workfile_id)</code>","text":"<p>Get actions for project.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>Optional[str]</code> <p>Project name.</p> required <code>folder_id</code> <code>Optional[str]</code> <p>Folder id.</p> required <code>task_id</code> <code>Optional[str]</code> <p>Task id.</p> required <code>workfile_id</code> <code>Optional[str]</code> <p>Workfile id.</p> required <p>Returns:</p> Type Description <code>list[ActionItem]</code> <p>list[ActionItem]: List of actions.</p> Source code in <code>client/ayon_core/tools/launcher/models/actions.py</code> <pre><code>def get_action_items(\n    self,\n    project_name: Optional[str],\n    folder_id: Optional[str],\n    task_id: Optional[str],\n    workfile_id: Optional[str],\n) -&gt; list[ActionItem]:\n    \"\"\"Get actions for project.\n\n    Args:\n        project_name (Optional[str]): Project name.\n        folder_id (Optional[str]): Folder id.\n        task_id (Optional[str]): Task id.\n        workfile_id (Optional[str]): Workfile id.\n\n    Returns:\n        list[ActionItem]: List of actions.\n\n    \"\"\"\n    selection = self._prepare_selection(\n        project_name, folder_id, task_id, workfile_id\n    )\n    output = []\n    action_items = self._get_action_items(project_name)\n    for identifier, action in self._get_action_objects().items():\n        if action.is_compatible(selection):\n            output.append(action_items[identifier])\n    output.extend(self._get_webactions(selection))\n\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/models/index.html#client.ayon_core.tools.launcher.models.LauncherSelectionModel","title":"<code>LauncherSelectionModel</code>","text":"<p>Model handling selection changes.</p> <p>Triggering events: - \"selection.project.changed\" - \"selection.folder.changed\" - \"selection.task.changed\" - \"selection.workfile.changed\"</p> Source code in <code>client/ayon_core/tools/launcher/models/selection.py</code> <pre><code>class LauncherSelectionModel:\n    \"\"\"Model handling selection changes.\n\n    Triggering events:\n    - \"selection.project.changed\"\n    - \"selection.folder.changed\"\n    - \"selection.task.changed\"\n    - \"selection.workfile.changed\"\n    \"\"\"\n\n    event_source = \"launcher.selection.model\"\n\n    def __init__(self, controller: AbstractLauncherBackend) -&gt; None:\n        self._controller = controller\n\n        self._project_name = None\n        self._folder_id = None\n        self._task_name = None\n        self._task_id = None\n        self._workfile_id = None\n\n    def get_selected_project_name(self) -&gt; Optional[str]:\n        return self._project_name\n\n    def set_selected_project(self, project_name: Optional[str]) -&gt; None:\n        if project_name == self._project_name:\n            return\n\n        self._project_name = project_name\n        self._controller.emit_event(\n            \"selection.project.changed\",\n            {\"project_name\": project_name},\n            self.event_source\n        )\n\n    def get_selected_folder_id(self) -&gt; Optional[str]:\n        return self._folder_id\n\n    def set_selected_folder(self, folder_id: Optional[str]) -&gt; None:\n        if folder_id == self._folder_id:\n            return\n\n        self._folder_id = folder_id\n        self._controller.emit_event(\n            \"selection.folder.changed\",\n            {\n                \"project_name\": self._project_name,\n                \"folder_id\": folder_id,\n            },\n            self.event_source\n        )\n\n    def get_selected_task_name(self) -&gt; Optional[str]:\n        return self._task_name\n\n    def get_selected_task_id(self) -&gt; Optional[str]:\n        return self._task_id\n\n    def set_selected_task(\n        self, task_id: Optional[str], task_name: Optional[str]\n    ) -&gt; None:\n        if task_id == self._task_id:\n            return\n\n        self._task_name = task_name\n        self._task_id = task_id\n        self._controller.emit_event(\n            \"selection.task.changed\",\n            {\n                \"project_name\": self._project_name,\n                \"folder_id\": self._folder_id,\n                \"task_name\": task_name,\n                \"task_id\": task_id,\n            },\n            self.event_source\n        )\n\n    def get_selected_workfile(self) -&gt; Optional[str]:\n        return self._workfile_id\n\n    def set_selected_workfile(self, workfile_id: Optional[str]) -&gt; None:\n        if workfile_id == self._workfile_id:\n            return\n\n        self._workfile_id = workfile_id\n        self._controller.emit_event(\n            \"selection.workfile.changed\",\n            {\n                \"project_name\": self._project_name,\n                \"folder_id\": self._folder_id,\n                \"task_name\": self._task_name,\n                \"task_id\": self._task_id,\n                \"workfile_id\": workfile_id,\n            },\n            self.event_source\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/models/actions.html","title":"actions","text":""},{"location":"autoapi/client/ayon_core/tools/launcher/models/actions.html#client.ayon_core.tools.launcher.models.actions.ActionsModel","title":"<code>ActionsModel</code>","text":"<p>Actions model.</p> <p>Parameters:</p> Name Type Description Default <code>controller</code> <code>AbstractLauncherBackend</code> <p>Controller instance.</p> required Source code in <code>client/ayon_core/tools/launcher/models/actions.py</code> <pre><code>class ActionsModel:\n    \"\"\"Actions model.\n\n    Args:\n        controller (AbstractLauncherBackend): Controller instance.\n    \"\"\"\n\n    def __init__(self, controller):\n        self._controller = controller\n\n        self._log = None\n\n        self._discovered_actions = None\n        self._actions = None\n        self._action_items = {}\n        self._webaction_items = NestedCacheItem(\n            levels=2, default_factory=list, lifetime=20,\n        )\n\n        self._variant = get_settings_variant()\n\n    @staticmethod\n    def calculate_full_label(label: str, variant_label: Optional[str]) -&gt; str:\n        \"\"\"Calculate full label from label and variant_label.\"\"\"\n        if variant_label:\n            return \" \".join([label, variant_label])\n        return label\n\n    @property\n    def log(self):\n        if self._log is None:\n            self._log = Logger.get_logger(self.__class__.__name__)\n        return self._log\n\n    def refresh(self):\n        self._discovered_actions = None\n        self._actions = None\n        self._action_items = {}\n        self._webaction_items.reset()\n\n        self._controller.emit_event(\"actions.refresh.started\")\n        self._get_action_objects()\n        self._controller.emit_event(\"actions.refresh.finished\")\n\n    def get_action_items(\n        self,\n        project_name: Optional[str],\n        folder_id: Optional[str],\n        task_id: Optional[str],\n        workfile_id: Optional[str],\n    ) -&gt; list[ActionItem]:\n        \"\"\"Get actions for project.\n\n        Args:\n            project_name (Optional[str]): Project name.\n            folder_id (Optional[str]): Folder id.\n            task_id (Optional[str]): Task id.\n            workfile_id (Optional[str]): Workfile id.\n\n        Returns:\n            list[ActionItem]: List of actions.\n\n        \"\"\"\n        selection = self._prepare_selection(\n            project_name, folder_id, task_id, workfile_id\n        )\n        output = []\n        action_items = self._get_action_items(project_name)\n        for identifier, action in self._get_action_objects().items():\n            if action.is_compatible(selection):\n                output.append(action_items[identifier])\n        output.extend(self._get_webactions(selection))\n\n        return output\n\n    def trigger_action(\n        self,\n        identifier,\n        project_name,\n        folder_id,\n        task_id,\n        workfile_id,\n    ):\n        selection = self._prepare_selection(\n            project_name, folder_id, task_id, workfile_id\n        )\n        failed = False\n        error_message = None\n        action_label = identifier\n        action_items = self._get_action_items(project_name)\n        trigger_id = uuid.uuid4().hex\n        try:\n            action = self._actions[identifier]\n            action_item = action_items[identifier]\n            action_label = action_item.full_label\n            self._controller.emit_event(\n                \"action.trigger.started\",\n                {\n                    \"trigger_id\": trigger_id,\n                    \"identifier\": identifier,\n                    \"full_label\": action_label,\n                }\n            )\n\n            action.process(selection)\n        except Exception as exc:\n            self.log.warning(\"Action trigger failed.\", exc_info=True)\n            failed = True\n            error_message = str(exc)\n\n        self._controller.emit_event(\n            \"action.trigger.finished\",\n            {\n                \"trigger_id\": trigger_id,\n                \"identifier\": identifier,\n                \"failed\": failed,\n                \"error_message\": error_message,\n                \"full_label\": action_label,\n            }\n        )\n\n    def trigger_webaction(self, context, action_label, form_data):\n        entity_type = None\n        entity_ids = []\n        identifier = context.identifier\n        folder_id = context.folder_id\n        task_id = context.task_id\n        workfile_id = context.workfile_id\n        project_name = context.project_name\n        addon_name = context.addon_name\n        addon_version = context.addon_version\n\n        if workfile_id:\n            entity_type = \"workfile\"\n            entity_ids.append(workfile_id)\n        elif task_id:\n            entity_type = \"task\"\n            entity_ids.append(task_id)\n        elif folder_id:\n            entity_type = \"folder\"\n            entity_ids.append(folder_id)\n\n        query = {\n            \"addonName\": addon_name,\n            \"addonVersion\": addon_version,\n            \"identifier\": identifier,\n            \"variant\": self._variant,\n        }\n        url = f\"actions/execute?{urlencode(query)}\"\n        request_data = {\n            \"projectName\": project_name,\n            \"entityType\": entity_type,\n            \"entityIds\": entity_ids,\n        }\n        if form_data is not None:\n            request_data[\"formData\"] = form_data\n\n        trigger_id = uuid.uuid4().hex\n        failed = False\n        try:\n            self._controller.emit_event(\n                \"webaction.trigger.started\",\n                {\n                    \"trigger_id\": trigger_id,\n                    \"identifier\": identifier,\n                    \"full_label\": action_label,\n                }\n            )\n\n            response = ayon_api.raw_post(url, json=request_data)\n            response.raise_for_status()\n            handle_response = self._handle_webaction_response(response.data)\n\n        except Exception:\n            failed = True\n            self.log.warning(\"Action trigger failed.\", exc_info=True)\n            handle_response = WebactionResponse(\n                \"unknown\",\n                False,\n                error_message=\"Failed to trigger webaction.\",\n            )\n\n        data = asdict(handle_response)\n        data.update({\n            \"trigger_failed\": failed,\n            \"trigger_id\": trigger_id,\n            \"identifier\": identifier,\n            \"full_label\": action_label,\n            \"project_name\": project_name,\n            \"folder_id\": folder_id,\n            \"task_id\": task_id,\n            \"workfile_id\": workfile_id,\n            \"addon_name\": addon_name,\n            \"addon_version\": addon_version,\n        })\n        self._controller.emit_event(\n            \"webaction.trigger.finished\",\n            data,\n        )\n\n    def get_action_config_values(self, context: WebactionContext):\n        selection = self._prepare_selection(\n            context.project_name,\n            context.folder_id,\n            context.task_id,\n            context.workfile_id,\n        )\n        if not selection.is_project_selected:\n            return {}\n\n        request_data = self._get_webaction_request_data(selection)\n\n        query = {\n            \"addonName\": context.addon_name,\n            \"addonVersion\": context.addon_version,\n            \"identifier\": context.identifier,\n            \"variant\": self._variant,\n        }\n        url = f\"actions/config?{urlencode(query)}\"\n        try:\n            response = ayon_api.post(url, **request_data)\n            response.raise_for_status()\n        except Exception:\n            self.log.warning(\n                \"Failed to collect webaction config values.\",\n                exc_info=True\n            )\n            return {}\n        return response.data\n\n    def set_action_config_values(self, context, values):\n        selection = self._prepare_selection(\n            context.project_name,\n            context.folder_id,\n            context.task_id,\n            context.workfile_id,\n        )\n        if not selection.is_project_selected:\n            return {}\n\n        request_data = self._get_webaction_request_data(selection)\n        request_data[\"value\"] = values\n\n        query = {\n            \"addonName\": context.addon_name,\n            \"addonVersion\": context.addon_version,\n            \"identifier\": context.identifier,\n            \"variant\": self._variant,\n        }\n        url = f\"actions/config?{urlencode(query)}\"\n        try:\n            response = ayon_api.post(url, **request_data)\n            response.raise_for_status()\n        except Exception:\n            self.log.warning(\n                \"Failed to store webaction config values.\",\n                exc_info=True\n            )\n\n    def _prepare_selection(\n        self, project_name, folder_id, task_id, workfile_id\n    ):\n        project_entity = None\n        if project_name:\n            project_entity = self._controller.get_project_entity(project_name)\n        project_settings = self._controller.get_project_settings(project_name)\n        return LauncherActionSelection(\n            project_name,\n            folder_id,\n            task_id,\n            workfile_id,\n            project_entity=project_entity,\n            project_settings=project_settings,\n        )\n\n    def _get_webaction_request_data(self, selection: LauncherActionSelection):\n        entity_type = None\n        entity_id = None\n        entity_subtypes = []\n        if selection.is_workfile_selected:\n            entity_type = \"workfile\"\n            entity_id = selection.workfile_id\n            entity_subtypes = []\n\n        elif selection.is_task_selected:\n            entity_type = \"task\"\n            entity_id = selection.task_entity[\"id\"]\n            entity_subtypes = [selection.task_entity[\"taskType\"]]\n\n        elif selection.is_folder_selected:\n            entity_type = \"folder\"\n            entity_id = selection.folder_entity[\"id\"]\n            entity_subtypes = [selection.folder_entity[\"folderType\"]]\n\n        elif selection.is_project_selected:\n            # Project actions are supported since AYON 1.9.1\n            ma, mi, pa, _, _ = ayon_api.get_server_version_tuple()\n            if (ma, mi, pa) &lt; (1, 9, 1):\n                return None\n            entity_type = \"project\"\n\n        entity_ids = []\n        if entity_id:\n            entity_ids.append(entity_id)\n\n        project_name = selection.project_name\n        return {\n            \"projectName\": project_name,\n            \"entityType\": entity_type,\n            \"entitySubtypes\": entity_subtypes,\n            \"entityIds\": entity_ids,\n        }\n\n    def _get_webactions(self, selection: LauncherActionSelection):\n        request_data = self._get_webaction_request_data(selection)\n        if request_data is None:\n            return []\n\n        project_name = selection.project_name\n        entity_id = None\n        if request_data[\"entityIds\"]:\n            entity_id = request_data[\"entityIds\"][0]\n\n        cache: CacheItem = self._webaction_items[project_name][entity_id]\n        if cache.is_valid:\n            return cache.get_data()\n\n        try:\n            # 'variant' query is supported since AYON backend 1.10.4\n            query = urlencode({\"variant\": self._variant, \"mode\": \"all\"})\n            response = ayon_api.post(\n                f\"actions/list?{query}\", **request_data\n            )\n            response.raise_for_status()\n        except Exception:\n            self.log.warning(\"Failed to collect webactions.\", exc_info=True)\n            return []\n\n        action_items = []\n        for idx, action in enumerate(response.data[\"actions\"]):\n            # NOTE Settings variant may be important for triggering?\n            # - action[\"variant\"]\n            icon = action.get(\"icon\")\n            if icon and icon[\"type\"] == \"url\":\n                if not urlparse(icon[\"url\"]).scheme:\n                    icon[\"type\"] = \"ayon_url\"\n\n            if icon:\n                try:\n                    icon = get_icon_def_from_data(icon)\n                except ValueError:\n                    icon = None\n\n            config_fields = action.get(\"configFields\") or []\n            variant_label = action[\"label\"]\n            group_label = action.get(\"groupLabel\")\n            if not group_label:\n                group_label = variant_label\n                variant_label = None\n\n            full_label = self.calculate_full_label(\n                group_label, variant_label\n            )\n            identifier = action[\"identifier\"]\n            order = action[\"order\"]\n            if order is None:\n                order = 0\n                self.log.warning(\n                    f\"Got webaction without order. Identifier: {identifier}\"\n                )\n\n            action_items.append(ActionItem(\n                action_type=\"webaction\",\n                identifier=action[\"identifier\"],\n                order=order,\n                suborder=idx,\n                label=group_label,\n                variant_label=variant_label,\n                full_label=full_label,\n                icon=icon,\n                addon_name=action[\"addonName\"],\n                addon_version=action[\"addonVersion\"],\n                config_fields=config_fields,\n                # category=action[\"category\"],\n            ))\n\n        cache.update_data(action_items)\n        return cache.get_data()\n\n    def _handle_webaction_response(self, data) -&gt; WebactionResponse:\n        response_type = data[\"type\"]\n        # Backwards compatibility -&gt; 'server' type is not available since\n        #   AYON backend 1.8.3\n        if response_type == \"server\":\n            return WebactionResponse(\n                response_type,\n                False,\n                error_message=\"Please use AYON web UI to run the action.\",\n            )\n\n        payload = data.get(\"payload\") or {}\n\n        download_uri = payload.get(\"extra_download\")\n        if download_uri is not None:\n            # Find out if is relative or absolute URL\n            if not urlparse(download_uri).scheme:\n                ayon_url = ayon_api.get_base_url().rstrip(\"/\")\n                path = download_uri.lstrip(\"/\")\n                download_uri = f\"{ayon_url}/{path}\"\n\n            # Use webbrowser to open file\n            webbrowser.open_new_tab(download_uri)\n\n        response = WebactionResponse(\n            response_type,\n            data[\"success\"],\n            data.get(\"message\"),\n            payload.get(\"extra_clipboard\"),\n        )\n        if response_type == \"simple\":\n            pass\n\n        elif response_type == \"redirect\":\n            # NOTE unused 'newTab' key because we always have to\n            #   open new tab from desktop app.\n            if not webbrowser.open_new_tab(payload[\"uri\"]):\n                payload.error_message = \"Failed to open web browser.\"\n\n        elif response_type == \"form\":\n            submit_icon = payload[\"submit_icon\"] or None\n            cancel_icon = payload[\"cancel_icon\"] or None\n            if submit_icon:\n                submit_icon = MaterialSymbolsIcon(submit_icon)\n\n            if cancel_icon:\n                cancel_icon = MaterialSymbolsIcon(cancel_icon)\n\n            response.form = WebactionForm(\n                fields=payload[\"fields\"],\n                title=payload[\"title\"],\n                submit_label=payload[\"submit_label\"],\n                cancel_label=payload[\"cancel_label\"],\n                submit_icon=submit_icon,\n                cancel_icon=cancel_icon,\n            )\n\n        elif response_type == \"launcher\":\n            # Run AYON launcher process with uri in arguments\n            # NOTE This does pass environment variables of current process\n            #   to the subprocess.\n            # NOTE We could 'take action' directly and use the arguments here\n            if payload is not None:\n                uri = payload[\"uri\"]\n            else:\n                uri = data[\"uri\"]\n\n            # Remove bundles from environment variables\n            env = os.environ.copy()\n            env.pop(\"AYON_BUNDLE_NAME\", None)\n            env.pop(\"AYON_STUDIO_BUNDLE_NAME\", None)\n            env = clean_envs_for_ayon_process(env)\n            run_detached_ayon_launcher_process(uri, env=env)\n\n        elif response_type in (\"query\", \"navigate\"):\n            response.error_message = (\n                \"Please use AYON web UI to run the action.\"\n            )\n\n        else:\n            self.log.warning(\n                f\"Unknown webaction response type '{response_type}'\"\n            )\n            response.error_message = \"Unknown webaction response type.\"\n\n        return response\n\n    def _get_discovered_action_classes(self):\n        if self._discovered_actions is None:\n            # NOTE We don't need to register the paths, but that would\n            #   require to change discovery logic and deprecate all functions\n            #   related to registering and discovering launcher actions.\n            addons_manager = self._controller.get_addons_manager()\n            actions_paths = addons_manager.collect_launcher_action_paths()\n            for path in actions_paths:\n                if path and os.path.exists(path):\n                    register_launcher_action_path(path)\n            self._discovered_actions = (\n                discover_launcher_actions()\n            )\n        return self._discovered_actions\n\n    def _get_action_objects(self):\n        if self._actions is None:\n            actions = {}\n            for cls in self._get_discovered_action_classes():\n                obj = cls()\n                identifier = getattr(obj, \"identifier\", None)\n                if identifier is None:\n                    identifier = cls.__name__\n                actions[identifier] = obj\n            self._actions = actions\n        return self._actions\n\n    def _get_action_items(self, project_name):\n        action_items = self._action_items.get(project_name)\n        if action_items is not None:\n            return action_items\n\n        project_entity = None\n        if project_name:\n            project_entity = self._controller.get_project_entity(project_name)\n        project_settings = self._controller.get_project_settings(project_name)\n\n        action_items = {}\n        for identifier, action in self._get_action_objects().items():\n            # Backwards compatibility from 0.3.3 (24/06/10)\n            # TODO: Remove in future releases\n            if hasattr(action, \"project_settings\"):\n                action.project_entities[project_name] = project_entity\n                action.project_settings[project_name] = project_settings\n\n            label = action.label or identifier\n            variant_label = getattr(action, \"label_variant\", None)\n            full_label = self.calculate_full_label(\n                label, variant_label\n            )\n            icon = get_action_icon(action)\n            order = action.order\n            # Make sure it is not 'None'\n            if order is None:\n                order = 0\n                self.log.warning(\n                    f\"Got action without order. Identifier: {identifier}\"\n                )\n\n            item = ActionItem(\n                action_type=\"local\",\n                identifier=identifier,\n                order=order,\n                suborder=0,\n                label=label,\n                variant_label=variant_label,\n                full_label=full_label,\n                icon=icon,\n                config_fields=[],\n            )\n            action_items[identifier] = item\n        self._action_items[project_name] = action_items\n        return action_items\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/models/actions.html#client.ayon_core.tools.launcher.models.actions.ActionsModel.calculate_full_label","title":"<code>calculate_full_label(label, variant_label)</code>  <code>staticmethod</code>","text":"<p>Calculate full label from label and variant_label.</p> Source code in <code>client/ayon_core/tools/launcher/models/actions.py</code> <pre><code>@staticmethod\ndef calculate_full_label(label: str, variant_label: Optional[str]) -&gt; str:\n    \"\"\"Calculate full label from label and variant_label.\"\"\"\n    if variant_label:\n        return \" \".join([label, variant_label])\n    return label\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/models/actions.html#client.ayon_core.tools.launcher.models.actions.ActionsModel.get_action_items","title":"<code>get_action_items(project_name, folder_id, task_id, workfile_id)</code>","text":"<p>Get actions for project.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>Optional[str]</code> <p>Project name.</p> required <code>folder_id</code> <code>Optional[str]</code> <p>Folder id.</p> required <code>task_id</code> <code>Optional[str]</code> <p>Task id.</p> required <code>workfile_id</code> <code>Optional[str]</code> <p>Workfile id.</p> required <p>Returns:</p> Type Description <code>list[ActionItem]</code> <p>list[ActionItem]: List of actions.</p> Source code in <code>client/ayon_core/tools/launcher/models/actions.py</code> <pre><code>def get_action_items(\n    self,\n    project_name: Optional[str],\n    folder_id: Optional[str],\n    task_id: Optional[str],\n    workfile_id: Optional[str],\n) -&gt; list[ActionItem]:\n    \"\"\"Get actions for project.\n\n    Args:\n        project_name (Optional[str]): Project name.\n        folder_id (Optional[str]): Folder id.\n        task_id (Optional[str]): Task id.\n        workfile_id (Optional[str]): Workfile id.\n\n    Returns:\n        list[ActionItem]: List of actions.\n\n    \"\"\"\n    selection = self._prepare_selection(\n        project_name, folder_id, task_id, workfile_id\n    )\n    output = []\n    action_items = self._get_action_items(project_name)\n    for identifier, action in self._get_action_objects().items():\n        if action.is_compatible(selection):\n            output.append(action_items[identifier])\n    output.extend(self._get_webactions(selection))\n\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/models/actions.html#client.ayon_core.tools.launcher.models.actions.get_action_icon","title":"<code>get_action_icon(action)</code>","text":"<p>Get action icon info.</p> <p>Parameters:</p> Name Type Description Default <code>action</code> <code>LacunherAction</code> <p>Action instance.</p> required <p>Returns:</p> Type Description <p>dict[str, str]: Icon info.</p> Source code in <code>client/ayon_core/tools/launcher/models/actions.py</code> <pre><code>def get_action_icon(action):\n    \"\"\"Get action icon info.\n\n    Args:\n        action (LacunherAction): Action instance.\n\n    Returns:\n        dict[str, str]: Icon info.\n    \"\"\"\n\n    icon = action.icon\n    if isinstance(icon, IconBase):\n        return icon\n\n    if not icon:\n        return AwesomeFontIcon(\"fa.cube\", color=\"white\")\n\n    if isinstance(icon, dict):\n        return icon\n\n    icon_path = resources.get_resource(icon)\n    if not os.path.exists(icon_path):\n        try:\n            icon_path = icon.format(resources.RESOURCES_DIR)\n        except Exception:\n            pass\n\n    if os.path.exists(icon_path):\n        return PathIcon(icon_path)\n\n    return {\n        \"type\": \"awesome-font\",\n        \"name\": icon,\n        \"color\": action.color or \"white\"\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/models/selection.html","title":"selection","text":""},{"location":"autoapi/client/ayon_core/tools/launcher/models/selection.html#client.ayon_core.tools.launcher.models.selection.LauncherSelectionModel","title":"<code>LauncherSelectionModel</code>","text":"<p>Model handling selection changes.</p> <p>Triggering events: - \"selection.project.changed\" - \"selection.folder.changed\" - \"selection.task.changed\" - \"selection.workfile.changed\"</p> Source code in <code>client/ayon_core/tools/launcher/models/selection.py</code> <pre><code>class LauncherSelectionModel:\n    \"\"\"Model handling selection changes.\n\n    Triggering events:\n    - \"selection.project.changed\"\n    - \"selection.folder.changed\"\n    - \"selection.task.changed\"\n    - \"selection.workfile.changed\"\n    \"\"\"\n\n    event_source = \"launcher.selection.model\"\n\n    def __init__(self, controller: AbstractLauncherBackend) -&gt; None:\n        self._controller = controller\n\n        self._project_name = None\n        self._folder_id = None\n        self._task_name = None\n        self._task_id = None\n        self._workfile_id = None\n\n    def get_selected_project_name(self) -&gt; Optional[str]:\n        return self._project_name\n\n    def set_selected_project(self, project_name: Optional[str]) -&gt; None:\n        if project_name == self._project_name:\n            return\n\n        self._project_name = project_name\n        self._controller.emit_event(\n            \"selection.project.changed\",\n            {\"project_name\": project_name},\n            self.event_source\n        )\n\n    def get_selected_folder_id(self) -&gt; Optional[str]:\n        return self._folder_id\n\n    def set_selected_folder(self, folder_id: Optional[str]) -&gt; None:\n        if folder_id == self._folder_id:\n            return\n\n        self._folder_id = folder_id\n        self._controller.emit_event(\n            \"selection.folder.changed\",\n            {\n                \"project_name\": self._project_name,\n                \"folder_id\": folder_id,\n            },\n            self.event_source\n        )\n\n    def get_selected_task_name(self) -&gt; Optional[str]:\n        return self._task_name\n\n    def get_selected_task_id(self) -&gt; Optional[str]:\n        return self._task_id\n\n    def set_selected_task(\n        self, task_id: Optional[str], task_name: Optional[str]\n    ) -&gt; None:\n        if task_id == self._task_id:\n            return\n\n        self._task_name = task_name\n        self._task_id = task_id\n        self._controller.emit_event(\n            \"selection.task.changed\",\n            {\n                \"project_name\": self._project_name,\n                \"folder_id\": self._folder_id,\n                \"task_name\": task_name,\n                \"task_id\": task_id,\n            },\n            self.event_source\n        )\n\n    def get_selected_workfile(self) -&gt; Optional[str]:\n        return self._workfile_id\n\n    def set_selected_workfile(self, workfile_id: Optional[str]) -&gt; None:\n        if workfile_id == self._workfile_id:\n            return\n\n        self._workfile_id = workfile_id\n        self._controller.emit_event(\n            \"selection.workfile.changed\",\n            {\n                \"project_name\": self._project_name,\n                \"folder_id\": self._folder_id,\n                \"task_name\": self._task_name,\n                \"task_id\": self._task_id,\n                \"workfile_id\": workfile_id,\n            },\n            self.event_source\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/models/workfiles.html","title":"workfiles","text":""},{"location":"autoapi/client/ayon_core/tools/launcher/ui/index.html","title":"ui","text":""},{"location":"autoapi/client/ayon_core/tools/launcher/ui/index.html#client.ayon_core.tools.launcher.ui.LauncherWindow","title":"<code>LauncherWindow</code>","text":"<p>               Bases: <code>QWidget</code></p> <p>Launcher interface</p> Source code in <code>client/ayon_core/tools/launcher/ui/window.py</code> <pre><code>class LauncherWindow(QtWidgets.QWidget):\n    \"\"\"Launcher interface\"\"\"\n    message_interval = 5000\n    refresh_interval = 10000\n    page_side_anim_interval = 250\n\n    def __init__(self, controller=None, parent=None):\n        super().__init__(parent)\n\n        if controller is None:\n            controller = BaseLauncherController()\n\n        icon = QtGui.QIcon(resources.get_ayon_icon_filepath())\n        self.setWindowIcon(icon)\n        self.setWindowTitle(\"Launcher\")\n        self.setFocusPolicy(QtCore.Qt.StrongFocus)\n        self.setAttribute(QtCore.Qt.WA_DeleteOnClose, False)\n\n        self.setStyleSheet(style.load_stylesheet())\n\n        # Allow minimize\n        self.setWindowFlags(\n            QtCore.Qt.Window\n            | QtCore.Qt.CustomizeWindowHint\n            | QtCore.Qt.WindowTitleHint\n            | QtCore.Qt.WindowMinimizeButtonHint\n            | QtCore.Qt.WindowCloseButtonHint\n        )\n\n        self._controller = controller\n\n        overlay_object = MessageOverlayObject(self)\n\n        # Main content - Pages &amp; Actions\n        content_body = QtWidgets.QSplitter(self)\n\n        # Pages\n        pages_widget = QtWidgets.QWidget(content_body)\n\n        # - First page - Projects\n        projects_page = QtWidgets.QWidget(pages_widget)\n        projects_header_widget = QtWidgets.QWidget(projects_page)\n\n        projects_filter_text = PlaceholderLineEdit(projects_header_widget)\n        projects_filter_text.setPlaceholderText(\"Filter projects...\")\n\n        refresh_btn = RefreshButton(projects_header_widget)\n\n        projects_header_layout = QtWidgets.QHBoxLayout(projects_header_widget)\n        projects_header_layout.setContentsMargins(0, 0, 0, 0)\n        projects_header_layout.addWidget(projects_filter_text, 1)\n        projects_header_layout.addWidget(refresh_btn, 0)\n\n        projects_widget = ProjectsWidget(controller, pages_widget)\n\n        projects_layout = QtWidgets.QVBoxLayout(projects_page)\n        projects_layout.setContentsMargins(0, 0, 0, 0)\n        projects_layout.addWidget(projects_header_widget, 0)\n        projects_layout.addWidget(projects_widget, 1)\n\n        # - Second page - Hierarchy (folders &amp; tasks)\n        hierarchy_page = HierarchyPage(controller, pages_widget)\n\n        pages_layout = QtWidgets.QHBoxLayout(pages_widget)\n        pages_layout.setContentsMargins(0, 0, 0, 0)\n        pages_layout.addWidget(projects_page, 1)\n        pages_layout.addWidget(hierarchy_page, 1)\n\n        # Actions\n        actions_widget = ActionsWidget(controller, content_body)\n\n        # Vertically split Pages and Actions\n        content_body.setContentsMargins(0, 0, 0, 0)\n        content_body.setSizePolicy(\n            QtWidgets.QSizePolicy.Expanding,\n            QtWidgets.QSizePolicy.Expanding\n        )\n        content_body.setOrientation(QtCore.Qt.Vertical)\n        content_body.addWidget(pages_widget)\n        content_body.addWidget(actions_widget)\n\n        # Set useful default sizes and set stretch\n        # for the pages so that is the only one that\n        # stretches on UI resize.\n        content_body.setStretchFactor(0, 10)\n        content_body.setSizes([580, 160])\n\n        # Footer\n        # footer_widget = QtWidgets.QWidget(self)\n        #\n        # action_history = ActionHistory(footer_widget)\n        # action_history.setStatusTip(\"Show Action History\")\n        #\n        # footer_layout = QtWidgets.QHBoxLayout(footer_widget)\n        # footer_layout.setContentsMargins(0, 0, 0, 0)\n        # footer_layout.addWidget(action_history, 0)\n\n        layout = QtWidgets.QVBoxLayout(self)\n        layout.addWidget(content_body, 1)\n        # layout.addWidget(footer_widget, 0)\n\n        actions_refresh_timer = QtCore.QTimer()\n        actions_refresh_timer.setInterval(self.refresh_interval)\n\n        page_slide_anim = QtCore.QVariantAnimation(self)\n        page_slide_anim.setDuration(self.page_side_anim_interval)\n        page_slide_anim.setStartValue(0.0)\n        page_slide_anim.setEndValue(1.0)\n        page_slide_anim.setEasingCurve(QtCore.QEasingCurve.OutQuad)\n\n        refresh_btn.clicked.connect(self._on_refresh_request)\n        projects_widget.refreshed.connect(self._on_projects_refresh)\n\n        actions_refresh_timer.timeout.connect(\n            self._on_actions_refresh_timeout)\n        page_slide_anim.valueChanged.connect(\n            self._on_page_slide_value_changed)\n        page_slide_anim.finished.connect(self._on_page_slide_finished)\n        projects_filter_text.textChanged.connect(\n            self._on_project_filter_change)\n\n        controller.register_event_callback(\n            \"selection.project.changed\",\n            self._on_project_selection_change,\n        )\n        controller.register_event_callback(\n            \"action.trigger.started\",\n            self._on_action_trigger_started,\n        )\n        controller.register_event_callback(\n            \"action.trigger.finished\",\n            self._on_action_trigger_finished,\n        )\n        controller.register_event_callback(\n            \"webaction.trigger.started\",\n            self._on_webaction_trigger_started,\n        )\n        controller.register_event_callback(\n            \"webaction.trigger.finished\",\n            self._on_webaction_trigger_finished,\n        )\n\n        self._overlay_object = overlay_object\n\n        self._controller = controller\n\n        self._is_on_projects_page = True\n        self._window_is_active = False\n        self._refresh_on_activate = False\n        self._selected_project_name = None\n\n        self._pages_widget = pages_widget\n        self._pages_layout = pages_layout\n        self._projects_page = projects_page\n        self._projects_widget = projects_widget\n        self._hierarchy_page = hierarchy_page\n        self._actions_widget = actions_widget\n        # self._action_history = action_history\n\n        self._actions_refresh_timer = actions_refresh_timer\n        self._page_slide_anim = page_slide_anim\n\n        hierarchy_page.setVisible(not self._is_on_projects_page)\n        self.resize(920, 740)\n\n    def showEvent(self, event):\n        super().showEvent(event)\n        self._window_is_active = True\n        if not self._actions_refresh_timer.isActive():\n            self._actions_refresh_timer.start()\n        self._controller.refresh()\n\n    def closeEvent(self, event):\n        super().closeEvent(event)\n        self._window_is_active = False\n        self._actions_refresh_timer.stop()\n\n    def changeEvent(self, event):\n        if event.type() in (\n            QtCore.QEvent.Type.WindowStateChange,\n            QtCore.QEvent.ActivationChange,\n        ):\n            is_active = self.isActiveWindow() and not self.isMinimized()\n            self._window_is_active = is_active\n            if is_active and self._refresh_on_activate:\n                self._refresh_on_activate = False\n                self._on_actions_refresh_timeout()\n                self._actions_refresh_timer.start()\n\n        super().changeEvent(event)\n\n    def _on_actions_refresh_timeout(self):\n        # Stop timer if widget is not visible\n        if self._window_is_active:\n            self._controller.refresh_actions()\n        else:\n            self._refresh_on_activate = True\n\n    def _on_project_selection_change(self, event):\n        project_name = event[\"project_name\"]\n        self._selected_project_name = project_name\n        if not project_name:\n            self._go_to_projects_page()\n\n        elif self._is_on_projects_page:\n            self._go_to_hierarchy_page(project_name)\n\n    def _on_project_filter_change(self, text):\n        self._projects_widget.set_name_filter(text)\n\n    def _on_refresh_request(self):\n        self._controller.refresh()\n\n    def _on_projects_refresh(self):\n        # Refresh only actions on projects page\n        if self._is_on_projects_page:\n            self._actions_widget.refresh()\n            return\n\n        # No projects were found -&gt; go back to projects page\n        if not self._projects_widget.has_content():\n            self._go_to_projects_page()\n            return\n\n        self._hierarchy_page.refresh()\n        self._actions_widget.refresh()\n\n    def _show_toast_message(self, message, success=True, message_id=None):\n        message_type = None\n        if not success:\n            message_type = \"error\"\n\n        self._overlay_object.add_message(\n            message, message_type, message_id=message_id\n        )\n\n    def _on_action_trigger_started(self, event):\n        self._show_toast_message(\n            \"Running: {}\".format(event[\"full_label\"]),\n            message_id=event[\"trigger_id\"],\n        )\n\n    def _on_action_trigger_finished(self, event):\n        action_label = event[\"full_label\"]\n        if event[\"failed\"]:\n            message = f\"Failed to run: {action_label}\"\n        else:\n            message = f\"Finished: {action_label}\"\n        self._show_toast_message(\n            message,\n            not event[\"failed\"],\n            message_id=event[\"trigger_id\"],\n        )\n\n    def _on_webaction_trigger_started(self, event):\n        self._show_toast_message(\n            \"Running: {}\".format(event[\"full_label\"]),\n            message_id=event[\"trigger_id\"],\n        )\n\n    def _on_webaction_trigger_finished(self, event):\n        clipboard_text = event[\"clipboard_text\"]\n        if clipboard_text:\n            clipboard = QtWidgets.QApplication.clipboard()\n            clipboard.setText(clipboard_text)\n\n        action_label = event[\"full_label\"]\n        # Avoid to show exception message\n        if event[\"trigger_failed\"]:\n            self._show_toast_message(\n                f\"Failed to run: {action_label}\",\n                message_id=event[\"trigger_id\"]\n            )\n            return\n\n        # Failed to run webaction, e.g. because of missing webaction handling\n        # - not reported by server\n        if event[\"error_message\"]:\n            self._show_toast_message(\n                event[\"error_message\"],\n                success=False,\n                message_id=event[\"trigger_id\"]\n            )\n            return\n\n        if event[\"message\"]:\n            self._show_toast_message(event[\"message\"], event[\"success\"])\n\n        if event[\"form\"]:\n            self._actions_widget.handle_webaction_form_event(event)\n\n    def _is_page_slide_anim_running(self):\n        return (\n            self._page_slide_anim.state() == QtCore.QAbstractAnimation.Running\n        )\n\n    def _go_to_projects_page(self):\n        if self._is_on_projects_page:\n            return\n\n        # Deselect project in projects widget\n        self._projects_widget.set_selected_project(None)\n        self._is_on_projects_page = True\n        self._hierarchy_page.set_page_visible(False)\n\n        self._start_page_slide_animation()\n\n    def _go_to_hierarchy_page(self, project_name):\n        if not self._is_on_projects_page:\n            return\n        self._is_on_projects_page = False\n        self._hierarchy_page.set_page_visible(True, project_name)\n\n        self._start_page_slide_animation()\n\n    def _start_page_slide_animation(self):\n        if self._is_on_projects_page:\n            direction = QtCore.QAbstractAnimation.Backward\n        else:\n            direction = QtCore.QAbstractAnimation.Forward\n        self._page_slide_anim.setDirection(direction)\n        if self._is_page_slide_anim_running():\n            return\n\n        layout_spacing = self._pages_layout.spacing()\n        if self._is_on_projects_page:\n            hierarchy_geo = self._hierarchy_page.geometry()\n            projects_geo = QtCore.QRect(hierarchy_geo)\n            projects_geo.moveRight(\n                hierarchy_geo.left() - (layout_spacing + 1))\n\n            self._projects_page.setVisible(True)\n\n        else:\n            projects_geo = self._projects_page.geometry()\n            hierarchy_geo = QtCore.QRect(projects_geo)\n            hierarchy_geo.moveLeft(projects_geo.right() + layout_spacing)\n            self._hierarchy_page.setVisible(True)\n\n        while self._pages_layout.count():\n            self._pages_layout.takeAt(0)\n\n        self._projects_page.setGeometry(projects_geo)\n        self._hierarchy_page.setGeometry(hierarchy_geo)\n\n        self._page_slide_anim.start()\n\n    def _on_page_slide_value_changed(self, value):\n        layout_spacing = self._pages_layout.spacing()\n        content_width = self._pages_widget.width() - layout_spacing\n        content_height = self._pages_widget.height()\n\n        # Visible widths of other widgets\n        hierarchy_width = int(content_width * value)\n\n        hierarchy_geo = QtCore.QRect(\n            content_width - hierarchy_width, 0, content_width, content_height\n        )\n        projects_geo = QtCore.QRect(hierarchy_geo)\n        projects_geo.moveRight(hierarchy_geo.left() - (layout_spacing + 1))\n\n        self._projects_page.setGeometry(projects_geo)\n        self._hierarchy_page.setGeometry(hierarchy_geo)\n\n    def _on_page_slide_finished(self):\n        self._pages_layout.addWidget(self._projects_page, 1)\n        self._pages_layout.addWidget(self._hierarchy_page, 1)\n        self._projects_page.setVisible(self._is_on_projects_page)\n        self._hierarchy_page.setVisible(not self._is_on_projects_page)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/ui/actions_widget.html","title":"actions_widget","text":""},{"location":"autoapi/client/ayon_core/tools/launcher/ui/actions_widget.html#client.ayon_core.tools.launcher.ui.actions_widget.ActionMenuPopup","title":"<code>ActionMenuPopup</code>","text":"<p>               Bases: <code>QWidget</code></p> <p>Popup widget for group varaints.</p> <p>The popup is handling most of the layout and showing of the items     manually.</p> There 4 parts <ol> <li>Shadow - semi transparent black widget used as shadow.</li> <li>Background - painted over the shadow with blur effect. All     other items are painted over.</li> <li>Label - show group label and positioned manually at the top     of the popup.</li> <li>View - View with variant action items. View is positioned     and resized manually according to the items in the group and then     animated using mask region.</li> </ol> Source code in <code>client/ayon_core/tools/launcher/ui/actions_widget.py</code> <pre><code>class ActionMenuPopup(QtWidgets.QWidget):\n    \"\"\"Popup widget for group varaints.\n\n    The popup is handling most of the layout and showing of the items\n        manually.\n\n    There 4 parts:\n        1. Shadow - semi transparent black widget used as shadow.\n        2. Background - painted over the shadow with blur effect. All\n            other items are painted over.\n        3. Label - show group label and positioned manually at the top\n            of the popup.\n        4. View - View with variant action items. View is positioned\n            and resized manually according to the items in the group and then\n            animated using mask region.\n\n    \"\"\"\n    action_triggered = QtCore.Signal(str)\n    config_requested = QtCore.Signal(str, QtCore.QPoint)\n\n    def __init__(self, parent):\n        super().__init__(parent)\n\n        self.setWindowFlags(QtCore.Qt.Tool | QtCore.Qt.FramelessWindowHint)\n        self.setAttribute(QtCore.Qt.WA_ShowWithoutActivating, True)\n        self.setAttribute(QtCore.Qt.WA_TranslucentBackground, True)\n\n        # Close widget if is not updated by event\n        close_timer = QtCore.QTimer()\n        close_timer.setSingleShot(True)\n        close_timer.setInterval(100)\n\n        expand_anim = QtCore.QVariantAnimation()\n        expand_anim.setDuration(60)\n        expand_anim.setEasingCurve(QtCore.QEasingCurve.InOutQuad)\n\n        sh_l, sh_t, sh_r, sh_b = SHADOW_FRAME_MARGINS\n\n        group_label = QtWidgets.QLabel(\"|\", self)\n        group_label.setObjectName(\"GroupLabel\")\n\n        # View with actions\n        view = ActionsView(self)\n        view.setGridSize(QtCore.QSize(75, 80))\n        view.setIconSize(QtCore.QSize(32, 32))\n        view.move(sh_l, sh_t)\n\n        view.stackUnder(group_label)\n\n        # Background draw\n        bg_frame = QtWidgets.QFrame(self)\n        bg_frame.setObjectName(\"ShadowFrame\")\n        bg_frame.stackUnder(view)\n\n        wrapper = QtWidgets.QFrame(self)\n        wrapper.setObjectName(\"Wrapper\")\n\n        effect = QtWidgets.QGraphicsBlurEffect(wrapper)\n        effect.setBlurRadius(3.0)\n        wrapper.setGraphicsEffect(effect)\n\n        bg_layout = QtWidgets.QVBoxLayout(bg_frame)\n        bg_layout.setContentsMargins(sh_l, sh_t, sh_r, sh_b)\n        bg_layout.addWidget(wrapper)\n\n        model = ActionMenuPopupModel()\n        proxy_model = ActionsProxyModel()\n        proxy_model.setSourceModel(model)\n\n        view.setModel(proxy_model)\n        view.setVerticalScrollBarPolicy(QtCore.Qt.ScrollBarAlwaysOff)\n        view.setHorizontalScrollBarPolicy(QtCore.Qt.ScrollBarAlwaysOff)\n\n        close_timer.timeout.connect(self.close)\n        expand_anim.valueChanged.connect(self._on_expand_anim)\n        expand_anim.finished.connect(self._on_expand_finish)\n\n        view.clicked.connect(self._on_clicked)\n        view.config_requested.connect(self._on_configs_trigger)\n\n        self._group_label = group_label\n        self._view = view\n        self._bg_frame = bg_frame\n        self._effect = effect\n        self._model = model\n        self._proxy_model = proxy_model\n\n        self._close_timer = close_timer\n        self._expand_anim = expand_anim\n\n        self._showed = False\n        self._current_id = None\n        self._right_to_left = False\n\n    def showEvent(self, event):\n        self._showed = True\n        super().showEvent(event)\n\n    def closeEvent(self, event):\n        self._showed = False\n        super().closeEvent(event)\n\n    def enterEvent(self, event):\n        super().leaveEvent(event)\n        self._close_timer.stop()\n\n    def leaveEvent(self, event):\n        super().leaveEvent(event)\n        # On macOs the popup does not get focus on show so leave\n        #   event is triggered when the animation is still running.\n        if (\n            IS_MACOS\n            and self._expand_anim.state() == QtCore.QAbstractAnimation.Running\n        ):\n            return\n        self._close_timer.start()\n\n    def show_items(self, group_label, action_id, action_items, pos):\n        self._group_label.setText(group_label)\n        if not action_items:\n            if self._showed:\n                self._close_timer.start()\n            self._current_id = None\n            return\n\n        self._close_timer.stop()\n\n        if action_id != self._current_id:\n            self.setGeometry(pos.x(), pos.y(), 1, 1)\n            self._current_id = action_id\n            self._update_items(action_items)\n\n        # Make sure is visible\n        if not self._showed:\n            self.show()\n\n        # Set geometry to position\n        # - first make sure widget changes from '_update_items'\n        #   are recalculated\n        app = QtWidgets.QApplication.instance()\n        app.processEvents()\n        items_count, start_size, target_size = self._get_size_hint()\n        self._model.fill_to_count(items_count)\n\n        label_sh = self._group_label.sizeHint()\n        label_width, label_height = label_sh.width(), label_sh.height()\n        window = self.screen()\n        window_geo = window.geometry()\n        _target_x = pos.x() + target_size.width()\n        _target_y = pos.y() + target_size.height() + label_height\n        right_to_left = (\n            _target_x &gt; window_geo.right()\n            or _target_y &gt; window_geo.bottom()\n        )\n\n        sh_l, sh_t, sh_r, sh_b = SHADOW_FRAME_MARGINS\n        viewport_offset = self._view.viewport().geometry().topLeft()\n        pos_x = pos.x() - (sh_l + viewport_offset.x() + 2)\n        pos_y = pos.y() - (sh_t + viewport_offset.y() + 1)\n        bg_x = bg_y = 0\n        sort_order = QtCore.Qt.DescendingOrder\n        if right_to_left:\n            sort_order = QtCore.Qt.AscendingOrder\n            size_diff = target_size - start_size\n            pos_x -= size_diff.width()\n            pos_y -= size_diff.height()\n            bg_x = size_diff.width()\n            bg_y = size_diff.height() - label_height\n\n        bg_geo = QtCore.QRect(\n            bg_x, bg_y,\n            start_size.width(), start_size.height() + label_height\n        )\n\n        label_pos_x = sh_l\n        label_pos_y = bg_y + sh_t\n        if label_width &lt; start_size.width():\n            label_pos_x = bg_x + (start_size.width() - label_width) // 2\n\n        if self._expand_anim.state() == QtCore.QAbstractAnimation.Running:\n            self._expand_anim.stop()\n\n        self._right_to_left = right_to_left\n\n        self._proxy_model.sort(0, sort_order)\n        self.setUpdatesEnabled(False)\n        self._view.setMask(\n            bg_geo.adjusted(sh_l, sh_t, -sh_r, -sh_b)\n        )\n        self._view.setMinimumWidth(target_size.width())\n        self._view.setMaximumWidth(target_size.width())\n        self._view.setMinimumHeight(target_size.height())\n        self._view.move(sh_l, sh_t + label_height)\n        self.setGeometry(\n            pos_x, pos_y - label_height,\n            target_size.width(), target_size.height() + label_height\n        )\n        self._bg_frame.setGeometry(bg_geo)\n        self._group_label.move(label_pos_x, label_pos_y)\n        self.setUpdatesEnabled(True)\n\n        self._expand_anim.updateCurrentTime(0)\n        self._expand_anim.setStartValue(start_size)\n        self._expand_anim.setEndValue(target_size)\n        self._expand_anim.start()\n\n        self.raise_()\n\n    def _on_clicked(self, index):\n        if not index or not index.isValid():\n            return\n\n        action_id = index.data(ACTION_ID_ROLE)\n        self.action_triggered.emit(action_id)\n\n    def _on_expand_anim(self, value):\n        if not self._showed:\n            if self._expand_anim.state() == QtCore.QAbstractAnimation.Running:\n                self._expand_anim.stop()\n            return\n\n        bg_geo = self._bg_frame.geometry()\n\n        label_sh = self._group_label.sizeHint()\n        label_width, label_height = label_sh.width(), label_sh.height()\n        if self._right_to_left:\n            popup_geo = self.geometry()\n            diff_size = popup_geo.size() - value\n            pos = QtCore.QPoint(\n                diff_size.width(), diff_size.height() - label_height\n            )\n\n            bg_geo.moveTopLeft(pos)\n\n        bg_geo.setWidth(value.width())\n        bg_geo.setHeight(value.height() + label_height)\n\n        label_width = self._group_label.sizeHint().width()\n        bgeo_tl = bg_geo.topLeft()\n        sh_l, sh_t, sh_r, sh_b = SHADOW_FRAME_MARGINS\n\n        label_pos_x = sh_l\n        if label_width &lt; value.width():\n            label_pos_x = bgeo_tl.x() + (value.width() - label_width) // 2\n\n        self.setUpdatesEnabled(False)\n        self._view.setMask(\n            bg_geo.adjusted(sh_l, sh_t, -sh_r, -sh_b)\n        )\n        self._group_label.move(label_pos_x, sh_t)\n        self._bg_frame.setGeometry(bg_geo)\n        self.setUpdatesEnabled(True)\n        self.update()\n\n    def _on_expand_finish(self):\n        # Make sure that size is recalculated if src and targe size is same\n        _, _, size = self._get_size_hint()\n        self._on_expand_anim(size)\n\n    def _get_size_hint(self):\n        grid_size = self._view.gridSize()\n        row_count = self._proxy_model.rowCount()\n        cols = 4\n        rows = 1\n        while True:\n            rows = row_count // cols\n            if row_count % cols:\n                rows += 1\n            if rows &lt;= cols:\n                break\n            cols += 1\n\n        if rows == 1:\n            cols = row_count\n\n        viewport_geo = self._view.viewport().geometry()\n        viewport_offset = viewport_geo.topLeft()\n        # QUESTION how to get the bottom and right margins from Qt?\n        vp_lr = viewport_offset.x()\n        vp_tb = viewport_offset.y()\n        m_l, m_t, m_r, m_b = (\n            s_m + vp_m\n            for s_m, vp_m in zip(\n                SHADOW_FRAME_MARGINS,\n                (vp_lr, vp_tb, vp_lr, vp_tb)\n            )\n        )\n        single_width = (\n            grid_size.width()\n            + self._view.horizontalOffset() + m_l + m_r + 1\n        )\n        single_height = (\n            grid_size.height()\n            + self._view.verticalOffset() + m_b + m_t + 1\n        )\n        total_width = single_width\n        total_height = single_height\n        if cols &gt; 1:\n            total_width += (\n                (cols - 1) * (self._view.spacing() + grid_size.width())\n            )\n\n        if rows &gt; 1:\n            total_height += (\n                (rows - 1) * (grid_size.height() + self._view.spacing())\n            )\n        return (\n            cols * rows,\n            QtCore.QSize(single_width, single_height),\n            QtCore.QSize(total_width, total_height)\n        )\n\n    def _update_items(self, action_items):\n        \"\"\"Update items in the tooltip.\"\"\"\n        # This method can be used to update the content of the tooltip\n        # with new icon, text and settings button visibility.\n        self._model.set_action_items(action_items)\n        self._view.update_on_refresh()\n\n    def _on_trigger(self, action_id):\n        self.action_triggered.emit(action_id)\n        self.close()\n\n    def _on_configs_trigger(self, action_id, center_pos):\n        self.config_requested.emit(action_id, center_pos)\n        self.close()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/ui/actions_widget.html#client.ayon_core.tools.launcher.ui.actions_widget.ActionMenuPopupModel","title":"<code>ActionMenuPopupModel</code>","text":"<p>               Bases: <code>QStandardItemModel</code></p> Source code in <code>client/ayon_core/tools/launcher/ui/actions_widget.py</code> <pre><code>class ActionMenuPopupModel(QtGui.QStandardItemModel):\n    def set_action_items(self, action_items):\n        \"\"\"Set action items for the popup.\"\"\"\n        root_item = self.invisibleRootItem()\n        root_item.removeRows(0, root_item.rowCount())\n\n        transparent_icon = TransparentIcon(256)\n        new_items = []\n        for action_item in action_items:\n            icon_def = action_item.icon\n            if not icon_def:\n                icon_def = transparent_icon\n\n            try:\n                icon = get_qt_icon(icon_def)\n            except Exception:\n                self._log.warning(\n                    \"Failed to parse icon definition\", exc_info=True\n                )\n                # Use empty icon if failed to parse definition\n                icon = get_qt_icon(transparent_icon)\n\n            item = QtGui.QStandardItem()\n            item.setFlags(QtCore.Qt.ItemIsEnabled)\n            item.setData(action_item.variant_label, QtCore.Qt.DisplayRole)\n            item.setData(action_item.full_label, QtCore.Qt.ToolTipRole)\n            item.setData(icon, QtCore.Qt.DecorationRole)\n            item.setData(action_item.identifier, ACTION_ID_ROLE)\n            item.setData(\n                bool(action_item.config_fields),\n                ACTION_HAS_CONFIGS_ROLE\n            )\n            item.setData(action_item.order, ACTION_ORDER_ROLE)\n            item.setData(action_item.suborder, ACTION_SUBORDER_ROLE)\n\n            new_items.append(item)\n\n        if new_items:\n            root_item.appendRows(new_items)\n\n    def fill_to_count(self, count: int):\n        \"\"\"Fill up items to specifi counter.\n\n        This is needed to visually organize structure or the viewed items. If\n            items are shown right to left then mouse would not hover over\n            last item i there are multiple rows that are uneven. This will\n            fill the \"first items\" with invisible items so visually it looks\n            correct.\n\n        Visually it will cause this:\n        [ ] [ ] [ ] [A]\n        [A] [A] [A] [A]\n\n        Instead of:\n        [A] [A] [A] [A]\n        [A] [ ] [ ] [ ]\n\n        \"\"\"\n        remainders = count - self.rowCount()\n        if not remainders:\n            return\n\n        items = []\n        for _ in range(remainders):\n            item = QtGui.QStandardItem()\n            item.setFlags(QtCore.Qt.NoItemFlags)\n            item.setData(True, PLACEHOLDER_ITEM_ROLE)\n            items.append(item)\n\n        root_item = self.invisibleRootItem()\n        root_item.appendRows(items)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/ui/actions_widget.html#client.ayon_core.tools.launcher.ui.actions_widget.ActionMenuPopupModel.fill_to_count","title":"<code>fill_to_count(count)</code>","text":"<p>Fill up items to specifi counter.</p> <p>This is needed to visually organize structure or the viewed items. If     items are shown right to left then mouse would not hover over     last item i there are multiple rows that are uneven. This will     fill the \"first items\" with invisible items so visually it looks     correct.</p> <p>Visually it will cause this: [ ][] [ ][A] [A][] [A][]</p> <p>Instead of: [A][] [A][] [A][ ] [ ][]</p> Source code in <code>client/ayon_core/tools/launcher/ui/actions_widget.py</code> <pre><code>def fill_to_count(self, count: int):\n    \"\"\"Fill up items to specifi counter.\n\n    This is needed to visually organize structure or the viewed items. If\n        items are shown right to left then mouse would not hover over\n        last item i there are multiple rows that are uneven. This will\n        fill the \"first items\" with invisible items so visually it looks\n        correct.\n\n    Visually it will cause this:\n    [ ] [ ] [ ] [A]\n    [A] [A] [A] [A]\n\n    Instead of:\n    [A] [A] [A] [A]\n    [A] [ ] [ ] [ ]\n\n    \"\"\"\n    remainders = count - self.rowCount()\n    if not remainders:\n        return\n\n    items = []\n    for _ in range(remainders):\n        item = QtGui.QStandardItem()\n        item.setFlags(QtCore.Qt.NoItemFlags)\n        item.setData(True, PLACEHOLDER_ITEM_ROLE)\n        items.append(item)\n\n    root_item = self.invisibleRootItem()\n    root_item.appendRows(items)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/ui/actions_widget.html#client.ayon_core.tools.launcher.ui.actions_widget.ActionMenuPopupModel.set_action_items","title":"<code>set_action_items(action_items)</code>","text":"<p>Set action items for the popup.</p> Source code in <code>client/ayon_core/tools/launcher/ui/actions_widget.py</code> <pre><code>def set_action_items(self, action_items):\n    \"\"\"Set action items for the popup.\"\"\"\n    root_item = self.invisibleRootItem()\n    root_item.removeRows(0, root_item.rowCount())\n\n    transparent_icon = TransparentIcon(256)\n    new_items = []\n    for action_item in action_items:\n        icon_def = action_item.icon\n        if not icon_def:\n            icon_def = transparent_icon\n\n        try:\n            icon = get_qt_icon(icon_def)\n        except Exception:\n            self._log.warning(\n                \"Failed to parse icon definition\", exc_info=True\n            )\n            # Use empty icon if failed to parse definition\n            icon = get_qt_icon(transparent_icon)\n\n        item = QtGui.QStandardItem()\n        item.setFlags(QtCore.Qt.ItemIsEnabled)\n        item.setData(action_item.variant_label, QtCore.Qt.DisplayRole)\n        item.setData(action_item.full_label, QtCore.Qt.ToolTipRole)\n        item.setData(icon, QtCore.Qt.DecorationRole)\n        item.setData(action_item.identifier, ACTION_ID_ROLE)\n        item.setData(\n            bool(action_item.config_fields),\n            ACTION_HAS_CONFIGS_ROLE\n        )\n        item.setData(action_item.order, ACTION_ORDER_ROLE)\n        item.setData(action_item.suborder, ACTION_SUBORDER_ROLE)\n\n        new_items.append(item)\n\n    if new_items:\n        root_item.appendRows(new_items)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/ui/actions_widget.html#client.ayon_core.tools.launcher.ui.actions_widget.ActionsQtModel","title":"<code>ActionsQtModel</code>","text":"<p>               Bases: <code>QStandardItemModel</code></p> <p>Qt model for actions.</p> <p>Parameters:</p> Name Type Description Default <code>controller</code> <code>AbstractLauncherFrontEnd</code> <p>Controller instance.</p> required Source code in <code>client/ayon_core/tools/launcher/ui/actions_widget.py</code> <pre><code>class ActionsQtModel(QtGui.QStandardItemModel):\n    \"\"\"Qt model for actions.\n\n    Args:\n        controller (AbstractLauncherFrontEnd): Controller instance.\n    \"\"\"\n\n    refreshed = QtCore.Signal()\n\n    def __init__(self, controller):\n        self._log = Logger.get_logger(self.__class__.__name__)\n        super().__init__()\n\n        controller.register_event_callback(\n            \"selection.project.changed\",\n            self._on_selection_project_changed,\n        )\n        controller.register_event_callback(\n            \"selection.folder.changed\",\n            self._on_selection_folder_changed,\n        )\n        controller.register_event_callback(\n            \"selection.task.changed\",\n            self._on_selection_task_changed,\n        )\n        controller.register_event_callback(\n            \"selection.workfile.changed\",\n            self._on_selection_workfile_changed,\n        )\n\n        self._controller = controller\n\n        self._items_by_id = {}\n        self._action_items_by_id = {}\n        self._groups_by_id = {}\n\n        self._selected_project_name = None\n        self._selected_folder_id = None\n        self._selected_task_id = None\n        self._selected_workfile_id = None\n\n    def get_selected_project_name(self):\n        return self._selected_project_name\n\n    def get_selected_folder_id(self):\n        return self._selected_folder_id\n\n    def get_selected_task_id(self):\n        return self._selected_task_id\n\n    def get_selected_workfile_id(self):\n        return self._selected_workfile_id\n\n    def get_group_items(self, action_id):\n        return self._groups_by_id[action_id]\n\n    def get_item_by_id(self, action_id):\n        return self._items_by_id.get(action_id)\n\n    def get_index_by_id(self, action_id):\n        item = self.get_item_by_id(action_id)\n        if item is not None:\n            return self.indexFromItem(item)\n        return QtCore.QModelIndex()\n\n    def get_group_item_by_action_id(self, action_id):\n        item = self._items_by_id.get(action_id)\n        if item is not None:\n            return item\n\n        for group_id, items in self._groups_by_id.items():\n            for item in items:\n                if item.identifier == action_id:\n                    return self._items_by_id[group_id]\n        return None\n\n    def get_action_item_by_id(self, action_id):\n        return self._action_items_by_id.get(action_id)\n\n    def _clear_items(self):\n        self._items_by_id = {}\n        self._action_items_by_id = {}\n        self._groups_by_id = {}\n        root = self.invisibleRootItem()\n        root.removeRows(0, root.rowCount())\n\n    def refresh(self):\n        items = self._controller.get_action_items(\n            self._selected_project_name,\n            self._selected_folder_id,\n            self._selected_task_id,\n            self._selected_workfile_id,\n        )\n        if not items:\n            self._clear_items()\n            self.refreshed.emit()\n            return\n\n        root_item = self.invisibleRootItem()\n\n        all_action_items_info = []\n        action_items_by_id = {}\n        items_by_label = collections.defaultdict(list)\n        for item in items:\n            action_items_by_id[item.identifier] = item\n            if not item.variant_label:\n                all_action_items_info.append((item, False))\n            else:\n                items_by_label[item.label].append(item)\n\n        groups_by_id = {}\n        for action_items in items_by_label.values():\n            action_items.sort(key=_variant_label_sort_getter, reverse=True)\n            first_item = next(iter(action_items))\n            all_action_items_info.append((first_item, len(action_items) &gt; 1))\n            groups_by_id[first_item.identifier] = action_items\n\n        transparent_icon = TransparentIcon(256)\n        new_items = []\n        items_by_id = {}\n        for action_item_info in all_action_items_info:\n            action_item, is_group = action_item_info\n            icon_def = action_item.icon\n            if not icon_def:\n                icon_def = transparent_icon\n\n            try:\n                icon = get_qt_icon(icon_def)\n            except Exception:\n                self._log.warning(\n                    \"Failed to parse icon definition\", exc_info=True\n                )\n                # Use empty icon if failed to parse definition\n                icon = get_qt_icon(transparent_icon)\n\n            if is_group:\n                has_configs = False\n                label = action_item.label\n            else:\n                label = action_item.full_label\n                has_configs = bool(action_item.config_fields)\n\n            item = self._items_by_id.get(action_item.identifier)\n            if item is None:\n                item = QtGui.QStandardItem()\n                item.setData(action_item.identifier, ACTION_ID_ROLE)\n                new_items.append(item)\n\n            item.setFlags(QtCore.Qt.ItemIsEnabled)\n            item.setData(label, QtCore.Qt.DisplayRole)\n            item.setData(label, QtCore.Qt.ToolTipRole)\n            item.setData(icon, QtCore.Qt.DecorationRole)\n            item.setData(is_group, ACTION_IS_GROUP_ROLE)\n            item.setData(has_configs, ACTION_HAS_CONFIGS_ROLE)\n            item.setData(action_item.action_type, ACTION_TYPE_ROLE)\n            item.setData(action_item.addon_name, ACTION_ADDON_NAME_ROLE)\n            item.setData(action_item.addon_version, ACTION_ADDON_VERSION_ROLE)\n            item.setData(action_item.order, ACTION_ORDER_ROLE)\n            item.setData(action_item.suborder, ACTION_SUBORDER_ROLE)\n            items_by_id[action_item.identifier] = item\n\n        if new_items:\n            root_item.appendRows(new_items)\n\n        to_remove = set(self._items_by_id.keys()) - set(items_by_id.keys())\n        for identifier in to_remove:\n            item = self._items_by_id.pop(identifier)\n            self._action_items_by_id.pop(identifier)\n            root_item.removeRow(item.row())\n\n        self._groups_by_id = groups_by_id\n        self._items_by_id = items_by_id\n        self._action_items_by_id = action_items_by_id\n        self.refreshed.emit()\n\n    def get_action_config_fields(self, action_id: str):\n        action_item = self._action_items_by_id.get(action_id)\n        if action_item is not None:\n            return action_item.config_fields\n        return None\n\n    def _on_selection_project_changed(self, event):\n        self._selected_project_name = event[\"project_name\"]\n        self._selected_folder_id = None\n        self._selected_task_id = None\n        self._selected_workfile_id = None\n        self.refresh()\n\n    def _on_selection_folder_changed(self, event):\n        self._selected_project_name = event[\"project_name\"]\n        self._selected_folder_id = event[\"folder_id\"]\n        self._selected_task_id = None\n        self._selected_workfile_id = None\n        self.refresh()\n\n    def _on_selection_task_changed(self, event):\n        self._selected_project_name = event[\"project_name\"]\n        self._selected_folder_id = event[\"folder_id\"]\n        self._selected_task_id = event[\"task_id\"]\n        self._selected_workfile_id = None\n        self.refresh()\n\n    def _on_selection_workfile_changed(self, event):\n        self._selected_project_name = event[\"project_name\"]\n        self._selected_folder_id = event[\"folder_id\"]\n        self._selected_task_id = event[\"task_id\"]\n        self._selected_workfile_id = event[\"workfile_id\"]\n        self.refresh()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/ui/actions_widget.html#client.ayon_core.tools.launcher.ui.actions_widget.ActionsView","title":"<code>ActionsView</code>","text":"<p>               Bases: <code>QListView</code></p> Source code in <code>client/ayon_core/tools/launcher/ui/actions_widget.py</code> <pre><code>class ActionsView(QtWidgets.QListView):\n    config_requested = QtCore.Signal(str, QtCore.QPoint)\n\n    def __init__(self, parent):\n        super().__init__(parent)\n        self.setViewMode(QtWidgets.QListView.IconMode)\n        self.setResizeMode(QtWidgets.QListView.Adjust)\n        self.setSelectionMode(QtWidgets.QListView.NoSelection)\n        self.setContextMenuPolicy(QtCore.Qt.CustomContextMenu)\n        self.setVerticalScrollMode(QtWidgets.QAbstractItemView.ScrollPerPixel)\n        self.setEditTriggers(QtWidgets.QAbstractItemView.NoEditTriggers)\n        self.setContentsMargins(0, 0, 0, 0)\n        self.setViewportMargins(0, 0, 0, 0)\n        self.setWrapping(True)\n        self.setSpacing(0)\n        self.setWordWrap(True)\n        self.setMouseTracking(True)\n\n        vertical_scroll = self.verticalScrollBar()\n        vertical_scroll.setSingleStep(8)\n\n        delegate = ActionDelegate(self)\n        self.setItemDelegate(delegate)\n\n        # Make view flickable\n        flick = FlickCharm(parent=self)\n        flick.activateOn(self)\n\n        self.customContextMenuRequested.connect(self._on_context_menu)\n\n        self._overlay_widgets = []\n        self._flick = flick\n        self._delegate = delegate\n\n    def _on_context_menu(self, point):\n        \"\"\"Creates menu to force skip opening last workfile.\"\"\"\n        index = self.indexAt(point)\n        if not index.isValid():\n            return\n        action_id = index.data(ACTION_ID_ROLE)\n        rect = self.visualRect(index)\n        global_center = self.mapToGlobal(rect.center())\n        self.config_requested.emit(action_id, global_center)\n\n    def update_on_refresh(self):\n        viewport = self.viewport()\n        viewport.update()\n        self._add_overlay_widgets()\n\n    def _add_overlay_widgets(self):\n        overlay_widgets = []\n        viewport = self.viewport()\n        model = self.model()\n        for row in range(model.rowCount()):\n            index = model.index(row, 0)\n            has_configs = index.data(ACTION_HAS_CONFIGS_ROLE)\n            widget = None\n            if has_configs:\n                item_id = index.data(ACTION_ID_ROLE)\n                widget = ActionOverlayWidget(item_id, viewport)\n                overlay_widgets.append(widget)\n            self.setIndexWidget(index, widget)\n\n        while self._overlay_widgets:\n            widget = self._overlay_widgets.pop(0)\n            widget.setVisible(False)\n            widget.setParent(None)\n            widget.deleteLater()\n\n        self._overlay_widgets = overlay_widgets\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/ui/actions_widget.html#client.ayon_core.tools.launcher.ui.actions_widget.ActionsWidget","title":"<code>ActionsWidget</code>","text":"<p>               Bases: <code>QWidget</code></p> Source code in <code>client/ayon_core/tools/launcher/ui/actions_widget.py</code> <pre><code>class ActionsWidget(QtWidgets.QWidget):\n    def __init__(self, controller, parent):\n        super().__init__(parent)\n\n        self._controller = controller\n\n        view = ActionsView(self)\n        view.setGridSize(QtCore.QSize(70, 75))\n        view.setIconSize(QtCore.QSize(30, 30))\n\n        model = ActionsQtModel(controller)\n\n        proxy_model = ActionsProxyModel()\n        proxy_model.setSourceModel(model)\n        view.setModel(proxy_model)\n\n        layout = QtWidgets.QHBoxLayout(self)\n        layout.setContentsMargins(0, 0, 0, 0)\n        layout.addWidget(view)\n\n        animation_timer = QtCore.QTimer()\n        animation_timer.setInterval(40)\n        animation_timer.timeout.connect(self._on_animation)\n\n        view.clicked.connect(self._on_clicked)\n        view.config_requested.connect(self._show_config_dialog)\n        model.refreshed.connect(self._on_model_refresh)\n\n        self._animated_items = set()\n        self._animation_timer = animation_timer\n\n        self._view = view\n        self._model = model\n        self._proxy_model = proxy_model\n\n        self._popup_widget = None\n\n        self._set_row_height(1)\n\n    def refresh(self):\n        self._model.refresh()\n\n    def handle_webaction_form_event(self, event):\n        # NOTE The 'ActionsWidget' should be responsible for handling this\n        #   but because we're showing messages to user it is handled by window\n        identifier = event[\"identifier\"]\n        form = event[\"form\"]\n        submit_icon = form[\"submit_icon\"]\n        if submit_icon:\n            submit_icon = get_qt_icon(submit_icon)\n\n        cancel_icon = form[\"cancel_icon\"]\n        if cancel_icon:\n            cancel_icon = get_qt_icon(cancel_icon)\n\n        dialog = self._create_attrs_dialog(\n            form[\"fields\"],\n            form[\"title\"],\n            form[\"submit_label\"],\n            form[\"cancel_label\"],\n            submit_icon,\n            cancel_icon,\n        )\n        dialog.setMinimumSize(380, 180)\n        result = dialog.exec_()\n        if result != QtWidgets.QDialog.Accepted:\n            return\n        form_data = dialog.get_values()\n        self._controller.trigger_webaction(\n            WebactionContext(\n                identifier,\n                event[\"project_name\"],\n                event[\"folder_id\"],\n                event[\"task_id\"],\n                event[\"workfile_id\"],\n                event[\"addon_name\"],\n                event[\"addon_version\"],\n            ),\n            event[\"full_label\"],\n            form_data,\n        )\n\n    def _set_row_height(self, rows):\n        self.setMinimumHeight(rows * 75)\n\n    def _on_model_refresh(self):\n        self._proxy_model.sort(0)\n        # Force repaint all items\n        self._view.update_on_refresh()\n\n    def _on_animation(self):\n        time_now = time.time()\n        for action_id in tuple(self._animated_items):\n            item = self._model.get_item_by_id(action_id)\n            if item is None:\n                self._animated_items.discard(action_id)\n                continue\n\n            start_time = item.data(ANIMATION_START_ROLE)\n            if start_time is None or (time_now - start_time) &gt; ANIMATION_LEN:\n                item.setData(0, ANIMATION_STATE_ROLE)\n                self._animated_items.discard(action_id)\n\n        if not self._animated_items:\n            self._animation_timer.stop()\n\n        self.update()\n\n    def _start_animation(self, index):\n        # Offset refresh timout\n        model_index = self._proxy_model.mapToSource(index)\n        if not model_index.isValid():\n            return\n        action_id = model_index.data(ACTION_ID_ROLE)\n        self._model.setData(model_index, time.time(), ANIMATION_START_ROLE)\n        self._model.setData(model_index, 1, ANIMATION_STATE_ROLE)\n        self._animated_items.add(action_id)\n        self._animation_timer.start()\n\n    def _on_clicked(self, index):\n        if not index or not index.isValid():\n            return\n\n        is_group = index.data(ACTION_IS_GROUP_ROLE)\n        action_id = index.data(ACTION_ID_ROLE)\n        if is_group:\n            self._show_group_popup(index)\n        else:\n            self._trigger_action(action_id, index)\n\n    def _get_popup_widget(self):\n        if self._popup_widget is None:\n            popup_widget = ActionMenuPopup(self)\n\n            popup_widget.action_triggered.connect(self._trigger_action)\n            popup_widget.config_requested.connect(self._show_config_dialog)\n            self._popup_widget = popup_widget\n        return self._popup_widget\n\n    def _show_group_popup(self, index):\n        action_id = index.data(ACTION_ID_ROLE)\n        group_label = index.data(QtCore.Qt.DisplayRole)\n        action_items = self._model.get_group_items(action_id)\n        rect = self._view.visualRect(index)\n        pos = self.mapToGlobal(rect.topLeft())\n\n        popup_widget = self._get_popup_widget()\n        popup_widget.show_items(\n            group_label, action_id, action_items, pos\n        )\n\n    def _trigger_action(self, action_id, index=None):\n        project_name = self._model.get_selected_project_name()\n        folder_id = self._model.get_selected_folder_id()\n        task_id = self._model.get_selected_task_id()\n        workfile_id = self._model.get_selected_workfile_id()\n        action_item = self._model.get_action_item_by_id(action_id)\n\n        if action_item.action_type == \"webaction\":\n            action_item = self._model.get_action_item_by_id(action_id)\n            context = WebactionContext(\n                identifier=action_id,\n                project_name=project_name,\n                folder_id=folder_id,\n                task_id=task_id,\n                workfile_id=workfile_id,\n                addon_name=action_item.addon_name,\n                addon_version=action_item.addon_version,\n            )\n            self._controller.trigger_webaction(\n                context, action_item.full_label\n            )\n        else:\n            self._controller.trigger_action(\n                action_id, project_name, folder_id, task_id, workfile_id\n            )\n\n        if index is None:\n            item = self._model.get_group_item_by_action_id(action_id)\n            if item is not None:\n                index = self._proxy_model.mapFromSource(item.index())\n\n        if index is not None:\n            self._start_animation(index)\n\n    def _show_config_dialog(self, action_id, center_point):\n        action_item = self._model.get_action_item_by_id(action_id)\n        config_fields = self._model.get_action_config_fields(action_id)\n        if not config_fields:\n            return\n\n        project_name = self._model.get_selected_project_name()\n        folder_id = self._model.get_selected_folder_id()\n        task_id = self._model.get_selected_task_id()\n        workfile_id = self._model.get_selected_workfile_id()\n        context = WebactionContext(\n            identifier=action_id,\n            project_name=project_name,\n            folder_id=folder_id,\n            task_id=task_id,\n            workfile_id=workfile_id,\n            addon_name=action_item.addon_name,\n            addon_version=action_item.addon_version,\n        )\n        values = self._controller.get_action_config_values(context)\n\n        dialog = self._create_attrs_dialog(\n            config_fields,\n            \"Action Config\",\n            \"Save\",\n            \"Cancel\",\n        )\n        dialog.set_values(values)\n        dialog.show()\n        self._center_dialog(dialog, center_point)\n        result = dialog.exec_()\n        if result == QtWidgets.QDialog.Accepted:\n            new_values = dialog.get_values()\n            self._controller.set_action_config_values(context, new_values)\n\n    @staticmethod\n    def _center_dialog(dialog, target_center_pos):\n        dialog_geo = dialog.geometry()\n        dialog_geo.moveCenter(target_center_pos)\n\n        screen = dialog.screen()\n        screen_geo = screen.availableGeometry()\n        if screen_geo.left() &gt; dialog_geo.left():\n            dialog_geo.moveLeft(screen_geo.left())\n        elif screen_geo.right() &lt; dialog_geo.right():\n            dialog_geo.moveRight(screen_geo.right())\n\n        if screen_geo.top() &gt; dialog_geo.top():\n            dialog_geo.moveTop(screen_geo.top())\n        elif screen_geo.bottom() &lt; dialog_geo.bottom():\n            dialog_geo.moveBottom(screen_geo.bottom())\n        dialog.move(dialog_geo.topLeft())\n\n    def _create_attrs_dialog(\n        self,\n        config_fields,\n        title,\n        submit_label,\n        cancel_label,\n        submit_icon=None,\n        cancel_icon=None,\n    ):\n        \"\"\"Creates attribute definitions dialog.\n\n        Types:\n            label - 'text'\n            text - 'label', 'value', 'placeholder', 'regex',\n                'multiline', 'syntax'\n            boolean - 'label', 'value'\n            select - 'label', 'value', 'options'\n            multiselect - 'label', 'value', 'options'\n            hidden - 'value'\n            integer - 'label', 'value', 'placeholder', 'min', 'max'\n            float - 'label', 'value', 'placeholder', 'min', 'max'\n\n        \"\"\"\n        attr_defs = webaction_fields_to_attribute_defs(config_fields)\n\n        dialog = AttributeDefinitionsDialog(\n            attr_defs,\n            title=title,\n            parent=self,\n        )\n        if submit_label:\n            dialog.set_submit_label(submit_label)\n        else:\n            dialog.set_submit_visible(False)\n\n        if submit_icon:\n            dialog.set_submit_icon(submit_icon)\n\n        if cancel_label:\n            dialog.set_cancel_label(cancel_label)\n        else:\n            dialog.set_cancel_visible(False)\n\n        if cancel_icon:\n            dialog.set_cancel_icon(cancel_icon)\n\n        return dialog\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/ui/hierarchy_page.html","title":"hierarchy_page","text":""},{"location":"autoapi/client/ayon_core/tools/launcher/ui/window.html","title":"window","text":""},{"location":"autoapi/client/ayon_core/tools/launcher/ui/window.html#client.ayon_core.tools.launcher.ui.window.LauncherWindow","title":"<code>LauncherWindow</code>","text":"<p>               Bases: <code>QWidget</code></p> <p>Launcher interface</p> Source code in <code>client/ayon_core/tools/launcher/ui/window.py</code> <pre><code>class LauncherWindow(QtWidgets.QWidget):\n    \"\"\"Launcher interface\"\"\"\n    message_interval = 5000\n    refresh_interval = 10000\n    page_side_anim_interval = 250\n\n    def __init__(self, controller=None, parent=None):\n        super().__init__(parent)\n\n        if controller is None:\n            controller = BaseLauncherController()\n\n        icon = QtGui.QIcon(resources.get_ayon_icon_filepath())\n        self.setWindowIcon(icon)\n        self.setWindowTitle(\"Launcher\")\n        self.setFocusPolicy(QtCore.Qt.StrongFocus)\n        self.setAttribute(QtCore.Qt.WA_DeleteOnClose, False)\n\n        self.setStyleSheet(style.load_stylesheet())\n\n        # Allow minimize\n        self.setWindowFlags(\n            QtCore.Qt.Window\n            | QtCore.Qt.CustomizeWindowHint\n            | QtCore.Qt.WindowTitleHint\n            | QtCore.Qt.WindowMinimizeButtonHint\n            | QtCore.Qt.WindowCloseButtonHint\n        )\n\n        self._controller = controller\n\n        overlay_object = MessageOverlayObject(self)\n\n        # Main content - Pages &amp; Actions\n        content_body = QtWidgets.QSplitter(self)\n\n        # Pages\n        pages_widget = QtWidgets.QWidget(content_body)\n\n        # - First page - Projects\n        projects_page = QtWidgets.QWidget(pages_widget)\n        projects_header_widget = QtWidgets.QWidget(projects_page)\n\n        projects_filter_text = PlaceholderLineEdit(projects_header_widget)\n        projects_filter_text.setPlaceholderText(\"Filter projects...\")\n\n        refresh_btn = RefreshButton(projects_header_widget)\n\n        projects_header_layout = QtWidgets.QHBoxLayout(projects_header_widget)\n        projects_header_layout.setContentsMargins(0, 0, 0, 0)\n        projects_header_layout.addWidget(projects_filter_text, 1)\n        projects_header_layout.addWidget(refresh_btn, 0)\n\n        projects_widget = ProjectsWidget(controller, pages_widget)\n\n        projects_layout = QtWidgets.QVBoxLayout(projects_page)\n        projects_layout.setContentsMargins(0, 0, 0, 0)\n        projects_layout.addWidget(projects_header_widget, 0)\n        projects_layout.addWidget(projects_widget, 1)\n\n        # - Second page - Hierarchy (folders &amp; tasks)\n        hierarchy_page = HierarchyPage(controller, pages_widget)\n\n        pages_layout = QtWidgets.QHBoxLayout(pages_widget)\n        pages_layout.setContentsMargins(0, 0, 0, 0)\n        pages_layout.addWidget(projects_page, 1)\n        pages_layout.addWidget(hierarchy_page, 1)\n\n        # Actions\n        actions_widget = ActionsWidget(controller, content_body)\n\n        # Vertically split Pages and Actions\n        content_body.setContentsMargins(0, 0, 0, 0)\n        content_body.setSizePolicy(\n            QtWidgets.QSizePolicy.Expanding,\n            QtWidgets.QSizePolicy.Expanding\n        )\n        content_body.setOrientation(QtCore.Qt.Vertical)\n        content_body.addWidget(pages_widget)\n        content_body.addWidget(actions_widget)\n\n        # Set useful default sizes and set stretch\n        # for the pages so that is the only one that\n        # stretches on UI resize.\n        content_body.setStretchFactor(0, 10)\n        content_body.setSizes([580, 160])\n\n        # Footer\n        # footer_widget = QtWidgets.QWidget(self)\n        #\n        # action_history = ActionHistory(footer_widget)\n        # action_history.setStatusTip(\"Show Action History\")\n        #\n        # footer_layout = QtWidgets.QHBoxLayout(footer_widget)\n        # footer_layout.setContentsMargins(0, 0, 0, 0)\n        # footer_layout.addWidget(action_history, 0)\n\n        layout = QtWidgets.QVBoxLayout(self)\n        layout.addWidget(content_body, 1)\n        # layout.addWidget(footer_widget, 0)\n\n        actions_refresh_timer = QtCore.QTimer()\n        actions_refresh_timer.setInterval(self.refresh_interval)\n\n        page_slide_anim = QtCore.QVariantAnimation(self)\n        page_slide_anim.setDuration(self.page_side_anim_interval)\n        page_slide_anim.setStartValue(0.0)\n        page_slide_anim.setEndValue(1.0)\n        page_slide_anim.setEasingCurve(QtCore.QEasingCurve.OutQuad)\n\n        refresh_btn.clicked.connect(self._on_refresh_request)\n        projects_widget.refreshed.connect(self._on_projects_refresh)\n\n        actions_refresh_timer.timeout.connect(\n            self._on_actions_refresh_timeout)\n        page_slide_anim.valueChanged.connect(\n            self._on_page_slide_value_changed)\n        page_slide_anim.finished.connect(self._on_page_slide_finished)\n        projects_filter_text.textChanged.connect(\n            self._on_project_filter_change)\n\n        controller.register_event_callback(\n            \"selection.project.changed\",\n            self._on_project_selection_change,\n        )\n        controller.register_event_callback(\n            \"action.trigger.started\",\n            self._on_action_trigger_started,\n        )\n        controller.register_event_callback(\n            \"action.trigger.finished\",\n            self._on_action_trigger_finished,\n        )\n        controller.register_event_callback(\n            \"webaction.trigger.started\",\n            self._on_webaction_trigger_started,\n        )\n        controller.register_event_callback(\n            \"webaction.trigger.finished\",\n            self._on_webaction_trigger_finished,\n        )\n\n        self._overlay_object = overlay_object\n\n        self._controller = controller\n\n        self._is_on_projects_page = True\n        self._window_is_active = False\n        self._refresh_on_activate = False\n        self._selected_project_name = None\n\n        self._pages_widget = pages_widget\n        self._pages_layout = pages_layout\n        self._projects_page = projects_page\n        self._projects_widget = projects_widget\n        self._hierarchy_page = hierarchy_page\n        self._actions_widget = actions_widget\n        # self._action_history = action_history\n\n        self._actions_refresh_timer = actions_refresh_timer\n        self._page_slide_anim = page_slide_anim\n\n        hierarchy_page.setVisible(not self._is_on_projects_page)\n        self.resize(920, 740)\n\n    def showEvent(self, event):\n        super().showEvent(event)\n        self._window_is_active = True\n        if not self._actions_refresh_timer.isActive():\n            self._actions_refresh_timer.start()\n        self._controller.refresh()\n\n    def closeEvent(self, event):\n        super().closeEvent(event)\n        self._window_is_active = False\n        self._actions_refresh_timer.stop()\n\n    def changeEvent(self, event):\n        if event.type() in (\n            QtCore.QEvent.Type.WindowStateChange,\n            QtCore.QEvent.ActivationChange,\n        ):\n            is_active = self.isActiveWindow() and not self.isMinimized()\n            self._window_is_active = is_active\n            if is_active and self._refresh_on_activate:\n                self._refresh_on_activate = False\n                self._on_actions_refresh_timeout()\n                self._actions_refresh_timer.start()\n\n        super().changeEvent(event)\n\n    def _on_actions_refresh_timeout(self):\n        # Stop timer if widget is not visible\n        if self._window_is_active:\n            self._controller.refresh_actions()\n        else:\n            self._refresh_on_activate = True\n\n    def _on_project_selection_change(self, event):\n        project_name = event[\"project_name\"]\n        self._selected_project_name = project_name\n        if not project_name:\n            self._go_to_projects_page()\n\n        elif self._is_on_projects_page:\n            self._go_to_hierarchy_page(project_name)\n\n    def _on_project_filter_change(self, text):\n        self._projects_widget.set_name_filter(text)\n\n    def _on_refresh_request(self):\n        self._controller.refresh()\n\n    def _on_projects_refresh(self):\n        # Refresh only actions on projects page\n        if self._is_on_projects_page:\n            self._actions_widget.refresh()\n            return\n\n        # No projects were found -&gt; go back to projects page\n        if not self._projects_widget.has_content():\n            self._go_to_projects_page()\n            return\n\n        self._hierarchy_page.refresh()\n        self._actions_widget.refresh()\n\n    def _show_toast_message(self, message, success=True, message_id=None):\n        message_type = None\n        if not success:\n            message_type = \"error\"\n\n        self._overlay_object.add_message(\n            message, message_type, message_id=message_id\n        )\n\n    def _on_action_trigger_started(self, event):\n        self._show_toast_message(\n            \"Running: {}\".format(event[\"full_label\"]),\n            message_id=event[\"trigger_id\"],\n        )\n\n    def _on_action_trigger_finished(self, event):\n        action_label = event[\"full_label\"]\n        if event[\"failed\"]:\n            message = f\"Failed to run: {action_label}\"\n        else:\n            message = f\"Finished: {action_label}\"\n        self._show_toast_message(\n            message,\n            not event[\"failed\"],\n            message_id=event[\"trigger_id\"],\n        )\n\n    def _on_webaction_trigger_started(self, event):\n        self._show_toast_message(\n            \"Running: {}\".format(event[\"full_label\"]),\n            message_id=event[\"trigger_id\"],\n        )\n\n    def _on_webaction_trigger_finished(self, event):\n        clipboard_text = event[\"clipboard_text\"]\n        if clipboard_text:\n            clipboard = QtWidgets.QApplication.clipboard()\n            clipboard.setText(clipboard_text)\n\n        action_label = event[\"full_label\"]\n        # Avoid to show exception message\n        if event[\"trigger_failed\"]:\n            self._show_toast_message(\n                f\"Failed to run: {action_label}\",\n                message_id=event[\"trigger_id\"]\n            )\n            return\n\n        # Failed to run webaction, e.g. because of missing webaction handling\n        # - not reported by server\n        if event[\"error_message\"]:\n            self._show_toast_message(\n                event[\"error_message\"],\n                success=False,\n                message_id=event[\"trigger_id\"]\n            )\n            return\n\n        if event[\"message\"]:\n            self._show_toast_message(event[\"message\"], event[\"success\"])\n\n        if event[\"form\"]:\n            self._actions_widget.handle_webaction_form_event(event)\n\n    def _is_page_slide_anim_running(self):\n        return (\n            self._page_slide_anim.state() == QtCore.QAbstractAnimation.Running\n        )\n\n    def _go_to_projects_page(self):\n        if self._is_on_projects_page:\n            return\n\n        # Deselect project in projects widget\n        self._projects_widget.set_selected_project(None)\n        self._is_on_projects_page = True\n        self._hierarchy_page.set_page_visible(False)\n\n        self._start_page_slide_animation()\n\n    def _go_to_hierarchy_page(self, project_name):\n        if not self._is_on_projects_page:\n            return\n        self._is_on_projects_page = False\n        self._hierarchy_page.set_page_visible(True, project_name)\n\n        self._start_page_slide_animation()\n\n    def _start_page_slide_animation(self):\n        if self._is_on_projects_page:\n            direction = QtCore.QAbstractAnimation.Backward\n        else:\n            direction = QtCore.QAbstractAnimation.Forward\n        self._page_slide_anim.setDirection(direction)\n        if self._is_page_slide_anim_running():\n            return\n\n        layout_spacing = self._pages_layout.spacing()\n        if self._is_on_projects_page:\n            hierarchy_geo = self._hierarchy_page.geometry()\n            projects_geo = QtCore.QRect(hierarchy_geo)\n            projects_geo.moveRight(\n                hierarchy_geo.left() - (layout_spacing + 1))\n\n            self._projects_page.setVisible(True)\n\n        else:\n            projects_geo = self._projects_page.geometry()\n            hierarchy_geo = QtCore.QRect(projects_geo)\n            hierarchy_geo.moveLeft(projects_geo.right() + layout_spacing)\n            self._hierarchy_page.setVisible(True)\n\n        while self._pages_layout.count():\n            self._pages_layout.takeAt(0)\n\n        self._projects_page.setGeometry(projects_geo)\n        self._hierarchy_page.setGeometry(hierarchy_geo)\n\n        self._page_slide_anim.start()\n\n    def _on_page_slide_value_changed(self, value):\n        layout_spacing = self._pages_layout.spacing()\n        content_width = self._pages_widget.width() - layout_spacing\n        content_height = self._pages_widget.height()\n\n        # Visible widths of other widgets\n        hierarchy_width = int(content_width * value)\n\n        hierarchy_geo = QtCore.QRect(\n            content_width - hierarchy_width, 0, content_width, content_height\n        )\n        projects_geo = QtCore.QRect(hierarchy_geo)\n        projects_geo.moveRight(hierarchy_geo.left() - (layout_spacing + 1))\n\n        self._projects_page.setGeometry(projects_geo)\n        self._hierarchy_page.setGeometry(hierarchy_geo)\n\n    def _on_page_slide_finished(self):\n        self._pages_layout.addWidget(self._projects_page, 1)\n        self._pages_layout.addWidget(self._hierarchy_page, 1)\n        self._projects_page.setVisible(self._is_on_projects_page)\n        self._hierarchy_page.setVisible(not self._is_on_projects_page)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/launcher/ui/workfiles_page.html","title":"workfiles_page","text":""},{"location":"autoapi/client/ayon_core/tools/loader/index.html","title":"loader","text":""},{"location":"autoapi/client/ayon_core/tools/loader/index.html#client.ayon_core.tools.loader.LoaderController","title":"<code>LoaderController</code>","text":"<p>               Bases: <code>BackendLoaderController</code>, <code>FrontendLoaderController</code></p> <p>Parameters:</p> Name Type Description Default <code>host</code> <code>Optional[AbstractHost]</code> <p>Host object. Defaults to None.</p> <code>None</code> Source code in <code>client/ayon_core/tools/loader/control.py</code> <pre><code>class LoaderController(BackendLoaderController, FrontendLoaderController):\n    \"\"\"\n\n    Args:\n        host (Optional[AbstractHost]): Host object. Defaults to None.\n    \"\"\"\n\n    def __init__(self, host: Optional[AbstractHost] = None) -&gt; None:\n        self._log = None\n        self._host = host\n\n        self._event_system = self._create_event_system()\n\n        self._project_settings = {}\n\n        self._project_anatomy_cache = NestedCacheItem(\n            levels=1, lifetime=60)\n        self._loaded_products_cache = CacheItem(\n            default_factory=set, lifetime=60)\n\n        self._selection_model = SelectionModel(self)\n        self._expected_selection = ExpectedSelection(self)\n        self._projects_model = ProjectsModel(self)\n        self._hierarchy_model = HierarchyModel(self)\n        self._products_model = ProductsModel(self)\n        self._loader_actions_model = LoaderActionsModel(self)\n        self._thumbnails_model = ThumbnailsModel()\n        self._sitesync_model = SiteSyncModel(self)\n        self._users_model = UsersModel(self)\n        self._settings_model = SettingsModel()\n\n    @property\n    def log(self):\n        if self._log is None:\n            self._log = logging.getLogger(self.__class__.__name__)\n        return self._log\n\n    # ---------------------------------\n    # Implementation of abstract methods\n    # ---------------------------------\n    def get_window_subtitle(self) -&gt; Optional[str]:\n        if self._host is None:\n            return None\n        return self._host.name\n\n    # Events system\n    def emit_event(self, topic, data=None, source=None):\n        \"\"\"Use implemented event system to trigger event.\"\"\"\n\n        if data is None:\n            data = {}\n        self._event_system.emit(topic, data, source)\n\n    def register_event_callback(self, topic, callback):\n        self._event_system.add_callback(topic, callback)\n\n    def reset(self):\n        self._emit_event(\"controller.reset.started\")\n\n        project_name = self.get_selected_project_name()\n        folder_ids = self.get_selected_folder_ids()\n\n        self._project_settings = {}\n\n        self._project_anatomy_cache.reset()\n        self._loaded_products_cache.reset()\n\n        self._products_model.reset()\n        self._hierarchy_model.reset()\n        self._loader_actions_model.reset()\n        self._projects_model.reset()\n        self._thumbnails_model.reset()\n        self._sitesync_model.reset()\n        self._users_model.reset()\n        self._settings_model.reset()\n\n        self._projects_model.refresh()\n\n        if not project_name and not folder_ids:\n            context = self.get_current_context()\n            project_name = context[\"project_name\"]\n            folder_id = context[\"folder_id\"]\n            self.set_expected_selection(project_name, folder_id)\n\n        self._emit_event(\"controller.reset.finished\")\n\n    # Expected selection helpers\n    def get_expected_selection_data(self):\n        return self._expected_selection.get_expected_selection_data()\n\n    def set_expected_selection(self, project_name, folder_id):\n        self._expected_selection.set_expected_selection(\n            project_name, folder_id\n        )\n\n    def expected_project_selected(self, project_name):\n        self._expected_selection.expected_project_selected(project_name)\n\n    def expected_folder_selected(self, folder_id):\n        self._expected_selection.expected_folder_selected(folder_id)\n\n    # Entity model wrappers\n    def get_project_items(self, sender=None):\n        return self._projects_model.get_project_items(sender)\n\n    def get_folder_type_items(self, project_name, sender=None):\n        return self._projects_model.get_folder_type_items(\n            project_name, sender\n        )\n\n    def get_project_status_items(self, project_name, sender=None):\n        return self._projects_model.get_project_status_items(\n            project_name, sender\n        )\n\n    def get_product_type_icons_mapping(\n        self, project_name: Optional[str]\n    ) -&gt; ProductTypeIconMapping:\n        return self._projects_model.get_product_type_icons_mapping(\n            project_name\n        )\n\n    def get_folder_items(self, project_name, sender=None):\n        return self._hierarchy_model.get_folder_items(project_name, sender)\n\n    def get_task_items(self, project_name, folder_ids, sender=None):\n        output = []\n        for folder_id in folder_ids:\n            output.extend(self._hierarchy_model.get_task_items(\n                project_name, folder_id, sender\n            ))\n        return output\n\n    def get_task_type_items(self, project_name, sender=None):\n        return self._projects_model.get_task_type_items(\n            project_name, sender\n        )\n\n    def get_folder_labels(self, project_name, folder_ids):\n        folder_items_by_id = self._hierarchy_model.get_folder_items_by_id(\n            project_name, folder_ids\n        )\n        output = {}\n        for folder_id, folder_item in folder_items_by_id.items():\n            label = None\n            if folder_item is not None:\n                label = folder_item.label\n            output[folder_id] = label\n        return output\n\n    def get_my_tasks_entity_ids(\n        self, project_name: str\n    ) -&gt; dict[str, list[str]]:\n        username = self._users_model.get_current_username()\n        assignees = []\n        if username:\n            assignees.append(username)\n        return self._hierarchy_model.get_entity_ids_for_assignees(\n            project_name, assignees\n        )\n\n    def get_available_tags_by_entity_type(\n        self, project_name: str\n    ) -&gt; dict[str, list[str]]:\n        return self._hierarchy_model.get_available_tags_by_entity_type(\n            project_name\n        )\n\n    def get_project_settings(self, project_name: str | None) -&gt; dict:\n        return self._settings_model.get_settings(project_name)\n\n    def get_project_anatomy_tags(self, project_name: str) -&gt; list[TagItem]:\n        return self._projects_model.get_project_anatomy_tags(project_name)\n\n    def get_product_items(self, project_name, folder_ids, sender=None):\n        return self._products_model.get_product_items(\n            project_name, folder_ids, sender)\n\n    def get_product_item(self, project_name, product_id):\n        return self._products_model.get_product_item(\n            project_name, product_id\n        )\n\n    def get_product_type_items(self, project_name):\n        return self._products_model.get_product_type_items(project_name)\n\n    def get_representation_items(\n        self, project_name, version_ids, sender=None\n    ):\n        return self._products_model.get_repre_items(\n            project_name, version_ids, sender\n        )\n\n    def get_versions_representation_count(\n        self, project_name, version_ids, sender=None\n    ):\n        return self._products_model.get_versions_repre_count(\n            project_name, version_ids, sender\n        )\n\n    def get_folder_thumbnail_ids(self, project_name, folder_ids):\n        return self._thumbnails_model.get_folder_thumbnail_ids(\n            project_name, folder_ids\n        )\n\n    def get_version_thumbnail_ids(self, project_name, version_ids):\n        return self._thumbnails_model.get_version_thumbnail_ids(\n            project_name, version_ids\n        )\n\n    def get_thumbnail_paths(\n        self,\n        project_name,\n        entity_type,\n        entity_ids,\n    ):\n        return self._thumbnails_model.get_thumbnail_paths(\n            project_name, entity_type, entity_ids\n        )\n\n    def change_products_group(self, project_name, product_ids, group_name):\n        self._products_model.change_products_group(\n            project_name, product_ids, group_name\n        )\n\n    def get_action_items(\n        self,\n        project_name: str,\n        entity_ids: set[str],\n        entity_type: str,\n    ) -&gt; list[ActionItem]:\n        action_items = self._loader_actions_model.get_action_items(\n            project_name, entity_ids, entity_type\n        )\n\n        site_sync_items = self._sitesync_model.get_sitesync_action_items(\n            project_name, entity_ids, entity_type\n        )\n        action_items.extend(site_sync_items)\n        return action_items\n\n    def trigger_action_item(\n        self,\n        identifier: str,\n        project_name: str,\n        selected_ids: set[str],\n        selected_entity_type: str,\n        data: Optional[dict[str, Any]],\n        options: dict[str, Any],\n        form_values: dict[str, Any],\n    ):\n        if self._sitesync_model.is_sitesync_action(identifier):\n            self._sitesync_model.trigger_action_item(\n                project_name,\n                data,\n            )\n            return\n\n        self._loader_actions_model.trigger_action_item(\n            identifier=identifier,\n            project_name=project_name,\n            selected_ids=selected_ids,\n            selected_entity_type=selected_entity_type,\n            data=data,\n            options=options,\n            form_values=form_values,\n        )\n\n    # Selection model wrappers\n    def get_selected_project_name(self):\n        return self._selection_model.get_selected_project_name()\n\n    def set_selected_project(self, project_name):\n        self._selection_model.set_selected_project(project_name)\n\n    # Selection model wrappers\n    def get_selected_folder_ids(self):\n        return self._selection_model.get_selected_folder_ids()\n\n    def set_selected_folders(self, folder_ids):\n        self._selection_model.set_selected_folders(folder_ids)\n\n    def get_selected_task_ids(self):\n        return self._selection_model.get_selected_task_ids()\n\n    def set_selected_tasks(self, task_ids):\n        self._selection_model.set_selected_tasks(task_ids)\n\n    def get_selected_version_ids(self):\n        return self._selection_model.get_selected_version_ids()\n\n    def set_selected_versions(self, version_ids):\n        self._selection_model.set_selected_versions(version_ids)\n\n    def get_selected_representation_ids(self):\n        return self._selection_model.get_selected_representation_ids()\n\n    def set_selected_representations(self, repre_ids):\n        self._selection_model.set_selected_representations(repre_ids)\n\n    def fill_root_in_source(self, source):\n        project_name = self.get_selected_project_name()\n        anatomy = self._get_project_anatomy(project_name)\n        if anatomy is None:\n            return source\n\n        try:\n            return anatomy.fill_root(source)\n        except Exception:\n            return source\n\n    def get_current_context(self):\n        if self._host is None:\n            return {\n                \"project_name\": None,\n                \"folder_id\": None,\n                \"task_name\": None,\n            }\n        if hasattr(self._host, \"get_current_context\"):\n            context = self._host.get_current_context()\n        else:\n            context = get_current_context()\n        folder_id = None\n        project_name = context.get(\"project_name\")\n        folder_path = context.get(\"folder_path\")\n        if project_name and folder_path:\n            folder_entity = ayon_api.get_folder_by_path(\n                project_name, folder_path, fields=[\"id\"]\n            )\n            if folder_entity:\n                folder_id = folder_entity[\"id\"]\n        return {\n            \"project_name\": project_name,\n            \"folder_id\": folder_id,\n            \"task_name\": context.get(\"task_name\"),\n        }\n\n    def get_loaded_product_ids(self):\n        if self._host is None:\n            return set()\n\n        context = self.get_current_context()\n        project_name = context[\"project_name\"]\n        if not project_name:\n            return set()\n\n        if not self._loaded_products_cache.is_valid:\n            try:\n                if isinstance(self._host, ILoadHost):\n                    containers = self._host.get_containers()\n                else:\n                    containers = self._host.ls()\n\n            except BaseException:\n                self.log.error(\n                    \"Failed to collect loaded products.\", exc_info=True\n                )\n                containers = []\n\n            repre_ids = set()\n            for container in containers:\n                try:\n                    repre_id = container.get(\"representation\")\n                    # Ignore invalid representation ids.\n                    # - invalid representation ids may be available if e.g. is\n                    #   opened scene from OpenPype whe 'ObjectId' was used\n                    #   instead of 'uuid'.\n                    # NOTE: Server call would crash if there is any invalid id.\n                    #   That would cause crash we won't get any information.\n                    uuid.UUID(repre_id)\n                    repre_ids.add(repre_id)\n                except (ValueError, TypeError, AttributeError):\n                    pass\n\n            product_ids = self._products_model.get_product_ids_by_repre_ids(\n                project_name, repre_ids\n            )\n            self._loaded_products_cache.update_data(product_ids)\n        return self._loaded_products_cache.get_data()\n\n    def is_sitesync_enabled(self, project_name=None):\n        return self._sitesync_model.is_sitesync_enabled(project_name)\n\n    def get_active_site_icon_def(self, project_name):\n        return self._sitesync_model.get_active_site_icon_def(project_name)\n\n    def get_remote_site_icon_def(self, project_name):\n        return self._sitesync_model.get_remote_site_icon_def(project_name)\n\n    def get_active_site(self, project_name):\n        return self._sitesync_model.get_active_site(project_name)\n\n    def get_remote_site(self, project_name):\n        return self._sitesync_model.get_remote_site(project_name)\n\n    def get_version_sync_availability(self, project_name, version_ids):\n        return self._sitesync_model.get_version_sync_availability(\n            project_name, version_ids\n        )\n\n    def get_representations_sync_status(\n        self, project_name, representation_ids\n    ):\n        return self._sitesync_model.get_representations_sync_status(\n            project_name, representation_ids\n        )\n\n    def is_loaded_products_supported(self):\n        return self._host is not None\n\n    def is_standard_projects_filter_enabled(self):\n        return self._host is not None\n\n    def get_product_types_filter(self):\n        output = ProductTypesFilter(\n            is_allow_list=False,\n            product_types=[]\n        )\n        # Without host is not determined context\n        if self._host is None:\n            return output\n\n        context = self.get_current_context()\n        project_name = context.get(\"project_name\")\n        if not project_name:\n            return output\n        settings = self.get_project_settings(project_name)\n        profiles = (\n            settings\n            [\"core\"]\n            [\"tools\"]\n            [\"loader\"]\n            [\"product_type_filter_profiles\"]\n        )\n        if not profiles:\n            return output\n\n        folder_id = context.get(\"folder_id\")\n        task_name = context.get(\"task_name\")\n        task_type = None\n        if folder_id and task_name:\n            task_entity = ayon_api.get_task_by_name(\n                project_name,\n                folder_id,\n                task_name,\n                fields={\"taskType\"}\n            )\n            if task_entity:\n                task_type = task_entity.get(\"taskType\")\n\n        host_name = getattr(self._host, \"name\", get_current_host_name())\n        profile = filter_profiles(\n            profiles,\n            {\n                \"host_names\": host_name,\n                \"task_types\": task_type,\n            }\n        )\n        if profile:\n            # TODO remove 'is_include' after release '0.4.3'\n            is_allow_list = profile.get(\"is_include\")\n            if is_allow_list is None:\n                is_allow_list = profile[\"filter_type\"] == \"is_allow_list\"\n            output = ProductTypesFilter(\n                is_allow_list=is_allow_list,\n                product_types=profile[\"filter_product_types\"]\n            )\n        return output\n\n    def _create_event_system(self):\n        return QueuedEventSystem()\n\n    def _emit_event(self, topic, data=None):\n        self._event_system.emit(topic, data or {}, \"controller\")\n\n    def _get_project_anatomy(self, project_name):\n        if not project_name:\n            return None\n        cache = self._project_anatomy_cache[project_name]\n        if not cache.is_valid:\n            cache.update_data(Anatomy(project_name))\n        return cache.get_data()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/index.html#client.ayon_core.tools.loader.LoaderController.emit_event","title":"<code>emit_event(topic, data=None, source=None)</code>","text":"<p>Use implemented event system to trigger event.</p> Source code in <code>client/ayon_core/tools/loader/control.py</code> <pre><code>def emit_event(self, topic, data=None, source=None):\n    \"\"\"Use implemented event system to trigger event.\"\"\"\n\n    if data is None:\n        data = {}\n    self._event_system.emit(topic, data, source)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html","title":"abstract","text":"<p>Abstract base classes for loader tool.</p>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.ActionItem","title":"<code>ActionItem</code>","text":"<p>Action item that can be triggered.</p> <p>Action item is defined for a specific context. To trigger the action use 'identifier' and context, it necessary also use 'options'.</p> <p>Parameters:</p> Name Type Description Default <code>identifier</code> <code>str</code> <p>Action identifier.</p> required <code>label</code> <code>str</code> <p>Action label.</p> required <code>group_label</code> <code>Optional[str]</code> <p>Group label.</p> required <code>icon</code> <code>IconBase | dict[str, Any] | None</code> <p>Action icon definition.</p> required <code>tooltip</code> <code>Optional[str]</code> <p>Action tooltip.</p> required <code>order</code> <code>int</code> <p>Action order.</p> required <code>data</code> <code>Optional[dict[str, Any]]</code> <p>Additional action data.</p> required <code>options</code> <code>Union[list[AbstractAttrDef], list[QArgument]]</code> <p>Action options. Note: 'qargparse' is considered as deprecated.</p> required Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>class ActionItem:\n    \"\"\"Action item that can be triggered.\n\n    Action item is defined for a specific context. To trigger the action\n    use 'identifier' and context, it necessary also use 'options'.\n\n    Args:\n        identifier (str): Action identifier.\n        label (str): Action label.\n        group_label (Optional[str]): Group label.\n        icon (IconBase | dict[str, Any] | None): Action icon definition.\n        tooltip (Optional[str]): Action tooltip.\n        order (int): Action order.\n        data (Optional[dict[str, Any]]): Additional action data.\n        options (Union[list[AbstractAttrDef], list[qargparse.QArgument]]):\n            Action options. Note: 'qargparse' is considered as deprecated.\n\n    \"\"\"\n    def __init__(\n        self,\n        identifier: str,\n        label: str,\n        group_label: Optional[str],\n        icon: IconBase | dict[str, Any] | None,\n        tooltip: Optional[str],\n        order: int,\n        data: Optional[dict[str, Any]],\n        options: Optional[list],\n    ):\n        self.identifier = identifier\n        self.label = label\n        self.group_label = group_label\n        self.icon = icon\n        self.tooltip = tooltip\n        self.data = data\n        self.order = order\n        self.options = options\n\n    def _options_to_data(self):\n        options = self.options\n        if not options:\n            return options\n        if isinstance(options[0], AbstractAttrDef):\n            return serialize_attr_defs(options)\n        # NOTE: Data conversion is not used by default in loader tool. But for\n        #   future development of detached UI tools it would be better to be\n        #   prepared for it.\n        raise NotImplementedError(\n            f\"{self.__class__.__name__}.to_data is not implemented.\"\n            \" Use Attribute definitions from 'ayon_core.lib'\"\n            \" instead of 'qargparse'.\"\n        )\n\n    def to_data(self) -&gt; dict[str, Any]:\n        options = self._options_to_data()\n        return {\n            \"identifier\": self.identifier,\n            \"label\": self.label,\n            \"group_label\": self.group_label,\n            \"icon\": self.icon,\n            \"tooltip\": self.tooltip,\n            \"order\": self.order,\n            \"data\": self.data,\n            \"options\": options,\n        }\n\n    @classmethod\n    def from_data(cls, data) -&gt; \"ActionItem\":\n        options = data[\"options\"]\n        if options:\n            options = deserialize_attr_defs(options)\n        data[\"options\"] = options\n        return cls(**data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.BackendLoaderController","title":"<code>BackendLoaderController</code>","text":"<p>               Bases: <code>_BaseLoaderController</code></p> <p>Backend loader controller abstraction.</p> <p>What backend logic requires from a controller for proper logic.</p> Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>class BackendLoaderController(_BaseLoaderController):\n    \"\"\"Backend loader controller abstraction.\n\n    What backend logic requires from a controller for proper logic.\n    \"\"\"\n\n    @abstractmethod\n    def emit_event(self, topic, data=None, source=None):\n        \"\"\"Emit event with a certain topic, data and source.\n\n        The event should be sent to both frontend and backend.\n\n        Args:\n            topic (str): Event topic name.\n            data (Optional[dict[str, Any]]): Event data.\n            source (Optional[str]): Event source.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_loaded_product_ids(self):\n        \"\"\"Return set of loaded product ids.\n\n        Returns:\n            set[str]: Set of loaded product ids.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_project_settings(self, project_name: str | None) -&gt; dict:\n        pass\n\n    @abstractmethod\n    def get_product_type_icons_mapping(\n        self, project_name: Optional[str]\n    ) -&gt; ProductTypeIconMapping:\n        \"\"\"Product type icons mapping.\n\n        Returns:\n            ProductTypeIconMapping: Product type icons mapping.\n\n        \"\"\"\n        pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.BackendLoaderController.emit_event","title":"<code>emit_event(topic, data=None, source=None)</code>  <code>abstractmethod</code>","text":"<p>Emit event with a certain topic, data and source.</p> <p>The event should be sent to both frontend and backend.</p> <p>Parameters:</p> Name Type Description Default <code>topic</code> <code>str</code> <p>Event topic name.</p> required <code>data</code> <code>Optional[dict[str, Any]]</code> <p>Event data.</p> <code>None</code> <code>source</code> <code>Optional[str]</code> <p>Event source.</p> <code>None</code> Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>@abstractmethod\ndef emit_event(self, topic, data=None, source=None):\n    \"\"\"Emit event with a certain topic, data and source.\n\n    The event should be sent to both frontend and backend.\n\n    Args:\n        topic (str): Event topic name.\n        data (Optional[dict[str, Any]]): Event data.\n        source (Optional[str]): Event source.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.BackendLoaderController.get_loaded_product_ids","title":"<code>get_loaded_product_ids()</code>  <code>abstractmethod</code>","text":"<p>Return set of loaded product ids.</p> <p>Returns:</p> Type Description <p>set[str]: Set of loaded product ids.</p> Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>@abstractmethod\ndef get_loaded_product_ids(self):\n    \"\"\"Return set of loaded product ids.\n\n    Returns:\n        set[str]: Set of loaded product ids.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.BackendLoaderController.get_product_type_icons_mapping","title":"<code>get_product_type_icons_mapping(project_name)</code>  <code>abstractmethod</code>","text":"<p>Product type icons mapping.</p> <p>Returns:</p> Name Type Description <code>ProductTypeIconMapping</code> <code>ProductTypeIconMapping</code> <p>Product type icons mapping.</p> Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>@abstractmethod\ndef get_product_type_icons_mapping(\n    self, project_name: Optional[str]\n) -&gt; ProductTypeIconMapping:\n    \"\"\"Product type icons mapping.\n\n    Returns:\n        ProductTypeIconMapping: Product type icons mapping.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.FrontendLoaderController","title":"<code>FrontendLoaderController</code>","text":"<p>               Bases: <code>_BaseLoaderController</code></p> Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>class FrontendLoaderController(_BaseLoaderController):\n    @abstractmethod\n    def get_window_subtitle(self) -&gt; Optional[str]:\n        \"\"\"Get window subtitle.\n\n        Returns:\n            Optional[str]: Window subtitle.\n\n        \"\"\"\n\n    @abstractmethod\n    def register_event_callback(self, topic, callback):\n        \"\"\"Register callback for an event topic.\n\n        Args:\n            topic (str): Event topic name.\n            callback (func): Callback triggered when the event is emitted.\n        \"\"\"\n\n        pass\n\n    # Expected selection helpers\n    @abstractmethod\n    def expected_project_selected(self, project_name):\n        \"\"\"Expected project was selected in frontend.\n\n        Args:\n            project_name (str): Project name.\n        \"\"\"\n\n        pass\n\n    @abstractmethod\n    def expected_folder_selected(self, folder_id):\n        \"\"\"Expected folder was selected in frontend.\n\n        Args:\n            folder_id (str): Folder id.\n        \"\"\"\n\n        pass\n\n    # Model wrapper calls\n    @abstractmethod\n    def get_project_items(self, sender=None):\n        \"\"\"Items for all projects available on server.\n\n        Triggers event topics \"projects.refresh.started\" and\n        \"projects.refresh.finished\" with data:\n            {\n                \"sender\": sender\n            }\n\n        Notes:\n            Filtering of projects is done in UI.\n\n        Args:\n            sender (Optional[str]): Sender who requested the items.\n\n        Returns:\n            list[ProjectItem]: List of project items.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_project_anatomy_tags(self, project_name: str) -&gt; list[TagItem]:\n        \"\"\"Tag items defined on project anatomy.\n\n        Args:\n            project_name (str): Project name.\n\n        Returns:\n            list[TagItem]: Tag definition items.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_folder_type_items(self, project_name, sender=None):\n        \"\"\"Folder type items for a project.\n\n        This function may trigger events with topics\n        'projects.folder_types.refresh.started' and\n        'projects.folder_types.refresh.finished' which will contain 'sender'\n        value in data.\n        That may help to avoid re-refresh of items in UI elements.\n\n        Args:\n            project_name (str): Project name.\n            sender (str): Who requested folder type items.\n\n        Returns:\n            list[FolderTypeItem]: Folder type information.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_task_items(\n        self,\n        project_name: str,\n        folder_ids: Iterable[str],\n        sender: Optional[str] = None,\n    ) -&gt; list[TaskItem]:\n        \"\"\"Task items for folder ids.\n\n        Args:\n            project_name (str): Project name.\n            folder_ids (Iterable[str]): Folder ids.\n            sender (Optional[str]): Sender who requested the items.\n\n        Returns:\n            list[TaskItem]: List of task items.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_task_type_items(self, project_name, sender=None):\n        \"\"\"Task type items for a project.\n\n        This function may trigger events with topics\n        'projects.task_types.refresh.started' and\n        'projects.task_types.refresh.finished' which will contain 'sender'\n        value in data.\n        That may help to avoid re-refresh of items in UI elements.\n\n        Args:\n            project_name (str): Project name.\n            sender (str): Who requested task type items.\n\n        Returns:\n            list[TaskTypeItem]: Task type information.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_folder_labels(self, project_name, folder_ids):\n        \"\"\"Get folder labels for folder ids.\n\n        Args:\n            project_name (str): Project name.\n            folder_ids (Iterable[str]): Folder ids.\n\n        Returns:\n            dict[str, Optional[str]]: Folder labels by folder id.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_my_tasks_entity_ids(\n        self, project_name: str\n    ) -&gt; dict[str, list[str]]:\n        \"\"\"Get entity ids for my tasks.\n\n        Args:\n            project_name (str): Project name.\n\n        Returns:\n            dict[str, list[str]]: Folder and task ids.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_available_tags_by_entity_type(\n        self, project_name: str\n    ) -&gt; dict[str, list[str]]:\n        \"\"\"Get available tags by entity type.\n\n        Args:\n            project_name (str): Project name.\n\n        Returns:\n            dict[str, list[str]]: Available tags by entity type.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_project_status_items(self, project_name, sender=None):\n        \"\"\"Items for all projects available on server.\n\n        Triggers event topics \"projects.statuses.refresh.started\" and\n        \"projects.statuses.refresh.finished\" with data:\n            {\n                \"sender\": sender,\n                \"project_name\": project_name\n            }\n\n        Args:\n            project_name (Union[str, None]): Project name.\n            sender (Optional[str]): Sender who requested the items.\n\n        Returns:\n            list[StatusItem]: List of status items.\n        \"\"\"\n\n        pass\n\n    @abstractmethod\n    def get_product_items(self, project_name, folder_ids, sender=None):\n        \"\"\"Product items for folder ids.\n\n        Triggers event topics \"products.refresh.started\" and\n        \"products.refresh.finished\" with data:\n            {\n                \"project_name\": project_name,\n                \"folder_ids\": folder_ids,\n                \"sender\": sender\n            }\n\n        Args:\n            project_name (str): Project name.\n            folder_ids (Iterable[str]): Folder ids.\n            sender (Optional[str]): Sender who requested the items.\n\n        Returns:\n            list[ProductItem]: List of product items.\n        \"\"\"\n\n        pass\n\n    @abstractmethod\n    def get_product_item(self, project_name, product_id):\n        \"\"\"Receive single product item.\n\n        Args:\n            project_name (str): Project name.\n            product_id (str): Product id.\n\n        Returns:\n             Union[ProductItem, None]: Product info or None if not found.\n        \"\"\"\n\n        pass\n\n    @abstractmethod\n    def get_product_type_items(self, project_name):\n        \"\"\"Product type items for a project.\n\n        Product types have defined if are checked for filtering or not.\n\n        Args:\n            project_name (Union[str, None]): Project name.\n\n        Returns:\n            list[ProductTypeItem]: List of product type items for a project.\n        \"\"\"\n\n        pass\n\n    @abstractmethod\n    def get_representation_items(\n        self, project_name, version_ids, sender=None\n    ):\n        \"\"\"Representation items for version ids.\n\n        Triggers event topics \"model.representations.refresh.started\" and\n        \"model.representations.refresh.finished\" with data:\n            {\n                \"project_name\": project_name,\n                \"version_ids\": version_ids,\n                \"sender\": sender\n            }\n\n        Args:\n            project_name (str): Project name.\n            version_ids (Iterable[str]): Version ids.\n            sender (Optional[str]): Sender who requested the items.\n\n        Returns:\n            list[RepreItem]: List of representation items.\n        \"\"\"\n\n        pass\n\n    @abstractmethod\n    def get_version_thumbnail_ids(self, project_name, version_ids):\n        \"\"\"Get thumbnail ids for version ids.\n\n        Args:\n            project_name (str): Project name.\n            version_ids (Iterable[str]): Version ids.\n\n        Returns:\n            dict[str, Union[str, Any]]: Thumbnail id by version id.\n        \"\"\"\n\n        pass\n\n    @abstractmethod\n    def get_folder_thumbnail_ids(self, project_name, folder_ids):\n        \"\"\"Get thumbnail ids for folder ids.\n\n        Args:\n            project_name (str): Project name.\n            folder_ids (Iterable[str]): Folder ids.\n\n        Returns:\n            dict[str, Union[str, Any]]: Thumbnail id by folder id.\n        \"\"\"\n\n        pass\n\n    @abstractmethod\n    def get_versions_representation_count(\n        self, project_name, version_ids, sender=None\n    ):\n        \"\"\"\n        Args:\n            project_name (str): Project name.\n            version_ids (Iterable[str]): Version ids.\n            sender (Optional[str]): Sender who requested the items.\n\n        Returns:\n            dict[str, int]: Representation count by version id.\n        \"\"\"\n\n        pass\n\n    @abstractmethod\n    def get_thumbnail_paths(\n        self,\n        project_name,\n        entity_type,\n        entity_ids\n    ):\n        \"\"\"Get thumbnail path for thumbnail id.\n\n        This method should get a path to a thumbnail based on thumbnail id.\n        Which probably means to download the thumbnail from server and store\n        it locally.\n\n        Args:\n            project_name (str): Project name.\n            entity_type (str): Entity type.\n            entity_ids (set[str]): Entity ids.\n\n        Returns:\n            dict[str, Union[str, None]]: Thumbnail path by entity id.\n        \"\"\"\n\n        pass\n\n    # Selection model wrapper calls\n    @abstractmethod\n    def get_selected_project_name(self):\n        \"\"\"Get selected project name.\n\n        The information is based on last selection from UI.\n\n        Returns:\n            Union[str, None]: Selected project name.\n        \"\"\"\n\n        pass\n\n    @abstractmethod\n    def get_selected_folder_ids(self):\n        \"\"\"Get selected folder ids.\n\n        The information is based on last selection from UI.\n\n        Returns:\n            list[str]: Selected folder ids.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_selected_task_ids(self):\n        \"\"\"Get selected task ids.\n\n        The information is based on last selection from UI.\n\n        Returns:\n            list[str]: Selected folder ids.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def set_selected_tasks(self, task_ids):\n        \"\"\"Set selected tasks.\n\n        Args:\n            task_ids (Iterable[str]): Selected task ids.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_selected_version_ids(self):\n        \"\"\"Get selected version ids.\n\n        The information is based on last selection from UI.\n\n        Returns:\n            list[str]: Selected version ids.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_selected_representation_ids(self):\n        \"\"\"Get selected representation ids.\n\n        The information is based on last selection from UI.\n\n        Returns:\n            list[str]: Selected representation ids.\n        \"\"\"\n\n        pass\n\n    @abstractmethod\n    def set_selected_project(self, project_name):\n        \"\"\"Set selected project.\n\n        Project selection changed in UI. Method triggers event with topic\n        \"selection.project.changed\" with data:\n            {\n                \"project_name\": self._project_name\n            }\n\n        Args:\n            project_name (Union[str, None]): Selected project name.\n        \"\"\"\n\n        pass\n\n    @abstractmethod\n    def set_selected_folders(self, folder_ids):\n        \"\"\"Set selected folders.\n\n        Folder selection changed in UI. Method triggers event with topic\n        \"selection.folders.changed\" with data:\n            {\n                \"project_name\": project_name,\n                \"folder_ids\": folder_ids\n            }\n\n        Args:\n            folder_ids (Iterable[str]): Selected folder ids.\n        \"\"\"\n\n        pass\n\n    @abstractmethod\n    def set_selected_versions(self, version_ids):\n        \"\"\"Set selected versions.\n\n        Version selection changed in UI. Method triggers event with topic\n        \"selection.versions.changed\" with data:\n            {\n                \"project_name\": project_name,\n                \"folder_ids\": folder_ids,\n                \"version_ids\": version_ids\n            }\n\n        Args:\n            version_ids (Iterable[str]): Selected version ids.\n        \"\"\"\n\n        pass\n\n    @abstractmethod\n    def set_selected_representations(self, repre_ids):\n        \"\"\"Set selected representations.\n\n        Representation selection changed in UI. Method triggers event with\n        topic \"selection.representations.changed\" with data:\n            {\n                \"project_name\": project_name,\n                \"folder_ids\": folder_ids,\n                \"version_ids\": version_ids,\n                \"representation_ids\": representation_ids\n            }\n\n        Args:\n            repre_ids (Iterable[str]): Selected representation ids.\n        \"\"\"\n\n        pass\n\n    # Load action items\n    @abstractmethod\n    def get_action_items(\n        self,\n        project_name: str,\n        entity_ids: set[str],\n        entity_type: str,\n    ) -&gt; list[ActionItem]:\n        \"\"\"Action items for versions selection.\n\n        Args:\n            project_name (str): Project name.\n            entity_ids (set[str]): Entity ids.\n            entity_type (str): Entity type.\n\n        Returns:\n            list[ActionItem]: List of action items.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def trigger_action_item(\n        self,\n        identifier: str,\n        project_name: str,\n        selected_ids: set[str],\n        selected_entity_type: str,\n        data: Optional[dict[str, Any]],\n        options: dict[str, Any],\n        form_values: dict[str, Any],\n    ):\n        \"\"\"Trigger action item.\n\n        Triggers event \"load.started\" with data:\n            {\n                \"identifier\": identifier,\n                \"id\": &lt;Random UUID&gt;,\n            }\n\n        And triggers \"load.finished\" with data:\n            {\n                \"identifier\": identifier,\n                \"id\": &lt;Random UUID&gt;,\n                \"error_info\": [...],\n            }\n\n        Args:\n            identifier (sttr): Plugin identifier.\n            project_name (str): Project name.\n            selected_ids (set[str]): Selected entity ids.\n            selected_entity_type (str): Selected entity type.\n            data (Optional[dict[str, Any]]): Additional action item data.\n            options (dict[str, Any]): Action option values from UI.\n            form_values (dict[str, Any]): Action form values from UI.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def change_products_group(\n        self,\n        project_name: str,\n        product_ids: set[str],\n        group_name: str,\n    ):\n        \"\"\"Change group of products.\n\n        Triggers event \"products.group.changed\" with data:\n            {\n                \"project_name\": project_name,\n                \"folder_ids\": folder_ids,\n                \"product_ids\": product_ids,\n                \"group_name\": group_name,\n            }\n\n        Args:\n            project_name (str): Project name.\n            product_ids (Iterable[str]): Product ids.\n            group_name (str): New group name.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def fill_root_in_source(self, source):\n        \"\"\"Fill root in source path.\n\n        Args:\n            source (Union[str, None]): Source of a published version. Usually\n                rootless workfile path.\n        \"\"\"\n\n        pass\n\n    # NOTE: Methods 'is_loaded_products_supported' and\n    #   'is_standard_projects_filter_enabled' are both based on being in host\n    #   or not. Maybe we could implement only single method 'is_in_host'?\n    @abstractmethod\n    def is_loaded_products_supported(self):\n        \"\"\"Is capable to get information about loaded products.\n\n        Returns:\n            bool: True if it is supported.\n        \"\"\"\n\n        pass\n\n    @abstractmethod\n    def is_standard_projects_filter_enabled(self):\n        \"\"\"Is standard projects filter enabled.\n\n        This is used for filtering out when loader tool is used in a host. In\n        that case only current project and library projects should be shown.\n\n        Returns:\n            bool: Frontend should filter out non-library projects, except\n                current context project.\n        \"\"\"\n\n        pass\n\n    # Site sync functions\n    @abstractmethod\n    def is_sitesync_enabled(self, project_name=None):\n        \"\"\"Is site sync enabled.\n\n        Site sync addon can be enabled but can be disabled per project.\n\n        When asked for enabled state without project name, it should return\n            True if site sync addon is available and enabled.\n\n        Args:\n            project_name (Optional[str]): Project name.\n\n        Returns:\n            bool: True if site sync is enabled.\n        \"\"\"\n\n        pass\n\n    @abstractmethod\n    def get_active_site_icon_def(self, project_name):\n        \"\"\"Active site icon definition.\n\n        Args:\n            project_name (Union[str, None]): Project name.\n\n        Returns:\n            Union[dict[str, Any], None]: Icon definition or None if site sync\n                is not enabled for the project.\n        \"\"\"\n\n        pass\n\n    @abstractmethod\n    def get_remote_site_icon_def(self, project_name):\n        \"\"\"Remote site icon definition.\n\n        Args:\n            project_name (Union[str, None]): Project name.\n\n        Returns:\n            Union[dict[str, Any], None]: Icon definition or None if site sync\n                is not enabled for the project.\n        \"\"\"\n\n        pass\n\n    @abstractmethod\n    def get_active_site(self, project_name: str) -&gt; str | None:\n        \"\"\"Active site name.\n\n        Args:\n            project_name (str): Project name.\n\n        Returns:\n            Union[str, None]: Site name or None if site sync is not enabled.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_remote_site(self, project_name: str) -&gt; str | None:\n        \"\"\"Remote site name.\n\n        Args:\n            project_name (str): Project name.\n\n        Returns:\n            Union[str, None]: Site name or None if site sync is not enabled.\n\n        \"\"\"\n\n        pass\n\n    @abstractmethod\n    def get_version_sync_availability(self, project_name, version_ids):\n        \"\"\"Version sync availability.\n\n        Args:\n            project_name (str): Project name.\n            version_ids (Iterable[str]): Version ids.\n\n        Returns:\n            dict[str, tuple[int, int]]: Sync availability by version id.\n        \"\"\"\n\n        pass\n\n    @abstractmethod\n    def get_representations_sync_status(\n        self, project_name, representation_ids\n    ):\n        \"\"\"Representations sync status.\n\n        Args:\n            project_name (str): Project name.\n            representation_ids (Iterable[str]): Representation ids.\n\n        Returns:\n            dict[str, tuple[int, int]]: Sync status by representation id.\n        \"\"\"\n\n        pass\n\n    @abstractmethod\n    def get_product_types_filter(self):\n        \"\"\"Return product type filter for current context.\n\n        Returns:\n            ProductTypesFilter: Product type filter for current context\n        \"\"\"\n\n        pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.FrontendLoaderController.change_products_group","title":"<code>change_products_group(project_name, product_ids, group_name)</code>  <code>abstractmethod</code>","text":"<p>Change group of products.</p> <p>Triggers event \"products.group.changed\" with data:     {         \"project_name\": project_name,         \"folder_ids\": folder_ids,         \"product_ids\": product_ids,         \"group_name\": group_name,     }</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>product_ids</code> <code>Iterable[str]</code> <p>Product ids.</p> required <code>group_name</code> <code>str</code> <p>New group name.</p> required Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>@abstractmethod\ndef change_products_group(\n    self,\n    project_name: str,\n    product_ids: set[str],\n    group_name: str,\n):\n    \"\"\"Change group of products.\n\n    Triggers event \"products.group.changed\" with data:\n        {\n            \"project_name\": project_name,\n            \"folder_ids\": folder_ids,\n            \"product_ids\": product_ids,\n            \"group_name\": group_name,\n        }\n\n    Args:\n        project_name (str): Project name.\n        product_ids (Iterable[str]): Product ids.\n        group_name (str): New group name.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.FrontendLoaderController.expected_folder_selected","title":"<code>expected_folder_selected(folder_id)</code>  <code>abstractmethod</code>","text":"<p>Expected folder was selected in frontend.</p> <p>Parameters:</p> Name Type Description Default <code>folder_id</code> <code>str</code> <p>Folder id.</p> required Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>@abstractmethod\ndef expected_folder_selected(self, folder_id):\n    \"\"\"Expected folder was selected in frontend.\n\n    Args:\n        folder_id (str): Folder id.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.FrontendLoaderController.expected_project_selected","title":"<code>expected_project_selected(project_name)</code>  <code>abstractmethod</code>","text":"<p>Expected project was selected in frontend.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>@abstractmethod\ndef expected_project_selected(self, project_name):\n    \"\"\"Expected project was selected in frontend.\n\n    Args:\n        project_name (str): Project name.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.FrontendLoaderController.fill_root_in_source","title":"<code>fill_root_in_source(source)</code>  <code>abstractmethod</code>","text":"<p>Fill root in source path.</p> <p>Parameters:</p> Name Type Description Default <code>source</code> <code>Union[str, None]</code> <p>Source of a published version. Usually rootless workfile path.</p> required Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>@abstractmethod\ndef fill_root_in_source(self, source):\n    \"\"\"Fill root in source path.\n\n    Args:\n        source (Union[str, None]): Source of a published version. Usually\n            rootless workfile path.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.FrontendLoaderController.get_action_items","title":"<code>get_action_items(project_name, entity_ids, entity_type)</code>  <code>abstractmethod</code>","text":"<p>Action items for versions selection.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>entity_ids</code> <code>set[str]</code> <p>Entity ids.</p> required <code>entity_type</code> <code>str</code> <p>Entity type.</p> required <p>Returns:</p> Type Description <code>list[ActionItem]</code> <p>list[ActionItem]: List of action items.</p> Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>@abstractmethod\ndef get_action_items(\n    self,\n    project_name: str,\n    entity_ids: set[str],\n    entity_type: str,\n) -&gt; list[ActionItem]:\n    \"\"\"Action items for versions selection.\n\n    Args:\n        project_name (str): Project name.\n        entity_ids (set[str]): Entity ids.\n        entity_type (str): Entity type.\n\n    Returns:\n        list[ActionItem]: List of action items.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.FrontendLoaderController.get_active_site","title":"<code>get_active_site(project_name)</code>  <code>abstractmethod</code>","text":"<p>Active site name.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <p>Returns:</p> Type Description <code>str | None</code> <p>Union[str, None]: Site name or None if site sync is not enabled.</p> Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>@abstractmethod\ndef get_active_site(self, project_name: str) -&gt; str | None:\n    \"\"\"Active site name.\n\n    Args:\n        project_name (str): Project name.\n\n    Returns:\n        Union[str, None]: Site name or None if site sync is not enabled.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.FrontendLoaderController.get_active_site_icon_def","title":"<code>get_active_site_icon_def(project_name)</code>  <code>abstractmethod</code>","text":"<p>Active site icon definition.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>Union[str, None]</code> <p>Project name.</p> required <p>Returns:</p> Type Description <p>Union[dict[str, Any], None]: Icon definition or None if site sync is not enabled for the project.</p> Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>@abstractmethod\ndef get_active_site_icon_def(self, project_name):\n    \"\"\"Active site icon definition.\n\n    Args:\n        project_name (Union[str, None]): Project name.\n\n    Returns:\n        Union[dict[str, Any], None]: Icon definition or None if site sync\n            is not enabled for the project.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.FrontendLoaderController.get_available_tags_by_entity_type","title":"<code>get_available_tags_by_entity_type(project_name)</code>  <code>abstractmethod</code>","text":"<p>Get available tags by entity type.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <p>Returns:</p> Type Description <code>dict[str, list[str]]</code> <p>dict[str, list[str]]: Available tags by entity type.</p> Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>@abstractmethod\ndef get_available_tags_by_entity_type(\n    self, project_name: str\n) -&gt; dict[str, list[str]]:\n    \"\"\"Get available tags by entity type.\n\n    Args:\n        project_name (str): Project name.\n\n    Returns:\n        dict[str, list[str]]: Available tags by entity type.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.FrontendLoaderController.get_folder_labels","title":"<code>get_folder_labels(project_name, folder_ids)</code>  <code>abstractmethod</code>","text":"<p>Get folder labels for folder ids.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>folder_ids</code> <code>Iterable[str]</code> <p>Folder ids.</p> required <p>Returns:</p> Type Description <p>dict[str, Optional[str]]: Folder labels by folder id.</p> Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>@abstractmethod\ndef get_folder_labels(self, project_name, folder_ids):\n    \"\"\"Get folder labels for folder ids.\n\n    Args:\n        project_name (str): Project name.\n        folder_ids (Iterable[str]): Folder ids.\n\n    Returns:\n        dict[str, Optional[str]]: Folder labels by folder id.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.FrontendLoaderController.get_folder_thumbnail_ids","title":"<code>get_folder_thumbnail_ids(project_name, folder_ids)</code>  <code>abstractmethod</code>","text":"<p>Get thumbnail ids for folder ids.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>folder_ids</code> <code>Iterable[str]</code> <p>Folder ids.</p> required <p>Returns:</p> Type Description <p>dict[str, Union[str, Any]]: Thumbnail id by folder id.</p> Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>@abstractmethod\ndef get_folder_thumbnail_ids(self, project_name, folder_ids):\n    \"\"\"Get thumbnail ids for folder ids.\n\n    Args:\n        project_name (str): Project name.\n        folder_ids (Iterable[str]): Folder ids.\n\n    Returns:\n        dict[str, Union[str, Any]]: Thumbnail id by folder id.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.FrontendLoaderController.get_folder_type_items","title":"<code>get_folder_type_items(project_name, sender=None)</code>  <code>abstractmethod</code>","text":"<p>Folder type items for a project.</p> <p>This function may trigger events with topics 'projects.folder_types.refresh.started' and 'projects.folder_types.refresh.finished' which will contain 'sender' value in data. That may help to avoid re-refresh of items in UI elements.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>sender</code> <code>str</code> <p>Who requested folder type items.</p> <code>None</code> <p>Returns:</p> Type Description <p>list[FolderTypeItem]: Folder type information.</p> Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>@abstractmethod\ndef get_folder_type_items(self, project_name, sender=None):\n    \"\"\"Folder type items for a project.\n\n    This function may trigger events with topics\n    'projects.folder_types.refresh.started' and\n    'projects.folder_types.refresh.finished' which will contain 'sender'\n    value in data.\n    That may help to avoid re-refresh of items in UI elements.\n\n    Args:\n        project_name (str): Project name.\n        sender (str): Who requested folder type items.\n\n    Returns:\n        list[FolderTypeItem]: Folder type information.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.FrontendLoaderController.get_my_tasks_entity_ids","title":"<code>get_my_tasks_entity_ids(project_name)</code>  <code>abstractmethod</code>","text":"<p>Get entity ids for my tasks.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <p>Returns:</p> Type Description <code>dict[str, list[str]]</code> <p>dict[str, list[str]]: Folder and task ids.</p> Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>@abstractmethod\ndef get_my_tasks_entity_ids(\n    self, project_name: str\n) -&gt; dict[str, list[str]]:\n    \"\"\"Get entity ids for my tasks.\n\n    Args:\n        project_name (str): Project name.\n\n    Returns:\n        dict[str, list[str]]: Folder and task ids.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.FrontendLoaderController.get_product_item","title":"<code>get_product_item(project_name, product_id)</code>  <code>abstractmethod</code>","text":"<p>Receive single product item.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>product_id</code> <code>str</code> <p>Product id.</p> required <p>Returns:</p> Type Description <p>Union[ProductItem, None]: Product info or None if not found.</p> Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>@abstractmethod\ndef get_product_item(self, project_name, product_id):\n    \"\"\"Receive single product item.\n\n    Args:\n        project_name (str): Project name.\n        product_id (str): Product id.\n\n    Returns:\n         Union[ProductItem, None]: Product info or None if not found.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.FrontendLoaderController.get_product_items","title":"<code>get_product_items(project_name, folder_ids, sender=None)</code>  <code>abstractmethod</code>","text":"<p>Product items for folder ids.</p> <p>Triggers event topics \"products.refresh.started\" and \"products.refresh.finished\" with data:     {         \"project_name\": project_name,         \"folder_ids\": folder_ids,         \"sender\": sender     }</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>folder_ids</code> <code>Iterable[str]</code> <p>Folder ids.</p> required <code>sender</code> <code>Optional[str]</code> <p>Sender who requested the items.</p> <code>None</code> <p>Returns:</p> Type Description <p>list[ProductItem]: List of product items.</p> Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>@abstractmethod\ndef get_product_items(self, project_name, folder_ids, sender=None):\n    \"\"\"Product items for folder ids.\n\n    Triggers event topics \"products.refresh.started\" and\n    \"products.refresh.finished\" with data:\n        {\n            \"project_name\": project_name,\n            \"folder_ids\": folder_ids,\n            \"sender\": sender\n        }\n\n    Args:\n        project_name (str): Project name.\n        folder_ids (Iterable[str]): Folder ids.\n        sender (Optional[str]): Sender who requested the items.\n\n    Returns:\n        list[ProductItem]: List of product items.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.FrontendLoaderController.get_product_type_items","title":"<code>get_product_type_items(project_name)</code>  <code>abstractmethod</code>","text":"<p>Product type items for a project.</p> <p>Product types have defined if are checked for filtering or not.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>Union[str, None]</code> <p>Project name.</p> required <p>Returns:</p> Type Description <p>list[ProductTypeItem]: List of product type items for a project.</p> Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>@abstractmethod\ndef get_product_type_items(self, project_name):\n    \"\"\"Product type items for a project.\n\n    Product types have defined if are checked for filtering or not.\n\n    Args:\n        project_name (Union[str, None]): Project name.\n\n    Returns:\n        list[ProductTypeItem]: List of product type items for a project.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.FrontendLoaderController.get_product_types_filter","title":"<code>get_product_types_filter()</code>  <code>abstractmethod</code>","text":"<p>Return product type filter for current context.</p> <p>Returns:</p> Name Type Description <code>ProductTypesFilter</code> <p>Product type filter for current context</p> Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>@abstractmethod\ndef get_product_types_filter(self):\n    \"\"\"Return product type filter for current context.\n\n    Returns:\n        ProductTypesFilter: Product type filter for current context\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.FrontendLoaderController.get_project_anatomy_tags","title":"<code>get_project_anatomy_tags(project_name)</code>  <code>abstractmethod</code>","text":"<p>Tag items defined on project anatomy.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <p>Returns:</p> Type Description <code>list[TagItem]</code> <p>list[TagItem]: Tag definition items.</p> Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>@abstractmethod\ndef get_project_anatomy_tags(self, project_name: str) -&gt; list[TagItem]:\n    \"\"\"Tag items defined on project anatomy.\n\n    Args:\n        project_name (str): Project name.\n\n    Returns:\n        list[TagItem]: Tag definition items.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.FrontendLoaderController.get_project_items","title":"<code>get_project_items(sender=None)</code>  <code>abstractmethod</code>","text":"<p>Items for all projects available on server.</p> <p>Triggers event topics \"projects.refresh.started\" and \"projects.refresh.finished\" with data:     {         \"sender\": sender     }</p> Notes <p>Filtering of projects is done in UI.</p> <p>Parameters:</p> Name Type Description Default <code>sender</code> <code>Optional[str]</code> <p>Sender who requested the items.</p> <code>None</code> <p>Returns:</p> Type Description <p>list[ProjectItem]: List of project items.</p> Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>@abstractmethod\ndef get_project_items(self, sender=None):\n    \"\"\"Items for all projects available on server.\n\n    Triggers event topics \"projects.refresh.started\" and\n    \"projects.refresh.finished\" with data:\n        {\n            \"sender\": sender\n        }\n\n    Notes:\n        Filtering of projects is done in UI.\n\n    Args:\n        sender (Optional[str]): Sender who requested the items.\n\n    Returns:\n        list[ProjectItem]: List of project items.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.FrontendLoaderController.get_project_status_items","title":"<code>get_project_status_items(project_name, sender=None)</code>  <code>abstractmethod</code>","text":"<p>Items for all projects available on server.</p> <p>Triggers event topics \"projects.statuses.refresh.started\" and \"projects.statuses.refresh.finished\" with data:     {         \"sender\": sender,         \"project_name\": project_name     }</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>Union[str, None]</code> <p>Project name.</p> required <code>sender</code> <code>Optional[str]</code> <p>Sender who requested the items.</p> <code>None</code> <p>Returns:</p> Type Description <p>list[StatusItem]: List of status items.</p> Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>@abstractmethod\ndef get_project_status_items(self, project_name, sender=None):\n    \"\"\"Items for all projects available on server.\n\n    Triggers event topics \"projects.statuses.refresh.started\" and\n    \"projects.statuses.refresh.finished\" with data:\n        {\n            \"sender\": sender,\n            \"project_name\": project_name\n        }\n\n    Args:\n        project_name (Union[str, None]): Project name.\n        sender (Optional[str]): Sender who requested the items.\n\n    Returns:\n        list[StatusItem]: List of status items.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.FrontendLoaderController.get_remote_site","title":"<code>get_remote_site(project_name)</code>  <code>abstractmethod</code>","text":"<p>Remote site name.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <p>Returns:</p> Type Description <code>str | None</code> <p>Union[str, None]: Site name or None if site sync is not enabled.</p> Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>@abstractmethod\ndef get_remote_site(self, project_name: str) -&gt; str | None:\n    \"\"\"Remote site name.\n\n    Args:\n        project_name (str): Project name.\n\n    Returns:\n        Union[str, None]: Site name or None if site sync is not enabled.\n\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.FrontendLoaderController.get_remote_site_icon_def","title":"<code>get_remote_site_icon_def(project_name)</code>  <code>abstractmethod</code>","text":"<p>Remote site icon definition.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>Union[str, None]</code> <p>Project name.</p> required <p>Returns:</p> Type Description <p>Union[dict[str, Any], None]: Icon definition or None if site sync is not enabled for the project.</p> Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>@abstractmethod\ndef get_remote_site_icon_def(self, project_name):\n    \"\"\"Remote site icon definition.\n\n    Args:\n        project_name (Union[str, None]): Project name.\n\n    Returns:\n        Union[dict[str, Any], None]: Icon definition or None if site sync\n            is not enabled for the project.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.FrontendLoaderController.get_representation_items","title":"<code>get_representation_items(project_name, version_ids, sender=None)</code>  <code>abstractmethod</code>","text":"<p>Representation items for version ids.</p> <p>Triggers event topics \"model.representations.refresh.started\" and \"model.representations.refresh.finished\" with data:     {         \"project_name\": project_name,         \"version_ids\": version_ids,         \"sender\": sender     }</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>version_ids</code> <code>Iterable[str]</code> <p>Version ids.</p> required <code>sender</code> <code>Optional[str]</code> <p>Sender who requested the items.</p> <code>None</code> <p>Returns:</p> Type Description <p>list[RepreItem]: List of representation items.</p> Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>@abstractmethod\ndef get_representation_items(\n    self, project_name, version_ids, sender=None\n):\n    \"\"\"Representation items for version ids.\n\n    Triggers event topics \"model.representations.refresh.started\" and\n    \"model.representations.refresh.finished\" with data:\n        {\n            \"project_name\": project_name,\n            \"version_ids\": version_ids,\n            \"sender\": sender\n        }\n\n    Args:\n        project_name (str): Project name.\n        version_ids (Iterable[str]): Version ids.\n        sender (Optional[str]): Sender who requested the items.\n\n    Returns:\n        list[RepreItem]: List of representation items.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.FrontendLoaderController.get_representations_sync_status","title":"<code>get_representations_sync_status(project_name, representation_ids)</code>  <code>abstractmethod</code>","text":"<p>Representations sync status.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>representation_ids</code> <code>Iterable[str]</code> <p>Representation ids.</p> required <p>Returns:</p> Type Description <p>dict[str, tuple[int, int]]: Sync status by representation id.</p> Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>@abstractmethod\ndef get_representations_sync_status(\n    self, project_name, representation_ids\n):\n    \"\"\"Representations sync status.\n\n    Args:\n        project_name (str): Project name.\n        representation_ids (Iterable[str]): Representation ids.\n\n    Returns:\n        dict[str, tuple[int, int]]: Sync status by representation id.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.FrontendLoaderController.get_selected_folder_ids","title":"<code>get_selected_folder_ids()</code>  <code>abstractmethod</code>","text":"<p>Get selected folder ids.</p> <p>The information is based on last selection from UI.</p> <p>Returns:</p> Type Description <p>list[str]: Selected folder ids.</p> Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>@abstractmethod\ndef get_selected_folder_ids(self):\n    \"\"\"Get selected folder ids.\n\n    The information is based on last selection from UI.\n\n    Returns:\n        list[str]: Selected folder ids.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.FrontendLoaderController.get_selected_project_name","title":"<code>get_selected_project_name()</code>  <code>abstractmethod</code>","text":"<p>Get selected project name.</p> <p>The information is based on last selection from UI.</p> <p>Returns:</p> Type Description <p>Union[str, None]: Selected project name.</p> Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>@abstractmethod\ndef get_selected_project_name(self):\n    \"\"\"Get selected project name.\n\n    The information is based on last selection from UI.\n\n    Returns:\n        Union[str, None]: Selected project name.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.FrontendLoaderController.get_selected_representation_ids","title":"<code>get_selected_representation_ids()</code>  <code>abstractmethod</code>","text":"<p>Get selected representation ids.</p> <p>The information is based on last selection from UI.</p> <p>Returns:</p> Type Description <p>list[str]: Selected representation ids.</p> Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>@abstractmethod\ndef get_selected_representation_ids(self):\n    \"\"\"Get selected representation ids.\n\n    The information is based on last selection from UI.\n\n    Returns:\n        list[str]: Selected representation ids.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.FrontendLoaderController.get_selected_task_ids","title":"<code>get_selected_task_ids()</code>  <code>abstractmethod</code>","text":"<p>Get selected task ids.</p> <p>The information is based on last selection from UI.</p> <p>Returns:</p> Type Description <p>list[str]: Selected folder ids.</p> Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>@abstractmethod\ndef get_selected_task_ids(self):\n    \"\"\"Get selected task ids.\n\n    The information is based on last selection from UI.\n\n    Returns:\n        list[str]: Selected folder ids.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.FrontendLoaderController.get_selected_version_ids","title":"<code>get_selected_version_ids()</code>  <code>abstractmethod</code>","text":"<p>Get selected version ids.</p> <p>The information is based on last selection from UI.</p> <p>Returns:</p> Type Description <p>list[str]: Selected version ids.</p> Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>@abstractmethod\ndef get_selected_version_ids(self):\n    \"\"\"Get selected version ids.\n\n    The information is based on last selection from UI.\n\n    Returns:\n        list[str]: Selected version ids.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.FrontendLoaderController.get_task_items","title":"<code>get_task_items(project_name, folder_ids, sender=None)</code>  <code>abstractmethod</code>","text":"<p>Task items for folder ids.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>folder_ids</code> <code>Iterable[str]</code> <p>Folder ids.</p> required <code>sender</code> <code>Optional[str]</code> <p>Sender who requested the items.</p> <code>None</code> <p>Returns:</p> Type Description <code>list[TaskItem]</code> <p>list[TaskItem]: List of task items.</p> Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>@abstractmethod\ndef get_task_items(\n    self,\n    project_name: str,\n    folder_ids: Iterable[str],\n    sender: Optional[str] = None,\n) -&gt; list[TaskItem]:\n    \"\"\"Task items for folder ids.\n\n    Args:\n        project_name (str): Project name.\n        folder_ids (Iterable[str]): Folder ids.\n        sender (Optional[str]): Sender who requested the items.\n\n    Returns:\n        list[TaskItem]: List of task items.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.FrontendLoaderController.get_task_type_items","title":"<code>get_task_type_items(project_name, sender=None)</code>  <code>abstractmethod</code>","text":"<p>Task type items for a project.</p> <p>This function may trigger events with topics 'projects.task_types.refresh.started' and 'projects.task_types.refresh.finished' which will contain 'sender' value in data. That may help to avoid re-refresh of items in UI elements.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>sender</code> <code>str</code> <p>Who requested task type items.</p> <code>None</code> <p>Returns:</p> Type Description <p>list[TaskTypeItem]: Task type information.</p> Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>@abstractmethod\ndef get_task_type_items(self, project_name, sender=None):\n    \"\"\"Task type items for a project.\n\n    This function may trigger events with topics\n    'projects.task_types.refresh.started' and\n    'projects.task_types.refresh.finished' which will contain 'sender'\n    value in data.\n    That may help to avoid re-refresh of items in UI elements.\n\n    Args:\n        project_name (str): Project name.\n        sender (str): Who requested task type items.\n\n    Returns:\n        list[TaskTypeItem]: Task type information.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.FrontendLoaderController.get_thumbnail_paths","title":"<code>get_thumbnail_paths(project_name, entity_type, entity_ids)</code>  <code>abstractmethod</code>","text":"<p>Get thumbnail path for thumbnail id.</p> <p>This method should get a path to a thumbnail based on thumbnail id. Which probably means to download the thumbnail from server and store it locally.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>entity_type</code> <code>str</code> <p>Entity type.</p> required <code>entity_ids</code> <code>set[str]</code> <p>Entity ids.</p> required <p>Returns:</p> Type Description <p>dict[str, Union[str, None]]: Thumbnail path by entity id.</p> Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>@abstractmethod\ndef get_thumbnail_paths(\n    self,\n    project_name,\n    entity_type,\n    entity_ids\n):\n    \"\"\"Get thumbnail path for thumbnail id.\n\n    This method should get a path to a thumbnail based on thumbnail id.\n    Which probably means to download the thumbnail from server and store\n    it locally.\n\n    Args:\n        project_name (str): Project name.\n        entity_type (str): Entity type.\n        entity_ids (set[str]): Entity ids.\n\n    Returns:\n        dict[str, Union[str, None]]: Thumbnail path by entity id.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.FrontendLoaderController.get_version_sync_availability","title":"<code>get_version_sync_availability(project_name, version_ids)</code>  <code>abstractmethod</code>","text":"<p>Version sync availability.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>version_ids</code> <code>Iterable[str]</code> <p>Version ids.</p> required <p>Returns:</p> Type Description <p>dict[str, tuple[int, int]]: Sync availability by version id.</p> Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>@abstractmethod\ndef get_version_sync_availability(self, project_name, version_ids):\n    \"\"\"Version sync availability.\n\n    Args:\n        project_name (str): Project name.\n        version_ids (Iterable[str]): Version ids.\n\n    Returns:\n        dict[str, tuple[int, int]]: Sync availability by version id.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.FrontendLoaderController.get_version_thumbnail_ids","title":"<code>get_version_thumbnail_ids(project_name, version_ids)</code>  <code>abstractmethod</code>","text":"<p>Get thumbnail ids for version ids.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>version_ids</code> <code>Iterable[str]</code> <p>Version ids.</p> required <p>Returns:</p> Type Description <p>dict[str, Union[str, Any]]: Thumbnail id by version id.</p> Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>@abstractmethod\ndef get_version_thumbnail_ids(self, project_name, version_ids):\n    \"\"\"Get thumbnail ids for version ids.\n\n    Args:\n        project_name (str): Project name.\n        version_ids (Iterable[str]): Version ids.\n\n    Returns:\n        dict[str, Union[str, Any]]: Thumbnail id by version id.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.FrontendLoaderController.get_versions_representation_count","title":"<code>get_versions_representation_count(project_name, version_ids, sender=None)</code>  <code>abstractmethod</code>","text":"<p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>version_ids</code> <code>Iterable[str]</code> <p>Version ids.</p> required <code>sender</code> <code>Optional[str]</code> <p>Sender who requested the items.</p> <code>None</code> <p>Returns:</p> Type Description <p>dict[str, int]: Representation count by version id.</p> Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>@abstractmethod\ndef get_versions_representation_count(\n    self, project_name, version_ids, sender=None\n):\n    \"\"\"\n    Args:\n        project_name (str): Project name.\n        version_ids (Iterable[str]): Version ids.\n        sender (Optional[str]): Sender who requested the items.\n\n    Returns:\n        dict[str, int]: Representation count by version id.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.FrontendLoaderController.get_window_subtitle","title":"<code>get_window_subtitle()</code>  <code>abstractmethod</code>","text":"<p>Get window subtitle.</p> <p>Returns:</p> Type Description <code>Optional[str]</code> <p>Optional[str]: Window subtitle.</p> Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>@abstractmethod\ndef get_window_subtitle(self) -&gt; Optional[str]:\n    \"\"\"Get window subtitle.\n\n    Returns:\n        Optional[str]: Window subtitle.\n\n    \"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.FrontendLoaderController.is_loaded_products_supported","title":"<code>is_loaded_products_supported()</code>  <code>abstractmethod</code>","text":"<p>Is capable to get information about loaded products.</p> <p>Returns:</p> Name Type Description <code>bool</code> <p>True if it is supported.</p> Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>@abstractmethod\ndef is_loaded_products_supported(self):\n    \"\"\"Is capable to get information about loaded products.\n\n    Returns:\n        bool: True if it is supported.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.FrontendLoaderController.is_sitesync_enabled","title":"<code>is_sitesync_enabled(project_name=None)</code>  <code>abstractmethod</code>","text":"<p>Is site sync enabled.</p> <p>Site sync addon can be enabled but can be disabled per project.</p> <p>When asked for enabled state without project name, it should return     True if site sync addon is available and enabled.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>Optional[str]</code> <p>Project name.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>bool</code> <p>True if site sync is enabled.</p> Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>@abstractmethod\ndef is_sitesync_enabled(self, project_name=None):\n    \"\"\"Is site sync enabled.\n\n    Site sync addon can be enabled but can be disabled per project.\n\n    When asked for enabled state without project name, it should return\n        True if site sync addon is available and enabled.\n\n    Args:\n        project_name (Optional[str]): Project name.\n\n    Returns:\n        bool: True if site sync is enabled.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.FrontendLoaderController.is_standard_projects_filter_enabled","title":"<code>is_standard_projects_filter_enabled()</code>  <code>abstractmethod</code>","text":"<p>Is standard projects filter enabled.</p> <p>This is used for filtering out when loader tool is used in a host. In that case only current project and library projects should be shown.</p> <p>Returns:</p> Name Type Description <code>bool</code> <p>Frontend should filter out non-library projects, except current context project.</p> Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>@abstractmethod\ndef is_standard_projects_filter_enabled(self):\n    \"\"\"Is standard projects filter enabled.\n\n    This is used for filtering out when loader tool is used in a host. In\n    that case only current project and library projects should be shown.\n\n    Returns:\n        bool: Frontend should filter out non-library projects, except\n            current context project.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.FrontendLoaderController.register_event_callback","title":"<code>register_event_callback(topic, callback)</code>  <code>abstractmethod</code>","text":"<p>Register callback for an event topic.</p> <p>Parameters:</p> Name Type Description Default <code>topic</code> <code>str</code> <p>Event topic name.</p> required <code>callback</code> <code>func</code> <p>Callback triggered when the event is emitted.</p> required Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>@abstractmethod\ndef register_event_callback(self, topic, callback):\n    \"\"\"Register callback for an event topic.\n\n    Args:\n        topic (str): Event topic name.\n        callback (func): Callback triggered when the event is emitted.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.FrontendLoaderController.set_selected_folders","title":"<code>set_selected_folders(folder_ids)</code>  <code>abstractmethod</code>","text":"<p>Set selected folders.</p> <p>Folder selection changed in UI. Method triggers event with topic \"selection.folders.changed\" with data:     {         \"project_name\": project_name,         \"folder_ids\": folder_ids     }</p> <p>Parameters:</p> Name Type Description Default <code>folder_ids</code> <code>Iterable[str]</code> <p>Selected folder ids.</p> required Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>@abstractmethod\ndef set_selected_folders(self, folder_ids):\n    \"\"\"Set selected folders.\n\n    Folder selection changed in UI. Method triggers event with topic\n    \"selection.folders.changed\" with data:\n        {\n            \"project_name\": project_name,\n            \"folder_ids\": folder_ids\n        }\n\n    Args:\n        folder_ids (Iterable[str]): Selected folder ids.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.FrontendLoaderController.set_selected_project","title":"<code>set_selected_project(project_name)</code>  <code>abstractmethod</code>","text":"<p>Set selected project.</p> <p>Project selection changed in UI. Method triggers event with topic \"selection.project.changed\" with data:     {         \"project_name\": self._project_name     }</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>Union[str, None]</code> <p>Selected project name.</p> required Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>@abstractmethod\ndef set_selected_project(self, project_name):\n    \"\"\"Set selected project.\n\n    Project selection changed in UI. Method triggers event with topic\n    \"selection.project.changed\" with data:\n        {\n            \"project_name\": self._project_name\n        }\n\n    Args:\n        project_name (Union[str, None]): Selected project name.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.FrontendLoaderController.set_selected_representations","title":"<code>set_selected_representations(repre_ids)</code>  <code>abstractmethod</code>","text":"<p>Set selected representations.</p> <p>Representation selection changed in UI. Method triggers event with topic \"selection.representations.changed\" with data:     {         \"project_name\": project_name,         \"folder_ids\": folder_ids,         \"version_ids\": version_ids,         \"representation_ids\": representation_ids     }</p> <p>Parameters:</p> Name Type Description Default <code>repre_ids</code> <code>Iterable[str]</code> <p>Selected representation ids.</p> required Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>@abstractmethod\ndef set_selected_representations(self, repre_ids):\n    \"\"\"Set selected representations.\n\n    Representation selection changed in UI. Method triggers event with\n    topic \"selection.representations.changed\" with data:\n        {\n            \"project_name\": project_name,\n            \"folder_ids\": folder_ids,\n            \"version_ids\": version_ids,\n            \"representation_ids\": representation_ids\n        }\n\n    Args:\n        repre_ids (Iterable[str]): Selected representation ids.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.FrontendLoaderController.set_selected_tasks","title":"<code>set_selected_tasks(task_ids)</code>  <code>abstractmethod</code>","text":"<p>Set selected tasks.</p> <p>Parameters:</p> Name Type Description Default <code>task_ids</code> <code>Iterable[str]</code> <p>Selected task ids.</p> required Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>@abstractmethod\ndef set_selected_tasks(self, task_ids):\n    \"\"\"Set selected tasks.\n\n    Args:\n        task_ids (Iterable[str]): Selected task ids.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.FrontendLoaderController.set_selected_versions","title":"<code>set_selected_versions(version_ids)</code>  <code>abstractmethod</code>","text":"<p>Set selected versions.</p> <p>Version selection changed in UI. Method triggers event with topic \"selection.versions.changed\" with data:     {         \"project_name\": project_name,         \"folder_ids\": folder_ids,         \"version_ids\": version_ids     }</p> <p>Parameters:</p> Name Type Description Default <code>version_ids</code> <code>Iterable[str]</code> <p>Selected version ids.</p> required Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>@abstractmethod\ndef set_selected_versions(self, version_ids):\n    \"\"\"Set selected versions.\n\n    Version selection changed in UI. Method triggers event with topic\n    \"selection.versions.changed\" with data:\n        {\n            \"project_name\": project_name,\n            \"folder_ids\": folder_ids,\n            \"version_ids\": version_ids\n        }\n\n    Args:\n        version_ids (Iterable[str]): Selected version ids.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.FrontendLoaderController.trigger_action_item","title":"<code>trigger_action_item(identifier, project_name, selected_ids, selected_entity_type, data, options, form_values)</code>  <code>abstractmethod</code>","text":"<p>Trigger action item.</p> <p>Triggers event \"load.started\" with data:     {         \"identifier\": identifier,         \"id\": ,     } <p>And triggers \"load.finished\" with data:     {         \"identifier\": identifier,         \"id\": ,         \"error_info\": [...],     } <p>Parameters:</p> Name Type Description Default <code>identifier</code> <code>sttr</code> <p>Plugin identifier.</p> required <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>selected_ids</code> <code>set[str]</code> <p>Selected entity ids.</p> required <code>selected_entity_type</code> <code>str</code> <p>Selected entity type.</p> required <code>data</code> <code>Optional[dict[str, Any]]</code> <p>Additional action item data.</p> required <code>options</code> <code>dict[str, Any]</code> <p>Action option values from UI.</p> required <code>form_values</code> <code>dict[str, Any]</code> <p>Action form values from UI.</p> required Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>@abstractmethod\ndef trigger_action_item(\n    self,\n    identifier: str,\n    project_name: str,\n    selected_ids: set[str],\n    selected_entity_type: str,\n    data: Optional[dict[str, Any]],\n    options: dict[str, Any],\n    form_values: dict[str, Any],\n):\n    \"\"\"Trigger action item.\n\n    Triggers event \"load.started\" with data:\n        {\n            \"identifier\": identifier,\n            \"id\": &lt;Random UUID&gt;,\n        }\n\n    And triggers \"load.finished\" with data:\n        {\n            \"identifier\": identifier,\n            \"id\": &lt;Random UUID&gt;,\n            \"error_info\": [...],\n        }\n\n    Args:\n        identifier (sttr): Plugin identifier.\n        project_name (str): Project name.\n        selected_ids (set[str]): Selected entity ids.\n        selected_entity_type (str): Selected entity type.\n        data (Optional[dict[str, Any]]): Additional action item data.\n        options (dict[str, Any]): Action option values from UI.\n        form_values (dict[str, Any]): Action form values from UI.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.ProductBaseTypeItem","title":"<code>ProductBaseTypeItem</code>","text":"<p>Item representing the product base type.</p> Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>class ProductBaseTypeItem:\n    \"\"\"Item representing the product base type.\"\"\"\n\n    def __init__(self, name: str, icon: AwesomeFontIcon):\n        \"\"\"Initialize product base type item.\"\"\"\n        self.name = name\n        self.icon = icon\n\n    def to_data(self) -&gt; dict[str, Any]:\n        \"\"\"Convert item to data dictionary.\n\n        Returns:\n            dict[str, Any]: Data representation of the item.\n\n        \"\"\"\n        return {\n            \"name\": self.name,\n            \"icon\": {\n                \"name\": self.icon.name,\n                \"color\": self.icon.color,\n            },\n        }\n\n    @classmethod\n    def from_data(\n        cls, data: dict[str, Any]\n    ) -&gt; ProductBaseTypeItem:\n        \"\"\"Create item from data dictionary.\n\n        Args:\n            data (dict[str, Any]): Data to create item from.\n\n        Returns:\n            ProductBaseTypeItem: Item created from the provided data.\n\n        \"\"\"\n        icon = data[\"icon\"]\n        data[\"icon\"] = AwesomeFontIcon(icon[\"name\"], color=icon[\"color\"])\n        return cls(**data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.ProductBaseTypeItem.__init__","title":"<code>__init__(name, icon)</code>","text":"<p>Initialize product base type item.</p> Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>def __init__(self, name: str, icon: AwesomeFontIcon):\n    \"\"\"Initialize product base type item.\"\"\"\n    self.name = name\n    self.icon = icon\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.ProductBaseTypeItem.from_data","title":"<code>from_data(data)</code>  <code>classmethod</code>","text":"<p>Create item from data dictionary.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict[str, Any]</code> <p>Data to create item from.</p> required <p>Returns:</p> Name Type Description <code>ProductBaseTypeItem</code> <code>ProductBaseTypeItem</code> <p>Item created from the provided data.</p> Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>@classmethod\ndef from_data(\n    cls, data: dict[str, Any]\n) -&gt; ProductBaseTypeItem:\n    \"\"\"Create item from data dictionary.\n\n    Args:\n        data (dict[str, Any]): Data to create item from.\n\n    Returns:\n        ProductBaseTypeItem: Item created from the provided data.\n\n    \"\"\"\n    icon = data[\"icon\"]\n    data[\"icon\"] = AwesomeFontIcon(icon[\"name\"], color=icon[\"color\"])\n    return cls(**data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.ProductBaseTypeItem.to_data","title":"<code>to_data()</code>","text":"<p>Convert item to data dictionary.</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict[str, Any]: Data representation of the item.</p> Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>def to_data(self) -&gt; dict[str, Any]:\n    \"\"\"Convert item to data dictionary.\n\n    Returns:\n        dict[str, Any]: Data representation of the item.\n\n    \"\"\"\n    return {\n        \"name\": self.name,\n        \"icon\": {\n            \"name\": self.icon.name,\n            \"color\": self.icon.color,\n        },\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.ProductItem","title":"<code>ProductItem</code>","text":"<p>Product item with it versions.</p> <p>Parameters:</p> Name Type Description Default <code>product_id</code> <code>str</code> <p>Product id.</p> required <code>product_type</code> <code>str</code> <p>Product type.</p> required <code>product_name</code> <code>str</code> <p>Product name.</p> required <code>product_icon</code> <code>dict[str, Any]</code> <p>Product icon definition.</p> required <code>product_in_scene</code> <code>bool</code> <p>Is product in scene (only when used in DCC).</p> required <code>group_name</code> <code>Union[str, None]</code> <p>Group name.</p> required <code>folder_id</code> <code>str</code> <p>Folder id.</p> required <code>folder_label</code> <code>str</code> <p>Folder label.</p> required <code>version_items</code> <code>dict[str, VersionItem]</code> <p>Version items by id.</p> required Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>class ProductItem:\n    \"\"\"Product item with it versions.\n\n    Args:\n        product_id (str): Product id.\n        product_type (str): Product type.\n        product_name (str): Product name.\n        product_icon (dict[str, Any]): Product icon definition.\n        product_in_scene (bool): Is product in scene (only when used in DCC).\n        group_name (Union[str, None]): Group name.\n        folder_id (str): Folder id.\n        folder_label (str): Folder label.\n        version_items (dict[str, VersionItem]): Version items by id.\n    \"\"\"\n\n    def __init__(\n        self,\n        product_id: str,\n        product_type: str,\n        product_base_type: str,\n        product_name: str,\n        product_icon: dict[str, Any],\n        group_name: Union[str, None],\n        folder_id: str,\n        folder_label: str,\n        version_items: dict[str, VersionItem],\n        product_in_scene: bool,\n    ):\n        self.product_id = product_id\n        self.product_type = product_type\n        self.product_base_type = product_base_type\n        self.product_name = product_name\n        self.product_icon = product_icon\n        self.product_in_scene = product_in_scene\n        self.group_name = group_name\n        self.folder_id = folder_id\n        self.folder_label = folder_label\n        self.version_items = version_items\n\n    def to_data(self) -&gt; dict[str, Any]:\n        return {\n            \"product_id\": self.product_id,\n            \"product_type\": self.product_type,\n            \"product_base_type\": self.product_base_type,\n            \"product_name\": self.product_name,\n            \"product_icon\": self.product_icon,\n            \"product_in_scene\": self.product_in_scene,\n            \"group_name\": self.group_name,\n            \"folder_id\": self.folder_id,\n            \"folder_label\": self.folder_label,\n            \"version_items\": {\n                version_id: version_item.to_data()\n                for version_id, version_item in self.version_items.items()\n            },\n        }\n\n    @classmethod\n    def from_data(cls, data):\n        version_items = {\n            version_id: VersionItem.from_data(version)\n            for version_id, version in data[\"version_items\"].items()\n        }\n        data[\"version_items\"] = version_items\n        return cls(**data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.ProductTypeItem","title":"<code>ProductTypeItem</code>","text":"<p>Item representing product type.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Product type name.</p> required <code>icon</code> <code>dict[str, Any]</code> <p>Product type icon definition.</p> required Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>class ProductTypeItem:\n    \"\"\"Item representing product type.\n\n    Args:\n        name (str): Product type name.\n        icon (dict[str, Any]): Product type icon definition.\n    \"\"\"\n\n    def __init__(self, name: str, icon: dict[str, Any]):\n        self.name = name\n        self.icon = icon\n\n    def to_data(self):\n        return {\n            \"name\": self.name,\n            \"icon\": self.icon,\n        }\n\n    @classmethod\n    def from_data(cls, data):\n        return cls(**data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.ProductTypesFilter","title":"<code>ProductTypesFilter</code>","text":"<p>Product types filter.</p> <p>Defines the filtering for product types.</p> Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>class ProductTypesFilter:\n    \"\"\"Product types filter.\n\n    Defines the filtering for product types.\n    \"\"\"\n    def __init__(self, product_types: list[str], is_allow_list: bool):\n        self.product_types: list[str] = product_types\n        self.is_allow_list: bool = is_allow_list\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.RepreItem","title":"<code>RepreItem</code>","text":"<p>Representation item.</p> <p>Parameters:</p> Name Type Description Default <code>representation_id</code> <code>str</code> <p>Representation id.</p> required <code>representation_name</code> <code>str</code> <p>Representation name.</p> required <code>representation_icon</code> <code>dict[str, Any]</code> <p>Representation icon definition.</p> required <code>product_name</code> <code>str</code> <p>Product name.</p> required <code>folder_label</code> <code>str</code> <p>Folder label.</p> required Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>class RepreItem:\n    \"\"\"Representation item.\n\n    Args:\n        representation_id (str): Representation id.\n        representation_name (str): Representation name.\n        representation_icon (dict[str, Any]): Representation icon definition.\n        product_name (str): Product name.\n        folder_label (str): Folder label.\n    \"\"\"\n\n    def __init__(\n        self,\n        representation_id,\n        representation_name,\n        representation_icon,\n        product_name,\n        folder_label\n    ):\n        self.representation_id = representation_id\n        self.representation_name = representation_name\n        self.representation_icon = representation_icon\n        self.product_name = product_name\n        self.folder_label = folder_label\n\n    def to_data(self):\n        return {\n            \"representation_id\": self.representation_id,\n            \"representation_name\": self.representation_name,\n            \"representation_icon\": self.representation_icon,\n            \"product_name\": self.product_name,\n            \"folder_label\": self.folder_label,\n        }\n\n    @classmethod\n    def from_data(cls, data):\n        return cls(**data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/abstract.html#client.ayon_core.tools.loader.abstract.VersionItem","title":"<code>VersionItem</code>","text":"<p>Version item.</p> <p>Object have implemented comparison operators to be sortable.</p> <p>Parameters:</p> Name Type Description Default <code>version_id</code> <code>str</code> <p>Version id.</p> required <code>version</code> <code>int</code> <p>Version. Can be negative when is hero version.</p> required <code>is_hero</code> <code>bool</code> <p>Is hero version.</p> required <code>product_id</code> <code>str</code> <p>Product id.</p> required <code>task_id</code> <code>Union[str, None]</code> <p>Task id.</p> required <code>thumbnail_id</code> <code>Union[str, None]</code> <p>Thumbnail id.</p> required <code>published_time</code> <code>Union[str, None]</code> <p>Published time in format '%Y%m%dT%H%M%SZ'.</p> required <code>status</code> <code>Union[str, None]</code> <p>Status name.</p> required <code>tags</code> <code>Union[list[str], None]</code> <p>Tags.</p> required <code>author</code> <code>Union[str, None]</code> <p>Author.</p> required <code>frame_range</code> <code>Union[str, None]</code> <p>Frame range.</p> required <code>duration</code> <code>Union[int, None]</code> <p>Duration.</p> required <code>handles</code> <code>Union[str, None]</code> <p>Handles.</p> required <code>step</code> <code>Union[int, None]</code> <p>Step.</p> required <code>comment</code> <code>Union[str, None]</code> <p>Comment.</p> required <code>source</code> <code>Union[str, None]</code> <p>Source.</p> required Source code in <code>client/ayon_core/tools/loader/abstract.py</code> <pre><code>class VersionItem:\n    \"\"\"Version item.\n\n    Object have implemented comparison operators to be sortable.\n\n    Args:\n        version_id (str): Version id.\n        version (int): Version. Can be negative when is hero version.\n        is_hero (bool): Is hero version.\n        product_id (str): Product id.\n        task_id (Union[str, None]): Task id.\n        thumbnail_id (Union[str, None]): Thumbnail id.\n        published_time (Union[str, None]): Published time in format\n            '%Y%m%dT%H%M%SZ'.\n        status (Union[str, None]): Status name.\n        tags (Union[list[str], None]): Tags.\n        author (Union[str, None]): Author.\n        frame_range (Union[str, None]): Frame range.\n        duration (Union[int, None]): Duration.\n        handles (Union[str, None]): Handles.\n        step (Union[int, None]): Step.\n        comment (Union[str, None]): Comment.\n        source (Union[str, None]): Source.\n    \"\"\"\n\n    def __init__(\n        self,\n        version_id: str,\n        version: int,\n        is_hero: bool,\n        product_id: str,\n        task_id: Optional[str],\n        thumbnail_id: Optional[str],\n        published_time: Optional[str],\n        tags: Optional[list[str]],\n        author: Optional[str],\n        status: Optional[str],\n        frame_range: Optional[str],\n        duration: Optional[int],\n        handles: Optional[str],\n        step: Optional[int],\n        comment: Optional[str],\n        source: Optional[str],\n    ):\n        self.version_id = version_id\n        self.product_id = product_id\n        self.task_id = task_id\n        self.thumbnail_id = thumbnail_id\n        self.version = version\n        self.is_hero = is_hero\n        self.published_time = published_time\n        self.author = author\n        self.tags = tags\n        self.status = status\n        self.frame_range = frame_range\n        self.duration = duration\n        self.handles = handles\n        self.step = step\n        self.comment = comment\n        self.source = source\n\n    def __eq__(self, other):\n        if not isinstance(other, VersionItem):\n            return False\n        return (\n            self.is_hero == other.is_hero\n            and self.version == other.version\n            and self.version_id == other.version_id\n            and self.product_id == other.product_id\n            and self.task_id == other.task_id\n        )\n\n    def __ne__(self, other):\n        return not self.__eq__(other)\n\n    def __gt__(self, other):\n        if not isinstance(other, VersionItem):\n            return False\n        # Make sure hero versions are positive\n        version = abs(self.version)\n        other_version = abs(other.version)\n        # Hero version is greater than non-hero\n        if version == other_version:\n            return not self.is_hero\n        return version &gt; other_version\n\n    def __lt__(self, other):\n        if not isinstance(other, VersionItem):\n            return True\n        # Make sure hero versions are positive\n        version = abs(self.version)\n        other_version = abs(other.version)\n        # Non-hero version is lesser than hero\n        if version == other_version:\n            return self.is_hero\n        return version &lt; other_version\n\n    def __ge__(self, other):\n        return self.__eq__(other) or self.__gt__(other)\n\n    def __le__(self, other):\n        return self.__eq__(other) or self.__lt__(other)\n\n    def to_data(self) -&gt; dict[str, Any]:\n        return {\n            \"version_id\": self.version_id,\n            \"product_id\": self.product_id,\n            \"task_id\": self.task_id,\n            \"thumbnail_id\": self.thumbnail_id,\n            \"version\": self.version,\n            \"is_hero\": self.is_hero,\n            \"published_time\": self.published_time,\n            \"author\": self.author,\n            \"tags\": self.tags,\n            \"status\": self.status,\n            \"frame_range\": self.frame_range,\n            \"duration\": self.duration,\n            \"handles\": self.handles,\n            \"step\": self.step,\n            \"comment\": self.comment,\n            \"source\": self.source,\n        }\n\n    @classmethod\n    def from_data(cls, data: dict[str, Any]) -&gt; VersionItem:\n        return cls(**data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/control.html","title":"control","text":""},{"location":"autoapi/client/ayon_core/tools/loader/control.html#client.ayon_core.tools.loader.control.LoaderController","title":"<code>LoaderController</code>","text":"<p>               Bases: <code>BackendLoaderController</code>, <code>FrontendLoaderController</code></p> <p>Parameters:</p> Name Type Description Default <code>host</code> <code>Optional[AbstractHost]</code> <p>Host object. Defaults to None.</p> <code>None</code> Source code in <code>client/ayon_core/tools/loader/control.py</code> <pre><code>class LoaderController(BackendLoaderController, FrontendLoaderController):\n    \"\"\"\n\n    Args:\n        host (Optional[AbstractHost]): Host object. Defaults to None.\n    \"\"\"\n\n    def __init__(self, host: Optional[AbstractHost] = None) -&gt; None:\n        self._log = None\n        self._host = host\n\n        self._event_system = self._create_event_system()\n\n        self._project_settings = {}\n\n        self._project_anatomy_cache = NestedCacheItem(\n            levels=1, lifetime=60)\n        self._loaded_products_cache = CacheItem(\n            default_factory=set, lifetime=60)\n\n        self._selection_model = SelectionModel(self)\n        self._expected_selection = ExpectedSelection(self)\n        self._projects_model = ProjectsModel(self)\n        self._hierarchy_model = HierarchyModel(self)\n        self._products_model = ProductsModel(self)\n        self._loader_actions_model = LoaderActionsModel(self)\n        self._thumbnails_model = ThumbnailsModel()\n        self._sitesync_model = SiteSyncModel(self)\n        self._users_model = UsersModel(self)\n        self._settings_model = SettingsModel()\n\n    @property\n    def log(self):\n        if self._log is None:\n            self._log = logging.getLogger(self.__class__.__name__)\n        return self._log\n\n    # ---------------------------------\n    # Implementation of abstract methods\n    # ---------------------------------\n    def get_window_subtitle(self) -&gt; Optional[str]:\n        if self._host is None:\n            return None\n        return self._host.name\n\n    # Events system\n    def emit_event(self, topic, data=None, source=None):\n        \"\"\"Use implemented event system to trigger event.\"\"\"\n\n        if data is None:\n            data = {}\n        self._event_system.emit(topic, data, source)\n\n    def register_event_callback(self, topic, callback):\n        self._event_system.add_callback(topic, callback)\n\n    def reset(self):\n        self._emit_event(\"controller.reset.started\")\n\n        project_name = self.get_selected_project_name()\n        folder_ids = self.get_selected_folder_ids()\n\n        self._project_settings = {}\n\n        self._project_anatomy_cache.reset()\n        self._loaded_products_cache.reset()\n\n        self._products_model.reset()\n        self._hierarchy_model.reset()\n        self._loader_actions_model.reset()\n        self._projects_model.reset()\n        self._thumbnails_model.reset()\n        self._sitesync_model.reset()\n        self._users_model.reset()\n        self._settings_model.reset()\n\n        self._projects_model.refresh()\n\n        if not project_name and not folder_ids:\n            context = self.get_current_context()\n            project_name = context[\"project_name\"]\n            folder_id = context[\"folder_id\"]\n            self.set_expected_selection(project_name, folder_id)\n\n        self._emit_event(\"controller.reset.finished\")\n\n    # Expected selection helpers\n    def get_expected_selection_data(self):\n        return self._expected_selection.get_expected_selection_data()\n\n    def set_expected_selection(self, project_name, folder_id):\n        self._expected_selection.set_expected_selection(\n            project_name, folder_id\n        )\n\n    def expected_project_selected(self, project_name):\n        self._expected_selection.expected_project_selected(project_name)\n\n    def expected_folder_selected(self, folder_id):\n        self._expected_selection.expected_folder_selected(folder_id)\n\n    # Entity model wrappers\n    def get_project_items(self, sender=None):\n        return self._projects_model.get_project_items(sender)\n\n    def get_folder_type_items(self, project_name, sender=None):\n        return self._projects_model.get_folder_type_items(\n            project_name, sender\n        )\n\n    def get_project_status_items(self, project_name, sender=None):\n        return self._projects_model.get_project_status_items(\n            project_name, sender\n        )\n\n    def get_product_type_icons_mapping(\n        self, project_name: Optional[str]\n    ) -&gt; ProductTypeIconMapping:\n        return self._projects_model.get_product_type_icons_mapping(\n            project_name\n        )\n\n    def get_folder_items(self, project_name, sender=None):\n        return self._hierarchy_model.get_folder_items(project_name, sender)\n\n    def get_task_items(self, project_name, folder_ids, sender=None):\n        output = []\n        for folder_id in folder_ids:\n            output.extend(self._hierarchy_model.get_task_items(\n                project_name, folder_id, sender\n            ))\n        return output\n\n    def get_task_type_items(self, project_name, sender=None):\n        return self._projects_model.get_task_type_items(\n            project_name, sender\n        )\n\n    def get_folder_labels(self, project_name, folder_ids):\n        folder_items_by_id = self._hierarchy_model.get_folder_items_by_id(\n            project_name, folder_ids\n        )\n        output = {}\n        for folder_id, folder_item in folder_items_by_id.items():\n            label = None\n            if folder_item is not None:\n                label = folder_item.label\n            output[folder_id] = label\n        return output\n\n    def get_my_tasks_entity_ids(\n        self, project_name: str\n    ) -&gt; dict[str, list[str]]:\n        username = self._users_model.get_current_username()\n        assignees = []\n        if username:\n            assignees.append(username)\n        return self._hierarchy_model.get_entity_ids_for_assignees(\n            project_name, assignees\n        )\n\n    def get_available_tags_by_entity_type(\n        self, project_name: str\n    ) -&gt; dict[str, list[str]]:\n        return self._hierarchy_model.get_available_tags_by_entity_type(\n            project_name\n        )\n\n    def get_project_settings(self, project_name: str | None) -&gt; dict:\n        return self._settings_model.get_settings(project_name)\n\n    def get_project_anatomy_tags(self, project_name: str) -&gt; list[TagItem]:\n        return self._projects_model.get_project_anatomy_tags(project_name)\n\n    def get_product_items(self, project_name, folder_ids, sender=None):\n        return self._products_model.get_product_items(\n            project_name, folder_ids, sender)\n\n    def get_product_item(self, project_name, product_id):\n        return self._products_model.get_product_item(\n            project_name, product_id\n        )\n\n    def get_product_type_items(self, project_name):\n        return self._products_model.get_product_type_items(project_name)\n\n    def get_representation_items(\n        self, project_name, version_ids, sender=None\n    ):\n        return self._products_model.get_repre_items(\n            project_name, version_ids, sender\n        )\n\n    def get_versions_representation_count(\n        self, project_name, version_ids, sender=None\n    ):\n        return self._products_model.get_versions_repre_count(\n            project_name, version_ids, sender\n        )\n\n    def get_folder_thumbnail_ids(self, project_name, folder_ids):\n        return self._thumbnails_model.get_folder_thumbnail_ids(\n            project_name, folder_ids\n        )\n\n    def get_version_thumbnail_ids(self, project_name, version_ids):\n        return self._thumbnails_model.get_version_thumbnail_ids(\n            project_name, version_ids\n        )\n\n    def get_thumbnail_paths(\n        self,\n        project_name,\n        entity_type,\n        entity_ids,\n    ):\n        return self._thumbnails_model.get_thumbnail_paths(\n            project_name, entity_type, entity_ids\n        )\n\n    def change_products_group(self, project_name, product_ids, group_name):\n        self._products_model.change_products_group(\n            project_name, product_ids, group_name\n        )\n\n    def get_action_items(\n        self,\n        project_name: str,\n        entity_ids: set[str],\n        entity_type: str,\n    ) -&gt; list[ActionItem]:\n        action_items = self._loader_actions_model.get_action_items(\n            project_name, entity_ids, entity_type\n        )\n\n        site_sync_items = self._sitesync_model.get_sitesync_action_items(\n            project_name, entity_ids, entity_type\n        )\n        action_items.extend(site_sync_items)\n        return action_items\n\n    def trigger_action_item(\n        self,\n        identifier: str,\n        project_name: str,\n        selected_ids: set[str],\n        selected_entity_type: str,\n        data: Optional[dict[str, Any]],\n        options: dict[str, Any],\n        form_values: dict[str, Any],\n    ):\n        if self._sitesync_model.is_sitesync_action(identifier):\n            self._sitesync_model.trigger_action_item(\n                project_name,\n                data,\n            )\n            return\n\n        self._loader_actions_model.trigger_action_item(\n            identifier=identifier,\n            project_name=project_name,\n            selected_ids=selected_ids,\n            selected_entity_type=selected_entity_type,\n            data=data,\n            options=options,\n            form_values=form_values,\n        )\n\n    # Selection model wrappers\n    def get_selected_project_name(self):\n        return self._selection_model.get_selected_project_name()\n\n    def set_selected_project(self, project_name):\n        self._selection_model.set_selected_project(project_name)\n\n    # Selection model wrappers\n    def get_selected_folder_ids(self):\n        return self._selection_model.get_selected_folder_ids()\n\n    def set_selected_folders(self, folder_ids):\n        self._selection_model.set_selected_folders(folder_ids)\n\n    def get_selected_task_ids(self):\n        return self._selection_model.get_selected_task_ids()\n\n    def set_selected_tasks(self, task_ids):\n        self._selection_model.set_selected_tasks(task_ids)\n\n    def get_selected_version_ids(self):\n        return self._selection_model.get_selected_version_ids()\n\n    def set_selected_versions(self, version_ids):\n        self._selection_model.set_selected_versions(version_ids)\n\n    def get_selected_representation_ids(self):\n        return self._selection_model.get_selected_representation_ids()\n\n    def set_selected_representations(self, repre_ids):\n        self._selection_model.set_selected_representations(repre_ids)\n\n    def fill_root_in_source(self, source):\n        project_name = self.get_selected_project_name()\n        anatomy = self._get_project_anatomy(project_name)\n        if anatomy is None:\n            return source\n\n        try:\n            return anatomy.fill_root(source)\n        except Exception:\n            return source\n\n    def get_current_context(self):\n        if self._host is None:\n            return {\n                \"project_name\": None,\n                \"folder_id\": None,\n                \"task_name\": None,\n            }\n        if hasattr(self._host, \"get_current_context\"):\n            context = self._host.get_current_context()\n        else:\n            context = get_current_context()\n        folder_id = None\n        project_name = context.get(\"project_name\")\n        folder_path = context.get(\"folder_path\")\n        if project_name and folder_path:\n            folder_entity = ayon_api.get_folder_by_path(\n                project_name, folder_path, fields=[\"id\"]\n            )\n            if folder_entity:\n                folder_id = folder_entity[\"id\"]\n        return {\n            \"project_name\": project_name,\n            \"folder_id\": folder_id,\n            \"task_name\": context.get(\"task_name\"),\n        }\n\n    def get_loaded_product_ids(self):\n        if self._host is None:\n            return set()\n\n        context = self.get_current_context()\n        project_name = context[\"project_name\"]\n        if not project_name:\n            return set()\n\n        if not self._loaded_products_cache.is_valid:\n            try:\n                if isinstance(self._host, ILoadHost):\n                    containers = self._host.get_containers()\n                else:\n                    containers = self._host.ls()\n\n            except BaseException:\n                self.log.error(\n                    \"Failed to collect loaded products.\", exc_info=True\n                )\n                containers = []\n\n            repre_ids = set()\n            for container in containers:\n                try:\n                    repre_id = container.get(\"representation\")\n                    # Ignore invalid representation ids.\n                    # - invalid representation ids may be available if e.g. is\n                    #   opened scene from OpenPype whe 'ObjectId' was used\n                    #   instead of 'uuid'.\n                    # NOTE: Server call would crash if there is any invalid id.\n                    #   That would cause crash we won't get any information.\n                    uuid.UUID(repre_id)\n                    repre_ids.add(repre_id)\n                except (ValueError, TypeError, AttributeError):\n                    pass\n\n            product_ids = self._products_model.get_product_ids_by_repre_ids(\n                project_name, repre_ids\n            )\n            self._loaded_products_cache.update_data(product_ids)\n        return self._loaded_products_cache.get_data()\n\n    def is_sitesync_enabled(self, project_name=None):\n        return self._sitesync_model.is_sitesync_enabled(project_name)\n\n    def get_active_site_icon_def(self, project_name):\n        return self._sitesync_model.get_active_site_icon_def(project_name)\n\n    def get_remote_site_icon_def(self, project_name):\n        return self._sitesync_model.get_remote_site_icon_def(project_name)\n\n    def get_active_site(self, project_name):\n        return self._sitesync_model.get_active_site(project_name)\n\n    def get_remote_site(self, project_name):\n        return self._sitesync_model.get_remote_site(project_name)\n\n    def get_version_sync_availability(self, project_name, version_ids):\n        return self._sitesync_model.get_version_sync_availability(\n            project_name, version_ids\n        )\n\n    def get_representations_sync_status(\n        self, project_name, representation_ids\n    ):\n        return self._sitesync_model.get_representations_sync_status(\n            project_name, representation_ids\n        )\n\n    def is_loaded_products_supported(self):\n        return self._host is not None\n\n    def is_standard_projects_filter_enabled(self):\n        return self._host is not None\n\n    def get_product_types_filter(self):\n        output = ProductTypesFilter(\n            is_allow_list=False,\n            product_types=[]\n        )\n        # Without host is not determined context\n        if self._host is None:\n            return output\n\n        context = self.get_current_context()\n        project_name = context.get(\"project_name\")\n        if not project_name:\n            return output\n        settings = self.get_project_settings(project_name)\n        profiles = (\n            settings\n            [\"core\"]\n            [\"tools\"]\n            [\"loader\"]\n            [\"product_type_filter_profiles\"]\n        )\n        if not profiles:\n            return output\n\n        folder_id = context.get(\"folder_id\")\n        task_name = context.get(\"task_name\")\n        task_type = None\n        if folder_id and task_name:\n            task_entity = ayon_api.get_task_by_name(\n                project_name,\n                folder_id,\n                task_name,\n                fields={\"taskType\"}\n            )\n            if task_entity:\n                task_type = task_entity.get(\"taskType\")\n\n        host_name = getattr(self._host, \"name\", get_current_host_name())\n        profile = filter_profiles(\n            profiles,\n            {\n                \"host_names\": host_name,\n                \"task_types\": task_type,\n            }\n        )\n        if profile:\n            # TODO remove 'is_include' after release '0.4.3'\n            is_allow_list = profile.get(\"is_include\")\n            if is_allow_list is None:\n                is_allow_list = profile[\"filter_type\"] == \"is_allow_list\"\n            output = ProductTypesFilter(\n                is_allow_list=is_allow_list,\n                product_types=profile[\"filter_product_types\"]\n            )\n        return output\n\n    def _create_event_system(self):\n        return QueuedEventSystem()\n\n    def _emit_event(self, topic, data=None):\n        self._event_system.emit(topic, data or {}, \"controller\")\n\n    def _get_project_anatomy(self, project_name):\n        if not project_name:\n            return None\n        cache = self._project_anatomy_cache[project_name]\n        if not cache.is_valid:\n            cache.update_data(Anatomy(project_name))\n        return cache.get_data()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/control.html#client.ayon_core.tools.loader.control.LoaderController.emit_event","title":"<code>emit_event(topic, data=None, source=None)</code>","text":"<p>Use implemented event system to trigger event.</p> Source code in <code>client/ayon_core/tools/loader/control.py</code> <pre><code>def emit_event(self, topic, data=None, source=None):\n    \"\"\"Use implemented event system to trigger event.\"\"\"\n\n    if data is None:\n        data = {}\n    self._event_system.emit(topic, data, source)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/models/index.html","title":"models","text":""},{"location":"autoapi/client/ayon_core/tools/loader/models/index.html#client.ayon_core.tools.loader.models.LoaderActionsModel","title":"<code>LoaderActionsModel</code>","text":"<p>Model for loader actions.</p> TODOs <p>Deprecate 'qargparse' usage in loaders and implement conversion     of 'ActionItem' to data (and 'from_data'). Use controller to get entities -&gt; possible only when     loaders are able to handle AYON vs. OpenPype logic. Add missing site sync logic, and if possible remove it from loaders. Implement loader actions to replace load plugins. Ask loader actions to return action items instead of guessing them.</p> Source code in <code>client/ayon_core/tools/loader/models/actions.py</code> <pre><code>class LoaderActionsModel:\n    \"\"\"Model for loader actions.\n\n    TODOs:\n        Deprecate 'qargparse' usage in loaders and implement conversion\n            of 'ActionItem' to data (and 'from_data').\n        Use controller to get entities -&gt; possible only when\n            loaders are able to handle AYON vs. OpenPype logic.\n        Add missing site sync logic, and if possible remove it from loaders.\n        Implement loader actions to replace load plugins.\n        Ask loader actions to return action items instead of guessing them.\n    \"\"\"\n\n    # Cache loader plugins for some time\n    # NOTE Set to '0' for development\n    loaders_cache_lifetime = 30\n\n    def __init__(self, controller):\n        self._log = Logger.get_logger(self.__class__.__name__)\n        self._controller = controller\n        self._current_context_project = NOT_SET\n        self._loaders_by_identifier = NestedCacheItem(\n            levels=1, lifetime=self.loaders_cache_lifetime)\n        self._product_loaders = NestedCacheItem(\n            levels=1, lifetime=self.loaders_cache_lifetime)\n        self._repre_loaders = NestedCacheItem(\n            levels=1, lifetime=self.loaders_cache_lifetime)\n        self._loader_actions = LoaderActionsContext()\n\n        self._projects_cache = NestedCacheItem(levels=1, lifetime=60)\n        self._folders_cache = NestedCacheItem(levels=2, lifetime=300)\n        self._tasks_cache = NestedCacheItem(levels=2, lifetime=300)\n        self._products_cache = NestedCacheItem(levels=2, lifetime=300)\n        self._versions_cache = NestedCacheItem(levels=2, lifetime=1200)\n        self._representations_cache = NestedCacheItem(levels=2, lifetime=1200)\n        self._repre_parents_cache = NestedCacheItem(levels=2, lifetime=1200)\n\n    def reset(self):\n        \"\"\"Reset the model with all cached items.\"\"\"\n\n        self._current_context_project = NOT_SET\n        self._loaders_by_identifier.reset()\n        self._product_loaders.reset()\n        self._repre_loaders.reset()\n        self._loader_actions.reset()\n\n        self._folders_cache.reset()\n        self._tasks_cache.reset()\n        self._products_cache.reset()\n        self._versions_cache.reset()\n        self._representations_cache.reset()\n        self._repre_parents_cache.reset()\n\n    def get_action_items(\n        self,\n        project_name: str,\n        entity_ids: set[str],\n        entity_type: str,\n    ) -&gt; list[ActionItem]:\n        version_context_by_id = {}\n        repre_context_by_id = {}\n        if entity_type == \"representation\":\n            (\n                version_context_by_id,\n                repre_context_by_id\n            ) = self._contexts_for_representations(project_name, entity_ids)\n\n        if entity_type == \"version\":\n            (\n                version_context_by_id,\n                repre_context_by_id\n            ) = self._contexts_for_versions(project_name, entity_ids)\n\n        action_items = self._get_action_items_for_contexts(\n            project_name,\n            version_context_by_id,\n            repre_context_by_id\n        )\n        action_items.extend(self._get_loader_action_items(\n            project_name,\n            entity_ids,\n            entity_type,\n            version_context_by_id,\n            repre_context_by_id,\n        ))\n        return action_items\n\n    def trigger_action_item(\n        self,\n        identifier: str,\n        project_name: str,\n        selected_ids: set[str],\n        selected_entity_type: str,\n        data: Optional[dict[str, Any]],\n        options: dict[str, Any],\n        form_values: dict[str, Any],\n    ):\n        \"\"\"Trigger action by identifier.\n\n        Triggers the action by identifier for given contexts.\n\n        Triggers events \"load.started\" and \"load.finished\". Finished event\n            also contains \"error_info\" key with error information if any\n            happened.\n\n        Args:\n            identifier (str): Plugin identifier.\n            project_name (str): Project name.\n            selected_ids (set[str]): Selected entity ids.\n            selected_entity_type (str): Selected entity type.\n            data (Optional[dict[str, Any]]): Additional action item data.\n            options (dict[str, Any]): Loader option values.\n            form_values (dict[str, Any]): Form values.\n\n        \"\"\"\n        event_data = {\n            \"identifier\": identifier,\n            \"project_name\": project_name,\n            \"selected_ids\": list(selected_ids),\n            \"selected_entity_type\": selected_entity_type,\n            \"data\": data,\n            \"id\": uuid.uuid4().hex,\n        }\n        self._controller.emit_event(\n            \"load.started\",\n            event_data,\n            ACTIONS_MODEL_SENDER,\n        )\n        if identifier != LOADER_PLUGIN_ID:\n            result = None\n            crashed = False\n            try:\n                result = self._loader_actions.execute_action(\n                    identifier=identifier,\n                    selection=LoaderActionSelection(\n                        project_name,\n                        selected_ids,\n                        selected_entity_type,\n                    ),\n                    data=data,\n                    form_values=form_values,\n                )\n\n            except Exception:\n                crashed = True\n                self._log.warning(\n                    f\"Failed to execute action '{identifier}'\",\n                    exc_info=True,\n                )\n\n            event_data[\"result\"] = result\n            event_data[\"crashed\"] = crashed\n            self._controller.emit_event(\n                \"loader.action.finished\",\n                event_data,\n                ACTIONS_MODEL_SENDER,\n            )\n            return\n\n        loader = self._get_loader_by_identifier(\n            project_name, data[\"loader\"]\n        )\n        entity_type = data[\"entity_type\"]\n        entity_ids = data[\"entity_ids\"]\n        if entity_type == \"version\":\n            error_info = self._trigger_version_loader(\n                loader,\n                options,\n                project_name,\n                entity_ids,\n            )\n        elif entity_type == \"representation\":\n            error_info = self._trigger_representation_loader(\n                loader,\n                options,\n                project_name,\n                entity_ids,\n            )\n        else:\n            raise NotImplementedError(\n                f\"Invalid entity type '{entity_type}' to trigger action item\"\n            )\n\n        event_data[\"error_info\"] = error_info\n        self._controller.emit_event(\n            \"load.finished\",\n            event_data,\n            ACTIONS_MODEL_SENDER,\n        )\n\n    def _get_current_context_project(self):\n        \"\"\"Get current context project name.\n\n        The value is based on controller (host) and cached.\n\n        Returns:\n            Union[str, None]: Current context project.\n        \"\"\"\n\n        if self._current_context_project is NOT_SET:\n            context = self._controller.get_current_context()\n            self._current_context_project = context[\"project_name\"]\n        return self._current_context_project\n\n    def _get_action_label(self, loader, representation=None):\n        \"\"\"Pull label info from loader class.\n\n        Args:\n            loader (LoaderPlugin): Plugin class.\n            representation (Optional[dict[str, Any]]): Representation data.\n\n        Returns:\n            str: Action label.\n        \"\"\"\n\n        label = getattr(loader, \"label\", None)\n        if label is None:\n            label = loader.__name__\n        if representation:\n            # Add the representation as suffix\n            label = \"{} ({})\".format(label, representation[\"name\"])\n        return label\n\n    def _get_action_icon(self, loader):\n        \"\"\"Pull icon info from loader class.\n\n        Args:\n            loader (LoaderPlugin): Plugin class.\n\n        Returns:\n            IconBase | dict[str, Any] | None: Icon definition based on\n                loader plugin.\n        \"\"\"\n\n        icon = getattr(loader, \"icon\", None)\n        # Support font-awesome icons using the `.icon` and `.color`\n        # attributes on plug-ins.\n        if isinstance(icon, str):\n            icon = {\n                \"type\": \"awesome-font\",\n                \"name\": icon,\n                \"color\": getattr(loader, \"color\", None) or \"white\"\n            }\n        return icon\n\n    def _get_action_tooltip(self, loader):\n        \"\"\"Pull tooltip info from loader class.\n\n        Args:\n            loader (LoaderPlugin): Plugin class.\n\n        Returns:\n            str: Action tooltip.\n        \"\"\"\n\n        # Add tooltip and statustip from Loader docstring\n        return inspect.getdoc(loader)\n\n    def _filter_loaders_by_tool_name(self, project_name, loaders):\n        \"\"\"Filter loaders by tool name.\n\n        Tool names are based on AYON tools loader tool and library\n        loader tool. The new tool merged both into one tool and the difference\n        is based only on current project name.\n\n        Args:\n            project_name (str): Project name.\n            loaders (list[LoaderPlugin]): List of loader plugins.\n\n        Returns:\n            list[LoaderPlugin]: Filtered list of loader plugins.\n        \"\"\"\n\n        # Keep filtering by tool name\n        # - if current context project name is same as project name we do\n        #   expect the tool is used as AYON loader tool, otherwise\n        #   as library loader tool.\n        if project_name == self._get_current_context_project():\n            tool_name = \"loader\"\n        else:\n            tool_name = \"library_loader\"\n        filtered_loaders = []\n        for loader in loaders:\n            tool_names = getattr(loader, \"tool_names\", None)\n            if (\n                tool_names is None\n                or \"*\" in tool_names\n                or tool_name in tool_names\n            ):\n                filtered_loaders.append(loader)\n        return filtered_loaders\n\n    def _create_loader_action_item(\n        self,\n        loader,\n        contexts,\n        entity_ids,\n        entity_type,\n        repre_name=None,\n    ):\n        label = self._get_action_label(loader)\n        if repre_name:\n            label = f\"{label} ({repre_name})\"\n        return ActionItem(\n            LOADER_PLUGIN_ID,\n            data={\n                \"entity_ids\": entity_ids,\n                \"entity_type\": entity_type,\n                \"loader\": get_loader_identifier(loader),\n            },\n            label=label,\n            group_label=None,\n            icon=self._get_action_icon(loader),\n            tooltip=self._get_action_tooltip(loader),\n            order=loader.order,\n            options=loader.get_options(contexts),\n        )\n\n    def _get_loaders(self, project_name):\n        \"\"\"Loaders with loaded settings for a project.\n\n        Questions:\n            Project name is required because of settings. Should we actually\n                pass in current project name instead of project name where\n                we want to show loaders for?\n\n        Returns:\n            tuple[list[ProductLoaderPlugin], list[LoaderPlugin]]: Discovered\n                loader plugins.\n        \"\"\"\n\n        loaders_by_identifier_c = self._loaders_by_identifier[project_name]\n        product_loaders_c = self._product_loaders[project_name]\n        repre_loaders_c = self._repre_loaders[project_name]\n        if loaders_by_identifier_c.is_valid:\n            return product_loaders_c.get_data(), repre_loaders_c.get_data()\n\n        # Get all representation-&gt;loader combinations available for the\n        # index under the cursor, so we can list the user the options.\n        available_loaders = self._filter_loaders_by_tool_name(\n            project_name, discover_loader_plugins(project_name)\n        )\n        repre_loaders = []\n        product_loaders = []\n        loaders_by_identifier = {}\n        for loader_cls in available_loaders:\n            if not loader_cls.enabled:\n                continue\n\n            identifier = get_loader_identifier(loader_cls)\n            loaders_by_identifier[identifier] = loader_cls\n            if issubclass(loader_cls, ProductLoaderPlugin):\n                product_loaders.append(loader_cls)\n            else:\n                repre_loaders.append(loader_cls)\n\n        loaders_by_identifier_c.update_data(loaders_by_identifier)\n        product_loaders_c.update_data(product_loaders)\n        repre_loaders_c.update_data(repre_loaders)\n\n        return product_loaders, repre_loaders\n\n    def _get_loader_by_identifier(self, project_name, identifier):\n        if not self._loaders_by_identifier[project_name].is_valid:\n            self._get_loaders(project_name)\n        loaders_by_identifier_c = self._loaders_by_identifier[project_name]\n        loaders_by_identifier = loaders_by_identifier_c.get_data()\n        return loaders_by_identifier.get(identifier)\n\n    def _contexts_for_versions(self, project_name, version_ids):\n        \"\"\"Get contexts for given version ids.\n\n        Prepare version contexts for 'ProductLoaderPlugin' and representation\n        contexts for 'LoaderPlugin' for all children representations of\n        given versions.\n\n        This method is very similar to '_contexts_for_representations' but the\n        queries of entities are called in a different order.\n\n        Args:\n            project_name (str): Project name.\n            version_ids (Iterable[str]): Version ids.\n\n        Returns:\n            tuple[list[dict[str, Any]], list[dict[str, Any]]]: Version and\n                representation contexts.\n        \"\"\"\n\n        # TODO fix hero version\n        version_context_by_id = {}\n        repre_context_by_id = {}\n        if not project_name and not version_ids:\n            return version_context_by_id, repre_context_by_id\n\n        version_entities = self._get_versions(\n            project_name, version_ids\n        )\n        version_entities_by_id = {}\n        version_entities_by_product_id = collections.defaultdict(list)\n        for version_entity in version_entities:\n            version_id = version_entity[\"id\"]\n            product_id = version_entity[\"productId\"]\n            version_entities_by_id[version_id] = version_entity\n            version_entities_by_product_id[product_id].append(version_entity)\n\n        _product_ids = set(version_entities_by_product_id.keys())\n        _product_entities = self._get_products(\n            project_name, _product_ids\n        )\n        product_entities_by_id = {p[\"id\"]: p for p in _product_entities}\n\n        _folder_ids = {p[\"folderId\"] for p in product_entities_by_id.values()}\n        _folder_entities = self._get_folders(\n            project_name, _folder_ids\n        )\n        folder_entities_by_id = {f[\"id\"]: f for f in _folder_entities}\n\n        project_entity = self._get_project(project_name)\n\n        for version_id, version_entity in version_entities_by_id.items():\n            product_id = version_entity[\"productId\"]\n            product_entity = product_entities_by_id[product_id]\n            folder_id = product_entity[\"folderId\"]\n            folder_entity = folder_entities_by_id[folder_id]\n            version_context_by_id[version_id] = {\n                \"project\": project_entity,\n                \"folder\": folder_entity,\n                \"product\": product_entity,\n                \"version\": version_entity,\n            }\n\n        all_repre_ids = set()\n        for repre_ids in self._get_repre_ids_by_version_ids(\n            project_name, version_ids\n        ).values():\n            all_repre_ids |= repre_ids\n\n        repre_entities = self._get_representations(\n            project_name, all_repre_ids\n        )\n        for repre_entity in repre_entities:\n            version_id = repre_entity[\"versionId\"]\n            version_entity = version_entities_by_id[version_id]\n            product_id = version_entity[\"productId\"]\n            product_entity = product_entities_by_id[product_id]\n            folder_id = product_entity[\"folderId\"]\n            folder_entity = folder_entities_by_id[folder_id]\n\n            repre_context_by_id[repre_entity[\"id\"]] = {\n                \"project\": project_entity,\n                \"folder\": folder_entity,\n                \"product\": product_entity,\n                \"version\": version_entity,\n                \"representation\": repre_entity,\n            }\n\n        return version_context_by_id, repre_context_by_id\n\n    def _contexts_for_representations(self, project_name, repre_ids):\n        \"\"\"Get contexts for given representation ids.\n\n        Prepare version contexts for 'ProductLoaderPlugin' and representation\n        contexts for 'LoaderPlugin' for all children representations of\n        given versions.\n\n        This method is very similar to '_contexts_for_versions' but the\n        queries of entities are called in a different order.\n\n        Args:\n            project_name (str): Project name.\n            repre_ids (Iterable[str]): Representation ids.\n\n        Returns:\n            tuple[list[dict[str, Any]], list[dict[str, Any]]]: Version and\n                representation contexts.\n\n        \"\"\"\n        version_context_by_id = {}\n        repre_context_by_id = {}\n        if not project_name and not repre_ids:\n            return version_context_by_id, repre_context_by_id\n\n        repre_entities = self._get_representations(\n            project_name, repre_ids\n        )\n        version_ids = {r[\"versionId\"] for r in repre_entities}\n        version_entities = self._get_versions(\n            project_name, version_ids\n        )\n        version_entities_by_id = {\n            v[\"id\"]: v for v in version_entities\n        }\n\n        product_ids = {v[\"productId\"] for v in version_entities_by_id.values()}\n        product_entities = self._get_products(\n            project_name, product_ids\n\n        )\n        product_entities_by_id = {\n            p[\"id\"]: p for p in product_entities\n        }\n\n        folder_ids = {p[\"folderId\"] for p in product_entities_by_id.values()}\n        folder_entities = self._get_folders(\n            project_name, folder_ids\n        )\n        folder_entities_by_id = {\n            f[\"id\"]: f for f in folder_entities\n        }\n\n        project_entity = self._get_project(project_name)\n\n        version_context_by_id = {}\n        for version_id, version_entity in version_entities_by_id.items():\n            product_id = version_entity[\"productId\"]\n            product_entity = product_entities_by_id[product_id]\n            folder_id = product_entity[\"folderId\"]\n            folder_entity = folder_entities_by_id[folder_id]\n            version_context_by_id[version_id] = {\n                \"project\": project_entity,\n                \"folder\": folder_entity,\n                \"product\": product_entity,\n                \"version\": version_entity,\n            }\n\n        for repre_entity in repre_entities:\n            version_id = repre_entity[\"versionId\"]\n            version_entity = version_entities_by_id[version_id]\n            product_id = version_entity[\"productId\"]\n            product_entity = product_entities_by_id[product_id]\n            folder_id = product_entity[\"folderId\"]\n            folder_entity = folder_entities_by_id[folder_id]\n\n            repre_context_by_id[repre_entity[\"id\"]] = {\n                \"project\": project_entity,\n                \"folder\": folder_entity,\n                \"product\": product_entity,\n                \"version\": version_entity,\n                \"representation\": repre_entity,\n            }\n        return version_context_by_id, repre_context_by_id\n\n    def _get_project(self, project_name: str) -&gt; dict[str, Any]:\n        cache = self._projects_cache[project_name]\n        if not cache.is_valid:\n            cache.update_data(ayon_api.get_project(project_name))\n        return cache.get_data()\n\n    def _get_folders(\n        self, project_name: str, folder_ids: set[str]\n    ) -&gt; list[dict[str, Any]]:\n        \"\"\"Get folders by ids.\"\"\"\n        return self._get_entities(\n            project_name,\n            folder_ids,\n            self._folders_cache,\n            ayon_api.get_folders,\n            \"folder_ids\",\n        )\n\n    def _get_products(\n        self, project_name: str, product_ids: set[str]\n    ) -&gt; list[dict[str, Any]]:\n        \"\"\"Get products by ids.\"\"\"\n        return self._get_entities(\n            project_name,\n            product_ids,\n            self._products_cache,\n            ayon_api.get_products,\n            \"product_ids\",\n        )\n\n    def _get_versions(\n        self, project_name: str, version_ids: set[str]\n    ) -&gt; list[dict[str, Any]]:\n        \"\"\"Get versions by ids.\"\"\"\n        return self._get_entities(\n            project_name,\n            version_ids,\n            self._versions_cache,\n            ayon_api.get_versions,\n            \"version_ids\",\n        )\n\n    def _get_representations(\n        self, project_name: str, representation_ids: set[str]\n    ) -&gt; list[dict[str, Any]]:\n        \"\"\"Get representations by ids.\"\"\"\n        return self._get_entities(\n            project_name,\n            representation_ids,\n            self._representations_cache,\n            ayon_api.get_representations,\n            \"representation_ids\",\n        )\n\n    def _get_repre_ids_by_version_ids(\n        self, project_name: str, version_ids: set[str]\n    ) -&gt; dict[str, set[str]]:\n        output = {}\n        if not version_ids:\n            return output\n\n        project_cache = self._repre_parents_cache[project_name]\n        missing_ids = set()\n        for version_id in version_ids:\n            cache = project_cache[version_id]\n            if cache.is_valid:\n                output[version_id] = cache.get_data()\n            else:\n                missing_ids.add(version_id)\n\n        if missing_ids:\n            repre_cache = self._representations_cache[project_name]\n            repres_by_parent_id = collections.defaultdict(list)\n            for repre in ayon_api.get_representations(\n                project_name, version_ids=missing_ids\n            ):\n                version_id = repre[\"versionId\"]\n                repre_cache[repre[\"id\"]].update_data(repre)\n                repres_by_parent_id[version_id].append(repre)\n\n            for version_id, repres in repres_by_parent_id.items():\n                repre_ids = {\n                    repre[\"id\"]\n                    for repre in repres\n                }\n                output[version_id] = set(repre_ids)\n                project_cache[version_id].update_data(repre_ids)\n\n        return output\n\n    def _get_entities(\n        self,\n        project_name: str,\n        entity_ids: set[str],\n        cache: NestedCacheItem,\n        getter: Callable,\n        filter_arg: str,\n    ) -&gt; list[dict[str, Any]]:\n        entities = []\n        if not entity_ids:\n            return entities\n\n        missing_ids = set()\n        project_cache = cache[project_name]\n        for entity_id in entity_ids:\n            entity_cache = project_cache[entity_id]\n            if entity_cache.is_valid:\n                entities.append(entity_cache.get_data())\n            else:\n                missing_ids.add(entity_id)\n\n        if missing_ids:\n            for entity in getter(project_name, **{filter_arg: missing_ids}):\n                entities.append(entity)\n                entity_id = entity[\"id\"]\n                project_cache[entity_id].update_data(entity)\n        return entities\n\n    def _get_action_items_for_contexts(\n        self,\n        project_name,\n        version_context_by_id,\n        repre_context_by_id\n    ):\n        \"\"\"Prepare action items based on contexts.\n\n        Actions are prepared based on discovered loader plugins and contexts.\n        The context must be valid for the loader plugin.\n\n        Args:\n            project_name (str): Project name.\n            version_context_by_id (dict[str, dict[str, Any]]): Version\n                contexts by version id.\n            repre_context_by_id (dict[str, dict[str, Any]]): Representation\n        \"\"\"\n\n        action_items = []\n        if not version_context_by_id and not repre_context_by_id:\n            return action_items\n\n        product_loaders, repre_loaders = self._get_loaders(project_name)\n\n        repre_contexts_by_name = collections.defaultdict(list)\n        for repre_context in repre_context_by_id.values():\n            repre_name = repre_context[\"representation\"][\"name\"]\n            repre_contexts_by_name[repre_name].append(repre_context)\n\n        for loader in repre_loaders:\n            for repre_name, repre_contexts in repre_contexts_by_name.items():\n                filtered_repre_contexts = filter_repre_contexts_by_loader(\n                    repre_contexts, loader)\n                if not filtered_repre_contexts:\n                    continue\n\n                repre_ids = {\n                    repre_context[\"representation\"][\"id\"]\n                    for repre_context in filtered_repre_contexts\n                }\n\n                item = self._create_loader_action_item(\n                    loader,\n                    repre_contexts,\n                    repre_ids,\n                    \"representation\",\n                    repre_name=repre_name,\n                )\n                action_items.append(item)\n\n        # Product Loaders.\n        product_folder_ids = set()\n        product_ids = set()\n        for product_context in version_context_by_id.values():\n            product_ids.add(product_context[\"product\"][\"id\"])\n            product_folder_ids.add(product_context[\"folder\"][\"id\"])\n\n        version_ids = set(version_context_by_id.keys())\n        version_contexts = list(version_context_by_id.values())\n        for loader in product_loaders:\n            item = self._create_loader_action_item(\n                loader,\n                version_contexts,\n                version_ids,\n                \"version\",\n            )\n            action_items.append(item)\n        return action_items\n\n    def _get_loader_action_items(\n        self,\n        project_name: str,\n        entity_ids: set[str],\n        entity_type: str,\n        version_context_by_id: dict[str, dict[str, Any]],\n        repre_context_by_id: dict[str, dict[str, Any]],\n    ) -&gt; list[ActionItem]:\n        \"\"\"\n\n        Args:\n            project_name (str): Project name.\n            entity_ids (set[str]): Selected entity ids.\n            entity_type (str): Selected entity type.\n            version_context_by_id (dict[str, dict[str, Any]]): Version context\n                by id.\n            repre_context_by_id (dict[str, dict[str, Any]]): Representation\n                context by id.\n\n        Returns:\n            list[ActionItem]: List of action items.\n\n        \"\"\"\n        entities_cache = self._prepare_entities_cache(\n            project_name,\n            entity_type,\n            version_context_by_id,\n            repre_context_by_id,\n        )\n        selection = LoaderActionSelection(\n            project_name,\n            entity_ids,\n            entity_type,\n            entities_cache=entities_cache\n        )\n        items = []\n        for action in self._loader_actions.get_action_items(selection):\n            items.append(ActionItem(\n                action.identifier,\n                label=action.label,\n                group_label=action.group_label,\n                icon=action.icon,\n                tooltip=None,  # action.tooltip,\n                order=action.order,\n                data=action.data,\n                options=None,  # action.options,\n            ))\n        return items\n\n    def _prepare_entities_cache(\n        self,\n        project_name: str,\n        entity_type: str,\n        version_context_by_id: dict[str, dict[str, Any]],\n        repre_context_by_id: dict[str, dict[str, Any]],\n    ):\n        project_entity = None\n        folders_by_id = {}\n        products_by_id = {}\n        versions_by_id = {}\n        representations_by_id = {}\n        for context in version_context_by_id.values():\n            if project_entity is None:\n                project_entity = context[\"project\"]\n            folder_entity = context[\"folder\"]\n            product_entity = context[\"product\"]\n            version_entity = context[\"version\"]\n            folders_by_id[folder_entity[\"id\"]] = folder_entity\n            products_by_id[product_entity[\"id\"]] = product_entity\n            versions_by_id[version_entity[\"id\"]] = version_entity\n\n        for context in repre_context_by_id.values():\n            repre_entity = context[\"representation\"]\n            representations_by_id[repre_entity[\"id\"]] = repre_entity\n\n        # Mapping has to be for all child entities which is available for\n        #   representations only if version is selected\n        representation_ids_by_version_id = {}\n        if entity_type == \"version\":\n            representation_ids_by_version_id = {\n                version_id: set()\n                for version_id in versions_by_id\n            }\n            for context in repre_context_by_id.values():\n                repre_entity = context[\"representation\"]\n                v_id = repre_entity[\"versionId\"]\n                representation_ids_by_version_id[v_id].add(repre_entity[\"id\"])\n\n        return SelectionEntitiesCache(\n            project_name,\n            project_entity=project_entity,\n            folders_by_id=folders_by_id,\n            products_by_id=products_by_id,\n            versions_by_id=versions_by_id,\n            representations_by_id=representations_by_id,\n            representation_ids_by_version_id=representation_ids_by_version_id,\n        )\n\n    def _trigger_version_loader(\n        self,\n        loader,\n        options,\n        project_name,\n        version_ids,\n    ):\n        \"\"\"Trigger version loader.\n\n        This triggers 'load' method of 'ProductLoaderPlugin' for given version\n        ids.\n\n        Note:\n            Even when the plugin is 'ProductLoaderPlugin' it actually expects\n                versions and should be named 'VersionLoaderPlugin'. Because it\n                is planned to refactor load system and introduce\n                'LoaderAction' plugins it is not relevant to change it\n                anymore.\n\n        Args:\n            loader (ProductLoaderPlugin): Loader plugin to use.\n            options (dict): Option values for loader.\n            project_name (str): Project name.\n            version_ids (Iterable[str]): Version ids.\n        \"\"\"\n\n        project_entity = ayon_api.get_project(project_name)\n\n        version_entities = list(ayon_api.get_versions(\n            project_name, version_ids=version_ids\n        ))\n        product_ids = {v[\"productId\"] for v in version_entities}\n        product_entities = self._get_products(\n            project_name, product_ids\n        )\n        product_entities_by_id = {p[\"id\"]: p for p in product_entities}\n        folder_ids = {p[\"folderId\"] for p in product_entities_by_id.values()}\n        folder_entities = self._get_folders(\n            project_name, folder_ids=folder_ids\n        )\n        folder_entities_by_id = {f[\"id\"]: f for f in folder_entities}\n        product_contexts = []\n        for version_entity in version_entities:\n            product_id = version_entity[\"productId\"]\n            product_entity = product_entities_by_id[product_id]\n            folder_id = product_entity[\"folderId\"]\n            folder_entity = folder_entities_by_id[folder_id]\n            product_contexts.append({\n                \"project\": project_entity,\n                \"folder\": folder_entity,\n                \"product\": product_entity,\n                \"version\": version_entity,\n            })\n\n        return self._load_products_by_loader(\n            loader, product_contexts, options\n        )\n\n    def _trigger_representation_loader(\n        self,\n        loader,\n        options,\n        project_name,\n        representation_ids,\n    ):\n        \"\"\"Trigger representation loader.\n\n        This triggers 'load' method of 'LoaderPlugin' for given representation\n            ids. For that are prepared contexts for each representation, with\n            all parent entities.\n\n        Args:\n            loader (LoaderPlugin): Loader plugin to use.\n            options (dict): Option values for loader.\n            project_name (str): Project name.\n            representation_ids (Iterable[str]): Representation ids.\n        \"\"\"\n\n        project_entity = ayon_api.get_project(project_name)\n        repre_entities = list(ayon_api.get_representations(\n            project_name, representation_ids=representation_ids\n        ))\n        version_ids = {r[\"versionId\"] for r in repre_entities}\n        version_entities = ayon_api.get_versions(\n            project_name, version_ids=version_ids\n        )\n        version_entities_by_id = {v[\"id\"]: v for v in version_entities}\n        product_ids = {\n            v[\"productId\"] for v in version_entities_by_id.values()\n        }\n        product_entities = ayon_api.get_products(\n            project_name, product_ids=product_ids\n        )\n        product_entities_by_id = {p[\"id\"]: p for p in product_entities}\n        folder_ids = {p[\"folderId\"] for p in product_entities_by_id.values()}\n        folder_entities = ayon_api.get_folders(\n            project_name, folder_ids=folder_ids\n        )\n        folder_entities_by_id = {f[\"id\"]: f for f in folder_entities}\n        repre_contexts = []\n        for repre_entity in repre_entities:\n            version_id = repre_entity[\"versionId\"]\n            version_entity = version_entities_by_id[version_id]\n            product_id = version_entity[\"productId\"]\n            product_entity = product_entities_by_id[product_id]\n            folder_id = product_entity[\"folderId\"]\n            folder_entity = folder_entities_by_id[folder_id]\n            repre_contexts.append({\n                \"project\": project_entity,\n                \"folder\": folder_entity,\n                \"product\": product_entity,\n                \"version\": version_entity,\n                \"representation\": repre_entity,\n            })\n\n        return self._load_representations_by_loader(\n            loader, repre_contexts, options\n        )\n\n    def _load_representations_by_loader(\n        self,\n        loader,\n        repre_contexts,\n        options\n    ):\n        \"\"\"Loops through list of repre_contexts and loads them with one loader\n\n        Args:\n            loader (LoaderPlugin): Loader plugin to use.\n            repre_contexts (list[dict]): Full info about selected\n                representations, containing repre, version, product, folder\n                and project entities.\n            options (dict): Data from options.\n        \"\"\"\n\n        error_info = []\n        for repre_context in repre_contexts:\n            version_entity = repre_context[\"version\"]\n            version = version_entity[\"version\"]\n            if version &lt; 0:\n                version = \"Hero\"\n            try:\n                load_with_repre_context(\n                    loader,\n                    repre_context,\n                    options=options\n                )\n\n            except IncompatibleLoaderError as exc:\n                print(exc)\n                error_info.append((\n                    \"Incompatible Loader\",\n                    None,\n                    repre_context[\"representation\"][\"name\"],\n                    repre_context[\"product\"][\"name\"],\n                    version\n                ))\n\n            except Exception as exc:\n                formatted_traceback = None\n                if not isinstance(exc, LoadError):\n                    exc_type, exc_value, exc_traceback = sys.exc_info()\n                    formatted_traceback = \"\".join(traceback.format_exception(\n                        exc_type, exc_value, exc_traceback\n                    ))\n\n                error_info.append((\n                    str(exc),\n                    formatted_traceback,\n                    repre_context[\"representation\"][\"name\"],\n                    repre_context[\"product\"][\"name\"],\n                    version\n                ))\n        return error_info\n\n    def _load_products_by_loader(\n        self,\n        loader,\n        version_contexts,\n        options\n    ):\n        \"\"\"Triggers load with ProductLoader type of loaders.\n\n        Warning:\n            Plugin is named 'ProductLoader' but version is passed to context\n                too.\n\n        Args:\n            loader (ProductLoader): Loader used to load.\n            version_contexts (list[dict[str, Any]]): For context for each\n                version.\n            options (dict[str, Any]): Options for loader that user could fill.\n        \"\"\"\n\n        error_info = []\n        if loader.is_multiple_contexts_compatible:\n            product_names = []\n            for context in version_contexts:\n                product_name = context.get(\"product\", {}).get(\"name\") or \"N/A\"\n                product_names.append(product_name)\n            try:\n                load_with_product_contexts(\n                    loader,\n                    version_contexts,\n                    options=options\n                )\n            except Exception as exc:\n                formatted_traceback = None\n                if not isinstance(exc, LoadError):\n                    exc_type, exc_value, exc_traceback = sys.exc_info()\n                    formatted_traceback = \"\".join(traceback.format_exception(\n                        exc_type, exc_value, exc_traceback\n                    ))\n                error_info.append((\n                    str(exc),\n                    formatted_traceback,\n                    None,\n                    \", \".join(product_names),\n                    None\n                ))\n        else:\n            for version_context in version_contexts:\n                product_name = (\n                    version_context.get(\"product\", {}).get(\"name\") or \"N/A\"\n                )\n                try:\n                    load_with_product_context(\n                        loader,\n                        version_context,\n                        options=options\n                    )\n\n                except Exception as exc:\n                    formatted_traceback = None\n                    if not isinstance(exc, LoadError):\n                        exc_type, exc_value, exc_traceback = sys.exc_info()\n                        formatted_traceback = \"\".join(\n                            traceback.format_exception(\n                                exc_type, exc_value, exc_traceback\n                            )\n                        )\n\n                    error_info.append((\n                        str(exc),\n                        formatted_traceback,\n                        None,\n                        product_name,\n                        None\n                    ))\n\n        return error_info\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/models/index.html#client.ayon_core.tools.loader.models.LoaderActionsModel.reset","title":"<code>reset()</code>","text":"<p>Reset the model with all cached items.</p> Source code in <code>client/ayon_core/tools/loader/models/actions.py</code> <pre><code>def reset(self):\n    \"\"\"Reset the model with all cached items.\"\"\"\n\n    self._current_context_project = NOT_SET\n    self._loaders_by_identifier.reset()\n    self._product_loaders.reset()\n    self._repre_loaders.reset()\n    self._loader_actions.reset()\n\n    self._folders_cache.reset()\n    self._tasks_cache.reset()\n    self._products_cache.reset()\n    self._versions_cache.reset()\n    self._representations_cache.reset()\n    self._repre_parents_cache.reset()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/models/index.html#client.ayon_core.tools.loader.models.LoaderActionsModel.trigger_action_item","title":"<code>trigger_action_item(identifier, project_name, selected_ids, selected_entity_type, data, options, form_values)</code>","text":"<p>Trigger action by identifier.</p> <p>Triggers the action by identifier for given contexts.</p> <p>Triggers events \"load.started\" and \"load.finished\". Finished event     also contains \"error_info\" key with error information if any     happened.</p> <p>Parameters:</p> Name Type Description Default <code>identifier</code> <code>str</code> <p>Plugin identifier.</p> required <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>selected_ids</code> <code>set[str]</code> <p>Selected entity ids.</p> required <code>selected_entity_type</code> <code>str</code> <p>Selected entity type.</p> required <code>data</code> <code>Optional[dict[str, Any]]</code> <p>Additional action item data.</p> required <code>options</code> <code>dict[str, Any]</code> <p>Loader option values.</p> required <code>form_values</code> <code>dict[str, Any]</code> <p>Form values.</p> required Source code in <code>client/ayon_core/tools/loader/models/actions.py</code> <pre><code>def trigger_action_item(\n    self,\n    identifier: str,\n    project_name: str,\n    selected_ids: set[str],\n    selected_entity_type: str,\n    data: Optional[dict[str, Any]],\n    options: dict[str, Any],\n    form_values: dict[str, Any],\n):\n    \"\"\"Trigger action by identifier.\n\n    Triggers the action by identifier for given contexts.\n\n    Triggers events \"load.started\" and \"load.finished\". Finished event\n        also contains \"error_info\" key with error information if any\n        happened.\n\n    Args:\n        identifier (str): Plugin identifier.\n        project_name (str): Project name.\n        selected_ids (set[str]): Selected entity ids.\n        selected_entity_type (str): Selected entity type.\n        data (Optional[dict[str, Any]]): Additional action item data.\n        options (dict[str, Any]): Loader option values.\n        form_values (dict[str, Any]): Form values.\n\n    \"\"\"\n    event_data = {\n        \"identifier\": identifier,\n        \"project_name\": project_name,\n        \"selected_ids\": list(selected_ids),\n        \"selected_entity_type\": selected_entity_type,\n        \"data\": data,\n        \"id\": uuid.uuid4().hex,\n    }\n    self._controller.emit_event(\n        \"load.started\",\n        event_data,\n        ACTIONS_MODEL_SENDER,\n    )\n    if identifier != LOADER_PLUGIN_ID:\n        result = None\n        crashed = False\n        try:\n            result = self._loader_actions.execute_action(\n                identifier=identifier,\n                selection=LoaderActionSelection(\n                    project_name,\n                    selected_ids,\n                    selected_entity_type,\n                ),\n                data=data,\n                form_values=form_values,\n            )\n\n        except Exception:\n            crashed = True\n            self._log.warning(\n                f\"Failed to execute action '{identifier}'\",\n                exc_info=True,\n            )\n\n        event_data[\"result\"] = result\n        event_data[\"crashed\"] = crashed\n        self._controller.emit_event(\n            \"loader.action.finished\",\n            event_data,\n            ACTIONS_MODEL_SENDER,\n        )\n        return\n\n    loader = self._get_loader_by_identifier(\n        project_name, data[\"loader\"]\n    )\n    entity_type = data[\"entity_type\"]\n    entity_ids = data[\"entity_ids\"]\n    if entity_type == \"version\":\n        error_info = self._trigger_version_loader(\n            loader,\n            options,\n            project_name,\n            entity_ids,\n        )\n    elif entity_type == \"representation\":\n        error_info = self._trigger_representation_loader(\n            loader,\n            options,\n            project_name,\n            entity_ids,\n        )\n    else:\n        raise NotImplementedError(\n            f\"Invalid entity type '{entity_type}' to trigger action item\"\n        )\n\n    event_data[\"error_info\"] = error_info\n    self._controller.emit_event(\n        \"load.finished\",\n        event_data,\n        ACTIONS_MODEL_SENDER,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/models/index.html#client.ayon_core.tools.loader.models.ProductsModel","title":"<code>ProductsModel</code>","text":"<p>Model for products, version and representation.</p> <p>All the entities are product based. This model prepares data for UI and caches it for faster access.</p> Note <p>Data are not used for actions model because that would require to     break OpenPype compatibility of 'LoaderPlugin's.</p> Source code in <code>client/ayon_core/tools/loader/models/products.py</code> <pre><code>class ProductsModel:\n    \"\"\"Model for products, version and representation.\n\n    All the entities are product based. This model prepares data for UI\n    and caches it for faster access.\n\n    Note:\n        Data are not used for actions model because that would require to\n            break OpenPype compatibility of 'LoaderPlugin's.\n    \"\"\"\n\n    lifetime = 60  # In seconds (minute by default)\n\n    def __init__(self, controller):\n        self._controller = controller\n\n        # Mapping helpers\n        # NOTE - mapping must be cleaned up with cache cleanup\n        self._product_item_by_id = collections.defaultdict(dict)\n        self._version_item_by_id = collections.defaultdict(dict)\n        self._product_folder_ids_mapping = collections.defaultdict(dict)\n\n        # Cache helpers\n        self._product_type_items_cache = NestedCacheItem(\n            levels=1, default_factory=list, lifetime=self.lifetime)\n        self._product_base_type_items_cache = NestedCacheItem(\n            levels=1, default_factory=list, lifetime=self.lifetime)\n        self._product_items_cache = NestedCacheItem(\n            levels=2, default_factory=dict, lifetime=self.lifetime)\n        self._repre_items_cache = NestedCacheItem(\n            levels=2, default_factory=dict, lifetime=self.lifetime)\n\n    def reset(self):\n        \"\"\"Reset model with all cached data.\"\"\"\n\n        self._product_item_by_id.clear()\n        self._version_item_by_id.clear()\n        self._product_folder_ids_mapping.clear()\n\n        self._product_type_items_cache.reset()\n        self._product_items_cache.reset()\n        self._repre_items_cache.reset()\n\n    def get_product_type_items(\n        self, project_name: Optional[str]\n    ) -&gt; list[ProductTypeItem]:\n        \"\"\"Product type items for project.\n\n        Args:\n            project_name (Union[str, None]): Project name.\n\n        Returns:\n            list[ProductTypeItem]: Product type items.\n\n        \"\"\"\n        if not project_name:\n            return []\n\n        cache = self._product_type_items_cache[project_name]\n        if not cache.is_valid:\n            icons_mapping = self._get_product_type_icons(project_name)\n            product_types = self._get_project_product_types(project_name)\n            cache.update_data([\n                ProductTypeItem(\n                    product_type[\"name\"],\n                    icons_mapping.get_icon(product_type=product_type[\"name\"]),\n                )\n                for product_type in product_types\n            ])\n        return cache.get_data()\n\n    def get_product_base_type_items(\n        self, project_name: Optional[str]\n    ) -&gt; list[ProductBaseTypeItem]:\n        \"\"\"Product base type items for the project.\n\n        Notes:\n            This will be used for filtering product types in UI when\n                product base types are fully implemented.\n\n        Args:\n            project_name (optional, str): Project name.\n\n        Returns:\n            list[ProductBaseTypeDict]: Product base type items.\n\n        \"\"\"\n        if not project_name:\n            return []\n\n        cache = self._product_base_type_items_cache[project_name]\n        if not cache.is_valid:\n            icons_mapping = self._get_product_type_icons(project_name)\n            product_base_types = []\n            # TODO add temp implementation here when it is actually\n            #   implemented and available on server.\n            if hasattr(ayon_api, \"get_project_product_base_types\"):\n                product_base_types = ayon_api.get_project_product_base_types(\n                    project_name\n                )\n            cache.update_data([\n                ProductBaseTypeItem(\n                    product_base_type[\"name\"],\n                    icons_mapping.get_icon(product_base_type[\"name\"]),\n                )\n                for product_base_type in product_base_types\n            ])\n        return cache.get_data()\n\n    def get_product_items(self, project_name, folder_ids, sender):\n        \"\"\"Product items with versions for project and folder ids.\n\n        Product items also contain version items. They're directly connected\n        to product items in the UI and the separation is not needed.\n\n        Args:\n            project_name (Union[str, None]): Project name.\n            folder_ids (Iterable[str]): Folder ids.\n            sender (Union[str, None]): Who triggered the method.\n\n        Returns:\n            list[ProductItem]: Product items.\n        \"\"\"\n\n        if not project_name or not folder_ids:\n            return []\n\n        project_cache = self._product_items_cache[project_name]\n        output = []\n        folder_ids_to_update = set()\n        for folder_id in folder_ids:\n            cache = project_cache[folder_id]\n            if cache.is_valid:\n                output.extend(cache.get_data().values())\n            else:\n                folder_ids_to_update.add(folder_id)\n\n        self._refresh_product_items(\n            project_name, folder_ids_to_update, sender)\n\n        for folder_id in folder_ids_to_update:\n            cache = project_cache[folder_id]\n            output.extend(cache.get_data().values())\n        return output\n\n    def get_product_item(self, project_name, product_id):\n        \"\"\"Get product item based on passed product id.\n\n        This method is using cached items, but if cache is not valid it also\n        can query the item.\n\n        Args:\n            project_name (Union[str, None]): Where to look for product.\n            product_id (Union[str, None]): Product id to receive.\n\n        Returns:\n            Union[ProductItem, None]: Product item or 'None' if not found.\n        \"\"\"\n\n        if not any((project_name, product_id)):\n            return None\n\n        product_items_by_id = self._product_item_by_id[project_name]\n        product_item = product_items_by_id.get(product_id)\n        if product_item is not None:\n            return product_item\n        for product_item in self._query_product_items_by_ids(\n            project_name, product_ids=[product_id]\n        ).values():\n            return product_item\n\n    def get_product_ids_by_repre_ids(self, project_name, repre_ids):\n        \"\"\"Get product ids based on passed representation ids.\n\n        Args:\n            project_name (str): Where to look for representations.\n            repre_ids (Iterable[str]): Representation ids.\n\n        Returns:\n            set[str]: Product ids for passed representation ids.\n        \"\"\"\n\n        # TODO look out how to use single server call\n        if not repre_ids:\n            return set()\n        repres = ayon_api.get_representations(\n            project_name, repre_ids, fields=[\"versionId\"]\n        )\n        version_ids = {repre[\"versionId\"] for repre in repres}\n        if not version_ids:\n            return set()\n        versions = ayon_api.get_versions(\n            project_name, version_ids=version_ids, fields=[\"productId\"]\n        )\n        return {v[\"productId\"] for v in versions}\n\n    def get_repre_items(self, project_name, version_ids, sender):\n        \"\"\"Get representation items for passed version ids.\n\n        Args:\n            project_name (str): Project name.\n            version_ids (Iterable[str]): Version ids.\n            sender (Union[str, None]): Who triggered the method.\n\n        Returns:\n            list[RepreItem]: Representation items.\n        \"\"\"\n\n        output = []\n        if not any((project_name, version_ids)):\n            return output\n\n        invalid_version_ids = set()\n        project_cache = self._repre_items_cache[project_name]\n        for version_id in version_ids:\n            version_cache = project_cache[version_id]\n            if version_cache.is_valid:\n                output.extend(version_cache.get_data().values())\n            else:\n                invalid_version_ids.add(version_id)\n\n        if invalid_version_ids:\n            self.refresh_representation_items(\n                project_name, invalid_version_ids, sender\n            )\n\n        for version_id in invalid_version_ids:\n            version_cache = project_cache[version_id]\n            output.extend(version_cache.get_data().values())\n\n        return output\n\n    def get_versions_repre_count(self, project_name, version_ids, sender):\n        \"\"\"Get representation count for passed version ids.\n\n        Args:\n            project_name (str): Project name.\n            version_ids (Iterable[str]): Version ids.\n            sender (Union[str, None]): Who triggered the method.\n\n        Returns:\n            dict[str, int]: Number of representations by version id.\n        \"\"\"\n\n        output = {}\n        if not any((project_name, version_ids)):\n            return output\n\n        invalid_version_ids = set()\n        project_cache = self._repre_items_cache[project_name]\n        for version_id in version_ids:\n            version_cache = project_cache[version_id]\n            if version_cache.is_valid:\n                output[version_id] = len(version_cache.get_data())\n            else:\n                invalid_version_ids.add(version_id)\n\n        if invalid_version_ids:\n            self.refresh_representation_items(\n                project_name, invalid_version_ids, sender\n            )\n\n        for version_id in invalid_version_ids:\n            version_cache = project_cache[version_id]\n            output[version_id] = len(version_cache.get_data())\n\n        return output\n\n    def change_products_group(self, project_name, product_ids, group_name):\n        \"\"\"Change group name for passed product ids.\n\n        Group name is stored in 'attrib' of product entity and is used in UI\n        to group items.\n\n        Method triggers \"products.group.changed\" event with data:\n            {\n                \"project_name\": project_name,\n                \"folder_ids\": folder_ids,\n                \"product_ids\": product_ids,\n                \"group_name\": group_name\n            }\n\n        Args:\n            project_name (str): Project name.\n            product_ids (Iterable[str]): Product ids to change group name for.\n            group_name (str): Group name to set.\n        \"\"\"\n\n        if not product_ids:\n            return\n\n        product_items = self._get_product_items_by_id(\n            project_name, product_ids\n        )\n        if not product_items:\n            return\n\n        session = OperationsSession()\n        folder_ids = set()\n        for product_item in product_items.values():\n            session.update_entity(\n                project_name,\n                \"product\",\n                product_item.product_id,\n                {\"attrib\": {\"productGroup\": group_name or None}}\n            )\n            folder_ids.add(product_item.folder_id)\n            product_item.group_name = group_name\n\n        session.commit()\n        self._controller.emit_event(\n            \"products.group.changed\",\n            {\n                \"project_name\": project_name,\n                \"folder_ids\": folder_ids,\n                \"product_ids\": product_ids,\n                \"group_name\": group_name,\n            },\n            PRODUCTS_MODEL_SENDER\n        )\n\n    def _get_project_product_types(self, project_name: str) -&gt; list[dict]:\n        \"\"\"This is a temporary solution for product types fetching.\n\n        There was a bug in ayon_api.get_project(...) which did not use GraphQl\n            but REST instead. That is fixed in ayon-python-api 1.2.6 that will\n            be as part of ayon launcher 1.4.3 release.\n\n        \"\"\"\n        if not project_name:\n            return []\n        query = project_graphql_query({\"productTypes.name\"})\n        query.set_variable_value(\"projectName\", project_name)\n        parsed_data = query.query(ayon_api.get_server_api_connection())\n        project = parsed_data[\"project\"]\n        if project is None:\n            return []\n        return project[\"productTypes\"]\n\n    def _get_product_type_icons(\n        self, project_name: Optional[str]\n    ) -&gt; ProductTypeIconMapping:\n        return self._controller.get_product_type_icons_mapping(project_name)\n\n    def _get_product_items_by_id(self, project_name, product_ids):\n        product_item_by_id = self._product_item_by_id[project_name]\n        missing_product_ids = set()\n        output = {}\n        for product_id in product_ids:\n            product_item = product_item_by_id.get(product_id)\n            if product_item is not None:\n                output[product_id] = product_item\n            else:\n                missing_product_ids.add(product_id)\n\n        output.update(\n            self._query_product_items_by_ids(\n                project_name, product_ids=missing_product_ids\n            )\n        )\n        return output\n\n    def _get_version_items_by_id(self, project_name, version_ids):\n        version_item_by_id = self._version_item_by_id[project_name]\n        missing_version_ids = set()\n        output = {}\n        for version_id in version_ids:\n            version_item = version_item_by_id.get(version_id)\n            if version_item is not None:\n                output[version_id] = version_item\n            else:\n                missing_version_ids.add(version_id)\n\n        output.update(\n            self._query_version_items_by_ids(\n                project_name, missing_version_ids\n            )\n        )\n        return output\n\n    def _create_product_items(\n        self,\n        project_name: str,\n        products: Iterable[ProductDict],\n        versions: Iterable[VersionDict],\n        folder_items=None,\n    ):\n        if folder_items is None:\n            folder_items = self._controller.get_folder_items(project_name)\n\n        loaded_product_ids = self._controller.get_loaded_product_ids()\n\n        versions_by_product_id = collections.defaultdict(list)\n        for version in versions:\n            versions_by_product_id[version[\"productId\"]].append(version)\n\n        output: dict[str, ProductItem] = {}\n        icons_mapping = self._get_product_type_icons(project_name)\n        for product in products:\n            product_id = product[\"id\"]\n            folder_id = product[\"folderId\"]\n            folder_item = folder_items.get(folder_id)\n            if not folder_item:\n                continue\n            versions = versions_by_product_id[product_id]\n            if not versions:\n                continue\n            product_item = product_item_from_entity(\n                product,\n                versions,\n                folder_item.label,\n                icons_mapping,\n                product_id in loaded_product_ids,\n            )\n            output[product_id] = product_item\n        return output\n\n    def _query_product_items_by_ids(\n        self,\n        project_name,\n        folder_ids=None,\n        product_ids=None,\n        folder_items=None\n    ):\n        \"\"\"Query product items.\n\n        This method does get from, or store to, cache attributes.\n\n        One of 'product_ids' or 'folder_ids' must be passed to the method.\n\n        Args:\n            project_name (str): Project name.\n            folder_ids (Optional[Iterable[str]]): Folder ids under which are\n                products.\n            product_ids (Optional[Iterable[str]]): Product ids to use.\n            folder_items (Optional[Dict[str, FolderItem]]): Prepared folder\n                items from controller.\n\n        Returns:\n            dict[str, ProductItem]: Product items by product id.\n        \"\"\"\n\n        if not folder_ids and not product_ids:\n            return {}\n\n        kwargs = {}\n        if folder_ids is not None:\n            kwargs[\"folder_ids\"] = folder_ids\n\n        if product_ids is not None:\n            kwargs[\"product_ids\"] = product_ids\n\n        products = list(ayon_api.get_products(project_name, **kwargs))\n        product_ids = {product[\"id\"] for product in products}\n\n        # Add 'status' to fields -&gt; fixed in ayon-python-api 1.0.4\n        fields = ayon_api.get_default_fields_for_type(\"version\")\n        fields.add(\"status\")\n        versions = ayon_api.get_versions(\n            project_name, product_ids=product_ids, fields=fields\n        )\n\n        return self._create_product_items(\n            project_name, products, versions, folder_items=folder_items\n        )\n\n    def _query_version_items_by_ids(self, project_name, version_ids):\n        versions = list(ayon_api.get_versions(\n            project_name, version_ids=version_ids\n        ))\n        product_ids = {version[\"productId\"] for version in versions}\n        products = list(ayon_api.get_products(\n            project_name, product_ids=product_ids\n        ))\n        product_items = self._create_product_items(\n            project_name, products, versions\n        )\n        version_items = {}\n        for product_item in product_items.values():\n            version_items.update(product_item.version_items)\n        return version_items\n\n    def _clear_product_version_items(self, project_name, folder_ids):\n        \"\"\"Clear product and version items from memory.\n\n        When products are re-queried for a folders, the old product and version\n        items in '_product_item_by_id' and '_version_item_by_id' should\n        be cleaned up from memory. And mapping in stored in\n        '_product_folder_ids_mapping' is not relevant either.\n\n        Args:\n            project_name (str): Name of project.\n            folder_ids (Iterable[str]): Folder ids which are being refreshed.\n        \"\"\"\n\n        project_mapping = self._product_folder_ids_mapping[project_name]\n        if not project_mapping:\n            return\n\n        product_item_by_id = self._product_item_by_id[project_name]\n        version_item_by_id = self._version_item_by_id[project_name]\n        for folder_id in folder_ids:\n            product_ids = project_mapping.pop(folder_id, None)\n            if not product_ids:\n                continue\n\n            for product_id in product_ids:\n                product_item = product_item_by_id.pop(product_id, None)\n                if product_item is None:\n                    continue\n                for version_item in product_item.version_items.values():\n                    version_item_by_id.pop(version_item.version_id, None)\n\n    def _refresh_product_items(self, project_name, folder_ids, sender):\n        \"\"\"Refresh product items and store them in cache.\n\n        Args:\n            project_name (str): Name of project.\n            folder_ids (Iterable[str]): Folder ids which are being refreshed.\n            sender (Union[str, None]): Who triggered the refresh.\n        \"\"\"\n\n        if not project_name or not folder_ids:\n            return\n\n        self._clear_product_version_items(project_name, folder_ids)\n\n        project_mapping = self._product_folder_ids_mapping[project_name]\n        product_item_by_id = self._product_item_by_id[project_name]\n        version_item_by_id = self._version_item_by_id[project_name]\n\n        for folder_id in folder_ids:\n            project_mapping[folder_id] = set()\n\n        with self._product_refresh_event_manager(\n            project_name, folder_ids, sender\n        ):\n            folder_items = self._controller.get_folder_items(project_name)\n            items_by_folder_id = {\n                folder_id: {}\n                for folder_id in folder_ids\n            }\n            product_items_by_id = self._query_product_items_by_ids(\n                project_name,\n                folder_ids=folder_ids,\n                folder_items=folder_items\n            )\n            for product_id, product_item in product_items_by_id.items():\n                folder_id = product_item.folder_id\n                items_by_folder_id[product_item.folder_id][product_id] = (\n                    product_item\n                )\n\n                project_mapping[folder_id].add(product_id)\n                product_item_by_id[product_id] = product_item\n                for version_id, version_item in (\n                    product_item.version_items.items()\n                ):\n                    version_item_by_id[version_id] = version_item\n\n            project_cache = self._product_items_cache[project_name]\n            for folder_id, product_items in items_by_folder_id.items():\n                project_cache[folder_id].update_data(product_items)\n\n    @contextlib.contextmanager\n    def _product_refresh_event_manager(\n        self, project_name, folder_ids, sender\n    ):\n        self._controller.emit_event(\n            \"products.refresh.started\",\n            {\n                \"project_name\": project_name,\n                \"folder_ids\": folder_ids,\n                \"sender\": sender,\n            },\n            PRODUCTS_MODEL_SENDER\n        )\n        try:\n            yield\n\n        finally:\n            self._controller.emit_event(\n                \"products.refresh.finished\",\n                {\n                    \"project_name\": project_name,\n                    \"folder_ids\": folder_ids,\n                    \"sender\": sender,\n                },\n                PRODUCTS_MODEL_SENDER\n            )\n\n    def refresh_representation_items(\n        self, project_name, version_ids, sender\n    ):\n        if not any((project_name, version_ids)):\n            return\n        self._controller.emit_event(\n            \"model.representations.refresh.started\",\n            {\n                \"project_name\": project_name,\n                \"version_ids\": version_ids,\n                \"sender\": sender,\n            },\n            PRODUCTS_MODEL_SENDER\n        )\n        failed = False\n        try:\n            self._refresh_representation_items(project_name, version_ids)\n        except Exception:\n            # TODO add more information about failed refresh\n            failed = True\n\n        self._controller.emit_event(\n            \"model.representations.refresh.finished\",\n            {\n                \"project_name\": project_name,\n                \"version_ids\": version_ids,\n                \"sender\": sender,\n                \"failed\": failed,\n            },\n            PRODUCTS_MODEL_SENDER\n        )\n\n    def _refresh_representation_items(self, project_name, version_ids):\n        representations = list(ayon_api.get_representations(\n            project_name,\n            version_ids=version_ids,\n            fields=[\"id\", \"name\", \"versionId\"]\n        ))\n\n        version_items_by_id = self._get_version_items_by_id(\n            project_name, version_ids\n        )\n        product_ids = {\n            version_item.product_id\n            for version_item in version_items_by_id.values()\n        }\n        product_items_by_id = self._get_product_items_by_id(\n            project_name, product_ids\n        )\n        repre_icon = AwesomeFontIcon(\n            \"fa.file-o\",\n            color=get_default_entity_icon_color(),\n        )\n        repre_items_by_version_id = collections.defaultdict(dict)\n        for representation in representations:\n            version_id = representation[\"versionId\"]\n            version_item = version_items_by_id.get(version_id)\n            if version_item is None:\n                continue\n            product_item = product_items_by_id.get(version_item.product_id)\n            if product_item is None:\n                continue\n            repre_id = representation[\"id\"]\n            repre_item = RepreItem(\n                repre_id,\n                representation[\"name\"],\n                repre_icon,\n                product_item.product_name,\n                product_item.folder_label,\n            )\n            repre_items_by_version_id[version_id][repre_id] = repre_item\n\n        project_cache = self._repre_items_cache[project_name]\n        for version_id, repre_items in repre_items_by_version_id.items():\n            version_cache = project_cache[version_id]\n            version_cache.update_data(repre_items)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/models/index.html#client.ayon_core.tools.loader.models.ProductsModel.change_products_group","title":"<code>change_products_group(project_name, product_ids, group_name)</code>","text":"<p>Change group name for passed product ids.</p> <p>Group name is stored in 'attrib' of product entity and is used in UI to group items.</p> <p>Method triggers \"products.group.changed\" event with data:     {         \"project_name\": project_name,         \"folder_ids\": folder_ids,         \"product_ids\": product_ids,         \"group_name\": group_name     }</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>product_ids</code> <code>Iterable[str]</code> <p>Product ids to change group name for.</p> required <code>group_name</code> <code>str</code> <p>Group name to set.</p> required Source code in <code>client/ayon_core/tools/loader/models/products.py</code> <pre><code>def change_products_group(self, project_name, product_ids, group_name):\n    \"\"\"Change group name for passed product ids.\n\n    Group name is stored in 'attrib' of product entity and is used in UI\n    to group items.\n\n    Method triggers \"products.group.changed\" event with data:\n        {\n            \"project_name\": project_name,\n            \"folder_ids\": folder_ids,\n            \"product_ids\": product_ids,\n            \"group_name\": group_name\n        }\n\n    Args:\n        project_name (str): Project name.\n        product_ids (Iterable[str]): Product ids to change group name for.\n        group_name (str): Group name to set.\n    \"\"\"\n\n    if not product_ids:\n        return\n\n    product_items = self._get_product_items_by_id(\n        project_name, product_ids\n    )\n    if not product_items:\n        return\n\n    session = OperationsSession()\n    folder_ids = set()\n    for product_item in product_items.values():\n        session.update_entity(\n            project_name,\n            \"product\",\n            product_item.product_id,\n            {\"attrib\": {\"productGroup\": group_name or None}}\n        )\n        folder_ids.add(product_item.folder_id)\n        product_item.group_name = group_name\n\n    session.commit()\n    self._controller.emit_event(\n        \"products.group.changed\",\n        {\n            \"project_name\": project_name,\n            \"folder_ids\": folder_ids,\n            \"product_ids\": product_ids,\n            \"group_name\": group_name,\n        },\n        PRODUCTS_MODEL_SENDER\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/models/index.html#client.ayon_core.tools.loader.models.ProductsModel.get_product_base_type_items","title":"<code>get_product_base_type_items(project_name)</code>","text":"<p>Product base type items for the project.</p> Notes <p>This will be used for filtering product types in UI when     product base types are fully implemented.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>(optional, str)</code> <p>Project name.</p> required <p>Returns:</p> Type Description <code>list[ProductBaseTypeItem]</code> <p>list[ProductBaseTypeDict]: Product base type items.</p> Source code in <code>client/ayon_core/tools/loader/models/products.py</code> <pre><code>def get_product_base_type_items(\n    self, project_name: Optional[str]\n) -&gt; list[ProductBaseTypeItem]:\n    \"\"\"Product base type items for the project.\n\n    Notes:\n        This will be used for filtering product types in UI when\n            product base types are fully implemented.\n\n    Args:\n        project_name (optional, str): Project name.\n\n    Returns:\n        list[ProductBaseTypeDict]: Product base type items.\n\n    \"\"\"\n    if not project_name:\n        return []\n\n    cache = self._product_base_type_items_cache[project_name]\n    if not cache.is_valid:\n        icons_mapping = self._get_product_type_icons(project_name)\n        product_base_types = []\n        # TODO add temp implementation here when it is actually\n        #   implemented and available on server.\n        if hasattr(ayon_api, \"get_project_product_base_types\"):\n            product_base_types = ayon_api.get_project_product_base_types(\n                project_name\n            )\n        cache.update_data([\n            ProductBaseTypeItem(\n                product_base_type[\"name\"],\n                icons_mapping.get_icon(product_base_type[\"name\"]),\n            )\n            for product_base_type in product_base_types\n        ])\n    return cache.get_data()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/models/index.html#client.ayon_core.tools.loader.models.ProductsModel.get_product_ids_by_repre_ids","title":"<code>get_product_ids_by_repre_ids(project_name, repre_ids)</code>","text":"<p>Get product ids based on passed representation ids.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Where to look for representations.</p> required <code>repre_ids</code> <code>Iterable[str]</code> <p>Representation ids.</p> required <p>Returns:</p> Type Description <p>set[str]: Product ids for passed representation ids.</p> Source code in <code>client/ayon_core/tools/loader/models/products.py</code> <pre><code>def get_product_ids_by_repre_ids(self, project_name, repre_ids):\n    \"\"\"Get product ids based on passed representation ids.\n\n    Args:\n        project_name (str): Where to look for representations.\n        repre_ids (Iterable[str]): Representation ids.\n\n    Returns:\n        set[str]: Product ids for passed representation ids.\n    \"\"\"\n\n    # TODO look out how to use single server call\n    if not repre_ids:\n        return set()\n    repres = ayon_api.get_representations(\n        project_name, repre_ids, fields=[\"versionId\"]\n    )\n    version_ids = {repre[\"versionId\"] for repre in repres}\n    if not version_ids:\n        return set()\n    versions = ayon_api.get_versions(\n        project_name, version_ids=version_ids, fields=[\"productId\"]\n    )\n    return {v[\"productId\"] for v in versions}\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/models/index.html#client.ayon_core.tools.loader.models.ProductsModel.get_product_item","title":"<code>get_product_item(project_name, product_id)</code>","text":"<p>Get product item based on passed product id.</p> <p>This method is using cached items, but if cache is not valid it also can query the item.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>Union[str, None]</code> <p>Where to look for product.</p> required <code>product_id</code> <code>Union[str, None]</code> <p>Product id to receive.</p> required <p>Returns:</p> Type Description <p>Union[ProductItem, None]: Product item or 'None' if not found.</p> Source code in <code>client/ayon_core/tools/loader/models/products.py</code> <pre><code>def get_product_item(self, project_name, product_id):\n    \"\"\"Get product item based on passed product id.\n\n    This method is using cached items, but if cache is not valid it also\n    can query the item.\n\n    Args:\n        project_name (Union[str, None]): Where to look for product.\n        product_id (Union[str, None]): Product id to receive.\n\n    Returns:\n        Union[ProductItem, None]: Product item or 'None' if not found.\n    \"\"\"\n\n    if not any((project_name, product_id)):\n        return None\n\n    product_items_by_id = self._product_item_by_id[project_name]\n    product_item = product_items_by_id.get(product_id)\n    if product_item is not None:\n        return product_item\n    for product_item in self._query_product_items_by_ids(\n        project_name, product_ids=[product_id]\n    ).values():\n        return product_item\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/models/index.html#client.ayon_core.tools.loader.models.ProductsModel.get_product_items","title":"<code>get_product_items(project_name, folder_ids, sender)</code>","text":"<p>Product items with versions for project and folder ids.</p> <p>Product items also contain version items. They're directly connected to product items in the UI and the separation is not needed.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>Union[str, None]</code> <p>Project name.</p> required <code>folder_ids</code> <code>Iterable[str]</code> <p>Folder ids.</p> required <code>sender</code> <code>Union[str, None]</code> <p>Who triggered the method.</p> required <p>Returns:</p> Type Description <p>list[ProductItem]: Product items.</p> Source code in <code>client/ayon_core/tools/loader/models/products.py</code> <pre><code>def get_product_items(self, project_name, folder_ids, sender):\n    \"\"\"Product items with versions for project and folder ids.\n\n    Product items also contain version items. They're directly connected\n    to product items in the UI and the separation is not needed.\n\n    Args:\n        project_name (Union[str, None]): Project name.\n        folder_ids (Iterable[str]): Folder ids.\n        sender (Union[str, None]): Who triggered the method.\n\n    Returns:\n        list[ProductItem]: Product items.\n    \"\"\"\n\n    if not project_name or not folder_ids:\n        return []\n\n    project_cache = self._product_items_cache[project_name]\n    output = []\n    folder_ids_to_update = set()\n    for folder_id in folder_ids:\n        cache = project_cache[folder_id]\n        if cache.is_valid:\n            output.extend(cache.get_data().values())\n        else:\n            folder_ids_to_update.add(folder_id)\n\n    self._refresh_product_items(\n        project_name, folder_ids_to_update, sender)\n\n    for folder_id in folder_ids_to_update:\n        cache = project_cache[folder_id]\n        output.extend(cache.get_data().values())\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/models/index.html#client.ayon_core.tools.loader.models.ProductsModel.get_product_type_items","title":"<code>get_product_type_items(project_name)</code>","text":"<p>Product type items for project.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>Union[str, None]</code> <p>Project name.</p> required <p>Returns:</p> Type Description <code>list[ProductTypeItem]</code> <p>list[ProductTypeItem]: Product type items.</p> Source code in <code>client/ayon_core/tools/loader/models/products.py</code> <pre><code>def get_product_type_items(\n    self, project_name: Optional[str]\n) -&gt; list[ProductTypeItem]:\n    \"\"\"Product type items for project.\n\n    Args:\n        project_name (Union[str, None]): Project name.\n\n    Returns:\n        list[ProductTypeItem]: Product type items.\n\n    \"\"\"\n    if not project_name:\n        return []\n\n    cache = self._product_type_items_cache[project_name]\n    if not cache.is_valid:\n        icons_mapping = self._get_product_type_icons(project_name)\n        product_types = self._get_project_product_types(project_name)\n        cache.update_data([\n            ProductTypeItem(\n                product_type[\"name\"],\n                icons_mapping.get_icon(product_type=product_type[\"name\"]),\n            )\n            for product_type in product_types\n        ])\n    return cache.get_data()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/models/index.html#client.ayon_core.tools.loader.models.ProductsModel.get_repre_items","title":"<code>get_repre_items(project_name, version_ids, sender)</code>","text":"<p>Get representation items for passed version ids.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>version_ids</code> <code>Iterable[str]</code> <p>Version ids.</p> required <code>sender</code> <code>Union[str, None]</code> <p>Who triggered the method.</p> required <p>Returns:</p> Type Description <p>list[RepreItem]: Representation items.</p> Source code in <code>client/ayon_core/tools/loader/models/products.py</code> <pre><code>def get_repre_items(self, project_name, version_ids, sender):\n    \"\"\"Get representation items for passed version ids.\n\n    Args:\n        project_name (str): Project name.\n        version_ids (Iterable[str]): Version ids.\n        sender (Union[str, None]): Who triggered the method.\n\n    Returns:\n        list[RepreItem]: Representation items.\n    \"\"\"\n\n    output = []\n    if not any((project_name, version_ids)):\n        return output\n\n    invalid_version_ids = set()\n    project_cache = self._repre_items_cache[project_name]\n    for version_id in version_ids:\n        version_cache = project_cache[version_id]\n        if version_cache.is_valid:\n            output.extend(version_cache.get_data().values())\n        else:\n            invalid_version_ids.add(version_id)\n\n    if invalid_version_ids:\n        self.refresh_representation_items(\n            project_name, invalid_version_ids, sender\n        )\n\n    for version_id in invalid_version_ids:\n        version_cache = project_cache[version_id]\n        output.extend(version_cache.get_data().values())\n\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/models/index.html#client.ayon_core.tools.loader.models.ProductsModel.get_versions_repre_count","title":"<code>get_versions_repre_count(project_name, version_ids, sender)</code>","text":"<p>Get representation count for passed version ids.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>version_ids</code> <code>Iterable[str]</code> <p>Version ids.</p> required <code>sender</code> <code>Union[str, None]</code> <p>Who triggered the method.</p> required <p>Returns:</p> Type Description <p>dict[str, int]: Number of representations by version id.</p> Source code in <code>client/ayon_core/tools/loader/models/products.py</code> <pre><code>def get_versions_repre_count(self, project_name, version_ids, sender):\n    \"\"\"Get representation count for passed version ids.\n\n    Args:\n        project_name (str): Project name.\n        version_ids (Iterable[str]): Version ids.\n        sender (Union[str, None]): Who triggered the method.\n\n    Returns:\n        dict[str, int]: Number of representations by version id.\n    \"\"\"\n\n    output = {}\n    if not any((project_name, version_ids)):\n        return output\n\n    invalid_version_ids = set()\n    project_cache = self._repre_items_cache[project_name]\n    for version_id in version_ids:\n        version_cache = project_cache[version_id]\n        if version_cache.is_valid:\n            output[version_id] = len(version_cache.get_data())\n        else:\n            invalid_version_ids.add(version_id)\n\n    if invalid_version_ids:\n        self.refresh_representation_items(\n            project_name, invalid_version_ids, sender\n        )\n\n    for version_id in invalid_version_ids:\n        version_cache = project_cache[version_id]\n        output[version_id] = len(version_cache.get_data())\n\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/models/index.html#client.ayon_core.tools.loader.models.ProductsModel.reset","title":"<code>reset()</code>","text":"<p>Reset model with all cached data.</p> Source code in <code>client/ayon_core/tools/loader/models/products.py</code> <pre><code>def reset(self):\n    \"\"\"Reset model with all cached data.\"\"\"\n\n    self._product_item_by_id.clear()\n    self._version_item_by_id.clear()\n    self._product_folder_ids_mapping.clear()\n\n    self._product_type_items_cache.reset()\n    self._product_items_cache.reset()\n    self._repre_items_cache.reset()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/models/index.html#client.ayon_core.tools.loader.models.SelectionModel","title":"<code>SelectionModel</code>","text":"<p>               Bases: <code>object</code></p> <p>Model handling selection changes.</p> <p>Triggering events: - \"selection.project.changed\" - \"selection.folders.changed\" - \"selection.versions.changed\"</p> Source code in <code>client/ayon_core/tools/loader/models/selection.py</code> <pre><code>class SelectionModel(object):\n    \"\"\"Model handling selection changes.\n\n    Triggering events:\n    - \"selection.project.changed\"\n    - \"selection.folders.changed\"\n    - \"selection.versions.changed\"\n    \"\"\"\n\n    event_source = \"selection.model\"\n\n    def __init__(self, controller):\n        self._controller = controller\n\n        self._project_name = None\n        self._folder_ids = set()\n        self._task_ids = set()\n        self._version_ids = set()\n        self._representation_ids = set()\n\n    def get_selected_project_name(self):\n        return self._project_name\n\n    def set_selected_project(self, project_name):\n        if self._project_name == project_name:\n            return\n\n        self._project_name = project_name\n        self._controller.emit_event(\n            \"selection.project.changed\",\n            {\"project_name\": self._project_name},\n            self.event_source\n        )\n\n    def get_selected_folder_ids(self):\n        return self._folder_ids\n\n    def set_selected_folders(self, folder_ids):\n        if folder_ids == self._folder_ids:\n            return\n\n        self._folder_ids = folder_ids\n        self._controller.emit_event(\n            \"selection.folders.changed\",\n            {\n                \"project_name\": self._project_name,\n                \"folder_ids\": folder_ids,\n            },\n            self.event_source\n        )\n\n    def get_selected_task_ids(self):\n        return self._task_ids\n\n    def set_selected_tasks(self, task_ids):\n        if task_ids == self._task_ids:\n            return\n\n        self._task_ids = task_ids\n        self._controller.emit_event(\n            \"selection.tasks.changed\",\n            {\n                \"project_name\": self._project_name,\n                \"task_ids\": task_ids,\n            },\n            self.event_source\n        )\n\n    def get_selected_version_ids(self):\n        return self._version_ids\n\n    def set_selected_versions(self, version_ids):\n        if version_ids == self._version_ids:\n            return\n\n        self._version_ids = version_ids\n        self._controller.emit_event(\n            \"selection.versions.changed\",\n            {\n                \"project_name\": self._project_name,\n                \"folder_ids\": self._folder_ids,\n                \"version_ids\": self._version_ids,\n            },\n            self.event_source\n        )\n\n    def get_selected_representation_ids(self):\n        return self._representation_ids\n\n    def set_selected_representations(self, repre_ids):\n        if repre_ids == self._representation_ids:\n            return\n\n        self._representation_ids = repre_ids\n        self._controller.emit_event(\n            \"selection.representations.changed\",\n            {\n                \"project_name\": self._project_name,\n                \"folder_ids\": self._folder_ids,\n                \"version_ids\": self._version_ids,\n                \"representation_ids\": self._representation_ids,\n            }\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/models/index.html#client.ayon_core.tools.loader.models.SiteSyncModel","title":"<code>SiteSyncModel</code>","text":"<p>Model handling site sync logic.</p> <p>Model cares about handling of site sync functionality. All public functions should be possible to call even if site sync is not available.</p> Source code in <code>client/ayon_core/tools/loader/models/sitesync.py</code> <pre><code>class SiteSyncModel:\n    \"\"\"Model handling site sync logic.\n\n    Model cares about handling of site sync functionality. All public\n    functions should be possible to call even if site sync is not available.\n    \"\"\"\n\n    lifetime = 60  # In seconds (minute by default)\n    status_lifetime = 20\n\n    def __init__(self, controller):\n        self._controller = controller\n\n        self._site_icons = None\n        self._sitesync_enabled_cache = NestedCacheItem(\n            levels=1, lifetime=self.lifetime\n        )\n        self._active_site_cache = NestedCacheItem(\n            levels=1, lifetime=self.lifetime\n        )\n        self._remote_site_cache = NestedCacheItem(\n            levels=1, lifetime=self.lifetime\n        )\n        self._version_availability_cache = NestedCacheItem(\n            levels=2,\n            default_factory=_default_version_availability,\n            lifetime=self.status_lifetime\n        )\n        self._repre_status_cache = NestedCacheItem(\n            levels=2,\n            default_factory=_default_repre_status,\n            lifetime=self.status_lifetime\n        )\n\n        manager = AddonsManager()\n        self._sitesync_addon = manager.get(\"sitesync\")\n\n    def reset(self):\n        self._site_icons = None\n        self._sitesync_enabled_cache.reset()\n        self._active_site_cache.reset()\n        self._remote_site_cache.reset()\n        self._version_availability_cache.reset()\n        self._repre_status_cache.reset()\n\n    def is_sitesync_enabled(self, project_name=None):\n        \"\"\"Site sync is enabled for a project.\n\n        Returns false if site sync addon is not available or enabled\n            or project has disabled it.\n\n        Args:\n            project_name (Union[str, None]): Project name. If project name\n                is 'None', True is returned if site sync addon\n                is available and enabled.\n\n        Returns:\n            bool: Site sync is enabled.\n        \"\"\"\n\n        if not self._is_sitesync_addon_enabled():\n            return False\n        cache = self._sitesync_enabled_cache[project_name]\n        if not cache.is_valid:\n            enabled = True\n            if project_name:\n                enabled = self._sitesync_addon.is_project_enabled(\n                    project_name, single=True\n                )\n            cache.update_data(enabled)\n        return cache.get_data()\n\n    def get_active_site(self, project_name):\n        \"\"\"Active site name for a project.\n\n        Args:\n            project_name (str): Project name.\n\n        Returns:\n            Union[str, None]: Remote site name.\n        \"\"\"\n\n        cache = self._active_site_cache[project_name]\n        if not cache.is_valid:\n            site_name = None\n            if project_name and self._is_sitesync_addon_enabled():\n                site_name = self._sitesync_addon.get_active_site(project_name)\n            cache.update_data(site_name)\n        return cache.get_data()\n\n    def get_remote_site(self, project_name):\n        \"\"\"Remote site name for a project.\n\n        Args:\n            project_name (str): Project name.\n\n        Returns:\n            Union[str, None]: Remote site name.\n        \"\"\"\n\n        cache = self._remote_site_cache[project_name]\n        if not cache.is_valid:\n            site_name = None\n            if project_name and self._is_sitesync_addon_enabled():\n                site_name = self._sitesync_addon.get_remote_site(project_name)\n            cache.update_data(site_name)\n        return cache.get_data()\n\n    def get_active_site_icon_def(self, project_name):\n        \"\"\"Active site icon definition.\n\n        Args:\n            project_name (Union[str, None]): Name of project.\n\n        Returns:\n            Union[dict[str, Any], None]: Site icon definition.\n        \"\"\"\n\n        if not project_name or not self.is_sitesync_enabled(project_name):\n            return None\n        active_site = self.get_active_site(project_name)\n        return self._get_site_icon_def(project_name, active_site)\n\n    def get_remote_site_icon_def(self, project_name):\n        \"\"\"Remote site icon definition.\n\n        Args:\n            project_name (Union[str, None]): Name of project.\n\n        Returns:\n            Union[dict[str, Any], None]: Site icon definition.\n        \"\"\"\n\n        if not project_name or not self.is_sitesync_enabled(project_name):\n            return None\n        remote_site = self.get_remote_site(project_name)\n        return self._get_site_icon_def(project_name, remote_site)\n\n    def _get_site_icon_def(self, project_name, site_name):\n        # use different icon for studio even if provider is 'local_drive'\n        if site_name == self._sitesync_addon.DEFAULT_SITE:\n            provider = \"studio\"\n        else:\n            provider = self._get_provider_for_site(project_name, site_name)\n        return self._get_provider_icon(provider)\n\n    def get_version_sync_availability(self, project_name, version_ids):\n        \"\"\"Returns how many representations are available on sites.\n\n        Returned value `{version_id: (4, 6)}` denotes that locally are\n            available 4 and remotely 6 representation.\n        NOTE: Available means they were synced to site.\n\n        Returns:\n            dict[str, tuple[int, int]]\n        \"\"\"\n\n        if not self.is_sitesync_enabled(project_name):\n            return {\n                version_id: _default_version_availability()\n                for version_id in version_ids\n            }\n\n        output = {}\n        project_cache = self._version_availability_cache[project_name]\n        invalid_ids = set()\n        for version_id in version_ids:\n            repre_cache = project_cache[version_id]\n            if repre_cache.is_valid:\n                output[version_id] = repre_cache.get_data()\n            else:\n                invalid_ids.add(version_id)\n\n        if invalid_ids:\n            self._refresh_version_availability(\n                project_name, invalid_ids\n            )\n            for version_id in invalid_ids:\n                version_cache = project_cache[version_id]\n                output[version_id] = version_cache.get_data()\n        return output\n\n    def get_representations_sync_status(\n        self, project_name, representation_ids\n    ):\n        \"\"\"\n\n        Args:\n            project_name (str): Project name.\n            representation_ids (Iterable[str]): Representation ids.\n\n        Returns:\n            dict[str, tuple[float, float]]\n        \"\"\"\n\n        if not self.is_sitesync_enabled(project_name):\n            return {\n                repre_id: _default_repre_status()\n                for repre_id in representation_ids\n            }\n\n        output = {}\n        project_cache = self._repre_status_cache[project_name]\n        invalid_ids = set()\n        for repre_id in representation_ids:\n            repre_cache = project_cache[repre_id]\n            if repre_cache.is_valid:\n                output[repre_id] = repre_cache.get_data()\n            else:\n                invalid_ids.add(repre_id)\n\n        if invalid_ids:\n            self._refresh_representations_sync_status(\n                project_name, invalid_ids\n            )\n            for repre_id in invalid_ids:\n                repre_cache = project_cache[repre_id]\n                output[repre_id] = repre_cache.get_data()\n        return output\n\n    def get_sitesync_action_items(\n        self, project_name, entity_ids, entity_type\n    ):\n        \"\"\"\n\n        Args:\n             project_name (str): Project name.\n             entity_ids (set[str]): Selected entity ids.\n             entity_type (str): Selected entity type.\n\n        Returns:\n            list[ActionItem]: Actions that can be shown in loader.\n\n        \"\"\"\n        if entity_type != \"representation\":\n            return []\n\n        if not self.is_sitesync_enabled(project_name):\n            return []\n\n        repres_status = self.get_representations_sync_status(\n            project_name, entity_ids\n        )\n\n        repre_ids_per_identifier = collections.defaultdict(set)\n        for repre_id in entity_ids:\n            repre_status = repres_status[repre_id]\n            local_status, remote_status = repre_status\n\n            if local_status:\n                repre_ids_per_identifier[UPLOAD_IDENTIFIER].add(repre_id)\n                repre_ids_per_identifier[REMOVE_IDENTIFIER].add(repre_id)\n\n            if remote_status:\n                repre_ids_per_identifier[DOWNLOAD_IDENTIFIER].add(repre_id)\n\n        action_items = []\n        for identifier, repre_ids in repre_ids_per_identifier.items():\n            if identifier == DOWNLOAD_IDENTIFIER:\n                action_items.append(self._create_download_action_item(\n                    project_name, repre_ids\n                ))\n            elif identifier == UPLOAD_IDENTIFIER:\n                action_items.append(self._create_upload_action_item(\n                    project_name, repre_ids\n                ))\n            elif identifier == REMOVE_IDENTIFIER:\n                action_items.append(self._create_delete_action_item(\n                    project_name, repre_ids\n                ))\n\n        return action_items\n\n    def is_sitesync_action(self, identifier: str) -&gt; bool:\n        \"\"\"Should be `identifier` handled by SiteSync.\n\n        Args:\n            identifier (str): Plugin identifier.\n\n        Returns:\n            bool: Should action be handled by SiteSync.\n\n        \"\"\"\n        return identifier == \"sitesync.loader.action\"\n\n    def trigger_action_item(\n        self,\n        project_name: str,\n        data: dict[str, Any],\n    ):\n        \"\"\"Resets status for site_name or remove local files.\n\n        Args:\n            project_name (str): Project name.\n            data (dict[str, Any]): Action item data.\n\n        \"\"\"\n        representation_ids = data[\"representation_ids\"]\n        action_identifier = data[\"action_identifier\"]\n        active_site = self.get_active_site(project_name)\n        remote_site = self.get_remote_site(project_name)\n\n        repre_entities_by_id = {\n            repre_entity[\"id\"]: repre_entity\n            for repre_entity in get_representations(\n                project_name, representation_ids=representation_ids\n            )\n        }\n        # TODO get product type from product entity instead of 'context'\n        #   on representation\n        product_type_by_repre_id = {}\n        for repre_id, repre_entity in repre_entities_by_id.items():\n            repre_context = repre_entity[\"context\"]\n            product_type = repre_context.get(\"product\", {}).get(\"type\")\n            if not product_type:\n                product_type = repre_context.get(\"family\")\n\n            product_type_by_repre_id[repre_id] = product_type\n\n        for repre_id in representation_ids:\n            repre_entity = repre_entities_by_id.get(repre_id)\n            product_type = product_type_by_repre_id[repre_id]\n            if action_identifier == DOWNLOAD_IDENTIFIER:\n                self._add_site(\n                    project_name, repre_entity, active_site, product_type\n                )\n\n            elif action_identifier == UPLOAD_IDENTIFIER:\n                self._add_site(\n                    project_name, repre_entity, remote_site, product_type\n                )\n\n            elif action_identifier == REMOVE_IDENTIFIER:\n                self._sitesync_addon.remove_site(\n                    project_name,\n                    repre_id,\n                    active_site,\n                    remove_local_files=True\n                )\n\n    def _is_sitesync_addon_enabled(self):\n        \"\"\"\n        Returns:\n            bool: Site sync addon is enabled.\n        \"\"\"\n\n        if self._sitesync_addon is None:\n            return False\n        return self._sitesync_addon.enabled\n\n    def _get_provider_for_site(self, project_name, site_name):\n        \"\"\"Provider for a site.\n\n        Args:\n            project_name (str): Project name.\n            site_name (str): Site name.\n\n        Returns:\n            Union[str, None]: Provider name.\n        \"\"\"\n\n        if not self._is_sitesync_addon_enabled():\n            return None\n        return self._sitesync_addon.get_provider_for_site(\n            project_name, site_name\n        )\n\n    def _get_provider_icon(self, provider):\n        \"\"\"site provider icons.\n\n        Returns:\n            Union[dict[str, Any], None]: Icon of site provider.\n        \"\"\"\n\n        if not provider:\n            return None\n\n        if self._site_icons is None:\n            self._site_icons = self._sitesync_addon.get_site_icons()\n        return self._site_icons.get(provider)\n\n    def _refresh_version_availability(self, project_name, version_ids):\n        if not project_name or not version_ids:\n            return\n        project_cache = self._version_availability_cache[project_name]\n\n        avail_by_id = self._sitesync_addon.get_version_availability(\n            project_name,\n            version_ids,\n            self.get_active_site(project_name),\n            self.get_remote_site(project_name),\n        )\n        for version_id in version_ids:\n            status = avail_by_id.get(version_id)\n            if status is None:\n                status = _default_version_availability()\n            project_cache[version_id].update_data(status)\n\n    def _refresh_representations_sync_status(\n        self, project_name, representation_ids\n    ):\n        if not project_name or not representation_ids:\n            return\n        project_cache = self._repre_status_cache[project_name]\n        status_by_repre_id = (\n            self._sitesync_addon.get_representations_sync_state(\n                project_name,\n                representation_ids,\n                self.get_active_site(project_name),\n                self.get_remote_site(project_name),\n            )\n        )\n        for repre_id in representation_ids:\n            status = status_by_repre_id.get(repre_id)\n            if status is None:\n                status = _default_repre_status()\n            project_cache[repre_id].update_data(status)\n\n    def _create_download_action_item(self, project_name, representation_ids):\n        return self._create_action_item(\n            project_name,\n            representation_ids,\n            DOWNLOAD_IDENTIFIER,\n            \"Download\",\n            \"Mark representation for download locally\",\n            \"fa.download\"\n        )\n\n    def _create_upload_action_item(self, project_name, representation_ids):\n        return self._create_action_item(\n            project_name,\n            representation_ids,\n            UPLOAD_IDENTIFIER,\n            \"Upload\",\n            \"Mark representation for upload remotely\",\n            \"fa.upload\"\n        )\n\n    def _create_delete_action_item(self, project_name, representation_ids):\n        return self._create_action_item(\n            project_name,\n            representation_ids,\n            REMOVE_IDENTIFIER,\n            \"Remove from local\",\n            \"Remove local synchronization\",\n            \"fa.trash\"\n        )\n\n    def _create_action_item(\n        self,\n        project_name,\n        representation_ids,\n        action_identifier,\n        label,\n        tooltip,\n        icon_name\n    ):\n        return ActionItem(\n            \"sitesync.loader.action\",\n            label=label,\n            group_label=None,\n            icon=AwesomeFontIcon(icon_name, color=\"#999999\"),\n            tooltip=tooltip,\n            order=1,\n            data={\n                \"representation_ids\": representation_ids,\n                \"action_identifier\": action_identifier,\n            },\n            options=None,\n        )\n\n    def _add_site(self, project_name, repre_entity, site_name, product_type):\n        self._sitesync_addon.add_site(\n            project_name, repre_entity[\"id\"], site_name, force=True\n        )\n\n        # TODO this should happen in site sync addon\n        if product_type != \"workfile\":\n            return\n\n        links = self._get_linked_representation_id(\n            project_name,\n            repre_entity,\n            \"reference\"\n        )\n        for link_repre_id in links:\n            if not self._sitesync_addon.is_representation_on_site(\n                project_name,\n                link_repre_id,\n                site_name\n            ):\n                print(\"Adding {} to linked representation: {}\".format(\n                    site_name, link_repre_id))\n                self._sitesync_addon.add_site(\n                    project_name,\n                    link_repre_id,\n                    site_name,\n                    force=True\n                )\n\n    def _get_linked_representation_id(\n        self,\n        project_name,\n        repre_entity,\n        link_type,\n        max_depth=None\n    ):\n        \"\"\"Returns list of linked ids of particular type (if provided).\n\n        One of representation document or representation id must be passed.\n        Note:\n            Representation links now works only from representation through\n                version back to representations.\n\n        Todos:\n            Missing depth query. Not sure how it did find more representations\n                in depth, probably links to version?\n            This function should probably live in sitesync addon?\n\n        Args:\n            project_name (str): Name of project where look for links.\n            repre_entity (dict[str, Any]): Representation entity.\n            link_type (str): Type of link (e.g. 'reference', ...).\n            max_depth (int): Limit recursion level. Default: 0\n\n        Returns:\n            List[ObjectId] Linked representation ids.\n        \"\"\"\n\n        if not repre_entity:\n            return []\n\n        version_id = repre_entity[\"versionId\"]\n        if max_depth is None or max_depth == 0:\n            max_depth = 1\n\n        link_types = None\n        if link_type:\n            link_types = [link_type]\n\n        # Store already found version ids to avoid recursion, and also to store\n        #   output -&gt; Don't forget to remove 'version_id' at the end!!!\n        linked_version_ids = {version_id}\n        # Each loop of depth will reset this variable\n        versions_to_check = {version_id}\n        for _ in range(max_depth):\n            if not versions_to_check:\n                break\n\n            versions_links = get_versions_links(\n                project_name,\n                versions_to_check,\n                link_types=link_types,\n                link_direction=\"in\")  # looking for 'in'puts for version\n\n            versions_to_check = set()\n            for links in versions_links.values():\n                for link in links:\n                    # Care only about version links\n                    if link[\"entityType\"] != \"version\":\n                        continue\n                    entity_id = link[\"entityId\"]\n                    linked_version_ids.add(entity_id)\n                    versions_to_check.add(entity_id)\n\n        linked_version_ids.remove(version_id)\n        if not linked_version_ids:\n            return []\n        representations = get_representations(\n            project_name,\n            version_ids=linked_version_ids,\n            fields=[\"id\"])\n        return [\n            repre[\"id\"]\n            for repre in representations\n        ]\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/models/index.html#client.ayon_core.tools.loader.models.SiteSyncModel.get_active_site","title":"<code>get_active_site(project_name)</code>","text":"<p>Active site name for a project.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <p>Returns:</p> Type Description <p>Union[str, None]: Remote site name.</p> Source code in <code>client/ayon_core/tools/loader/models/sitesync.py</code> <pre><code>def get_active_site(self, project_name):\n    \"\"\"Active site name for a project.\n\n    Args:\n        project_name (str): Project name.\n\n    Returns:\n        Union[str, None]: Remote site name.\n    \"\"\"\n\n    cache = self._active_site_cache[project_name]\n    if not cache.is_valid:\n        site_name = None\n        if project_name and self._is_sitesync_addon_enabled():\n            site_name = self._sitesync_addon.get_active_site(project_name)\n        cache.update_data(site_name)\n    return cache.get_data()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/models/index.html#client.ayon_core.tools.loader.models.SiteSyncModel.get_active_site_icon_def","title":"<code>get_active_site_icon_def(project_name)</code>","text":"<p>Active site icon definition.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>Union[str, None]</code> <p>Name of project.</p> required <p>Returns:</p> Type Description <p>Union[dict[str, Any], None]: Site icon definition.</p> Source code in <code>client/ayon_core/tools/loader/models/sitesync.py</code> <pre><code>def get_active_site_icon_def(self, project_name):\n    \"\"\"Active site icon definition.\n\n    Args:\n        project_name (Union[str, None]): Name of project.\n\n    Returns:\n        Union[dict[str, Any], None]: Site icon definition.\n    \"\"\"\n\n    if not project_name or not self.is_sitesync_enabled(project_name):\n        return None\n    active_site = self.get_active_site(project_name)\n    return self._get_site_icon_def(project_name, active_site)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/models/index.html#client.ayon_core.tools.loader.models.SiteSyncModel.get_remote_site","title":"<code>get_remote_site(project_name)</code>","text":"<p>Remote site name for a project.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <p>Returns:</p> Type Description <p>Union[str, None]: Remote site name.</p> Source code in <code>client/ayon_core/tools/loader/models/sitesync.py</code> <pre><code>def get_remote_site(self, project_name):\n    \"\"\"Remote site name for a project.\n\n    Args:\n        project_name (str): Project name.\n\n    Returns:\n        Union[str, None]: Remote site name.\n    \"\"\"\n\n    cache = self._remote_site_cache[project_name]\n    if not cache.is_valid:\n        site_name = None\n        if project_name and self._is_sitesync_addon_enabled():\n            site_name = self._sitesync_addon.get_remote_site(project_name)\n        cache.update_data(site_name)\n    return cache.get_data()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/models/index.html#client.ayon_core.tools.loader.models.SiteSyncModel.get_remote_site_icon_def","title":"<code>get_remote_site_icon_def(project_name)</code>","text":"<p>Remote site icon definition.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>Union[str, None]</code> <p>Name of project.</p> required <p>Returns:</p> Type Description <p>Union[dict[str, Any], None]: Site icon definition.</p> Source code in <code>client/ayon_core/tools/loader/models/sitesync.py</code> <pre><code>def get_remote_site_icon_def(self, project_name):\n    \"\"\"Remote site icon definition.\n\n    Args:\n        project_name (Union[str, None]): Name of project.\n\n    Returns:\n        Union[dict[str, Any], None]: Site icon definition.\n    \"\"\"\n\n    if not project_name or not self.is_sitesync_enabled(project_name):\n        return None\n    remote_site = self.get_remote_site(project_name)\n    return self._get_site_icon_def(project_name, remote_site)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/models/index.html#client.ayon_core.tools.loader.models.SiteSyncModel.get_representations_sync_status","title":"<code>get_representations_sync_status(project_name, representation_ids)</code>","text":"<p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>representation_ids</code> <code>Iterable[str]</code> <p>Representation ids.</p> required <p>Returns:</p> Type Description <p>dict[str, tuple[float, float]]</p> Source code in <code>client/ayon_core/tools/loader/models/sitesync.py</code> <pre><code>def get_representations_sync_status(\n    self, project_name, representation_ids\n):\n    \"\"\"\n\n    Args:\n        project_name (str): Project name.\n        representation_ids (Iterable[str]): Representation ids.\n\n    Returns:\n        dict[str, tuple[float, float]]\n    \"\"\"\n\n    if not self.is_sitesync_enabled(project_name):\n        return {\n            repre_id: _default_repre_status()\n            for repre_id in representation_ids\n        }\n\n    output = {}\n    project_cache = self._repre_status_cache[project_name]\n    invalid_ids = set()\n    for repre_id in representation_ids:\n        repre_cache = project_cache[repre_id]\n        if repre_cache.is_valid:\n            output[repre_id] = repre_cache.get_data()\n        else:\n            invalid_ids.add(repre_id)\n\n    if invalid_ids:\n        self._refresh_representations_sync_status(\n            project_name, invalid_ids\n        )\n        for repre_id in invalid_ids:\n            repre_cache = project_cache[repre_id]\n            output[repre_id] = repre_cache.get_data()\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/models/index.html#client.ayon_core.tools.loader.models.SiteSyncModel.get_sitesync_action_items","title":"<code>get_sitesync_action_items(project_name, entity_ids, entity_type)</code>","text":"<p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>entity_ids</code> <code>set[str]</code> <p>Selected entity ids.</p> required <code>entity_type</code> <code>str</code> <p>Selected entity type.</p> required <p>Returns:</p> Type Description <p>list[ActionItem]: Actions that can be shown in loader.</p> Source code in <code>client/ayon_core/tools/loader/models/sitesync.py</code> <pre><code>def get_sitesync_action_items(\n    self, project_name, entity_ids, entity_type\n):\n    \"\"\"\n\n    Args:\n         project_name (str): Project name.\n         entity_ids (set[str]): Selected entity ids.\n         entity_type (str): Selected entity type.\n\n    Returns:\n        list[ActionItem]: Actions that can be shown in loader.\n\n    \"\"\"\n    if entity_type != \"representation\":\n        return []\n\n    if not self.is_sitesync_enabled(project_name):\n        return []\n\n    repres_status = self.get_representations_sync_status(\n        project_name, entity_ids\n    )\n\n    repre_ids_per_identifier = collections.defaultdict(set)\n    for repre_id in entity_ids:\n        repre_status = repres_status[repre_id]\n        local_status, remote_status = repre_status\n\n        if local_status:\n            repre_ids_per_identifier[UPLOAD_IDENTIFIER].add(repre_id)\n            repre_ids_per_identifier[REMOVE_IDENTIFIER].add(repre_id)\n\n        if remote_status:\n            repre_ids_per_identifier[DOWNLOAD_IDENTIFIER].add(repre_id)\n\n    action_items = []\n    for identifier, repre_ids in repre_ids_per_identifier.items():\n        if identifier == DOWNLOAD_IDENTIFIER:\n            action_items.append(self._create_download_action_item(\n                project_name, repre_ids\n            ))\n        elif identifier == UPLOAD_IDENTIFIER:\n            action_items.append(self._create_upload_action_item(\n                project_name, repre_ids\n            ))\n        elif identifier == REMOVE_IDENTIFIER:\n            action_items.append(self._create_delete_action_item(\n                project_name, repre_ids\n            ))\n\n    return action_items\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/models/index.html#client.ayon_core.tools.loader.models.SiteSyncModel.get_version_sync_availability","title":"<code>get_version_sync_availability(project_name, version_ids)</code>","text":"<p>Returns how many representations are available on sites.</p> <p>Returned value <code>{version_id: (4, 6)}</code> denotes that locally are     available 4 and remotely 6 representation. NOTE: Available means they were synced to site.</p> <p>Returns:</p> Type Description <p>dict[str, tuple[int, int]]</p> Source code in <code>client/ayon_core/tools/loader/models/sitesync.py</code> <pre><code>def get_version_sync_availability(self, project_name, version_ids):\n    \"\"\"Returns how many representations are available on sites.\n\n    Returned value `{version_id: (4, 6)}` denotes that locally are\n        available 4 and remotely 6 representation.\n    NOTE: Available means they were synced to site.\n\n    Returns:\n        dict[str, tuple[int, int]]\n    \"\"\"\n\n    if not self.is_sitesync_enabled(project_name):\n        return {\n            version_id: _default_version_availability()\n            for version_id in version_ids\n        }\n\n    output = {}\n    project_cache = self._version_availability_cache[project_name]\n    invalid_ids = set()\n    for version_id in version_ids:\n        repre_cache = project_cache[version_id]\n        if repre_cache.is_valid:\n            output[version_id] = repre_cache.get_data()\n        else:\n            invalid_ids.add(version_id)\n\n    if invalid_ids:\n        self._refresh_version_availability(\n            project_name, invalid_ids\n        )\n        for version_id in invalid_ids:\n            version_cache = project_cache[version_id]\n            output[version_id] = version_cache.get_data()\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/models/index.html#client.ayon_core.tools.loader.models.SiteSyncModel.is_sitesync_action","title":"<code>is_sitesync_action(identifier)</code>","text":"<p>Should be <code>identifier</code> handled by SiteSync.</p> <p>Parameters:</p> Name Type Description Default <code>identifier</code> <code>str</code> <p>Plugin identifier.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>Should action be handled by SiteSync.</p> Source code in <code>client/ayon_core/tools/loader/models/sitesync.py</code> <pre><code>def is_sitesync_action(self, identifier: str) -&gt; bool:\n    \"\"\"Should be `identifier` handled by SiteSync.\n\n    Args:\n        identifier (str): Plugin identifier.\n\n    Returns:\n        bool: Should action be handled by SiteSync.\n\n    \"\"\"\n    return identifier == \"sitesync.loader.action\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/models/index.html#client.ayon_core.tools.loader.models.SiteSyncModel.is_sitesync_enabled","title":"<code>is_sitesync_enabled(project_name=None)</code>","text":"<p>Site sync is enabled for a project.</p> <p>Returns false if site sync addon is not available or enabled     or project has disabled it.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>Union[str, None]</code> <p>Project name. If project name is 'None', True is returned if site sync addon is available and enabled.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>bool</code> <p>Site sync is enabled.</p> Source code in <code>client/ayon_core/tools/loader/models/sitesync.py</code> <pre><code>def is_sitesync_enabled(self, project_name=None):\n    \"\"\"Site sync is enabled for a project.\n\n    Returns false if site sync addon is not available or enabled\n        or project has disabled it.\n\n    Args:\n        project_name (Union[str, None]): Project name. If project name\n            is 'None', True is returned if site sync addon\n            is available and enabled.\n\n    Returns:\n        bool: Site sync is enabled.\n    \"\"\"\n\n    if not self._is_sitesync_addon_enabled():\n        return False\n    cache = self._sitesync_enabled_cache[project_name]\n    if not cache.is_valid:\n        enabled = True\n        if project_name:\n            enabled = self._sitesync_addon.is_project_enabled(\n                project_name, single=True\n            )\n        cache.update_data(enabled)\n    return cache.get_data()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/models/index.html#client.ayon_core.tools.loader.models.SiteSyncModel.trigger_action_item","title":"<code>trigger_action_item(project_name, data)</code>","text":"<p>Resets status for site_name or remove local files.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>data</code> <code>dict[str, Any]</code> <p>Action item data.</p> required Source code in <code>client/ayon_core/tools/loader/models/sitesync.py</code> <pre><code>def trigger_action_item(\n    self,\n    project_name: str,\n    data: dict[str, Any],\n):\n    \"\"\"Resets status for site_name or remove local files.\n\n    Args:\n        project_name (str): Project name.\n        data (dict[str, Any]): Action item data.\n\n    \"\"\"\n    representation_ids = data[\"representation_ids\"]\n    action_identifier = data[\"action_identifier\"]\n    active_site = self.get_active_site(project_name)\n    remote_site = self.get_remote_site(project_name)\n\n    repre_entities_by_id = {\n        repre_entity[\"id\"]: repre_entity\n        for repre_entity in get_representations(\n            project_name, representation_ids=representation_ids\n        )\n    }\n    # TODO get product type from product entity instead of 'context'\n    #   on representation\n    product_type_by_repre_id = {}\n    for repre_id, repre_entity in repre_entities_by_id.items():\n        repre_context = repre_entity[\"context\"]\n        product_type = repre_context.get(\"product\", {}).get(\"type\")\n        if not product_type:\n            product_type = repre_context.get(\"family\")\n\n        product_type_by_repre_id[repre_id] = product_type\n\n    for repre_id in representation_ids:\n        repre_entity = repre_entities_by_id.get(repre_id)\n        product_type = product_type_by_repre_id[repre_id]\n        if action_identifier == DOWNLOAD_IDENTIFIER:\n            self._add_site(\n                project_name, repre_entity, active_site, product_type\n            )\n\n        elif action_identifier == UPLOAD_IDENTIFIER:\n            self._add_site(\n                project_name, repre_entity, remote_site, product_type\n            )\n\n        elif action_identifier == REMOVE_IDENTIFIER:\n            self._sitesync_addon.remove_site(\n                project_name,\n                repre_id,\n                active_site,\n                remove_local_files=True\n            )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/models/actions.html","title":"actions","text":""},{"location":"autoapi/client/ayon_core/tools/loader/models/actions.html#client.ayon_core.tools.loader.models.actions.LoaderActionsModel","title":"<code>LoaderActionsModel</code>","text":"<p>Model for loader actions.</p> TODOs <p>Deprecate 'qargparse' usage in loaders and implement conversion     of 'ActionItem' to data (and 'from_data'). Use controller to get entities -&gt; possible only when     loaders are able to handle AYON vs. OpenPype logic. Add missing site sync logic, and if possible remove it from loaders. Implement loader actions to replace load plugins. Ask loader actions to return action items instead of guessing them.</p> Source code in <code>client/ayon_core/tools/loader/models/actions.py</code> <pre><code>class LoaderActionsModel:\n    \"\"\"Model for loader actions.\n\n    TODOs:\n        Deprecate 'qargparse' usage in loaders and implement conversion\n            of 'ActionItem' to data (and 'from_data').\n        Use controller to get entities -&gt; possible only when\n            loaders are able to handle AYON vs. OpenPype logic.\n        Add missing site sync logic, and if possible remove it from loaders.\n        Implement loader actions to replace load plugins.\n        Ask loader actions to return action items instead of guessing them.\n    \"\"\"\n\n    # Cache loader plugins for some time\n    # NOTE Set to '0' for development\n    loaders_cache_lifetime = 30\n\n    def __init__(self, controller):\n        self._log = Logger.get_logger(self.__class__.__name__)\n        self._controller = controller\n        self._current_context_project = NOT_SET\n        self._loaders_by_identifier = NestedCacheItem(\n            levels=1, lifetime=self.loaders_cache_lifetime)\n        self._product_loaders = NestedCacheItem(\n            levels=1, lifetime=self.loaders_cache_lifetime)\n        self._repre_loaders = NestedCacheItem(\n            levels=1, lifetime=self.loaders_cache_lifetime)\n        self._loader_actions = LoaderActionsContext()\n\n        self._projects_cache = NestedCacheItem(levels=1, lifetime=60)\n        self._folders_cache = NestedCacheItem(levels=2, lifetime=300)\n        self._tasks_cache = NestedCacheItem(levels=2, lifetime=300)\n        self._products_cache = NestedCacheItem(levels=2, lifetime=300)\n        self._versions_cache = NestedCacheItem(levels=2, lifetime=1200)\n        self._representations_cache = NestedCacheItem(levels=2, lifetime=1200)\n        self._repre_parents_cache = NestedCacheItem(levels=2, lifetime=1200)\n\n    def reset(self):\n        \"\"\"Reset the model with all cached items.\"\"\"\n\n        self._current_context_project = NOT_SET\n        self._loaders_by_identifier.reset()\n        self._product_loaders.reset()\n        self._repre_loaders.reset()\n        self._loader_actions.reset()\n\n        self._folders_cache.reset()\n        self._tasks_cache.reset()\n        self._products_cache.reset()\n        self._versions_cache.reset()\n        self._representations_cache.reset()\n        self._repre_parents_cache.reset()\n\n    def get_action_items(\n        self,\n        project_name: str,\n        entity_ids: set[str],\n        entity_type: str,\n    ) -&gt; list[ActionItem]:\n        version_context_by_id = {}\n        repre_context_by_id = {}\n        if entity_type == \"representation\":\n            (\n                version_context_by_id,\n                repre_context_by_id\n            ) = self._contexts_for_representations(project_name, entity_ids)\n\n        if entity_type == \"version\":\n            (\n                version_context_by_id,\n                repre_context_by_id\n            ) = self._contexts_for_versions(project_name, entity_ids)\n\n        action_items = self._get_action_items_for_contexts(\n            project_name,\n            version_context_by_id,\n            repre_context_by_id\n        )\n        action_items.extend(self._get_loader_action_items(\n            project_name,\n            entity_ids,\n            entity_type,\n            version_context_by_id,\n            repre_context_by_id,\n        ))\n        return action_items\n\n    def trigger_action_item(\n        self,\n        identifier: str,\n        project_name: str,\n        selected_ids: set[str],\n        selected_entity_type: str,\n        data: Optional[dict[str, Any]],\n        options: dict[str, Any],\n        form_values: dict[str, Any],\n    ):\n        \"\"\"Trigger action by identifier.\n\n        Triggers the action by identifier for given contexts.\n\n        Triggers events \"load.started\" and \"load.finished\". Finished event\n            also contains \"error_info\" key with error information if any\n            happened.\n\n        Args:\n            identifier (str): Plugin identifier.\n            project_name (str): Project name.\n            selected_ids (set[str]): Selected entity ids.\n            selected_entity_type (str): Selected entity type.\n            data (Optional[dict[str, Any]]): Additional action item data.\n            options (dict[str, Any]): Loader option values.\n            form_values (dict[str, Any]): Form values.\n\n        \"\"\"\n        event_data = {\n            \"identifier\": identifier,\n            \"project_name\": project_name,\n            \"selected_ids\": list(selected_ids),\n            \"selected_entity_type\": selected_entity_type,\n            \"data\": data,\n            \"id\": uuid.uuid4().hex,\n        }\n        self._controller.emit_event(\n            \"load.started\",\n            event_data,\n            ACTIONS_MODEL_SENDER,\n        )\n        if identifier != LOADER_PLUGIN_ID:\n            result = None\n            crashed = False\n            try:\n                result = self._loader_actions.execute_action(\n                    identifier=identifier,\n                    selection=LoaderActionSelection(\n                        project_name,\n                        selected_ids,\n                        selected_entity_type,\n                    ),\n                    data=data,\n                    form_values=form_values,\n                )\n\n            except Exception:\n                crashed = True\n                self._log.warning(\n                    f\"Failed to execute action '{identifier}'\",\n                    exc_info=True,\n                )\n\n            event_data[\"result\"] = result\n            event_data[\"crashed\"] = crashed\n            self._controller.emit_event(\n                \"loader.action.finished\",\n                event_data,\n                ACTIONS_MODEL_SENDER,\n            )\n            return\n\n        loader = self._get_loader_by_identifier(\n            project_name, data[\"loader\"]\n        )\n        entity_type = data[\"entity_type\"]\n        entity_ids = data[\"entity_ids\"]\n        if entity_type == \"version\":\n            error_info = self._trigger_version_loader(\n                loader,\n                options,\n                project_name,\n                entity_ids,\n            )\n        elif entity_type == \"representation\":\n            error_info = self._trigger_representation_loader(\n                loader,\n                options,\n                project_name,\n                entity_ids,\n            )\n        else:\n            raise NotImplementedError(\n                f\"Invalid entity type '{entity_type}' to trigger action item\"\n            )\n\n        event_data[\"error_info\"] = error_info\n        self._controller.emit_event(\n            \"load.finished\",\n            event_data,\n            ACTIONS_MODEL_SENDER,\n        )\n\n    def _get_current_context_project(self):\n        \"\"\"Get current context project name.\n\n        The value is based on controller (host) and cached.\n\n        Returns:\n            Union[str, None]: Current context project.\n        \"\"\"\n\n        if self._current_context_project is NOT_SET:\n            context = self._controller.get_current_context()\n            self._current_context_project = context[\"project_name\"]\n        return self._current_context_project\n\n    def _get_action_label(self, loader, representation=None):\n        \"\"\"Pull label info from loader class.\n\n        Args:\n            loader (LoaderPlugin): Plugin class.\n            representation (Optional[dict[str, Any]]): Representation data.\n\n        Returns:\n            str: Action label.\n        \"\"\"\n\n        label = getattr(loader, \"label\", None)\n        if label is None:\n            label = loader.__name__\n        if representation:\n            # Add the representation as suffix\n            label = \"{} ({})\".format(label, representation[\"name\"])\n        return label\n\n    def _get_action_icon(self, loader):\n        \"\"\"Pull icon info from loader class.\n\n        Args:\n            loader (LoaderPlugin): Plugin class.\n\n        Returns:\n            IconBase | dict[str, Any] | None: Icon definition based on\n                loader plugin.\n        \"\"\"\n\n        icon = getattr(loader, \"icon\", None)\n        # Support font-awesome icons using the `.icon` and `.color`\n        # attributes on plug-ins.\n        if isinstance(icon, str):\n            icon = {\n                \"type\": \"awesome-font\",\n                \"name\": icon,\n                \"color\": getattr(loader, \"color\", None) or \"white\"\n            }\n        return icon\n\n    def _get_action_tooltip(self, loader):\n        \"\"\"Pull tooltip info from loader class.\n\n        Args:\n            loader (LoaderPlugin): Plugin class.\n\n        Returns:\n            str: Action tooltip.\n        \"\"\"\n\n        # Add tooltip and statustip from Loader docstring\n        return inspect.getdoc(loader)\n\n    def _filter_loaders_by_tool_name(self, project_name, loaders):\n        \"\"\"Filter loaders by tool name.\n\n        Tool names are based on AYON tools loader tool and library\n        loader tool. The new tool merged both into one tool and the difference\n        is based only on current project name.\n\n        Args:\n            project_name (str): Project name.\n            loaders (list[LoaderPlugin]): List of loader plugins.\n\n        Returns:\n            list[LoaderPlugin]: Filtered list of loader plugins.\n        \"\"\"\n\n        # Keep filtering by tool name\n        # - if current context project name is same as project name we do\n        #   expect the tool is used as AYON loader tool, otherwise\n        #   as library loader tool.\n        if project_name == self._get_current_context_project():\n            tool_name = \"loader\"\n        else:\n            tool_name = \"library_loader\"\n        filtered_loaders = []\n        for loader in loaders:\n            tool_names = getattr(loader, \"tool_names\", None)\n            if (\n                tool_names is None\n                or \"*\" in tool_names\n                or tool_name in tool_names\n            ):\n                filtered_loaders.append(loader)\n        return filtered_loaders\n\n    def _create_loader_action_item(\n        self,\n        loader,\n        contexts,\n        entity_ids,\n        entity_type,\n        repre_name=None,\n    ):\n        label = self._get_action_label(loader)\n        if repre_name:\n            label = f\"{label} ({repre_name})\"\n        return ActionItem(\n            LOADER_PLUGIN_ID,\n            data={\n                \"entity_ids\": entity_ids,\n                \"entity_type\": entity_type,\n                \"loader\": get_loader_identifier(loader),\n            },\n            label=label,\n            group_label=None,\n            icon=self._get_action_icon(loader),\n            tooltip=self._get_action_tooltip(loader),\n            order=loader.order,\n            options=loader.get_options(contexts),\n        )\n\n    def _get_loaders(self, project_name):\n        \"\"\"Loaders with loaded settings for a project.\n\n        Questions:\n            Project name is required because of settings. Should we actually\n                pass in current project name instead of project name where\n                we want to show loaders for?\n\n        Returns:\n            tuple[list[ProductLoaderPlugin], list[LoaderPlugin]]: Discovered\n                loader plugins.\n        \"\"\"\n\n        loaders_by_identifier_c = self._loaders_by_identifier[project_name]\n        product_loaders_c = self._product_loaders[project_name]\n        repre_loaders_c = self._repre_loaders[project_name]\n        if loaders_by_identifier_c.is_valid:\n            return product_loaders_c.get_data(), repre_loaders_c.get_data()\n\n        # Get all representation-&gt;loader combinations available for the\n        # index under the cursor, so we can list the user the options.\n        available_loaders = self._filter_loaders_by_tool_name(\n            project_name, discover_loader_plugins(project_name)\n        )\n        repre_loaders = []\n        product_loaders = []\n        loaders_by_identifier = {}\n        for loader_cls in available_loaders:\n            if not loader_cls.enabled:\n                continue\n\n            identifier = get_loader_identifier(loader_cls)\n            loaders_by_identifier[identifier] = loader_cls\n            if issubclass(loader_cls, ProductLoaderPlugin):\n                product_loaders.append(loader_cls)\n            else:\n                repre_loaders.append(loader_cls)\n\n        loaders_by_identifier_c.update_data(loaders_by_identifier)\n        product_loaders_c.update_data(product_loaders)\n        repre_loaders_c.update_data(repre_loaders)\n\n        return product_loaders, repre_loaders\n\n    def _get_loader_by_identifier(self, project_name, identifier):\n        if not self._loaders_by_identifier[project_name].is_valid:\n            self._get_loaders(project_name)\n        loaders_by_identifier_c = self._loaders_by_identifier[project_name]\n        loaders_by_identifier = loaders_by_identifier_c.get_data()\n        return loaders_by_identifier.get(identifier)\n\n    def _contexts_for_versions(self, project_name, version_ids):\n        \"\"\"Get contexts for given version ids.\n\n        Prepare version contexts for 'ProductLoaderPlugin' and representation\n        contexts for 'LoaderPlugin' for all children representations of\n        given versions.\n\n        This method is very similar to '_contexts_for_representations' but the\n        queries of entities are called in a different order.\n\n        Args:\n            project_name (str): Project name.\n            version_ids (Iterable[str]): Version ids.\n\n        Returns:\n            tuple[list[dict[str, Any]], list[dict[str, Any]]]: Version and\n                representation contexts.\n        \"\"\"\n\n        # TODO fix hero version\n        version_context_by_id = {}\n        repre_context_by_id = {}\n        if not project_name and not version_ids:\n            return version_context_by_id, repre_context_by_id\n\n        version_entities = self._get_versions(\n            project_name, version_ids\n        )\n        version_entities_by_id = {}\n        version_entities_by_product_id = collections.defaultdict(list)\n        for version_entity in version_entities:\n            version_id = version_entity[\"id\"]\n            product_id = version_entity[\"productId\"]\n            version_entities_by_id[version_id] = version_entity\n            version_entities_by_product_id[product_id].append(version_entity)\n\n        _product_ids = set(version_entities_by_product_id.keys())\n        _product_entities = self._get_products(\n            project_name, _product_ids\n        )\n        product_entities_by_id = {p[\"id\"]: p for p in _product_entities}\n\n        _folder_ids = {p[\"folderId\"] for p in product_entities_by_id.values()}\n        _folder_entities = self._get_folders(\n            project_name, _folder_ids\n        )\n        folder_entities_by_id = {f[\"id\"]: f for f in _folder_entities}\n\n        project_entity = self._get_project(project_name)\n\n        for version_id, version_entity in version_entities_by_id.items():\n            product_id = version_entity[\"productId\"]\n            product_entity = product_entities_by_id[product_id]\n            folder_id = product_entity[\"folderId\"]\n            folder_entity = folder_entities_by_id[folder_id]\n            version_context_by_id[version_id] = {\n                \"project\": project_entity,\n                \"folder\": folder_entity,\n                \"product\": product_entity,\n                \"version\": version_entity,\n            }\n\n        all_repre_ids = set()\n        for repre_ids in self._get_repre_ids_by_version_ids(\n            project_name, version_ids\n        ).values():\n            all_repre_ids |= repre_ids\n\n        repre_entities = self._get_representations(\n            project_name, all_repre_ids\n        )\n        for repre_entity in repre_entities:\n            version_id = repre_entity[\"versionId\"]\n            version_entity = version_entities_by_id[version_id]\n            product_id = version_entity[\"productId\"]\n            product_entity = product_entities_by_id[product_id]\n            folder_id = product_entity[\"folderId\"]\n            folder_entity = folder_entities_by_id[folder_id]\n\n            repre_context_by_id[repre_entity[\"id\"]] = {\n                \"project\": project_entity,\n                \"folder\": folder_entity,\n                \"product\": product_entity,\n                \"version\": version_entity,\n                \"representation\": repre_entity,\n            }\n\n        return version_context_by_id, repre_context_by_id\n\n    def _contexts_for_representations(self, project_name, repre_ids):\n        \"\"\"Get contexts for given representation ids.\n\n        Prepare version contexts for 'ProductLoaderPlugin' and representation\n        contexts for 'LoaderPlugin' for all children representations of\n        given versions.\n\n        This method is very similar to '_contexts_for_versions' but the\n        queries of entities are called in a different order.\n\n        Args:\n            project_name (str): Project name.\n            repre_ids (Iterable[str]): Representation ids.\n\n        Returns:\n            tuple[list[dict[str, Any]], list[dict[str, Any]]]: Version and\n                representation contexts.\n\n        \"\"\"\n        version_context_by_id = {}\n        repre_context_by_id = {}\n        if not project_name and not repre_ids:\n            return version_context_by_id, repre_context_by_id\n\n        repre_entities = self._get_representations(\n            project_name, repre_ids\n        )\n        version_ids = {r[\"versionId\"] for r in repre_entities}\n        version_entities = self._get_versions(\n            project_name, version_ids\n        )\n        version_entities_by_id = {\n            v[\"id\"]: v for v in version_entities\n        }\n\n        product_ids = {v[\"productId\"] for v in version_entities_by_id.values()}\n        product_entities = self._get_products(\n            project_name, product_ids\n\n        )\n        product_entities_by_id = {\n            p[\"id\"]: p for p in product_entities\n        }\n\n        folder_ids = {p[\"folderId\"] for p in product_entities_by_id.values()}\n        folder_entities = self._get_folders(\n            project_name, folder_ids\n        )\n        folder_entities_by_id = {\n            f[\"id\"]: f for f in folder_entities\n        }\n\n        project_entity = self._get_project(project_name)\n\n        version_context_by_id = {}\n        for version_id, version_entity in version_entities_by_id.items():\n            product_id = version_entity[\"productId\"]\n            product_entity = product_entities_by_id[product_id]\n            folder_id = product_entity[\"folderId\"]\n            folder_entity = folder_entities_by_id[folder_id]\n            version_context_by_id[version_id] = {\n                \"project\": project_entity,\n                \"folder\": folder_entity,\n                \"product\": product_entity,\n                \"version\": version_entity,\n            }\n\n        for repre_entity in repre_entities:\n            version_id = repre_entity[\"versionId\"]\n            version_entity = version_entities_by_id[version_id]\n            product_id = version_entity[\"productId\"]\n            product_entity = product_entities_by_id[product_id]\n            folder_id = product_entity[\"folderId\"]\n            folder_entity = folder_entities_by_id[folder_id]\n\n            repre_context_by_id[repre_entity[\"id\"]] = {\n                \"project\": project_entity,\n                \"folder\": folder_entity,\n                \"product\": product_entity,\n                \"version\": version_entity,\n                \"representation\": repre_entity,\n            }\n        return version_context_by_id, repre_context_by_id\n\n    def _get_project(self, project_name: str) -&gt; dict[str, Any]:\n        cache = self._projects_cache[project_name]\n        if not cache.is_valid:\n            cache.update_data(ayon_api.get_project(project_name))\n        return cache.get_data()\n\n    def _get_folders(\n        self, project_name: str, folder_ids: set[str]\n    ) -&gt; list[dict[str, Any]]:\n        \"\"\"Get folders by ids.\"\"\"\n        return self._get_entities(\n            project_name,\n            folder_ids,\n            self._folders_cache,\n            ayon_api.get_folders,\n            \"folder_ids\",\n        )\n\n    def _get_products(\n        self, project_name: str, product_ids: set[str]\n    ) -&gt; list[dict[str, Any]]:\n        \"\"\"Get products by ids.\"\"\"\n        return self._get_entities(\n            project_name,\n            product_ids,\n            self._products_cache,\n            ayon_api.get_products,\n            \"product_ids\",\n        )\n\n    def _get_versions(\n        self, project_name: str, version_ids: set[str]\n    ) -&gt; list[dict[str, Any]]:\n        \"\"\"Get versions by ids.\"\"\"\n        return self._get_entities(\n            project_name,\n            version_ids,\n            self._versions_cache,\n            ayon_api.get_versions,\n            \"version_ids\",\n        )\n\n    def _get_representations(\n        self, project_name: str, representation_ids: set[str]\n    ) -&gt; list[dict[str, Any]]:\n        \"\"\"Get representations by ids.\"\"\"\n        return self._get_entities(\n            project_name,\n            representation_ids,\n            self._representations_cache,\n            ayon_api.get_representations,\n            \"representation_ids\",\n        )\n\n    def _get_repre_ids_by_version_ids(\n        self, project_name: str, version_ids: set[str]\n    ) -&gt; dict[str, set[str]]:\n        output = {}\n        if not version_ids:\n            return output\n\n        project_cache = self._repre_parents_cache[project_name]\n        missing_ids = set()\n        for version_id in version_ids:\n            cache = project_cache[version_id]\n            if cache.is_valid:\n                output[version_id] = cache.get_data()\n            else:\n                missing_ids.add(version_id)\n\n        if missing_ids:\n            repre_cache = self._representations_cache[project_name]\n            repres_by_parent_id = collections.defaultdict(list)\n            for repre in ayon_api.get_representations(\n                project_name, version_ids=missing_ids\n            ):\n                version_id = repre[\"versionId\"]\n                repre_cache[repre[\"id\"]].update_data(repre)\n                repres_by_parent_id[version_id].append(repre)\n\n            for version_id, repres in repres_by_parent_id.items():\n                repre_ids = {\n                    repre[\"id\"]\n                    for repre in repres\n                }\n                output[version_id] = set(repre_ids)\n                project_cache[version_id].update_data(repre_ids)\n\n        return output\n\n    def _get_entities(\n        self,\n        project_name: str,\n        entity_ids: set[str],\n        cache: NestedCacheItem,\n        getter: Callable,\n        filter_arg: str,\n    ) -&gt; list[dict[str, Any]]:\n        entities = []\n        if not entity_ids:\n            return entities\n\n        missing_ids = set()\n        project_cache = cache[project_name]\n        for entity_id in entity_ids:\n            entity_cache = project_cache[entity_id]\n            if entity_cache.is_valid:\n                entities.append(entity_cache.get_data())\n            else:\n                missing_ids.add(entity_id)\n\n        if missing_ids:\n            for entity in getter(project_name, **{filter_arg: missing_ids}):\n                entities.append(entity)\n                entity_id = entity[\"id\"]\n                project_cache[entity_id].update_data(entity)\n        return entities\n\n    def _get_action_items_for_contexts(\n        self,\n        project_name,\n        version_context_by_id,\n        repre_context_by_id\n    ):\n        \"\"\"Prepare action items based on contexts.\n\n        Actions are prepared based on discovered loader plugins and contexts.\n        The context must be valid for the loader plugin.\n\n        Args:\n            project_name (str): Project name.\n            version_context_by_id (dict[str, dict[str, Any]]): Version\n                contexts by version id.\n            repre_context_by_id (dict[str, dict[str, Any]]): Representation\n        \"\"\"\n\n        action_items = []\n        if not version_context_by_id and not repre_context_by_id:\n            return action_items\n\n        product_loaders, repre_loaders = self._get_loaders(project_name)\n\n        repre_contexts_by_name = collections.defaultdict(list)\n        for repre_context in repre_context_by_id.values():\n            repre_name = repre_context[\"representation\"][\"name\"]\n            repre_contexts_by_name[repre_name].append(repre_context)\n\n        for loader in repre_loaders:\n            for repre_name, repre_contexts in repre_contexts_by_name.items():\n                filtered_repre_contexts = filter_repre_contexts_by_loader(\n                    repre_contexts, loader)\n                if not filtered_repre_contexts:\n                    continue\n\n                repre_ids = {\n                    repre_context[\"representation\"][\"id\"]\n                    for repre_context in filtered_repre_contexts\n                }\n\n                item = self._create_loader_action_item(\n                    loader,\n                    repre_contexts,\n                    repre_ids,\n                    \"representation\",\n                    repre_name=repre_name,\n                )\n                action_items.append(item)\n\n        # Product Loaders.\n        product_folder_ids = set()\n        product_ids = set()\n        for product_context in version_context_by_id.values():\n            product_ids.add(product_context[\"product\"][\"id\"])\n            product_folder_ids.add(product_context[\"folder\"][\"id\"])\n\n        version_ids = set(version_context_by_id.keys())\n        version_contexts = list(version_context_by_id.values())\n        for loader in product_loaders:\n            item = self._create_loader_action_item(\n                loader,\n                version_contexts,\n                version_ids,\n                \"version\",\n            )\n            action_items.append(item)\n        return action_items\n\n    def _get_loader_action_items(\n        self,\n        project_name: str,\n        entity_ids: set[str],\n        entity_type: str,\n        version_context_by_id: dict[str, dict[str, Any]],\n        repre_context_by_id: dict[str, dict[str, Any]],\n    ) -&gt; list[ActionItem]:\n        \"\"\"\n\n        Args:\n            project_name (str): Project name.\n            entity_ids (set[str]): Selected entity ids.\n            entity_type (str): Selected entity type.\n            version_context_by_id (dict[str, dict[str, Any]]): Version context\n                by id.\n            repre_context_by_id (dict[str, dict[str, Any]]): Representation\n                context by id.\n\n        Returns:\n            list[ActionItem]: List of action items.\n\n        \"\"\"\n        entities_cache = self._prepare_entities_cache(\n            project_name,\n            entity_type,\n            version_context_by_id,\n            repre_context_by_id,\n        )\n        selection = LoaderActionSelection(\n            project_name,\n            entity_ids,\n            entity_type,\n            entities_cache=entities_cache\n        )\n        items = []\n        for action in self._loader_actions.get_action_items(selection):\n            items.append(ActionItem(\n                action.identifier,\n                label=action.label,\n                group_label=action.group_label,\n                icon=action.icon,\n                tooltip=None,  # action.tooltip,\n                order=action.order,\n                data=action.data,\n                options=None,  # action.options,\n            ))\n        return items\n\n    def _prepare_entities_cache(\n        self,\n        project_name: str,\n        entity_type: str,\n        version_context_by_id: dict[str, dict[str, Any]],\n        repre_context_by_id: dict[str, dict[str, Any]],\n    ):\n        project_entity = None\n        folders_by_id = {}\n        products_by_id = {}\n        versions_by_id = {}\n        representations_by_id = {}\n        for context in version_context_by_id.values():\n            if project_entity is None:\n                project_entity = context[\"project\"]\n            folder_entity = context[\"folder\"]\n            product_entity = context[\"product\"]\n            version_entity = context[\"version\"]\n            folders_by_id[folder_entity[\"id\"]] = folder_entity\n            products_by_id[product_entity[\"id\"]] = product_entity\n            versions_by_id[version_entity[\"id\"]] = version_entity\n\n        for context in repre_context_by_id.values():\n            repre_entity = context[\"representation\"]\n            representations_by_id[repre_entity[\"id\"]] = repre_entity\n\n        # Mapping has to be for all child entities which is available for\n        #   representations only if version is selected\n        representation_ids_by_version_id = {}\n        if entity_type == \"version\":\n            representation_ids_by_version_id = {\n                version_id: set()\n                for version_id in versions_by_id\n            }\n            for context in repre_context_by_id.values():\n                repre_entity = context[\"representation\"]\n                v_id = repre_entity[\"versionId\"]\n                representation_ids_by_version_id[v_id].add(repre_entity[\"id\"])\n\n        return SelectionEntitiesCache(\n            project_name,\n            project_entity=project_entity,\n            folders_by_id=folders_by_id,\n            products_by_id=products_by_id,\n            versions_by_id=versions_by_id,\n            representations_by_id=representations_by_id,\n            representation_ids_by_version_id=representation_ids_by_version_id,\n        )\n\n    def _trigger_version_loader(\n        self,\n        loader,\n        options,\n        project_name,\n        version_ids,\n    ):\n        \"\"\"Trigger version loader.\n\n        This triggers 'load' method of 'ProductLoaderPlugin' for given version\n        ids.\n\n        Note:\n            Even when the plugin is 'ProductLoaderPlugin' it actually expects\n                versions and should be named 'VersionLoaderPlugin'. Because it\n                is planned to refactor load system and introduce\n                'LoaderAction' plugins it is not relevant to change it\n                anymore.\n\n        Args:\n            loader (ProductLoaderPlugin): Loader plugin to use.\n            options (dict): Option values for loader.\n            project_name (str): Project name.\n            version_ids (Iterable[str]): Version ids.\n        \"\"\"\n\n        project_entity = ayon_api.get_project(project_name)\n\n        version_entities = list(ayon_api.get_versions(\n            project_name, version_ids=version_ids\n        ))\n        product_ids = {v[\"productId\"] for v in version_entities}\n        product_entities = self._get_products(\n            project_name, product_ids\n        )\n        product_entities_by_id = {p[\"id\"]: p for p in product_entities}\n        folder_ids = {p[\"folderId\"] for p in product_entities_by_id.values()}\n        folder_entities = self._get_folders(\n            project_name, folder_ids=folder_ids\n        )\n        folder_entities_by_id = {f[\"id\"]: f for f in folder_entities}\n        product_contexts = []\n        for version_entity in version_entities:\n            product_id = version_entity[\"productId\"]\n            product_entity = product_entities_by_id[product_id]\n            folder_id = product_entity[\"folderId\"]\n            folder_entity = folder_entities_by_id[folder_id]\n            product_contexts.append({\n                \"project\": project_entity,\n                \"folder\": folder_entity,\n                \"product\": product_entity,\n                \"version\": version_entity,\n            })\n\n        return self._load_products_by_loader(\n            loader, product_contexts, options\n        )\n\n    def _trigger_representation_loader(\n        self,\n        loader,\n        options,\n        project_name,\n        representation_ids,\n    ):\n        \"\"\"Trigger representation loader.\n\n        This triggers 'load' method of 'LoaderPlugin' for given representation\n            ids. For that are prepared contexts for each representation, with\n            all parent entities.\n\n        Args:\n            loader (LoaderPlugin): Loader plugin to use.\n            options (dict): Option values for loader.\n            project_name (str): Project name.\n            representation_ids (Iterable[str]): Representation ids.\n        \"\"\"\n\n        project_entity = ayon_api.get_project(project_name)\n        repre_entities = list(ayon_api.get_representations(\n            project_name, representation_ids=representation_ids\n        ))\n        version_ids = {r[\"versionId\"] for r in repre_entities}\n        version_entities = ayon_api.get_versions(\n            project_name, version_ids=version_ids\n        )\n        version_entities_by_id = {v[\"id\"]: v for v in version_entities}\n        product_ids = {\n            v[\"productId\"] for v in version_entities_by_id.values()\n        }\n        product_entities = ayon_api.get_products(\n            project_name, product_ids=product_ids\n        )\n        product_entities_by_id = {p[\"id\"]: p for p in product_entities}\n        folder_ids = {p[\"folderId\"] for p in product_entities_by_id.values()}\n        folder_entities = ayon_api.get_folders(\n            project_name, folder_ids=folder_ids\n        )\n        folder_entities_by_id = {f[\"id\"]: f for f in folder_entities}\n        repre_contexts = []\n        for repre_entity in repre_entities:\n            version_id = repre_entity[\"versionId\"]\n            version_entity = version_entities_by_id[version_id]\n            product_id = version_entity[\"productId\"]\n            product_entity = product_entities_by_id[product_id]\n            folder_id = product_entity[\"folderId\"]\n            folder_entity = folder_entities_by_id[folder_id]\n            repre_contexts.append({\n                \"project\": project_entity,\n                \"folder\": folder_entity,\n                \"product\": product_entity,\n                \"version\": version_entity,\n                \"representation\": repre_entity,\n            })\n\n        return self._load_representations_by_loader(\n            loader, repre_contexts, options\n        )\n\n    def _load_representations_by_loader(\n        self,\n        loader,\n        repre_contexts,\n        options\n    ):\n        \"\"\"Loops through list of repre_contexts and loads them with one loader\n\n        Args:\n            loader (LoaderPlugin): Loader plugin to use.\n            repre_contexts (list[dict]): Full info about selected\n                representations, containing repre, version, product, folder\n                and project entities.\n            options (dict): Data from options.\n        \"\"\"\n\n        error_info = []\n        for repre_context in repre_contexts:\n            version_entity = repre_context[\"version\"]\n            version = version_entity[\"version\"]\n            if version &lt; 0:\n                version = \"Hero\"\n            try:\n                load_with_repre_context(\n                    loader,\n                    repre_context,\n                    options=options\n                )\n\n            except IncompatibleLoaderError as exc:\n                print(exc)\n                error_info.append((\n                    \"Incompatible Loader\",\n                    None,\n                    repre_context[\"representation\"][\"name\"],\n                    repre_context[\"product\"][\"name\"],\n                    version\n                ))\n\n            except Exception as exc:\n                formatted_traceback = None\n                if not isinstance(exc, LoadError):\n                    exc_type, exc_value, exc_traceback = sys.exc_info()\n                    formatted_traceback = \"\".join(traceback.format_exception(\n                        exc_type, exc_value, exc_traceback\n                    ))\n\n                error_info.append((\n                    str(exc),\n                    formatted_traceback,\n                    repre_context[\"representation\"][\"name\"],\n                    repre_context[\"product\"][\"name\"],\n                    version\n                ))\n        return error_info\n\n    def _load_products_by_loader(\n        self,\n        loader,\n        version_contexts,\n        options\n    ):\n        \"\"\"Triggers load with ProductLoader type of loaders.\n\n        Warning:\n            Plugin is named 'ProductLoader' but version is passed to context\n                too.\n\n        Args:\n            loader (ProductLoader): Loader used to load.\n            version_contexts (list[dict[str, Any]]): For context for each\n                version.\n            options (dict[str, Any]): Options for loader that user could fill.\n        \"\"\"\n\n        error_info = []\n        if loader.is_multiple_contexts_compatible:\n            product_names = []\n            for context in version_contexts:\n                product_name = context.get(\"product\", {}).get(\"name\") or \"N/A\"\n                product_names.append(product_name)\n            try:\n                load_with_product_contexts(\n                    loader,\n                    version_contexts,\n                    options=options\n                )\n            except Exception as exc:\n                formatted_traceback = None\n                if not isinstance(exc, LoadError):\n                    exc_type, exc_value, exc_traceback = sys.exc_info()\n                    formatted_traceback = \"\".join(traceback.format_exception(\n                        exc_type, exc_value, exc_traceback\n                    ))\n                error_info.append((\n                    str(exc),\n                    formatted_traceback,\n                    None,\n                    \", \".join(product_names),\n                    None\n                ))\n        else:\n            for version_context in version_contexts:\n                product_name = (\n                    version_context.get(\"product\", {}).get(\"name\") or \"N/A\"\n                )\n                try:\n                    load_with_product_context(\n                        loader,\n                        version_context,\n                        options=options\n                    )\n\n                except Exception as exc:\n                    formatted_traceback = None\n                    if not isinstance(exc, LoadError):\n                        exc_type, exc_value, exc_traceback = sys.exc_info()\n                        formatted_traceback = \"\".join(\n                            traceback.format_exception(\n                                exc_type, exc_value, exc_traceback\n                            )\n                        )\n\n                    error_info.append((\n                        str(exc),\n                        formatted_traceback,\n                        None,\n                        product_name,\n                        None\n                    ))\n\n        return error_info\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/models/actions.html#client.ayon_core.tools.loader.models.actions.LoaderActionsModel.reset","title":"<code>reset()</code>","text":"<p>Reset the model with all cached items.</p> Source code in <code>client/ayon_core/tools/loader/models/actions.py</code> <pre><code>def reset(self):\n    \"\"\"Reset the model with all cached items.\"\"\"\n\n    self._current_context_project = NOT_SET\n    self._loaders_by_identifier.reset()\n    self._product_loaders.reset()\n    self._repre_loaders.reset()\n    self._loader_actions.reset()\n\n    self._folders_cache.reset()\n    self._tasks_cache.reset()\n    self._products_cache.reset()\n    self._versions_cache.reset()\n    self._representations_cache.reset()\n    self._repre_parents_cache.reset()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/models/actions.html#client.ayon_core.tools.loader.models.actions.LoaderActionsModel.trigger_action_item","title":"<code>trigger_action_item(identifier, project_name, selected_ids, selected_entity_type, data, options, form_values)</code>","text":"<p>Trigger action by identifier.</p> <p>Triggers the action by identifier for given contexts.</p> <p>Triggers events \"load.started\" and \"load.finished\". Finished event     also contains \"error_info\" key with error information if any     happened.</p> <p>Parameters:</p> Name Type Description Default <code>identifier</code> <code>str</code> <p>Plugin identifier.</p> required <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>selected_ids</code> <code>set[str]</code> <p>Selected entity ids.</p> required <code>selected_entity_type</code> <code>str</code> <p>Selected entity type.</p> required <code>data</code> <code>Optional[dict[str, Any]]</code> <p>Additional action item data.</p> required <code>options</code> <code>dict[str, Any]</code> <p>Loader option values.</p> required <code>form_values</code> <code>dict[str, Any]</code> <p>Form values.</p> required Source code in <code>client/ayon_core/tools/loader/models/actions.py</code> <pre><code>def trigger_action_item(\n    self,\n    identifier: str,\n    project_name: str,\n    selected_ids: set[str],\n    selected_entity_type: str,\n    data: Optional[dict[str, Any]],\n    options: dict[str, Any],\n    form_values: dict[str, Any],\n):\n    \"\"\"Trigger action by identifier.\n\n    Triggers the action by identifier for given contexts.\n\n    Triggers events \"load.started\" and \"load.finished\". Finished event\n        also contains \"error_info\" key with error information if any\n        happened.\n\n    Args:\n        identifier (str): Plugin identifier.\n        project_name (str): Project name.\n        selected_ids (set[str]): Selected entity ids.\n        selected_entity_type (str): Selected entity type.\n        data (Optional[dict[str, Any]]): Additional action item data.\n        options (dict[str, Any]): Loader option values.\n        form_values (dict[str, Any]): Form values.\n\n    \"\"\"\n    event_data = {\n        \"identifier\": identifier,\n        \"project_name\": project_name,\n        \"selected_ids\": list(selected_ids),\n        \"selected_entity_type\": selected_entity_type,\n        \"data\": data,\n        \"id\": uuid.uuid4().hex,\n    }\n    self._controller.emit_event(\n        \"load.started\",\n        event_data,\n        ACTIONS_MODEL_SENDER,\n    )\n    if identifier != LOADER_PLUGIN_ID:\n        result = None\n        crashed = False\n        try:\n            result = self._loader_actions.execute_action(\n                identifier=identifier,\n                selection=LoaderActionSelection(\n                    project_name,\n                    selected_ids,\n                    selected_entity_type,\n                ),\n                data=data,\n                form_values=form_values,\n            )\n\n        except Exception:\n            crashed = True\n            self._log.warning(\n                f\"Failed to execute action '{identifier}'\",\n                exc_info=True,\n            )\n\n        event_data[\"result\"] = result\n        event_data[\"crashed\"] = crashed\n        self._controller.emit_event(\n            \"loader.action.finished\",\n            event_data,\n            ACTIONS_MODEL_SENDER,\n        )\n        return\n\n    loader = self._get_loader_by_identifier(\n        project_name, data[\"loader\"]\n    )\n    entity_type = data[\"entity_type\"]\n    entity_ids = data[\"entity_ids\"]\n    if entity_type == \"version\":\n        error_info = self._trigger_version_loader(\n            loader,\n            options,\n            project_name,\n            entity_ids,\n        )\n    elif entity_type == \"representation\":\n        error_info = self._trigger_representation_loader(\n            loader,\n            options,\n            project_name,\n            entity_ids,\n        )\n    else:\n        raise NotImplementedError(\n            f\"Invalid entity type '{entity_type}' to trigger action item\"\n        )\n\n    event_data[\"error_info\"] = error_info\n    self._controller.emit_event(\n        \"load.finished\",\n        event_data,\n        ACTIONS_MODEL_SENDER,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/models/products.html","title":"products","text":"<p>Products model for loader tools.</p>"},{"location":"autoapi/client/ayon_core/tools/loader/models/products.html#client.ayon_core.tools.loader.models.products.ProductsModel","title":"<code>ProductsModel</code>","text":"<p>Model for products, version and representation.</p> <p>All the entities are product based. This model prepares data for UI and caches it for faster access.</p> Note <p>Data are not used for actions model because that would require to     break OpenPype compatibility of 'LoaderPlugin's.</p> Source code in <code>client/ayon_core/tools/loader/models/products.py</code> <pre><code>class ProductsModel:\n    \"\"\"Model for products, version and representation.\n\n    All the entities are product based. This model prepares data for UI\n    and caches it for faster access.\n\n    Note:\n        Data are not used for actions model because that would require to\n            break OpenPype compatibility of 'LoaderPlugin's.\n    \"\"\"\n\n    lifetime = 60  # In seconds (minute by default)\n\n    def __init__(self, controller):\n        self._controller = controller\n\n        # Mapping helpers\n        # NOTE - mapping must be cleaned up with cache cleanup\n        self._product_item_by_id = collections.defaultdict(dict)\n        self._version_item_by_id = collections.defaultdict(dict)\n        self._product_folder_ids_mapping = collections.defaultdict(dict)\n\n        # Cache helpers\n        self._product_type_items_cache = NestedCacheItem(\n            levels=1, default_factory=list, lifetime=self.lifetime)\n        self._product_base_type_items_cache = NestedCacheItem(\n            levels=1, default_factory=list, lifetime=self.lifetime)\n        self._product_items_cache = NestedCacheItem(\n            levels=2, default_factory=dict, lifetime=self.lifetime)\n        self._repre_items_cache = NestedCacheItem(\n            levels=2, default_factory=dict, lifetime=self.lifetime)\n\n    def reset(self):\n        \"\"\"Reset model with all cached data.\"\"\"\n\n        self._product_item_by_id.clear()\n        self._version_item_by_id.clear()\n        self._product_folder_ids_mapping.clear()\n\n        self._product_type_items_cache.reset()\n        self._product_items_cache.reset()\n        self._repre_items_cache.reset()\n\n    def get_product_type_items(\n        self, project_name: Optional[str]\n    ) -&gt; list[ProductTypeItem]:\n        \"\"\"Product type items for project.\n\n        Args:\n            project_name (Union[str, None]): Project name.\n\n        Returns:\n            list[ProductTypeItem]: Product type items.\n\n        \"\"\"\n        if not project_name:\n            return []\n\n        cache = self._product_type_items_cache[project_name]\n        if not cache.is_valid:\n            icons_mapping = self._get_product_type_icons(project_name)\n            product_types = self._get_project_product_types(project_name)\n            cache.update_data([\n                ProductTypeItem(\n                    product_type[\"name\"],\n                    icons_mapping.get_icon(product_type=product_type[\"name\"]),\n                )\n                for product_type in product_types\n            ])\n        return cache.get_data()\n\n    def get_product_base_type_items(\n        self, project_name: Optional[str]\n    ) -&gt; list[ProductBaseTypeItem]:\n        \"\"\"Product base type items for the project.\n\n        Notes:\n            This will be used for filtering product types in UI when\n                product base types are fully implemented.\n\n        Args:\n            project_name (optional, str): Project name.\n\n        Returns:\n            list[ProductBaseTypeDict]: Product base type items.\n\n        \"\"\"\n        if not project_name:\n            return []\n\n        cache = self._product_base_type_items_cache[project_name]\n        if not cache.is_valid:\n            icons_mapping = self._get_product_type_icons(project_name)\n            product_base_types = []\n            # TODO add temp implementation here when it is actually\n            #   implemented and available on server.\n            if hasattr(ayon_api, \"get_project_product_base_types\"):\n                product_base_types = ayon_api.get_project_product_base_types(\n                    project_name\n                )\n            cache.update_data([\n                ProductBaseTypeItem(\n                    product_base_type[\"name\"],\n                    icons_mapping.get_icon(product_base_type[\"name\"]),\n                )\n                for product_base_type in product_base_types\n            ])\n        return cache.get_data()\n\n    def get_product_items(self, project_name, folder_ids, sender):\n        \"\"\"Product items with versions for project and folder ids.\n\n        Product items also contain version items. They're directly connected\n        to product items in the UI and the separation is not needed.\n\n        Args:\n            project_name (Union[str, None]): Project name.\n            folder_ids (Iterable[str]): Folder ids.\n            sender (Union[str, None]): Who triggered the method.\n\n        Returns:\n            list[ProductItem]: Product items.\n        \"\"\"\n\n        if not project_name or not folder_ids:\n            return []\n\n        project_cache = self._product_items_cache[project_name]\n        output = []\n        folder_ids_to_update = set()\n        for folder_id in folder_ids:\n            cache = project_cache[folder_id]\n            if cache.is_valid:\n                output.extend(cache.get_data().values())\n            else:\n                folder_ids_to_update.add(folder_id)\n\n        self._refresh_product_items(\n            project_name, folder_ids_to_update, sender)\n\n        for folder_id in folder_ids_to_update:\n            cache = project_cache[folder_id]\n            output.extend(cache.get_data().values())\n        return output\n\n    def get_product_item(self, project_name, product_id):\n        \"\"\"Get product item based on passed product id.\n\n        This method is using cached items, but if cache is not valid it also\n        can query the item.\n\n        Args:\n            project_name (Union[str, None]): Where to look for product.\n            product_id (Union[str, None]): Product id to receive.\n\n        Returns:\n            Union[ProductItem, None]: Product item or 'None' if not found.\n        \"\"\"\n\n        if not any((project_name, product_id)):\n            return None\n\n        product_items_by_id = self._product_item_by_id[project_name]\n        product_item = product_items_by_id.get(product_id)\n        if product_item is not None:\n            return product_item\n        for product_item in self._query_product_items_by_ids(\n            project_name, product_ids=[product_id]\n        ).values():\n            return product_item\n\n    def get_product_ids_by_repre_ids(self, project_name, repre_ids):\n        \"\"\"Get product ids based on passed representation ids.\n\n        Args:\n            project_name (str): Where to look for representations.\n            repre_ids (Iterable[str]): Representation ids.\n\n        Returns:\n            set[str]: Product ids for passed representation ids.\n        \"\"\"\n\n        # TODO look out how to use single server call\n        if not repre_ids:\n            return set()\n        repres = ayon_api.get_representations(\n            project_name, repre_ids, fields=[\"versionId\"]\n        )\n        version_ids = {repre[\"versionId\"] for repre in repres}\n        if not version_ids:\n            return set()\n        versions = ayon_api.get_versions(\n            project_name, version_ids=version_ids, fields=[\"productId\"]\n        )\n        return {v[\"productId\"] for v in versions}\n\n    def get_repre_items(self, project_name, version_ids, sender):\n        \"\"\"Get representation items for passed version ids.\n\n        Args:\n            project_name (str): Project name.\n            version_ids (Iterable[str]): Version ids.\n            sender (Union[str, None]): Who triggered the method.\n\n        Returns:\n            list[RepreItem]: Representation items.\n        \"\"\"\n\n        output = []\n        if not any((project_name, version_ids)):\n            return output\n\n        invalid_version_ids = set()\n        project_cache = self._repre_items_cache[project_name]\n        for version_id in version_ids:\n            version_cache = project_cache[version_id]\n            if version_cache.is_valid:\n                output.extend(version_cache.get_data().values())\n            else:\n                invalid_version_ids.add(version_id)\n\n        if invalid_version_ids:\n            self.refresh_representation_items(\n                project_name, invalid_version_ids, sender\n            )\n\n        for version_id in invalid_version_ids:\n            version_cache = project_cache[version_id]\n            output.extend(version_cache.get_data().values())\n\n        return output\n\n    def get_versions_repre_count(self, project_name, version_ids, sender):\n        \"\"\"Get representation count for passed version ids.\n\n        Args:\n            project_name (str): Project name.\n            version_ids (Iterable[str]): Version ids.\n            sender (Union[str, None]): Who triggered the method.\n\n        Returns:\n            dict[str, int]: Number of representations by version id.\n        \"\"\"\n\n        output = {}\n        if not any((project_name, version_ids)):\n            return output\n\n        invalid_version_ids = set()\n        project_cache = self._repre_items_cache[project_name]\n        for version_id in version_ids:\n            version_cache = project_cache[version_id]\n            if version_cache.is_valid:\n                output[version_id] = len(version_cache.get_data())\n            else:\n                invalid_version_ids.add(version_id)\n\n        if invalid_version_ids:\n            self.refresh_representation_items(\n                project_name, invalid_version_ids, sender\n            )\n\n        for version_id in invalid_version_ids:\n            version_cache = project_cache[version_id]\n            output[version_id] = len(version_cache.get_data())\n\n        return output\n\n    def change_products_group(self, project_name, product_ids, group_name):\n        \"\"\"Change group name for passed product ids.\n\n        Group name is stored in 'attrib' of product entity and is used in UI\n        to group items.\n\n        Method triggers \"products.group.changed\" event with data:\n            {\n                \"project_name\": project_name,\n                \"folder_ids\": folder_ids,\n                \"product_ids\": product_ids,\n                \"group_name\": group_name\n            }\n\n        Args:\n            project_name (str): Project name.\n            product_ids (Iterable[str]): Product ids to change group name for.\n            group_name (str): Group name to set.\n        \"\"\"\n\n        if not product_ids:\n            return\n\n        product_items = self._get_product_items_by_id(\n            project_name, product_ids\n        )\n        if not product_items:\n            return\n\n        session = OperationsSession()\n        folder_ids = set()\n        for product_item in product_items.values():\n            session.update_entity(\n                project_name,\n                \"product\",\n                product_item.product_id,\n                {\"attrib\": {\"productGroup\": group_name or None}}\n            )\n            folder_ids.add(product_item.folder_id)\n            product_item.group_name = group_name\n\n        session.commit()\n        self._controller.emit_event(\n            \"products.group.changed\",\n            {\n                \"project_name\": project_name,\n                \"folder_ids\": folder_ids,\n                \"product_ids\": product_ids,\n                \"group_name\": group_name,\n            },\n            PRODUCTS_MODEL_SENDER\n        )\n\n    def _get_project_product_types(self, project_name: str) -&gt; list[dict]:\n        \"\"\"This is a temporary solution for product types fetching.\n\n        There was a bug in ayon_api.get_project(...) which did not use GraphQl\n            but REST instead. That is fixed in ayon-python-api 1.2.6 that will\n            be as part of ayon launcher 1.4.3 release.\n\n        \"\"\"\n        if not project_name:\n            return []\n        query = project_graphql_query({\"productTypes.name\"})\n        query.set_variable_value(\"projectName\", project_name)\n        parsed_data = query.query(ayon_api.get_server_api_connection())\n        project = parsed_data[\"project\"]\n        if project is None:\n            return []\n        return project[\"productTypes\"]\n\n    def _get_product_type_icons(\n        self, project_name: Optional[str]\n    ) -&gt; ProductTypeIconMapping:\n        return self._controller.get_product_type_icons_mapping(project_name)\n\n    def _get_product_items_by_id(self, project_name, product_ids):\n        product_item_by_id = self._product_item_by_id[project_name]\n        missing_product_ids = set()\n        output = {}\n        for product_id in product_ids:\n            product_item = product_item_by_id.get(product_id)\n            if product_item is not None:\n                output[product_id] = product_item\n            else:\n                missing_product_ids.add(product_id)\n\n        output.update(\n            self._query_product_items_by_ids(\n                project_name, product_ids=missing_product_ids\n            )\n        )\n        return output\n\n    def _get_version_items_by_id(self, project_name, version_ids):\n        version_item_by_id = self._version_item_by_id[project_name]\n        missing_version_ids = set()\n        output = {}\n        for version_id in version_ids:\n            version_item = version_item_by_id.get(version_id)\n            if version_item is not None:\n                output[version_id] = version_item\n            else:\n                missing_version_ids.add(version_id)\n\n        output.update(\n            self._query_version_items_by_ids(\n                project_name, missing_version_ids\n            )\n        )\n        return output\n\n    def _create_product_items(\n        self,\n        project_name: str,\n        products: Iterable[ProductDict],\n        versions: Iterable[VersionDict],\n        folder_items=None,\n    ):\n        if folder_items is None:\n            folder_items = self._controller.get_folder_items(project_name)\n\n        loaded_product_ids = self._controller.get_loaded_product_ids()\n\n        versions_by_product_id = collections.defaultdict(list)\n        for version in versions:\n            versions_by_product_id[version[\"productId\"]].append(version)\n\n        output: dict[str, ProductItem] = {}\n        icons_mapping = self._get_product_type_icons(project_name)\n        for product in products:\n            product_id = product[\"id\"]\n            folder_id = product[\"folderId\"]\n            folder_item = folder_items.get(folder_id)\n            if not folder_item:\n                continue\n            versions = versions_by_product_id[product_id]\n            if not versions:\n                continue\n            product_item = product_item_from_entity(\n                product,\n                versions,\n                folder_item.label,\n                icons_mapping,\n                product_id in loaded_product_ids,\n            )\n            output[product_id] = product_item\n        return output\n\n    def _query_product_items_by_ids(\n        self,\n        project_name,\n        folder_ids=None,\n        product_ids=None,\n        folder_items=None\n    ):\n        \"\"\"Query product items.\n\n        This method does get from, or store to, cache attributes.\n\n        One of 'product_ids' or 'folder_ids' must be passed to the method.\n\n        Args:\n            project_name (str): Project name.\n            folder_ids (Optional[Iterable[str]]): Folder ids under which are\n                products.\n            product_ids (Optional[Iterable[str]]): Product ids to use.\n            folder_items (Optional[Dict[str, FolderItem]]): Prepared folder\n                items from controller.\n\n        Returns:\n            dict[str, ProductItem]: Product items by product id.\n        \"\"\"\n\n        if not folder_ids and not product_ids:\n            return {}\n\n        kwargs = {}\n        if folder_ids is not None:\n            kwargs[\"folder_ids\"] = folder_ids\n\n        if product_ids is not None:\n            kwargs[\"product_ids\"] = product_ids\n\n        products = list(ayon_api.get_products(project_name, **kwargs))\n        product_ids = {product[\"id\"] for product in products}\n\n        # Add 'status' to fields -&gt; fixed in ayon-python-api 1.0.4\n        fields = ayon_api.get_default_fields_for_type(\"version\")\n        fields.add(\"status\")\n        versions = ayon_api.get_versions(\n            project_name, product_ids=product_ids, fields=fields\n        )\n\n        return self._create_product_items(\n            project_name, products, versions, folder_items=folder_items\n        )\n\n    def _query_version_items_by_ids(self, project_name, version_ids):\n        versions = list(ayon_api.get_versions(\n            project_name, version_ids=version_ids\n        ))\n        product_ids = {version[\"productId\"] for version in versions}\n        products = list(ayon_api.get_products(\n            project_name, product_ids=product_ids\n        ))\n        product_items = self._create_product_items(\n            project_name, products, versions\n        )\n        version_items = {}\n        for product_item in product_items.values():\n            version_items.update(product_item.version_items)\n        return version_items\n\n    def _clear_product_version_items(self, project_name, folder_ids):\n        \"\"\"Clear product and version items from memory.\n\n        When products are re-queried for a folders, the old product and version\n        items in '_product_item_by_id' and '_version_item_by_id' should\n        be cleaned up from memory. And mapping in stored in\n        '_product_folder_ids_mapping' is not relevant either.\n\n        Args:\n            project_name (str): Name of project.\n            folder_ids (Iterable[str]): Folder ids which are being refreshed.\n        \"\"\"\n\n        project_mapping = self._product_folder_ids_mapping[project_name]\n        if not project_mapping:\n            return\n\n        product_item_by_id = self._product_item_by_id[project_name]\n        version_item_by_id = self._version_item_by_id[project_name]\n        for folder_id in folder_ids:\n            product_ids = project_mapping.pop(folder_id, None)\n            if not product_ids:\n                continue\n\n            for product_id in product_ids:\n                product_item = product_item_by_id.pop(product_id, None)\n                if product_item is None:\n                    continue\n                for version_item in product_item.version_items.values():\n                    version_item_by_id.pop(version_item.version_id, None)\n\n    def _refresh_product_items(self, project_name, folder_ids, sender):\n        \"\"\"Refresh product items and store them in cache.\n\n        Args:\n            project_name (str): Name of project.\n            folder_ids (Iterable[str]): Folder ids which are being refreshed.\n            sender (Union[str, None]): Who triggered the refresh.\n        \"\"\"\n\n        if not project_name or not folder_ids:\n            return\n\n        self._clear_product_version_items(project_name, folder_ids)\n\n        project_mapping = self._product_folder_ids_mapping[project_name]\n        product_item_by_id = self._product_item_by_id[project_name]\n        version_item_by_id = self._version_item_by_id[project_name]\n\n        for folder_id in folder_ids:\n            project_mapping[folder_id] = set()\n\n        with self._product_refresh_event_manager(\n            project_name, folder_ids, sender\n        ):\n            folder_items = self._controller.get_folder_items(project_name)\n            items_by_folder_id = {\n                folder_id: {}\n                for folder_id in folder_ids\n            }\n            product_items_by_id = self._query_product_items_by_ids(\n                project_name,\n                folder_ids=folder_ids,\n                folder_items=folder_items\n            )\n            for product_id, product_item in product_items_by_id.items():\n                folder_id = product_item.folder_id\n                items_by_folder_id[product_item.folder_id][product_id] = (\n                    product_item\n                )\n\n                project_mapping[folder_id].add(product_id)\n                product_item_by_id[product_id] = product_item\n                for version_id, version_item in (\n                    product_item.version_items.items()\n                ):\n                    version_item_by_id[version_id] = version_item\n\n            project_cache = self._product_items_cache[project_name]\n            for folder_id, product_items in items_by_folder_id.items():\n                project_cache[folder_id].update_data(product_items)\n\n    @contextlib.contextmanager\n    def _product_refresh_event_manager(\n        self, project_name, folder_ids, sender\n    ):\n        self._controller.emit_event(\n            \"products.refresh.started\",\n            {\n                \"project_name\": project_name,\n                \"folder_ids\": folder_ids,\n                \"sender\": sender,\n            },\n            PRODUCTS_MODEL_SENDER\n        )\n        try:\n            yield\n\n        finally:\n            self._controller.emit_event(\n                \"products.refresh.finished\",\n                {\n                    \"project_name\": project_name,\n                    \"folder_ids\": folder_ids,\n                    \"sender\": sender,\n                },\n                PRODUCTS_MODEL_SENDER\n            )\n\n    def refresh_representation_items(\n        self, project_name, version_ids, sender\n    ):\n        if not any((project_name, version_ids)):\n            return\n        self._controller.emit_event(\n            \"model.representations.refresh.started\",\n            {\n                \"project_name\": project_name,\n                \"version_ids\": version_ids,\n                \"sender\": sender,\n            },\n            PRODUCTS_MODEL_SENDER\n        )\n        failed = False\n        try:\n            self._refresh_representation_items(project_name, version_ids)\n        except Exception:\n            # TODO add more information about failed refresh\n            failed = True\n\n        self._controller.emit_event(\n            \"model.representations.refresh.finished\",\n            {\n                \"project_name\": project_name,\n                \"version_ids\": version_ids,\n                \"sender\": sender,\n                \"failed\": failed,\n            },\n            PRODUCTS_MODEL_SENDER\n        )\n\n    def _refresh_representation_items(self, project_name, version_ids):\n        representations = list(ayon_api.get_representations(\n            project_name,\n            version_ids=version_ids,\n            fields=[\"id\", \"name\", \"versionId\"]\n        ))\n\n        version_items_by_id = self._get_version_items_by_id(\n            project_name, version_ids\n        )\n        product_ids = {\n            version_item.product_id\n            for version_item in version_items_by_id.values()\n        }\n        product_items_by_id = self._get_product_items_by_id(\n            project_name, product_ids\n        )\n        repre_icon = AwesomeFontIcon(\n            \"fa.file-o\",\n            color=get_default_entity_icon_color(),\n        )\n        repre_items_by_version_id = collections.defaultdict(dict)\n        for representation in representations:\n            version_id = representation[\"versionId\"]\n            version_item = version_items_by_id.get(version_id)\n            if version_item is None:\n                continue\n            product_item = product_items_by_id.get(version_item.product_id)\n            if product_item is None:\n                continue\n            repre_id = representation[\"id\"]\n            repre_item = RepreItem(\n                repre_id,\n                representation[\"name\"],\n                repre_icon,\n                product_item.product_name,\n                product_item.folder_label,\n            )\n            repre_items_by_version_id[version_id][repre_id] = repre_item\n\n        project_cache = self._repre_items_cache[project_name]\n        for version_id, repre_items in repre_items_by_version_id.items():\n            version_cache = project_cache[version_id]\n            version_cache.update_data(repre_items)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/models/products.html#client.ayon_core.tools.loader.models.products.ProductsModel.change_products_group","title":"<code>change_products_group(project_name, product_ids, group_name)</code>","text":"<p>Change group name for passed product ids.</p> <p>Group name is stored in 'attrib' of product entity and is used in UI to group items.</p> <p>Method triggers \"products.group.changed\" event with data:     {         \"project_name\": project_name,         \"folder_ids\": folder_ids,         \"product_ids\": product_ids,         \"group_name\": group_name     }</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>product_ids</code> <code>Iterable[str]</code> <p>Product ids to change group name for.</p> required <code>group_name</code> <code>str</code> <p>Group name to set.</p> required Source code in <code>client/ayon_core/tools/loader/models/products.py</code> <pre><code>def change_products_group(self, project_name, product_ids, group_name):\n    \"\"\"Change group name for passed product ids.\n\n    Group name is stored in 'attrib' of product entity and is used in UI\n    to group items.\n\n    Method triggers \"products.group.changed\" event with data:\n        {\n            \"project_name\": project_name,\n            \"folder_ids\": folder_ids,\n            \"product_ids\": product_ids,\n            \"group_name\": group_name\n        }\n\n    Args:\n        project_name (str): Project name.\n        product_ids (Iterable[str]): Product ids to change group name for.\n        group_name (str): Group name to set.\n    \"\"\"\n\n    if not product_ids:\n        return\n\n    product_items = self._get_product_items_by_id(\n        project_name, product_ids\n    )\n    if not product_items:\n        return\n\n    session = OperationsSession()\n    folder_ids = set()\n    for product_item in product_items.values():\n        session.update_entity(\n            project_name,\n            \"product\",\n            product_item.product_id,\n            {\"attrib\": {\"productGroup\": group_name or None}}\n        )\n        folder_ids.add(product_item.folder_id)\n        product_item.group_name = group_name\n\n    session.commit()\n    self._controller.emit_event(\n        \"products.group.changed\",\n        {\n            \"project_name\": project_name,\n            \"folder_ids\": folder_ids,\n            \"product_ids\": product_ids,\n            \"group_name\": group_name,\n        },\n        PRODUCTS_MODEL_SENDER\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/models/products.html#client.ayon_core.tools.loader.models.products.ProductsModel.get_product_base_type_items","title":"<code>get_product_base_type_items(project_name)</code>","text":"<p>Product base type items for the project.</p> Notes <p>This will be used for filtering product types in UI when     product base types are fully implemented.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>(optional, str)</code> <p>Project name.</p> required <p>Returns:</p> Type Description <code>list[ProductBaseTypeItem]</code> <p>list[ProductBaseTypeDict]: Product base type items.</p> Source code in <code>client/ayon_core/tools/loader/models/products.py</code> <pre><code>def get_product_base_type_items(\n    self, project_name: Optional[str]\n) -&gt; list[ProductBaseTypeItem]:\n    \"\"\"Product base type items for the project.\n\n    Notes:\n        This will be used for filtering product types in UI when\n            product base types are fully implemented.\n\n    Args:\n        project_name (optional, str): Project name.\n\n    Returns:\n        list[ProductBaseTypeDict]: Product base type items.\n\n    \"\"\"\n    if not project_name:\n        return []\n\n    cache = self._product_base_type_items_cache[project_name]\n    if not cache.is_valid:\n        icons_mapping = self._get_product_type_icons(project_name)\n        product_base_types = []\n        # TODO add temp implementation here when it is actually\n        #   implemented and available on server.\n        if hasattr(ayon_api, \"get_project_product_base_types\"):\n            product_base_types = ayon_api.get_project_product_base_types(\n                project_name\n            )\n        cache.update_data([\n            ProductBaseTypeItem(\n                product_base_type[\"name\"],\n                icons_mapping.get_icon(product_base_type[\"name\"]),\n            )\n            for product_base_type in product_base_types\n        ])\n    return cache.get_data()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/models/products.html#client.ayon_core.tools.loader.models.products.ProductsModel.get_product_ids_by_repre_ids","title":"<code>get_product_ids_by_repre_ids(project_name, repre_ids)</code>","text":"<p>Get product ids based on passed representation ids.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Where to look for representations.</p> required <code>repre_ids</code> <code>Iterable[str]</code> <p>Representation ids.</p> required <p>Returns:</p> Type Description <p>set[str]: Product ids for passed representation ids.</p> Source code in <code>client/ayon_core/tools/loader/models/products.py</code> <pre><code>def get_product_ids_by_repre_ids(self, project_name, repre_ids):\n    \"\"\"Get product ids based on passed representation ids.\n\n    Args:\n        project_name (str): Where to look for representations.\n        repre_ids (Iterable[str]): Representation ids.\n\n    Returns:\n        set[str]: Product ids for passed representation ids.\n    \"\"\"\n\n    # TODO look out how to use single server call\n    if not repre_ids:\n        return set()\n    repres = ayon_api.get_representations(\n        project_name, repre_ids, fields=[\"versionId\"]\n    )\n    version_ids = {repre[\"versionId\"] for repre in repres}\n    if not version_ids:\n        return set()\n    versions = ayon_api.get_versions(\n        project_name, version_ids=version_ids, fields=[\"productId\"]\n    )\n    return {v[\"productId\"] for v in versions}\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/models/products.html#client.ayon_core.tools.loader.models.products.ProductsModel.get_product_item","title":"<code>get_product_item(project_name, product_id)</code>","text":"<p>Get product item based on passed product id.</p> <p>This method is using cached items, but if cache is not valid it also can query the item.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>Union[str, None]</code> <p>Where to look for product.</p> required <code>product_id</code> <code>Union[str, None]</code> <p>Product id to receive.</p> required <p>Returns:</p> Type Description <p>Union[ProductItem, None]: Product item or 'None' if not found.</p> Source code in <code>client/ayon_core/tools/loader/models/products.py</code> <pre><code>def get_product_item(self, project_name, product_id):\n    \"\"\"Get product item based on passed product id.\n\n    This method is using cached items, but if cache is not valid it also\n    can query the item.\n\n    Args:\n        project_name (Union[str, None]): Where to look for product.\n        product_id (Union[str, None]): Product id to receive.\n\n    Returns:\n        Union[ProductItem, None]: Product item or 'None' if not found.\n    \"\"\"\n\n    if not any((project_name, product_id)):\n        return None\n\n    product_items_by_id = self._product_item_by_id[project_name]\n    product_item = product_items_by_id.get(product_id)\n    if product_item is not None:\n        return product_item\n    for product_item in self._query_product_items_by_ids(\n        project_name, product_ids=[product_id]\n    ).values():\n        return product_item\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/models/products.html#client.ayon_core.tools.loader.models.products.ProductsModel.get_product_items","title":"<code>get_product_items(project_name, folder_ids, sender)</code>","text":"<p>Product items with versions for project and folder ids.</p> <p>Product items also contain version items. They're directly connected to product items in the UI and the separation is not needed.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>Union[str, None]</code> <p>Project name.</p> required <code>folder_ids</code> <code>Iterable[str]</code> <p>Folder ids.</p> required <code>sender</code> <code>Union[str, None]</code> <p>Who triggered the method.</p> required <p>Returns:</p> Type Description <p>list[ProductItem]: Product items.</p> Source code in <code>client/ayon_core/tools/loader/models/products.py</code> <pre><code>def get_product_items(self, project_name, folder_ids, sender):\n    \"\"\"Product items with versions for project and folder ids.\n\n    Product items also contain version items. They're directly connected\n    to product items in the UI and the separation is not needed.\n\n    Args:\n        project_name (Union[str, None]): Project name.\n        folder_ids (Iterable[str]): Folder ids.\n        sender (Union[str, None]): Who triggered the method.\n\n    Returns:\n        list[ProductItem]: Product items.\n    \"\"\"\n\n    if not project_name or not folder_ids:\n        return []\n\n    project_cache = self._product_items_cache[project_name]\n    output = []\n    folder_ids_to_update = set()\n    for folder_id in folder_ids:\n        cache = project_cache[folder_id]\n        if cache.is_valid:\n            output.extend(cache.get_data().values())\n        else:\n            folder_ids_to_update.add(folder_id)\n\n    self._refresh_product_items(\n        project_name, folder_ids_to_update, sender)\n\n    for folder_id in folder_ids_to_update:\n        cache = project_cache[folder_id]\n        output.extend(cache.get_data().values())\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/models/products.html#client.ayon_core.tools.loader.models.products.ProductsModel.get_product_type_items","title":"<code>get_product_type_items(project_name)</code>","text":"<p>Product type items for project.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>Union[str, None]</code> <p>Project name.</p> required <p>Returns:</p> Type Description <code>list[ProductTypeItem]</code> <p>list[ProductTypeItem]: Product type items.</p> Source code in <code>client/ayon_core/tools/loader/models/products.py</code> <pre><code>def get_product_type_items(\n    self, project_name: Optional[str]\n) -&gt; list[ProductTypeItem]:\n    \"\"\"Product type items for project.\n\n    Args:\n        project_name (Union[str, None]): Project name.\n\n    Returns:\n        list[ProductTypeItem]: Product type items.\n\n    \"\"\"\n    if not project_name:\n        return []\n\n    cache = self._product_type_items_cache[project_name]\n    if not cache.is_valid:\n        icons_mapping = self._get_product_type_icons(project_name)\n        product_types = self._get_project_product_types(project_name)\n        cache.update_data([\n            ProductTypeItem(\n                product_type[\"name\"],\n                icons_mapping.get_icon(product_type=product_type[\"name\"]),\n            )\n            for product_type in product_types\n        ])\n    return cache.get_data()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/models/products.html#client.ayon_core.tools.loader.models.products.ProductsModel.get_repre_items","title":"<code>get_repre_items(project_name, version_ids, sender)</code>","text":"<p>Get representation items for passed version ids.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>version_ids</code> <code>Iterable[str]</code> <p>Version ids.</p> required <code>sender</code> <code>Union[str, None]</code> <p>Who triggered the method.</p> required <p>Returns:</p> Type Description <p>list[RepreItem]: Representation items.</p> Source code in <code>client/ayon_core/tools/loader/models/products.py</code> <pre><code>def get_repre_items(self, project_name, version_ids, sender):\n    \"\"\"Get representation items for passed version ids.\n\n    Args:\n        project_name (str): Project name.\n        version_ids (Iterable[str]): Version ids.\n        sender (Union[str, None]): Who triggered the method.\n\n    Returns:\n        list[RepreItem]: Representation items.\n    \"\"\"\n\n    output = []\n    if not any((project_name, version_ids)):\n        return output\n\n    invalid_version_ids = set()\n    project_cache = self._repre_items_cache[project_name]\n    for version_id in version_ids:\n        version_cache = project_cache[version_id]\n        if version_cache.is_valid:\n            output.extend(version_cache.get_data().values())\n        else:\n            invalid_version_ids.add(version_id)\n\n    if invalid_version_ids:\n        self.refresh_representation_items(\n            project_name, invalid_version_ids, sender\n        )\n\n    for version_id in invalid_version_ids:\n        version_cache = project_cache[version_id]\n        output.extend(version_cache.get_data().values())\n\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/models/products.html#client.ayon_core.tools.loader.models.products.ProductsModel.get_versions_repre_count","title":"<code>get_versions_repre_count(project_name, version_ids, sender)</code>","text":"<p>Get representation count for passed version ids.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>version_ids</code> <code>Iterable[str]</code> <p>Version ids.</p> required <code>sender</code> <code>Union[str, None]</code> <p>Who triggered the method.</p> required <p>Returns:</p> Type Description <p>dict[str, int]: Number of representations by version id.</p> Source code in <code>client/ayon_core/tools/loader/models/products.py</code> <pre><code>def get_versions_repre_count(self, project_name, version_ids, sender):\n    \"\"\"Get representation count for passed version ids.\n\n    Args:\n        project_name (str): Project name.\n        version_ids (Iterable[str]): Version ids.\n        sender (Union[str, None]): Who triggered the method.\n\n    Returns:\n        dict[str, int]: Number of representations by version id.\n    \"\"\"\n\n    output = {}\n    if not any((project_name, version_ids)):\n        return output\n\n    invalid_version_ids = set()\n    project_cache = self._repre_items_cache[project_name]\n    for version_id in version_ids:\n        version_cache = project_cache[version_id]\n        if version_cache.is_valid:\n            output[version_id] = len(version_cache.get_data())\n        else:\n            invalid_version_ids.add(version_id)\n\n    if invalid_version_ids:\n        self.refresh_representation_items(\n            project_name, invalid_version_ids, sender\n        )\n\n    for version_id in invalid_version_ids:\n        version_cache = project_cache[version_id]\n        output[version_id] = len(version_cache.get_data())\n\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/models/products.html#client.ayon_core.tools.loader.models.products.ProductsModel.reset","title":"<code>reset()</code>","text":"<p>Reset model with all cached data.</p> Source code in <code>client/ayon_core/tools/loader/models/products.py</code> <pre><code>def reset(self):\n    \"\"\"Reset model with all cached data.\"\"\"\n\n    self._product_item_by_id.clear()\n    self._version_item_by_id.clear()\n    self._product_folder_ids_mapping.clear()\n\n    self._product_type_items_cache.reset()\n    self._product_items_cache.reset()\n    self._repre_items_cache.reset()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/models/products.html#client.ayon_core.tools.loader.models.products.product_base_type_item_from_data","title":"<code>product_base_type_item_from_data(product_base_type_data)</code>","text":"<p>Create product base type item from data.</p> <p>Parameters:</p> Name Type Description Default <code>product_base_type_data</code> <code>ProductBaseTypeDict</code> <p>Product base type data.</p> required <p>Returns:</p> Name Type Description <code>ProductBaseTypeDict</code> <code>ProductBaseTypeItem</code> <p>Product base type item.</p> Source code in <code>client/ayon_core/tools/loader/models/products.py</code> <pre><code>def product_base_type_item_from_data(\n    product_base_type_data: ProductBaseTypeDict\n) -&gt; ProductBaseTypeItem:\n    \"\"\"Create product base type item from data.\n\n    Args:\n        product_base_type_data (ProductBaseTypeDict): Product base type data.\n\n    Returns:\n        ProductBaseTypeDict: Product base type item.\n\n    \"\"\"\n    icon = AwesomeFontIcon(\"fa.folder\", color=\"#0091B2\")\n    return ProductBaseTypeItem(\n        name=product_base_type_data[\"name\"],\n        icon=icon,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/models/selection.html","title":"selection","text":""},{"location":"autoapi/client/ayon_core/tools/loader/models/selection.html#client.ayon_core.tools.loader.models.selection.SelectionModel","title":"<code>SelectionModel</code>","text":"<p>               Bases: <code>object</code></p> <p>Model handling selection changes.</p> <p>Triggering events: - \"selection.project.changed\" - \"selection.folders.changed\" - \"selection.versions.changed\"</p> Source code in <code>client/ayon_core/tools/loader/models/selection.py</code> <pre><code>class SelectionModel(object):\n    \"\"\"Model handling selection changes.\n\n    Triggering events:\n    - \"selection.project.changed\"\n    - \"selection.folders.changed\"\n    - \"selection.versions.changed\"\n    \"\"\"\n\n    event_source = \"selection.model\"\n\n    def __init__(self, controller):\n        self._controller = controller\n\n        self._project_name = None\n        self._folder_ids = set()\n        self._task_ids = set()\n        self._version_ids = set()\n        self._representation_ids = set()\n\n    def get_selected_project_name(self):\n        return self._project_name\n\n    def set_selected_project(self, project_name):\n        if self._project_name == project_name:\n            return\n\n        self._project_name = project_name\n        self._controller.emit_event(\n            \"selection.project.changed\",\n            {\"project_name\": self._project_name},\n            self.event_source\n        )\n\n    def get_selected_folder_ids(self):\n        return self._folder_ids\n\n    def set_selected_folders(self, folder_ids):\n        if folder_ids == self._folder_ids:\n            return\n\n        self._folder_ids = folder_ids\n        self._controller.emit_event(\n            \"selection.folders.changed\",\n            {\n                \"project_name\": self._project_name,\n                \"folder_ids\": folder_ids,\n            },\n            self.event_source\n        )\n\n    def get_selected_task_ids(self):\n        return self._task_ids\n\n    def set_selected_tasks(self, task_ids):\n        if task_ids == self._task_ids:\n            return\n\n        self._task_ids = task_ids\n        self._controller.emit_event(\n            \"selection.tasks.changed\",\n            {\n                \"project_name\": self._project_name,\n                \"task_ids\": task_ids,\n            },\n            self.event_source\n        )\n\n    def get_selected_version_ids(self):\n        return self._version_ids\n\n    def set_selected_versions(self, version_ids):\n        if version_ids == self._version_ids:\n            return\n\n        self._version_ids = version_ids\n        self._controller.emit_event(\n            \"selection.versions.changed\",\n            {\n                \"project_name\": self._project_name,\n                \"folder_ids\": self._folder_ids,\n                \"version_ids\": self._version_ids,\n            },\n            self.event_source\n        )\n\n    def get_selected_representation_ids(self):\n        return self._representation_ids\n\n    def set_selected_representations(self, repre_ids):\n        if repre_ids == self._representation_ids:\n            return\n\n        self._representation_ids = repre_ids\n        self._controller.emit_event(\n            \"selection.representations.changed\",\n            {\n                \"project_name\": self._project_name,\n                \"folder_ids\": self._folder_ids,\n                \"version_ids\": self._version_ids,\n                \"representation_ids\": self._representation_ids,\n            }\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/models/sitesync.html","title":"sitesync","text":""},{"location":"autoapi/client/ayon_core/tools/loader/models/sitesync.html#client.ayon_core.tools.loader.models.sitesync.SiteSyncModel","title":"<code>SiteSyncModel</code>","text":"<p>Model handling site sync logic.</p> <p>Model cares about handling of site sync functionality. All public functions should be possible to call even if site sync is not available.</p> Source code in <code>client/ayon_core/tools/loader/models/sitesync.py</code> <pre><code>class SiteSyncModel:\n    \"\"\"Model handling site sync logic.\n\n    Model cares about handling of site sync functionality. All public\n    functions should be possible to call even if site sync is not available.\n    \"\"\"\n\n    lifetime = 60  # In seconds (minute by default)\n    status_lifetime = 20\n\n    def __init__(self, controller):\n        self._controller = controller\n\n        self._site_icons = None\n        self._sitesync_enabled_cache = NestedCacheItem(\n            levels=1, lifetime=self.lifetime\n        )\n        self._active_site_cache = NestedCacheItem(\n            levels=1, lifetime=self.lifetime\n        )\n        self._remote_site_cache = NestedCacheItem(\n            levels=1, lifetime=self.lifetime\n        )\n        self._version_availability_cache = NestedCacheItem(\n            levels=2,\n            default_factory=_default_version_availability,\n            lifetime=self.status_lifetime\n        )\n        self._repre_status_cache = NestedCacheItem(\n            levels=2,\n            default_factory=_default_repre_status,\n            lifetime=self.status_lifetime\n        )\n\n        manager = AddonsManager()\n        self._sitesync_addon = manager.get(\"sitesync\")\n\n    def reset(self):\n        self._site_icons = None\n        self._sitesync_enabled_cache.reset()\n        self._active_site_cache.reset()\n        self._remote_site_cache.reset()\n        self._version_availability_cache.reset()\n        self._repre_status_cache.reset()\n\n    def is_sitesync_enabled(self, project_name=None):\n        \"\"\"Site sync is enabled for a project.\n\n        Returns false if site sync addon is not available or enabled\n            or project has disabled it.\n\n        Args:\n            project_name (Union[str, None]): Project name. If project name\n                is 'None', True is returned if site sync addon\n                is available and enabled.\n\n        Returns:\n            bool: Site sync is enabled.\n        \"\"\"\n\n        if not self._is_sitesync_addon_enabled():\n            return False\n        cache = self._sitesync_enabled_cache[project_name]\n        if not cache.is_valid:\n            enabled = True\n            if project_name:\n                enabled = self._sitesync_addon.is_project_enabled(\n                    project_name, single=True\n                )\n            cache.update_data(enabled)\n        return cache.get_data()\n\n    def get_active_site(self, project_name):\n        \"\"\"Active site name for a project.\n\n        Args:\n            project_name (str): Project name.\n\n        Returns:\n            Union[str, None]: Remote site name.\n        \"\"\"\n\n        cache = self._active_site_cache[project_name]\n        if not cache.is_valid:\n            site_name = None\n            if project_name and self._is_sitesync_addon_enabled():\n                site_name = self._sitesync_addon.get_active_site(project_name)\n            cache.update_data(site_name)\n        return cache.get_data()\n\n    def get_remote_site(self, project_name):\n        \"\"\"Remote site name for a project.\n\n        Args:\n            project_name (str): Project name.\n\n        Returns:\n            Union[str, None]: Remote site name.\n        \"\"\"\n\n        cache = self._remote_site_cache[project_name]\n        if not cache.is_valid:\n            site_name = None\n            if project_name and self._is_sitesync_addon_enabled():\n                site_name = self._sitesync_addon.get_remote_site(project_name)\n            cache.update_data(site_name)\n        return cache.get_data()\n\n    def get_active_site_icon_def(self, project_name):\n        \"\"\"Active site icon definition.\n\n        Args:\n            project_name (Union[str, None]): Name of project.\n\n        Returns:\n            Union[dict[str, Any], None]: Site icon definition.\n        \"\"\"\n\n        if not project_name or not self.is_sitesync_enabled(project_name):\n            return None\n        active_site = self.get_active_site(project_name)\n        return self._get_site_icon_def(project_name, active_site)\n\n    def get_remote_site_icon_def(self, project_name):\n        \"\"\"Remote site icon definition.\n\n        Args:\n            project_name (Union[str, None]): Name of project.\n\n        Returns:\n            Union[dict[str, Any], None]: Site icon definition.\n        \"\"\"\n\n        if not project_name or not self.is_sitesync_enabled(project_name):\n            return None\n        remote_site = self.get_remote_site(project_name)\n        return self._get_site_icon_def(project_name, remote_site)\n\n    def _get_site_icon_def(self, project_name, site_name):\n        # use different icon for studio even if provider is 'local_drive'\n        if site_name == self._sitesync_addon.DEFAULT_SITE:\n            provider = \"studio\"\n        else:\n            provider = self._get_provider_for_site(project_name, site_name)\n        return self._get_provider_icon(provider)\n\n    def get_version_sync_availability(self, project_name, version_ids):\n        \"\"\"Returns how many representations are available on sites.\n\n        Returned value `{version_id: (4, 6)}` denotes that locally are\n            available 4 and remotely 6 representation.\n        NOTE: Available means they were synced to site.\n\n        Returns:\n            dict[str, tuple[int, int]]\n        \"\"\"\n\n        if not self.is_sitesync_enabled(project_name):\n            return {\n                version_id: _default_version_availability()\n                for version_id in version_ids\n            }\n\n        output = {}\n        project_cache = self._version_availability_cache[project_name]\n        invalid_ids = set()\n        for version_id in version_ids:\n            repre_cache = project_cache[version_id]\n            if repre_cache.is_valid:\n                output[version_id] = repre_cache.get_data()\n            else:\n                invalid_ids.add(version_id)\n\n        if invalid_ids:\n            self._refresh_version_availability(\n                project_name, invalid_ids\n            )\n            for version_id in invalid_ids:\n                version_cache = project_cache[version_id]\n                output[version_id] = version_cache.get_data()\n        return output\n\n    def get_representations_sync_status(\n        self, project_name, representation_ids\n    ):\n        \"\"\"\n\n        Args:\n            project_name (str): Project name.\n            representation_ids (Iterable[str]): Representation ids.\n\n        Returns:\n            dict[str, tuple[float, float]]\n        \"\"\"\n\n        if not self.is_sitesync_enabled(project_name):\n            return {\n                repre_id: _default_repre_status()\n                for repre_id in representation_ids\n            }\n\n        output = {}\n        project_cache = self._repre_status_cache[project_name]\n        invalid_ids = set()\n        for repre_id in representation_ids:\n            repre_cache = project_cache[repre_id]\n            if repre_cache.is_valid:\n                output[repre_id] = repre_cache.get_data()\n            else:\n                invalid_ids.add(repre_id)\n\n        if invalid_ids:\n            self._refresh_representations_sync_status(\n                project_name, invalid_ids\n            )\n            for repre_id in invalid_ids:\n                repre_cache = project_cache[repre_id]\n                output[repre_id] = repre_cache.get_data()\n        return output\n\n    def get_sitesync_action_items(\n        self, project_name, entity_ids, entity_type\n    ):\n        \"\"\"\n\n        Args:\n             project_name (str): Project name.\n             entity_ids (set[str]): Selected entity ids.\n             entity_type (str): Selected entity type.\n\n        Returns:\n            list[ActionItem]: Actions that can be shown in loader.\n\n        \"\"\"\n        if entity_type != \"representation\":\n            return []\n\n        if not self.is_sitesync_enabled(project_name):\n            return []\n\n        repres_status = self.get_representations_sync_status(\n            project_name, entity_ids\n        )\n\n        repre_ids_per_identifier = collections.defaultdict(set)\n        for repre_id in entity_ids:\n            repre_status = repres_status[repre_id]\n            local_status, remote_status = repre_status\n\n            if local_status:\n                repre_ids_per_identifier[UPLOAD_IDENTIFIER].add(repre_id)\n                repre_ids_per_identifier[REMOVE_IDENTIFIER].add(repre_id)\n\n            if remote_status:\n                repre_ids_per_identifier[DOWNLOAD_IDENTIFIER].add(repre_id)\n\n        action_items = []\n        for identifier, repre_ids in repre_ids_per_identifier.items():\n            if identifier == DOWNLOAD_IDENTIFIER:\n                action_items.append(self._create_download_action_item(\n                    project_name, repre_ids\n                ))\n            elif identifier == UPLOAD_IDENTIFIER:\n                action_items.append(self._create_upload_action_item(\n                    project_name, repre_ids\n                ))\n            elif identifier == REMOVE_IDENTIFIER:\n                action_items.append(self._create_delete_action_item(\n                    project_name, repre_ids\n                ))\n\n        return action_items\n\n    def is_sitesync_action(self, identifier: str) -&gt; bool:\n        \"\"\"Should be `identifier` handled by SiteSync.\n\n        Args:\n            identifier (str): Plugin identifier.\n\n        Returns:\n            bool: Should action be handled by SiteSync.\n\n        \"\"\"\n        return identifier == \"sitesync.loader.action\"\n\n    def trigger_action_item(\n        self,\n        project_name: str,\n        data: dict[str, Any],\n    ):\n        \"\"\"Resets status for site_name or remove local files.\n\n        Args:\n            project_name (str): Project name.\n            data (dict[str, Any]): Action item data.\n\n        \"\"\"\n        representation_ids = data[\"representation_ids\"]\n        action_identifier = data[\"action_identifier\"]\n        active_site = self.get_active_site(project_name)\n        remote_site = self.get_remote_site(project_name)\n\n        repre_entities_by_id = {\n            repre_entity[\"id\"]: repre_entity\n            for repre_entity in get_representations(\n                project_name, representation_ids=representation_ids\n            )\n        }\n        # TODO get product type from product entity instead of 'context'\n        #   on representation\n        product_type_by_repre_id = {}\n        for repre_id, repre_entity in repre_entities_by_id.items():\n            repre_context = repre_entity[\"context\"]\n            product_type = repre_context.get(\"product\", {}).get(\"type\")\n            if not product_type:\n                product_type = repre_context.get(\"family\")\n\n            product_type_by_repre_id[repre_id] = product_type\n\n        for repre_id in representation_ids:\n            repre_entity = repre_entities_by_id.get(repre_id)\n            product_type = product_type_by_repre_id[repre_id]\n            if action_identifier == DOWNLOAD_IDENTIFIER:\n                self._add_site(\n                    project_name, repre_entity, active_site, product_type\n                )\n\n            elif action_identifier == UPLOAD_IDENTIFIER:\n                self._add_site(\n                    project_name, repre_entity, remote_site, product_type\n                )\n\n            elif action_identifier == REMOVE_IDENTIFIER:\n                self._sitesync_addon.remove_site(\n                    project_name,\n                    repre_id,\n                    active_site,\n                    remove_local_files=True\n                )\n\n    def _is_sitesync_addon_enabled(self):\n        \"\"\"\n        Returns:\n            bool: Site sync addon is enabled.\n        \"\"\"\n\n        if self._sitesync_addon is None:\n            return False\n        return self._sitesync_addon.enabled\n\n    def _get_provider_for_site(self, project_name, site_name):\n        \"\"\"Provider for a site.\n\n        Args:\n            project_name (str): Project name.\n            site_name (str): Site name.\n\n        Returns:\n            Union[str, None]: Provider name.\n        \"\"\"\n\n        if not self._is_sitesync_addon_enabled():\n            return None\n        return self._sitesync_addon.get_provider_for_site(\n            project_name, site_name\n        )\n\n    def _get_provider_icon(self, provider):\n        \"\"\"site provider icons.\n\n        Returns:\n            Union[dict[str, Any], None]: Icon of site provider.\n        \"\"\"\n\n        if not provider:\n            return None\n\n        if self._site_icons is None:\n            self._site_icons = self._sitesync_addon.get_site_icons()\n        return self._site_icons.get(provider)\n\n    def _refresh_version_availability(self, project_name, version_ids):\n        if not project_name or not version_ids:\n            return\n        project_cache = self._version_availability_cache[project_name]\n\n        avail_by_id = self._sitesync_addon.get_version_availability(\n            project_name,\n            version_ids,\n            self.get_active_site(project_name),\n            self.get_remote_site(project_name),\n        )\n        for version_id in version_ids:\n            status = avail_by_id.get(version_id)\n            if status is None:\n                status = _default_version_availability()\n            project_cache[version_id].update_data(status)\n\n    def _refresh_representations_sync_status(\n        self, project_name, representation_ids\n    ):\n        if not project_name or not representation_ids:\n            return\n        project_cache = self._repre_status_cache[project_name]\n        status_by_repre_id = (\n            self._sitesync_addon.get_representations_sync_state(\n                project_name,\n                representation_ids,\n                self.get_active_site(project_name),\n                self.get_remote_site(project_name),\n            )\n        )\n        for repre_id in representation_ids:\n            status = status_by_repre_id.get(repre_id)\n            if status is None:\n                status = _default_repre_status()\n            project_cache[repre_id].update_data(status)\n\n    def _create_download_action_item(self, project_name, representation_ids):\n        return self._create_action_item(\n            project_name,\n            representation_ids,\n            DOWNLOAD_IDENTIFIER,\n            \"Download\",\n            \"Mark representation for download locally\",\n            \"fa.download\"\n        )\n\n    def _create_upload_action_item(self, project_name, representation_ids):\n        return self._create_action_item(\n            project_name,\n            representation_ids,\n            UPLOAD_IDENTIFIER,\n            \"Upload\",\n            \"Mark representation for upload remotely\",\n            \"fa.upload\"\n        )\n\n    def _create_delete_action_item(self, project_name, representation_ids):\n        return self._create_action_item(\n            project_name,\n            representation_ids,\n            REMOVE_IDENTIFIER,\n            \"Remove from local\",\n            \"Remove local synchronization\",\n            \"fa.trash\"\n        )\n\n    def _create_action_item(\n        self,\n        project_name,\n        representation_ids,\n        action_identifier,\n        label,\n        tooltip,\n        icon_name\n    ):\n        return ActionItem(\n            \"sitesync.loader.action\",\n            label=label,\n            group_label=None,\n            icon=AwesomeFontIcon(icon_name, color=\"#999999\"),\n            tooltip=tooltip,\n            order=1,\n            data={\n                \"representation_ids\": representation_ids,\n                \"action_identifier\": action_identifier,\n            },\n            options=None,\n        )\n\n    def _add_site(self, project_name, repre_entity, site_name, product_type):\n        self._sitesync_addon.add_site(\n            project_name, repre_entity[\"id\"], site_name, force=True\n        )\n\n        # TODO this should happen in site sync addon\n        if product_type != \"workfile\":\n            return\n\n        links = self._get_linked_representation_id(\n            project_name,\n            repre_entity,\n            \"reference\"\n        )\n        for link_repre_id in links:\n            if not self._sitesync_addon.is_representation_on_site(\n                project_name,\n                link_repre_id,\n                site_name\n            ):\n                print(\"Adding {} to linked representation: {}\".format(\n                    site_name, link_repre_id))\n                self._sitesync_addon.add_site(\n                    project_name,\n                    link_repre_id,\n                    site_name,\n                    force=True\n                )\n\n    def _get_linked_representation_id(\n        self,\n        project_name,\n        repre_entity,\n        link_type,\n        max_depth=None\n    ):\n        \"\"\"Returns list of linked ids of particular type (if provided).\n\n        One of representation document or representation id must be passed.\n        Note:\n            Representation links now works only from representation through\n                version back to representations.\n\n        Todos:\n            Missing depth query. Not sure how it did find more representations\n                in depth, probably links to version?\n            This function should probably live in sitesync addon?\n\n        Args:\n            project_name (str): Name of project where look for links.\n            repre_entity (dict[str, Any]): Representation entity.\n            link_type (str): Type of link (e.g. 'reference', ...).\n            max_depth (int): Limit recursion level. Default: 0\n\n        Returns:\n            List[ObjectId] Linked representation ids.\n        \"\"\"\n\n        if not repre_entity:\n            return []\n\n        version_id = repre_entity[\"versionId\"]\n        if max_depth is None or max_depth == 0:\n            max_depth = 1\n\n        link_types = None\n        if link_type:\n            link_types = [link_type]\n\n        # Store already found version ids to avoid recursion, and also to store\n        #   output -&gt; Don't forget to remove 'version_id' at the end!!!\n        linked_version_ids = {version_id}\n        # Each loop of depth will reset this variable\n        versions_to_check = {version_id}\n        for _ in range(max_depth):\n            if not versions_to_check:\n                break\n\n            versions_links = get_versions_links(\n                project_name,\n                versions_to_check,\n                link_types=link_types,\n                link_direction=\"in\")  # looking for 'in'puts for version\n\n            versions_to_check = set()\n            for links in versions_links.values():\n                for link in links:\n                    # Care only about version links\n                    if link[\"entityType\"] != \"version\":\n                        continue\n                    entity_id = link[\"entityId\"]\n                    linked_version_ids.add(entity_id)\n                    versions_to_check.add(entity_id)\n\n        linked_version_ids.remove(version_id)\n        if not linked_version_ids:\n            return []\n        representations = get_representations(\n            project_name,\n            version_ids=linked_version_ids,\n            fields=[\"id\"])\n        return [\n            repre[\"id\"]\n            for repre in representations\n        ]\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/models/sitesync.html#client.ayon_core.tools.loader.models.sitesync.SiteSyncModel.get_active_site","title":"<code>get_active_site(project_name)</code>","text":"<p>Active site name for a project.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <p>Returns:</p> Type Description <p>Union[str, None]: Remote site name.</p> Source code in <code>client/ayon_core/tools/loader/models/sitesync.py</code> <pre><code>def get_active_site(self, project_name):\n    \"\"\"Active site name for a project.\n\n    Args:\n        project_name (str): Project name.\n\n    Returns:\n        Union[str, None]: Remote site name.\n    \"\"\"\n\n    cache = self._active_site_cache[project_name]\n    if not cache.is_valid:\n        site_name = None\n        if project_name and self._is_sitesync_addon_enabled():\n            site_name = self._sitesync_addon.get_active_site(project_name)\n        cache.update_data(site_name)\n    return cache.get_data()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/models/sitesync.html#client.ayon_core.tools.loader.models.sitesync.SiteSyncModel.get_active_site_icon_def","title":"<code>get_active_site_icon_def(project_name)</code>","text":"<p>Active site icon definition.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>Union[str, None]</code> <p>Name of project.</p> required <p>Returns:</p> Type Description <p>Union[dict[str, Any], None]: Site icon definition.</p> Source code in <code>client/ayon_core/tools/loader/models/sitesync.py</code> <pre><code>def get_active_site_icon_def(self, project_name):\n    \"\"\"Active site icon definition.\n\n    Args:\n        project_name (Union[str, None]): Name of project.\n\n    Returns:\n        Union[dict[str, Any], None]: Site icon definition.\n    \"\"\"\n\n    if not project_name or not self.is_sitesync_enabled(project_name):\n        return None\n    active_site = self.get_active_site(project_name)\n    return self._get_site_icon_def(project_name, active_site)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/models/sitesync.html#client.ayon_core.tools.loader.models.sitesync.SiteSyncModel.get_remote_site","title":"<code>get_remote_site(project_name)</code>","text":"<p>Remote site name for a project.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <p>Returns:</p> Type Description <p>Union[str, None]: Remote site name.</p> Source code in <code>client/ayon_core/tools/loader/models/sitesync.py</code> <pre><code>def get_remote_site(self, project_name):\n    \"\"\"Remote site name for a project.\n\n    Args:\n        project_name (str): Project name.\n\n    Returns:\n        Union[str, None]: Remote site name.\n    \"\"\"\n\n    cache = self._remote_site_cache[project_name]\n    if not cache.is_valid:\n        site_name = None\n        if project_name and self._is_sitesync_addon_enabled():\n            site_name = self._sitesync_addon.get_remote_site(project_name)\n        cache.update_data(site_name)\n    return cache.get_data()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/models/sitesync.html#client.ayon_core.tools.loader.models.sitesync.SiteSyncModel.get_remote_site_icon_def","title":"<code>get_remote_site_icon_def(project_name)</code>","text":"<p>Remote site icon definition.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>Union[str, None]</code> <p>Name of project.</p> required <p>Returns:</p> Type Description <p>Union[dict[str, Any], None]: Site icon definition.</p> Source code in <code>client/ayon_core/tools/loader/models/sitesync.py</code> <pre><code>def get_remote_site_icon_def(self, project_name):\n    \"\"\"Remote site icon definition.\n\n    Args:\n        project_name (Union[str, None]): Name of project.\n\n    Returns:\n        Union[dict[str, Any], None]: Site icon definition.\n    \"\"\"\n\n    if not project_name or not self.is_sitesync_enabled(project_name):\n        return None\n    remote_site = self.get_remote_site(project_name)\n    return self._get_site_icon_def(project_name, remote_site)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/models/sitesync.html#client.ayon_core.tools.loader.models.sitesync.SiteSyncModel.get_representations_sync_status","title":"<code>get_representations_sync_status(project_name, representation_ids)</code>","text":"<p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>representation_ids</code> <code>Iterable[str]</code> <p>Representation ids.</p> required <p>Returns:</p> Type Description <p>dict[str, tuple[float, float]]</p> Source code in <code>client/ayon_core/tools/loader/models/sitesync.py</code> <pre><code>def get_representations_sync_status(\n    self, project_name, representation_ids\n):\n    \"\"\"\n\n    Args:\n        project_name (str): Project name.\n        representation_ids (Iterable[str]): Representation ids.\n\n    Returns:\n        dict[str, tuple[float, float]]\n    \"\"\"\n\n    if not self.is_sitesync_enabled(project_name):\n        return {\n            repre_id: _default_repre_status()\n            for repre_id in representation_ids\n        }\n\n    output = {}\n    project_cache = self._repre_status_cache[project_name]\n    invalid_ids = set()\n    for repre_id in representation_ids:\n        repre_cache = project_cache[repre_id]\n        if repre_cache.is_valid:\n            output[repre_id] = repre_cache.get_data()\n        else:\n            invalid_ids.add(repre_id)\n\n    if invalid_ids:\n        self._refresh_representations_sync_status(\n            project_name, invalid_ids\n        )\n        for repre_id in invalid_ids:\n            repre_cache = project_cache[repre_id]\n            output[repre_id] = repre_cache.get_data()\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/models/sitesync.html#client.ayon_core.tools.loader.models.sitesync.SiteSyncModel.get_sitesync_action_items","title":"<code>get_sitesync_action_items(project_name, entity_ids, entity_type)</code>","text":"<p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>entity_ids</code> <code>set[str]</code> <p>Selected entity ids.</p> required <code>entity_type</code> <code>str</code> <p>Selected entity type.</p> required <p>Returns:</p> Type Description <p>list[ActionItem]: Actions that can be shown in loader.</p> Source code in <code>client/ayon_core/tools/loader/models/sitesync.py</code> <pre><code>def get_sitesync_action_items(\n    self, project_name, entity_ids, entity_type\n):\n    \"\"\"\n\n    Args:\n         project_name (str): Project name.\n         entity_ids (set[str]): Selected entity ids.\n         entity_type (str): Selected entity type.\n\n    Returns:\n        list[ActionItem]: Actions that can be shown in loader.\n\n    \"\"\"\n    if entity_type != \"representation\":\n        return []\n\n    if not self.is_sitesync_enabled(project_name):\n        return []\n\n    repres_status = self.get_representations_sync_status(\n        project_name, entity_ids\n    )\n\n    repre_ids_per_identifier = collections.defaultdict(set)\n    for repre_id in entity_ids:\n        repre_status = repres_status[repre_id]\n        local_status, remote_status = repre_status\n\n        if local_status:\n            repre_ids_per_identifier[UPLOAD_IDENTIFIER].add(repre_id)\n            repre_ids_per_identifier[REMOVE_IDENTIFIER].add(repre_id)\n\n        if remote_status:\n            repre_ids_per_identifier[DOWNLOAD_IDENTIFIER].add(repre_id)\n\n    action_items = []\n    for identifier, repre_ids in repre_ids_per_identifier.items():\n        if identifier == DOWNLOAD_IDENTIFIER:\n            action_items.append(self._create_download_action_item(\n                project_name, repre_ids\n            ))\n        elif identifier == UPLOAD_IDENTIFIER:\n            action_items.append(self._create_upload_action_item(\n                project_name, repre_ids\n            ))\n        elif identifier == REMOVE_IDENTIFIER:\n            action_items.append(self._create_delete_action_item(\n                project_name, repre_ids\n            ))\n\n    return action_items\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/models/sitesync.html#client.ayon_core.tools.loader.models.sitesync.SiteSyncModel.get_version_sync_availability","title":"<code>get_version_sync_availability(project_name, version_ids)</code>","text":"<p>Returns how many representations are available on sites.</p> <p>Returned value <code>{version_id: (4, 6)}</code> denotes that locally are     available 4 and remotely 6 representation. NOTE: Available means they were synced to site.</p> <p>Returns:</p> Type Description <p>dict[str, tuple[int, int]]</p> Source code in <code>client/ayon_core/tools/loader/models/sitesync.py</code> <pre><code>def get_version_sync_availability(self, project_name, version_ids):\n    \"\"\"Returns how many representations are available on sites.\n\n    Returned value `{version_id: (4, 6)}` denotes that locally are\n        available 4 and remotely 6 representation.\n    NOTE: Available means they were synced to site.\n\n    Returns:\n        dict[str, tuple[int, int]]\n    \"\"\"\n\n    if not self.is_sitesync_enabled(project_name):\n        return {\n            version_id: _default_version_availability()\n            for version_id in version_ids\n        }\n\n    output = {}\n    project_cache = self._version_availability_cache[project_name]\n    invalid_ids = set()\n    for version_id in version_ids:\n        repre_cache = project_cache[version_id]\n        if repre_cache.is_valid:\n            output[version_id] = repre_cache.get_data()\n        else:\n            invalid_ids.add(version_id)\n\n    if invalid_ids:\n        self._refresh_version_availability(\n            project_name, invalid_ids\n        )\n        for version_id in invalid_ids:\n            version_cache = project_cache[version_id]\n            output[version_id] = version_cache.get_data()\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/models/sitesync.html#client.ayon_core.tools.loader.models.sitesync.SiteSyncModel.is_sitesync_action","title":"<code>is_sitesync_action(identifier)</code>","text":"<p>Should be <code>identifier</code> handled by SiteSync.</p> <p>Parameters:</p> Name Type Description Default <code>identifier</code> <code>str</code> <p>Plugin identifier.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>Should action be handled by SiteSync.</p> Source code in <code>client/ayon_core/tools/loader/models/sitesync.py</code> <pre><code>def is_sitesync_action(self, identifier: str) -&gt; bool:\n    \"\"\"Should be `identifier` handled by SiteSync.\n\n    Args:\n        identifier (str): Plugin identifier.\n\n    Returns:\n        bool: Should action be handled by SiteSync.\n\n    \"\"\"\n    return identifier == \"sitesync.loader.action\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/models/sitesync.html#client.ayon_core.tools.loader.models.sitesync.SiteSyncModel.is_sitesync_enabled","title":"<code>is_sitesync_enabled(project_name=None)</code>","text":"<p>Site sync is enabled for a project.</p> <p>Returns false if site sync addon is not available or enabled     or project has disabled it.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>Union[str, None]</code> <p>Project name. If project name is 'None', True is returned if site sync addon is available and enabled.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>bool</code> <p>Site sync is enabled.</p> Source code in <code>client/ayon_core/tools/loader/models/sitesync.py</code> <pre><code>def is_sitesync_enabled(self, project_name=None):\n    \"\"\"Site sync is enabled for a project.\n\n    Returns false if site sync addon is not available or enabled\n        or project has disabled it.\n\n    Args:\n        project_name (Union[str, None]): Project name. If project name\n            is 'None', True is returned if site sync addon\n            is available and enabled.\n\n    Returns:\n        bool: Site sync is enabled.\n    \"\"\"\n\n    if not self._is_sitesync_addon_enabled():\n        return False\n    cache = self._sitesync_enabled_cache[project_name]\n    if not cache.is_valid:\n        enabled = True\n        if project_name:\n            enabled = self._sitesync_addon.is_project_enabled(\n                project_name, single=True\n            )\n        cache.update_data(enabled)\n    return cache.get_data()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/models/sitesync.html#client.ayon_core.tools.loader.models.sitesync.SiteSyncModel.trigger_action_item","title":"<code>trigger_action_item(project_name, data)</code>","text":"<p>Resets status for site_name or remove local files.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>data</code> <code>dict[str, Any]</code> <p>Action item data.</p> required Source code in <code>client/ayon_core/tools/loader/models/sitesync.py</code> <pre><code>def trigger_action_item(\n    self,\n    project_name: str,\n    data: dict[str, Any],\n):\n    \"\"\"Resets status for site_name or remove local files.\n\n    Args:\n        project_name (str): Project name.\n        data (dict[str, Any]): Action item data.\n\n    \"\"\"\n    representation_ids = data[\"representation_ids\"]\n    action_identifier = data[\"action_identifier\"]\n    active_site = self.get_active_site(project_name)\n    remote_site = self.get_remote_site(project_name)\n\n    repre_entities_by_id = {\n        repre_entity[\"id\"]: repre_entity\n        for repre_entity in get_representations(\n            project_name, representation_ids=representation_ids\n        )\n    }\n    # TODO get product type from product entity instead of 'context'\n    #   on representation\n    product_type_by_repre_id = {}\n    for repre_id, repre_entity in repre_entities_by_id.items():\n        repre_context = repre_entity[\"context\"]\n        product_type = repre_context.get(\"product\", {}).get(\"type\")\n        if not product_type:\n            product_type = repre_context.get(\"family\")\n\n        product_type_by_repre_id[repre_id] = product_type\n\n    for repre_id in representation_ids:\n        repre_entity = repre_entities_by_id.get(repre_id)\n        product_type = product_type_by_repre_id[repre_id]\n        if action_identifier == DOWNLOAD_IDENTIFIER:\n            self._add_site(\n                project_name, repre_entity, active_site, product_type\n            )\n\n        elif action_identifier == UPLOAD_IDENTIFIER:\n            self._add_site(\n                project_name, repre_entity, remote_site, product_type\n            )\n\n        elif action_identifier == REMOVE_IDENTIFIER:\n            self._sitesync_addon.remove_site(\n                project_name,\n                repre_id,\n                active_site,\n                remove_local_files=True\n            )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/ui/index.html","title":"ui","text":""},{"location":"autoapi/client/ayon_core/tools/loader/ui/_multicombobox.html","title":"_multicombobox","text":""},{"location":"autoapi/client/ayon_core/tools/loader/ui/_multicombobox.html#client.ayon_core.tools.loader.ui._multicombobox.CustomPaintDelegate","title":"<code>CustomPaintDelegate</code>","text":"<p>               Bases: <code>QStyledItemDelegate</code></p> <p>Delegate showing status name and short name.</p> Source code in <code>client/ayon_core/tools/loader/ui/_multicombobox.py</code> <pre><code>class CustomPaintDelegate(QtWidgets.QStyledItemDelegate):\n    \"\"\"Delegate showing status name and short name.\"\"\"\n    _empty_icon = None\n    _checked_value = checkstate_enum_to_int(QtCore.Qt.Checked)\n    _checked_bg_color = QtGui.QColor(\"#2C3B4C\")\n\n    def __init__(\n        self,\n        text_role: int,\n        short_text_role: int,\n        text_color_role: int,\n        icon_role: int,\n        item_type_role: Optional[int] = None,\n        parent=None\n    ):\n        super().__init__(parent)\n        self._text_role = text_role\n        self._text_color_role = text_color_role\n        self._short_text_role = short_text_role\n        self._icon_role = icon_role\n        self._item_type_role = item_type_role\n\n    @classmethod\n    def _get_empty_icon(cls):\n        if cls._empty_icon is None:\n            pix = QtGui.QPixmap(1, 1)\n            pix.fill(QtCore.Qt.transparent)\n            cls._empty_icon = QtGui.QIcon(pix)\n        return cls._empty_icon\n\n    def paint(self, painter, option, index):\n        item_type = None\n        if self._item_type_role is not None:\n            item_type = index.data(self._item_type_role)\n\n        if item_type is None:\n            item_type = VALUE_ITEM_TYPE\n\n        if item_type == STANDARD_ITEM_TYPE:\n            super().paint(painter, option, index)\n            return\n\n        elif item_type == SEPARATOR_ITEM_TYPE:\n            self._paint_separator(painter, option, index)\n            return\n\n        if option.widget:\n            style = option.widget.style()\n        else:\n            style = QtWidgets.QApplication.style()\n\n        self.initStyleOption(option, index)\n\n        mode = QtGui.QIcon.Normal\n        if not (option.state &amp; QtWidgets.QStyle.State_Enabled):\n            mode = QtGui.QIcon.Disabled\n        elif option.state &amp; QtWidgets.QStyle.State_Selected:\n            mode = QtGui.QIcon.Selected\n        state = QtGui.QIcon.Off\n        if option.state &amp; QtWidgets.QStyle.State_Open:\n            state = QtGui.QIcon.On\n        icon = self._get_index_icon(index)\n        if icon is None or icon.isNull():\n            icon = self._get_empty_icon()\n\n        option.features |= QtWidgets.QStyleOptionViewItem.HasDecoration\n\n        # Disable visible check indicator\n        # - checkstate is displayed by background color\n        option.features &amp;= (\n            ~QtWidgets.QStyleOptionViewItem.HasCheckIndicator\n        )\n\n        option.icon = icon\n        act_size = icon.actualSize(option.decorationSize, mode, state)\n        option.decorationSize = QtCore.QSize(\n            min(option.decorationSize.width(), act_size.width()),\n            min(option.decorationSize.height(), act_size.height())\n        )\n\n        text = self._get_index_name(index)\n        if text:\n            option.features |= QtWidgets.QStyleOptionViewItem.HasDisplay\n            option.text = text\n\n        painter.save()\n        painter.setClipRect(option.rect)\n\n        is_checked = (\n            index.data(QtCore.Qt.CheckStateRole) == self._checked_value\n        )\n        if is_checked:\n            painter.fillRect(option.rect, self._checked_bg_color)\n\n        icon_rect = style.subElementRect(\n            QtWidgets.QCommonStyle.SE_ItemViewItemDecoration,\n            option,\n            option.widget\n        )\n        text_rect = style.subElementRect(\n            QtWidgets.QCommonStyle.SE_ItemViewItemText,\n            option,\n            option.widget\n        )\n\n        # Draw background\n        style.drawPrimitive(\n            QtWidgets.QCommonStyle.PE_PanelItemViewItem,\n            option,\n            painter,\n            option.widget\n        )\n\n        # Draw icon\n        option.icon.paint(\n            painter,\n            icon_rect,\n            option.decorationAlignment,\n            mode,\n            state\n        )\n        fm = QtGui.QFontMetrics(option.font)\n        if text_rect.width() &lt; fm.width(text):\n            text = self._get_index_short_name(index)\n            if not text or text_rect.width() &lt; fm.width(text):\n                text = \"\"\n\n        fg_color = self._get_index_text_color(index)\n        pen = painter.pen()\n        pen.setColor(fg_color)\n        painter.setPen(pen)\n\n        painter.drawText(\n            text_rect,\n            option.displayAlignment,\n            text\n        )\n\n        if option.state &amp; QtWidgets.QStyle.State_HasFocus:\n            focus_opt = QtWidgets.QStyleOptionFocusRect()\n            focus_opt.state = option.state\n            focus_opt.direction = option.direction\n            focus_opt.rect = option.rect\n            focus_opt.fontMetrics = option.fontMetrics\n            focus_opt.palette = option.palette\n\n            focus_opt.rect = style.subElementRect(\n                QtWidgets.QCommonStyle.SE_ItemViewItemFocusRect,\n                option,\n                option.widget\n            )\n            focus_opt.state |= (\n                QtWidgets.QStyle.State_KeyboardFocusChange\n                | QtWidgets.QStyle.State_Item\n            )\n            focus_opt.backgroundColor = option.palette.color(\n                (\n                    QtGui.QPalette.Normal\n                    if option.state &amp; QtWidgets.QStyle.State_Enabled\n                    else QtGui.QPalette.Disabled\n                ),\n                (\n                    QtGui.QPalette.Highlight\n                    if option.state &amp; QtWidgets.QStyle.State_Selected\n                    else QtGui.QPalette.Window\n                )\n            )\n            style.drawPrimitive(\n                QtWidgets.QCommonStyle.PE_FrameFocusRect,\n                focus_opt,\n                painter,\n                option.widget\n            )\n\n        painter.restore()\n\n    def _paint_separator(self, painter, option, index):\n        painter.save()\n        painter.setClipRect(option.rect)\n\n        style = option.widget.style()\n        style.drawPrimitive(\n            QtWidgets.QCommonStyle.PE_PanelItemViewItem,\n            option,\n            painter,\n            option.widget\n        )\n\n        pen = painter.pen()\n        pen.setWidth(2)\n        painter.setPen(pen)\n        mid_y = (option.rect.top() + option.rect.bottom()) * 0.5\n        painter.drawLine(\n            QtCore.QPointF(option.rect.left(), mid_y),\n            QtCore.QPointF(option.rect.right(), mid_y)\n        )\n\n        painter.restore()\n\n    def _get_index_name(self, index):\n        return index.data(self._text_role)\n\n    def _get_index_short_name(self, index):\n        if self._short_text_role is None:\n            return None\n        return index.data(self._short_text_role)\n\n    def _get_index_text_color(self, index):\n        color = None\n        if self._text_color_role is not None:\n            color = index.data(self._text_color_role)\n        if color is not None:\n            return QtGui.QColor(color)\n        return QtGui.QColor(QtCore.Qt.white)\n\n    def _get_index_icon(self, index):\n        icon = None\n        if self._icon_role is not None:\n            icon = index.data(self._icon_role)\n        if icon is None:\n            return QtGui.QIcon()\n        return icon\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/ui/_multicombobox.html#client.ayon_core.tools.loader.ui._multicombobox.CustomPaintMultiselectComboBox","title":"<code>CustomPaintMultiselectComboBox</code>","text":"<p>               Bases: <code>QComboBox</code></p> Source code in <code>client/ayon_core/tools/loader/ui/_multicombobox.py</code> <pre><code>class CustomPaintMultiselectComboBox(QtWidgets.QComboBox):\n    value_changed = QtCore.Signal()\n    focused_in = QtCore.Signal()\n\n    ignored_keys = {\n        QtCore.Qt.Key_Up,\n        QtCore.Qt.Key_Down,\n        QtCore.Qt.Key_PageDown,\n        QtCore.Qt.Key_PageUp,\n        QtCore.Qt.Key_Home,\n        QtCore.Qt.Key_End,\n    }\n    _top_bottom_margins = 1\n    _top_bottom_padding = 2\n    _left_right_padding = 3\n    _item_bg_color = QtGui.QColor(\"#31424e\")\n\n    def __init__(\n        self,\n        text_role,\n        short_text_role,\n        text_color_role,\n        icon_role,\n        value_role=None,\n        item_type_role=None,\n        model=None,\n        placeholder=None,\n        parent=None,\n    ):\n        super().__init__(parent=parent)\n        self.setFocusPolicy(QtCore.Qt.StrongFocus)\n\n        if model is not None:\n            self.setModel(model)\n\n        combo_view = QtWidgets.QListView(self)\n\n        self.setView(combo_view)\n\n        item_delegate = CustomPaintDelegate(\n            text_role=text_role,\n            short_text_role=short_text_role,\n            text_color_role=text_color_role,\n            icon_role=icon_role,\n            item_type_role=item_type_role,\n            parent=combo_view,\n        )\n        combo_view.setItemDelegateForColumn(0, item_delegate)\n\n        if value_role is None:\n            value_role = text_role\n\n        self._combo_view = combo_view\n        self._item_delegate = item_delegate\n        self._value_role = value_role\n        self._text_role = text_role\n        self._short_text_role = short_text_role\n        self._text_color_role = text_color_role\n        self._icon_role = icon_role\n        self._item_type_role = item_type_role\n\n        self._popup_is_shown = False\n        self._block_mouse_release_timer = QtCore.QTimer(self, singleShot=True)\n        self._initial_mouse_pos = None\n        self._placeholder_text = placeholder\n\n        self._custom_text = None\n        self._all_unchecked_as_checked = True\n\n    def all_unchecked_as_checked(self) -&gt; bool:\n        return self._all_unchecked_as_checked\n\n    def set_all_unchecked_as_checked(self, value: bool):\n        \"\"\"Set if all unchecked items should be treated as checked.\n\n        Args:\n            value (bool): If True, all unchecked items will be treated\n                as checked.\n\n        \"\"\"\n        self._all_unchecked_as_checked = value\n\n    def get_placeholder_text(self) -&gt; Optional[str]:\n        return self._placeholder_text\n\n    def set_placeholder_text(self, text: Optional[str]):\n        \"\"\"Set the placeholder text.\n\n        Text shown when nothing is selected.\n\n        Args:\n            text (str | None): The placeholder text.\n\n        \"\"\"\n        if text == self._placeholder_text:\n            return\n        self._placeholder_text = text\n        self.repaint()\n\n    def set_custom_text(self, text: Optional[str]):\n        \"\"\"Set the placeholder text.\n\n        Text always shown in combobox field.\n\n        Args:\n            text (str | None): The text. Use 'None' to reset to default.\n\n        \"\"\"\n        if text == self._custom_text:\n            return\n        self._custom_text = text\n        self.repaint()\n\n    def focusInEvent(self, event):\n        self.focused_in.emit()\n        return super().focusInEvent(event)\n\n    def mousePressEvent(self, event):\n        \"\"\"Reimplemented.\"\"\"\n        self._popup_is_shown = False\n        super().mousePressEvent(event)\n        if self._popup_is_shown:\n            self._initial_mouse_pos = self.mapToGlobal(event.pos())\n            self._block_mouse_release_timer.start(\n                QtWidgets.QApplication.doubleClickInterval()\n            )\n\n    def showPopup(self):\n        \"\"\"Reimplemented.\"\"\"\n        super().showPopup()\n        view = self.view()\n        view.installEventFilter(self)\n        view.viewport().installEventFilter(self)\n        self._popup_is_shown = True\n\n    def hidePopup(self):\n        \"\"\"Reimplemented.\"\"\"\n        self.view().removeEventFilter(self)\n        self.view().viewport().removeEventFilter(self)\n        self._popup_is_shown = False\n        self._initial_mouse_pos = None\n        super().hidePopup()\n        self.view().clearFocus()\n\n    def _event_popup_shown(self, obj, event):\n        if not self._popup_is_shown:\n            return\n\n        current_index = self.view().currentIndex()\n        model = self.model()\n\n        if event.type() == QtCore.QEvent.MouseMove:\n            if (\n                self.view().isVisible()\n                and self._initial_mouse_pos is not None\n                and self._block_mouse_release_timer.isActive()\n            ):\n                diff = obj.mapToGlobal(event.pos()) - self._initial_mouse_pos\n                if diff.manhattanLength() &gt; 9:\n                    self._block_mouse_release_timer.stop()\n            return\n\n        index_flags = current_index.flags()\n        state = checkstate_int_to_enum(\n            current_index.data(QtCore.Qt.CheckStateRole)\n        )\n\n        new_state = None\n\n        if event.type() == QtCore.QEvent.MouseButtonRelease:\n            new_state = self._mouse_released_event_handle(\n                event, current_index, index_flags, state\n            )\n\n        elif event.type() == QtCore.QEvent.KeyPress:\n            new_state = self._key_press_event_handler(\n                event, current_index, index_flags, state\n            )\n\n        if new_state is not None:\n            model.setData(current_index, new_state, QtCore.Qt.CheckStateRole)\n            self.view().update(current_index)\n            self.repaint()\n            self.value_changed.emit()\n            return True\n\n    def eventFilter(self, obj, event):\n        \"\"\"Reimplemented.\"\"\"\n        result = self._event_popup_shown(obj, event)\n        if result is not None:\n            return result\n\n        return super().eventFilter(obj, event)\n\n    def addItem(self, *args, **kwargs):\n        idx = self.count()\n        super().addItem(*args, **kwargs)\n        self.model().item(idx).setCheckable(True)\n\n    def paintEvent(self, event):\n        \"\"\"Reimplemented.\"\"\"\n        painter = QtWidgets.QStylePainter(self)\n        option = QtWidgets.QStyleOptionComboBox()\n        self.initStyleOption(option)\n        painter.drawComplexControl(QtWidgets.QStyle.CC_ComboBox, option)\n\n        idxs = self._get_checked_idx()\n        # draw the icon and text\n        draw_items = False\n        combotext = None\n        if self._custom_text is not None:\n            combotext = self._custom_text\n        elif not idxs:\n            combotext = self._placeholder_text\n        else:\n            draw_items = True\n\n        content_field_rect = self.style().subControlRect(\n            QtWidgets.QStyle.CC_ComboBox,\n            option,\n            QtWidgets.QStyle.SC_ComboBoxEditField\n        ).adjusted(1, 0, -1, 0)\n\n        if draw_items:\n            self._paint_items(painter, idxs, content_field_rect)\n        else:\n            color = option.palette.color(QtGui.QPalette.Text)\n            color.setAlpha(67)\n            pen = painter.pen()\n            pen.setColor(color)\n            painter.setPen(pen)\n            painter.drawText(\n                content_field_rect,\n                QtCore.Qt.AlignLeft | QtCore.Qt.AlignVCenter,\n                combotext\n            )\n\n        painter.end()\n\n    def _paint_items(self, painter, indexes, content_rect):\n        origin_rect = QtCore.QRect(content_rect)\n\n        model = self.model()\n        available_width = content_rect.width()\n        total_used_width = 0\n\n        painter.save()\n\n        spacing = 2\n\n        for idx in indexes:\n            index = model.index(idx, 0)\n            if not index.isValid():\n                continue\n\n            icon = index.data(self._icon_role)\n            text = index.data(self._text_role)\n            valid_icon = icon is not None and not icon.isNull()\n            if valid_icon:\n                sizes = icon.availableSizes()\n                if sizes:\n                    valid_icon = any(size.width() &gt; 1 for size in sizes)\n\n            if valid_icon:\n                metrics = self.fontMetrics()\n                icon_rect = QtCore.QRect(content_rect)\n                diff = icon_rect.height() - metrics.height()\n                if diff &lt; 0:\n                    diff = 0\n                top_offset = diff // 2\n                bottom_offset = diff - top_offset\n                icon_rect.adjust(0, top_offset, 0, -bottom_offset)\n                used_width = metrics.height()\n                if total_used_width &gt; 0:\n                    total_used_width += spacing\n                total_used_width += used_width\n                if total_used_width &gt; available_width:\n                    break\n\n                icon_rect.setWidth(used_width)\n                icon.paint(\n                    painter,\n                    icon_rect,\n                    QtCore.Qt.AlignCenter,\n                    QtGui.QIcon.Normal,\n                    QtGui.QIcon.On\n                )\n                content_rect.setLeft(icon_rect.right() + spacing)\n\n            elif text:\n                bg_height = (\n                    content_rect.height()\n                    - (2 * self._top_bottom_margins)\n                )\n                font_height = bg_height - (2 * self._top_bottom_padding)\n\n                bg_top_y = content_rect.y() + self._top_bottom_margins\n\n                font = self.font()\n                font.setPixelSize(font_height)\n                metrics = QtGui.QFontMetrics(font)\n                painter.setFont(font)\n\n                label_rect = metrics.boundingRect(text)\n\n                bg_width = label_rect.width() + (2 * self._left_right_padding)\n                if total_used_width &gt; 0:\n                    total_used_width += spacing\n                total_used_width += bg_width\n                if total_used_width &gt; available_width:\n                    break\n\n                bg_rect = QtCore.QRectF(label_rect)\n                bg_rect.moveTop(bg_top_y)\n                bg_rect.moveLeft(content_rect.left())\n                bg_rect.setWidth(bg_width)\n                bg_rect.setHeight(bg_height)\n\n                label_rect.moveTop(bg_top_y)\n                label_rect.moveLeft(\n                    content_rect.left() + self._left_right_padding\n                )\n\n                path = QtGui.QPainterPath()\n                path.addRoundedRect(bg_rect, 5, 5)\n\n                painter.fillPath(path, self._item_bg_color)\n                painter.drawText(label_rect, QtCore.Qt.AlignCenter, text)\n\n                content_rect.setLeft(bg_rect.right() + spacing)\n\n        painter.restore()\n\n        if total_used_width &gt; available_width:\n            ellide_dots = chr(0x2026)\n            painter.drawText(origin_rect, QtCore.Qt.AlignRight, ellide_dots)\n\n    def setItemCheckState(self, index, state):\n        self.setItemData(index, state, QtCore.Qt.CheckStateRole)\n\n    def set_value(\n        self,\n        values: Optional[Iterable[Any]],\n        role: Optional[int] = None,\n    ):\n        if role is None:\n            role = self._value_role\n\n        for idx in range(self.count()):\n            value = self.itemData(idx, role=role)\n            check_state = CHECKED_INT\n            if values is None or value not in values:\n                check_state = UNCHECKED_INT\n            self.setItemData(idx, check_state, QtCore.Qt.CheckStateRole)\n        self.repaint()\n\n    def get_value_info(\n        self,\n        role: Optional[int] = None,\n        propagate_all_unchecked_as_checked: bool = None\n    ) -&gt; List[Tuple[Any, bool]]:\n        \"\"\"Get the values and their checked state.\n\n        Args:\n            role (int | None): The role to get the values from.\n                If None, the default value role is used.\n            propagate_all_unchecked_as_checked (bool | None): If True,\n                all unchecked items will be treated as checked.\n                If None, the current value of\n                'propagate_all_unchecked_as_checked' is used.\n\n        Returns:\n            List[Tuple[Any, bool]]: The values and their checked state.\n\n        \"\"\"\n        if role is None:\n            role = self._value_role\n\n        if propagate_all_unchecked_as_checked is None:\n            propagate_all_unchecked_as_checked = (\n                self._all_unchecked_as_checked\n            )\n\n        items = []\n        all_unchecked = True\n        for idx in range(self.count()):\n            item_type = self.itemData(idx, role=self._item_type_role)\n            if item_type is not None and item_type != VALUE_ITEM_TYPE:\n                continue\n\n            state = checkstate_int_to_enum(\n                self.itemData(idx, role=QtCore.Qt.CheckStateRole)\n            )\n            checked = state == QtCore.Qt.Checked\n            if checked:\n                all_unchecked = False\n            items.append(\n                (self.itemData(idx, role=role), checked)\n            )\n\n        if propagate_all_unchecked_as_checked and all_unchecked:\n            items = [\n                (value, True)\n                for value, checked in items\n            ]\n        return items\n\n    def get_value(self, role=None):\n        if role is None:\n            role = self._value_role\n\n        return [\n            value\n            for value, checked in self.get_value_info(role)\n            if checked\n        ]\n\n    def wheelEvent(self, event):\n        event.ignore()\n\n    def keyPressEvent(self, event):\n        if (\n            event.key() == QtCore.Qt.Key_Down\n            and event.modifiers() &amp; QtCore.Qt.AltModifier\n        ):\n            return self.showPopup()\n\n        if event.key() in self.ignored_keys:\n            return event.ignore()\n\n        return super().keyPressEvent(event)\n\n    def _get_checked_idx(self) -&gt; List[int]:\n        checked_indexes = []\n        for idx in range(self.count()):\n            item_type = self.itemData(idx, role=self._item_type_role)\n            if item_type is not None and item_type != VALUE_ITEM_TYPE:\n                continue\n\n            state = checkstate_int_to_enum(\n                self.itemData(idx, role=QtCore.Qt.CheckStateRole)\n            )\n            if state == QtCore.Qt.Checked:\n                checked_indexes.append(idx)\n        return checked_indexes\n\n    def _mouse_released_event_handle(\n        self, event, current_index, index_flags, state\n    ):\n        if (\n            self._block_mouse_release_timer.isActive()\n            or not current_index.isValid()\n            or not self.view().isVisible()\n            or not self.view().rect().contains(event.pos())\n            or not index_flags &amp; QtCore.Qt.ItemIsSelectable\n            or not index_flags &amp; QtCore.Qt.ItemIsEnabled\n            or not index_flags &amp; QtCore.Qt.ItemIsUserCheckable\n        ):\n            return None\n\n        if state == QtCore.Qt.Checked:\n            return UNCHECKED_INT\n        return CHECKED_INT\n\n    def _key_press_event_handler(\n        self, event, current_index, index_flags, state\n    ):\n        # TODO: handle QtCore.Qt.Key_Enter, Key_Return?\n        if event.key() != QtCore.Qt.Key_Space:\n            return None\n\n        if (\n            index_flags &amp; QtCore.Qt.ItemIsUserCheckable\n            and index_flags &amp; ITEM_IS_USER_TRISTATE\n        ):\n            return (checkstate_enum_to_int(state) + 1) % 3\n\n        if index_flags &amp; QtCore.Qt.ItemIsUserCheckable:\n            # toggle the current items check state\n            if state != QtCore.Qt.Checked:\n                return CHECKED_INT\n            return UNCHECKED_INT\n        return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/ui/_multicombobox.html#client.ayon_core.tools.loader.ui._multicombobox.CustomPaintMultiselectComboBox.eventFilter","title":"<code>eventFilter(obj, event)</code>","text":"<p>Reimplemented.</p> Source code in <code>client/ayon_core/tools/loader/ui/_multicombobox.py</code> <pre><code>def eventFilter(self, obj, event):\n    \"\"\"Reimplemented.\"\"\"\n    result = self._event_popup_shown(obj, event)\n    if result is not None:\n        return result\n\n    return super().eventFilter(obj, event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/ui/_multicombobox.html#client.ayon_core.tools.loader.ui._multicombobox.CustomPaintMultiselectComboBox.get_value_info","title":"<code>get_value_info(role=None, propagate_all_unchecked_as_checked=None)</code>","text":"<p>Get the values and their checked state.</p> <p>Parameters:</p> Name Type Description Default <code>role</code> <code>int | None</code> <p>The role to get the values from. If None, the default value role is used.</p> <code>None</code> <code>propagate_all_unchecked_as_checked</code> <code>bool | None</code> <p>If True, all unchecked items will be treated as checked. If None, the current value of 'propagate_all_unchecked_as_checked' is used.</p> <code>None</code> <p>Returns:</p> Type Description <code>List[Tuple[Any, bool]]</code> <p>List[Tuple[Any, bool]]: The values and their checked state.</p> Source code in <code>client/ayon_core/tools/loader/ui/_multicombobox.py</code> <pre><code>def get_value_info(\n    self,\n    role: Optional[int] = None,\n    propagate_all_unchecked_as_checked: bool = None\n) -&gt; List[Tuple[Any, bool]]:\n    \"\"\"Get the values and their checked state.\n\n    Args:\n        role (int | None): The role to get the values from.\n            If None, the default value role is used.\n        propagate_all_unchecked_as_checked (bool | None): If True,\n            all unchecked items will be treated as checked.\n            If None, the current value of\n            'propagate_all_unchecked_as_checked' is used.\n\n    Returns:\n        List[Tuple[Any, bool]]: The values and their checked state.\n\n    \"\"\"\n    if role is None:\n        role = self._value_role\n\n    if propagate_all_unchecked_as_checked is None:\n        propagate_all_unchecked_as_checked = (\n            self._all_unchecked_as_checked\n        )\n\n    items = []\n    all_unchecked = True\n    for idx in range(self.count()):\n        item_type = self.itemData(idx, role=self._item_type_role)\n        if item_type is not None and item_type != VALUE_ITEM_TYPE:\n            continue\n\n        state = checkstate_int_to_enum(\n            self.itemData(idx, role=QtCore.Qt.CheckStateRole)\n        )\n        checked = state == QtCore.Qt.Checked\n        if checked:\n            all_unchecked = False\n        items.append(\n            (self.itemData(idx, role=role), checked)\n        )\n\n    if propagate_all_unchecked_as_checked and all_unchecked:\n        items = [\n            (value, True)\n            for value, checked in items\n        ]\n    return items\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/ui/_multicombobox.html#client.ayon_core.tools.loader.ui._multicombobox.CustomPaintMultiselectComboBox.hidePopup","title":"<code>hidePopup()</code>","text":"<p>Reimplemented.</p> Source code in <code>client/ayon_core/tools/loader/ui/_multicombobox.py</code> <pre><code>def hidePopup(self):\n    \"\"\"Reimplemented.\"\"\"\n    self.view().removeEventFilter(self)\n    self.view().viewport().removeEventFilter(self)\n    self._popup_is_shown = False\n    self._initial_mouse_pos = None\n    super().hidePopup()\n    self.view().clearFocus()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/ui/_multicombobox.html#client.ayon_core.tools.loader.ui._multicombobox.CustomPaintMultiselectComboBox.mousePressEvent","title":"<code>mousePressEvent(event)</code>","text":"<p>Reimplemented.</p> Source code in <code>client/ayon_core/tools/loader/ui/_multicombobox.py</code> <pre><code>def mousePressEvent(self, event):\n    \"\"\"Reimplemented.\"\"\"\n    self._popup_is_shown = False\n    super().mousePressEvent(event)\n    if self._popup_is_shown:\n        self._initial_mouse_pos = self.mapToGlobal(event.pos())\n        self._block_mouse_release_timer.start(\n            QtWidgets.QApplication.doubleClickInterval()\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/ui/_multicombobox.html#client.ayon_core.tools.loader.ui._multicombobox.CustomPaintMultiselectComboBox.paintEvent","title":"<code>paintEvent(event)</code>","text":"<p>Reimplemented.</p> Source code in <code>client/ayon_core/tools/loader/ui/_multicombobox.py</code> <pre><code>def paintEvent(self, event):\n    \"\"\"Reimplemented.\"\"\"\n    painter = QtWidgets.QStylePainter(self)\n    option = QtWidgets.QStyleOptionComboBox()\n    self.initStyleOption(option)\n    painter.drawComplexControl(QtWidgets.QStyle.CC_ComboBox, option)\n\n    idxs = self._get_checked_idx()\n    # draw the icon and text\n    draw_items = False\n    combotext = None\n    if self._custom_text is not None:\n        combotext = self._custom_text\n    elif not idxs:\n        combotext = self._placeholder_text\n    else:\n        draw_items = True\n\n    content_field_rect = self.style().subControlRect(\n        QtWidgets.QStyle.CC_ComboBox,\n        option,\n        QtWidgets.QStyle.SC_ComboBoxEditField\n    ).adjusted(1, 0, -1, 0)\n\n    if draw_items:\n        self._paint_items(painter, idxs, content_field_rect)\n    else:\n        color = option.palette.color(QtGui.QPalette.Text)\n        color.setAlpha(67)\n        pen = painter.pen()\n        pen.setColor(color)\n        painter.setPen(pen)\n        painter.drawText(\n            content_field_rect,\n            QtCore.Qt.AlignLeft | QtCore.Qt.AlignVCenter,\n            combotext\n        )\n\n    painter.end()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/ui/_multicombobox.html#client.ayon_core.tools.loader.ui._multicombobox.CustomPaintMultiselectComboBox.set_all_unchecked_as_checked","title":"<code>set_all_unchecked_as_checked(value)</code>","text":"<p>Set if all unchecked items should be treated as checked.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>bool</code> <p>If True, all unchecked items will be treated as checked.</p> required Source code in <code>client/ayon_core/tools/loader/ui/_multicombobox.py</code> <pre><code>def set_all_unchecked_as_checked(self, value: bool):\n    \"\"\"Set if all unchecked items should be treated as checked.\n\n    Args:\n        value (bool): If True, all unchecked items will be treated\n            as checked.\n\n    \"\"\"\n    self._all_unchecked_as_checked = value\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/ui/_multicombobox.html#client.ayon_core.tools.loader.ui._multicombobox.CustomPaintMultiselectComboBox.set_custom_text","title":"<code>set_custom_text(text)</code>","text":"<p>Set the placeholder text.</p> <p>Text always shown in combobox field.</p> <p>Parameters:</p> Name Type Description Default <code>text</code> <code>str | None</code> <p>The text. Use 'None' to reset to default.</p> required Source code in <code>client/ayon_core/tools/loader/ui/_multicombobox.py</code> <pre><code>def set_custom_text(self, text: Optional[str]):\n    \"\"\"Set the placeholder text.\n\n    Text always shown in combobox field.\n\n    Args:\n        text (str | None): The text. Use 'None' to reset to default.\n\n    \"\"\"\n    if text == self._custom_text:\n        return\n    self._custom_text = text\n    self.repaint()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/ui/_multicombobox.html#client.ayon_core.tools.loader.ui._multicombobox.CustomPaintMultiselectComboBox.set_placeholder_text","title":"<code>set_placeholder_text(text)</code>","text":"<p>Set the placeholder text.</p> <p>Text shown when nothing is selected.</p> <p>Parameters:</p> Name Type Description Default <code>text</code> <code>str | None</code> <p>The placeholder text.</p> required Source code in <code>client/ayon_core/tools/loader/ui/_multicombobox.py</code> <pre><code>def set_placeholder_text(self, text: Optional[str]):\n    \"\"\"Set the placeholder text.\n\n    Text shown when nothing is selected.\n\n    Args:\n        text (str | None): The placeholder text.\n\n    \"\"\"\n    if text == self._placeholder_text:\n        return\n    self._placeholder_text = text\n    self.repaint()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/ui/_multicombobox.html#client.ayon_core.tools.loader.ui._multicombobox.CustomPaintMultiselectComboBox.showPopup","title":"<code>showPopup()</code>","text":"<p>Reimplemented.</p> Source code in <code>client/ayon_core/tools/loader/ui/_multicombobox.py</code> <pre><code>def showPopup(self):\n    \"\"\"Reimplemented.\"\"\"\n    super().showPopup()\n    view = self.view()\n    view.installEventFilter(self)\n    view.viewport().installEventFilter(self)\n    self._popup_is_shown = True\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/ui/actions_utils.html","title":"actions_utils","text":""},{"location":"autoapi/client/ayon_core/tools/loader/ui/folders_widget.html","title":"folders_widget","text":""},{"location":"autoapi/client/ayon_core/tools/loader/ui/folders_widget.html#client.ayon_core.tools.loader.ui.folders_widget.LoaderFoldersWidget","title":"<code>LoaderFoldersWidget</code>","text":"<p>               Bases: <code>QWidget</code></p> <p>Folders widget.</p> <p>Widget that handles folders view, model and selection.</p> <p>Expected selection handling is disabled by default. If enabled, the widget will handle the expected in predefined way. Widget is listening to event 'expected_selection_changed' with expected event data below, the same data must be available when called method 'get_expected_selection_data' on controller.</p> <p>{     \"folder\": {         \"current\": bool,               # Folder is what should be set now         \"folder_id\": Union[str, None], # Folder id that should be selected     },     ... }</p> <p>Selection is confirmed by calling method 'expected_folder_selected' on controller.</p> <p>Parameters:</p> Name Type Description Default <code>controller</code> <code>AbstractWorkfilesFrontend</code> <p>The control object.</p> required <code>parent</code> <code>QWidget</code> <p>The parent widget.</p> required Source code in <code>client/ayon_core/tools/loader/ui/folders_widget.py</code> <pre><code>class LoaderFoldersWidget(QtWidgets.QWidget):\n    \"\"\"Folders widget.\n\n    Widget that handles folders view, model and selection.\n\n    Expected selection handling is disabled by default. If enabled, the\n    widget will handle the expected in predefined way. Widget is listening\n    to event 'expected_selection_changed' with expected event data below,\n    the same data must be available when called method\n    'get_expected_selection_data' on controller.\n\n    {\n        \"folder\": {\n            \"current\": bool,               # Folder is what should be set now\n            \"folder_id\": Union[str, None], # Folder id that should be selected\n        },\n        ...\n    }\n\n    Selection is confirmed by calling method 'expected_folder_selected' on\n    controller.\n\n\n    Args:\n        controller (AbstractWorkfilesFrontend): The control object.\n        parent (QtWidgets.QWidget): The parent widget.\n    \"\"\"\n\n    refreshed = QtCore.Signal()\n\n    def __init__(self, controller, parent):\n        super(LoaderFoldersWidget, self).__init__(parent)\n\n        folders_view = DeselectableTreeView(self)\n        folders_view.setHeaderHidden(True)\n        folders_view.setSelectionMode(\n            QtWidgets.QAbstractItemView.ExtendedSelection)\n\n        folders_model = LoaderFoldersModel(controller)\n        folders_proxy_model = FoldersProxyModel()\n        folders_proxy_model.setSourceModel(folders_model)\n        folders_proxy_model.setSortCaseSensitivity(QtCore.Qt.CaseInsensitive)\n\n        folders_label_delegate = UnderlinesFolderDelegate(folders_view)\n\n        folders_view.setModel(folders_proxy_model)\n        folders_view.setItemDelegate(folders_label_delegate)\n\n        main_layout = QtWidgets.QHBoxLayout(self)\n        main_layout.setContentsMargins(0, 0, 0, 0)\n        main_layout.addWidget(folders_view, 1)\n\n        controller.register_event_callback(\n            \"selection.project.changed\",\n            self._on_project_selection_change,\n        )\n        controller.register_event_callback(\n            \"folders.refresh.finished\",\n            self._on_folders_refresh_finished\n        )\n        controller.register_event_callback(\n            \"controller.refresh.finished\",\n            self._on_controller_refresh\n        )\n        controller.register_event_callback(\n            \"expected_selection_changed\",\n            self._on_expected_selection_change\n        )\n\n        selection_model = folders_view.selectionModel()\n        selection_model.selectionChanged.connect(self._on_selection_change)\n\n        folders_model.refreshed.connect(self._on_model_refresh)\n\n        self._controller = controller\n        self._folders_view = folders_view\n        self._folders_model = folders_model\n        self._folders_proxy_model = folders_proxy_model\n        self._folders_label_delegate = folders_label_delegate\n\n        self._expected_selection = None\n\n    def set_name_filter(self, name):\n        \"\"\"Set filter of folder name.\n\n        Args:\n            name (str): The string filter.\n        \"\"\"\n\n        self._folders_proxy_model.setFilterFixedString(name)\n        if name:\n            self._folders_view.expandAll()\n\n    def set_folder_ids_filter(self, folder_ids: Optional[list[str]]):\n        \"\"\"Set filter of folder ids.\n\n        Args:\n            folder_ids (list[str]): The list of folder ids.\n\n        \"\"\"\n        self._folders_proxy_model.set_folder_ids_filter(folder_ids)\n\n    def set_merged_products_selection(self, items):\n        \"\"\"\n\n        Args:\n            items (list[dict[str, Any]]): List of merged items with folder\n                ids.\n        \"\"\"\n\n        self._folders_model.set_merged_products_selection(items)\n\n    def refresh(self):\n        self._folders_model.refresh()\n\n    def _on_project_selection_change(self, event):\n        project_name = event[\"project_name\"]\n        self._set_project_name(project_name)\n\n    def _set_project_name(self, project_name):\n        self._folders_model.set_project_name(project_name)\n\n    def _clear(self):\n        self._folders_model.clear()\n\n    def _on_folders_refresh_finished(self, event):\n        if event[\"sender\"] != FOLDERS_MODEL_SENDER_NAME:\n            self._set_project_name(event[\"project_name\"])\n\n    def _on_controller_refresh(self):\n        self._update_expected_selection()\n\n    def _on_model_refresh(self):\n        if self._expected_selection:\n            self._set_expected_selection()\n        self._folders_proxy_model.sort(0)\n        self.refreshed.emit()\n\n    def _get_selected_item_ids(self):\n        selection_model = self._folders_view.selectionModel()\n        item_ids = []\n        for index in selection_model.selectedIndexes():\n            item_id = index.data(FOLDER_ID_ROLE)\n            if item_id is not None:\n                item_ids.append(item_id)\n        return item_ids\n\n    def _on_selection_change(self):\n        item_ids = self._get_selected_item_ids()\n        self._controller.set_selected_folders(item_ids)\n\n    # Expected selection handling\n    def _on_expected_selection_change(self, event):\n        self._update_expected_selection(event.data)\n\n    def _update_expected_selection(self, expected_data=None):\n        if expected_data is None:\n            expected_data = self._controller.get_expected_selection_data()\n\n        folder_data = expected_data.get(\"folder\")\n        if not folder_data or not folder_data[\"current\"]:\n            return\n\n        folder_id = folder_data[\"id\"]\n        self._expected_selection = folder_id\n        if not self._folders_model.is_refreshing:\n            self._set_expected_selection()\n\n    def _set_expected_selection(self):\n        folder_id = self._expected_selection\n        selected_ids = self._get_selected_item_ids()\n        self._expected_selection = None\n        skip_selection = (\n            folder_id is None\n            or (\n                folder_id in selected_ids\n                and len(selected_ids) == 1\n            )\n        )\n        if not skip_selection:\n            index = self._folders_model.get_index_by_id(folder_id)\n            if index.isValid():\n                proxy_index = self._folders_proxy_model.mapFromSource(index)\n                self._folders_view.setCurrentIndex(proxy_index)\n        self._controller.expected_folder_selected(folder_id)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/ui/folders_widget.html#client.ayon_core.tools.loader.ui.folders_widget.LoaderFoldersWidget.set_folder_ids_filter","title":"<code>set_folder_ids_filter(folder_ids)</code>","text":"<p>Set filter of folder ids.</p> <p>Parameters:</p> Name Type Description Default <code>folder_ids</code> <code>list[str]</code> <p>The list of folder ids.</p> required Source code in <code>client/ayon_core/tools/loader/ui/folders_widget.py</code> <pre><code>def set_folder_ids_filter(self, folder_ids: Optional[list[str]]):\n    \"\"\"Set filter of folder ids.\n\n    Args:\n        folder_ids (list[str]): The list of folder ids.\n\n    \"\"\"\n    self._folders_proxy_model.set_folder_ids_filter(folder_ids)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/ui/folders_widget.html#client.ayon_core.tools.loader.ui.folders_widget.LoaderFoldersWidget.set_merged_products_selection","title":"<code>set_merged_products_selection(items)</code>","text":"<p>Parameters:</p> Name Type Description Default <code>items</code> <code>list[dict[str, Any]]</code> <p>List of merged items with folder ids.</p> required Source code in <code>client/ayon_core/tools/loader/ui/folders_widget.py</code> <pre><code>def set_merged_products_selection(self, items):\n    \"\"\"\n\n    Args:\n        items (list[dict[str, Any]]): List of merged items with folder\n            ids.\n    \"\"\"\n\n    self._folders_model.set_merged_products_selection(items)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/ui/folders_widget.html#client.ayon_core.tools.loader.ui.folders_widget.LoaderFoldersWidget.set_name_filter","title":"<code>set_name_filter(name)</code>","text":"<p>Set filter of folder name.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The string filter.</p> required Source code in <code>client/ayon_core/tools/loader/ui/folders_widget.py</code> <pre><code>def set_name_filter(self, name):\n    \"\"\"Set filter of folder name.\n\n    Args:\n        name (str): The string filter.\n    \"\"\"\n\n    self._folders_proxy_model.setFilterFixedString(name)\n    if name:\n        self._folders_view.expandAll()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/ui/folders_widget.html#client.ayon_core.tools.loader.ui.folders_widget.UnderlinesFolderDelegate","title":"<code>UnderlinesFolderDelegate</code>","text":"<p>               Bases: <code>QItemDelegate</code></p> <p>Item delegate drawing bars under folder label.</p> <p>This is used in loader tool. Multiselection of folders may group products by name under colored groups. Selected color groups are then propagated back to selected folders as underlines.</p> Source code in <code>client/ayon_core/tools/loader/ui/folders_widget.py</code> <pre><code>class UnderlinesFolderDelegate(QtWidgets.QItemDelegate):\n    \"\"\"Item delegate drawing bars under folder label.\n\n    This is used in loader tool. Multiselection of folders\n    may group products by name under colored groups. Selected color groups are\n    then propagated back to selected folders as underlines.\n    \"\"\"\n    bar_height = 3\n\n    def __init__(self, *args, **kwargs):\n        super(UnderlinesFolderDelegate, self).__init__(*args, **kwargs)\n        colors = get_objected_colors(\"loader\", \"asset-view\")\n        self._selected_color = colors[\"selected\"].get_qcolor()\n        self._hover_color = colors[\"hover\"].get_qcolor()\n        self._selected_hover_color = colors[\"selected-hover\"].get_qcolor()\n\n    def sizeHint(self, option, index):\n        \"\"\"Add bar height to size hint.\"\"\"\n        result = super(UnderlinesFolderDelegate, self).sizeHint(option, index)\n        height = result.height()\n        result.setHeight(height + self.bar_height)\n\n        return result\n\n    def paint(self, painter, option, index):\n        \"\"\"Replicate painting of an item and draw color bars if needed.\"\"\"\n\n        painter.save()\n\n        item_rect = QtCore.QRect(option.rect)\n        item_rect.setHeight(option.rect.height() - self.bar_height)\n\n        product_colors = index.data(UNDERLINE_COLORS_ROLE) or []\n\n        product_colors_width = 0\n        if product_colors:\n            product_colors_width = option.rect.width() / len(product_colors)\n\n        product_rects = []\n        counter = 0\n        for product_c in product_colors:\n            new_color = None\n            new_rect = None\n            if product_c:\n                new_color = QtGui.QColor(product_c)\n\n                new_rect = QtCore.QRect(\n                    option.rect.left() + (counter * product_colors_width),\n                    option.rect.top() + (\n                        option.rect.height() - self.bar_height\n                    ),\n                    product_colors_width,\n                    self.bar_height\n                )\n            product_rects.append((new_color, new_rect))\n            counter += 1\n\n        # Background\n        if option.state &amp; QtWidgets.QStyle.State_Selected:\n            if len(product_colors) == 0:\n                item_rect.setTop(item_rect.top() + (self.bar_height / 2))\n\n            if option.state &amp; QtWidgets.QStyle.State_MouseOver:\n                bg_color = self._selected_hover_color\n            else:\n                bg_color = self._selected_color\n        else:\n            item_rect.setTop(item_rect.top() + (self.bar_height / 2))\n            if option.state &amp; QtWidgets.QStyle.State_MouseOver:\n                bg_color = self._hover_color\n            else:\n                bg_color = QtGui.QColor()\n                bg_color.setAlpha(0)\n\n        # When not needed to do a rounded corners (easier and without\n        #   painter restore):\n        painter.fillRect(\n            option.rect,\n            QtGui.QBrush(bg_color)\n        )\n\n        if option.state &amp; QtWidgets.QStyle.State_Selected:\n            for color, product_rect in product_rects:\n                if not color or not product_rect:\n                    continue\n                painter.fillRect(product_rect, QtGui.QBrush(color))\n\n        # Icon\n        icon_index = index.model().index(\n            index.row(), index.column(), index.parent()\n        )\n        # - Default icon_rect if not icon\n        icon_rect = QtCore.QRect(\n            item_rect.left(),\n            item_rect.top(),\n            # To make sure it's same size all the time\n            option.rect.height() - self.bar_height,\n            option.rect.height() - self.bar_height\n        )\n        icon = index.model().data(icon_index, QtCore.Qt.DecorationRole)\n\n        if icon:\n            mode = QtGui.QIcon.Normal\n            if not (option.state &amp; QtWidgets.QStyle.State_Enabled):\n                mode = QtGui.QIcon.Disabled\n            elif option.state &amp; QtWidgets.QStyle.State_Selected:\n                mode = QtGui.QIcon.Selected\n\n            if isinstance(icon, QtGui.QPixmap):\n                icon = QtGui.QIcon(icon)\n                option.decorationSize = icon.size() / icon.devicePixelRatio()\n\n            elif isinstance(icon, QtGui.QColor):\n                pixmap = QtGui.QPixmap(option.decorationSize)\n                pixmap.fill(icon)\n                icon = QtGui.QIcon(pixmap)\n\n            elif isinstance(icon, QtGui.QImage):\n                icon = QtGui.QIcon(QtGui.QPixmap.fromImage(icon))\n                option.decorationSize = icon.size() / icon.devicePixelRatio()\n\n            elif isinstance(icon, QtGui.QIcon):\n                state = QtGui.QIcon.Off\n                if option.state &amp; QtWidgets.QStyle.State_Open:\n                    state = QtGui.QIcon.On\n                actual_size = option.icon.actualSize(\n                    option.decorationSize, mode, state\n                )\n                option.decorationSize = QtCore.QSize(\n                    min(option.decorationSize.width(), actual_size.width()),\n                    min(option.decorationSize.height(), actual_size.height())\n                )\n\n            state = QtGui.QIcon.Off\n            if option.state &amp; QtWidgets.QStyle.State_Open:\n                state = QtGui.QIcon.On\n\n            icon.paint(\n                painter, icon_rect,\n                QtCore.Qt.AlignLeft, mode, state\n            )\n\n        # Text\n        text_rect = QtCore.QRect(\n            icon_rect.left() + icon_rect.width() + 2,\n            item_rect.top(),\n            item_rect.width(),\n            item_rect.height()\n        )\n\n        painter.drawText(\n            text_rect, QtCore.Qt.AlignVCenter,\n            index.data(QtCore.Qt.DisplayRole)\n        )\n\n        painter.restore()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/ui/folders_widget.html#client.ayon_core.tools.loader.ui.folders_widget.UnderlinesFolderDelegate.paint","title":"<code>paint(painter, option, index)</code>","text":"<p>Replicate painting of an item and draw color bars if needed.</p> Source code in <code>client/ayon_core/tools/loader/ui/folders_widget.py</code> <pre><code>def paint(self, painter, option, index):\n    \"\"\"Replicate painting of an item and draw color bars if needed.\"\"\"\n\n    painter.save()\n\n    item_rect = QtCore.QRect(option.rect)\n    item_rect.setHeight(option.rect.height() - self.bar_height)\n\n    product_colors = index.data(UNDERLINE_COLORS_ROLE) or []\n\n    product_colors_width = 0\n    if product_colors:\n        product_colors_width = option.rect.width() / len(product_colors)\n\n    product_rects = []\n    counter = 0\n    for product_c in product_colors:\n        new_color = None\n        new_rect = None\n        if product_c:\n            new_color = QtGui.QColor(product_c)\n\n            new_rect = QtCore.QRect(\n                option.rect.left() + (counter * product_colors_width),\n                option.rect.top() + (\n                    option.rect.height() - self.bar_height\n                ),\n                product_colors_width,\n                self.bar_height\n            )\n        product_rects.append((new_color, new_rect))\n        counter += 1\n\n    # Background\n    if option.state &amp; QtWidgets.QStyle.State_Selected:\n        if len(product_colors) == 0:\n            item_rect.setTop(item_rect.top() + (self.bar_height / 2))\n\n        if option.state &amp; QtWidgets.QStyle.State_MouseOver:\n            bg_color = self._selected_hover_color\n        else:\n            bg_color = self._selected_color\n    else:\n        item_rect.setTop(item_rect.top() + (self.bar_height / 2))\n        if option.state &amp; QtWidgets.QStyle.State_MouseOver:\n            bg_color = self._hover_color\n        else:\n            bg_color = QtGui.QColor()\n            bg_color.setAlpha(0)\n\n    # When not needed to do a rounded corners (easier and without\n    #   painter restore):\n    painter.fillRect(\n        option.rect,\n        QtGui.QBrush(bg_color)\n    )\n\n    if option.state &amp; QtWidgets.QStyle.State_Selected:\n        for color, product_rect in product_rects:\n            if not color or not product_rect:\n                continue\n            painter.fillRect(product_rect, QtGui.QBrush(color))\n\n    # Icon\n    icon_index = index.model().index(\n        index.row(), index.column(), index.parent()\n    )\n    # - Default icon_rect if not icon\n    icon_rect = QtCore.QRect(\n        item_rect.left(),\n        item_rect.top(),\n        # To make sure it's same size all the time\n        option.rect.height() - self.bar_height,\n        option.rect.height() - self.bar_height\n    )\n    icon = index.model().data(icon_index, QtCore.Qt.DecorationRole)\n\n    if icon:\n        mode = QtGui.QIcon.Normal\n        if not (option.state &amp; QtWidgets.QStyle.State_Enabled):\n            mode = QtGui.QIcon.Disabled\n        elif option.state &amp; QtWidgets.QStyle.State_Selected:\n            mode = QtGui.QIcon.Selected\n\n        if isinstance(icon, QtGui.QPixmap):\n            icon = QtGui.QIcon(icon)\n            option.decorationSize = icon.size() / icon.devicePixelRatio()\n\n        elif isinstance(icon, QtGui.QColor):\n            pixmap = QtGui.QPixmap(option.decorationSize)\n            pixmap.fill(icon)\n            icon = QtGui.QIcon(pixmap)\n\n        elif isinstance(icon, QtGui.QImage):\n            icon = QtGui.QIcon(QtGui.QPixmap.fromImage(icon))\n            option.decorationSize = icon.size() / icon.devicePixelRatio()\n\n        elif isinstance(icon, QtGui.QIcon):\n            state = QtGui.QIcon.Off\n            if option.state &amp; QtWidgets.QStyle.State_Open:\n                state = QtGui.QIcon.On\n            actual_size = option.icon.actualSize(\n                option.decorationSize, mode, state\n            )\n            option.decorationSize = QtCore.QSize(\n                min(option.decorationSize.width(), actual_size.width()),\n                min(option.decorationSize.height(), actual_size.height())\n            )\n\n        state = QtGui.QIcon.Off\n        if option.state &amp; QtWidgets.QStyle.State_Open:\n            state = QtGui.QIcon.On\n\n        icon.paint(\n            painter, icon_rect,\n            QtCore.Qt.AlignLeft, mode, state\n        )\n\n    # Text\n    text_rect = QtCore.QRect(\n        icon_rect.left() + icon_rect.width() + 2,\n        item_rect.top(),\n        item_rect.width(),\n        item_rect.height()\n    )\n\n    painter.drawText(\n        text_rect, QtCore.Qt.AlignVCenter,\n        index.data(QtCore.Qt.DisplayRole)\n    )\n\n    painter.restore()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/ui/folders_widget.html#client.ayon_core.tools.loader.ui.folders_widget.UnderlinesFolderDelegate.sizeHint","title":"<code>sizeHint(option, index)</code>","text":"<p>Add bar height to size hint.</p> Source code in <code>client/ayon_core/tools/loader/ui/folders_widget.py</code> <pre><code>def sizeHint(self, option, index):\n    \"\"\"Add bar height to size hint.\"\"\"\n    result = super(UnderlinesFolderDelegate, self).sizeHint(option, index)\n    height = result.height()\n    result.setHeight(height + self.bar_height)\n\n    return result\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/ui/info_widget.html","title":"info_widget","text":""},{"location":"autoapi/client/ayon_core/tools/loader/ui/info_widget.html#client.ayon_core.tools.loader.ui.info_widget.InfoWidget","title":"<code>InfoWidget</code>","text":"<p>               Bases: <code>QWidget</code></p> <p>A Widget that display information about a specific version</p> Source code in <code>client/ayon_core/tools/loader/ui/info_widget.py</code> <pre><code>class InfoWidget(QtWidgets.QWidget):\n    \"\"\"A Widget that display information about a specific version\"\"\"\n    def __init__(self, controller, parent):\n        super(InfoWidget, self).__init__(parent=parent)\n\n        label_widget = QtWidgets.QLabel(\"Version Info\", self)\n        info_text_widget = VersionTextEdit(controller, self)\n        info_text_widget.setReadOnly(True)\n\n        layout = QtWidgets.QVBoxLayout(self)\n        layout.setContentsMargins(0, 0, 0, 0)\n        layout.addWidget(label_widget, 0)\n        layout.addWidget(info_text_widget, 1)\n\n        self._controller = controller\n\n        self._info_text_widget = info_text_widget\n        self._label_widget = label_widget\n\n    def set_selected_version_info(self, project_name, items):\n        if not items or not project_name:\n            self._info_text_widget.set_current_item()\n            return\n        first_item = next(iter(items))\n        product_item = self._controller.get_product_item(\n            project_name,\n            first_item[\"product_id\"],\n        )\n        version_id = first_item[\"version_id\"]\n        version_item = None\n        if product_item is not None:\n            version_item = product_item.version_items.get(version_id)\n\n        self._info_text_widget.set_current_item(product_item, version_item)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/ui/info_widget.html#client.ayon_core.tools.loader.ui.info_widget.VersionTextEdit","title":"<code>VersionTextEdit</code>","text":"<p>               Bases: <code>QTextEdit</code></p> <p>QTextEdit that displays version specific information.</p> <p>This also overrides the context menu to add actions like copying source path to clipboard or copying the raw data of the version to clipboard.</p> Source code in <code>client/ayon_core/tools/loader/ui/info_widget.py</code> <pre><code>class VersionTextEdit(QtWidgets.QTextEdit):\n    \"\"\"QTextEdit that displays version specific information.\n\n    This also overrides the context menu to add actions like copying\n    source path to clipboard or copying the raw data of the version\n    to clipboard.\n\n    \"\"\"\n    def __init__(self, controller, parent):\n        super(VersionTextEdit, self).__init__(parent=parent)\n\n        self._version_item = None\n        self._product_item = None\n\n        self._controller = controller\n\n        # Reset\n        self.set_current_item()\n\n    def set_current_item(self, product_item=None, version_item=None):\n        \"\"\"\n\n        Args:\n            product_item (Union[ProductItem, None]): Product item.\n            version_item (Union[VersionItem, None]): Version item to display.\n        \"\"\"\n\n        self._product_item = product_item\n        self._version_item = version_item\n\n        if version_item is None:\n            # Reset state to empty\n            self.setText(\"\")\n            return\n\n        version_label = format_version(abs(version_item.version))\n        if version_item.version &lt; 0:\n            version_label = \"Hero version {}\".format(version_label)\n\n        # Define readable creation timestamp\n        created = version_item.published_time\n        created = datetime.datetime.strptime(created, \"%Y%m%dT%H%M%SZ\")\n        created = datetime.datetime.strftime(created, \"%b %d %Y %H:%M\")\n\n        comment = version_item.comment or \"No comment\"\n        source = version_item.source or \"No source\"\n\n        self.setHtml(\n            (\n                \"&lt;h2&gt;{product_name}&lt;/h2&gt;\"\n                \"&lt;h3&gt;{version_label}&lt;/h3&gt;\"\n                \"&lt;b&gt;Comment&lt;/b&gt;&lt;br&gt;\"\n                \"{comment}&lt;br&gt;&lt;br&gt;\"\n\n                \"&lt;b&gt;Created&lt;/b&gt;&lt;br&gt;\"\n                \"{created}&lt;br&gt;&lt;br&gt;\"\n\n                \"&lt;b&gt;Source&lt;/b&gt;&lt;br&gt;\"\n                \"{source}\"\n            ).format(\n                product_name=product_item.product_name,\n                version_label=version_label,\n                comment=comment,\n                created=created,\n                source=source,\n            )\n        )\n\n    def contextMenuEvent(self, event):\n        \"\"\"Context menu with additional actions\"\"\"\n        menu = self.createStandardContextMenu()\n\n        # Add additional actions when any text, so we can assume\n        #   the version is set.\n        source = None\n        if self._version_item is not None:\n            source = self._version_item.source\n\n        if source:\n            menu.addSeparator()\n            action = QtWidgets.QAction(\n                \"Copy source path to clipboard\", menu\n            )\n            action.triggered.connect(self._on_copy_source)\n            menu.addAction(action)\n\n        menu.exec_(event.globalPos())\n\n    def _on_copy_source(self):\n        \"\"\"Copy formatted source path to clipboard.\"\"\"\n\n        source = self._version_item.source\n        if not source:\n            return\n\n        filled_source = self._controller.fill_root_in_source(source)\n        clipboard = QtWidgets.QApplication.clipboard()\n        clipboard.setText(filled_source)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/ui/info_widget.html#client.ayon_core.tools.loader.ui.info_widget.VersionTextEdit.contextMenuEvent","title":"<code>contextMenuEvent(event)</code>","text":"<p>Context menu with additional actions</p> Source code in <code>client/ayon_core/tools/loader/ui/info_widget.py</code> <pre><code>def contextMenuEvent(self, event):\n    \"\"\"Context menu with additional actions\"\"\"\n    menu = self.createStandardContextMenu()\n\n    # Add additional actions when any text, so we can assume\n    #   the version is set.\n    source = None\n    if self._version_item is not None:\n        source = self._version_item.source\n\n    if source:\n        menu.addSeparator()\n        action = QtWidgets.QAction(\n            \"Copy source path to clipboard\", menu\n        )\n        action.triggered.connect(self._on_copy_source)\n        menu.addAction(action)\n\n    menu.exec_(event.globalPos())\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/ui/info_widget.html#client.ayon_core.tools.loader.ui.info_widget.VersionTextEdit.set_current_item","title":"<code>set_current_item(product_item=None, version_item=None)</code>","text":"<p>Parameters:</p> Name Type Description Default <code>product_item</code> <code>Union[ProductItem, None]</code> <p>Product item.</p> <code>None</code> <code>version_item</code> <code>Union[VersionItem, None]</code> <p>Version item to display.</p> <code>None</code> Source code in <code>client/ayon_core/tools/loader/ui/info_widget.py</code> <pre><code>def set_current_item(self, product_item=None, version_item=None):\n    \"\"\"\n\n    Args:\n        product_item (Union[ProductItem, None]): Product item.\n        version_item (Union[VersionItem, None]): Version item to display.\n    \"\"\"\n\n    self._product_item = product_item\n    self._version_item = version_item\n\n    if version_item is None:\n        # Reset state to empty\n        self.setText(\"\")\n        return\n\n    version_label = format_version(abs(version_item.version))\n    if version_item.version &lt; 0:\n        version_label = \"Hero version {}\".format(version_label)\n\n    # Define readable creation timestamp\n    created = version_item.published_time\n    created = datetime.datetime.strptime(created, \"%Y%m%dT%H%M%SZ\")\n    created = datetime.datetime.strftime(created, \"%b %d %Y %H:%M\")\n\n    comment = version_item.comment or \"No comment\"\n    source = version_item.source or \"No source\"\n\n    self.setHtml(\n        (\n            \"&lt;h2&gt;{product_name}&lt;/h2&gt;\"\n            \"&lt;h3&gt;{version_label}&lt;/h3&gt;\"\n            \"&lt;b&gt;Comment&lt;/b&gt;&lt;br&gt;\"\n            \"{comment}&lt;br&gt;&lt;br&gt;\"\n\n            \"&lt;b&gt;Created&lt;/b&gt;&lt;br&gt;\"\n            \"{created}&lt;br&gt;&lt;br&gt;\"\n\n            \"&lt;b&gt;Source&lt;/b&gt;&lt;br&gt;\"\n            \"{source}\"\n        ).format(\n            product_name=product_item.product_name,\n            version_label=version_label,\n            comment=comment,\n            created=created,\n            source=source,\n        )\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/ui/product_group_dialog.html","title":"product_group_dialog","text":""},{"location":"autoapi/client/ayon_core/tools/loader/ui/products_delegates.html","title":"products_delegates","text":""},{"location":"autoapi/client/ayon_core/tools/loader/ui/products_delegates.html#client.ayon_core.tools.loader.ui.products_delegates.LoadedInSceneDelegate","title":"<code>LoadedInSceneDelegate</code>","text":"<p>               Bases: <code>QStyledItemDelegate</code></p> <p>Delegate for Loaded in Scene state columns.</p> <p>Shows \"Yes\" or \"No\" for 1 or 0 values, or \"N/A\" for other values. Colorizes green or dark grey based on values.</p> Source code in <code>client/ayon_core/tools/loader/ui/products_delegates.py</code> <pre><code>class LoadedInSceneDelegate(QtWidgets.QStyledItemDelegate):\n    \"\"\"Delegate for Loaded in Scene state columns.\n\n    Shows \"Yes\" or \"No\" for 1 or 0 values, or \"N/A\" for other values.\n    Colorizes green or dark grey based on values.\n    \"\"\"\n\n    def __init__(self, *args, **kwargs):\n        super(LoadedInSceneDelegate, self).__init__(*args, **kwargs)\n        self._colors = {\n            1: QtGui.QColor(80, 170, 80),\n            0: QtGui.QColor(90, 90, 90),\n        }\n        self._default_color = QtGui.QColor(90, 90, 90)\n\n    def displayText(self, value, locale):\n        if value == 0:\n            return \"No\"\n        elif value == 1:\n            return \"Yes\"\n        return \"N/A\"\n\n    def initStyleOption(self, option, index):\n        super(LoadedInSceneDelegate, self).initStyleOption(option, index)\n\n        # Colorize based on value\n        value = index.data(PRODUCT_IN_SCENE_ROLE)\n        color = self._colors.get(value, self._default_color)\n        option.palette.setBrush(QtGui.QPalette.Text, color)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/ui/products_delegates.html#client.ayon_core.tools.loader.ui.products_delegates.SiteSyncDelegate","title":"<code>SiteSyncDelegate</code>","text":"<p>               Bases: <code>QStyledItemDelegate</code></p> <p>Paints icons and downloaded representation ration for both sites.</p> Source code in <code>client/ayon_core/tools/loader/ui/products_delegates.py</code> <pre><code>class SiteSyncDelegate(QtWidgets.QStyledItemDelegate):\n    \"\"\"Paints icons and downloaded representation ration for both sites.\"\"\"\n\n    def __init__(self, *args, **kwargs):\n        super().__init__(*args, **kwargs)\n        self._items_rects = []\n\n    def paint(self, painter, option, index):\n        super().paint(painter, option, index)\n        self._items_rects = []\n        option = QtWidgets.QStyleOptionViewItem(option)\n        option.showDecorationSelected = True\n\n        active_icon = index.data(ACTIVE_SITE_ICON_ROLE)\n        remote_icon = index.data(REMOTE_SITE_ICON_ROLE)\n        active_site_name = index.data(ACTIVE_SITE_NAME_ROLE)\n        remote_site_name = index.data(REMOTE_SITE_NAME_ROLE)\n\n        availability_active = \"{}/{}\".format(\n            index.data(SYNC_ACTIVE_SITE_AVAILABILITY),\n            index.data(REPRESENTATIONS_COUNT_ROLE)\n        )\n        availability_remote = \"{}/{}\".format(\n            index.data(SYNC_REMOTE_SITE_AVAILABILITY),\n            index.data(REPRESENTATIONS_COUNT_ROLE)\n        )\n\n        if availability_active is None or availability_remote is None:\n            return\n\n        items_to_draw = [\n            (value, icon, site_name)\n            for value, icon, site_name in (\n                (availability_active, active_icon, active_site_name),\n                (availability_remote, remote_icon, remote_site_name),\n            )\n            if icon\n        ]\n        if not items_to_draw:\n            return\n\n        icon_size = QtCore.QSize(24, 24)\n        padding = 10\n        pos_x = option.rect.x()\n\n        item_width = int(option.rect.width() / len(items_to_draw))\n        if item_width &lt; 1:\n            item_width = 0\n\n        for value, icon, site_name in items_to_draw:\n            item_rect = QtCore.QRect(\n                pos_x,\n                option.rect.y(),\n                item_width,\n                option.rect.height()\n            )\n            self._items_rects.append((item_rect, site_name))\n            # Prepare pos_x for next item\n            pos_x = item_rect.x() + item_rect.width()\n\n            pixmap = icon.pixmap(icon.actualSize(icon_size))\n            point = QtCore.QPoint(\n                item_rect.x() + padding,\n                item_rect.y() + ((item_rect.height() - pixmap.height()) * 0.5)\n            )\n            painter.drawPixmap(point, pixmap)\n\n            icon_offset = icon_size.width() + (padding * 2)\n            text_rect = QtCore.QRect(item_rect)\n            text_rect.setLeft(text_rect.left() + icon_offset)\n            if text_rect.width() &lt; 1:\n                continue\n\n            painter.drawText(\n                text_rect,\n                option.displayAlignment,\n                value\n            )\n\n    def editorEvent(self, event, model, option, index):\n        result = super().editorEvent(\n            event, model, option, index\n        )\n        if event.type() == QtCore.QEvent.MouseMove:\n            mouse_pos = event.pos()\n            tooltip = \"\"\n            for item_rect, site_name in self._items_rects:\n                if item_rect.contains(mouse_pos):\n                    tooltip = site_name\n                    break\n            if tooltip:\n                QtWidgets.QToolTip.showText(event.globalPos(), tooltip)\n            else:\n                QtWidgets.QToolTip.hideText()\n        elif event.type() == QtCore.QEvent.Leave:\n            QtWidgets.QToolTip.hideText()\n        return result\n\n    def displayText(self, value, locale):\n        pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/ui/products_delegates.html#client.ayon_core.tools.loader.ui.products_delegates.VersionDelegate","title":"<code>VersionDelegate</code>","text":"<p>               Bases: <code>QStyledItemDelegate</code></p> <p>A delegate that display version integer formatted as version string.</p> Source code in <code>client/ayon_core/tools/loader/ui/products_delegates.py</code> <pre><code>class VersionDelegate(QtWidgets.QStyledItemDelegate):\n    \"\"\"A delegate that display version integer formatted as version string.\"\"\"\n\n    version_changed = QtCore.Signal(str, str)\n\n    def __init__(self, *args, **kwargs):\n        super().__init__(*args, **kwargs)\n\n        self._editor_by_id: dict[str, VersionComboBox] = {}\n        self._task_ids_filter = None\n        self._statuses_filter = None\n        self._version_tags_filter = None\n        self._task_tags_filter = None\n\n    def displayText(self, value, locale):\n        if not isinstance(value, numbers.Integral):\n            return \"N/A\"\n        return format_version(value)\n\n    def set_tasks_filter(self, task_ids):\n        self._task_ids_filter = set(task_ids)\n        for widget in self._editor_by_id.values():\n            widget.set_tasks_filter(task_ids)\n\n    def set_statuses_filter(self, status_names):\n        if status_names is not None:\n            status_names = set(status_names)\n        self._statuses_filter = status_names\n        for widget in self._editor_by_id.values():\n            widget.set_statuses_filter(status_names)\n\n    def set_version_tags_filter(self, tags):\n        if tags is not None:\n            tags = set(tags)\n        self._version_tags_filter = tags\n        for widget in self._editor_by_id.values():\n            widget.set_version_tags_filter(tags)\n\n    def set_task_tags_filter(self, tags):\n        if tags is not None:\n            tags = set(tags)\n        self._task_tags_filter = tags\n        for widget in self._editor_by_id.values():\n            widget.set_task_tags_filter(tags)\n\n    def paint(self, painter, option, index):\n        fg_color = index.data(QtCore.Qt.ForegroundRole)\n        if fg_color:\n            if isinstance(fg_color, QtGui.QBrush):\n                fg_color = fg_color.color()\n            elif isinstance(fg_color, QtGui.QColor):\n                pass\n            else:\n                fg_color = None\n\n        if not fg_color:\n            return super().paint(painter, option, index)\n\n        if option.widget:\n            style = option.widget.style()\n        else:\n            style = QtWidgets.QApplication.style()\n\n        style.drawControl(\n            QtWidgets.QCommonStyle.CE_ItemViewItem,\n            option,\n            painter,\n            option.widget\n        )\n\n        painter.save()\n\n        text = self.displayText(\n            index.data(QtCore.Qt.DisplayRole), option.locale\n        )\n        pen = painter.pen()\n        pen.setColor(fg_color)\n        painter.setPen(pen)\n\n        text_rect = style.subElementRect(\n            QtWidgets.QCommonStyle.SE_ItemViewItemText,\n            option\n        )\n        text_margin = style.proxy().pixelMetric(\n            QtWidgets.QCommonStyle.PM_FocusFrameHMargin,\n            option,\n            option.widget\n        ) + 1\n\n        painter.drawText(\n            text_rect.adjusted(text_margin, 0, - text_margin, 0),\n            option.displayAlignment,\n            text\n        )\n\n        painter.restore()\n\n    def createEditor(self, parent, option, index):\n        product_id = index.data(PRODUCT_ID_ROLE)\n        if not product_id:\n            return\n\n        item_id = uuid.uuid4().hex\n\n        editor = VersionComboBox(product_id, parent)\n        editor.setProperty(\"itemId\", item_id)\n        editor.setFocusPolicy(QtCore.Qt.NoFocus)\n\n        editor.value_changed.connect(self._on_editor_change)\n        editor.destroyed.connect(self._on_destroy)\n\n        self._editor_by_id[item_id] = editor\n\n        return editor\n\n    def setEditorData(self, editor, index):\n        editor.clear()\n\n        # Current value of the index\n        product_id = index.data(PRODUCT_ID_ROLE)\n        version_id = index.data(VERSION_ID_ROLE)\n        model = index.model()\n        while hasattr(model, \"sourceModel\"):\n            model = model.sourceModel()\n        versions = model.get_version_items_by_product_id(product_id)\n        task_tags_by_version_id = {\n            version_item.version_id: model.get_task_tags_by_id(\n                version_item.task_id\n            )\n            for version_item in versions\n        }\n\n        editor.update_versions(versions, version_id, task_tags_by_version_id)\n        editor.set_tasks_filter(self._task_ids_filter)\n        editor.set_task_tags_filter(self._task_tags_filter)\n        editor.set_statuses_filter(self._statuses_filter)\n\n    def setModelData(self, editor, model, index):\n        \"\"\"Apply the integer version back in the model\"\"\"\n\n        version_id = editor.itemData(editor.currentIndex())\n        model.setData(index, version_id, VERSION_NAME_EDIT_ROLE)\n\n    def _on_editor_change(self, product_id, version_id):\n        self.version_changed.emit(product_id, version_id)\n\n    def _on_destroy(self, obj):\n        item_id = obj.property(\"itemId\")\n        self._editor_by_id.pop(item_id, None)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/ui/products_delegates.html#client.ayon_core.tools.loader.ui.products_delegates.VersionDelegate.setModelData","title":"<code>setModelData(editor, model, index)</code>","text":"<p>Apply the integer version back in the model</p> Source code in <code>client/ayon_core/tools/loader/ui/products_delegates.py</code> <pre><code>def setModelData(self, editor, model, index):\n    \"\"\"Apply the integer version back in the model\"\"\"\n\n    version_id = editor.itemData(editor.currentIndex())\n    model.setData(index, version_id, VERSION_NAME_EDIT_ROLE)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/ui/products_model.html","title":"products_model","text":""},{"location":"autoapi/client/ayon_core/tools/loader/ui/products_model.html#client.ayon_core.tools.loader.ui.products_model.ProductsModel","title":"<code>ProductsModel</code>","text":"<p>               Bases: <code>QStandardItemModel</code></p> Source code in <code>client/ayon_core/tools/loader/ui/products_model.py</code> <pre><code>class ProductsModel(QtGui.QStandardItemModel):\n    refreshed = QtCore.Signal()\n    version_changed = QtCore.Signal()\n    column_labels = [\n        \"Product name\",\n        \"Product type\",\n        \"Product base type\",\n        \"Folder\",\n        \"Version\",\n        \"Status\",\n        \"Time\",\n        \"Author\",\n        \"Frames\",\n        \"Duration\",\n        \"Handles\",\n        \"Step\",\n        \"In scene\",\n        \"Availability\",\n    ]\n    merged_items_colors = [\n        (\"#{0:02x}{1:02x}{2:02x}\".format(*c), QtGui.QColor(*c))\n        for c in [\n            (55, 161, 222),   # Light Blue\n            (231, 176, 0),    # Yellow\n            (154, 13, 255),   # Purple\n            (130, 184, 30),   # Light Green\n            (211, 79, 63),    # Light Red\n            (179, 181, 182),  # Grey\n            (194, 57, 179),   # Pink\n            (0, 120, 215),    # Dark Blue\n            (0, 204, 106),    # Dark Green\n            (247, 99, 12),    # Orange\n        ]\n    ]\n\n    product_name_col = column_labels.index(\"Product name\")\n    product_type_col = column_labels.index(\"Product type\")\n    product_base_type_col = column_labels.index(\"Product base type\")\n    folders_label_col = column_labels.index(\"Folder\")\n    version_col = column_labels.index(\"Version\")\n    status_col = column_labels.index(\"Status\")\n    published_time_col = column_labels.index(\"Time\")\n    author_col = column_labels.index(\"Author\")\n    frame_range_col = column_labels.index(\"Frames\")\n    duration_col = column_labels.index(\"Duration\")\n    handles_col = column_labels.index(\"Handles\")\n    step_col = column_labels.index(\"Step\")\n    in_scene_col = column_labels.index(\"In scene\")\n    sitesync_avail_col = column_labels.index(\"Availability\")\n    _display_role_mapping = {\n        product_name_col: QtCore.Qt.DisplayRole,\n        product_type_col: PRODUCT_TYPE_ROLE,\n        product_base_type_col: PRODUCT_BASE_TYPE_ROLE,\n        folders_label_col: FOLDER_LABEL_ROLE,\n        version_col: VERSION_NAME_ROLE,\n        status_col: VERSION_STATUS_NAME_ROLE,\n        published_time_col: VERSION_PUBLISH_TIME_ROLE,\n        author_col: VERSION_AUTHOR_ROLE,\n        frame_range_col: VERSION_FRAME_RANGE_ROLE,\n        duration_col: VERSION_DURATION_ROLE,\n        handles_col: VERSION_HANDLES_ROLE,\n        step_col: VERSION_STEP_ROLE,\n        in_scene_col: PRODUCT_IN_SCENE_ROLE,\n        sitesync_avail_col: VERSION_AVAILABLE_ROLE,\n\n    }\n\n    def __init__(self, controller):\n        super().__init__()\n        self.setColumnCount(len(self.column_labels))\n        for idx, label in enumerate(self.column_labels):\n            self.setHeaderData(idx, QtCore.Qt.Horizontal, label)\n        self._controller = controller\n\n        # Variables to store 'QStandardItem'\n        self._items_by_id = {}\n        self._group_items_by_name = {}\n        self._merged_items_by_id = {}\n\n        # product item objects (they have version information)\n        self._product_items_by_id = {}\n        self._grouping_enabled = True\n        self._reset_merge_color = False\n        self._color_iterator = self._color_iter()\n        self._group_icon = None\n\n        self._last_project_name = None\n        self._last_folder_ids = []\n        self._last_project_statuses = {}\n        self._last_status_icons_by_name = {}\n        self._last_task_tags_by_task_id = {}\n\n    def get_product_item_indexes(self):\n        return [\n            self.indexFromItem(item)\n            for item in self._items_by_id.values()\n        ]\n\n    def get_product_item_by_id(self, product_id):\n        \"\"\"\n\n        Args:\n            product_id (str): Product id.\n\n        Returns:\n            Union[ProductItem, None]: Product item with version information.\n        \"\"\"\n\n        return self._product_items_by_id.get(product_id)\n\n    def set_product_version(self, product_id, version_id):\n        if version_id is None:\n            return\n\n        product_item = self._items_by_id.get(product_id)\n        if product_item is None:\n            return\n\n        index = self.indexFromItem(product_item)\n        self.setData(index, version_id, VERSION_NAME_EDIT_ROLE)\n\n    def set_enable_grouping(self, enable_grouping):\n        if enable_grouping is self._grouping_enabled:\n            return\n        self._grouping_enabled = enable_grouping\n        # Ignore change if groups are not available\n        self.refresh(\n            self._last_project_name,\n            self._last_folder_ids\n        )\n\n    def get_task_tags_by_id(self, task_id):\n        return self._last_task_tags_by_task_id.get(task_id, set())\n\n    def get_version_items_by_product_id(self, product_id: str):\n        product_item = self._product_items_by_id.get(product_id)\n        if product_item is None:\n            return None\n        version_items = list(product_item.version_items.values())\n        version_items.sort(reverse=True)\n        return version_items\n\n    def flags(self, index):\n        # Make the version column editable\n        if index.column() == self.version_col and index.data(PRODUCT_ID_ROLE):\n            return (\n                QtCore.Qt.ItemIsEnabled\n                | QtCore.Qt.ItemIsSelectable\n                | QtCore.Qt.ItemIsEditable\n            )\n        if index.column() != 0:\n            index = self.index(index.row(), 0, index.parent())\n        return super().flags(index)\n\n    def data(self, index, role=None):\n        if role is None:\n            role = QtCore.Qt.DisplayRole\n\n        if not index.isValid():\n            return None\n\n        if role in (VERSION_STATUS_SHORT_ROLE, VERSION_STATUS_COLOR_ROLE):\n            status_name = self.data(index, VERSION_STATUS_NAME_ROLE)\n            status_item = self._last_project_statuses.get(status_name)\n            if status_item is None:\n                return \"\"\n            if role == VERSION_STATUS_SHORT_ROLE:\n                return status_item.short\n            return status_item.color\n\n        col = index.column()\n        if col == self.status_col and role == QtCore.Qt.DecorationRole:\n            role = VERSION_STATUS_ICON_ROLE\n\n        if role == VERSION_STATUS_ICON_ROLE:\n            status_name = self.data(index, VERSION_STATUS_NAME_ROLE)\n            return self._get_status_icon(status_name)\n\n        if col == 0:\n            return super().data(index, role)\n\n        if role == QtCore.Qt.DecorationRole:\n            return None\n\n        if (\n            role == VERSION_NAME_EDIT_ROLE\n            or (role == QtCore.Qt.EditRole and col == self.version_col)\n        ):\n            index = self.index(index.row(), 0, index.parent())\n            product_id = index.data(PRODUCT_ID_ROLE)\n            product_item = self._product_items_by_id.get(product_id)\n            if product_item is None:\n                return None\n            version_items = list(product_item.version_items.values())\n            version_items.sort(reverse=True)\n            return version_items\n\n        if role == QtCore.Qt.EditRole:\n            return None\n\n        if role == QtCore.Qt.DisplayRole:\n            if not index.data(PRODUCT_ID_ROLE):\n                return None\n            role = self._display_role_mapping.get(col)\n            if role is None:\n                return None\n\n        index = self.index(index.row(), 0, index.parent())\n\n        return super().data(index, role)\n\n    def setData(self, index, value, role=None):\n        if not index.isValid():\n            return False\n\n        if role is None:\n            role = QtCore.Qt.EditRole\n\n        col = index.column()\n        if col == self.version_col and role == QtCore.Qt.EditRole:\n            role = VERSION_NAME_EDIT_ROLE\n\n        if role == VERSION_NAME_EDIT_ROLE:\n            if col != 0:\n                index = self.index(index.row(), 0, index.parent())\n            product_id = index.data(PRODUCT_ID_ROLE)\n            product_item = self._product_items_by_id[product_id]\n            final_version_item = None\n            for v_id, version_item in product_item.version_items.items():\n                if v_id == value:\n                    final_version_item = version_item\n                    break\n\n            if final_version_item is None:\n                return False\n            if index.data(VERSION_ID_ROLE) == final_version_item.version_id:\n                return True\n            item = self.itemFromIndex(index)\n            self._set_version_data_to_product_item(item, final_version_item)\n            self.version_changed.emit()\n            return True\n        return super().setData(index, value, role)\n\n    def _get_next_color(self):\n        return next(self._color_iterator)\n\n    def _color_iter(self):\n        while True:\n            for color in self.merged_items_colors:\n                if self._reset_merge_color:\n                    self._reset_merge_color = False\n                    break\n                yield color\n\n    def _get_status_icon(self, status_name):\n        icon = self._last_status_icons_by_name.get(status_name)\n        if icon is not None:\n            return icon\n\n        status_item = self._last_project_statuses.get(status_name)\n        if status_item is not None:\n            icon = get_qt_icon(MaterialSymbolsIcon(\n                status_item.icon,\n                color=status_item.color,\n            ))\n\n        if icon is None:\n            icon = QtGui.QIcon()\n\n        self._last_status_icons_by_name[status_name] = icon\n        return icon\n\n    def _clear(self):\n        root_item = self.invisibleRootItem()\n        root_item.removeRows(0, root_item.rowCount())\n\n        self._items_by_id = {}\n        self._group_items_by_name = {}\n        self._merged_items_by_id = {}\n        self._product_items_by_id = {}\n        self._reset_merge_color = True\n\n    def _get_group_icon(self):\n        if self._group_icon is None:\n            self._group_icon = get_qt_icon(AwesomeFontIcon(\n                \"fa.object-group\",\n                color=get_default_entity_icon_color()\n            ))\n        return self._group_icon\n\n    def _get_group_model_item(self, group_name):\n        model_item = self._group_items_by_name.get(group_name)\n        if model_item is None:\n            model_item = QtGui.QStandardItem(group_name)\n            model_item.setData(\n                self._get_group_icon(), QtCore.Qt.DecorationRole\n            )\n            model_item.setData(0, GROUP_TYPE_ROLE)\n            model_item.setEditable(False)\n            model_item.setColumnCount(self.columnCount())\n            self._group_items_by_name[group_name] = model_item\n        return model_item\n\n    def _get_merged_model_item(self, path, count, hex_color):\n        model_item = self._merged_items_by_id.get(path)\n        if model_item is None:\n            model_item = QtGui.QStandardItem()\n            model_item.setData(1, GROUP_TYPE_ROLE)\n            model_item.setData(hex_color, MERGED_COLOR_ROLE)\n            model_item.setEditable(False)\n            model_item.setColumnCount(self.columnCount())\n            self._merged_items_by_id[path] = model_item\n        label = \"{} ({})\".format(path, count)\n        model_item.setData(label, QtCore.Qt.DisplayRole)\n        return model_item\n\n    def _set_version_data_to_product_item(\n        self,\n        model_item,\n        version_item,\n        repre_count_by_version_id=None,\n        sync_availability_by_version_id=None,\n    ):\n        \"\"\"\n\n        Args:\n            model_item (QtGui.QStandardItem): Item which should have values\n                from version item.\n            version_item (VersionItem): Item from entities model with\n                information about version.\n            repre_count_by_version_id (Optional[str, int]): Mapping of\n                representation count by version id.\n            sync_availability_by_version_id (Optional[str, Tuple[int, int]]):\n                Mapping of sync availability by version id.\n\n        \"\"\"\n        model_item.setData(version_item.version_id, VERSION_ID_ROLE)\n        model_item.setData(version_item.task_id, TASK_ID_ROLE)\n        model_item.setData(version_item.version, VERSION_NAME_ROLE)\n        model_item.setData(version_item.is_hero, VERSION_HERO_ROLE)\n        model_item.setData(\n            version_item.published_time, VERSION_PUBLISH_TIME_ROLE\n        )\n        model_item.setData(version_item.author, VERSION_AUTHOR_ROLE)\n        model_item.setData(version_item.status, VERSION_STATUS_NAME_ROLE)\n        model_item.setData(version_item.frame_range, VERSION_FRAME_RANGE_ROLE)\n        model_item.setData(version_item.duration, VERSION_DURATION_ROLE)\n        model_item.setData(version_item.handles, VERSION_HANDLES_ROLE)\n        model_item.setData(version_item.step, VERSION_STEP_ROLE)\n        model_item.setData(\n            version_item.thumbnail_id, VERSION_THUMBNAIL_ID_ROLE)\n\n        # TODO call site sync methods for all versions at once\n        project_name = self._last_project_name\n        version_id = version_item.version_id\n        if repre_count_by_version_id is None:\n            repre_count_by_version_id = (\n                self._controller.get_versions_representation_count(\n                    project_name, [version_id]\n                )\n            )\n        if sync_availability_by_version_id is None:\n            sync_availability_by_version_id = (\n                self._controller.get_version_sync_availability(\n                    project_name, [version_id]\n                )\n            )\n        repre_count = repre_count_by_version_id[version_id]\n        active, remote = sync_availability_by_version_id[version_id]\n\n        model_item.setData(repre_count, REPRESENTATIONS_COUNT_ROLE)\n        model_item.setData(active, SYNC_ACTIVE_SITE_AVAILABILITY)\n        model_item.setData(remote, SYNC_REMOTE_SITE_AVAILABILITY)\n\n    def _get_product_model_item(\n        self,\n        product_item,\n        active_site_icon,\n        remote_site_icon,\n        active_site_name,\n        remote_site_name,\n        repre_count_by_version_id,\n        sync_availability_by_version_id,\n        last_version_by_product_id,\n    ):\n        model_item = self._items_by_id.get(product_item.product_id)\n        last_version = last_version_by_product_id[product_item.product_id]\n\n        statuses = {\n            version_item.status\n            for version_item in product_item.version_items.values()\n        }\n        version_tags = set()\n        task_tags = set()\n        for version_item in product_item.version_items.values():\n            version_tags |= set(version_item.tags)\n            _task_tags = self._last_task_tags_by_task_id.get(\n                version_item.task_id\n            )\n            if _task_tags:\n                task_tags |= set(_task_tags)\n\n        if model_item is None:\n            product_id = product_item.product_id\n            model_item = QtGui.QStandardItem(product_item.product_name)\n            model_item.setEditable(False)\n            icon = get_qt_icon(product_item.product_icon)\n            model_item.setColumnCount(self.columnCount())\n            model_item.setData(icon, QtCore.Qt.DecorationRole)\n            model_item.setData(product_id, PRODUCT_ID_ROLE)\n            model_item.setData(product_item.product_name, PRODUCT_NAME_ROLE)\n            model_item.setData(\n                product_item.product_base_type, PRODUCT_BASE_TYPE_ROLE\n            )\n            model_item.setData(product_item.product_type, PRODUCT_TYPE_ROLE)\n            model_item.setData(product_item.folder_id, FOLDER_ID_ROLE)\n\n            self._product_items_by_id[product_id] = product_item\n            self._items_by_id[product_id] = model_item\n\n        model_item.setData(\"|\".join(statuses), STATUS_NAME_FILTER_ROLE)\n        model_item.setData(\"|\".join(version_tags), VERSION_TAGS_FILTER_ROLE)\n        model_item.setData(\"|\".join(task_tags), TASK_TAGS_FILTER_ROLE)\n        model_item.setData(product_item.folder_label, FOLDER_LABEL_ROLE)\n        in_scene = 1 if product_item.product_in_scene else 0\n        model_item.setData(in_scene, PRODUCT_IN_SCENE_ROLE)\n\n        model_item.setData(active_site_icon, ACTIVE_SITE_ICON_ROLE)\n        model_item.setData(remote_site_icon, REMOTE_SITE_ICON_ROLE)\n        model_item.setData(active_site_name, ACTIVE_SITE_NAME_ROLE)\n        model_item.setData(remote_site_name, REMOTE_SITE_NAME_ROLE)\n\n        self._set_version_data_to_product_item(\n            model_item,\n            last_version,\n            repre_count_by_version_id,\n            sync_availability_by_version_id,\n        )\n        return model_item\n\n    def get_last_project_name(self):\n        return self._last_project_name\n\n    def refresh(self, project_name, folder_ids):\n        self._clear()\n\n        self._last_project_name = project_name\n        self._last_folder_ids = folder_ids\n        status_items = self._controller.get_project_status_items(project_name)\n        self._last_project_statuses = {\n            status_item.name: status_item\n            for status_item in status_items\n        }\n        self._last_status_icons_by_name = {}\n\n        task_items = self._controller.get_task_items(\n            project_name, folder_ids, sender=PRODUCTS_MODEL_SENDER_NAME\n        )\n        self._last_task_tags_by_task_id = {\n            task_item.task_id: task_item.tags\n            for task_item in task_items\n        }\n\n        active_site_icon_def = self._controller.get_active_site_icon_def(\n            project_name\n        )\n        remote_site_icon_def = self._controller.get_remote_site_icon_def(\n            project_name\n        )\n        active_site_name = self._controller.get_active_site(project_name)\n        remote_site_name = self._controller.get_remote_site(project_name)\n        active_site_icon = get_qt_icon(active_site_icon_def)\n        remote_site_icon = get_qt_icon(remote_site_icon_def)\n\n        product_items = self._controller.get_product_items(\n            project_name,\n            folder_ids,\n            sender=PRODUCTS_MODEL_SENDER_NAME\n        )\n\n        product_items_by_id = {\n            product_item.product_id: product_item\n            for product_item in product_items\n        }\n        last_version_by_product_id = {}\n        for product_item in product_items:\n            versions = list(product_item.version_items.values())\n            versions.sort()\n            last_version = versions[-1]\n            last_version_by_product_id[product_item.product_id] = (\n                last_version\n            )\n\n        version_ids = {\n            version_item.version_id\n            for version_item in last_version_by_product_id.values()\n        }\n        repre_count_by_version_id = (\n            self._controller.get_versions_representation_count(\n                project_name, version_ids\n            )\n        )\n        sync_availability_by_version_id = (\n            self._controller.get_version_sync_availability(\n                project_name, version_ids\n            )\n        )\n\n        # Prepare product groups\n        product_name_matches_by_group = collections.defaultdict(dict)\n        for product_item in product_items_by_id.values():\n            group_name = None\n            if self._grouping_enabled:\n                group_name = product_item.group_name\n\n            product_name = product_item.product_name\n            group = product_name_matches_by_group[group_name]\n            group.setdefault(product_name, []).append(product_item)\n\n        group_names = set(product_name_matches_by_group.keys())\n\n        root_item = self.invisibleRootItem()\n        new_root_items = []\n        merged_paths = set()\n        for group_name in group_names:\n            key_parts = []\n            if group_name:\n                key_parts.append(group_name)\n\n            groups = product_name_matches_by_group[group_name]\n            merged_product_items = {}\n            top_items = []\n            group_product_types = set()\n            group_status_names = set()\n            for product_name, product_items in groups.items():\n                group_product_types |= {p.product_type for p in product_items}\n                for product_item in product_items:\n                    group_status_names |= {\n                        version_item.status\n                        for version_item in product_item.version_items.values()\n                    }\n                    group_product_types.add(product_item.product_type)\n\n                if len(product_items) == 1:\n                    top_items.append(product_items[0])\n                else:\n                    path = \"/\".join(key_parts + [product_name])\n                    merged_paths.add(path)\n                    merged_product_items[path] = (\n                        product_name,\n                        product_items,\n                    )\n\n            parent_item = None\n            if group_name:\n                parent_item = self._get_group_model_item(group_name)\n                parent_item.setData(\n                    \"|\".join(group_product_types),\n                    PRODUCT_TYPE_ROLE\n                )\n                parent_item.setData(\n                    \"|\".join(group_status_names),\n                    STATUS_NAME_FILTER_ROLE\n                )\n\n            new_items = []\n            if parent_item is not None and parent_item.row() &lt; 0:\n                new_root_items.append(parent_item)\n\n            for product_item in top_items:\n                item = self._get_product_model_item(\n                    product_item,\n                    active_site_icon,\n                    remote_site_icon,\n                    active_site_name,\n                    remote_site_name,\n                    repre_count_by_version_id,\n                    sync_availability_by_version_id,\n                    last_version_by_product_id,\n                )\n                new_items.append(item)\n\n            for path_info in merged_product_items.values():\n                product_name, product_items = path_info\n                (merged_color_hex, merged_color_qt) = self._get_next_color()\n                merged_color = qtawesome.icon(\n                    \"fa.circle\", color=merged_color_qt\n                )\n                merged_item = self._get_merged_model_item(\n                    product_name, len(product_items), merged_color_hex)\n                merged_item.setData(merged_color, QtCore.Qt.DecorationRole)\n                new_items.append(merged_item)\n\n                merged_product_types = set()\n                merged_status_names = set()\n                new_merged_items = []\n                for product_item in product_items:\n                    item = self._get_product_model_item(\n                        product_item,\n                        active_site_icon,\n                        remote_site_icon,\n                        active_site_name,\n                        remote_site_name,\n                        repre_count_by_version_id,\n                        sync_availability_by_version_id,\n                        last_version_by_product_id,\n                    )\n                    new_merged_items.append(item)\n                    merged_product_types.add(product_item.product_type)\n                    merged_status_names |= {\n                        version_item.status\n                        for version_item in (\n                            product_item.version_items.values()\n                        )\n                    }\n\n                merged_item.setData(\n                    \"|\".join(merged_product_types),\n                    PRODUCT_TYPE_ROLE\n                )\n                merged_item.setData(\n                    \"|\".join(merged_status_names),\n                    STATUS_NAME_FILTER_ROLE\n                )\n                if new_merged_items:\n                    merged_item.appendRows(new_merged_items)\n\n            if not new_items:\n                continue\n\n            if parent_item is None:\n                new_root_items.extend(new_items)\n            else:\n                parent_item.appendRows(new_items)\n\n        if new_root_items:\n            root_item.appendRows(new_root_items)\n\n        self.refreshed.emit()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/ui/products_model.html#client.ayon_core.tools.loader.ui.products_model.ProductsModel.get_product_item_by_id","title":"<code>get_product_item_by_id(product_id)</code>","text":"<p>Parameters:</p> Name Type Description Default <code>product_id</code> <code>str</code> <p>Product id.</p> required <p>Returns:</p> Type Description <p>Union[ProductItem, None]: Product item with version information.</p> Source code in <code>client/ayon_core/tools/loader/ui/products_model.py</code> <pre><code>def get_product_item_by_id(self, product_id):\n    \"\"\"\n\n    Args:\n        product_id (str): Product id.\n\n    Returns:\n        Union[ProductItem, None]: Product item with version information.\n    \"\"\"\n\n    return self._product_items_by_id.get(product_id)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/ui/products_widget.html","title":"products_widget","text":""},{"location":"autoapi/client/ayon_core/tools/loader/ui/products_widget.html#client.ayon_core.tools.loader.ui.products_widget.ProductsWidget","title":"<code>ProductsWidget</code>","text":"<p>               Bases: <code>QWidget</code></p> Source code in <code>client/ayon_core/tools/loader/ui/products_widget.py</code> <pre><code>class ProductsWidget(QtWidgets.QWidget):\n    refreshed = QtCore.Signal()\n    merged_products_selection_changed = QtCore.Signal()\n    selection_changed = QtCore.Signal()\n    default_widths = (\n        200,  # Product name\n        90,   # Product type\n        90,   # Product base type\n        130,  # Folder label\n        60,   # Version\n        100,  # Status\n        125,  # Time\n        75,   # Author\n        75,   # Frames\n        60,   # Duration\n        55,   # Handles\n        10,   # Step\n        25,   # Loaded in scene\n        65,   # Site sync info\n    )\n\n    def __init__(self, controller, parent):\n        super(ProductsWidget, self).__init__(parent)\n\n        self._controller = controller\n\n        products_view = DeselectableTreeView(self)\n        # TODO - define custom object name in style\n        products_view.setObjectName(\"ProductView\")\n        products_view.setSelectionMode(\n            QtWidgets.QAbstractItemView.ExtendedSelection\n        )\n        products_view.setAllColumnsShowFocus(False)\n        # TODO - add context menu\n        products_view.setContextMenuPolicy(QtCore.Qt.CustomContextMenu)\n        products_view.setSortingEnabled(True)\n        # Sort by product type\n        products_view.sortByColumn(1, QtCore.Qt.AscendingOrder)\n        products_view.setAlternatingRowColors(True)\n\n        products_model = ProductsModel(controller)\n        products_proxy_model = ProductsProxyModel()\n        products_proxy_model.setSourceModel(products_model)\n\n        products_view.setModel(products_proxy_model)\n\n        for idx, width in enumerate(self.default_widths):\n            products_view.setColumnWidth(idx, width)\n\n        version_delegate = VersionDelegate()\n        time_delegate = PrettyTimeDelegate()\n        status_delegate = StatusDelegate(\n            VERSION_STATUS_NAME_ROLE,\n            VERSION_STATUS_SHORT_ROLE,\n            VERSION_STATUS_COLOR_ROLE,\n            VERSION_STATUS_ICON_ROLE,\n        )\n        in_scene_delegate = LoadedInSceneDelegate()\n        sitesync_delegate = SiteSyncDelegate()\n\n        for col, delegate in (\n            (products_model.version_col, version_delegate),\n            (products_model.published_time_col, time_delegate),\n            (products_model.status_col, status_delegate),\n            (products_model.in_scene_col, in_scene_delegate),\n            (products_model.sitesync_avail_col, sitesync_delegate),\n        ):\n            products_view.setItemDelegateForColumn(col, delegate)\n\n        main_layout = QtWidgets.QHBoxLayout(self)\n        main_layout.setContentsMargins(0, 0, 0, 0)\n        main_layout.addWidget(products_view, 1)\n\n        products_proxy_model.rowsInserted.connect(self._on_rows_inserted)\n        products_proxy_model.rowsMoved.connect(self._on_rows_moved)\n        products_model.refreshed.connect(self._on_refresh)\n        products_model.version_changed.connect(self._on_version_change)\n        products_view.customContextMenuRequested.connect(\n            self._on_context_menu)\n        products_view_sel_model = products_view.selectionModel()\n        products_view_sel_model.selectionChanged.connect(\n            self._on_selection_change)\n        version_delegate.version_changed.connect(\n            self._on_version_delegate_change\n        )\n\n        controller.register_event_callback(\n            \"selection.folders.changed\",\n            self._on_folders_selection_change,\n        )\n        controller.register_event_callback(\n            \"products.refresh.finished\",\n            self._on_products_refresh_finished\n        )\n        controller.register_event_callback(\n            \"products.group.changed\",\n            self._on_group_changed\n        )\n\n        self._products_view = products_view\n        self._products_model = products_model\n        self._products_proxy_model = products_proxy_model\n\n        self._version_delegate = version_delegate\n        self._time_delegate = time_delegate\n        self._status_delegate = status_delegate\n        self._in_scene_delegate = in_scene_delegate\n        self._sitesync_delegate = sitesync_delegate\n\n        self._selected_project_name = None\n        self._selected_folder_ids = set()\n\n        self._selected_merged_products = []\n        self._selected_versions_info = []\n\n        # Set initial state of widget\n        # - Hide folders column\n        self._update_folders_label_visible()\n        # - Hide in scene column if is not supported (this won't change)\n        products_view.setColumnHidden(\n            products_model.in_scene_col,\n            not controller.is_loaded_products_supported()\n        )\n        self._set_sitesync_visibility(\n            self._controller.is_sitesync_enabled()\n        )\n\n        if not is_product_base_type_supported():\n            # Hide product base type column\n            products_view.setColumnHidden(\n                products_model.product_base_type_col, True\n            )\n\n    def set_name_filter(self, name):\n        \"\"\"Set filter of product name.\n\n        Args:\n            name (str): The string filter.\n\n        \"\"\"\n        self._products_proxy_model.setFilterFixedString(name)\n\n    def set_tasks_filter(self, task_ids):\n        \"\"\"Set filter of version tasks.\n\n        Args:\n            task_ids (set[str]): Task ids.\n\n        \"\"\"\n        self._version_delegate.set_tasks_filter(task_ids)\n        self._products_proxy_model.set_tasks_filter(task_ids)\n\n    def set_statuses_filter(self, status_names):\n        \"\"\"Set filter of version statuses.\n\n        Args:\n            status_names (list[str]): The list of status names.\n\n        \"\"\"\n        self._version_delegate.set_statuses_filter(status_names)\n        self._products_proxy_model.set_statuses_filter(status_names)\n\n    def set_version_tags_filter(self, version_tags):\n        self._version_delegate.set_version_tags_filter(version_tags)\n        self._products_proxy_model.set_version_tags_filter(version_tags)\n\n    def set_task_tags_filter(self, task_tags):\n        self._version_delegate.set_task_tags_filter(task_tags)\n        self._products_proxy_model.set_task_tags_filter(task_tags)\n\n    def set_product_type_filter(self, product_type_filters):\n        \"\"\"\n\n        Args:\n            product_type_filters (dict[str, bool]): The filter of product\n                types.\n        \"\"\"\n\n        self._products_proxy_model.set_product_type_filters(\n            product_type_filters\n        )\n\n    def set_enable_grouping(self, enable_grouping):\n        self._products_model.set_enable_grouping(enable_grouping)\n\n    def get_selected_merged_products(self):\n        return self._selected_merged_products\n\n    def get_selected_version_info(self):\n        return self._selected_versions_info\n\n    def refresh(self):\n        self._refresh_model()\n\n    def _set_sitesync_visibility(self, sitesync_enabled):\n        self._products_view.setColumnHidden(\n            self._products_model.sitesync_avail_col,\n            not sitesync_enabled\n        )\n\n    def _fill_version_editor(self):\n        model = self._products_proxy_model\n        index_queue = collections.deque()\n        for row in range(model.rowCount()):\n            index_queue.append((row, None))\n\n        version_col = self._products_model.version_col\n        while index_queue:\n            (row, parent_index) = index_queue.popleft()\n            args = [row, 0]\n            if parent_index is not None:\n                args.append(parent_index)\n            index = model.index(*args)\n            rows = model.rowCount(index)\n            for row in range(rows):\n                index_queue.append((row, index))\n\n            product_id = model.data(index, PRODUCT_ID_ROLE)\n            if product_id is not None:\n                args[1] = version_col\n                v_index = model.index(*args)\n                self._products_view.openPersistentEditor(v_index)\n\n    def _on_refresh(self):\n        self._fill_version_editor()\n        self.refreshed.emit()\n\n    def _on_rows_inserted(self):\n        self._fill_version_editor()\n\n    def _on_rows_moved(self):\n        self._fill_version_editor()\n\n    def _refresh_model(self):\n        self._products_model.refresh(\n            self._selected_project_name,\n            self._selected_folder_ids\n        )\n\n    def _on_context_menu(self, point):\n        selection_model = self._products_view.selectionModel()\n        model = self._products_view.model()\n        project_name = self._products_model.get_last_project_name()\n\n        version_ids = set()\n        indexes_queue = collections.deque()\n        indexes_queue.extend(selection_model.selectedIndexes())\n        while indexes_queue:\n            index = indexes_queue.popleft()\n            for row in range(model.rowCount(index)):\n                child_index = model.index(row, 0, index)\n                indexes_queue.append(child_index)\n            version_id = model.data(index, VERSION_ID_ROLE)\n            if version_id is not None:\n                version_ids.add(version_id)\n\n        action_items = self._controller.get_action_items(\n            project_name, version_ids, \"version\"\n        )\n\n        # Prepare global point where to show the menu\n        global_point = self._products_view.mapToGlobal(point)\n\n        result = show_actions_menu(\n            action_items,\n            global_point,\n            len(version_ids) == 1,\n            self\n        )\n        action_item, options = result\n        if action_item is None or options is None:\n            return\n\n        self._controller.trigger_action_item(\n            identifier=action_item.identifier,\n            project_name=project_name,\n            selected_ids=version_ids,\n            selected_entity_type=\"version\",\n            data=action_item.data,\n            options=options,\n            form_values={},\n        )\n\n    def _on_selection_change(self):\n        selected_merged_products = []\n        selection_model = self._products_view.selectionModel()\n        model = self._products_view.model()\n        indexes_queue = collections.deque()\n        indexes_queue.extend(selection_model.selectedIndexes())\n\n        # Helper for 'version_items' to avoid duplicated items\n        all_product_ids = set()\n        selected_version_ids = set()\n        # Version items contains information about selected version items\n        selected_versions_info = []\n        while indexes_queue:\n            index = indexes_queue.popleft()\n            if index.column() != 0:\n                continue\n\n            group_type = model.data(index, GROUP_TYPE_ROLE)\n            if group_type is None:\n                product_id = model.data(index, PRODUCT_ID_ROLE)\n                # Skip duplicates - when group and item are selected the item\n                #   would be in the loop multiple times\n                if product_id in all_product_ids:\n                    continue\n\n                all_product_ids.add(product_id)\n\n                version_id = model.data(index, VERSION_ID_ROLE)\n                selected_version_ids.add(version_id)\n\n                thumbnail_id = model.data(index, VERSION_THUMBNAIL_ID_ROLE)\n                selected_versions_info.append({\n                    \"folder_id\": model.data(index, FOLDER_ID_ROLE),\n                    \"product_id\": product_id,\n                    \"version_id\": version_id,\n                    \"thumbnail_id\": thumbnail_id,\n                })\n                continue\n\n            if group_type == 0:\n                for row in range(model.rowCount(index)):\n                    child_index = model.index(row, 0, index)\n                    indexes_queue.append(child_index)\n                continue\n\n            if group_type != 1:\n                continue\n\n            item_folder_ids = set()\n            for row in range(model.rowCount(index)):\n                child_index = model.index(row, 0, index)\n                indexes_queue.append(child_index)\n\n                folder_id = model.data(child_index, FOLDER_ID_ROLE)\n                item_folder_ids.add(folder_id)\n\n            if not item_folder_ids:\n                continue\n\n            hex_color = model.data(index, MERGED_COLOR_ROLE)\n            item_data = {\n                \"color\": hex_color,\n                \"folder_ids\": item_folder_ids\n            }\n            selected_merged_products.append(item_data)\n\n        prev_selected_merged_products = self._selected_merged_products\n        self._selected_merged_products = selected_merged_products\n        self._selected_versions_info = selected_versions_info\n\n        if selected_merged_products != prev_selected_merged_products:\n            self.merged_products_selection_changed.emit()\n        self.selection_changed.emit()\n        self._controller.set_selected_versions(selected_version_ids)\n\n    def _on_version_change(self):\n        self._on_selection_change()\n\n    def _on_version_delegate_change(self, product_id, version_id):\n        self._products_model.set_product_version(product_id, version_id)\n\n    def _on_folders_selection_change(self, event):\n        project_name = event[\"project_name\"]\n        sitesync_enabled = self._controller.is_sitesync_enabled(\n            project_name\n        )\n        self._set_sitesync_visibility(sitesync_enabled)\n        self._selected_project_name = project_name\n        self._selected_folder_ids = event[\"folder_ids\"]\n        self._refresh_model()\n        self._update_folders_label_visible()\n\n    def _update_folders_label_visible(self):\n        folders_label_hidden = len(self._selected_folder_ids) &lt;= 1\n        self._products_view.setColumnHidden(\n            self._products_model.folders_label_col,\n            folders_label_hidden\n        )\n\n    def _on_products_refresh_finished(self, event):\n        if event[\"sender\"] != PRODUCTS_MODEL_SENDER_NAME:\n            self._refresh_model()\n\n    def _on_group_changed(self, event):\n        if event[\"project_name\"] != self._selected_project_name:\n            return\n        folder_ids = event[\"folder_ids\"]\n        if not set(folder_ids).intersection(set(self._selected_folder_ids)):\n            return\n        self.refresh()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/ui/products_widget.html#client.ayon_core.tools.loader.ui.products_widget.ProductsWidget.set_name_filter","title":"<code>set_name_filter(name)</code>","text":"<p>Set filter of product name.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The string filter.</p> required Source code in <code>client/ayon_core/tools/loader/ui/products_widget.py</code> <pre><code>def set_name_filter(self, name):\n    \"\"\"Set filter of product name.\n\n    Args:\n        name (str): The string filter.\n\n    \"\"\"\n    self._products_proxy_model.setFilterFixedString(name)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/ui/products_widget.html#client.ayon_core.tools.loader.ui.products_widget.ProductsWidget.set_product_type_filter","title":"<code>set_product_type_filter(product_type_filters)</code>","text":"<p>Parameters:</p> Name Type Description Default <code>product_type_filters</code> <code>dict[str, bool]</code> <p>The filter of product types.</p> required Source code in <code>client/ayon_core/tools/loader/ui/products_widget.py</code> <pre><code>def set_product_type_filter(self, product_type_filters):\n    \"\"\"\n\n    Args:\n        product_type_filters (dict[str, bool]): The filter of product\n            types.\n    \"\"\"\n\n    self._products_proxy_model.set_product_type_filters(\n        product_type_filters\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/ui/products_widget.html#client.ayon_core.tools.loader.ui.products_widget.ProductsWidget.set_statuses_filter","title":"<code>set_statuses_filter(status_names)</code>","text":"<p>Set filter of version statuses.</p> <p>Parameters:</p> Name Type Description Default <code>status_names</code> <code>list[str]</code> <p>The list of status names.</p> required Source code in <code>client/ayon_core/tools/loader/ui/products_widget.py</code> <pre><code>def set_statuses_filter(self, status_names):\n    \"\"\"Set filter of version statuses.\n\n    Args:\n        status_names (list[str]): The list of status names.\n\n    \"\"\"\n    self._version_delegate.set_statuses_filter(status_names)\n    self._products_proxy_model.set_statuses_filter(status_names)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/ui/products_widget.html#client.ayon_core.tools.loader.ui.products_widget.ProductsWidget.set_tasks_filter","title":"<code>set_tasks_filter(task_ids)</code>","text":"<p>Set filter of version tasks.</p> <p>Parameters:</p> Name Type Description Default <code>task_ids</code> <code>set[str]</code> <p>Task ids.</p> required Source code in <code>client/ayon_core/tools/loader/ui/products_widget.py</code> <pre><code>def set_tasks_filter(self, task_ids):\n    \"\"\"Set filter of version tasks.\n\n    Args:\n        task_ids (set[str]): Task ids.\n\n    \"\"\"\n    self._version_delegate.set_tasks_filter(task_ids)\n    self._products_proxy_model.set_tasks_filter(task_ids)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/ui/repres_widget.html","title":"repres_widget","text":""},{"location":"autoapi/client/ayon_core/tools/loader/ui/search_bar.html","title":"search_bar","text":""},{"location":"autoapi/client/ayon_core/tools/loader/ui/search_bar.html#client.ayon_core.tools.loader.ui.search_bar.CloseButton","title":"<code>CloseButton</code>","text":"<p>               Bases: <code>SquareButton</code></p> <p>Close button for search item display widget.</p> Source code in <code>client/ayon_core/tools/loader/ui/search_bar.py</code> <pre><code>class CloseButton(SquareButton):\n    \"\"\"Close button for search item display widget.\"\"\"\n    _icon = None\n    _hover_color = None\n\n    def __init__(self, *args, **kwargs):\n        super().__init__(*args, **kwargs)\n        if self.__class__._icon is None:\n            self.__class__._icon = get_qt_icon(\n                MaterialSymbolsIcon(\"close\", color=\"#FFFFFF\")\n            )\n        if self.__class__._hover_color is None:\n            color = get_objected_colors(\"bg-view-selection-hover\")\n            self.__class__._hover_color = color.get_qcolor()\n\n        self.setIcon(self.__class__._icon)\n\n    def paintEvent(self, event):\n        \"\"\"Override paint event to draw a close button.\"\"\"\n        painter = QtWidgets.QStylePainter(self)\n        painter.setRenderHint(QtGui.QPainter.Antialiasing)\n        option = QtWidgets.QStyleOptionButton()\n        self.initStyleOption(option)\n        icon = self.icon()\n        size = min(self.width(), self.height())\n        rect = QtCore.QRect(0, 0, size, size)\n        rect.adjust(2, 2, -2, -2)\n        painter.setPen(QtCore.Qt.NoPen)\n        bg_color = QtCore.Qt.transparent\n        if option.state &amp; QtWidgets.QStyle.State_MouseOver:\n            bg_color = self._hover_color\n\n        painter.setBrush(bg_color)\n        painter.setClipRect(event.rect())\n        painter.drawEllipse(rect)\n        rect.adjust(2, 2, -2, -2)\n        icon.paint(painter, rect)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/ui/search_bar.html#client.ayon_core.tools.loader.ui.search_bar.CloseButton.paintEvent","title":"<code>paintEvent(event)</code>","text":"<p>Override paint event to draw a close button.</p> Source code in <code>client/ayon_core/tools/loader/ui/search_bar.py</code> <pre><code>def paintEvent(self, event):\n    \"\"\"Override paint event to draw a close button.\"\"\"\n    painter = QtWidgets.QStylePainter(self)\n    painter.setRenderHint(QtGui.QPainter.Antialiasing)\n    option = QtWidgets.QStyleOptionButton()\n    self.initStyleOption(option)\n    icon = self.icon()\n    size = min(self.width(), self.height())\n    rect = QtCore.QRect(0, 0, size, size)\n    rect.adjust(2, 2, -2, -2)\n    painter.setPen(QtCore.Qt.NoPen)\n    bg_color = QtCore.Qt.transparent\n    if option.state &amp; QtWidgets.QStyle.State_MouseOver:\n        bg_color = self._hover_color\n\n    painter.setBrush(bg_color)\n    painter.setClipRect(event.rect())\n    painter.drawEllipse(rect)\n    rect.adjust(2, 2, -2, -2)\n    icon.paint(painter, rect)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/ui/search_bar.html#client.ayon_core.tools.loader.ui.search_bar.FilterDefinition","title":"<code>FilterDefinition</code>  <code>dataclass</code>","text":"<p>Search bar definition.</p> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Name of the definition.</p> <code>title</code> <code>str</code> <p>Title of the search bar.</p> <code>icon</code> <code>str</code> <p>Icon name for the search bar.</p> <code>placeholder</code> <code>str</code> <p>Placeholder text for the search bar.</p> Source code in <code>client/ayon_core/tools/loader/ui/search_bar.py</code> <pre><code>@dataclass\nclass FilterDefinition:\n    \"\"\"Search bar definition.\n\n    Attributes:\n        name (str): Name of the definition.\n        title (str): Title of the search bar.\n        icon (str): Icon name for the search bar.\n        placeholder (str): Placeholder text for the search bar.\n\n    \"\"\"\n    name: str\n    title: str\n    filter_type: str\n    icon: IconBase | dict[str, Any] | None = None\n    placeholder: Optional[str] = None\n    items: Optional[list[dict[str, str]]] = None\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/ui/search_bar.html#client.ayon_core.tools.loader.ui.search_bar.FilterItemButton","title":"<code>FilterItemButton</code>","text":"<p>               Bases: <code>BaseClickableFrame</code></p> Source code in <code>client/ayon_core/tools/loader/ui/search_bar.py</code> <pre><code>class FilterItemButton(BaseClickableFrame):\n    filter_requested = QtCore.Signal(str)\n\n    def __init__(\n        self,\n        filter_def: FilterDefinition,\n        parent: QtWidgets.QWidget,\n    ):\n        super().__init__(parent)\n\n        self._filter_def = filter_def\n\n        title_widget = QtWidgets.QLabel(filter_def.title, self)\n\n        main_layout = QtWidgets.QHBoxLayout(self)\n        main_layout.setContentsMargins(5, 5, 5, 5)\n        main_layout.addWidget(title_widget, 1)\n\n        self._title_widget = title_widget\n\n    def _mouse_release_callback(self):\n        \"\"\"Handle mouse release event to emit filter request.\"\"\"\n        self.filter_requested.emit(self._filter_def.name)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/ui/search_bar.html#client.ayon_core.tools.loader.ui.search_bar.FilterValueItemButton","title":"<code>FilterValueItemButton</code>","text":"<p>               Bases: <code>BaseClickableFrame</code></p> Source code in <code>client/ayon_core/tools/loader/ui/search_bar.py</code> <pre><code>class FilterValueItemButton(BaseClickableFrame):\n    selected = QtCore.Signal(str)\n\n    def __init__(self, widget_id, value, icon, color, parent):\n        super().__init__(parent)\n\n        title_widget = QtWidgets.QLabel(str(value), self)\n        if color:\n            title_widget.setStyleSheet(f\"color: {color};\")\n\n        main_layout = QtWidgets.QHBoxLayout(self)\n        main_layout.setContentsMargins(5, 5, 5, 5)\n        main_layout.addWidget(title_widget, 1)\n\n        self._icon_widget = None\n        self._title_widget = title_widget\n        self._main_layout = main_layout\n        self._selected = False\n        self._value = value\n        self._widget_id = widget_id\n\n        if icon:\n            self.set_icon(icon)\n\n    def set_icon(self, icon: dict[str, Any]):\n        \"\"\"Set the icon for the widget.\"\"\"\n        icon = get_qt_icon(icon)\n        pixmap = icon.pixmap(64, 64)\n        if self._icon_widget is None:\n            self._icon_widget = PixmapLabel(pixmap, self)\n            self._main_layout.insertWidget(0, self._icon_widget, 0)\n        else:\n            self._icon_widget.setPixmap(pixmap)\n\n    def get_value(self):\n        return self._value\n\n    def set_selected(self, selected: bool):\n        \"\"\"Set the selection state of the widget.\"\"\"\n        if self._selected == selected:\n            return\n        self._selected = selected\n        self.setProperty(\"selected\", \"1\" if selected else \"\")\n        self.style().polish(self)\n\n    def is_selected(self) -&gt; bool:\n        return self._selected\n\n    def _mouse_release_callback(self):\n        \"\"\"Handle mouse release event to emit filter request.\"\"\"\n        self.selected.emit(self._widget_id)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/ui/search_bar.html#client.ayon_core.tools.loader.ui.search_bar.FilterValueItemButton.set_icon","title":"<code>set_icon(icon)</code>","text":"<p>Set the icon for the widget.</p> Source code in <code>client/ayon_core/tools/loader/ui/search_bar.py</code> <pre><code>def set_icon(self, icon: dict[str, Any]):\n    \"\"\"Set the icon for the widget.\"\"\"\n    icon = get_qt_icon(icon)\n    pixmap = icon.pixmap(64, 64)\n    if self._icon_widget is None:\n        self._icon_widget = PixmapLabel(pixmap, self)\n        self._main_layout.insertWidget(0, self._icon_widget, 0)\n    else:\n        self._icon_widget.setPixmap(pixmap)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/ui/search_bar.html#client.ayon_core.tools.loader.ui.search_bar.FilterValueItemButton.set_selected","title":"<code>set_selected(selected)</code>","text":"<p>Set the selection state of the widget.</p> Source code in <code>client/ayon_core/tools/loader/ui/search_bar.py</code> <pre><code>def set_selected(self, selected: bool):\n    \"\"\"Set the selection state of the widget.\"\"\"\n    if self._selected == selected:\n        return\n    self._selected = selected\n    self.setProperty(\"selected\", \"1\" if selected else \"\")\n    self.style().polish(self)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/ui/search_bar.html#client.ayon_core.tools.loader.ui.search_bar.FilterValueItemsView","title":"<code>FilterValueItemsView</code>","text":"<p>               Bases: <code>QWidget</code></p> Source code in <code>client/ayon_core/tools/loader/ui/search_bar.py</code> <pre><code>class FilterValueItemsView(QtWidgets.QWidget):\n    value_changed = QtCore.Signal()\n    close_requested = QtCore.Signal()\n    back_requested = QtCore.Signal()\n\n    def __init__(self, parent):\n        super().__init__(parent)\n\n        filter_input = QtWidgets.QLineEdit(self)\n        filter_input.setPlaceholderText(\"Filter items...\")\n\n        # Timeout is used to delay the filter focus change on 'showEvent'\n        # - the focus is changed to something else if is not delayed\n        filter_timeout = QtCore.QTimer(self)\n        filter_timeout.setSingleShot(True)\n        filter_timeout.setInterval(20)\n\n        scroll_area = QtWidgets.QScrollArea(self)\n        scroll_area.setObjectName(\"ScrollArea\")\n        srcoll_viewport = scroll_area.viewport()\n        srcoll_viewport.setContentsMargins(0, 0, 0, 0)\n        scroll_area.setWidgetResizable(True)\n        scroll_area.setMinimumHeight(20)\n        scroll_area.setMaximumHeight(400)\n\n        content_widget = QtWidgets.QWidget(scroll_area)\n        content_widget.setObjectName(\"ContentWidget\")\n\n        content_layout = QtWidgets.QVBoxLayout(content_widget)\n        content_layout.setContentsMargins(0, 0, 0, 0)\n\n        scroll_area.setWidget(content_widget)\n\n        btns_sep = SeparatorWidget(size=1, parent=self)\n        btns_widget = QtWidgets.QWidget(self)\n        btns_widget.setAttribute(QtCore.Qt.WA_TranslucentBackground, True)\n\n        back_btn = QtWidgets.QPushButton(\"Back\", btns_widget)\n        back_btn.setObjectName(\"BackButton\")\n        back_btn.setIcon(get_qt_icon(\n            MaterialSymbolsIcon(\"arrow_back\")\n        ))\n\n        select_all_btn = QtWidgets.QPushButton(\"Select all\", btns_widget)\n        clear_btn = QtWidgets.QPushButton(\"Clear\", btns_widget)\n        swap_btn = QtWidgets.QPushButton(\"Invert\", btns_widget)\n\n        confirm_btn = QtWidgets.QPushButton(\"Confirm\", btns_widget)\n        confirm_btn.setObjectName(\"ConfirmButton\")\n        confirm_btn.clicked.connect(self.close_requested)\n\n        btns_layout = QtWidgets.QHBoxLayout(btns_widget)\n        btns_layout.setContentsMargins(0, 0, 0, 0)\n        btns_layout.addWidget(back_btn, 0)\n        btns_layout.addStretch(1)\n        btns_layout.addWidget(select_all_btn, 0)\n        btns_layout.addWidget(clear_btn, 0)\n        btns_layout.addWidget(swap_btn, 0)\n        btns_layout.addStretch(1)\n        btns_layout.addWidget(confirm_btn, 0)\n\n        main_layout = QtWidgets.QVBoxLayout(self)\n        main_layout.setContentsMargins(0, 0, 0, 0)\n        main_layout.addWidget(filter_input, 0)\n        main_layout.addWidget(scroll_area)\n        main_layout.addWidget(btns_sep, 0)\n        main_layout.addWidget(btns_widget, 0)\n\n        filter_timeout.timeout.connect(self._on_filter_timeout)\n        filter_input.textChanged.connect(self._on_filter_change)\n        filter_input.returnPressed.connect(self.close_requested)\n        back_btn.clicked.connect(self.back_requested)\n        select_all_btn.clicked.connect(self._on_select_all)\n        clear_btn.clicked.connect(self._on_clear_selection)\n        swap_btn.clicked.connect(self._on_swap_selection)\n\n        self._filter_timeout = filter_timeout\n        self._filter_input = filter_input\n        self._btns_widget = btns_widget\n        self._multiselection = False\n        self._content_layout = content_layout\n        self._last_selected_widget = None\n        self._widgets_by_id = {}\n\n    def showEvent(self, event):\n        super().showEvent(event)\n        self._filter_timeout.start()\n\n    def keyPressEvent(self, event):\n        if event.key() in (QtCore.Qt.Key_Enter, QtCore.Qt.Key_Return):\n            event.accept()\n            self.close_requested.emit()\n            return\n\n        if event.key() in (\n            QtCore.Qt.Key_Backtab,\n            QtCore.Qt.Key_Backspace,\n        ):\n            event.accept()\n            set_line_edit_focus(self._filter_input, backspace=True)\n            return\n\n        valid_modifiers = event.modifiers() in (\n            QtCore.Qt.NoModifier,\n            QtCore.Qt.ShiftModifier,\n        )\n        if valid_modifiers and event.key() not in (\n            QtCore.Qt.Key_Escape,\n            QtCore.Qt.Key_Tab,\n            QtCore.Qt.Key_Return,\n        ):\n            text = event.text()\n            if text:\n                event.accept()\n                set_line_edit_focus(self._filter_input, append_text=text)\n                return\n\n        super().keyPressEvent(event)\n\n    def set_value(self, value):\n        current_value = self.get_value()\n        if self._multiselection:\n            if value is None:\n                value = []\n            if not isinstance(value, list):\n                value = [value]\n            for widget in self._widgets_by_id.values():\n                selected = widget.get_value() in value\n                if selected and self._last_selected_widget is None:\n                    self._last_selected_widget = widget\n                widget.set_selected(selected)\n\n            if value != current_value:\n                self.value_changed.emit()\n            return\n\n        if isinstance(value, list):\n            if len(value) &gt; 0:\n                value = value[0]\n            else:\n                value = None\n\n        if value is None:\n            widget = next(iter(self._widgets_by_id.values()))\n            value = widget.get_value()\n\n        self._last_selected_widget = None\n        for widget in self._widgets_by_id.values():\n            selected = widget.get_value() in value\n            widget.set_selected(selected)\n            if selected:\n                self._last_selected_widget = widget\n\n        if self._last_selected_widget is None:\n            widget = next(iter(self._widgets_by_id.values()))\n            self._last_selected_widget = widget\n            widget.set_selected(True)\n\n        if value != current_value:\n            self.value_changed.emit()\n\n    def set_multiselection(self, multiselection: bool):\n        self._multiselection = multiselection\n        if not self._widgets_by_id or not self._multiselection:\n            self._btns_widget.setVisible(False)\n        else:\n            self._btns_widget.setVisible(True)\n\n        if not self._widgets_by_id or self._multiselection:\n            return\n\n        value_changed = False\n        if self._last_selected_widget is None:\n            value_changed = True\n            self._last_selected_widget = next(\n                iter(self._widgets_by_id.values())\n            )\n        for widget in self._widgets_by_id.values():\n            widget.set_selected(widget is self._last_selected_widget)\n\n        if value_changed:\n            self.value_changed.emit()\n\n    def get_value(self):\n        \"\"\"Get the value from the items view.\"\"\"\n        if self._multiselection:\n            return [\n                widget.get_value()\n                for widget in self._widgets_by_id.values()\n                if widget.is_selected()\n            ]\n        if self._last_selected_widget is not None:\n            return self._last_selected_widget.get_value()\n        return None\n\n    def set_items(self, items: list[dict[str, Any]]):\n        while self._content_layout.count() &gt; 0:\n            item = self._content_layout.takeAt(0)\n            widget = item.widget()\n            if widget is not None:\n                widget.setVisible(False)\n                widget.deleteLater()\n\n        self._widgets_by_id = {}\n        self._last_selected_widget = None\n        # Change filter\n        self._filter_input.setText(\"\")\n\n        for item in items:\n            widget_id = uuid.uuid4().hex\n            widget = FilterValueItemButton(\n                widget_id,\n                item[\"value\"],\n                item.get(\"icon\"),\n                item.get(\"color\"),\n                self,\n            )\n            widget.selected.connect(self._on_item_clicked)\n            self._widgets_by_id[widget_id] = widget\n            self._content_layout.addWidget(widget, 0)\n\n        if self._content_layout.count() == 0:\n            empty_label = QtWidgets.QLabel(\n                \"No items to select from...\", self\n            )\n            self._btns_widget.setVisible(False)\n            self._filter_input.setVisible(False)\n            self._content_layout.addWidget(empty_label, 0)\n        else:\n            self._filter_input.setVisible(True)\n            self._btns_widget.setVisible(self._multiselection)\n        self._content_layout.addStretch(1)\n\n    def _on_filter_timeout(self):\n        self._filter_input.setFocus()\n\n    def _on_filter_change(self, text):\n        text = text.lower()\n        for widget in self._widgets_by_id.values():\n            visible = not text or text in widget.get_value().lower()\n            widget.setVisible(visible)\n\n    def _on_select_all(self):\n        changed = False\n        for widget in self._widgets_by_id.values():\n            if not widget.is_selected():\n                changed = True\n                widget.set_selected(True)\n                if self._last_selected_widget is None:\n                    self._last_selected_widget = widget\n\n        if changed:\n            self.value_changed.emit()\n\n    def _on_swap_selection(self):\n        self._last_selected_widget = None\n        for widget in self._widgets_by_id.values():\n            selected = not widget.is_selected()\n            widget.set_selected(selected)\n            if selected and self._last_selected_widget is None:\n                self._last_selected_widget = widget\n\n        self.value_changed.emit()\n\n    def _on_clear_selection(self):\n        self._last_selected_widget = None\n        changed = False\n        for widget in self._widgets_by_id.values():\n            if widget.is_selected():\n                changed = True\n                widget.set_selected(False)\n\n        if changed:\n            self.value_changed.emit()\n\n    def _on_item_clicked(self, widget_id):\n        widget = self._widgets_by_id.get(widget_id)\n        if widget is None:\n            return\n\n        previous_widget = self._last_selected_widget\n        self._last_selected_widget = widget\n        if self._multiselection:\n            widget.set_selected(not widget.is_selected())\n        else:\n            widget.set_selected(True)\n            if previous_widget is not None:\n                previous_widget.set_selected(False)\n        self.value_changed.emit()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/ui/search_bar.html#client.ayon_core.tools.loader.ui.search_bar.FilterValueItemsView.get_value","title":"<code>get_value()</code>","text":"<p>Get the value from the items view.</p> Source code in <code>client/ayon_core/tools/loader/ui/search_bar.py</code> <pre><code>def get_value(self):\n    \"\"\"Get the value from the items view.\"\"\"\n    if self._multiselection:\n        return [\n            widget.get_value()\n            for widget in self._widgets_by_id.values()\n            if widget.is_selected()\n        ]\n    if self._last_selected_widget is not None:\n        return self._last_selected_widget.get_value()\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/ui/search_bar.html#client.ayon_core.tools.loader.ui.search_bar.FilterValuePopup","title":"<code>FilterValuePopup</code>","text":"<p>               Bases: <code>QWidget</code></p> Source code in <code>client/ayon_core/tools/loader/ui/search_bar.py</code> <pre><code>class FilterValuePopup(QtWidgets.QWidget):\n    value_changed = QtCore.Signal(str)\n    closed = QtCore.Signal(str)\n    back_requested = QtCore.Signal(str)\n\n    def __init__(self, parent):\n        super().__init__(parent)\n        self.setWindowFlags(QtCore.Qt.Popup | QtCore.Qt.FramelessWindowHint)\n        self.setAttribute(QtCore.Qt.WA_TranslucentBackground, True)\n\n        shadow_frame = QtWidgets.QFrame(self)\n        shadow_frame.setObjectName(\"ShadowFrame\")\n\n        wrapper = QtWidgets.QWidget(self)\n        wrapper.setObjectName(\"PopupWrapper\")\n\n        text_input = FilterValueTextInput(wrapper)\n        text_input.setVisible(False)\n\n        items_view = FilterValueItemsView(wrapper)\n        items_view.setVisible(False)\n\n        wraper_layout = QtWidgets.QVBoxLayout(wrapper)\n        wraper_layout.setContentsMargins(5, 5, 5, 5)\n        wraper_layout.setSpacing(5)\n        wraper_layout.addWidget(text_input, 0)\n        wraper_layout.addWidget(items_view, 0)\n\n        main_layout = QtWidgets.QHBoxLayout(self)\n        main_layout.setContentsMargins(2, 2, 2, 2)\n        main_layout.addWidget(wrapper)\n\n        text_input.value_changed.connect(self._text_changed)\n        text_input.close_requested.connect(self._close_requested)\n        text_input.back_requested.connect(self._back_requested)\n\n        items_view.value_changed.connect(self._selection_changed)\n        items_view.close_requested.connect(self._close_requested)\n        items_view.back_requested.connect(self._back_requested)\n\n        shadow_frame.stackUnder(wrapper)\n\n        self._shadow_frame = shadow_frame\n        self._wrapper = wrapper\n        self._wrapper_layout = wraper_layout\n        self._text_input = text_input\n        self._items_view = items_view\n\n        self._active_widget = None\n        self._filter_name = None\n        self._preferred_width = None\n\n    def set_preferred_width(self, width: int):\n        self._preferred_width = width\n\n    def sizeHint(self):\n        sh = super().sizeHint()\n        if self._preferred_width is not None:\n            sh.setWidth(self._preferred_width)\n        return sh\n\n    def set_text_filter(self, text: str):\n        if self._active_widget is None:\n            return\n\n        if self._active_widget is self._text_input:\n            self._active_widget.set_text_filter(text)\n\n    def set_filter_item(\n        self,\n        filter_def: FilterDefinition,\n        value,\n    ):\n        self._text_input.setVisible(False)\n        self._items_view.setVisible(False)\n        self._filter_name = filter_def.name\n        self._active_widget = None\n        if filter_def.filter_type == \"text\":\n            if filter_def.items:\n                if value is None:\n                    value = filter_def.items[0][\"value\"]\n                self._active_widget = self._items_view\n                self._items_view.set_items(filter_def.items)\n                self._items_view.set_multiselection(False)\n                self._items_view.set_value(value)\n            else:\n                if value is None:\n                    value = \"\"\n                self._text_input.set_placeholder_text(\n                    filter_def.placeholder or \"\"\n                )\n                self._text_input.set_value(value)\n                self._active_widget = self._text_input\n\n        elif filter_def.filter_type == \"list\":\n            if value is None:\n                value = []\n            self._items_view.set_items(filter_def.items)\n            self._items_view.set_multiselection(True)\n            self._items_view.set_value(value)\n            self._active_widget = self._items_view\n\n        if self._active_widget is not None:\n            self._active_widget.setVisible(True)\n\n    def showEvent(self, event):\n        super().showEvent(event)\n        if self._active_widget is not None:\n            self._active_widget.setFocus()\n        self._update_shadow()\n\n    def closeEvent(self, event):\n        super().closeEvent(event)\n        self.closed.emit(self._filter_name)\n\n    def hideEvent(self, event):\n        super().hideEvent(event)\n        self.closed.emit(self._filter_name)\n\n    def resizeEvent(self, event):\n        super().resizeEvent(event)\n        self._update_shadow()\n\n    def _update_shadow(self):\n        geo = self.geometry()\n        geo.moveTopLeft(QtCore.QPoint(0, 0))\n        self._shadow_frame.setGeometry(geo)\n\n    def get_value(self):\n        \"\"\"Get the value from the active widget.\"\"\"\n        if self._active_widget is self._text_input:\n            return self._text_input.get_value()\n        elif self._active_widget is self._items_view:\n            return self._active_widget.get_value()\n        return None\n\n    def _text_changed(self):\n        \"\"\"Handle text change in the text input.\"\"\"\n        if self._active_widget is self._text_input:\n            # Emit value changed signal if text input is active\n            self.value_changed.emit(self._filter_name)\n\n    def _selection_changed(self):\n        self.value_changed.emit(self._filter_name)\n\n    def _close_requested(self):\n        self.close()\n\n    def _back_requested(self):\n        self.back_requested.emit(self._filter_name)\n        self.close()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/ui/search_bar.html#client.ayon_core.tools.loader.ui.search_bar.FilterValuePopup.get_value","title":"<code>get_value()</code>","text":"<p>Get the value from the active widget.</p> Source code in <code>client/ayon_core/tools/loader/ui/search_bar.py</code> <pre><code>def get_value(self):\n    \"\"\"Get the value from the active widget.\"\"\"\n    if self._active_widget is self._text_input:\n        return self._text_input.get_value()\n    elif self._active_widget is self._items_view:\n        return self._active_widget.get_value()\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/ui/search_bar.html#client.ayon_core.tools.loader.ui.search_bar.FiltersBar","title":"<code>FiltersBar</code>","text":"<p>               Bases: <code>BaseClickableFrame</code></p> Source code in <code>client/ayon_core/tools/loader/ui/search_bar.py</code> <pre><code>class FiltersBar(BaseClickableFrame):\n    filter_changed = QtCore.Signal(str)\n\n    def __init__(self, parent):\n        super().__init__(parent)\n\n        search_icon = get_qt_icon(\n            MaterialSymbolsIcon(\"search\", color=\"#FFFFFF\")\n        )\n        search_btn = SquareButton(self)\n        search_btn.setIcon(search_icon)\n        search_btn.setFlat(True)\n        search_btn.setObjectName(\"SearchButton\")\n\n        # Wrapper is used to avoid squashing filters\n        # - the filters are positioned manually without layout\n        filters_wrap = QtWidgets.QWidget(self)\n        filters_wrap.setAttribute(QtCore.Qt.WA_TranslucentBackground, True)\n\n        # Widget where set filters are displayed\n        filters_widget = QtWidgets.QWidget(filters_wrap)\n        filters_widget.setAttribute(QtCore.Qt.WA_TranslucentBackground, True)\n        filters_layout = QtWidgets.QHBoxLayout(filters_widget)\n        filters_layout.setContentsMargins(0, 0, 0, 0)\n        filters_layout.addStretch(1)\n\n        main_layout = QtWidgets.QHBoxLayout(self)\n        main_layout.setContentsMargins(4, 4, 4, 4)\n        main_layout.setSpacing(5)\n        main_layout.addWidget(search_btn, 0)\n        main_layout.addWidget(filters_wrap, 1)\n\n        filters_popup = FiltersPopup(self)\n        filter_value_popup = FilterValuePopup(self)\n\n        search_btn.clicked.connect(self._on_filters_request)\n        filters_popup.text_filter_requested.connect(\n            self._on_text_filter_request\n        )\n\n        self._search_btn = search_btn\n        self._filters_wrap = filters_wrap\n        self._filters_widget = filters_widget\n        self._filters_layout = filters_layout\n        self._widgets_by_name = {}\n        self._filter_defs_by_name = {}\n        self._filters_popup = filters_popup\n        self._filter_value_popup = filter_value_popup\n\n    def showEvent(self, event):\n        super().showEvent(event)\n        self._update_filters_geo()\n\n    def resizeEvent(self, event):\n        super().resizeEvent(event)\n        self._update_filters_geo()\n\n    def wheelEvent(self, event):\n        scroll_speed = 15\n        diff = event.angleDelta().y() / 120.0\n        pos_x = self._filters_widget.pos().x()\n        if diff &gt; 0:\n            pos_x = min(0, pos_x + scroll_speed)\n            self._filters_widget.move(pos_x, 0)\n            return\n\n        rect = self._filters_wrap.rect()\n        size_hint = self._filters_widget.sizeHint()\n        if size_hint.width() &lt; rect.width():\n            return\n        pos_x = max(\n            pos_x - scroll_speed, rect.width() - size_hint.width()\n        )\n        self._filters_widget.move(pos_x, 0)\n\n    def show_filters_popup(self):\n        filter_defs = [\n            filter_def\n            for filter_def in self._filter_defs_by_name.values()\n            if filter_def.name not in self._widgets_by_name\n        ]\n        filters_popup = FiltersPopup(self)\n        filters_popup.filter_requested.connect(self._on_filter_request)\n        filters_popup.text_filter_requested.connect(\n            self._on_text_filter_request\n        )\n        filters_popup.set_filter_items(filter_defs)\n        filters_popup.set_preferred_width(self.width())\n\n        old_popup, self._filters_popup = self._filters_popup, filters_popup\n\n        self._filter_value_popup.setVisible(False)\n        old_popup.setVisible(False)\n        old_popup.deleteLater()\n\n        self._show_popup(filters_popup)\n\n    def set_search_items(self, filter_defs: list[FilterDefinition]):\n        self._filter_defs_by_name = {\n            filter_def.name: filter_def\n            for filter_def in filter_defs\n        }\n\n    def get_filter_value(self, name: str) -&gt; Optional[Any]:\n        \"\"\"Get the value of a filter by its name.\"\"\"\n        item_widget = self._widgets_by_name.get(name)\n        if item_widget is not None:\n            value = item_widget.get_value()\n            if isinstance(value, list) and len(value) == 0:\n                return None\n            return value\n        return None\n\n    def set_filter_value(self, name: str, value: Any):\n        \"\"\"Set the value of a filter by its name.\"\"\"\n        if name not in self._filter_defs_by_name:\n            return\n\n        item_widget = self._widgets_by_name.get(name)\n        if item_widget is None:\n            self.add_item(name)\n            item_widget = self._widgets_by_name.get(name)\n\n        item_widget.set_value(value)\n        self.filter_changed.emit(name)\n\n    def add_item(self, name: str):\n        \"\"\"Add a new item to the search bar.\n\n        Args:\n            name (str): Search definition name.\n\n        \"\"\"\n        filter_def = self._filter_defs_by_name.get(name)\n        if filter_def is None:\n            return\n\n        item_widget = self._widgets_by_name.get(name)\n        if item_widget is not None:\n            return\n\n        item_widget = SearchItemDisplayWidget(\n            filter_def,\n            parent=self._filters_widget,\n        )\n        item_widget.edit_requested.connect(self._on_filter_request)\n        item_widget.close_requested.connect(self._on_item_close_requested)\n        self._widgets_by_name[name] = item_widget\n        idx = self._filters_layout.count() - 1\n        self._filters_layout.insertWidget(idx, item_widget, 0)\n\n    def _update_filters_geo(self):\n        geo = self._filters_wrap.geometry()\n        geo.moveTopLeft(QtCore.QPoint(0, 0))\n        # Arbitrary width\n        geo.setWidth(3000)\n\n        self._filters_widget.setGeometry(geo)\n\n    def _reposition_filters_widget(self):\n        rect = self._filters_wrap.rect()\n        size_hint = self._filters_widget.sizeHint()\n        if size_hint.width() &lt; rect.width():\n            self._filters_widget.move(0, 0)\n            return\n        pos_x = self._filters_widget.pos().x()\n        pos_x = max(\n            pos_x, rect.width() - size_hint.width()\n        )\n        self._filters_widget.move(pos_x, 0)\n\n    def _mouse_release_callback(self):\n        self.show_filters_popup()\n\n    def _on_filters_request(self):\n        self.show_filters_popup()\n\n    def _on_text_filter_request(self, text: str):\n        if \"product_name\" not in self._filter_defs_by_name:\n            return\n\n        self._on_filter_request(\"product_name\")\n        self._filter_value_popup.set_text_filter(text)\n\n    def _on_filter_request(self, filter_name: str):\n        \"\"\"Handle filter request from the popup.\"\"\"\n        self.add_item(filter_name)\n        self._filters_popup.hide()\n        filter_def = self._filter_defs_by_name.get(filter_name)\n        widget = self._widgets_by_name.get(filter_name)\n        value = None\n        if widget is not None:\n            value = widget.get_value()\n\n        filter_value_popup = FilterValuePopup(self)\n        filter_value_popup.set_preferred_width(self.width())\n        filter_value_popup.set_filter_item(filter_def, value)\n        filter_value_popup.value_changed.connect(self._on_filter_value_change)\n        filter_value_popup.closed.connect(self._on_filter_value_closed)\n        filter_value_popup.back_requested.connect(self._on_filter_value_back)\n\n        old_popup, self._filter_value_popup = (\n            self._filter_value_popup, filter_value_popup\n        )\n\n        old_popup.setVisible(False)\n        old_popup.deleteLater()\n\n        self._filters_popup.setVisible(False)\n\n        self._show_popup(filter_value_popup)\n        self._on_filter_value_change(filter_def.name)\n\n    def _show_popup(self, popup: QtWidgets.QWidget):\n        \"\"\"Show a popup widget.\"\"\"\n        geo = self.geometry()\n        bl_pos_g = self.mapToGlobal(QtCore.QPoint(0, geo.height() + 5))\n        popup.show()\n        popup.move(bl_pos_g.x(), bl_pos_g.y())\n        popup.raise_()\n\n    def _on_filter_value_change(self, name):\n        value = self._filter_value_popup.get_value()\n        item_widget = self._widgets_by_name.get(name)\n        item_widget.set_value(value)\n        self.filter_changed.emit(name)\n\n    def _on_filter_value_closed(self, name):\n        widget = self._widgets_by_name.get(name)\n        if widget is None:\n            return\n\n        value = widget.get_value()\n        if not value:\n            self._on_item_close_requested(name)\n\n    def _on_filter_value_back(self, name):\n        self._on_filter_value_closed(name)\n        self.show_filters_popup()\n\n    def _on_item_close_requested(self, name):\n        widget = self._widgets_by_name.pop(name, None)\n        if widget is None:\n            return\n        idx = self._filters_layout.indexOf(widget)\n        if idx &gt; -1:\n            self._filters_layout.takeAt(idx)\n            widget.setVisible(False)\n            widget.deleteLater()\n            self.filter_changed.emit(name)\n\n        self._reposition_filters_widget()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/ui/search_bar.html#client.ayon_core.tools.loader.ui.search_bar.FiltersBar.add_item","title":"<code>add_item(name)</code>","text":"<p>Add a new item to the search bar.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Search definition name.</p> required Source code in <code>client/ayon_core/tools/loader/ui/search_bar.py</code> <pre><code>def add_item(self, name: str):\n    \"\"\"Add a new item to the search bar.\n\n    Args:\n        name (str): Search definition name.\n\n    \"\"\"\n    filter_def = self._filter_defs_by_name.get(name)\n    if filter_def is None:\n        return\n\n    item_widget = self._widgets_by_name.get(name)\n    if item_widget is not None:\n        return\n\n    item_widget = SearchItemDisplayWidget(\n        filter_def,\n        parent=self._filters_widget,\n    )\n    item_widget.edit_requested.connect(self._on_filter_request)\n    item_widget.close_requested.connect(self._on_item_close_requested)\n    self._widgets_by_name[name] = item_widget\n    idx = self._filters_layout.count() - 1\n    self._filters_layout.insertWidget(idx, item_widget, 0)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/ui/search_bar.html#client.ayon_core.tools.loader.ui.search_bar.FiltersBar.get_filter_value","title":"<code>get_filter_value(name)</code>","text":"<p>Get the value of a filter by its name.</p> Source code in <code>client/ayon_core/tools/loader/ui/search_bar.py</code> <pre><code>def get_filter_value(self, name: str) -&gt; Optional[Any]:\n    \"\"\"Get the value of a filter by its name.\"\"\"\n    item_widget = self._widgets_by_name.get(name)\n    if item_widget is not None:\n        value = item_widget.get_value()\n        if isinstance(value, list) and len(value) == 0:\n            return None\n        return value\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/ui/search_bar.html#client.ayon_core.tools.loader.ui.search_bar.FiltersBar.set_filter_value","title":"<code>set_filter_value(name, value)</code>","text":"<p>Set the value of a filter by its name.</p> Source code in <code>client/ayon_core/tools/loader/ui/search_bar.py</code> <pre><code>def set_filter_value(self, name: str, value: Any):\n    \"\"\"Set the value of a filter by its name.\"\"\"\n    if name not in self._filter_defs_by_name:\n        return\n\n    item_widget = self._widgets_by_name.get(name)\n    if item_widget is None:\n        self.add_item(name)\n        item_widget = self._widgets_by_name.get(name)\n\n    item_widget.set_value(value)\n    self.filter_changed.emit(name)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/ui/search_bar.html#client.ayon_core.tools.loader.ui.search_bar.SearchItemDisplayWidget","title":"<code>SearchItemDisplayWidget</code>","text":"<p>               Bases: <code>BaseClickableFrame</code></p> <p>Widget displaying a set filter in the bar.</p> Source code in <code>client/ayon_core/tools/loader/ui/search_bar.py</code> <pre><code>class SearchItemDisplayWidget(BaseClickableFrame):\n    \"\"\"Widget displaying a set filter in the bar.\"\"\"\n    close_requested = QtCore.Signal(str)\n    edit_requested = QtCore.Signal(str)\n\n    def __init__(\n        self,\n        filter_def: FilterDefinition,\n        parent: QtWidgets.QWidget,\n    ):\n        super().__init__(parent)\n\n        self._filter_def = filter_def\n\n        title_widget = QtWidgets.QLabel(f\"{filter_def.title}:\", self)\n\n        value_wrapper = QtWidgets.QWidget(self)\n        value_wrapper.setAttribute(QtCore.Qt.WA_TranslucentBackground, True)\n        value_widget = QtWidgets.QLabel(value_wrapper)\n        value_widget.setObjectName(\"ValueWidget\")\n        value_widget.setText(\"\")\n        value_layout = QtWidgets.QVBoxLayout(value_wrapper)\n        value_layout.setContentsMargins(2, 2, 2, 2)\n        value_layout.addWidget(value_widget)\n\n        close_btn = CloseButton(self)\n\n        main_layout = QtWidgets.QHBoxLayout(self)\n        main_layout.setContentsMargins(4, 0, 0, 0)\n        main_layout.setSpacing(0)\n        main_layout.addWidget(title_widget, 0)\n        main_layout.addWidget(value_wrapper, 0)\n        main_layout.addWidget(close_btn, 0)\n\n        close_btn.clicked.connect(self._on_remove_clicked)\n\n        self._value_wrapper = value_wrapper\n        self._value_widget = value_widget\n        self._value = None\n\n    def set_value(self, value: \"str | list[str]\"):\n        text = \"\"\n        ellide = True\n        if value is None:\n            pass\n        elif isinstance(value, str):\n            text = value\n        elif len(value) == 1:\n            text = value[0]\n        elif len(value) &gt; 1:\n            ellide = False\n            text = f\"Items: {len(value)}\"\n\n        if ellide and len(text) &gt; 9:\n            text = text[:9] + \"...\"\n\n        text = \" \" + text + \" \"\n\n        self._value = copy.deepcopy(value)\n        self._value_widget.setText(text)\n\n    def get_value(self):\n        return copy.deepcopy(self._value)\n\n    def _on_remove_clicked(self):\n        self.close_requested.emit(self._filter_def.name)\n\n    def _mouse_release_callback(self):\n        self.edit_requested.emit(self._filter_def.name)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/ui/tasks_widget.html","title":"tasks_widget","text":""},{"location":"autoapi/client/ayon_core/tools/loader/ui/tasks_widget.html#client.ayon_core.tools.loader.ui.tasks_widget.LoaderTasksQtModel","title":"<code>LoaderTasksQtModel</code>","text":"<p>               Bases: <code>TasksQtModel</code></p> Source code in <code>client/ayon_core/tools/loader/ui/tasks_widget.py</code> <pre><code>class LoaderTasksQtModel(TasksQtModel):\n    column_labels = [\n        \"Task name\",\n        \"Task type\",\n        \"Folder\"\n    ]\n\n    def __init__(self, controller):\n        super().__init__(controller)\n\n        self._items_by_id = {}\n        self._groups_by_name = {}\n        self._last_folder_ids = set()\n        # This item is used to be able filter versions without any task\n        # - do not mismatch with '_empty_tasks_item' item from 'TasksQtModel'\n        self._no_tasks_item = None\n\n    def refresh(self):\n        \"\"\"Refresh tasks for selected folders.\"\"\"\n\n        self._refresh(self._last_project_name, self._last_folder_ids)\n\n    def set_context(self, project_name, folder_ids):\n        self._refresh(project_name, folder_ids)\n\n    # Mark some functions from 'TasksQtModel' as not implemented\n    def get_index_by_name(self, task_name):\n        raise NotImplementedError(\n            \"Method 'get_index_by_name' is not implemented.\"\n        )\n\n    def get_last_folder_id(self):\n        raise NotImplementedError(\n            \"Method 'get_last_folder_id' is not implemented.\"\n        )\n\n    def flags(self, index):\n        if index.column() != 0:\n            index = self.index(index.row(), 0, index.parent())\n        return super().flags(index)\n\n    def _get_no_tasks_item(self):\n        if self._no_tasks_item is None:\n            item = QtGui.QStandardItem(\"No task\")\n            icon = get_qt_icon(MaterialSymbolsIcon(\n                \"indeterminate_check_box\",\n                color=get_default_entity_icon_color(),\n            ))\n            item.setData(icon, QtCore.Qt.DecorationRole)\n            item.setData(NO_TASKS_ID, ITEM_ID_ROLE)\n            item.setEditable(False)\n            self._no_tasks_item = item\n        return self._no_tasks_item\n\n    def _refresh(self, project_name, folder_ids):\n        self._is_refreshing = True\n        self._last_project_name = project_name\n        self._last_folder_ids = folder_ids\n        if not folder_ids:\n            self._add_invalid_selection_item()\n            self._current_refresh_thread = None\n            self._is_refreshing = False\n            self.refreshed.emit()\n            return\n\n        thread_id = hashlib.sha256(\n            \"|\".join(sorted(folder_ids)).encode()\n        ).hexdigest()\n        thread = self._refresh_threads.get(thread_id)\n        if thread is not None:\n            self._current_refresh_thread = thread\n            return\n        thread = RefreshThread(\n            thread_id,\n            self._thread_getter,\n            project_name,\n            folder_ids\n        )\n        self._current_refresh_thread = thread\n        self._refresh_threads[thread.id] = thread\n        thread.refresh_finished.connect(self._on_refresh_thread)\n        thread.start()\n\n    def _thread_getter(self, project_name, folder_ids):\n        task_items = self._controller.get_task_items(\n            project_name, folder_ids, sender=TASKS_MODEL_SENDER_NAME\n        )\n        task_type_items = {}\n        if hasattr(self._controller, \"get_task_type_items\"):\n            task_type_items = self._controller.get_task_type_items(\n                project_name, sender=TASKS_MODEL_SENDER_NAME\n            )\n        folder_ids = {\n            task_item.parent_id\n            for task_item in task_items\n        }\n        folder_labels_by_id = self._controller.get_folder_labels(\n            project_name, folder_ids\n        )\n        return task_items, task_type_items, folder_labels_by_id\n\n    def _on_refresh_thread(self, thread_id):\n        \"\"\"Callback when refresh thread is finished.\n\n        Technically can be running multiple refresh threads at the same time,\n        to avoid using values from wrong thread, we check if thread id is\n        current refresh thread id.\n\n        Tasks are stored by name, so if a folder has same task name as\n        previously selected folder it keeps the selection.\n\n        Args:\n            thread_id (str): Thread id.\n        \"\"\"\n\n        # Make sure to remove thread from '_refresh_threads' dict\n        thread = self._refresh_threads.pop(thread_id)\n        if (\n            self._current_refresh_thread is None\n            or thread_id != self._current_refresh_thread.id\n        ):\n            return\n\n        self._fill_data_from_thread(thread)\n\n        root_item = self.invisibleRootItem()\n        self._has_content = root_item.rowCount() &gt; 0\n        self._current_refresh_thread = None\n        self._is_refreshing = False\n        self.refreshed.emit()\n\n    def _clear_items(self):\n        self._items_by_id = {}\n        self._groups_by_name = {}\n        super()._clear_items()\n\n    def _fill_data_from_thread(self, thread):\n        task_items, task_type_items, folder_labels_by_id = thread.get_result()\n        # Task items are refreshed\n        if task_items is None:\n            return\n\n        # No tasks are available on folder\n        if not task_items:\n            self._add_empty_task_item()\n            return\n        self._remove_invalid_items()\n\n        task_type_item_by_name = {\n            task_type_item.name: task_type_item\n            for task_type_item in task_type_items\n        }\n        task_type_icon_cache = {}\n        current_ids = set()\n        items_by_name = collections.defaultdict(list)\n        for task_item in task_items:\n            task_id = task_item.task_id\n            current_ids.add(task_id)\n            item = self._items_by_id.get(task_id)\n            if item is None:\n                item = QtGui.QStandardItem()\n                item.setColumnCount(self.columnCount())\n                item.setEditable(False)\n                self._items_by_id[task_id] = item\n\n            icon = self._get_task_item_icon(\n                task_item,\n                task_type_item_by_name,\n                task_type_icon_cache\n            )\n            name = task_item.name\n            folder_id = task_item.parent_id\n            folder_label = folder_labels_by_id.get(folder_id)\n\n            item.setData(name, QtCore.Qt.DisplayRole)\n            item.setData(name, ITEM_NAME_ROLE)\n            item.setData(task_item.id, ITEM_ID_ROLE)\n            item.setData(task_item.task_type, TASK_TYPE_ROLE)\n            item.setData(folder_id, PARENT_ID_ROLE)\n            item.setData(folder_label, FOLDER_LABEL_ROLE)\n            item.setData(icon, QtCore.Qt.DecorationRole)\n\n            items_by_name[name].append(item)\n\n        root_item = self.invisibleRootItem()\n\n        # Make sure item is not parented\n        # - this is laziness to avoid re-parenting items which does\n        #   complicate the code with no benefit\n        queue = collections.deque()\n        queue.append((None, root_item))\n        while queue:\n            (parent, item) = queue.popleft()\n            if not item.hasChildren():\n                if parent:\n                    parent.takeRow(item.row())\n                continue\n\n            for row in range(item.rowCount()):\n                queue.append((item, item.child(row, 0)))\n\n            queue.append((parent, item))\n\n        used_group_names = set()\n        new_root_items = [\n            self._get_no_tasks_item()\n        ]\n        for name, items in items_by_name.items():\n            if len(items) == 1:\n                new_root_items.extend(items)\n                continue\n\n            used_group_names.add(name)\n            group_item = self._groups_by_name.get(name)\n            if group_item is None:\n                group_item = QtGui.QStandardItem()\n                group_item.setData(name, QtCore.Qt.DisplayRole)\n                group_item.setEditable(False)\n                group_item.setColumnCount(self.columnCount())\n                self._groups_by_name[name] = group_item\n\n            # Use icon from first item\n            first_item_icon = items[0].data(QtCore.Qt.DecorationRole)\n            task_ids = [\n                item.data(ITEM_ID_ROLE)\n                for item in items\n            ]\n\n            group_item.setData(first_item_icon, QtCore.Qt.DecorationRole)\n            group_item.setData(\"|\".join(task_ids), ITEM_ID_ROLE)\n\n            group_item.appendRows(items)\n\n            new_root_items.append(group_item)\n\n        # Remove unused caches\n        for task_id in set(self._items_by_id) - current_ids:\n            self._items_by_id.pop(task_id)\n\n        for name in set(self._groups_by_name) - used_group_names:\n            self._groups_by_name.pop(name)\n\n        if new_root_items:\n            root_item.appendRows(new_root_items)\n\n    def data(self, index, role=None):\n        if not index.isValid():\n            return None\n\n        if role is None:\n            role = QtCore.Qt.DisplayRole\n\n        col = index.column()\n        if col != 0:\n            index = self.index(index.row(), 0, index.parent())\n\n        if col == 1:\n            if role == QtCore.Qt.DisplayRole:\n                role = TASK_TYPE_ROLE\n            else:\n                return None\n\n        if col == 2:\n            if role == QtCore.Qt.DisplayRole:\n                role = FOLDER_LABEL_ROLE\n            else:\n                return None\n\n        return super().data(index, role)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/ui/tasks_widget.html#client.ayon_core.tools.loader.ui.tasks_widget.LoaderTasksQtModel.refresh","title":"<code>refresh()</code>","text":"<p>Refresh tasks for selected folders.</p> Source code in <code>client/ayon_core/tools/loader/ui/tasks_widget.py</code> <pre><code>def refresh(self):\n    \"\"\"Refresh tasks for selected folders.\"\"\"\n\n    self._refresh(self._last_project_name, self._last_folder_ids)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/ui/tasks_widget.html#client.ayon_core.tools.loader.ui.tasks_widget.LoaderTasksWidget","title":"<code>LoaderTasksWidget</code>","text":"<p>               Bases: <code>QWidget</code></p> Source code in <code>client/ayon_core/tools/loader/ui/tasks_widget.py</code> <pre><code>class LoaderTasksWidget(QtWidgets.QWidget):\n    refreshed = QtCore.Signal()\n\n    def __init__(self, controller, parent):\n        super().__init__(parent)\n\n        tasks_view = DeselectableTreeView(self)\n        tasks_view.setSelectionMode(\n            QtWidgets.QAbstractItemView.ExtendedSelection\n        )\n        tasks_view.setIndentation(0)\n\n        tasks_model = LoaderTasksQtModel(controller)\n        tasks_proxy_model = LoaderTasksProxyModel()\n        tasks_proxy_model.setSourceModel(tasks_model)\n        tasks_proxy_model.setSortCaseSensitivity(QtCore.Qt.CaseInsensitive)\n\n        tasks_view.setModel(tasks_proxy_model)\n        # Hide folder column by default\n        tasks_view.setColumnHidden(2, True)\n\n        main_layout = QtWidgets.QHBoxLayout(self)\n        main_layout.setContentsMargins(0, 0, 0, 0)\n        main_layout.addWidget(tasks_view, 1)\n\n        controller.register_event_callback(\n            \"selection.folders.changed\",\n            self._on_folders_selection_changed,\n        )\n\n        selection_model = tasks_view.selectionModel()\n        selection_model.selectionChanged.connect(self._on_selection_change)\n\n        tasks_model.refreshed.connect(self._on_model_refresh)\n\n        self._controller = controller\n        self._tasks_view = tasks_view\n        self._tasks_model = tasks_model\n        self._tasks_proxy_model = tasks_proxy_model\n\n        self._fisrt_show = True\n\n    def showEvent(self, event):\n        super().showEvent(event)\n        if self._fisrt_show:\n            self._fisrt_show = False\n            header_widget = self._tasks_view.header()\n            header_widget.resizeSection(0, 200)\n\n    def set_name_filter(self, name):\n        \"\"\"Set filter of folder name.\n\n        Args:\n            name (str): The string filter.\n\n        \"\"\"\n        self._tasks_proxy_model.setFilterFixedString(name)\n        if name:\n            self._tasks_view.expandAll()\n\n    def set_task_ids_filter(self, task_ids: Optional[list[str]]):\n        \"\"\"Set filter of folder ids.\n\n        Args:\n            task_ids (list[str]): The list of folder ids.\n\n        \"\"\"\n        self._tasks_proxy_model.set_task_ids_filter(task_ids)\n\n    def refresh(self):\n        self._tasks_model.refresh()\n\n    def _clear(self):\n        self._tasks_model.clear()\n\n    def _on_folders_selection_changed(self, event):\n        project_name = event[\"project_name\"]\n        folder_ids = event[\"folder_ids\"]\n        self._tasks_view.setColumnHidden(2, len(folder_ids) == 1)\n        self._tasks_model.set_context(project_name, folder_ids)\n\n    def _on_model_refresh(self):\n        self._tasks_proxy_model.sort(0)\n        self.refreshed.emit()\n\n    def _get_selected_item_ids(self):\n        selection_model = self._tasks_view.selectionModel()\n        item_ids = set()\n        for index in selection_model.selectedIndexes():\n            item_id = index.data(ITEM_ID_ROLE)\n            if item_id is None:\n                continue\n            if item_id == NO_TASKS_ID:\n                item_ids.add(None)\n            else:\n                item_ids |= set(item_id.split(\"|\"))\n        return item_ids\n\n    def _on_selection_change(self):\n        item_ids = self._get_selected_item_ids()\n        self._controller.set_selected_tasks(item_ids)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/ui/tasks_widget.html#client.ayon_core.tools.loader.ui.tasks_widget.LoaderTasksWidget.set_name_filter","title":"<code>set_name_filter(name)</code>","text":"<p>Set filter of folder name.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The string filter.</p> required Source code in <code>client/ayon_core/tools/loader/ui/tasks_widget.py</code> <pre><code>def set_name_filter(self, name):\n    \"\"\"Set filter of folder name.\n\n    Args:\n        name (str): The string filter.\n\n    \"\"\"\n    self._tasks_proxy_model.setFilterFixedString(name)\n    if name:\n        self._tasks_view.expandAll()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/ui/tasks_widget.html#client.ayon_core.tools.loader.ui.tasks_widget.LoaderTasksWidget.set_task_ids_filter","title":"<code>set_task_ids_filter(task_ids)</code>","text":"<p>Set filter of folder ids.</p> <p>Parameters:</p> Name Type Description Default <code>task_ids</code> <code>list[str]</code> <p>The list of folder ids.</p> required Source code in <code>client/ayon_core/tools/loader/ui/tasks_widget.py</code> <pre><code>def set_task_ids_filter(self, task_ids: Optional[list[str]]):\n    \"\"\"Set filter of folder ids.\n\n    Args:\n        task_ids (list[str]): The list of folder ids.\n\n    \"\"\"\n    self._tasks_proxy_model.set_task_ids_filter(task_ids)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/loader/ui/window.html","title":"window","text":""},{"location":"autoapi/client/ayon_core/tools/publisher/index.html","title":"publisher","text":""},{"location":"autoapi/client/ayon_core/tools/publisher/abstract.html","title":"abstract","text":""},{"location":"autoapi/client/ayon_core/tools/publisher/abstract.html#client.ayon_core.tools.publisher.abstract.AbstractPublisherBackend","title":"<code>AbstractPublisherBackend</code>","text":"<p>               Bases: <code>AbstractPublisherCommon</code></p> Source code in <code>client/ayon_core/tools/publisher/abstract.py</code> <pre><code>class AbstractPublisherBackend(AbstractPublisherCommon):\n    @abstractmethod\n    def is_headless(self) -&gt; bool:\n        \"\"\"Controller is in headless mode.\n\n        Notes:\n            Not sure if this method is relevant in UI tool?\n\n        Returns:\n            bool: Headless mode.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_host(self) -&gt; AbstractHost:\n        pass\n\n    @abstractmethod\n    def get_create_context(self) -&gt; CreateContext:\n        pass\n\n    @abstractmethod\n    def get_task_item_by_name(\n        self,\n        project_name: str,\n        folder_id: str,\n        task_name: str,\n        sender: Optional[str] = None\n    ) -&gt; Union[TaskItem, None]:\n        pass\n\n    @abstractmethod\n    def get_project_settings(self, project_name: str | None) -&gt; dict:\n        pass\n\n    @abstractmethod\n    def get_project_entity(\n        self, project_name: str\n    ) -&gt; Union[Dict[str, Any], None]:\n        pass\n\n    @abstractmethod\n    def get_folder_entity(\n        self, project_name: str, folder_id: str\n    ) -&gt; Union[Dict[str, Any], None]:\n        pass\n\n    @abstractmethod\n    def get_folder_item_by_path(\n        self, project_name: str, folder_path: str\n    ) -&gt; Union[FolderItem, None]:\n        pass\n\n    @abstractmethod\n    def get_task_entity(\n        self, project_name: str, task_id: str\n    ) -&gt; Union[Dict[str, Any], None]:\n        pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/abstract.html#client.ayon_core.tools.publisher.abstract.AbstractPublisherBackend.is_headless","title":"<code>is_headless()</code>  <code>abstractmethod</code>","text":"<p>Controller is in headless mode.</p> Notes <p>Not sure if this method is relevant in UI tool?</p> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>Headless mode.</p> Source code in <code>client/ayon_core/tools/publisher/abstract.py</code> <pre><code>@abstractmethod\ndef is_headless(self) -&gt; bool:\n    \"\"\"Controller is in headless mode.\n\n    Notes:\n        Not sure if this method is relevant in UI tool?\n\n    Returns:\n        bool: Headless mode.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/abstract.html#client.ayon_core.tools.publisher.abstract.AbstractPublisherCommon","title":"<code>AbstractPublisherCommon</code>","text":"<p>               Bases: <code>ABC</code></p> Source code in <code>client/ayon_core/tools/publisher/abstract.py</code> <pre><code>class AbstractPublisherCommon(ABC):\n    @abstractmethod\n    def register_event_callback(self, topic, callback):\n        \"\"\"Register event callback.\n\n        Listen for events with given topic.\n\n        Args:\n            topic (str): Name of topic.\n            callback (Callable): Callback that will be called when event\n                is triggered.\n        \"\"\"\n\n        pass\n\n    @abstractmethod\n    def emit_event(\n        self, topic: str,\n        data: Optional[Dict[str, Any]] = None,\n        source: Optional[str] = None\n    ):\n        \"\"\"Emit event.\n\n        Args:\n            topic (str): Event topic used for callbacks filtering.\n            data (Optional[dict[str, Any]]): Event data.\n            source (Optional[str]): Event source.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def emit_card_message(\n        self,\n        message: str,\n        message_type: Optional[str] = CardMessageTypes.standard\n    ):\n        \"\"\"Emit a card message which can have a lifetime.\n\n        This is for UI purposes. Method can be extended to more arguments\n        in future e.g. different message timeout or type (color).\n\n        Args:\n            message (str): Message that will be shown.\n            message_type (Optional[str]): Message type.\n        \"\"\"\n\n        pass\n\n    @abstractmethod\n    def get_current_project_name(self) -&gt; Union[str, None]:\n        \"\"\"Current context project name.\n\n        Returns:\n            str: Name of project.\n        \"\"\"\n\n        pass\n\n    @abstractmethod\n    def get_current_folder_path(self) -&gt; Union[str, None]:\n        \"\"\"Current context folder path.\n\n        Returns:\n            Union[str, None]: Folder path.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_current_task_name(self) -&gt; Union[str, None]:\n        \"\"\"Current context task name.\n\n        Returns:\n            Union[str, None]: Name of task.\n        \"\"\"\n\n        pass\n\n    @abstractmethod\n    def host_context_has_changed(self) -&gt; bool:\n        \"\"\"Host context changed after last reset.\n\n        'CreateContext' has this option available using 'context_has_changed'.\n\n        Returns:\n            bool: Context has changed.\n        \"\"\"\n\n        pass\n\n    @abstractmethod\n    def reset(self):\n        \"\"\"Reset whole controller.\n\n        This should reset create context, publish context and all variables\n        that are related to it.\n        \"\"\"\n\n        pass\n\n    @abstractmethod\n    def get_comment_def(self) -&gt; CommentDef:\n        \"\"\"Get comment attribute definition.\n\n        This can define how the Comment field should behave, like having\n        a minimum amount of required characters before being allowed to\n        publish.\n\n        \"\"\"\n        pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/abstract.html#client.ayon_core.tools.publisher.abstract.AbstractPublisherCommon.emit_card_message","title":"<code>emit_card_message(message, message_type=CardMessageTypes.standard)</code>  <code>abstractmethod</code>","text":"<p>Emit a card message which can have a lifetime.</p> <p>This is for UI purposes. Method can be extended to more arguments in future e.g. different message timeout or type (color).</p> <p>Parameters:</p> Name Type Description Default <code>message</code> <code>str</code> <p>Message that will be shown.</p> required <code>message_type</code> <code>Optional[str]</code> <p>Message type.</p> <code>standard</code> Source code in <code>client/ayon_core/tools/publisher/abstract.py</code> <pre><code>@abstractmethod\ndef emit_card_message(\n    self,\n    message: str,\n    message_type: Optional[str] = CardMessageTypes.standard\n):\n    \"\"\"Emit a card message which can have a lifetime.\n\n    This is for UI purposes. Method can be extended to more arguments\n    in future e.g. different message timeout or type (color).\n\n    Args:\n        message (str): Message that will be shown.\n        message_type (Optional[str]): Message type.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/abstract.html#client.ayon_core.tools.publisher.abstract.AbstractPublisherCommon.emit_event","title":"<code>emit_event(topic, data=None, source=None)</code>  <code>abstractmethod</code>","text":"<p>Emit event.</p> <p>Parameters:</p> Name Type Description Default <code>topic</code> <code>str</code> <p>Event topic used for callbacks filtering.</p> required <code>data</code> <code>Optional[dict[str, Any]]</code> <p>Event data.</p> <code>None</code> <code>source</code> <code>Optional[str]</code> <p>Event source.</p> <code>None</code> Source code in <code>client/ayon_core/tools/publisher/abstract.py</code> <pre><code>@abstractmethod\ndef emit_event(\n    self, topic: str,\n    data: Optional[Dict[str, Any]] = None,\n    source: Optional[str] = None\n):\n    \"\"\"Emit event.\n\n    Args:\n        topic (str): Event topic used for callbacks filtering.\n        data (Optional[dict[str, Any]]): Event data.\n        source (Optional[str]): Event source.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/abstract.html#client.ayon_core.tools.publisher.abstract.AbstractPublisherCommon.get_comment_def","title":"<code>get_comment_def()</code>  <code>abstractmethod</code>","text":"<p>Get comment attribute definition.</p> <p>This can define how the Comment field should behave, like having a minimum amount of required characters before being allowed to publish.</p> Source code in <code>client/ayon_core/tools/publisher/abstract.py</code> <pre><code>@abstractmethod\ndef get_comment_def(self) -&gt; CommentDef:\n    \"\"\"Get comment attribute definition.\n\n    This can define how the Comment field should behave, like having\n    a minimum amount of required characters before being allowed to\n    publish.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/abstract.html#client.ayon_core.tools.publisher.abstract.AbstractPublisherCommon.get_current_folder_path","title":"<code>get_current_folder_path()</code>  <code>abstractmethod</code>","text":"<p>Current context folder path.</p> <p>Returns:</p> Type Description <code>Union[str, None]</code> <p>Union[str, None]: Folder path.</p> Source code in <code>client/ayon_core/tools/publisher/abstract.py</code> <pre><code>@abstractmethod\ndef get_current_folder_path(self) -&gt; Union[str, None]:\n    \"\"\"Current context folder path.\n\n    Returns:\n        Union[str, None]: Folder path.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/abstract.html#client.ayon_core.tools.publisher.abstract.AbstractPublisherCommon.get_current_project_name","title":"<code>get_current_project_name()</code>  <code>abstractmethod</code>","text":"<p>Current context project name.</p> <p>Returns:</p> Name Type Description <code>str</code> <code>Union[str, None]</code> <p>Name of project.</p> Source code in <code>client/ayon_core/tools/publisher/abstract.py</code> <pre><code>@abstractmethod\ndef get_current_project_name(self) -&gt; Union[str, None]:\n    \"\"\"Current context project name.\n\n    Returns:\n        str: Name of project.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/abstract.html#client.ayon_core.tools.publisher.abstract.AbstractPublisherCommon.get_current_task_name","title":"<code>get_current_task_name()</code>  <code>abstractmethod</code>","text":"<p>Current context task name.</p> <p>Returns:</p> Type Description <code>Union[str, None]</code> <p>Union[str, None]: Name of task.</p> Source code in <code>client/ayon_core/tools/publisher/abstract.py</code> <pre><code>@abstractmethod\ndef get_current_task_name(self) -&gt; Union[str, None]:\n    \"\"\"Current context task name.\n\n    Returns:\n        Union[str, None]: Name of task.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/abstract.html#client.ayon_core.tools.publisher.abstract.AbstractPublisherCommon.host_context_has_changed","title":"<code>host_context_has_changed()</code>  <code>abstractmethod</code>","text":"<p>Host context changed after last reset.</p> <p>'CreateContext' has this option available using 'context_has_changed'.</p> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>Context has changed.</p> Source code in <code>client/ayon_core/tools/publisher/abstract.py</code> <pre><code>@abstractmethod\ndef host_context_has_changed(self) -&gt; bool:\n    \"\"\"Host context changed after last reset.\n\n    'CreateContext' has this option available using 'context_has_changed'.\n\n    Returns:\n        bool: Context has changed.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/abstract.html#client.ayon_core.tools.publisher.abstract.AbstractPublisherCommon.register_event_callback","title":"<code>register_event_callback(topic, callback)</code>  <code>abstractmethod</code>","text":"<p>Register event callback.</p> <p>Listen for events with given topic.</p> <p>Parameters:</p> Name Type Description Default <code>topic</code> <code>str</code> <p>Name of topic.</p> required <code>callback</code> <code>Callable</code> <p>Callback that will be called when event is triggered.</p> required Source code in <code>client/ayon_core/tools/publisher/abstract.py</code> <pre><code>@abstractmethod\ndef register_event_callback(self, topic, callback):\n    \"\"\"Register event callback.\n\n    Listen for events with given topic.\n\n    Args:\n        topic (str): Name of topic.\n        callback (Callable): Callback that will be called when event\n            is triggered.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/abstract.html#client.ayon_core.tools.publisher.abstract.AbstractPublisherCommon.reset","title":"<code>reset()</code>  <code>abstractmethod</code>","text":"<p>Reset whole controller.</p> <p>This should reset create context, publish context and all variables that are related to it.</p> Source code in <code>client/ayon_core/tools/publisher/abstract.py</code> <pre><code>@abstractmethod\ndef reset(self):\n    \"\"\"Reset whole controller.\n\n    This should reset create context, publish context and all variables\n    that are related to it.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/abstract.html#client.ayon_core.tools.publisher.abstract.AbstractPublisherFrontend","title":"<code>AbstractPublisherFrontend</code>","text":"<p>               Bases: <code>AbstractPublisherCommon</code></p> Source code in <code>client/ayon_core/tools/publisher/abstract.py</code> <pre><code>class AbstractPublisherFrontend(AbstractPublisherCommon):\n    @abstractmethod\n    def get_window_subtitle(self) -&gt; Optional[str]:\n        \"\"\"Get window subtitle.\n\n        Returns:\n            Optional[str]: Window subtitle.\n\n        \"\"\"\n\n    @abstractmethod\n    def register_event_callback(self, topic: str, callback: Callable):\n        pass\n\n    @abstractmethod\n    def is_host_valid(self) -&gt; bool:\n        \"\"\"Host is valid for creation part.\n\n        Host must have implemented certain functionality to be able to create\n            in Publisher tool.\n\n        Returns:\n            bool: Host can handle creation of instances.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_context_title(self) -&gt; Union[str, None]:\n        \"\"\"Get context title for artist shown at the top of main window.\n\n        Returns:\n            Union[str, None]: Context title for window or None. In case of None\n                a warning is displayed (not nice for artists).\n        \"\"\"\n\n        pass\n\n    @abstractmethod\n    def get_task_items_by_folder_paths(\n        self, folder_paths: Iterable[str]\n    ) -&gt; Dict[str, List[TaskItem]]:\n        pass\n\n    @abstractmethod\n    def get_folder_items(\n        self, project_name: str, sender: Optional[str] = None\n    ) -&gt; List[FolderItem]:\n        pass\n\n    @abstractmethod\n    def get_task_items(\n        self, project_name: str, folder_id: str, sender: Optional[str] = None\n    ) -&gt; List[TaskItem]:\n        pass\n\n    @abstractmethod\n    def get_folder_type_items(\n        self, project_name: str, sender: Optional[str] = None\n    ) -&gt; List[FolderTypeItem]:\n        pass\n\n    @abstractmethod\n    def get_task_type_items(\n        self, project_name: str, sender: Optional[str] = None\n    ) -&gt; List[TaskTypeItem]:\n        pass\n\n    @abstractmethod\n    def are_folder_paths_valid(self, folder_paths: Iterable[str]) -&gt; bool:\n        \"\"\"Folder paths do exist in project.\n\n        Args:\n            folder_paths (Iterable[str]): List of folder paths.\n\n        Returns:\n            bool: All folder paths exist in project.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_folder_id_from_path(self, folder_path: str) -&gt; Optional[str]:\n        \"\"\"Get folder id from folder path.\"\"\"\n        pass\n\n    @abstractmethod\n    def get_my_tasks_entity_ids(\n        self, project_name: str\n    ) -&gt; dict[str, list[str]]:\n        \"\"\"Get entity ids for my tasks.\n\n        Args:\n            project_name (str): Project name.\n\n        Returns:\n            dict[str, list[str]]: Folder and task ids.\n\n        \"\"\"\n        pass\n\n    # --- Create ---\n    @abstractmethod\n    def get_creator_items(self) -&gt; Dict[str, \"CreatorItem\"]:\n        \"\"\"Creator items by identifier.\n\n        Returns:\n            Dict[str, CreatorItem]: Creator items that will be shown to user.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_creator_item_by_id(\n        self, identifier: str\n    ) -&gt; Optional[\"CreatorItem\"]:\n        \"\"\"Get creator item by identifier.\n\n        Args:\n            identifier (str): Create plugin identifier.\n\n        Returns:\n            Optional[CreatorItem]: Creator item or None.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_creator_icon(\n        self, identifier: str\n    ) -&gt; Union[str, Dict[str, Any], None]:\n        \"\"\"Receive creator's icon by identifier.\n\n        Todos:\n            Icon should be part of 'CreatorItem'.\n\n        Args:\n            identifier (str): Creator's identifier.\n\n        Returns:\n            Union[str, None]: Creator's icon string.\n        \"\"\"\n\n        pass\n\n    @abstractmethod\n    def get_convertor_items(self) -&gt; Dict[str, ConvertorItem]:\n        \"\"\"Convertor items by identifier.\n\n        Returns:\n            Dict[str, ConvertorItem]: Convertor items that can be triggered\n                by user.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_instance_items(self) -&gt; List[\"InstanceItem\"]:\n        \"\"\"Collected/created instances.\n\n        Returns:\n            List[InstanceItem]: List of created instances.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_instance_items_by_id(\n        self, instance_ids: Optional[Iterable[str]] = None\n    ) -&gt; Dict[str, Union[\"InstanceItem\", None]]:\n        pass\n\n    @abstractmethod\n    def get_instances_context_info(\n        self, instance_ids: Optional[Iterable[str]] = None\n    ):\n        pass\n\n    @abstractmethod\n    def set_instances_context_info(\n        self, changes_by_instance_id: Dict[str, Dict[str, Any]]\n    ):\n        pass\n\n    @abstractmethod\n    def set_instances_active_state(\n        self, active_state_by_id: Dict[str, bool]\n    ):\n        pass\n\n    @abstractmethod\n    def get_existing_product_names(self, folder_path: str) -&gt; List[str]:\n        pass\n\n    @abstractmethod\n    def get_creator_attribute_definitions(\n        self, instance_ids: Iterable[str]\n    ) -&gt; List[Tuple[AbstractAttrDef, Dict[str, Dict[str, Any]]]]:\n        pass\n\n    @abstractmethod\n    def set_instances_create_attr_values(\n        self, instance_ids: Iterable[str], key: str, value: Any\n    ):\n        pass\n\n    @abstractmethod\n    def revert_instances_create_attr_values(\n        self,\n        instance_ids: List[\"Union[str, None]\"],\n        key: str,\n    ):\n        pass\n\n    @abstractmethod\n    def get_publish_attribute_definitions(\n        self,\n        instance_ids: Iterable[str],\n        include_context: bool\n    ) -&gt; list[PublishAttrDefsInfo]:\n        pass\n\n    @abstractmethod\n    def set_instances_publish_attr_values(\n        self,\n        instance_ids: Iterable[str],\n        plugin_name: str,\n        key: str,\n        value: Any\n    ):\n        pass\n\n    @abstractmethod\n    def revert_instances_publish_attr_values(\n        self,\n        instance_ids: List[\"Union[str, None]\"],\n        plugin_name: str,\n        key: str,\n    ):\n        pass\n\n    @abstractmethod\n    def trigger_pre_create_button_callback(\n        self, identifier: str, button_name: str\n    ) -&gt; None:\n        pass\n\n    @abstractmethod\n    def trigger_create_button_callback(\n        self,\n        button_name: str,\n        instance_ids: list[str],\n    ) -&gt; None:\n        pass\n\n    @abstractmethod\n    def trigger_publish_button_callback(\n        self,\n        plugin_name: str,\n        button_name: str,\n        instance_ids: list[str | None],\n    ) -&gt; None:\n        pass\n\n    @abstractmethod\n    def get_product_name(\n        self,\n        creator_identifier: str,\n        product_type: str,\n        variant: str,\n        folder_path: Union[str, None],\n        task_name: Union[str, None],\n        instance_id: Optional[str] = None\n    ):\n        \"\"\"Get product name based on passed data.\n\n        Args:\n            creator_identifier (str): Identifier of creator which should be\n                responsible for product name creation.\n            product_type (str): Product type.\n            variant (str): Variant value from user's input.\n            folder_path (str): Folder path for which is instance created.\n            task_name (str): Name of task for which is instance created.\n            instance_id (Union[str, None]): Existing instance id when product\n                name is updated.\n        \"\"\"\n\n        pass\n\n    @abstractmethod\n    def create(\n        self,\n        creator_identifier: str,\n        product_name: str,\n        instance_data: Dict[str, Any],\n        options: Dict[str, Any],\n    ):\n        \"\"\"Trigger creation by creator identifier.\n\n        Should also trigger refresh of instances.\n\n        Args:\n            creator_identifier (str): Identifier of Creator plugin.\n            product_type (str): Product type.\n            product_name (str): Calculated product name.\n            instance_data (Dict[str, Any]): Base instance data with variant,\n                folder path and task name.\n            options (Dict[str, Any]): Data from pre-create attributes.\n        \"\"\"\n\n        pass\n\n    @abstractmethod\n    def trigger_convertor_items(self, convertor_identifiers: List[str]):\n        pass\n\n    @abstractmethod\n    def remove_instances(self, instance_ids: Iterable[str]):\n        \"\"\"Remove list of instances from create context.\"\"\"\n        # TODO expect instance ids\n\n        pass\n\n    @abstractmethod\n    def save_changes(self) -&gt; bool:\n        \"\"\"Save changes in create context.\n\n        Save can crash because of unexpected errors.\n\n        Returns:\n            bool: Save was successful.\n        \"\"\"\n\n        pass\n\n    # --- Publish ---\n    @abstractmethod\n    def publish(self):\n        \"\"\"Trigger publishing without any order limitations.\"\"\"\n\n        pass\n\n    @abstractmethod\n    def validate(self):\n        \"\"\"Trigger publishing which will stop after validation order.\"\"\"\n\n        pass\n\n    @abstractmethod\n    def stop_publish(self):\n        \"\"\"Stop publishing can be also used to pause publishing.\n\n        Pause of publishing is possible only if all plugins successfully\n        finished.\n        \"\"\"\n\n        pass\n\n    @abstractmethod\n    def run_action(self, plugin_id: str, action_id: str):\n        \"\"\"Trigger pyblish action on a plugin.\n\n        Args:\n            plugin_id (str): Publish plugin id.\n            action_id (str): Publish action id.\n        \"\"\"\n\n        pass\n\n    @abstractmethod\n    def publish_has_started(self) -&gt; bool:\n        \"\"\"Has publishing finished.\n\n        Returns:\n            bool: If publishing finished and all plugins were iterated.\n        \"\"\"\n\n        pass\n\n    @abstractmethod\n    def publish_has_finished(self) -&gt; bool:\n        \"\"\"Has publishing finished.\n\n        Returns:\n            bool: If publishing finished and all plugins were iterated.\n        \"\"\"\n\n        pass\n\n    @abstractmethod\n    def publish_is_running(self) -&gt; bool:\n        \"\"\"Publishing is running right now.\n\n        Returns:\n            bool: If publishing is in progress.\n        \"\"\"\n\n        pass\n\n    @abstractmethod\n    def publish_has_validated(self) -&gt; bool:\n        \"\"\"Publish validation passed.\n\n        Returns:\n            bool: If publishing passed last possible validation order.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def publish_can_continue(self):\n        \"\"\"Publish has still plugins to process and did not crash yet.\n\n        Returns:\n            bool: Publishing can continue in processing.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def publish_has_crashed(self) -&gt; bool:\n        \"\"\"Publishing crashed for any reason.\n\n        Returns:\n            bool: Publishing crashed.\n        \"\"\"\n\n        pass\n\n    @abstractmethod\n    def publish_has_validation_errors(self) -&gt; bool:\n        \"\"\"During validation happened at least one validation error.\n\n        Returns:\n            bool: Validation error was raised during validation.\n        \"\"\"\n\n        pass\n\n    @abstractmethod\n    def get_publish_progress(self) -&gt; int:\n        \"\"\"Current progress number.\n\n        Returns:\n            int: Current progress value from 0 to 'publish_max_progress'.\n        \"\"\"\n\n        pass\n\n    @abstractmethod\n    def get_publish_max_progress(self) -&gt; int:\n        \"\"\"Get maximum possible progress number.\n\n        Returns:\n            int: Number that can be used as 100% of publish progress bar.\n        \"\"\"\n\n        pass\n\n    @abstractmethod\n    def get_publish_error_info(self) -&gt; Optional[\"PublishErrorInfo\"]:\n        \"\"\"Current error message which cause fail of publishing.\n\n        Returns:\n            Optional[PublishErrorInfo]: Error info or None.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_publish_report(self) -&gt; PublishReport:\n        pass\n\n    @abstractmethod\n    def get_publish_report_data(self) -&gt; dict[str, Any]:\n        pass\n\n    @abstractmethod\n    def get_publish_errors_report(self):\n        pass\n\n    @abstractmethod\n    def store_publish_report(self, filepath: str) -&gt; None:\n        pass\n\n    @abstractmethod\n    def set_comment(self, comment: str):\n        \"\"\"Set comment on pyblish context.\n\n        Set \"comment\" key on current pyblish.api.Context data.\n\n        Args:\n            comment (str): Artist's comment.\n        \"\"\"\n\n        pass\n\n    @abstractmethod\n    def get_thumbnail_paths_for_instances(\n        self, instance_ids: List[str]\n    ) -&gt; Dict[str, Union[str, None]]:\n        pass\n\n    @abstractmethod\n    def set_thumbnail_paths_for_instances(\n        self, thumbnail_path_mapping: Dict[str, Optional[str]]\n    ):\n        pass\n\n    @abstractmethod\n    def get_thumbnail_temp_dir_path(self) -&gt; str:\n        \"\"\"Path to directory where thumbnails can be temporarily stored.\n\n        Returns:\n            str: Path to a directory.\n        \"\"\"\n\n        pass\n\n    @abstractmethod\n    def clear_thumbnail_temp_dir_path(self):\n        \"\"\"Remove content of thumbnail temp directory.\"\"\"\n\n        pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/abstract.html#client.ayon_core.tools.publisher.abstract.AbstractPublisherFrontend.are_folder_paths_valid","title":"<code>are_folder_paths_valid(folder_paths)</code>  <code>abstractmethod</code>","text":"<p>Folder paths do exist in project.</p> <p>Parameters:</p> Name Type Description Default <code>folder_paths</code> <code>Iterable[str]</code> <p>List of folder paths.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>All folder paths exist in project.</p> Source code in <code>client/ayon_core/tools/publisher/abstract.py</code> <pre><code>@abstractmethod\ndef are_folder_paths_valid(self, folder_paths: Iterable[str]) -&gt; bool:\n    \"\"\"Folder paths do exist in project.\n\n    Args:\n        folder_paths (Iterable[str]): List of folder paths.\n\n    Returns:\n        bool: All folder paths exist in project.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/abstract.html#client.ayon_core.tools.publisher.abstract.AbstractPublisherFrontend.clear_thumbnail_temp_dir_path","title":"<code>clear_thumbnail_temp_dir_path()</code>  <code>abstractmethod</code>","text":"<p>Remove content of thumbnail temp directory.</p> Source code in <code>client/ayon_core/tools/publisher/abstract.py</code> <pre><code>@abstractmethod\ndef clear_thumbnail_temp_dir_path(self):\n    \"\"\"Remove content of thumbnail temp directory.\"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/abstract.html#client.ayon_core.tools.publisher.abstract.AbstractPublisherFrontend.create","title":"<code>create(creator_identifier, product_name, instance_data, options)</code>  <code>abstractmethod</code>","text":"<p>Trigger creation by creator identifier.</p> <p>Should also trigger refresh of instances.</p> <p>Parameters:</p> Name Type Description Default <code>creator_identifier</code> <code>str</code> <p>Identifier of Creator plugin.</p> required <code>product_type</code> <code>str</code> <p>Product type.</p> required <code>product_name</code> <code>str</code> <p>Calculated product name.</p> required <code>instance_data</code> <code>Dict[str, Any]</code> <p>Base instance data with variant, folder path and task name.</p> required <code>options</code> <code>Dict[str, Any]</code> <p>Data from pre-create attributes.</p> required Source code in <code>client/ayon_core/tools/publisher/abstract.py</code> <pre><code>@abstractmethod\ndef create(\n    self,\n    creator_identifier: str,\n    product_name: str,\n    instance_data: Dict[str, Any],\n    options: Dict[str, Any],\n):\n    \"\"\"Trigger creation by creator identifier.\n\n    Should also trigger refresh of instances.\n\n    Args:\n        creator_identifier (str): Identifier of Creator plugin.\n        product_type (str): Product type.\n        product_name (str): Calculated product name.\n        instance_data (Dict[str, Any]): Base instance data with variant,\n            folder path and task name.\n        options (Dict[str, Any]): Data from pre-create attributes.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/abstract.html#client.ayon_core.tools.publisher.abstract.AbstractPublisherFrontend.get_context_title","title":"<code>get_context_title()</code>  <code>abstractmethod</code>","text":"<p>Get context title for artist shown at the top of main window.</p> <p>Returns:</p> Type Description <code>Union[str, None]</code> <p>Union[str, None]: Context title for window or None. In case of None a warning is displayed (not nice for artists).</p> Source code in <code>client/ayon_core/tools/publisher/abstract.py</code> <pre><code>@abstractmethod\ndef get_context_title(self) -&gt; Union[str, None]:\n    \"\"\"Get context title for artist shown at the top of main window.\n\n    Returns:\n        Union[str, None]: Context title for window or None. In case of None\n            a warning is displayed (not nice for artists).\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/abstract.html#client.ayon_core.tools.publisher.abstract.AbstractPublisherFrontend.get_convertor_items","title":"<code>get_convertor_items()</code>  <code>abstractmethod</code>","text":"<p>Convertor items by identifier.</p> <p>Returns:</p> Type Description <code>Dict[str, ConvertorItem]</code> <p>Dict[str, ConvertorItem]: Convertor items that can be triggered by user.</p> Source code in <code>client/ayon_core/tools/publisher/abstract.py</code> <pre><code>@abstractmethod\ndef get_convertor_items(self) -&gt; Dict[str, ConvertorItem]:\n    \"\"\"Convertor items by identifier.\n\n    Returns:\n        Dict[str, ConvertorItem]: Convertor items that can be triggered\n            by user.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/abstract.html#client.ayon_core.tools.publisher.abstract.AbstractPublisherFrontend.get_creator_icon","title":"<code>get_creator_icon(identifier)</code>  <code>abstractmethod</code>","text":"<p>Receive creator's icon by identifier.</p> Todos <p>Icon should be part of 'CreatorItem'.</p> <p>Parameters:</p> Name Type Description Default <code>identifier</code> <code>str</code> <p>Creator's identifier.</p> required <p>Returns:</p> Type Description <code>Union[str, Dict[str, Any], None]</code> <p>Union[str, None]: Creator's icon string.</p> Source code in <code>client/ayon_core/tools/publisher/abstract.py</code> <pre><code>@abstractmethod\ndef get_creator_icon(\n    self, identifier: str\n) -&gt; Union[str, Dict[str, Any], None]:\n    \"\"\"Receive creator's icon by identifier.\n\n    Todos:\n        Icon should be part of 'CreatorItem'.\n\n    Args:\n        identifier (str): Creator's identifier.\n\n    Returns:\n        Union[str, None]: Creator's icon string.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/abstract.html#client.ayon_core.tools.publisher.abstract.AbstractPublisherFrontend.get_creator_item_by_id","title":"<code>get_creator_item_by_id(identifier)</code>  <code>abstractmethod</code>","text":"<p>Get creator item by identifier.</p> <p>Parameters:</p> Name Type Description Default <code>identifier</code> <code>str</code> <p>Create plugin identifier.</p> required <p>Returns:</p> Type Description <code>Optional['CreatorItem']</code> <p>Optional[CreatorItem]: Creator item or None.</p> Source code in <code>client/ayon_core/tools/publisher/abstract.py</code> <pre><code>@abstractmethod\ndef get_creator_item_by_id(\n    self, identifier: str\n) -&gt; Optional[\"CreatorItem\"]:\n    \"\"\"Get creator item by identifier.\n\n    Args:\n        identifier (str): Create plugin identifier.\n\n    Returns:\n        Optional[CreatorItem]: Creator item or None.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/abstract.html#client.ayon_core.tools.publisher.abstract.AbstractPublisherFrontend.get_creator_items","title":"<code>get_creator_items()</code>  <code>abstractmethod</code>","text":"<p>Creator items by identifier.</p> <p>Returns:</p> Type Description <code>Dict[str, 'CreatorItem']</code> <p>Dict[str, CreatorItem]: Creator items that will be shown to user.</p> Source code in <code>client/ayon_core/tools/publisher/abstract.py</code> <pre><code>@abstractmethod\ndef get_creator_items(self) -&gt; Dict[str, \"CreatorItem\"]:\n    \"\"\"Creator items by identifier.\n\n    Returns:\n        Dict[str, CreatorItem]: Creator items that will be shown to user.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/abstract.html#client.ayon_core.tools.publisher.abstract.AbstractPublisherFrontend.get_folder_id_from_path","title":"<code>get_folder_id_from_path(folder_path)</code>  <code>abstractmethod</code>","text":"<p>Get folder id from folder path.</p> Source code in <code>client/ayon_core/tools/publisher/abstract.py</code> <pre><code>@abstractmethod\ndef get_folder_id_from_path(self, folder_path: str) -&gt; Optional[str]:\n    \"\"\"Get folder id from folder path.\"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/abstract.html#client.ayon_core.tools.publisher.abstract.AbstractPublisherFrontend.get_instance_items","title":"<code>get_instance_items()</code>  <code>abstractmethod</code>","text":"<p>Collected/created instances.</p> <p>Returns:</p> Type Description <code>List['InstanceItem']</code> <p>List[InstanceItem]: List of created instances.</p> Source code in <code>client/ayon_core/tools/publisher/abstract.py</code> <pre><code>@abstractmethod\ndef get_instance_items(self) -&gt; List[\"InstanceItem\"]:\n    \"\"\"Collected/created instances.\n\n    Returns:\n        List[InstanceItem]: List of created instances.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/abstract.html#client.ayon_core.tools.publisher.abstract.AbstractPublisherFrontend.get_my_tasks_entity_ids","title":"<code>get_my_tasks_entity_ids(project_name)</code>  <code>abstractmethod</code>","text":"<p>Get entity ids for my tasks.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <p>Returns:</p> Type Description <code>dict[str, list[str]]</code> <p>dict[str, list[str]]: Folder and task ids.</p> Source code in <code>client/ayon_core/tools/publisher/abstract.py</code> <pre><code>@abstractmethod\ndef get_my_tasks_entity_ids(\n    self, project_name: str\n) -&gt; dict[str, list[str]]:\n    \"\"\"Get entity ids for my tasks.\n\n    Args:\n        project_name (str): Project name.\n\n    Returns:\n        dict[str, list[str]]: Folder and task ids.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/abstract.html#client.ayon_core.tools.publisher.abstract.AbstractPublisherFrontend.get_product_name","title":"<code>get_product_name(creator_identifier, product_type, variant, folder_path, task_name, instance_id=None)</code>  <code>abstractmethod</code>","text":"<p>Get product name based on passed data.</p> <p>Parameters:</p> Name Type Description Default <code>creator_identifier</code> <code>str</code> <p>Identifier of creator which should be responsible for product name creation.</p> required <code>product_type</code> <code>str</code> <p>Product type.</p> required <code>variant</code> <code>str</code> <p>Variant value from user's input.</p> required <code>folder_path</code> <code>str</code> <p>Folder path for which is instance created.</p> required <code>task_name</code> <code>str</code> <p>Name of task for which is instance created.</p> required <code>instance_id</code> <code>Union[str, None]</code> <p>Existing instance id when product name is updated.</p> <code>None</code> Source code in <code>client/ayon_core/tools/publisher/abstract.py</code> <pre><code>@abstractmethod\ndef get_product_name(\n    self,\n    creator_identifier: str,\n    product_type: str,\n    variant: str,\n    folder_path: Union[str, None],\n    task_name: Union[str, None],\n    instance_id: Optional[str] = None\n):\n    \"\"\"Get product name based on passed data.\n\n    Args:\n        creator_identifier (str): Identifier of creator which should be\n            responsible for product name creation.\n        product_type (str): Product type.\n        variant (str): Variant value from user's input.\n        folder_path (str): Folder path for which is instance created.\n        task_name (str): Name of task for which is instance created.\n        instance_id (Union[str, None]): Existing instance id when product\n            name is updated.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/abstract.html#client.ayon_core.tools.publisher.abstract.AbstractPublisherFrontend.get_publish_error_info","title":"<code>get_publish_error_info()</code>  <code>abstractmethod</code>","text":"<p>Current error message which cause fail of publishing.</p> <p>Returns:</p> Type Description <code>Optional['PublishErrorInfo']</code> <p>Optional[PublishErrorInfo]: Error info or None.</p> Source code in <code>client/ayon_core/tools/publisher/abstract.py</code> <pre><code>@abstractmethod\ndef get_publish_error_info(self) -&gt; Optional[\"PublishErrorInfo\"]:\n    \"\"\"Current error message which cause fail of publishing.\n\n    Returns:\n        Optional[PublishErrorInfo]: Error info or None.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/abstract.html#client.ayon_core.tools.publisher.abstract.AbstractPublisherFrontend.get_publish_max_progress","title":"<code>get_publish_max_progress()</code>  <code>abstractmethod</code>","text":"<p>Get maximum possible progress number.</p> <p>Returns:</p> Name Type Description <code>int</code> <code>int</code> <p>Number that can be used as 100% of publish progress bar.</p> Source code in <code>client/ayon_core/tools/publisher/abstract.py</code> <pre><code>@abstractmethod\ndef get_publish_max_progress(self) -&gt; int:\n    \"\"\"Get maximum possible progress number.\n\n    Returns:\n        int: Number that can be used as 100% of publish progress bar.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/abstract.html#client.ayon_core.tools.publisher.abstract.AbstractPublisherFrontend.get_publish_progress","title":"<code>get_publish_progress()</code>  <code>abstractmethod</code>","text":"<p>Current progress number.</p> <p>Returns:</p> Name Type Description <code>int</code> <code>int</code> <p>Current progress value from 0 to 'publish_max_progress'.</p> Source code in <code>client/ayon_core/tools/publisher/abstract.py</code> <pre><code>@abstractmethod\ndef get_publish_progress(self) -&gt; int:\n    \"\"\"Current progress number.\n\n    Returns:\n        int: Current progress value from 0 to 'publish_max_progress'.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/abstract.html#client.ayon_core.tools.publisher.abstract.AbstractPublisherFrontend.get_thumbnail_temp_dir_path","title":"<code>get_thumbnail_temp_dir_path()</code>  <code>abstractmethod</code>","text":"<p>Path to directory where thumbnails can be temporarily stored.</p> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Path to a directory.</p> Source code in <code>client/ayon_core/tools/publisher/abstract.py</code> <pre><code>@abstractmethod\ndef get_thumbnail_temp_dir_path(self) -&gt; str:\n    \"\"\"Path to directory where thumbnails can be temporarily stored.\n\n    Returns:\n        str: Path to a directory.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/abstract.html#client.ayon_core.tools.publisher.abstract.AbstractPublisherFrontend.get_window_subtitle","title":"<code>get_window_subtitle()</code>  <code>abstractmethod</code>","text":"<p>Get window subtitle.</p> <p>Returns:</p> Type Description <code>Optional[str]</code> <p>Optional[str]: Window subtitle.</p> Source code in <code>client/ayon_core/tools/publisher/abstract.py</code> <pre><code>@abstractmethod\ndef get_window_subtitle(self) -&gt; Optional[str]:\n    \"\"\"Get window subtitle.\n\n    Returns:\n        Optional[str]: Window subtitle.\n\n    \"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/abstract.html#client.ayon_core.tools.publisher.abstract.AbstractPublisherFrontend.is_host_valid","title":"<code>is_host_valid()</code>  <code>abstractmethod</code>","text":"<p>Host is valid for creation part.</p> <p>Host must have implemented certain functionality to be able to create     in Publisher tool.</p> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>Host can handle creation of instances.</p> Source code in <code>client/ayon_core/tools/publisher/abstract.py</code> <pre><code>@abstractmethod\ndef is_host_valid(self) -&gt; bool:\n    \"\"\"Host is valid for creation part.\n\n    Host must have implemented certain functionality to be able to create\n        in Publisher tool.\n\n    Returns:\n        bool: Host can handle creation of instances.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/abstract.html#client.ayon_core.tools.publisher.abstract.AbstractPublisherFrontend.publish","title":"<code>publish()</code>  <code>abstractmethod</code>","text":"<p>Trigger publishing without any order limitations.</p> Source code in <code>client/ayon_core/tools/publisher/abstract.py</code> <pre><code>@abstractmethod\ndef publish(self):\n    \"\"\"Trigger publishing without any order limitations.\"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/abstract.html#client.ayon_core.tools.publisher.abstract.AbstractPublisherFrontend.publish_can_continue","title":"<code>publish_can_continue()</code>  <code>abstractmethod</code>","text":"<p>Publish has still plugins to process and did not crash yet.</p> <p>Returns:</p> Name Type Description <code>bool</code> <p>Publishing can continue in processing.</p> Source code in <code>client/ayon_core/tools/publisher/abstract.py</code> <pre><code>@abstractmethod\ndef publish_can_continue(self):\n    \"\"\"Publish has still plugins to process and did not crash yet.\n\n    Returns:\n        bool: Publishing can continue in processing.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/abstract.html#client.ayon_core.tools.publisher.abstract.AbstractPublisherFrontend.publish_has_crashed","title":"<code>publish_has_crashed()</code>  <code>abstractmethod</code>","text":"<p>Publishing crashed for any reason.</p> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>Publishing crashed.</p> Source code in <code>client/ayon_core/tools/publisher/abstract.py</code> <pre><code>@abstractmethod\ndef publish_has_crashed(self) -&gt; bool:\n    \"\"\"Publishing crashed for any reason.\n\n    Returns:\n        bool: Publishing crashed.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/abstract.html#client.ayon_core.tools.publisher.abstract.AbstractPublisherFrontend.publish_has_finished","title":"<code>publish_has_finished()</code>  <code>abstractmethod</code>","text":"<p>Has publishing finished.</p> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>If publishing finished and all plugins were iterated.</p> Source code in <code>client/ayon_core/tools/publisher/abstract.py</code> <pre><code>@abstractmethod\ndef publish_has_finished(self) -&gt; bool:\n    \"\"\"Has publishing finished.\n\n    Returns:\n        bool: If publishing finished and all plugins were iterated.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/abstract.html#client.ayon_core.tools.publisher.abstract.AbstractPublisherFrontend.publish_has_started","title":"<code>publish_has_started()</code>  <code>abstractmethod</code>","text":"<p>Has publishing finished.</p> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>If publishing finished and all plugins were iterated.</p> Source code in <code>client/ayon_core/tools/publisher/abstract.py</code> <pre><code>@abstractmethod\ndef publish_has_started(self) -&gt; bool:\n    \"\"\"Has publishing finished.\n\n    Returns:\n        bool: If publishing finished and all plugins were iterated.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/abstract.html#client.ayon_core.tools.publisher.abstract.AbstractPublisherFrontend.publish_has_validated","title":"<code>publish_has_validated()</code>  <code>abstractmethod</code>","text":"<p>Publish validation passed.</p> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>If publishing passed last possible validation order.</p> Source code in <code>client/ayon_core/tools/publisher/abstract.py</code> <pre><code>@abstractmethod\ndef publish_has_validated(self) -&gt; bool:\n    \"\"\"Publish validation passed.\n\n    Returns:\n        bool: If publishing passed last possible validation order.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/abstract.html#client.ayon_core.tools.publisher.abstract.AbstractPublisherFrontend.publish_has_validation_errors","title":"<code>publish_has_validation_errors()</code>  <code>abstractmethod</code>","text":"<p>During validation happened at least one validation error.</p> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>Validation error was raised during validation.</p> Source code in <code>client/ayon_core/tools/publisher/abstract.py</code> <pre><code>@abstractmethod\ndef publish_has_validation_errors(self) -&gt; bool:\n    \"\"\"During validation happened at least one validation error.\n\n    Returns:\n        bool: Validation error was raised during validation.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/abstract.html#client.ayon_core.tools.publisher.abstract.AbstractPublisherFrontend.publish_is_running","title":"<code>publish_is_running()</code>  <code>abstractmethod</code>","text":"<p>Publishing is running right now.</p> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>If publishing is in progress.</p> Source code in <code>client/ayon_core/tools/publisher/abstract.py</code> <pre><code>@abstractmethod\ndef publish_is_running(self) -&gt; bool:\n    \"\"\"Publishing is running right now.\n\n    Returns:\n        bool: If publishing is in progress.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/abstract.html#client.ayon_core.tools.publisher.abstract.AbstractPublisherFrontend.remove_instances","title":"<code>remove_instances(instance_ids)</code>  <code>abstractmethod</code>","text":"<p>Remove list of instances from create context.</p> Source code in <code>client/ayon_core/tools/publisher/abstract.py</code> <pre><code>@abstractmethod\ndef remove_instances(self, instance_ids: Iterable[str]):\n    \"\"\"Remove list of instances from create context.\"\"\"\n    # TODO expect instance ids\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/abstract.html#client.ayon_core.tools.publisher.abstract.AbstractPublisherFrontend.run_action","title":"<code>run_action(plugin_id, action_id)</code>  <code>abstractmethod</code>","text":"<p>Trigger pyblish action on a plugin.</p> <p>Parameters:</p> Name Type Description Default <code>plugin_id</code> <code>str</code> <p>Publish plugin id.</p> required <code>action_id</code> <code>str</code> <p>Publish action id.</p> required Source code in <code>client/ayon_core/tools/publisher/abstract.py</code> <pre><code>@abstractmethod\ndef run_action(self, plugin_id: str, action_id: str):\n    \"\"\"Trigger pyblish action on a plugin.\n\n    Args:\n        plugin_id (str): Publish plugin id.\n        action_id (str): Publish action id.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/abstract.html#client.ayon_core.tools.publisher.abstract.AbstractPublisherFrontend.save_changes","title":"<code>save_changes()</code>  <code>abstractmethod</code>","text":"<p>Save changes in create context.</p> <p>Save can crash because of unexpected errors.</p> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>Save was successful.</p> Source code in <code>client/ayon_core/tools/publisher/abstract.py</code> <pre><code>@abstractmethod\ndef save_changes(self) -&gt; bool:\n    \"\"\"Save changes in create context.\n\n    Save can crash because of unexpected errors.\n\n    Returns:\n        bool: Save was successful.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/abstract.html#client.ayon_core.tools.publisher.abstract.AbstractPublisherFrontend.set_comment","title":"<code>set_comment(comment)</code>  <code>abstractmethod</code>","text":"<p>Set comment on pyblish context.</p> <p>Set \"comment\" key on current pyblish.api.Context data.</p> <p>Parameters:</p> Name Type Description Default <code>comment</code> <code>str</code> <p>Artist's comment.</p> required Source code in <code>client/ayon_core/tools/publisher/abstract.py</code> <pre><code>@abstractmethod\ndef set_comment(self, comment: str):\n    \"\"\"Set comment on pyblish context.\n\n    Set \"comment\" key on current pyblish.api.Context data.\n\n    Args:\n        comment (str): Artist's comment.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/abstract.html#client.ayon_core.tools.publisher.abstract.AbstractPublisherFrontend.stop_publish","title":"<code>stop_publish()</code>  <code>abstractmethod</code>","text":"<p>Stop publishing can be also used to pause publishing.</p> <p>Pause of publishing is possible only if all plugins successfully finished.</p> Source code in <code>client/ayon_core/tools/publisher/abstract.py</code> <pre><code>@abstractmethod\ndef stop_publish(self):\n    \"\"\"Stop publishing can be also used to pause publishing.\n\n    Pause of publishing is possible only if all plugins successfully\n    finished.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/abstract.html#client.ayon_core.tools.publisher.abstract.AbstractPublisherFrontend.validate","title":"<code>validate()</code>  <code>abstractmethod</code>","text":"<p>Trigger publishing which will stop after validation order.</p> Source code in <code>client/ayon_core/tools/publisher/abstract.py</code> <pre><code>@abstractmethod\ndef validate(self):\n    \"\"\"Trigger publishing which will stop after validation order.\"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/abstract.html#client.ayon_core.tools.publisher.abstract.CommentDef","title":"<code>CommentDef</code>  <code>dataclass</code>","text":"<p>Comment attribute definition.</p> Source code in <code>client/ayon_core/tools/publisher/abstract.py</code> <pre><code>@dataclass\nclass CommentDef:\n    \"\"\"Comment attribute definition.\"\"\"\n    minimum_chars_required: int\n\n    def to_data(self):\n        return asdict(self)\n\n    @classmethod\n    def from_data(cls, data):\n        return cls(**data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/app.html","title":"app","text":""},{"location":"autoapi/client/ayon_core/tools/publisher/constants.html","title":"constants","text":""},{"location":"autoapi/client/ayon_core/tools/publisher/control.html","title":"control","text":""},{"location":"autoapi/client/ayon_core/tools/publisher/control.html#client.ayon_core.tools.publisher.control.PublisherController","title":"<code>PublisherController</code>","text":"<p>               Bases: <code>AbstractPublisherBackend</code>, <code>AbstractPublisherFrontend</code></p> <p>Middleware between UI, CreateContext and publish Context.</p> <p>Handle both creation and publishing parts.</p> Known topics <p>\"show.detailed.help\" - Detailed help requested (UI related). \"show.card.message\" - Show card message request (UI related).</p> <p>Parameters:</p> Name Type Description Default <code>headless</code> <code>bool</code> <p>Headless publishing. ATM not implemented or used.</p> <code>False</code> Source code in <code>client/ayon_core/tools/publisher/control.py</code> <pre><code>class PublisherController(\n    AbstractPublisherBackend,\n    AbstractPublisherFrontend,\n):\n    \"\"\"Middleware between UI, CreateContext and publish Context.\n\n    Handle both creation and publishing parts.\n\n    Known topics:\n        \"show.detailed.help\" - Detailed help requested (UI related).\n        \"show.card.message\" - Show card message request (UI related).\n        # --- Create model ---\n        \"create.model.reset\" - Reset of create model.\n        \"instances.create.failed\" - Creation failed.\n        \"convertors.convert.failed\" - Convertor failed.\n        \"instances.save.failed\" - Save failed.\n        \"instance.thumbnail.changed\" - Thumbnail changed.\n        \"instances.collection.failed\" - Collection of instances failed.\n        \"convertors.find.failed\" - Convertor find failed.\n        \"instances.create.failed\" - Create instances failed.\n        \"instances.remove.failed\" - Remove instances failed.\n        \"create.context.added.instance\" - Create instance added to context.\n        \"create.context.value.changed\" - Create instance or context value\n            changed.\n        \"create.context.pre.create.attrs.changed\" - Pre create attributes\n            changed.\n        \"create.context.create.attrs.changed\" - Create attributes changed.\n        \"create.context.publish.attrs.changed\" - Publish attributes changed.\n        \"create.context.instance.requirement.changed\" - Instance requirement\n            changed.\n        \"create.context.removed.instance\" - Instance removed from context.\n        \"create.model.instances.context.changed\" - Instances changed context.\n            like folder, task or variant.\n        # --- Publish model ---\n        \"plugins.refresh.finished\" - Plugins refreshed.\n        \"publish.reset.finished\" - Reset finished.\n        \"controller.reset.started\" - Controller reset started.\n        \"controller.reset.finished\" - Controller reset finished.\n        \"publish.process.started\" - Publishing started. Can be started from\n            paused state.\n        \"publish.process.stopped\" - Publishing stopped/paused process.\n        \"publish.process.plugin.changed\" - Plugin state has changed.\n        \"publish.process.instance.changed\" - Instance state has changed.\n        \"publish.has_validated.changed\" - Attr 'publish_has_validated'\n            changed.\n        \"publish.is_running.changed\" - Attr 'publish_is_running' changed.\n        \"publish.has_crashed.changed\" - Attr 'publish_has_crashed' changed.\n        \"publish.publish_error.changed\" - Attr 'publish_error'\n        \"publish.has_validation_errors.changed\" - Attr\n            'has_validation_errors' changed.\n        \"publish.max_progress.changed\" - Attr 'publish_max_progress'\n            changed.\n        \"publish.progress.changed\" - Attr 'publish_progress' changed.\n        \"publish.finished.changed\" - Attr 'publish_has_finished' changed.\n\n    Args:\n        headless (bool): Headless publishing. ATM not implemented or used.\n\n    \"\"\"\n    _log = None\n\n    def __init__(self, headless=False):\n        super().__init__()\n\n        self._log = None\n        self._event_system = self._create_event_system()\n\n        self._host = registered_host()\n        self._headless = headless\n\n        self._settings_model = SettingsModel()\n        self._create_model = CreateModel(self)\n        self._publish_model = PublishModel(self)\n\n        self._projects_model = ProjectsModel(self)\n        self._hierarchy_model = HierarchyModel(self)\n        self._users_model = UsersModel(self)\n\n    @property\n    def log(self):\n        \"\"\"Controller's logger object.\n\n        Returns:\n            logging.Logger: Logger object that can be used for logging.\n        \"\"\"\n\n        if self._log is None:\n            self._log = logging.getLogger(self.__class__.__name__)\n        return self._log\n\n    def get_window_subtitle(self) -&gt; Optional[str]:\n        if self._host is None:\n            return None\n        return self._host.name\n\n    def is_headless(self):\n        return self._headless\n\n    def get_host(self):\n        return self._host\n\n    def get_create_context(self):\n        return self._create_model.get_create_context()\n\n    def is_host_valid(self) -&gt; bool:\n        return self._create_model.is_host_valid()\n\n    # Events system\n    def emit_event(self, topic, data=None, source=None):\n        \"\"\"Use implemented event system to trigger event.\"\"\"\n\n        if data is None:\n            data = {}\n        self._event_system.emit(topic, data, source)\n\n    def emit_card_message(\n        self, message, message_type=CardMessageTypes.standard\n    ):\n        self._emit_event(\n            \"show.card.message\",\n            {\n                \"message\": message,\n                \"message_type\": message_type\n            }\n        )\n\n    def register_event_callback(self, topic, callback):\n        self._event_system.add_callback(topic, callback)\n\n    def get_current_project_name(self):\n        \"\"\"Current project context defined by host.\n\n        Returns:\n            str: Project name.\n\n        \"\"\"\n        return self._create_model.get_current_project_name()\n\n    def get_current_folder_path(self):\n        \"\"\"Current context folder path defined by host.\n\n        Returns:\n            Union[str, None]: Folder path or None if folder is not set.\n        \"\"\"\n\n        return self._create_model.get_current_folder_path()\n\n    def get_current_task_name(self):\n        \"\"\"Current context task name defined by host.\n\n        Returns:\n            Union[str, None]: Task name or None if task is not set.\n        \"\"\"\n\n        return self._create_model.get_current_task_name()\n\n    def host_context_has_changed(self):\n        return self._create_model.host_context_has_changed()\n\n    def get_creator_items(self):\n        \"\"\"Creators that can be shown in create dialog.\"\"\"\n        return self._create_model.get_creator_items()\n\n    def get_creator_item_by_id(self, identifier):\n        return self._create_model.get_creator_item_by_id(identifier)\n\n    def get_creator_icon(self, identifier):\n        \"\"\"Function to receive icon for creator identifier.\n\n        Args:\n            identifier (str): Creator's identifier for which should\n                be icon returned.\n\n        \"\"\"\n        return self._create_model.get_creator_icon(identifier)\n\n    def get_instance_items(self):\n        \"\"\"Current instances in create context.\"\"\"\n        return self._create_model.get_instance_items()\n\n    def get_instance_items_by_id(self, instance_ids=None):\n        return self._create_model.get_instance_items_by_id(instance_ids)\n\n    def get_instances_context_info(self, instance_ids=None):\n        return self._create_model.get_instances_context_info(instance_ids)\n\n    def set_instances_context_info(self, changes_by_instance_id):\n        return self._create_model.set_instances_context_info(\n            changes_by_instance_id\n        )\n\n    def set_instances_active_state(self, active_state_by_id):\n        self._create_model.set_instances_active_state(active_state_by_id)\n\n    def get_convertor_items(self):\n        return self._create_model.get_convertor_items()\n\n    def get_project_settings(self, project_name: str | None) -&gt; dict:\n        return self._settings_model.get_settings(project_name)\n\n    def get_project_entity(self, project_name):\n        return self._projects_model.get_project_entity(project_name)\n\n    def get_folder_type_items(self, project_name, sender=None):\n        return self._projects_model.get_folder_type_items(\n            project_name, sender\n        )\n\n    def get_task_type_items(self, project_name, sender=None):\n        return self._projects_model.get_task_type_items(\n            project_name, sender\n        )\n\n    # Hierarchy model\n    def get_folder_items(self, project_name, sender=None):\n        return self._hierarchy_model.get_folder_items(project_name, sender)\n\n    def get_task_items(self, project_name, folder_id, sender=None):\n        return self._hierarchy_model.get_task_items(\n            project_name, folder_id, sender\n        )\n\n    def get_folder_entity(self, project_name, folder_id):\n        return self._hierarchy_model.get_folder_entity(\n            project_name, folder_id\n        )\n\n    def get_task_entity(self, project_name, task_id):\n        return self._hierarchy_model.get_task_entity(project_name, task_id)\n\n    def get_folder_item_by_path(self, project_name, folder_path):\n        return self._hierarchy_model.get_folder_item_by_path(\n            project_name, folder_path\n        )\n\n    def get_task_item_by_name(\n        self, project_name, folder_id, task_name, sender=None\n    ):\n        return self._hierarchy_model.get_task_item_by_name(\n            project_name, folder_id, task_name, sender\n        )\n\n    # Publisher custom method\n    def get_folder_id_from_path(self, folder_path):\n        if not folder_path:\n            return None\n        folder_item = self.get_folder_item_by_path(\n            self.get_current_project_name(), folder_path\n        )\n        if folder_item:\n            return folder_item.entity_id\n        return None\n\n    def get_task_items_by_folder_paths(self, folder_paths):\n        if not folder_paths:\n            return {}\n\n        folder_items = self._hierarchy_model.get_folder_items_by_paths(\n            self.get_current_project_name(), folder_paths\n        )\n\n        output = {\n            folder_path: []\n            for folder_path in folder_paths\n        }\n        project_name = self.get_current_project_name()\n        for folder_path, folder_item in folder_items.items():\n            task_items = []\n            if folder_item is not None:\n                task_items = self._hierarchy_model.get_task_items(\n                    project_name, folder_item.entity_id, None\n                )\n            output[folder_path] = task_items\n\n        return output\n\n    def are_folder_paths_valid(self, folder_paths):\n        if not folder_paths:\n            return True\n        folder_paths = set(folder_paths)\n        folder_items = self._hierarchy_model.get_folder_items_by_paths(\n            self.get_current_project_name(), folder_paths\n        )\n        for folder_item in folder_items.values():\n            if folder_item is None:\n                return False\n        return True\n\n    def get_my_tasks_entity_ids(\n        self, project_name: str\n    ) -&gt; dict[str, list[str]]:\n        username = self._users_model.get_current_username()\n        assignees = []\n        if username:\n            assignees.append(username)\n        return self._hierarchy_model.get_entity_ids_for_assignees(\n            project_name, assignees\n        )\n\n    # --- Publish specific callbacks ---\n    def get_context_title(self):\n        \"\"\"Get context title for artist shown at the top of main window.\"\"\"\n\n        context_title = None\n        if hasattr(self._host, \"get_context_title\"):\n            context_title = self._host.get_context_title()\n\n        if context_title is None:\n            context_title = os.environ.get(\"AYON_APP_NAME\")\n            if context_title is None:\n                context_title = os.environ.get(\"AYON_HOST_NAME\")\n\n        return context_title\n\n    def get_existing_product_names(self, folder_path):\n        if not folder_path:\n            return None\n        project_name = self.get_current_project_name()\n        folder_item = self._hierarchy_model.get_folder_item_by_path(\n            project_name, folder_path\n        )\n        if not folder_item:\n            return None\n\n        product_entities = ayon_api.get_products(\n            project_name,\n            folder_ids={folder_item.entity_id},\n            fields={\"name\"}\n        )\n        return {\n            product_entity[\"name\"]\n            for product_entity in product_entities\n        }\n\n    def reset(self):\n        \"\"\"Reset everything related to creation and publishing.\"\"\"\n        self.stop_publish()\n\n        self._emit_event(\"controller.reset.started\")\n\n        self._hierarchy_model.reset()\n        self._users_model.reset()\n\n        # Publish part must be reset after plugins\n        self._settings_model.reset()\n        self._create_model.reset()\n        self._publish_model.reset()\n\n        self._emit_event(\"controller.reset.finished\")\n\n        self.emit_card_message(\"Refreshed..\")\n\n    def get_thumbnail_paths_for_instances(self, instance_ids):\n        return self._create_model.get_thumbnail_paths_for_instances(\n            instance_ids\n        )\n\n    def set_thumbnail_paths_for_instances(self, thumbnail_path_mapping):\n        self._create_model.set_thumbnail_paths_for_instances(\n            thumbnail_path_mapping\n        )\n\n    def get_thumbnail_temp_dir_path(self):\n        \"\"\"Return path to directory where thumbnails can be temporary stored.\n\n        Returns:\n            str: Path to a directory.\n        \"\"\"\n\n        return os.path.join(\n            tempfile.gettempdir(),\n            \"publisher_thumbnails\",\n            get_process_id()\n        )\n\n    def clear_thumbnail_temp_dir_path(self):\n        \"\"\"Remove content of thumbnail temp directory.\"\"\"\n\n        dirpath = self.get_thumbnail_temp_dir_path()\n        if os.path.exists(dirpath):\n            shutil.rmtree(dirpath)\n\n    def get_creator_attribute_definitions(self, instance_ids):\n        \"\"\"Collect creator attribute definitions for multuple instances.\n\n        Args:\n            instance_ids (List[str]): List of created instances for\n                which should be attribute definitions returned.\n\n        \"\"\"\n        return self._create_model.get_creator_attribute_definitions(\n            instance_ids\n        )\n\n    def set_instances_create_attr_values(self, instance_ids, key, value):\n        return self._create_model.set_instances_create_attr_values(\n            instance_ids, key, value\n        )\n\n    def revert_instances_create_attr_values(self, instance_ids, key):\n        self._create_model.revert_instances_create_attr_values(\n            instance_ids, key\n        )\n\n    def get_publish_attribute_definitions(self, instance_ids, include_context):\n        \"\"\"Collect publish attribute definitions for passed instances.\n\n        Args:\n            instance_ids (List[str]): List of created instances for\n                which should be attribute definitions returned.\n            include_context (bool): Add context specific attribute definitions.\n\n        \"\"\"\n        return self._create_model.get_publish_attribute_definitions(\n            instance_ids, include_context\n        )\n\n    def set_instances_publish_attr_values(\n        self, instance_ids, plugin_name, key, value\n    ):\n        return self._create_model.set_instances_publish_attr_values(\n            instance_ids, plugin_name, key, value\n        )\n\n    def revert_instances_publish_attr_values(\n        self, instance_ids, plugin_name, key\n    ):\n        return self._create_model.revert_instances_publish_attr_values(\n            instance_ids, plugin_name, key\n        )\n\n    def trigger_pre_create_button_callback(\n        self, identifier: str, button_name: str\n    ) -&gt; None:\n        self._create_model.trigger_pre_create_button_callback(\n            identifier, button_name\n        )\n\n    def trigger_create_button_callback(\n        self,\n        button_name: str,\n        instance_ids: list[str],\n    ) -&gt; None:\n        self._create_model.trigger_create_button_callback(\n            button_name, instance_ids\n        )\n\n    def trigger_publish_button_callback(\n        self,\n        plugin_name: str,\n        button_name: str,\n        instance_ids: list[str | None],\n    ) -&gt; None:\n        self._create_model.trigger_publish_button_callback(\n            plugin_name, button_name, instance_ids\n        )\n\n    def get_product_name(\n        self,\n        creator_identifier: str,\n        product_type: str,\n        variant: str,\n        folder_path: Union[str, None],\n        task_name: Union[str, None],\n        instance_id: Optional[str] = None\n    ):\n        \"\"\"Get product name based on passed data.\n\n        Args:\n            creator_identifier (str): Identifier of creator which should be\n                responsible for product name creation.\n            variant (str): Variant value from user's input.\n            folder_path (str): Folder path for which is instance created.\n            task_name (str): Name of task for which is instance created.\n            instance_id (Optional[str]): Existing instance id when product\n                name is updated.\n        \"\"\"\n\n        return self._create_model.get_product_name(\n            creator_identifier,\n            product_type,\n            variant,\n            folder_path,\n            task_name,\n            instance_id=None\n        )\n\n    def trigger_convertor_items(self, convertor_identifiers):\n        \"\"\"Trigger legacy item convertors.\n\n        This functionality requires to save and reset CreateContext. The reset\n        is needed so Creators can collect converted items.\n\n        Args:\n            convertor_identifiers (list[str]): Identifiers of convertor\n                plugins.\n        \"\"\"\n\n        self._create_model.trigger_convertor_items(convertor_identifiers)\n\n        self.reset()\n\n    def create(\n        self,\n        creator_identifier: str,\n        product_name: str,\n        instance_data: dict[str, Any],\n        options: dict[str, Any],\n    ):\n        \"\"\"Trigger creation and refresh of instances in UI.\"\"\"\n\n        return self._create_model.create(\n            creator_identifier,\n            product_name,\n            instance_data,\n            options,\n        )\n\n    def save_changes(self, show_message=True):\n        \"\"\"Save changes happened during creation.\n\n        Trigger save of changes using host api. This functionality does not\n        validate anything. It is required to do checks before this method is\n        called to be able to give user actionable response e.g. check of\n        context using 'host_context_has_changed'.\n\n        Args:\n            show_message (bool): Show message that changes were\n                saved successfully.\n\n        Returns:\n            bool: Save of changes was successful.\n\n        \"\"\"\n        return self._create_model.save_changes(show_message)\n\n    def remove_instances(self, instance_ids):\n        \"\"\"Remove instances based on instance ids.\n\n        Args:\n            instance_ids (List[str]): List of instance ids to remove.\n\n        \"\"\"\n        self._create_model.remove_instances(instance_ids)\n\n    def publish_has_started(self):\n        return self._publish_model.has_started()\n\n    def publish_has_finished(self):\n        return self._publish_model.has_finished()\n\n    def publish_is_running(self):\n        return self._publish_model.is_running()\n\n    def publish_has_validated(self):\n        return self._publish_model.has_validated()\n\n    def publish_has_crashed(self):\n        return self._publish_model.is_crashed()\n\n    def publish_has_validation_errors(self):\n        return self._publish_model.has_validation_errors()\n\n    def publish_can_continue(self):\n        return self._publish_model.publish_can_continue()\n\n    def get_publish_max_progress(self):\n        return self._publish_model.get_max_progress()\n\n    def get_publish_progress(self):\n        return self._publish_model.get_progress()\n\n    def get_publish_error_info(self):\n        return self._publish_model.get_error_info()\n\n    def get_publish_report(self):\n        return self._publish_model.get_publish_report()\n\n    def get_publish_report_data(self) -&gt; dict[str, Any]:\n        return self._publish_model.get_publish_report_data()\n\n    def store_publish_report(self, filepath: str) -&gt; None:\n        self._publish_model.store_publish_report(filepath)\n\n    def get_publish_errors_report(self):\n        return self._publish_model.get_publish_errors_report()\n\n    def set_comment(self, comment):\n        \"\"\"Set comment from ui to pyblish context.\n\n        This should be called always before publishing is started but should\n        happen only once on first publish start thus variable\n        '_publish_comment_is_set' is used to keep track about the information.\n        \"\"\"\n\n        self._publish_model.set_comment(comment)\n\n    def publish(self):\n        \"\"\"Run publishing.\n\n        Make sure all changes are saved before method is called (Call\n        'save_changes' and check output).\n        \"\"\"\n        self._start_publish(False)\n\n    def validate(self):\n        \"\"\"Run publishing and stop after Validation.\n\n        Make sure all changes are saved before method is called (Call\n        'save_changes' and check output).\n        \"\"\"\n        self._start_publish(True)\n\n    def stop_publish(self):\n        \"\"\"Stop publishing process (any reason).\"\"\"\n        self._publish_model.stop_publish()\n\n    def run_action(self, plugin_id, action_id):\n        self._publish_model.run_action(plugin_id, action_id)\n\n    def _create_event_system(self):\n        return QueuedEventSystem()\n\n    def _emit_event(self, topic, data=None):\n        self.emit_event(topic, data, \"controller\")\n\n    def _start_publish(self, up_validation):\n        self._publish_model.set_publish_up_validation(up_validation)\n        self._publish_model.start_publish(wait=True)\n\n    def get_comment_def(self) -&gt; CommentDef:\n        # Take the cached settings from the Create Context\n        settings = self.get_create_context().get_current_project_settings()\n        comment_minimum_required_chars: int = (\n            settings\n            .get(\"core\", {})\n            .get(\"tools\", {})\n            .get(\"publish\", {})\n            .get(\"comment_minimum_required_chars\", 0)\n        )\n        return CommentDef(\n            minimum_chars_required=comment_minimum_required_chars\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/control.html#client.ayon_core.tools.publisher.control.PublisherController---create-model-","title":"--- Create model ---","text":"<p>\"create.model.reset\" - Reset of create model. \"instances.create.failed\" - Creation failed. \"convertors.convert.failed\" - Convertor failed. \"instances.save.failed\" - Save failed. \"instance.thumbnail.changed\" - Thumbnail changed. \"instances.collection.failed\" - Collection of instances failed. \"convertors.find.failed\" - Convertor find failed. \"instances.create.failed\" - Create instances failed. \"instances.remove.failed\" - Remove instances failed. \"create.context.added.instance\" - Create instance added to context. \"create.context.value.changed\" - Create instance or context value     changed. \"create.context.pre.create.attrs.changed\" - Pre create attributes     changed. \"create.context.create.attrs.changed\" - Create attributes changed. \"create.context.publish.attrs.changed\" - Publish attributes changed. \"create.context.instance.requirement.changed\" - Instance requirement     changed. \"create.context.removed.instance\" - Instance removed from context. \"create.model.instances.context.changed\" - Instances changed context.     like folder, task or variant.</p>"},{"location":"autoapi/client/ayon_core/tools/publisher/control.html#client.ayon_core.tools.publisher.control.PublisherController---publish-model-","title":"--- Publish model ---","text":"<p>\"plugins.refresh.finished\" - Plugins refreshed. \"publish.reset.finished\" - Reset finished. \"controller.reset.started\" - Controller reset started. \"controller.reset.finished\" - Controller reset finished. \"publish.process.started\" - Publishing started. Can be started from     paused state. \"publish.process.stopped\" - Publishing stopped/paused process. \"publish.process.plugin.changed\" - Plugin state has changed. \"publish.process.instance.changed\" - Instance state has changed. \"publish.has_validated.changed\" - Attr 'publish_has_validated'     changed. \"publish.is_running.changed\" - Attr 'publish_is_running' changed. \"publish.has_crashed.changed\" - Attr 'publish_has_crashed' changed. \"publish.publish_error.changed\" - Attr 'publish_error' \"publish.has_validation_errors.changed\" - Attr     'has_validation_errors' changed. \"publish.max_progress.changed\" - Attr 'publish_max_progress'     changed. \"publish.progress.changed\" - Attr 'publish_progress' changed. \"publish.finished.changed\" - Attr 'publish_has_finished' changed.</p>"},{"location":"autoapi/client/ayon_core/tools/publisher/control.html#client.ayon_core.tools.publisher.control.PublisherController.log","title":"<code>log</code>  <code>property</code>","text":"<p>Controller's logger object.</p> <p>Returns:</p> Type Description <p>logging.Logger: Logger object that can be used for logging.</p>"},{"location":"autoapi/client/ayon_core/tools/publisher/control.html#client.ayon_core.tools.publisher.control.PublisherController.clear_thumbnail_temp_dir_path","title":"<code>clear_thumbnail_temp_dir_path()</code>","text":"<p>Remove content of thumbnail temp directory.</p> Source code in <code>client/ayon_core/tools/publisher/control.py</code> <pre><code>def clear_thumbnail_temp_dir_path(self):\n    \"\"\"Remove content of thumbnail temp directory.\"\"\"\n\n    dirpath = self.get_thumbnail_temp_dir_path()\n    if os.path.exists(dirpath):\n        shutil.rmtree(dirpath)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/control.html#client.ayon_core.tools.publisher.control.PublisherController.create","title":"<code>create(creator_identifier, product_name, instance_data, options)</code>","text":"<p>Trigger creation and refresh of instances in UI.</p> Source code in <code>client/ayon_core/tools/publisher/control.py</code> <pre><code>def create(\n    self,\n    creator_identifier: str,\n    product_name: str,\n    instance_data: dict[str, Any],\n    options: dict[str, Any],\n):\n    \"\"\"Trigger creation and refresh of instances in UI.\"\"\"\n\n    return self._create_model.create(\n        creator_identifier,\n        product_name,\n        instance_data,\n        options,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/control.html#client.ayon_core.tools.publisher.control.PublisherController.emit_event","title":"<code>emit_event(topic, data=None, source=None)</code>","text":"<p>Use implemented event system to trigger event.</p> Source code in <code>client/ayon_core/tools/publisher/control.py</code> <pre><code>def emit_event(self, topic, data=None, source=None):\n    \"\"\"Use implemented event system to trigger event.\"\"\"\n\n    if data is None:\n        data = {}\n    self._event_system.emit(topic, data, source)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/control.html#client.ayon_core.tools.publisher.control.PublisherController.get_context_title","title":"<code>get_context_title()</code>","text":"<p>Get context title for artist shown at the top of main window.</p> Source code in <code>client/ayon_core/tools/publisher/control.py</code> <pre><code>def get_context_title(self):\n    \"\"\"Get context title for artist shown at the top of main window.\"\"\"\n\n    context_title = None\n    if hasattr(self._host, \"get_context_title\"):\n        context_title = self._host.get_context_title()\n\n    if context_title is None:\n        context_title = os.environ.get(\"AYON_APP_NAME\")\n        if context_title is None:\n            context_title = os.environ.get(\"AYON_HOST_NAME\")\n\n    return context_title\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/control.html#client.ayon_core.tools.publisher.control.PublisherController.get_creator_attribute_definitions","title":"<code>get_creator_attribute_definitions(instance_ids)</code>","text":"<p>Collect creator attribute definitions for multuple instances.</p> <p>Parameters:</p> Name Type Description Default <code>instance_ids</code> <code>List[str]</code> <p>List of created instances for which should be attribute definitions returned.</p> required Source code in <code>client/ayon_core/tools/publisher/control.py</code> <pre><code>def get_creator_attribute_definitions(self, instance_ids):\n    \"\"\"Collect creator attribute definitions for multuple instances.\n\n    Args:\n        instance_ids (List[str]): List of created instances for\n            which should be attribute definitions returned.\n\n    \"\"\"\n    return self._create_model.get_creator_attribute_definitions(\n        instance_ids\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/control.html#client.ayon_core.tools.publisher.control.PublisherController.get_creator_icon","title":"<code>get_creator_icon(identifier)</code>","text":"<p>Function to receive icon for creator identifier.</p> <p>Parameters:</p> Name Type Description Default <code>identifier</code> <code>str</code> <p>Creator's identifier for which should be icon returned.</p> required Source code in <code>client/ayon_core/tools/publisher/control.py</code> <pre><code>def get_creator_icon(self, identifier):\n    \"\"\"Function to receive icon for creator identifier.\n\n    Args:\n        identifier (str): Creator's identifier for which should\n            be icon returned.\n\n    \"\"\"\n    return self._create_model.get_creator_icon(identifier)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/control.html#client.ayon_core.tools.publisher.control.PublisherController.get_creator_items","title":"<code>get_creator_items()</code>","text":"<p>Creators that can be shown in create dialog.</p> Source code in <code>client/ayon_core/tools/publisher/control.py</code> <pre><code>def get_creator_items(self):\n    \"\"\"Creators that can be shown in create dialog.\"\"\"\n    return self._create_model.get_creator_items()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/control.html#client.ayon_core.tools.publisher.control.PublisherController.get_current_folder_path","title":"<code>get_current_folder_path()</code>","text":"<p>Current context folder path defined by host.</p> <p>Returns:</p> Type Description <p>Union[str, None]: Folder path or None if folder is not set.</p> Source code in <code>client/ayon_core/tools/publisher/control.py</code> <pre><code>def get_current_folder_path(self):\n    \"\"\"Current context folder path defined by host.\n\n    Returns:\n        Union[str, None]: Folder path or None if folder is not set.\n    \"\"\"\n\n    return self._create_model.get_current_folder_path()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/control.html#client.ayon_core.tools.publisher.control.PublisherController.get_current_project_name","title":"<code>get_current_project_name()</code>","text":"<p>Current project context defined by host.</p> <p>Returns:</p> Name Type Description <code>str</code> <p>Project name.</p> Source code in <code>client/ayon_core/tools/publisher/control.py</code> <pre><code>def get_current_project_name(self):\n    \"\"\"Current project context defined by host.\n\n    Returns:\n        str: Project name.\n\n    \"\"\"\n    return self._create_model.get_current_project_name()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/control.html#client.ayon_core.tools.publisher.control.PublisherController.get_current_task_name","title":"<code>get_current_task_name()</code>","text":"<p>Current context task name defined by host.</p> <p>Returns:</p> Type Description <p>Union[str, None]: Task name or None if task is not set.</p> Source code in <code>client/ayon_core/tools/publisher/control.py</code> <pre><code>def get_current_task_name(self):\n    \"\"\"Current context task name defined by host.\n\n    Returns:\n        Union[str, None]: Task name or None if task is not set.\n    \"\"\"\n\n    return self._create_model.get_current_task_name()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/control.html#client.ayon_core.tools.publisher.control.PublisherController.get_instance_items","title":"<code>get_instance_items()</code>","text":"<p>Current instances in create context.</p> Source code in <code>client/ayon_core/tools/publisher/control.py</code> <pre><code>def get_instance_items(self):\n    \"\"\"Current instances in create context.\"\"\"\n    return self._create_model.get_instance_items()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/control.html#client.ayon_core.tools.publisher.control.PublisherController.get_product_name","title":"<code>get_product_name(creator_identifier, product_type, variant, folder_path, task_name, instance_id=None)</code>","text":"<p>Get product name based on passed data.</p> <p>Parameters:</p> Name Type Description Default <code>creator_identifier</code> <code>str</code> <p>Identifier of creator which should be responsible for product name creation.</p> required <code>variant</code> <code>str</code> <p>Variant value from user's input.</p> required <code>folder_path</code> <code>str</code> <p>Folder path for which is instance created.</p> required <code>task_name</code> <code>str</code> <p>Name of task for which is instance created.</p> required <code>instance_id</code> <code>Optional[str]</code> <p>Existing instance id when product name is updated.</p> <code>None</code> Source code in <code>client/ayon_core/tools/publisher/control.py</code> <pre><code>def get_product_name(\n    self,\n    creator_identifier: str,\n    product_type: str,\n    variant: str,\n    folder_path: Union[str, None],\n    task_name: Union[str, None],\n    instance_id: Optional[str] = None\n):\n    \"\"\"Get product name based on passed data.\n\n    Args:\n        creator_identifier (str): Identifier of creator which should be\n            responsible for product name creation.\n        variant (str): Variant value from user's input.\n        folder_path (str): Folder path for which is instance created.\n        task_name (str): Name of task for which is instance created.\n        instance_id (Optional[str]): Existing instance id when product\n            name is updated.\n    \"\"\"\n\n    return self._create_model.get_product_name(\n        creator_identifier,\n        product_type,\n        variant,\n        folder_path,\n        task_name,\n        instance_id=None\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/control.html#client.ayon_core.tools.publisher.control.PublisherController.get_publish_attribute_definitions","title":"<code>get_publish_attribute_definitions(instance_ids, include_context)</code>","text":"<p>Collect publish attribute definitions for passed instances.</p> <p>Parameters:</p> Name Type Description Default <code>instance_ids</code> <code>List[str]</code> <p>List of created instances for which should be attribute definitions returned.</p> required <code>include_context</code> <code>bool</code> <p>Add context specific attribute definitions.</p> required Source code in <code>client/ayon_core/tools/publisher/control.py</code> <pre><code>def get_publish_attribute_definitions(self, instance_ids, include_context):\n    \"\"\"Collect publish attribute definitions for passed instances.\n\n    Args:\n        instance_ids (List[str]): List of created instances for\n            which should be attribute definitions returned.\n        include_context (bool): Add context specific attribute definitions.\n\n    \"\"\"\n    return self._create_model.get_publish_attribute_definitions(\n        instance_ids, include_context\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/control.html#client.ayon_core.tools.publisher.control.PublisherController.get_thumbnail_temp_dir_path","title":"<code>get_thumbnail_temp_dir_path()</code>","text":"<p>Return path to directory where thumbnails can be temporary stored.</p> <p>Returns:</p> Name Type Description <code>str</code> <p>Path to a directory.</p> Source code in <code>client/ayon_core/tools/publisher/control.py</code> <pre><code>def get_thumbnail_temp_dir_path(self):\n    \"\"\"Return path to directory where thumbnails can be temporary stored.\n\n    Returns:\n        str: Path to a directory.\n    \"\"\"\n\n    return os.path.join(\n        tempfile.gettempdir(),\n        \"publisher_thumbnails\",\n        get_process_id()\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/control.html#client.ayon_core.tools.publisher.control.PublisherController.publish","title":"<code>publish()</code>","text":"<p>Run publishing.</p> <p>Make sure all changes are saved before method is called (Call 'save_changes' and check output).</p> Source code in <code>client/ayon_core/tools/publisher/control.py</code> <pre><code>def publish(self):\n    \"\"\"Run publishing.\n\n    Make sure all changes are saved before method is called (Call\n    'save_changes' and check output).\n    \"\"\"\n    self._start_publish(False)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/control.html#client.ayon_core.tools.publisher.control.PublisherController.remove_instances","title":"<code>remove_instances(instance_ids)</code>","text":"<p>Remove instances based on instance ids.</p> <p>Parameters:</p> Name Type Description Default <code>instance_ids</code> <code>List[str]</code> <p>List of instance ids to remove.</p> required Source code in <code>client/ayon_core/tools/publisher/control.py</code> <pre><code>def remove_instances(self, instance_ids):\n    \"\"\"Remove instances based on instance ids.\n\n    Args:\n        instance_ids (List[str]): List of instance ids to remove.\n\n    \"\"\"\n    self._create_model.remove_instances(instance_ids)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/control.html#client.ayon_core.tools.publisher.control.PublisherController.reset","title":"<code>reset()</code>","text":"<p>Reset everything related to creation and publishing.</p> Source code in <code>client/ayon_core/tools/publisher/control.py</code> <pre><code>def reset(self):\n    \"\"\"Reset everything related to creation and publishing.\"\"\"\n    self.stop_publish()\n\n    self._emit_event(\"controller.reset.started\")\n\n    self._hierarchy_model.reset()\n    self._users_model.reset()\n\n    # Publish part must be reset after plugins\n    self._settings_model.reset()\n    self._create_model.reset()\n    self._publish_model.reset()\n\n    self._emit_event(\"controller.reset.finished\")\n\n    self.emit_card_message(\"Refreshed..\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/control.html#client.ayon_core.tools.publisher.control.PublisherController.save_changes","title":"<code>save_changes(show_message=True)</code>","text":"<p>Save changes happened during creation.</p> <p>Trigger save of changes using host api. This functionality does not validate anything. It is required to do checks before this method is called to be able to give user actionable response e.g. check of context using 'host_context_has_changed'.</p> <p>Parameters:</p> Name Type Description Default <code>show_message</code> <code>bool</code> <p>Show message that changes were saved successfully.</p> <code>True</code> <p>Returns:</p> Name Type Description <code>bool</code> <p>Save of changes was successful.</p> Source code in <code>client/ayon_core/tools/publisher/control.py</code> <pre><code>def save_changes(self, show_message=True):\n    \"\"\"Save changes happened during creation.\n\n    Trigger save of changes using host api. This functionality does not\n    validate anything. It is required to do checks before this method is\n    called to be able to give user actionable response e.g. check of\n    context using 'host_context_has_changed'.\n\n    Args:\n        show_message (bool): Show message that changes were\n            saved successfully.\n\n    Returns:\n        bool: Save of changes was successful.\n\n    \"\"\"\n    return self._create_model.save_changes(show_message)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/control.html#client.ayon_core.tools.publisher.control.PublisherController.set_comment","title":"<code>set_comment(comment)</code>","text":"<p>Set comment from ui to pyblish context.</p> <p>This should be called always before publishing is started but should happen only once on first publish start thus variable '_publish_comment_is_set' is used to keep track about the information.</p> Source code in <code>client/ayon_core/tools/publisher/control.py</code> <pre><code>def set_comment(self, comment):\n    \"\"\"Set comment from ui to pyblish context.\n\n    This should be called always before publishing is started but should\n    happen only once on first publish start thus variable\n    '_publish_comment_is_set' is used to keep track about the information.\n    \"\"\"\n\n    self._publish_model.set_comment(comment)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/control.html#client.ayon_core.tools.publisher.control.PublisherController.stop_publish","title":"<code>stop_publish()</code>","text":"<p>Stop publishing process (any reason).</p> Source code in <code>client/ayon_core/tools/publisher/control.py</code> <pre><code>def stop_publish(self):\n    \"\"\"Stop publishing process (any reason).\"\"\"\n    self._publish_model.stop_publish()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/control.html#client.ayon_core.tools.publisher.control.PublisherController.trigger_convertor_items","title":"<code>trigger_convertor_items(convertor_identifiers)</code>","text":"<p>Trigger legacy item convertors.</p> <p>This functionality requires to save and reset CreateContext. The reset is needed so Creators can collect converted items.</p> <p>Parameters:</p> Name Type Description Default <code>convertor_identifiers</code> <code>list[str]</code> <p>Identifiers of convertor plugins.</p> required Source code in <code>client/ayon_core/tools/publisher/control.py</code> <pre><code>def trigger_convertor_items(self, convertor_identifiers):\n    \"\"\"Trigger legacy item convertors.\n\n    This functionality requires to save and reset CreateContext. The reset\n    is needed so Creators can collect converted items.\n\n    Args:\n        convertor_identifiers (list[str]): Identifiers of convertor\n            plugins.\n    \"\"\"\n\n    self._create_model.trigger_convertor_items(convertor_identifiers)\n\n    self.reset()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/control.html#client.ayon_core.tools.publisher.control.PublisherController.validate","title":"<code>validate()</code>","text":"<p>Run publishing and stop after Validation.</p> <p>Make sure all changes are saved before method is called (Call 'save_changes' and check output).</p> Source code in <code>client/ayon_core/tools/publisher/control.py</code> <pre><code>def validate(self):\n    \"\"\"Run publishing and stop after Validation.\n\n    Make sure all changes are saved before method is called (Call\n    'save_changes' and check output).\n    \"\"\"\n    self._start_publish(True)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/control_qt.html","title":"control_qt","text":""},{"location":"autoapi/client/ayon_core/tools/publisher/control_qt.html#client.ayon_core.tools.publisher.control_qt.MainThreadItem","title":"<code>MainThreadItem</code>","text":"<p>Callback with args and kwargs.</p> Source code in <code>client/ayon_core/tools/publisher/control_qt.py</code> <pre><code>class MainThreadItem:\n    \"\"\"Callback with args and kwargs.\"\"\"\n\n    def __init__(self, callback, *args, **kwargs):\n        self.callback = callback\n        self.args = args\n        self.kwargs = kwargs\n\n    def __call__(self):\n        self.process()\n\n    def process(self):\n        self.callback(*self.args, **self.kwargs)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/control_qt.html#client.ayon_core.tools.publisher.control_qt.MainThreadProcess","title":"<code>MainThreadProcess</code>","text":"<p>               Bases: <code>QObject</code></p> <p>Qt based main thread process executor.</p> <p>Has timer which controls each 50ms if there is new item to process.</p> <p>This approach gives ability to update UI meanwhile plugin is in progress.</p> Source code in <code>client/ayon_core/tools/publisher/control_qt.py</code> <pre><code>class MainThreadProcess(QtCore.QObject):\n    \"\"\"Qt based main thread process executor.\n\n    Has timer which controls each 50ms if there is new item to process.\n\n    This approach gives ability to update UI meanwhile plugin is in progress.\n    \"\"\"\n\n    count_timeout = 2\n\n    def __init__(self):\n        super().__init__()\n        self._items_to_process = collections.deque()\n\n        timer = QtCore.QTimer()\n        timer.setInterval(0)\n\n        timer.timeout.connect(self._execute)\n\n        self._timer = timer\n\n    def add_item(self, item):\n        self._items_to_process.append(item)\n\n    def _execute(self):\n        if not self._items_to_process:\n            return\n\n        item = self._items_to_process.popleft()\n        item.process()\n\n    def start(self):\n        if not self._timer.isActive():\n            self._timer.start()\n\n    def stop(self):\n        if self._timer.isActive():\n            self._timer.stop()\n\n    def clear(self):\n        self._items_to_process = collections.deque()\n        self.stop()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/window.html","title":"window","text":""},{"location":"autoapi/client/ayon_core/tools/publisher/window.html#client.ayon_core.tools.publisher.window.PublisherWindow","title":"<code>PublisherWindow</code>","text":"<p>               Bases: <code>QDialog</code></p> <p>Main window of publisher.</p> Source code in <code>client/ayon_core/tools/publisher/window.py</code> <pre><code>class PublisherWindow(QtWidgets.QDialog):\n    \"\"\"Main window of publisher.\"\"\"\n    default_width = 1300\n    default_height = 800\n    footer_border = 8\n    publish_footer_spacer = 2\n\n    def __init__(\n        self,\n        parent: Optional[QtWidgets.QWidget] = None,\n        controller: Optional[AbstractPublisherFrontend] = None,\n        reset_on_show: Optional[bool] = None\n    ):\n        if controller is None:\n            controller = QtPublisherController()\n\n        title = \"AYON Publisher\"\n        subtitle = controller.get_window_subtitle()\n        if subtitle:\n            title += f\" - {subtitle}\"\n\n        super().__init__(parent)\n\n        self.setObjectName(\"PublishWindow\")\n\n        self.setWindowTitle(title)\n        icon = QtGui.QIcon(resources.get_ayon_icon_filepath())\n        self.setWindowIcon(icon)\n\n        if reset_on_show is None:\n            reset_on_show = True\n\n        self.setWindowFlags(\n            QtCore.Qt.Window\n            | QtCore.Qt.WindowTitleHint\n            | QtCore.Qt.WindowMaximizeButtonHint\n            | QtCore.Qt.WindowMinimizeButtonHint\n            | QtCore.Qt.WindowCloseButtonHint\n        )\n\n        help_dialog = HelpDialog(controller, self)\n\n        overlay_object = MessageOverlayObject(self)\n\n        # Header\n        header_widget = QtWidgets.QWidget(self)\n\n        icon_pixmap = QtGui.QPixmap(resources.get_ayon_icon_filepath())\n        icon_label = PixmapLabel(icon_pixmap, header_widget)\n        icon_label.setObjectName(\"PublishContextLabel\")\n\n        context_label = QtWidgets.QLabel(header_widget)\n        context_label.setObjectName(\"PublishContextLabel\")\n\n        header_extra_widget = QtWidgets.QWidget(header_widget)\n\n        help_btn = HelpButton(header_widget)\n\n        header_layout = QtWidgets.QHBoxLayout(header_widget)\n        header_layout.setContentsMargins(15, 15, 0, 15)\n        header_layout.setSpacing(15)\n        header_layout.addWidget(icon_label, 0)\n        header_layout.addWidget(context_label, 0)\n        header_layout.addStretch(1)\n        header_layout.addWidget(header_extra_widget, 0)\n        header_layout.addWidget(help_btn, 0)\n\n        # Tabs widget under header\n        tabs_widget = PublisherTabsWidget(self)\n        create_tab = tabs_widget.add_tab(\"Create\", \"create\")\n        tabs_widget.add_tab(\"Publish\", \"publish\")\n        tabs_widget.add_tab(\"Report\", \"report\")\n        tabs_widget.add_tab(\"Details\", \"details\")\n\n        # Widget where is stacked publish overlay and widgets that should be\n        #   covered by it\n        under_publish_stack = QtWidgets.QWidget(self)\n        # Added wrap widget where all widgets under overlay are added\n        # - this is because footer is also under overlay and the top part\n        #       is faked with floating frame\n        under_publish_widget = QtWidgets.QWidget(under_publish_stack)\n\n        # Footer\n        footer_widget = QtWidgets.QWidget(under_publish_widget)\n        footer_bottom_widget = QtWidgets.QWidget(footer_widget)\n\n        comment_input = PlaceholderLineEdit(footer_widget)\n        comment_input.setObjectName(\"PublishCommentInput\")\n        comment_input.setPlaceholderText(\n            \"Attach a comment to your publish\"\n        )\n\n        save_btn = SaveBtn(footer_widget)\n        reset_btn = ResetBtn(footer_widget)\n        stop_btn = StopBtn(footer_widget)\n        validate_btn = ValidateBtn(footer_widget)\n        publish_btn = PublishBtn(footer_widget)\n\n        footer_bottom_layout = QtWidgets.QHBoxLayout(footer_bottom_widget)\n        footer_bottom_layout.setContentsMargins(0, 0, 0, 0)\n        footer_bottom_layout.addStretch(1)\n        footer_bottom_layout.addWidget(save_btn, 0)\n        footer_bottom_layout.addWidget(reset_btn, 0)\n        footer_bottom_layout.addWidget(stop_btn, 0)\n        footer_bottom_layout.addWidget(validate_btn, 0)\n        footer_bottom_layout.addWidget(publish_btn, 0)\n\n        # Spacer helps keep distance of Publish Frame when comment input\n        #   is hidden - so when is shrunken it is not overlaying pages\n        footer_spacer = QtWidgets.QWidget(footer_widget)\n        footer_spacer.setMinimumHeight(self.publish_footer_spacer)\n        footer_spacer.setMaximumHeight(self.publish_footer_spacer)\n        footer_spacer.setVisible(False)\n\n        footer_layout = QtWidgets.QVBoxLayout(footer_widget)\n        footer_margins = footer_layout.contentsMargins()\n\n        footer_layout.setContentsMargins(\n            footer_margins.left() + self.footer_border,\n            footer_margins.top(),\n            footer_margins.right() + self.footer_border,\n            footer_margins.bottom() + self.footer_border\n        )\n\n        footer_layout.addWidget(comment_input, 0)\n        footer_layout.addWidget(footer_spacer, 0)\n        footer_layout.addWidget(footer_bottom_widget, 0)\n\n        # Content\n        # - wrap stacked widget under one more widget to be able to propagate\n        #   margins (QStackedLayout can't have margins)\n        content_widget = QtWidgets.QWidget(under_publish_widget)\n\n        content_stacked_widget = QtWidgets.QWidget(content_widget)\n\n        content_layout = QtWidgets.QVBoxLayout(content_widget)\n        marings = content_layout.contentsMargins()\n        marings.setLeft(marings.left() * 2)\n        marings.setRight(marings.right() * 2)\n        marings.setTop(marings.top() * 2)\n        marings.setBottom(0)\n        content_layout.setContentsMargins(marings)\n        content_layout.addWidget(content_stacked_widget, 1)\n\n        # Overview - create and attributes part\n        overview_widget = OverviewWidget(\n            controller, content_stacked_widget\n        )\n\n        report_widget = ReportPageWidget(controller, content_stacked_widget)\n\n        # Details - Publish details\n        publish_details_widget = PublishReportViewerWidget(\n            content_stacked_widget\n        )\n\n        content_stacked_layout = QtWidgets.QStackedLayout(\n            content_stacked_widget\n        )\n        content_stacked_layout.setContentsMargins(0, 0, 0, 0)\n        content_stacked_layout.setStackingMode(\n            QtWidgets.QStackedLayout.StackAll\n        )\n        content_stacked_layout.addWidget(overview_widget)\n        content_stacked_layout.addWidget(report_widget)\n        content_stacked_layout.addWidget(publish_details_widget)\n        content_stacked_layout.setCurrentWidget(overview_widget)\n\n        under_publish_layout = QtWidgets.QVBoxLayout(under_publish_widget)\n        under_publish_layout.setContentsMargins(0, 0, 0, 0)\n        under_publish_layout.setSpacing(0)\n        under_publish_layout.addWidget(content_widget, 1)\n        under_publish_layout.addWidget(footer_widget, 0)\n\n        # Overlay which covers inputs during publishing\n        publish_overlay = QtWidgets.QFrame(under_publish_stack)\n        publish_overlay.setObjectName(\"OverlayFrame\")\n\n        under_publish_stack_layout = QtWidgets.QStackedLayout(\n            under_publish_stack\n        )\n        under_publish_stack_layout.setContentsMargins(0, 0, 0, 0)\n        under_publish_stack_layout.setStackingMode(\n            QtWidgets.QStackedLayout.StackAll\n        )\n        under_publish_stack_layout.addWidget(under_publish_widget)\n        under_publish_stack_layout.addWidget(publish_overlay)\n        under_publish_stack_layout.setCurrentWidget(under_publish_widget)\n\n        # Add main frame to this window\n        main_layout = QtWidgets.QVBoxLayout(self)\n        main_layout.setContentsMargins(0, 0, 0, 0)\n        main_layout.setSpacing(0)\n        main_layout.addWidget(header_widget, 0)\n        main_layout.addWidget(tabs_widget, 0)\n        main_layout.addWidget(under_publish_stack, 1)\n\n        # Floating publish frame\n        publish_frame = PublishFrame(controller, self.footer_border, self)\n\n        create_overlay_button = CreateNextPageOverlay(self)\n\n        show_timer = QtCore.QTimer()\n        show_timer.setInterval(1)\n        show_timer.timeout.connect(self._on_show_timer)\n\n        comment_invalid_timer = QtCore.QTimer()\n        comment_invalid_timer.setSingleShot(True)\n        comment_invalid_timer.setInterval(2500)\n        comment_invalid_timer.timeout.connect(\n            self._on_comment_invalid_timeout\n        )\n\n        errors_dialog_message_timer = QtCore.QTimer()\n        errors_dialog_message_timer.setInterval(100)\n        errors_dialog_message_timer.timeout.connect(\n            self._on_errors_message_timeout\n        )\n\n        help_btn.clicked.connect(self._on_help_click)\n        tabs_widget.tab_changed.connect(self._on_tab_change)\n        overview_widget.create_requested.connect(\n            self._on_create_request\n        )\n        overview_widget.convert_requested.connect(\n            self._on_convert_requested\n        )\n        overview_widget.publish_tab_requested.connect(\n            self._go_to_publish_tab\n        )\n\n        save_btn.clicked.connect(self._on_save_clicked)\n        reset_btn.clicked.connect(self._on_reset_clicked)\n        stop_btn.clicked.connect(self._on_stop_clicked)\n        validate_btn.clicked.connect(self._on_validate_clicked)\n        publish_btn.clicked.connect(self._on_publish_clicked)\n\n        publish_frame.details_page_requested.connect(self._go_to_details_tab)\n        create_overlay_button.clicked.connect(\n            self._on_create_overlay_button_click\n        )\n\n        controller.register_event_callback(\n            \"create.model.reset\", self._on_create_model_reset\n        )\n        controller.register_event_callback(\n            \"create.context.added.instance\",\n            self._event_callback_validate_instances\n        )\n        controller.register_event_callback(\n            \"create.context.removed.instance\",\n            self._event_callback_validate_instances\n        )\n        controller.register_event_callback(\n            \"create.model.instances.context.changed\",\n            self._event_callback_validate_instances\n        )\n        controller.register_event_callback(\n            \"publish.reset.finished\", self._on_publish_reset\n        )\n        controller.register_event_callback(\n            \"controller.reset.finished\", self._on_controller_reset\n        )\n        controller.register_event_callback(\n            \"publish.process.started\", self._on_publish_start\n        )\n        controller.register_event_callback(\n            \"publish.has_validated.changed\", self._on_publish_validated_change\n        )\n        controller.register_event_callback(\n            \"publish.finished.changed\", self._on_publish_finished_change\n        )\n        controller.register_event_callback(\n            \"publish.process.stopped\", self._on_publish_stop\n        )\n        controller.register_event_callback(\n            \"show.card.message\", self._on_overlay_message\n        )\n        controller.register_event_callback(\n            \"instances.collection.failed\", self._on_creator_error\n        )\n        controller.register_event_callback(\n            \"instances.save.failed\", self._on_creator_error\n        )\n        controller.register_event_callback(\n            \"instances.remove.failed\", self._on_creator_error\n        )\n        controller.register_event_callback(\n            \"instances.create.failed\", self._on_creator_error\n        )\n        controller.register_event_callback(\n            \"convertors.convert.failed\", self._on_convertor_error\n        )\n        controller.register_event_callback(\n            \"convertors.find.failed\", self._on_convertor_error\n        )\n        controller.register_event_callback(\n            \"publish.action.failed\", self._on_action_error\n        )\n        controller.register_event_callback(\n            \"export_report.request\", self._export_report\n        )\n        controller.register_event_callback(\n            \"copy_report.request\", self._copy_report\n        )\n\n        # Store extra header widget for TrayPublisher\n        # - can be used to add additional widgets to header between context\n        #   label and help button\n        self._help_dialog = help_dialog\n        self._help_btn = help_btn\n\n        self._header_extra_widget = header_extra_widget\n\n        self._tabs_widget = tabs_widget\n        self._create_tab = create_tab\n\n        self._under_publish_stack_layout = under_publish_stack_layout\n\n        self._under_publish_widget = under_publish_widget\n        self._publish_overlay = publish_overlay\n        self._publish_frame = publish_frame\n\n        self._content_widget = content_widget\n        self._content_stacked_layout = content_stacked_layout\n\n        self._overview_widget = overview_widget\n        self._report_widget = report_widget\n        self._publish_details_widget = publish_details_widget\n\n        self._context_label = context_label\n\n        self._comment_input = comment_input\n        self._footer_spacer = footer_spacer\n\n        self._save_btn = save_btn\n        self._reset_btn = reset_btn\n        self._stop_btn = stop_btn\n        self._validate_btn = validate_btn\n        self._publish_btn = publish_btn\n\n        self._overlay_object = overlay_object\n\n        self._controller: AbstractPublisherFrontend = controller\n\n        self._first_show = True\n        self._first_reset = True\n        # This is a little bit confusing but 'reset_on_first_show' is too long\n        #   for init\n        self._reset_on_first_show = reset_on_show\n        self._reset_on_show = True\n        self._publish_frame_visible = None\n        self._tab_on_reset = None\n\n        self._create_context_valid: bool = True\n        self._blocked_by_crashed_paths: bool = False\n\n        self._error_messages_to_show = collections.deque()\n        self._errors_dialog_message_timer = errors_dialog_message_timer\n\n        self._set_publish_visibility(False)\n\n        self._create_overlay_button = create_overlay_button\n        self._app_event_listener_installed = False\n\n        self._show_timer = show_timer\n        self._comment_invalid_timer = comment_invalid_timer\n        self._show_counter = 0\n        self._window_is_visible = False\n\n        self._update_footer_state()\n\n    @property\n    def controller(self) -&gt; AbstractPublisherFrontend:\n        \"\"\"Kept for compatibility with traypublisher.\"\"\"\n        return self._controller\n\n    def show_and_publish(self, comment=None):\n        \"\"\"Show the window and start publishing.\n\n        The method will reset controller and start the publishing afterwards.\n\n        Todos:\n            Move validations from '_on_publish_clicked' and change of\n                'comment' value in controller to controller so it can be\n                simplified.\n\n        Args:\n            comment (Optional[str]): Comment to be set to publish.\n                If is set to 'None' a comment is not changed at all.\n        \"\"\"\n\n        self._reset_on_show = False\n        self._reset_on_first_show = False\n\n        if comment is not None:\n            self.set_comment(comment)\n        self.make_sure_is_visible()\n        # Reset controller\n        self._controller.reset()\n        # Fake publish click to trigger save validation and propagate\n        #   comment to controller\n        self._on_publish_clicked()\n\n    def set_comment(self, comment):\n        \"\"\"Change comment text.\n\n        Todos:\n            Be able to set the comment via controller.\n\n        Args:\n            comment (str): Comment text.\n        \"\"\"\n\n        self._comment_input.setText(comment)\n\n    def make_sure_is_visible(self):\n        if self._window_is_visible:\n            self.setWindowState(QtCore.Qt.WindowActive)\n        else:\n            self.show()\n\n        self.raise_()\n        self.activateWindow()\n        self.showNormal()\n\n    def showEvent(self, event):\n        self._window_is_visible = True\n        super().showEvent(event)\n        if self._first_show:\n            self._first_show = False\n            self._on_first_show()\n\n        self._show_timer.start()\n\n    def resizeEvent(self, event):\n        super().resizeEvent(event)\n        self._update_publish_frame_rect()\n        self._update_create_overlay_size()\n\n    def closeEvent(self, event):\n        self._window_is_visible = False\n        self._uninstall_app_event_listener()\n        # TODO capture changes and ask user if wants to save changes on close\n        if not self._controller.host_context_has_changed():\n            self._save_changes(False)\n        self._comment_input.setText(\"\")  # clear comment\n        self._reset_on_show = True\n        self._controller.clear_thumbnail_temp_dir_path()\n        # Trigger custom event that should be captured only in UI\n        #   - backend (controller) must not be dependent on this event topic!!!\n        self._controller.emit_event(\"main.window.closed\", {}, \"window\")\n        super().closeEvent(event)\n\n    def leaveEvent(self, event):\n        super().leaveEvent(event)\n        self._update_create_overlay_visibility()\n\n    def eventFilter(self, obj, event):\n        if event.type() == QtCore.QEvent.MouseMove:\n            self._update_create_overlay_visibility(event.globalPos())\n        return super().eventFilter(obj, event)\n\n    def _install_app_event_listener(self):\n        if self._app_event_listener_installed:\n            return\n        self._app_event_listener_installed = True\n        app = QtWidgets.QApplication.instance()\n        app.installEventFilter(self)\n\n    def _uninstall_app_event_listener(self):\n        if not self._app_event_listener_installed:\n            return\n        self._app_event_listener_installed = False\n        app = QtWidgets.QApplication.instance()\n        app.removeEventFilter(self)\n\n    def keyPressEvent(self, event):\n        if event.key() in {\n            # Ignore escape button to close window\n            QtCore.Qt.Key_Escape,\n            # Ignore enter keyboard event which by default triggers\n            #   first available button in QDialog\n            QtCore.Qt.Key_Enter,\n            QtCore.Qt.Key_Return,\n        }:\n            event.accept()\n            return\n\n        save_match = event.matches(QtGui.QKeySequence.Save)\n        # PySide2 and PySide6 support\n        if not isinstance(save_match, bool):\n            save_match = save_match == QtGui.QKeySequence.ExactMatch\n\n        if save_match:\n            if not self._controller.publish_has_started:\n                self._save_changes(True)\n            event.accept()\n            return\n\n        # PySide6 Support\n        if hasattr(event, \"keyCombination\"):\n            reset_match_result = ResetKeySequence.matches(\n                QtGui.QKeySequence(event.keyCombination())\n            )\n        else:\n            reset_match_result = ResetKeySequence.matches(\n                QtGui.QKeySequence(event.modifiers() | event.key())\n            )\n\n        if reset_match_result == QtGui.QKeySequence.ExactMatch:\n            if not self._controller.publish_is_running:\n                self.reset()\n            event.accept()\n            return\n\n        super().keyPressEvent(event)\n\n    def _on_overlay_message(self, event):\n        self._overlay_object.add_message(\n            event[\"message\"],\n            event.get(\"message_type\")\n        )\n\n    def _on_first_show(self):\n        self.resize(self.default_width, self.default_height)\n        self.setStyleSheet(style.load_stylesheet())\n        center_window(self)\n        self._reset_on_show = self._reset_on_first_show\n\n    def _on_show_timer(self):\n        # Add 1 to counter until hits 2\n        if self._show_counter &lt; 3:\n            self._show_counter += 1\n            return\n\n        # Stop the timer\n        self._show_timer.stop()\n        # Reset counter when done for next show event\n        self._show_counter = 0\n\n        self._update_create_overlay_size()\n        self._update_create_overlay_visibility()\n        if self._is_on_create_tab():\n            self._install_app_event_listener()\n\n        # Reset if requested\n        if self._reset_on_show:\n            self._reset_on_show = False\n            self.reset()\n\n    def _checks_before_save(self, explicit_save):\n        \"\"\"Save of changes may trigger some issues.\n\n        Check if context did change and ask user if he is really sure the\n        save should happen. A dialog can be shown during this method.\n\n        Args:\n            explicit_save (bool): Method was called when user explicitly asked\n                for save. Value affects shown message.\n\n        Returns:\n            bool: Save can happen.\n        \"\"\"\n\n        if not self._controller.host_context_has_changed():\n            return True\n\n        title = \"Host context changed\"\n        if explicit_save:\n            message = (\n                \"Context has changed since Publisher window was refreshed last\"\n                \" time.\\n\\nAre you sure you want to save changes?\"\n            )\n        else:\n            message = (\n                \"Your action requires save of changes but context has changed\"\n                \" since Publisher window was refreshed last time.\\n\\nAre you\"\n                \" sure you want to continue and save changes?\"\n            )\n\n        result = QtWidgets.QMessageBox.question(\n            self,\n            title,\n            message,\n            QtWidgets.QMessageBox.Save | QtWidgets.QMessageBox.Cancel\n        )\n        return result == QtWidgets.QMessageBox.Save\n\n    def _save_changes(self, explicit_save):\n        \"\"\"Save changes of Creation part.\n\n        All possible triggers of save changes were moved to main window (here),\n        so it can handle possible issues with save at one place. Do checks,\n        so user don't accidentally save changes to different file or using\n        different context.\n        Moving responsibility to this place gives option to show the dialog and\n        wait for user's response without breaking action he wanted to do.\n\n        Args:\n            explicit_save (bool): Method was called when user explicitly asked\n                for save. Value affects shown message.\n\n        Returns:\n            bool: Save happened successfully.\n        \"\"\"\n\n        if not self._checks_before_save(explicit_save):\n            return False\n        return self._controller.save_changes()\n\n    def reset(self):\n        self._controller.reset()\n\n    def set_context_label(self, label):\n        self._context_label.setText(label)\n\n    def set_tab_on_reset(self, tab):\n        \"\"\"Define tab that will be selected on window show.\n\n        This is single use method, when publisher window is showed the value is\n            unset and not used on next show.\n\n        Args:\n            tab (Union[int, Literal[create, publish, details, report]]: Index\n                or name of tab which will be selected on show (after reset).\n        \"\"\"\n\n        self._tab_on_reset = tab\n\n    def set_current_tab(self, tab):\n        if tab == \"create\":\n            self._go_to_create_tab()\n        elif tab == \"publish\":\n            self._go_to_publish_tab()\n        elif tab == \"report\":\n            self._go_to_report_tab()\n        elif tab == \"details\":\n            self._go_to_details_tab()\n\n        if not self._window_is_visible:\n            self.set_tab_on_reset(tab)\n\n    def _update_publish_details_widget(\n        self,\n        force: bool = False,\n        report: PublishReport | None = None,\n    ) -&gt; None:\n        if (\n            report is None\n            and not force\n            and not self._is_on_details_tab()\n        ):\n            return\n\n        if report is None:\n            report = self._controller.get_publish_report()\n\n        self._publish_details_widget.set_report(report)\n\n    def _on_help_click(self):\n        if self._help_dialog.isVisible():\n            return\n\n        self._help_dialog.show()\n\n        window = self.window()\n        screen = window.screen()\n        screen_geo = screen.geometry()\n\n        window_geo = window.geometry()\n        dialog_x = window_geo.x() + window_geo.width()\n        dialog_right = (dialog_x + self._help_dialog.width()) - 1\n        diff = dialog_right - screen_geo.right()\n        if diff &gt; 0:\n            dialog_x -= diff\n\n        self._help_dialog.setGeometry(\n            dialog_x, window_geo.y(),\n            self._help_dialog.width(), self._help_dialog.height()\n        )\n\n    def _on_create_overlay_button_click(self):\n        self._create_overlay_button.set_under_mouse(False)\n        self._go_to_publish_tab()\n\n    def _on_tab_change(self, old_tab, new_tab):\n        if old_tab == \"details\":\n            self._publish_details_widget.set_active(False)\n\n        if new_tab == \"details\":\n            self._content_stacked_layout.setCurrentWidget(\n                self._publish_details_widget\n            )\n            self._update_publish_details_widget()\n            self._publish_details_widget.set_active(True)\n\n        elif new_tab == \"report\":\n            self._content_stacked_layout.setCurrentWidget(\n                self._report_widget\n            )\n\n        old_on_overview = old_tab in (\"create\", \"publish\")\n        if new_tab in (\"create\", \"publish\"):\n            self._content_stacked_layout.setCurrentWidget(\n                self._overview_widget\n            )\n            # Overview state is animated only when switching between\n            #   'create' and 'publish' tab\n            self._overview_widget.set_state(new_tab, old_on_overview)\n\n        elif old_on_overview:\n            # Make sure animation finished if previous tab was 'create'\n            #   or 'publish'. That is just for safety to avoid stuck animation\n            #   when user clicks too fast.\n            self._overview_widget.make_sure_animation_is_finished()\n\n        is_create = new_tab == \"create\"\n        if is_create:\n            self._install_app_event_listener()\n        else:\n            self._uninstall_app_event_listener()\n        self._create_overlay_button.set_visible(is_create)\n\n    def _on_context_or_active_change(self):\n        self._validate_create_instances()\n\n    def _on_create_request(self):\n        self._go_to_create_tab()\n\n    def _on_convert_requested(self):\n        if not self._save_changes(False):\n            return\n        convertor_identifiers = (\n            self._overview_widget.get_selected_legacy_convertors()\n        )\n        self._controller.trigger_convertor_items(convertor_identifiers)\n\n    def _set_current_tab(self, identifier):\n        self._tabs_widget.set_current_tab(identifier)\n\n    def _is_current_tab(self, identifier):\n        return self._tabs_widget.is_current_tab(identifier)\n\n    def _go_to_create_tab(self):\n        if self._create_tab.isEnabled():\n            self._set_current_tab(\"create\")\n            return\n\n        self._overlay_object.add_message(\n            \"Can't switch to Create tab because publishing is paused.\",\n            message_type=\"info\"\n        )\n\n    def _go_to_publish_tab(self):\n        self._set_current_tab(\"publish\")\n\n    def _go_to_report_tab(self):\n        self._set_current_tab(\"report\")\n\n    def _go_to_details_tab(self):\n        self._set_current_tab(\"details\")\n\n    def _is_on_create_tab(self):\n        return self._is_current_tab(\"create\")\n\n    def _is_on_publish_tab(self):\n        return self._is_current_tab(\"publish\")\n\n    def _is_on_report_tab(self):\n        return self._is_current_tab(\"report\")\n\n    def _is_on_details_tab(self):\n        return self._is_current_tab(\"details\")\n\n    def _set_publish_overlay_visibility(self, visible):\n        if visible:\n            widget = self._publish_overlay\n        else:\n            widget = self._under_publish_widget\n        self._under_publish_stack_layout.setCurrentWidget(widget)\n\n    def _set_publish_visibility(self, visible):\n        if visible is self._publish_frame_visible:\n            return\n        self._publish_frame_visible = visible\n        self._publish_frame.setVisible(visible)\n        self._update_publish_frame_rect()\n\n    def _on_save_clicked(self):\n        self._save_changes(True)\n\n    def _on_reset_clicked(self):\n        self.reset()\n\n    def _on_stop_clicked(self):\n        self._controller.stop_publish()\n\n    def _set_publish_comment(self):\n        self._controller.set_comment(self._comment_input.text())\n\n    def _on_validate_clicked(self):\n        if self._validate_comment() and self._save_changes(False):\n            self._set_publish_comment()\n            self._controller.validate()\n\n    def _on_publish_clicked(self):\n        if self._validate_comment() and self._save_changes(False):\n            self._set_publish_comment()\n            self._controller.publish()\n\n    def _validate_comment(self) -&gt; bool:\n        # Validate comment length\n        comment_def = self._controller.get_comment_def()\n        char_count = len(self._comment_input.text().strip())\n        if (\n            comment_def.minimum_chars_required\n            and char_count &lt; comment_def.minimum_chars_required\n        ):\n            self._overlay_object.add_message(\n                \"Please enter a comment of at least \"\n                f\"{comment_def.minimum_chars_required} characters\",\n                message_type=\"error\"\n            )\n            self._invalidate_comment_field()\n            return False\n        return True\n\n    def _invalidate_comment_field(self):\n        self._comment_invalid_timer.start()\n        self._comment_input.setStyleSheet(\"border-color: #DD2020\")\n        # Set focus so user can start typing and is pointed towards the field\n        self._comment_input.setFocus()\n        self._comment_input.setCursorPosition(\n            len(self._comment_input.text())\n        )\n\n    def _on_comment_invalid_timeout(self):\n        # Reset style\n        self._comment_input.setStyleSheet(\"\")\n\n    def _set_create_context_valid(self, valid: bool) -&gt; None:\n        self._create_context_valid = valid\n        self._update_footer_state()\n\n    def _set_blocked(self, blocked: bool) -&gt; None:\n        self._blocked_by_crashed_paths = blocked\n        self._overview_widget.setEnabled(not blocked)\n        self._update_footer_state()\n        if not blocked:\n            return\n\n        self.set_tab_on_reset(\"details\")\n        self._go_to_details_tab()\n\n        QtWidgets.QMessageBox.critical(\n            self,\n            \"Failed to load plugins\",\n            (\n                \"Failed to load plugins that do prevent you from\"\n                \" using publish tool.\\n\"\n                \"Please contact your TD or administrator.\"\n            )\n        )\n\n    def _update_footer_state(self) -&gt; None:\n        enabled = (\n            not self._blocked_by_crashed_paths\n            and self._create_context_valid\n        )\n        save_enabled = not self._blocked_by_crashed_paths\n\n        self._save_btn.setEnabled(save_enabled)\n        self._reset_btn.setEnabled(True)\n        self._stop_btn.setEnabled(False)\n        self._validate_btn.setEnabled(enabled)\n        self._publish_btn.setEnabled(enabled)\n\n    def _on_publish_reset(self):\n        self._create_tab.setEnabled(True)\n        self._set_comment_input_visiblity(True)\n        self._set_publish_overlay_visibility(False)\n        self._set_publish_visibility(False)\n\n        report = self._controller.get_publish_report()\n        blocked = bool(report.blocking_crashed_paths)\n        self._set_blocked(blocked)\n        self._update_publish_details_widget(report=report)\n\n    def _on_controller_reset(self):\n        self._update_publish_details_widget(force=True)\n        self._first_reset, first_reset = False, self._first_reset\n        if self._tab_on_reset is not None:\n            self._tab_on_reset, new_tab = None, self._tab_on_reset\n            self._set_current_tab(new_tab)\n            return\n\n        # On first reset change tab based on available items\n        # - if there is at least one instance the tab is changed to 'publish'\n        #   otherwise 'create' is used\n        # - this happens only on first show\n        if first_reset:\n            self._go_to_create_tab()\n\n        elif self._is_on_report_tab():\n            # Go to 'Publish' tab if is on 'Details' tab\n            #   - this can happen when publishing started and was reset\n            #       at that moment it doesn't make sense to stay at publish\n            #       specific tabs.\n            self._go_to_publish_tab()\n\n    def _on_publish_start(self):\n        self._create_tab.setEnabled(False)\n\n        self._reset_btn.setEnabled(False)\n        self._stop_btn.setEnabled(True)\n        self._validate_btn.setEnabled(False)\n        self._publish_btn.setEnabled(False)\n\n        self._set_comment_input_visiblity(False)\n        self._set_publish_visibility(True)\n        self._set_publish_overlay_visibility(True)\n\n        self._publish_details_widget.close_details_popup()\n\n        if self._is_on_create_tab():\n            self._go_to_publish_tab()\n\n    def _on_publish_validated_change(self, event):\n        if event[\"value\"]:\n            self._validate_btn.setEnabled(False)\n\n    def _on_publish_finished_change(self, event):\n        if event[\"value\"]:\n            # Successful publish, remove comment from UI\n            self._comment_input.setText(\"\")\n\n    def _on_publish_stop(self):\n        self._set_publish_overlay_visibility(False)\n        self._reset_btn.setEnabled(True)\n        self._stop_btn.setEnabled(False)\n        if self._is_on_publish_tab():\n            self._go_to_report_tab()\n\n        publish_enabled = self._controller.publish_can_continue()\n        validate_enabled = (\n            publish_enabled and not self._controller.publish_has_validated()\n        )\n        self._validate_btn.setEnabled(validate_enabled)\n        self._publish_btn.setEnabled(publish_enabled)\n\n        if not publish_enabled:\n            self._publish_frame.set_shrunk_state(True)\n\n        self._update_publish_details_widget()\n\n    def _validate_create_instances(self):\n        if not self._controller.is_host_valid():\n            self._set_create_context_valid(True)\n            return\n\n        active_instances_by_id = {\n            instance.id: instance\n            for instance in self._controller.get_instance_items()\n            if instance.is_active\n        }\n        context_info_by_id = self._controller.get_instances_context_info(\n            active_instances_by_id.keys()\n        )\n        all_valid = None\n        for instance_id, instance in active_instances_by_id.items():\n            context_info = context_info_by_id[instance_id]\n            if not context_info.is_valid:\n                all_valid = False\n                break\n\n            if all_valid is None:\n                all_valid = True\n\n        self._set_create_context_valid(bool(all_valid))\n\n    def _on_create_model_reset(self):\n        self._validate_create_instances()\n\n        context_title = self._controller.get_context_title()\n        self.set_context_label(context_title)\n        self._update_publish_details_widget()\n\n    def _event_callback_validate_instances(self, _event):\n        self._validate_create_instances()\n\n    def _set_comment_input_visiblity(self, visible):\n        self._comment_input.setVisible(visible)\n        self._footer_spacer.setVisible(not visible)\n\n    def _update_publish_frame_rect(self):\n        if not self._publish_frame_visible:\n            return\n\n        window_size = self.size()\n        size_hint = self._publish_frame.minimumSizeHint()\n\n        width = window_size.width()\n        height = size_hint.height()\n\n        self._publish_frame.resize(width, height)\n\n        self._publish_frame.move(\n            0, window_size.height() - height\n        )\n\n    def add_error_message_dialog(self, title, failed_info, message_start=None):\n        self._error_messages_to_show.append(\n            (title, failed_info, message_start)\n        )\n        self._errors_dialog_message_timer.start()\n\n    def _on_errors_message_timeout(self):\n        if not self._error_messages_to_show:\n            self._errors_dialog_message_timer.stop()\n            return\n\n        item = self._error_messages_to_show.popleft()\n        title, failed_info, message_start = item\n        dialog = ErrorsMessageBox(\n            title, failed_info, message_start, self\n        )\n        dialog.exec_()\n        dialog.deleteLater()\n\n    def _on_creator_error(self, event):\n        new_failed_info = []\n        for item in event[\"failed_info\"]:\n            new_item = copy.deepcopy(item)\n            new_item[\"label\"] = new_item.pop(\"creator_label\")\n            new_item[\"identifier\"] = new_item.pop(\"creator_identifier\")\n            new_failed_info.append(new_item)\n        self.add_error_message_dialog(\n            event[\"title\"],\n             new_failed_info,\n             \"Creator:\"\n        )\n\n    def _on_convertor_error(self, event):\n        new_failed_info = []\n        for item in event[\"failed_info\"]:\n            new_item = copy.deepcopy(item)\n            new_item[\"identifier\"] = new_item.pop(\"convertor_identifier\")\n            new_failed_info.append(new_item)\n        self.add_error_message_dialog(\n            event[\"title\"], new_failed_info, \"Convertor:\"\n        )\n\n    def _on_action_error(self, event):\n        self.add_error_message_dialog(\n            event[\"title\"],\n            [{\n                \"message\": event[\"message\"],\n                \"traceback\": event[\"traceback\"],\n                \"label\": event[\"label\"],\n                \"identifier\": event[\"identifier\"]\n            }],\n            \"Action:\"\n        )\n\n    def _update_create_overlay_size(self):\n        metrics = self._create_overlay_button.fontMetrics()\n        height = int(metrics.height())\n        width = int(height * 0.7)\n        end_pos_x = self.width()\n        start_pos_x = end_pos_x - width\n\n        center = self._content_widget.parent().mapTo(\n            self,\n            self._content_widget.rect().center()\n        )\n        pos_y = center.y() - (height * 0.5)\n\n        self._create_overlay_button.setGeometry(\n            start_pos_x, pos_y,\n            width, height\n        )\n\n    def _update_create_overlay_visibility(self, global_pos=None):\n        if global_pos is None:\n            global_pos = QtGui.QCursor.pos()\n\n        under_mouse = False\n        my_pos = self.mapFromGlobal(global_pos)\n        if self.rect().contains(my_pos):\n            widget_geo = self._overview_widget.get_product_views_geo()\n            widget_x = widget_geo.left() + (widget_geo.width() * 0.5)\n            under_mouse = widget_x &lt; global_pos.x()\n        self._create_overlay_button.set_under_mouse(under_mouse)\n\n    def _copy_report(self):\n        data = self._controller.get_publish_report_data()\n        report_string = json.dumps(data, indent=4)\n\n        mime_data = QtCore.QMimeData()\n        mime_data.setText(report_string)\n        QtWidgets.QApplication.instance().clipboard().setMimeData(\n            mime_data\n        )\n        self._controller.emit_card_message(\n            \"Report added to clipboard\",\n            CardMessageTypes.info\n        )\n\n    def _export_report(self):\n        default_filename = \"publish-report-{}\".format(\n            time.strftime(\"%y%m%d-%H-%M\")\n        )\n        default_filepath = os.path.join(\n            os.path.expanduser(\"~\"),\n            default_filename\n        )\n        new_filepath, ext = QtWidgets.QFileDialog.getSaveFileName(\n            self, \"Save report\", default_filepath, \".json\"\n        )\n        if not ext or not new_filepath:\n            return\n\n        full_path = new_filepath + ext\n        self._controller.store_publish_report(full_path)\n        self._controller.emit_card_message(\n            \"Report saved\",\n            CardMessageTypes.info\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/window.html#client.ayon_core.tools.publisher.window.PublisherWindow.controller","title":"<code>controller</code>  <code>property</code>","text":"<p>Kept for compatibility with traypublisher.</p>"},{"location":"autoapi/client/ayon_core/tools/publisher/window.html#client.ayon_core.tools.publisher.window.PublisherWindow.set_comment","title":"<code>set_comment(comment)</code>","text":"<p>Change comment text.</p> Todos <p>Be able to set the comment via controller.</p> <p>Parameters:</p> Name Type Description Default <code>comment</code> <code>str</code> <p>Comment text.</p> required Source code in <code>client/ayon_core/tools/publisher/window.py</code> <pre><code>def set_comment(self, comment):\n    \"\"\"Change comment text.\n\n    Todos:\n        Be able to set the comment via controller.\n\n    Args:\n        comment (str): Comment text.\n    \"\"\"\n\n    self._comment_input.setText(comment)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/window.html#client.ayon_core.tools.publisher.window.PublisherWindow.set_tab_on_reset","title":"<code>set_tab_on_reset(tab)</code>","text":"<p>Define tab that will be selected on window show.</p> <p>This is single use method, when publisher window is showed the value is     unset and not used on next show.</p> <p>Parameters:</p> Name Type Description Default <code>tab (Union[int, Literal[create, publish, details, report]]</code> <p>Index or name of tab which will be selected on show (after reset).</p> required Source code in <code>client/ayon_core/tools/publisher/window.py</code> <pre><code>def set_tab_on_reset(self, tab):\n    \"\"\"Define tab that will be selected on window show.\n\n    This is single use method, when publisher window is showed the value is\n        unset and not used on next show.\n\n    Args:\n        tab (Union[int, Literal[create, publish, details, report]]: Index\n            or name of tab which will be selected on show (after reset).\n    \"\"\"\n\n    self._tab_on_reset = tab\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/window.html#client.ayon_core.tools.publisher.window.PublisherWindow.show_and_publish","title":"<code>show_and_publish(comment=None)</code>","text":"<p>Show the window and start publishing.</p> <p>The method will reset controller and start the publishing afterwards.</p> Todos <p>Move validations from '_on_publish_clicked' and change of     'comment' value in controller to controller so it can be     simplified.</p> <p>Parameters:</p> Name Type Description Default <code>comment</code> <code>Optional[str]</code> <p>Comment to be set to publish. If is set to 'None' a comment is not changed at all.</p> <code>None</code> Source code in <code>client/ayon_core/tools/publisher/window.py</code> <pre><code>def show_and_publish(self, comment=None):\n    \"\"\"Show the window and start publishing.\n\n    The method will reset controller and start the publishing afterwards.\n\n    Todos:\n        Move validations from '_on_publish_clicked' and change of\n            'comment' value in controller to controller so it can be\n            simplified.\n\n    Args:\n        comment (Optional[str]): Comment to be set to publish.\n            If is set to 'None' a comment is not changed at all.\n    \"\"\"\n\n    self._reset_on_show = False\n    self._reset_on_first_show = False\n\n    if comment is not None:\n        self.set_comment(comment)\n    self.make_sure_is_visible()\n    # Reset controller\n    self._controller.reset()\n    # Fake publish click to trigger save validation and propagate\n    #   comment to controller\n    self._on_publish_clicked()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/models/index.html","title":"models","text":""},{"location":"autoapi/client/ayon_core/tools/publisher/models/index.html#client.ayon_core.tools.publisher.models.CreateModel","title":"<code>CreateModel</code>","text":"Source code in <code>client/ayon_core/tools/publisher/models/create.py</code> <pre><code>class CreateModel:\n    _CONTEXT_KEYS = {\n        \"active\",\n        \"folderPath\",\n        \"task\",\n        \"variant\",\n        \"productName\",\n    }\n\n    def __init__(self, controller: AbstractPublisherBackend):\n        self._log = None\n        self._controller: AbstractPublisherBackend = controller\n\n        self._create_context = CreateContext(\n            controller.get_host(),\n            headless=controller.is_headless(),\n            reset=False\n        )\n        # State flags to prevent executing method which is already in progress\n        self._creator_items = None\n\n    @property\n    def log(self) -&gt; logging.Logger:\n        if self._log is None:\n            self._log = logging.getLogger(self.__class__.__name__)\n        return self._log\n\n    def is_host_valid(self) -&gt; bool:\n        return self._create_context.host_is_valid\n\n    def get_create_context(self) -&gt; CreateContext:\n        return self._create_context\n\n    def get_current_project_name(self) -&gt; Union[str, None]:\n        \"\"\"Current project context defined by host.\n\n        Returns:\n            str: Project name.\n\n        \"\"\"\n        return self._create_context.get_current_project_name()\n\n    def get_current_folder_path(self) -&gt; Union[str, None]:\n        \"\"\"Current context folder path defined by host.\n\n        Returns:\n            Union[str, None]: Folder path or None if folder is not set.\n        \"\"\"\n\n        return self._create_context.get_current_folder_path()\n\n    def get_current_task_name(self) -&gt; Union[str, None]:\n        \"\"\"Current context task name defined by host.\n\n        Returns:\n            Union[str, None]: Task name or None if task is not set.\n        \"\"\"\n\n        return self._create_context.get_current_task_name()\n\n    def host_context_has_changed(self) -&gt; bool:\n        return self._create_context.context_has_changed\n\n    def reset(self):\n        self._create_context.reset_preparation()\n\n        # Reset current context\n        self._create_context.reset_current_context()\n\n        self._create_context.reset_plugins()\n        # Reset creator items\n        self._creator_items = None\n\n        self._reset_instances()\n\n        self._emit_event(\"create.model.reset\")\n\n        self._create_context.add_instances_added_callback(\n            self._cc_added_instance\n        )\n        self._create_context.add_instances_removed_callback(\n            self._cc_removed_instance\n        )\n        self._create_context.add_value_changed_callback(\n            self._cc_value_changed\n        )\n        self._create_context.add_pre_create_attr_defs_change_callback(\n            self._cc_pre_create_attr_changed\n        )\n        self._create_context.add_create_attr_defs_change_callback(\n            self._cc_create_attr_changed\n        )\n        self._create_context.add_publish_attr_defs_change_callback(\n            self._cc_publish_attr_changed\n        )\n        self._create_context.add_instance_requirement_change_callback(\n            self._cc_instance_requirement_changed\n        )\n        self._create_context.add_instance_parent_change_callback(\n            self._cc_instance_parent_changed\n        )\n\n        self._create_context.reset_finalization()\n\n    def get_creator_items(self) -&gt; dict[str, CreatorItem]:\n        \"\"\"Creators that can be shown in create dialog.\"\"\"\n        if self._creator_items is None:\n            self._refresh_creator_items()\n        return self._creator_items\n\n    def get_creator_item_by_id(\n        self, identifier: str\n    ) -&gt; Union[CreatorItem, None]:\n        items = self.get_creator_items()\n        return items.get(identifier)\n\n    def get_creator_icon(\n        self, identifier: str\n    ) -&gt; Union[str, Dict[str, Any], None]:\n        \"\"\"Function to receive icon for creator identifier.\n\n        Args:\n            identifier (str): Creator's identifier for which should\n                be icon returned.\n\n        \"\"\"\n        creator_item = self.get_creator_item_by_id(identifier)\n        if creator_item is not None:\n            return creator_item.icon\n        return None\n\n    def get_instance_items(self) -&gt; List[InstanceItem]:\n        \"\"\"Current instances in create context.\"\"\"\n        return [\n            InstanceItem.from_instance(instance)\n            for instance in self._create_context.instances_by_id.values()\n        ]\n\n    def get_instance_item_by_id(\n        self, instance_id: str\n    ) -&gt; Union[InstanceItem, None]:\n        instance = self._create_context.instances_by_id.get(instance_id)\n        if instance is None:\n            return None\n\n        return InstanceItem.from_instance(instance)\n\n    def get_instance_items_by_id(\n        self, instance_ids: Optional[Iterable[str]] = None\n    ) -&gt; Dict[str, Union[InstanceItem, None]]:\n        if instance_ids is None:\n            instance_ids = self._create_context.instances_by_id.keys()\n        return {\n            instance_id: self.get_instance_item_by_id(instance_id)\n            for instance_id in instance_ids\n        }\n\n    def get_instances_context_info(\n        self, instance_ids: Optional[Iterable[str]] = None\n    ):\n        instances = self._get_instances_by_id(instance_ids).values()\n        return self._create_context.get_instances_context_info(\n            instances\n        )\n\n    def set_instances_context_info(self, changes_by_instance_id):\n        with self._create_context.bulk_value_changes(CREATE_EVENT_SOURCE):\n            for instance_id, changes in changes_by_instance_id.items():\n                instance = self._get_instance_by_id(instance_id)\n                for key, value in changes.items():\n                    instance[key] = value\n        self._emit_event(\n            \"create.model.instances.context.changed\",\n            {\n                \"instance_ids\": list(changes_by_instance_id.keys())\n            }\n        )\n\n    def set_instances_active_state(\n        self, active_state_by_id: Dict[str, bool]\n    ):\n        changed_ids = set()\n        with self._create_context.bulk_value_changes(CREATE_EVENT_SOURCE):\n            for instance_id, active in active_state_by_id.items():\n                instance = self._create_context.get_instance_by_id(instance_id)\n                if instance[\"active\"] is not active:\n                    instance[\"active\"] = active\n                    changed_ids.add(instance_id)\n\n        if not changed_ids:\n            return\n\n        self._emit_event(\n            \"create.model.instances.context.changed\",\n            {\n                \"instance_ids\": changed_ids\n            }\n        )\n\n    def get_convertor_items(self) -&gt; Dict[str, ConvertorItem]:\n        return self._create_context.convertor_items_by_id\n\n    def get_product_name(\n        self,\n        creator_identifier: str,\n        product_type: str,\n        variant: str,\n        folder_path: Union[str, None],\n        task_name: Union[str, None],\n        instance_id: Optional[str] = None\n    ) -&gt; str:\n        \"\"\"Get product name based on passed data.\n\n        Args:\n            creator_identifier (str): Identifier of creator which should be\n                responsible for product name creation.\n            variant (str): Variant value from user's input.\n            folder_path (str): Folder path for which is instance created.\n            task_name (str): Name of task for which is instance created.\n            instance_id (Union[str, None]): Existing instance id when product\n                name is updated.\n        \"\"\"\n\n        creator = self._creators[creator_identifier]\n\n        instance = None\n        if instance_id:\n            instance = self._get_instance_by_id(instance_id)\n\n        project_name = self._controller.get_current_project_name()\n        folder_item = self._controller.get_folder_item_by_path(\n            project_name, folder_path\n        )\n        folder_entity = None\n        task_item = None\n        task_entity = None\n        if folder_item is not None:\n            folder_entity = self._controller.get_folder_entity(\n                project_name, folder_item.entity_id\n            )\n            task_item = self._controller.get_task_item_by_name(\n                project_name,\n                folder_item.entity_id,\n                task_name,\n                CREATE_EVENT_SOURCE\n            )\n\n        if task_item is not None:\n            task_entity = self._controller.get_task_entity(\n                project_name, task_item.task_id\n            )\n\n        project_entity = self._controller.get_project_entity(project_name)\n        args = (\n            project_name,\n            folder_entity,\n            task_entity,\n            variant\n        )\n        kwargs = {\n            \"instance\": instance,\n            # Backwards compatibility for 'project_entity' argument (24/07/08)\n            \"project_entity\": project_entity,\n            \"product_type\": product_type,\n        }\n        # Backwards compatibility for 'project_entity' argument (24/07/08)\n        # Backwards compatibility for 'product_type' argument (26/01/19)\n        for kwarg in (\n            \"product_type\",\n            \"project_entity\",\n        ):\n            if not is_func_signature_supported(\n                creator.get_product_name, *args, **kwargs\n            ):\n                kwargs.pop(kwarg)\n        return creator.get_product_name(*args, **kwargs)\n\n    def create(\n        self,\n        creator_identifier: str,\n        product_name: str,\n        instance_data: Dict[str, Any],\n        options: Dict[str, Any],\n    ):\n        \"\"\"Trigger creation and refresh of instances in UI.\"\"\"\n\n        success = True\n        try:\n            with self._create_context.bulk_add_instances():\n                self._create_context.create_with_unified_error(\n                    creator_identifier,\n                    product_name,\n                    instance_data,\n                    options,\n                )\n\n        except CreatorsOperationFailed as exc:\n            success = False\n            self._emit_event(\n                \"instances.create.failed\",\n                {\n                    \"title\": \"Creation failed\",\n                    \"failed_info\": exc.failed_info\n                }\n            )\n\n        return success\n\n    def trigger_convertor_items(self, convertor_identifiers: List[str]):\n        \"\"\"Trigger legacy item convertors.\n\n        This functionality requires to save and reset CreateContext. The reset\n        is needed so Creators can collect converted items.\n\n        Args:\n            convertor_identifiers (list[str]): Identifiers of convertor\n                plugins.\n        \"\"\"\n\n        success = True\n        try:\n            self._create_context.run_convertors(convertor_identifiers)\n\n        except ConvertorsOperationFailed as exc:\n            success = False\n            self._emit_event(\n                \"convertors.convert.failed\",\n                {\n                    \"title\": \"Conversion failed\",\n                    \"failed_info\": exc.failed_info\n                }\n            )\n\n        if success:\n            self._controller.emit_card_message(\n                \"Conversion finished\"\n            )\n        else:\n            self._controller.emit_card_message(\n                \"Conversion failed\",\n                CardMessageTypes.error\n            )\n\n    def save_changes(self, show_message: Optional[bool] = True) -&gt; bool:\n        \"\"\"Save changes happened during creation.\n\n        Trigger save of changes using host api. This functionality does not\n        validate anything. It is required to do checks before this method is\n        called to be able to give user actionable response e.g. check of\n        context using 'host_context_has_changed'.\n\n        Args:\n            show_message (bool): Show message that changes were\n                saved successfully.\n\n        Returns:\n            bool: Save of changes was successful.\n        \"\"\"\n\n        if not self._create_context.host_is_valid:\n            # TODO remove\n            # Fake success save when host is not valid for CreateContext\n            #   this is for testing as experimental feature\n            return True\n\n        try:\n            self._create_context.save_changes()\n            if show_message:\n                self._controller.emit_card_message(\"Saved changes..\")\n            return True\n\n        except CreatorsOperationFailed as exc:\n            self._emit_event(\n                \"instances.save.failed\",\n                {\n                    \"title\": \"Instances save failed\",\n                    \"failed_info\": exc.failed_info\n                }\n            )\n\n        return False\n\n    def remove_instances(self, instance_ids: List[str]):\n        \"\"\"Remove instances based on instance ids.\n\n        Args:\n            instance_ids (List[str]): List of instance ids to remove.\n        \"\"\"\n\n        # QUESTION Expect that instances are really removed? In that case reset\n        #    is not required.\n        self._remove_instances_from_context(instance_ids)\n\n    def set_instances_create_attr_values(self, instance_ids, key, value):\n        self._set_instances_create_attr_values(instance_ids, key, value)\n\n    def revert_instances_create_attr_values(self, instance_ids, key):\n        self._set_instances_create_attr_values(\n            instance_ids, key, _DEFAULT_VALUE\n        )\n\n    def get_creator_attribute_definitions(\n        self, instance_ids: List[str]\n    ) -&gt; List[Tuple[AbstractAttrDef, Dict[str, Dict[str, Any]]]]:\n        \"\"\"Collect creator attribute definitions for multuple instances.\n\n        Args:\n            instance_ids (List[str]): List of created instances for\n                which should be attribute definitions returned.\n\n        \"\"\"\n        # NOTE it would be great if attrdefs would have hash method implemented\n        #   so they could be used as keys in dictionary\n        output = []\n        _attr_defs = {}\n        for instance_id in instance_ids:\n            instance = self._get_instance_by_id(instance_id)\n            for attr_def in instance.creator_attribute_defs:\n                found_idx = None\n                for idx, _attr_def in _attr_defs.items():\n                    if attr_def == _attr_def:\n                        found_idx = idx\n                        break\n\n                value = None\n                if attr_def.is_value_def:\n                    value = instance.creator_attributes[attr_def.key]\n\n                if found_idx is None:\n                    idx = len(output)\n                    output.append((\n                        attr_def,\n                        {\n                            instance_id: {\n                                \"value\": value,\n                                \"default\": attr_def.default\n                            }\n                        }\n                    ))\n                    _attr_defs[idx] = attr_def\n                else:\n                    _, info_by_id = output[found_idx]\n                    info_by_id[instance_id] = {\n                        \"value\": value,\n                        \"default\": attr_def.default\n                    }\n\n        return output\n\n    def set_instances_publish_attr_values(\n        self, instance_ids, plugin_name, key, value\n    ):\n        self._set_instances_publish_attr_values(\n            instance_ids, plugin_name, key, value\n        )\n\n    def revert_instances_publish_attr_values(\n        self, instance_ids, plugin_name, key\n    ):\n        self._set_instances_publish_attr_values(\n            instance_ids, plugin_name, key, _DEFAULT_VALUE\n        )\n\n    def trigger_pre_create_button_callback(\n        self, identifier: str, button_name: str\n    ) -&gt; None:\n        self._create_context.trigger_pre_create_button_callback(\n            identifier, button_name\n        )\n\n    def trigger_create_button_callback(\n        self,\n        button_name: str,\n        instance_ids: list[str],\n    ) -&gt; None:\n        self._create_context.trigger_create_button_callback(\n            button_name, instance_ids\n        )\n\n    def trigger_publish_button_callback(\n        self,\n        plugin_name: str,\n        button_name: str,\n        instance_ids: list[str | None],\n    ) -&gt; None:\n        self._create_context.trigger_publish_button_callback(\n            plugin_name, button_name, instance_ids\n        )\n\n    def get_publish_attribute_definitions(\n        self,\n        instance_ids: List[str],\n        include_context: bool\n    ) -&gt; list[PublishAttrDefsInfo]:\n        \"\"\"Collect publish attribute definitions for passed instances.\n\n        Args:\n            instance_ids (List[str]): List of created instances for\n                which should be attribute definitions returned.\n            include_context (bool): Add context specific attribute definitions.\n\n        \"\"\"\n        _tmp_items = []\n        if include_context:\n            _tmp_items.append(self._create_context)\n\n        for instance_id in instance_ids:\n            _tmp_items.append(self._get_instance_by_id(instance_id))\n\n        all_defs_by_plugin_name = {}\n        all_plugin_values = {}\n        instance_ids_by_name = {}\n        for item in _tmp_items:\n            item_id = None\n            if isinstance(item, CreatedInstance):\n                item_id = item.id\n\n            for plugin_name, attr_val in item.publish_attributes.items():\n                if not isinstance(attr_val, AttributeValues):\n                    continue\n                attr_defs = attr_val.attr_defs\n                if not attr_defs:\n                    continue\n\n                plugin_attr_defs = all_defs_by_plugin_name.setdefault(\n                    plugin_name, []\n                )\n                instance_ids = instance_ids_by_name.get(plugin_name)\n                if instance_ids is None:\n                    instance_ids = set()\n                    instance_ids_by_name[plugin_name] = instance_ids\n                instance_ids.add(item_id)\n\n                plugin_values = all_plugin_values.setdefault(plugin_name, {})\n                plugin_attr_defs.append(attr_defs)\n\n                for attr_def in attr_defs:\n                    if isinstance(attr_def, UIDef):\n                        continue\n                    attr_values = plugin_values.setdefault(attr_def.key, [])\n                    attr_values.append(\n                        (item_id, attr_val[attr_def.key], attr_def.default)\n                    )\n\n        attr_defs_by_plugin_name = {}\n        for plugin_name, attr_defs in all_defs_by_plugin_name.items():\n            attr_defs_by_plugin_name[plugin_name] = merge_attr_defs(attr_defs)\n\n        output = []\n        for plugin in self._create_context.plugins_with_defs:\n            plugin_name = plugin.__name__\n            if plugin_name not in all_defs_by_plugin_name:\n                continue\n            instance_ids = instance_ids_by_name[plugin_name]\n            output.append(\n                PublishAttrDefsInfo(\n                    plugin_name,\n                    attr_defs_by_plugin_name[plugin_name],\n                    all_plugin_values[plugin_name],\n                    instance_ids,\n                )\n            )\n        return output\n\n    def get_thumbnail_paths_for_instances(\n        self, instance_ids: List[str]\n    ) -&gt; Dict[str, Union[str, None]]:\n        thumbnail_paths_by_instance_id = (\n            self._create_context.thumbnail_paths_by_instance_id\n        )\n        return {\n            instance_id: thumbnail_paths_by_instance_id.get(instance_id)\n            for instance_id in instance_ids\n        }\n\n    def set_thumbnail_paths_for_instances(\n        self, thumbnail_path_mapping: Dict[str, str]\n    ):\n        thumbnail_paths_by_instance_id = (\n            self._create_context.thumbnail_paths_by_instance_id\n        )\n        for instance_id, thumbnail_path in thumbnail_path_mapping.items():\n            thumbnail_paths_by_instance_id[instance_id] = thumbnail_path\n\n        self._emit_event(\n            \"instance.thumbnail.changed\",\n            {\n                \"mapping\": thumbnail_path_mapping\n            }\n        )\n\n    def _emit_event(\n        self,\n        topic: str,\n        data: Optional[Dict[str, Any]] = None\n    ):\n        self._controller.emit_event(topic, data, CREATE_EVENT_SOURCE)\n\n    def _get_current_project_settings(self) -&gt; Dict[str, Any]:\n        \"\"\"Current project settings.\n\n        Returns:\n            dict\n        \"\"\"\n\n        return self._create_context.get_current_project_settings()\n\n    @property\n    def _creators(self) -&gt; Dict[str, BaseCreator]:\n        \"\"\"All creators loaded in create context.\"\"\"\n\n        return self._create_context.creators\n\n    def _get_instance_by_id(\n        self, instance_id: str\n    ) -&gt; Union[CreatedInstance, None]:\n        return self._create_context.instances_by_id.get(instance_id)\n\n    def _get_instances_by_id(\n        self, instance_ids: Optional[Iterable[str]]\n    ) -&gt; Dict[str, Union[CreatedInstance, None]]:\n        if instance_ids is None:\n            instance_ids = self._create_context.instances_by_id.keys()\n        return {\n            instance_id: self._get_instance_by_id(instance_id)\n            for instance_id in instance_ids\n        }\n\n    def _reset_instances(self):\n        \"\"\"Reset create instances.\"\"\"\n\n        self._create_context.reset_context_data()\n        with self._create_context.bulk_add_instances():\n            try:\n                self._create_context.reset_instances()\n            except CreatorsOperationFailed as exc:\n                self._emit_event(\n                    \"instances.collection.failed\",\n                    {\n                        \"title\": \"Instance collection failed\",\n                        \"failed_info\": exc.failed_info\n                    }\n                )\n\n            try:\n                self._create_context.find_convertor_items()\n            except ConvertorsOperationFailed as exc:\n                self._emit_event(\n                    \"convertors.find.failed\",\n                    {\n                        \"title\": \"Collection of unsupported product failed\",\n                        \"failed_info\": exc.failed_info\n                    }\n                )\n\n            try:\n                self._create_context.execute_autocreators()\n\n            except CreatorsOperationFailed as exc:\n                self._emit_event(\n                    \"instances.create.failed\",\n                    {\n                        \"title\": \"AutoCreation failed\",\n                        \"failed_info\": exc.failed_info\n                    }\n                )\n\n    def _remove_instances_from_context(self, instance_ids: List[str]):\n        instances_by_id = self._create_context.instances_by_id\n        instances = [\n            instances_by_id[instance_id]\n            for instance_id in instance_ids\n        ]\n        try:\n            self._create_context.remove_instances(instances)\n        except CreatorsOperationFailed as exc:\n            self._emit_event(\n                \"instances.remove.failed\",\n                {\n                    \"title\": \"Instance removement failed\",\n                    \"failed_info\": exc.failed_info\n                }\n            )\n\n    def _refresh_creator_items(self, identifiers=None):\n        if identifiers is None or self._creator_items is None:\n            identifiers = set(self._create_context.creators.keys())\n\n        if self._creator_items is None:\n            self._creator_items = {}\n\n        allowed_creator_pattern = self._get_allowed_creators_pattern()\n\n        for identifier in identifiers:\n            creator = self._create_context.creators.get(identifier)\n            if creator is None:\n                continue\n\n            try:\n                creator_item = CreatorItem.from_creator(creator)\n            except Exception:\n                self.log.error(\n                    \"Failed to create creator item for '%s'\",\n                    identifier,\n                    exc_info=True\n                )\n                self._creator_items.pop(identifier, None)\n                continue\n\n            self._creator_items[identifier] = creator_item\n            for ui_item in creator_item.ui_items:\n                ui_item.filtered = not self._is_label_allowed(\n                    ui_item.label, allowed_creator_pattern\n                )\n                if ui_item.filtered:\n                    self.log.debug(f\"{ui_item.label} not allowed for context\")\n\n    def _set_instances_create_attr_values(self, instance_ids, key, value):\n        with self._create_context.bulk_value_changes(CREATE_EVENT_SOURCE):\n            for instance_id in instance_ids:\n                instance = self._get_instance_by_id(instance_id)\n                creator_attributes = instance[\"creator_attributes\"]\n                attr_def = creator_attributes.get_attr_def(key)\n                if (\n                    attr_def is None\n                    or not attr_def.is_value_def\n                    or not attr_def.visible\n                    or not attr_def.enabled\n                ):\n                    continue\n\n                if value is _DEFAULT_VALUE:\n                    creator_attributes[key] = attr_def.default\n\n                elif attr_def.is_value_valid(value):\n                    creator_attributes[key] = copy.deepcopy(value)\n\n    def _set_instances_publish_attr_values(\n        self, instance_ids, plugin_name, key, value\n    ):\n        with self._create_context.bulk_value_changes(CREATE_EVENT_SOURCE):\n            for instance_id in instance_ids:\n                if instance_id is None:\n                    instance = self._create_context\n                else:\n                    instance = self._get_instance_by_id(instance_id)\n                plugin_val = instance.publish_attributes[plugin_name]\n                attr_def = plugin_val.get_attr_def(key)\n                # Ignore if attribute is not available or enabled/visible\n                #   on the instance, or the value is not valid for definition\n                if (\n                    attr_def is None\n                    or not attr_def.is_value_def\n                    or not attr_def.visible\n                    or not attr_def.enabled\n                ):\n                    continue\n\n                if value is _DEFAULT_VALUE:\n                    plugin_val[key] = attr_def.default\n\n                elif attr_def.is_value_valid(value):\n                    plugin_val[key] = value\n\n    def _cc_added_instance(self, event):\n        instance_ids = {\n            instance.id\n            for instance in event.data[\"instances\"]\n        }\n        self._emit_event(\n            \"create.context.added.instance\",\n            {\"instance_ids\": instance_ids},\n        )\n\n    def _cc_removed_instance(self, event):\n        instance_ids = {\n            instance.id\n            for instance in event.data[\"instances\"]\n        }\n        self._emit_event(\n            \"create.context.removed.instance\",\n            {\"instance_ids\": instance_ids},\n        )\n\n    def _cc_value_changed(self, event):\n        if event.source == CREATE_EVENT_SOURCE:\n            return\n\n        instance_changes = {}\n        context_changed_ids = set()\n        for item in event.data[\"changes\"]:\n            instance_id = None\n            if item[\"instance\"]:\n                instance_id = item[\"instance\"].id\n            changes = item[\"changes\"]\n            instance_changes[instance_id] = changes\n            if instance_id is None:\n                continue\n\n            if self._CONTEXT_KEYS.intersection(set(changes)):\n                context_changed_ids.add(instance_id)\n\n        self._emit_event(\n            \"create.context.value.changed\",\n            {\"instance_changes\": instance_changes},\n        )\n        if context_changed_ids:\n            self._emit_event(\n                \"create.model.instances.context.changed\",\n                {\"instance_ids\": list(context_changed_ids)},\n            )\n\n    def _cc_pre_create_attr_changed(self, event):\n        identifiers = event[\"identifiers\"]\n        self._refresh_creator_items(identifiers)\n        self._emit_event(\n            \"create.context.pre.create.attrs.changed\",\n            {\"identifiers\": identifiers},\n        )\n\n    def _cc_create_attr_changed(self, event):\n        instance_ids = {\n            instance.id\n            for instance in event.data[\"instances\"]\n        }\n        self._emit_event(\n            \"create.context.create.attrs.changed\",\n            {\"instance_ids\": instance_ids},\n        )\n\n    def _cc_publish_attr_changed(self, event):\n        instance_changes = event.data[\"instance_changes\"]\n        event_data = {\n            instance_id: instance_data[\"plugin_names\"]\n            for instance_id, instance_data in instance_changes.items()\n        }\n        self._emit_event(\n            \"create.context.publish.attrs.changed\",\n            event_data,\n        )\n\n    def _cc_instance_requirement_changed(self, event):\n        instance_ids = {\n            instance.id\n            for instance in event.data[\"instances\"]\n        }\n        self._emit_event(\n            \"create.model.instance.requirement.changed\",\n            {\"instance_ids\": instance_ids},\n        )\n\n    def _cc_instance_parent_changed(self, event):\n        instance_ids = {\n            instance.id\n            for instance in event.data[\"instances\"]\n        }\n        self._emit_event(\n            \"create.model.instance.parent.changed\",\n            {\"instance_ids\": instance_ids},\n        )\n\n    def _get_allowed_creators_pattern(self) -&gt; Union[Pattern, None]:\n        \"\"\"Provide regex pattern for configured creator labels in this context\n\n        If no profile matches current context, it shows all creators.\n        Support usage of regular expressions for configured values.\n        Returns:\n            (re.Pattern)[optional]: None or regex compiled patterns\n                into single one ('Render|Image.*')\n        \"\"\"\n\n        task_type = self._create_context.get_current_task_type()\n        project_settings = self._get_current_project_settings()\n\n        filter_creator_profiles = (\n            project_settings\n            [\"core\"]\n            [\"tools\"]\n            [\"creator\"]\n            [\"filter_creator_profiles\"]\n        )\n        filtering_criteria = {\n            \"task_names\": self.get_current_task_name(),\n            \"task_types\": task_type,\n            \"host_names\": self._create_context.host_name\n        }\n        profile = filter_profiles(\n            filter_creator_profiles,\n            filtering_criteria,\n            logger=self.log\n        )\n\n        allowed_creator_pattern = None\n        if profile:\n            allowed_creator_labels = {\n                label\n                for label in profile[\"creator_labels\"]\n                if label\n            }\n            self.log.debug(f\"Only allowed `{allowed_creator_labels}` creators\")\n            allowed_creator_pattern = (\n                re.compile(\"|\".join(allowed_creator_labels)))\n        return allowed_creator_pattern\n\n    def _is_label_allowed(\n        self,\n        label: str,\n        allowed_labels_regex: Union[Pattern, None]\n    ) -&gt; bool:\n        \"\"\"Implement regex support for allowed labels.\n\n        Args:\n            label (str): Label of creator - shown in Publisher\n            allowed_labels_regex (re.Pattern): compiled regular expression\n        \"\"\"\n        if not allowed_labels_regex:\n            return True\n        return bool(allowed_labels_regex.match(label))\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/models/index.html#client.ayon_core.tools.publisher.models.CreateModel.create","title":"<code>create(creator_identifier, product_name, instance_data, options)</code>","text":"<p>Trigger creation and refresh of instances in UI.</p> Source code in <code>client/ayon_core/tools/publisher/models/create.py</code> <pre><code>def create(\n    self,\n    creator_identifier: str,\n    product_name: str,\n    instance_data: Dict[str, Any],\n    options: Dict[str, Any],\n):\n    \"\"\"Trigger creation and refresh of instances in UI.\"\"\"\n\n    success = True\n    try:\n        with self._create_context.bulk_add_instances():\n            self._create_context.create_with_unified_error(\n                creator_identifier,\n                product_name,\n                instance_data,\n                options,\n            )\n\n    except CreatorsOperationFailed as exc:\n        success = False\n        self._emit_event(\n            \"instances.create.failed\",\n            {\n                \"title\": \"Creation failed\",\n                \"failed_info\": exc.failed_info\n            }\n        )\n\n    return success\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/models/index.html#client.ayon_core.tools.publisher.models.CreateModel.get_creator_attribute_definitions","title":"<code>get_creator_attribute_definitions(instance_ids)</code>","text":"<p>Collect creator attribute definitions for multuple instances.</p> <p>Parameters:</p> Name Type Description Default <code>instance_ids</code> <code>List[str]</code> <p>List of created instances for which should be attribute definitions returned.</p> required Source code in <code>client/ayon_core/tools/publisher/models/create.py</code> <pre><code>def get_creator_attribute_definitions(\n    self, instance_ids: List[str]\n) -&gt; List[Tuple[AbstractAttrDef, Dict[str, Dict[str, Any]]]]:\n    \"\"\"Collect creator attribute definitions for multuple instances.\n\n    Args:\n        instance_ids (List[str]): List of created instances for\n            which should be attribute definitions returned.\n\n    \"\"\"\n    # NOTE it would be great if attrdefs would have hash method implemented\n    #   so they could be used as keys in dictionary\n    output = []\n    _attr_defs = {}\n    for instance_id in instance_ids:\n        instance = self._get_instance_by_id(instance_id)\n        for attr_def in instance.creator_attribute_defs:\n            found_idx = None\n            for idx, _attr_def in _attr_defs.items():\n                if attr_def == _attr_def:\n                    found_idx = idx\n                    break\n\n            value = None\n            if attr_def.is_value_def:\n                value = instance.creator_attributes[attr_def.key]\n\n            if found_idx is None:\n                idx = len(output)\n                output.append((\n                    attr_def,\n                    {\n                        instance_id: {\n                            \"value\": value,\n                            \"default\": attr_def.default\n                        }\n                    }\n                ))\n                _attr_defs[idx] = attr_def\n            else:\n                _, info_by_id = output[found_idx]\n                info_by_id[instance_id] = {\n                    \"value\": value,\n                    \"default\": attr_def.default\n                }\n\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/models/index.html#client.ayon_core.tools.publisher.models.CreateModel.get_creator_icon","title":"<code>get_creator_icon(identifier)</code>","text":"<p>Function to receive icon for creator identifier.</p> <p>Parameters:</p> Name Type Description Default <code>identifier</code> <code>str</code> <p>Creator's identifier for which should be icon returned.</p> required Source code in <code>client/ayon_core/tools/publisher/models/create.py</code> <pre><code>def get_creator_icon(\n    self, identifier: str\n) -&gt; Union[str, Dict[str, Any], None]:\n    \"\"\"Function to receive icon for creator identifier.\n\n    Args:\n        identifier (str): Creator's identifier for which should\n            be icon returned.\n\n    \"\"\"\n    creator_item = self.get_creator_item_by_id(identifier)\n    if creator_item is not None:\n        return creator_item.icon\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/models/index.html#client.ayon_core.tools.publisher.models.CreateModel.get_creator_items","title":"<code>get_creator_items()</code>","text":"<p>Creators that can be shown in create dialog.</p> Source code in <code>client/ayon_core/tools/publisher/models/create.py</code> <pre><code>def get_creator_items(self) -&gt; dict[str, CreatorItem]:\n    \"\"\"Creators that can be shown in create dialog.\"\"\"\n    if self._creator_items is None:\n        self._refresh_creator_items()\n    return self._creator_items\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/models/index.html#client.ayon_core.tools.publisher.models.CreateModel.get_current_folder_path","title":"<code>get_current_folder_path()</code>","text":"<p>Current context folder path defined by host.</p> <p>Returns:</p> Type Description <code>Union[str, None]</code> <p>Union[str, None]: Folder path or None if folder is not set.</p> Source code in <code>client/ayon_core/tools/publisher/models/create.py</code> <pre><code>def get_current_folder_path(self) -&gt; Union[str, None]:\n    \"\"\"Current context folder path defined by host.\n\n    Returns:\n        Union[str, None]: Folder path or None if folder is not set.\n    \"\"\"\n\n    return self._create_context.get_current_folder_path()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/models/index.html#client.ayon_core.tools.publisher.models.CreateModel.get_current_project_name","title":"<code>get_current_project_name()</code>","text":"<p>Current project context defined by host.</p> <p>Returns:</p> Name Type Description <code>str</code> <code>Union[str, None]</code> <p>Project name.</p> Source code in <code>client/ayon_core/tools/publisher/models/create.py</code> <pre><code>def get_current_project_name(self) -&gt; Union[str, None]:\n    \"\"\"Current project context defined by host.\n\n    Returns:\n        str: Project name.\n\n    \"\"\"\n    return self._create_context.get_current_project_name()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/models/index.html#client.ayon_core.tools.publisher.models.CreateModel.get_current_task_name","title":"<code>get_current_task_name()</code>","text":"<p>Current context task name defined by host.</p> <p>Returns:</p> Type Description <code>Union[str, None]</code> <p>Union[str, None]: Task name or None if task is not set.</p> Source code in <code>client/ayon_core/tools/publisher/models/create.py</code> <pre><code>def get_current_task_name(self) -&gt; Union[str, None]:\n    \"\"\"Current context task name defined by host.\n\n    Returns:\n        Union[str, None]: Task name or None if task is not set.\n    \"\"\"\n\n    return self._create_context.get_current_task_name()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/models/index.html#client.ayon_core.tools.publisher.models.CreateModel.get_instance_items","title":"<code>get_instance_items()</code>","text":"<p>Current instances in create context.</p> Source code in <code>client/ayon_core/tools/publisher/models/create.py</code> <pre><code>def get_instance_items(self) -&gt; List[InstanceItem]:\n    \"\"\"Current instances in create context.\"\"\"\n    return [\n        InstanceItem.from_instance(instance)\n        for instance in self._create_context.instances_by_id.values()\n    ]\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/models/index.html#client.ayon_core.tools.publisher.models.CreateModel.get_product_name","title":"<code>get_product_name(creator_identifier, product_type, variant, folder_path, task_name, instance_id=None)</code>","text":"<p>Get product name based on passed data.</p> <p>Parameters:</p> Name Type Description Default <code>creator_identifier</code> <code>str</code> <p>Identifier of creator which should be responsible for product name creation.</p> required <code>variant</code> <code>str</code> <p>Variant value from user's input.</p> required <code>folder_path</code> <code>str</code> <p>Folder path for which is instance created.</p> required <code>task_name</code> <code>str</code> <p>Name of task for which is instance created.</p> required <code>instance_id</code> <code>Union[str, None]</code> <p>Existing instance id when product name is updated.</p> <code>None</code> Source code in <code>client/ayon_core/tools/publisher/models/create.py</code> <pre><code>def get_product_name(\n    self,\n    creator_identifier: str,\n    product_type: str,\n    variant: str,\n    folder_path: Union[str, None],\n    task_name: Union[str, None],\n    instance_id: Optional[str] = None\n) -&gt; str:\n    \"\"\"Get product name based on passed data.\n\n    Args:\n        creator_identifier (str): Identifier of creator which should be\n            responsible for product name creation.\n        variant (str): Variant value from user's input.\n        folder_path (str): Folder path for which is instance created.\n        task_name (str): Name of task for which is instance created.\n        instance_id (Union[str, None]): Existing instance id when product\n            name is updated.\n    \"\"\"\n\n    creator = self._creators[creator_identifier]\n\n    instance = None\n    if instance_id:\n        instance = self._get_instance_by_id(instance_id)\n\n    project_name = self._controller.get_current_project_name()\n    folder_item = self._controller.get_folder_item_by_path(\n        project_name, folder_path\n    )\n    folder_entity = None\n    task_item = None\n    task_entity = None\n    if folder_item is not None:\n        folder_entity = self._controller.get_folder_entity(\n            project_name, folder_item.entity_id\n        )\n        task_item = self._controller.get_task_item_by_name(\n            project_name,\n            folder_item.entity_id,\n            task_name,\n            CREATE_EVENT_SOURCE\n        )\n\n    if task_item is not None:\n        task_entity = self._controller.get_task_entity(\n            project_name, task_item.task_id\n        )\n\n    project_entity = self._controller.get_project_entity(project_name)\n    args = (\n        project_name,\n        folder_entity,\n        task_entity,\n        variant\n    )\n    kwargs = {\n        \"instance\": instance,\n        # Backwards compatibility for 'project_entity' argument (24/07/08)\n        \"project_entity\": project_entity,\n        \"product_type\": product_type,\n    }\n    # Backwards compatibility for 'project_entity' argument (24/07/08)\n    # Backwards compatibility for 'product_type' argument (26/01/19)\n    for kwarg in (\n        \"product_type\",\n        \"project_entity\",\n    ):\n        if not is_func_signature_supported(\n            creator.get_product_name, *args, **kwargs\n        ):\n            kwargs.pop(kwarg)\n    return creator.get_product_name(*args, **kwargs)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/models/index.html#client.ayon_core.tools.publisher.models.CreateModel.get_publish_attribute_definitions","title":"<code>get_publish_attribute_definitions(instance_ids, include_context)</code>","text":"<p>Collect publish attribute definitions for passed instances.</p> <p>Parameters:</p> Name Type Description Default <code>instance_ids</code> <code>List[str]</code> <p>List of created instances for which should be attribute definitions returned.</p> required <code>include_context</code> <code>bool</code> <p>Add context specific attribute definitions.</p> required Source code in <code>client/ayon_core/tools/publisher/models/create.py</code> <pre><code>def get_publish_attribute_definitions(\n    self,\n    instance_ids: List[str],\n    include_context: bool\n) -&gt; list[PublishAttrDefsInfo]:\n    \"\"\"Collect publish attribute definitions for passed instances.\n\n    Args:\n        instance_ids (List[str]): List of created instances for\n            which should be attribute definitions returned.\n        include_context (bool): Add context specific attribute definitions.\n\n    \"\"\"\n    _tmp_items = []\n    if include_context:\n        _tmp_items.append(self._create_context)\n\n    for instance_id in instance_ids:\n        _tmp_items.append(self._get_instance_by_id(instance_id))\n\n    all_defs_by_plugin_name = {}\n    all_plugin_values = {}\n    instance_ids_by_name = {}\n    for item in _tmp_items:\n        item_id = None\n        if isinstance(item, CreatedInstance):\n            item_id = item.id\n\n        for plugin_name, attr_val in item.publish_attributes.items():\n            if not isinstance(attr_val, AttributeValues):\n                continue\n            attr_defs = attr_val.attr_defs\n            if not attr_defs:\n                continue\n\n            plugin_attr_defs = all_defs_by_plugin_name.setdefault(\n                plugin_name, []\n            )\n            instance_ids = instance_ids_by_name.get(plugin_name)\n            if instance_ids is None:\n                instance_ids = set()\n                instance_ids_by_name[plugin_name] = instance_ids\n            instance_ids.add(item_id)\n\n            plugin_values = all_plugin_values.setdefault(plugin_name, {})\n            plugin_attr_defs.append(attr_defs)\n\n            for attr_def in attr_defs:\n                if isinstance(attr_def, UIDef):\n                    continue\n                attr_values = plugin_values.setdefault(attr_def.key, [])\n                attr_values.append(\n                    (item_id, attr_val[attr_def.key], attr_def.default)\n                )\n\n    attr_defs_by_plugin_name = {}\n    for plugin_name, attr_defs in all_defs_by_plugin_name.items():\n        attr_defs_by_plugin_name[plugin_name] = merge_attr_defs(attr_defs)\n\n    output = []\n    for plugin in self._create_context.plugins_with_defs:\n        plugin_name = plugin.__name__\n        if plugin_name not in all_defs_by_plugin_name:\n            continue\n        instance_ids = instance_ids_by_name[plugin_name]\n        output.append(\n            PublishAttrDefsInfo(\n                plugin_name,\n                attr_defs_by_plugin_name[plugin_name],\n                all_plugin_values[plugin_name],\n                instance_ids,\n            )\n        )\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/models/index.html#client.ayon_core.tools.publisher.models.CreateModel.remove_instances","title":"<code>remove_instances(instance_ids)</code>","text":"<p>Remove instances based on instance ids.</p> <p>Parameters:</p> Name Type Description Default <code>instance_ids</code> <code>List[str]</code> <p>List of instance ids to remove.</p> required Source code in <code>client/ayon_core/tools/publisher/models/create.py</code> <pre><code>def remove_instances(self, instance_ids: List[str]):\n    \"\"\"Remove instances based on instance ids.\n\n    Args:\n        instance_ids (List[str]): List of instance ids to remove.\n    \"\"\"\n\n    # QUESTION Expect that instances are really removed? In that case reset\n    #    is not required.\n    self._remove_instances_from_context(instance_ids)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/models/index.html#client.ayon_core.tools.publisher.models.CreateModel.save_changes","title":"<code>save_changes(show_message=True)</code>","text":"<p>Save changes happened during creation.</p> <p>Trigger save of changes using host api. This functionality does not validate anything. It is required to do checks before this method is called to be able to give user actionable response e.g. check of context using 'host_context_has_changed'.</p> <p>Parameters:</p> Name Type Description Default <code>show_message</code> <code>bool</code> <p>Show message that changes were saved successfully.</p> <code>True</code> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>Save of changes was successful.</p> Source code in <code>client/ayon_core/tools/publisher/models/create.py</code> <pre><code>def save_changes(self, show_message: Optional[bool] = True) -&gt; bool:\n    \"\"\"Save changes happened during creation.\n\n    Trigger save of changes using host api. This functionality does not\n    validate anything. It is required to do checks before this method is\n    called to be able to give user actionable response e.g. check of\n    context using 'host_context_has_changed'.\n\n    Args:\n        show_message (bool): Show message that changes were\n            saved successfully.\n\n    Returns:\n        bool: Save of changes was successful.\n    \"\"\"\n\n    if not self._create_context.host_is_valid:\n        # TODO remove\n        # Fake success save when host is not valid for CreateContext\n        #   this is for testing as experimental feature\n        return True\n\n    try:\n        self._create_context.save_changes()\n        if show_message:\n            self._controller.emit_card_message(\"Saved changes..\")\n        return True\n\n    except CreatorsOperationFailed as exc:\n        self._emit_event(\n            \"instances.save.failed\",\n            {\n                \"title\": \"Instances save failed\",\n                \"failed_info\": exc.failed_info\n            }\n        )\n\n    return False\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/models/index.html#client.ayon_core.tools.publisher.models.CreateModel.trigger_convertor_items","title":"<code>trigger_convertor_items(convertor_identifiers)</code>","text":"<p>Trigger legacy item convertors.</p> <p>This functionality requires to save and reset CreateContext. The reset is needed so Creators can collect converted items.</p> <p>Parameters:</p> Name Type Description Default <code>convertor_identifiers</code> <code>list[str]</code> <p>Identifiers of convertor plugins.</p> required Source code in <code>client/ayon_core/tools/publisher/models/create.py</code> <pre><code>def trigger_convertor_items(self, convertor_identifiers: List[str]):\n    \"\"\"Trigger legacy item convertors.\n\n    This functionality requires to save and reset CreateContext. The reset\n    is needed so Creators can collect converted items.\n\n    Args:\n        convertor_identifiers (list[str]): Identifiers of convertor\n            plugins.\n    \"\"\"\n\n    success = True\n    try:\n        self._create_context.run_convertors(convertor_identifiers)\n\n    except ConvertorsOperationFailed as exc:\n        success = False\n        self._emit_event(\n            \"convertors.convert.failed\",\n            {\n                \"title\": \"Conversion failed\",\n                \"failed_info\": exc.failed_info\n            }\n        )\n\n    if success:\n        self._controller.emit_card_message(\n            \"Conversion finished\"\n        )\n    else:\n        self._controller.emit_card_message(\n            \"Conversion failed\",\n            CardMessageTypes.error\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/models/index.html#client.ayon_core.tools.publisher.models.CreatorItem","title":"<code>CreatorItem</code>","text":"<p>Wrapper around Creator plugin.</p> <p>Object can be serialized and recreated.</p> Source code in <code>client/ayon_core/tools/publisher/models/create.py</code> <pre><code>class CreatorItem:\n    \"\"\"Wrapper around Creator plugin.\n\n    Object can be serialized and recreated.\n    \"\"\"\n\n    def __init__(\n        self,\n        identifier: str,\n        creator_type: CreatorType,\n        product_base_type: str,\n        label: str,\n        group_label: str,\n        icon: IconBase | dict[str, Any] | str | None,\n        description: Union[str, None],\n        detailed_description: Union[str, None],\n        default_variant: Union[str, None],\n        default_variants: Union[List[str], None],\n        create_allow_context_change: Union[bool, None],\n        create_allow_thumbnail: Union[bool, None],\n        show_order: int,\n        pre_create_attributes_defs: List[AbstractAttrDef],\n        ui_items: list[CreatorUIItem],\n    ):\n        self.identifier: str = identifier\n        self.creator_type: CreatorType = creator_type\n        self.product_base_type: str = product_base_type\n        self.label: str = label\n        self.group_label: str = group_label\n        self.icon: IconBase | dict[str, Any] | str | None = icon\n        self.description: Union[str, None] = description\n        self.detailed_description: Union[bool, None] = detailed_description\n        self.default_variant: Union[bool, None] = default_variant\n        self.default_variants: Union[List[str], None] = default_variants\n        self.create_allow_context_change: Union[bool, None] = (\n            create_allow_context_change\n        )\n        self.create_allow_thumbnail: Union[bool, None] = create_allow_thumbnail\n        self.show_order: int = show_order\n        self.pre_create_attributes_defs: List[AbstractAttrDef] = (\n            pre_create_attributes_defs\n        )\n        self.ui_items: list[CreatorUIItem] = ui_items\n\n    def get_group_label(self) -&gt; str:\n        return self.group_label\n\n    @classmethod\n    def from_creator(cls, creator: BaseCreator) -&gt; \"CreatorItem\":\n        creator_type: CreatorType = CreatorTypes.base\n        if isinstance(creator, AutoCreator):\n            creator_type = CreatorTypes.auto\n        elif isinstance(creator, HiddenCreator):\n            creator_type = CreatorTypes.hidden\n        elif isinstance(creator, Creator):\n            creator_type = CreatorTypes.artist\n\n        description = None\n        detail_description = None\n        default_variant = None\n        default_variants = None\n        pre_create_attr_defs = None\n        create_allow_context_change = None\n        create_allow_thumbnail = None\n        show_order = creator.order\n        if creator_type is CreatorTypes.artist:\n            description = creator.get_description()\n            detail_description = creator.get_detail_description()\n            default_variant = creator.get_default_variant()\n            default_variants = creator.get_default_variants()\n            pre_create_attr_defs = creator.get_pre_create_attr_defs()\n            create_allow_context_change = creator.create_allow_context_change\n            create_allow_thumbnail = creator.create_allow_thumbnail\n            show_order = creator.show_order\n\n        ui_items = []\n        product_type_items: list[ProductTypeItem] = (\n            creator.get_product_type_items() or []\n        )\n        for item in product_type_items:\n            ui_item = CreatorUIItem(\n                item.product_type,\n                item.label or creator.label,\n            )\n            ui_items.append(ui_item)\n\n        if not ui_items:\n            ui_items.append(CreatorUIItem(\n                creator.product_base_type,\n                creator.label,\n            ))\n\n        identifier = creator.identifier\n        return cls(\n            identifier,\n            creator_type,\n            creator.product_base_type,\n            creator.label or identifier,\n            creator.get_group_label(),\n            creator.get_icon(),\n            description,\n            detail_description,\n            default_variant,\n            default_variants,\n            create_allow_context_change,\n            create_allow_thumbnail,\n            show_order,\n            pre_create_attr_defs,\n            ui_items,\n        )\n\n    def to_data(self) -&gt; Dict[str, Any]:\n        pre_create_attributes_defs = None\n        if self.pre_create_attributes_defs is not None:\n            pre_create_attributes_defs = serialize_attr_defs(\n                self.pre_create_attributes_defs\n            )\n        icon = self.icon\n        if isinstance(icon, IconBase):\n            icon = icon.to_data()\n            icon[\"__iconBase__\"] = True\n\n        return {\n            \"identifier\": self.identifier,\n            \"creator_type\": str(self.creator_type),\n            \"product_base_type\": self.product_base_type,\n            \"label\": self.label,\n            \"group_label\": self.group_label,\n            \"icon\": self.icon,\n            \"description\": self.description,\n            \"detailed_description\": self.detailed_description,\n            \"default_variant\": self.default_variant,\n            \"default_variants\": self.default_variants,\n            \"create_allow_context_change\": self.create_allow_context_change,\n            \"create_allow_thumbnail\": self.create_allow_thumbnail,\n            \"show_order\": self.show_order,\n            \"pre_create_attributes_defs\": pre_create_attributes_defs,\n            \"ui_items\": [item.to_data() for item in self.ui_items],\n        }\n\n    @classmethod\n    def from_data(cls, data: Dict[str, Any]) -&gt; \"CreatorItem\":\n        icon = data[\"icon\"]\n        if isinstance(icon, dict) and icon.pop(\"__iconBase__\", False):\n            data[\"icon\"] = get_icon_def_from_data(icon)\n\n        pre_create_attributes_defs = data[\"pre_create_attributes_defs\"]\n        if pre_create_attributes_defs is not None:\n            data[\"pre_create_attributes_defs\"] = deserialize_attr_defs(\n                pre_create_attributes_defs\n            )\n\n        data[\"creator_type\"] = CreatorTypes.from_str(data[\"creator_type\"])\n        data[\"ui_items\"] = [\n            CreatorUIItem.from_data(item)\n            for item in data[\"ui_items\"]\n        ]\n        return cls(**data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/models/index.html#client.ayon_core.tools.publisher.models.PublishModel","title":"<code>PublishModel</code>","text":"Source code in <code>client/ayon_core/tools/publisher/models/publish.py</code> <pre><code>class PublishModel:\n    def __init__(self, controller: AbstractPublisherBackend):\n        self._controller = controller\n\n        self._log_to_console: bool = env_value_to_bool(\n            \"AYON_PUBLISHER_PRINT_LOGS\", default=False\n        )\n\n        # Publishing should stop at validation stage\n        self._publish_up_validation: bool = False\n        self._publish_comment_is_set: bool = False\n\n        # Any other exception that happened during publishing\n        self._publish_error_info: PublishErrorInfo | None = None\n        # Publishing is in progress\n        self._publish_is_running: bool = False\n        # Publishing is over validation order\n        self._publish_has_validated: bool = False\n\n        self._publish_has_validation_errors: bool = False\n        self._publish_has_crashed: bool = False\n        # All publish plugins are processed\n        self._publish_has_started: bool = False\n        self._publish_has_finished: bool = False\n        self._publish_max_progress: int = 0\n        self._publish_progress: int = 0\n\n        self._publish_plugins: list[pyblish.api.Plugin] = []\n        self._publish_plugins_proxy: PublishPluginsProxy = (\n            PublishPluginsProxy([])\n        )\n\n        # pyblish.api.Context\n        self._publish_context = None\n        # Pyblish report\n        self._publish_report: PublishReportMaker = PublishReportMaker()\n        # Store exceptions of publish error\n        self._publish_errors: PublishErrors = PublishErrors()\n\n        # This information is not much important for controller but for widget\n        #   which can change (and set) the comment.\n        self._publish_comment_is_set: bool = False\n\n        # Validation order\n        # - plugin with order same or higher than this value is extractor or\n        #   higher\n        self._validation_order: int = (\n            pyblish.api.ValidatorOrder + PLUGIN_ORDER_OFFSET\n        )\n\n        # Plugin iterator\n        self._main_thread_iter: Generator[partial] = self._default_iterator()\n\n        self._log_handler: MessageHandler = MessageHandler()\n\n    def reset(self):\n        # Allow to change behavior during process lifetime\n        self._log_to_console = env_value_to_bool(\n            \"AYON_PUBLISHER_PRINT_LOGS\", default=False\n        )\n\n        create_context = self._controller.get_create_context()\n\n        self._publish_up_validation = False\n        self._publish_comment_is_set = False\n        self._publish_has_started = False\n\n        self._set_publish_error_info(None)\n        self._set_progress(0)\n        self._set_is_running(False)\n        self._set_has_validated(False)\n        self._set_is_crashed(False)\n        self._set_has_validation_errors(False)\n        self._set_finished(False)\n\n        self._main_thread_iter = self._publish_iterator()\n        self._publish_context = pyblish.api.Context()\n        # Make sure \"comment\" is set on publish context\n        self._publish_context.data[\"comment\"] = \"\"\n        # Add access to create context during publishing\n        # - must not be used for changing CreatedInstances during publishing!\n        # QUESTION\n        # - pop the key after first collector using it would be safest option?\n        self._publish_context.data[\"create_context\"] = create_context\n        publish_plugins = create_context.publish_plugins\n        self._publish_plugins = publish_plugins\n        self._publish_plugins_proxy = PublishPluginsProxy(\n            publish_plugins\n        )\n        blocking_crashed_paths = filter_crashed_publish_paths(\n            create_context.get_current_project_name(),\n            set(create_context.publish_discover_result.crashed_file_paths),\n            project_settings=create_context.get_current_project_settings(),\n        )\n        self._publish_report.reset_with_discover_results(\n            create_context.creator_discover_result,\n            create_context.convertor_discover_result,\n            create_context.publish_discover_result,\n            blocking_crashed_paths,\n        )\n        for plugin in create_context.publish_plugins_mismatch_targets:\n            self._publish_report.set_plugin_skipped(plugin.id)\n        self._publish_errors.reset(self._publish_plugins_proxy)\n\n        self._set_max_progress(len(publish_plugins))\n\n        self._emit_event(\"publish.reset.finished\")\n\n    def set_publish_up_validation(self, value: bool):\n        self._publish_up_validation = value\n\n    def start_publish(self, wait: bool = True):\n        \"\"\"Run publishing.\n\n        Make sure all changes are saved before method is called (Call\n        'save_changes' and check output).\n        \"\"\"\n        if self._publish_up_validation and self._publish_has_validated:\n            return\n\n        self._start_publish()\n\n        if not wait:\n            return\n\n        while self.is_running():\n            func = self.get_next_process_func()\n            func()\n\n    def get_next_process_func(self) -&gt; partial:\n        # Raise error if this function is called when publishing\n        #   is not running\n        if not self._publish_is_running:\n            raise ValueError(\"Publish is not running\")\n\n        # Validations of progress before using iterator\n        # Any unexpected error happened\n        # - everything should stop\n        if self._publish_has_crashed:\n            return partial(self.stop_publish)\n\n        # Stop if validation is over and validation errors happened\n        #   or publishing should stop at validation\n        if (\n            self._publish_has_validated\n            and (\n                self._publish_has_validation_errors\n                or self._publish_up_validation\n            )\n        ):\n            return partial(self.stop_publish)\n\n        # Everything is ok so try to get new processing item\n        return next(self._main_thread_iter)\n\n    def stop_publish(self):\n        if self._publish_is_running:\n            self._stop_publish()\n\n    def is_running(self) -&gt; bool:\n        return self._publish_is_running\n\n    def is_crashed(self) -&gt; bool:\n        return self._publish_has_crashed\n\n    def has_started(self) -&gt; bool:\n        return self._publish_has_started\n\n    def has_finished(self) -&gt; bool:\n        return self._publish_has_finished\n\n    def has_validated(self) -&gt; bool:\n        return self._publish_has_validated\n\n    def has_validation_errors(self) -&gt; bool:\n        return self._publish_has_validation_errors\n\n    def publish_can_continue(self) -&gt; bool:\n        return (\n            not self._publish_has_crashed\n            and not self._publish_has_validation_errors\n            and not self._publish_has_finished\n        )\n\n    def get_progress(self) -&gt; int:\n        return self._publish_progress\n\n    def get_max_progress(self) -&gt; int:\n        return self._publish_max_progress\n\n    def get_publish_report(self) -&gt; PublishReport:\n        self._publish_report.update_publish_instances(self._publish_context)\n        return self._publish_report.get_report()\n\n    def get_publish_report_data(self) -&gt; dict[str, Any]:\n        report: PublishReport = self.get_publish_report()\n        return report.to_data(self._publish_report.current_plugin_id)\n\n    def get_publish_errors_report(self) -&gt; PublishErrorsReport:\n        return self._publish_errors.create_report()\n\n    def get_error_info(self) -&gt; PublishErrorInfo | None:\n        return self._publish_error_info\n\n    def store_publish_report(self, filepath: str) -&gt; None:\n        report: PublishReport = self.get_publish_report()\n        report.store_to_file(filepath)\n\n    def set_comment(self, comment: str):\n        # Ignore change of comment when publishing started\n        if self._publish_has_started:\n            return\n        self._publish_context.data[\"comment\"] = comment\n        self._publish_comment_is_set = True\n\n    def run_action(self, plugin_id: str, action_id: str):\n        # TODO handle result in UI\n        plugin = self._publish_plugins_proxy.get_plugin(plugin_id)\n        action = self._publish_plugins_proxy.get_action(plugin_id, action_id)\n\n        result = pyblish.plugin.process(\n            plugin, self._publish_context, None, action.id\n        )\n        exception = result.get(\"error\")\n        if exception:\n            self._emit_event(\n                \"publish.action.failed\",\n                {\n                    \"title\": \"Action failed\",\n                    \"message\": \"Action failed.\",\n                    \"traceback\": \"\".join(\n                        traceback.format_exception(\n                            type(exception),\n                            exception,\n                            exception.__traceback__\n                        )\n                    ),\n                    \"label\": action.__name__,\n                    \"identifier\": action.id\n                }\n            )\n\n        self._publish_report.add_action_result(action, result)\n\n        self._controller.emit_card_message(\"Action finished.\")\n\n    def _emit_event(self, topic: str, data: dict[str, Any] | None = None):\n        self._controller.emit_event(topic, data, PUBLISH_EVENT_SOURCE)\n\n    def _set_finished(self, value: bool):\n        if self._publish_has_finished != value:\n            self._publish_has_finished = value\n            self._emit_event(\n                \"publish.finished.changed\",\n                {\"value\": value}\n            )\n\n    def _set_is_running(self, value: bool):\n        if self._publish_is_running != value:\n            self._publish_is_running = value\n            self._emit_event(\n                \"publish.is_running.changed\",\n                {\"value\": value}\n            )\n\n    def _set_has_validated(self, value: bool):\n        if self._publish_has_validated != value:\n            self._publish_has_validated = value\n            self._emit_event(\n                \"publish.has_validated.changed\",\n                {\"value\": value}\n            )\n\n    def _set_is_crashed(self, value: bool):\n        if self._publish_has_crashed != value:\n            self._publish_has_crashed = value\n            self._emit_event(\n                \"publish.has_crashed.changed\",\n                {\"value\": value}\n            )\n\n    def _set_has_validation_errors(self, value: bool):\n        if self._publish_has_validation_errors != value:\n            self._publish_has_validation_errors = value\n            self._emit_event(\n                \"publish.has_validation_errors.changed\",\n                {\"value\": value}\n            )\n\n    def _set_max_progress(self, value: int):\n        if self._publish_max_progress != value:\n            self._publish_max_progress = value\n            self._emit_event(\n                \"publish.max_progress.changed\",\n                {\"value\": value}\n            )\n\n    def _set_progress(self, value: int):\n        if self._publish_progress != value:\n            self._publish_progress = value\n            self._emit_event(\n                \"publish.progress.changed\",\n                {\"value\": value}\n            )\n\n    def _set_publish_error_info(self, value: PublishErrorInfo | None):\n        if self._publish_error_info != value:\n            self._publish_error_info = value\n            self._emit_event(\n                \"publish.publish_error.changed\",\n                {\"value\": value}\n            )\n\n    def _default_iterator(self):\n        \"\"\"Iterator used on initialization.\n\n        Should be replaced by real iterator when 'reset' is called.\n\n        Returns:\n            collections.abc.Generator[partial]: Generator with partial\n                functions that should be called in main thread.\n\n        \"\"\"\n        while True:\n            yield partial(self.stop_publish)\n\n    def _start_publish(self):\n        \"\"\"Start or continue in publishing.\"\"\"\n        if self._publish_is_running:\n            return\n\n        self._set_is_running(True)\n        self._publish_has_started = True\n\n        self._emit_event(\"publish.process.started\")\n\n    def _stop_publish(self):\n        \"\"\"Stop or pause publishing.\"\"\"\n        self._set_is_running(False)\n\n        self._emit_event(\"publish.process.stopped\")\n\n    def _publish_iterator(self) -&gt; Iterable[partial]:\n        \"\"\"Main logic center of publishing.\n\n        Iterator returns `partial` objects with callbacks that should be\n        processed in main thread (threaded in future?). Cares about changing\n        states of currently processed publish plugin and instance. Also\n        change state of processed orders like validation order has passed etc.\n\n        Also stops publishing, if should stop on validation.\n        \"\"\"\n\n        for idx, plugin in enumerate(self._publish_plugins):\n            self._publish_progress = idx\n\n            # Check if plugin is over validation order\n            if (\n                not self._publish_has_validated\n                and plugin.order &gt;= self._validation_order\n            ):\n                self._set_has_validated(True)\n                if (\n                    self._publish_up_validation\n                    or self._publish_has_validation_errors\n                ):\n                    yield partial(self.stop_publish)\n\n            # Add plugin to publish report\n            self._publish_report.add_plugin_iter(\n                plugin.id, self._publish_context)\n\n            # WARNING This is hack fix for optional plugins\n            if not self._is_publish_plugin_active(plugin):\n                self._publish_report.set_plugin_skipped(plugin.id)\n                continue\n\n            # Trigger callback that new plugin is going to be processed\n            plugin_label = plugin.__name__\n            if hasattr(plugin, \"label\") and plugin.label:\n                plugin_label = plugin.label\n            self._emit_event(\n                \"publish.process.plugin.changed\",\n                {\"plugin_label\": plugin_label}\n            )\n\n            # Plugin is instance plugin\n            if plugin.__instanceEnabled__:\n                instances = pyblish.logic.instances_by_plugin(\n                    self._publish_context, plugin\n                )\n                if not instances:\n                    self._publish_report.set_plugin_skipped(plugin.id)\n                    continue\n\n                for instance in instances:\n                    if instance.data.get(\"publish\") is False:\n                        continue\n\n                    instance_label = (\n                        instance.data.get(\"label\")\n                        or instance.data[\"name\"]\n                    )\n                    self._emit_event(\n                        \"publish.process.instance.changed\",\n                        {\"instance_label\": instance_label}\n                    )\n\n                    yield partial(\n                        self._process_and_continue, plugin, instance\n                    )\n            else:\n                families = collect_families_from_instances(\n                    self._publish_context, only_active=True\n                )\n                plugins = pyblish.logic.plugins_by_families(\n                    [plugin], families\n                )\n                if not plugins:\n                    self._publish_report.set_plugin_skipped(plugin.id)\n                    continue\n\n                instance_label = (\n                    self._publish_context.data.get(\"label\")\n                    or self._publish_context.data.get(\"name\")\n                    or \"Context\"\n                )\n                self._emit_event(\n                    \"publish.process.instance.changed\",\n                    {\"instance_label\": instance_label}\n                )\n                yield partial(\n                    self._process_and_continue, plugin, None\n                )\n\n            self._publish_report.set_plugin_passed(plugin.id)\n\n        # Cleanup of publishing process\n        self._set_finished(True)\n        self._set_progress(self._publish_max_progress)\n        yield partial(self.stop_publish)\n\n    @contextmanager\n    def _log_manager(self, plugin: pyblish.api.Plugin):\n        root = logging.getLogger()\n        if not self._log_to_console:\n            plugin.log.propagate = False\n            plugin.log.addHandler(self._log_handler)\n            root.addHandler(self._log_handler)\n\n        try:\n            if self._log_to_console:\n                yield None\n            else:\n                yield self._log_handler\n\n        finally:\n            if not self._log_to_console:\n                plugin.log.propagate = True\n                plugin.log.removeHandler(self._log_handler)\n                root.removeHandler(self._log_handler)\n            self._log_handler.clear_records()\n\n    def _process_and_continue(\n        self,\n        plugin: pyblish.api.Plugin,\n        instance: pyblish.api.Instance\n    ):\n        with self._log_manager(plugin) as log_handler:\n            result = pyblish.plugin.process(\n                plugin, self._publish_context, instance\n            )\n            if log_handler is not None:\n                records = log_handler.get_records()\n                exception = result.get(\"error\")\n                if exception is not None and records:\n                    last_record = records[-1]\n                    if (\n                        last_record.name == \"pyblish.plugin\"\n                        and last_record.levelno == logging.ERROR\n                    ):\n                        # Remove last record made by pyblish\n                        # - `log.exception(formatted_traceback)`\n                        records.pop(-1)\n                result[\"records\"] = records\n\n        exception = result.get(\"error\")\n        if exception:\n            if (\n                isinstance(exception, PublishValidationError)\n                and not self._publish_has_validated\n            ):\n                result[\"is_validation_error\"] = True\n                self._add_validation_error(result)\n\n            else:\n                if isinstance(exception, PublishError):\n                    if not exception.title:\n                        exception.title = plugin.label or plugin.__name__\n                    self._add_publish_error_to_report(result)\n\n                error_info = PublishErrorInfo.from_exception(exception)\n                self._set_publish_error_info(error_info)\n                self._set_is_crashed(True)\n\n                result[\"is_validation_error\"] = False\n\n        self._publish_report.add_result(plugin.id, result)\n\n    def _add_validation_error(self, result: dict[str, Any]):\n        self._set_has_validation_errors(True)\n        self._add_publish_error_to_report(result)\n\n    def _add_publish_error_to_report(self, result: dict[str, Any]):\n        self._publish_errors.add_error(\n            result[\"plugin\"],\n            result[\"error\"],\n            result[\"instance\"]\n        )\n\n    def _is_publish_plugin_active(self, plugin: pyblish.api.Plugin) -&gt; bool:\n        \"\"\"Decide if publish plugin is active.\n\n        This is hack because 'active' is mis-used in mixin\n        'OptionalPyblishPluginMixin' where 'active' is used for default value\n        of optional plugins. Because of that is 'active' state of plugin\n        which inherit from 'OptionalPyblishPluginMixin' ignored. That affects\n        headless publishing inside host, potentially remote publishing.\n\n        We have to change that to match pyblish base, but we can do that\n        only when all hosts use Publisher because the change requires\n        change of settings schemas.\n\n        Args:\n            plugin (pyblish.Plugin): Plugin which should be checked if is\n                active.\n\n        Returns:\n            bool: Is plugin active.\n        \"\"\"\n\n        if plugin.active:\n            return True\n\n        if not plugin.optional:\n            return False\n\n        if OptionalPyblishPluginMixin in inspect.getmro(plugin):\n            return True\n        return False\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/models/index.html#client.ayon_core.tools.publisher.models.PublishModel.start_publish","title":"<code>start_publish(wait=True)</code>","text":"<p>Run publishing.</p> <p>Make sure all changes are saved before method is called (Call 'save_changes' and check output).</p> Source code in <code>client/ayon_core/tools/publisher/models/publish.py</code> <pre><code>def start_publish(self, wait: bool = True):\n    \"\"\"Run publishing.\n\n    Make sure all changes are saved before method is called (Call\n    'save_changes' and check output).\n    \"\"\"\n    if self._publish_up_validation and self._publish_has_validated:\n        return\n\n    self._start_publish()\n\n    if not wait:\n        return\n\n    while self.is_running():\n        func = self.get_next_process_func()\n        func()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/models/create.html","title":"create","text":""},{"location":"autoapi/client/ayon_core/tools/publisher/models/create.html#client.ayon_core.tools.publisher.models.create.CreateModel","title":"<code>CreateModel</code>","text":"Source code in <code>client/ayon_core/tools/publisher/models/create.py</code> <pre><code>class CreateModel:\n    _CONTEXT_KEYS = {\n        \"active\",\n        \"folderPath\",\n        \"task\",\n        \"variant\",\n        \"productName\",\n    }\n\n    def __init__(self, controller: AbstractPublisherBackend):\n        self._log = None\n        self._controller: AbstractPublisherBackend = controller\n\n        self._create_context = CreateContext(\n            controller.get_host(),\n            headless=controller.is_headless(),\n            reset=False\n        )\n        # State flags to prevent executing method which is already in progress\n        self._creator_items = None\n\n    @property\n    def log(self) -&gt; logging.Logger:\n        if self._log is None:\n            self._log = logging.getLogger(self.__class__.__name__)\n        return self._log\n\n    def is_host_valid(self) -&gt; bool:\n        return self._create_context.host_is_valid\n\n    def get_create_context(self) -&gt; CreateContext:\n        return self._create_context\n\n    def get_current_project_name(self) -&gt; Union[str, None]:\n        \"\"\"Current project context defined by host.\n\n        Returns:\n            str: Project name.\n\n        \"\"\"\n        return self._create_context.get_current_project_name()\n\n    def get_current_folder_path(self) -&gt; Union[str, None]:\n        \"\"\"Current context folder path defined by host.\n\n        Returns:\n            Union[str, None]: Folder path or None if folder is not set.\n        \"\"\"\n\n        return self._create_context.get_current_folder_path()\n\n    def get_current_task_name(self) -&gt; Union[str, None]:\n        \"\"\"Current context task name defined by host.\n\n        Returns:\n            Union[str, None]: Task name or None if task is not set.\n        \"\"\"\n\n        return self._create_context.get_current_task_name()\n\n    def host_context_has_changed(self) -&gt; bool:\n        return self._create_context.context_has_changed\n\n    def reset(self):\n        self._create_context.reset_preparation()\n\n        # Reset current context\n        self._create_context.reset_current_context()\n\n        self._create_context.reset_plugins()\n        # Reset creator items\n        self._creator_items = None\n\n        self._reset_instances()\n\n        self._emit_event(\"create.model.reset\")\n\n        self._create_context.add_instances_added_callback(\n            self._cc_added_instance\n        )\n        self._create_context.add_instances_removed_callback(\n            self._cc_removed_instance\n        )\n        self._create_context.add_value_changed_callback(\n            self._cc_value_changed\n        )\n        self._create_context.add_pre_create_attr_defs_change_callback(\n            self._cc_pre_create_attr_changed\n        )\n        self._create_context.add_create_attr_defs_change_callback(\n            self._cc_create_attr_changed\n        )\n        self._create_context.add_publish_attr_defs_change_callback(\n            self._cc_publish_attr_changed\n        )\n        self._create_context.add_instance_requirement_change_callback(\n            self._cc_instance_requirement_changed\n        )\n        self._create_context.add_instance_parent_change_callback(\n            self._cc_instance_parent_changed\n        )\n\n        self._create_context.reset_finalization()\n\n    def get_creator_items(self) -&gt; dict[str, CreatorItem]:\n        \"\"\"Creators that can be shown in create dialog.\"\"\"\n        if self._creator_items is None:\n            self._refresh_creator_items()\n        return self._creator_items\n\n    def get_creator_item_by_id(\n        self, identifier: str\n    ) -&gt; Union[CreatorItem, None]:\n        items = self.get_creator_items()\n        return items.get(identifier)\n\n    def get_creator_icon(\n        self, identifier: str\n    ) -&gt; Union[str, Dict[str, Any], None]:\n        \"\"\"Function to receive icon for creator identifier.\n\n        Args:\n            identifier (str): Creator's identifier for which should\n                be icon returned.\n\n        \"\"\"\n        creator_item = self.get_creator_item_by_id(identifier)\n        if creator_item is not None:\n            return creator_item.icon\n        return None\n\n    def get_instance_items(self) -&gt; List[InstanceItem]:\n        \"\"\"Current instances in create context.\"\"\"\n        return [\n            InstanceItem.from_instance(instance)\n            for instance in self._create_context.instances_by_id.values()\n        ]\n\n    def get_instance_item_by_id(\n        self, instance_id: str\n    ) -&gt; Union[InstanceItem, None]:\n        instance = self._create_context.instances_by_id.get(instance_id)\n        if instance is None:\n            return None\n\n        return InstanceItem.from_instance(instance)\n\n    def get_instance_items_by_id(\n        self, instance_ids: Optional[Iterable[str]] = None\n    ) -&gt; Dict[str, Union[InstanceItem, None]]:\n        if instance_ids is None:\n            instance_ids = self._create_context.instances_by_id.keys()\n        return {\n            instance_id: self.get_instance_item_by_id(instance_id)\n            for instance_id in instance_ids\n        }\n\n    def get_instances_context_info(\n        self, instance_ids: Optional[Iterable[str]] = None\n    ):\n        instances = self._get_instances_by_id(instance_ids).values()\n        return self._create_context.get_instances_context_info(\n            instances\n        )\n\n    def set_instances_context_info(self, changes_by_instance_id):\n        with self._create_context.bulk_value_changes(CREATE_EVENT_SOURCE):\n            for instance_id, changes in changes_by_instance_id.items():\n                instance = self._get_instance_by_id(instance_id)\n                for key, value in changes.items():\n                    instance[key] = value\n        self._emit_event(\n            \"create.model.instances.context.changed\",\n            {\n                \"instance_ids\": list(changes_by_instance_id.keys())\n            }\n        )\n\n    def set_instances_active_state(\n        self, active_state_by_id: Dict[str, bool]\n    ):\n        changed_ids = set()\n        with self._create_context.bulk_value_changes(CREATE_EVENT_SOURCE):\n            for instance_id, active in active_state_by_id.items():\n                instance = self._create_context.get_instance_by_id(instance_id)\n                if instance[\"active\"] is not active:\n                    instance[\"active\"] = active\n                    changed_ids.add(instance_id)\n\n        if not changed_ids:\n            return\n\n        self._emit_event(\n            \"create.model.instances.context.changed\",\n            {\n                \"instance_ids\": changed_ids\n            }\n        )\n\n    def get_convertor_items(self) -&gt; Dict[str, ConvertorItem]:\n        return self._create_context.convertor_items_by_id\n\n    def get_product_name(\n        self,\n        creator_identifier: str,\n        product_type: str,\n        variant: str,\n        folder_path: Union[str, None],\n        task_name: Union[str, None],\n        instance_id: Optional[str] = None\n    ) -&gt; str:\n        \"\"\"Get product name based on passed data.\n\n        Args:\n            creator_identifier (str): Identifier of creator which should be\n                responsible for product name creation.\n            variant (str): Variant value from user's input.\n            folder_path (str): Folder path for which is instance created.\n            task_name (str): Name of task for which is instance created.\n            instance_id (Union[str, None]): Existing instance id when product\n                name is updated.\n        \"\"\"\n\n        creator = self._creators[creator_identifier]\n\n        instance = None\n        if instance_id:\n            instance = self._get_instance_by_id(instance_id)\n\n        project_name = self._controller.get_current_project_name()\n        folder_item = self._controller.get_folder_item_by_path(\n            project_name, folder_path\n        )\n        folder_entity = None\n        task_item = None\n        task_entity = None\n        if folder_item is not None:\n            folder_entity = self._controller.get_folder_entity(\n                project_name, folder_item.entity_id\n            )\n            task_item = self._controller.get_task_item_by_name(\n                project_name,\n                folder_item.entity_id,\n                task_name,\n                CREATE_EVENT_SOURCE\n            )\n\n        if task_item is not None:\n            task_entity = self._controller.get_task_entity(\n                project_name, task_item.task_id\n            )\n\n        project_entity = self._controller.get_project_entity(project_name)\n        args = (\n            project_name,\n            folder_entity,\n            task_entity,\n            variant\n        )\n        kwargs = {\n            \"instance\": instance,\n            # Backwards compatibility for 'project_entity' argument (24/07/08)\n            \"project_entity\": project_entity,\n            \"product_type\": product_type,\n        }\n        # Backwards compatibility for 'project_entity' argument (24/07/08)\n        # Backwards compatibility for 'product_type' argument (26/01/19)\n        for kwarg in (\n            \"product_type\",\n            \"project_entity\",\n        ):\n            if not is_func_signature_supported(\n                creator.get_product_name, *args, **kwargs\n            ):\n                kwargs.pop(kwarg)\n        return creator.get_product_name(*args, **kwargs)\n\n    def create(\n        self,\n        creator_identifier: str,\n        product_name: str,\n        instance_data: Dict[str, Any],\n        options: Dict[str, Any],\n    ):\n        \"\"\"Trigger creation and refresh of instances in UI.\"\"\"\n\n        success = True\n        try:\n            with self._create_context.bulk_add_instances():\n                self._create_context.create_with_unified_error(\n                    creator_identifier,\n                    product_name,\n                    instance_data,\n                    options,\n                )\n\n        except CreatorsOperationFailed as exc:\n            success = False\n            self._emit_event(\n                \"instances.create.failed\",\n                {\n                    \"title\": \"Creation failed\",\n                    \"failed_info\": exc.failed_info\n                }\n            )\n\n        return success\n\n    def trigger_convertor_items(self, convertor_identifiers: List[str]):\n        \"\"\"Trigger legacy item convertors.\n\n        This functionality requires to save and reset CreateContext. The reset\n        is needed so Creators can collect converted items.\n\n        Args:\n            convertor_identifiers (list[str]): Identifiers of convertor\n                plugins.\n        \"\"\"\n\n        success = True\n        try:\n            self._create_context.run_convertors(convertor_identifiers)\n\n        except ConvertorsOperationFailed as exc:\n            success = False\n            self._emit_event(\n                \"convertors.convert.failed\",\n                {\n                    \"title\": \"Conversion failed\",\n                    \"failed_info\": exc.failed_info\n                }\n            )\n\n        if success:\n            self._controller.emit_card_message(\n                \"Conversion finished\"\n            )\n        else:\n            self._controller.emit_card_message(\n                \"Conversion failed\",\n                CardMessageTypes.error\n            )\n\n    def save_changes(self, show_message: Optional[bool] = True) -&gt; bool:\n        \"\"\"Save changes happened during creation.\n\n        Trigger save of changes using host api. This functionality does not\n        validate anything. It is required to do checks before this method is\n        called to be able to give user actionable response e.g. check of\n        context using 'host_context_has_changed'.\n\n        Args:\n            show_message (bool): Show message that changes were\n                saved successfully.\n\n        Returns:\n            bool: Save of changes was successful.\n        \"\"\"\n\n        if not self._create_context.host_is_valid:\n            # TODO remove\n            # Fake success save when host is not valid for CreateContext\n            #   this is for testing as experimental feature\n            return True\n\n        try:\n            self._create_context.save_changes()\n            if show_message:\n                self._controller.emit_card_message(\"Saved changes..\")\n            return True\n\n        except CreatorsOperationFailed as exc:\n            self._emit_event(\n                \"instances.save.failed\",\n                {\n                    \"title\": \"Instances save failed\",\n                    \"failed_info\": exc.failed_info\n                }\n            )\n\n        return False\n\n    def remove_instances(self, instance_ids: List[str]):\n        \"\"\"Remove instances based on instance ids.\n\n        Args:\n            instance_ids (List[str]): List of instance ids to remove.\n        \"\"\"\n\n        # QUESTION Expect that instances are really removed? In that case reset\n        #    is not required.\n        self._remove_instances_from_context(instance_ids)\n\n    def set_instances_create_attr_values(self, instance_ids, key, value):\n        self._set_instances_create_attr_values(instance_ids, key, value)\n\n    def revert_instances_create_attr_values(self, instance_ids, key):\n        self._set_instances_create_attr_values(\n            instance_ids, key, _DEFAULT_VALUE\n        )\n\n    def get_creator_attribute_definitions(\n        self, instance_ids: List[str]\n    ) -&gt; List[Tuple[AbstractAttrDef, Dict[str, Dict[str, Any]]]]:\n        \"\"\"Collect creator attribute definitions for multuple instances.\n\n        Args:\n            instance_ids (List[str]): List of created instances for\n                which should be attribute definitions returned.\n\n        \"\"\"\n        # NOTE it would be great if attrdefs would have hash method implemented\n        #   so they could be used as keys in dictionary\n        output = []\n        _attr_defs = {}\n        for instance_id in instance_ids:\n            instance = self._get_instance_by_id(instance_id)\n            for attr_def in instance.creator_attribute_defs:\n                found_idx = None\n                for idx, _attr_def in _attr_defs.items():\n                    if attr_def == _attr_def:\n                        found_idx = idx\n                        break\n\n                value = None\n                if attr_def.is_value_def:\n                    value = instance.creator_attributes[attr_def.key]\n\n                if found_idx is None:\n                    idx = len(output)\n                    output.append((\n                        attr_def,\n                        {\n                            instance_id: {\n                                \"value\": value,\n                                \"default\": attr_def.default\n                            }\n                        }\n                    ))\n                    _attr_defs[idx] = attr_def\n                else:\n                    _, info_by_id = output[found_idx]\n                    info_by_id[instance_id] = {\n                        \"value\": value,\n                        \"default\": attr_def.default\n                    }\n\n        return output\n\n    def set_instances_publish_attr_values(\n        self, instance_ids, plugin_name, key, value\n    ):\n        self._set_instances_publish_attr_values(\n            instance_ids, plugin_name, key, value\n        )\n\n    def revert_instances_publish_attr_values(\n        self, instance_ids, plugin_name, key\n    ):\n        self._set_instances_publish_attr_values(\n            instance_ids, plugin_name, key, _DEFAULT_VALUE\n        )\n\n    def trigger_pre_create_button_callback(\n        self, identifier: str, button_name: str\n    ) -&gt; None:\n        self._create_context.trigger_pre_create_button_callback(\n            identifier, button_name\n        )\n\n    def trigger_create_button_callback(\n        self,\n        button_name: str,\n        instance_ids: list[str],\n    ) -&gt; None:\n        self._create_context.trigger_create_button_callback(\n            button_name, instance_ids\n        )\n\n    def trigger_publish_button_callback(\n        self,\n        plugin_name: str,\n        button_name: str,\n        instance_ids: list[str | None],\n    ) -&gt; None:\n        self._create_context.trigger_publish_button_callback(\n            plugin_name, button_name, instance_ids\n        )\n\n    def get_publish_attribute_definitions(\n        self,\n        instance_ids: List[str],\n        include_context: bool\n    ) -&gt; list[PublishAttrDefsInfo]:\n        \"\"\"Collect publish attribute definitions for passed instances.\n\n        Args:\n            instance_ids (List[str]): List of created instances for\n                which should be attribute definitions returned.\n            include_context (bool): Add context specific attribute definitions.\n\n        \"\"\"\n        _tmp_items = []\n        if include_context:\n            _tmp_items.append(self._create_context)\n\n        for instance_id in instance_ids:\n            _tmp_items.append(self._get_instance_by_id(instance_id))\n\n        all_defs_by_plugin_name = {}\n        all_plugin_values = {}\n        instance_ids_by_name = {}\n        for item in _tmp_items:\n            item_id = None\n            if isinstance(item, CreatedInstance):\n                item_id = item.id\n\n            for plugin_name, attr_val in item.publish_attributes.items():\n                if not isinstance(attr_val, AttributeValues):\n                    continue\n                attr_defs = attr_val.attr_defs\n                if not attr_defs:\n                    continue\n\n                plugin_attr_defs = all_defs_by_plugin_name.setdefault(\n                    plugin_name, []\n                )\n                instance_ids = instance_ids_by_name.get(plugin_name)\n                if instance_ids is None:\n                    instance_ids = set()\n                    instance_ids_by_name[plugin_name] = instance_ids\n                instance_ids.add(item_id)\n\n                plugin_values = all_plugin_values.setdefault(plugin_name, {})\n                plugin_attr_defs.append(attr_defs)\n\n                for attr_def in attr_defs:\n                    if isinstance(attr_def, UIDef):\n                        continue\n                    attr_values = plugin_values.setdefault(attr_def.key, [])\n                    attr_values.append(\n                        (item_id, attr_val[attr_def.key], attr_def.default)\n                    )\n\n        attr_defs_by_plugin_name = {}\n        for plugin_name, attr_defs in all_defs_by_plugin_name.items():\n            attr_defs_by_plugin_name[plugin_name] = merge_attr_defs(attr_defs)\n\n        output = []\n        for plugin in self._create_context.plugins_with_defs:\n            plugin_name = plugin.__name__\n            if plugin_name not in all_defs_by_plugin_name:\n                continue\n            instance_ids = instance_ids_by_name[plugin_name]\n            output.append(\n                PublishAttrDefsInfo(\n                    plugin_name,\n                    attr_defs_by_plugin_name[plugin_name],\n                    all_plugin_values[plugin_name],\n                    instance_ids,\n                )\n            )\n        return output\n\n    def get_thumbnail_paths_for_instances(\n        self, instance_ids: List[str]\n    ) -&gt; Dict[str, Union[str, None]]:\n        thumbnail_paths_by_instance_id = (\n            self._create_context.thumbnail_paths_by_instance_id\n        )\n        return {\n            instance_id: thumbnail_paths_by_instance_id.get(instance_id)\n            for instance_id in instance_ids\n        }\n\n    def set_thumbnail_paths_for_instances(\n        self, thumbnail_path_mapping: Dict[str, str]\n    ):\n        thumbnail_paths_by_instance_id = (\n            self._create_context.thumbnail_paths_by_instance_id\n        )\n        for instance_id, thumbnail_path in thumbnail_path_mapping.items():\n            thumbnail_paths_by_instance_id[instance_id] = thumbnail_path\n\n        self._emit_event(\n            \"instance.thumbnail.changed\",\n            {\n                \"mapping\": thumbnail_path_mapping\n            }\n        )\n\n    def _emit_event(\n        self,\n        topic: str,\n        data: Optional[Dict[str, Any]] = None\n    ):\n        self._controller.emit_event(topic, data, CREATE_EVENT_SOURCE)\n\n    def _get_current_project_settings(self) -&gt; Dict[str, Any]:\n        \"\"\"Current project settings.\n\n        Returns:\n            dict\n        \"\"\"\n\n        return self._create_context.get_current_project_settings()\n\n    @property\n    def _creators(self) -&gt; Dict[str, BaseCreator]:\n        \"\"\"All creators loaded in create context.\"\"\"\n\n        return self._create_context.creators\n\n    def _get_instance_by_id(\n        self, instance_id: str\n    ) -&gt; Union[CreatedInstance, None]:\n        return self._create_context.instances_by_id.get(instance_id)\n\n    def _get_instances_by_id(\n        self, instance_ids: Optional[Iterable[str]]\n    ) -&gt; Dict[str, Union[CreatedInstance, None]]:\n        if instance_ids is None:\n            instance_ids = self._create_context.instances_by_id.keys()\n        return {\n            instance_id: self._get_instance_by_id(instance_id)\n            for instance_id in instance_ids\n        }\n\n    def _reset_instances(self):\n        \"\"\"Reset create instances.\"\"\"\n\n        self._create_context.reset_context_data()\n        with self._create_context.bulk_add_instances():\n            try:\n                self._create_context.reset_instances()\n            except CreatorsOperationFailed as exc:\n                self._emit_event(\n                    \"instances.collection.failed\",\n                    {\n                        \"title\": \"Instance collection failed\",\n                        \"failed_info\": exc.failed_info\n                    }\n                )\n\n            try:\n                self._create_context.find_convertor_items()\n            except ConvertorsOperationFailed as exc:\n                self._emit_event(\n                    \"convertors.find.failed\",\n                    {\n                        \"title\": \"Collection of unsupported product failed\",\n                        \"failed_info\": exc.failed_info\n                    }\n                )\n\n            try:\n                self._create_context.execute_autocreators()\n\n            except CreatorsOperationFailed as exc:\n                self._emit_event(\n                    \"instances.create.failed\",\n                    {\n                        \"title\": \"AutoCreation failed\",\n                        \"failed_info\": exc.failed_info\n                    }\n                )\n\n    def _remove_instances_from_context(self, instance_ids: List[str]):\n        instances_by_id = self._create_context.instances_by_id\n        instances = [\n            instances_by_id[instance_id]\n            for instance_id in instance_ids\n        ]\n        try:\n            self._create_context.remove_instances(instances)\n        except CreatorsOperationFailed as exc:\n            self._emit_event(\n                \"instances.remove.failed\",\n                {\n                    \"title\": \"Instance removement failed\",\n                    \"failed_info\": exc.failed_info\n                }\n            )\n\n    def _refresh_creator_items(self, identifiers=None):\n        if identifiers is None or self._creator_items is None:\n            identifiers = set(self._create_context.creators.keys())\n\n        if self._creator_items is None:\n            self._creator_items = {}\n\n        allowed_creator_pattern = self._get_allowed_creators_pattern()\n\n        for identifier in identifiers:\n            creator = self._create_context.creators.get(identifier)\n            if creator is None:\n                continue\n\n            try:\n                creator_item = CreatorItem.from_creator(creator)\n            except Exception:\n                self.log.error(\n                    \"Failed to create creator item for '%s'\",\n                    identifier,\n                    exc_info=True\n                )\n                self._creator_items.pop(identifier, None)\n                continue\n\n            self._creator_items[identifier] = creator_item\n            for ui_item in creator_item.ui_items:\n                ui_item.filtered = not self._is_label_allowed(\n                    ui_item.label, allowed_creator_pattern\n                )\n                if ui_item.filtered:\n                    self.log.debug(f\"{ui_item.label} not allowed for context\")\n\n    def _set_instances_create_attr_values(self, instance_ids, key, value):\n        with self._create_context.bulk_value_changes(CREATE_EVENT_SOURCE):\n            for instance_id in instance_ids:\n                instance = self._get_instance_by_id(instance_id)\n                creator_attributes = instance[\"creator_attributes\"]\n                attr_def = creator_attributes.get_attr_def(key)\n                if (\n                    attr_def is None\n                    or not attr_def.is_value_def\n                    or not attr_def.visible\n                    or not attr_def.enabled\n                ):\n                    continue\n\n                if value is _DEFAULT_VALUE:\n                    creator_attributes[key] = attr_def.default\n\n                elif attr_def.is_value_valid(value):\n                    creator_attributes[key] = copy.deepcopy(value)\n\n    def _set_instances_publish_attr_values(\n        self, instance_ids, plugin_name, key, value\n    ):\n        with self._create_context.bulk_value_changes(CREATE_EVENT_SOURCE):\n            for instance_id in instance_ids:\n                if instance_id is None:\n                    instance = self._create_context\n                else:\n                    instance = self._get_instance_by_id(instance_id)\n                plugin_val = instance.publish_attributes[plugin_name]\n                attr_def = plugin_val.get_attr_def(key)\n                # Ignore if attribute is not available or enabled/visible\n                #   on the instance, or the value is not valid for definition\n                if (\n                    attr_def is None\n                    or not attr_def.is_value_def\n                    or not attr_def.visible\n                    or not attr_def.enabled\n                ):\n                    continue\n\n                if value is _DEFAULT_VALUE:\n                    plugin_val[key] = attr_def.default\n\n                elif attr_def.is_value_valid(value):\n                    plugin_val[key] = value\n\n    def _cc_added_instance(self, event):\n        instance_ids = {\n            instance.id\n            for instance in event.data[\"instances\"]\n        }\n        self._emit_event(\n            \"create.context.added.instance\",\n            {\"instance_ids\": instance_ids},\n        )\n\n    def _cc_removed_instance(self, event):\n        instance_ids = {\n            instance.id\n            for instance in event.data[\"instances\"]\n        }\n        self._emit_event(\n            \"create.context.removed.instance\",\n            {\"instance_ids\": instance_ids},\n        )\n\n    def _cc_value_changed(self, event):\n        if event.source == CREATE_EVENT_SOURCE:\n            return\n\n        instance_changes = {}\n        context_changed_ids = set()\n        for item in event.data[\"changes\"]:\n            instance_id = None\n            if item[\"instance\"]:\n                instance_id = item[\"instance\"].id\n            changes = item[\"changes\"]\n            instance_changes[instance_id] = changes\n            if instance_id is None:\n                continue\n\n            if self._CONTEXT_KEYS.intersection(set(changes)):\n                context_changed_ids.add(instance_id)\n\n        self._emit_event(\n            \"create.context.value.changed\",\n            {\"instance_changes\": instance_changes},\n        )\n        if context_changed_ids:\n            self._emit_event(\n                \"create.model.instances.context.changed\",\n                {\"instance_ids\": list(context_changed_ids)},\n            )\n\n    def _cc_pre_create_attr_changed(self, event):\n        identifiers = event[\"identifiers\"]\n        self._refresh_creator_items(identifiers)\n        self._emit_event(\n            \"create.context.pre.create.attrs.changed\",\n            {\"identifiers\": identifiers},\n        )\n\n    def _cc_create_attr_changed(self, event):\n        instance_ids = {\n            instance.id\n            for instance in event.data[\"instances\"]\n        }\n        self._emit_event(\n            \"create.context.create.attrs.changed\",\n            {\"instance_ids\": instance_ids},\n        )\n\n    def _cc_publish_attr_changed(self, event):\n        instance_changes = event.data[\"instance_changes\"]\n        event_data = {\n            instance_id: instance_data[\"plugin_names\"]\n            for instance_id, instance_data in instance_changes.items()\n        }\n        self._emit_event(\n            \"create.context.publish.attrs.changed\",\n            event_data,\n        )\n\n    def _cc_instance_requirement_changed(self, event):\n        instance_ids = {\n            instance.id\n            for instance in event.data[\"instances\"]\n        }\n        self._emit_event(\n            \"create.model.instance.requirement.changed\",\n            {\"instance_ids\": instance_ids},\n        )\n\n    def _cc_instance_parent_changed(self, event):\n        instance_ids = {\n            instance.id\n            for instance in event.data[\"instances\"]\n        }\n        self._emit_event(\n            \"create.model.instance.parent.changed\",\n            {\"instance_ids\": instance_ids},\n        )\n\n    def _get_allowed_creators_pattern(self) -&gt; Union[Pattern, None]:\n        \"\"\"Provide regex pattern for configured creator labels in this context\n\n        If no profile matches current context, it shows all creators.\n        Support usage of regular expressions for configured values.\n        Returns:\n            (re.Pattern)[optional]: None or regex compiled patterns\n                into single one ('Render|Image.*')\n        \"\"\"\n\n        task_type = self._create_context.get_current_task_type()\n        project_settings = self._get_current_project_settings()\n\n        filter_creator_profiles = (\n            project_settings\n            [\"core\"]\n            [\"tools\"]\n            [\"creator\"]\n            [\"filter_creator_profiles\"]\n        )\n        filtering_criteria = {\n            \"task_names\": self.get_current_task_name(),\n            \"task_types\": task_type,\n            \"host_names\": self._create_context.host_name\n        }\n        profile = filter_profiles(\n            filter_creator_profiles,\n            filtering_criteria,\n            logger=self.log\n        )\n\n        allowed_creator_pattern = None\n        if profile:\n            allowed_creator_labels = {\n                label\n                for label in profile[\"creator_labels\"]\n                if label\n            }\n            self.log.debug(f\"Only allowed `{allowed_creator_labels}` creators\")\n            allowed_creator_pattern = (\n                re.compile(\"|\".join(allowed_creator_labels)))\n        return allowed_creator_pattern\n\n    def _is_label_allowed(\n        self,\n        label: str,\n        allowed_labels_regex: Union[Pattern, None]\n    ) -&gt; bool:\n        \"\"\"Implement regex support for allowed labels.\n\n        Args:\n            label (str): Label of creator - shown in Publisher\n            allowed_labels_regex (re.Pattern): compiled regular expression\n        \"\"\"\n        if not allowed_labels_regex:\n            return True\n        return bool(allowed_labels_regex.match(label))\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/models/create.html#client.ayon_core.tools.publisher.models.create.CreateModel.create","title":"<code>create(creator_identifier, product_name, instance_data, options)</code>","text":"<p>Trigger creation and refresh of instances in UI.</p> Source code in <code>client/ayon_core/tools/publisher/models/create.py</code> <pre><code>def create(\n    self,\n    creator_identifier: str,\n    product_name: str,\n    instance_data: Dict[str, Any],\n    options: Dict[str, Any],\n):\n    \"\"\"Trigger creation and refresh of instances in UI.\"\"\"\n\n    success = True\n    try:\n        with self._create_context.bulk_add_instances():\n            self._create_context.create_with_unified_error(\n                creator_identifier,\n                product_name,\n                instance_data,\n                options,\n            )\n\n    except CreatorsOperationFailed as exc:\n        success = False\n        self._emit_event(\n            \"instances.create.failed\",\n            {\n                \"title\": \"Creation failed\",\n                \"failed_info\": exc.failed_info\n            }\n        )\n\n    return success\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/models/create.html#client.ayon_core.tools.publisher.models.create.CreateModel.get_creator_attribute_definitions","title":"<code>get_creator_attribute_definitions(instance_ids)</code>","text":"<p>Collect creator attribute definitions for multuple instances.</p> <p>Parameters:</p> Name Type Description Default <code>instance_ids</code> <code>List[str]</code> <p>List of created instances for which should be attribute definitions returned.</p> required Source code in <code>client/ayon_core/tools/publisher/models/create.py</code> <pre><code>def get_creator_attribute_definitions(\n    self, instance_ids: List[str]\n) -&gt; List[Tuple[AbstractAttrDef, Dict[str, Dict[str, Any]]]]:\n    \"\"\"Collect creator attribute definitions for multuple instances.\n\n    Args:\n        instance_ids (List[str]): List of created instances for\n            which should be attribute definitions returned.\n\n    \"\"\"\n    # NOTE it would be great if attrdefs would have hash method implemented\n    #   so they could be used as keys in dictionary\n    output = []\n    _attr_defs = {}\n    for instance_id in instance_ids:\n        instance = self._get_instance_by_id(instance_id)\n        for attr_def in instance.creator_attribute_defs:\n            found_idx = None\n            for idx, _attr_def in _attr_defs.items():\n                if attr_def == _attr_def:\n                    found_idx = idx\n                    break\n\n            value = None\n            if attr_def.is_value_def:\n                value = instance.creator_attributes[attr_def.key]\n\n            if found_idx is None:\n                idx = len(output)\n                output.append((\n                    attr_def,\n                    {\n                        instance_id: {\n                            \"value\": value,\n                            \"default\": attr_def.default\n                        }\n                    }\n                ))\n                _attr_defs[idx] = attr_def\n            else:\n                _, info_by_id = output[found_idx]\n                info_by_id[instance_id] = {\n                    \"value\": value,\n                    \"default\": attr_def.default\n                }\n\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/models/create.html#client.ayon_core.tools.publisher.models.create.CreateModel.get_creator_icon","title":"<code>get_creator_icon(identifier)</code>","text":"<p>Function to receive icon for creator identifier.</p> <p>Parameters:</p> Name Type Description Default <code>identifier</code> <code>str</code> <p>Creator's identifier for which should be icon returned.</p> required Source code in <code>client/ayon_core/tools/publisher/models/create.py</code> <pre><code>def get_creator_icon(\n    self, identifier: str\n) -&gt; Union[str, Dict[str, Any], None]:\n    \"\"\"Function to receive icon for creator identifier.\n\n    Args:\n        identifier (str): Creator's identifier for which should\n            be icon returned.\n\n    \"\"\"\n    creator_item = self.get_creator_item_by_id(identifier)\n    if creator_item is not None:\n        return creator_item.icon\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/models/create.html#client.ayon_core.tools.publisher.models.create.CreateModel.get_creator_items","title":"<code>get_creator_items()</code>","text":"<p>Creators that can be shown in create dialog.</p> Source code in <code>client/ayon_core/tools/publisher/models/create.py</code> <pre><code>def get_creator_items(self) -&gt; dict[str, CreatorItem]:\n    \"\"\"Creators that can be shown in create dialog.\"\"\"\n    if self._creator_items is None:\n        self._refresh_creator_items()\n    return self._creator_items\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/models/create.html#client.ayon_core.tools.publisher.models.create.CreateModel.get_current_folder_path","title":"<code>get_current_folder_path()</code>","text":"<p>Current context folder path defined by host.</p> <p>Returns:</p> Type Description <code>Union[str, None]</code> <p>Union[str, None]: Folder path or None if folder is not set.</p> Source code in <code>client/ayon_core/tools/publisher/models/create.py</code> <pre><code>def get_current_folder_path(self) -&gt; Union[str, None]:\n    \"\"\"Current context folder path defined by host.\n\n    Returns:\n        Union[str, None]: Folder path or None if folder is not set.\n    \"\"\"\n\n    return self._create_context.get_current_folder_path()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/models/create.html#client.ayon_core.tools.publisher.models.create.CreateModel.get_current_project_name","title":"<code>get_current_project_name()</code>","text":"<p>Current project context defined by host.</p> <p>Returns:</p> Name Type Description <code>str</code> <code>Union[str, None]</code> <p>Project name.</p> Source code in <code>client/ayon_core/tools/publisher/models/create.py</code> <pre><code>def get_current_project_name(self) -&gt; Union[str, None]:\n    \"\"\"Current project context defined by host.\n\n    Returns:\n        str: Project name.\n\n    \"\"\"\n    return self._create_context.get_current_project_name()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/models/create.html#client.ayon_core.tools.publisher.models.create.CreateModel.get_current_task_name","title":"<code>get_current_task_name()</code>","text":"<p>Current context task name defined by host.</p> <p>Returns:</p> Type Description <code>Union[str, None]</code> <p>Union[str, None]: Task name or None if task is not set.</p> Source code in <code>client/ayon_core/tools/publisher/models/create.py</code> <pre><code>def get_current_task_name(self) -&gt; Union[str, None]:\n    \"\"\"Current context task name defined by host.\n\n    Returns:\n        Union[str, None]: Task name or None if task is not set.\n    \"\"\"\n\n    return self._create_context.get_current_task_name()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/models/create.html#client.ayon_core.tools.publisher.models.create.CreateModel.get_instance_items","title":"<code>get_instance_items()</code>","text":"<p>Current instances in create context.</p> Source code in <code>client/ayon_core/tools/publisher/models/create.py</code> <pre><code>def get_instance_items(self) -&gt; List[InstanceItem]:\n    \"\"\"Current instances in create context.\"\"\"\n    return [\n        InstanceItem.from_instance(instance)\n        for instance in self._create_context.instances_by_id.values()\n    ]\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/models/create.html#client.ayon_core.tools.publisher.models.create.CreateModel.get_product_name","title":"<code>get_product_name(creator_identifier, product_type, variant, folder_path, task_name, instance_id=None)</code>","text":"<p>Get product name based on passed data.</p> <p>Parameters:</p> Name Type Description Default <code>creator_identifier</code> <code>str</code> <p>Identifier of creator which should be responsible for product name creation.</p> required <code>variant</code> <code>str</code> <p>Variant value from user's input.</p> required <code>folder_path</code> <code>str</code> <p>Folder path for which is instance created.</p> required <code>task_name</code> <code>str</code> <p>Name of task for which is instance created.</p> required <code>instance_id</code> <code>Union[str, None]</code> <p>Existing instance id when product name is updated.</p> <code>None</code> Source code in <code>client/ayon_core/tools/publisher/models/create.py</code> <pre><code>def get_product_name(\n    self,\n    creator_identifier: str,\n    product_type: str,\n    variant: str,\n    folder_path: Union[str, None],\n    task_name: Union[str, None],\n    instance_id: Optional[str] = None\n) -&gt; str:\n    \"\"\"Get product name based on passed data.\n\n    Args:\n        creator_identifier (str): Identifier of creator which should be\n            responsible for product name creation.\n        variant (str): Variant value from user's input.\n        folder_path (str): Folder path for which is instance created.\n        task_name (str): Name of task for which is instance created.\n        instance_id (Union[str, None]): Existing instance id when product\n            name is updated.\n    \"\"\"\n\n    creator = self._creators[creator_identifier]\n\n    instance = None\n    if instance_id:\n        instance = self._get_instance_by_id(instance_id)\n\n    project_name = self._controller.get_current_project_name()\n    folder_item = self._controller.get_folder_item_by_path(\n        project_name, folder_path\n    )\n    folder_entity = None\n    task_item = None\n    task_entity = None\n    if folder_item is not None:\n        folder_entity = self._controller.get_folder_entity(\n            project_name, folder_item.entity_id\n        )\n        task_item = self._controller.get_task_item_by_name(\n            project_name,\n            folder_item.entity_id,\n            task_name,\n            CREATE_EVENT_SOURCE\n        )\n\n    if task_item is not None:\n        task_entity = self._controller.get_task_entity(\n            project_name, task_item.task_id\n        )\n\n    project_entity = self._controller.get_project_entity(project_name)\n    args = (\n        project_name,\n        folder_entity,\n        task_entity,\n        variant\n    )\n    kwargs = {\n        \"instance\": instance,\n        # Backwards compatibility for 'project_entity' argument (24/07/08)\n        \"project_entity\": project_entity,\n        \"product_type\": product_type,\n    }\n    # Backwards compatibility for 'project_entity' argument (24/07/08)\n    # Backwards compatibility for 'product_type' argument (26/01/19)\n    for kwarg in (\n        \"product_type\",\n        \"project_entity\",\n    ):\n        if not is_func_signature_supported(\n            creator.get_product_name, *args, **kwargs\n        ):\n            kwargs.pop(kwarg)\n    return creator.get_product_name(*args, **kwargs)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/models/create.html#client.ayon_core.tools.publisher.models.create.CreateModel.get_publish_attribute_definitions","title":"<code>get_publish_attribute_definitions(instance_ids, include_context)</code>","text":"<p>Collect publish attribute definitions for passed instances.</p> <p>Parameters:</p> Name Type Description Default <code>instance_ids</code> <code>List[str]</code> <p>List of created instances for which should be attribute definitions returned.</p> required <code>include_context</code> <code>bool</code> <p>Add context specific attribute definitions.</p> required Source code in <code>client/ayon_core/tools/publisher/models/create.py</code> <pre><code>def get_publish_attribute_definitions(\n    self,\n    instance_ids: List[str],\n    include_context: bool\n) -&gt; list[PublishAttrDefsInfo]:\n    \"\"\"Collect publish attribute definitions for passed instances.\n\n    Args:\n        instance_ids (List[str]): List of created instances for\n            which should be attribute definitions returned.\n        include_context (bool): Add context specific attribute definitions.\n\n    \"\"\"\n    _tmp_items = []\n    if include_context:\n        _tmp_items.append(self._create_context)\n\n    for instance_id in instance_ids:\n        _tmp_items.append(self._get_instance_by_id(instance_id))\n\n    all_defs_by_plugin_name = {}\n    all_plugin_values = {}\n    instance_ids_by_name = {}\n    for item in _tmp_items:\n        item_id = None\n        if isinstance(item, CreatedInstance):\n            item_id = item.id\n\n        for plugin_name, attr_val in item.publish_attributes.items():\n            if not isinstance(attr_val, AttributeValues):\n                continue\n            attr_defs = attr_val.attr_defs\n            if not attr_defs:\n                continue\n\n            plugin_attr_defs = all_defs_by_plugin_name.setdefault(\n                plugin_name, []\n            )\n            instance_ids = instance_ids_by_name.get(plugin_name)\n            if instance_ids is None:\n                instance_ids = set()\n                instance_ids_by_name[plugin_name] = instance_ids\n            instance_ids.add(item_id)\n\n            plugin_values = all_plugin_values.setdefault(plugin_name, {})\n            plugin_attr_defs.append(attr_defs)\n\n            for attr_def in attr_defs:\n                if isinstance(attr_def, UIDef):\n                    continue\n                attr_values = plugin_values.setdefault(attr_def.key, [])\n                attr_values.append(\n                    (item_id, attr_val[attr_def.key], attr_def.default)\n                )\n\n    attr_defs_by_plugin_name = {}\n    for plugin_name, attr_defs in all_defs_by_plugin_name.items():\n        attr_defs_by_plugin_name[plugin_name] = merge_attr_defs(attr_defs)\n\n    output = []\n    for plugin in self._create_context.plugins_with_defs:\n        plugin_name = plugin.__name__\n        if plugin_name not in all_defs_by_plugin_name:\n            continue\n        instance_ids = instance_ids_by_name[plugin_name]\n        output.append(\n            PublishAttrDefsInfo(\n                plugin_name,\n                attr_defs_by_plugin_name[plugin_name],\n                all_plugin_values[plugin_name],\n                instance_ids,\n            )\n        )\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/models/create.html#client.ayon_core.tools.publisher.models.create.CreateModel.remove_instances","title":"<code>remove_instances(instance_ids)</code>","text":"<p>Remove instances based on instance ids.</p> <p>Parameters:</p> Name Type Description Default <code>instance_ids</code> <code>List[str]</code> <p>List of instance ids to remove.</p> required Source code in <code>client/ayon_core/tools/publisher/models/create.py</code> <pre><code>def remove_instances(self, instance_ids: List[str]):\n    \"\"\"Remove instances based on instance ids.\n\n    Args:\n        instance_ids (List[str]): List of instance ids to remove.\n    \"\"\"\n\n    # QUESTION Expect that instances are really removed? In that case reset\n    #    is not required.\n    self._remove_instances_from_context(instance_ids)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/models/create.html#client.ayon_core.tools.publisher.models.create.CreateModel.save_changes","title":"<code>save_changes(show_message=True)</code>","text":"<p>Save changes happened during creation.</p> <p>Trigger save of changes using host api. This functionality does not validate anything. It is required to do checks before this method is called to be able to give user actionable response e.g. check of context using 'host_context_has_changed'.</p> <p>Parameters:</p> Name Type Description Default <code>show_message</code> <code>bool</code> <p>Show message that changes were saved successfully.</p> <code>True</code> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>Save of changes was successful.</p> Source code in <code>client/ayon_core/tools/publisher/models/create.py</code> <pre><code>def save_changes(self, show_message: Optional[bool] = True) -&gt; bool:\n    \"\"\"Save changes happened during creation.\n\n    Trigger save of changes using host api. This functionality does not\n    validate anything. It is required to do checks before this method is\n    called to be able to give user actionable response e.g. check of\n    context using 'host_context_has_changed'.\n\n    Args:\n        show_message (bool): Show message that changes were\n            saved successfully.\n\n    Returns:\n        bool: Save of changes was successful.\n    \"\"\"\n\n    if not self._create_context.host_is_valid:\n        # TODO remove\n        # Fake success save when host is not valid for CreateContext\n        #   this is for testing as experimental feature\n        return True\n\n    try:\n        self._create_context.save_changes()\n        if show_message:\n            self._controller.emit_card_message(\"Saved changes..\")\n        return True\n\n    except CreatorsOperationFailed as exc:\n        self._emit_event(\n            \"instances.save.failed\",\n            {\n                \"title\": \"Instances save failed\",\n                \"failed_info\": exc.failed_info\n            }\n        )\n\n    return False\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/models/create.html#client.ayon_core.tools.publisher.models.create.CreateModel.trigger_convertor_items","title":"<code>trigger_convertor_items(convertor_identifiers)</code>","text":"<p>Trigger legacy item convertors.</p> <p>This functionality requires to save and reset CreateContext. The reset is needed so Creators can collect converted items.</p> <p>Parameters:</p> Name Type Description Default <code>convertor_identifiers</code> <code>list[str]</code> <p>Identifiers of convertor plugins.</p> required Source code in <code>client/ayon_core/tools/publisher/models/create.py</code> <pre><code>def trigger_convertor_items(self, convertor_identifiers: List[str]):\n    \"\"\"Trigger legacy item convertors.\n\n    This functionality requires to save and reset CreateContext. The reset\n    is needed so Creators can collect converted items.\n\n    Args:\n        convertor_identifiers (list[str]): Identifiers of convertor\n            plugins.\n    \"\"\"\n\n    success = True\n    try:\n        self._create_context.run_convertors(convertor_identifiers)\n\n    except ConvertorsOperationFailed as exc:\n        success = False\n        self._emit_event(\n            \"convertors.convert.failed\",\n            {\n                \"title\": \"Conversion failed\",\n                \"failed_info\": exc.failed_info\n            }\n        )\n\n    if success:\n        self._controller.emit_card_message(\n            \"Conversion finished\"\n        )\n    else:\n        self._controller.emit_card_message(\n            \"Conversion failed\",\n            CardMessageTypes.error\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/models/create.html#client.ayon_core.tools.publisher.models.create.CreatorItem","title":"<code>CreatorItem</code>","text":"<p>Wrapper around Creator plugin.</p> <p>Object can be serialized and recreated.</p> Source code in <code>client/ayon_core/tools/publisher/models/create.py</code> <pre><code>class CreatorItem:\n    \"\"\"Wrapper around Creator plugin.\n\n    Object can be serialized and recreated.\n    \"\"\"\n\n    def __init__(\n        self,\n        identifier: str,\n        creator_type: CreatorType,\n        product_base_type: str,\n        label: str,\n        group_label: str,\n        icon: IconBase | dict[str, Any] | str | None,\n        description: Union[str, None],\n        detailed_description: Union[str, None],\n        default_variant: Union[str, None],\n        default_variants: Union[List[str], None],\n        create_allow_context_change: Union[bool, None],\n        create_allow_thumbnail: Union[bool, None],\n        show_order: int,\n        pre_create_attributes_defs: List[AbstractAttrDef],\n        ui_items: list[CreatorUIItem],\n    ):\n        self.identifier: str = identifier\n        self.creator_type: CreatorType = creator_type\n        self.product_base_type: str = product_base_type\n        self.label: str = label\n        self.group_label: str = group_label\n        self.icon: IconBase | dict[str, Any] | str | None = icon\n        self.description: Union[str, None] = description\n        self.detailed_description: Union[bool, None] = detailed_description\n        self.default_variant: Union[bool, None] = default_variant\n        self.default_variants: Union[List[str], None] = default_variants\n        self.create_allow_context_change: Union[bool, None] = (\n            create_allow_context_change\n        )\n        self.create_allow_thumbnail: Union[bool, None] = create_allow_thumbnail\n        self.show_order: int = show_order\n        self.pre_create_attributes_defs: List[AbstractAttrDef] = (\n            pre_create_attributes_defs\n        )\n        self.ui_items: list[CreatorUIItem] = ui_items\n\n    def get_group_label(self) -&gt; str:\n        return self.group_label\n\n    @classmethod\n    def from_creator(cls, creator: BaseCreator) -&gt; \"CreatorItem\":\n        creator_type: CreatorType = CreatorTypes.base\n        if isinstance(creator, AutoCreator):\n            creator_type = CreatorTypes.auto\n        elif isinstance(creator, HiddenCreator):\n            creator_type = CreatorTypes.hidden\n        elif isinstance(creator, Creator):\n            creator_type = CreatorTypes.artist\n\n        description = None\n        detail_description = None\n        default_variant = None\n        default_variants = None\n        pre_create_attr_defs = None\n        create_allow_context_change = None\n        create_allow_thumbnail = None\n        show_order = creator.order\n        if creator_type is CreatorTypes.artist:\n            description = creator.get_description()\n            detail_description = creator.get_detail_description()\n            default_variant = creator.get_default_variant()\n            default_variants = creator.get_default_variants()\n            pre_create_attr_defs = creator.get_pre_create_attr_defs()\n            create_allow_context_change = creator.create_allow_context_change\n            create_allow_thumbnail = creator.create_allow_thumbnail\n            show_order = creator.show_order\n\n        ui_items = []\n        product_type_items: list[ProductTypeItem] = (\n            creator.get_product_type_items() or []\n        )\n        for item in product_type_items:\n            ui_item = CreatorUIItem(\n                item.product_type,\n                item.label or creator.label,\n            )\n            ui_items.append(ui_item)\n\n        if not ui_items:\n            ui_items.append(CreatorUIItem(\n                creator.product_base_type,\n                creator.label,\n            ))\n\n        identifier = creator.identifier\n        return cls(\n            identifier,\n            creator_type,\n            creator.product_base_type,\n            creator.label or identifier,\n            creator.get_group_label(),\n            creator.get_icon(),\n            description,\n            detail_description,\n            default_variant,\n            default_variants,\n            create_allow_context_change,\n            create_allow_thumbnail,\n            show_order,\n            pre_create_attr_defs,\n            ui_items,\n        )\n\n    def to_data(self) -&gt; Dict[str, Any]:\n        pre_create_attributes_defs = None\n        if self.pre_create_attributes_defs is not None:\n            pre_create_attributes_defs = serialize_attr_defs(\n                self.pre_create_attributes_defs\n            )\n        icon = self.icon\n        if isinstance(icon, IconBase):\n            icon = icon.to_data()\n            icon[\"__iconBase__\"] = True\n\n        return {\n            \"identifier\": self.identifier,\n            \"creator_type\": str(self.creator_type),\n            \"product_base_type\": self.product_base_type,\n            \"label\": self.label,\n            \"group_label\": self.group_label,\n            \"icon\": self.icon,\n            \"description\": self.description,\n            \"detailed_description\": self.detailed_description,\n            \"default_variant\": self.default_variant,\n            \"default_variants\": self.default_variants,\n            \"create_allow_context_change\": self.create_allow_context_change,\n            \"create_allow_thumbnail\": self.create_allow_thumbnail,\n            \"show_order\": self.show_order,\n            \"pre_create_attributes_defs\": pre_create_attributes_defs,\n            \"ui_items\": [item.to_data() for item in self.ui_items],\n        }\n\n    @classmethod\n    def from_data(cls, data: Dict[str, Any]) -&gt; \"CreatorItem\":\n        icon = data[\"icon\"]\n        if isinstance(icon, dict) and icon.pop(\"__iconBase__\", False):\n            data[\"icon\"] = get_icon_def_from_data(icon)\n\n        pre_create_attributes_defs = data[\"pre_create_attributes_defs\"]\n        if pre_create_attributes_defs is not None:\n            data[\"pre_create_attributes_defs\"] = deserialize_attr_defs(\n                pre_create_attributes_defs\n            )\n\n        data[\"creator_type\"] = CreatorTypes.from_str(data[\"creator_type\"])\n        data[\"ui_items\"] = [\n            CreatorUIItem.from_data(item)\n            for item in data[\"ui_items\"]\n        ]\n        return cls(**data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/models/publish.html","title":"publish","text":""},{"location":"autoapi/client/ayon_core/tools/publisher/models/publish.html#client.ayon_core.tools.publisher.models.publish.PublishErrorItem","title":"<code>PublishErrorItem</code>","text":"<p>Data driven publish error item.</p> <p>Prepared data container with information about publish error and it's source plugin.</p> <p>Can be converted to raw data and recreated should be used for controller and UI connection.</p> <p>Parameters:</p> Name Type Description Default <code>instance_id</code> <code>str | None</code> <p>Pyblish instance id to which is publish error connected.</p> required <code>instance_label</code> <code>str | None</code> <p>Prepared instance label.</p> required <code>plugin_id</code> <code>str</code> <p>Pyblish plugin id which triggered the publish error. Id is generated using 'PublishPluginsProxy'.</p> required <code>is_context_plugin</code> <code>bool</code> <p>Error happened on context.</p> required <code>title</code> <code>str</code> <p>Error title.</p> required <code>description</code> <code>str</code> <p>Error description.</p> required <code>detail</code> <code>str</code> <p>Error detail.</p> required Source code in <code>client/ayon_core/tools/publisher/models/publish.py</code> <pre><code>class PublishErrorItem:\n    \"\"\"Data driven publish error item.\n\n    Prepared data container with information about publish error and it's\n    source plugin.\n\n    Can be converted to raw data and recreated should be used for controller\n    and UI connection.\n\n    Args:\n        instance_id (str | None): Pyblish instance id to which is\n            publish error connected.\n        instance_label (str | None): Prepared instance label.\n        plugin_id (str): Pyblish plugin id which triggered the publish\n            error. Id is generated using 'PublishPluginsProxy'.\n        is_context_plugin (bool): Error happened on context.\n        title (str): Error title.\n        description (str): Error description.\n        detail (str): Error detail.\n\n    \"\"\"\n    def __init__(\n        self,\n        instance_id: str | None,\n        instance_label: str | None,\n        plugin_id: str,\n        is_context_plugin: bool,\n        is_validation_error: bool,\n        title: str,\n        description: str,\n        detail: str\n    ):\n        self.instance_id: str | None = instance_id\n        self.instance_label: str | None = instance_label\n        self.plugin_id: str = plugin_id\n        self.is_context_plugin: bool = is_context_plugin\n        self.is_validation_error: bool = is_validation_error\n        self.title: str = title\n        self.description: str = description\n        self.detail: str = detail\n\n    def to_data(self) -&gt; dict[str, str | bool | None]:\n        \"\"\"Serialize object to dictionary.\n\n        Returns:\n            dict[str, str | bool | None]: Serialized object data.\n\n        \"\"\"\n        return {\n            \"instance_id\": self.instance_id,\n            \"instance_label\": self.instance_label,\n            \"plugin_id\": self.plugin_id,\n            \"is_context_plugin\": self.is_context_plugin,\n            \"is_validation_error\": self.is_validation_error,\n            \"title\": self.title,\n            \"description\": self.description,\n            \"detail\": self.detail,\n        }\n\n    @classmethod\n    def from_result(\n        cls,\n        plugin_id: str,\n        error: PublishError,\n        instance: pyblish.api.Instance | None\n    ):\n        \"\"\"Create new object based on resukt from controller.\n\n        Returns:\n            PublishErrorItem: New object with filled data.\n        \"\"\"\n\n        instance_label = None\n        instance_id = None\n        if instance is not None:\n            instance_label = (\n                instance.data.get(\"label\") or instance.data.get(\"name\")\n            )\n            instance_id = instance.id\n\n        return cls(\n            instance_id,\n            instance_label,\n            plugin_id,\n            instance is None,\n            isinstance(error, PublishValidationError),\n            error.title,\n            error.description,\n            error.detail,\n        )\n\n    @classmethod\n    def from_data(cls, data):\n        return cls(**data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/models/publish.html#client.ayon_core.tools.publisher.models.publish.PublishErrorItem.from_result","title":"<code>from_result(plugin_id, error, instance)</code>  <code>classmethod</code>","text":"<p>Create new object based on resukt from controller.</p> <p>Returns:</p> Name Type Description <code>PublishErrorItem</code> <p>New object with filled data.</p> Source code in <code>client/ayon_core/tools/publisher/models/publish.py</code> <pre><code>@classmethod\ndef from_result(\n    cls,\n    plugin_id: str,\n    error: PublishError,\n    instance: pyblish.api.Instance | None\n):\n    \"\"\"Create new object based on resukt from controller.\n\n    Returns:\n        PublishErrorItem: New object with filled data.\n    \"\"\"\n\n    instance_label = None\n    instance_id = None\n    if instance is not None:\n        instance_label = (\n            instance.data.get(\"label\") or instance.data.get(\"name\")\n        )\n        instance_id = instance.id\n\n    return cls(\n        instance_id,\n        instance_label,\n        plugin_id,\n        instance is None,\n        isinstance(error, PublishValidationError),\n        error.title,\n        error.description,\n        error.detail,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/models/publish.html#client.ayon_core.tools.publisher.models.publish.PublishErrorItem.to_data","title":"<code>to_data()</code>","text":"<p>Serialize object to dictionary.</p> <p>Returns:</p> Type Description <code>dict[str, str | bool | None]</code> <p>dict[str, str | bool | None]: Serialized object data.</p> Source code in <code>client/ayon_core/tools/publisher/models/publish.py</code> <pre><code>def to_data(self) -&gt; dict[str, str | bool | None]:\n    \"\"\"Serialize object to dictionary.\n\n    Returns:\n        dict[str, str | bool | None]: Serialized object data.\n\n    \"\"\"\n    return {\n        \"instance_id\": self.instance_id,\n        \"instance_label\": self.instance_label,\n        \"plugin_id\": self.plugin_id,\n        \"is_context_plugin\": self.is_context_plugin,\n        \"is_validation_error\": self.is_validation_error,\n        \"title\": self.title,\n        \"description\": self.description,\n        \"detail\": self.detail,\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/models/publish.html#client.ayon_core.tools.publisher.models.publish.PublishErrors","title":"<code>PublishErrors</code>","text":"<p>Object to keep track about publish errors by plugin.</p> Source code in <code>client/ayon_core/tools/publisher/models/publish.py</code> <pre><code>class PublishErrors:\n    \"\"\"Object to keep track about publish errors by plugin.\"\"\"\n\n    def __init__(self):\n        self._plugins_proxy: PublishPluginsProxy | None = None\n        self._error_items: list[PublishErrorItem] = []\n        self._plugin_action_items: dict[\n            str, list[PublishPluginActionItem]\n        ] = {}\n\n    def __bool__(self):\n        return self.has_errors\n\n    @property\n    def has_errors(self) -&gt; bool:\n        \"\"\"At least one error was added.\"\"\"\n\n        return bool(self._error_items)\n\n    def reset(self, plugins_proxy: PublishPluginsProxy):\n        \"\"\"Reset object to default state.\n\n        Args:\n            plugins_proxy (PublishPluginsProxy): Proxy which store plugins,\n                actions by ids and create mapping of action ids by plugin ids.\n        \"\"\"\n\n        self._plugins_proxy = plugins_proxy\n        self._error_items = []\n        self._plugin_action_items = {}\n\n    def create_report(self) -&gt; PublishErrorsReport:\n        \"\"\"Create report based on currently existing errors.\n\n        Returns:\n            PublishErrorsReport: Publish error report with all\n                error information and publish plugin action items.\n        \"\"\"\n\n        return PublishErrorsReport(\n            self._error_items, self._plugin_action_items\n        )\n\n    def add_error(\n        self,\n        plugin: pyblish.api.Plugin,\n        error: PublishError,\n        instance: pyblish.api.Instance | None\n    ):\n        \"\"\"Add error from pyblish result.\n\n        Args:\n            plugin (pyblish.api.Plugin): Plugin which triggered error.\n            error (PublishError): Publish error.\n            instance (pyblish.api.Instance | None): Instance on which was\n                error raised or None if was raised on context.\n        \"\"\"\n\n        # Make sure the cached report is cleared\n        plugin_id = self._plugins_proxy.get_plugin_id(plugin)\n        if not error.title:\n            if hasattr(plugin, \"label\") and plugin.label:\n                plugin_label = plugin.label\n            else:\n                plugin_label = plugin.__name__\n            error.title = plugin_label\n\n        self._error_items.append(\n            PublishErrorItem.from_result(plugin_id, error, instance)\n        )\n        if plugin_id in self._plugin_action_items:\n            return\n\n        plugin_actions = self._plugins_proxy.get_plugin_action_items(\n            plugin_id\n        )\n        self._plugin_action_items[plugin_id] = plugin_actions\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/models/publish.html#client.ayon_core.tools.publisher.models.publish.PublishErrors.has_errors","title":"<code>has_errors</code>  <code>property</code>","text":"<p>At least one error was added.</p>"},{"location":"autoapi/client/ayon_core/tools/publisher/models/publish.html#client.ayon_core.tools.publisher.models.publish.PublishErrors.add_error","title":"<code>add_error(plugin, error, instance)</code>","text":"<p>Add error from pyblish result.</p> <p>Parameters:</p> Name Type Description Default <code>plugin</code> <code>Plugin</code> <p>Plugin which triggered error.</p> required <code>error</code> <code>PublishError</code> <p>Publish error.</p> required <code>instance</code> <code>Instance | None</code> <p>Instance on which was error raised or None if was raised on context.</p> required Source code in <code>client/ayon_core/tools/publisher/models/publish.py</code> <pre><code>def add_error(\n    self,\n    plugin: pyblish.api.Plugin,\n    error: PublishError,\n    instance: pyblish.api.Instance | None\n):\n    \"\"\"Add error from pyblish result.\n\n    Args:\n        plugin (pyblish.api.Plugin): Plugin which triggered error.\n        error (PublishError): Publish error.\n        instance (pyblish.api.Instance | None): Instance on which was\n            error raised or None if was raised on context.\n    \"\"\"\n\n    # Make sure the cached report is cleared\n    plugin_id = self._plugins_proxy.get_plugin_id(plugin)\n    if not error.title:\n        if hasattr(plugin, \"label\") and plugin.label:\n            plugin_label = plugin.label\n        else:\n            plugin_label = plugin.__name__\n        error.title = plugin_label\n\n    self._error_items.append(\n        PublishErrorItem.from_result(plugin_id, error, instance)\n    )\n    if plugin_id in self._plugin_action_items:\n        return\n\n    plugin_actions = self._plugins_proxy.get_plugin_action_items(\n        plugin_id\n    )\n    self._plugin_action_items[plugin_id] = plugin_actions\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/models/publish.html#client.ayon_core.tools.publisher.models.publish.PublishErrors.create_report","title":"<code>create_report()</code>","text":"<p>Create report based on currently existing errors.</p> <p>Returns:</p> Name Type Description <code>PublishErrorsReport</code> <code>PublishErrorsReport</code> <p>Publish error report with all error information and publish plugin action items.</p> Source code in <code>client/ayon_core/tools/publisher/models/publish.py</code> <pre><code>def create_report(self) -&gt; PublishErrorsReport:\n    \"\"\"Create report based on currently existing errors.\n\n    Returns:\n        PublishErrorsReport: Publish error report with all\n            error information and publish plugin action items.\n    \"\"\"\n\n    return PublishErrorsReport(\n        self._error_items, self._plugin_action_items\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/models/publish.html#client.ayon_core.tools.publisher.models.publish.PublishErrors.reset","title":"<code>reset(plugins_proxy)</code>","text":"<p>Reset object to default state.</p> <p>Parameters:</p> Name Type Description Default <code>plugins_proxy</code> <code>PublishPluginsProxy</code> <p>Proxy which store plugins, actions by ids and create mapping of action ids by plugin ids.</p> required Source code in <code>client/ayon_core/tools/publisher/models/publish.py</code> <pre><code>def reset(self, plugins_proxy: PublishPluginsProxy):\n    \"\"\"Reset object to default state.\n\n    Args:\n        plugins_proxy (PublishPluginsProxy): Proxy which store plugins,\n            actions by ids and create mapping of action ids by plugin ids.\n    \"\"\"\n\n    self._plugins_proxy = plugins_proxy\n    self._error_items = []\n    self._plugin_action_items = {}\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/models/publish.html#client.ayon_core.tools.publisher.models.publish.PublishErrorsReport","title":"<code>PublishErrorsReport</code>","text":"<p>Publish errors report that can be parsed to raw data.</p> <p>Parameters:</p> Name Type Description Default <code>error_items</code> <code>list[PublishErrorItem]</code> <p>List of publish errors.</p> required <code>plugin_action_items</code> <code>dict[str, list[PublishPluginActionItem]]</code> <p>Action items by plugin id.</p> required Source code in <code>client/ayon_core/tools/publisher/models/publish.py</code> <pre><code>class PublishErrorsReport:\n    \"\"\"Publish errors report that can be parsed to raw data.\n\n    Args:\n        error_items (list[PublishErrorItem]): List of publish errors.\n        plugin_action_items (dict[str, list[PublishPluginActionItem]]): Action\n            items by plugin id.\n\n    \"\"\"\n    def __init__(self, error_items, plugin_action_items):\n        self._error_items = error_items\n        self._plugin_action_items = plugin_action_items\n\n    def __iter__(self) -&gt; Iterable[PublishErrorItem]:\n        for item in self._error_items:\n            yield item\n\n    def group_items_by_title(self) -&gt; list[dict[str, Any]]:\n        \"\"\"Group errors by plugin and their titles.\n\n        Items are grouped by plugin and title -&gt; same title from different\n        plugin is different item. Items are ordered by plugin order.\n\n        Returns:\n            list[dict[str, Any]]: List where each item title, instance\n                information related to title and possible plugin actions.\n        \"\"\"\n\n        ordered_plugin_ids = []\n        error_items_by_plugin_id = collections.defaultdict(list)\n        for error_item in self._error_items:\n            plugin_id = error_item.plugin_id\n            if plugin_id not in ordered_plugin_ids:\n                ordered_plugin_ids.append(plugin_id)\n            error_items_by_plugin_id[plugin_id].append(error_item)\n\n        grouped_error_items = []\n        for plugin_id in ordered_plugin_ids:\n            plugin_action_items = self._plugin_action_items[plugin_id]\n            error_items = error_items_by_plugin_id[plugin_id]\n\n            titles = []\n            error_items_by_title = collections.defaultdict(list)\n            for error_item in error_items:\n                title = error_item.title\n                if title not in titles:\n                    titles.append(error_item.title)\n                error_items_by_title[title].append(error_item)\n\n            for title in titles:\n                grouped_error_items.append({\n                    \"id\": uuid.uuid4().hex,\n                    \"plugin_id\": plugin_id,\n                    \"plugin_action_items\": list(plugin_action_items),\n                    \"error_items\": error_items_by_title[title],\n                    \"title\": title\n                })\n        return grouped_error_items\n\n    def to_data(self):\n        \"\"\"Serialize object to dictionary.\n\n        Returns:\n            dict[str, Any]: Serialized data.\n        \"\"\"\n\n        error_items = [\n            item.to_data()\n            for item in self._error_items\n        ]\n\n        plugin_action_items = {\n            plugin_id: [\n                action_item.to_data()\n                for action_item in action_items\n            ]\n            for plugin_id, action_items in self._plugin_action_items.items()\n        }\n\n        return {\n            \"error_items\": error_items,\n            \"plugin_action_items\": plugin_action_items\n        }\n\n    @classmethod\n    def from_data(\n        cls, data: dict[str, Any]\n    ) -&gt; \"PublishErrorsReport\":\n        \"\"\"Recreate object from data.\n\n        Args:\n            data (dict[str, Any]): Data to recreate object. Can be created\n                using 'to_data' method.\n\n        Returns:\n            PublishErrorsReport: New object based on data.\n        \"\"\"\n\n        error_items = [\n            PublishErrorItem.from_data(error_item)\n            for error_item in data[\"error_items\"]\n        ]\n        plugin_action_items = {}\n        for action_item in data[\"plugin_action_items\"]:\n            item = PublishPluginActionItem.from_data(action_item)\n            action_items = plugin_action_items.setdefault(item.plugin_id, [])\n            action_items.append(item)\n\n        return cls(error_items, plugin_action_items)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/models/publish.html#client.ayon_core.tools.publisher.models.publish.PublishErrorsReport.from_data","title":"<code>from_data(data)</code>  <code>classmethod</code>","text":"<p>Recreate object from data.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict[str, Any]</code> <p>Data to recreate object. Can be created using 'to_data' method.</p> required <p>Returns:</p> Name Type Description <code>PublishErrorsReport</code> <code>'PublishErrorsReport'</code> <p>New object based on data.</p> Source code in <code>client/ayon_core/tools/publisher/models/publish.py</code> <pre><code>@classmethod\ndef from_data(\n    cls, data: dict[str, Any]\n) -&gt; \"PublishErrorsReport\":\n    \"\"\"Recreate object from data.\n\n    Args:\n        data (dict[str, Any]): Data to recreate object. Can be created\n            using 'to_data' method.\n\n    Returns:\n        PublishErrorsReport: New object based on data.\n    \"\"\"\n\n    error_items = [\n        PublishErrorItem.from_data(error_item)\n        for error_item in data[\"error_items\"]\n    ]\n    plugin_action_items = {}\n    for action_item in data[\"plugin_action_items\"]:\n        item = PublishPluginActionItem.from_data(action_item)\n        action_items = plugin_action_items.setdefault(item.plugin_id, [])\n        action_items.append(item)\n\n    return cls(error_items, plugin_action_items)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/models/publish.html#client.ayon_core.tools.publisher.models.publish.PublishErrorsReport.group_items_by_title","title":"<code>group_items_by_title()</code>","text":"<p>Group errors by plugin and their titles.</p> <p>Items are grouped by plugin and title -&gt; same title from different plugin is different item. Items are ordered by plugin order.</p> <p>Returns:</p> Type Description <code>list[dict[str, Any]]</code> <p>list[dict[str, Any]]: List where each item title, instance information related to title and possible plugin actions.</p> Source code in <code>client/ayon_core/tools/publisher/models/publish.py</code> <pre><code>def group_items_by_title(self) -&gt; list[dict[str, Any]]:\n    \"\"\"Group errors by plugin and their titles.\n\n    Items are grouped by plugin and title -&gt; same title from different\n    plugin is different item. Items are ordered by plugin order.\n\n    Returns:\n        list[dict[str, Any]]: List where each item title, instance\n            information related to title and possible plugin actions.\n    \"\"\"\n\n    ordered_plugin_ids = []\n    error_items_by_plugin_id = collections.defaultdict(list)\n    for error_item in self._error_items:\n        plugin_id = error_item.plugin_id\n        if plugin_id not in ordered_plugin_ids:\n            ordered_plugin_ids.append(plugin_id)\n        error_items_by_plugin_id[plugin_id].append(error_item)\n\n    grouped_error_items = []\n    for plugin_id in ordered_plugin_ids:\n        plugin_action_items = self._plugin_action_items[plugin_id]\n        error_items = error_items_by_plugin_id[plugin_id]\n\n        titles = []\n        error_items_by_title = collections.defaultdict(list)\n        for error_item in error_items:\n            title = error_item.title\n            if title not in titles:\n                titles.append(error_item.title)\n            error_items_by_title[title].append(error_item)\n\n        for title in titles:\n            grouped_error_items.append({\n                \"id\": uuid.uuid4().hex,\n                \"plugin_id\": plugin_id,\n                \"plugin_action_items\": list(plugin_action_items),\n                \"error_items\": error_items_by_title[title],\n                \"title\": title\n            })\n    return grouped_error_items\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/models/publish.html#client.ayon_core.tools.publisher.models.publish.PublishErrorsReport.to_data","title":"<code>to_data()</code>","text":"<p>Serialize object to dictionary.</p> <p>Returns:</p> Type Description <p>dict[str, Any]: Serialized data.</p> Source code in <code>client/ayon_core/tools/publisher/models/publish.py</code> <pre><code>def to_data(self):\n    \"\"\"Serialize object to dictionary.\n\n    Returns:\n        dict[str, Any]: Serialized data.\n    \"\"\"\n\n    error_items = [\n        item.to_data()\n        for item in self._error_items\n    ]\n\n    plugin_action_items = {\n        plugin_id: [\n            action_item.to_data()\n            for action_item in action_items\n        ]\n        for plugin_id, action_items in self._plugin_action_items.items()\n    }\n\n    return {\n        \"error_items\": error_items,\n        \"plugin_action_items\": plugin_action_items\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/models/publish.html#client.ayon_core.tools.publisher.models.publish.PublishModel","title":"<code>PublishModel</code>","text":"Source code in <code>client/ayon_core/tools/publisher/models/publish.py</code> <pre><code>class PublishModel:\n    def __init__(self, controller: AbstractPublisherBackend):\n        self._controller = controller\n\n        self._log_to_console: bool = env_value_to_bool(\n            \"AYON_PUBLISHER_PRINT_LOGS\", default=False\n        )\n\n        # Publishing should stop at validation stage\n        self._publish_up_validation: bool = False\n        self._publish_comment_is_set: bool = False\n\n        # Any other exception that happened during publishing\n        self._publish_error_info: PublishErrorInfo | None = None\n        # Publishing is in progress\n        self._publish_is_running: bool = False\n        # Publishing is over validation order\n        self._publish_has_validated: bool = False\n\n        self._publish_has_validation_errors: bool = False\n        self._publish_has_crashed: bool = False\n        # All publish plugins are processed\n        self._publish_has_started: bool = False\n        self._publish_has_finished: bool = False\n        self._publish_max_progress: int = 0\n        self._publish_progress: int = 0\n\n        self._publish_plugins: list[pyblish.api.Plugin] = []\n        self._publish_plugins_proxy: PublishPluginsProxy = (\n            PublishPluginsProxy([])\n        )\n\n        # pyblish.api.Context\n        self._publish_context = None\n        # Pyblish report\n        self._publish_report: PublishReportMaker = PublishReportMaker()\n        # Store exceptions of publish error\n        self._publish_errors: PublishErrors = PublishErrors()\n\n        # This information is not much important for controller but for widget\n        #   which can change (and set) the comment.\n        self._publish_comment_is_set: bool = False\n\n        # Validation order\n        # - plugin with order same or higher than this value is extractor or\n        #   higher\n        self._validation_order: int = (\n            pyblish.api.ValidatorOrder + PLUGIN_ORDER_OFFSET\n        )\n\n        # Plugin iterator\n        self._main_thread_iter: Generator[partial] = self._default_iterator()\n\n        self._log_handler: MessageHandler = MessageHandler()\n\n    def reset(self):\n        # Allow to change behavior during process lifetime\n        self._log_to_console = env_value_to_bool(\n            \"AYON_PUBLISHER_PRINT_LOGS\", default=False\n        )\n\n        create_context = self._controller.get_create_context()\n\n        self._publish_up_validation = False\n        self._publish_comment_is_set = False\n        self._publish_has_started = False\n\n        self._set_publish_error_info(None)\n        self._set_progress(0)\n        self._set_is_running(False)\n        self._set_has_validated(False)\n        self._set_is_crashed(False)\n        self._set_has_validation_errors(False)\n        self._set_finished(False)\n\n        self._main_thread_iter = self._publish_iterator()\n        self._publish_context = pyblish.api.Context()\n        # Make sure \"comment\" is set on publish context\n        self._publish_context.data[\"comment\"] = \"\"\n        # Add access to create context during publishing\n        # - must not be used for changing CreatedInstances during publishing!\n        # QUESTION\n        # - pop the key after first collector using it would be safest option?\n        self._publish_context.data[\"create_context\"] = create_context\n        publish_plugins = create_context.publish_plugins\n        self._publish_plugins = publish_plugins\n        self._publish_plugins_proxy = PublishPluginsProxy(\n            publish_plugins\n        )\n        blocking_crashed_paths = filter_crashed_publish_paths(\n            create_context.get_current_project_name(),\n            set(create_context.publish_discover_result.crashed_file_paths),\n            project_settings=create_context.get_current_project_settings(),\n        )\n        self._publish_report.reset_with_discover_results(\n            create_context.creator_discover_result,\n            create_context.convertor_discover_result,\n            create_context.publish_discover_result,\n            blocking_crashed_paths,\n        )\n        for plugin in create_context.publish_plugins_mismatch_targets:\n            self._publish_report.set_plugin_skipped(plugin.id)\n        self._publish_errors.reset(self._publish_plugins_proxy)\n\n        self._set_max_progress(len(publish_plugins))\n\n        self._emit_event(\"publish.reset.finished\")\n\n    def set_publish_up_validation(self, value: bool):\n        self._publish_up_validation = value\n\n    def start_publish(self, wait: bool = True):\n        \"\"\"Run publishing.\n\n        Make sure all changes are saved before method is called (Call\n        'save_changes' and check output).\n        \"\"\"\n        if self._publish_up_validation and self._publish_has_validated:\n            return\n\n        self._start_publish()\n\n        if not wait:\n            return\n\n        while self.is_running():\n            func = self.get_next_process_func()\n            func()\n\n    def get_next_process_func(self) -&gt; partial:\n        # Raise error if this function is called when publishing\n        #   is not running\n        if not self._publish_is_running:\n            raise ValueError(\"Publish is not running\")\n\n        # Validations of progress before using iterator\n        # Any unexpected error happened\n        # - everything should stop\n        if self._publish_has_crashed:\n            return partial(self.stop_publish)\n\n        # Stop if validation is over and validation errors happened\n        #   or publishing should stop at validation\n        if (\n            self._publish_has_validated\n            and (\n                self._publish_has_validation_errors\n                or self._publish_up_validation\n            )\n        ):\n            return partial(self.stop_publish)\n\n        # Everything is ok so try to get new processing item\n        return next(self._main_thread_iter)\n\n    def stop_publish(self):\n        if self._publish_is_running:\n            self._stop_publish()\n\n    def is_running(self) -&gt; bool:\n        return self._publish_is_running\n\n    def is_crashed(self) -&gt; bool:\n        return self._publish_has_crashed\n\n    def has_started(self) -&gt; bool:\n        return self._publish_has_started\n\n    def has_finished(self) -&gt; bool:\n        return self._publish_has_finished\n\n    def has_validated(self) -&gt; bool:\n        return self._publish_has_validated\n\n    def has_validation_errors(self) -&gt; bool:\n        return self._publish_has_validation_errors\n\n    def publish_can_continue(self) -&gt; bool:\n        return (\n            not self._publish_has_crashed\n            and not self._publish_has_validation_errors\n            and not self._publish_has_finished\n        )\n\n    def get_progress(self) -&gt; int:\n        return self._publish_progress\n\n    def get_max_progress(self) -&gt; int:\n        return self._publish_max_progress\n\n    def get_publish_report(self) -&gt; PublishReport:\n        self._publish_report.update_publish_instances(self._publish_context)\n        return self._publish_report.get_report()\n\n    def get_publish_report_data(self) -&gt; dict[str, Any]:\n        report: PublishReport = self.get_publish_report()\n        return report.to_data(self._publish_report.current_plugin_id)\n\n    def get_publish_errors_report(self) -&gt; PublishErrorsReport:\n        return self._publish_errors.create_report()\n\n    def get_error_info(self) -&gt; PublishErrorInfo | None:\n        return self._publish_error_info\n\n    def store_publish_report(self, filepath: str) -&gt; None:\n        report: PublishReport = self.get_publish_report()\n        report.store_to_file(filepath)\n\n    def set_comment(self, comment: str):\n        # Ignore change of comment when publishing started\n        if self._publish_has_started:\n            return\n        self._publish_context.data[\"comment\"] = comment\n        self._publish_comment_is_set = True\n\n    def run_action(self, plugin_id: str, action_id: str):\n        # TODO handle result in UI\n        plugin = self._publish_plugins_proxy.get_plugin(plugin_id)\n        action = self._publish_plugins_proxy.get_action(plugin_id, action_id)\n\n        result = pyblish.plugin.process(\n            plugin, self._publish_context, None, action.id\n        )\n        exception = result.get(\"error\")\n        if exception:\n            self._emit_event(\n                \"publish.action.failed\",\n                {\n                    \"title\": \"Action failed\",\n                    \"message\": \"Action failed.\",\n                    \"traceback\": \"\".join(\n                        traceback.format_exception(\n                            type(exception),\n                            exception,\n                            exception.__traceback__\n                        )\n                    ),\n                    \"label\": action.__name__,\n                    \"identifier\": action.id\n                }\n            )\n\n        self._publish_report.add_action_result(action, result)\n\n        self._controller.emit_card_message(\"Action finished.\")\n\n    def _emit_event(self, topic: str, data: dict[str, Any] | None = None):\n        self._controller.emit_event(topic, data, PUBLISH_EVENT_SOURCE)\n\n    def _set_finished(self, value: bool):\n        if self._publish_has_finished != value:\n            self._publish_has_finished = value\n            self._emit_event(\n                \"publish.finished.changed\",\n                {\"value\": value}\n            )\n\n    def _set_is_running(self, value: bool):\n        if self._publish_is_running != value:\n            self._publish_is_running = value\n            self._emit_event(\n                \"publish.is_running.changed\",\n                {\"value\": value}\n            )\n\n    def _set_has_validated(self, value: bool):\n        if self._publish_has_validated != value:\n            self._publish_has_validated = value\n            self._emit_event(\n                \"publish.has_validated.changed\",\n                {\"value\": value}\n            )\n\n    def _set_is_crashed(self, value: bool):\n        if self._publish_has_crashed != value:\n            self._publish_has_crashed = value\n            self._emit_event(\n                \"publish.has_crashed.changed\",\n                {\"value\": value}\n            )\n\n    def _set_has_validation_errors(self, value: bool):\n        if self._publish_has_validation_errors != value:\n            self._publish_has_validation_errors = value\n            self._emit_event(\n                \"publish.has_validation_errors.changed\",\n                {\"value\": value}\n            )\n\n    def _set_max_progress(self, value: int):\n        if self._publish_max_progress != value:\n            self._publish_max_progress = value\n            self._emit_event(\n                \"publish.max_progress.changed\",\n                {\"value\": value}\n            )\n\n    def _set_progress(self, value: int):\n        if self._publish_progress != value:\n            self._publish_progress = value\n            self._emit_event(\n                \"publish.progress.changed\",\n                {\"value\": value}\n            )\n\n    def _set_publish_error_info(self, value: PublishErrorInfo | None):\n        if self._publish_error_info != value:\n            self._publish_error_info = value\n            self._emit_event(\n                \"publish.publish_error.changed\",\n                {\"value\": value}\n            )\n\n    def _default_iterator(self):\n        \"\"\"Iterator used on initialization.\n\n        Should be replaced by real iterator when 'reset' is called.\n\n        Returns:\n            collections.abc.Generator[partial]: Generator with partial\n                functions that should be called in main thread.\n\n        \"\"\"\n        while True:\n            yield partial(self.stop_publish)\n\n    def _start_publish(self):\n        \"\"\"Start or continue in publishing.\"\"\"\n        if self._publish_is_running:\n            return\n\n        self._set_is_running(True)\n        self._publish_has_started = True\n\n        self._emit_event(\"publish.process.started\")\n\n    def _stop_publish(self):\n        \"\"\"Stop or pause publishing.\"\"\"\n        self._set_is_running(False)\n\n        self._emit_event(\"publish.process.stopped\")\n\n    def _publish_iterator(self) -&gt; Iterable[partial]:\n        \"\"\"Main logic center of publishing.\n\n        Iterator returns `partial` objects with callbacks that should be\n        processed in main thread (threaded in future?). Cares about changing\n        states of currently processed publish plugin and instance. Also\n        change state of processed orders like validation order has passed etc.\n\n        Also stops publishing, if should stop on validation.\n        \"\"\"\n\n        for idx, plugin in enumerate(self._publish_plugins):\n            self._publish_progress = idx\n\n            # Check if plugin is over validation order\n            if (\n                not self._publish_has_validated\n                and plugin.order &gt;= self._validation_order\n            ):\n                self._set_has_validated(True)\n                if (\n                    self._publish_up_validation\n                    or self._publish_has_validation_errors\n                ):\n                    yield partial(self.stop_publish)\n\n            # Add plugin to publish report\n            self._publish_report.add_plugin_iter(\n                plugin.id, self._publish_context)\n\n            # WARNING This is hack fix for optional plugins\n            if not self._is_publish_plugin_active(plugin):\n                self._publish_report.set_plugin_skipped(plugin.id)\n                continue\n\n            # Trigger callback that new plugin is going to be processed\n            plugin_label = plugin.__name__\n            if hasattr(plugin, \"label\") and plugin.label:\n                plugin_label = plugin.label\n            self._emit_event(\n                \"publish.process.plugin.changed\",\n                {\"plugin_label\": plugin_label}\n            )\n\n            # Plugin is instance plugin\n            if plugin.__instanceEnabled__:\n                instances = pyblish.logic.instances_by_plugin(\n                    self._publish_context, plugin\n                )\n                if not instances:\n                    self._publish_report.set_plugin_skipped(plugin.id)\n                    continue\n\n                for instance in instances:\n                    if instance.data.get(\"publish\") is False:\n                        continue\n\n                    instance_label = (\n                        instance.data.get(\"label\")\n                        or instance.data[\"name\"]\n                    )\n                    self._emit_event(\n                        \"publish.process.instance.changed\",\n                        {\"instance_label\": instance_label}\n                    )\n\n                    yield partial(\n                        self._process_and_continue, plugin, instance\n                    )\n            else:\n                families = collect_families_from_instances(\n                    self._publish_context, only_active=True\n                )\n                plugins = pyblish.logic.plugins_by_families(\n                    [plugin], families\n                )\n                if not plugins:\n                    self._publish_report.set_plugin_skipped(plugin.id)\n                    continue\n\n                instance_label = (\n                    self._publish_context.data.get(\"label\")\n                    or self._publish_context.data.get(\"name\")\n                    or \"Context\"\n                )\n                self._emit_event(\n                    \"publish.process.instance.changed\",\n                    {\"instance_label\": instance_label}\n                )\n                yield partial(\n                    self._process_and_continue, plugin, None\n                )\n\n            self._publish_report.set_plugin_passed(plugin.id)\n\n        # Cleanup of publishing process\n        self._set_finished(True)\n        self._set_progress(self._publish_max_progress)\n        yield partial(self.stop_publish)\n\n    @contextmanager\n    def _log_manager(self, plugin: pyblish.api.Plugin):\n        root = logging.getLogger()\n        if not self._log_to_console:\n            plugin.log.propagate = False\n            plugin.log.addHandler(self._log_handler)\n            root.addHandler(self._log_handler)\n\n        try:\n            if self._log_to_console:\n                yield None\n            else:\n                yield self._log_handler\n\n        finally:\n            if not self._log_to_console:\n                plugin.log.propagate = True\n                plugin.log.removeHandler(self._log_handler)\n                root.removeHandler(self._log_handler)\n            self._log_handler.clear_records()\n\n    def _process_and_continue(\n        self,\n        plugin: pyblish.api.Plugin,\n        instance: pyblish.api.Instance\n    ):\n        with self._log_manager(plugin) as log_handler:\n            result = pyblish.plugin.process(\n                plugin, self._publish_context, instance\n            )\n            if log_handler is not None:\n                records = log_handler.get_records()\n                exception = result.get(\"error\")\n                if exception is not None and records:\n                    last_record = records[-1]\n                    if (\n                        last_record.name == \"pyblish.plugin\"\n                        and last_record.levelno == logging.ERROR\n                    ):\n                        # Remove last record made by pyblish\n                        # - `log.exception(formatted_traceback)`\n                        records.pop(-1)\n                result[\"records\"] = records\n\n        exception = result.get(\"error\")\n        if exception:\n            if (\n                isinstance(exception, PublishValidationError)\n                and not self._publish_has_validated\n            ):\n                result[\"is_validation_error\"] = True\n                self._add_validation_error(result)\n\n            else:\n                if isinstance(exception, PublishError):\n                    if not exception.title:\n                        exception.title = plugin.label or plugin.__name__\n                    self._add_publish_error_to_report(result)\n\n                error_info = PublishErrorInfo.from_exception(exception)\n                self._set_publish_error_info(error_info)\n                self._set_is_crashed(True)\n\n                result[\"is_validation_error\"] = False\n\n        self._publish_report.add_result(plugin.id, result)\n\n    def _add_validation_error(self, result: dict[str, Any]):\n        self._set_has_validation_errors(True)\n        self._add_publish_error_to_report(result)\n\n    def _add_publish_error_to_report(self, result: dict[str, Any]):\n        self._publish_errors.add_error(\n            result[\"plugin\"],\n            result[\"error\"],\n            result[\"instance\"]\n        )\n\n    def _is_publish_plugin_active(self, plugin: pyblish.api.Plugin) -&gt; bool:\n        \"\"\"Decide if publish plugin is active.\n\n        This is hack because 'active' is mis-used in mixin\n        'OptionalPyblishPluginMixin' where 'active' is used for default value\n        of optional plugins. Because of that is 'active' state of plugin\n        which inherit from 'OptionalPyblishPluginMixin' ignored. That affects\n        headless publishing inside host, potentially remote publishing.\n\n        We have to change that to match pyblish base, but we can do that\n        only when all hosts use Publisher because the change requires\n        change of settings schemas.\n\n        Args:\n            plugin (pyblish.Plugin): Plugin which should be checked if is\n                active.\n\n        Returns:\n            bool: Is plugin active.\n        \"\"\"\n\n        if plugin.active:\n            return True\n\n        if not plugin.optional:\n            return False\n\n        if OptionalPyblishPluginMixin in inspect.getmro(plugin):\n            return True\n        return False\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/models/publish.html#client.ayon_core.tools.publisher.models.publish.PublishModel.start_publish","title":"<code>start_publish(wait=True)</code>","text":"<p>Run publishing.</p> <p>Make sure all changes are saved before method is called (Call 'save_changes' and check output).</p> Source code in <code>client/ayon_core/tools/publisher/models/publish.py</code> <pre><code>def start_publish(self, wait: bool = True):\n    \"\"\"Run publishing.\n\n    Make sure all changes are saved before method is called (Call\n    'save_changes' and check output).\n    \"\"\"\n    if self._publish_up_validation and self._publish_has_validated:\n        return\n\n    self._start_publish()\n\n    if not wait:\n        return\n\n    while self.is_running():\n        func = self.get_next_process_func()\n        func()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/models/publish.html#client.ayon_core.tools.publisher.models.publish.PublishPluginActionItem","title":"<code>PublishPluginActionItem</code>","text":"<p>Representation of publish plugin action.</p> <p>Data driven object which is used as proxy for controller and UI.</p> <p>Parameters:</p> Name Type Description Default <code>action_id</code> <code>str</code> <p>Action id.</p> required <code>plugin_id</code> <code>str</code> <p>Plugin id.</p> required <code>active</code> <code>bool</code> <p>Action is active.</p> required <code>label</code> <code>str</code> <p>Action's label.</p> required Source code in <code>client/ayon_core/tools/publisher/models/publish.py</code> <pre><code>class PublishPluginActionItem:\n    \"\"\"Representation of publish plugin action.\n\n    Data driven object which is used as proxy for controller and UI.\n\n    Args:\n        action_id (str): Action id.\n        plugin_id (str): Plugin id.\n        active (bool): Action is active.\n        on_filter (Literal[\"all\", \"notProcessed\", \"processed\", \"failed\",\n            \"warning\", \"failedOrWarning\", \"succeeded\"]): Actions have 'on'\n            attribute which define  when can be action triggered\n            (e.g. 'all', 'failed', ...).\n        label (str): Action's label.\n        icon (str | None) Action's icon.\n    \"\"\"\n\n    def __init__(\n        self,\n        action_id: str,\n        plugin_id: str,\n        active: bool,\n        on_filter: str,\n        label: str,\n        icon: str | None,\n    ):\n        self.action_id: str = action_id\n        self.plugin_id: str = plugin_id\n        self.active: bool = active\n        self.on_filter: str = on_filter\n        self.label: str = label\n        self.icon: str | None = icon\n\n    def to_data(self) -&gt; dict[str, str | bool | None]:\n        \"\"\"Serialize object to dictionary.\n\n        Returns:\n            dict[str, str | bool | None]: Serialized object.\n\n        \"\"\"\n        return {\n            \"action_id\": self.action_id,\n            \"plugin_id\": self.plugin_id,\n            \"active\": self.active,\n            \"on_filter\": self.on_filter,\n            \"label\": self.label,\n            \"icon\": self.icon\n        }\n\n    @classmethod\n    def from_data(\n        cls, data: dict[str, str | bool | None]\n    ) -&gt; \"PublishPluginActionItem\":\n        \"\"\"Create object from data.\n\n        Args:\n            data (dict[str, str | bool | None]): Data used to recreate\n                object.\n\n        Returns:\n            PublishPluginActionItem: Object created using data.\n        \"\"\"\n\n        return cls(**data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/models/publish.html#client.ayon_core.tools.publisher.models.publish.PublishPluginActionItem.from_data","title":"<code>from_data(data)</code>  <code>classmethod</code>","text":"<p>Create object from data.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict[str, str | bool | None]</code> <p>Data used to recreate object.</p> required <p>Returns:</p> Name Type Description <code>PublishPluginActionItem</code> <code>'PublishPluginActionItem'</code> <p>Object created using data.</p> Source code in <code>client/ayon_core/tools/publisher/models/publish.py</code> <pre><code>@classmethod\ndef from_data(\n    cls, data: dict[str, str | bool | None]\n) -&gt; \"PublishPluginActionItem\":\n    \"\"\"Create object from data.\n\n    Args:\n        data (dict[str, str | bool | None]): Data used to recreate\n            object.\n\n    Returns:\n        PublishPluginActionItem: Object created using data.\n    \"\"\"\n\n    return cls(**data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/models/publish.html#client.ayon_core.tools.publisher.models.publish.PublishPluginActionItem.to_data","title":"<code>to_data()</code>","text":"<p>Serialize object to dictionary.</p> <p>Returns:</p> Type Description <code>dict[str, str | bool | None]</code> <p>dict[str, str | bool | None]: Serialized object.</p> Source code in <code>client/ayon_core/tools/publisher/models/publish.py</code> <pre><code>def to_data(self) -&gt; dict[str, str | bool | None]:\n    \"\"\"Serialize object to dictionary.\n\n    Returns:\n        dict[str, str | bool | None]: Serialized object.\n\n    \"\"\"\n    return {\n        \"action_id\": self.action_id,\n        \"plugin_id\": self.plugin_id,\n        \"active\": self.active,\n        \"on_filter\": self.on_filter,\n        \"label\": self.label,\n        \"icon\": self.icon\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/models/publish.html#client.ayon_core.tools.publisher.models.publish.PublishPluginsProxy","title":"<code>PublishPluginsProxy</code>","text":"<p>Wrapper around publish plugin.</p> <p>Prepare mapping for publish plugins and actions. Also can create serializable data for plugin actions so UI don't have to have access to them.</p> <p>This object is created in process where publishing is actually running.</p> Notes <p>Actions have id but single action can be used on multiple plugins so     to run an action is needed combination of plugin and action.</p> <p>Parameters:</p> Name Type Description Default <code>plugins [list[pyblish.api.Plugin]]</code> <p>Discovered plugins that will be processed.</p> required Source code in <code>client/ayon_core/tools/publisher/models/publish.py</code> <pre><code>class PublishPluginsProxy:\n    \"\"\"Wrapper around publish plugin.\n\n    Prepare mapping for publish plugins and actions. Also can create\n    serializable data for plugin actions so UI don't have to have access to\n    them.\n\n    This object is created in process where publishing is actually running.\n\n    Notes:\n        Actions have id but single action can be used on multiple plugins so\n            to run an action is needed combination of plugin and action.\n\n    Args:\n        plugins [list[pyblish.api.Plugin]]: Discovered plugins that will be\n            processed.\n    \"\"\"\n\n    def __init__(self, plugins: list[pyblish.api.Plugin]):\n        plugins_by_id: dict[str, pyblish.api.Plugin] = {}\n        actions_by_plugin_id: dict[str, dict[str, pyblish.api.Action]] = {}\n        action_ids_by_plugin_id: dict[str, list[str]] = {}\n        for plugin in plugins:\n            plugin_id = plugin.id\n            plugins_by_id[plugin_id] = plugin\n\n            action_ids = []\n            actions_by_id = {}\n            action_ids_by_plugin_id[plugin_id] = action_ids\n            actions_by_plugin_id[plugin_id] = actions_by_id\n\n            actions = getattr(plugin, \"actions\", None) or []\n            for action in actions:\n                action_id = action.id\n                action_ids.append(action_id)\n                actions_by_id[action_id] = action\n\n        self._plugins_by_id: dict[str, pyblish.api.Plugin] = plugins_by_id\n        self._actions_by_plugin_id: dict[\n            str, dict[str, pyblish.api.Action]\n        ] = actions_by_plugin_id\n        self._action_ids_by_plugin_id: dict[str, list[str]] = (\n            action_ids_by_plugin_id\n        )\n\n    def get_action(\n        self, plugin_id: str, action_id: str\n    ) -&gt; pyblish.api.Action:\n        return self._actions_by_plugin_id[plugin_id][action_id]\n\n    def get_plugin(self, plugin_id: str) -&gt; pyblish.api.Plugin:\n        return self._plugins_by_id[plugin_id]\n\n    def get_plugin_id(self, plugin: pyblish.api.Plugin) -&gt; str:\n        \"\"\"Get id of plugin based on plugin object.\n\n        It's used for validation errors report.\n\n        Args:\n            plugin (pyblish.api.Plugin): Publish plugin for which id should be\n                returned.\n\n        Returns:\n            str: Plugin id.\n        \"\"\"\n\n        return plugin.id\n\n    def get_plugin_action_items(\n        self, plugin_id: str\n    ) -&gt; list[PublishPluginActionItem]:\n        \"\"\"Get plugin action items for plugin by its id.\n\n        Args:\n            plugin_id (str): Publish plugin id.\n\n        Returns:\n            list[PublishPluginActionItem]: Items with information about publish\n                plugin actions.\n        \"\"\"\n\n        return [\n            self._create_action_item(\n                self.get_action(plugin_id, action_id), plugin_id\n            )\n            for action_id in self._action_ids_by_plugin_id[plugin_id]\n        ]\n\n    def _create_action_item(\n        self, action: pyblish.api.Action, plugin_id: str\n    ) -&gt; PublishPluginActionItem:\n        label = action.label or action.__name__\n        icon = getattr(action, \"icon\", None)\n        return PublishPluginActionItem(\n            action.id,\n            plugin_id,\n            action.active,\n            action.on,\n            label,\n            icon\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/models/publish.html#client.ayon_core.tools.publisher.models.publish.PublishPluginsProxy.get_plugin_action_items","title":"<code>get_plugin_action_items(plugin_id)</code>","text":"<p>Get plugin action items for plugin by its id.</p> <p>Parameters:</p> Name Type Description Default <code>plugin_id</code> <code>str</code> <p>Publish plugin id.</p> required <p>Returns:</p> Type Description <code>list[PublishPluginActionItem]</code> <p>list[PublishPluginActionItem]: Items with information about publish plugin actions.</p> Source code in <code>client/ayon_core/tools/publisher/models/publish.py</code> <pre><code>def get_plugin_action_items(\n    self, plugin_id: str\n) -&gt; list[PublishPluginActionItem]:\n    \"\"\"Get plugin action items for plugin by its id.\n\n    Args:\n        plugin_id (str): Publish plugin id.\n\n    Returns:\n        list[PublishPluginActionItem]: Items with information about publish\n            plugin actions.\n    \"\"\"\n\n    return [\n        self._create_action_item(\n            self.get_action(plugin_id, action_id), plugin_id\n        )\n        for action_id in self._action_ids_by_plugin_id[plugin_id]\n    ]\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/models/publish.html#client.ayon_core.tools.publisher.models.publish.PublishPluginsProxy.get_plugin_id","title":"<code>get_plugin_id(plugin)</code>","text":"<p>Get id of plugin based on plugin object.</p> <p>It's used for validation errors report.</p> <p>Parameters:</p> Name Type Description Default <code>plugin</code> <code>Plugin</code> <p>Publish plugin for which id should be returned.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Plugin id.</p> Source code in <code>client/ayon_core/tools/publisher/models/publish.py</code> <pre><code>def get_plugin_id(self, plugin: pyblish.api.Plugin) -&gt; str:\n    \"\"\"Get id of plugin based on plugin object.\n\n    It's used for validation errors report.\n\n    Args:\n        plugin (pyblish.api.Plugin): Publish plugin for which id should be\n            returned.\n\n    Returns:\n        str: Plugin id.\n    \"\"\"\n\n    return plugin.id\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/models/publish.html#client.ayon_core.tools.publisher.models.publish.collect_families_from_instances","title":"<code>collect_families_from_instances(instances, only_active=False)</code>","text":"<p>Collect all families for passed publish instances.</p> <p>Parameters:</p> Name Type Description Default <code>instances</code> <code>list[Instance]</code> <p>List of publish instances from which are families collected.</p> required <code>only_active</code> <code>bool</code> <p>Return families only for active instances.</p> <code>False</code> <p>Returns:</p> Type Description <code>list[str]</code> <p>list[str]: Families available on instances.</p> Source code in <code>client/ayon_core/tools/publisher/models/publish.py</code> <pre><code>def collect_families_from_instances(\n    instances: list[pyblish.api.Instance],\n    only_active: bool = False,\n) -&gt; list[str]:\n    \"\"\"Collect all families for passed publish instances.\n\n    Args:\n        instances (list[pyblish.api.Instance]): List of publish instances from\n            which are families collected.\n        only_active (bool): Return families only for active instances.\n\n    Returns:\n        list[str]: Families available on instances.\n\n    \"\"\"\n    all_families = set()\n    for instance in instances:\n        if only_active:\n            if instance.data.get(\"publish\") is False:\n                continue\n        family = instance.data.get(\"family\")\n        if family:\n            all_families.add(family)\n\n        families = instance.data.get(\"families\") or tuple()\n        for family in families:\n            all_families.add(family)\n\n    return list(all_families)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/publish_report_viewer/index.html","title":"publish_report_viewer","text":""},{"location":"autoapi/client/ayon_core/tools/publisher/publish_report_viewer/constants.html","title":"constants","text":""},{"location":"autoapi/client/ayon_core/tools/publisher/publish_report_viewer/delegates.html","title":"delegates","text":""},{"location":"autoapi/client/ayon_core/tools/publisher/publish_report_viewer/delegates.html#client.ayon_core.tools.publisher.publish_report_viewer.delegates.GroupItemDelegate","title":"<code>GroupItemDelegate</code>","text":"<p>               Bases: <code>QStyledItemDelegate</code></p> <p>Generic delegate for instance header</p> Source code in <code>client/ayon_core/tools/publisher/publish_report_viewer/delegates.py</code> <pre><code>class GroupItemDelegate(QtWidgets.QStyledItemDelegate):\n    \"\"\"Generic delegate for instance header\"\"\"\n\n    _item_icons_by_name_and_size = collections.defaultdict(dict)\n\n    _minus_pixmaps = {}\n    _plus_pixmaps = {}\n    _path_stroker = None\n\n    _item_pix_offset_ratio = 1.0 / 5.0\n    _item_border_size = 1.0 / 7.0\n    _group_pix_offset_ratio = 1.0 / 3.0\n    _group_pix_stroke_size_ratio = 1.0 / 7.0\n\n    @classmethod\n    def _get_path_stroker(cls):\n        if cls._path_stroker is None:\n            path_stroker = QtGui.QPainterPathStroker()\n            path_stroker.setCapStyle(QtCore.Qt.RoundCap)\n            path_stroker.setJoinStyle(QtCore.Qt.RoundJoin)\n\n            cls._path_stroker = path_stroker\n        return cls._path_stroker\n\n    @classmethod\n    def _get_plus_pixmap(cls, size):\n        pix = cls._minus_pixmaps.get(size)\n        if pix is not None:\n            return pix\n\n        pix = QtGui.QPixmap(size, size)\n        pix.fill(QtCore.Qt.transparent)\n\n        offset = int(size * cls._group_pix_offset_ratio)\n        pnt_1 = QtCore.QPoint(offset, int(size / 2))\n        pnt_2 = QtCore.QPoint(size - offset, int(size / 2))\n        pnt_3 = QtCore.QPoint(int(size / 2), offset)\n        pnt_4 = QtCore.QPoint(int(size / 2), size - offset)\n        path_1 = QtGui.QPainterPath(pnt_1)\n        path_1.lineTo(pnt_2)\n        path_2 = QtGui.QPainterPath(pnt_3)\n        path_2.lineTo(pnt_4)\n\n        path_stroker = cls._get_path_stroker()\n        path_stroker.setWidth(size * cls._group_pix_stroke_size_ratio)\n        stroked_path_1 = path_stroker.createStroke(path_1)\n        stroked_path_2 = path_stroker.createStroke(path_2)\n\n        pix = QtGui.QPixmap(size, size)\n        pix.fill(QtCore.Qt.transparent)\n\n        painter = QtGui.QPainter(pix)\n        painter.setRenderHint(QtGui.QPainter.Antialiasing)\n        painter.setPen(QtCore.Qt.transparent)\n        painter.setBrush(QtCore.Qt.white)\n        painter.drawPath(stroked_path_1)\n        painter.drawPath(stroked_path_2)\n        painter.end()\n\n        cls._minus_pixmaps[size] = pix\n\n        return pix\n\n    @classmethod\n    def _get_minus_pixmap(cls, size):\n        pix = cls._plus_pixmaps.get(size)\n        if pix is not None:\n            return pix\n\n        offset = int(size * cls._group_pix_offset_ratio)\n        pnt_1 = QtCore.QPoint(offset, int(size / 2))\n        pnt_2 = QtCore.QPoint(size - offset, int(size / 2))\n        path = QtGui.QPainterPath(pnt_1)\n        path.lineTo(pnt_2)\n        path_stroker = cls._get_path_stroker()\n        path_stroker.setWidth(size * cls._group_pix_stroke_size_ratio)\n        stroked_path = path_stroker.createStroke(path)\n\n        pix = QtGui.QPixmap(size, size)\n        pix.fill(QtCore.Qt.transparent)\n\n        painter = QtGui.QPainter(pix)\n        painter.setRenderHint(QtGui.QPainter.Antialiasing)\n        painter.setPen(QtCore.Qt.transparent)\n        painter.setBrush(QtCore.Qt.white)\n        painter.drawPath(stroked_path)\n        painter.end()\n\n        cls._plus_pixmaps[size] = pix\n\n        return pix\n\n    @classmethod\n    def _get_icon_color(cls, name):\n        if name == \"error\":\n            return QtGui.QColor(colors[\"error\"])\n        return QtGui.QColor(QtCore.Qt.white)\n\n    @classmethod\n    def _get_icon(cls, name, size):\n        icons_by_size = cls._item_icons_by_name_and_size[name]\n        if icons_by_size and size in icons_by_size:\n            return icons_by_size[size]\n\n        offset = int(size * cls._item_pix_offset_ratio)\n        offset_size = size - (2 * offset)\n        pix = QtGui.QPixmap(size, size)\n        pix.fill(QtCore.Qt.transparent)\n\n        painter = QtGui.QPainter(pix)\n        painter.setRenderHint(QtGui.QPainter.Antialiasing)\n\n        draw_ellipse = True\n        if name == \"error\":\n            color = QtGui.QColor(colors[\"error\"])\n            painter.setPen(QtCore.Qt.NoPen)\n            painter.setBrush(color)\n\n        elif name == \"skipped\":\n            color = QtGui.QColor(QtCore.Qt.white)\n            pen = QtGui.QPen(color)\n            pen.setWidth(int(size * cls._item_border_size))\n            painter.setPen(pen)\n            painter.setBrush(QtCore.Qt.transparent)\n\n        elif name == \"passed\":\n            color = QtGui.QColor(colors[\"ok\"])\n            painter.setPen(QtCore.Qt.NoPen)\n            painter.setBrush(color)\n\n        elif name == \"removed\":\n            draw_ellipse = False\n\n            offset = offset * 1.5\n            p1 = QtCore.QPoint(offset, offset)\n            p2 = QtCore.QPoint(size - offset, size - offset)\n            p3 = QtCore.QPoint(offset, size - offset)\n            p4 = QtCore.QPoint(size - offset, offset)\n\n            pen = QtGui.QPen(QtCore.Qt.white)\n            pen.setWidth(offset_size / 4)\n            pen.setCapStyle(QtCore.Qt.RoundCap)\n            painter.setPen(pen)\n            painter.setBrush(QtCore.Qt.transparent)\n            painter.drawLine(p1, p2)\n            painter.drawLine(p3, p4)\n\n        else:\n            color = QtGui.QColor(QtCore.Qt.white)\n            painter.setPen(QtCore.Qt.NoPen)\n            painter.setBrush(color)\n\n        if draw_ellipse:\n            painter.drawEllipse(offset, offset, offset_size, offset_size)\n\n        painter.end()\n\n        cls._item_icons_by_name_and_size[name][size] = pix\n\n        return pix\n\n    def paint(self, painter, option, index):\n        if index.data(ITEM_IS_GROUP_ROLE):\n            self.group_item_paint(painter, option, index)\n        else:\n            self.item_paint(painter, option, index)\n\n    def item_paint(self, painter, option, index):\n        self.initStyleOption(option, index)\n\n        widget = option.widget\n        if widget:\n            style = widget.style()\n        else:\n            style = QtWidgets.QApplicaion.style()\n\n        style.proxy().drawPrimitive(\n            QtWidgets.QStyle.PE_PanelItemViewItem, option, painter, widget\n        )\n        _rect = style.proxy().subElementRect(\n            QtWidgets.QStyle.SE_ItemViewItemText, option, widget\n        )\n        bg_rect = QtCore.QRectF(option.rect)\n        bg_rect.setY(_rect.y())\n        bg_rect.setHeight(_rect.height())\n\n        expander_rect = QtCore.QRectF(bg_rect)\n        expander_rect.setWidth(expander_rect.height() + 5)\n\n        label_rect = QtCore.QRectF(\n            expander_rect.x() + expander_rect.width(),\n            expander_rect.y(),\n            bg_rect.width() - expander_rect.width(),\n            expander_rect.height()\n        )\n\n        icon_size = expander_rect.height()\n        if index.data(ITEM_ERRORED_ROLE):\n            expander_icon = self._get_icon(\"error\", icon_size)\n        elif index.data(PLUGIN_SKIPPED_ROLE):\n            expander_icon = self._get_icon(\"skipped\", icon_size)\n        elif index.data(PLUGIN_PASSED_ROLE):\n            expander_icon = self._get_icon(\"passed\", icon_size)\n        elif index.data(INSTANCE_REMOVED_ROLE):\n            expander_icon = self._get_icon(\"removed\", icon_size)\n        else:\n            expander_icon = self._get_icon(\"\", icon_size)\n\n        label = index.data(QtCore.Qt.DisplayRole)\n        label = option.fontMetrics.elidedText(\n            label, QtCore.Qt.ElideRight, label_rect.width()\n        )\n\n        painter.save()\n        # Draw icon\n        pix_point = QtCore.QPoint(\n            expander_rect.center().x() - int(expander_icon.width() / 2),\n            expander_rect.top()\n        )\n        painter.drawPixmap(pix_point, expander_icon)\n\n        # Draw label\n        painter.setFont(option.font)\n        painter.drawText(label_rect, QtCore.Qt.AlignVCenter, label)\n\n        # Ok, we're done, tidy up.\n        painter.restore()\n\n    def group_item_paint(self, painter, option, index):\n        \"\"\"Paint text\n         _\n        My label\n        \"\"\"\n        self.initStyleOption(option, index)\n\n        widget = option.widget\n        if widget:\n            style = widget.style()\n        else:\n            style = QtWidgets.QApplicaion.style()\n        _rect = style.proxy().subElementRect(\n            QtWidgets.QStyle.SE_ItemViewItemText, option, widget\n        )\n\n        bg_rect = QtCore.QRectF(option.rect)\n        bg_rect.setY(_rect.y())\n        bg_rect.setHeight(_rect.height())\n\n        expander_height = bg_rect.height()\n        expander_width = expander_height + 5\n        expander_y_offset = expander_height % 2\n        expander_height -= expander_y_offset\n        expander_rect = QtCore.QRectF(\n            bg_rect.x(),\n            bg_rect.y() + expander_y_offset,\n            expander_width,\n            expander_height\n        )\n\n        label_rect = QtCore.QRectF(\n            bg_rect.x() + expander_width,\n            bg_rect.y(),\n            bg_rect.width() - expander_width,\n            bg_rect.height()\n        )\n\n        bg_path = QtGui.QPainterPath()\n        radius = (bg_rect.height() / 2) - 0.01\n        bg_path.addRoundedRect(bg_rect, radius, radius)\n\n        painter.fillPath(bg_path, colors[\"group\"])\n\n        selected = option.state &amp; QtWidgets.QStyle.State_Selected\n        hovered = option.state &amp; QtWidgets.QStyle.State_MouseOver\n\n        if selected and hovered:\n            painter.fillPath(bg_path, colors[\"selected\"])\n        elif hovered:\n            painter.fillPath(bg_path, colors[\"hover\"])\n\n        expanded = self.parent().isExpanded(index)\n        if expanded:\n            expander_icon = self._get_minus_pixmap(expander_height)\n        else:\n            expander_icon = self._get_plus_pixmap(expander_height)\n\n        label = index.data(QtCore.Qt.DisplayRole)\n        label = option.fontMetrics.elidedText(\n            label, QtCore.Qt.ElideRight, label_rect.width()\n        )\n\n        # Maintain reference to state, so we can restore it once we're done\n        painter.save()\n        pix_point = QtCore.QPoint(\n            expander_rect.center().x() - int(expander_icon.width() / 2),\n            expander_rect.top()\n        )\n        painter.drawPixmap(pix_point, expander_icon)\n\n        # Draw label\n        painter.setFont(option.font)\n        painter.drawText(label_rect, QtCore.Qt.AlignVCenter, label)\n\n        # Ok, we're done, tidy up.\n        painter.restore()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/publish_report_viewer/delegates.html#client.ayon_core.tools.publisher.publish_report_viewer.delegates.GroupItemDelegate.group_item_paint","title":"<code>group_item_paint(painter, option, index)</code>","text":"<p>Paint text  _ My label</p> Source code in <code>client/ayon_core/tools/publisher/publish_report_viewer/delegates.py</code> <pre><code>def group_item_paint(self, painter, option, index):\n    \"\"\"Paint text\n     _\n    My label\n    \"\"\"\n    self.initStyleOption(option, index)\n\n    widget = option.widget\n    if widget:\n        style = widget.style()\n    else:\n        style = QtWidgets.QApplicaion.style()\n    _rect = style.proxy().subElementRect(\n        QtWidgets.QStyle.SE_ItemViewItemText, option, widget\n    )\n\n    bg_rect = QtCore.QRectF(option.rect)\n    bg_rect.setY(_rect.y())\n    bg_rect.setHeight(_rect.height())\n\n    expander_height = bg_rect.height()\n    expander_width = expander_height + 5\n    expander_y_offset = expander_height % 2\n    expander_height -= expander_y_offset\n    expander_rect = QtCore.QRectF(\n        bg_rect.x(),\n        bg_rect.y() + expander_y_offset,\n        expander_width,\n        expander_height\n    )\n\n    label_rect = QtCore.QRectF(\n        bg_rect.x() + expander_width,\n        bg_rect.y(),\n        bg_rect.width() - expander_width,\n        bg_rect.height()\n    )\n\n    bg_path = QtGui.QPainterPath()\n    radius = (bg_rect.height() / 2) - 0.01\n    bg_path.addRoundedRect(bg_rect, radius, radius)\n\n    painter.fillPath(bg_path, colors[\"group\"])\n\n    selected = option.state &amp; QtWidgets.QStyle.State_Selected\n    hovered = option.state &amp; QtWidgets.QStyle.State_MouseOver\n\n    if selected and hovered:\n        painter.fillPath(bg_path, colors[\"selected\"])\n    elif hovered:\n        painter.fillPath(bg_path, colors[\"hover\"])\n\n    expanded = self.parent().isExpanded(index)\n    if expanded:\n        expander_icon = self._get_minus_pixmap(expander_height)\n    else:\n        expander_icon = self._get_plus_pixmap(expander_height)\n\n    label = index.data(QtCore.Qt.DisplayRole)\n    label = option.fontMetrics.elidedText(\n        label, QtCore.Qt.ElideRight, label_rect.width()\n    )\n\n    # Maintain reference to state, so we can restore it once we're done\n    painter.save()\n    pix_point = QtCore.QPoint(\n        expander_rect.center().x() - int(expander_icon.width() / 2),\n        expander_rect.top()\n    )\n    painter.drawPixmap(pix_point, expander_icon)\n\n    # Draw label\n    painter.setFont(option.font)\n    painter.drawText(label_rect, QtCore.Qt.AlignVCenter, label)\n\n    # Ok, we're done, tidy up.\n    painter.restore()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/publish_report_viewer/model.html","title":"model","text":""},{"location":"autoapi/client/ayon_core/tools/publisher/publish_report_viewer/widgets.html","title":"widgets","text":""},{"location":"autoapi/client/ayon_core/tools/publisher/publish_report_viewer/widgets.html#client.ayon_core.tools.publisher.publish_report_viewer.widgets.DeselectableTreeView","title":"<code>DeselectableTreeView</code>","text":"<p>               Bases: <code>QTreeView</code></p> <p>A tree view that deselects on clicking on an empty area in the view</p> Source code in <code>client/ayon_core/tools/publisher/publish_report_viewer/widgets.py</code> <pre><code>class DeselectableTreeView(QtWidgets.QTreeView):\n    \"\"\"A tree view that deselects on clicking on an empty area in the view\"\"\"\n\n    def mousePressEvent(self, event):\n        index = self.indexAt(event.pos())\n        clear_selection = False\n        if not index.isValid():\n            modifiers = QtWidgets.QApplication.keyboardModifiers()\n            if modifiers == QtCore.Qt.ShiftModifier:\n                return\n            elif modifiers == QtCore.Qt.ControlModifier:\n                return\n            clear_selection = True\n        else:\n            indexes = self.selectedIndexes()\n            if len(indexes) == 1 and index in indexes:\n                clear_selection = True\n\n        if clear_selection:\n            # clear the selection\n            self.clearSelection()\n            # clear the current index\n            self.setCurrentIndex(QtCore.QModelIndex())\n            event.accept()\n            return\n\n        QtWidgets.QTreeView.mousePressEvent(self, event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/publish_report_viewer/window.html","title":"window","text":""},{"location":"autoapi/client/ayon_core/tools/publisher/publish_report_viewer/window.html#client.ayon_core.tools.publisher.publish_report_viewer.window.PublishReportItem","title":"<code>PublishReportItem</code>","text":"<p>Report item representing one file in report directory.</p> Source code in <code>client/ayon_core/tools/publisher/publish_report_viewer/window.py</code> <pre><code>class PublishReportItem:\n    \"\"\"Report item representing one file in report directory.\"\"\"\n\n    def __init__(\n        self,\n        report: PublishReport | None,\n        report_path: str | None,\n    ) -&gt; str:\n        label_changed = False\n        if report is None:\n            created_at_obj = arrow.utcnow()\n            report_id = uuid.uuid4().hex\n        else:\n            created_at_obj = arrow.get(report.created_at).to(\"local\")\n            label = report.label\n            if not label:\n                date_str = created_at_obj.strftime(\"%Y-%m-%d %H:%M:%S\")\n                report.label = date_str\n                label_changed = True\n            report_id = report.id\n\n        self.report_id = report_id\n        self.report_path = report_path\n        self.report = report\n        self.created_at = float(created_at_obj.float_timestamp)\n\n        self._label_changed = label_changed\n\n    @classmethod\n    def from_filepath(\n        cls, filepath: str, report_path: str | None = None\n    ) -&gt; \"PublishReportItem\":\n        \"\"\"Create report item from file path.\n\n        Args:\n            filepath (str): Path to report file.\n\n        \"\"\"\n        report = None\n        new_label = None\n        try:\n            report = PublishReport.from_filepath(filepath)\n            if report_path is None:\n                report_path = os.path.join(get_reports_dir(), report.id)\n                report.store_to_file(report_path)\n\n        except Exception as exc:\n            traceback.print_exception(*sys.exc_info())\n            print(f\"Failed to load report from file: {filepath}. {exc}\")\n\n        obj = cls(report, report_path)\n        if new_label:\n            obj.label = new_label\n        return obj\n\n    @property\n    def id(self):\n        \"\"\"Publish report id.\n\n        Returns:\n            str: Publish report id.\n\n        \"\"\"\n        return self.report_id\n\n    def get_label(self) -&gt; str:\n        \"\"\"Publish report label.\n\n        Returns:\n            str: Publish report label showed in UI.\n\n        \"\"\"\n        if self.report is None:\n            return \"!!! Failed to load !!!\"\n        return self.report.label\n\n    def set_label(self, label: str) -&gt; None:\n        \"\"\"Set publish report label.\n\n        Args:\n            label (str): New publish report label.\n\n        \"\"\"\n        if self.report is None:\n            return\n\n        if self.report.label == label:\n            return\n\n        self.report.label = label\n        self._label_changed = True\n\n    label = property(get_label, set_label)\n\n    def save(self):\n        \"\"\"Save publish report to file.\"\"\"\n        if self.report_path is None or self.report is None:\n            return\n\n        save = False\n        if (\n            self._label_changed\n            or not os.path.exists(self.report_path)\n        ):\n            save = True\n\n        if not save:\n            return\n\n        self.report.store_to_file(self.report_path)\n\n        self._label_changed = False\n\n    def remove_file(self) -&gt; None:\n        \"\"\"Remove report file.\"\"\"\n        if self.report_path is None:\n            return\n\n        if os.path.exists(self.report_path):\n            os.remove(self.report_path)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/publish_report_viewer/window.html#client.ayon_core.tools.publisher.publish_report_viewer.window.PublishReportItem.id","title":"<code>id</code>  <code>property</code>","text":"<p>Publish report id.</p> <p>Returns:</p> Name Type Description <code>str</code> <p>Publish report id.</p>"},{"location":"autoapi/client/ayon_core/tools/publisher/publish_report_viewer/window.html#client.ayon_core.tools.publisher.publish_report_viewer.window.PublishReportItem.from_filepath","title":"<code>from_filepath(filepath, report_path=None)</code>  <code>classmethod</code>","text":"<p>Create report item from file path.</p> <p>Parameters:</p> Name Type Description Default <code>filepath</code> <code>str</code> <p>Path to report file.</p> required Source code in <code>client/ayon_core/tools/publisher/publish_report_viewer/window.py</code> <pre><code>@classmethod\ndef from_filepath(\n    cls, filepath: str, report_path: str | None = None\n) -&gt; \"PublishReportItem\":\n    \"\"\"Create report item from file path.\n\n    Args:\n        filepath (str): Path to report file.\n\n    \"\"\"\n    report = None\n    new_label = None\n    try:\n        report = PublishReport.from_filepath(filepath)\n        if report_path is None:\n            report_path = os.path.join(get_reports_dir(), report.id)\n            report.store_to_file(report_path)\n\n    except Exception as exc:\n        traceback.print_exception(*sys.exc_info())\n        print(f\"Failed to load report from file: {filepath}. {exc}\")\n\n    obj = cls(report, report_path)\n    if new_label:\n        obj.label = new_label\n    return obj\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/publish_report_viewer/window.html#client.ayon_core.tools.publisher.publish_report_viewer.window.PublishReportItem.get_label","title":"<code>get_label()</code>","text":"<p>Publish report label.</p> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Publish report label showed in UI.</p> Source code in <code>client/ayon_core/tools/publisher/publish_report_viewer/window.py</code> <pre><code>def get_label(self) -&gt; str:\n    \"\"\"Publish report label.\n\n    Returns:\n        str: Publish report label showed in UI.\n\n    \"\"\"\n    if self.report is None:\n        return \"!!! Failed to load !!!\"\n    return self.report.label\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/publish_report_viewer/window.html#client.ayon_core.tools.publisher.publish_report_viewer.window.PublishReportItem.remove_file","title":"<code>remove_file()</code>","text":"<p>Remove report file.</p> Source code in <code>client/ayon_core/tools/publisher/publish_report_viewer/window.py</code> <pre><code>def remove_file(self) -&gt; None:\n    \"\"\"Remove report file.\"\"\"\n    if self.report_path is None:\n        return\n\n    if os.path.exists(self.report_path):\n        os.remove(self.report_path)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/publish_report_viewer/window.html#client.ayon_core.tools.publisher.publish_report_viewer.window.PublishReportItem.save","title":"<code>save()</code>","text":"<p>Save publish report to file.</p> Source code in <code>client/ayon_core/tools/publisher/publish_report_viewer/window.py</code> <pre><code>def save(self):\n    \"\"\"Save publish report to file.\"\"\"\n    if self.report_path is None or self.report is None:\n        return\n\n    save = False\n    if (\n        self._label_changed\n        or not os.path.exists(self.report_path)\n    ):\n        save = True\n\n    if not save:\n        return\n\n    self.report.store_to_file(self.report_path)\n\n    self._label_changed = False\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/publish_report_viewer/window.html#client.ayon_core.tools.publisher.publish_report_viewer.window.PublishReportItem.set_label","title":"<code>set_label(label)</code>","text":"<p>Set publish report label.</p> <p>Parameters:</p> Name Type Description Default <code>label</code> <code>str</code> <p>New publish report label.</p> required Source code in <code>client/ayon_core/tools/publisher/publish_report_viewer/window.py</code> <pre><code>def set_label(self, label: str) -&gt; None:\n    \"\"\"Set publish report label.\n\n    Args:\n        label (str): New publish report label.\n\n    \"\"\"\n    if self.report is None:\n        return\n\n    if self.report.label == label:\n        return\n\n    self.report.label = label\n    self._label_changed = True\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/publish_report_viewer/window.html#client.ayon_core.tools.publisher.publish_report_viewer.window.PublisherReportHandler","title":"<code>PublisherReportHandler</code>","text":"<p>Class handling storing publish report items.</p> Source code in <code>client/ayon_core/tools/publisher/publish_report_viewer/window.py</code> <pre><code>class PublisherReportHandler:\n    \"\"\"Class handling storing publish report items.\"\"\"\n\n    def __init__(self):\n        self._reports_loaded = False\n        self._reports_by_id: dict[str, PublishReportItem] = {}\n\n    def reset(self):\n        self._reports_loaded = False\n        self._reports_by_id = {}\n\n    def iter_reports(self) -&gt; Generator[PublishReportItem, None, None]:\n        self._load_reports()\n        for report in self._reports_by_id.values():\n            yield report\n\n    def get_item_by_id(self, item_id: str) -&gt; PublishReportItem | None:\n        \"\"\"Get report item by id.\n\n        Args:\n            item_id (str): Report item id.\n\n        Returns:\n            PublishReportItem | None: Report item or None if not found.\n        \"\"\"\n        self._load_reports()\n        return self._reports_by_id.get(item_id)\n\n    def remove_report_item(self, item_id: str) -&gt; None:\n        \"\"\"Remove report item by id.\n\n        Remove from cache and also remove the file with the content.\n\n        Args:\n            item_id (str): Report item id.\n        \"\"\"\n\n        item = self._reports_by_id.pop(item_id, None)\n        if item is None:\n            return\n\n        try:\n            item.remove_file()\n        except Exception:\n            pass\n\n    def _load_reports(self) -&gt; None:\n        if self._reports_loaded:\n            return\n\n        reports_by_id = {}\n        report_dir = get_reports_dir()\n        for filename in os.listdir(report_dir):\n            filepath = os.path.join(report_dir, filename)\n            item = PublishReportItem.from_filepath(\n                filepath, report_path=filepath\n            )\n            reports_by_id[item.id] = item\n\n        self._reports_loaded = True\n        self._reports_by_id = reports_by_id\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/publish_report_viewer/window.html#client.ayon_core.tools.publisher.publish_report_viewer.window.PublisherReportHandler.get_item_by_id","title":"<code>get_item_by_id(item_id)</code>","text":"<p>Get report item by id.</p> <p>Parameters:</p> Name Type Description Default <code>item_id</code> <code>str</code> <p>Report item id.</p> required <p>Returns:</p> Type Description <code>PublishReportItem | None</code> <p>PublishReportItem | None: Report item or None if not found.</p> Source code in <code>client/ayon_core/tools/publisher/publish_report_viewer/window.py</code> <pre><code>def get_item_by_id(self, item_id: str) -&gt; PublishReportItem | None:\n    \"\"\"Get report item by id.\n\n    Args:\n        item_id (str): Report item id.\n\n    Returns:\n        PublishReportItem | None: Report item or None if not found.\n    \"\"\"\n    self._load_reports()\n    return self._reports_by_id.get(item_id)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/publish_report_viewer/window.html#client.ayon_core.tools.publisher.publish_report_viewer.window.PublisherReportHandler.remove_report_item","title":"<code>remove_report_item(item_id)</code>","text":"<p>Remove report item by id.</p> <p>Remove from cache and also remove the file with the content.</p> <p>Parameters:</p> Name Type Description Default <code>item_id</code> <code>str</code> <p>Report item id.</p> required Source code in <code>client/ayon_core/tools/publisher/publish_report_viewer/window.py</code> <pre><code>def remove_report_item(self, item_id: str) -&gt; None:\n    \"\"\"Remove report item by id.\n\n    Remove from cache and also remove the file with the content.\n\n    Args:\n        item_id (str): Report item id.\n    \"\"\"\n\n    item = self._reports_by_id.pop(item_id, None)\n    if item is None:\n        return\n\n    try:\n        item.remove_file()\n    except Exception:\n        pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/publish_report_viewer/window.html#client.ayon_core.tools.publisher.publish_report_viewer.window.get_reports_dir","title":"<code>get_reports_dir()</code>","text":"<p>Root directory where publish reports are stored for next session.</p> <p>Returns:</p> Name Type Description <code>str</code> <p>Path to directory where reports are stored.</p> Source code in <code>client/ayon_core/tools/publisher/publish_report_viewer/window.py</code> <pre><code>def get_reports_dir():\n    \"\"\"Root directory where publish reports are stored for next session.\n\n    Returns:\n        str: Path to directory where reports are stored.\n    \"\"\"\n\n    report_dir = get_launcher_local_dir(\"publish_report_viewer\")\n    os.makedirs(report_dir, exist_ok=True)\n    return report_dir\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/index.html","title":"widgets","text":""},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/index.html#client.ayon_core.tools.publisher.widgets.HelpButton","title":"<code>HelpButton</code>","text":"<p>               Bases: <code>QPushButton</code></p> <p>Button used to trigger help dialog.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/help_widget.py</code> <pre><code>class HelpButton(QtWidgets.QPushButton):\n    \"\"\"Button used to trigger help dialog.\"\"\"\n\n    def __init__(self, parent):\n        super().__init__(parent)\n        self.setObjectName(\"CreateDialogHelpButton\")\n        self.setText(\"?\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/index.html#client.ayon_core.tools.publisher.widgets.OverviewWidget","title":"<code>OverviewWidget</code>","text":"<p>               Bases: <code>QFrame</code></p> Source code in <code>client/ayon_core/tools/publisher/widgets/overview_widget.py</code> <pre><code>class OverviewWidget(QtWidgets.QFrame):\n    create_requested = QtCore.Signal()\n    convert_requested = QtCore.Signal()\n    publish_tab_requested = QtCore.Signal()\n\n    anim_end_value = 200\n    anim_duration = 200\n\n    def __init__(\n        self, controller: AbstractPublisherFrontend, parent: QtWidgets.QWidget\n    ):\n        super().__init__(parent)\n\n        self._refreshing_instances = False\n        self._controller: AbstractPublisherFrontend = controller\n\n        product_content_widget = QtWidgets.QWidget(self)\n\n        create_widget = CreateWidget(controller, product_content_widget)\n\n        # --- Created Products/Instances ---\n        # Common widget for creation and overview\n        product_views_widget = BorderedLabelWidget(\n            \"Products to publish\",\n            product_content_widget\n        )\n\n        product_view_cards = InstanceCardView(controller, product_views_widget)\n        product_list_view = InstanceListView(controller, product_views_widget)\n        product_list_view.set_parent_grouping(False)\n        product_list_view_grouped = InstanceListView(\n            controller, product_views_widget\n        )\n        product_list_view_grouped.set_parent_grouping(True)\n\n        product_views_layout = QtWidgets.QStackedLayout()\n        product_views_layout.addWidget(product_view_cards)\n        product_views_layout.addWidget(product_list_view)\n        product_views_layout.addWidget(product_list_view_grouped)\n        product_views_layout.setCurrentWidget(product_view_cards)\n\n        # Buttons at the bottom of product view\n        create_btn = CreateInstanceBtn(product_views_widget)\n        delete_btn = RemoveInstanceBtn(product_views_widget)\n        change_view_btn = ChangeViewBtn(product_views_widget)\n\n        # --- Overview ---\n        # Product details widget\n        product_attributes_wrap = BorderedLabelWidget(\n            \"Publish options\", product_content_widget\n        )\n        product_attributes_wrap.setVisible(False)\n        product_attributes_widget = ProductInfoWidget(\n            controller, product_attributes_wrap\n        )\n        product_attributes_wrap.set_center_widget(product_attributes_widget)\n\n        # Layout of buttons at the bottom of product view\n        product_view_btns_layout = QtWidgets.QHBoxLayout()\n        product_view_btns_layout.setContentsMargins(0, 5, 0, 0)\n        product_view_btns_layout.addWidget(create_btn)\n        product_view_btns_layout.addSpacing(5)\n        product_view_btns_layout.addWidget(delete_btn)\n        product_view_btns_layout.addStretch(1)\n        product_view_btns_layout.addWidget(change_view_btn)\n\n        # Layout of view and buttons\n        # - widget 'product_view_widget' is necessary\n        # - only layout won't be resized automatically to minimum size hint\n        #   on child resize request!\n        product_view_widget = QtWidgets.QWidget(product_views_widget)\n        product_view_layout = QtWidgets.QVBoxLayout(product_view_widget)\n        product_view_layout.setContentsMargins(0, 0, 0, 0)\n        product_view_layout.addLayout(product_views_layout, 1)\n        product_view_layout.addLayout(product_view_btns_layout, 0)\n\n        product_views_widget.set_center_widget(product_view_widget)\n\n        # Whole product layout with attributes and details\n        product_content_layout = QtWidgets.QHBoxLayout(product_content_widget)\n        product_content_layout.setContentsMargins(0, 0, 0, 0)\n        product_content_layout.addWidget(create_widget, 7)\n        product_content_layout.addWidget(product_views_widget, 3)\n        product_content_layout.addWidget(product_attributes_wrap, 7)\n\n        # Product frame layout\n        main_layout = QtWidgets.QVBoxLayout(self)\n        main_layout.setContentsMargins(0, 0, 0, 0)\n        main_layout.addWidget(product_content_widget, 1)\n\n        change_anim = QtCore.QVariantAnimation()\n        change_anim.setStartValue(float(0))\n        change_anim.setEndValue(float(self.anim_end_value))\n        change_anim.setDuration(self.anim_duration)\n        change_anim.setEasingCurve(QtCore.QEasingCurve.InOutQuad)\n\n        # --- Calbacks for instances/products view ---\n        create_btn.clicked.connect(self._on_create_clicked)\n        delete_btn.clicked.connect(self._on_delete_clicked)\n        change_view_btn.clicked.connect(self._on_change_view_clicked)\n\n        change_anim.valueChanged.connect(self._on_change_anim)\n        change_anim.finished.connect(self._on_change_anim_finished)\n\n        # Selection changed\n        product_list_view.selection_changed.connect(\n            self._on_product_change\n        )\n        product_list_view.double_clicked.connect(\n            self.publish_tab_requested\n        )\n        product_list_view_grouped.selection_changed.connect(\n            self._on_product_change\n        )\n        product_list_view_grouped.double_clicked.connect(\n            self.publish_tab_requested\n        )\n        product_view_cards.selection_changed.connect(\n            self._on_product_change\n        )\n        product_view_cards.double_clicked.connect(\n            self.publish_tab_requested\n        )\n        # Instance context has changed\n        product_attributes_widget.convert_requested.connect(\n            self._on_convert_requested\n        )\n\n        # --- Controller callbacks ---\n        controller.register_event_callback(\n            \"publish.process.started\", self._on_publish_start\n        )\n        controller.register_event_callback(\n            \"controller.reset.started\", self._on_controller_reset_start\n        )\n        controller.register_event_callback(\n            \"publish.reset.finished\", self._on_publish_reset\n        )\n        controller.register_event_callback(\n            \"create.model.reset\",\n            self._on_create_model_reset\n        )\n        controller.register_event_callback(\n            \"create.context.added.instance\",\n            self._on_instances_added\n        )\n        controller.register_event_callback(\n            \"create.context.removed.instance\",\n            self._on_instances_removed\n        )\n        controller.register_event_callback(\n            \"create.model.instances.context.changed\",\n            self._on_instance_context_change\n        )\n        controller.register_event_callback(\n            \"create.model.instance.requirement.changed\",\n            self._on_instance_requirement_changed\n        )\n        controller.register_event_callback(\n            \"create.model.instance.parent.changed\",\n            self._on_instance_parent_changed\n        )\n\n        self._product_content_widget = product_content_widget\n        self._product_content_layout = product_content_layout\n\n        self._product_view_cards = product_view_cards\n        self._product_list_view = product_list_view\n        self._product_list_view_grouped = product_list_view_grouped\n        self._product_views_layout = product_views_layout\n\n        self._create_btn = create_btn\n        self._delete_btn = delete_btn\n        self._change_view_btn = change_view_btn\n\n        self._product_attributes_widget = product_attributes_widget\n        self._create_widget = create_widget\n        self._product_views_widget = product_views_widget\n        self._product_attributes_wrap = product_attributes_wrap\n\n        self._change_anim = change_anim\n\n        # Start in create mode\n        self._current_state = \"create\"\n        product_attributes_wrap.setVisible(False)\n\n    def make_sure_animation_is_finished(self):\n        if self._change_anim.state() == QtCore.QAbstractAnimation.Running:\n            self._change_anim.stop()\n        self._on_change_anim_finished()\n\n    def set_state(self, new_state, animate):\n        if new_state == self._current_state:\n            return\n\n        self._current_state = new_state\n\n        if not animate:\n            self.make_sure_animation_is_finished()\n            return\n\n        if new_state == \"create\":\n            direction = QtCore.QAbstractAnimation.Backward\n        else:\n            direction = QtCore.QAbstractAnimation.Forward\n        self._change_anim.setDirection(direction)\n\n        if (\n            self._change_anim.state() != QtCore.QAbstractAnimation.Running\n        ):\n            self._start_animation()\n\n    def _start_animation(self):\n        views_geo = self._product_views_widget.geometry()\n        layout_spacing = self._product_content_layout.spacing()\n        if self._create_widget.isVisible():\n            create_geo = self._create_widget.geometry()\n            product_geo = QtCore.QRect(create_geo)\n            product_geo.moveTop(views_geo.top())\n            product_geo.moveLeft(views_geo.right() + layout_spacing)\n            self._product_attributes_wrap.setVisible(True)\n\n        elif self._product_attributes_wrap.isVisible():\n            product_geo = self._product_attributes_wrap.geometry()\n            create_geo = QtCore.QRect(product_geo)\n            create_geo.moveTop(views_geo.top())\n            create_geo.moveRight(views_geo.left() - (layout_spacing + 1))\n            self._create_widget.setVisible(True)\n        else:\n            self._change_anim.start()\n            return\n\n        while self._product_content_layout.count():\n            self._product_content_layout.takeAt(0)\n        self._product_views_widget.setGeometry(views_geo)\n        self._create_widget.setGeometry(create_geo)\n        self._product_attributes_wrap.setGeometry(product_geo)\n\n        self._change_anim.start()\n\n    def get_product_views_geo(self):\n        parent = self._product_views_widget.parent()\n        global_pos = parent.mapToGlobal(self._product_views_widget.pos())\n        return QtCore.QRect(\n            global_pos.x(),\n            global_pos.y(),\n            self._product_views_widget.width(),\n            self._product_views_widget.height()\n        )\n\n    def has_items(self):\n        view = self._get_current_view()\n        return view.has_items()\n\n    def _on_create_clicked(self):\n        \"\"\"Pass signal to parent widget which should care about changing state.\n\n        We don't change anything here until the parent will care about it.\n        \"\"\"\n\n        self.create_requested.emit()\n\n    def _on_delete_clicked(self):\n        instance_ids, _, _ = self.get_selected_items()\n\n        # Ask user if he really wants to remove instances\n        dialog = QtWidgets.QMessageBox(self)\n        dialog.setIcon(QtWidgets.QMessageBox.Question)\n        dialog.setWindowTitle(\"Are you sure?\")\n        if len(instance_ids) &gt; 1:\n            msg = (\n                \"Do you really want to remove {} instances?\"\n            ).format(len(instance_ids))\n        else:\n            msg = (\n                \"Do you really want to remove the instance?\"\n            )\n        dialog.setText(msg)\n        dialog.setStandardButtons(\n            QtWidgets.QMessageBox.Ok | QtWidgets.QMessageBox.Cancel\n        )\n        dialog.setDefaultButton(QtWidgets.QMessageBox.Ok)\n        dialog.setEscapeButton(QtWidgets.QMessageBox.Cancel)\n        dialog.exec_()\n        # Skip if OK was not clicked\n        if dialog.result() == QtWidgets.QMessageBox.Ok:\n            instance_ids = set(instance_ids)\n            self._controller.remove_instances(instance_ids)\n\n    def _on_change_view_clicked(self):\n        self._change_view_type()\n\n    def _on_product_change(self, *_args):\n        # Ignore changes if in middle of refreshing\n        if self._refreshing_instances:\n            return\n\n        instance_ids, context_selected, convertor_identifiers = (\n            self.get_selected_items()\n        )\n\n        # Disable delete button if nothing is selected\n        self._delete_btn.setEnabled(len(instance_ids) &gt; 0)\n        instances_by_id = self._controller.get_instance_items_by_id(\n            instance_ids\n        )\n        instances = [\n            instances_by_id[instance_id]\n            for instance_id in instance_ids\n        ]\n        self._product_attributes_widget.set_current_instances(\n            instances, context_selected, convertor_identifiers\n        )\n\n    def _on_change_anim(self, value):\n        self._create_widget.setVisible(True)\n        self._product_attributes_wrap.setVisible(True)\n        layout_spacing = self._product_content_layout.spacing()\n\n        content_width = (\n            self._product_content_widget.width() - (layout_spacing * 2)\n        )\n        content_height = self._product_content_widget.height()\n        views_width = max(\n            int(content_width * 0.3),\n            self._product_views_widget.minimumWidth()\n        )\n        width = content_width - views_width\n        # Visible widths of other widgets\n        product_attrs_width = int((float(width) / self.anim_end_value) * value)\n        create_width = width - product_attrs_width\n\n        views_geo = QtCore.QRect(\n            create_width + layout_spacing, 0,\n            views_width, content_height\n        )\n        create_geo = QtCore.QRect(0, 0, width, content_height)\n        product_attrs_geo = QtCore.QRect(create_geo)\n        create_geo.moveRight(views_geo.left() - (layout_spacing + 1))\n        product_attrs_geo.moveLeft(views_geo.right() + layout_spacing)\n\n        self._product_views_widget.setGeometry(views_geo)\n        self._create_widget.setGeometry(create_geo)\n        self._product_attributes_wrap.setGeometry(product_attrs_geo)\n\n    def _on_change_anim_finished(self):\n        self._change_visibility_for_state()\n        self._product_content_layout.addWidget(self._create_widget, 7)\n        self._product_content_layout.addWidget(self._product_views_widget, 3)\n        self._product_content_layout.addWidget(\n            self._product_attributes_wrap, 7\n        )\n\n    def _change_visibility_for_state(self):\n        self._create_widget.setVisible(\n            self._current_state == \"create\"\n        )\n        self._product_attributes_wrap.setVisible(\n            self._current_state == \"publish\"\n        )\n\n    def _on_instance_context_change(self, event):\n        self._refresh_instance_states(event[\"instance_ids\"])\n\n    def _on_instance_requirement_changed(self, event):\n        self._refresh_instance_states(event[\"instance_ids\"])\n\n    def _on_instance_parent_changed(self, event):\n        self._refresh_instance_states(event[\"instance_ids\"])\n\n    def _refresh_instance_states(self, instance_ids):\n        current_view = self._get_current_view()\n        for view in self._iter_views():\n            if view is current_view:\n                current_view = view\n            elif view.refreshed:\n                view.set_refreshed(False)\n\n        current_view.refresh_instance_states(instance_ids)\n\n    def _on_convert_requested(self):\n        self.convert_requested.emit()\n\n    def get_selected_items(self):\n        \"\"\"Selected items in current view widget.\n\n        Returns:\n            tuple[list[str], bool, list[str]]: Selected items. List of\n                instance ids, context is selected, list of selected legacy\n                convertor plugins.\n        \"\"\"\n\n        view = self._get_current_view()\n        return view.get_selected_items()\n\n    def get_selected_legacy_convertors(self):\n        \"\"\"Selected legacy convertor identifiers.\n\n        Returns:\n            list[str]: Selected legacy convertor identifiers.\n                Example: ['io.ayon.creators.houdini.legacy']\n        \"\"\"\n\n        _, _, convertor_identifiers = self.get_selected_items()\n        return convertor_identifiers\n\n    def _change_view_type(self):\n        old_view = self._get_current_view()\n\n        idx = self._product_views_layout.currentIndex()\n        new_idx = (idx + 1) % self._product_views_layout.count()\n\n        new_view = self._get_view_by_idx(new_idx)\n        if not new_view.refreshed:\n            new_view.refresh()\n            new_view.set_refreshed(True)\n        else:\n            new_view.refresh_instance_states()\n\n        instance_ids, context_selected, convertor_identifiers = (\n            old_view.get_selected_items()\n        )\n        new_view.set_selected_items(\n            instance_ids, context_selected, convertor_identifiers\n        )\n        view_type = \"list\"\n        if new_view is self._product_list_view_grouped:\n            view_type = \"card\"\n        elif new_view is self._product_list_view:\n            view_type = \"list-parent-grouping\"\n\n        self._change_view_btn.set_view_type(view_type)\n        self._product_views_layout.setCurrentIndex(new_idx)\n\n        self._on_product_change()\n\n    def _iter_views(self) -&gt; Generator[AbstractInstanceView, None, None]:\n        for idx in range(self._product_views_layout.count()):\n            widget = self._product_views_layout.widget(idx)\n            if not isinstance(widget, AbstractInstanceView):\n                raise TypeError(\n                    \"Current widget is not instance of 'AbstractInstanceView'\"\n                )\n            yield widget\n\n    def _get_current_view(self) -&gt; AbstractInstanceView:\n        widget = self._product_views_layout.currentWidget()\n        if isinstance(widget, AbstractInstanceView):\n            return widget\n        raise TypeError(\n            \"Current widget is not instance of 'AbstractInstanceView'\"\n        )\n\n    def _get_view_by_idx(self, idx: int) -&gt; AbstractInstanceView:\n        widget = self._product_views_layout.widget(idx)\n        if isinstance(widget, AbstractInstanceView):\n            return widget\n        raise TypeError(\n            \"Current widget is not instance of 'AbstractInstanceView'\"\n        )\n\n    def _refresh_instances(self):\n        if self._refreshing_instances:\n            return\n\n        self._refreshing_instances = True\n\n        for view in self._iter_views():\n            view.set_refreshed(False)\n\n        view = self._get_current_view()\n        view.refresh()\n        view.set_refreshed(True)\n\n        self._refreshing_instances = False\n\n        # Force to change instance and refresh details\n        self._on_product_change()\n\n        # Give a change to process Resize Request\n        QtWidgets.QApplication.processEvents()\n        # Trigger update geometry\n        view.updateGeometry()\n\n    def _on_publish_start(self):\n        \"\"\"Publish started.\"\"\"\n\n        self._create_btn.setEnabled(False)\n        self._product_attributes_wrap.setEnabled(False)\n        for view in self._iter_views():\n            view.set_active_toggle_enabled(False)\n\n    def _on_controller_reset_start(self):\n        \"\"\"Controller reset started.\"\"\"\n\n        for view in self._iter_views():\n            view.set_active_toggle_enabled(True)\n\n    def _on_publish_reset(self):\n        \"\"\"Context in controller has been reseted.\"\"\"\n\n        self._create_btn.setEnabled(True)\n        self._product_attributes_wrap.setEnabled(True)\n        self._product_content_widget.setEnabled(\n            self._controller.is_host_valid()\n        )\n\n    def _on_create_model_reset(self):\n        self._refresh_instances()\n\n    def _on_instances_added(self):\n        view = self._get_current_view()\n        is_card_view = False\n        count = 0\n        if isinstance(view, InstanceCardView):\n            is_card_view = True\n            count = view.get_current_instance_count()\n\n        self._refresh_instances()\n\n        if is_card_view and count &lt; 10:\n            new_count = view.get_current_instance_count()\n            if new_count &gt; count and new_count &gt;= 10:\n                self._change_view_type()\n\n    def _on_instances_removed(self):\n        self._refresh_instances()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/index.html#client.ayon_core.tools.publisher.widgets.OverviewWidget.get_selected_items","title":"<code>get_selected_items()</code>","text":"<p>Selected items in current view widget.</p> <p>Returns:</p> Type Description <p>tuple[list[str], bool, list[str]]: Selected items. List of instance ids, context is selected, list of selected legacy convertor plugins.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/overview_widget.py</code> <pre><code>def get_selected_items(self):\n    \"\"\"Selected items in current view widget.\n\n    Returns:\n        tuple[list[str], bool, list[str]]: Selected items. List of\n            instance ids, context is selected, list of selected legacy\n            convertor plugins.\n    \"\"\"\n\n    view = self._get_current_view()\n    return view.get_selected_items()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/index.html#client.ayon_core.tools.publisher.widgets.OverviewWidget.get_selected_legacy_convertors","title":"<code>get_selected_legacy_convertors()</code>","text":"<p>Selected legacy convertor identifiers.</p> <p>Returns:</p> Type Description <p>list[str]: Selected legacy convertor identifiers. Example: ['io.ayon.creators.houdini.legacy']</p> Source code in <code>client/ayon_core/tools/publisher/widgets/overview_widget.py</code> <pre><code>def get_selected_legacy_convertors(self):\n    \"\"\"Selected legacy convertor identifiers.\n\n    Returns:\n        list[str]: Selected legacy convertor identifiers.\n            Example: ['io.ayon.creators.houdini.legacy']\n    \"\"\"\n\n    _, _, convertor_identifiers = self.get_selected_items()\n    return convertor_identifiers\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/index.html#client.ayon_core.tools.publisher.widgets.PublishBtn","title":"<code>PublishBtn</code>","text":"<p>               Bases: <code>PublishIconBtn</code></p> <p>Publish start publish button.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/widgets.py</code> <pre><code>class PublishBtn(PublishIconBtn):\n    \"\"\"Publish start publish button.\"\"\"\n    def __init__(self, parent=None):\n        icon_path = get_icon_path(\"play\")\n        super().__init__(icon_path, \"Publish\", parent)\n        self.setToolTip(\"Publish\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/index.html#client.ayon_core.tools.publisher.widgets.PublishFrame","title":"<code>PublishFrame</code>","text":"<p>               Bases: <code>QWidget</code></p> <p>Frame showed during publishing.</p> <p>Shows all information related to publishing. Contains validation error widget which is showed if only validation error happens during validation.</p> <p>Processing layer is default layer. Validation error layer is shown if only validation exception is raised during publishing. Report layer is available only when publishing process is stopped and must be manually triggered to change into that layer.</p> <p>+------------------------------------------------------------------------+ |                             &lt; Main label &gt;                             | |                             &lt; Label top &gt;                              | |        (####                10%                  )       | |   | |   | +------------------------------------------------------------------------+ Source code in <code>client/ayon_core/tools/publisher/widgets/publish_frame.py</code> <pre><code>class PublishFrame(QtWidgets.QWidget):\n    \"\"\"Frame showed during publishing.\n\n    Shows all information related to publishing. Contains validation error\n    widget which is showed if only validation error happens during validation.\n\n    Processing layer is default layer. Validation error layer is shown if only\n    validation exception is raised during publishing. Report layer is available\n    only when publishing process is stopped and must be manually triggered to\n    change into that layer.\n\n    +------------------------------------------------------------------------+\n    |                             &lt; Main label &gt;                             |\n    |                             &lt; Label top &gt;                              |\n    |        (####                10%  &lt;Progress bar&gt;                )       |\n    | &lt;Instance label&gt;                                        &lt;Plugin label&gt; |\n    | &lt;Report&gt;                              &lt;Reset&gt;&lt;Stop&gt;&lt;Validate&gt;&lt;Publish&gt; |\n    +------------------------------------------------------------------------+\n    \"\"\"\n\n    details_page_requested = QtCore.Signal()\n\n    def __init__(\n        self,\n        controller: AbstractPublisherFrontend,\n        borders: int,\n        parent: QtWidgets.QWidget\n    ):\n        super().__init__(parent)\n\n        # Bottom part of widget where process and callback buttons are showed\n        # - QFrame used to be able set background using stylesheets easily\n        #   and not override all children widgets style\n        content_frame = QtWidgets.QFrame(self)\n        content_frame.setObjectName(\"PublishInfoFrame\")\n\n        top_content_widget = QtWidgets.QWidget(content_frame)\n\n        # Center widget displaying current state (without any specific info)\n        main_label = QtWidgets.QLabel(top_content_widget)\n        main_label.setObjectName(\"PublishInfoMainLabel\")\n        main_label.setAlignment(QtCore.Qt.AlignCenter)\n\n        # Supporting labels for main label\n        # Top label is displayed just under main label\n        message_label_top = QtWidgets.QLabel(top_content_widget)\n        message_label_top.setAlignment(QtCore.Qt.AlignCenter)\n\n        # Label showing currently processed instance\n        progress_widget = QtWidgets.QWidget(top_content_widget)\n        instance_plugin_widget = QtWidgets.QWidget(progress_widget)\n        instance_label = QtWidgets.QLabel(\n            \"&lt;Instance name&gt;\", instance_plugin_widget\n        )\n        instance_label.setAlignment(\n            QtCore.Qt.AlignLeft | QtCore.Qt.AlignVCenter\n        )\n        # Label showing currently processed plugin\n        plugin_label = QtWidgets.QLabel(\n            \"&lt;Plugin name&gt;\", instance_plugin_widget\n        )\n        plugin_label.setAlignment(\n            QtCore.Qt.AlignRight | QtCore.Qt.AlignVCenter\n        )\n        instance_plugin_layout = QtWidgets.QHBoxLayout(instance_plugin_widget)\n        instance_plugin_layout.setContentsMargins(0, 0, 0, 0)\n        instance_plugin_layout.addWidget(instance_label, 1)\n        instance_plugin_layout.addWidget(plugin_label, 1)\n\n        # Progress bar showing progress of publishing\n        progress_bar = QtWidgets.QProgressBar(progress_widget)\n        progress_bar.setObjectName(\"PublishProgressBar\")\n\n        progress_layout = QtWidgets.QVBoxLayout(progress_widget)\n        progress_layout.setSpacing(5)\n        progress_layout.setContentsMargins(0, 0, 0, 0)\n        progress_layout.addWidget(instance_plugin_widget, 0)\n        progress_layout.addWidget(progress_bar, 0)\n\n        top_content_layout = QtWidgets.QVBoxLayout(top_content_widget)\n        top_content_layout.setContentsMargins(0, 0, 0, 0)\n        top_content_layout.setSpacing(5)\n        top_content_layout.setAlignment(QtCore.Qt.AlignCenter)\n        top_content_layout.addWidget(main_label)\n        # TODO stretches should be probably replaced by spacing...\n        # - stretch in floating frame doesn't make sense\n        top_content_layout.addWidget(message_label_top)\n        top_content_layout.addWidget(progress_widget)\n\n        # Publishing buttons to stop, reset or trigger publishing\n        footer_widget = QtWidgets.QWidget(content_frame)\n\n        report_btn = PublishReportBtn(footer_widget)\n\n        shrunk_main_label = QtWidgets.QLabel(footer_widget)\n        shrunk_main_label.setObjectName(\"PublishInfoMainLabel\")\n        shrunk_main_label.setAlignment(\n            QtCore.Qt.AlignVCenter | QtCore.Qt.AlignLeft\n        )\n\n        reset_btn = ResetBtn(footer_widget)\n        stop_btn = StopBtn(footer_widget)\n        validate_btn = ValidateBtn(footer_widget)\n        publish_btn = PublishBtn(footer_widget)\n\n        report_btn.add_action(\"Go to details\", \"go_to_report\")\n        report_btn.add_action(\"Copy report\", \"copy_report\")\n        report_btn.add_action(\"Export report\", \"export_report\")\n\n        # Footer on info frame layout\n        footer_layout = QtWidgets.QHBoxLayout(footer_widget)\n        footer_layout.setContentsMargins(0, 0, 0, 0)\n        footer_layout.addWidget(report_btn, 0)\n        footer_layout.addWidget(shrunk_main_label, 1)\n        footer_layout.addWidget(reset_btn, 0)\n        footer_layout.addWidget(stop_btn, 0)\n        footer_layout.addWidget(validate_btn, 0)\n        footer_layout.addWidget(publish_btn, 0)\n\n        # Info frame content\n        content_layout = QtWidgets.QVBoxLayout(content_frame)\n        content_layout.setSpacing(5)\n\n        content_layout.addWidget(top_content_widget)\n        content_layout.addWidget(footer_widget)\n\n        main_layout = QtWidgets.QVBoxLayout(self)\n        main_layout.setContentsMargins(borders, 0, borders, borders)\n        main_layout.addWidget(content_frame)\n\n        shrunk_anim = QtCore.QVariantAnimation()\n        shrunk_anim.setDuration(140)\n        shrunk_anim.setEasingCurve(QtCore.QEasingCurve.InOutQuad)\n\n        # Force translucent background for widgets\n        for widget in (\n            self,\n            top_content_widget,\n            footer_widget,\n            progress_widget,\n            instance_plugin_widget,\n        ):\n            widget.setAttribute(QtCore.Qt.WA_TranslucentBackground)\n\n        report_btn.triggered.connect(self._on_report_triggered)\n        reset_btn.clicked.connect(self._on_reset_clicked)\n        stop_btn.clicked.connect(self._on_stop_clicked)\n        validate_btn.clicked.connect(self._on_validate_clicked)\n        publish_btn.clicked.connect(self._on_publish_clicked)\n\n        shrunk_anim.valueChanged.connect(self._on_shrunk_anim)\n        shrunk_anim.finished.connect(self._on_shrunk_anim_finish)\n\n        controller.register_event_callback(\n            \"publish.reset.finished\", self._on_publish_reset\n        )\n        controller.register_event_callback(\n            \"publish.process.started\", self._on_publish_start\n        )\n        controller.register_event_callback(\n            \"publish.has_validated.changed\", self._on_publish_validated_change\n        )\n        controller.register_event_callback(\n            \"publish.process.stopped\", self._on_publish_stop\n        )\n\n        controller.register_event_callback(\n            \"publish.process.instance.changed\", self._on_instance_change\n        )\n        controller.register_event_callback(\n            \"publish.process.plugin.changed\", self._on_plugin_change\n        )\n\n        self._shrunk_anim = shrunk_anim\n\n        self._controller: AbstractPublisherFrontend = controller\n\n        self._content_frame = content_frame\n        self._content_layout = content_layout\n        self._top_content_layout = top_content_layout\n        self._top_content_widget = top_content_widget\n\n        self._main_label = main_label\n        self._message_label_top = message_label_top\n\n        self._instance_label = instance_label\n        self._plugin_label = plugin_label\n\n        self._progress_bar = progress_bar\n        self._progress_widget = progress_widget\n\n        self._shrunk_main_label = shrunk_main_label\n        self._reset_btn = reset_btn\n        self._stop_btn = stop_btn\n        self._validate_btn = validate_btn\n        self._publish_btn = publish_btn\n\n        self._shrunken = False\n        self._top_widget_max_height = None\n        self._top_widget_size_policy = top_content_widget.sizePolicy()\n        self._last_instance_label = None\n        self._last_plugin_label = None\n\n    def mouseReleaseEvent(self, event):\n        super().mouseReleaseEvent(event)\n        self._change_shrunk_state()\n\n    def _change_shrunk_state(self):\n        self.set_shrunk_state(not self._shrunken)\n\n    def set_shrunk_state(self, shrunk):\n        if shrunk is self._shrunken:\n            return\n\n        if self._top_widget_max_height is None:\n            self._top_widget_max_height = (\n                self._top_content_widget.maximumHeight()\n            )\n\n        self._shrunken = shrunk\n\n        anim_is_running = (\n            self._shrunk_anim.state() == QtCore.QAbstractAnimation.Running\n        )\n        if not self.isVisible():\n            if anim_is_running:\n                self._shrunk_anim.stop()\n            self._on_shrunk_anim_finish()\n            return\n\n        start = 0\n        end = 0\n        if shrunk:\n            start = self._top_content_widget.height()\n        else:\n            if anim_is_running:\n                start = self._shrunk_anim.currentValue()\n            hint = self._top_content_widget.minimumSizeHint()\n            end = hint.height()\n\n        self._shrunk_anim.setStartValue(float(start))\n        self._shrunk_anim.setEndValue(float(end))\n        if not anim_is_running:\n            self._shrunk_anim.start()\n\n    def _on_shrunk_anim(self, value):\n        diff = self._top_content_widget.height() - int(value)\n        if not self._top_content_widget.isVisible():\n            diff -= self._content_layout.spacing()\n\n        window_pos = self.pos()\n        window_pos_y = window_pos.y() + diff\n        window_height = self.height() - diff\n\n        self._top_content_widget.setMinimumHeight(value)\n        self._top_content_widget.setMaximumHeight(value)\n        self._top_content_widget.setVisible(True)\n\n        self.resize(self.width(), window_height)\n        self.move(window_pos.x(), window_pos_y)\n\n    def _on_shrunk_anim_finish(self):\n        self._top_content_widget.setVisible(not self._shrunken)\n        self._top_content_widget.setMinimumHeight(0)\n        self._top_content_widget.setMaximumHeight(\n            self._top_widget_max_height\n        )\n        self._top_content_widget.setSizePolicy(self._top_widget_size_policy)\n\n        if self._shrunken:\n            self._shrunk_main_label.setText(self._main_label.text())\n        else:\n            self._shrunk_main_label.setText(\"\")\n\n        if self._shrunken:\n            content_frame_hint = self._content_frame.sizeHint()\n\n            layout = self.layout()\n            margins = layout.contentsMargins()\n            window_height = (\n                content_frame_hint.height()\n                + margins.bottom()\n                + margins.top()\n            )\n            diff = self.height() - window_height\n            window_pos = self.pos()\n            window_pos_y = window_pos.y() + diff\n            self.resize(self.width(), window_height)\n            self.move(window_pos.x(), window_pos_y)\n\n    def _set_main_label(self, message):\n        self._main_label.setText(message)\n        if self._shrunken:\n            self._shrunk_main_label.setText(message)\n\n    def _on_publish_reset(self):\n        self._last_instance_label = None\n        self._last_plugin_label = None\n\n        self._set_success_property()\n        self._set_progress_visibility(True)\n\n        self._main_label.setText(\"\")\n        self._message_label_top.setText(\"\")\n\n        self._reset_btn.setEnabled(True)\n        self._stop_btn.setEnabled(False)\n        self._validate_btn.setEnabled(True)\n        self._publish_btn.setEnabled(True)\n\n        self._progress_bar.setValue(\n            self._controller.get_publish_progress()\n        )\n        self._progress_bar.setMaximum(\n            self._controller.get_publish_max_progress()\n        )\n\n    def _on_publish_start(self):\n        if self._last_plugin_label:\n            self._plugin_label.setText(self._last_plugin_label)\n\n        if self._last_instance_label:\n            self._instance_label.setText(self._last_instance_label)\n\n        self._set_success_property(3)\n        self._set_progress_visibility(True)\n        self._set_main_label(\"Publishing...\")\n        self._message_label_top.setText(\"\")\n\n        self._reset_btn.setEnabled(False)\n        self._stop_btn.setEnabled(True)\n        self._validate_btn.setEnabled(False)\n        self._publish_btn.setEnabled(False)\n\n        self.set_shrunk_state(False)\n\n    def _on_publish_validated_change(self, event):\n        if event[\"value\"]:\n            self._validate_btn.setEnabled(False)\n\n    def _on_instance_change(self, event):\n        \"\"\"Change instance label when instance is going to be processed.\"\"\"\n\n        self._last_instance_label = event[\"instance_label\"]\n        self._instance_label.setText(event[\"instance_label\"])\n        QtWidgets.QApplication.processEvents()\n\n    def _on_plugin_change(self, event):\n        \"\"\"Change plugin label when instance is going to be processed.\"\"\"\n\n        self._last_plugin_label = event[\"plugin_label\"]\n        self._progress_bar.setValue(self._controller.get_publish_progress())\n        self._plugin_label.setText(event[\"plugin_label\"])\n        QtWidgets.QApplication.processEvents()\n\n    def _on_publish_stop(self):\n        self._progress_bar.setValue(self._controller.get_publish_progress())\n\n        self._reset_btn.setEnabled(True)\n        self._stop_btn.setEnabled(False)\n\n        self._instance_label.setText(\"\")\n        self._plugin_label.setText(\"\")\n\n        publish_enabled = self._controller.publish_can_continue()\n        validate_enabled = (\n            publish_enabled and not self._controller.publish_has_validated()\n        )\n        self._validate_btn.setEnabled(validate_enabled)\n        self._publish_btn.setEnabled(publish_enabled)\n\n        if self._controller.publish_has_crashed():\n            self._set_error_msg()\n\n        elif self._controller.publish_has_validation_errors():\n            self._set_progress_visibility(False)\n            self._set_validation_errors()\n\n        elif self._controller.publish_has_finished():\n            self._set_finished()\n\n        else:\n            self._set_stopped()\n\n    def _set_stopped(self):\n        main_label = \"Publish paused\"\n        if self._controller.publish_has_validated:\n            main_label += \" - Validation passed\"\n\n        self._set_main_label(main_label)\n        self._message_label_top.setText(\n            \"Hit publish (play button) to continue.\"\n        )\n\n        self._set_success_property(4)\n\n    def _set_error_msg(self):\n        \"\"\"Show error message to artist on publish crash.\"\"\"\n\n        self._set_main_label(\"Error happened\")\n        error_info = self._controller.get_publish_error_info()\n\n        error_message = \"Unknown error happened\"\n        if error_info is not None:\n            error_message = error_info.message\n\n        self._message_label_top.setText(error_message)\n\n        self._set_success_property(1)\n\n    def _set_validation_errors(self):\n        self._set_main_label(\"Your publish didn't pass studio validations\")\n        self._message_label_top.setText(\"Check results above please\")\n        self._set_success_property(2)\n\n    def _set_finished(self):\n        self._set_main_label(\"Finished\")\n        self._message_label_top.setText(\"\")\n        self._set_success_property(0)\n\n    def _set_progress_visibility(self, visible):\n        window_height = self.height()\n        self._progress_widget.setVisible(visible)\n        # Ignore rescaling and move of widget if is shrunken of progress bar\n        #   should be visible\n        if self._shrunken or visible:\n            return\n\n        height = self._progress_widget.height()\n        diff = height + self._top_content_layout.spacing()\n\n        window_pos = self.pos()\n        window_pos_y = self.pos().y() + diff\n        window_height -= diff\n\n        self.resize(self.width(), window_height)\n        self.move(window_pos.x(), window_pos_y)\n\n    def _set_success_property(self, state=None):\n        \"\"\"Apply styles by state.\n\n        State enum:\n        - None - Default state after restart\n        - 0 - Success finish\n        - 1 - Error happened\n        - 2 - Validation error\n        - 3 - In progress\n        - 4 - Stopped/Paused\n        \"\"\"\n\n        if state is None:\n            state = \"\"\n        else:\n            state = str(state)\n\n        for widget in (self._progress_bar, self._content_frame):\n            if widget.property(\"state\") != state:\n                widget.setProperty(\"state\", state)\n                widget.style().polish(widget)\n\n    def _on_report_triggered(self, identifier):\n        if identifier == \"export_report\":\n            self._controller.emit_event(\n                \"export_report.request\", {}, \"publish_frame\")\n\n        elif identifier == \"copy_report\":\n            self._controller.emit_event(\n                \"copy_report.request\", {}, \"publish_frame\")\n\n        elif identifier == \"go_to_report\":\n            self.details_page_requested.emit()\n\n    def _on_reset_clicked(self):\n        self._controller.reset()\n\n    def _on_stop_clicked(self):\n        self._controller.stop_publish()\n\n    def _on_validate_clicked(self):\n        self._controller.validate()\n\n    def _on_publish_clicked(self):\n        self._controller.publish()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/index.html#client.ayon_core.tools.publisher.widgets.ReportPageWidget","title":"<code>ReportPageWidget</code>","text":"<p>               Bases: <code>QFrame</code></p> <p>Widgets showing report for artis.</p> <p>There are 5 possible states: 1. Publishing did not start yet.         &gt; Only label. 2. Publishing is paused.                \u2510 3. Publishing successfully finished.    \u2502&gt; Instances with logs. 4. Publishing crashed.                  \u2518 5. Crashed because of publish error.     &gt; Errors with logs.</p> <p>This widget is shown if publish errors happened.</p> <p>Shows publish error titles with instances on which they happened and publish error detail with possible actions (repair).</p> Source code in <code>client/ayon_core/tools/publisher/widgets/report_page.py</code> <pre><code>class ReportPageWidget(QtWidgets.QFrame):\n    \"\"\"Widgets showing report for artis.\n\n    There are 5 possible states:\n    1. Publishing did not start yet.         &gt; Only label.\n    2. Publishing is paused.                \u2510\n    3. Publishing successfully finished.    \u2502&gt; Instances with logs.\n    4. Publishing crashed.                  \u2518\n    5. Crashed because of publish error.     &gt; Errors with logs.\n\n    This widget is shown if publish errors happened.\n\n    Shows publish error titles with instances on which they happened\n    and publish error detail with possible actions (repair).\n    \"\"\"\n\n    def __init__(\n        self, controller: AbstractPublisherFrontend, parent: QtWidgets.QWidget\n    ):\n        super().__init__(parent)\n\n        header_label = QtWidgets.QLabel(self)\n        header_label.setAlignment(QtCore.Qt.AlignCenter)\n        header_label.setObjectName(\"PublishReportHeader\")\n\n        publish_instances_widget = ReportsWidget(controller, self)\n\n        layout = QtWidgets.QVBoxLayout(self)\n        layout.setContentsMargins(0, 0, 0, 0)\n        layout.addWidget(header_label, 0)\n        layout.addWidget(publish_instances_widget, 0)\n\n        controller.register_event_callback(\n            \"publish.process.started\", self._on_publish_start\n        )\n        controller.register_event_callback(\n            \"publish.reset.finished\", self._on_publish_reset\n        )\n        controller.register_event_callback(\n            \"publish.process.stopped\", self._on_publish_stop\n        )\n\n        self._header_label = header_label\n        self._publish_instances_widget = publish_instances_widget\n\n        self._controller: AbstractPublisherFrontend = controller\n\n    def _update_label(self):\n        if not self._controller.publish_has_started():\n            # This probably never happen when this widget is visible\n            header_label = \"Nothing to report until you run publish\"\n        elif self._controller.publish_has_crashed():\n            header_label = \"Publish error report\"\n        elif self._controller.publish_has_validation_errors():\n            header_label = \"Publish validation report\"\n        elif self._controller.publish_has_finished():\n            header_label = \"Publish success report\"\n        else:\n            header_label = \"Publish report\"\n        self._header_label.setText(header_label)\n\n    def _update_state(self):\n        self._update_label()\n        publish_started = self._controller.publish_has_started()\n        self._publish_instances_widget.setVisible(publish_started)\n        if publish_started:\n            self._publish_instances_widget.update_data()\n\n        self.updateGeometry()\n\n    def _on_publish_start(self):\n        self._update_state()\n\n    def _on_publish_reset(self):\n        self._update_state()\n\n    def _on_publish_stop(self):\n        self._update_state()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/index.html#client.ayon_core.tools.publisher.widgets.ResetBtn","title":"<code>ResetBtn</code>","text":"<p>               Bases: <code>PublishIconBtn</code></p> <p>Publish reset button.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/widgets.py</code> <pre><code>class ResetBtn(PublishIconBtn):\n    \"\"\"Publish reset button.\"\"\"\n    def __init__(self, parent=None):\n        icon_path = get_icon_path(\"refresh\")\n        super().__init__(icon_path, parent)\n        self.setToolTip(\n            \"Reset &amp; discard changes ({})\".format(ResetKeySequence.toString())\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/index.html#client.ayon_core.tools.publisher.widgets.SaveBtn","title":"<code>SaveBtn</code>","text":"<p>               Bases: <code>PublishIconBtn</code></p> <p>Save context and instances information.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/widgets.py</code> <pre><code>class SaveBtn(PublishIconBtn):\n    \"\"\"Save context and instances information.\"\"\"\n    def __init__(self, parent=None):\n        icon_path = get_icon_path(\"save\")\n        super().__init__(icon_path, parent)\n        self.setToolTip(\n            \"Save changes ({})\".format(\n                QtGui.QKeySequence(QtGui.QKeySequence.Save).toString()\n            )\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/index.html#client.ayon_core.tools.publisher.widgets.StopBtn","title":"<code>StopBtn</code>","text":"<p>               Bases: <code>PublishIconBtn</code></p> <p>Publish stop button.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/widgets.py</code> <pre><code>class StopBtn(PublishIconBtn):\n    \"\"\"Publish stop button.\"\"\"\n    def __init__(self, parent):\n        icon_path = get_icon_path(\"stop\")\n        super().__init__(icon_path, parent)\n        self.setToolTip(\"Stop/Pause publishing\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/index.html#client.ayon_core.tools.publisher.widgets.ValidateBtn","title":"<code>ValidateBtn</code>","text":"<p>               Bases: <code>PublishIconBtn</code></p> <p>Publish validate button.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/widgets.py</code> <pre><code>class ValidateBtn(PublishIconBtn):\n    \"\"\"Publish validate button.\"\"\"\n    def __init__(self, parent=None):\n        icon_path = get_icon_path(\"validate\")\n        super().__init__(icon_path, parent)\n        self.setToolTip(\"Validate\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/index.html#client.ayon_core.tools.publisher.widgets.get_icon","title":"<code>get_icon(icon_name=None, filename=None)</code>","text":"<p>Load image from './images' as QICon.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/icons.py</code> <pre><code>def get_icon(icon_name=None, filename=None):\n    \"\"\"Load image from './images' as QICon.\"\"\"\n    pix = get_pixmap(icon_name, filename)\n    if pix:\n        return QtGui.QIcon(pix)\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/index.html#client.ayon_core.tools.publisher.widgets.get_icon_path","title":"<code>get_icon_path(icon_name=None, filename=None)</code>","text":"<p>Path to image in './images' folder.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/icons.py</code> <pre><code>def get_icon_path(icon_name=None, filename=None):\n    \"\"\"Path to image in './images' folder.\"\"\"\n    if icon_name is None and filename is None:\n        return None\n\n    if filename is None:\n        filename = \"{}.png\".format(icon_name)\n\n    path = os.path.join(\n        os.path.dirname(os.path.abspath(__file__)),\n        \"images\",\n        filename\n    )\n    if os.path.exists(path):\n        return path\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/index.html#client.ayon_core.tools.publisher.widgets.get_pixmap","title":"<code>get_pixmap(icon_name=None, filename=None)</code>","text":"<p>Load image from './images' as QPixmap.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/icons.py</code> <pre><code>def get_pixmap(icon_name=None, filename=None):\n    \"\"\"Load image from './images' as QPixmap.\"\"\"\n    path = get_icon_path(icon_name, filename)\n    if path:\n        return QtGui.QPixmap(path)\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/border_label_widget.html","title":"border_label_widget","text":""},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/border_label_widget.html#client.ayon_core.tools.publisher.widgets.border_label_widget.BorderedLabelWidget","title":"<code>BorderedLabelWidget</code>","text":"<p>               Bases: <code>QFrame</code></p> <p>Draws borders around widget with label in the middle of top.</p> <p>\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500 Label \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510 \u2502                      \u2502 \u2502                      \u2502 \u2502       CONTENT        \u2502 \u2502                      \u2502 \u2502                      \u2502 \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518</p> Source code in <code>client/ayon_core/tools/publisher/widgets/border_label_widget.py</code> <pre><code>class BorderedLabelWidget(QtWidgets.QFrame):\n    \"\"\"Draws borders around widget with label in the middle of top.\n\n    \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500 Label \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n    \u2502                      \u2502\n    \u2502                      \u2502\n    \u2502       CONTENT        \u2502\n    \u2502                      \u2502\n    \u2502                      \u2502\n    \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n    \"\"\"\n    def __init__(self, label, parent):\n        super().__init__(parent)\n        color_value = get_objected_colors(\"border\")\n        color = None\n        if color_value:\n            color = color_value.get_qcolor()\n\n        line_size = 1\n\n        top_left_w = _HTopCornerLineWidget(color, line_size, True, self)\n        top_right_w = _HTopCornerLineWidget(color, line_size, False, self)\n\n        label_widget = QtWidgets.QLabel(label, self)\n\n        top_layout = QtWidgets.QHBoxLayout()\n        top_layout.setContentsMargins(0, 0, 0, 0)\n        top_layout.setSpacing(5)\n        top_layout.addWidget(top_left_w, 1)\n        top_layout.addWidget(label_widget, 0)\n        top_layout.addWidget(top_right_w, 1)\n\n        left_w = _VLineWidget(color, line_size, True, self)\n        right_w = _VLineWidget(color, line_size, False, self)\n\n        bottom_w = _HBottomLineWidget(color, line_size, self)\n\n        center_layout = QtWidgets.QHBoxLayout()\n        center_layout.setContentsMargins(5, 5, 5, 5)\n\n        layout = QtWidgets.QGridLayout(self)\n        layout.setContentsMargins(0, 0, 0, 0)\n        layout.setSpacing(0)\n\n        layout.addLayout(top_layout, 0, 0, 1, 3)\n\n        layout.addWidget(left_w, 1, 0)\n        layout.addLayout(center_layout, 1, 1)\n        layout.addWidget(right_w, 1, 2)\n\n        layout.addWidget(bottom_w, 2, 0, 1, 3)\n\n        layout.setColumnStretch(1, 1)\n        layout.setRowStretch(1, 1)\n\n        self._widget = None\n\n        self._radius = 0\n        self._line_size = line_size\n\n        self._top_left_w = top_left_w\n        self._top_right_w = top_right_w\n        self._left_w = left_w\n        self._right_w = right_w\n        self._bottom_w = bottom_w\n        self._label_widget = label_widget\n        self._center_layout = center_layout\n\n    def set_content_margins(self, value):\n        \"\"\"Set margins around content.\"\"\"\n        self._center_layout.setContentsMargins(\n            value, value, value, value\n        )\n\n    def set_line_size(self, line_size):\n        if self._line_size == line_size:\n            return\n        self._line_size = line_size\n        for widget in (\n            self._top_left_w,\n            self._top_right_w,\n            self._left_w,\n            self._right_w,\n            self._bottom_w\n        ):\n            widget.set_line_size(line_size)\n        self._recalculate_sizes()\n\n    def showEvent(self, event):\n        super().showEvent(event)\n        self._recalculate_sizes()\n\n    def _recalculate_sizes(self):\n        height = self._label_widget.height()\n        radius = int((height + (height % 2)) / 2)\n        self._radius = radius\n\n        radius_size = self._line_size + 1\n        if radius_size &lt; radius:\n            radius_size = radius\n\n        if radius:\n            side_width = self._line_size + radius\n        else:\n            side_width = self._line_size + 1\n\n        # Don't use fixed width/height as that would set also set\n        #   the other size (When fixed width is set then is also set\n        #   fixed height).\n        self._left_w.setMinimumWidth(side_width)\n        self._left_w.setMaximumWidth(side_width)\n        self._right_w.setMinimumWidth(side_width)\n        self._right_w.setMaximumWidth(side_width)\n        self._bottom_w.setMinimumHeight(radius_size)\n        self._bottom_w.setMaximumHeight(radius_size)\n        self._bottom_w.set_radius(radius)\n        self._top_right_w.set_radius(radius)\n        self._top_left_w.set_radius(radius)\n        if self._widget:\n            self._widget.update()\n\n    def set_center_widget(self, widget):\n        \"\"\"Set content widget and add it to center.\"\"\"\n        while self._center_layout.count():\n            item = self._center_layout.takeAt(0)\n            widget = item.widget()\n            if widget:\n                widget.deleteLater()\n\n        self._widget = widget\n        if isinstance(widget, QtWidgets.QLayout):\n            self._center_layout.addLayout(widget)\n        else:\n            self._center_layout.addWidget(widget)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/border_label_widget.html#client.ayon_core.tools.publisher.widgets.border_label_widget.BorderedLabelWidget.set_center_widget","title":"<code>set_center_widget(widget)</code>","text":"<p>Set content widget and add it to center.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/border_label_widget.py</code> <pre><code>def set_center_widget(self, widget):\n    \"\"\"Set content widget and add it to center.\"\"\"\n    while self._center_layout.count():\n        item = self._center_layout.takeAt(0)\n        widget = item.widget()\n        if widget:\n            widget.deleteLater()\n\n    self._widget = widget\n    if isinstance(widget, QtWidgets.QLayout):\n        self._center_layout.addLayout(widget)\n    else:\n        self._center_layout.addWidget(widget)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/border_label_widget.html#client.ayon_core.tools.publisher.widgets.border_label_widget.BorderedLabelWidget.set_content_margins","title":"<code>set_content_margins(value)</code>","text":"<p>Set margins around content.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/border_label_widget.py</code> <pre><code>def set_content_margins(self, value):\n    \"\"\"Set margins around content.\"\"\"\n    self._center_layout.setContentsMargins(\n        value, value, value, value\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/card_view_widgets.html","title":"card_view_widgets","text":"<p>Card view instance with more information about each instance.</p> <p>Instances are grouped under groups. Groups are defined by <code>creator_label</code> attribute on instance (Group defined by creator).</p> <p>Only one item can be selected at a time.</p> <pre><code>&lt;i&gt; : Icon. Can have Warning icon when context is not right\n\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n\u2502  Context             \u2502\n\u2502 &lt;Group 1&gt; \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500 \u2502\n\u2502 &lt;i&gt; &lt;Instance 1&gt;  [x]\u2502\n\u2502 &lt;i&gt; &lt;Instance 2&gt;  [x]\u2502\n\u2502 &lt;Group 2&gt; \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500 \u2502\n\u2502 &lt;i&gt; &lt;Instance 3&gt;  [x]\u2502\n\u2502 ...                  \u2502\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/card_view_widgets.html#client.ayon_core.tools.publisher.widgets.card_view_widgets.BaseGroupWidget","title":"<code>BaseGroupWidget</code>","text":"<p>               Bases: <code>QWidget</code></p> Source code in <code>client/ayon_core/tools/publisher/widgets/card_view_widgets.py</code> <pre><code>class BaseGroupWidget(QtWidgets.QWidget):\n    selected = QtCore.Signal(str, str, str)\n    removed_selected = QtCore.Signal()\n    double_clicked = QtCore.Signal()\n\n    def __init__(self, group_name, parent):\n        super().__init__(parent)\n\n        label_widget = QtWidgets.QLabel(group_name, self)\n\n        line_widget = QtWidgets.QWidget(self)\n        line_widget.setObjectName(\"Separator\")\n        line_widget.setMinimumHeight(2)\n        line_widget.setMaximumHeight(2)\n\n        label_layout = QtWidgets.QHBoxLayout()\n        label_layout.setAlignment(QtCore.Qt.AlignVCenter)\n        label_layout.setSpacing(10)\n        label_layout.setContentsMargins(0, 0, 0, 0)\n        label_layout.addWidget(label_widget, 0)\n        label_layout.addWidget(line_widget, 1)\n\n        layout = QtWidgets.QVBoxLayout(self)\n        layout.setContentsMargins(0, 0, 0, 0)\n        layout.addLayout(label_layout, 0)\n\n        self._group = group_name\n\n        self._widgets_by_id = {}\n\n        self._label_widget = label_widget\n        self._content_layout = layout\n\n    @property\n    def group_name(self):\n        \"\"\"Group which widget represent.\n\n        Returns:\n            str: Name of group.\n        \"\"\"\n\n        return self._group\n\n    def set_widgets(\n        self,\n        widgets_by_id: dict[str, QtWidgets.QWidget],\n        ordered_ids: list[str],\n    ) -&gt; None:\n        self._remove_all_except(set(self._widgets_by_id))\n        idx = 1\n        for item_id in ordered_ids:\n            widget = widgets_by_id[item_id]\n            self._content_layout.insertWidget(idx, widget)\n            self._widgets_by_id[item_id] = widget\n            idx += 1\n\n    def take_widgets(self, widget_ids: set[str]):\n        for widget_id in widget_ids:\n            widget = self._widgets_by_id.pop(widget_id)\n            index = self._content_layout.indexOf(widget)\n            if index &gt;= 0:\n                self._content_layout.takeAt(index)\n\n    def _remove_all_except(self, item_ids):\n        item_ids = set(item_ids)\n        # Remove instance widgets that are not in passed instances\n        for item_id in tuple(self._widgets_by_id.keys()):\n            if item_id in item_ids:\n                continue\n\n            widget = self._widgets_by_id.pop(item_id)\n            if widget.is_selected:\n                self.removed_selected.emit()\n\n            widget.setVisible(False)\n            self._content_layout.removeWidget(widget)\n            widget.deleteLater()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/card_view_widgets.html#client.ayon_core.tools.publisher.widgets.card_view_widgets.BaseGroupWidget.group_name","title":"<code>group_name</code>  <code>property</code>","text":"<p>Group which widget represent.</p> <p>Returns:</p> Name Type Description <code>str</code> <p>Name of group.</p>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/card_view_widgets.html#client.ayon_core.tools.publisher.widgets.card_view_widgets.CardWidget","title":"<code>CardWidget</code>","text":"<p>               Bases: <code>BaseClickableFrame</code></p> <p>Clickable card used as bigger button.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/card_view_widgets.py</code> <pre><code>class CardWidget(BaseClickableFrame):\n    \"\"\"Clickable card used as bigger button.\"\"\"\n\n    selected = QtCore.Signal(str, str, str)\n    # Group identifier of card\n    # - this must be set because if send when mouse is released with card id\n    _group_identifier = None\n    double_clicked = QtCore.Signal()\n\n    def __init__(self, parent):\n        super().__init__(parent)\n        self.setObjectName(\"CardViewWidget\")\n\n        self._selected = False\n        self._id = None\n\n    def mouseDoubleClickEvent(self, event):\n        super().mouseDoubleClickEvent(event)\n        if self._is_valid_double_click(event):\n            self.double_clicked.emit()\n\n    @property\n    def id(self):\n        \"\"\"Id of card.\"\"\"\n\n        return self._id\n\n    @property\n    def is_selected(self):\n        \"\"\"Is card selected.\"\"\"\n        return self._selected\n\n    def set_selected(self, selected):\n        \"\"\"Set card as selected.\"\"\"\n        if selected is self._selected:\n            return\n\n        self._selected = selected\n        state = \"selected\" if selected else \"\"\n        self.setProperty(\"state\", state)\n        self.style().polish(self)\n\n    def _mouse_release_callback(self):\n        \"\"\"Trigger selected signal.\"\"\"\n\n        modifiers = QtWidgets.QApplication.keyboardModifiers()\n        selection_type = SelectionTypes.clear\n        if bool(modifiers &amp; QtCore.Qt.ShiftModifier):\n            selection_type = SelectionTypes.extend_to\n\n        elif bool(modifiers &amp; QtCore.Qt.ControlModifier):\n            selection_type = SelectionTypes.extend\n\n        self.selected.emit(self._id, self._group_identifier, selection_type)\n\n    def _is_valid_double_click(self, event):\n        return True\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/card_view_widgets.html#client.ayon_core.tools.publisher.widgets.card_view_widgets.CardWidget.id","title":"<code>id</code>  <code>property</code>","text":"<p>Id of card.</p>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/card_view_widgets.html#client.ayon_core.tools.publisher.widgets.card_view_widgets.CardWidget.is_selected","title":"<code>is_selected</code>  <code>property</code>","text":"<p>Is card selected.</p>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/card_view_widgets.html#client.ayon_core.tools.publisher.widgets.card_view_widgets.CardWidget.set_selected","title":"<code>set_selected(selected)</code>","text":"<p>Set card as selected.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/card_view_widgets.py</code> <pre><code>def set_selected(self, selected):\n    \"\"\"Set card as selected.\"\"\"\n    if selected is self._selected:\n        return\n\n    self._selected = selected\n    state = \"selected\" if selected else \"\"\n    self.setProperty(\"state\", state)\n    self.style().polish(self)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/card_view_widgets.html#client.ayon_core.tools.publisher.widgets.card_view_widgets.ContextCardWidget","title":"<code>ContextCardWidget</code>","text":"<p>               Bases: <code>CardWidget</code></p> <p>Card for global context.</p> <p>Is not visually under group widget and is always at the top of card view.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/card_view_widgets.py</code> <pre><code>class ContextCardWidget(CardWidget):\n    \"\"\"Card for global context.\n\n    Is not visually under group widget and is always at the top of card view.\n    \"\"\"\n\n    def __init__(self, parent: QtWidgets.QWidget):\n        super().__init__(parent)\n\n        self._id = CONTEXT_ID\n        self._group_identifier = CONTEXT_GROUP\n\n        icon_widget = PublishPixmapLabel(None, self)\n        icon_widget.setObjectName(\"ProductTypeIconLabel\")\n\n        label_widget = QtWidgets.QLabel(f\"&lt;span&gt;{CONTEXT_LABEL}&lt;/span&gt;\", self)\n        # HTML text will cause that label start catch mouse clicks\n        # - disabling with changing interaction flag\n        label_widget.setTextInteractionFlags(\n            QtCore.Qt.NoTextInteraction\n        )\n\n        icon_layout = QtWidgets.QHBoxLayout()\n        icon_layout.setContentsMargins(5, 5, 5, 5)\n        icon_layout.addWidget(icon_widget)\n\n        layout = QtWidgets.QHBoxLayout(self)\n        layout.setContentsMargins(0, 2, 10, 2)\n        layout.addLayout(icon_layout, 0)\n        layout.addWidget(label_widget, 1)\n\n        self._icon_widget = icon_widget\n        self._label_widget = label_widget\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/card_view_widgets.html#client.ayon_core.tools.publisher.widgets.card_view_widgets.ConvertorItemCardWidget","title":"<code>ConvertorItemCardWidget</code>","text":"<p>               Bases: <code>CardWidget</code></p> <p>Card for global context.</p> <p>Is not visually under group widget and is always at the top of card view.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/card_view_widgets.py</code> <pre><code>class ConvertorItemCardWidget(CardWidget):\n    \"\"\"Card for global context.\n\n    Is not visually under group widget and is always at the top of card view.\n    \"\"\"\n\n    def __init__(self, item, parent):\n        super().__init__(parent)\n\n        self._id = item.id\n        self.identifier = item.identifier\n        self._group_identifier = CONVERTOR_ITEM_GROUP\n\n        icon_widget = IconValuePixmapLabel(\"fa.magic\", self)\n        icon_widget.setObjectName(\"ProductTypeIconLabel\")\n\n        label_widget = QtWidgets.QLabel(item.label, self)\n\n        icon_layout = QtWidgets.QHBoxLayout()\n        icon_layout.setContentsMargins(10, 5, 5, 5)\n        icon_layout.addWidget(icon_widget)\n\n        layout = QtWidgets.QHBoxLayout(self)\n        layout.setContentsMargins(0, 2, 10, 2)\n        layout.addLayout(icon_layout, 0)\n        layout.addWidget(label_widget, 1)\n\n        self._icon_widget = icon_widget\n        self._label_widget = label_widget\n\n    def update_item(self, item):\n        self._id = item.id\n        self.identifier = item.identifier\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/card_view_widgets.html#client.ayon_core.tools.publisher.widgets.card_view_widgets.InstanceCardView","title":"<code>InstanceCardView</code>","text":"<p>               Bases: <code>AbstractInstanceView</code></p> <p>Publish access to card view.</p> <p>Wrapper of all widgets in card view.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/card_view_widgets.py</code> <pre><code>class InstanceCardView(AbstractInstanceView):\n    \"\"\"Publish access to card view.\n\n    Wrapper of all widgets in card view.\n    \"\"\"\n\n    double_clicked = QtCore.Signal()\n\n    def __init__(self, controller, parent):\n        super().__init__(parent)\n\n        self._controller: AbstractPublisherFrontend = controller\n\n        scroll_area = QtWidgets.QScrollArea(self)\n        scroll_area.setWidgetResizable(True)\n        scroll_area.setHorizontalScrollBarPolicy(QtCore.Qt.ScrollBarAlwaysOff)\n        scroll_area.setVerticalScrollBarPolicy(QtCore.Qt.ScrollBarAsNeeded)\n        scrollbar_bg = scroll_area.verticalScrollBar().parent()\n        if scrollbar_bg:\n            scrollbar_bg.setAttribute(QtCore.Qt.WA_TranslucentBackground)\n        scroll_area.setViewportMargins(0, 0, 0, 0)\n\n        content_widget = QtWidgets.QWidget(scroll_area)\n\n        scroll_area.setWidget(content_widget)\n\n        content_layout = QtWidgets.QVBoxLayout(content_widget)\n        content_layout.setContentsMargins(0, 0, 0, 0)\n        content_layout.addStretch(1)\n\n        layout = QtWidgets.QVBoxLayout(self)\n        layout.setContentsMargins(0, 0, 0, 0)\n        layout.addWidget(scroll_area)\n\n        self._scroll_area = scroll_area\n        self._content_layout = content_layout\n        self._content_widget = content_widget\n\n        self._active_toggle_enabled: bool = True\n        self._convertors_group: Optional[BaseGroupWidget] = None\n        self._convertor_widgets_by_id: dict[str, ConvertorItemCardWidget] = {}\n        self._convertor_ids: list[str] = []\n\n        self._group_name_by_instance_id: dict[str, str] = {}\n        self._instance_ids_by_group_name: dict[str, list[str]] = (\n            collections.defaultdict(list)\n        )\n        self._ordered_groups = []\n        self._context_widget: Optional[ContextCardWidget] = None\n        self._widgets_by_id: dict[str, InstanceCardWidget] = {}\n        self._widgets_by_group: dict[str, BaseGroupWidget] = {}\n\n        self._parent_id_by_id = {}\n        self._instance_ids_by_parent_id = collections.defaultdict(set)\n\n        self._explicitly_selected_instance_ids = []\n        self._explicitly_selected_groups = []\n\n        self.setSizePolicy(\n            QtWidgets.QSizePolicy.Minimum,\n            self.sizePolicy().verticalPolicy()\n        )\n\n    def sizeHint(self):\n        \"\"\"Modify sizeHint based on visibility of scroll bars.\"\"\"\n        # Calculate width hint by content widget and vertical scroll bar\n        scroll_bar = self._scroll_area.verticalScrollBar()\n        width = (\n            self._content_widget.sizeHint().width()\n            + scroll_bar.sizeHint().width()\n        )\n\n        result = super().sizeHint()\n        result.setWidth(width)\n        return result\n\n    def get_current_instance_count(self) -&gt; int:\n        \"\"\"How many instances are currently in the view.\"\"\"\n        return len(self._widgets_by_id)\n\n    def _get_affected_ids(self, instance_ids: set[str]) -&gt; set[str]:\n        affected_ids = set()\n        affected_queue = collections.deque()\n        affected_queue.extend(instance_ids)\n        while affected_queue:\n            instance_id = affected_queue.popleft()\n            if instance_id in affected_ids:\n                continue\n            affected_ids.add(instance_id)\n            parent_id = instance_id\n            while True:\n                parent_id = self._parent_id_by_id[parent_id]\n                if parent_id is None:\n                    break\n                affected_ids.add(parent_id)\n\n            child_ids = set(self._instance_ids_by_parent_id[instance_id])\n            affected_queue.extend(child_ids - affected_ids)\n        return affected_ids\n\n    def _toggle_instances(\n        self,\n        new_value: Optional[bool],\n        active_id: Optional[str] = None,\n    ) -&gt; None:\n        instance_ids = {\n            widget.id\n            for widget in self._get_selected_instance_widgets()\n            if widget.is_selected\n        }\n        active_by_id = {}\n        if active_id and active_id not in instance_ids:\n            instance_ids = {active_id}\n\n        ids_to_toggle = set(instance_ids)\n\n        affected_ids = self._get_affected_ids(instance_ids)\n\n        _queue = collections.deque()\n        _queue.append((set(self._instance_ids_by_parent_id[None]), True))\n        discarted_ids = set()\n        while _queue:\n            if not instance_ids:\n                break\n\n            chilren_ids, is_parent_active = _queue.pop()\n            for instance_id in chilren_ids:\n                if instance_id not in affected_ids:\n                    continue\n\n                widget = self._widgets_by_id[instance_id]\n                if is_parent_active is not widget.is_parent_active():\n                    widget.set_parent_active(is_parent_active)\n\n                instance_ids.discard(instance_id)\n                if instance_id in ids_to_toggle:\n                    discarted_ids.add(instance_id)\n                    old_value = widget.is_active()\n                    value = new_value\n                    if value is None:\n                        value = not old_value\n\n                    widget.set_active(value)\n                    if widget.is_parent_active():\n                        active_by_id[instance_id] = widget.is_active()\n\n                children_ids = self._instance_ids_by_parent_id[instance_id]\n                children = {\n                    child_id\n                    for child_id in children_ids\n                    if child_id not in discarted_ids\n                }\n\n                if children:\n                    instance_ids |= children\n                    _queue.append((children, widget.is_active()))\n\n                if not instance_ids:\n                    break\n\n        if active_by_id:\n            self._controller.set_instances_active_state(active_by_id)\n\n    def keyPressEvent(self, event):\n        if event.key() == QtCore.Qt.Key_Space:\n            self._toggle_instances(None)\n            return True\n\n        elif event.key() == QtCore.Qt.Key_Backspace:\n            self._toggle_instances(False)\n            return True\n\n        elif event.key() == QtCore.Qt.Key_Return:\n            self._toggle_instances(True)\n            return True\n\n        return super().keyPressEvent(event)\n\n    def _get_selected_widgets(self):\n        output = []\n        if (\n            self._context_widget is not None\n            and self._context_widget.is_selected\n        ):\n            output.append(self._context_widget)\n\n        output.extend(self._get_selected_convertor_widgets())\n        output.extend(self._get_selected_instance_widgets())\n        return output\n\n    def _get_selected_instance_widgets(self) -&gt; list[InstanceCardWidget]:\n        return [\n            widget\n            for widget in self._widgets_by_id.values()\n            if widget.is_selected\n        ]\n\n    def _get_selected_convertor_widgets(self) -&gt; list[ConvertorItemCardWidget]:\n        return [\n            widget\n            for widget in self._convertor_widgets_by_id.values()\n            if widget.is_selected\n        ]\n\n    def _get_selected_item_ids(self):\n        output = []\n        if (\n            self._context_widget is not None\n            and self._context_widget.is_selected\n        ):\n            output.append(CONTEXT_ID)\n\n        output.extend(\n            conv_id\n            for conv_id, widget in self._widgets_by_id.items()\n            if widget.is_selected\n        )\n\n        output.extend(\n            widget.id\n            for instance_id, widget in self._widgets_by_id.items()\n            if widget.is_selected\n        )\n        return output\n\n    def refresh(self):\n        \"\"\"Refresh instances in view based on CreatedContext.\"\"\"\n        self._make_sure_context_widget_exists()\n\n        self._update_convertors_group()\n        context_info_by_id = self._controller.get_instances_context_info()\n\n        # Prepare instances by group and identifiers by group\n        instances_by_group = collections.defaultdict(list)\n        identifiers_by_group = collections.defaultdict(set)\n        identifiers: set[str] = set()\n        instances_by_id = {}\n        parent_id_by_id = {}\n        instance_ids_by_parent_id = collections.defaultdict(set)\n        instance_items = self._controller.get_instance_items()\n        for instance in instance_items:\n            group_name = instance.group_label\n            instances_by_group[group_name].append(instance)\n            identifiers_by_group[group_name].add(\n                instance.creator_identifier\n            )\n            identifiers.add(instance.creator_identifier)\n            instances_by_id[instance.id] = instance\n            instance_ids_by_parent_id[instance.parent_instance_id].add(\n                instance.id\n            )\n            parent_id_by_id[instance.id] = instance.parent_instance_id\n\n        parent_active_by_id = {\n            instance_id: False\n            for instance_id in instances_by_id\n        }\n        _queue = collections.deque()\n        _queue.append((None, True))\n        while _queue:\n            parent_id, is_parent_active = _queue.popleft()\n            for instance_id in instance_ids_by_parent_id[parent_id]:\n                instance_item = instances_by_id[instance_id]\n                is_active = instance_item.is_active\n                if (\n                    not is_parent_active\n                    and instance_item.parent_flags &amp; ParentFlags.share_active\n                ):\n                    is_active = False\n\n                parent_active_by_id[instance_id] = is_parent_active\n                _queue.append(\n                    (instance_id, is_active)\n                )\n\n        # Remove groups that were not found in passed instances\n        groups_to_remove = (\n            set(self._widgets_by_group) - set(instances_by_group)\n        )\n        ids_to_remove = (\n            set(self._widgets_by_id) - set(instances_by_id)\n        )\n\n        # Sort groups\n        sorted_group_names = list(sorted(instances_by_group.keys()))\n\n        # Keep track of widget indexes\n        # - we start with 1 because Context item as at the top\n        widget_idx = 1\n        if self._convertors_group is not None:\n            widget_idx += 1\n\n        group_by_instance_id = {}\n        instance_ids_by_group_name = collections.defaultdict(list)\n        group_icons = {\n            identifier: self._controller.get_creator_icon(identifier)\n            for identifier in identifiers\n        }\n        for group_name in sorted_group_names:\n            if group_name not in self._widgets_by_group:\n                group_widget = BaseGroupWidget(\n                    group_name, self._content_widget\n                )\n                group_widget.double_clicked.connect(self.double_clicked)\n                self._content_layout.insertWidget(widget_idx, group_widget)\n                self._widgets_by_group[group_name] = group_widget\n\n            widget_idx += 1\n\n            instances = instances_by_group[group_name]\n            for instance in instances:\n                group_by_instance_id[instance.id] = group_name\n                instance_ids_by_group_name[group_name].append(instance.id)\n\n            self._update_instance_widgets(\n                group_name,\n                instances,\n                context_info_by_id,\n                parent_active_by_id,\n                group_icons,\n            )\n\n        # Remove empty groups\n        for group_name in groups_to_remove:\n            widget = self._widgets_by_group.pop(group_name)\n            widget.setVisible(False)\n            self._content_layout.removeWidget(widget)\n            widget.deleteLater()\n\n            if group_name in self._explicitly_selected_groups:\n                self._explicitly_selected_groups.remove(group_name)\n\n        for instance_id in ids_to_remove:\n            widget = self._widgets_by_id.pop(instance_id)\n            widget.setVisible(False)\n            widget.deleteLater()\n\n        sorted_group_names.insert(0, CONTEXT_GROUP)\n\n        self._parent_id_by_id = parent_id_by_id\n        self._instance_ids_by_parent_id = instance_ids_by_parent_id\n        self._group_name_by_instance_id = group_by_instance_id\n        self._instance_ids_by_group_name = instance_ids_by_group_name\n        self._ordered_groups = sorted_group_names\n\n    def has_items(self) -&gt; bool:\n        if self._convertors_group is not None:\n            return True\n        if self._widgets_by_id:\n            return True\n        return False\n\n    def _update_instance_widgets(\n        self,\n        group_name: str,\n        instances: list[InstanceItem],\n        context_info_by_id: dict[str, InstanceContextInfo],\n        parent_active_by_id: dict[str, bool],\n        group_icons: dict[str, str],\n    ) -&gt; None:\n        \"\"\"Update instances for the group.\n\n        Args:\n            instances (list[InstanceItem]): List of instances in\n                CreateContext.\n            context_info_by_id (dict[str, InstanceContextInfo]): Instance\n                context info by instance id.\n            parent_active_by_id (dict[str, bool]): Instance has active parent.\n\n        \"\"\"\n        # Store instances by id and by product name\n        group_widget: BaseGroupWidget = self._widgets_by_group[group_name]\n        instances_by_id = {}\n        instances_by_product_name = collections.defaultdict(list)\n        for instance in instances:\n            instances_by_id[instance.id] = instance\n            product_name = instance.product_name\n            instances_by_product_name[product_name].append(instance)\n\n        to_remove_ids = set(\n            self._instance_ids_by_group_name[group_name]\n        ) - set(instances_by_id)\n        group_widget.take_widgets(to_remove_ids)\n\n        # Sort instances by product name\n        sorted_product_names = list(sorted(instances_by_product_name.keys()))\n\n        # Add new instances to widget\n        ordered_ids = []\n        widgets_by_id = {}\n        for product_names in sorted_product_names:\n            for instance in instances_by_product_name[product_names]:\n                context_info = context_info_by_id[instance.id]\n                is_parent_active = parent_active_by_id[instance.id]\n                if instance.id in self._widgets_by_id:\n                    widget = self._widgets_by_id[instance.id]\n                    widget.update_instance(\n                        instance, context_info, is_parent_active\n                    )\n                else:\n                    group_icon = group_icons[instance.creator_identifier]\n                    widget = InstanceCardWidget(\n                        instance,\n                        context_info,\n                        is_parent_active,\n                        group_icon,\n                        group_widget,\n                    )\n                    widget.selected.connect(self._on_widget_selection)\n                    widget.active_changed.connect(self._on_active_changed)\n                    widget.double_clicked.connect(self.double_clicked)\n                    self._widgets_by_id[instance.id] = widget\n\n                ordered_ids.append(instance.id)\n                widgets_by_id[instance.id] = widget\n\n        group_widget.set_widgets(widgets_by_id, ordered_ids)\n\n    def _make_sure_context_widget_exists(self):\n        # Create context item if is not already existing\n        # - this must be as first thing to do as context item should be at the\n        #   top\n        if self._context_widget is not None:\n            return\n\n        widget = ContextCardWidget(self._content_widget)\n        widget.selected.connect(self._on_widget_selection)\n        widget.double_clicked.connect(self.double_clicked)\n\n        self._context_widget = widget\n\n        self.selection_changed.emit()\n        self._content_layout.insertWidget(0, widget)\n\n    def _update_convertors_group(self):\n        convertor_items = self._controller.get_convertor_items()\n        if not convertor_items and self._convertors_group is None:\n            return\n\n        ids_to_remove = set(self._convertor_widgets_by_id) - set(\n            convertor_items\n        )\n        if ids_to_remove:\n            self._convertors_group.take_widgets(ids_to_remove)\n\n        for conv_id in ids_to_remove:\n            widget = self._convertor_widgets_by_id.pop(conv_id)\n            widget.setVisible(False)\n            widget.deleteLater()\n\n        if not convertor_items:\n            self._convertors_group.setVisible(False)\n            self._content_layout.removeWidget(self._convertors_group)\n            self._convertors_group.deleteLater()\n            self._convertors_group = None\n            self._convertor_ids = []\n            self._convertor_widgets_by_id = {}\n            return\n\n        if self._convertors_group is None:\n            group_widget = BaseGroupWidget(\n                CONVERTOR_ITEM_GROUP, self._content_widget\n            )\n            self._content_layout.insertWidget(1, group_widget)\n            self._convertors_group = group_widget\n\n        # TODO create convertor widgets\n        items_by_label = collections.defaultdict(list)\n        for item in convertor_items.values():\n            items_by_label[item.label].append(item)\n\n        # Sort instances by product name\n        sorted_labels = list(sorted(items_by_label.keys()))\n\n        # Add new instances to widget\n        convertor_ids: list[str] = []\n        widgets_by_id: dict[str, ConvertorItemCardWidget] = {}\n        for label in sorted_labels:\n            for item in items_by_label[label]:\n                convertor_ids.append(item.id)\n                if item.id in self._convertor_widgets_by_id:\n                    widget = self._convertor_widgets_by_id[item.id]\n                    widget.update_item(item)\n                else:\n                    widget = ConvertorItemCardWidget(item, self)\n                    widget.selected.connect(self._on_widget_selection)\n                    widget.double_clicked.connect(self.double_clicked)\n                    self._convertor_widgets_by_id[item.id] = widget\n                widgets_by_id[item.id] = widget\n\n        self._convertors_group.set_widgets(widgets_by_id, convertor_ids)\n        self._convertor_ids = convertor_ids\n        self._convertor_widgets_by_id = widgets_by_id\n\n    def refresh_instance_states(self, instance_ids=None):\n        \"\"\"Trigger update of instances on group widgets.\"\"\"\n        if instance_ids is not None:\n            instance_ids = set(instance_ids)\n        context_info_by_id = self._controller.get_instances_context_info()\n        instance_items_by_id = self._controller.get_instance_items_by_id(\n            instance_ids\n        )\n        instance_ids: set[str] = set(instance_items_by_id)\n        available_ids: set[str] = set(instance_items_by_id)\n\n        affected_ids = self._get_affected_ids(instance_ids)\n\n        _queue = collections.deque()\n        _queue.append((set(self._instance_ids_by_parent_id[None]), True))\n        while _queue:\n            if not affected_ids:\n                break\n\n            chilren_ids, is_parent_active = _queue.pop()\n            for instance_id in chilren_ids:\n                if instance_id not in affected_ids:\n                    continue\n                affected_ids.discard(instance_id)\n                widget = self._widgets_by_id[instance_id]\n                if instance_id in instance_ids:\n                    instance_ids.discard(instance_id)\n                    if instance_id in available_ids:\n                        available_ids.discard(instance_id)\n                        widget.update_instance(\n                            instance_items_by_id[instance_id],\n                            context_info_by_id[instance_id],\n                            is_parent_active,\n                        )\n                    else:\n                        widget.set_parent_active(is_parent_active)\n\n                if not affected_ids:\n                    break\n\n                children = set(self._instance_ids_by_parent_id[instance_id])\n                if children:\n                    instance_ids |= children\n                    _queue.append((children, widget.is_active()))\n\n    def _on_active_changed(self, instance_id: str, value: bool) -&gt; None:\n        self._toggle_instances(value, instance_id)\n\n    def _on_widget_selection(self, instance_id, group_name, selection_type):\n        \"\"\"Select specific item by instance id.\n\n        Pass `CONTEXT_ID` as instance id and empty string as group to select\n        global context item.\n        \"\"\"\n        if instance_id == CONTEXT_ID:\n            new_widget = self._context_widget\n\n        else:\n            if group_name == CONVERTOR_ITEM_GROUP:\n                new_widget = self._convertor_widgets_by_id[instance_id]\n            else:\n                new_widget = self._widgets_by_id[instance_id]\n\n        if selection_type == SelectionTypes.clear:\n            self._select_item_clear(instance_id, group_name, new_widget)\n        elif selection_type == SelectionTypes.extend:\n            self._select_item_extend(instance_id, group_name, new_widget)\n        elif selection_type == SelectionTypes.extend_to:\n            self._select_item_extend_to(instance_id, group_name, new_widget)\n\n        self.selection_changed.emit()\n\n    def _select_item_clear(self, instance_id, group_name, new_widget):\n        \"\"\"Select specific item by instance id and clear previous selection.\n\n        Pass `CONTEXT_ID` as instance id and empty string as group to select\n        global context item.\n        \"\"\"\n\n        selected_widgets = self._get_selected_widgets()\n        for widget in selected_widgets:\n            if widget.id != instance_id:\n                widget.set_selected(False)\n\n        self._explicitly_selected_groups = [group_name]\n        self._explicitly_selected_instance_ids = [instance_id]\n\n        if new_widget is not None:\n            new_widget.set_selected(True)\n\n    def _select_item_extend(self, instance_id, group_name, new_widget):\n        \"\"\"Add/Remove single item to/from current selection.\n\n        If item is already selected the selection is removed.\n        \"\"\"\n\n        self._explicitly_selected_instance_ids = (\n            self._get_selected_item_ids()\n        )\n        if new_widget.is_selected:\n            self._explicitly_selected_instance_ids.remove(instance_id)\n            new_widget.set_selected(False)\n            remove_group = False\n            if instance_id == CONTEXT_ID:\n                remove_group = True\n            else:\n                has_selected_items = False\n                if group_name == CONVERTOR_ITEM_GROUP:\n                    for widget in self._convertor_widgets_by_id.values():\n                        if widget.is_selected:\n                            has_selected_items = True\n                            break\n                else:\n                    group_ids = self._instance_ids_by_group_name[group_name]\n                    for instance_id in group_ids:\n                        widget = self._widgets_by_id[instance_id]\n                        if widget.is_selected:\n                            has_selected_items = True\n                            break\n\n                if not has_selected_items:\n                    remove_group = True\n\n            if remove_group:\n                self._explicitly_selected_groups.remove(group_name)\n            return\n\n        self._explicitly_selected_instance_ids.append(instance_id)\n        if group_name in self._explicitly_selected_groups:\n            self._explicitly_selected_groups.remove(group_name)\n        self._explicitly_selected_groups.append(group_name)\n        new_widget.set_selected(True)\n\n    def _select_item_extend_to(self, instance_id, group_name, new_widget):\n        \"\"\"Extend selected items to specific instance id.\n\n        This method is handling Shift+click selection of widgets. Selection\n        is not stored to explicit selection items. That's because user can\n        shift select again and it should use last explicit selected item as\n        source item for selection.\n\n        Items selected via this function can get to explicit selection only if\n        selection is extended by one specific item ('_select_item_extend').\n        From that moment the selection is locked to new last explicit selected\n        item.\n\n        It's required to traverse through group widgets in their UI order and\n        through their instances in UI order. All explicitly selected items\n        must not change their selection state during this function. Passed\n        instance id can be above or under last selected item so a start item\n        and end item must be found to be able know which direction is selection\n        happening.\n        \"\"\"\n\n        # Start group name (in '_ordered_groups')\n        start_group = None\n        # End group name (in '_ordered_groups')\n        end_group = None\n        # Instance id of first selected item\n        start_instance_id = None\n        # Instance id of last selected item\n        end_instance_id = None\n\n        # Get previously selected group by explicit selected groups\n        previous_group = None\n        if self._explicitly_selected_groups:\n            previous_group = self._explicitly_selected_groups[-1]\n\n        # Find last explicitly selected instance id\n        previous_last_selected_id = None\n        if self._explicitly_selected_instance_ids:\n            previous_last_selected_id = (\n                self._explicitly_selected_instance_ids[-1]\n            )\n\n        # If last instance id was not found or available then last selected\n        #   group is also invalid.\n        # NOTE: This probably never happen?\n        if previous_last_selected_id is None:\n            previous_group = None\n\n        # Check if previously selected group is available and find out if\n        #   new instance group is above or under previous selection\n        # - based on these information are start/end group/instance filled\n        if previous_group in self._ordered_groups:\n            new_idx = self._ordered_groups.index(group_name)\n            prev_idx = self._ordered_groups.index(previous_group)\n            if new_idx &lt; prev_idx:\n                start_group = group_name\n                end_group = previous_group\n                start_instance_id = instance_id\n                end_instance_id = previous_last_selected_id\n            else:\n                start_group = previous_group\n                end_group = group_name\n                start_instance_id = previous_last_selected_id\n                end_instance_id = instance_id\n\n        # If start group is not set then use context item group name\n        if start_group is None:\n            start_group = CONTEXT_GROUP\n\n        # If start instance id is not filled then use context id (similar to\n        #   group)\n        if start_instance_id is None:\n            start_instance_id = CONTEXT_ID\n\n        # If end group is not defined then use passed group name\n        #   - this can be happen when previous group was not selected\n        #   - when this happens the selection will probably happen from context\n        #       item to item selected by user\n        if end_group is None:\n            end_group = group_name\n\n        # If end instance is not filled then use instance selected by user\n        if end_instance_id is None:\n            end_instance_id = instance_id\n\n        # Start and end group are the same\n        # - a different logic is needed in that case\n        same_group = start_group == end_group\n\n        # Process known information and change selection of items\n        passed_start_group = False\n        passed_end_group = False\n        # Go through ordered groups (from top to bottom) and change selection\n        for name in self._ordered_groups:\n            # Prepare sorted instance widgets\n            if name == CONTEXT_GROUP:\n                sorted_widgets = [self._context_widget]\n            else:\n                if name == CONVERTOR_ITEM_GROUP:\n                    sorted_widgets = [\n                        self._convertor_widgets_by_id[conv_id]\n                        for conv_id in self._convertor_ids\n                    ]\n                else:\n                    instance_ids = self._instance_ids_by_group_name[name]\n                    sorted_widgets = [\n                        self._widgets_by_id[instance_id]\n                        for instance_id in instance_ids\n                    ]\n\n            # Change selection based on explicit selection if start group\n            #   was not passed yet\n            if not passed_start_group:\n                if name != start_group:\n                    for widget in sorted_widgets:\n                        widget.set_selected(\n                            widget.id in self._explicitly_selected_instance_ids\n                        )\n                    continue\n\n            # Change selection based on explicit selection if end group\n            #   already passed\n            if passed_end_group:\n                for widget in sorted_widgets:\n                    widget.set_selected(\n                        widget.id in self._explicitly_selected_instance_ids\n                    )\n                continue\n\n            # Start group is already passed and end group was not yet hit\n            if same_group:\n                passed_start_group = True\n                passed_end_group = True\n                passed_start_instance = False\n                passed_end_instance = False\n                for widget in sorted_widgets:\n                    if not passed_start_instance:\n                        if widget.id in (start_instance_id, end_instance_id):\n                            if widget.id != start_instance_id:\n                                # Swap start/end instance if start instance is\n                                #   after end\n                                # - fix 'passed_end_instance' check\n                                start_instance_id, end_instance_id = (\n                                    end_instance_id, start_instance_id\n                                )\n                            passed_start_instance = True\n\n                    # Find out if widget should be selected\n                    select = False\n                    if passed_end_instance:\n                        select = False\n\n                    elif passed_start_instance:\n                        select = True\n\n                    # Check if instance is in explicitly selected items if\n                    #   should ont be selected\n                    if (\n                        not select\n                        and widget.id in self._explicitly_selected_instance_ids\n                    ):\n                        select = True\n\n                    widget.set_selected(select)\n\n                    if (\n                        not passed_end_instance\n                        and widget.id == end_instance_id\n                    ):\n                        passed_end_instance = True\n\n            elif name == start_group:\n                # First group from which selection should start\n                # - look for start instance first from which the selection\n                #   should happen\n                passed_start_group = True\n                passed_start_instance = False\n                for widget in sorted_widgets:\n                    if widget.id == start_instance_id:\n                        passed_start_instance = True\n\n                    select = False\n                    # Check if passed start instance or instance is\n                    #   in explicitly selected items to be selected\n                    if (\n                        passed_start_instance\n                        or widget.id in self._explicitly_selected_instance_ids\n                    ):\n                        select = True\n                    widget.set_selected(select)\n\n            elif name == end_group:\n                # Last group where selection should happen\n                # - look for end instance first after which the selection\n                #   should stop\n                passed_end_group = True\n                passed_end_instance = False\n                for widget in sorted_widgets:\n                    select = False\n                    # Check if not yet passed end instance or if instance is\n                    #   in explicitly selected items to be selected\n                    if (\n                        not passed_end_instance\n                        or widget.id in self._explicitly_selected_instance_ids\n                    ):\n                        select = True\n\n                    widget.set_selected(select)\n\n                    if widget.id == end_instance_id:\n                        passed_end_instance = True\n\n            else:\n                # Just select everything between start and end group\n                for widget in sorted_widgets:\n                    widget.set_selected(True)\n\n    def get_selected_items(self):\n        \"\"\"Get selected instance ids and context.\"\"\"\n\n        context_selected = (\n            self._context_widget is not None\n            and self._context_widget.is_selected\n        )\n        instances = [\n            widget.id\n            for widget in self._get_selected_instance_widgets()\n        ]\n        convertor_identifiers = [\n            widget.identifier\n            for widget in self._get_selected_convertor_widgets()\n        ]\n        return instances, context_selected, convertor_identifiers\n\n    def set_selected_items(\n        self, instance_ids, context_selected, convertor_identifiers\n    ):\n        s_instance_ids = set(instance_ids)\n        s_convertor_identifiers = set(convertor_identifiers)\n        cur_ids, cur_context, cur_convertor_identifiers = (\n            self.get_selected_items()\n        )\n        if (\n            set(cur_ids) == s_instance_ids\n            and cur_context == context_selected\n            and set(cur_convertor_identifiers) == s_convertor_identifiers\n        ):\n            return\n\n        selected_groups = []\n        selected_instances = []\n        if context_selected:\n            selected_groups.append(CONTEXT_GROUP)\n            selected_instances.append(CONTEXT_ID)\n\n        self._context_widget.set_selected(context_selected)\n\n        for group_name in self._ordered_groups:\n            if group_name == CONTEXT_GROUP:\n                continue\n\n            is_convertor_group = group_name == CONVERTOR_ITEM_GROUP\n            if is_convertor_group:\n                sorted_widgets = [\n                    self._convertor_widgets_by_id[conv_id]\n                    for conv_id in self._convertor_ids\n                ]\n            else:\n                instance_ids = self._instance_ids_by_group_name[group_name]\n                sorted_widgets = [\n                    self._widgets_by_id[instance_id]\n                    for instance_id in instance_ids\n                ]\n\n            group_selected = False\n            for widget in sorted_widgets:\n                select = False\n                if is_convertor_group:\n                    is_in = widget.identifier in s_convertor_identifiers\n                else:\n                    is_in = widget.id in s_instance_ids\n                if is_in:\n                    selected_instances.append(widget.id)\n                    group_selected = True\n                    select = True\n                widget.set_selected(select)\n\n            if group_selected:\n                selected_groups.append(group_name)\n\n        self._explicitly_selected_groups = selected_groups\n        self._explicitly_selected_instance_ids = selected_instances\n\n    def set_active_toggle_enabled(self, enabled):\n        if self._active_toggle_enabled is enabled:\n            return\n        self._active_toggle_enabled = enabled\n        for widget in self._widgets_by_id.values():\n            widget.set_active_toggle_enabled(enabled)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/card_view_widgets.html#client.ayon_core.tools.publisher.widgets.card_view_widgets.InstanceCardView.get_current_instance_count","title":"<code>get_current_instance_count()</code>","text":"<p>How many instances are currently in the view.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/card_view_widgets.py</code> <pre><code>def get_current_instance_count(self) -&gt; int:\n    \"\"\"How many instances are currently in the view.\"\"\"\n    return len(self._widgets_by_id)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/card_view_widgets.html#client.ayon_core.tools.publisher.widgets.card_view_widgets.InstanceCardView.get_selected_items","title":"<code>get_selected_items()</code>","text":"<p>Get selected instance ids and context.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/card_view_widgets.py</code> <pre><code>def get_selected_items(self):\n    \"\"\"Get selected instance ids and context.\"\"\"\n\n    context_selected = (\n        self._context_widget is not None\n        and self._context_widget.is_selected\n    )\n    instances = [\n        widget.id\n        for widget in self._get_selected_instance_widgets()\n    ]\n    convertor_identifiers = [\n        widget.identifier\n        for widget in self._get_selected_convertor_widgets()\n    ]\n    return instances, context_selected, convertor_identifiers\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/card_view_widgets.html#client.ayon_core.tools.publisher.widgets.card_view_widgets.InstanceCardView.refresh","title":"<code>refresh()</code>","text":"<p>Refresh instances in view based on CreatedContext.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/card_view_widgets.py</code> <pre><code>def refresh(self):\n    \"\"\"Refresh instances in view based on CreatedContext.\"\"\"\n    self._make_sure_context_widget_exists()\n\n    self._update_convertors_group()\n    context_info_by_id = self._controller.get_instances_context_info()\n\n    # Prepare instances by group and identifiers by group\n    instances_by_group = collections.defaultdict(list)\n    identifiers_by_group = collections.defaultdict(set)\n    identifiers: set[str] = set()\n    instances_by_id = {}\n    parent_id_by_id = {}\n    instance_ids_by_parent_id = collections.defaultdict(set)\n    instance_items = self._controller.get_instance_items()\n    for instance in instance_items:\n        group_name = instance.group_label\n        instances_by_group[group_name].append(instance)\n        identifiers_by_group[group_name].add(\n            instance.creator_identifier\n        )\n        identifiers.add(instance.creator_identifier)\n        instances_by_id[instance.id] = instance\n        instance_ids_by_parent_id[instance.parent_instance_id].add(\n            instance.id\n        )\n        parent_id_by_id[instance.id] = instance.parent_instance_id\n\n    parent_active_by_id = {\n        instance_id: False\n        for instance_id in instances_by_id\n    }\n    _queue = collections.deque()\n    _queue.append((None, True))\n    while _queue:\n        parent_id, is_parent_active = _queue.popleft()\n        for instance_id in instance_ids_by_parent_id[parent_id]:\n            instance_item = instances_by_id[instance_id]\n            is_active = instance_item.is_active\n            if (\n                not is_parent_active\n                and instance_item.parent_flags &amp; ParentFlags.share_active\n            ):\n                is_active = False\n\n            parent_active_by_id[instance_id] = is_parent_active\n            _queue.append(\n                (instance_id, is_active)\n            )\n\n    # Remove groups that were not found in passed instances\n    groups_to_remove = (\n        set(self._widgets_by_group) - set(instances_by_group)\n    )\n    ids_to_remove = (\n        set(self._widgets_by_id) - set(instances_by_id)\n    )\n\n    # Sort groups\n    sorted_group_names = list(sorted(instances_by_group.keys()))\n\n    # Keep track of widget indexes\n    # - we start with 1 because Context item as at the top\n    widget_idx = 1\n    if self._convertors_group is not None:\n        widget_idx += 1\n\n    group_by_instance_id = {}\n    instance_ids_by_group_name = collections.defaultdict(list)\n    group_icons = {\n        identifier: self._controller.get_creator_icon(identifier)\n        for identifier in identifiers\n    }\n    for group_name in sorted_group_names:\n        if group_name not in self._widgets_by_group:\n            group_widget = BaseGroupWidget(\n                group_name, self._content_widget\n            )\n            group_widget.double_clicked.connect(self.double_clicked)\n            self._content_layout.insertWidget(widget_idx, group_widget)\n            self._widgets_by_group[group_name] = group_widget\n\n        widget_idx += 1\n\n        instances = instances_by_group[group_name]\n        for instance in instances:\n            group_by_instance_id[instance.id] = group_name\n            instance_ids_by_group_name[group_name].append(instance.id)\n\n        self._update_instance_widgets(\n            group_name,\n            instances,\n            context_info_by_id,\n            parent_active_by_id,\n            group_icons,\n        )\n\n    # Remove empty groups\n    for group_name in groups_to_remove:\n        widget = self._widgets_by_group.pop(group_name)\n        widget.setVisible(False)\n        self._content_layout.removeWidget(widget)\n        widget.deleteLater()\n\n        if group_name in self._explicitly_selected_groups:\n            self._explicitly_selected_groups.remove(group_name)\n\n    for instance_id in ids_to_remove:\n        widget = self._widgets_by_id.pop(instance_id)\n        widget.setVisible(False)\n        widget.deleteLater()\n\n    sorted_group_names.insert(0, CONTEXT_GROUP)\n\n    self._parent_id_by_id = parent_id_by_id\n    self._instance_ids_by_parent_id = instance_ids_by_parent_id\n    self._group_name_by_instance_id = group_by_instance_id\n    self._instance_ids_by_group_name = instance_ids_by_group_name\n    self._ordered_groups = sorted_group_names\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/card_view_widgets.html#client.ayon_core.tools.publisher.widgets.card_view_widgets.InstanceCardView.refresh_instance_states","title":"<code>refresh_instance_states(instance_ids=None)</code>","text":"<p>Trigger update of instances on group widgets.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/card_view_widgets.py</code> <pre><code>def refresh_instance_states(self, instance_ids=None):\n    \"\"\"Trigger update of instances on group widgets.\"\"\"\n    if instance_ids is not None:\n        instance_ids = set(instance_ids)\n    context_info_by_id = self._controller.get_instances_context_info()\n    instance_items_by_id = self._controller.get_instance_items_by_id(\n        instance_ids\n    )\n    instance_ids: set[str] = set(instance_items_by_id)\n    available_ids: set[str] = set(instance_items_by_id)\n\n    affected_ids = self._get_affected_ids(instance_ids)\n\n    _queue = collections.deque()\n    _queue.append((set(self._instance_ids_by_parent_id[None]), True))\n    while _queue:\n        if not affected_ids:\n            break\n\n        chilren_ids, is_parent_active = _queue.pop()\n        for instance_id in chilren_ids:\n            if instance_id not in affected_ids:\n                continue\n            affected_ids.discard(instance_id)\n            widget = self._widgets_by_id[instance_id]\n            if instance_id in instance_ids:\n                instance_ids.discard(instance_id)\n                if instance_id in available_ids:\n                    available_ids.discard(instance_id)\n                    widget.update_instance(\n                        instance_items_by_id[instance_id],\n                        context_info_by_id[instance_id],\n                        is_parent_active,\n                    )\n                else:\n                    widget.set_parent_active(is_parent_active)\n\n            if not affected_ids:\n                break\n\n            children = set(self._instance_ids_by_parent_id[instance_id])\n            if children:\n                instance_ids |= children\n                _queue.append((children, widget.is_active()))\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/card_view_widgets.html#client.ayon_core.tools.publisher.widgets.card_view_widgets.InstanceCardView.sizeHint","title":"<code>sizeHint()</code>","text":"<p>Modify sizeHint based on visibility of scroll bars.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/card_view_widgets.py</code> <pre><code>def sizeHint(self):\n    \"\"\"Modify sizeHint based on visibility of scroll bars.\"\"\"\n    # Calculate width hint by content widget and vertical scroll bar\n    scroll_bar = self._scroll_area.verticalScrollBar()\n    width = (\n        self._content_widget.sizeHint().width()\n        + scroll_bar.sizeHint().width()\n    )\n\n    result = super().sizeHint()\n    result.setWidth(width)\n    return result\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/card_view_widgets.html#client.ayon_core.tools.publisher.widgets.card_view_widgets.InstanceCardWidget","title":"<code>InstanceCardWidget</code>","text":"<p>               Bases: <code>CardWidget</code></p> <p>Card widget representing instance.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/card_view_widgets.py</code> <pre><code>class InstanceCardWidget(CardWidget):\n    \"\"\"Card widget representing instance.\"\"\"\n\n    active_changed = QtCore.Signal(str, bool)\n\n    def __init__(\n        self,\n        instance,\n        context_info,\n        is_parent_active: bool,\n        group_icon,\n        parent: BaseGroupWidget,\n    ):\n        super().__init__(parent)\n\n        self.instance = instance\n        self._is_active = instance.is_active\n\n        self._id = instance.id\n        self._group_identifier = instance.group_label\n        self._group_icon = group_icon\n        self._is_parent_active = is_parent_active\n        self._toggle_is_enabled = True\n\n        self._last_product_name = None\n        self._last_variant = None\n        self._last_label = None\n        self._last_folder_path = None\n        self._last_task_name = None\n\n        icon_widget = IconValuePixmapLabel(group_icon, self)\n        icon_widget.setObjectName(\"ProductTypeIconLabel\")\n        context_warning = ContextWarningLabel(self)\n\n        icon_layout = QtWidgets.QHBoxLayout()\n        icon_layout.setContentsMargins(10, 5, 5, 5)\n        icon_layout.addWidget(icon_widget)\n        icon_layout.addWidget(context_warning)\n\n        label_widget = QtWidgets.QLabel(self)\n        active_checkbox = NiceCheckbox(parent=self)\n\n        expand_btn = QtWidgets.QToolButton(self)\n        # Not yet implemented\n        expand_btn.setVisible(False)\n        expand_btn.setObjectName(\"ArrowBtn\")\n        expand_btn.setArrowType(QtCore.Qt.DownArrow)\n        expand_btn.setMaximumWidth(14)\n        expand_btn.setEnabled(False)\n\n        top_layout = QtWidgets.QHBoxLayout()\n        top_layout.addLayout(icon_layout, 0)\n        top_layout.addWidget(label_widget, 1)\n        top_layout.addWidget(context_warning, 0)\n        top_layout.addWidget(active_checkbox, 0)\n        top_layout.addWidget(expand_btn, 0)\n\n        detail_widget = QtWidgets.QWidget(self)\n        detail_widget.setVisible(False)\n\n        layout = QtWidgets.QHBoxLayout(self)\n        layout.setContentsMargins(0, 2, 10, 2)\n        layout.addLayout(top_layout)\n        layout.addWidget(detail_widget)\n\n        active_checkbox.setAttribute(QtCore.Qt.WA_TranslucentBackground)\n        expand_btn.setAttribute(QtCore.Qt.WA_TranslucentBackground)\n\n        active_checkbox.stateChanged.connect(self._on_active_change)\n        expand_btn.clicked.connect(self._on_expend_clicked)\n\n        self._icon_widget = icon_widget\n        self._label_widget = label_widget\n        self._context_warning = context_warning\n        self._active_checkbox = active_checkbox\n        self._expand_btn = expand_btn\n\n        self._detail_widget = detail_widget\n\n        self._update_instance_values(context_info, is_parent_active)\n\n    def set_active_toggle_enabled(self, enabled: bool) -&gt; None:\n        if self._toggle_is_enabled is enabled:\n            return\n        self._toggle_is_enabled = enabled\n        self._update_checkbox_state()\n\n    def is_active(self) -&gt; bool:\n        return self._active_checkbox.isChecked()\n\n    def set_active(self, active: Optional[bool]) -&gt; None:\n        if not self.is_checkbox_enabled():\n            return\n        if active is None:\n            active = not self._is_active\n        self._set_checked(active)\n\n    def is_parent_active(self) -&gt; bool:\n        return self._is_parent_active\n\n    def set_parent_active(self, is_active: bool) -&gt; None:\n        if self._is_parent_active is is_active:\n            return\n        self._is_parent_active = is_active\n        self._update_checkbox_state()\n\n    def is_checkbox_enabled(self) -&gt; bool:\n        \"\"\"Checkbox can be changed by user.\"\"\"\n        return (\n            self._used_parent_active()\n            and not self.instance.is_mandatory\n        )\n\n    def update_instance(self, instance, context_info, is_parent_active):\n        \"\"\"Update instance object and update UI.\"\"\"\n        self.instance = instance\n        self._is_active = instance.is_active\n        self._update_instance_values(context_info, is_parent_active)\n\n    def _validate_context(self, context_info):\n        valid = context_info.is_valid\n        self._icon_widget.setVisible(valid)\n        self._context_warning.setVisible(not valid)\n\n    @staticmethod\n    def _get_card_widget_sub_label(\n        folder_path: Optional[str],\n        task_name: Optional[str],\n    ) -&gt; str:\n        sublabel = \"\"\n        if folder_path:\n            folder_name = folder_path.rsplit(\"/\", 1)[-1]\n            sublabel = f\"&lt;b&gt;{folder_name}&lt;/b&gt;\"\n            if task_name:\n                sublabel += f\" - &lt;i&gt;{task_name}&lt;/i&gt;\"\n        return sublabel\n\n    def _update_product_name(self):\n        variant = self.instance.variant\n        product_name = self.instance.product_name\n        label = self.instance.label\n        folder_path = self.instance.folder_path\n        task_name = self.instance.task_name\n\n        if (\n            variant == self._last_variant\n            and product_name == self._last_product_name\n            and label == self._last_label\n            and folder_path == self._last_folder_path\n            and task_name == self._last_task_name\n        ):\n            return\n\n        self._last_variant = variant\n        self._last_product_name = product_name\n        self._last_label = label\n        self._last_folder_path = folder_path\n        self._last_task_name = task_name\n\n        # Make `variant` bold\n        label = html_escape(self.instance.label)\n        found_parts = set(re.findall(variant, label, re.IGNORECASE))\n        if found_parts:\n            for part in found_parts:\n                replacement = f\"&lt;b&gt;{part}&lt;/b&gt;\"\n                label = label.replace(part, replacement)\n\n        label = f\"&lt;span&gt;{label}&lt;/span&gt;\"\n        sublabel = self._get_card_widget_sub_label(folder_path, task_name)\n        if sublabel:\n            label += f\"&lt;br/&gt;&lt;span style=\\\"font-size: 8pt;\\\"&gt;{sublabel}&lt;/span&gt;\"\n\n        self._label_widget.setText(label)\n        # HTML text will cause that label start catch mouse clicks\n        # - disabling with changing interaction flag\n        self._label_widget.setTextInteractionFlags(\n            QtCore.Qt.NoTextInteraction\n        )\n\n    def _update_instance_values(self, context_info, is_parent_active):\n        \"\"\"Update instance data\"\"\"\n        self._is_parent_active = is_parent_active\n        self._update_product_name()\n        self._update_checkbox_state()\n        self._validate_context(context_info)\n\n    def _update_checkbox_state(self):\n        parent_is_enabled = self._used_parent_active()\n        self._label_widget.setEnabled(parent_is_enabled)\n        self._active_checkbox.setEnabled(\n            self._toggle_is_enabled\n            and not self.instance.is_mandatory\n            and parent_is_enabled\n        )\n        # Hide checkbox for mandatory instances\n        self._active_checkbox.setVisible(not self.instance.is_mandatory)\n\n        # Visually disable instance if parent is disabled\n        checked = parent_is_enabled and self._is_active\n        self._set_checked(checked)\n\n    def _set_checked(self, checked: bool) -&gt; None:\n        if checked is not self._active_checkbox.isChecked():\n            self._active_checkbox.blockSignals(True)\n            self._active_checkbox.setChecked(checked)\n            self._active_checkbox.blockSignals(False)\n\n    def _used_parent_active(self) -&gt; bool:\n        parent_enabled = True\n        if self.instance.parent_flags &amp; ParentFlags.share_active:\n            parent_enabled = self._is_parent_active\n        return parent_enabled\n\n    def _set_expanded(self, expanded=None):\n        if expanded is None:\n            expanded = not self._detail_widget.isVisible()\n        self._detail_widget.setVisible(expanded)\n\n    def _on_active_change(self):\n        if not self.is_checkbox_enabled():\n            return\n        new_value = self._active_checkbox.isChecked()\n        old_value = self._is_active\n        if new_value is old_value:\n            return\n        self._is_active = new_value\n        self.active_changed.emit(self._id, new_value)\n\n    def _on_expend_clicked(self):\n        self._set_expanded()\n\n    def _is_valid_double_click(self, event):\n        widget = self.childAt(event.pos())\n        if (\n            widget is self._active_checkbox\n            or widget is self._expand_btn\n        ):\n            return False\n        return True\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/card_view_widgets.html#client.ayon_core.tools.publisher.widgets.card_view_widgets.InstanceCardWidget.is_checkbox_enabled","title":"<code>is_checkbox_enabled()</code>","text":"<p>Checkbox can be changed by user.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/card_view_widgets.py</code> <pre><code>def is_checkbox_enabled(self) -&gt; bool:\n    \"\"\"Checkbox can be changed by user.\"\"\"\n    return (\n        self._used_parent_active()\n        and not self.instance.is_mandatory\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/card_view_widgets.html#client.ayon_core.tools.publisher.widgets.card_view_widgets.InstanceCardWidget.update_instance","title":"<code>update_instance(instance, context_info, is_parent_active)</code>","text":"<p>Update instance object and update UI.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/card_view_widgets.py</code> <pre><code>def update_instance(self, instance, context_info, is_parent_active):\n    \"\"\"Update instance object and update UI.\"\"\"\n    self.instance = instance\n    self._is_active = instance.is_active\n    self._update_instance_values(context_info, is_parent_active)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/create_context_widgets.html","title":"create_context_widgets","text":""},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/create_context_widgets.html#client.ayon_core.tools.publisher.widgets.create_context_widgets.CreateHierarchyController","title":"<code>CreateHierarchyController</code>","text":"<p>Controller for hierarchy widgets.</p> <p>Helper for handling hierarchy widgets in create tab. It handles selection of folder and task to properly propagate it to other widgets.</p> <p>At the same time handles expected selection so can pre-select folder and task based on current context.</p> <p>Parameters:</p> Name Type Description Default <code>controller</code> <code>PublisherController</code> <p>Publisher controller.</p> required Source code in <code>client/ayon_core/tools/publisher/widgets/create_context_widgets.py</code> <pre><code>class CreateHierarchyController:\n    \"\"\"Controller for hierarchy widgets.\n\n    Helper for handling hierarchy widgets in create tab. It handles selection\n    of folder and task to properly propagate it to other widgets.\n\n    At the same time handles expected selection so can pre-select folder and\n    task based on current context.\n\n    Args:\n        controller (PublisherController): Publisher controller.\n\n    \"\"\"\n    def __init__(self, controller: AbstractPublisherFrontend):\n        self._event_system = QueuedEventSystem()\n        self._controller: AbstractPublisherFrontend = controller\n        self._selection_model = CreateSelectionModel(self)\n        self._expected_selection = HierarchyExpectedSelection(\n            self, handle_project=False\n        )\n\n    # Events system\n    @property\n    def event_system(self):\n        return self._event_system\n\n    def emit_event(self, topic, data=None, source=None):\n        \"\"\"Use implemented event system to trigger event.\"\"\"\n\n        if data is None:\n            data = {}\n        self.event_system.emit(topic, data, source)\n\n    def register_event_callback(self, topic, callback):\n        self.event_system.add_callback(topic, callback)\n\n    def get_project_name(self):\n        return self._controller.get_current_project_name()\n\n    def get_folder_items(self, project_name, sender=None):\n        return self._controller.get_folder_items(project_name, sender)\n\n    def get_task_items(self, project_name, folder_id, sender=None):\n        return self._controller.get_task_items(\n            project_name, folder_id, sender\n        )\n\n    def get_folder_type_items(self, project_name, sender=None):\n        return self._controller.get_folder_type_items(\n            project_name, sender\n        )\n\n    def get_task_type_items(self, project_name, sender=None):\n        return self._controller.get_task_type_items(\n            project_name, sender\n        )\n\n    # Selection model\n    def set_selected_project(self, project_name):\n        self._selection_model.set_selected_project(project_name)\n\n    def set_selected_folder(self, folder_id):\n        self._selection_model.set_selected_folder(folder_id)\n\n    def set_selected_task(self, task_id, task_name):\n        self._selection_model.set_selected_task(task_id, task_name)\n\n    # Expected selection\n    def get_expected_selection_data(self):\n        return self._expected_selection.get_expected_selection_data()\n\n    def set_expected_selection(self, project_name, folder_id, task_name):\n        self._expected_selection.set_expected_selection(\n            project_name, folder_id, task_name\n        )\n\n    def expected_folder_selected(self, folder_id):\n        self._expected_selection.expected_folder_selected(folder_id)\n\n    def expected_task_selected(self, folder_id, task_name):\n        self._expected_selection.expected_task_selected(folder_id, task_name)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/create_context_widgets.html#client.ayon_core.tools.publisher.widgets.create_context_widgets.CreateHierarchyController.emit_event","title":"<code>emit_event(topic, data=None, source=None)</code>","text":"<p>Use implemented event system to trigger event.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/create_context_widgets.py</code> <pre><code>def emit_event(self, topic, data=None, source=None):\n    \"\"\"Use implemented event system to trigger event.\"\"\"\n\n    if data is None:\n        data = {}\n    self.event_system.emit(topic, data, source)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/create_context_widgets.html#client.ayon_core.tools.publisher.widgets.create_context_widgets.CreateSelectionModel","title":"<code>CreateSelectionModel</code>","text":"<p>               Bases: <code>object</code></p> <p>Model handling selection changes.</p> <p>Triggering events: - \"selection.project.changed\" - \"selection.folder.changed\" - \"selection.task.changed\"</p> Source code in <code>client/ayon_core/tools/publisher/widgets/create_context_widgets.py</code> <pre><code>class CreateSelectionModel(object):\n    \"\"\"Model handling selection changes.\n\n    Triggering events:\n    - \"selection.project.changed\"\n    - \"selection.folder.changed\"\n    - \"selection.task.changed\"\n    \"\"\"\n\n    event_source = \"publisher.create.selection.model\"\n\n    def __init__(self, controller: \"CreateHierarchyController\"):\n        self._controller: CreateHierarchyController = controller\n\n        self._project_name = None\n        self._folder_id = None\n        self._task_name = None\n        self._task_id = None\n\n    def get_selected_project_name(self):\n        return self._project_name\n\n    def set_selected_project(self, project_name):\n        if project_name == self._project_name:\n            return\n\n        self._project_name = project_name\n        self._controller.emit_event(\n            \"selection.project.changed\",\n            {\"project_name\": project_name},\n            self.event_source\n        )\n\n    def get_selected_folder_id(self):\n        return self._folder_id\n\n    def set_selected_folder(self, folder_id):\n        if folder_id == self._folder_id:\n            return\n\n        self._folder_id = folder_id\n        self._controller.emit_event(\n            \"selection.folder.changed\",\n            {\n                \"project_name\": self._project_name,\n                \"folder_id\": folder_id,\n            },\n            self.event_source\n        )\n\n    def get_selected_task_name(self):\n        return self._task_name\n\n    def get_selected_task_id(self):\n        return self._task_id\n\n    def set_selected_task(self, task_id, task_name):\n        if task_id == self._task_id:\n            return\n\n        self._task_name = task_name\n        self._task_id = task_id\n        self._controller.emit_event(\n            \"selection.task.changed\",\n            {\n                \"project_name\": self._project_name,\n                \"folder_id\": self._folder_id,\n                \"task_name\": task_name,\n                \"task_id\": task_id,\n            },\n            self.event_source\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/create_widget.html","title":"create_widget","text":""},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/create_widget.html#client.ayon_core.tools.publisher.widgets.create_widget.CreateWidget","title":"<code>CreateWidget</code>","text":"<p>               Bases: <code>QWidget</code></p> Source code in <code>client/ayon_core/tools/publisher/widgets/create_widget.py</code> <pre><code>class CreateWidget(QtWidgets.QWidget):\n    def __init__(self, controller, parent=None):\n        super().__init__(parent)\n\n        self._controller: AbstractPublisherFrontend = controller\n\n        self._folder_path = None\n        self._product_names = None\n        self._selected_creator_identifier = None\n        self._selected_product_type = None\n\n        self._prereq_available = False\n\n        name_pattern = \"^[{}]*$\".format(PRODUCT_NAME_ALLOWED_SYMBOLS)\n        self._name_pattern = name_pattern\n        self._compiled_name_pattern = re.compile(name_pattern)\n\n        main_splitter_widget = QtWidgets.QSplitter(self)\n\n        context_widget = CreateContextWidget(controller, main_splitter_widget)\n\n        # --- Creators view ---\n        creators_widget = QtWidgets.QWidget(main_splitter_widget)\n\n        creator_short_desc_widget = CreatorShortDescWidget(creators_widget)\n\n        attr_separator_widget = QtWidgets.QWidget(creators_widget)\n        attr_separator_widget.setObjectName(\"Separator\")\n        attr_separator_widget.setMinimumHeight(1)\n        attr_separator_widget.setMaximumHeight(1)\n\n        creators_splitter = QtWidgets.QSplitter(creators_widget)\n\n        creators_view_widget = QtWidgets.QWidget(creators_splitter)\n\n        creator_view_label = QtWidgets.QLabel(\n            \"Choose publish type\", creators_view_widget\n        )\n\n        creators_view = QtWidgets.QListView(creators_view_widget)\n        creators_model = QtGui.QStandardItemModel()\n        creators_sort_model = CreatorsProxyModel()\n        creators_sort_model.setSourceModel(creators_model)\n        creators_view.setModel(creators_sort_model)\n\n        creators_view_layout = QtWidgets.QVBoxLayout(creators_view_widget)\n        creators_view_layout.setContentsMargins(0, 0, 0, 0)\n        creators_view_layout.addWidget(creator_view_label, 0)\n        creators_view_layout.addWidget(creators_view, 1)\n\n        # --- Creator attr defs ---\n        creators_attrs_widget = QtWidgets.QWidget(creators_splitter)\n\n        # Top part - variant / product name + thumbnail\n        creators_attrs_top = QtWidgets.QWidget(creators_attrs_widget)\n\n        # Basics - variant / product name\n        creator_basics_widget = ResizeControlWidget(creators_attrs_top)\n\n        product_variant_label = QtWidgets.QLabel(\n            \"Create options\", creator_basics_widget\n        )\n\n        product_variant_widget = QtWidgets.QWidget(creator_basics_widget)\n        # Variant and product input\n        variant_widget = HintedLineEdit(parent=product_variant_widget)\n        variant_widget.set_text_widget_object_name(\"VariantInput\")\n        variant_widget.setToolTip(VARIANT_TOOLTIP)\n\n        product_name_input = QtWidgets.QLineEdit(product_variant_widget)\n        product_name_input.setEnabled(False)\n\n        product_variant_layout = QtWidgets.QFormLayout(product_variant_widget)\n        product_variant_layout.setContentsMargins(0, 0, 0, 0)\n        product_variant_layout.setHorizontalSpacing(INPUTS_LAYOUT_HSPACING)\n        product_variant_layout.setVerticalSpacing(INPUTS_LAYOUT_VSPACING)\n        product_variant_layout.addRow(\"Variant\", variant_widget)\n        product_variant_layout.addRow(\"Product\", product_name_input)\n\n        creator_basics_layout = QtWidgets.QVBoxLayout(creator_basics_widget)\n        creator_basics_layout.setContentsMargins(0, 0, 0, 0)\n        creator_basics_layout.addWidget(product_variant_label, 0)\n        creator_basics_layout.addWidget(product_variant_widget, 0)\n\n        thumbnail_widget = ThumbnailWidget(controller, creators_attrs_top)\n\n        creators_attrs_top_layout = QtWidgets.QHBoxLayout(creators_attrs_top)\n        creators_attrs_top_layout.setContentsMargins(0, 0, 0, 0)\n        creators_attrs_top_layout.addWidget(creator_basics_widget, 1)\n        creators_attrs_top_layout.addWidget(thumbnail_widget, 0)\n\n        # Precreate attributes widget\n        pre_create_widget = PreCreateWidget(controller, creators_attrs_widget)\n\n        # Create button\n        create_btn_wrapper = QtWidgets.QWidget(creators_attrs_widget)\n        create_btn = CreateBtn(create_btn_wrapper)\n        create_btn.setEnabled(False)\n\n        create_btn_wrap_layout = QtWidgets.QHBoxLayout(create_btn_wrapper)\n        create_btn_wrap_layout.setContentsMargins(0, 0, 0, 0)\n        create_btn_wrap_layout.addStretch(1)\n        create_btn_wrap_layout.addWidget(create_btn, 0)\n\n        creators_attrs_layout = QtWidgets.QVBoxLayout(creators_attrs_widget)\n        creators_attrs_layout.setContentsMargins(0, 0, 0, 0)\n        creators_attrs_layout.addWidget(creators_attrs_top, 0)\n        creators_attrs_layout.addWidget(pre_create_widget, 1)\n        creators_attrs_layout.addWidget(create_btn_wrapper, 0)\n\n        creators_splitter.addWidget(creators_view_widget)\n        creators_splitter.addWidget(creators_attrs_widget)\n        creators_splitter.setStretchFactor(0, 1)\n        creators_splitter.setStretchFactor(1, 2)\n\n        creators_layout = QtWidgets.QVBoxLayout(creators_widget)\n        creators_layout.setContentsMargins(0, 0, 0, 0)\n        creators_layout.addWidget(creator_short_desc_widget, 0)\n        creators_layout.addWidget(attr_separator_widget, 0)\n        creators_layout.addWidget(creators_splitter, 1)\n        # ------------\n\n        # --- Detailed information about creator ---\n        # Detailed description of creator\n        # TODO this has no way how can be showed now\n\n        # -------------------------------------------\n        main_splitter_widget.addWidget(context_widget)\n        main_splitter_widget.addWidget(creators_widget)\n        main_splitter_widget.setStretchFactor(0, 1)\n        main_splitter_widget.setStretchFactor(1, 3)\n\n        layout = QtWidgets.QVBoxLayout(self)\n        layout.setContentsMargins(0, 0, 0, 0)\n        layout.addWidget(main_splitter_widget, 1)\n\n        prereq_timer = QtCore.QTimer()\n        prereq_timer.setInterval(50)\n        prereq_timer.setSingleShot(True)\n\n        prereq_timer.timeout.connect(self._invalidate_prereq)\n\n        create_btn.clicked.connect(self._on_create)\n        creator_basics_widget.resized.connect(self._on_creator_basics_resize)\n        variant_widget.returnPressed.connect(self._on_create)\n        variant_widget.textChanged.connect(self._on_variant_change)\n        creators_view.selectionModel().currentChanged.connect(\n            self._on_creator_item_change\n        )\n        context_widget.folder_changed.connect(self._on_folder_change)\n        context_widget.task_changed.connect(self._on_task_change)\n        thumbnail_widget.thumbnail_created.connect(self._on_thumbnail_create)\n        thumbnail_widget.thumbnail_cleared.connect(self._on_thumbnail_clear)\n\n        controller.register_event_callback(\n            \"main.window.closed\", self._on_main_window_close\n        )\n        controller.register_event_callback(\n            \"controller.reset.finished\", self._on_controler_reset\n        )\n        controller.register_event_callback(\n            \"create.context.pre.create.attrs.changed\",\n            self._pre_create_attr_changed\n        )\n\n        self._main_splitter_widget = main_splitter_widget\n\n        self._creators_splitter = creators_splitter\n\n        self._context_widget = context_widget\n\n        self.product_name_input = product_name_input\n\n        self._variant_widget = variant_widget\n\n        self._creators_model = creators_model\n        self._creators_sort_model = creators_sort_model\n        self._creators_view = creators_view\n        self._create_btn = create_btn\n\n        self._creator_short_desc_widget = creator_short_desc_widget\n        self._creator_basics_widget = creator_basics_widget\n        self._thumbnail_widget = thumbnail_widget\n        self._pre_create_widget = pre_create_widget\n        self._attr_separator_widget = attr_separator_widget\n\n        self._prereq_timer = prereq_timer\n        self._first_show = True\n        self._last_thumbnail_path = None\n\n        self._last_current_context_folder_path = None\n        self._last_current_context_task = None\n        self._use_current_context = True\n        self._current_creator_variant_hints = []\n\n    def get_current_folder_path(self):\n        return self._controller.get_current_folder_path()\n\n    def get_current_task_name(self):\n        return self._controller.get_current_task_name()\n\n    def _context_change_is_enabled(self):\n        return self._context_widget.is_enabled()\n\n    def _get_folder_path(self):\n        folder_path = None\n        if self._context_change_is_enabled():\n            folder_path = self._context_widget.get_selected_folder_path()\n        return folder_path or None\n\n    def _get_folder_id(self):\n        folder_id = None\n        if self._context_widget.is_enabled():\n            folder_id = self._context_widget.get_selected_folder_id()\n        return folder_id\n\n    def _get_task_name(self):\n        task_name = None\n        if self._context_change_is_enabled():\n            # Don't use selection of task if folder is not set\n            folder_path = self._context_widget.get_selected_folder_path()\n            if folder_path:\n                task_name = self._context_widget.get_selected_task_name()\n        return task_name\n\n    def _set_context_enabled(self, enabled):\n        check_prereq = self._context_widget.is_enabled() != enabled\n        self._context_widget.set_enabled(enabled)\n        if check_prereq:\n            self._invalidate_prereq()\n\n    def _on_main_window_close(self):\n        \"\"\"Publisher window was closed.\"\"\"\n\n        # Use current context on next refresh\n        self._use_current_context = True\n\n    def refresh(self):\n        current_folder_path = self._controller.get_current_folder_path()\n        current_task_name = self._controller.get_current_task_name()\n\n        # Get context before refresh to keep selection of folder and\n        #   task widgets\n        folder_path = self._get_folder_path()\n        task_name = self._get_task_name()\n\n        # Replace by current context if last loaded context was\n        #   'current context' before reset\n        if (\n            self._use_current_context\n            or (\n                self._last_current_context_folder_path\n                and folder_path == self._last_current_context_folder_path\n                and task_name == self._last_current_context_task\n            )\n        ):\n            folder_path = current_folder_path\n            task_name = current_task_name\n\n        # Store values for future refresh\n        self._last_current_context_folder_path = current_folder_path\n        self._last_current_context_task = current_task_name\n        self._use_current_context = False\n\n        self._prereq_available = False\n\n        # Disable context widget so refresh of folder will use context folder\n        #   path\n        self._set_context_enabled(False)\n\n        # Refresh data before update of creators\n        self._context_widget.refresh()\n        self._refresh_product_name()\n\n        # Then refresh creators which may trigger callbacks using refreshed\n        #   data\n        self._refresh_creators()\n\n        folder_id = self._controller.get_folder_id_from_path(folder_path)\n        self._context_widget.update_current_context_btn()\n        self._context_widget.set_selected_context(folder_id, task_name)\n\n        self._invalidate_prereq_deffered()\n\n    def _invalidate_prereq_deffered(self):\n        self._prereq_timer.start()\n\n    def _invalidate_prereq(self):\n        prereq_available = True\n        creator_btn_tooltips = []\n\n        available_creators = self._creators_model.rowCount() &gt; 0\n        if available_creators != self._creators_view.isEnabled():\n            self._creators_view.setEnabled(available_creators)\n\n        if not available_creators:\n            prereq_available = False\n            creator_btn_tooltips.append(\"Creator is not selected\")\n\n        if (\n            self._context_change_is_enabled()\n            and self._get_folder_path() is None\n        ):\n            # QUESTION how to handle invalid folder?\n            prereq_available = False\n            creator_btn_tooltips.append(\"Context is not selected\")\n\n        if prereq_available != self._prereq_available:\n            self._prereq_available = prereq_available\n\n            self._create_btn.setEnabled(prereq_available)\n\n            self._variant_widget.setEnabled(prereq_available)\n\n        tooltip = \"\"\n        if creator_btn_tooltips:\n            tooltip = \"\\n\".join(creator_btn_tooltips)\n        self._create_btn.setToolTip(tooltip)\n\n        self._on_variant_change()\n\n    def _refresh_product_name(self):\n        folder_path = self._get_folder_path()\n\n        # Skip if folder did not change\n        if self._folder_path and self._folder_path == folder_path:\n            return\n\n        # Make sure `_folder_path` and `_product_names` variables are reset\n        self._folder_path = folder_path\n        self._product_names = None\n        if folder_path is None:\n            return\n\n        product_names = self._controller.get_existing_product_names(\n            folder_path\n        )\n\n        self._product_names = product_names\n        if product_names is None:\n            self.product_name_input.setText(\"&lt; Folder is not set &gt;\")\n\n    def _refresh_creators(self):\n        # Refresh creators and add their product base types to list\n        existing_items = collections.defaultdict(dict)\n        for row in range(self._creators_model.rowCount()):\n            item = self._creators_model.item(row, 0)\n            identifier = item.data(CREATOR_IDENTIFIER_ROLE)\n            product_type = item.data(PRODUCT_TYPE_ROLE)\n            existing_items[identifier][product_type] = item\n\n        # Add new create plugins\n        new_creators = collections.defaultdict(set)\n        creator_items_by_identifier = self._controller.get_creator_items()\n        for identifier, creator_item in creator_items_by_identifier.items():\n            if creator_item.creator_type != \"artist\":\n                continue\n\n            # TODO add details about creator\n            for ui_item in creator_item.ui_items:\n                if ui_item.filtered:\n                    continue\n\n                items_by_identifier = new_creators[identifier]\n                items_by_identifier.add(ui_item.product_type)\n                is_new = False\n                item = existing_items[identifier].get(ui_item.product_type)\n                if item is None:\n                    is_new = True\n                    item = QtGui.QStandardItem()\n                    item.setFlags(\n                        QtCore.Qt.ItemIsEnabled | QtCore.Qt.ItemIsSelectable\n                    )\n\n                item.setData(ui_item.label, QtCore.Qt.DisplayRole)\n                item.setData(creator_item.show_order, CREATOR_SORT_ROLE)\n                item.setData(identifier, CREATOR_IDENTIFIER_ROLE)\n                item.setData(\n                    creator_item.product_base_type,\n                    PRODUCT_BASE_TYPE_ROLE\n                )\n                item.setData(ui_item.product_type, PRODUCT_TYPE_ROLE)\n                item.setData(\n                    creator_item.create_allow_thumbnail,\n                    CREATOR_THUMBNAIL_ENABLED_ROLE\n                )\n                if is_new:\n                    self._creators_model.appendRow(item)\n\n        # Remove create plugins that are no more available\n        for identifier, items_by_pt in existing_items.items():\n            n_product_types = new_creators[identifier]\n            for product_type, item in items_by_pt.items():\n                if product_type not in n_product_types:\n                    self._creators_model.takeRow(item.row())\n\n        if self._creators_model.rowCount() &lt; 1:\n            return\n\n        self._creators_sort_model.sort(0)\n        # Make sure there is a selection\n        indexes = self._creators_view.selectedIndexes()\n        if not indexes:\n            index = self._creators_sort_model.index(0, 0)\n            self._creators_view.setCurrentIndex(index)\n        else:\n            index = indexes[0]\n\n        identifier = index.data(CREATOR_IDENTIFIER_ROLE)\n        product_type = index.data(PRODUCT_TYPE_ROLE)\n        create_item = creator_items_by_identifier.get(identifier)\n\n        self._set_creator(create_item, product_type)\n\n    def _on_controler_reset(self):\n        # Trigger refresh only if is visible\n        self.refresh()\n\n    def _pre_create_attr_changed(self, event):\n        if (\n            self._selected_creator_identifier is None\n            or self._selected_creator_identifier not in event[\"identifiers\"]\n        ):\n            return\n\n        self._set_creator_by_identifier(\n            self._selected_creator_identifier,\n            self._selected_product_type,\n        )\n\n    def _on_folder_change(self):\n        self._refresh_product_name()\n        if self._context_change_is_enabled():\n            self._invalidate_prereq_deffered()\n\n    def _on_task_change(self):\n        if self._context_change_is_enabled():\n            self._invalidate_prereq_deffered()\n\n    def _on_thumbnail_create(self, thumbnail_path):\n        self._last_thumbnail_path = thumbnail_path\n        self._thumbnail_widget.set_current_thumbnails([thumbnail_path])\n\n    def _on_thumbnail_clear(self):\n        self._last_thumbnail_path = None\n\n    def _on_creator_item_change(self, new_index, _old_index):\n        identifier = None\n        product_type = None\n        if new_index.isValid():\n            identifier = new_index.data(CREATOR_IDENTIFIER_ROLE)\n            product_type = new_index.data(PRODUCT_TYPE_ROLE)\n        self._set_creator_by_identifier(identifier, product_type)\n\n    def _set_creator_detailed_text(self, creator_item):\n        # TODO implement\n        description = \"\"\n        if creator_item is not None:\n            description = creator_item.detailed_description or description\n        self._controller.emit_event(\n            \"show.detailed.help\",\n            {\n                \"message\": description\n            },\n            \"create.widget\"\n        )\n\n    def _set_creator_by_identifier(\n        self,\n        identifier: Union[str, None],\n        product_type: Union[str, None],\n    ) -&gt; None:\n        creator_item = self._controller.get_creator_item_by_id(\n            identifier\n        )\n        self._set_creator(creator_item, product_type)\n\n    def _set_creator(\n        self,\n        creator_item: Union[\"CreatorItem\", None],\n        product_type: Union[str, None],\n    ) -&gt; None:\n        \"\"\"Set current creator item.\n\n        Args:\n            creator_item (CreatorItem): Item representing creator that can be\n                triggered by artist.\n            product_type (str): Product type of creator item.\n\n        \"\"\"\n        self._creator_short_desc_widget.set_creator_item(\n            creator_item, product_type\n        )\n        self._set_creator_detailed_text(creator_item)\n        self._pre_create_widget.set_creator_item(creator_item)\n\n        if not creator_item:\n            self._selected_creator_identifier = None\n            self._selected_product_type = None\n            self._set_context_enabled(False)\n            return\n\n        self._selected_creator_identifier = creator_item.identifier\n        self._selected_product_type = product_type\n\n        if (\n            creator_item.create_allow_context_change\n            != self._context_change_is_enabled()\n        ):\n            self._set_context_enabled(creator_item.create_allow_context_change)\n            self._refresh_product_name()\n\n        self._thumbnail_widget.setVisible(\n            creator_item.create_allow_thumbnail\n        )\n\n        default_variants = creator_item.default_variants\n        if not default_variants:\n            default_variants = [DEFAULT_VARIANT_VALUE]\n\n        default_variant = creator_item.default_variant\n        if not default_variant:\n            default_variant = default_variants[0]\n\n        self._current_creator_variant_hints = list(default_variants)\n        self._variant_widget.set_options(default_variants)\n\n        variant_text = default_variant or DEFAULT_VARIANT_VALUE\n        # Make sure product name is updated to new plugin\n        if variant_text == self._variant_widget.text():\n            self._on_variant_change()\n        else:\n            self._variant_widget.setText(variant_text)\n\n    def _on_variant_change(self, variant_value=None):\n        if not self._prereq_available:\n            return\n\n        # This should probably never happen?\n        if not self._selected_creator_identifier:\n            if self.product_name_input.text():\n                self.product_name_input.setText(\"\")\n            return\n\n        if variant_value is None:\n            variant_value = self._variant_widget.text()\n\n        if not self._compiled_name_pattern.match(variant_value):\n            self._create_btn.setEnabled(False)\n            self._set_variant_state_property(\"invalid\")\n            self.product_name_input.setText(\"&lt; Invalid variant &gt;\")\n            return\n\n        if not self._context_change_is_enabled():\n            self._create_btn.setEnabled(True)\n            self._set_variant_state_property(\"\")\n            self.product_name_input.setText(\"&lt; Valid variant &gt;\")\n            return\n\n        folder_path = self._get_folder_path()\n        task_name = self._get_task_name()\n        # Calculate product name with Creator plugin\n        try:\n            product_name = self._controller.get_product_name(\n                self._selected_creator_identifier,\n                self._selected_product_type,\n                variant_value,\n                folder_path,\n                task_name,\n            )\n        except TaskNotSetError:\n            self._create_btn.setEnabled(False)\n            self._set_variant_state_property(\"invalid\")\n            self.product_name_input.setText(\"&lt; Missing task &gt;\")\n            return\n\n        self.product_name_input.setText(product_name)\n\n        self._create_btn.setEnabled(True)\n        self._validate_product_name(product_name, variant_value)\n\n    def _validate_product_name(self, product_name, variant_value):\n        # Get all products of the current folder\n        if self._product_names:\n            existing_product_names = set(self._product_names)\n        else:\n            existing_product_names = set()\n        existing_product_names_low = set(\n            _name.lower()\n            for _name in existing_product_names\n        )\n\n        # Replace\n        compare_regex = re.compile(re.sub(\n            variant_value, \"(.+)\", product_name, flags=re.IGNORECASE\n        ))\n        variant_hints = set()\n        if variant_value:\n            for _name in existing_product_names:\n                _result = compare_regex.search(_name)\n                if _result:\n                    variant_hints |= set(_result.groups())\n\n        options = list(self._current_creator_variant_hints)\n        if options:\n            options.append(\"---\")\n        options.extend(sorted(variant_hints))\n        # Add hints to actions\n        self._variant_widget.set_options(options)\n\n        # Indicate product existence\n        if not variant_value:\n            property_value = \"empty\"\n\n        elif product_name.lower() in existing_product_names_low:\n            # validate existence of product name with lowered text\n            #   - \"renderMain\" vs. \"rendermain\" mean same path item for\n            #   windows\n            property_value = \"exists\"\n        else:\n            property_value = \"new\"\n\n        self._set_variant_state_property(property_value)\n\n        variant_is_valid = variant_value.strip() != \"\"\n        if variant_is_valid != self._create_btn.isEnabled():\n            self._create_btn.setEnabled(variant_is_valid)\n\n    def _set_variant_state_property(self, state):\n        self._variant_widget.set_text_widget_property(\"state\", state)\n\n    def _on_first_show(self):\n        width = self.width()\n        part = int(width / 9)\n        context_width = part * 3\n        create_sel_width = part * 2\n        rem_width = width - context_width\n        self._main_splitter_widget.setSizes([context_width, rem_width])\n        rem_width -= create_sel_width\n        self._creators_splitter.setSizes([create_sel_width, rem_width])\n\n    def showEvent(self, event):\n        super().showEvent(event)\n        if self._first_show:\n            self._first_show = False\n            self._on_first_show()\n\n    def _on_creator_basics_resize(self):\n        self._thumbnail_widget.set_height(\n            self._creator_basics_widget.sizeHint().height()\n        )\n\n    def _on_create(self):\n        indexes = self._creators_view.selectedIndexes()\n        if not indexes or len(indexes) &gt; 1:\n            return\n\n        if not self._create_btn.isEnabled():\n            return\n\n        index = indexes[0]\n        creator_identifier = index.data(CREATOR_IDENTIFIER_ROLE)\n        product_type = index.data(PRODUCT_TYPE_ROLE)\n        product_base_type = index.data(PRODUCT_BASE_TYPE_ROLE)\n        variant = self._variant_widget.text()\n        # Care about product name only if context change is enabled\n        product_name = None\n        folder_path = None\n        task_name = None\n        if self._context_change_is_enabled():\n            product_name = self.product_name_input.text()\n            folder_path = self._get_folder_path()\n            task_name = self._get_task_name()\n\n        pre_create_data = self._pre_create_widget.current_value()\n        if index.data(CREATOR_THUMBNAIL_ENABLED_ROLE):\n            pre_create_data[PRE_CREATE_THUMBNAIL_KEY] = (\n                self._last_thumbnail_path\n            )\n\n        # Where to define these data?\n        # - what data show be stored?\n        instance_data = {\n            \"folderPath\": folder_path,\n            \"task\": task_name,\n            \"variant\": variant,\n            \"productBaseType\": product_base_type,\n            \"productType\": product_type,\n        }\n\n        success = self._controller.create(\n            creator_identifier,\n            product_name,\n            instance_data,\n            pre_create_data\n        )\n\n        if success:\n            self._set_creator_by_identifier(\n                self._selected_creator_identifier,\n                self._selected_product_type,\n            )\n            self._variant_widget.setText(variant)\n            self._controller.emit_card_message(\"Creation finished...\")\n            self._last_thumbnail_path = None\n            self._thumbnail_widget.set_current_thumbnails()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/folders_dialog.html","title":"folders_dialog","text":""},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/folders_dialog.html#client.ayon_core.tools.publisher.widgets.folders_dialog.FoldersDialog","title":"<code>FoldersDialog</code>","text":"<p>               Bases: <code>QDialog</code></p> <p>Dialog to select folder for a context of instance.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/folders_dialog.py</code> <pre><code>class FoldersDialog(QtWidgets.QDialog):\n    \"\"\"Dialog to select folder for a context of instance.\"\"\"\n\n    def __init__(self, controller, parent):\n        super().__init__(parent)\n        self.setWindowTitle(\"Select folder\")\n\n        filters_widget = FoldersFiltersWidget(self)\n\n        folders_controller = FoldersDialogController(controller)\n        folders_widget = FoldersWidget(folders_controller, self)\n        folders_widget.set_deselectable(True)\n\n        ok_btn = QtWidgets.QPushButton(\"OK\", self)\n        cancel_btn = QtWidgets.QPushButton(\"Cancel\", self)\n\n        btns_layout = QtWidgets.QHBoxLayout()\n        btns_layout.addStretch(1)\n        btns_layout.addWidget(ok_btn)\n        btns_layout.addWidget(cancel_btn)\n\n        layout = QtWidgets.QVBoxLayout(self)\n        layout.setSpacing(5)\n        layout.addWidget(filters_widget, 0)\n        layout.addWidget(folders_widget, 1)\n        layout.addLayout(btns_layout, 0)\n\n        controller.register_event_callback(\n            \"controller.reset.finished\", self._on_controller_reset\n        )\n\n        folders_widget.double_clicked.connect(self._on_ok_clicked)\n        filters_widget.text_changed.connect(self._on_filter_change)\n        filters_widget.my_tasks_changed.connect(self._on_my_tasks_change)\n        ok_btn.clicked.connect(self._on_ok_clicked)\n        cancel_btn.clicked.connect(self._on_cancel_clicked)\n\n        self._controller = controller\n        self._filters_widget = filters_widget\n        self._ok_btn = ok_btn\n        self._cancel_btn = cancel_btn\n\n        self._folders_widget = folders_widget\n\n        self._selected_folder_path = None\n        # Soft refresh is enabled\n        # - reset will happen at all cost if soft reset is enabled\n        # - adds ability to call reset on multiple places without repeating\n        self._soft_reset_enabled = True\n\n        self._first_show = True\n        self._default_height = 500\n\n        self._project_name = None\n\n    def showEvent(self, event):\n        \"\"\"Refresh folders widget on show.\"\"\"\n        super().showEvent(event)\n        if self._first_show:\n            self._first_show = False\n            self._on_first_show()\n        # Refresh on show\n        self.reset(False)\n\n    def reset(self, force=True):\n        \"\"\"Reset widget.\"\"\"\n        if not force and not self._soft_reset_enabled:\n            return\n\n        self._project_name = self._controller.get_current_project_name()\n        if self._soft_reset_enabled:\n            self._soft_reset_enabled = False\n\n        self._folders_widget.set_project_name(self._project_name)\n        self._on_my_tasks_change(self._filters_widget.is_my_tasks_checked())\n\n    def get_selected_folder_path(self):\n        \"\"\"Get selected folder path.\"\"\"\n        return self._selected_folder_path\n\n    def set_selected_folders(self, folder_paths: list[str]) -&gt; None:\n        \"\"\"Change preselected folder before showing the dialog.\n\n        This also resets model and clean filter.\n        \"\"\"\n        self.reset(False)\n        self._filters_widget.set_text(\"\")\n        self._filters_widget.set_my_tasks_checked(False)\n\n        folder_id = None\n        for folder_path in folder_paths:\n            folder_id = self._controller.get_folder_id_from_path(folder_path)\n            if folder_id:\n                break\n        if folder_id:\n            self._folders_widget.set_selected_folder(folder_id)\n\n    def _on_first_show(self):\n        center = self.rect().center()\n        size = self.size()\n        size.setHeight(self._default_height)\n\n        self.resize(size)\n        new_pos = self.mapToGlobal(center)\n        new_pos.setX(new_pos.x() - int(self.width() / 2))\n        new_pos.setY(new_pos.y() - int(self.height() / 2))\n        self.move(new_pos)\n\n    def _on_controller_reset(self):\n        # Change reset enabled so model is reset on show event\n        self._soft_reset_enabled = True\n\n    def _on_filter_change(self, text):\n        \"\"\"Trigger change of filter of folders.\"\"\"\n        self._folders_widget.set_name_filter(text)\n\n    def _on_cancel_clicked(self):\n        self.done(0)\n\n    def _on_ok_clicked(self):\n        self._selected_folder_path = (\n            self._folders_widget.get_selected_folder_path()\n        )\n        self.done(1)\n\n    def _on_my_tasks_change(self, enabled: bool) -&gt; None:\n        folder_ids = None\n        if enabled:\n            entity_ids = self._controller.get_my_tasks_entity_ids(\n                self._project_name\n            )\n            folder_ids = entity_ids[\"folder_ids\"]\n        self._folders_widget.set_folder_ids_filter(folder_ids)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/folders_dialog.html#client.ayon_core.tools.publisher.widgets.folders_dialog.FoldersDialog.get_selected_folder_path","title":"<code>get_selected_folder_path()</code>","text":"<p>Get selected folder path.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/folders_dialog.py</code> <pre><code>def get_selected_folder_path(self):\n    \"\"\"Get selected folder path.\"\"\"\n    return self._selected_folder_path\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/folders_dialog.html#client.ayon_core.tools.publisher.widgets.folders_dialog.FoldersDialog.reset","title":"<code>reset(force=True)</code>","text":"<p>Reset widget.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/folders_dialog.py</code> <pre><code>def reset(self, force=True):\n    \"\"\"Reset widget.\"\"\"\n    if not force and not self._soft_reset_enabled:\n        return\n\n    self._project_name = self._controller.get_current_project_name()\n    if self._soft_reset_enabled:\n        self._soft_reset_enabled = False\n\n    self._folders_widget.set_project_name(self._project_name)\n    self._on_my_tasks_change(self._filters_widget.is_my_tasks_checked())\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/folders_dialog.html#client.ayon_core.tools.publisher.widgets.folders_dialog.FoldersDialog.set_selected_folders","title":"<code>set_selected_folders(folder_paths)</code>","text":"<p>Change preselected folder before showing the dialog.</p> <p>This also resets model and clean filter.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/folders_dialog.py</code> <pre><code>def set_selected_folders(self, folder_paths: list[str]) -&gt; None:\n    \"\"\"Change preselected folder before showing the dialog.\n\n    This also resets model and clean filter.\n    \"\"\"\n    self.reset(False)\n    self._filters_widget.set_text(\"\")\n    self._filters_widget.set_my_tasks_checked(False)\n\n    folder_id = None\n    for folder_path in folder_paths:\n        folder_id = self._controller.get_folder_id_from_path(folder_path)\n        if folder_id:\n            break\n    if folder_id:\n        self._folders_widget.set_selected_folder(folder_id)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/folders_dialog.html#client.ayon_core.tools.publisher.widgets.folders_dialog.FoldersDialog.showEvent","title":"<code>showEvent(event)</code>","text":"<p>Refresh folders widget on show.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/folders_dialog.py</code> <pre><code>def showEvent(self, event):\n    \"\"\"Refresh folders widget on show.\"\"\"\n    super().showEvent(event)\n    if self._first_show:\n        self._first_show = False\n        self._on_first_show()\n    # Refresh on show\n    self.reset(False)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/folders_dialog.html#client.ayon_core.tools.publisher.widgets.folders_dialog.FoldersDialogController","title":"<code>FoldersDialogController</code>","text":"Source code in <code>client/ayon_core/tools/publisher/widgets/folders_dialog.py</code> <pre><code>class FoldersDialogController:\n    def __init__(self, controller: AbstractPublisherFrontend):\n        self._event_system = QueuedEventSystem()\n        self._controller: AbstractPublisherFrontend = controller\n\n    @property\n    def event_system(self):\n        return self._event_system\n\n    def emit_event(self, topic, data=None, source=None):\n        \"\"\"Use implemented event system to trigger event.\"\"\"\n\n        if data is None:\n            data = {}\n        self.event_system.emit(topic, data, source)\n\n    def register_event_callback(self, topic, callback):\n        self.event_system.add_callback(topic, callback)\n\n    def get_folder_items(self, project_name, sender=None):\n        return self._controller.get_folder_items(project_name, sender)\n\n    def get_folder_type_items(self, project_name, sender=None):\n        return self._controller.get_folder_type_items(\n            project_name, sender\n        )\n\n    def set_selected_folder(self, folder_id):\n        pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/folders_dialog.html#client.ayon_core.tools.publisher.widgets.folders_dialog.FoldersDialogController.emit_event","title":"<code>emit_event(topic, data=None, source=None)</code>","text":"<p>Use implemented event system to trigger event.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/folders_dialog.py</code> <pre><code>def emit_event(self, topic, data=None, source=None):\n    \"\"\"Use implemented event system to trigger event.\"\"\"\n\n    if data is None:\n        data = {}\n    self.event_system.emit(topic, data, source)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/help_widget.html","title":"help_widget","text":""},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/help_widget.html#client.ayon_core.tools.publisher.widgets.help_widget.HelpButton","title":"<code>HelpButton</code>","text":"<p>               Bases: <code>QPushButton</code></p> <p>Button used to trigger help dialog.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/help_widget.py</code> <pre><code>class HelpButton(QtWidgets.QPushButton):\n    \"\"\"Button used to trigger help dialog.\"\"\"\n\n    def __init__(self, parent):\n        super().__init__(parent)\n        self.setObjectName(\"CreateDialogHelpButton\")\n        self.setText(\"?\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/help_widget.html#client.ayon_core.tools.publisher.widgets.help_widget.HelpWidget","title":"<code>HelpWidget</code>","text":"<p>               Bases: <code>QWidget</code></p> <p>Widget showing help for single functionality.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/help_widget.py</code> <pre><code>class HelpWidget(QtWidgets.QWidget):\n    \"\"\"Widget showing help for single functionality.\"\"\"\n\n    def __init__(self, parent):\n        super().__init__(parent)\n\n        # TODO add hints what to help with?\n        detail_description_input = QtWidgets.QTextEdit(self)\n        detail_description_input.setObjectName(\"CreatorDetailedDescription\")\n        detail_description_input.setTextInteractionFlags(\n            QtCore.Qt.TextBrowserInteraction\n        )\n\n        main_layout = QtWidgets.QVBoxLayout(self)\n        main_layout.setContentsMargins(0, 0, 0, 0)\n        main_layout.setSpacing(0)\n        main_layout.addWidget(detail_description_input, 1)\n\n        self._detail_description_input = detail_description_input\n\n        self.set_detailed_text()\n\n    def set_detailed_text(self, text=None):\n        if not text:\n            text = \"We didn't prepare help for this part...\"\n\n        if commonmark:\n            html = commonmark.commonmark(text)\n            self._detail_description_input.setHtml(html)\n        elif hasattr(self._detail_description_input, \"setMarkdown\"):\n            self._detail_description_input.setMarkdown(text)\n        else:\n            self._detail_description_input.setText(text)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/icons.html","title":"icons","text":""},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/icons.html#client.ayon_core.tools.publisher.widgets.icons.get_icon","title":"<code>get_icon(icon_name=None, filename=None)</code>","text":"<p>Load image from './images' as QICon.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/icons.py</code> <pre><code>def get_icon(icon_name=None, filename=None):\n    \"\"\"Load image from './images' as QICon.\"\"\"\n    pix = get_pixmap(icon_name, filename)\n    if pix:\n        return QtGui.QIcon(pix)\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/icons.html#client.ayon_core.tools.publisher.widgets.icons.get_icon_path","title":"<code>get_icon_path(icon_name=None, filename=None)</code>","text":"<p>Path to image in './images' folder.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/icons.py</code> <pre><code>def get_icon_path(icon_name=None, filename=None):\n    \"\"\"Path to image in './images' folder.\"\"\"\n    if icon_name is None and filename is None:\n        return None\n\n    if filename is None:\n        filename = \"{}.png\".format(icon_name)\n\n    path = os.path.join(\n        os.path.dirname(os.path.abspath(__file__)),\n        \"images\",\n        filename\n    )\n    if os.path.exists(path):\n        return path\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/icons.html#client.ayon_core.tools.publisher.widgets.icons.get_image","title":"<code>get_image(icon_name=None, filename=None)</code>","text":"<p>Load image from './images' as QImage.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/icons.py</code> <pre><code>def get_image(icon_name=None, filename=None):\n    \"\"\"Load image from './images' as QImage.\"\"\"\n    path = get_icon_path(icon_name, filename)\n    if path:\n        return QtGui.QImage(path)\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/icons.html#client.ayon_core.tools.publisher.widgets.icons.get_pixmap","title":"<code>get_pixmap(icon_name=None, filename=None)</code>","text":"<p>Load image from './images' as QPixmap.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/icons.py</code> <pre><code>def get_pixmap(icon_name=None, filename=None):\n    \"\"\"Load image from './images' as QPixmap.\"\"\"\n    path = get_icon_path(icon_name, filename)\n    if path:\n        return QtGui.QPixmap(path)\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/list_view_widgets.html","title":"list_view_widgets","text":"<p>Simple easy instance view grouping instances into collapsible groups.</p> <p>View has multiselection ability. Groups are defined by <code>creator_label</code> attribute on instance (Group defined by creator).</p> <p>Each item can be enabled/disabled with their checkbox, whole group can be enabled/disabled with checkbox on group or selection can be enabled disabled using checkbox or keyboard key presses: - Space - change state of selection to opposite - Enter - enable selection - Backspace - disable selection</p> <pre><code>|- Context\n|- &lt;Group 1&gt; [x]\n|  |- &lt;Instance 1&gt; [x]\n|  |- &lt;Instance 2&gt; [x]\n|  ...\n|- &lt;Group 2&gt; [ ]\n|  |- &lt;Instance 3&gt; [ ]\n|  ...\n...\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/list_view_widgets.html#client.ayon_core.tools.publisher.widgets.list_view_widgets.InstanceListGroupWidget","title":"<code>InstanceListGroupWidget</code>","text":"<p>               Bases: <code>BaseClickableFrame</code></p> <p>Widget representing group of instances.</p> <p>Has label of group and checkbox modifying all of its children.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/list_view_widgets.py</code> <pre><code>class InstanceListGroupWidget(BaseClickableFrame):\n    \"\"\"Widget representing group of instances.\n\n    Has label of group and checkbox modifying all of its children.\n    \"\"\"\n    toggle_requested = QtCore.Signal(str, int)\n    expand_change_requested = QtCore.Signal(str)\n\n    def __init__(self, group_name, parent):\n        super().__init__(parent)\n        self.setObjectName(\"InstanceListGroupWidget\")\n\n        self.group_name = group_name\n\n        name_label = QtWidgets.QLabel(group_name, self)\n\n        toggle_checkbox = NiceCheckbox(parent=self)\n\n        layout = QtWidgets.QHBoxLayout(self)\n        layout.setContentsMargins(2, 0, 2, 0)\n        layout.addWidget(\n            name_label, 1, QtCore.Qt.AlignLeft | QtCore.Qt.AlignVCenter\n        )\n        layout.addWidget(toggle_checkbox, 0)\n\n        name_label.setAttribute(QtCore.Qt.WA_TranslucentBackground)\n\n        toggle_checkbox.stateChanged.connect(self._on_checkbox_change)\n\n        self._ignore_state_change = False\n\n        self._expected_checkstate = None\n\n        self.name_label = name_label\n        self.toggle_checkbox = toggle_checkbox\n\n    def set_checkstate(self, state):\n        \"\"\"Change checkstate of \"active\" checkbox.\n\n        Args:\n            state(QtCore.Qt.CheckState): Checkstate of checkbox. Have 3\n                variants Unchecked, Checked and PartiallyChecked.\n        \"\"\"\n\n        if self.checkstate() == state:\n            return\n        self._ignore_state_change = True\n        self.toggle_checkbox.setCheckState(state)\n        self._ignore_state_change = False\n\n    def checkstate(self):\n        \"\"\"Current checkstate of \"active\" checkbox.\"\"\"\n\n        return self.toggle_checkbox.checkState()\n\n    def set_active_toggle_enabled(self, enabled):\n        self.toggle_checkbox.setEnabled(enabled)\n\n    def _on_checkbox_change(self, state):\n        if not self._ignore_state_change:\n            self.toggle_requested.emit(self.group_name, state)\n\n    def _mouse_release_callback(self):\n        self.expand_change_requested.emit(self.group_name)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/list_view_widgets.html#client.ayon_core.tools.publisher.widgets.list_view_widgets.InstanceListGroupWidget.checkstate","title":"<code>checkstate()</code>","text":"<p>Current checkstate of \"active\" checkbox.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/list_view_widgets.py</code> <pre><code>def checkstate(self):\n    \"\"\"Current checkstate of \"active\" checkbox.\"\"\"\n\n    return self.toggle_checkbox.checkState()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/list_view_widgets.html#client.ayon_core.tools.publisher.widgets.list_view_widgets.InstanceListGroupWidget.set_checkstate","title":"<code>set_checkstate(state)</code>","text":"<p>Change checkstate of \"active\" checkbox.</p> <p>Parameters:</p> Name Type Description Default <code>state</code> <code>CheckState</code> <p>Checkstate of checkbox. Have 3 variants Unchecked, Checked and PartiallyChecked.</p> required Source code in <code>client/ayon_core/tools/publisher/widgets/list_view_widgets.py</code> <pre><code>def set_checkstate(self, state):\n    \"\"\"Change checkstate of \"active\" checkbox.\n\n    Args:\n        state(QtCore.Qt.CheckState): Checkstate of checkbox. Have 3\n            variants Unchecked, Checked and PartiallyChecked.\n    \"\"\"\n\n    if self.checkstate() == state:\n        return\n    self._ignore_state_change = True\n    self.toggle_checkbox.setCheckState(state)\n    self._ignore_state_change = False\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/list_view_widgets.html#client.ayon_core.tools.publisher.widgets.list_view_widgets.InstanceListItemWidget","title":"<code>InstanceListItemWidget</code>","text":"<p>               Bases: <code>QWidget</code></p> <p>Widget with instance info drawn over delegate paint.</p> <p>This is required to be able to use custom checkbox on custom place.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/list_view_widgets.py</code> <pre><code>class InstanceListItemWidget(QtWidgets.QWidget):\n    \"\"\"Widget with instance info drawn over delegate paint.\n\n    This is required to be able to use custom checkbox on custom place.\n    \"\"\"\n    active_changed = QtCore.Signal(str, bool)\n    double_clicked = QtCore.Signal()\n\n    def __init__(\n        self,\n        instance: InstanceItem,\n        context_info: InstanceContextInfo,\n        parent_is_active: bool,\n        parent: QtWidgets.QWidget,\n    ):\n        super().__init__(parent)\n\n        self._instance_id = instance.id\n\n        instance_label = instance.label\n        if instance_label is None:\n            # Do not cause UI crash if label is 'None'\n            instance_label = \"No label\"\n\n        instance_label = html_escape(instance_label)\n\n        product_name_label = QtWidgets.QLabel(instance_label, self)\n        product_name_label.setObjectName(\"ListViewProductName\")\n\n        active_checkbox = NiceCheckbox(parent=self)\n\n        layout = QtWidgets.QHBoxLayout(self)\n        layout.setContentsMargins(2, 0, 2, 0)\n        layout.addWidget(product_name_label)\n        layout.addStretch(1)\n        layout.addWidget(active_checkbox)\n\n        for widget in (\n            self,\n            product_name_label,\n            active_checkbox,\n        ):\n            widget.setAttribute(QtCore.Qt.WA_TranslucentBackground)\n\n        active_checkbox.stateChanged.connect(self._on_active_change)\n\n        self._instance_label_widget = product_name_label\n        self._active_checkbox = active_checkbox\n\n        # Instance info\n        self._has_valid_context = context_info.is_valid\n        self._is_mandatory = instance.is_mandatory\n        self._instance_is_active = instance.is_active\n        self._parent_flags = instance.parent_flags\n\n        # Parent active state is fluent and can change\n        self._parent_is_active = parent_is_active\n\n        # Widget logic info\n        self._state = None\n        self._toggle_is_enabled = True\n\n        self._update_style_state()\n        self._update_checkbox_state()\n\n    def mouseDoubleClickEvent(self, event):\n        widget = self.childAt(event.pos())\n        super().mouseDoubleClickEvent(event)\n        if widget is not self._active_checkbox:\n            self.double_clicked.emit()\n\n    def is_active(self) -&gt; bool:\n        \"\"\"Instance is activated.\"\"\"\n        return self._active_checkbox.isChecked()\n\n    def is_checkbox_enabled(self) -&gt; bool:\n        \"\"\"Checkbox can be changed by user.\"\"\"\n        return (\n            self._used_parent_active()\n            and not self._is_mandatory\n        )\n\n    def set_active_toggle_enabled(self, enabled: bool) -&gt; None:\n        \"\"\"Toggle can be available for user.\"\"\"\n        self._toggle_is_enabled = enabled\n        self._update_checkbox_state()\n\n    def set_active(self, new_value: Optional[bool]) -&gt; None:\n        \"\"\"Change active state of instance and checkbox by user interaction.\n\n        Args:\n            new_value (Optional[bool]): New active state of instance. Toggle\n                if is 'None'.\n\n        \"\"\"\n        # Do not allow to change state if is mandatory or parent is not active\n        if not self.is_checkbox_enabled():\n            return\n\n        if new_value is None:\n            new_value = not self._active_checkbox.isChecked()\n        # Update instance active state\n        self._instance_is_active = new_value\n        self._set_checked(new_value)\n\n    def update_instance(\n        self,\n        instance: InstanceItem,\n        context_info: InstanceContextInfo,\n        parent_is_active: bool,\n    ) -&gt; None:\n        \"\"\"Update instance object.\"\"\"\n        # Check product name\n        self._instance_id = instance.id\n        label = instance.label\n        if label != self._instance_label_widget.text():\n            self._instance_label_widget.setText(html_escape(label))\n\n        self._is_mandatory = instance.is_mandatory\n        self._instance_is_active = instance.is_active\n        self._has_valid_context = context_info.is_valid\n        self._parent_is_active = parent_is_active\n        self._parent_flags = instance.parent_flags\n\n        self._update_checkbox_state()\n        self._update_style_state()\n\n    def is_parent_active(self) -&gt; bool:\n        return self._parent_is_active\n\n    def _used_parent_active(self) -&gt; bool:\n        parent_enabled = True\n        if self._parent_flags &amp; ParentFlags.share_active:\n            parent_enabled = self._parent_is_active\n        return parent_enabled\n\n    def set_parent_is_active(self, active: bool) -&gt; None:\n        if self._parent_is_active is active:\n            return\n        self._parent_is_active = active\n        self._update_style_state()\n        self._update_checkbox_state()\n\n    def _set_checked(self, checked: bool) -&gt; None:\n        \"\"\"Change checked state in UI without triggering checkstate change.\"\"\"\n        old_value = self._active_checkbox.isChecked()\n        if checked is not old_value:\n            self._active_checkbox.blockSignals(True)\n            self._active_checkbox.setChecked(checked)\n            self._active_checkbox.blockSignals(False)\n\n    def _update_style_state(self) -&gt; None:\n        state = \"\"\n        if not self._used_parent_active():\n            state = \"disabled\"\n        elif not self._has_valid_context:\n            state = \"invalid\"\n\n        if state == self._state:\n            return\n        self._state = state\n        self._instance_label_widget.setProperty(\"state\", state)\n        self._instance_label_widget.style().polish(self._instance_label_widget)\n\n    def _update_checkbox_state(self) -&gt; None:\n        parent_enabled = self._used_parent_active()\n\n        self._active_checkbox.setEnabled(\n            self._toggle_is_enabled\n            and not self._is_mandatory\n            and parent_enabled\n        )\n        # Hide checkbox for mandatory instances\n        self._active_checkbox.setVisible(not self._is_mandatory)\n\n        # Visually disable instance if parent is disabled\n        checked = parent_enabled and self._instance_is_active\n        self._set_checked(checked)\n\n    def _on_active_change(self):\n        self.active_changed.emit(\n            self._instance_id, self._active_checkbox.isChecked()\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/list_view_widgets.html#client.ayon_core.tools.publisher.widgets.list_view_widgets.InstanceListItemWidget.is_active","title":"<code>is_active()</code>","text":"<p>Instance is activated.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/list_view_widgets.py</code> <pre><code>def is_active(self) -&gt; bool:\n    \"\"\"Instance is activated.\"\"\"\n    return self._active_checkbox.isChecked()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/list_view_widgets.html#client.ayon_core.tools.publisher.widgets.list_view_widgets.InstanceListItemWidget.is_checkbox_enabled","title":"<code>is_checkbox_enabled()</code>","text":"<p>Checkbox can be changed by user.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/list_view_widgets.py</code> <pre><code>def is_checkbox_enabled(self) -&gt; bool:\n    \"\"\"Checkbox can be changed by user.\"\"\"\n    return (\n        self._used_parent_active()\n        and not self._is_mandatory\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/list_view_widgets.html#client.ayon_core.tools.publisher.widgets.list_view_widgets.InstanceListItemWidget.set_active","title":"<code>set_active(new_value)</code>","text":"<p>Change active state of instance and checkbox by user interaction.</p> <p>Parameters:</p> Name Type Description Default <code>new_value</code> <code>Optional[bool]</code> <p>New active state of instance. Toggle if is 'None'.</p> required Source code in <code>client/ayon_core/tools/publisher/widgets/list_view_widgets.py</code> <pre><code>def set_active(self, new_value: Optional[bool]) -&gt; None:\n    \"\"\"Change active state of instance and checkbox by user interaction.\n\n    Args:\n        new_value (Optional[bool]): New active state of instance. Toggle\n            if is 'None'.\n\n    \"\"\"\n    # Do not allow to change state if is mandatory or parent is not active\n    if not self.is_checkbox_enabled():\n        return\n\n    if new_value is None:\n        new_value = not self._active_checkbox.isChecked()\n    # Update instance active state\n    self._instance_is_active = new_value\n    self._set_checked(new_value)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/list_view_widgets.html#client.ayon_core.tools.publisher.widgets.list_view_widgets.InstanceListItemWidget.set_active_toggle_enabled","title":"<code>set_active_toggle_enabled(enabled)</code>","text":"<p>Toggle can be available for user.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/list_view_widgets.py</code> <pre><code>def set_active_toggle_enabled(self, enabled: bool) -&gt; None:\n    \"\"\"Toggle can be available for user.\"\"\"\n    self._toggle_is_enabled = enabled\n    self._update_checkbox_state()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/list_view_widgets.html#client.ayon_core.tools.publisher.widgets.list_view_widgets.InstanceListItemWidget.update_instance","title":"<code>update_instance(instance, context_info, parent_is_active)</code>","text":"<p>Update instance object.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/list_view_widgets.py</code> <pre><code>def update_instance(\n    self,\n    instance: InstanceItem,\n    context_info: InstanceContextInfo,\n    parent_is_active: bool,\n) -&gt; None:\n    \"\"\"Update instance object.\"\"\"\n    # Check product name\n    self._instance_id = instance.id\n    label = instance.label\n    if label != self._instance_label_widget.text():\n        self._instance_label_widget.setText(html_escape(label))\n\n    self._is_mandatory = instance.is_mandatory\n    self._instance_is_active = instance.is_active\n    self._has_valid_context = context_info.is_valid\n    self._parent_is_active = parent_is_active\n    self._parent_flags = instance.parent_flags\n\n    self._update_checkbox_state()\n    self._update_style_state()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/list_view_widgets.html#client.ayon_core.tools.publisher.widgets.list_view_widgets.InstanceListView","title":"<code>InstanceListView</code>","text":"<p>               Bases: <code>AbstractInstanceView</code></p> <p>Widget providing abstract methods of AbstractInstanceView for list view.</p> <p>This is public access to and from list view.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/list_view_widgets.py</code> <pre><code>class InstanceListView(AbstractInstanceView):\n    \"\"\"Widget providing abstract methods of AbstractInstanceView for list view.\n\n    This is public access to and from list view.\n    \"\"\"\n\n    double_clicked = QtCore.Signal()\n\n    def __init__(\n        self, controller: AbstractPublisherFrontend, parent: QtWidgets.QWidget\n    ):\n        super().__init__(parent)\n\n        self._controller: AbstractPublisherFrontend = controller\n\n        instance_view = InstanceTreeView(self)\n        instance_delegate = ListItemDelegate(instance_view)\n        instance_view.setItemDelegate(instance_delegate)\n        instance_model = QtGui.QStandardItemModel()\n\n        proxy_model = QtCore.QSortFilterProxyModel()\n        proxy_model.setSourceModel(instance_model)\n        proxy_model.setFilterCaseSensitivity(QtCore.Qt.CaseInsensitive)\n        proxy_model.setSortRole(SORT_VALUE_ROLE)\n        proxy_model.setFilterKeyColumn(0)\n        proxy_model.setDynamicSortFilter(True)\n\n        instance_view.setModel(proxy_model)\n\n        layout = QtWidgets.QHBoxLayout(self)\n        layout.setContentsMargins(0, 0, 0, 0)\n        layout.addWidget(instance_view)\n\n        instance_view.selectionModel().selectionChanged.connect(\n            self._on_selection_change\n        )\n        instance_view.toggle_requested.connect(self._on_toggle_request)\n        instance_view.double_clicked.connect(self.double_clicked)\n\n        self._group_items = {}\n        self._group_widgets = {}\n        self._widgets_by_id: dict[str, InstanceListItemWidget] = {}\n        self._items_by_id = {}\n        self._parent_id_by_id = {}\n        self._instance_ids_by_parent_id = collections.defaultdict(set)\n        # Group by instance id for handling of active state\n        self._group_by_instance_id = {}\n        self._context_item = None\n        self._context_widget = None\n        self._missing_parent_item = None\n        self._parent_grouping = True\n\n        self._convertor_group_item = None\n        self._convertor_group_widget = None\n        self._convertor_items_by_id = {}\n\n        self._instance_view = instance_view\n        self._instance_delegate = instance_delegate\n        self._instance_model = instance_model\n        self._proxy_model = proxy_model\n\n        self._active_toggle_enabled = True\n\n    def _on_toggle_request(self, toggle: int) -&gt; None:\n        if not self._active_toggle_enabled:\n            return\n\n        if toggle == -1:\n            active = None\n        elif toggle == 1:\n            active = True\n        else:\n            active = False\n        self._toggle_active_state(active)\n\n    def _update_group_checkstate(self, group_name):\n        \"\"\"Update checkstate of one group.\"\"\"\n        widget = self._group_widgets.get(group_name)\n        if widget is None:\n            return\n\n        activity = None\n        for (\n            instance_id, instance_group_name\n        ) in self._group_by_instance_id.items():\n            if instance_group_name != group_name:\n                continue\n\n            instance_widget = self._widgets_by_id.get(instance_id)\n            if not instance_widget:\n                continue\n\n            if activity is None:\n                activity = int(instance_widget.is_active())\n\n            elif activity != instance_widget.is_active():\n                activity = -1\n                break\n\n        if activity is None:\n            return\n\n        state = QtCore.Qt.PartiallyChecked\n        if activity == 0:\n            state = QtCore.Qt.Unchecked\n        elif activity == 1:\n            state = QtCore.Qt.Checked\n        widget.set_checkstate(state)\n\n    def refresh(self):\n        \"\"\"Refresh instances in the view.\"\"\"\n        # Sort view at the end of refresh\n        # - is turned off until any change in view happens\n        sort_at_the_end = False\n        # Create or use already existing context item\n        # - context widget does not change so we don't have to update anything\n        if self._make_sure_context_item_exists():\n            sort_at_the_end = True\n\n        self._update_convertor_items_group()\n\n        context_info_by_id = self._controller.get_instances_context_info()\n        instance_items = self._controller.get_instance_items()\n        # Prepare instances by their groups\n        instances_by_group_name = collections.defaultdict(list)\n        instances_by_parent_id = collections.defaultdict(list)\n        instance_ids_by_parent_id = collections.defaultdict(set)\n        group_names = set()\n        instance_ids = set()\n        for instance in instance_items:\n            instance_ids.add(instance.id)\n            instance_ids_by_parent_id[instance.parent_instance_id].add(\n                instance.id\n            )\n            if instance.parent_instance_id:\n                instances_by_parent_id[instance.parent_instance_id].append(\n                    instance\n                )\n                if self._parent_grouping:\n                    continue\n\n            group_label = instance.group_label\n            group_names.add(group_label)\n            instances_by_group_name[group_label].append(instance)\n            self._group_by_instance_id[instance.id] = group_label\n\n        # Create new groups based on prepared `instances_by_group_name`\n        if self._make_sure_groups_exists(group_names):\n            sort_at_the_end = True\n\n        # Remove groups that are not available anymore\n        self._remove_groups_except(group_names)\n        self._remove_instances_except(instance_items)\n\n        expand_to_items = []\n        widgets_by_id = {}\n        group_items = [\n            (\n                self._group_widgets[group_name],\n                instances_by_group_name[group_name],\n                group_item,\n            )\n            for group_name, group_item in self._group_items.items()\n        ]\n\n        # Handle orphaned instances\n        missing_parent_ids = set(instances_by_parent_id) - instance_ids\n        if not missing_parent_ids:\n            # Make sure the item is not in view if there are no orhpaned items\n            self._remove_missing_parent_item()\n        else:\n            # Add orphaned group item and append them to 'group_items'\n            orphans_item = self._add_missing_parent_item()\n            for instance_id in missing_parent_ids:\n                group_items.append((\n                    None,\n                    instances_by_parent_id[instance_id],\n                    orphans_item,\n                ))\n\n        items_with_instance = {}\n        # Process changes in each group item\n        # - create new instance, update existing and remove not existing\n        for group_widget, group_instances, group_item in group_items:\n            # Group widget is not set if is orphaned\n            #   - This might need to be changed in future if widget could\n            #       be 'None'\n            is_orpaned_item = group_widget is None\n\n            # Collect all new instances by parent id\n            # - 'None' is used if parent is group item\n            new_items = collections.defaultdict(list)\n            # Tuples of model item and instance itself\n            for instance in group_instances:\n                _queue = collections.deque()\n                _queue.append((instance, group_item, None))\n                while _queue:\n                    instance, parent_item, parent_id = _queue.popleft()\n                    instance_id = instance.id\n                    # Remove group name from groups mapping\n                    if parent_id is not None:\n                        self._group_by_instance_id.pop(instance_id, None)\n\n                    # Create new item and store it as new\n                    item = self._items_by_id.get(instance_id)\n                    if item is None:\n                        item = QtGui.QStandardItem()\n                        item.setData(instance_id, INSTANCE_ID_ROLE)\n                        self._items_by_id[instance_id] = item\n                        new_items[parent_id].append(item)\n\n                    elif item.parent() is not parent_item:\n                        current_parent = item.parent()\n                        if current_parent is not None:\n                            current_parent.takeRow(item.row())\n                        new_items[parent_id].append(item)\n\n                    self._parent_id_by_id[instance_id] = parent_id\n\n                    items_with_instance[instance.id] = (\n                        item,\n                        instance,\n                        is_orpaned_item,\n                    )\n\n                    item.setData(instance.product_name, SORT_VALUE_ROLE)\n                    item.setData(instance.product_name, GROUP_ROLE)\n\n                    if not self._parent_grouping:\n                        continue\n\n                    children = instances_by_parent_id.pop(instance_id, [])\n                    for child in children:\n                        _queue.append((child, item, instance_id))\n\n            # Process new instance items and add them to model and create\n            #   their widgets\n            if new_items:\n                # Trigger sort at the end when new instances are available\n                sort_at_the_end = True\n\n                # Add items under group item\n                for parent_id, items in new_items.items():\n                    if parent_id is None or not self._parent_grouping:\n                        parent_item = group_item\n                    else:\n                        parent_item = self._items_by_id[parent_id]\n\n                    parent_item.appendRows(items)\n\n        ids_order = []\n        ids_queue = collections.deque()\n        ids_queue.extend(instance_ids_by_parent_id[None])\n        while ids_queue:\n            parent_id = ids_queue.popleft()\n            ids_order.append(parent_id)\n            ids_queue.extend(instance_ids_by_parent_id[parent_id])\n        ids_order.extend(set(items_with_instance) - set(ids_order))\n\n        for instance_id in ids_order:\n            item, instance, is_orpaned_item = items_with_instance[instance_id]\n            context_info = context_info_by_id[instance.id]\n            # TODO expand all parents\n            if not context_info.is_valid:\n                expand_to_items.append(item)\n\n            parent_active = True\n            if is_orpaned_item:\n                parent_active = False\n\n            parent_id = instance.parent_instance_id\n            if parent_id:\n                parent_widget = widgets_by_id.get(parent_id)\n                parent_active = False\n                if parent_widget is not None:\n                    parent_active = parent_widget.is_active()\n            item_index = self._instance_model.indexFromItem(item)\n            proxy_index = self._proxy_model.mapFromSource(item_index)\n            widget = self._instance_view.indexWidget(proxy_index)\n            if isinstance(widget, InstanceListItemWidget):\n                widget.update_instance(\n                    instance,\n                    context_info,\n                    parent_active,\n                )\n            else:\n                widget = InstanceListItemWidget(\n                    instance,\n                    context_info,\n                    parent_active,\n                    self._instance_view\n                )\n                widget.active_changed.connect(self._on_active_changed)\n                widget.double_clicked.connect(self.double_clicked)\n                self._instance_view.setIndexWidget(proxy_index, widget)\n            widget.set_active_toggle_enabled(\n                self._active_toggle_enabled\n            )\n\n            widgets_by_id[instance.id] = widget\n            self._widgets_by_id.pop(instance.id, None)\n\n        for widget in self._widgets_by_id.values():\n            widget.setVisible(False)\n            widget.deleteLater()\n\n        self._widgets_by_id = widgets_by_id\n        self._instance_ids_by_parent_id = instance_ids_by_parent_id\n\n        # Set checkstate of group checkbox\n        for group_name in self._group_items:\n            self._update_group_checkstate(group_name)\n\n        # Expand items marked for expanding\n        items_to_expand = []\n        _marked_ids = set()\n        for item in expand_to_items:\n            parent = item.parent()\n            _items = []\n            while True:\n                # Parent is not set or is group (groups are separate)\n                if parent is None or parent.data(IS_GROUP_ROLE):\n                    break\n                instance_id = parent.data(INSTANCE_ID_ROLE)\n                # Parent was already marked for expanding\n                if instance_id in _marked_ids:\n                    break\n                _marked_ids.add(instance_id)\n                _items.append(parent)\n                parent = parent.parent()\n\n            items_to_expand.extend(reversed(_items))\n\n        for item in items_to_expand:\n            proxy_index = self._proxy_model.mapFromSource(item.index())\n            self._instance_view.expand(proxy_index)\n\n        # Trigger sort at the end of refresh\n        if sort_at_the_end:\n            self._proxy_model.sort(0)\n\n    def _make_sure_context_item_exists(self) -&gt; bool:\n        if self._context_item is not None:\n            return False\n\n        root_item = self._instance_model.invisibleRootItem()\n        context_item = QtGui.QStandardItem()\n        context_item.setData(0, SORT_VALUE_ROLE)\n        context_item.setData(CONTEXT_ID, INSTANCE_ID_ROLE)\n\n        root_item.appendRow(context_item)\n\n        index = self._instance_model.index(\n            context_item.row(), context_item.column()\n        )\n        proxy_index = self._proxy_model.mapFromSource(index)\n        widget = ListContextWidget(self._instance_view)\n        widget.double_clicked.connect(self.double_clicked)\n        self._instance_view.setIndexWidget(proxy_index, widget)\n\n        self._context_widget = widget\n        self._context_item = context_item\n        return True\n\n    def _update_convertor_items_group(self) -&gt; bool:\n        created_new_items = False\n        convertor_items_by_id = self._controller.get_convertor_items()\n        group_item = self._convertor_group_item\n        if not convertor_items_by_id and group_item is None:\n            return created_new_items\n\n        root_item = self._instance_model.invisibleRootItem()\n        if not convertor_items_by_id:\n            root_item.takeRow(group_item.row())\n            self._convertor_group_widget.deleteLater()\n            self._convertor_group_widget = None\n            self._convertor_items_by_id = {}\n            return created_new_items\n\n        if group_item is None:\n            created_new_items = True\n            group_item = QtGui.QStandardItem()\n            group_item.setData(CONVERTOR_ITEM_GROUP, GROUP_ROLE)\n            group_item.setData(1, SORT_VALUE_ROLE)\n            group_item.setData(True, IS_GROUP_ROLE)\n            group_item.setFlags(QtCore.Qt.ItemIsEnabled)\n\n            root_item.appendRow(group_item)\n\n            index = self._instance_model.index(\n                group_item.row(), group_item.column()\n            )\n            proxy_index = self._proxy_model.mapFromSource(index)\n            widget = InstanceListGroupWidget(\n                CONVERTOR_ITEM_GROUP, self._instance_view\n            )\n            widget.toggle_checkbox.setVisible(False)\n\n            self._instance_view.setIndexWidget(proxy_index, widget)\n\n            self._convertor_group_item = group_item\n            self._convertor_group_widget = widget\n\n        for row in reversed(range(group_item.rowCount())):\n            child_item = group_item.child(row)\n            child_identifier = child_item.data(CONVERTER_IDENTIFIER_ROLE)\n            if child_identifier not in convertor_items_by_id:\n                self._convertor_items_by_id.pop(child_identifier, None)\n                group_item.takeRow(row)\n\n        new_items = []\n        for identifier, convertor_item in convertor_items_by_id.items():\n            item = self._convertor_items_by_id.get(identifier)\n            if item is None:\n                created_new_items = True\n                item = QtGui.QStandardItem(convertor_item.label)\n                new_items.append(item)\n            item.setData(convertor_item.id, INSTANCE_ID_ROLE)\n            item.setData(convertor_item.label, SORT_VALUE_ROLE)\n            item.setData(CONVERTOR_ITEM_GROUP, GROUP_ROLE)\n            item.setData(\n                convertor_item.identifier, CONVERTER_IDENTIFIER_ROLE\n            )\n            self._convertor_items_by_id[identifier] = item\n\n        if new_items:\n            group_item.appendRows(new_items)\n\n        return created_new_items\n\n    def _make_sure_groups_exists(self, group_names: set[str]) -&gt; bool:\n        new_group_items = []\n        for group_name in group_names:\n            if group_name in self._group_items:\n                continue\n\n            group_item = QtGui.QStandardItem()\n            group_item.setData(group_name, GROUP_ROLE)\n            group_item.setData(group_name, SORT_VALUE_ROLE)\n            group_item.setData(True, IS_GROUP_ROLE)\n            group_item.setFlags(QtCore.Qt.ItemIsEnabled)\n            self._group_items[group_name] = group_item\n            new_group_items.append(group_item)\n\n        # Add new group items to root item if there are any\n        if not new_group_items:\n            return False\n\n        # Access to root item of main model\n        root_item = self._instance_model.invisibleRootItem()\n        root_item.appendRows(new_group_items)\n\n        # Create widget for each new group item and store it for future usage\n        for group_item in new_group_items:\n            index = self._instance_model.index(\n                group_item.row(), group_item.column()\n            )\n            proxy_index = self._proxy_model.mapFromSource(index)\n            group_name = group_item.data(GROUP_ROLE)\n            widget = InstanceListGroupWidget(group_name, self._instance_view)\n            widget.set_active_toggle_enabled(\n                self._active_toggle_enabled\n            )\n            widget.toggle_requested.connect(self._on_group_toggle_request)\n            widget.expand_change_requested.connect(\n                self._on_expand_toggle_request\n            )\n            self._group_widgets[group_name] = widget\n            self._instance_view.setIndexWidget(proxy_index, widget)\n\n        return True\n\n    def _remove_groups_except(self, group_names: set[str]) -&gt; None:\n        # Remove groups that are not available anymore\n        root_item = self._instance_model.invisibleRootItem()\n        for group_name in tuple(self._group_items.keys()):\n            if group_name in group_names:\n                continue\n\n            group_item = self._group_items.pop(group_name)\n            root_item.takeRow(group_item.row())\n            widget = self._group_widgets.pop(group_name)\n            widget.setVisible(False)\n            widget.deleteLater()\n\n    def _remove_instances_except(self, instance_items: list[InstanceItem]):\n        parent_id_by_id = {\n            item.id: item.parent_instance_id\n            for item in instance_items\n        }\n        instance_ids = set(parent_id_by_id)\n        all_removed_ids = set(self._items_by_id) - instance_ids\n        queue = collections.deque()\n        for group_item in self._group_items.values():\n            queue.append((group_item, None))\n        while queue:\n            parent_item, parent_id = queue.popleft()\n            children = [\n                parent_item.child(row)\n                for row in range(parent_item.rowCount())\n            ]\n            for child in children:\n                instance_id = child.data(INSTANCE_ID_ROLE)\n                if instance_id not in parent_id_by_id:\n                    parent_item.takeRow(child.row())\n                elif parent_id != parent_id_by_id[instance_id]:\n                    parent_item.takeRow(child.row())\n\n                queue.append((child, instance_id))\n\n        for instance_id in all_removed_ids:\n            self._items_by_id.pop(instance_id)\n            self._parent_id_by_id.pop(instance_id)\n            self._group_by_instance_id.pop(instance_id, None)\n            widget = self._widgets_by_id.pop(instance_id, None)\n            if widget is not None:\n                widget.setVisible(False)\n                widget.deleteLater()\n\n    def _add_missing_parent_item(self) -&gt; QtGui.QStandardItem:\n        label = \"! Orphaned instances !\"\n        if self._missing_parent_item is None:\n            item = QtGui.QStandardItem()\n            item.setData(label, GROUP_ROLE)\n            item.setData(\"_\", SORT_VALUE_ROLE)\n            item.setData(True, IS_GROUP_ROLE)\n            item.setFlags(QtCore.Qt.ItemIsEnabled)\n            self._missing_parent_item = item\n\n        if self._missing_parent_item.row() &lt; 0:\n            root_item = self._instance_model.invisibleRootItem()\n            root_item.appendRow(self._missing_parent_item)\n            index = self._missing_parent_item.index()\n            proxy_index = self._proxy_model.mapFromSource(index)\n            widget = InstanceListGroupWidget(label, self._instance_view)\n            widget.toggle_checkbox.setVisible(False)\n            self._instance_view.setIndexWidget(proxy_index, widget)\n        return self._missing_parent_item\n\n    def _remove_missing_parent_item(self) -&gt; None:\n        if self._missing_parent_item is None:\n            return\n\n        row = self._missing_parent_item.row()\n        if row &lt; 0:\n            return\n\n        parent = self._missing_parent_item.parent()\n        if parent is None:\n            parent = self._instance_model.invisibleRootItem()\n        index = self._missing_parent_item.index()\n        proxy_index = self._proxy_model.mapFromSource(index)\n        widget = self._instance_view.indexWidget(proxy_index)\n        if widget is not None:\n            widget.setVisible(False)\n            widget.deleteLater()\n        parent.takeRow(self._missing_parent_item.row())\n        _queue = collections.deque()\n        _queue.append(self._missing_parent_item)\n        while _queue:\n            item = _queue.popleft()\n            for _ in range(item.rowCount()):\n                child = item.child(0)\n                _queue.append(child)\n                item.takeRow(0)\n\n        self._missing_parent_item = None\n\n    def refresh_instance_states(self, instance_ids=None):\n        \"\"\"Trigger update of all instances.\"\"\"\n        if instance_ids is not None:\n            instance_ids = set(instance_ids)\n\n        context_info_by_id = self._controller.get_instances_context_info(\n            instance_ids\n        )\n        instance_items_by_id = self._controller.get_instance_items_by_id(\n            instance_ids\n        )\n        instance_ids = set(instance_items_by_id)\n        available_ids = set(instance_ids)\n\n        _queue = collections.deque()\n        _queue.append((set(self._instance_ids_by_parent_id[None]), True))\n\n        discarted_ids = set()\n        while _queue:\n            if not instance_ids:\n                break\n\n            children_ids, parent_active = _queue.popleft()\n            for instance_id in children_ids:\n                widget = self._widgets_by_id[instance_id]\n                # Parent active state changed -&gt; traverse children too\n                add_children = False\n                if instance_id in instance_ids:\n                    add_children = (\n                        parent_active is not widget.is_parent_active()\n                    )\n                    if instance_id in available_ids:\n                        available_ids.discard(instance_id)\n                        widget.update_instance(\n                            instance_items_by_id[instance_id],\n                            context_info_by_id[instance_id],\n                            parent_active,\n                        )\n\n                    instance_ids.discard(instance_id)\n                    discarted_ids.add(instance_id)\n\n                if parent_active is not widget.is_parent_active():\n                    widget.set_parent_is_active(parent_active)\n                    add_children = True\n\n                if not add_children:\n                    if not instance_ids:\n                        break\n                    continue\n\n                _children = set(self._instance_ids_by_parent_id[instance_id])\n                if _children:\n                    instance_ids |= _children\n                    _queue.append((_children, widget.is_active()))\n\n                if not instance_ids:\n                    break\n\n    def parent_grouping_enabled(self) -&gt; bool:\n        return self._parent_grouping\n\n    def set_parent_grouping(self, parent_grouping: bool) -&gt; None:\n        self._parent_grouping = parent_grouping\n\n    def _on_active_changed(self, changed_instance_id, new_value):\n        self._toggle_active_state(new_value, changed_instance_id)\n\n    def _toggle_active_state(\n        self,\n        new_value: Optional[bool],\n        active_id: Optional[str] = None,\n        instance_ids: Optional[set[str]] = None,\n    ) -&gt; None:\n        if instance_ids is None:\n            instance_ids, _, _ = self.get_selected_items()\n        if active_id and active_id not in instance_ids:\n            instance_ids = {active_id}\n\n        active_by_id = {}\n        _queue = collections.deque()\n        _queue.append((set(self._instance_ids_by_parent_id[None]), True))\n\n        while _queue:\n            children_ids, parent_active = _queue.popleft()\n            for instance_id in children_ids:\n                widget = self._widgets_by_id[instance_id]\n                widget.set_parent_is_active(parent_active)\n                if instance_id in instance_ids:\n                    value = new_value\n                    if value is None:\n                        value = not widget.is_active()\n                    widget.set_active(value)\n                    active_by_id[instance_id] = value\n\n                children = set(\n                    self._instance_ids_by_parent_id[instance_id]\n                )\n                if children:\n                    _queue.append((children, widget.is_active()))\n\n        self._controller.set_instances_active_state(active_by_id)\n\n        group_names = set()\n        for instance_id in active_by_id:\n            group_name = self._group_by_instance_id.get(instance_id)\n            if group_name is not None:\n                group_names.add(group_name)\n\n        for group_name in group_names:\n            self._update_group_checkstate(group_name)\n\n    def _on_selection_change(self, *_args):\n        self.selection_changed.emit()\n\n    def _on_expand_toggle_request(self, group_name):\n        group_item = self._group_items.get(group_name)\n        if not group_item:\n            return\n        proxy_index = self._proxy_model.mapFromSource(group_item.index())\n        new_state = not self._instance_view.isExpanded(proxy_index)\n        self._instance_view.setExpanded(proxy_index, new_state)\n\n    def _on_group_toggle_request(self, group_name, state):\n        state = checkstate_int_to_enum(state)\n        if state == QtCore.Qt.PartiallyChecked:\n            return\n\n        group_item = self._group_items.get(group_name)\n        if not group_item:\n            return\n\n        active = state == QtCore.Qt.Checked\n\n        instance_ids = set()\n        for row in range(group_item.rowCount()):\n            child = group_item.child(row)\n            instance_id = child.data(INSTANCE_ID_ROLE)\n            instance_ids.add(instance_id)\n\n        self._toggle_active_state(active, instance_ids=instance_ids)\n\n        proxy_index = self._proxy_model.mapFromSource(group_item.index())\n        if not self._instance_view.isExpanded(proxy_index):\n            self._instance_view.expand(proxy_index)\n\n    def has_items(self) -&gt; bool:\n        if self._convertor_group_widget is not None:\n            return True\n        if self._group_items:\n            return True\n        return False\n\n    def get_selected_items(self) -&gt; tuple[list[str], bool, list[str]]:\n        \"\"\"Get selected instance ids and context selection.\n\n        Returns:\n            tuple[list[str], bool, list[str]]: Selected instance ids,\n                boolean if context is selected and selected convertor ids.\n\n        \"\"\"\n        instance_ids = []\n        convertor_identifiers = []\n        context_selected = False\n\n        for index in self._instance_view.selectionModel().selectedIndexes():\n            convertor_identifier = index.data(CONVERTER_IDENTIFIER_ROLE)\n            if convertor_identifier is not None:\n                convertor_identifiers.append(convertor_identifier)\n                continue\n\n            instance_id = index.data(INSTANCE_ID_ROLE)\n            if not context_selected and instance_id == CONTEXT_ID:\n                context_selected = True\n\n            elif instance_id is not None:\n                instance_ids.append(instance_id)\n\n        return instance_ids, context_selected, convertor_identifiers\n\n    def set_selected_items(\n        self, instance_ids, context_selected, convertor_identifiers\n    ):\n        s_instance_ids = set(instance_ids)\n        s_convertor_identifiers = set(convertor_identifiers)\n        cur_ids, cur_context, cur_convertor_identifiers = (\n            self.get_selected_items()\n        )\n        if (\n            set(cur_ids) == s_instance_ids\n            and cur_context == context_selected\n            and set(cur_convertor_identifiers) == s_convertor_identifiers\n        ):\n            return\n\n        view = self._instance_view\n        src_model = self._instance_model\n        proxy_model = self._proxy_model\n\n        select_indexes = []\n\n        select_queue = collections.deque()\n        select_queue.append(\n            (src_model.invisibleRootItem(), [])\n        )\n        while select_queue:\n            queue_item = select_queue.popleft()\n            item, parent_items = queue_item\n\n            if item.hasChildren():\n                new_parent_items = list(parent_items)\n                new_parent_items.append(item)\n                for row in range(item.rowCount()):\n                    select_queue.append(\n                        (item.child(row), list(new_parent_items))\n                    )\n\n            convertor_identifier = item.data(CONVERTER_IDENTIFIER_ROLE)\n\n            select = False\n            expand_parent = True\n            if convertor_identifier is not None:\n                if convertor_identifier in s_convertor_identifiers:\n                    select = True\n            else:\n                instance_id = item.data(INSTANCE_ID_ROLE)\n                if instance_id == CONTEXT_ID:\n                    if context_selected:\n                        select = True\n                        expand_parent = False\n\n                elif instance_id in s_instance_ids:\n                    select = True\n\n            if not select:\n                continue\n\n            select_indexes.append(item.index())\n            if not expand_parent:\n                continue\n\n            for parent_item in parent_items:\n                index = parent_item.index()\n                proxy_index = proxy_model.mapFromSource(index)\n                if not view.isExpanded(proxy_index):\n                    view.expand(proxy_index)\n\n        selection_model = view.selectionModel()\n        if not select_indexes:\n            selection_model.clear()\n            return\n\n        if len(select_indexes) == 1:\n            proxy_index = proxy_model.mapFromSource(select_indexes[0])\n            selection_model.setCurrentIndex(\n                proxy_index,\n                QtCore.QItemSelectionModel.ClearAndSelect\n                | QtCore.QItemSelectionModel.Rows\n            )\n            return\n\n        first_index = proxy_model.mapFromSource(select_indexes.pop(0))\n        last_index = proxy_model.mapFromSource(select_indexes.pop(-1))\n\n        selection_model.setCurrentIndex(\n            first_index,\n            QtCore.QItemSelectionModel.ClearAndSelect\n            | QtCore.QItemSelectionModel.Rows\n        )\n\n        for index in select_indexes:\n            proxy_index = proxy_model.mapFromSource(index)\n            selection_model.select(\n                proxy_index,\n                QtCore.QItemSelectionModel.Select\n                | QtCore.QItemSelectionModel.Rows\n            )\n\n        selection_model.setCurrentIndex(\n            last_index,\n            QtCore.QItemSelectionModel.Select\n            | QtCore.QItemSelectionModel.Rows\n        )\n\n    def set_active_toggle_enabled(self, enabled: bool) -&gt; None:\n        if self._active_toggle_enabled is enabled:\n            return\n\n        self._active_toggle_enabled = enabled\n        for widget in self._widgets_by_id.values():\n            if isinstance(widget, InstanceListItemWidget):\n                widget.set_active_toggle_enabled(enabled)\n\n        for widget in self._group_widgets.values():\n            if isinstance(widget, InstanceListGroupWidget):\n                widget.set_active_toggle_enabled(enabled)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/list_view_widgets.html#client.ayon_core.tools.publisher.widgets.list_view_widgets.InstanceListView.get_selected_items","title":"<code>get_selected_items()</code>","text":"<p>Get selected instance ids and context selection.</p> <p>Returns:</p> Type Description <code>tuple[list[str], bool, list[str]]</code> <p>tuple[list[str], bool, list[str]]: Selected instance ids, boolean if context is selected and selected convertor ids.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/list_view_widgets.py</code> <pre><code>def get_selected_items(self) -&gt; tuple[list[str], bool, list[str]]:\n    \"\"\"Get selected instance ids and context selection.\n\n    Returns:\n        tuple[list[str], bool, list[str]]: Selected instance ids,\n            boolean if context is selected and selected convertor ids.\n\n    \"\"\"\n    instance_ids = []\n    convertor_identifiers = []\n    context_selected = False\n\n    for index in self._instance_view.selectionModel().selectedIndexes():\n        convertor_identifier = index.data(CONVERTER_IDENTIFIER_ROLE)\n        if convertor_identifier is not None:\n            convertor_identifiers.append(convertor_identifier)\n            continue\n\n        instance_id = index.data(INSTANCE_ID_ROLE)\n        if not context_selected and instance_id == CONTEXT_ID:\n            context_selected = True\n\n        elif instance_id is not None:\n            instance_ids.append(instance_id)\n\n    return instance_ids, context_selected, convertor_identifiers\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/list_view_widgets.html#client.ayon_core.tools.publisher.widgets.list_view_widgets.InstanceListView.refresh","title":"<code>refresh()</code>","text":"<p>Refresh instances in the view.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/list_view_widgets.py</code> <pre><code>def refresh(self):\n    \"\"\"Refresh instances in the view.\"\"\"\n    # Sort view at the end of refresh\n    # - is turned off until any change in view happens\n    sort_at_the_end = False\n    # Create or use already existing context item\n    # - context widget does not change so we don't have to update anything\n    if self._make_sure_context_item_exists():\n        sort_at_the_end = True\n\n    self._update_convertor_items_group()\n\n    context_info_by_id = self._controller.get_instances_context_info()\n    instance_items = self._controller.get_instance_items()\n    # Prepare instances by their groups\n    instances_by_group_name = collections.defaultdict(list)\n    instances_by_parent_id = collections.defaultdict(list)\n    instance_ids_by_parent_id = collections.defaultdict(set)\n    group_names = set()\n    instance_ids = set()\n    for instance in instance_items:\n        instance_ids.add(instance.id)\n        instance_ids_by_parent_id[instance.parent_instance_id].add(\n            instance.id\n        )\n        if instance.parent_instance_id:\n            instances_by_parent_id[instance.parent_instance_id].append(\n                instance\n            )\n            if self._parent_grouping:\n                continue\n\n        group_label = instance.group_label\n        group_names.add(group_label)\n        instances_by_group_name[group_label].append(instance)\n        self._group_by_instance_id[instance.id] = group_label\n\n    # Create new groups based on prepared `instances_by_group_name`\n    if self._make_sure_groups_exists(group_names):\n        sort_at_the_end = True\n\n    # Remove groups that are not available anymore\n    self._remove_groups_except(group_names)\n    self._remove_instances_except(instance_items)\n\n    expand_to_items = []\n    widgets_by_id = {}\n    group_items = [\n        (\n            self._group_widgets[group_name],\n            instances_by_group_name[group_name],\n            group_item,\n        )\n        for group_name, group_item in self._group_items.items()\n    ]\n\n    # Handle orphaned instances\n    missing_parent_ids = set(instances_by_parent_id) - instance_ids\n    if not missing_parent_ids:\n        # Make sure the item is not in view if there are no orhpaned items\n        self._remove_missing_parent_item()\n    else:\n        # Add orphaned group item and append them to 'group_items'\n        orphans_item = self._add_missing_parent_item()\n        for instance_id in missing_parent_ids:\n            group_items.append((\n                None,\n                instances_by_parent_id[instance_id],\n                orphans_item,\n            ))\n\n    items_with_instance = {}\n    # Process changes in each group item\n    # - create new instance, update existing and remove not existing\n    for group_widget, group_instances, group_item in group_items:\n        # Group widget is not set if is orphaned\n        #   - This might need to be changed in future if widget could\n        #       be 'None'\n        is_orpaned_item = group_widget is None\n\n        # Collect all new instances by parent id\n        # - 'None' is used if parent is group item\n        new_items = collections.defaultdict(list)\n        # Tuples of model item and instance itself\n        for instance in group_instances:\n            _queue = collections.deque()\n            _queue.append((instance, group_item, None))\n            while _queue:\n                instance, parent_item, parent_id = _queue.popleft()\n                instance_id = instance.id\n                # Remove group name from groups mapping\n                if parent_id is not None:\n                    self._group_by_instance_id.pop(instance_id, None)\n\n                # Create new item and store it as new\n                item = self._items_by_id.get(instance_id)\n                if item is None:\n                    item = QtGui.QStandardItem()\n                    item.setData(instance_id, INSTANCE_ID_ROLE)\n                    self._items_by_id[instance_id] = item\n                    new_items[parent_id].append(item)\n\n                elif item.parent() is not parent_item:\n                    current_parent = item.parent()\n                    if current_parent is not None:\n                        current_parent.takeRow(item.row())\n                    new_items[parent_id].append(item)\n\n                self._parent_id_by_id[instance_id] = parent_id\n\n                items_with_instance[instance.id] = (\n                    item,\n                    instance,\n                    is_orpaned_item,\n                )\n\n                item.setData(instance.product_name, SORT_VALUE_ROLE)\n                item.setData(instance.product_name, GROUP_ROLE)\n\n                if not self._parent_grouping:\n                    continue\n\n                children = instances_by_parent_id.pop(instance_id, [])\n                for child in children:\n                    _queue.append((child, item, instance_id))\n\n        # Process new instance items and add them to model and create\n        #   their widgets\n        if new_items:\n            # Trigger sort at the end when new instances are available\n            sort_at_the_end = True\n\n            # Add items under group item\n            for parent_id, items in new_items.items():\n                if parent_id is None or not self._parent_grouping:\n                    parent_item = group_item\n                else:\n                    parent_item = self._items_by_id[parent_id]\n\n                parent_item.appendRows(items)\n\n    ids_order = []\n    ids_queue = collections.deque()\n    ids_queue.extend(instance_ids_by_parent_id[None])\n    while ids_queue:\n        parent_id = ids_queue.popleft()\n        ids_order.append(parent_id)\n        ids_queue.extend(instance_ids_by_parent_id[parent_id])\n    ids_order.extend(set(items_with_instance) - set(ids_order))\n\n    for instance_id in ids_order:\n        item, instance, is_orpaned_item = items_with_instance[instance_id]\n        context_info = context_info_by_id[instance.id]\n        # TODO expand all parents\n        if not context_info.is_valid:\n            expand_to_items.append(item)\n\n        parent_active = True\n        if is_orpaned_item:\n            parent_active = False\n\n        parent_id = instance.parent_instance_id\n        if parent_id:\n            parent_widget = widgets_by_id.get(parent_id)\n            parent_active = False\n            if parent_widget is not None:\n                parent_active = parent_widget.is_active()\n        item_index = self._instance_model.indexFromItem(item)\n        proxy_index = self._proxy_model.mapFromSource(item_index)\n        widget = self._instance_view.indexWidget(proxy_index)\n        if isinstance(widget, InstanceListItemWidget):\n            widget.update_instance(\n                instance,\n                context_info,\n                parent_active,\n            )\n        else:\n            widget = InstanceListItemWidget(\n                instance,\n                context_info,\n                parent_active,\n                self._instance_view\n            )\n            widget.active_changed.connect(self._on_active_changed)\n            widget.double_clicked.connect(self.double_clicked)\n            self._instance_view.setIndexWidget(proxy_index, widget)\n        widget.set_active_toggle_enabled(\n            self._active_toggle_enabled\n        )\n\n        widgets_by_id[instance.id] = widget\n        self._widgets_by_id.pop(instance.id, None)\n\n    for widget in self._widgets_by_id.values():\n        widget.setVisible(False)\n        widget.deleteLater()\n\n    self._widgets_by_id = widgets_by_id\n    self._instance_ids_by_parent_id = instance_ids_by_parent_id\n\n    # Set checkstate of group checkbox\n    for group_name in self._group_items:\n        self._update_group_checkstate(group_name)\n\n    # Expand items marked for expanding\n    items_to_expand = []\n    _marked_ids = set()\n    for item in expand_to_items:\n        parent = item.parent()\n        _items = []\n        while True:\n            # Parent is not set or is group (groups are separate)\n            if parent is None or parent.data(IS_GROUP_ROLE):\n                break\n            instance_id = parent.data(INSTANCE_ID_ROLE)\n            # Parent was already marked for expanding\n            if instance_id in _marked_ids:\n                break\n            _marked_ids.add(instance_id)\n            _items.append(parent)\n            parent = parent.parent()\n\n        items_to_expand.extend(reversed(_items))\n\n    for item in items_to_expand:\n        proxy_index = self._proxy_model.mapFromSource(item.index())\n        self._instance_view.expand(proxy_index)\n\n    # Trigger sort at the end of refresh\n    if sort_at_the_end:\n        self._proxy_model.sort(0)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/list_view_widgets.html#client.ayon_core.tools.publisher.widgets.list_view_widgets.InstanceListView.refresh_instance_states","title":"<code>refresh_instance_states(instance_ids=None)</code>","text":"<p>Trigger update of all instances.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/list_view_widgets.py</code> <pre><code>def refresh_instance_states(self, instance_ids=None):\n    \"\"\"Trigger update of all instances.\"\"\"\n    if instance_ids is not None:\n        instance_ids = set(instance_ids)\n\n    context_info_by_id = self._controller.get_instances_context_info(\n        instance_ids\n    )\n    instance_items_by_id = self._controller.get_instance_items_by_id(\n        instance_ids\n    )\n    instance_ids = set(instance_items_by_id)\n    available_ids = set(instance_ids)\n\n    _queue = collections.deque()\n    _queue.append((set(self._instance_ids_by_parent_id[None]), True))\n\n    discarted_ids = set()\n    while _queue:\n        if not instance_ids:\n            break\n\n        children_ids, parent_active = _queue.popleft()\n        for instance_id in children_ids:\n            widget = self._widgets_by_id[instance_id]\n            # Parent active state changed -&gt; traverse children too\n            add_children = False\n            if instance_id in instance_ids:\n                add_children = (\n                    parent_active is not widget.is_parent_active()\n                )\n                if instance_id in available_ids:\n                    available_ids.discard(instance_id)\n                    widget.update_instance(\n                        instance_items_by_id[instance_id],\n                        context_info_by_id[instance_id],\n                        parent_active,\n                    )\n\n                instance_ids.discard(instance_id)\n                discarted_ids.add(instance_id)\n\n            if parent_active is not widget.is_parent_active():\n                widget.set_parent_is_active(parent_active)\n                add_children = True\n\n            if not add_children:\n                if not instance_ids:\n                    break\n                continue\n\n            _children = set(self._instance_ids_by_parent_id[instance_id])\n            if _children:\n                instance_ids |= _children\n                _queue.append((_children, widget.is_active()))\n\n            if not instance_ids:\n                break\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/list_view_widgets.html#client.ayon_core.tools.publisher.widgets.list_view_widgets.InstanceTreeView","title":"<code>InstanceTreeView</code>","text":"<p>               Bases: <code>QTreeView</code></p> <p>View showing instances and their groups.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/list_view_widgets.py</code> <pre><code>class InstanceTreeView(QtWidgets.QTreeView):\n    \"\"\"View showing instances and their groups.\"\"\"\n    toggle_requested = QtCore.Signal(int)\n    double_clicked = QtCore.Signal()\n\n    def __init__(self, *args, **kwargs):\n        super().__init__(*args, **kwargs)\n\n        self.setIndentation(20)\n        self.setObjectName(\"InstanceListView\")\n        self.setHeaderHidden(True)\n        self.setExpandsOnDoubleClick(False)\n        self.setSelectionMode(\n            QtWidgets.QAbstractItemView.ExtendedSelection\n        )\n        self.viewport().setMouseTracking(True)\n\n    def get_selected_instance_ids(self) -&gt; set[str]:\n        \"\"\"Ids of selected instances.\"\"\"\n        instance_ids = set()\n        for index in self.selectionModel().selectedIndexes():\n            if index.data(CONVERTER_IDENTIFIER_ROLE) is not None:\n                continue\n\n            instance_id = index.data(INSTANCE_ID_ROLE)\n            if instance_id is not None:\n                instance_ids.add(instance_id)\n        return instance_ids\n\n    def event(self, event):\n        if not event.type() == QtCore.QEvent.KeyPress:\n            pass\n\n        elif event.key() == QtCore.Qt.Key_Space:\n            self.toggle_requested.emit(-1)\n            return True\n\n        elif event.key() == QtCore.Qt.Key_Backspace:\n            self.toggle_requested.emit(0)\n            return True\n\n        elif event.key() == QtCore.Qt.Key_Return:\n            self.toggle_requested.emit(1)\n            return True\n\n        return super().event(event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/list_view_widgets.html#client.ayon_core.tools.publisher.widgets.list_view_widgets.InstanceTreeView.get_selected_instance_ids","title":"<code>get_selected_instance_ids()</code>","text":"<p>Ids of selected instances.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/list_view_widgets.py</code> <pre><code>def get_selected_instance_ids(self) -&gt; set[str]:\n    \"\"\"Ids of selected instances.\"\"\"\n    instance_ids = set()\n    for index in self.selectionModel().selectedIndexes():\n        if index.data(CONVERTER_IDENTIFIER_ROLE) is not None:\n            continue\n\n        instance_id = index.data(INSTANCE_ID_ROLE)\n        if instance_id is not None:\n            instance_ids.add(instance_id)\n    return instance_ids\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/list_view_widgets.html#client.ayon_core.tools.publisher.widgets.list_view_widgets.ListContextWidget","title":"<code>ListContextWidget</code>","text":"<p>               Bases: <code>QFrame</code></p> <p>Context (or global attributes) widget.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/list_view_widgets.py</code> <pre><code>class ListContextWidget(QtWidgets.QFrame):\n    \"\"\"Context (or global attributes) widget.\"\"\"\n    double_clicked = QtCore.Signal()\n\n    def __init__(self, parent):\n        super().__init__(parent)\n\n        label_widget = QtWidgets.QLabel(CONTEXT_LABEL, self)\n\n        layout = QtWidgets.QHBoxLayout(self)\n        layout.setContentsMargins(5, 0, 2, 0)\n        layout.addWidget(\n            label_widget, 1, QtCore.Qt.AlignLeft | QtCore.Qt.AlignVCenter\n        )\n\n        self.setAttribute(QtCore.Qt.WA_TranslucentBackground)\n        label_widget.setAttribute(QtCore.Qt.WA_TranslucentBackground)\n\n        self.label_widget = label_widget\n\n    def mouseDoubleClickEvent(self, event):\n        super().mouseDoubleClickEvent(event)\n        self.double_clicked.emit()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/list_view_widgets.html#client.ayon_core.tools.publisher.widgets.list_view_widgets.ListItemDelegate","title":"<code>ListItemDelegate</code>","text":"<p>               Bases: <code>QStyledItemDelegate</code></p> <p>Generic delegate for instance group.</p> <p>All indexes having <code>IS_GROUP_ROLE</code> data set to True will use <code>group_item_paint</code> method to draw it's content otherwise default styled item delegate paint method is used.</p> <p>Goal is to draw group items with different colors for normal, hover and pressed state.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/list_view_widgets.py</code> <pre><code>class ListItemDelegate(QtWidgets.QStyledItemDelegate):\n    \"\"\"Generic delegate for instance group.\n\n    All indexes having `IS_GROUP_ROLE` data set to True will use\n    `group_item_paint` method to draw it's content otherwise default styled\n    item delegate paint method is used.\n\n    Goal is to draw group items with different colors for normal, hover and\n    pressed state.\n    \"\"\"\n    radius_ratio = 0.3\n\n    def __init__(self, parent):\n        super().__init__(parent)\n\n        group_color_info = get_objected_colors(\"publisher\", \"list-view-group\")\n\n        self._group_colors = {\n            key: value.get_qcolor()\n            for key, value in group_color_info.items()\n        }\n\n    def paint(self, painter, option, index):\n        if index.data(IS_GROUP_ROLE):\n            self.group_item_paint(painter, option, index)\n        else:\n            super().paint(painter, option, index)\n\n    def group_item_paint(self, painter, option, index):\n        \"\"\"Paint group item.\"\"\"\n        self.initStyleOption(option, index)\n\n        bg_rect = QtCore.QRectF(\n            option.rect.left(), option.rect.top() + 1,\n            option.rect.width(), option.rect.height() - 2\n        )\n        ratio = bg_rect.height() * self.radius_ratio\n        bg_path = QtGui.QPainterPath()\n        bg_path.addRoundedRect(\n            QtCore.QRectF(bg_rect), ratio, ratio\n        )\n\n        painter.save()\n        painter.setRenderHints(\n            QtGui.QPainter.Antialiasing\n            | QtGui.QPainter.SmoothPixmapTransform\n            | QtGui.QPainter.TextAntialiasing\n        )\n\n        # Draw backgrounds\n        painter.fillPath(bg_path, self._group_colors[\"bg\"])\n        selected = option.state &amp; QtWidgets.QStyle.State_Selected\n        hovered = option.state &amp; QtWidgets.QStyle.State_MouseOver\n        if selected and hovered:\n            painter.fillPath(bg_path, self._group_colors[\"bg-selected-hover\"])\n\n        elif hovered:\n            painter.fillPath(bg_path, self._group_colors[\"bg-hover\"])\n\n        painter.restore()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/list_view_widgets.html#client.ayon_core.tools.publisher.widgets.list_view_widgets.ListItemDelegate.group_item_paint","title":"<code>group_item_paint(painter, option, index)</code>","text":"<p>Paint group item.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/list_view_widgets.py</code> <pre><code>def group_item_paint(self, painter, option, index):\n    \"\"\"Paint group item.\"\"\"\n    self.initStyleOption(option, index)\n\n    bg_rect = QtCore.QRectF(\n        option.rect.left(), option.rect.top() + 1,\n        option.rect.width(), option.rect.height() - 2\n    )\n    ratio = bg_rect.height() * self.radius_ratio\n    bg_path = QtGui.QPainterPath()\n    bg_path.addRoundedRect(\n        QtCore.QRectF(bg_rect), ratio, ratio\n    )\n\n    painter.save()\n    painter.setRenderHints(\n        QtGui.QPainter.Antialiasing\n        | QtGui.QPainter.SmoothPixmapTransform\n        | QtGui.QPainter.TextAntialiasing\n    )\n\n    # Draw backgrounds\n    painter.fillPath(bg_path, self._group_colors[\"bg\"])\n    selected = option.state &amp; QtWidgets.QStyle.State_Selected\n    hovered = option.state &amp; QtWidgets.QStyle.State_MouseOver\n    if selected and hovered:\n        painter.fillPath(bg_path, self._group_colors[\"bg-selected-hover\"])\n\n    elif hovered:\n        painter.fillPath(bg_path, self._group_colors[\"bg-hover\"])\n\n    painter.restore()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/overview_widget.html","title":"overview_widget","text":""},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/overview_widget.html#client.ayon_core.tools.publisher.widgets.overview_widget.OverviewWidget","title":"<code>OverviewWidget</code>","text":"<p>               Bases: <code>QFrame</code></p> Source code in <code>client/ayon_core/tools/publisher/widgets/overview_widget.py</code> <pre><code>class OverviewWidget(QtWidgets.QFrame):\n    create_requested = QtCore.Signal()\n    convert_requested = QtCore.Signal()\n    publish_tab_requested = QtCore.Signal()\n\n    anim_end_value = 200\n    anim_duration = 200\n\n    def __init__(\n        self, controller: AbstractPublisherFrontend, parent: QtWidgets.QWidget\n    ):\n        super().__init__(parent)\n\n        self._refreshing_instances = False\n        self._controller: AbstractPublisherFrontend = controller\n\n        product_content_widget = QtWidgets.QWidget(self)\n\n        create_widget = CreateWidget(controller, product_content_widget)\n\n        # --- Created Products/Instances ---\n        # Common widget for creation and overview\n        product_views_widget = BorderedLabelWidget(\n            \"Products to publish\",\n            product_content_widget\n        )\n\n        product_view_cards = InstanceCardView(controller, product_views_widget)\n        product_list_view = InstanceListView(controller, product_views_widget)\n        product_list_view.set_parent_grouping(False)\n        product_list_view_grouped = InstanceListView(\n            controller, product_views_widget\n        )\n        product_list_view_grouped.set_parent_grouping(True)\n\n        product_views_layout = QtWidgets.QStackedLayout()\n        product_views_layout.addWidget(product_view_cards)\n        product_views_layout.addWidget(product_list_view)\n        product_views_layout.addWidget(product_list_view_grouped)\n        product_views_layout.setCurrentWidget(product_view_cards)\n\n        # Buttons at the bottom of product view\n        create_btn = CreateInstanceBtn(product_views_widget)\n        delete_btn = RemoveInstanceBtn(product_views_widget)\n        change_view_btn = ChangeViewBtn(product_views_widget)\n\n        # --- Overview ---\n        # Product details widget\n        product_attributes_wrap = BorderedLabelWidget(\n            \"Publish options\", product_content_widget\n        )\n        product_attributes_wrap.setVisible(False)\n        product_attributes_widget = ProductInfoWidget(\n            controller, product_attributes_wrap\n        )\n        product_attributes_wrap.set_center_widget(product_attributes_widget)\n\n        # Layout of buttons at the bottom of product view\n        product_view_btns_layout = QtWidgets.QHBoxLayout()\n        product_view_btns_layout.setContentsMargins(0, 5, 0, 0)\n        product_view_btns_layout.addWidget(create_btn)\n        product_view_btns_layout.addSpacing(5)\n        product_view_btns_layout.addWidget(delete_btn)\n        product_view_btns_layout.addStretch(1)\n        product_view_btns_layout.addWidget(change_view_btn)\n\n        # Layout of view and buttons\n        # - widget 'product_view_widget' is necessary\n        # - only layout won't be resized automatically to minimum size hint\n        #   on child resize request!\n        product_view_widget = QtWidgets.QWidget(product_views_widget)\n        product_view_layout = QtWidgets.QVBoxLayout(product_view_widget)\n        product_view_layout.setContentsMargins(0, 0, 0, 0)\n        product_view_layout.addLayout(product_views_layout, 1)\n        product_view_layout.addLayout(product_view_btns_layout, 0)\n\n        product_views_widget.set_center_widget(product_view_widget)\n\n        # Whole product layout with attributes and details\n        product_content_layout = QtWidgets.QHBoxLayout(product_content_widget)\n        product_content_layout.setContentsMargins(0, 0, 0, 0)\n        product_content_layout.addWidget(create_widget, 7)\n        product_content_layout.addWidget(product_views_widget, 3)\n        product_content_layout.addWidget(product_attributes_wrap, 7)\n\n        # Product frame layout\n        main_layout = QtWidgets.QVBoxLayout(self)\n        main_layout.setContentsMargins(0, 0, 0, 0)\n        main_layout.addWidget(product_content_widget, 1)\n\n        change_anim = QtCore.QVariantAnimation()\n        change_anim.setStartValue(float(0))\n        change_anim.setEndValue(float(self.anim_end_value))\n        change_anim.setDuration(self.anim_duration)\n        change_anim.setEasingCurve(QtCore.QEasingCurve.InOutQuad)\n\n        # --- Calbacks for instances/products view ---\n        create_btn.clicked.connect(self._on_create_clicked)\n        delete_btn.clicked.connect(self._on_delete_clicked)\n        change_view_btn.clicked.connect(self._on_change_view_clicked)\n\n        change_anim.valueChanged.connect(self._on_change_anim)\n        change_anim.finished.connect(self._on_change_anim_finished)\n\n        # Selection changed\n        product_list_view.selection_changed.connect(\n            self._on_product_change\n        )\n        product_list_view.double_clicked.connect(\n            self.publish_tab_requested\n        )\n        product_list_view_grouped.selection_changed.connect(\n            self._on_product_change\n        )\n        product_list_view_grouped.double_clicked.connect(\n            self.publish_tab_requested\n        )\n        product_view_cards.selection_changed.connect(\n            self._on_product_change\n        )\n        product_view_cards.double_clicked.connect(\n            self.publish_tab_requested\n        )\n        # Instance context has changed\n        product_attributes_widget.convert_requested.connect(\n            self._on_convert_requested\n        )\n\n        # --- Controller callbacks ---\n        controller.register_event_callback(\n            \"publish.process.started\", self._on_publish_start\n        )\n        controller.register_event_callback(\n            \"controller.reset.started\", self._on_controller_reset_start\n        )\n        controller.register_event_callback(\n            \"publish.reset.finished\", self._on_publish_reset\n        )\n        controller.register_event_callback(\n            \"create.model.reset\",\n            self._on_create_model_reset\n        )\n        controller.register_event_callback(\n            \"create.context.added.instance\",\n            self._on_instances_added\n        )\n        controller.register_event_callback(\n            \"create.context.removed.instance\",\n            self._on_instances_removed\n        )\n        controller.register_event_callback(\n            \"create.model.instances.context.changed\",\n            self._on_instance_context_change\n        )\n        controller.register_event_callback(\n            \"create.model.instance.requirement.changed\",\n            self._on_instance_requirement_changed\n        )\n        controller.register_event_callback(\n            \"create.model.instance.parent.changed\",\n            self._on_instance_parent_changed\n        )\n\n        self._product_content_widget = product_content_widget\n        self._product_content_layout = product_content_layout\n\n        self._product_view_cards = product_view_cards\n        self._product_list_view = product_list_view\n        self._product_list_view_grouped = product_list_view_grouped\n        self._product_views_layout = product_views_layout\n\n        self._create_btn = create_btn\n        self._delete_btn = delete_btn\n        self._change_view_btn = change_view_btn\n\n        self._product_attributes_widget = product_attributes_widget\n        self._create_widget = create_widget\n        self._product_views_widget = product_views_widget\n        self._product_attributes_wrap = product_attributes_wrap\n\n        self._change_anim = change_anim\n\n        # Start in create mode\n        self._current_state = \"create\"\n        product_attributes_wrap.setVisible(False)\n\n    def make_sure_animation_is_finished(self):\n        if self._change_anim.state() == QtCore.QAbstractAnimation.Running:\n            self._change_anim.stop()\n        self._on_change_anim_finished()\n\n    def set_state(self, new_state, animate):\n        if new_state == self._current_state:\n            return\n\n        self._current_state = new_state\n\n        if not animate:\n            self.make_sure_animation_is_finished()\n            return\n\n        if new_state == \"create\":\n            direction = QtCore.QAbstractAnimation.Backward\n        else:\n            direction = QtCore.QAbstractAnimation.Forward\n        self._change_anim.setDirection(direction)\n\n        if (\n            self._change_anim.state() != QtCore.QAbstractAnimation.Running\n        ):\n            self._start_animation()\n\n    def _start_animation(self):\n        views_geo = self._product_views_widget.geometry()\n        layout_spacing = self._product_content_layout.spacing()\n        if self._create_widget.isVisible():\n            create_geo = self._create_widget.geometry()\n            product_geo = QtCore.QRect(create_geo)\n            product_geo.moveTop(views_geo.top())\n            product_geo.moveLeft(views_geo.right() + layout_spacing)\n            self._product_attributes_wrap.setVisible(True)\n\n        elif self._product_attributes_wrap.isVisible():\n            product_geo = self._product_attributes_wrap.geometry()\n            create_geo = QtCore.QRect(product_geo)\n            create_geo.moveTop(views_geo.top())\n            create_geo.moveRight(views_geo.left() - (layout_spacing + 1))\n            self._create_widget.setVisible(True)\n        else:\n            self._change_anim.start()\n            return\n\n        while self._product_content_layout.count():\n            self._product_content_layout.takeAt(0)\n        self._product_views_widget.setGeometry(views_geo)\n        self._create_widget.setGeometry(create_geo)\n        self._product_attributes_wrap.setGeometry(product_geo)\n\n        self._change_anim.start()\n\n    def get_product_views_geo(self):\n        parent = self._product_views_widget.parent()\n        global_pos = parent.mapToGlobal(self._product_views_widget.pos())\n        return QtCore.QRect(\n            global_pos.x(),\n            global_pos.y(),\n            self._product_views_widget.width(),\n            self._product_views_widget.height()\n        )\n\n    def has_items(self):\n        view = self._get_current_view()\n        return view.has_items()\n\n    def _on_create_clicked(self):\n        \"\"\"Pass signal to parent widget which should care about changing state.\n\n        We don't change anything here until the parent will care about it.\n        \"\"\"\n\n        self.create_requested.emit()\n\n    def _on_delete_clicked(self):\n        instance_ids, _, _ = self.get_selected_items()\n\n        # Ask user if he really wants to remove instances\n        dialog = QtWidgets.QMessageBox(self)\n        dialog.setIcon(QtWidgets.QMessageBox.Question)\n        dialog.setWindowTitle(\"Are you sure?\")\n        if len(instance_ids) &gt; 1:\n            msg = (\n                \"Do you really want to remove {} instances?\"\n            ).format(len(instance_ids))\n        else:\n            msg = (\n                \"Do you really want to remove the instance?\"\n            )\n        dialog.setText(msg)\n        dialog.setStandardButtons(\n            QtWidgets.QMessageBox.Ok | QtWidgets.QMessageBox.Cancel\n        )\n        dialog.setDefaultButton(QtWidgets.QMessageBox.Ok)\n        dialog.setEscapeButton(QtWidgets.QMessageBox.Cancel)\n        dialog.exec_()\n        # Skip if OK was not clicked\n        if dialog.result() == QtWidgets.QMessageBox.Ok:\n            instance_ids = set(instance_ids)\n            self._controller.remove_instances(instance_ids)\n\n    def _on_change_view_clicked(self):\n        self._change_view_type()\n\n    def _on_product_change(self, *_args):\n        # Ignore changes if in middle of refreshing\n        if self._refreshing_instances:\n            return\n\n        instance_ids, context_selected, convertor_identifiers = (\n            self.get_selected_items()\n        )\n\n        # Disable delete button if nothing is selected\n        self._delete_btn.setEnabled(len(instance_ids) &gt; 0)\n        instances_by_id = self._controller.get_instance_items_by_id(\n            instance_ids\n        )\n        instances = [\n            instances_by_id[instance_id]\n            for instance_id in instance_ids\n        ]\n        self._product_attributes_widget.set_current_instances(\n            instances, context_selected, convertor_identifiers\n        )\n\n    def _on_change_anim(self, value):\n        self._create_widget.setVisible(True)\n        self._product_attributes_wrap.setVisible(True)\n        layout_spacing = self._product_content_layout.spacing()\n\n        content_width = (\n            self._product_content_widget.width() - (layout_spacing * 2)\n        )\n        content_height = self._product_content_widget.height()\n        views_width = max(\n            int(content_width * 0.3),\n            self._product_views_widget.minimumWidth()\n        )\n        width = content_width - views_width\n        # Visible widths of other widgets\n        product_attrs_width = int((float(width) / self.anim_end_value) * value)\n        create_width = width - product_attrs_width\n\n        views_geo = QtCore.QRect(\n            create_width + layout_spacing, 0,\n            views_width, content_height\n        )\n        create_geo = QtCore.QRect(0, 0, width, content_height)\n        product_attrs_geo = QtCore.QRect(create_geo)\n        create_geo.moveRight(views_geo.left() - (layout_spacing + 1))\n        product_attrs_geo.moveLeft(views_geo.right() + layout_spacing)\n\n        self._product_views_widget.setGeometry(views_geo)\n        self._create_widget.setGeometry(create_geo)\n        self._product_attributes_wrap.setGeometry(product_attrs_geo)\n\n    def _on_change_anim_finished(self):\n        self._change_visibility_for_state()\n        self._product_content_layout.addWidget(self._create_widget, 7)\n        self._product_content_layout.addWidget(self._product_views_widget, 3)\n        self._product_content_layout.addWidget(\n            self._product_attributes_wrap, 7\n        )\n\n    def _change_visibility_for_state(self):\n        self._create_widget.setVisible(\n            self._current_state == \"create\"\n        )\n        self._product_attributes_wrap.setVisible(\n            self._current_state == \"publish\"\n        )\n\n    def _on_instance_context_change(self, event):\n        self._refresh_instance_states(event[\"instance_ids\"])\n\n    def _on_instance_requirement_changed(self, event):\n        self._refresh_instance_states(event[\"instance_ids\"])\n\n    def _on_instance_parent_changed(self, event):\n        self._refresh_instance_states(event[\"instance_ids\"])\n\n    def _refresh_instance_states(self, instance_ids):\n        current_view = self._get_current_view()\n        for view in self._iter_views():\n            if view is current_view:\n                current_view = view\n            elif view.refreshed:\n                view.set_refreshed(False)\n\n        current_view.refresh_instance_states(instance_ids)\n\n    def _on_convert_requested(self):\n        self.convert_requested.emit()\n\n    def get_selected_items(self):\n        \"\"\"Selected items in current view widget.\n\n        Returns:\n            tuple[list[str], bool, list[str]]: Selected items. List of\n                instance ids, context is selected, list of selected legacy\n                convertor plugins.\n        \"\"\"\n\n        view = self._get_current_view()\n        return view.get_selected_items()\n\n    def get_selected_legacy_convertors(self):\n        \"\"\"Selected legacy convertor identifiers.\n\n        Returns:\n            list[str]: Selected legacy convertor identifiers.\n                Example: ['io.ayon.creators.houdini.legacy']\n        \"\"\"\n\n        _, _, convertor_identifiers = self.get_selected_items()\n        return convertor_identifiers\n\n    def _change_view_type(self):\n        old_view = self._get_current_view()\n\n        idx = self._product_views_layout.currentIndex()\n        new_idx = (idx + 1) % self._product_views_layout.count()\n\n        new_view = self._get_view_by_idx(new_idx)\n        if not new_view.refreshed:\n            new_view.refresh()\n            new_view.set_refreshed(True)\n        else:\n            new_view.refresh_instance_states()\n\n        instance_ids, context_selected, convertor_identifiers = (\n            old_view.get_selected_items()\n        )\n        new_view.set_selected_items(\n            instance_ids, context_selected, convertor_identifiers\n        )\n        view_type = \"list\"\n        if new_view is self._product_list_view_grouped:\n            view_type = \"card\"\n        elif new_view is self._product_list_view:\n            view_type = \"list-parent-grouping\"\n\n        self._change_view_btn.set_view_type(view_type)\n        self._product_views_layout.setCurrentIndex(new_idx)\n\n        self._on_product_change()\n\n    def _iter_views(self) -&gt; Generator[AbstractInstanceView, None, None]:\n        for idx in range(self._product_views_layout.count()):\n            widget = self._product_views_layout.widget(idx)\n            if not isinstance(widget, AbstractInstanceView):\n                raise TypeError(\n                    \"Current widget is not instance of 'AbstractInstanceView'\"\n                )\n            yield widget\n\n    def _get_current_view(self) -&gt; AbstractInstanceView:\n        widget = self._product_views_layout.currentWidget()\n        if isinstance(widget, AbstractInstanceView):\n            return widget\n        raise TypeError(\n            \"Current widget is not instance of 'AbstractInstanceView'\"\n        )\n\n    def _get_view_by_idx(self, idx: int) -&gt; AbstractInstanceView:\n        widget = self._product_views_layout.widget(idx)\n        if isinstance(widget, AbstractInstanceView):\n            return widget\n        raise TypeError(\n            \"Current widget is not instance of 'AbstractInstanceView'\"\n        )\n\n    def _refresh_instances(self):\n        if self._refreshing_instances:\n            return\n\n        self._refreshing_instances = True\n\n        for view in self._iter_views():\n            view.set_refreshed(False)\n\n        view = self._get_current_view()\n        view.refresh()\n        view.set_refreshed(True)\n\n        self._refreshing_instances = False\n\n        # Force to change instance and refresh details\n        self._on_product_change()\n\n        # Give a change to process Resize Request\n        QtWidgets.QApplication.processEvents()\n        # Trigger update geometry\n        view.updateGeometry()\n\n    def _on_publish_start(self):\n        \"\"\"Publish started.\"\"\"\n\n        self._create_btn.setEnabled(False)\n        self._product_attributes_wrap.setEnabled(False)\n        for view in self._iter_views():\n            view.set_active_toggle_enabled(False)\n\n    def _on_controller_reset_start(self):\n        \"\"\"Controller reset started.\"\"\"\n\n        for view in self._iter_views():\n            view.set_active_toggle_enabled(True)\n\n    def _on_publish_reset(self):\n        \"\"\"Context in controller has been reseted.\"\"\"\n\n        self._create_btn.setEnabled(True)\n        self._product_attributes_wrap.setEnabled(True)\n        self._product_content_widget.setEnabled(\n            self._controller.is_host_valid()\n        )\n\n    def _on_create_model_reset(self):\n        self._refresh_instances()\n\n    def _on_instances_added(self):\n        view = self._get_current_view()\n        is_card_view = False\n        count = 0\n        if isinstance(view, InstanceCardView):\n            is_card_view = True\n            count = view.get_current_instance_count()\n\n        self._refresh_instances()\n\n        if is_card_view and count &lt; 10:\n            new_count = view.get_current_instance_count()\n            if new_count &gt; count and new_count &gt;= 10:\n                self._change_view_type()\n\n    def _on_instances_removed(self):\n        self._refresh_instances()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/overview_widget.html#client.ayon_core.tools.publisher.widgets.overview_widget.OverviewWidget.get_selected_items","title":"<code>get_selected_items()</code>","text":"<p>Selected items in current view widget.</p> <p>Returns:</p> Type Description <p>tuple[list[str], bool, list[str]]: Selected items. List of instance ids, context is selected, list of selected legacy convertor plugins.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/overview_widget.py</code> <pre><code>def get_selected_items(self):\n    \"\"\"Selected items in current view widget.\n\n    Returns:\n        tuple[list[str], bool, list[str]]: Selected items. List of\n            instance ids, context is selected, list of selected legacy\n            convertor plugins.\n    \"\"\"\n\n    view = self._get_current_view()\n    return view.get_selected_items()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/overview_widget.html#client.ayon_core.tools.publisher.widgets.overview_widget.OverviewWidget.get_selected_legacy_convertors","title":"<code>get_selected_legacy_convertors()</code>","text":"<p>Selected legacy convertor identifiers.</p> <p>Returns:</p> Type Description <p>list[str]: Selected legacy convertor identifiers. Example: ['io.ayon.creators.houdini.legacy']</p> Source code in <code>client/ayon_core/tools/publisher/widgets/overview_widget.py</code> <pre><code>def get_selected_legacy_convertors(self):\n    \"\"\"Selected legacy convertor identifiers.\n\n    Returns:\n        list[str]: Selected legacy convertor identifiers.\n            Example: ['io.ayon.creators.houdini.legacy']\n    \"\"\"\n\n    _, _, convertor_identifiers = self.get_selected_items()\n    return convertor_identifiers\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/precreate_widget.html","title":"precreate_widget","text":""},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/product_attributes.html","title":"product_attributes","text":""},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/product_attributes.html#client.ayon_core.tools.publisher.widgets.product_attributes.CreatorAttrsWidget","title":"<code>CreatorAttrsWidget</code>","text":"<p>               Bases: <code>QWidget</code></p> <p>Widget showing creator specific attributes for selected instances.</p> <p>Attributes are defined on creator so are dynamic. Their look and type is based on attribute definitions that are defined in <code>~/ayon_core/lib/attribute_definitions.py</code> and their widget representation in <code>~/ayon_core/tools/attribute_defs/*</code>.</p> <p>Widgets are disabled if context of instance is not valid.</p> <p>Definitions are shown for all instance no matter if they are created with different creators. If creator have same (similar) definitions their widgets are merged into one (different label does not count).</p> Source code in <code>client/ayon_core/tools/publisher/widgets/product_attributes.py</code> <pre><code>class CreatorAttrsWidget(QtWidgets.QWidget):\n    \"\"\"Widget showing creator specific attributes for selected instances.\n\n    Attributes are defined on creator so are dynamic. Their look and type is\n    based on attribute definitions that are defined in\n    `~/ayon_core/lib/attribute_definitions.py` and their widget\n    representation in `~/ayon_core/tools/attribute_defs/*`.\n\n    Widgets are disabled if context of instance is not valid.\n\n    Definitions are shown for all instance no matter if they are created with\n    different creators. If creator have same (similar) definitions their\n    widgets are merged into one (different label does not count).\n    \"\"\"\n\n    def __init__(\n        self, controller: AbstractPublisherFrontend, parent: QtWidgets.QWidget\n    ):\n        super().__init__(parent)\n\n        scroll_area = QtWidgets.QScrollArea(self)\n        scroll_area.setWidgetResizable(True)\n\n        main_layout = QtWidgets.QHBoxLayout(self)\n        main_layout.setContentsMargins(0, 0, 0, 0)\n        main_layout.setSpacing(0)\n        main_layout.addWidget(scroll_area, 1)\n\n        controller.register_event_callback(\n            \"create.context.create.attrs.changed\",\n            self._on_instance_attr_defs_change\n        )\n        controller.register_event_callback(\n            \"create.context.value.changed\",\n            self._on_instance_value_change\n        )\n\n        self._main_layout = main_layout\n\n        self._controller: AbstractPublisherFrontend = controller\n        self._scroll_area = scroll_area\n\n        self._attr_def_info_by_id: Dict[str, _CreateAttrDefInfo] = {}\n        self._current_instance_ids = set()\n\n        # To store content of scroll area to prevent garbage collection\n        self._content_widget = None\n\n    def set_instances_valid(self, valid):\n        \"\"\"Change valid state of current instances.\"\"\"\n\n        if (\n            self._content_widget is not None\n            and self._content_widget.isEnabled() != valid\n        ):\n            self._content_widget.setEnabled(valid)\n\n    def set_current_instances(self, instance_ids):\n        \"\"\"Set current instances for which are attribute definitions shown.\"\"\"\n\n        self._current_instance_ids = set(instance_ids)\n        self._refresh_content()\n\n    def _refresh_content(self):\n        prev_content_widget = self._scroll_area.widget()\n        if prev_content_widget:\n            self._scroll_area.takeWidget()\n            prev_content_widget.hide()\n            prev_content_widget.deleteLater()\n\n        self._content_widget = None\n        self._attr_def_info_by_id = {}\n\n        result = self._controller.get_creator_attribute_definitions(\n            self._current_instance_ids\n        )\n\n        content_widget = QtWidgets.QWidget(self._scroll_area)\n        content_layout = QtWidgets.QGridLayout(content_widget)\n        content_layout.setColumnStretch(0, 0)\n        content_layout.setColumnStretch(1, 1)\n        content_layout.setAlignment(QtCore.Qt.AlignTop)\n        content_layout.setHorizontalSpacing(INPUTS_LAYOUT_HSPACING)\n        content_layout.setVerticalSpacing(INPUTS_LAYOUT_VSPACING)\n\n        row = 0\n        for attr_def, info_by_id in result:\n            if isinstance(attr_def, ButtonDef):\n                inner_callback = CreateButtonCallback(\n                    self._controller,\n                    attr_def.key,\n                    list(info_by_id),\n                )\n                attr_def = attr_def.clone()\n                attr_def.set_callback(inner_callback)\n\n            widget = create_widget_for_attr_def(\n                attr_def, content_widget, handle_revert_to_default=False\n            )\n            default_values = []\n            if attr_def.is_value_def:\n                values = []\n                for item in info_by_id.values():\n                    values.append(item[\"value\"])\n                    # 'set' cannot be used for default values because they can\n                    #    be unhashable types, e.g. 'list'.\n                    default = item[\"default\"]\n                    if default not in default_values:\n                        default_values.append(default)\n\n                if len(values) == 1:\n                    value = values[0]\n                    if value is not None:\n                        widget.set_value(values[0])\n                else:\n                    widget.set_value(values, True)\n\n            widget.value_changed.connect(self._input_value_changed)\n            widget.revert_to_default_requested.connect(\n                self._on_request_revert_to_default\n            )\n            attr_def_info = _CreateAttrDefInfo(\n                attr_def, list(info_by_id), default_values, None\n            )\n            self._attr_def_info_by_id[attr_def.id] = attr_def_info\n\n            if not attr_def.visible:\n                continue\n\n            col_num = 0\n            if attr_def.is_label_horizontal and (\n                attr_def.is_value_def\n                or isinstance(attr_def, ButtonDef)\n            ):\n                col_num = 1\n\n            expand_cols = 2 - col_num\n\n            is_overriden = False\n            label = attr_def.label\n            if isinstance(attr_def, UILabelDef):\n                label = None\n\n            elif attr_def.is_value_def:\n                is_overriden = any(\n                    item[\"value\"] != item[\"default\"]\n                    for item in info_by_id.values()\n                )\n\n            if label:\n                label_widget = AttributeDefinitionsLabel(\n                    attr_def.id, label, self\n                )\n                tooltip = attr_def.tooltip\n                if tooltip:\n                    label_widget.setToolTip(tooltip)\n\n                if attr_def.is_label_horizontal:\n                    label_widget.setAlignment(\n                        QtCore.Qt.AlignRight\n                        | QtCore.Qt.AlignVCenter\n                    )\n                content_layout.addWidget(\n                    label_widget, row, 0, 1, expand_cols\n                )\n                if not attr_def.is_label_horizontal:\n                    row += 1\n                attr_def_info.label_widget = label_widget\n                label_widget.set_overridden(is_overriden)\n                label_widget.revert_to_default_requested.connect(\n                    self._on_request_revert_to_default\n                )\n\n            content_layout.addWidget(\n                widget, row, col_num, 1, expand_cols\n            )\n            row += 1\n\n        self._scroll_area.setWidget(content_widget)\n        self._content_widget = content_widget\n\n    def _on_instance_attr_defs_change(self, event):\n        for instance_id in event.data[\"instance_ids\"]:\n            if instance_id in self._current_instance_ids:\n                self._refresh_content()\n                break\n\n    def _on_instance_value_change(self, event):\n        # TODO try to find more optimized way to update values instead of\n        #     force refresh of all of them.\n        for instance_id, changes in event[\"instance_changes\"].items():\n            if (\n                instance_id in self._current_instance_ids\n                and \"creator_attributes\" in changes\n            ):\n                self._refresh_content()\n                break\n\n    def _input_value_changed(self, value, attr_id):\n        attr_def_info = self._attr_def_info_by_id.get(attr_id)\n        if attr_def_info is None:\n            return\n\n        if attr_def_info.label_widget is not None:\n            defaults = attr_def_info.defaults\n            is_overriden = len(defaults) != 1 or value not in defaults\n            attr_def_info.label_widget.set_overridden(is_overriden)\n\n        self._controller.set_instances_create_attr_values(\n            attr_def_info.instance_ids,\n            attr_def_info.attr_def.key,\n            value\n        )\n\n    def _on_request_revert_to_default(self, attr_id):\n        attr_def_info = self._attr_def_info_by_id.get(attr_id)\n        if attr_def_info is None:\n            return\n        self._controller.revert_instances_create_attr_values(\n            attr_def_info.instance_ids,\n            attr_def_info.attr_def.key,\n        )\n        self._refresh_content()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/product_attributes.html#client.ayon_core.tools.publisher.widgets.product_attributes.CreatorAttrsWidget.set_current_instances","title":"<code>set_current_instances(instance_ids)</code>","text":"<p>Set current instances for which are attribute definitions shown.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/product_attributes.py</code> <pre><code>def set_current_instances(self, instance_ids):\n    \"\"\"Set current instances for which are attribute definitions shown.\"\"\"\n\n    self._current_instance_ids = set(instance_ids)\n    self._refresh_content()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/product_attributes.html#client.ayon_core.tools.publisher.widgets.product_attributes.CreatorAttrsWidget.set_instances_valid","title":"<code>set_instances_valid(valid)</code>","text":"<p>Change valid state of current instances.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/product_attributes.py</code> <pre><code>def set_instances_valid(self, valid):\n    \"\"\"Change valid state of current instances.\"\"\"\n\n    if (\n        self._content_widget is not None\n        and self._content_widget.isEnabled() != valid\n    ):\n        self._content_widget.setEnabled(valid)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/product_attributes.html#client.ayon_core.tools.publisher.widgets.product_attributes.PublishPluginAttrsWidget","title":"<code>PublishPluginAttrsWidget</code>","text":"<p>               Bases: <code>QWidget</code></p> <p>Widget showing publish plugin attributes for selected instances.</p> <p>Attributes are defined on publish plugins. Publish plugin may define attribute definitions but must inherit <code>AYONPyblishPluginMixin</code> (~/ayon_core/pipeline/publish). At the moment requires to implement <code>get_attribute_defs</code> and <code>convert_attribute_values</code> class methods.</p> <p>Look and type of attributes is based on attribute definitions that are defined in <code>~/ayon_core/lib/attribute_definitions.py</code> and their widget representation in <code>~/ayon_core/tools/attribute_defs/*</code>.</p> <p>Widgets are disabled if context of instance is not valid.</p> <p>Definitions are shown for all instance no matter if they have different product base types. Similar definitions are merged into one (different label does not count).</p> Source code in <code>client/ayon_core/tools/publisher/widgets/product_attributes.py</code> <pre><code>class PublishPluginAttrsWidget(QtWidgets.QWidget):\n    \"\"\"Widget showing publish plugin attributes for selected instances.\n\n    Attributes are defined on publish plugins. Publish plugin may define\n    attribute definitions but must inherit `AYONPyblishPluginMixin`\n    (~/ayon_core/pipeline/publish). At the moment requires to implement\n    `get_attribute_defs` and `convert_attribute_values` class methods.\n\n    Look and type of attributes is based on attribute definitions that are\n    defined in `~/ayon_core/lib/attribute_definitions.py` and their\n    widget representation in `~/ayon_core/tools/attribute_defs/*`.\n\n    Widgets are disabled if context of instance is not valid.\n\n    Definitions are shown for all instance no matter if they have different\n    product base types. Similar definitions are merged into one (different\n    label does not count).\n    \"\"\"\n\n    def __init__(\n        self, controller: AbstractPublisherFrontend, parent: QtWidgets.QWidget\n    ):\n        super().__init__(parent)\n\n        scroll_area = QtWidgets.QScrollArea(self)\n        scroll_area.setWidgetResizable(True)\n\n        main_layout = QtWidgets.QHBoxLayout(self)\n        main_layout.setContentsMargins(0, 0, 0, 0)\n        main_layout.setSpacing(0)\n        main_layout.addWidget(scroll_area, 1)\n\n        controller.register_event_callback(\n            \"create.context.publish.attrs.changed\",\n            self._on_instance_attr_defs_change\n        )\n        controller.register_event_callback(\n            \"create.context.value.changed\",\n            self._on_instance_value_change\n        )\n\n        self._current_instance_ids = set()\n        self._context_selected = False\n\n        self._main_layout = main_layout\n\n        self._controller: AbstractPublisherFrontend = controller\n        self._scroll_area = scroll_area\n\n        self._attr_def_info_by_id: Dict[str, _PublishAttrDefInfo] = {}\n\n        # Store content of scroll area to prevent garbage collection\n        self._content_widget = None\n\n    def set_instances_valid(self, valid):\n        \"\"\"Change valid state of current instances.\"\"\"\n        if (\n            self._content_widget is not None\n            and self._content_widget.isEnabled() != valid\n        ):\n            self._content_widget.setEnabled(valid)\n\n    def set_current_instances(self, instance_ids, context_selected):\n        \"\"\"Set current instances for which are attribute definitions shown.\"\"\"\n\n        self._current_instance_ids = set(instance_ids)\n        self._context_selected = context_selected\n        self._refresh_content()\n\n    def _refresh_content(self):\n        prev_content_widget = self._scroll_area.widget()\n        if prev_content_widget:\n            self._scroll_area.takeWidget()\n            prev_content_widget.hide()\n            prev_content_widget.deleteLater()\n\n        self._content_widget = None\n\n        self._attr_def_info_by_id = {}\n\n        result = self._controller.get_publish_attribute_definitions(\n            self._current_instance_ids, self._context_selected\n        )\n\n        content_widget = QtWidgets.QWidget(self._scroll_area)\n        attr_def_widget = QtWidgets.QWidget(content_widget)\n        attr_def_layout = QtWidgets.QGridLayout(attr_def_widget)\n        attr_def_layout.setColumnStretch(0, 0)\n        attr_def_layout.setColumnStretch(1, 1)\n        attr_def_layout.setHorizontalSpacing(INPUTS_LAYOUT_HSPACING)\n        attr_def_layout.setVerticalSpacing(INPUTS_LAYOUT_VSPACING)\n\n        content_layout = QtWidgets.QVBoxLayout(content_widget)\n        content_layout.addWidget(attr_def_widget, 0)\n        content_layout.addStretch(1)\n\n        row = 0\n        for plugin_attr_defs in result:\n            for attr_def in plugin_attr_defs.attr_defs:\n                values = []\n                default_values = []\n                is_overriden = False\n                for (instance_id, value, default_value) in (\n                    plugin_attr_defs.values.get(attr_def.key, [])\n                ):\n                    values.append(value)\n                    if not is_overriden and value != default_value:\n                        is_overriden = True\n                    # 'set' cannot be used for default values because they can\n                    #    be unhashable types, e.g. 'list'.\n                    if default_value not in default_values:\n                        default_values.append(default_value)\n\n                if isinstance(attr_def, ButtonDef):\n                    inner_callback = PublishButtonCallback(\n                        self._controller,\n                        attr_def.key,\n                        plugin_attr_defs.plugin_name,\n                        list(plugin_attr_defs.instance_ids),\n                    )\n                    attr_def = attr_def.clone()\n                    attr_def.set_callback(inner_callback)\n\n                widget = create_widget_for_attr_def(\n                    attr_def, content_widget, handle_revert_to_default=False\n                )\n                visible_widget = attr_def.visible\n                # Hide unknown values of publish plugins\n                # - The keys in most of the cases does not represent what\n                #    would label represent\n                if isinstance(attr_def, UnknownDef):\n                    widget.setVisible(False)\n                    visible_widget = False\n\n                label_widget = None\n                if visible_widget:\n                    col_num = 0\n                    if attr_def.is_label_horizontal and (\n                        attr_def.is_value_def\n                        or isinstance(attr_def, ButtonDef)\n                    ):\n                        col_num = 1\n\n                    expand_cols = 2 - col_num\n\n                    label = attr_def.label\n                    if isinstance(attr_def, UILabelDef):\n                        label = None\n\n                    if label:\n                        label_widget = AttributeDefinitionsLabel(\n                            attr_def.id, label, content_widget\n                        )\n                        label_widget.revert_to_default_requested.connect(\n                            self._on_request_revert_to_default\n                        )\n                        tooltip = attr_def.tooltip\n                        if tooltip:\n                            label_widget.setToolTip(tooltip)\n                        if attr_def.is_label_horizontal:\n                            label_widget.setAlignment(\n                                QtCore.Qt.AlignRight\n                                | QtCore.Qt.AlignVCenter\n                            )\n                        attr_def_layout.addWidget(\n                            label_widget, row, 0, 1, expand_cols\n                        )\n                        if not attr_def.is_label_horizontal:\n                            row += 1\n                    attr_def_layout.addWidget(\n                        widget, row, col_num, 1, expand_cols\n                    )\n                    row += 1\n\n                if not attr_def.is_value_def:\n                    continue\n\n                widget.value_changed.connect(self._input_value_changed)\n                widget.revert_to_default_requested.connect(\n                    self._on_request_revert_to_default\n                )\n\n                multivalue = len(values) &gt; 1\n\n                self._attr_def_info_by_id[attr_def.id] = _PublishAttrDefInfo(\n                    attr_def,\n                    plugin_attr_defs.plugin_name,\n                    plugin_attr_defs.instance_ids,\n                    default_values,\n                    label_widget,\n                )\n\n                if multivalue:\n                    widget.set_value(values, multivalue)\n                else:\n                    widget.set_value(values[0])\n\n                if label_widget is not None:\n                    label_widget.set_overridden(is_overriden)\n\n        self._scroll_area.setWidget(content_widget)\n        self._content_widget = content_widget\n\n    def _input_value_changed(self, value, attr_id):\n        attr_def_info = self._attr_def_info_by_id.get(attr_id)\n        if attr_def_info is None:\n            return\n\n        if attr_def_info.label_widget is not None:\n            defaults = attr_def_info.defaults\n            is_overriden = len(defaults) != 1 or value not in defaults\n            attr_def_info.label_widget.set_overridden(is_overriden)\n\n        self._controller.set_instances_publish_attr_values(\n            attr_def_info.instance_ids,\n            attr_def_info.plugin_name,\n            attr_def_info.attr_def.key,\n            value\n        )\n\n    def _on_request_revert_to_default(self, attr_id):\n        attr_def_info = self._attr_def_info_by_id.get(attr_id)\n        if attr_def_info is None:\n            return\n\n        self._controller.revert_instances_publish_attr_values(\n            attr_def_info.instance_ids,\n            attr_def_info.plugin_name,\n            attr_def_info.attr_def.key,\n        )\n        self._refresh_content()\n\n    def _on_instance_attr_defs_change(self, event):\n        for instance_id in event.data:\n            if (\n                instance_id is None and self._context_selected\n                or instance_id in self._current_instance_ids\n            ):\n                self._refresh_content()\n                break\n\n    def _on_instance_value_change(self, event):\n        # TODO try to find more optimized way to update values instead of\n        #     force refresh of all of them.\n        for instance_id, changes in event[\"instance_changes\"].items():\n            if (\n                instance_id in self._current_instance_ids\n                and \"publish_attributes\" in changes\n            ):\n                self._refresh_content()\n                break\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/product_attributes.html#client.ayon_core.tools.publisher.widgets.product_attributes.PublishPluginAttrsWidget.set_current_instances","title":"<code>set_current_instances(instance_ids, context_selected)</code>","text":"<p>Set current instances for which are attribute definitions shown.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/product_attributes.py</code> <pre><code>def set_current_instances(self, instance_ids, context_selected):\n    \"\"\"Set current instances for which are attribute definitions shown.\"\"\"\n\n    self._current_instance_ids = set(instance_ids)\n    self._context_selected = context_selected\n    self._refresh_content()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/product_attributes.html#client.ayon_core.tools.publisher.widgets.product_attributes.PublishPluginAttrsWidget.set_instances_valid","title":"<code>set_instances_valid(valid)</code>","text":"<p>Change valid state of current instances.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/product_attributes.py</code> <pre><code>def set_instances_valid(self, valid):\n    \"\"\"Change valid state of current instances.\"\"\"\n    if (\n        self._content_widget is not None\n        and self._content_widget.isEnabled() != valid\n    ):\n        self._content_widget.setEnabled(valid)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/product_context.html","title":"product_context","text":""},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/product_context.html#client.ayon_core.tools.publisher.widgets.product_context.FoldersFields","title":"<code>FoldersFields</code>","text":"<p>               Bases: <code>BaseClickableFrame</code></p> <p>Field where folder path of selected instance/s is showed.</p> <p>Click on the field will trigger <code>FoldersDialog</code>.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/product_context.py</code> <pre><code>class FoldersFields(BaseClickableFrame):\n    \"\"\"Field where folder path of selected instance/s is showed.\n\n    Click on the field will trigger `FoldersDialog`.\n    \"\"\"\n    value_changed = QtCore.Signal()\n\n    def __init__(\n        self, controller: AbstractPublisherFrontend, parent: QtWidgets.QWidget\n    ):\n        super().__init__(parent)\n        self.setObjectName(\"FolderPathInputWidget\")\n\n        # Don't use 'self' for parent!\n        # - this widget has specific styles\n        dialog = FoldersDialog(controller, parent)\n\n        name_input = ClickableLineEdit(self)\n        name_input.setObjectName(\"FolderPathInput\")\n\n        icon_name = \"fa.window-maximize\"\n        icon = qtawesome.icon(icon_name, color=\"white\")\n        icon_btn = QtWidgets.QPushButton(self)\n        icon_btn.setIcon(icon)\n        icon_btn.setObjectName(\"FolderPathInputButton\")\n\n        layout = QtWidgets.QHBoxLayout(self)\n        layout.setContentsMargins(0, 0, 0, 0)\n        layout.setSpacing(0)\n        layout.addWidget(name_input, 1)\n        layout.addWidget(icon_btn, 0)\n\n        # Make sure all widgets are vertically extended to highest widget\n        for widget in (\n            name_input,\n            icon_btn\n        ):\n            size_policy = widget.sizePolicy()\n            size_policy.setVerticalPolicy(\n                QtWidgets.QSizePolicy.MinimumExpanding)\n            widget.setSizePolicy(size_policy)\n        name_input.clicked.connect(self._mouse_release_callback)\n        icon_btn.clicked.connect(self._mouse_release_callback)\n        dialog.finished.connect(self._on_dialog_finish)\n\n        self._controller: AbstractPublisherFrontend = controller\n        self._dialog = dialog\n        self._name_input = name_input\n        self._icon_btn = icon_btn\n\n        self._origin_value = []\n        self._origin_selection = []\n        self._selected_items = []\n        self._has_value_changed = False\n        self._is_valid = True\n        self._multiselection_text = None\n\n    def _on_dialog_finish(self, result):\n        if not result:\n            return\n\n        folder_path = self._dialog.get_selected_folder_path()\n        if folder_path is None:\n            return\n\n        self._selected_items = [folder_path]\n        self._has_value_changed = (\n            self._origin_value != self._selected_items\n        )\n        self.set_text(folder_path)\n        self._set_is_valid(True)\n\n        self.value_changed.emit()\n\n    def _mouse_release_callback(self):\n        self._dialog.set_selected_folders(self._selected_items)\n        self._dialog.open()\n\n    def set_multiselection_text(self, text):\n        \"\"\"Change text for multiselection of different folders.\n\n        When there are selected multiple instances at once and they don't have\n        same folder in context.\n        \"\"\"\n        self._multiselection_text = text\n\n    def _set_is_valid(self, valid):\n        if valid == self._is_valid:\n            return\n        self._is_valid = valid\n        state = \"\"\n        if not valid:\n            state = \"invalid\"\n        self._set_state_property(state)\n\n    def _set_state_property(self, state):\n        set_style_property(self, \"state\", state)\n        set_style_property(self._name_input, \"state\", state)\n        set_style_property(self._icon_btn, \"state\", state)\n\n    def is_valid(self):\n        \"\"\"Is folder valid.\"\"\"\n        return self._is_valid\n\n    def has_value_changed(self):\n        \"\"\"Value of folder has changed.\"\"\"\n        return self._has_value_changed\n\n    def get_selected_items(self):\n        \"\"\"Selected folder paths.\"\"\"\n        return list(self._selected_items)\n\n    def set_text(self, text):\n        \"\"\"Set text in text field.\n\n        Does not change selected items (folders).\n        \"\"\"\n        self._name_input.setText(text)\n        self._name_input.end(False)\n\n    def set_selected_items(self, folder_paths=None):\n        \"\"\"Set folder paths for selection of instances.\n\n        Passed folder paths are validated and if there are 2 or more different\n        folder paths then multiselection text is shown.\n\n        Args:\n            folder_paths (list, tuple, set, NoneType): List of folder paths.\n\n        \"\"\"\n        if folder_paths is None:\n            folder_paths = []\n\n        self._has_value_changed = False\n        self._origin_value = list(folder_paths)\n        self._selected_items = list(folder_paths)\n        is_valid = self._controller.are_folder_paths_valid(folder_paths)\n        if not folder_paths:\n            self.set_text(\"\")\n\n        elif len(folder_paths) == 1:\n            folder_path = tuple(folder_paths)[0]\n            self.set_text(folder_path)\n        else:\n            multiselection_text = self._multiselection_text\n            if multiselection_text is None:\n                multiselection_text = \"|\".join(folder_paths)\n            self.set_text(multiselection_text)\n\n        self._set_is_valid(is_valid)\n\n    def reset_to_origin(self):\n        \"\"\"Change to folder paths set with last `set_selected_items` call.\"\"\"\n        self.set_selected_items(self._origin_value)\n\n    def confirm_value(self):\n        self._origin_value = copy.deepcopy(self._selected_items)\n        self._has_value_changed = False\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/product_context.html#client.ayon_core.tools.publisher.widgets.product_context.FoldersFields.get_selected_items","title":"<code>get_selected_items()</code>","text":"<p>Selected folder paths.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/product_context.py</code> <pre><code>def get_selected_items(self):\n    \"\"\"Selected folder paths.\"\"\"\n    return list(self._selected_items)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/product_context.html#client.ayon_core.tools.publisher.widgets.product_context.FoldersFields.has_value_changed","title":"<code>has_value_changed()</code>","text":"<p>Value of folder has changed.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/product_context.py</code> <pre><code>def has_value_changed(self):\n    \"\"\"Value of folder has changed.\"\"\"\n    return self._has_value_changed\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/product_context.html#client.ayon_core.tools.publisher.widgets.product_context.FoldersFields.is_valid","title":"<code>is_valid()</code>","text":"<p>Is folder valid.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/product_context.py</code> <pre><code>def is_valid(self):\n    \"\"\"Is folder valid.\"\"\"\n    return self._is_valid\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/product_context.html#client.ayon_core.tools.publisher.widgets.product_context.FoldersFields.reset_to_origin","title":"<code>reset_to_origin()</code>","text":"<p>Change to folder paths set with last <code>set_selected_items</code> call.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/product_context.py</code> <pre><code>def reset_to_origin(self):\n    \"\"\"Change to folder paths set with last `set_selected_items` call.\"\"\"\n    self.set_selected_items(self._origin_value)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/product_context.html#client.ayon_core.tools.publisher.widgets.product_context.FoldersFields.set_multiselection_text","title":"<code>set_multiselection_text(text)</code>","text":"<p>Change text for multiselection of different folders.</p> <p>When there are selected multiple instances at once and they don't have same folder in context.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/product_context.py</code> <pre><code>def set_multiselection_text(self, text):\n    \"\"\"Change text for multiselection of different folders.\n\n    When there are selected multiple instances at once and they don't have\n    same folder in context.\n    \"\"\"\n    self._multiselection_text = text\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/product_context.html#client.ayon_core.tools.publisher.widgets.product_context.FoldersFields.set_selected_items","title":"<code>set_selected_items(folder_paths=None)</code>","text":"<p>Set folder paths for selection of instances.</p> <p>Passed folder paths are validated and if there are 2 or more different folder paths then multiselection text is shown.</p> <p>Parameters:</p> Name Type Description Default <code>folder_paths</code> <code>(list, tuple, set, NoneType)</code> <p>List of folder paths.</p> <code>None</code> Source code in <code>client/ayon_core/tools/publisher/widgets/product_context.py</code> <pre><code>def set_selected_items(self, folder_paths=None):\n    \"\"\"Set folder paths for selection of instances.\n\n    Passed folder paths are validated and if there are 2 or more different\n    folder paths then multiselection text is shown.\n\n    Args:\n        folder_paths (list, tuple, set, NoneType): List of folder paths.\n\n    \"\"\"\n    if folder_paths is None:\n        folder_paths = []\n\n    self._has_value_changed = False\n    self._origin_value = list(folder_paths)\n    self._selected_items = list(folder_paths)\n    is_valid = self._controller.are_folder_paths_valid(folder_paths)\n    if not folder_paths:\n        self.set_text(\"\")\n\n    elif len(folder_paths) == 1:\n        folder_path = tuple(folder_paths)[0]\n        self.set_text(folder_path)\n    else:\n        multiselection_text = self._multiselection_text\n        if multiselection_text is None:\n            multiselection_text = \"|\".join(folder_paths)\n        self.set_text(multiselection_text)\n\n    self._set_is_valid(is_valid)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/product_context.html#client.ayon_core.tools.publisher.widgets.product_context.FoldersFields.set_text","title":"<code>set_text(text)</code>","text":"<p>Set text in text field.</p> <p>Does not change selected items (folders).</p> Source code in <code>client/ayon_core/tools/publisher/widgets/product_context.py</code> <pre><code>def set_text(self, text):\n    \"\"\"Set text in text field.\n\n    Does not change selected items (folders).\n    \"\"\"\n    self._name_input.setText(text)\n    self._name_input.end(False)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/product_context.html#client.ayon_core.tools.publisher.widgets.product_context.GlobalAttrsWidget","title":"<code>GlobalAttrsWidget</code>","text":"<p>               Bases: <code>QWidget</code></p> <p>Global attributes to define context and product name of instances.</p> <p>product name is or may be affected on context. Gives abiity to modify context and product name of instance. This change is not autopromoted but must be submitted.</p> <p>Warning: Until artist hit <code>Submit</code> changes must not be propagated to instance data.</p> <p>Global attributes contain these widgets: Variant:           [  text input  ] Folder:            [ folder dialog ] Task:              [   combobox   ] Product base type: [   immutable  ] product name:      [   immutable  ]                       [Submit][Cancel]</p> Source code in <code>client/ayon_core/tools/publisher/widgets/product_context.py</code> <pre><code>class GlobalAttrsWidget(QtWidgets.QWidget):\n    \"\"\"Global attributes to define context and product name of instances.\n\n    product name is or may be affected on context. Gives abiity to modify\n    context and product name of instance. This change is not autopromoted but\n    must be submitted.\n\n    Warning: Until artist hit `Submit` changes must not be propagated to\n    instance data.\n\n    Global attributes contain these widgets:\n    Variant:           [  text input  ]\n    Folder:            [ folder dialog ]\n    Task:              [   combobox   ]\n    Product base type: [   immutable  ]\n    product name:      [   immutable  ]\n                          [Submit] [Cancel]\n\n    \"\"\"\n    multiselection_text = \"&lt; Multiselection &gt;\"\n    unknown_value = \"N/A\"\n\n    def __init__(\n        self, controller: AbstractPublisherFrontend, parent: QtWidgets.QWidget\n    ):\n        super().__init__(parent)\n\n        self._controller: AbstractPublisherFrontend = controller\n        self._current_instances_by_id: dict[str, \"InstanceItem\"] = {}\n        self._invalid_task_item_ids: set[str] = set()\n\n        variant_input = VariantInputWidget(self)\n        folder_value_widget = FoldersFields(controller, self)\n        task_value_widget = TasksCombobox(controller, self)\n        product_base_type_value_widget = MultipleItemWidget(self)\n        product_value_widget = MultipleItemWidget(self)\n\n        variant_input.set_multiselection_text(self.multiselection_text)\n        folder_value_widget.set_multiselection_text(self.multiselection_text)\n        task_value_widget.set_multiselection_text(self.multiselection_text)\n\n        variant_input.set_value()\n        folder_value_widget.set_selected_items()\n        task_value_widget.set_selected_items()\n        product_base_type_value_widget.set_value()\n        product_value_widget.set_value()\n\n        submit_btn = QtWidgets.QPushButton(\"Confirm\", self)\n        cancel_btn = QtWidgets.QPushButton(\"Cancel\", self)\n        submit_btn.setEnabled(False)\n        cancel_btn.setEnabled(False)\n\n        btns_layout = QtWidgets.QHBoxLayout()\n        btns_layout.setContentsMargins(0, 0, 0, 0)\n        btns_layout.addStretch(1)\n        btns_layout.setSpacing(5)\n        btns_layout.addWidget(submit_btn)\n        btns_layout.addWidget(cancel_btn)\n\n        main_layout = QtWidgets.QFormLayout(self)\n        main_layout.setHorizontalSpacing(INPUTS_LAYOUT_HSPACING)\n        main_layout.setVerticalSpacing(INPUTS_LAYOUT_VSPACING)\n        main_layout.addRow(\"Variant\", variant_input)\n        main_layout.addRow(\"Folder\", folder_value_widget)\n        main_layout.addRow(\"Task\", task_value_widget)\n        main_layout.addRow(\n            \"Product base type\",\n            product_base_type_value_widget\n        )\n        main_layout.addRow(\"Product name\", product_value_widget)\n        main_layout.addRow(btns_layout)\n\n        variant_input.value_changed.connect(self._on_variant_change)\n        folder_value_widget.value_changed.connect(self._on_folder_change)\n        task_value_widget.value_changed.connect(self._on_task_change)\n        submit_btn.clicked.connect(self._on_submit)\n        cancel_btn.clicked.connect(self._on_cancel)\n\n        controller.register_event_callback(\n            \"create.context.value.changed\",\n            self._on_instance_value_change\n        )\n\n        self.variant_input = variant_input\n        self.folder_value_widget = folder_value_widget\n        self.task_value_widget = task_value_widget\n        self._product_base_type_value_widget = product_base_type_value_widget\n        self.product_value_widget = product_value_widget\n        self.submit_btn = submit_btn\n        self.cancel_btn = cancel_btn\n\n    def _on_submit(self):\n        \"\"\"Commit changes for selected instances.\"\"\"\n\n        variant_value = None\n        folder_path = None\n        task_name = None\n        if self.variant_input.has_value_changed():\n            variant_value = self.variant_input.get_value()[0]\n\n        if self.folder_value_widget.has_value_changed():\n            folder_path = self.folder_value_widget.get_selected_items()[0]\n\n        if self.task_value_widget.has_value_changed():\n            task_name = self.task_value_widget.get_selected_items()[0]\n\n        product_names = set()\n        invalid_tasks = False\n        folder_paths = []\n        changes_by_id = {}\n        for item in self._current_instances_by_id.values():\n            # Ignore instances that have promised context\n            if item.has_promised_context:\n                continue\n\n            instance_changes = {}\n            new_variant_value = item.variant\n            new_folder_path = item.folder_path\n            new_task_name = item.task_name\n            if variant_value is not None:\n                instance_changes[\"variant\"] = variant_value\n                new_variant_value = variant_value\n\n            if folder_path is not None:\n                instance_changes[\"folderPath\"] = folder_path\n                new_folder_path = folder_path\n\n            if task_name is not None:\n                instance_changes[\"task\"] = task_name or None\n                new_task_name = task_name or None\n\n            folder_paths.append(new_folder_path)\n            try:\n                new_product_name = self._controller.get_product_name(\n                    item.creator_identifier,\n                    item.product_type,\n                    new_variant_value,\n                    new_folder_path,\n                    new_task_name,\n                    item.id,\n                )\n                self._invalid_task_item_ids.discard(item.id)\n\n            except TaskNotSetError:\n                self._invalid_task_item_ids.add(item.id)\n                invalid_tasks = True\n                product_names.add(item.product_name)\n                continue\n\n            product_names.add(new_product_name)\n            if item.product_name != new_product_name:\n                instance_changes[\"productName\"] = new_product_name\n\n            if instance_changes:\n                changes_by_id[item.id] = instance_changes\n\n        if invalid_tasks:\n            self.task_value_widget.set_invalid_empty_task()\n\n        self.product_value_widget.set_value(product_names)\n\n        self._set_btns_enabled(False)\n        self._set_btns_visible(invalid_tasks)\n\n        if variant_value is not None:\n            self.variant_input.confirm_value()\n\n        if folder_path is not None:\n            self.folder_value_widget.confirm_value()\n\n        if task_name is not None:\n            self.task_value_widget.confirm_value(folder_paths)\n\n        self._controller.set_instances_context_info(changes_by_id)\n        self._refresh_items()\n\n    def _on_cancel(self):\n        \"\"\"Cancel changes and set back to their irigin value.\"\"\"\n\n        self.variant_input.reset_to_origin()\n        self.folder_value_widget.reset_to_origin()\n        self.task_value_widget.reset_to_origin()\n        self._set_btns_enabled(False)\n\n    def _on_value_change(self):\n        any_invalid = (\n            not self.variant_input.is_valid()\n            or not self.folder_value_widget.is_valid()\n            or not self.task_value_widget.is_valid()\n        )\n        any_changed = (\n            self.variant_input.has_value_changed()\n            or self.folder_value_widget.has_value_changed()\n            or self.task_value_widget.has_value_changed()\n        )\n        self._set_btns_visible(any_changed or any_invalid)\n        self.cancel_btn.setEnabled(any_changed)\n        self.submit_btn.setEnabled(not any_invalid)\n\n    def _on_variant_change(self):\n        self._on_value_change()\n\n    def _on_folder_change(self):\n        folder_paths = self.folder_value_widget.get_selected_items()\n        self.task_value_widget.set_folder_paths(folder_paths)\n        self._on_value_change()\n\n    def _on_task_change(self):\n        self._on_value_change()\n\n    def _set_btns_visible(self, visible):\n        self.cancel_btn.setVisible(visible)\n        self.submit_btn.setVisible(visible)\n\n    def _set_btns_enabled(self, enabled):\n        self.cancel_btn.setEnabled(enabled)\n        self.submit_btn.setEnabled(enabled)\n\n    def set_current_instances(self, instances):\n        \"\"\"Set currently selected instances.\n\n        Args:\n            instances (List[InstanceItem]): List of selected instances.\n                Empty instances tells that nothing or context is selected.\n        \"\"\"\n        self._set_btns_visible(False)\n\n        self._current_instances_by_id = {\n            instance.id: instance\n            for instance in instances\n        }\n        self._invalid_task_item_ids = set()\n        self._refresh_content()\n\n    def _refresh_items(self):\n        instance_ids = set(self._current_instances_by_id.keys())\n        self._current_instances_by_id = (\n            self._controller.get_instance_items_by_id(instance_ids)\n        )\n\n    def _refresh_content(self):\n        folder_paths = set()\n        variants = set()\n        product_base_types = set()\n        product_names = set()\n\n        editable = True\n        if len(self._current_instances_by_id) == 0:\n            editable = False\n\n        folder_task_combinations = []\n        context_editable = None\n        invalid_tasks = False\n        for item in self._current_instances_by_id.values():\n            if not item.has_promised_context:\n                context_editable = True\n            elif context_editable is None:\n                context_editable = False\n                if item.id in self._invalid_task_item_ids:\n                    invalid_tasks = True\n\n            # NOTE I'm not sure how this can even happen?\n            if item.creator_identifier is None:\n                editable = False\n\n            variants.add(item.variant or self.unknown_value)\n            product_base_types.add(\n                item.product_base_type or self.unknown_value\n            )\n            folder_path = item.folder_path or self.unknown_value\n            task_name = item.task_name or \"\"\n            folder_paths.add(folder_path)\n            folder_task_combinations.append((folder_path, task_name))\n            product_names.add(item.product_name or self.unknown_value)\n\n        if not editable:\n            context_editable = False\n        elif context_editable is None:\n            context_editable = True\n\n        self.variant_input.set_value(variants)\n\n        # Set context of folder widget\n        self.folder_value_widget.set_selected_items(folder_paths)\n        # Set context of task widget\n        self.task_value_widget.set_selected_items(folder_task_combinations)\n        self._product_base_type_value_widget.set_value(product_base_types)\n        self.product_value_widget.set_value(product_names)\n\n        self.variant_input.setEnabled(editable)\n        self.folder_value_widget.setEnabled(context_editable)\n        self.task_value_widget.setEnabled(context_editable)\n\n        if invalid_tasks:\n            self.task_value_widget.set_invalid_empty_task()\n\n        if not editable:\n            folder_tooltip = \"Select instances to change folder path.\"\n            task_tooltip = \"Select instances to change task name.\"\n        elif not context_editable:\n            folder_tooltip = \"Folder path is defined by Create plugin.\"\n            task_tooltip = \"Task is defined by Create plugin.\"\n        else:\n            folder_tooltip = \"Change folder path of selected instances.\"\n            task_tooltip = \"Change task of selected instances.\"\n\n        self.folder_value_widget.setToolTip(folder_tooltip)\n        self.task_value_widget.setToolTip(task_tooltip)\n\n    def _on_instance_value_change(self, event):\n        if not self._current_instances_by_id:\n            return\n\n        changed = False\n        for instance_id, changes in event[\"instance_changes\"].items():\n            if instance_id not in self._current_instances_by_id:\n                continue\n\n            for key in (\n                \"folderPath\",\n                \"task\",\n                \"variant\",\n                \"productType\",\n                \"productName\",\n            ):\n                if key in changes:\n                    changed = True\n                    break\n            if changed:\n                break\n\n        if changed:\n            self._refresh_items()\n            self._refresh_content()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/product_context.html#client.ayon_core.tools.publisher.widgets.product_context.GlobalAttrsWidget.set_current_instances","title":"<code>set_current_instances(instances)</code>","text":"<p>Set currently selected instances.</p> <p>Parameters:</p> Name Type Description Default <code>instances</code> <code>List[InstanceItem]</code> <p>List of selected instances. Empty instances tells that nothing or context is selected.</p> required Source code in <code>client/ayon_core/tools/publisher/widgets/product_context.py</code> <pre><code>def set_current_instances(self, instances):\n    \"\"\"Set currently selected instances.\n\n    Args:\n        instances (List[InstanceItem]): List of selected instances.\n            Empty instances tells that nothing or context is selected.\n    \"\"\"\n    self._set_btns_visible(False)\n\n    self._current_instances_by_id = {\n        instance.id: instance\n        for instance in instances\n    }\n    self._invalid_task_item_ids = set()\n    self._refresh_content()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/product_context.html#client.ayon_core.tools.publisher.widgets.product_context.TasksCombobox","title":"<code>TasksCombobox</code>","text":"<p>               Bases: <code>QComboBox</code></p> <p>Combobox to show tasks for selected instances.</p> <p>Combobox gives ability to select only from intersection of task names for folder paths in selected instances.</p> <p>If folder paths in selected instances does not have same tasks  then combobox will be empty.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/product_context.py</code> <pre><code>class TasksCombobox(QtWidgets.QComboBox):\n    \"\"\"Combobox to show tasks for selected instances.\n\n    Combobox gives ability to select only from intersection of task names for\n    folder paths in selected instances.\n\n    If folder paths in selected instances does not have same tasks\n     then combobox will be empty.\n    \"\"\"\n    value_changed = QtCore.Signal()\n\n    def __init__(\n        self, controller: AbstractPublisherFrontend, parent: QtWidgets.QWidget\n    ):\n        super().__init__(parent)\n        self.setObjectName(\"TasksCombobox\")\n\n        # Set empty delegate to propagate stylesheet to a combobox\n        delegate = QtWidgets.QStyledItemDelegate()\n        self.setItemDelegate(delegate)\n\n        model = TasksModel(controller, True)\n        proxy_model = TasksComboboxProxy()\n        proxy_model.setSourceModel(model)\n        self.setModel(proxy_model)\n\n        self.currentIndexChanged.connect(self._on_index_change)\n\n        self._delegate = delegate\n        self._model = model\n        self._proxy_model = proxy_model\n        self._origin_value = []\n        self._origin_selection = []\n        self._selected_items = []\n        self._has_value_changed = False\n        self._ignore_index_change = False\n        self._multiselection_text = None\n        self._is_valid = True\n\n        self._text = None\n\n        # Make sure combobox is extended horizontally\n        size_policy = self.sizePolicy()\n        size_policy.setHorizontalPolicy(\n            QtWidgets.QSizePolicy.MinimumExpanding)\n        self.setSizePolicy(size_policy)\n\n    def set_invalid_empty_task(self, invalid=True):\n        self._proxy_model.set_filter_empty(invalid)\n        if invalid:\n            self._set_is_valid(False)\n            self.set_text(\n                \"&lt; One or more products require Task selected &gt;\"\n            )\n        else:\n            self.set_text(None)\n\n    def set_multiselection_text(self, text):\n        \"\"\"Change text shown when multiple different tasks are in context.\"\"\"\n        self._multiselection_text = text\n\n    def _on_index_change(self):\n        if self._ignore_index_change:\n            return\n\n        self.set_text(None)\n        text = self.currentText()\n        idx = self.findText(text)\n        if idx &lt; 0:\n            return\n\n        self._set_is_valid(True)\n        self._selected_items = [text]\n        self._has_value_changed = (\n            self._origin_selection != self._selected_items\n        )\n\n        self.value_changed.emit()\n\n    def set_text(self, text):\n        \"\"\"Set context shown in combobox without changing selected items.\"\"\"\n        if text == self._text:\n            return\n\n        self._text = text\n        self.repaint()\n\n    def paintEvent(self, event):\n        \"\"\"Paint custom text without using QLineEdit.\n\n        The easiest way how to draw custom text in combobox and keep combobox\n        properties and event handling.\n        \"\"\"\n        painter = QtGui.QPainter(self)\n        painter.setPen(self.palette().color(QtGui.QPalette.Text))\n        opt = QtWidgets.QStyleOptionComboBox()\n        self.initStyleOption(opt)\n        if self._text is not None:\n            opt.currentText = self._text\n\n        style = self.style()\n        style.drawComplexControl(\n            QtWidgets.QStyle.CC_ComboBox, opt, painter, self\n        )\n        style.drawControl(\n            QtWidgets.QStyle.CE_ComboBoxLabel, opt, painter, self\n        )\n        painter.end()\n\n    def is_valid(self):\n        \"\"\"Are all selected items valid.\"\"\"\n        return self._is_valid\n\n    def has_value_changed(self):\n        \"\"\"Did selection of task changed.\"\"\"\n        return self._has_value_changed\n\n    def _set_is_valid(self, valid):\n        if valid == self._is_valid:\n            return\n        self._is_valid = valid\n        state = \"\"\n        if not valid:\n            state = \"invalid\"\n        self._set_state_property(state)\n\n    def _set_state_property(self, state):\n        current_value = self.property(\"state\")\n        if current_value != state:\n            self.setProperty(\"state\", state)\n            self.style().polish(self)\n\n    def get_selected_items(self):\n        \"\"\"Get selected tasks.\n\n        If value has changed then will return list with single item.\n\n        Returns:\n            list: Selected tasks.\n        \"\"\"\n        return list(self._selected_items)\n\n    def set_folder_paths(self, folder_paths):\n        \"\"\"Set folder paths for which should show tasks.\"\"\"\n        self._ignore_index_change = True\n\n        self._model.set_folder_paths(folder_paths)\n        self._proxy_model.set_filter_empty(False)\n        self._proxy_model.sort(0)\n\n        self._ignore_index_change = False\n\n        # It is a bug if not exactly one folder got here\n        if len(folder_paths) != 1:\n            self.set_selected_item(\"\")\n            self._set_is_valid(False)\n            return\n\n        folder_path = tuple(folder_paths)[0]\n\n        is_valid = False\n        if self._selected_items:\n            is_valid = True\n\n        valid_task_names = []\n        for task_name in self._selected_items:\n            _is_valid = self._model.is_task_name_valid(folder_path, task_name)\n            if _is_valid:\n                valid_task_names.append(task_name)\n            else:\n                is_valid = _is_valid\n\n        self._selected_items = valid_task_names\n        if len(self._selected_items) == 0:\n            self.set_selected_item(\"\")\n\n        elif len(self._selected_items) == 1:\n            self.set_selected_item(self._selected_items[0])\n\n        else:\n            multiselection_text = self._multiselection_text\n            if multiselection_text is None:\n                multiselection_text = \"|\".join(self._selected_items)\n            self.set_selected_item(multiselection_text)\n\n        self._set_is_valid(is_valid)\n\n    def confirm_value(self, folder_paths):\n        new_task_name = self._selected_items[0]\n        self._origin_value = [\n            (folder_path, new_task_name)\n            for folder_path in folder_paths\n        ]\n        self._origin_selection = copy.deepcopy(self._selected_items)\n        self._has_value_changed = False\n\n    def set_selected_items(self, folder_task_combinations=None):\n        \"\"\"Set items for selected instances.\n\n        Args:\n            folder_task_combinations (list): List of tuples. Each item in\n                the list contain folder path and task name.\n        \"\"\"\n        self._proxy_model.set_filter_empty(False)\n        self._proxy_model.sort(0)\n\n        if folder_task_combinations is None:\n            folder_task_combinations = []\n\n        task_names = set()\n        task_names_by_folder_path = collections.defaultdict(set)\n        for folder_path, task_name in folder_task_combinations:\n            task_names.add(task_name)\n            task_names_by_folder_path[folder_path].add(task_name)\n        folder_paths = set(task_names_by_folder_path.keys())\n\n        self._ignore_index_change = True\n\n        self._model.set_folder_paths(folder_paths)\n\n        self._has_value_changed = False\n\n        self._origin_value = copy.deepcopy(folder_task_combinations)\n\n        self._origin_selection = list(task_names)\n        self._selected_items = list(task_names)\n        # Reset current index\n        self.setCurrentIndex(-1)\n        is_valid = True\n        if not task_names:\n            self.set_selected_item(\"\")\n\n        elif len(task_names) == 1:\n            task_name = tuple(task_names)[0]\n            idx = self.findText(task_name)\n            is_valid = not idx &lt; 0\n            if not is_valid and len(folder_paths) &gt; 1:\n                is_valid = self._validate_task_names_by_folder_paths(\n                    task_names_by_folder_path\n                )\n            self.set_selected_item(task_name)\n\n        else:\n            for task_name in task_names:\n                idx = self.findText(task_name)\n                is_valid = not idx &lt; 0\n                if not is_valid:\n                    break\n\n            if not is_valid and len(folder_paths) &gt; 1:\n                is_valid = self._validate_task_names_by_folder_paths(\n                    task_names_by_folder_path\n                )\n            multiselection_text = self._multiselection_text\n            if multiselection_text is None:\n                multiselection_text = \"|\".join(task_names)\n            self.set_selected_item(multiselection_text)\n\n        self._set_is_valid(is_valid)\n\n        self._ignore_index_change = False\n\n        self.value_changed.emit()\n\n    def _validate_task_names_by_folder_paths(self, task_names_by_folder_path):\n        for folder_path, task_names in task_names_by_folder_path.items():\n            for task_name in task_names:\n                if not self._model.is_task_name_valid(folder_path, task_name):\n                    return False\n        return True\n\n    def set_selected_item(self, item_name):\n        \"\"\"Set task which is set on selected instance.\n\n        Args:\n            item_name(str): Task name which should be selected.\n        \"\"\"\n        idx = self.findText(item_name)\n        # Set current index (must be set to -1 if is invalid)\n        self.setCurrentIndex(idx)\n        self.set_text(item_name)\n\n    def reset_to_origin(self):\n        \"\"\"Change to task names set with last `set_selected_items` call.\"\"\"\n        self.set_selected_items(self._origin_value)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/product_context.html#client.ayon_core.tools.publisher.widgets.product_context.TasksCombobox.get_selected_items","title":"<code>get_selected_items()</code>","text":"<p>Get selected tasks.</p> <p>If value has changed then will return list with single item.</p> <p>Returns:</p> Name Type Description <code>list</code> <p>Selected tasks.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/product_context.py</code> <pre><code>def get_selected_items(self):\n    \"\"\"Get selected tasks.\n\n    If value has changed then will return list with single item.\n\n    Returns:\n        list: Selected tasks.\n    \"\"\"\n    return list(self._selected_items)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/product_context.html#client.ayon_core.tools.publisher.widgets.product_context.TasksCombobox.has_value_changed","title":"<code>has_value_changed()</code>","text":"<p>Did selection of task changed.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/product_context.py</code> <pre><code>def has_value_changed(self):\n    \"\"\"Did selection of task changed.\"\"\"\n    return self._has_value_changed\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/product_context.html#client.ayon_core.tools.publisher.widgets.product_context.TasksCombobox.is_valid","title":"<code>is_valid()</code>","text":"<p>Are all selected items valid.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/product_context.py</code> <pre><code>def is_valid(self):\n    \"\"\"Are all selected items valid.\"\"\"\n    return self._is_valid\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/product_context.html#client.ayon_core.tools.publisher.widgets.product_context.TasksCombobox.paintEvent","title":"<code>paintEvent(event)</code>","text":"<p>Paint custom text without using QLineEdit.</p> <p>The easiest way how to draw custom text in combobox and keep combobox properties and event handling.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/product_context.py</code> <pre><code>def paintEvent(self, event):\n    \"\"\"Paint custom text without using QLineEdit.\n\n    The easiest way how to draw custom text in combobox and keep combobox\n    properties and event handling.\n    \"\"\"\n    painter = QtGui.QPainter(self)\n    painter.setPen(self.palette().color(QtGui.QPalette.Text))\n    opt = QtWidgets.QStyleOptionComboBox()\n    self.initStyleOption(opt)\n    if self._text is not None:\n        opt.currentText = self._text\n\n    style = self.style()\n    style.drawComplexControl(\n        QtWidgets.QStyle.CC_ComboBox, opt, painter, self\n    )\n    style.drawControl(\n        QtWidgets.QStyle.CE_ComboBoxLabel, opt, painter, self\n    )\n    painter.end()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/product_context.html#client.ayon_core.tools.publisher.widgets.product_context.TasksCombobox.reset_to_origin","title":"<code>reset_to_origin()</code>","text":"<p>Change to task names set with last <code>set_selected_items</code> call.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/product_context.py</code> <pre><code>def reset_to_origin(self):\n    \"\"\"Change to task names set with last `set_selected_items` call.\"\"\"\n    self.set_selected_items(self._origin_value)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/product_context.html#client.ayon_core.tools.publisher.widgets.product_context.TasksCombobox.set_folder_paths","title":"<code>set_folder_paths(folder_paths)</code>","text":"<p>Set folder paths for which should show tasks.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/product_context.py</code> <pre><code>def set_folder_paths(self, folder_paths):\n    \"\"\"Set folder paths for which should show tasks.\"\"\"\n    self._ignore_index_change = True\n\n    self._model.set_folder_paths(folder_paths)\n    self._proxy_model.set_filter_empty(False)\n    self._proxy_model.sort(0)\n\n    self._ignore_index_change = False\n\n    # It is a bug if not exactly one folder got here\n    if len(folder_paths) != 1:\n        self.set_selected_item(\"\")\n        self._set_is_valid(False)\n        return\n\n    folder_path = tuple(folder_paths)[0]\n\n    is_valid = False\n    if self._selected_items:\n        is_valid = True\n\n    valid_task_names = []\n    for task_name in self._selected_items:\n        _is_valid = self._model.is_task_name_valid(folder_path, task_name)\n        if _is_valid:\n            valid_task_names.append(task_name)\n        else:\n            is_valid = _is_valid\n\n    self._selected_items = valid_task_names\n    if len(self._selected_items) == 0:\n        self.set_selected_item(\"\")\n\n    elif len(self._selected_items) == 1:\n        self.set_selected_item(self._selected_items[0])\n\n    else:\n        multiselection_text = self._multiselection_text\n        if multiselection_text is None:\n            multiselection_text = \"|\".join(self._selected_items)\n        self.set_selected_item(multiselection_text)\n\n    self._set_is_valid(is_valid)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/product_context.html#client.ayon_core.tools.publisher.widgets.product_context.TasksCombobox.set_multiselection_text","title":"<code>set_multiselection_text(text)</code>","text":"<p>Change text shown when multiple different tasks are in context.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/product_context.py</code> <pre><code>def set_multiselection_text(self, text):\n    \"\"\"Change text shown when multiple different tasks are in context.\"\"\"\n    self._multiselection_text = text\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/product_context.html#client.ayon_core.tools.publisher.widgets.product_context.TasksCombobox.set_selected_item","title":"<code>set_selected_item(item_name)</code>","text":"<p>Set task which is set on selected instance.</p> <p>Parameters:</p> Name Type Description Default <code>item_name</code> <code>str</code> <p>Task name which should be selected.</p> required Source code in <code>client/ayon_core/tools/publisher/widgets/product_context.py</code> <pre><code>def set_selected_item(self, item_name):\n    \"\"\"Set task which is set on selected instance.\n\n    Args:\n        item_name(str): Task name which should be selected.\n    \"\"\"\n    idx = self.findText(item_name)\n    # Set current index (must be set to -1 if is invalid)\n    self.setCurrentIndex(idx)\n    self.set_text(item_name)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/product_context.html#client.ayon_core.tools.publisher.widgets.product_context.TasksCombobox.set_selected_items","title":"<code>set_selected_items(folder_task_combinations=None)</code>","text":"<p>Set items for selected instances.</p> <p>Parameters:</p> Name Type Description Default <code>folder_task_combinations</code> <code>list</code> <p>List of tuples. Each item in the list contain folder path and task name.</p> <code>None</code> Source code in <code>client/ayon_core/tools/publisher/widgets/product_context.py</code> <pre><code>def set_selected_items(self, folder_task_combinations=None):\n    \"\"\"Set items for selected instances.\n\n    Args:\n        folder_task_combinations (list): List of tuples. Each item in\n            the list contain folder path and task name.\n    \"\"\"\n    self._proxy_model.set_filter_empty(False)\n    self._proxy_model.sort(0)\n\n    if folder_task_combinations is None:\n        folder_task_combinations = []\n\n    task_names = set()\n    task_names_by_folder_path = collections.defaultdict(set)\n    for folder_path, task_name in folder_task_combinations:\n        task_names.add(task_name)\n        task_names_by_folder_path[folder_path].add(task_name)\n    folder_paths = set(task_names_by_folder_path.keys())\n\n    self._ignore_index_change = True\n\n    self._model.set_folder_paths(folder_paths)\n\n    self._has_value_changed = False\n\n    self._origin_value = copy.deepcopy(folder_task_combinations)\n\n    self._origin_selection = list(task_names)\n    self._selected_items = list(task_names)\n    # Reset current index\n    self.setCurrentIndex(-1)\n    is_valid = True\n    if not task_names:\n        self.set_selected_item(\"\")\n\n    elif len(task_names) == 1:\n        task_name = tuple(task_names)[0]\n        idx = self.findText(task_name)\n        is_valid = not idx &lt; 0\n        if not is_valid and len(folder_paths) &gt; 1:\n            is_valid = self._validate_task_names_by_folder_paths(\n                task_names_by_folder_path\n            )\n        self.set_selected_item(task_name)\n\n    else:\n        for task_name in task_names:\n            idx = self.findText(task_name)\n            is_valid = not idx &lt; 0\n            if not is_valid:\n                break\n\n        if not is_valid and len(folder_paths) &gt; 1:\n            is_valid = self._validate_task_names_by_folder_paths(\n                task_names_by_folder_path\n            )\n        multiselection_text = self._multiselection_text\n        if multiselection_text is None:\n            multiselection_text = \"|\".join(task_names)\n        self.set_selected_item(multiselection_text)\n\n    self._set_is_valid(is_valid)\n\n    self._ignore_index_change = False\n\n    self.value_changed.emit()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/product_context.html#client.ayon_core.tools.publisher.widgets.product_context.TasksCombobox.set_text","title":"<code>set_text(text)</code>","text":"<p>Set context shown in combobox without changing selected items.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/product_context.py</code> <pre><code>def set_text(self, text):\n    \"\"\"Set context shown in combobox without changing selected items.\"\"\"\n    if text == self._text:\n        return\n\n    self._text = text\n    self.repaint()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/product_context.html#client.ayon_core.tools.publisher.widgets.product_context.VariantInputWidget","title":"<code>VariantInputWidget</code>","text":"<p>               Bases: <code>PlaceholderLineEdit</code></p> <p>Input widget for variant.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/product_context.py</code> <pre><code>class VariantInputWidget(PlaceholderLineEdit):\n    \"\"\"Input widget for variant.\"\"\"\n    value_changed = QtCore.Signal()\n\n    def __init__(self, parent):\n        super().__init__(parent)\n\n        self.setObjectName(\"VariantInput\")\n        self.setToolTip(VARIANT_TOOLTIP)\n\n        name_pattern = \"^[{}]*$\".format(PRODUCT_NAME_ALLOWED_SYMBOLS)\n        self._name_pattern = name_pattern\n        self._compiled_name_pattern = re.compile(name_pattern)\n\n        self._origin_value = []\n        self._current_value = []\n\n        self._ignore_value_change = False\n        self._has_value_changed = False\n        self._multiselection_text = None\n\n        self._is_valid = True\n\n        self.textChanged.connect(self._on_text_change)\n\n    def is_valid(self):\n        \"\"\"Is variant text valid.\"\"\"\n        return self._is_valid\n\n    def has_value_changed(self):\n        \"\"\"Value of variant has changed.\"\"\"\n        return self._has_value_changed\n\n    def _set_state_property(self, state):\n        current_value = self.property(\"state\")\n        if current_value != state:\n            self.setProperty(\"state\", state)\n            self.style().polish(self)\n\n    def set_multiselection_text(self, text):\n        \"\"\"Change text of multiselection.\"\"\"\n        self._multiselection_text = text\n\n    def confirm_value(self):\n        self._origin_value = copy.deepcopy(self._current_value)\n        self._has_value_changed = False\n\n    def _set_is_valid(self, valid):\n        if valid == self._is_valid:\n            return\n        self._is_valid = valid\n        state = \"\"\n        if not valid:\n            state = \"invalid\"\n        self._set_state_property(state)\n\n    def _on_text_change(self):\n        if self._ignore_value_change:\n            return\n\n        is_valid = bool(self._compiled_name_pattern.match(self.text()))\n        self._set_is_valid(is_valid)\n\n        self._current_value = [self.text()]\n        self._has_value_changed = self._current_value != self._origin_value\n\n        self.value_changed.emit()\n\n    def reset_to_origin(self):\n        \"\"\"Set origin value of selected instances.\"\"\"\n        self.set_value(self._origin_value)\n\n    def get_value(self):\n        \"\"\"Get current value.\n\n        Origin value returned if didn't change.\n        \"\"\"\n        return copy.deepcopy(self._current_value)\n\n    def set_value(self, variants=None):\n        \"\"\"Set value of currently selected instances.\"\"\"\n        if variants is None:\n            variants = []\n\n        self._ignore_value_change = True\n\n        self._has_value_changed = False\n\n        self._origin_value = list(variants)\n        self._current_value = list(variants)\n\n        self.setPlaceholderText(\"\")\n        if not variants:\n            self.setText(\"\")\n\n        elif len(variants) == 1:\n            self.setText(self._current_value[0])\n\n        else:\n            multiselection_text = self._multiselection_text\n            if multiselection_text is None:\n                multiselection_text = \"|\".join(variants)\n            self.setText(\"\")\n            self.setPlaceholderText(multiselection_text)\n\n        self._ignore_value_change = False\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/product_context.html#client.ayon_core.tools.publisher.widgets.product_context.VariantInputWidget.get_value","title":"<code>get_value()</code>","text":"<p>Get current value.</p> <p>Origin value returned if didn't change.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/product_context.py</code> <pre><code>def get_value(self):\n    \"\"\"Get current value.\n\n    Origin value returned if didn't change.\n    \"\"\"\n    return copy.deepcopy(self._current_value)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/product_context.html#client.ayon_core.tools.publisher.widgets.product_context.VariantInputWidget.has_value_changed","title":"<code>has_value_changed()</code>","text":"<p>Value of variant has changed.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/product_context.py</code> <pre><code>def has_value_changed(self):\n    \"\"\"Value of variant has changed.\"\"\"\n    return self._has_value_changed\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/product_context.html#client.ayon_core.tools.publisher.widgets.product_context.VariantInputWidget.is_valid","title":"<code>is_valid()</code>","text":"<p>Is variant text valid.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/product_context.py</code> <pre><code>def is_valid(self):\n    \"\"\"Is variant text valid.\"\"\"\n    return self._is_valid\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/product_context.html#client.ayon_core.tools.publisher.widgets.product_context.VariantInputWidget.reset_to_origin","title":"<code>reset_to_origin()</code>","text":"<p>Set origin value of selected instances.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/product_context.py</code> <pre><code>def reset_to_origin(self):\n    \"\"\"Set origin value of selected instances.\"\"\"\n    self.set_value(self._origin_value)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/product_context.html#client.ayon_core.tools.publisher.widgets.product_context.VariantInputWidget.set_multiselection_text","title":"<code>set_multiselection_text(text)</code>","text":"<p>Change text of multiselection.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/product_context.py</code> <pre><code>def set_multiselection_text(self, text):\n    \"\"\"Change text of multiselection.\"\"\"\n    self._multiselection_text = text\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/product_context.html#client.ayon_core.tools.publisher.widgets.product_context.VariantInputWidget.set_value","title":"<code>set_value(variants=None)</code>","text":"<p>Set value of currently selected instances.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/product_context.py</code> <pre><code>def set_value(self, variants=None):\n    \"\"\"Set value of currently selected instances.\"\"\"\n    if variants is None:\n        variants = []\n\n    self._ignore_value_change = True\n\n    self._has_value_changed = False\n\n    self._origin_value = list(variants)\n    self._current_value = list(variants)\n\n    self.setPlaceholderText(\"\")\n    if not variants:\n        self.setText(\"\")\n\n    elif len(variants) == 1:\n        self.setText(self._current_value[0])\n\n    else:\n        multiselection_text = self._multiselection_text\n        if multiselection_text is None:\n            multiselection_text = \"|\".join(variants)\n        self.setText(\"\")\n        self.setPlaceholderText(multiselection_text)\n\n    self._ignore_value_change = False\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/product_info.html","title":"product_info","text":""},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/product_info.html#client.ayon_core.tools.publisher.widgets.product_info.ProductInfoWidget","title":"<code>ProductInfoWidget</code>","text":"<p>               Bases: <code>QWidget</code></p> <p>Wrapper widget where attributes of instance/s are modified. \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510 \u2502   Global        \u2502             \u2502 \u2502   attributes    \u2502  Thumbnail  \u2502  TOP \u2502                 \u2502             \u2502 \u251c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2524 \u2502  Creator    \u2502   Publish       \u2502 \u2502  attributes \u2502   plugin        \u2502  BOTTOM \u2502             \u2502   attributes    \u2502 \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518</p> Source code in <code>client/ayon_core/tools/publisher/widgets/product_info.py</code> <pre><code>class ProductInfoWidget(QtWidgets.QWidget):\n    \"\"\"Wrapper widget where attributes of instance/s are modified.\n    \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n    \u2502   Global        \u2502             \u2502\n    \u2502   attributes    \u2502  Thumbnail  \u2502  TOP\n    \u2502                 \u2502             \u2502\n    \u251c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2524\n    \u2502  Creator    \u2502   Publish       \u2502\n    \u2502  attributes \u2502   plugin        \u2502  BOTTOM\n    \u2502             \u2502   attributes    \u2502\n    \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n    \"\"\"\n    convert_requested = QtCore.Signal()\n\n    def __init__(\n        self, controller: AbstractPublisherFrontend, parent: QtWidgets.QWidget\n    ):\n        super().__init__(parent)\n\n        # TOP PART\n        top_widget = QtWidgets.QWidget(self)\n\n        # Global attributes\n        global_attrs_widget = GlobalAttrsWidget(controller, top_widget)\n        thumbnail_widget = ThumbnailWidget(controller, top_widget)\n\n        top_layout = QtWidgets.QHBoxLayout(top_widget)\n        top_layout.setContentsMargins(0, 0, 0, 0)\n        top_layout.addWidget(global_attrs_widget, 7)\n        top_layout.addWidget(thumbnail_widget, 3)\n\n        # BOTTOM PART\n        bottom_widget = QtWidgets.QWidget(self)\n\n        # Wrap Creator attributes to widget to be able add convert button\n        creator_widget = QtWidgets.QWidget(bottom_widget)\n\n        # Convert button widget (with layout to handle stretch)\n        convert_widget = QtWidgets.QWidget(creator_widget)\n        convert_label = QtWidgets.QLabel(creator_widget)\n        # Set the label text with 'setText' to apply html\n        convert_label.setText(\n            (\n                \"Found old publishable products\"\n                \" incompatible with new publisher.\"\n                \"&lt;br/&gt;&lt;br/&gt;Press the &lt;b&gt;update products&lt;/b&gt; button\"\n                \" to automatically update them\"\n                \" to be able to publish again.\"\n            )\n        )\n        convert_label.setWordWrap(True)\n        convert_label.setAlignment(QtCore.Qt.AlignCenter)\n\n        convert_btn = QtWidgets.QPushButton(\n            \"Update products\", convert_widget\n        )\n        convert_separator = QtWidgets.QFrame(convert_widget)\n        convert_separator.setObjectName(\"Separator\")\n        convert_separator.setMinimumHeight(1)\n        convert_separator.setMaximumHeight(1)\n\n        convert_layout = QtWidgets.QGridLayout(convert_widget)\n        convert_layout.setContentsMargins(5, 0, 5, 0)\n        convert_layout.setVerticalSpacing(10)\n        convert_layout.addWidget(convert_label, 0, 0, 1, 3)\n        convert_layout.addWidget(convert_btn, 1, 1)\n        convert_layout.addWidget(convert_separator, 2, 0, 1, 3)\n        convert_layout.setColumnStretch(0, 1)\n        convert_layout.setColumnStretch(1, 0)\n        convert_layout.setColumnStretch(2, 1)\n\n        # Creator attributes widget\n        creator_attrs_widget = CreatorAttrsWidget(\n            controller, creator_widget\n        )\n        creator_layout = QtWidgets.QVBoxLayout(creator_widget)\n        creator_layout.setContentsMargins(0, 0, 0, 0)\n        creator_layout.addWidget(convert_widget, 0)\n        creator_layout.addWidget(creator_attrs_widget, 1)\n\n        publish_attrs_widget = PublishPluginAttrsWidget(\n            controller, bottom_widget\n        )\n\n        bottom_separator = QtWidgets.QWidget(bottom_widget)\n        bottom_separator.setObjectName(\"Separator\")\n        bottom_separator.setMinimumWidth(1)\n\n        bottom_layout = QtWidgets.QHBoxLayout(bottom_widget)\n        bottom_layout.setContentsMargins(0, 0, 0, 0)\n        bottom_layout.addWidget(creator_widget, 1)\n        bottom_layout.addWidget(bottom_separator, 0)\n        bottom_layout.addWidget(publish_attrs_widget, 1)\n\n        top_bottom = QtWidgets.QWidget(self)\n        top_bottom.setObjectName(\"Separator\")\n        top_bottom.setMinimumHeight(1)\n\n        layout = QtWidgets.QVBoxLayout(self)\n        layout.addWidget(top_widget, 0)\n        layout.addWidget(top_bottom, 0)\n        layout.addWidget(bottom_widget, 1)\n\n        self._convertor_identifiers = None\n        self._current_instances = []\n        self._context_selected = False\n        self._all_instances_valid = True\n\n        convert_btn.clicked.connect(self._on_convert_click)\n        thumbnail_widget.thumbnail_created.connect(self._on_thumbnail_create)\n        thumbnail_widget.thumbnail_cleared.connect(self._on_thumbnail_clear)\n\n        controller.register_event_callback(\n            \"create.model.instances.context.changed\",\n            self._on_instance_context_change\n        )\n        controller.register_event_callback(\n            \"instance.thumbnail.changed\",\n            self._on_thumbnail_changed\n        )\n\n        self._controller: AbstractPublisherFrontend = controller\n\n        self._convert_widget = convert_widget\n\n        self.global_attrs_widget = global_attrs_widget\n\n        self.creator_attrs_widget = creator_attrs_widget\n        self.publish_attrs_widget = publish_attrs_widget\n        self._thumbnail_widget = thumbnail_widget\n\n        self.top_bottom = top_bottom\n        self.bottom_separator = bottom_separator\n\n    def set_current_instances(\n        self, instances, context_selected, convertor_identifiers\n    ):\n        \"\"\"Change currently selected items.\n\n        Args:\n            instances (List[InstanceItem]): List of currently selected\n                instances.\n            context_selected (bool): Is context selected.\n            convertor_identifiers (List[str]): Identifiers of convert items.\n\n        \"\"\"\n        s_convertor_identifiers = set(convertor_identifiers)\n        self._current_instances = instances\n        self._context_selected = context_selected\n        self._convertor_identifiers = s_convertor_identifiers\n        self._refresh_instances()\n\n    def _refresh_instances(self):\n        instance_ids = {\n            instance.id\n            for instance in self._current_instances\n        }\n        context_info_by_id = self._controller.get_instances_context_info(\n            instance_ids\n        )\n\n        all_valid = True\n        for context_info in context_info_by_id.values():\n            if not context_info.is_valid:\n                all_valid = False\n                break\n\n        self._all_instances_valid = all_valid\n\n        self._convert_widget.setVisible(len(self._convertor_identifiers) &gt; 0)\n        self.global_attrs_widget.set_current_instances(\n            self._current_instances\n        )\n        self.creator_attrs_widget.set_current_instances(instance_ids)\n        self.publish_attrs_widget.set_current_instances(\n            instance_ids, self._context_selected\n        )\n        self.creator_attrs_widget.set_instances_valid(all_valid)\n        self.publish_attrs_widget.set_instances_valid(all_valid)\n\n        self._update_thumbnails()\n\n    def _on_instance_context_change(self):\n        instance_ids = {\n            instance.id\n            for instance in self._current_instances\n        }\n        context_info_by_id = self._controller.get_instances_context_info(\n            instance_ids\n        )\n        all_valid = True\n        for instance_id, context_info in context_info_by_id.items():\n            if not context_info.is_valid:\n                all_valid = False\n                break\n\n        self._all_instances_valid = all_valid\n        self.creator_attrs_widget.set_instances_valid(all_valid)\n        self.publish_attrs_widget.set_instances_valid(all_valid)\n\n    def _on_convert_click(self):\n        self.convert_requested.emit()\n\n    def _on_thumbnail_create(self, path):\n        instance_ids = [\n            instance.id\n            for instance in self._current_instances\n        ]\n        if self._context_selected:\n            instance_ids.append(None)\n\n        if not instance_ids:\n            return\n\n        mapping = {}\n        if len(instance_ids) == 1:\n            mapping[instance_ids[0]] = path\n\n        else:\n            for instance_id in instance_ids:\n                root = os.path.dirname(path)\n                ext = os.path.splitext(path)[-1]\n                dst_path = os.path.join(root, str(uuid.uuid4()) + ext)\n                shutil.copy(path, dst_path)\n                mapping[instance_id] = dst_path\n\n        self._controller.set_thumbnail_paths_for_instances(mapping)\n\n    def _on_thumbnail_clear(self):\n        instance_ids = [\n            instance.id\n            for instance in self._current_instances\n        ]\n        if self._context_selected:\n            instance_ids.append(None)\n\n        if not instance_ids:\n            return\n\n        mapping = {\n            instance_id: None\n            for instance_id in instance_ids\n        }\n        self._controller.set_thumbnail_paths_for_instances(mapping)\n\n    def _on_thumbnail_changed(self, event):\n        self._update_thumbnails()\n\n    def _update_thumbnails(self):\n        instance_ids = [\n            instance.id\n            for instance in self._current_instances\n        ]\n        if self._context_selected:\n            instance_ids.append(None)\n\n        if not instance_ids:\n            self._thumbnail_widget.setVisible(False)\n            self._thumbnail_widget.set_current_thumbnails(None)\n            return\n\n        mapping = self._controller.get_thumbnail_paths_for_instances(\n            instance_ids\n        )\n        thumbnail_paths = []\n        for instance_id in instance_ids:\n            path = mapping[instance_id]\n            if path:\n                thumbnail_paths.append(path)\n\n        self._thumbnail_widget.setVisible(True)\n        self._thumbnail_widget.set_current_thumbnails(thumbnail_paths)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/product_info.html#client.ayon_core.tools.publisher.widgets.product_info.ProductInfoWidget.set_current_instances","title":"<code>set_current_instances(instances, context_selected, convertor_identifiers)</code>","text":"<p>Change currently selected items.</p> <p>Parameters:</p> Name Type Description Default <code>instances</code> <code>List[InstanceItem]</code> <p>List of currently selected instances.</p> required <code>context_selected</code> <code>bool</code> <p>Is context selected.</p> required <code>convertor_identifiers</code> <code>List[str]</code> <p>Identifiers of convert items.</p> required Source code in <code>client/ayon_core/tools/publisher/widgets/product_info.py</code> <pre><code>def set_current_instances(\n    self, instances, context_selected, convertor_identifiers\n):\n    \"\"\"Change currently selected items.\n\n    Args:\n        instances (List[InstanceItem]): List of currently selected\n            instances.\n        context_selected (bool): Is context selected.\n        convertor_identifiers (List[str]): Identifiers of convert items.\n\n    \"\"\"\n    s_convertor_identifiers = set(convertor_identifiers)\n    self._current_instances = instances\n    self._context_selected = context_selected\n    self._convertor_identifiers = s_convertor_identifiers\n    self._refresh_instances()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/publish_frame.html","title":"publish_frame","text":""},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/publish_frame.html#client.ayon_core.tools.publisher.widgets.publish_frame.PublishFrame","title":"<code>PublishFrame</code>","text":"<p>               Bases: <code>QWidget</code></p> <p>Frame showed during publishing.</p> <p>Shows all information related to publishing. Contains validation error widget which is showed if only validation error happens during validation.</p> <p>Processing layer is default layer. Validation error layer is shown if only validation exception is raised during publishing. Report layer is available only when publishing process is stopped and must be manually triggered to change into that layer.</p> <p>+------------------------------------------------------------------------+ |                             &lt; Main label &gt;                             | |                             &lt; Label top &gt;                              | |        (####                10%                  )       | |   | |   | +------------------------------------------------------------------------+ Source code in <code>client/ayon_core/tools/publisher/widgets/publish_frame.py</code> <pre><code>class PublishFrame(QtWidgets.QWidget):\n    \"\"\"Frame showed during publishing.\n\n    Shows all information related to publishing. Contains validation error\n    widget which is showed if only validation error happens during validation.\n\n    Processing layer is default layer. Validation error layer is shown if only\n    validation exception is raised during publishing. Report layer is available\n    only when publishing process is stopped and must be manually triggered to\n    change into that layer.\n\n    +------------------------------------------------------------------------+\n    |                             &lt; Main label &gt;                             |\n    |                             &lt; Label top &gt;                              |\n    |        (####                10%  &lt;Progress bar&gt;                )       |\n    | &lt;Instance label&gt;                                        &lt;Plugin label&gt; |\n    | &lt;Report&gt;                              &lt;Reset&gt;&lt;Stop&gt;&lt;Validate&gt;&lt;Publish&gt; |\n    +------------------------------------------------------------------------+\n    \"\"\"\n\n    details_page_requested = QtCore.Signal()\n\n    def __init__(\n        self,\n        controller: AbstractPublisherFrontend,\n        borders: int,\n        parent: QtWidgets.QWidget\n    ):\n        super().__init__(parent)\n\n        # Bottom part of widget where process and callback buttons are showed\n        # - QFrame used to be able set background using stylesheets easily\n        #   and not override all children widgets style\n        content_frame = QtWidgets.QFrame(self)\n        content_frame.setObjectName(\"PublishInfoFrame\")\n\n        top_content_widget = QtWidgets.QWidget(content_frame)\n\n        # Center widget displaying current state (without any specific info)\n        main_label = QtWidgets.QLabel(top_content_widget)\n        main_label.setObjectName(\"PublishInfoMainLabel\")\n        main_label.setAlignment(QtCore.Qt.AlignCenter)\n\n        # Supporting labels for main label\n        # Top label is displayed just under main label\n        message_label_top = QtWidgets.QLabel(top_content_widget)\n        message_label_top.setAlignment(QtCore.Qt.AlignCenter)\n\n        # Label showing currently processed instance\n        progress_widget = QtWidgets.QWidget(top_content_widget)\n        instance_plugin_widget = QtWidgets.QWidget(progress_widget)\n        instance_label = QtWidgets.QLabel(\n            \"&lt;Instance name&gt;\", instance_plugin_widget\n        )\n        instance_label.setAlignment(\n            QtCore.Qt.AlignLeft | QtCore.Qt.AlignVCenter\n        )\n        # Label showing currently processed plugin\n        plugin_label = QtWidgets.QLabel(\n            \"&lt;Plugin name&gt;\", instance_plugin_widget\n        )\n        plugin_label.setAlignment(\n            QtCore.Qt.AlignRight | QtCore.Qt.AlignVCenter\n        )\n        instance_plugin_layout = QtWidgets.QHBoxLayout(instance_plugin_widget)\n        instance_plugin_layout.setContentsMargins(0, 0, 0, 0)\n        instance_plugin_layout.addWidget(instance_label, 1)\n        instance_plugin_layout.addWidget(plugin_label, 1)\n\n        # Progress bar showing progress of publishing\n        progress_bar = QtWidgets.QProgressBar(progress_widget)\n        progress_bar.setObjectName(\"PublishProgressBar\")\n\n        progress_layout = QtWidgets.QVBoxLayout(progress_widget)\n        progress_layout.setSpacing(5)\n        progress_layout.setContentsMargins(0, 0, 0, 0)\n        progress_layout.addWidget(instance_plugin_widget, 0)\n        progress_layout.addWidget(progress_bar, 0)\n\n        top_content_layout = QtWidgets.QVBoxLayout(top_content_widget)\n        top_content_layout.setContentsMargins(0, 0, 0, 0)\n        top_content_layout.setSpacing(5)\n        top_content_layout.setAlignment(QtCore.Qt.AlignCenter)\n        top_content_layout.addWidget(main_label)\n        # TODO stretches should be probably replaced by spacing...\n        # - stretch in floating frame doesn't make sense\n        top_content_layout.addWidget(message_label_top)\n        top_content_layout.addWidget(progress_widget)\n\n        # Publishing buttons to stop, reset or trigger publishing\n        footer_widget = QtWidgets.QWidget(content_frame)\n\n        report_btn = PublishReportBtn(footer_widget)\n\n        shrunk_main_label = QtWidgets.QLabel(footer_widget)\n        shrunk_main_label.setObjectName(\"PublishInfoMainLabel\")\n        shrunk_main_label.setAlignment(\n            QtCore.Qt.AlignVCenter | QtCore.Qt.AlignLeft\n        )\n\n        reset_btn = ResetBtn(footer_widget)\n        stop_btn = StopBtn(footer_widget)\n        validate_btn = ValidateBtn(footer_widget)\n        publish_btn = PublishBtn(footer_widget)\n\n        report_btn.add_action(\"Go to details\", \"go_to_report\")\n        report_btn.add_action(\"Copy report\", \"copy_report\")\n        report_btn.add_action(\"Export report\", \"export_report\")\n\n        # Footer on info frame layout\n        footer_layout = QtWidgets.QHBoxLayout(footer_widget)\n        footer_layout.setContentsMargins(0, 0, 0, 0)\n        footer_layout.addWidget(report_btn, 0)\n        footer_layout.addWidget(shrunk_main_label, 1)\n        footer_layout.addWidget(reset_btn, 0)\n        footer_layout.addWidget(stop_btn, 0)\n        footer_layout.addWidget(validate_btn, 0)\n        footer_layout.addWidget(publish_btn, 0)\n\n        # Info frame content\n        content_layout = QtWidgets.QVBoxLayout(content_frame)\n        content_layout.setSpacing(5)\n\n        content_layout.addWidget(top_content_widget)\n        content_layout.addWidget(footer_widget)\n\n        main_layout = QtWidgets.QVBoxLayout(self)\n        main_layout.setContentsMargins(borders, 0, borders, borders)\n        main_layout.addWidget(content_frame)\n\n        shrunk_anim = QtCore.QVariantAnimation()\n        shrunk_anim.setDuration(140)\n        shrunk_anim.setEasingCurve(QtCore.QEasingCurve.InOutQuad)\n\n        # Force translucent background for widgets\n        for widget in (\n            self,\n            top_content_widget,\n            footer_widget,\n            progress_widget,\n            instance_plugin_widget,\n        ):\n            widget.setAttribute(QtCore.Qt.WA_TranslucentBackground)\n\n        report_btn.triggered.connect(self._on_report_triggered)\n        reset_btn.clicked.connect(self._on_reset_clicked)\n        stop_btn.clicked.connect(self._on_stop_clicked)\n        validate_btn.clicked.connect(self._on_validate_clicked)\n        publish_btn.clicked.connect(self._on_publish_clicked)\n\n        shrunk_anim.valueChanged.connect(self._on_shrunk_anim)\n        shrunk_anim.finished.connect(self._on_shrunk_anim_finish)\n\n        controller.register_event_callback(\n            \"publish.reset.finished\", self._on_publish_reset\n        )\n        controller.register_event_callback(\n            \"publish.process.started\", self._on_publish_start\n        )\n        controller.register_event_callback(\n            \"publish.has_validated.changed\", self._on_publish_validated_change\n        )\n        controller.register_event_callback(\n            \"publish.process.stopped\", self._on_publish_stop\n        )\n\n        controller.register_event_callback(\n            \"publish.process.instance.changed\", self._on_instance_change\n        )\n        controller.register_event_callback(\n            \"publish.process.plugin.changed\", self._on_plugin_change\n        )\n\n        self._shrunk_anim = shrunk_anim\n\n        self._controller: AbstractPublisherFrontend = controller\n\n        self._content_frame = content_frame\n        self._content_layout = content_layout\n        self._top_content_layout = top_content_layout\n        self._top_content_widget = top_content_widget\n\n        self._main_label = main_label\n        self._message_label_top = message_label_top\n\n        self._instance_label = instance_label\n        self._plugin_label = plugin_label\n\n        self._progress_bar = progress_bar\n        self._progress_widget = progress_widget\n\n        self._shrunk_main_label = shrunk_main_label\n        self._reset_btn = reset_btn\n        self._stop_btn = stop_btn\n        self._validate_btn = validate_btn\n        self._publish_btn = publish_btn\n\n        self._shrunken = False\n        self._top_widget_max_height = None\n        self._top_widget_size_policy = top_content_widget.sizePolicy()\n        self._last_instance_label = None\n        self._last_plugin_label = None\n\n    def mouseReleaseEvent(self, event):\n        super().mouseReleaseEvent(event)\n        self._change_shrunk_state()\n\n    def _change_shrunk_state(self):\n        self.set_shrunk_state(not self._shrunken)\n\n    def set_shrunk_state(self, shrunk):\n        if shrunk is self._shrunken:\n            return\n\n        if self._top_widget_max_height is None:\n            self._top_widget_max_height = (\n                self._top_content_widget.maximumHeight()\n            )\n\n        self._shrunken = shrunk\n\n        anim_is_running = (\n            self._shrunk_anim.state() == QtCore.QAbstractAnimation.Running\n        )\n        if not self.isVisible():\n            if anim_is_running:\n                self._shrunk_anim.stop()\n            self._on_shrunk_anim_finish()\n            return\n\n        start = 0\n        end = 0\n        if shrunk:\n            start = self._top_content_widget.height()\n        else:\n            if anim_is_running:\n                start = self._shrunk_anim.currentValue()\n            hint = self._top_content_widget.minimumSizeHint()\n            end = hint.height()\n\n        self._shrunk_anim.setStartValue(float(start))\n        self._shrunk_anim.setEndValue(float(end))\n        if not anim_is_running:\n            self._shrunk_anim.start()\n\n    def _on_shrunk_anim(self, value):\n        diff = self._top_content_widget.height() - int(value)\n        if not self._top_content_widget.isVisible():\n            diff -= self._content_layout.spacing()\n\n        window_pos = self.pos()\n        window_pos_y = window_pos.y() + diff\n        window_height = self.height() - diff\n\n        self._top_content_widget.setMinimumHeight(value)\n        self._top_content_widget.setMaximumHeight(value)\n        self._top_content_widget.setVisible(True)\n\n        self.resize(self.width(), window_height)\n        self.move(window_pos.x(), window_pos_y)\n\n    def _on_shrunk_anim_finish(self):\n        self._top_content_widget.setVisible(not self._shrunken)\n        self._top_content_widget.setMinimumHeight(0)\n        self._top_content_widget.setMaximumHeight(\n            self._top_widget_max_height\n        )\n        self._top_content_widget.setSizePolicy(self._top_widget_size_policy)\n\n        if self._shrunken:\n            self._shrunk_main_label.setText(self._main_label.text())\n        else:\n            self._shrunk_main_label.setText(\"\")\n\n        if self._shrunken:\n            content_frame_hint = self._content_frame.sizeHint()\n\n            layout = self.layout()\n            margins = layout.contentsMargins()\n            window_height = (\n                content_frame_hint.height()\n                + margins.bottom()\n                + margins.top()\n            )\n            diff = self.height() - window_height\n            window_pos = self.pos()\n            window_pos_y = window_pos.y() + diff\n            self.resize(self.width(), window_height)\n            self.move(window_pos.x(), window_pos_y)\n\n    def _set_main_label(self, message):\n        self._main_label.setText(message)\n        if self._shrunken:\n            self._shrunk_main_label.setText(message)\n\n    def _on_publish_reset(self):\n        self._last_instance_label = None\n        self._last_plugin_label = None\n\n        self._set_success_property()\n        self._set_progress_visibility(True)\n\n        self._main_label.setText(\"\")\n        self._message_label_top.setText(\"\")\n\n        self._reset_btn.setEnabled(True)\n        self._stop_btn.setEnabled(False)\n        self._validate_btn.setEnabled(True)\n        self._publish_btn.setEnabled(True)\n\n        self._progress_bar.setValue(\n            self._controller.get_publish_progress()\n        )\n        self._progress_bar.setMaximum(\n            self._controller.get_publish_max_progress()\n        )\n\n    def _on_publish_start(self):\n        if self._last_plugin_label:\n            self._plugin_label.setText(self._last_plugin_label)\n\n        if self._last_instance_label:\n            self._instance_label.setText(self._last_instance_label)\n\n        self._set_success_property(3)\n        self._set_progress_visibility(True)\n        self._set_main_label(\"Publishing...\")\n        self._message_label_top.setText(\"\")\n\n        self._reset_btn.setEnabled(False)\n        self._stop_btn.setEnabled(True)\n        self._validate_btn.setEnabled(False)\n        self._publish_btn.setEnabled(False)\n\n        self.set_shrunk_state(False)\n\n    def _on_publish_validated_change(self, event):\n        if event[\"value\"]:\n            self._validate_btn.setEnabled(False)\n\n    def _on_instance_change(self, event):\n        \"\"\"Change instance label when instance is going to be processed.\"\"\"\n\n        self._last_instance_label = event[\"instance_label\"]\n        self._instance_label.setText(event[\"instance_label\"])\n        QtWidgets.QApplication.processEvents()\n\n    def _on_plugin_change(self, event):\n        \"\"\"Change plugin label when instance is going to be processed.\"\"\"\n\n        self._last_plugin_label = event[\"plugin_label\"]\n        self._progress_bar.setValue(self._controller.get_publish_progress())\n        self._plugin_label.setText(event[\"plugin_label\"])\n        QtWidgets.QApplication.processEvents()\n\n    def _on_publish_stop(self):\n        self._progress_bar.setValue(self._controller.get_publish_progress())\n\n        self._reset_btn.setEnabled(True)\n        self._stop_btn.setEnabled(False)\n\n        self._instance_label.setText(\"\")\n        self._plugin_label.setText(\"\")\n\n        publish_enabled = self._controller.publish_can_continue()\n        validate_enabled = (\n            publish_enabled and not self._controller.publish_has_validated()\n        )\n        self._validate_btn.setEnabled(validate_enabled)\n        self._publish_btn.setEnabled(publish_enabled)\n\n        if self._controller.publish_has_crashed():\n            self._set_error_msg()\n\n        elif self._controller.publish_has_validation_errors():\n            self._set_progress_visibility(False)\n            self._set_validation_errors()\n\n        elif self._controller.publish_has_finished():\n            self._set_finished()\n\n        else:\n            self._set_stopped()\n\n    def _set_stopped(self):\n        main_label = \"Publish paused\"\n        if self._controller.publish_has_validated:\n            main_label += \" - Validation passed\"\n\n        self._set_main_label(main_label)\n        self._message_label_top.setText(\n            \"Hit publish (play button) to continue.\"\n        )\n\n        self._set_success_property(4)\n\n    def _set_error_msg(self):\n        \"\"\"Show error message to artist on publish crash.\"\"\"\n\n        self._set_main_label(\"Error happened\")\n        error_info = self._controller.get_publish_error_info()\n\n        error_message = \"Unknown error happened\"\n        if error_info is not None:\n            error_message = error_info.message\n\n        self._message_label_top.setText(error_message)\n\n        self._set_success_property(1)\n\n    def _set_validation_errors(self):\n        self._set_main_label(\"Your publish didn't pass studio validations\")\n        self._message_label_top.setText(\"Check results above please\")\n        self._set_success_property(2)\n\n    def _set_finished(self):\n        self._set_main_label(\"Finished\")\n        self._message_label_top.setText(\"\")\n        self._set_success_property(0)\n\n    def _set_progress_visibility(self, visible):\n        window_height = self.height()\n        self._progress_widget.setVisible(visible)\n        # Ignore rescaling and move of widget if is shrunken of progress bar\n        #   should be visible\n        if self._shrunken or visible:\n            return\n\n        height = self._progress_widget.height()\n        diff = height + self._top_content_layout.spacing()\n\n        window_pos = self.pos()\n        window_pos_y = self.pos().y() + diff\n        window_height -= diff\n\n        self.resize(self.width(), window_height)\n        self.move(window_pos.x(), window_pos_y)\n\n    def _set_success_property(self, state=None):\n        \"\"\"Apply styles by state.\n\n        State enum:\n        - None - Default state after restart\n        - 0 - Success finish\n        - 1 - Error happened\n        - 2 - Validation error\n        - 3 - In progress\n        - 4 - Stopped/Paused\n        \"\"\"\n\n        if state is None:\n            state = \"\"\n        else:\n            state = str(state)\n\n        for widget in (self._progress_bar, self._content_frame):\n            if widget.property(\"state\") != state:\n                widget.setProperty(\"state\", state)\n                widget.style().polish(widget)\n\n    def _on_report_triggered(self, identifier):\n        if identifier == \"export_report\":\n            self._controller.emit_event(\n                \"export_report.request\", {}, \"publish_frame\")\n\n        elif identifier == \"copy_report\":\n            self._controller.emit_event(\n                \"copy_report.request\", {}, \"publish_frame\")\n\n        elif identifier == \"go_to_report\":\n            self.details_page_requested.emit()\n\n    def _on_reset_clicked(self):\n        self._controller.reset()\n\n    def _on_stop_clicked(self):\n        self._controller.stop_publish()\n\n    def _on_validate_clicked(self):\n        self._controller.validate()\n\n    def _on_publish_clicked(self):\n        self._controller.publish()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/report_page.html","title":"report_page","text":""},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/report_page.html#client.ayon_core.tools.publisher.widgets.report_page.ActionButton","title":"<code>ActionButton</code>","text":"<p>               Bases: <code>BaseClickableFrame</code></p> <p>Plugin's action callback button.</p> <p>Action may have label or icon or both.</p> <p>Parameters:</p> Name Type Description Default <code>plugin_action_item</code> <code>PublishPluginActionItem</code> <p>Action item that can be triggered by its id.</p> required Source code in <code>client/ayon_core/tools/publisher/widgets/report_page.py</code> <pre><code>class ActionButton(BaseClickableFrame):\n    \"\"\"Plugin's action callback button.\n\n    Action may have label or icon or both.\n\n    Args:\n        plugin_action_item (PublishPluginActionItem): Action item that can be\n            triggered by its id.\n    \"\"\"\n\n    action_clicked = QtCore.Signal(str, str)\n\n    def __init__(self, plugin_action_item, parent):\n        super().__init__(parent)\n\n        self.setObjectName(\"PublishActionButton\")\n\n        self.plugin_action_item = plugin_action_item\n\n        action_label = plugin_action_item.label\n        action_icon = plugin_action_item.icon\n        label_widget = QtWidgets.QLabel(action_label, self)\n        icon_label = None\n        if action_icon:\n            icon_label = IconValuePixmapLabel(action_icon, self)\n\n        layout = QtWidgets.QHBoxLayout(self)\n        layout.setContentsMargins(5, 0, 5, 0)\n        layout.addWidget(label_widget, 1)\n        if icon_label:\n            layout.addWidget(icon_label, 0)\n\n        self.setSizePolicy(\n            QtWidgets.QSizePolicy.Minimum,\n            self.sizePolicy().verticalPolicy()\n        )\n\n    def _mouse_release_callback(self):\n        self.action_clicked.emit(\n            self.plugin_action_item.plugin_id,\n            self.plugin_action_item.action_id\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/report_page.html#client.ayon_core.tools.publisher.widgets.report_page.CrashWidget","title":"<code>CrashWidget</code>","text":"<p>               Bases: <code>QWidget</code></p> <p>Widget shown when publishing crashes.</p> <p>Contains only minimal information for artist with easy access to report actions.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/report_page.py</code> <pre><code>class CrashWidget(QtWidgets.QWidget):\n    \"\"\"Widget shown when publishing crashes.\n\n    Contains only minimal information for artist with easy access to report\n    actions.\n    \"\"\"\n\n    def __init__(\n        self, controller: AbstractPublisherFrontend, parent: QtWidgets.QWidget\n    ):\n        super().__init__(parent)\n\n        main_label = QtWidgets.QLabel(\"This is not your fault\", self)\n        main_label.setAlignment(QtCore.Qt.AlignCenter)\n        main_label.setObjectName(\"PublishCrashMainLabel\")\n\n        report_label = QtWidgets.QLabel(\n            (\n                \"Please report the error to your pipeline support\"\n                \" using one of the options below.\"\n            ),\n            self\n        )\n        report_label.setAlignment(QtCore.Qt.AlignCenter)\n        report_label.setWordWrap(True)\n        report_label.setObjectName(\"PublishCrashReportLabel\")\n\n        btns_widget = QtWidgets.QWidget(self)\n        copy_clipboard_btn = QtWidgets.QPushButton(\n            \"Copy to clipboard\", btns_widget)\n        save_to_disk_btn = QtWidgets.QPushButton(\n            \"Save to disk\", btns_widget)\n\n        btns_layout = QtWidgets.QHBoxLayout(btns_widget)\n        btns_layout.setContentsMargins(0, 0, 0, 0)\n        btns_layout.setSpacing(0)\n        btns_layout.addStretch(1)\n        btns_layout.addWidget(copy_clipboard_btn, 0)\n        btns_layout.addSpacing(20)\n        btns_layout.addWidget(save_to_disk_btn, 0)\n        btns_layout.addStretch(1)\n\n        layout = QtWidgets.QVBoxLayout(self)\n        layout.setContentsMargins(5, 5, 5, 5)\n        layout.setSpacing(0)\n        layout.addStretch(1)\n        layout.addWidget(main_label, 0)\n        layout.addSpacing(30)\n        layout.addWidget(report_label, 0)\n        layout.addSpacing(30)\n        layout.addWidget(btns_widget, 0)\n        layout.addStretch(2)\n\n        copy_clipboard_btn.clicked.connect(self._on_copy_to_clipboard)\n        save_to_disk_btn.clicked.connect(self._on_save_to_disk_click)\n\n        self._controller: AbstractPublisherFrontend = controller\n\n    def _on_copy_to_clipboard(self):\n        self._controller.emit_event(\n            \"copy_report.request\", {}, \"report_page\")\n\n    def _on_save_to_disk_click(self):\n        self._controller.emit_event(\n            \"export_report.request\", {}, \"report_page\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/report_page.html#client.ayon_core.tools.publisher.widgets.report_page.InstanceLogsWidget","title":"<code>InstanceLogsWidget</code>","text":"<p>               Bases: <code>QWidget</code></p> <p>Widget showing logs of one publish instance.</p> <p>Parameters:</p> Name Type Description Default <code>label</code> <code>str</code> <p>Instance label.</p> required <code>logs</code> <code>list[ReportLogWrap]</code> <p>Logs to show.</p> required <code>parent</code> <code>QWidget</code> <p>Parent widget.</p> required Source code in <code>client/ayon_core/tools/publisher/widgets/report_page.py</code> <pre><code>class InstanceLogsWidget(QtWidgets.QWidget):\n    \"\"\"Widget showing logs of one publish instance.\n\n    Args:\n        label (str): Instance label.\n        logs (list[ReportLogWrap]): Logs to show.\n        parent (QtWidgets.QWidget): Parent widget.\n    \"\"\"\n\n    def __init__(\n        self,\n        label: str,\n        logs: list[ReportLogWrap],\n        parent: QtWidgets.QWidget,\n    ):\n        super().__init__(parent)\n\n        self.setAttribute(QtCore.Qt.WA_TranslucentBackground)\n\n        label_widget = QtWidgets.QLabel(label, self)\n        label_widget.setObjectName(\"PublishInstanceLogsLabel\")\n        label_widget.setWordWrap(True)\n        logs_grid = LogsWithIconsView(logs, self)\n\n        layout = QtWidgets.QVBoxLayout(self)\n        layout.setContentsMargins(0, 0, 0, 0)\n        layout.addWidget(label_widget, 0)\n        layout.addWidget(logs_grid, 0)\n\n        self._logs_grid = logs_grid\n        self._label_widget = label_widget\n\n    def set_log_filters(\n        self,\n        visibility_filter: int,\n        plugin_ids: set[str],\n    ) -&gt; None:\n        \"\"\"Change logs filter.\n\n        Args:\n            visibility_filter (int): Number contained of flags for each log\n                type and level.\n            plugin_ids (set[str]): Plugin ids to which are logs filtered.\n\n        \"\"\"\n        self._logs_grid.set_log_filters(visibility_filter, plugin_ids)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/report_page.html#client.ayon_core.tools.publisher.widgets.report_page.InstanceLogsWidget.set_log_filters","title":"<code>set_log_filters(visibility_filter, plugin_ids)</code>","text":"<p>Change logs filter.</p> <p>Parameters:</p> Name Type Description Default <code>visibility_filter</code> <code>int</code> <p>Number contained of flags for each log type and level.</p> required <code>plugin_ids</code> <code>set[str]</code> <p>Plugin ids to which are logs filtered.</p> required Source code in <code>client/ayon_core/tools/publisher/widgets/report_page.py</code> <pre><code>def set_log_filters(\n    self,\n    visibility_filter: int,\n    plugin_ids: set[str],\n) -&gt; None:\n    \"\"\"Change logs filter.\n\n    Args:\n        visibility_filter (int): Number contained of flags for each log\n            type and level.\n        plugin_ids (set[str]): Plugin ids to which are logs filtered.\n\n    \"\"\"\n    self._logs_grid.set_log_filters(visibility_filter, plugin_ids)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/report_page.html#client.ayon_core.tools.publisher.widgets.report_page.InstancesLogsView","title":"<code>InstancesLogsView</code>","text":"<p>               Bases: <code>QFrame</code></p> <p>Publish instances logs view widget.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/report_page.py</code> <pre><code>class InstancesLogsView(QtWidgets.QFrame):\n    \"\"\"Publish instances logs view widget.\"\"\"\n\n    def __init__(self, parent):\n        super().__init__(parent)\n\n        self.setAttribute(QtCore.Qt.WA_TranslucentBackground)\n\n        scroll_area = QtWidgets.QScrollArea(self)\n        scroll_area.setWidgetResizable(True)\n        scroll_area.setHorizontalScrollBarPolicy(QtCore.Qt.ScrollBarAlwaysOff)\n        scroll_area.setVerticalScrollBarPolicy(QtCore.Qt.ScrollBarAsNeeded)\n        scroll_area.setAttribute(QtCore.Qt.WA_TranslucentBackground)\n        scrollbar_bg = scroll_area.verticalScrollBar().parent()\n        if scrollbar_bg:\n            scrollbar_bg.setAttribute(QtCore.Qt.WA_TranslucentBackground)\n\n        content_wrap_widget = QtWidgets.QWidget(scroll_area)\n        content_wrap_widget.setMinimumWidth(80)\n        content_wrap_widget.setObjectName(\"InstancesLogsView\")\n\n        content_widget = QtWidgets.QWidget(content_wrap_widget)\n        content_widget.setAttribute(QtCore.Qt.WA_TranslucentBackground)\n\n        content_layout = QtWidgets.QVBoxLayout(content_widget)\n        content_layout.setContentsMargins(8, 8, 8, 8)\n        content_layout.setSpacing(15)\n\n        scroll_area.setWidget(content_wrap_widget)\n\n        content_wrap_layout = QtWidgets.QVBoxLayout(content_wrap_widget)\n        content_wrap_layout.setContentsMargins(0, 0, 0, 0)\n        content_wrap_layout.addWidget(content_widget, 0)\n        content_wrap_layout.addStretch(1)\n\n        layout = QtWidgets.QVBoxLayout(self)\n        layout.setContentsMargins(0, 0, 0, 0)\n        layout.addWidget(scroll_area, 1)\n\n        self._visible_filters = (\n            LOG_INFO_VISIBLE\n            | LOG_WARNING_VISIBLE\n            | LOG_ERROR_VISIBLE\n            | LOG_CRITICAL_VISIBLE\n            | ERROR_VISIBLE\n            | INFO_VISIBLE\n        )\n\n        self._content_widget = content_widget\n        self._content_layout = content_layout\n\n        self._instance_logs: list[tuple[str, str, list[ReportLogWrap]]] = []\n        self._views_by_instance_id: dict[str, InstanceLogsWidget] = {}\n        self._is_showed: bool = False\n        self._update_needed: bool = False\n        self._instance_ids_filter: set[str] = set()\n        self._plugin_ids_filter: set[str] = set()\n\n    def showEvent(self, event):\n        super().showEvent(event)\n        self._is_showed = True\n        self._update_instances()\n\n    def hideEvent(self, event):\n        super().hideEvent(event)\n        self._is_showed = False\n\n    def closeEvent(self, event):\n        super().closeEvent(event)\n        self._is_showed = False\n\n    def update_report(self, report: PublishReport):\n        \"\"\"Update publish instance from report.\n\n        Args:\n            report (PublishReport): Instance data from report.\n\n        \"\"\"\n        instances_h = [\n            _SortHelper(i)\n            for i in report.instances_by_id.values()\n        ]\n        instances_h.sort()\n        instances = [i.instance for i in instances_h]\n\n        logs_by_instance_id = collections.defaultdict(list)\n        for plugin_id, instance_id, log in report.iter_logs():\n            logs_by_instance_id[instance_id].append(\n                ReportLogWrap(\n                    log=log,\n                    instance_id=instance_id,\n                    plugin_id=plugin_id,\n                )\n            )\n\n        instance_logs: list[tuple[str, str, list[ReportLogWrap]]] = [\n            (\n                instance.id,\n                instance.label,\n                logs_by_instance_id[instance.id]\n            )\n            for instance in instances\n        ]\n        instance_logs.insert(\n            0,\n            (\n                CONTEXT_ID,\n                report.context.label,\n                logs_by_instance_id[CONTEXT_ID]\n            )\n        )\n\n        self._instance_logs = instance_logs\n        self._update_needed: bool = True\n        self._instance_ids_filter: set[str] = set()\n        self._plugin_ids_filter: set[str] = set()\n        self._update_instances()\n\n    def set_instances_filter(self, instance_ids: set[str]) -&gt; None:\n        \"\"\"Set instance filter.\n\n        Args:\n            instance_ids (set[str] | None): Set of instance ids to keep\n                visible. Pass empty set to hide all items.\n        \"\"\"\n        if self._instance_ids_filter == instance_ids:\n            return\n\n        self._instance_ids_filter = instance_ids\n        if not instance_ids:\n            for widget in self._views_by_instance_id.values():\n                widget.setVisible(True)\n        else:\n            for instance_id, widget in self._views_by_instance_id.items():\n                widget.setVisible(instance_id in self._instance_ids_filter)\n\n    def set_plugins_filter(self, plugin_ids: set[str]) -&gt; None:\n        if self._plugin_ids_filter == plugin_ids:\n            return\n        self._plugin_ids_filter = plugin_ids\n        for widget in self._views_by_instance_id.values():\n            widget.set_log_filters(\n                self._visible_filters, self._plugin_ids_filter\n            )\n\n    def _update_instances(self) -&gt; None:\n        if not self._is_showed or not self._update_needed:\n            return\n\n        self._clear_widgets()\n\n        for instance_id, instance_label, logs in self._instance_logs:\n            widget = InstanceLogsWidget(\n                instance_label, logs, self._content_widget\n            )\n            if (\n                self._instance_ids_filter\n                and instance_id not in self._instance_ids_filter\n            ):\n                widget.setVisible(False)\n            widget.set_log_filters(\n                self._visible_filters, self._plugin_ids_filter\n            )\n\n            self._views_by_instance_id[instance_id] = widget\n            self._content_layout.addWidget(widget, 0)\n\n    def _clear_widgets(self):\n        \"\"\"Remove all widgets from layout and from cache.\"\"\"\n\n        while self._content_layout.count():\n            item = self._content_layout.takeAt(0)\n            widget = item.widget()\n            if widget:\n                widget.setVisible(False)\n                widget.deleteLater()\n        self._views_by_instance_id = {}\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/report_page.html#client.ayon_core.tools.publisher.widgets.report_page.InstancesLogsView.set_instances_filter","title":"<code>set_instances_filter(instance_ids)</code>","text":"<p>Set instance filter.</p> <p>Parameters:</p> Name Type Description Default <code>instance_ids</code> <code>set[str] | None</code> <p>Set of instance ids to keep visible. Pass empty set to hide all items.</p> required Source code in <code>client/ayon_core/tools/publisher/widgets/report_page.py</code> <pre><code>def set_instances_filter(self, instance_ids: set[str]) -&gt; None:\n    \"\"\"Set instance filter.\n\n    Args:\n        instance_ids (set[str] | None): Set of instance ids to keep\n            visible. Pass empty set to hide all items.\n    \"\"\"\n    if self._instance_ids_filter == instance_ids:\n        return\n\n    self._instance_ids_filter = instance_ids\n    if not instance_ids:\n        for widget in self._views_by_instance_id.values():\n            widget.setVisible(True)\n    else:\n        for instance_id, widget in self._views_by_instance_id.items():\n            widget.setVisible(instance_id in self._instance_ids_filter)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/report_page.html#client.ayon_core.tools.publisher.widgets.report_page.InstancesLogsView.update_report","title":"<code>update_report(report)</code>","text":"<p>Update publish instance from report.</p> <p>Parameters:</p> Name Type Description Default <code>report</code> <code>PublishReport</code> <p>Instance data from report.</p> required Source code in <code>client/ayon_core/tools/publisher/widgets/report_page.py</code> <pre><code>def update_report(self, report: PublishReport):\n    \"\"\"Update publish instance from report.\n\n    Args:\n        report (PublishReport): Instance data from report.\n\n    \"\"\"\n    instances_h = [\n        _SortHelper(i)\n        for i in report.instances_by_id.values()\n    ]\n    instances_h.sort()\n    instances = [i.instance for i in instances_h]\n\n    logs_by_instance_id = collections.defaultdict(list)\n    for plugin_id, instance_id, log in report.iter_logs():\n        logs_by_instance_id[instance_id].append(\n            ReportLogWrap(\n                log=log,\n                instance_id=instance_id,\n                plugin_id=plugin_id,\n            )\n        )\n\n    instance_logs: list[tuple[str, str, list[ReportLogWrap]]] = [\n        (\n            instance.id,\n            instance.label,\n            logs_by_instance_id[instance.id]\n        )\n        for instance in instances\n    ]\n    instance_logs.insert(\n        0,\n        (\n            CONTEXT_ID,\n            report.context.label,\n            logs_by_instance_id[CONTEXT_ID]\n        )\n    )\n\n    self._instance_logs = instance_logs\n    self._update_needed: bool = True\n    self._instance_ids_filter: set[str] = set()\n    self._plugin_ids_filter: set[str] = set()\n    self._update_instances()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/report_page.html#client.ayon_core.tools.publisher.widgets.report_page.LogIconFrame","title":"<code>LogIconFrame</code>","text":"<p>               Bases: <code>QFrame</code></p> <p>Draw log item icon next to message.</p> Todos <p>Paint event could be slow, maybe we could cache the image into pixmaps     so each item does not have to redraw it again.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/report_page.py</code> <pre><code>class LogIconFrame(QtWidgets.QFrame):\n    \"\"\"Draw log item icon next to message.\n\n    Todos:\n        Paint event could be slow, maybe we could cache the image into pixmaps\n            so each item does not have to redraw it again.\n    \"\"\"\n\n    info_color = QtGui.QColor(\"#ffffff\")\n    error_color = QtGui.QColor(\"#ff4a4a\")\n    level_to_color = dict((\n        (10, QtGui.QColor(\"#ff66e8\")),\n        (20, QtGui.QColor(\"#66abff\")),\n        (30, QtGui.QColor(\"#ffba66\")),\n        (40, QtGui.QColor(\"#ff4d58\")),\n        (50, QtGui.QColor(\"#ff4f75\")),\n    ))\n    _error_pix = None\n    _validation_error_pix = None\n\n    def __init__(self, parent, log_type, log_level, is_validation_error):\n        super().__init__(parent)\n\n        self.setAttribute(QtCore.Qt.WA_TranslucentBackground)\n\n        self._is_record = log_type == \"record\"\n        self._is_error = log_type == \"error\"\n        self._is_validation_error = bool(is_validation_error)\n        self._log_color = self.level_to_color.get(log_level)\n\n    @classmethod\n    def get_validation_error_icon(cls):\n        if cls._validation_error_pix is None:\n            cls._validation_error_pix = get_pixmap(\"warning\")\n        return cls._validation_error_pix\n\n    @classmethod\n    def get_error_icon(cls):\n        if cls._error_pix is None:\n            cls._error_pix = get_pixmap(\"error\")\n        return cls._error_pix\n\n    def minimumSizeHint(self):\n        fm = self.fontMetrics()\n        size = fm.height()\n        return QtCore.QSize(size, size)\n\n    def paintEvent(self, event):\n        painter = QtGui.QPainter(self)\n        painter.setRenderHints(\n            QtGui.QPainter.Antialiasing\n            | QtGui.QPainter.SmoothPixmapTransform\n        )\n        painter.setPen(QtCore.Qt.NoPen)\n        rect = self.rect()\n        new_size = min(rect.width(), rect.height())\n        new_rect = QtCore.QRect(1, 1, new_size - 2, new_size - 2)\n        if self._is_error:\n            if self._is_validation_error:\n                error_icon = self.get_validation_error_icon()\n            else:\n                error_icon = self.get_error_icon()\n            scaled_error_icon = error_icon.scaled(\n                new_rect.size(),\n                QtCore.Qt.KeepAspectRatio,\n                QtCore.Qt.SmoothTransformation\n            )\n            painter.drawPixmap(new_rect, scaled_error_icon)\n\n        else:\n            if self._is_record:\n                color = self._log_color\n            else:\n                color = QtGui.QColor(255, 255, 255)\n            painter.setBrush(color)\n            painter.drawEllipse(new_rect)\n        painter.end()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/report_page.html#client.ayon_core.tools.publisher.widgets.report_page.LogsWithIconsView","title":"<code>LogsWithIconsView</code>","text":"<p>               Bases: <code>QWidget</code></p> <p>Show logs in a grid with 2 columns.</p> <p>First column is for icon second is for message.</p> Todos <p>Add filtering by type (exception, debug, info, etc.).</p> Source code in <code>client/ayon_core/tools/publisher/widgets/report_page.py</code> <pre><code>class LogsWithIconsView(QtWidgets.QWidget):\n    \"\"\"Show logs in a grid with 2 columns.\n\n    First column is for icon second is for message.\n\n    Todos:\n        Add filtering by type (exception, debug, info, etc.).\n    \"\"\"\n\n    def __init__(\n        self,\n        log_wraps: list[ReportLogWrap],\n        parent: QtWidgets.QWidget,\n    ):\n        super().__init__(parent)\n        self.setAttribute(QtCore.Qt.WA_TranslucentBackground)\n\n        logs_layout = QtWidgets.QVBoxLayout(self)\n        logs_layout.setContentsMargins(0, 0, 0, 0)\n        logs_layout.setSpacing(4)\n\n        widgets_by_flag = collections.defaultdict(list)\n        widgets_by_plugins_id = collections.defaultdict(list)\n\n        for log_wrap in log_wraps:\n            widget = LogItemWidget(log_wrap, self)\n            widgets_by_flag[widget.type_flag].append(widget)\n            widgets_by_plugins_id[widget.plugin_id].append(widget)\n            logs_layout.addWidget(widget, 0)\n\n        self._widgets_by_flag: dict[str, list[LogItemWidget]] = (\n            widgets_by_flag\n        )\n        self._widgets_by_plugins_id: dict[str, list[LogItemWidget]] = (\n            widgets_by_plugins_id\n        )\n\n        self._visibility_by_flags = {\n            LOG_DEBUG_VISIBLE: True,\n            LOG_INFO_VISIBLE: True,\n            LOG_WARNING_VISIBLE: True,\n            LOG_ERROR_VISIBLE: True,\n            LOG_CRITICAL_VISIBLE: True,\n            ERROR_VISIBLE: True,\n            INFO_VISIBLE: True,\n        }\n        self._flags_filter: int = sum(self._visibility_by_flags.keys())\n        self._plugin_ids_filter: set[str] = set()\n\n    def set_log_filters(\n        self, visibility_filter: int, plugin_ids: set[str]\n    ) -&gt; None:\n        if self._flags_filter != visibility_filter:\n            self._flags_filter = visibility_filter\n            self._update_flags_filtering()\n\n        if self._plugin_ids_filter != plugin_ids:\n            self._plugin_ids_filter = plugin_ids\n            self._update_plugin_filtering()\n\n    def _update_flags_filtering(self):\n        for flag in (\n            LOG_DEBUG_VISIBLE,\n            LOG_INFO_VISIBLE,\n            LOG_WARNING_VISIBLE,\n            LOG_ERROR_VISIBLE,\n            LOG_CRITICAL_VISIBLE,\n            ERROR_VISIBLE,\n            INFO_VISIBLE,\n        ):\n            visible = (self._flags_filter &amp; flag) != 0\n            if visible is not self._visibility_by_flags[flag]:\n                self._visibility_by_flags[flag] = visible\n                for widget in self._widgets_by_flag[flag]:\n                    widget.set_log_type_filtered(not visible)\n\n    def _update_plugin_filtering(self):\n        if not self._plugin_ids_filter:\n            for widgets in self._widgets_by_plugins_id.values():\n                for widget in widgets:\n                    widget.set_plugin_filtered(False)\n\n        else:\n            for plugin_id, widgets in self._widgets_by_plugins_id.items():\n                filtered = plugin_id not in self._plugin_ids_filter\n                for widget in widgets:\n                    widget.set_plugin_filtered(filtered)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/report_page.html#client.ayon_core.tools.publisher.widgets.report_page.PublishActionsWidget","title":"<code>PublishActionsWidget</code>","text":"<p>               Bases: <code>QFrame</code></p> <p>Wrapper widget for plugin actions.</p> <p>Change actions based on selected publish error.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/report_page.py</code> <pre><code>class PublishActionsWidget(QtWidgets.QFrame):\n    \"\"\"Wrapper widget for plugin actions.\n\n    Change actions based on selected publish error.\n    \"\"\"\n\n    def __init__(\n        self, controller: AbstractPublisherFrontend, parent: QtWidgets.QWidget\n    ):\n        super().__init__(parent)\n\n        self.setAttribute(QtCore.Qt.WA_TranslucentBackground)\n\n        content_widget = QtWidgets.QWidget(self)\n        content_layout = FlowLayout(content_widget)\n        content_layout.setContentsMargins(0, 0, 0, 0)\n\n        layout = QtWidgets.QHBoxLayout(self)\n        layout.setContentsMargins(0, 0, 0, 0)\n        layout.addWidget(content_widget)\n\n        self._controller: AbstractPublisherFrontend = controller\n        self._content_widget = content_widget\n        self._content_layout = content_layout\n\n        self._actions_mapping = {}\n\n        self._visible_mode = True\n\n    def _update_visibility(self):\n        self.setVisible(\n            self._visible_mode\n            and self._content_layout.count() &gt; 0\n        )\n\n    def set_visible_mode(self, visible):\n        if self._visible_mode is visible:\n            return\n        self._visible_mode = visible\n        self._update_visibility()\n\n    def _clear(self):\n        \"\"\"Remove actions from widget.\"\"\"\n        while self._content_layout.count():\n            item = self._content_layout.takeAt(0)\n            widget = item.widget()\n            if widget:\n                widget.setVisible(False)\n                widget.deleteLater()\n        self._actions_mapping = {}\n\n    def set_error_info(self, error_info):\n        \"\"\"Set selected plugin and show it's actions.\n\n        Clears current actions from widget and recreate them from the plugin.\n\n        Args:\n            Dict[str, Any]: Object holding error items, title and possible\n                actions to run.\n        \"\"\"\n\n        self._clear()\n\n        if not error_info:\n            self.setVisible(False)\n            return\n\n        plugin_action_items = error_info[\"plugin_action_items\"]\n        for plugin_action_item in plugin_action_items:\n            if not plugin_action_item.active:\n                continue\n\n            if plugin_action_item.on_filter not in (\"failed\", \"all\"):\n                continue\n\n            action_id = plugin_action_item.action_id\n            self._actions_mapping[action_id] = plugin_action_item\n\n            action_btn = ActionButton(plugin_action_item, self._content_widget)\n            action_btn.action_clicked.connect(self._on_action_click)\n            self._content_layout.addWidget(action_btn)\n\n        self._update_visibility()\n\n    def _on_action_click(self, plugin_id, action_id):\n        self._controller.run_action(plugin_id, action_id)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/report_page.html#client.ayon_core.tools.publisher.widgets.report_page.PublishActionsWidget.set_error_info","title":"<code>set_error_info(error_info)</code>","text":"<p>Set selected plugin and show it's actions.</p> <p>Clears current actions from widget and recreate them from the plugin.</p> <p>Parameters:</p> Name Type Description Default <code>Dict[str, Any]</code> <p>Object holding error items, title and possible actions to run.</p> required Source code in <code>client/ayon_core/tools/publisher/widgets/report_page.py</code> <pre><code>def set_error_info(self, error_info):\n    \"\"\"Set selected plugin and show it's actions.\n\n    Clears current actions from widget and recreate them from the plugin.\n\n    Args:\n        Dict[str, Any]: Object holding error items, title and possible\n            actions to run.\n    \"\"\"\n\n    self._clear()\n\n    if not error_info:\n        self.setVisible(False)\n        return\n\n    plugin_action_items = error_info[\"plugin_action_items\"]\n    for plugin_action_item in plugin_action_items:\n        if not plugin_action_item.active:\n            continue\n\n        if plugin_action_item.on_filter not in (\"failed\", \"all\"):\n            continue\n\n        action_id = plugin_action_item.action_id\n        self._actions_mapping[action_id] = plugin_action_item\n\n        action_btn = ActionButton(plugin_action_item, self._content_widget)\n        action_btn.action_clicked.connect(self._on_action_click)\n        self._content_layout.addWidget(action_btn)\n\n    self._update_visibility()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/report_page.html#client.ayon_core.tools.publisher.widgets.report_page.PublishErrorInstanceList","title":"<code>PublishErrorInstanceList</code>","text":"<p>               Bases: <code>QListView</code></p> <p>List of publish instances that caused a publish error.</p> <p>Instances are collected per plugin's publish error title.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/report_page.py</code> <pre><code>class PublishErrorInstanceList(QtWidgets.QListView):\n    \"\"\"List of publish instances that caused a publish error.\n\n    Instances are collected per plugin's publish error title.\n    \"\"\"\n    def __init__(self, *args, **kwargs):\n        super().__init__(*args, **kwargs)\n\n        self.setObjectName(\"PublishErrorInstanceList\")\n\n        self.setHorizontalScrollBarPolicy(QtCore.Qt.ScrollBarAlwaysOff)\n        self.setSelectionMode(QtWidgets.QAbstractItemView.ExtendedSelection)\n\n    def minimumSizeHint(self):\n        return self.sizeHint()\n\n    def sizeHint(self):\n        result = super().sizeHint()\n        row_count = self.model().rowCount()\n        height = 0\n        if row_count &gt; 0:\n            height = self.sizeHintForRow(0) * row_count\n        result.setHeight(height)\n        return result\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/report_page.html#client.ayon_core.tools.publisher.widgets.report_page.PublishErrorTitleWidget","title":"<code>PublishErrorTitleWidget</code>","text":"<p>               Bases: <code>QWidget</code></p> <p>Title of publish error.</p> <p>Widget is used as radio button so requires clickable functionality and changing style on selection/deselection.</p> <p>Has toggle button to show/hide instances on which publish error happened if there is a list (Valdation error may happen on context).</p> Source code in <code>client/ayon_core/tools/publisher/widgets/report_page.py</code> <pre><code>class PublishErrorTitleWidget(QtWidgets.QWidget):\n    \"\"\"Title of publish error.\n\n    Widget is used as radio button so requires clickable functionality and\n    changing style on selection/deselection.\n\n    Has toggle button to show/hide instances on which publish error happened\n    if there is a list (Valdation error may happen on context).\n    \"\"\"\n\n    selected = QtCore.Signal(str)\n    instance_changed = QtCore.Signal(str)\n    _error_pixmap = None\n\n    def __init__(self, title_id, error_info, parent):\n        super().__init__(parent)\n\n        self._title_id = title_id\n        self._error_info = error_info\n        self._selected = False\n\n        instances_model = QtGui.QStandardItemModel()\n\n        instance_ids = []\n\n        items = []\n        is_context_plugin = False\n        is_crashing_error = False\n        for error_item in error_info[\"error_items\"]:\n            is_crashing_error = not error_item.is_validation_error\n            is_context_plugin = error_item.is_context_plugin\n            if is_context_plugin:\n                instance_ids.append(CONTEXT_ID)\n                # Add fake item to have minimum size hint of view widget\n                items.append(QtGui.QStandardItem(CONTEXT_LABEL))\n                continue\n\n            label = error_item.instance_label\n            item = QtGui.QStandardItem(label)\n            item.setFlags(\n                QtCore.Qt.ItemIsEnabled | QtCore.Qt.ItemIsSelectable\n            )\n            item.setData(label, QtCore.Qt.ToolTipRole)\n            item.setData(error_item.instance_id, INSTANCE_ID_ROLE)\n            items.append(item)\n            instance_ids.append(error_item.instance_id)\n\n        if items:\n            root_item = instances_model.invisibleRootItem()\n            root_item.appendRows(items)\n\n        title_frame = ClickableFrame(self)\n        title_frame.setObjectName(\"PublishErrorTitleFrame\")\n\n        toggle_instance_btn = QtWidgets.QToolButton(title_frame)\n        toggle_instance_btn.setObjectName(\"ArrowBtn\")\n        toggle_instance_btn.setArrowType(QtCore.Qt.RightArrow)\n        toggle_instance_btn.setMaximumWidth(14)\n        if is_context_plugin:\n            toggle_instance_btn.setArrowType(QtCore.Qt.NoArrow)\n\n        icon_label = None\n        if is_crashing_error:\n            error_pixmap = self._get_error_pixmap()\n            icon_label = PublishPixmapLabel(error_pixmap, self)\n\n        label_widget = QtWidgets.QLabel(error_info[\"title\"], title_frame)\n\n        title_frame_layout = QtWidgets.QHBoxLayout(title_frame)\n        title_frame_layout.setContentsMargins(8, 8, 8, 8)\n        title_frame_layout.setSpacing(0)\n        if icon_label is not None:\n            title_frame_layout.addWidget(icon_label, 0)\n            title_frame_layout.addSpacing(6)\n        title_frame_layout.addWidget(label_widget, 1)\n        title_frame_layout.addWidget(toggle_instance_btn, 0)\n\n        instances_view = PublishErrorInstanceList(self)\n        instances_view.setModel(instances_model)\n\n        self.setLayoutDirection(QtCore.Qt.LeftToRight)\n\n        view_widget = QtWidgets.QWidget(self)\n        view_layout = QtWidgets.QHBoxLayout(view_widget)\n        view_layout.setContentsMargins(0, 0, 0, 0)\n        view_layout.setSpacing(0)\n        view_layout.addSpacing(14)\n        view_layout.addWidget(instances_view, 0)\n\n        layout = QtWidgets.QVBoxLayout(self)\n        layout.setSpacing(0)\n        layout.setContentsMargins(0, 0, 0, 0)\n        layout.addWidget(title_frame, 0)\n        layout.addWidget(view_widget, 0)\n        view_widget.setVisible(False)\n\n        if not is_context_plugin:\n            toggle_instance_btn.clicked.connect(self._on_toggle_btn_click)\n\n        title_frame.clicked.connect(self._mouse_release_callback)\n        instances_view.selectionModel().selectionChanged.connect(\n            self._on_selection_change\n        )\n\n        self._title_frame = title_frame\n\n        self._toggle_instance_btn = toggle_instance_btn\n\n        self._view_widget = view_widget\n\n        self._instances_model = instances_model\n        self._instances_view = instances_view\n\n        self._is_context_plugin = is_context_plugin\n        self._is_crashing_error = is_crashing_error\n\n        self._instance_ids = instance_ids\n        self._expanded = False\n\n    def sizeHint(self):\n        result = super().sizeHint()\n        expected_width = max(\n            self._view_widget.minimumSizeHint().width(),\n            self._view_widget.sizeHint().width()\n        )\n\n        if expected_width &lt; 200:\n            expected_width = 200\n\n        if result.width() &lt; expected_width:\n            result.setWidth(expected_width)\n\n        return result\n\n    def minimumSizeHint(self):\n        return self.sizeHint()\n\n    def _mouse_release_callback(self):\n        \"\"\"Mark this widget as selected on click.\"\"\"\n\n        self.set_selected(True)\n\n    @property\n    def is_selected(self):\n        \"\"\"Is widget marked a selected.\n\n        Returns:\n            bool: Item is selected or not.\n        \"\"\"\n\n        return self._selected\n\n    @property\n    def id(self):\n        return self._title_id\n\n    @property\n    def is_crashing_error(self):\n        return self._is_crashing_error\n\n    def _change_style_property(self, selected):\n        \"\"\"Change style of widget based on selection.\"\"\"\n\n        value = \"1\" if selected else \"\"\n        self._title_frame.setProperty(\"selected\", value)\n        self._title_frame.style().polish(self._title_frame)\n\n    def set_selected(self, selected=None):\n        \"\"\"Change selected state of widget.\"\"\"\n\n        if selected is None:\n            selected = not self._selected\n\n        # Clear instance view selection on deselect\n        if not selected:\n            self._instances_view.clearSelection()\n\n        # Skip if has same value\n        if selected == self._selected:\n            return\n\n        self._selected = selected\n        self._change_style_property(selected)\n        if selected:\n            self.selected.emit(self._title_id)\n            self._set_expanded(True)\n\n    @classmethod\n    def _get_error_pixmap(cls):\n        if cls._error_pixmap is None:\n            cls._error_pixmap = get_pixmap(\"error\")\n        return cls._error_pixmap\n\n    def _on_toggle_btn_click(self):\n        \"\"\"Show/hide instances list.\"\"\"\n\n        self._set_expanded()\n\n    def _set_expanded(self, expanded=None):\n        if expanded is None:\n            expanded = not self._expanded\n\n        elif expanded is self._expanded:\n            return\n\n        if expanded and self._is_context_plugin:\n            return\n\n        self._expanded = expanded\n        self._view_widget.setVisible(expanded)\n        if expanded:\n            self._toggle_instance_btn.setArrowType(QtCore.Qt.DownArrow)\n        else:\n            self._toggle_instance_btn.setArrowType(QtCore.Qt.RightArrow)\n\n    def _on_selection_change(self):\n        self.instance_changed.emit(self._title_id)\n\n    def get_selected_instances(self):\n        if self._is_context_plugin:\n            return [CONTEXT_ID]\n        sel_model = self._instances_view.selectionModel()\n        return [\n            index.data(INSTANCE_ID_ROLE)\n            for index in sel_model.selectedIndexes()\n            if index.isValid()\n        ]\n\n    def get_available_instances(self):\n        return list(self._instance_ids)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/report_page.html#client.ayon_core.tools.publisher.widgets.report_page.PublishErrorTitleWidget.is_selected","title":"<code>is_selected</code>  <code>property</code>","text":"<p>Is widget marked a selected.</p> <p>Returns:</p> Name Type Description <code>bool</code> <p>Item is selected or not.</p>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/report_page.html#client.ayon_core.tools.publisher.widgets.report_page.PublishErrorTitleWidget.set_selected","title":"<code>set_selected(selected=None)</code>","text":"<p>Change selected state of widget.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/report_page.py</code> <pre><code>def set_selected(self, selected=None):\n    \"\"\"Change selected state of widget.\"\"\"\n\n    if selected is None:\n        selected = not self._selected\n\n    # Clear instance view selection on deselect\n    if not selected:\n        self._instances_view.clearSelection()\n\n    # Skip if has same value\n    if selected == self._selected:\n        return\n\n    self._selected = selected\n    self._change_style_property(selected)\n    if selected:\n        self.selected.emit(self._title_id)\n        self._set_expanded(True)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/report_page.html#client.ayon_core.tools.publisher.widgets.report_page.PublishErrorsView","title":"<code>PublishErrorsView</code>","text":"<p>               Bases: <code>QWidget</code></p> Source code in <code>client/ayon_core/tools/publisher/widgets/report_page.py</code> <pre><code>class PublishErrorsView(QtWidgets.QWidget):\n    selection_changed = QtCore.Signal()\n\n    def __init__(self, parent):\n        super().__init__(parent)\n\n        errors_scroll = VerticalScrollArea(self)\n        errors_scroll.setWidgetResizable(True)\n\n        errors_widget = QtWidgets.QWidget(errors_scroll)\n        errors_widget.setAttribute(QtCore.Qt.WA_TranslucentBackground)\n\n        errors_scroll.setWidget(errors_widget)\n\n        errors_layout = QtWidgets.QVBoxLayout(errors_widget)\n        # Add 5 margin to left so the is not directly on the edge of the\n        #   scroll widget\n        errors_layout.setContentsMargins(5, 0, 0, 0)\n\n        main_layout = QtWidgets.QVBoxLayout(self)\n        main_layout.setContentsMargins(8, 8, 8, 8)\n        main_layout.addWidget(errors_scroll, 1)\n\n        self._errors_widget = errors_widget\n        self._errors_layout = errors_layout\n        self._title_widgets = {}\n        self._previous_select = None\n\n    def _clear(self):\n        \"\"\"Delete all dynamic widgets and hide all wrappers.\"\"\"\n\n        self._title_widgets = {}\n        self._previous_select = None\n        while self._errors_layout.count():\n            item = self._errors_layout.takeAt(0)\n            widget = item.widget()\n            if widget:\n                widget.deleteLater()\n\n    def set_errors(self, grouped_error_items):\n        \"\"\"Set errors into context and created titles.\n\n        Args:\n            grouped_error_items (List[Dict[str, Any]]): Report\n                with information about publish errors and publish plugin\n                actions.\n        \"\"\"\n\n        self._clear()\n\n        select_id = None\n        for title_item in grouped_error_items:\n            title_id = title_item[\"id\"]\n            if select_id is None:\n                select_id = title_id\n            widget = PublishErrorTitleWidget(title_id, title_item, self)\n            widget.selected.connect(self._on_select)\n            widget.instance_changed.connect(self._on_instance_change)\n            if widget.is_crashing_error:\n                select_id = title_id\n            self._errors_layout.addWidget(widget)\n            self._title_widgets[title_id] = widget\n\n        self._errors_layout.addStretch(1)\n\n        if select_id:\n            self._title_widgets[select_id].set_selected(True)\n        else:\n            self.selection_changed.emit()\n\n        self.updateGeometry()\n\n    def _on_select(self, title_id):\n        if self._previous_select:\n            if self._previous_select.id == title_id:\n                return\n            self._previous_select.set_selected(False)\n\n        self._previous_select = self._title_widgets[title_id]\n        self.selection_changed.emit()\n\n    def _on_instance_change(self, title_id):\n        if self._previous_select and self._previous_select.id != title_id:\n            self._title_widgets[title_id].set_selected(True)\n        else:\n            self.selection_changed.emit()\n\n    def get_selected_items(self):\n        if not self._previous_select:\n            return None, []\n\n        title_id = self._previous_select.id\n        instance_ids = self._previous_select.get_selected_instances()\n        if not instance_ids:\n            instance_ids = self._previous_select.get_available_instances()\n        return title_id, instance_ids\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/report_page.html#client.ayon_core.tools.publisher.widgets.report_page.PublishErrorsView.set_errors","title":"<code>set_errors(grouped_error_items)</code>","text":"<p>Set errors into context and created titles.</p> <p>Parameters:</p> Name Type Description Default <code>grouped_error_items</code> <code>List[Dict[str, Any]]</code> <p>Report with information about publish errors and publish plugin actions.</p> required Source code in <code>client/ayon_core/tools/publisher/widgets/report_page.py</code> <pre><code>def set_errors(self, grouped_error_items):\n    \"\"\"Set errors into context and created titles.\n\n    Args:\n        grouped_error_items (List[Dict[str, Any]]): Report\n            with information about publish errors and publish plugin\n            actions.\n    \"\"\"\n\n    self._clear()\n\n    select_id = None\n    for title_item in grouped_error_items:\n        title_id = title_item[\"id\"]\n        if select_id is None:\n            select_id = title_id\n        widget = PublishErrorTitleWidget(title_id, title_item, self)\n        widget.selected.connect(self._on_select)\n        widget.instance_changed.connect(self._on_instance_change)\n        if widget.is_crashing_error:\n            select_id = title_id\n        self._errors_layout.addWidget(widget)\n        self._title_widgets[title_id] = widget\n\n    self._errors_layout.addStretch(1)\n\n    if select_id:\n        self._title_widgets[select_id].set_selected(True)\n    else:\n        self.selection_changed.emit()\n\n    self.updateGeometry()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/report_page.html#client.ayon_core.tools.publisher.widgets.report_page.PublishInstanceCardWidget","title":"<code>PublishInstanceCardWidget</code>","text":"<p>               Bases: <code>BaseClickableFrame</code></p> Source code in <code>client/ayon_core/tools/publisher/widgets/report_page.py</code> <pre><code>class PublishInstanceCardWidget(BaseClickableFrame):\n    selection_requested = QtCore.Signal(str)\n\n    _warning_pix = None\n    _error_pix = None\n    _success_pix = None\n    _in_progress_pix = None\n\n    def __init__(\n        self,\n        instance_id: str,\n        label: str,\n        icon,\n        logs_summary: LogsSummary,\n        publish_can_continue: bool,\n        parent: QtWidgets.QWidget,\n    ):\n        super().__init__(parent)\n\n        self.setObjectName(\"CardViewWidget\")\n\n        icon_widget = IconValuePixmapLabel(icon, self)\n        icon_widget.setObjectName(\"ProductTypeIconLabel\")\n\n        label_widget = QtWidgets.QLabel(label, self)\n\n        if instance_id in logs_summary.errored_instance_ids:\n            state_pix = self.get_error_pix()\n        elif instance_id in logs_summary.warned_instance_ids:\n            state_pix = self.get_warning_pix()\n        elif publish_can_continue:\n            state_pix = self.get_in_progress_pix()\n        else:\n            state_pix = self.get_success_pix()\n\n        state_label = IconValuePixmapLabel(state_pix, self)\n\n        layout = QtWidgets.QHBoxLayout(self)\n        layout.setContentsMargins(10, 7, 10, 7)\n        layout.addWidget(icon_widget, 0)\n        layout.addWidget(label_widget, 1)\n        layout.addWidget(state_label, 0)\n\n        # Change direction -&gt; parent is scroll area where scrolls are on\n        #   left side\n        self.setLayoutDirection(QtCore.Qt.LeftToRight)\n\n        self._id = instance_id\n\n        self._selected = False\n\n        self._update_style_state()\n\n    @classmethod\n    def _prepare_pixes(cls):\n        publisher_colors = get_objected_colors(\"publisher\")\n        cls._warning_pix = paint_image_with_color(\n            get_image(\"warning\"),\n            publisher_colors[\"warning\"].get_qcolor()\n        )\n        cls._error_pix = paint_image_with_color(\n            get_image(\"error\"),\n            publisher_colors[\"error\"].get_qcolor()\n        )\n        cls._success_pix = paint_image_with_color(\n            get_image(\"success\"),\n            publisher_colors[\"success\"].get_qcolor()\n        )\n        cls._in_progress_pix = paint_image_with_color(\n            get_image(\"success\"),\n            publisher_colors[\"progress\"].get_qcolor()\n        )\n\n    @classmethod\n    def get_warning_pix(cls):\n        if cls._warning_pix is None:\n            cls._prepare_pixes()\n        return cls._warning_pix\n\n    @classmethod\n    def get_error_pix(cls):\n        if cls._error_pix is None:\n            cls._prepare_pixes()\n        return cls._error_pix\n\n    @classmethod\n    def get_success_pix(cls):\n        if cls._success_pix is None:\n            cls._prepare_pixes()\n        return cls._success_pix\n\n    @classmethod\n    def get_in_progress_pix(cls):\n        if cls._in_progress_pix is None:\n            cls._prepare_pixes()\n        return cls._in_progress_pix\n\n    @property\n    def id(self):\n        \"\"\"Id of card.\n\n        Returns:\n            str: Id of item.\n        \"\"\"\n\n        return self._id\n\n    @property\n    def is_selected(self):\n        \"\"\"Is card selected.\n\n        Returns:\n            bool: Item widget is marked as selected.\n        \"\"\"\n\n        return self._selected\n\n    def set_selected(self, selected):\n        \"\"\"Set card as selected.\n\n        Args:\n            selected (bool): Item should be marked as selected.\n        \"\"\"\n\n        if selected == self._selected:\n            return\n        self._selected = selected\n        self._update_style_state()\n\n    def _update_style_state(self):\n        state = \"\"\n        if self._selected:\n            state = \"selected\"\n\n        self.setProperty(\"state\", state)\n        self.style().polish(self)\n\n    def _mouse_release_callback(self):\n        \"\"\"Trigger selected signal.\"\"\"\n\n        self.selection_requested.emit(self.id)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/report_page.html#client.ayon_core.tools.publisher.widgets.report_page.PublishInstanceCardWidget.id","title":"<code>id</code>  <code>property</code>","text":"<p>Id of card.</p> <p>Returns:</p> Name Type Description <code>str</code> <p>Id of item.</p>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/report_page.html#client.ayon_core.tools.publisher.widgets.report_page.PublishInstanceCardWidget.is_selected","title":"<code>is_selected</code>  <code>property</code>","text":"<p>Is card selected.</p> <p>Returns:</p> Name Type Description <code>bool</code> <p>Item widget is marked as selected.</p>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/report_page.html#client.ayon_core.tools.publisher.widgets.report_page.PublishInstanceCardWidget.set_selected","title":"<code>set_selected(selected)</code>","text":"<p>Set card as selected.</p> <p>Parameters:</p> Name Type Description Default <code>selected</code> <code>bool</code> <p>Item should be marked as selected.</p> required Source code in <code>client/ayon_core/tools/publisher/widgets/report_page.py</code> <pre><code>def set_selected(self, selected):\n    \"\"\"Set card as selected.\n\n    Args:\n        selected (bool): Item should be marked as selected.\n    \"\"\"\n\n    if selected == self._selected:\n        return\n    self._selected = selected\n    self._update_style_state()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/report_page.html#client.ayon_core.tools.publisher.widgets.report_page.PublishInstancesViewWidget","title":"<code>PublishInstancesViewWidget</code>","text":"<p>               Bases: <code>QWidget</code></p> Source code in <code>client/ayon_core/tools/publisher/widgets/report_page.py</code> <pre><code>class PublishInstancesViewWidget(QtWidgets.QWidget):\n    # Sane minimum width of instance cards - size calulated using font metrics\n    _min_width_measure_string = 24 * \"O\"\n    selection_changed = QtCore.Signal()\n\n    def __init__(\n        self, controller: AbstractPublisherFrontend, parent: QtWidgets.QWidget\n    ):\n        super().__init__(parent)\n\n        scroll_area = VerticalScrollArea(self)\n        scroll_area.setWidgetResizable(True)\n        scroll_area.setHorizontalScrollBarPolicy(QtCore.Qt.ScrollBarAlwaysOff)\n        scroll_area.setVerticalScrollBarPolicy(QtCore.Qt.ScrollBarAsNeeded)\n        scrollbar_bg = scroll_area.verticalScrollBar().parent()\n        if scrollbar_bg:\n            scrollbar_bg.setAttribute(QtCore.Qt.WA_TranslucentBackground)\n        scroll_area.setViewportMargins(0, 0, 0, 0)\n\n        instance_view = QtWidgets.QWidget(scroll_area)\n\n        scroll_area.setWidget(instance_view)\n\n        instance_layout = QtWidgets.QVBoxLayout(instance_view)\n        instance_layout.setContentsMargins(0, 0, 0, 0)\n        instance_layout.addStretch(1)\n\n        layout = QtWidgets.QVBoxLayout(self)\n        layout.setContentsMargins(0, 0, 0, 0)\n        layout.addWidget(scroll_area, 1)\n\n        self._controller: AbstractPublisherFrontend = controller\n        self._scroll_area = scroll_area\n        self._instance_view = instance_view\n        self._instance_layout = instance_layout\n\n        self._context_widget = None\n\n        self._widgets_by_instance_id = {}\n        self._group_widgets = []\n        self._ordered_widgets = []\n\n        self._explicitly_selected_instance_ids = []\n\n        self.setSizePolicy(\n            QtWidgets.QSizePolicy.Minimum,\n            self.sizePolicy().verticalPolicy()\n        )\n\n    def sizeHint(self):\n        \"\"\"Modify sizeHint based on visibility of scroll bars.\"\"\"\n        # Calculate width hint by content widget and vertical scroll bar\n        scroll_bar = self._scroll_area.verticalScrollBar()\n        view_size = self._instance_view.sizeHint().width()\n        fm = self._instance_view.fontMetrics()\n        width = (\n            max(view_size, fm.width(self._min_width_measure_string))\n            + scroll_bar.sizeHint().width()\n        )\n\n        result = super().sizeHint()\n        result.setWidth(width)\n        return result\n\n    def _get_selected_widgets(self):\n        return [\n            widget\n            for widget in self._ordered_widgets\n            if widget.is_selected\n        ]\n\n    def get_selected_instance_ids(self) -&gt; set[str]:\n        return {\n            widget.id\n            for widget in self._get_selected_widgets()\n        }\n\n    def clear(self):\n        \"\"\"Remove actions from widget.\"\"\"\n        while self._instance_layout.count():\n            item = self._instance_layout.takeAt(0)\n            widget = item.widget()\n            if widget:\n                widget.setVisible(False)\n                widget.deleteLater()\n        self._ordered_widgets = []\n        self._group_widgets = []\n        self._widgets_by_instance_id = {}\n\n    def set_report(self, report: PublishReport):\n        self.clear()\n        identifiers = {\n            instance_info.creator_identifier\n            for instance_info in report.instances_by_id.values()\n        }\n        identifiers.add(CONTEXT_ID)\n        identifier_icons = {\n            identifier: self._controller.get_creator_icon(identifier)\n            for identifier in identifiers\n        }\n\n        widgets: list[PublishInstanceCardWidget] = []\n        group_widgets: list[FamilyGroupLabel] = []\n        logs_summary: LogsSummary = report.get_logs_summary()\n\n        publish_can_continue: bool = self._controller.publish_can_continue()\n        instances_by_family = collections.defaultdict(list)\n        for instance_info in report.instances_by_id.values():\n            if not instance_info.exists:\n                continue\n            instances_by_family[instance_info.family].append(instance_info)\n\n        context_widget = PublishInstanceCardWidget(\n            CONTEXT_ID,\n            report.context.label,\n            identifier_icons[CONTEXT_ID],\n            logs_summary,\n            publish_can_continue,\n            self._instance_view\n        )\n        context_widget.selection_requested.connect(self._on_selection_request)\n        self._instance_layout.addWidget(context_widget, 0)\n\n        widgets.append(context_widget)\n        self._widgets_by_instance_id[context_widget.id] = context_widget\n\n        sorted_by_family = sorted(\n            instances_by_family.items(), key=lambda i: i[0]\n        )\n        for family, instance_infos in sorted_by_family:\n            # Only instance without family is context\n            if family:\n                group_widget = FamilyGroupLabel(family, self._instance_view)\n                self._instance_layout.addWidget(group_widget, 0)\n                group_widgets.append(group_widget)\n\n            instance_infos.sort(key=lambda i: i.label)\n            for instance_info in instance_infos:\n                icon = identifier_icons[instance_info.creator_identifier]\n\n                widget = PublishInstanceCardWidget(\n                    instance_info.id,\n                    instance_info.label,\n                    icon,\n                    logs_summary,\n                    publish_can_continue,\n                    self._instance_view\n                )\n                widget.selection_requested.connect(self._on_selection_request)\n                self._instance_layout.addWidget(widget, 0)\n\n                widgets.append(widget)\n                self._widgets_by_instance_id[widget.id] = widget\n        self._instance_layout.addStretch(1)\n        self._ordered_widgets = widgets\n        self._group_widgets = group_widgets\n\n    def _on_selection_request(self, instance_id):\n        instance_widget = self._widgets_by_instance_id[instance_id]\n        selected_widgets = self._get_selected_widgets()\n        if instance_widget in selected_widgets:\n            instance_widget.set_selected(False)\n        else:\n            instance_widget.set_selected(True)\n            for widget in selected_widgets:\n                widget.set_selected(False)\n        self.selection_changed.emit()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/report_page.html#client.ayon_core.tools.publisher.widgets.report_page.PublishInstancesViewWidget.clear","title":"<code>clear()</code>","text":"<p>Remove actions from widget.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/report_page.py</code> <pre><code>def clear(self):\n    \"\"\"Remove actions from widget.\"\"\"\n    while self._instance_layout.count():\n        item = self._instance_layout.takeAt(0)\n        widget = item.widget()\n        if widget:\n            widget.setVisible(False)\n            widget.deleteLater()\n    self._ordered_widgets = []\n    self._group_widgets = []\n    self._widgets_by_instance_id = {}\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/report_page.html#client.ayon_core.tools.publisher.widgets.report_page.PublishInstancesViewWidget.sizeHint","title":"<code>sizeHint()</code>","text":"<p>Modify sizeHint based on visibility of scroll bars.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/report_page.py</code> <pre><code>def sizeHint(self):\n    \"\"\"Modify sizeHint based on visibility of scroll bars.\"\"\"\n    # Calculate width hint by content widget and vertical scroll bar\n    scroll_bar = self._scroll_area.verticalScrollBar()\n    view_size = self._instance_view.sizeHint().width()\n    fm = self._instance_view.fontMetrics()\n    width = (\n        max(view_size, fm.width(self._min_width_measure_string))\n        + scroll_bar.sizeHint().width()\n    )\n\n    result = super().sizeHint()\n    result.setWidth(width)\n    return result\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/report_page.html#client.ayon_core.tools.publisher.widgets.report_page.ReportPageWidget","title":"<code>ReportPageWidget</code>","text":"<p>               Bases: <code>QFrame</code></p> <p>Widgets showing report for artis.</p> <p>There are 5 possible states: 1. Publishing did not start yet.         &gt; Only label. 2. Publishing is paused.                \u2510 3. Publishing successfully finished.    \u2502&gt; Instances with logs. 4. Publishing crashed.                  \u2518 5. Crashed because of publish error.     &gt; Errors with logs.</p> <p>This widget is shown if publish errors happened.</p> <p>Shows publish error titles with instances on which they happened and publish error detail with possible actions (repair).</p> Source code in <code>client/ayon_core/tools/publisher/widgets/report_page.py</code> <pre><code>class ReportPageWidget(QtWidgets.QFrame):\n    \"\"\"Widgets showing report for artis.\n\n    There are 5 possible states:\n    1. Publishing did not start yet.         &gt; Only label.\n    2. Publishing is paused.                \u2510\n    3. Publishing successfully finished.    \u2502&gt; Instances with logs.\n    4. Publishing crashed.                  \u2518\n    5. Crashed because of publish error.     &gt; Errors with logs.\n\n    This widget is shown if publish errors happened.\n\n    Shows publish error titles with instances on which they happened\n    and publish error detail with possible actions (repair).\n    \"\"\"\n\n    def __init__(\n        self, controller: AbstractPublisherFrontend, parent: QtWidgets.QWidget\n    ):\n        super().__init__(parent)\n\n        header_label = QtWidgets.QLabel(self)\n        header_label.setAlignment(QtCore.Qt.AlignCenter)\n        header_label.setObjectName(\"PublishReportHeader\")\n\n        publish_instances_widget = ReportsWidget(controller, self)\n\n        layout = QtWidgets.QVBoxLayout(self)\n        layout.setContentsMargins(0, 0, 0, 0)\n        layout.addWidget(header_label, 0)\n        layout.addWidget(publish_instances_widget, 0)\n\n        controller.register_event_callback(\n            \"publish.process.started\", self._on_publish_start\n        )\n        controller.register_event_callback(\n            \"publish.reset.finished\", self._on_publish_reset\n        )\n        controller.register_event_callback(\n            \"publish.process.stopped\", self._on_publish_stop\n        )\n\n        self._header_label = header_label\n        self._publish_instances_widget = publish_instances_widget\n\n        self._controller: AbstractPublisherFrontend = controller\n\n    def _update_label(self):\n        if not self._controller.publish_has_started():\n            # This probably never happen when this widget is visible\n            header_label = \"Nothing to report until you run publish\"\n        elif self._controller.publish_has_crashed():\n            header_label = \"Publish error report\"\n        elif self._controller.publish_has_validation_errors():\n            header_label = \"Publish validation report\"\n        elif self._controller.publish_has_finished():\n            header_label = \"Publish success report\"\n        else:\n            header_label = \"Publish report\"\n        self._header_label.setText(header_label)\n\n    def _update_state(self):\n        self._update_label()\n        publish_started = self._controller.publish_has_started()\n        self._publish_instances_widget.setVisible(publish_started)\n        if publish_started:\n            self._publish_instances_widget.update_data()\n\n        self.updateGeometry()\n\n    def _on_publish_start(self):\n        self._update_state()\n\n    def _on_publish_reset(self):\n        self._update_state()\n\n    def _on_publish_stop(self):\n        self._update_state()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/report_page.html#client.ayon_core.tools.publisher.widgets.report_page.ReportsWidget","title":"<code>ReportsWidget</code>","text":"<p>               Bases: <code>QWidget</code></p>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/report_page.html#client.ayon_core.tools.publisher.widgets.report_page.ReportsWidget--crash-layout","title":"Crash layout","text":"<p>\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510 \u2502Views \u2502 Logs    \u2502 Details \u2502 \u2502      \u2502         \u2502         \u2502 \u2502      \u2502         \u2502         \u2502 \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518</p>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/report_page.html#client.ayon_core.tools.publisher.widgets.report_page.ReportsWidget--success-layout","title":"Success layout","text":"<p>\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510 \u2502View  \u2502 Logs              \u2502 \u2502      \u2502                   \u2502 \u2502      \u2502                   \u2502 \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518</p>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/report_page.html#client.ayon_core.tools.publisher.widgets.report_page.ReportsWidget--publish-errors-layout","title":"Publish errors layout","text":"<p>\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510 \u2502Views \u2502 Actions \u2502         \u2502 \u2502      \u251c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2524 Details \u2502 \u2502      \u2502 Logs    \u2502         \u2502 \u2502      \u2502         \u2502         \u2502 \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518</p> Source code in <code>client/ayon_core/tools/publisher/widgets/report_page.py</code> <pre><code>class ReportsWidget(QtWidgets.QWidget):\n    \"\"\"\n        # Crash layout\n        \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n        \u2502Views \u2502 Logs    \u2502 Details \u2502\n        \u2502      \u2502         \u2502         \u2502\n        \u2502      \u2502         \u2502         \u2502\n        \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n        # Success layout\n        \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n        \u2502View  \u2502 Logs              \u2502\n        \u2502      \u2502                   \u2502\n        \u2502      \u2502                   \u2502\n        \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n        # Publish errors layout\n        \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n        \u2502Views \u2502 Actions \u2502         \u2502\n        \u2502      \u251c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2524 Details \u2502\n        \u2502      \u2502 Logs    \u2502         \u2502\n        \u2502      \u2502         \u2502         \u2502\n        \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n    \"\"\"\n\n    def __init__(\n        self, controller: AbstractPublisherFrontend, parent: QtWidgets.QWidget\n    ):\n        super().__init__(parent)\n\n        # Instances view\n        views_widget = QtWidgets.QWidget(self)\n\n        instances_view = PublishInstancesViewWidget(controller, views_widget)\n\n        publish_error_view = PublishErrorsView(views_widget)\n\n        views_layout = QtWidgets.QStackedLayout(views_widget)\n        views_layout.setContentsMargins(0, 0, 0, 0)\n        views_layout.addWidget(instances_view)\n        views_layout.addWidget(publish_error_view)\n\n        views_layout.setCurrentWidget(instances_view)\n\n        # Error description with actions and optional detail\n        details_widget = QtWidgets.QFrame(self)\n        details_widget.setObjectName(\"PublishInstancesDetails\")\n\n        # Actions widget\n        actions_widget = PublishActionsWidget(controller, details_widget)\n\n        pages_widget = QtWidgets.QWidget(details_widget)\n\n        # Crash information\n        crash_widget = CrashWidget(controller, details_widget)\n\n        # Logs view\n        logs_view = InstancesLogsView(pages_widget)\n\n        # Validation details\n        # Description and details inputs are in scroll\n        # - single scroll for both inputs, they are forced to not use theirs\n        detail_inputs_spacer = QtWidgets.QWidget(pages_widget)\n        detail_inputs_spacer.setMinimumWidth(30)\n        detail_inputs_spacer.setMaximumWidth(30)\n\n        detail_input_scroll = QtWidgets.QScrollArea(pages_widget)\n\n        detail_inputs_widget = PublishFailWidget(detail_input_scroll)\n        detail_inputs_widget.setAttribute(QtCore.Qt.WA_TranslucentBackground)\n\n        detail_input_scroll.setWidget(detail_inputs_widget)\n        detail_input_scroll.setWidgetResizable(True)\n        detail_input_scroll.setViewportMargins(0, 0, 0, 0)\n\n        # Layout pages\n        pages_layout = QtWidgets.QHBoxLayout(pages_widget)\n        pages_layout.setContentsMargins(0, 0, 0, 0)\n        pages_layout.addWidget(crash_widget, 1)\n        pages_layout.addWidget(logs_view, 1)\n        pages_layout.addWidget(detail_inputs_spacer, 0)\n        pages_layout.addWidget(detail_input_scroll, 1)\n\n        details_layout = QtWidgets.QVBoxLayout(details_widget)\n        details_layout.setContentsMargins(8, 16, 8, 16)\n        details_layout.setSpacing(8)\n        details_layout.addWidget(actions_widget, 0)\n        details_layout.addWidget(pages_widget, 1)\n\n        content_layout = QtWidgets.QHBoxLayout(self)\n        content_layout.setContentsMargins(0, 0, 0, 0)\n        content_layout.addWidget(views_widget, 0)\n        content_layout.addWidget(details_widget, 1)\n\n        instances_view.selection_changed.connect(self._on_instance_selection)\n        publish_error_view.selection_changed.connect(\n            self._on_error_selection)\n\n        self._views_layout = views_layout\n        self._instances_view = instances_view\n        self._publish_error_view = publish_error_view\n\n        self._actions_widget = actions_widget\n        self._detail_inputs_widget = detail_inputs_widget\n        self._logs_view = logs_view\n        self._detail_inputs_spacer = detail_inputs_spacer\n        self._detail_input_scroll = detail_input_scroll\n        self._crash_widget = crash_widget\n\n        self._controller: AbstractPublisherFrontend = controller\n\n        self._publish_errors_by_id = {}\n\n    def update_data(self):\n        has_validation_error = self._controller.publish_has_validation_errors()\n        has_finished = self._controller.publish_has_finished()\n        has_crashed = self._controller.publish_has_crashed()\n        error_info = None\n        if has_crashed:\n            error_info = self._controller.get_publish_error_info()\n\n        publish_error_mode = False\n        if error_info is not None:\n            publish_error_mode = not error_info.is_unknown_error\n        elif has_validation_error:\n            publish_error_mode = True\n\n        if publish_error_mode:\n            view = self._publish_error_view\n        else:\n            view = self._instances_view\n\n        self._views_layout.setCurrentWidget(view)\n\n        self._actions_widget.set_visible_mode(publish_error_mode)\n        self._detail_inputs_spacer.setVisible(publish_error_mode)\n        self._detail_input_scroll.setVisible(publish_error_mode)\n\n        logs_visible = publish_error_mode or has_finished or not has_crashed\n        self._logs_view.setVisible(logs_visible)\n        self._crash_widget.setVisible(not logs_visible)\n\n        # Instance view &amp; logs update\n        report = self._controller.get_publish_report()\n        self._instances_view.set_report(report)\n        self._logs_view.update_report(report)\n\n        # Publish errors\n        publish_errors_report = self._controller.get_publish_errors_report()\n        grouped_error_items = publish_errors_report.group_items_by_title()\n\n        publish_errors_by_id = {\n            title_item[\"id\"]: title_item\n            for title_item in grouped_error_items\n        }\n\n        self._publish_errors_by_id = publish_errors_by_id\n        self._publish_error_view.set_errors(grouped_error_items)\n\n    def _on_instance_selection(self):\n        instance_ids = self._instances_view.get_selected_instance_ids()\n        self._logs_view.set_instances_filter(instance_ids)\n\n    def _on_error_selection(self):\n        title_id, instance_ids = (\n            self._publish_error_view.get_selected_items())\n        error_info = self._publish_errors_by_id.get(title_id)\n        if error_info is None:\n            self._actions_widget.set_error_info(None)\n            self._detail_inputs_widget.set_error_item(None)\n            return\n\n        self._logs_view.set_instances_filter(instance_ids)\n        self._logs_view.set_plugins_filter({error_info[\"plugin_id\"]})\n\n        match_error_item = None\n        for error_item in error_info[\"error_items\"]:\n            instance_id = error_item.instance_id or CONTEXT_ID\n            if instance_id in instance_ids:\n                match_error_item = error_item\n                break\n\n        self._actions_widget.set_error_info(error_info)\n        self._detail_inputs_widget.set_error_item(match_error_item)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/report_page.html#client.ayon_core.tools.publisher.widgets.report_page.VerticalScrollArea","title":"<code>VerticalScrollArea</code>","text":"<p>               Bases: <code>QScrollArea</code></p> <p>Scroll area for publish error titles.</p> <p>The biggest difference is that the scroll area has scroll bar on left side and resize of content will also resize scrollarea itself.</p> <p>Resize if deferred by 100ms because at the moment of resize are not yet propagated sizes and visibility of scroll bars.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/report_page.py</code> <pre><code>class VerticalScrollArea(QtWidgets.QScrollArea):\n    \"\"\"Scroll area for publish error titles.\n\n    The biggest difference is that the scroll area has scroll bar on left side\n    and resize of content will also resize scrollarea itself.\n\n    Resize if deferred by 100ms because at the moment of resize are not yet\n    propagated sizes and visibility of scroll bars.\n    \"\"\"\n\n    def __init__(self, *args, **kwargs):\n        super().__init__(*args, **kwargs)\n\n        self.setHorizontalScrollBarPolicy(QtCore.Qt.ScrollBarAlwaysOff)\n        self.setVerticalScrollBarPolicy(QtCore.Qt.ScrollBarAsNeeded)\n        self.setLayoutDirection(QtCore.Qt.RightToLeft)\n\n        self.setObjectName(\"PublisherVerticalScrollArea\")\n\n        self.setAttribute(QtCore.Qt.WA_TranslucentBackground)\n        # Background of scrollbar will be transparent\n        scrollbar_bg = self.verticalScrollBar().parent()\n        if scrollbar_bg:\n            scrollbar_bg.setAttribute(QtCore.Qt.WA_TranslucentBackground)\n        self.setViewportMargins(0, 0, 0, 0)\n\n        self.verticalScrollBar().installEventFilter(self)\n\n        # Timer with 100ms offset after changing size\n        size_changed_timer = QtCore.QTimer()\n        size_changed_timer.setInterval(100)\n        size_changed_timer.setSingleShot(True)\n\n        size_changed_timer.timeout.connect(self._on_timer_timeout)\n        self._size_changed_timer = size_changed_timer\n\n    def setVerticalScrollBar(self, widget):\n        old_widget = self.verticalScrollBar()\n        if old_widget:\n            old_widget.removeEventFilter(self)\n\n        super().setVerticalScrollBar(widget)\n        if widget:\n            widget.installEventFilter(self)\n\n    def setWidget(self, widget):\n        old_widget = self.widget()\n        if old_widget:\n            old_widget.removeEventFilter(self)\n\n        super().setWidget(widget)\n        if widget:\n            widget.installEventFilter(self)\n\n    def _on_timer_timeout(self):\n        width = self.widget().width()\n        if self.verticalScrollBar().isVisible():\n            width += self.verticalScrollBar().width()\n        self.setMinimumWidth(width)\n\n    def eventFilter(self, obj, event):\n        if (\n            event.type() == QtCore.QEvent.Resize\n            and (obj is self.widget() or obj is self.verticalScrollBar())\n        ):\n            self._size_changed_timer.start()\n        return super().eventFilter(obj, event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/screenshot_widget.html","title":"screenshot_widget","text":""},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/screenshot_widget.html#client.ayon_core.tools.publisher.widgets.screenshot_widget.ScreenMarquee","title":"<code>ScreenMarquee</code>","text":"<p>               Bases: <code>QObject</code></p> <p>Dialog to interactively define screen area.</p> <p>This allows to select a screen area through a marquee selection.</p> <p>You can use any of its classmethods for easily saving an image, capturing to QClipboard or returning a QPixmap, respectively <code>capture_to_file</code>, <code>capture_to_clipboard</code> and <code>capture_to_pixmap</code>.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/screenshot_widget.py</code> <pre><code>class ScreenMarquee(QtCore.QObject):\n    \"\"\"Dialog to interactively define screen area.\n\n    This allows to select a screen area through a marquee selection.\n\n    You can use any of its classmethods for easily saving an image,\n    capturing to QClipboard or returning a QPixmap, respectively\n    `capture_to_file`, `capture_to_clipboard` and `capture_to_pixmap`.\n    \"\"\"\n\n    def __init__(self, parent=None):\n        super().__init__(parent=parent)\n\n        screens_by_id = {}\n        for screen in QtWidgets.QApplication.screens():\n            screen_id = uuid.uuid4().hex\n            screen_dialog = ScreenMarqueeDialog(screen, screen_id)\n            screens_by_id[screen_id] = screen_dialog\n            screen_dialog.mouse_moved.connect(self._on_mouse_move)\n            screen_dialog.mouse_pressed.connect(self._on_mouse_press)\n            screen_dialog.mouse_released.connect(self._on_mouse_release)\n            screen_dialog.close_requested.connect(self._on_close_request)\n\n        self._screens_by_id = screens_by_id\n        self._finished = False\n        self._captured = False\n        self._start_pos = None\n        self._end_pos = None\n        self._start_screen_id = None\n        self._pix = None\n\n    def get_captured_pixmap(self):\n        if self._pix is None:\n            return QtGui.QPixmap()\n        return self._pix\n\n    def _close_dialogs(self):\n        for dialog in self._screens_by_id.values():\n            dialog.close()\n\n    def _on_close_request(self):\n        self._close_dialogs()\n        self._finished = True\n\n    def _on_mouse_release(self, pos):\n        start_screen_dialog = self._screens_by_id.get(self._start_screen_id)\n        if start_screen_dialog is None:\n            self._finished = True\n            self._captured = False\n            return\n\n        end_pos = start_screen_dialog.convert_end_pos(pos)\n\n        self._close_dialogs()\n        self._end_pos = end_pos\n        self._finished = True\n        self._captured = True\n\n    def _on_mouse_press(self, pos, screen_id):\n        self._start_pos = pos\n        self._start_screen_id = screen_id\n\n    def _on_mouse_move(self):\n        for dialog in self._screens_by_id.values():\n            dialog.repaint()\n\n    def start_capture(self):\n        for dialog in self._screens_by_id.values():\n            dialog.show()\n            # Activate so Escape event is not ignored.\n            dialog.setWindowState(QtCore.Qt.WindowActive)\n\n        app = QtWidgets.QApplication.instance()\n        while not self._finished:\n            app.processEvents()\n\n        # Give time to cloe dialogs\n        for _ in range(2):\n            app.processEvents()\n\n        if self._captured:\n            self._pix = self.get_desktop_pixmap(\n                self._start_pos, self._end_pos\n            )\n\n    @classmethod\n    def get_desktop_pixmap(cls, pos_start, pos_end):\n        \"\"\"Performs a screen capture on the specified rectangle.\n\n        Args:\n            pos_start (QtCore.QPoint): Start of screen capture.\n            pos_end (QtCore.QPoint): End of screen capture.\n\n        Returns:\n            QtGui.QPixmap: Captured pixmap image\n\n        \"\"\"\n        # Unify start and end points\n        # - start is top left\n        # - end is bottom right\n        if pos_start.y() &gt; pos_end.y():\n            pos_start, pos_end = pos_end, pos_start\n\n        if pos_start.x() &gt; pos_end.x():\n            new_start = QtCore.QPoint(pos_end.x(), pos_start.y())\n            new_end = QtCore.QPoint(pos_start.x(), pos_end.y())\n            pos_start = new_start\n            pos_end = new_end\n\n        # Validate if the rectangle is valid\n        rect = QtCore.QRect(pos_start, pos_end)\n        if rect.width() &lt; 1 or rect.height() &lt; 1:\n            return QtGui.QPixmap()\n\n        screen = QtWidgets.QApplication.screenAt(pos_start)\n        return screen.grabWindow(\n            0,\n            pos_start.x() - screen.geometry().x(),\n            pos_start.y() - screen.geometry().y(),\n            pos_end.x() - pos_start.x(),\n            pos_end.y() - pos_start.y()\n        )\n        # Multiscreen capture that does not work\n        # - does not handle pixel aspect ratio and positioning of screens\n\n        # most_left = None\n        # most_top = None\n        # for screen in QtWidgets.QApplication.screens():\n        #     screen_geo = screen.geometry()\n        #     if most_left is None or most_left &gt; screen_geo.x():\n        #         most_left = screen_geo.x()\n        #\n        #     if most_top is None or most_top &gt; screen_geo.y():\n        #         most_top = screen_geo.y()\n        #\n        # most_left = most_left or 0\n        # most_top = most_top or 0\n        #\n        # screen_pixes = []\n        # for screen in QtWidgets.QApplication.screens():\n        #     screen_geo = screen.geometry()\n        #     if not screen_geo.intersects(rect):\n        #         continue\n        #\n        #     pos_l_x = screen_geo.x()\n        #     pos_l_y = screen_geo.y()\n        #     pos_r_x = screen_geo.x() + screen_geo.width()\n        #     pos_r_y = screen_geo.y() + screen_geo.height()\n        #     if pos_start.x() &gt; pos_l_x:\n        #         pos_l_x = pos_start.x()\n        #\n        #     if pos_start.y() &gt; pos_l_y:\n        #         pos_l_y = pos_start.y()\n        #\n        #     if pos_end.x() &lt; pos_r_x:\n        #         pos_r_x = pos_end.x()\n        #\n        #     if pos_end.y() &lt; pos_r_y:\n        #         pos_r_y = pos_end.y()\n        #\n        #     capture_pos_x = pos_l_x - screen_geo.x()\n        #     capture_pos_y = pos_l_y - screen_geo.y()\n        #     capture_screen_width = pos_r_x - pos_l_x\n        #     capture_screen_height = pos_r_y - pos_l_y\n        #     screen_pix = screen.grabWindow(\n        #         0,\n        #         capture_pos_x, capture_pos_y,\n        #         capture_screen_width, capture_screen_height\n        #     )\n        #     paste_point = QtCore.QPoint(\n        #         (pos_l_x - screen_geo.x()) - rect.x(),\n        #         (pos_l_y - screen_geo.y()) - rect.y()\n        #     )\n        #     screen_pixes.append((screen_pix, paste_point))\n        #\n        # output_pix = QtGui.QPixmap(rect.width(), rect.height())\n        # output_pix.fill(QtCore.Qt.transparent)\n        # pix_painter = QtGui.QPainter()\n        # pix_painter.begin(output_pix)\n        # render_hints = (\n        #     QtGui.QPainter.Antialiasing\n        #     | QtGui.QPainter.SmoothPixmapTransform\n        # )\n        # pix_painter.setRenderHints(render_hints)\n        # for item in screen_pixes:\n        #     (screen_pix, offset) = item\n        #     pix_painter.drawPixmap(offset, screen_pix)\n        #\n        # pix_painter.end()\n        #\n        # return output_pix\n\n    @classmethod\n    def capture_to_pixmap(cls):\n        \"\"\"Take screenshot with marquee into pixmap.\n\n        Note:\n            The pixmap can be invalid (use 'isNull' to check).\n\n        Returns:\n            QtGui.QPixmap: Captured pixmap image.\n        \"\"\"\n        tool = cls()\n        tool.start_capture()\n        return tool.get_captured_pixmap()\n\n    @classmethod\n    def capture_to_file(cls, filepath=None):\n        \"\"\"Take screenshot with marquee into file.\n\n        Args:\n            filepath (Optional[str]): Path where screenshot will be saved.\n\n        Returns:\n            Union[str, None]: Path to the saved screenshot, or None if user\n                cancelled the operation.\n        \"\"\"\n\n        pixmap = cls.capture_to_pixmap()\n        if pixmap.isNull():\n            return None\n\n        if filepath is None:\n            with tempfile.NamedTemporaryFile(\n                prefix=\"screenshot_\", suffix=\".png\", delete=False\n            ) as tmpfile:\n                filepath = tmpfile.name\n\n        else:\n            output_dir = os.path.dirname(filepath)\n            if not os.path.exists(output_dir):\n                os.makedirs(output_dir)\n\n        pixmap.save(filepath)\n        return filepath\n\n    @classmethod\n    def capture_to_clipboard(cls):\n        \"\"\"Take screenshot with marquee into clipboard.\n\n        Notes:\n            Screenshot is not in clipboard if user cancelled the operation.\n\n        Returns:\n            bool: Screenshot was added to clipboard.\n        \"\"\"\n\n        clipboard = QtWidgets.QApplication.clipboard()\n        pixmap = cls.capture_to_pixmap()\n        if pixmap.isNull():\n            return False\n        image = pixmap.toImage()\n        clipboard.setImage(image, QtGui.QClipboard.Clipboard)\n        return True\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/screenshot_widget.html#client.ayon_core.tools.publisher.widgets.screenshot_widget.ScreenMarquee.capture_to_clipboard","title":"<code>capture_to_clipboard()</code>  <code>classmethod</code>","text":"<p>Take screenshot with marquee into clipboard.</p> Notes <p>Screenshot is not in clipboard if user cancelled the operation.</p> <p>Returns:</p> Name Type Description <code>bool</code> <p>Screenshot was added to clipboard.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/screenshot_widget.py</code> <pre><code>@classmethod\ndef capture_to_clipboard(cls):\n    \"\"\"Take screenshot with marquee into clipboard.\n\n    Notes:\n        Screenshot is not in clipboard if user cancelled the operation.\n\n    Returns:\n        bool: Screenshot was added to clipboard.\n    \"\"\"\n\n    clipboard = QtWidgets.QApplication.clipboard()\n    pixmap = cls.capture_to_pixmap()\n    if pixmap.isNull():\n        return False\n    image = pixmap.toImage()\n    clipboard.setImage(image, QtGui.QClipboard.Clipboard)\n    return True\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/screenshot_widget.html#client.ayon_core.tools.publisher.widgets.screenshot_widget.ScreenMarquee.capture_to_file","title":"<code>capture_to_file(filepath=None)</code>  <code>classmethod</code>","text":"<p>Take screenshot with marquee into file.</p> <p>Parameters:</p> Name Type Description Default <code>filepath</code> <code>Optional[str]</code> <p>Path where screenshot will be saved.</p> <code>None</code> <p>Returns:</p> Type Description <p>Union[str, None]: Path to the saved screenshot, or None if user cancelled the operation.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/screenshot_widget.py</code> <pre><code>@classmethod\ndef capture_to_file(cls, filepath=None):\n    \"\"\"Take screenshot with marquee into file.\n\n    Args:\n        filepath (Optional[str]): Path where screenshot will be saved.\n\n    Returns:\n        Union[str, None]: Path to the saved screenshot, or None if user\n            cancelled the operation.\n    \"\"\"\n\n    pixmap = cls.capture_to_pixmap()\n    if pixmap.isNull():\n        return None\n\n    if filepath is None:\n        with tempfile.NamedTemporaryFile(\n            prefix=\"screenshot_\", suffix=\".png\", delete=False\n        ) as tmpfile:\n            filepath = tmpfile.name\n\n    else:\n        output_dir = os.path.dirname(filepath)\n        if not os.path.exists(output_dir):\n            os.makedirs(output_dir)\n\n    pixmap.save(filepath)\n    return filepath\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/screenshot_widget.html#client.ayon_core.tools.publisher.widgets.screenshot_widget.ScreenMarquee.capture_to_pixmap","title":"<code>capture_to_pixmap()</code>  <code>classmethod</code>","text":"<p>Take screenshot with marquee into pixmap.</p> Note <p>The pixmap can be invalid (use 'isNull' to check).</p> <p>Returns:</p> Type Description <p>QtGui.QPixmap: Captured pixmap image.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/screenshot_widget.py</code> <pre><code>@classmethod\ndef capture_to_pixmap(cls):\n    \"\"\"Take screenshot with marquee into pixmap.\n\n    Note:\n        The pixmap can be invalid (use 'isNull' to check).\n\n    Returns:\n        QtGui.QPixmap: Captured pixmap image.\n    \"\"\"\n    tool = cls()\n    tool.start_capture()\n    return tool.get_captured_pixmap()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/screenshot_widget.html#client.ayon_core.tools.publisher.widgets.screenshot_widget.ScreenMarquee.get_desktop_pixmap","title":"<code>get_desktop_pixmap(pos_start, pos_end)</code>  <code>classmethod</code>","text":"<p>Performs a screen capture on the specified rectangle.</p> <p>Parameters:</p> Name Type Description Default <code>pos_start</code> <code>QPoint</code> <p>Start of screen capture.</p> required <code>pos_end</code> <code>QPoint</code> <p>End of screen capture.</p> required <p>Returns:</p> Type Description <p>QtGui.QPixmap: Captured pixmap image</p> Source code in <code>client/ayon_core/tools/publisher/widgets/screenshot_widget.py</code> <pre><code>@classmethod\ndef get_desktop_pixmap(cls, pos_start, pos_end):\n    \"\"\"Performs a screen capture on the specified rectangle.\n\n    Args:\n        pos_start (QtCore.QPoint): Start of screen capture.\n        pos_end (QtCore.QPoint): End of screen capture.\n\n    Returns:\n        QtGui.QPixmap: Captured pixmap image\n\n    \"\"\"\n    # Unify start and end points\n    # - start is top left\n    # - end is bottom right\n    if pos_start.y() &gt; pos_end.y():\n        pos_start, pos_end = pos_end, pos_start\n\n    if pos_start.x() &gt; pos_end.x():\n        new_start = QtCore.QPoint(pos_end.x(), pos_start.y())\n        new_end = QtCore.QPoint(pos_start.x(), pos_end.y())\n        pos_start = new_start\n        pos_end = new_end\n\n    # Validate if the rectangle is valid\n    rect = QtCore.QRect(pos_start, pos_end)\n    if rect.width() &lt; 1 or rect.height() &lt; 1:\n        return QtGui.QPixmap()\n\n    screen = QtWidgets.QApplication.screenAt(pos_start)\n    return screen.grabWindow(\n        0,\n        pos_start.x() - screen.geometry().x(),\n        pos_start.y() - screen.geometry().y(),\n        pos_end.x() - pos_start.x(),\n        pos_end.y() - pos_start.y()\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/screenshot_widget.html#client.ayon_core.tools.publisher.widgets.screenshot_widget.ScreenMarqueeDialog","title":"<code>ScreenMarqueeDialog</code>","text":"<p>               Bases: <code>QDialog</code></p> Source code in <code>client/ayon_core/tools/publisher/widgets/screenshot_widget.py</code> <pre><code>class ScreenMarqueeDialog(QtWidgets.QDialog):\n    mouse_moved = QtCore.Signal()\n    mouse_pressed = QtCore.Signal(QtCore.QPoint, str)\n    mouse_released = QtCore.Signal(QtCore.QPoint)\n    close_requested = QtCore.Signal()\n\n    def __init__(self, screen: QtCore.QObject, screen_id: str):\n        super().__init__()\n        self.setWindowFlags(\n            QtCore.Qt.Window\n            | QtCore.Qt.FramelessWindowHint\n            | QtCore.Qt.WindowStaysOnTopHint\n            | QtCore.Qt.CustomizeWindowHint\n        )\n        self.setAttribute(QtCore.Qt.WA_TranslucentBackground)\n        self.setCursor(QtCore.Qt.CrossCursor)\n        self.setMouseTracking(True)\n\n        screen.geometryChanged.connect(self._fit_screen_geometry)\n\n        self._screen = screen\n        self._opacity = 100\n        self._click_pos = None\n        self._screen_id = screen_id\n\n    def set_click_pos(self, pos):\n        self._click_pos = pos\n        self.repaint()\n\n    def convert_end_pos(self, pos):\n        glob_pos = self.mapFromGlobal(pos)\n        new_pos = self._convert_pos(glob_pos)\n        return self.mapToGlobal(new_pos)\n\n    def paintEvent(self, event):\n        \"\"\"Paint event\"\"\"\n        # Convert click and current mouse positions to local space.\n        mouse_pos = self._convert_pos(self.mapFromGlobal(QtGui.QCursor.pos()))\n\n        rect = event.rect()\n        fill_path = QtGui.QPainterPath()\n        fill_path.addRect(rect)\n\n        capture_rect = None\n        if self._click_pos is not None:\n            click_pos = self.mapFromGlobal(self._click_pos)\n            capture_rect = QtCore.QRect(click_pos, mouse_pos)\n\n            # Clear the capture area\n            sub_path = QtGui.QPainterPath()\n            sub_path.addRect(capture_rect)\n            fill_path = fill_path.subtracted(sub_path)\n\n        painter = QtGui.QPainter(self)\n        painter.setRenderHints(\n            QtGui.QPainter.Antialiasing\n            | QtGui.QPainter.SmoothPixmapTransform\n        )\n\n        # Draw background. Aside from aesthetics, this makes the full\n        # tool region accept mouse events.\n        painter.setBrush(QtGui.QColor(0, 0, 0, self._opacity))\n        painter.setPen(QtCore.Qt.NoPen)\n        painter.drawPath(fill_path)\n\n        # Draw cropping markers at current mouse position\n        pen_color = QtGui.QColor(255, 255, 255, self._opacity)\n        pen = QtGui.QPen(pen_color, 1, QtCore.Qt.DotLine)\n        painter.setPen(pen)\n        painter.drawLine(\n            rect.left(), mouse_pos.y(),\n            rect.right(), mouse_pos.y()\n        )\n        painter.drawLine(\n            mouse_pos.x(), rect.top(),\n            mouse_pos.x(), rect.bottom()\n        )\n\n        # Draw rectangle around selection area\n        if capture_rect is not None:\n            pen_color = QtGui.QColor(92, 173, 214)\n            pen = QtGui.QPen(pen_color, 2)\n            painter.setPen(pen)\n            painter.setBrush(QtCore.Qt.NoBrush)\n            l_x = capture_rect.left()\n            r_x = capture_rect.right()\n            if l_x &gt; r_x:\n                l_x, r_x = r_x, l_x\n            t_y = capture_rect.top()\n            b_y = capture_rect.bottom()\n            if t_y &gt; b_y:\n                t_y, b_y = b_y, t_y\n\n            # -1 to draw 1px over the border\n            r_x -= 1\n            b_y -= 1\n            sel_rect = QtCore.QRect(\n                QtCore.QPoint(l_x, t_y),\n                QtCore.QPoint(r_x, b_y)\n            )\n            painter.drawRect(sel_rect)\n\n        painter.end()\n\n    def mousePressEvent(self, event):\n        \"\"\"Mouse click event\"\"\"\n\n        if event.button() == QtCore.Qt.LeftButton:\n            # Begin click drag operation\n            self._click_pos = event.globalPos()\n            self.mouse_pressed.emit(self._click_pos, self._screen_id)\n\n    def mouseReleaseEvent(self, event):\n        \"\"\"Mouse release event\"\"\"\n        if event.button() == QtCore.Qt.LeftButton:\n            # End click drag operation and commit the current capture rect\n            self._click_pos = None\n            self.mouse_released.emit(event.globalPos())\n\n    def mouseMoveEvent(self, event):\n        \"\"\"Mouse move event\"\"\"\n        self.mouse_moved.emit()\n\n    def keyPressEvent(self, event):\n        \"\"\"Mouse press event\"\"\"\n        if event.key() == QtCore.Qt.Key_Escape:\n            self._click_pos = None\n            event.accept()\n            self.close_requested.emit()\n            return\n        return super().keyPressEvent(event)\n\n    def showEvent(self, event):\n        super().showEvent(event)\n        self._fit_screen_geometry()\n\n    def closeEvent(self, event):\n        self._click_pos = None\n        super().closeEvent(event)\n\n    def _convert_pos(self, pos):\n        geo = self.geometry()\n        if pos.x() &gt; geo.width():\n            pos.setX(geo.width() - 1)\n        elif pos.x() &lt; 0:\n            pos.setX(0)\n\n        if pos.y() &gt; geo.height():\n            pos.setY(geo.height() - 1)\n        elif pos.y() &lt; 0:\n            pos.setY(0)\n        return pos\n\n    def _fit_screen_geometry(self):\n        # On macOs it is required to set screen explicitly\n        if hasattr(self, \"setScreen\"):\n            self.setScreen(self._screen)\n        self.setGeometry(self._screen.geometry())\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/screenshot_widget.html#client.ayon_core.tools.publisher.widgets.screenshot_widget.ScreenMarqueeDialog.keyPressEvent","title":"<code>keyPressEvent(event)</code>","text":"<p>Mouse press event</p> Source code in <code>client/ayon_core/tools/publisher/widgets/screenshot_widget.py</code> <pre><code>def keyPressEvent(self, event):\n    \"\"\"Mouse press event\"\"\"\n    if event.key() == QtCore.Qt.Key_Escape:\n        self._click_pos = None\n        event.accept()\n        self.close_requested.emit()\n        return\n    return super().keyPressEvent(event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/screenshot_widget.html#client.ayon_core.tools.publisher.widgets.screenshot_widget.ScreenMarqueeDialog.mouseMoveEvent","title":"<code>mouseMoveEvent(event)</code>","text":"<p>Mouse move event</p> Source code in <code>client/ayon_core/tools/publisher/widgets/screenshot_widget.py</code> <pre><code>def mouseMoveEvent(self, event):\n    \"\"\"Mouse move event\"\"\"\n    self.mouse_moved.emit()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/screenshot_widget.html#client.ayon_core.tools.publisher.widgets.screenshot_widget.ScreenMarqueeDialog.mousePressEvent","title":"<code>mousePressEvent(event)</code>","text":"<p>Mouse click event</p> Source code in <code>client/ayon_core/tools/publisher/widgets/screenshot_widget.py</code> <pre><code>def mousePressEvent(self, event):\n    \"\"\"Mouse click event\"\"\"\n\n    if event.button() == QtCore.Qt.LeftButton:\n        # Begin click drag operation\n        self._click_pos = event.globalPos()\n        self.mouse_pressed.emit(self._click_pos, self._screen_id)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/screenshot_widget.html#client.ayon_core.tools.publisher.widgets.screenshot_widget.ScreenMarqueeDialog.mouseReleaseEvent","title":"<code>mouseReleaseEvent(event)</code>","text":"<p>Mouse release event</p> Source code in <code>client/ayon_core/tools/publisher/widgets/screenshot_widget.py</code> <pre><code>def mouseReleaseEvent(self, event):\n    \"\"\"Mouse release event\"\"\"\n    if event.button() == QtCore.Qt.LeftButton:\n        # End click drag operation and commit the current capture rect\n        self._click_pos = None\n        self.mouse_released.emit(event.globalPos())\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/screenshot_widget.html#client.ayon_core.tools.publisher.widgets.screenshot_widget.ScreenMarqueeDialog.paintEvent","title":"<code>paintEvent(event)</code>","text":"<p>Paint event</p> Source code in <code>client/ayon_core/tools/publisher/widgets/screenshot_widget.py</code> <pre><code>def paintEvent(self, event):\n    \"\"\"Paint event\"\"\"\n    # Convert click and current mouse positions to local space.\n    mouse_pos = self._convert_pos(self.mapFromGlobal(QtGui.QCursor.pos()))\n\n    rect = event.rect()\n    fill_path = QtGui.QPainterPath()\n    fill_path.addRect(rect)\n\n    capture_rect = None\n    if self._click_pos is not None:\n        click_pos = self.mapFromGlobal(self._click_pos)\n        capture_rect = QtCore.QRect(click_pos, mouse_pos)\n\n        # Clear the capture area\n        sub_path = QtGui.QPainterPath()\n        sub_path.addRect(capture_rect)\n        fill_path = fill_path.subtracted(sub_path)\n\n    painter = QtGui.QPainter(self)\n    painter.setRenderHints(\n        QtGui.QPainter.Antialiasing\n        | QtGui.QPainter.SmoothPixmapTransform\n    )\n\n    # Draw background. Aside from aesthetics, this makes the full\n    # tool region accept mouse events.\n    painter.setBrush(QtGui.QColor(0, 0, 0, self._opacity))\n    painter.setPen(QtCore.Qt.NoPen)\n    painter.drawPath(fill_path)\n\n    # Draw cropping markers at current mouse position\n    pen_color = QtGui.QColor(255, 255, 255, self._opacity)\n    pen = QtGui.QPen(pen_color, 1, QtCore.Qt.DotLine)\n    painter.setPen(pen)\n    painter.drawLine(\n        rect.left(), mouse_pos.y(),\n        rect.right(), mouse_pos.y()\n    )\n    painter.drawLine(\n        mouse_pos.x(), rect.top(),\n        mouse_pos.x(), rect.bottom()\n    )\n\n    # Draw rectangle around selection area\n    if capture_rect is not None:\n        pen_color = QtGui.QColor(92, 173, 214)\n        pen = QtGui.QPen(pen_color, 2)\n        painter.setPen(pen)\n        painter.setBrush(QtCore.Qt.NoBrush)\n        l_x = capture_rect.left()\n        r_x = capture_rect.right()\n        if l_x &gt; r_x:\n            l_x, r_x = r_x, l_x\n        t_y = capture_rect.top()\n        b_y = capture_rect.bottom()\n        if t_y &gt; b_y:\n            t_y, b_y = b_y, t_y\n\n        # -1 to draw 1px over the border\n        r_x -= 1\n        b_y -= 1\n        sel_rect = QtCore.QRect(\n            QtCore.QPoint(l_x, t_y),\n            QtCore.QPoint(r_x, b_y)\n        )\n        painter.drawRect(sel_rect)\n\n    painter.end()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/screenshot_widget.html#client.ayon_core.tools.publisher.widgets.screenshot_widget.capture_to_clipboard","title":"<code>capture_to_clipboard()</code>","text":"<p>Take screenshot with marquee into clipboard.</p> Notes <p>Screenshot is not in clipboard if user cancelled the operation.</p> <p>Returns:</p> Name Type Description <code>bool</code> <p>Screenshot was added to clipboard.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/screenshot_widget.py</code> <pre><code>def capture_to_clipboard():\n    \"\"\"Take screenshot with marquee into clipboard.\n\n    Notes:\n        Screenshot is not in clipboard if user cancelled the operation.\n\n    Returns:\n        bool: Screenshot was added to clipboard.\n    \"\"\"\n\n    return ScreenMarquee.capture_to_clipboard()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/screenshot_widget.html#client.ayon_core.tools.publisher.widgets.screenshot_widget.capture_to_file","title":"<code>capture_to_file(filepath=None)</code>","text":"<p>Take screenshot with marquee into file.</p> <p>Parameters:</p> Name Type Description Default <code>filepath</code> <code>Optional[str]</code> <p>Path where screenshot will be saved.</p> <code>None</code> <p>Returns:</p> Type Description <p>Union[str, None]: Path to the saved screenshot, or None if user cancelled the operation.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/screenshot_widget.py</code> <pre><code>def capture_to_file(filepath=None):\n    \"\"\"Take screenshot with marquee into file.\n\n    Args:\n        filepath (Optional[str]): Path where screenshot will be saved.\n\n    Returns:\n        Union[str, None]: Path to the saved screenshot, or None if user\n            cancelled the operation.\n    \"\"\"\n\n    return ScreenMarquee.capture_to_file(filepath)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/screenshot_widget.html#client.ayon_core.tools.publisher.widgets.screenshot_widget.capture_to_pixmap","title":"<code>capture_to_pixmap()</code>","text":"<p>Take screenshot with marquee into pixmap.</p> Note <p>The pixmap can be invalid (use 'isNull' to check).</p> <p>Returns:</p> Type Description <p>QtGui.QPixmap: Captured pixmap image.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/screenshot_widget.py</code> <pre><code>def capture_to_pixmap():\n    \"\"\"Take screenshot with marquee into pixmap.\n\n    Note:\n        The pixmap can be invalid (use 'isNull' to check).\n\n    Returns:\n        QtGui.QPixmap: Captured pixmap image.\n    \"\"\"\n\n    return ScreenMarquee.capture_to_pixmap()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/tabs_widget.html","title":"tabs_widget","text":""},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/tasks_model.html","title":"tasks_model","text":""},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/tasks_model.html#client.ayon_core.tools.publisher.widgets.tasks_model.TasksModel","title":"<code>TasksModel</code>","text":"<p>               Bases: <code>QStandardItemModel</code></p> <p>Tasks model.</p> <p>Task model must have set context of folder paths.</p> <p>Items in model are based on 0-infinite folders. Always contain an interserction of context folder tasks. When no folders are in context them model is empty if 2 or more are in context folders that don't have tasks with same names then model is empty too.</p> <p>Parameters:</p> Name Type Description Default <code>controller</code> <code>AbstractPublisherFrontend</code> <p>Controller which handles creation and publishing.</p> required Source code in <code>client/ayon_core/tools/publisher/widgets/tasks_model.py</code> <pre><code>class TasksModel(QtGui.QStandardItemModel):\n    \"\"\"Tasks model.\n\n    Task model must have set context of folder paths.\n\n    Items in model are based on 0-infinite folders. Always contain\n    an interserction of context folder tasks. When no folders are in context\n    them model is empty if 2 or more are in context folders that don't have\n    tasks with same names then model is empty too.\n\n    Args:\n        controller (AbstractPublisherFrontend): Controller which handles\n            creation and publishing.\n\n    \"\"\"\n    def __init__(\n        self,\n        controller: AbstractPublisherFrontend,\n        allow_empty_task: Optional[bool] = False\n    ):\n        super().__init__()\n\n        self._allow_empty_task = allow_empty_task\n        self._controller: AbstractPublisherFrontend = controller\n        self._items_by_name = {}\n        self._folder_paths = []\n        self._task_names_by_folder_path = {}\n\n    def set_folder_paths(self, folder_paths):\n        \"\"\"Set folders context.\"\"\"\n        self._folder_paths = folder_paths\n        self.reset()\n\n    @staticmethod\n    def get_intersection_of_tasks(task_names_by_folder_path):\n        \"\"\"Calculate intersection of task names from passed data.\n\n        Example:\n        ```\n        # Passed `task_names_by_folder_path`\n        {\n            \"/folder_1\": [\"compositing\", \"animation\"],\n            \"/folder_2\": [\"compositing\", \"editorial\"]\n        }\n        ```\n        Result:\n        ```\n        # Set\n        {\"compositing\"}\n        ```\n\n        Args:\n            task_names_by_folder_path (dict): Task names in iterable by parent.\n        \"\"\"\n        tasks = None\n        for task_names in task_names_by_folder_path.values():\n            if tasks is None:\n                tasks = set(task_names)\n            else:\n                tasks &amp;= set(task_names)\n\n            if not tasks:\n                break\n        return tasks or set()\n\n    def is_task_name_valid(self, folder_path, task_name):\n        \"\"\"Is task name available for folder.\n\n        Todos:\n            Move this method to PublisherController.\n\n        Args:\n            folder_path (str): Fodler path where should look for task.\n            task_name (str): Name of task which should be available in folder\n                tasks.\n        \"\"\"\n        if folder_path not in self._task_names_by_folder_path:\n            return False\n\n        if self._allow_empty_task and not task_name:\n            return True\n\n        task_names = self._task_names_by_folder_path[folder_path]\n        if task_name in task_names:\n            return True\n        return False\n\n    def reset(self):\n        \"\"\"Update model by current context.\"\"\"\n        if not self._folder_paths:\n            self._items_by_name = {}\n            self._task_names_by_folder_path = {}\n            root_item = self.invisibleRootItem()\n            root_item.removeRows(0, self.rowCount())\n            return\n\n        task_items_by_folder_path = (\n            self._controller.get_task_items_by_folder_paths(\n                self._folder_paths\n            )\n        )\n\n        task_names_by_folder_path = {\n            folder_path: {item.name for item in task_items}\n            for folder_path, task_items in task_items_by_folder_path.items()\n        }\n        self._task_names_by_folder_path = task_names_by_folder_path\n\n        new_task_names = self.get_intersection_of_tasks(\n            task_names_by_folder_path\n        )\n        if self._allow_empty_task:\n            new_task_names.add(\"\")\n        old_task_names = set(self._items_by_name.keys())\n        if new_task_names == old_task_names:\n            return\n\n        root_item = self.invisibleRootItem()\n        for task_name in old_task_names:\n            if task_name not in new_task_names:\n                item = self._items_by_name.pop(task_name)\n                root_item.removeRow(item.row())\n\n        default_icon = get_qt_icon(AwesomeFontIcon(\n            \"fa.male\",\n            color=get_default_entity_icon_color(),\n        ))\n        new_items = []\n        task_type_items = {\n            task_type_item.name: task_type_item\n            for task_type_item in self._controller.get_task_type_items(\n                self._controller.get_current_project_name()\n            )\n        }\n        type_item_by_task_name = {}\n        for task_items in task_items_by_folder_path.values():\n            for task_item in task_items:\n                task_name = task_item.name\n                if (\n                    task_name not in new_task_names\n                    or task_name in type_item_by_task_name\n                ):\n                    continue\n                task_type_name = task_item.task_type\n                task_type_item = task_type_items.get(task_type_name)\n                if task_type_item:\n                    type_item_by_task_name[task_name] = task_type_item\n\n        for task_name in new_task_names:\n            item = self._items_by_name.get(task_name)\n            if item is None:\n                item = QtGui.QStandardItem(task_name)\n                item.setData(task_name, TASK_NAME_ROLE)\n                self._items_by_name[task_name] = item\n                new_items.append(item)\n\n            if not task_name:\n                continue\n\n            icon = icon_name = icon_color = None\n            task_type_item = type_item_by_task_name.get(task_name)\n            if task_type_item is not None:\n                icon_name = task_type_item.icon\n                icon_color = task_type_item.color\n            if icon_name:\n                if not icon_color:\n                    icon_color = get_default_entity_icon_color()\n                icon = get_qt_icon(\n                    MaterialSymbolsIcon(icon_name, color=icon_color)\n                )\n            if icon is None:\n                icon = default_icon\n            item.setData(icon, QtCore.Qt.DecorationRole)\n\n        if new_items:\n            root_item.appendRows(new_items)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/tasks_model.html#client.ayon_core.tools.publisher.widgets.tasks_model.TasksModel.get_intersection_of_tasks","title":"<code>get_intersection_of_tasks(task_names_by_folder_path)</code>  <code>staticmethod</code>","text":"<p>Calculate intersection of task names from passed data.</p> <p>Example:</p> <pre><code># Passed `task_names_by_folder_path`\n{\n    \"/folder_1\": [\"compositing\", \"animation\"],\n    \"/folder_2\": [\"compositing\", \"editorial\"]\n}\n</code></pre> <p>Result:</p> <pre><code># Set\n{\"compositing\"}\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>task_names_by_folder_path</code> <code>dict</code> <p>Task names in iterable by parent.</p> required Source code in <code>client/ayon_core/tools/publisher/widgets/tasks_model.py</code> <pre><code>@staticmethod\ndef get_intersection_of_tasks(task_names_by_folder_path):\n    \"\"\"Calculate intersection of task names from passed data.\n\n    Example:\n    ```\n    # Passed `task_names_by_folder_path`\n    {\n        \"/folder_1\": [\"compositing\", \"animation\"],\n        \"/folder_2\": [\"compositing\", \"editorial\"]\n    }\n    ```\n    Result:\n    ```\n    # Set\n    {\"compositing\"}\n    ```\n\n    Args:\n        task_names_by_folder_path (dict): Task names in iterable by parent.\n    \"\"\"\n    tasks = None\n    for task_names in task_names_by_folder_path.values():\n        if tasks is None:\n            tasks = set(task_names)\n        else:\n            tasks &amp;= set(task_names)\n\n        if not tasks:\n            break\n    return tasks or set()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/tasks_model.html#client.ayon_core.tools.publisher.widgets.tasks_model.TasksModel.is_task_name_valid","title":"<code>is_task_name_valid(folder_path, task_name)</code>","text":"<p>Is task name available for folder.</p> Todos <p>Move this method to PublisherController.</p> <p>Parameters:</p> Name Type Description Default <code>folder_path</code> <code>str</code> <p>Fodler path where should look for task.</p> required <code>task_name</code> <code>str</code> <p>Name of task which should be available in folder tasks.</p> required Source code in <code>client/ayon_core/tools/publisher/widgets/tasks_model.py</code> <pre><code>def is_task_name_valid(self, folder_path, task_name):\n    \"\"\"Is task name available for folder.\n\n    Todos:\n        Move this method to PublisherController.\n\n    Args:\n        folder_path (str): Fodler path where should look for task.\n        task_name (str): Name of task which should be available in folder\n            tasks.\n    \"\"\"\n    if folder_path not in self._task_names_by_folder_path:\n        return False\n\n    if self._allow_empty_task and not task_name:\n        return True\n\n    task_names = self._task_names_by_folder_path[folder_path]\n    if task_name in task_names:\n        return True\n    return False\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/tasks_model.html#client.ayon_core.tools.publisher.widgets.tasks_model.TasksModel.reset","title":"<code>reset()</code>","text":"<p>Update model by current context.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/tasks_model.py</code> <pre><code>def reset(self):\n    \"\"\"Update model by current context.\"\"\"\n    if not self._folder_paths:\n        self._items_by_name = {}\n        self._task_names_by_folder_path = {}\n        root_item = self.invisibleRootItem()\n        root_item.removeRows(0, self.rowCount())\n        return\n\n    task_items_by_folder_path = (\n        self._controller.get_task_items_by_folder_paths(\n            self._folder_paths\n        )\n    )\n\n    task_names_by_folder_path = {\n        folder_path: {item.name for item in task_items}\n        for folder_path, task_items in task_items_by_folder_path.items()\n    }\n    self._task_names_by_folder_path = task_names_by_folder_path\n\n    new_task_names = self.get_intersection_of_tasks(\n        task_names_by_folder_path\n    )\n    if self._allow_empty_task:\n        new_task_names.add(\"\")\n    old_task_names = set(self._items_by_name.keys())\n    if new_task_names == old_task_names:\n        return\n\n    root_item = self.invisibleRootItem()\n    for task_name in old_task_names:\n        if task_name not in new_task_names:\n            item = self._items_by_name.pop(task_name)\n            root_item.removeRow(item.row())\n\n    default_icon = get_qt_icon(AwesomeFontIcon(\n        \"fa.male\",\n        color=get_default_entity_icon_color(),\n    ))\n    new_items = []\n    task_type_items = {\n        task_type_item.name: task_type_item\n        for task_type_item in self._controller.get_task_type_items(\n            self._controller.get_current_project_name()\n        )\n    }\n    type_item_by_task_name = {}\n    for task_items in task_items_by_folder_path.values():\n        for task_item in task_items:\n            task_name = task_item.name\n            if (\n                task_name not in new_task_names\n                or task_name in type_item_by_task_name\n            ):\n                continue\n            task_type_name = task_item.task_type\n            task_type_item = task_type_items.get(task_type_name)\n            if task_type_item:\n                type_item_by_task_name[task_name] = task_type_item\n\n    for task_name in new_task_names:\n        item = self._items_by_name.get(task_name)\n        if item is None:\n            item = QtGui.QStandardItem(task_name)\n            item.setData(task_name, TASK_NAME_ROLE)\n            self._items_by_name[task_name] = item\n            new_items.append(item)\n\n        if not task_name:\n            continue\n\n        icon = icon_name = icon_color = None\n        task_type_item = type_item_by_task_name.get(task_name)\n        if task_type_item is not None:\n            icon_name = task_type_item.icon\n            icon_color = task_type_item.color\n        if icon_name:\n            if not icon_color:\n                icon_color = get_default_entity_icon_color()\n            icon = get_qt_icon(\n                MaterialSymbolsIcon(icon_name, color=icon_color)\n            )\n        if icon is None:\n            icon = default_icon\n        item.setData(icon, QtCore.Qt.DecorationRole)\n\n    if new_items:\n        root_item.appendRows(new_items)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/tasks_model.html#client.ayon_core.tools.publisher.widgets.tasks_model.TasksModel.set_folder_paths","title":"<code>set_folder_paths(folder_paths)</code>","text":"<p>Set folders context.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/tasks_model.py</code> <pre><code>def set_folder_paths(self, folder_paths):\n    \"\"\"Set folders context.\"\"\"\n    self._folder_paths = folder_paths\n    self.reset()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/thumbnail_widget.html","title":"thumbnail_widget","text":""},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/thumbnail_widget.html#client.ayon_core.tools.publisher.widgets.thumbnail_widget.ThumbnailWidget","title":"<code>ThumbnailWidget</code>","text":"<p>               Bases: <code>QWidget</code></p> <p>Instance thumbnail widget.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/thumbnail_widget.py</code> <pre><code>class ThumbnailWidget(QtWidgets.QWidget):\n    \"\"\"Instance thumbnail widget.\"\"\"\n\n    thumbnail_created = QtCore.Signal(str)\n    thumbnail_cleared = QtCore.Signal()\n\n    def __init__(\n        self, controller: AbstractPublisherFrontend, parent: QtWidgets.QWidget\n    ):\n        # Missing implementation for thumbnail\n        # - widget kept to make a visial offset of global attr widget offset\n        super().__init__(parent)\n        self.setAcceptDrops(True)\n\n        thumbnail_painter = ThumbnailPainterWidget(self)\n\n        icon_color = get_objected_colors(\"bg-view-selection\").get_qcolor()\n        icon_color.setAlpha(255)\n\n        buttons_widget = QtWidgets.QWidget(self)\n        buttons_widget.setAttribute(QtCore.Qt.WA_TranslucentBackground)\n\n        clear_image = get_image(\"clear_thumbnail\")\n        clear_pix = paint_image_with_color(clear_image, icon_color)\n        clear_button = PixmapButton(clear_pix, buttons_widget)\n        clear_button.setObjectName(\"ThumbnailPixmapHoverButton\")\n        clear_button.setToolTip(\"Clear thumbnail\")\n\n        take_screenshot_image = get_image(\"take_screenshot\")\n        take_screenshot_pix = paint_image_with_color(\n            take_screenshot_image, icon_color)\n        take_screenshot_btn = PixmapButton(\n            take_screenshot_pix, buttons_widget)\n        take_screenshot_btn.setObjectName(\"ThumbnailPixmapHoverButton\")\n        take_screenshot_btn.setToolTip(\"Take screenshot\")\n\n        paste_image = get_image(\"paste\")\n        paste_pix = paint_image_with_color(paste_image, icon_color)\n        paste_btn = PixmapButton(paste_pix, buttons_widget)\n        paste_btn.setObjectName(\"ThumbnailPixmapHoverButton\")\n        paste_btn.setToolTip(\"Paste from clipboard\")\n\n        browse_image = get_image(\"browse\")\n        browse_pix = paint_image_with_color(browse_image, icon_color)\n        browse_btn = PixmapButton(browse_pix, buttons_widget)\n        browse_btn.setObjectName(\"ThumbnailPixmapHoverButton\")\n        browse_btn.setToolTip(\"Browse...\")\n\n        buttons_layout = QtWidgets.QHBoxLayout(buttons_widget)\n        buttons_layout.setContentsMargins(0, 0, 0, 0)\n        buttons_layout.addWidget(take_screenshot_btn, 0)\n        buttons_layout.addWidget(paste_btn, 0)\n        buttons_layout.addWidget(browse_btn, 0)\n        buttons_layout.addWidget(clear_button, 0)\n\n        layout = QtWidgets.QHBoxLayout(self)\n        layout.setContentsMargins(0, 0, 0, 0)\n        layout.addWidget(thumbnail_painter)\n\n        clear_button.clicked.connect(self._on_clear_clicked)\n        take_screenshot_btn.clicked.connect(self._on_take_screenshot)\n        paste_btn.clicked.connect(self._on_paste_from_clipboard)\n        browse_btn.clicked.connect(self._on_browse_clicked)\n\n        self._controller: AbstractPublisherFrontend = controller\n        self._output_dir = controller.get_thumbnail_temp_dir_path()\n\n        self._review_extensions = set(IMAGE_EXTENSIONS) | set(VIDEO_EXTENSIONS)\n\n        self._height = None\n        self._width = None\n        self._adapted_to_size = True\n        self._last_width = None\n        self._last_height = None\n        self._hide_on_finish = False\n\n        self._buttons_widget = buttons_widget\n        self._thumbnail_painter = thumbnail_painter\n        self._clear_button = clear_button\n        self._take_screenshot_btn = take_screenshot_btn\n        self._paste_btn = paste_btn\n        self._browse_btn = browse_btn\n\n        clear_button.setEnabled(False)\n\n    @property\n    def width_ratio(self):\n        return self._thumbnail_painter.width_ratio\n\n    @property\n    def height_ratio(self):\n        return self._thumbnail_painter.height_ratio\n\n    def _get_filepath_from_event(self, event):\n        mime_data = event.mimeData()\n        if not mime_data.hasUrls():\n            return None\n\n        filepaths = []\n        for url in mime_data.urls():\n            filepath = url.toLocalFile()\n            if os.path.exists(filepath):\n                filepaths.append(filepath)\n\n        if len(filepaths) == 1:\n            filepath = filepaths[0]\n            ext = os.path.splitext(filepath)[-1]\n            if ext in self._review_extensions:\n                return filepath\n        return None\n\n    def dragEnterEvent(self, event):\n        filepath = self._get_filepath_from_event(event)\n        if filepath:\n            event.setDropAction(QtCore.Qt.CopyAction)\n            event.accept()\n\n    def dragLeaveEvent(self, event):\n        event.accept()\n\n    def dropEvent(self, event):\n        filepath = self._get_filepath_from_event(event)\n        if not filepath:\n            return\n\n        output = export_thumbnail(filepath, self._output_dir)\n        if output:\n            self.thumbnail_created.emit(output)\n        else:\n            self._controller.emit_card_message(\n                \"Couldn't convert the source for thumbnail\",\n                CardMessageTypes.error\n            )\n\n    def set_adapted_to_hint(self, enabled):\n        self._adapted_to_size = enabled\n        if self._width is not None:\n            self.setMinimumHeight(0)\n            self._width = None\n\n        if self._height is not None:\n            self.setMinimumWidth(0)\n            self._height = None\n\n    def set_width(self, width):\n        if self._width == width:\n            return\n\n        self._adapted_to_size = False\n        self._width = width\n        self.setMinimumHeight(int(\n            (width / self.width_ratio) * self.height_ratio\n        ))\n        if self._height is not None:\n            self.setMinimumWidth(0)\n            self._height = None\n        self._thumbnail_painter.clear_cache()\n\n    def set_height(self, height):\n        if self._height == height:\n            return\n\n        self._height = height\n        self._adapted_to_size = False\n        self.setMinimumWidth(int(\n            (height / self.height_ratio) * self.width_ratio\n        ))\n        if self._width is not None:\n            self.setMinimumHeight(0)\n            self._width = None\n\n        self._thumbnail_painter.clear_cache()\n\n    def _set_current_thumbails(self, thumbnail_paths):\n        self._thumbnail_painter.set_current_thumbnails(thumbnail_paths)\n        self._update_buttons_position()\n\n    def set_current_thumbnails(self, thumbnail_paths=None):\n        self._thumbnail_painter.set_current_thumbnails(thumbnail_paths)\n        self._update_buttons_position()\n        self._clear_button.setEnabled(self._thumbnail_painter.has_pixes)\n\n    def _on_clear_clicked(self):\n        self.set_current_thumbnails()\n        self.thumbnail_cleared.emit()\n        self._clear_button.setEnabled(False)\n\n    def _on_take_screenshot(self):\n        window = self.window()\n        state = window.windowState()\n        window.setWindowState(QtCore.Qt.WindowMinimized)\n        output_path = os.path.join(\n            self._output_dir, uuid.uuid4().hex + \".png\")\n        if capture_to_file(output_path):\n            self.thumbnail_created.emit(output_path)\n        # restore original window state\n        window.setWindowState(state)\n\n    def _on_paste_from_clipboard(self):\n        \"\"\"Set thumbnail from a pixmap image in the system clipboard\"\"\"\n\n        clipboard = QtWidgets.QApplication.clipboard()\n        pixmap = clipboard.pixmap()\n        if pixmap.isNull():\n            return\n\n        # Save as temporary file\n        output_path = os.path.join(\n            self._output_dir, uuid.uuid4().hex + \".png\")\n\n        output_dir = os.path.dirname(output_path)\n        if not os.path.exists(output_dir):\n            os.makedirs(output_dir)\n\n        if pixmap.save(output_path):\n            self.thumbnail_created.emit(output_path)\n\n    def _on_browse_clicked(self):\n        ext_filter = \"Source (*{0})\".format(\n            \" *\".join(self._review_extensions)\n        )\n        filepath, _ = QtWidgets.QFileDialog.getOpenFileName(\n            self, \"Choose thumbnail\", os.path.expanduser(\"~\"), ext_filter\n        )\n        if not filepath:\n            return\n        valid_path = False\n        ext = os.path.splitext(filepath)[-1].lower()\n        if ext in self._review_extensions:\n            valid_path = True\n\n        output = None\n        if valid_path:\n            output = export_thumbnail(filepath, self._output_dir)\n\n        if output:\n            self.thumbnail_created.emit(output)\n        else:\n            self._controller.emit_card_message(\n                \"Couldn't convert the source for thumbnail\",\n                CardMessageTypes.error\n            )\n\n    def _adapt_to_size(self):\n        if not self._adapted_to_size:\n            return\n\n        width = self.width()\n        height = self.height()\n        if width == self._last_width and height == self._last_height:\n            return\n\n        self._last_width = width\n        self._last_height = height\n        self._thumbnail_painter.clear_cache()\n\n    def _update_buttons_position(self):\n        size = self.size()\n        my_width = size.width()\n        my_height = size.height()\n        buttons_sh = self._buttons_widget.sizeHint()\n        buttons_height = buttons_sh.height()\n        buttons_width = buttons_sh.width()\n        pos_x = my_width - (buttons_width + 3)\n        pos_y = my_height - (buttons_height + 3)\n        if pos_x &lt; 0:\n            pos_x = 0\n            buttons_width = my_width\n        if pos_y &lt; 0:\n            pos_y = 0\n            buttons_height = my_height\n        self._buttons_widget.setGeometry(\n            pos_x,\n            pos_y,\n            buttons_width,\n            buttons_height\n        )\n\n    def resizeEvent(self, event):\n        super().resizeEvent(event)\n        self._adapt_to_size()\n        self._update_buttons_position()\n\n    def showEvent(self, event):\n        super().showEvent(event)\n        self._adapt_to_size()\n        self._update_buttons_position()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/utils.html","title":"utils","text":""},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/widgets.html","title":"widgets","text":""},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/widgets.html#client.ayon_core.tools.publisher.widgets.widgets.AbstractInstanceView","title":"<code>AbstractInstanceView</code>","text":"<p>               Bases: <code>QWidget</code></p> <p>Abstract class for instance view in creation part.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/widgets.py</code> <pre><code>class AbstractInstanceView(QtWidgets.QWidget):\n    \"\"\"Abstract class for instance view in creation part.\"\"\"\n    selection_changed = QtCore.Signal()\n    # Refreshed attribute is not changed by view itself\n    # - widget which triggers `refresh` is changing the state\n    # TODO store that information in widget which cares about refreshing\n    refreshed = False\n\n    def set_refreshed(self, refreshed):\n        \"\"\"View is refreshed with last instances.\n\n        Views are not updated all the time. Only if are visible.\n        \"\"\"\n        self.refreshed = refreshed\n\n    def refresh(self):\n        \"\"\"Refresh instances in the view from current `CreatedContext`.\"\"\"\n        raise NotImplementedError((\n            \"{} Method 'refresh' is not implemented.\"\n        ).format(self.__class__.__name__))\n\n    def has_items(self):\n        \"\"\"View has at least one item.\n\n        This is more a question for controller but is called from widget\n        which should probably should not use controller.\n\n        Returns:\n            bool: There is at least one instance or conversion item.\n        \"\"\"\n\n        raise NotImplementedError((\n            \"{} Method 'has_items' is not implemented.\"\n        ).format(self.__class__.__name__))\n\n    def get_selected_items(self):\n        \"\"\"Selected instances required for callbacks.\n\n        Example: When delete button is clicked to know what should be deleted.\n        \"\"\"\n\n        raise NotImplementedError((\n            \"{} Method 'get_selected_items' is not implemented.\"\n        ).format(self.__class__.__name__))\n\n    def set_selected_items(\n        self, instance_ids, context_selected, convertor_identifiers\n    ):\n        \"\"\"Change selection for instances and context.\n\n        Used to applying selection from one view to other.\n\n        Args:\n            instance_ids (List[str]): Selected instance ids.\n            context_selected (bool): Context is selected.\n            convertor_identifiers (List[str]): Selected convertor identifiers.\n\n        \"\"\"\n        raise NotImplementedError((\n            \"{} Method 'set_selected_items' is not implemented.\"\n        ).format(self.__class__.__name__))\n\n    def set_active_toggle_enabled(self, enabled):\n        \"\"\"Instances are disabled for changing enabled state.\n\n        Active state should stay the same until is \"unset\".\n\n        Args:\n            enabled (bool): Instance state can be changed.\n        \"\"\"\n\n        raise NotImplementedError((\n            \"{} Method 'set_active_toggle_enabled' is not implemented.\"\n        ).format(self.__class__.__name__))\n\n    def refresh_instance_states(self, instance_ids=None):\n        \"\"\"Refresh instance states.\n\n        Args:\n            instance_ids: Optional[Iterable[str]]: Instance ids to refresh.\n                If not passed then all instances are refreshed.\n\n        \"\"\"\n\n        raise NotImplementedError(\n            f\"{self.__class__.__name__} Method 'refresh_instance_states'\"\n            \" is not implemented.\"\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/widgets.html#client.ayon_core.tools.publisher.widgets.widgets.AbstractInstanceView.get_selected_items","title":"<code>get_selected_items()</code>","text":"<p>Selected instances required for callbacks.</p> <p>Example: When delete button is clicked to know what should be deleted.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/widgets.py</code> <pre><code>def get_selected_items(self):\n    \"\"\"Selected instances required for callbacks.\n\n    Example: When delete button is clicked to know what should be deleted.\n    \"\"\"\n\n    raise NotImplementedError((\n        \"{} Method 'get_selected_items' is not implemented.\"\n    ).format(self.__class__.__name__))\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/widgets.html#client.ayon_core.tools.publisher.widgets.widgets.AbstractInstanceView.has_items","title":"<code>has_items()</code>","text":"<p>View has at least one item.</p> <p>This is more a question for controller but is called from widget which should probably should not use controller.</p> <p>Returns:</p> Name Type Description <code>bool</code> <p>There is at least one instance or conversion item.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/widgets.py</code> <pre><code>def has_items(self):\n    \"\"\"View has at least one item.\n\n    This is more a question for controller but is called from widget\n    which should probably should not use controller.\n\n    Returns:\n        bool: There is at least one instance or conversion item.\n    \"\"\"\n\n    raise NotImplementedError((\n        \"{} Method 'has_items' is not implemented.\"\n    ).format(self.__class__.__name__))\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/widgets.html#client.ayon_core.tools.publisher.widgets.widgets.AbstractInstanceView.refresh","title":"<code>refresh()</code>","text":"<p>Refresh instances in the view from current <code>CreatedContext</code>.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/widgets.py</code> <pre><code>def refresh(self):\n    \"\"\"Refresh instances in the view from current `CreatedContext`.\"\"\"\n    raise NotImplementedError((\n        \"{} Method 'refresh' is not implemented.\"\n    ).format(self.__class__.__name__))\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/widgets.html#client.ayon_core.tools.publisher.widgets.widgets.AbstractInstanceView.refresh_instance_states","title":"<code>refresh_instance_states(instance_ids=None)</code>","text":"<p>Refresh instance states.</p> <p>Parameters:</p> Name Type Description Default <code>instance_ids</code> <p>Optional[Iterable[str]]: Instance ids to refresh. If not passed then all instances are refreshed.</p> <code>None</code> Source code in <code>client/ayon_core/tools/publisher/widgets/widgets.py</code> <pre><code>def refresh_instance_states(self, instance_ids=None):\n    \"\"\"Refresh instance states.\n\n    Args:\n        instance_ids: Optional[Iterable[str]]: Instance ids to refresh.\n            If not passed then all instances are refreshed.\n\n    \"\"\"\n\n    raise NotImplementedError(\n        f\"{self.__class__.__name__} Method 'refresh_instance_states'\"\n        \" is not implemented.\"\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/widgets.html#client.ayon_core.tools.publisher.widgets.widgets.AbstractInstanceView.set_active_toggle_enabled","title":"<code>set_active_toggle_enabled(enabled)</code>","text":"<p>Instances are disabled for changing enabled state.</p> <p>Active state should stay the same until is \"unset\".</p> <p>Parameters:</p> Name Type Description Default <code>enabled</code> <code>bool</code> <p>Instance state can be changed.</p> required Source code in <code>client/ayon_core/tools/publisher/widgets/widgets.py</code> <pre><code>def set_active_toggle_enabled(self, enabled):\n    \"\"\"Instances are disabled for changing enabled state.\n\n    Active state should stay the same until is \"unset\".\n\n    Args:\n        enabled (bool): Instance state can be changed.\n    \"\"\"\n\n    raise NotImplementedError((\n        \"{} Method 'set_active_toggle_enabled' is not implemented.\"\n    ).format(self.__class__.__name__))\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/widgets.html#client.ayon_core.tools.publisher.widgets.widgets.AbstractInstanceView.set_refreshed","title":"<code>set_refreshed(refreshed)</code>","text":"<p>View is refreshed with last instances.</p> <p>Views are not updated all the time. Only if are visible.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/widgets.py</code> <pre><code>def set_refreshed(self, refreshed):\n    \"\"\"View is refreshed with last instances.\n\n    Views are not updated all the time. Only if are visible.\n    \"\"\"\n    self.refreshed = refreshed\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/widgets.html#client.ayon_core.tools.publisher.widgets.widgets.AbstractInstanceView.set_selected_items","title":"<code>set_selected_items(instance_ids, context_selected, convertor_identifiers)</code>","text":"<p>Change selection for instances and context.</p> <p>Used to applying selection from one view to other.</p> <p>Parameters:</p> Name Type Description Default <code>instance_ids</code> <code>List[str]</code> <p>Selected instance ids.</p> required <code>context_selected</code> <code>bool</code> <p>Context is selected.</p> required <code>convertor_identifiers</code> <code>List[str]</code> <p>Selected convertor identifiers.</p> required Source code in <code>client/ayon_core/tools/publisher/widgets/widgets.py</code> <pre><code>def set_selected_items(\n    self, instance_ids, context_selected, convertor_identifiers\n):\n    \"\"\"Change selection for instances and context.\n\n    Used to applying selection from one view to other.\n\n    Args:\n        instance_ids (List[str]): Selected instance ids.\n        context_selected (bool): Context is selected.\n        convertor_identifiers (List[str]): Selected convertor identifiers.\n\n    \"\"\"\n    raise NotImplementedError((\n        \"{} Method 'set_selected_items' is not implemented.\"\n    ).format(self.__class__.__name__))\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/widgets.html#client.ayon_core.tools.publisher.widgets.widgets.ChangeViewBtn","title":"<code>ChangeViewBtn</code>","text":"<p>               Bases: <code>IconButton</code></p> <p>Toggle views button.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/widgets.py</code> <pre><code>class ChangeViewBtn(IconButton):\n    \"\"\"Toggle views button.\"\"\"\n    def __init__(self, parent=None):\n        super().__init__(parent)\n        self.set_view_type(\"list\")\n\n    def set_view_type(self, view_type):\n        if view_type == \"list\":\n            # icon_name = \"data_table\"\n            icon_name = \"dehaze\"\n            tooltip = \"Change to list view\"\n        elif view_type == \"card\":\n            icon_name = \"view_agenda\"\n            tooltip = \"Change to card view\"\n        else:\n            icon_name = \"segment\"\n            tooltip = \"Change to parent grouping view\"\n\n        # \"format_align_right\"\n        # \"segment\"\n        icon = get_qt_icon(MaterialSymbolsIcon(icon_name))\n        self.setIcon(icon)\n        self.setToolTip(tooltip)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/widgets.html#client.ayon_core.tools.publisher.widgets.widgets.ClickableLineEdit","title":"<code>ClickableLineEdit</code>","text":"<p>               Bases: <code>QLineEdit</code></p> <p>QLineEdit capturing left mouse click.</p> <p>Triggers <code>clicked</code> signal on mouse click.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/widgets.py</code> <pre><code>class ClickableLineEdit(QtWidgets.QLineEdit):\n    \"\"\"QLineEdit capturing left mouse click.\n\n    Triggers `clicked` signal on mouse click.\n    \"\"\"\n    clicked = QtCore.Signal()\n\n    def __init__(self, *args, **kwargs):\n        super().__init__(*args, **kwargs)\n        self.setReadOnly(True)\n        self._mouse_pressed = False\n\n    def mousePressEvent(self, event):\n        if event.button() == QtCore.Qt.LeftButton:\n            self._mouse_pressed = True\n        event.accept()\n\n    def mouseMoveEvent(self, event):\n        event.accept()\n\n    def mouseReleaseEvent(self, event):\n        if self._mouse_pressed:\n            self._mouse_pressed = False\n            if self.rect().contains(event.pos()):\n                self.clicked.emit()\n        event.accept()\n\n    def mouseDoubleClickEvent(self, event):\n        event.accept()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/widgets.html#client.ayon_core.tools.publisher.widgets.widgets.ContextWarningLabel","title":"<code>ContextWarningLabel</code>","text":"<p>               Bases: <code>PublishPixmapLabel</code></p> <p>Pixmap label with warning icon.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/widgets.py</code> <pre><code>class ContextWarningLabel(PublishPixmapLabel):\n    \"\"\"Pixmap label with warning icon.\"\"\"\n    def __init__(self, parent):\n        pix = get_pixmap(\"warning\")\n\n        super().__init__(pix, parent)\n\n        self.setToolTip(\n            \"Contain invalid context. Please check details.\"\n        )\n        self.setObjectName(\"ProductTypeIconLabel\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/widgets.html#client.ayon_core.tools.publisher.widgets.widgets.CreateBtn","title":"<code>CreateBtn</code>","text":"<p>               Bases: <code>PublishIconBtn</code></p> <p>Create instance button.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/widgets.py</code> <pre><code>class CreateBtn(PublishIconBtn):\n    \"\"\"Create instance button.\"\"\"\n\n    def __init__(self, parent=None):\n        icon_path = get_icon_path(\"create\")\n        super().__init__(icon_path, \"Create\", parent)\n        self.setToolTip(\"Create new product/s\")\n        self.setLayoutDirection(QtCore.Qt.RightToLeft)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/widgets.html#client.ayon_core.tools.publisher.widgets.widgets.CreateInstanceBtn","title":"<code>CreateInstanceBtn</code>","text":"<p>               Bases: <code>PublishIconBtn</code></p> <p>Create add button.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/widgets.py</code> <pre><code>class CreateInstanceBtn(PublishIconBtn):\n    \"\"\"Create add button.\"\"\"\n    def __init__(self, parent=None):\n        icon_path = get_icon_path(\"add\")\n        super().__init__(icon_path, parent)\n        self.setToolTip(\"Create new instance\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/widgets.html#client.ayon_core.tools.publisher.widgets.widgets.IconValuePixmapLabel","title":"<code>IconValuePixmapLabel</code>","text":"<p>               Bases: <code>PublishPixmapLabel</code></p> <p>Label resizing to width and height of font.</p> <p>Handle icon parsing from creators/instances. Using of QAwesome module of path to images.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/widgets.py</code> <pre><code>class IconValuePixmapLabel(PublishPixmapLabel):\n    \"\"\"Label resizing to width and height of font.\n\n    Handle icon parsing from creators/instances. Using of QAwesome module\n    of path to images.\n    \"\"\"\n    default_size = 200\n\n    def __init__(self, icon_def, parent):\n        source_pixmap = self._parse_icon_def(icon_def)\n\n        super().__init__(source_pixmap, parent)\n\n    def set_icon_def(self, icon_def):\n        \"\"\"Set icon by it's definition name.\n\n        Args:\n            icon_def (str): Name of FontAwesome icon or path to image.\n        \"\"\"\n        source_pixmap = self._parse_icon_def(icon_def)\n        self.set_source_pixmap(source_pixmap)\n\n    def _default_pixmap(self):\n        pix = QtGui.QPixmap(1, 1)\n        pix.fill(QtCore.Qt.transparent)\n        return pix\n\n    def _parse_icon_def(self, icon_def):\n        icon = parse_icon_def(icon_def, self.default_size, self.default_size)\n        if icon:\n            return icon\n        return self._default_pixmap()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/widgets.html#client.ayon_core.tools.publisher.widgets.widgets.IconValuePixmapLabel.set_icon_def","title":"<code>set_icon_def(icon_def)</code>","text":"<p>Set icon by it's definition name.</p> <p>Parameters:</p> Name Type Description Default <code>icon_def</code> <code>str</code> <p>Name of FontAwesome icon or path to image.</p> required Source code in <code>client/ayon_core/tools/publisher/widgets/widgets.py</code> <pre><code>def set_icon_def(self, icon_def):\n    \"\"\"Set icon by it's definition name.\n\n    Args:\n        icon_def (str): Name of FontAwesome icon or path to image.\n    \"\"\"\n    source_pixmap = self._parse_icon_def(icon_def)\n    self.set_source_pixmap(source_pixmap)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/widgets.html#client.ayon_core.tools.publisher.widgets.widgets.MultipleItemWidget","title":"<code>MultipleItemWidget</code>","text":"<p>               Bases: <code>QWidget</code></p> <p>Widget for immutable text which can have more than one value.</p> <p>Content may be bigger than widget's size and does not have scroll but has flick widget on top (is possible to move around with clicked mouse).</p> Source code in <code>client/ayon_core/tools/publisher/widgets/widgets.py</code> <pre><code>class MultipleItemWidget(QtWidgets.QWidget):\n    \"\"\"Widget for immutable text which can have more than one value.\n\n    Content may be bigger than widget's size and does not have scroll but has\n    flick widget on top (is possible to move around with clicked mouse).\n    \"\"\"\n\n    def __init__(self, parent):\n        super().__init__(parent)\n\n        model = QtGui.QStandardItemModel()\n\n        view = QtWidgets.QListView(self)\n        view.setObjectName(\"MultipleItemView\")\n        view.setLayoutMode(QtWidgets.QListView.Batched)\n        view.setViewMode(QtWidgets.QListView.IconMode)\n        view.setResizeMode(QtWidgets.QListView.Adjust)\n        view.setWrapping(False)\n        view.setSpacing(2)\n        view.setModel(model)\n        view.setHorizontalScrollBarPolicy(QtCore.Qt.ScrollBarAlwaysOff)\n        view.setVerticalScrollBarPolicy(QtCore.Qt.ScrollBarAlwaysOff)\n\n        flick = FlickCharm(parent=view)\n        flick.activateOn(view)\n\n        layout = QtWidgets.QHBoxLayout(self)\n        layout.setContentsMargins(0, 0, 0, 0)\n        layout.addWidget(view)\n\n        model.rowsInserted.connect(self._on_insert)\n\n        self._view = view\n        self._model = model\n\n        self._value = []\n\n    def _on_insert(self):\n        self._update_size()\n\n    def _update_size(self):\n        model = self._view.model()\n        if model.rowCount() == 0:\n            return\n        height = self._view.sizeHintForRow(0)\n        self.setMaximumHeight(height + (2 * self._view.spacing()))\n\n    def showEvent(self, event):\n        super().showEvent(event)\n        tmp_item = None\n        if not self._value:\n            # Add temp item to be able calculate maximum height of widget\n            tmp_item = QtGui.QStandardItem(\"tmp\")\n            self._model.appendRow(tmp_item)\n            self._update_size()\n\n        if tmp_item is not None:\n            self._model.clear()\n\n    def resizeEvent(self, event):\n        super().resizeEvent(event)\n        self._update_size()\n\n    def set_value(self, value=None):\n        \"\"\"Set value/s of currently selected instance.\"\"\"\n        if value is None:\n            value = []\n        self._value = value\n\n        self._model.clear()\n        for item_text in value:\n            item = QtGui.QStandardItem(item_text)\n            item.setEditable(False)\n            item.setSelectable(False)\n            self._model.appendRow(item)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/widgets.html#client.ayon_core.tools.publisher.widgets.widgets.MultipleItemWidget.set_value","title":"<code>set_value(value=None)</code>","text":"<p>Set value/s of currently selected instance.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/widgets.py</code> <pre><code>def set_value(self, value=None):\n    \"\"\"Set value/s of currently selected instance.\"\"\"\n    if value is None:\n        value = []\n    self._value = value\n\n    self._model.clear()\n    for item_text in value:\n        item = QtGui.QStandardItem(item_text)\n        item.setEditable(False)\n        item.setSelectable(False)\n        self._model.appendRow(item)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/widgets.html#client.ayon_core.tools.publisher.widgets.widgets.PublishBtn","title":"<code>PublishBtn</code>","text":"<p>               Bases: <code>PublishIconBtn</code></p> <p>Publish start publish button.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/widgets.py</code> <pre><code>class PublishBtn(PublishIconBtn):\n    \"\"\"Publish start publish button.\"\"\"\n    def __init__(self, parent=None):\n        icon_path = get_icon_path(\"play\")\n        super().__init__(icon_path, \"Publish\", parent)\n        self.setToolTip(\"Publish\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/widgets.html#client.ayon_core.tools.publisher.widgets.widgets.PublishIconBtn","title":"<code>PublishIconBtn</code>","text":"<p>               Bases: <code>IconButton</code></p> <p>Button using alpha of source image to redraw with different color.</p> <p>Main class for buttons showed in publisher.</p> <p>TODO: Add different states: - normal           : before publishing - publishing       : publishing is running - validation error : validation error happened - error            : other error happened - success          : publishing finished</p> Source code in <code>client/ayon_core/tools/publisher/widgets/widgets.py</code> <pre><code>class PublishIconBtn(IconButton):\n    \"\"\"Button using alpha of source image to redraw with different color.\n\n    Main class for buttons showed in publisher.\n\n    TODO:\n    Add different states:\n    - normal           : before publishing\n    - publishing       : publishing is running\n    - validation error : validation error happened\n    - error            : other error happened\n    - success          : publishing finished\n    \"\"\"\n\n    def __init__(self, pixmap_path, *args, **kwargs):\n        super().__init__(*args, **kwargs)\n\n        colors = get_objected_colors()\n        icon = self.generate_icon(\n            pixmap_path,\n            enabled_color=colors[\"font\"].get_qcolor(),\n            disabled_color=colors[\"font-disabled\"].get_qcolor())\n        self.setIcon(icon)\n\n    def generate_icon(self, pixmap_path, enabled_color, disabled_color):\n        icon = QtGui.QIcon()\n        image = QtGui.QImage(pixmap_path)\n        enabled_pixmap = self.paint_image_with_color(image, enabled_color)\n        icon.addPixmap(enabled_pixmap, QtGui.QIcon.Normal)\n        disabled_pixmap = self.paint_image_with_color(image, disabled_color)\n        icon.addPixmap(disabled_pixmap, QtGui.QIcon.Disabled)\n        return icon\n\n    @staticmethod\n    def paint_image_with_color(image, color):\n        \"\"\"Redraw image with single color using it's alpha.\n\n        It is expected that input image is singlecolor image with alpha.\n\n        Args:\n            image (QImage): Loaded image with alpha.\n            color (QColor): Color that will be used to paint image.\n        \"\"\"\n        width = image.width()\n        height = image.height()\n        partition = 8\n        part_w = int(width / partition)\n        part_h = int(height / partition)\n        part_w -= part_w % 2\n        part_h -= part_h % 2\n        scaled_image = image.scaled(\n            width - (2 * part_w),\n            height - (2 * part_h),\n            QtCore.Qt.IgnoreAspectRatio,\n            QtCore.Qt.SmoothTransformation\n        )\n        alpha_mask = scaled_image.createAlphaMask()\n        alpha_region = QtGui.QRegion(QtGui.QBitmap.fromImage(alpha_mask))\n        alpha_region.translate(part_w, part_h)\n\n        pixmap = QtGui.QPixmap(width, height)\n        pixmap.fill(QtCore.Qt.transparent)\n\n        painter = QtGui.QPainter(pixmap)\n        painter.setClipRegion(alpha_region)\n        painter.setPen(QtCore.Qt.NoPen)\n        painter.setBrush(color)\n        painter.drawRect(QtCore.QRect(0, 0, width, height))\n        painter.end()\n\n        return pixmap\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/widgets.html#client.ayon_core.tools.publisher.widgets.widgets.PublishIconBtn.paint_image_with_color","title":"<code>paint_image_with_color(image, color)</code>  <code>staticmethod</code>","text":"<p>Redraw image with single color using it's alpha.</p> <p>It is expected that input image is singlecolor image with alpha.</p> <p>Parameters:</p> Name Type Description Default <code>image</code> <code>QImage</code> <p>Loaded image with alpha.</p> required <code>color</code> <code>QColor</code> <p>Color that will be used to paint image.</p> required Source code in <code>client/ayon_core/tools/publisher/widgets/widgets.py</code> <pre><code>@staticmethod\ndef paint_image_with_color(image, color):\n    \"\"\"Redraw image with single color using it's alpha.\n\n    It is expected that input image is singlecolor image with alpha.\n\n    Args:\n        image (QImage): Loaded image with alpha.\n        color (QColor): Color that will be used to paint image.\n    \"\"\"\n    width = image.width()\n    height = image.height()\n    partition = 8\n    part_w = int(width / partition)\n    part_h = int(height / partition)\n    part_w -= part_w % 2\n    part_h -= part_h % 2\n    scaled_image = image.scaled(\n        width - (2 * part_w),\n        height - (2 * part_h),\n        QtCore.Qt.IgnoreAspectRatio,\n        QtCore.Qt.SmoothTransformation\n    )\n    alpha_mask = scaled_image.createAlphaMask()\n    alpha_region = QtGui.QRegion(QtGui.QBitmap.fromImage(alpha_mask))\n    alpha_region.translate(part_w, part_h)\n\n    pixmap = QtGui.QPixmap(width, height)\n    pixmap.fill(QtCore.Qt.transparent)\n\n    painter = QtGui.QPainter(pixmap)\n    painter.setClipRegion(alpha_region)\n    painter.setPen(QtCore.Qt.NoPen)\n    painter.setBrush(color)\n    painter.drawRect(QtCore.QRect(0, 0, width, height))\n    painter.end()\n\n    return pixmap\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/widgets.html#client.ayon_core.tools.publisher.widgets.widgets.PublishReportBtn","title":"<code>PublishReportBtn</code>","text":"<p>               Bases: <code>PublishIconBtn</code></p> <p>Publish report button.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/widgets.py</code> <pre><code>class PublishReportBtn(PublishIconBtn):\n    \"\"\"Publish report button.\"\"\"\n\n    triggered = QtCore.Signal(str)\n\n    def __init__(self, parent=None):\n        icon_path = get_icon_path(\"view_report\")\n        super().__init__(icon_path, parent)\n        self.setToolTip(\"Copy report\")\n        self._actions = []\n\n    def add_action(self, label, identifier):\n        self._actions.append(\n            (label, identifier)\n        )\n\n    def _on_action_trigger(self, identifier):\n        self.triggered.emit(identifier)\n\n    def mouseReleaseEvent(self, event):\n        super().mouseReleaseEvent(event)\n        menu = QtWidgets.QMenu(self)\n        actions = []\n        for item in self._actions:\n            label, identifier = item\n            action = QtWidgets.QAction(label, menu)\n            action.triggered.connect(\n                functools.partial(self._on_action_trigger, identifier)\n            )\n            actions.append(action)\n        menu.addActions(actions)\n        menu.exec_(event.globalPos())\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/widgets.html#client.ayon_core.tools.publisher.widgets.widgets.RemoveInstanceBtn","title":"<code>RemoveInstanceBtn</code>","text":"<p>               Bases: <code>PublishIconBtn</code></p> <p>Create remove button.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/widgets.py</code> <pre><code>class RemoveInstanceBtn(PublishIconBtn):\n    \"\"\"Create remove button.\"\"\"\n    def __init__(self, parent=None):\n        icon_path = resources.get_icon_path(\"delete\")\n        super().__init__(icon_path, parent)\n        self.setToolTip(\"Remove selected instances\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/widgets.html#client.ayon_core.tools.publisher.widgets.widgets.ResetBtn","title":"<code>ResetBtn</code>","text":"<p>               Bases: <code>PublishIconBtn</code></p> <p>Publish reset button.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/widgets.py</code> <pre><code>class ResetBtn(PublishIconBtn):\n    \"\"\"Publish reset button.\"\"\"\n    def __init__(self, parent=None):\n        icon_path = get_icon_path(\"refresh\")\n        super().__init__(icon_path, parent)\n        self.setToolTip(\n            \"Reset &amp; discard changes ({})\".format(ResetKeySequence.toString())\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/widgets.html#client.ayon_core.tools.publisher.widgets.widgets.SaveBtn","title":"<code>SaveBtn</code>","text":"<p>               Bases: <code>PublishIconBtn</code></p> <p>Save context and instances information.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/widgets.py</code> <pre><code>class SaveBtn(PublishIconBtn):\n    \"\"\"Save context and instances information.\"\"\"\n    def __init__(self, parent=None):\n        icon_path = get_icon_path(\"save\")\n        super().__init__(icon_path, parent)\n        self.setToolTip(\n            \"Save changes ({})\".format(\n                QtGui.QKeySequence(QtGui.QKeySequence.Save).toString()\n            )\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/widgets.html#client.ayon_core.tools.publisher.widgets.widgets.StopBtn","title":"<code>StopBtn</code>","text":"<p>               Bases: <code>PublishIconBtn</code></p> <p>Publish stop button.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/widgets.py</code> <pre><code>class StopBtn(PublishIconBtn):\n    \"\"\"Publish stop button.\"\"\"\n    def __init__(self, parent):\n        icon_path = get_icon_path(\"stop\")\n        super().__init__(icon_path, parent)\n        self.setToolTip(\"Stop/Pause publishing\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/publisher/widgets/widgets.html#client.ayon_core.tools.publisher.widgets.widgets.ValidateBtn","title":"<code>ValidateBtn</code>","text":"<p>               Bases: <code>PublishIconBtn</code></p> <p>Publish validate button.</p> Source code in <code>client/ayon_core/tools/publisher/widgets/widgets.py</code> <pre><code>class ValidateBtn(PublishIconBtn):\n    \"\"\"Publish validate button.\"\"\"\n    def __init__(self, parent=None):\n        icon_path = get_icon_path(\"validate\")\n        super().__init__(icon_path, parent)\n        self.setToolTip(\"Validate\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/push_to_project/index.html","title":"push_to_project","text":""},{"location":"autoapi/client/ayon_core/tools/push_to_project/index.html#client.ayon_core.tools.push_to_project.PushToContextController","title":"<code>PushToContextController</code>","text":"Source code in <code>client/ayon_core/tools/push_to_project/control.py</code> <pre><code>class PushToContextController:\n    def __init__(self, project_name=None, version_ids=None):\n        self._event_system = self._create_event_system()\n\n        self._settings_model = SettingsModel()\n        self._projects_model = ProjectsModel(self)\n        self._hierarchy_model = HierarchyModel(self)\n        self._integrate_model = IntegrateModel(self)\n\n        self._selection_model = PushToProjectSelectionModel(self)\n        self._user_values = UserPublishValuesModel(self)\n\n        self._src_project_name = None\n        self._src_version_ids = []\n        self._src_folder_entity = None\n        self._src_folder_task_entities = {}\n        self._src_version_entities = []\n        self._src_product_entities = {}\n        self._src_label = None\n\n        self._submission_enabled = False\n        self._process_thread = None\n        self._process_item_id = None\n\n        self._use_original_name = False\n        self._version_up = False\n\n        self.set_source(project_name, version_ids)\n\n    # Events system\n    def emit_event(self, topic, data=None, source=None):\n        \"\"\"Use implemented event system to trigger event.\"\"\"\n\n        if data is None:\n            data = {}\n        self._event_system.emit(topic, data, source)\n\n    def register_event_callback(self, topic, callback):\n        self._event_system.add_callback(topic, callback)\n\n    def set_source(self, project_name, version_ids):\n        \"\"\"Set source project and version.\n\n        There is currently assumption that tool is working on products of same\n        folder.\n\n        Args:\n            project_name (Union[str, None]): Source project name.\n            version_ids (Optional[list[str]]): Version ids.\n        \"\"\"\n        if not project_name or not version_ids:\n            return\n        if (\n            project_name == self._src_project_name\n            and version_ids == self._src_version_ids\n        ):\n            return\n\n        self._src_project_name = project_name\n        self._src_version_ids = version_ids\n        self._src_label = None\n        folder_entity = None\n        task_entities = {}\n        product_entities = []\n        version_entities = []\n        if project_name and self._src_version_ids:\n            version_entities = list(ayon_api.get_versions(\n                project_name, version_ids=self._src_version_ids))\n\n        if version_entities:\n            product_ids = [\n                version_entity[\"productId\"]\n                for version_entity in version_entities\n            ]\n            product_entities = list(ayon_api.get_products(\n                project_name, product_ids=product_ids)\n            )\n\n        if product_entities:\n            # all products for same folder\n            product_entity = product_entities[0]\n            folder_entity = ayon_api.get_folder_by_id(\n                project_name, product_entity[\"folderId\"]\n            )\n\n        if folder_entity:\n            task_entities = {\n                task_entity[\"name\"]: task_entity\n                for task_entity in ayon_api.get_tasks(\n                    project_name, folder_ids=[folder_entity[\"id\"]]\n                )\n            }\n\n        self._src_folder_entity = folder_entity\n        self._src_folder_task_entities = task_entities\n        self._src_version_entities = version_entities\n        self._src_product_entities = {\n            product[\"id\"]: product\n            for product in product_entities\n        }\n        if folder_entity:\n            self._user_values.set_new_folder_name(folder_entity[\"name\"])\n            variant = self._get_src_variant()\n            if variant:\n                self._user_values.set_variant(variant)\n\n            comment = version_entities[0][\"attrib\"].get(\"comment\")\n            if comment:\n                self._user_values.set_comment(comment)\n\n        self._emit_event(\n            \"source.changed\",\n            {\n                \"project_name\": project_name,\n                \"version_ids\": self._src_version_ids\n            }\n        )\n\n    def get_source_label(self):\n        \"\"\"Get source label.\n\n        Returns:\n            str: Label describing source project and version as path.\n        \"\"\"\n\n        if self._src_label is None:\n            self._src_label = self._prepare_source_label()\n        return self._src_label\n\n    def get_project_settings(self, project_name):\n        return self._settings_model.get_settings(project_name)\n\n    def get_project_items(self, sender=None):\n        return self._projects_model.get_project_items(sender)\n\n    def get_task_type_items(\n        self, project_name: str, sender: str | None = None\n    ) -&gt; list[TaskTypeItem]:\n        return self._projects_model.get_task_type_items(\n            project_name, sender=sender\n        )\n\n    def get_folder_items(self, project_name, sender=None):\n        return self._hierarchy_model.get_folder_items(project_name, sender)\n\n    def get_task_items(self, project_name, folder_id, sender=None):\n        return self._hierarchy_model.get_task_items(\n            project_name, folder_id, sender\n        )\n\n    def get_user_values(self):\n        return self._user_values.get_data()\n\n    def original_names_required(self):\n        \"\"\"Checks if original product names must be used.\n\n        Currently simple check if multiple versions, but if multiple products\n        with different product_base_type were used, it wouldn't be necessary.\n        \"\"\"\n        return len(self._src_version_entities) &gt; 1\n\n    def set_user_value_folder_name(self, folder_name):\n        self._user_values.set_new_folder_name(folder_name)\n        self._invalidate()\n\n    def set_user_value_variant(self, variant):\n        self._user_values.set_variant(variant)\n        self._invalidate()\n\n    def set_user_value_comment(self, comment):\n        self._user_values.set_comment(comment)\n        self._invalidate()\n\n    def set_selected_project(self, project_name):\n        self._selection_model.set_selected_project(project_name)\n        self._invalidate()\n\n    def set_selected_folder(self, folder_id):\n        self._selection_model.set_selected_folder(folder_id)\n        self._invalidate()\n\n    def set_selected_task(self, task_id, task_name):\n        self._selection_model.set_selected_task(task_id, task_name)\n\n    def get_process_items(self) -&gt; dict[str, ProjectPushItemProcess]:\n        \"\"\"Returns dict of all ProjectPushItemProcess items \"\"\"\n        return self._integrate_model.get_items()\n\n    # Processing methods\n    def submit(self, wait=True):\n        if not self._submission_enabled:\n            return []\n\n        if self._process_thread is not None:\n            return []\n\n        item_ids = []\n        for src_version_entity in self._src_version_entities:\n            item_id = self._integrate_model.create_process_item(\n                self._src_project_name,\n                src_version_entity[\"id\"],\n                self._selection_model.get_selected_project_name(),\n                self._selection_model.get_selected_folder_id(),\n                self._selection_model.get_selected_task_name(),\n                self._user_values.variant,\n                comment=self._user_values.comment,\n                new_folder_name=self._user_values.new_folder_name,\n                version_up=self._version_up,\n                use_original_name=self._use_original_name,\n            )\n            item_ids.append(item_id)\n\n        self._process_item_ids = item_ids\n        self._emit_event(\"submit.started\")\n        if wait:\n            self._submit_callback()\n            self._process_item_ids = []\n            return item_ids\n\n        thread = threading.Thread(target=self._submit_callback)\n        self._process_thread = thread\n        thread.start()\n        return item_ids\n\n    def set_version_up(self, state):\n        self._version_up = state\n\n    def wait_for_process_thread(self):\n        if self._process_thread is None:\n            return\n        self._process_thread.join()\n        self._process_thread = None\n\n    def _prepare_source_label(self):\n        if not self._src_project_name or not self._src_version_ids:\n            return \"Source is not defined\"\n\n        folder_entity = self._src_folder_entity\n        if not folder_entity:\n            return \"Source is invalid\"\n\n        folder_path = folder_entity[\"path\"]\n        src_labels = []\n        for version_entity in self._src_version_entities:\n            product_entity = self._src_product_entities.get(\n                version_entity[\"productId\"]\n            )\n            src_labels.append(\n                \"Source: {}{}/{}/v{:0&gt;3}\".format(\n                    self._src_project_name,\n                    folder_path,\n                    product_entity[\"name\"],\n                    version_entity[\"version\"],\n                )\n            )\n\n        return \"\\n\".join(src_labels)\n\n    def _get_task_info_from_repre_entities(\n        self, task_entities, repre_entities\n    ):\n        found_comb = []\n        for repre_entity in repre_entities:\n            context = repre_entity[\"context\"]\n            repre_task_name = context.get(\"task\")\n            if repre_task_name is None:\n                continue\n\n            if isinstance(repre_task_name, dict):\n                repre_task_name = repre_task_name.get(\"name\")\n\n            task_name = None\n            task_type = None\n            if repre_task_name:\n                task_info = task_entities.get(repre_task_name) or {}\n                task_name = task_info.get(\"name\")\n                task_type = task_info.get(\"type\")\n\n            if task_name and task_type:\n                return task_name, task_type\n\n            if task_name:\n                found_comb.append((task_name, task_type))\n\n        for task_name, task_type in found_comb:\n            return task_name, task_type\n        return None, None\n\n    def _get_src_variant(self):\n        project_name = self._src_project_name\n        # parse variant only from first version\n        version_entity = self._src_version_entities[0]\n        task_entities = self._src_folder_task_entities\n        repre_entities = ayon_api.get_representations(\n            project_name, version_ids={version_entity[\"id\"]}\n        )\n        task_name, task_type = self._get_task_info_from_repre_entities(\n            task_entities, repre_entities\n        )\n        product_entity = self._src_product_entities.get(\n            version_entity[\"productId\"]\n        )\n\n        project_settings = self.get_project_settings(project_name)\n        product_base_type = product_entity.get(\"productBaseType\")\n        product_type = product_entity[\"productType\"]\n        if not product_base_type:\n            product_base_type = product_type\n        template = get_product_name_template(\n            self._src_project_name,\n            product_base_type,\n            task_name,\n            task_type,\n            None,\n            project_settings=project_settings\n        )\n        template_low = template.lower()\n        variant_placeholder = \"{variant}\"\n        if (\n            variant_placeholder not in template_low\n            or (not task_name and \"{task\" in template_low)\n        ):\n            return \"\"\n\n        idx = template_low.index(variant_placeholder)\n        template_s = template[:idx]\n        template_e = template[idx + len(variant_placeholder):]\n        fill_data = prepare_template_data({\n            \"family\": product_base_type,\n            \"product\": {\n                \"type\": product_type,\n                \"basetype\": product_base_type,\n            },\n            \"task\": task_name\n        })\n        try:\n            product_s = template_s.format(**fill_data)\n            product_e = template_e.format(**fill_data)\n        except Exception as exc:\n            print(\"Failed format\", exc)\n            return \"\"\n\n        product_name = product_entity[\"name\"]\n        if (\n            (product_s and not product_name.startswith(product_s))\n            or (product_e and not product_name.endswith(product_e))\n        ):\n            return \"\"\n\n        if product_s:\n            product_name = product_name[len(product_s):]\n        if product_e:\n            product_name = product_name[:len(product_e)]\n        return product_name\n\n    def _check_submit_validations(self):\n        if not self._selection_model.get_selected_project_name():\n            return False\n\n        if (\n            self._user_values.new_folder_name is None\n            and not self._selection_model.get_selected_folder_id()\n        ):\n            return False\n\n        if self._use_original_name:\n            return True\n\n        if not self._user_values.is_valid:\n            return False\n\n        return True\n\n    def _invalidate(self):\n        submission_enabled = self._check_submit_validations()\n        if submission_enabled == self._submission_enabled:\n            return\n        self._submission_enabled = submission_enabled\n        self._emit_event(\n            \"submission.enabled.changed\",\n            {\"enabled\": submission_enabled}\n        )\n\n    def _submit_callback(self):\n        process_item_ids = self._process_item_ids\n        for process_item_id in process_item_ids:\n            self._integrate_model.integrate_item(process_item_id)\n\n        self._emit_event(\"submit.finished\", {})\n\n        if process_item_ids is self._process_item_ids:\n            self._process_item_ids = []\n\n    def _emit_event(self, topic, data=None):\n        if data is None:\n            data = {}\n        self.emit_event(topic, data, \"controller\")\n\n    def _create_event_system(self):\n        return QueuedEventSystem()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/push_to_project/index.html#client.ayon_core.tools.push_to_project.PushToContextController.emit_event","title":"<code>emit_event(topic, data=None, source=None)</code>","text":"<p>Use implemented event system to trigger event.</p> Source code in <code>client/ayon_core/tools/push_to_project/control.py</code> <pre><code>def emit_event(self, topic, data=None, source=None):\n    \"\"\"Use implemented event system to trigger event.\"\"\"\n\n    if data is None:\n        data = {}\n    self._event_system.emit(topic, data, source)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/push_to_project/index.html#client.ayon_core.tools.push_to_project.PushToContextController.get_process_items","title":"<code>get_process_items()</code>","text":"<p>Returns dict of all ProjectPushItemProcess items</p> Source code in <code>client/ayon_core/tools/push_to_project/control.py</code> <pre><code>def get_process_items(self) -&gt; dict[str, ProjectPushItemProcess]:\n    \"\"\"Returns dict of all ProjectPushItemProcess items \"\"\"\n    return self._integrate_model.get_items()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/push_to_project/index.html#client.ayon_core.tools.push_to_project.PushToContextController.get_source_label","title":"<code>get_source_label()</code>","text":"<p>Get source label.</p> <p>Returns:</p> Name Type Description <code>str</code> <p>Label describing source project and version as path.</p> Source code in <code>client/ayon_core/tools/push_to_project/control.py</code> <pre><code>def get_source_label(self):\n    \"\"\"Get source label.\n\n    Returns:\n        str: Label describing source project and version as path.\n    \"\"\"\n\n    if self._src_label is None:\n        self._src_label = self._prepare_source_label()\n    return self._src_label\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/push_to_project/index.html#client.ayon_core.tools.push_to_project.PushToContextController.original_names_required","title":"<code>original_names_required()</code>","text":"<p>Checks if original product names must be used.</p> <p>Currently simple check if multiple versions, but if multiple products with different product_base_type were used, it wouldn't be necessary.</p> Source code in <code>client/ayon_core/tools/push_to_project/control.py</code> <pre><code>def original_names_required(self):\n    \"\"\"Checks if original product names must be used.\n\n    Currently simple check if multiple versions, but if multiple products\n    with different product_base_type were used, it wouldn't be necessary.\n    \"\"\"\n    return len(self._src_version_entities) &gt; 1\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/push_to_project/index.html#client.ayon_core.tools.push_to_project.PushToContextController.set_source","title":"<code>set_source(project_name, version_ids)</code>","text":"<p>Set source project and version.</p> <p>There is currently assumption that tool is working on products of same folder.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>Union[str, None]</code> <p>Source project name.</p> required <code>version_ids</code> <code>Optional[list[str]]</code> <p>Version ids.</p> required Source code in <code>client/ayon_core/tools/push_to_project/control.py</code> <pre><code>def set_source(self, project_name, version_ids):\n    \"\"\"Set source project and version.\n\n    There is currently assumption that tool is working on products of same\n    folder.\n\n    Args:\n        project_name (Union[str, None]): Source project name.\n        version_ids (Optional[list[str]]): Version ids.\n    \"\"\"\n    if not project_name or not version_ids:\n        return\n    if (\n        project_name == self._src_project_name\n        and version_ids == self._src_version_ids\n    ):\n        return\n\n    self._src_project_name = project_name\n    self._src_version_ids = version_ids\n    self._src_label = None\n    folder_entity = None\n    task_entities = {}\n    product_entities = []\n    version_entities = []\n    if project_name and self._src_version_ids:\n        version_entities = list(ayon_api.get_versions(\n            project_name, version_ids=self._src_version_ids))\n\n    if version_entities:\n        product_ids = [\n            version_entity[\"productId\"]\n            for version_entity in version_entities\n        ]\n        product_entities = list(ayon_api.get_products(\n            project_name, product_ids=product_ids)\n        )\n\n    if product_entities:\n        # all products for same folder\n        product_entity = product_entities[0]\n        folder_entity = ayon_api.get_folder_by_id(\n            project_name, product_entity[\"folderId\"]\n        )\n\n    if folder_entity:\n        task_entities = {\n            task_entity[\"name\"]: task_entity\n            for task_entity in ayon_api.get_tasks(\n                project_name, folder_ids=[folder_entity[\"id\"]]\n            )\n        }\n\n    self._src_folder_entity = folder_entity\n    self._src_folder_task_entities = task_entities\n    self._src_version_entities = version_entities\n    self._src_product_entities = {\n        product[\"id\"]: product\n        for product in product_entities\n    }\n    if folder_entity:\n        self._user_values.set_new_folder_name(folder_entity[\"name\"])\n        variant = self._get_src_variant()\n        if variant:\n            self._user_values.set_variant(variant)\n\n        comment = version_entities[0][\"attrib\"].get(\"comment\")\n        if comment:\n            self._user_values.set_comment(comment)\n\n    self._emit_event(\n        \"source.changed\",\n        {\n            \"project_name\": project_name,\n            \"version_ids\": self._src_version_ids\n        }\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/push_to_project/control.html","title":"control","text":""},{"location":"autoapi/client/ayon_core/tools/push_to_project/control.html#client.ayon_core.tools.push_to_project.control.PushToContextController","title":"<code>PushToContextController</code>","text":"Source code in <code>client/ayon_core/tools/push_to_project/control.py</code> <pre><code>class PushToContextController:\n    def __init__(self, project_name=None, version_ids=None):\n        self._event_system = self._create_event_system()\n\n        self._settings_model = SettingsModel()\n        self._projects_model = ProjectsModel(self)\n        self._hierarchy_model = HierarchyModel(self)\n        self._integrate_model = IntegrateModel(self)\n\n        self._selection_model = PushToProjectSelectionModel(self)\n        self._user_values = UserPublishValuesModel(self)\n\n        self._src_project_name = None\n        self._src_version_ids = []\n        self._src_folder_entity = None\n        self._src_folder_task_entities = {}\n        self._src_version_entities = []\n        self._src_product_entities = {}\n        self._src_label = None\n\n        self._submission_enabled = False\n        self._process_thread = None\n        self._process_item_id = None\n\n        self._use_original_name = False\n        self._version_up = False\n\n        self.set_source(project_name, version_ids)\n\n    # Events system\n    def emit_event(self, topic, data=None, source=None):\n        \"\"\"Use implemented event system to trigger event.\"\"\"\n\n        if data is None:\n            data = {}\n        self._event_system.emit(topic, data, source)\n\n    def register_event_callback(self, topic, callback):\n        self._event_system.add_callback(topic, callback)\n\n    def set_source(self, project_name, version_ids):\n        \"\"\"Set source project and version.\n\n        There is currently assumption that tool is working on products of same\n        folder.\n\n        Args:\n            project_name (Union[str, None]): Source project name.\n            version_ids (Optional[list[str]]): Version ids.\n        \"\"\"\n        if not project_name or not version_ids:\n            return\n        if (\n            project_name == self._src_project_name\n            and version_ids == self._src_version_ids\n        ):\n            return\n\n        self._src_project_name = project_name\n        self._src_version_ids = version_ids\n        self._src_label = None\n        folder_entity = None\n        task_entities = {}\n        product_entities = []\n        version_entities = []\n        if project_name and self._src_version_ids:\n            version_entities = list(ayon_api.get_versions(\n                project_name, version_ids=self._src_version_ids))\n\n        if version_entities:\n            product_ids = [\n                version_entity[\"productId\"]\n                for version_entity in version_entities\n            ]\n            product_entities = list(ayon_api.get_products(\n                project_name, product_ids=product_ids)\n            )\n\n        if product_entities:\n            # all products for same folder\n            product_entity = product_entities[0]\n            folder_entity = ayon_api.get_folder_by_id(\n                project_name, product_entity[\"folderId\"]\n            )\n\n        if folder_entity:\n            task_entities = {\n                task_entity[\"name\"]: task_entity\n                for task_entity in ayon_api.get_tasks(\n                    project_name, folder_ids=[folder_entity[\"id\"]]\n                )\n            }\n\n        self._src_folder_entity = folder_entity\n        self._src_folder_task_entities = task_entities\n        self._src_version_entities = version_entities\n        self._src_product_entities = {\n            product[\"id\"]: product\n            for product in product_entities\n        }\n        if folder_entity:\n            self._user_values.set_new_folder_name(folder_entity[\"name\"])\n            variant = self._get_src_variant()\n            if variant:\n                self._user_values.set_variant(variant)\n\n            comment = version_entities[0][\"attrib\"].get(\"comment\")\n            if comment:\n                self._user_values.set_comment(comment)\n\n        self._emit_event(\n            \"source.changed\",\n            {\n                \"project_name\": project_name,\n                \"version_ids\": self._src_version_ids\n            }\n        )\n\n    def get_source_label(self):\n        \"\"\"Get source label.\n\n        Returns:\n            str: Label describing source project and version as path.\n        \"\"\"\n\n        if self._src_label is None:\n            self._src_label = self._prepare_source_label()\n        return self._src_label\n\n    def get_project_settings(self, project_name):\n        return self._settings_model.get_settings(project_name)\n\n    def get_project_items(self, sender=None):\n        return self._projects_model.get_project_items(sender)\n\n    def get_task_type_items(\n        self, project_name: str, sender: str | None = None\n    ) -&gt; list[TaskTypeItem]:\n        return self._projects_model.get_task_type_items(\n            project_name, sender=sender\n        )\n\n    def get_folder_items(self, project_name, sender=None):\n        return self._hierarchy_model.get_folder_items(project_name, sender)\n\n    def get_task_items(self, project_name, folder_id, sender=None):\n        return self._hierarchy_model.get_task_items(\n            project_name, folder_id, sender\n        )\n\n    def get_user_values(self):\n        return self._user_values.get_data()\n\n    def original_names_required(self):\n        \"\"\"Checks if original product names must be used.\n\n        Currently simple check if multiple versions, but if multiple products\n        with different product_base_type were used, it wouldn't be necessary.\n        \"\"\"\n        return len(self._src_version_entities) &gt; 1\n\n    def set_user_value_folder_name(self, folder_name):\n        self._user_values.set_new_folder_name(folder_name)\n        self._invalidate()\n\n    def set_user_value_variant(self, variant):\n        self._user_values.set_variant(variant)\n        self._invalidate()\n\n    def set_user_value_comment(self, comment):\n        self._user_values.set_comment(comment)\n        self._invalidate()\n\n    def set_selected_project(self, project_name):\n        self._selection_model.set_selected_project(project_name)\n        self._invalidate()\n\n    def set_selected_folder(self, folder_id):\n        self._selection_model.set_selected_folder(folder_id)\n        self._invalidate()\n\n    def set_selected_task(self, task_id, task_name):\n        self._selection_model.set_selected_task(task_id, task_name)\n\n    def get_process_items(self) -&gt; dict[str, ProjectPushItemProcess]:\n        \"\"\"Returns dict of all ProjectPushItemProcess items \"\"\"\n        return self._integrate_model.get_items()\n\n    # Processing methods\n    def submit(self, wait=True):\n        if not self._submission_enabled:\n            return []\n\n        if self._process_thread is not None:\n            return []\n\n        item_ids = []\n        for src_version_entity in self._src_version_entities:\n            item_id = self._integrate_model.create_process_item(\n                self._src_project_name,\n                src_version_entity[\"id\"],\n                self._selection_model.get_selected_project_name(),\n                self._selection_model.get_selected_folder_id(),\n                self._selection_model.get_selected_task_name(),\n                self._user_values.variant,\n                comment=self._user_values.comment,\n                new_folder_name=self._user_values.new_folder_name,\n                version_up=self._version_up,\n                use_original_name=self._use_original_name,\n            )\n            item_ids.append(item_id)\n\n        self._process_item_ids = item_ids\n        self._emit_event(\"submit.started\")\n        if wait:\n            self._submit_callback()\n            self._process_item_ids = []\n            return item_ids\n\n        thread = threading.Thread(target=self._submit_callback)\n        self._process_thread = thread\n        thread.start()\n        return item_ids\n\n    def set_version_up(self, state):\n        self._version_up = state\n\n    def wait_for_process_thread(self):\n        if self._process_thread is None:\n            return\n        self._process_thread.join()\n        self._process_thread = None\n\n    def _prepare_source_label(self):\n        if not self._src_project_name or not self._src_version_ids:\n            return \"Source is not defined\"\n\n        folder_entity = self._src_folder_entity\n        if not folder_entity:\n            return \"Source is invalid\"\n\n        folder_path = folder_entity[\"path\"]\n        src_labels = []\n        for version_entity in self._src_version_entities:\n            product_entity = self._src_product_entities.get(\n                version_entity[\"productId\"]\n            )\n            src_labels.append(\n                \"Source: {}{}/{}/v{:0&gt;3}\".format(\n                    self._src_project_name,\n                    folder_path,\n                    product_entity[\"name\"],\n                    version_entity[\"version\"],\n                )\n            )\n\n        return \"\\n\".join(src_labels)\n\n    def _get_task_info_from_repre_entities(\n        self, task_entities, repre_entities\n    ):\n        found_comb = []\n        for repre_entity in repre_entities:\n            context = repre_entity[\"context\"]\n            repre_task_name = context.get(\"task\")\n            if repre_task_name is None:\n                continue\n\n            if isinstance(repre_task_name, dict):\n                repre_task_name = repre_task_name.get(\"name\")\n\n            task_name = None\n            task_type = None\n            if repre_task_name:\n                task_info = task_entities.get(repre_task_name) or {}\n                task_name = task_info.get(\"name\")\n                task_type = task_info.get(\"type\")\n\n            if task_name and task_type:\n                return task_name, task_type\n\n            if task_name:\n                found_comb.append((task_name, task_type))\n\n        for task_name, task_type in found_comb:\n            return task_name, task_type\n        return None, None\n\n    def _get_src_variant(self):\n        project_name = self._src_project_name\n        # parse variant only from first version\n        version_entity = self._src_version_entities[0]\n        task_entities = self._src_folder_task_entities\n        repre_entities = ayon_api.get_representations(\n            project_name, version_ids={version_entity[\"id\"]}\n        )\n        task_name, task_type = self._get_task_info_from_repre_entities(\n            task_entities, repre_entities\n        )\n        product_entity = self._src_product_entities.get(\n            version_entity[\"productId\"]\n        )\n\n        project_settings = self.get_project_settings(project_name)\n        product_base_type = product_entity.get(\"productBaseType\")\n        product_type = product_entity[\"productType\"]\n        if not product_base_type:\n            product_base_type = product_type\n        template = get_product_name_template(\n            self._src_project_name,\n            product_base_type,\n            task_name,\n            task_type,\n            None,\n            project_settings=project_settings\n        )\n        template_low = template.lower()\n        variant_placeholder = \"{variant}\"\n        if (\n            variant_placeholder not in template_low\n            or (not task_name and \"{task\" in template_low)\n        ):\n            return \"\"\n\n        idx = template_low.index(variant_placeholder)\n        template_s = template[:idx]\n        template_e = template[idx + len(variant_placeholder):]\n        fill_data = prepare_template_data({\n            \"family\": product_base_type,\n            \"product\": {\n                \"type\": product_type,\n                \"basetype\": product_base_type,\n            },\n            \"task\": task_name\n        })\n        try:\n            product_s = template_s.format(**fill_data)\n            product_e = template_e.format(**fill_data)\n        except Exception as exc:\n            print(\"Failed format\", exc)\n            return \"\"\n\n        product_name = product_entity[\"name\"]\n        if (\n            (product_s and not product_name.startswith(product_s))\n            or (product_e and not product_name.endswith(product_e))\n        ):\n            return \"\"\n\n        if product_s:\n            product_name = product_name[len(product_s):]\n        if product_e:\n            product_name = product_name[:len(product_e)]\n        return product_name\n\n    def _check_submit_validations(self):\n        if not self._selection_model.get_selected_project_name():\n            return False\n\n        if (\n            self._user_values.new_folder_name is None\n            and not self._selection_model.get_selected_folder_id()\n        ):\n            return False\n\n        if self._use_original_name:\n            return True\n\n        if not self._user_values.is_valid:\n            return False\n\n        return True\n\n    def _invalidate(self):\n        submission_enabled = self._check_submit_validations()\n        if submission_enabled == self._submission_enabled:\n            return\n        self._submission_enabled = submission_enabled\n        self._emit_event(\n            \"submission.enabled.changed\",\n            {\"enabled\": submission_enabled}\n        )\n\n    def _submit_callback(self):\n        process_item_ids = self._process_item_ids\n        for process_item_id in process_item_ids:\n            self._integrate_model.integrate_item(process_item_id)\n\n        self._emit_event(\"submit.finished\", {})\n\n        if process_item_ids is self._process_item_ids:\n            self._process_item_ids = []\n\n    def _emit_event(self, topic, data=None):\n        if data is None:\n            data = {}\n        self.emit_event(topic, data, \"controller\")\n\n    def _create_event_system(self):\n        return QueuedEventSystem()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/push_to_project/control.html#client.ayon_core.tools.push_to_project.control.PushToContextController.emit_event","title":"<code>emit_event(topic, data=None, source=None)</code>","text":"<p>Use implemented event system to trigger event.</p> Source code in <code>client/ayon_core/tools/push_to_project/control.py</code> <pre><code>def emit_event(self, topic, data=None, source=None):\n    \"\"\"Use implemented event system to trigger event.\"\"\"\n\n    if data is None:\n        data = {}\n    self._event_system.emit(topic, data, source)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/push_to_project/control.html#client.ayon_core.tools.push_to_project.control.PushToContextController.get_process_items","title":"<code>get_process_items()</code>","text":"<p>Returns dict of all ProjectPushItemProcess items</p> Source code in <code>client/ayon_core/tools/push_to_project/control.py</code> <pre><code>def get_process_items(self) -&gt; dict[str, ProjectPushItemProcess]:\n    \"\"\"Returns dict of all ProjectPushItemProcess items \"\"\"\n    return self._integrate_model.get_items()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/push_to_project/control.html#client.ayon_core.tools.push_to_project.control.PushToContextController.get_source_label","title":"<code>get_source_label()</code>","text":"<p>Get source label.</p> <p>Returns:</p> Name Type Description <code>str</code> <p>Label describing source project and version as path.</p> Source code in <code>client/ayon_core/tools/push_to_project/control.py</code> <pre><code>def get_source_label(self):\n    \"\"\"Get source label.\n\n    Returns:\n        str: Label describing source project and version as path.\n    \"\"\"\n\n    if self._src_label is None:\n        self._src_label = self._prepare_source_label()\n    return self._src_label\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/push_to_project/control.html#client.ayon_core.tools.push_to_project.control.PushToContextController.original_names_required","title":"<code>original_names_required()</code>","text":"<p>Checks if original product names must be used.</p> <p>Currently simple check if multiple versions, but if multiple products with different product_base_type were used, it wouldn't be necessary.</p> Source code in <code>client/ayon_core/tools/push_to_project/control.py</code> <pre><code>def original_names_required(self):\n    \"\"\"Checks if original product names must be used.\n\n    Currently simple check if multiple versions, but if multiple products\n    with different product_base_type were used, it wouldn't be necessary.\n    \"\"\"\n    return len(self._src_version_entities) &gt; 1\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/push_to_project/control.html#client.ayon_core.tools.push_to_project.control.PushToContextController.set_source","title":"<code>set_source(project_name, version_ids)</code>","text":"<p>Set source project and version.</p> <p>There is currently assumption that tool is working on products of same folder.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>Union[str, None]</code> <p>Source project name.</p> required <code>version_ids</code> <code>Optional[list[str]]</code> <p>Version ids.</p> required Source code in <code>client/ayon_core/tools/push_to_project/control.py</code> <pre><code>def set_source(self, project_name, version_ids):\n    \"\"\"Set source project and version.\n\n    There is currently assumption that tool is working on products of same\n    folder.\n\n    Args:\n        project_name (Union[str, None]): Source project name.\n        version_ids (Optional[list[str]]): Version ids.\n    \"\"\"\n    if not project_name or not version_ids:\n        return\n    if (\n        project_name == self._src_project_name\n        and version_ids == self._src_version_ids\n    ):\n        return\n\n    self._src_project_name = project_name\n    self._src_version_ids = version_ids\n    self._src_label = None\n    folder_entity = None\n    task_entities = {}\n    product_entities = []\n    version_entities = []\n    if project_name and self._src_version_ids:\n        version_entities = list(ayon_api.get_versions(\n            project_name, version_ids=self._src_version_ids))\n\n    if version_entities:\n        product_ids = [\n            version_entity[\"productId\"]\n            for version_entity in version_entities\n        ]\n        product_entities = list(ayon_api.get_products(\n            project_name, product_ids=product_ids)\n        )\n\n    if product_entities:\n        # all products for same folder\n        product_entity = product_entities[0]\n        folder_entity = ayon_api.get_folder_by_id(\n            project_name, product_entity[\"folderId\"]\n        )\n\n    if folder_entity:\n        task_entities = {\n            task_entity[\"name\"]: task_entity\n            for task_entity in ayon_api.get_tasks(\n                project_name, folder_ids=[folder_entity[\"id\"]]\n            )\n        }\n\n    self._src_folder_entity = folder_entity\n    self._src_folder_task_entities = task_entities\n    self._src_version_entities = version_entities\n    self._src_product_entities = {\n        product[\"id\"]: product\n        for product in product_entities\n    }\n    if folder_entity:\n        self._user_values.set_new_folder_name(folder_entity[\"name\"])\n        variant = self._get_src_variant()\n        if variant:\n            self._user_values.set_variant(variant)\n\n        comment = version_entities[0][\"attrib\"].get(\"comment\")\n        if comment:\n            self._user_values.set_comment(comment)\n\n    self._emit_event(\n        \"source.changed\",\n        {\n            \"project_name\": project_name,\n            \"version_ids\": self._src_version_ids\n        }\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/push_to_project/main.html","title":"main","text":""},{"location":"autoapi/client/ayon_core/tools/push_to_project/main.html#client.ayon_core.tools.push_to_project.main.main","title":"<code>main(project, versions)</code>","text":"<p>Run PushToProject tool to integrate version in different project.</p> <p>Parameters:</p> Name Type Description Default <code>project</code> <code>str</code> <p>Source project name.</p> required <code>versions</code> <code>str</code> <p>comma separated versions for same context</p> required Source code in <code>client/ayon_core/tools/push_to_project/main.py</code> <pre><code>@click.command()\n@click.option(\"--project\", help=\"Source project name\")\n@click.option(\"--versions\", help=\"Source version ids\")\ndef main(project, versions):\n    \"\"\"Run PushToProject tool to integrate version in different project.\n\n    Args:\n        project (str): Source project name.\n        versions (str): comma separated versions for same context\n    \"\"\"\n\n    main_show(project, versions.split(\",\"))\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/push_to_project/models/index.html","title":"models","text":""},{"location":"autoapi/client/ayon_core/tools/push_to_project/models/index.html#client.ayon_core.tools.push_to_project.models.IntegrateModel","title":"<code>IntegrateModel</code>","text":"Source code in <code>client/ayon_core/tools/push_to_project/models/integrate.py</code> <pre><code>class IntegrateModel:\n    def __init__(self, controller):\n        self._controller = controller\n        self._process_items = {}\n\n    def reset(self):\n        self._process_items = {}\n\n    def emit_event(self, topic, data=None, source=None):\n        self._controller.emit_event(topic, data, source)\n\n    def create_process_item(\n        self,\n        src_project_name,\n        src_version_id,\n        dst_project_name,\n        dst_folder_id,\n        dst_task_name,\n        variant,\n        comment,\n        new_folder_name,\n        version_up,\n        use_original_name\n    ):\n        \"\"\"Create new item for integration.\n\n        Args:\n            src_project_name (str): Source project name.\n            src_version_id (str): Source version id.\n            dst_project_name (str): Destination project name.\n            dst_folder_id (str): Destination folder id.\n            dst_task_name (str): Destination task name.\n            variant (str): Variant name.\n            comment (Union[str, None]): Comment.\n            new_folder_name (Union[str, None]): New folder name.\n            version_up (bool): Should destination product be versioned up\n            use_original_name (bool): If original product names should be used\n\n        Returns:\n            str: Item id. The id can be used to trigger integration or get\n                status information.\n        \"\"\"\n\n        item = ProjectPushItem(\n            src_project_name,\n            src_version_id,\n            dst_project_name,\n            dst_folder_id,\n            dst_task_name,\n            variant,\n            comment=comment,\n            new_folder_name=new_folder_name,\n            version_up=version_up,\n            use_original_name=use_original_name\n        )\n        process_item = ProjectPushItemProcess(self, item)\n        self._process_items[item.item_id] = process_item\n        return item.item_id\n\n    def integrate_item(self, item_id):\n        \"\"\"Start integration of item.\n\n        Args:\n            item_id (str): Item id which should be integrated.\n        \"\"\"\n\n        item = self._process_items.get(item_id)\n        if item is None or item.started:\n            return\n        item.integrate()\n\n    def get_items(self) -&gt; dict[str, ProjectPushItemProcess]:\n        \"\"\"Returns dict of all ProjectPushItemProcess items \"\"\"\n        return self._process_items\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/push_to_project/models/index.html#client.ayon_core.tools.push_to_project.models.IntegrateModel.create_process_item","title":"<code>create_process_item(src_project_name, src_version_id, dst_project_name, dst_folder_id, dst_task_name, variant, comment, new_folder_name, version_up, use_original_name)</code>","text":"<p>Create new item for integration.</p> <p>Parameters:</p> Name Type Description Default <code>src_project_name</code> <code>str</code> <p>Source project name.</p> required <code>src_version_id</code> <code>str</code> <p>Source version id.</p> required <code>dst_project_name</code> <code>str</code> <p>Destination project name.</p> required <code>dst_folder_id</code> <code>str</code> <p>Destination folder id.</p> required <code>dst_task_name</code> <code>str</code> <p>Destination task name.</p> required <code>variant</code> <code>str</code> <p>Variant name.</p> required <code>comment</code> <code>Union[str, None]</code> <p>Comment.</p> required <code>new_folder_name</code> <code>Union[str, None]</code> <p>New folder name.</p> required <code>version_up</code> <code>bool</code> <p>Should destination product be versioned up</p> required <code>use_original_name</code> <code>bool</code> <p>If original product names should be used</p> required <p>Returns:</p> Name Type Description <code>str</code> <p>Item id. The id can be used to trigger integration or get status information.</p> Source code in <code>client/ayon_core/tools/push_to_project/models/integrate.py</code> <pre><code>def create_process_item(\n    self,\n    src_project_name,\n    src_version_id,\n    dst_project_name,\n    dst_folder_id,\n    dst_task_name,\n    variant,\n    comment,\n    new_folder_name,\n    version_up,\n    use_original_name\n):\n    \"\"\"Create new item for integration.\n\n    Args:\n        src_project_name (str): Source project name.\n        src_version_id (str): Source version id.\n        dst_project_name (str): Destination project name.\n        dst_folder_id (str): Destination folder id.\n        dst_task_name (str): Destination task name.\n        variant (str): Variant name.\n        comment (Union[str, None]): Comment.\n        new_folder_name (Union[str, None]): New folder name.\n        version_up (bool): Should destination product be versioned up\n        use_original_name (bool): If original product names should be used\n\n    Returns:\n        str: Item id. The id can be used to trigger integration or get\n            status information.\n    \"\"\"\n\n    item = ProjectPushItem(\n        src_project_name,\n        src_version_id,\n        dst_project_name,\n        dst_folder_id,\n        dst_task_name,\n        variant,\n        comment=comment,\n        new_folder_name=new_folder_name,\n        version_up=version_up,\n        use_original_name=use_original_name\n    )\n    process_item = ProjectPushItemProcess(self, item)\n    self._process_items[item.item_id] = process_item\n    return item.item_id\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/push_to_project/models/index.html#client.ayon_core.tools.push_to_project.models.IntegrateModel.get_items","title":"<code>get_items()</code>","text":"<p>Returns dict of all ProjectPushItemProcess items</p> Source code in <code>client/ayon_core/tools/push_to_project/models/integrate.py</code> <pre><code>def get_items(self) -&gt; dict[str, ProjectPushItemProcess]:\n    \"\"\"Returns dict of all ProjectPushItemProcess items \"\"\"\n    return self._process_items\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/push_to_project/models/index.html#client.ayon_core.tools.push_to_project.models.IntegrateModel.integrate_item","title":"<code>integrate_item(item_id)</code>","text":"<p>Start integration of item.</p> <p>Parameters:</p> Name Type Description Default <code>item_id</code> <code>str</code> <p>Item id which should be integrated.</p> required Source code in <code>client/ayon_core/tools/push_to_project/models/integrate.py</code> <pre><code>def integrate_item(self, item_id):\n    \"\"\"Start integration of item.\n\n    Args:\n        item_id (str): Item id which should be integrated.\n    \"\"\"\n\n    item = self._process_items.get(item_id)\n    if item is None or item.started:\n        return\n    item.integrate()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/push_to_project/models/index.html#client.ayon_core.tools.push_to_project.models.PushToProjectSelectionModel","title":"<code>PushToProjectSelectionModel</code>","text":"<p>               Bases: <code>object</code></p> <p>Model handling selection changes.</p> <p>Triggering events: - \"selection.project.changed\" - \"selection.folder.changed\" - \"selection.task.changed\"</p> Source code in <code>client/ayon_core/tools/push_to_project/models/selection.py</code> <pre><code>class PushToProjectSelectionModel(object):\n    \"\"\"Model handling selection changes.\n\n    Triggering events:\n    - \"selection.project.changed\"\n    - \"selection.folder.changed\"\n    - \"selection.task.changed\"\n    \"\"\"\n\n    event_source = \"push-to-project.selection.model\"\n\n    def __init__(self, controller):\n        self._controller = controller\n\n        self._project_name = None\n        self._folder_id = None\n        self._task_name = None\n        self._task_id = None\n\n    def get_selected_project_name(self):\n        return self._project_name\n\n    def set_selected_project(self, project_name):\n        if project_name == self._project_name:\n            return\n\n        self._project_name = project_name\n        self._controller.emit_event(\n            \"selection.project.changed\",\n            {\"project_name\": project_name},\n            self.event_source\n        )\n\n    def get_selected_folder_id(self):\n        return self._folder_id\n\n    def set_selected_folder(self, folder_id):\n        if folder_id == self._folder_id:\n            return\n\n        self._folder_id = folder_id\n        self._controller.emit_event(\n            \"selection.folder.changed\",\n            {\n                \"project_name\": self._project_name,\n                \"folder_id\": folder_id,\n            },\n            self.event_source\n        )\n\n    def get_selected_task_name(self):\n        return self._task_name\n\n    def get_selected_task_id(self):\n        return self._task_id\n\n    def set_selected_task(self, task_id, task_name):\n        if task_id == self._task_id:\n            return\n\n        self._task_name = task_name\n        self._task_id = task_id\n        self._controller.emit_event(\n            \"selection.task.changed\",\n            {\n                \"project_name\": self._project_name,\n                \"folder_id\": self._folder_id,\n                \"task_name\": task_name,\n                \"task_id\": task_id,\n            },\n            self.event_source\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/push_to_project/models/index.html#client.ayon_core.tools.push_to_project.models.UserPublishValuesModel","title":"<code>UserPublishValuesModel</code>","text":"<p>Helper object to validate values required for push to different project.</p> <p>Parameters:</p> Name Type Description Default <code>controller</code> <code>PushToContextController</code> <p>Event system to catch and emit events.</p> required Source code in <code>client/ayon_core/tools/push_to_project/models/user_values.py</code> <pre><code>class UserPublishValuesModel:\n    \"\"\"Helper object to validate values required for push to different project.\n\n    Args:\n        controller (PushToContextController): Event system to catch\n            and emit events.\n    \"\"\"\n\n    folder_name_regex = re.compile(\"^[a-zA-Z0-9_.]+$\")\n    variant_regex = re.compile(\"^[{}]+$\".format(PRODUCT_NAME_ALLOWED_SYMBOLS))\n\n    def __init__(self, controller):\n        self._controller = controller\n        self._new_folder_name = None\n        self._variant = None\n        self._comment = None\n        self._is_variant_valid = False\n        self._is_new_folder_name_valid = False\n\n        self.set_new_folder_name(\"\")\n        self.set_variant(\"\")\n        self.set_comment(\"\")\n\n    @property\n    def new_folder_name(self):\n        return self._new_folder_name\n\n    @property\n    def variant(self):\n        return self._variant\n\n    @property\n    def comment(self):\n        return self._comment\n\n    @property\n    def is_variant_valid(self):\n        return self._is_variant_valid\n\n    @property\n    def is_new_folder_name_valid(self):\n        return self._is_new_folder_name_valid\n\n    @property\n    def is_valid(self):\n        return self.is_variant_valid and self.is_new_folder_name_valid\n\n    def get_data(self):\n        return {\n            \"new_folder_name\": self._new_folder_name,\n            \"variant\": self._variant,\n            \"comment\": self._comment,\n            \"is_variant_valid\": self._is_variant_valid,\n            \"is_new_folder_name_valid\": self._is_new_folder_name_valid,\n            \"is_valid\": self.is_valid\n        }\n\n    def set_variant(self, variant):\n        if variant == self._variant:\n            return\n\n        self._variant = variant\n        is_valid = False\n        if variant:\n            is_valid = self.variant_regex.match(variant) is not None\n        self._is_variant_valid = is_valid\n\n        self._controller.emit_event(\n            \"variant.changed\",\n            {\n                \"variant\": variant,\n                \"is_valid\": self._is_variant_valid,\n            },\n            \"user_values\"\n        )\n\n    def set_new_folder_name(self, folder_name):\n        if self._new_folder_name == folder_name:\n            return\n\n        self._new_folder_name = folder_name\n        if folder_name is None:\n            is_valid = True\n        elif not folder_name:\n            is_valid = False\n        else:\n            is_valid = (\n                self.folder_name_regex.match(folder_name) is not None\n            )\n        self._is_new_folder_name_valid = is_valid\n        self._controller.emit_event(\n            \"new_folder_name.changed\",\n            {\n                \"new_folder_name\": self._new_folder_name,\n                \"is_valid\": self._is_new_folder_name_valid,\n            },\n            \"user_values\"\n        )\n\n    def set_comment(self, comment):\n        if comment == self._comment:\n            return\n        self._comment = comment\n        self._controller.emit_event(\n            \"comment.changed\",\n            {\"comment\": comment},\n            \"user_values\"\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/push_to_project/models/integrate.html","title":"integrate","text":""},{"location":"autoapi/client/ayon_core/tools/push_to_project/models/integrate.html#client.ayon_core.tools.push_to_project.models.integrate.IntegrateModel","title":"<code>IntegrateModel</code>","text":"Source code in <code>client/ayon_core/tools/push_to_project/models/integrate.py</code> <pre><code>class IntegrateModel:\n    def __init__(self, controller):\n        self._controller = controller\n        self._process_items = {}\n\n    def reset(self):\n        self._process_items = {}\n\n    def emit_event(self, topic, data=None, source=None):\n        self._controller.emit_event(topic, data, source)\n\n    def create_process_item(\n        self,\n        src_project_name,\n        src_version_id,\n        dst_project_name,\n        dst_folder_id,\n        dst_task_name,\n        variant,\n        comment,\n        new_folder_name,\n        version_up,\n        use_original_name\n    ):\n        \"\"\"Create new item for integration.\n\n        Args:\n            src_project_name (str): Source project name.\n            src_version_id (str): Source version id.\n            dst_project_name (str): Destination project name.\n            dst_folder_id (str): Destination folder id.\n            dst_task_name (str): Destination task name.\n            variant (str): Variant name.\n            comment (Union[str, None]): Comment.\n            new_folder_name (Union[str, None]): New folder name.\n            version_up (bool): Should destination product be versioned up\n            use_original_name (bool): If original product names should be used\n\n        Returns:\n            str: Item id. The id can be used to trigger integration or get\n                status information.\n        \"\"\"\n\n        item = ProjectPushItem(\n            src_project_name,\n            src_version_id,\n            dst_project_name,\n            dst_folder_id,\n            dst_task_name,\n            variant,\n            comment=comment,\n            new_folder_name=new_folder_name,\n            version_up=version_up,\n            use_original_name=use_original_name\n        )\n        process_item = ProjectPushItemProcess(self, item)\n        self._process_items[item.item_id] = process_item\n        return item.item_id\n\n    def integrate_item(self, item_id):\n        \"\"\"Start integration of item.\n\n        Args:\n            item_id (str): Item id which should be integrated.\n        \"\"\"\n\n        item = self._process_items.get(item_id)\n        if item is None or item.started:\n            return\n        item.integrate()\n\n    def get_items(self) -&gt; dict[str, ProjectPushItemProcess]:\n        \"\"\"Returns dict of all ProjectPushItemProcess items \"\"\"\n        return self._process_items\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/push_to_project/models/integrate.html#client.ayon_core.tools.push_to_project.models.integrate.IntegrateModel.create_process_item","title":"<code>create_process_item(src_project_name, src_version_id, dst_project_name, dst_folder_id, dst_task_name, variant, comment, new_folder_name, version_up, use_original_name)</code>","text":"<p>Create new item for integration.</p> <p>Parameters:</p> Name Type Description Default <code>src_project_name</code> <code>str</code> <p>Source project name.</p> required <code>src_version_id</code> <code>str</code> <p>Source version id.</p> required <code>dst_project_name</code> <code>str</code> <p>Destination project name.</p> required <code>dst_folder_id</code> <code>str</code> <p>Destination folder id.</p> required <code>dst_task_name</code> <code>str</code> <p>Destination task name.</p> required <code>variant</code> <code>str</code> <p>Variant name.</p> required <code>comment</code> <code>Union[str, None]</code> <p>Comment.</p> required <code>new_folder_name</code> <code>Union[str, None]</code> <p>New folder name.</p> required <code>version_up</code> <code>bool</code> <p>Should destination product be versioned up</p> required <code>use_original_name</code> <code>bool</code> <p>If original product names should be used</p> required <p>Returns:</p> Name Type Description <code>str</code> <p>Item id. The id can be used to trigger integration or get status information.</p> Source code in <code>client/ayon_core/tools/push_to_project/models/integrate.py</code> <pre><code>def create_process_item(\n    self,\n    src_project_name,\n    src_version_id,\n    dst_project_name,\n    dst_folder_id,\n    dst_task_name,\n    variant,\n    comment,\n    new_folder_name,\n    version_up,\n    use_original_name\n):\n    \"\"\"Create new item for integration.\n\n    Args:\n        src_project_name (str): Source project name.\n        src_version_id (str): Source version id.\n        dst_project_name (str): Destination project name.\n        dst_folder_id (str): Destination folder id.\n        dst_task_name (str): Destination task name.\n        variant (str): Variant name.\n        comment (Union[str, None]): Comment.\n        new_folder_name (Union[str, None]): New folder name.\n        version_up (bool): Should destination product be versioned up\n        use_original_name (bool): If original product names should be used\n\n    Returns:\n        str: Item id. The id can be used to trigger integration or get\n            status information.\n    \"\"\"\n\n    item = ProjectPushItem(\n        src_project_name,\n        src_version_id,\n        dst_project_name,\n        dst_folder_id,\n        dst_task_name,\n        variant,\n        comment=comment,\n        new_folder_name=new_folder_name,\n        version_up=version_up,\n        use_original_name=use_original_name\n    )\n    process_item = ProjectPushItemProcess(self, item)\n    self._process_items[item.item_id] = process_item\n    return item.item_id\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/push_to_project/models/integrate.html#client.ayon_core.tools.push_to_project.models.integrate.IntegrateModel.get_items","title":"<code>get_items()</code>","text":"<p>Returns dict of all ProjectPushItemProcess items</p> Source code in <code>client/ayon_core/tools/push_to_project/models/integrate.py</code> <pre><code>def get_items(self) -&gt; dict[str, ProjectPushItemProcess]:\n    \"\"\"Returns dict of all ProjectPushItemProcess items \"\"\"\n    return self._process_items\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/push_to_project/models/integrate.html#client.ayon_core.tools.push_to_project.models.integrate.IntegrateModel.integrate_item","title":"<code>integrate_item(item_id)</code>","text":"<p>Start integration of item.</p> <p>Parameters:</p> Name Type Description Default <code>item_id</code> <code>str</code> <p>Item id which should be integrated.</p> required Source code in <code>client/ayon_core/tools/push_to_project/models/integrate.py</code> <pre><code>def integrate_item(self, item_id):\n    \"\"\"Start integration of item.\n\n    Args:\n        item_id (str): Item id which should be integrated.\n    \"\"\"\n\n    item = self._process_items.get(item_id)\n    if item is None or item.started:\n        return\n    item.integrate()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/push_to_project/models/integrate.html#client.ayon_core.tools.push_to_project.models.integrate.ProjectPushItemProcess","title":"<code>ProjectPushItemProcess</code>","text":"<p>Parameters:</p> Name Type Description Default <code>model</code> <code>IntegrateModel</code> <p>Model which is processing item.</p> required <code>item</code> <code>ProjectPushItem</code> <p>Item which is being processed.</p> required Source code in <code>client/ayon_core/tools/push_to_project/models/integrate.py</code> <pre><code>class ProjectPushItemProcess:\n    \"\"\"\n    Args:\n        model (IntegrateModel): Model which is processing item.\n        item (ProjectPushItem): Item which is being processed.\n    \"\"\"\n\n    # TODO where to get host?!!!\n    host_name = \"republisher\"\n\n    def __init__(self, model, item):\n        self._model = model\n        self._item = item\n\n        self._src_folder_entity = None\n        self._src_product_entity = None\n        self._src_version_entity = None\n        self._src_reviewable_files = None\n        self._src_repre_items = None\n\n        self._project_entity = None\n        self._anatomy = None\n        self._folder_entity = None\n        self._task_info = None\n        self._product_entity = None\n        self._version_entity = None\n\n        self._product_base_type = None\n        self._product_type = None\n        self._product_name = None\n\n        self._project_settings = None\n        self._template_name = None\n\n        self._status = ProjectPushItemStatus()\n        self._operations = OperationsSession()\n        self._file_transaction = FileTransaction()\n\n        self._messages = []\n\n    @property\n    def item_id(self):\n        return self._item.item_id\n\n    @property\n    def started(self):\n        return self._status.started\n\n    def get_status_data(self):\n        return self._status.to_data()\n\n    def integrate(self):\n        self._status.started = True\n        try:\n            self._log_info(\"Process started\")\n            self._fill_source_variables()\n            self._log_info(\"Source entities were found\")\n            self._fill_destination_project()\n            self._log_info(\"Destination project was found\")\n            self._fill_or_create_destination_folder()\n            self._log_info(\"Destination folder was determined\")\n            self._fill_or_create_destination_task()\n            self._log_info(\"Destination task was determined\")\n            self._determine_product_base_type()\n            self._determine_publish_template_name()\n            self._determine_product_name()\n            self._make_sure_product_exists()\n            self._make_sure_version_exists()\n            self._log_info(\"Prerequirements were prepared\")\n            self._integrate_representations()\n            self._log_info(\"Representations created\")\n            self._reupload_reviewables()\n            self._log_info(\"Integration finished\")\n\n        except PushToProjectError as exc:\n            if not self._status.failed:\n                self._status.set_failed(str(exc))\n\n        except Exception as exc:\n            _exc, _value, _tb = sys.exc_info()\n            product_name = self._src_product_entity[\"name\"]\n            self._status.set_failed(\n                \"Unhandled error happened for `{}`: {}\".format(\n                    product_name, str(exc)\n                ),\n                (_exc, _value, _tb)\n            )\n\n        finally:\n            self._status.finished = True\n            self._emit_event(\n                \"push.finished.changed\",\n                {\n                    \"finished\": True,\n                    \"item_id\": self.item_id,\n                }\n            )\n\n    def _emit_event(self, topic, data):\n        self._model.emit_event(topic, data)\n\n    # Loggin helpers\n    # TODO better logging\n    def _add_message(self, message, level):\n        message_obj = StatusMessage(message, level)\n        self._messages.append(message_obj)\n        self._emit_event(\n            \"push.message.added\",\n            {\n                \"message\": message,\n                \"level\": level,\n                \"item_id\": self.item_id,\n            }\n        )\n        print(message_obj)\n        return message_obj\n\n    def _log_debug(self, message):\n        return self._add_message(message, \"debug\")\n\n    def _log_info(self, message):\n        return self._add_message(message, \"info\")\n\n    def _log_warning(self, message):\n        return self._add_message(message, \"warning\")\n\n    def _log_error(self, message):\n        return self._add_message(message, \"error\")\n\n    def _log_critical(self, message):\n        return self._add_message(message, \"critical\")\n\n    def _fill_source_variables(self):\n        src_project_name = self._item.src_project_name\n        src_version_id = self._item.src_version_id\n\n        project_entity = ayon_api.get_project(src_project_name)\n        if not project_entity:\n            self._status.set_failed(\n                f\"Source project \\\"{src_project_name}\\\" was not found\"\n            )\n\n            self._emit_event(\n                \"push.failed.changed\",\n                {\"item_id\": self.item_id}\n            )\n            raise PushToProjectError(self._status.fail_reason)\n\n        self._log_debug(f\"Project '{src_project_name}' found\")\n\n        version_entity = ayon_api.get_version_by_id(\n            src_project_name, src_version_id\n        )\n        if not version_entity:\n            self._status.set_failed((\n                f\"Source version with id \\\"{src_version_id}\\\"\"\n                f\" was not found in project \\\"{src_project_name}\\\"\"\n            ))\n            raise PushToProjectError(self._status.fail_reason)\n\n        product_id = version_entity[\"productId\"]\n        product_entity = ayon_api.get_product_by_id(\n            src_project_name, product_id\n        )\n        if not product_entity:\n            self._status.set_failed((\n                f\"Could find product with id \\\"{product_id}\\\"\"\n                f\" in project \\\"{src_project_name}\\\"\"\n            ))\n            raise PushToProjectError(self._status.fail_reason)\n\n        folder_id = product_entity[\"folderId\"]\n        folder_entity = ayon_api.get_folder_by_id(\n            src_project_name, folder_id, own_attributes=True\n        )\n        if not folder_entity:\n            self._status.set_failed((\n                f\"Could find folder with id \\\"{folder_id}\\\"\"\n                f\" in project \\\"{src_project_name}\\\"\"\n            ))\n            raise PushToProjectError(self._status.fail_reason)\n\n        anatomy = Anatomy(src_project_name, project_entity=project_entity)\n\n        repre_entities = ayon_api.get_representations(\n            src_project_name,\n            version_ids={src_version_id}\n        )\n        repre_items = [\n            ProjectPushRepreItem(repre_entity, anatomy.roots)\n            for repre_entity in repre_entities\n        ]\n        self._log_debug((\n            f\"Found {len(repre_items)} representations on\"\n            f\" version {src_version_id} in project '{src_project_name}'\"\n        ))\n        if not repre_items:\n            self._status.set_failed(\n                \"Source version does not have representations\"\n                f\" (Version id: {src_version_id})\"\n            )\n            raise PushToProjectError(self._status.fail_reason)\n\n        reviewable_files = []\n        for activity in ayon_api.get_activities(\n            src_project_name,\n            entity_ids={src_version_id},\n            activity_types={\"reviewable\"},\n            fields={\n                \"files.id\",\n                \"files.name\",\n                \"files.mime\",\n            },\n        ):\n            reviewable_files.extend(activity[\"files\"])\n\n        self._src_folder_entity = folder_entity\n        self._src_product_entity = product_entity\n        self._src_version_entity = version_entity\n        self._src_reviewable_files = reviewable_files\n        self._src_repre_items = repre_items\n\n    def _fill_destination_project(self):\n        # --- Destination entities ---\n        dst_project_name = self._item.dst_project_name\n        # Validate project existence\n        dst_project_entity = ayon_api.get_project(dst_project_name)\n        if not dst_project_entity:\n            self._status.set_failed(\n                f\"Destination project '{dst_project_name}' was not found\"\n            )\n            raise PushToProjectError(self._status.fail_reason)\n\n        self._log_debug(\n            f\"Destination project '{dst_project_name}' found\"\n        )\n        self._project_entity = dst_project_entity\n        self._anatomy = Anatomy(\n            dst_project_name,\n            project_entity=dst_project_entity\n        )\n        self._project_settings = get_project_settings(\n            self._item.dst_project_name\n        )\n\n    def _create_folder(\n        self,\n        src_folder_entity: dict[str, Any],\n        project_entity: dict[str, Any],\n        parent_folder_entity: dict[str, Any],\n        folder_name: str\n    ):\n        parent_id = None\n        if parent_folder_entity:\n            parent_id = parent_folder_entity[\"id\"]\n\n        folder_name_low = folder_name.lower()\n        other_folder_entities = ayon_api.get_folders(\n            project_entity[\"name\"],\n            parent_ids=[parent_id],\n            fields={\"id\", \"name\"}\n        )\n        for other_folder_entity in other_folder_entities:\n            other_name = other_folder_entity[\"name\"]\n            if other_name.lower() != folder_name_low:\n                continue\n\n            self._log_debug((\n                f\"Found already existing folder with name \\\"{other_name}\\\"\"\n                f\" which match requested name \\\"{folder_name}\\\"\"\n            ))\n            return ayon_api.get_folder_by_id(\n                project_entity[\"name\"], other_folder_entity[\"id\"]\n            )\n\n        # TODO should we hard pass attribute values?\n        data_keys = (\n            \"clipIn\",\n            \"clipOut\",\n            \"frameStart\",\n            \"frameEnd\",\n            \"handleStart\",\n            \"handleEnd\",\n            \"resolutionWidth\",\n            \"resolutionHeight\",\n            \"fps\",\n            \"pixelAspect\",\n        )\n        new_folder_attrib = {}\n        src_attrib = src_folder_entity[\"attrib\"]\n        for attr_name, attr_value in src_attrib.items():\n            if attr_name in data_keys:\n                new_folder_attrib[attr_name] = attr_value\n\n        new_folder_name = ayon_api.slugify_string(folder_name)\n        folder_label = None\n        if new_folder_name != folder_name:\n            folder_label = folder_name\n\n        src_folder_type = src_folder_entity[\"folderType\"]\n        dst_folder_type = self._get_dst_folder_type(\n            project_entity,\n            src_folder_type\n        )\n        new_thumbnail_id = self._create_new_folder_thumbnail(\n            project_entity, src_folder_entity)\n        folder_entity = new_folder_entity(\n            folder_name,\n            dst_folder_type,\n            parent_id=parent_id,\n            attribs=new_folder_attrib,\n            thumbnail_id=new_thumbnail_id\n        )\n        if folder_label:\n            folder_entity[\"label\"] = folder_label\n\n        self._operations.create_entity(\n            project_entity[\"name\"],\n            \"folder\",\n            folder_entity\n        )\n        self._log_info(\n            f\"Creating new folder with name \\\"{folder_name}\\\"\"\n        )\n        # Calculate path for usage in rest of logic\n        parent_path = \"\"\n        if parent_folder_entity:\n            parent_path = parent_folder_entity[\"path\"]\n        folder_entity[\"path\"] = \"/\".join([parent_path, folder_name])\n        return folder_entity\n\n    def _create_new_folder_thumbnail(\n        self,\n        project_entity: dict[str, Any],\n        src_folder_entity: dict[str, Any]\n    ) -&gt; Optional[str]:\n        \"\"\"Copy thumbnail possibly set on folder.\n\n        Could be different from representation thumbnails, and it is only shown\n        when folder is selected.\n        \"\"\"\n        if not src_folder_entity[\"thumbnailId\"]:\n            return None\n\n        thumbnail = ayon_api.get_folder_thumbnail(\n            self._item.src_project_name,\n            src_folder_entity[\"id\"],\n            src_folder_entity[\"thumbnailId\"]\n        )\n        if not thumbnail.id:\n            return None\n\n        with tempfile.NamedTemporaryFile(delete=False) as tmp_file:\n            tmp_file.write(thumbnail.content)\n            temp_file_path = tmp_file.name\n\n        new_thumbnail_id = None\n        try:\n            new_thumbnail_id = ayon_api.create_thumbnail(\n                project_entity[\"name\"], temp_file_path)\n        finally:\n            if os.path.exists(temp_file_path):\n                os.remove(temp_file_path)\n        return new_thumbnail_id\n\n    def _get_dst_folder_type(\n        self,\n        project_entity: dict[str, Any],\n        src_folder_type: str\n    ) -&gt; str:\n        \"\"\"Get new folder type.\"\"\"\n        for folder_type in project_entity[\"folderTypes\"]:\n            if folder_type[\"name\"].lower() == src_folder_type.lower():\n                return folder_type[\"name\"]\n\n        self._status.set_failed(\n            f\"'{src_folder_type}' folder type is not configured in \"\n            f\"project Anatomy.\"\n        )\n        raise PushToProjectError(self._status.fail_reason)\n\n    def _fill_or_create_destination_folder(self):\n        dst_project_name = self._item.dst_project_name\n        dst_folder_id = self._item.dst_folder_id\n        new_folder_name = self._item.new_folder_name\n        if not dst_folder_id and not new_folder_name:\n            self._status.set_failed(\n                \"Push item does not have defined destination folder\"\n            )\n            raise PushToProjectError(self._status.fail_reason)\n\n        # Get folder entity\n        parent_folder_entity = None\n        if dst_folder_id:\n            parent_folder_entity = ayon_api.get_folder_by_id(\n                self._item.dst_project_name, self._item.dst_folder_id\n            )\n            if not parent_folder_entity:\n                self._status.set_failed(\n                    f\"Could find folder with id \\\"{dst_folder_id}\\\"\"\n                    f\" in project \\\"{dst_project_name}\\\"\"\n                )\n                raise PushToProjectError(self._status.fail_reason)\n\n        if not new_folder_name:\n            folder_entity = parent_folder_entity\n        else:\n            folder_entity = self._create_folder(\n                self._src_folder_entity,\n                self._project_entity,\n                parent_folder_entity,\n                new_folder_name\n            )\n        self._folder_entity = folder_entity\n\n    def _fill_or_create_destination_task(self):\n        folder_entity = self._folder_entity\n        dst_task_name = self._item.dst_task_name\n        dst_project_name = self._item.dst_project_name\n\n        folder_path = folder_entity[\"path\"]\n        folder_tasks = {\n            task_entity[\"name\"].lower(): task_entity\n            for task_entity in ayon_api.get_tasks(\n                dst_project_name, folder_ids=[folder_entity[\"id\"]]\n            )\n        }\n\n        if not dst_task_name:\n            src_task_info = self._get_src_task_info()\n            if not src_task_info:  # really no task selected nor on source\n                self._task_info = {}\n                return\n\n            dst_task_name = src_task_info[\"name\"]\n            if dst_task_name.lower() not in folder_tasks:\n                task_info = self._make_sure_task_exists(\n                    folder_entity, src_task_info\n                )\n                folder_tasks[dst_task_name.lower()] = task_info\n\n        task_info = folder_tasks.get(dst_task_name.lower())\n        if not task_info:\n            self._status.set_failed(\n                f\"Could find task with name \\\"{dst_task_name}\\\"\"\n                f\" on folder \\\"{folder_path}\\\"\"\n                f\" in project \\\"{dst_project_name}\\\"\"\n            )\n            raise PushToProjectError(self._status.fail_reason)\n\n        # Create copy of task info to avoid changing data in task entity\n        task_info = copy.deepcopy(task_info)\n        task_info[\"name\"] = dst_task_name\n        # Fill rest of task information based on task type\n        task_type_name = task_info[\"taskType\"]\n        task_types_by_name = {\n            task_type[\"name\"]: task_type\n            for task_type in self._project_entity[\"taskTypes\"]\n        }\n        task_type_info = copy.deepcopy(\n            task_types_by_name.get(task_type_name, {})\n        )\n        task_type_info.pop(\"name\")  # do not overwrite real task name\n        task_info.update(task_type_info)\n        self._task_info = task_info\n\n    def _determine_product_base_type(self):\n        product_entity = self._src_product_entity\n        product_base_type = product_entity.get(\"productBaseType\")\n        product_type = product_entity[\"productType\"]\n        if not product_base_type:\n            product_base_type = product_type\n        if not product_base_type:\n            self._status.set_failed(\n                \"Couldn't figure out product type from source product\"\n            )\n            raise PushToProjectError(self._status.fail_reason)\n\n        self._log_debug(\n            f\"Publishing product type is '{product_base_type}'\"\n            f\" (Based on source product)\"\n        )\n        self._product_type = product_type\n        self._product_base_type = product_base_type\n\n    def _determine_publish_template_name(self):\n        template_name = get_publish_template_name(\n            project_name=self._item.dst_project_name,\n            host_name=self.host_name,\n            product_base_type=self._product_base_type,\n            task_name=self._task_info.get(\"name\"),\n            task_type=self._task_info.get(\"type\"),\n            project_settings=self._project_settings,\n        )\n        self._log_debug(\n            f\"Using template '{template_name}' for integration\"\n        )\n        self._template_name = template_name\n\n    def _determine_product_name(self):\n        if self._item.use_original_name:\n            product_name = self._src_product_entity[\"name\"]\n        else:\n            try:\n                product_name = get_product_name(\n                    self._item.dst_project_name,\n                    folder_entity=self._folder_entity,\n                    task_entity=self._task_info,\n                    host_name=self.host_name,\n                    product_type=self._product_type,\n                    product_base_type=self._product_base_type,\n                    variant=self._item.variant,\n                    project_settings=self._project_settings\n                )\n            except TaskNotSetError:\n                self._status.set_failed(\n                    \"Target product name template requires task name. To\"\n                    \" continue you have to select target task or change\"\n                    \" settings &lt;b&gt;ayon+settings://core/tools/creator/\"\n                    \"product_name_profiles\"\n                    f\"?project={self._item.dst_project_name}&lt;/b&gt;.\"\n                )\n                raise PushToProjectError(self._status.fail_reason)\n\n        self._log_info(\n            f\"Push will be integrating to product with name '{product_name}'\"\n        )\n        self._product_name = product_name\n\n    def _make_sure_product_exists(self):\n        project_name = self._item.dst_project_name\n        folder_id = self._folder_entity[\"id\"]\n        product_name = self._product_name\n        product_entity = ayon_api.get_product_by_name(\n            project_name, product_name, folder_id\n        )\n        if product_entity:\n            self._product_entity = product_entity\n            return product_entity\n\n        src_attrib = self._src_product_entity[\"attrib\"]\n\n        dst_attrib = {}\n        for key in {\n            \"description\",\n            \"productGroup\",\n        }:\n            value = src_attrib.get(key)\n            if value:\n                dst_attrib[key] = value\n\n        kwargs = dict(\n            name=product_name,\n            product_type=self._product_type,\n            product_base_type=self._product_base_type,\n            folder_id=folder_id,\n            attribs=dst_attrib,\n        )\n        # Backwards compatibility 26/01/28\n        # Check if 'product_base_type' is supported argument\n        if not is_func_signature_supported(\n            new_product_entity, **kwargs\n        ):\n            kwargs[\"product_type\"] = kwargs.pop(\"product_base_type\")\n\n        product_entity = new_product_entity(**kwargs)\n        self._operations.create_entity(\n            project_name, \"product\", product_entity\n        )\n        self._product_entity = product_entity\n\n    def _make_sure_version_exists(self):\n        \"\"\"Make sure version document exits in database.\"\"\"\n\n        project_name = self._item.dst_project_name\n        version_up = self._item.version_up\n        src_version_entity = self._src_version_entity\n        product_entity = self._product_entity\n        product_id = product_entity[\"id\"]\n        product_base_type = product_entity.get(\"productBaseType\")\n        product_type = product_entity[\"productType\"]\n        if not product_base_type:\n            product_base_type = product_type\n        src_attrib = src_version_entity[\"attrib\"]\n\n        dst_attrib = {}\n        for key in {\n            \"productType\",\n            \"productTypes\",\n            \"families\",\n            \"fps\",\n            \"pixelAspect\",\n            \"clipIn\",\n            \"clipOut\",\n            \"frameStart\",\n            \"frameEnd\",\n            \"handleStart\",\n            \"handleEnd\",\n            \"resolutionWidth\",\n            \"resolutionHeight\",\n            \"colorSpace\",\n            \"source\",\n            \"comment\",\n            \"description\",\n            \"intent\",\n        }:\n            value = src_attrib.get(key)\n            if value:\n                dst_attrib[key] = value\n\n        last_version_entity = ayon_api.get_last_version_by_product_id(\n            project_name, product_id\n        )\n        if last_version_entity is None:\n            dst_version = get_versioning_start(\n                project_name,\n                self.host_name,\n                task_name=self._task_info.get(\"name\"),\n                task_type=self._task_info.get(\"taskType\"),\n                product_base_type=product_base_type,\n                product_name=product_entity[\"name\"],\n            )\n        else:\n            dst_version = int(last_version_entity[\"version\"])\n            if version_up:\n                dst_version += 1\n\n        existing_version_entity = ayon_api.get_version_by_name(\n            project_name, dst_version, product_id\n        )\n        thumbnail_id = self._copy_version_thumbnail()\n\n        # Update existing version\n        if existing_version_entity:\n            updata_data = {\"attrib\": dst_attrib}\n            if thumbnail_id:\n                updata_data[\"thumbnailId\"] = thumbnail_id\n            self._operations.update_entity(\n                project_name,\n                \"version\",\n                existing_version_entity[\"id\"],\n                {\"attrib\": dst_attrib}\n            )\n            existing_version_entity[\"attrib\"].update(dst_attrib)\n            self._version_entity = existing_version_entity\n            return\n        copied_tags = self._get_transferable_tags(src_version_entity)\n        copied_status = self._get_transferable_status(src_version_entity)\n\n        description_parts = []\n        dst_attr_description = dst_attrib.get(\"description\")\n        if dst_attr_description:\n            description_parts.append(dst_attr_description)\n\n        description = self._create_src_version_description(\n            self._item.src_project_name,\n            src_version_entity\n        )\n        if description:\n            description_parts.append(description)\n\n        dst_attrib[\"description\"] = \"\\n\\n\".join(description_parts)\n\n        version_entity = new_version_entity(\n            dst_version,\n            product_id,\n            status=copied_status,\n            tags=copied_tags,\n            task_id=self._task_info.get(\"id\"),\n            attribs=dst_attrib,\n            thumbnail_id=thumbnail_id,\n        )\n        self._operations.create_entity(\n            project_name, \"version\", version_entity\n        )\n        self._version_entity = version_entity\n\n    def _make_sure_task_exists(\n        self,\n        folder_entity: dict[str, Any],\n        task_info: dict[str, Any],\n    ) -&gt; dict[str, Any]:\n        \"\"\"Creates destination task from source task information\"\"\"\n        project_name = self._item.dst_project_name\n        found_task_type = None\n        src_task_type = task_info[\"taskType\"]\n        for task_type in self._project_entity[\"taskTypes\"]:\n            if task_type[\"name\"].lower() == src_task_type.lower():\n                found_task_type = task_type[\"name\"]\n                break\n\n        if not found_task_type:\n            self._status.set_failed(\n                f\"'{src_task_type}' task type is not configured in \"\n                 \"project Anatomy.\"\n            )\n\n            raise PushToProjectError(self._status.fail_reason)\n\n        task_info = self._operations.create_task(\n            project_name,\n            task_info[\"name\"],\n            folder_id=folder_entity[\"id\"],\n            task_type=found_task_type,\n            attrib=task_info[\"attrib\"],\n        )\n        self._task_info = task_info.data\n        return self._task_info\n\n    def _get_src_task_info(self):\n        src_version_entity = self._src_version_entity\n        if not src_version_entity[\"taskId\"]:\n            return None\n        src_task = ayon_api.get_task_by_id(\n            self._item.src_project_name, src_version_entity[\"taskId\"]\n        )\n        return src_task\n\n    def _integrate_representations(self):\n        try:\n            self._real_integrate_representations()\n        except Exception:\n            self._operations.clear()\n            self._file_transaction.rollback()\n            raise\n\n    def _real_integrate_representations(self):\n        version_entity = self._version_entity\n        version_id = version_entity[\"id\"]\n        existing_repres = ayon_api.get_representations(\n            self._item.dst_project_name,\n            version_ids={version_id}\n        )\n        existing_repres_by_low_name = {\n            repre_entity[\"name\"].lower(): repre_entity\n            for repre_entity in existing_repres\n        }\n        template_name = self._template_name\n        anatomy = self._anatomy\n        formatting_data = get_template_data(\n            self._project_entity,\n            self._folder_entity,\n            self._task_info,\n            self.host_name\n        )\n        formatting_data.update({\n            \"product\": {\n                \"name\": self._product_name,\n                \"type\": self._product_type,\n                \"basetype\": self._product_base_type,\n            },\n            \"version\": version_entity[\"version\"]\n        })\n\n        publish_template = anatomy.get_template_item(\"publish\", template_name)\n        path_template = publish_template[\"path\"].template.replace(\"\\\\\", \"/\")\n        file_template = publish_template[\"file\"]\n        self._log_info(\"Preparing files to transfer\")\n        processed_repre_items = self._prepare_file_transactions(\n            anatomy, template_name, formatting_data, file_template\n        )\n        self._file_transaction.process()\n        self._log_info(\"Preparing database changes\")\n        self._prepare_database_operations(\n            version_id,\n            processed_repre_items,\n            path_template,\n            existing_repres_by_low_name\n        )\n        self._log_info(\"Finalization\")\n        self._operations.commit()\n        self._file_transaction.finalize()\n\n    def _prepare_file_transactions(\n        self, anatomy, template_name, formatting_data, file_template\n    ):\n        processed_repre_items = []\n        for repre_item in self._src_repre_items:\n            repre_entity = repre_item.repre_entity\n            repre_name = repre_entity[\"name\"]\n            repre_format_data = copy.deepcopy(formatting_data)\n\n            repre_context = self._update_repre_context(\n                copy.deepcopy(repre_entity),\n                formatting_data\n            )\n\n            repre_format_data[\"representation\"] = repre_name\n            for src_file in repre_item.src_files:\n                ext = os.path.splitext(src_file.path)[-1]\n                repre_format_data[\"ext\"] = ext[1:]\n                break\n\n            # Re-use 'output' from source representation\n            repre_output_name = repre_entity[\"context\"].get(\"output\")\n            if repre_output_name is not None:\n                repre_format_data[\"output\"] = repre_output_name\n\n            template_obj = anatomy.get_template_item(\n                \"publish\", template_name, \"directory\"\n            )\n            folder_path = template_obj.format_strict(formatting_data)\n            folder_path_rootless = folder_path.rootless\n            repre_filepaths = []\n            published_path = None\n            for src_file in repre_item.src_files:\n                file_data = copy.deepcopy(repre_format_data)\n                frame = src_file.frame\n                if frame is not None:\n                    file_data[\"frame\"] = frame\n\n                udim = src_file.udim\n                if udim is not None:\n                    file_data[\"udim\"] = udim\n\n                filename = file_template.format_strict(file_data)\n                dst_filepath = os.path.normpath(\n                    os.path.join(folder_path, filename)\n                )\n                dst_rootless_path = os.path.normpath(\n                    os.path.join(folder_path_rootless, filename)\n                )\n                if published_path is None or frame == repre_item.frame:\n                    published_path = dst_filepath\n\n                repre_filepaths.append((dst_filepath, dst_rootless_path))\n                self._file_transaction.add(src_file.path, dst_filepath)\n\n            for resource_file in repre_item.resource_files:\n                dst_filepath = os.path.normpath(\n                    os.path.join(folder_path, resource_file.relative_path)\n                )\n                dst_rootless_path = os.path.normpath(\n                    os.path.join(\n                        folder_path_rootless, resource_file.relative_path\n                    )\n                )\n                repre_filepaths.append((dst_filepath, dst_rootless_path))\n                self._file_transaction.add(resource_file.path, dst_filepath)\n            processed_repre_items.append(\n                (repre_item, repre_filepaths, repre_context, published_path)\n            )\n        return processed_repre_items\n\n    def _prepare_database_operations(\n        self,\n        version_id,\n        processed_repre_items,\n        path_template,\n        existing_repres_by_low_name\n    ):\n        added_repre_names = set()\n        for item in processed_repre_items:\n            (repre_item, repre_filepaths, repre_context, published_path) = item\n            repre_name = repre_item.repre_entity[\"name\"]\n            added_repre_names.add(repre_name.lower())\n            new_repre_attributes = {\n                \"path\": published_path,\n                \"template\": path_template\n            }\n            new_repre_files = []\n            for (path, rootless_path) in repre_filepaths:\n                new_repre_files.append({\n                    \"id\": create_entity_id(),\n                    \"name\": os.path.basename(rootless_path),\n                    \"path\": rootless_path,\n                    \"size\": os.path.getsize(path),\n                    \"hash\": source_hash(path),\n                    \"hash_type\": \"op3\",\n                })\n\n            existing_repre = existing_repres_by_low_name.get(\n                repre_name.lower()\n            )\n            entity_id = None\n            if existing_repre:\n                entity_id = existing_repre[\"id\"]\n            repre_entity = new_representation_entity(\n                repre_name,\n                version_id,\n                new_repre_files,\n                data={\"context\": repre_context},\n                attribs=new_repre_attributes,\n                entity_id=entity_id\n            )\n            if not existing_repre:\n                self._operations.create_entity(\n                    self._item.dst_project_name,\n                    \"representation\",\n                    repre_entity\n                )\n            else:\n                changes = {}\n                for key, value in repre_entity.items():\n                    if key == \"attrib\":\n                        continue\n                    if value != existing_repre.get(key):\n                        changes[key] = value\n                attrib_changes = {}\n                for key, value in repre_entity[\"attrib\"].items():\n                    if value != existing_repre[\"attrib\"].get(key):\n                        attrib_changes[key] = value\n                if attrib_changes:\n                    changes[\"attrib\"] = attrib_changes\n\n                if changes:\n                    self._operations.update_entity(\n                        self._item.dst_project_name,\n                        \"representation\",\n                        entity_id,\n                        changes,\n                    )\n\n        existing_repre_names = set(existing_repres_by_low_name.keys())\n        for repre_name in (existing_repre_names - added_repre_names):\n            repre_entity = existing_repres_by_low_name[repre_name]\n            self._operations.update_entity(\n                self._item.dst_project_name,\n                \"representation\",\n                repre_entity[\"id\"],\n                {\"active\": False}\n            )\n\n    def _copy_version_thumbnail(self) -&gt; Optional[str]:\n        thumbnail_id = self._src_version_entity[\"thumbnailId\"]\n        if not thumbnail_id:\n            return None\n        path = get_thumbnail_path(\n            self._item.src_project_name,\n            \"version\",\n            self._src_version_entity[\"id\"],\n            thumbnail_id\n        )\n        if not path:\n            return None\n        return ayon_api.create_thumbnail(\n            self._item.dst_project_name,\n            path\n        )\n\n    def _update_repre_context(self, repre_entity, formatting_data):\n        \"\"\"Replace old context value with new ones.\n\n        Folder might change, project definitely changes etc.\n        \"\"\"\n        repre_context = repre_entity[\"context\"]\n        for context_key, context_value in repre_context.items():\n            if context_value and isinstance(context_value, dict):\n                for context_sub_key in context_value.keys():\n                    value_to_update = formatting_data.get(context_key, {}).get(\n                        context_sub_key\n                    )\n                    if value_to_update:\n                        repre_context[context_key][context_sub_key] = (\n                            value_to_update\n                        )\n            else:\n                value_to_update = formatting_data.get(context_key)\n                if value_to_update:\n                    repre_context[context_key] = value_to_update\n        if \"task\" not in formatting_data:\n            repre_context.pop(\"task\", None)\n        return repre_context\n\n    def _get_transferable_tags(self, src_version_entity):\n        \"\"\"Copy over only tags present in destination project\"\"\"\n        dst_project_tags = [\n            tag[\"name\"] for tag in self._project_entity[\"tags\"]\n        ]\n        copied_tags = []\n        for src_tag in src_version_entity[\"tags\"]:\n            if src_tag in dst_project_tags:\n                copied_tags.append(src_tag)\n        return copied_tags\n\n    def _get_transferable_status(self, src_version_entity):\n        \"\"\"Copy over status, first status if not matching found\"\"\"\n        dst_project_statuses = {\n            status[\"name\"]: status\n            for status in self._project_entity[\"statuses\"]\n        }\n        copied_status = dst_project_statuses.get(src_version_entity[\"status\"])\n        if copied_status:\n            return copied_status[\"name\"]\n        return None\n\n    def _create_src_version_description(\n            self,\n            src_project_name: str,\n            src_version_entity: dict[str, Any]\n    ) -&gt; str:\n        \"\"\"Creates description text about source version.\"\"\"\n        src_version_id = src_version_entity[\"id\"]\n        src_author = src_version_entity[\"author\"]\n        query = \"&amp;\".join([\n            f\"project={src_project_name}\",\n            \"type=version\",\n            f\"id={src_version_id}\"\n        ])\n        version_url = (\n            f\"{ayon_api.get_base_url()}\"\n            f\"/projects/{src_project_name}/products?{query}\"\n        )\n        description = (\n            f\"Version copied from from  {version_url} \"\n            f\"created by '{src_author}', \"\n        )\n\n        return description\n\n    def _reupload_reviewables(self):\n        tmp_dir = tempfile.mkdtemp(prefix=\"ayon_push_\")\n        try:\n            for file_item in self._src_reviewable_files:\n                dst_path = os.path.join(tmp_dir, file_item[\"name\"])\n                progress = ayon_api.download_project_file(\n                    self._item.src_project_name,\n                    file_item[\"id\"],\n                    dst_path,\n                )\n                if progress.failed:\n                    reason = progress.get_fail_reason()\n                    raise PushToProjectError(\n                        f\"Failed to download reviewable file: '{reason}'\"\n                    )\n\n                ayon_api.upload_reviewable(\n                    self._item.dst_project_name,\n                    self._version_entity[\"id\"],\n                    dst_path,\n                    content_type=file_item[\"mime\"],\n                    # Pass headers to fix bug in ayon-api (fixed in 1.2.15)\n                    headers={},\n                )\n\n        finally:\n            shutil.rmtree(tmp_dir)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/push_to_project/models/integrate.html#client.ayon_core.tools.push_to_project.models.integrate.ProjectPushItemStatus","title":"<code>ProjectPushItemStatus</code>","text":"Source code in <code>client/ayon_core/tools/push_to_project/models/integrate.py</code> <pre><code>class ProjectPushItemStatus:\n    def __init__(\n        self,\n        started=False,\n        failed=False,\n        finished=False,\n        fail_reason=None,\n        full_traceback=None\n    ):\n        self.started = started\n        self.failed = failed\n        self.finished = finished\n        self.fail_reason = fail_reason\n        self.full_traceback = full_traceback\n\n    def set_failed(self, fail_reason, exc_info=None):\n        \"\"\"Set status as failed.\n\n        Attribute 'fail_reason' can change automatically based on passed value.\n        Reason is unset if 'failed' is 'False' and is set do default reason if\n        is set to 'True' and reason is not set.\n\n        Args:\n            fail_reason (str): Reason why failed.\n            exc_info(tuple): Exception info.\n        \"\"\"\n\n        failed = True\n        if not fail_reason and not exc_info:\n            failed = False\n\n        full_traceback = None\n        if exc_info is not None:\n            full_traceback = \"\".join(traceback.format_exception(*exc_info))\n            if not fail_reason:\n                fail_reason = \"Failed without specified reason\"\n\n        self.failed = failed\n        self.fail_reason = fail_reason or None\n        self.full_traceback = full_traceback\n\n    def to_data(self):\n        return {\n            \"started\": self.started,\n            \"failed\": self.failed,\n            \"finished\": self.finished,\n            \"fail_reason\": self.fail_reason,\n            \"full_traceback\": self.full_traceback,\n        }\n\n    @classmethod\n    def from_data(cls, data):\n        return cls(**data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/push_to_project/models/integrate.html#client.ayon_core.tools.push_to_project.models.integrate.ProjectPushItemStatus.set_failed","title":"<code>set_failed(fail_reason, exc_info=None)</code>","text":"<p>Set status as failed.</p> <p>Attribute 'fail_reason' can change automatically based on passed value. Reason is unset if 'failed' is 'False' and is set do default reason if is set to 'True' and reason is not set.</p> <p>Parameters:</p> Name Type Description Default <code>fail_reason</code> <code>str</code> <p>Reason why failed.</p> required <code>exc_info</code> <code>tuple</code> <p>Exception info.</p> <code>None</code> Source code in <code>client/ayon_core/tools/push_to_project/models/integrate.py</code> <pre><code>def set_failed(self, fail_reason, exc_info=None):\n    \"\"\"Set status as failed.\n\n    Attribute 'fail_reason' can change automatically based on passed value.\n    Reason is unset if 'failed' is 'False' and is set do default reason if\n    is set to 'True' and reason is not set.\n\n    Args:\n        fail_reason (str): Reason why failed.\n        exc_info(tuple): Exception info.\n    \"\"\"\n\n    failed = True\n    if not fail_reason and not exc_info:\n        failed = False\n\n    full_traceback = None\n    if exc_info is not None:\n        full_traceback = \"\".join(traceback.format_exception(*exc_info))\n        if not fail_reason:\n            fail_reason = \"Failed without specified reason\"\n\n    self.failed = failed\n    self.fail_reason = fail_reason or None\n    self.full_traceback = full_traceback\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/push_to_project/models/integrate.html#client.ayon_core.tools.push_to_project.models.integrate.ProjectPushRepreItem","title":"<code>ProjectPushRepreItem</code>","text":"<p>Representation item.</p> <p>Representation item based on representation document and project roots.</p> <p>Representation document may have reference to: - source files: Files defined with publish template - resource files: Files that should be in publish directory     but filenames are not template based.</p> <p>Parameters:</p> Name Type Description Default <code>repre_entity</code> <code>dict[str, Ant]</code> <p>Representation entity.</p> required <code>roots</code> <code>dict[str, str]</code> <p>Project roots (based on project anatomy).</p> required Source code in <code>client/ayon_core/tools/push_to_project/models/integrate.py</code> <pre><code>class ProjectPushRepreItem:\n    \"\"\"Representation item.\n\n    Representation item based on representation document and project roots.\n\n    Representation document may have reference to:\n    - source files: Files defined with publish template\n    - resource files: Files that should be in publish directory\n        but filenames are not template based.\n\n    Args:\n        repre_entity (dict[str, Ant]): Representation entity.\n        roots (dict[str, str]): Project roots (based on project anatomy).\n    \"\"\"\n\n    def __init__(self, repre_entity, roots):\n        self._repre_entity = repre_entity\n        self._roots = roots\n        self._src_files = None\n        self._resource_files = None\n        self._frame = UNKNOWN\n\n    @property\n    def repre_entity(self):\n        return self._repre_entity\n\n    @property\n    def src_files(self):\n        if self._src_files is None:\n            self.get_source_files()\n        return self._src_files\n\n    @property\n    def resource_files(self):\n        if self._resource_files is None:\n            self.get_source_files()\n        return self._resource_files\n\n    @staticmethod\n    def _clean_path(path):\n        new_value = path.replace(\"\\\\\", \"/\")\n        while \"//\" in new_value:\n            new_value = new_value.replace(\"//\", \"/\")\n        return new_value\n\n    @staticmethod\n    def _get_relative_path(path, src_dirpath):\n        dirpath, basename = os.path.split(path)\n        if not dirpath.lower().startswith(src_dirpath.lower()):\n            return None\n\n        relative_dir = dirpath[len(src_dirpath):].lstrip(\"/\")\n        if relative_dir:\n            relative_path = \"/\".join([relative_dir, basename])\n        else:\n            relative_path = basename\n        return relative_path\n\n    @property\n    def frame(self):\n        \"\"\"First frame of representation files.\n\n        This value will be in representation document context if is sequence.\n\n        Returns:\n            Union[int, None]: First frame in representation files based on\n                source files or None if frame is not part of filename.\n        \"\"\"\n\n        if self._frame is UNKNOWN:\n            frame = None\n            for src_file in self.src_files:\n                src_frame = src_file.frame\n                if (\n                    src_frame is not None\n                    and (frame is None or src_frame &lt; frame)\n                ):\n                    frame = src_frame\n            self._frame = frame\n        return self._frame\n\n    @staticmethod\n    def validate_source_files(src_files, resource_files):\n        if not src_files:\n            raise AssertionError((\n                \"Couldn't figure out source files from representation.\"\n                \" Found resource files {}\"\n            ).format(\", \".join(str(i) for i in resource_files)))\n\n        invalid_items = [\n            item\n            for item in itertools.chain(src_files, resource_files)\n            if not item.is_valid_file\n        ]\n        if invalid_items:\n            raise AssertionError((\n                \"Source files that were not found on disk: {}\"\n            ).format(\", \".join(str(i) for i in invalid_items)))\n\n    def get_source_files(self):\n        if self._src_files is not None:\n            return self._src_files, self._resource_files\n\n        repre_context = self.repre_entity[\"context\"]\n        if \"frame\" in repre_context or \"udim\" in repre_context:\n            src_files, resource_files = self._get_source_files_with_frames()\n        else:\n            src_files, resource_files = self._get_source_files()\n\n        self.validate_source_files(src_files, resource_files)\n\n        self._src_files = src_files\n        self._resource_files = resource_files\n        return self._src_files, self._resource_files\n\n    def _get_source_files_with_frames(self):\n        frame_placeholder = \"__frame__\"\n        udim_placeholder = \"__udim__\"\n        src_files = []\n        resource_files = []\n        template = self.repre_entity[\"attrib\"][\"template\"]\n        # Remove padding from 'udim' and 'frame' formatting keys\n        # - \"{frame:0&gt;4}\" -&gt; \"{frame}\"\n        for key in (\"udim\", \"frame\"):\n            sub_part = \"{\" + key + \"[^}]*}\"\n            replacement = \"{{{}}}\".format(key)\n            template = re.sub(sub_part, replacement, template)\n\n        repre_context = self.repre_entity[\"context\"]\n        fill_repre_context = copy.deepcopy(repre_context)\n        if \"frame\" in fill_repre_context:\n            fill_repre_context[\"frame\"] = frame_placeholder\n\n        if \"udim\" in fill_repre_context:\n            fill_repre_context[\"udim\"] = udim_placeholder\n\n        fill_roots = fill_repre_context[\"root\"]\n        for root_name in tuple(fill_roots.keys()):\n            fill_roots[root_name] = \"{{root[{}]}}\".format(root_name)\n        repre_path = StringTemplate.format_template(\n            template, fill_repre_context)\n        repre_path = self._clean_path(repre_path)\n        src_dirpath, src_basename = os.path.split(repre_path)\n        src_basename = (\n            re.escape(src_basename)\n            .replace(frame_placeholder, \"(?P&lt;frame&gt;[0-9]+)\")\n            .replace(udim_placeholder, \"(?P&lt;udim&gt;[0-9]+)\")\n        )\n        src_basename_regex = re.compile(\"^{}$\".format(src_basename))\n        for file_info in self.repre_entity[\"files\"]:\n            filepath_template = self._clean_path(file_info[\"path\"])\n            filepath = self._clean_path(\n                filepath_template.format(root=self._roots)\n            )\n            dirpath, basename = os.path.split(filepath_template)\n            if (\n                dirpath.lower() != src_dirpath.lower()\n                or not src_basename_regex.match(basename)\n            ):\n                relative_path = self._get_relative_path(filepath, src_dirpath)\n                resource_files.append(ResourceFile(filepath, relative_path))\n                continue\n\n            frame = None\n            udim = None\n            for item in src_basename_regex.finditer(basename):\n                group_name = item.lastgroup\n                value = item.group(group_name)\n                if group_name == \"frame\":\n                    frame = int(value)\n                elif group_name == \"udim\":\n                    udim = value\n\n            src_files.append(SourceFile(filepath, frame, udim))\n\n        return src_files, resource_files\n\n    def _get_source_files(self):\n        src_files = []\n        resource_files = []\n        template = self.repre_entity[\"attrib\"][\"template\"]\n        repre_context = self.repre_entity[\"context\"]\n        fill_repre_context = copy.deepcopy(repre_context)\n        fill_roots = fill_repre_context[\"root\"]\n        for root_name in tuple(fill_roots.keys()):\n            fill_roots[root_name] = \"{{root[{}]}}\".format(root_name)\n        repre_path = StringTemplate.format_template(template,\n                                                    fill_repre_context)\n        repre_path = self._clean_path(repre_path)\n        src_dirpath = os.path.dirname(repre_path)\n        for file_info in self.repre_entity[\"files\"]:\n            filepath_template = self._clean_path(file_info[\"path\"])\n            filepath = self._clean_path(\n                filepath_template.format(root=self._roots))\n\n            if filepath_template.lower() == repre_path.lower():\n                src_files.append(\n                    SourceFile(repre_path.format(root=self._roots))\n                )\n            else:\n                relative_path = self._get_relative_path(\n                    filepath_template, src_dirpath\n                )\n                resource_files.append(\n                    ResourceFile(filepath, relative_path)\n                )\n        return src_files, resource_files\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/push_to_project/models/integrate.html#client.ayon_core.tools.push_to_project.models.integrate.ProjectPushRepreItem.frame","title":"<code>frame</code>  <code>property</code>","text":"<p>First frame of representation files.</p> <p>This value will be in representation document context if is sequence.</p> <p>Returns:</p> Type Description <p>Union[int, None]: First frame in representation files based on source files or None if frame is not part of filename.</p>"},{"location":"autoapi/client/ayon_core/tools/push_to_project/models/selection.html","title":"selection","text":""},{"location":"autoapi/client/ayon_core/tools/push_to_project/models/selection.html#client.ayon_core.tools.push_to_project.models.selection.PushToProjectSelectionModel","title":"<code>PushToProjectSelectionModel</code>","text":"<p>               Bases: <code>object</code></p> <p>Model handling selection changes.</p> <p>Triggering events: - \"selection.project.changed\" - \"selection.folder.changed\" - \"selection.task.changed\"</p> Source code in <code>client/ayon_core/tools/push_to_project/models/selection.py</code> <pre><code>class PushToProjectSelectionModel(object):\n    \"\"\"Model handling selection changes.\n\n    Triggering events:\n    - \"selection.project.changed\"\n    - \"selection.folder.changed\"\n    - \"selection.task.changed\"\n    \"\"\"\n\n    event_source = \"push-to-project.selection.model\"\n\n    def __init__(self, controller):\n        self._controller = controller\n\n        self._project_name = None\n        self._folder_id = None\n        self._task_name = None\n        self._task_id = None\n\n    def get_selected_project_name(self):\n        return self._project_name\n\n    def set_selected_project(self, project_name):\n        if project_name == self._project_name:\n            return\n\n        self._project_name = project_name\n        self._controller.emit_event(\n            \"selection.project.changed\",\n            {\"project_name\": project_name},\n            self.event_source\n        )\n\n    def get_selected_folder_id(self):\n        return self._folder_id\n\n    def set_selected_folder(self, folder_id):\n        if folder_id == self._folder_id:\n            return\n\n        self._folder_id = folder_id\n        self._controller.emit_event(\n            \"selection.folder.changed\",\n            {\n                \"project_name\": self._project_name,\n                \"folder_id\": folder_id,\n            },\n            self.event_source\n        )\n\n    def get_selected_task_name(self):\n        return self._task_name\n\n    def get_selected_task_id(self):\n        return self._task_id\n\n    def set_selected_task(self, task_id, task_name):\n        if task_id == self._task_id:\n            return\n\n        self._task_name = task_name\n        self._task_id = task_id\n        self._controller.emit_event(\n            \"selection.task.changed\",\n            {\n                \"project_name\": self._project_name,\n                \"folder_id\": self._folder_id,\n                \"task_name\": task_name,\n                \"task_id\": task_id,\n            },\n            self.event_source\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/push_to_project/models/user_values.html","title":"user_values","text":""},{"location":"autoapi/client/ayon_core/tools/push_to_project/models/user_values.html#client.ayon_core.tools.push_to_project.models.user_values.UserPublishValuesModel","title":"<code>UserPublishValuesModel</code>","text":"<p>Helper object to validate values required for push to different project.</p> <p>Parameters:</p> Name Type Description Default <code>controller</code> <code>PushToContextController</code> <p>Event system to catch and emit events.</p> required Source code in <code>client/ayon_core/tools/push_to_project/models/user_values.py</code> <pre><code>class UserPublishValuesModel:\n    \"\"\"Helper object to validate values required for push to different project.\n\n    Args:\n        controller (PushToContextController): Event system to catch\n            and emit events.\n    \"\"\"\n\n    folder_name_regex = re.compile(\"^[a-zA-Z0-9_.]+$\")\n    variant_regex = re.compile(\"^[{}]+$\".format(PRODUCT_NAME_ALLOWED_SYMBOLS))\n\n    def __init__(self, controller):\n        self._controller = controller\n        self._new_folder_name = None\n        self._variant = None\n        self._comment = None\n        self._is_variant_valid = False\n        self._is_new_folder_name_valid = False\n\n        self.set_new_folder_name(\"\")\n        self.set_variant(\"\")\n        self.set_comment(\"\")\n\n    @property\n    def new_folder_name(self):\n        return self._new_folder_name\n\n    @property\n    def variant(self):\n        return self._variant\n\n    @property\n    def comment(self):\n        return self._comment\n\n    @property\n    def is_variant_valid(self):\n        return self._is_variant_valid\n\n    @property\n    def is_new_folder_name_valid(self):\n        return self._is_new_folder_name_valid\n\n    @property\n    def is_valid(self):\n        return self.is_variant_valid and self.is_new_folder_name_valid\n\n    def get_data(self):\n        return {\n            \"new_folder_name\": self._new_folder_name,\n            \"variant\": self._variant,\n            \"comment\": self._comment,\n            \"is_variant_valid\": self._is_variant_valid,\n            \"is_new_folder_name_valid\": self._is_new_folder_name_valid,\n            \"is_valid\": self.is_valid\n        }\n\n    def set_variant(self, variant):\n        if variant == self._variant:\n            return\n\n        self._variant = variant\n        is_valid = False\n        if variant:\n            is_valid = self.variant_regex.match(variant) is not None\n        self._is_variant_valid = is_valid\n\n        self._controller.emit_event(\n            \"variant.changed\",\n            {\n                \"variant\": variant,\n                \"is_valid\": self._is_variant_valid,\n            },\n            \"user_values\"\n        )\n\n    def set_new_folder_name(self, folder_name):\n        if self._new_folder_name == folder_name:\n            return\n\n        self._new_folder_name = folder_name\n        if folder_name is None:\n            is_valid = True\n        elif not folder_name:\n            is_valid = False\n        else:\n            is_valid = (\n                self.folder_name_regex.match(folder_name) is not None\n            )\n        self._is_new_folder_name_valid = is_valid\n        self._controller.emit_event(\n            \"new_folder_name.changed\",\n            {\n                \"new_folder_name\": self._new_folder_name,\n                \"is_valid\": self._is_new_folder_name_valid,\n            },\n            \"user_values\"\n        )\n\n    def set_comment(self, comment):\n        if comment == self._comment:\n            return\n        self._comment = comment\n        self._controller.emit_event(\n            \"comment.changed\",\n            {\"comment\": comment},\n            \"user_values\"\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/push_to_project/ui/index.html","title":"ui","text":""},{"location":"autoapi/client/ayon_core/tools/push_to_project/ui/index.html#client.ayon_core.tools.push_to_project.ui.PushToContextSelectWindow","title":"<code>PushToContextSelectWindow</code>","text":"<p>               Bases: <code>QWidget</code></p> Source code in <code>client/ayon_core/tools/push_to_project/ui/window.py</code> <pre><code>class PushToContextSelectWindow(QtWidgets.QWidget):\n    def __init__(self, controller=None):\n        super(PushToContextSelectWindow, self).__init__()\n        if controller is None:\n            controller = PushToContextController()\n        self._controller = controller\n\n        self.setWindowTitle(\"Push to project (select context)\")\n        self.setWindowIcon(QtGui.QIcon(get_app_icon_path()))\n\n        main_context_widget = QtWidgets.QWidget(self)\n\n        header_widget = QtWidgets.QWidget(main_context_widget)\n\n        library_only_label = QtWidgets.QLabel(\n            \"Show only libraries\",\n            header_widget\n        )\n        library_only_checkbox = NiceCheckbox(\n            True, parent=header_widget)\n\n        header_label = QtWidgets.QLabel(\n            controller.get_source_label(),\n            header_widget\n        )\n\n        header_layout = QtWidgets.QHBoxLayout(header_widget)\n        header_layout.setContentsMargins(0, 0, 0, 0)\n        header_layout.addWidget(header_label, 1)\n        header_layout.addWidget(library_only_label, 0)\n        header_layout.addWidget(library_only_checkbox, 0)\n\n        main_splitter = QtWidgets.QSplitter(\n            QtCore.Qt.Horizontal, main_context_widget\n        )\n\n        context_widget = QtWidgets.QWidget(main_splitter)\n\n        projects_combobox = ProjectsCombobox(controller, context_widget)\n        projects_combobox.set_select_item_visible(True)\n        projects_combobox.set_standard_filter_enabled(True)\n\n        context_splitter = QtWidgets.QSplitter(\n            QtCore.Qt.Vertical, context_widget\n        )\n\n        folders_widget = FoldersWidget(controller, context_splitter)\n        folders_widget.set_deselectable(True)\n        tasks_widget = TasksWidget(controller, context_splitter)\n\n        context_splitter.addWidget(folders_widget)\n        context_splitter.addWidget(tasks_widget)\n\n        context_layout = QtWidgets.QVBoxLayout(context_widget)\n        context_layout.setContentsMargins(0, 0, 0, 0)\n        context_layout.addWidget(projects_combobox, 0)\n        context_layout.addWidget(context_splitter, 1)\n\n        # --- Inputs widget ---\n        inputs_widget = QtWidgets.QWidget(main_splitter)\n\n        new_folder_checkbox = NiceCheckbox(True, parent=inputs_widget)\n        original_names_checkbox = NiceCheckbox(False, parent=inputs_widget)\n\n        folder_name_input = PlaceholderLineEdit(inputs_widget)\n        folder_name_input.setPlaceholderText(\"&lt; Name of new folder &gt;\")\n        folder_name_input.setObjectName(\"ValidatedLineEdit\")\n        folder_name_input.setEnabled(new_folder_checkbox.isChecked())\n\n        variant_input = PlaceholderLineEdit(inputs_widget)\n        variant_input.setPlaceholderText(\"&lt; Variant &gt;\")\n        variant_input.setObjectName(\"ValidatedLineEdit\")\n\n        version_up_checkbox = NiceCheckbox(True, parent=inputs_widget)\n\n        comment_input = PlaceholderLineEdit(inputs_widget)\n        comment_input.setPlaceholderText(\"&lt; Publish comment &gt;\")\n\n        inputs_layout = QtWidgets.QFormLayout(inputs_widget)\n        inputs_layout.setContentsMargins(0, 0, 0, 0)\n        inputs_layout.addRow(\"Create new folder\", new_folder_checkbox)\n        inputs_layout.addRow(\"New folder name\", folder_name_input)\n        inputs_layout.addRow(\"Variant\", variant_input)\n        inputs_layout.addRow(\n            \"Use original product names\", original_names_checkbox\n        )\n        inputs_layout.addRow(\n            \"Version up existing Product\", version_up_checkbox\n        )\n        inputs_layout.addRow(\"Comment\", comment_input)\n\n        main_splitter.addWidget(context_widget)\n        main_splitter.addWidget(inputs_widget)\n\n        # --- Buttons widget ---\n        btns_widget = QtWidgets.QWidget(self)\n        cancel_btn = QtWidgets.QPushButton(\"Cancel\", btns_widget)\n        publish_btn = QtWidgets.QPushButton(\"Publish\", btns_widget)\n\n        btns_layout = QtWidgets.QHBoxLayout(btns_widget)\n        btns_layout.setContentsMargins(0, 0, 0, 0)\n        btns_layout.addStretch(1)\n        btns_layout.addWidget(cancel_btn, 0)\n        btns_layout.addWidget(publish_btn, 0)\n\n        sep_1 = SeparatorWidget(parent=main_context_widget)\n        sep_2 = SeparatorWidget(parent=main_context_widget)\n        main_context_layout = QtWidgets.QVBoxLayout(main_context_widget)\n        main_context_layout.addWidget(header_widget, 0)\n        main_context_layout.addWidget(sep_1, 0)\n        main_context_layout.addWidget(main_splitter, 1)\n        main_context_layout.addWidget(sep_2, 0)\n        main_context_layout.addWidget(btns_widget, 0)\n\n        # NOTE This was added in hurry\n        # - should be reorganized and changed styles\n        overlay_widget = QtWidgets.QFrame(self)\n        overlay_widget.setObjectName(\"OverlayFrame\")\n\n        overlay_label = QtWidgets.QLabel(overlay_widget)\n        overlay_label.setAlignment(QtCore.Qt.AlignCenter)\n        overlay_label.setWordWrap(True)\n        overlay_label.setTextInteractionFlags(\n            QtCore.Qt.TextBrowserInteraction\n        )\n\n        overlay_btns_widget = QtWidgets.QWidget(overlay_widget)\n        overlay_btns_widget.setAttribute(QtCore.Qt.WA_TranslucentBackground)\n\n        # Add try again button (requires changes in controller)\n        overlay_try_btn = QtWidgets.QPushButton(\n            \"Try again\", overlay_btns_widget\n        )\n        overlay_try_btn.setToolTip(\n            \"Hide overlay and modify submit information.\"\n        )\n\n        show_detail_btn = QtWidgets.QPushButton(\n            \"Show error detail\", overlay_btns_widget\n        )\n        show_detail_btn.setToolTip(\n            \"Show error detail dialog to copy full error.\"\n        )\n        original_names_checkbox.setToolTip(\n            \"Required for multi copy, doesn't allow changes variant values.\"\n        )\n        version_up_checkbox.setToolTip(\n            \"Version up existing product. If not selected version will be \"\n            \"updated.\"\n        )\n\n        overlay_close_btn = QtWidgets.QPushButton(\n            \"Close\", overlay_btns_widget\n        )\n        overlay_close_btn.setToolTip(\"Discard changes and close window.\")\n\n        overlay_btns_layout = QtWidgets.QHBoxLayout(overlay_btns_widget)\n        overlay_btns_layout.setContentsMargins(0, 0, 0, 0)\n        overlay_btns_layout.setSpacing(10)\n        overlay_btns_layout.addStretch(1)\n        overlay_btns_layout.addWidget(overlay_try_btn, 0)\n        overlay_btns_layout.addWidget(show_detail_btn, 0)\n        overlay_btns_layout.addWidget(overlay_close_btn, 0)\n        overlay_btns_layout.addStretch(1)\n\n        overlay_layout = QtWidgets.QVBoxLayout(overlay_widget)\n        overlay_layout.addWidget(overlay_label, 0)\n        overlay_layout.addWidget(overlay_btns_widget, 0)\n        overlay_layout.setAlignment(QtCore.Qt.AlignCenter)\n\n        main_layout = QtWidgets.QStackedLayout(self)\n        main_layout.setContentsMargins(0, 0, 0, 0)\n        main_layout.addWidget(main_context_widget)\n        main_layout.addWidget(overlay_widget)\n        main_layout.setStackingMode(QtWidgets.QStackedLayout.StackAll)\n        main_layout.setCurrentWidget(main_context_widget)\n\n        show_timer = QtCore.QTimer()\n        show_timer.setInterval(0)\n\n        main_thread_timer = QtCore.QTimer()\n        main_thread_timer.setInterval(10)\n\n        user_input_changed_timer = QtCore.QTimer()\n        user_input_changed_timer.setInterval(200)\n        user_input_changed_timer.setSingleShot(True)\n\n        main_thread_timer.timeout.connect(self._on_main_thread_timer)\n        show_timer.timeout.connect(self._on_show_timer)\n        user_input_changed_timer.timeout.connect(self._on_user_input_timer)\n        new_folder_checkbox.stateChanged.connect(self._on_new_folder_check)\n        folder_name_input.textChanged.connect(self._on_new_folder_change)\n        variant_input.textChanged.connect(self._on_variant_change)\n        comment_input.textChanged.connect(self._on_comment_change)\n        library_only_checkbox.stateChanged.connect(self._on_library_only_change)\n        original_names_checkbox.stateChanged.connect(\n            self._on_original_names_change)\n        version_up_checkbox.stateChanged.connect(\n            self._on_version_up_checkbox_change)\n\n        publish_btn.clicked.connect(self._on_select_click)\n        cancel_btn.clicked.connect(self._on_close_click)\n        show_detail_btn.clicked.connect(self._on_show_detail_click)\n        overlay_close_btn.clicked.connect(self._on_close_click)\n        overlay_try_btn.clicked.connect(self._on_try_again_click)\n\n        controller.register_event_callback(\n            \"new_folder_name.changed\",\n            self._on_controller_new_folder_change\n        )\n        controller.register_event_callback(\n            \"variant.changed\", self._on_controller_variant_change\n        )\n        controller.register_event_callback(\n            \"comment.changed\", self._on_controller_comment_change\n        )\n        controller.register_event_callback(\n            \"submission.enabled.changed\", self._on_submission_change\n        )\n        controller.register_event_callback(\n            \"source.changed\", self._on_controller_source_change\n        )\n        controller.register_event_callback(\n            \"submit.started\", self._on_controller_submit_start\n        )\n        controller.register_event_callback(\n            \"submit.finished\", self._on_controller_submit_end\n        )\n        controller.register_event_callback(\n            \"push.message.added\", self._on_push_message\n        )\n\n        self._main_layout = main_layout\n\n        self._main_context_widget = main_context_widget\n\n        self._header_label = header_label\n        self._main_splitter = main_splitter\n\n        self._projects_combobox = projects_combobox\n        self._folders_widget = folders_widget\n        self._tasks_widget = tasks_widget\n\n        self._variant_input = variant_input\n        self._new_folder_checkbox = new_folder_checkbox\n        self._folder_name_input = folder_name_input\n        self._comment_input = comment_input\n        self._use_original_names_checkbox = original_names_checkbox\n        self._library_only_checkbox = library_only_checkbox\n\n        self._publish_btn = publish_btn\n\n        self._overlay_widget = overlay_widget\n        self._show_detail_btn = show_detail_btn\n        self._overlay_close_btn = overlay_close_btn\n        self._overlay_try_btn = overlay_try_btn\n        self._overlay_label = overlay_label\n\n        self._error_detail_dialog = ErrorDetailDialog(self)\n\n        self._user_input_changed_timer = user_input_changed_timer\n        # Store current value on input text change\n        #   The value is unset when is passed to controller\n        # The goal is to have controll over changes happened during user change\n        #   in UI and controller auto-changes\n        self._new_folder_name_enabled = None\n        self._new_folder_name_input_text = None\n        self._variant_input_text = None\n        self._comment_input_text = None\n        self._version_up_checkbox = version_up_checkbox\n\n        self._first_show = True\n        self._show_timer = show_timer\n        self._show_counter = 0\n\n        self._main_thread_timer = main_thread_timer\n        self._main_thread_timer_can_stop = True\n        self._last_submit_message = None\n\n        self._variant_is_valid = None\n        self._folder_is_valid = None\n\n        publish_btn.setEnabled(False)\n        show_detail_btn.setVisible(False)\n        overlay_close_btn.setVisible(False)\n        overlay_try_btn.setVisible(False)\n        version_up_checkbox.setChecked(False)\n\n    # Support of public api function of controller\n    def set_source(self, project_name, version_ids):\n        \"\"\"Set source project and version.\n\n        Call the method on controller.\n\n        Args:\n            project_name (Union[str, None]): Name of project.\n            version_ids (Union[str, None]): comma separated Version ids.\n        \"\"\"\n\n        self._controller.set_source(project_name, version_ids)\n\n    def showEvent(self, event):\n        super(PushToContextSelectWindow, self).showEvent(event)\n        if self._first_show:\n            self._first_show = False\n            self._on_first_show()\n\n    def refresh(self):\n        user_values = self._controller.get_user_values()\n        new_folder_name = user_values[\"new_folder_name\"]\n        variant = user_values[\"variant\"]\n        self._folder_name_input.setText(new_folder_name or \"\")\n        self._variant_input.setText(variant or \"\")\n        self._invalidate_variant(user_values[\"is_variant_valid\"])\n        self._invalidate_use_original_names(\n            self._use_original_names_checkbox.isChecked())\n        self._invalidate_new_folder_name(\n            new_folder_name, user_values[\"is_new_folder_name_valid\"]\n        )\n        self._projects_combobox.refresh()\n\n    def _on_first_show(self):\n        width = 740\n        height = 640\n        inputs_width = 360\n        self.setStyleSheet(load_stylesheet())\n        self.resize(width, height)\n        self._main_splitter.setSizes([width - inputs_width, inputs_width])\n        self._show_timer.start()\n\n    def _on_show_timer(self):\n        if self._show_counter &lt; 3:\n            self._show_counter += 1\n            return\n        self._show_timer.stop()\n\n        self._show_counter = 0\n\n        self.refresh()\n\n    def _on_new_folder_check(self):\n        self._new_folder_name_enabled = self._new_folder_checkbox.isChecked()\n        self._folder_name_input.setEnabled(self._new_folder_name_enabled)\n        self._user_input_changed_timer.start()\n\n    def _on_new_folder_change(self, text):\n        self._new_folder_name_input_text = text\n        self._user_input_changed_timer.start()\n\n    def _on_variant_change(self, text):\n        self._variant_input_text = text\n        self._user_input_changed_timer.start()\n\n    def _on_comment_change(self, text):\n        self._comment_input_text = text\n        self._user_input_changed_timer.start()\n\n    def _on_library_only_change(self) -&gt; None:\n        \"\"\"Change toggle state, reset filter, recalculate dropdown\"\"\"\n        is_checked = self._library_only_checkbox.isChecked()\n        self._projects_combobox.set_standard_filter_enabled(is_checked)\n\n    def _on_original_names_change(self) -&gt; None:\n        is_checked = self._use_original_names_checkbox.isChecked()\n        self._invalidate_use_original_names(is_checked)\n\n    def _on_version_up_checkbox_change(self) -&gt; None:\n        is_checked = self._version_up_checkbox.isChecked()\n        self._controller.set_version_up(is_checked)\n\n    def _on_user_input_timer(self):\n        folder_name_enabled = self._new_folder_name_enabled\n        folder_name = self._new_folder_name_input_text\n        if folder_name is not None or folder_name_enabled is not None:\n            self._new_folder_name_input_text = None\n            self._new_folder_name_enabled = None\n            if not self._new_folder_checkbox.isChecked():\n                folder_name = None\n            elif folder_name is None:\n                folder_name = self._folder_name_input.text()\n            self._controller.set_user_value_folder_name(folder_name)\n\n        variant = self._variant_input_text\n        if variant is not None:\n            self._variant_input_text = None\n            self._controller.set_user_value_variant(variant)\n\n        comment = self._comment_input_text\n        if comment is not None:\n            self._comment_input_text = None\n            self._controller.set_user_value_comment(comment)\n\n    def _on_controller_new_folder_change(self, event):\n        folder_name = event[\"new_folder_name\"]\n        if (\n            self._new_folder_name_input_text is None\n            and folder_name != self._folder_name_input.text()\n        ):\n            self._folder_name_input.setText(folder_name)\n\n        self._invalidate_new_folder_name(folder_name, event[\"is_valid\"])\n\n    def _on_controller_variant_change(self, event):\n        is_valid = event[\"is_valid\"]\n        variant = event[\"variant\"]\n        if (\n            self._variant_input_text is None\n            and variant != self._variant_input.text()\n        ):\n            self._variant_input.setText(variant)\n\n        self._invalidate_variant(is_valid)\n\n    def _on_controller_comment_change(self, event):\n        comment = event[\"comment\"]\n        if (\n            self._comment_input_text is None\n            and comment != self._comment_input.text()\n        ):\n            self._comment_input.setText(comment)\n\n    def _on_controller_source_change(self):\n        self._header_label.setText(self._controller.get_source_label())\n\n    def _invalidate_new_folder_name(self, folder_name, is_valid):\n        self._tasks_widget.setVisible(folder_name is None)\n        if self._folder_is_valid is is_valid:\n            return\n        self._folder_is_valid = is_valid\n        state = \"\"\n        if folder_name is not None:\n            state = \"valid\" if is_valid else \"invalid\"\n        set_style_property(self._folder_name_input, \"state\", state)\n\n    def _invalidate_variant(self, is_valid):\n        self._variant_is_valid = is_valid\n        state = \"valid\" if is_valid else \"invalid\"\n        set_style_property(self._variant_input, \"state\", state)\n\n    def _invalidate_use_original_names(self, use_original_names):\n        \"\"\"Checks if original names must be used.\n\n        Invalidates Variant if necessary\n        \"\"\"\n        if self._controller.original_names_required():\n            use_original_names = True\n\n        self._variant_input.setEnabled(not use_original_names)\n        self._invalidate_variant(not use_original_names)\n\n        self._controller._use_original_name = use_original_names\n        self._use_original_names_checkbox.setChecked(use_original_names)\n\n    def _on_submission_change(self, event):\n        self._publish_btn.setEnabled(event[\"enabled\"])\n\n    def _on_show_detail_click(self):\n        self._error_detail_dialog.show()\n\n    def _on_close_click(self):\n        self.close()\n\n    def _on_select_click(self):\n        self._process_item_id = self._controller.submit(wait=False)\n\n    def _on_try_again_click(self):\n        self._process_item_id = None\n        self._last_submit_message = None\n\n        self._error_detail_dialog.close()\n\n        self._overlay_close_btn.setVisible(False)\n        self._overlay_try_btn.setVisible(False)\n        self._show_detail_btn.setVisible(False)\n        self._main_layout.setCurrentWidget(self._main_context_widget)\n\n    def _on_main_thread_timer(self):\n        if self._last_submit_message:\n            self._overlay_label.setText(self._last_submit_message)\n            self._last_submit_message = None\n\n        failed_pushes = []\n        fail_tracebacks = []\n        for process_item in self._controller.get_process_items().values():\n            process_status = process_item.get_status_data()\n            if process_status[\"failed\"]:\n                failed_pushes.append(process_status)\n        # push_failed = process_status[\"failed\"]\n        # fail_traceback = process_status[\"full_traceback\"]\n        if self._main_thread_timer_can_stop:\n            self._main_thread_timer.stop()\n            self._overlay_close_btn.setVisible(True)\n            if failed_pushes:\n                self._overlay_try_btn.setVisible(True)\n                fail_tracebacks = [\n                    process_status[\"full_traceback\"]\n                    for process_status in failed_pushes\n                    if process_status[\"full_traceback\"]\n                ]\n                if fail_tracebacks:\n                    self._show_detail_btn.setVisible(True)\n\n        if failed_pushes:\n            reasons = [\n                process_status[\"fail_reason\"]\n                for process_status in failed_pushes\n            ]\n            if fail_tracebacks:\n                reason = \"\\n\".join(reasons)\n                message = (\n                    \"Unhandled error happened.\"\n                    \" Check error detail for more information.\"\n                )\n                self._error_detail_dialog.set_detail(\n                    reason, \"\\n\".join(fail_tracebacks)\n                )\n            else:\n                message = f\"Push Failed:\\n{reasons}\"\n\n            self._overlay_label.setText(message)\n            set_style_property(self._overlay_close_btn, \"state\", \"error\")\n\n        if self._main_thread_timer_can_stop:\n            # Join thread in controller\n            self._controller.wait_for_process_thread()\n            # Reset process item to None\n            self._process_item_id = None\n\n    def _on_controller_submit_start(self):\n        self._main_thread_timer_can_stop = False\n        self._main_thread_timer.start()\n        self._main_layout.setCurrentWidget(self._overlay_widget)\n        self._overlay_label.setText(\"Submission started\")\n\n    def _on_controller_submit_end(self):\n        self._main_thread_timer_can_stop = True\n\n    def _on_push_message(self, event):\n        self._last_submit_message = event[\"message\"]\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/push_to_project/ui/index.html#client.ayon_core.tools.push_to_project.ui.PushToContextSelectWindow.set_source","title":"<code>set_source(project_name, version_ids)</code>","text":"<p>Set source project and version.</p> <p>Call the method on controller.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>Union[str, None]</code> <p>Name of project.</p> required <code>version_ids</code> <code>Union[str, None]</code> <p>comma separated Version ids.</p> required Source code in <code>client/ayon_core/tools/push_to_project/ui/window.py</code> <pre><code>def set_source(self, project_name, version_ids):\n    \"\"\"Set source project and version.\n\n    Call the method on controller.\n\n    Args:\n        project_name (Union[str, None]): Name of project.\n        version_ids (Union[str, None]): comma separated Version ids.\n    \"\"\"\n\n    self._controller.set_source(project_name, version_ids)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/push_to_project/ui/window.html","title":"window","text":""},{"location":"autoapi/client/ayon_core/tools/push_to_project/ui/window.html#client.ayon_core.tools.push_to_project.ui.window.PushToContextSelectWindow","title":"<code>PushToContextSelectWindow</code>","text":"<p>               Bases: <code>QWidget</code></p> Source code in <code>client/ayon_core/tools/push_to_project/ui/window.py</code> <pre><code>class PushToContextSelectWindow(QtWidgets.QWidget):\n    def __init__(self, controller=None):\n        super(PushToContextSelectWindow, self).__init__()\n        if controller is None:\n            controller = PushToContextController()\n        self._controller = controller\n\n        self.setWindowTitle(\"Push to project (select context)\")\n        self.setWindowIcon(QtGui.QIcon(get_app_icon_path()))\n\n        main_context_widget = QtWidgets.QWidget(self)\n\n        header_widget = QtWidgets.QWidget(main_context_widget)\n\n        library_only_label = QtWidgets.QLabel(\n            \"Show only libraries\",\n            header_widget\n        )\n        library_only_checkbox = NiceCheckbox(\n            True, parent=header_widget)\n\n        header_label = QtWidgets.QLabel(\n            controller.get_source_label(),\n            header_widget\n        )\n\n        header_layout = QtWidgets.QHBoxLayout(header_widget)\n        header_layout.setContentsMargins(0, 0, 0, 0)\n        header_layout.addWidget(header_label, 1)\n        header_layout.addWidget(library_only_label, 0)\n        header_layout.addWidget(library_only_checkbox, 0)\n\n        main_splitter = QtWidgets.QSplitter(\n            QtCore.Qt.Horizontal, main_context_widget\n        )\n\n        context_widget = QtWidgets.QWidget(main_splitter)\n\n        projects_combobox = ProjectsCombobox(controller, context_widget)\n        projects_combobox.set_select_item_visible(True)\n        projects_combobox.set_standard_filter_enabled(True)\n\n        context_splitter = QtWidgets.QSplitter(\n            QtCore.Qt.Vertical, context_widget\n        )\n\n        folders_widget = FoldersWidget(controller, context_splitter)\n        folders_widget.set_deselectable(True)\n        tasks_widget = TasksWidget(controller, context_splitter)\n\n        context_splitter.addWidget(folders_widget)\n        context_splitter.addWidget(tasks_widget)\n\n        context_layout = QtWidgets.QVBoxLayout(context_widget)\n        context_layout.setContentsMargins(0, 0, 0, 0)\n        context_layout.addWidget(projects_combobox, 0)\n        context_layout.addWidget(context_splitter, 1)\n\n        # --- Inputs widget ---\n        inputs_widget = QtWidgets.QWidget(main_splitter)\n\n        new_folder_checkbox = NiceCheckbox(True, parent=inputs_widget)\n        original_names_checkbox = NiceCheckbox(False, parent=inputs_widget)\n\n        folder_name_input = PlaceholderLineEdit(inputs_widget)\n        folder_name_input.setPlaceholderText(\"&lt; Name of new folder &gt;\")\n        folder_name_input.setObjectName(\"ValidatedLineEdit\")\n        folder_name_input.setEnabled(new_folder_checkbox.isChecked())\n\n        variant_input = PlaceholderLineEdit(inputs_widget)\n        variant_input.setPlaceholderText(\"&lt; Variant &gt;\")\n        variant_input.setObjectName(\"ValidatedLineEdit\")\n\n        version_up_checkbox = NiceCheckbox(True, parent=inputs_widget)\n\n        comment_input = PlaceholderLineEdit(inputs_widget)\n        comment_input.setPlaceholderText(\"&lt; Publish comment &gt;\")\n\n        inputs_layout = QtWidgets.QFormLayout(inputs_widget)\n        inputs_layout.setContentsMargins(0, 0, 0, 0)\n        inputs_layout.addRow(\"Create new folder\", new_folder_checkbox)\n        inputs_layout.addRow(\"New folder name\", folder_name_input)\n        inputs_layout.addRow(\"Variant\", variant_input)\n        inputs_layout.addRow(\n            \"Use original product names\", original_names_checkbox\n        )\n        inputs_layout.addRow(\n            \"Version up existing Product\", version_up_checkbox\n        )\n        inputs_layout.addRow(\"Comment\", comment_input)\n\n        main_splitter.addWidget(context_widget)\n        main_splitter.addWidget(inputs_widget)\n\n        # --- Buttons widget ---\n        btns_widget = QtWidgets.QWidget(self)\n        cancel_btn = QtWidgets.QPushButton(\"Cancel\", btns_widget)\n        publish_btn = QtWidgets.QPushButton(\"Publish\", btns_widget)\n\n        btns_layout = QtWidgets.QHBoxLayout(btns_widget)\n        btns_layout.setContentsMargins(0, 0, 0, 0)\n        btns_layout.addStretch(1)\n        btns_layout.addWidget(cancel_btn, 0)\n        btns_layout.addWidget(publish_btn, 0)\n\n        sep_1 = SeparatorWidget(parent=main_context_widget)\n        sep_2 = SeparatorWidget(parent=main_context_widget)\n        main_context_layout = QtWidgets.QVBoxLayout(main_context_widget)\n        main_context_layout.addWidget(header_widget, 0)\n        main_context_layout.addWidget(sep_1, 0)\n        main_context_layout.addWidget(main_splitter, 1)\n        main_context_layout.addWidget(sep_2, 0)\n        main_context_layout.addWidget(btns_widget, 0)\n\n        # NOTE This was added in hurry\n        # - should be reorganized and changed styles\n        overlay_widget = QtWidgets.QFrame(self)\n        overlay_widget.setObjectName(\"OverlayFrame\")\n\n        overlay_label = QtWidgets.QLabel(overlay_widget)\n        overlay_label.setAlignment(QtCore.Qt.AlignCenter)\n        overlay_label.setWordWrap(True)\n        overlay_label.setTextInteractionFlags(\n            QtCore.Qt.TextBrowserInteraction\n        )\n\n        overlay_btns_widget = QtWidgets.QWidget(overlay_widget)\n        overlay_btns_widget.setAttribute(QtCore.Qt.WA_TranslucentBackground)\n\n        # Add try again button (requires changes in controller)\n        overlay_try_btn = QtWidgets.QPushButton(\n            \"Try again\", overlay_btns_widget\n        )\n        overlay_try_btn.setToolTip(\n            \"Hide overlay and modify submit information.\"\n        )\n\n        show_detail_btn = QtWidgets.QPushButton(\n            \"Show error detail\", overlay_btns_widget\n        )\n        show_detail_btn.setToolTip(\n            \"Show error detail dialog to copy full error.\"\n        )\n        original_names_checkbox.setToolTip(\n            \"Required for multi copy, doesn't allow changes variant values.\"\n        )\n        version_up_checkbox.setToolTip(\n            \"Version up existing product. If not selected version will be \"\n            \"updated.\"\n        )\n\n        overlay_close_btn = QtWidgets.QPushButton(\n            \"Close\", overlay_btns_widget\n        )\n        overlay_close_btn.setToolTip(\"Discard changes and close window.\")\n\n        overlay_btns_layout = QtWidgets.QHBoxLayout(overlay_btns_widget)\n        overlay_btns_layout.setContentsMargins(0, 0, 0, 0)\n        overlay_btns_layout.setSpacing(10)\n        overlay_btns_layout.addStretch(1)\n        overlay_btns_layout.addWidget(overlay_try_btn, 0)\n        overlay_btns_layout.addWidget(show_detail_btn, 0)\n        overlay_btns_layout.addWidget(overlay_close_btn, 0)\n        overlay_btns_layout.addStretch(1)\n\n        overlay_layout = QtWidgets.QVBoxLayout(overlay_widget)\n        overlay_layout.addWidget(overlay_label, 0)\n        overlay_layout.addWidget(overlay_btns_widget, 0)\n        overlay_layout.setAlignment(QtCore.Qt.AlignCenter)\n\n        main_layout = QtWidgets.QStackedLayout(self)\n        main_layout.setContentsMargins(0, 0, 0, 0)\n        main_layout.addWidget(main_context_widget)\n        main_layout.addWidget(overlay_widget)\n        main_layout.setStackingMode(QtWidgets.QStackedLayout.StackAll)\n        main_layout.setCurrentWidget(main_context_widget)\n\n        show_timer = QtCore.QTimer()\n        show_timer.setInterval(0)\n\n        main_thread_timer = QtCore.QTimer()\n        main_thread_timer.setInterval(10)\n\n        user_input_changed_timer = QtCore.QTimer()\n        user_input_changed_timer.setInterval(200)\n        user_input_changed_timer.setSingleShot(True)\n\n        main_thread_timer.timeout.connect(self._on_main_thread_timer)\n        show_timer.timeout.connect(self._on_show_timer)\n        user_input_changed_timer.timeout.connect(self._on_user_input_timer)\n        new_folder_checkbox.stateChanged.connect(self._on_new_folder_check)\n        folder_name_input.textChanged.connect(self._on_new_folder_change)\n        variant_input.textChanged.connect(self._on_variant_change)\n        comment_input.textChanged.connect(self._on_comment_change)\n        library_only_checkbox.stateChanged.connect(self._on_library_only_change)\n        original_names_checkbox.stateChanged.connect(\n            self._on_original_names_change)\n        version_up_checkbox.stateChanged.connect(\n            self._on_version_up_checkbox_change)\n\n        publish_btn.clicked.connect(self._on_select_click)\n        cancel_btn.clicked.connect(self._on_close_click)\n        show_detail_btn.clicked.connect(self._on_show_detail_click)\n        overlay_close_btn.clicked.connect(self._on_close_click)\n        overlay_try_btn.clicked.connect(self._on_try_again_click)\n\n        controller.register_event_callback(\n            \"new_folder_name.changed\",\n            self._on_controller_new_folder_change\n        )\n        controller.register_event_callback(\n            \"variant.changed\", self._on_controller_variant_change\n        )\n        controller.register_event_callback(\n            \"comment.changed\", self._on_controller_comment_change\n        )\n        controller.register_event_callback(\n            \"submission.enabled.changed\", self._on_submission_change\n        )\n        controller.register_event_callback(\n            \"source.changed\", self._on_controller_source_change\n        )\n        controller.register_event_callback(\n            \"submit.started\", self._on_controller_submit_start\n        )\n        controller.register_event_callback(\n            \"submit.finished\", self._on_controller_submit_end\n        )\n        controller.register_event_callback(\n            \"push.message.added\", self._on_push_message\n        )\n\n        self._main_layout = main_layout\n\n        self._main_context_widget = main_context_widget\n\n        self._header_label = header_label\n        self._main_splitter = main_splitter\n\n        self._projects_combobox = projects_combobox\n        self._folders_widget = folders_widget\n        self._tasks_widget = tasks_widget\n\n        self._variant_input = variant_input\n        self._new_folder_checkbox = new_folder_checkbox\n        self._folder_name_input = folder_name_input\n        self._comment_input = comment_input\n        self._use_original_names_checkbox = original_names_checkbox\n        self._library_only_checkbox = library_only_checkbox\n\n        self._publish_btn = publish_btn\n\n        self._overlay_widget = overlay_widget\n        self._show_detail_btn = show_detail_btn\n        self._overlay_close_btn = overlay_close_btn\n        self._overlay_try_btn = overlay_try_btn\n        self._overlay_label = overlay_label\n\n        self._error_detail_dialog = ErrorDetailDialog(self)\n\n        self._user_input_changed_timer = user_input_changed_timer\n        # Store current value on input text change\n        #   The value is unset when is passed to controller\n        # The goal is to have controll over changes happened during user change\n        #   in UI and controller auto-changes\n        self._new_folder_name_enabled = None\n        self._new_folder_name_input_text = None\n        self._variant_input_text = None\n        self._comment_input_text = None\n        self._version_up_checkbox = version_up_checkbox\n\n        self._first_show = True\n        self._show_timer = show_timer\n        self._show_counter = 0\n\n        self._main_thread_timer = main_thread_timer\n        self._main_thread_timer_can_stop = True\n        self._last_submit_message = None\n\n        self._variant_is_valid = None\n        self._folder_is_valid = None\n\n        publish_btn.setEnabled(False)\n        show_detail_btn.setVisible(False)\n        overlay_close_btn.setVisible(False)\n        overlay_try_btn.setVisible(False)\n        version_up_checkbox.setChecked(False)\n\n    # Support of public api function of controller\n    def set_source(self, project_name, version_ids):\n        \"\"\"Set source project and version.\n\n        Call the method on controller.\n\n        Args:\n            project_name (Union[str, None]): Name of project.\n            version_ids (Union[str, None]): comma separated Version ids.\n        \"\"\"\n\n        self._controller.set_source(project_name, version_ids)\n\n    def showEvent(self, event):\n        super(PushToContextSelectWindow, self).showEvent(event)\n        if self._first_show:\n            self._first_show = False\n            self._on_first_show()\n\n    def refresh(self):\n        user_values = self._controller.get_user_values()\n        new_folder_name = user_values[\"new_folder_name\"]\n        variant = user_values[\"variant\"]\n        self._folder_name_input.setText(new_folder_name or \"\")\n        self._variant_input.setText(variant or \"\")\n        self._invalidate_variant(user_values[\"is_variant_valid\"])\n        self._invalidate_use_original_names(\n            self._use_original_names_checkbox.isChecked())\n        self._invalidate_new_folder_name(\n            new_folder_name, user_values[\"is_new_folder_name_valid\"]\n        )\n        self._projects_combobox.refresh()\n\n    def _on_first_show(self):\n        width = 740\n        height = 640\n        inputs_width = 360\n        self.setStyleSheet(load_stylesheet())\n        self.resize(width, height)\n        self._main_splitter.setSizes([width - inputs_width, inputs_width])\n        self._show_timer.start()\n\n    def _on_show_timer(self):\n        if self._show_counter &lt; 3:\n            self._show_counter += 1\n            return\n        self._show_timer.stop()\n\n        self._show_counter = 0\n\n        self.refresh()\n\n    def _on_new_folder_check(self):\n        self._new_folder_name_enabled = self._new_folder_checkbox.isChecked()\n        self._folder_name_input.setEnabled(self._new_folder_name_enabled)\n        self._user_input_changed_timer.start()\n\n    def _on_new_folder_change(self, text):\n        self._new_folder_name_input_text = text\n        self._user_input_changed_timer.start()\n\n    def _on_variant_change(self, text):\n        self._variant_input_text = text\n        self._user_input_changed_timer.start()\n\n    def _on_comment_change(self, text):\n        self._comment_input_text = text\n        self._user_input_changed_timer.start()\n\n    def _on_library_only_change(self) -&gt; None:\n        \"\"\"Change toggle state, reset filter, recalculate dropdown\"\"\"\n        is_checked = self._library_only_checkbox.isChecked()\n        self._projects_combobox.set_standard_filter_enabled(is_checked)\n\n    def _on_original_names_change(self) -&gt; None:\n        is_checked = self._use_original_names_checkbox.isChecked()\n        self._invalidate_use_original_names(is_checked)\n\n    def _on_version_up_checkbox_change(self) -&gt; None:\n        is_checked = self._version_up_checkbox.isChecked()\n        self._controller.set_version_up(is_checked)\n\n    def _on_user_input_timer(self):\n        folder_name_enabled = self._new_folder_name_enabled\n        folder_name = self._new_folder_name_input_text\n        if folder_name is not None or folder_name_enabled is not None:\n            self._new_folder_name_input_text = None\n            self._new_folder_name_enabled = None\n            if not self._new_folder_checkbox.isChecked():\n                folder_name = None\n            elif folder_name is None:\n                folder_name = self._folder_name_input.text()\n            self._controller.set_user_value_folder_name(folder_name)\n\n        variant = self._variant_input_text\n        if variant is not None:\n            self._variant_input_text = None\n            self._controller.set_user_value_variant(variant)\n\n        comment = self._comment_input_text\n        if comment is not None:\n            self._comment_input_text = None\n            self._controller.set_user_value_comment(comment)\n\n    def _on_controller_new_folder_change(self, event):\n        folder_name = event[\"new_folder_name\"]\n        if (\n            self._new_folder_name_input_text is None\n            and folder_name != self._folder_name_input.text()\n        ):\n            self._folder_name_input.setText(folder_name)\n\n        self._invalidate_new_folder_name(folder_name, event[\"is_valid\"])\n\n    def _on_controller_variant_change(self, event):\n        is_valid = event[\"is_valid\"]\n        variant = event[\"variant\"]\n        if (\n            self._variant_input_text is None\n            and variant != self._variant_input.text()\n        ):\n            self._variant_input.setText(variant)\n\n        self._invalidate_variant(is_valid)\n\n    def _on_controller_comment_change(self, event):\n        comment = event[\"comment\"]\n        if (\n            self._comment_input_text is None\n            and comment != self._comment_input.text()\n        ):\n            self._comment_input.setText(comment)\n\n    def _on_controller_source_change(self):\n        self._header_label.setText(self._controller.get_source_label())\n\n    def _invalidate_new_folder_name(self, folder_name, is_valid):\n        self._tasks_widget.setVisible(folder_name is None)\n        if self._folder_is_valid is is_valid:\n            return\n        self._folder_is_valid = is_valid\n        state = \"\"\n        if folder_name is not None:\n            state = \"valid\" if is_valid else \"invalid\"\n        set_style_property(self._folder_name_input, \"state\", state)\n\n    def _invalidate_variant(self, is_valid):\n        self._variant_is_valid = is_valid\n        state = \"valid\" if is_valid else \"invalid\"\n        set_style_property(self._variant_input, \"state\", state)\n\n    def _invalidate_use_original_names(self, use_original_names):\n        \"\"\"Checks if original names must be used.\n\n        Invalidates Variant if necessary\n        \"\"\"\n        if self._controller.original_names_required():\n            use_original_names = True\n\n        self._variant_input.setEnabled(not use_original_names)\n        self._invalidate_variant(not use_original_names)\n\n        self._controller._use_original_name = use_original_names\n        self._use_original_names_checkbox.setChecked(use_original_names)\n\n    def _on_submission_change(self, event):\n        self._publish_btn.setEnabled(event[\"enabled\"])\n\n    def _on_show_detail_click(self):\n        self._error_detail_dialog.show()\n\n    def _on_close_click(self):\n        self.close()\n\n    def _on_select_click(self):\n        self._process_item_id = self._controller.submit(wait=False)\n\n    def _on_try_again_click(self):\n        self._process_item_id = None\n        self._last_submit_message = None\n\n        self._error_detail_dialog.close()\n\n        self._overlay_close_btn.setVisible(False)\n        self._overlay_try_btn.setVisible(False)\n        self._show_detail_btn.setVisible(False)\n        self._main_layout.setCurrentWidget(self._main_context_widget)\n\n    def _on_main_thread_timer(self):\n        if self._last_submit_message:\n            self._overlay_label.setText(self._last_submit_message)\n            self._last_submit_message = None\n\n        failed_pushes = []\n        fail_tracebacks = []\n        for process_item in self._controller.get_process_items().values():\n            process_status = process_item.get_status_data()\n            if process_status[\"failed\"]:\n                failed_pushes.append(process_status)\n        # push_failed = process_status[\"failed\"]\n        # fail_traceback = process_status[\"full_traceback\"]\n        if self._main_thread_timer_can_stop:\n            self._main_thread_timer.stop()\n            self._overlay_close_btn.setVisible(True)\n            if failed_pushes:\n                self._overlay_try_btn.setVisible(True)\n                fail_tracebacks = [\n                    process_status[\"full_traceback\"]\n                    for process_status in failed_pushes\n                    if process_status[\"full_traceback\"]\n                ]\n                if fail_tracebacks:\n                    self._show_detail_btn.setVisible(True)\n\n        if failed_pushes:\n            reasons = [\n                process_status[\"fail_reason\"]\n                for process_status in failed_pushes\n            ]\n            if fail_tracebacks:\n                reason = \"\\n\".join(reasons)\n                message = (\n                    \"Unhandled error happened.\"\n                    \" Check error detail for more information.\"\n                )\n                self._error_detail_dialog.set_detail(\n                    reason, \"\\n\".join(fail_tracebacks)\n                )\n            else:\n                message = f\"Push Failed:\\n{reasons}\"\n\n            self._overlay_label.setText(message)\n            set_style_property(self._overlay_close_btn, \"state\", \"error\")\n\n        if self._main_thread_timer_can_stop:\n            # Join thread in controller\n            self._controller.wait_for_process_thread()\n            # Reset process item to None\n            self._process_item_id = None\n\n    def _on_controller_submit_start(self):\n        self._main_thread_timer_can_stop = False\n        self._main_thread_timer.start()\n        self._main_layout.setCurrentWidget(self._overlay_widget)\n        self._overlay_label.setText(\"Submission started\")\n\n    def _on_controller_submit_end(self):\n        self._main_thread_timer_can_stop = True\n\n    def _on_push_message(self, event):\n        self._last_submit_message = event[\"message\"]\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/push_to_project/ui/window.html#client.ayon_core.tools.push_to_project.ui.window.PushToContextSelectWindow.set_source","title":"<code>set_source(project_name, version_ids)</code>","text":"<p>Set source project and version.</p> <p>Call the method on controller.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>Union[str, None]</code> <p>Name of project.</p> required <code>version_ids</code> <code>Union[str, None]</code> <p>comma separated Version ids.</p> required Source code in <code>client/ayon_core/tools/push_to_project/ui/window.py</code> <pre><code>def set_source(self, project_name, version_ids):\n    \"\"\"Set source project and version.\n\n    Call the method on controller.\n\n    Args:\n        project_name (Union[str, None]): Name of project.\n        version_ids (Union[str, None]): comma separated Version ids.\n    \"\"\"\n\n    self._controller.set_source(project_name, version_ids)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/resources/index.html","title":"resources","text":""},{"location":"autoapi/client/ayon_core/tools/resources/index.html#client.ayon_core.tools.resources.get_icon","title":"<code>get_icon(icon_name=None, filename=None)</code>","text":"<p>Load image from './images' as QICon.</p> Source code in <code>client/ayon_core/tools/resources/__init__.py</code> <pre><code>def get_icon(icon_name=None, filename=None):\n    \"\"\"Load image from './images' as QICon.\"\"\"\n    pix = get_pixmap(icon_name, filename)\n    if pix:\n        return QtGui.QIcon(pix)\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/resources/index.html#client.ayon_core.tools.resources.get_icon_path","title":"<code>get_icon_path(icon_name=None, filename=None)</code>","text":"<p>Path to image in './images' folder.</p> Source code in <code>client/ayon_core/tools/resources/__init__.py</code> <pre><code>def get_icon_path(icon_name=None, filename=None):\n    \"\"\"Path to image in './images' folder.\"\"\"\n    if icon_name is None and filename is None:\n        return None\n\n    if filename is None:\n        filename = \"{}.png\".format(icon_name)\n\n    path = os.path.join(\n        os.path.dirname(os.path.abspath(__file__)),\n        \"images\",\n        filename\n    )\n    if os.path.exists(path):\n        return path\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/resources/index.html#client.ayon_core.tools.resources.get_image","title":"<code>get_image(icon_name=None, filename=None)</code>","text":"<p>Load image from './images' as QImage.</p> Source code in <code>client/ayon_core/tools/resources/__init__.py</code> <pre><code>def get_image(icon_name=None, filename=None):\n    \"\"\"Load image from './images' as QImage.\"\"\"\n    path = get_icon_path(icon_name, filename)\n    if path:\n        return QtGui.QImage(path)\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/resources/index.html#client.ayon_core.tools.resources.get_pixmap","title":"<code>get_pixmap(icon_name=None, filename=None)</code>","text":"<p>Load image from './images' as QPixmap.</p> Source code in <code>client/ayon_core/tools/resources/__init__.py</code> <pre><code>def get_pixmap(icon_name=None, filename=None):\n    \"\"\"Load image from './images' as QPixmap.\"\"\"\n    path = get_icon_path(icon_name, filename)\n    if path:\n        return QtGui.QPixmap(path)\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/sceneinventory/index.html","title":"sceneinventory","text":""},{"location":"autoapi/client/ayon_core/tools/sceneinventory/index.html#client.ayon_core.tools.sceneinventory.SceneInventoryController","title":"<code>SceneInventoryController</code>","text":"<p>This is a temporary controller for AYON.</p> <p>Goal of this controller is to provide a way to get current context.</p> <p>Also provides (hopefully) cleaner api for site sync.</p> Source code in <code>client/ayon_core/tools/sceneinventory/control.py</code> <pre><code>class SceneInventoryController:\n    \"\"\"This is a temporary controller for AYON.\n\n    Goal of this controller is to provide a way to get current context.\n\n    Also provides (hopefully) cleaner api for site sync.\n    \"\"\"\n\n    def __init__(self, host=None):\n        if host is None:\n            host = registered_host()\n        self._host = host\n        self._current_context = None\n        self._current_project = None\n        self._current_folder_id = None\n        self._current_folder_set = False\n\n        self._containers_model = ContainersModel(self)\n        self._sitesync_model = SiteSyncModel(self)\n        # Switch dialog requirements\n        self._hierarchy_model = HierarchyModel(self)\n        self._projects_model = ProjectsModel(self)\n        self._event_system = self._create_event_system()\n\n    def get_window_subtitle(self) -&gt; Optional[str]:\n        if self._host is None:\n            return None\n        return self._host.name\n\n    def get_host(self) -&gt; ILoadHost:\n        return self._host\n\n    def emit_event(self, topic, data=None, source=None):\n        if data is None:\n            data = {}\n        self._event_system.emit(topic, data, source)\n\n    def register_event_callback(self, topic, callback):\n        self._event_system.add_callback(topic, callback)\n\n    def reset(self):\n        self._current_context = None\n        self._current_project = None\n        self._current_folder_id = None\n        self._current_folder_set = False\n\n        self._containers_model.reset()\n        self._sitesync_model.reset()\n        self._hierarchy_model.reset()\n\n    def get_current_context(self):\n        if self._current_context is None:\n            if hasattr(self._host, \"get_current_context\"):\n                self._current_context = self._host.get_current_context()\n            else:\n                self._current_context = get_current_context()\n        return self._current_context\n\n    def get_current_project_name(self):\n        if self._current_project is None:\n            self._current_project = self.get_current_context()[\"project_name\"]\n        return self._current_project\n\n    def get_current_folder_id(self):\n        if self._current_folder_set:\n            return self._current_folder_id\n\n        context = self.get_current_context()\n        project_name = context[\"project_name\"]\n        folder_path = context.get(\"folder_path\")\n        folder_id = None\n        if folder_path:\n            folder = ayon_api.get_folder_by_path(project_name, folder_path)\n            if folder:\n                folder_id = folder[\"id\"]\n\n        self._current_folder_id = folder_id\n        self._current_folder_set = True\n        return self._current_folder_id\n\n    def get_project_status_items(self, project_name=None):\n        if project_name is None:\n            project_name = self.get_current_project_name()\n        return self._projects_model.get_project_status_items(\n            project_name, None\n        )\n\n    def get_product_type_icons_mapping(\n        self, project_name: Optional[str]\n    ) -&gt; ProductTypeIconMapping:\n        return self._projects_model.get_product_type_icons_mapping(\n            project_name\n        )\n\n    # Containers methods\n    def get_containers(self):\n        return self._containers_model.get_containers()\n\n    def get_containers_by_item_ids(self, item_ids):\n        return self._containers_model.get_containers_by_item_ids(item_ids)\n\n    def get_container_items(self):\n        return self._containers_model.get_container_items()\n\n    def get_container_items_by_id(self, item_ids):\n        return self._containers_model.get_container_items_by_id(item_ids)\n\n    def get_representation_info_items(self, project_name, representation_ids):\n        return self._containers_model.get_representation_info_items(\n            project_name, representation_ids\n        )\n\n    def get_version_items(self, project_name, product_ids):\n        return self._containers_model.get_version_items(\n            project_name, product_ids)\n\n    # Site Sync methods\n    def is_sitesync_enabled(self):\n        return self._sitesync_model.is_sitesync_enabled()\n\n    def get_sites_information(self, project_name):\n        return self._sitesync_model.get_sites_information(project_name)\n\n    def get_site_provider_icons(self):\n        return self._sitesync_model.get_site_provider_icons()\n\n    def get_representations_site_progress(\n        self, project_name, representation_ids\n    ):\n        return self._sitesync_model.get_representations_site_progress(\n            project_name, representation_ids\n        )\n\n    def resync_representations(\n        self, project_name, representation_ids, site_type\n    ):\n        return self._sitesync_model.resync_representations(\n            project_name,\n            representation_ids,\n            site_type\n        )\n\n    # Switch dialog methods\n    def get_folder_items(self, project_name, sender=None):\n        return self._hierarchy_model.get_folder_items(project_name, sender)\n\n    def get_folder_label(self, folder_id):\n        if not folder_id:\n            return None\n        project_name = self.get_current_project_name()\n        folder_item = self._hierarchy_model.get_folder_item(\n            project_name, folder_id)\n        if folder_item is None:\n            return None\n        return folder_item.label\n\n    def _create_event_system(self):\n        return QueuedEventSystem()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/sceneinventory/control.html","title":"control","text":""},{"location":"autoapi/client/ayon_core/tools/sceneinventory/control.html#client.ayon_core.tools.sceneinventory.control.SceneInventoryController","title":"<code>SceneInventoryController</code>","text":"<p>This is a temporary controller for AYON.</p> <p>Goal of this controller is to provide a way to get current context.</p> <p>Also provides (hopefully) cleaner api for site sync.</p> Source code in <code>client/ayon_core/tools/sceneinventory/control.py</code> <pre><code>class SceneInventoryController:\n    \"\"\"This is a temporary controller for AYON.\n\n    Goal of this controller is to provide a way to get current context.\n\n    Also provides (hopefully) cleaner api for site sync.\n    \"\"\"\n\n    def __init__(self, host=None):\n        if host is None:\n            host = registered_host()\n        self._host = host\n        self._current_context = None\n        self._current_project = None\n        self._current_folder_id = None\n        self._current_folder_set = False\n\n        self._containers_model = ContainersModel(self)\n        self._sitesync_model = SiteSyncModel(self)\n        # Switch dialog requirements\n        self._hierarchy_model = HierarchyModel(self)\n        self._projects_model = ProjectsModel(self)\n        self._event_system = self._create_event_system()\n\n    def get_window_subtitle(self) -&gt; Optional[str]:\n        if self._host is None:\n            return None\n        return self._host.name\n\n    def get_host(self) -&gt; ILoadHost:\n        return self._host\n\n    def emit_event(self, topic, data=None, source=None):\n        if data is None:\n            data = {}\n        self._event_system.emit(topic, data, source)\n\n    def register_event_callback(self, topic, callback):\n        self._event_system.add_callback(topic, callback)\n\n    def reset(self):\n        self._current_context = None\n        self._current_project = None\n        self._current_folder_id = None\n        self._current_folder_set = False\n\n        self._containers_model.reset()\n        self._sitesync_model.reset()\n        self._hierarchy_model.reset()\n\n    def get_current_context(self):\n        if self._current_context is None:\n            if hasattr(self._host, \"get_current_context\"):\n                self._current_context = self._host.get_current_context()\n            else:\n                self._current_context = get_current_context()\n        return self._current_context\n\n    def get_current_project_name(self):\n        if self._current_project is None:\n            self._current_project = self.get_current_context()[\"project_name\"]\n        return self._current_project\n\n    def get_current_folder_id(self):\n        if self._current_folder_set:\n            return self._current_folder_id\n\n        context = self.get_current_context()\n        project_name = context[\"project_name\"]\n        folder_path = context.get(\"folder_path\")\n        folder_id = None\n        if folder_path:\n            folder = ayon_api.get_folder_by_path(project_name, folder_path)\n            if folder:\n                folder_id = folder[\"id\"]\n\n        self._current_folder_id = folder_id\n        self._current_folder_set = True\n        return self._current_folder_id\n\n    def get_project_status_items(self, project_name=None):\n        if project_name is None:\n            project_name = self.get_current_project_name()\n        return self._projects_model.get_project_status_items(\n            project_name, None\n        )\n\n    def get_product_type_icons_mapping(\n        self, project_name: Optional[str]\n    ) -&gt; ProductTypeIconMapping:\n        return self._projects_model.get_product_type_icons_mapping(\n            project_name\n        )\n\n    # Containers methods\n    def get_containers(self):\n        return self._containers_model.get_containers()\n\n    def get_containers_by_item_ids(self, item_ids):\n        return self._containers_model.get_containers_by_item_ids(item_ids)\n\n    def get_container_items(self):\n        return self._containers_model.get_container_items()\n\n    def get_container_items_by_id(self, item_ids):\n        return self._containers_model.get_container_items_by_id(item_ids)\n\n    def get_representation_info_items(self, project_name, representation_ids):\n        return self._containers_model.get_representation_info_items(\n            project_name, representation_ids\n        )\n\n    def get_version_items(self, project_name, product_ids):\n        return self._containers_model.get_version_items(\n            project_name, product_ids)\n\n    # Site Sync methods\n    def is_sitesync_enabled(self):\n        return self._sitesync_model.is_sitesync_enabled()\n\n    def get_sites_information(self, project_name):\n        return self._sitesync_model.get_sites_information(project_name)\n\n    def get_site_provider_icons(self):\n        return self._sitesync_model.get_site_provider_icons()\n\n    def get_representations_site_progress(\n        self, project_name, representation_ids\n    ):\n        return self._sitesync_model.get_representations_site_progress(\n            project_name, representation_ids\n        )\n\n    def resync_representations(\n        self, project_name, representation_ids, site_type\n    ):\n        return self._sitesync_model.resync_representations(\n            project_name,\n            representation_ids,\n            site_type\n        )\n\n    # Switch dialog methods\n    def get_folder_items(self, project_name, sender=None):\n        return self._hierarchy_model.get_folder_items(project_name, sender)\n\n    def get_folder_label(self, folder_id):\n        if not folder_id:\n            return None\n        project_name = self.get_current_project_name()\n        folder_item = self._hierarchy_model.get_folder_item(\n            project_name, folder_id)\n        if folder_item is None:\n            return None\n        return folder_item.label\n\n    def _create_event_system(self):\n        return QueuedEventSystem()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/sceneinventory/delegates.html","title":"delegates","text":""},{"location":"autoapi/client/ayon_core/tools/sceneinventory/delegates.html#client.ayon_core.tools.sceneinventory.delegates.VersionDelegate","title":"<code>VersionDelegate</code>","text":"<p>               Bases: <code>QStyledItemDelegate</code></p> <p>A delegate that display version integer formatted as version string.</p> Source code in <code>client/ayon_core/tools/sceneinventory/delegates.py</code> <pre><code>class VersionDelegate(QtWidgets.QStyledItemDelegate):\n    \"\"\"A delegate that display version integer formatted as version string.\"\"\"\n    _locked_icon = None\n\n    def paint(self, painter, option, index):\n        fg_color = index.data(QtCore.Qt.ForegroundRole)\n        if fg_color:\n            if isinstance(fg_color, QtGui.QBrush):\n                fg_color = fg_color.color()\n            elif isinstance(fg_color, QtGui.QColor):\n                pass\n            else:\n                fg_color = None\n\n        if not fg_color:\n            return super().paint(painter, option, index)\n\n        if option.widget:\n            style = option.widget.style()\n        else:\n            style = QtWidgets.QApplication.style()\n\n        style.drawControl(\n            QtWidgets.QStyle.CE_ItemViewItem,\n            option,\n            painter,\n            option.widget\n        )\n\n        painter.save()\n\n        text = index.data(VERSION_LABEL_ROLE)\n        pen = painter.pen()\n        pen.setColor(fg_color)\n        painter.setPen(pen)\n\n        text_rect = style.subElementRect(\n            QtWidgets.QStyle.SE_ItemViewItemText,\n            option\n        )\n        text_margin = style.proxy().pixelMetric(\n            QtWidgets.QStyle.PM_FocusFrameHMargin, option, option.widget\n        ) + 1\n\n        text_rect_f = text_rect.adjusted(\n            text_margin, 0, - text_margin, 0\n        )\n\n        painter.drawText(\n            text_rect_f,\n            option.displayAlignment,\n            text\n        )\n        if index.data(CONTAINER_VERSION_LOCKED_ROLE) is True:\n            icon = self._get_locked_icon()\n            size = max(text_rect_f.height() // 2, 16)\n            margin = (text_rect_f.height() - size) // 2\n\n            icon_rect = QtCore.QRect(\n                text_rect_f.right() - size,\n                text_rect_f.top() + margin,\n                size,\n                size\n            )\n            icon.paint(painter, icon_rect)\n\n        painter.restore()\n\n    def _get_locked_icon(cls):\n        if cls._locked_icon is None:\n            cls._locked_icon = get_qt_icon(\n                MaterialSymbolsIcon(\"lock\", color=\"white\")\n            )\n        return cls._locked_icon\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/sceneinventory/model.html","title":"model","text":""},{"location":"autoapi/client/ayon_core/tools/sceneinventory/model.html#client.ayon_core.tools.sceneinventory.model.FilterProxyModel","title":"<code>FilterProxyModel</code>","text":"<p>               Bases: <code>QSortFilterProxyModel</code></p> <p>Filter model to where key column's value is in the filtered tags</p> Source code in <code>client/ayon_core/tools/sceneinventory/model.py</code> <pre><code>class FilterProxyModel(QtCore.QSortFilterProxyModel):\n    \"\"\"Filter model to where key column's value is in the filtered tags\"\"\"\n\n    def __init__(self, *args, **kwargs):\n        super().__init__(*args, **kwargs)\n        self.setDynamicSortFilter(True)\n        self.setFilterCaseSensitivity(QtCore.Qt.CaseInsensitive)\n        self._filter_outdated = False\n        self._hierarchy_view = False\n\n    def filterAcceptsRow(self, row, parent):\n        model = self.sourceModel()\n        source_index = model.index(row, self.filterKeyColumn(), parent)\n\n        # Always allow bottom entries (individual containers), since their\n        # parent group hidden if it wouldn't have been validated.\n        if source_index.data(IS_CONTAINER_ITEM_ROLE):\n            return True\n\n        if self._filter_outdated:\n            # When filtering to outdated we filter the up to date entries\n            # thus we \"allow\" them when they are outdated\n            if source_index.data(VERSION_IS_LATEST_ROLE):\n                return False\n\n        # Filter by regex\n        if hasattr(self, \"filterRegularExpression\"):\n            regex = self.filterRegularExpression()\n        else:\n            regex = self.filterRegExp()\n\n        if not self._matches(row, parent, regex.pattern()):\n            return False\n        return True\n\n    def set_filter_outdated(self, state):\n        \"\"\"Set whether to show the outdated entries only.\"\"\"\n        state = bool(state)\n\n        if state != self._filter_outdated:\n            self._filter_outdated = state\n            self.invalidateFilter()\n\n    def set_hierarchy_view(self, state):\n        state = bool(state)\n\n        if state != self._hierarchy_view:\n            self._hierarchy_view = state\n\n    def _matches(self, row, parent, pattern):\n        \"\"\"Return whether row matches regex pattern.\n\n        Args:\n            row (int): row number in model\n            parent (QtCore.QModelIndex): parent index\n            pattern (regex.pattern): pattern to check for in key\n\n        Returns:\n            bool\n\n        \"\"\"\n        if not pattern:\n            return True\n\n        flags = 0\n        if self.sortCaseSensitivity() == QtCore.Qt.CaseInsensitive:\n            flags = re.IGNORECASE\n\n        regex = re.compile(re.escape(pattern), flags=flags)\n\n        model = self.sourceModel()\n        column = self.filterKeyColumn()\n        role = self.filterRole()\n\n        matches_queue = collections.deque()\n        matches_queue.append((row, parent))\n        while matches_queue:\n            queue_item = matches_queue.popleft()\n            row, parent = queue_item\n\n            index = model.index(row, column, parent)\n            value = model.data(index, role)\n            if regex.search(value):\n                return True\n\n            for idx in range(model.rowCount(index)):\n                matches_queue.append((idx, index))\n\n        return False\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/sceneinventory/model.html#client.ayon_core.tools.sceneinventory.model.FilterProxyModel.set_filter_outdated","title":"<code>set_filter_outdated(state)</code>","text":"<p>Set whether to show the outdated entries only.</p> Source code in <code>client/ayon_core/tools/sceneinventory/model.py</code> <pre><code>def set_filter_outdated(self, state):\n    \"\"\"Set whether to show the outdated entries only.\"\"\"\n    state = bool(state)\n\n    if state != self._filter_outdated:\n        self._filter_outdated = state\n        self.invalidateFilter()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/sceneinventory/model.html#client.ayon_core.tools.sceneinventory.model.InventoryModel","title":"<code>InventoryModel</code>","text":"<p>               Bases: <code>QStandardItemModel</code></p> <p>The model for the inventory</p> Source code in <code>client/ayon_core/tools/sceneinventory/model.py</code> <pre><code>class InventoryModel(QtGui.QStandardItemModel):\n    \"\"\"The model for the inventory\"\"\"\n\n    column_labels = [\n        \"Name\",\n        \"Version\",\n        \"Status\",\n        \"Count\",\n        \"Product type\",\n        \"Group\",\n        \"Loader\",\n        \"Object name\",\n        \"Active site\",\n        \"Remote site\",\n        \"Project\",\n    ]\n    name_col = column_labels.index(\"Name\")\n    version_col = column_labels.index(\"Version\")\n    status_col = column_labels.index(\"Status\")\n    count_col = column_labels.index(\"Count\")\n    product_type_col = column_labels.index(\"Product type\")\n    product_group_col = column_labels.index(\"Group\")\n    loader_col = column_labels.index(\"Loader\")\n    object_name_col = column_labels.index(\"Object name\")\n    active_site_col = column_labels.index(\"Active site\")\n    remote_site_col = column_labels.index(\"Remote site\")\n    project_col = column_labels.index(\"Project\")\n    display_role_by_column = {\n        name_col: QtCore.Qt.DisplayRole,\n        version_col: VERSION_LABEL_ROLE,\n        status_col: STATUS_NAME_ROLE,\n        count_col: COUNT_ROLE,\n        product_type_col: PRODUCT_TYPE_ROLE,\n        product_group_col: PRODUCT_GROUP_NAME_ROLE,\n        loader_col: LOADER_NAME_ROLE,\n        object_name_col: OBJECT_NAME_ROLE,\n        project_col: PROJECT_NAME_ROLE,\n        active_site_col: ACTIVE_SITE_PROGRESS_ROLE,\n        remote_site_col: REMOTE_SITE_PROGRESS_ROLE,\n    }\n    decoration_role_by_column = {\n        name_col: QtCore.Qt.DecorationRole,\n        product_type_col: PRODUCT_TYPE_ICON_ROLE,\n        product_group_col: PRODUCT_GROUP_ICON_ROLE,\n        active_site_col: ACTIVE_SITE_ICON_ROLE,\n        remote_site_col: REMOTE_SITE_ICON_ROLE,\n    }\n    foreground_role_by_column = {\n        name_col: NAME_COLOR_ROLE,\n        version_col: VERSION_COLOR_ROLE,\n        status_col: STATUS_COLOR_ROLE,\n    }\n    width_by_column = {\n        name_col: 250,\n        version_col: 55,\n        status_col: 100,\n        count_col: 55,\n        product_type_col: 150,\n        product_group_col: 120,\n        loader_col: 150,\n        project_col: 150,\n    }\n\n    OUTDATED_COLOR = QtGui.QColor(235, 30, 30)\n    CHILD_OUTDATED_COLOR = QtGui.QColor(200, 160, 30)\n    GRAYOUT_COLOR = QtGui.QColor(160, 160, 160)\n\n    def __init__(self, controller, parent=None):\n        super().__init__(parent)\n\n        self.setColumnCount(len(self.column_labels))\n        for idx, label in enumerate(self.column_labels):\n            self.setHeaderData(idx, QtCore.Qt.Horizontal, label)\n\n        self.log = logging.getLogger(self.__class__.__name__)\n\n        self._controller = controller\n\n        self._hierarchy_view = False\n\n        self._default_icon_color = get_default_entity_icon_color()\n\n        self._last_project_statuses = collections.defaultdict(dict)\n        self._last_status_icons_by_name = collections.defaultdict(dict)\n\n    def outdated(self, item):\n        return item.get(\"isOutdated\", True)\n\n    def refresh(self, selected=None):\n        \"\"\"Refresh the model\"\"\"\n        # for debugging or testing, injecting items from outside\n        container_items = self._controller.get_container_items()\n\n        self._clear_items()\n\n        project_names = set()\n        repre_ids_by_project = collections.defaultdict(set)\n        version_items_by_project = collections.defaultdict(dict)\n        repre_info_by_id_by_project = collections.defaultdict(dict)\n        item_by_repre_id_by_project = collections.defaultdict(\n            lambda: collections.defaultdict(list))\n        for container_item in container_items:\n            # if (\n            #     selected is not None\n            #     and container_item.item_id not in selected\n            # ):\n            #     continue\n            project_name = container_item.project_name\n            representation_id = container_item.representation_id\n            project_names.add(project_name)\n            repre_ids_by_project[project_name].add(representation_id)\n            (\n                item_by_repre_id_by_project\n                [project_name]\n                [representation_id]\n            ).append(container_item)\n\n        for project_name, representation_ids in repre_ids_by_project.items():\n            repre_info = self._controller.get_representation_info_items(\n                project_name, representation_ids\n            )\n            repre_info_by_id_by_project[project_name] = repre_info\n\n            product_ids = {\n                repre_info.product_id\n                for repre_info in repre_info.values()\n                if repre_info.is_valid\n            }\n            version_items = self._controller.get_version_items(\n                project_name, product_ids\n            )\n            version_items_by_project[project_name] = version_items\n\n        # SiteSync addon information\n        progress_by_project = {\n            project_name: self._controller.get_representations_site_progress(\n                project_name, repre_ids\n            )\n            for project_name, repre_ids in repre_ids_by_project.items()\n        }\n\n        sites_info_by_project_name = {\n            project_name: self._controller.get_sites_information(project_name)\n            for project_name in project_names\n        }\n        site_icons = {\n            provider: get_qt_icon(icon_def)\n            for provider, icon_def in (\n                self._controller.get_site_provider_icons().items()\n            )\n        }\n        last_project_statuses = collections.defaultdict(dict)\n        for project_name in project_names:\n            status_items_by_name = {\n                status_item.name: status_item\n                for status_item in self._controller.get_project_status_items(\n                    project_name\n                )\n            }\n            last_project_statuses[project_name] = status_items_by_name\n        self._last_project_statuses = last_project_statuses\n        self._last_status_icons_by_name = collections.defaultdict(dict)\n\n        group_item_icon = qtawesome.icon(\n            \"fa.folder\", color=self._default_icon_color\n        )\n        valid_item_icon = qtawesome.icon(\n            \"fa.file-o\", color=self._default_icon_color\n        )\n        invalid_item_icon = qtawesome.icon(\n            \"fa.exclamation-circle\", color=self._default_icon_color\n        )\n        group_icon = qtawesome.icon(\n            \"fa.object-group\", color=self._default_icon_color\n        )\n        group_item_font = QtGui.QFont()\n        group_item_font.setBold(True)\n\n        root_item = self.invisibleRootItem()\n        group_items = []\n        for project_name, items_by_repre_id in (\n            item_by_repre_id_by_project.items()\n        ):\n            sites_info = sites_info_by_project_name[project_name]\n            active_site_icon = site_icons.get(\n                sites_info[\"active_site_provider\"]\n            )\n            remote_site_icon = site_icons.get(\n                sites_info[\"remote_site_provider\"]\n            )\n\n            progress_by_id = progress_by_project[project_name]\n            repre_info_by_id = repre_info_by_id_by_project[project_name]\n            version_items_by_product_id = (\n                version_items_by_project[project_name]\n            )\n            for repre_id, container_items in items_by_repre_id.items():\n                repre_info = repre_info_by_id[repre_id]\n                version_color = None\n                if not repre_info.is_valid:\n                    version_label = \"N/A\"\n                    group_name = \"&lt; Entity N/A &gt;\"\n                    item_icon = invalid_item_icon\n                    is_latest = False\n                    is_hero = False\n                    status_name = None\n\n                else:\n                    group_name = \"{}_{}: ({})\".format(\n                        repre_info.folder_path.rsplit(\"/\")[-1],\n                        repre_info.product_name,\n                        repre_info.representation_name\n                    )\n                    item_icon = valid_item_icon\n\n                    version_items = (\n                        version_items_by_product_id[repre_info.product_id]\n                    )\n                    version_item = version_items[repre_info.version_id]\n                    version_label = format_version(version_item.version)\n                    is_hero = version_item.version &lt; 0\n                    is_latest = version_item.is_latest\n                    if not version_item.is_latest:\n                        version_color = self.OUTDATED_COLOR\n                    status_name = version_item.status\n\n                (\n                    status_color, status_short, status_icon\n                ) = self._get_status_data(project_name, status_name)\n\n                repre_name = (\n                    repre_info.representation_name or\n                    \"&lt;unknown representation&gt;\"\n                )\n                container_model_items = []\n                for container_item in container_items:\n                    object_name = container_item.object_name or \"&lt;none&gt;\"\n                    unique_name = repre_name + object_name\n                    item = QtGui.QStandardItem()\n                    item.setColumnCount(root_item.columnCount())\n                    item.setData(container_item.namespace,\n                                 QtCore.Qt.DisplayRole)\n                    item.setData(self.GRAYOUT_COLOR, NAME_COLOR_ROLE)\n                    item.setData(self.GRAYOUT_COLOR, VERSION_COLOR_ROLE)\n                    item.setData(item_icon, QtCore.Qt.DecorationRole)\n                    item.setData(repre_info.product_id, PRODUCT_ID_ROLE)\n                    item.setData(container_item.item_id, ITEM_ID_ROLE)\n                    item.setData(version_label, VERSION_LABEL_ROLE)\n                    item.setData(container_item.loader_name, LOADER_NAME_ROLE)\n                    item.setData(container_item.object_name, OBJECT_NAME_ROLE)\n                    item.setData(True, IS_CONTAINER_ITEM_ROLE)\n                    item.setData(unique_name, ITEM_UNIQUE_NAME_ROLE)\n                    item.setData(\n                        container_item.version_locked,\n                        CONTAINER_VERSION_LOCKED_ROLE\n                    )\n                    container_model_items.append(item)\n\n                progress = progress_by_id[repre_id]\n                active_site_progress = \"{}%\".format(\n                    max(progress[\"active_site\"], 0) * 100\n                )\n                remote_site_progress = \"{}%\".format(\n                    max(progress[\"remote_site\"], 0) * 100\n                )\n                product_type_icon = get_qt_icon(repre_info.product_type_icon)\n                group_item = QtGui.QStandardItem()\n                group_item.setColumnCount(root_item.columnCount())\n                group_item.setData(group_name, QtCore.Qt.DisplayRole)\n                group_item.setData(group_name, ITEM_UNIQUE_NAME_ROLE)\n                group_item.setData(group_item_icon, QtCore.Qt.DecorationRole)\n                group_item.setData(group_item_font, QtCore.Qt.FontRole)\n                group_item.setData(repre_info.product_id, PRODUCT_ID_ROLE)\n                group_item.setData(repre_info.product_type, PRODUCT_TYPE_ROLE)\n                group_item.setData(product_type_icon, PRODUCT_TYPE_ICON_ROLE)\n                group_item.setData(is_latest, VERSION_IS_LATEST_ROLE)\n                group_item.setData(is_hero, VERSION_IS_HERO_ROLE)\n                group_item.setData(version_label, VERSION_LABEL_ROLE)\n                group_item.setData(len(container_items), COUNT_ROLE)\n                group_item.setData(status_name, STATUS_NAME_ROLE)\n                group_item.setData(status_short, STATUS_SHORT_ROLE)\n                group_item.setData(status_color, STATUS_COLOR_ROLE)\n                group_item.setData(status_icon, STATUS_ICON_ROLE)\n                group_item.setData(project_name, PROJECT_NAME_ROLE)\n\n                group_item.setData(\n                    active_site_progress, ACTIVE_SITE_PROGRESS_ROLE\n                )\n                group_item.setData(\n                    remote_site_progress, REMOTE_SITE_PROGRESS_ROLE\n                )\n                group_item.setData(active_site_icon, ACTIVE_SITE_ICON_ROLE)\n                group_item.setData(remote_site_icon, REMOTE_SITE_ICON_ROLE)\n                group_item.setData(False, IS_CONTAINER_ITEM_ROLE)\n\n                if version_color is not None:\n                    group_item.setData(version_color, VERSION_COLOR_ROLE)\n\n                if repre_info.product_group:\n                    group_item.setData(\n                        repre_info.product_group, PRODUCT_GROUP_NAME_ROLE\n                    )\n                    group_item.setData(group_icon, PRODUCT_GROUP_ICON_ROLE)\n\n                group_item.appendRows(container_model_items)\n                group_items.append(group_item)\n\n        if group_items:\n            root_item.appendRows(group_items)\n\n    def flags(self, index):\n        return QtCore.Qt.ItemIsEnabled | QtCore.Qt.ItemIsSelectable\n\n    def data(self, index, role):\n        if not index.isValid():\n            return\n\n        col = index.column()\n        if role == QtCore.Qt.DisplayRole:\n            role = self.display_role_by_column.get(col)\n            if role is None:\n                print(col, role)\n                return None\n\n        elif role == QtCore.Qt.DecorationRole:\n            role = self.decoration_role_by_column.get(col)\n            if role is None:\n                return None\n\n        elif role == QtCore.Qt.ForegroundRole:\n            role = self.foreground_role_by_column.get(col)\n            if role is None:\n                return None\n\n        if col != 0:\n            index = self.index(index.row(), 0, index.parent())\n\n        return super().data(index, role)\n\n    def set_hierarchy_view(self, state):\n        \"\"\"Set whether to display products in hierarchy view.\"\"\"\n        state = bool(state)\n\n        if state != self._hierarchy_view:\n            self._hierarchy_view = state\n\n    def get_outdated_item_ids(self, ignore_hero=True):\n        outdated_item_ids = []\n        root_item = self.invisibleRootItem()\n        for row in range(root_item.rowCount()):\n            group_item = root_item.child(row)\n            if group_item.data(VERSION_IS_LATEST_ROLE):\n                continue\n\n            if ignore_hero and group_item.data(VERSION_IS_HERO_ROLE):\n                continue\n\n            for idx in range(group_item.rowCount()):\n                item = group_item.child(idx)\n                outdated_item_ids.append(item.data(ITEM_ID_ROLE))\n        return outdated_item_ids\n\n    def _clear_items(self):\n        root_item = self.invisibleRootItem()\n        root_item.removeRows(0, root_item.rowCount())\n\n    def _get_status_data(self, project_name, status_name):\n        status_item = self._last_project_statuses[project_name].get(\n            status_name\n        )\n        status_icon = self._get_status_icon(\n            project_name, status_name, status_item\n        )\n        status_color = status_short = None\n        if status_item is not None:\n            status_color = status_item.color\n            status_short = status_item.short\n        return status_color, status_short, status_icon\n\n    def _get_status_icon(self, project_name, status_name, status_item):\n        icon = self._last_status_icons_by_name[project_name].get(status_name)\n        if icon is not None:\n            return icon\n\n        icon = None\n        if status_item is not None:\n            icon = get_qt_icon(\n                MaterialSymbolsIcon(\n                    status_item.icon,\n                    color=status_item.color\n                )\n            )\n        if icon is None:\n            icon = QtGui.QIcon()\n        self._last_status_icons_by_name[project_name][status_name] = icon\n        return icon\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/sceneinventory/model.html#client.ayon_core.tools.sceneinventory.model.InventoryModel.refresh","title":"<code>refresh(selected=None)</code>","text":"<p>Refresh the model</p> Source code in <code>client/ayon_core/tools/sceneinventory/model.py</code> <pre><code>def refresh(self, selected=None):\n    \"\"\"Refresh the model\"\"\"\n    # for debugging or testing, injecting items from outside\n    container_items = self._controller.get_container_items()\n\n    self._clear_items()\n\n    project_names = set()\n    repre_ids_by_project = collections.defaultdict(set)\n    version_items_by_project = collections.defaultdict(dict)\n    repre_info_by_id_by_project = collections.defaultdict(dict)\n    item_by_repre_id_by_project = collections.defaultdict(\n        lambda: collections.defaultdict(list))\n    for container_item in container_items:\n        # if (\n        #     selected is not None\n        #     and container_item.item_id not in selected\n        # ):\n        #     continue\n        project_name = container_item.project_name\n        representation_id = container_item.representation_id\n        project_names.add(project_name)\n        repre_ids_by_project[project_name].add(representation_id)\n        (\n            item_by_repre_id_by_project\n            [project_name]\n            [representation_id]\n        ).append(container_item)\n\n    for project_name, representation_ids in repre_ids_by_project.items():\n        repre_info = self._controller.get_representation_info_items(\n            project_name, representation_ids\n        )\n        repre_info_by_id_by_project[project_name] = repre_info\n\n        product_ids = {\n            repre_info.product_id\n            for repre_info in repre_info.values()\n            if repre_info.is_valid\n        }\n        version_items = self._controller.get_version_items(\n            project_name, product_ids\n        )\n        version_items_by_project[project_name] = version_items\n\n    # SiteSync addon information\n    progress_by_project = {\n        project_name: self._controller.get_representations_site_progress(\n            project_name, repre_ids\n        )\n        for project_name, repre_ids in repre_ids_by_project.items()\n    }\n\n    sites_info_by_project_name = {\n        project_name: self._controller.get_sites_information(project_name)\n        for project_name in project_names\n    }\n    site_icons = {\n        provider: get_qt_icon(icon_def)\n        for provider, icon_def in (\n            self._controller.get_site_provider_icons().items()\n        )\n    }\n    last_project_statuses = collections.defaultdict(dict)\n    for project_name in project_names:\n        status_items_by_name = {\n            status_item.name: status_item\n            for status_item in self._controller.get_project_status_items(\n                project_name\n            )\n        }\n        last_project_statuses[project_name] = status_items_by_name\n    self._last_project_statuses = last_project_statuses\n    self._last_status_icons_by_name = collections.defaultdict(dict)\n\n    group_item_icon = qtawesome.icon(\n        \"fa.folder\", color=self._default_icon_color\n    )\n    valid_item_icon = qtawesome.icon(\n        \"fa.file-o\", color=self._default_icon_color\n    )\n    invalid_item_icon = qtawesome.icon(\n        \"fa.exclamation-circle\", color=self._default_icon_color\n    )\n    group_icon = qtawesome.icon(\n        \"fa.object-group\", color=self._default_icon_color\n    )\n    group_item_font = QtGui.QFont()\n    group_item_font.setBold(True)\n\n    root_item = self.invisibleRootItem()\n    group_items = []\n    for project_name, items_by_repre_id in (\n        item_by_repre_id_by_project.items()\n    ):\n        sites_info = sites_info_by_project_name[project_name]\n        active_site_icon = site_icons.get(\n            sites_info[\"active_site_provider\"]\n        )\n        remote_site_icon = site_icons.get(\n            sites_info[\"remote_site_provider\"]\n        )\n\n        progress_by_id = progress_by_project[project_name]\n        repre_info_by_id = repre_info_by_id_by_project[project_name]\n        version_items_by_product_id = (\n            version_items_by_project[project_name]\n        )\n        for repre_id, container_items in items_by_repre_id.items():\n            repre_info = repre_info_by_id[repre_id]\n            version_color = None\n            if not repre_info.is_valid:\n                version_label = \"N/A\"\n                group_name = \"&lt; Entity N/A &gt;\"\n                item_icon = invalid_item_icon\n                is_latest = False\n                is_hero = False\n                status_name = None\n\n            else:\n                group_name = \"{}_{}: ({})\".format(\n                    repre_info.folder_path.rsplit(\"/\")[-1],\n                    repre_info.product_name,\n                    repre_info.representation_name\n                )\n                item_icon = valid_item_icon\n\n                version_items = (\n                    version_items_by_product_id[repre_info.product_id]\n                )\n                version_item = version_items[repre_info.version_id]\n                version_label = format_version(version_item.version)\n                is_hero = version_item.version &lt; 0\n                is_latest = version_item.is_latest\n                if not version_item.is_latest:\n                    version_color = self.OUTDATED_COLOR\n                status_name = version_item.status\n\n            (\n                status_color, status_short, status_icon\n            ) = self._get_status_data(project_name, status_name)\n\n            repre_name = (\n                repre_info.representation_name or\n                \"&lt;unknown representation&gt;\"\n            )\n            container_model_items = []\n            for container_item in container_items:\n                object_name = container_item.object_name or \"&lt;none&gt;\"\n                unique_name = repre_name + object_name\n                item = QtGui.QStandardItem()\n                item.setColumnCount(root_item.columnCount())\n                item.setData(container_item.namespace,\n                             QtCore.Qt.DisplayRole)\n                item.setData(self.GRAYOUT_COLOR, NAME_COLOR_ROLE)\n                item.setData(self.GRAYOUT_COLOR, VERSION_COLOR_ROLE)\n                item.setData(item_icon, QtCore.Qt.DecorationRole)\n                item.setData(repre_info.product_id, PRODUCT_ID_ROLE)\n                item.setData(container_item.item_id, ITEM_ID_ROLE)\n                item.setData(version_label, VERSION_LABEL_ROLE)\n                item.setData(container_item.loader_name, LOADER_NAME_ROLE)\n                item.setData(container_item.object_name, OBJECT_NAME_ROLE)\n                item.setData(True, IS_CONTAINER_ITEM_ROLE)\n                item.setData(unique_name, ITEM_UNIQUE_NAME_ROLE)\n                item.setData(\n                    container_item.version_locked,\n                    CONTAINER_VERSION_LOCKED_ROLE\n                )\n                container_model_items.append(item)\n\n            progress = progress_by_id[repre_id]\n            active_site_progress = \"{}%\".format(\n                max(progress[\"active_site\"], 0) * 100\n            )\n            remote_site_progress = \"{}%\".format(\n                max(progress[\"remote_site\"], 0) * 100\n            )\n            product_type_icon = get_qt_icon(repre_info.product_type_icon)\n            group_item = QtGui.QStandardItem()\n            group_item.setColumnCount(root_item.columnCount())\n            group_item.setData(group_name, QtCore.Qt.DisplayRole)\n            group_item.setData(group_name, ITEM_UNIQUE_NAME_ROLE)\n            group_item.setData(group_item_icon, QtCore.Qt.DecorationRole)\n            group_item.setData(group_item_font, QtCore.Qt.FontRole)\n            group_item.setData(repre_info.product_id, PRODUCT_ID_ROLE)\n            group_item.setData(repre_info.product_type, PRODUCT_TYPE_ROLE)\n            group_item.setData(product_type_icon, PRODUCT_TYPE_ICON_ROLE)\n            group_item.setData(is_latest, VERSION_IS_LATEST_ROLE)\n            group_item.setData(is_hero, VERSION_IS_HERO_ROLE)\n            group_item.setData(version_label, VERSION_LABEL_ROLE)\n            group_item.setData(len(container_items), COUNT_ROLE)\n            group_item.setData(status_name, STATUS_NAME_ROLE)\n            group_item.setData(status_short, STATUS_SHORT_ROLE)\n            group_item.setData(status_color, STATUS_COLOR_ROLE)\n            group_item.setData(status_icon, STATUS_ICON_ROLE)\n            group_item.setData(project_name, PROJECT_NAME_ROLE)\n\n            group_item.setData(\n                active_site_progress, ACTIVE_SITE_PROGRESS_ROLE\n            )\n            group_item.setData(\n                remote_site_progress, REMOTE_SITE_PROGRESS_ROLE\n            )\n            group_item.setData(active_site_icon, ACTIVE_SITE_ICON_ROLE)\n            group_item.setData(remote_site_icon, REMOTE_SITE_ICON_ROLE)\n            group_item.setData(False, IS_CONTAINER_ITEM_ROLE)\n\n            if version_color is not None:\n                group_item.setData(version_color, VERSION_COLOR_ROLE)\n\n            if repre_info.product_group:\n                group_item.setData(\n                    repre_info.product_group, PRODUCT_GROUP_NAME_ROLE\n                )\n                group_item.setData(group_icon, PRODUCT_GROUP_ICON_ROLE)\n\n            group_item.appendRows(container_model_items)\n            group_items.append(group_item)\n\n    if group_items:\n        root_item.appendRows(group_items)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/sceneinventory/model.html#client.ayon_core.tools.sceneinventory.model.InventoryModel.set_hierarchy_view","title":"<code>set_hierarchy_view(state)</code>","text":"<p>Set whether to display products in hierarchy view.</p> Source code in <code>client/ayon_core/tools/sceneinventory/model.py</code> <pre><code>def set_hierarchy_view(self, state):\n    \"\"\"Set whether to display products in hierarchy view.\"\"\"\n    state = bool(state)\n\n    if state != self._hierarchy_view:\n        self._hierarchy_view = state\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/sceneinventory/select_version_dialog.html","title":"select_version_dialog","text":""},{"location":"autoapi/client/ayon_core/tools/sceneinventory/view.html","title":"view","text":""},{"location":"autoapi/client/ayon_core/tools/sceneinventory/view.html#client.ayon_core.tools.sceneinventory.view.SceneInventoryView","title":"<code>SceneInventoryView</code>","text":"<p>               Bases: <code>QTreeView</code></p> Source code in <code>client/ayon_core/tools/sceneinventory/view.py</code> <pre><code>class SceneInventoryView(QtWidgets.QTreeView):\n    data_changed = QtCore.Signal()\n    hierarchy_view_changed = QtCore.Signal(bool)\n\n    def __init__(self, controller, parent):\n        super().__init__(parent=parent)\n\n        # view settings\n        self.setIndentation(12)\n        self.setAlternatingRowColors(True)\n        self.setSortingEnabled(True)\n        self.setSelectionMode(QtWidgets.QAbstractItemView.ExtendedSelection)\n        self.setContextMenuPolicy(QtCore.Qt.CustomContextMenu)\n\n        model = InventoryModel(controller)\n        proxy_model = FilterProxyModel()\n        proxy_model.setSourceModel(model)\n        proxy_model.setSortCaseSensitivity(QtCore.Qt.CaseInsensitive)\n\n        self.setModel(proxy_model)\n\n        version_delegate = VersionDelegate()\n        status_delegate = StatusDelegate(\n            STATUS_NAME_ROLE,\n            STATUS_SHORT_ROLE,\n            STATUS_COLOR_ROLE,\n            STATUS_ICON_ROLE,\n        )\n        for col, delegate in (\n            (model.version_col, version_delegate),\n            (model.status_col, status_delegate),\n        ):\n            self.setItemDelegateForColumn(col, delegate)\n\n        # set some nice default widths for the view\n        for col, width in model.width_by_column.items():\n            self.setColumnWidth(col, width)\n\n        sync_enabled = controller.is_sitesync_enabled()\n        self.setColumnHidden(model.active_site_col, not sync_enabled)\n        self.setColumnHidden(model.remote_site_col, not sync_enabled)\n\n        self.customContextMenuRequested.connect(self._show_right_mouse_menu)\n\n        self._model = model\n        self._proxy_model = proxy_model\n        self._version_delegate = version_delegate\n        self._status_delegate = status_delegate\n\n        self._hierarchy_view = False\n        self._selected = None\n\n        self._controller = controller\n\n    def refresh(self):\n        with preserve_expanded_rows(\n            tree_view=self,\n            role=ITEM_UNIQUE_NAME_ROLE\n        ):\n            with preserve_selection(\n                tree_view=self,\n                role=ITEM_UNIQUE_NAME_ROLE,\n                current_index=False\n            ):\n                kwargs = {}\n                # TODO do not touch view's inner attribute\n                if self._hierarchy_view:\n                    kwargs[\"selected\"] = self._selected\n                self._model.refresh(**kwargs)\n\n    def set_hierarchy_view(self, enabled):\n        self._proxy_model.set_hierarchy_view(enabled)\n        self._model.set_hierarchy_view(enabled)\n\n    def set_text_filter(self, text_filter):\n        if hasattr(self._proxy_model, \"setFilterRegularExpression\"):\n            self._proxy_model.setFilterRegularExpression(text_filter)\n        else:\n            self._proxy_model.setFilterRegExp(text_filter)\n\n    def set_filter_outdated(self, enabled):\n        self._proxy_model.set_filter_outdated(enabled)\n\n    def get_selected_indexes(self):\n        \"\"\"Get the selected rows\"\"\"\n        indexes, _ = self._get_selected_indexes()\n        return indexes\n\n    def get_selected_item_ids(self):\n        return self._get_item_ids_from_indexes(\n            self.get_selected_indexes()\n        )\n\n    def get_selected_container_indexes(self):\n        return self._get_container_indexes(\n            self.get_selected_indexes()\n        )\n\n    def _get_selected_indexes(self):\n        selection_model = self.selectionModel()\n        indexes = selection_model.selectedRows()\n        active = self.currentIndex()\n        active = active.sibling(active.row(), 0)\n        if active not in indexes:\n            indexes.append(active)\n        return indexes, active\n\n    def _get_item_ids_from_indexes(self, indexes):\n        return {\n            index.data(ITEM_ID_ROLE)\n            for index in self._get_container_indexes(indexes)\n        }\n\n    def _set_hierarchy_view(self, enabled):\n        if enabled == self._hierarchy_view:\n            return\n        self._hierarchy_view = enabled\n        self.hierarchy_view_changed.emit(enabled)\n\n    def _enter_hierarchy(self, item_ids):\n        self._selected = set(item_ids)\n        self._set_hierarchy_view(True)\n        self.data_changed.emit()\n        self.expandToDepth(1)\n        self.setStyleSheet(\"border-color: #fb9c15;\")\n\n    def _leave_hierarchy(self):\n        self._set_hierarchy_view(False)\n        self.data_changed.emit()\n        self.setStyleSheet(\"\")\n\n    def _build_item_menu_for_selection(self, menu, indexes, active_index):\n        item_ids = {\n            index.data(ITEM_ID_ROLE)\n            for index in indexes\n        }\n        item_ids.discard(None)\n        if not item_ids:\n            return\n\n        container_items_by_id = self._controller.get_container_items_by_id(\n            item_ids\n        )\n\n        active_repre_id = None\n        if active_index is not None:\n            for index in self._get_container_indexes({active_index}):\n                item_id = index.data(ITEM_ID_ROLE)\n                container_item = container_items_by_id[item_id]\n                active_repre_id = container_item.representation_id\n                break\n        repre_ids_by_project = collections.defaultdict(set)\n        for container_item in container_items_by_id.values():\n            repre_id = container_item.representation_id\n            project_name = container_item.project_name\n            repre_ids_by_project[project_name].add(repre_id)\n\n        repre_info_by_project = {}\n        repre_ids_by_project_name = {}\n        version_ids_by_project = {}\n        product_ids_by_project = {}\n        for project_name, repre_ids in repre_ids_by_project.items():\n            repres_info = self._controller.get_representation_info_items(\n                project_name, repre_ids\n            )\n\n            repre_info_by_project[project_name] = repres_info\n            repre_ids = set()\n            version_ids = set()\n            product_ids = set()\n            for repre_id, repre_info in repres_info.items():\n                if not repre_info.is_valid:\n                    continue\n                repre_ids.add(repre_id)\n                version_ids.add(repre_info.version_id)\n                product_ids.add(repre_info.product_id)\n\n            repre_ids_by_project_name[project_name] = repre_ids\n            version_ids_by_project[project_name] = version_ids\n            product_ids_by_project[project_name] = product_ids\n\n        # Exclude items that are \"NOT FOUND\" since setting versions, updating\n        # and removal won't work for those items.\n        filtered_items = []\n        for container_item in container_items_by_id.values():\n            project_name = container_item.project_name\n            repre_id = container_item.representation_id\n            repre_info_by_id = repre_info_by_project.get(project_name, {})\n            repre_info = repre_info_by_id.get(repre_id)\n            if repre_info and repre_info.is_valid:\n                filtered_items.append(container_item)\n\n        # remove\n        remove_icon = qtawesome.icon(\"fa.remove\", color=DEFAULT_COLOR)\n        remove_action = QtWidgets.QAction(remove_icon, \"Remove items\", menu)\n        remove_action.triggered.connect(\n            lambda: self._show_remove_warning_dialog(item_ids))\n\n        if not filtered_items:\n            # Keep remove action for invalid items\n            menu.addAction(remove_action)\n            return\n\n        version_items_by_project = {\n            project_name: self._controller.get_version_items(\n                project_name, product_ids\n            )\n            for project_name, product_ids in product_ids_by_project.items()\n        }\n\n        has_outdated = False\n        has_loaded_hero_versions = False\n        has_available_hero_version = False\n        has_outdated_approved = False\n        last_version_by_product_id = {}\n        for project_name, version_items_by_product_id in (\n            version_items_by_project.items()\n        ):\n            version_ids = version_ids_by_project[project_name]\n            for product_id, version_items_by_id in (\n                version_items_by_product_id.items()\n            ):\n                _has_outdated_approved = False\n                _last_approved_version_item = None\n                for version_item in version_items_by_id.values():\n                    if version_item.is_hero:\n                        has_available_hero_version = True\n\n                    elif version_item.is_last_approved:\n                        _last_approved_version_item = version_item\n                        _has_outdated_approved = True\n\n                    if version_item.version_id not in version_ids:\n                        continue\n\n                    if version_item.is_hero:\n                        has_loaded_hero_versions = True\n                    elif not version_item.is_latest:\n                        has_outdated = True\n\n                if (\n                    _has_outdated_approved\n                    and _last_approved_version_item is not None\n                ):\n                    last_version_by_product_id[product_id] = (\n                        _last_approved_version_item\n                    )\n                    has_outdated_approved = True\n\n        switch_to_versioned = None\n        if has_loaded_hero_versions:\n            update_icon = qtawesome.icon(\n                \"fa.asterisk\",\n                color=DEFAULT_COLOR\n            )\n            switch_to_versioned = QtWidgets.QAction(\n                update_icon,\n                \"Switch to versioned\",\n                menu\n            )\n            switch_to_versioned.triggered.connect(\n                lambda: self._on_switch_to_versioned(item_ids)\n            )\n\n        update_to_last_approved_action = None\n        approved_version_by_item_id = {}\n        if has_outdated_approved:\n            for container_item in container_items_by_id.values():\n                project_name = container_item.project_name\n                repre_id = container_item.representation_id\n                repre_info = repre_info_by_project[project_name][repre_id]\n                if not repre_info or not repre_info.is_valid:\n                    continue\n                version_item = last_version_by_product_id.get(\n                    repre_info.product_id\n                )\n                if (\n                    version_item is None\n                    or version_item.version_id == repre_info.version_id\n                ):\n                    continue\n                approved_version_by_item_id[container_item.item_id] = (\n                    version_item.version\n                )\n\n        if approved_version_by_item_id:\n            update_icon = qtawesome.icon(\n                \"fa.angle-double-up\",\n                color=\"#00f0b4\"\n            )\n            update_to_last_approved_action = QtWidgets.QAction(\n                update_icon,\n                \"Update to last approved\",\n                menu\n            )\n            update_to_last_approved_action.triggered.connect(\n                lambda: self._update_containers_to_approved_versions(\n                    approved_version_by_item_id\n                )\n            )\n\n        update_to_latest_action = None\n        if has_outdated or has_loaded_hero_versions:\n            update_icon = qtawesome.icon(\n                \"fa.angle-double-up\",\n                color=DEFAULT_COLOR\n            )\n            update_to_latest_action = QtWidgets.QAction(\n                update_icon,\n                \"Update to latest\",\n                menu\n            )\n            update_to_latest_action.triggered.connect(\n                lambda: self._update_containers_to_version(\n                    item_ids, version=-1\n                )\n            )\n\n        change_to_hero = None\n        if has_available_hero_version:\n            # TODO change icon\n            change_icon = qtawesome.icon(\n                \"fa.asterisk\",\n                color=\"#00b359\"\n            )\n            change_to_hero = QtWidgets.QAction(\n                change_icon,\n                \"Change to hero\",\n                menu\n            )\n            change_to_hero.triggered.connect(\n                lambda: self._update_containers_to_version(\n                    item_ids, version=HeroVersionType(-1)\n                )\n            )\n\n        # set version\n        set_version_action = None\n        if active_repre_id is not None:\n            set_version_icon = qtawesome.icon(\n                \"fa.hashtag\", color=DEFAULT_COLOR\n            )\n            set_version_action = QtWidgets.QAction(\n                set_version_icon,\n                \"Set version\",\n                menu\n            )\n            set_version_action.triggered.connect(\n                lambda: self._show_version_dialog(item_ids, active_repre_id)\n            )\n\n        # switch folder\n        switch_folder_icon = qtawesome.icon(\"fa.sitemap\", color=DEFAULT_COLOR)\n        switch_folder_action = QtWidgets.QAction(\n            switch_folder_icon,\n            \"Switch Folder\",\n            menu\n        )\n        switch_folder_action.triggered.connect(\n            lambda: self._show_switch_dialog(item_ids))\n\n        # add the actions\n        if switch_to_versioned:\n            menu.addAction(switch_to_versioned)\n\n        if update_to_last_approved_action:\n            menu.addAction(update_to_last_approved_action)\n\n        if update_to_latest_action:\n            menu.addAction(update_to_latest_action)\n\n        if change_to_hero:\n            menu.addAction(change_to_hero)\n\n        if set_version_action is not None:\n            menu.addAction(set_version_action)\n        menu.addAction(switch_folder_action)\n\n        menu.addSeparator()\n\n        menu.addAction(remove_action)\n\n        self._handle_sitesync(menu, repre_ids_by_project_name)\n\n    def _handle_sitesync(self, menu, repre_ids_by_project_name):\n        \"\"\"Adds actions for download/upload when SyncServer is enabled\n\n        Args:\n            menu (OptionMenu)\n            repre_ids_by_project_name (Dict[str, Set[str]]): Representation\n                ids by project name.\n\n        Returns:\n            (OptionMenu)\n        \"\"\"\n\n        if not self._controller.is_sitesync_enabled():\n            return\n\n        if not repre_ids_by_project_name:\n            return\n\n        menu.addSeparator()\n\n        download_icon = qtawesome.icon(\"fa.download\", color=DEFAULT_COLOR)\n        download_active_action = QtWidgets.QAction(\n            download_icon,\n            \"Download\",\n            menu\n        )\n        download_active_action.triggered.connect(\n            lambda: self._add_sites(\n                repre_ids_by_project_name, \"active_site\"\n            )\n        )\n\n        upload_icon = qtawesome.icon(\"fa.upload\", color=DEFAULT_COLOR)\n        upload_remote_action = QtWidgets.QAction(\n            upload_icon,\n            \"Upload\",\n            menu\n        )\n        upload_remote_action.triggered.connect(\n            lambda: self._add_sites(\n                repre_ids_by_project_name, \"remote_site\"\n            )\n        )\n\n        menu.addAction(download_active_action)\n        menu.addAction(upload_remote_action)\n\n    def _add_sites(self, repre_ids_by_project_name, site_type):\n        \"\"\"(Re)sync all 'repre_ids' to specific site.\n\n        It checks if opposite site has fully available content to limit\n        accidents. (ReSync active when no remote &gt;&gt; losing active content)\n\n        Args:\n            repre_ids_by_project_name (Dict[str, Set[str]]): Representation\n                ids by project name.\n            site_type (Literal[active_site, remote_site]): Site type.\n\n        \"\"\"\n        for project_name, repre_ids in repre_ids_by_project_name.items():\n            self._controller.resync_representations(\n                project_name, repre_ids, site_type\n            )\n\n        self.data_changed.emit()\n\n    def _build_item_menu(self, indexes=None, active_index=None):\n        \"\"\"Create menu for the selected items\"\"\"\n        menu = QtWidgets.QMenu(self)\n\n        # These two actions should be able to work without selection\n        # expand all items\n        expand_all_action = QtWidgets.QAction(menu, text=\"Expand all items\")\n        expand_all_action.triggered.connect(self.expandAll)\n\n        # collapse all items\n        collapse_action = QtWidgets.QAction(menu, text=\"Collapse all items\")\n        collapse_action.triggered.connect(self.collapseAll)\n\n        if not indexes:\n            indexes = []\n\n        item_ids = {\n            index.data(ITEM_ID_ROLE)\n            for index in indexes\n        }\n        item_ids.discard(None)\n\n        # add the actions\n        self._build_item_menu_for_selection(menu, indexes, active_index)\n\n        menu.addAction(expand_all_action)\n        menu.addAction(collapse_action)\n\n        custom_actions = self._get_custom_actions(item_ids)\n        if custom_actions:\n            submenu = QtWidgets.QMenu(\"Actions\", self)\n            for action in custom_actions:\n                color = action.color or DEFAULT_COLOR\n                icon_def = action.icon\n                if isinstance(action.icon, str):\n                    icon_def = {\n                        \"type\": \"awesome-font\",\n                        \"name\": icon_def,\n                        \"color\": color,\n                    }\n                icon = get_qt_icon(icon_def)\n                action_item = QtWidgets.QAction(icon, action.label, submenu)\n                action_item.triggered.connect(\n                    partial(\n                        self._process_custom_action, action, item_ids\n                    )\n                )\n\n                submenu.addAction(action_item)\n\n            menu.addMenu(submenu)\n\n        # go back to flat view\n        back_to_flat_action = None\n        if self._hierarchy_view:\n            back_to_flat_icon = qtawesome.icon(\"fa.list\", color=DEFAULT_COLOR)\n            back_to_flat_action = QtWidgets.QAction(\n                back_to_flat_icon,\n                \"Back to Full-View\",\n                menu\n            )\n            back_to_flat_action.triggered.connect(self._leave_hierarchy)\n\n        # send items to hierarchy view\n        enter_hierarchy_icon = qtawesome.icon(\"fa.indent\", color=\"#d8d8d8\")\n        enter_hierarchy_action = QtWidgets.QAction(\n            enter_hierarchy_icon,\n            \"Cherry-Pick (Hierarchy)\",\n            menu\n        )\n        enter_hierarchy_action.triggered.connect(\n            lambda: self._enter_hierarchy(item_ids))\n\n        if indexes:\n            menu.addAction(enter_hierarchy_action)\n\n        if back_to_flat_action is not None:\n            menu.addAction(back_to_flat_action)\n\n        return menu\n\n    def _get_custom_actions(self, item_ids):\n        \"\"\"Get the registered Inventory Actions\n\n        Args:\n            item_ids (Iterable[str]): collection of containers\n\n        Returns:\n            list: collection of filter and initialized actions\n        \"\"\"\n\n        def sorter(Plugin):\n            \"\"\"Sort based on order attribute of the plugin\"\"\"\n            return Plugin.order\n\n        # Fedd an empty dict if no selection, this will ensure the compat\n        # lookup always work, so plugin can interact with Scene Inventory\n        # reversely.\n        if not item_ids:\n            containers = [dict()]\n        else:\n            containers_by_id = self._controller.get_containers_by_item_ids(\n                item_ids\n            )\n            containers = list(containers_by_id.values())\n\n        # Check which action will be available in the menu\n        Plugins = discover_inventory_actions()\n        compatible = [\n            p()\n            for p in Plugins\n            if any(p.is_compatible(c) for c in containers)\n        ]\n\n        return sorted(compatible, key=sorter)\n\n    def _process_custom_action(self, action, item_ids):\n        \"\"\"Run action and if results are returned positive update the view\n\n        If the result is list or dict, will select view items by the result.\n\n        Args:\n            action (InventoryAction): Inventory Action instance\n            item_ids (Iterable[str]): Data of currently selected items\n\n        Returns:\n            None\n        \"\"\"\n        containers_by_id = self._controller.get_containers_by_item_ids(\n            item_ids\n        )\n        result = action.process(list(containers_by_id.values()))\n        if result:\n            self.data_changed.emit()\n\n            if isinstance(result, (list, set)):\n                self._select_items_by_action(result)\n\n            elif isinstance(result, dict):\n                self._select_items_by_action(\n                    result[\"objectNames\"], result[\"options\"]\n                )\n\n    def _select_items_by_action(self, object_names, options=None):\n        \"\"\"Select view items by the result of action\n\n        Args:\n            object_names (list or set): A list/set of container object name\n            options (dict): GUI operation options.\n\n        Returns:\n            None\n\n        \"\"\"\n        options = options or dict()\n\n        if options.get(\"clear\", True):\n            self.clearSelection()\n\n        model = self.model()\n        object_names = set(object_names)\n        if self._hierarchy_view:\n            item_ids = set()\n            for index in iter_model_rows(model):\n                if not index.data(IS_CONTAINER_ITEM_ROLE):\n                    continue\n                if index.data(OBJECT_NAME_ROLE) in object_names:\n                    item_id = index.data(ITEM_ID_ROLE)\n                    if item_id:\n                        item_ids.add(item_id)\n\n            if not self._selected.issuperset(item_ids):\n                # If any container not in current cherry-picked view, update\n                # view before selecting them.\n                self._selected.update(item_ids)\n                self.data_changed.emit()\n\n        selection_model = self.selectionModel()\n\n        select_mode = {\n            \"select\": QtCore.QItemSelectionModel.Select,\n            \"deselect\": QtCore.QItemSelectionModel.Deselect,\n            \"toggle\": QtCore.QItemSelectionModel.Toggle,\n        }[options.get(\"mode\", \"select\")]\n\n        for index in iter_model_rows(model):\n            if not index.data(IS_CONTAINER_ITEM_ROLE):\n                continue\n            name = index.data(OBJECT_NAME_ROLE)\n            if name in object_names:\n                self.scrollTo(index)  # Ensure item is visible\n                flags = select_mode | QtCore.QItemSelectionModel.Rows\n                selection_model.select(index, flags)\n\n                object_names.remove(name)\n\n            if len(object_names) == 0:\n                break\n\n    def _show_right_mouse_menu(self, pos):\n        \"\"\"Display the menu when at the position of the item clicked\"\"\"\n\n        globalpos = self.viewport().mapToGlobal(pos)\n\n        if not self.selectionModel().hasSelection():\n            # Build menu without selection, feed an empty list\n            menu = self._build_item_menu()\n            menu.exec_(globalpos)\n            return\n\n        indexes, active_index = self._get_selected_indexes()\n\n        # Extend to the sub-items\n        all_indexes = self._extend_to_children(indexes)\n\n        menu = self._build_item_menu(all_indexes, active_index)\n        menu.exec_(globalpos)\n\n    def _get_container_indexes(self, indexes):\n        container_indexes = []\n        indexes_queue = collections.deque()\n        indexes_queue.extend(indexes)\n        # Ignore already added containers\n        items_ids = set()\n        while indexes_queue:\n            index = indexes_queue.popleft()\n            if index.data(IS_CONTAINER_ITEM_ROLE):\n                item_id = index.data(ITEM_ID_ROLE)\n                if item_id in items_ids:\n                    continue\n                items_ids.add(item_id)\n                container_indexes.append(index)\n                continue\n            model = index.model()\n            for row in range(model.rowCount(index)):\n                child = model.index(row, 0, parent=index)\n                indexes_queue.append(child)\n        return container_indexes\n\n    def _extend_to_children(self, indexes):\n        \"\"\"Extend the indices to the children indices.\n\n        Top-level indices are extended to its children indices. Sub-items\n        are kept as is.\n\n        Args:\n            indexes (list): The indices to extend.\n\n        Returns:\n            list: The children indices\n\n        \"\"\"\n        def get_children(index):\n            model = index.model()\n            for row in range(model.rowCount(index)):\n                yield model.index(row, 0, parent=index)\n\n        subitems = set()\n        for index in indexes:\n            if index.parent().isValid() and index not in subitems:\n                subitems.add(index)\n\n                if self._hierarchy_view:\n                    # Assume this is a group item\n                    for child in get_children(index):\n                        subitems.add(child)\n            else:\n                # is top level item\n                for child in get_children(index):\n                    subitems.add(child)\n\n        return list(subitems)\n\n    def _show_version_dialog(self, item_ids, active_repre_id):\n        \"\"\"Create a dialog with the available versions for the selected file\n\n        Args:\n            item_ids (Iterable[str]): List of item ids to run the\n                \"set_version\" for.\n            active_repre_id (Union[str, None]): Active representation id.\n\n        Returns:\n            None\n\n        \"\"\"\n        container_items_by_id = self._controller.get_container_items_by_id(\n            item_ids\n        )\n        project_names = set()\n        repre_ids_by_project = collections.defaultdict(set)\n        for container_item in container_items_by_id.values():\n            repre_id = container_item.representation_id\n            project_name = container_item.project_name\n            project_names.add(project_name)\n            repre_ids_by_project[project_name].add(repre_id)\n\n        # active_project_name = None\n        active_repre_info = None\n        repre_info_by_project = {}\n        version_items_by_project = {}\n        for project_name, repre_ids in repre_ids_by_project.items():\n            repres_info = self._controller.get_representation_info_items(\n                project_name, repre_ids\n            )\n            if active_repre_info is None:\n                # active_project_name = project_name\n                active_repre_info = repres_info.get(active_repre_id)\n\n            product_ids = {\n                repre_info.product_id\n                for repre_info in repres_info.values()\n                if repre_info.is_valid\n            }\n            version_items_by_product_id = self._controller.get_version_items(\n                project_name, product_ids\n            )\n\n            repre_info_by_project[project_name] = repres_info\n            version_items_by_project[project_name] = (\n                version_items_by_product_id\n            )\n\n        active_version_id = active_repre_info.version_id\n        # active_product_id = active_repre_info.product_id\n\n        versions = set()\n        product_ids = set()\n        version_items = []\n        product_ids_by_version_by_project = {}\n        for project_name, version_items_by_product_id in (\n            version_items_by_project.items()\n        ):\n            product_ids_by_version = collections.defaultdict(set)\n            product_ids_by_version_by_project[project_name] = (\n                product_ids_by_version\n            )\n            for version_items_by_id in version_items_by_product_id.values():\n                for version_item in version_items_by_id.values():\n                    version = version_item.version\n                    _prod_version = version\n                    if _prod_version &lt; 0:\n                        _prod_version = -1\n                    product_ids_by_version[_prod_version].add(\n                        version_item.product_id\n                    )\n                    product_ids.add(version_item.product_id)\n                    if version in versions:\n                        continue\n                    versions.add(version)\n                    version_items.append(version_item)\n\n        def version_sorter(item):\n            hero_value = 0\n            i_version = item.version\n            if i_version &lt; 0:\n                hero_value = 1\n                i_version = abs(i_version)\n            return i_version, hero_value\n\n        version_items.sort(key=version_sorter, reverse=True)\n        show_statuses = len(product_ids) == 1\n        status_items_by_name = {}\n        if show_statuses:\n            status_items_by_name = {\n                status_item.name: status_item\n                for status_item in self._controller.get_project_status_items()\n            }\n\n        version_options = []\n        active_version_idx = 0\n        for idx, version_item in enumerate(version_items):\n            version = version_item.version\n            label = format_version(version)\n            if version_item.version_id == active_version_id:\n                active_version_idx = idx\n\n            status_name = version_item.status\n            status_short = None\n            status_color = None\n            status_icon = None\n            status_item = status_items_by_name.get(status_name)\n            if status_item:\n                status_short = status_item.short\n                status_color = status_item.color\n                status_icon = status_item.icon\n            version_options.append(\n                VersionOption(\n                    version,\n                    label,\n                    status_name,\n                    status_short,\n                    status_color,\n                    status_icon,\n                )\n            )\n\n        version_option = SelectVersionDialog.ask_for_version(\n            version_options,\n            active_version_idx,\n            show_statuses=show_statuses,\n            parent=self\n        )\n        if version_option is None:\n            return\n\n        product_version = version = version_option.version\n        if version &lt; 0:\n            product_version = -1\n            version = HeroVersionType(version)\n\n        filtered_item_ids = set()\n        for container_item in container_items_by_id.values():\n            project_name = container_item.project_name\n            product_ids_by_version = (\n                product_ids_by_version_by_project[project_name]\n            )\n            product_ids = product_ids_by_version[product_version]\n            repre_id = container_item.representation_id\n            repre_info = repre_info_by_project[project_name][repre_id]\n            if repre_info.product_id in product_ids:\n                filtered_item_ids.add(container_item.item_id)\n\n        self._update_containers_to_version(\n            filtered_item_ids, version\n        )\n\n    def _show_switch_dialog(self, item_ids):\n        \"\"\"Display Switch dialog\"\"\"\n        container_items_by_id = self._controller.get_container_items_by_id(\n            item_ids\n        )\n        container_ids_by_project_name = collections.defaultdict(set)\n        for container_id, container_item in container_items_by_id.items():\n            project_name = container_item.project_name\n            container_ids_by_project_name[project_name].add(container_id)\n\n        for project_name, container_ids in (\n            container_ids_by_project_name.items()\n        ):\n            containers_by_id = self._controller.get_containers_by_item_ids(\n                container_ids\n            )\n            dialog = SwitchAssetDialog(\n                self._controller,\n                project_name,\n                list(containers_by_id.values()),\n                self\n            )\n            dialog.switched.connect(self.data_changed.emit)\n            dialog.show()\n\n    def _show_remove_warning_dialog(self, item_ids):\n        \"\"\"Prompt a dialog to inform the user the action will remove items\"\"\"\n        containers_by_id = self._controller.get_containers_by_item_ids(\n            item_ids\n        )\n        containers = list(containers_by_id.values())\n        accept = QtWidgets.QMessageBox.Ok\n        buttons = accept | QtWidgets.QMessageBox.Cancel\n\n        state = QtWidgets.QMessageBox.question(\n            self,\n            \"Are you sure?\",\n            f\"Are you sure you want to remove {len(containers)} item(s)\",\n            buttons=buttons,\n            defaultButton=accept\n        )\n\n        if state != accept:\n            return\n\n        for container in containers:\n            remove_container(container)\n        self.data_changed.emit()\n\n    def _show_version_error_dialog(self, version, item_ids, exception):\n        \"\"\"Shows QMessageBox when version switch doesn't work\n\n        Args:\n            version: str or int or None\n            item_ids (Iterable[str]): List of item ids to run the\n            exception (Exception): Exception that occurred\n        \"\"\"\n        if version == -1:\n            version_str = \"latest\"\n        elif isinstance(version, HeroVersionType):\n            version_str = \"hero\"\n        elif isinstance(version, int):\n            version_str = \"v{:03d}\".format(version)\n        else:\n            version_str = version\n\n        dialog = QtWidgets.QMessageBox(self)\n        dialog.setIcon(QtWidgets.QMessageBox.Warning)\n        dialog.setStyleSheet(style.load_stylesheet())\n        dialog.setWindowTitle(\"Update failed\")\n\n        switch_btn = dialog.addButton(\n            \"Switch Folder\",\n            QtWidgets.QMessageBox.ActionRole\n        )\n        switch_btn.clicked.connect(lambda: self._show_switch_dialog(item_ids))\n\n        dialog.addButton(QtWidgets.QMessageBox.Cancel)\n\n        msg = (\n            \"Version update to '{}' failed with the following error:\\n\"\n            \"{}.\"\n            \"\\n\\nPlease update to version with a valid representation\"\n            \" OR \\n use 'Switch Folder' button to change folder.\"\n        ).format(version_str, exception)\n        dialog.setText(msg)\n        dialog.exec_()\n\n    def update_all(self):\n        \"\"\"Update all items that are currently 'outdated' in the view\"\"\"\n        # Get the source model through the proxy model\n        item_ids = self._model.get_outdated_item_ids()\n        if not item_ids:\n            log.info(\"Nothing to update.\")\n            return\n\n        # Trigger update to latest\n        self._update_containers_to_version(item_ids, version=-1)\n\n    def _on_switch_to_versioned(self, item_ids):\n        # Get container items by ID\n        containers_items_by_id = self._controller.get_container_items_by_id(\n            item_ids)\n        # Extract project names and their corresponding representation IDs\n        repre_ids_by_project = collections.defaultdict(set)\n        for container_item in containers_items_by_id.values():\n            project_name = container_item.project_name\n            repre_id = container_item.representation_id\n            repre_ids_by_project[project_name].add(repre_id)\n\n        # Get representation info items by ID\n        repres_info_by_project = {}\n        version_items_by_project = {}\n        for project_name, repre_ids in repre_ids_by_project.items():\n            repre_info_by_id = self._controller.get_representation_info_items(\n                project_name, repre_ids\n            )\n            repres_info_by_project[project_name] = repre_info_by_id\n\n            product_ids = {\n                repre_info.product_id\n                for repre_info in repre_info_by_id.values()\n                if repre_info.is_valid\n            }\n            version_items_by_product_id = self._controller.get_version_items(\n                project_name, product_ids\n            )\n            version_items_by_project[project_name] = (\n                version_items_by_product_id\n            )\n\n        update_containers = []\n        update_versions = []\n        for container_item in containers_items_by_id.values():\n            project_name = container_item.project_name\n            repre_id = container_item.representation_id\n\n            repre_info_by_id = repres_info_by_project[project_name]\n            repre_info = repre_info_by_id[repre_id]\n\n            version_items_by_product_id = (\n                version_items_by_project[project_name]\n            )\n            product_id = repre_info.product_id\n            version_items_by_id = version_items_by_product_id[product_id]\n            version_item = version_items_by_id.get(repre_info.version_id, {})\n            if not version_item or not version_item.is_hero:\n                continue\n\n            version = abs(version_item.version)\n            version_found = False\n            for version_item in version_items_by_id.values():\n                if version_item.is_hero:\n                    continue\n                if version_item.version == version:\n                    version_found = True\n                    break\n\n            if not version_found:\n                continue\n\n            update_containers.append(container_item.item_id)\n            update_versions.append(version)\n\n            # Specify version per item to update to\n            self._update_containers(update_containers, update_versions)\n\n    def _update_containers(self, item_ids, versions):\n        \"\"\"Helper to update items to given version (or version per item)\n\n        If at least one item is specified this will always try to refresh\n        the inventory even if errors occurred on any of the items.\n\n        Arguments:\n            item_ids (Iterable[str]): Items to update\n            versions (Iterable[Union[int, HeroVersion]]): Version to set to.\n                This can be a list specifying a version for each item.\n                Like `update_container` version -1 sets the latest version\n                and HeroTypeVersion instances set the hero version.\n\n        \"\"\"\n\n        # We allow a unique version to be specified per item. In that case\n        # the length must match with the items\n        assert len(item_ids) == len(versions), (\n            \"Number of items mismatches number of versions: \"\n            f\"{len(item_ids)} items - {len(versions)} versions\"\n        )\n\n        # Trigger update to latest\n        containers_by_id = self._controller.get_containers_by_item_ids(\n            item_ids\n        )\n        try:\n            for item_id, item_version in zip(item_ids, versions):\n                container = containers_by_id[item_id]\n                if container.get(\"version_locked\"):\n                    continue\n                try:\n                    update_container(container, item_version)\n                except Exception as exc:\n                    log.warning(\"Update failed\", exc_info=True)\n                    self._show_version_error_dialog(\n                        item_version, [item_id], exc\n                    )\n        finally:\n            # Always update the scene inventory view, even if errors occurred\n            self.data_changed.emit()\n\n    def _update_containers_to_version(self, item_ids, version):\n        \"\"\"Helper to update items to given version (or version per item)\n\n        If at least one item is specified this will always try to refresh\n        the inventory even if errors occurred on any of the items.\n\n        Arguments:\n            item_ids (Iterable[str]): Items to update\n            version (Union[int, HeroVersion]): Version to set to.\n                This can be a list specifying a version for each item.\n                Like `update_container` version -1 sets the latest version\n                and HeroTypeVersion instances set the hero version.\n\n        \"\"\"\n        versions = [version for _ in range(len(item_ids))]\n        self._update_containers(item_ids, versions)\n\n    def _update_containers_to_approved_versions(\n        self, approved_version_by_item_id\n    ):\n        \"\"\"Helper to update items to given version (or version per item)\n\n        If at least one item is specified this will always try to refresh\n        the inventory even if errors occurred on any of the items.\n\n        Arguments:\n            approved_version_by_item_id (Dict[str, int]): Version to set by\n                item id.\n\n        \"\"\"\n        versions = []\n        item_ids = []\n        for item_id, version in approved_version_by_item_id.items():\n            item_ids.append(item_id)\n            versions.append(version)\n\n        self._update_containers(item_ids, versions)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/sceneinventory/view.html#client.ayon_core.tools.sceneinventory.view.SceneInventoryView.get_selected_indexes","title":"<code>get_selected_indexes()</code>","text":"<p>Get the selected rows</p> Source code in <code>client/ayon_core/tools/sceneinventory/view.py</code> <pre><code>def get_selected_indexes(self):\n    \"\"\"Get the selected rows\"\"\"\n    indexes, _ = self._get_selected_indexes()\n    return indexes\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/sceneinventory/view.html#client.ayon_core.tools.sceneinventory.view.SceneInventoryView.update_all","title":"<code>update_all()</code>","text":"<p>Update all items that are currently 'outdated' in the view</p> Source code in <code>client/ayon_core/tools/sceneinventory/view.py</code> <pre><code>def update_all(self):\n    \"\"\"Update all items that are currently 'outdated' in the view\"\"\"\n    # Get the source model through the proxy model\n    item_ids = self._model.get_outdated_item_ids()\n    if not item_ids:\n        log.info(\"Nothing to update.\")\n        return\n\n    # Trigger update to latest\n    self._update_containers_to_version(item_ids, version=-1)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/sceneinventory/window.html","title":"window","text":""},{"location":"autoapi/client/ayon_core/tools/sceneinventory/window.html#client.ayon_core.tools.sceneinventory.window.SceneInventoryWindow","title":"<code>SceneInventoryWindow</code>","text":"<p>               Bases: <code>QDialog</code></p> <p>Scene Inventory window</p> Source code in <code>client/ayon_core/tools/sceneinventory/window.py</code> <pre><code>class SceneInventoryWindow(QtWidgets.QDialog):\n    \"\"\"Scene Inventory window\"\"\"\n\n    def __init__(self, controller=None, parent=None):\n        if controller is None:\n            controller = SceneInventoryController()\n\n        title = \"AYON Scene Inventory\"\n        subtitle = controller.get_window_subtitle()\n        if subtitle:\n            title += f\" - {subtitle}\"\n\n        super().__init__(parent)\n\n        self.setWindowTitle(title)\n        icon = QtGui.QIcon(resources.get_ayon_icon_filepath())\n        self.setWindowIcon(icon)\n\n        self.setObjectName(\"SceneInventory\")\n\n        self.resize(1100, 480)\n\n        filter_label = QtWidgets.QLabel(\"Search\", self)\n        text_filter = PlaceholderLineEdit(self)\n        text_filter.setPlaceholderText(\"Filter by name...\")\n\n        outdated_only_checkbox = QtWidgets.QCheckBox(\n            \"Filter to outdated\", self\n        )\n        outdated_only_checkbox.setToolTip(\"Show outdated files only\")\n        outdated_only_checkbox.setChecked(False)\n\n        update_all_icon = qtawesome.icon(\"fa.arrow-up\", color=\"white\")\n        update_all_button = QtWidgets.QPushButton(self)\n        update_all_button.setToolTip(\"Update all outdated to latest version\")\n        update_all_button.setIcon(update_all_icon)\n\n        refresh_icon = qtawesome.icon(\"fa.refresh\", color=\"white\")\n        refresh_button = QtWidgets.QPushButton(self)\n        refresh_button.setToolTip(\"Refresh\")\n        refresh_button.setIcon(refresh_icon)\n\n        headers_widget = QtWidgets.QWidget(self)\n        headers_layout = QtWidgets.QHBoxLayout(headers_widget)\n        headers_layout.setContentsMargins(0, 0, 0, 0)\n        headers_layout.addWidget(filter_label, 0)\n        headers_layout.addWidget(text_filter, 1)\n        headers_layout.addWidget(outdated_only_checkbox, 0)\n        headers_layout.addWidget(update_all_button, 0)\n        headers_layout.addWidget(refresh_button, 0)\n\n        view = SceneInventoryView(controller, self)\n\n        main_layout = QtWidgets.QVBoxLayout(self)\n        main_layout.addWidget(headers_widget, 0)\n        main_layout.addWidget(view, 1)\n\n        show_timer = QtCore.QTimer()\n        show_timer.setInterval(0)\n        show_timer.setSingleShot(False)\n\n        # signals\n        show_timer.timeout.connect(self._on_show_timer)\n        text_filter.textChanged.connect(self._on_text_filter_change)\n        outdated_only_checkbox.stateChanged.connect(\n            self._on_outdated_state_change\n        )\n        view.hierarchy_view_changed.connect(\n            self._on_hierarchy_view_change\n        )\n        view.data_changed.connect(self._on_refresh_request)\n        refresh_button.clicked.connect(self._on_refresh_request)\n        update_all_button.clicked.connect(self._on_update_all)\n\n        self._show_timer = show_timer\n        self._show_counter = 0\n        self._controller = controller\n        self._update_all_button = update_all_button\n        self._outdated_only_checkbox = outdated_only_checkbox\n        self._view = view\n\n        self._first_show = True\n\n    def showEvent(self, event):\n        super(SceneInventoryWindow, self).showEvent(event)\n        if self._first_show:\n            self._first_show = False\n            self.setStyleSheet(style.load_stylesheet())\n\n        self._show_counter = 0\n        self._show_timer.start()\n\n    def keyPressEvent(self, event):\n        \"\"\"Custom keyPressEvent.\n\n        Override keyPressEvent to do nothing so that Maya's panels won't\n        take focus when pressing \"SHIFT\" whilst mouse is over viewport or\n        outliner. This way users don't accidentally perform Maya commands\n        whilst trying to name an instance.\n\n        \"\"\"\n        pass\n\n    def _on_refresh_request(self):\n        \"\"\"Signal callback to trigger 'refresh' without any arguments.\"\"\"\n\n        self.refresh()\n\n    def refresh(self):\n        self._controller.reset()\n        self._view.refresh()\n\n    def _on_show_timer(self):\n        if self._show_counter &lt; 3:\n            self._show_counter += 1\n            return\n        self._show_timer.stop()\n        self.refresh()\n\n    def _on_hierarchy_view_change(self, enabled):\n        self._view.set_hierarchy_view(enabled)\n\n    def _on_text_filter_change(self, text_filter):\n        self._view.set_text_filter(text_filter)\n\n    def _on_outdated_state_change(self):\n        self._view.set_filter_outdated(\n            self._outdated_only_checkbox.isChecked()\n        )\n\n    def _on_update_all(self):\n        self._view.update_all()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/sceneinventory/window.html#client.ayon_core.tools.sceneinventory.window.SceneInventoryWindow.keyPressEvent","title":"<code>keyPressEvent(event)</code>","text":"<p>Custom keyPressEvent.</p> <p>Override keyPressEvent to do nothing so that Maya's panels won't take focus when pressing \"SHIFT\" whilst mouse is over viewport or outliner. This way users don't accidentally perform Maya commands whilst trying to name an instance.</p> Source code in <code>client/ayon_core/tools/sceneinventory/window.py</code> <pre><code>def keyPressEvent(self, event):\n    \"\"\"Custom keyPressEvent.\n\n    Override keyPressEvent to do nothing so that Maya's panels won't\n    take focus when pressing \"SHIFT\" whilst mouse is over viewport or\n    outliner. This way users don't accidentally perform Maya commands\n    whilst trying to name an instance.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/sceneinventory/models/index.html","title":"models","text":""},{"location":"autoapi/client/ayon_core/tools/sceneinventory/models/index.html#client.ayon_core.tools.sceneinventory.models.SiteSyncModel","title":"<code>SiteSyncModel</code>","text":"Source code in <code>client/ayon_core/tools/sceneinventory/models/sitesync.py</code> <pre><code>class SiteSyncModel:\n    def __init__(self, controller):\n        self._controller = controller\n\n        self._sitesync_addon = NOT_SET\n        self._sitesync_enabled = None\n        self._active_site = {}\n        self._remote_site = {}\n        self._active_site_provider = {}\n        self._remote_site_provider = {}\n\n    def reset(self):\n        self._sitesync_addon = NOT_SET\n        self._sitesync_enabled = None\n        self._active_site = {}\n        self._remote_site = {}\n        self._active_site_provider = {}\n        self._remote_site_provider = {}\n\n    def is_sitesync_enabled(self):\n        \"\"\"Site sync is enabled.\n\n        Returns:\n            bool: Is enabled or not.\n        \"\"\"\n\n        self._cache_sitesync_addon()\n        return self._sitesync_enabled\n\n    def get_site_provider_icons(self):\n        \"\"\"Icon paths per provider.\n\n        Returns:\n            dict[str, str]: Path by provider name.\n        \"\"\"\n\n        if not self.is_sitesync_enabled():\n            return {}\n        sitesync_addon = self._get_sitesync_addon()\n        return sitesync_addon.get_site_icons()\n\n    def get_sites_information(self, project_name):\n        return {\n            \"active_site\": self._get_active_site(project_name),\n            \"remote_site\": self._get_remote_site(project_name),\n            \"active_site_provider\": self._get_active_site_provider(\n                project_name\n            ),\n            \"remote_site_provider\": self._get_remote_site_provider(\n                project_name\n            )\n        }\n\n    def get_representations_site_progress(\n        self, project_name, representation_ids\n    ):\n        \"\"\"Get progress of representations sync.\"\"\"\n\n        representation_ids = set(representation_ids)\n        output = {\n            repre_id: {\n                \"active_site\": 0,\n                \"remote_site\": 0,\n            }\n            for repre_id in representation_ids\n        }\n        if not self.is_sitesync_enabled():\n            return output\n\n        sitesync_addon = self._get_sitesync_addon()\n        repre_entities = ayon_api.get_representations(\n            project_name, representation_ids\n        )\n        active_site = self._get_active_site(project_name)\n        remote_site = self._get_remote_site(project_name)\n\n        for repre_entity in repre_entities:\n            repre_output = output[repre_entity[\"id\"]]\n            result = sitesync_addon.get_progress_for_repre(\n                repre_entity, active_site, remote_site\n            )\n            repre_output[\"active_site\"] = result[active_site]\n            repre_output[\"remote_site\"] = result[remote_site]\n\n        return output\n\n    def resync_representations(\n        self, project_name, representation_ids, site_type\n    ):\n        \"\"\"\n\n        Args:\n            project_name (str): Project name.\n            representation_ids (Iterable[str]): Representation ids.\n            site_type (Literal[active_site, remote_site]): Site type.\n        \"\"\"\n        sitesync_addon = self._get_sitesync_addon()\n        active_site = self._get_active_site(project_name)\n        remote_site = self._get_remote_site(project_name)\n        progress = self.get_representations_site_progress(\n            project_name, representation_ids\n        )\n        for repre_id in representation_ids:\n            repre_progress = progress.get(repre_id)\n            if not repre_progress:\n                continue\n\n            if site_type == \"active_site\":\n                # check opposite from added site, must be 1 or unable to sync\n                check_progress = repre_progress[\"remote_site\"]\n                site = active_site\n            else:\n                check_progress = repre_progress[\"active_site\"]\n                site = remote_site\n\n            if check_progress == 1:\n                sitesync_addon.add_site(\n                    project_name, repre_id, site, force=True\n                )\n\n    def _get_sitesync_addon(self):\n        self._cache_sitesync_addon()\n        return self._sitesync_addon\n\n    def _cache_sitesync_addon(self):\n        if self._sitesync_addon is not NOT_SET:\n            return self._sitesync_addon\n        manager = AddonsManager()\n        sitesync_addon = manager.get(\"sitesync\")\n        sync_enabled = sitesync_addon is not None and sitesync_addon.enabled\n        self._sitesync_addon = sitesync_addon\n        self._sitesync_enabled = sync_enabled\n\n    def _get_active_site(self, project_name):\n        if project_name not in self._active_site:\n            self._cache_sites(project_name)\n        return self._active_site[project_name]\n\n    def _get_remote_site(self, project_name):\n        if project_name not in self._remote_site:\n            self._cache_sites(project_name)\n        return self._remote_site[project_name]\n\n    def _get_active_site_provider(self, project_name):\n        if project_name not in self._active_site_provider:\n            self._cache_sites(project_name)\n        return self._active_site_provider[project_name]\n\n    def _get_remote_site_provider(self, project_name):\n        if project_name not in self._remote_site_provider:\n            self._cache_sites(project_name)\n        return self._remote_site_provider[project_name]\n\n    def _cache_sites(self, project_name):\n        self._active_site[project_name] = None\n        self._remote_site[project_name] = None\n        self._active_site_provider[project_name] = None\n        self._remote_site_provider[project_name] = None\n        if not self.is_sitesync_enabled():\n            return\n\n        sitesync_addon = self._get_sitesync_addon()\n        active_site = sitesync_addon.get_active_site(project_name)\n        remote_site = sitesync_addon.get_remote_site(project_name)\n        active_site_provider = \"studio\"\n        remote_site_provider = \"studio\"\n        if active_site != \"studio\":\n            active_site_provider = sitesync_addon.get_provider_for_site(\n                project_name, active_site\n            )\n        if remote_site != \"studio\":\n            remote_site_provider = sitesync_addon.get_provider_for_site(\n                project_name, remote_site\n            )\n\n        self._active_site[project_name] = active_site\n        self._remote_site[project_name] = remote_site\n        self._active_site_provider[project_name] = active_site_provider\n        self._remote_site_provider[project_name] = remote_site_provider\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/sceneinventory/models/index.html#client.ayon_core.tools.sceneinventory.models.SiteSyncModel.get_representations_site_progress","title":"<code>get_representations_site_progress(project_name, representation_ids)</code>","text":"<p>Get progress of representations sync.</p> Source code in <code>client/ayon_core/tools/sceneinventory/models/sitesync.py</code> <pre><code>def get_representations_site_progress(\n    self, project_name, representation_ids\n):\n    \"\"\"Get progress of representations sync.\"\"\"\n\n    representation_ids = set(representation_ids)\n    output = {\n        repre_id: {\n            \"active_site\": 0,\n            \"remote_site\": 0,\n        }\n        for repre_id in representation_ids\n    }\n    if not self.is_sitesync_enabled():\n        return output\n\n    sitesync_addon = self._get_sitesync_addon()\n    repre_entities = ayon_api.get_representations(\n        project_name, representation_ids\n    )\n    active_site = self._get_active_site(project_name)\n    remote_site = self._get_remote_site(project_name)\n\n    for repre_entity in repre_entities:\n        repre_output = output[repre_entity[\"id\"]]\n        result = sitesync_addon.get_progress_for_repre(\n            repre_entity, active_site, remote_site\n        )\n        repre_output[\"active_site\"] = result[active_site]\n        repre_output[\"remote_site\"] = result[remote_site]\n\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/sceneinventory/models/index.html#client.ayon_core.tools.sceneinventory.models.SiteSyncModel.get_site_provider_icons","title":"<code>get_site_provider_icons()</code>","text":"<p>Icon paths per provider.</p> <p>Returns:</p> Type Description <p>dict[str, str]: Path by provider name.</p> Source code in <code>client/ayon_core/tools/sceneinventory/models/sitesync.py</code> <pre><code>def get_site_provider_icons(self):\n    \"\"\"Icon paths per provider.\n\n    Returns:\n        dict[str, str]: Path by provider name.\n    \"\"\"\n\n    if not self.is_sitesync_enabled():\n        return {}\n    sitesync_addon = self._get_sitesync_addon()\n    return sitesync_addon.get_site_icons()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/sceneinventory/models/index.html#client.ayon_core.tools.sceneinventory.models.SiteSyncModel.is_sitesync_enabled","title":"<code>is_sitesync_enabled()</code>","text":"<p>Site sync is enabled.</p> <p>Returns:</p> Name Type Description <code>bool</code> <p>Is enabled or not.</p> Source code in <code>client/ayon_core/tools/sceneinventory/models/sitesync.py</code> <pre><code>def is_sitesync_enabled(self):\n    \"\"\"Site sync is enabled.\n\n    Returns:\n        bool: Is enabled or not.\n    \"\"\"\n\n    self._cache_sitesync_addon()\n    return self._sitesync_enabled\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/sceneinventory/models/index.html#client.ayon_core.tools.sceneinventory.models.SiteSyncModel.resync_representations","title":"<code>resync_representations(project_name, representation_ids, site_type)</code>","text":"<p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>representation_ids</code> <code>Iterable[str]</code> <p>Representation ids.</p> required <code>site_type</code> <code>Literal[active_site, remote_site]</code> <p>Site type.</p> required Source code in <code>client/ayon_core/tools/sceneinventory/models/sitesync.py</code> <pre><code>def resync_representations(\n    self, project_name, representation_ids, site_type\n):\n    \"\"\"\n\n    Args:\n        project_name (str): Project name.\n        representation_ids (Iterable[str]): Representation ids.\n        site_type (Literal[active_site, remote_site]): Site type.\n    \"\"\"\n    sitesync_addon = self._get_sitesync_addon()\n    active_site = self._get_active_site(project_name)\n    remote_site = self._get_remote_site(project_name)\n    progress = self.get_representations_site_progress(\n        project_name, representation_ids\n    )\n    for repre_id in representation_ids:\n        repre_progress = progress.get(repre_id)\n        if not repre_progress:\n            continue\n\n        if site_type == \"active_site\":\n            # check opposite from added site, must be 1 or unable to sync\n            check_progress = repre_progress[\"remote_site\"]\n            site = active_site\n        else:\n            check_progress = repre_progress[\"active_site\"]\n            site = remote_site\n\n        if check_progress == 1:\n            sitesync_addon.add_site(\n                project_name, repre_id, site, force=True\n            )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/sceneinventory/models/containers.html","title":"containers","text":""},{"location":"autoapi/client/ayon_core/tools/sceneinventory/models/containers.html#client.ayon_core.tools.sceneinventory.models.containers.get_representations_hierarchy","title":"<code>get_representations_hierarchy(project_name, representation_ids)</code>","text":"<p>Find representations parents by representation id.</p> <p>Representation parent entities up to project.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project where to look for entities.</p> required <code>representation_ids</code> <code>Iterable[str]</code> <p>Representation ids.</p> required <p>Returns:</p> Type Description <p>dict[str, RepresentationParents]: Parent entities by representation id.</p> Source code in <code>client/ayon_core/tools/sceneinventory/models/containers.py</code> <pre><code>def get_representations_hierarchy(project_name, representation_ids):\n    \"\"\"Find representations parents by representation id.\n\n    Representation parent entities up to project.\n\n    Args:\n         project_name (str): Project where to look for entities.\n         representation_ids (Iterable[str]): Representation ids.\n\n    Returns:\n        dict[str, RepresentationParents]: Parent entities by\n            representation id.\n\n    \"\"\"\n    if not representation_ids:\n        return {}\n\n    repre_ids = set(representation_ids)\n    output = {\n        repre_id: RepresentationHierarchy(None, None, None, None)\n        for repre_id in representation_ids\n    }\n\n    query = representations_parent_ids_qraphql_query()\n    query.set_variable_value(\"projectName\", project_name)\n    query.set_variable_value(\"representationIds\", list(repre_ids))\n\n    con = ayon_api.get_server_api_connection()\n    parsed_data = query.query(con)\n    for repre in parsed_data[\"project\"][\"representations\"]:\n        repre_id = repre[\"id\"]\n        version = repre.pop(\"version\")\n        product = version.pop(\"product\")\n        folder = product.pop(\"folder\")\n\n        output[repre_id] = RepresentationHierarchy(\n            folder, product, version, repre\n        )\n\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/sceneinventory/models/sitesync.html","title":"sitesync","text":""},{"location":"autoapi/client/ayon_core/tools/sceneinventory/models/sitesync.html#client.ayon_core.tools.sceneinventory.models.sitesync.SiteSyncModel","title":"<code>SiteSyncModel</code>","text":"Source code in <code>client/ayon_core/tools/sceneinventory/models/sitesync.py</code> <pre><code>class SiteSyncModel:\n    def __init__(self, controller):\n        self._controller = controller\n\n        self._sitesync_addon = NOT_SET\n        self._sitesync_enabled = None\n        self._active_site = {}\n        self._remote_site = {}\n        self._active_site_provider = {}\n        self._remote_site_provider = {}\n\n    def reset(self):\n        self._sitesync_addon = NOT_SET\n        self._sitesync_enabled = None\n        self._active_site = {}\n        self._remote_site = {}\n        self._active_site_provider = {}\n        self._remote_site_provider = {}\n\n    def is_sitesync_enabled(self):\n        \"\"\"Site sync is enabled.\n\n        Returns:\n            bool: Is enabled or not.\n        \"\"\"\n\n        self._cache_sitesync_addon()\n        return self._sitesync_enabled\n\n    def get_site_provider_icons(self):\n        \"\"\"Icon paths per provider.\n\n        Returns:\n            dict[str, str]: Path by provider name.\n        \"\"\"\n\n        if not self.is_sitesync_enabled():\n            return {}\n        sitesync_addon = self._get_sitesync_addon()\n        return sitesync_addon.get_site_icons()\n\n    def get_sites_information(self, project_name):\n        return {\n            \"active_site\": self._get_active_site(project_name),\n            \"remote_site\": self._get_remote_site(project_name),\n            \"active_site_provider\": self._get_active_site_provider(\n                project_name\n            ),\n            \"remote_site_provider\": self._get_remote_site_provider(\n                project_name\n            )\n        }\n\n    def get_representations_site_progress(\n        self, project_name, representation_ids\n    ):\n        \"\"\"Get progress of representations sync.\"\"\"\n\n        representation_ids = set(representation_ids)\n        output = {\n            repre_id: {\n                \"active_site\": 0,\n                \"remote_site\": 0,\n            }\n            for repre_id in representation_ids\n        }\n        if not self.is_sitesync_enabled():\n            return output\n\n        sitesync_addon = self._get_sitesync_addon()\n        repre_entities = ayon_api.get_representations(\n            project_name, representation_ids\n        )\n        active_site = self._get_active_site(project_name)\n        remote_site = self._get_remote_site(project_name)\n\n        for repre_entity in repre_entities:\n            repre_output = output[repre_entity[\"id\"]]\n            result = sitesync_addon.get_progress_for_repre(\n                repre_entity, active_site, remote_site\n            )\n            repre_output[\"active_site\"] = result[active_site]\n            repre_output[\"remote_site\"] = result[remote_site]\n\n        return output\n\n    def resync_representations(\n        self, project_name, representation_ids, site_type\n    ):\n        \"\"\"\n\n        Args:\n            project_name (str): Project name.\n            representation_ids (Iterable[str]): Representation ids.\n            site_type (Literal[active_site, remote_site]): Site type.\n        \"\"\"\n        sitesync_addon = self._get_sitesync_addon()\n        active_site = self._get_active_site(project_name)\n        remote_site = self._get_remote_site(project_name)\n        progress = self.get_representations_site_progress(\n            project_name, representation_ids\n        )\n        for repre_id in representation_ids:\n            repre_progress = progress.get(repre_id)\n            if not repre_progress:\n                continue\n\n            if site_type == \"active_site\":\n                # check opposite from added site, must be 1 or unable to sync\n                check_progress = repre_progress[\"remote_site\"]\n                site = active_site\n            else:\n                check_progress = repre_progress[\"active_site\"]\n                site = remote_site\n\n            if check_progress == 1:\n                sitesync_addon.add_site(\n                    project_name, repre_id, site, force=True\n                )\n\n    def _get_sitesync_addon(self):\n        self._cache_sitesync_addon()\n        return self._sitesync_addon\n\n    def _cache_sitesync_addon(self):\n        if self._sitesync_addon is not NOT_SET:\n            return self._sitesync_addon\n        manager = AddonsManager()\n        sitesync_addon = manager.get(\"sitesync\")\n        sync_enabled = sitesync_addon is not None and sitesync_addon.enabled\n        self._sitesync_addon = sitesync_addon\n        self._sitesync_enabled = sync_enabled\n\n    def _get_active_site(self, project_name):\n        if project_name not in self._active_site:\n            self._cache_sites(project_name)\n        return self._active_site[project_name]\n\n    def _get_remote_site(self, project_name):\n        if project_name not in self._remote_site:\n            self._cache_sites(project_name)\n        return self._remote_site[project_name]\n\n    def _get_active_site_provider(self, project_name):\n        if project_name not in self._active_site_provider:\n            self._cache_sites(project_name)\n        return self._active_site_provider[project_name]\n\n    def _get_remote_site_provider(self, project_name):\n        if project_name not in self._remote_site_provider:\n            self._cache_sites(project_name)\n        return self._remote_site_provider[project_name]\n\n    def _cache_sites(self, project_name):\n        self._active_site[project_name] = None\n        self._remote_site[project_name] = None\n        self._active_site_provider[project_name] = None\n        self._remote_site_provider[project_name] = None\n        if not self.is_sitesync_enabled():\n            return\n\n        sitesync_addon = self._get_sitesync_addon()\n        active_site = sitesync_addon.get_active_site(project_name)\n        remote_site = sitesync_addon.get_remote_site(project_name)\n        active_site_provider = \"studio\"\n        remote_site_provider = \"studio\"\n        if active_site != \"studio\":\n            active_site_provider = sitesync_addon.get_provider_for_site(\n                project_name, active_site\n            )\n        if remote_site != \"studio\":\n            remote_site_provider = sitesync_addon.get_provider_for_site(\n                project_name, remote_site\n            )\n\n        self._active_site[project_name] = active_site\n        self._remote_site[project_name] = remote_site\n        self._active_site_provider[project_name] = active_site_provider\n        self._remote_site_provider[project_name] = remote_site_provider\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/sceneinventory/models/sitesync.html#client.ayon_core.tools.sceneinventory.models.sitesync.SiteSyncModel.get_representations_site_progress","title":"<code>get_representations_site_progress(project_name, representation_ids)</code>","text":"<p>Get progress of representations sync.</p> Source code in <code>client/ayon_core/tools/sceneinventory/models/sitesync.py</code> <pre><code>def get_representations_site_progress(\n    self, project_name, representation_ids\n):\n    \"\"\"Get progress of representations sync.\"\"\"\n\n    representation_ids = set(representation_ids)\n    output = {\n        repre_id: {\n            \"active_site\": 0,\n            \"remote_site\": 0,\n        }\n        for repre_id in representation_ids\n    }\n    if not self.is_sitesync_enabled():\n        return output\n\n    sitesync_addon = self._get_sitesync_addon()\n    repre_entities = ayon_api.get_representations(\n        project_name, representation_ids\n    )\n    active_site = self._get_active_site(project_name)\n    remote_site = self._get_remote_site(project_name)\n\n    for repre_entity in repre_entities:\n        repre_output = output[repre_entity[\"id\"]]\n        result = sitesync_addon.get_progress_for_repre(\n            repre_entity, active_site, remote_site\n        )\n        repre_output[\"active_site\"] = result[active_site]\n        repre_output[\"remote_site\"] = result[remote_site]\n\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/sceneinventory/models/sitesync.html#client.ayon_core.tools.sceneinventory.models.sitesync.SiteSyncModel.get_site_provider_icons","title":"<code>get_site_provider_icons()</code>","text":"<p>Icon paths per provider.</p> <p>Returns:</p> Type Description <p>dict[str, str]: Path by provider name.</p> Source code in <code>client/ayon_core/tools/sceneinventory/models/sitesync.py</code> <pre><code>def get_site_provider_icons(self):\n    \"\"\"Icon paths per provider.\n\n    Returns:\n        dict[str, str]: Path by provider name.\n    \"\"\"\n\n    if not self.is_sitesync_enabled():\n        return {}\n    sitesync_addon = self._get_sitesync_addon()\n    return sitesync_addon.get_site_icons()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/sceneinventory/models/sitesync.html#client.ayon_core.tools.sceneinventory.models.sitesync.SiteSyncModel.is_sitesync_enabled","title":"<code>is_sitesync_enabled()</code>","text":"<p>Site sync is enabled.</p> <p>Returns:</p> Name Type Description <code>bool</code> <p>Is enabled or not.</p> Source code in <code>client/ayon_core/tools/sceneinventory/models/sitesync.py</code> <pre><code>def is_sitesync_enabled(self):\n    \"\"\"Site sync is enabled.\n\n    Returns:\n        bool: Is enabled or not.\n    \"\"\"\n\n    self._cache_sitesync_addon()\n    return self._sitesync_enabled\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/sceneinventory/models/sitesync.html#client.ayon_core.tools.sceneinventory.models.sitesync.SiteSyncModel.resync_representations","title":"<code>resync_representations(project_name, representation_ids, site_type)</code>","text":"<p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>representation_ids</code> <code>Iterable[str]</code> <p>Representation ids.</p> required <code>site_type</code> <code>Literal[active_site, remote_site]</code> <p>Site type.</p> required Source code in <code>client/ayon_core/tools/sceneinventory/models/sitesync.py</code> <pre><code>def resync_representations(\n    self, project_name, representation_ids, site_type\n):\n    \"\"\"\n\n    Args:\n        project_name (str): Project name.\n        representation_ids (Iterable[str]): Representation ids.\n        site_type (Literal[active_site, remote_site]): Site type.\n    \"\"\"\n    sitesync_addon = self._get_sitesync_addon()\n    active_site = self._get_active_site(project_name)\n    remote_site = self._get_remote_site(project_name)\n    progress = self.get_representations_site_progress(\n        project_name, representation_ids\n    )\n    for repre_id in representation_ids:\n        repre_progress = progress.get(repre_id)\n        if not repre_progress:\n            continue\n\n        if site_type == \"active_site\":\n            # check opposite from added site, must be 1 or unable to sync\n            check_progress = repre_progress[\"remote_site\"]\n            site = active_site\n        else:\n            check_progress = repre_progress[\"active_site\"]\n            site = remote_site\n\n        if check_progress == 1:\n            sitesync_addon.add_site(\n                project_name, repre_id, site, force=True\n            )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/sceneinventory/switch_dialog/index.html","title":"switch_dialog","text":""},{"location":"autoapi/client/ayon_core/tools/sceneinventory/switch_dialog/index.html#client.ayon_core.tools.sceneinventory.switch_dialog.SwitchAssetDialog","title":"<code>SwitchAssetDialog</code>","text":"<p>               Bases: <code>QDialog</code></p> <p>Widget to support asset switching</p> Source code in <code>client/ayon_core/tools/sceneinventory/switch_dialog/dialog.py</code> <pre><code>class SwitchAssetDialog(QtWidgets.QDialog):\n    \"\"\"Widget to support asset switching\"\"\"\n\n    MIN_WIDTH = 550\n\n    switched = QtCore.Signal()\n\n    def __init__(self, controller, project_name, items, parent=None):\n        super().__init__(parent)\n\n        current_project_name = controller.get_current_project_name()\n        folder_id = None\n        if current_project_name == project_name:\n            folder_id = controller.get_current_folder_id()\n\n        self.setWindowTitle(\"Switch selected items ...\")\n\n        # Force and keep focus dialog\n        self.setModal(True)\n\n        folders_field = FoldersField(controller, self)\n        products_combox = SearchComboBox(self)\n        repres_combobox = SearchComboBox(self)\n\n        products_combox.set_placeholder(\"&lt;product&gt;\")\n        repres_combobox.set_placeholder(\"&lt;representation&gt;\")\n\n        folder_label = QtWidgets.QLabel(self)\n        product_label = QtWidgets.QLabel(self)\n        repre_label = QtWidgets.QLabel(self)\n\n        current_folder_btn = QtWidgets.QPushButton(\"Use current folder\", self)\n\n        accept_icon = qtawesome.icon(\"fa.check\", color=\"white\")\n        accept_btn = ButtonWithMenu(self)\n        accept_btn.setIcon(accept_icon)\n\n        main_layout = QtWidgets.QGridLayout(self)\n        # Folder column\n        main_layout.addWidget(current_folder_btn, 0, 0)\n        main_layout.addWidget(folders_field, 1, 0)\n        main_layout.addWidget(folder_label, 2, 0)\n        # Product column\n        main_layout.addWidget(products_combox, 1, 1)\n        main_layout.addWidget(product_label, 2, 1)\n        # Representation column\n        main_layout.addWidget(repres_combobox, 1, 2)\n        main_layout.addWidget(repre_label, 2, 2)\n        # Btn column\n        main_layout.addWidget(accept_btn, 1, 3)\n        main_layout.setColumnStretch(0, 1)\n        main_layout.setColumnStretch(1, 1)\n        main_layout.setColumnStretch(2, 1)\n        main_layout.setColumnStretch(3, 0)\n\n        show_timer = QtCore.QTimer()\n        show_timer.setInterval(0)\n        show_timer.setSingleShot(False)\n\n        show_timer.timeout.connect(self._on_show_timer)\n        folders_field.value_changed.connect(\n            self._combobox_value_changed\n        )\n        products_combox.currentIndexChanged.connect(\n            self._combobox_value_changed\n        )\n        repres_combobox.currentIndexChanged.connect(\n            self._combobox_value_changed\n        )\n        accept_btn.clicked.connect(self._on_accept)\n        current_folder_btn.clicked.connect(self._on_current_folder)\n\n        self._show_timer = show_timer\n        self._show_counter = 0\n\n        self._current_folder_btn = current_folder_btn\n\n        self._folders_field = folders_field\n        self._products_combox = products_combox\n        self._representations_box = repres_combobox\n\n        self._folder_label = folder_label\n        self._product_label = product_label\n        self._repre_label = repre_label\n\n        self._accept_btn = accept_btn\n\n        self.setMinimumWidth(self.MIN_WIDTH)\n\n        # Set default focus to accept button so you don't directly type in\n        # first asset field, this also allows to see the placeholder value.\n        accept_btn.setFocus()\n\n        self._folder_entities_by_id = {}\n        self._product_entities_by_id = {}\n        self._version_entities_by_id = {}\n        self._repre_entities_by_id = {}\n\n        self._missing_folder_ids = set()\n        self._missing_product_ids = set()\n        self._missing_version_ids = set()\n        self._missing_repre_ids = set()\n        self._missing_entities = False\n\n        self._inactive_folder_ids = set()\n        self._inactive_product_ids = set()\n        self._inactive_repre_ids = set()\n\n        self._init_folder_id = None\n        self._init_product_name = None\n        self._init_repre_name = None\n\n        self._fill_check = False\n        self._project_name = project_name\n        self._folder_id = folder_id\n\n        self._current_folder_btn.setEnabled(folder_id is not None)\n\n        self._controller = controller\n\n        self._items = items\n        self._prepare_content_data()\n\n    def showEvent(self, event):\n        super().showEvent(event)\n        self._show_timer.start()\n\n    def refresh(self, init_refresh=False):\n        \"\"\"Build the need comboboxes with content\"\"\"\n        if not self._fill_check and not init_refresh:\n            return\n\n        self._fill_check = False\n\n        validation_state = ValidationState()\n        self._folders_field.refresh()\n        # Set other comboboxes to empty if any document is missing or\n        #   any folder of loaded representations is archived.\n        self._is_folder_ok(validation_state)\n        if validation_state.folder_ok:\n            product_values = self._get_product_box_values()\n            self._fill_combobox(product_values, \"product\")\n            self._is_product_ok(validation_state)\n\n        if validation_state.folder_ok and validation_state.product_ok:\n            repre_values = sorted(self._representations_box_values())\n            self._fill_combobox(repre_values, \"repre\")\n            self._is_repre_ok(validation_state)\n\n        # Fill comboboxes with values\n        self.set_labels()\n\n        self.apply_validations(validation_state)\n\n        self._build_loaders_menu()\n\n        if init_refresh:\n            # pre select context if possible\n            self._folders_field.set_selected_item(self._init_folder_id)\n            self._products_combox.set_valid_value(self._init_product_name)\n            self._representations_box.set_valid_value(self._init_repre_name)\n\n        self._fill_check = True\n\n    def set_labels(self):\n        folder_label = self._folders_field.get_selected_folder_label()\n        product_label = self._products_combox.get_valid_value()\n        repre_label = self._representations_box.get_valid_value()\n\n        default = \"*No changes\"\n        self._folder_label.setText(folder_label or default)\n        self._product_label.setText(product_label or default)\n        self._repre_label.setText(repre_label or default)\n\n    def apply_validations(self, validation_state):\n        error_msg = \"*Please select\"\n        error_sheet = \"border: 1px solid red;\"\n\n        product_sheet = None\n        repre_sheet = None\n        accept_state = \"\"\n        if validation_state.folder_ok is False:\n            self._folder_label.setText(error_msg)\n        elif validation_state.product_ok is False:\n            product_sheet = error_sheet\n            self._product_label.setText(error_msg)\n        elif validation_state.repre_ok is False:\n            repre_sheet = error_sheet\n            self._repre_label.setText(error_msg)\n\n        if validation_state.all_ok:\n            accept_state = \"1\"\n\n        self._folders_field.set_valid(validation_state.folder_ok)\n        self._products_combox.setStyleSheet(product_sheet or \"\")\n        self._representations_box.setStyleSheet(repre_sheet or \"\")\n\n        self._accept_btn.setEnabled(validation_state.all_ok)\n        self._set_style_property(self._accept_btn, \"state\", accept_state)\n\n    def find_last_versions(self, product_ids):\n        project_name = self._project_name\n        return ayon_api.get_last_versions(\n            project_name,\n            product_ids,\n            fields={\"id\", \"productId\", \"version\"}\n        )\n\n    def _on_show_timer(self):\n        if self._show_counter == 2:\n            self._show_timer.stop()\n            self.refresh(True)\n        else:\n            self._show_counter += 1\n\n    def _prepare_content_data(self):\n        repre_ids = {\n            item[\"representation\"]\n            for item in self._items\n        }\n\n        project_name = self._project_name\n        repre_entities = list(ayon_api.get_representations(\n            project_name,\n            representation_ids=repre_ids,\n        ))\n        repres_by_id = {r[\"id\"]: r for r in repre_entities}\n\n        content_repre_entities_by_id = {}\n        inactive_repre_ids = set()\n        missing_repre_ids = set()\n        version_ids = set()\n        for repre_id in repre_ids:\n            repre_entity = repres_by_id.get(repre_id)\n            if repre_entity is None:\n                missing_repre_ids.add(repre_id)\n            elif not repres_by_id[repre_id][\"active\"]:\n                inactive_repre_ids.add(repre_id)\n                version_ids.add(repre_entity[\"versionId\"])\n            else:\n                content_repre_entities_by_id[repre_id] = repre_entity\n                version_ids.add(repre_entity[\"versionId\"])\n\n        version_entities = ayon_api.get_versions(\n            project_name,\n            version_ids=version_ids\n        )\n        content_version_entities_by_id = {}\n        for version_entity in version_entities:\n            version_id = version_entity[\"id\"]\n            content_version_entities_by_id[version_id] = version_entity\n\n        missing_version_ids = set()\n        product_ids = set()\n        for version_id in version_ids:\n            version_entity = content_version_entities_by_id.get(version_id)\n            if version_entity is None:\n                missing_version_ids.add(version_id)\n            else:\n                product_ids.add(version_entity[\"productId\"])\n\n        product_entities = ayon_api.get_products(\n            project_name, product_ids=product_ids\n        )\n        product_entities_by_id = {p[\"id\"]: p for p in product_entities}\n\n        folder_ids = set()\n        inactive_product_ids = set()\n        missing_product_ids = set()\n        content_product_entities_by_id = {}\n        for product_id in product_ids:\n            product_entity = product_entities_by_id.get(product_id)\n            if product_entity is None:\n                missing_product_ids.add(product_id)\n            else:\n                folder_ids.add(product_entity[\"folderId\"])\n                content_product_entities_by_id[product_id] = product_entity\n\n        folder_entities = ayon_api.get_folders(\n            project_name, folder_ids=folder_ids, active=None\n        )\n        folder_entities_by_id = {\n            folder_entity[\"id\"]: folder_entity\n            for folder_entity in folder_entities\n        }\n\n        missing_folder_ids = set()\n        inactive_folder_ids = set()\n        content_folder_entities_by_id = {}\n        for folder_id in folder_ids:\n            folder_entity = folder_entities_by_id.get(folder_id)\n            if folder_entity is None:\n                missing_folder_ids.add(folder_id)\n            elif not folder_entity[\"active\"]:\n                inactive_folder_ids.add(folder_id)\n            else:\n                content_folder_entities_by_id[folder_id] = folder_entity\n\n        # stash context values, works only for single representation\n        init_folder_id = None\n        init_product_name = None\n        init_repre_name = None\n        if len(repre_entities) == 1:\n            init_repre_entity = repre_entities[0]\n            init_version_entity = content_version_entities_by_id.get(\n                init_repre_entity[\"versionId\"])\n            init_product_entity = None\n            init_folder_entity = None\n            if init_version_entity:\n                init_product_entity = content_product_entities_by_id.get(\n                    init_version_entity[\"productId\"]\n                )\n            if init_product_entity:\n                init_folder_entity = content_folder_entities_by_id.get(\n                    init_product_entity[\"folderId\"]\n                )\n            if init_folder_entity:\n                init_repre_name = init_repre_entity[\"name\"]\n                init_product_name = init_product_entity[\"name\"]\n                init_folder_id = init_folder_entity[\"id\"]\n\n        self._init_folder_id = init_folder_id\n        self._init_product_name = init_product_name\n        self._init_repre_name = init_repre_name\n\n        self._folder_entities_by_id = content_folder_entities_by_id\n        self._product_entities_by_id = content_product_entities_by_id\n        self._version_entities_by_id = content_version_entities_by_id\n        self._repre_entities_by_id = content_repre_entities_by_id\n\n        self._missing_folder_ids = missing_folder_ids\n        self._missing_product_ids = missing_product_ids\n        self._missing_version_ids = missing_version_ids\n        self._missing_repre_ids = missing_repre_ids\n        self._missing_entities = (\n            bool(missing_folder_ids)\n            or bool(missing_version_ids)\n            or bool(missing_product_ids)\n            or bool(missing_repre_ids)\n        )\n\n        self._inactive_folder_ids = inactive_folder_ids\n        self._inactive_product_ids = inactive_product_ids\n        self._inactive_repre_ids = inactive_repre_ids\n\n    def _combobox_value_changed(self, *args, **kwargs):\n        self.refresh()\n\n    def _build_loaders_menu(self):\n        repre_ids = self._get_current_output_repre_ids()\n        loaders = self._get_loaders(repre_ids)\n        # Get and destroy the action group\n        self._accept_btn.clear_actions()\n\n        if not loaders:\n            return\n\n        # Build new action group\n        group = QtWidgets.QActionGroup(self._accept_btn)\n\n        for loader in loaders:\n            # Label\n            label = getattr(loader, \"label\", None)\n            if label is None:\n                label = loader.__name__\n\n            action = group.addAction(label)\n            # action = QtWidgets.QAction(label)\n            action.setData(loader)\n\n            # Support font-awesome icons using the `.icon` and `.color`\n            # attributes on plug-ins.\n            icon = getattr(loader, \"icon\", None)\n            if icon is not None:\n                try:\n                    key = \"fa.{0}\".format(icon)\n                    color = getattr(loader, \"color\", \"white\")\n                    action.setIcon(qtawesome.icon(key, color=color))\n\n                except Exception as exc:\n                    print(\"Unable to set icon for loader {}: {}\".format(\n                        loader, str(exc)\n                    ))\n\n            self._accept_btn.add_action(action)\n\n        group.triggered.connect(self._on_action_clicked)\n\n    def _on_action_clicked(self, action):\n        loader_plugin = action.data()\n        self._trigger_switch(loader_plugin)\n\n    def _get_loaders(self, repre_ids):\n        repre_contexts = None\n        if repre_ids:\n            repre_contexts = get_repres_contexts(repre_ids)\n\n        if not repre_contexts:\n            return list()\n\n        available_loaders = []\n        for loader_plugin in discover_loader_plugins():\n            # Skip loaders without switch method\n            if not hasattr(loader_plugin, \"switch\"):\n                continue\n\n            # Skip utility loaders\n            if (\n                hasattr(loader_plugin, \"is_utility\")\n                and loader_plugin.is_utility\n            ):\n                continue\n            available_loaders.append(loader_plugin)\n\n        loaders = None\n        for repre_context in repre_contexts.values():\n            _loaders = set(loaders_from_repre_context(\n                available_loaders, repre_context\n            ))\n            if loaders is None:\n                loaders = _loaders\n            else:\n                loaders = _loaders.intersection(loaders)\n\n            if not loaders:\n                break\n\n        if loaders is None:\n            loaders = []\n        else:\n            loaders = list(loaders)\n\n        return loaders\n\n    def _fill_combobox(self, values, combobox_type):\n        if combobox_type == \"product\":\n            combobox_widget = self._products_combox\n        elif combobox_type == \"repre\":\n            combobox_widget = self._representations_box\n        else:\n            return\n        selected_value = combobox_widget.get_valid_value()\n\n        # Fill combobox\n        if values is not None:\n            combobox_widget.populate(list(sorted(values)))\n            if selected_value and selected_value in values:\n                index = None\n                for idx in range(combobox_widget.count()):\n                    if selected_value == str(combobox_widget.itemText(idx)):\n                        index = idx\n                        break\n                if index is not None:\n                    combobox_widget.setCurrentIndex(index)\n\n    def _set_style_property(self, widget, name, value):\n        cur_value = widget.property(name)\n        if cur_value == value:\n            return\n        widget.setProperty(name, value)\n        widget.style().polish(widget)\n\n    def _get_current_output_repre_ids(self):\n        # NOTE hero versions are not used because it is expected that\n        # hero version has same representations as latests\n        selected_folder_id = self._folders_field.get_selected_folder_id()\n        selected_product_name = self._products_combox.currentText()\n        selected_repre = self._representations_box.currentText()\n\n        # Nothing is selected\n        # [ ] [ ] [ ]\n        if (\n            not selected_folder_id\n            and not selected_product_name\n            and not selected_repre\n        ):\n            return list(self._repre_entities_by_id.keys())\n\n        # Everything is selected\n        # [x] [x] [x]\n        if selected_folder_id and selected_product_name and selected_repre:\n            return self._get_current_output_repre_ids_xxx(\n                selected_folder_id, selected_product_name, selected_repre\n            )\n\n        # [x] [x] [ ]\n        # If folder and product is selected\n        if selected_folder_id and selected_product_name:\n            return self._get_current_output_repre_ids_xxo(\n                selected_folder_id, selected_product_name\n            )\n\n        # [x] [ ] [x]\n        # If folder and repre is selected\n        if selected_folder_id and selected_repre:\n            return self._get_current_output_repre_ids_xox(\n                selected_folder_id, selected_repre\n            )\n\n        # [x] [ ] [ ]\n        # If folder and product is selected\n        if selected_folder_id:\n            return self._get_current_output_repre_ids_xoo(selected_folder_id)\n\n        # [ ] [x] [x]\n        if selected_product_name and selected_repre:\n            return self._get_current_output_repre_ids_oxx(\n                selected_product_name, selected_repre\n            )\n\n        # [ ] [x] [ ]\n        if selected_product_name:\n            return self._get_current_output_repre_ids_oxo(\n                selected_product_name\n            )\n\n        # [ ] [ ] [x]\n        return self._get_current_output_repre_ids_oox(selected_repre)\n\n    def _get_current_output_repre_ids_xxx(\n        self, folder_id, selected_product_name, selected_repre\n    ):\n        project_name = self._project_name\n        product_entity = ayon_api.get_product_by_name(\n            project_name,\n            selected_product_name,\n            folder_id,\n            fields={\"id\"}\n        )\n\n        product_id = product_entity[\"id\"]\n        last_versions_by_product_id = self.find_last_versions([product_id])\n        version_entity = last_versions_by_product_id.get(product_id)\n        if not version_entity:\n            return []\n\n        repre_entities = ayon_api.get_representations(\n            project_name,\n            version_ids={version_entity[\"id\"]},\n            representation_names={selected_repre},\n            fields={\"id\"}\n        )\n        return {repre_entity[\"id\"] for repre_entity in repre_entities}\n\n    def _get_current_output_repre_ids_xxo(self, folder_id, product_name):\n        project_name = self._project_name\n        product_entity = ayon_api.get_product_by_name(\n            project_name,\n            product_name,\n            folder_id,\n            fields={\"id\"}\n        )\n        if not product_entity:\n            return []\n\n        repre_names = set()\n        for repre_entity in self._repre_entities_by_id.values():\n            repre_names.add(repre_entity[\"name\"])\n\n        # TODO where to take version ids?\n        version_ids = []\n        repre_entities = ayon_api.get_representations(\n            project_name,\n            representation_names=repre_names,\n            version_ids=version_ids,\n            fields={\"id\"}\n        )\n        return {repre_entity[\"id\"] for repre_entity in repre_entities}\n\n    def _get_current_output_repre_ids_xox(self, folder_id, selected_repre):\n        product_names = {\n            product_entity[\"name\"]\n            for product_entity in self._product_entities_by_id.values()\n        }\n\n        project_name = self._project_name\n        product_entities = ayon_api.get_products(\n            project_name,\n            folder_ids=[folder_id],\n            product_names=product_names,\n            fields={\"id\", \"name\"}\n        )\n        product_name_by_id = {\n            product_entity[\"id\"]: product_entity[\"name\"]\n            for product_entity in product_entities\n        }\n        product_ids = list(product_name_by_id.keys())\n        last_versions_by_product_id = self.find_last_versions(product_ids)\n        last_version_id_by_product_name = {}\n        for product_id, last_version in last_versions_by_product_id.items():\n            product_name = product_name_by_id[product_id]\n            last_version_id_by_product_name[product_name] = (\n                last_version[\"id\"]\n            )\n\n        repre_entities = ayon_api.get_representations(\n            project_name,\n            version_ids=last_version_id_by_product_name.values(),\n            representation_names={selected_repre},\n            fields={\"id\"}\n        )\n        return {repre_entity[\"id\"] for repre_entity in repre_entities}\n\n    def _get_current_output_repre_ids_xoo(self, folder_id):\n        project_name = self._project_name\n        repres_by_product_name = collections.defaultdict(set)\n        for repre_entity in self._repre_entities_by_id.values():\n            version_id = repre_entity[\"versionId\"]\n            version_entity = self._version_entities_by_id[version_id]\n            product_id = version_entity[\"productId\"]\n            product_entity = self._product_entities_by_id[product_id]\n            product_name = product_entity[\"name\"]\n            repres_by_product_name[product_name].add(repre_entity[\"name\"])\n\n        product_entities = list(ayon_api.get_products(\n            project_name,\n            folder_ids=[folder_id],\n            product_names=repres_by_product_name.keys(),\n            fields={\"id\", \"name\"}\n        ))\n        product_name_by_id = {\n            product_entity[\"id\"]: product_entity[\"name\"]\n            for product_entity in product_entities\n        }\n        product_ids = list(product_name_by_id.keys())\n        last_versions_by_product_id = self.find_last_versions(product_ids)\n        last_version_id_by_product_name = {}\n        for product_id, last_version in last_versions_by_product_id.items():\n            product_name = product_name_by_id[product_id]\n            last_version_id_by_product_name[product_name] = (\n                last_version[\"id\"]\n            )\n\n        repre_names_by_version_id = {}\n        for product_name, repre_names in repres_by_product_name.items():\n            version_id = last_version_id_by_product_name.get(product_name)\n            # This should not happen but why to crash?\n            if version_id is not None:\n                repre_names_by_version_id[version_id] = list(repre_names)\n\n        repre_entities = ayon_api.get_representations(\n            project_name,\n            names_by_version_ids=repre_names_by_version_id,\n            fields={\"id\"}\n        )\n        return {repre_entity[\"id\"] for repre_entity in repre_entities}\n\n    def _get_current_output_repre_ids_oxx(\n        self, product_name, selected_repre\n    ):\n        project_name = self._project_name\n        product_entities = ayon_api.get_products(\n            project_name,\n            folder_ids=self._folder_entities_by_id.keys(),\n            product_names=[product_name],\n            fields={\"id\"}\n        )\n        product_ids = {\n            product_entity[\"id\"] for product_entity in product_entities\n        }\n        last_versions_by_product_id = self.find_last_versions(product_ids)\n        last_version_ids = {\n            last_version[\"id\"]\n            for last_version in last_versions_by_product_id.values()\n        }\n\n        repre_entities = ayon_api.get_representations(\n            project_name,\n            version_ids=last_version_ids,\n            representation_names={selected_repre},\n            fields={\"id\"}\n        )\n        return {repre_entity[\"id\"] for repre_entity in repre_entities}\n\n    def _get_current_output_repre_ids_oxo(self, product_name):\n        project_name = self._project_name\n        product_entities = ayon_api.get_products(\n            project_name,\n            folder_ids=self._folder_entities_by_id.keys(),\n            product_names={product_name},\n            fields={\"id\", \"folderId\"}\n        )\n        product_entities_by_id = {\n            product_entity[\"id\"]: product_entity\n            for product_entity in product_entities\n        }\n        if not product_entities_by_id:\n            return list()\n\n        last_versions_by_product_id = self.find_last_versions(\n            product_entities_by_id.keys()\n        )\n\n        product_id_by_version_id = {}\n        for product_id, last_version in last_versions_by_product_id.items():\n            version_id = last_version[\"id\"]\n            product_id_by_version_id[version_id] = product_id\n\n        if not product_id_by_version_id:\n            return list()\n\n        repre_names_by_folder_id = collections.defaultdict(set)\n        for repre_entity in self._repre_entities_by_id.values():\n            version_id = repre_entity[\"versionId\"]\n            version_entity = self._version_entities_by_id[version_id]\n            product_id = version_entity[\"productId\"]\n            product_entity = self._product_entities_by_id[product_id]\n            folder_id = product_entity[\"folderId\"]\n            folder_entity = self._folder_entities_by_id[folder_id]\n            folder_id = folder_entity[\"id\"]\n            repre_names_by_folder_id[folder_id].add(repre_entity[\"name\"])\n\n        repre_names_by_version_id = {}\n        for last_version_id, product_id in product_id_by_version_id.items():\n            product_entity = product_entities_by_id[product_id]\n            folder_id = product_entity[\"folderId\"]\n            repre_names = repre_names_by_folder_id.get(folder_id)\n            if not repre_names:\n                continue\n            repre_names_by_version_id[last_version_id] = repre_names\n\n        repre_entities = ayon_api.get_representations(\n            project_name,\n            names_by_version_ids=repre_names_by_version_id,\n            fields={\"id\"}\n        )\n        return {repre_entity[\"id\"] for repre_entity in repre_entities}\n\n    def _get_current_output_repre_ids_oox(self, selected_repre):\n        project_name = self._project_name\n        repre_entities = ayon_api.get_representations(\n            project_name,\n            representation_names=[selected_repre],\n            version_ids=self._version_entities_by_id.keys(),\n            fields={\"id\"}\n        )\n        return {repre_entity[\"id\"] for repre_entity in repre_entities}\n\n    def _get_product_box_values(self):\n        project_name = self._project_name\n        selected_folder_id = self._folders_field.get_selected_folder_id()\n        if selected_folder_id:\n            folder_ids = [selected_folder_id]\n        else:\n            folder_ids = list(self._folder_entities_by_id.keys())\n\n        product_entities = ayon_api.get_products(\n            project_name,\n            folder_ids=folder_ids,\n            fields={\"folderId\", \"name\"}\n        )\n\n        product_names_by_parent_id = collections.defaultdict(set)\n        for product_entity in product_entities:\n            product_names_by_parent_id[product_entity[\"folderId\"]].add(\n                product_entity[\"name\"]\n            )\n\n        possible_product_names = None\n        for product_names in product_names_by_parent_id.values():\n            if possible_product_names is None:\n                possible_product_names = product_names\n            else:\n                possible_product_names = possible_product_names.intersection(\n                    product_names)\n\n            if not possible_product_names:\n                break\n\n        if not possible_product_names:\n            return []\n        return list(possible_product_names)\n\n    def _representations_box_values(self):\n        # NOTE hero versions are not used because it is expected that\n        # hero version has same representations as latests\n        project_name = self._project_name\n        selected_folder_id = self._folders_field.get_selected_folder_id()\n        selected_product_name = self._products_combox.currentText()\n\n        # If nothing is selected\n        # [ ] [ ] [?]\n        if not selected_folder_id and not selected_product_name:\n            # Find all representations of selection's products\n            possible_repres = ayon_api.get_representations(\n                project_name,\n                version_ids=self._version_entities_by_id.keys(),\n                fields={\"versionId\", \"name\"}\n            )\n\n            possible_repres_by_parent = collections.defaultdict(set)\n            for repre in possible_repres:\n                possible_repres_by_parent[repre[\"versionId\"]].add(\n                    repre[\"name\"]\n                )\n\n            output_repres = None\n            for repre_names in possible_repres_by_parent.values():\n                if output_repres is None:\n                    output_repres = repre_names\n                else:\n                    output_repres = (output_repres &amp; repre_names)\n\n                if not output_repres:\n                    break\n\n            return list(output_repres or list())\n\n        # [x] [x] [?]\n        if selected_folder_id and selected_product_name:\n            product_entity = ayon_api.get_product_by_name(\n                project_name,\n                selected_product_name,\n                selected_folder_id,\n                fields={\"id\"}\n            )\n\n            product_id = product_entity[\"id\"]\n            last_versions_by_product_id = self.find_last_versions([product_id])\n            version_entity = last_versions_by_product_id.get(product_id)\n            repre_entities = ayon_api.get_representations(\n                project_name,\n                version_ids={version_entity[\"id\"]},\n                fields={\"name\"}\n            )\n            return {\n                repre_entity[\"name\"]\n                for repre_entity in repre_entities\n            }\n\n        # [x] [ ] [?]\n        # If only folder is selected\n        if selected_folder_id:\n            # Filter products by names from content\n            product_names = {\n                product_entity[\"name\"]\n                for product_entity in self._product_entities_by_id.values()\n            }\n\n            product_entities = ayon_api.get_products(\n                project_name,\n                folder_ids={selected_folder_id},\n                product_names=product_names,\n                fields={\"id\"}\n            )\n            product_ids = {\n                product_entity[\"id\"]\n                for product_entity in product_entities\n            }\n            if not product_ids:\n                return list()\n\n            last_versions_by_product_id = self.find_last_versions(product_ids)\n            product_id_by_version_id = {}\n            for product_id, last_version in (\n                last_versions_by_product_id.items()\n            ):\n                version_id = last_version[\"id\"]\n                product_id_by_version_id[version_id] = product_id\n\n            if not product_id_by_version_id:\n                return list()\n\n            repre_entities = list(ayon_api.get_representations(\n                project_name,\n                version_ids=product_id_by_version_id.keys(),\n                fields={\"name\", \"versionId\"}\n            ))\n            if not repre_entities:\n                return list()\n\n            repre_names_by_parent = collections.defaultdict(set)\n            for repre_entity in repre_entities:\n                repre_names_by_parent[repre_entity[\"versionId\"]].add(\n                    repre_entity[\"name\"]\n                )\n\n            available_repres = None\n            for repre_names in repre_names_by_parent.values():\n                if available_repres is None:\n                    available_repres = repre_names\n                    continue\n\n                available_repres = available_repres.intersection(repre_names)\n\n            return list(available_repres)\n\n        # [ ] [x] [?]\n        product_entities = list(ayon_api.get_products(\n            project_name,\n            folder_ids=self._folder_entities_by_id.keys(),\n            product_names=[selected_product_name],\n            fields={\"id\", \"folderId\"}\n        ))\n        if not product_entities:\n            return list()\n\n        product_entities_by_id = {\n            product_entity[\"id\"]: product_entity\n            for product_entity in product_entities\n        }\n        last_versions_by_product_id = self.find_last_versions(\n            product_entities_by_id.keys()\n        )\n\n        product_id_by_version_id = {}\n        for product_id, last_version in last_versions_by_product_id.items():\n            version_id = last_version[\"id\"]\n            product_id_by_version_id[version_id] = product_id\n\n        if not product_id_by_version_id:\n            return list()\n\n        repre_entities = list(\n            ayon_api.get_representations(\n                project_name,\n                version_ids=product_id_by_version_id.keys(),\n                fields={\"name\", \"versionId\"}\n            )\n        )\n        if not repre_entities:\n            return list()\n\n        repre_names_by_folder_id = collections.defaultdict(set)\n        for repre_entity in repre_entities:\n            product_id = product_id_by_version_id[repre_entity[\"versionId\"]]\n            folder_id = product_entities_by_id[product_id][\"folderId\"]\n            repre_names_by_folder_id[folder_id].add(repre_entity[\"name\"])\n\n        available_repres = None\n        for repre_names in repre_names_by_folder_id.values():\n            if available_repres is None:\n                available_repres = repre_names\n                continue\n\n            available_repres = available_repres.intersection(repre_names)\n\n        return list(available_repres)\n\n    def _is_folder_ok(self, validation_state):\n        selected_folder_id = self._folders_field.get_selected_folder_id()\n        if (\n            selected_folder_id is None\n            and (self._missing_entities or self._inactive_folder_ids)\n        ):\n            validation_state.folder_ok = False\n\n    def _is_product_ok(self, validation_state):\n        selected_folder_id = self._folders_field.get_selected_folder_id()\n        selected_product_name = self._products_combox.get_valid_value()\n\n        # [?] [x] [?]\n        # If product is selected then must be ok\n        if selected_product_name is not None:\n            return\n\n        # [ ] [ ] [?]\n        if selected_folder_id is None:\n            # If there were archived products and folder is not selected\n            if self._inactive_product_ids:\n                validation_state.product_ok = False\n            return\n\n        # [x] [ ] [?]\n        project_name = self._project_name\n        product_entities = ayon_api.get_products(\n            project_name, folder_ids=[selected_folder_id], fields={\"name\"}\n        )\n\n        product_names = set(\n            product_entity[\"name\"]\n            for product_entity in product_entities\n        )\n\n        for product_entity in self._product_entities_by_id.values():\n            if product_entity[\"name\"] not in product_names:\n                validation_state.product_ok = False\n                break\n\n    def _is_repre_ok(self, validation_state):\n        selected_folder_id = self._folders_field.get_selected_folder_id()\n        selected_product_name = self._products_combox.get_valid_value()\n        selected_repre = self._representations_box.get_valid_value()\n\n        # [?] [?] [x]\n        # If product is selected then must be ok\n        if selected_repre is not None:\n            return\n\n        # [ ] [ ] [ ]\n        if selected_folder_id is None and selected_product_name is None:\n            if (\n                self._inactive_repre_ids\n                or self._missing_version_ids\n                or self._missing_repre_ids\n            ):\n                validation_state.repre_ok = False\n            return\n\n        # [x] [x] [ ]\n        project_name = self._project_name\n        if (\n            selected_folder_id is not None\n            and selected_product_name is not None\n        ):\n            product_entity = ayon_api.get_product_by_name(\n                project_name,\n                selected_product_name,\n                selected_folder_id,\n                fields={\"id\"}\n            )\n            product_id = product_entity[\"id\"]\n            last_versions_by_product_id = self.find_last_versions([product_id])\n            last_version = last_versions_by_product_id.get(product_id)\n            if not last_version:\n                validation_state.repre_ok = False\n                return\n\n            repre_entities = ayon_api.get_representations(\n                project_name,\n                version_ids={last_version[\"id\"]},\n                fields={\"name\"}\n            )\n\n            repre_names = set(\n                repre_entity[\"name\"]\n                for repre_entity in repre_entities\n            )\n            for repre_entity in self._repre_entities_by_id.values():\n                if repre_entity[\"name\"] not in repre_names:\n                    validation_state.repre_ok = False\n                    break\n            return\n\n        # [x] [ ] [ ]\n        if selected_folder_id is not None:\n            product_entities = list(ayon_api.get_products(\n                project_name,\n                folder_ids={selected_folder_id},\n                fields={\"id\", \"name\"}\n            ))\n\n            product_name_by_id = {}\n            product_ids = set()\n            for product_entity in product_entities:\n                product_id = product_entity[\"id\"]\n                product_ids.add(product_id)\n                product_name_by_id[product_id] = product_entity[\"name\"]\n\n            last_versions_by_product_id = self.find_last_versions(product_ids)\n\n            product_id_by_version_id = {}\n            for product_id, last_version in (\n                last_versions_by_product_id.items()\n            ):\n                version_id = last_version[\"id\"]\n                product_id_by_version_id[version_id] = product_id\n\n            repre_entities = ayon_api.get_representations(\n                project_name,\n                version_ids=product_id_by_version_id.keys(),\n                fields={\"name\", \"versionId\"}\n            )\n            repres_by_product_name = collections.defaultdict(set)\n            for repre_entity in repre_entities:\n                version_id = repre_entity[\"versionId\"]\n                product_id = product_id_by_version_id[version_id]\n                product_name = product_name_by_id[product_id]\n                repres_by_product_name[product_name].add(repre_entity[\"name\"])\n\n            for repre_entity in self._repre_entities_by_id.values():\n                version_id = repre_entity[\"versionId\"]\n                version_entity = self._version_entities_by_id[version_id]\n                product_id = version_entity[\"productId\"]\n                product_entity = self._product_entities_by_id[product_id]\n                repre_names = repres_by_product_name[product_entity[\"name\"]]\n                if repre_entity[\"name\"] not in repre_names:\n                    validation_state.repre_ok = False\n                    break\n            return\n\n        # [ ] [x] [ ]\n        # Product entities\n        product_entities = ayon_api.get_products(\n            project_name,\n            folder_ids=self._folder_entities_by_id.keys(),\n            product_names={selected_product_name},\n            fields={\"id\", \"name\", \"folderId\"}\n        )\n        product_entities_by_id = {}\n        for product_entity in product_entities:\n            product_entities_by_id[product_entity[\"id\"]] = product_entity\n\n        last_versions_by_product_id = self.find_last_versions(\n            product_entities_by_id.keys()\n        )\n        product_id_by_version_id = {}\n        for product_id, last_version in last_versions_by_product_id.items():\n            version_id = last_version[\"id\"]\n            product_id_by_version_id[version_id] = product_id\n\n        repre_entities = ayon_api.get_representations(\n            project_name,\n            version_ids=product_id_by_version_id.keys(),\n            fields={\"name\", \"versionId\"}\n        )\n        repres_by_folder_id = collections.defaultdict(set)\n        for repre_entity in repre_entities:\n            product_id = product_id_by_version_id[repre_entity[\"versionId\"]]\n            folder_id = product_entities_by_id[product_id][\"folderId\"]\n            repres_by_folder_id[folder_id].add(repre_entity[\"name\"])\n\n        for repre_entity in self._repre_entities_by_id.values():\n            version_id = repre_entity[\"versionId\"]\n            version_entity = self._version_entities_by_id[version_id]\n            product_id = version_entity[\"productId\"]\n            product_entity = self._product_entities_by_id[product_id]\n            folder_id = product_entity[\"folderId\"]\n            repre_names = repres_by_folder_id[folder_id]\n            if repre_entity[\"name\"] not in repre_names:\n                validation_state.repre_ok = False\n                break\n\n    def _on_current_folder(self):\n        # Set initial folder as current.\n        folder_id = self._controller.get_current_folder_id()\n        if not folder_id:\n            return\n\n        selected_folder_id = self._folders_field.get_selected_folder_id()\n        if folder_id == selected_folder_id:\n            return\n\n        self._folders_field.set_selected_item(folder_id)\n        self._combobox_value_changed()\n\n    def _on_accept(self):\n        self._trigger_switch()\n\n    def _trigger_switch(self, loader=None):\n        # Use None when not a valid value or when placeholder value\n        selected_folder_id = self._folders_field.get_selected_folder_id()\n        selected_product_name = self._products_combox.get_valid_value()\n        selected_representation = self._representations_box.get_valid_value()\n\n        project_name = self._project_name\n        if selected_folder_id:\n            folder_ids = {selected_folder_id}\n        else:\n            folder_ids = set(self._folder_entities_by_id.keys())\n\n        product_names = None\n        if selected_product_name:\n            product_names = [selected_product_name]\n\n        product_entities = list(ayon_api.get_products(\n            project_name,\n            product_names=product_names,\n            folder_ids=folder_ids\n        ))\n        product_ids = set()\n        product_entities_by_parent_and_name = collections.defaultdict(dict)\n        for product_entity in product_entities:\n            product_ids.add(product_entity[\"id\"])\n            folder_id = product_entity[\"folderId\"]\n            name = product_entity[\"name\"]\n            product_entities_by_parent_and_name[folder_id][name] = (\n                product_entity\n            )\n\n        # versions\n        _version_entities = ayon_api.get_versions(\n            project_name, product_ids=product_ids\n        )\n        version_entities = list(reversed(\n            sorted(_version_entities, key=lambda item: item[\"version\"])\n        ))\n\n        version_ids = set()\n        version_entities_by_product_id = collections.defaultdict(dict)\n        hero_version_entities_by_product_id = {}\n        for version_entity in version_entities:\n            version_ids.add(version_entity[\"id\"])\n            product_id = version_entity[\"productId\"]\n            version = version_entity[\"version\"]\n            if version &lt; 0:\n                hero_version_entities_by_product_id[product_id] = (\n                    version_entity\n                )\n                continue\n            version_entities_by_product_id[product_id][version] = (\n                version_entity\n            )\n\n        repre_entities = ayon_api.get_representations(\n            project_name, version_ids=version_ids\n        )\n        repre_entities_by_name_version_id = collections.defaultdict(dict)\n        for repre_entity in repre_entities:\n            version_id = repre_entity[\"versionId\"]\n            name = repre_entity[\"name\"]\n            repre_entities_by_name_version_id[version_id][name] = repre_entity\n\n        for container in self._items:\n            self._switch_container(\n                container,\n                loader,\n                selected_folder_id,\n                selected_product_name,\n                selected_representation,\n                product_entities_by_parent_and_name,\n                version_entities_by_product_id,\n                hero_version_entities_by_product_id,\n                repre_entities_by_name_version_id,\n            )\n\n        self.switched.emit()\n\n        self.close()\n\n    def _switch_container(\n        self,\n        container,\n        loader,\n        selected_folder_id,\n        selected_product_name,\n        selected_representation,\n        product_entities_by_parent_and_name,\n        version_entities_by_product_id,\n        hero_version_entities_by_product_id,\n        repre_entities_by_name_version_id,\n    ):\n        container_repre_id = container[\"representation\"]\n        container_repre = self._repre_entities_by_id[container_repre_id]\n        container_repre_name = container_repre[\"name\"]\n        container_version_id = container_repre[\"versionId\"]\n\n        container_version = self._version_entities_by_id[container_version_id]\n\n        container_product_id = container_version[\"productId\"]\n        container_product = self._product_entities_by_id[container_product_id]\n        container_product_name = container_product[\"name\"]\n\n        container_folder_id = container_product[\"folderId\"]\n\n        if selected_folder_id:\n            folder_id = selected_folder_id\n        else:\n            folder_id = container_folder_id\n\n        products_by_name = product_entities_by_parent_and_name[folder_id]\n        if selected_product_name:\n            product_entity = products_by_name[selected_product_name]\n        else:\n            product_entity = products_by_name[container_product[\"name\"]]\n\n        repre_entity = None\n        product_id = product_entity[\"id\"]\n        if container_version[\"version\"] &lt; 0:\n            hero_version = hero_version_entities_by_product_id.get(\n                product_id\n            )\n            if hero_version:\n                _repres = repre_entities_by_name_version_id.get(\n                    hero_version[\"id\"]\n                )\n                if selected_representation:\n                    repre_entity = _repres.get(selected_representation)\n                else:\n                    repre_entity = _repres.get(container_repre_name)\n\n        if not repre_entity:\n            version_entities_by_version = (\n                version_entities_by_product_id[product_id]\n            )\n            # If folder or product are selected for switching, we use latest\n            # version else we try to keep the current container version.\n            version = None\n            if (\n                selected_folder_id in (None, container_folder_id)\n                and selected_product_name in (None, container_product_name)\n            ):\n                version = container_version.get(\"version\")\n\n            version_entity = None\n            if version is not None:\n                version_entity = version_entities_by_version.get(version)\n\n            if version_entity is None:\n                version_name = max(version_entities_by_version)\n                version_entity = version_entities_by_version[version_name]\n\n            version_id = version_entity[\"id\"]\n            repres_by_name = repre_entities_by_name_version_id[version_id]\n            if selected_representation:\n                repre_entity = repres_by_name[selected_representation]\n            else:\n                repre_entity = repres_by_name[container_repre_name]\n\n        error = None\n        try:\n            switch_container(container, repre_entity, loader)\n        except (\n            LoaderSwitchNotImplementedError,\n            IncompatibleLoaderError,\n            LoaderNotFoundError,\n        ) as exc:\n            error = str(exc)\n        except Exception:\n            error = (\n                \"Switch asset failed. \"\n                \"Search console log for more details.\"\n            )\n        if error is not None:\n            log.warning((\n                \"Couldn't switch asset.\"\n                \"See traceback for more information.\"\n            ), exc_info=True)\n            dialog = QtWidgets.QMessageBox(self)\n            dialog.setWindowTitle(\"Switch asset failed\")\n            dialog.setText(error)\n            dialog.exec_()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/sceneinventory/switch_dialog/index.html#client.ayon_core.tools.sceneinventory.switch_dialog.SwitchAssetDialog.refresh","title":"<code>refresh(init_refresh=False)</code>","text":"<p>Build the need comboboxes with content</p> Source code in <code>client/ayon_core/tools/sceneinventory/switch_dialog/dialog.py</code> <pre><code>def refresh(self, init_refresh=False):\n    \"\"\"Build the need comboboxes with content\"\"\"\n    if not self._fill_check and not init_refresh:\n        return\n\n    self._fill_check = False\n\n    validation_state = ValidationState()\n    self._folders_field.refresh()\n    # Set other comboboxes to empty if any document is missing or\n    #   any folder of loaded representations is archived.\n    self._is_folder_ok(validation_state)\n    if validation_state.folder_ok:\n        product_values = self._get_product_box_values()\n        self._fill_combobox(product_values, \"product\")\n        self._is_product_ok(validation_state)\n\n    if validation_state.folder_ok and validation_state.product_ok:\n        repre_values = sorted(self._representations_box_values())\n        self._fill_combobox(repre_values, \"repre\")\n        self._is_repre_ok(validation_state)\n\n    # Fill comboboxes with values\n    self.set_labels()\n\n    self.apply_validations(validation_state)\n\n    self._build_loaders_menu()\n\n    if init_refresh:\n        # pre select context if possible\n        self._folders_field.set_selected_item(self._init_folder_id)\n        self._products_combox.set_valid_value(self._init_product_name)\n        self._representations_box.set_valid_value(self._init_repre_name)\n\n    self._fill_check = True\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/sceneinventory/switch_dialog/dialog.html","title":"dialog","text":""},{"location":"autoapi/client/ayon_core/tools/sceneinventory/switch_dialog/dialog.html#client.ayon_core.tools.sceneinventory.switch_dialog.dialog.SwitchAssetDialog","title":"<code>SwitchAssetDialog</code>","text":"<p>               Bases: <code>QDialog</code></p> <p>Widget to support asset switching</p> Source code in <code>client/ayon_core/tools/sceneinventory/switch_dialog/dialog.py</code> <pre><code>class SwitchAssetDialog(QtWidgets.QDialog):\n    \"\"\"Widget to support asset switching\"\"\"\n\n    MIN_WIDTH = 550\n\n    switched = QtCore.Signal()\n\n    def __init__(self, controller, project_name, items, parent=None):\n        super().__init__(parent)\n\n        current_project_name = controller.get_current_project_name()\n        folder_id = None\n        if current_project_name == project_name:\n            folder_id = controller.get_current_folder_id()\n\n        self.setWindowTitle(\"Switch selected items ...\")\n\n        # Force and keep focus dialog\n        self.setModal(True)\n\n        folders_field = FoldersField(controller, self)\n        products_combox = SearchComboBox(self)\n        repres_combobox = SearchComboBox(self)\n\n        products_combox.set_placeholder(\"&lt;product&gt;\")\n        repres_combobox.set_placeholder(\"&lt;representation&gt;\")\n\n        folder_label = QtWidgets.QLabel(self)\n        product_label = QtWidgets.QLabel(self)\n        repre_label = QtWidgets.QLabel(self)\n\n        current_folder_btn = QtWidgets.QPushButton(\"Use current folder\", self)\n\n        accept_icon = qtawesome.icon(\"fa.check\", color=\"white\")\n        accept_btn = ButtonWithMenu(self)\n        accept_btn.setIcon(accept_icon)\n\n        main_layout = QtWidgets.QGridLayout(self)\n        # Folder column\n        main_layout.addWidget(current_folder_btn, 0, 0)\n        main_layout.addWidget(folders_field, 1, 0)\n        main_layout.addWidget(folder_label, 2, 0)\n        # Product column\n        main_layout.addWidget(products_combox, 1, 1)\n        main_layout.addWidget(product_label, 2, 1)\n        # Representation column\n        main_layout.addWidget(repres_combobox, 1, 2)\n        main_layout.addWidget(repre_label, 2, 2)\n        # Btn column\n        main_layout.addWidget(accept_btn, 1, 3)\n        main_layout.setColumnStretch(0, 1)\n        main_layout.setColumnStretch(1, 1)\n        main_layout.setColumnStretch(2, 1)\n        main_layout.setColumnStretch(3, 0)\n\n        show_timer = QtCore.QTimer()\n        show_timer.setInterval(0)\n        show_timer.setSingleShot(False)\n\n        show_timer.timeout.connect(self._on_show_timer)\n        folders_field.value_changed.connect(\n            self._combobox_value_changed\n        )\n        products_combox.currentIndexChanged.connect(\n            self._combobox_value_changed\n        )\n        repres_combobox.currentIndexChanged.connect(\n            self._combobox_value_changed\n        )\n        accept_btn.clicked.connect(self._on_accept)\n        current_folder_btn.clicked.connect(self._on_current_folder)\n\n        self._show_timer = show_timer\n        self._show_counter = 0\n\n        self._current_folder_btn = current_folder_btn\n\n        self._folders_field = folders_field\n        self._products_combox = products_combox\n        self._representations_box = repres_combobox\n\n        self._folder_label = folder_label\n        self._product_label = product_label\n        self._repre_label = repre_label\n\n        self._accept_btn = accept_btn\n\n        self.setMinimumWidth(self.MIN_WIDTH)\n\n        # Set default focus to accept button so you don't directly type in\n        # first asset field, this also allows to see the placeholder value.\n        accept_btn.setFocus()\n\n        self._folder_entities_by_id = {}\n        self._product_entities_by_id = {}\n        self._version_entities_by_id = {}\n        self._repre_entities_by_id = {}\n\n        self._missing_folder_ids = set()\n        self._missing_product_ids = set()\n        self._missing_version_ids = set()\n        self._missing_repre_ids = set()\n        self._missing_entities = False\n\n        self._inactive_folder_ids = set()\n        self._inactive_product_ids = set()\n        self._inactive_repre_ids = set()\n\n        self._init_folder_id = None\n        self._init_product_name = None\n        self._init_repre_name = None\n\n        self._fill_check = False\n        self._project_name = project_name\n        self._folder_id = folder_id\n\n        self._current_folder_btn.setEnabled(folder_id is not None)\n\n        self._controller = controller\n\n        self._items = items\n        self._prepare_content_data()\n\n    def showEvent(self, event):\n        super().showEvent(event)\n        self._show_timer.start()\n\n    def refresh(self, init_refresh=False):\n        \"\"\"Build the need comboboxes with content\"\"\"\n        if not self._fill_check and not init_refresh:\n            return\n\n        self._fill_check = False\n\n        validation_state = ValidationState()\n        self._folders_field.refresh()\n        # Set other comboboxes to empty if any document is missing or\n        #   any folder of loaded representations is archived.\n        self._is_folder_ok(validation_state)\n        if validation_state.folder_ok:\n            product_values = self._get_product_box_values()\n            self._fill_combobox(product_values, \"product\")\n            self._is_product_ok(validation_state)\n\n        if validation_state.folder_ok and validation_state.product_ok:\n            repre_values = sorted(self._representations_box_values())\n            self._fill_combobox(repre_values, \"repre\")\n            self._is_repre_ok(validation_state)\n\n        # Fill comboboxes with values\n        self.set_labels()\n\n        self.apply_validations(validation_state)\n\n        self._build_loaders_menu()\n\n        if init_refresh:\n            # pre select context if possible\n            self._folders_field.set_selected_item(self._init_folder_id)\n            self._products_combox.set_valid_value(self._init_product_name)\n            self._representations_box.set_valid_value(self._init_repre_name)\n\n        self._fill_check = True\n\n    def set_labels(self):\n        folder_label = self._folders_field.get_selected_folder_label()\n        product_label = self._products_combox.get_valid_value()\n        repre_label = self._representations_box.get_valid_value()\n\n        default = \"*No changes\"\n        self._folder_label.setText(folder_label or default)\n        self._product_label.setText(product_label or default)\n        self._repre_label.setText(repre_label or default)\n\n    def apply_validations(self, validation_state):\n        error_msg = \"*Please select\"\n        error_sheet = \"border: 1px solid red;\"\n\n        product_sheet = None\n        repre_sheet = None\n        accept_state = \"\"\n        if validation_state.folder_ok is False:\n            self._folder_label.setText(error_msg)\n        elif validation_state.product_ok is False:\n            product_sheet = error_sheet\n            self._product_label.setText(error_msg)\n        elif validation_state.repre_ok is False:\n            repre_sheet = error_sheet\n            self._repre_label.setText(error_msg)\n\n        if validation_state.all_ok:\n            accept_state = \"1\"\n\n        self._folders_field.set_valid(validation_state.folder_ok)\n        self._products_combox.setStyleSheet(product_sheet or \"\")\n        self._representations_box.setStyleSheet(repre_sheet or \"\")\n\n        self._accept_btn.setEnabled(validation_state.all_ok)\n        self._set_style_property(self._accept_btn, \"state\", accept_state)\n\n    def find_last_versions(self, product_ids):\n        project_name = self._project_name\n        return ayon_api.get_last_versions(\n            project_name,\n            product_ids,\n            fields={\"id\", \"productId\", \"version\"}\n        )\n\n    def _on_show_timer(self):\n        if self._show_counter == 2:\n            self._show_timer.stop()\n            self.refresh(True)\n        else:\n            self._show_counter += 1\n\n    def _prepare_content_data(self):\n        repre_ids = {\n            item[\"representation\"]\n            for item in self._items\n        }\n\n        project_name = self._project_name\n        repre_entities = list(ayon_api.get_representations(\n            project_name,\n            representation_ids=repre_ids,\n        ))\n        repres_by_id = {r[\"id\"]: r for r in repre_entities}\n\n        content_repre_entities_by_id = {}\n        inactive_repre_ids = set()\n        missing_repre_ids = set()\n        version_ids = set()\n        for repre_id in repre_ids:\n            repre_entity = repres_by_id.get(repre_id)\n            if repre_entity is None:\n                missing_repre_ids.add(repre_id)\n            elif not repres_by_id[repre_id][\"active\"]:\n                inactive_repre_ids.add(repre_id)\n                version_ids.add(repre_entity[\"versionId\"])\n            else:\n                content_repre_entities_by_id[repre_id] = repre_entity\n                version_ids.add(repre_entity[\"versionId\"])\n\n        version_entities = ayon_api.get_versions(\n            project_name,\n            version_ids=version_ids\n        )\n        content_version_entities_by_id = {}\n        for version_entity in version_entities:\n            version_id = version_entity[\"id\"]\n            content_version_entities_by_id[version_id] = version_entity\n\n        missing_version_ids = set()\n        product_ids = set()\n        for version_id in version_ids:\n            version_entity = content_version_entities_by_id.get(version_id)\n            if version_entity is None:\n                missing_version_ids.add(version_id)\n            else:\n                product_ids.add(version_entity[\"productId\"])\n\n        product_entities = ayon_api.get_products(\n            project_name, product_ids=product_ids\n        )\n        product_entities_by_id = {p[\"id\"]: p for p in product_entities}\n\n        folder_ids = set()\n        inactive_product_ids = set()\n        missing_product_ids = set()\n        content_product_entities_by_id = {}\n        for product_id in product_ids:\n            product_entity = product_entities_by_id.get(product_id)\n            if product_entity is None:\n                missing_product_ids.add(product_id)\n            else:\n                folder_ids.add(product_entity[\"folderId\"])\n                content_product_entities_by_id[product_id] = product_entity\n\n        folder_entities = ayon_api.get_folders(\n            project_name, folder_ids=folder_ids, active=None\n        )\n        folder_entities_by_id = {\n            folder_entity[\"id\"]: folder_entity\n            for folder_entity in folder_entities\n        }\n\n        missing_folder_ids = set()\n        inactive_folder_ids = set()\n        content_folder_entities_by_id = {}\n        for folder_id in folder_ids:\n            folder_entity = folder_entities_by_id.get(folder_id)\n            if folder_entity is None:\n                missing_folder_ids.add(folder_id)\n            elif not folder_entity[\"active\"]:\n                inactive_folder_ids.add(folder_id)\n            else:\n                content_folder_entities_by_id[folder_id] = folder_entity\n\n        # stash context values, works only for single representation\n        init_folder_id = None\n        init_product_name = None\n        init_repre_name = None\n        if len(repre_entities) == 1:\n            init_repre_entity = repre_entities[0]\n            init_version_entity = content_version_entities_by_id.get(\n                init_repre_entity[\"versionId\"])\n            init_product_entity = None\n            init_folder_entity = None\n            if init_version_entity:\n                init_product_entity = content_product_entities_by_id.get(\n                    init_version_entity[\"productId\"]\n                )\n            if init_product_entity:\n                init_folder_entity = content_folder_entities_by_id.get(\n                    init_product_entity[\"folderId\"]\n                )\n            if init_folder_entity:\n                init_repre_name = init_repre_entity[\"name\"]\n                init_product_name = init_product_entity[\"name\"]\n                init_folder_id = init_folder_entity[\"id\"]\n\n        self._init_folder_id = init_folder_id\n        self._init_product_name = init_product_name\n        self._init_repre_name = init_repre_name\n\n        self._folder_entities_by_id = content_folder_entities_by_id\n        self._product_entities_by_id = content_product_entities_by_id\n        self._version_entities_by_id = content_version_entities_by_id\n        self._repre_entities_by_id = content_repre_entities_by_id\n\n        self._missing_folder_ids = missing_folder_ids\n        self._missing_product_ids = missing_product_ids\n        self._missing_version_ids = missing_version_ids\n        self._missing_repre_ids = missing_repre_ids\n        self._missing_entities = (\n            bool(missing_folder_ids)\n            or bool(missing_version_ids)\n            or bool(missing_product_ids)\n            or bool(missing_repre_ids)\n        )\n\n        self._inactive_folder_ids = inactive_folder_ids\n        self._inactive_product_ids = inactive_product_ids\n        self._inactive_repre_ids = inactive_repre_ids\n\n    def _combobox_value_changed(self, *args, **kwargs):\n        self.refresh()\n\n    def _build_loaders_menu(self):\n        repre_ids = self._get_current_output_repre_ids()\n        loaders = self._get_loaders(repre_ids)\n        # Get and destroy the action group\n        self._accept_btn.clear_actions()\n\n        if not loaders:\n            return\n\n        # Build new action group\n        group = QtWidgets.QActionGroup(self._accept_btn)\n\n        for loader in loaders:\n            # Label\n            label = getattr(loader, \"label\", None)\n            if label is None:\n                label = loader.__name__\n\n            action = group.addAction(label)\n            # action = QtWidgets.QAction(label)\n            action.setData(loader)\n\n            # Support font-awesome icons using the `.icon` and `.color`\n            # attributes on plug-ins.\n            icon = getattr(loader, \"icon\", None)\n            if icon is not None:\n                try:\n                    key = \"fa.{0}\".format(icon)\n                    color = getattr(loader, \"color\", \"white\")\n                    action.setIcon(qtawesome.icon(key, color=color))\n\n                except Exception as exc:\n                    print(\"Unable to set icon for loader {}: {}\".format(\n                        loader, str(exc)\n                    ))\n\n            self._accept_btn.add_action(action)\n\n        group.triggered.connect(self._on_action_clicked)\n\n    def _on_action_clicked(self, action):\n        loader_plugin = action.data()\n        self._trigger_switch(loader_plugin)\n\n    def _get_loaders(self, repre_ids):\n        repre_contexts = None\n        if repre_ids:\n            repre_contexts = get_repres_contexts(repre_ids)\n\n        if not repre_contexts:\n            return list()\n\n        available_loaders = []\n        for loader_plugin in discover_loader_plugins():\n            # Skip loaders without switch method\n            if not hasattr(loader_plugin, \"switch\"):\n                continue\n\n            # Skip utility loaders\n            if (\n                hasattr(loader_plugin, \"is_utility\")\n                and loader_plugin.is_utility\n            ):\n                continue\n            available_loaders.append(loader_plugin)\n\n        loaders = None\n        for repre_context in repre_contexts.values():\n            _loaders = set(loaders_from_repre_context(\n                available_loaders, repre_context\n            ))\n            if loaders is None:\n                loaders = _loaders\n            else:\n                loaders = _loaders.intersection(loaders)\n\n            if not loaders:\n                break\n\n        if loaders is None:\n            loaders = []\n        else:\n            loaders = list(loaders)\n\n        return loaders\n\n    def _fill_combobox(self, values, combobox_type):\n        if combobox_type == \"product\":\n            combobox_widget = self._products_combox\n        elif combobox_type == \"repre\":\n            combobox_widget = self._representations_box\n        else:\n            return\n        selected_value = combobox_widget.get_valid_value()\n\n        # Fill combobox\n        if values is not None:\n            combobox_widget.populate(list(sorted(values)))\n            if selected_value and selected_value in values:\n                index = None\n                for idx in range(combobox_widget.count()):\n                    if selected_value == str(combobox_widget.itemText(idx)):\n                        index = idx\n                        break\n                if index is not None:\n                    combobox_widget.setCurrentIndex(index)\n\n    def _set_style_property(self, widget, name, value):\n        cur_value = widget.property(name)\n        if cur_value == value:\n            return\n        widget.setProperty(name, value)\n        widget.style().polish(widget)\n\n    def _get_current_output_repre_ids(self):\n        # NOTE hero versions are not used because it is expected that\n        # hero version has same representations as latests\n        selected_folder_id = self._folders_field.get_selected_folder_id()\n        selected_product_name = self._products_combox.currentText()\n        selected_repre = self._representations_box.currentText()\n\n        # Nothing is selected\n        # [ ] [ ] [ ]\n        if (\n            not selected_folder_id\n            and not selected_product_name\n            and not selected_repre\n        ):\n            return list(self._repre_entities_by_id.keys())\n\n        # Everything is selected\n        # [x] [x] [x]\n        if selected_folder_id and selected_product_name and selected_repre:\n            return self._get_current_output_repre_ids_xxx(\n                selected_folder_id, selected_product_name, selected_repre\n            )\n\n        # [x] [x] [ ]\n        # If folder and product is selected\n        if selected_folder_id and selected_product_name:\n            return self._get_current_output_repre_ids_xxo(\n                selected_folder_id, selected_product_name\n            )\n\n        # [x] [ ] [x]\n        # If folder and repre is selected\n        if selected_folder_id and selected_repre:\n            return self._get_current_output_repre_ids_xox(\n                selected_folder_id, selected_repre\n            )\n\n        # [x] [ ] [ ]\n        # If folder and product is selected\n        if selected_folder_id:\n            return self._get_current_output_repre_ids_xoo(selected_folder_id)\n\n        # [ ] [x] [x]\n        if selected_product_name and selected_repre:\n            return self._get_current_output_repre_ids_oxx(\n                selected_product_name, selected_repre\n            )\n\n        # [ ] [x] [ ]\n        if selected_product_name:\n            return self._get_current_output_repre_ids_oxo(\n                selected_product_name\n            )\n\n        # [ ] [ ] [x]\n        return self._get_current_output_repre_ids_oox(selected_repre)\n\n    def _get_current_output_repre_ids_xxx(\n        self, folder_id, selected_product_name, selected_repre\n    ):\n        project_name = self._project_name\n        product_entity = ayon_api.get_product_by_name(\n            project_name,\n            selected_product_name,\n            folder_id,\n            fields={\"id\"}\n        )\n\n        product_id = product_entity[\"id\"]\n        last_versions_by_product_id = self.find_last_versions([product_id])\n        version_entity = last_versions_by_product_id.get(product_id)\n        if not version_entity:\n            return []\n\n        repre_entities = ayon_api.get_representations(\n            project_name,\n            version_ids={version_entity[\"id\"]},\n            representation_names={selected_repre},\n            fields={\"id\"}\n        )\n        return {repre_entity[\"id\"] for repre_entity in repre_entities}\n\n    def _get_current_output_repre_ids_xxo(self, folder_id, product_name):\n        project_name = self._project_name\n        product_entity = ayon_api.get_product_by_name(\n            project_name,\n            product_name,\n            folder_id,\n            fields={\"id\"}\n        )\n        if not product_entity:\n            return []\n\n        repre_names = set()\n        for repre_entity in self._repre_entities_by_id.values():\n            repre_names.add(repre_entity[\"name\"])\n\n        # TODO where to take version ids?\n        version_ids = []\n        repre_entities = ayon_api.get_representations(\n            project_name,\n            representation_names=repre_names,\n            version_ids=version_ids,\n            fields={\"id\"}\n        )\n        return {repre_entity[\"id\"] for repre_entity in repre_entities}\n\n    def _get_current_output_repre_ids_xox(self, folder_id, selected_repre):\n        product_names = {\n            product_entity[\"name\"]\n            for product_entity in self._product_entities_by_id.values()\n        }\n\n        project_name = self._project_name\n        product_entities = ayon_api.get_products(\n            project_name,\n            folder_ids=[folder_id],\n            product_names=product_names,\n            fields={\"id\", \"name\"}\n        )\n        product_name_by_id = {\n            product_entity[\"id\"]: product_entity[\"name\"]\n            for product_entity in product_entities\n        }\n        product_ids = list(product_name_by_id.keys())\n        last_versions_by_product_id = self.find_last_versions(product_ids)\n        last_version_id_by_product_name = {}\n        for product_id, last_version in last_versions_by_product_id.items():\n            product_name = product_name_by_id[product_id]\n            last_version_id_by_product_name[product_name] = (\n                last_version[\"id\"]\n            )\n\n        repre_entities = ayon_api.get_representations(\n            project_name,\n            version_ids=last_version_id_by_product_name.values(),\n            representation_names={selected_repre},\n            fields={\"id\"}\n        )\n        return {repre_entity[\"id\"] for repre_entity in repre_entities}\n\n    def _get_current_output_repre_ids_xoo(self, folder_id):\n        project_name = self._project_name\n        repres_by_product_name = collections.defaultdict(set)\n        for repre_entity in self._repre_entities_by_id.values():\n            version_id = repre_entity[\"versionId\"]\n            version_entity = self._version_entities_by_id[version_id]\n            product_id = version_entity[\"productId\"]\n            product_entity = self._product_entities_by_id[product_id]\n            product_name = product_entity[\"name\"]\n            repres_by_product_name[product_name].add(repre_entity[\"name\"])\n\n        product_entities = list(ayon_api.get_products(\n            project_name,\n            folder_ids=[folder_id],\n            product_names=repres_by_product_name.keys(),\n            fields={\"id\", \"name\"}\n        ))\n        product_name_by_id = {\n            product_entity[\"id\"]: product_entity[\"name\"]\n            for product_entity in product_entities\n        }\n        product_ids = list(product_name_by_id.keys())\n        last_versions_by_product_id = self.find_last_versions(product_ids)\n        last_version_id_by_product_name = {}\n        for product_id, last_version in last_versions_by_product_id.items():\n            product_name = product_name_by_id[product_id]\n            last_version_id_by_product_name[product_name] = (\n                last_version[\"id\"]\n            )\n\n        repre_names_by_version_id = {}\n        for product_name, repre_names in repres_by_product_name.items():\n            version_id = last_version_id_by_product_name.get(product_name)\n            # This should not happen but why to crash?\n            if version_id is not None:\n                repre_names_by_version_id[version_id] = list(repre_names)\n\n        repre_entities = ayon_api.get_representations(\n            project_name,\n            names_by_version_ids=repre_names_by_version_id,\n            fields={\"id\"}\n        )\n        return {repre_entity[\"id\"] for repre_entity in repre_entities}\n\n    def _get_current_output_repre_ids_oxx(\n        self, product_name, selected_repre\n    ):\n        project_name = self._project_name\n        product_entities = ayon_api.get_products(\n            project_name,\n            folder_ids=self._folder_entities_by_id.keys(),\n            product_names=[product_name],\n            fields={\"id\"}\n        )\n        product_ids = {\n            product_entity[\"id\"] for product_entity in product_entities\n        }\n        last_versions_by_product_id = self.find_last_versions(product_ids)\n        last_version_ids = {\n            last_version[\"id\"]\n            for last_version in last_versions_by_product_id.values()\n        }\n\n        repre_entities = ayon_api.get_representations(\n            project_name,\n            version_ids=last_version_ids,\n            representation_names={selected_repre},\n            fields={\"id\"}\n        )\n        return {repre_entity[\"id\"] for repre_entity in repre_entities}\n\n    def _get_current_output_repre_ids_oxo(self, product_name):\n        project_name = self._project_name\n        product_entities = ayon_api.get_products(\n            project_name,\n            folder_ids=self._folder_entities_by_id.keys(),\n            product_names={product_name},\n            fields={\"id\", \"folderId\"}\n        )\n        product_entities_by_id = {\n            product_entity[\"id\"]: product_entity\n            for product_entity in product_entities\n        }\n        if not product_entities_by_id:\n            return list()\n\n        last_versions_by_product_id = self.find_last_versions(\n            product_entities_by_id.keys()\n        )\n\n        product_id_by_version_id = {}\n        for product_id, last_version in last_versions_by_product_id.items():\n            version_id = last_version[\"id\"]\n            product_id_by_version_id[version_id] = product_id\n\n        if not product_id_by_version_id:\n            return list()\n\n        repre_names_by_folder_id = collections.defaultdict(set)\n        for repre_entity in self._repre_entities_by_id.values():\n            version_id = repre_entity[\"versionId\"]\n            version_entity = self._version_entities_by_id[version_id]\n            product_id = version_entity[\"productId\"]\n            product_entity = self._product_entities_by_id[product_id]\n            folder_id = product_entity[\"folderId\"]\n            folder_entity = self._folder_entities_by_id[folder_id]\n            folder_id = folder_entity[\"id\"]\n            repre_names_by_folder_id[folder_id].add(repre_entity[\"name\"])\n\n        repre_names_by_version_id = {}\n        for last_version_id, product_id in product_id_by_version_id.items():\n            product_entity = product_entities_by_id[product_id]\n            folder_id = product_entity[\"folderId\"]\n            repre_names = repre_names_by_folder_id.get(folder_id)\n            if not repre_names:\n                continue\n            repre_names_by_version_id[last_version_id] = repre_names\n\n        repre_entities = ayon_api.get_representations(\n            project_name,\n            names_by_version_ids=repre_names_by_version_id,\n            fields={\"id\"}\n        )\n        return {repre_entity[\"id\"] for repre_entity in repre_entities}\n\n    def _get_current_output_repre_ids_oox(self, selected_repre):\n        project_name = self._project_name\n        repre_entities = ayon_api.get_representations(\n            project_name,\n            representation_names=[selected_repre],\n            version_ids=self._version_entities_by_id.keys(),\n            fields={\"id\"}\n        )\n        return {repre_entity[\"id\"] for repre_entity in repre_entities}\n\n    def _get_product_box_values(self):\n        project_name = self._project_name\n        selected_folder_id = self._folders_field.get_selected_folder_id()\n        if selected_folder_id:\n            folder_ids = [selected_folder_id]\n        else:\n            folder_ids = list(self._folder_entities_by_id.keys())\n\n        product_entities = ayon_api.get_products(\n            project_name,\n            folder_ids=folder_ids,\n            fields={\"folderId\", \"name\"}\n        )\n\n        product_names_by_parent_id = collections.defaultdict(set)\n        for product_entity in product_entities:\n            product_names_by_parent_id[product_entity[\"folderId\"]].add(\n                product_entity[\"name\"]\n            )\n\n        possible_product_names = None\n        for product_names in product_names_by_parent_id.values():\n            if possible_product_names is None:\n                possible_product_names = product_names\n            else:\n                possible_product_names = possible_product_names.intersection(\n                    product_names)\n\n            if not possible_product_names:\n                break\n\n        if not possible_product_names:\n            return []\n        return list(possible_product_names)\n\n    def _representations_box_values(self):\n        # NOTE hero versions are not used because it is expected that\n        # hero version has same representations as latests\n        project_name = self._project_name\n        selected_folder_id = self._folders_field.get_selected_folder_id()\n        selected_product_name = self._products_combox.currentText()\n\n        # If nothing is selected\n        # [ ] [ ] [?]\n        if not selected_folder_id and not selected_product_name:\n            # Find all representations of selection's products\n            possible_repres = ayon_api.get_representations(\n                project_name,\n                version_ids=self._version_entities_by_id.keys(),\n                fields={\"versionId\", \"name\"}\n            )\n\n            possible_repres_by_parent = collections.defaultdict(set)\n            for repre in possible_repres:\n                possible_repres_by_parent[repre[\"versionId\"]].add(\n                    repre[\"name\"]\n                )\n\n            output_repres = None\n            for repre_names in possible_repres_by_parent.values():\n                if output_repres is None:\n                    output_repres = repre_names\n                else:\n                    output_repres = (output_repres &amp; repre_names)\n\n                if not output_repres:\n                    break\n\n            return list(output_repres or list())\n\n        # [x] [x] [?]\n        if selected_folder_id and selected_product_name:\n            product_entity = ayon_api.get_product_by_name(\n                project_name,\n                selected_product_name,\n                selected_folder_id,\n                fields={\"id\"}\n            )\n\n            product_id = product_entity[\"id\"]\n            last_versions_by_product_id = self.find_last_versions([product_id])\n            version_entity = last_versions_by_product_id.get(product_id)\n            repre_entities = ayon_api.get_representations(\n                project_name,\n                version_ids={version_entity[\"id\"]},\n                fields={\"name\"}\n            )\n            return {\n                repre_entity[\"name\"]\n                for repre_entity in repre_entities\n            }\n\n        # [x] [ ] [?]\n        # If only folder is selected\n        if selected_folder_id:\n            # Filter products by names from content\n            product_names = {\n                product_entity[\"name\"]\n                for product_entity in self._product_entities_by_id.values()\n            }\n\n            product_entities = ayon_api.get_products(\n                project_name,\n                folder_ids={selected_folder_id},\n                product_names=product_names,\n                fields={\"id\"}\n            )\n            product_ids = {\n                product_entity[\"id\"]\n                for product_entity in product_entities\n            }\n            if not product_ids:\n                return list()\n\n            last_versions_by_product_id = self.find_last_versions(product_ids)\n            product_id_by_version_id = {}\n            for product_id, last_version in (\n                last_versions_by_product_id.items()\n            ):\n                version_id = last_version[\"id\"]\n                product_id_by_version_id[version_id] = product_id\n\n            if not product_id_by_version_id:\n                return list()\n\n            repre_entities = list(ayon_api.get_representations(\n                project_name,\n                version_ids=product_id_by_version_id.keys(),\n                fields={\"name\", \"versionId\"}\n            ))\n            if not repre_entities:\n                return list()\n\n            repre_names_by_parent = collections.defaultdict(set)\n            for repre_entity in repre_entities:\n                repre_names_by_parent[repre_entity[\"versionId\"]].add(\n                    repre_entity[\"name\"]\n                )\n\n            available_repres = None\n            for repre_names in repre_names_by_parent.values():\n                if available_repres is None:\n                    available_repres = repre_names\n                    continue\n\n                available_repres = available_repres.intersection(repre_names)\n\n            return list(available_repres)\n\n        # [ ] [x] [?]\n        product_entities = list(ayon_api.get_products(\n            project_name,\n            folder_ids=self._folder_entities_by_id.keys(),\n            product_names=[selected_product_name],\n            fields={\"id\", \"folderId\"}\n        ))\n        if not product_entities:\n            return list()\n\n        product_entities_by_id = {\n            product_entity[\"id\"]: product_entity\n            for product_entity in product_entities\n        }\n        last_versions_by_product_id = self.find_last_versions(\n            product_entities_by_id.keys()\n        )\n\n        product_id_by_version_id = {}\n        for product_id, last_version in last_versions_by_product_id.items():\n            version_id = last_version[\"id\"]\n            product_id_by_version_id[version_id] = product_id\n\n        if not product_id_by_version_id:\n            return list()\n\n        repre_entities = list(\n            ayon_api.get_representations(\n                project_name,\n                version_ids=product_id_by_version_id.keys(),\n                fields={\"name\", \"versionId\"}\n            )\n        )\n        if not repre_entities:\n            return list()\n\n        repre_names_by_folder_id = collections.defaultdict(set)\n        for repre_entity in repre_entities:\n            product_id = product_id_by_version_id[repre_entity[\"versionId\"]]\n            folder_id = product_entities_by_id[product_id][\"folderId\"]\n            repre_names_by_folder_id[folder_id].add(repre_entity[\"name\"])\n\n        available_repres = None\n        for repre_names in repre_names_by_folder_id.values():\n            if available_repres is None:\n                available_repres = repre_names\n                continue\n\n            available_repres = available_repres.intersection(repre_names)\n\n        return list(available_repres)\n\n    def _is_folder_ok(self, validation_state):\n        selected_folder_id = self._folders_field.get_selected_folder_id()\n        if (\n            selected_folder_id is None\n            and (self._missing_entities or self._inactive_folder_ids)\n        ):\n            validation_state.folder_ok = False\n\n    def _is_product_ok(self, validation_state):\n        selected_folder_id = self._folders_field.get_selected_folder_id()\n        selected_product_name = self._products_combox.get_valid_value()\n\n        # [?] [x] [?]\n        # If product is selected then must be ok\n        if selected_product_name is not None:\n            return\n\n        # [ ] [ ] [?]\n        if selected_folder_id is None:\n            # If there were archived products and folder is not selected\n            if self._inactive_product_ids:\n                validation_state.product_ok = False\n            return\n\n        # [x] [ ] [?]\n        project_name = self._project_name\n        product_entities = ayon_api.get_products(\n            project_name, folder_ids=[selected_folder_id], fields={\"name\"}\n        )\n\n        product_names = set(\n            product_entity[\"name\"]\n            for product_entity in product_entities\n        )\n\n        for product_entity in self._product_entities_by_id.values():\n            if product_entity[\"name\"] not in product_names:\n                validation_state.product_ok = False\n                break\n\n    def _is_repre_ok(self, validation_state):\n        selected_folder_id = self._folders_field.get_selected_folder_id()\n        selected_product_name = self._products_combox.get_valid_value()\n        selected_repre = self._representations_box.get_valid_value()\n\n        # [?] [?] [x]\n        # If product is selected then must be ok\n        if selected_repre is not None:\n            return\n\n        # [ ] [ ] [ ]\n        if selected_folder_id is None and selected_product_name is None:\n            if (\n                self._inactive_repre_ids\n                or self._missing_version_ids\n                or self._missing_repre_ids\n            ):\n                validation_state.repre_ok = False\n            return\n\n        # [x] [x] [ ]\n        project_name = self._project_name\n        if (\n            selected_folder_id is not None\n            and selected_product_name is not None\n        ):\n            product_entity = ayon_api.get_product_by_name(\n                project_name,\n                selected_product_name,\n                selected_folder_id,\n                fields={\"id\"}\n            )\n            product_id = product_entity[\"id\"]\n            last_versions_by_product_id = self.find_last_versions([product_id])\n            last_version = last_versions_by_product_id.get(product_id)\n            if not last_version:\n                validation_state.repre_ok = False\n                return\n\n            repre_entities = ayon_api.get_representations(\n                project_name,\n                version_ids={last_version[\"id\"]},\n                fields={\"name\"}\n            )\n\n            repre_names = set(\n                repre_entity[\"name\"]\n                for repre_entity in repre_entities\n            )\n            for repre_entity in self._repre_entities_by_id.values():\n                if repre_entity[\"name\"] not in repre_names:\n                    validation_state.repre_ok = False\n                    break\n            return\n\n        # [x] [ ] [ ]\n        if selected_folder_id is not None:\n            product_entities = list(ayon_api.get_products(\n                project_name,\n                folder_ids={selected_folder_id},\n                fields={\"id\", \"name\"}\n            ))\n\n            product_name_by_id = {}\n            product_ids = set()\n            for product_entity in product_entities:\n                product_id = product_entity[\"id\"]\n                product_ids.add(product_id)\n                product_name_by_id[product_id] = product_entity[\"name\"]\n\n            last_versions_by_product_id = self.find_last_versions(product_ids)\n\n            product_id_by_version_id = {}\n            for product_id, last_version in (\n                last_versions_by_product_id.items()\n            ):\n                version_id = last_version[\"id\"]\n                product_id_by_version_id[version_id] = product_id\n\n            repre_entities = ayon_api.get_representations(\n                project_name,\n                version_ids=product_id_by_version_id.keys(),\n                fields={\"name\", \"versionId\"}\n            )\n            repres_by_product_name = collections.defaultdict(set)\n            for repre_entity in repre_entities:\n                version_id = repre_entity[\"versionId\"]\n                product_id = product_id_by_version_id[version_id]\n                product_name = product_name_by_id[product_id]\n                repres_by_product_name[product_name].add(repre_entity[\"name\"])\n\n            for repre_entity in self._repre_entities_by_id.values():\n                version_id = repre_entity[\"versionId\"]\n                version_entity = self._version_entities_by_id[version_id]\n                product_id = version_entity[\"productId\"]\n                product_entity = self._product_entities_by_id[product_id]\n                repre_names = repres_by_product_name[product_entity[\"name\"]]\n                if repre_entity[\"name\"] not in repre_names:\n                    validation_state.repre_ok = False\n                    break\n            return\n\n        # [ ] [x] [ ]\n        # Product entities\n        product_entities = ayon_api.get_products(\n            project_name,\n            folder_ids=self._folder_entities_by_id.keys(),\n            product_names={selected_product_name},\n            fields={\"id\", \"name\", \"folderId\"}\n        )\n        product_entities_by_id = {}\n        for product_entity in product_entities:\n            product_entities_by_id[product_entity[\"id\"]] = product_entity\n\n        last_versions_by_product_id = self.find_last_versions(\n            product_entities_by_id.keys()\n        )\n        product_id_by_version_id = {}\n        for product_id, last_version in last_versions_by_product_id.items():\n            version_id = last_version[\"id\"]\n            product_id_by_version_id[version_id] = product_id\n\n        repre_entities = ayon_api.get_representations(\n            project_name,\n            version_ids=product_id_by_version_id.keys(),\n            fields={\"name\", \"versionId\"}\n        )\n        repres_by_folder_id = collections.defaultdict(set)\n        for repre_entity in repre_entities:\n            product_id = product_id_by_version_id[repre_entity[\"versionId\"]]\n            folder_id = product_entities_by_id[product_id][\"folderId\"]\n            repres_by_folder_id[folder_id].add(repre_entity[\"name\"])\n\n        for repre_entity in self._repre_entities_by_id.values():\n            version_id = repre_entity[\"versionId\"]\n            version_entity = self._version_entities_by_id[version_id]\n            product_id = version_entity[\"productId\"]\n            product_entity = self._product_entities_by_id[product_id]\n            folder_id = product_entity[\"folderId\"]\n            repre_names = repres_by_folder_id[folder_id]\n            if repre_entity[\"name\"] not in repre_names:\n                validation_state.repre_ok = False\n                break\n\n    def _on_current_folder(self):\n        # Set initial folder as current.\n        folder_id = self._controller.get_current_folder_id()\n        if not folder_id:\n            return\n\n        selected_folder_id = self._folders_field.get_selected_folder_id()\n        if folder_id == selected_folder_id:\n            return\n\n        self._folders_field.set_selected_item(folder_id)\n        self._combobox_value_changed()\n\n    def _on_accept(self):\n        self._trigger_switch()\n\n    def _trigger_switch(self, loader=None):\n        # Use None when not a valid value or when placeholder value\n        selected_folder_id = self._folders_field.get_selected_folder_id()\n        selected_product_name = self._products_combox.get_valid_value()\n        selected_representation = self._representations_box.get_valid_value()\n\n        project_name = self._project_name\n        if selected_folder_id:\n            folder_ids = {selected_folder_id}\n        else:\n            folder_ids = set(self._folder_entities_by_id.keys())\n\n        product_names = None\n        if selected_product_name:\n            product_names = [selected_product_name]\n\n        product_entities = list(ayon_api.get_products(\n            project_name,\n            product_names=product_names,\n            folder_ids=folder_ids\n        ))\n        product_ids = set()\n        product_entities_by_parent_and_name = collections.defaultdict(dict)\n        for product_entity in product_entities:\n            product_ids.add(product_entity[\"id\"])\n            folder_id = product_entity[\"folderId\"]\n            name = product_entity[\"name\"]\n            product_entities_by_parent_and_name[folder_id][name] = (\n                product_entity\n            )\n\n        # versions\n        _version_entities = ayon_api.get_versions(\n            project_name, product_ids=product_ids\n        )\n        version_entities = list(reversed(\n            sorted(_version_entities, key=lambda item: item[\"version\"])\n        ))\n\n        version_ids = set()\n        version_entities_by_product_id = collections.defaultdict(dict)\n        hero_version_entities_by_product_id = {}\n        for version_entity in version_entities:\n            version_ids.add(version_entity[\"id\"])\n            product_id = version_entity[\"productId\"]\n            version = version_entity[\"version\"]\n            if version &lt; 0:\n                hero_version_entities_by_product_id[product_id] = (\n                    version_entity\n                )\n                continue\n            version_entities_by_product_id[product_id][version] = (\n                version_entity\n            )\n\n        repre_entities = ayon_api.get_representations(\n            project_name, version_ids=version_ids\n        )\n        repre_entities_by_name_version_id = collections.defaultdict(dict)\n        for repre_entity in repre_entities:\n            version_id = repre_entity[\"versionId\"]\n            name = repre_entity[\"name\"]\n            repre_entities_by_name_version_id[version_id][name] = repre_entity\n\n        for container in self._items:\n            self._switch_container(\n                container,\n                loader,\n                selected_folder_id,\n                selected_product_name,\n                selected_representation,\n                product_entities_by_parent_and_name,\n                version_entities_by_product_id,\n                hero_version_entities_by_product_id,\n                repre_entities_by_name_version_id,\n            )\n\n        self.switched.emit()\n\n        self.close()\n\n    def _switch_container(\n        self,\n        container,\n        loader,\n        selected_folder_id,\n        selected_product_name,\n        selected_representation,\n        product_entities_by_parent_and_name,\n        version_entities_by_product_id,\n        hero_version_entities_by_product_id,\n        repre_entities_by_name_version_id,\n    ):\n        container_repre_id = container[\"representation\"]\n        container_repre = self._repre_entities_by_id[container_repre_id]\n        container_repre_name = container_repre[\"name\"]\n        container_version_id = container_repre[\"versionId\"]\n\n        container_version = self._version_entities_by_id[container_version_id]\n\n        container_product_id = container_version[\"productId\"]\n        container_product = self._product_entities_by_id[container_product_id]\n        container_product_name = container_product[\"name\"]\n\n        container_folder_id = container_product[\"folderId\"]\n\n        if selected_folder_id:\n            folder_id = selected_folder_id\n        else:\n            folder_id = container_folder_id\n\n        products_by_name = product_entities_by_parent_and_name[folder_id]\n        if selected_product_name:\n            product_entity = products_by_name[selected_product_name]\n        else:\n            product_entity = products_by_name[container_product[\"name\"]]\n\n        repre_entity = None\n        product_id = product_entity[\"id\"]\n        if container_version[\"version\"] &lt; 0:\n            hero_version = hero_version_entities_by_product_id.get(\n                product_id\n            )\n            if hero_version:\n                _repres = repre_entities_by_name_version_id.get(\n                    hero_version[\"id\"]\n                )\n                if selected_representation:\n                    repre_entity = _repres.get(selected_representation)\n                else:\n                    repre_entity = _repres.get(container_repre_name)\n\n        if not repre_entity:\n            version_entities_by_version = (\n                version_entities_by_product_id[product_id]\n            )\n            # If folder or product are selected for switching, we use latest\n            # version else we try to keep the current container version.\n            version = None\n            if (\n                selected_folder_id in (None, container_folder_id)\n                and selected_product_name in (None, container_product_name)\n            ):\n                version = container_version.get(\"version\")\n\n            version_entity = None\n            if version is not None:\n                version_entity = version_entities_by_version.get(version)\n\n            if version_entity is None:\n                version_name = max(version_entities_by_version)\n                version_entity = version_entities_by_version[version_name]\n\n            version_id = version_entity[\"id\"]\n            repres_by_name = repre_entities_by_name_version_id[version_id]\n            if selected_representation:\n                repre_entity = repres_by_name[selected_representation]\n            else:\n                repre_entity = repres_by_name[container_repre_name]\n\n        error = None\n        try:\n            switch_container(container, repre_entity, loader)\n        except (\n            LoaderSwitchNotImplementedError,\n            IncompatibleLoaderError,\n            LoaderNotFoundError,\n        ) as exc:\n            error = str(exc)\n        except Exception:\n            error = (\n                \"Switch asset failed. \"\n                \"Search console log for more details.\"\n            )\n        if error is not None:\n            log.warning((\n                \"Couldn't switch asset.\"\n                \"See traceback for more information.\"\n            ), exc_info=True)\n            dialog = QtWidgets.QMessageBox(self)\n            dialog.setWindowTitle(\"Switch asset failed\")\n            dialog.setText(error)\n            dialog.exec_()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/sceneinventory/switch_dialog/dialog.html#client.ayon_core.tools.sceneinventory.switch_dialog.dialog.SwitchAssetDialog.refresh","title":"<code>refresh(init_refresh=False)</code>","text":"<p>Build the need comboboxes with content</p> Source code in <code>client/ayon_core/tools/sceneinventory/switch_dialog/dialog.py</code> <pre><code>def refresh(self, init_refresh=False):\n    \"\"\"Build the need comboboxes with content\"\"\"\n    if not self._fill_check and not init_refresh:\n        return\n\n    self._fill_check = False\n\n    validation_state = ValidationState()\n    self._folders_field.refresh()\n    # Set other comboboxes to empty if any document is missing or\n    #   any folder of loaded representations is archived.\n    self._is_folder_ok(validation_state)\n    if validation_state.folder_ok:\n        product_values = self._get_product_box_values()\n        self._fill_combobox(product_values, \"product\")\n        self._is_product_ok(validation_state)\n\n    if validation_state.folder_ok and validation_state.product_ok:\n        repre_values = sorted(self._representations_box_values())\n        self._fill_combobox(repre_values, \"repre\")\n        self._is_repre_ok(validation_state)\n\n    # Fill comboboxes with values\n    self.set_labels()\n\n    self.apply_validations(validation_state)\n\n    self._build_loaders_menu()\n\n    if init_refresh:\n        # pre select context if possible\n        self._folders_field.set_selected_item(self._init_folder_id)\n        self._products_combox.set_valid_value(self._init_product_name)\n        self._representations_box.set_valid_value(self._init_repre_name)\n\n    self._fill_check = True\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/sceneinventory/switch_dialog/folders_input.html","title":"folders_input","text":""},{"location":"autoapi/client/ayon_core/tools/sceneinventory/switch_dialog/folders_input.html#client.ayon_core.tools.sceneinventory.switch_dialog.folders_input.ClickableLineEdit","title":"<code>ClickableLineEdit</code>","text":"<p>               Bases: <code>QLineEdit</code></p> <p>QLineEdit capturing left mouse click.</p> <p>Triggers <code>clicked</code> signal on mouse click.</p> Source code in <code>client/ayon_core/tools/sceneinventory/switch_dialog/folders_input.py</code> <pre><code>class ClickableLineEdit(QtWidgets.QLineEdit):\n    \"\"\"QLineEdit capturing left mouse click.\n\n    Triggers `clicked` signal on mouse click.\n    \"\"\"\n    clicked = QtCore.Signal()\n\n    def __init__(self, *args, **kwargs):\n        super(ClickableLineEdit, self).__init__(*args, **kwargs)\n        self.setReadOnly(True)\n        self._mouse_pressed = False\n\n    def mousePressEvent(self, event):\n        if event.button() == QtCore.Qt.LeftButton:\n            self._mouse_pressed = True\n        event.accept()\n\n    def mouseMoveEvent(self, event):\n        event.accept()\n\n    def mouseReleaseEvent(self, event):\n        if self._mouse_pressed:\n            self._mouse_pressed = False\n            if self.rect().contains(event.pos()):\n                self.clicked.emit()\n        event.accept()\n\n    def mouseDoubleClickEvent(self, event):\n        event.accept()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/sceneinventory/switch_dialog/folders_input.html#client.ayon_core.tools.sceneinventory.switch_dialog.folders_input.FoldersDialog","title":"<code>FoldersDialog</code>","text":"<p>               Bases: <code>QDialog</code></p> <p>Dialog to select asset for a context of instance.</p> Source code in <code>client/ayon_core/tools/sceneinventory/switch_dialog/folders_input.py</code> <pre><code>class FoldersDialog(QtWidgets.QDialog):\n    \"\"\"Dialog to select asset for a context of instance.\"\"\"\n\n    def __init__(self, controller, parent):\n        super(FoldersDialog, self).__init__(parent)\n        self.setWindowTitle(\"Select folder\")\n\n        filter_input = PlaceholderLineEdit(self)\n        filter_input.setPlaceholderText(\"Filter folders..\")\n\n        controller_wrap = ControllerWrap(controller)\n        folders_widget = FoldersWidget(controller_wrap, self)\n        folders_widget.set_deselectable(True)\n\n        ok_btn = QtWidgets.QPushButton(\"OK\", self)\n        cancel_btn = QtWidgets.QPushButton(\"Cancel\", self)\n\n        btns_layout = QtWidgets.QHBoxLayout()\n        btns_layout.addStretch(1)\n        btns_layout.addWidget(ok_btn)\n        btns_layout.addWidget(cancel_btn)\n\n        layout = QtWidgets.QVBoxLayout(self)\n        layout.addWidget(filter_input, 0)\n        layout.addWidget(folders_widget, 1)\n        layout.addLayout(btns_layout, 0)\n\n        folders_widget.double_clicked.connect(self._on_ok_clicked)\n        folders_widget.refreshed.connect(self._on_folders_refresh)\n        filter_input.textChanged.connect(self._on_filter_change)\n        ok_btn.clicked.connect(self._on_ok_clicked)\n        cancel_btn.clicked.connect(self._on_cancel_clicked)\n\n        self._filter_input = filter_input\n        self._ok_btn = ok_btn\n        self._cancel_btn = cancel_btn\n\n        self._folders_widget = folders_widget\n        self._controller_wrap = controller_wrap\n\n        # Set selected folder only when user confirms the dialog\n        self._selected_folder_id = None\n        self._selected_folder_label = None\n\n        self._folder_id_to_select = NOT_SET\n\n        self._first_show = True\n        self._default_height = 500\n\n    def showEvent(self, event):\n        \"\"\"Refresh asset model on show.\"\"\"\n        super(FoldersDialog, self).showEvent(event)\n        if self._first_show:\n            self._first_show = False\n            self._on_first_show()\n\n    def refresh(self):\n        project_name = self._controller_wrap.get_current_project_name()\n        self._folders_widget.set_project_name(project_name)\n\n    def _on_first_show(self):\n        center = self.rect().center()\n        size = self.size()\n        size.setHeight(self._default_height)\n\n        self.resize(size)\n        new_pos = self.mapToGlobal(center)\n        new_pos.setX(new_pos.x() - int(self.width() / 2))\n        new_pos.setY(new_pos.y() - int(self.height() / 2))\n        self.move(new_pos)\n\n    def _on_folders_refresh(self):\n        if self._folder_id_to_select is NOT_SET:\n            return\n        self._folders_widget.set_selected_folder(self._folder_id_to_select)\n        self._folder_id_to_select = NOT_SET\n\n    def _on_filter_change(self, text):\n        \"\"\"Trigger change of filter of folders.\"\"\"\n\n        self._folders_widget.set_name_filter(text)\n\n    def _on_cancel_clicked(self):\n        self.done(0)\n\n    def _on_ok_clicked(self):\n        self._selected_folder_id = (\n            self._folders_widget.get_selected_folder_id()\n        )\n        self._selected_folder_label = (\n            self._folders_widget.get_selected_folder_label()\n        )\n        self.done(1)\n\n    def set_selected_folder(self, folder_id):\n        \"\"\"Change preselected folder before showing the dialog.\n\n        This also resets model and clean filter.\n        \"\"\"\n\n        if (\n            self._folders_widget.is_refreshing\n            or self._folders_widget.get_project_name() is None\n        ):\n            self._folder_id_to_select = folder_id\n        else:\n            self._folders_widget.set_selected_folder(folder_id)\n\n    def get_selected_folder_id(self):\n        \"\"\"Get selected folder id.\n\n        Returns:\n            Union[str, None]: Selected folder id or None if nothing\n                is selected.\n        \"\"\"\n        return self._selected_folder_id\n\n    def get_selected_folder_label(self):\n        return self._selected_folder_label\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/sceneinventory/switch_dialog/folders_input.html#client.ayon_core.tools.sceneinventory.switch_dialog.folders_input.FoldersDialog.get_selected_folder_id","title":"<code>get_selected_folder_id()</code>","text":"<p>Get selected folder id.</p> <p>Returns:</p> Type Description <p>Union[str, None]: Selected folder id or None if nothing is selected.</p> Source code in <code>client/ayon_core/tools/sceneinventory/switch_dialog/folders_input.py</code> <pre><code>def get_selected_folder_id(self):\n    \"\"\"Get selected folder id.\n\n    Returns:\n        Union[str, None]: Selected folder id or None if nothing\n            is selected.\n    \"\"\"\n    return self._selected_folder_id\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/sceneinventory/switch_dialog/folders_input.html#client.ayon_core.tools.sceneinventory.switch_dialog.folders_input.FoldersDialog.set_selected_folder","title":"<code>set_selected_folder(folder_id)</code>","text":"<p>Change preselected folder before showing the dialog.</p> <p>This also resets model and clean filter.</p> Source code in <code>client/ayon_core/tools/sceneinventory/switch_dialog/folders_input.py</code> <pre><code>def set_selected_folder(self, folder_id):\n    \"\"\"Change preselected folder before showing the dialog.\n\n    This also resets model and clean filter.\n    \"\"\"\n\n    if (\n        self._folders_widget.is_refreshing\n        or self._folders_widget.get_project_name() is None\n    ):\n        self._folder_id_to_select = folder_id\n    else:\n        self._folders_widget.set_selected_folder(folder_id)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/sceneinventory/switch_dialog/folders_input.html#client.ayon_core.tools.sceneinventory.switch_dialog.folders_input.FoldersDialog.showEvent","title":"<code>showEvent(event)</code>","text":"<p>Refresh asset model on show.</p> Source code in <code>client/ayon_core/tools/sceneinventory/switch_dialog/folders_input.py</code> <pre><code>def showEvent(self, event):\n    \"\"\"Refresh asset model on show.\"\"\"\n    super(FoldersDialog, self).showEvent(event)\n    if self._first_show:\n        self._first_show = False\n        self._on_first_show()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/sceneinventory/switch_dialog/folders_input.html#client.ayon_core.tools.sceneinventory.switch_dialog.folders_input.FoldersField","title":"<code>FoldersField</code>","text":"<p>               Bases: <code>BaseClickableFrame</code></p> <p>Field where asset name of selected instance/s is showed.</p> <p>Click on the field will trigger <code>FoldersDialog</code>.</p> Source code in <code>client/ayon_core/tools/sceneinventory/switch_dialog/folders_input.py</code> <pre><code>class FoldersField(BaseClickableFrame):\n    \"\"\"Field where asset name of selected instance/s is showed.\n\n    Click on the field will trigger `FoldersDialog`.\n    \"\"\"\n    value_changed = QtCore.Signal()\n\n    def __init__(self, controller, parent):\n        super(FoldersField, self).__init__(parent)\n        self.setObjectName(\"FolderPathInputWidget\")\n\n        # Don't use 'self' for parent!\n        # - this widget has specific styles\n        dialog = FoldersDialog(controller, parent)\n\n        name_input = ClickableLineEdit(self)\n        name_input.setObjectName(\"FolderPathInput\")\n\n        icon = qtawesome.icon(\"fa.window-maximize\", color=\"white\")\n        icon_btn = QtWidgets.QPushButton(self)\n        icon_btn.setIcon(icon)\n        icon_btn.setObjectName(\"FolderPathInputButton\")\n\n        layout = QtWidgets.QHBoxLayout(self)\n        layout.setContentsMargins(0, 0, 0, 0)\n        layout.setSpacing(0)\n        layout.addWidget(name_input, 1)\n        layout.addWidget(icon_btn, 0)\n\n        # Make sure all widgets are vertically extended to highest widget\n        for widget in (\n            name_input,\n            icon_btn\n        ):\n            w_size_policy = widget.sizePolicy()\n            w_size_policy.setVerticalPolicy(\n                QtWidgets.QSizePolicy.MinimumExpanding)\n            widget.setSizePolicy(w_size_policy)\n\n        size_policy = self.sizePolicy()\n        size_policy.setVerticalPolicy(QtWidgets.QSizePolicy.Maximum)\n        self.setSizePolicy(size_policy)\n\n        name_input.clicked.connect(self._mouse_release_callback)\n        icon_btn.clicked.connect(self._mouse_release_callback)\n        dialog.finished.connect(self._on_dialog_finish)\n\n        self._controller = controller\n        self._dialog = dialog\n        self._name_input = name_input\n        self._icon_btn = icon_btn\n\n        self._selected_folder_id = None\n        self._selected_folder_label = None\n        self._selected_items = []\n        self._is_valid = True\n\n    def refresh(self):\n        self._dialog.refresh()\n\n    def is_valid(self):\n        \"\"\"Is asset valid.\"\"\"\n        return self._is_valid\n\n    def get_selected_folder_id(self):\n        \"\"\"Selected asset names.\"\"\"\n        return self._selected_folder_id\n\n    def get_selected_folder_label(self):\n        return self._selected_folder_label\n\n    def set_text(self, text):\n        \"\"\"Set text in text field.\n\n        Does not change selected items (assets).\n        \"\"\"\n        self._name_input.setText(text)\n\n    def set_valid(self, is_valid):\n        state = \"\"\n        if not is_valid:\n            state = \"invalid\"\n        self._set_state_property(state)\n\n    def set_selected_item(self, folder_id=None, folder_label=None):\n        \"\"\"Set folder for selection.\n\n        Args:\n            folder_id (Optional[str]): Folder id to select.\n            folder_label (Optional[str]): Folder label.\n        \"\"\"\n\n        self._selected_folder_id = folder_id\n        if not folder_id:\n            folder_label = None\n        elif folder_id and not folder_label:\n            folder_label = self._controller.get_folder_label(folder_id)\n        self._selected_folder_label = folder_label\n        self.set_text(folder_label if folder_label else \"&lt;folder&gt;\")\n\n    def _on_dialog_finish(self, result):\n        if not result:\n            return\n\n        folder_id = self._dialog.get_selected_folder_id()\n        folder_label = self._dialog.get_selected_folder_label()\n        self.set_selected_item(folder_id, folder_label)\n\n        self.value_changed.emit()\n\n    def _mouse_release_callback(self):\n        self._dialog.set_selected_folder(self._selected_folder_id)\n        self._dialog.open()\n\n    def _set_state_property(self, state):\n        set_style_property(self, \"state\", state)\n        set_style_property(self._name_input, \"state\", state)\n        set_style_property(self._icon_btn, \"state\", state)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/sceneinventory/switch_dialog/folders_input.html#client.ayon_core.tools.sceneinventory.switch_dialog.folders_input.FoldersField.get_selected_folder_id","title":"<code>get_selected_folder_id()</code>","text":"<p>Selected asset names.</p> Source code in <code>client/ayon_core/tools/sceneinventory/switch_dialog/folders_input.py</code> <pre><code>def get_selected_folder_id(self):\n    \"\"\"Selected asset names.\"\"\"\n    return self._selected_folder_id\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/sceneinventory/switch_dialog/folders_input.html#client.ayon_core.tools.sceneinventory.switch_dialog.folders_input.FoldersField.is_valid","title":"<code>is_valid()</code>","text":"<p>Is asset valid.</p> Source code in <code>client/ayon_core/tools/sceneinventory/switch_dialog/folders_input.py</code> <pre><code>def is_valid(self):\n    \"\"\"Is asset valid.\"\"\"\n    return self._is_valid\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/sceneinventory/switch_dialog/folders_input.html#client.ayon_core.tools.sceneinventory.switch_dialog.folders_input.FoldersField.set_selected_item","title":"<code>set_selected_item(folder_id=None, folder_label=None)</code>","text":"<p>Set folder for selection.</p> <p>Parameters:</p> Name Type Description Default <code>folder_id</code> <code>Optional[str]</code> <p>Folder id to select.</p> <code>None</code> <code>folder_label</code> <code>Optional[str]</code> <p>Folder label.</p> <code>None</code> Source code in <code>client/ayon_core/tools/sceneinventory/switch_dialog/folders_input.py</code> <pre><code>def set_selected_item(self, folder_id=None, folder_label=None):\n    \"\"\"Set folder for selection.\n\n    Args:\n        folder_id (Optional[str]): Folder id to select.\n        folder_label (Optional[str]): Folder label.\n    \"\"\"\n\n    self._selected_folder_id = folder_id\n    if not folder_id:\n        folder_label = None\n    elif folder_id and not folder_label:\n        folder_label = self._controller.get_folder_label(folder_id)\n    self._selected_folder_label = folder_label\n    self.set_text(folder_label if folder_label else \"&lt;folder&gt;\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/sceneinventory/switch_dialog/folders_input.html#client.ayon_core.tools.sceneinventory.switch_dialog.folders_input.FoldersField.set_text","title":"<code>set_text(text)</code>","text":"<p>Set text in text field.</p> <p>Does not change selected items (assets).</p> Source code in <code>client/ayon_core/tools/sceneinventory/switch_dialog/folders_input.py</code> <pre><code>def set_text(self, text):\n    \"\"\"Set text in text field.\n\n    Does not change selected items (assets).\n    \"\"\"\n    self._name_input.setText(text)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/sceneinventory/switch_dialog/widgets.html","title":"widgets","text":""},{"location":"autoapi/client/ayon_core/tools/sceneinventory/switch_dialog/widgets.html#client.ayon_core.tools.sceneinventory.switch_dialog.widgets.SearchComboBox","title":"<code>SearchComboBox</code>","text":"<p>               Bases: <code>QComboBox</code></p> <p>Searchable ComboBox with empty placeholder value as first value</p> Source code in <code>client/ayon_core/tools/sceneinventory/switch_dialog/widgets.py</code> <pre><code>class SearchComboBox(QtWidgets.QComboBox):\n    \"\"\"Searchable ComboBox with empty placeholder value as first value\"\"\"\n\n    def __init__(self, parent):\n        super(SearchComboBox, self).__init__(parent)\n\n        self.setEditable(True)\n        self.setInsertPolicy(QtWidgets.QComboBox.NoInsert)\n\n        combobox_delegate = QtWidgets.QStyledItemDelegate(self)\n        self.setItemDelegate(combobox_delegate)\n\n        completer = self.completer()\n        completer.setCompletionMode(\n            QtWidgets.QCompleter.PopupCompletion\n        )\n        completer.setCaseSensitivity(QtCore.Qt.CaseInsensitive)\n\n        completer_view = completer.popup()\n        completer_view.setObjectName(\"CompleterView\")\n        completer_delegate = QtWidgets.QStyledItemDelegate(completer_view)\n        completer_view.setItemDelegate(completer_delegate)\n        completer_view.setStyleSheet(style.load_stylesheet())\n\n        self._combobox_delegate = combobox_delegate\n\n        self._completer_delegate = completer_delegate\n        self._completer = completer\n\n    def set_placeholder(self, placeholder):\n        self.lineEdit().setPlaceholderText(placeholder)\n\n    def populate(self, items):\n        self.clear()\n        self.addItems([\"\"])     # ensure first item is placeholder\n        self.addItems(items)\n\n    def get_valid_value(self):\n        \"\"\"Return the current text if it's a valid value else None\n\n        Note: The empty placeholder value is valid and returns as \"\"\n\n        \"\"\"\n\n        text = self.currentText()\n        lookup = set(self.itemText(i) for i in range(self.count()))\n        if text not in lookup:\n            return None\n\n        return text or None\n\n    def set_valid_value(self, value):\n        \"\"\"Try to locate 'value' and pre-select it in dropdown.\"\"\"\n        index = self.findText(value)\n        if index &gt; -1:\n            self.setCurrentIndex(index)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/sceneinventory/switch_dialog/widgets.html#client.ayon_core.tools.sceneinventory.switch_dialog.widgets.SearchComboBox.get_valid_value","title":"<code>get_valid_value()</code>","text":"<p>Return the current text if it's a valid value else None</p> <p>Note: The empty placeholder value is valid and returns as \"\"</p> Source code in <code>client/ayon_core/tools/sceneinventory/switch_dialog/widgets.py</code> <pre><code>def get_valid_value(self):\n    \"\"\"Return the current text if it's a valid value else None\n\n    Note: The empty placeholder value is valid and returns as \"\"\n\n    \"\"\"\n\n    text = self.currentText()\n    lookup = set(self.itemText(i) for i in range(self.count()))\n    if text not in lookup:\n        return None\n\n    return text or None\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/sceneinventory/switch_dialog/widgets.html#client.ayon_core.tools.sceneinventory.switch_dialog.widgets.SearchComboBox.set_valid_value","title":"<code>set_valid_value(value)</code>","text":"<p>Try to locate 'value' and pre-select it in dropdown.</p> Source code in <code>client/ayon_core/tools/sceneinventory/switch_dialog/widgets.py</code> <pre><code>def set_valid_value(self, value):\n    \"\"\"Try to locate 'value' and pre-select it in dropdown.\"\"\"\n    index = self.findText(value)\n    if index &gt; -1:\n        self.setCurrentIndex(index)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/stdout_broker/index.html","title":"stdout_broker","text":""},{"location":"autoapi/client/ayon_core/tools/stdout_broker/index.html#client.ayon_core.tools.stdout_broker.StdOutBroker","title":"<code>StdOutBroker</code>","text":"<p>Application showing console in Services tray for non python hosts instead of cmd window.</p> Source code in <code>client/ayon_core/tools/stdout_broker/broker.py</code> <pre><code>class StdOutBroker:\n    \"\"\"\n    Application showing console in Services tray for non python hosts\n    instead of cmd window.\n    \"\"\"\n    MAX_LINES = 10000\n    TIMER_TIMEOUT = 0.200\n\n    def __init__(self, host_name):\n        self.host_name = host_name\n        self.webserver_client = None\n\n        self.original_stdout_write = None\n        self.original_stderr_write = None\n        self.log_queue = collections.deque()\n\n        date_str = datetime.now().strftime(\"%d%m%Y%H%M%S\")\n        self.host_id = \"{}_{}\".format(self.host_name, date_str)\n\n        self._std_available = False\n        self._is_running = False\n        self._catch_std_outputs()\n\n        self._timer = None\n\n    @property\n    def send_to_tray(self):\n        \"\"\"Checks if connected to tray and have access to logs.\"\"\"\n        return self.webserver_client and self._std_available\n\n    def start(self):\n        \"\"\"Start app, create and start timer\"\"\"\n        if not self._std_available or self._is_running:\n            return\n        self._is_running = True\n        self._create_timer()\n        self._connect_to_tray()\n\n    def stop(self):\n        \"\"\"Disconnect from Tray, process last logs\"\"\"\n        if not self._is_running:\n            return\n        self._is_running = False\n        self._process_queue()\n        self._disconnect_from_tray()\n\n    def host_connected(self):\n        \"\"\"Send to Tray console that host is ready - icon change. \"\"\"\n        log.info(\"Host {} connected\".format(self.host_id))\n\n        payload = {\n            \"host\": self.host_id,\n            \"action\": HostMsgAction.INITIALIZED,\n            \"text\": \"Integration with {}\".format(\n                str.capitalize(self.host_name))\n        }\n        self._send(payload)\n\n    def _create_timer(self):\n        timer = threading.Timer(self.TIMER_TIMEOUT, self._timer_callback)\n        timer.start()\n        self._timer = timer\n\n    def _timer_callback(self):\n        if not self._is_running:\n            return\n        self._process_queue()\n        self._create_timer()\n\n    def _connect_to_tray(self):\n        \"\"\"Connect to Tray webserver to pass console output. \"\"\"\n        if not self._std_available:  # not content to log\n            return\n        ws = websocket.WebSocket()\n        webserver_url = os.environ.get(\"AYON_WEBSERVER_URL\")\n\n        if not webserver_url:\n            print(\"Unknown webserver url, cannot connect to pass log\")\n            return\n\n        webserver_url = webserver_url.replace(\"http\", \"ws\")\n        ws.connect(\"{}/ws/host_listener\".format(webserver_url))\n        self.webserver_client = ws\n\n        payload = {\n            \"host\": self.host_id,\n            \"action\": HostMsgAction.CONNECTING,\n            \"text\": \"Integration with {}\".format(\n                str.capitalize(self.host_name))\n        }\n        self._send(payload)\n\n    def _disconnect_from_tray(self):\n        \"\"\"Send to Tray that host is closing - remove from Services. \"\"\"\n        print(\"Host {} closing\".format(self.host_name))\n        if not self.webserver_client:\n            return\n\n        payload = {\n            \"host\": self.host_id,\n            \"action\": HostMsgAction.CLOSE,\n            \"text\": \"Integration with {}\".format(\n                str.capitalize(self.host_name))\n        }\n\n        self._send(payload)\n        self.webserver_client.close()\n\n    def _catch_std_outputs(self):\n        \"\"\"Redirects standard out and error to own functions\"\"\"\n        if sys.stdout:\n            self.original_stdout_write = sys.stdout.write\n            sys.stdout.write = self._my_stdout_write\n            self._std_available = True\n\n        if sys.stderr:\n            self.original_stderr_write = sys.stderr.write\n            sys.stderr.write = self._my_stderr_write\n            self._std_available = True\n\n    def _my_stdout_write(self, text):\n        \"\"\"Appends outputted text to queue, keep writing to original stdout\"\"\"\n        if self.original_stdout_write is not None:\n            self.original_stdout_write(text)\n        if self.send_to_tray:\n            self.log_queue.append(text)\n\n    def _my_stderr_write(self, text):\n        \"\"\"Appends outputted text to queue, keep writing to original stderr\"\"\"\n        if self.original_stderr_write is not None:\n            self.original_stderr_write(text)\n        if self.send_to_tray:\n            self.log_queue.append(text)\n\n    def _process_queue(self):\n        \"\"\"Sends lines and purges queue\"\"\"\n        if not self.send_to_tray:\n            return\n\n        lines = tuple(self.log_queue)\n        self.log_queue.clear()\n        if lines:\n            payload = {\n                \"host\": self.host_id,\n                \"action\": HostMsgAction.ADD,\n                \"text\": \"\\n\".join(lines)\n            }\n\n            self._send(payload)\n\n    def _send(self, payload):\n        \"\"\"Worker method to send to existing websocket connection.\"\"\"\n        if not self.send_to_tray:\n            return\n\n        try:\n            self.webserver_client.send(json.dumps(payload))\n        except ConnectionResetError:  # Tray closed\n            self._connect_to_tray()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/stdout_broker/index.html#client.ayon_core.tools.stdout_broker.StdOutBroker.send_to_tray","title":"<code>send_to_tray</code>  <code>property</code>","text":"<p>Checks if connected to tray and have access to logs.</p>"},{"location":"autoapi/client/ayon_core/tools/stdout_broker/index.html#client.ayon_core.tools.stdout_broker.StdOutBroker.host_connected","title":"<code>host_connected()</code>","text":"<p>Send to Tray console that host is ready - icon change.</p> Source code in <code>client/ayon_core/tools/stdout_broker/broker.py</code> <pre><code>def host_connected(self):\n    \"\"\"Send to Tray console that host is ready - icon change. \"\"\"\n    log.info(\"Host {} connected\".format(self.host_id))\n\n    payload = {\n        \"host\": self.host_id,\n        \"action\": HostMsgAction.INITIALIZED,\n        \"text\": \"Integration with {}\".format(\n            str.capitalize(self.host_name))\n    }\n    self._send(payload)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/stdout_broker/index.html#client.ayon_core.tools.stdout_broker.StdOutBroker.start","title":"<code>start()</code>","text":"<p>Start app, create and start timer</p> Source code in <code>client/ayon_core/tools/stdout_broker/broker.py</code> <pre><code>def start(self):\n    \"\"\"Start app, create and start timer\"\"\"\n    if not self._std_available or self._is_running:\n        return\n    self._is_running = True\n    self._create_timer()\n    self._connect_to_tray()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/stdout_broker/index.html#client.ayon_core.tools.stdout_broker.StdOutBroker.stop","title":"<code>stop()</code>","text":"<p>Disconnect from Tray, process last logs</p> Source code in <code>client/ayon_core/tools/stdout_broker/broker.py</code> <pre><code>def stop(self):\n    \"\"\"Disconnect from Tray, process last logs\"\"\"\n    if not self._is_running:\n        return\n    self._is_running = False\n    self._process_queue()\n    self._disconnect_from_tray()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/stdout_broker/broker.html","title":"broker","text":""},{"location":"autoapi/client/ayon_core/tools/stdout_broker/broker.html#client.ayon_core.tools.stdout_broker.broker.StdOutBroker","title":"<code>StdOutBroker</code>","text":"<p>Application showing console in Services tray for non python hosts instead of cmd window.</p> Source code in <code>client/ayon_core/tools/stdout_broker/broker.py</code> <pre><code>class StdOutBroker:\n    \"\"\"\n    Application showing console in Services tray for non python hosts\n    instead of cmd window.\n    \"\"\"\n    MAX_LINES = 10000\n    TIMER_TIMEOUT = 0.200\n\n    def __init__(self, host_name):\n        self.host_name = host_name\n        self.webserver_client = None\n\n        self.original_stdout_write = None\n        self.original_stderr_write = None\n        self.log_queue = collections.deque()\n\n        date_str = datetime.now().strftime(\"%d%m%Y%H%M%S\")\n        self.host_id = \"{}_{}\".format(self.host_name, date_str)\n\n        self._std_available = False\n        self._is_running = False\n        self._catch_std_outputs()\n\n        self._timer = None\n\n    @property\n    def send_to_tray(self):\n        \"\"\"Checks if connected to tray and have access to logs.\"\"\"\n        return self.webserver_client and self._std_available\n\n    def start(self):\n        \"\"\"Start app, create and start timer\"\"\"\n        if not self._std_available or self._is_running:\n            return\n        self._is_running = True\n        self._create_timer()\n        self._connect_to_tray()\n\n    def stop(self):\n        \"\"\"Disconnect from Tray, process last logs\"\"\"\n        if not self._is_running:\n            return\n        self._is_running = False\n        self._process_queue()\n        self._disconnect_from_tray()\n\n    def host_connected(self):\n        \"\"\"Send to Tray console that host is ready - icon change. \"\"\"\n        log.info(\"Host {} connected\".format(self.host_id))\n\n        payload = {\n            \"host\": self.host_id,\n            \"action\": HostMsgAction.INITIALIZED,\n            \"text\": \"Integration with {}\".format(\n                str.capitalize(self.host_name))\n        }\n        self._send(payload)\n\n    def _create_timer(self):\n        timer = threading.Timer(self.TIMER_TIMEOUT, self._timer_callback)\n        timer.start()\n        self._timer = timer\n\n    def _timer_callback(self):\n        if not self._is_running:\n            return\n        self._process_queue()\n        self._create_timer()\n\n    def _connect_to_tray(self):\n        \"\"\"Connect to Tray webserver to pass console output. \"\"\"\n        if not self._std_available:  # not content to log\n            return\n        ws = websocket.WebSocket()\n        webserver_url = os.environ.get(\"AYON_WEBSERVER_URL\")\n\n        if not webserver_url:\n            print(\"Unknown webserver url, cannot connect to pass log\")\n            return\n\n        webserver_url = webserver_url.replace(\"http\", \"ws\")\n        ws.connect(\"{}/ws/host_listener\".format(webserver_url))\n        self.webserver_client = ws\n\n        payload = {\n            \"host\": self.host_id,\n            \"action\": HostMsgAction.CONNECTING,\n            \"text\": \"Integration with {}\".format(\n                str.capitalize(self.host_name))\n        }\n        self._send(payload)\n\n    def _disconnect_from_tray(self):\n        \"\"\"Send to Tray that host is closing - remove from Services. \"\"\"\n        print(\"Host {} closing\".format(self.host_name))\n        if not self.webserver_client:\n            return\n\n        payload = {\n            \"host\": self.host_id,\n            \"action\": HostMsgAction.CLOSE,\n            \"text\": \"Integration with {}\".format(\n                str.capitalize(self.host_name))\n        }\n\n        self._send(payload)\n        self.webserver_client.close()\n\n    def _catch_std_outputs(self):\n        \"\"\"Redirects standard out and error to own functions\"\"\"\n        if sys.stdout:\n            self.original_stdout_write = sys.stdout.write\n            sys.stdout.write = self._my_stdout_write\n            self._std_available = True\n\n        if sys.stderr:\n            self.original_stderr_write = sys.stderr.write\n            sys.stderr.write = self._my_stderr_write\n            self._std_available = True\n\n    def _my_stdout_write(self, text):\n        \"\"\"Appends outputted text to queue, keep writing to original stdout\"\"\"\n        if self.original_stdout_write is not None:\n            self.original_stdout_write(text)\n        if self.send_to_tray:\n            self.log_queue.append(text)\n\n    def _my_stderr_write(self, text):\n        \"\"\"Appends outputted text to queue, keep writing to original stderr\"\"\"\n        if self.original_stderr_write is not None:\n            self.original_stderr_write(text)\n        if self.send_to_tray:\n            self.log_queue.append(text)\n\n    def _process_queue(self):\n        \"\"\"Sends lines and purges queue\"\"\"\n        if not self.send_to_tray:\n            return\n\n        lines = tuple(self.log_queue)\n        self.log_queue.clear()\n        if lines:\n            payload = {\n                \"host\": self.host_id,\n                \"action\": HostMsgAction.ADD,\n                \"text\": \"\\n\".join(lines)\n            }\n\n            self._send(payload)\n\n    def _send(self, payload):\n        \"\"\"Worker method to send to existing websocket connection.\"\"\"\n        if not self.send_to_tray:\n            return\n\n        try:\n            self.webserver_client.send(json.dumps(payload))\n        except ConnectionResetError:  # Tray closed\n            self._connect_to_tray()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/stdout_broker/broker.html#client.ayon_core.tools.stdout_broker.broker.StdOutBroker.send_to_tray","title":"<code>send_to_tray</code>  <code>property</code>","text":"<p>Checks if connected to tray and have access to logs.</p>"},{"location":"autoapi/client/ayon_core/tools/stdout_broker/broker.html#client.ayon_core.tools.stdout_broker.broker.StdOutBroker.host_connected","title":"<code>host_connected()</code>","text":"<p>Send to Tray console that host is ready - icon change.</p> Source code in <code>client/ayon_core/tools/stdout_broker/broker.py</code> <pre><code>def host_connected(self):\n    \"\"\"Send to Tray console that host is ready - icon change. \"\"\"\n    log.info(\"Host {} connected\".format(self.host_id))\n\n    payload = {\n        \"host\": self.host_id,\n        \"action\": HostMsgAction.INITIALIZED,\n        \"text\": \"Integration with {}\".format(\n            str.capitalize(self.host_name))\n    }\n    self._send(payload)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/stdout_broker/broker.html#client.ayon_core.tools.stdout_broker.broker.StdOutBroker.start","title":"<code>start()</code>","text":"<p>Start app, create and start timer</p> Source code in <code>client/ayon_core/tools/stdout_broker/broker.py</code> <pre><code>def start(self):\n    \"\"\"Start app, create and start timer\"\"\"\n    if not self._std_available or self._is_running:\n        return\n    self._is_running = True\n    self._create_timer()\n    self._connect_to_tray()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/stdout_broker/broker.html#client.ayon_core.tools.stdout_broker.broker.StdOutBroker.stop","title":"<code>stop()</code>","text":"<p>Disconnect from Tray, process last logs</p> Source code in <code>client/ayon_core/tools/stdout_broker/broker.py</code> <pre><code>def stop(self):\n    \"\"\"Disconnect from Tray, process last logs\"\"\"\n    if not self._is_running:\n        return\n    self._is_running = False\n    self._process_queue()\n    self._disconnect_from_tray()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/stdout_broker/window.html","title":"window","text":""},{"location":"autoapi/client/ayon_core/tools/stdout_broker/window.html#client.ayon_core.tools.stdout_broker.window.ConsoleDialog","title":"<code>ConsoleDialog</code>","text":"<p>               Bases: <code>QDialog</code></p> <p>Qt dialog to show stdout instead of unwieldy cmd window</p> Source code in <code>client/ayon_core/tools/stdout_broker/window.py</code> <pre><code>class ConsoleDialog(QtWidgets.QDialog):\n    \"\"\"Qt dialog to show stdout instead of unwieldy cmd window\"\"\"\n    WIDTH = 720\n    HEIGHT = 450\n    MAX_LINES = 10000\n\n    sdict = {\n        r\"&gt;&gt;&gt; \":\n            '&lt;span style=\"font-weight: bold;color:#EE5C42\"&gt; &gt;&gt;&gt; &lt;/span&gt;',\n        r\"!!!(?!\\sCRI|\\sERR)\":\n            '&lt;span style=\"font-weight: bold;color:red\"&gt; !!! &lt;/span&gt;',\n        r\"\\-\\-\\- \":\n            '&lt;span style=\"font-weight: bold;color:cyan\"&gt; --- &lt;/span&gt;',\n        r\"\\*\\*\\*(?!\\sWRN)\":\n            '&lt;span style=\"font-weight: bold;color:#FFD700\"&gt; *** &lt;/span&gt;',\n        r\"\\*\\*\\* WRN\":\n            '&lt;span style=\"font-weight: bold;color:#FFD700\"&gt; *** WRN&lt;/span&gt;',\n        r\"  \\- \":\n            '&lt;span style=\"font-weight: bold;color:#FFD700\"&gt;  - &lt;/span&gt;',\n        r\"\\[ \":\n            '&lt;span style=\"font-weight: bold;color:#66CDAA\"&gt;[&lt;/span&gt;',\n        r\"\\]\":\n            '&lt;span style=\"font-weight: bold;color:#66CDAA\"&gt;]&lt;/span&gt;',\n        r\"{\":\n            '&lt;span style=\"color:#66CDAA\"&gt;{',\n        r\"}\":\n            r\"}&lt;/span&gt;\",\n        r\"\\(\":\n            '&lt;span style=\"color:#66CDAA\"&gt;(',\n        r\"\\)\":\n            r\")&lt;/span&gt;\",\n        r\"^\\.\\.\\. \":\n            '&lt;span style=\"font-weight: bold;color:#EE5C42\"&gt; ... &lt;/span&gt;',\n        r\"!!! ERR: \":\n            '&lt;span style=\"font-weight: bold;color:#EE5C42\"&gt; !!! ERR: &lt;/span&gt;',\n        r\"!!! CRI: \":\n            '&lt;span style=\"font-weight: bold;color:red\"&gt; !!! CRI: &lt;/span&gt;',\n        r\"(?i)failed\":\n            '&lt;span style=\"font-weight: bold;color:#EE5C42\"&gt; FAILED &lt;/span&gt;',\n        r\"(?i)error\":\n            '&lt;span style=\"font-weight: bold;color:#EE5C42\"&gt; ERROR &lt;/span&gt;'\n    }\n\n    def __init__(self, text, parent=None):\n        super(ConsoleDialog, self).__init__(parent)\n        layout = QtWidgets.QHBoxLayout(parent)\n\n        plain_text = QtWidgets.QPlainTextEdit(self)\n        plain_text.setReadOnly(True)\n        plain_text.resize(self.WIDTH, self.HEIGHT)\n        plain_text.maximumBlockCount = self.MAX_LINES\n\n        while text:\n            plain_text.appendPlainText(text.popleft().strip())\n\n        layout.addWidget(plain_text)\n\n        self.setWindowTitle(\"Console output\")\n\n        self.plain_text = plain_text\n\n        self.setStyleSheet(style.load_stylesheet())\n\n        self.resize(self.WIDTH, self.HEIGHT)\n\n    def append_text(self, new_text):\n        if isinstance(new_text, str):\n            new_text = collections.deque(new_text.split(\"\\n\"))\n        while new_text:\n            text = new_text.popleft()\n            if text:\n                self.plain_text.appendHtml(self.color(text))\n\n    def _multiple_replace(self, text, adict):\n        \"\"\"Replace multiple tokens defined in dict.\n\n        Find and replace all occurrences of strings defined in dict is\n        supplied string.\n\n        Args:\n            text (str): string to be searched\n            adict (dict): dictionary with `{'search': 'replace'}`\n\n        Returns:\n            str: string with replaced tokens\n\n        \"\"\"\n        for r, v in adict.items():\n            text = re.sub(r, v, text)\n\n        return text\n\n    def color(self, message):\n        \"\"\"Color message with html tags. \"\"\"\n        message = self._multiple_replace(message, self.sdict)\n\n        return message\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/stdout_broker/window.html#client.ayon_core.tools.stdout_broker.window.ConsoleDialog.color","title":"<code>color(message)</code>","text":"<p>Color message with html tags.</p> Source code in <code>client/ayon_core/tools/stdout_broker/window.py</code> <pre><code>def color(self, message):\n    \"\"\"Color message with html tags. \"\"\"\n    message = self._multiple_replace(message, self.sdict)\n\n    return message\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/texture_copy/index.html","title":"texture_copy","text":""},{"location":"autoapi/client/ayon_core/tools/texture_copy/app.html","title":"app","text":""},{"location":"autoapi/client/ayon_core/tools/texture_copy/app.html#client.ayon_core.tools.texture_copy.app.TextureCopy","title":"<code>TextureCopy</code>","text":"Source code in <code>client/ayon_core/tools/texture_copy/app.py</code> <pre><code>class TextureCopy:\n    def _get_textures(self, path):\n        textures = []\n        for dir, subdir, files in os.walk(path):\n            textures.extend(\n                os.path.join(dir, x) for x in files\n                if os.path.splitext(x)[1].lower() in texture_extensions)\n        return textures\n\n    def _get_destination_path(self, folder_entity, project_entity):\n        project_name = project_entity[\"name\"]\n\n        product_name = \"Main\"\n        product_type = \"texture\"\n        template_data = get_template_data(project_entity, folder_entity)\n        template_data.update({\n            \"product\": {\n                \"name\": product_name,\n                \"type\": product_type,\n            },\n        })\n        anatomy = Anatomy(project_name, project_entity=project_entity)\n        template_obj = anatomy.get_template_item(\n            \"publish\", \"texture\", \"path\"\n        )\n        return template_obj.format_strict(template_data)\n\n    def _get_version(self, path):\n        versions = [0]\n        dirs = [f.path for f in os.scandir(path) if f.is_dir()]\n        for d in dirs:\n            ver = re.search(r'^v(\\d+)$',\n                            os.path.basename(d),\n                            flags=re.IGNORECASE)\n            if ver is not None:\n                versions.append(int(ver.group(1)))\n\n        return max(versions) + 1\n\n    def _copy_textures(self, textures, destination):\n        for tex in textures:\n            dst = os.path.join(destination,\n                               os.path.basename(tex))\n            t.echo(\"  - Copy {} -&gt; {}\".format(tex, dst))\n            try:\n                copyfile(tex, dst)\n            except Exception as e:\n                t.echo(\"!!! Copying failed\")\n                t.echo(\"!!! {}\".format(e))\n                exit(1)\n\n    def process(self, project_name, folder_path, path):\n        \"\"\"\n        Process all textures found in path and copy them to folder under\n        project.\n        \"\"\"\n\n        t.echo(\"&gt;&gt;&gt; Looking for textures ...\")\n        textures = self._get_textures(path)\n        if len(textures) &lt; 1:\n            t.echo(\"!!! no textures found.\")\n            exit(1)\n        else:\n            t.echo(\"&gt;&gt;&gt; Found {} textures ...\".format(len(textures)))\n\n        project_entity = ayon_api.get_project(project_name)\n        if not project_entity:\n            t.echo(\"!!! Project name [ {} ] not found.\".format(project_name))\n            exit(1)\n\n        folder_entity = ayon_api.get_folder_by_path(project_name, folder_path)\n        if not folder_entity:\n            t.echo(\n                \"!!! Folder [ {} ] not found in project\".format(folder_path)\n            )\n            exit(1)\n        t.echo(\n            (\n                \"&gt;&gt;&gt; Project [ {} ] and folder [ {} ] seems to be OK ...\"\n            ).format(project_entity['name'], folder_entity['path'])\n        )\n\n        dst_path = self._get_destination_path(folder_entity, project_entity)\n        t.echo(\"--- Using [ {} ] as destination path\".format(dst_path))\n        if not os.path.exists(dst_path):\n            try:\n                os.makedirs(dst_path)\n            except IOError as e:\n                t.echo(\"!!! Unable to create destination directory\")\n                t.echo(\"!!! {}\".format(e))\n                exit(1)\n        version = '%02d' % self._get_version(dst_path)\n        t.echo(\"--- Using version [ {} ]\".format(version))\n        dst_path = os.path.join(dst_path, \"v{}\".format(version))\n        t.echo(\"--- Final destination path [ {} ]\".format(dst_path))\n        try:\n            os.makedirs(dst_path)\n        except FileExistsError:\n            t.echo(\"!!! Somethings wrong, version directory already exists\")\n            exit(1)\n        except IOError as e:\n            t.echo(\"!!! Cannot create version directory\")\n            t.echo(\"!!! {}\".format(e))\n            exit(1)\n\n        t.echo(\"&gt;&gt;&gt; copying textures  ...\")\n        self._copy_textures(textures, dst_path)\n        t.echo(\"&gt;&gt;&gt; done.\")\n        t.echo(\"&lt;&lt;&lt; terminating ...\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/texture_copy/app.html#client.ayon_core.tools.texture_copy.app.TextureCopy.process","title":"<code>process(project_name, folder_path, path)</code>","text":"<p>Process all textures found in path and copy them to folder under project.</p> Source code in <code>client/ayon_core/tools/texture_copy/app.py</code> <pre><code>def process(self, project_name, folder_path, path):\n    \"\"\"\n    Process all textures found in path and copy them to folder under\n    project.\n    \"\"\"\n\n    t.echo(\"&gt;&gt;&gt; Looking for textures ...\")\n    textures = self._get_textures(path)\n    if len(textures) &lt; 1:\n        t.echo(\"!!! no textures found.\")\n        exit(1)\n    else:\n        t.echo(\"&gt;&gt;&gt; Found {} textures ...\".format(len(textures)))\n\n    project_entity = ayon_api.get_project(project_name)\n    if not project_entity:\n        t.echo(\"!!! Project name [ {} ] not found.\".format(project_name))\n        exit(1)\n\n    folder_entity = ayon_api.get_folder_by_path(project_name, folder_path)\n    if not folder_entity:\n        t.echo(\n            \"!!! Folder [ {} ] not found in project\".format(folder_path)\n        )\n        exit(1)\n    t.echo(\n        (\n            \"&gt;&gt;&gt; Project [ {} ] and folder [ {} ] seems to be OK ...\"\n        ).format(project_entity['name'], folder_entity['path'])\n    )\n\n    dst_path = self._get_destination_path(folder_entity, project_entity)\n    t.echo(\"--- Using [ {} ] as destination path\".format(dst_path))\n    if not os.path.exists(dst_path):\n        try:\n            os.makedirs(dst_path)\n        except IOError as e:\n            t.echo(\"!!! Unable to create destination directory\")\n            t.echo(\"!!! {}\".format(e))\n            exit(1)\n    version = '%02d' % self._get_version(dst_path)\n    t.echo(\"--- Using version [ {} ]\".format(version))\n    dst_path = os.path.join(dst_path, \"v{}\".format(version))\n    t.echo(\"--- Final destination path [ {} ]\".format(dst_path))\n    try:\n        os.makedirs(dst_path)\n    except FileExistsError:\n        t.echo(\"!!! Somethings wrong, version directory already exists\")\n        exit(1)\n    except IOError as e:\n        t.echo(\"!!! Cannot create version directory\")\n        t.echo(\"!!! {}\".format(e))\n        exit(1)\n\n    t.echo(\"&gt;&gt;&gt; copying textures  ...\")\n    self._copy_textures(textures, dst_path)\n    t.echo(\"&gt;&gt;&gt; done.\")\n    t.echo(\"&lt;&lt;&lt; terminating ...\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/tray/index.html","title":"tray","text":""},{"location":"autoapi/client/ayon_core/tools/tray/index.html#client.ayon_core.tools.tray.get_tray_server_url","title":"<code>get_tray_server_url(validate=False, server_url=None, variant=None, timeout=None)</code>","text":"<p>Get tray server url.</p> <p>Does not validate if tray is running.</p> <p>Parameters:</p> Name Type Description Default <code>server_url</code> <code>Optional[str]</code> <p>AYON server url.</p> <code>None</code> <code>variant</code> <code>Optional[str]</code> <p>Settings variant.</p> <code>None</code> <code>validate</code> <code>Optional[bool]</code> <p>Validate if tray is running. By default, does not validate.</p> <code>False</code> <code>timeout</code> <code>Optional[int]</code> <p>Timeout for tray start-up.</p> <code>None</code> <p>Returns:</p> Type Description <code>Optional[str]</code> <p>Optional[str]: Tray server url.</p> Source code in <code>client/ayon_core/tools/tray/lib.py</code> <pre><code>def get_tray_server_url(\n    validate: Optional[bool] = False,\n    server_url: Optional[str] = None,\n    variant: Optional[str] = None,\n    timeout: Optional[int] = None\n) -&gt; Optional[str]:\n    \"\"\"Get tray server url.\n\n    Does not validate if tray is running.\n\n    Args:\n        server_url (Optional[str]): AYON server url.\n        variant (Optional[str]): Settings variant.\n        validate (Optional[bool]): Validate if tray is running.\n            By default, does not validate.\n        timeout (Optional[int]): Timeout for tray start-up.\n\n    Returns:\n        Optional[str]: Tray server url.\n\n    \"\"\"\n    tray_info = TrayInfo.new(\n        server_url, variant, timeout, wait_to_start=True\n    )\n    if validate:\n        return tray_info.get_tray_url()\n    return tray_info.get_file_url()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/tray/index.html#client.ayon_core.tools.tray.get_tray_state","title":"<code>get_tray_state(server_url=None, variant=None)</code>","text":"<p>Get tray state for AYON server and variant.</p> <p>Parameters:</p> Name Type Description Default <code>server_url</code> <code>Optional[str]</code> <p>AYON server url.</p> <code>None</code> <code>variant</code> <code>Optional[str]</code> <p>Settings variant.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>int</code> <code>int</code> <p>Tray state.</p> Source code in <code>client/ayon_core/tools/tray/lib.py</code> <pre><code>def get_tray_state(\n    server_url: Optional[str] = None,\n    variant: Optional[str] = None\n) -&gt; int:\n    \"\"\"Get tray state for AYON server and variant.\n\n    Args:\n        server_url (Optional[str]): AYON server url.\n        variant (Optional[str]): Settings variant.\n\n    Returns:\n        int: Tray state.\n\n    \"\"\"\n    tray_info = get_tray_information(server_url, variant)\n    return tray_info.state\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/tray/index.html#client.ayon_core.tools.tray.is_tray_running","title":"<code>is_tray_running(server_url=None, variant=None)</code>","text":"<p>Check if tray is running.</p> <p>Parameters:</p> Name Type Description Default <code>server_url</code> <code>Optional[str]</code> <p>AYON server url.</p> <code>None</code> <code>variant</code> <code>Optional[str]</code> <p>Settings variant.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if tray is running</p> Source code in <code>client/ayon_core/tools/tray/lib.py</code> <pre><code>def is_tray_running(\n    server_url: Optional[str] = None,\n    variant: Optional[str] = None\n) -&gt; bool:\n    \"\"\"Check if tray is running.\n\n    Args:\n        server_url (Optional[str]): AYON server url.\n        variant (Optional[str]): Settings variant.\n\n    Returns:\n        bool: True if tray is running\n\n    \"\"\"\n    state = get_tray_state(server_url, variant)\n    return state != TrayState.NOT_RUNNING\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/tray/index.html#client.ayon_core.tools.tray.make_sure_tray_is_running","title":"<code>make_sure_tray_is_running(ayon_url=None, variant=None, username=None, env=None)</code>","text":"<p>Make sure that tray for AYON url and variant is running.</p> <p>Parameters:</p> Name Type Description Default <code>ayon_url</code> <code>Optional[str]</code> <p>AYON server url.</p> <code>None</code> <code>variant</code> <code>Optional[str]</code> <p>Settings variant.</p> <code>None</code> <code>username</code> <code>Optional[str]</code> <p>Username under which should be tray running.</p> <code>None</code> <code>env</code> <code>Optional[Dict[str, str]]</code> <p>Environment variables for the process.</p> <code>None</code> Source code in <code>client/ayon_core/tools/tray/lib.py</code> <pre><code>def make_sure_tray_is_running(\n    ayon_url: Optional[str] = None,\n    variant: Optional[str] = None,\n    username: Optional[str] = None,\n    env: Optional[Dict[str, str]] = None\n):\n    \"\"\"Make sure that tray for AYON url and variant is running.\n\n    Args:\n        ayon_url (Optional[str]): AYON server url.\n        variant (Optional[str]): Settings variant.\n        username (Optional[str]): Username under which should be tray running.\n        env (Optional[Dict[str, str]]): Environment variables for the process.\n\n    \"\"\"\n    tray_info = TrayInfo.new(\n        ayon_url, variant, wait_to_start=False\n    )\n    if tray_info.state == TrayState.STARTING:\n        tray_info.wait_to_start()\n\n    if tray_info.state == TrayState.RUNNING:\n        if not username:\n            username = get_ayon_username()\n        if tray_info.get_ayon_username() == username:\n            return\n\n    args = get_ayon_launcher_args(\"tray\", \"--force\")\n    if env is None:\n        env = os.environ.copy()\n\n    # Make sure 'QT_API' is not set\n    env.pop(\"QT_API\", None)\n\n    if ayon_url:\n        env[\"AYON_SERVER_URL\"] = ayon_url\n\n    # TODO maybe handle variant in a better way\n    if variant:\n        if variant == \"staging\":\n            args.append(\"--use-staging\")\n\n    run_detached_process(args, env=env)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/tray/lib.html","title":"lib","text":""},{"location":"autoapi/client/ayon_core/tools/tray/lib.html#client.ayon_core.tools.tray.lib.get_tray_file_info","title":"<code>get_tray_file_info(server_url=None, variant=None)</code>","text":"<p>Get tray information from file.</p> <p>Metadata information about running tray that should contain tray     server url.</p> <p>Parameters:</p> Name Type Description Default <code>server_url</code> <code>Optional[str]</code> <p>AYON server url.</p> <code>None</code> <code>variant</code> <code>Optional[str]</code> <p>Settings variant.</p> <code>None</code> <p>Returns:</p> Type Description <code>Optional[Dict[str, Any]]</code> <p>Optional[Dict[str, Any]]: Tray information.</p> Source code in <code>client/ayon_core/tools/tray/lib.py</code> <pre><code>def get_tray_file_info(\n    server_url: Optional[str] = None,\n    variant: Optional[str] = None\n) -&gt; Optional[Dict[str, Any]]:\n    \"\"\"Get tray information from file.\n\n    Metadata information about running tray that should contain tray\n        server url.\n\n    Args:\n        server_url (Optional[str]): AYON server url.\n        variant (Optional[str]): Settings variant.\n\n    Returns:\n        Optional[Dict[str, Any]]: Tray information.\n\n    \"\"\"\n    file_info, _ = _get_tray_file_info(server_url, variant)\n    return file_info\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/tray/lib.html#client.ayon_core.tools.tray.lib.get_tray_information","title":"<code>get_tray_information(server_url=None, variant=None, timeout=None)</code>","text":"<p>Get information about tray.</p> <p>Parameters:</p> Name Type Description Default <code>server_url</code> <code>Optional[str]</code> <p>AYON server url.</p> <code>None</code> <code>variant</code> <code>Optional[str]</code> <p>Settings variant.</p> <code>None</code> <code>timeout</code> <code>Optional[int]</code> <p>Timeout for tray start-up.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>TrayInfo</code> <code>TrayInfo</code> <p>Tray information.</p> Source code in <code>client/ayon_core/tools/tray/lib.py</code> <pre><code>def get_tray_information(\n    server_url: Optional[str] = None,\n    variant: Optional[str] = None,\n    timeout: Optional[int] = None,\n) -&gt; TrayInfo:\n    \"\"\"Get information about tray.\n\n    Args:\n        server_url (Optional[str]): AYON server url.\n        variant (Optional[str]): Settings variant.\n        timeout (Optional[int]): Timeout for tray start-up.\n\n    Returns:\n        TrayInfo: Tray information.\n\n    \"\"\"\n    return TrayInfo.new(server_url, variant, timeout)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/tray/lib.html#client.ayon_core.tools.tray.lib.get_tray_server_url","title":"<code>get_tray_server_url(validate=False, server_url=None, variant=None, timeout=None)</code>","text":"<p>Get tray server url.</p> <p>Does not validate if tray is running.</p> <p>Parameters:</p> Name Type Description Default <code>server_url</code> <code>Optional[str]</code> <p>AYON server url.</p> <code>None</code> <code>variant</code> <code>Optional[str]</code> <p>Settings variant.</p> <code>None</code> <code>validate</code> <code>Optional[bool]</code> <p>Validate if tray is running. By default, does not validate.</p> <code>False</code> <code>timeout</code> <code>Optional[int]</code> <p>Timeout for tray start-up.</p> <code>None</code> <p>Returns:</p> Type Description <code>Optional[str]</code> <p>Optional[str]: Tray server url.</p> Source code in <code>client/ayon_core/tools/tray/lib.py</code> <pre><code>def get_tray_server_url(\n    validate: Optional[bool] = False,\n    server_url: Optional[str] = None,\n    variant: Optional[str] = None,\n    timeout: Optional[int] = None\n) -&gt; Optional[str]:\n    \"\"\"Get tray server url.\n\n    Does not validate if tray is running.\n\n    Args:\n        server_url (Optional[str]): AYON server url.\n        variant (Optional[str]): Settings variant.\n        validate (Optional[bool]): Validate if tray is running.\n            By default, does not validate.\n        timeout (Optional[int]): Timeout for tray start-up.\n\n    Returns:\n        Optional[str]: Tray server url.\n\n    \"\"\"\n    tray_info = TrayInfo.new(\n        server_url, variant, timeout, wait_to_start=True\n    )\n    if validate:\n        return tray_info.get_tray_url()\n    return tray_info.get_file_url()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/tray/lib.html#client.ayon_core.tools.tray.lib.get_tray_state","title":"<code>get_tray_state(server_url=None, variant=None)</code>","text":"<p>Get tray state for AYON server and variant.</p> <p>Parameters:</p> Name Type Description Default <code>server_url</code> <code>Optional[str]</code> <p>AYON server url.</p> <code>None</code> <code>variant</code> <code>Optional[str]</code> <p>Settings variant.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>int</code> <code>int</code> <p>Tray state.</p> Source code in <code>client/ayon_core/tools/tray/lib.py</code> <pre><code>def get_tray_state(\n    server_url: Optional[str] = None,\n    variant: Optional[str] = None\n) -&gt; int:\n    \"\"\"Get tray state for AYON server and variant.\n\n    Args:\n        server_url (Optional[str]): AYON server url.\n        variant (Optional[str]): Settings variant.\n\n    Returns:\n        int: Tray state.\n\n    \"\"\"\n    tray_info = get_tray_information(server_url, variant)\n    return tray_info.state\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/tray/lib.html#client.ayon_core.tools.tray.lib.get_tray_storage_dir","title":"<code>get_tray_storage_dir()</code>","text":"<p>Get tray storage directory.</p> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Tray storage directory where metadata files are stored.</p> Source code in <code>client/ayon_core/tools/tray/lib.py</code> <pre><code>def get_tray_storage_dir() -&gt; str:\n    \"\"\"Get tray storage directory.\n\n    Returns:\n        str: Tray storage directory where metadata files are stored.\n\n    \"\"\"\n    return get_launcher_local_dir(\"tray\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/tray/lib.html#client.ayon_core.tools.tray.lib.is_tray_running","title":"<code>is_tray_running(server_url=None, variant=None)</code>","text":"<p>Check if tray is running.</p> <p>Parameters:</p> Name Type Description Default <code>server_url</code> <code>Optional[str]</code> <p>AYON server url.</p> <code>None</code> <code>variant</code> <code>Optional[str]</code> <p>Settings variant.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if tray is running</p> Source code in <code>client/ayon_core/tools/tray/lib.py</code> <pre><code>def is_tray_running(\n    server_url: Optional[str] = None,\n    variant: Optional[str] = None\n) -&gt; bool:\n    \"\"\"Check if tray is running.\n\n    Args:\n        server_url (Optional[str]): AYON server url.\n        variant (Optional[str]): Settings variant.\n\n    Returns:\n        bool: True if tray is running\n\n    \"\"\"\n    state = get_tray_state(server_url, variant)\n    return state != TrayState.NOT_RUNNING\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/tray/lib.html#client.ayon_core.tools.tray.lib.make_sure_tray_is_running","title":"<code>make_sure_tray_is_running(ayon_url=None, variant=None, username=None, env=None)</code>","text":"<p>Make sure that tray for AYON url and variant is running.</p> <p>Parameters:</p> Name Type Description Default <code>ayon_url</code> <code>Optional[str]</code> <p>AYON server url.</p> <code>None</code> <code>variant</code> <code>Optional[str]</code> <p>Settings variant.</p> <code>None</code> <code>username</code> <code>Optional[str]</code> <p>Username under which should be tray running.</p> <code>None</code> <code>env</code> <code>Optional[Dict[str, str]]</code> <p>Environment variables for the process.</p> <code>None</code> Source code in <code>client/ayon_core/tools/tray/lib.py</code> <pre><code>def make_sure_tray_is_running(\n    ayon_url: Optional[str] = None,\n    variant: Optional[str] = None,\n    username: Optional[str] = None,\n    env: Optional[Dict[str, str]] = None\n):\n    \"\"\"Make sure that tray for AYON url and variant is running.\n\n    Args:\n        ayon_url (Optional[str]): AYON server url.\n        variant (Optional[str]): Settings variant.\n        username (Optional[str]): Username under which should be tray running.\n        env (Optional[Dict[str, str]]): Environment variables for the process.\n\n    \"\"\"\n    tray_info = TrayInfo.new(\n        ayon_url, variant, wait_to_start=False\n    )\n    if tray_info.state == TrayState.STARTING:\n        tray_info.wait_to_start()\n\n    if tray_info.state == TrayState.RUNNING:\n        if not username:\n            username = get_ayon_username()\n        if tray_info.get_ayon_username() == username:\n            return\n\n    args = get_ayon_launcher_args(\"tray\", \"--force\")\n    if env is None:\n        env = os.environ.copy()\n\n    # Make sure 'QT_API' is not set\n    env.pop(\"QT_API\", None)\n\n    if ayon_url:\n        env[\"AYON_SERVER_URL\"] = ayon_url\n\n    # TODO maybe handle variant in a better way\n    if variant:\n        if variant == \"staging\":\n            args.append(\"--use-staging\")\n\n    run_detached_process(args, env=env)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/tray/lib.html#client.ayon_core.tools.tray.lib.remove_tray_server_url","title":"<code>remove_tray_server_url(force=False)</code>","text":"<p>Remove tray information file.</p> <p>Called from tray logic, do not use on your own.</p> <p>Parameters:</p> Name Type Description Default <code>force</code> <code>Optional[bool]</code> <p>Force remove tray information file.</p> <code>False</code> Source code in <code>client/ayon_core/tools/tray/lib.py</code> <pre><code>def remove_tray_server_url(force: Optional[bool] = False):\n    \"\"\"Remove tray information file.\n\n    Called from tray logic, do not use on your own.\n\n    Args:\n        force (Optional[bool]): Force remove tray information file.\n\n    \"\"\"\n    filepath = _get_tray_info_filepath()\n    if not os.path.exists(filepath):\n        return\n\n    try:\n        with open(filepath, \"r\") as stream:\n            data = json.load(stream)\n    except BaseException:\n        data = {}\n\n    if (\n        force\n        or not data\n        or data.get(\"pid\") == os.getpid()\n        or not _is_process_running(data.get(\"pid\"))\n    ):\n        os.remove(filepath)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/tray/lib.html#client.ayon_core.tools.tray.lib.set_tray_server_url","title":"<code>set_tray_server_url(tray_url, started)</code>","text":"<p>Add tray server information file.</p> <p>Called from tray logic, do not use on your own.</p> <p>Parameters:</p> Name Type Description Default <code>tray_url</code> <code>Optional[str]</code> <p>Webserver url with port.</p> required <code>started</code> <code>bool</code> <p>If tray is started. When set to 'False' it means that tray is starting up.</p> required Source code in <code>client/ayon_core/tools/tray/lib.py</code> <pre><code>def set_tray_server_url(tray_url: Optional[str], started: bool):\n    \"\"\"Add tray server information file.\n\n    Called from tray logic, do not use on your own.\n\n    Args:\n        tray_url (Optional[str]): Webserver url with port.\n        started (bool): If tray is started. When set to 'False' it means\n            that tray is starting up.\n\n    \"\"\"\n    info = TrayInfo.new(wait_to_start=False)\n    if (\n        info.pid\n        and info.pid != os.getpid()\n        and info.state in (TrayState.RUNNING, TrayState.STARTING)\n    ):\n        raise TrayIsRunningError(\"Tray is already running.\")\n\n    filepath = _get_tray_info_filepath()\n    os.makedirs(os.path.dirname(filepath), exist_ok=True)\n    data = {\n        \"url\": tray_url,\n        \"pid\": os.getpid(),\n        \"started\": started\n    }\n    with open(filepath, \"w\") as stream:\n        json.dump(data, stream)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/tray/lib.html#client.ayon_core.tools.tray.lib.show_message_in_tray","title":"<code>show_message_in_tray(title, message, icon=None, msecs=None, tray_url=None)</code>","text":"<p>Show message in tray.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <code>str</code> <p>Message title.</p> required <code>message</code> <code>str</code> <p>Message content.</p> required <code>icon</code> <code>Optional[Literal[information, warning, critical]]</code> <p>Icon for the message.</p> <code>None</code> <code>msecs</code> <code>Optional[int]</code> <p>Duration of the message.</p> <code>None</code> <code>tray_url</code> <code>Optional[str]</code> <p>Tray server url.</p> <code>None</code> Source code in <code>client/ayon_core/tools/tray/lib.py</code> <pre><code>def show_message_in_tray(\n    title, message, icon=None, msecs=None, tray_url=None\n):\n    \"\"\"Show message in tray.\n\n    Args:\n        title (str): Message title.\n        message (str): Message content.\n        icon (Optional[Literal[\"information\", \"warning\", \"critical\"]]): Icon\n            for the message.\n        msecs (Optional[int]): Duration of the message.\n        tray_url (Optional[str]): Tray server url.\n\n    \"\"\"\n    if not tray_url:\n        tray_url = get_tray_server_url()\n\n    # TODO handle this case, e.g. raise an error?\n    if not tray_url:\n        return\n\n    # TODO handle response, can fail whole request or can fail on status\n    requests.post(\n        f\"{tray_url}/tray/message\",\n        json={\n            \"title\": title,\n            \"message\": message,\n            \"icon\": icon,\n            \"msecs\": msecs\n        }\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/tray/structures.html","title":"structures","text":""},{"location":"autoapi/client/ayon_core/tools/tray/ui/index.html","title":"ui","text":""},{"location":"autoapi/client/ayon_core/tools/tray/ui/__main__.html","title":"__main__","text":""},{"location":"autoapi/client/ayon_core/tools/tray/ui/addons_manager.html","title":"addons_manager","text":""},{"location":"autoapi/client/ayon_core/tools/tray/ui/addons_manager.html#client.ayon_core.tools.tray.ui.addons_manager.TrayAddonsManager","title":"<code>TrayAddonsManager</code>","text":"<p>               Bases: <code>AddonsManager</code></p> Source code in <code>client/ayon_core/tools/tray/ui/addons_manager.py</code> <pre><code>class TrayAddonsManager(AddonsManager):\n    # TODO do not use env variable\n    webserver_url_env = \"AYON_WEBSERVER_URL\"\n    # Define order of addons in menu\n    # TODO find better way how to define order\n    addons_menu_order = (\n        \"ftrack\",\n        \"kitsu\",\n        \"launcher_tool\",\n        \"clockify\",\n    )\n\n    def __init__(self, tray_manager):\n        super().__init__(initialize=False)\n\n        self._tray_manager = tray_manager\n\n        self._webserver_manager = WebServerManager(find_free_port(), None)\n\n        self.doubleclick_callbacks = {}\n        self.doubleclick_callback = None\n\n    def print_report(self, include_tray: bool = True) -&gt; None:\n        \"\"\"Change default behavior of print_report method.\"\"\"\n        super().print_report(include_tray=include_tray)\n\n    @property\n    def webserver_url(self):\n        return self._webserver_manager.url\n\n    def get_doubleclick_callback(self):\n        callback_name = self.doubleclick_callback\n        return self.doubleclick_callbacks.get(callback_name)\n\n    def add_doubleclick_callback(self, addon, callback):\n        \"\"\"Register double-click callbacks on tray icon.\n\n        Currently, there is no way how to determine which is launched. Name of\n        callback can be defined with `doubleclick_callback` attribute.\n\n        Missing feature how to define default callback.\n\n        Args:\n            addon (AYONAddon): Addon object.\n            callback (FunctionType): Function callback.\n        \"\"\"\n\n        callback_name = \"_\".join([addon.name, callback.__name__])\n        if callback_name not in self.doubleclick_callbacks:\n            self.doubleclick_callbacks[callback_name] = callback\n            if self.doubleclick_callback is None:\n                self.doubleclick_callback = callback_name\n            return\n\n        self.log.warning(\n            f\"Callback with name \\\"{callback_name}\\\" is already registered.\"\n        )\n\n    def initialize(self, tray_menu):\n        self.initialize_addons()\n        self.tray_init()\n        self.connect_addons()\n        self.tray_menu(tray_menu)\n\n    def add_route(self, request_method: str, path: str, handler: Callable):\n        self._webserver_manager.add_route(request_method, path, handler)\n\n    def add_static(self, prefix: str, path: str):\n        self._webserver_manager.add_static(prefix, path)\n\n    def add_addon_route(\n        self,\n        addon_name: str,\n        path: str,\n        request_method: str,\n        handler: Callable\n    ) -&gt; str:\n        return self._webserver_manager.add_addon_route(\n            addon_name,\n            path,\n            request_method,\n            handler\n        )\n\n    def add_addon_static(\n        self, addon_name: str, prefix: str, path: str\n    ) -&gt; str:\n        return self._webserver_manager.add_addon_static(\n            addon_name,\n            prefix,\n            path\n        )\n\n    def get_enabled_tray_addons(self):\n        \"\"\"Enabled tray addons.\n\n        Returns:\n            list[AYONAddon]: Enabled addons that inherit from tray interface.\n        \"\"\"\n\n        return [\n            addon\n            for addon in self.get_enabled_addons()\n            if isinstance(addon, ITrayAddon)\n        ]\n\n    def restart_tray(self):\n        if self._tray_manager:\n            self._tray_manager.restart()\n\n    def tray_init(self):\n        self._init_tray_webserver()\n        time_start = time.time()\n        prev_start_time = time_start\n        for addon in self.get_enabled_tray_addons():\n            report_row = self._report_by_name[addon.name]\n            try:\n                addon._tray_manager = self._tray_manager\n                addon.tray_init()\n                addon.tray_initialized = True\n            except Exception:\n                self.log.warning(\n                    f\"Addon \\\"{addon.name}\\\" crashed on `tray_init`.\",\n                    exc_info=True\n                )\n            now = time.time()\n            report_row.tray_init_time = now - prev_start_time\n            prev_start_time = now\n\n        self._total_row.tray_init_time = time.time() - time_start\n\n    def connect_addons(self):\n        self._webserver_manager.connect_with_addons(\n            self.get_enabled_addons()\n        )\n        super().connect_addons()\n\n    def tray_menu(self, tray_menu):\n        ordered_addons = []\n        enabled_by_name = {\n            addon.name: addon\n            for addon in self.get_enabled_tray_addons()\n        }\n\n        for name in self.addons_menu_order:\n            addon_by_name = enabled_by_name.pop(name, None)\n            if addon_by_name:\n                ordered_addons.append(addon_by_name)\n        ordered_addons.extend(enabled_by_name.values())\n\n        time_start = time.time()\n        prev_start_time = time_start\n        for addon in ordered_addons:\n            if not addon.tray_initialized:\n                continue\n\n            report_row = self._report_by_name[addon.name]\n\n            try:\n                addon.tray_menu(tray_menu)\n            except Exception:\n                # Unset initialized mark\n                addon.tray_initialized = False\n                self.log.warning(\n                    f\"Addon \\\"{addon.name}\\\" crashed on `tray_menu`.\",\n                    exc_info=True\n                )\n            now = time.time()\n            report_row.tray_menu_time = now - prev_start_time\n            prev_start_time = now\n\n        self._total_row.tray_menu_time = time.time() - time_start\n\n    def start_addons(self):\n        self._webserver_manager.start_server()\n\n        time_start = time.time()\n        prev_start_time = time_start\n        for addon in self.get_enabled_tray_addons():\n            if not addon.tray_initialized:\n                if isinstance(addon, ITrayService):\n                    addon.set_service_failed_icon()\n                continue\n\n            report_row = self._report_by_name[addon.name]\n            try:\n                addon.tray_start()\n            except Exception:\n                self.log.warning(\n                    f\"Addon \\\"{addon.name}\\\" crashed on `tray_start`.\",\n                    exc_info=True\n                )\n            now = time.time()\n            report_row.tray_start_time = now - prev_start_time\n            prev_start_time = now\n\n        self._total_row.tray_start_time = time.time() - time_start\n\n    def on_exit(self):\n        self._webserver_manager.stop_server()\n        for addon in self.get_enabled_tray_addons():\n            if addon.tray_initialized:\n                try:\n                    addon.tray_exit()\n                except Exception:\n                    self.log.warning(\n                        f\"Addon \\\"{addon.name}\\\" crashed on `tray_exit`.\",\n                        exc_info=True\n                    )\n\n    def get_tray_webserver(self):\n        # TODO rename/remove method\n        return self._webserver_manager\n\n    def _init_tray_webserver(self):\n        webserver_url = self.webserver_url\n        statics_url = f\"{webserver_url}/res\"\n\n        # Deprecated\n        # TODO stop using these env variables\n        # - function 'get_tray_server_url' should be used instead\n        os.environ[self.webserver_url_env] = webserver_url\n        os.environ[\"AYON_STATICS_SERVER\"] = statics_url\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/tray/ui/addons_manager.html#client.ayon_core.tools.tray.ui.addons_manager.TrayAddonsManager.add_doubleclick_callback","title":"<code>add_doubleclick_callback(addon, callback)</code>","text":"<p>Register double-click callbacks on tray icon.</p> <p>Currently, there is no way how to determine which is launched. Name of callback can be defined with <code>doubleclick_callback</code> attribute.</p> <p>Missing feature how to define default callback.</p> <p>Parameters:</p> Name Type Description Default <code>addon</code> <code>AYONAddon</code> <p>Addon object.</p> required <code>callback</code> <code>FunctionType</code> <p>Function callback.</p> required Source code in <code>client/ayon_core/tools/tray/ui/addons_manager.py</code> <pre><code>def add_doubleclick_callback(self, addon, callback):\n    \"\"\"Register double-click callbacks on tray icon.\n\n    Currently, there is no way how to determine which is launched. Name of\n    callback can be defined with `doubleclick_callback` attribute.\n\n    Missing feature how to define default callback.\n\n    Args:\n        addon (AYONAddon): Addon object.\n        callback (FunctionType): Function callback.\n    \"\"\"\n\n    callback_name = \"_\".join([addon.name, callback.__name__])\n    if callback_name not in self.doubleclick_callbacks:\n        self.doubleclick_callbacks[callback_name] = callback\n        if self.doubleclick_callback is None:\n            self.doubleclick_callback = callback_name\n        return\n\n    self.log.warning(\n        f\"Callback with name \\\"{callback_name}\\\" is already registered.\"\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/tray/ui/addons_manager.html#client.ayon_core.tools.tray.ui.addons_manager.TrayAddonsManager.get_enabled_tray_addons","title":"<code>get_enabled_tray_addons()</code>","text":"<p>Enabled tray addons.</p> <p>Returns:</p> Type Description <p>list[AYONAddon]: Enabled addons that inherit from tray interface.</p> Source code in <code>client/ayon_core/tools/tray/ui/addons_manager.py</code> <pre><code>def get_enabled_tray_addons(self):\n    \"\"\"Enabled tray addons.\n\n    Returns:\n        list[AYONAddon]: Enabled addons that inherit from tray interface.\n    \"\"\"\n\n    return [\n        addon\n        for addon in self.get_enabled_addons()\n        if isinstance(addon, ITrayAddon)\n    ]\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/tray/ui/addons_manager.html#client.ayon_core.tools.tray.ui.addons_manager.TrayAddonsManager.print_report","title":"<code>print_report(include_tray=True)</code>","text":"<p>Change default behavior of print_report method.</p> Source code in <code>client/ayon_core/tools/tray/ui/addons_manager.py</code> <pre><code>def print_report(self, include_tray: bool = True) -&gt; None:\n    \"\"\"Change default behavior of print_report method.\"\"\"\n    super().print_report(include_tray=include_tray)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/tray/ui/dialogs.html","title":"dialogs","text":""},{"location":"autoapi/client/ayon_core/tools/tray/ui/dialogs.html#client.ayon_core.tools.tray.ui.dialogs.PixmapLabel","title":"<code>PixmapLabel</code>","text":"<p>               Bases: <code>QLabel</code></p> <p>Label resizing image to height of font.</p> Source code in <code>client/ayon_core/tools/tray/ui/dialogs.py</code> <pre><code>class PixmapLabel(QtWidgets.QLabel):\n    \"\"\"Label resizing image to height of font.\"\"\"\n    def __init__(self, pixmap, parent):\n        super(PixmapLabel, self).__init__(parent)\n        self._empty_pixmap = QtGui.QPixmap(0, 0)\n        self._source_pixmap = pixmap\n\n    def set_source_pixmap(self, pixmap):\n        \"\"\"Change source image.\"\"\"\n        self._source_pixmap = pixmap\n        self._set_resized_pix()\n\n    def _get_pix_size(self):\n        size = self.fontMetrics().height() * 3\n        return size, size\n\n    def _set_resized_pix(self):\n        if self._source_pixmap is None:\n            self.setPixmap(self._empty_pixmap)\n            return\n        width, height = self._get_pix_size()\n        self.setPixmap(\n            self._source_pixmap.scaled(\n                width,\n                height,\n                QtCore.Qt.KeepAspectRatio,\n                QtCore.Qt.SmoothTransformation\n            )\n        )\n\n    def resizeEvent(self, event):\n        self._set_resized_pix()\n        super(PixmapLabel, self).resizeEvent(event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/tray/ui/dialogs.html#client.ayon_core.tools.tray.ui.dialogs.PixmapLabel.set_source_pixmap","title":"<code>set_source_pixmap(pixmap)</code>","text":"<p>Change source image.</p> Source code in <code>client/ayon_core/tools/tray/ui/dialogs.py</code> <pre><code>def set_source_pixmap(self, pixmap):\n    \"\"\"Change source image.\"\"\"\n    self._source_pixmap = pixmap\n    self._set_resized_pix()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/tray/ui/host_console_listener.html","title":"host_console_listener","text":""},{"location":"autoapi/client/ayon_core/tools/tray/ui/host_console_listener.html#client.ayon_core.tools.tray.ui.host_console_listener.HostListener","title":"<code>HostListener</code>","text":"Source code in <code>client/ayon_core/tools/tray/ui/host_console_listener.py</code> <pre><code>class HostListener:\n    def __init__(self, addons_manager, tray_manager):\n        self._tray_manager = tray_manager\n        self._window_per_id = {}  # dialogs per host name\n        self._action_per_id = {}  # QAction per host name\n\n        addons_manager.add_route(\n            \"*\", \"/ws/host_listener\", self.websocket_handler\n        )\n\n    def _host_is_connecting(self, host_name, label):\n        \"\"\" Initialize dialog, adds to submenu.\"\"\"\n        ITrayService.services_submenu(self._tray_manager)\n        services_submenu = self._tray_manager.get_services_submenu()\n        action = QtWidgets.QAction(label, services_submenu)\n        action.triggered.connect(lambda: self.show_widget(host_name))\n\n        services_submenu.addAction(action)\n        self._action_per_id[host_name] = action\n        self._set_host_icon(host_name, IconType.IDLE)\n        widget = ConsoleDialog(\"\")\n        self._window_per_id[host_name] = widget\n\n    def _set_host_icon(self, host_name, icon_type):\n        \"\"\"Assigns icon to action for 'host_name' with 'icon_type'.\n\n            Action must exist in self._action_per_id\n\n            Args:\n                host_name (str)\n                icon_type (IconType)\n        \"\"\"\n        action = self._action_per_id.get(host_name)\n        if not action:\n            raise ValueError(\"Unknown host {}\".format(host_name))\n\n        icon = None\n        if icon_type == IconType.IDLE:\n            icon = ITrayService.get_icon_idle()\n        elif icon_type == IconType.RUNNING:\n            icon = ITrayService.get_icon_running()\n        elif icon_type == IconType.FAILED:\n            icon = ITrayService.get_icon_failed()\n        else:\n            log.info(\"Unknown icon type {} for {}\".format(icon_type,\n                                                          host_name))\n        action.setIcon(icon)\n\n    def show_widget(self, host_name):\n        \"\"\"Shows prepared widget for 'host_name'.\n\n            Dialog get initialized when 'host_name' is connecting.\n        \"\"\"\n        self._tray_manager.execute_in_main_thread(\n            self._show_widget, host_name\n        )\n\n    def _show_widget(self, host_name):\n        widget = self._window_per_id[host_name]\n        widget.show()\n        widget.raise_()\n        widget.activateWindow()\n\n    async def websocket_handler(self, request):\n        ws = web.WebSocketResponse()\n        await ws.prepare(request)\n\n        widget = None\n        try:\n            async for msg in ws:\n                if msg.type == aiohttp.WSMsgType.TEXT:\n                    host_name, action, text = self._parse_message(msg)\n\n                    if action == HostMsgAction.CONNECTING:\n                        self._action_per_id[host_name] = None\n                        # must be sent to main thread, or action wont trigger\n                        self._tray_manager.execute_in_main_thread(\n                            self._host_is_connecting, host_name, text\n                        )\n                    elif action == HostMsgAction.CLOSE:\n                        # clean close\n                        self._close(host_name)\n                        await ws.close()\n                    elif action == HostMsgAction.INITIALIZED:\n                        self._tray_manager.execute_in_main_thread(\n                            # must be queued as _host_is_connecting might not\n                            # be triggered/finished yet\n                            self._set_host_icon, host_name, IconType.RUNNING\n                        )\n                    elif action == HostMsgAction.ADD:\n                        self._tray_manager.execute_in_main_thread(\n                            self._add_text, host_name, text\n                        )\n                elif msg.type == aiohttp.WSMsgType.ERROR:\n                    print('ws connection closed with exception %s' %\n                          ws.exception())\n                    host_name, _, _ = self._parse_message(msg)\n                    self._set_host_icon(host_name, IconType.FAILED)\n        except CancelledError:  # recoverable\n            pass\n        except Exception as exc:\n            log.warning(\"Exception during communication\", exc_info=True)\n            if widget:\n                error_msg = str(exc)\n                widget.append_text(error_msg)\n\n        return ws\n\n    def _add_text(self, host_name, text):\n        widget = self._window_per_id[host_name]\n        widget.append_text(text)\n\n    def _close(self, host_name):\n        \"\"\" Clean close - remove from menu, delete widget.\"\"\"\n        services_submenu = self._tray_manager.get_services_submenu()\n        action = self._action_per_id.pop(host_name)\n        services_submenu.removeAction(action)\n        widget = self._window_per_id.pop(host_name)\n        if widget.isVisible():\n            widget.hide()\n        widget.deleteLater()\n\n    def _parse_message(self, msg):\n        data = json.loads(msg.data)\n        action = data.get(\"action\")\n        host_name = data[\"host\"]\n        value = data.get(\"text\")\n\n        return host_name, action, value\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/tray/ui/host_console_listener.html#client.ayon_core.tools.tray.ui.host_console_listener.HostListener.show_widget","title":"<code>show_widget(host_name)</code>","text":"<p>Shows prepared widget for 'host_name'.</p> <p>Dialog get initialized when 'host_name' is connecting.</p> Source code in <code>client/ayon_core/tools/tray/ui/host_console_listener.py</code> <pre><code>def show_widget(self, host_name):\n    \"\"\"Shows prepared widget for 'host_name'.\n\n        Dialog get initialized when 'host_name' is connecting.\n    \"\"\"\n    self._tray_manager.execute_in_main_thread(\n        self._show_widget, host_name\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/tray/ui/info_widget.html","title":"info_widget","text":""},{"location":"autoapi/client/ayon_core/tools/tray/ui/info_widget.html#client.ayon_core.tools.tray.ui.info_widget.InfoSubWidget","title":"<code>InfoSubWidget</code>","text":"<p>               Bases: <code>QWidget</code></p> Source code in <code>client/ayon_core/tools/tray/ui/info_widget.py</code> <pre><code>class InfoSubWidget(QtWidgets.QWidget):\n    not_applicable = \"N/A\"\n\n    def __init__(self, parent=None):\n        super(InfoSubWidget, self).__init__(parent)\n\n        self.env_view = None\n\n        main_layout = QtWidgets.QVBoxLayout(self)\n        main_layout.setContentsMargins(0, 0, 0, 0)\n        main_layout.setAlignment(QtCore.Qt.AlignTop)\n        main_layout.addWidget(self._create_info_widget(), 0)\n        main_layout.addWidget(self._create_separator(), 0)\n        main_layout.addWidget(self._create_workstation_widget(), 0)\n        main_layout.addWidget(self._create_separator(), 0)\n        main_layout.addWidget(self._create_environ_widget(), 1)\n\n    def set_content_height(self, height):\n        if self.env_view:\n            self.env_view.setMinimumHeight(height)\n\n    def set_scroll_enabled(self, value):\n        if self.env_view:\n            self.env_view.set_scroll_enabled(value)\n\n    def _create_separator(self):\n        separator_widget = QtWidgets.QWidget(self)\n        separator_widget.setObjectName(\"Separator\")\n        separator_widget.setMinimumHeight(2)\n        separator_widget.setMaximumHeight(2)\n        return separator_widget\n\n    def _create_workstation_widget(self):\n        key_label_mapping = {\n            \"system_name\": \"System:\",\n            \"local_id\": \"Local ID:\",\n            \"username\": \"Username:\",\n            \"hostname\": \"Hostname:\",\n            \"hostip\": \"Host IP:\"\n        }\n        keys_order = [\n            \"system_name\",\n            \"local_id\",\n            \"username\",\n            \"hostname\",\n            \"hostip\"\n        ]\n        workstation_info = get_workstation_info()\n        for key in workstation_info.keys():\n            if key not in keys_order:\n                keys_order.append(key)\n\n        wokstation_info_widget = CollapsibleWidget(\"Workstation info\", self)\n\n        info_widget = QtWidgets.QWidget(self)\n        info_layout = QtWidgets.QGridLayout(info_widget)\n        # Add spacer to 3rd column\n        info_layout.addWidget(QtWidgets.QWidget(info_widget), 0, 2)\n        info_layout.setColumnStretch(2, 1)\n\n        for key in keys_order:\n            if key not in workstation_info:\n                continue\n\n            label = key_label_mapping.get(key, key)\n            value = workstation_info[key]\n            row = info_layout.rowCount()\n            info_layout.addWidget(\n                QtWidgets.QLabel(label), row, 0, 1, 1\n            )\n            value_label = QtWidgets.QLabel(value)\n            value_label.setTextInteractionFlags(\n                QtCore.Qt.TextSelectableByMouse\n            )\n            info_layout.addWidget(\n                value_label, row, 1, 1, 1\n            )\n\n        wokstation_info_widget.set_content_widget(info_widget)\n        wokstation_info_widget.toggle_content()\n\n        return wokstation_info_widget\n\n    def _create_environ_widget(self):\n        env_widget = CollapsibleWidget(\"Environments\", self)\n\n        env_view = EnvironmentsView(env_widget)\n        env_view.setMinimumHeight(300)\n        env_widget.set_content_widget(env_view)\n\n        self.env_view = env_view\n\n        return env_widget\n\n    def _create_info_widget(self):\n        \"\"\"Create widget with information about application.\"\"\"\n\n        executable_args = get_ayon_launcher_args()\n        username = \"N/A\"\n        user_info = ayon_api.get_user()\n        if user_info:\n            username = user_info.get(\"name\") or username\n            full_name = user_info.get(\"attrib\", {}).get(\"fullName\")\n            if full_name:\n                username = \"{} ({})\".format(full_name, username)\n        info_values = {\n            \"executable\": executable_args[-1],\n            \"server_url\": os.environ[\"AYON_SERVER_URL\"],\n            \"bundle_name\": os.environ[\"AYON_BUNDLE_NAME\"],\n            \"username\": username\n        }\n        key_label_mapping = {\n            \"executable\": \"AYON Executable:\",\n            \"server_url\": \"AYON Server:\",\n            \"bundle_name\": \"AYON Bundle:\",\n            \"username\": \"AYON Username:\"\n        }\n        # Prepare keys order\n        keys_order = [\n            \"server_url\",\n            \"bundle_name\",\n            \"username\",\n            \"executable\",\n        ]\n\n        for key in info_values.keys():\n            if key not in keys_order:\n                keys_order.append(key)\n\n        # Create widgets\n        info_widget = QtWidgets.QWidget(self)\n        info_layout = QtWidgets.QGridLayout(info_widget)\n        # Add spacer to 3rd column\n        info_layout.addWidget(QtWidgets.QWidget(info_widget), 0, 2)\n        info_layout.setColumnStretch(2, 1)\n\n        title_label = QtWidgets.QLabel(info_widget)\n        title_label.setText(\"Application information\")\n        title_label.setStyleSheet(\"font-weight: bold;\")\n        info_layout.addWidget(title_label, 0, 0, 1, 2)\n\n        for key in keys_order:\n            if key not in info_values:\n                continue\n            value = info_values[key]\n            label = key_label_mapping.get(key, key)\n            row = info_layout.rowCount()\n            info_layout.addWidget(\n                QtWidgets.QLabel(label), row, 0, 1, 1\n            )\n            value_label = QtWidgets.QLabel(value)\n            value_label.setTextInteractionFlags(\n                QtCore.Qt.TextSelectableByMouse\n            )\n            info_layout.addWidget(\n                value_label, row, 1, 1, 1\n            )\n\n        row = info_layout.rowCount()\n        info_layout.addWidget(\n            QtWidgets.QLabel(\"Core Addon:\"), row, 0, 1, 1\n        )\n        value_label = QtWidgets.QLabel(ayon_core.version.__version__)\n        value_label.setTextInteractionFlags(\n            QtCore.Qt.TextSelectableByMouse\n        )\n        info_layout.addWidget(\n            value_label, row, 1, 1, 1\n        )\n        return info_widget\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/tray/ui/tray.html","title":"tray","text":""},{"location":"autoapi/client/ayon_core/tools/tray/ui/tray.html#client.ayon_core.tools.tray.ui.tray.SystemTrayIcon","title":"<code>SystemTrayIcon</code>","text":"<p>               Bases: <code>QSystemTrayIcon</code></p> <p>Tray widget.</p> <p>:param parent: Main widget that cares about all GUIs :type parent: QtWidgets.QMainWindow</p> Source code in <code>client/ayon_core/tools/tray/ui/tray.py</code> <pre><code>class SystemTrayIcon(QtWidgets.QSystemTrayIcon):\n    \"\"\"Tray widget.\n\n    :param parent: Main widget that cares about all GUIs\n    :type parent: QtWidgets.QMainWindow\n    \"\"\"\n\n    doubleclick_time_ms = 100\n\n    def __init__(self, parent):\n        icon = QtGui.QIcon(resources.get_ayon_icon_filepath())\n\n        super().__init__(icon, parent)\n\n        self._exited = False\n\n        self._doubleclick = False\n        self._click_pos = None\n        self._initializing_addons = False\n\n        # Store parent - QtWidgets.QMainWindow()\n        self._parent = parent\n\n        # Setup menu in Tray\n        self.menu = QtWidgets.QMenu()\n        self.menu.setStyleSheet(style.load_stylesheet())\n\n        # Set addons\n        self._tray_manager = TrayManager(self, parent)\n\n        # Add menu to Context of SystemTrayIcon\n        self.setContextMenu(self.menu)\n\n        atexit.register(self.exit)\n\n        # Catch activate event for left click if not on MacOS\n        #   - MacOS has this ability by design and is harder to modify this\n        #       behavior\n        if platform.system().lower() == \"darwin\":\n            return\n\n        self.activated.connect(self.on_systray_activated)\n\n        click_timer = QtCore.QTimer()\n        click_timer.setInterval(self.doubleclick_time_ms)\n        click_timer.timeout.connect(self._click_timer_timeout)\n\n        self._click_timer = click_timer\n\n    def is_closing(self) -&gt; bool:\n        return self._tray_manager.is_closing()\n\n    @property\n    def initializing_addons(self):\n        return self._initializing_addons\n\n    def initialize_addons(self):\n        self._initializing_addons = True\n        try:\n            self._tray_manager.initialize_addons()\n        finally:\n            self._initializing_addons = False\n\n    def _click_timer_timeout(self):\n        self._click_timer.stop()\n        doubleclick = self._doubleclick\n        # Reset bool value\n        self._doubleclick = False\n        if doubleclick:\n            self._tray_manager.execute_doubleclick()\n        else:\n            self._show_context_menu()\n\n    def _show_context_menu(self):\n        pos = self._click_pos\n        self._click_pos = None\n        if pos is None:\n            pos = QtGui.QCursor().pos()\n        self.contextMenu().popup(pos)\n\n    def on_systray_activated(self, reason):\n        # show contextMenu if left click\n        if reason == QtWidgets.QSystemTrayIcon.Trigger:\n            if self._tray_manager.doubleclick_callback:\n                self._click_pos = QtGui.QCursor().pos()\n                self._click_timer.start()\n            else:\n                self._show_context_menu()\n\n        elif reason == QtWidgets.QSystemTrayIcon.DoubleClick:\n            self._doubleclick = True\n\n    def exit(self):\n        \"\"\" Exit whole application.\n\n        - Icon won't stay in tray after exit.\n        \"\"\"\n        if self._exited:\n            return\n        self._exited = True\n\n        self.hide()\n        self._tray_manager.on_exit()\n        QtCore.QCoreApplication.exit()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/tray/ui/tray.html#client.ayon_core.tools.tray.ui.tray.SystemTrayIcon.exit","title":"<code>exit()</code>","text":"<p>Exit whole application.</p> <ul> <li>Icon won't stay in tray after exit.</li> </ul> Source code in <code>client/ayon_core/tools/tray/ui/tray.py</code> <pre><code>def exit(self):\n    \"\"\" Exit whole application.\n\n    - Icon won't stay in tray after exit.\n    \"\"\"\n    if self._exited:\n        return\n    self._exited = True\n\n    self.hide()\n    self._tray_manager.on_exit()\n    QtCore.QCoreApplication.exit()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/tray/ui/tray.html#client.ayon_core.tools.tray.ui.tray.TrayManager","title":"<code>TrayManager</code>","text":"<p>Cares about context of application.</p> <p>Load submenus, actions, separators and addons into tray's context.</p> Source code in <code>client/ayon_core/tools/tray/ui/tray.py</code> <pre><code>class TrayManager:\n    \"\"\"Cares about context of application.\n\n    Load submenus, actions, separators and addons into tray's context.\n    \"\"\"\n    def __init__(self, tray_widget, main_window):\n        self.tray_widget = tray_widget\n        self.main_window = main_window\n        self._info_widget = None\n        self._restart_action = None\n\n        self.log = Logger.get_logger(self.__class__.__name__)\n\n        studio_settings = get_studio_settings()\n\n        update_check_interval = studio_settings[\"core\"].get(\n            \"update_check_interval\"\n        )\n        if update_check_interval is None:\n            update_check_interval = 5\n\n        update_check_interval = update_check_interval * 60 * 1000\n\n        # create timer loop to check callback functions\n        main_thread_timer = QtCore.QTimer()\n        main_thread_timer.setInterval(300)\n\n        update_check_timer = QtCore.QTimer()\n        if update_check_interval &gt; 0:\n            update_check_timer.setInterval(update_check_interval)\n\n        main_thread_timer.timeout.connect(self._main_thread_execution)\n        update_check_timer.timeout.connect(self._on_update_check_timer)\n\n        self._addons_manager = TrayAddonsManager(self)\n        self._host_listener = HostListener(self._addons_manager, self)\n\n        self.errors = []\n\n        self._outdated_dialog = None\n\n        self._launcher_window = None\n        self._browser_window = None\n        self._console_window = ConsoleInterpreterWindow()\n        self._publish_report_viewer_window = PublishReportViewerWindow()\n\n        self._update_check_timer = update_check_timer\n        self._update_check_interval = update_check_interval\n        self._main_thread_timer = main_thread_timer\n        self._main_thread_callbacks = collections.deque()\n        self._execution_in_progress = None\n        self._services_submenu = None\n        self._start_time = time.time()\n\n        # Cache AYON username used in process\n        # - it can change only by changing ayon_api global connection\n        #   should be safe for tray application to cache the value only once\n        self._cached_username = None\n        self._closing = False\n        try:\n            set_tray_server_url(\n                self._addons_manager.webserver_url, False\n            )\n        except TrayIsRunningError:\n            self.log.error(\"Tray is already running.\")\n            self._closing = True\n\n    def is_closing(self):\n        return self._closing\n\n    @property\n    def doubleclick_callback(self):\n        \"\"\"Double-click callback for Tray icon.\"\"\"\n        callback = self._addons_manager.get_doubleclick_callback()\n        if callback is None:\n            callback = self._show_launcher_window\n        return callback\n\n    def execute_doubleclick(self):\n        \"\"\"Execute double click callback in main thread.\"\"\"\n        callback = self.doubleclick_callback\n        if callback is not None:\n            self.execute_in_main_thread(callback)\n\n    def show_tray_message(self, title, message, icon=None, msecs=None):\n        \"\"\"Show tray message.\n\n        Args:\n            title (str): Title of message.\n            message (str): Content of message.\n            icon (QSystemTrayIcon.MessageIcon): Message's icon. Default is\n                Information icon, may differ by Qt version.\n            msecs (int): Duration of message visibility in milliseconds.\n                Default is 10000 msecs, may differ by Qt version.\n        \"\"\"\n        args = [title, message]\n        kwargs = {}\n        if icon:\n            kwargs[\"icon\"] = icon\n        if msecs:\n            kwargs[\"msecs\"] = msecs\n\n        self.tray_widget.showMessage(*args, **kwargs)\n        # TODO validate 'self.tray_widget.supportsMessages()'\n\n    def initialize_addons(self):\n        \"\"\"Add addons to tray.\"\"\"\n        if self._closing:\n            return\n\n        tray_menu = self.tray_widget.menu\n        # Add launcher at first place\n        launcher_action = QtWidgets.QAction(\n            \"Launcher\", tray_menu\n        )\n        launcher_action.triggered.connect(self._show_launcher_window)\n        tray_menu.addAction(launcher_action)\n\n        console_action = ITrayAddon.add_action_to_admin_submenu(\n            \"Console\", tray_menu\n        )\n        console_action.triggered.connect(self._show_console_window)\n\n        publish_report_viewer_action = ITrayAddon.add_action_to_admin_submenu(\n            \"Publish report viewer\", tray_menu\n        )\n        publish_report_viewer_action.triggered.connect(\n            self._show_publish_report_viewer\n        )\n\n        self._addons_manager.initialize(tray_menu)\n\n        # Add browser action after addon actions\n        browser_action = QtWidgets.QAction(\n            \"Browser\", tray_menu\n        )\n        browser_action.triggered.connect(self._show_browser_window)\n        tray_menu.addAction(browser_action)\n\n        self._addons_manager.add_route(\n            \"GET\", \"/tray\", self._web_get_tray_info\n        )\n        self._addons_manager.add_route(\n            \"POST\", \"/tray/message\", self._web_show_tray_message\n        )\n\n        admin_submenu = ITrayAddon.admin_submenu(tray_menu)\n        tray_menu.addMenu(admin_submenu)\n\n        # Add services if they are\n        services_submenu = ITrayService.services_submenu(tray_menu)\n        self._services_submenu = services_submenu\n        tray_menu.addMenu(services_submenu)\n\n        # Add separator\n        tray_menu.addSeparator()\n\n        self._add_version_item()\n\n        # Add Exit action to menu\n        exit_action = QtWidgets.QAction(\"Exit\", self.tray_widget)\n        exit_action.triggered.connect(self.tray_widget.exit)\n        tray_menu.addAction(exit_action)\n\n        # Tell each addon which addons were imported\n        # TODO Capture only webserver issues (the only thing that can crash).\n        try:\n            self._addons_manager.start_addons()\n        except Exception:\n            self.log.error(\n                \"Failed to start addons.\",\n                exc_info=True\n            )\n            return self.exit()\n\n        # Print time report\n        self._addons_manager.print_report()\n\n        self._main_thread_timer.start()\n\n        if self._update_check_interval &gt; 0:\n            self._update_check_timer.start()\n\n        self.execute_in_main_thread(self._startup_validations)\n        try:\n            set_tray_server_url(\n                self._addons_manager.webserver_url, True\n            )\n        except TrayIsRunningError:\n            self.log.warning(\"Other tray started meanwhile. Exiting.\")\n            self.exit()\n\n        project_bundle = os.getenv(\"AYON_BUNDLE_NAME\")\n        studio_bundle = os.getenv(\"AYON_STUDIO_BUNDLE_NAME\")\n        if studio_bundle and project_bundle != studio_bundle:\n            self.log.info(\n                f\"Project bundle '{project_bundle}' is defined, but tray\"\n                \" cannot be running in project scope. Restarting tray to use\"\n                \" studio bundle.\"\n            )\n            self.restart()\n\n    def get_services_submenu(self):\n        return self._services_submenu\n\n    def restart(self):\n        \"\"\"Restart Tray tool.\n\n        First creates new process with same argument and close current tray.\n        \"\"\"\n\n        self._closing = True\n\n        args = get_ayon_launcher_args()\n\n        # Create a copy of sys.argv\n        additional_args = list(sys.argv)\n        # Remove first argument from 'sys.argv'\n        # - when running from code the first argument is 'start.py'\n        # - when running from build the first argument is executable\n        additional_args.pop(0)\n        additional_args = [\n            arg\n            for arg in additional_args\n            if arg not in {\"--use-staging\", \"--use-dev\"}\n        ]\n\n        if is_dev_mode_enabled():\n            additional_args.append(\"--use-dev\")\n        elif is_staging_enabled():\n            additional_args.append(\"--use-staging\")\n\n        if \"--project\" in additional_args:\n            idx = additional_args.index(\"--project\")\n            additional_args.pop(idx)\n            additional_args.pop(idx)\n\n        args.extend(additional_args)\n\n        envs = dict(os.environ.items())\n        for key in {\n            \"AYON_BUNDLE_NAME\",\n            \"AYON_STUDIO_BUNDLE_NAME\",\n            \"AYON_PROJECT_NAME\",\n        }:\n            envs.pop(key, None)\n\n        # Remove any existing addon path from 'PYTHONPATH'\n        addons_dir = os.environ.get(\"AYON_ADDONS_DIR\", \"\")\n        if addons_dir:\n            addons_dir = os.path.normpath(addons_dir)\n        addons_dir = addons_dir.lower()\n\n        pythonpath = envs.get(\"PYTHONPATH\") or \"\"\n        new_python_paths = []\n        for path in pythonpath.split(os.pathsep):\n            if not path:\n                continue\n            path = os.path.normpath(path)\n            if path.lower().startswith(addons_dir):\n                continue\n            new_python_paths.append(path)\n\n        envs[\"PYTHONPATH\"] = os.pathsep.join(new_python_paths)\n\n        # Start new process\n        run_detached_process(args, env=envs)\n        # Exit current tray process\n        self.exit()\n\n    def exit(self):\n        self._closing = True\n        if self._main_thread_timer.isActive():\n            self.execute_in_main_thread(self.tray_widget.exit)\n        else:\n            self.tray_widget.exit()\n\n    def on_exit(self):\n        remove_tray_server_url()\n        self._addons_manager.on_exit()\n\n    def execute_in_main_thread(self, callback, *args, **kwargs):\n        if isinstance(callback, WrappedCallbackItem):\n            item = callback\n        else:\n            item = WrappedCallbackItem(callback, *args, **kwargs)\n\n        self._main_thread_callbacks.append(item)\n\n        return item\n\n    async def _web_get_tray_info(self, _request: Request) -&gt; Response:\n        if self._cached_username is None:\n            self._cached_username = ayon_api.get_user()[\"name\"]\n\n        return json_response({\n            \"username\": self._cached_username,\n            \"bundle\": os.getenv(\"AYON_BUNDLE_NAME\"),\n            \"studio_bundle\": os.getenv(\"AYON_STUDIO_BUNDLE_NAME\"),\n            \"dev_mode\": is_dev_mode_enabled(),\n            \"staging_mode\": is_staging_enabled(),\n            \"addons\": {\n                addon.name: addon.version\n                for addon in self._addons_manager.get_enabled_addons()\n            },\n            \"installer_version\": os.getenv(\"AYON_VERSION\"),\n            \"running_time\": time.time() - self._start_time,\n        })\n\n    async def _web_show_tray_message(self, request: Request) -&gt; Response:\n        data = await request.json()\n        try:\n            title = data[\"title\"]\n            message = data[\"message\"]\n            icon = data.get(\"icon\")\n            msecs = data.get(\"msecs\")\n        except KeyError as exc:\n            return json_response(\n                {\n                    \"error\": f\"Missing required data. {exc}\",\n                    \"success\": False,\n                },\n                status=400,\n            )\n\n        if icon == \"information\":\n            icon = QtWidgets.QSystemTrayIconInformation\n        elif icon == \"warning\":\n            icon = QtWidgets.QSystemTrayIconWarning\n        elif icon == \"critical\":\n            icon = QtWidgets.QSystemTrayIcon.Critical\n        else:\n            icon = None\n\n        self.execute_in_main_thread(\n            self.show_tray_message, title, message, icon, msecs\n        )\n        return json_response({\"success\": True})\n\n    def _on_update_check_timer(self):\n        try:\n            bundles = ayon_api.get_bundles()\n            user = ayon_api.get_user()\n            # This is a workaround for bug in ayon-python-api\n            if user.get(\"code\") == 401:\n                raise Exception(\"Unauthorized\")\n        except Exception:\n            self._revalidate_ayon_auth()\n            if self._closing:\n                return\n\n            try:\n                bundles = ayon_api.get_bundles()\n            except Exception:\n                return\n\n        if is_dev_mode_enabled():\n            return\n\n        bundle_type = (\n            \"stagingBundle\"\n            if is_staging_enabled()\n            else \"productionBundle\"\n        )\n\n        expected_bundle = bundles.get(bundle_type)\n        current_bundle = os.environ.get(\"AYON_BUNDLE_NAME\")\n        is_expected = expected_bundle == current_bundle\n        if is_expected or expected_bundle is None:\n            self._restart_action.setVisible(False)\n            if (\n                self._outdated_dialog is not None\n                and self._outdated_dialog.isVisible()\n            ):\n                self._outdated_dialog.close_silently()\n            return\n\n        self._restart_action.setVisible(True)\n\n        if self._outdated_dialog is None:\n            self._outdated_dialog = UpdateDialog()\n            self._outdated_dialog.restart_requested.connect(\n                self._restart_and_install\n            )\n            self._outdated_dialog.ignore_requested.connect(\n                self._outdated_bundle_ignored\n            )\n\n        self._outdated_dialog.show()\n        self._outdated_dialog.raise_()\n        self._outdated_dialog.activateWindow()\n\n    def _revalidate_ayon_auth(self):\n        result = self._show_ayon_login(restart_on_token_change=False)\n        if self._closing:\n            return False\n\n        if not result.new_token:\n            self.exit()\n            return False\n        return True\n\n    def _restart_and_install(self):\n        self.restart()\n\n    def _outdated_bundle_ignored(self):\n        self.show_tray_message(\n            \"AYON update ignored\",\n            (\n                \"Please restart AYON launcher as soon as possible\"\n                \" to propagate updates.\"\n            )\n        )\n\n    def _main_thread_execution(self):\n        try:\n            if self._execution_in_progress:\n                return\n            self._execution_in_progress = True\n            for _ in range(len(self._main_thread_callbacks)):\n                if self._main_thread_callbacks:\n                    item = self._main_thread_callbacks.popleft()\n                    try:\n                        item.execute()\n                    except BaseException:\n                        self.log.erorr(\n                            \"Main thread execution failed\", exc_info=True\n                        )\n\n            self._execution_in_progress = False\n\n        except KeyboardInterrupt:\n            self.execute_in_main_thread(self.exit)\n\n    def _startup_validations(self):\n        \"\"\"Run possible startup validations.\"\"\"\n        # Trigger bundle validation on start\n        self._update_check_timer.timeout.emit()\n\n    def _add_version_item(self):\n        tray_menu = self.tray_widget.menu\n        login_action = QtWidgets.QAction(\"Login\", self.tray_widget)\n        login_action.triggered.connect(self._on_ayon_login)\n        tray_menu.addAction(login_action)\n        version_string = os.getenv(\"AYON_VERSION\", \"AYON Info\")\n\n        version_action = QtWidgets.QAction(version_string, self.tray_widget)\n        version_action.triggered.connect(self._on_version_action)\n\n        restart_action = QtWidgets.QAction(\n            \"Restart &amp;&amp; Update\", self.tray_widget\n        )\n        restart_action.triggered.connect(self._on_restart_action)\n        restart_action.setVisible(False)\n\n        tray_menu.addAction(version_action)\n        tray_menu.addAction(restart_action)\n        tray_menu.addSeparator()\n\n        self._restart_action = restart_action\n\n    def _on_ayon_login(self):\n        self.execute_in_main_thread(\n            self._show_ayon_login,\n            restart_on_token_change=True\n        )\n\n    def _show_ayon_login(self, restart_on_token_change):\n        from ayon_common.connection.credentials import change_user_ui\n\n        result = change_user_ui()\n        if result.shutdown:\n            self.exit()\n            return result\n\n        restart = result.restart\n        if restart_on_token_change and result.token_changed:\n            restart = True\n\n        if restart:\n            # Remove environment variables from current connection\n            # - keep develop, staging, headless values\n            for key in {\n                \"AYON_SERVER_URL\",\n                \"AYON_API_KEY\",\n                \"AYON_BUNDLE_NAME\",\n                \"AYON_STUDIO_BUNDLE_NAME\",\n                \"AYON_PROJECT_NAME\",\n            }:\n                os.environ.pop(key, None)\n            self.restart()\n        return result\n\n    def _on_restart_action(self):\n        self.restart()\n\n    def _restart_ayon(self):\n        args = get_ayon_launcher_args()\n\n        # Create a copy of sys.argv\n        additional_args = list(sys.argv)\n        # Remove first argument from 'sys.argv'\n        # - when running from code the first argument is 'start.py'\n        # - when running from build the first argument is executable\n        additional_args.pop(0)\n        additional_args = [\n            arg\n            for arg in additional_args\n            if arg not in {\"--use-staging\", \"--use-dev\"}\n        ]\n\n        if is_dev_mode_enabled():\n            additional_args.append(\"--use-dev\")\n        elif is_staging_enabled():\n            additional_args.append(\"--use-staging\")\n\n        args.extend(additional_args)\n\n        envs = dict(os.environ.items())\n        for key in {\n            \"AYON_BUNDLE_NAME\",\n            \"AYON_STUDIO_BUNDLE_NAME\",\n            \"AYON_PROJECT_NAME\",\n        }:\n            envs.pop(key, None)\n\n        run_detached_process(args, env=envs)\n        self.exit()\n\n    def _on_version_action(self):\n        if self._info_widget is None:\n            self._info_widget = InfoWidget()\n\n        self._info_widget.show()\n        self._info_widget.raise_()\n        self._info_widget.activateWindow()\n\n    def _show_launcher_window(self):\n        if self._launcher_window is None:\n            self._launcher_window = LauncherWindow()\n\n        self._launcher_window.show()\n        self._launcher_window.raise_()\n        self._launcher_window.activateWindow()\n\n    def _show_browser_window(self):\n        if self._browser_window is None:\n            self._browser_window = LoaderWindow()\n            self._browser_window.setWindowTitle(\"AYON Browser\")\n            install_ayon_plugins()\n\n        self._browser_window.show()\n        self._browser_window.raise_()\n        self._browser_window.activateWindow()\n\n    def _show_console_window(self):\n        self._console_window.show()\n        self._console_window.raise_()\n        self._console_window.activateWindow()\n\n    def _show_publish_report_viewer(self):\n        self._publish_report_viewer_window.refresh()\n        self._publish_report_viewer_window.show()\n        self._publish_report_viewer_window.raise_()\n        self._publish_report_viewer_window.activateWindow()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/tray/ui/tray.html#client.ayon_core.tools.tray.ui.tray.TrayManager.doubleclick_callback","title":"<code>doubleclick_callback</code>  <code>property</code>","text":"<p>Double-click callback for Tray icon.</p>"},{"location":"autoapi/client/ayon_core/tools/tray/ui/tray.html#client.ayon_core.tools.tray.ui.tray.TrayManager.execute_doubleclick","title":"<code>execute_doubleclick()</code>","text":"<p>Execute double click callback in main thread.</p> Source code in <code>client/ayon_core/tools/tray/ui/tray.py</code> <pre><code>def execute_doubleclick(self):\n    \"\"\"Execute double click callback in main thread.\"\"\"\n    callback = self.doubleclick_callback\n    if callback is not None:\n        self.execute_in_main_thread(callback)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/tray/ui/tray.html#client.ayon_core.tools.tray.ui.tray.TrayManager.initialize_addons","title":"<code>initialize_addons()</code>","text":"<p>Add addons to tray.</p> Source code in <code>client/ayon_core/tools/tray/ui/tray.py</code> <pre><code>def initialize_addons(self):\n    \"\"\"Add addons to tray.\"\"\"\n    if self._closing:\n        return\n\n    tray_menu = self.tray_widget.menu\n    # Add launcher at first place\n    launcher_action = QtWidgets.QAction(\n        \"Launcher\", tray_menu\n    )\n    launcher_action.triggered.connect(self._show_launcher_window)\n    tray_menu.addAction(launcher_action)\n\n    console_action = ITrayAddon.add_action_to_admin_submenu(\n        \"Console\", tray_menu\n    )\n    console_action.triggered.connect(self._show_console_window)\n\n    publish_report_viewer_action = ITrayAddon.add_action_to_admin_submenu(\n        \"Publish report viewer\", tray_menu\n    )\n    publish_report_viewer_action.triggered.connect(\n        self._show_publish_report_viewer\n    )\n\n    self._addons_manager.initialize(tray_menu)\n\n    # Add browser action after addon actions\n    browser_action = QtWidgets.QAction(\n        \"Browser\", tray_menu\n    )\n    browser_action.triggered.connect(self._show_browser_window)\n    tray_menu.addAction(browser_action)\n\n    self._addons_manager.add_route(\n        \"GET\", \"/tray\", self._web_get_tray_info\n    )\n    self._addons_manager.add_route(\n        \"POST\", \"/tray/message\", self._web_show_tray_message\n    )\n\n    admin_submenu = ITrayAddon.admin_submenu(tray_menu)\n    tray_menu.addMenu(admin_submenu)\n\n    # Add services if they are\n    services_submenu = ITrayService.services_submenu(tray_menu)\n    self._services_submenu = services_submenu\n    tray_menu.addMenu(services_submenu)\n\n    # Add separator\n    tray_menu.addSeparator()\n\n    self._add_version_item()\n\n    # Add Exit action to menu\n    exit_action = QtWidgets.QAction(\"Exit\", self.tray_widget)\n    exit_action.triggered.connect(self.tray_widget.exit)\n    tray_menu.addAction(exit_action)\n\n    # Tell each addon which addons were imported\n    # TODO Capture only webserver issues (the only thing that can crash).\n    try:\n        self._addons_manager.start_addons()\n    except Exception:\n        self.log.error(\n            \"Failed to start addons.\",\n            exc_info=True\n        )\n        return self.exit()\n\n    # Print time report\n    self._addons_manager.print_report()\n\n    self._main_thread_timer.start()\n\n    if self._update_check_interval &gt; 0:\n        self._update_check_timer.start()\n\n    self.execute_in_main_thread(self._startup_validations)\n    try:\n        set_tray_server_url(\n            self._addons_manager.webserver_url, True\n        )\n    except TrayIsRunningError:\n        self.log.warning(\"Other tray started meanwhile. Exiting.\")\n        self.exit()\n\n    project_bundle = os.getenv(\"AYON_BUNDLE_NAME\")\n    studio_bundle = os.getenv(\"AYON_STUDIO_BUNDLE_NAME\")\n    if studio_bundle and project_bundle != studio_bundle:\n        self.log.info(\n            f\"Project bundle '{project_bundle}' is defined, but tray\"\n            \" cannot be running in project scope. Restarting tray to use\"\n            \" studio bundle.\"\n        )\n        self.restart()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/tray/ui/tray.html#client.ayon_core.tools.tray.ui.tray.TrayManager.restart","title":"<code>restart()</code>","text":"<p>Restart Tray tool.</p> <p>First creates new process with same argument and close current tray.</p> Source code in <code>client/ayon_core/tools/tray/ui/tray.py</code> <pre><code>def restart(self):\n    \"\"\"Restart Tray tool.\n\n    First creates new process with same argument and close current tray.\n    \"\"\"\n\n    self._closing = True\n\n    args = get_ayon_launcher_args()\n\n    # Create a copy of sys.argv\n    additional_args = list(sys.argv)\n    # Remove first argument from 'sys.argv'\n    # - when running from code the first argument is 'start.py'\n    # - when running from build the first argument is executable\n    additional_args.pop(0)\n    additional_args = [\n        arg\n        for arg in additional_args\n        if arg not in {\"--use-staging\", \"--use-dev\"}\n    ]\n\n    if is_dev_mode_enabled():\n        additional_args.append(\"--use-dev\")\n    elif is_staging_enabled():\n        additional_args.append(\"--use-staging\")\n\n    if \"--project\" in additional_args:\n        idx = additional_args.index(\"--project\")\n        additional_args.pop(idx)\n        additional_args.pop(idx)\n\n    args.extend(additional_args)\n\n    envs = dict(os.environ.items())\n    for key in {\n        \"AYON_BUNDLE_NAME\",\n        \"AYON_STUDIO_BUNDLE_NAME\",\n        \"AYON_PROJECT_NAME\",\n    }:\n        envs.pop(key, None)\n\n    # Remove any existing addon path from 'PYTHONPATH'\n    addons_dir = os.environ.get(\"AYON_ADDONS_DIR\", \"\")\n    if addons_dir:\n        addons_dir = os.path.normpath(addons_dir)\n    addons_dir = addons_dir.lower()\n\n    pythonpath = envs.get(\"PYTHONPATH\") or \"\"\n    new_python_paths = []\n    for path in pythonpath.split(os.pathsep):\n        if not path:\n            continue\n        path = os.path.normpath(path)\n        if path.lower().startswith(addons_dir):\n            continue\n        new_python_paths.append(path)\n\n    envs[\"PYTHONPATH\"] = os.pathsep.join(new_python_paths)\n\n    # Start new process\n    run_detached_process(args, env=envs)\n    # Exit current tray process\n    self.exit()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/tray/ui/tray.html#client.ayon_core.tools.tray.ui.tray.TrayManager.show_tray_message","title":"<code>show_tray_message(title, message, icon=None, msecs=None)</code>","text":"<p>Show tray message.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <code>str</code> <p>Title of message.</p> required <code>message</code> <code>str</code> <p>Content of message.</p> required <code>icon</code> <code>MessageIcon</code> <p>Message's icon. Default is Information icon, may differ by Qt version.</p> <code>None</code> <code>msecs</code> <code>int</code> <p>Duration of message visibility in milliseconds. Default is 10000 msecs, may differ by Qt version.</p> <code>None</code> Source code in <code>client/ayon_core/tools/tray/ui/tray.py</code> <pre><code>def show_tray_message(self, title, message, icon=None, msecs=None):\n    \"\"\"Show tray message.\n\n    Args:\n        title (str): Title of message.\n        message (str): Content of message.\n        icon (QSystemTrayIcon.MessageIcon): Message's icon. Default is\n            Information icon, may differ by Qt version.\n        msecs (int): Duration of message visibility in milliseconds.\n            Default is 10000 msecs, may differ by Qt version.\n    \"\"\"\n    args = [title, message]\n    kwargs = {}\n    if icon:\n        kwargs[\"icon\"] = icon\n    if msecs:\n        kwargs[\"msecs\"] = msecs\n\n    self.tray_widget.showMessage(*args, **kwargs)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/tray/webserver/index.html","title":"webserver","text":""},{"location":"autoapi/client/ayon_core/tools/tray/webserver/index.html#client.ayon_core.tools.tray.webserver.RestApiEndpoint","title":"<code>RestApiEndpoint</code>","text":"<p>Helper endpoint class for single endpoint.</p> <p>Class can define <code>get</code>, <code>post</code>, <code>put</code> or <code>delete</code> async methods for the endpoint.</p> Source code in <code>client/ayon_core/tools/tray/webserver/base_routes.py</code> <pre><code>class RestApiEndpoint:\n    \"\"\"Helper endpoint class for single endpoint.\n\n    Class can define `get`, `post`, `put` or `delete` async methods for the\n    endpoint.\n    \"\"\"\n    def __init__(self):\n        methods = {}\n\n        for method_name in DEFAULT_METHODS:\n            method = getattr(self, method_name.lower(), None)\n            if method:\n                methods[method_name.upper()] = method\n\n        self.methods = methods\n\n    async def dispatch(self, request: Request):\n        method = self.methods.get(request.method.upper())\n        if not method:\n            raise HTTPMethodNotAllowed(\"\", DEFAULT_METHODS)\n\n        wanted_args = list(inspect.signature(method).parameters.keys())\n\n        available_args = request.match_info.copy()\n        available_args[\"request\"] = request\n\n        unsatisfied_args = set(wanted_args) - set(available_args.keys())\n        if unsatisfied_args:\n            # Expected match info that doesn't exist\n            raise HttpBadRequest(\"\")\n\n        return await method(**{\n            arg_name: available_args[arg_name]\n            for arg_name in wanted_args\n        })\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/tray/webserver/index.html#client.ayon_core.tools.tray.webserver.WebServerManager","title":"<code>WebServerManager</code>","text":"<p>Manger that care about web server thread.</p> Source code in <code>client/ayon_core/tools/tray/webserver/server.py</code> <pre><code>class WebServerManager:\n    \"\"\"Manger that care about web server thread.\"\"\"\n\n    def __init__(\n        self, port: Optional[int] = None, host: Optional[str] = None\n    ):\n        self._log = None\n\n        self.port = port or 8079\n        self.host = host or \"127.0.0.1\"\n\n        self.on_stop_callbacks = []\n\n        self.app = web.Application(\n            middlewares=[\n                cors_middleware(\n                    origins=[re.compile(r\"^https?\\:\\/\\/localhost\")]\n                )\n            ]\n        )\n\n        # add route with multiple methods for single \"external app\"\n        self.webserver_thread = WebServerThread(self)\n\n        self.add_static(\"/res\", RESOURCES_DIR)\n\n    @property\n    def log(self):\n        if self._log is None:\n            self._log = Logger.get_logger(self.__class__.__name__)\n        return self._log\n\n    @property\n    def url(self) -&gt; str:\n        return f\"http://{self.host}:{self.port}\"\n\n    def add_route(self, request_method: str, path: str, handler: Callable):\n        self.app.router.add_route(request_method, path, handler)\n\n    def add_static(self, prefix: str, path: str):\n        self.app.router.add_static(prefix, path)\n\n    def add_addon_route(\n        self,\n        addon_name: str,\n        path: str,\n        request_method: str,\n        handler: Callable\n    ) -&gt; str:\n        path = path.lstrip(\"/\")\n        full_path = f\"/addons/{addon_name}/{path}\"\n        self.app.router.add_route(request_method, full_path, handler)\n        return full_path\n\n    def add_addon_static(\n        self, addon_name: str, prefix: str, path: str\n    ) -&gt; str:\n        full_path = f\"/addons/{addon_name}/{prefix}\"\n        self.app.router.add_static(full_path, path)\n        return full_path\n\n    def connect_with_addons(self, addons):\n        for addon in addons:\n            if not hasattr(addon, \"webserver_initialization\"):\n                continue\n\n            try:\n                addon.webserver_initialization(self)\n            except Exception:\n                self.log.warning(\n                    f\"Failed to connect addon \\\"{addon.name}\\\" to webserver.\",\n                    exc_info=True\n                )\n\n    def start_server(self):\n        if self.webserver_thread and not self.webserver_thread.is_alive():\n            self.webserver_thread.start()\n\n    def stop_server(self):\n        if not self.is_running:\n            return\n        try:\n            self.log.debug(\"Stopping Web server\")\n            self.webserver_thread.is_running = False\n            self.webserver_thread.stop()\n\n        except Exception:\n            self.log.warning(\n                \"Error has happened during Killing Web server\",\n                exc_info=True\n            )\n\n    @property\n    def is_running(self) -&gt; bool:\n        if not self.webserver_thread:\n            return False\n        return self.webserver_thread.is_running\n\n    def thread_stopped(self):\n        for callback in self.on_stop_callbacks:\n            callback()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/tray/webserver/index.html#client.ayon_core.tools.tray.webserver.find_free_port","title":"<code>find_free_port(port_from=None, port_to=None, exclude_ports=None, host=None)</code>","text":"<p>Find available socket port from entered range.</p> <p>It is also possible to only check if entered port is available.</p> <p>Parameters:</p> Name Type Description Default <code>port_from</code> <code>int</code> <p>Port number which is checked as first.</p> <code>None</code> <code>port_to</code> <code>int</code> <p>Last port that is checked in sequence from entered <code>port_from</code>. Only <code>port_from</code> is checked if is not entered. Nothing is processed if is equeal to <code>port_from</code>!</p> <code>None</code> <code>exclude_ports</code> <code>(list, tuple, set)</code> <p>List of ports that won't be checked form entered range.</p> <code>None</code> <code>host</code> <code>str</code> <p>Host where will check for free ports. Set to \"127.0.0.1\" by default.</p> <code>None</code> Source code in <code>client/ayon_core/tools/tray/webserver/server.py</code> <pre><code>def find_free_port(\n    port_from=None, port_to=None, exclude_ports=None, host=None\n):\n    \"\"\"Find available socket port from entered range.\n\n    It is also possible to only check if entered port is available.\n\n    Args:\n        port_from (int): Port number which is checked as first.\n        port_to (int): Last port that is checked in sequence from entered\n            `port_from`. Only `port_from` is checked if is not entered.\n            Nothing is processed if is equeal to `port_from`!\n        exclude_ports (list, tuple, set): List of ports that won't be\n            checked form entered range.\n        host (str): Host where will check for free ports. Set to\n            \"127.0.0.1\" by default.\n    \"\"\"\n    if port_from is None:\n        port_from = 8079\n\n    if port_to is None:\n        port_to = 65535\n\n    # Excluded ports (e.g. reserved for other servers/clients)\n    if exclude_ports is None:\n        exclude_ports = []\n\n    # Default host is localhost but it is possible to look for other hosts\n    if host is None:\n        host = \"127.0.0.1\"\n\n    found_port = None\n    while True:\n        port = random.randint(port_from, port_to)\n        if port in exclude_ports:\n            continue\n\n        sock = None\n        try:\n            sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)\n            sock.bind((host, port))\n            found_port = port\n\n        except socket.error:\n            continue\n\n        finally:\n            if sock:\n                sock.close()\n\n        if found_port is not None:\n            break\n\n    return found_port\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/tray/webserver/base_routes.html","title":"base_routes","text":"<p>Helper functions or classes for Webserver module.</p> <p>These must not be imported in module itself to not break in-DCC process.</p>"},{"location":"autoapi/client/ayon_core/tools/tray/webserver/base_routes.html#client.ayon_core.tools.tray.webserver.base_routes.RestApiEndpoint","title":"<code>RestApiEndpoint</code>","text":"<p>Helper endpoint class for single endpoint.</p> <p>Class can define <code>get</code>, <code>post</code>, <code>put</code> or <code>delete</code> async methods for the endpoint.</p> Source code in <code>client/ayon_core/tools/tray/webserver/base_routes.py</code> <pre><code>class RestApiEndpoint:\n    \"\"\"Helper endpoint class for single endpoint.\n\n    Class can define `get`, `post`, `put` or `delete` async methods for the\n    endpoint.\n    \"\"\"\n    def __init__(self):\n        methods = {}\n\n        for method_name in DEFAULT_METHODS:\n            method = getattr(self, method_name.lower(), None)\n            if method:\n                methods[method_name.upper()] = method\n\n        self.methods = methods\n\n    async def dispatch(self, request: Request):\n        method = self.methods.get(request.method.upper())\n        if not method:\n            raise HTTPMethodNotAllowed(\"\", DEFAULT_METHODS)\n\n        wanted_args = list(inspect.signature(method).parameters.keys())\n\n        available_args = request.match_info.copy()\n        available_args[\"request\"] = request\n\n        unsatisfied_args = set(wanted_args) - set(available_args.keys())\n        if unsatisfied_args:\n            # Expected match info that doesn't exist\n            raise HttpBadRequest(\"\")\n\n        return await method(**{\n            arg_name: available_args[arg_name]\n            for arg_name in wanted_args\n        })\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/tray/webserver/cors_middleware.html","title":"cors_middleware","text":"<p>=============== CORS Middleware =============== .. versionadded:: 0.2.0 Dealing with CORS headers for aiohttp applications. IMPORTANT: There is a <code>aiohttp-cors &lt;https://pypi.org/project/aiohttp_cors/&gt;</code> library, which handles CORS headers by attaching additional handlers to aiohttp application for OPTIONS (preflight) requests. In same time this CORS middleware mimics the logic of <code>django-cors-headers &lt;https://pypi.org/project/django-cors-headers&gt;</code>, where all handling done in the middleware without any additional handlers. This approach allows aiohttp application to respond with CORS headers for OPTIONS or wildcard handlers, which is not possible with <code>aiohttp-cors</code> due to https://github.com/aio-libs/aiohttp-cors/issues/241 issue. For detailed information about CORS (Cross Origin Resource Sharing) please visit: - <code>Wikipedia &lt;https://en.m.wikipedia.org/wiki/Cross-origin_resource_sharing&gt;</code> - Or <code>MDN &lt;https://developer.mozilla.org/en-US/docs/Web/HTTP/CORS&gt;</code> Configuration ============= IMPORTANT: By default, CORS middleware do not allow any origins to access content from your aiohttp appliction. Which means, you need carefully check possible options and provide custom values for your needs. Usage ===== .. code-block:: python     import re     from aiohttp import web     from aiohttp_middlewares import cors_middleware     from aiohttp_middlewares.cors import DEFAULT_ALLOW_HEADERS     # Unsecure configuration to allow all CORS requests     app = web.Application(         middlewares=[cors_middleware(allow_all=True)]     )     # Allow CORS requests from URL http://localhost:3000     app = web.Application(         middlewares=[             cors_middleware(origins=[\"http://localhost:3000\"])         ]     )     # Allow CORS requests from all localhost urls     app = web.Application(         middlewares=[             cors_middleware(                 origins=[re.compile(r\"^https?\\:\\/\\/localhost\")]             )         ]     )     # Allow CORS requests from https://frontend.myapp.com as well     # as allow credentials     CORS_ALLOW_ORIGINS = [\"https://frontend.myapp.com\"]     app = web.Application(         middlewares=[             cors_middleware(                 origins=CORS_ALLOW_ORIGINS,                 allow_credentials=True,             )         ]     )     # Allow CORS requests only for API urls     app = web.Application(         middelwares=[             cors_middleware(                 origins=CORS_ALLOW_ORIGINS,                 urls=[re.compile(r\"^\\/api\")],             )         ]     )     # Allow CORS requests for POST &amp; PATCH methods, and for all     # default headers and <code>X-Client-UID</code>     app = web.Application(         middlewares=[             cors_middleware(                 origings=CORS_ALLOW_ORIGINS,                 allow_methods=(\"POST\", \"PATCH\"),                 allow_headers=DEFAULT_ALLOW_HEADERS                 + (\"X-Client-UID\",),             )         ]     )</p>"},{"location":"autoapi/client/ayon_core/tools/tray/webserver/cors_middleware.html#client.ayon_core.tools.tray.webserver.cors_middleware.cors_middleware","title":"<code>cors_middleware(*, allow_all=False, origins=None, urls=None, expose_headers=None, allow_headers=DEFAULT_ALLOW_HEADERS, allow_methods=DEFAULT_ALLOW_METHODS, allow_credentials=False, max_age=None)</code>","text":"<p>Middleware to provide CORS headers for aiohttp applications. :param allow_all:     When enabled, allow any Origin to access content from your aiohttp web     application. Please be careful with enabling this option as it may     result in security issues for your application. By default: <code>False</code> :param origins:     Allow content access for given list of origins. Support supplying     strings for exact origin match or regex instances. By default: <code>None</code> :param urls:     Allow contect access for given list of URLs in aiohttp application.     By default: apply CORS headers for all URLs :param expose_headers:     List of headers to be exposed with every CORS request. By default:     <code>None</code> :param allow_headers:     List of allowed headers. By default:     .. code-block:: python         (             \"accept\",             \"accept-encoding\",             \"authorization\",             \"content-type\",             \"dnt\",             \"origin\",             \"user-agent\",             \"x-csrftoken\",             \"x-requested-with\",         ) :param allow_methods:     List of allowed methods. By default:     .. code-block:: python         (\"DELETE\", \"GET\", \"OPTIONS\", \"PATCH\", \"POST\", \"PUT\") :param allow_credentials:     When enabled apply allow credentials header in response, which results     in sharing cookies on shared resources. Please be careful with     allowing credentials for CORS requests. By default: <code>False</code> :param max_age: Access control max age in seconds. By default: <code>None</code></p> Source code in <code>client/ayon_core/tools/tray/webserver/cors_middleware.py</code> <pre><code>def cors_middleware(\n    *,\n    allow_all: bool = False,\n    origins: UrlCollection = None,\n    urls: UrlCollection = None,\n    expose_headers: StrCollection = None,\n    allow_headers: StrCollection = DEFAULT_ALLOW_HEADERS,\n    allow_methods: StrCollection = DEFAULT_ALLOW_METHODS,\n    allow_credentials: bool = False,\n    max_age: int = None,\n) -&gt; Middleware:\n    \"\"\"Middleware to provide CORS headers for aiohttp applications.\n    :param allow_all:\n        When enabled, allow any Origin to access content from your aiohttp web\n        application. **Please be careful with enabling this option as it may\n        result in security issues for your application.** By default: ``False``\n    :param origins:\n        Allow content access for given list of origins. Support supplying\n        strings for exact origin match or regex instances. By default: ``None``\n    :param urls:\n        Allow contect access for given list of URLs in aiohttp application.\n        By default: *apply CORS headers for all URLs*\n    :param expose_headers:\n        List of headers to be exposed with every CORS request. By default:\n        ``None``\n    :param allow_headers:\n        List of allowed headers. By default:\n        .. code-block:: python\n            (\n                \"accept\",\n                \"accept-encoding\",\n                \"authorization\",\n                \"content-type\",\n                \"dnt\",\n                \"origin\",\n                \"user-agent\",\n                \"x-csrftoken\",\n                \"x-requested-with\",\n            )\n    :param allow_methods:\n        List of allowed methods. By default:\n        .. code-block:: python\n            (\"DELETE\", \"GET\", \"OPTIONS\", \"PATCH\", \"POST\", \"PUT\")\n    :param allow_credentials:\n        When enabled apply allow credentials header in response, which results\n        in sharing cookies on shared resources. **Please be careful with\n        allowing credentials for CORS requests.** By default: ``False``\n    :param max_age: Access control max age in seconds. By default: ``None``\n    \"\"\"\n    check_urls: UrlCollection = DEFAULT_URLS if urls is None else urls\n\n    @web.middleware\n    async def middleware(\n        request: web.Request, handler: Handler\n    ) -&gt; web.StreamResponse:\n        # Initial vars\n        request_method = request.method\n        request_path = request.rel_url.path\n\n        # Is this an OPTIONS request\n        is_options_request = request_method == \"OPTIONS\"\n\n        # Is this a preflight request\n        is_preflight_request = (\n            is_options_request\n            and ACCESS_CONTROL_REQUEST_METHOD in request.headers\n        )\n\n        # Log extra data\n        log_extra = {\n            \"is_preflight_request\": is_preflight_request,\n            \"method\": request_method.lower(),\n            \"path\": request_path,\n        }\n\n        # Check whether CORS should be enabled for given URL or not. By default\n        # CORS enabled for all URLs\n        if not match_items(check_urls, request_path):\n            logger.debug(\n                \"Request should not be processed via CORS middleware\",\n                extra=log_extra,\n            )\n            return await handler(request)\n\n        # If this is a preflight request - generate empty response\n        if is_preflight_request:\n            response = web.StreamResponse()\n        # Otherwise - call actual handler\n        else:\n            response = await handler(request)\n\n        # Now check origin heaer\n        origin = request.headers.get(\"Origin\")\n        # Empty origin - do nothing\n        if not origin:\n            logger.debug(\n                \"Request does not have Origin header. CORS headers not \"\n                \"available for given requests\",\n                extra=log_extra,\n            )\n            return response\n\n        # Set allow credentials header if necessary\n        if allow_credentials:\n            response.headers[ACCESS_CONTROL_ALLOW_CREDENTIALS] = \"true\"\n\n        # Check whether current origin satisfies CORS policy\n        if not allow_all and not (origins and match_items(origins, origin)):\n            logger.debug(\n                \"CORS headers not allowed for given Origin\", extra=log_extra\n            )\n            return response\n\n        # Now start supplying CORS headers\n        # First one is Access-Control-Allow-Origin\n        if allow_all and not allow_credentials:\n            cors_origin = \"*\"\n        else:\n            cors_origin = origin\n        response.headers[ACCESS_CONTROL_ALLOW_ORIGIN] = cors_origin\n\n        # Then Access-Control-Expose-Headers\n        if expose_headers:\n            response.headers[ACCESS_CONTROL_EXPOSE_HEADERS] = \", \".join(\n                expose_headers\n            )\n\n        # Now, if this is an options request, respond with extra Allow headers\n        if is_options_request:\n            response.headers[ACCESS_CONTROL_ALLOW_HEADERS] = \", \".join(\n                allow_headers\n            )\n            response.headers[ACCESS_CONTROL_ALLOW_METHODS] = \", \".join(\n                allow_methods\n            )\n            if max_age is not None:\n                response.headers[ACCESS_CONTROL_MAX_AGE] = str(max_age)\n\n        # If this is preflight request - do not allow other middlewares to\n        # process this request\n        if is_preflight_request:\n            logger.debug(\n                \"Provide CORS headers with empty response for preflight \"\n                \"request\",\n                extra=log_extra,\n            )\n            raise web.HTTPOk(text=\"\", headers=response.headers)\n\n        # Otherwise return normal response\n        logger.debug(\"Provide CORS headers for request\", extra=log_extra)\n        return response\n\n    return middleware\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/tray/webserver/cors_middleware.html#client.ayon_core.tools.tray.webserver.cors_middleware.match_items","title":"<code>match_items(items, value)</code>","text":"<p>Go through all items and try to match item with given value.</p> Source code in <code>client/ayon_core/tools/tray/webserver/cors_middleware.py</code> <pre><code>def match_items(items: UrlCollection, value: str) -&gt; bool:\n    \"\"\"Go through all items and try to match item with given value.\"\"\"\n    return any(match_path(item, value) for item in items)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/tray/webserver/server.html","title":"server","text":""},{"location":"autoapi/client/ayon_core/tools/tray/webserver/server.html#client.ayon_core.tools.tray.webserver.server.WebServerManager","title":"<code>WebServerManager</code>","text":"<p>Manger that care about web server thread.</p> Source code in <code>client/ayon_core/tools/tray/webserver/server.py</code> <pre><code>class WebServerManager:\n    \"\"\"Manger that care about web server thread.\"\"\"\n\n    def __init__(\n        self, port: Optional[int] = None, host: Optional[str] = None\n    ):\n        self._log = None\n\n        self.port = port or 8079\n        self.host = host or \"127.0.0.1\"\n\n        self.on_stop_callbacks = []\n\n        self.app = web.Application(\n            middlewares=[\n                cors_middleware(\n                    origins=[re.compile(r\"^https?\\:\\/\\/localhost\")]\n                )\n            ]\n        )\n\n        # add route with multiple methods for single \"external app\"\n        self.webserver_thread = WebServerThread(self)\n\n        self.add_static(\"/res\", RESOURCES_DIR)\n\n    @property\n    def log(self):\n        if self._log is None:\n            self._log = Logger.get_logger(self.__class__.__name__)\n        return self._log\n\n    @property\n    def url(self) -&gt; str:\n        return f\"http://{self.host}:{self.port}\"\n\n    def add_route(self, request_method: str, path: str, handler: Callable):\n        self.app.router.add_route(request_method, path, handler)\n\n    def add_static(self, prefix: str, path: str):\n        self.app.router.add_static(prefix, path)\n\n    def add_addon_route(\n        self,\n        addon_name: str,\n        path: str,\n        request_method: str,\n        handler: Callable\n    ) -&gt; str:\n        path = path.lstrip(\"/\")\n        full_path = f\"/addons/{addon_name}/{path}\"\n        self.app.router.add_route(request_method, full_path, handler)\n        return full_path\n\n    def add_addon_static(\n        self, addon_name: str, prefix: str, path: str\n    ) -&gt; str:\n        full_path = f\"/addons/{addon_name}/{prefix}\"\n        self.app.router.add_static(full_path, path)\n        return full_path\n\n    def connect_with_addons(self, addons):\n        for addon in addons:\n            if not hasattr(addon, \"webserver_initialization\"):\n                continue\n\n            try:\n                addon.webserver_initialization(self)\n            except Exception:\n                self.log.warning(\n                    f\"Failed to connect addon \\\"{addon.name}\\\" to webserver.\",\n                    exc_info=True\n                )\n\n    def start_server(self):\n        if self.webserver_thread and not self.webserver_thread.is_alive():\n            self.webserver_thread.start()\n\n    def stop_server(self):\n        if not self.is_running:\n            return\n        try:\n            self.log.debug(\"Stopping Web server\")\n            self.webserver_thread.is_running = False\n            self.webserver_thread.stop()\n\n        except Exception:\n            self.log.warning(\n                \"Error has happened during Killing Web server\",\n                exc_info=True\n            )\n\n    @property\n    def is_running(self) -&gt; bool:\n        if not self.webserver_thread:\n            return False\n        return self.webserver_thread.is_running\n\n    def thread_stopped(self):\n        for callback in self.on_stop_callbacks:\n            callback()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/tray/webserver/server.html#client.ayon_core.tools.tray.webserver.server.WebServerThread","title":"<code>WebServerThread</code>","text":"<p>               Bases: <code>Thread</code></p> <p>Listener for requests in thread.</p> Source code in <code>client/ayon_core/tools/tray/webserver/server.py</code> <pre><code>class WebServerThread(threading.Thread):\n    \"\"\" Listener for requests in thread.\"\"\"\n\n    def __init__(self, manager):\n        self._log = None\n\n        super(WebServerThread, self).__init__()\n\n        self.is_running = False\n        self.manager = manager\n        self.loop = None\n        self.runner = None\n        self.site = None\n        self.tasks = []\n\n    @property\n    def log(self):\n        if self._log is None:\n            self._log = Logger.get_logger(self.__class__.__name__)\n        return self._log\n\n    @property\n    def port(self):\n        return self.manager.port\n\n    @property\n    def host(self):\n        return self.manager.host\n\n    def run(self):\n        self.is_running = True\n\n        try:\n            self.log.info(\"Starting WebServer server\")\n            self.loop = asyncio.new_event_loop()  # create new loop for thread\n            asyncio.set_event_loop(self.loop)\n\n            self.loop.run_until_complete(self.start_server())\n\n            self.log.debug(\n                \"Running Web server on URL: \\\"localhost:{}\\\"\".format(self.port)\n            )\n\n            asyncio.ensure_future(self.check_shutdown(), loop=self.loop)\n            self.loop.run_forever()\n\n        except Exception:\n            self.log.warning(\n                \"Web Server service has failed\", exc_info=True\n            )\n        finally:\n            self.loop.close()  # optional\n\n        self.is_running = False\n        self.manager.thread_stopped()\n        self.log.info(\"Web server stopped\")\n\n    async def start_server(self):\n        \"\"\" Starts runner and TCPsite \"\"\"\n        self.runner = web.AppRunner(self.manager.app)\n        await self.runner.setup()\n        self.site = web.TCPSite(self.runner, self.host, self.port)\n        await self.site.start()\n\n    def stop(self):\n        \"\"\"Sets is_running flag to false, 'check_shutdown' shuts server down\"\"\"\n        self.is_running = False\n\n    async def check_shutdown(self):\n        \"\"\" Future that is running and checks if server should be running\n            periodically.\n        \"\"\"\n        while self.is_running:\n            while self.tasks:\n                task = self.tasks.pop(0)\n                self.log.debug(\"waiting for task {}\".format(task))\n                await task\n                self.log.debug(\"returned value {}\".format(task.result))\n\n            await asyncio.sleep(0.5)\n\n        self.log.debug(\"Starting shutdown\")\n        await self.site.stop()\n        self.log.debug(\"Site stopped\")\n        await self.runner.cleanup()\n        self.log.debug(\"Runner stopped\")\n        tasks = [\n            task\n            for task in asyncio.all_tasks()\n            if task is not asyncio.current_task()\n        ]\n        list(map(lambda task: task.cancel(), tasks))  # cancel all the tasks\n        results = await asyncio.gather(*tasks, return_exceptions=True)\n        self.log.debug(\n            f'Finished awaiting cancelled tasks, results: {results}...'\n        )\n        await self.loop.shutdown_asyncgens()\n        # to really make sure everything else has time to stop\n        await asyncio.sleep(0.07)\n        self.loop.stop()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/tray/webserver/server.html#client.ayon_core.tools.tray.webserver.server.WebServerThread.check_shutdown","title":"<code>check_shutdown()</code>  <code>async</code>","text":"<p>Future that is running and checks if server should be running periodically.</p> Source code in <code>client/ayon_core/tools/tray/webserver/server.py</code> <pre><code>async def check_shutdown(self):\n    \"\"\" Future that is running and checks if server should be running\n        periodically.\n    \"\"\"\n    while self.is_running:\n        while self.tasks:\n            task = self.tasks.pop(0)\n            self.log.debug(\"waiting for task {}\".format(task))\n            await task\n            self.log.debug(\"returned value {}\".format(task.result))\n\n        await asyncio.sleep(0.5)\n\n    self.log.debug(\"Starting shutdown\")\n    await self.site.stop()\n    self.log.debug(\"Site stopped\")\n    await self.runner.cleanup()\n    self.log.debug(\"Runner stopped\")\n    tasks = [\n        task\n        for task in asyncio.all_tasks()\n        if task is not asyncio.current_task()\n    ]\n    list(map(lambda task: task.cancel(), tasks))  # cancel all the tasks\n    results = await asyncio.gather(*tasks, return_exceptions=True)\n    self.log.debug(\n        f'Finished awaiting cancelled tasks, results: {results}...'\n    )\n    await self.loop.shutdown_asyncgens()\n    # to really make sure everything else has time to stop\n    await asyncio.sleep(0.07)\n    self.loop.stop()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/tray/webserver/server.html#client.ayon_core.tools.tray.webserver.server.WebServerThread.start_server","title":"<code>start_server()</code>  <code>async</code>","text":"<p>Starts runner and TCPsite</p> Source code in <code>client/ayon_core/tools/tray/webserver/server.py</code> <pre><code>async def start_server(self):\n    \"\"\" Starts runner and TCPsite \"\"\"\n    self.runner = web.AppRunner(self.manager.app)\n    await self.runner.setup()\n    self.site = web.TCPSite(self.runner, self.host, self.port)\n    await self.site.start()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/tray/webserver/server.html#client.ayon_core.tools.tray.webserver.server.WebServerThread.stop","title":"<code>stop()</code>","text":"<p>Sets is_running flag to false, 'check_shutdown' shuts server down</p> Source code in <code>client/ayon_core/tools/tray/webserver/server.py</code> <pre><code>def stop(self):\n    \"\"\"Sets is_running flag to false, 'check_shutdown' shuts server down\"\"\"\n    self.is_running = False\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/tray/webserver/server.html#client.ayon_core.tools.tray.webserver.server.find_free_port","title":"<code>find_free_port(port_from=None, port_to=None, exclude_ports=None, host=None)</code>","text":"<p>Find available socket port from entered range.</p> <p>It is also possible to only check if entered port is available.</p> <p>Parameters:</p> Name Type Description Default <code>port_from</code> <code>int</code> <p>Port number which is checked as first.</p> <code>None</code> <code>port_to</code> <code>int</code> <p>Last port that is checked in sequence from entered <code>port_from</code>. Only <code>port_from</code> is checked if is not entered. Nothing is processed if is equeal to <code>port_from</code>!</p> <code>None</code> <code>exclude_ports</code> <code>(list, tuple, set)</code> <p>List of ports that won't be checked form entered range.</p> <code>None</code> <code>host</code> <code>str</code> <p>Host where will check for free ports. Set to \"127.0.0.1\" by default.</p> <code>None</code> Source code in <code>client/ayon_core/tools/tray/webserver/server.py</code> <pre><code>def find_free_port(\n    port_from=None, port_to=None, exclude_ports=None, host=None\n):\n    \"\"\"Find available socket port from entered range.\n\n    It is also possible to only check if entered port is available.\n\n    Args:\n        port_from (int): Port number which is checked as first.\n        port_to (int): Last port that is checked in sequence from entered\n            `port_from`. Only `port_from` is checked if is not entered.\n            Nothing is processed if is equeal to `port_from`!\n        exclude_ports (list, tuple, set): List of ports that won't be\n            checked form entered range.\n        host (str): Host where will check for free ports. Set to\n            \"127.0.0.1\" by default.\n    \"\"\"\n    if port_from is None:\n        port_from = 8079\n\n    if port_to is None:\n        port_to = 65535\n\n    # Excluded ports (e.g. reserved for other servers/clients)\n    if exclude_ports is None:\n        exclude_ports = []\n\n    # Default host is localhost but it is possible to look for other hosts\n    if host is None:\n        host = \"127.0.0.1\"\n\n    found_port = None\n    while True:\n        port = random.randint(port_from, port_to)\n        if port in exclude_ports:\n            continue\n\n        sock = None\n        try:\n            sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)\n            sock.bind((host, port))\n            found_port = port\n\n        except socket.error:\n            continue\n\n        finally:\n            if sock:\n                sock.close()\n\n        if found_port is not None:\n            break\n\n    return found_port\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html","title":"utils","text":""},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.BaseClickableFrame","title":"<code>BaseClickableFrame</code>","text":"<p>               Bases: <code>QFrame</code></p> <p>Widget that catch left mouse click and can trigger a callback.</p> <p>Callback is defined by overriding <code>_mouse_release_callback</code>.</p> Source code in <code>client/ayon_core/tools/utils/widgets.py</code> <pre><code>class BaseClickableFrame(QtWidgets.QFrame):\n    \"\"\"Widget that catch left mouse click and can trigger a callback.\n\n    Callback is defined by overriding `_mouse_release_callback`.\n    \"\"\"\n    def __init__(self, parent):\n        super().__init__(parent)\n\n        self._mouse_pressed = False\n\n    def _mouse_release_callback(self):\n        pass\n\n    def mousePressEvent(self, event):\n        super().mousePressEvent(event)\n        if event.isAccepted():\n            return\n        if event.button() == QtCore.Qt.LeftButton:\n            self._mouse_pressed = True\n            event.accept()\n\n    def mouseReleaseEvent(self, event):\n        pressed, self._mouse_pressed = self._mouse_pressed, False\n        super().mouseReleaseEvent(event)\n        if event.isAccepted():\n            return\n\n        accepted = pressed and self.rect().contains(event.pos())\n        if accepted:\n            event.accept()\n            self._mouse_release_callback()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.ClassicExpandBtn","title":"<code>ClassicExpandBtn</code>","text":"<p>               Bases: <code>ExpandBtn</code></p> <p>Same as 'ExpandBtn' but with arrow images.</p> Source code in <code>client/ayon_core/tools/utils/widgets.py</code> <pre><code>class ClassicExpandBtn(ExpandBtn):\n    \"\"\"Same as 'ExpandBtn' but with arrow images.\"\"\"\n\n    def _create_pix_widget(self, parent=None):\n        if parent is None:\n            parent = self\n        return ClassicExpandBtnLabel(parent)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.ClickableFrame","title":"<code>ClickableFrame</code>","text":"<p>               Bases: <code>BaseClickableFrame</code></p> <p>Extended clickable frame which triggers 'clicked' signal.</p> Source code in <code>client/ayon_core/tools/utils/widgets.py</code> <pre><code>class ClickableFrame(BaseClickableFrame):\n    \"\"\"Extended clickable frame which triggers 'clicked' signal.\"\"\"\n    clicked = QtCore.Signal()\n\n    def _mouse_release_callback(self):\n        self.clicked.emit()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.ClickableLabel","title":"<code>ClickableLabel</code>","text":"<p>               Bases: <code>QLabel</code></p> <p>Label that catch left mouse click and can trigger 'clicked' signal.</p> Source code in <code>client/ayon_core/tools/utils/widgets.py</code> <pre><code>class ClickableLabel(QtWidgets.QLabel):\n    \"\"\"Label that catch left mouse click and can trigger 'clicked' signal.\"\"\"\n    clicked = QtCore.Signal()\n\n    def __init__(self, *args, **kwargs):\n        super().__init__(*args, **kwargs)\n\n        self._mouse_pressed = False\n\n    def mousePressEvent(self, event):\n        if event.button() == QtCore.Qt.LeftButton:\n            self._mouse_pressed = True\n        super().mousePressEvent(event)\n\n    def mouseReleaseEvent(self, event):\n        if self._mouse_pressed:\n            self._mouse_pressed = False\n            if self.rect().contains(event.pos()):\n                self.clicked.emit()\n\n        super().mouseReleaseEvent(event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.ComboBox","title":"<code>ComboBox</code>","text":"<p>               Bases: <code>QComboBox</code></p> <p>Base of combobox with pre-implement changes used in tools.</p> <p>Combobox is using styled delegate by default so stylesheets are propagated.</p> <p>Items are not changed on scroll until the combobox is in focus.</p> Source code in <code>client/ayon_core/tools/utils/widgets.py</code> <pre><code>class ComboBox(QtWidgets.QComboBox):\n    \"\"\"Base of combobox with pre-implement changes used in tools.\n\n    Combobox is using styled delegate by default so stylesheets are propagated.\n\n    Items are not changed on scroll until the combobox is in focus.\n    \"\"\"\n\n    def __init__(self, *args, **kwargs):\n        super().__init__(*args, **kwargs)\n        delegate = QtWidgets.QStyledItemDelegate()\n        self.setItemDelegate(delegate)\n        self.setFocusPolicy(QtCore.Qt.StrongFocus)\n\n        self._delegate = delegate\n\n    def wheelEvent(self, event):\n        if self.hasFocus():\n            return super().wheelEvent(event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.CustomTextComboBox","title":"<code>CustomTextComboBox</code>","text":"<p>               Bases: <code>ComboBox</code></p> <p>Combobox which can have different text showed.</p> Source code in <code>client/ayon_core/tools/utils/widgets.py</code> <pre><code>class CustomTextComboBox(ComboBox):\n    \"\"\"Combobox which can have different text showed.\"\"\"\n\n    def __init__(self, *args, **kwargs):\n        self._custom_text = None\n        super().__init__(*args, **kwargs)\n\n    def set_custom_text(self, text=None):\n        if self._custom_text != text:\n            self._custom_text = text\n            self.repaint()\n\n    def paintEvent(self, event):\n        painter = QtWidgets.QStylePainter(self)\n        option = QtWidgets.QStyleOptionComboBox()\n        self.initStyleOption(option)\n        if self._custom_text is not None:\n            option.currentText = self._custom_text\n        painter.drawComplexControl(QtWidgets.QStyle.CC_ComboBox, option)\n        painter.drawControl(QtWidgets.QStyle.CE_ComboBoxLabel, option)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.DeselectableTreeView","title":"<code>DeselectableTreeView</code>","text":"<p>               Bases: <code>QTreeView</code></p> <p>A tree view that deselects on clicking on an empty area in the view</p> Source code in <code>client/ayon_core/tools/utils/views.py</code> <pre><code>class DeselectableTreeView(QtWidgets.QTreeView):\n    \"\"\"A tree view that deselects on clicking on an empty area in the view\"\"\"\n\n    def __init__(self, *args, **kwargs):\n        super().__init__(*args, **kwargs)\n        self.setIndentation(20)\n\n    def mousePressEvent(self, event):\n        index = self.indexAt(event.pos())\n        if not index.isValid():\n            # clear the selection\n            self.clearSelection()\n            # clear the current index\n            self.setCurrentIndex(QtCore.QModelIndex())\n\n        elif (\n            self.selectionModel().isSelected(index)\n            and len(self.selectionModel().selectedRows()) == 1\n            and event.modifiers() == QtCore.Qt.NoModifier\n        ):\n            event.setModifiers(QtCore.Qt.ControlModifier)\n\n        super().mousePressEvent(event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.DynamicQThread","title":"<code>DynamicQThread</code>","text":"<p>               Bases: <code>QThread</code></p> <p>QThread which can run any function with argument and kwargs.</p> <p>Parameters:</p> Name Type Description Default <code>func</code> <code>function</code> <p>Function which will be called.</p> required <code>args</code> <code>tuple</code> <p>Arguments which will be passed to function.</p> <code>None</code> <code>kwargs</code> <code>tuple</code> <p>Keyword arguments which will be passed to function.</p> <code>None</code> <code>parent</code> <code>QObject</code> <p>Parent of thread.</p> <code>None</code> Source code in <code>client/ayon_core/tools/utils/lib.py</code> <pre><code>class DynamicQThread(QtCore.QThread):\n    \"\"\"QThread which can run any function with argument and kwargs.\n\n    Args:\n        func (function): Function which will be called.\n        args (tuple): Arguments which will be passed to function.\n        kwargs (tuple): Keyword arguments which will be passed to function.\n        parent (QObject): Parent of thread.\n    \"\"\"\n    def __init__(self, func, args=None, kwargs=None, parent=None):\n        super(DynamicQThread, self).__init__(parent)\n        if args is None:\n            args = tuple()\n        if kwargs is None:\n            kwargs = {}\n        self._func = func\n        self._args = args\n        self._kwargs = kwargs\n\n    def run(self):\n        \"\"\"Execute the function with arguments.\"\"\"\n        self._func(*self._args, **self._kwargs)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.DynamicQThread.run","title":"<code>run()</code>","text":"<p>Execute the function with arguments.</p> Source code in <code>client/ayon_core/tools/utils/lib.py</code> <pre><code>def run(self):\n    \"\"\"Execute the function with arguments.\"\"\"\n    self._func(*self._args, **self._kwargs)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.ElideLabel","title":"<code>ElideLabel</code>","text":"<p>               Bases: <code>QLabel</code></p> <p>Label which elide text.</p> <p>By default, elide happens on right side. Can be changed with 'set_elide_mode' method.</p> <p>It is not possible to use other features of QLabel like word wrap or interactive text. This is a simple label which elide text.</p> Source code in <code>client/ayon_core/tools/utils/widgets.py</code> <pre><code>class ElideLabel(QtWidgets.QLabel):\n    \"\"\"Label which elide text.\n\n    By default, elide happens on right side. Can be changed with\n    'set_elide_mode' method.\n\n    It is not possible to use other features of QLabel like word wrap or\n    interactive text. This is a simple label which elide text.\n    \"\"\"\n    def __init__(self, *args, **kwargs):\n        super().__init__(*args, **kwargs)\n        self.setSizePolicy(\n            QtWidgets.QSizePolicy.Expanding,\n            QtWidgets.QSizePolicy.Preferred\n        )\n        # Store text set during init\n        self._text = self.text()\n        # Define initial elide mode\n        self._elide_mode = QtCore.Qt.ElideRight\n        # Make sure that text of QLabel is empty\n        super().setText(\"\")\n\n    def setText(self, text):\n        # Update private text attribute and force update\n        self._text = text\n        self.update()\n\n    def setWordWrap(self, word_wrap):\n        # Word wrap is not supported in 'ElideLabel'\n        if word_wrap:\n            raise ValueError(\"Word wrap is not supported in 'ElideLabel'.\")\n\n    def contextMenuEvent(self, event):\n        menu = self.create_context_menu(event.pos())\n        if menu is None:\n            event.ignore()\n            return\n        event.accept()\n        menu.setAttribute(QtCore.Qt.WA_DeleteOnClose)\n        menu.popup(event.globalPos())\n\n    def create_context_menu(self, pos):\n        if not self._text:\n            return None\n        menu = QtWidgets.QMenu(self)\n\n        # Copy text action\n        copy_action = menu.addAction(\"Copy\")\n        copy_action.setObjectName(\"edit-copy\")\n        icon = QtGui.QIcon.fromTheme(\"edit-copy\")\n        if not icon.isNull():\n            copy_action.setIcon(icon)\n\n        copy_action.triggered.connect(self._on_copy_text)\n        return menu\n\n    def set_set(self, text):\n        self.setText(text)\n\n    def set_elide_mode(self, elide_mode):\n        \"\"\"Change elide type.\n\n        Args:\n            elide_mode: Possible elide type. Available in 'QtCore.Qt'\n                'ElideLeft', 'ElideRight' and 'ElideMiddle'.\n\n        \"\"\"\n        if elide_mode == QtCore.Qt.ElideNone:\n            raise ValueError(\n                \"Invalid elide type. 'ElideNone' is not supported.\"\n            )\n\n        if elide_mode not in (\n            QtCore.Qt.ElideLeft,\n            QtCore.Qt.ElideRight,\n            QtCore.Qt.ElideMiddle,\n        ):\n            raise ValueError(f\"Unknown value '{elide_mode}'\")\n        self._elide_mode = elide_mode\n        self.update()\n\n    def paintEvent(self, event):\n        super().paintEvent(event)\n\n        painter = QtGui.QPainter(self)\n        fm = painter.fontMetrics()\n        elided_line = fm.elidedText(\n            self._text, self._elide_mode, self.width()\n        )\n        painter.drawText(QtCore.QPoint(0, fm.ascent()), elided_line)\n\n    def _on_copy_text(self):\n        clipboard = QtWidgets.QApplication.clipboard()\n        clipboard.setText(self._text)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.ElideLabel.set_elide_mode","title":"<code>set_elide_mode(elide_mode)</code>","text":"<p>Change elide type.</p> <p>Parameters:</p> Name Type Description Default <code>elide_mode</code> <p>Possible elide type. Available in 'QtCore.Qt' 'ElideLeft', 'ElideRight' and 'ElideMiddle'.</p> required Source code in <code>client/ayon_core/tools/utils/widgets.py</code> <pre><code>def set_elide_mode(self, elide_mode):\n    \"\"\"Change elide type.\n\n    Args:\n        elide_mode: Possible elide type. Available in 'QtCore.Qt'\n            'ElideLeft', 'ElideRight' and 'ElideMiddle'.\n\n    \"\"\"\n    if elide_mode == QtCore.Qt.ElideNone:\n        raise ValueError(\n            \"Invalid elide type. 'ElideNone' is not supported.\"\n        )\n\n    if elide_mode not in (\n        QtCore.Qt.ElideLeft,\n        QtCore.Qt.ElideRight,\n        QtCore.Qt.ElideMiddle,\n    ):\n        raise ValueError(f\"Unknown value '{elide_mode}'\")\n    self._elide_mode = elide_mode\n    self.update()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.ExpandingTextEdit","title":"<code>ExpandingTextEdit</code>","text":"<p>               Bases: <code>QTextEdit</code></p> <p>QTextEdit which does not have sroll area but expands height.</p> Source code in <code>client/ayon_core/tools/utils/widgets.py</code> <pre><code>class ExpandingTextEdit(QtWidgets.QTextEdit):\n    \"\"\"QTextEdit which does not have sroll area but expands height.\"\"\"\n\n    def __init__(self, parent=None):\n        super().__init__(parent)\n\n        size_policy = self.sizePolicy()\n        size_policy.setHeightForWidth(True)\n        size_policy.setVerticalPolicy(QtWidgets.QSizePolicy.Preferred)\n        self.setSizePolicy(size_policy)\n\n        self.setHorizontalScrollBarPolicy(QtCore.Qt.ScrollBarAlwaysOff)\n        self.setVerticalScrollBarPolicy(QtCore.Qt.ScrollBarAlwaysOff)\n\n        doc = self.document()\n        doc.contentsChanged.connect(self._on_doc_change)\n\n    def _on_doc_change(self):\n        self.updateGeometry()\n\n    def hasHeightForWidth(self):\n        return True\n\n    def heightForWidth(self, width):\n        margins = self.contentsMargins()\n\n        document_width = 0\n        margins_size = margins.left() + margins.right()\n        if width &gt;= margins_size:\n            document_width = width - margins_size\n\n        document = self.document().clone()\n        document.setTextWidth(document_width)\n\n        return math.ceil(\n            margins.top()\n            + document.size().height()\n            + margins.bottom()\n            + 2\n        )\n\n    def sizeHint(self):\n        width = super().sizeHint().width()\n        return QtCore.QSize(width, self.heightForWidth(width))\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.FlowLayout","title":"<code>FlowLayout</code>","text":"<p>               Bases: <code>QLayout</code></p> <p>Layout that organize widgets by minimum size into a flow layout.</p> <p>Layout is putting widget from left to right and top to bottom. When widget can't fit a row it is added to next line. Minimum size matches widget with biggest 'sizeHint' width and height using calculated geometry.</p> <p>Content margins are part of calculations. It is possible to define horizontal and vertical spacing.</p> <p>Layout does not support stretch and spacing items.</p> Todos <p>Unified width concept -&gt; use width of largest item so all of them are     same. This could allow to have minimum columns option too.</p> Source code in <code>client/ayon_core/tools/utils/layouts.py</code> <pre><code>class FlowLayout(QtWidgets.QLayout):\n    \"\"\"Layout that organize widgets by minimum size into a flow layout.\n\n    Layout is putting widget from left to right and top to bottom. When widget\n    can't fit a row it is added to next line. Minimum size matches widget with\n    biggest 'sizeHint' width and height using calculated geometry.\n\n    Content margins are part of calculations. It is possible to define\n    horizontal and vertical spacing.\n\n    Layout does not support stretch and spacing items.\n\n    Todos:\n        Unified width concept -&gt; use width of largest item so all of them are\n            same. This could allow to have minimum columns option too.\n    \"\"\"\n\n    def __init__(self, parent=None):\n        super(FlowLayout, self).__init__(parent)\n\n        # spaces between each item\n        self._horizontal_spacing = 5\n        self._vertical_spacing = 5\n\n        self._items = []\n\n    def __del__(self):\n        while self.count():\n            self.takeAt(0, False)\n\n    def isEmpty(self):\n        for item in self._items:\n            if not item.isEmpty():\n                return False\n        return True\n\n    def setSpacing(self, spacing):\n        self._horizontal_spacing = spacing\n        self._vertical_spacing = spacing\n        self.invalidate()\n\n    def setHorizontalSpacing(self, spacing):\n        self._horizontal_spacing = spacing\n        self.invalidate()\n\n    def setVerticalSpacing(self, spacing):\n        self._vertical_spacing = spacing\n        self.invalidate()\n\n    def addItem(self, item):\n        self._items.append(item)\n        self.invalidate()\n\n    def count(self):\n        return len(self._items)\n\n    def itemAt(self, index):\n        if 0 &lt;= index &lt; len(self._items):\n            return self._items[index]\n        return None\n\n    def takeAt(self, index, invalidate=True):\n        if 0 &lt;= index &lt; len(self._items):\n            item = self._items.pop(index)\n            if invalidate:\n                self.invalidate()\n            return item\n        return None\n\n    def expandingDirections(self):\n        return QtCore.Qt.Orientations(QtCore.Qt.Vertical)\n\n    def hasHeightForWidth(self):\n        return True\n\n    def heightForWidth(self, width):\n        return self._setup_geometry(QtCore.QRect(0, 0, width, 0), True)\n\n    def setGeometry(self, rect):\n        super(FlowLayout, self).setGeometry(rect)\n        self._setup_geometry(rect)\n\n    def sizeHint(self):\n        return self.minimumSize()\n\n    def minimumSize(self):\n        size = QtCore.QSize(0, 0)\n        for item in self._items:\n            widget = item.widget()\n            if widget is not None:\n                parent = widget.parent()\n                if not widget.isVisibleTo(parent):\n                    continue\n            size = size.expandedTo(item.minimumSize())\n\n        if size.width() &lt; 1 or size.height() &lt; 1:\n            return size\n        l_margin, t_margin, r_margin, b_margin = self.getContentsMargins()\n        size += QtCore.QSize(l_margin + r_margin, t_margin + b_margin)\n        return size\n\n    def _setup_geometry(self, rect, only_calculate=False):\n        h_spacing = self._horizontal_spacing\n        v_spacing = self._vertical_spacing\n        l_margin, t_margin, r_margin, b_margin = self.getContentsMargins()\n\n        left_x = rect.x() + l_margin\n        top_y = rect.y() + t_margin\n        pos_x = left_x\n        pos_y = top_y\n        row_height = 0\n        for item in self._items:\n            item_hint = item.sizeHint()\n            item_width = item_hint.width()\n            item_height = item_hint.height()\n            if item_width &lt; 1 or item_height &lt; 1:\n                continue\n\n            end_x = pos_x + item_width\n\n            wrap = (\n                row_height &gt; 0\n                and (\n                    end_x &gt; rect.right()\n                    or (end_x + r_margin) &gt; rect.right()\n                )\n            )\n            if not wrap:\n                next_pos_x = end_x + h_spacing\n            else:\n                pos_x = left_x\n                pos_y += row_height + v_spacing\n                next_pos_x = pos_x + item_width + h_spacing\n                row_height = 0\n\n            if not only_calculate:\n                item.setGeometry(\n                    QtCore.QRect(pos_x, pos_y, item_width, item_height)\n                )\n\n            pos_x = next_pos_x\n            row_height = max(row_height, item_height)\n\n        height = (pos_y - top_y) + row_height\n        if height &gt; 0:\n            height += b_margin\n        return height\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.FocusDoubleSpinBox","title":"<code>FocusDoubleSpinBox</code>","text":"<p>               Bases: <code>QDoubleSpinBox</code></p> <p>QDoubleSpinBox which allow scroll wheel changes only in active state.</p> Source code in <code>client/ayon_core/tools/utils/widgets.py</code> <pre><code>class FocusDoubleSpinBox(QtWidgets.QDoubleSpinBox):\n    \"\"\"QDoubleSpinBox which allow scroll wheel changes only in active state.\"\"\"\n\n    def __init__(self, *args, **kwargs):\n        super(FocusDoubleSpinBox, self).__init__(*args, **kwargs)\n        self.setFocusPolicy(QtCore.Qt.StrongFocus)\n\n    def wheelEvent(self, event):\n        if not self.hasFocus():\n            event.ignore()\n        else:\n            super(FocusDoubleSpinBox, self).wheelEvent(event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.FocusSpinBox","title":"<code>FocusSpinBox</code>","text":"<p>               Bases: <code>QSpinBox</code></p> <p>QSpinBox which allow scroll wheel changes only in active state.</p> Source code in <code>client/ayon_core/tools/utils/widgets.py</code> <pre><code>class FocusSpinBox(QtWidgets.QSpinBox):\n    \"\"\"QSpinBox which allow scroll wheel changes only in active state.\"\"\"\n\n    def __init__(self, *args, **kwargs):\n        super(FocusSpinBox, self).__init__(*args, **kwargs)\n        self.setFocusPolicy(QtCore.Qt.StrongFocus)\n\n    def wheelEvent(self, event):\n        if not self.hasFocus():\n            event.ignore()\n        else:\n            super(FocusSpinBox, self).wheelEvent(event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.FoldersFiltersWidget","title":"<code>FoldersFiltersWidget</code>","text":"<p>               Bases: <code>QWidget</code></p> <p>Helper widget for most commonly used filters in context selection.</p> Source code in <code>client/ayon_core/tools/utils/folders_widget.py</code> <pre><code>class FoldersFiltersWidget(QtWidgets.QWidget):\n    \"\"\"Helper widget for most commonly used filters in context selection.\"\"\"\n    text_changed = QtCore.Signal(str)\n    my_tasks_changed = QtCore.Signal(bool)\n\n    def __init__(self, parent: QtWidgets.QWidget) -&gt; None:\n        super().__init__(parent)\n\n        folders_filter_input = PlaceholderLineEdit(self)\n        folders_filter_input.setPlaceholderText(\"Folder name filter...\")\n\n        my_tasks_tooltip = (\n            \"Filter folders and task to only those you are assigned to.\"\n        )\n        my_tasks_label = QtWidgets.QLabel(\"My tasks\", self)\n        my_tasks_label.setToolTip(my_tasks_tooltip)\n\n        my_tasks_checkbox = NiceCheckbox(self)\n        my_tasks_checkbox.setChecked(False)\n        my_tasks_checkbox.setToolTip(my_tasks_tooltip)\n\n        layout = QtWidgets.QHBoxLayout(self)\n        layout.setContentsMargins(0, 0, 0, 0)\n        layout.setSpacing(5)\n        layout.addWidget(folders_filter_input, 1)\n        layout.addWidget(my_tasks_label, 0)\n        layout.addWidget(my_tasks_checkbox, 0)\n\n        folders_filter_input.textChanged.connect(self.text_changed)\n        my_tasks_checkbox.stateChanged.connect(self._on_my_tasks_change)\n\n        self._folders_filter_input = folders_filter_input\n        self._my_tasks_checkbox = my_tasks_checkbox\n\n    def is_my_tasks_checked(self) -&gt; bool:\n        return self._my_tasks_checkbox.isChecked()\n\n    def text(self) -&gt; str:\n        return self._folders_filter_input.text()\n\n    def set_text(self, text: str) -&gt; None:\n        self._folders_filter_input.setText(text)\n\n    def set_my_tasks_checked(self, checked: bool) -&gt; None:\n        self._my_tasks_checkbox.setChecked(checked)\n\n    def _on_my_tasks_change(self, _state: int) -&gt; None:\n        self.my_tasks_changed.emit(self._my_tasks_checkbox.isChecked())\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.FoldersQtModel","title":"<code>FoldersQtModel</code>","text":"<p>               Bases: <code>QStandardItemModel</code></p> <p>Folders model which cares about refresh of folders.</p> <p>Parameters:</p> Name Type Description Default <code>controller</code> <code>AbstractWorkfilesFrontend</code> <p>The control object.</p> required Source code in <code>client/ayon_core/tools/utils/folders_widget.py</code> <pre><code>class FoldersQtModel(QtGui.QStandardItemModel):\n    \"\"\"Folders model which cares about refresh of folders.\n\n    Args:\n        controller (AbstractWorkfilesFrontend): The control object.\n\n    \"\"\"\n    _default_folder_icon = None\n    refreshed = QtCore.Signal()\n\n    def __init__(self, controller):\n        super().__init__()\n\n        self.setColumnCount(1)\n        self.setHeaderData(0, QtCore.Qt.Horizontal, \"Folders\")\n\n        self._controller = controller\n        self._items_by_id = {}\n        self._parent_id_by_id = {}\n\n        self._refresh_threads = {}\n        self._current_refresh_thread = None\n        self._last_project_name = None\n\n        self._has_content = False\n        self._is_refreshing = False\n\n    @property\n    def is_refreshing(self):\n        \"\"\"Model is refreshing.\n\n        Returns:\n            bool: True if model is refreshing.\n        \"\"\"\n        return self._is_refreshing\n\n    @property\n    def has_content(self):\n        \"\"\"Has at least one folder.\n\n        Returns:\n            bool: True if model has at least one folder.\n        \"\"\"\n\n        return self._has_content\n\n    def refresh(self):\n        \"\"\"Refresh folders for last selected project.\n\n        Force to update folders model from controller. This may or may not\n        trigger query from server, that's based on controller's cache.\n        \"\"\"\n\n        self.set_project_name(self._last_project_name)\n\n    def get_index_by_id(self, item_id):\n        \"\"\"Get index by folder id.\n\n        Returns:\n            QtCore.QModelIndex: Index of the folder. Can be invalid if folder\n                is not available.\n        \"\"\"\n        item = self._items_by_id.get(item_id)\n        if item is None:\n            return QtCore.QModelIndex()\n        return self.indexFromItem(item)\n\n    def get_item_id_by_path(self, folder_path):\n        \"\"\"Get folder id by path.\n\n        Args:\n            folder_path (str): Folder path.\n\n        Returns:\n            Union[str, None]: Folder id or None if folder is not available.\n\n        \"\"\"\n        for folder_id, item in self._items_by_id.items():\n            if item.data(FOLDER_PATH_ROLE) == folder_path:\n                return folder_id\n        return None\n\n    def get_project_name(self):\n        \"\"\"Project name which model currently use.\n\n        Returns:\n            Union[str, None]: Currently used project name.\n        \"\"\"\n\n        return self._last_project_name\n\n    def set_project_name(self, project_name):\n        \"\"\"Refresh folders items.\n\n        Refresh start thread because it can cause that controller can\n        start query from database if folders are not cached.\n        \"\"\"\n\n        if not project_name:\n            self._last_project_name = project_name\n            self._fill_items({}, {})\n            self._current_refresh_thread = None\n            return\n\n        self._is_refreshing = True\n\n        if self._last_project_name != project_name:\n            self._clear_items()\n        self._last_project_name = project_name\n\n        thread = self._refresh_threads.get(project_name)\n        if thread is not None:\n            self._current_refresh_thread = thread\n            return\n\n        thread = RefreshThread(\n            project_name,\n            self._thread_getter,\n            project_name\n        )\n        self._current_refresh_thread = thread\n        self._refresh_threads[thread.id] = thread\n        thread.refresh_finished.connect(self._on_refresh_thread)\n        thread.start()\n\n    @classmethod\n    def _get_default_folder_icon(cls):\n        if cls._default_folder_icon is None:\n            cls._default_folder_icon = get_qt_icon(\n                AwesomeFontIcon(\n                    \"fa.folder\",\n                    color=get_default_entity_icon_color(),\n                )\n            )\n        return cls._default_folder_icon\n\n    def _clear_items(self):\n        self._items_by_id = {}\n        self._parent_id_by_id = {}\n        self._has_content = False\n        root_item = self.invisibleRootItem()\n        root_item.removeRows(0, root_item.rowCount())\n\n    def _thread_getter(self, project_name):\n        folder_items = self._controller.get_folder_items(\n            project_name, FOLDERS_MODEL_SENDER_NAME\n        )\n        folder_type_items = []\n        if hasattr(self._controller, \"get_folder_type_items\"):\n            folder_type_items = self._controller.get_folder_type_items(\n                project_name, FOLDERS_MODEL_SENDER_NAME\n            )\n        return folder_items, folder_type_items\n\n    def _on_refresh_thread(self, thread_id):\n        \"\"\"Callback when refresh thread is finished.\n\n        Technically can be running multiple refresh threads at the same time,\n        to avoid using values from wrong thread, we check if thread id is\n        current refresh thread id.\n\n        Folders are stored by id.\n\n        Args:\n            thread_id (str): Thread id.\n        \"\"\"\n\n        # Make sure to remove thread from '_refresh_threads' dict\n        thread = self._refresh_threads.pop(thread_id)\n        if (\n            self._current_refresh_thread is None\n            or thread_id != self._current_refresh_thread.id\n        ):\n            return\n        if thread.failed:\n            # TODO visualize that refresh failed\n            folder_items, folder_type_items = {}, []\n        else:\n            folder_items, folder_type_items = thread.get_result()\n        self._fill_items(folder_items, folder_type_items)\n        self._current_refresh_thread = None\n\n    def _get_folder_item_icon(\n        self,\n        folder_item,\n        folder_type_item_by_name,\n        folder_type_icon_cache\n    ):\n        icon = folder_type_icon_cache.get(folder_item.folder_type)\n        if icon is not None:\n            return icon\n\n        folder_type_item = folder_type_item_by_name.get(\n            folder_item.folder_type\n        )\n        icon = None\n        if folder_type_item is not None:\n            icon = get_qt_icon(MaterialSymbolsIcon(\n                folder_type_item.icon,\n                color=get_default_entity_icon_color(),\n            ))\n\n        if icon is None:\n            icon = self._get_default_folder_icon()\n        folder_type_icon_cache[folder_item.folder_type] = icon\n        return icon\n\n    def _fill_item_data(\n        self,\n        item,\n        folder_item,\n        folder_type_item_by_name,\n        folder_type_icon_cache\n    ):\n        \"\"\"\n\n        Args:\n            item (QtGui.QStandardItem): Item to fill data.\n            folder_item (FolderItem): Folder item.\n\n        \"\"\"\n        icon = self._get_folder_item_icon(\n            folder_item,\n            folder_type_item_by_name,\n            folder_type_icon_cache\n        )\n        item.setData(folder_item.entity_id, FOLDER_ID_ROLE)\n        item.setData(folder_item.name, FOLDER_NAME_ROLE)\n        item.setData(folder_item.path, FOLDER_PATH_ROLE)\n        item.setData(folder_item.folder_type, FOLDER_TYPE_ROLE)\n        item.setData(folder_item.label, QtCore.Qt.DisplayRole)\n        item.setData(icon, QtCore.Qt.DecorationRole)\n\n    def _fill_items(self, folder_items_by_id, folder_type_items):\n        if not folder_items_by_id:\n            if folder_items_by_id is not None:\n                self._clear_items()\n            self._is_refreshing = False\n            self.refreshed.emit()\n            return\n\n        folder_type_item_by_name = {\n            folder_type.name: folder_type\n            for folder_type in folder_type_items\n        }\n        folder_type_icon_cache = {}\n        self._has_content = True\n\n        folder_ids = set(folder_items_by_id)\n        ids_to_remove = set(self._items_by_id) - folder_ids\n\n        folder_items_by_parent = collections.defaultdict(dict)\n        for folder_item in folder_items_by_id.values():\n            (\n                folder_items_by_parent\n                [folder_item.parent_id]\n                [folder_item.entity_id]\n            ) = folder_item\n\n        hierarchy_queue = collections.deque()\n        hierarchy_queue.append((self.invisibleRootItem(), None))\n\n        # Keep pointers to removed items until the refresh finishes\n        #   - some children of the items could be moved and reused elsewhere\n        removed_items = []\n        while hierarchy_queue:\n            item = hierarchy_queue.popleft()\n            parent_item, parent_id = item\n            folder_items = folder_items_by_parent[parent_id]\n\n            items_by_id = {}\n            folder_ids_to_add = set(folder_items)\n            for row_idx in reversed(range(parent_item.rowCount())):\n                child_item = parent_item.child(row_idx)\n                child_id = child_item.data(FOLDER_ID_ROLE)\n                if child_id in ids_to_remove:\n                    removed_items.append(parent_item.takeRow(row_idx))\n                else:\n                    items_by_id[child_id] = child_item\n\n            new_items = []\n            for item_id in folder_ids_to_add:\n                folder_item = folder_items[item_id]\n                item = items_by_id.get(item_id)\n                if item is None:\n                    is_new = True\n                    item = QtGui.QStandardItem()\n                    item.setEditable(False)\n                else:\n                    is_new = self._parent_id_by_id[item_id] != parent_id\n\n                self._fill_item_data(\n                    item,\n                    folder_item,\n                    folder_type_item_by_name,\n                    folder_type_icon_cache\n                )\n                if is_new:\n                    new_items.append(item)\n                self._items_by_id[item_id] = item\n                self._parent_id_by_id[item_id] = parent_id\n\n                hierarchy_queue.append((item, item_id))\n\n            if new_items:\n                parent_item.appendRows(new_items)\n\n        for item_id in ids_to_remove:\n            self._items_by_id.pop(item_id)\n            self._parent_id_by_id.pop(item_id)\n\n        self._is_refreshing = False\n        self.refreshed.emit()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.FoldersQtModel.has_content","title":"<code>has_content</code>  <code>property</code>","text":"<p>Has at least one folder.</p> <p>Returns:</p> Name Type Description <code>bool</code> <p>True if model has at least one folder.</p>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.FoldersQtModel.is_refreshing","title":"<code>is_refreshing</code>  <code>property</code>","text":"<p>Model is refreshing.</p> <p>Returns:</p> Name Type Description <code>bool</code> <p>True if model is refreshing.</p>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.FoldersQtModel.get_index_by_id","title":"<code>get_index_by_id(item_id)</code>","text":"<p>Get index by folder id.</p> <p>Returns:</p> Type Description <p>QtCore.QModelIndex: Index of the folder. Can be invalid if folder is not available.</p> Source code in <code>client/ayon_core/tools/utils/folders_widget.py</code> <pre><code>def get_index_by_id(self, item_id):\n    \"\"\"Get index by folder id.\n\n    Returns:\n        QtCore.QModelIndex: Index of the folder. Can be invalid if folder\n            is not available.\n    \"\"\"\n    item = self._items_by_id.get(item_id)\n    if item is None:\n        return QtCore.QModelIndex()\n    return self.indexFromItem(item)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.FoldersQtModel.get_item_id_by_path","title":"<code>get_item_id_by_path(folder_path)</code>","text":"<p>Get folder id by path.</p> <p>Parameters:</p> Name Type Description Default <code>folder_path</code> <code>str</code> <p>Folder path.</p> required <p>Returns:</p> Type Description <p>Union[str, None]: Folder id or None if folder is not available.</p> Source code in <code>client/ayon_core/tools/utils/folders_widget.py</code> <pre><code>def get_item_id_by_path(self, folder_path):\n    \"\"\"Get folder id by path.\n\n    Args:\n        folder_path (str): Folder path.\n\n    Returns:\n        Union[str, None]: Folder id or None if folder is not available.\n\n    \"\"\"\n    for folder_id, item in self._items_by_id.items():\n        if item.data(FOLDER_PATH_ROLE) == folder_path:\n            return folder_id\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.FoldersQtModel.get_project_name","title":"<code>get_project_name()</code>","text":"<p>Project name which model currently use.</p> <p>Returns:</p> Type Description <p>Union[str, None]: Currently used project name.</p> Source code in <code>client/ayon_core/tools/utils/folders_widget.py</code> <pre><code>def get_project_name(self):\n    \"\"\"Project name which model currently use.\n\n    Returns:\n        Union[str, None]: Currently used project name.\n    \"\"\"\n\n    return self._last_project_name\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.FoldersQtModel.refresh","title":"<code>refresh()</code>","text":"<p>Refresh folders for last selected project.</p> <p>Force to update folders model from controller. This may or may not trigger query from server, that's based on controller's cache.</p> Source code in <code>client/ayon_core/tools/utils/folders_widget.py</code> <pre><code>def refresh(self):\n    \"\"\"Refresh folders for last selected project.\n\n    Force to update folders model from controller. This may or may not\n    trigger query from server, that's based on controller's cache.\n    \"\"\"\n\n    self.set_project_name(self._last_project_name)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.FoldersQtModel.set_project_name","title":"<code>set_project_name(project_name)</code>","text":"<p>Refresh folders items.</p> <p>Refresh start thread because it can cause that controller can start query from database if folders are not cached.</p> Source code in <code>client/ayon_core/tools/utils/folders_widget.py</code> <pre><code>def set_project_name(self, project_name):\n    \"\"\"Refresh folders items.\n\n    Refresh start thread because it can cause that controller can\n    start query from database if folders are not cached.\n    \"\"\"\n\n    if not project_name:\n        self._last_project_name = project_name\n        self._fill_items({}, {})\n        self._current_refresh_thread = None\n        return\n\n    self._is_refreshing = True\n\n    if self._last_project_name != project_name:\n        self._clear_items()\n    self._last_project_name = project_name\n\n    thread = self._refresh_threads.get(project_name)\n    if thread is not None:\n        self._current_refresh_thread = thread\n        return\n\n    thread = RefreshThread(\n        project_name,\n        self._thread_getter,\n        project_name\n    )\n    self._current_refresh_thread = thread\n    self._refresh_threads[thread.id] = thread\n    thread.refresh_finished.connect(self._on_refresh_thread)\n    thread.start()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.FoldersWidget","title":"<code>FoldersWidget</code>","text":"<p>               Bases: <code>QWidget</code></p> <p>Folders widget.</p> <p>Widget that handles folders view, model and selection.</p> <p>Expected selection handling is disabled by default. If enabled, the widget will handle the expected in predefined way. Widget is listening to event 'expected_selection_changed' with expected event data below, the same data must be available when called method 'get_expected_selection_data' on controller.</p> <p>{     \"folder\": {         \"current\": bool,               # Folder is what should be set now         \"folder_id\": Union[str, None], # Folder id that should be selected     },     ... }</p> <p>Selection is confirmed by calling method 'expected_folder_selected' on controller.</p> <p>Parameters:</p> Name Type Description Default <code>controller</code> <code>AbstractWorkfilesFrontend</code> <p>The control object.</p> required <code>parent</code> <code>QWidget</code> <p>The parent widget.</p> required <code>handle_expected_selection</code> <code>bool</code> <p>If True, the widget will handle the expected selection. Defaults to False.</p> <code>False</code> Source code in <code>client/ayon_core/tools/utils/folders_widget.py</code> <pre><code>class FoldersWidget(QtWidgets.QWidget):\n    \"\"\"Folders widget.\n\n    Widget that handles folders view, model and selection.\n\n    Expected selection handling is disabled by default. If enabled, the\n    widget will handle the expected in predefined way. Widget is listening\n    to event 'expected_selection_changed' with expected event data below,\n    the same data must be available when called method\n    'get_expected_selection_data' on controller.\n\n    {\n        \"folder\": {\n            \"current\": bool,               # Folder is what should be set now\n            \"folder_id\": Union[str, None], # Folder id that should be selected\n        },\n        ...\n    }\n\n    Selection is confirmed by calling method 'expected_folder_selected' on\n    controller.\n\n\n    Args:\n        controller (AbstractWorkfilesFrontend): The control object.\n        parent (QtWidgets.QWidget): The parent widget.\n        handle_expected_selection (bool): If True, the widget will handle\n            the expected selection. Defaults to False.\n    \"\"\"\n\n    double_clicked = QtCore.Signal(QtGui.QMouseEvent)\n    selection_changed = QtCore.Signal()\n    refreshed = QtCore.Signal()\n\n    def __init__(self, controller, parent, handle_expected_selection=False):\n        super().__init__(parent)\n\n        folders_view = TreeView(self)\n        folders_view.setHeaderHidden(True)\n\n        folders_model = FoldersQtModel(controller)\n        folders_proxy_model = FoldersProxyModel()\n        folders_proxy_model.setSourceModel(folders_model)\n        folders_proxy_model.setSortCaseSensitivity(QtCore.Qt.CaseInsensitive)\n\n        folders_view.setModel(folders_proxy_model)\n\n        main_layout = QtWidgets.QHBoxLayout(self)\n        main_layout.setContentsMargins(0, 0, 0, 0)\n        main_layout.addWidget(folders_view, 1)\n\n        controller.register_event_callback(\n            \"selection.project.changed\",\n            self._on_project_selection_change,\n        )\n        controller.register_event_callback(\n            \"folders.refresh.finished\",\n            self._on_folders_refresh_finished\n        )\n        controller.register_event_callback(\n            \"controller.refresh.finished\",\n            self._on_controller_refresh\n        )\n        controller.register_event_callback(\n            \"expected_selection_changed\",\n            self._on_expected_selection_change\n        )\n\n        selection_model = folders_view.selectionModel()\n        selection_model.selectionChanged.connect(self._on_selection_change)\n        folders_view.double_clicked.connect(self.double_clicked)\n        folders_model.refreshed.connect(self._on_model_refresh)\n\n        self._controller = controller\n        self._folders_view = folders_view\n        self._folders_model = folders_model\n        self._folders_proxy_model = folders_proxy_model\n\n        self._handle_expected_selection = handle_expected_selection\n        self._expected_selection = None\n\n    @property\n    def is_refreshing(self):\n        \"\"\"Model is refreshing.\n\n        Returns:\n            bool: True if model is refreshing.\n        \"\"\"\n\n        return self._folders_model.is_refreshing\n\n    @property\n    def has_content(self):\n        \"\"\"Has at least one folder.\n\n        Returns:\n            bool: True if model has at least one folder.\n        \"\"\"\n\n        return self._folders_model.has_content\n\n    def set_name_filter(self, name):\n        \"\"\"Set filter of folder name.\n\n        Args:\n            name (str): The string filter.\n        \"\"\"\n\n        self._folders_proxy_model.setFilterFixedString(name)\n        if name:\n            self._folders_view.expandAll()\n\n    def set_folder_ids_filter(self, folder_ids: Optional[list[str]]):\n        \"\"\"Set filter of folder ids.\n\n        Args:\n            folder_ids (list[str]): The list of folder ids.\n\n        \"\"\"\n        self._folders_proxy_model.set_folder_ids_filter(folder_ids)\n\n    def set_header_visible(self, visible: bool):\n        self._folders_view.setHeaderHidden(not visible)\n\n    def refresh(self):\n        \"\"\"Refresh folders model.\n\n        Force to update folders model from controller.\n        \"\"\"\n\n        self._folders_model.refresh()\n\n    def get_project_name(self):\n        \"\"\"Project name in which folders widget currently is.\n\n        Returns:\n            Union[str, None]: Currently used project name.\n        \"\"\"\n\n        return self._folders_model.get_project_name()\n\n    def set_project_name(self, project_name):\n        \"\"\"Set project name.\n\n        Do not use this method when controller is handling selection of\n        project using 'selection.project.changed' event.\n\n        Args:\n            project_name (str): Project name.\n        \"\"\"\n\n        self._folders_model.set_project_name(project_name)\n\n    def get_selected_folder_id(self):\n        \"\"\"Get selected folder id.\n\n        Returns:\n            Union[str, None]: Folder id which is selected.\n        \"\"\"\n\n        return self._get_selected_item_id()\n\n    def get_selected_folder_path(self):\n        \"\"\"Get selected folder id.\n\n        Returns:\n            Union[str, None]: Folder path which is selected.\n        \"\"\"\n\n        return self._get_selected_item_value(FOLDER_PATH_ROLE)\n\n    def get_selected_folder_label(self):\n        \"\"\"Selected folder label.\n\n        Returns:\n            Union[str, None]: Selected folder label.\n        \"\"\"\n\n        item_id = self._get_selected_item_id()\n        return self.get_folder_label(item_id)\n\n    def get_folder_label(self, folder_id):\n        \"\"\"Folder label for a given folder id.\n\n        Returns:\n            Union[str, None]: Folder label.\n        \"\"\"\n\n        index = self._folders_model.get_index_by_id(folder_id)\n        if index.isValid():\n            return index.data(QtCore.Qt.DisplayRole)\n        return None\n\n    def set_selected_folder(self, folder_id):\n        \"\"\"Change selection.\n\n        Args:\n            folder_id (Union[str, None]): Folder id or None to deselect.\n\n        Returns:\n            bool: Requested folder was selected.\n        \"\"\"\n        if folder_id is None:\n            self._folders_view.clearSelection()\n            return True\n\n        if folder_id == self._get_selected_item_id():\n            return True\n        index = self._folders_model.get_index_by_id(folder_id)\n        if not index.isValid():\n            return False\n\n        proxy_index = self._folders_proxy_model.mapFromSource(index)\n        if not proxy_index.isValid():\n            return False\n\n        selection_model = self._folders_view.selectionModel()\n        selection_model.setCurrentIndex(\n            proxy_index, QtCore.QItemSelectionModel.SelectCurrent\n        )\n        return True\n\n    def set_selected_folder_path(self, folder_path):\n        \"\"\"Set selected folder by path.\n\n        Args:\n            folder_path (str): Folder path.\n\n        Returns:\n            bool: Requested folder was selected.\n\n        \"\"\"\n        if folder_path is None:\n            self._folders_view.clearSelection()\n            return True\n\n        folder_id = self._folders_model.get_item_id_by_path(folder_path)\n        if folder_id is None:\n            return False\n        return self.set_selected_folder(folder_id)\n\n    def set_deselectable(self, enabled):\n        \"\"\"Set deselectable mode.\n\n        Items in view can be deselected.\n\n        Args:\n            enabled (bool): Enable deselectable mode.\n        \"\"\"\n\n        self._folders_view.set_deselectable(enabled)\n\n    def _get_selected_index(self):\n        return self._folders_model.get_index_by_id(\n            self.get_selected_folder_id()\n        )\n\n    def _on_project_selection_change(self, event):\n        project_name = event[\"project_name\"]\n        self.set_project_name(project_name)\n\n    def _on_folders_refresh_finished(self, event):\n        if event[\"sender\"] != FOLDERS_MODEL_SENDER_NAME:\n            self.set_project_name(event[\"project_name\"])\n\n    def _on_controller_refresh(self):\n        self._update_expected_selection()\n\n    def _on_model_refresh(self):\n        if self._expected_selection:\n            self._set_expected_selection()\n        self._folders_proxy_model.sort(0)\n        self.refreshed.emit()\n\n    def _get_selected_item_id(self):\n        return self._get_selected_item_value(FOLDER_ID_ROLE)\n\n    def _get_selected_item_value(self, role):\n        selection_model = self._folders_view.selectionModel()\n        for index in selection_model.selectedIndexes():\n            item_id = index.data(role)\n            if item_id is not None:\n                return item_id\n        return None\n\n    def _on_selection_change(self):\n        item_id = self._get_selected_item_id()\n        self._controller.set_selected_folder(item_id)\n        self.selection_changed.emit()\n\n    # Expected selection handling\n    def _on_expected_selection_change(self, event):\n        self._update_expected_selection(event.data)\n\n    def _update_expected_selection(self, expected_data=None):\n        if not self._handle_expected_selection:\n            return\n\n        if expected_data is None:\n            expected_data = self._controller.get_expected_selection_data()\n\n        folder_data = expected_data.get(\"folder\")\n        if not folder_data or not folder_data[\"current\"]:\n            return\n\n        folder_id = folder_data[\"id\"]\n        self._expected_selection = folder_id\n        if not self._folders_model.is_refreshing:\n            self._set_expected_selection()\n\n    def _set_expected_selection(self):\n        if not self._handle_expected_selection:\n            return\n\n        folder_id = self._expected_selection\n        self._expected_selection = None\n        if folder_id is not None:\n            self.set_selected_folder(folder_id)\n        self._controller.expected_folder_selected(folder_id)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.FoldersWidget.has_content","title":"<code>has_content</code>  <code>property</code>","text":"<p>Has at least one folder.</p> <p>Returns:</p> Name Type Description <code>bool</code> <p>True if model has at least one folder.</p>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.FoldersWidget.is_refreshing","title":"<code>is_refreshing</code>  <code>property</code>","text":"<p>Model is refreshing.</p> <p>Returns:</p> Name Type Description <code>bool</code> <p>True if model is refreshing.</p>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.FoldersWidget.get_folder_label","title":"<code>get_folder_label(folder_id)</code>","text":"<p>Folder label for a given folder id.</p> <p>Returns:</p> Type Description <p>Union[str, None]: Folder label.</p> Source code in <code>client/ayon_core/tools/utils/folders_widget.py</code> <pre><code>def get_folder_label(self, folder_id):\n    \"\"\"Folder label for a given folder id.\n\n    Returns:\n        Union[str, None]: Folder label.\n    \"\"\"\n\n    index = self._folders_model.get_index_by_id(folder_id)\n    if index.isValid():\n        return index.data(QtCore.Qt.DisplayRole)\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.FoldersWidget.get_project_name","title":"<code>get_project_name()</code>","text":"<p>Project name in which folders widget currently is.</p> <p>Returns:</p> Type Description <p>Union[str, None]: Currently used project name.</p> Source code in <code>client/ayon_core/tools/utils/folders_widget.py</code> <pre><code>def get_project_name(self):\n    \"\"\"Project name in which folders widget currently is.\n\n    Returns:\n        Union[str, None]: Currently used project name.\n    \"\"\"\n\n    return self._folders_model.get_project_name()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.FoldersWidget.get_selected_folder_id","title":"<code>get_selected_folder_id()</code>","text":"<p>Get selected folder id.</p> <p>Returns:</p> Type Description <p>Union[str, None]: Folder id which is selected.</p> Source code in <code>client/ayon_core/tools/utils/folders_widget.py</code> <pre><code>def get_selected_folder_id(self):\n    \"\"\"Get selected folder id.\n\n    Returns:\n        Union[str, None]: Folder id which is selected.\n    \"\"\"\n\n    return self._get_selected_item_id()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.FoldersWidget.get_selected_folder_label","title":"<code>get_selected_folder_label()</code>","text":"<p>Selected folder label.</p> <p>Returns:</p> Type Description <p>Union[str, None]: Selected folder label.</p> Source code in <code>client/ayon_core/tools/utils/folders_widget.py</code> <pre><code>def get_selected_folder_label(self):\n    \"\"\"Selected folder label.\n\n    Returns:\n        Union[str, None]: Selected folder label.\n    \"\"\"\n\n    item_id = self._get_selected_item_id()\n    return self.get_folder_label(item_id)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.FoldersWidget.get_selected_folder_path","title":"<code>get_selected_folder_path()</code>","text":"<p>Get selected folder id.</p> <p>Returns:</p> Type Description <p>Union[str, None]: Folder path which is selected.</p> Source code in <code>client/ayon_core/tools/utils/folders_widget.py</code> <pre><code>def get_selected_folder_path(self):\n    \"\"\"Get selected folder id.\n\n    Returns:\n        Union[str, None]: Folder path which is selected.\n    \"\"\"\n\n    return self._get_selected_item_value(FOLDER_PATH_ROLE)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.FoldersWidget.refresh","title":"<code>refresh()</code>","text":"<p>Refresh folders model.</p> <p>Force to update folders model from controller.</p> Source code in <code>client/ayon_core/tools/utils/folders_widget.py</code> <pre><code>def refresh(self):\n    \"\"\"Refresh folders model.\n\n    Force to update folders model from controller.\n    \"\"\"\n\n    self._folders_model.refresh()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.FoldersWidget.set_deselectable","title":"<code>set_deselectable(enabled)</code>","text":"<p>Set deselectable mode.</p> <p>Items in view can be deselected.</p> <p>Parameters:</p> Name Type Description Default <code>enabled</code> <code>bool</code> <p>Enable deselectable mode.</p> required Source code in <code>client/ayon_core/tools/utils/folders_widget.py</code> <pre><code>def set_deselectable(self, enabled):\n    \"\"\"Set deselectable mode.\n\n    Items in view can be deselected.\n\n    Args:\n        enabled (bool): Enable deselectable mode.\n    \"\"\"\n\n    self._folders_view.set_deselectable(enabled)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.FoldersWidget.set_folder_ids_filter","title":"<code>set_folder_ids_filter(folder_ids)</code>","text":"<p>Set filter of folder ids.</p> <p>Parameters:</p> Name Type Description Default <code>folder_ids</code> <code>list[str]</code> <p>The list of folder ids.</p> required Source code in <code>client/ayon_core/tools/utils/folders_widget.py</code> <pre><code>def set_folder_ids_filter(self, folder_ids: Optional[list[str]]):\n    \"\"\"Set filter of folder ids.\n\n    Args:\n        folder_ids (list[str]): The list of folder ids.\n\n    \"\"\"\n    self._folders_proxy_model.set_folder_ids_filter(folder_ids)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.FoldersWidget.set_name_filter","title":"<code>set_name_filter(name)</code>","text":"<p>Set filter of folder name.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The string filter.</p> required Source code in <code>client/ayon_core/tools/utils/folders_widget.py</code> <pre><code>def set_name_filter(self, name):\n    \"\"\"Set filter of folder name.\n\n    Args:\n        name (str): The string filter.\n    \"\"\"\n\n    self._folders_proxy_model.setFilterFixedString(name)\n    if name:\n        self._folders_view.expandAll()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.FoldersWidget.set_project_name","title":"<code>set_project_name(project_name)</code>","text":"<p>Set project name.</p> <p>Do not use this method when controller is handling selection of project using 'selection.project.changed' event.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required Source code in <code>client/ayon_core/tools/utils/folders_widget.py</code> <pre><code>def set_project_name(self, project_name):\n    \"\"\"Set project name.\n\n    Do not use this method when controller is handling selection of\n    project using 'selection.project.changed' event.\n\n    Args:\n        project_name (str): Project name.\n    \"\"\"\n\n    self._folders_model.set_project_name(project_name)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.FoldersWidget.set_selected_folder","title":"<code>set_selected_folder(folder_id)</code>","text":"<p>Change selection.</p> <p>Parameters:</p> Name Type Description Default <code>folder_id</code> <code>Union[str, None]</code> <p>Folder id or None to deselect.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <p>Requested folder was selected.</p> Source code in <code>client/ayon_core/tools/utils/folders_widget.py</code> <pre><code>def set_selected_folder(self, folder_id):\n    \"\"\"Change selection.\n\n    Args:\n        folder_id (Union[str, None]): Folder id or None to deselect.\n\n    Returns:\n        bool: Requested folder was selected.\n    \"\"\"\n    if folder_id is None:\n        self._folders_view.clearSelection()\n        return True\n\n    if folder_id == self._get_selected_item_id():\n        return True\n    index = self._folders_model.get_index_by_id(folder_id)\n    if not index.isValid():\n        return False\n\n    proxy_index = self._folders_proxy_model.mapFromSource(index)\n    if not proxy_index.isValid():\n        return False\n\n    selection_model = self._folders_view.selectionModel()\n    selection_model.setCurrentIndex(\n        proxy_index, QtCore.QItemSelectionModel.SelectCurrent\n    )\n    return True\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.FoldersWidget.set_selected_folder_path","title":"<code>set_selected_folder_path(folder_path)</code>","text":"<p>Set selected folder by path.</p> <p>Parameters:</p> Name Type Description Default <code>folder_path</code> <code>str</code> <p>Folder path.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <p>Requested folder was selected.</p> Source code in <code>client/ayon_core/tools/utils/folders_widget.py</code> <pre><code>def set_selected_folder_path(self, folder_path):\n    \"\"\"Set selected folder by path.\n\n    Args:\n        folder_path (str): Folder path.\n\n    Returns:\n        bool: Requested folder was selected.\n\n    \"\"\"\n    if folder_path is None:\n        self._folders_view.clearSelection()\n        return True\n\n    folder_id = self._folders_model.get_item_id_by_path(folder_path)\n    if folder_id is None:\n        return False\n    return self.set_selected_folder(folder_id)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.IconButton","title":"<code>IconButton</code>","text":"<p>               Bases: <code>QPushButton</code></p> <p>PushButton with icon and size of font.</p> <p>Using font metrics height as icon size reference.</p> Source code in <code>client/ayon_core/tools/utils/widgets.py</code> <pre><code>class IconButton(QtWidgets.QPushButton):\n    \"\"\"PushButton with icon and size of font.\n\n    Using font metrics height as icon size reference.\n    \"\"\"\n\n    def __init__(self, *args, **kwargs):\n        super(IconButton, self).__init__(*args, **kwargs)\n        self.setObjectName(\"IconButton\")\n\n    def sizeHint(self):\n        result = super(IconButton, self).sizeHint()\n        icon_h = self.iconSize().height()\n        font_height = self.fontMetrics().height()\n        text_set = bool(self.text())\n        if not text_set and icon_h &lt; font_height:\n            new_size = result.height() - icon_h + font_height\n            result.setHeight(new_size)\n            result.setWidth(new_size)\n\n        return result\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.ListView","title":"<code>ListView</code>","text":"<p>               Bases: <code>QListView</code></p> <p>A tree view that deselects on clicking on an empty area in the view</p> Source code in <code>client/ayon_core/tools/utils/views.py</code> <pre><code>class ListView(QtWidgets.QListView):\n    \"\"\"A tree view that deselects on clicking on an empty area in the view\"\"\"\n    double_clicked = QtCore.Signal(QtGui.QMouseEvent)\n\n    def __init__(self, *args, **kwargs):\n        super().__init__(*args, **kwargs)\n        self._deselectable = False\n\n        self._flick_charm_activated = False\n        self._flick_charm = FlickCharm(parent=self)\n        self._before_flick_scroll_mode = None\n\n    def is_deselectable(self):\n        return self._deselectable\n\n    def set_deselectable(self, deselectable):\n        self._deselectable = deselectable\n\n    deselectable = property(is_deselectable, set_deselectable)\n\n    def mousePressEvent(self, event):\n        if self._deselectable:\n            index = self.indexAt(event.pos())\n            if not index.isValid():\n                # clear the selection\n                self.clearSelection()\n                # clear the current index\n                self.setCurrentIndex(QtCore.QModelIndex())\n        super().mousePressEvent(event)\n\n    def mouseDoubleClickEvent(self, event):\n        self.double_clicked.emit(event)\n\n        return super().mouseDoubleClickEvent(event)\n\n    def activate_flick_charm(self):\n        if self._flick_charm_activated:\n            return\n        self._flick_charm_activated = True\n        self._before_flick_scroll_mode = self.verticalScrollMode()\n        self._flick_charm.activateOn(self)\n        self.setVerticalScrollMode(QtWidgets.QAbstractItemView.ScrollPerPixel)\n\n    def deactivate_flick_charm(self):\n        if not self._flick_charm_activated:\n            return\n        self._flick_charm_activated = False\n        self._flick_charm.deactivateFrom(self)\n        if self._before_flick_scroll_mode is not None:\n            self.setVerticalScrollMode(self._before_flick_scroll_mode)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.MessageOverlayObject","title":"<code>MessageOverlayObject</code>","text":"<p>               Bases: <code>QObject</code></p> <p>Object that can be used to add overlay messages.</p> <p>Parameters:</p> Name Type Description Default <code>widget</code> <code>QWidget</code> required Source code in <code>client/ayon_core/tools/utils/overlay_messages.py</code> <pre><code>class MessageOverlayObject(QtCore.QObject):\n    \"\"\"Object that can be used to add overlay messages.\n\n    Args:\n        widget (QWidget):\n    \"\"\"\n\n    def __init__(self, widget, default_timeout=None):\n        super(MessageOverlayObject, self).__init__()\n\n        widget.installEventFilter(self)\n\n        # Timer which triggers recalculation of message positions\n        recalculate_timer = QtCore.QTimer()\n        recalculate_timer.setInterval(10)\n\n        recalculate_timer.timeout.connect(self._recalculate_positions)\n\n        self._widget = widget\n        self._recalculate_timer = recalculate_timer\n\n        self._messages_order = []\n        self._closing_messages = set()\n        self._messages = {}\n        self._spacing = 5\n        self._move_size = 4\n        self._move_size_remove = 8\n        self._default_timeout = default_timeout\n\n    def add_message(\n        self, message, message_type=None, timeout=None, message_id=None\n    ):\n        \"\"\"Add single message into overlay.\n\n        Args:\n            message (str): Message that will be shown.\n            timeout (int): Message timeout.\n            message_type (str): Message type can be used as property in\n                stylesheets.\n            message_id (str): UUID of already existing message to update\n                it's message and timeout. Is created with different id if is\n                not available anymore.\n\n        Returns:\n            str: UUID of message which can be used to update message.\n        \"\"\"\n        # Skip empty messages\n        if not message:\n            return\n\n        if timeout is None:\n            timeout = self._default_timeout\n\n        # Create unique id of message\n        widget = None\n        if message_id is not None:\n            widget = self._messages.get(message_id)\n\n        if message_id is None:\n            message_id = str(uuid.uuid4())\n\n        elif message_id in self._messages_order:\n            self._messages_order.remove(message_id)\n\n        if widget is not None:\n            # NOTE: Update of message won't change paint order which should be\n            #   ok in most of cases as it matters only when messages are\n            #   animated\n            widget.update_message(message, message_type, timeout)\n        else:\n            # Create message widget\n            widget = OverlayMessageWidget(\n                message_id, message, self._widget, message_type, timeout\n            )\n            widget.close_requested.connect(self._on_message_close_request)\n            widget.show()\n\n        # Move widget outside of window\n        pos = widget.pos()\n        pos.setY(pos.y() - widget.height())\n        widget.move(pos)\n        # Store message\n        self._messages[message_id] = widget\n        self._messages_order.append(message_id)\n        # Trigger recalculation timer\n        self._recalculate_timer.start()\n\n        return message_id\n\n    def _on_message_close_request(self, message_id):\n        \"\"\"Message widget requested removement.\"\"\"\n\n        widget = self._messages.get(message_id)\n        if widget is not None:\n            # Add message to closing messages and start recalculation\n            self._closing_messages.add(message_id)\n            self._recalculate_timer.start()\n\n    def _recalculate_positions(self):\n        \"\"\"Recalculate positions of widgets.\"\"\"\n\n        # Skip if there are no messages to process\n        if not self._messages_order:\n            self._recalculate_timer.stop()\n            return\n\n        # All message widgets are in expected positions\n        all_at_place = True\n        # Starting y position\n        pos_y = self._spacing\n        # Current widget width\n        widget_width = self._widget.width()\n        max_width = widget_width - (2 * self._spacing)\n        widget_half_width = widget_width / 2\n\n        # Store message ids that should be removed\n        message_ids_to_remove = set()\n        for message_id in reversed(self._messages_order):\n            widget = self._messages[message_id]\n            pos = widget.pos()\n            # Messages to remove are moved upwards\n            if message_id in self._closing_messages:\n                bottom = pos.y() + widget.height()\n                # Add message to remove if is not visible\n                if bottom &lt; 0 or self._move_size_remove &lt; 1:\n                    message_ids_to_remove.add(message_id)\n                    continue\n\n                # Calculate new y position of message\n                dst_pos_y = pos.y() - self._move_size_remove\n\n            else:\n                # Calculate y position of message\n                # - use y position of previous message widget and add\n                #   move size if is not in final destination yet\n                if widget.underMouse():\n                    dst_pos_y = pos.y()\n                elif pos.y() == pos_y or self._move_size &lt; 1:\n                    dst_pos_y = pos_y\n                elif pos.y() &lt; pos_y:\n                    dst_pos_y = min(pos_y, pos.y() + self._move_size)\n                else:\n                    dst_pos_y = max(pos_y, pos.y() - self._move_size)\n\n            # Store if widget is in place where should be\n            if all_at_place and dst_pos_y != pos_y:\n                all_at_place = False\n\n            # Calculate ideal width and height of message widget\n            height = widget.heightForWidth(max_width)\n            w_size_hint = widget.size_hint_without_word_wrap()\n            widget.resize(min(max_width, w_size_hint.width()), height)\n\n            # Center message widget\n            size = widget.size()\n            pos_x = widget_half_width - (size.width() / 2)\n            # Move widget to destination position\n            widget.move(pos_x, dst_pos_y)\n\n            # Add message widget height and spacing for next message widget\n            pos_y += size.height() + self._spacing\n\n        # Remove widgets to remove\n        for message_id in message_ids_to_remove:\n            self._messages_order.remove(message_id)\n            self._closing_messages.remove(message_id)\n            widget = self._messages.pop(message_id)\n            widget.hide()\n            widget.deleteLater()\n\n        # Stop recalculation timer if all widgets are where should be\n        if all_at_place:\n            self._recalculate_timer.stop()\n\n    def eventFilter(self, source, event):\n        # Trigger recalculation of timer on resize of widget\n        if source is self._widget and event.type() == QtCore.QEvent.Resize:\n            self._recalculate_timer.start()\n\n        return super(MessageOverlayObject, self).eventFilter(source, event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.MessageOverlayObject.add_message","title":"<code>add_message(message, message_type=None, timeout=None, message_id=None)</code>","text":"<p>Add single message into overlay.</p> <p>Parameters:</p> Name Type Description Default <code>message</code> <code>str</code> <p>Message that will be shown.</p> required <code>timeout</code> <code>int</code> <p>Message timeout.</p> <code>None</code> <code>message_type</code> <code>str</code> <p>Message type can be used as property in stylesheets.</p> <code>None</code> <code>message_id</code> <code>str</code> <p>UUID of already existing message to update it's message and timeout. Is created with different id if is not available anymore.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <p>UUID of message which can be used to update message.</p> Source code in <code>client/ayon_core/tools/utils/overlay_messages.py</code> <pre><code>def add_message(\n    self, message, message_type=None, timeout=None, message_id=None\n):\n    \"\"\"Add single message into overlay.\n\n    Args:\n        message (str): Message that will be shown.\n        timeout (int): Message timeout.\n        message_type (str): Message type can be used as property in\n            stylesheets.\n        message_id (str): UUID of already existing message to update\n            it's message and timeout. Is created with different id if is\n            not available anymore.\n\n    Returns:\n        str: UUID of message which can be used to update message.\n    \"\"\"\n    # Skip empty messages\n    if not message:\n        return\n\n    if timeout is None:\n        timeout = self._default_timeout\n\n    # Create unique id of message\n    widget = None\n    if message_id is not None:\n        widget = self._messages.get(message_id)\n\n    if message_id is None:\n        message_id = str(uuid.uuid4())\n\n    elif message_id in self._messages_order:\n        self._messages_order.remove(message_id)\n\n    if widget is not None:\n        # NOTE: Update of message won't change paint order which should be\n        #   ok in most of cases as it matters only when messages are\n        #   animated\n        widget.update_message(message, message_type, timeout)\n    else:\n        # Create message widget\n        widget = OverlayMessageWidget(\n            message_id, message, self._widget, message_type, timeout\n        )\n        widget.close_requested.connect(self._on_message_close_request)\n        widget.show()\n\n    # Move widget outside of window\n    pos = widget.pos()\n    pos.setY(pos.y() - widget.height())\n    widget.move(pos)\n    # Store message\n    self._messages[message_id] = widget\n    self._messages_order.append(message_id)\n    # Trigger recalculation timer\n    self._recalculate_timer.start()\n\n    return message_id\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.MultiSelectionComboBox","title":"<code>MultiSelectionComboBox</code>","text":"<p>               Bases: <code>QComboBox</code></p> Source code in <code>client/ayon_core/tools/utils/multiselection_combobox.py</code> <pre><code>class MultiSelectionComboBox(QtWidgets.QComboBox):\n    value_changed = QtCore.Signal()\n    focused_in = QtCore.Signal()\n\n    ignored_keys = {\n        QtCore.Qt.Key_Up,\n        QtCore.Qt.Key_Down,\n        QtCore.Qt.Key_PageDown,\n        QtCore.Qt.Key_PageUp,\n        QtCore.Qt.Key_Home,\n        QtCore.Qt.Key_End,\n    }\n\n    top_bottom_padding = 2\n    left_right_padding = 3\n    left_offset = 4\n    top_bottom_margins = 1\n    item_spacing = 5\n\n    item_bg_color = QtGui.QColor(\"#31424e\")\n    _placeholder_color = None\n\n    def __init__(\n        self, parent=None, placeholder=\"\", separator=\", \", **kwargs\n    ):\n        super().__init__(parent=parent, **kwargs)\n        self.setObjectName(\"MultiSelectionComboBox\")\n        self.setFocusPolicy(QtCore.Qt.StrongFocus)\n\n        self._popup_is_shown = False\n        self._block_mouse_release_timer = QtCore.QTimer(self, singleShot=True)\n        self._initial_mouse_pos = None\n        self._separator = separator\n        self._placeholder_text = placeholder or \"\"\n        delegate = ComboItemDelegate(self)\n        self.setItemDelegate(delegate)\n\n        self._lines = {}\n        self._item_height = None\n        self._custom_text = None\n        self._delegate = delegate\n\n    def get_placeholder_text(self):\n        return self._placeholder_text\n\n    def set_placeholder_text(self, text):\n        self._placeholder_text = text or \"\"\n        self._update_size_hint()\n\n    def set_custom_text(self, text):\n        self._custom_text = text\n        self._update_size_hint()\n\n    def focusInEvent(self, event):\n        self.focused_in.emit()\n        return super(MultiSelectionComboBox, self).focusInEvent(event)\n\n    def mousePressEvent(self, event):\n        \"\"\"Reimplemented.\"\"\"\n        self._popup_is_shown = False\n        super(MultiSelectionComboBox, self).mousePressEvent(event)\n        if self._popup_is_shown:\n            self._initial_mouse_pos = self.mapToGlobal(event.pos())\n            self._block_mouse_release_timer.start(\n                QtWidgets.QApplication.doubleClickInterval()\n            )\n\n    def showPopup(self):\n        \"\"\"Reimplemented.\"\"\"\n        super(MultiSelectionComboBox, self).showPopup()\n        view = self.view()\n        view.installEventFilter(self)\n        view.viewport().installEventFilter(self)\n        self._popup_is_shown = True\n\n    def hidePopup(self):\n        \"\"\"Reimplemented.\"\"\"\n        self.view().removeEventFilter(self)\n        self.view().viewport().removeEventFilter(self)\n        self._popup_is_shown = False\n        self._initial_mouse_pos = None\n        super(MultiSelectionComboBox, self).hidePopup()\n        self.view().clearFocus()\n\n    def _event_popup_shown(self, obj, event):\n        if not self._popup_is_shown:\n            return\n\n        current_index = self.view().currentIndex()\n        model = self.model()\n\n        if event.type() == QtCore.QEvent.MouseMove:\n            if (\n                self.view().isVisible()\n                and self._initial_mouse_pos is not None\n                and self._block_mouse_release_timer.isActive()\n            ):\n                diff = obj.mapToGlobal(event.pos()) - self._initial_mouse_pos\n                if diff.manhattanLength() &gt; 9:\n                    self._block_mouse_release_timer.stop()\n            return\n\n        index_flags = current_index.flags()\n        state = checkstate_int_to_enum(\n            current_index.data(QtCore.Qt.CheckStateRole)\n        )\n        new_state = None\n\n        if event.type() == QtCore.QEvent.MouseButtonRelease:\n            if (\n                self._block_mouse_release_timer.isActive()\n                or not current_index.isValid()\n                or not self.view().isVisible()\n                or not self.view().rect().contains(event.pos())\n                or not index_flags &amp; QtCore.Qt.ItemIsSelectable\n                or not index_flags &amp; QtCore.Qt.ItemIsEnabled\n                or not index_flags &amp; QtCore.Qt.ItemIsUserCheckable\n            ):\n                return\n\n            if state == QtCore.Qt.Unchecked:\n                new_state = CHECKED_INT\n            else:\n                new_state = UNCHECKED_INT\n\n        elif event.type() == QtCore.QEvent.KeyPress:\n            # TODO: handle QtCore.Qt.Key_Enter, Key_Return?\n            if event.key() == QtCore.Qt.Key_Space:\n                if (\n                    index_flags &amp; QtCore.Qt.ItemIsUserCheckable\n                    and index_flags &amp; ITEM_IS_USER_TRISTATE\n                ):\n                    new_state = (checkstate_enum_to_int(state) + 1) % 3\n\n                elif index_flags &amp; QtCore.Qt.ItemIsUserCheckable:\n                    # toggle the current items check state\n                    if state != QtCore.Qt.Checked:\n                        new_state = CHECKED_INT\n                    else:\n                        new_state = UNCHECKED_INT\n\n        if new_state is not None:\n            model.setData(current_index, new_state, QtCore.Qt.CheckStateRole)\n            self.view().update(current_index)\n            self._update_size_hint()\n            self.value_changed.emit()\n            return True\n\n    def eventFilter(self, obj, event):\n        \"\"\"Reimplemented.\"\"\"\n        result = self._event_popup_shown(obj, event)\n        if result is not None:\n            return result\n\n        return super(MultiSelectionComboBox, self).eventFilter(obj, event)\n\n    def addItem(self, *args, **kwargs):\n        idx = self.count()\n        super(MultiSelectionComboBox, self).addItem(*args, **kwargs)\n        self.model().item(idx).setCheckable(True)\n\n    def paintEvent(self, event):\n        \"\"\"Reimplemented.\"\"\"\n        painter = QtWidgets.QStylePainter(self)\n        option = QtWidgets.QStyleOptionComboBox()\n        self.initStyleOption(option)\n        painter.drawComplexControl(QtWidgets.QStyle.CC_ComboBox, option)\n\n        items = self.checked_items_text()\n        # draw the icon and text\n        draw_text = True\n        combotext = None\n        if self._custom_text is not None:\n            combotext = self._custom_text\n        elif not items:\n            combotext = self._placeholder_text\n        else:\n            draw_text = False\n\n        if draw_text:\n            color = self._get_placeholder_color()\n            pen = painter.pen()\n            pen.setColor(color)\n            painter.setPen(pen)\n\n            left_x = option.rect.left() + self.left_offset\n\n            font = self.font()\n            # This is hardcoded point size from styles\n            font.setPointSize(10)\n            painter.setFont(font)\n\n            label_rect = QtCore.QRect(option.rect)\n            label_rect.moveLeft(left_x)\n\n            painter.drawText(\n                label_rect,\n                QtCore.Qt.AlignLeft | QtCore.Qt.AlignVCenter,\n                combotext\n            )\n            return\n\n        if self._item_height is None:\n            self.updateGeometry()\n            self.update()\n            return\n\n        font_metrics = self.fontMetrics()\n        for line, items in self._lines.items():\n            top_y = (\n                option.rect.top()\n                + (line * self._item_height)\n                + self.top_bottom_margins\n            )\n            left_x = option.rect.left() + self.left_offset\n            for item in items:\n                label_rect = font_metrics.boundingRect(item)\n                label_height = label_rect.height()\n\n                label_rect.moveTop(top_y)\n                label_rect.moveLeft(left_x)\n                label_rect.setHeight(self._item_height)\n                label_rect.setWidth(\n                    label_rect.width() + self.left_right_padding\n                )\n\n                if not draw_text:\n                    bg_rect = QtCore.QRectF(label_rect)\n                    bg_rect.setWidth(\n                        label_rect.width() + self.left_right_padding\n                    )\n                    left_x = bg_rect.right() + self.item_spacing\n\n                    bg_rect.setHeight(\n                        label_height + (2 * self.top_bottom_padding)\n                    )\n                    bg_rect.moveTop(bg_rect.top() + self.top_bottom_margins)\n\n                    path = QtGui.QPainterPath()\n                    path.addRoundedRect(bg_rect, 5, 5)\n\n                    painter.fillPath(path, self.item_bg_color)\n\n                label_rect.moveLeft(label_rect.x() + self.left_right_padding)\n\n                painter.drawText(\n                    label_rect,\n                    QtCore.Qt.AlignLeft | QtCore.Qt.AlignVCenter,\n                    item\n                )\n\n    def resizeEvent(self, *args, **kwargs):\n        super(MultiSelectionComboBox, self).resizeEvent(*args, **kwargs)\n        self._update_size_hint()\n\n    def _update_size_hint(self):\n        if self._custom_text is not None:\n            self.update()\n            return\n        self._lines = {}\n\n        items = self.checked_items_text()\n        if not items:\n            self.update()\n            self.repaint()\n            return\n\n        option = QtWidgets.QStyleOptionComboBox()\n        self.initStyleOption(option)\n        btn_rect = self.style().subControlRect(\n            QtWidgets.QStyle.CC_ComboBox,\n            option,\n            QtWidgets.QStyle.SC_ComboBoxArrow\n        )\n        total_width = option.rect.width() - btn_rect.width()\n\n        line = 0\n        self._lines = {line: []}\n\n        font_metrics = self.fontMetrics()\n        default_left_x = 0 + self.left_offset\n        left_x = int(default_left_x)\n        for item in items:\n            rect = font_metrics.boundingRect(item)\n            width = rect.width() + (2 * self.left_right_padding)\n            right_x = left_x + width\n            if right_x &gt; total_width:\n                left_x = int(default_left_x)\n                if self._lines.get(line):\n                    line += 1\n                    self._lines[line] = [item]\n                    left_x += width\n                else:\n                    self._lines[line] = [item]\n                    line += 1\n            else:\n                if line in self._lines:\n                    self._lines[line].append(item)\n                else:\n                    self._lines[line] = [item]\n                left_x = left_x + width + self.item_spacing\n\n        self.update()\n        self.updateGeometry()\n\n    def sizeHint(self):\n        value = super(MultiSelectionComboBox, self).sizeHint()\n        lines = 1\n        if self._custom_text is None:\n            lines = len(self._lines)\n            if lines == 0:\n                lines = 1\n\n        if self._item_height is None:\n            self._item_height = (\n                self.fontMetrics().height()\n                + (2 * self.top_bottom_padding)\n                + (2 * self.top_bottom_margins)\n            )\n        value.setHeight(\n            (lines * self._item_height)\n            + (2 * self.top_bottom_margins)\n        )\n        return value\n\n    def setItemCheckState(self, index, state):\n        self.setItemData(index, state, QtCore.Qt.CheckStateRole)\n\n    def set_value(self, values):\n        for idx in range(self.count()):\n            value = self.itemData(idx, role=QtCore.Qt.UserRole)\n            if value in values:\n                check_state = CHECKED_INT\n            else:\n                check_state = UNCHECKED_INT\n            self.setItemData(idx, check_state, QtCore.Qt.CheckStateRole)\n        self._update_size_hint()\n\n    def value(self):\n        items = list()\n        for idx in range(self.count()):\n            state = checkstate_int_to_enum(\n                self.itemData(idx, role=QtCore.Qt.CheckStateRole)\n            )\n            if state == QtCore.Qt.Checked:\n                items.append(\n                    self.itemData(idx, role=QtCore.Qt.UserRole)\n                )\n        return items\n\n    def checked_items_text(self):\n        items = list()\n        for idx in range(self.count()):\n            state = checkstate_int_to_enum(\n                self.itemData(idx, role=QtCore.Qt.CheckStateRole)\n            )\n            if state == QtCore.Qt.Checked:\n                items.append(self.itemText(idx))\n        return items\n\n    def wheelEvent(self, event):\n        event.ignore()\n\n    def keyPressEvent(self, event):\n        if (\n            event.key() == QtCore.Qt.Key_Down\n            and event.modifiers() &amp; QtCore.Qt.AltModifier\n        ):\n            return self.showPopup()\n\n        if event.key() in self.ignored_keys:\n            return event.ignore()\n\n        return super(MultiSelectionComboBox, self).keyPressEvent(event)\n\n    @classmethod\n    def _get_placeholder_color(cls):\n        if cls._placeholder_color is None:\n            color_obj = get_objected_colors(\"font\")\n            color = color_obj.get_qcolor()\n            color.setAlpha(67)\n            cls._placeholder_color = color\n        return cls._placeholder_color\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.MultiSelectionComboBox.eventFilter","title":"<code>eventFilter(obj, event)</code>","text":"<p>Reimplemented.</p> Source code in <code>client/ayon_core/tools/utils/multiselection_combobox.py</code> <pre><code>def eventFilter(self, obj, event):\n    \"\"\"Reimplemented.\"\"\"\n    result = self._event_popup_shown(obj, event)\n    if result is not None:\n        return result\n\n    return super(MultiSelectionComboBox, self).eventFilter(obj, event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.MultiSelectionComboBox.hidePopup","title":"<code>hidePopup()</code>","text":"<p>Reimplemented.</p> Source code in <code>client/ayon_core/tools/utils/multiselection_combobox.py</code> <pre><code>def hidePopup(self):\n    \"\"\"Reimplemented.\"\"\"\n    self.view().removeEventFilter(self)\n    self.view().viewport().removeEventFilter(self)\n    self._popup_is_shown = False\n    self._initial_mouse_pos = None\n    super(MultiSelectionComboBox, self).hidePopup()\n    self.view().clearFocus()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.MultiSelectionComboBox.mousePressEvent","title":"<code>mousePressEvent(event)</code>","text":"<p>Reimplemented.</p> Source code in <code>client/ayon_core/tools/utils/multiselection_combobox.py</code> <pre><code>def mousePressEvent(self, event):\n    \"\"\"Reimplemented.\"\"\"\n    self._popup_is_shown = False\n    super(MultiSelectionComboBox, self).mousePressEvent(event)\n    if self._popup_is_shown:\n        self._initial_mouse_pos = self.mapToGlobal(event.pos())\n        self._block_mouse_release_timer.start(\n            QtWidgets.QApplication.doubleClickInterval()\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.MultiSelectionComboBox.paintEvent","title":"<code>paintEvent(event)</code>","text":"<p>Reimplemented.</p> Source code in <code>client/ayon_core/tools/utils/multiselection_combobox.py</code> <pre><code>def paintEvent(self, event):\n    \"\"\"Reimplemented.\"\"\"\n    painter = QtWidgets.QStylePainter(self)\n    option = QtWidgets.QStyleOptionComboBox()\n    self.initStyleOption(option)\n    painter.drawComplexControl(QtWidgets.QStyle.CC_ComboBox, option)\n\n    items = self.checked_items_text()\n    # draw the icon and text\n    draw_text = True\n    combotext = None\n    if self._custom_text is not None:\n        combotext = self._custom_text\n    elif not items:\n        combotext = self._placeholder_text\n    else:\n        draw_text = False\n\n    if draw_text:\n        color = self._get_placeholder_color()\n        pen = painter.pen()\n        pen.setColor(color)\n        painter.setPen(pen)\n\n        left_x = option.rect.left() + self.left_offset\n\n        font = self.font()\n        # This is hardcoded point size from styles\n        font.setPointSize(10)\n        painter.setFont(font)\n\n        label_rect = QtCore.QRect(option.rect)\n        label_rect.moveLeft(left_x)\n\n        painter.drawText(\n            label_rect,\n            QtCore.Qt.AlignLeft | QtCore.Qt.AlignVCenter,\n            combotext\n        )\n        return\n\n    if self._item_height is None:\n        self.updateGeometry()\n        self.update()\n        return\n\n    font_metrics = self.fontMetrics()\n    for line, items in self._lines.items():\n        top_y = (\n            option.rect.top()\n            + (line * self._item_height)\n            + self.top_bottom_margins\n        )\n        left_x = option.rect.left() + self.left_offset\n        for item in items:\n            label_rect = font_metrics.boundingRect(item)\n            label_height = label_rect.height()\n\n            label_rect.moveTop(top_y)\n            label_rect.moveLeft(left_x)\n            label_rect.setHeight(self._item_height)\n            label_rect.setWidth(\n                label_rect.width() + self.left_right_padding\n            )\n\n            if not draw_text:\n                bg_rect = QtCore.QRectF(label_rect)\n                bg_rect.setWidth(\n                    label_rect.width() + self.left_right_padding\n                )\n                left_x = bg_rect.right() + self.item_spacing\n\n                bg_rect.setHeight(\n                    label_height + (2 * self.top_bottom_padding)\n                )\n                bg_rect.moveTop(bg_rect.top() + self.top_bottom_margins)\n\n                path = QtGui.QPainterPath()\n                path.addRoundedRect(bg_rect, 5, 5)\n\n                painter.fillPath(path, self.item_bg_color)\n\n            label_rect.moveLeft(label_rect.x() + self.left_right_padding)\n\n            painter.drawText(\n                label_rect,\n                QtCore.Qt.AlignLeft | QtCore.Qt.AlignVCenter,\n                item\n            )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.MultiSelectionComboBox.showPopup","title":"<code>showPopup()</code>","text":"<p>Reimplemented.</p> Source code in <code>client/ayon_core/tools/utils/multiselection_combobox.py</code> <pre><code>def showPopup(self):\n    \"\"\"Reimplemented.\"\"\"\n    super(MultiSelectionComboBox, self).showPopup()\n    view = self.view()\n    view.installEventFilter(self)\n    view.viewport().installEventFilter(self)\n    self._popup_is_shown = True\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.PixmapLabel","title":"<code>PixmapLabel</code>","text":"<p>               Bases: <code>QLabel</code></p> <p>Label resizing image to height of font.</p> Source code in <code>client/ayon_core/tools/utils/widgets.py</code> <pre><code>class PixmapLabel(QtWidgets.QLabel):\n    \"\"\"Label resizing image to height of font.\"\"\"\n    def __init__(self, pixmap, parent):\n        super(PixmapLabel, self).__init__(parent)\n        self._empty_pixmap = QtGui.QPixmap(0, 0)\n        self._source_pixmap = pixmap\n\n        self._last_width = 0\n        self._last_height = 0\n\n    def set_source_pixmap(self, pixmap):\n        \"\"\"Change source image.\"\"\"\n        self._source_pixmap = pixmap\n        self._set_resized_pix()\n\n    def _get_pix_size(self):\n        size = self.fontMetrics().height()\n        size += size % 2\n        return size, size\n\n    def minimumSizeHint(self):\n        width, height = self._get_pix_size()\n        if width != self._last_width or height != self._last_height:\n            self._set_resized_pix()\n        return QtCore.QSize(width, height)\n\n    def _set_resized_pix(self):\n        if self._source_pixmap is None:\n            self.setPixmap(self._empty_pixmap)\n            return\n        width, height = self._get_pix_size()\n        self.setPixmap(\n            self._source_pixmap.scaled(\n                width,\n                height,\n                QtCore.Qt.KeepAspectRatio,\n                QtCore.Qt.SmoothTransformation\n            )\n        )\n        self._last_width = width\n        self._last_height = height\n\n    def resizeEvent(self, event):\n        self._set_resized_pix()\n        super().resizeEvent(event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.PixmapLabel.set_source_pixmap","title":"<code>set_source_pixmap(pixmap)</code>","text":"<p>Change source image.</p> Source code in <code>client/ayon_core/tools/utils/widgets.py</code> <pre><code>def set_source_pixmap(self, pixmap):\n    \"\"\"Change source image.\"\"\"\n    self._source_pixmap = pixmap\n    self._set_resized_pix()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.PlaceholderLineEdit","title":"<code>PlaceholderLineEdit</code>","text":"<p>               Bases: <code>QLineEdit</code></p> <p>Set placeholder color of QLineEdit in Qt 5.12 and higher.</p> Source code in <code>client/ayon_core/tools/utils/widgets.py</code> <pre><code>class PlaceholderLineEdit(QtWidgets.QLineEdit):\n    \"\"\"Set placeholder color of QLineEdit in Qt 5.12 and higher.\"\"\"\n    def __init__(self, *args, **kwargs):\n        super().__init__(*args, **kwargs)\n        self.setMaxLength(2147483647)\n        # Change placeholder palette color\n        if hasattr(QtGui.QPalette, \"PlaceholderText\"):\n            filter_palette = self.palette()\n            filter_palette.setColor(\n                QtGui.QPalette.PlaceholderText,\n                _Cache.get_placeholder_color()\n            )\n            self.setPalette(filter_palette)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.PlaceholderPlainTextEdit","title":"<code>PlaceholderPlainTextEdit</code>","text":"<p>               Bases: <code>QPlainTextEdit</code></p> <p>Set placeholder color of QPlainTextEdit in Qt 5.12 and higher.</p> Source code in <code>client/ayon_core/tools/utils/widgets.py</code> <pre><code>class PlaceholderPlainTextEdit(QtWidgets.QPlainTextEdit):\n    \"\"\"Set placeholder color of QPlainTextEdit in Qt 5.12 and higher.\"\"\"\n    def __init__(self, *args, **kwargs):\n        super().__init__(*args, **kwargs)\n        # Change placeholder palette color\n        if hasattr(QtGui.QPalette, \"PlaceholderText\"):\n            viewport = self.viewport()\n            filter_palette = viewport.palette()\n            filter_palette.setColor(\n                QtGui.QPalette.PlaceholderText,\n                _Cache.get_placeholder_color()\n            )\n            viewport.setPalette(filter_palette)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.PopupUpdateKeys","title":"<code>PopupUpdateKeys</code>","text":"<p>               Bases: <code>SimplePopup</code></p> <p>Simple popup with checkbox.</p> Source code in <code>client/ayon_core/tools/utils/dialogs.py</code> <pre><code>class PopupUpdateKeys(SimplePopup):\n    \"\"\"Simple popup with checkbox.\"\"\"\n\n    on_clicked_state = QtCore.Signal(bool)\n\n    def __init__(self, parent=None, *args, **kwargs):\n        super().__init__(parent=parent, *args, **kwargs)\n\n        layout = self.layout()\n\n        # Insert toggle for Update keys\n        toggle = QtWidgets.QCheckBox(\"Update Keys\", self)\n        layout.insertWidget(1, toggle)\n\n        self.on_clicked.connect(self.emit_click_with_state)\n\n        layout.insertStretch(1, 1)\n\n        self._toggle_checkbox = toggle\n\n    def is_toggle_checked(self):\n        return self._toggle_checkbox.isChecked()\n\n    def emit_click_with_state(self):\n        \"\"\"Emit the on_clicked_state signal with the toggled state\"\"\"\n        checked = self._toggle_checkbox.isChecked()\n        self.on_clicked_state.emit(checked)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.PopupUpdateKeys.emit_click_with_state","title":"<code>emit_click_with_state()</code>","text":"<p>Emit the on_clicked_state signal with the toggled state</p> Source code in <code>client/ayon_core/tools/utils/dialogs.py</code> <pre><code>def emit_click_with_state(self):\n    \"\"\"Emit the on_clicked_state signal with the toggled state\"\"\"\n    checked = self._toggle_checkbox.isChecked()\n    self.on_clicked_state.emit(checked)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.ProjectsCombobox","title":"<code>ProjectsCombobox</code>","text":"<p>               Bases: <code>QWidget</code></p> Source code in <code>client/ayon_core/tools/utils/projects_widget.py</code> <pre><code>class ProjectsCombobox(QtWidgets.QWidget):\n    refreshed = QtCore.Signal()\n    selection_changed = QtCore.Signal(str)\n\n    def __init__(\n        self,\n        controller: AbstractProjectController,\n        parent: QtWidgets.QWidget,\n        handle_expected_selection: bool = False,\n    ):\n        super().__init__(parent)\n\n        projects_combobox = QtWidgets.QComboBox(self)\n        combobox_delegate = ProjectsDelegate(projects_combobox)\n        projects_combobox.setItemDelegate(combobox_delegate)\n        projects_model = ProjectsQtModel(controller)\n        projects_proxy_model = ProjectSortFilterProxy()\n        projects_proxy_model.setSourceModel(projects_model)\n        projects_combobox.setModel(projects_proxy_model)\n\n        main_layout = QtWidgets.QHBoxLayout(self)\n        main_layout.setContentsMargins(0, 0, 0, 0)\n        main_layout.addWidget(projects_combobox, 1)\n\n        projects_model.refreshed.connect(self._on_model_refresh)\n\n        controller.register_event_callback(\n            \"projects.refresh.finished\",\n            self._on_projects_refresh_finished\n        )\n        controller.register_event_callback(\n            \"controller.refresh.finished\",\n            self._on_controller_refresh\n        )\n        controller.register_event_callback(\n            \"expected_selection_changed\",\n            self._on_expected_selection_change\n        )\n\n        projects_combobox.currentIndexChanged.connect(\n            self._on_current_index_changed\n        )\n\n        self._controller = controller\n        self._listen_selection_change = True\n        self._select_item_visible = False\n\n        self._handle_expected_selection = handle_expected_selection\n        self._expected_selection = None\n\n        self._projects_combobox = projects_combobox\n        self._projects_model = projects_model\n        self._projects_proxy_model = projects_proxy_model\n        self._combobox_delegate = combobox_delegate\n\n    def refresh(self):\n        self._projects_model.refresh()\n\n    def set_selection(self, project_name: str):\n        \"\"\"Set selection to a given project.\n\n        Selection change is ignored if project is not found.\n\n        Args:\n            project_name (str): Name of project.\n\n        Returns:\n            bool: True if selection was changed, False otherwise. NOTE:\n                Selection may not be changed if project is not found, or if\n                project is already selected.\n\n        \"\"\"\n        idx = self._projects_combobox.findData(\n            project_name, PROJECT_NAME_ROLE)\n        if idx &lt; 0:\n            return False\n        if idx != self._projects_combobox.currentIndex():\n            self._projects_combobox.setCurrentIndex(idx)\n            return True\n        return False\n\n    def set_listen_to_selection_change(self, listen: bool):\n        \"\"\"Disable listening to changes of the selection.\n\n        Because combobox is triggering selection change when it's model\n        is refreshed, it's necessary to disable listening to selection for\n        some cases, e.g. when is on a different page of UI and should be just\n        refreshed.\n\n        Args:\n            listen (bool): Enable or disable listening to selection changes.\n        \"\"\"\n\n        self._listen_selection_change = listen\n\n    def get_selected_project_name(self):\n        \"\"\"Name of selected project.\n\n        Returns:\n            Union[str, None]: Name of selected project, or None if no project\n        \"\"\"\n\n        idx = self._projects_combobox.currentIndex()\n        if idx &lt; 0:\n            return None\n        return self._projects_combobox.itemData(idx, PROJECT_NAME_ROLE)\n\n    def set_current_context_project(self, project_name: str):\n        self._projects_model.set_current_context_project(project_name)\n        self._projects_proxy_model.invalidateFilter()\n\n    def set_select_item_visible(self, visible: bool):\n        self._select_item_visible = visible\n        self._projects_model.set_select_item_visible(visible)\n        self._update_select_item_visiblity()\n\n    def set_libraries_separator_visible(self, visible):\n        self._projects_model.set_libraries_separator_visible(visible)\n\n    def is_active_filter_enabled(self):\n        return self._projects_proxy_model.is_active_filter_enabled()\n\n    def set_active_filter_enabled(self, enabled):\n        return self._projects_proxy_model.set_active_filter_enabled(enabled)\n\n    def set_standard_filter_enabled(self, enabled):\n        return self._projects_proxy_model.set_standard_filter_enabled(enabled)\n\n    def set_library_filter_enabled(self, enabled):\n        return self._projects_proxy_model.set_library_filter_enabled(enabled)\n\n    def _update_select_item_visiblity(self, **kwargs):\n        if not self._select_item_visible:\n            return\n        if \"project_name\" not in kwargs:\n            project_name = self.get_selected_project_name()\n        else:\n            project_name = kwargs.get(\"project_name\")\n\n        # Hide the item if a project is selected\n        self._projects_model.set_selected_project(project_name)\n\n    def _on_current_index_changed(self, idx):\n        if not self._listen_selection_change:\n            return\n        project_name = self._projects_combobox.itemData(\n            idx, PROJECT_NAME_ROLE)\n        self._update_select_item_visiblity(project_name=project_name)\n        self._controller.set_selected_project(project_name)\n        self.selection_changed.emit(project_name or \"\")\n\n    def _on_model_refresh(self):\n        self._projects_proxy_model.sort(0)\n        self._projects_proxy_model.invalidateFilter()\n        if self._expected_selection:\n            self._set_expected_selection()\n        self._update_select_item_visiblity()\n        self.refreshed.emit()\n\n    def _on_projects_refresh_finished(self, event):\n        if event[\"sender\"] != PROJECTS_MODEL_SENDER:\n            self._projects_model.refresh()\n\n    def _on_controller_refresh(self):\n        self._update_expected_selection()\n\n    # Expected selection handling\n    def _on_expected_selection_change(self, event):\n        self._update_expected_selection(event.data)\n\n    def _set_expected_selection(self):\n        if not self._handle_expected_selection:\n            return\n        project_name = self._expected_selection\n        if project_name is not None:\n            if project_name != self.get_selected_project_name():\n                self.set_selection(project_name)\n            else:\n                # Fake project change\n                self._on_current_index_changed(\n                    self._projects_combobox.currentIndex()\n                )\n\n        self._controller.expected_project_selected(project_name)\n\n    def _update_expected_selection(self, expected_data=None):\n        if not self._handle_expected_selection:\n            return\n        if expected_data is None:\n            expected_data = self._controller.get_expected_selection_data()\n\n        project_data = expected_data.get(\"project\")\n        if (\n            not project_data\n            or not project_data[\"current\"]\n            or project_data[\"selected\"]\n        ):\n            return\n        self._expected_selection = project_data[\"name\"]\n        if not self._projects_model.is_refreshing:\n            self._set_expected_selection()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.ProjectsCombobox.get_selected_project_name","title":"<code>get_selected_project_name()</code>","text":"<p>Name of selected project.</p> <p>Returns:</p> Type Description <p>Union[str, None]: Name of selected project, or None if no project</p> Source code in <code>client/ayon_core/tools/utils/projects_widget.py</code> <pre><code>def get_selected_project_name(self):\n    \"\"\"Name of selected project.\n\n    Returns:\n        Union[str, None]: Name of selected project, or None if no project\n    \"\"\"\n\n    idx = self._projects_combobox.currentIndex()\n    if idx &lt; 0:\n        return None\n    return self._projects_combobox.itemData(idx, PROJECT_NAME_ROLE)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.ProjectsCombobox.set_listen_to_selection_change","title":"<code>set_listen_to_selection_change(listen)</code>","text":"<p>Disable listening to changes of the selection.</p> <p>Because combobox is triggering selection change when it's model is refreshed, it's necessary to disable listening to selection for some cases, e.g. when is on a different page of UI and should be just refreshed.</p> <p>Parameters:</p> Name Type Description Default <code>listen</code> <code>bool</code> <p>Enable or disable listening to selection changes.</p> required Source code in <code>client/ayon_core/tools/utils/projects_widget.py</code> <pre><code>def set_listen_to_selection_change(self, listen: bool):\n    \"\"\"Disable listening to changes of the selection.\n\n    Because combobox is triggering selection change when it's model\n    is refreshed, it's necessary to disable listening to selection for\n    some cases, e.g. when is on a different page of UI and should be just\n    refreshed.\n\n    Args:\n        listen (bool): Enable or disable listening to selection changes.\n    \"\"\"\n\n    self._listen_selection_change = listen\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.ProjectsCombobox.set_selection","title":"<code>set_selection(project_name)</code>","text":"<p>Set selection to a given project.</p> <p>Selection change is ignored if project is not found.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Name of project.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <p>True if selection was changed, False otherwise. NOTE: Selection may not be changed if project is not found, or if project is already selected.</p> Source code in <code>client/ayon_core/tools/utils/projects_widget.py</code> <pre><code>def set_selection(self, project_name: str):\n    \"\"\"Set selection to a given project.\n\n    Selection change is ignored if project is not found.\n\n    Args:\n        project_name (str): Name of project.\n\n    Returns:\n        bool: True if selection was changed, False otherwise. NOTE:\n            Selection may not be changed if project is not found, or if\n            project is already selected.\n\n    \"\"\"\n    idx = self._projects_combobox.findData(\n        project_name, PROJECT_NAME_ROLE)\n    if idx &lt; 0:\n        return False\n    if idx != self._projects_combobox.currentIndex():\n        self._projects_combobox.setCurrentIndex(idx)\n        return True\n    return False\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.ProjectsQtModel","title":"<code>ProjectsQtModel</code>","text":"<p>               Bases: <code>QStandardItemModel</code></p> Source code in <code>client/ayon_core/tools/utils/projects_widget.py</code> <pre><code>class ProjectsQtModel(QtGui.QStandardItemModel):\n    refreshed = QtCore.Signal()\n\n    def __init__(self, controller: AbstractProjectController):\n        super().__init__()\n        self._controller = controller\n\n        self._project_items = {}\n        self._has_libraries = False\n\n        self._empty_item = None\n        self._empty_item_added = False\n\n        self._select_item = None\n        self._select_item_added = False\n        self._select_item_visible = None\n\n        self._libraries_sep_item = None\n        self._libraries_sep_item_added = False\n        self._libraries_sep_item_visible = False\n\n        self._current_context_project = None\n\n        self._selected_project = None\n\n        self._refresh_thread = None\n\n    @property\n    def is_refreshing(self):\n        return self._refresh_thread is not None\n\n    def refresh(self):\n        self._refresh()\n\n    def has_content(self):\n        return len(self._project_items) &gt; 0\n\n    def get_index_by_project_name(self, project_name):\n        \"\"\"Get index of project by name.\n\n        Args:\n            project_name (str): Project name.\n\n        Returns:\n            QtCore.QModelIndex: Index of project item. Index is not valid\n                if project is not found.\n\n        \"\"\"\n        item = self._project_items.get(project_name)\n        if item is None:\n            return QtCore.QModelIndex()\n        return self.indexFromItem(item)\n\n    def set_select_item_visible(self, visible):\n        if self._select_item_visible is visible:\n            return\n        self._select_item_visible = visible\n\n        if self._selected_project is None:\n            self._add_select_item()\n\n    def set_libraries_separator_visible(self, visible):\n        if self._libraries_sep_item_visible is visible:\n            return\n        self._libraries_sep_item_visible = visible\n\n    def set_selected_project(self, project_name):\n        if not self._select_item_visible:\n            return\n\n        self._selected_project = project_name\n        if project_name is None:\n            self._add_select_item()\n        else:\n            self._remove_select_item()\n\n    def set_current_context_project(self, project_name):\n        if project_name == self._current_context_project:\n            return\n        self._unset_current_context_project(self._current_context_project)\n        self._current_context_project = project_name\n        self._set_current_context_project(project_name)\n\n    def _set_current_context_project(self, project_name):\n        item = self._project_items.get(project_name)\n        if item is None:\n            return\n        item.setData(True, PROJECT_IS_CURRENT_ROLE)\n\n    def _unset_current_context_project(self, project_name):\n        item = self._project_items.get(project_name)\n        if item is None:\n            return\n        item.setData(False, PROJECT_IS_CURRENT_ROLE)\n\n    def _add_empty_item(self):\n        if self._empty_item_added:\n            return\n        self._empty_item_added = True\n        item = self._get_empty_item()\n        root_item = self.invisibleRootItem()\n        root_item.appendRow(item)\n\n    def _remove_empty_item(self):\n        if not self._empty_item_added:\n            return\n        self._empty_item_added = False\n        root_item = self.invisibleRootItem()\n        item = self._get_empty_item()\n        root_item.takeRow(item.row())\n\n    def _get_empty_item(self):\n        if self._empty_item is None:\n            item = QtGui.QStandardItem(\"&lt; No projects &gt;\")\n            item.setFlags(QtCore.Qt.NoItemFlags)\n            self._empty_item = item\n        return self._empty_item\n\n    def _get_library_sep_item(self):\n        if self._libraries_sep_item is not None:\n            return self._libraries_sep_item\n\n        item = QtGui.QStandardItem()\n        item.setData(\"Libraries\", QtCore.Qt.DisplayRole)\n        item.setData(True, LIBRARY_PROJECT_SEPARATOR_ROLE)\n        item.setFlags(QtCore.Qt.NoItemFlags)\n        self._libraries_sep_item = item\n        return item\n\n    def _add_library_sep_item(self):\n        if (\n            not self._libraries_sep_item_visible\n            or self._libraries_sep_item_added\n        ):\n            return\n        self._libraries_sep_item_added = True\n        item = self._get_library_sep_item()\n        root_item = self.invisibleRootItem()\n        root_item.appendRow(item)\n\n    def _remove_library_sep_item(self):\n        if (\n            not self._libraries_sep_item_added\n        ):\n            return\n        self._libraries_sep_item_added = False\n        item = self._get_library_sep_item()\n        root_item = self.invisibleRootItem()\n        root_item.takeRow(item.row())\n\n    def _add_select_item(self):\n        if self._select_item_added:\n            return\n        self._select_item_added = True\n        item = self._get_select_item()\n        root_item = self.invisibleRootItem()\n        root_item.appendRow(item)\n\n    def _remove_select_item(self):\n        if not self._select_item_added:\n            return\n        self._select_item_added = False\n        root_item = self.invisibleRootItem()\n        item = self._get_select_item()\n        root_item.takeRow(item.row())\n\n    def _get_select_item(self):\n        if self._select_item is None:\n            item = QtGui.QStandardItem(\"&lt; Select project &gt;\")\n            item.setEditable(False)\n            self._select_item = item\n        return self._select_item\n\n    def _refresh(self):\n        if self._refresh_thread is not None:\n            return\n\n        refresh_thread = RefreshThread(\n            \"projects\", self._query_project_items\n        )\n        refresh_thread.refresh_finished.connect(self._refresh_finished)\n\n        self._refresh_thread = refresh_thread\n        refresh_thread.start()\n\n    def _query_project_items(self):\n        return self._controller.get_project_items(\n            sender=PROJECTS_MODEL_SENDER\n        )\n\n    def _refresh_finished(self):\n        # TODO check if failed\n        result = self._refresh_thread.get_result()\n        if result is not None:\n            self._fill_items(result)\n\n        self._refresh_thread = None\n        if result is None:\n            self._refresh()\n        else:\n            self.refreshed.emit()\n\n    def _fill_items(self, project_items: list[ProjectItem]):\n        new_project_names = {\n            project_item.name\n            for project_item in project_items\n        }\n\n        # Handle \"Select item\" visibility\n        if self._select_item_visible:\n            # Add select project. if previously selected project is not in\n            #   project items\n            if self._selected_project not in new_project_names:\n                self._add_select_item()\n            else:\n                self._remove_select_item()\n\n        root_item = self.invisibleRootItem()\n\n        items_to_remove = set(self._project_items.keys()) - new_project_names\n        for project_name in items_to_remove:\n            item = self._project_items.pop(project_name)\n            root_item.takeRow(item.row())\n\n        has_library_project = False\n        new_items = []\n        for project_item in project_items:\n            project_name = project_item.name\n            item = self._project_items.get(project_name)\n            if project_item.is_library:\n                has_library_project = True\n            if item is None:\n                item = QtGui.QStandardItem()\n                item.setEditable(False)\n                new_items.append(item)\n            icon = get_qt_icon(project_item.icon)\n            item.setData(project_name, QtCore.Qt.DisplayRole)\n            item.setData(icon, QtCore.Qt.DecorationRole)\n            item.setData(project_name, PROJECT_NAME_ROLE)\n            item.setData(project_item.active, PROJECT_IS_ACTIVE_ROLE)\n            item.setData(project_item.is_library, PROJECT_IS_LIBRARY_ROLE)\n            item.setData(project_item.is_pinned, PROJECT_IS_PINNED_ROLE)\n            is_current = project_name == self._current_context_project\n            item.setData(is_current, PROJECT_IS_CURRENT_ROLE)\n            self._project_items[project_name] = item\n\n        self._set_current_context_project(self._current_context_project)\n\n        self._has_libraries = has_library_project\n\n        if new_items:\n            root_item.appendRows(new_items)\n\n        if self.has_content():\n            # Make sure \"No projects\" item is removed\n            self._remove_empty_item()\n            if has_library_project:\n                self._add_library_sep_item()\n            else:\n                self._remove_library_sep_item()\n        else:\n            # Keep only \"No projects\" item\n            self._add_empty_item()\n            self._remove_select_item()\n            self._remove_library_sep_item()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.ProjectsQtModel.get_index_by_project_name","title":"<code>get_index_by_project_name(project_name)</code>","text":"<p>Get index of project by name.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <p>Returns:</p> Type Description <p>QtCore.QModelIndex: Index of project item. Index is not valid if project is not found.</p> Source code in <code>client/ayon_core/tools/utils/projects_widget.py</code> <pre><code>def get_index_by_project_name(self, project_name):\n    \"\"\"Get index of project by name.\n\n    Args:\n        project_name (str): Project name.\n\n    Returns:\n        QtCore.QModelIndex: Index of project item. Index is not valid\n            if project is not found.\n\n    \"\"\"\n    item = self._project_items.get(project_name)\n    if item is None:\n        return QtCore.QModelIndex()\n    return self.indexFromItem(item)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.ProjectsWidget","title":"<code>ProjectsWidget</code>","text":"<p>               Bases: <code>QWidget</code></p> <p>Projects widget showing projects in list.</p> Source code in <code>client/ayon_core/tools/utils/projects_widget.py</code> <pre><code>class ProjectsWidget(QtWidgets.QWidget):\n    \"\"\"Projects widget showing projects in list.\n\n    Warnings:\n        This widget does not support expected selection handling.\n\n    \"\"\"\n    refreshed = QtCore.Signal()\n    selection_changed = QtCore.Signal(str)\n    double_clicked = QtCore.Signal()\n\n    def __init__(\n        self,\n        controller: AbstractProjectController,\n        parent: Optional[QtWidgets.QWidget] = None\n    ):\n        super().__init__(parent=parent)\n\n        projects_view = ListView(parent=self)\n        projects_view.setResizeMode(QtWidgets.QListView.Adjust)\n        projects_view.setVerticalScrollMode(\n            QtWidgets.QAbstractItemView.ScrollPerPixel\n        )\n        projects_view.setAlternatingRowColors(False)\n        projects_view.setWrapping(False)\n        projects_view.setWordWrap(False)\n        projects_view.setSpacing(0)\n        projects_delegate = ProjectsDelegate(projects_view)\n        projects_view.setItemDelegate(projects_delegate)\n        projects_view.activate_flick_charm()\n        projects_view.set_deselectable(True)\n\n        projects_model = ProjectsQtModel(controller)\n        projects_proxy_model = ProjectSortFilterProxy()\n        projects_proxy_model.setSourceModel(projects_model)\n        projects_view.setModel(projects_proxy_model)\n\n        main_layout = QtWidgets.QVBoxLayout(self)\n        main_layout.setContentsMargins(0, 0, 0, 0)\n        main_layout.addWidget(projects_view, 1)\n\n        projects_view.selectionModel().selectionChanged.connect(\n            self._on_selection_change\n        )\n        projects_view.double_clicked.connect(self.double_clicked)\n        projects_model.refreshed.connect(self._on_model_refresh)\n\n        controller.register_event_callback(\n            \"projects.refresh.finished\",\n            self._on_projects_refresh_finished\n        )\n\n        self._controller = controller\n\n        self._projects_view = projects_view\n        self._projects_model = projects_model\n        self._projects_proxy_model = projects_proxy_model\n        self._projects_delegate = projects_delegate\n\n    def refresh(self):\n        self._projects_model.refresh()\n\n    def has_content(self) -&gt; bool:\n        \"\"\"Model has at least one project.\n\n        Returns:\n             bool: True if there is any content in the model.\n\n        \"\"\"\n        return self._projects_model.has_content()\n\n    def set_name_filter(self, text: str):\n        self._projects_proxy_model.setFilterFixedString(text)\n\n    def get_selected_project(self) -&gt; Optional[str]:\n        selection_model = self._projects_view.selectionModel()\n        for index in selection_model.selectedIndexes():\n            project_name = index.data(PROJECT_NAME_ROLE)\n            if project_name:\n                return project_name\n        return None\n\n    def set_selected_project(self, project_name: Optional[str]):\n        if project_name is None:\n            self._projects_view.clearSelection()\n            self._projects_view.setCurrentIndex(QtCore.QModelIndex())\n            return\n\n        index = self._projects_model.get_index_by_project_name(project_name)\n        if not index.isValid():\n            return\n        proxy_index = self._projects_proxy_model.mapFromSource(index)\n        if proxy_index.isValid():\n            selection_model = self._projects_view.selectionModel()\n            selection_model.select(\n                proxy_index,\n                QtCore.QItemSelectionModel.ClearAndSelect\n            )\n\n    def _on_model_refresh(self):\n        self._projects_proxy_model.sort(0)\n        self._projects_proxy_model.invalidateFilter()\n        self.refreshed.emit()\n\n    def _on_selection_change(self, new_selection, _old_selection):\n        project_name = None\n        for index in new_selection.indexes():\n            name = index.data(PROJECT_NAME_ROLE)\n            if name:\n                project_name = name\n                break\n        self.selection_changed.emit(project_name or \"\")\n        self._controller.set_selected_project(project_name)\n\n    def _on_projects_refresh_finished(self, event):\n        if event[\"sender\"] != PROJECTS_MODEL_SENDER:\n            self._projects_model.refresh()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.ProjectsWidget.has_content","title":"<code>has_content()</code>","text":"<p>Model has at least one project.</p> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if there is any content in the model.</p> Source code in <code>client/ayon_core/tools/utils/projects_widget.py</code> <pre><code>def has_content(self) -&gt; bool:\n    \"\"\"Model has at least one project.\n\n    Returns:\n         bool: True if there is any content in the model.\n\n    \"\"\"\n    return self._projects_model.has_content()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.RecursiveSortFilterProxyModel","title":"<code>RecursiveSortFilterProxyModel</code>","text":"<p>               Bases: <code>QSortFilterProxyModel</code></p> <p>Recursive proxy model. Item is not filtered if any children match the filter. Use case: Filtering by string - parent won't be filtered if does not match     the filter string but first checks if any children does.</p> Source code in <code>client/ayon_core/tools/utils/models.py</code> <pre><code>class RecursiveSortFilterProxyModel(QtCore.QSortFilterProxyModel):\n    \"\"\"Recursive proxy model.\n    Item is not filtered if any children match the filter.\n    Use case: Filtering by string - parent won't be filtered if does not match\n        the filter string but first checks if any children does.\n    \"\"\"\n\n    def __init__(self, *args, **kwargs):\n        super(RecursiveSortFilterProxyModel, self).__init__(*args, **kwargs)\n        recursive_enabled = False\n        if hasattr(self, \"setRecursiveFilteringEnabled\"):\n            self.setRecursiveFilteringEnabled(True)\n            recursive_enabled = True\n        self._recursive_enabled = recursive_enabled\n\n    def filterAcceptsRow(self, row, parent_index):\n        if hasattr(self, \"filterRegExp\"):\n            regex = self.filterRegExp()\n        else:\n            regex = self.filterRegularExpression()\n\n        pattern = regex.pattern()\n        if pattern:\n            model = self.sourceModel()\n            source_index = model.index(\n                row, self.filterKeyColumn(), parent_index\n            )\n            if source_index.isValid():\n                pattern = regex.pattern()\n\n                # Check current index itself\n                value = model.data(source_index, self.filterRole())\n                matched = bool(re.search(pattern, value, re.IGNORECASE))\n                if matched or self._recursive_enabled:\n                    return matched\n\n                rows = model.rowCount(source_index)\n                for idx in range(rows):\n                    if self.filterAcceptsRow(idx, source_index):\n                        return True\n\n                # Otherwise filter it\n                return False\n\n        return super(RecursiveSortFilterProxyModel, self).filterAcceptsRow(\n            row, parent_index\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.ScrollMessageBox","title":"<code>ScrollMessageBox</code>","text":"<p>               Bases: <code>QDialog</code></p> <p>Basic version of scrollable QMessageBox.</p> <p>No other existing dialog implementation is scrollable.</p> <p>Parameters:</p> Name Type Description Default <code>icon</code> <code>Icon | QIcon</code> <p>Icon to display.</p> required <code>title</code> <code>str</code> <p>Window title.</p> required <code>messages</code> <code>list[str]</code> <p>List of messages.</p> required <code>cancelable</code> <code>Optional[bool]</code> <p>True if Cancel button should be added.</p> <code>False</code> Source code in <code>client/ayon_core/tools/utils/dialogs.py</code> <pre><code>class ScrollMessageBox(QtWidgets.QDialog):\n    \"\"\"Basic version of scrollable QMessageBox.\n\n    No other existing dialog implementation is scrollable.\n\n    Args:\n        icon (QtWidgets.QMessageBox.Icon | QtGui.QIcon): Icon to display.\n        title (str): Window title.\n        messages (list[str]): List of messages.\n        cancelable (Optional[bool]): True if Cancel button should be added.\n\n    \"\"\"\n    def __init__(self, icon, title, messages, cancelable=False):\n        super().__init__()\n        self.setWindowTitle(title)\n        self.icon = icon\n        self._messages = messages\n\n        self.setWindowFlags(QtCore.Qt.WindowTitleHint)\n\n        wrapped_icon = icon_widget(self, icon)\n\n        scroll_widget = QtWidgets.QScrollArea(self)\n        scroll_widget.setWidgetResizable(True)\n        content_widget = QtWidgets.QWidget(self)\n        scroll_widget.setWidget(content_widget)\n\n        message_len = 0\n        content_layout = QtWidgets.QVBoxLayout(content_widget)\n        for message in messages:\n            label_widget = QtWidgets.QLabel(message, content_widget)\n            content_layout.addWidget(label_widget)\n            message_len = max(message_len, len(message))\n        content_layout.addStretch(1)\n\n        # Set minimum width\n        scroll_widget.setMinimumWidth(360)\n\n        buttons = QtWidgets.QDialogButtonBox.Ok\n        if cancelable:\n            buttons |= QtWidgets.QDialogButtonBox.Cancel\n\n        btn_box = QtWidgets.QDialogButtonBox(buttons)\n        btn_box.accepted.connect(self.accept)\n\n        if cancelable:\n            btn_box.reject.connect(self.reject)\n\n        btn = QtWidgets.QPushButton(\"Copy to clipboard\")\n        btn.clicked.connect(self._on_copy_click)\n        btn_box.addButton(btn, QtWidgets.QDialogButtonBox.NoRole)\n\n        main_layout = QtWidgets.QGridLayout(self)\n        main_layout.addWidget(wrapped_icon, 0, 0,\n            alignment=QtCore.Qt.AlignmentFlag.AlignTop,\n        )\n        main_layout.addWidget(scroll_widget, 0, 1)\n        main_layout.addWidget(btn_box, 1, 1)\n        main_layout.setRowStretch(0, 1)\n        main_layout.setRowStretch(1, 0)\n\n    def _on_copy_click(self):\n        clipboard = QtWidgets.QApplication.clipboard()\n        clipboard.setText(\"\\n\".join(self._messages))\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.SeparatorWidget","title":"<code>SeparatorWidget</code>","text":"<p>               Bases: <code>QFrame</code></p> <p>Prepared widget that can be used as separator with predefined color.</p> <p>Parameters:</p> Name Type Description Default <code>size</code> <code>int</code> <p>Size of separator (width or height).</p> <code>2</code> <code>orientation</code> <code>Horizontal | Vertical</code> <p>Orintation of widget.</p> <code>Horizontal</code> <code>parent</code> <code>QWidget</code> <p>Parent widget.</p> <code>None</code> Source code in <code>client/ayon_core/tools/utils/widgets.py</code> <pre><code>class SeparatorWidget(QtWidgets.QFrame):\n    \"\"\"Prepared widget that can be used as separator with predefined color.\n\n    Args:\n        size (int): Size of separator (width or height).\n        orientation (Qt.Horizontal|Qt.Vertical): Orintation of widget.\n        parent (QtWidgets.QWidget): Parent widget.\n    \"\"\"\n\n    def __init__(self, size=2, orientation=QtCore.Qt.Horizontal, parent=None):\n        super(SeparatorWidget, self).__init__(parent)\n\n        self.setObjectName(\"Separator\")\n\n        maximum_width = self.maximumWidth()\n        maximum_height = self.maximumHeight()\n\n        self._size = None\n        self._orientation = orientation\n        self._maximum_width = maximum_width\n        self._maximum_height = maximum_height\n        self.set_size(size)\n\n    def set_size(self, size):\n        if size != self._size:\n            self._set_size(size)\n\n    def _set_size(self, size):\n        if self._orientation == QtCore.Qt.Vertical:\n            self.setMinimumWidth(size)\n            self.setMaximumWidth(size)\n        else:\n            self.setMinimumHeight(size)\n            self.setMaximumHeight(size)\n\n        self._size = size\n\n    def set_orientation(self, orientation):\n        if self._orientation == orientation:\n            return\n\n        # Reset min/max sizes in opposite direction\n        if self._orientation == QtCore.Qt.Vertical:\n            self.setMinimumHeight(0)\n            self.setMaximumHeight(self._maximum_height)\n        else:\n            self.setMinimumWidth(0)\n            self.setMaximumWidth(self._maximum_width)\n\n        self._orientation = orientation\n\n        self._set_size(self._size)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.SimpleFoldersWidget","title":"<code>SimpleFoldersWidget</code>","text":"<p>               Bases: <code>FoldersWidget</code></p> Source code in <code>client/ayon_core/tools/utils/folders_widget.py</code> <pre><code>class SimpleFoldersWidget(FoldersWidget):\n    def __init__(self, controller=None, *args, **kwargs):\n        if controller is None:\n            controller = SimpleFoldersController()\n        super(SimpleFoldersWidget, self).__init__(controller, *args, **kwargs)\n\n    def set_project_name(self, project_name):\n        self._controller.set_selected_project(project_name)\n        super(SimpleFoldersWidget, self).set_project_name(project_name)\n\n    def _on_project_selection_change(self, event):\n        \"\"\"Ignore project selection change from controller.\n\n        Only who can trigger project change is this widget with\n            'set_project_name' which already cares about project change.\n\n        Args:\n            event (Event): Triggered event.\n        \"\"\"\n        pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.SimplePopup","title":"<code>SimplePopup</code>","text":"<p>               Bases: <code>QDialog</code></p> <p>A Popup that moves itself to bottom right of screen on show event.</p> <p>The UI contains a message label and a red highlighted button to \"show\" or perform another custom action from this pop-up.</p> Source code in <code>client/ayon_core/tools/utils/dialogs.py</code> <pre><code>class SimplePopup(QtWidgets.QDialog):\n    \"\"\"A Popup that moves itself to bottom right of screen on show event.\n\n    The UI contains a message label and a red highlighted button to \"show\"\n    or perform another custom action from this pop-up.\n\n    \"\"\"\n\n    on_clicked = QtCore.Signal()\n\n    def __init__(self, parent=None, *args, **kwargs):\n        super().__init__(parent=parent, *args, **kwargs)\n\n        # Set default title\n        self.setWindowTitle(\"Popup\")\n\n        self.setContentsMargins(0, 0, 0, 0)\n\n        message_label = QtWidgets.QLabel(\"\", self)\n        message_label.setStyleSheet(\"\"\"\n        QLabel {\n            font-size: 12px;\n        }\n        \"\"\")\n        confirm_btn = QtWidgets.QPushButton(\"Show\", self)\n        confirm_btn.setSizePolicy(\n            QtWidgets.QSizePolicy.Maximum,\n            QtWidgets.QSizePolicy.Maximum\n        )\n        confirm_btn.setStyleSheet(\n            \"\"\"QPushButton { background-color: #BB0000 }\"\"\"\n        )\n\n        # Layout\n        layout = QtWidgets.QHBoxLayout(self)\n        layout.setContentsMargins(10, 5, 10, 10)\n\n        # Increase spacing slightly for readability\n        layout.setSpacing(10)\n        layout.addWidget(message_label)\n        layout.addWidget(confirm_btn)\n\n        # Signals\n        confirm_btn.clicked.connect(self._on_clicked)\n\n        # Default size\n        self.resize(400, 40)\n\n        self._message_label = message_label\n        self._confirm_btn = confirm_btn\n\n    def set_message(self, message):\n        self._message_label.setText(message)\n\n    def set_button_text(self, text):\n        self._confirm_btn.setText(text)\n\n    def setMessage(self, message):\n        self.set_message(message)\n\n    def setButtonText(self, text):\n        self.set_button_text(text)\n\n    def showEvent(self, event):\n        # Position popup based on contents on show event\n        geo = self._calculate_window_geometry()\n        self.setGeometry(geo)\n\n        return super().showEvent(event)\n\n    def _on_clicked(self):\n        \"\"\"Callback for when the 'show' button is clicked.\n\n        Raises the parent (if any)\n\n        \"\"\"\n\n        parent = self.parent()\n        self.close()\n\n        # Trigger the signal\n        self.on_clicked.emit()\n\n        if parent:\n            parent.raise_()\n\n    def _calculate_window_geometry(self):\n        \"\"\"Respond to status changes\n\n        On creation, align window with screen bottom right.\n\n        \"\"\"\n\n        window = self\n\n        width = window.width()\n        width = max(width, window.minimumWidth())\n\n        height = window.height()\n        height = max(height, window.sizeHint().height())\n\n        try:\n            screen = window.screen()\n            desktop_geometry = screen.availableGeometry()\n        except AttributeError:\n            # Backwards compatibility for older Qt versions\n            # PySide6 removed QDesktopWidget\n            desktop_geometry = QtWidgets.QDesktopWidget().availableGeometry()\n\n        window_geometry = window.geometry()\n\n        screen_width = window_geometry.width()\n        screen_height = window_geometry.height()\n\n        # Calculate width and height of system tray\n        systray_width = window_geometry.width() - desktop_geometry.width()\n        systray_height = window_geometry.height() - desktop_geometry.height()\n\n        padding = 10\n\n        x = screen_width - width\n        y = screen_height - height\n\n        x -= systray_width + padding\n        y -= systray_height + padding\n\n        return QtCore.QRect(x, y, width, height)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.SquareButton","title":"<code>SquareButton</code>","text":"<p>               Bases: <code>QPushButton</code></p> <p>Make button square shape.</p> <p>Change width to match height on resize.</p> Source code in <code>client/ayon_core/tools/utils/widgets.py</code> <pre><code>class SquareButton(QtWidgets.QPushButton):\n    \"\"\"Make button square shape.\n\n    Change width to match height on resize.\n    \"\"\"\n\n    def __init__(self, *args, **kwargs):\n        super().__init__(*args, **kwargs)\n\n        sp = self.sizePolicy()\n        sp.setVerticalPolicy(QtWidgets.QSizePolicy.Minimum)\n        sp.setHorizontalPolicy(QtWidgets.QSizePolicy.Minimum)\n        self.setSizePolicy(sp)\n        self._ideal_width = None\n\n    def showEvent(self, event):\n        super().showEvent(event)\n        self._ideal_width = self.height()\n        self.updateGeometry()\n\n    def resizeEvent(self, event):\n        super().resizeEvent(event)\n        self._ideal_width = self.height()\n        self.updateGeometry()\n\n    def sizeHint(self):\n        sh = super().sizeHint()\n        ideal_width = self._ideal_width\n        if ideal_width is None:\n            ideal_width = sh.height()\n        sh.setWidth(ideal_width)\n        return sh\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.TasksQtModel","title":"<code>TasksQtModel</code>","text":"<p>               Bases: <code>QStandardItemModel</code></p> <p>Tasks model which cares about refresh of tasks by folder id.</p> <p>Parameters:</p> Name Type Description Default <code>controller</code> <code>AbstractWorkfilesFrontend</code> <p>The control object.</p> required Source code in <code>client/ayon_core/tools/utils/tasks_widget.py</code> <pre><code>class TasksQtModel(QtGui.QStandardItemModel):\n    \"\"\"Tasks model which cares about refresh of tasks by folder id.\n\n    Args:\n        controller (AbstractWorkfilesFrontend): The control object.\n\n    \"\"\"\n    _default_task_icon = None\n    refreshed = QtCore.Signal()\n    project_changed = QtCore.Signal()\n    column_labels = [\"Tasks\"]\n\n    def __init__(self, controller):\n        super().__init__()\n\n        self.setColumnCount(len(self.column_labels))\n        for idx, label in enumerate(self.column_labels):\n            self.setHeaderData(idx, QtCore.Qt.Horizontal, label)\n\n        self._controller = controller\n\n        self._items_by_name = {}\n        self._has_content = False\n        self._is_refreshing = False\n\n        self._invalid_selection_item_used = False\n        self._invalid_selection_item = None\n        self._empty_tasks_item_used = False\n        self._empty_tasks_item = None\n\n        self._last_project_name = None\n        self._last_folder_id = None\n\n        self._refresh_threads = {}\n        self._current_refresh_thread = None\n\n        # Initial state\n        self._add_invalid_selection_item()\n\n    def _clear_items(self):\n        self._items_by_name = {}\n        self._has_content = False\n        self._remove_invalid_items()\n        root_item = self.invisibleRootItem()\n        while root_item.rowCount() != 0:\n            root_item.takeRow(0)\n\n    def refresh(self):\n        \"\"\"Refresh tasks for last project and folder.\"\"\"\n\n        self._refresh(self._last_project_name, self._last_folder_id)\n\n    def set_context(self, project_name, folder_id):\n        \"\"\"Set context for which should be tasks showed.\n\n        Args:\n            project_name (Union[str]): Name of project.\n            folder_id (Union[str, None]): Folder id.\n        \"\"\"\n\n        self._refresh(project_name, folder_id)\n\n    def get_index_by_name(self, task_name):\n        \"\"\"Find item by name and return its index.\n\n        Returns:\n            QtCore.QModelIndex: Index of item. Is invalid if task is not\n                found by name.\n        \"\"\"\n\n        item = self._items_by_name.get(task_name)\n        if item is None:\n            return QtCore.QModelIndex()\n        return self.indexFromItem(item)\n\n    def get_last_project_name(self):\n        \"\"\"Get last refreshed project name.\n\n        Returns:\n            Union[str, None]: Project name.\n        \"\"\"\n\n        return self._last_project_name\n\n    def get_last_folder_id(self):\n        \"\"\"Get last refreshed folder id.\n\n        Returns:\n            Union[str, None]: Folder id.\n        \"\"\"\n\n        return self._last_folder_id\n\n    def _get_invalid_selection_item(self):\n        if self._invalid_selection_item is None:\n            item = QtGui.QStandardItem(\"Select a folder\")\n            item.setFlags(QtCore.Qt.NoItemFlags)\n            icon = get_qt_icon(AwesomeFontIcon(\n                \"fa.times\",\n                color=get_disabled_entity_icon_color(),\n            ))\n            item.setData(icon, QtCore.Qt.DecorationRole)\n            self._invalid_selection_item = item\n        return self._invalid_selection_item\n\n    def _get_empty_task_item(self):\n        if self._empty_tasks_item is None:\n            item = QtGui.QStandardItem(\"No task\")\n            icon = get_qt_icon(AwesomeFontIcon(\n                \"fa.exclamation-circle\",\n                color=get_disabled_entity_icon_color(),\n            ))\n            item.setData(icon, QtCore.Qt.DecorationRole)\n            item.setFlags(QtCore.Qt.NoItemFlags)\n            self._empty_tasks_item = item\n        return self._empty_tasks_item\n\n    def _add_invalid_item(self, item):\n        self._clear_items()\n        root_item = self.invisibleRootItem()\n        root_item.appendRow(item)\n\n    def _remove_invalid_item(self, item):\n        root_item = self.invisibleRootItem()\n        root_item.takeRow(item.row())\n\n    def _remove_invalid_items(self):\n        self._remove_invalid_selection_item()\n        self._remove_empty_task_item()\n\n    def _add_invalid_selection_item(self):\n        if not self._invalid_selection_item_used:\n            self._add_invalid_item(self._get_invalid_selection_item())\n            self._invalid_selection_item_used = True\n\n    def _remove_invalid_selection_item(self):\n        if self._invalid_selection_item:\n            self._remove_invalid_item(self._get_invalid_selection_item())\n            self._invalid_selection_item_used = False\n\n    def _add_empty_task_item(self):\n        if not self._empty_tasks_item_used:\n            self._add_invalid_item(self._get_empty_task_item())\n            self._empty_tasks_item_used = True\n\n    def _remove_empty_task_item(self):\n        if self._empty_tasks_item_used:\n            self._remove_invalid_item(self._get_empty_task_item())\n            self._empty_tasks_item_used = False\n\n    def _refresh(self, project_name, folder_id):\n        project_changed = self._last_project_name != project_name\n        self._is_refreshing = True\n        self._last_project_name = project_name\n        self._last_folder_id = folder_id\n        if project_changed:\n            self.project_changed.emit()\n\n        if not folder_id:\n            self._add_invalid_selection_item()\n            self._current_refresh_thread = None\n            self._is_refreshing = False\n            self.refreshed.emit()\n            return\n\n        thread = self._refresh_threads.get(folder_id)\n        if thread is not None:\n            self._current_refresh_thread = thread\n            return\n        thread = RefreshThread(\n            folder_id,\n            self._thread_getter,\n            project_name,\n            folder_id\n        )\n        self._current_refresh_thread = thread\n        self._refresh_threads[thread.id] = thread\n        thread.refresh_finished.connect(self._on_refresh_thread)\n        thread.start()\n\n    def _thread_getter(self, project_name, folder_id):\n        task_items = self._controller.get_task_items(\n            project_name, folder_id, sender=TASKS_MODEL_SENDER_NAME\n        )\n        task_type_items = {}\n        if hasattr(self._controller, \"get_task_type_items\"):\n            task_type_items = self._controller.get_task_type_items(\n                project_name, sender=TASKS_MODEL_SENDER_NAME\n            )\n        return task_items, task_type_items\n\n    @classmethod\n    def _get_default_task_icon(cls):\n        if cls._default_task_icon is None:\n            cls._default_task_icon = get_qt_icon(AwesomeFontIcon(\n                \"fa.male\",\n                color=get_default_entity_icon_color()\n            ))\n        return cls._default_task_icon\n\n    def _get_task_item_icon(\n        self,\n        task_item,\n        task_type_item_by_name,\n        task_type_icon_cache\n    ):\n        icon = task_type_icon_cache.get(task_item.task_type)\n        if icon is not None:\n            return icon\n\n        task_type_item = task_type_item_by_name.get(\n            task_item.task_type\n        )\n        icon = None\n        if task_type_item is not None:\n            color = task_type_item.color or get_default_entity_icon_color()\n            icon = get_qt_icon(MaterialSymbolsIcon(\n                task_type_item.icon,\n                color=color,\n            ))\n\n        if icon is None:\n            icon = self._get_default_task_icon()\n        task_type_icon_cache[task_item.task_type] = icon\n        return icon\n\n    def _fill_data_from_thread(self, thread):\n        task_items, task_type_items = thread.get_result()\n        # Task items are refreshed\n        if task_items is None:\n            return\n\n        # No tasks are available on folder\n        if not task_items:\n            self._add_empty_task_item()\n            return\n        self._remove_invalid_items()\n\n        task_type_item_by_name = {\n            task_type_item.name: task_type_item\n            for task_type_item in task_type_items\n        }\n        task_type_icon_cache = {}\n        new_items = []\n        new_names = set()\n        for task_item in task_items:\n            name = task_item.name\n            new_names.add(name)\n            item = self._items_by_name.get(name)\n            if item is None:\n                item = QtGui.QStandardItem()\n                item.setEditable(False)\n                new_items.append(item)\n                self._items_by_name[name] = item\n\n            icon = self._get_task_item_icon(\n                task_item,\n                task_type_item_by_name,\n                task_type_icon_cache\n            )\n            item.setData(task_item.full_label, QtCore.Qt.DisplayRole)\n            item.setData(name, ITEM_NAME_ROLE)\n            item.setData(task_item.id, ITEM_ID_ROLE)\n            item.setData(task_item.task_type, TASK_TYPE_ROLE)\n            item.setData(task_item.parent_id, PARENT_ID_ROLE)\n            item.setData(task_item.task_type_order, TASK_TYPE_ORDER_ROLE)\n            item.setData(icon, QtCore.Qt.DecorationRole)\n\n        root_item = self.invisibleRootItem()\n\n        for name in set(self._items_by_name) - new_names:\n            item = self._items_by_name.pop(name)\n            root_item.removeRow(item.row())\n\n        if new_items:\n            root_item.appendRows(new_items)\n\n    def _on_refresh_thread(self, thread_id):\n        \"\"\"Callback when refresh thread is finished.\n\n        Technically can be running multiple refresh threads at the same time,\n        to avoid using values from wrong thread, we check if thread id is\n        current refresh thread id.\n\n        Tasks are stored by name, so if a folder has same task name as\n        previously selected folder it keeps the selection.\n\n        Args:\n            thread_id (str): Thread id.\n        \"\"\"\n        # Make sure to remove thread from '_refresh_threads' dict\n        thread = self._refresh_threads.pop(thread_id)\n        if (\n            self._current_refresh_thread is None\n            or thread_id != self._current_refresh_thread.id\n        ):\n            return\n\n        self._fill_data_from_thread(thread)\n\n        root_item = self.invisibleRootItem()\n        self._has_content = root_item.rowCount() &gt; 0\n        self._current_refresh_thread = None\n        self._is_refreshing = False\n        self.refreshed.emit()\n\n    @property\n    def is_refreshing(self):\n        \"\"\"Model is refreshing.\n\n        Returns:\n            bool: Model is refreshing\n        \"\"\"\n\n        return self._is_refreshing\n\n    @property\n    def has_content(self):\n        \"\"\"Model has content.\n\n        Returns:\n            bools: Have at least one task.\n        \"\"\"\n\n        return self._has_content\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.TasksQtModel.has_content","title":"<code>has_content</code>  <code>property</code>","text":"<p>Model has content.</p> <p>Returns:</p> Name Type Description <code>bools</code> <p>Have at least one task.</p>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.TasksQtModel.is_refreshing","title":"<code>is_refreshing</code>  <code>property</code>","text":"<p>Model is refreshing.</p> <p>Returns:</p> Name Type Description <code>bool</code> <p>Model is refreshing</p>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.TasksQtModel.get_index_by_name","title":"<code>get_index_by_name(task_name)</code>","text":"<p>Find item by name and return its index.</p> <p>Returns:</p> Type Description <p>QtCore.QModelIndex: Index of item. Is invalid if task is not found by name.</p> Source code in <code>client/ayon_core/tools/utils/tasks_widget.py</code> <pre><code>def get_index_by_name(self, task_name):\n    \"\"\"Find item by name and return its index.\n\n    Returns:\n        QtCore.QModelIndex: Index of item. Is invalid if task is not\n            found by name.\n    \"\"\"\n\n    item = self._items_by_name.get(task_name)\n    if item is None:\n        return QtCore.QModelIndex()\n    return self.indexFromItem(item)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.TasksQtModel.get_last_folder_id","title":"<code>get_last_folder_id()</code>","text":"<p>Get last refreshed folder id.</p> <p>Returns:</p> Type Description <p>Union[str, None]: Folder id.</p> Source code in <code>client/ayon_core/tools/utils/tasks_widget.py</code> <pre><code>def get_last_folder_id(self):\n    \"\"\"Get last refreshed folder id.\n\n    Returns:\n        Union[str, None]: Folder id.\n    \"\"\"\n\n    return self._last_folder_id\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.TasksQtModel.get_last_project_name","title":"<code>get_last_project_name()</code>","text":"<p>Get last refreshed project name.</p> <p>Returns:</p> Type Description <p>Union[str, None]: Project name.</p> Source code in <code>client/ayon_core/tools/utils/tasks_widget.py</code> <pre><code>def get_last_project_name(self):\n    \"\"\"Get last refreshed project name.\n\n    Returns:\n        Union[str, None]: Project name.\n    \"\"\"\n\n    return self._last_project_name\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.TasksQtModel.refresh","title":"<code>refresh()</code>","text":"<p>Refresh tasks for last project and folder.</p> Source code in <code>client/ayon_core/tools/utils/tasks_widget.py</code> <pre><code>def refresh(self):\n    \"\"\"Refresh tasks for last project and folder.\"\"\"\n\n    self._refresh(self._last_project_name, self._last_folder_id)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.TasksQtModel.set_context","title":"<code>set_context(project_name, folder_id)</code>","text":"<p>Set context for which should be tasks showed.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>Union[str]</code> <p>Name of project.</p> required <code>folder_id</code> <code>Union[str, None]</code> <p>Folder id.</p> required Source code in <code>client/ayon_core/tools/utils/tasks_widget.py</code> <pre><code>def set_context(self, project_name, folder_id):\n    \"\"\"Set context for which should be tasks showed.\n\n    Args:\n        project_name (Union[str]): Name of project.\n        folder_id (Union[str, None]): Folder id.\n    \"\"\"\n\n    self._refresh(project_name, folder_id)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.TasksWidget","title":"<code>TasksWidget</code>","text":"<p>               Bases: <code>QWidget</code></p> <p>Tasks widget.</p> <p>Widget that handles tasks view, model and selection.</p> <p>Parameters:</p> Name Type Description Default <code>controller</code> <code>AbstractWorkfilesFrontend</code> <p>Workfiles controller.</p> required <code>parent</code> <code>QWidget</code> <p>Parent widget.</p> required <code>handle_expected_selection</code> <code>Optional[bool]</code> <p>Handle expected selection.</p> <code>False</code> Source code in <code>client/ayon_core/tools/utils/tasks_widget.py</code> <pre><code>class TasksWidget(QtWidgets.QWidget):\n    \"\"\"Tasks widget.\n\n    Widget that handles tasks view, model and selection.\n\n    Args:\n        controller (AbstractWorkfilesFrontend): Workfiles controller.\n        parent (QtWidgets.QWidget): Parent widget.\n        handle_expected_selection (Optional[bool]): Handle expected selection.\n    \"\"\"\n    log = Logger.get_logger(\"TasksWidget\")\n\n    refreshed = QtCore.Signal()\n    selection_changed = QtCore.Signal()\n\n    def __init__(self, controller, parent, handle_expected_selection=False):\n        super().__init__(parent)\n\n        tasks_view = DeselectableTreeView(self)\n        tasks_view.setIndentation(0)\n\n        tasks_model = TasksQtModel(controller)\n        tasks_proxy_model = TasksProxyModel()\n        tasks_proxy_model.setSourceModel(tasks_model)\n        tasks_proxy_model.setSortCaseSensitivity(QtCore.Qt.CaseInsensitive)\n\n        tasks_view.setModel(tasks_proxy_model)\n\n        main_layout = QtWidgets.QHBoxLayout(self)\n        main_layout.setContentsMargins(0, 0, 0, 0)\n        main_layout.addWidget(tasks_view, 1)\n\n        controller.register_event_callback(\n            \"tasks.refresh.finished\",\n            self._on_tasks_refresh_finished\n        )\n        controller.register_event_callback(\n            \"selection.folder.changed\",\n            self._folder_selection_changed\n        )\n        controller.register_event_callback(\n            \"expected_selection_changed\",\n            self._on_expected_selection_change\n        )\n\n        selection_model = tasks_view.selectionModel()\n        selection_model.selectionChanged.connect(self._on_selection_change)\n\n        tasks_model.refreshed.connect(self._on_tasks_model_refresh)\n        tasks_model.project_changed.connect(self._on_tasks_project_change)\n\n        self._controller = controller\n        self._tasks_view = tasks_view\n        self._tasks_model = tasks_model\n        self._tasks_proxy_model = tasks_proxy_model\n\n        self._selected_folder_id = None\n\n        self._handle_expected_selection = handle_expected_selection\n        self._expected_selection_data = None\n\n    def refresh(self):\n        \"\"\"Refresh folders for last selected project.\n\n        Force to update folders model from controller. This may or may not\n        trigger query from server, that's based on controller's cache.\n        \"\"\"\n        self._tasks_model.refresh()\n\n    def get_selected_task_info(self):\n        \"\"\"Get selected task info.\n\n        Example output::\n\n            {\n                \"task_id\": \"5e7e3e3e3e3e3e3e3e3e3e3e\",\n                \"task_name\": \"modeling\",\n                \"task_type\": \"Modeling\"\n            }\n\n        Returns:\n            dict[str, Union[str, None]]: Task info.\n\n        \"\"\"\n        _, task_id, task_name, task_type = self._get_selected_item_ids()\n        return {\n            \"task_id\": task_id,\n            \"task_name\": task_name,\n            \"task_type\": task_type,\n        }\n\n    def get_selected_task_id(self):\n        \"\"\"Get selected task id.\n\n        Returns:\n            Union[str, None]: Task id.\n\n        \"\"\"\n        return self.get_selected_task_info()[\"task_id\"]\n\n    def get_selected_task_name(self):\n        \"\"\"Get selected task name.\n\n        Returns:\n            Union[str, None]: Task name.\n        \"\"\"\n\n        return self.get_selected_task_info()[\"task_name\"]\n\n    def get_selected_task_type(self):\n        \"\"\"Get selected task type.\n\n        Returns:\n            Union[str, None]: Task type.\n\n        \"\"\"\n        return self.get_selected_task_info()[\"task_type\"]\n\n    def set_selected_task(self, task_name):\n        \"\"\"Set selected task by name.\n\n        Args:\n            task_name (str): Task name.\n\n        Returns:\n            bool: Task was selected.\n\n        \"\"\"\n        if task_name is None:\n            self._tasks_view.clearSelection()\n            return True\n\n        if task_name == self.get_selected_task_name():\n            return True\n        index = self._tasks_model.get_index_by_name(task_name)\n        if not index.isValid():\n            return False\n\n        proxy_index = self._tasks_proxy_model.mapFromSource(index)\n        if not proxy_index.isValid():\n            return False\n\n        selection_model = self._folders_view.selectionModel()\n        selection_model.setCurrentIndex(\n            proxy_index, QtCore.QItemSelectionModel.SelectCurrent\n        )\n        return True\n\n    def set_task_ids_filter(self, task_ids: Optional[list[str]]):\n        \"\"\"Set filter of folder ids.\n\n        Args:\n            task_ids (list[str]): The list of folder ids.\n\n        \"\"\"\n        self._tasks_proxy_model.set_task_ids_filter(task_ids)\n\n    def _on_tasks_refresh_finished(self, event):\n        \"\"\"Tasks were refreshed in controller.\n\n        Ignore if refresh was triggered by tasks model, or refreshed folder is\n        not the same as currently selected folder.\n\n        Args:\n            event (Event): Event object.\n        \"\"\"\n\n        # Refresh only if current folder id is the same\n        if (\n            event[\"sender\"] == TASKS_MODEL_SENDER_NAME\n            or event[\"folder_id\"] != self._selected_folder_id\n        ):\n            return\n\n        self._tasks_model.set_context(\n            event[\"project_name\"], self._selected_folder_id\n        )\n\n    def _folder_selection_changed(self, event):\n        self._selected_folder_id = event[\"folder_id\"]\n        self._tasks_model.set_context(\n            event[\"project_name\"], self._selected_folder_id\n        )\n\n    def _on_tasks_model_refresh(self):\n        if not self._set_expected_selection():\n            self._on_selection_change()\n\n        self._update_task_type_sorting()\n        self.refreshed.emit()\n\n    def _on_tasks_project_change(self):\n        self._update_task_type_sorting()\n\n    def _get_selected_item_ids(self):\n        selection_model = self._tasks_view.selectionModel()\n        for index in selection_model.selectedIndexes():\n            task_id = index.data(ITEM_ID_ROLE)\n            task_name = index.data(ITEM_NAME_ROLE)\n            task_type = index.data(TASK_TYPE_ROLE)\n            parent_id = index.data(PARENT_ID_ROLE)\n            if task_name is not None:\n                return parent_id, task_id, task_name, task_type\n        return self._selected_folder_id, None, None, None\n\n    def _on_selection_change(self):\n        # Don't trigger task change during refresh\n        #   - a task was deselected if that happens\n        #   - can cause crash triggered during tasks refreshing\n        if self._tasks_model.is_refreshing:\n            return\n\n        _parent_id, task_id, task_name, _ = self._get_selected_item_ids()\n        self._controller.set_selected_task(task_id, task_name)\n        self.selection_changed.emit()\n\n    # Expected selection handling\n    def _on_expected_selection_change(self, event):\n        self._update_expected_selection(event.data)\n\n    def _set_expected_selection(self):\n        if not self._handle_expected_selection:\n            return False\n\n        if self._expected_selection_data is None:\n            return False\n        folder_id = self._expected_selection_data[\"folder_id\"]\n        task_name = self._expected_selection_data[\"task_name\"]\n        self._expected_selection_data = None\n        model_folder_id = self._tasks_model.get_last_folder_id()\n        if folder_id != model_folder_id:\n            return False\n        if task_name is not None:\n            index = self._tasks_model.get_index_by_name(task_name)\n            if index.isValid():\n                proxy_index = self._tasks_proxy_model.mapFromSource(index)\n                self._tasks_view.setCurrentIndex(proxy_index)\n        self._controller.expected_task_selected(folder_id, task_name)\n        return True\n\n    def _update_task_type_sorting(self):\n        project_name = self._tasks_model.get_last_project_name()\n        if project_name is None:\n            return\n\n        use_task_type_sorting = False\n        if hasattr(self._controller, \"get_project_settings\"):\n            settings = self._controller.get_project_settings(project_name)\n            use_task_type_sorting = (\n                settings[\"core\"][\"tools\"][\"general\"][\"use_task_type_sorting\"]\n            )\n        else:\n            self.log.warning(\n                f\"Controller '{self._controller}' doesn't have\"\n                \" 'get_project_settings' method, task type\"\n                \" sorting will be disabled.\"\n            )\n\n        self._tasks_proxy_model.set_task_type_sorting_enabled(\n            use_task_type_sorting\n        )\n        self._tasks_proxy_model.sort(0)\n\n    def _update_expected_selection(self, expected_data=None):\n        if not self._handle_expected_selection:\n            return\n        if expected_data is None:\n            expected_data = self._controller.get_expected_selection_data()\n\n        folder_data = expected_data.get(\"folder\")\n        task_data = expected_data.get(\"task\")\n        if (\n            not folder_data\n            or not task_data\n            or not task_data[\"current\"]\n        ):\n            return\n        folder_id = folder_data[\"id\"]\n        self._expected_selection_data = {\n            \"task_name\": task_data[\"name\"],\n            \"folder_id\": folder_id,\n        }\n        model_folder_id = self._tasks_model.get_last_folder_id()\n        if folder_id != model_folder_id or self._tasks_model.is_refreshing:\n            return\n        self._set_expected_selection()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.TasksWidget.get_selected_task_id","title":"<code>get_selected_task_id()</code>","text":"<p>Get selected task id.</p> <p>Returns:</p> Type Description <p>Union[str, None]: Task id.</p> Source code in <code>client/ayon_core/tools/utils/tasks_widget.py</code> <pre><code>def get_selected_task_id(self):\n    \"\"\"Get selected task id.\n\n    Returns:\n        Union[str, None]: Task id.\n\n    \"\"\"\n    return self.get_selected_task_info()[\"task_id\"]\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.TasksWidget.get_selected_task_info","title":"<code>get_selected_task_info()</code>","text":"<p>Get selected task info.</p> <p>Example output::</p> <pre><code>{\n    \"task_id\": \"5e7e3e3e3e3e3e3e3e3e3e3e\",\n    \"task_name\": \"modeling\",\n    \"task_type\": \"Modeling\"\n}\n</code></pre> <p>Returns:</p> Type Description <p>dict[str, Union[str, None]]: Task info.</p> Source code in <code>client/ayon_core/tools/utils/tasks_widget.py</code> <pre><code>def get_selected_task_info(self):\n    \"\"\"Get selected task info.\n\n    Example output::\n\n        {\n            \"task_id\": \"5e7e3e3e3e3e3e3e3e3e3e3e\",\n            \"task_name\": \"modeling\",\n            \"task_type\": \"Modeling\"\n        }\n\n    Returns:\n        dict[str, Union[str, None]]: Task info.\n\n    \"\"\"\n    _, task_id, task_name, task_type = self._get_selected_item_ids()\n    return {\n        \"task_id\": task_id,\n        \"task_name\": task_name,\n        \"task_type\": task_type,\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.TasksWidget.get_selected_task_name","title":"<code>get_selected_task_name()</code>","text":"<p>Get selected task name.</p> <p>Returns:</p> Type Description <p>Union[str, None]: Task name.</p> Source code in <code>client/ayon_core/tools/utils/tasks_widget.py</code> <pre><code>def get_selected_task_name(self):\n    \"\"\"Get selected task name.\n\n    Returns:\n        Union[str, None]: Task name.\n    \"\"\"\n\n    return self.get_selected_task_info()[\"task_name\"]\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.TasksWidget.get_selected_task_type","title":"<code>get_selected_task_type()</code>","text":"<p>Get selected task type.</p> <p>Returns:</p> Type Description <p>Union[str, None]: Task type.</p> Source code in <code>client/ayon_core/tools/utils/tasks_widget.py</code> <pre><code>def get_selected_task_type(self):\n    \"\"\"Get selected task type.\n\n    Returns:\n        Union[str, None]: Task type.\n\n    \"\"\"\n    return self.get_selected_task_info()[\"task_type\"]\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.TasksWidget.refresh","title":"<code>refresh()</code>","text":"<p>Refresh folders for last selected project.</p> <p>Force to update folders model from controller. This may or may not trigger query from server, that's based on controller's cache.</p> Source code in <code>client/ayon_core/tools/utils/tasks_widget.py</code> <pre><code>def refresh(self):\n    \"\"\"Refresh folders for last selected project.\n\n    Force to update folders model from controller. This may or may not\n    trigger query from server, that's based on controller's cache.\n    \"\"\"\n    self._tasks_model.refresh()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.TasksWidget.set_selected_task","title":"<code>set_selected_task(task_name)</code>","text":"<p>Set selected task by name.</p> <p>Parameters:</p> Name Type Description Default <code>task_name</code> <code>str</code> <p>Task name.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <p>Task was selected.</p> Source code in <code>client/ayon_core/tools/utils/tasks_widget.py</code> <pre><code>def set_selected_task(self, task_name):\n    \"\"\"Set selected task by name.\n\n    Args:\n        task_name (str): Task name.\n\n    Returns:\n        bool: Task was selected.\n\n    \"\"\"\n    if task_name is None:\n        self._tasks_view.clearSelection()\n        return True\n\n    if task_name == self.get_selected_task_name():\n        return True\n    index = self._tasks_model.get_index_by_name(task_name)\n    if not index.isValid():\n        return False\n\n    proxy_index = self._tasks_proxy_model.mapFromSource(index)\n    if not proxy_index.isValid():\n        return False\n\n    selection_model = self._folders_view.selectionModel()\n    selection_model.setCurrentIndex(\n        proxy_index, QtCore.QItemSelectionModel.SelectCurrent\n    )\n    return True\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.TasksWidget.set_task_ids_filter","title":"<code>set_task_ids_filter(task_ids)</code>","text":"<p>Set filter of folder ids.</p> <p>Parameters:</p> Name Type Description Default <code>task_ids</code> <code>list[str]</code> <p>The list of folder ids.</p> required Source code in <code>client/ayon_core/tools/utils/tasks_widget.py</code> <pre><code>def set_task_ids_filter(self, task_ids: Optional[list[str]]):\n    \"\"\"Set filter of folder ids.\n\n    Args:\n        task_ids (list[str]): The list of folder ids.\n\n    \"\"\"\n    self._tasks_proxy_model.set_task_ids_filter(task_ids)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.ThumbnailPainterWidget","title":"<code>ThumbnailPainterWidget</code>","text":"<p>               Bases: <code>QWidget</code></p> <p>Widget for painting of thumbnails.</p> <p>The widget use is to paint thumbnail or multiple thumbnails in a defined area. Is not meant to show them in a grid but in overlay.</p> <p>It is expected that there is a logic that will provide thumbnails to paint and set them using 'set_current_thumbnails' or 'set_current_thumbnail_paths'.</p> Source code in <code>client/ayon_core/tools/utils/thumbnail_paint_widget.py</code> <pre><code>class ThumbnailPainterWidget(QtWidgets.QWidget):\n    \"\"\"Widget for painting of thumbnails.\n\n    The widget use is to paint thumbnail or multiple thumbnails in a defined\n    area. Is not meant to show them in a grid but in overlay.\n\n    It is expected that there is a logic that will provide thumbnails to\n    paint and set them using 'set_current_thumbnails' or\n    'set_current_thumbnail_paths'.\n    \"\"\"\n\n    width_ratio = 3.0\n    height_ratio = 2.0\n    border_width = 1\n    max_thumbnails = 3\n    offset_sep = 4\n    checker_boxes_count = 20\n\n    def __init__(self, parent):\n        super(ThumbnailPainterWidget, self).__init__(parent)\n\n        border_color = get_objected_colors(\"bg-buttons\").get_qcolor()\n        thumbnail_bg_color = get_objected_colors(\"bg-view\").get_qcolor()\n\n        default_image = get_image(\"thumbnail.png\")\n        default_pix = paint_image_with_color(default_image, border_color)\n\n        self._border_color = border_color\n        self._thumbnail_bg_color = thumbnail_bg_color\n        self._default_pix = default_pix\n\n        self._cached_pix = None\n        self._current_pixes = None\n        self._has_pixes = False\n\n        self._bg_color = QtCore.Qt.transparent\n        self._use_checker = True\n        self._checker_color_1 = QtGui.QColor(89, 89, 89)\n        self._checker_color_2 = QtGui.QColor(188, 187, 187)\n\n    def set_background_color(self, color):\n        self._bg_color = color\n        self.repaint()\n\n    def set_use_checkboard(self, use_checker):\n        if self._use_checker is use_checker:\n            return\n        self._use_checker = use_checker\n        self.repaint()\n\n    def set_checker_colors(self, color_1, color_2):\n        self._checker_color_1 = color_1\n        self._checker_color_2 = color_2\n        self.repaint()\n\n    def set_border_color(self, color):\n        \"\"\"Change border color.\n\n        Args:\n            color (QtGui.QColor): Color to set.\n        \"\"\"\n\n        self._border_color = color\n        self._default_pix = None\n        self.clear_cache()\n\n    def set_thumbnail_bg_color(self, color):\n        \"\"\"Change background color.\n\n        Args:\n            color (QtGui.QColor): Color to set.\n        \"\"\"\n\n        self._thumbnail_bg_color = color\n        self.clear_cache()\n\n    @property\n    def has_pixes(self):\n        \"\"\"Has set thumbnails.\n\n        Returns:\n            bool: True if widget has thumbnails to paint.\n        \"\"\"\n\n        return self._has_pixes\n\n    def clear_cache(self):\n        \"\"\"Clear cache of resized thumbnails and repaint widget.\"\"\"\n\n        self._cached_pix = None\n        self.repaint()\n\n    def set_current_thumbnails(self, pixmaps=None):\n        \"\"\"Set current thumbnails.\n\n        Args:\n            pixmaps (Optional[List[QtGui.QPixmap]]): List of pixmaps.\n        \"\"\"\n\n        self._current_pixes = pixmaps or None\n        self._has_pixes = self._current_pixes is not None\n        self.clear_cache()\n\n    def set_current_thumbnail_paths(self, thumbnail_paths=None):\n        \"\"\"Set current thumbnails.\n\n        Set current thumbnails using paths to a files.\n\n        Args:\n            thumbnail_paths (Optional[List[str]]): List of paths to thumbnail\n                sources.\n        \"\"\"\n\n        pixes = []\n        if thumbnail_paths:\n            for thumbnail_path in thumbnail_paths:\n                pixes.append(QtGui.QPixmap(thumbnail_path))\n\n        self.set_current_thumbnails(pixes)\n\n    def paintEvent(self, event):\n        if self._cached_pix is None:\n            self._cache_pix()\n\n        painter = QtGui.QPainter()\n        painter.begin(self)\n        painter.setRenderHint(QtGui.QPainter.Antialiasing)\n        painter.drawPixmap(0, 0, self._cached_pix)\n        painter.end()\n\n    def resizeEvent(self, event):\n        self._cached_pix = None\n        super(ThumbnailPainterWidget, self).resizeEvent(event)\n\n    def _get_default_pix(self):\n        if self._default_pix is None:\n            default_image = get_image(\"thumbnail\")\n            default_pix = paint_image_with_color(\n                default_image, self._border_color)\n            self._default_pix = default_pix\n        return self._default_pix\n\n    def _paint_tile(self, width, height):\n        if not self._use_checker:\n            tile_pix = QtGui.QPixmap(width, width)\n            tile_pix.fill(self._bg_color)\n            return tile_pix\n\n        checker_size = int(float(width) / self.checker_boxes_count)\n        if checker_size &lt; 1:\n            checker_size = 1\n\n        checker_pix = QtGui.QPixmap(checker_size * 2, checker_size * 2)\n        checker_pix.fill(QtCore.Qt.transparent)\n        checker_painter = QtGui.QPainter()\n        checker_painter.begin(checker_pix)\n        checker_painter.setPen(QtCore.Qt.NoPen)\n        checker_painter.setBrush(self._checker_color_1)\n        checker_painter.drawRect(\n            0, 0, checker_pix.width(), checker_pix.height()\n        )\n        checker_painter.setBrush(self._checker_color_2)\n        checker_painter.drawRect(\n            0, 0, checker_size, checker_size\n        )\n        checker_painter.drawRect(\n            checker_size, checker_size, checker_size, checker_size\n        )\n        checker_painter.end()\n        return checker_pix\n\n    def _paint_default_pix(self, pix_width, pix_height):\n        full_border_width = 2 * self.border_width\n        width = pix_width - full_border_width\n        height = pix_height - full_border_width\n        if width &gt; 100:\n            width = int(width * 0.6)\n            height = int(height * 0.6)\n\n        scaled_pix = self._get_default_pix().scaled(\n            width,\n            height,\n            QtCore.Qt.KeepAspectRatio,\n            QtCore.Qt.SmoothTransformation\n        )\n        pos_x = int(\n            (pix_width - scaled_pix.width()) / 2\n        )\n        pos_y = int(\n            (pix_height - scaled_pix.height()) / 2\n        )\n        new_pix = QtGui.QPixmap(pix_width, pix_height)\n        new_pix.fill(QtCore.Qt.transparent)\n        pix_painter = QtGui.QPainter()\n        pix_painter.begin(new_pix)\n        render_hints = (\n            QtGui.QPainter.Antialiasing\n            | QtGui.QPainter.SmoothPixmapTransform\n        )\n\n        pix_painter.setRenderHints(render_hints)\n        pix_painter.drawPixmap(pos_x, pos_y, scaled_pix)\n        pix_painter.end()\n        return new_pix\n\n    def _draw_thumbnails(self, thumbnails, pix_width, pix_height):\n        full_border_width = 2 * self.border_width\n\n        checker_pix = self._paint_tile(pix_width, pix_height)\n\n        backgrounded_images = []\n        for src_pix in thumbnails:\n            scaled_pix = src_pix.scaled(\n                pix_width - full_border_width,\n                pix_height - full_border_width,\n                QtCore.Qt.KeepAspectRatio,\n                QtCore.Qt.SmoothTransformation\n            )\n            pos_x = int(\n                (pix_width - scaled_pix.width()) / 2\n            )\n            pos_y = int(\n                (pix_height - scaled_pix.height()) / 2\n            )\n\n            new_pix = QtGui.QPixmap(pix_width, pix_height)\n            new_pix.fill(QtCore.Qt.transparent)\n            pix_painter = QtGui.QPainter()\n            pix_painter.begin(new_pix)\n            render_hints = (\n                QtGui.QPainter.Antialiasing\n                | QtGui.QPainter.SmoothPixmapTransform\n            )\n            pix_painter.setRenderHints(render_hints)\n\n            tiled_rect = QtCore.QRectF(\n                pos_x, pos_y, scaled_pix.width(), scaled_pix.height()\n            )\n            pix_painter.drawTiledPixmap(\n                tiled_rect,\n                checker_pix,\n                QtCore.QPointF(0.0, 0.0)\n            )\n            pix_painter.drawPixmap(pos_x, pos_y, scaled_pix)\n            pix_painter.end()\n            backgrounded_images.append(new_pix)\n        return backgrounded_images\n\n    def _paint_dash_line(self, painter, rect):\n        pen = QtGui.QPen()\n        pen.setWidth(1)\n        pen.setBrush(QtCore.Qt.darkGray)\n        pen.setStyle(QtCore.Qt.DashLine)\n\n        new_rect = rect.adjusted(1, 1, -1, -1)\n        painter.setPen(pen)\n        painter.setBrush(QtCore.Qt.transparent)\n        # painter.drawRect(rect)\n        painter.drawRect(new_rect)\n\n    def _cache_pix(self):\n        rect = self.rect()\n        rect_width = rect.width()\n        rect_height = rect.height()\n\n        pix_x_offset = 0\n        pix_y_offset = 0\n        expected_height = int(\n            (rect_width / self.width_ratio) * self.height_ratio\n        )\n        if expected_height &gt; rect_height:\n            expected_height = rect_height\n            expected_width = int(\n                (rect_height / self.height_ratio) * self.width_ratio\n            )\n            pix_x_offset = (rect_width - expected_width) / 2\n        else:\n            expected_width = rect_width\n            pix_y_offset = (rect_height - expected_height) / 2\n\n        if self._current_pixes is None:\n            used_default_pix = True\n            pixes_to_draw = None\n            pixes_len = 1\n        else:\n            used_default_pix = False\n            pixes_to_draw = self._current_pixes\n            if len(pixes_to_draw) &gt; self.max_thumbnails:\n                pixes_to_draw = pixes_to_draw[:-self.max_thumbnails]\n            pixes_len = len(pixes_to_draw)\n\n        width_offset, height_offset = self._get_pix_offset_size(\n            expected_width, expected_height, pixes_len\n        )\n        pix_width = expected_width - width_offset\n        pix_height = expected_height - height_offset\n\n        if used_default_pix:\n            thumbnail_images = [self._paint_default_pix(pix_width, pix_height)]\n        else:\n            thumbnail_images = self._draw_thumbnails(\n                pixes_to_draw, pix_width, pix_height\n            )\n\n        if pixes_len == 1:\n            width_offset_part = 0\n            height_offset_part = 0\n        else:\n            width_offset_part = int(float(width_offset) / (pixes_len - 1))\n            height_offset_part = int(float(height_offset) / (pixes_len - 1))\n        full_width_offset = width_offset + pix_x_offset\n\n        final_pix = QtGui.QPixmap(rect_width, rect_height)\n        final_pix.fill(QtCore.Qt.transparent)\n\n        bg_pen = QtGui.QPen()\n        bg_pen.setWidth(self.border_width)\n        bg_pen.setColor(self._border_color)\n\n        final_painter = QtGui.QPainter()\n        final_painter.begin(final_pix)\n        render_hints = (\n            QtGui.QPainter.Antialiasing\n            | QtGui.QPainter.SmoothPixmapTransform\n        )\n\n        final_painter.setRenderHints(render_hints)\n\n        final_painter.setBrush(QtGui.QBrush(self._thumbnail_bg_color))\n        final_painter.setPen(bg_pen)\n        final_painter.drawRect(rect)\n\n        for idx, pix in enumerate(thumbnail_images):\n            x_offset = full_width_offset - (width_offset_part * idx)\n            y_offset = (height_offset_part * idx) + pix_y_offset\n            final_painter.drawPixmap(x_offset, y_offset, pix)\n\n        # Draw drop enabled dashes\n        if used_default_pix:\n            self._paint_dash_line(final_painter, rect)\n\n        final_painter.end()\n\n        self._cached_pix = final_pix\n\n    def _get_pix_offset_size(self, width, height, image_count):\n        if image_count == 1:\n            return 0, 0\n\n        part_width = width / self.offset_sep\n        part_height = height / self.offset_sep\n        return part_width, part_height\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.ThumbnailPainterWidget.has_pixes","title":"<code>has_pixes</code>  <code>property</code>","text":"<p>Has set thumbnails.</p> <p>Returns:</p> Name Type Description <code>bool</code> <p>True if widget has thumbnails to paint.</p>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.ThumbnailPainterWidget.clear_cache","title":"<code>clear_cache()</code>","text":"<p>Clear cache of resized thumbnails and repaint widget.</p> Source code in <code>client/ayon_core/tools/utils/thumbnail_paint_widget.py</code> <pre><code>def clear_cache(self):\n    \"\"\"Clear cache of resized thumbnails and repaint widget.\"\"\"\n\n    self._cached_pix = None\n    self.repaint()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.ThumbnailPainterWidget.set_border_color","title":"<code>set_border_color(color)</code>","text":"<p>Change border color.</p> <p>Parameters:</p> Name Type Description Default <code>color</code> <code>QColor</code> <p>Color to set.</p> required Source code in <code>client/ayon_core/tools/utils/thumbnail_paint_widget.py</code> <pre><code>def set_border_color(self, color):\n    \"\"\"Change border color.\n\n    Args:\n        color (QtGui.QColor): Color to set.\n    \"\"\"\n\n    self._border_color = color\n    self._default_pix = None\n    self.clear_cache()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.ThumbnailPainterWidget.set_current_thumbnail_paths","title":"<code>set_current_thumbnail_paths(thumbnail_paths=None)</code>","text":"<p>Set current thumbnails.</p> <p>Set current thumbnails using paths to a files.</p> <p>Parameters:</p> Name Type Description Default <code>thumbnail_paths</code> <code>Optional[List[str]]</code> <p>List of paths to thumbnail sources.</p> <code>None</code> Source code in <code>client/ayon_core/tools/utils/thumbnail_paint_widget.py</code> <pre><code>def set_current_thumbnail_paths(self, thumbnail_paths=None):\n    \"\"\"Set current thumbnails.\n\n    Set current thumbnails using paths to a files.\n\n    Args:\n        thumbnail_paths (Optional[List[str]]): List of paths to thumbnail\n            sources.\n    \"\"\"\n\n    pixes = []\n    if thumbnail_paths:\n        for thumbnail_path in thumbnail_paths:\n            pixes.append(QtGui.QPixmap(thumbnail_path))\n\n    self.set_current_thumbnails(pixes)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.ThumbnailPainterWidget.set_current_thumbnails","title":"<code>set_current_thumbnails(pixmaps=None)</code>","text":"<p>Set current thumbnails.</p> <p>Parameters:</p> Name Type Description Default <code>pixmaps</code> <code>Optional[List[QPixmap]]</code> <p>List of pixmaps.</p> <code>None</code> Source code in <code>client/ayon_core/tools/utils/thumbnail_paint_widget.py</code> <pre><code>def set_current_thumbnails(self, pixmaps=None):\n    \"\"\"Set current thumbnails.\n\n    Args:\n        pixmaps (Optional[List[QtGui.QPixmap]]): List of pixmaps.\n    \"\"\"\n\n    self._current_pixes = pixmaps or None\n    self._has_pixes = self._current_pixes is not None\n    self.clear_cache()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.ThumbnailPainterWidget.set_thumbnail_bg_color","title":"<code>set_thumbnail_bg_color(color)</code>","text":"<p>Change background color.</p> <p>Parameters:</p> Name Type Description Default <code>color</code> <code>QColor</code> <p>Color to set.</p> required Source code in <code>client/ayon_core/tools/utils/thumbnail_paint_widget.py</code> <pre><code>def set_thumbnail_bg_color(self, color):\n    \"\"\"Change background color.\n\n    Args:\n        color (QtGui.QColor): Color to set.\n    \"\"\"\n\n    self._thumbnail_bg_color = color\n    self.clear_cache()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.TreeView","title":"<code>TreeView</code>","text":"<p>               Bases: <code>QTreeView</code></p> <p>Ultimate TreeView with flick charm and double click signals.</p> <p>Tree view have deselectable mode, which allows to deselect items by clicking on item area without any items.</p> Todos <p>Add refresh animation.</p> Source code in <code>client/ayon_core/tools/utils/views.py</code> <pre><code>class TreeView(QtWidgets.QTreeView):\n    \"\"\"Ultimate TreeView with flick charm and double click signals.\n\n    Tree view have deselectable mode, which allows to deselect items by\n    clicking on item area without any items.\n\n    Todos:\n        Add refresh animation.\n    \"\"\"\n\n    double_clicked = QtCore.Signal(QtGui.QMouseEvent)\n\n    def __init__(self, *args, **kwargs):\n        super().__init__(*args, **kwargs)\n        self.setIndentation(20)\n\n        self._deselectable = False\n\n        self._flick_charm_activated = False\n        self._flick_charm = FlickCharm(parent=self)\n        self._before_flick_scroll_mode = None\n\n    def is_deselectable(self):\n        return self._deselectable\n\n    def set_deselectable(self, deselectable):\n        self._deselectable = deselectable\n\n    deselectable = property(is_deselectable, set_deselectable)\n\n    def mousePressEvent(self, event):\n        if self._deselectable:\n            index = self.indexAt(event.pos())\n            if not index.isValid():\n                # clear the selection\n                self.clearSelection()\n                # clear the current index\n                self.setCurrentIndex(QtCore.QModelIndex())\n        super().mousePressEvent(event)\n\n    def mouseDoubleClickEvent(self, event):\n        self.double_clicked.emit(event)\n\n        return super().mouseDoubleClickEvent(event)\n\n    def activate_flick_charm(self):\n        if self._flick_charm_activated:\n            return\n        self._flick_charm_activated = True\n        self._before_flick_scroll_mode = self.verticalScrollMode()\n        self._flick_charm.activateOn(self)\n        self.setVerticalScrollMode(QtWidgets.QAbstractItemView.ScrollPerPixel)\n\n    def deactivate_flick_charm(self):\n        if not self._flick_charm_activated:\n            return\n        self._flick_charm_activated = False\n        self._flick_charm.deactivateFrom(self)\n        if self._before_flick_scroll_mode is not None:\n            self.setVerticalScrollMode(self._before_flick_scroll_mode)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.VerticalExpandButton","title":"<code>VerticalExpandButton</code>","text":"<p>               Bases: <code>QPushButton</code></p> <p>Button which is expanding vertically.</p> <p>By default, button is a little bit smaller than other widgets like     QLineEdit. This button is expanding vertically to match size of     other widgets, next to it.</p> Source code in <code>client/ayon_core/tools/utils/widgets.py</code> <pre><code>class VerticalExpandButton(QtWidgets.QPushButton):\n    \"\"\"Button which is expanding vertically.\n\n    By default, button is a little bit smaller than other widgets like\n        QLineEdit. This button is expanding vertically to match size of\n        other widgets, next to it.\n    \"\"\"\n\n    def __init__(self, parent=None):\n        super(VerticalExpandButton, self).__init__(parent)\n\n        sp = self.sizePolicy()\n        sp.setVerticalPolicy(QtWidgets.QSizePolicy.Minimum)\n        self.setSizePolicy(sp)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.WrappedCallbackItem","title":"<code>WrappedCallbackItem</code>","text":"<p>Structure to store information about callback and args/kwargs for it.</p> <p>Item can be used to execute callback in main thread which may be needed for execution of Qt objects.</p> <p>Item store callback (callable variable), arguments and keyword arguments for the callback. Item hold information about it's process.</p> Source code in <code>client/ayon_core/tools/utils/lib.py</code> <pre><code>class WrappedCallbackItem:\n    \"\"\"Structure to store information about callback and args/kwargs for it.\n\n    Item can be used to execute callback in main thread which may be needed\n    for execution of Qt objects.\n\n    Item store callback (callable variable), arguments and keyword arguments\n    for the callback. Item hold information about it's process.\n    \"\"\"\n    not_set = object()\n    _log = None\n\n    def __init__(self, callback, *args, **kwargs):\n        self._done = False\n        self._exception = self.not_set\n        self._result = self.not_set\n        self._callback = callback\n        self._args = args\n        self._kwargs = kwargs\n\n    def __call__(self):\n        self.execute()\n\n    @property\n    def log(self):\n        cls = self.__class__\n        if cls._log is None:\n            cls._log = Logger.get_logger(cls.__name__)\n        return cls._log\n\n    @property\n    def done(self):\n        return self._done\n\n    @property\n    def exception(self):\n        return self._exception\n\n    @property\n    def result(self):\n        return self._result\n\n    def execute(self):\n        \"\"\"Execute callback and store its result.\n\n        Method must be called from main thread. Item is marked as `done`\n        when callback execution finished. Store output of callback of exception\n        information when callback raises one.\n        \"\"\"\n        if self.done:\n            self.log.warning(\"- item is already processed\")\n            return\n\n        try:\n            result = self._callback(*self._args, **self._kwargs)\n            self._result = result\n\n        except Exception as exc:\n            self._exception = exc\n\n        finally:\n            self._done = True\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.WrappedCallbackItem.execute","title":"<code>execute()</code>","text":"<p>Execute callback and store its result.</p> <p>Method must be called from main thread. Item is marked as <code>done</code> when callback execution finished. Store output of callback of exception information when callback raises one.</p> Source code in <code>client/ayon_core/tools/utils/lib.py</code> <pre><code>def execute(self):\n    \"\"\"Execute callback and store its result.\n\n    Method must be called from main thread. Item is marked as `done`\n    when callback execution finished. Store output of callback of exception\n    information when callback raises one.\n    \"\"\"\n    if self.done:\n        self.log.warning(\"- item is already processed\")\n        return\n\n    try:\n        result = self._callback(*self._args, **self._kwargs)\n        self._result = result\n\n    except Exception as exc:\n        self._exception = exc\n\n    finally:\n        self._done = True\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.get_ayon_qt_app","title":"<code>get_ayon_qt_app()</code>","text":"<p>Main Qt application initialized for AYON processed.</p> <p>This function should be used only inside AYON-launcher process     and never inside other processes.</p> Source code in <code>client/ayon_core/tools/utils/lib.py</code> <pre><code>def get_ayon_qt_app():\n    \"\"\"Main Qt application initialized for AYON processed.\n\n    This function should be used only inside AYON-launcher process\n        and never inside other processes.\n    \"\"\"\n\n    app = get_qt_app()\n    app.setWindowIcon(QtGui.QIcon(get_app_icon_path()))\n    return app\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.get_qt_app","title":"<code>get_qt_app()</code>","text":"<p>Get Qt application.</p> <p>The function initializes new Qt application if it is not already initialized. It also sets some attributes to the application to ensure that it will work properly on high DPI displays.</p> <p>Returns:</p> Type Description <p>QtWidgets.QApplication: Current Qt application.</p> Source code in <code>client/ayon_core/tools/utils/lib.py</code> <pre><code>def get_qt_app():\n    \"\"\"Get Qt application.\n\n    The function initializes new Qt application if it is not already\n    initialized. It also sets some attributes to the application to\n    ensure that it will work properly on high DPI displays.\n\n    Returns:\n        QtWidgets.QApplication: Current Qt application.\n    \"\"\"\n\n    app = QtWidgets.QApplication.instance()\n    if app is None:\n        for attr_name in (\n            \"AA_EnableHighDpiScaling\",\n            \"AA_UseHighDpiPixmaps\",\n        ):\n            attr = getattr(QtCore.Qt, attr_name, None)\n            if attr is not None:\n                QtWidgets.QApplication.setAttribute(attr)\n\n        policy = os.getenv(\"QT_SCALE_FACTOR_ROUNDING_POLICY\")\n        if (\n            hasattr(\n                QtWidgets.QApplication, \"setHighDpiScaleFactorRoundingPolicy\"\n            )\n            and not policy\n        ):\n            QtWidgets.QApplication.setHighDpiScaleFactorRoundingPolicy(\n                QtCore.Qt.HighDpiScaleFactorRoundingPolicy.PassThrough\n            )\n\n        app = QtWidgets.QApplication(sys.argv)\n\n    return app\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.get_qt_icon","title":"<code>get_qt_icon(icon_def, *, default=_PLACEHOLDER)</code>","text":"<p>Returns icon from cache or creates new one.</p> <p>Parameters:</p> Name Type Description Default <code>icon_def</code> <code>IconBase | dict[str, Any</code> <p>Icon definition.</p> required <code>default</code> <code>Any</code> <p>Default value to return if icon is not found.</p> <code>_PLACEHOLDER</code> <p>Returns:</p> Type Description <code>QIcon | Any</code> <p>QtGui.QIcon: Icon.</p> Source code in <code>client/ayon_core/tools/utils/lib.py</code> <pre><code>def get_qt_icon(\n    icon_def: IconBase | dict[str, Any],\n    *,\n    default: Any = _PLACEHOLDER,\n) -&gt; QtGui.QIcon | Any:\n    \"\"\"Returns icon from cache or creates new one.\n\n    Args:\n        icon_def (IconBase | dict[str, Any): Icon definition.\n        default (Any): Default value to return if icon is not found.\n\n    Returns:\n        QtGui.QIcon: Icon.\n\n    \"\"\"\n    return _IconsCache.get_icon(icon_def, default=default)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.get_warning_pixmap","title":"<code>get_warning_pixmap(color=None)</code>","text":"<p>Warning icon as QPixmap.</p> <p>Parameters:</p> Name Type Description Default <code>color</code> <code>QColor</code> <p>Color that will be used to paint warning icon.</p> <code>None</code> Source code in <code>client/ayon_core/tools/utils/lib.py</code> <pre><code>def get_warning_pixmap(color=None):\n    \"\"\"Warning icon as QPixmap.\n\n    Args:\n        color(QtGui.QColor): Color that will be used to paint warning icon.\n    \"\"\"\n    src_image_path = get_image_path(\"warning.png\")\n    src_image = QtGui.QImage(src_image_path)\n    if color is None:\n        color = get_objected_colors(\"delete-btn-bg\").get_qcolor()\n\n    return paint_image_with_color(src_image, color)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.paint_image_with_color","title":"<code>paint_image_with_color(image, color)</code>","text":"<p>Redraw image with single color using it's alpha.</p> <p>It is expected that input image is singlecolor image with alpha.</p> <p>Parameters:</p> Name Type Description Default <code>image</code> <code>QImage</code> <p>Loaded image with alpha.</p> required <code>color</code> <code>QColor</code> <p>Color that will be used to paint image.</p> required Source code in <code>client/ayon_core/tools/utils/lib.py</code> <pre><code>def paint_image_with_color(image, color):\n    \"\"\"Redraw image with single color using it's alpha.\n\n    It is expected that input image is singlecolor image with alpha.\n\n    Args:\n        image (QImage): Loaded image with alpha.\n        color (QColor): Color that will be used to paint image.\n    \"\"\"\n    width = image.width()\n    height = image.height()\n\n    alpha_mask = image.createAlphaMask()\n    alpha_region = QtGui.QRegion(QtGui.QBitmap.fromImage(alpha_mask))\n\n    pixmap = QtGui.QPixmap(width, height)\n    pixmap.fill(QtCore.Qt.transparent)\n\n    painter = QtGui.QPainter(pixmap)\n    render_hints = (\n        QtGui.QPainter.Antialiasing\n        | QtGui.QPainter.SmoothPixmapTransform\n    )\n    painter.setRenderHints(render_hints)\n\n    painter.setClipRegion(alpha_region)\n    painter.setPen(QtCore.Qt.NoPen)\n    painter.setBrush(color)\n    painter.drawRect(QtCore.QRect(0, 0, width, height))\n    painter.end()\n\n    return pixmap\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.set_style_property","title":"<code>set_style_property(widget, property_name, property_value)</code>","text":"<p>Set widget's property that may affect style.</p> <p>If current property value is different then style of widget is polished.</p> Source code in <code>client/ayon_core/tools/utils/lib.py</code> <pre><code>def set_style_property(widget, property_name, property_value):\n    \"\"\"Set widget's property that may affect style.\n\n    If current property value is different then style of widget is polished.\n    \"\"\"\n    cur_value = widget.property(property_name)\n    if cur_value == property_value:\n        return\n    widget.setProperty(property_name, property_value)\n    style = widget.style()\n    style.polish(widget)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/index.html#client.ayon_core.tools.utils.show_message_dialog","title":"<code>show_message_dialog(title, message, level=None, parent=None)</code>","text":"<p>Parameters:</p> Name Type Description Default <code>title</code> <code>str</code> <p>Title of dialog.</p> required <code>message</code> <code>str</code> <p>Message to display.</p> required <code>level</code> <code>Literal[info, warning, critical]</code> <p>Level of dialog.</p> <code>None</code> <code>parent</code> <code>Optional[QObject]</code> <p>Parent widget.</p> <code>None</code> Source code in <code>client/ayon_core/tools/utils/dialogs.py</code> <pre><code>def show_message_dialog(title, message, level=None, parent=None):\n    \"\"\"\n\n    Args:\n        title (str): Title of dialog.\n        message (str): Message to display.\n        level (Literal[\"info\", \"warning\", \"critical\"]): Level of dialog.\n        parent (Optional[QtCore.QObject]): Parent widget.\n\n    \"\"\"\n    if level is None:\n        level = \"info\"\n\n    if level == \"info\":\n        function = QtWidgets.QMessageBox.information\n    elif level == \"warning\":\n        function = QtWidgets.QMessageBox.warning\n    elif level == \"critical\":\n        function = QtWidgets.QMessageBox.critical\n    else:\n        raise ValueError(f\"Invalid level: {level}\")\n    function(parent, title, message)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/constants.html","title":"constants","text":""},{"location":"autoapi/client/ayon_core/tools/utils/delegates.html","title":"delegates","text":""},{"location":"autoapi/client/ayon_core/tools/utils/delegates.html#client.ayon_core.tools.utils.delegates.PrettyTimeDelegate","title":"<code>PrettyTimeDelegate</code>","text":"<p>               Bases: <code>QStyledItemDelegate</code></p> <p>A delegate that displays a timestamp as a pretty date.</p> <p>This displays dates like <code>pretty_date</code>.</p> Source code in <code>client/ayon_core/tools/utils/delegates.py</code> <pre><code>class PrettyTimeDelegate(QtWidgets.QStyledItemDelegate):\n    \"\"\"A delegate that displays a timestamp as a pretty date.\n\n    This displays dates like `pretty_date`.\n\n    \"\"\"\n\n    def __init__(\n        self,\n        *,\n        default: str | None = None,\n        parent: QtCore.QObject | None = None,\n    ) -&gt; None:\n        self._default_value = default\n        super().__init__(parent)\n\n    def displayText(self, value, locale):\n        if value is not None:\n            value = pretty_timestamp(value)\n            if value is not None:\n                return value\n        return self._default_value\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/delegates.html#client.ayon_core.tools.utils.delegates.StatusDelegate","title":"<code>StatusDelegate</code>","text":"<p>               Bases: <code>QStyledItemDelegate</code></p> <p>Delegate showing status name and short name.</p> Source code in <code>client/ayon_core/tools/utils/delegates.py</code> <pre><code>class StatusDelegate(QtWidgets.QStyledItemDelegate):\n    \"\"\"Delegate showing status name and short name.\"\"\"\n    def __init__(\n        self,\n        status_name_role,\n        status_short_name_role,\n        status_color_role,\n        status_icon_role,\n        *args, **kwargs\n    ):\n        super().__init__(*args, **kwargs)\n        self.status_name_role = status_name_role\n        self.status_short_name_role = status_short_name_role\n        self.status_color_role = status_color_role\n        self.status_icon_role = status_icon_role\n\n    def paint(self, painter, option, index):\n        if option.widget:\n            style = option.widget.style()\n        else:\n            style = QtWidgets.QApplication.style()\n\n        self.initStyleOption(option, index)\n\n        mode = QtGui.QIcon.Normal\n        if not (option.state &amp; QtWidgets.QStyle.State_Enabled):\n            mode = QtGui.QIcon.Disabled\n        elif option.state &amp; QtWidgets.QStyle.State_Selected:\n            mode = QtGui.QIcon.Selected\n        state = QtGui.QIcon.Off\n        if option.state &amp; QtWidgets.QStyle.State_Open:\n            state = QtGui.QIcon.On\n        icon = self._get_status_icon(index)\n        option.features |= QtWidgets.QStyleOptionViewItem.HasDecoration\n        option.icon = icon\n        act_size = icon.actualSize(option.decorationSize, mode, state)\n        option.decorationSize = QtCore.QSize(\n            min(option.decorationSize.width(), act_size.width()),\n            min(option.decorationSize.height(), act_size.height())\n        )\n\n        text = self._get_status_name(index)\n        if text:\n            option.features |= QtWidgets.QStyleOptionViewItem.HasDisplay\n            option.text = text\n\n        painter.save()\n        painter.setClipRect(option.rect)\n\n        icon_rect = style.subElementRect(\n            QtWidgets.QCommonStyle.SE_ItemViewItemDecoration,\n            option,\n            option.widget\n        )\n        text_rect = style.subElementRect(\n            QtWidgets.QCommonStyle.SE_ItemViewItemText,\n            option,\n            option.widget\n        )\n\n        # Draw background\n        style.drawPrimitive(\n            QtWidgets.QCommonStyle.PE_PanelItemViewItem,\n            option,\n            painter,\n            option.widget\n        )\n\n        # Draw icon\n        option.icon.paint(\n            painter,\n            icon_rect,\n            option.decorationAlignment,\n            mode,\n            state\n        )\n        fm = QtGui.QFontMetrics(option.font)\n        if text_rect.width() &lt; fm.width(text):\n            short_text = self._get_status_short_name(index)\n            if short_text:\n                text = short_text\n\n            text = fm.elidedText(\n                text, QtCore.Qt.ElideRight, text_rect.width()\n            )\n            # Allow at least one character\n            if len(text) &lt; 2:\n                text = \"\"\n\n        fg_color = self._get_status_color(index)\n        pen = painter.pen()\n        pen.setColor(fg_color)\n        painter.setPen(pen)\n\n        painter.drawText(\n            text_rect,\n            option.displayAlignment,\n            text\n        )\n\n        if option.state &amp; QtWidgets.QStyle.State_HasFocus:\n            focus_opt = QtWidgets.QStyleOptionFocusRect()\n            focus_opt.state = option.state\n            focus_opt.direction = option.direction\n            focus_opt.rect = option.rect\n            focus_opt.fontMetrics = option.fontMetrics\n            focus_opt.palette = option.palette\n\n            focus_opt.rect = style.subElementRect(\n                QtWidgets.QCommonStyle.SE_ItemViewItemFocusRect,\n                option,\n                option.widget\n            )\n            focus_opt.state |= (\n                QtWidgets.QStyle.State_KeyboardFocusChange\n                | QtWidgets.QStyle.State_Item\n            )\n            focus_opt.backgroundColor = option.palette.color(\n                (\n                    QtGui.QPalette.Normal\n                    if option.state &amp; QtWidgets.QStyle.State_Enabled\n                    else QtGui.QPalette.Disabled\n                ),\n                (\n                    QtGui.QPalette.Highlight\n                    if option.state &amp; QtWidgets.QStyle.State_Selected\n                    else QtGui.QPalette.Window\n                )\n            )\n            style.drawPrimitive(\n                QtWidgets.QCommonStyle.PE_FrameFocusRect,\n                focus_opt,\n                painter,\n                option.widget\n            )\n\n        painter.restore()\n\n    def _get_status_name(self, index):\n        return index.data(self.status_name_role)\n\n    def _get_status_short_name(self, index):\n        return index.data(self.status_short_name_role)\n\n    def _get_status_color(self, index):\n        return QtGui.QColor(index.data(self.status_color_role))\n\n    def _get_status_icon(self, index):\n        icon = None\n        if self.status_icon_role is not None:\n            icon = index.data(self.status_icon_role)\n        if icon is None:\n            return QtGui.QIcon()\n        return icon\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/delegates.html#client.ayon_core.tools.utils.delegates.pretty_date","title":"<code>pretty_date(t, now=None, strftime='%b %d %Y %H:%M')</code>","text":"<p>Parse datetime to readable timestamp</p> Within first ten seconds <ul> <li>\"just now\",</li> </ul> <p>Within first minute ago:     - \"%S seconds ago\" Within one hour ago:     - \"%M minutes ago\". Within one day ago:     - \"%H:%M hours ago\" Else:     \"%Y-%m-%d %H:%M:%S\"</p> Source code in <code>client/ayon_core/tools/utils/delegates.py</code> <pre><code>def pretty_date(t, now=None, strftime=\"%b %d %Y %H:%M\"):\n    \"\"\"Parse datetime to readable timestamp\n\n    Within first ten seconds:\n        - \"just now\",\n    Within first minute ago:\n        - \"%S seconds ago\"\n    Within one hour ago:\n        - \"%M minutes ago\".\n    Within one day ago:\n        - \"%H:%M hours ago\"\n    Else:\n        \"%Y-%m-%d %H:%M:%S\"\n\n    \"\"\"\n\n    assert isinstance(t, datetime)\n    if now is None:\n        now = datetime.now()\n    assert isinstance(now, datetime)\n    diff = now - t\n\n    second_diff = diff.seconds\n    day_diff = diff.days\n\n    # future (consider as just now)\n    if day_diff &lt; 0:\n        return \"just now\"\n\n    # history\n    if day_diff == 0:\n        if second_diff &lt; 10:\n            return \"just now\"\n        if second_diff &lt; 60:\n            return str(second_diff) + \" seconds ago\"\n        if second_diff &lt; 120:\n            return \"a minute ago\"\n        if second_diff &lt; 3600:\n            return str(second_diff // 60) + \" minutes ago\"\n        if second_diff &lt; 86400:\n            minutes = (second_diff % 3600) // 60\n            hours = second_diff // 3600\n            return f\"{hours}:{minutes:02d} hours ago\"\n\n    return t.strftime(strftime)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/delegates.html#client.ayon_core.tools.utils.delegates.pretty_timestamp","title":"<code>pretty_timestamp(t, now=None)</code>","text":"<p>Parse timestamp to user readable format</p> <p>pretty_timestamp(\"20170614T151122Z\", now=\"20170614T151123Z\") 'just now'</p> <p>pretty_timestamp(\"20170614T151122Z\", now=\"20170614T171222Z\") '2:01 hours ago'</p> <p>Parameters:</p> Name Type Description Default <code>t</code> <code>str</code> <p>The time string to parse.</p> required <p>Returns:</p> Name Type Description <code>str</code> <p>human readable \"recent\" date.</p> Source code in <code>client/ayon_core/tools/utils/delegates.py</code> <pre><code>def pretty_timestamp(t, now=None):\n    \"\"\"Parse timestamp to user readable format\n\n    &gt;&gt;&gt; pretty_timestamp(\"20170614T151122Z\", now=\"20170614T151123Z\")\n    'just now'\n\n    &gt;&gt;&gt; pretty_timestamp(\"20170614T151122Z\", now=\"20170614T171222Z\")\n    '2:01 hours ago'\n\n    Args:\n        t (str): The time string to parse.\n        now (str, optional)\n\n    Returns:\n        str: human readable \"recent\" date.\n\n    \"\"\"\n\n    if now is not None:\n        try:\n            now = time.strptime(now, \"%Y%m%dT%H%M%SZ\")\n            now = datetime.fromtimestamp(time.mktime(now))\n        except ValueError as e:\n            log.warning(\"Can't parse 'now' time format: {0} {1}\".format(t, e))\n            return None\n\n    if isinstance(t, float):\n        dt = datetime.fromtimestamp(t)\n    else:\n        # Parse the time from datetime format\n        try:\n            t = time.strptime(t, \"%Y%m%dT%H%M%SZ\")\n        except ValueError as e:\n            log.warning(\"Can't parse time format: {0} {1}\".format(t, e))\n            return None\n        dt = datetime.fromtimestamp(time.mktime(t))\n\n    # prettify\n    return pretty_date(dt, now=now)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/dialogs.html","title":"dialogs","text":""},{"location":"autoapi/client/ayon_core/tools/utils/dialogs.html#client.ayon_core.tools.utils.dialogs.PopupUpdateKeys","title":"<code>PopupUpdateKeys</code>","text":"<p>               Bases: <code>SimplePopup</code></p> <p>Simple popup with checkbox.</p> Source code in <code>client/ayon_core/tools/utils/dialogs.py</code> <pre><code>class PopupUpdateKeys(SimplePopup):\n    \"\"\"Simple popup with checkbox.\"\"\"\n\n    on_clicked_state = QtCore.Signal(bool)\n\n    def __init__(self, parent=None, *args, **kwargs):\n        super().__init__(parent=parent, *args, **kwargs)\n\n        layout = self.layout()\n\n        # Insert toggle for Update keys\n        toggle = QtWidgets.QCheckBox(\"Update Keys\", self)\n        layout.insertWidget(1, toggle)\n\n        self.on_clicked.connect(self.emit_click_with_state)\n\n        layout.insertStretch(1, 1)\n\n        self._toggle_checkbox = toggle\n\n    def is_toggle_checked(self):\n        return self._toggle_checkbox.isChecked()\n\n    def emit_click_with_state(self):\n        \"\"\"Emit the on_clicked_state signal with the toggled state\"\"\"\n        checked = self._toggle_checkbox.isChecked()\n        self.on_clicked_state.emit(checked)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/dialogs.html#client.ayon_core.tools.utils.dialogs.PopupUpdateKeys.emit_click_with_state","title":"<code>emit_click_with_state()</code>","text":"<p>Emit the on_clicked_state signal with the toggled state</p> Source code in <code>client/ayon_core/tools/utils/dialogs.py</code> <pre><code>def emit_click_with_state(self):\n    \"\"\"Emit the on_clicked_state signal with the toggled state\"\"\"\n    checked = self._toggle_checkbox.isChecked()\n    self.on_clicked_state.emit(checked)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/dialogs.html#client.ayon_core.tools.utils.dialogs.ScrollMessageBox","title":"<code>ScrollMessageBox</code>","text":"<p>               Bases: <code>QDialog</code></p> <p>Basic version of scrollable QMessageBox.</p> <p>No other existing dialog implementation is scrollable.</p> <p>Parameters:</p> Name Type Description Default <code>icon</code> <code>Icon | QIcon</code> <p>Icon to display.</p> required <code>title</code> <code>str</code> <p>Window title.</p> required <code>messages</code> <code>list[str]</code> <p>List of messages.</p> required <code>cancelable</code> <code>Optional[bool]</code> <p>True if Cancel button should be added.</p> <code>False</code> Source code in <code>client/ayon_core/tools/utils/dialogs.py</code> <pre><code>class ScrollMessageBox(QtWidgets.QDialog):\n    \"\"\"Basic version of scrollable QMessageBox.\n\n    No other existing dialog implementation is scrollable.\n\n    Args:\n        icon (QtWidgets.QMessageBox.Icon | QtGui.QIcon): Icon to display.\n        title (str): Window title.\n        messages (list[str]): List of messages.\n        cancelable (Optional[bool]): True if Cancel button should be added.\n\n    \"\"\"\n    def __init__(self, icon, title, messages, cancelable=False):\n        super().__init__()\n        self.setWindowTitle(title)\n        self.icon = icon\n        self._messages = messages\n\n        self.setWindowFlags(QtCore.Qt.WindowTitleHint)\n\n        wrapped_icon = icon_widget(self, icon)\n\n        scroll_widget = QtWidgets.QScrollArea(self)\n        scroll_widget.setWidgetResizable(True)\n        content_widget = QtWidgets.QWidget(self)\n        scroll_widget.setWidget(content_widget)\n\n        message_len = 0\n        content_layout = QtWidgets.QVBoxLayout(content_widget)\n        for message in messages:\n            label_widget = QtWidgets.QLabel(message, content_widget)\n            content_layout.addWidget(label_widget)\n            message_len = max(message_len, len(message))\n        content_layout.addStretch(1)\n\n        # Set minimum width\n        scroll_widget.setMinimumWidth(360)\n\n        buttons = QtWidgets.QDialogButtonBox.Ok\n        if cancelable:\n            buttons |= QtWidgets.QDialogButtonBox.Cancel\n\n        btn_box = QtWidgets.QDialogButtonBox(buttons)\n        btn_box.accepted.connect(self.accept)\n\n        if cancelable:\n            btn_box.reject.connect(self.reject)\n\n        btn = QtWidgets.QPushButton(\"Copy to clipboard\")\n        btn.clicked.connect(self._on_copy_click)\n        btn_box.addButton(btn, QtWidgets.QDialogButtonBox.NoRole)\n\n        main_layout = QtWidgets.QGridLayout(self)\n        main_layout.addWidget(wrapped_icon, 0, 0,\n            alignment=QtCore.Qt.AlignmentFlag.AlignTop,\n        )\n        main_layout.addWidget(scroll_widget, 0, 1)\n        main_layout.addWidget(btn_box, 1, 1)\n        main_layout.setRowStretch(0, 1)\n        main_layout.setRowStretch(1, 0)\n\n    def _on_copy_click(self):\n        clipboard = QtWidgets.QApplication.clipboard()\n        clipboard.setText(\"\\n\".join(self._messages))\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/dialogs.html#client.ayon_core.tools.utils.dialogs.SimplePopup","title":"<code>SimplePopup</code>","text":"<p>               Bases: <code>QDialog</code></p> <p>A Popup that moves itself to bottom right of screen on show event.</p> <p>The UI contains a message label and a red highlighted button to \"show\" or perform another custom action from this pop-up.</p> Source code in <code>client/ayon_core/tools/utils/dialogs.py</code> <pre><code>class SimplePopup(QtWidgets.QDialog):\n    \"\"\"A Popup that moves itself to bottom right of screen on show event.\n\n    The UI contains a message label and a red highlighted button to \"show\"\n    or perform another custom action from this pop-up.\n\n    \"\"\"\n\n    on_clicked = QtCore.Signal()\n\n    def __init__(self, parent=None, *args, **kwargs):\n        super().__init__(parent=parent, *args, **kwargs)\n\n        # Set default title\n        self.setWindowTitle(\"Popup\")\n\n        self.setContentsMargins(0, 0, 0, 0)\n\n        message_label = QtWidgets.QLabel(\"\", self)\n        message_label.setStyleSheet(\"\"\"\n        QLabel {\n            font-size: 12px;\n        }\n        \"\"\")\n        confirm_btn = QtWidgets.QPushButton(\"Show\", self)\n        confirm_btn.setSizePolicy(\n            QtWidgets.QSizePolicy.Maximum,\n            QtWidgets.QSizePolicy.Maximum\n        )\n        confirm_btn.setStyleSheet(\n            \"\"\"QPushButton { background-color: #BB0000 }\"\"\"\n        )\n\n        # Layout\n        layout = QtWidgets.QHBoxLayout(self)\n        layout.setContentsMargins(10, 5, 10, 10)\n\n        # Increase spacing slightly for readability\n        layout.setSpacing(10)\n        layout.addWidget(message_label)\n        layout.addWidget(confirm_btn)\n\n        # Signals\n        confirm_btn.clicked.connect(self._on_clicked)\n\n        # Default size\n        self.resize(400, 40)\n\n        self._message_label = message_label\n        self._confirm_btn = confirm_btn\n\n    def set_message(self, message):\n        self._message_label.setText(message)\n\n    def set_button_text(self, text):\n        self._confirm_btn.setText(text)\n\n    def setMessage(self, message):\n        self.set_message(message)\n\n    def setButtonText(self, text):\n        self.set_button_text(text)\n\n    def showEvent(self, event):\n        # Position popup based on contents on show event\n        geo = self._calculate_window_geometry()\n        self.setGeometry(geo)\n\n        return super().showEvent(event)\n\n    def _on_clicked(self):\n        \"\"\"Callback for when the 'show' button is clicked.\n\n        Raises the parent (if any)\n\n        \"\"\"\n\n        parent = self.parent()\n        self.close()\n\n        # Trigger the signal\n        self.on_clicked.emit()\n\n        if parent:\n            parent.raise_()\n\n    def _calculate_window_geometry(self):\n        \"\"\"Respond to status changes\n\n        On creation, align window with screen bottom right.\n\n        \"\"\"\n\n        window = self\n\n        width = window.width()\n        width = max(width, window.minimumWidth())\n\n        height = window.height()\n        height = max(height, window.sizeHint().height())\n\n        try:\n            screen = window.screen()\n            desktop_geometry = screen.availableGeometry()\n        except AttributeError:\n            # Backwards compatibility for older Qt versions\n            # PySide6 removed QDesktopWidget\n            desktop_geometry = QtWidgets.QDesktopWidget().availableGeometry()\n\n        window_geometry = window.geometry()\n\n        screen_width = window_geometry.width()\n        screen_height = window_geometry.height()\n\n        # Calculate width and height of system tray\n        systray_width = window_geometry.width() - desktop_geometry.width()\n        systray_height = window_geometry.height() - desktop_geometry.height()\n\n        padding = 10\n\n        x = screen_width - width\n        y = screen_height - height\n\n        x -= systray_width + padding\n        y -= systray_height + padding\n\n        return QtCore.QRect(x, y, width, height)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/dialogs.html#client.ayon_core.tools.utils.dialogs.icon_widget","title":"<code>icon_widget(parent, icon, size=64)</code>","text":"<p>Wrap an Icon into a QLabel with a fixed size.</p> <p>Parameters:</p> Name Type Description Default <code>icon</code> <code>Icon | QIcon</code> <p>Icon to display.</p> required <code>size</code> <code>int</code> <p>Size of the icon in pixels (default: 64).</p> <code>64</code> <p>Returns:</p> Type Description <code>QLabel</code> <p>QtWidgets.QLabel: Label with the icon.</p> Source code in <code>client/ayon_core/tools/utils/dialogs.py</code> <pre><code>def icon_widget(parent, icon, size: int = 64) -&gt; QtWidgets.QLabel:\n    \"\"\"Wrap an Icon into a QLabel with a fixed size.\n\n    Args:\n        icon (QtWidgets.QMessageBox.Icon | QtGui.QIcon): Icon to display.\n        size (int): Size of the icon in pixels (default: 64).\n\n    Returns:\n        QtWidgets.QLabel: Label with the icon.\n\n    \"\"\"\n    label = QtWidgets.QLabel(parent)\n    if isinstance(icon, QtGui.QIcon):\n        label.setPixmap(icon.pixmap(size, size))\n    else:\n        label.setPixmap(QtWidgets.QMessageBox.standardIcon(icon))\n\n    return label\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/dialogs.html#client.ayon_core.tools.utils.dialogs.show_message_dialog","title":"<code>show_message_dialog(title, message, level=None, parent=None)</code>","text":"<p>Parameters:</p> Name Type Description Default <code>title</code> <code>str</code> <p>Title of dialog.</p> required <code>message</code> <code>str</code> <p>Message to display.</p> required <code>level</code> <code>Literal[info, warning, critical]</code> <p>Level of dialog.</p> <code>None</code> <code>parent</code> <code>Optional[QObject]</code> <p>Parent widget.</p> <code>None</code> Source code in <code>client/ayon_core/tools/utils/dialogs.py</code> <pre><code>def show_message_dialog(title, message, level=None, parent=None):\n    \"\"\"\n\n    Args:\n        title (str): Title of dialog.\n        message (str): Message to display.\n        level (Literal[\"info\", \"warning\", \"critical\"]): Level of dialog.\n        parent (Optional[QtCore.QObject]): Parent widget.\n\n    \"\"\"\n    if level is None:\n        level = \"info\"\n\n    if level == \"info\":\n        function = QtWidgets.QMessageBox.information\n    elif level == \"warning\":\n        function = QtWidgets.QMessageBox.warning\n    elif level == \"critical\":\n        function = QtWidgets.QMessageBox.critical\n    else:\n        raise ValueError(f\"Invalid level: {level}\")\n    function(parent, title, message)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/error_dialog.html","title":"error_dialog","text":""},{"location":"autoapi/client/ayon_core/tools/utils/folders_widget.html","title":"folders_widget","text":""},{"location":"autoapi/client/ayon_core/tools/utils/folders_widget.html#client.ayon_core.tools.utils.folders_widget.FoldersFiltersWidget","title":"<code>FoldersFiltersWidget</code>","text":"<p>               Bases: <code>QWidget</code></p> <p>Helper widget for most commonly used filters in context selection.</p> Source code in <code>client/ayon_core/tools/utils/folders_widget.py</code> <pre><code>class FoldersFiltersWidget(QtWidgets.QWidget):\n    \"\"\"Helper widget for most commonly used filters in context selection.\"\"\"\n    text_changed = QtCore.Signal(str)\n    my_tasks_changed = QtCore.Signal(bool)\n\n    def __init__(self, parent: QtWidgets.QWidget) -&gt; None:\n        super().__init__(parent)\n\n        folders_filter_input = PlaceholderLineEdit(self)\n        folders_filter_input.setPlaceholderText(\"Folder name filter...\")\n\n        my_tasks_tooltip = (\n            \"Filter folders and task to only those you are assigned to.\"\n        )\n        my_tasks_label = QtWidgets.QLabel(\"My tasks\", self)\n        my_tasks_label.setToolTip(my_tasks_tooltip)\n\n        my_tasks_checkbox = NiceCheckbox(self)\n        my_tasks_checkbox.setChecked(False)\n        my_tasks_checkbox.setToolTip(my_tasks_tooltip)\n\n        layout = QtWidgets.QHBoxLayout(self)\n        layout.setContentsMargins(0, 0, 0, 0)\n        layout.setSpacing(5)\n        layout.addWidget(folders_filter_input, 1)\n        layout.addWidget(my_tasks_label, 0)\n        layout.addWidget(my_tasks_checkbox, 0)\n\n        folders_filter_input.textChanged.connect(self.text_changed)\n        my_tasks_checkbox.stateChanged.connect(self._on_my_tasks_change)\n\n        self._folders_filter_input = folders_filter_input\n        self._my_tasks_checkbox = my_tasks_checkbox\n\n    def is_my_tasks_checked(self) -&gt; bool:\n        return self._my_tasks_checkbox.isChecked()\n\n    def text(self) -&gt; str:\n        return self._folders_filter_input.text()\n\n    def set_text(self, text: str) -&gt; None:\n        self._folders_filter_input.setText(text)\n\n    def set_my_tasks_checked(self, checked: bool) -&gt; None:\n        self._my_tasks_checkbox.setChecked(checked)\n\n    def _on_my_tasks_change(self, _state: int) -&gt; None:\n        self.my_tasks_changed.emit(self._my_tasks_checkbox.isChecked())\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/folders_widget.html#client.ayon_core.tools.utils.folders_widget.FoldersQtModel","title":"<code>FoldersQtModel</code>","text":"<p>               Bases: <code>QStandardItemModel</code></p> <p>Folders model which cares about refresh of folders.</p> <p>Parameters:</p> Name Type Description Default <code>controller</code> <code>AbstractWorkfilesFrontend</code> <p>The control object.</p> required Source code in <code>client/ayon_core/tools/utils/folders_widget.py</code> <pre><code>class FoldersQtModel(QtGui.QStandardItemModel):\n    \"\"\"Folders model which cares about refresh of folders.\n\n    Args:\n        controller (AbstractWorkfilesFrontend): The control object.\n\n    \"\"\"\n    _default_folder_icon = None\n    refreshed = QtCore.Signal()\n\n    def __init__(self, controller):\n        super().__init__()\n\n        self.setColumnCount(1)\n        self.setHeaderData(0, QtCore.Qt.Horizontal, \"Folders\")\n\n        self._controller = controller\n        self._items_by_id = {}\n        self._parent_id_by_id = {}\n\n        self._refresh_threads = {}\n        self._current_refresh_thread = None\n        self._last_project_name = None\n\n        self._has_content = False\n        self._is_refreshing = False\n\n    @property\n    def is_refreshing(self):\n        \"\"\"Model is refreshing.\n\n        Returns:\n            bool: True if model is refreshing.\n        \"\"\"\n        return self._is_refreshing\n\n    @property\n    def has_content(self):\n        \"\"\"Has at least one folder.\n\n        Returns:\n            bool: True if model has at least one folder.\n        \"\"\"\n\n        return self._has_content\n\n    def refresh(self):\n        \"\"\"Refresh folders for last selected project.\n\n        Force to update folders model from controller. This may or may not\n        trigger query from server, that's based on controller's cache.\n        \"\"\"\n\n        self.set_project_name(self._last_project_name)\n\n    def get_index_by_id(self, item_id):\n        \"\"\"Get index by folder id.\n\n        Returns:\n            QtCore.QModelIndex: Index of the folder. Can be invalid if folder\n                is not available.\n        \"\"\"\n        item = self._items_by_id.get(item_id)\n        if item is None:\n            return QtCore.QModelIndex()\n        return self.indexFromItem(item)\n\n    def get_item_id_by_path(self, folder_path):\n        \"\"\"Get folder id by path.\n\n        Args:\n            folder_path (str): Folder path.\n\n        Returns:\n            Union[str, None]: Folder id or None if folder is not available.\n\n        \"\"\"\n        for folder_id, item in self._items_by_id.items():\n            if item.data(FOLDER_PATH_ROLE) == folder_path:\n                return folder_id\n        return None\n\n    def get_project_name(self):\n        \"\"\"Project name which model currently use.\n\n        Returns:\n            Union[str, None]: Currently used project name.\n        \"\"\"\n\n        return self._last_project_name\n\n    def set_project_name(self, project_name):\n        \"\"\"Refresh folders items.\n\n        Refresh start thread because it can cause that controller can\n        start query from database if folders are not cached.\n        \"\"\"\n\n        if not project_name:\n            self._last_project_name = project_name\n            self._fill_items({}, {})\n            self._current_refresh_thread = None\n            return\n\n        self._is_refreshing = True\n\n        if self._last_project_name != project_name:\n            self._clear_items()\n        self._last_project_name = project_name\n\n        thread = self._refresh_threads.get(project_name)\n        if thread is not None:\n            self._current_refresh_thread = thread\n            return\n\n        thread = RefreshThread(\n            project_name,\n            self._thread_getter,\n            project_name\n        )\n        self._current_refresh_thread = thread\n        self._refresh_threads[thread.id] = thread\n        thread.refresh_finished.connect(self._on_refresh_thread)\n        thread.start()\n\n    @classmethod\n    def _get_default_folder_icon(cls):\n        if cls._default_folder_icon is None:\n            cls._default_folder_icon = get_qt_icon(\n                AwesomeFontIcon(\n                    \"fa.folder\",\n                    color=get_default_entity_icon_color(),\n                )\n            )\n        return cls._default_folder_icon\n\n    def _clear_items(self):\n        self._items_by_id = {}\n        self._parent_id_by_id = {}\n        self._has_content = False\n        root_item = self.invisibleRootItem()\n        root_item.removeRows(0, root_item.rowCount())\n\n    def _thread_getter(self, project_name):\n        folder_items = self._controller.get_folder_items(\n            project_name, FOLDERS_MODEL_SENDER_NAME\n        )\n        folder_type_items = []\n        if hasattr(self._controller, \"get_folder_type_items\"):\n            folder_type_items = self._controller.get_folder_type_items(\n                project_name, FOLDERS_MODEL_SENDER_NAME\n            )\n        return folder_items, folder_type_items\n\n    def _on_refresh_thread(self, thread_id):\n        \"\"\"Callback when refresh thread is finished.\n\n        Technically can be running multiple refresh threads at the same time,\n        to avoid using values from wrong thread, we check if thread id is\n        current refresh thread id.\n\n        Folders are stored by id.\n\n        Args:\n            thread_id (str): Thread id.\n        \"\"\"\n\n        # Make sure to remove thread from '_refresh_threads' dict\n        thread = self._refresh_threads.pop(thread_id)\n        if (\n            self._current_refresh_thread is None\n            or thread_id != self._current_refresh_thread.id\n        ):\n            return\n        if thread.failed:\n            # TODO visualize that refresh failed\n            folder_items, folder_type_items = {}, []\n        else:\n            folder_items, folder_type_items = thread.get_result()\n        self._fill_items(folder_items, folder_type_items)\n        self._current_refresh_thread = None\n\n    def _get_folder_item_icon(\n        self,\n        folder_item,\n        folder_type_item_by_name,\n        folder_type_icon_cache\n    ):\n        icon = folder_type_icon_cache.get(folder_item.folder_type)\n        if icon is not None:\n            return icon\n\n        folder_type_item = folder_type_item_by_name.get(\n            folder_item.folder_type\n        )\n        icon = None\n        if folder_type_item is not None:\n            icon = get_qt_icon(MaterialSymbolsIcon(\n                folder_type_item.icon,\n                color=get_default_entity_icon_color(),\n            ))\n\n        if icon is None:\n            icon = self._get_default_folder_icon()\n        folder_type_icon_cache[folder_item.folder_type] = icon\n        return icon\n\n    def _fill_item_data(\n        self,\n        item,\n        folder_item,\n        folder_type_item_by_name,\n        folder_type_icon_cache\n    ):\n        \"\"\"\n\n        Args:\n            item (QtGui.QStandardItem): Item to fill data.\n            folder_item (FolderItem): Folder item.\n\n        \"\"\"\n        icon = self._get_folder_item_icon(\n            folder_item,\n            folder_type_item_by_name,\n            folder_type_icon_cache\n        )\n        item.setData(folder_item.entity_id, FOLDER_ID_ROLE)\n        item.setData(folder_item.name, FOLDER_NAME_ROLE)\n        item.setData(folder_item.path, FOLDER_PATH_ROLE)\n        item.setData(folder_item.folder_type, FOLDER_TYPE_ROLE)\n        item.setData(folder_item.label, QtCore.Qt.DisplayRole)\n        item.setData(icon, QtCore.Qt.DecorationRole)\n\n    def _fill_items(self, folder_items_by_id, folder_type_items):\n        if not folder_items_by_id:\n            if folder_items_by_id is not None:\n                self._clear_items()\n            self._is_refreshing = False\n            self.refreshed.emit()\n            return\n\n        folder_type_item_by_name = {\n            folder_type.name: folder_type\n            for folder_type in folder_type_items\n        }\n        folder_type_icon_cache = {}\n        self._has_content = True\n\n        folder_ids = set(folder_items_by_id)\n        ids_to_remove = set(self._items_by_id) - folder_ids\n\n        folder_items_by_parent = collections.defaultdict(dict)\n        for folder_item in folder_items_by_id.values():\n            (\n                folder_items_by_parent\n                [folder_item.parent_id]\n                [folder_item.entity_id]\n            ) = folder_item\n\n        hierarchy_queue = collections.deque()\n        hierarchy_queue.append((self.invisibleRootItem(), None))\n\n        # Keep pointers to removed items until the refresh finishes\n        #   - some children of the items could be moved and reused elsewhere\n        removed_items = []\n        while hierarchy_queue:\n            item = hierarchy_queue.popleft()\n            parent_item, parent_id = item\n            folder_items = folder_items_by_parent[parent_id]\n\n            items_by_id = {}\n            folder_ids_to_add = set(folder_items)\n            for row_idx in reversed(range(parent_item.rowCount())):\n                child_item = parent_item.child(row_idx)\n                child_id = child_item.data(FOLDER_ID_ROLE)\n                if child_id in ids_to_remove:\n                    removed_items.append(parent_item.takeRow(row_idx))\n                else:\n                    items_by_id[child_id] = child_item\n\n            new_items = []\n            for item_id in folder_ids_to_add:\n                folder_item = folder_items[item_id]\n                item = items_by_id.get(item_id)\n                if item is None:\n                    is_new = True\n                    item = QtGui.QStandardItem()\n                    item.setEditable(False)\n                else:\n                    is_new = self._parent_id_by_id[item_id] != parent_id\n\n                self._fill_item_data(\n                    item,\n                    folder_item,\n                    folder_type_item_by_name,\n                    folder_type_icon_cache\n                )\n                if is_new:\n                    new_items.append(item)\n                self._items_by_id[item_id] = item\n                self._parent_id_by_id[item_id] = parent_id\n\n                hierarchy_queue.append((item, item_id))\n\n            if new_items:\n                parent_item.appendRows(new_items)\n\n        for item_id in ids_to_remove:\n            self._items_by_id.pop(item_id)\n            self._parent_id_by_id.pop(item_id)\n\n        self._is_refreshing = False\n        self.refreshed.emit()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/folders_widget.html#client.ayon_core.tools.utils.folders_widget.FoldersQtModel.has_content","title":"<code>has_content</code>  <code>property</code>","text":"<p>Has at least one folder.</p> <p>Returns:</p> Name Type Description <code>bool</code> <p>True if model has at least one folder.</p>"},{"location":"autoapi/client/ayon_core/tools/utils/folders_widget.html#client.ayon_core.tools.utils.folders_widget.FoldersQtModel.is_refreshing","title":"<code>is_refreshing</code>  <code>property</code>","text":"<p>Model is refreshing.</p> <p>Returns:</p> Name Type Description <code>bool</code> <p>True if model is refreshing.</p>"},{"location":"autoapi/client/ayon_core/tools/utils/folders_widget.html#client.ayon_core.tools.utils.folders_widget.FoldersQtModel.get_index_by_id","title":"<code>get_index_by_id(item_id)</code>","text":"<p>Get index by folder id.</p> <p>Returns:</p> Type Description <p>QtCore.QModelIndex: Index of the folder. Can be invalid if folder is not available.</p> Source code in <code>client/ayon_core/tools/utils/folders_widget.py</code> <pre><code>def get_index_by_id(self, item_id):\n    \"\"\"Get index by folder id.\n\n    Returns:\n        QtCore.QModelIndex: Index of the folder. Can be invalid if folder\n            is not available.\n    \"\"\"\n    item = self._items_by_id.get(item_id)\n    if item is None:\n        return QtCore.QModelIndex()\n    return self.indexFromItem(item)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/folders_widget.html#client.ayon_core.tools.utils.folders_widget.FoldersQtModel.get_item_id_by_path","title":"<code>get_item_id_by_path(folder_path)</code>","text":"<p>Get folder id by path.</p> <p>Parameters:</p> Name Type Description Default <code>folder_path</code> <code>str</code> <p>Folder path.</p> required <p>Returns:</p> Type Description <p>Union[str, None]: Folder id or None if folder is not available.</p> Source code in <code>client/ayon_core/tools/utils/folders_widget.py</code> <pre><code>def get_item_id_by_path(self, folder_path):\n    \"\"\"Get folder id by path.\n\n    Args:\n        folder_path (str): Folder path.\n\n    Returns:\n        Union[str, None]: Folder id or None if folder is not available.\n\n    \"\"\"\n    for folder_id, item in self._items_by_id.items():\n        if item.data(FOLDER_PATH_ROLE) == folder_path:\n            return folder_id\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/folders_widget.html#client.ayon_core.tools.utils.folders_widget.FoldersQtModel.get_project_name","title":"<code>get_project_name()</code>","text":"<p>Project name which model currently use.</p> <p>Returns:</p> Type Description <p>Union[str, None]: Currently used project name.</p> Source code in <code>client/ayon_core/tools/utils/folders_widget.py</code> <pre><code>def get_project_name(self):\n    \"\"\"Project name which model currently use.\n\n    Returns:\n        Union[str, None]: Currently used project name.\n    \"\"\"\n\n    return self._last_project_name\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/folders_widget.html#client.ayon_core.tools.utils.folders_widget.FoldersQtModel.refresh","title":"<code>refresh()</code>","text":"<p>Refresh folders for last selected project.</p> <p>Force to update folders model from controller. This may or may not trigger query from server, that's based on controller's cache.</p> Source code in <code>client/ayon_core/tools/utils/folders_widget.py</code> <pre><code>def refresh(self):\n    \"\"\"Refresh folders for last selected project.\n\n    Force to update folders model from controller. This may or may not\n    trigger query from server, that's based on controller's cache.\n    \"\"\"\n\n    self.set_project_name(self._last_project_name)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/folders_widget.html#client.ayon_core.tools.utils.folders_widget.FoldersQtModel.set_project_name","title":"<code>set_project_name(project_name)</code>","text":"<p>Refresh folders items.</p> <p>Refresh start thread because it can cause that controller can start query from database if folders are not cached.</p> Source code in <code>client/ayon_core/tools/utils/folders_widget.py</code> <pre><code>def set_project_name(self, project_name):\n    \"\"\"Refresh folders items.\n\n    Refresh start thread because it can cause that controller can\n    start query from database if folders are not cached.\n    \"\"\"\n\n    if not project_name:\n        self._last_project_name = project_name\n        self._fill_items({}, {})\n        self._current_refresh_thread = None\n        return\n\n    self._is_refreshing = True\n\n    if self._last_project_name != project_name:\n        self._clear_items()\n    self._last_project_name = project_name\n\n    thread = self._refresh_threads.get(project_name)\n    if thread is not None:\n        self._current_refresh_thread = thread\n        return\n\n    thread = RefreshThread(\n        project_name,\n        self._thread_getter,\n        project_name\n    )\n    self._current_refresh_thread = thread\n    self._refresh_threads[thread.id] = thread\n    thread.refresh_finished.connect(self._on_refresh_thread)\n    thread.start()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/folders_widget.html#client.ayon_core.tools.utils.folders_widget.FoldersWidget","title":"<code>FoldersWidget</code>","text":"<p>               Bases: <code>QWidget</code></p> <p>Folders widget.</p> <p>Widget that handles folders view, model and selection.</p> <p>Expected selection handling is disabled by default. If enabled, the widget will handle the expected in predefined way. Widget is listening to event 'expected_selection_changed' with expected event data below, the same data must be available when called method 'get_expected_selection_data' on controller.</p> <p>{     \"folder\": {         \"current\": bool,               # Folder is what should be set now         \"folder_id\": Union[str, None], # Folder id that should be selected     },     ... }</p> <p>Selection is confirmed by calling method 'expected_folder_selected' on controller.</p> <p>Parameters:</p> Name Type Description Default <code>controller</code> <code>AbstractWorkfilesFrontend</code> <p>The control object.</p> required <code>parent</code> <code>QWidget</code> <p>The parent widget.</p> required <code>handle_expected_selection</code> <code>bool</code> <p>If True, the widget will handle the expected selection. Defaults to False.</p> <code>False</code> Source code in <code>client/ayon_core/tools/utils/folders_widget.py</code> <pre><code>class FoldersWidget(QtWidgets.QWidget):\n    \"\"\"Folders widget.\n\n    Widget that handles folders view, model and selection.\n\n    Expected selection handling is disabled by default. If enabled, the\n    widget will handle the expected in predefined way. Widget is listening\n    to event 'expected_selection_changed' with expected event data below,\n    the same data must be available when called method\n    'get_expected_selection_data' on controller.\n\n    {\n        \"folder\": {\n            \"current\": bool,               # Folder is what should be set now\n            \"folder_id\": Union[str, None], # Folder id that should be selected\n        },\n        ...\n    }\n\n    Selection is confirmed by calling method 'expected_folder_selected' on\n    controller.\n\n\n    Args:\n        controller (AbstractWorkfilesFrontend): The control object.\n        parent (QtWidgets.QWidget): The parent widget.\n        handle_expected_selection (bool): If True, the widget will handle\n            the expected selection. Defaults to False.\n    \"\"\"\n\n    double_clicked = QtCore.Signal(QtGui.QMouseEvent)\n    selection_changed = QtCore.Signal()\n    refreshed = QtCore.Signal()\n\n    def __init__(self, controller, parent, handle_expected_selection=False):\n        super().__init__(parent)\n\n        folders_view = TreeView(self)\n        folders_view.setHeaderHidden(True)\n\n        folders_model = FoldersQtModel(controller)\n        folders_proxy_model = FoldersProxyModel()\n        folders_proxy_model.setSourceModel(folders_model)\n        folders_proxy_model.setSortCaseSensitivity(QtCore.Qt.CaseInsensitive)\n\n        folders_view.setModel(folders_proxy_model)\n\n        main_layout = QtWidgets.QHBoxLayout(self)\n        main_layout.setContentsMargins(0, 0, 0, 0)\n        main_layout.addWidget(folders_view, 1)\n\n        controller.register_event_callback(\n            \"selection.project.changed\",\n            self._on_project_selection_change,\n        )\n        controller.register_event_callback(\n            \"folders.refresh.finished\",\n            self._on_folders_refresh_finished\n        )\n        controller.register_event_callback(\n            \"controller.refresh.finished\",\n            self._on_controller_refresh\n        )\n        controller.register_event_callback(\n            \"expected_selection_changed\",\n            self._on_expected_selection_change\n        )\n\n        selection_model = folders_view.selectionModel()\n        selection_model.selectionChanged.connect(self._on_selection_change)\n        folders_view.double_clicked.connect(self.double_clicked)\n        folders_model.refreshed.connect(self._on_model_refresh)\n\n        self._controller = controller\n        self._folders_view = folders_view\n        self._folders_model = folders_model\n        self._folders_proxy_model = folders_proxy_model\n\n        self._handle_expected_selection = handle_expected_selection\n        self._expected_selection = None\n\n    @property\n    def is_refreshing(self):\n        \"\"\"Model is refreshing.\n\n        Returns:\n            bool: True if model is refreshing.\n        \"\"\"\n\n        return self._folders_model.is_refreshing\n\n    @property\n    def has_content(self):\n        \"\"\"Has at least one folder.\n\n        Returns:\n            bool: True if model has at least one folder.\n        \"\"\"\n\n        return self._folders_model.has_content\n\n    def set_name_filter(self, name):\n        \"\"\"Set filter of folder name.\n\n        Args:\n            name (str): The string filter.\n        \"\"\"\n\n        self._folders_proxy_model.setFilterFixedString(name)\n        if name:\n            self._folders_view.expandAll()\n\n    def set_folder_ids_filter(self, folder_ids: Optional[list[str]]):\n        \"\"\"Set filter of folder ids.\n\n        Args:\n            folder_ids (list[str]): The list of folder ids.\n\n        \"\"\"\n        self._folders_proxy_model.set_folder_ids_filter(folder_ids)\n\n    def set_header_visible(self, visible: bool):\n        self._folders_view.setHeaderHidden(not visible)\n\n    def refresh(self):\n        \"\"\"Refresh folders model.\n\n        Force to update folders model from controller.\n        \"\"\"\n\n        self._folders_model.refresh()\n\n    def get_project_name(self):\n        \"\"\"Project name in which folders widget currently is.\n\n        Returns:\n            Union[str, None]: Currently used project name.\n        \"\"\"\n\n        return self._folders_model.get_project_name()\n\n    def set_project_name(self, project_name):\n        \"\"\"Set project name.\n\n        Do not use this method when controller is handling selection of\n        project using 'selection.project.changed' event.\n\n        Args:\n            project_name (str): Project name.\n        \"\"\"\n\n        self._folders_model.set_project_name(project_name)\n\n    def get_selected_folder_id(self):\n        \"\"\"Get selected folder id.\n\n        Returns:\n            Union[str, None]: Folder id which is selected.\n        \"\"\"\n\n        return self._get_selected_item_id()\n\n    def get_selected_folder_path(self):\n        \"\"\"Get selected folder id.\n\n        Returns:\n            Union[str, None]: Folder path which is selected.\n        \"\"\"\n\n        return self._get_selected_item_value(FOLDER_PATH_ROLE)\n\n    def get_selected_folder_label(self):\n        \"\"\"Selected folder label.\n\n        Returns:\n            Union[str, None]: Selected folder label.\n        \"\"\"\n\n        item_id = self._get_selected_item_id()\n        return self.get_folder_label(item_id)\n\n    def get_folder_label(self, folder_id):\n        \"\"\"Folder label for a given folder id.\n\n        Returns:\n            Union[str, None]: Folder label.\n        \"\"\"\n\n        index = self._folders_model.get_index_by_id(folder_id)\n        if index.isValid():\n            return index.data(QtCore.Qt.DisplayRole)\n        return None\n\n    def set_selected_folder(self, folder_id):\n        \"\"\"Change selection.\n\n        Args:\n            folder_id (Union[str, None]): Folder id or None to deselect.\n\n        Returns:\n            bool: Requested folder was selected.\n        \"\"\"\n        if folder_id is None:\n            self._folders_view.clearSelection()\n            return True\n\n        if folder_id == self._get_selected_item_id():\n            return True\n        index = self._folders_model.get_index_by_id(folder_id)\n        if not index.isValid():\n            return False\n\n        proxy_index = self._folders_proxy_model.mapFromSource(index)\n        if not proxy_index.isValid():\n            return False\n\n        selection_model = self._folders_view.selectionModel()\n        selection_model.setCurrentIndex(\n            proxy_index, QtCore.QItemSelectionModel.SelectCurrent\n        )\n        return True\n\n    def set_selected_folder_path(self, folder_path):\n        \"\"\"Set selected folder by path.\n\n        Args:\n            folder_path (str): Folder path.\n\n        Returns:\n            bool: Requested folder was selected.\n\n        \"\"\"\n        if folder_path is None:\n            self._folders_view.clearSelection()\n            return True\n\n        folder_id = self._folders_model.get_item_id_by_path(folder_path)\n        if folder_id is None:\n            return False\n        return self.set_selected_folder(folder_id)\n\n    def set_deselectable(self, enabled):\n        \"\"\"Set deselectable mode.\n\n        Items in view can be deselected.\n\n        Args:\n            enabled (bool): Enable deselectable mode.\n        \"\"\"\n\n        self._folders_view.set_deselectable(enabled)\n\n    def _get_selected_index(self):\n        return self._folders_model.get_index_by_id(\n            self.get_selected_folder_id()\n        )\n\n    def _on_project_selection_change(self, event):\n        project_name = event[\"project_name\"]\n        self.set_project_name(project_name)\n\n    def _on_folders_refresh_finished(self, event):\n        if event[\"sender\"] != FOLDERS_MODEL_SENDER_NAME:\n            self.set_project_name(event[\"project_name\"])\n\n    def _on_controller_refresh(self):\n        self._update_expected_selection()\n\n    def _on_model_refresh(self):\n        if self._expected_selection:\n            self._set_expected_selection()\n        self._folders_proxy_model.sort(0)\n        self.refreshed.emit()\n\n    def _get_selected_item_id(self):\n        return self._get_selected_item_value(FOLDER_ID_ROLE)\n\n    def _get_selected_item_value(self, role):\n        selection_model = self._folders_view.selectionModel()\n        for index in selection_model.selectedIndexes():\n            item_id = index.data(role)\n            if item_id is not None:\n                return item_id\n        return None\n\n    def _on_selection_change(self):\n        item_id = self._get_selected_item_id()\n        self._controller.set_selected_folder(item_id)\n        self.selection_changed.emit()\n\n    # Expected selection handling\n    def _on_expected_selection_change(self, event):\n        self._update_expected_selection(event.data)\n\n    def _update_expected_selection(self, expected_data=None):\n        if not self._handle_expected_selection:\n            return\n\n        if expected_data is None:\n            expected_data = self._controller.get_expected_selection_data()\n\n        folder_data = expected_data.get(\"folder\")\n        if not folder_data or not folder_data[\"current\"]:\n            return\n\n        folder_id = folder_data[\"id\"]\n        self._expected_selection = folder_id\n        if not self._folders_model.is_refreshing:\n            self._set_expected_selection()\n\n    def _set_expected_selection(self):\n        if not self._handle_expected_selection:\n            return\n\n        folder_id = self._expected_selection\n        self._expected_selection = None\n        if folder_id is not None:\n            self.set_selected_folder(folder_id)\n        self._controller.expected_folder_selected(folder_id)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/folders_widget.html#client.ayon_core.tools.utils.folders_widget.FoldersWidget.has_content","title":"<code>has_content</code>  <code>property</code>","text":"<p>Has at least one folder.</p> <p>Returns:</p> Name Type Description <code>bool</code> <p>True if model has at least one folder.</p>"},{"location":"autoapi/client/ayon_core/tools/utils/folders_widget.html#client.ayon_core.tools.utils.folders_widget.FoldersWidget.is_refreshing","title":"<code>is_refreshing</code>  <code>property</code>","text":"<p>Model is refreshing.</p> <p>Returns:</p> Name Type Description <code>bool</code> <p>True if model is refreshing.</p>"},{"location":"autoapi/client/ayon_core/tools/utils/folders_widget.html#client.ayon_core.tools.utils.folders_widget.FoldersWidget.get_folder_label","title":"<code>get_folder_label(folder_id)</code>","text":"<p>Folder label for a given folder id.</p> <p>Returns:</p> Type Description <p>Union[str, None]: Folder label.</p> Source code in <code>client/ayon_core/tools/utils/folders_widget.py</code> <pre><code>def get_folder_label(self, folder_id):\n    \"\"\"Folder label for a given folder id.\n\n    Returns:\n        Union[str, None]: Folder label.\n    \"\"\"\n\n    index = self._folders_model.get_index_by_id(folder_id)\n    if index.isValid():\n        return index.data(QtCore.Qt.DisplayRole)\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/folders_widget.html#client.ayon_core.tools.utils.folders_widget.FoldersWidget.get_project_name","title":"<code>get_project_name()</code>","text":"<p>Project name in which folders widget currently is.</p> <p>Returns:</p> Type Description <p>Union[str, None]: Currently used project name.</p> Source code in <code>client/ayon_core/tools/utils/folders_widget.py</code> <pre><code>def get_project_name(self):\n    \"\"\"Project name in which folders widget currently is.\n\n    Returns:\n        Union[str, None]: Currently used project name.\n    \"\"\"\n\n    return self._folders_model.get_project_name()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/folders_widget.html#client.ayon_core.tools.utils.folders_widget.FoldersWidget.get_selected_folder_id","title":"<code>get_selected_folder_id()</code>","text":"<p>Get selected folder id.</p> <p>Returns:</p> Type Description <p>Union[str, None]: Folder id which is selected.</p> Source code in <code>client/ayon_core/tools/utils/folders_widget.py</code> <pre><code>def get_selected_folder_id(self):\n    \"\"\"Get selected folder id.\n\n    Returns:\n        Union[str, None]: Folder id which is selected.\n    \"\"\"\n\n    return self._get_selected_item_id()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/folders_widget.html#client.ayon_core.tools.utils.folders_widget.FoldersWidget.get_selected_folder_label","title":"<code>get_selected_folder_label()</code>","text":"<p>Selected folder label.</p> <p>Returns:</p> Type Description <p>Union[str, None]: Selected folder label.</p> Source code in <code>client/ayon_core/tools/utils/folders_widget.py</code> <pre><code>def get_selected_folder_label(self):\n    \"\"\"Selected folder label.\n\n    Returns:\n        Union[str, None]: Selected folder label.\n    \"\"\"\n\n    item_id = self._get_selected_item_id()\n    return self.get_folder_label(item_id)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/folders_widget.html#client.ayon_core.tools.utils.folders_widget.FoldersWidget.get_selected_folder_path","title":"<code>get_selected_folder_path()</code>","text":"<p>Get selected folder id.</p> <p>Returns:</p> Type Description <p>Union[str, None]: Folder path which is selected.</p> Source code in <code>client/ayon_core/tools/utils/folders_widget.py</code> <pre><code>def get_selected_folder_path(self):\n    \"\"\"Get selected folder id.\n\n    Returns:\n        Union[str, None]: Folder path which is selected.\n    \"\"\"\n\n    return self._get_selected_item_value(FOLDER_PATH_ROLE)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/folders_widget.html#client.ayon_core.tools.utils.folders_widget.FoldersWidget.refresh","title":"<code>refresh()</code>","text":"<p>Refresh folders model.</p> <p>Force to update folders model from controller.</p> Source code in <code>client/ayon_core/tools/utils/folders_widget.py</code> <pre><code>def refresh(self):\n    \"\"\"Refresh folders model.\n\n    Force to update folders model from controller.\n    \"\"\"\n\n    self._folders_model.refresh()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/folders_widget.html#client.ayon_core.tools.utils.folders_widget.FoldersWidget.set_deselectable","title":"<code>set_deselectable(enabled)</code>","text":"<p>Set deselectable mode.</p> <p>Items in view can be deselected.</p> <p>Parameters:</p> Name Type Description Default <code>enabled</code> <code>bool</code> <p>Enable deselectable mode.</p> required Source code in <code>client/ayon_core/tools/utils/folders_widget.py</code> <pre><code>def set_deselectable(self, enabled):\n    \"\"\"Set deselectable mode.\n\n    Items in view can be deselected.\n\n    Args:\n        enabled (bool): Enable deselectable mode.\n    \"\"\"\n\n    self._folders_view.set_deselectable(enabled)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/folders_widget.html#client.ayon_core.tools.utils.folders_widget.FoldersWidget.set_folder_ids_filter","title":"<code>set_folder_ids_filter(folder_ids)</code>","text":"<p>Set filter of folder ids.</p> <p>Parameters:</p> Name Type Description Default <code>folder_ids</code> <code>list[str]</code> <p>The list of folder ids.</p> required Source code in <code>client/ayon_core/tools/utils/folders_widget.py</code> <pre><code>def set_folder_ids_filter(self, folder_ids: Optional[list[str]]):\n    \"\"\"Set filter of folder ids.\n\n    Args:\n        folder_ids (list[str]): The list of folder ids.\n\n    \"\"\"\n    self._folders_proxy_model.set_folder_ids_filter(folder_ids)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/folders_widget.html#client.ayon_core.tools.utils.folders_widget.FoldersWidget.set_name_filter","title":"<code>set_name_filter(name)</code>","text":"<p>Set filter of folder name.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The string filter.</p> required Source code in <code>client/ayon_core/tools/utils/folders_widget.py</code> <pre><code>def set_name_filter(self, name):\n    \"\"\"Set filter of folder name.\n\n    Args:\n        name (str): The string filter.\n    \"\"\"\n\n    self._folders_proxy_model.setFilterFixedString(name)\n    if name:\n        self._folders_view.expandAll()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/folders_widget.html#client.ayon_core.tools.utils.folders_widget.FoldersWidget.set_project_name","title":"<code>set_project_name(project_name)</code>","text":"<p>Set project name.</p> <p>Do not use this method when controller is handling selection of project using 'selection.project.changed' event.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required Source code in <code>client/ayon_core/tools/utils/folders_widget.py</code> <pre><code>def set_project_name(self, project_name):\n    \"\"\"Set project name.\n\n    Do not use this method when controller is handling selection of\n    project using 'selection.project.changed' event.\n\n    Args:\n        project_name (str): Project name.\n    \"\"\"\n\n    self._folders_model.set_project_name(project_name)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/folders_widget.html#client.ayon_core.tools.utils.folders_widget.FoldersWidget.set_selected_folder","title":"<code>set_selected_folder(folder_id)</code>","text":"<p>Change selection.</p> <p>Parameters:</p> Name Type Description Default <code>folder_id</code> <code>Union[str, None]</code> <p>Folder id or None to deselect.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <p>Requested folder was selected.</p> Source code in <code>client/ayon_core/tools/utils/folders_widget.py</code> <pre><code>def set_selected_folder(self, folder_id):\n    \"\"\"Change selection.\n\n    Args:\n        folder_id (Union[str, None]): Folder id or None to deselect.\n\n    Returns:\n        bool: Requested folder was selected.\n    \"\"\"\n    if folder_id is None:\n        self._folders_view.clearSelection()\n        return True\n\n    if folder_id == self._get_selected_item_id():\n        return True\n    index = self._folders_model.get_index_by_id(folder_id)\n    if not index.isValid():\n        return False\n\n    proxy_index = self._folders_proxy_model.mapFromSource(index)\n    if not proxy_index.isValid():\n        return False\n\n    selection_model = self._folders_view.selectionModel()\n    selection_model.setCurrentIndex(\n        proxy_index, QtCore.QItemSelectionModel.SelectCurrent\n    )\n    return True\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/folders_widget.html#client.ayon_core.tools.utils.folders_widget.FoldersWidget.set_selected_folder_path","title":"<code>set_selected_folder_path(folder_path)</code>","text":"<p>Set selected folder by path.</p> <p>Parameters:</p> Name Type Description Default <code>folder_path</code> <code>str</code> <p>Folder path.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <p>Requested folder was selected.</p> Source code in <code>client/ayon_core/tools/utils/folders_widget.py</code> <pre><code>def set_selected_folder_path(self, folder_path):\n    \"\"\"Set selected folder by path.\n\n    Args:\n        folder_path (str): Folder path.\n\n    Returns:\n        bool: Requested folder was selected.\n\n    \"\"\"\n    if folder_path is None:\n        self._folders_view.clearSelection()\n        return True\n\n    folder_id = self._folders_model.get_item_id_by_path(folder_path)\n    if folder_id is None:\n        return False\n    return self.set_selected_folder(folder_id)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/folders_widget.html#client.ayon_core.tools.utils.folders_widget.SimpleFoldersController","title":"<code>SimpleFoldersController</code>","text":"<p>               Bases: <code>object</code></p> Source code in <code>client/ayon_core/tools/utils/folders_widget.py</code> <pre><code>class SimpleFoldersController(object):\n    def __init__(self):\n        self._event_system = self._create_event_system()\n        self._projects_model = ProjectsModel(self)\n        self._hierarchy_model = HierarchyModel(self)\n        self._selection_model = SimpleSelectionModel(self)\n        self._expected_selection = HierarchyExpectedSelection(\n            self, handle_project=False, handle_folder=True, handle_task=False\n        )\n\n    def emit_event(self, topic, data=None, source=None):\n        \"\"\"Use implemented event system to trigger event.\"\"\"\n\n        if data is None:\n            data = {}\n        self._event_system.emit(topic, data, source)\n\n    def register_event_callback(self, topic, callback):\n        self._event_system.add_callback(topic, callback)\n\n    # Model functions\n    def get_folder_items(self, project_name, sender=None):\n        return self._hierarchy_model.get_folder_items(project_name, sender)\n\n    def get_folder_type_items(self, project_name, sender=None):\n        return self._projects_model.get_folder_type_items(\n            project_name, sender\n        )\n\n    def set_selected_project(self, project_name):\n        self._selection_model.set_selected_project(project_name)\n\n    def set_selected_folder(self, folder_id):\n        self._selection_model.set_selected_folder(folder_id)\n\n    def get_expected_selection_data(self):\n        self._expected_selection.get_expected_selection_data()\n\n    def expected_folder_selected(self, folder_id):\n        self._expected_selection.expected_folder_selected(folder_id)\n\n    def _create_event_system(self):\n        return QueuedEventSystem()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/folders_widget.html#client.ayon_core.tools.utils.folders_widget.SimpleFoldersController.emit_event","title":"<code>emit_event(topic, data=None, source=None)</code>","text":"<p>Use implemented event system to trigger event.</p> Source code in <code>client/ayon_core/tools/utils/folders_widget.py</code> <pre><code>def emit_event(self, topic, data=None, source=None):\n    \"\"\"Use implemented event system to trigger event.\"\"\"\n\n    if data is None:\n        data = {}\n    self._event_system.emit(topic, data, source)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/folders_widget.html#client.ayon_core.tools.utils.folders_widget.SimpleFoldersWidget","title":"<code>SimpleFoldersWidget</code>","text":"<p>               Bases: <code>FoldersWidget</code></p> Source code in <code>client/ayon_core/tools/utils/folders_widget.py</code> <pre><code>class SimpleFoldersWidget(FoldersWidget):\n    def __init__(self, controller=None, *args, **kwargs):\n        if controller is None:\n            controller = SimpleFoldersController()\n        super(SimpleFoldersWidget, self).__init__(controller, *args, **kwargs)\n\n    def set_project_name(self, project_name):\n        self._controller.set_selected_project(project_name)\n        super(SimpleFoldersWidget, self).set_project_name(project_name)\n\n    def _on_project_selection_change(self, event):\n        \"\"\"Ignore project selection change from controller.\n\n        Only who can trigger project change is this widget with\n            'set_project_name' which already cares about project change.\n\n        Args:\n            event (Event): Triggered event.\n        \"\"\"\n        pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/folders_widget.html#client.ayon_core.tools.utils.folders_widget.SimpleSelectionModel","title":"<code>SimpleSelectionModel</code>","text":"<p>               Bases: <code>object</code></p> <p>Model handling selection changes.</p> <p>Triggering events: - \"selection.project.changed\" - \"selection.folder.changed\"</p> Source code in <code>client/ayon_core/tools/utils/folders_widget.py</code> <pre><code>class SimpleSelectionModel(object):\n    \"\"\"Model handling selection changes.\n\n    Triggering events:\n    - \"selection.project.changed\"\n    - \"selection.folder.changed\"\n    \"\"\"\n\n    event_source = \"selection.model\"\n\n    def __init__(self, controller):\n        self._controller = controller\n\n        self._project_name = None\n        self._folder_id = None\n        self._task_id = None\n        self._task_name = None\n\n    def get_selected_project_name(self):\n        return self._project_name\n\n    def set_selected_project(self, project_name):\n        self._project_name = project_name\n        self._controller.emit_event(\n            \"selection.project.changed\",\n            {\"project_name\": project_name},\n            self.event_source\n        )\n\n    def get_selected_folder_id(self):\n        return self._folder_id\n\n    def set_selected_folder(self, folder_id):\n        if folder_id == self._folder_id:\n            return\n        self._folder_id = folder_id\n        self._controller.emit_event(\n            \"selection.folder.changed\",\n            {\n                \"project_name\": self._project_name,\n                \"folder_id\": folder_id,\n            },\n            self.event_source\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/host_tools.html","title":"host_tools","text":"<p>Single access point to all tools usable in hosts.</p> <p>It is possible to create <code>HostToolsHelper</code> in host implementation or use singleton approach with global functions (using helper anyway).</p>"},{"location":"autoapi/client/ayon_core/tools/utils/host_tools.html#client.ayon_core.tools.utils.host_tools.HostToolsHelper","title":"<code>HostToolsHelper</code>","text":"<p>Create and cache tool windows in memory.</p> <p>Almost all methods expect parent widget but the parent is used only on first tool creation.</p> <p>Class may also contain tools that are available only for one or few hosts.</p> Source code in <code>client/ayon_core/tools/utils/host_tools.py</code> <pre><code>class HostToolsHelper:\n    \"\"\"Create and cache tool windows in memory.\n\n    Almost all methods expect parent widget but the parent is used only on\n    first tool creation.\n\n    Class may also contain tools that are available only for one or few hosts.\n    \"\"\"\n\n    def __init__(self, parent=None):\n        self._log = None\n        # Global parent for all tools (may and may not be set)\n        self._parent = parent\n\n        # Prepare attributes for all tools\n        self._workfiles_tool = None\n        self._loader_tool = None\n        self._publisher_tool = None\n        self._scene_inventory_tool = None\n        self._experimental_tools_dialog = None\n\n    @property\n    def log(self):\n        if self._log is None:\n            self._log = Logger.get_logger(self.__class__.__name__)\n        return self._log\n\n    def get_workfiles_tool(self, parent):\n        \"\"\"Create, cache and return workfiles tool window.\"\"\"\n        if self._workfiles_tool is None:\n            from ayon_core.tools.workfiles.widgets import WorkfilesToolWindow\n\n            workfiles_window = WorkfilesToolWindow(parent=parent)\n            self._workfiles_tool = workfiles_window\n\n        return self._workfiles_tool\n\n    def show_workfiles(\n        self,\n        parent: QWidget | None = None,\n        *,\n        use_context: bool = False,\n        save: bool | None = None,\n        on_top: bool | None = None,\n    ) -&gt; QWidget:\n        \"\"\"Workfiles tool for changing context and saving workfiles.\"\"\"\n\n        with qt_app_context():\n            workfiles_tool = self.get_workfiles_tool(parent)\n            workfiles_tool.ensure_visible(use_context, save, on_top)\n\n        return workfiles_tool\n\n    def get_loader_tool(self, parent):\n        \"\"\"Create, cache and return loader tool window.\"\"\"\n        if self._loader_tool is None:\n            from ayon_core.tools.loader.ui import LoaderWindow\n            from ayon_core.tools.loader import LoaderController\n\n            host = registered_host()\n            ILoadHost.validate_load_methods(host)\n\n            controller = LoaderController(host=host)\n            loader_window = LoaderWindow(\n                controller=controller,\n                parent=parent or self._parent\n            )\n\n            self._loader_tool = loader_window\n\n        return self._loader_tool\n\n    def show_loader(\n        self, parent: QWidget | None = None\n    ) -&gt; QWidget:\n        \"\"\"Loader tool for loading representations.\n\n        Args:\n            parent (QWidget): parent widget\n\n        Returns:\n            QWidget of the tool shown.\n\n        \"\"\"\n        with qt_app_context():\n            loader_tool = self.get_loader_tool(parent)\n\n            loader_tool.show()\n            loader_tool.raise_()\n            loader_tool.activateWindow()\n            loader_tool.showNormal()\n            loader_tool.refresh()\n        return loader_tool\n\n    def get_scene_inventory_tool(self, parent):\n        \"\"\"Create, cache and return scene inventory tool window.\"\"\"\n        if self._scene_inventory_tool is None:\n            host = registered_host()\n            ILoadHost.validate_load_methods(host)\n\n            from ayon_core.tools.sceneinventory.window import (\n                SceneInventoryWindow)\n\n            scene_inventory_window = SceneInventoryWindow(\n                parent=parent or self._parent\n            )\n            self._scene_inventory_tool = scene_inventory_window\n\n        return self._scene_inventory_tool\n\n    def show_scene_inventory(\n        self, parent: QWidget | None = None\n    ) -&gt; QWidget:\n        \"\"\"Show tool maintain loaded containers.\"\"\"\n        with qt_app_context():\n            scene_inventory_tool = self.get_scene_inventory_tool(parent)\n            scene_inventory_tool.show()\n            scene_inventory_tool.refresh()\n\n            # Pull window to the front.\n            scene_inventory_tool.raise_()\n            scene_inventory_tool.activateWindow()\n            scene_inventory_tool.showNormal()\n\n        return scene_inventory_tool\n\n    def get_library_loader_tool(self, parent):\n        \"\"\"Create, cache and return library loader tool window.\"\"\"\n        return self.get_loader_tool(parent)\n\n    def show_library_loader(self, parent: QWidget | None = None) -&gt; QWidget:\n        \"\"\"Loader tool for loading representations from library project.\n\n        Args:\n            parent (QWidget): parent widget\n\n        Returns:\n            QWidget of the tool shown.\n\n        \"\"\"\n        return self.show_loader(parent)\n\n    def show_publish(\n        self, parent: QWidget | None = None\n    ) -&gt; QWidget:\n        \"\"\"Try showing the most desirable publish GUI\n\n        This function cycles through the currently registered\n        graphical user interfaces, if any, and presents it to\n        the user.\n        \"\"\"\n\n        pyblish_show = self._discover_pyblish_gui()\n        return pyblish_show(parent)\n\n    def _discover_pyblish_gui(self):\n        \"\"\"Return the most desirable of the currently registered GUIs\"\"\"\n        # Prefer last registered\n        guis = list(reversed(pyblish.api.registered_guis()))\n        for gui in guis:\n            try:\n                gui = __import__(gui).show\n            except (ImportError, AttributeError):\n                continue\n            else:\n                return gui\n\n        raise ImportError(\"No Pyblish GUI found\")\n\n    def get_experimental_tools_dialog(self, parent=None):\n        \"\"\"Dialog of experimental tools.\n\n        For some hosts it is not easy to modify menu of tools. For\n        those cases was added experimental tools dialog which is Qt based\n        and can dynamically filled by experimental tools so\n        host need only single \"Experimental tools\" button to see them.\n\n        Dialog can be also empty with a message that there are not available\n        experimental tools.\n        \"\"\"\n        if self._experimental_tools_dialog is None:\n            from ayon_core.tools.experimental_tools import (\n                ExperimentalToolsDialog\n            )\n\n            self._experimental_tools_dialog = ExperimentalToolsDialog(parent)\n        return self._experimental_tools_dialog\n\n    def show_experimental_tools_dialog(\n        self, parent: QWidget | None = None\n    ) -&gt; QWidget:\n        \"\"\"Show dialog with experimental tools.\n\n        Args:\n            parent (QWidget): parent widget.\n\n        Returns:\n            QWidget of the tool shown.\n\n        \"\"\"\n        with qt_app_context():\n            dialog = self.get_experimental_tools_dialog(parent)\n\n            dialog.show()\n            dialog.raise_()\n            dialog.activateWindow()\n            dialog.showNormal()\n\n        return dialog\n\n    def get_publisher_tool(self, parent=None, controller=None):\n        \"\"\"Create, cache and return publisher window.\"\"\"\n\n        if self._publisher_tool is None:\n            from ayon_core.tools.publisher.window import PublisherWindow\n\n            host = registered_host()\n            IPublishHost.validate_publish_methods(host)\n\n            publisher_window = PublisherWindow(\n                controller=controller,\n                parent=parent or self._parent\n            )\n            self._publisher_tool = publisher_window\n\n        return self._publisher_tool\n\n    def show_publisher_tool(\n        self,\n        parent: QWidget | None = None,\n        controller: AbstractPublisherFrontend | None = None,\n        tab: PublisherTabs | None = None\n    ) -&gt; QWidget:\n        with qt_app_context():\n            window = self.get_publisher_tool(parent, controller)\n            if tab:\n                window.set_current_tab(tab)\n            window.make_sure_is_visible()\n            return window\n\n    def get_tool_by_name(\n        self,\n        tool_name: PublisherTabs,\n        parent: QWidget | None = None,\n        *args,\n        **kwargs\n    ) -&gt; QWidget | None:\n        \"\"\"Show tool by its name.\n\n        This is helper for `show_tool_by_name` if you need only get\n        tool window without showing it.\n\n        Args:\n            tool_name: name of the tool\n            parent: tool parent\n            *args: tool args\n            **kwargs: tool kwargs\n\n        Returns:\n            QWidget of the tool shown or None if tool name is unknown\n            or tool can't be created.\n\n        \"\"\"\n        if tool_name == \"workfiles\":\n            return self.get_workfiles_tool(parent)\n\n        if tool_name == \"loader\":\n            return self.get_loader_tool(parent)\n\n        if tool_name == \"libraryloader\":\n            return self.get_library_loader_tool(parent)\n\n        if tool_name == \"sceneinventory\":\n            return self.get_scene_inventory_tool(parent)\n\n        if tool_name == \"publisher\":\n            return self.get_publisher_tool(parent, *args, **kwargs)\n\n        if tool_name == \"experimental_tools\":\n            return self.get_experimental_tools_dialog(parent)\n\n        if tool_name == \"publish\":\n            self.log.info(\"Can't return publish tool window.\")\n            return None\n\n        self.log.warning(\n            'Cannot show unknown tool name: \"%s\"', tool_name)\n        return None\n\n    def show_tool_by_name(\n        self,\n        tool_name: ToolName,\n        parent: QWidget | None = None,\n        *args,\n        **kwargs\n    ) -&gt; QWidget | None:\n        \"\"\"Show tool by its name.\n\n        This is helper for showing tool window by its name. It is possible\n        to pass additional args and kwargs.\n\n        Args:\n            tool_name: name of the tool\n            parent: tool parent\n            *args: tool args\n            **kwargs: tool kwargs\n\n        Returns:\n            QWidget of the tool shown or None if tool name is unknown\n            or tool can't be created.\n\n        \"\"\"\n        if tool_name == \"workfiles\":\n            return self.show_workfiles(parent, *args, **kwargs)\n\n        if tool_name == \"loader\":\n            return self.show_loader(parent)\n\n        if tool_name == \"libraryloader\":\n            return self.show_library_loader(parent)\n\n        if tool_name == \"sceneinventory\":\n            return self.show_scene_inventory(parent)\n\n        if tool_name == \"publish\":\n            return self.show_publish(parent)\n\n        if tool_name == \"publisher\":\n            return self.show_publisher_tool(parent, *args, **kwargs)\n\n        if tool_name == \"experimental_tools\":\n            return self.show_experimental_tools_dialog(parent)\n\n        self.log.warning(\n            \"Can't show unknown tool name: \\\"%s\\\"\", tool_name\n        )\n        return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/host_tools.html#client.ayon_core.tools.utils.host_tools.HostToolsHelper.get_experimental_tools_dialog","title":"<code>get_experimental_tools_dialog(parent=None)</code>","text":"<p>Dialog of experimental tools.</p> <p>For some hosts it is not easy to modify menu of tools. For those cases was added experimental tools dialog which is Qt based and can dynamically filled by experimental tools so host need only single \"Experimental tools\" button to see them.</p> <p>Dialog can be also empty with a message that there are not available experimental tools.</p> Source code in <code>client/ayon_core/tools/utils/host_tools.py</code> <pre><code>def get_experimental_tools_dialog(self, parent=None):\n    \"\"\"Dialog of experimental tools.\n\n    For some hosts it is not easy to modify menu of tools. For\n    those cases was added experimental tools dialog which is Qt based\n    and can dynamically filled by experimental tools so\n    host need only single \"Experimental tools\" button to see them.\n\n    Dialog can be also empty with a message that there are not available\n    experimental tools.\n    \"\"\"\n    if self._experimental_tools_dialog is None:\n        from ayon_core.tools.experimental_tools import (\n            ExperimentalToolsDialog\n        )\n\n        self._experimental_tools_dialog = ExperimentalToolsDialog(parent)\n    return self._experimental_tools_dialog\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/host_tools.html#client.ayon_core.tools.utils.host_tools.HostToolsHelper.get_library_loader_tool","title":"<code>get_library_loader_tool(parent)</code>","text":"<p>Create, cache and return library loader tool window.</p> Source code in <code>client/ayon_core/tools/utils/host_tools.py</code> <pre><code>def get_library_loader_tool(self, parent):\n    \"\"\"Create, cache and return library loader tool window.\"\"\"\n    return self.get_loader_tool(parent)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/host_tools.html#client.ayon_core.tools.utils.host_tools.HostToolsHelper.get_loader_tool","title":"<code>get_loader_tool(parent)</code>","text":"<p>Create, cache and return loader tool window.</p> Source code in <code>client/ayon_core/tools/utils/host_tools.py</code> <pre><code>def get_loader_tool(self, parent):\n    \"\"\"Create, cache and return loader tool window.\"\"\"\n    if self._loader_tool is None:\n        from ayon_core.tools.loader.ui import LoaderWindow\n        from ayon_core.tools.loader import LoaderController\n\n        host = registered_host()\n        ILoadHost.validate_load_methods(host)\n\n        controller = LoaderController(host=host)\n        loader_window = LoaderWindow(\n            controller=controller,\n            parent=parent or self._parent\n        )\n\n        self._loader_tool = loader_window\n\n    return self._loader_tool\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/host_tools.html#client.ayon_core.tools.utils.host_tools.HostToolsHelper.get_publisher_tool","title":"<code>get_publisher_tool(parent=None, controller=None)</code>","text":"<p>Create, cache and return publisher window.</p> Source code in <code>client/ayon_core/tools/utils/host_tools.py</code> <pre><code>def get_publisher_tool(self, parent=None, controller=None):\n    \"\"\"Create, cache and return publisher window.\"\"\"\n\n    if self._publisher_tool is None:\n        from ayon_core.tools.publisher.window import PublisherWindow\n\n        host = registered_host()\n        IPublishHost.validate_publish_methods(host)\n\n        publisher_window = PublisherWindow(\n            controller=controller,\n            parent=parent or self._parent\n        )\n        self._publisher_tool = publisher_window\n\n    return self._publisher_tool\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/host_tools.html#client.ayon_core.tools.utils.host_tools.HostToolsHelper.get_scene_inventory_tool","title":"<code>get_scene_inventory_tool(parent)</code>","text":"<p>Create, cache and return scene inventory tool window.</p> Source code in <code>client/ayon_core/tools/utils/host_tools.py</code> <pre><code>def get_scene_inventory_tool(self, parent):\n    \"\"\"Create, cache and return scene inventory tool window.\"\"\"\n    if self._scene_inventory_tool is None:\n        host = registered_host()\n        ILoadHost.validate_load_methods(host)\n\n        from ayon_core.tools.sceneinventory.window import (\n            SceneInventoryWindow)\n\n        scene_inventory_window = SceneInventoryWindow(\n            parent=parent or self._parent\n        )\n        self._scene_inventory_tool = scene_inventory_window\n\n    return self._scene_inventory_tool\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/host_tools.html#client.ayon_core.tools.utils.host_tools.HostToolsHelper.get_tool_by_name","title":"<code>get_tool_by_name(tool_name, parent=None, *args, **kwargs)</code>","text":"<p>Show tool by its name.</p> <p>This is helper for <code>show_tool_by_name</code> if you need only get tool window without showing it.</p> <p>Parameters:</p> Name Type Description Default <code>tool_name</code> <code>PublisherTabs</code> <p>name of the tool</p> required <code>parent</code> <code>QWidget | None</code> <p>tool parent</p> <code>None</code> <code>*args</code> <p>tool args</p> <code>()</code> <code>**kwargs</code> <p>tool kwargs</p> <code>{}</code> <p>Returns:</p> Type Description <code>QWidget | None</code> <p>QWidget of the tool shown or None if tool name is unknown</p> <code>QWidget | None</code> <p>or tool can't be created.</p> Source code in <code>client/ayon_core/tools/utils/host_tools.py</code> <pre><code>def get_tool_by_name(\n    self,\n    tool_name: PublisherTabs,\n    parent: QWidget | None = None,\n    *args,\n    **kwargs\n) -&gt; QWidget | None:\n    \"\"\"Show tool by its name.\n\n    This is helper for `show_tool_by_name` if you need only get\n    tool window without showing it.\n\n    Args:\n        tool_name: name of the tool\n        parent: tool parent\n        *args: tool args\n        **kwargs: tool kwargs\n\n    Returns:\n        QWidget of the tool shown or None if tool name is unknown\n        or tool can't be created.\n\n    \"\"\"\n    if tool_name == \"workfiles\":\n        return self.get_workfiles_tool(parent)\n\n    if tool_name == \"loader\":\n        return self.get_loader_tool(parent)\n\n    if tool_name == \"libraryloader\":\n        return self.get_library_loader_tool(parent)\n\n    if tool_name == \"sceneinventory\":\n        return self.get_scene_inventory_tool(parent)\n\n    if tool_name == \"publisher\":\n        return self.get_publisher_tool(parent, *args, **kwargs)\n\n    if tool_name == \"experimental_tools\":\n        return self.get_experimental_tools_dialog(parent)\n\n    if tool_name == \"publish\":\n        self.log.info(\"Can't return publish tool window.\")\n        return None\n\n    self.log.warning(\n        'Cannot show unknown tool name: \"%s\"', tool_name)\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/host_tools.html#client.ayon_core.tools.utils.host_tools.HostToolsHelper.get_workfiles_tool","title":"<code>get_workfiles_tool(parent)</code>","text":"<p>Create, cache and return workfiles tool window.</p> Source code in <code>client/ayon_core/tools/utils/host_tools.py</code> <pre><code>def get_workfiles_tool(self, parent):\n    \"\"\"Create, cache and return workfiles tool window.\"\"\"\n    if self._workfiles_tool is None:\n        from ayon_core.tools.workfiles.widgets import WorkfilesToolWindow\n\n        workfiles_window = WorkfilesToolWindow(parent=parent)\n        self._workfiles_tool = workfiles_window\n\n    return self._workfiles_tool\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/host_tools.html#client.ayon_core.tools.utils.host_tools.HostToolsHelper.show_experimental_tools_dialog","title":"<code>show_experimental_tools_dialog(parent=None)</code>","text":"<p>Show dialog with experimental tools.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <code>QWidget</code> <p>parent widget.</p> <code>None</code> <p>Returns:</p> Type Description <code>QWidget</code> <p>QWidget of the tool shown.</p> Source code in <code>client/ayon_core/tools/utils/host_tools.py</code> <pre><code>def show_experimental_tools_dialog(\n    self, parent: QWidget | None = None\n) -&gt; QWidget:\n    \"\"\"Show dialog with experimental tools.\n\n    Args:\n        parent (QWidget): parent widget.\n\n    Returns:\n        QWidget of the tool shown.\n\n    \"\"\"\n    with qt_app_context():\n        dialog = self.get_experimental_tools_dialog(parent)\n\n        dialog.show()\n        dialog.raise_()\n        dialog.activateWindow()\n        dialog.showNormal()\n\n    return dialog\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/host_tools.html#client.ayon_core.tools.utils.host_tools.HostToolsHelper.show_library_loader","title":"<code>show_library_loader(parent=None)</code>","text":"<p>Loader tool for loading representations from library project.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <code>QWidget</code> <p>parent widget</p> <code>None</code> <p>Returns:</p> Type Description <code>QWidget</code> <p>QWidget of the tool shown.</p> Source code in <code>client/ayon_core/tools/utils/host_tools.py</code> <pre><code>def show_library_loader(self, parent: QWidget | None = None) -&gt; QWidget:\n    \"\"\"Loader tool for loading representations from library project.\n\n    Args:\n        parent (QWidget): parent widget\n\n    Returns:\n        QWidget of the tool shown.\n\n    \"\"\"\n    return self.show_loader(parent)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/host_tools.html#client.ayon_core.tools.utils.host_tools.HostToolsHelper.show_loader","title":"<code>show_loader(parent=None)</code>","text":"<p>Loader tool for loading representations.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <code>QWidget</code> <p>parent widget</p> <code>None</code> <p>Returns:</p> Type Description <code>QWidget</code> <p>QWidget of the tool shown.</p> Source code in <code>client/ayon_core/tools/utils/host_tools.py</code> <pre><code>def show_loader(\n    self, parent: QWidget | None = None\n) -&gt; QWidget:\n    \"\"\"Loader tool for loading representations.\n\n    Args:\n        parent (QWidget): parent widget\n\n    Returns:\n        QWidget of the tool shown.\n\n    \"\"\"\n    with qt_app_context():\n        loader_tool = self.get_loader_tool(parent)\n\n        loader_tool.show()\n        loader_tool.raise_()\n        loader_tool.activateWindow()\n        loader_tool.showNormal()\n        loader_tool.refresh()\n    return loader_tool\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/host_tools.html#client.ayon_core.tools.utils.host_tools.HostToolsHelper.show_publish","title":"<code>show_publish(parent=None)</code>","text":"<p>Try showing the most desirable publish GUI</p> <p>This function cycles through the currently registered graphical user interfaces, if any, and presents it to the user.</p> Source code in <code>client/ayon_core/tools/utils/host_tools.py</code> <pre><code>def show_publish(\n    self, parent: QWidget | None = None\n) -&gt; QWidget:\n    \"\"\"Try showing the most desirable publish GUI\n\n    This function cycles through the currently registered\n    graphical user interfaces, if any, and presents it to\n    the user.\n    \"\"\"\n\n    pyblish_show = self._discover_pyblish_gui()\n    return pyblish_show(parent)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/host_tools.html#client.ayon_core.tools.utils.host_tools.HostToolsHelper.show_scene_inventory","title":"<code>show_scene_inventory(parent=None)</code>","text":"<p>Show tool maintain loaded containers.</p> Source code in <code>client/ayon_core/tools/utils/host_tools.py</code> <pre><code>def show_scene_inventory(\n    self, parent: QWidget | None = None\n) -&gt; QWidget:\n    \"\"\"Show tool maintain loaded containers.\"\"\"\n    with qt_app_context():\n        scene_inventory_tool = self.get_scene_inventory_tool(parent)\n        scene_inventory_tool.show()\n        scene_inventory_tool.refresh()\n\n        # Pull window to the front.\n        scene_inventory_tool.raise_()\n        scene_inventory_tool.activateWindow()\n        scene_inventory_tool.showNormal()\n\n    return scene_inventory_tool\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/host_tools.html#client.ayon_core.tools.utils.host_tools.HostToolsHelper.show_tool_by_name","title":"<code>show_tool_by_name(tool_name, parent=None, *args, **kwargs)</code>","text":"<p>Show tool by its name.</p> <p>This is helper for showing tool window by its name. It is possible to pass additional args and kwargs.</p> <p>Parameters:</p> Name Type Description Default <code>tool_name</code> <code>ToolName</code> <p>name of the tool</p> required <code>parent</code> <code>QWidget | None</code> <p>tool parent</p> <code>None</code> <code>*args</code> <p>tool args</p> <code>()</code> <code>**kwargs</code> <p>tool kwargs</p> <code>{}</code> <p>Returns:</p> Type Description <code>QWidget | None</code> <p>QWidget of the tool shown or None if tool name is unknown</p> <code>QWidget | None</code> <p>or tool can't be created.</p> Source code in <code>client/ayon_core/tools/utils/host_tools.py</code> <pre><code>def show_tool_by_name(\n    self,\n    tool_name: ToolName,\n    parent: QWidget | None = None,\n    *args,\n    **kwargs\n) -&gt; QWidget | None:\n    \"\"\"Show tool by its name.\n\n    This is helper for showing tool window by its name. It is possible\n    to pass additional args and kwargs.\n\n    Args:\n        tool_name: name of the tool\n        parent: tool parent\n        *args: tool args\n        **kwargs: tool kwargs\n\n    Returns:\n        QWidget of the tool shown or None if tool name is unknown\n        or tool can't be created.\n\n    \"\"\"\n    if tool_name == \"workfiles\":\n        return self.show_workfiles(parent, *args, **kwargs)\n\n    if tool_name == \"loader\":\n        return self.show_loader(parent)\n\n    if tool_name == \"libraryloader\":\n        return self.show_library_loader(parent)\n\n    if tool_name == \"sceneinventory\":\n        return self.show_scene_inventory(parent)\n\n    if tool_name == \"publish\":\n        return self.show_publish(parent)\n\n    if tool_name == \"publisher\":\n        return self.show_publisher_tool(parent, *args, **kwargs)\n\n    if tool_name == \"experimental_tools\":\n        return self.show_experimental_tools_dialog(parent)\n\n    self.log.warning(\n        \"Can't show unknown tool name: \\\"%s\\\"\", tool_name\n    )\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/host_tools.html#client.ayon_core.tools.utils.host_tools.HostToolsHelper.show_workfiles","title":"<code>show_workfiles(parent=None, *, use_context=False, save=None, on_top=None)</code>","text":"<p>Workfiles tool for changing context and saving workfiles.</p> Source code in <code>client/ayon_core/tools/utils/host_tools.py</code> <pre><code>def show_workfiles(\n    self,\n    parent: QWidget | None = None,\n    *,\n    use_context: bool = False,\n    save: bool | None = None,\n    on_top: bool | None = None,\n) -&gt; QWidget:\n    \"\"\"Workfiles tool for changing context and saving workfiles.\"\"\"\n\n    with qt_app_context():\n        workfiles_tool = self.get_workfiles_tool(parent)\n        workfiles_tool.ensure_visible(use_context, save, on_top)\n\n    return workfiles_tool\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/host_tools.html#client.ayon_core.tools.utils.host_tools.show_experimental_tools_dialog","title":"<code>show_experimental_tools_dialog(parent=None)</code>","text":"<p>Show experimental tools dialog.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <code>QWidget | None</code> <p>tool parent</p> <code>None</code> <p>Returns:</p> Type Description <code>QWidget | None</code> <p>QWidget of the tool</p> Source code in <code>client/ayon_core/tools/utils/host_tools.py</code> <pre><code>def show_experimental_tools_dialog(\n    parent: QWidget | None = None\n) -&gt; QWidget | None:\n    \"\"\"Show experimental tools dialog.\n\n    Args:\n        parent: tool parent\n\n    Returns:\n        QWidget of the tool\n\n    \"\"\"\n    return _SingletonPoint.show_tool_by_name(\n        \"experimental_tools\", parent\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/host_tools.html#client.ayon_core.tools.utils.host_tools.show_loader","title":"<code>show_loader(parent=None, *, use_context=False)</code>","text":"<p>Show loader tool.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <code>QWidget | None</code> <p>tool parent,</p> <code>None</code> <code>use_context</code> <code>bool</code> <p>use context,</p> <code>False</code> <p>Returns:</p> Type Description <code>QWidget | None</code> <p>QWidget of the tool</p> Source code in <code>client/ayon_core/tools/utils/host_tools.py</code> <pre><code>def show_loader(\n    parent: QWidget | None = None,\n    *,\n    use_context: bool = False\n) -&gt; QWidget | None:\n    \"\"\"Show loader tool.\n\n    Args:\n        parent: tool parent,\n        use_context: use context,\n\n    Returns:\n        QWidget of the tool\n\n    \"\"\"\n    return _SingletonPoint.show_tool_by_name(\n        \"loader\", parent, use_context=use_context\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/host_tools.html#client.ayon_core.tools.utils.host_tools.show_publish","title":"<code>show_publish(parent=None)</code>","text":"<p>Show publish tool.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <code>QWidget | None</code> <p>tool parent</p> <code>None</code> <p>Returns:</p> Type Description <code>QWidget | None</code> <p>QWidget of the tool</p> Source code in <code>client/ayon_core/tools/utils/host_tools.py</code> <pre><code>def show_publish(parent: QWidget | None = None) -&gt; QWidget | None:\n    \"\"\"Show publish tool.\n\n    Args:\n        parent: tool parent\n\n    Returns:\n        QWidget of the tool\n\n    \"\"\"\n    return _SingletonPoint.show_tool_by_name(\"publish\", parent)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/host_tools.html#client.ayon_core.tools.utils.host_tools.show_publisher","title":"<code>show_publisher(parent=None, **kwargs)</code>","text":"<p>Show publisher tool.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <code>QWidget | None</code> <p>tool parent,</p> <code>None</code> <code>**kwargs</code> <p>tool kwargs,</p> <code>{}</code> <p>Returns:</p> Type Description <code>QWidget | None</code> <p>QWidget of the tool</p> Source code in <code>client/ayon_core/tools/utils/host_tools.py</code> <pre><code>def show_publisher(\n    parent: QWidget | None = None,\n    **kwargs\n) -&gt; QWidget | None:\n    \"\"\"Show publisher tool.\n\n    Args:\n        parent: tool parent,\n        **kwargs: tool kwargs,\n\n    Returns:\n        QWidget of the tool\n\n    \"\"\"\n    return _SingletonPoint.show_tool_by_name(\n        \"publisher\", parent, **kwargs\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/host_tools.html#client.ayon_core.tools.utils.host_tools.show_scene_inventory","title":"<code>show_scene_inventory(parent=None)</code>","text":"<p>Show scene inventory tool.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <code>QWidget | None</code> <p>tool parent,</p> <code>None</code> <p>Returns:</p> Type Description <code>QWidget | None</code> <p>QWidget of the tool</p> Source code in <code>client/ayon_core/tools/utils/host_tools.py</code> <pre><code>def show_scene_inventory(\n    parent: QWidget | None = None\n) -&gt; QWidget | None:\n    \"\"\"Show scene inventory tool.\n\n    Args:\n        parent: tool parent,\n\n    Returns:\n        QWidget of the tool\n\n    \"\"\"\n    return _SingletonPoint.show_tool_by_name(\n        \"sceneinventory\", parent\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/host_tools.html#client.ayon_core.tools.utils.host_tools.show_tool_by_name","title":"<code>show_tool_by_name(tool_name, parent=None, *args, **kwargs)</code>","text":"<p>Show tool by its name.</p> <p>Parameters:</p> Name Type Description Default <code>tool_name</code> <code>ToolName</code> <p>tool name,</p> required <code>parent</code> <code>QWidget | None</code> <p>tool parent,</p> <code>None</code> <code>*args</code> <p>tool args,</p> <code>()</code> <code>**kwargs</code> <p>tool kwargs,</p> <code>{}</code> <p>Returns:</p> Type Description <code>QWidget | None</code> <p>QWidget of the tool</p> Source code in <code>client/ayon_core/tools/utils/host_tools.py</code> <pre><code>def show_tool_by_name(\n    tool_name: ToolName,\n    parent: QWidget | None = None,\n    *args,\n    **kwargs\n) -&gt; QWidget | None:\n    \"\"\"Show tool by its name.\n\n    Args:\n        tool_name: tool name,\n        parent: tool parent,\n        *args: tool args,\n        **kwargs: tool kwargs,\n\n    Returns:\n        QWidget of the tool\n\n    \"\"\"\n    return _SingletonPoint.show_tool_by_name(\n        tool_name, parent, *args, **kwargs\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/host_tools.html#client.ayon_core.tools.utils.host_tools.show_workfiles","title":"<code>show_workfiles(*args, **kwargs)</code>","text":"<p>Show workfiles tool.</p> <p>Parameters:</p> Name Type Description Default <code>*args</code> <p>tool args,</p> <code>()</code> <code>**kwargs</code> <p>tool kwargs,</p> <code>{}</code> <p>Returns:</p> Type Description <code>QWidget | None</code> <p>QWidget of the tool</p> Source code in <code>client/ayon_core/tools/utils/host_tools.py</code> <pre><code>def show_workfiles(*args, **kwargs) -&gt; QWidget | None:\n    \"\"\"Show workfiles tool.\n\n    Args:\n        *args: tool args,\n        **kwargs: tool kwargs,\n\n    Returns:\n        QWidget of the tool\n    \"\"\"\n    return _SingletonPoint.show_tool_by_name(\n        \"workfiles\", *args, **kwargs\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/layouts.html","title":"layouts","text":""},{"location":"autoapi/client/ayon_core/tools/utils/layouts.html#client.ayon_core.tools.utils.layouts.FlowLayout","title":"<code>FlowLayout</code>","text":"<p>               Bases: <code>QLayout</code></p> <p>Layout that organize widgets by minimum size into a flow layout.</p> <p>Layout is putting widget from left to right and top to bottom. When widget can't fit a row it is added to next line. Minimum size matches widget with biggest 'sizeHint' width and height using calculated geometry.</p> <p>Content margins are part of calculations. It is possible to define horizontal and vertical spacing.</p> <p>Layout does not support stretch and spacing items.</p> Todos <p>Unified width concept -&gt; use width of largest item so all of them are     same. This could allow to have minimum columns option too.</p> Source code in <code>client/ayon_core/tools/utils/layouts.py</code> <pre><code>class FlowLayout(QtWidgets.QLayout):\n    \"\"\"Layout that organize widgets by minimum size into a flow layout.\n\n    Layout is putting widget from left to right and top to bottom. When widget\n    can't fit a row it is added to next line. Minimum size matches widget with\n    biggest 'sizeHint' width and height using calculated geometry.\n\n    Content margins are part of calculations. It is possible to define\n    horizontal and vertical spacing.\n\n    Layout does not support stretch and spacing items.\n\n    Todos:\n        Unified width concept -&gt; use width of largest item so all of them are\n            same. This could allow to have minimum columns option too.\n    \"\"\"\n\n    def __init__(self, parent=None):\n        super(FlowLayout, self).__init__(parent)\n\n        # spaces between each item\n        self._horizontal_spacing = 5\n        self._vertical_spacing = 5\n\n        self._items = []\n\n    def __del__(self):\n        while self.count():\n            self.takeAt(0, False)\n\n    def isEmpty(self):\n        for item in self._items:\n            if not item.isEmpty():\n                return False\n        return True\n\n    def setSpacing(self, spacing):\n        self._horizontal_spacing = spacing\n        self._vertical_spacing = spacing\n        self.invalidate()\n\n    def setHorizontalSpacing(self, spacing):\n        self._horizontal_spacing = spacing\n        self.invalidate()\n\n    def setVerticalSpacing(self, spacing):\n        self._vertical_spacing = spacing\n        self.invalidate()\n\n    def addItem(self, item):\n        self._items.append(item)\n        self.invalidate()\n\n    def count(self):\n        return len(self._items)\n\n    def itemAt(self, index):\n        if 0 &lt;= index &lt; len(self._items):\n            return self._items[index]\n        return None\n\n    def takeAt(self, index, invalidate=True):\n        if 0 &lt;= index &lt; len(self._items):\n            item = self._items.pop(index)\n            if invalidate:\n                self.invalidate()\n            return item\n        return None\n\n    def expandingDirections(self):\n        return QtCore.Qt.Orientations(QtCore.Qt.Vertical)\n\n    def hasHeightForWidth(self):\n        return True\n\n    def heightForWidth(self, width):\n        return self._setup_geometry(QtCore.QRect(0, 0, width, 0), True)\n\n    def setGeometry(self, rect):\n        super(FlowLayout, self).setGeometry(rect)\n        self._setup_geometry(rect)\n\n    def sizeHint(self):\n        return self.minimumSize()\n\n    def minimumSize(self):\n        size = QtCore.QSize(0, 0)\n        for item in self._items:\n            widget = item.widget()\n            if widget is not None:\n                parent = widget.parent()\n                if not widget.isVisibleTo(parent):\n                    continue\n            size = size.expandedTo(item.minimumSize())\n\n        if size.width() &lt; 1 or size.height() &lt; 1:\n            return size\n        l_margin, t_margin, r_margin, b_margin = self.getContentsMargins()\n        size += QtCore.QSize(l_margin + r_margin, t_margin + b_margin)\n        return size\n\n    def _setup_geometry(self, rect, only_calculate=False):\n        h_spacing = self._horizontal_spacing\n        v_spacing = self._vertical_spacing\n        l_margin, t_margin, r_margin, b_margin = self.getContentsMargins()\n\n        left_x = rect.x() + l_margin\n        top_y = rect.y() + t_margin\n        pos_x = left_x\n        pos_y = top_y\n        row_height = 0\n        for item in self._items:\n            item_hint = item.sizeHint()\n            item_width = item_hint.width()\n            item_height = item_hint.height()\n            if item_width &lt; 1 or item_height &lt; 1:\n                continue\n\n            end_x = pos_x + item_width\n\n            wrap = (\n                row_height &gt; 0\n                and (\n                    end_x &gt; rect.right()\n                    or (end_x + r_margin) &gt; rect.right()\n                )\n            )\n            if not wrap:\n                next_pos_x = end_x + h_spacing\n            else:\n                pos_x = left_x\n                pos_y += row_height + v_spacing\n                next_pos_x = pos_x + item_width + h_spacing\n                row_height = 0\n\n            if not only_calculate:\n                item.setGeometry(\n                    QtCore.QRect(pos_x, pos_y, item_width, item_height)\n                )\n\n            pos_x = next_pos_x\n            row_height = max(row_height, item_height)\n\n        height = (pos_y - top_y) + row_height\n        if height &gt; 0:\n            height += b_margin\n        return height\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/lib.html","title":"lib","text":""},{"location":"autoapi/client/ayon_core/tools/utils/lib.html#client.ayon_core.tools.utils.lib.DynamicQThread","title":"<code>DynamicQThread</code>","text":"<p>               Bases: <code>QThread</code></p> <p>QThread which can run any function with argument and kwargs.</p> <p>Parameters:</p> Name Type Description Default <code>func</code> <code>function</code> <p>Function which will be called.</p> required <code>args</code> <code>tuple</code> <p>Arguments which will be passed to function.</p> <code>None</code> <code>kwargs</code> <code>tuple</code> <p>Keyword arguments which will be passed to function.</p> <code>None</code> <code>parent</code> <code>QObject</code> <p>Parent of thread.</p> <code>None</code> Source code in <code>client/ayon_core/tools/utils/lib.py</code> <pre><code>class DynamicQThread(QtCore.QThread):\n    \"\"\"QThread which can run any function with argument and kwargs.\n\n    Args:\n        func (function): Function which will be called.\n        args (tuple): Arguments which will be passed to function.\n        kwargs (tuple): Keyword arguments which will be passed to function.\n        parent (QObject): Parent of thread.\n    \"\"\"\n    def __init__(self, func, args=None, kwargs=None, parent=None):\n        super(DynamicQThread, self).__init__(parent)\n        if args is None:\n            args = tuple()\n        if kwargs is None:\n            kwargs = {}\n        self._func = func\n        self._args = args\n        self._kwargs = kwargs\n\n    def run(self):\n        \"\"\"Execute the function with arguments.\"\"\"\n        self._func(*self._args, **self._kwargs)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/lib.html#client.ayon_core.tools.utils.lib.DynamicQThread.run","title":"<code>run()</code>","text":"<p>Execute the function with arguments.</p> Source code in <code>client/ayon_core/tools/utils/lib.py</code> <pre><code>def run(self):\n    \"\"\"Execute the function with arguments.\"\"\"\n    self._func(*self._args, **self._kwargs)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/lib.html#client.ayon_core.tools.utils.lib.RefreshThread","title":"<code>RefreshThread</code>","text":"<p>               Bases: <code>QThread</code></p> Source code in <code>client/ayon_core/tools/utils/lib.py</code> <pre><code>class RefreshThread(QtCore.QThread):\n    refresh_finished = QtCore.Signal(str)\n\n    def __init__(self, thread_id, func, *args, **kwargs):\n        super(RefreshThread, self).__init__()\n        self._id = thread_id\n        self._callback = partial(func, *args, **kwargs)\n        self._exception = None\n        self._traceback = None\n        self._result = None\n        self.finished.connect(self._on_finish_callback)\n\n    @property\n    def id(self) -&gt; str:\n        return self._id\n\n    @property\n    def failed(self) -&gt; bool:\n        return self._exception is not None\n\n    def run(self):\n        try:\n            self._result = self._callback()\n        except Exception as exc:\n            exc_type, exc_value, exc_traceback = sys.exc_info()\n            err_traceback = \"\".join(traceback.format_exception(\n                exc_type, exc_value, exc_traceback\n            ))\n            print(err_traceback)\n            self._traceback = err_traceback\n            self._exception = exc\n\n    def get_result(self) -&gt; Any:\n        return self._result\n\n    def get_exception(self) -&gt; Union[BaseException, None]:\n        return self._exception\n\n    def get_traceback(self) -&gt; Union[str, None]:\n        return self._traceback\n\n    def _on_finish_callback(self):\n        \"\"\"Trigger custom signal with thread id.\n\n        Listening for 'finished' signal we make sure that execution of thread\n            finished and QThread object can be safely deleted.\n        \"\"\"\n\n        self.refresh_finished.emit(self.id)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/lib.html#client.ayon_core.tools.utils.lib.WrappedCallbackItem","title":"<code>WrappedCallbackItem</code>","text":"<p>Structure to store information about callback and args/kwargs for it.</p> <p>Item can be used to execute callback in main thread which may be needed for execution of Qt objects.</p> <p>Item store callback (callable variable), arguments and keyword arguments for the callback. Item hold information about it's process.</p> Source code in <code>client/ayon_core/tools/utils/lib.py</code> <pre><code>class WrappedCallbackItem:\n    \"\"\"Structure to store information about callback and args/kwargs for it.\n\n    Item can be used to execute callback in main thread which may be needed\n    for execution of Qt objects.\n\n    Item store callback (callable variable), arguments and keyword arguments\n    for the callback. Item hold information about it's process.\n    \"\"\"\n    not_set = object()\n    _log = None\n\n    def __init__(self, callback, *args, **kwargs):\n        self._done = False\n        self._exception = self.not_set\n        self._result = self.not_set\n        self._callback = callback\n        self._args = args\n        self._kwargs = kwargs\n\n    def __call__(self):\n        self.execute()\n\n    @property\n    def log(self):\n        cls = self.__class__\n        if cls._log is None:\n            cls._log = Logger.get_logger(cls.__name__)\n        return cls._log\n\n    @property\n    def done(self):\n        return self._done\n\n    @property\n    def exception(self):\n        return self._exception\n\n    @property\n    def result(self):\n        return self._result\n\n    def execute(self):\n        \"\"\"Execute callback and store its result.\n\n        Method must be called from main thread. Item is marked as `done`\n        when callback execution finished. Store output of callback of exception\n        information when callback raises one.\n        \"\"\"\n        if self.done:\n            self.log.warning(\"- item is already processed\")\n            return\n\n        try:\n            result = self._callback(*self._args, **self._kwargs)\n            self._result = result\n\n        except Exception as exc:\n            self._exception = exc\n\n        finally:\n            self._done = True\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/lib.html#client.ayon_core.tools.utils.lib.WrappedCallbackItem.execute","title":"<code>execute()</code>","text":"<p>Execute callback and store its result.</p> <p>Method must be called from main thread. Item is marked as <code>done</code> when callback execution finished. Store output of callback of exception information when callback raises one.</p> Source code in <code>client/ayon_core/tools/utils/lib.py</code> <pre><code>def execute(self):\n    \"\"\"Execute callback and store its result.\n\n    Method must be called from main thread. Item is marked as `done`\n    when callback execution finished. Store output of callback of exception\n    information when callback raises one.\n    \"\"\"\n    if self.done:\n        self.log.warning(\"- item is already processed\")\n        return\n\n    try:\n        result = self._callback(*self._args, **self._kwargs)\n        self._result = result\n\n    except Exception as exc:\n        self._exception = exc\n\n    finally:\n        self._done = True\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/lib.html#client.ayon_core.tools.utils.lib.center_window","title":"<code>center_window(window)</code>","text":"<p>Move window to center of it's screen.</p> Source code in <code>client/ayon_core/tools/utils/lib.py</code> <pre><code>def center_window(window):\n    \"\"\"Move window to center of it's screen.\"\"\"\n    screen = window.screen()\n    screen_geo = screen.geometry()\n\n    geo = window.frameGeometry()\n    geo.moveCenter(screen_geo.center())\n    if geo.y() &lt; screen_geo.y():\n        geo.setY(screen_geo.y())\n    window.move(geo.topLeft())\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/lib.html#client.ayon_core.tools.utils.lib.format_version","title":"<code>format_version(value)</code>","text":"<p>Formats integer to displayable version name</p> Source code in <code>client/ayon_core/tools/utils/lib.py</code> <pre><code>def format_version(value):\n    \"\"\"Formats integer to displayable version name\"\"\"\n    value = int(value)  # convert e.g. HeroVersionType to its version value\n    label = \"v{0:03d}\".format(abs(value))\n    if value &lt; 0:\n        return \"[{}]\".format(label)\n    return label\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/lib.html#client.ayon_core.tools.utils.lib.get_ayon_qt_app","title":"<code>get_ayon_qt_app()</code>","text":"<p>Main Qt application initialized for AYON processed.</p> <p>This function should be used only inside AYON-launcher process     and never inside other processes.</p> Source code in <code>client/ayon_core/tools/utils/lib.py</code> <pre><code>def get_ayon_qt_app():\n    \"\"\"Main Qt application initialized for AYON processed.\n\n    This function should be used only inside AYON-launcher process\n        and never inside other processes.\n    \"\"\"\n\n    app = get_qt_app()\n    app.setWindowIcon(QtGui.QIcon(get_app_icon_path()))\n    return app\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/lib.html#client.ayon_core.tools.utils.lib.get_qt_app","title":"<code>get_qt_app()</code>","text":"<p>Get Qt application.</p> <p>The function initializes new Qt application if it is not already initialized. It also sets some attributes to the application to ensure that it will work properly on high DPI displays.</p> <p>Returns:</p> Type Description <p>QtWidgets.QApplication: Current Qt application.</p> Source code in <code>client/ayon_core/tools/utils/lib.py</code> <pre><code>def get_qt_app():\n    \"\"\"Get Qt application.\n\n    The function initializes new Qt application if it is not already\n    initialized. It also sets some attributes to the application to\n    ensure that it will work properly on high DPI displays.\n\n    Returns:\n        QtWidgets.QApplication: Current Qt application.\n    \"\"\"\n\n    app = QtWidgets.QApplication.instance()\n    if app is None:\n        for attr_name in (\n            \"AA_EnableHighDpiScaling\",\n            \"AA_UseHighDpiPixmaps\",\n        ):\n            attr = getattr(QtCore.Qt, attr_name, None)\n            if attr is not None:\n                QtWidgets.QApplication.setAttribute(attr)\n\n        policy = os.getenv(\"QT_SCALE_FACTOR_ROUNDING_POLICY\")\n        if (\n            hasattr(\n                QtWidgets.QApplication, \"setHighDpiScaleFactorRoundingPolicy\"\n            )\n            and not policy\n        ):\n            QtWidgets.QApplication.setHighDpiScaleFactorRoundingPolicy(\n                QtCore.Qt.HighDpiScaleFactorRoundingPolicy.PassThrough\n            )\n\n        app = QtWidgets.QApplication(sys.argv)\n\n    return app\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/lib.html#client.ayon_core.tools.utils.lib.get_qt_icon","title":"<code>get_qt_icon(icon_def, *, default=_PLACEHOLDER)</code>","text":"<p>Returns icon from cache or creates new one.</p> <p>Parameters:</p> Name Type Description Default <code>icon_def</code> <code>IconBase | dict[str, Any</code> <p>Icon definition.</p> required <code>default</code> <code>Any</code> <p>Default value to return if icon is not found.</p> <code>_PLACEHOLDER</code> <p>Returns:</p> Type Description <code>QIcon | Any</code> <p>QtGui.QIcon: Icon.</p> Source code in <code>client/ayon_core/tools/utils/lib.py</code> <pre><code>def get_qt_icon(\n    icon_def: IconBase | dict[str, Any],\n    *,\n    default: Any = _PLACEHOLDER,\n) -&gt; QtGui.QIcon | Any:\n    \"\"\"Returns icon from cache or creates new one.\n\n    Args:\n        icon_def (IconBase | dict[str, Any): Icon definition.\n        default (Any): Default value to return if icon is not found.\n\n    Returns:\n        QtGui.QIcon: Icon.\n\n    \"\"\"\n    return _IconsCache.get_icon(icon_def, default=default)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/lib.html#client.ayon_core.tools.utils.lib.get_qta_icon_by_name_and_color","title":"<code>get_qta_icon_by_name_and_color(icon_name, icon_color)</code>","text":"<p>Returns icon from cache or creates new one.</p> <p>Parameters:</p> Name Type Description Default <code>icon_name</code> <code>str</code> <p>Icon name.</p> required <code>icon_color</code> <code>str</code> <p>Icon color.</p> required <p>Returns:</p> Type Description <p>QtGui.QIcon: Icon.</p> Source code in <code>client/ayon_core/tools/utils/lib.py</code> <pre><code>def get_qta_icon_by_name_and_color(icon_name, icon_color):\n    \"\"\"Returns icon from cache or creates new one.\n\n    Args:\n        icon_name (str): Icon name.\n        icon_color (str): Icon color.\n\n    Returns:\n        QtGui.QIcon: Icon.\n\n    \"\"\"\n    return _IconsCache.get_qta_icon_by_name_and_color(icon_name, icon_color)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/lib.html#client.ayon_core.tools.utils.lib.get_warning_pixmap","title":"<code>get_warning_pixmap(color=None)</code>","text":"<p>Warning icon as QPixmap.</p> <p>Parameters:</p> Name Type Description Default <code>color</code> <code>QColor</code> <p>Color that will be used to paint warning icon.</p> <code>None</code> Source code in <code>client/ayon_core/tools/utils/lib.py</code> <pre><code>def get_warning_pixmap(color=None):\n    \"\"\"Warning icon as QPixmap.\n\n    Args:\n        color(QtGui.QColor): Color that will be used to paint warning icon.\n    \"\"\"\n    src_image_path = get_image_path(\"warning.png\")\n    src_image = QtGui.QImage(src_image_path)\n    if color is None:\n        color = get_objected_colors(\"delete-btn-bg\").get_qcolor()\n\n    return paint_image_with_color(src_image, color)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/lib.html#client.ayon_core.tools.utils.lib.html_escape","title":"<code>html_escape(text)</code>","text":"<p>Basic escape of html syntax symbols in text.</p> Source code in <code>client/ayon_core/tools/utils/lib.py</code> <pre><code>def html_escape(text):\n    \"\"\"Basic escape of html syntax symbols in text.\"\"\"\n\n    return (\n        text\n        .replace(\"&amp;\", \"&amp;amp;\")\n        .replace(\"&lt;\", \"&amp;lt;\")\n        .replace(\"&gt;\", \"&amp;gt;\")\n        .replace('\"', \"&amp;quot;\")\n        .replace(\"'\", \"&amp;#x27;\")\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/lib.html#client.ayon_core.tools.utils.lib.iter_model_rows","title":"<code>iter_model_rows(model, column=0, include_root=False)</code>","text":"<p>Iterate over all row indices in a model</p> Source code in <code>client/ayon_core/tools/utils/lib.py</code> <pre><code>def iter_model_rows(model, column=0, include_root=False):\n    \"\"\"Iterate over all row indices in a model\"\"\"\n    indexes_queue = collections.deque()\n    # start iteration at root\n    indexes_queue.append(QtCore.QModelIndex())\n    while indexes_queue:\n        index = indexes_queue.popleft()\n        # Add children to the iterations\n        for child_row in range(model.rowCount(index)):\n            indexes_queue.append(model.index(child_row, column, index))\n\n        if not include_root and not index.isValid():\n            continue\n\n        yield index\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/lib.html#client.ayon_core.tools.utils.lib.paint_image_with_color","title":"<code>paint_image_with_color(image, color)</code>","text":"<p>Redraw image with single color using it's alpha.</p> <p>It is expected that input image is singlecolor image with alpha.</p> <p>Parameters:</p> Name Type Description Default <code>image</code> <code>QImage</code> <p>Loaded image with alpha.</p> required <code>color</code> <code>QColor</code> <p>Color that will be used to paint image.</p> required Source code in <code>client/ayon_core/tools/utils/lib.py</code> <pre><code>def paint_image_with_color(image, color):\n    \"\"\"Redraw image with single color using it's alpha.\n\n    It is expected that input image is singlecolor image with alpha.\n\n    Args:\n        image (QImage): Loaded image with alpha.\n        color (QColor): Color that will be used to paint image.\n    \"\"\"\n    width = image.width()\n    height = image.height()\n\n    alpha_mask = image.createAlphaMask()\n    alpha_region = QtGui.QRegion(QtGui.QBitmap.fromImage(alpha_mask))\n\n    pixmap = QtGui.QPixmap(width, height)\n    pixmap.fill(QtCore.Qt.transparent)\n\n    painter = QtGui.QPainter(pixmap)\n    render_hints = (\n        QtGui.QPainter.Antialiasing\n        | QtGui.QPainter.SmoothPixmapTransform\n    )\n    painter.setRenderHints(render_hints)\n\n    painter.setClipRegion(alpha_region)\n    painter.setPen(QtCore.Qt.NoPen)\n    painter.setBrush(color)\n    painter.drawRect(QtCore.QRect(0, 0, width, height))\n    painter.end()\n\n    return pixmap\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/lib.html#client.ayon_core.tools.utils.lib.preserve_expanded_rows","title":"<code>preserve_expanded_rows(tree_view, column=0, role=None)</code>","text":"<p>Preserves expanded row in QTreeView by column's data role.</p> <p>This function is created to maintain the expand vs collapse status of the model items. When refresh is triggered the items which are expanded will stay expanded and vice versa.</p> <p>Parameters:</p> Name Type Description Default <code>tree_view</code> <code>QTreeView</code> <p>the tree view which is nested in the application</p> required <code>column</code> <code>int</code> <p>the column to retrieve the data from</p> <code>0</code> <code>role</code> <code>int</code> <p>the role which dictates what will be returned</p> <code>None</code> <p>Returns:</p> Type Description <p>None</p> Source code in <code>client/ayon_core/tools/utils/lib.py</code> <pre><code>@contextlib.contextmanager\ndef preserve_expanded_rows(tree_view, column=0, role=None):\n    \"\"\"Preserves expanded row in QTreeView by column's data role.\n\n    This function is created to maintain the expand vs collapse status of\n    the model items. When refresh is triggered the items which are expanded\n    will stay expanded and vice versa.\n\n    Arguments:\n        tree_view (QWidgets.QTreeView): the tree view which is\n            nested in the application\n        column (int): the column to retrieve the data from\n        role (int): the role which dictates what will be returned\n\n    Returns:\n        None\n\n    \"\"\"\n    if role is None:\n        role = QtCore.Qt.DisplayRole\n    model = tree_view.model()\n\n    expanded = set()\n\n    for index in iter_model_rows(model, column=column, include_root=False):\n        if tree_view.isExpanded(index):\n            value = index.data(role)\n            expanded.add(value)\n\n    try:\n        yield\n    finally:\n        if not expanded:\n            return\n\n        for index in iter_model_rows(model, column=column, include_root=False):\n            value = index.data(role)\n            state = value in expanded\n            if state:\n                tree_view.expand(index)\n            else:\n                tree_view.collapse(index)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/lib.html#client.ayon_core.tools.utils.lib.preserve_selection","title":"<code>preserve_selection(tree_view, column=0, role=None, current_index=True)</code>","text":"<p>Preserves row selection in QTreeView by column's data role.</p> <p>This function is created to maintain the selection status of the model items. When refresh is triggered the items which are expanded will stay expanded and vice versa.</p> <pre><code>tree_view (QWidgets.QTreeView): the tree view nested in the application\ncolumn (int): the column to retrieve the data from\nrole (int): the role which dictates what will be returned\n</code></pre> <p>Returns:</p> Type Description <p>None</p> Source code in <code>client/ayon_core/tools/utils/lib.py</code> <pre><code>@contextlib.contextmanager\ndef preserve_selection(tree_view, column=0, role=None, current_index=True):\n    \"\"\"Preserves row selection in QTreeView by column's data role.\n\n    This function is created to maintain the selection status of\n    the model items. When refresh is triggered the items which are expanded\n    will stay expanded and vice versa.\n\n        tree_view (QWidgets.QTreeView): the tree view nested in the application\n        column (int): the column to retrieve the data from\n        role (int): the role which dictates what will be returned\n\n    Returns:\n        None\n\n    \"\"\"\n    if role is None:\n        role = QtCore.Qt.DisplayRole\n    model = tree_view.model()\n    selection_model = tree_view.selectionModel()\n    flags = (\n        QtCore.QItemSelectionModel.Select\n        | QtCore.QItemSelectionModel.Rows\n    )\n\n    if current_index:\n        current_index_value = tree_view.currentIndex().data(role)\n    else:\n        current_index_value = None\n\n    selected_rows = selection_model.selectedRows()\n    if not selected_rows:\n        yield\n        return\n\n    selected = set(row.data(role) for row in selected_rows)\n    try:\n        yield\n    finally:\n        if not selected:\n            return\n\n        # Go through all indices, select the ones with similar data\n        for index in iter_model_rows(model, column=column, include_root=False):\n            value = index.data(role)\n            state = value in selected\n            if state:\n                tree_view.scrollTo(index)  # Ensure item is visible\n                selection_model.select(index, flags)\n\n            if current_index_value and value == current_index_value:\n                selection_model.setCurrentIndex(\n                    index, selection_model.NoUpdate\n                )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/lib.html#client.ayon_core.tools.utils.lib.set_style_property","title":"<code>set_style_property(widget, property_name, property_value)</code>","text":"<p>Set widget's property that may affect style.</p> <p>If current property value is different then style of widget is polished.</p> Source code in <code>client/ayon_core/tools/utils/lib.py</code> <pre><code>def set_style_property(widget, property_name, property_value):\n    \"\"\"Set widget's property that may affect style.\n\n    If current property value is different then style of widget is polished.\n    \"\"\"\n    cur_value = widget.property(property_name)\n    if cur_value == property_value:\n        return\n    widget.setProperty(property_name, property_value)\n    style = widget.style()\n    style.polish(widget)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/models.html","title":"models","text":""},{"location":"autoapi/client/ayon_core/tools/utils/models.html#client.ayon_core.tools.utils.models.Item","title":"<code>Item</code>","text":"<p>               Bases: <code>dict</code></p> <p>An item that can be represented in a tree view using <code>TreeModel</code>.</p> <p>The item can store data just like a regular dictionary.</p> <p>data = {\"name\": \"John\", \"score\": 10} item = Item(data) assert item[\"name\"] == \"John\"</p> Source code in <code>client/ayon_core/tools/utils/models.py</code> <pre><code>class Item(dict):\n    \"\"\"An item that can be represented in a tree view using `TreeModel`.\n\n    The item can store data just like a regular dictionary.\n\n    &gt;&gt;&gt; data = {\"name\": \"John\", \"score\": 10}\n    &gt;&gt;&gt; item = Item(data)\n    &gt;&gt;&gt; assert item[\"name\"] == \"John\"\n\n    \"\"\"\n\n    def __init__(self, data=None):\n        super(Item, self).__init__()\n\n        self._children = list()\n        self._parent = None\n\n        if data is not None:\n            assert isinstance(data, dict)\n            self.update(data)\n\n    def childCount(self):\n        return len(self._children)\n\n    def child(self, row):\n\n        if row &gt;= len(self._children):\n            log.warning(\"Invalid row as child: {0}\".format(row))\n            return\n\n        return self._children[row]\n\n    def children(self):\n        return self._children\n\n    def parent(self):\n        return self._parent\n\n    def row(self):\n        \"\"\"\n        Returns:\n             int: Index of this item under parent\"\"\"\n        if self._parent is not None:\n            siblings = self.parent().children()\n            return siblings.index(self)\n        return -1\n\n    def add_child(self, child):\n        \"\"\"Add a child to this item\"\"\"\n        child._parent = self\n        self._children.append(child)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/models.html#client.ayon_core.tools.utils.models.Item.add_child","title":"<code>add_child(child)</code>","text":"<p>Add a child to this item</p> Source code in <code>client/ayon_core/tools/utils/models.py</code> <pre><code>def add_child(self, child):\n    \"\"\"Add a child to this item\"\"\"\n    child._parent = self\n    self._children.append(child)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/models.html#client.ayon_core.tools.utils.models.Item.row","title":"<code>row()</code>","text":"<p>Returns:</p> Name Type Description <code>int</code> <p>Index of this item under parent</p> Source code in <code>client/ayon_core/tools/utils/models.py</code> <pre><code>def row(self):\n    \"\"\"\n    Returns:\n         int: Index of this item under parent\"\"\"\n    if self._parent is not None:\n        siblings = self.parent().children()\n        return siblings.index(self)\n    return -1\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/models.html#client.ayon_core.tools.utils.models.RecursiveSortFilterProxyModel","title":"<code>RecursiveSortFilterProxyModel</code>","text":"<p>               Bases: <code>QSortFilterProxyModel</code></p> <p>Recursive proxy model. Item is not filtered if any children match the filter. Use case: Filtering by string - parent won't be filtered if does not match     the filter string but first checks if any children does.</p> Source code in <code>client/ayon_core/tools/utils/models.py</code> <pre><code>class RecursiveSortFilterProxyModel(QtCore.QSortFilterProxyModel):\n    \"\"\"Recursive proxy model.\n    Item is not filtered if any children match the filter.\n    Use case: Filtering by string - parent won't be filtered if does not match\n        the filter string but first checks if any children does.\n    \"\"\"\n\n    def __init__(self, *args, **kwargs):\n        super(RecursiveSortFilterProxyModel, self).__init__(*args, **kwargs)\n        recursive_enabled = False\n        if hasattr(self, \"setRecursiveFilteringEnabled\"):\n            self.setRecursiveFilteringEnabled(True)\n            recursive_enabled = True\n        self._recursive_enabled = recursive_enabled\n\n    def filterAcceptsRow(self, row, parent_index):\n        if hasattr(self, \"filterRegExp\"):\n            regex = self.filterRegExp()\n        else:\n            regex = self.filterRegularExpression()\n\n        pattern = regex.pattern()\n        if pattern:\n            model = self.sourceModel()\n            source_index = model.index(\n                row, self.filterKeyColumn(), parent_index\n            )\n            if source_index.isValid():\n                pattern = regex.pattern()\n\n                # Check current index itself\n                value = model.data(source_index, self.filterRole())\n                matched = bool(re.search(pattern, value, re.IGNORECASE))\n                if matched or self._recursive_enabled:\n                    return matched\n\n                rows = model.rowCount(source_index)\n                for idx in range(rows):\n                    if self.filterAcceptsRow(idx, source_index):\n                        return True\n\n                # Otherwise filter it\n                return False\n\n        return super(RecursiveSortFilterProxyModel, self).filterAcceptsRow(\n            row, parent_index\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/models.html#client.ayon_core.tools.utils.models.TreeModel","title":"<code>TreeModel</code>","text":"<p>               Bases: <code>QAbstractItemModel</code></p> Source code in <code>client/ayon_core/tools/utils/models.py</code> <pre><code>class TreeModel(QtCore.QAbstractItemModel):\n\n    Columns = list()\n    ItemRole = QtCore.Qt.UserRole + 1\n    item_class = None\n\n    def __init__(self, parent=None):\n        super(TreeModel, self).__init__(parent)\n        self._root_item = self.ItemClass()\n\n    @property\n    def ItemClass(self):\n        if self.item_class is not None:\n            return self.item_class\n        return Item\n\n    def rowCount(self, parent=None):\n        if parent is None or not parent.isValid():\n            parent_item = self._root_item\n        else:\n            parent_item = parent.internalPointer()\n        return parent_item.childCount()\n\n    def columnCount(self, parent):\n        return len(self.Columns)\n\n    def data(self, index, role):\n        if not index.isValid():\n            return None\n\n        if role == QtCore.Qt.DisplayRole or role == QtCore.Qt.EditRole:\n            item = index.internalPointer()\n            column = index.column()\n\n            key = self.Columns[column]\n            return item.get(key, None)\n\n        if role == self.ItemRole:\n            return index.internalPointer()\n\n    def setData(self, index, value, role=QtCore.Qt.EditRole):\n        \"\"\"Change the data on the items.\n\n        Returns:\n            bool: Whether the edit was successful\n        \"\"\"\n\n        if index.isValid():\n            if role == QtCore.Qt.EditRole:\n\n                item = index.internalPointer()\n                column = index.column()\n                key = self.Columns[column]\n                item[key] = value\n\n                self.dataChanged.emit(index, index, [role])\n\n                # must return true if successful\n                return True\n\n        return False\n\n    def setColumns(self, keys):\n        assert isinstance(keys, (list, tuple))\n        self.Columns = keys\n\n    def headerData(self, section, orientation, role):\n\n        if role == QtCore.Qt.DisplayRole:\n            if section &lt; len(self.Columns):\n                return self.Columns[section]\n\n        super(TreeModel, self).headerData(section, orientation, role)\n\n    def flags(self, index):\n        flags = QtCore.Qt.ItemIsEnabled\n\n        item = index.internalPointer()\n        if item.get(\"enabled\", True):\n            flags |= QtCore.Qt.ItemIsSelectable\n\n        return flags\n\n    def parent(self, index):\n\n        item = index.internalPointer()\n        parent_item = item.parent()\n\n        # If it has no parents we return invalid\n        if parent_item == self._root_item or not parent_item:\n            return QtCore.QModelIndex()\n\n        return self.createIndex(parent_item.row(), 0, parent_item)\n\n    def index(self, row, column, parent=None):\n        \"\"\"Return index for row/column under parent\"\"\"\n\n        if parent is None or not parent.isValid():\n            parent_item = self._root_item\n        else:\n            parent_item = parent.internalPointer()\n\n        child_item = parent_item.child(row)\n        if child_item:\n            return self.createIndex(row, column, child_item)\n        else:\n            return QtCore.QModelIndex()\n\n    def add_child(self, item, parent=None):\n        if parent is None:\n            parent = self._root_item\n\n        parent.add_child(item)\n\n    def column_name(self, column):\n        \"\"\"Return column key by index\"\"\"\n\n        if column &lt; len(self.Columns):\n            return self.Columns[column]\n\n    def clear(self):\n        self.beginResetModel()\n        self._root_item = self.ItemClass()\n        self.endResetModel()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/models.html#client.ayon_core.tools.utils.models.TreeModel.column_name","title":"<code>column_name(column)</code>","text":"<p>Return column key by index</p> Source code in <code>client/ayon_core/tools/utils/models.py</code> <pre><code>def column_name(self, column):\n    \"\"\"Return column key by index\"\"\"\n\n    if column &lt; len(self.Columns):\n        return self.Columns[column]\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/models.html#client.ayon_core.tools.utils.models.TreeModel.index","title":"<code>index(row, column, parent=None)</code>","text":"<p>Return index for row/column under parent</p> Source code in <code>client/ayon_core/tools/utils/models.py</code> <pre><code>def index(self, row, column, parent=None):\n    \"\"\"Return index for row/column under parent\"\"\"\n\n    if parent is None or not parent.isValid():\n        parent_item = self._root_item\n    else:\n        parent_item = parent.internalPointer()\n\n    child_item = parent_item.child(row)\n    if child_item:\n        return self.createIndex(row, column, child_item)\n    else:\n        return QtCore.QModelIndex()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/models.html#client.ayon_core.tools.utils.models.TreeModel.setData","title":"<code>setData(index, value, role=QtCore.Qt.EditRole)</code>","text":"<p>Change the data on the items.</p> <p>Returns:</p> Name Type Description <code>bool</code> <p>Whether the edit was successful</p> Source code in <code>client/ayon_core/tools/utils/models.py</code> <pre><code>def setData(self, index, value, role=QtCore.Qt.EditRole):\n    \"\"\"Change the data on the items.\n\n    Returns:\n        bool: Whether the edit was successful\n    \"\"\"\n\n    if index.isValid():\n        if role == QtCore.Qt.EditRole:\n\n            item = index.internalPointer()\n            column = index.column()\n            key = self.Columns[column]\n            item[key] = value\n\n            self.dataChanged.emit(index, index, [role])\n\n            # must return true if successful\n            return True\n\n    return False\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/multiselection_combobox.html","title":"multiselection_combobox","text":""},{"location":"autoapi/client/ayon_core/tools/utils/multiselection_combobox.html#client.ayon_core.tools.utils.multiselection_combobox.ComboItemDelegate","title":"<code>ComboItemDelegate</code>","text":"<p>               Bases: <code>QStyledItemDelegate</code></p> <p>Helper styled delegate (mostly based on existing private Qt's delegate used by the QtWidgets.QComboBox). Used to style the popup like a list view (e.g windows style).</p> Source code in <code>client/ayon_core/tools/utils/multiselection_combobox.py</code> <pre><code>class ComboItemDelegate(QtWidgets.QStyledItemDelegate):\n    \"\"\"\n    Helper styled delegate (mostly based on existing private Qt's\n    delegate used by the QtWidgets.QComboBox). Used to style the popup like a\n    list view (e.g windows style).\n    \"\"\"\n\n    def paint(self, painter, option, index):\n        option = QtWidgets.QStyleOptionViewItem(option)\n        option.showDecorationSelected = True\n\n        # option.state &amp;= (\n        #     ~QtWidgets.QStyle.State_HasFocus\n        #     &amp; ~QtWidgets.QStyle.State_MouseOver\n        # )\n        super(ComboItemDelegate, self).paint(painter, option, index)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/multiselection_combobox.html#client.ayon_core.tools.utils.multiselection_combobox.MultiSelectionComboBox","title":"<code>MultiSelectionComboBox</code>","text":"<p>               Bases: <code>QComboBox</code></p> Source code in <code>client/ayon_core/tools/utils/multiselection_combobox.py</code> <pre><code>class MultiSelectionComboBox(QtWidgets.QComboBox):\n    value_changed = QtCore.Signal()\n    focused_in = QtCore.Signal()\n\n    ignored_keys = {\n        QtCore.Qt.Key_Up,\n        QtCore.Qt.Key_Down,\n        QtCore.Qt.Key_PageDown,\n        QtCore.Qt.Key_PageUp,\n        QtCore.Qt.Key_Home,\n        QtCore.Qt.Key_End,\n    }\n\n    top_bottom_padding = 2\n    left_right_padding = 3\n    left_offset = 4\n    top_bottom_margins = 1\n    item_spacing = 5\n\n    item_bg_color = QtGui.QColor(\"#31424e\")\n    _placeholder_color = None\n\n    def __init__(\n        self, parent=None, placeholder=\"\", separator=\", \", **kwargs\n    ):\n        super().__init__(parent=parent, **kwargs)\n        self.setObjectName(\"MultiSelectionComboBox\")\n        self.setFocusPolicy(QtCore.Qt.StrongFocus)\n\n        self._popup_is_shown = False\n        self._block_mouse_release_timer = QtCore.QTimer(self, singleShot=True)\n        self._initial_mouse_pos = None\n        self._separator = separator\n        self._placeholder_text = placeholder or \"\"\n        delegate = ComboItemDelegate(self)\n        self.setItemDelegate(delegate)\n\n        self._lines = {}\n        self._item_height = None\n        self._custom_text = None\n        self._delegate = delegate\n\n    def get_placeholder_text(self):\n        return self._placeholder_text\n\n    def set_placeholder_text(self, text):\n        self._placeholder_text = text or \"\"\n        self._update_size_hint()\n\n    def set_custom_text(self, text):\n        self._custom_text = text\n        self._update_size_hint()\n\n    def focusInEvent(self, event):\n        self.focused_in.emit()\n        return super(MultiSelectionComboBox, self).focusInEvent(event)\n\n    def mousePressEvent(self, event):\n        \"\"\"Reimplemented.\"\"\"\n        self._popup_is_shown = False\n        super(MultiSelectionComboBox, self).mousePressEvent(event)\n        if self._popup_is_shown:\n            self._initial_mouse_pos = self.mapToGlobal(event.pos())\n            self._block_mouse_release_timer.start(\n                QtWidgets.QApplication.doubleClickInterval()\n            )\n\n    def showPopup(self):\n        \"\"\"Reimplemented.\"\"\"\n        super(MultiSelectionComboBox, self).showPopup()\n        view = self.view()\n        view.installEventFilter(self)\n        view.viewport().installEventFilter(self)\n        self._popup_is_shown = True\n\n    def hidePopup(self):\n        \"\"\"Reimplemented.\"\"\"\n        self.view().removeEventFilter(self)\n        self.view().viewport().removeEventFilter(self)\n        self._popup_is_shown = False\n        self._initial_mouse_pos = None\n        super(MultiSelectionComboBox, self).hidePopup()\n        self.view().clearFocus()\n\n    def _event_popup_shown(self, obj, event):\n        if not self._popup_is_shown:\n            return\n\n        current_index = self.view().currentIndex()\n        model = self.model()\n\n        if event.type() == QtCore.QEvent.MouseMove:\n            if (\n                self.view().isVisible()\n                and self._initial_mouse_pos is not None\n                and self._block_mouse_release_timer.isActive()\n            ):\n                diff = obj.mapToGlobal(event.pos()) - self._initial_mouse_pos\n                if diff.manhattanLength() &gt; 9:\n                    self._block_mouse_release_timer.stop()\n            return\n\n        index_flags = current_index.flags()\n        state = checkstate_int_to_enum(\n            current_index.data(QtCore.Qt.CheckStateRole)\n        )\n        new_state = None\n\n        if event.type() == QtCore.QEvent.MouseButtonRelease:\n            if (\n                self._block_mouse_release_timer.isActive()\n                or not current_index.isValid()\n                or not self.view().isVisible()\n                or not self.view().rect().contains(event.pos())\n                or not index_flags &amp; QtCore.Qt.ItemIsSelectable\n                or not index_flags &amp; QtCore.Qt.ItemIsEnabled\n                or not index_flags &amp; QtCore.Qt.ItemIsUserCheckable\n            ):\n                return\n\n            if state == QtCore.Qt.Unchecked:\n                new_state = CHECKED_INT\n            else:\n                new_state = UNCHECKED_INT\n\n        elif event.type() == QtCore.QEvent.KeyPress:\n            # TODO: handle QtCore.Qt.Key_Enter, Key_Return?\n            if event.key() == QtCore.Qt.Key_Space:\n                if (\n                    index_flags &amp; QtCore.Qt.ItemIsUserCheckable\n                    and index_flags &amp; ITEM_IS_USER_TRISTATE\n                ):\n                    new_state = (checkstate_enum_to_int(state) + 1) % 3\n\n                elif index_flags &amp; QtCore.Qt.ItemIsUserCheckable:\n                    # toggle the current items check state\n                    if state != QtCore.Qt.Checked:\n                        new_state = CHECKED_INT\n                    else:\n                        new_state = UNCHECKED_INT\n\n        if new_state is not None:\n            model.setData(current_index, new_state, QtCore.Qt.CheckStateRole)\n            self.view().update(current_index)\n            self._update_size_hint()\n            self.value_changed.emit()\n            return True\n\n    def eventFilter(self, obj, event):\n        \"\"\"Reimplemented.\"\"\"\n        result = self._event_popup_shown(obj, event)\n        if result is not None:\n            return result\n\n        return super(MultiSelectionComboBox, self).eventFilter(obj, event)\n\n    def addItem(self, *args, **kwargs):\n        idx = self.count()\n        super(MultiSelectionComboBox, self).addItem(*args, **kwargs)\n        self.model().item(idx).setCheckable(True)\n\n    def paintEvent(self, event):\n        \"\"\"Reimplemented.\"\"\"\n        painter = QtWidgets.QStylePainter(self)\n        option = QtWidgets.QStyleOptionComboBox()\n        self.initStyleOption(option)\n        painter.drawComplexControl(QtWidgets.QStyle.CC_ComboBox, option)\n\n        items = self.checked_items_text()\n        # draw the icon and text\n        draw_text = True\n        combotext = None\n        if self._custom_text is not None:\n            combotext = self._custom_text\n        elif not items:\n            combotext = self._placeholder_text\n        else:\n            draw_text = False\n\n        if draw_text:\n            color = self._get_placeholder_color()\n            pen = painter.pen()\n            pen.setColor(color)\n            painter.setPen(pen)\n\n            left_x = option.rect.left() + self.left_offset\n\n            font = self.font()\n            # This is hardcoded point size from styles\n            font.setPointSize(10)\n            painter.setFont(font)\n\n            label_rect = QtCore.QRect(option.rect)\n            label_rect.moveLeft(left_x)\n\n            painter.drawText(\n                label_rect,\n                QtCore.Qt.AlignLeft | QtCore.Qt.AlignVCenter,\n                combotext\n            )\n            return\n\n        if self._item_height is None:\n            self.updateGeometry()\n            self.update()\n            return\n\n        font_metrics = self.fontMetrics()\n        for line, items in self._lines.items():\n            top_y = (\n                option.rect.top()\n                + (line * self._item_height)\n                + self.top_bottom_margins\n            )\n            left_x = option.rect.left() + self.left_offset\n            for item in items:\n                label_rect = font_metrics.boundingRect(item)\n                label_height = label_rect.height()\n\n                label_rect.moveTop(top_y)\n                label_rect.moveLeft(left_x)\n                label_rect.setHeight(self._item_height)\n                label_rect.setWidth(\n                    label_rect.width() + self.left_right_padding\n                )\n\n                if not draw_text:\n                    bg_rect = QtCore.QRectF(label_rect)\n                    bg_rect.setWidth(\n                        label_rect.width() + self.left_right_padding\n                    )\n                    left_x = bg_rect.right() + self.item_spacing\n\n                    bg_rect.setHeight(\n                        label_height + (2 * self.top_bottom_padding)\n                    )\n                    bg_rect.moveTop(bg_rect.top() + self.top_bottom_margins)\n\n                    path = QtGui.QPainterPath()\n                    path.addRoundedRect(bg_rect, 5, 5)\n\n                    painter.fillPath(path, self.item_bg_color)\n\n                label_rect.moveLeft(label_rect.x() + self.left_right_padding)\n\n                painter.drawText(\n                    label_rect,\n                    QtCore.Qt.AlignLeft | QtCore.Qt.AlignVCenter,\n                    item\n                )\n\n    def resizeEvent(self, *args, **kwargs):\n        super(MultiSelectionComboBox, self).resizeEvent(*args, **kwargs)\n        self._update_size_hint()\n\n    def _update_size_hint(self):\n        if self._custom_text is not None:\n            self.update()\n            return\n        self._lines = {}\n\n        items = self.checked_items_text()\n        if not items:\n            self.update()\n            self.repaint()\n            return\n\n        option = QtWidgets.QStyleOptionComboBox()\n        self.initStyleOption(option)\n        btn_rect = self.style().subControlRect(\n            QtWidgets.QStyle.CC_ComboBox,\n            option,\n            QtWidgets.QStyle.SC_ComboBoxArrow\n        )\n        total_width = option.rect.width() - btn_rect.width()\n\n        line = 0\n        self._lines = {line: []}\n\n        font_metrics = self.fontMetrics()\n        default_left_x = 0 + self.left_offset\n        left_x = int(default_left_x)\n        for item in items:\n            rect = font_metrics.boundingRect(item)\n            width = rect.width() + (2 * self.left_right_padding)\n            right_x = left_x + width\n            if right_x &gt; total_width:\n                left_x = int(default_left_x)\n                if self._lines.get(line):\n                    line += 1\n                    self._lines[line] = [item]\n                    left_x += width\n                else:\n                    self._lines[line] = [item]\n                    line += 1\n            else:\n                if line in self._lines:\n                    self._lines[line].append(item)\n                else:\n                    self._lines[line] = [item]\n                left_x = left_x + width + self.item_spacing\n\n        self.update()\n        self.updateGeometry()\n\n    def sizeHint(self):\n        value = super(MultiSelectionComboBox, self).sizeHint()\n        lines = 1\n        if self._custom_text is None:\n            lines = len(self._lines)\n            if lines == 0:\n                lines = 1\n\n        if self._item_height is None:\n            self._item_height = (\n                self.fontMetrics().height()\n                + (2 * self.top_bottom_padding)\n                + (2 * self.top_bottom_margins)\n            )\n        value.setHeight(\n            (lines * self._item_height)\n            + (2 * self.top_bottom_margins)\n        )\n        return value\n\n    def setItemCheckState(self, index, state):\n        self.setItemData(index, state, QtCore.Qt.CheckStateRole)\n\n    def set_value(self, values):\n        for idx in range(self.count()):\n            value = self.itemData(idx, role=QtCore.Qt.UserRole)\n            if value in values:\n                check_state = CHECKED_INT\n            else:\n                check_state = UNCHECKED_INT\n            self.setItemData(idx, check_state, QtCore.Qt.CheckStateRole)\n        self._update_size_hint()\n\n    def value(self):\n        items = list()\n        for idx in range(self.count()):\n            state = checkstate_int_to_enum(\n                self.itemData(idx, role=QtCore.Qt.CheckStateRole)\n            )\n            if state == QtCore.Qt.Checked:\n                items.append(\n                    self.itemData(idx, role=QtCore.Qt.UserRole)\n                )\n        return items\n\n    def checked_items_text(self):\n        items = list()\n        for idx in range(self.count()):\n            state = checkstate_int_to_enum(\n                self.itemData(idx, role=QtCore.Qt.CheckStateRole)\n            )\n            if state == QtCore.Qt.Checked:\n                items.append(self.itemText(idx))\n        return items\n\n    def wheelEvent(self, event):\n        event.ignore()\n\n    def keyPressEvent(self, event):\n        if (\n            event.key() == QtCore.Qt.Key_Down\n            and event.modifiers() &amp; QtCore.Qt.AltModifier\n        ):\n            return self.showPopup()\n\n        if event.key() in self.ignored_keys:\n            return event.ignore()\n\n        return super(MultiSelectionComboBox, self).keyPressEvent(event)\n\n    @classmethod\n    def _get_placeholder_color(cls):\n        if cls._placeholder_color is None:\n            color_obj = get_objected_colors(\"font\")\n            color = color_obj.get_qcolor()\n            color.setAlpha(67)\n            cls._placeholder_color = color\n        return cls._placeholder_color\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/multiselection_combobox.html#client.ayon_core.tools.utils.multiselection_combobox.MultiSelectionComboBox.eventFilter","title":"<code>eventFilter(obj, event)</code>","text":"<p>Reimplemented.</p> Source code in <code>client/ayon_core/tools/utils/multiselection_combobox.py</code> <pre><code>def eventFilter(self, obj, event):\n    \"\"\"Reimplemented.\"\"\"\n    result = self._event_popup_shown(obj, event)\n    if result is not None:\n        return result\n\n    return super(MultiSelectionComboBox, self).eventFilter(obj, event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/multiselection_combobox.html#client.ayon_core.tools.utils.multiselection_combobox.MultiSelectionComboBox.hidePopup","title":"<code>hidePopup()</code>","text":"<p>Reimplemented.</p> Source code in <code>client/ayon_core/tools/utils/multiselection_combobox.py</code> <pre><code>def hidePopup(self):\n    \"\"\"Reimplemented.\"\"\"\n    self.view().removeEventFilter(self)\n    self.view().viewport().removeEventFilter(self)\n    self._popup_is_shown = False\n    self._initial_mouse_pos = None\n    super(MultiSelectionComboBox, self).hidePopup()\n    self.view().clearFocus()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/multiselection_combobox.html#client.ayon_core.tools.utils.multiselection_combobox.MultiSelectionComboBox.mousePressEvent","title":"<code>mousePressEvent(event)</code>","text":"<p>Reimplemented.</p> Source code in <code>client/ayon_core/tools/utils/multiselection_combobox.py</code> <pre><code>def mousePressEvent(self, event):\n    \"\"\"Reimplemented.\"\"\"\n    self._popup_is_shown = False\n    super(MultiSelectionComboBox, self).mousePressEvent(event)\n    if self._popup_is_shown:\n        self._initial_mouse_pos = self.mapToGlobal(event.pos())\n        self._block_mouse_release_timer.start(\n            QtWidgets.QApplication.doubleClickInterval()\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/multiselection_combobox.html#client.ayon_core.tools.utils.multiselection_combobox.MultiSelectionComboBox.paintEvent","title":"<code>paintEvent(event)</code>","text":"<p>Reimplemented.</p> Source code in <code>client/ayon_core/tools/utils/multiselection_combobox.py</code> <pre><code>def paintEvent(self, event):\n    \"\"\"Reimplemented.\"\"\"\n    painter = QtWidgets.QStylePainter(self)\n    option = QtWidgets.QStyleOptionComboBox()\n    self.initStyleOption(option)\n    painter.drawComplexControl(QtWidgets.QStyle.CC_ComboBox, option)\n\n    items = self.checked_items_text()\n    # draw the icon and text\n    draw_text = True\n    combotext = None\n    if self._custom_text is not None:\n        combotext = self._custom_text\n    elif not items:\n        combotext = self._placeholder_text\n    else:\n        draw_text = False\n\n    if draw_text:\n        color = self._get_placeholder_color()\n        pen = painter.pen()\n        pen.setColor(color)\n        painter.setPen(pen)\n\n        left_x = option.rect.left() + self.left_offset\n\n        font = self.font()\n        # This is hardcoded point size from styles\n        font.setPointSize(10)\n        painter.setFont(font)\n\n        label_rect = QtCore.QRect(option.rect)\n        label_rect.moveLeft(left_x)\n\n        painter.drawText(\n            label_rect,\n            QtCore.Qt.AlignLeft | QtCore.Qt.AlignVCenter,\n            combotext\n        )\n        return\n\n    if self._item_height is None:\n        self.updateGeometry()\n        self.update()\n        return\n\n    font_metrics = self.fontMetrics()\n    for line, items in self._lines.items():\n        top_y = (\n            option.rect.top()\n            + (line * self._item_height)\n            + self.top_bottom_margins\n        )\n        left_x = option.rect.left() + self.left_offset\n        for item in items:\n            label_rect = font_metrics.boundingRect(item)\n            label_height = label_rect.height()\n\n            label_rect.moveTop(top_y)\n            label_rect.moveLeft(left_x)\n            label_rect.setHeight(self._item_height)\n            label_rect.setWidth(\n                label_rect.width() + self.left_right_padding\n            )\n\n            if not draw_text:\n                bg_rect = QtCore.QRectF(label_rect)\n                bg_rect.setWidth(\n                    label_rect.width() + self.left_right_padding\n                )\n                left_x = bg_rect.right() + self.item_spacing\n\n                bg_rect.setHeight(\n                    label_height + (2 * self.top_bottom_padding)\n                )\n                bg_rect.moveTop(bg_rect.top() + self.top_bottom_margins)\n\n                path = QtGui.QPainterPath()\n                path.addRoundedRect(bg_rect, 5, 5)\n\n                painter.fillPath(path, self.item_bg_color)\n\n            label_rect.moveLeft(label_rect.x() + self.left_right_padding)\n\n            painter.drawText(\n                label_rect,\n                QtCore.Qt.AlignLeft | QtCore.Qt.AlignVCenter,\n                item\n            )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/multiselection_combobox.html#client.ayon_core.tools.utils.multiselection_combobox.MultiSelectionComboBox.showPopup","title":"<code>showPopup()</code>","text":"<p>Reimplemented.</p> Source code in <code>client/ayon_core/tools/utils/multiselection_combobox.py</code> <pre><code>def showPopup(self):\n    \"\"\"Reimplemented.\"\"\"\n    super(MultiSelectionComboBox, self).showPopup()\n    view = self.view()\n    view.installEventFilter(self)\n    view.viewport().installEventFilter(self)\n    self._popup_is_shown = True\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/nice_checkbox.html","title":"nice_checkbox","text":""},{"location":"autoapi/client/ayon_core/tools/utils/overlay_messages.html","title":"overlay_messages","text":""},{"location":"autoapi/client/ayon_core/tools/utils/overlay_messages.html#client.ayon_core.tools.utils.overlay_messages.CloseButton","title":"<code>CloseButton</code>","text":"<p>               Bases: <code>QFrame</code></p> <p>Close button drawed manually.</p> Source code in <code>client/ayon_core/tools/utils/overlay_messages.py</code> <pre><code>class CloseButton(QtWidgets.QFrame):\n    \"\"\"Close button drawed manually.\"\"\"\n\n    clicked = QtCore.Signal()\n\n    def __init__(self, parent):\n        super(CloseButton, self).__init__(parent)\n        close_btn_color = get_objected_colors(\"overlay-messages\", \"close-btn\")\n        self._color = close_btn_color.get_qcolor()\n        self._mouse_pressed = False\n        policy = QtWidgets.QSizePolicy(\n            QtWidgets.QSizePolicy.Fixed,\n            QtWidgets.QSizePolicy.Fixed\n        )\n        self.setSizePolicy(policy)\n\n    def sizeHint(self):\n        size = self.fontMetrics().height()\n        return QtCore.QSize(size, size)\n\n    def mousePressEvent(self, event):\n        if event.button() == QtCore.Qt.LeftButton:\n            self._mouse_pressed = True\n        super(CloseButton, self).mousePressEvent(event)\n\n    def mouseReleaseEvent(self, event):\n        if self._mouse_pressed:\n            self._mouse_pressed = False\n            if self.rect().contains(event.pos()):\n                self.clicked.emit()\n\n        super(CloseButton, self).mouseReleaseEvent(event)\n\n    def paintEvent(self, event):\n        rect = self.rect()\n        painter = QtGui.QPainter(self)\n        painter.setClipRect(event.rect())\n        pen = QtGui.QPen()\n        pen.setWidth(2)\n        pen.setColor(self._color)\n        pen.setStyle(QtCore.Qt.SolidLine)\n        pen.setCapStyle(QtCore.Qt.RoundCap)\n        painter.setPen(pen)\n        offset = int(rect.height() / 4)\n        top = rect.top() + offset\n        left = rect.left() + offset\n        right = rect.right() - offset\n        bottom = rect.bottom() - offset\n        painter.drawLine(\n            left, top,\n            right, bottom\n        )\n        painter.drawLine(\n            left, bottom,\n            right, top\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/overlay_messages.html#client.ayon_core.tools.utils.overlay_messages.MessageOverlayObject","title":"<code>MessageOverlayObject</code>","text":"<p>               Bases: <code>QObject</code></p> <p>Object that can be used to add overlay messages.</p> <p>Parameters:</p> Name Type Description Default <code>widget</code> <code>QWidget</code> required Source code in <code>client/ayon_core/tools/utils/overlay_messages.py</code> <pre><code>class MessageOverlayObject(QtCore.QObject):\n    \"\"\"Object that can be used to add overlay messages.\n\n    Args:\n        widget (QWidget):\n    \"\"\"\n\n    def __init__(self, widget, default_timeout=None):\n        super(MessageOverlayObject, self).__init__()\n\n        widget.installEventFilter(self)\n\n        # Timer which triggers recalculation of message positions\n        recalculate_timer = QtCore.QTimer()\n        recalculate_timer.setInterval(10)\n\n        recalculate_timer.timeout.connect(self._recalculate_positions)\n\n        self._widget = widget\n        self._recalculate_timer = recalculate_timer\n\n        self._messages_order = []\n        self._closing_messages = set()\n        self._messages = {}\n        self._spacing = 5\n        self._move_size = 4\n        self._move_size_remove = 8\n        self._default_timeout = default_timeout\n\n    def add_message(\n        self, message, message_type=None, timeout=None, message_id=None\n    ):\n        \"\"\"Add single message into overlay.\n\n        Args:\n            message (str): Message that will be shown.\n            timeout (int): Message timeout.\n            message_type (str): Message type can be used as property in\n                stylesheets.\n            message_id (str): UUID of already existing message to update\n                it's message and timeout. Is created with different id if is\n                not available anymore.\n\n        Returns:\n            str: UUID of message which can be used to update message.\n        \"\"\"\n        # Skip empty messages\n        if not message:\n            return\n\n        if timeout is None:\n            timeout = self._default_timeout\n\n        # Create unique id of message\n        widget = None\n        if message_id is not None:\n            widget = self._messages.get(message_id)\n\n        if message_id is None:\n            message_id = str(uuid.uuid4())\n\n        elif message_id in self._messages_order:\n            self._messages_order.remove(message_id)\n\n        if widget is not None:\n            # NOTE: Update of message won't change paint order which should be\n            #   ok in most of cases as it matters only when messages are\n            #   animated\n            widget.update_message(message, message_type, timeout)\n        else:\n            # Create message widget\n            widget = OverlayMessageWidget(\n                message_id, message, self._widget, message_type, timeout\n            )\n            widget.close_requested.connect(self._on_message_close_request)\n            widget.show()\n\n        # Move widget outside of window\n        pos = widget.pos()\n        pos.setY(pos.y() - widget.height())\n        widget.move(pos)\n        # Store message\n        self._messages[message_id] = widget\n        self._messages_order.append(message_id)\n        # Trigger recalculation timer\n        self._recalculate_timer.start()\n\n        return message_id\n\n    def _on_message_close_request(self, message_id):\n        \"\"\"Message widget requested removement.\"\"\"\n\n        widget = self._messages.get(message_id)\n        if widget is not None:\n            # Add message to closing messages and start recalculation\n            self._closing_messages.add(message_id)\n            self._recalculate_timer.start()\n\n    def _recalculate_positions(self):\n        \"\"\"Recalculate positions of widgets.\"\"\"\n\n        # Skip if there are no messages to process\n        if not self._messages_order:\n            self._recalculate_timer.stop()\n            return\n\n        # All message widgets are in expected positions\n        all_at_place = True\n        # Starting y position\n        pos_y = self._spacing\n        # Current widget width\n        widget_width = self._widget.width()\n        max_width = widget_width - (2 * self._spacing)\n        widget_half_width = widget_width / 2\n\n        # Store message ids that should be removed\n        message_ids_to_remove = set()\n        for message_id in reversed(self._messages_order):\n            widget = self._messages[message_id]\n            pos = widget.pos()\n            # Messages to remove are moved upwards\n            if message_id in self._closing_messages:\n                bottom = pos.y() + widget.height()\n                # Add message to remove if is not visible\n                if bottom &lt; 0 or self._move_size_remove &lt; 1:\n                    message_ids_to_remove.add(message_id)\n                    continue\n\n                # Calculate new y position of message\n                dst_pos_y = pos.y() - self._move_size_remove\n\n            else:\n                # Calculate y position of message\n                # - use y position of previous message widget and add\n                #   move size if is not in final destination yet\n                if widget.underMouse():\n                    dst_pos_y = pos.y()\n                elif pos.y() == pos_y or self._move_size &lt; 1:\n                    dst_pos_y = pos_y\n                elif pos.y() &lt; pos_y:\n                    dst_pos_y = min(pos_y, pos.y() + self._move_size)\n                else:\n                    dst_pos_y = max(pos_y, pos.y() - self._move_size)\n\n            # Store if widget is in place where should be\n            if all_at_place and dst_pos_y != pos_y:\n                all_at_place = False\n\n            # Calculate ideal width and height of message widget\n            height = widget.heightForWidth(max_width)\n            w_size_hint = widget.size_hint_without_word_wrap()\n            widget.resize(min(max_width, w_size_hint.width()), height)\n\n            # Center message widget\n            size = widget.size()\n            pos_x = widget_half_width - (size.width() / 2)\n            # Move widget to destination position\n            widget.move(pos_x, dst_pos_y)\n\n            # Add message widget height and spacing for next message widget\n            pos_y += size.height() + self._spacing\n\n        # Remove widgets to remove\n        for message_id in message_ids_to_remove:\n            self._messages_order.remove(message_id)\n            self._closing_messages.remove(message_id)\n            widget = self._messages.pop(message_id)\n            widget.hide()\n            widget.deleteLater()\n\n        # Stop recalculation timer if all widgets are where should be\n        if all_at_place:\n            self._recalculate_timer.stop()\n\n    def eventFilter(self, source, event):\n        # Trigger recalculation of timer on resize of widget\n        if source is self._widget and event.type() == QtCore.QEvent.Resize:\n            self._recalculate_timer.start()\n\n        return super(MessageOverlayObject, self).eventFilter(source, event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/overlay_messages.html#client.ayon_core.tools.utils.overlay_messages.MessageOverlayObject.add_message","title":"<code>add_message(message, message_type=None, timeout=None, message_id=None)</code>","text":"<p>Add single message into overlay.</p> <p>Parameters:</p> Name Type Description Default <code>message</code> <code>str</code> <p>Message that will be shown.</p> required <code>timeout</code> <code>int</code> <p>Message timeout.</p> <code>None</code> <code>message_type</code> <code>str</code> <p>Message type can be used as property in stylesheets.</p> <code>None</code> <code>message_id</code> <code>str</code> <p>UUID of already existing message to update it's message and timeout. Is created with different id if is not available anymore.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>str</code> <p>UUID of message which can be used to update message.</p> Source code in <code>client/ayon_core/tools/utils/overlay_messages.py</code> <pre><code>def add_message(\n    self, message, message_type=None, timeout=None, message_id=None\n):\n    \"\"\"Add single message into overlay.\n\n    Args:\n        message (str): Message that will be shown.\n        timeout (int): Message timeout.\n        message_type (str): Message type can be used as property in\n            stylesheets.\n        message_id (str): UUID of already existing message to update\n            it's message and timeout. Is created with different id if is\n            not available anymore.\n\n    Returns:\n        str: UUID of message which can be used to update message.\n    \"\"\"\n    # Skip empty messages\n    if not message:\n        return\n\n    if timeout is None:\n        timeout = self._default_timeout\n\n    # Create unique id of message\n    widget = None\n    if message_id is not None:\n        widget = self._messages.get(message_id)\n\n    if message_id is None:\n        message_id = str(uuid.uuid4())\n\n    elif message_id in self._messages_order:\n        self._messages_order.remove(message_id)\n\n    if widget is not None:\n        # NOTE: Update of message won't change paint order which should be\n        #   ok in most of cases as it matters only when messages are\n        #   animated\n        widget.update_message(message, message_type, timeout)\n    else:\n        # Create message widget\n        widget = OverlayMessageWidget(\n            message_id, message, self._widget, message_type, timeout\n        )\n        widget.close_requested.connect(self._on_message_close_request)\n        widget.show()\n\n    # Move widget outside of window\n    pos = widget.pos()\n    pos.setY(pos.y() - widget.height())\n    widget.move(pos)\n    # Store message\n    self._messages[message_id] = widget\n    self._messages_order.append(message_id)\n    # Trigger recalculation timer\n    self._recalculate_timer.start()\n\n    return message_id\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/overlay_messages.html#client.ayon_core.tools.utils.overlay_messages.OverlayMessageWidget","title":"<code>OverlayMessageWidget</code>","text":"<p>               Bases: <code>QFrame</code></p> <p>Message widget showed as overlay.</p> <p>Message is hidden after timeout but can be overriden by mouse hover. Mouse hover can add additional 2 seconds of widget's visibility.</p> <p>Parameters:</p> Name Type Description Default <code>message_id</code> <code>str</code> <p>Unique identifier of message widget for 'MessageOverlayObject'.</p> required <code>message</code> <code>str</code> <p>Text shown in message.</p> required <code>parent</code> <code>QWidget</code> <p>Parent widget where message is visible.</p> required <code>timeout</code> <code>int</code> <p>Timeout of message's visibility (default 5000).</p> <code>None</code> <code>message_type</code> <code>str</code> <p>Property which can be used in styles for specific kid of message.</p> <code>None</code> Source code in <code>client/ayon_core/tools/utils/overlay_messages.py</code> <pre><code>class OverlayMessageWidget(QtWidgets.QFrame):\n    \"\"\"Message widget showed as overlay.\n\n    Message is hidden after timeout but can be overriden by mouse hover.\n    Mouse hover can add additional 2 seconds of widget's visibility.\n\n    Args:\n        message_id (str): Unique identifier of message widget for\n            'MessageOverlayObject'.\n        message (str): Text shown in message.\n        parent (QWidget): Parent widget where message is visible.\n        timeout (int): Timeout of message's visibility (default 5000).\n        message_type (str): Property which can be used in styles for specific\n            kid of message.\n    \"\"\"\n\n    close_requested = QtCore.Signal(str)\n    _default_timeout = 5000\n\n    def __init__(\n        self, message_id, message, parent, message_type=None, timeout=None\n    ):\n        super(OverlayMessageWidget, self).__init__(parent)\n        self.setObjectName(\"OverlayMessageWidget\")\n\n        if message_type:\n            set_style_property(self, \"type\", message_type)\n\n        if not timeout:\n            timeout = self._default_timeout\n        timeout_timer = QtCore.QTimer()\n        timeout_timer.setInterval(timeout)\n        timeout_timer.setSingleShot(True)\n\n        hover_timer = QtCore.QTimer()\n        hover_timer.setInterval(2000)\n        hover_timer.setSingleShot(True)\n\n        label_widget = QtWidgets.QLabel(message, self)\n        label_widget.setAlignment(QtCore.Qt.AlignCenter)\n        label_widget.setWordWrap(True)\n        close_btn = CloseButton(self)\n\n        layout = QtWidgets.QHBoxLayout(self)\n        layout.setContentsMargins(5, 5, 0, 5)\n        layout.addWidget(label_widget, 1)\n        layout.addWidget(close_btn, 0)\n\n        close_btn.clicked.connect(self._on_close_clicked)\n        timeout_timer.timeout.connect(self._on_timer_timeout)\n        hover_timer.timeout.connect(self._on_hover_timeout)\n\n        self._label_widget = label_widget\n        self._message_id = message_id\n        self._timeout_timer = timeout_timer\n        self._hover_timer = hover_timer\n\n    def update_message(self, message, message_type=None, timeout=None):\n        self._label_widget.setText(message)\n        if timeout:\n            self._timeout_timer.setInterval(timeout)\n\n        set_style_property(self, \"type\", message_type)\n\n        self._timeout_timer.start()\n\n    def size_hint_without_word_wrap(self):\n        \"\"\"Size hint in cases that word wrap of label is disabled.\"\"\"\n        self._label_widget.setWordWrap(False)\n        size_hint = self.sizeHint()\n        self._label_widget.setWordWrap(True)\n        return size_hint\n\n    def showEvent(self, event):\n        \"\"\"Start timeout on show.\"\"\"\n        super(OverlayMessageWidget, self).showEvent(event)\n        self._timeout_timer.start()\n\n    def _on_timer_timeout(self):\n        \"\"\"On message timeout.\"\"\"\n        # Skip closing if hover timer is active\n        if not self._hover_timer.isActive():\n            self._close_message()\n\n    def _on_hover_timeout(self):\n        \"\"\"Hover timer timed out.\"\"\"\n        # Check if is still under widget\n        if self.underMouse():\n            self._hover_timer.start()\n        else:\n            self._close_message()\n\n    def _on_close_clicked(self):\n        self._close_message()\n\n    def _close_message(self):\n        \"\"\"Emmit close request to 'MessageOverlayObject'.\"\"\"\n        self.close_requested.emit(self._message_id)\n\n    def enterEvent(self, event):\n        \"\"\"Start hover timer on hover.\"\"\"\n        super(OverlayMessageWidget, self).enterEvent(event)\n        self._hover_timer.start()\n\n    def leaveEvent(self, event):\n        \"\"\"Start hover timer on hover leave.\"\"\"\n        super(OverlayMessageWidget, self).leaveEvent(event)\n        self._hover_timer.start()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/overlay_messages.html#client.ayon_core.tools.utils.overlay_messages.OverlayMessageWidget.enterEvent","title":"<code>enterEvent(event)</code>","text":"<p>Start hover timer on hover.</p> Source code in <code>client/ayon_core/tools/utils/overlay_messages.py</code> <pre><code>def enterEvent(self, event):\n    \"\"\"Start hover timer on hover.\"\"\"\n    super(OverlayMessageWidget, self).enterEvent(event)\n    self._hover_timer.start()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/overlay_messages.html#client.ayon_core.tools.utils.overlay_messages.OverlayMessageWidget.leaveEvent","title":"<code>leaveEvent(event)</code>","text":"<p>Start hover timer on hover leave.</p> Source code in <code>client/ayon_core/tools/utils/overlay_messages.py</code> <pre><code>def leaveEvent(self, event):\n    \"\"\"Start hover timer on hover leave.\"\"\"\n    super(OverlayMessageWidget, self).leaveEvent(event)\n    self._hover_timer.start()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/overlay_messages.html#client.ayon_core.tools.utils.overlay_messages.OverlayMessageWidget.showEvent","title":"<code>showEvent(event)</code>","text":"<p>Start timeout on show.</p> Source code in <code>client/ayon_core/tools/utils/overlay_messages.py</code> <pre><code>def showEvent(self, event):\n    \"\"\"Start timeout on show.\"\"\"\n    super(OverlayMessageWidget, self).showEvent(event)\n    self._timeout_timer.start()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/overlay_messages.html#client.ayon_core.tools.utils.overlay_messages.OverlayMessageWidget.size_hint_without_word_wrap","title":"<code>size_hint_without_word_wrap()</code>","text":"<p>Size hint in cases that word wrap of label is disabled.</p> Source code in <code>client/ayon_core/tools/utils/overlay_messages.py</code> <pre><code>def size_hint_without_word_wrap(self):\n    \"\"\"Size hint in cases that word wrap of label is disabled.\"\"\"\n    self._label_widget.setWordWrap(False)\n    size_hint = self.sizeHint()\n    self._label_widget.setWordWrap(True)\n    return size_hint\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/projects_widget.html","title":"projects_widget","text":""},{"location":"autoapi/client/ayon_core/tools/utils/projects_widget.html#client.ayon_core.tools.utils.projects_widget.ProjectsCombobox","title":"<code>ProjectsCombobox</code>","text":"<p>               Bases: <code>QWidget</code></p> Source code in <code>client/ayon_core/tools/utils/projects_widget.py</code> <pre><code>class ProjectsCombobox(QtWidgets.QWidget):\n    refreshed = QtCore.Signal()\n    selection_changed = QtCore.Signal(str)\n\n    def __init__(\n        self,\n        controller: AbstractProjectController,\n        parent: QtWidgets.QWidget,\n        handle_expected_selection: bool = False,\n    ):\n        super().__init__(parent)\n\n        projects_combobox = QtWidgets.QComboBox(self)\n        combobox_delegate = ProjectsDelegate(projects_combobox)\n        projects_combobox.setItemDelegate(combobox_delegate)\n        projects_model = ProjectsQtModel(controller)\n        projects_proxy_model = ProjectSortFilterProxy()\n        projects_proxy_model.setSourceModel(projects_model)\n        projects_combobox.setModel(projects_proxy_model)\n\n        main_layout = QtWidgets.QHBoxLayout(self)\n        main_layout.setContentsMargins(0, 0, 0, 0)\n        main_layout.addWidget(projects_combobox, 1)\n\n        projects_model.refreshed.connect(self._on_model_refresh)\n\n        controller.register_event_callback(\n            \"projects.refresh.finished\",\n            self._on_projects_refresh_finished\n        )\n        controller.register_event_callback(\n            \"controller.refresh.finished\",\n            self._on_controller_refresh\n        )\n        controller.register_event_callback(\n            \"expected_selection_changed\",\n            self._on_expected_selection_change\n        )\n\n        projects_combobox.currentIndexChanged.connect(\n            self._on_current_index_changed\n        )\n\n        self._controller = controller\n        self._listen_selection_change = True\n        self._select_item_visible = False\n\n        self._handle_expected_selection = handle_expected_selection\n        self._expected_selection = None\n\n        self._projects_combobox = projects_combobox\n        self._projects_model = projects_model\n        self._projects_proxy_model = projects_proxy_model\n        self._combobox_delegate = combobox_delegate\n\n    def refresh(self):\n        self._projects_model.refresh()\n\n    def set_selection(self, project_name: str):\n        \"\"\"Set selection to a given project.\n\n        Selection change is ignored if project is not found.\n\n        Args:\n            project_name (str): Name of project.\n\n        Returns:\n            bool: True if selection was changed, False otherwise. NOTE:\n                Selection may not be changed if project is not found, or if\n                project is already selected.\n\n        \"\"\"\n        idx = self._projects_combobox.findData(\n            project_name, PROJECT_NAME_ROLE)\n        if idx &lt; 0:\n            return False\n        if idx != self._projects_combobox.currentIndex():\n            self._projects_combobox.setCurrentIndex(idx)\n            return True\n        return False\n\n    def set_listen_to_selection_change(self, listen: bool):\n        \"\"\"Disable listening to changes of the selection.\n\n        Because combobox is triggering selection change when it's model\n        is refreshed, it's necessary to disable listening to selection for\n        some cases, e.g. when is on a different page of UI and should be just\n        refreshed.\n\n        Args:\n            listen (bool): Enable or disable listening to selection changes.\n        \"\"\"\n\n        self._listen_selection_change = listen\n\n    def get_selected_project_name(self):\n        \"\"\"Name of selected project.\n\n        Returns:\n            Union[str, None]: Name of selected project, or None if no project\n        \"\"\"\n\n        idx = self._projects_combobox.currentIndex()\n        if idx &lt; 0:\n            return None\n        return self._projects_combobox.itemData(idx, PROJECT_NAME_ROLE)\n\n    def set_current_context_project(self, project_name: str):\n        self._projects_model.set_current_context_project(project_name)\n        self._projects_proxy_model.invalidateFilter()\n\n    def set_select_item_visible(self, visible: bool):\n        self._select_item_visible = visible\n        self._projects_model.set_select_item_visible(visible)\n        self._update_select_item_visiblity()\n\n    def set_libraries_separator_visible(self, visible):\n        self._projects_model.set_libraries_separator_visible(visible)\n\n    def is_active_filter_enabled(self):\n        return self._projects_proxy_model.is_active_filter_enabled()\n\n    def set_active_filter_enabled(self, enabled):\n        return self._projects_proxy_model.set_active_filter_enabled(enabled)\n\n    def set_standard_filter_enabled(self, enabled):\n        return self._projects_proxy_model.set_standard_filter_enabled(enabled)\n\n    def set_library_filter_enabled(self, enabled):\n        return self._projects_proxy_model.set_library_filter_enabled(enabled)\n\n    def _update_select_item_visiblity(self, **kwargs):\n        if not self._select_item_visible:\n            return\n        if \"project_name\" not in kwargs:\n            project_name = self.get_selected_project_name()\n        else:\n            project_name = kwargs.get(\"project_name\")\n\n        # Hide the item if a project is selected\n        self._projects_model.set_selected_project(project_name)\n\n    def _on_current_index_changed(self, idx):\n        if not self._listen_selection_change:\n            return\n        project_name = self._projects_combobox.itemData(\n            idx, PROJECT_NAME_ROLE)\n        self._update_select_item_visiblity(project_name=project_name)\n        self._controller.set_selected_project(project_name)\n        self.selection_changed.emit(project_name or \"\")\n\n    def _on_model_refresh(self):\n        self._projects_proxy_model.sort(0)\n        self._projects_proxy_model.invalidateFilter()\n        if self._expected_selection:\n            self._set_expected_selection()\n        self._update_select_item_visiblity()\n        self.refreshed.emit()\n\n    def _on_projects_refresh_finished(self, event):\n        if event[\"sender\"] != PROJECTS_MODEL_SENDER:\n            self._projects_model.refresh()\n\n    def _on_controller_refresh(self):\n        self._update_expected_selection()\n\n    # Expected selection handling\n    def _on_expected_selection_change(self, event):\n        self._update_expected_selection(event.data)\n\n    def _set_expected_selection(self):\n        if not self._handle_expected_selection:\n            return\n        project_name = self._expected_selection\n        if project_name is not None:\n            if project_name != self.get_selected_project_name():\n                self.set_selection(project_name)\n            else:\n                # Fake project change\n                self._on_current_index_changed(\n                    self._projects_combobox.currentIndex()\n                )\n\n        self._controller.expected_project_selected(project_name)\n\n    def _update_expected_selection(self, expected_data=None):\n        if not self._handle_expected_selection:\n            return\n        if expected_data is None:\n            expected_data = self._controller.get_expected_selection_data()\n\n        project_data = expected_data.get(\"project\")\n        if (\n            not project_data\n            or not project_data[\"current\"]\n            or project_data[\"selected\"]\n        ):\n            return\n        self._expected_selection = project_data[\"name\"]\n        if not self._projects_model.is_refreshing:\n            self._set_expected_selection()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/projects_widget.html#client.ayon_core.tools.utils.projects_widget.ProjectsCombobox.get_selected_project_name","title":"<code>get_selected_project_name()</code>","text":"<p>Name of selected project.</p> <p>Returns:</p> Type Description <p>Union[str, None]: Name of selected project, or None if no project</p> Source code in <code>client/ayon_core/tools/utils/projects_widget.py</code> <pre><code>def get_selected_project_name(self):\n    \"\"\"Name of selected project.\n\n    Returns:\n        Union[str, None]: Name of selected project, or None if no project\n    \"\"\"\n\n    idx = self._projects_combobox.currentIndex()\n    if idx &lt; 0:\n        return None\n    return self._projects_combobox.itemData(idx, PROJECT_NAME_ROLE)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/projects_widget.html#client.ayon_core.tools.utils.projects_widget.ProjectsCombobox.set_listen_to_selection_change","title":"<code>set_listen_to_selection_change(listen)</code>","text":"<p>Disable listening to changes of the selection.</p> <p>Because combobox is triggering selection change when it's model is refreshed, it's necessary to disable listening to selection for some cases, e.g. when is on a different page of UI and should be just refreshed.</p> <p>Parameters:</p> Name Type Description Default <code>listen</code> <code>bool</code> <p>Enable or disable listening to selection changes.</p> required Source code in <code>client/ayon_core/tools/utils/projects_widget.py</code> <pre><code>def set_listen_to_selection_change(self, listen: bool):\n    \"\"\"Disable listening to changes of the selection.\n\n    Because combobox is triggering selection change when it's model\n    is refreshed, it's necessary to disable listening to selection for\n    some cases, e.g. when is on a different page of UI and should be just\n    refreshed.\n\n    Args:\n        listen (bool): Enable or disable listening to selection changes.\n    \"\"\"\n\n    self._listen_selection_change = listen\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/projects_widget.html#client.ayon_core.tools.utils.projects_widget.ProjectsCombobox.set_selection","title":"<code>set_selection(project_name)</code>","text":"<p>Set selection to a given project.</p> <p>Selection change is ignored if project is not found.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Name of project.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <p>True if selection was changed, False otherwise. NOTE: Selection may not be changed if project is not found, or if project is already selected.</p> Source code in <code>client/ayon_core/tools/utils/projects_widget.py</code> <pre><code>def set_selection(self, project_name: str):\n    \"\"\"Set selection to a given project.\n\n    Selection change is ignored if project is not found.\n\n    Args:\n        project_name (str): Name of project.\n\n    Returns:\n        bool: True if selection was changed, False otherwise. NOTE:\n            Selection may not be changed if project is not found, or if\n            project is already selected.\n\n    \"\"\"\n    idx = self._projects_combobox.findData(\n        project_name, PROJECT_NAME_ROLE)\n    if idx &lt; 0:\n        return False\n    if idx != self._projects_combobox.currentIndex():\n        self._projects_combobox.setCurrentIndex(idx)\n        return True\n    return False\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/projects_widget.html#client.ayon_core.tools.utils.projects_widget.ProjectsQtModel","title":"<code>ProjectsQtModel</code>","text":"<p>               Bases: <code>QStandardItemModel</code></p> Source code in <code>client/ayon_core/tools/utils/projects_widget.py</code> <pre><code>class ProjectsQtModel(QtGui.QStandardItemModel):\n    refreshed = QtCore.Signal()\n\n    def __init__(self, controller: AbstractProjectController):\n        super().__init__()\n        self._controller = controller\n\n        self._project_items = {}\n        self._has_libraries = False\n\n        self._empty_item = None\n        self._empty_item_added = False\n\n        self._select_item = None\n        self._select_item_added = False\n        self._select_item_visible = None\n\n        self._libraries_sep_item = None\n        self._libraries_sep_item_added = False\n        self._libraries_sep_item_visible = False\n\n        self._current_context_project = None\n\n        self._selected_project = None\n\n        self._refresh_thread = None\n\n    @property\n    def is_refreshing(self):\n        return self._refresh_thread is not None\n\n    def refresh(self):\n        self._refresh()\n\n    def has_content(self):\n        return len(self._project_items) &gt; 0\n\n    def get_index_by_project_name(self, project_name):\n        \"\"\"Get index of project by name.\n\n        Args:\n            project_name (str): Project name.\n\n        Returns:\n            QtCore.QModelIndex: Index of project item. Index is not valid\n                if project is not found.\n\n        \"\"\"\n        item = self._project_items.get(project_name)\n        if item is None:\n            return QtCore.QModelIndex()\n        return self.indexFromItem(item)\n\n    def set_select_item_visible(self, visible):\n        if self._select_item_visible is visible:\n            return\n        self._select_item_visible = visible\n\n        if self._selected_project is None:\n            self._add_select_item()\n\n    def set_libraries_separator_visible(self, visible):\n        if self._libraries_sep_item_visible is visible:\n            return\n        self._libraries_sep_item_visible = visible\n\n    def set_selected_project(self, project_name):\n        if not self._select_item_visible:\n            return\n\n        self._selected_project = project_name\n        if project_name is None:\n            self._add_select_item()\n        else:\n            self._remove_select_item()\n\n    def set_current_context_project(self, project_name):\n        if project_name == self._current_context_project:\n            return\n        self._unset_current_context_project(self._current_context_project)\n        self._current_context_project = project_name\n        self._set_current_context_project(project_name)\n\n    def _set_current_context_project(self, project_name):\n        item = self._project_items.get(project_name)\n        if item is None:\n            return\n        item.setData(True, PROJECT_IS_CURRENT_ROLE)\n\n    def _unset_current_context_project(self, project_name):\n        item = self._project_items.get(project_name)\n        if item is None:\n            return\n        item.setData(False, PROJECT_IS_CURRENT_ROLE)\n\n    def _add_empty_item(self):\n        if self._empty_item_added:\n            return\n        self._empty_item_added = True\n        item = self._get_empty_item()\n        root_item = self.invisibleRootItem()\n        root_item.appendRow(item)\n\n    def _remove_empty_item(self):\n        if not self._empty_item_added:\n            return\n        self._empty_item_added = False\n        root_item = self.invisibleRootItem()\n        item = self._get_empty_item()\n        root_item.takeRow(item.row())\n\n    def _get_empty_item(self):\n        if self._empty_item is None:\n            item = QtGui.QStandardItem(\"&lt; No projects &gt;\")\n            item.setFlags(QtCore.Qt.NoItemFlags)\n            self._empty_item = item\n        return self._empty_item\n\n    def _get_library_sep_item(self):\n        if self._libraries_sep_item is not None:\n            return self._libraries_sep_item\n\n        item = QtGui.QStandardItem()\n        item.setData(\"Libraries\", QtCore.Qt.DisplayRole)\n        item.setData(True, LIBRARY_PROJECT_SEPARATOR_ROLE)\n        item.setFlags(QtCore.Qt.NoItemFlags)\n        self._libraries_sep_item = item\n        return item\n\n    def _add_library_sep_item(self):\n        if (\n            not self._libraries_sep_item_visible\n            or self._libraries_sep_item_added\n        ):\n            return\n        self._libraries_sep_item_added = True\n        item = self._get_library_sep_item()\n        root_item = self.invisibleRootItem()\n        root_item.appendRow(item)\n\n    def _remove_library_sep_item(self):\n        if (\n            not self._libraries_sep_item_added\n        ):\n            return\n        self._libraries_sep_item_added = False\n        item = self._get_library_sep_item()\n        root_item = self.invisibleRootItem()\n        root_item.takeRow(item.row())\n\n    def _add_select_item(self):\n        if self._select_item_added:\n            return\n        self._select_item_added = True\n        item = self._get_select_item()\n        root_item = self.invisibleRootItem()\n        root_item.appendRow(item)\n\n    def _remove_select_item(self):\n        if not self._select_item_added:\n            return\n        self._select_item_added = False\n        root_item = self.invisibleRootItem()\n        item = self._get_select_item()\n        root_item.takeRow(item.row())\n\n    def _get_select_item(self):\n        if self._select_item is None:\n            item = QtGui.QStandardItem(\"&lt; Select project &gt;\")\n            item.setEditable(False)\n            self._select_item = item\n        return self._select_item\n\n    def _refresh(self):\n        if self._refresh_thread is not None:\n            return\n\n        refresh_thread = RefreshThread(\n            \"projects\", self._query_project_items\n        )\n        refresh_thread.refresh_finished.connect(self._refresh_finished)\n\n        self._refresh_thread = refresh_thread\n        refresh_thread.start()\n\n    def _query_project_items(self):\n        return self._controller.get_project_items(\n            sender=PROJECTS_MODEL_SENDER\n        )\n\n    def _refresh_finished(self):\n        # TODO check if failed\n        result = self._refresh_thread.get_result()\n        if result is not None:\n            self._fill_items(result)\n\n        self._refresh_thread = None\n        if result is None:\n            self._refresh()\n        else:\n            self.refreshed.emit()\n\n    def _fill_items(self, project_items: list[ProjectItem]):\n        new_project_names = {\n            project_item.name\n            for project_item in project_items\n        }\n\n        # Handle \"Select item\" visibility\n        if self._select_item_visible:\n            # Add select project. if previously selected project is not in\n            #   project items\n            if self._selected_project not in new_project_names:\n                self._add_select_item()\n            else:\n                self._remove_select_item()\n\n        root_item = self.invisibleRootItem()\n\n        items_to_remove = set(self._project_items.keys()) - new_project_names\n        for project_name in items_to_remove:\n            item = self._project_items.pop(project_name)\n            root_item.takeRow(item.row())\n\n        has_library_project = False\n        new_items = []\n        for project_item in project_items:\n            project_name = project_item.name\n            item = self._project_items.get(project_name)\n            if project_item.is_library:\n                has_library_project = True\n            if item is None:\n                item = QtGui.QStandardItem()\n                item.setEditable(False)\n                new_items.append(item)\n            icon = get_qt_icon(project_item.icon)\n            item.setData(project_name, QtCore.Qt.DisplayRole)\n            item.setData(icon, QtCore.Qt.DecorationRole)\n            item.setData(project_name, PROJECT_NAME_ROLE)\n            item.setData(project_item.active, PROJECT_IS_ACTIVE_ROLE)\n            item.setData(project_item.is_library, PROJECT_IS_LIBRARY_ROLE)\n            item.setData(project_item.is_pinned, PROJECT_IS_PINNED_ROLE)\n            is_current = project_name == self._current_context_project\n            item.setData(is_current, PROJECT_IS_CURRENT_ROLE)\n            self._project_items[project_name] = item\n\n        self._set_current_context_project(self._current_context_project)\n\n        self._has_libraries = has_library_project\n\n        if new_items:\n            root_item.appendRows(new_items)\n\n        if self.has_content():\n            # Make sure \"No projects\" item is removed\n            self._remove_empty_item()\n            if has_library_project:\n                self._add_library_sep_item()\n            else:\n                self._remove_library_sep_item()\n        else:\n            # Keep only \"No projects\" item\n            self._add_empty_item()\n            self._remove_select_item()\n            self._remove_library_sep_item()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/projects_widget.html#client.ayon_core.tools.utils.projects_widget.ProjectsQtModel.get_index_by_project_name","title":"<code>get_index_by_project_name(project_name)</code>","text":"<p>Get index of project by name.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <p>Returns:</p> Type Description <p>QtCore.QModelIndex: Index of project item. Index is not valid if project is not found.</p> Source code in <code>client/ayon_core/tools/utils/projects_widget.py</code> <pre><code>def get_index_by_project_name(self, project_name):\n    \"\"\"Get index of project by name.\n\n    Args:\n        project_name (str): Project name.\n\n    Returns:\n        QtCore.QModelIndex: Index of project item. Index is not valid\n            if project is not found.\n\n    \"\"\"\n    item = self._project_items.get(project_name)\n    if item is None:\n        return QtCore.QModelIndex()\n    return self.indexFromItem(item)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/projects_widget.html#client.ayon_core.tools.utils.projects_widget.ProjectsWidget","title":"<code>ProjectsWidget</code>","text":"<p>               Bases: <code>QWidget</code></p> <p>Projects widget showing projects in list.</p> Source code in <code>client/ayon_core/tools/utils/projects_widget.py</code> <pre><code>class ProjectsWidget(QtWidgets.QWidget):\n    \"\"\"Projects widget showing projects in list.\n\n    Warnings:\n        This widget does not support expected selection handling.\n\n    \"\"\"\n    refreshed = QtCore.Signal()\n    selection_changed = QtCore.Signal(str)\n    double_clicked = QtCore.Signal()\n\n    def __init__(\n        self,\n        controller: AbstractProjectController,\n        parent: Optional[QtWidgets.QWidget] = None\n    ):\n        super().__init__(parent=parent)\n\n        projects_view = ListView(parent=self)\n        projects_view.setResizeMode(QtWidgets.QListView.Adjust)\n        projects_view.setVerticalScrollMode(\n            QtWidgets.QAbstractItemView.ScrollPerPixel\n        )\n        projects_view.setAlternatingRowColors(False)\n        projects_view.setWrapping(False)\n        projects_view.setWordWrap(False)\n        projects_view.setSpacing(0)\n        projects_delegate = ProjectsDelegate(projects_view)\n        projects_view.setItemDelegate(projects_delegate)\n        projects_view.activate_flick_charm()\n        projects_view.set_deselectable(True)\n\n        projects_model = ProjectsQtModel(controller)\n        projects_proxy_model = ProjectSortFilterProxy()\n        projects_proxy_model.setSourceModel(projects_model)\n        projects_view.setModel(projects_proxy_model)\n\n        main_layout = QtWidgets.QVBoxLayout(self)\n        main_layout.setContentsMargins(0, 0, 0, 0)\n        main_layout.addWidget(projects_view, 1)\n\n        projects_view.selectionModel().selectionChanged.connect(\n            self._on_selection_change\n        )\n        projects_view.double_clicked.connect(self.double_clicked)\n        projects_model.refreshed.connect(self._on_model_refresh)\n\n        controller.register_event_callback(\n            \"projects.refresh.finished\",\n            self._on_projects_refresh_finished\n        )\n\n        self._controller = controller\n\n        self._projects_view = projects_view\n        self._projects_model = projects_model\n        self._projects_proxy_model = projects_proxy_model\n        self._projects_delegate = projects_delegate\n\n    def refresh(self):\n        self._projects_model.refresh()\n\n    def has_content(self) -&gt; bool:\n        \"\"\"Model has at least one project.\n\n        Returns:\n             bool: True if there is any content in the model.\n\n        \"\"\"\n        return self._projects_model.has_content()\n\n    def set_name_filter(self, text: str):\n        self._projects_proxy_model.setFilterFixedString(text)\n\n    def get_selected_project(self) -&gt; Optional[str]:\n        selection_model = self._projects_view.selectionModel()\n        for index in selection_model.selectedIndexes():\n            project_name = index.data(PROJECT_NAME_ROLE)\n            if project_name:\n                return project_name\n        return None\n\n    def set_selected_project(self, project_name: Optional[str]):\n        if project_name is None:\n            self._projects_view.clearSelection()\n            self._projects_view.setCurrentIndex(QtCore.QModelIndex())\n            return\n\n        index = self._projects_model.get_index_by_project_name(project_name)\n        if not index.isValid():\n            return\n        proxy_index = self._projects_proxy_model.mapFromSource(index)\n        if proxy_index.isValid():\n            selection_model = self._projects_view.selectionModel()\n            selection_model.select(\n                proxy_index,\n                QtCore.QItemSelectionModel.ClearAndSelect\n            )\n\n    def _on_model_refresh(self):\n        self._projects_proxy_model.sort(0)\n        self._projects_proxy_model.invalidateFilter()\n        self.refreshed.emit()\n\n    def _on_selection_change(self, new_selection, _old_selection):\n        project_name = None\n        for index in new_selection.indexes():\n            name = index.data(PROJECT_NAME_ROLE)\n            if name:\n                project_name = name\n                break\n        self.selection_changed.emit(project_name or \"\")\n        self._controller.set_selected_project(project_name)\n\n    def _on_projects_refresh_finished(self, event):\n        if event[\"sender\"] != PROJECTS_MODEL_SENDER:\n            self._projects_model.refresh()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/projects_widget.html#client.ayon_core.tools.utils.projects_widget.ProjectsWidget.has_content","title":"<code>has_content()</code>","text":"<p>Model has at least one project.</p> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if there is any content in the model.</p> Source code in <code>client/ayon_core/tools/utils/projects_widget.py</code> <pre><code>def has_content(self) -&gt; bool:\n    \"\"\"Model has at least one project.\n\n    Returns:\n         bool: True if there is any content in the model.\n\n    \"\"\"\n    return self._projects_model.has_content()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/sliders.html","title":"sliders","text":""},{"location":"autoapi/client/ayon_core/tools/utils/tasks_widget.html","title":"tasks_widget","text":""},{"location":"autoapi/client/ayon_core/tools/utils/tasks_widget.html#client.ayon_core.tools.utils.tasks_widget.TasksQtModel","title":"<code>TasksQtModel</code>","text":"<p>               Bases: <code>QStandardItemModel</code></p> <p>Tasks model which cares about refresh of tasks by folder id.</p> <p>Parameters:</p> Name Type Description Default <code>controller</code> <code>AbstractWorkfilesFrontend</code> <p>The control object.</p> required Source code in <code>client/ayon_core/tools/utils/tasks_widget.py</code> <pre><code>class TasksQtModel(QtGui.QStandardItemModel):\n    \"\"\"Tasks model which cares about refresh of tasks by folder id.\n\n    Args:\n        controller (AbstractWorkfilesFrontend): The control object.\n\n    \"\"\"\n    _default_task_icon = None\n    refreshed = QtCore.Signal()\n    project_changed = QtCore.Signal()\n    column_labels = [\"Tasks\"]\n\n    def __init__(self, controller):\n        super().__init__()\n\n        self.setColumnCount(len(self.column_labels))\n        for idx, label in enumerate(self.column_labels):\n            self.setHeaderData(idx, QtCore.Qt.Horizontal, label)\n\n        self._controller = controller\n\n        self._items_by_name = {}\n        self._has_content = False\n        self._is_refreshing = False\n\n        self._invalid_selection_item_used = False\n        self._invalid_selection_item = None\n        self._empty_tasks_item_used = False\n        self._empty_tasks_item = None\n\n        self._last_project_name = None\n        self._last_folder_id = None\n\n        self._refresh_threads = {}\n        self._current_refresh_thread = None\n\n        # Initial state\n        self._add_invalid_selection_item()\n\n    def _clear_items(self):\n        self._items_by_name = {}\n        self._has_content = False\n        self._remove_invalid_items()\n        root_item = self.invisibleRootItem()\n        while root_item.rowCount() != 0:\n            root_item.takeRow(0)\n\n    def refresh(self):\n        \"\"\"Refresh tasks for last project and folder.\"\"\"\n\n        self._refresh(self._last_project_name, self._last_folder_id)\n\n    def set_context(self, project_name, folder_id):\n        \"\"\"Set context for which should be tasks showed.\n\n        Args:\n            project_name (Union[str]): Name of project.\n            folder_id (Union[str, None]): Folder id.\n        \"\"\"\n\n        self._refresh(project_name, folder_id)\n\n    def get_index_by_name(self, task_name):\n        \"\"\"Find item by name and return its index.\n\n        Returns:\n            QtCore.QModelIndex: Index of item. Is invalid if task is not\n                found by name.\n        \"\"\"\n\n        item = self._items_by_name.get(task_name)\n        if item is None:\n            return QtCore.QModelIndex()\n        return self.indexFromItem(item)\n\n    def get_last_project_name(self):\n        \"\"\"Get last refreshed project name.\n\n        Returns:\n            Union[str, None]: Project name.\n        \"\"\"\n\n        return self._last_project_name\n\n    def get_last_folder_id(self):\n        \"\"\"Get last refreshed folder id.\n\n        Returns:\n            Union[str, None]: Folder id.\n        \"\"\"\n\n        return self._last_folder_id\n\n    def _get_invalid_selection_item(self):\n        if self._invalid_selection_item is None:\n            item = QtGui.QStandardItem(\"Select a folder\")\n            item.setFlags(QtCore.Qt.NoItemFlags)\n            icon = get_qt_icon(AwesomeFontIcon(\n                \"fa.times\",\n                color=get_disabled_entity_icon_color(),\n            ))\n            item.setData(icon, QtCore.Qt.DecorationRole)\n            self._invalid_selection_item = item\n        return self._invalid_selection_item\n\n    def _get_empty_task_item(self):\n        if self._empty_tasks_item is None:\n            item = QtGui.QStandardItem(\"No task\")\n            icon = get_qt_icon(AwesomeFontIcon(\n                \"fa.exclamation-circle\",\n                color=get_disabled_entity_icon_color(),\n            ))\n            item.setData(icon, QtCore.Qt.DecorationRole)\n            item.setFlags(QtCore.Qt.NoItemFlags)\n            self._empty_tasks_item = item\n        return self._empty_tasks_item\n\n    def _add_invalid_item(self, item):\n        self._clear_items()\n        root_item = self.invisibleRootItem()\n        root_item.appendRow(item)\n\n    def _remove_invalid_item(self, item):\n        root_item = self.invisibleRootItem()\n        root_item.takeRow(item.row())\n\n    def _remove_invalid_items(self):\n        self._remove_invalid_selection_item()\n        self._remove_empty_task_item()\n\n    def _add_invalid_selection_item(self):\n        if not self._invalid_selection_item_used:\n            self._add_invalid_item(self._get_invalid_selection_item())\n            self._invalid_selection_item_used = True\n\n    def _remove_invalid_selection_item(self):\n        if self._invalid_selection_item:\n            self._remove_invalid_item(self._get_invalid_selection_item())\n            self._invalid_selection_item_used = False\n\n    def _add_empty_task_item(self):\n        if not self._empty_tasks_item_used:\n            self._add_invalid_item(self._get_empty_task_item())\n            self._empty_tasks_item_used = True\n\n    def _remove_empty_task_item(self):\n        if self._empty_tasks_item_used:\n            self._remove_invalid_item(self._get_empty_task_item())\n            self._empty_tasks_item_used = False\n\n    def _refresh(self, project_name, folder_id):\n        project_changed = self._last_project_name != project_name\n        self._is_refreshing = True\n        self._last_project_name = project_name\n        self._last_folder_id = folder_id\n        if project_changed:\n            self.project_changed.emit()\n\n        if not folder_id:\n            self._add_invalid_selection_item()\n            self._current_refresh_thread = None\n            self._is_refreshing = False\n            self.refreshed.emit()\n            return\n\n        thread = self._refresh_threads.get(folder_id)\n        if thread is not None:\n            self._current_refresh_thread = thread\n            return\n        thread = RefreshThread(\n            folder_id,\n            self._thread_getter,\n            project_name,\n            folder_id\n        )\n        self._current_refresh_thread = thread\n        self._refresh_threads[thread.id] = thread\n        thread.refresh_finished.connect(self._on_refresh_thread)\n        thread.start()\n\n    def _thread_getter(self, project_name, folder_id):\n        task_items = self._controller.get_task_items(\n            project_name, folder_id, sender=TASKS_MODEL_SENDER_NAME\n        )\n        task_type_items = {}\n        if hasattr(self._controller, \"get_task_type_items\"):\n            task_type_items = self._controller.get_task_type_items(\n                project_name, sender=TASKS_MODEL_SENDER_NAME\n            )\n        return task_items, task_type_items\n\n    @classmethod\n    def _get_default_task_icon(cls):\n        if cls._default_task_icon is None:\n            cls._default_task_icon = get_qt_icon(AwesomeFontIcon(\n                \"fa.male\",\n                color=get_default_entity_icon_color()\n            ))\n        return cls._default_task_icon\n\n    def _get_task_item_icon(\n        self,\n        task_item,\n        task_type_item_by_name,\n        task_type_icon_cache\n    ):\n        icon = task_type_icon_cache.get(task_item.task_type)\n        if icon is not None:\n            return icon\n\n        task_type_item = task_type_item_by_name.get(\n            task_item.task_type\n        )\n        icon = None\n        if task_type_item is not None:\n            color = task_type_item.color or get_default_entity_icon_color()\n            icon = get_qt_icon(MaterialSymbolsIcon(\n                task_type_item.icon,\n                color=color,\n            ))\n\n        if icon is None:\n            icon = self._get_default_task_icon()\n        task_type_icon_cache[task_item.task_type] = icon\n        return icon\n\n    def _fill_data_from_thread(self, thread):\n        task_items, task_type_items = thread.get_result()\n        # Task items are refreshed\n        if task_items is None:\n            return\n\n        # No tasks are available on folder\n        if not task_items:\n            self._add_empty_task_item()\n            return\n        self._remove_invalid_items()\n\n        task_type_item_by_name = {\n            task_type_item.name: task_type_item\n            for task_type_item in task_type_items\n        }\n        task_type_icon_cache = {}\n        new_items = []\n        new_names = set()\n        for task_item in task_items:\n            name = task_item.name\n            new_names.add(name)\n            item = self._items_by_name.get(name)\n            if item is None:\n                item = QtGui.QStandardItem()\n                item.setEditable(False)\n                new_items.append(item)\n                self._items_by_name[name] = item\n\n            icon = self._get_task_item_icon(\n                task_item,\n                task_type_item_by_name,\n                task_type_icon_cache\n            )\n            item.setData(task_item.full_label, QtCore.Qt.DisplayRole)\n            item.setData(name, ITEM_NAME_ROLE)\n            item.setData(task_item.id, ITEM_ID_ROLE)\n            item.setData(task_item.task_type, TASK_TYPE_ROLE)\n            item.setData(task_item.parent_id, PARENT_ID_ROLE)\n            item.setData(task_item.task_type_order, TASK_TYPE_ORDER_ROLE)\n            item.setData(icon, QtCore.Qt.DecorationRole)\n\n        root_item = self.invisibleRootItem()\n\n        for name in set(self._items_by_name) - new_names:\n            item = self._items_by_name.pop(name)\n            root_item.removeRow(item.row())\n\n        if new_items:\n            root_item.appendRows(new_items)\n\n    def _on_refresh_thread(self, thread_id):\n        \"\"\"Callback when refresh thread is finished.\n\n        Technically can be running multiple refresh threads at the same time,\n        to avoid using values from wrong thread, we check if thread id is\n        current refresh thread id.\n\n        Tasks are stored by name, so if a folder has same task name as\n        previously selected folder it keeps the selection.\n\n        Args:\n            thread_id (str): Thread id.\n        \"\"\"\n        # Make sure to remove thread from '_refresh_threads' dict\n        thread = self._refresh_threads.pop(thread_id)\n        if (\n            self._current_refresh_thread is None\n            or thread_id != self._current_refresh_thread.id\n        ):\n            return\n\n        self._fill_data_from_thread(thread)\n\n        root_item = self.invisibleRootItem()\n        self._has_content = root_item.rowCount() &gt; 0\n        self._current_refresh_thread = None\n        self._is_refreshing = False\n        self.refreshed.emit()\n\n    @property\n    def is_refreshing(self):\n        \"\"\"Model is refreshing.\n\n        Returns:\n            bool: Model is refreshing\n        \"\"\"\n\n        return self._is_refreshing\n\n    @property\n    def has_content(self):\n        \"\"\"Model has content.\n\n        Returns:\n            bools: Have at least one task.\n        \"\"\"\n\n        return self._has_content\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/tasks_widget.html#client.ayon_core.tools.utils.tasks_widget.TasksQtModel.has_content","title":"<code>has_content</code>  <code>property</code>","text":"<p>Model has content.</p> <p>Returns:</p> Name Type Description <code>bools</code> <p>Have at least one task.</p>"},{"location":"autoapi/client/ayon_core/tools/utils/tasks_widget.html#client.ayon_core.tools.utils.tasks_widget.TasksQtModel.is_refreshing","title":"<code>is_refreshing</code>  <code>property</code>","text":"<p>Model is refreshing.</p> <p>Returns:</p> Name Type Description <code>bool</code> <p>Model is refreshing</p>"},{"location":"autoapi/client/ayon_core/tools/utils/tasks_widget.html#client.ayon_core.tools.utils.tasks_widget.TasksQtModel.get_index_by_name","title":"<code>get_index_by_name(task_name)</code>","text":"<p>Find item by name and return its index.</p> <p>Returns:</p> Type Description <p>QtCore.QModelIndex: Index of item. Is invalid if task is not found by name.</p> Source code in <code>client/ayon_core/tools/utils/tasks_widget.py</code> <pre><code>def get_index_by_name(self, task_name):\n    \"\"\"Find item by name and return its index.\n\n    Returns:\n        QtCore.QModelIndex: Index of item. Is invalid if task is not\n            found by name.\n    \"\"\"\n\n    item = self._items_by_name.get(task_name)\n    if item is None:\n        return QtCore.QModelIndex()\n    return self.indexFromItem(item)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/tasks_widget.html#client.ayon_core.tools.utils.tasks_widget.TasksQtModel.get_last_folder_id","title":"<code>get_last_folder_id()</code>","text":"<p>Get last refreshed folder id.</p> <p>Returns:</p> Type Description <p>Union[str, None]: Folder id.</p> Source code in <code>client/ayon_core/tools/utils/tasks_widget.py</code> <pre><code>def get_last_folder_id(self):\n    \"\"\"Get last refreshed folder id.\n\n    Returns:\n        Union[str, None]: Folder id.\n    \"\"\"\n\n    return self._last_folder_id\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/tasks_widget.html#client.ayon_core.tools.utils.tasks_widget.TasksQtModel.get_last_project_name","title":"<code>get_last_project_name()</code>","text":"<p>Get last refreshed project name.</p> <p>Returns:</p> Type Description <p>Union[str, None]: Project name.</p> Source code in <code>client/ayon_core/tools/utils/tasks_widget.py</code> <pre><code>def get_last_project_name(self):\n    \"\"\"Get last refreshed project name.\n\n    Returns:\n        Union[str, None]: Project name.\n    \"\"\"\n\n    return self._last_project_name\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/tasks_widget.html#client.ayon_core.tools.utils.tasks_widget.TasksQtModel.refresh","title":"<code>refresh()</code>","text":"<p>Refresh tasks for last project and folder.</p> Source code in <code>client/ayon_core/tools/utils/tasks_widget.py</code> <pre><code>def refresh(self):\n    \"\"\"Refresh tasks for last project and folder.\"\"\"\n\n    self._refresh(self._last_project_name, self._last_folder_id)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/tasks_widget.html#client.ayon_core.tools.utils.tasks_widget.TasksQtModel.set_context","title":"<code>set_context(project_name, folder_id)</code>","text":"<p>Set context for which should be tasks showed.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>Union[str]</code> <p>Name of project.</p> required <code>folder_id</code> <code>Union[str, None]</code> <p>Folder id.</p> required Source code in <code>client/ayon_core/tools/utils/tasks_widget.py</code> <pre><code>def set_context(self, project_name, folder_id):\n    \"\"\"Set context for which should be tasks showed.\n\n    Args:\n        project_name (Union[str]): Name of project.\n        folder_id (Union[str, None]): Folder id.\n    \"\"\"\n\n    self._refresh(project_name, folder_id)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/tasks_widget.html#client.ayon_core.tools.utils.tasks_widget.TasksWidget","title":"<code>TasksWidget</code>","text":"<p>               Bases: <code>QWidget</code></p> <p>Tasks widget.</p> <p>Widget that handles tasks view, model and selection.</p> <p>Parameters:</p> Name Type Description Default <code>controller</code> <code>AbstractWorkfilesFrontend</code> <p>Workfiles controller.</p> required <code>parent</code> <code>QWidget</code> <p>Parent widget.</p> required <code>handle_expected_selection</code> <code>Optional[bool]</code> <p>Handle expected selection.</p> <code>False</code> Source code in <code>client/ayon_core/tools/utils/tasks_widget.py</code> <pre><code>class TasksWidget(QtWidgets.QWidget):\n    \"\"\"Tasks widget.\n\n    Widget that handles tasks view, model and selection.\n\n    Args:\n        controller (AbstractWorkfilesFrontend): Workfiles controller.\n        parent (QtWidgets.QWidget): Parent widget.\n        handle_expected_selection (Optional[bool]): Handle expected selection.\n    \"\"\"\n    log = Logger.get_logger(\"TasksWidget\")\n\n    refreshed = QtCore.Signal()\n    selection_changed = QtCore.Signal()\n\n    def __init__(self, controller, parent, handle_expected_selection=False):\n        super().__init__(parent)\n\n        tasks_view = DeselectableTreeView(self)\n        tasks_view.setIndentation(0)\n\n        tasks_model = TasksQtModel(controller)\n        tasks_proxy_model = TasksProxyModel()\n        tasks_proxy_model.setSourceModel(tasks_model)\n        tasks_proxy_model.setSortCaseSensitivity(QtCore.Qt.CaseInsensitive)\n\n        tasks_view.setModel(tasks_proxy_model)\n\n        main_layout = QtWidgets.QHBoxLayout(self)\n        main_layout.setContentsMargins(0, 0, 0, 0)\n        main_layout.addWidget(tasks_view, 1)\n\n        controller.register_event_callback(\n            \"tasks.refresh.finished\",\n            self._on_tasks_refresh_finished\n        )\n        controller.register_event_callback(\n            \"selection.folder.changed\",\n            self._folder_selection_changed\n        )\n        controller.register_event_callback(\n            \"expected_selection_changed\",\n            self._on_expected_selection_change\n        )\n\n        selection_model = tasks_view.selectionModel()\n        selection_model.selectionChanged.connect(self._on_selection_change)\n\n        tasks_model.refreshed.connect(self._on_tasks_model_refresh)\n        tasks_model.project_changed.connect(self._on_tasks_project_change)\n\n        self._controller = controller\n        self._tasks_view = tasks_view\n        self._tasks_model = tasks_model\n        self._tasks_proxy_model = tasks_proxy_model\n\n        self._selected_folder_id = None\n\n        self._handle_expected_selection = handle_expected_selection\n        self._expected_selection_data = None\n\n    def refresh(self):\n        \"\"\"Refresh folders for last selected project.\n\n        Force to update folders model from controller. This may or may not\n        trigger query from server, that's based on controller's cache.\n        \"\"\"\n        self._tasks_model.refresh()\n\n    def get_selected_task_info(self):\n        \"\"\"Get selected task info.\n\n        Example output::\n\n            {\n                \"task_id\": \"5e7e3e3e3e3e3e3e3e3e3e3e\",\n                \"task_name\": \"modeling\",\n                \"task_type\": \"Modeling\"\n            }\n\n        Returns:\n            dict[str, Union[str, None]]: Task info.\n\n        \"\"\"\n        _, task_id, task_name, task_type = self._get_selected_item_ids()\n        return {\n            \"task_id\": task_id,\n            \"task_name\": task_name,\n            \"task_type\": task_type,\n        }\n\n    def get_selected_task_id(self):\n        \"\"\"Get selected task id.\n\n        Returns:\n            Union[str, None]: Task id.\n\n        \"\"\"\n        return self.get_selected_task_info()[\"task_id\"]\n\n    def get_selected_task_name(self):\n        \"\"\"Get selected task name.\n\n        Returns:\n            Union[str, None]: Task name.\n        \"\"\"\n\n        return self.get_selected_task_info()[\"task_name\"]\n\n    def get_selected_task_type(self):\n        \"\"\"Get selected task type.\n\n        Returns:\n            Union[str, None]: Task type.\n\n        \"\"\"\n        return self.get_selected_task_info()[\"task_type\"]\n\n    def set_selected_task(self, task_name):\n        \"\"\"Set selected task by name.\n\n        Args:\n            task_name (str): Task name.\n\n        Returns:\n            bool: Task was selected.\n\n        \"\"\"\n        if task_name is None:\n            self._tasks_view.clearSelection()\n            return True\n\n        if task_name == self.get_selected_task_name():\n            return True\n        index = self._tasks_model.get_index_by_name(task_name)\n        if not index.isValid():\n            return False\n\n        proxy_index = self._tasks_proxy_model.mapFromSource(index)\n        if not proxy_index.isValid():\n            return False\n\n        selection_model = self._folders_view.selectionModel()\n        selection_model.setCurrentIndex(\n            proxy_index, QtCore.QItemSelectionModel.SelectCurrent\n        )\n        return True\n\n    def set_task_ids_filter(self, task_ids: Optional[list[str]]):\n        \"\"\"Set filter of folder ids.\n\n        Args:\n            task_ids (list[str]): The list of folder ids.\n\n        \"\"\"\n        self._tasks_proxy_model.set_task_ids_filter(task_ids)\n\n    def _on_tasks_refresh_finished(self, event):\n        \"\"\"Tasks were refreshed in controller.\n\n        Ignore if refresh was triggered by tasks model, or refreshed folder is\n        not the same as currently selected folder.\n\n        Args:\n            event (Event): Event object.\n        \"\"\"\n\n        # Refresh only if current folder id is the same\n        if (\n            event[\"sender\"] == TASKS_MODEL_SENDER_NAME\n            or event[\"folder_id\"] != self._selected_folder_id\n        ):\n            return\n\n        self._tasks_model.set_context(\n            event[\"project_name\"], self._selected_folder_id\n        )\n\n    def _folder_selection_changed(self, event):\n        self._selected_folder_id = event[\"folder_id\"]\n        self._tasks_model.set_context(\n            event[\"project_name\"], self._selected_folder_id\n        )\n\n    def _on_tasks_model_refresh(self):\n        if not self._set_expected_selection():\n            self._on_selection_change()\n\n        self._update_task_type_sorting()\n        self.refreshed.emit()\n\n    def _on_tasks_project_change(self):\n        self._update_task_type_sorting()\n\n    def _get_selected_item_ids(self):\n        selection_model = self._tasks_view.selectionModel()\n        for index in selection_model.selectedIndexes():\n            task_id = index.data(ITEM_ID_ROLE)\n            task_name = index.data(ITEM_NAME_ROLE)\n            task_type = index.data(TASK_TYPE_ROLE)\n            parent_id = index.data(PARENT_ID_ROLE)\n            if task_name is not None:\n                return parent_id, task_id, task_name, task_type\n        return self._selected_folder_id, None, None, None\n\n    def _on_selection_change(self):\n        # Don't trigger task change during refresh\n        #   - a task was deselected if that happens\n        #   - can cause crash triggered during tasks refreshing\n        if self._tasks_model.is_refreshing:\n            return\n\n        _parent_id, task_id, task_name, _ = self._get_selected_item_ids()\n        self._controller.set_selected_task(task_id, task_name)\n        self.selection_changed.emit()\n\n    # Expected selection handling\n    def _on_expected_selection_change(self, event):\n        self._update_expected_selection(event.data)\n\n    def _set_expected_selection(self):\n        if not self._handle_expected_selection:\n            return False\n\n        if self._expected_selection_data is None:\n            return False\n        folder_id = self._expected_selection_data[\"folder_id\"]\n        task_name = self._expected_selection_data[\"task_name\"]\n        self._expected_selection_data = None\n        model_folder_id = self._tasks_model.get_last_folder_id()\n        if folder_id != model_folder_id:\n            return False\n        if task_name is not None:\n            index = self._tasks_model.get_index_by_name(task_name)\n            if index.isValid():\n                proxy_index = self._tasks_proxy_model.mapFromSource(index)\n                self._tasks_view.setCurrentIndex(proxy_index)\n        self._controller.expected_task_selected(folder_id, task_name)\n        return True\n\n    def _update_task_type_sorting(self):\n        project_name = self._tasks_model.get_last_project_name()\n        if project_name is None:\n            return\n\n        use_task_type_sorting = False\n        if hasattr(self._controller, \"get_project_settings\"):\n            settings = self._controller.get_project_settings(project_name)\n            use_task_type_sorting = (\n                settings[\"core\"][\"tools\"][\"general\"][\"use_task_type_sorting\"]\n            )\n        else:\n            self.log.warning(\n                f\"Controller '{self._controller}' doesn't have\"\n                \" 'get_project_settings' method, task type\"\n                \" sorting will be disabled.\"\n            )\n\n        self._tasks_proxy_model.set_task_type_sorting_enabled(\n            use_task_type_sorting\n        )\n        self._tasks_proxy_model.sort(0)\n\n    def _update_expected_selection(self, expected_data=None):\n        if not self._handle_expected_selection:\n            return\n        if expected_data is None:\n            expected_data = self._controller.get_expected_selection_data()\n\n        folder_data = expected_data.get(\"folder\")\n        task_data = expected_data.get(\"task\")\n        if (\n            not folder_data\n            or not task_data\n            or not task_data[\"current\"]\n        ):\n            return\n        folder_id = folder_data[\"id\"]\n        self._expected_selection_data = {\n            \"task_name\": task_data[\"name\"],\n            \"folder_id\": folder_id,\n        }\n        model_folder_id = self._tasks_model.get_last_folder_id()\n        if folder_id != model_folder_id or self._tasks_model.is_refreshing:\n            return\n        self._set_expected_selection()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/tasks_widget.html#client.ayon_core.tools.utils.tasks_widget.TasksWidget.get_selected_task_id","title":"<code>get_selected_task_id()</code>","text":"<p>Get selected task id.</p> <p>Returns:</p> Type Description <p>Union[str, None]: Task id.</p> Source code in <code>client/ayon_core/tools/utils/tasks_widget.py</code> <pre><code>def get_selected_task_id(self):\n    \"\"\"Get selected task id.\n\n    Returns:\n        Union[str, None]: Task id.\n\n    \"\"\"\n    return self.get_selected_task_info()[\"task_id\"]\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/tasks_widget.html#client.ayon_core.tools.utils.tasks_widget.TasksWidget.get_selected_task_info","title":"<code>get_selected_task_info()</code>","text":"<p>Get selected task info.</p> <p>Example output::</p> <pre><code>{\n    \"task_id\": \"5e7e3e3e3e3e3e3e3e3e3e3e\",\n    \"task_name\": \"modeling\",\n    \"task_type\": \"Modeling\"\n}\n</code></pre> <p>Returns:</p> Type Description <p>dict[str, Union[str, None]]: Task info.</p> Source code in <code>client/ayon_core/tools/utils/tasks_widget.py</code> <pre><code>def get_selected_task_info(self):\n    \"\"\"Get selected task info.\n\n    Example output::\n\n        {\n            \"task_id\": \"5e7e3e3e3e3e3e3e3e3e3e3e\",\n            \"task_name\": \"modeling\",\n            \"task_type\": \"Modeling\"\n        }\n\n    Returns:\n        dict[str, Union[str, None]]: Task info.\n\n    \"\"\"\n    _, task_id, task_name, task_type = self._get_selected_item_ids()\n    return {\n        \"task_id\": task_id,\n        \"task_name\": task_name,\n        \"task_type\": task_type,\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/tasks_widget.html#client.ayon_core.tools.utils.tasks_widget.TasksWidget.get_selected_task_name","title":"<code>get_selected_task_name()</code>","text":"<p>Get selected task name.</p> <p>Returns:</p> Type Description <p>Union[str, None]: Task name.</p> Source code in <code>client/ayon_core/tools/utils/tasks_widget.py</code> <pre><code>def get_selected_task_name(self):\n    \"\"\"Get selected task name.\n\n    Returns:\n        Union[str, None]: Task name.\n    \"\"\"\n\n    return self.get_selected_task_info()[\"task_name\"]\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/tasks_widget.html#client.ayon_core.tools.utils.tasks_widget.TasksWidget.get_selected_task_type","title":"<code>get_selected_task_type()</code>","text":"<p>Get selected task type.</p> <p>Returns:</p> Type Description <p>Union[str, None]: Task type.</p> Source code in <code>client/ayon_core/tools/utils/tasks_widget.py</code> <pre><code>def get_selected_task_type(self):\n    \"\"\"Get selected task type.\n\n    Returns:\n        Union[str, None]: Task type.\n\n    \"\"\"\n    return self.get_selected_task_info()[\"task_type\"]\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/tasks_widget.html#client.ayon_core.tools.utils.tasks_widget.TasksWidget.refresh","title":"<code>refresh()</code>","text":"<p>Refresh folders for last selected project.</p> <p>Force to update folders model from controller. This may or may not trigger query from server, that's based on controller's cache.</p> Source code in <code>client/ayon_core/tools/utils/tasks_widget.py</code> <pre><code>def refresh(self):\n    \"\"\"Refresh folders for last selected project.\n\n    Force to update folders model from controller. This may or may not\n    trigger query from server, that's based on controller's cache.\n    \"\"\"\n    self._tasks_model.refresh()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/tasks_widget.html#client.ayon_core.tools.utils.tasks_widget.TasksWidget.set_selected_task","title":"<code>set_selected_task(task_name)</code>","text":"<p>Set selected task by name.</p> <p>Parameters:</p> Name Type Description Default <code>task_name</code> <code>str</code> <p>Task name.</p> required <p>Returns:</p> Name Type Description <code>bool</code> <p>Task was selected.</p> Source code in <code>client/ayon_core/tools/utils/tasks_widget.py</code> <pre><code>def set_selected_task(self, task_name):\n    \"\"\"Set selected task by name.\n\n    Args:\n        task_name (str): Task name.\n\n    Returns:\n        bool: Task was selected.\n\n    \"\"\"\n    if task_name is None:\n        self._tasks_view.clearSelection()\n        return True\n\n    if task_name == self.get_selected_task_name():\n        return True\n    index = self._tasks_model.get_index_by_name(task_name)\n    if not index.isValid():\n        return False\n\n    proxy_index = self._tasks_proxy_model.mapFromSource(index)\n    if not proxy_index.isValid():\n        return False\n\n    selection_model = self._folders_view.selectionModel()\n    selection_model.setCurrentIndex(\n        proxy_index, QtCore.QItemSelectionModel.SelectCurrent\n    )\n    return True\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/tasks_widget.html#client.ayon_core.tools.utils.tasks_widget.TasksWidget.set_task_ids_filter","title":"<code>set_task_ids_filter(task_ids)</code>","text":"<p>Set filter of folder ids.</p> <p>Parameters:</p> Name Type Description Default <code>task_ids</code> <code>list[str]</code> <p>The list of folder ids.</p> required Source code in <code>client/ayon_core/tools/utils/tasks_widget.py</code> <pre><code>def set_task_ids_filter(self, task_ids: Optional[list[str]]):\n    \"\"\"Set filter of folder ids.\n\n    Args:\n        task_ids (list[str]): The list of folder ids.\n\n    \"\"\"\n    self._tasks_proxy_model.set_task_ids_filter(task_ids)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/thumbnail_paint_widget.html","title":"thumbnail_paint_widget","text":""},{"location":"autoapi/client/ayon_core/tools/utils/thumbnail_paint_widget.html#client.ayon_core.tools.utils.thumbnail_paint_widget.ThumbnailPainterWidget","title":"<code>ThumbnailPainterWidget</code>","text":"<p>               Bases: <code>QWidget</code></p> <p>Widget for painting of thumbnails.</p> <p>The widget use is to paint thumbnail or multiple thumbnails in a defined area. Is not meant to show them in a grid but in overlay.</p> <p>It is expected that there is a logic that will provide thumbnails to paint and set them using 'set_current_thumbnails' or 'set_current_thumbnail_paths'.</p> Source code in <code>client/ayon_core/tools/utils/thumbnail_paint_widget.py</code> <pre><code>class ThumbnailPainterWidget(QtWidgets.QWidget):\n    \"\"\"Widget for painting of thumbnails.\n\n    The widget use is to paint thumbnail or multiple thumbnails in a defined\n    area. Is not meant to show them in a grid but in overlay.\n\n    It is expected that there is a logic that will provide thumbnails to\n    paint and set them using 'set_current_thumbnails' or\n    'set_current_thumbnail_paths'.\n    \"\"\"\n\n    width_ratio = 3.0\n    height_ratio = 2.0\n    border_width = 1\n    max_thumbnails = 3\n    offset_sep = 4\n    checker_boxes_count = 20\n\n    def __init__(self, parent):\n        super(ThumbnailPainterWidget, self).__init__(parent)\n\n        border_color = get_objected_colors(\"bg-buttons\").get_qcolor()\n        thumbnail_bg_color = get_objected_colors(\"bg-view\").get_qcolor()\n\n        default_image = get_image(\"thumbnail.png\")\n        default_pix = paint_image_with_color(default_image, border_color)\n\n        self._border_color = border_color\n        self._thumbnail_bg_color = thumbnail_bg_color\n        self._default_pix = default_pix\n\n        self._cached_pix = None\n        self._current_pixes = None\n        self._has_pixes = False\n\n        self._bg_color = QtCore.Qt.transparent\n        self._use_checker = True\n        self._checker_color_1 = QtGui.QColor(89, 89, 89)\n        self._checker_color_2 = QtGui.QColor(188, 187, 187)\n\n    def set_background_color(self, color):\n        self._bg_color = color\n        self.repaint()\n\n    def set_use_checkboard(self, use_checker):\n        if self._use_checker is use_checker:\n            return\n        self._use_checker = use_checker\n        self.repaint()\n\n    def set_checker_colors(self, color_1, color_2):\n        self._checker_color_1 = color_1\n        self._checker_color_2 = color_2\n        self.repaint()\n\n    def set_border_color(self, color):\n        \"\"\"Change border color.\n\n        Args:\n            color (QtGui.QColor): Color to set.\n        \"\"\"\n\n        self._border_color = color\n        self._default_pix = None\n        self.clear_cache()\n\n    def set_thumbnail_bg_color(self, color):\n        \"\"\"Change background color.\n\n        Args:\n            color (QtGui.QColor): Color to set.\n        \"\"\"\n\n        self._thumbnail_bg_color = color\n        self.clear_cache()\n\n    @property\n    def has_pixes(self):\n        \"\"\"Has set thumbnails.\n\n        Returns:\n            bool: True if widget has thumbnails to paint.\n        \"\"\"\n\n        return self._has_pixes\n\n    def clear_cache(self):\n        \"\"\"Clear cache of resized thumbnails and repaint widget.\"\"\"\n\n        self._cached_pix = None\n        self.repaint()\n\n    def set_current_thumbnails(self, pixmaps=None):\n        \"\"\"Set current thumbnails.\n\n        Args:\n            pixmaps (Optional[List[QtGui.QPixmap]]): List of pixmaps.\n        \"\"\"\n\n        self._current_pixes = pixmaps or None\n        self._has_pixes = self._current_pixes is not None\n        self.clear_cache()\n\n    def set_current_thumbnail_paths(self, thumbnail_paths=None):\n        \"\"\"Set current thumbnails.\n\n        Set current thumbnails using paths to a files.\n\n        Args:\n            thumbnail_paths (Optional[List[str]]): List of paths to thumbnail\n                sources.\n        \"\"\"\n\n        pixes = []\n        if thumbnail_paths:\n            for thumbnail_path in thumbnail_paths:\n                pixes.append(QtGui.QPixmap(thumbnail_path))\n\n        self.set_current_thumbnails(pixes)\n\n    def paintEvent(self, event):\n        if self._cached_pix is None:\n            self._cache_pix()\n\n        painter = QtGui.QPainter()\n        painter.begin(self)\n        painter.setRenderHint(QtGui.QPainter.Antialiasing)\n        painter.drawPixmap(0, 0, self._cached_pix)\n        painter.end()\n\n    def resizeEvent(self, event):\n        self._cached_pix = None\n        super(ThumbnailPainterWidget, self).resizeEvent(event)\n\n    def _get_default_pix(self):\n        if self._default_pix is None:\n            default_image = get_image(\"thumbnail\")\n            default_pix = paint_image_with_color(\n                default_image, self._border_color)\n            self._default_pix = default_pix\n        return self._default_pix\n\n    def _paint_tile(self, width, height):\n        if not self._use_checker:\n            tile_pix = QtGui.QPixmap(width, width)\n            tile_pix.fill(self._bg_color)\n            return tile_pix\n\n        checker_size = int(float(width) / self.checker_boxes_count)\n        if checker_size &lt; 1:\n            checker_size = 1\n\n        checker_pix = QtGui.QPixmap(checker_size * 2, checker_size * 2)\n        checker_pix.fill(QtCore.Qt.transparent)\n        checker_painter = QtGui.QPainter()\n        checker_painter.begin(checker_pix)\n        checker_painter.setPen(QtCore.Qt.NoPen)\n        checker_painter.setBrush(self._checker_color_1)\n        checker_painter.drawRect(\n            0, 0, checker_pix.width(), checker_pix.height()\n        )\n        checker_painter.setBrush(self._checker_color_2)\n        checker_painter.drawRect(\n            0, 0, checker_size, checker_size\n        )\n        checker_painter.drawRect(\n            checker_size, checker_size, checker_size, checker_size\n        )\n        checker_painter.end()\n        return checker_pix\n\n    def _paint_default_pix(self, pix_width, pix_height):\n        full_border_width = 2 * self.border_width\n        width = pix_width - full_border_width\n        height = pix_height - full_border_width\n        if width &gt; 100:\n            width = int(width * 0.6)\n            height = int(height * 0.6)\n\n        scaled_pix = self._get_default_pix().scaled(\n            width,\n            height,\n            QtCore.Qt.KeepAspectRatio,\n            QtCore.Qt.SmoothTransformation\n        )\n        pos_x = int(\n            (pix_width - scaled_pix.width()) / 2\n        )\n        pos_y = int(\n            (pix_height - scaled_pix.height()) / 2\n        )\n        new_pix = QtGui.QPixmap(pix_width, pix_height)\n        new_pix.fill(QtCore.Qt.transparent)\n        pix_painter = QtGui.QPainter()\n        pix_painter.begin(new_pix)\n        render_hints = (\n            QtGui.QPainter.Antialiasing\n            | QtGui.QPainter.SmoothPixmapTransform\n        )\n\n        pix_painter.setRenderHints(render_hints)\n        pix_painter.drawPixmap(pos_x, pos_y, scaled_pix)\n        pix_painter.end()\n        return new_pix\n\n    def _draw_thumbnails(self, thumbnails, pix_width, pix_height):\n        full_border_width = 2 * self.border_width\n\n        checker_pix = self._paint_tile(pix_width, pix_height)\n\n        backgrounded_images = []\n        for src_pix in thumbnails:\n            scaled_pix = src_pix.scaled(\n                pix_width - full_border_width,\n                pix_height - full_border_width,\n                QtCore.Qt.KeepAspectRatio,\n                QtCore.Qt.SmoothTransformation\n            )\n            pos_x = int(\n                (pix_width - scaled_pix.width()) / 2\n            )\n            pos_y = int(\n                (pix_height - scaled_pix.height()) / 2\n            )\n\n            new_pix = QtGui.QPixmap(pix_width, pix_height)\n            new_pix.fill(QtCore.Qt.transparent)\n            pix_painter = QtGui.QPainter()\n            pix_painter.begin(new_pix)\n            render_hints = (\n                QtGui.QPainter.Antialiasing\n                | QtGui.QPainter.SmoothPixmapTransform\n            )\n            pix_painter.setRenderHints(render_hints)\n\n            tiled_rect = QtCore.QRectF(\n                pos_x, pos_y, scaled_pix.width(), scaled_pix.height()\n            )\n            pix_painter.drawTiledPixmap(\n                tiled_rect,\n                checker_pix,\n                QtCore.QPointF(0.0, 0.0)\n            )\n            pix_painter.drawPixmap(pos_x, pos_y, scaled_pix)\n            pix_painter.end()\n            backgrounded_images.append(new_pix)\n        return backgrounded_images\n\n    def _paint_dash_line(self, painter, rect):\n        pen = QtGui.QPen()\n        pen.setWidth(1)\n        pen.setBrush(QtCore.Qt.darkGray)\n        pen.setStyle(QtCore.Qt.DashLine)\n\n        new_rect = rect.adjusted(1, 1, -1, -1)\n        painter.setPen(pen)\n        painter.setBrush(QtCore.Qt.transparent)\n        # painter.drawRect(rect)\n        painter.drawRect(new_rect)\n\n    def _cache_pix(self):\n        rect = self.rect()\n        rect_width = rect.width()\n        rect_height = rect.height()\n\n        pix_x_offset = 0\n        pix_y_offset = 0\n        expected_height = int(\n            (rect_width / self.width_ratio) * self.height_ratio\n        )\n        if expected_height &gt; rect_height:\n            expected_height = rect_height\n            expected_width = int(\n                (rect_height / self.height_ratio) * self.width_ratio\n            )\n            pix_x_offset = (rect_width - expected_width) / 2\n        else:\n            expected_width = rect_width\n            pix_y_offset = (rect_height - expected_height) / 2\n\n        if self._current_pixes is None:\n            used_default_pix = True\n            pixes_to_draw = None\n            pixes_len = 1\n        else:\n            used_default_pix = False\n            pixes_to_draw = self._current_pixes\n            if len(pixes_to_draw) &gt; self.max_thumbnails:\n                pixes_to_draw = pixes_to_draw[:-self.max_thumbnails]\n            pixes_len = len(pixes_to_draw)\n\n        width_offset, height_offset = self._get_pix_offset_size(\n            expected_width, expected_height, pixes_len\n        )\n        pix_width = expected_width - width_offset\n        pix_height = expected_height - height_offset\n\n        if used_default_pix:\n            thumbnail_images = [self._paint_default_pix(pix_width, pix_height)]\n        else:\n            thumbnail_images = self._draw_thumbnails(\n                pixes_to_draw, pix_width, pix_height\n            )\n\n        if pixes_len == 1:\n            width_offset_part = 0\n            height_offset_part = 0\n        else:\n            width_offset_part = int(float(width_offset) / (pixes_len - 1))\n            height_offset_part = int(float(height_offset) / (pixes_len - 1))\n        full_width_offset = width_offset + pix_x_offset\n\n        final_pix = QtGui.QPixmap(rect_width, rect_height)\n        final_pix.fill(QtCore.Qt.transparent)\n\n        bg_pen = QtGui.QPen()\n        bg_pen.setWidth(self.border_width)\n        bg_pen.setColor(self._border_color)\n\n        final_painter = QtGui.QPainter()\n        final_painter.begin(final_pix)\n        render_hints = (\n            QtGui.QPainter.Antialiasing\n            | QtGui.QPainter.SmoothPixmapTransform\n        )\n\n        final_painter.setRenderHints(render_hints)\n\n        final_painter.setBrush(QtGui.QBrush(self._thumbnail_bg_color))\n        final_painter.setPen(bg_pen)\n        final_painter.drawRect(rect)\n\n        for idx, pix in enumerate(thumbnail_images):\n            x_offset = full_width_offset - (width_offset_part * idx)\n            y_offset = (height_offset_part * idx) + pix_y_offset\n            final_painter.drawPixmap(x_offset, y_offset, pix)\n\n        # Draw drop enabled dashes\n        if used_default_pix:\n            self._paint_dash_line(final_painter, rect)\n\n        final_painter.end()\n\n        self._cached_pix = final_pix\n\n    def _get_pix_offset_size(self, width, height, image_count):\n        if image_count == 1:\n            return 0, 0\n\n        part_width = width / self.offset_sep\n        part_height = height / self.offset_sep\n        return part_width, part_height\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/thumbnail_paint_widget.html#client.ayon_core.tools.utils.thumbnail_paint_widget.ThumbnailPainterWidget.has_pixes","title":"<code>has_pixes</code>  <code>property</code>","text":"<p>Has set thumbnails.</p> <p>Returns:</p> Name Type Description <code>bool</code> <p>True if widget has thumbnails to paint.</p>"},{"location":"autoapi/client/ayon_core/tools/utils/thumbnail_paint_widget.html#client.ayon_core.tools.utils.thumbnail_paint_widget.ThumbnailPainterWidget.clear_cache","title":"<code>clear_cache()</code>","text":"<p>Clear cache of resized thumbnails and repaint widget.</p> Source code in <code>client/ayon_core/tools/utils/thumbnail_paint_widget.py</code> <pre><code>def clear_cache(self):\n    \"\"\"Clear cache of resized thumbnails and repaint widget.\"\"\"\n\n    self._cached_pix = None\n    self.repaint()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/thumbnail_paint_widget.html#client.ayon_core.tools.utils.thumbnail_paint_widget.ThumbnailPainterWidget.set_border_color","title":"<code>set_border_color(color)</code>","text":"<p>Change border color.</p> <p>Parameters:</p> Name Type Description Default <code>color</code> <code>QColor</code> <p>Color to set.</p> required Source code in <code>client/ayon_core/tools/utils/thumbnail_paint_widget.py</code> <pre><code>def set_border_color(self, color):\n    \"\"\"Change border color.\n\n    Args:\n        color (QtGui.QColor): Color to set.\n    \"\"\"\n\n    self._border_color = color\n    self._default_pix = None\n    self.clear_cache()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/thumbnail_paint_widget.html#client.ayon_core.tools.utils.thumbnail_paint_widget.ThumbnailPainterWidget.set_current_thumbnail_paths","title":"<code>set_current_thumbnail_paths(thumbnail_paths=None)</code>","text":"<p>Set current thumbnails.</p> <p>Set current thumbnails using paths to a files.</p> <p>Parameters:</p> Name Type Description Default <code>thumbnail_paths</code> <code>Optional[List[str]]</code> <p>List of paths to thumbnail sources.</p> <code>None</code> Source code in <code>client/ayon_core/tools/utils/thumbnail_paint_widget.py</code> <pre><code>def set_current_thumbnail_paths(self, thumbnail_paths=None):\n    \"\"\"Set current thumbnails.\n\n    Set current thumbnails using paths to a files.\n\n    Args:\n        thumbnail_paths (Optional[List[str]]): List of paths to thumbnail\n            sources.\n    \"\"\"\n\n    pixes = []\n    if thumbnail_paths:\n        for thumbnail_path in thumbnail_paths:\n            pixes.append(QtGui.QPixmap(thumbnail_path))\n\n    self.set_current_thumbnails(pixes)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/thumbnail_paint_widget.html#client.ayon_core.tools.utils.thumbnail_paint_widget.ThumbnailPainterWidget.set_current_thumbnails","title":"<code>set_current_thumbnails(pixmaps=None)</code>","text":"<p>Set current thumbnails.</p> <p>Parameters:</p> Name Type Description Default <code>pixmaps</code> <code>Optional[List[QPixmap]]</code> <p>List of pixmaps.</p> <code>None</code> Source code in <code>client/ayon_core/tools/utils/thumbnail_paint_widget.py</code> <pre><code>def set_current_thumbnails(self, pixmaps=None):\n    \"\"\"Set current thumbnails.\n\n    Args:\n        pixmaps (Optional[List[QtGui.QPixmap]]): List of pixmaps.\n    \"\"\"\n\n    self._current_pixes = pixmaps or None\n    self._has_pixes = self._current_pixes is not None\n    self.clear_cache()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/thumbnail_paint_widget.html#client.ayon_core.tools.utils.thumbnail_paint_widget.ThumbnailPainterWidget.set_thumbnail_bg_color","title":"<code>set_thumbnail_bg_color(color)</code>","text":"<p>Change background color.</p> <p>Parameters:</p> Name Type Description Default <code>color</code> <code>QColor</code> <p>Color to set.</p> required Source code in <code>client/ayon_core/tools/utils/thumbnail_paint_widget.py</code> <pre><code>def set_thumbnail_bg_color(self, color):\n    \"\"\"Change background color.\n\n    Args:\n        color (QtGui.QColor): Color to set.\n    \"\"\"\n\n    self._thumbnail_bg_color = color\n    self.clear_cache()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/views.html","title":"views","text":""},{"location":"autoapi/client/ayon_core/tools/utils/views.html#client.ayon_core.tools.utils.views.DeselectableTreeView","title":"<code>DeselectableTreeView</code>","text":"<p>               Bases: <code>QTreeView</code></p> <p>A tree view that deselects on clicking on an empty area in the view</p> Source code in <code>client/ayon_core/tools/utils/views.py</code> <pre><code>class DeselectableTreeView(QtWidgets.QTreeView):\n    \"\"\"A tree view that deselects on clicking on an empty area in the view\"\"\"\n\n    def __init__(self, *args, **kwargs):\n        super().__init__(*args, **kwargs)\n        self.setIndentation(20)\n\n    def mousePressEvent(self, event):\n        index = self.indexAt(event.pos())\n        if not index.isValid():\n            # clear the selection\n            self.clearSelection()\n            # clear the current index\n            self.setCurrentIndex(QtCore.QModelIndex())\n\n        elif (\n            self.selectionModel().isSelected(index)\n            and len(self.selectionModel().selectedRows()) == 1\n            and event.modifiers() == QtCore.Qt.NoModifier\n        ):\n            event.setModifiers(QtCore.Qt.ControlModifier)\n\n        super().mousePressEvent(event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/views.html#client.ayon_core.tools.utils.views.ListView","title":"<code>ListView</code>","text":"<p>               Bases: <code>QListView</code></p> <p>A tree view that deselects on clicking on an empty area in the view</p> Source code in <code>client/ayon_core/tools/utils/views.py</code> <pre><code>class ListView(QtWidgets.QListView):\n    \"\"\"A tree view that deselects on clicking on an empty area in the view\"\"\"\n    double_clicked = QtCore.Signal(QtGui.QMouseEvent)\n\n    def __init__(self, *args, **kwargs):\n        super().__init__(*args, **kwargs)\n        self._deselectable = False\n\n        self._flick_charm_activated = False\n        self._flick_charm = FlickCharm(parent=self)\n        self._before_flick_scroll_mode = None\n\n    def is_deselectable(self):\n        return self._deselectable\n\n    def set_deselectable(self, deselectable):\n        self._deselectable = deselectable\n\n    deselectable = property(is_deselectable, set_deselectable)\n\n    def mousePressEvent(self, event):\n        if self._deselectable:\n            index = self.indexAt(event.pos())\n            if not index.isValid():\n                # clear the selection\n                self.clearSelection()\n                # clear the current index\n                self.setCurrentIndex(QtCore.QModelIndex())\n        super().mousePressEvent(event)\n\n    def mouseDoubleClickEvent(self, event):\n        self.double_clicked.emit(event)\n\n        return super().mouseDoubleClickEvent(event)\n\n    def activate_flick_charm(self):\n        if self._flick_charm_activated:\n            return\n        self._flick_charm_activated = True\n        self._before_flick_scroll_mode = self.verticalScrollMode()\n        self._flick_charm.activateOn(self)\n        self.setVerticalScrollMode(QtWidgets.QAbstractItemView.ScrollPerPixel)\n\n    def deactivate_flick_charm(self):\n        if not self._flick_charm_activated:\n            return\n        self._flick_charm_activated = False\n        self._flick_charm.deactivateFrom(self)\n        if self._before_flick_scroll_mode is not None:\n            self.setVerticalScrollMode(self._before_flick_scroll_mode)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/views.html#client.ayon_core.tools.utils.views.TreeView","title":"<code>TreeView</code>","text":"<p>               Bases: <code>QTreeView</code></p> <p>Ultimate TreeView with flick charm and double click signals.</p> <p>Tree view have deselectable mode, which allows to deselect items by clicking on item area without any items.</p> Todos <p>Add refresh animation.</p> Source code in <code>client/ayon_core/tools/utils/views.py</code> <pre><code>class TreeView(QtWidgets.QTreeView):\n    \"\"\"Ultimate TreeView with flick charm and double click signals.\n\n    Tree view have deselectable mode, which allows to deselect items by\n    clicking on item area without any items.\n\n    Todos:\n        Add refresh animation.\n    \"\"\"\n\n    double_clicked = QtCore.Signal(QtGui.QMouseEvent)\n\n    def __init__(self, *args, **kwargs):\n        super().__init__(*args, **kwargs)\n        self.setIndentation(20)\n\n        self._deselectable = False\n\n        self._flick_charm_activated = False\n        self._flick_charm = FlickCharm(parent=self)\n        self._before_flick_scroll_mode = None\n\n    def is_deselectable(self):\n        return self._deselectable\n\n    def set_deselectable(self, deselectable):\n        self._deselectable = deselectable\n\n    deselectable = property(is_deselectable, set_deselectable)\n\n    def mousePressEvent(self, event):\n        if self._deselectable:\n            index = self.indexAt(event.pos())\n            if not index.isValid():\n                # clear the selection\n                self.clearSelection()\n                # clear the current index\n                self.setCurrentIndex(QtCore.QModelIndex())\n        super().mousePressEvent(event)\n\n    def mouseDoubleClickEvent(self, event):\n        self.double_clicked.emit(event)\n\n        return super().mouseDoubleClickEvent(event)\n\n    def activate_flick_charm(self):\n        if self._flick_charm_activated:\n            return\n        self._flick_charm_activated = True\n        self._before_flick_scroll_mode = self.verticalScrollMode()\n        self._flick_charm.activateOn(self)\n        self.setVerticalScrollMode(QtWidgets.QAbstractItemView.ScrollPerPixel)\n\n    def deactivate_flick_charm(self):\n        if not self._flick_charm_activated:\n            return\n        self._flick_charm_activated = False\n        self._flick_charm.deactivateFrom(self)\n        if self._before_flick_scroll_mode is not None:\n            self.setVerticalScrollMode(self._before_flick_scroll_mode)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/widgets.html","title":"widgets","text":""},{"location":"autoapi/client/ayon_core/tools/utils/widgets.html#client.ayon_core.tools.utils.widgets.BaseClickableFrame","title":"<code>BaseClickableFrame</code>","text":"<p>               Bases: <code>QFrame</code></p> <p>Widget that catch left mouse click and can trigger a callback.</p> <p>Callback is defined by overriding <code>_mouse_release_callback</code>.</p> Source code in <code>client/ayon_core/tools/utils/widgets.py</code> <pre><code>class BaseClickableFrame(QtWidgets.QFrame):\n    \"\"\"Widget that catch left mouse click and can trigger a callback.\n\n    Callback is defined by overriding `_mouse_release_callback`.\n    \"\"\"\n    def __init__(self, parent):\n        super().__init__(parent)\n\n        self._mouse_pressed = False\n\n    def _mouse_release_callback(self):\n        pass\n\n    def mousePressEvent(self, event):\n        super().mousePressEvent(event)\n        if event.isAccepted():\n            return\n        if event.button() == QtCore.Qt.LeftButton:\n            self._mouse_pressed = True\n            event.accept()\n\n    def mouseReleaseEvent(self, event):\n        pressed, self._mouse_pressed = self._mouse_pressed, False\n        super().mouseReleaseEvent(event)\n        if event.isAccepted():\n            return\n\n        accepted = pressed and self.rect().contains(event.pos())\n        if accepted:\n            event.accept()\n            self._mouse_release_callback()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/widgets.html#client.ayon_core.tools.utils.widgets.ClassicExpandBtn","title":"<code>ClassicExpandBtn</code>","text":"<p>               Bases: <code>ExpandBtn</code></p> <p>Same as 'ExpandBtn' but with arrow images.</p> Source code in <code>client/ayon_core/tools/utils/widgets.py</code> <pre><code>class ClassicExpandBtn(ExpandBtn):\n    \"\"\"Same as 'ExpandBtn' but with arrow images.\"\"\"\n\n    def _create_pix_widget(self, parent=None):\n        if parent is None:\n            parent = self\n        return ClassicExpandBtnLabel(parent)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/widgets.html#client.ayon_core.tools.utils.widgets.ClickableFrame","title":"<code>ClickableFrame</code>","text":"<p>               Bases: <code>BaseClickableFrame</code></p> <p>Extended clickable frame which triggers 'clicked' signal.</p> Source code in <code>client/ayon_core/tools/utils/widgets.py</code> <pre><code>class ClickableFrame(BaseClickableFrame):\n    \"\"\"Extended clickable frame which triggers 'clicked' signal.\"\"\"\n    clicked = QtCore.Signal()\n\n    def _mouse_release_callback(self):\n        self.clicked.emit()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/widgets.html#client.ayon_core.tools.utils.widgets.ClickableLabel","title":"<code>ClickableLabel</code>","text":"<p>               Bases: <code>QLabel</code></p> <p>Label that catch left mouse click and can trigger 'clicked' signal.</p> Source code in <code>client/ayon_core/tools/utils/widgets.py</code> <pre><code>class ClickableLabel(QtWidgets.QLabel):\n    \"\"\"Label that catch left mouse click and can trigger 'clicked' signal.\"\"\"\n    clicked = QtCore.Signal()\n\n    def __init__(self, *args, **kwargs):\n        super().__init__(*args, **kwargs)\n\n        self._mouse_pressed = False\n\n    def mousePressEvent(self, event):\n        if event.button() == QtCore.Qt.LeftButton:\n            self._mouse_pressed = True\n        super().mousePressEvent(event)\n\n    def mouseReleaseEvent(self, event):\n        if self._mouse_pressed:\n            self._mouse_pressed = False\n            if self.rect().contains(event.pos()):\n                self.clicked.emit()\n\n        super().mouseReleaseEvent(event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/widgets.html#client.ayon_core.tools.utils.widgets.ComboBox","title":"<code>ComboBox</code>","text":"<p>               Bases: <code>QComboBox</code></p> <p>Base of combobox with pre-implement changes used in tools.</p> <p>Combobox is using styled delegate by default so stylesheets are propagated.</p> <p>Items are not changed on scroll until the combobox is in focus.</p> Source code in <code>client/ayon_core/tools/utils/widgets.py</code> <pre><code>class ComboBox(QtWidgets.QComboBox):\n    \"\"\"Base of combobox with pre-implement changes used in tools.\n\n    Combobox is using styled delegate by default so stylesheets are propagated.\n\n    Items are not changed on scroll until the combobox is in focus.\n    \"\"\"\n\n    def __init__(self, *args, **kwargs):\n        super().__init__(*args, **kwargs)\n        delegate = QtWidgets.QStyledItemDelegate()\n        self.setItemDelegate(delegate)\n        self.setFocusPolicy(QtCore.Qt.StrongFocus)\n\n        self._delegate = delegate\n\n    def wheelEvent(self, event):\n        if self.hasFocus():\n            return super().wheelEvent(event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/widgets.html#client.ayon_core.tools.utils.widgets.CustomTextComboBox","title":"<code>CustomTextComboBox</code>","text":"<p>               Bases: <code>ComboBox</code></p> <p>Combobox which can have different text showed.</p> Source code in <code>client/ayon_core/tools/utils/widgets.py</code> <pre><code>class CustomTextComboBox(ComboBox):\n    \"\"\"Combobox which can have different text showed.\"\"\"\n\n    def __init__(self, *args, **kwargs):\n        self._custom_text = None\n        super().__init__(*args, **kwargs)\n\n    def set_custom_text(self, text=None):\n        if self._custom_text != text:\n            self._custom_text = text\n            self.repaint()\n\n    def paintEvent(self, event):\n        painter = QtWidgets.QStylePainter(self)\n        option = QtWidgets.QStyleOptionComboBox()\n        self.initStyleOption(option)\n        if self._custom_text is not None:\n            option.currentText = self._custom_text\n        painter.drawComplexControl(QtWidgets.QStyle.CC_ComboBox, option)\n        painter.drawControl(QtWidgets.QStyle.CE_ComboBoxLabel, option)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/widgets.html#client.ayon_core.tools.utils.widgets.ElideLabel","title":"<code>ElideLabel</code>","text":"<p>               Bases: <code>QLabel</code></p> <p>Label which elide text.</p> <p>By default, elide happens on right side. Can be changed with 'set_elide_mode' method.</p> <p>It is not possible to use other features of QLabel like word wrap or interactive text. This is a simple label which elide text.</p> Source code in <code>client/ayon_core/tools/utils/widgets.py</code> <pre><code>class ElideLabel(QtWidgets.QLabel):\n    \"\"\"Label which elide text.\n\n    By default, elide happens on right side. Can be changed with\n    'set_elide_mode' method.\n\n    It is not possible to use other features of QLabel like word wrap or\n    interactive text. This is a simple label which elide text.\n    \"\"\"\n    def __init__(self, *args, **kwargs):\n        super().__init__(*args, **kwargs)\n        self.setSizePolicy(\n            QtWidgets.QSizePolicy.Expanding,\n            QtWidgets.QSizePolicy.Preferred\n        )\n        # Store text set during init\n        self._text = self.text()\n        # Define initial elide mode\n        self._elide_mode = QtCore.Qt.ElideRight\n        # Make sure that text of QLabel is empty\n        super().setText(\"\")\n\n    def setText(self, text):\n        # Update private text attribute and force update\n        self._text = text\n        self.update()\n\n    def setWordWrap(self, word_wrap):\n        # Word wrap is not supported in 'ElideLabel'\n        if word_wrap:\n            raise ValueError(\"Word wrap is not supported in 'ElideLabel'.\")\n\n    def contextMenuEvent(self, event):\n        menu = self.create_context_menu(event.pos())\n        if menu is None:\n            event.ignore()\n            return\n        event.accept()\n        menu.setAttribute(QtCore.Qt.WA_DeleteOnClose)\n        menu.popup(event.globalPos())\n\n    def create_context_menu(self, pos):\n        if not self._text:\n            return None\n        menu = QtWidgets.QMenu(self)\n\n        # Copy text action\n        copy_action = menu.addAction(\"Copy\")\n        copy_action.setObjectName(\"edit-copy\")\n        icon = QtGui.QIcon.fromTheme(\"edit-copy\")\n        if not icon.isNull():\n            copy_action.setIcon(icon)\n\n        copy_action.triggered.connect(self._on_copy_text)\n        return menu\n\n    def set_set(self, text):\n        self.setText(text)\n\n    def set_elide_mode(self, elide_mode):\n        \"\"\"Change elide type.\n\n        Args:\n            elide_mode: Possible elide type. Available in 'QtCore.Qt'\n                'ElideLeft', 'ElideRight' and 'ElideMiddle'.\n\n        \"\"\"\n        if elide_mode == QtCore.Qt.ElideNone:\n            raise ValueError(\n                \"Invalid elide type. 'ElideNone' is not supported.\"\n            )\n\n        if elide_mode not in (\n            QtCore.Qt.ElideLeft,\n            QtCore.Qt.ElideRight,\n            QtCore.Qt.ElideMiddle,\n        ):\n            raise ValueError(f\"Unknown value '{elide_mode}'\")\n        self._elide_mode = elide_mode\n        self.update()\n\n    def paintEvent(self, event):\n        super().paintEvent(event)\n\n        painter = QtGui.QPainter(self)\n        fm = painter.fontMetrics()\n        elided_line = fm.elidedText(\n            self._text, self._elide_mode, self.width()\n        )\n        painter.drawText(QtCore.QPoint(0, fm.ascent()), elided_line)\n\n    def _on_copy_text(self):\n        clipboard = QtWidgets.QApplication.clipboard()\n        clipboard.setText(self._text)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/widgets.html#client.ayon_core.tools.utils.widgets.ElideLabel.set_elide_mode","title":"<code>set_elide_mode(elide_mode)</code>","text":"<p>Change elide type.</p> <p>Parameters:</p> Name Type Description Default <code>elide_mode</code> <p>Possible elide type. Available in 'QtCore.Qt' 'ElideLeft', 'ElideRight' and 'ElideMiddle'.</p> required Source code in <code>client/ayon_core/tools/utils/widgets.py</code> <pre><code>def set_elide_mode(self, elide_mode):\n    \"\"\"Change elide type.\n\n    Args:\n        elide_mode: Possible elide type. Available in 'QtCore.Qt'\n            'ElideLeft', 'ElideRight' and 'ElideMiddle'.\n\n    \"\"\"\n    if elide_mode == QtCore.Qt.ElideNone:\n        raise ValueError(\n            \"Invalid elide type. 'ElideNone' is not supported.\"\n        )\n\n    if elide_mode not in (\n        QtCore.Qt.ElideLeft,\n        QtCore.Qt.ElideRight,\n        QtCore.Qt.ElideMiddle,\n    ):\n        raise ValueError(f\"Unknown value '{elide_mode}'\")\n    self._elide_mode = elide_mode\n    self.update()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/widgets.html#client.ayon_core.tools.utils.widgets.ExpandBtnLabel","title":"<code>ExpandBtnLabel</code>","text":"<p>               Bases: <code>QLabel</code></p> <p>Label showing expand icon meant for ExpandBtn.</p> Source code in <code>client/ayon_core/tools/utils/widgets.py</code> <pre><code>class ExpandBtnLabel(QtWidgets.QLabel):\n    \"\"\"Label showing expand icon meant for ExpandBtn.\"\"\"\n    state_changed = QtCore.Signal()\n\n    branch_closed_path = get_style_image_path(\"branch_closed\")\n    branch_open_path = get_style_image_path(\"branch_open\")\n\n    def __init__(self, parent):\n        super(ExpandBtnLabel, self).__init__(parent)\n        self._source_collapsed_pix = self._create_collapsed_pixmap()\n        self._source_expanded_pix = self._create_expanded_pixmap()\n\n        self._current_image = self._source_collapsed_pix\n        self._collapsed = True\n\n    def _create_collapsed_pixmap(self):\n        return QtGui.QPixmap(self.branch_closed_path)\n\n    def _create_expanded_pixmap(self):\n        return QtGui.QPixmap(self.branch_open_path)\n\n    @property\n    def collapsed(self):\n        return self._collapsed\n\n    def set_collapsed(self, collapsed=None):\n        if collapsed is None:\n            collapsed = not self._collapsed\n        if self._collapsed == collapsed:\n            return\n        self._collapsed = collapsed\n        if collapsed:\n            self._current_image = self._source_collapsed_pix\n        else:\n            self._current_image = self._source_expanded_pix\n        self._set_resized_pix()\n        self.state_changed.emit()\n\n    def resizeEvent(self, event):\n        self._set_resized_pix()\n        super(ExpandBtnLabel, self).resizeEvent(event)\n\n    def _set_resized_pix(self):\n        size = int(self.fontMetrics().height() / 2)\n        if size &lt; 1:\n            size = 1\n        size += size % 2\n        self.setPixmap(\n            self._current_image.scaled(\n                size,\n                size,\n                QtCore.Qt.KeepAspectRatio,\n                QtCore.Qt.SmoothTransformation\n            )\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/widgets.html#client.ayon_core.tools.utils.widgets.ExpandingTextEdit","title":"<code>ExpandingTextEdit</code>","text":"<p>               Bases: <code>QTextEdit</code></p> <p>QTextEdit which does not have sroll area but expands height.</p> Source code in <code>client/ayon_core/tools/utils/widgets.py</code> <pre><code>class ExpandingTextEdit(QtWidgets.QTextEdit):\n    \"\"\"QTextEdit which does not have sroll area but expands height.\"\"\"\n\n    def __init__(self, parent=None):\n        super().__init__(parent)\n\n        size_policy = self.sizePolicy()\n        size_policy.setHeightForWidth(True)\n        size_policy.setVerticalPolicy(QtWidgets.QSizePolicy.Preferred)\n        self.setSizePolicy(size_policy)\n\n        self.setHorizontalScrollBarPolicy(QtCore.Qt.ScrollBarAlwaysOff)\n        self.setVerticalScrollBarPolicy(QtCore.Qt.ScrollBarAlwaysOff)\n\n        doc = self.document()\n        doc.contentsChanged.connect(self._on_doc_change)\n\n    def _on_doc_change(self):\n        self.updateGeometry()\n\n    def hasHeightForWidth(self):\n        return True\n\n    def heightForWidth(self, width):\n        margins = self.contentsMargins()\n\n        document_width = 0\n        margins_size = margins.left() + margins.right()\n        if width &gt;= margins_size:\n            document_width = width - margins_size\n\n        document = self.document().clone()\n        document.setTextWidth(document_width)\n\n        return math.ceil(\n            margins.top()\n            + document.size().height()\n            + margins.bottom()\n            + 2\n        )\n\n    def sizeHint(self):\n        width = super().sizeHint().width()\n        return QtCore.QSize(width, self.heightForWidth(width))\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/widgets.html#client.ayon_core.tools.utils.widgets.FocusDoubleSpinBox","title":"<code>FocusDoubleSpinBox</code>","text":"<p>               Bases: <code>QDoubleSpinBox</code></p> <p>QDoubleSpinBox which allow scroll wheel changes only in active state.</p> Source code in <code>client/ayon_core/tools/utils/widgets.py</code> <pre><code>class FocusDoubleSpinBox(QtWidgets.QDoubleSpinBox):\n    \"\"\"QDoubleSpinBox which allow scroll wheel changes only in active state.\"\"\"\n\n    def __init__(self, *args, **kwargs):\n        super(FocusDoubleSpinBox, self).__init__(*args, **kwargs)\n        self.setFocusPolicy(QtCore.Qt.StrongFocus)\n\n    def wheelEvent(self, event):\n        if not self.hasFocus():\n            event.ignore()\n        else:\n            super(FocusDoubleSpinBox, self).wheelEvent(event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/widgets.html#client.ayon_core.tools.utils.widgets.FocusSpinBox","title":"<code>FocusSpinBox</code>","text":"<p>               Bases: <code>QSpinBox</code></p> <p>QSpinBox which allow scroll wheel changes only in active state.</p> Source code in <code>client/ayon_core/tools/utils/widgets.py</code> <pre><code>class FocusSpinBox(QtWidgets.QSpinBox):\n    \"\"\"QSpinBox which allow scroll wheel changes only in active state.\"\"\"\n\n    def __init__(self, *args, **kwargs):\n        super(FocusSpinBox, self).__init__(*args, **kwargs)\n        self.setFocusPolicy(QtCore.Qt.StrongFocus)\n\n    def wheelEvent(self, event):\n        if not self.hasFocus():\n            event.ignore()\n        else:\n            super(FocusSpinBox, self).wheelEvent(event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/widgets.html#client.ayon_core.tools.utils.widgets.IconButton","title":"<code>IconButton</code>","text":"<p>               Bases: <code>QPushButton</code></p> <p>PushButton with icon and size of font.</p> <p>Using font metrics height as icon size reference.</p> Source code in <code>client/ayon_core/tools/utils/widgets.py</code> <pre><code>class IconButton(QtWidgets.QPushButton):\n    \"\"\"PushButton with icon and size of font.\n\n    Using font metrics height as icon size reference.\n    \"\"\"\n\n    def __init__(self, *args, **kwargs):\n        super(IconButton, self).__init__(*args, **kwargs)\n        self.setObjectName(\"IconButton\")\n\n    def sizeHint(self):\n        result = super(IconButton, self).sizeHint()\n        icon_h = self.iconSize().height()\n        font_height = self.fontMetrics().height()\n        text_set = bool(self.text())\n        if not text_set and icon_h &lt; font_height:\n            new_size = result.height() - icon_h + font_height\n            result.setHeight(new_size)\n            result.setWidth(new_size)\n\n        return result\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/widgets.html#client.ayon_core.tools.utils.widgets.ImageButton","title":"<code>ImageButton</code>","text":"<p>               Bases: <code>QPushButton</code></p> <p>PushButton with icon and size of font.</p> <p>Using font metrics height as icon size reference.</p> <p>TODO: - handle changes of screen (different resolution)</p> Source code in <code>client/ayon_core/tools/utils/widgets.py</code> <pre><code>class ImageButton(QtWidgets.QPushButton):\n    \"\"\"PushButton with icon and size of font.\n\n    Using font metrics height as icon size reference.\n\n    TODO:\n    - handle changes of screen (different resolution)\n    \"\"\"\n\n    def __init__(self, *args, **kwargs):\n        super(ImageButton, self).__init__(*args, **kwargs)\n        self.setObjectName(\"ImageButton\")\n\n    def _change_size(self):\n        font_height = self.fontMetrics().height()\n        self.setIconSize(QtCore.QSize(font_height, font_height))\n\n    def showEvent(self, event):\n        super(ImageButton, self).showEvent(event)\n\n        self._change_size()\n\n    def sizeHint(self):\n        return self.iconSize()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/widgets.html#client.ayon_core.tools.utils.widgets.OptionBox","title":"<code>OptionBox</code>","text":"<p>               Bases: <code>QLabel</code></p> <p>Option box widget class for <code>OptionalActionWidget</code></p> Source code in <code>client/ayon_core/tools/utils/widgets.py</code> <pre><code>class OptionBox(QtWidgets.QLabel):\n    \"\"\"Option box widget class for `OptionalActionWidget`\"\"\"\n\n    clicked = QtCore.Signal()\n\n    def __init__(self, parent):\n        super(OptionBox, self).__init__(parent)\n\n        self.setAlignment(QtCore.Qt.AlignCenter)\n\n        icon = qtawesome.icon(\"fa.sticky-note-o\", color=\"#c6c6c6\")\n        pixmap = icon.pixmap(18, 18)\n        self.setPixmap(pixmap)\n\n    def is_hovered(self, global_pos):\n        if global_pos is None:\n            return False\n        pos = self.mapFromGlobal(global_pos)\n        return self.rect().contains(pos)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/widgets.html#client.ayon_core.tools.utils.widgets.OptionDialog","title":"<code>OptionDialog</code>","text":"<p>               Bases: <code>QDialog</code></p> <p>Option dialog shown by option box</p> Source code in <code>client/ayon_core/tools/utils/widgets.py</code> <pre><code>class OptionDialog(QtWidgets.QDialog):\n    \"\"\"Option dialog shown by option box\"\"\"\n\n    def __init__(self, parent=None):\n        super(OptionDialog, self).__init__(parent)\n        self.setModal(True)\n        self._options = dict()\n\n    def create(self, options):\n        parser = qargparse.QArgumentParser(arguments=options)\n\n        decision_widget = QtWidgets.QWidget(self)\n        accept_btn = QtWidgets.QPushButton(\"Accept\", decision_widget)\n        cancel_btn = QtWidgets.QPushButton(\"Cancel\", decision_widget)\n\n        decision_layout = QtWidgets.QHBoxLayout(decision_widget)\n        decision_layout.addWidget(accept_btn)\n        decision_layout.addWidget(cancel_btn)\n\n        layout = QtWidgets.QVBoxLayout(self)\n        layout.addWidget(parser)\n        layout.addWidget(decision_widget)\n\n        accept_btn.clicked.connect(self.accept)\n        cancel_btn.clicked.connect(self.reject)\n        parser.changed.connect(self.on_changed)\n\n    def on_changed(self, argument):\n        self._options[argument[\"name\"]] = argument.read()\n\n    def parse(self):\n        return self._options.copy()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/widgets.html#client.ayon_core.tools.utils.widgets.OptionalAction","title":"<code>OptionalAction</code>","text":"<p>               Bases: <code>QWidgetAction</code></p> <p>Menu action with option box</p> <p>A menu action like Maya's menu item with option box, implemented by subclassing <code>QtWidgets.QWidgetAction</code>.</p> Source code in <code>client/ayon_core/tools/utils/widgets.py</code> <pre><code>class OptionalAction(QtWidgets.QWidgetAction):\n    \"\"\"Menu action with option box\n\n    A menu action like Maya's menu item with option box, implemented by\n    subclassing `QtWidgets.QWidgetAction`.\n\n    \"\"\"\n\n    def __init__(self, label, icon, use_option, parent):\n        super().__init__(parent)\n        self.label = label\n        self.icon = icon\n        self.use_option = use_option\n        self.option_tip = \"\"\n        self.optioned = False\n        self.widget = None\n\n    def createWidget(self, parent):\n        widget = OptionalActionWidget(self.label, parent)\n        self.widget = widget\n\n        if self.icon:\n            widget.setIcon(self.icon)\n\n        if self.use_option:\n            widget.option.clicked.connect(self.on_option)\n            widget.option.setToolTip(self.option_tip)\n        else:\n            widget.option.setVisible(False)\n\n        return widget\n\n    def set_option_tip(self, options):\n        sep = \"\\n\\n\"\n        if not options or not isinstance(options[0], AbstractAttrDef):\n            def mak(opt):\n                return opt[\"name\"] + \" :\\n    \" + opt[\"help\"]\n            self.option_tip = sep.join(mak(opt) for opt in options)\n            return\n\n        option_items = []\n        for option in options:\n            option_lines = []\n            if option.label:\n                option_lines.append(\n                    \"{} ({}) :\".format(option.label, option.key)\n                )\n            else:\n                option_lines.append(\"{} :\".format(option.key))\n\n            if option.tooltip:\n                option_lines.append(\"    - {}\".format(option.tooltip))\n            option_items.append(\"\\n\".join(option_lines))\n\n        self.option_tip = sep.join(option_items)\n\n    def on_option(self):\n        self.optioned = True\n\n    def set_highlight(self, state, global_pos=None):\n        option_state = False\n        if self.use_option:\n            option_state = self.widget.option.is_hovered(global_pos)\n        self.widget.set_hover_properties(state, option_state)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/widgets.html#client.ayon_core.tools.utils.widgets.OptionalActionWidget","title":"<code>OptionalActionWidget</code>","text":"<p>               Bases: <code>QWidget</code></p> <p>Main widget class for <code>OptionalAction</code></p> Source code in <code>client/ayon_core/tools/utils/widgets.py</code> <pre><code>class OptionalActionWidget(QtWidgets.QWidget):\n    \"\"\"Main widget class for `OptionalAction`\"\"\"\n\n    def __init__(self, label, parent=None):\n        super().__init__(parent)\n\n        body_widget = QtWidgets.QWidget(self)\n        body_widget.setObjectName(\"OptionalActionBody\")\n\n        icon = QtWidgets.QLabel(body_widget)\n        label = QtWidgets.QLabel(label, body_widget)\n        # (NOTE) For removing ugly QLable shadow FX when highlighted in Nuke.\n        #   See https://stackoverflow.com/q/52838690/4145300\n        label.setStyle(QtWidgets.QStyleFactory.create(\"Plastique\"))\n        option = OptionBox(body_widget)\n        option.setObjectName(\"OptionalActionOption\")\n\n        icon.setFixedSize(24, 16)\n        option.setFixedSize(30, 30)\n\n        body_layout = QtWidgets.QHBoxLayout(body_widget)\n        body_layout.setContentsMargins(4, 0, 4, 0)\n        body_layout.setSpacing(2)\n        body_layout.addWidget(icon)\n        body_layout.addWidget(label)\n\n        layout = QtWidgets.QHBoxLayout(self)\n        layout.setContentsMargins(2, 1, 2, 1)\n        layout.setSpacing(0)\n        layout.addWidget(body_widget)\n        layout.addWidget(option)\n\n        body_widget.setMouseTracking(True)\n        label.setMouseTracking(True)\n        option.setMouseTracking(True)\n        self.setMouseTracking(True)\n        self.setFixedHeight(32)\n\n        self.icon = icon\n        self.label = label\n        self.option = option\n        self.body = body_widget\n\n    def set_hover_properties(self, hovered, option_hovered):\n        body_state = \"\"\n        option_state = \"\"\n        if hovered:\n            body_state = \"hover\"\n\n        if option_hovered:\n            option_state = \"hover\"\n\n        if self.body.property(\"state\") != body_state:\n            self.body.setProperty(\"state\", body_state)\n            self.body.style().polish(self.body)\n\n        if self.option.property(\"state\") != option_state:\n            self.option.setProperty(\"state\", option_state)\n            self.option.style().polish(self.option)\n\n    def setIcon(self, icon):\n        pixmap = icon.pixmap(16, 16)\n        self.icon.setPixmap(pixmap)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/widgets.html#client.ayon_core.tools.utils.widgets.OptionalMenu","title":"<code>OptionalMenu</code>","text":"<p>               Bases: <code>QMenu</code></p> <p>A subclass of <code>QtWidgets.QMenu</code> to work with <code>OptionalAction</code></p> <p>This menu has reimplemented <code>mouseReleaseEvent</code>, <code>mouseMoveEvent</code> and <code>leaveEvent</code> to provide better action highlighting and triggering for actions that were instances of <code>QtWidgets.QWidgetAction</code>.</p> Source code in <code>client/ayon_core/tools/utils/widgets.py</code> <pre><code>class OptionalMenu(QtWidgets.QMenu):\n    \"\"\"A subclass of `QtWidgets.QMenu` to work with `OptionalAction`\n\n    This menu has reimplemented `mouseReleaseEvent`, `mouseMoveEvent` and\n    `leaveEvent` to provide better action highlighting and triggering for\n    actions that were instances of `QtWidgets.QWidgetAction`.\n\n    \"\"\"\n    def mouseReleaseEvent(self, event):\n        \"\"\"Emit option clicked signal if mouse released on it\"\"\"\n        active = self.actionAt(event.pos())\n        if isinstance(active, OptionalAction) and active.use_option:\n            option = active.widget.option\n            if option.is_hovered(event.globalPos()):\n                option.clicked.emit()\n        super().mouseReleaseEvent(event)\n\n    def mouseMoveEvent(self, event):\n        \"\"\"Add highlight to active action\"\"\"\n        active = self.actionAt(event.pos())\n        for action in self.actions():\n            if isinstance(action, OptionalAction):\n                action.set_highlight(action is active, event.globalPos())\n        super().mouseMoveEvent(event)\n\n    def leaveEvent(self, event):\n        \"\"\"Remove highlight from all actions\"\"\"\n        for action in self.actions():\n            if isinstance(action, OptionalAction):\n                action.set_highlight(False)\n        super().leaveEvent(event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/widgets.html#client.ayon_core.tools.utils.widgets.OptionalMenu.leaveEvent","title":"<code>leaveEvent(event)</code>","text":"<p>Remove highlight from all actions</p> Source code in <code>client/ayon_core/tools/utils/widgets.py</code> <pre><code>def leaveEvent(self, event):\n    \"\"\"Remove highlight from all actions\"\"\"\n    for action in self.actions():\n        if isinstance(action, OptionalAction):\n            action.set_highlight(False)\n    super().leaveEvent(event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/widgets.html#client.ayon_core.tools.utils.widgets.OptionalMenu.mouseMoveEvent","title":"<code>mouseMoveEvent(event)</code>","text":"<p>Add highlight to active action</p> Source code in <code>client/ayon_core/tools/utils/widgets.py</code> <pre><code>def mouseMoveEvent(self, event):\n    \"\"\"Add highlight to active action\"\"\"\n    active = self.actionAt(event.pos())\n    for action in self.actions():\n        if isinstance(action, OptionalAction):\n            action.set_highlight(action is active, event.globalPos())\n    super().mouseMoveEvent(event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/widgets.html#client.ayon_core.tools.utils.widgets.OptionalMenu.mouseReleaseEvent","title":"<code>mouseReleaseEvent(event)</code>","text":"<p>Emit option clicked signal if mouse released on it</p> Source code in <code>client/ayon_core/tools/utils/widgets.py</code> <pre><code>def mouseReleaseEvent(self, event):\n    \"\"\"Emit option clicked signal if mouse released on it\"\"\"\n    active = self.actionAt(event.pos())\n    if isinstance(active, OptionalAction) and active.use_option:\n        option = active.widget.option\n        if option.is_hovered(event.globalPos()):\n            option.clicked.emit()\n    super().mouseReleaseEvent(event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/widgets.html#client.ayon_core.tools.utils.widgets.PixmapLabel","title":"<code>PixmapLabel</code>","text":"<p>               Bases: <code>QLabel</code></p> <p>Label resizing image to height of font.</p> Source code in <code>client/ayon_core/tools/utils/widgets.py</code> <pre><code>class PixmapLabel(QtWidgets.QLabel):\n    \"\"\"Label resizing image to height of font.\"\"\"\n    def __init__(self, pixmap, parent):\n        super(PixmapLabel, self).__init__(parent)\n        self._empty_pixmap = QtGui.QPixmap(0, 0)\n        self._source_pixmap = pixmap\n\n        self._last_width = 0\n        self._last_height = 0\n\n    def set_source_pixmap(self, pixmap):\n        \"\"\"Change source image.\"\"\"\n        self._source_pixmap = pixmap\n        self._set_resized_pix()\n\n    def _get_pix_size(self):\n        size = self.fontMetrics().height()\n        size += size % 2\n        return size, size\n\n    def minimumSizeHint(self):\n        width, height = self._get_pix_size()\n        if width != self._last_width or height != self._last_height:\n            self._set_resized_pix()\n        return QtCore.QSize(width, height)\n\n    def _set_resized_pix(self):\n        if self._source_pixmap is None:\n            self.setPixmap(self._empty_pixmap)\n            return\n        width, height = self._get_pix_size()\n        self.setPixmap(\n            self._source_pixmap.scaled(\n                width,\n                height,\n                QtCore.Qt.KeepAspectRatio,\n                QtCore.Qt.SmoothTransformation\n            )\n        )\n        self._last_width = width\n        self._last_height = height\n\n    def resizeEvent(self, event):\n        self._set_resized_pix()\n        super().resizeEvent(event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/widgets.html#client.ayon_core.tools.utils.widgets.PixmapLabel.set_source_pixmap","title":"<code>set_source_pixmap(pixmap)</code>","text":"<p>Change source image.</p> Source code in <code>client/ayon_core/tools/utils/widgets.py</code> <pre><code>def set_source_pixmap(self, pixmap):\n    \"\"\"Change source image.\"\"\"\n    self._source_pixmap = pixmap\n    self._set_resized_pix()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/widgets.html#client.ayon_core.tools.utils.widgets.PlaceholderLineEdit","title":"<code>PlaceholderLineEdit</code>","text":"<p>               Bases: <code>QLineEdit</code></p> <p>Set placeholder color of QLineEdit in Qt 5.12 and higher.</p> Source code in <code>client/ayon_core/tools/utils/widgets.py</code> <pre><code>class PlaceholderLineEdit(QtWidgets.QLineEdit):\n    \"\"\"Set placeholder color of QLineEdit in Qt 5.12 and higher.\"\"\"\n    def __init__(self, *args, **kwargs):\n        super().__init__(*args, **kwargs)\n        self.setMaxLength(2147483647)\n        # Change placeholder palette color\n        if hasattr(QtGui.QPalette, \"PlaceholderText\"):\n            filter_palette = self.palette()\n            filter_palette.setColor(\n                QtGui.QPalette.PlaceholderText,\n                _Cache.get_placeholder_color()\n            )\n            self.setPalette(filter_palette)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/widgets.html#client.ayon_core.tools.utils.widgets.PlaceholderPlainTextEdit","title":"<code>PlaceholderPlainTextEdit</code>","text":"<p>               Bases: <code>QPlainTextEdit</code></p> <p>Set placeholder color of QPlainTextEdit in Qt 5.12 and higher.</p> Source code in <code>client/ayon_core/tools/utils/widgets.py</code> <pre><code>class PlaceholderPlainTextEdit(QtWidgets.QPlainTextEdit):\n    \"\"\"Set placeholder color of QPlainTextEdit in Qt 5.12 and higher.\"\"\"\n    def __init__(self, *args, **kwargs):\n        super().__init__(*args, **kwargs)\n        # Change placeholder palette color\n        if hasattr(QtGui.QPalette, \"PlaceholderText\"):\n            viewport = self.viewport()\n            filter_palette = viewport.palette()\n            filter_palette.setColor(\n                QtGui.QPalette.PlaceholderText,\n                _Cache.get_placeholder_color()\n            )\n            viewport.setPalette(filter_palette)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/widgets.html#client.ayon_core.tools.utils.widgets.SeparatorWidget","title":"<code>SeparatorWidget</code>","text":"<p>               Bases: <code>QFrame</code></p> <p>Prepared widget that can be used as separator with predefined color.</p> <p>Parameters:</p> Name Type Description Default <code>size</code> <code>int</code> <p>Size of separator (width or height).</p> <code>2</code> <code>orientation</code> <code>Horizontal | Vertical</code> <p>Orintation of widget.</p> <code>Horizontal</code> <code>parent</code> <code>QWidget</code> <p>Parent widget.</p> <code>None</code> Source code in <code>client/ayon_core/tools/utils/widgets.py</code> <pre><code>class SeparatorWidget(QtWidgets.QFrame):\n    \"\"\"Prepared widget that can be used as separator with predefined color.\n\n    Args:\n        size (int): Size of separator (width or height).\n        orientation (Qt.Horizontal|Qt.Vertical): Orintation of widget.\n        parent (QtWidgets.QWidget): Parent widget.\n    \"\"\"\n\n    def __init__(self, size=2, orientation=QtCore.Qt.Horizontal, parent=None):\n        super(SeparatorWidget, self).__init__(parent)\n\n        self.setObjectName(\"Separator\")\n\n        maximum_width = self.maximumWidth()\n        maximum_height = self.maximumHeight()\n\n        self._size = None\n        self._orientation = orientation\n        self._maximum_width = maximum_width\n        self._maximum_height = maximum_height\n        self.set_size(size)\n\n    def set_size(self, size):\n        if size != self._size:\n            self._set_size(size)\n\n    def _set_size(self, size):\n        if self._orientation == QtCore.Qt.Vertical:\n            self.setMinimumWidth(size)\n            self.setMaximumWidth(size)\n        else:\n            self.setMinimumHeight(size)\n            self.setMaximumHeight(size)\n\n        self._size = size\n\n    def set_orientation(self, orientation):\n        if self._orientation == orientation:\n            return\n\n        # Reset min/max sizes in opposite direction\n        if self._orientation == QtCore.Qt.Vertical:\n            self.setMinimumHeight(0)\n            self.setMaximumHeight(self._maximum_height)\n        else:\n            self.setMinimumWidth(0)\n            self.setMaximumWidth(self._maximum_width)\n\n        self._orientation = orientation\n\n        self._set_size(self._size)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/widgets.html#client.ayon_core.tools.utils.widgets.SquareButton","title":"<code>SquareButton</code>","text":"<p>               Bases: <code>QPushButton</code></p> <p>Make button square shape.</p> <p>Change width to match height on resize.</p> Source code in <code>client/ayon_core/tools/utils/widgets.py</code> <pre><code>class SquareButton(QtWidgets.QPushButton):\n    \"\"\"Make button square shape.\n\n    Change width to match height on resize.\n    \"\"\"\n\n    def __init__(self, *args, **kwargs):\n        super().__init__(*args, **kwargs)\n\n        sp = self.sizePolicy()\n        sp.setVerticalPolicy(QtWidgets.QSizePolicy.Minimum)\n        sp.setHorizontalPolicy(QtWidgets.QSizePolicy.Minimum)\n        self.setSizePolicy(sp)\n        self._ideal_width = None\n\n    def showEvent(self, event):\n        super().showEvent(event)\n        self._ideal_width = self.height()\n        self.updateGeometry()\n\n    def resizeEvent(self, event):\n        super().resizeEvent(event)\n        self._ideal_width = self.height()\n        self.updateGeometry()\n\n    def sizeHint(self):\n        sh = super().sizeHint()\n        ideal_width = self._ideal_width\n        if ideal_width is None:\n            ideal_width = sh.height()\n        sh.setWidth(ideal_width)\n        return sh\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/widgets.html#client.ayon_core.tools.utils.widgets.VerticalExpandButton","title":"<code>VerticalExpandButton</code>","text":"<p>               Bases: <code>QPushButton</code></p> <p>Button which is expanding vertically.</p> <p>By default, button is a little bit smaller than other widgets like     QLineEdit. This button is expanding vertically to match size of     other widgets, next to it.</p> Source code in <code>client/ayon_core/tools/utils/widgets.py</code> <pre><code>class VerticalExpandButton(QtWidgets.QPushButton):\n    \"\"\"Button which is expanding vertically.\n\n    By default, button is a little bit smaller than other widgets like\n        QLineEdit. This button is expanding vertically to match size of\n        other widgets, next to it.\n    \"\"\"\n\n    def __init__(self, parent=None):\n        super(VerticalExpandButton, self).__init__(parent)\n\n        sp = self.sizePolicy()\n        sp.setVerticalPolicy(QtWidgets.QSizePolicy.Minimum)\n        self.setSizePolicy(sp)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/color_widgets/index.html","title":"color_widgets","text":""},{"location":"autoapi/client/ayon_core/tools/utils/color_widgets/color_inputs.html","title":"color_inputs","text":""},{"location":"autoapi/client/ayon_core/tools/utils/color_widgets/color_picker_widget.html","title":"color_picker_widget","text":""},{"location":"autoapi/client/ayon_core/tools/utils/color_widgets/color_screen_pick.html","title":"color_screen_pick","text":""},{"location":"autoapi/client/ayon_core/tools/utils/color_widgets/color_triangle.html","title":"color_triangle","text":""},{"location":"autoapi/client/ayon_core/tools/utils/color_widgets/color_triangle.html#client.ayon_core.tools.utils.color_widgets.color_triangle.QtColorTriangle","title":"<code>QtColorTriangle</code>","text":"<p>               Bases: <code>QWidget</code></p> <p>The QtColorTriangle class provides a triangular color selection widget.</p> <p>This widget uses the HSV color model, and is therefore useful for selecting colors by eye.</p> <p>The triangle in the center of the widget is used for selecting saturation and value, and the surrounding circle is used for selecting hue.</p> <p>Use set_color() and color() to set and get the current color.</p> Source code in <code>client/ayon_core/tools/utils/color_widgets/color_triangle.py</code> <pre><code>class QtColorTriangle(QtWidgets.QWidget):\n    \"\"\"The QtColorTriangle class provides a triangular color selection widget.\n\n    This widget uses the HSV color model, and is therefore useful for\n    selecting colors by eye.\n\n    The triangle in the center of the widget is used for selecting\n    saturation and value, and the surrounding circle is used for\n    selecting hue.\n\n    Use set_color() and color() to set and get the current color.\n    \"\"\"\n    color_changed = QtCore.Signal(QtGui.QColor)\n\n    # Thick of color wheel ratio where 1 is fully filled circle\n    inner_radius_ratio = 5.0\n    # Ratio where hue selector on wheel is relative to `inner_radius_ratio`\n    # - middle of the wheel is twice `inner_radius_ratio`\n    selector_radius_ratio = inner_radius_ratio * 2\n    # Size ratio of selectors on wheel and in triangle\n    ellipse_size_ratio = 10.0\n    # Ration of selectors thickness\n    ellipse_thick_ratio = 50.0\n    # Hue offset on color wheel (0 - 359)\n    # - red on top if set to \"0\"\n    hue_offset = 90\n\n    def __init__(self, parent=None):\n        super(QtColorTriangle, self).__init__(parent)\n        self.setSizePolicy(\n            QtWidgets.QSizePolicy.Minimum,\n            QtWidgets.QSizePolicy.Minimum\n        )\n        self.setFocusPolicy(QtCore.Qt.StrongFocus)\n\n        self.angle_a = float()\n        self.angle_b = float()\n        self.angle_c = float()\n\n        self.bg_image = QtGui.QImage(\n            self.sizeHint(), QtGui.QImage.Format_RGB32\n        )\n        self.cur_color = QtGui.QColor()\n        self.point_a = QtCore.QPointF()\n        self.point_b = QtCore.QPointF()\n        self.point_c = QtCore.QPointF()\n        self.point_d = QtCore.QPointF()\n\n        self.cur_hue = int()\n\n        self.pen_width = int()\n        self.ellipse_size = int()\n        self.outer_radius = int()\n        self.selector_pos = QtCore.QPointF()\n\n        self.sel_mode = TriangleState.IdleState\n\n        self._triangle_outline_pen = QtGui.QPen(\n            QtGui.QColor(40, 40, 40, 128),\n            2\n        )\n        # Prepare hue numbers for color circle\n        _hue_circle_range = []\n        for idx in range(11):\n            # Some Qt versions may require:\n            # hue = int(idx * 360.0)\n            percent_idx = idx * 0.1\n            hue = int(360.0 - (percent_idx * 360.0))\n            _hue_circle_range.append((percent_idx, hue))\n        self._hue_circle_range = tuple(_hue_circle_range)\n\n        color = QtGui.QColor()\n        color.setHsv(0, 255, 255)\n        self.set_color(color)\n\n    def set_color(self, col):\n        if (\n            col.red() == self.cur_color.red()\n            and col.green() == self.cur_color.green()\n            and col.blue() == self.cur_color.blue()\n        ):\n            return\n\n        self.cur_color = col\n\n        hue, *_ = self.cur_color.getHsv()\n\n        # Never use an invalid hue to display colors\n        if hue != -1:\n            self.cur_hue = hue\n\n        angle_with_offset = (360 - self.cur_hue - self.hue_offset) % 360\n        self.angle_a = (angle_with_offset * TWOPI) / 360.0\n        self.angle_a += PI / 2.0\n        if self.angle_a &gt; TWOPI:\n            self.angle_a -= TWOPI\n\n        self.angle_b = self.angle_a + TWOPI / 3\n        self.angle_c = self.angle_b + TWOPI / 3\n\n        if self.angle_b &gt; TWOPI:\n            self.angle_b -= TWOPI\n        if self.angle_c &gt; TWOPI:\n            self.angle_c -= TWOPI\n\n        cx = float(self.contentsRect().center().x())\n        cy = float(self.contentsRect().center().y())\n        inner_radius = (\n            self.outer_radius\n            - (self.outer_radius / self.inner_radius_ratio)\n        )\n        selector_radius = (\n            self.outer_radius\n            - (self.outer_radius / self.selector_radius_ratio)\n        )\n        self.point_a = QtCore.QPointF(\n            cx + (cos(self.angle_a) * inner_radius),\n            cy - (sin(self.angle_a) * inner_radius)\n        )\n        self.point_b = QtCore.QPointF(\n            cx + (cos(self.angle_b) * inner_radius),\n            cy - (sin(self.angle_b) * inner_radius)\n        )\n        self.point_c = QtCore.QPointF(\n            cx + (cos(self.angle_c) * inner_radius),\n            cy - (sin(self.angle_c) * inner_radius)\n        )\n        self.point_d = QtCore.QPointF(\n            cx + (cos(self.angle_a) * selector_radius),\n            cy - (sin(self.angle_a) * selector_radius)\n        )\n\n        self.selector_pos = self._point_from_color(self.cur_color)\n        self.update()\n\n        self.color_changed.emit(self.cur_color)\n\n    def heightForWidth(self, width):\n        return width\n\n    def polish(self):\n        size_w = self.contentsRect().width()\n        size_h = self.contentsRect().height()\n        if size_w &lt; size_h:\n            size = size_w\n        else:\n            size = size_h\n\n        self.outer_radius = (size - 1) / 2\n\n        self.pen_width = int(\n            ceil(self.outer_radius / self.ellipse_thick_ratio)\n        )\n        self.ellipse_size = int(\n            ceil(self.outer_radius / self.ellipse_size_ratio)\n        )\n\n        cx = float(self.contentsRect().center().x())\n        cy = float(self.contentsRect().center().y())\n\n        inner_radius = (\n            self.outer_radius\n            - (self.outer_radius / self.inner_radius_ratio)\n        )\n        selector_radius = (\n            self.outer_radius\n            - (self.outer_radius / self.selector_radius_ratio)\n        )\n        self.point_a = QtCore.QPointF(\n            cx + (cos(self.angle_a) * inner_radius),\n            cy - (sin(self.angle_a) * inner_radius)\n        )\n        self.point_b = QtCore.QPointF(\n            cx + (cos(self.angle_b) * inner_radius),\n            cy - (sin(self.angle_b) * inner_radius)\n        )\n        self.point_c = QtCore.QPointF(\n            cx + (cos(self.angle_c) * inner_radius),\n            cy - (sin(self.angle_c) * inner_radius)\n        )\n        self.point_d = QtCore.QPointF(\n            cx + (cos(self.angle_a) * selector_radius),\n            cy - (sin(self.angle_a) * selector_radius)\n        )\n\n        self.selector_pos = self._point_from_color(self.cur_color)\n\n        self.update()\n\n    def paintEvent(self, event):\n        painter = QtGui.QPainter(self)\n        if event.rect().intersects(self.contentsRect()):\n            event_region = event.region()\n            if hasattr(event_region, \"intersect\"):\n                clip_region = event_region.intersect(self.contentsRect())\n            else:\n                clip_region = event_region.intersected(\n                    self.contentsRect()\n                )\n            painter.setClipRegion(clip_region)\n\n        self.paint_bg()\n\n        # Blit the static generated background with the hue gradient onto\n        # the double buffer.\n        buf = QtGui.QImage(\n            self.bg_image.width(),\n            self.bg_image.height(),\n            QtGui.QImage.Format_RGB32\n        )\n\n        # Draw the trigon\n        # Find the color with only the hue, and max value and saturation\n        hue_color = QtGui.QColor()\n        hue_color.setHsv(self.cur_hue, 255, 255)\n\n        # Draw the triangle\n        self.drawTrigon(\n            buf, self.point_a, self.point_b, self.point_c, hue_color\n        )\n\n        # Slow step: convert the image to a pixmap\n        pix = self.bg_image.copy()\n        pix_painter = QtGui.QPainter(pix)\n\n        pix_painter.setRenderHint(QtGui.QPainter.Antialiasing)\n\n        trigon_path = QtGui.QPainterPath()\n        trigon_path.moveTo(self.point_a)\n        trigon_path.lineTo(self.point_b)\n        trigon_path.lineTo(self.point_c)\n        trigon_path.closeSubpath()\n        pix_painter.setClipPath(trigon_path)\n\n        pix_painter.drawImage(0, 0, buf)\n\n        pix_painter.setClipping(False)\n\n        # Draw an outline of the triangle\n        pix_painter.setPen(self._triangle_outline_pen)\n        pix_painter.drawLine(self.point_a, self.point_b)\n        pix_painter.drawLine(self.point_b, self.point_c)\n        pix_painter.drawLine(self.point_c, self.point_a)\n\n        # Draw the color wheel selector\n        pix_painter.setPen(QtGui.QPen(QtCore.Qt.white, self.pen_width))\n        pix_painter.drawEllipse(\n            int(self.point_d.x() - self.ellipse_size / 2.0),\n            int(self.point_d.y() - self.ellipse_size / 2.0),\n            self.ellipse_size, self.ellipse_size\n        )\n\n        # Draw the triangle selector\n        pix_painter.setBrush(self.cur_color)\n        pix_painter.drawEllipse(\n            QtCore.QRectF(\n                self.selector_pos.x() - self.ellipse_size / 2.0,\n                self.selector_pos.y() - self.ellipse_size / 2.0,\n                self.ellipse_size + 0.5,\n                self.ellipse_size + 0.5\n            )\n        )\n\n        pix_painter.end()\n        # Blit\n        painter.drawPixmap(self.contentsRect().topLeft(), pix)\n        painter.end()\n\n    def mouseMoveEvent(self, event):\n        if (event.buttons() &amp; QtCore.Qt.LeftButton) == 0:\n            return\n\n        depos = QtCore.QPointF(\n            event.pos().x(),\n            event.pos().y()\n        )\n        new_color = False\n\n        if self.sel_mode is TriangleState.SelectingHueState:\n            self.angle_a = self._angle_at(depos, self.contentsRect())\n            self.angle_b = self.angle_a + (TWOPI / 3.0)\n            self.angle_c = self.angle_b + (TWOPI / 3.0)\n            if self.angle_b &gt; TWOPI:\n                self.angle_b -= TWOPI\n            if self.angle_c &gt; TWOPI:\n                self.angle_c -= TWOPI\n\n            am = self.angle_a - (PI / 2)\n            if am &lt; 0:\n                am += TWOPI\n            self.cur_hue = (\n                360 - int((am * 360.0) / TWOPI) - self.hue_offset\n            ) % 360\n            hue, sat, val, _ = self.cur_color.getHsv()\n\n            if self.cur_hue != hue:\n                new_color = True\n                self.cur_color.setHsv(self.cur_hue, sat, val)\n\n            cx = float(self.contentsRect().center().x())\n            cy = float(self.contentsRect().center().y())\n            inner_radius = (\n                self.outer_radius\n                - (self.outer_radius / self.inner_radius_ratio)\n            )\n            selector_radius = (\n                self.outer_radius\n                - (self.outer_radius / self.selector_radius_ratio)\n            )\n            self.point_a = QtCore.QPointF(\n                cx + (cos(self.angle_a) * inner_radius),\n                cy - (sin(self.angle_a) * inner_radius)\n            )\n            self.point_b = QtCore.QPointF(\n                cx + (cos(self.angle_b) * inner_radius),\n                cy - (sin(self.angle_b) * inner_radius)\n            )\n            self.point_c = QtCore.QPointF(\n                cx + (cos(self.angle_c) * inner_radius),\n                cy - (sin(self.angle_c) * inner_radius)\n            )\n            self.point_d = QtCore.QPointF(\n                cx + (cos(self.angle_a) * selector_radius),\n                cy - (sin(self.angle_a) * selector_radius)\n            )\n\n            self.selector_pos = self._point_from_color(self.cur_color)\n        else:\n            aa = Vertex(QtCore.Qt.transparent, self.point_a)\n            bb = Vertex(QtCore.Qt.transparent, self.point_b)\n            cc = Vertex(QtCore.Qt.transparent, self.point_c)\n\n            self.selector_pos = self._move_point_to_triangle(\n                depos.x(), depos.y(), aa, bb, cc\n            )\n            col = self._color_from_point(self.selector_pos)\n            if col != self.cur_color:\n                # Ensure that hue does not change when selecting\n                # saturation and value.\n                _, sat, val, _ = col.getHsv()\n                self.cur_color.setHsv(self.cur_hue, sat, val)\n                new_color = True\n\n        if new_color:\n            self.color_changed.emit(self.cur_color)\n\n        self.update()\n\n    def mousePressEvent(self, event):\n        # Only respond to the left mouse button.\n        if event.button() != QtCore.Qt.LeftButton:\n            return\n\n        depos = QtCore.QPointF(\n            event.pos().x(),\n            event.pos().y()\n        )\n        rad = self._radius_at(depos, self.contentsRect())\n        new_color = False\n\n        # As in mouseMoveEvent, either find the a, b, c angles or the\n        # radian position of the selector, then order an update.\n        inner_radius = (\n            self.outer_radius - (self.outer_radius / self.inner_radius_ratio)\n        )\n        if rad &gt; inner_radius:\n            self.sel_mode = TriangleState.SelectingHueState\n\n            self.angle_a = self._angle_at(depos, self.contentsRect())\n            self.angle_b = self.angle_a + TWOPI / 3.0\n            self.angle_c = self.angle_b + TWOPI / 3.0\n            if self.angle_b &gt; TWOPI:\n                self.angle_b -= TWOPI\n            if self.angle_c &gt; TWOPI:\n                self.angle_c -= TWOPI\n\n            am = self.angle_a - PI / 2\n            if am &lt; 0:\n                am += TWOPI\n\n            self.cur_hue = (\n                360 - int((am * 360.0) / TWOPI) - self.hue_offset\n            ) % 360\n            hue, sat, val, _ = self.cur_color.getHsv()\n\n            if hue != self.cur_hue:\n                new_color = True\n                self.cur_color.setHsv(self.cur_hue, sat, val)\n\n            cx = float(self.contentsRect().center().x())\n            cy = float(self.contentsRect().center().y())\n\n            self.point_a = QtCore.QPointF(\n                cx + (cos(self.angle_a) * inner_radius),\n                cy - (sin(self.angle_a) * inner_radius)\n            )\n            self.point_b = QtCore.QPointF(\n                cx + (cos(self.angle_b) * inner_radius),\n                cy - (sin(self.angle_b) * inner_radius)\n            )\n            self.point_c = QtCore.QPointF(\n                cx + (cos(self.angle_c) * inner_radius),\n                cy - (sin(self.angle_c) * inner_radius)\n            )\n\n            selector_radius = (\n                self.outer_radius\n                - (self.outer_radius / self.selector_radius_ratio)\n            )\n            self.point_d = QtCore.QPointF(\n                cx + (cos(self.angle_a) * selector_radius),\n                cy - (sin(self.angle_a) * selector_radius)\n            )\n\n            self.selector_pos = self._point_from_color(self.cur_color)\n            self.color_changed.emit(self.cur_color)\n        else:\n            self.sel_mode = TriangleState.SelectingSatValueState\n\n            aa = Vertex(QtCore.Qt.transparent, self.point_a)\n            bb = Vertex(QtCore.Qt.transparent, self.point_b)\n            cc = Vertex(QtCore.Qt.transparent, self.point_c)\n\n            self.selector_pos = self._move_point_to_triangle(\n                depos.x(), depos.y(), aa, bb, cc\n            )\n            col = self._color_from_point(self.selector_pos)\n            if col != self.cur_color:\n                self.cur_color = col\n                new_color = True\n\n        if new_color:\n            self.color_changed.emit(self.cur_color)\n\n        self.update()\n\n    def mouseReleaseEvent(self, event):\n        if event.button() == QtCore.Qt.LeftButton:\n            self.sel_mode = TriangleState.IdleState\n\n    def keyPressEvent(self, event):\n        key = event.key()\n        if key == QtCore.Qt.Key_Left:\n            self.cur_hue -= 1\n            if self.cur_hue &lt; 0:\n                self.cur_hue += 360\n            _, sat, val, _ = self.cur_color.getHsv()\n\n            tmp = QtGui.QColor()\n            tmp.setHsv(self.cur_hue, sat, val)\n            self.set_color(tmp)\n\n        elif key == QtCore.Qt.Key_Right:\n            self.cur_hue += 1\n            if (self.cur_hue &gt; 359):\n                self.cur_hue -= 360\n            _, sat, val, _ = self.cur_color.getHsv()\n            tmp = QtGui.QColor()\n            tmp.setHsv(self.cur_hue, sat, val)\n            self.set_color(tmp)\n\n        elif key == QtCore.Qt.Key_Up:\n            _, sat, val, _ = self.cur_color.getHsv()\n            if event.modifiers() &amp; QtCore.Qt.ShiftModifier:\n                if sat &gt; 5:\n                    sat -= 5\n                else:\n                    sat = 0\n            else:\n                if val &gt; 5:\n                    val -= 5\n                else:\n                    val = 0\n\n            tmp = QtGui.QColor()\n            tmp.setHsv(self.cur_hue, sat, val)\n            self.set_color(tmp)\n\n        elif key == QtCore.Qt.Key_Down:\n            _, sat, val, _ = self.cur_color.getHsv()\n            if event.modifiers() &amp; QtCore.Qt.ShiftModifier:\n                if sat &lt; 250:\n                    sat += 5\n                else:\n                    sat = 255\n            else:\n                if val &lt; 250:\n                    val += 5\n                else:\n                    val = 255\n\n            tmp = QtGui.QColor()\n            tmp.setHsv(self.cur_hue, sat, val)\n            self.set_color(tmp)\n\n    def resizeEvent(self, _event):\n        size_w = self.contentsRect().width()\n        size_h = self.contentsRect().height()\n        if size_w &lt; size_h:\n            size = size_w\n        else:\n            size = size_h\n\n        self.outer_radius = (size - 1) / 2\n\n        self.pen_width = int(\n            ceil(self.outer_radius / self.ellipse_thick_ratio)\n        )\n        self.ellipse_size = int(\n            ceil(self.outer_radius / self.ellipse_size_ratio)\n        )\n\n        cx = float(self.contentsRect().center().x())\n        cy = float(self.contentsRect().center().y())\n        inner_radius = (\n            self.outer_radius\n            - (self.outer_radius / self.inner_radius_ratio)\n        )\n        selector_radius = (\n            self.outer_radius\n            - (self.outer_radius / self.selector_radius_ratio)\n        )\n        self.point_a = QtCore.QPointF(\n            cx + (cos(self.angle_a) * inner_radius),\n            cy - (sin(self.angle_a) * inner_radius)\n        )\n        self.point_b = QtCore.QPointF(\n            cx + (cos(self.angle_b) * inner_radius),\n            cy - (sin(self.angle_b) * inner_radius)\n        )\n        self.point_c = QtCore.QPointF(\n            cx + (cos(self.angle_c) * inner_radius),\n            cy - (sin(self.angle_c) * inner_radius)\n        )\n        self.point_d = QtCore.QPointF(\n            cx + (cos(self.angle_a) * selector_radius),\n            cy - (sin(self.angle_a) * selector_radius)\n        )\n\n        # Find the current position of the selector\n        self.selector_pos = self._point_from_color(self.cur_color)\n\n        self.update()\n\n    def drawTrigon(self, buf, pa, pb, pc, color):\n        # Create three Vertex objects. A Vertex contains a double-point\n        # coordinate and a color.\n        # pa is the tip of the arrow\n        # pb is the black corner\n        # pc is the white corner\n        p1 = Vertex(color, pa)\n        p2 = Vertex(QtCore.Qt.black, pb)\n        p3 = Vertex(QtCore.Qt.white, pc)\n\n        # Sort. Make p1 above p2, which is above p3 (using y coordinate).\n        # Bubble sorting is fastest here.\n        if p1.point.y() &gt; p2.point.y():\n            p1, p2 = p2, p1\n        if p1.point.y() &gt; p3.point.y():\n            p1, p3 = p3, p1\n        if p2.point.y() &gt; p3.point.y():\n            p2, p3 = p3, p2\n\n        # All the three y deltas are &gt;= 0\n        p1p2ydist = float(p2.point.y() - p1.point.y())\n        p1p3ydist = float(p3.point.y() - p1.point.y())\n        p2p3ydist = float(p3.point.y() - p2.point.y())\n        p1p2xdist = float(p2.point.x() - p1.point.x())\n        p1p3xdist = float(p3.point.x() - p1.point.x())\n        p2p3xdist = float(p3.point.x() - p2.point.x())\n\n        # The first x delta decides wether we have a lefty or a righty\n        # trigon.\n        lefty = p1p2xdist &lt; 0\n\n        # Left and right colors and X values. The key in this map is the\n        # y values. Our goal is to fill these structures with all the\n        # information needed to do a single pass top-to-bottom,\n        # left-to-right drawing of the trigon.\n        leftColors = {}\n        rightColors = {}\n        leftX = {}\n        rightX = {}\n\n        # Scan longy - find all left and right colors and X-values for\n        # the tallest edge (p1-p3).\n        # Initialize with known values\n        x = p1.point.x()\n        source = p1.color\n        dest = p3.color\n        r = source.r\n        g = source.g\n        b = source.b\n        y1 = int(floor(p1.point.y()))\n        y2 = int(floor(p3.point.y()))\n\n        # Find slopes (notice that if the y dists are 0, we don't care\n        # about the slopes)\n        xdelta = 0.0\n        rdelta = 0.0\n        gdelta = 0.0\n        bdelta = 0.0\n        if p1p3ydist != 0.0:\n            xdelta = p1p3xdist / p1p3ydist\n            rdelta = (dest.r - r) / p1p3ydist\n            gdelta = (dest.g - g) / p1p3ydist\n            bdelta = (dest.b - b) / p1p3ydist\n\n        # Calculate gradients using linear approximation\n        for y in range(y1, y2):\n            if lefty:\n                rightColors[y] = DoubleColor(r, g, b)\n                rightX[y] = x\n            else:\n                leftColors[y] = DoubleColor(r, g, b)\n                leftX[y] = x\n\n            r += rdelta\n            g += gdelta\n            b += bdelta\n            x += xdelta\n\n        # Scan top shorty - find all left and right colors and x-values\n        # for the topmost of the two not-tallest short edges.\n        x = p1.point.x()\n        source = p1.color\n        dest = p2.color\n        r = source.r\n        g = source.g\n        b = source.b\n        y1 = int(floor(p1.point.y()))\n        y2 = int(floor(p2.point.y()))\n\n        # Find slopes (notice that if the y dists are 0, we don't care\n        # about the slopes)\n        xdelta = 0.0\n        rdelta = 0.0\n        gdelta = 0.0\n        bdelta = 0.0\n        if p1p2ydist != 0.0:\n            xdelta = p1p2xdist / p1p2ydist\n            rdelta = (dest.r - r) / p1p2ydist\n            gdelta = (dest.g - g) / p1p2ydist\n            bdelta = (dest.b - b) / p1p2ydist\n\n        # Calculate gradients using linear approximation\n        for y in range(y1, y2):\n            if lefty:\n                leftColors[y] = DoubleColor(r, g, b)\n                leftX[y] = x\n            else:\n                rightColors[y] = DoubleColor(r, g, b)\n                rightX[y] = x\n\n            r += rdelta\n            g += gdelta\n            b += bdelta\n            x += xdelta\n\n        # Scan bottom shorty - find all left and right colors and\n        # x-values for the bottommost of the two not-tallest short edges.\n        x = p2.point.x()\n        source = p2.color\n        dest = p3.color\n        r = source.r\n        g = source.g\n        b = source.b\n        y1 = int(floor(p2.point.y()))\n        y2 = int(floor(p3.point.y()))\n\n        # Find slopes (notice that if the y dists are 0, we don't care\n        # about the slopes)\n        xdelta = 0.0\n        rdelta = 0.0\n        gdelta = 0.0\n        bdelta = 0.0\n        if p2p3ydist != 0.0:\n            xdelta = p2p3xdist / p2p3ydist\n            rdelta = (dest.r - r) / p2p3ydist\n            gdelta = (dest.g - g) / p2p3ydist\n            bdelta = (dest.b - b) / p2p3ydist\n\n        # Calculate gradients using linear approximation\n        for y in range(y1, y2):\n            if lefty:\n                leftColors[y] = DoubleColor(r, g, b)\n                leftX[y] = x\n            else:\n                rightColors[y] = DoubleColor(r, g, b)\n                rightX[y] = x\n\n            r += rdelta\n            g += gdelta\n            b += bdelta\n            x += xdelta\n\n        # Inner loop. For each y in the left map of x-values, draw one\n        # line from left to right.\n        p3yfloor = int(floor(p3.point.y()))\n        p1yfloor = int(floor(p1.point.y()))\n        for y in range(p1yfloor, p3yfloor):\n            lx = leftX[y]\n            rx = rightX[y]\n\n            # if the xdist is 0, don't draw anything.\n            xdist = rx - lx\n            if xdist == 0.0:\n                continue\n\n            lxi = int(floor(lx))\n            rxi = int(floor(rx))\n            rc = rightColors[y]\n            lc = leftColors[y]\n\n            r = lc.r\n            g = lc.g\n            b = lc.b\n            rdelta = (rc.r - r) / xdist\n            gdelta = (rc.g - g) / xdist\n            bdelta = (rc.b - b) / xdist\n\n            # Draw 2 more pixels on left side for smoothing\n            for x in range(lxi - 2, lxi):\n                buf.setPixel(x, y, QtGui.qRgb(int(r), int(g), int(b)))\n\n            # Inner loop 2. Draws the line from left to right.\n            for x in range(lxi, rxi):\n                buf.setPixel(x, y, QtGui.qRgb(int(r), int(g), int(b)))\n                r += rdelta\n                g += gdelta\n                b += bdelta\n\n            # Draw 2 more pixels on right side for smoothing\n            for x in range(rxi, rxi + 3):\n                buf.setPixel(x, y, QtGui.qRgb(int(r), int(g), int(b)))\n\n    def _radius_at(self, pos, rect):\n        mousexdist = pos.x() - float(rect.center().x())\n        mouseydist = pos.y() - float(rect.center().y())\n        return sqrt(mousexdist ** 2 + mouseydist ** 2)\n\n    def _angle_at(self, pos, rect):\n        mousexdist = pos.x() - float(rect.center().x())\n        mouseydist = pos.y() - float(rect.center().y())\n        mouserad = sqrt(mousexdist ** 2 + mouseydist ** 2)\n        if mouserad == 0.0:\n            return 0.0\n\n        angle = acos(mousexdist / mouserad)\n        if mouseydist &gt;= 0:\n            angle = TWOPI - angle\n\n        return angle\n\n    def _point_from_color(self, col):\n        # Simplifications for the corner cases.\n        if col == QtCore.Qt.black:\n            return self.point_b\n        elif col == QtCore.Qt.white:\n            return self.point_c\n\n        # Find the x and y slopes\n        ab_deltax = self.point_b.x() - self.point_a.x()\n        ab_deltay = self.point_b.y() - self.point_a.y()\n        bc_deltax = self.point_c.x() - self.point_b.x()\n        bc_deltay = self.point_c.y() - self.point_b.y()\n        ac_deltax = self.point_c.x() - self.point_a.x()\n        ac_deltay = self.point_c.y() - self.point_a.y()\n\n        # Extract the h,s,v values of col.\n        _, sat, val, _ = col.getHsv()\n\n        # Find the line that passes through the triangle where the value\n        # is equal to our color's value.\n        p1 = self.point_a.x() + (ab_deltax * float(255 - val)) / 255.0\n        q1 = self.point_a.y() + (ab_deltay * float(255 - val)) / 255.0\n        p2 = self.point_b.x() + (bc_deltax * float(val)) / 255.0\n        q2 = self.point_b.y() + (bc_deltay * float(val)) / 255.0\n\n        # Find the line that passes through the triangle where the\n        # saturation is equal to our color's value.\n        p3 = self.point_a.x() + (ac_deltax * float(255 - sat)) / 255.0\n        q3 = self.point_a.y() + (ac_deltay * float(255 - sat)) / 255.0\n        p4 = self.point_b.x()\n        q4 = self.point_b.y()\n\n        # Find the intersection between these lines.\n        if p1 != p2:\n            a = (q2 - q1) / (p2 - p1)\n            c = (q4 - q3) / (p4 - p3)\n            b = q1 - a * p1\n            d = q3 - c * p3\n\n            x = (d - b) / (a - c)\n            y = a * x + b\n        else:\n            x = p1\n            p4_p3 = p4 - p3\n            if p4_p3 == 0:\n                y = 0\n            else:\n                y = q3 + (x - p3) * (q4 - q3) / p4_p3\n\n        return QtCore.QPointF(x, y)\n\n    def _color_from_point(self, p):\n        # Find the outer radius of the hue gradient.\n        size_w = self.contentsRect().width()\n        size_h = self.contentsRect().height()\n        if size_w &lt; size_h:\n            size = size_w\n        else:\n            size = size_h\n        outer_radius = (size - 1) / 2\n\n        # Find the center coordinates\n        cx = float(self.contentsRect().center().x())\n        cy = float(self.contentsRect().center().y())\n\n        # Find the a, b and c from their angles, the center of the rect\n        # and the radius of the hue gradient donut.\n        inner_radius = outer_radius - (outer_radius / self.inner_radius_ratio)\n        pa = QtCore.QPointF(\n            cx + (cos(self.angle_a) * inner_radius),\n            cy - (sin(self.angle_a) * inner_radius)\n        )\n        pb = QtCore.QPointF(\n            cx + (cos(self.angle_b) * inner_radius),\n            cy - (sin(self.angle_b) * inner_radius)\n        )\n        pc = QtCore.QPointF(\n            cx + (cos(self.angle_c) * inner_radius),\n            cy - (sin(self.angle_c) * inner_radius)\n        )\n\n        # Find the hue value from the angle of the 'a' point.\n        angle = self.angle_a - PI / 2.0\n        if angle &lt; 0:\n            angle += TWOPI\n        hue = (\n            360\n            - int(floor((360.0 * angle) / TWOPI))\n            - self.hue_offset\n        ) % 360\n\n        # Create the color of the 'a' corner point. We know that b is\n        # black and c is white.\n        color = QtGui.QColor()\n        color.setHsv(hue, 255, 255)\n\n        # See also drawTrigon(), which basically does exactly the same to\n        # determine all colors in the trigon.\n        p1 = Vertex(color, pa)\n        p2 = Vertex(QtCore.Qt.black, pb)\n        p3 = Vertex(QtCore.Qt.white, pc)\n\n        # Make sure p1 is above p2, which is above p3.\n        if p1.point.y() &gt; p2.point.y():\n            p1, p2 = p2, p1\n        if p1.point.y() &gt; p3.point.y():\n            p1, p3 = p3, p1\n        if p2.point.y() &gt; p3.point.y():\n            p2, p3 = p3, p2\n\n        # Find the slopes of all edges in the trigon. All the three y\n        # deltas here are positive because of the above sorting.\n        p1p2ydist = p2.point.y() - p1.point.y()\n        p1p3ydist = p3.point.y() - p1.point.y()\n        p2p3ydist = p3.point.y() - p2.point.y()\n        p1p2xdist = p2.point.x() - p1.point.x()\n        p1p3xdist = p3.point.x() - p1.point.x()\n        p2p3xdist = p3.point.x() - p2.point.x()\n\n        # The first x delta decides wether we have a lefty or a righty\n        # trigon. A lefty trigon has its tallest edge on the right hand\n        # side of the trigon. The righty trigon has it on its left side.\n        # This property determines wether the left or the right set of x\n        # coordinates will be continuous.\n        lefty = p1p2xdist &lt; 0\n\n        # Find whether the selector's y is in the first or second shorty,\n        # counting from the top and downwards. This is used to find the\n        # color at the selector point.\n        firstshorty = (p.y() &gt;= p1.point.y() and p.y() &lt; p2.point.y())\n\n        # From the y value of the selector's position, find the left and\n        # right x values.\n        if lefty:\n            if firstshorty:\n                if (floor(p1p2ydist) != 0.0):\n                    leftx = (\n                        p1.point.x()\n                        + ((p1p2xdist * (p.y() - p1.point.y())) / p1p2ydist)\n                    )\n                else:\n                    leftx = min(p1.point.x(), p2.point.x())\n\n            else:\n                if (floor(p2p3ydist) != 0.0):\n                    leftx = (\n                        p2.point.x()\n                        + (p2p3xdist * (p.y() - p2.point.y())) / p2p3ydist\n                    )\n                else:\n                    leftx = min(p2.point.x(), p3.point.x())\n\n            rightx = (\n                p1.point.x()\n                + ((p1p3xdist * (p.y() - p1.point.y())) / p1p3ydist)\n            )\n        else:\n            leftx = (\n                p1.point.x()\n                + ((p1p3xdist * (p.y() - p1.point.y())) / p1p3ydist)\n            )\n            if firstshorty:\n                if floor(p1p2ydist) != 0.0:\n                    rightx = (\n                        p1.point.x()\n                        + ((p1p2xdist * (p.y() - p1.point.y())) / p1p2ydist)\n                    )\n                else:\n                    rightx = max(p1.point.x(), p2.point.x())\n\n            else:\n                if floor(p2p3ydist) != 0.0:\n                    rightx = (\n                        p2.point.x()\n                        + ((p2p3xdist * (p.y() - p2.point.y())) / p2p3ydist)\n                    )\n                else:\n                    rightx = max(p2.point.x(), p3.point.x())\n\n        # Find the r,g,b values of the points on the trigon's edges that\n        # are to the left and right of the selector.\n        if firstshorty:\n            if floor(p1p2ydist) != 0.0:\n                p_p1_ratio = (p.y() - p1.point.y()) / p1p2ydist\n                p2_p_ratio = (p2.point.y() - p.y()) / p1p2ydist\n                rshort = (p2.color.r * p_p1_ratio) + (p1.color.r * p2_p_ratio)\n                gshort = (p2.color.g * p_p1_ratio) + (p1.color.g * p2_p_ratio)\n                bshort = (p2.color.b * p_p1_ratio) + (p1.color.b * p2_p_ratio)\n            elif lefty:\n                if p1.point.x() &lt;= p2.point.x():\n                    rshort = p1.color.r\n                    gshort = p1.color.g\n                    bshort = p1.color.b\n                else:\n                    rshort = p2.color.r\n                    gshort = p2.color.g\n                    bshort = p2.color.b\n\n            else:\n                if p1.point.x() &gt; p2.point.x():\n                    rshort = p1.color.r\n                    gshort = p1.color.g\n                    bshort = p1.color.b\n                else:\n                    rshort = p2.color.r\n                    gshort = p2.color.g\n                    bshort = p2.color.b\n\n        else:\n            if floor(p2p3ydist) != 0.0:\n                p_p2_ratio = (p.y() - p2.point.y()) / p2p3ydist\n                p3_p_ratio = (p3.point.y() - p.y()) / p2p3ydist\n                rshort = (p3.color.r * p_p2_ratio) + (p2.color.r * p3_p_ratio)\n                gshort = (p3.color.g * p_p2_ratio) + (p2.color.g * p3_p_ratio)\n                bshort = (p3.color.b * p_p2_ratio) + (p2.color.b * p3_p_ratio)\n            elif lefty:\n                if p2.point.x() &lt;= p3.point.x():\n                    rshort = p2.color.r\n                    gshort = p2.color.g\n                    bshort = p2.color.b\n                else:\n                    rshort = p3.color.r\n                    gshort = p3.color.g\n                    bshort = p3.color.b\n\n            else:\n                if p2.point.x() &gt; p3.point.x():\n                    rshort = p2.color.r\n                    gshort = p2.color.g\n                    bshort = p2.color.b\n                else:\n                    rshort = p3.color.r\n                    gshort = p3.color.g\n                    bshort = p3.color.b\n\n        # p1p3ydist is never 0\n        p_p1_ratio = (p.y() - p1.point.y()) / p1p3ydist\n        p3_p_ratio = (p3.point.y() - p.y()) / p1p3ydist\n        rlong = (p3.color.r * p_p1_ratio) + (p1.color.r * p3_p_ratio)\n        glong = (p3.color.g * p_p1_ratio) + (p1.color.g * p3_p_ratio)\n        blong = (p3.color.b * p_p1_ratio) + (p1.color.b * p3_p_ratio)\n\n        # rshort,gshort,bshort is the color on one of the shortys.\n        # rlong,glong,blong is the color on the longy. So depending on\n        # wether we have a lefty trigon or not, we can determine which\n        # colors are on the left and right edge.\n        if lefty:\n            rl = rshort\n            gl = gshort\n            bl = bshort\n            rr = rlong\n            gr = glong\n            br = blong\n        else:\n            rl = rlong\n            gl = glong\n            bl = blong\n            rr = rshort\n            gr = gshort\n            br = bshort\n\n        # Find the distance from the left x to the right x (xdist). Then\n        # find the distances from the selector to each of these (saxdist\n        # and saxdist2). These distances are used to find the color at\n        # the selector.\n        xdist = rightx - leftx\n        saxdist = p.x() - leftx\n        saxdist2 = xdist - saxdist\n\n        # Now determine the r,g,b values of the selector using a linear\n        # approximation.\n        if xdist != 0.0:\n            r = (saxdist2 * rl / xdist) + (saxdist * rr / xdist)\n            g = (saxdist2 * gl / xdist) + (saxdist * gr / xdist)\n            b = (saxdist2 * bl / xdist) + (saxdist * br / xdist)\n        else:\n            # In theory, the left and right color will be equal here. But\n            # because of the loss of precision, we get an error on both\n            # colors. The best approximation we can get is from adding\n            # the two errors, which in theory will eliminate the error\n            # but in practise will only minimize it.\n            r = (rl + rr) / 2\n            g = (gl + gr) / 2\n            b = (bl + br) / 2\n\n        # Now floor the color components and fit them into proper\n        # boundaries. This again is to compensate for the error caused by\n        # loss of precision.\n        ri = int(floor(r))\n        gi = int(floor(g))\n        bi = int(floor(b))\n        if ri &lt; 0:\n            ri = 0\n        elif ri &gt; 255:\n            ri = 255\n\n        if gi &lt; 0:\n            gi = 0\n        elif gi &gt; 255:\n            gi = 255\n\n        if bi &lt; 0:\n            bi = 0\n        elif bi &gt; 255:\n            bi = 255\n\n        # Voila, we have the color at the point of the selector.\n        return QtGui.QColor(ri, gi, bi)\n\n    def paint_bg(self):\n        bg_image = QtGui.QPixmap(self.contentsRect().size())\n        bg_image.fill(QtCore.Qt.transparent)\n        self.bg_image = bg_image\n\n        painter = QtGui.QPainter(self.bg_image)\n\n        painter.setRenderHint(QtGui.QPainter.Antialiasing)\n\n        hue_gradient = QtGui.QConicalGradient(\n            bg_image.rect().center(), 90 - self.hue_offset\n        )\n        sat_val_gradient = QtGui.QConicalGradient(\n            bg_image.rect().center(), 90 - self.hue_offset\n        )\n\n        hue_color = QtGui.QColor()\n        sat_val_color = QtGui.QColor()\n        _, sat, val, _ = self.cur_color.getHsv()\n\n        for idx, hue in self._hue_circle_range:\n            hue_color.setHsv(hue, 255, 255)\n            sat_val_color.setHsv(hue, sat, val)\n\n            hue_gradient.setColorAt(idx, hue_color)\n            sat_val_gradient.setColorAt(idx, sat_val_color)\n\n        inner_radius = self.outer_radius - (\n            self.outer_radius / self.inner_radius_ratio\n        )\n        half_radius = self.outer_radius - (\n            (self.outer_radius - inner_radius) / 2\n        )\n\n        hue_inner_radius_rect = QtCore.QRectF(\n            bg_image.rect().center().x() - inner_radius,\n            bg_image.rect().center().y() - inner_radius,\n            inner_radius * 2 + 1,\n            inner_radius * 2 + 1\n        )\n        hue_outer_radius_rect = QtCore.QRectF(\n            bg_image.rect().center().x() - half_radius - 1,\n            bg_image.rect().center().y() - half_radius - 1,\n            half_radius * 2 + 3,\n            half_radius * 2 + 3\n        )\n        sat_val_inner_radius_rect = QtCore.QRectF(\n            bg_image.rect().center().x() - half_radius,\n            bg_image.rect().center().y() - half_radius,\n            half_radius * 2 + 1,\n            half_radius * 2 + 1\n        )\n        sat_val_outer_radius_rect = QtCore.QRectF(\n            bg_image.rect().center().x() - self.outer_radius,\n            bg_image.rect().center().y() - self.outer_radius,\n            self.outer_radius * 2 + 1,\n            self.outer_radius * 2 + 1\n        )\n        hue_path = QtGui.QPainterPath()\n        hue_path.addEllipse(hue_inner_radius_rect)\n        hue_path.addEllipse(hue_outer_radius_rect)\n\n        sat_val_path = QtGui.QPainterPath()\n        sat_val_path.addEllipse(sat_val_inner_radius_rect)\n        sat_val_path.addEllipse(sat_val_outer_radius_rect)\n\n        painter.save()\n        painter.setClipPath(hue_path)\n        painter.fillRect(self.bg_image.rect(), hue_gradient)\n        painter.restore()\n\n        painter.save()\n        painter.setClipPath(sat_val_path)\n        painter.fillRect(self.bg_image.rect(), sat_val_gradient)\n        painter.restore()\n\n        painter.end()\n\n    @staticmethod\n    def vlen(x, y):\n        return sqrt((x ** 2) + (y ** 2))\n\n    @staticmethod\n    def vprod(x1, y1, x2, y2):\n        return x1 * x2 + y1 * y2\n\n    @staticmethod\n    def _angle_between_angles(p, a1, a2):\n        if a1 &gt; a2:\n            a2 += TWOPI\n            if p &lt; PI:\n                p += TWOPI\n\n        return p &gt;= a1 and p &lt; a2\n\n    @staticmethod\n    def _point_above_point(x, y, px, py, ax, ay, bx, by):\n        floored_ax = floor(ax)\n        floored_bx = floor(bx)\n        floored_ay = floor(ay)\n        floored_by = floor(by)\n\n        if floored_ax == floored_bx:\n            # line is vertical\n            if floored_ay &lt; floored_by:\n                return x &lt; ax\n            elif floored_ay &gt; floored_by:\n                return x &gt; ax\n            return not (x == ax and y == ay)\n\n        if floored_ax &gt; floored_bx:\n            if floored_ay &lt; floored_by:\n                # line is draw upright-to-downleft\n                return (floor(x) &lt; floor(px) or floor(y) &lt; floor(py))\n            elif floored_ay &gt; floored_by:\n                # line is draw downright-to-upleft\n                return (floor(x) &gt; floor(px) or floor(y) &lt; floor(py))\n            # line is flat horizontal\n            return y &lt; ay\n\n        if floored_ay &lt; floored_by:\n            # line is draw upleft-to-downright\n            return (floor(x) &lt; floor(px) or floor(y) &gt; floor(py))\n        elif floored_ay &gt; floored_by:\n            # line is draw downleft-to-upright\n            return (floor(x) &gt; floor(px) or floor(y) &gt; floor(py))\n        # line is flat horizontal\n        return y &gt; ay\n\n    @staticmethod\n    def _point_in_line(x, y, ax, ay, bx, by):\n        if ax &gt; bx:\n            if ay &lt; by:\n                # line is draw upright-to-downleft\n\n                # if (x,y) is in on or above the upper right point,\n                # return -1.\n                if y &lt;= ay and x &gt;= ax:\n                    return -1\n\n                # if (x,y) is in on or below the lower left point,\n                # return 1.\n                if y &gt;= by and x &lt;= bx:\n                    return 1\n            else:\n                # line is draw downright-to-upleft\n\n                # If the line is flat, only use the x coordinate.\n                if floor(ay) == floor(by):\n                    # if (x is to the right of the rightmost point,\n                    # return -1. otherwise if x is to the left of the\n                    # leftmost point, return 1.\n                    if x &gt;= ax:\n                        return -1\n                    elif x &lt;= bx:\n                        return 1\n                else:\n                    # if (x,y) is on or below the lower right point,\n                    # return -1.\n                    if y &gt;= ay and x &gt;= ax:\n                        return -1\n\n                    # if (x,y) is on or above the upper left point, return 1.\n                    if y &lt;= by and x &lt;= bx:\n                        return 1\n        else:\n            if ay &lt; by:\n                # line is draw upleft-to-downright\n\n                # If (x,y) is on or above the upper left point, return -1.\n                if y &lt;= ay and x &lt;= ax:\n                    return -1\n\n                # If (x,y) is on or below the lower right point, return 1.\n                if y &gt;= by and x &gt;= bx:\n                    return 1\n            else:\n                # line is draw downleft-to-upright\n\n                # If the line is flat, only use the x coordinate.\n                if floor(ay) == floor(by):\n                    if x &lt;= ax:\n                        return -1\n                    elif x &gt;= bx:\n                        return 1\n                else:\n                    # If (x,y) is on or below the lower left point, return -1.\n                    if y &gt;= ay and x &lt;= ax:\n                        return -1\n\n                    # If (x,y) is on or above the upper right point, return 1.\n                    if y &lt;= by and x &gt;= bx:\n                        return 1\n\n        # No tests proved that (x,y) was outside [(ax,ay),(bx,by)], so we\n        # assume it's inside the line's bounds.\n        return 0\n\n    def _move_point_to_triangle(self, x, y, a, b, c):\n        # Let v1A be the vector from (x,y) to a.\n        # Let v2A be the vector from a to b.\n        # Find the angle alphaA between v1A and v2A.\n        v1xA = x - a.point.x()\n        v1yA = y - a.point.y()\n        v2xA = b.point.x() - a.point.x()\n        v2yA = b.point.y() - a.point.y()\n        vpA = self.vprod(v1xA, v1yA, v2xA, v2yA)\n        cosA = vpA / (self.vlen(v1xA, v1yA) * self.vlen(v2xA, v2yA))\n        alphaA = acos(cosA)\n\n        # Let v1B be the vector from x to b.\n        # Let v2B be the vector from b to c.\n        v1xB = x - b.point.x()\n        v1yB = y - b.point.y()\n        v2xB = c.point.x() - b.point.x()\n        v2yB = c.point.y() - b.point.y()\n        vpB = self.vprod(v1xB, v1yB, v2xB, v2yB)\n        cosB = vpB / (self.vlen(v1xB, v1yB) * self.vlen(v2xB, v2yB))\n        alphaB = acos(cosB)\n\n        # Let v1C be the vector from x to c.\n        # Let v2C be the vector from c back to a.\n        v1xC = x - c.point.x()\n        v1yC = y - c.point.y()\n        v2xC = a.point.x() - c.point.x()\n        v2yC = a.point.y() - c.point.y()\n        vpC = self.vprod(v1xC, v1yC, v2xC, v2yC)\n        cosC = vpC / (self.vlen(v1xC, v1yC) * self.vlen(v2xC, v2yC))\n        alphaC = acos(cosC)\n\n        # Find the radian angles between the (1,0) vector and the points\n        # A, B, C and (x,y). Use this information to determine which of\n        # the edges we should project (x,y) onto.\n        angleA = self._angle_at(a.point, self.contentsRect())\n        angleB = self._angle_at(b.point, self.contentsRect())\n        angleC = self._angle_at(c.point, self.contentsRect())\n        angleP = self._angle_at(QtCore.QPointF(x, y), self.contentsRect())\n\n        # If (x,y) is in the a-b area, project onto the a-b vector.\n        if self._angle_between_angles(angleP, angleA, angleB):\n            # Find the distance from (x,y) to a. Then use the slope of\n            # the a-b vector with this distance and the angle between a-b\n            # and a-(x,y) to determine the point of intersection of the\n            # perpendicular projection from (x,y) onto a-b.\n            pdist = sqrt(\n                ((x - a.point.x()) ** 2) + ((y - a.point.y()) ** 2)\n            )\n\n            # the length of all edges is always &gt; 0\n            p0x = (\n                a.point.x()\n                + ((b.point.x() - a.point.x()) / self.vlen(v2xB, v2yB))\n                * cos(alphaA) * pdist\n            )\n            p0y = (\n                a.point.y()\n                + ((b.point.y() - a.point.y()) / self.vlen(v2xB, v2yB))\n                * cos(alphaA) * pdist\n            )\n\n            # If (x,y) is above the a-b line, which basically means it's\n            # outside the triangle, then return its projection onto a-b.\n            if self._point_above_point(\n                x, y,\n                p0x, p0y,\n                a.point.x(), a.point.y(),\n                b.point.x(), b.point.y()\n            ):\n                # If the projection is \"outside\" a, return a. If it is\n                # outside b, return b. Otherwise return the projection.\n                n = self._point_in_line(\n                    p0x, p0y,\n                    a.point.x(), a.point.y(),\n                    b.point.x(), b.point.y()\n                )\n                if n &lt; 0:\n                    return a.point\n                elif n &gt; 0:\n                    return b.point\n\n                return QtCore.QPointF(p0x, p0y)\n\n        elif self._angle_between_angles(angleP, angleB, angleC):\n            # If (x,y) is in the b-c area, project onto the b-c vector.\n            pdist = sqrt(\n                ((x - b.point.x()) ** 2) + ((y - b.point.y()) ** 2)\n            )\n\n            # the length of all edges is always &gt; 0\n            p0x = (\n                b.point.x()\n                + ((c.point.x() - b.point.x()) / self.vlen(v2xC, v2yC))\n                * cos(alphaB) * pdist\n            )\n            p0y = (\n                b.point.y()\n                + ((c.point.y() - b.point.y()) / self.vlen(v2xC, v2yC))\n                * cos(alphaB)\n                * pdist\n            )\n\n            if self._point_above_point(\n                x, y,\n                p0x, p0y,\n                b.point.x(), b.point.y(),\n                c.point.x(), c.point.y()\n            ):\n                n = self._point_in_line(\n                    p0x, p0y,\n                    b.point.x(), b.point.y(),\n                    c.point.x(), c.point.y()\n                )\n                if n &lt; 0:\n                    return b.point\n                elif n &gt; 0:\n                    return c.point\n                return QtCore.QPointF(p0x, p0y)\n\n        elif self._angle_between_angles(angleP, angleC, angleA):\n            # If (x,y) is in the c-a area, project onto the c-a vector.\n            pdist = sqrt(\n                ((x - c.point.x()) ** 2) + ((y - c.point.y()) ** 2)\n            )\n\n            # the length of all edges is always &gt; 0\n            p0x = (\n                c.point.x()\n                + ((a.point.x() - c.point.x()) / self.vlen(v2xA, v2yA))\n                * cos(alphaC)\n                * pdist\n            )\n            p0y = (\n                c.point.y()\n                + ((a.point.y() - c.point.y()) / self.vlen(v2xA, v2yA))\n                * cos(alphaC) * pdist\n            )\n\n            if self._point_above_point(\n                x, y,\n                p0x, p0y,\n                c.point.x(), c.point.y(),\n                a.point.x(), a.point.y()\n            ):\n                n = self._point_in_line(\n                    p0x, p0y,\n                    c.point.x(), c.point.y(),\n                    a.point.x(), a.point.y()\n                )\n                if n &lt; 0:\n                    return c.point\n                elif n &gt; 0:\n                    return a.point\n                return QtCore.QPointF(p0x, p0y)\n\n        # (x,y) is inside the triangle (inside a-b, b-c and a-c).\n        return QtCore.QPointF(x, y)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/color_widgets/color_view.html","title":"color_view","text":""},{"location":"autoapi/client/ayon_core/tools/utils/images/index.html","title":"images","text":""},{"location":"autoapi/client/ayon_core/tools/utils/images/index.html#client.ayon_core.tools.utils.images.get_icon","title":"<code>get_icon(filename)</code>","text":"<p>Load image from './images' as QIcon.</p> <p>Returns:</p> Type Description <p>Union[QtGui.QIcon, None]: QIcon or None if not found.</p> Source code in <code>client/ayon_core/tools/utils/images/__init__.py</code> <pre><code>def get_icon(filename):\n    \"\"\"Load image from './images' as QIcon.\n\n    Returns:\n        Union[QtGui.QIcon, None]: QIcon or None if not found.\n    \"\"\"\n\n    pix = get_pixmap(filename)\n    if pix:\n        return QtGui.QIcon(pix)\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/images/index.html#client.ayon_core.tools.utils.images.get_image","title":"<code>get_image(filename)</code>","text":"<p>Load image from './images' as QImage.</p> <p>Returns:</p> Type Description <p>Union[QtGui.QImage, None]: QImage or None if not found.</p> Source code in <code>client/ayon_core/tools/utils/images/__init__.py</code> <pre><code>def get_image(filename):\n    \"\"\"Load image from './images' as QImage.\n\n    Returns:\n        Union[QtGui.QImage, None]: QImage or None if not found.\n    \"\"\"\n\n    path = get_image_path(filename)\n    if path:\n        return QtGui.QImage(path)\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/images/index.html#client.ayon_core.tools.utils.images.get_image_path","title":"<code>get_image_path(filename)</code>","text":"<p>Get image path from './images'.</p> <p>Returns:</p> Type Description <p>Union[str, None]: Path to image file or None if not found.</p> Source code in <code>client/ayon_core/tools/utils/images/__init__.py</code> <pre><code>def get_image_path(filename):\n    \"\"\"Get image path from './images'.\n\n    Returns:\n        Union[str, None]: Path to image file or None if not found.\n    \"\"\"\n\n    path = os.path.join(IMAGES_DIR, filename)\n    if os.path.exists(path):\n        return path\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/utils/images/index.html#client.ayon_core.tools.utils.images.get_pixmap","title":"<code>get_pixmap(filename)</code>","text":"<p>Load image from './images' as QPixmap.</p> <p>Returns:</p> Type Description <p>Union[QtGui.QPixmap, None]: QPixmap or None if not found.</p> Source code in <code>client/ayon_core/tools/utils/images/__init__.py</code> <pre><code>def get_pixmap(filename):\n    \"\"\"Load image from './images' as QPixmap.\n\n    Returns:\n        Union[QtGui.QPixmap, None]: QPixmap or None if not found.\n    \"\"\"\n\n    path = get_image_path(filename)\n    if path:\n        return QtGui.QPixmap(path)\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfile_template_build/index.html","title":"workfile_template_build","text":""},{"location":"autoapi/client/ayon_core/tools/workfile_template_build/index.html#client.ayon_core.tools.workfile_template_build.open_template_ui","title":"<code>open_template_ui(builder, main_window)</code>","text":"<p>Open template from <code>builder</code></p> <p>Asks user about overwriting current scene and feedback exceptions.</p> Source code in <code>client/ayon_core/tools/workfile_template_build/lib.py</code> <pre><code>def open_template_ui(builder, main_window):\n    \"\"\"Open template from `builder`\n\n    Asks user about overwriting current scene and feedback exceptions.\n    \"\"\"\n    result = QtWidgets.QMessageBox.question(\n        main_window,\n        \"Opening template\",\n        \"Caution! You will lose unsaved changes.\\nDo you want to continue?\",\n        QtWidgets.QMessageBox.Yes | QtWidgets.QMessageBox.No\n    )\n    if result == QtWidgets.QMessageBox.Yes:\n        try:\n            builder.open_template()\n        except Exception:\n            show_message_dialog(\n                title=\"Template Load Failed\",\n                message=\"\".join(traceback.format_exc()),\n                parent=main_window,\n                level=\"critical\"\n            )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfile_template_build/lib.html","title":"lib","text":""},{"location":"autoapi/client/ayon_core/tools/workfile_template_build/lib.html#client.ayon_core.tools.workfile_template_build.lib.open_template_ui","title":"<code>open_template_ui(builder, main_window)</code>","text":"<p>Open template from <code>builder</code></p> <p>Asks user about overwriting current scene and feedback exceptions.</p> Source code in <code>client/ayon_core/tools/workfile_template_build/lib.py</code> <pre><code>def open_template_ui(builder, main_window):\n    \"\"\"Open template from `builder`\n\n    Asks user about overwriting current scene and feedback exceptions.\n    \"\"\"\n    result = QtWidgets.QMessageBox.question(\n        main_window,\n        \"Opening template\",\n        \"Caution! You will lose unsaved changes.\\nDo you want to continue?\",\n        QtWidgets.QMessageBox.Yes | QtWidgets.QMessageBox.No\n    )\n    if result == QtWidgets.QMessageBox.Yes:\n        try:\n            builder.open_template()\n        except Exception:\n            show_message_dialog(\n                title=\"Template Load Failed\",\n                message=\"\".join(traceback.format_exc()),\n                parent=main_window,\n                level=\"critical\"\n            )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfile_template_build/window.html","title":"window","text":""},{"location":"autoapi/client/ayon_core/tools/workfiles/index.html","title":"workfiles","text":""},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html","title":"abstract","text":""},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesBackend","title":"<code>AbstractWorkfilesBackend</code>","text":"<p>               Bases: <code>AbstractWorkfilesCommon</code></p> Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>class AbstractWorkfilesBackend(AbstractWorkfilesCommon):\n    # Current context\n    @abstractmethod\n    def get_host_name(self):\n        \"\"\"Name of host.\n\n        Returns:\n            str: Name of host.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_current_project_name(self):\n        \"\"\"Project name from current context of host.\n\n        Returns:\n            str: Name of project.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_current_folder_id(self):\n        \"\"\"Folder id from current context of host.\n\n        Returns:\n            Union[str, None]: Folder id or None if host does not have\n                any context.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_current_task_name(self):\n        \"\"\"Task name from current context of host.\n\n        Returns:\n            Union[str, None]: Task name or None if host does not have\n                any context.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_current_workfile(self):\n        \"\"\"Current workfile from current context of host.\n\n        Returns:\n            Union[str, None]: Path to workfile or None if host does\n                not have opened specific file.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_project_settings(self, project_name: str | None) -&gt; dict:\n        pass\n\n    @property\n    @abstractmethod\n    def project_anatomy(self):\n        \"\"\"Project anatomy for current project.\n\n        Returns:\n            Anatomy: Project anatomy.\n\n        \"\"\"\n        pass\n\n    @property\n    @abstractmethod\n    def project_settings(self):\n        \"\"\"Project settings for current project.\n\n        Returns:\n            dict[str, Any]: Project settings.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_project_entity(self, project_name):\n        \"\"\"Get project entity by name.\n\n        Args:\n            project_name (str): Project name.\n\n        Returns:\n            dict[str, Any]: Project entity data.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_folder_entity(self, project_name, folder_id):\n        \"\"\"Get folder entity by id.\n\n        Args:\n            project_name (str): Project name.\n            folder_id (str): Folder id.\n\n        Returns:\n            dict[str, Any]: Folder entity data.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_task_entity(self, project_name, task_id):\n        \"\"\"Get task entity by id.\n\n        Args:\n            project_name (str): Project name.\n            task_id (str): Task id.\n\n        Returns:\n            dict[str, Any]: Task entity data.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_workfile_entities(self, task_id: str):\n        \"\"\"Workfile entities for given task.\n\n        Args:\n            task_id (str): Task id.\n\n        Returns:\n            list[dict[str, Any]]: List of workfile entities.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def emit_event(self, topic, data=None, source=None):\n        \"\"\"Emit event.\n\n        Args:\n            topic (str): Event topic used for callbacks filtering.\n            data (Optional[dict[str, Any]]): Event data.\n            source (Optional[str]): Event source.\n\n        \"\"\"\n        pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesBackend.project_anatomy","title":"<code>project_anatomy</code>  <code>abstractmethod</code> <code>property</code>","text":"<p>Project anatomy for current project.</p> <p>Returns:</p> Name Type Description <code>Anatomy</code> <p>Project anatomy.</p>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesBackend.project_settings","title":"<code>project_settings</code>  <code>abstractmethod</code> <code>property</code>","text":"<p>Project settings for current project.</p> <p>Returns:</p> Type Description <p>dict[str, Any]: Project settings.</p>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesBackend.emit_event","title":"<code>emit_event(topic, data=None, source=None)</code>  <code>abstractmethod</code>","text":"<p>Emit event.</p> <p>Parameters:</p> Name Type Description Default <code>topic</code> <code>str</code> <p>Event topic used for callbacks filtering.</p> required <code>data</code> <code>Optional[dict[str, Any]]</code> <p>Event data.</p> <code>None</code> <code>source</code> <code>Optional[str]</code> <p>Event source.</p> <code>None</code> Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>@abstractmethod\ndef emit_event(self, topic, data=None, source=None):\n    \"\"\"Emit event.\n\n    Args:\n        topic (str): Event topic used for callbacks filtering.\n        data (Optional[dict[str, Any]]): Event data.\n        source (Optional[str]): Event source.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesBackend.get_current_folder_id","title":"<code>get_current_folder_id()</code>  <code>abstractmethod</code>","text":"<p>Folder id from current context of host.</p> <p>Returns:</p> Type Description <p>Union[str, None]: Folder id or None if host does not have any context.</p> Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>@abstractmethod\ndef get_current_folder_id(self):\n    \"\"\"Folder id from current context of host.\n\n    Returns:\n        Union[str, None]: Folder id or None if host does not have\n            any context.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesBackend.get_current_project_name","title":"<code>get_current_project_name()</code>  <code>abstractmethod</code>","text":"<p>Project name from current context of host.</p> <p>Returns:</p> Name Type Description <code>str</code> <p>Name of project.</p> Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>@abstractmethod\ndef get_current_project_name(self):\n    \"\"\"Project name from current context of host.\n\n    Returns:\n        str: Name of project.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesBackend.get_current_task_name","title":"<code>get_current_task_name()</code>  <code>abstractmethod</code>","text":"<p>Task name from current context of host.</p> <p>Returns:</p> Type Description <p>Union[str, None]: Task name or None if host does not have any context.</p> Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>@abstractmethod\ndef get_current_task_name(self):\n    \"\"\"Task name from current context of host.\n\n    Returns:\n        Union[str, None]: Task name or None if host does not have\n            any context.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesBackend.get_current_workfile","title":"<code>get_current_workfile()</code>  <code>abstractmethod</code>","text":"<p>Current workfile from current context of host.</p> <p>Returns:</p> Type Description <p>Union[str, None]: Path to workfile or None if host does not have opened specific file.</p> Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>@abstractmethod\ndef get_current_workfile(self):\n    \"\"\"Current workfile from current context of host.\n\n    Returns:\n        Union[str, None]: Path to workfile or None if host does\n            not have opened specific file.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesBackend.get_folder_entity","title":"<code>get_folder_entity(project_name, folder_id)</code>  <code>abstractmethod</code>","text":"<p>Get folder entity by id.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>folder_id</code> <code>str</code> <p>Folder id.</p> required <p>Returns:</p> Type Description <p>dict[str, Any]: Folder entity data.</p> Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>@abstractmethod\ndef get_folder_entity(self, project_name, folder_id):\n    \"\"\"Get folder entity by id.\n\n    Args:\n        project_name (str): Project name.\n        folder_id (str): Folder id.\n\n    Returns:\n        dict[str, Any]: Folder entity data.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesBackend.get_host_name","title":"<code>get_host_name()</code>  <code>abstractmethod</code>","text":"<p>Name of host.</p> <p>Returns:</p> Name Type Description <code>str</code> <p>Name of host.</p> Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>@abstractmethod\ndef get_host_name(self):\n    \"\"\"Name of host.\n\n    Returns:\n        str: Name of host.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesBackend.get_project_entity","title":"<code>get_project_entity(project_name)</code>  <code>abstractmethod</code>","text":"<p>Get project entity by name.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <p>Returns:</p> Type Description <p>dict[str, Any]: Project entity data.</p> Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>@abstractmethod\ndef get_project_entity(self, project_name):\n    \"\"\"Get project entity by name.\n\n    Args:\n        project_name (str): Project name.\n\n    Returns:\n        dict[str, Any]: Project entity data.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesBackend.get_task_entity","title":"<code>get_task_entity(project_name, task_id)</code>  <code>abstractmethod</code>","text":"<p>Get task entity by id.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>task_id</code> <code>str</code> <p>Task id.</p> required <p>Returns:</p> Type Description <p>dict[str, Any]: Task entity data.</p> Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>@abstractmethod\ndef get_task_entity(self, project_name, task_id):\n    \"\"\"Get task entity by id.\n\n    Args:\n        project_name (str): Project name.\n        task_id (str): Task id.\n\n    Returns:\n        dict[str, Any]: Task entity data.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesBackend.get_workfile_entities","title":"<code>get_workfile_entities(task_id)</code>  <code>abstractmethod</code>","text":"<p>Workfile entities for given task.</p> <p>Parameters:</p> Name Type Description Default <code>task_id</code> <code>str</code> <p>Task id.</p> required <p>Returns:</p> Type Description <p>list[dict[str, Any]]: List of workfile entities.</p> Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>@abstractmethod\ndef get_workfile_entities(self, task_id: str):\n    \"\"\"Workfile entities for given task.\n\n    Args:\n        task_id (str): Task id.\n\n    Returns:\n        list[dict[str, Any]]: List of workfile entities.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesCommon","title":"<code>AbstractWorkfilesCommon</code>","text":"<p>               Bases: <code>ABC</code></p> Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>class AbstractWorkfilesCommon(ABC):\n    @abstractmethod\n    def is_host_valid(self):\n        \"\"\"Host is valid for workfiles tool work.\n\n        Returns:\n            bool: True if host is valid.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_workfile_extensions(self):\n        \"\"\"Get possible workfile extensions.\n\n        Defined by host implementation.\n\n        Returns:\n            Iterable[str]: List of extensions.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def is_save_enabled(self):\n        \"\"\"Is workfile save enabled.\n\n        Returns:\n            bool: True if save is enabled.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def set_save_enabled(self, enabled):\n        \"\"\"Enable or disabled workfile save.\n\n        Args:\n            enabled (bool): Enable save workfile when True.\n\n        \"\"\"\n        pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesCommon.get_workfile_extensions","title":"<code>get_workfile_extensions()</code>  <code>abstractmethod</code>","text":"<p>Get possible workfile extensions.</p> <p>Defined by host implementation.</p> <p>Returns:</p> Type Description <p>Iterable[str]: List of extensions.</p> Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>@abstractmethod\ndef get_workfile_extensions(self):\n    \"\"\"Get possible workfile extensions.\n\n    Defined by host implementation.\n\n    Returns:\n        Iterable[str]: List of extensions.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesCommon.is_host_valid","title":"<code>is_host_valid()</code>  <code>abstractmethod</code>","text":"<p>Host is valid for workfiles tool work.</p> <p>Returns:</p> Name Type Description <code>bool</code> <p>True if host is valid.</p> Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>@abstractmethod\ndef is_host_valid(self):\n    \"\"\"Host is valid for workfiles tool work.\n\n    Returns:\n        bool: True if host is valid.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesCommon.is_save_enabled","title":"<code>is_save_enabled()</code>  <code>abstractmethod</code>","text":"<p>Is workfile save enabled.</p> <p>Returns:</p> Name Type Description <code>bool</code> <p>True if save is enabled.</p> Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>@abstractmethod\ndef is_save_enabled(self):\n    \"\"\"Is workfile save enabled.\n\n    Returns:\n        bool: True if save is enabled.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesCommon.set_save_enabled","title":"<code>set_save_enabled(enabled)</code>  <code>abstractmethod</code>","text":"<p>Enable or disabled workfile save.</p> <p>Parameters:</p> Name Type Description Default <code>enabled</code> <code>bool</code> <p>Enable save workfile when True.</p> required Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>@abstractmethod\ndef set_save_enabled(self, enabled):\n    \"\"\"Enable or disabled workfile save.\n\n    Args:\n        enabled (bool): Enable save workfile when True.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesFrontend","title":"<code>AbstractWorkfilesFrontend</code>","text":"<p>               Bases: <code>AbstractWorkfilesCommon</code></p> <p>UI controller abstraction that is used for workfiles tool frontend.</p> <p>Abstraction to provide data for UI and to handle UI events.</p> <p>Provide access to abstract backend data, like folders and tasks. Cares about handling of selection, keep information about current UI selection and have ability to tell what selection should UI show.</p> <p>Selection is separated into 2 parts, first is what UI elements tell about selection, and second is what UI should show as selected.</p> Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>class AbstractWorkfilesFrontend(AbstractWorkfilesCommon):\n    \"\"\"UI controller abstraction that is used for workfiles tool frontend.\n\n    Abstraction to provide data for UI and to handle UI events.\n\n    Provide access to abstract backend data, like folders and tasks. Cares\n    about handling of selection, keep information about current UI selection\n    and have ability to tell what selection should UI show.\n\n    Selection is separated into 2 parts, first is what UI elements tell\n    about selection, and second is what UI should show as selected.\n    \"\"\"\n\n    @abstractmethod\n    def get_window_subtitle(self) -&gt; Optional[str]:\n        \"\"\"Get window subtitle.\n\n        Returns:\n            Optional[str]: Window subtitle.\n\n        \"\"\"\n\n    @abstractmethod\n    def register_event_callback(self, topic, callback):\n        \"\"\"Register event callback.\n\n        Listen for events with given topic.\n\n        Args:\n            topic (str): Name of topic.\n            callback (Callable): Callback that will be called when event\n                is triggered.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_user_items_by_name(self):\n        \"\"\"Get user items available on AYON server.\n\n        Returns:\n            Dict[str, UserItem]: User items by username.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_folder_type_items(self, project_name, sender=None):\n        \"\"\"Folder type items for a project.\n\n        This function may trigger events with topics\n        'projects.folder_types.refresh.started' and\n        'projects.folder_types.refresh.finished' which will contain 'sender'\n        value in data.\n        That may help to avoid re-refresh of items in UI elements.\n\n        Args:\n            project_name (str): Project name.\n            sender (str): Who requested folder type items.\n\n        Returns:\n            list[FolderTypeItem]: Folder type information.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_task_type_items(self, project_name, sender=None):\n        \"\"\"Task type items for a project.\n\n        This function may trigger events with topics\n        'projects.task_types.refresh.started' and\n        'projects.task_types.refresh.finished' which will contain 'sender'\n        value in data.\n        That may help to avoid re-refresh of items in UI elements.\n\n        Args:\n            project_name (str): Project name.\n            sender (str): Who requested task type items.\n\n        Returns:\n            list[TaskTypeItem]: Task type information.\n\n        \"\"\"\n        pass\n\n    # Host information\n    @abstractmethod\n    def get_workfile_extensions(self):\n        \"\"\"Each host can define extensions that can be used for workfile.\n\n        Returns:\n            List[str]: File extensions that can be used as workfile for\n                current host.\n\n        \"\"\"\n        pass\n\n    # Selection information\n    @abstractmethod\n    def get_selected_folder_id(self):\n        \"\"\"Currently selected folder id.\n\n        Returns:\n            Union[str, None]: Folder id or None if no folder is selected.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def set_selected_folder(self, folder_id):\n        \"\"\"Change selected folder.\n\n        This deselects currently selected task.\n\n        Args:\n            folder_id (Union[str, None]): Folder id or None if no folder\n                is selected.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_selected_task_id(self):\n        \"\"\"Currently selected task id.\n\n        Returns:\n            Union[str, None]: Task id or None if no folder is selected.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_selected_task_name(self):\n        \"\"\"Currently selected task name.\n\n        Returns:\n            Union[str, None]: Task name or None if no folder is selected.\n        \"\"\"\n\n        pass\n\n    @abstractmethod\n    def set_selected_task(self, task_id, task_name):\n        \"\"\"Change selected task.\n\n        Args:\n            task_id (Union[str, None]): Task id or None if no task\n                is selected.\n            task_name (Union[str, None]): Task name or None if no task\n                is selected.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_selected_workfile_path(self):\n        \"\"\"Currently selected workarea workile.\n\n        Returns:\n            Union[str, None]: Selected workfile path.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def set_selected_workfile_path(\n        self, rootless_path, path, workfile_entity_id\n    ):\n        \"\"\"Change selected workfile path.\n\n        Args:\n            rootless_path (Union[str, None]): Selected workfile rootless path.\n            path (Union[str, None]): Selected workfile path.\n            workfile_entity_id (Union[str, None]): Workfile entity id.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_selected_representation_id(self):\n        \"\"\"Currently selected workfile representation id.\n\n        Returns:\n            Union[str, None]: Representation id or None if no representation\n                is selected.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def set_selected_representation_id(self, representation_id):\n        \"\"\"Change selected representation.\n\n        Args:\n            representation_id (Union[str, None]): Selected workfile\n                representation id.\n\n        \"\"\"\n        pass\n\n    def get_selected_context(self):\n        \"\"\"Obtain selected context.\n\n        Returns:\n            dict[str, Union[str, None]]: Selected context.\n\n        \"\"\"\n        return {\n            \"folder_id\": self.get_selected_folder_id(),\n            \"task_id\": self.get_selected_task_id(),\n            \"task_name\": self.get_selected_task_name(),\n            \"workfile_path\": self.get_selected_workfile_path(),\n            \"representation_id\": self.get_selected_representation_id(),\n        }\n\n    # Expected selection\n    # - expected selection is used to restore selection after refresh\n    #   or when current context should be used\n    @abstractmethod\n    def set_expected_selection(\n        self,\n        folder_id,\n        task_name,\n        workfile_name=None,\n        representation_id=None\n    ):\n        \"\"\"Define what should be selected in UI.\n\n        Expected selection provide a way to define/change selection of\n        sequential UI elements. For example, if folder and task should be\n        selected a task element should wait until folder element has selected\n        folder.\n\n        Triggers 'expected_selection.changed' event.\n\n        Args:\n            folder_id (str): Folder id.\n            task_name (str): Task name.\n            workfile_name (Optional[str]): Workfile name. Used for workarea\n                files UI element.\n            representation_id (Optional[str]): Representation id. Used for\n                published filed UI element.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_expected_selection_data(self):\n        \"\"\"Data of expected selection.\n\n        TODOs:\n            Return defined object instead of dict.\n\n        Returns:\n            dict[str, Any]: Expected selection data.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def expected_folder_selected(self, folder_id):\n        \"\"\"Expected folder was selected in UI.\n\n        Args:\n            folder_id (str): Folder id which was selected.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def expected_task_selected(self, folder_id, task_name):\n        \"\"\"Expected task was selected in UI.\n\n        Args:\n            folder_id (str): Folder id under which task is.\n            task_name (str): Task name which was selected.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def expected_representation_selected(\n        self, folder_id, task_name, representation_id\n    ):\n        \"\"\"Expected representation was selected in UI.\n\n        Args:\n            folder_id (str): Folder id under which representation is.\n            task_name (str): Task name under which representation is.\n            representation_id (str): Representation id which was selected.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def expected_workfile_selected(self, folder_id, task_name, workfile_name):\n        \"\"\"Expected workfile was selected in UI.\n\n        Args:\n            folder_id (str): Folder id under which workfile is.\n            task_name (str): Task name under which workfile is.\n            workfile_name (str): Workfile filename which was selected.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def go_to_current_context(self):\n        \"\"\"Set expected selection to current context.\"\"\"\n\n        pass\n\n    # Model functions\n    @abstractmethod\n    def get_folder_items(self, project_name, sender):\n        \"\"\"Folder items to visualize project hierarchy.\n\n        This function may trigger events 'folders.refresh.started' and\n        'folders.refresh.finished' which will contain 'sender' value in data.\n        That may help to avoid re-refresh of folder items in UI elements.\n\n        Args:\n            project_name (str): Project name for which are folders requested.\n            sender (str): Who requested folder items.\n\n        Returns:\n            list[FolderItem]: Minimum possible information needed\n                for visualisation of folder hierarchy.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_task_items(self, project_name, folder_id, sender):\n        \"\"\"Task items.\n\n        This function may trigger events 'tasks.refresh.started' and\n        'tasks.refresh.finished' which will contain 'sender' value in data.\n        That may help to avoid re-refresh of task items in UI elements.\n\n        Args:\n            project_name (str): Project name for which are tasks requested.\n            folder_id (str): Folder ID for which are tasks requested.\n            sender (str): Who requested folder items.\n\n        Returns:\n            list[TaskItem]: Minimum possible information needed\n                for visualisation of tasks.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def has_unsaved_changes(self):\n        \"\"\"Has host unsaved change in currently running session.\n\n        Returns:\n            bool: Has unsaved changes.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_workarea_dir_by_context(self, folder_id, task_id):\n        \"\"\"Get workarea directory by context.\n\n        Args:\n            folder_id (str): Folder id.\n            task_id (str): Task id.\n\n        Returns:\n            str: Workarea directory.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_workarea_file_items(self, folder_id, task_name, sender=None):\n        \"\"\"Get workarea file items.\n\n        Args:\n            folder_id (str): Folder id.\n            task_name (str): Task name.\n            sender (Optional[str]): Who requested workarea file items.\n\n        Returns:\n            list[WorkfileInfo]: List of workarea file items.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_workarea_save_as_data(self, folder_id, task_id):\n        \"\"\"Prepare data for Save As operation.\n\n        Todos:\n            Return defined object instead of dict.\n\n        Args:\n            folder_id (str): Folder id.\n            task_id (str): Task id.\n\n        Returns:\n            dict[str, Any]: Data for Save As operation.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def fill_workarea_filepath(\n        self,\n        folder_id,\n        task_id,\n        extension,\n        use_last_version,\n        version,\n        comment,\n    ):\n        \"\"\"Calculate workfile path for passed context.\n\n        Args:\n            folder_id (str): Folder id.\n            task_id (str): Task id.\n            extension (str): File extension.\n            use_last_version (bool): Use last version.\n            version (int): Version used if 'use_last_version' if 'False'.\n            comment (str): User's comment (subversion).\n\n        Returns:\n            WorkareaFilepathResult: Result of the operation.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_published_file_items(self, folder_id: str, task_id: str):\n        \"\"\"Get published file items.\n\n        Args:\n            folder_id (str): Folder id.\n            task_id (Union[str, None]): Task id.\n\n        Returns:\n            list[PublishedWorkfileInfo]: List of published file items.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_published_workfile_info(\n        self,\n        folder_id: Optional[str],\n        representation_id: Optional[str],\n    ) -&gt; PublishedWorkfileWrap:\n        \"\"\"Get published workfile info by representation ID.\n\n        Args:\n            folder_id (Optional[str]): Folder id.\n            representation_id (Optional[str]): Representation id.\n\n        Returns:\n            PublishedWorkfileWrap: Published workfile info or None\n                if not found.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def get_workfile_info(self, folder_id, task_id, rootless_path):\n        \"\"\"Workfile info from database.\n\n        Args:\n            folder_id (str): Folder id.\n            task_id (str): Task id.\n            rootless_path (str): Workfile path.\n\n        Returns:\n            Optional[WorkfileInfo]: Workfile info or None if was passed\n                invalid context.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def save_workfile_info(\n        self,\n        task_id,\n        rootless_path,\n        version=None,\n        comment=None,\n        description=None,\n    ):\n        \"\"\"Save workfile info to database.\n\n        At this moment the only information which can be saved about\n            workfile is 'description'.\n\n        If value of 'version', 'comment' or 'description' is 'None' it is not\n            added/updated to entity.\n\n        Args:\n            task_id (str): Task id.\n            rootless_path (str): Rootless workfile path.\n            version (Optional[int]): Version of workfile.\n            comment (Optional[str]): User's comment (subversion).\n            description (Optional[str]): Workfile description.\n\n        \"\"\"\n        pass\n\n    # General commands\n    @abstractmethod\n    def reset(self):\n        \"\"\"Reset everything, models, ui etc.\n\n        Triggers 'controller.reset.started' event at the beginning and\n        'controller.reset.finished' at the end.\n\n        \"\"\"\n        pass\n\n    # Controller actions\n    @abstractmethod\n    def open_workfile(self, folder_id, task_id, filepath):\n        \"\"\"Open a workfile for context.\n\n        Args:\n            folder_id (str): Folder id.\n            task_id (str): Task id.\n            filepath (str): Workfile path.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def save_current_workfile(self):\n        \"\"\"Save state of current workfile.\"\"\"\n\n        pass\n\n    @abstractmethod\n    def save_as_workfile(\n        self,\n        folder_id,\n        task_id,\n        rootless_workdir,\n        workdir,\n        filename,\n        version,\n        comment,\n        description,\n    ):\n        \"\"\"Save current state of workfile to workarea.\n\n        Args:\n            folder_id (str): Folder id.\n            task_id (str): Task id.\n            rootless_workdir (str): Workarea directory.\n            filename (str): Workarea filename.\n            template_key (str): Template key used to get the workdir\n                and filename.\n            version (Optional[int]): Version of workfile.\n            comment (Optional[str]): User's comment (subversion).\n            description (Optional[str]): Workfile description.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def copy_workfile_representation(\n        self,\n        representation_id,\n        representation_filepath,\n        folder_id,\n        task_id,\n        workdir,\n        filename,\n        rootless_workdir,\n        version,\n        comment,\n        description,\n    ):\n        \"\"\"Action to copy published workfile representation to workarea.\n\n        Triggers 'copy_representation.started' event on start and\n        'copy_representation.finished' event with '{\"failed\": bool}'.\n\n        Args:\n            representation_id (str): Representation id.\n            representation_filepath (str): Path to representation file.\n            folder_id (str): Folder id.\n            task_id (str): Task id.\n            workdir (str): Workarea directory.\n            filename (str): Workarea filename.\n            rootless_workdir (str): Rootless workdir.\n            version (int): Workfile version.\n            comment (str): User's comment (subversion).\n            description (str): Description note.\n\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def duplicate_workfile(\n        self,\n        folder_id,\n        task_id,\n        src_filepath,\n        rootless_workdir,\n        workdir,\n        filename,\n        description,\n        version,\n        comment\n    ):\n        \"\"\"Duplicate workfile.\n\n        Workfiles is not opened when done.\n\n        Args:\n            folder_id (str): Folder id.\n            task_id (str): Task id.\n            src_filepath (str): Source workfile path.\n            rootless_workdir (str): Rootless workdir.\n            workdir (str): Destination workdir.\n            filename (str): Destination filename.\n            version (int): Workfile version.\n            comment (str): User's comment (subversion).\n            description (str): Workfile description.\n        \"\"\"\n        pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesFrontend.copy_workfile_representation","title":"<code>copy_workfile_representation(representation_id, representation_filepath, folder_id, task_id, workdir, filename, rootless_workdir, version, comment, description)</code>  <code>abstractmethod</code>","text":"<p>Action to copy published workfile representation to workarea.</p> <p>Triggers 'copy_representation.started' event on start and 'copy_representation.finished' event with '{\"failed\": bool}'.</p> <p>Parameters:</p> Name Type Description Default <code>representation_id</code> <code>str</code> <p>Representation id.</p> required <code>representation_filepath</code> <code>str</code> <p>Path to representation file.</p> required <code>folder_id</code> <code>str</code> <p>Folder id.</p> required <code>task_id</code> <code>str</code> <p>Task id.</p> required <code>workdir</code> <code>str</code> <p>Workarea directory.</p> required <code>filename</code> <code>str</code> <p>Workarea filename.</p> required <code>rootless_workdir</code> <code>str</code> <p>Rootless workdir.</p> required <code>version</code> <code>int</code> <p>Workfile version.</p> required <code>comment</code> <code>str</code> <p>User's comment (subversion).</p> required <code>description</code> <code>str</code> <p>Description note.</p> required Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>@abstractmethod\ndef copy_workfile_representation(\n    self,\n    representation_id,\n    representation_filepath,\n    folder_id,\n    task_id,\n    workdir,\n    filename,\n    rootless_workdir,\n    version,\n    comment,\n    description,\n):\n    \"\"\"Action to copy published workfile representation to workarea.\n\n    Triggers 'copy_representation.started' event on start and\n    'copy_representation.finished' event with '{\"failed\": bool}'.\n\n    Args:\n        representation_id (str): Representation id.\n        representation_filepath (str): Path to representation file.\n        folder_id (str): Folder id.\n        task_id (str): Task id.\n        workdir (str): Workarea directory.\n        filename (str): Workarea filename.\n        rootless_workdir (str): Rootless workdir.\n        version (int): Workfile version.\n        comment (str): User's comment (subversion).\n        description (str): Description note.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesFrontend.duplicate_workfile","title":"<code>duplicate_workfile(folder_id, task_id, src_filepath, rootless_workdir, workdir, filename, description, version, comment)</code>  <code>abstractmethod</code>","text":"<p>Duplicate workfile.</p> <p>Workfiles is not opened when done.</p> <p>Parameters:</p> Name Type Description Default <code>folder_id</code> <code>str</code> <p>Folder id.</p> required <code>task_id</code> <code>str</code> <p>Task id.</p> required <code>src_filepath</code> <code>str</code> <p>Source workfile path.</p> required <code>rootless_workdir</code> <code>str</code> <p>Rootless workdir.</p> required <code>workdir</code> <code>str</code> <p>Destination workdir.</p> required <code>filename</code> <code>str</code> <p>Destination filename.</p> required <code>version</code> <code>int</code> <p>Workfile version.</p> required <code>comment</code> <code>str</code> <p>User's comment (subversion).</p> required <code>description</code> <code>str</code> <p>Workfile description.</p> required Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>@abstractmethod\ndef duplicate_workfile(\n    self,\n    folder_id,\n    task_id,\n    src_filepath,\n    rootless_workdir,\n    workdir,\n    filename,\n    description,\n    version,\n    comment\n):\n    \"\"\"Duplicate workfile.\n\n    Workfiles is not opened when done.\n\n    Args:\n        folder_id (str): Folder id.\n        task_id (str): Task id.\n        src_filepath (str): Source workfile path.\n        rootless_workdir (str): Rootless workdir.\n        workdir (str): Destination workdir.\n        filename (str): Destination filename.\n        version (int): Workfile version.\n        comment (str): User's comment (subversion).\n        description (str): Workfile description.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesFrontend.expected_folder_selected","title":"<code>expected_folder_selected(folder_id)</code>  <code>abstractmethod</code>","text":"<p>Expected folder was selected in UI.</p> <p>Parameters:</p> Name Type Description Default <code>folder_id</code> <code>str</code> <p>Folder id which was selected.</p> required Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>@abstractmethod\ndef expected_folder_selected(self, folder_id):\n    \"\"\"Expected folder was selected in UI.\n\n    Args:\n        folder_id (str): Folder id which was selected.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesFrontend.expected_representation_selected","title":"<code>expected_representation_selected(folder_id, task_name, representation_id)</code>  <code>abstractmethod</code>","text":"<p>Expected representation was selected in UI.</p> <p>Parameters:</p> Name Type Description Default <code>folder_id</code> <code>str</code> <p>Folder id under which representation is.</p> required <code>task_name</code> <code>str</code> <p>Task name under which representation is.</p> required <code>representation_id</code> <code>str</code> <p>Representation id which was selected.</p> required Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>@abstractmethod\ndef expected_representation_selected(\n    self, folder_id, task_name, representation_id\n):\n    \"\"\"Expected representation was selected in UI.\n\n    Args:\n        folder_id (str): Folder id under which representation is.\n        task_name (str): Task name under which representation is.\n        representation_id (str): Representation id which was selected.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesFrontend.expected_task_selected","title":"<code>expected_task_selected(folder_id, task_name)</code>  <code>abstractmethod</code>","text":"<p>Expected task was selected in UI.</p> <p>Parameters:</p> Name Type Description Default <code>folder_id</code> <code>str</code> <p>Folder id under which task is.</p> required <code>task_name</code> <code>str</code> <p>Task name which was selected.</p> required Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>@abstractmethod\ndef expected_task_selected(self, folder_id, task_name):\n    \"\"\"Expected task was selected in UI.\n\n    Args:\n        folder_id (str): Folder id under which task is.\n        task_name (str): Task name which was selected.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesFrontend.expected_workfile_selected","title":"<code>expected_workfile_selected(folder_id, task_name, workfile_name)</code>  <code>abstractmethod</code>","text":"<p>Expected workfile was selected in UI.</p> <p>Parameters:</p> Name Type Description Default <code>folder_id</code> <code>str</code> <p>Folder id under which workfile is.</p> required <code>task_name</code> <code>str</code> <p>Task name under which workfile is.</p> required <code>workfile_name</code> <code>str</code> <p>Workfile filename which was selected.</p> required Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>@abstractmethod\ndef expected_workfile_selected(self, folder_id, task_name, workfile_name):\n    \"\"\"Expected workfile was selected in UI.\n\n    Args:\n        folder_id (str): Folder id under which workfile is.\n        task_name (str): Task name under which workfile is.\n        workfile_name (str): Workfile filename which was selected.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesFrontend.fill_workarea_filepath","title":"<code>fill_workarea_filepath(folder_id, task_id, extension, use_last_version, version, comment)</code>  <code>abstractmethod</code>","text":"<p>Calculate workfile path for passed context.</p> <p>Parameters:</p> Name Type Description Default <code>folder_id</code> <code>str</code> <p>Folder id.</p> required <code>task_id</code> <code>str</code> <p>Task id.</p> required <code>extension</code> <code>str</code> <p>File extension.</p> required <code>use_last_version</code> <code>bool</code> <p>Use last version.</p> required <code>version</code> <code>int</code> <p>Version used if 'use_last_version' if 'False'.</p> required <code>comment</code> <code>str</code> <p>User's comment (subversion).</p> required <p>Returns:</p> Name Type Description <code>WorkareaFilepathResult</code> <p>Result of the operation.</p> Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>@abstractmethod\ndef fill_workarea_filepath(\n    self,\n    folder_id,\n    task_id,\n    extension,\n    use_last_version,\n    version,\n    comment,\n):\n    \"\"\"Calculate workfile path for passed context.\n\n    Args:\n        folder_id (str): Folder id.\n        task_id (str): Task id.\n        extension (str): File extension.\n        use_last_version (bool): Use last version.\n        version (int): Version used if 'use_last_version' if 'False'.\n        comment (str): User's comment (subversion).\n\n    Returns:\n        WorkareaFilepathResult: Result of the operation.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesFrontend.get_expected_selection_data","title":"<code>get_expected_selection_data()</code>  <code>abstractmethod</code>","text":"<p>Data of expected selection.</p> TODOs <p>Return defined object instead of dict.</p> <p>Returns:</p> Type Description <p>dict[str, Any]: Expected selection data.</p> Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>@abstractmethod\ndef get_expected_selection_data(self):\n    \"\"\"Data of expected selection.\n\n    TODOs:\n        Return defined object instead of dict.\n\n    Returns:\n        dict[str, Any]: Expected selection data.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesFrontend.get_folder_items","title":"<code>get_folder_items(project_name, sender)</code>  <code>abstractmethod</code>","text":"<p>Folder items to visualize project hierarchy.</p> <p>This function may trigger events 'folders.refresh.started' and 'folders.refresh.finished' which will contain 'sender' value in data. That may help to avoid re-refresh of folder items in UI elements.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name for which are folders requested.</p> required <code>sender</code> <code>str</code> <p>Who requested folder items.</p> required <p>Returns:</p> Type Description <p>list[FolderItem]: Minimum possible information needed for visualisation of folder hierarchy.</p> Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>@abstractmethod\ndef get_folder_items(self, project_name, sender):\n    \"\"\"Folder items to visualize project hierarchy.\n\n    This function may trigger events 'folders.refresh.started' and\n    'folders.refresh.finished' which will contain 'sender' value in data.\n    That may help to avoid re-refresh of folder items in UI elements.\n\n    Args:\n        project_name (str): Project name for which are folders requested.\n        sender (str): Who requested folder items.\n\n    Returns:\n        list[FolderItem]: Minimum possible information needed\n            for visualisation of folder hierarchy.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesFrontend.get_folder_type_items","title":"<code>get_folder_type_items(project_name, sender=None)</code>  <code>abstractmethod</code>","text":"<p>Folder type items for a project.</p> <p>This function may trigger events with topics 'projects.folder_types.refresh.started' and 'projects.folder_types.refresh.finished' which will contain 'sender' value in data. That may help to avoid re-refresh of items in UI elements.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>sender</code> <code>str</code> <p>Who requested folder type items.</p> <code>None</code> <p>Returns:</p> Type Description <p>list[FolderTypeItem]: Folder type information.</p> Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>@abstractmethod\ndef get_folder_type_items(self, project_name, sender=None):\n    \"\"\"Folder type items for a project.\n\n    This function may trigger events with topics\n    'projects.folder_types.refresh.started' and\n    'projects.folder_types.refresh.finished' which will contain 'sender'\n    value in data.\n    That may help to avoid re-refresh of items in UI elements.\n\n    Args:\n        project_name (str): Project name.\n        sender (str): Who requested folder type items.\n\n    Returns:\n        list[FolderTypeItem]: Folder type information.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesFrontend.get_published_file_items","title":"<code>get_published_file_items(folder_id, task_id)</code>  <code>abstractmethod</code>","text":"<p>Get published file items.</p> <p>Parameters:</p> Name Type Description Default <code>folder_id</code> <code>str</code> <p>Folder id.</p> required <code>task_id</code> <code>Union[str, None]</code> <p>Task id.</p> required <p>Returns:</p> Type Description <p>list[PublishedWorkfileInfo]: List of published file items.</p> Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>@abstractmethod\ndef get_published_file_items(self, folder_id: str, task_id: str):\n    \"\"\"Get published file items.\n\n    Args:\n        folder_id (str): Folder id.\n        task_id (Union[str, None]): Task id.\n\n    Returns:\n        list[PublishedWorkfileInfo]: List of published file items.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesFrontend.get_published_workfile_info","title":"<code>get_published_workfile_info(folder_id, representation_id)</code>  <code>abstractmethod</code>","text":"<p>Get published workfile info by representation ID.</p> <p>Parameters:</p> Name Type Description Default <code>folder_id</code> <code>Optional[str]</code> <p>Folder id.</p> required <code>representation_id</code> <code>Optional[str]</code> <p>Representation id.</p> required <p>Returns:</p> Name Type Description <code>PublishedWorkfileWrap</code> <code>PublishedWorkfileWrap</code> <p>Published workfile info or None if not found.</p> Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>@abstractmethod\ndef get_published_workfile_info(\n    self,\n    folder_id: Optional[str],\n    representation_id: Optional[str],\n) -&gt; PublishedWorkfileWrap:\n    \"\"\"Get published workfile info by representation ID.\n\n    Args:\n        folder_id (Optional[str]): Folder id.\n        representation_id (Optional[str]): Representation id.\n\n    Returns:\n        PublishedWorkfileWrap: Published workfile info or None\n            if not found.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesFrontend.get_selected_context","title":"<code>get_selected_context()</code>","text":"<p>Obtain selected context.</p> <p>Returns:</p> Type Description <p>dict[str, Union[str, None]]: Selected context.</p> Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>def get_selected_context(self):\n    \"\"\"Obtain selected context.\n\n    Returns:\n        dict[str, Union[str, None]]: Selected context.\n\n    \"\"\"\n    return {\n        \"folder_id\": self.get_selected_folder_id(),\n        \"task_id\": self.get_selected_task_id(),\n        \"task_name\": self.get_selected_task_name(),\n        \"workfile_path\": self.get_selected_workfile_path(),\n        \"representation_id\": self.get_selected_representation_id(),\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesFrontend.get_selected_folder_id","title":"<code>get_selected_folder_id()</code>  <code>abstractmethod</code>","text":"<p>Currently selected folder id.</p> <p>Returns:</p> Type Description <p>Union[str, None]: Folder id or None if no folder is selected.</p> Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>@abstractmethod\ndef get_selected_folder_id(self):\n    \"\"\"Currently selected folder id.\n\n    Returns:\n        Union[str, None]: Folder id or None if no folder is selected.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesFrontend.get_selected_representation_id","title":"<code>get_selected_representation_id()</code>  <code>abstractmethod</code>","text":"<p>Currently selected workfile representation id.</p> <p>Returns:</p> Type Description <p>Union[str, None]: Representation id or None if no representation is selected.</p> Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>@abstractmethod\ndef get_selected_representation_id(self):\n    \"\"\"Currently selected workfile representation id.\n\n    Returns:\n        Union[str, None]: Representation id or None if no representation\n            is selected.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesFrontend.get_selected_task_id","title":"<code>get_selected_task_id()</code>  <code>abstractmethod</code>","text":"<p>Currently selected task id.</p> <p>Returns:</p> Type Description <p>Union[str, None]: Task id or None if no folder is selected.</p> Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>@abstractmethod\ndef get_selected_task_id(self):\n    \"\"\"Currently selected task id.\n\n    Returns:\n        Union[str, None]: Task id or None if no folder is selected.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesFrontend.get_selected_task_name","title":"<code>get_selected_task_name()</code>  <code>abstractmethod</code>","text":"<p>Currently selected task name.</p> <p>Returns:</p> Type Description <p>Union[str, None]: Task name or None if no folder is selected.</p> Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>@abstractmethod\ndef get_selected_task_name(self):\n    \"\"\"Currently selected task name.\n\n    Returns:\n        Union[str, None]: Task name or None if no folder is selected.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesFrontend.get_selected_workfile_path","title":"<code>get_selected_workfile_path()</code>  <code>abstractmethod</code>","text":"<p>Currently selected workarea workile.</p> <p>Returns:</p> Type Description <p>Union[str, None]: Selected workfile path.</p> Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>@abstractmethod\ndef get_selected_workfile_path(self):\n    \"\"\"Currently selected workarea workile.\n\n    Returns:\n        Union[str, None]: Selected workfile path.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesFrontend.get_task_items","title":"<code>get_task_items(project_name, folder_id, sender)</code>  <code>abstractmethod</code>","text":"<p>Task items.</p> <p>This function may trigger events 'tasks.refresh.started' and 'tasks.refresh.finished' which will contain 'sender' value in data. That may help to avoid re-refresh of task items in UI elements.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name for which are tasks requested.</p> required <code>folder_id</code> <code>str</code> <p>Folder ID for which are tasks requested.</p> required <code>sender</code> <code>str</code> <p>Who requested folder items.</p> required <p>Returns:</p> Type Description <p>list[TaskItem]: Minimum possible information needed for visualisation of tasks.</p> Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>@abstractmethod\ndef get_task_items(self, project_name, folder_id, sender):\n    \"\"\"Task items.\n\n    This function may trigger events 'tasks.refresh.started' and\n    'tasks.refresh.finished' which will contain 'sender' value in data.\n    That may help to avoid re-refresh of task items in UI elements.\n\n    Args:\n        project_name (str): Project name for which are tasks requested.\n        folder_id (str): Folder ID for which are tasks requested.\n        sender (str): Who requested folder items.\n\n    Returns:\n        list[TaskItem]: Minimum possible information needed\n            for visualisation of tasks.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesFrontend.get_task_type_items","title":"<code>get_task_type_items(project_name, sender=None)</code>  <code>abstractmethod</code>","text":"<p>Task type items for a project.</p> <p>This function may trigger events with topics 'projects.task_types.refresh.started' and 'projects.task_types.refresh.finished' which will contain 'sender' value in data. That may help to avoid re-refresh of items in UI elements.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>Project name.</p> required <code>sender</code> <code>str</code> <p>Who requested task type items.</p> <code>None</code> <p>Returns:</p> Type Description <p>list[TaskTypeItem]: Task type information.</p> Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>@abstractmethod\ndef get_task_type_items(self, project_name, sender=None):\n    \"\"\"Task type items for a project.\n\n    This function may trigger events with topics\n    'projects.task_types.refresh.started' and\n    'projects.task_types.refresh.finished' which will contain 'sender'\n    value in data.\n    That may help to avoid re-refresh of items in UI elements.\n\n    Args:\n        project_name (str): Project name.\n        sender (str): Who requested task type items.\n\n    Returns:\n        list[TaskTypeItem]: Task type information.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesFrontend.get_user_items_by_name","title":"<code>get_user_items_by_name()</code>  <code>abstractmethod</code>","text":"<p>Get user items available on AYON server.</p> <p>Returns:</p> Type Description <p>Dict[str, UserItem]: User items by username.</p> Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>@abstractmethod\ndef get_user_items_by_name(self):\n    \"\"\"Get user items available on AYON server.\n\n    Returns:\n        Dict[str, UserItem]: User items by username.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesFrontend.get_window_subtitle","title":"<code>get_window_subtitle()</code>  <code>abstractmethod</code>","text":"<p>Get window subtitle.</p> <p>Returns:</p> Type Description <code>Optional[str]</code> <p>Optional[str]: Window subtitle.</p> Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>@abstractmethod\ndef get_window_subtitle(self) -&gt; Optional[str]:\n    \"\"\"Get window subtitle.\n\n    Returns:\n        Optional[str]: Window subtitle.\n\n    \"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesFrontend.get_workarea_dir_by_context","title":"<code>get_workarea_dir_by_context(folder_id, task_id)</code>  <code>abstractmethod</code>","text":"<p>Get workarea directory by context.</p> <p>Parameters:</p> Name Type Description Default <code>folder_id</code> <code>str</code> <p>Folder id.</p> required <code>task_id</code> <code>str</code> <p>Task id.</p> required <p>Returns:</p> Name Type Description <code>str</code> <p>Workarea directory.</p> Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>@abstractmethod\ndef get_workarea_dir_by_context(self, folder_id, task_id):\n    \"\"\"Get workarea directory by context.\n\n    Args:\n        folder_id (str): Folder id.\n        task_id (str): Task id.\n\n    Returns:\n        str: Workarea directory.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesFrontend.get_workarea_file_items","title":"<code>get_workarea_file_items(folder_id, task_name, sender=None)</code>  <code>abstractmethod</code>","text":"<p>Get workarea file items.</p> <p>Parameters:</p> Name Type Description Default <code>folder_id</code> <code>str</code> <p>Folder id.</p> required <code>task_name</code> <code>str</code> <p>Task name.</p> required <code>sender</code> <code>Optional[str]</code> <p>Who requested workarea file items.</p> <code>None</code> <p>Returns:</p> Type Description <p>list[WorkfileInfo]: List of workarea file items.</p> Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>@abstractmethod\ndef get_workarea_file_items(self, folder_id, task_name, sender=None):\n    \"\"\"Get workarea file items.\n\n    Args:\n        folder_id (str): Folder id.\n        task_name (str): Task name.\n        sender (Optional[str]): Who requested workarea file items.\n\n    Returns:\n        list[WorkfileInfo]: List of workarea file items.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesFrontend.get_workarea_save_as_data","title":"<code>get_workarea_save_as_data(folder_id, task_id)</code>  <code>abstractmethod</code>","text":"<p>Prepare data for Save As operation.</p> Todos <p>Return defined object instead of dict.</p> <p>Parameters:</p> Name Type Description Default <code>folder_id</code> <code>str</code> <p>Folder id.</p> required <code>task_id</code> <code>str</code> <p>Task id.</p> required <p>Returns:</p> Type Description <p>dict[str, Any]: Data for Save As operation.</p> Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>@abstractmethod\ndef get_workarea_save_as_data(self, folder_id, task_id):\n    \"\"\"Prepare data for Save As operation.\n\n    Todos:\n        Return defined object instead of dict.\n\n    Args:\n        folder_id (str): Folder id.\n        task_id (str): Task id.\n\n    Returns:\n        dict[str, Any]: Data for Save As operation.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesFrontend.get_workfile_extensions","title":"<code>get_workfile_extensions()</code>  <code>abstractmethod</code>","text":"<p>Each host can define extensions that can be used for workfile.</p> <p>Returns:</p> Type Description <p>List[str]: File extensions that can be used as workfile for current host.</p> Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>@abstractmethod\ndef get_workfile_extensions(self):\n    \"\"\"Each host can define extensions that can be used for workfile.\n\n    Returns:\n        List[str]: File extensions that can be used as workfile for\n            current host.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesFrontend.get_workfile_info","title":"<code>get_workfile_info(folder_id, task_id, rootless_path)</code>  <code>abstractmethod</code>","text":"<p>Workfile info from database.</p> <p>Parameters:</p> Name Type Description Default <code>folder_id</code> <code>str</code> <p>Folder id.</p> required <code>task_id</code> <code>str</code> <p>Task id.</p> required <code>rootless_path</code> <code>str</code> <p>Workfile path.</p> required <p>Returns:</p> Type Description <p>Optional[WorkfileInfo]: Workfile info or None if was passed invalid context.</p> Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>@abstractmethod\ndef get_workfile_info(self, folder_id, task_id, rootless_path):\n    \"\"\"Workfile info from database.\n\n    Args:\n        folder_id (str): Folder id.\n        task_id (str): Task id.\n        rootless_path (str): Workfile path.\n\n    Returns:\n        Optional[WorkfileInfo]: Workfile info or None if was passed\n            invalid context.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesFrontend.go_to_current_context","title":"<code>go_to_current_context()</code>  <code>abstractmethod</code>","text":"<p>Set expected selection to current context.</p> Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>@abstractmethod\ndef go_to_current_context(self):\n    \"\"\"Set expected selection to current context.\"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesFrontend.has_unsaved_changes","title":"<code>has_unsaved_changes()</code>  <code>abstractmethod</code>","text":"<p>Has host unsaved change in currently running session.</p> <p>Returns:</p> Name Type Description <code>bool</code> <p>Has unsaved changes.</p> Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>@abstractmethod\ndef has_unsaved_changes(self):\n    \"\"\"Has host unsaved change in currently running session.\n\n    Returns:\n        bool: Has unsaved changes.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesFrontend.open_workfile","title":"<code>open_workfile(folder_id, task_id, filepath)</code>  <code>abstractmethod</code>","text":"<p>Open a workfile for context.</p> <p>Parameters:</p> Name Type Description Default <code>folder_id</code> <code>str</code> <p>Folder id.</p> required <code>task_id</code> <code>str</code> <p>Task id.</p> required <code>filepath</code> <code>str</code> <p>Workfile path.</p> required Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>@abstractmethod\ndef open_workfile(self, folder_id, task_id, filepath):\n    \"\"\"Open a workfile for context.\n\n    Args:\n        folder_id (str): Folder id.\n        task_id (str): Task id.\n        filepath (str): Workfile path.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesFrontend.register_event_callback","title":"<code>register_event_callback(topic, callback)</code>  <code>abstractmethod</code>","text":"<p>Register event callback.</p> <p>Listen for events with given topic.</p> <p>Parameters:</p> Name Type Description Default <code>topic</code> <code>str</code> <p>Name of topic.</p> required <code>callback</code> <code>Callable</code> <p>Callback that will be called when event is triggered.</p> required Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>@abstractmethod\ndef register_event_callback(self, topic, callback):\n    \"\"\"Register event callback.\n\n    Listen for events with given topic.\n\n    Args:\n        topic (str): Name of topic.\n        callback (Callable): Callback that will be called when event\n            is triggered.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesFrontend.reset","title":"<code>reset()</code>  <code>abstractmethod</code>","text":"<p>Reset everything, models, ui etc.</p> <p>Triggers 'controller.reset.started' event at the beginning and 'controller.reset.finished' at the end.</p> Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>@abstractmethod\ndef reset(self):\n    \"\"\"Reset everything, models, ui etc.\n\n    Triggers 'controller.reset.started' event at the beginning and\n    'controller.reset.finished' at the end.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesFrontend.save_as_workfile","title":"<code>save_as_workfile(folder_id, task_id, rootless_workdir, workdir, filename, version, comment, description)</code>  <code>abstractmethod</code>","text":"<p>Save current state of workfile to workarea.</p> <p>Parameters:</p> Name Type Description Default <code>folder_id</code> <code>str</code> <p>Folder id.</p> required <code>task_id</code> <code>str</code> <p>Task id.</p> required <code>rootless_workdir</code> <code>str</code> <p>Workarea directory.</p> required <code>filename</code> <code>str</code> <p>Workarea filename.</p> required <code>template_key</code> <code>str</code> <p>Template key used to get the workdir and filename.</p> required <code>version</code> <code>Optional[int]</code> <p>Version of workfile.</p> required <code>comment</code> <code>Optional[str]</code> <p>User's comment (subversion).</p> required <code>description</code> <code>Optional[str]</code> <p>Workfile description.</p> required Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>@abstractmethod\ndef save_as_workfile(\n    self,\n    folder_id,\n    task_id,\n    rootless_workdir,\n    workdir,\n    filename,\n    version,\n    comment,\n    description,\n):\n    \"\"\"Save current state of workfile to workarea.\n\n    Args:\n        folder_id (str): Folder id.\n        task_id (str): Task id.\n        rootless_workdir (str): Workarea directory.\n        filename (str): Workarea filename.\n        template_key (str): Template key used to get the workdir\n            and filename.\n        version (Optional[int]): Version of workfile.\n        comment (Optional[str]): User's comment (subversion).\n        description (Optional[str]): Workfile description.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesFrontend.save_current_workfile","title":"<code>save_current_workfile()</code>  <code>abstractmethod</code>","text":"<p>Save state of current workfile.</p> Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>@abstractmethod\ndef save_current_workfile(self):\n    \"\"\"Save state of current workfile.\"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesFrontend.save_workfile_info","title":"<code>save_workfile_info(task_id, rootless_path, version=None, comment=None, description=None)</code>  <code>abstractmethod</code>","text":"<p>Save workfile info to database.</p> <p>At this moment the only information which can be saved about     workfile is 'description'.</p> <p>If value of 'version', 'comment' or 'description' is 'None' it is not     added/updated to entity.</p> <p>Parameters:</p> Name Type Description Default <code>task_id</code> <code>str</code> <p>Task id.</p> required <code>rootless_path</code> <code>str</code> <p>Rootless workfile path.</p> required <code>version</code> <code>Optional[int]</code> <p>Version of workfile.</p> <code>None</code> <code>comment</code> <code>Optional[str]</code> <p>User's comment (subversion).</p> <code>None</code> <code>description</code> <code>Optional[str]</code> <p>Workfile description.</p> <code>None</code> Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>@abstractmethod\ndef save_workfile_info(\n    self,\n    task_id,\n    rootless_path,\n    version=None,\n    comment=None,\n    description=None,\n):\n    \"\"\"Save workfile info to database.\n\n    At this moment the only information which can be saved about\n        workfile is 'description'.\n\n    If value of 'version', 'comment' or 'description' is 'None' it is not\n        added/updated to entity.\n\n    Args:\n        task_id (str): Task id.\n        rootless_path (str): Rootless workfile path.\n        version (Optional[int]): Version of workfile.\n        comment (Optional[str]): User's comment (subversion).\n        description (Optional[str]): Workfile description.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesFrontend.set_expected_selection","title":"<code>set_expected_selection(folder_id, task_name, workfile_name=None, representation_id=None)</code>  <code>abstractmethod</code>","text":"<p>Define what should be selected in UI.</p> <p>Expected selection provide a way to define/change selection of sequential UI elements. For example, if folder and task should be selected a task element should wait until folder element has selected folder.</p> <p>Triggers 'expected_selection.changed' event.</p> <p>Parameters:</p> Name Type Description Default <code>folder_id</code> <code>str</code> <p>Folder id.</p> required <code>task_name</code> <code>str</code> <p>Task name.</p> required <code>workfile_name</code> <code>Optional[str]</code> <p>Workfile name. Used for workarea files UI element.</p> <code>None</code> <code>representation_id</code> <code>Optional[str]</code> <p>Representation id. Used for published filed UI element.</p> <code>None</code> Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>@abstractmethod\ndef set_expected_selection(\n    self,\n    folder_id,\n    task_name,\n    workfile_name=None,\n    representation_id=None\n):\n    \"\"\"Define what should be selected in UI.\n\n    Expected selection provide a way to define/change selection of\n    sequential UI elements. For example, if folder and task should be\n    selected a task element should wait until folder element has selected\n    folder.\n\n    Triggers 'expected_selection.changed' event.\n\n    Args:\n        folder_id (str): Folder id.\n        task_name (str): Task name.\n        workfile_name (Optional[str]): Workfile name. Used for workarea\n            files UI element.\n        representation_id (Optional[str]): Representation id. Used for\n            published filed UI element.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesFrontend.set_selected_folder","title":"<code>set_selected_folder(folder_id)</code>  <code>abstractmethod</code>","text":"<p>Change selected folder.</p> <p>This deselects currently selected task.</p> <p>Parameters:</p> Name Type Description Default <code>folder_id</code> <code>Union[str, None]</code> <p>Folder id or None if no folder is selected.</p> required Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>@abstractmethod\ndef set_selected_folder(self, folder_id):\n    \"\"\"Change selected folder.\n\n    This deselects currently selected task.\n\n    Args:\n        folder_id (Union[str, None]): Folder id or None if no folder\n            is selected.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesFrontend.set_selected_representation_id","title":"<code>set_selected_representation_id(representation_id)</code>  <code>abstractmethod</code>","text":"<p>Change selected representation.</p> <p>Parameters:</p> Name Type Description Default <code>representation_id</code> <code>Union[str, None]</code> <p>Selected workfile representation id.</p> required Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>@abstractmethod\ndef set_selected_representation_id(self, representation_id):\n    \"\"\"Change selected representation.\n\n    Args:\n        representation_id (Union[str, None]): Selected workfile\n            representation id.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesFrontend.set_selected_task","title":"<code>set_selected_task(task_id, task_name)</code>  <code>abstractmethod</code>","text":"<p>Change selected task.</p> <p>Parameters:</p> Name Type Description Default <code>task_id</code> <code>Union[str, None]</code> <p>Task id or None if no task is selected.</p> required <code>task_name</code> <code>Union[str, None]</code> <p>Task name or None if no task is selected.</p> required Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>@abstractmethod\ndef set_selected_task(self, task_id, task_name):\n    \"\"\"Change selected task.\n\n    Args:\n        task_id (Union[str, None]): Task id or None if no task\n            is selected.\n        task_name (Union[str, None]): Task name or None if no task\n            is selected.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.AbstractWorkfilesFrontend.set_selected_workfile_path","title":"<code>set_selected_workfile_path(rootless_path, path, workfile_entity_id)</code>  <code>abstractmethod</code>","text":"<p>Change selected workfile path.</p> <p>Parameters:</p> Name Type Description Default <code>rootless_path</code> <code>Union[str, None]</code> <p>Selected workfile rootless path.</p> required <code>path</code> <code>Union[str, None]</code> <p>Selected workfile path.</p> required <code>workfile_entity_id</code> <code>Union[str, None]</code> <p>Workfile entity id.</p> required Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>@abstractmethod\ndef set_selected_workfile_path(\n    self, rootless_path, path, workfile_entity_id\n):\n    \"\"\"Change selected workfile path.\n\n    Args:\n        rootless_path (Union[str, None]): Selected workfile rootless path.\n        path (Union[str, None]): Selected workfile path.\n        workfile_entity_id (Union[str, None]): Workfile entity id.\n\n    \"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.FolderItem","title":"<code>FolderItem</code>","text":"<p>Item representing folder entity on a server.</p> <p>Folder can be a child of another folder or a project.</p> <p>Parameters:</p> Name Type Description Default <code>entity_id</code> <code>str</code> <p>Folder id.</p> required <code>parent_id</code> <code>Union[str, None]</code> <p>Parent folder id. If 'None' then project is parent.</p> required <code>name</code> <code>str</code> <p>Name of folder.</p> required <code>label</code> <code>str</code> <p>Folder label.</p> required <code>icon_name</code> <code>str</code> <p>Name of icon from font awesome.</p> required <code>icon_color</code> <code>str</code> <p>Hex color string that will be used for icon.</p> required Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>class FolderItem:\n    \"\"\"Item representing folder entity on a server.\n\n    Folder can be a child of another folder or a project.\n\n    Args:\n        entity_id (str): Folder id.\n        parent_id (Union[str, None]): Parent folder id. If 'None' then project\n            is parent.\n        name (str): Name of folder.\n        label (str): Folder label.\n        icon_name (str): Name of icon from font awesome.\n        icon_color (str): Hex color string that will be used for icon.\n\n    \"\"\"\n    def __init__(\n        self, entity_id, parent_id, name, label, icon_name, icon_color\n    ):\n        self.entity_id = entity_id\n        self.parent_id = parent_id\n        self.name = name\n        self.icon_name = icon_name or \"fa.folder\"\n        self.icon_color = icon_color or get_default_entity_icon_color()\n        self.label = label or name\n\n    def to_data(self):\n        \"\"\"Converts folder item to data.\n\n        Returns:\n            dict[str, Any]: Folder item data.\n\n        \"\"\"\n        return {\n            \"entity_id\": self.entity_id,\n            \"parent_id\": self.parent_id,\n            \"name\": self.name,\n            \"label\": self.label,\n            \"icon_name\": self.icon_name,\n            \"icon_color\": self.icon_color,\n        }\n\n    @classmethod\n    def from_data(cls, data):\n        \"\"\"Re-creates folder item from data.\n\n        Args:\n            data (dict[str, Any]): Folder item data.\n\n        Returns:\n            FolderItem: Folder item.\n\n        \"\"\"\n        return cls(**data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.FolderItem.from_data","title":"<code>from_data(data)</code>  <code>classmethod</code>","text":"<p>Re-creates folder item from data.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict[str, Any]</code> <p>Folder item data.</p> required <p>Returns:</p> Name Type Description <code>FolderItem</code> <p>Folder item.</p> Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>@classmethod\ndef from_data(cls, data):\n    \"\"\"Re-creates folder item from data.\n\n    Args:\n        data (dict[str, Any]): Folder item data.\n\n    Returns:\n        FolderItem: Folder item.\n\n    \"\"\"\n    return cls(**data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.FolderItem.to_data","title":"<code>to_data()</code>","text":"<p>Converts folder item to data.</p> <p>Returns:</p> Type Description <p>dict[str, Any]: Folder item data.</p> Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>def to_data(self):\n    \"\"\"Converts folder item to data.\n\n    Returns:\n        dict[str, Any]: Folder item data.\n\n    \"\"\"\n    return {\n        \"entity_id\": self.entity_id,\n        \"parent_id\": self.parent_id,\n        \"name\": self.name,\n        \"label\": self.label,\n        \"icon_name\": self.icon_name,\n        \"icon_color\": self.icon_color,\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.PublishedWorkfileWrap","title":"<code>PublishedWorkfileWrap</code>","text":"<p>Wrapper for workfile info that also contains version comment.</p> Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>class PublishedWorkfileWrap:\n    \"\"\"Wrapper for workfile info that also contains version comment.\"\"\"\n    def __init__(\n        self,\n        info: Optional[PublishedWorkfileInfo] = None,\n        comment: Optional[str] = None,\n    ) -&gt; None:\n        self.info = info\n        self.comment = comment\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.TaskItem","title":"<code>TaskItem</code>","text":"<p>Task item representing task entity on a server.</p> <p>Task is child of a folder.</p> <p>Task item has label that is used for display in UI. The label is by     default using task name and type.</p> <p>Parameters:</p> Name Type Description Default <code>task_id</code> <code>str</code> <p>Task id.</p> required <code>name</code> <code>str</code> <p>Name of task.</p> required <code>task_type</code> <code>str</code> <p>Type of task.</p> required <code>parent_id</code> <code>str</code> <p>Parent folder id.</p> required <code>icon_name</code> <code>str</code> <p>Name of icon from font awesome.</p> required <code>icon_color</code> <code>str</code> <p>Hex color string that will be used for icon.</p> required Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>class TaskItem:\n    \"\"\"Task item representing task entity on a server.\n\n    Task is child of a folder.\n\n    Task item has label that is used for display in UI. The label is by\n        default using task name and type.\n\n    Args:\n        task_id (str): Task id.\n        name (str): Name of task.\n        task_type (str): Type of task.\n        parent_id (str): Parent folder id.\n        icon_name (str): Name of icon from font awesome.\n        icon_color (str): Hex color string that will be used for icon.\n\n    \"\"\"\n    def __init__(\n        self, task_id, name, task_type, parent_id, icon_name, icon_color\n    ):\n        self.task_id = task_id\n        self.name = name\n        self.task_type = task_type\n        self.parent_id = parent_id\n        self.icon_name = icon_name or \"fa.male\"\n        self.icon_color = icon_color or get_default_entity_icon_color()\n        self._label = None\n\n    @property\n    def id(self):\n        \"\"\"Alias for task_id.\n\n        Returns:\n            str: Task id.\n\n        \"\"\"\n        return self.task_id\n\n    @property\n    def label(self):\n        \"\"\"Label of task item for UI.\n\n        Returns:\n            str: Label of task item.\n\n        \"\"\"\n        if self._label is None:\n            self._label = \"{} ({})\".format(self.name, self.task_type)\n        return self._label\n\n    def to_data(self):\n        \"\"\"Converts task item to data.\n\n        Returns:\n            dict[str, Any]: Task item data.\n\n        \"\"\"\n        return {\n            \"task_id\": self.task_id,\n            \"name\": self.name,\n            \"parent_id\": self.parent_id,\n            \"task_type\": self.task_type,\n            \"icon_name\": self.icon_name,\n            \"icon_color\": self.icon_color,\n        }\n\n    @classmethod\n    def from_data(cls, data):\n        \"\"\"Re-create task item from data.\n\n        Args:\n            data (dict[str, Any]): Task item data.\n\n        Returns:\n            TaskItem: Task item.\n\n        \"\"\"\n        return cls(**data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.TaskItem.id","title":"<code>id</code>  <code>property</code>","text":"<p>Alias for task_id.</p> <p>Returns:</p> Name Type Description <code>str</code> <p>Task id.</p>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.TaskItem.label","title":"<code>label</code>  <code>property</code>","text":"<p>Label of task item for UI.</p> <p>Returns:</p> Name Type Description <code>str</code> <p>Label of task item.</p>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.TaskItem.from_data","title":"<code>from_data(data)</code>  <code>classmethod</code>","text":"<p>Re-create task item from data.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict[str, Any]</code> <p>Task item data.</p> required <p>Returns:</p> Name Type Description <code>TaskItem</code> <p>Task item.</p> Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>@classmethod\ndef from_data(cls, data):\n    \"\"\"Re-create task item from data.\n\n    Args:\n        data (dict[str, Any]): Task item data.\n\n    Returns:\n        TaskItem: Task item.\n\n    \"\"\"\n    return cls(**data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.TaskItem.to_data","title":"<code>to_data()</code>","text":"<p>Converts task item to data.</p> <p>Returns:</p> Type Description <p>dict[str, Any]: Task item data.</p> Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>def to_data(self):\n    \"\"\"Converts task item to data.\n\n    Returns:\n        dict[str, Any]: Task item data.\n\n    \"\"\"\n    return {\n        \"task_id\": self.task_id,\n        \"name\": self.name,\n        \"parent_id\": self.parent_id,\n        \"task_type\": self.task_type,\n        \"icon_name\": self.icon_name,\n        \"icon_color\": self.icon_color,\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/abstract.html#client.ayon_core.tools.workfiles.abstract.WorkareaFilepathResult","title":"<code>WorkareaFilepathResult</code>","text":"<p>Result of workarea file formatting.</p> <p>Parameters:</p> Name Type Description Default <code>root</code> <code>str</code> <p>Root path of workarea.</p> required <code>filename</code> <code>str</code> <p>Filename.</p> required <code>exists</code> <code>bool</code> <p>True if file exists.</p> required <code>filepath</code> <code>str</code> <p>Filepath. If not provided it will be constructed from root and filename.</p> <code>None</code> Source code in <code>client/ayon_core/tools/workfiles/abstract.py</code> <pre><code>class WorkareaFilepathResult:\n    \"\"\"Result of workarea file formatting.\n\n    Args:\n        root (str): Root path of workarea.\n        filename (str): Filename.\n        exists (bool): True if file exists.\n        filepath (str): Filepath. If not provided it will be constructed\n            from root and filename.\n\n    \"\"\"\n    def __init__(self, root, filename, exists, filepath=None):\n        if not filepath and root and filename:\n            filepath = os.path.join(root, filename)\n        self.root = root\n        self.filename = filename\n        self.exists = exists\n        self.filepath = filepath\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/control.html","title":"control","text":""},{"location":"autoapi/client/ayon_core/tools/workfiles/control.html#client.ayon_core.tools.workfiles.control.BaseWorkfileController","title":"<code>BaseWorkfileController</code>","text":"<p>               Bases: <code>AbstractWorkfilesFrontend</code>, <code>AbstractWorkfilesBackend</code></p> Source code in <code>client/ayon_core/tools/workfiles/control.py</code> <pre><code>class BaseWorkfileController(\n    AbstractWorkfilesFrontend, AbstractWorkfilesBackend\n):\n    def __init__(self, host=None):\n        if host is None:\n            host = registered_host()\n\n        host_is_valid = isinstance(host, IWorkfileHost)\n\n        self._host = host\n        self._host_is_valid = host_is_valid\n\n        self._project_anatomy = None\n        self._event_system = None\n        self._log = None\n        self._username = NOT_SET\n\n        self._current_project_name = None\n        self._current_folder_path = None\n        self._current_folder_id = None\n        self._current_task_name = None\n        self._save_is_enabled = True\n\n        # Expected selected folder and task\n        self._settings_model = SettingsModel()\n        self._users_model = self._create_users_model()\n        self._expected_selection = self._create_expected_selection_obj()\n        self._selection_model = self._create_selection_model()\n        self._projects_model = self._create_projects_model()\n        self._hierarchy_model = self._create_hierarchy_model()\n        self._workfiles_model = self._create_workfiles_model()\n\n    @property\n    def log(self):\n        if self._log is None:\n            self._log = Logger.get_logger(\"WorkfilesUI\")\n        return self._log\n\n    def get_window_subtitle(self) -&gt; Optional[str]:\n        if self._host is None:\n            return None\n        return self._host.name\n\n    def is_host_valid(self):\n        return self._host_is_valid\n\n    def _create_users_model(self):\n        return UsersModel(self)\n\n    def _create_workfiles_model(self):\n        return WorkfilesModel(self._host, self)\n\n    def _create_expected_selection_obj(self):\n        return WorkfilesToolExpectedSelection(self)\n\n    def _create_projects_model(self):\n        return ProjectsModel(self)\n\n    def _create_selection_model(self):\n        return SelectionModel(self)\n\n    def _create_hierarchy_model(self):\n        return HierarchyModel(self)\n\n    @property\n    def event_system(self):\n        \"\"\"Inner event system for workfiles tool controller.\n\n        Is used for communication with UI. Event system is created on demand.\n\n        Returns:\n            QueuedEventSystem: Event system which can trigger callbacks\n                for topics.\n        \"\"\"\n\n        if self._event_system is None:\n            self._event_system = QueuedEventSystem()\n        return self._event_system\n\n    # ----------------------------------------------------\n    # Implementation of methods required for backend logic\n    # ----------------------------------------------------\n    def get_project_settings(self, project_name):\n        return self._settings_model.get_settings(project_name)\n\n    @property\n    def project_settings(self):\n        return self.get_project_settings(self.get_current_project_name())\n\n    @property\n    def project_anatomy(self):\n        if self._project_anatomy is None:\n            self._project_anatomy = Anatomy(self.get_current_project_name())\n        return self._project_anatomy\n\n    def get_project_entity(self, project_name):\n        return self._projects_model.get_project_entity(\n            project_name)\n\n    def get_folder_type_items(self, project_name, sender=None):\n        return self._projects_model.get_folder_type_items(\n            project_name, sender\n        )\n\n    def get_task_type_items(self, project_name, sender=None):\n        return self._projects_model.get_task_type_items(\n            project_name, sender\n        )\n\n    def get_folder_entity(self, project_name, folder_id):\n        return self._hierarchy_model.get_folder_entity(\n            project_name, folder_id)\n\n    def get_task_entity(self, project_name, task_id):\n        return self._hierarchy_model.get_task_entity(\n            project_name, task_id)\n\n    # ---------------------------------\n    # Implementation of abstract methods\n    # ---------------------------------\n    def emit_event(self, topic, data=None, source=None):\n        \"\"\"Use implemented event system to trigger event.\"\"\"\n\n        if data is None:\n            data = {}\n        self.event_system.emit(topic, data, source)\n\n    def register_event_callback(self, topic, callback):\n        self.event_system.add_callback(topic, callback)\n\n    def is_save_enabled(self):\n        \"\"\"Is workfile save enabled.\n\n        Returns:\n            bool: True if save is enabled.\n        \"\"\"\n\n        return self._save_is_enabled\n\n    def set_save_enabled(self, enabled):\n        \"\"\"Enable or disabled workfile save.\n\n        Args:\n            enabled (bool): Enable save workfile when True.\n        \"\"\"\n\n        if self._save_is_enabled == enabled:\n            return\n\n        self._save_is_enabled = enabled\n        self._emit_event(\n            \"workfile_save_enable.changed\",\n            {\"enabled\": enabled}\n        )\n\n    def get_user_items_by_name(self):\n        project_name = self.get_current_project_name()\n        return self._users_model.get_user_items_by_name(project_name)\n\n    # Host information\n    def get_workfile_extensions(self):\n        return self._host.get_workfile_extensions()\n\n    def has_unsaved_changes(self):\n        return self._host.workfile_has_unsaved_changes()\n\n    # Current context\n    def get_host_name(self):\n        return self._host.name\n\n    def get_current_project_name(self):\n        return self._current_project_name\n\n    def get_current_folder_id(self):\n        return self._current_folder_id\n\n    def get_current_task_name(self):\n        return self._current_task_name\n\n    def get_current_workfile(self):\n        return self._workfiles_model.get_current_workfile()\n\n    # Selection information\n    def get_selected_folder_id(self):\n        return self._selection_model.get_selected_folder_id()\n\n    def set_selected_folder(self, folder_id):\n        self._selection_model.set_selected_folder(folder_id)\n\n    def get_selected_task_id(self):\n        return self._selection_model.get_selected_task_id()\n\n    def get_selected_task_name(self):\n        return self._selection_model.get_selected_task_name()\n\n    def set_selected_task(self, task_id, task_name):\n        return self._selection_model.set_selected_task(task_id, task_name)\n\n    def get_selected_workfile_path(self):\n        return self._selection_model.get_selected_workfile_path()\n\n    def set_selected_workfile_path(\n        self, rootless_path, path, workfile_entity_id\n    ):\n        self._selection_model.set_selected_workfile_path(\n            rootless_path, path, workfile_entity_id\n        )\n\n    def get_selected_representation_id(self):\n        return self._selection_model.get_selected_representation_id()\n\n    def set_selected_representation_id(self, representation_id):\n        self._selection_model.set_selected_representation_id(\n            representation_id)\n\n    def set_expected_selection(\n        self,\n        folder_id,\n        task_name,\n        workfile_name=None,\n        representation_id=None\n    ):\n        self._expected_selection.set_expected_selection(\n            self.get_current_project_name(),\n            folder_id,\n            task_name,\n            workfile_name,\n            representation_id\n        )\n        self._trigger_expected_selection_changed()\n\n    def expected_folder_selected(self, folder_id):\n        if self._expected_selection.expected_folder_selected(folder_id):\n            self._trigger_expected_selection_changed()\n\n    def expected_task_selected(self, folder_id, task_name):\n        if self._expected_selection.expected_task_selected(\n            folder_id, task_name\n        ):\n            self._trigger_expected_selection_changed()\n\n    def expected_workfile_selected(self, folder_id, task_name, workfile_name):\n        if self._expected_selection.expected_workfile_selected(\n            folder_id, task_name, workfile_name\n        ):\n            self._trigger_expected_selection_changed()\n\n    def expected_representation_selected(\n        self, folder_id, task_name, representation_id\n    ):\n        if self._expected_selection.expected_representation_selected(\n            folder_id, task_name, representation_id\n        ):\n            self._trigger_expected_selection_changed()\n\n    def get_expected_selection_data(self):\n        return self._expected_selection.get_expected_selection_data()\n\n    def go_to_current_context(self):\n        self.set_expected_selection(\n            self._current_folder_id, self._current_task_name\n        )\n\n    # Model functions\n    def get_folder_items(self, project_name, sender=None):\n        return self._hierarchy_model.get_folder_items(project_name, sender)\n\n    def get_task_items(self, project_name, folder_id, sender=None):\n        return self._hierarchy_model.get_task_items(\n            project_name, folder_id, sender\n        )\n\n    def get_workarea_dir_by_context(self, folder_id, task_id):\n        return self._workfiles_model.get_workarea_dir_by_context(\n            folder_id, task_id)\n\n    def get_workarea_file_items(self, folder_id, task_name, sender=None):\n        task_id = self._get_task_id(folder_id, task_name)\n        return self._workfiles_model.get_workarea_file_items(\n            folder_id, task_id\n        )\n\n    def get_workarea_save_as_data(self, folder_id, task_id):\n        return self._workfiles_model.get_workarea_save_as_data(\n            folder_id, task_id)\n\n    def fill_workarea_filepath(\n        self,\n        folder_id,\n        task_id,\n        extension,\n        use_last_version,\n        version,\n        comment,\n    ):\n        return self._workfiles_model.fill_workarea_filepath(\n            folder_id,\n            task_id,\n            extension,\n            use_last_version,\n            version,\n            comment,\n        )\n\n    def get_published_file_items(self, folder_id, task_id):\n        return self._workfiles_model.get_published_file_items(\n            folder_id, task_id\n        )\n\n    def get_published_workfile_info(\n        self,\n        folder_id: Optional[str],\n        representation_id: Optional[str],\n    ) -&gt; PublishedWorkfileWrap:\n        return self._workfiles_model.get_published_workfile_info(\n            folder_id, representation_id\n        )\n\n    def get_workfile_info(self, folder_id, task_id, rootless_path):\n        return self._workfiles_model.get_workfile_info(\n            folder_id, task_id, rootless_path\n        )\n\n    def save_workfile_info(\n        self,\n        task_id,\n        rootless_path,\n        version=None,\n        comment=None,\n        description=None,\n    ):\n        self._workfiles_model.save_workfile_info(\n            task_id,\n            rootless_path,\n            version,\n            comment,\n            description,\n        )\n\n    def get_workfile_entities(self, task_id):\n        return self._workfiles_model.get_workfile_entities(task_id)\n\n    def reset(self):\n        if not self._host_is_valid:\n            self._emit_event(\"controller.reset.started\")\n            self._emit_event(\"controller.reset.finished\")\n            return\n        expected_folder_id = self.get_selected_folder_id()\n        expected_task_name = self.get_selected_task_name()\n        expected_work_path = self.get_selected_workfile_path()\n        expected_repre_id = self.get_selected_representation_id()\n        expected_work_name = None\n        if expected_work_path:\n            expected_work_name = os.path.basename(expected_work_path)\n\n        self._emit_event(\"controller.reset.started\")\n\n        context = self._get_host_current_context()\n\n        project_name = context[\"project_name\"]\n        folder_path = context[\"folder_path\"]\n        task_name = context[\"task_name\"]\n        current_file = self.get_current_workfile()\n        folder_id = None\n        if folder_path:\n            folder = ayon_api.get_folder_by_path(project_name, folder_path)\n            if folder:\n                folder_id = folder[\"id\"]\n\n        self._project_anatomy = None\n\n        self._current_project_name = project_name\n        self._current_folder_path = folder_path\n        self._current_folder_id = folder_id\n        self._current_task_name = task_name\n\n        self._settings_model.reset()\n        self._users_model.reset()\n        self._projects_model.reset()\n        self._hierarchy_model.reset()\n        self._workfiles_model.reset()\n\n        if not expected_folder_id:\n            expected_folder_id = folder_id\n            expected_task_name = task_name\n            if current_file:\n                expected_work_name = os.path.basename(current_file)\n\n        self._emit_event(\"controller.reset.finished\")\n\n        self._expected_selection.set_expected_selection(\n            project_name,\n            expected_folder_id,\n            expected_task_name,\n            expected_work_name,\n            expected_repre_id,\n        )\n\n    # Controller actions\n    def open_workfile(self, folder_id, task_id, filepath):\n        self._workfiles_model.open_workfile(folder_id, task_id, filepath)\n\n    def save_current_workfile(self):\n        self._workfiles_model.save_current_workfile()\n\n    def save_as_workfile(\n        self,\n        folder_id,\n        task_id,\n        rootless_workdir,\n        workdir,\n        filename,\n        version,\n        comment,\n        description,\n    ):\n        self._workfiles_model.save_as_workfile(\n            folder_id,\n            task_id,\n            rootless_workdir,\n            workdir,\n            filename,\n            version,\n            comment,\n            description,\n        )\n\n    def copy_workfile_representation(\n        self,\n        representation_id,\n        representation_filepath,\n        folder_id,\n        task_id,\n        workdir,\n        filename,\n        rootless_workdir,\n        version,\n        comment,\n        description,\n    ):\n        self._workfiles_model.copy_workfile_representation(\n            representation_id,\n            representation_filepath,\n            folder_id,\n            task_id,\n            workdir,\n            filename,\n            rootless_workdir,\n            version,\n            comment,\n            description,\n        )\n\n    def duplicate_workfile(\n        self,\n        folder_id,\n        task_id,\n        src_filepath,\n        rootless_workdir,\n        workdir,\n        filename,\n        version,\n        comment,\n        description\n    ):\n        self._workfiles_model.duplicate_workfile(\n            folder_id,\n            task_id,\n            src_filepath,\n            rootless_workdir,\n            workdir,\n            filename,\n            version,\n            comment,\n            description,\n        )\n\n    def get_my_tasks_entity_ids(self, project_name: str):\n        username = self._get_my_username()\n        assignees = []\n        if username:\n            assignees.append(username)\n        return self._hierarchy_model.get_entity_ids_for_assignees(\n            project_name, assignees\n        )\n\n    def _get_my_username(self):\n        if self._username is NOT_SET:\n            self._username = get_ayon_username()\n        return self._username\n\n    def _emit_event(self, topic, data=None):\n        self.emit_event(topic, data, \"controller\")\n\n    def _get_task_id(self, folder_id, task_name, sender=None):\n        task_item = self._hierarchy_model.get_task_item_by_name(\n            self.get_current_project_name(),\n            folder_id,\n            task_name,\n            sender\n        )\n        if not task_item:\n            return None\n        return task_item.id\n\n    def _get_host_current_context(self):\n        if hasattr(self._host, \"get_current_context\"):\n            return self._host.get_current_context()\n        return get_global_context()\n\n    # Expected selection\n    # - expected selection is used to restore selection after refresh\n    #   or when current context should be used\n    def _trigger_expected_selection_changed(self):\n        self._emit_event(\n            \"expected_selection_changed\",\n            self._expected_selection.get_expected_selection_data(),\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/control.html#client.ayon_core.tools.workfiles.control.BaseWorkfileController.event_system","title":"<code>event_system</code>  <code>property</code>","text":"<p>Inner event system for workfiles tool controller.</p> <p>Is used for communication with UI. Event system is created on demand.</p> <p>Returns:</p> Name Type Description <code>QueuedEventSystem</code> <p>Event system which can trigger callbacks for topics.</p>"},{"location":"autoapi/client/ayon_core/tools/workfiles/control.html#client.ayon_core.tools.workfiles.control.BaseWorkfileController.emit_event","title":"<code>emit_event(topic, data=None, source=None)</code>","text":"<p>Use implemented event system to trigger event.</p> Source code in <code>client/ayon_core/tools/workfiles/control.py</code> <pre><code>def emit_event(self, topic, data=None, source=None):\n    \"\"\"Use implemented event system to trigger event.\"\"\"\n\n    if data is None:\n        data = {}\n    self.event_system.emit(topic, data, source)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/control.html#client.ayon_core.tools.workfiles.control.BaseWorkfileController.is_save_enabled","title":"<code>is_save_enabled()</code>","text":"<p>Is workfile save enabled.</p> <p>Returns:</p> Name Type Description <code>bool</code> <p>True if save is enabled.</p> Source code in <code>client/ayon_core/tools/workfiles/control.py</code> <pre><code>def is_save_enabled(self):\n    \"\"\"Is workfile save enabled.\n\n    Returns:\n        bool: True if save is enabled.\n    \"\"\"\n\n    return self._save_is_enabled\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/control.html#client.ayon_core.tools.workfiles.control.BaseWorkfileController.set_save_enabled","title":"<code>set_save_enabled(enabled)</code>","text":"<p>Enable or disabled workfile save.</p> <p>Parameters:</p> Name Type Description Default <code>enabled</code> <code>bool</code> <p>Enable save workfile when True.</p> required Source code in <code>client/ayon_core/tools/workfiles/control.py</code> <pre><code>def set_save_enabled(self, enabled):\n    \"\"\"Enable or disabled workfile save.\n\n    Args:\n        enabled (bool): Enable save workfile when True.\n    \"\"\"\n\n    if self._save_is_enabled == enabled:\n        return\n\n    self._save_is_enabled = enabled\n    self._emit_event(\n        \"workfile_save_enable.changed\",\n        {\"enabled\": enabled}\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/lock_dialog.html","title":"lock_dialog","text":""},{"location":"autoapi/client/ayon_core/tools/workfiles/models/index.html","title":"models","text":""},{"location":"autoapi/client/ayon_core/tools/workfiles/models/index.html#client.ayon_core.tools.workfiles.models.SelectionModel","title":"<code>SelectionModel</code>","text":"<p>               Bases: <code>object</code></p> <p>Model handling selection changes.</p> <p>Triggering events: - \"selection.folder.changed\" - \"selection.task.changed\" - \"selection.workarea.changed\" - \"selection.representation.changed\"</p> Source code in <code>client/ayon_core/tools/workfiles/models/selection.py</code> <pre><code>class SelectionModel(object):\n    \"\"\"Model handling selection changes.\n\n    Triggering events:\n    - \"selection.folder.changed\"\n    - \"selection.task.changed\"\n    - \"selection.workarea.changed\"\n    - \"selection.representation.changed\"\n    \"\"\"\n\n    event_source = \"selection.model\"\n\n    def __init__(self, controller):\n        self._controller = controller\n\n        self._folder_id = None\n        self._task_name = None\n        self._task_id = None\n        self._workfile_path = None\n        self._rootless_workfile_path = None\n        self._workfile_entity_id = None\n        self._representation_id = None\n\n    def get_selected_folder_id(self):\n        return self._folder_id\n\n    def set_selected_folder(self, folder_id):\n        if folder_id == self._folder_id:\n            return\n\n        self._folder_id = folder_id\n        self._controller.emit_event(\n            \"selection.folder.changed\",\n            {\n                \"project_name\": self._controller.get_current_project_name(),\n                \"folder_id\": folder_id\n            },\n            self.event_source\n        )\n\n    def get_selected_task_name(self):\n        return self._task_name\n\n    def get_selected_task_id(self):\n        return self._task_id\n\n    def set_selected_task(self, task_id, task_name):\n        if task_id == self._task_id:\n            return\n\n        self._task_name = task_name\n        self._task_id = task_id\n        self._controller.emit_event(\n            \"selection.task.changed\",\n            {\n                \"project_name\": self._controller.get_current_project_name(),\n                \"folder_id\": self._folder_id,\n                \"task_name\": task_name,\n                \"task_id\": task_id\n            },\n            self.event_source\n        )\n\n    def get_selected_workfile_path(self):\n        return self._workfile_path\n\n    def get_selected_workfile_data(self):\n        return {\n            \"project_name\": self._controller.get_current_project_name(),\n            \"path\": self._workfile_path,\n            \"rootless_path\": self._rootless_workfile_path,\n            \"folder_id\": self._folder_id,\n            \"task_name\": self._task_name,\n            \"task_id\": self._task_id,\n            \"workfile_entity_id\": self._workfile_entity_id,\n        }\n\n    def set_selected_workfile_path(\n        self, rootless_path, path, workfile_entity_id\n    ):\n        if path == self._workfile_path:\n            return\n\n        self._rootless_workfile_path = rootless_path\n        self._workfile_path = path\n        self._workfile_entity_id = workfile_entity_id\n        self._controller.emit_event(\n            \"selection.workarea.changed\",\n            self.get_selected_workfile_data(),\n            self.event_source\n        )\n\n    def get_selected_representation_id(self):\n        return self._representation_id\n\n    def get_selected_representation_data(self):\n        return {\n            \"project_name\": self._controller.get_current_project_name(),\n            \"folder_id\": self._folder_id,\n            \"task_id\": self._task_id,\n            \"representation_id\": self._representation_id,\n        }\n\n    def set_selected_representation_id(self, representation_id):\n        if representation_id == self._representation_id:\n            return\n        self._representation_id = representation_id\n        self._controller.emit_event(\n            \"selection.representation.changed\",\n            self.get_selected_representation_data(),\n            self.event_source\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/models/index.html#client.ayon_core.tools.workfiles.models.WorkfilesModel","title":"<code>WorkfilesModel</code>","text":"<p>Workfiles model.</p> Source code in <code>client/ayon_core/tools/workfiles/models/workfiles.py</code> <pre><code>class WorkfilesModel:\n    \"\"\"Workfiles model.\"\"\"\n\n    def __init__(\n        self,\n        host: IWorkfileHost,\n        controller: AbstractWorkfilesBackend\n    ):\n        self._host: IWorkfileHost = host\n        self._controller: AbstractWorkfilesBackend = controller\n\n        self._log = Logger.get_logger(\"WorkfilesModel\")\n        extensions = None\n        if controller.is_host_valid():\n            extensions = controller.get_workfile_extensions()\n        self._extensions: Optional[set[str]] = extensions\n\n        self._current_username = _NOT_SET\n\n        # Workarea\n        self._base_data = None\n        self._fill_data_by_folder_id = {}\n        self._task_data_by_folder_id = {}\n        self._workdir_by_context = {}\n        self._workarea_file_items_mapping = {}\n        self._workarea_file_items_cache = NestedCacheItem(\n            levels=1, default_factory=list\n        )\n\n        # Published workfiles\n        self._repre_by_id = {}\n        self._version_comment_by_id = {}\n        self._published_workfile_items_cache = NestedCacheItem(\n            levels=1, default_factory=list\n        )\n\n        # Entities\n        self._workfile_entities_by_task_id = {}\n\n    def reset(self):\n        self._base_data = None\n        self._fill_data_by_folder_id = {}\n        self._task_data_by_folder_id = {}\n        self._workdir_by_context = {}\n        self._workarea_file_items_mapping = {}\n        self._workarea_file_items_cache.reset()\n\n        self._repre_by_id = {}\n        self._version_comment_by_id = {}\n        self._published_workfile_items_cache.reset()\n\n        self._workfile_entities_by_task_id = {}\n\n    # Host functionality\n    def get_current_workfile(self):\n        return self._host.get_current_workfile()\n\n    def open_workfile(self, folder_id, task_id, filepath):\n        self._emit_event(\"open_workfile.started\")\n\n        failed = False\n        try:\n            self._open_workfile(folder_id, task_id, filepath)\n\n        except Exception:\n            failed = True\n            self._log.warning(\"Open of workfile failed\", exc_info=True)\n\n        self._emit_event(\n            \"open_workfile.finished\",\n            {\"failed\": failed},\n        )\n\n    def save_current_workfile(self):\n        current_file = self.get_current_workfile()\n        self._host.save_workfile(current_file)\n\n    def save_as_workfile(\n        self,\n        folder_id,\n        task_id,\n        rootless_workdir,\n        workdir,\n        filename,\n        version,\n        comment,\n        description,\n    ):\n        self._emit_event(\"save_as.started\")\n\n        filepath = os.path.join(workdir, filename)\n        rootless_path = f\"{rootless_workdir}/{filename}\"\n        project_name = self._controller.get_current_project_name()\n        project_entity = self._controller.get_project_entity(project_name)\n        folder_entity = self._controller.get_folder_entity(\n            project_name, folder_id\n        )\n        task_entity = self._controller.get_task_entity(\n            project_name, task_id\n        )\n\n        prepared_data = SaveWorkfileOptionalData(\n            project_entity=project_entity,\n            anatomy=self._controller.project_anatomy,\n            project_settings=self._controller.project_settings,\n            rootless_path=rootless_path,\n            workfile_entities=self.get_workfile_entities(task_id),\n        )\n        failed = False\n        try:\n            self._host.save_workfile_with_context(\n                filepath,\n                folder_entity,\n                task_entity,\n                version=version,\n                comment=comment,\n                description=description,\n                prepared_data=prepared_data,\n            )\n            self._update_workfile_info(\n                task_id, rootless_path, description\n            )\n            self._update_current_context(\n                folder_id, folder_entity[\"path\"], task_entity[\"name\"]\n            )\n\n        except Exception:\n            failed = True\n            self._log.warning(\"Save as failed\", exc_info=True)\n\n        self._emit_event(\n            \"save_as.finished\",\n            {\"failed\": failed},\n        )\n\n    def copy_workfile_representation(\n        self,\n        representation_id,\n        representation_filepath,\n        folder_id,\n        task_id,\n        workdir,\n        filename,\n        rootless_workdir,\n        version,\n        comment,\n        description,\n    ):\n        self._emit_event(\"copy_representation.started\")\n\n        project_name = self._project_name\n        project_entity = self._controller.get_project_entity(project_name)\n        folder_entity = self._controller.get_folder_entity(\n            project_name, folder_id\n        )\n        task_entity = self._controller.get_task_entity(\n            project_name, task_id\n        )\n        repre_entity = self._repre_by_id.get(representation_id)\n        dst_filepath = os.path.join(workdir, filename)\n        rootless_path = f\"{rootless_workdir}/{filename}\"\n\n        prepared_data = CopyPublishedWorkfileOptionalData(\n            project_entity=project_entity,\n            anatomy=self._controller.project_anatomy,\n            project_settings=self._controller.project_settings,\n            rootless_path=rootless_path,\n            representation_path=representation_filepath,\n            workfile_entities=self.get_workfile_entities(task_id),\n            src_anatomy=self._controller.project_anatomy,\n        )\n        failed = False\n        try:\n            self._host.copy_workfile_representation(\n                project_name,\n                repre_entity,\n                dst_filepath,\n                folder_entity,\n                task_entity,\n                version=version,\n                comment=comment,\n                description=description,\n                prepared_data=prepared_data,\n            )\n            self._update_workfile_info(\n                task_id, rootless_path, description\n            )\n            self._update_current_context(\n                folder_id, folder_entity[\"path\"], task_entity[\"name\"]\n            )\n\n        except Exception:\n            failed = True\n            self._log.warning(\n                \"Copy of workfile representation failed\", exc_info=True\n            )\n\n        self._emit_event(\n            \"copy_representation.finished\",\n            {\"failed\": failed},\n        )\n\n    def duplicate_workfile(\n        self,\n        folder_id,\n        task_id,\n        src_filepath,\n        rootless_workdir,\n        workdir,\n        filename,\n        version,\n        comment,\n        description\n    ):\n        self._emit_event(\"workfile_duplicate.started\")\n\n        project_name = self._controller.get_current_project_name()\n        project_entity = self._controller.get_project_entity(project_name)\n        folder_entity = self._controller.get_folder_entity(\n            project_name, folder_id\n        )\n        task_entity = self._controller.get_task_entity(project_name, task_id)\n        workfile_entities = self.get_workfile_entities(task_id)\n        rootless_path = f\"{rootless_workdir}/{filename}\"\n        workfile_path = os.path.join(workdir, filename)\n\n        prepared_data = CopyWorkfileOptionalData(\n            project_entity=project_entity,\n            project_settings=self._controller.project_settings,\n            anatomy=self._controller.project_anatomy,\n            rootless_path=rootless_path,\n            workfile_entities=workfile_entities,\n        )\n        failed = False\n        try:\n            self._host.copy_workfile(\n                src_filepath,\n                workfile_path,\n                folder_entity,\n                task_entity,\n                version=version,\n                comment=comment,\n                description=description,\n                prepared_data=prepared_data,\n            )\n\n        except Exception:\n            failed = True\n            self._log.warning(\"Duplication of workfile failed\", exc_info=True)\n\n        self._emit_event(\n            \"workfile_duplicate.finished\",\n            {\"failed\": failed},\n        )\n\n    def get_workfile_entities(self, task_id: str):\n        if not task_id:\n            return []\n        workfile_entities = self._workfile_entities_by_task_id.get(task_id)\n        if workfile_entities is None:\n            workfile_entities = list(ayon_api.get_workfiles_info(\n                self._project_name,\n                task_ids=[task_id],\n            ))\n            self._workfile_entities_by_task_id[task_id] = workfile_entities\n        return workfile_entities\n\n    def get_workfile_info(\n        self,\n        folder_id: Optional[str],\n        task_id: Optional[str],\n        rootless_path: Optional[str]\n    ):\n        if not folder_id or not task_id or not rootless_path:\n            return None\n\n        mapping = self._workarea_file_items_mapping.get(task_id)\n        if mapping is None:\n            self._cache_file_items(folder_id, task_id)\n            mapping = self._workarea_file_items_mapping[task_id]\n        return mapping.get(rootless_path)\n\n    def save_workfile_info(\n        self,\n        task_id: str,\n        rootless_path: str,\n        version: Optional[int],\n        comment: Optional[str],\n        description: Optional[str],\n    ):\n        self._save_workfile_info(\n            task_id,\n            rootless_path,\n            version,\n            comment,\n            description,\n        )\n\n        self._update_file_description(\n            task_id, rootless_path, description\n        )\n\n    def get_workarea_dir_by_context(\n        self, folder_id: str, task_id: str\n    ) -&gt; Optional[str]:\n        \"\"\"Workarea dir for passed context.\n\n        The directory path is based on project anatomy templates.\n\n        Args:\n            folder_id (str): Folder id.\n            task_id (str): Task id.\n\n        Returns:\n            Optional[str]: Workarea dir path or None for invalid context.\n\n        \"\"\"\n        if not folder_id or not task_id:\n            return None\n        folder_mapping = self._workdir_by_context.setdefault(folder_id, {})\n        workdir = folder_mapping.get(task_id)\n        if workdir is not None:\n            return workdir\n\n        workdir_data = self._prepare_fill_data(folder_id, task_id)\n\n        workdir = get_workdir_with_workdir_data(\n            workdir_data,\n            self._project_name,\n            anatomy=self._controller.project_anatomy,\n        )\n        folder_mapping[task_id] = workdir\n        return workdir\n\n    def get_workarea_file_items(self, folder_id, task_id):\n        \"\"\"Workfile items for passed context from workarea.\n\n        Args:\n            folder_id (Optional[str]): Folder id.\n            task_id (Optional[str]): Task id.\n\n        Returns:\n            list[WorkfileInfo]: List of file items matching workarea of passed\n                context.\n\n        \"\"\"\n        return self._cache_file_items(folder_id, task_id)\n\n    def get_workarea_save_as_data(\n        self, folder_id: Optional[str], task_id: Optional[str]\n    ) -&gt; dict[str, Any]:\n        folder_entity = None\n        task_entity = None\n        if folder_id:\n            folder_entity = self._controller.get_folder_entity(\n                self._project_name, folder_id\n            )\n            if folder_entity and task_id:\n                task_entity = self._controller.get_task_entity(\n                    self._project_name, task_id\n                )\n\n        if not folder_entity or not task_entity or self._extensions is None:\n            return {\n                \"template_key\": None,\n                \"template_has_version\": None,\n                \"template_has_comment\": None,\n                \"ext\": None,\n                \"workdir\": None,\n                \"rootless_workdir\": None,\n                \"comment\": None,\n                \"comment_hints\": None,\n                \"last_version\": None,\n                \"extensions\": None,\n            }\n\n        anatomy = self._controller.project_anatomy\n        fill_data = self._prepare_fill_data(folder_id, task_id)\n        template_key = self._get_template_key(fill_data)\n\n        current_workfile = self._controller.get_current_workfile()\n        current_filename = None\n        current_ext = None\n        if current_workfile:\n            current_filename = os.path.basename(current_workfile)\n            current_ext = os.path.splitext(current_filename)[1].lower()\n\n        extensions = self._extensions\n        if not current_ext and extensions:\n            current_ext = tuple(extensions)[0]\n\n        workdir = self._get_workdir(anatomy, template_key, fill_data)\n\n        rootless_workdir = workdir.rootless\n        if platform.system().lower() == \"windows\":\n            rootless_workdir = rootless_workdir.replace(\"\\\\\", \"/\")\n\n        file_template = anatomy.get_template_item(\n            \"work\", template_key, \"file\"\n        )\n        file_template_str = file_template.template\n\n        template_has_version = \"{version\" in file_template_str\n        template_has_comment = \"{comment\" in file_template_str\n\n        file_items = self.get_workarea_file_items(folder_id, task_id)\n        comment_hints = set()\n        comment = None\n        for item in file_items:\n            filepath = item.filepath\n            filename = os.path.basename(filepath)\n            if filename == current_filename:\n                comment = item.comment\n\n            if item.comment:\n                comment_hints.add(item.comment)\n        comment_hints = list(comment_hints)\n\n        last_version = self._get_last_workfile_version(\n            file_items, task_entity\n        )\n\n        return {\n            \"template_key\": template_key,\n            \"template_has_version\": template_has_version,\n            \"template_has_comment\": template_has_comment,\n            \"ext\": current_ext,\n            \"workdir\": workdir,\n            \"rootless_workdir\": rootless_workdir,\n            \"comment\": comment,\n            \"comment_hints\": comment_hints,\n            \"last_version\": last_version,\n            \"extensions\": extensions,\n        }\n\n    def fill_workarea_filepath(\n        self,\n        folder_id: str,\n        task_id: str,\n        extension: str,\n        use_last_version: bool,\n        version: int,\n        comment: str,\n    ) -&gt; WorkareaFilepathResult:\n        \"\"\"Fill workarea filepath based on context.\n\n        Args:\n            folder_id (str): Folder id.\n            task_id (str): Task id.\n            extension (str): File extension.\n            use_last_version (bool): Use last version.\n            version (int): Version number.\n            comment (str): Comment.\n\n        Returns:\n            WorkareaFilepathResult: Workarea filepath result.\n\n        \"\"\"\n        anatomy = self._controller.project_anatomy\n        fill_data = self._prepare_fill_data(folder_id, task_id)\n\n        template_key = self._get_template_key(fill_data)\n\n        workdir = self._get_workdir(anatomy, template_key, fill_data)\n\n        file_template = anatomy.get_template_item(\n            \"work\", template_key, \"file\"\n        )\n\n        if use_last_version:\n            file_items = self.get_workarea_file_items(folder_id, task_id)\n            task_entity = self._controller.get_task_entity(\n                self._project_name, task_id\n            )\n            version = self._get_last_workfile_version(\n                file_items, task_entity\n            )\n        fill_data[\"version\"] = version\n        fill_data[\"ext\"] = extension.lstrip(\".\")\n\n        if comment:\n            fill_data[\"comment\"] = comment\n\n        filename = file_template.format(fill_data)\n        if not filename.solved:\n            filename = None\n\n        exists = False\n        if filename:\n            filepath = os.path.join(workdir, filename)\n            exists = os.path.exists(filepath)\n\n        return WorkareaFilepathResult(\n            workdir,\n            filename,\n            exists\n        )\n\n    def get_published_file_items(\n        self, folder_id: Optional[str], task_id: Optional[str]\n    ) -&gt; list[PublishedWorkfileInfo]:\n        \"\"\"Published workfiles for passed context.\n\n        Args:\n            folder_id (Optional[str]): Folder id.\n            task_id (Optional[str]): Task id.\n\n        Returns:\n            list[PublishedWorkfileInfo]: List of files for published workfiles.\n\n        \"\"\"\n        if not folder_id:\n            return []\n\n        cache = self._published_workfile_items_cache[folder_id]\n        if not cache.is_valid:\n            project_name = self._project_name\n            anatomy = self._controller.project_anatomy\n            # TODO remove when server &gt;= 1.14.0 is required\n            # Backwards compatibility for product base types\n            filter_key = \"product_base_types\"\n            if ayon_api.get_server_version_tuple() &lt; (1, 14, 0):\n                filter_key = \"product_types\"\n            filter_kwargs = {filter_key: {\"workfile\"}}\n            product_entities = list(ayon_api.get_products(\n                project_name,\n                folder_ids={folder_id},\n                **filter_kwargs,\n                fields={\"id\", \"name\"}\n            ))\n\n            version_entities = []\n            product_ids = {product[\"id\"] for product in product_entities}\n            if product_ids:\n                # Get version docs of products with their families\n                version_entities = list(ayon_api.get_versions(\n                    project_name,\n                    product_ids=product_ids,\n                    fields={\"id\", \"author\", \"taskId\", \"attrib.comment\"},\n                ))\n\n            repre_entities = []\n            if version_entities:\n                repre_entities = list(ayon_api.get_representations(\n                    project_name,\n                    version_ids={v[\"id\"] for v in version_entities}\n                ))\n\n            self._repre_by_id.update({\n                repre_entity[\"id\"]: repre_entity\n                for repre_entity in repre_entities\n            })\n\n            # Map versions by representation ID for easy lookup\n            self._version_comment_by_id.update({\n                version_entity[\"id\"]: version_entity[\"attrib\"].get(\"comment\")\n                for version_entity in version_entities\n            })\n\n            project_entity = self._controller.get_project_entity(project_name)\n\n            prepared_data = ListPublishedWorkfilesOptionalData(\n                project_entity=project_entity,\n                anatomy=anatomy,\n                project_settings=self._controller.project_settings,\n                product_entities=product_entities,\n                version_entities=version_entities,\n                repre_entities=repre_entities,\n            )\n            cache.update_data(self._host.list_published_workfiles(\n                project_name,\n                folder_id,\n                prepared_data=prepared_data,\n            ))\n\n        items = cache.get_data()\n\n        if task_id:\n            items = [\n                item\n                for item in items\n                if item.task_id == task_id\n            ]\n        return items\n\n    def get_published_workfile_info(\n        self,\n        folder_id: Optional[str],\n        representation_id: Optional[str],\n    ) -&gt; PublishedWorkfileWrap:\n        \"\"\"Get published workfile info by representation ID.\n\n        Args:\n            folder_id (Optional[str]): Folder id.\n            representation_id (Optional[str]): Representation id.\n\n        Returns:\n            PublishedWorkfileWrap: Published workfile info or None\n                if not found.\n\n        \"\"\"\n        if not representation_id:\n            return PublishedWorkfileWrap()\n\n        # Search through all cached published workfile items\n        for item in self.get_published_file_items(folder_id, None):\n            if item.representation_id == representation_id:\n                comment = self._get_published_workfile_version_comment(\n                    representation_id\n                )\n                return PublishedWorkfileWrap(item, comment)\n        return PublishedWorkfileWrap()\n\n    @property\n    def _project_name(self) -&gt; str:\n        return self._controller.get_current_project_name()\n\n    @property\n    def _host_name(self) -&gt; str:\n        return self._host.name\n\n    def _emit_event(self, topic, data=None):\n        self._controller.emit_event(topic, data, \"workfiles\")\n\n    def _get_current_username(self) -&gt; str:\n        if self._current_username is _NOT_SET:\n            self._current_username = get_ayon_username()\n        return self._current_username\n\n    def _get_published_workfile_version_comment(\n        self, representation_id: str\n    ) -&gt; Optional[str]:\n        \"\"\"Get version comment for published workfile.\n\n        Args:\n            representation_id (str): Representation id.\n\n        Returns:\n            Optional[str]: Version comment or None.\n\n        \"\"\"\n        if not representation_id:\n            return None\n        repre = self._repre_by_id.get(representation_id)\n        if not repre:\n            return None\n        return self._version_comment_by_id.get(repre[\"versionId\"])\n\n    # --- Host ---\n    def _open_workfile(self, folder_id: str, task_id: str, filepath: str):\n        # TODO move to workfiles pipeline\n        project_name = self._project_name\n        project_entity = self._controller.get_project_entity(project_name)\n        folder_entity = self._controller.get_folder_entity(\n            project_name, folder_id\n        )\n        task_entity = self._controller.get_task_entity(\n            project_name, task_id\n        )\n        prepared_data = OpenWorkfileOptionalData(\n            project_entity=project_entity,\n            anatomy=self._controller.project_anatomy,\n            project_settings=self._controller.project_settings,\n        )\n        self._host.open_workfile_with_context(\n            filepath, folder_entity, task_entity, prepared_data=prepared_data\n        )\n        self._update_current_context(\n            folder_id, folder_entity[\"path\"], task_entity[\"name\"]\n        )\n\n    def _update_current_context(self, folder_id, folder_path, task_name):\n        self._current_folder_id = folder_id\n        self._current_folder_path = folder_path\n        self._current_task_name = task_name\n\n    # --- Workarea ---\n    def _reset_workarea_file_items(self, task_id: str):\n        cache: CacheItem = self._workarea_file_items_cache[task_id]\n        cache.set_invalid()\n        self._workarea_file_items_mapping.pop(task_id, None)\n\n    def _get_base_data(self) -&gt; dict[str, Any]:\n        if self._base_data is None:\n            base_data = get_template_data(\n                ayon_api.get_project(self._project_name),\n                host_name=self._host_name,\n            )\n            self._base_data = base_data\n        return copy.deepcopy(self._base_data)\n\n    def _get_folder_data(self, folder_id: str) -&gt; dict[str, Any]:\n        fill_data = self._fill_data_by_folder_id.get(folder_id)\n        if fill_data is None:\n            folder = self._controller.get_folder_entity(\n                self._project_name, folder_id\n            )\n            fill_data = get_folder_template_data(folder, self._project_name)\n            self._fill_data_by_folder_id[folder_id] = fill_data\n        return copy.deepcopy(fill_data)\n\n    def _get_task_data(\n        self,\n        project_entity: dict[str, Any],\n        folder_id: str,\n        task_id: str\n    ) -&gt; dict[str, Any]:\n        task_data = self._task_data_by_folder_id.setdefault(folder_id, {})\n        if task_id not in task_data:\n            task_entity = self._controller.get_task_entity(\n                self._project_name, task_id\n            )\n            if task_entity:\n                task_data[task_id] = get_task_template_data(\n                    project_entity, task_entity\n                )\n        return copy.deepcopy(task_data[task_id])\n\n    def _prepare_fill_data(\n        self, folder_id: str, task_id: str\n    ) -&gt; dict[str, Any]:\n        if not folder_id or not task_id:\n            return {}\n\n        base_data = self._get_base_data()\n        project_name = base_data[\"project\"][\"name\"]\n        folder_data = self._get_folder_data(folder_id)\n        project_entity = self._controller.get_project_entity(project_name)\n        task_data = self._get_task_data(project_entity, folder_id, task_id)\n\n        base_data.update(folder_data)\n        base_data.update(task_data)\n\n        return base_data\n\n    def _cache_file_items(\n        self, folder_id: Optional[str], task_id: Optional[str]\n    ) -&gt; list[WorkfileInfo]:\n        if not folder_id or not task_id:\n            return []\n\n        cache: CacheItem = self._workarea_file_items_cache[task_id]\n        if cache.is_valid:\n            return cache.get_data()\n\n        project_entity = self._controller.get_project_entity(\n            self._project_name\n        )\n        folder_entity = self._controller.get_folder_entity(\n            self._project_name, folder_id\n        )\n        task_entity = self._controller.get_task_entity(\n            self._project_name, task_id\n        )\n        anatomy = self._controller.project_anatomy\n        project_settings = self._controller.project_settings\n        workfile_entities = self._controller.get_workfile_entities(task_id)\n\n        fill_data = self._prepare_fill_data(folder_id, task_id)\n        template_key = self._get_template_key(fill_data)\n\n        prepared_data = ListWorkfilesOptionalData(\n            project_entity=project_entity,\n            anatomy=anatomy,\n            project_settings=project_settings,\n            template_key=template_key,\n            workfile_entities=workfile_entities,\n        )\n\n        items = self._host.list_workfiles(\n            self._project_name,\n            folder_entity,\n            task_entity,\n            prepared_data=prepared_data,\n        )\n        cache.update_data(items)\n\n        # Cache items by entity ids and rootless path\n        self._workarea_file_items_mapping[task_id] = {\n            item.rootless_path: item\n            for item in items\n        }\n\n        return items\n\n    def _get_template_key(self, fill_data: dict[str, Any]) -&gt; str:\n        task_type = fill_data.get(\"task\", {}).get(\"type\")\n        # TODO cache\n        return get_workfile_template_key(\n            self._project_name,\n            task_type,\n            self._host_name,\n            project_settings=self._controller.project_settings,\n        )\n\n    def _get_last_workfile_version(\n        self, file_items: list[WorkfileInfo], task_entity: dict[str, Any]\n    ) -&gt; int:\n        \"\"\"\n\n        Todos:\n            Validate if logic of this function is correct. It does return\n                last version + 1 which might be wrong.\n\n        Args:\n            file_items (list[WorkfileInfo]): Workfile items.\n            task_entity (dict[str, Any]): Task entity.\n\n        Returns:\n            int: Next workfile version.\n\n        \"\"\"\n        versions = {\n            item.version\n            for item in file_items\n            if item.version is not None\n        }\n        if versions:\n            return max(versions) + 1\n\n        return get_versioning_start(\n            self._project_name,\n            self._host_name,\n            task_name=task_entity[\"name\"],\n            task_type=task_entity[\"taskType\"],\n            product_base_type=\"workfile\",\n            project_settings=self._controller.project_settings,\n        )\n\n    def _get_workdir(\n        self, anatomy: \"Anatomy\", template_key: str, fill_data: dict[str, Any]\n    ):\n        directory_template = anatomy.get_template_item(\n            \"work\", template_key, \"directory\"\n        )\n        return directory_template.format_strict(fill_data).normalized()\n\n    def _update_workfile_info(\n        self,\n        task_id: str,\n        rootless_path: str,\n        description: str,\n    ):\n        self._update_file_description(task_id, rootless_path, description)\n        self._reset_workarea_file_items(task_id)\n\n        # Update workfile entity cache if are cached\n        if task_id in self._workfile_entities_by_task_id:\n            workfile_entities = self.get_workfile_entities(task_id)\n\n            target_workfile_entity = None\n            for workfile_entity in workfile_entities:\n                if rootless_path == workfile_entity[\"path\"]:\n                    target_workfile_entity = workfile_entity\n                    break\n\n            if target_workfile_entity is None:\n                self._workfile_entities_by_task_id.pop(task_id, None)\n                self.get_workfile_entities(task_id)\n            else:\n                target_workfile_entity[\"attrib\"][\"description\"] = description\n\n    def _update_file_description(\n        self, task_id: str, rootless_path: str, description: str\n    ):\n        mapping = self._workarea_file_items_mapping.get(task_id)\n        if not mapping:\n            return\n        item = mapping.get(rootless_path)\n        if item is not None:\n            item.description = description\n\n    # --- Workfile entities ---\n    def _save_workfile_info(\n        self,\n        task_id: str,\n        rootless_path: str,\n        version: Optional[int],\n        comment: Optional[str],\n        description: Optional[str],\n    ):\n        workfile_entity = save_workfile_info(\n            self._controller.get_current_project_name(),\n            task_id,\n            rootless_path,\n            self._controller.get_host_name(),\n            version=version,\n            comment=comment,\n            description=description,\n            workfile_entities=self.get_workfile_entities(task_id),\n        )\n        # Update cache\n        workfile_entities = self.get_workfile_entities(task_id)\n        match_idx = None\n        for idx, entity in enumerate(workfile_entities):\n            if entity[\"id\"] == workfile_entity[\"id\"]:\n                # Update existing entity\n                match_idx = idx\n                break\n\n        if match_idx is None:\n            workfile_entities.append(workfile_entity)\n        else:\n            workfile_entities[match_idx] = workfile_entity\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/models/index.html#client.ayon_core.tools.workfiles.models.WorkfilesModel.fill_workarea_filepath","title":"<code>fill_workarea_filepath(folder_id, task_id, extension, use_last_version, version, comment)</code>","text":"<p>Fill workarea filepath based on context.</p> <p>Parameters:</p> Name Type Description Default <code>folder_id</code> <code>str</code> <p>Folder id.</p> required <code>task_id</code> <code>str</code> <p>Task id.</p> required <code>extension</code> <code>str</code> <p>File extension.</p> required <code>use_last_version</code> <code>bool</code> <p>Use last version.</p> required <code>version</code> <code>int</code> <p>Version number.</p> required <code>comment</code> <code>str</code> <p>Comment.</p> required <p>Returns:</p> Name Type Description <code>WorkareaFilepathResult</code> <code>WorkareaFilepathResult</code> <p>Workarea filepath result.</p> Source code in <code>client/ayon_core/tools/workfiles/models/workfiles.py</code> <pre><code>def fill_workarea_filepath(\n    self,\n    folder_id: str,\n    task_id: str,\n    extension: str,\n    use_last_version: bool,\n    version: int,\n    comment: str,\n) -&gt; WorkareaFilepathResult:\n    \"\"\"Fill workarea filepath based on context.\n\n    Args:\n        folder_id (str): Folder id.\n        task_id (str): Task id.\n        extension (str): File extension.\n        use_last_version (bool): Use last version.\n        version (int): Version number.\n        comment (str): Comment.\n\n    Returns:\n        WorkareaFilepathResult: Workarea filepath result.\n\n    \"\"\"\n    anatomy = self._controller.project_anatomy\n    fill_data = self._prepare_fill_data(folder_id, task_id)\n\n    template_key = self._get_template_key(fill_data)\n\n    workdir = self._get_workdir(anatomy, template_key, fill_data)\n\n    file_template = anatomy.get_template_item(\n        \"work\", template_key, \"file\"\n    )\n\n    if use_last_version:\n        file_items = self.get_workarea_file_items(folder_id, task_id)\n        task_entity = self._controller.get_task_entity(\n            self._project_name, task_id\n        )\n        version = self._get_last_workfile_version(\n            file_items, task_entity\n        )\n    fill_data[\"version\"] = version\n    fill_data[\"ext\"] = extension.lstrip(\".\")\n\n    if comment:\n        fill_data[\"comment\"] = comment\n\n    filename = file_template.format(fill_data)\n    if not filename.solved:\n        filename = None\n\n    exists = False\n    if filename:\n        filepath = os.path.join(workdir, filename)\n        exists = os.path.exists(filepath)\n\n    return WorkareaFilepathResult(\n        workdir,\n        filename,\n        exists\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/models/index.html#client.ayon_core.tools.workfiles.models.WorkfilesModel.get_published_file_items","title":"<code>get_published_file_items(folder_id, task_id)</code>","text":"<p>Published workfiles for passed context.</p> <p>Parameters:</p> Name Type Description Default <code>folder_id</code> <code>Optional[str]</code> <p>Folder id.</p> required <code>task_id</code> <code>Optional[str]</code> <p>Task id.</p> required <p>Returns:</p> Type Description <code>list[PublishedWorkfileInfo]</code> <p>list[PublishedWorkfileInfo]: List of files for published workfiles.</p> Source code in <code>client/ayon_core/tools/workfiles/models/workfiles.py</code> <pre><code>def get_published_file_items(\n    self, folder_id: Optional[str], task_id: Optional[str]\n) -&gt; list[PublishedWorkfileInfo]:\n    \"\"\"Published workfiles for passed context.\n\n    Args:\n        folder_id (Optional[str]): Folder id.\n        task_id (Optional[str]): Task id.\n\n    Returns:\n        list[PublishedWorkfileInfo]: List of files for published workfiles.\n\n    \"\"\"\n    if not folder_id:\n        return []\n\n    cache = self._published_workfile_items_cache[folder_id]\n    if not cache.is_valid:\n        project_name = self._project_name\n        anatomy = self._controller.project_anatomy\n        # TODO remove when server &gt;= 1.14.0 is required\n        # Backwards compatibility for product base types\n        filter_key = \"product_base_types\"\n        if ayon_api.get_server_version_tuple() &lt; (1, 14, 0):\n            filter_key = \"product_types\"\n        filter_kwargs = {filter_key: {\"workfile\"}}\n        product_entities = list(ayon_api.get_products(\n            project_name,\n            folder_ids={folder_id},\n            **filter_kwargs,\n            fields={\"id\", \"name\"}\n        ))\n\n        version_entities = []\n        product_ids = {product[\"id\"] for product in product_entities}\n        if product_ids:\n            # Get version docs of products with their families\n            version_entities = list(ayon_api.get_versions(\n                project_name,\n                product_ids=product_ids,\n                fields={\"id\", \"author\", \"taskId\", \"attrib.comment\"},\n            ))\n\n        repre_entities = []\n        if version_entities:\n            repre_entities = list(ayon_api.get_representations(\n                project_name,\n                version_ids={v[\"id\"] for v in version_entities}\n            ))\n\n        self._repre_by_id.update({\n            repre_entity[\"id\"]: repre_entity\n            for repre_entity in repre_entities\n        })\n\n        # Map versions by representation ID for easy lookup\n        self._version_comment_by_id.update({\n            version_entity[\"id\"]: version_entity[\"attrib\"].get(\"comment\")\n            for version_entity in version_entities\n        })\n\n        project_entity = self._controller.get_project_entity(project_name)\n\n        prepared_data = ListPublishedWorkfilesOptionalData(\n            project_entity=project_entity,\n            anatomy=anatomy,\n            project_settings=self._controller.project_settings,\n            product_entities=product_entities,\n            version_entities=version_entities,\n            repre_entities=repre_entities,\n        )\n        cache.update_data(self._host.list_published_workfiles(\n            project_name,\n            folder_id,\n            prepared_data=prepared_data,\n        ))\n\n    items = cache.get_data()\n\n    if task_id:\n        items = [\n            item\n            for item in items\n            if item.task_id == task_id\n        ]\n    return items\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/models/index.html#client.ayon_core.tools.workfiles.models.WorkfilesModel.get_published_workfile_info","title":"<code>get_published_workfile_info(folder_id, representation_id)</code>","text":"<p>Get published workfile info by representation ID.</p> <p>Parameters:</p> Name Type Description Default <code>folder_id</code> <code>Optional[str]</code> <p>Folder id.</p> required <code>representation_id</code> <code>Optional[str]</code> <p>Representation id.</p> required <p>Returns:</p> Name Type Description <code>PublishedWorkfileWrap</code> <code>PublishedWorkfileWrap</code> <p>Published workfile info or None if not found.</p> Source code in <code>client/ayon_core/tools/workfiles/models/workfiles.py</code> <pre><code>def get_published_workfile_info(\n    self,\n    folder_id: Optional[str],\n    representation_id: Optional[str],\n) -&gt; PublishedWorkfileWrap:\n    \"\"\"Get published workfile info by representation ID.\n\n    Args:\n        folder_id (Optional[str]): Folder id.\n        representation_id (Optional[str]): Representation id.\n\n    Returns:\n        PublishedWorkfileWrap: Published workfile info or None\n            if not found.\n\n    \"\"\"\n    if not representation_id:\n        return PublishedWorkfileWrap()\n\n    # Search through all cached published workfile items\n    for item in self.get_published_file_items(folder_id, None):\n        if item.representation_id == representation_id:\n            comment = self._get_published_workfile_version_comment(\n                representation_id\n            )\n            return PublishedWorkfileWrap(item, comment)\n    return PublishedWorkfileWrap()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/models/index.html#client.ayon_core.tools.workfiles.models.WorkfilesModel.get_workarea_dir_by_context","title":"<code>get_workarea_dir_by_context(folder_id, task_id)</code>","text":"<p>Workarea dir for passed context.</p> <p>The directory path is based on project anatomy templates.</p> <p>Parameters:</p> Name Type Description Default <code>folder_id</code> <code>str</code> <p>Folder id.</p> required <code>task_id</code> <code>str</code> <p>Task id.</p> required <p>Returns:</p> Type Description <code>Optional[str]</code> <p>Optional[str]: Workarea dir path or None for invalid context.</p> Source code in <code>client/ayon_core/tools/workfiles/models/workfiles.py</code> <pre><code>def get_workarea_dir_by_context(\n    self, folder_id: str, task_id: str\n) -&gt; Optional[str]:\n    \"\"\"Workarea dir for passed context.\n\n    The directory path is based on project anatomy templates.\n\n    Args:\n        folder_id (str): Folder id.\n        task_id (str): Task id.\n\n    Returns:\n        Optional[str]: Workarea dir path or None for invalid context.\n\n    \"\"\"\n    if not folder_id or not task_id:\n        return None\n    folder_mapping = self._workdir_by_context.setdefault(folder_id, {})\n    workdir = folder_mapping.get(task_id)\n    if workdir is not None:\n        return workdir\n\n    workdir_data = self._prepare_fill_data(folder_id, task_id)\n\n    workdir = get_workdir_with_workdir_data(\n        workdir_data,\n        self._project_name,\n        anatomy=self._controller.project_anatomy,\n    )\n    folder_mapping[task_id] = workdir\n    return workdir\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/models/index.html#client.ayon_core.tools.workfiles.models.WorkfilesModel.get_workarea_file_items","title":"<code>get_workarea_file_items(folder_id, task_id)</code>","text":"<p>Workfile items for passed context from workarea.</p> <p>Parameters:</p> Name Type Description Default <code>folder_id</code> <code>Optional[str]</code> <p>Folder id.</p> required <code>task_id</code> <code>Optional[str]</code> <p>Task id.</p> required <p>Returns:</p> Type Description <p>list[WorkfileInfo]: List of file items matching workarea of passed context.</p> Source code in <code>client/ayon_core/tools/workfiles/models/workfiles.py</code> <pre><code>def get_workarea_file_items(self, folder_id, task_id):\n    \"\"\"Workfile items for passed context from workarea.\n\n    Args:\n        folder_id (Optional[str]): Folder id.\n        task_id (Optional[str]): Task id.\n\n    Returns:\n        list[WorkfileInfo]: List of file items matching workarea of passed\n            context.\n\n    \"\"\"\n    return self._cache_file_items(folder_id, task_id)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/models/selection.html","title":"selection","text":""},{"location":"autoapi/client/ayon_core/tools/workfiles/models/selection.html#client.ayon_core.tools.workfiles.models.selection.SelectionModel","title":"<code>SelectionModel</code>","text":"<p>               Bases: <code>object</code></p> <p>Model handling selection changes.</p> <p>Triggering events: - \"selection.folder.changed\" - \"selection.task.changed\" - \"selection.workarea.changed\" - \"selection.representation.changed\"</p> Source code in <code>client/ayon_core/tools/workfiles/models/selection.py</code> <pre><code>class SelectionModel(object):\n    \"\"\"Model handling selection changes.\n\n    Triggering events:\n    - \"selection.folder.changed\"\n    - \"selection.task.changed\"\n    - \"selection.workarea.changed\"\n    - \"selection.representation.changed\"\n    \"\"\"\n\n    event_source = \"selection.model\"\n\n    def __init__(self, controller):\n        self._controller = controller\n\n        self._folder_id = None\n        self._task_name = None\n        self._task_id = None\n        self._workfile_path = None\n        self._rootless_workfile_path = None\n        self._workfile_entity_id = None\n        self._representation_id = None\n\n    def get_selected_folder_id(self):\n        return self._folder_id\n\n    def set_selected_folder(self, folder_id):\n        if folder_id == self._folder_id:\n            return\n\n        self._folder_id = folder_id\n        self._controller.emit_event(\n            \"selection.folder.changed\",\n            {\n                \"project_name\": self._controller.get_current_project_name(),\n                \"folder_id\": folder_id\n            },\n            self.event_source\n        )\n\n    def get_selected_task_name(self):\n        return self._task_name\n\n    def get_selected_task_id(self):\n        return self._task_id\n\n    def set_selected_task(self, task_id, task_name):\n        if task_id == self._task_id:\n            return\n\n        self._task_name = task_name\n        self._task_id = task_id\n        self._controller.emit_event(\n            \"selection.task.changed\",\n            {\n                \"project_name\": self._controller.get_current_project_name(),\n                \"folder_id\": self._folder_id,\n                \"task_name\": task_name,\n                \"task_id\": task_id\n            },\n            self.event_source\n        )\n\n    def get_selected_workfile_path(self):\n        return self._workfile_path\n\n    def get_selected_workfile_data(self):\n        return {\n            \"project_name\": self._controller.get_current_project_name(),\n            \"path\": self._workfile_path,\n            \"rootless_path\": self._rootless_workfile_path,\n            \"folder_id\": self._folder_id,\n            \"task_name\": self._task_name,\n            \"task_id\": self._task_id,\n            \"workfile_entity_id\": self._workfile_entity_id,\n        }\n\n    def set_selected_workfile_path(\n        self, rootless_path, path, workfile_entity_id\n    ):\n        if path == self._workfile_path:\n            return\n\n        self._rootless_workfile_path = rootless_path\n        self._workfile_path = path\n        self._workfile_entity_id = workfile_entity_id\n        self._controller.emit_event(\n            \"selection.workarea.changed\",\n            self.get_selected_workfile_data(),\n            self.event_source\n        )\n\n    def get_selected_representation_id(self):\n        return self._representation_id\n\n    def get_selected_representation_data(self):\n        return {\n            \"project_name\": self._controller.get_current_project_name(),\n            \"folder_id\": self._folder_id,\n            \"task_id\": self._task_id,\n            \"representation_id\": self._representation_id,\n        }\n\n    def set_selected_representation_id(self, representation_id):\n        if representation_id == self._representation_id:\n            return\n        self._representation_id = representation_id\n        self._controller.emit_event(\n            \"selection.representation.changed\",\n            self.get_selected_representation_data(),\n            self.event_source\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/models/workfiles.html","title":"workfiles","text":""},{"location":"autoapi/client/ayon_core/tools/workfiles/models/workfiles.html#client.ayon_core.tools.workfiles.models.workfiles.WorkfilesModel","title":"<code>WorkfilesModel</code>","text":"<p>Workfiles model.</p> Source code in <code>client/ayon_core/tools/workfiles/models/workfiles.py</code> <pre><code>class WorkfilesModel:\n    \"\"\"Workfiles model.\"\"\"\n\n    def __init__(\n        self,\n        host: IWorkfileHost,\n        controller: AbstractWorkfilesBackend\n    ):\n        self._host: IWorkfileHost = host\n        self._controller: AbstractWorkfilesBackend = controller\n\n        self._log = Logger.get_logger(\"WorkfilesModel\")\n        extensions = None\n        if controller.is_host_valid():\n            extensions = controller.get_workfile_extensions()\n        self._extensions: Optional[set[str]] = extensions\n\n        self._current_username = _NOT_SET\n\n        # Workarea\n        self._base_data = None\n        self._fill_data_by_folder_id = {}\n        self._task_data_by_folder_id = {}\n        self._workdir_by_context = {}\n        self._workarea_file_items_mapping = {}\n        self._workarea_file_items_cache = NestedCacheItem(\n            levels=1, default_factory=list\n        )\n\n        # Published workfiles\n        self._repre_by_id = {}\n        self._version_comment_by_id = {}\n        self._published_workfile_items_cache = NestedCacheItem(\n            levels=1, default_factory=list\n        )\n\n        # Entities\n        self._workfile_entities_by_task_id = {}\n\n    def reset(self):\n        self._base_data = None\n        self._fill_data_by_folder_id = {}\n        self._task_data_by_folder_id = {}\n        self._workdir_by_context = {}\n        self._workarea_file_items_mapping = {}\n        self._workarea_file_items_cache.reset()\n\n        self._repre_by_id = {}\n        self._version_comment_by_id = {}\n        self._published_workfile_items_cache.reset()\n\n        self._workfile_entities_by_task_id = {}\n\n    # Host functionality\n    def get_current_workfile(self):\n        return self._host.get_current_workfile()\n\n    def open_workfile(self, folder_id, task_id, filepath):\n        self._emit_event(\"open_workfile.started\")\n\n        failed = False\n        try:\n            self._open_workfile(folder_id, task_id, filepath)\n\n        except Exception:\n            failed = True\n            self._log.warning(\"Open of workfile failed\", exc_info=True)\n\n        self._emit_event(\n            \"open_workfile.finished\",\n            {\"failed\": failed},\n        )\n\n    def save_current_workfile(self):\n        current_file = self.get_current_workfile()\n        self._host.save_workfile(current_file)\n\n    def save_as_workfile(\n        self,\n        folder_id,\n        task_id,\n        rootless_workdir,\n        workdir,\n        filename,\n        version,\n        comment,\n        description,\n    ):\n        self._emit_event(\"save_as.started\")\n\n        filepath = os.path.join(workdir, filename)\n        rootless_path = f\"{rootless_workdir}/{filename}\"\n        project_name = self._controller.get_current_project_name()\n        project_entity = self._controller.get_project_entity(project_name)\n        folder_entity = self._controller.get_folder_entity(\n            project_name, folder_id\n        )\n        task_entity = self._controller.get_task_entity(\n            project_name, task_id\n        )\n\n        prepared_data = SaveWorkfileOptionalData(\n            project_entity=project_entity,\n            anatomy=self._controller.project_anatomy,\n            project_settings=self._controller.project_settings,\n            rootless_path=rootless_path,\n            workfile_entities=self.get_workfile_entities(task_id),\n        )\n        failed = False\n        try:\n            self._host.save_workfile_with_context(\n                filepath,\n                folder_entity,\n                task_entity,\n                version=version,\n                comment=comment,\n                description=description,\n                prepared_data=prepared_data,\n            )\n            self._update_workfile_info(\n                task_id, rootless_path, description\n            )\n            self._update_current_context(\n                folder_id, folder_entity[\"path\"], task_entity[\"name\"]\n            )\n\n        except Exception:\n            failed = True\n            self._log.warning(\"Save as failed\", exc_info=True)\n\n        self._emit_event(\n            \"save_as.finished\",\n            {\"failed\": failed},\n        )\n\n    def copy_workfile_representation(\n        self,\n        representation_id,\n        representation_filepath,\n        folder_id,\n        task_id,\n        workdir,\n        filename,\n        rootless_workdir,\n        version,\n        comment,\n        description,\n    ):\n        self._emit_event(\"copy_representation.started\")\n\n        project_name = self._project_name\n        project_entity = self._controller.get_project_entity(project_name)\n        folder_entity = self._controller.get_folder_entity(\n            project_name, folder_id\n        )\n        task_entity = self._controller.get_task_entity(\n            project_name, task_id\n        )\n        repre_entity = self._repre_by_id.get(representation_id)\n        dst_filepath = os.path.join(workdir, filename)\n        rootless_path = f\"{rootless_workdir}/{filename}\"\n\n        prepared_data = CopyPublishedWorkfileOptionalData(\n            project_entity=project_entity,\n            anatomy=self._controller.project_anatomy,\n            project_settings=self._controller.project_settings,\n            rootless_path=rootless_path,\n            representation_path=representation_filepath,\n            workfile_entities=self.get_workfile_entities(task_id),\n            src_anatomy=self._controller.project_anatomy,\n        )\n        failed = False\n        try:\n            self._host.copy_workfile_representation(\n                project_name,\n                repre_entity,\n                dst_filepath,\n                folder_entity,\n                task_entity,\n                version=version,\n                comment=comment,\n                description=description,\n                prepared_data=prepared_data,\n            )\n            self._update_workfile_info(\n                task_id, rootless_path, description\n            )\n            self._update_current_context(\n                folder_id, folder_entity[\"path\"], task_entity[\"name\"]\n            )\n\n        except Exception:\n            failed = True\n            self._log.warning(\n                \"Copy of workfile representation failed\", exc_info=True\n            )\n\n        self._emit_event(\n            \"copy_representation.finished\",\n            {\"failed\": failed},\n        )\n\n    def duplicate_workfile(\n        self,\n        folder_id,\n        task_id,\n        src_filepath,\n        rootless_workdir,\n        workdir,\n        filename,\n        version,\n        comment,\n        description\n    ):\n        self._emit_event(\"workfile_duplicate.started\")\n\n        project_name = self._controller.get_current_project_name()\n        project_entity = self._controller.get_project_entity(project_name)\n        folder_entity = self._controller.get_folder_entity(\n            project_name, folder_id\n        )\n        task_entity = self._controller.get_task_entity(project_name, task_id)\n        workfile_entities = self.get_workfile_entities(task_id)\n        rootless_path = f\"{rootless_workdir}/{filename}\"\n        workfile_path = os.path.join(workdir, filename)\n\n        prepared_data = CopyWorkfileOptionalData(\n            project_entity=project_entity,\n            project_settings=self._controller.project_settings,\n            anatomy=self._controller.project_anatomy,\n            rootless_path=rootless_path,\n            workfile_entities=workfile_entities,\n        )\n        failed = False\n        try:\n            self._host.copy_workfile(\n                src_filepath,\n                workfile_path,\n                folder_entity,\n                task_entity,\n                version=version,\n                comment=comment,\n                description=description,\n                prepared_data=prepared_data,\n            )\n\n        except Exception:\n            failed = True\n            self._log.warning(\"Duplication of workfile failed\", exc_info=True)\n\n        self._emit_event(\n            \"workfile_duplicate.finished\",\n            {\"failed\": failed},\n        )\n\n    def get_workfile_entities(self, task_id: str):\n        if not task_id:\n            return []\n        workfile_entities = self._workfile_entities_by_task_id.get(task_id)\n        if workfile_entities is None:\n            workfile_entities = list(ayon_api.get_workfiles_info(\n                self._project_name,\n                task_ids=[task_id],\n            ))\n            self._workfile_entities_by_task_id[task_id] = workfile_entities\n        return workfile_entities\n\n    def get_workfile_info(\n        self,\n        folder_id: Optional[str],\n        task_id: Optional[str],\n        rootless_path: Optional[str]\n    ):\n        if not folder_id or not task_id or not rootless_path:\n            return None\n\n        mapping = self._workarea_file_items_mapping.get(task_id)\n        if mapping is None:\n            self._cache_file_items(folder_id, task_id)\n            mapping = self._workarea_file_items_mapping[task_id]\n        return mapping.get(rootless_path)\n\n    def save_workfile_info(\n        self,\n        task_id: str,\n        rootless_path: str,\n        version: Optional[int],\n        comment: Optional[str],\n        description: Optional[str],\n    ):\n        self._save_workfile_info(\n            task_id,\n            rootless_path,\n            version,\n            comment,\n            description,\n        )\n\n        self._update_file_description(\n            task_id, rootless_path, description\n        )\n\n    def get_workarea_dir_by_context(\n        self, folder_id: str, task_id: str\n    ) -&gt; Optional[str]:\n        \"\"\"Workarea dir for passed context.\n\n        The directory path is based on project anatomy templates.\n\n        Args:\n            folder_id (str): Folder id.\n            task_id (str): Task id.\n\n        Returns:\n            Optional[str]: Workarea dir path or None for invalid context.\n\n        \"\"\"\n        if not folder_id or not task_id:\n            return None\n        folder_mapping = self._workdir_by_context.setdefault(folder_id, {})\n        workdir = folder_mapping.get(task_id)\n        if workdir is not None:\n            return workdir\n\n        workdir_data = self._prepare_fill_data(folder_id, task_id)\n\n        workdir = get_workdir_with_workdir_data(\n            workdir_data,\n            self._project_name,\n            anatomy=self._controller.project_anatomy,\n        )\n        folder_mapping[task_id] = workdir\n        return workdir\n\n    def get_workarea_file_items(self, folder_id, task_id):\n        \"\"\"Workfile items for passed context from workarea.\n\n        Args:\n            folder_id (Optional[str]): Folder id.\n            task_id (Optional[str]): Task id.\n\n        Returns:\n            list[WorkfileInfo]: List of file items matching workarea of passed\n                context.\n\n        \"\"\"\n        return self._cache_file_items(folder_id, task_id)\n\n    def get_workarea_save_as_data(\n        self, folder_id: Optional[str], task_id: Optional[str]\n    ) -&gt; dict[str, Any]:\n        folder_entity = None\n        task_entity = None\n        if folder_id:\n            folder_entity = self._controller.get_folder_entity(\n                self._project_name, folder_id\n            )\n            if folder_entity and task_id:\n                task_entity = self._controller.get_task_entity(\n                    self._project_name, task_id\n                )\n\n        if not folder_entity or not task_entity or self._extensions is None:\n            return {\n                \"template_key\": None,\n                \"template_has_version\": None,\n                \"template_has_comment\": None,\n                \"ext\": None,\n                \"workdir\": None,\n                \"rootless_workdir\": None,\n                \"comment\": None,\n                \"comment_hints\": None,\n                \"last_version\": None,\n                \"extensions\": None,\n            }\n\n        anatomy = self._controller.project_anatomy\n        fill_data = self._prepare_fill_data(folder_id, task_id)\n        template_key = self._get_template_key(fill_data)\n\n        current_workfile = self._controller.get_current_workfile()\n        current_filename = None\n        current_ext = None\n        if current_workfile:\n            current_filename = os.path.basename(current_workfile)\n            current_ext = os.path.splitext(current_filename)[1].lower()\n\n        extensions = self._extensions\n        if not current_ext and extensions:\n            current_ext = tuple(extensions)[0]\n\n        workdir = self._get_workdir(anatomy, template_key, fill_data)\n\n        rootless_workdir = workdir.rootless\n        if platform.system().lower() == \"windows\":\n            rootless_workdir = rootless_workdir.replace(\"\\\\\", \"/\")\n\n        file_template = anatomy.get_template_item(\n            \"work\", template_key, \"file\"\n        )\n        file_template_str = file_template.template\n\n        template_has_version = \"{version\" in file_template_str\n        template_has_comment = \"{comment\" in file_template_str\n\n        file_items = self.get_workarea_file_items(folder_id, task_id)\n        comment_hints = set()\n        comment = None\n        for item in file_items:\n            filepath = item.filepath\n            filename = os.path.basename(filepath)\n            if filename == current_filename:\n                comment = item.comment\n\n            if item.comment:\n                comment_hints.add(item.comment)\n        comment_hints = list(comment_hints)\n\n        last_version = self._get_last_workfile_version(\n            file_items, task_entity\n        )\n\n        return {\n            \"template_key\": template_key,\n            \"template_has_version\": template_has_version,\n            \"template_has_comment\": template_has_comment,\n            \"ext\": current_ext,\n            \"workdir\": workdir,\n            \"rootless_workdir\": rootless_workdir,\n            \"comment\": comment,\n            \"comment_hints\": comment_hints,\n            \"last_version\": last_version,\n            \"extensions\": extensions,\n        }\n\n    def fill_workarea_filepath(\n        self,\n        folder_id: str,\n        task_id: str,\n        extension: str,\n        use_last_version: bool,\n        version: int,\n        comment: str,\n    ) -&gt; WorkareaFilepathResult:\n        \"\"\"Fill workarea filepath based on context.\n\n        Args:\n            folder_id (str): Folder id.\n            task_id (str): Task id.\n            extension (str): File extension.\n            use_last_version (bool): Use last version.\n            version (int): Version number.\n            comment (str): Comment.\n\n        Returns:\n            WorkareaFilepathResult: Workarea filepath result.\n\n        \"\"\"\n        anatomy = self._controller.project_anatomy\n        fill_data = self._prepare_fill_data(folder_id, task_id)\n\n        template_key = self._get_template_key(fill_data)\n\n        workdir = self._get_workdir(anatomy, template_key, fill_data)\n\n        file_template = anatomy.get_template_item(\n            \"work\", template_key, \"file\"\n        )\n\n        if use_last_version:\n            file_items = self.get_workarea_file_items(folder_id, task_id)\n            task_entity = self._controller.get_task_entity(\n                self._project_name, task_id\n            )\n            version = self._get_last_workfile_version(\n                file_items, task_entity\n            )\n        fill_data[\"version\"] = version\n        fill_data[\"ext\"] = extension.lstrip(\".\")\n\n        if comment:\n            fill_data[\"comment\"] = comment\n\n        filename = file_template.format(fill_data)\n        if not filename.solved:\n            filename = None\n\n        exists = False\n        if filename:\n            filepath = os.path.join(workdir, filename)\n            exists = os.path.exists(filepath)\n\n        return WorkareaFilepathResult(\n            workdir,\n            filename,\n            exists\n        )\n\n    def get_published_file_items(\n        self, folder_id: Optional[str], task_id: Optional[str]\n    ) -&gt; list[PublishedWorkfileInfo]:\n        \"\"\"Published workfiles for passed context.\n\n        Args:\n            folder_id (Optional[str]): Folder id.\n            task_id (Optional[str]): Task id.\n\n        Returns:\n            list[PublishedWorkfileInfo]: List of files for published workfiles.\n\n        \"\"\"\n        if not folder_id:\n            return []\n\n        cache = self._published_workfile_items_cache[folder_id]\n        if not cache.is_valid:\n            project_name = self._project_name\n            anatomy = self._controller.project_anatomy\n            # TODO remove when server &gt;= 1.14.0 is required\n            # Backwards compatibility for product base types\n            filter_key = \"product_base_types\"\n            if ayon_api.get_server_version_tuple() &lt; (1, 14, 0):\n                filter_key = \"product_types\"\n            filter_kwargs = {filter_key: {\"workfile\"}}\n            product_entities = list(ayon_api.get_products(\n                project_name,\n                folder_ids={folder_id},\n                **filter_kwargs,\n                fields={\"id\", \"name\"}\n            ))\n\n            version_entities = []\n            product_ids = {product[\"id\"] for product in product_entities}\n            if product_ids:\n                # Get version docs of products with their families\n                version_entities = list(ayon_api.get_versions(\n                    project_name,\n                    product_ids=product_ids,\n                    fields={\"id\", \"author\", \"taskId\", \"attrib.comment\"},\n                ))\n\n            repre_entities = []\n            if version_entities:\n                repre_entities = list(ayon_api.get_representations(\n                    project_name,\n                    version_ids={v[\"id\"] for v in version_entities}\n                ))\n\n            self._repre_by_id.update({\n                repre_entity[\"id\"]: repre_entity\n                for repre_entity in repre_entities\n            })\n\n            # Map versions by representation ID for easy lookup\n            self._version_comment_by_id.update({\n                version_entity[\"id\"]: version_entity[\"attrib\"].get(\"comment\")\n                for version_entity in version_entities\n            })\n\n            project_entity = self._controller.get_project_entity(project_name)\n\n            prepared_data = ListPublishedWorkfilesOptionalData(\n                project_entity=project_entity,\n                anatomy=anatomy,\n                project_settings=self._controller.project_settings,\n                product_entities=product_entities,\n                version_entities=version_entities,\n                repre_entities=repre_entities,\n            )\n            cache.update_data(self._host.list_published_workfiles(\n                project_name,\n                folder_id,\n                prepared_data=prepared_data,\n            ))\n\n        items = cache.get_data()\n\n        if task_id:\n            items = [\n                item\n                for item in items\n                if item.task_id == task_id\n            ]\n        return items\n\n    def get_published_workfile_info(\n        self,\n        folder_id: Optional[str],\n        representation_id: Optional[str],\n    ) -&gt; PublishedWorkfileWrap:\n        \"\"\"Get published workfile info by representation ID.\n\n        Args:\n            folder_id (Optional[str]): Folder id.\n            representation_id (Optional[str]): Representation id.\n\n        Returns:\n            PublishedWorkfileWrap: Published workfile info or None\n                if not found.\n\n        \"\"\"\n        if not representation_id:\n            return PublishedWorkfileWrap()\n\n        # Search through all cached published workfile items\n        for item in self.get_published_file_items(folder_id, None):\n            if item.representation_id == representation_id:\n                comment = self._get_published_workfile_version_comment(\n                    representation_id\n                )\n                return PublishedWorkfileWrap(item, comment)\n        return PublishedWorkfileWrap()\n\n    @property\n    def _project_name(self) -&gt; str:\n        return self._controller.get_current_project_name()\n\n    @property\n    def _host_name(self) -&gt; str:\n        return self._host.name\n\n    def _emit_event(self, topic, data=None):\n        self._controller.emit_event(topic, data, \"workfiles\")\n\n    def _get_current_username(self) -&gt; str:\n        if self._current_username is _NOT_SET:\n            self._current_username = get_ayon_username()\n        return self._current_username\n\n    def _get_published_workfile_version_comment(\n        self, representation_id: str\n    ) -&gt; Optional[str]:\n        \"\"\"Get version comment for published workfile.\n\n        Args:\n            representation_id (str): Representation id.\n\n        Returns:\n            Optional[str]: Version comment or None.\n\n        \"\"\"\n        if not representation_id:\n            return None\n        repre = self._repre_by_id.get(representation_id)\n        if not repre:\n            return None\n        return self._version_comment_by_id.get(repre[\"versionId\"])\n\n    # --- Host ---\n    def _open_workfile(self, folder_id: str, task_id: str, filepath: str):\n        # TODO move to workfiles pipeline\n        project_name = self._project_name\n        project_entity = self._controller.get_project_entity(project_name)\n        folder_entity = self._controller.get_folder_entity(\n            project_name, folder_id\n        )\n        task_entity = self._controller.get_task_entity(\n            project_name, task_id\n        )\n        prepared_data = OpenWorkfileOptionalData(\n            project_entity=project_entity,\n            anatomy=self._controller.project_anatomy,\n            project_settings=self._controller.project_settings,\n        )\n        self._host.open_workfile_with_context(\n            filepath, folder_entity, task_entity, prepared_data=prepared_data\n        )\n        self._update_current_context(\n            folder_id, folder_entity[\"path\"], task_entity[\"name\"]\n        )\n\n    def _update_current_context(self, folder_id, folder_path, task_name):\n        self._current_folder_id = folder_id\n        self._current_folder_path = folder_path\n        self._current_task_name = task_name\n\n    # --- Workarea ---\n    def _reset_workarea_file_items(self, task_id: str):\n        cache: CacheItem = self._workarea_file_items_cache[task_id]\n        cache.set_invalid()\n        self._workarea_file_items_mapping.pop(task_id, None)\n\n    def _get_base_data(self) -&gt; dict[str, Any]:\n        if self._base_data is None:\n            base_data = get_template_data(\n                ayon_api.get_project(self._project_name),\n                host_name=self._host_name,\n            )\n            self._base_data = base_data\n        return copy.deepcopy(self._base_data)\n\n    def _get_folder_data(self, folder_id: str) -&gt; dict[str, Any]:\n        fill_data = self._fill_data_by_folder_id.get(folder_id)\n        if fill_data is None:\n            folder = self._controller.get_folder_entity(\n                self._project_name, folder_id\n            )\n            fill_data = get_folder_template_data(folder, self._project_name)\n            self._fill_data_by_folder_id[folder_id] = fill_data\n        return copy.deepcopy(fill_data)\n\n    def _get_task_data(\n        self,\n        project_entity: dict[str, Any],\n        folder_id: str,\n        task_id: str\n    ) -&gt; dict[str, Any]:\n        task_data = self._task_data_by_folder_id.setdefault(folder_id, {})\n        if task_id not in task_data:\n            task_entity = self._controller.get_task_entity(\n                self._project_name, task_id\n            )\n            if task_entity:\n                task_data[task_id] = get_task_template_data(\n                    project_entity, task_entity\n                )\n        return copy.deepcopy(task_data[task_id])\n\n    def _prepare_fill_data(\n        self, folder_id: str, task_id: str\n    ) -&gt; dict[str, Any]:\n        if not folder_id or not task_id:\n            return {}\n\n        base_data = self._get_base_data()\n        project_name = base_data[\"project\"][\"name\"]\n        folder_data = self._get_folder_data(folder_id)\n        project_entity = self._controller.get_project_entity(project_name)\n        task_data = self._get_task_data(project_entity, folder_id, task_id)\n\n        base_data.update(folder_data)\n        base_data.update(task_data)\n\n        return base_data\n\n    def _cache_file_items(\n        self, folder_id: Optional[str], task_id: Optional[str]\n    ) -&gt; list[WorkfileInfo]:\n        if not folder_id or not task_id:\n            return []\n\n        cache: CacheItem = self._workarea_file_items_cache[task_id]\n        if cache.is_valid:\n            return cache.get_data()\n\n        project_entity = self._controller.get_project_entity(\n            self._project_name\n        )\n        folder_entity = self._controller.get_folder_entity(\n            self._project_name, folder_id\n        )\n        task_entity = self._controller.get_task_entity(\n            self._project_name, task_id\n        )\n        anatomy = self._controller.project_anatomy\n        project_settings = self._controller.project_settings\n        workfile_entities = self._controller.get_workfile_entities(task_id)\n\n        fill_data = self._prepare_fill_data(folder_id, task_id)\n        template_key = self._get_template_key(fill_data)\n\n        prepared_data = ListWorkfilesOptionalData(\n            project_entity=project_entity,\n            anatomy=anatomy,\n            project_settings=project_settings,\n            template_key=template_key,\n            workfile_entities=workfile_entities,\n        )\n\n        items = self._host.list_workfiles(\n            self._project_name,\n            folder_entity,\n            task_entity,\n            prepared_data=prepared_data,\n        )\n        cache.update_data(items)\n\n        # Cache items by entity ids and rootless path\n        self._workarea_file_items_mapping[task_id] = {\n            item.rootless_path: item\n            for item in items\n        }\n\n        return items\n\n    def _get_template_key(self, fill_data: dict[str, Any]) -&gt; str:\n        task_type = fill_data.get(\"task\", {}).get(\"type\")\n        # TODO cache\n        return get_workfile_template_key(\n            self._project_name,\n            task_type,\n            self._host_name,\n            project_settings=self._controller.project_settings,\n        )\n\n    def _get_last_workfile_version(\n        self, file_items: list[WorkfileInfo], task_entity: dict[str, Any]\n    ) -&gt; int:\n        \"\"\"\n\n        Todos:\n            Validate if logic of this function is correct. It does return\n                last version + 1 which might be wrong.\n\n        Args:\n            file_items (list[WorkfileInfo]): Workfile items.\n            task_entity (dict[str, Any]): Task entity.\n\n        Returns:\n            int: Next workfile version.\n\n        \"\"\"\n        versions = {\n            item.version\n            for item in file_items\n            if item.version is not None\n        }\n        if versions:\n            return max(versions) + 1\n\n        return get_versioning_start(\n            self._project_name,\n            self._host_name,\n            task_name=task_entity[\"name\"],\n            task_type=task_entity[\"taskType\"],\n            product_base_type=\"workfile\",\n            project_settings=self._controller.project_settings,\n        )\n\n    def _get_workdir(\n        self, anatomy: \"Anatomy\", template_key: str, fill_data: dict[str, Any]\n    ):\n        directory_template = anatomy.get_template_item(\n            \"work\", template_key, \"directory\"\n        )\n        return directory_template.format_strict(fill_data).normalized()\n\n    def _update_workfile_info(\n        self,\n        task_id: str,\n        rootless_path: str,\n        description: str,\n    ):\n        self._update_file_description(task_id, rootless_path, description)\n        self._reset_workarea_file_items(task_id)\n\n        # Update workfile entity cache if are cached\n        if task_id in self._workfile_entities_by_task_id:\n            workfile_entities = self.get_workfile_entities(task_id)\n\n            target_workfile_entity = None\n            for workfile_entity in workfile_entities:\n                if rootless_path == workfile_entity[\"path\"]:\n                    target_workfile_entity = workfile_entity\n                    break\n\n            if target_workfile_entity is None:\n                self._workfile_entities_by_task_id.pop(task_id, None)\n                self.get_workfile_entities(task_id)\n            else:\n                target_workfile_entity[\"attrib\"][\"description\"] = description\n\n    def _update_file_description(\n        self, task_id: str, rootless_path: str, description: str\n    ):\n        mapping = self._workarea_file_items_mapping.get(task_id)\n        if not mapping:\n            return\n        item = mapping.get(rootless_path)\n        if item is not None:\n            item.description = description\n\n    # --- Workfile entities ---\n    def _save_workfile_info(\n        self,\n        task_id: str,\n        rootless_path: str,\n        version: Optional[int],\n        comment: Optional[str],\n        description: Optional[str],\n    ):\n        workfile_entity = save_workfile_info(\n            self._controller.get_current_project_name(),\n            task_id,\n            rootless_path,\n            self._controller.get_host_name(),\n            version=version,\n            comment=comment,\n            description=description,\n            workfile_entities=self.get_workfile_entities(task_id),\n        )\n        # Update cache\n        workfile_entities = self.get_workfile_entities(task_id)\n        match_idx = None\n        for idx, entity in enumerate(workfile_entities):\n            if entity[\"id\"] == workfile_entity[\"id\"]:\n                # Update existing entity\n                match_idx = idx\n                break\n\n        if match_idx is None:\n            workfile_entities.append(workfile_entity)\n        else:\n            workfile_entities[match_idx] = workfile_entity\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/models/workfiles.html#client.ayon_core.tools.workfiles.models.workfiles.WorkfilesModel.fill_workarea_filepath","title":"<code>fill_workarea_filepath(folder_id, task_id, extension, use_last_version, version, comment)</code>","text":"<p>Fill workarea filepath based on context.</p> <p>Parameters:</p> Name Type Description Default <code>folder_id</code> <code>str</code> <p>Folder id.</p> required <code>task_id</code> <code>str</code> <p>Task id.</p> required <code>extension</code> <code>str</code> <p>File extension.</p> required <code>use_last_version</code> <code>bool</code> <p>Use last version.</p> required <code>version</code> <code>int</code> <p>Version number.</p> required <code>comment</code> <code>str</code> <p>Comment.</p> required <p>Returns:</p> Name Type Description <code>WorkareaFilepathResult</code> <code>WorkareaFilepathResult</code> <p>Workarea filepath result.</p> Source code in <code>client/ayon_core/tools/workfiles/models/workfiles.py</code> <pre><code>def fill_workarea_filepath(\n    self,\n    folder_id: str,\n    task_id: str,\n    extension: str,\n    use_last_version: bool,\n    version: int,\n    comment: str,\n) -&gt; WorkareaFilepathResult:\n    \"\"\"Fill workarea filepath based on context.\n\n    Args:\n        folder_id (str): Folder id.\n        task_id (str): Task id.\n        extension (str): File extension.\n        use_last_version (bool): Use last version.\n        version (int): Version number.\n        comment (str): Comment.\n\n    Returns:\n        WorkareaFilepathResult: Workarea filepath result.\n\n    \"\"\"\n    anatomy = self._controller.project_anatomy\n    fill_data = self._prepare_fill_data(folder_id, task_id)\n\n    template_key = self._get_template_key(fill_data)\n\n    workdir = self._get_workdir(anatomy, template_key, fill_data)\n\n    file_template = anatomy.get_template_item(\n        \"work\", template_key, \"file\"\n    )\n\n    if use_last_version:\n        file_items = self.get_workarea_file_items(folder_id, task_id)\n        task_entity = self._controller.get_task_entity(\n            self._project_name, task_id\n        )\n        version = self._get_last_workfile_version(\n            file_items, task_entity\n        )\n    fill_data[\"version\"] = version\n    fill_data[\"ext\"] = extension.lstrip(\".\")\n\n    if comment:\n        fill_data[\"comment\"] = comment\n\n    filename = file_template.format(fill_data)\n    if not filename.solved:\n        filename = None\n\n    exists = False\n    if filename:\n        filepath = os.path.join(workdir, filename)\n        exists = os.path.exists(filepath)\n\n    return WorkareaFilepathResult(\n        workdir,\n        filename,\n        exists\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/models/workfiles.html#client.ayon_core.tools.workfiles.models.workfiles.WorkfilesModel.get_published_file_items","title":"<code>get_published_file_items(folder_id, task_id)</code>","text":"<p>Published workfiles for passed context.</p> <p>Parameters:</p> Name Type Description Default <code>folder_id</code> <code>Optional[str]</code> <p>Folder id.</p> required <code>task_id</code> <code>Optional[str]</code> <p>Task id.</p> required <p>Returns:</p> Type Description <code>list[PublishedWorkfileInfo]</code> <p>list[PublishedWorkfileInfo]: List of files for published workfiles.</p> Source code in <code>client/ayon_core/tools/workfiles/models/workfiles.py</code> <pre><code>def get_published_file_items(\n    self, folder_id: Optional[str], task_id: Optional[str]\n) -&gt; list[PublishedWorkfileInfo]:\n    \"\"\"Published workfiles for passed context.\n\n    Args:\n        folder_id (Optional[str]): Folder id.\n        task_id (Optional[str]): Task id.\n\n    Returns:\n        list[PublishedWorkfileInfo]: List of files for published workfiles.\n\n    \"\"\"\n    if not folder_id:\n        return []\n\n    cache = self._published_workfile_items_cache[folder_id]\n    if not cache.is_valid:\n        project_name = self._project_name\n        anatomy = self._controller.project_anatomy\n        # TODO remove when server &gt;= 1.14.0 is required\n        # Backwards compatibility for product base types\n        filter_key = \"product_base_types\"\n        if ayon_api.get_server_version_tuple() &lt; (1, 14, 0):\n            filter_key = \"product_types\"\n        filter_kwargs = {filter_key: {\"workfile\"}}\n        product_entities = list(ayon_api.get_products(\n            project_name,\n            folder_ids={folder_id},\n            **filter_kwargs,\n            fields={\"id\", \"name\"}\n        ))\n\n        version_entities = []\n        product_ids = {product[\"id\"] for product in product_entities}\n        if product_ids:\n            # Get version docs of products with their families\n            version_entities = list(ayon_api.get_versions(\n                project_name,\n                product_ids=product_ids,\n                fields={\"id\", \"author\", \"taskId\", \"attrib.comment\"},\n            ))\n\n        repre_entities = []\n        if version_entities:\n            repre_entities = list(ayon_api.get_representations(\n                project_name,\n                version_ids={v[\"id\"] for v in version_entities}\n            ))\n\n        self._repre_by_id.update({\n            repre_entity[\"id\"]: repre_entity\n            for repre_entity in repre_entities\n        })\n\n        # Map versions by representation ID for easy lookup\n        self._version_comment_by_id.update({\n            version_entity[\"id\"]: version_entity[\"attrib\"].get(\"comment\")\n            for version_entity in version_entities\n        })\n\n        project_entity = self._controller.get_project_entity(project_name)\n\n        prepared_data = ListPublishedWorkfilesOptionalData(\n            project_entity=project_entity,\n            anatomy=anatomy,\n            project_settings=self._controller.project_settings,\n            product_entities=product_entities,\n            version_entities=version_entities,\n            repre_entities=repre_entities,\n        )\n        cache.update_data(self._host.list_published_workfiles(\n            project_name,\n            folder_id,\n            prepared_data=prepared_data,\n        ))\n\n    items = cache.get_data()\n\n    if task_id:\n        items = [\n            item\n            for item in items\n            if item.task_id == task_id\n        ]\n    return items\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/models/workfiles.html#client.ayon_core.tools.workfiles.models.workfiles.WorkfilesModel.get_published_workfile_info","title":"<code>get_published_workfile_info(folder_id, representation_id)</code>","text":"<p>Get published workfile info by representation ID.</p> <p>Parameters:</p> Name Type Description Default <code>folder_id</code> <code>Optional[str]</code> <p>Folder id.</p> required <code>representation_id</code> <code>Optional[str]</code> <p>Representation id.</p> required <p>Returns:</p> Name Type Description <code>PublishedWorkfileWrap</code> <code>PublishedWorkfileWrap</code> <p>Published workfile info or None if not found.</p> Source code in <code>client/ayon_core/tools/workfiles/models/workfiles.py</code> <pre><code>def get_published_workfile_info(\n    self,\n    folder_id: Optional[str],\n    representation_id: Optional[str],\n) -&gt; PublishedWorkfileWrap:\n    \"\"\"Get published workfile info by representation ID.\n\n    Args:\n        folder_id (Optional[str]): Folder id.\n        representation_id (Optional[str]): Representation id.\n\n    Returns:\n        PublishedWorkfileWrap: Published workfile info or None\n            if not found.\n\n    \"\"\"\n    if not representation_id:\n        return PublishedWorkfileWrap()\n\n    # Search through all cached published workfile items\n    for item in self.get_published_file_items(folder_id, None):\n        if item.representation_id == representation_id:\n            comment = self._get_published_workfile_version_comment(\n                representation_id\n            )\n            return PublishedWorkfileWrap(item, comment)\n    return PublishedWorkfileWrap()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/models/workfiles.html#client.ayon_core.tools.workfiles.models.workfiles.WorkfilesModel.get_workarea_dir_by_context","title":"<code>get_workarea_dir_by_context(folder_id, task_id)</code>","text":"<p>Workarea dir for passed context.</p> <p>The directory path is based on project anatomy templates.</p> <p>Parameters:</p> Name Type Description Default <code>folder_id</code> <code>str</code> <p>Folder id.</p> required <code>task_id</code> <code>str</code> <p>Task id.</p> required <p>Returns:</p> Type Description <code>Optional[str]</code> <p>Optional[str]: Workarea dir path or None for invalid context.</p> Source code in <code>client/ayon_core/tools/workfiles/models/workfiles.py</code> <pre><code>def get_workarea_dir_by_context(\n    self, folder_id: str, task_id: str\n) -&gt; Optional[str]:\n    \"\"\"Workarea dir for passed context.\n\n    The directory path is based on project anatomy templates.\n\n    Args:\n        folder_id (str): Folder id.\n        task_id (str): Task id.\n\n    Returns:\n        Optional[str]: Workarea dir path or None for invalid context.\n\n    \"\"\"\n    if not folder_id or not task_id:\n        return None\n    folder_mapping = self._workdir_by_context.setdefault(folder_id, {})\n    workdir = folder_mapping.get(task_id)\n    if workdir is not None:\n        return workdir\n\n    workdir_data = self._prepare_fill_data(folder_id, task_id)\n\n    workdir = get_workdir_with_workdir_data(\n        workdir_data,\n        self._project_name,\n        anatomy=self._controller.project_anatomy,\n    )\n    folder_mapping[task_id] = workdir\n    return workdir\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/models/workfiles.html#client.ayon_core.tools.workfiles.models.workfiles.WorkfilesModel.get_workarea_file_items","title":"<code>get_workarea_file_items(folder_id, task_id)</code>","text":"<p>Workfile items for passed context from workarea.</p> <p>Parameters:</p> Name Type Description Default <code>folder_id</code> <code>Optional[str]</code> <p>Folder id.</p> required <code>task_id</code> <code>Optional[str]</code> <p>Task id.</p> required <p>Returns:</p> Type Description <p>list[WorkfileInfo]: List of file items matching workarea of passed context.</p> Source code in <code>client/ayon_core/tools/workfiles/models/workfiles.py</code> <pre><code>def get_workarea_file_items(self, folder_id, task_id):\n    \"\"\"Workfile items for passed context from workarea.\n\n    Args:\n        folder_id (Optional[str]): Folder id.\n        task_id (Optional[str]): Task id.\n\n    Returns:\n        list[WorkfileInfo]: List of file items matching workarea of passed\n            context.\n\n    \"\"\"\n    return self._cache_file_items(folder_id, task_id)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/widgets/index.html","title":"widgets","text":""},{"location":"autoapi/client/ayon_core/tools/workfiles/widgets/index.html#client.ayon_core.tools.workfiles.widgets.WorkfilesToolWindow","title":"<code>WorkfilesToolWindow</code>","text":"<p>               Bases: <code>QWidget</code></p> <p>WorkFiles Window.</p> <p>Main windows of workfiles tool.</p> <p>Parameters:</p> Name Type Description Default <code>controller</code> <code>AbstractWorkfilesFrontend</code> <p>Frontend controller.</p> <code>None</code> <code>parent</code> <code>Optional[QWidget]</code> <p>Parent widget.</p> <code>None</code> Source code in <code>client/ayon_core/tools/workfiles/widgets/window.py</code> <pre><code>class WorkfilesToolWindow(QtWidgets.QWidget):\n    \"\"\"WorkFiles Window.\n\n    Main windows of workfiles tool.\n\n    Args:\n        controller (AbstractWorkfilesFrontend): Frontend controller.\n        parent (Optional[QtWidgets.QWidget]): Parent widget.\n    \"\"\"\n    def __init__(self, controller=None, parent=None):\n        if controller is None:\n            controller = BaseWorkfileController()\n\n        title = \"AYON Workfiles\"\n        subtitle = controller.get_window_subtitle()\n        if subtitle:\n            title += f\" - {subtitle}\"\n\n        super().__init__(parent=parent)\n        self.setWindowTitle(title)\n        icon = QtGui.QIcon(resources.get_ayon_icon_filepath())\n        self.setWindowIcon(icon)\n        flags = self.windowFlags() | QtCore.Qt.Window\n        self.setWindowFlags(flags)\n\n        self._default_window_flags = flags\n\n        self._folders_widget = None\n\n        self._files_widget = None\n\n        self._first_show = True\n        self._controller_refreshed = False\n        self._context_to_set = None\n        # Host validation should happen only once\n        self._host_is_valid = None\n\n        self._controller = controller\n\n        # Create pages widget and set it as central widget\n        pages_widget = QtWidgets.QStackedWidget(self)\n\n        home_page_widget = QtWidgets.QWidget(pages_widget)\n        home_body_widget = QtWidgets.QWidget(home_page_widget)\n\n        col_1_widget = self._create_col_1_widget(controller, parent)\n        tasks_widget = TasksWidget(\n            controller, home_body_widget, handle_expected_selection=True\n        )\n        col_3_widget = self._create_col_3_widget(controller, home_body_widget)\n        side_panel = SidePanelWidget(controller, home_body_widget)\n\n        pages_widget.addWidget(home_page_widget)\n\n        # Build home\n        home_page_layout = QtWidgets.QVBoxLayout(home_page_widget)\n        home_page_layout.addWidget(home_body_widget)\n\n        # Build home - body\n        body_layout = QtWidgets.QVBoxLayout(home_body_widget)\n        split_widget = QtWidgets.QSplitter(home_body_widget)\n        split_widget.addWidget(col_1_widget)\n        split_widget.addWidget(tasks_widget)\n        split_widget.addWidget(col_3_widget)\n        split_widget.addWidget(side_panel)\n        split_widget.setSizes([350, 175, 550, 190])\n\n        body_layout.addWidget(split_widget)\n\n        main_layout = QtWidgets.QHBoxLayout(self)\n        main_layout.addWidget(pages_widget, 1)\n        main_layout.setContentsMargins(0, 0, 0, 0)\n\n        overlay_messages_widget = MessageOverlayObject(self)\n        overlay_invalid_host = InvalidHostOverlay(self)\n        overlay_invalid_host.setVisible(False)\n\n        show_timer = QtCore.QTimer()\n        show_timer.setSingleShot(True)\n        show_timer.setInterval(50)\n\n        show_timer.timeout.connect(self._on_show)\n\n        controller.register_event_callback(\n            \"save_as.finished\",\n            self._on_save_as_finished,\n        )\n        controller.register_event_callback(\n            \"copy_representation.finished\",\n            self._on_copy_representation_finished,\n        )\n        controller.register_event_callback(\n            \"workfile_duplicate.finished\",\n            self._on_duplicate_finished\n        )\n        controller.register_event_callback(\n            \"open_workfile.finished\",\n            self._on_open_finished\n        )\n        controller.register_event_callback(\n            \"controller.reset.started\",\n            self._on_controller_refresh_started,\n        )\n        controller.register_event_callback(\n            \"controller.reset.finished\",\n            self._on_controller_refresh_finished,\n        )\n\n        self._overlay_messages_widget = overlay_messages_widget\n        self._overlay_invalid_host = overlay_invalid_host\n        self._home_page_widget = home_page_widget\n        self._pages_widget = pages_widget\n        self._home_body_widget = home_body_widget\n        self._split_widget = split_widget\n\n        self._project_name = self._controller.get_current_project_name()\n\n        self._tasks_widget = tasks_widget\n        self._side_panel = side_panel\n\n        self._show_timer = show_timer\n\n        self._post_init()\n\n    def _post_init(self):\n        self._on_published_checkbox_changed()\n\n        # Force focus on the open button by default, required for Houdini.\n        self._files_widget.setFocus()\n\n        self.resize(1260, 600)\n\n    def _create_col_1_widget(self, controller, parent):\n        col_widget = QtWidgets.QWidget(parent)\n        header_widget = QtWidgets.QWidget(col_widget)\n\n        filters_widget = FoldersFiltersWidget(header_widget)\n\n        go_to_current_btn = GoToCurrentButton(header_widget)\n        refresh_btn = RefreshButton(header_widget)\n\n        header_layout = QtWidgets.QHBoxLayout(header_widget)\n        header_layout.setContentsMargins(0, 0, 0, 0)\n        header_layout.addWidget(filters_widget, 1)\n        header_layout.addWidget(go_to_current_btn, 0)\n        header_layout.addWidget(refresh_btn, 0)\n\n        folder_widget = FoldersWidget(\n            controller, col_widget, handle_expected_selection=True\n        )\n\n        col_layout = QtWidgets.QVBoxLayout(col_widget)\n        col_layout.setContentsMargins(0, 0, 0, 0)\n        col_layout.addWidget(header_widget, 0)\n        col_layout.addWidget(folder_widget, 1)\n\n        filters_widget.text_changed.connect(self._on_folder_filter_change)\n        filters_widget.my_tasks_changed.connect(\n            self._on_my_tasks_checkbox_state_changed\n        )\n        go_to_current_btn.clicked.connect(self._on_go_to_current_clicked)\n        refresh_btn.clicked.connect(self._on_refresh_clicked)\n\n        self._folders_widget = folder_widget\n\n        self._filters_widget = filters_widget\n\n        return col_widget\n\n    def _create_col_3_widget(self, controller, parent):\n        col_widget = QtWidgets.QWidget(parent)\n\n        header_widget = QtWidgets.QWidget(col_widget)\n\n        files_filter_input = PlaceholderLineEdit(header_widget)\n        files_filter_input.setPlaceholderText(\"Filter files..\")\n\n        published_checkbox = QtWidgets.QCheckBox(\"Published\", header_widget)\n        published_checkbox.setToolTip(\"Show published workfiles\")\n\n        header_layout = QtWidgets.QHBoxLayout(header_widget)\n        header_layout.setContentsMargins(0, 0, 0, 0)\n        header_layout.addWidget(files_filter_input, 1)\n        header_layout.addWidget(published_checkbox, 0)\n\n        files_widget = FilesWidget(controller, col_widget)\n\n        col_layout = QtWidgets.QVBoxLayout(col_widget)\n        col_layout.setContentsMargins(0, 0, 0, 0)\n        col_layout.addWidget(header_widget, 0)\n        col_layout.addWidget(files_widget, 1)\n\n        files_filter_input.textChanged.connect(\n            self._on_file_text_filter_change)\n        published_checkbox.stateChanged.connect(\n            self._on_published_checkbox_changed\n        )\n\n        self._files_filter_input = files_filter_input\n        self._published_checkbox = published_checkbox\n\n        self._files_widget = files_widget\n\n        return col_widget\n\n    def set_window_on_top(self, on_top):\n        \"\"\"Set window on top of other windows.\n\n        Args:\n            on_top (bool): Show on top of other windows.\n        \"\"\"\n\n        flags = self._default_window_flags\n        if on_top:\n            flags |= QtCore.Qt.WindowStaysOnTopHint\n        if self.windowFlags() != flags:\n            self.setWindowFlags(flags)\n\n    def ensure_visible(self, use_context=True, save=True, on_top=False):\n        \"\"\"Ensure the window is visible.\n\n        This method expects arguments for compatibility with previous variant\n            of Workfiles tool.\n\n        Args:\n            use_context (Optional[bool]): DEPRECATED: This argument is\n                ignored.\n            save (Optional[bool]): Allow to save workfiles.\n            on_top (Optional[bool]): Show on top of other windows.\n        \"\"\"\n\n        save = True if save is None else save\n        on_top = False if on_top is None else on_top\n\n        is_visible = self.isVisible()\n        self._controller.set_save_enabled(save)\n        self.set_window_on_top(on_top)\n\n        self.show()\n        self.raise_()\n        self.activateWindow()\n        if is_visible:\n            self.refresh()\n\n    def refresh(self):\n        \"\"\"Trigger refresh of workfiles tool controller.\"\"\"\n\n        self._controller.reset()\n\n    def showEvent(self, event):\n        super(WorkfilesToolWindow, self).showEvent(event)\n        self._show_timer.start()\n        if self._first_show:\n            self._first_show = False\n            self.setStyleSheet(style.load_stylesheet())\n\n    def keyPressEvent(self, event):\n        \"\"\"Custom keyPressEvent.\n\n        Override keyPressEvent to do nothing so that Maya's panels won't\n        take focus when pressing \"SHIFT\" whilst mouse is over viewport or\n        outliner. This way users don't accidentally perform Maya commands\n        whilst trying to name an instance.\n        \"\"\"\n\n        pass\n\n    def _on_show(self):\n        self.refresh()\n\n    def _on_file_text_filter_change(self, text):\n        self._files_widget.set_text_filter(text)\n\n    def _on_published_checkbox_changed(self):\n        \"\"\"Publish mode changed.\n\n        Tell children widgets about it so they can handle the mode.\n        \"\"\"\n\n        published_mode = self._published_checkbox.isChecked()\n        self._files_widget.set_published_mode(published_mode)\n        self._side_panel.set_published_mode(published_mode)\n\n    def _on_folder_filter_change(self, text):\n        self._folders_widget.set_name_filter(text)\n\n    def _on_go_to_current_clicked(self):\n        self._controller.go_to_current_context()\n\n    def _on_refresh_clicked(self):\n        self.refresh()\n\n    def _on_controller_refresh_started(self):\n        self._controller_refreshed = True\n\n    def _on_controller_refresh_finished(self):\n        if self._host_is_valid is None:\n            self._host_is_valid = self._controller.is_host_valid()\n            self._overlay_invalid_host.setVisible(not self._host_is_valid)\n\n        if not self._host_is_valid:\n            return\n\n        self._project_name = self._controller.get_current_project_name()\n        self._folders_widget.set_project_name(self._project_name)\n        self._tasks_widget.refresh()\n\n        # Update my tasks\n        self._on_my_tasks_checkbox_state_changed(\n            self._filters_widget.is_my_tasks_checked()\n        )\n\n    def _on_save_as_finished(self, event):\n        if event[\"failed\"]:\n            self._overlay_messages_widget.add_message(\n                \"Failed to save workfile\",\n                \"error\",\n            )\n        else:\n            self._overlay_messages_widget.add_message(\n                \"Workfile saved\"\n            )\n\n    def _on_copy_representation_finished(self, event):\n        if event[\"failed\"]:\n            self._overlay_messages_widget.add_message(\n                \"Failed to copy published workfile\",\n                \"error\",\n            )\n        else:\n            self._overlay_messages_widget.add_message(\n                \"Publish workfile saved\"\n            )\n\n    def _on_duplicate_finished(self, event):\n        if event[\"failed\"]:\n            self._overlay_messages_widget.add_message(\n                \"Failed to duplicate workfile\",\n                \"error\",\n            )\n        else:\n            self._overlay_messages_widget.add_message(\n                \"Workfile duplicated\"\n            )\n\n    def _on_open_finished(self, event):\n        if event[\"failed\"]:\n            self._overlay_messages_widget.add_message(\n                \"Failed to open workfile\",\n                \"error\",\n            )\n        else:\n            self.close()\n\n    def _on_my_tasks_checkbox_state_changed(self, enabled: bool) -&gt; None:\n        folder_ids = None\n        task_ids = None\n        if enabled:\n            entity_ids = self._controller.get_my_tasks_entity_ids(\n                self._project_name\n            )\n            folder_ids = entity_ids[\"folder_ids\"]\n            task_ids = entity_ids[\"task_ids\"]\n        self._folders_widget.set_folder_ids_filter(folder_ids)\n        self._tasks_widget.set_task_ids_filter(task_ids)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/widgets/index.html#client.ayon_core.tools.workfiles.widgets.WorkfilesToolWindow.ensure_visible","title":"<code>ensure_visible(use_context=True, save=True, on_top=False)</code>","text":"<p>Ensure the window is visible.</p> <p>This method expects arguments for compatibility with previous variant     of Workfiles tool.</p> <p>Parameters:</p> Name Type Description Default <code>use_context</code> <code>Optional[bool]</code> <p>DEPRECATED: This argument is ignored.</p> <code>True</code> <code>save</code> <code>Optional[bool]</code> <p>Allow to save workfiles.</p> <code>True</code> <code>on_top</code> <code>Optional[bool]</code> <p>Show on top of other windows.</p> <code>False</code> Source code in <code>client/ayon_core/tools/workfiles/widgets/window.py</code> <pre><code>def ensure_visible(self, use_context=True, save=True, on_top=False):\n    \"\"\"Ensure the window is visible.\n\n    This method expects arguments for compatibility with previous variant\n        of Workfiles tool.\n\n    Args:\n        use_context (Optional[bool]): DEPRECATED: This argument is\n            ignored.\n        save (Optional[bool]): Allow to save workfiles.\n        on_top (Optional[bool]): Show on top of other windows.\n    \"\"\"\n\n    save = True if save is None else save\n    on_top = False if on_top is None else on_top\n\n    is_visible = self.isVisible()\n    self._controller.set_save_enabled(save)\n    self.set_window_on_top(on_top)\n\n    self.show()\n    self.raise_()\n    self.activateWindow()\n    if is_visible:\n        self.refresh()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/widgets/index.html#client.ayon_core.tools.workfiles.widgets.WorkfilesToolWindow.keyPressEvent","title":"<code>keyPressEvent(event)</code>","text":"<p>Custom keyPressEvent.</p> <p>Override keyPressEvent to do nothing so that Maya's panels won't take focus when pressing \"SHIFT\" whilst mouse is over viewport or outliner. This way users don't accidentally perform Maya commands whilst trying to name an instance.</p> Source code in <code>client/ayon_core/tools/workfiles/widgets/window.py</code> <pre><code>def keyPressEvent(self, event):\n    \"\"\"Custom keyPressEvent.\n\n    Override keyPressEvent to do nothing so that Maya's panels won't\n    take focus when pressing \"SHIFT\" whilst mouse is over viewport or\n    outliner. This way users don't accidentally perform Maya commands\n    whilst trying to name an instance.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/widgets/index.html#client.ayon_core.tools.workfiles.widgets.WorkfilesToolWindow.refresh","title":"<code>refresh()</code>","text":"<p>Trigger refresh of workfiles tool controller.</p> Source code in <code>client/ayon_core/tools/workfiles/widgets/window.py</code> <pre><code>def refresh(self):\n    \"\"\"Trigger refresh of workfiles tool controller.\"\"\"\n\n    self._controller.reset()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/widgets/index.html#client.ayon_core.tools.workfiles.widgets.WorkfilesToolWindow.set_window_on_top","title":"<code>set_window_on_top(on_top)</code>","text":"<p>Set window on top of other windows.</p> <p>Parameters:</p> Name Type Description Default <code>on_top</code> <code>bool</code> <p>Show on top of other windows.</p> required Source code in <code>client/ayon_core/tools/workfiles/widgets/window.py</code> <pre><code>def set_window_on_top(self, on_top):\n    \"\"\"Set window on top of other windows.\n\n    Args:\n        on_top (bool): Show on top of other windows.\n    \"\"\"\n\n    flags = self._default_window_flags\n    if on_top:\n        flags |= QtCore.Qt.WindowStaysOnTopHint\n    if self.windowFlags() != flags:\n        self.setWindowFlags(flags)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/widgets/constants.html","title":"constants","text":""},{"location":"autoapi/client/ayon_core/tools/workfiles/widgets/files_widget.html","title":"files_widget","text":""},{"location":"autoapi/client/ayon_core/tools/workfiles/widgets/files_widget.html#client.ayon_core.tools.workfiles.widgets.files_widget.FilesWidget","title":"<code>FilesWidget</code>","text":"<p>               Bases: <code>QWidget</code></p> <p>A widget displaying files that allows to save and open files.</p> <p>Parameters:</p> Name Type Description Default <code>controller</code> <code>AbstractWorkfilesFrontend</code> <p>The control object.</p> required <code>parent</code> <code>QWidget</code> <p>The parent widget.</p> required Source code in <code>client/ayon_core/tools/workfiles/widgets/files_widget.py</code> <pre><code>class FilesWidget(QtWidgets.QWidget):\n    \"\"\"A widget displaying files that allows to save and open files.\n\n    Args:\n        controller (AbstractWorkfilesFrontend): The control object.\n        parent (QtWidgets.QWidget): The parent widget.\n    \"\"\"\n\n    def __init__(self, controller, parent):\n        super(FilesWidget, self).__init__(parent)\n\n        files_widget = QtWidgets.QStackedWidget(self)\n        workarea_widget = WorkAreaFilesWidget(controller, files_widget)\n        published_widget = PublishedFilesWidget(controller, files_widget)\n        files_widget.addWidget(workarea_widget)\n        files_widget.addWidget(published_widget)\n\n        btns_widget = QtWidgets.QWidget(self)\n\n        workarea_btns_widget = QtWidgets.QWidget(btns_widget)\n        workarea_btn_open = QtWidgets.QPushButton(\n            \"Open\", workarea_btns_widget)\n        workarea_btn_browse = QtWidgets.QPushButton(\n            \"Browse\", workarea_btns_widget)\n        workarea_btn_save = QtWidgets.QPushButton(\n            \"Save As\", workarea_btns_widget)\n\n        workarea_btns_layout = QtWidgets.QHBoxLayout(workarea_btns_widget)\n        workarea_btns_layout.setContentsMargins(0, 0, 0, 0)\n        workarea_btns_layout.addWidget(workarea_btn_open, 1)\n        workarea_btns_layout.addWidget(workarea_btn_browse, 1)\n        workarea_btns_layout.addWidget(workarea_btn_save, 1)\n\n        published_btns_widget = QtWidgets.QWidget(btns_widget)\n        published_btn_copy_n_open = QtWidgets.QPushButton(\n            \"Copy &amp;&amp; Open\", published_btns_widget\n        )\n        published_btn_change_context = QtWidgets.QPushButton(\n            \"Choose different context\", published_btns_widget\n        )\n        published_btn_cancel = QtWidgets.QPushButton(\n            \"Cancel\", published_btns_widget\n        )\n\n        published_btns_layout = QtWidgets.QHBoxLayout(published_btns_widget)\n        published_btns_layout.setContentsMargins(0, 0, 0, 0)\n        published_btns_layout.addWidget(published_btn_copy_n_open, 1)\n        published_btns_layout.addWidget(published_btn_change_context, 1)\n        published_btns_layout.addWidget(published_btn_cancel, 1)\n\n        btns_layout = QtWidgets.QVBoxLayout(btns_widget)\n        btns_layout.setContentsMargins(0, 0, 0, 0)\n        btns_layout.addWidget(workarea_btns_widget, 1)\n        btns_layout.addWidget(published_btns_widget, 1)\n\n        main_layout = QtWidgets.QVBoxLayout(self)\n        main_layout.setContentsMargins(0, 0, 0, 0)\n        main_layout.addWidget(files_widget, 1)\n        main_layout.addWidget(btns_widget, 0)\n\n        controller.register_event_callback(\n            \"selection.workarea.changed\",\n            self._on_workarea_path_changed\n        )\n        controller.register_event_callback(\n            \"selection.representation.changed\",\n            self._on_published_repre_changed\n        )\n        controller.register_event_callback(\n            \"selection.task.changed\",\n            self._on_task_changed\n        )\n        controller.register_event_callback(\n            \"copy_representation.finished\",\n            self._on_copy_representation_finished,\n        )\n        controller.register_event_callback(\n            \"workfile_save_enable.changed\",\n            self._on_workfile_save_enabled_change,\n        )\n\n        workarea_widget.open_current_requested.connect(\n            self._on_current_open_requests)\n        workarea_widget.duplicate_requested.connect(\n            self._on_duplicate_request)\n        workarea_btn_open.clicked.connect(self._on_workarea_open_clicked)\n        workarea_btn_browse.clicked.connect(self._on_workarea_browse_clicked)\n        workarea_btn_save.clicked.connect(self._on_workarea_save_clicked)\n\n        published_widget.save_as_requested.connect(self._on_save_as_request)\n        published_btn_copy_n_open.clicked.connect(\n            self._on_published_save_clicked)\n        published_btn_change_context.clicked.connect(\n            self._on_published_change_context_clicked)\n        published_btn_cancel.clicked.connect(\n            self._on_published_cancel_clicked)\n\n        self._selected_folder_id = None\n        self._selected_task_id = None\n        self._selected_task_name = None\n\n        self._pre_select_folder_id = None\n        self._pre_select_task_name = None\n\n        self._select_context_mode = False\n        self._valid_selected_context = False\n        self._valid_representation_id = False\n        self._tmp_text_filter = None\n        self._is_save_enabled = True\n\n        self._controller = controller\n        self._files_widget = files_widget\n        self._workarea_widget = workarea_widget\n        self._published_widget = published_widget\n        self._workarea_btns_widget = workarea_btns_widget\n        self._published_btns_widget = published_btns_widget\n\n        self._workarea_btn_open = workarea_btn_open\n        self._workarea_btn_browse = workarea_btn_browse\n        self._workarea_btn_save = workarea_btn_save\n\n        self._published_widget = published_widget\n        self._published_btn_copy_n_open = published_btn_copy_n_open\n        self._published_btn_change_context = published_btn_change_context\n        self._published_btn_cancel = published_btn_cancel\n\n        # Initial setup\n        workarea_btn_open.setEnabled(False)\n        workarea_btn_browse.setEnabled(False)\n        workarea_btn_save.setEnabled(False)\n        published_btn_copy_n_open.setEnabled(False)\n        published_btn_change_context.setEnabled(False)\n        published_btn_cancel.setVisible(False)\n\n    def set_published_mode(self, published_mode):\n        # Make sure context selection is disabled\n        self._set_select_contex_mode(False)\n        # Change current widget\n        self._files_widget.setCurrentWidget((\n            self._published_widget\n            if published_mode\n            else self._workarea_widget\n        ))\n        # Pass the mode to the widgets, so they can start/stop handle events\n        self._workarea_widget.set_published_mode(published_mode)\n        self._published_widget.set_published_mode(published_mode)\n\n        # Change available buttons\n        self._workarea_btns_widget.setVisible(not published_mode)\n        self._published_btns_widget.setVisible(published_mode)\n\n    def set_text_filter(self, text_filter):\n        if self._select_context_mode:\n            self._tmp_text_filter = text_filter\n            return\n        self._workarea_widget.set_text_filter(text_filter)\n        self._published_widget.set_text_filter(text_filter)\n\n    def _exec_save_as_dialog(self):\n        \"\"\"Show SaveAs dialog using currently selected context.\n\n        Returns:\n            Union[dict[str, Any], None]: Result of the dialog.\n        \"\"\"\n\n        dialog = SaveAsDialog(self._controller, self)\n        dialog.update_context()\n        dialog.exec_()\n        return dialog.get_result()\n\n    # -------------------------------------------------------------\n    # Workarea workfiles\n    # -------------------------------------------------------------\n    def _open_workfile(self, folder_id, task_name, filepath):\n        if self._controller.has_unsaved_changes():\n            result = self._save_changes_prompt()\n            if result is None:\n                return\n\n            if result:\n                self._controller.save_current_workfile()\n        self._controller.open_workfile(folder_id, task_name, filepath)\n\n    def _on_workarea_open_clicked(self):\n        path = self._workarea_widget.get_selected_path()\n        if not path:\n            return\n        folder_id = self._selected_folder_id\n        task_id = self._selected_task_id\n        self._open_workfile(folder_id, task_id, path)\n\n    def _on_current_open_requests(self):\n        # TODO validate if item under mouse is enabled\n        # - this uses selected item, but that does not have to be the one\n        #   under mouse\n        self._on_workarea_open_clicked()\n\n    def _on_duplicate_request(self):\n        filepath = self._workarea_widget.get_selected_path()\n        if filepath is None:\n            return\n\n        result = self._exec_save_as_dialog()\n        if result is None:\n            return\n        folder_id = self._selected_folder_id\n        task_id = self._selected_task_id\n        self._controller.duplicate_workfile(\n            folder_id,\n            task_id,\n            filepath,\n            result[\"rootless_workdir\"],\n            result[\"workdir\"],\n            result[\"filename\"],\n            version=result[\"version\"],\n            comment=result[\"comment\"],\n            description=result[\"description\"]\n        )\n\n    def _on_workarea_browse_clicked(self):\n        extnsions = self._controller.get_workfile_extensions()\n        ext_filter = \"Work File (*{0})\".format(\n            \" *\".join(extnsions)\n        )\n        dir_key = \"directory\"\n        if qtpy.API in (\"pyside\", \"pyside2\", \"pyside6\"):\n            dir_key = \"dir\"\n\n        selected_context = self._controller.get_selected_context()\n        workfile_root = self._controller.get_workarea_dir_by_context(\n            selected_context[\"folder_id\"], selected_context[\"task_id\"]\n        )\n        # Find existing directory of workfile root\n        #   - Qt will use 'cwd' instead, if path does not exist, which may lead\n        #       to igniter directory\n        while workfile_root:\n            if os.path.exists(workfile_root):\n                break\n            workfile_root = os.path.dirname(workfile_root)\n\n        kwargs = {\n            \"caption\": \"Work Files\",\n            \"filter\": ext_filter,\n            dir_key: workfile_root\n        }\n\n        filepath = QtWidgets.QFileDialog.getOpenFileName(**kwargs)[0]\n        if not filepath:\n            return\n\n        folder_id = self._selected_folder_id\n        task_id = self._selected_task_id\n        self._open_workfile(folder_id, task_id, filepath)\n\n    def _on_workarea_save_clicked(self):\n        result = self._exec_save_as_dialog()\n        if result is None:\n            return\n        self._controller.save_as_workfile(\n            result[\"folder_id\"],\n            result[\"task_id\"],\n            result[\"rootless_workdir\"],\n            result[\"workdir\"],\n            result[\"filename\"],\n            version=result[\"version\"],\n            comment=result[\"comment\"],\n            description=result[\"description\"]\n        )\n\n    def _on_workarea_path_changed(self, event):\n        valid_path = event[\"path\"] is not None\n        self._workarea_btn_open.setEnabled(valid_path)\n\n    # -------------------------------------------------------------\n    # Published workfiles\n    # -------------------------------------------------------------\n    def _update_published_btns_state(self):\n        enabled = (\n            self._valid_representation_id\n            and self._is_save_enabled\n        )\n        self._published_btn_copy_n_open.setEnabled(\n            enabled and self._valid_selected_context\n        )\n        self._published_btn_change_context.setEnabled(enabled)\n\n    def _update_workarea_btns_state(self):\n        enabled = self._is_save_enabled and self._valid_selected_context\n        self._workarea_btn_save.setEnabled(enabled)\n        self._workarea_btn_browse.setEnabled(self._valid_selected_context)\n\n    def _on_published_repre_changed(self, event):\n        self._valid_representation_id = event[\"representation_id\"] is not None\n        self._update_published_btns_state()\n\n    def _on_task_changed(self, event):\n        self._selected_folder_id = event[\"folder_id\"]\n        self._selected_task_id = event[\"task_id\"]\n        self._selected_task_name = event[\"task_name\"]\n        self._valid_selected_context = (\n            self._selected_folder_id is not None\n            and self._selected_task_id is not None\n        )\n        self._update_published_btns_state()\n        self._update_workarea_btns_state()\n\n    def _on_published_save_clicked(self):\n        result = self._exec_save_as_dialog()\n        if result is None:\n            return\n\n        repre_info = self._published_widget.get_selected_repre_info()\n        self._controller.copy_workfile_representation(\n            repre_info[\"representation_id\"],\n            repre_info[\"filepath\"],\n            result[\"folder_id\"],\n            result[\"task_id\"],\n            result[\"workdir\"],\n            result[\"filename\"],\n            result[\"rootless_workdir\"],\n            version=result[\"version\"],\n            comment=result[\"comment\"],\n            description=result[\"description\"],\n        )\n\n    def _on_save_as_request(self):\n        # Make sure the save is enabled\n        if self._is_save_enabled and self._valid_selected_context:\n            self._on_published_save_clicked()\n\n    def _set_select_contex_mode(self, enabled):\n        if self._select_context_mode is enabled:\n            return\n\n        if enabled:\n            self._pre_select_folder_id = self._selected_folder_id\n            self._pre_select_task_name = self._selected_task_name\n        else:\n            self._pre_select_folder_id = None\n            self._pre_select_task_name = None\n        self._select_context_mode = enabled\n        self._published_btn_cancel.setVisible(enabled)\n        self._published_btn_change_context.setVisible(not enabled)\n        self._published_widget.set_select_context_mode(enabled)\n\n        if not enabled and self._tmp_text_filter is not None:\n            self.set_text_filter(self._tmp_text_filter)\n            self._tmp_text_filter = None\n\n    def _on_published_change_context_clicked(self):\n        self._set_select_contex_mode(True)\n\n    def _should_set_pre_select_context(self):\n        if self._pre_select_folder_id is None:\n            return False\n        if self._pre_select_folder_id != self._selected_folder_id:\n            return True\n        if self._pre_select_task_name is None:\n            return False\n        return self._pre_select_task_name != self._selected_task_name\n\n    def _on_published_cancel_clicked(self):\n        folder_id = self._pre_select_folder_id\n        task_name = self._pre_select_task_name\n        representation_id = self._published_widget.get_selected_repre_id()\n        should_change_selection = self._should_set_pre_select_context()\n        self._set_select_contex_mode(False)\n        if should_change_selection:\n            self._controller.set_expected_selection(\n                folder_id, task_name, representation_id=representation_id\n            )\n\n    def _on_copy_representation_finished(self, event):\n        \"\"\"Callback for when copy representation is finished.\n\n        Make sure that select context mode is disabled when representation\n        copy is finished.\n\n        Args:\n            event (Event): Event object.\n        \"\"\"\n\n        if not event[\"failed\"]:\n            self._set_select_contex_mode(False)\n\n    def _on_workfile_save_enabled_change(self, event):\n        enabled = event[\"enabled\"]\n        self._is_save_enabled = enabled\n        self._update_published_btns_state()\n        self._update_workarea_btns_state()\n\n    def _save_changes_prompt(self):\n        \"\"\"Ask user if wants to save changes to current file.\n\n        Returns:\n            Union[bool, None]: True if user wants to save changes, False if\n                user does not want to save changes, None if user cancels\n                operation.\n        \"\"\"\n        messagebox = QtWidgets.QMessageBox(parent=self)\n        messagebox.setWindowFlags(\n            messagebox.windowFlags() | QtCore.Qt.FramelessWindowHint\n        )\n        messagebox.setIcon(QtWidgets.QMessageBox.Warning)\n        messagebox.setWindowTitle(\"Unsaved Changes!\")\n        messagebox.setText(\n            \"There are unsaved changes to the current file.\"\n            \"\\nDo you want to save the changes?\"\n        )\n        messagebox.setStandardButtons(\n            QtWidgets.QMessageBox.Yes\n            | QtWidgets.QMessageBox.No\n            | QtWidgets.QMessageBox.Cancel\n        )\n\n        result = messagebox.exec_()\n        if result == QtWidgets.QMessageBox.Yes:\n            return True\n        if result == QtWidgets.QMessageBox.No:\n            return False\n        return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/widgets/files_widget_published.html","title":"files_widget_published","text":""},{"location":"autoapi/client/ayon_core/tools/workfiles/widgets/files_widget_published.html#client.ayon_core.tools.workfiles.widgets.files_widget_published.PublishedFilesModel","title":"<code>PublishedFilesModel</code>","text":"<p>               Bases: <code>QStandardItemModel</code></p> <p>A model for displaying files.</p> <p>Parameters:</p> Name Type Description Default <code>controller</code> <code>AbstractWorkfilesFrontend</code> <p>The control object.</p> required Source code in <code>client/ayon_core/tools/workfiles/widgets/files_widget_published.py</code> <pre><code>class PublishedFilesModel(QtGui.QStandardItemModel):\n    \"\"\"A model for displaying files.\n\n    Args:\n        controller (AbstractWorkfilesFrontend): The control object.\n    \"\"\"\n\n    columns = [\n        \"Name\",\n        \"Author\",\n        \"Date Modified\",\n    ]\n    date_modified_col = columns.index(\"Date Modified\")\n\n    def __init__(self, controller):\n        super(PublishedFilesModel, self).__init__()\n\n        self.setColumnCount(len(self.columns))\n        for idx, label in enumerate(self.columns):\n            self.setHeaderData(idx, QtCore.Qt.Horizontal, label)\n\n        controller.register_event_callback(\n            \"selection.task.changed\",\n            self._on_task_changed\n        )\n        controller.register_event_callback(\n            \"selection.folder.changed\",\n            self._on_folder_changed\n        )\n\n        self._file_icon = qtawesome.icon(\n            \"fa.file-o\",\n            color=get_default_entity_icon_color()\n        )\n        self._controller = controller\n        self._items_by_id = {}\n        self._missing_context_item = None\n        self._missing_context_used = False\n        self._empty_root_item = None\n        self._empty_item_used = False\n\n        self._published_mode = False\n        self._context_select_mode = False\n\n        self._last_folder_id = None\n        self._last_task_id = None\n\n        self._add_empty_item()\n\n    def set_published_mode(self, published_mode):\n        if self._published_mode == published_mode:\n            return\n        self._published_mode = published_mode\n        if published_mode:\n            self._fill_items()\n        elif self._context_select_mode:\n            self.set_select_context_mode(False)\n\n    def set_select_context_mode(self, select_mode):\n        if self._context_select_mode is select_mode:\n            return\n        self._context_select_mode = select_mode\n        if not select_mode and self._published_mode:\n            self._fill_items()\n\n    def get_index_by_representation_id(self, representation_id):\n        item = self._items_by_id.get(representation_id)\n        if item is None:\n            return QtCore.QModelIndex()\n        return self.indexFromItem(item)\n\n    def refresh(self):\n        if self._published_mode:\n            self._fill_items()\n\n    def _clear_items(self):\n        self._remove_missing_context_item()\n        self._remove_empty_item()\n        if self._items_by_id:\n            root = self.invisibleRootItem()\n            root.removeRows(0, root.rowCount())\n            self._items_by_id = {}\n\n    def _get_missing_context_item(self):\n        if self._missing_context_item is None:\n            message = \"Select folder\"\n            item = QtGui.QStandardItem(message)\n            icon = qtawesome.icon(\n                \"fa.times\",\n                color=get_disabled_entity_icon_color()\n            )\n            item.setData(icon, QtCore.Qt.DecorationRole)\n            item.setFlags(QtCore.Qt.NoItemFlags)\n            item.setColumnCount(self.columnCount())\n            self._missing_context_item = item\n        return self._missing_context_item\n\n    def _add_missing_context_item(self):\n        if self._missing_context_used:\n            return\n        self._clear_items()\n        root_item = self.invisibleRootItem()\n        root_item.appendRow(self._get_missing_context_item())\n        self._missing_context_used = True\n\n    def _remove_missing_context_item(self):\n        if not self._missing_context_used:\n            return\n        root_item = self.invisibleRootItem()\n        root_item.takeRow(self._missing_context_item.row())\n        self._missing_context_used = False\n\n    def _get_empty_root_item(self):\n        if self._empty_root_item is None:\n            message = \"Didn't find any published workfiles.\"\n            item = QtGui.QStandardItem(message)\n            icon = qtawesome.icon(\n                \"fa.times\",\n                color=get_disabled_entity_icon_color()\n            )\n            item.setData(icon, QtCore.Qt.DecorationRole)\n            item.setFlags(QtCore.Qt.NoItemFlags)\n            item.setColumnCount(self.columnCount())\n            self._empty_root_item = item\n        return self._empty_root_item\n\n    def _add_empty_item(self):\n        if self._empty_item_used:\n            return\n        self._clear_items()\n        root_item = self.invisibleRootItem()\n        root_item.appendRow(self._get_empty_root_item())\n        self._empty_item_used = True\n\n    def _remove_empty_item(self):\n        if not self._empty_item_used:\n            return\n        root_item = self.invisibleRootItem()\n        root_item.takeRow(self._empty_root_item.row())\n        self._empty_item_used = False\n\n    def _on_folder_changed(self, event):\n        self._last_folder_id = event[\"folder_id\"]\n        if self._context_select_mode:\n            return\n\n        if self._published_mode:\n            self._fill_items()\n\n    def _on_task_changed(self, event):\n        self._last_folder_id = event[\"folder_id\"]\n        self._last_task_id = event[\"task_id\"]\n        if self._context_select_mode:\n            return\n\n        if self._published_mode:\n            self._fill_items()\n\n    def _fill_items(self):\n        folder_id = self._last_folder_id\n        task_id = self._last_task_id\n        if not folder_id:\n            self._add_missing_context_item()\n            return\n\n        file_items = self._controller.get_published_file_items(\n            folder_id, task_id\n        )\n        root_item = self.invisibleRootItem()\n        if not file_items:\n            self._add_empty_item()\n            return\n        self._remove_empty_item()\n        self._remove_missing_context_item()\n\n        user_items_by_name = self._controller.get_user_items_by_name()\n\n        items_to_remove = set(self._items_by_id.keys())\n        new_items = []\n        for file_item in file_items:\n            repre_id = file_item.representation_id\n            if repre_id in self._items_by_id:\n                items_to_remove.discard(repre_id)\n                item = self._items_by_id[repre_id]\n            else:\n                item = QtGui.QStandardItem()\n                new_items.append(item)\n                item.setColumnCount(self.columnCount())\n                item.setData(self._file_icon, QtCore.Qt.DecorationRole)\n                item.setData(repre_id, REPRE_ID_ROLE)\n\n            if file_item.available:\n                flags = QtCore.Qt.ItemIsEnabled | QtCore.Qt.ItemIsSelectable\n            else:\n                flags = QtCore.Qt.NoItemFlags\n\n            author = file_item.author\n            user_item = user_items_by_name.get(author)\n            if user_item is not None and user_item.full_name:\n                author = user_item.full_name\n\n            filename = os.path.basename(file_item.filepath)\n\n            item.setFlags(flags)\n            item.setData(filename, QtCore.Qt.DisplayRole)\n            item.setData(file_item.filepath, FILEPATH_ROLE)\n            item.setData(author, AUTHOR_ROLE)\n            item.setData(file_item.file_modified, DATE_MODIFIED_ROLE)\n\n            self._items_by_id[repre_id] = item\n\n        if new_items:\n            root_item.appendRows(new_items)\n\n        for repre_id in items_to_remove:\n            item = self._items_by_id.pop(repre_id)\n            root_item.removeRow(item.row())\n\n        if root_item.rowCount() == 0:\n            self._add_empty_item()\n\n    def flags(self, index):\n        # Use flags of first column for all columns\n        if index.column() != 0:\n            index = self.index(index.row(), 0, index.parent())\n        return super().flags(index)\n\n    def data(self, index, role=None):\n        if role is None:\n            role = QtCore.Qt.DisplayRole\n\n        # Handle roles for first column\n        col = index.column()\n        if col == 0:\n            return super().data(index, role)\n\n        if role == QtCore.Qt.DecorationRole:\n            return None\n\n        if role in (QtCore.Qt.DisplayRole, QtCore.Qt.EditRole):\n            if col == 1:\n                role = AUTHOR_ROLE\n            elif col == 2:\n                role = DATE_MODIFIED_ROLE\n            else:\n                return None\n        index = self.index(index.row(), 0, index.parent())\n\n        return super().data(index, role)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/widgets/files_widget_published.html#client.ayon_core.tools.workfiles.widgets.files_widget_published.PublishedFilesWidget","title":"<code>PublishedFilesWidget</code>","text":"<p>               Bases: <code>QWidget</code></p> <p>Published workfiles widget.</p> <p>Parameters:</p> Name Type Description Default <code>controller</code> <code>AbstractWorkfilesFrontend</code> <p>The control object.</p> required <code>parent</code> <code>QWidget</code> <p>The parent widget.</p> required Source code in <code>client/ayon_core/tools/workfiles/widgets/files_widget_published.py</code> <pre><code>class PublishedFilesWidget(QtWidgets.QWidget):\n    \"\"\"Published workfiles widget.\n\n    Args:\n        controller (AbstractWorkfilesFrontend): The control object.\n        parent (QtWidgets.QWidget): The parent widget.\n    \"\"\"\n\n    selection_changed = QtCore.Signal()\n    save_as_requested = QtCore.Signal()\n\n    def __init__(self, controller, parent):\n        super(PublishedFilesWidget, self).__init__(parent)\n\n        view = TreeView(self)\n        view.setSortingEnabled(True)\n        view.setContextMenuPolicy(QtCore.Qt.CustomContextMenu)\n        # Smaller indentation\n        view.setIndentation(0)\n\n        model = PublishedFilesModel(controller)\n        proxy_model = QtCore.QSortFilterProxyModel()\n        proxy_model.setSourceModel(model)\n        proxy_model.setSortCaseSensitivity(QtCore.Qt.CaseInsensitive)\n        proxy_model.setDynamicSortFilter(True)\n\n        view.setModel(proxy_model)\n\n        time_delegate = PrettyTimeDelegate()\n        view.setItemDelegateForColumn(model.date_modified_col, time_delegate)\n\n        # Default to a wider first filename column it is what we mostly care\n        # about and the date modified is relatively small anyway.\n        view.setColumnWidth(0, 330)\n\n        select_overlay = SelectContextOverlay(view)\n        select_overlay.setVisible(False)\n\n        main_layout = QtWidgets.QVBoxLayout(self)\n        main_layout.setContentsMargins(0, 0, 0, 0)\n        main_layout.addWidget(view, 1)\n\n        selection_model = view.selectionModel()\n        selection_model.selectionChanged.connect(self._on_selection_change)\n        view.double_clicked.connect(self._on_mouse_double_click)\n\n        controller.register_event_callback(\n            \"expected_selection_changed\",\n            self._on_expected_selection_change\n        )\n\n        self._view = view\n        self._select_overlay = select_overlay\n        self._model = model\n        self._proxy_model = proxy_model\n        self._time_delegate = time_delegate\n        self._controller = controller\n\n    def set_published_mode(self, published_mode):\n        self._model.set_published_mode(published_mode)\n\n    def set_select_context_mode(self, select_mode):\n        self._model.set_select_context_mode(select_mode)\n        self._select_overlay.setVisible(select_mode)\n\n    def set_text_filter(self, text_filter):\n        self._proxy_model.setFilterFixedString(text_filter)\n\n    def get_selected_repre_info(self):\n        selection_model = self._view.selectionModel()\n        representation_id = None\n        filepath = None\n        for index in selection_model.selectedIndexes():\n            representation_id = index.data(REPRE_ID_ROLE)\n            filepath = index.data(FILEPATH_ROLE)\n\n        return {\n            \"representation_id\": representation_id,\n            \"filepath\": filepath,\n        }\n\n    def get_selected_repre_id(self):\n        return self.get_selected_repre_info()[\"representation_id\"]\n\n    def _on_selection_change(self):\n        repre_id = self.get_selected_repre_id()\n        self._controller.set_selected_representation_id(repre_id)\n\n    def _on_mouse_double_click(self, event):\n        if event.button() == QtCore.Qt.LeftButton:\n            self.save_as_requested.emit()\n\n    def _on_expected_selection_change(self, event):\n        repre_info = event[\"representation\"]\n        if not repre_info[\"current\"]:\n            return\n\n        self._model.refresh()\n\n        representation_id = repre_info[\"id\"]\n        selected_repre_id = self.get_selected_repre_id()\n        if (\n            representation_id is not None\n            and representation_id != selected_repre_id\n        ):\n            index = self._model.get_index_by_representation_id(\n                representation_id)\n            if index.isValid():\n                proxy_index = self._proxy_model.mapFromSource(index)\n                self._view.setCurrentIndex(proxy_index)\n\n        self._controller.expected_representation_selected(\n            event[\"folder\"][\"id\"], event[\"task\"][\"name\"], representation_id\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/widgets/files_widget_published.html#client.ayon_core.tools.workfiles.widgets.files_widget_published.SelectContextOverlay","title":"<code>SelectContextOverlay</code>","text":"<p>               Bases: <code>BaseOverlayFrame</code></p> <p>Overlay for files view when user should select context.</p> Todos <p>The look of this overlay should be improved, it is \"not nice\" now.</p> Source code in <code>client/ayon_core/tools/workfiles/widgets/files_widget_published.py</code> <pre><code>class SelectContextOverlay(BaseOverlayFrame):\n    \"\"\"Overlay for files view when user should select context.\n\n    Todos:\n        The look of this overlay should be improved, it is \"not nice\" now.\n    \"\"\"\n\n    def __init__(self, parent):\n        super(SelectContextOverlay, self).__init__(parent)\n\n        label_widget = QtWidgets.QLabel(\n            \"Please choose context on the left&lt;br/&gt;&amp;lt\",\n            self\n        )\n        label_widget.setAlignment(QtCore.Qt.AlignCenter)\n        label_widget.setObjectName(\"OverlayFrameLabel\")\n\n        layout = QtWidgets.QHBoxLayout(self)\n        layout.addWidget(label_widget, 1, QtCore.Qt.AlignCenter)\n\n        label_widget.setAttribute(QtCore.Qt.WA_TranslucentBackground)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/widgets/files_widget_workarea.html","title":"files_widget_workarea","text":""},{"location":"autoapi/client/ayon_core/tools/workfiles/widgets/files_widget_workarea.html#client.ayon_core.tools.workfiles.widgets.files_widget_workarea.WorkAreaFilesModel","title":"<code>WorkAreaFilesModel</code>","text":"<p>               Bases: <code>QStandardItemModel</code></p> <p>A model for workare workfiles.</p> <p>Parameters:</p> Name Type Description Default <code>controller</code> <code>AbstractWorkfilesFrontend</code> <p>The control object.</p> required Source code in <code>client/ayon_core/tools/workfiles/widgets/files_widget_workarea.py</code> <pre><code>class WorkAreaFilesModel(QtGui.QStandardItemModel):\n    \"\"\"A model for workare workfiles.\n\n    Args:\n        controller (AbstractWorkfilesFrontend): The control object.\n    \"\"\"\n\n    refreshed = QtCore.Signal()\n    columns = [\n        \"Name\",\n        \"Author\",\n        \"Date Modified\",\n    ]\n    date_modified_col = columns.index(\"Date Modified\")\n\n    def __init__(self, controller):\n        super(WorkAreaFilesModel, self).__init__()\n\n        self.setColumnCount(len(self.columns))\n\n        for idx, label in enumerate(self.columns):\n            self.setHeaderData(idx, QtCore.Qt.Horizontal, label)\n\n        controller.register_event_callback(\n            \"selection.folder.changed\",\n            self._on_folder_changed\n        )\n        controller.register_event_callback(\n            \"selection.task.changed\",\n            self._on_task_changed\n        )\n        controller.register_event_callback(\n            \"workfile_duplicate.finished\",\n            self._on_duplicate_finished\n        )\n        controller.register_event_callback(\n            \"save_as.finished\",\n            self._on_save_as_finished\n        )\n\n        self._file_icon = qtawesome.icon(\n            \"fa.file-o\",\n            color=get_default_entity_icon_color()\n        )\n        self._controller = controller\n        self._items_by_filename = {}\n        self._missing_context_item = None\n        self._missing_context_used = False\n        self._empty_root_item = None\n        self._empty_item_used = False\n        self._published_mode = False\n        self._selected_folder_id = None\n        self._selected_task_name = None\n\n        self._add_missing_context_item()\n\n    def get_index_by_filename(self, filename):\n        item = self._items_by_filename.get(filename)\n        if item is None:\n            return QtCore.QModelIndex()\n        return self.indexFromItem(item)\n\n    def refresh(self):\n        if not self._published_mode:\n            self._fill_items()\n\n    def _get_missing_context_item(self):\n        if self._missing_context_item is None:\n            message = \"Select folder and task\"\n            item = QtGui.QStandardItem(message)\n            icon = qtawesome.icon(\n                \"fa.times\",\n                color=get_disabled_entity_icon_color()\n            )\n            item.setData(icon, QtCore.Qt.DecorationRole)\n            item.setFlags(QtCore.Qt.NoItemFlags)\n            item.setColumnCount(self.columnCount())\n            self._missing_context_item = item\n        return self._missing_context_item\n\n    def _clear_items(self):\n        self._remove_missing_context_item()\n        self._remove_empty_item()\n        if self._items_by_filename:\n            root = self.invisibleRootItem()\n            root.removeRows(0, root.rowCount())\n            self._items_by_filename = {}\n\n    def _add_missing_context_item(self):\n        if self._missing_context_used:\n            return\n        self._clear_items()\n        root_item = self.invisibleRootItem()\n        root_item.appendRow(self._get_missing_context_item())\n        self._missing_context_used = True\n\n    def _remove_missing_context_item(self):\n        if not self._missing_context_used:\n            return\n        root_item = self.invisibleRootItem()\n        root_item.takeRow(self._missing_context_item.row())\n        self._missing_context_used = False\n\n    def _get_empty_root_item(self):\n        if self._empty_root_item is None:\n            message = \"Work Area is empty..\"\n            item = QtGui.QStandardItem(message)\n            icon = qtawesome.icon(\n                \"fa.exclamation-circle\",\n                color=get_disabled_entity_icon_color()\n            )\n            item.setData(icon, QtCore.Qt.DecorationRole)\n            item.setFlags(QtCore.Qt.NoItemFlags)\n            item.setColumnCount(self.columnCount())\n            self._empty_root_item = item\n        return self._empty_root_item\n\n    def _add_empty_item(self):\n        if self._empty_item_used:\n            return\n        self._clear_items()\n        root_item = self.invisibleRootItem()\n        root_item.appendRow(self._get_empty_root_item())\n        self._empty_item_used = True\n\n    def _remove_empty_item(self):\n        if not self._empty_item_used:\n            return\n        root_item = self.invisibleRootItem()\n        root_item.takeRow(self._empty_root_item.row())\n        self._empty_item_used = False\n\n    def _on_folder_changed(self, event):\n        self._selected_folder_id = event[\"folder_id\"]\n        if not self._published_mode:\n            self._fill_items()\n\n    def _on_task_changed(self, event):\n        self._selected_folder_id = event[\"folder_id\"]\n        self._selected_task_name = event[\"task_name\"]\n        if not self._published_mode:\n            self._fill_items()\n\n    def _on_duplicate_finished(self, event):\n        if event[\"failed\"]:\n            return\n\n        if not self._published_mode:\n            self._fill_items()\n\n    def _on_save_as_finished(self, event):\n        if event[\"failed\"]:\n            return\n\n        if not self._published_mode:\n            self._fill_items()\n\n    def _fill_items(self):\n        try:\n            self._fill_items_impl()\n        finally:\n            self.refreshed.emit()\n\n    def _fill_items_impl(self):\n        folder_id = self._selected_folder_id\n        task_name = self._selected_task_name\n        if not folder_id or not task_name:\n            self._add_missing_context_item()\n            return\n\n        file_items = self._controller.get_workarea_file_items(\n            folder_id, task_name\n        )\n        root_item = self.invisibleRootItem()\n        if not file_items:\n            self._add_empty_item()\n            return\n        self._remove_empty_item()\n        self._remove_missing_context_item()\n        user_items_by_name = self._controller.get_user_items_by_name()\n\n        items_to_remove = set(self._items_by_filename.keys())\n        new_items = []\n        for file_item in file_items:\n            filename = os.path.basename(file_item.filepath)\n            if filename in self._items_by_filename:\n                items_to_remove.discard(filename)\n                item = self._items_by_filename[filename]\n            else:\n                item = QtGui.QStandardItem()\n                new_items.append(item)\n                item.setColumnCount(self.columnCount())\n                item.setData(self._file_icon, QtCore.Qt.DecorationRole)\n                item.setData(filename, QtCore.Qt.DisplayRole)\n                item.setData(filename, FILENAME_ROLE)\n\n            flags = QtCore.Qt.ItemIsSelectable\n            if file_item.available:\n                flags |= QtCore.Qt.ItemIsEnabled\n            item.setFlags(flags)\n            updated_by = file_item.updated_by\n            user_item = user_items_by_name.get(updated_by)\n            if user_item is not None and user_item.full_name:\n                updated_by = user_item.full_name\n\n            item.setData(\n                file_item.workfile_entity_id, WORKFILE_ENTITY_ID_ROLE\n            )\n            item.setData(file_item.filepath, FILEPATH_ROLE)\n            item.setData(file_item.rootless_path, ROOTLESS_PATH_ROLE)\n            item.setData(file_item.file_modified, DATE_MODIFIED_ROLE)\n            item.setData(updated_by, AUTHOR_ROLE)\n\n            self._items_by_filename[filename] = item\n\n        if new_items:\n            root_item.appendRows(new_items)\n\n        for filename in items_to_remove:\n            item = self._items_by_filename.pop(filename)\n            root_item.removeRow(item.row())\n\n        if root_item.rowCount() == 0:\n            self._add_empty_item()\n\n    def flags(self, index):\n        # Use flags of first column for all columns\n        if index.column() != 0:\n            index = self.index(index.row(), 0, index.parent())\n        return super().flags(index)\n\n    def data(self, index, role=None):\n        if role is None:\n            role = QtCore.Qt.DisplayRole\n\n        # Handle roles for first column\n        col = index.column()\n        if col == 0:\n            return super().data(index, role)\n\n        if role == QtCore.Qt.DecorationRole:\n            return None\n\n        if role in (QtCore.Qt.DisplayRole, QtCore.Qt.EditRole):\n            if col == 1:\n                role = AUTHOR_ROLE\n            elif col == 2:\n                role = DATE_MODIFIED_ROLE\n            else:\n                return None\n        index = self.index(index.row(), 0, index.parent())\n\n        return super().data(index, role)\n\n    def set_published_mode(self, published_mode):\n        if self._published_mode == published_mode:\n            return\n        self._published_mode = published_mode\n        if not published_mode:\n            self._fill_items()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/widgets/files_widget_workarea.html#client.ayon_core.tools.workfiles.widgets.files_widget_workarea.WorkAreaFilesWidget","title":"<code>WorkAreaFilesWidget</code>","text":"<p>               Bases: <code>QWidget</code></p> <p>Workarea files widget.</p> <p>Parameters:</p> Name Type Description Default <code>controller</code> <code>AbstractWorkfilesFrontend</code> <p>The control object.</p> required <code>parent</code> <code>QWidget</code> <p>The parent widget.</p> required Source code in <code>client/ayon_core/tools/workfiles/widgets/files_widget_workarea.py</code> <pre><code>class WorkAreaFilesWidget(QtWidgets.QWidget):\n    \"\"\"Workarea files widget.\n\n    Args:\n        controller (AbstractWorkfilesFrontend): The control object.\n        parent (QtWidgets.QWidget): The parent widget.\n    \"\"\"\n\n    selection_changed = QtCore.Signal()\n    open_current_requested = QtCore.Signal()\n    duplicate_requested = QtCore.Signal()\n\n    def __init__(self, controller, parent):\n        super(WorkAreaFilesWidget, self).__init__(parent)\n\n        view = TreeView(self)\n        view.setSortingEnabled(True)\n        view.setContextMenuPolicy(QtCore.Qt.CustomContextMenu)\n        # Smaller indentation\n        view.setIndentation(0)\n\n        model = WorkAreaFilesModel(controller)\n        proxy_model = QtCore.QSortFilterProxyModel()\n        proxy_model.setSourceModel(model)\n        proxy_model.setSortCaseSensitivity(QtCore.Qt.CaseInsensitive)\n        proxy_model.setDynamicSortFilter(True)\n\n        view.setModel(proxy_model)\n\n        time_delegate = PrettyTimeDelegate()\n        view.setItemDelegateForColumn(model.date_modified_col, time_delegate)\n\n        # Default to a wider first filename column it is what we mostly care\n        # about and the date modified is relatively small anyway.\n        view.setColumnWidth(0, 330)\n\n        main_layout = QtWidgets.QVBoxLayout(self)\n        main_layout.setContentsMargins(0, 0, 0, 0)\n        main_layout.addWidget(view, 1)\n\n        selection_model = view.selectionModel()\n        selection_model.selectionChanged.connect(self._on_selection_change)\n        view.double_clicked.connect(self._on_mouse_double_click)\n        view.customContextMenuRequested.connect(self._on_context_menu)\n        model.refreshed.connect(self._on_model_refresh)\n\n        controller.register_event_callback(\n            \"expected_selection_changed\",\n            self._on_expected_selection_change\n        )\n\n        self._view = view\n        self._model = model\n        self._proxy_model = proxy_model\n        self._time_delegate = time_delegate\n        self._controller = controller\n\n        self._published_mode = False\n        self._change_selection_on_refresh = True\n\n    def set_published_mode(self, published_mode):\n        \"\"\"Set the published mode.\n\n        Widget should ignore most of events when in published mode is enabled.\n\n        Args:\n            published_mode (bool): The published mode.\n        \"\"\"\n\n        self._model.set_published_mode(published_mode)\n        self._published_mode = published_mode\n\n    def set_text_filter(self, text_filter):\n        \"\"\"Set the text filter.\n\n        Args:\n            text_filter (str): The text filter.\n        \"\"\"\n\n        self._proxy_model.setFilterFixedString(text_filter)\n\n    def _get_selected_info(self):\n        selection_model = self._view.selectionModel()\n        workfile_entity_id = filename = rootless_path = filepath = None\n        for index in selection_model.selectedIndexes():\n            filepath = index.data(FILEPATH_ROLE)\n            rootless_path = index.data(ROOTLESS_PATH_ROLE)\n            filename = index.data(FILENAME_ROLE)\n            workfile_entity_id = index.data(WORKFILE_ENTITY_ID_ROLE)\n\n        return {\n            \"filepath\": filepath,\n            \"rootless_path\": rootless_path,\n            \"filename\": filename,\n            \"workfile_entity_id\": workfile_entity_id,\n        }\n\n    def get_selected_path(self):\n        \"\"\"Selected filepath.\n\n        Returns:\n            Union[str, None]: The selected filepath or None if nothing is\n                selected.\n        \"\"\"\n        return self._get_selected_info()[\"filepath\"]\n\n    def _on_selection_change(self):\n        info = self._get_selected_info()\n        self._controller.set_selected_workfile_path(\n            info[\"rootless_path\"],\n            info[\"filepath\"],\n            info[\"workfile_entity_id\"],\n        )\n\n    def _on_mouse_double_click(self, event):\n        if event.button() == QtCore.Qt.LeftButton:\n            self.open_current_requested.emit()\n\n    def _on_context_menu(self, point):\n        index = self._view.indexAt(point)\n        if not index.isValid():\n            return\n\n        if not index.flags() &amp; QtCore.Qt.ItemIsEnabled:\n            return\n\n        menu = QtWidgets.QMenu(self)\n\n        # Duplicate\n        action = QtWidgets.QAction(\"Duplicate\", menu)\n        tip = \"Duplicate selected file.\"\n        action.setToolTip(tip)\n        action.setStatusTip(tip)\n        action.triggered.connect(self._on_duplicate_pressed)\n        menu.addAction(action)\n\n        # Show the context action menu\n        global_point = self._view.mapToGlobal(point)\n        _ = menu.exec_(global_point)\n\n    def _on_duplicate_pressed(self):\n        self.duplicate_requested.emit()\n\n    def _on_expected_selection_change(self, event):\n        workfile_info = event[\"workfile\"]\n        if not workfile_info[\"current\"]:\n            return\n\n        self._change_selection_on_refresh = False\n        self._model.refresh()\n        self._change_selection_on_refresh = True\n\n        workfile_name = workfile_info[\"name\"]\n        if (\n            workfile_name is not None\n            and workfile_name != self._get_selected_info()[\"filename\"]\n        ):\n            index = self._model.get_index_by_filename(workfile_name)\n            if index.isValid():\n                proxy_index = self._proxy_model.mapFromSource(index)\n                self._view.setCurrentIndex(proxy_index)\n\n        self._controller.expected_workfile_selected(\n            event[\"folder\"][\"id\"], event[\"task\"][\"name\"], workfile_name\n        )\n\n    def _on_model_refresh(self):\n        if not self._change_selection_on_refresh:\n            return\n\n        # Find the row with latest date modified\n        indexes = [\n            self._proxy_model.index(idx, 0)\n            for idx in range(self._proxy_model.rowCount())\n        ]\n        filtered_indexes = [\n            index\n            for index in indexes\n            if self._proxy_model.flags(index) &amp; QtCore.Qt.ItemIsEnabled\n        ]\n        if not filtered_indexes:\n            return\n\n        latest_index = max(\n            filtered_indexes,\n            key=lambda model_index: model_index.data(DATE_MODIFIED_ROLE) or 0\n        )\n\n        # Select row of latest modified\n        selection_model = self._view.selectionModel()\n        selection_model.select(\n            latest_index,\n            (\n                QtCore.QItemSelectionModel.ClearAndSelect\n                | QtCore.QItemSelectionModel.Current\n                | QtCore.QItemSelectionModel.Rows\n            )\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/widgets/files_widget_workarea.html#client.ayon_core.tools.workfiles.widgets.files_widget_workarea.WorkAreaFilesWidget.get_selected_path","title":"<code>get_selected_path()</code>","text":"<p>Selected filepath.</p> <p>Returns:</p> Type Description <p>Union[str, None]: The selected filepath or None if nothing is selected.</p> Source code in <code>client/ayon_core/tools/workfiles/widgets/files_widget_workarea.py</code> <pre><code>def get_selected_path(self):\n    \"\"\"Selected filepath.\n\n    Returns:\n        Union[str, None]: The selected filepath or None if nothing is\n            selected.\n    \"\"\"\n    return self._get_selected_info()[\"filepath\"]\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/widgets/files_widget_workarea.html#client.ayon_core.tools.workfiles.widgets.files_widget_workarea.WorkAreaFilesWidget.set_published_mode","title":"<code>set_published_mode(published_mode)</code>","text":"<p>Set the published mode.</p> <p>Widget should ignore most of events when in published mode is enabled.</p> <p>Parameters:</p> Name Type Description Default <code>published_mode</code> <code>bool</code> <p>The published mode.</p> required Source code in <code>client/ayon_core/tools/workfiles/widgets/files_widget_workarea.py</code> <pre><code>def set_published_mode(self, published_mode):\n    \"\"\"Set the published mode.\n\n    Widget should ignore most of events when in published mode is enabled.\n\n    Args:\n        published_mode (bool): The published mode.\n    \"\"\"\n\n    self._model.set_published_mode(published_mode)\n    self._published_mode = published_mode\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/widgets/files_widget_workarea.html#client.ayon_core.tools.workfiles.widgets.files_widget_workarea.WorkAreaFilesWidget.set_text_filter","title":"<code>set_text_filter(text_filter)</code>","text":"<p>Set the text filter.</p> <p>Parameters:</p> Name Type Description Default <code>text_filter</code> <code>str</code> <p>The text filter.</p> required Source code in <code>client/ayon_core/tools/workfiles/widgets/files_widget_workarea.py</code> <pre><code>def set_text_filter(self, text_filter):\n    \"\"\"Set the text filter.\n\n    Args:\n        text_filter (str): The text filter.\n    \"\"\"\n\n    self._proxy_model.setFilterFixedString(text_filter)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/widgets/save_as_dialog.html","title":"save_as_dialog","text":""},{"location":"autoapi/client/ayon_core/tools/workfiles/widgets/save_as_dialog.html#client.ayon_core.tools.workfiles.widgets.save_as_dialog.SaveAsDialog","title":"<code>SaveAsDialog</code>","text":"<p>               Bases: <code>QDialog</code></p> <p>Save as dialog to define a unique filename inside workdir.</p> <p>The filename is calculated in controller where UI sends values from dialog inputs.</p> <p>Parameters:</p> Name Type Description Default <code>controller</code> <code>AbstractWorkfilesFrontend</code> <p>The control object.</p> required Source code in <code>client/ayon_core/tools/workfiles/widgets/save_as_dialog.py</code> <pre><code>class SaveAsDialog(QtWidgets.QDialog):\n    \"\"\"Save as dialog to define a unique filename inside workdir.\n\n    The filename is calculated in controller where UI sends values from\n    dialog inputs.\n\n    Args:\n        controller (AbstractWorkfilesFrontend): The control object.\n    \"\"\"\n\n    def __init__(self, controller, parent):\n        super(SaveAsDialog, self).__init__(parent=parent)\n        self.setWindowFlags(self.windowFlags() | QtCore.Qt.FramelessWindowHint)\n\n        self._controller = controller\n\n        self._folder_id = None\n        self._task_id = None\n        self._last_version = None\n        self._template_key = None\n        self._comment_value = None\n        self._version_value = None\n        self._ext_value = None\n        self._filename = None\n        self._workdir = None\n        self._rootless_workdir = None\n\n        self._result = None\n\n        # Btns widget\n        btns_widget = QtWidgets.QWidget(self)\n\n        btn_ok = QtWidgets.QPushButton(\"Ok\", btns_widget)\n        btn_cancel = QtWidgets.QPushButton(\"Cancel\", btns_widget)\n\n        btns_layout = QtWidgets.QHBoxLayout(btns_widget)\n        btns_layout.addWidget(btn_ok)\n        btns_layout.addWidget(btn_cancel)\n\n        # Inputs widget\n        inputs_widget = QtWidgets.QWidget(self)\n\n        # Version widget\n        version_widget = QtWidgets.QWidget(inputs_widget)\n\n        # Version number input\n        version_input = QtWidgets.QSpinBox(version_widget)\n        version_input.setMinimum(1)\n        version_input.setMaximum(9999)\n\n        # Last version checkbox\n        last_version_check = QtWidgets.QCheckBox(\n            \"Next Available Version\", version_widget\n        )\n        last_version_check.setChecked(True)\n\n        version_layout = QtWidgets.QHBoxLayout(version_widget)\n        version_layout.setContentsMargins(0, 0, 0, 0)\n        version_layout.addWidget(version_input)\n        version_layout.addWidget(last_version_check)\n\n        # Artist note widget\n        description_input = PlaceholderPlainTextEdit(inputs_widget)\n        description_input.setPlaceholderText(\n            \"Provide a note about this workfile.\")\n\n        # Preview widget\n        preview_widget = QtWidgets.QLabel(\"Preview filename\", inputs_widget)\n        preview_widget.setWordWrap(True)\n\n        # Subversion input\n        subversion_input = SubversionLineEdit(inputs_widget)\n        subversion_input.set_placeholder(\"Will be part of filename.\")\n\n        # Extensions combobox\n        extension_combobox = QtWidgets.QComboBox(inputs_widget)\n        # Add styled delegate to use stylesheets\n        extension_delegate = QtWidgets.QStyledItemDelegate()\n        extension_combobox.setItemDelegate(extension_delegate)\n\n        version_label = QtWidgets.QLabel(\"Version:\", inputs_widget)\n        subversion_label = QtWidgets.QLabel(\"Subversion:\", inputs_widget)\n        extension_label = QtWidgets.QLabel(\"Extension:\", inputs_widget)\n        preview_label = QtWidgets.QLabel(\"Preview:\", inputs_widget)\n        description_label = QtWidgets.QLabel(\"Artist Note:\", inputs_widget)\n\n        # Build inputs\n        inputs_layout = QtWidgets.QGridLayout(inputs_widget)\n        inputs_layout.addWidget(version_label, 0, 0)\n        inputs_layout.addWidget(version_widget, 0, 1)\n        inputs_layout.addWidget(subversion_label, 1, 0)\n        inputs_layout.addWidget(subversion_input, 1, 1)\n        inputs_layout.addWidget(extension_label, 2, 0)\n        inputs_layout.addWidget(extension_combobox, 2, 1)\n        inputs_layout.addWidget(preview_label, 3, 0)\n        inputs_layout.addWidget(preview_widget, 3, 1)\n        inputs_layout.addWidget(description_label, 4, 0, 1, 2)\n        inputs_layout.addWidget(description_input, 5, 0, 1, 2)\n\n        # Build layout\n        main_layout = QtWidgets.QVBoxLayout(self)\n        main_layout.addWidget(inputs_widget)\n        main_layout.addWidget(btns_widget)\n\n        # Signal callback registration\n        version_input.valueChanged.connect(self._on_version_spinbox_change)\n        last_version_check.stateChanged.connect(\n            self._on_version_checkbox_change\n        )\n\n        subversion_input.text_changed.connect(self._on_comment_change)\n        extension_combobox.currentIndexChanged.connect(\n            self._on_extension_change)\n\n        btn_ok.pressed.connect(self._on_ok_pressed)\n        btn_cancel.pressed.connect(self._on_cancel_pressed)\n\n        # Store objects\n        self._inputs_layout = inputs_layout\n\n        self._btn_ok = btn_ok\n        self._btn_cancel = btn_cancel\n\n        self._version_widget = version_widget\n\n        self._version_input = version_input\n        self._last_version_check = last_version_check\n\n        self._extension_delegate = extension_delegate\n        self._extension_combobox = extension_combobox\n        self._subversion_input = subversion_input\n        self._preview_widget = preview_widget\n        self._description_input = description_input\n\n        self._version_label = version_label\n        self._subversion_label = subversion_label\n        self._extension_label = extension_label\n        self._preview_label = preview_label\n        self._description_label = description_label\n\n        # Post init setup\n\n        # Allow \"Enter\" key to accept the save.\n        btn_ok.setDefault(True)\n\n        # Disable version input if last version is checked\n        version_input.setEnabled(not last_version_check.isChecked())\n\n        # Force default focus to comment, some hosts didn't automatically\n        # apply focus to this line edit (e.g. Houdini)\n        subversion_input.setFocus()\n\n    def get_result(self):\n        return self._result\n\n    def update_context(self):\n        # Add version only if template contains version key\n        # - since the version can be padded with \"{version:0&gt;4}\" we only search\n        #   for \"{version\".\n        selected_context = self._controller.get_selected_context()\n        folder_id = selected_context[\"folder_id\"]\n        task_id = selected_context[\"task_id\"]\n        data = self._controller.get_workarea_save_as_data(folder_id, task_id)\n        last_version = data[\"last_version\"]\n        comment = data[\"comment\"]\n        comment_hints = data[\"comment_hints\"]\n\n        template_has_version = data[\"template_has_version\"]\n        template_has_comment = data[\"template_has_comment\"]\n\n        self._folder_id = folder_id\n        self._task_id = task_id\n        self._workdir = data[\"workdir\"]\n        self._rootless_workdir = data[\"rootless_workdir\"]\n        self._comment_value = data[\"comment\"]\n        self._ext_value = data[\"ext\"]\n        self._template_key = data[\"template_key\"]\n        self._last_version = data[\"last_version\"]\n\n        self._extension_combobox.clear()\n        self._extension_combobox.addItems(data[\"extensions\"])\n\n        self._version_input.setValue(last_version)\n\n        vw_idx = self._inputs_layout.indexOf(self._version_widget)\n        self._version_label.setVisible(template_has_version)\n        self._version_widget.setVisible(template_has_version)\n        if template_has_version:\n            if vw_idx == -1:\n                self._inputs_layout.addWidget(self._version_label, 0, 0)\n                self._inputs_layout.addWidget(self._version_widget, 0, 1)\n        elif vw_idx != -1:\n            self._inputs_layout.takeAt(vw_idx)\n            self._inputs_layout.takeAt(\n                self._inputs_layout.indexOf(self._version_label)\n            )\n\n        cw_idx = self._inputs_layout.indexOf(self._subversion_input)\n        self._subversion_label.setVisible(template_has_comment)\n        self._subversion_input.setVisible(template_has_comment)\n        if template_has_comment:\n            if cw_idx == -1:\n                self._inputs_layout.addWidget(self._subversion_label, 1, 0)\n                self._inputs_layout.addWidget(self._subversion_input, 1, 1)\n        elif cw_idx != -1:\n            self._inputs_layout.takeAt(cw_idx)\n            self._inputs_layout.takeAt(\n                self._inputs_layout.indexOf(self._subversion_label)\n            )\n\n        if template_has_comment:\n            self._subversion_input.set_text(comment or \"\")\n            self._subversion_input.set_values(comment_hints)\n        self._update_filename()\n\n    def _on_version_spinbox_change(self, value):\n        if value == self._version_value:\n            return\n        self._version_value = value\n        if not self._last_version_check.isChecked():\n            self._update_filename()\n\n    def _on_version_checkbox_change(self):\n        use_last_version = self._last_version_check.isChecked()\n        self._version_input.setEnabled(not use_last_version)\n        if use_last_version:\n            self._version_input.blockSignals(True)\n            self._version_input.setValue(self._last_version)\n            self._version_input.blockSignals(False)\n        self._update_filename()\n\n    def _on_comment_change(self, text):\n        if self._comment_value == text:\n            return\n        self._comment_value = text\n        self._update_filename()\n\n    def _on_extension_change(self):\n        ext = self._extension_combobox.currentText()\n        if ext == self._ext_value:\n            return\n        self._ext_value = ext\n        self._update_filename()\n\n    def _on_ok_pressed(self):\n        self._result = {\n            \"filename\": self._filename,\n            \"workdir\": self._workdir,\n            \"rootless_workdir\": self._rootless_workdir,\n            \"folder_id\": self._folder_id,\n            \"task_id\": self._task_id,\n            \"template_key\": self._template_key,\n            \"version\": self._version_value,\n            \"comment\": self._comment_value,\n            \"description\": self._description_input.toPlainText(),\n        }\n        self.close()\n\n    def _on_cancel_pressed(self):\n        self.close()\n\n    def _update_filename(self):\n        result = self._controller.fill_workarea_filepath(\n            self._folder_id,\n            self._task_id,\n            self._ext_value,\n            self._last_version_check.isChecked(),\n            self._version_value,\n            self._comment_value,\n        )\n        self._filename = result.filename\n        self._btn_ok.setEnabled(not result.exists)\n\n        if result.exists:\n            self._preview_widget.setText((\n                \"&lt;font color='red'&gt;Cannot create \\\"{}\\\" because file exists!\"\n                \"&lt;/font&gt;\"\n            ).format(result.filename))\n        else:\n            self._preview_widget.setText(\n                \"&lt;font color='green'&gt;{}&lt;/font&gt;\".format(result.filename)\n            )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/widgets/save_as_dialog.html#client.ayon_core.tools.workfiles.widgets.save_as_dialog.SubversionLineEdit","title":"<code>SubversionLineEdit</code>","text":"<p>               Bases: <code>QWidget</code></p> <p>QLineEdit with QPushButton for drop down selection of list of strings</p> Source code in <code>client/ayon_core/tools/workfiles/widgets/save_as_dialog.py</code> <pre><code>class SubversionLineEdit(QtWidgets.QWidget):\n    \"\"\"QLineEdit with QPushButton for drop down selection of list of strings\"\"\"\n\n    text_changed = QtCore.Signal(str)\n\n    def __init__(self, *args, **kwargs):\n        super(SubversionLineEdit, self).__init__(*args, **kwargs)\n\n        input_field = PlaceholderLineEdit(self)\n        menu_btn = QtWidgets.QPushButton(self)\n        menu_btn.setFixedWidth(18)\n\n        menu = QtWidgets.QMenu(self)\n        menu_btn.setMenu(menu)\n\n        layout = QtWidgets.QHBoxLayout(self)\n        layout.setContentsMargins(0, 0, 0, 0)\n        layout.setSpacing(3)\n\n        layout.addWidget(input_field, 1)\n        layout.addWidget(menu_btn, 0)\n\n        input_field.textChanged.connect(self.text_changed)\n\n        self.setFocusProxy(input_field)\n\n        self._input_field = input_field\n        self._menu_btn = menu_btn\n        self._menu = menu\n\n    def set_placeholder(self, placeholder):\n        self._input_field.setPlaceholderText(placeholder)\n\n    def set_text(self, text):\n        self._input_field.setText(text)\n\n    def set_values(self, values):\n        self._update(values)\n\n    def _on_button_clicked(self):\n        self._menu.exec_()\n\n    def _on_action_clicked(self, action):\n        self._input_field.setText(action.text())\n\n    def _update(self, values):\n        \"\"\"Create optional predefined product names\n\n        Args:\n            default_names(list): all predefined names\n\n        Returns:\n             None\n        \"\"\"\n\n        menu = self._menu\n        button = self._menu_btn\n\n        state = any(values)\n        button.setEnabled(state)\n        if state is False:\n            return\n\n        # Include an empty string\n        values = [\"\"] + sorted(values)\n\n        # Get and destroy the action group\n        group = button.findChild(QtWidgets.QActionGroup)\n        if group:\n            group.deleteLater()\n\n        # Build new action group\n        group = QtWidgets.QActionGroup(button)\n        for name in values:\n            action = group.addAction(name)\n            menu.addAction(action)\n\n        group.triggered.connect(self._on_action_clicked)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/widgets/side_panel.html","title":"side_panel","text":""},{"location":"autoapi/client/ayon_core/tools/workfiles/widgets/side_panel.html#client.ayon_core.tools.workfiles.widgets.side_panel.SidePanelWidget","title":"<code>SidePanelWidget</code>","text":"<p>               Bases: <code>QWidget</code></p> <p>Details about selected workfile.</p> Todos <p>At this moment only shows created and modified date of file     or its size.</p> <p>Parameters:</p> Name Type Description Default <code>controller</code> <code>AbstractWorkfilesFrontend</code> <p>The control object.</p> required <code>parent</code> <code>QWidget</code> <p>The parent widget.</p> required Source code in <code>client/ayon_core/tools/workfiles/widgets/side_panel.py</code> <pre><code>class SidePanelWidget(QtWidgets.QWidget):\n    \"\"\"Details about selected workfile.\n\n    Todos:\n        At this moment only shows created and modified date of file\n            or its size.\n\n    Args:\n        controller (AbstractWorkfilesFrontend): The control object.\n        parent (QtWidgets.QWidget): The parent widget.\n    \"\"\"\n\n    published_workfile_message = (\n        \"&lt;b&gt;INFO&lt;/b&gt;: Opened published workfiles will be stored in\"\n        \" temp directory on your machine. Current temp size: &lt;b&gt;{}&lt;/b&gt;.\"\n    )\n\n    def __init__(self, controller, parent):\n        super(SidePanelWidget, self).__init__(parent)\n\n        details_label = QtWidgets.QLabel(\"Details\", self)\n        details_input = QtWidgets.QPlainTextEdit(self)\n        details_input.setReadOnly(True)\n\n        description_widget = QtWidgets.QWidget(self)\n        description_label = QtWidgets.QLabel(\"Artist note\", description_widget)\n        description_input = QtWidgets.QPlainTextEdit(description_widget)\n        btn_description_save = QtWidgets.QPushButton(\n            \"Save note\", description_widget\n        )\n\n        description_layout = QtWidgets.QVBoxLayout(description_widget)\n        description_layout.setContentsMargins(0, 0, 0, 0)\n        description_layout.addWidget(description_label, 0)\n        description_layout.addWidget(description_input, 1)\n        description_layout.addWidget(\n            btn_description_save, 0, alignment=QtCore.Qt.AlignRight\n        )\n\n        main_layout = QtWidgets.QVBoxLayout(self)\n        main_layout.setContentsMargins(0, 0, 0, 0)\n        main_layout.addWidget(details_label, 0)\n        main_layout.addWidget(details_input, 1)\n        main_layout.addWidget(description_widget, 1)\n\n        description_input.textChanged.connect(self._on_description_change)\n        btn_description_save.clicked.connect(self._on_save_click)\n\n        controller.register_event_callback(\n            \"selection.workarea.changed\",\n            self._on_workarea_selection_change\n        )\n        controller.register_event_callback(\n            \"selection.representation.changed\",\n            self._on_representation_selection_change,\n        )\n\n        self._details_input = details_input\n        self._description_widget = description_widget\n        self._description_input = description_input\n        self._btn_description_save = btn_description_save\n\n        self._folder_id = None\n        self._task_id = None\n        self._filepath = None\n        self._rootless_path = None\n        self._representation_id = None\n        self._orig_description = \"\"\n        self._controller = controller\n\n        self._set_context(False, None, None)\n\n    def set_published_mode(self, published_mode: bool) -&gt; None:\n        \"\"\"Change published mode.\n\n        Args:\n            published_mode (bool): Published mode enabled.\n        \"\"\"\n\n        self._description_widget.setVisible(not published_mode)\n        # Clear the context when switching modes to avoid showing stale data\n        if published_mode:\n            self._set_publish_context(\n                self._folder_id,\n                self._task_id,\n                self._representation_id,\n            )\n        else:\n            self._set_workarea_context(\n                self._folder_id,\n                self._task_id,\n                self._rootless_path,\n                self._filepath,\n            )\n\n    def _on_workarea_selection_change(self, event):\n        folder_id = event[\"folder_id\"]\n        task_id = event[\"task_id\"]\n        filepath = event[\"path\"]\n        rootless_path = event[\"rootless_path\"]\n\n        self._set_workarea_context(\n            folder_id, task_id, rootless_path, filepath\n        )\n\n    def _on_representation_selection_change(self, event):\n        folder_id = event[\"folder_id\"]\n        task_id = event[\"task_id\"]\n        representation_id = event[\"representation_id\"]\n\n        self._set_publish_context(folder_id, task_id, representation_id)\n\n    def _on_description_change(self):\n        text = self._description_input.toPlainText()\n        self._btn_description_save.setEnabled(self._orig_description != text)\n\n    def _on_save_click(self):\n        description = self._description_input.toPlainText()\n        self._controller.save_workfile_info(\n            self._task_id,\n            self._rootless_path,\n            description=description,\n        )\n        self._orig_description = description\n        self._btn_description_save.setEnabled(False)\n\n    def _set_workarea_context(\n        self,\n        folder_id: Optional[str],\n        task_id: Optional[str],\n        rootless_path: Optional[str],\n        filepath: Optional[str],\n    ) -&gt; None:\n        self._rootless_path = rootless_path\n        self._filepath = filepath\n\n        workfile_info = None\n        # Check if folder, task and file are selected\n        if folder_id and task_id and rootless_path:\n            workfile_info = self._controller.get_workfile_info(\n                folder_id, task_id, rootless_path\n            )\n\n        if workfile_info is None:\n            self._orig_description = \"\"\n            self._description_input.setPlainText(\"\")\n            self._set_context(False, folder_id, task_id)\n            return\n\n        self._set_context(\n            True,\n            folder_id,\n            task_id,\n            file_created=workfile_info.file_created,\n            file_modified=workfile_info.file_modified,\n            size_value=workfile_info.file_size,\n            created_by=workfile_info.created_by,\n            updated_by=workfile_info.updated_by,\n        )\n\n        description = workfile_info.description\n        self._orig_description = description\n        self._description_input.setPlainText(description)\n\n    def _set_publish_context(\n        self,\n        folder_id: Optional[str],\n        task_id: Optional[str],\n        representation_id: Optional[str],\n    ) -&gt; None:\n        self._representation_id = representation_id\n        published_workfile_wrap = self._controller.get_published_workfile_info(\n            folder_id,\n            representation_id,\n        )\n        info = published_workfile_wrap.info\n        comment = published_workfile_wrap.comment\n        if info is None:\n            self._set_context(False, folder_id, task_id)\n            return\n\n        self._set_context(\n            True,\n            folder_id,\n            task_id,\n            file_created=info.file_created,\n            file_modified=info.file_modified,\n            size_value=info.file_size,\n            created_by=info.author,\n            comment=comment,\n        )\n\n    def _set_context(\n        self,\n        is_valid: bool,\n        folder_id: Optional[str],\n        task_id: Optional[str],\n        *,\n        file_created: Optional[int] = None,\n        file_modified: Optional[int] = None,\n        size_value: Optional[int] = None,\n        created_by: Optional[str] = None,\n        updated_by: Optional[str] = None,\n        comment: Optional[str] = None,\n    ) -&gt; None:\n        self._folder_id = folder_id\n        self._task_id = task_id\n\n        self._details_input.setEnabled(is_valid)\n        self._description_input.setEnabled(is_valid)\n        self._btn_description_save.setEnabled(is_valid)\n        if not is_valid:\n            self._details_input.setPlainText(\"\")\n            return\n\n        datetime_format = \"%b %d %Y %H:%M:%S\"\n        if file_created:\n            file_created = datetime.datetime.fromtimestamp(file_created)\n\n        if file_modified:\n            file_modified = datetime.datetime.fromtimestamp(\n                file_modified\n            )\n\n        user_items_by_name = self._controller.get_user_items_by_name()\n\n        def convert_username(username_v):\n            user_item = user_items_by_name.get(username_v)\n            if user_item is not None and user_item.full_name:\n                return user_item.full_name\n            return username_v\n\n        lines = []\n        if size_value is not None:\n            size_value = file_size_to_string(size_value)\n            lines.append(f\"&lt;b&gt;Size:&lt;/b&gt;&lt;br/&gt;{size_value}\")\n\n        # Add version comment for published workfiles\n        if comment:\n            lines.append(f\"&lt;b&gt;Comment:&lt;/b&gt;&lt;br/&gt;{comment}\")\n\n        if created_by or file_created:\n            lines.append(\"&lt;b&gt;Created:&lt;/b&gt;\")\n            if created_by:\n                lines.append(convert_username(created_by))\n            if file_created:\n                lines.append(file_created.strftime(datetime_format))\n\n        if updated_by or file_modified:\n            lines.append(\"&lt;b&gt;Modified:&lt;/b&gt;\")\n            if updated_by:\n                lines.append(convert_username(updated_by))\n            if file_modified:\n                lines.append(file_modified.strftime(datetime_format))\n\n        # Set as empty string\n        self._details_input.setPlainText(\"\")\n        self._details_input.appendHtml(\"&lt;br/&gt;\".join(lines))\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/widgets/side_panel.html#client.ayon_core.tools.workfiles.widgets.side_panel.SidePanelWidget.set_published_mode","title":"<code>set_published_mode(published_mode)</code>","text":"<p>Change published mode.</p> <p>Parameters:</p> Name Type Description Default <code>published_mode</code> <code>bool</code> <p>Published mode enabled.</p> required Source code in <code>client/ayon_core/tools/workfiles/widgets/side_panel.py</code> <pre><code>def set_published_mode(self, published_mode: bool) -&gt; None:\n    \"\"\"Change published mode.\n\n    Args:\n        published_mode (bool): Published mode enabled.\n    \"\"\"\n\n    self._description_widget.setVisible(not published_mode)\n    # Clear the context when switching modes to avoid showing stale data\n    if published_mode:\n        self._set_publish_context(\n            self._folder_id,\n            self._task_id,\n            self._representation_id,\n        )\n    else:\n        self._set_workarea_context(\n            self._folder_id,\n            self._task_id,\n            self._rootless_path,\n            self._filepath,\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/widgets/utils.html","title":"utils","text":""},{"location":"autoapi/client/ayon_core/tools/workfiles/widgets/utils.html#client.ayon_core.tools.workfiles.widgets.utils.BaseOverlayFrame","title":"<code>BaseOverlayFrame</code>","text":"<p>               Bases: <code>QFrame</code></p> <p>Base frame for overlay widgets.</p> <p>Has implemented automated resize and event filtering.</p> Source code in <code>client/ayon_core/tools/workfiles/widgets/utils.py</code> <pre><code>class BaseOverlayFrame(QtWidgets.QFrame):\n    \"\"\"Base frame for overlay widgets.\n\n    Has implemented automated resize and event filtering.\n    \"\"\"\n\n    def __init__(self, parent):\n        super(BaseOverlayFrame, self).__init__(parent)\n        self.setObjectName(\"OverlayFrame\")\n\n        self._parent = parent\n\n    def setVisible(self, visible):\n        super(BaseOverlayFrame, self).setVisible(visible)\n        if visible:\n            self._parent.installEventFilter(self)\n            self.resize(self._parent.size())\n        else:\n            self._parent.removeEventFilter(self)\n\n    def eventFilter(self, obj, event):\n        if event.type() == QtCore.QEvent.Resize:\n            self.resize(obj.size())\n\n        return super(BaseOverlayFrame, self).eventFilter(obj, event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/widgets/window.html","title":"window","text":""},{"location":"autoapi/client/ayon_core/tools/workfiles/widgets/window.html#client.ayon_core.tools.workfiles.widgets.window.WorkfilesToolWindow","title":"<code>WorkfilesToolWindow</code>","text":"<p>               Bases: <code>QWidget</code></p> <p>WorkFiles Window.</p> <p>Main windows of workfiles tool.</p> <p>Parameters:</p> Name Type Description Default <code>controller</code> <code>AbstractWorkfilesFrontend</code> <p>Frontend controller.</p> <code>None</code> <code>parent</code> <code>Optional[QWidget]</code> <p>Parent widget.</p> <code>None</code> Source code in <code>client/ayon_core/tools/workfiles/widgets/window.py</code> <pre><code>class WorkfilesToolWindow(QtWidgets.QWidget):\n    \"\"\"WorkFiles Window.\n\n    Main windows of workfiles tool.\n\n    Args:\n        controller (AbstractWorkfilesFrontend): Frontend controller.\n        parent (Optional[QtWidgets.QWidget]): Parent widget.\n    \"\"\"\n    def __init__(self, controller=None, parent=None):\n        if controller is None:\n            controller = BaseWorkfileController()\n\n        title = \"AYON Workfiles\"\n        subtitle = controller.get_window_subtitle()\n        if subtitle:\n            title += f\" - {subtitle}\"\n\n        super().__init__(parent=parent)\n        self.setWindowTitle(title)\n        icon = QtGui.QIcon(resources.get_ayon_icon_filepath())\n        self.setWindowIcon(icon)\n        flags = self.windowFlags() | QtCore.Qt.Window\n        self.setWindowFlags(flags)\n\n        self._default_window_flags = flags\n\n        self._folders_widget = None\n\n        self._files_widget = None\n\n        self._first_show = True\n        self._controller_refreshed = False\n        self._context_to_set = None\n        # Host validation should happen only once\n        self._host_is_valid = None\n\n        self._controller = controller\n\n        # Create pages widget and set it as central widget\n        pages_widget = QtWidgets.QStackedWidget(self)\n\n        home_page_widget = QtWidgets.QWidget(pages_widget)\n        home_body_widget = QtWidgets.QWidget(home_page_widget)\n\n        col_1_widget = self._create_col_1_widget(controller, parent)\n        tasks_widget = TasksWidget(\n            controller, home_body_widget, handle_expected_selection=True\n        )\n        col_3_widget = self._create_col_3_widget(controller, home_body_widget)\n        side_panel = SidePanelWidget(controller, home_body_widget)\n\n        pages_widget.addWidget(home_page_widget)\n\n        # Build home\n        home_page_layout = QtWidgets.QVBoxLayout(home_page_widget)\n        home_page_layout.addWidget(home_body_widget)\n\n        # Build home - body\n        body_layout = QtWidgets.QVBoxLayout(home_body_widget)\n        split_widget = QtWidgets.QSplitter(home_body_widget)\n        split_widget.addWidget(col_1_widget)\n        split_widget.addWidget(tasks_widget)\n        split_widget.addWidget(col_3_widget)\n        split_widget.addWidget(side_panel)\n        split_widget.setSizes([350, 175, 550, 190])\n\n        body_layout.addWidget(split_widget)\n\n        main_layout = QtWidgets.QHBoxLayout(self)\n        main_layout.addWidget(pages_widget, 1)\n        main_layout.setContentsMargins(0, 0, 0, 0)\n\n        overlay_messages_widget = MessageOverlayObject(self)\n        overlay_invalid_host = InvalidHostOverlay(self)\n        overlay_invalid_host.setVisible(False)\n\n        show_timer = QtCore.QTimer()\n        show_timer.setSingleShot(True)\n        show_timer.setInterval(50)\n\n        show_timer.timeout.connect(self._on_show)\n\n        controller.register_event_callback(\n            \"save_as.finished\",\n            self._on_save_as_finished,\n        )\n        controller.register_event_callback(\n            \"copy_representation.finished\",\n            self._on_copy_representation_finished,\n        )\n        controller.register_event_callback(\n            \"workfile_duplicate.finished\",\n            self._on_duplicate_finished\n        )\n        controller.register_event_callback(\n            \"open_workfile.finished\",\n            self._on_open_finished\n        )\n        controller.register_event_callback(\n            \"controller.reset.started\",\n            self._on_controller_refresh_started,\n        )\n        controller.register_event_callback(\n            \"controller.reset.finished\",\n            self._on_controller_refresh_finished,\n        )\n\n        self._overlay_messages_widget = overlay_messages_widget\n        self._overlay_invalid_host = overlay_invalid_host\n        self._home_page_widget = home_page_widget\n        self._pages_widget = pages_widget\n        self._home_body_widget = home_body_widget\n        self._split_widget = split_widget\n\n        self._project_name = self._controller.get_current_project_name()\n\n        self._tasks_widget = tasks_widget\n        self._side_panel = side_panel\n\n        self._show_timer = show_timer\n\n        self._post_init()\n\n    def _post_init(self):\n        self._on_published_checkbox_changed()\n\n        # Force focus on the open button by default, required for Houdini.\n        self._files_widget.setFocus()\n\n        self.resize(1260, 600)\n\n    def _create_col_1_widget(self, controller, parent):\n        col_widget = QtWidgets.QWidget(parent)\n        header_widget = QtWidgets.QWidget(col_widget)\n\n        filters_widget = FoldersFiltersWidget(header_widget)\n\n        go_to_current_btn = GoToCurrentButton(header_widget)\n        refresh_btn = RefreshButton(header_widget)\n\n        header_layout = QtWidgets.QHBoxLayout(header_widget)\n        header_layout.setContentsMargins(0, 0, 0, 0)\n        header_layout.addWidget(filters_widget, 1)\n        header_layout.addWidget(go_to_current_btn, 0)\n        header_layout.addWidget(refresh_btn, 0)\n\n        folder_widget = FoldersWidget(\n            controller, col_widget, handle_expected_selection=True\n        )\n\n        col_layout = QtWidgets.QVBoxLayout(col_widget)\n        col_layout.setContentsMargins(0, 0, 0, 0)\n        col_layout.addWidget(header_widget, 0)\n        col_layout.addWidget(folder_widget, 1)\n\n        filters_widget.text_changed.connect(self._on_folder_filter_change)\n        filters_widget.my_tasks_changed.connect(\n            self._on_my_tasks_checkbox_state_changed\n        )\n        go_to_current_btn.clicked.connect(self._on_go_to_current_clicked)\n        refresh_btn.clicked.connect(self._on_refresh_clicked)\n\n        self._folders_widget = folder_widget\n\n        self._filters_widget = filters_widget\n\n        return col_widget\n\n    def _create_col_3_widget(self, controller, parent):\n        col_widget = QtWidgets.QWidget(parent)\n\n        header_widget = QtWidgets.QWidget(col_widget)\n\n        files_filter_input = PlaceholderLineEdit(header_widget)\n        files_filter_input.setPlaceholderText(\"Filter files..\")\n\n        published_checkbox = QtWidgets.QCheckBox(\"Published\", header_widget)\n        published_checkbox.setToolTip(\"Show published workfiles\")\n\n        header_layout = QtWidgets.QHBoxLayout(header_widget)\n        header_layout.setContentsMargins(0, 0, 0, 0)\n        header_layout.addWidget(files_filter_input, 1)\n        header_layout.addWidget(published_checkbox, 0)\n\n        files_widget = FilesWidget(controller, col_widget)\n\n        col_layout = QtWidgets.QVBoxLayout(col_widget)\n        col_layout.setContentsMargins(0, 0, 0, 0)\n        col_layout.addWidget(header_widget, 0)\n        col_layout.addWidget(files_widget, 1)\n\n        files_filter_input.textChanged.connect(\n            self._on_file_text_filter_change)\n        published_checkbox.stateChanged.connect(\n            self._on_published_checkbox_changed\n        )\n\n        self._files_filter_input = files_filter_input\n        self._published_checkbox = published_checkbox\n\n        self._files_widget = files_widget\n\n        return col_widget\n\n    def set_window_on_top(self, on_top):\n        \"\"\"Set window on top of other windows.\n\n        Args:\n            on_top (bool): Show on top of other windows.\n        \"\"\"\n\n        flags = self._default_window_flags\n        if on_top:\n            flags |= QtCore.Qt.WindowStaysOnTopHint\n        if self.windowFlags() != flags:\n            self.setWindowFlags(flags)\n\n    def ensure_visible(self, use_context=True, save=True, on_top=False):\n        \"\"\"Ensure the window is visible.\n\n        This method expects arguments for compatibility with previous variant\n            of Workfiles tool.\n\n        Args:\n            use_context (Optional[bool]): DEPRECATED: This argument is\n                ignored.\n            save (Optional[bool]): Allow to save workfiles.\n            on_top (Optional[bool]): Show on top of other windows.\n        \"\"\"\n\n        save = True if save is None else save\n        on_top = False if on_top is None else on_top\n\n        is_visible = self.isVisible()\n        self._controller.set_save_enabled(save)\n        self.set_window_on_top(on_top)\n\n        self.show()\n        self.raise_()\n        self.activateWindow()\n        if is_visible:\n            self.refresh()\n\n    def refresh(self):\n        \"\"\"Trigger refresh of workfiles tool controller.\"\"\"\n\n        self._controller.reset()\n\n    def showEvent(self, event):\n        super(WorkfilesToolWindow, self).showEvent(event)\n        self._show_timer.start()\n        if self._first_show:\n            self._first_show = False\n            self.setStyleSheet(style.load_stylesheet())\n\n    def keyPressEvent(self, event):\n        \"\"\"Custom keyPressEvent.\n\n        Override keyPressEvent to do nothing so that Maya's panels won't\n        take focus when pressing \"SHIFT\" whilst mouse is over viewport or\n        outliner. This way users don't accidentally perform Maya commands\n        whilst trying to name an instance.\n        \"\"\"\n\n        pass\n\n    def _on_show(self):\n        self.refresh()\n\n    def _on_file_text_filter_change(self, text):\n        self._files_widget.set_text_filter(text)\n\n    def _on_published_checkbox_changed(self):\n        \"\"\"Publish mode changed.\n\n        Tell children widgets about it so they can handle the mode.\n        \"\"\"\n\n        published_mode = self._published_checkbox.isChecked()\n        self._files_widget.set_published_mode(published_mode)\n        self._side_panel.set_published_mode(published_mode)\n\n    def _on_folder_filter_change(self, text):\n        self._folders_widget.set_name_filter(text)\n\n    def _on_go_to_current_clicked(self):\n        self._controller.go_to_current_context()\n\n    def _on_refresh_clicked(self):\n        self.refresh()\n\n    def _on_controller_refresh_started(self):\n        self._controller_refreshed = True\n\n    def _on_controller_refresh_finished(self):\n        if self._host_is_valid is None:\n            self._host_is_valid = self._controller.is_host_valid()\n            self._overlay_invalid_host.setVisible(not self._host_is_valid)\n\n        if not self._host_is_valid:\n            return\n\n        self._project_name = self._controller.get_current_project_name()\n        self._folders_widget.set_project_name(self._project_name)\n        self._tasks_widget.refresh()\n\n        # Update my tasks\n        self._on_my_tasks_checkbox_state_changed(\n            self._filters_widget.is_my_tasks_checked()\n        )\n\n    def _on_save_as_finished(self, event):\n        if event[\"failed\"]:\n            self._overlay_messages_widget.add_message(\n                \"Failed to save workfile\",\n                \"error\",\n            )\n        else:\n            self._overlay_messages_widget.add_message(\n                \"Workfile saved\"\n            )\n\n    def _on_copy_representation_finished(self, event):\n        if event[\"failed\"]:\n            self._overlay_messages_widget.add_message(\n                \"Failed to copy published workfile\",\n                \"error\",\n            )\n        else:\n            self._overlay_messages_widget.add_message(\n                \"Publish workfile saved\"\n            )\n\n    def _on_duplicate_finished(self, event):\n        if event[\"failed\"]:\n            self._overlay_messages_widget.add_message(\n                \"Failed to duplicate workfile\",\n                \"error\",\n            )\n        else:\n            self._overlay_messages_widget.add_message(\n                \"Workfile duplicated\"\n            )\n\n    def _on_open_finished(self, event):\n        if event[\"failed\"]:\n            self._overlay_messages_widget.add_message(\n                \"Failed to open workfile\",\n                \"error\",\n            )\n        else:\n            self.close()\n\n    def _on_my_tasks_checkbox_state_changed(self, enabled: bool) -&gt; None:\n        folder_ids = None\n        task_ids = None\n        if enabled:\n            entity_ids = self._controller.get_my_tasks_entity_ids(\n                self._project_name\n            )\n            folder_ids = entity_ids[\"folder_ids\"]\n            task_ids = entity_ids[\"task_ids\"]\n        self._folders_widget.set_folder_ids_filter(folder_ids)\n        self._tasks_widget.set_task_ids_filter(task_ids)\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/widgets/window.html#client.ayon_core.tools.workfiles.widgets.window.WorkfilesToolWindow.ensure_visible","title":"<code>ensure_visible(use_context=True, save=True, on_top=False)</code>","text":"<p>Ensure the window is visible.</p> <p>This method expects arguments for compatibility with previous variant     of Workfiles tool.</p> <p>Parameters:</p> Name Type Description Default <code>use_context</code> <code>Optional[bool]</code> <p>DEPRECATED: This argument is ignored.</p> <code>True</code> <code>save</code> <code>Optional[bool]</code> <p>Allow to save workfiles.</p> <code>True</code> <code>on_top</code> <code>Optional[bool]</code> <p>Show on top of other windows.</p> <code>False</code> Source code in <code>client/ayon_core/tools/workfiles/widgets/window.py</code> <pre><code>def ensure_visible(self, use_context=True, save=True, on_top=False):\n    \"\"\"Ensure the window is visible.\n\n    This method expects arguments for compatibility with previous variant\n        of Workfiles tool.\n\n    Args:\n        use_context (Optional[bool]): DEPRECATED: This argument is\n            ignored.\n        save (Optional[bool]): Allow to save workfiles.\n        on_top (Optional[bool]): Show on top of other windows.\n    \"\"\"\n\n    save = True if save is None else save\n    on_top = False if on_top is None else on_top\n\n    is_visible = self.isVisible()\n    self._controller.set_save_enabled(save)\n    self.set_window_on_top(on_top)\n\n    self.show()\n    self.raise_()\n    self.activateWindow()\n    if is_visible:\n        self.refresh()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/widgets/window.html#client.ayon_core.tools.workfiles.widgets.window.WorkfilesToolWindow.keyPressEvent","title":"<code>keyPressEvent(event)</code>","text":"<p>Custom keyPressEvent.</p> <p>Override keyPressEvent to do nothing so that Maya's panels won't take focus when pressing \"SHIFT\" whilst mouse is over viewport or outliner. This way users don't accidentally perform Maya commands whilst trying to name an instance.</p> Source code in <code>client/ayon_core/tools/workfiles/widgets/window.py</code> <pre><code>def keyPressEvent(self, event):\n    \"\"\"Custom keyPressEvent.\n\n    Override keyPressEvent to do nothing so that Maya's panels won't\n    take focus when pressing \"SHIFT\" whilst mouse is over viewport or\n    outliner. This way users don't accidentally perform Maya commands\n    whilst trying to name an instance.\n    \"\"\"\n\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/widgets/window.html#client.ayon_core.tools.workfiles.widgets.window.WorkfilesToolWindow.refresh","title":"<code>refresh()</code>","text":"<p>Trigger refresh of workfiles tool controller.</p> Source code in <code>client/ayon_core/tools/workfiles/widgets/window.py</code> <pre><code>def refresh(self):\n    \"\"\"Trigger refresh of workfiles tool controller.\"\"\"\n\n    self._controller.reset()\n</code></pre>"},{"location":"autoapi/client/ayon_core/tools/workfiles/widgets/window.html#client.ayon_core.tools.workfiles.widgets.window.WorkfilesToolWindow.set_window_on_top","title":"<code>set_window_on_top(on_top)</code>","text":"<p>Set window on top of other windows.</p> <p>Parameters:</p> Name Type Description Default <code>on_top</code> <code>bool</code> <p>Show on top of other windows.</p> required Source code in <code>client/ayon_core/tools/workfiles/widgets/window.py</code> <pre><code>def set_window_on_top(self, on_top):\n    \"\"\"Set window on top of other windows.\n\n    Args:\n        on_top (bool): Show on top of other windows.\n    \"\"\"\n\n    flags = self._default_window_flags\n    if on_top:\n        flags |= QtCore.Qt.WindowStaysOnTopHint\n    if self.windowFlags() != flags:\n        self.setWindowFlags(flags)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/index.html","title":"ui","text":""},{"location":"autoapi/client/ayon_core/ui/color_utils.html","title":"color_utils","text":""},{"location":"autoapi/client/ayon_core/ui/color_utils.html#client.ayon_core.ui.color_utils.compute_color_for_contrast","title":"<code>compute_color_for_contrast(background, foreground, min_contrast_ratio=4.5)</code>  <code>cached</code>","text":"<p>Adjust foreground color to achieve minimum contrast with background.</p> <p>Preserves the hue and saturation of the original foreground color, adjusting only the lightness to achieve the required contrast ratio.</p> <p>Parameters:</p> Name Type Description Default <code>background</code> <code>tuple</code> <p>The background color to contrast against.</p> required <code>foreground</code> <code>tuple</code> <p>The foreground color to adjust.</p> required <code>min_contrast_ratio</code> <code>float</code> <p>Target minimum contrast ratio (default 4.5:1).</p> <code>4.5</code> <p>Returns:</p> Type Description <code>QColor</code> <p>A QColor with the same hue/saturation but adjusted lightness</p> <code>QColor</code> <p>to meet the contrast requirement. Returns original if already</p> <code>QColor</code> <p>sufficient.</p> Source code in <code>client/ayon_core/ui/color_utils.py</code> <pre><code>@lru_cache(maxsize=256)\ndef compute_color_for_contrast(\n    background: tuple,  # (r, g, b, a)\n    foreground: tuple,  # (r, g, b, a)\n    min_contrast_ratio: float = 4.5,\n) -&gt; QColor:\n    \"\"\"Adjust foreground color to achieve minimum contrast with background.\n\n    Preserves the hue and saturation of the original foreground color,\n    adjusting only the lightness to achieve the required contrast ratio.\n\n    Args:\n        background: The background color to contrast against.\n        foreground: The foreground color to adjust.\n        min_contrast_ratio: Target minimum contrast ratio (default 4.5:1).\n\n    Returns:\n        A QColor with the same hue/saturation but adjusted lightness\n        to meet the contrast requirement. Returns original if already\n        sufficient.\n    \"\"\"\n\n    bg_lum = relative_luminance(*background)\n    fg_lum = relative_luminance(*foreground)\n\n    # Check if already sufficient\n    current_ratio = contrast_ratio(bg_lum, fg_lum)\n    if current_ratio &gt;= min_contrast_ratio:\n        # print(f\"Final ratio: {current_ratio}: {QColor(*foreground).name()}\")\n        return QColor(*foreground)\n\n    fg_col = QColor(*foreground)\n\n    # Get HSL components (hue, saturation preserved)\n    h = fg_col.hslHueF()\n    s = fg_col.hslSaturationF()\n    original_l = fg_col.lightnessF()\n\n    # Determine if we should lighten or darken based on background\n    # Use luminance midpoint (0.1791 for 4.5:1 contrast with both black/white)\n    should_lighten = bg_lum &lt; 0.1791\n\n    # Binary search for optimal lightness\n    if should_lighten:\n        low, high = original_l, 1.0\n        # return QColor(\"white\")\n    else:\n        low, high = 0.0, original_l\n        # return QColor(\"black\")\n\n    result = QColor(\n        fg_col.red(),\n        fg_col.green(),\n        fg_col.blue(),\n        fg_col.alpha(),\n    )\n    original_alpha = result.alpha()\n\n    # Binary search (8 iterations gives ~0.4% precision)\n    for _ in range(8):\n        mid = (low + high) * 0.5\n        result.setHslF(h if h &gt;= 0 else 0.0, s, mid)\n        result.setAlpha(original_alpha)  # Preserve alpha\n\n        test_lum = relative_luminance(*result.toRgb().toTuple())\n        test_ratio = contrast_ratio(bg_lum, test_lum)\n\n        if test_ratio &gt;= min_contrast_ratio:\n            if should_lighten:\n                high = mid  # Can use darker shade\n            else:\n                low = mid  # Can use lighter shade\n        else:\n            if should_lighten:\n                low = mid  # Need lighter\n            else:\n                high = mid  # Need darker\n\n    # Final adjustment to ensure we meet the threshold\n    final_l = high if should_lighten else low\n    result.setHslF(h if h &gt;= 0 else 0.0, s, max(0.0, min(1.0, final_l)))\n    result.setAlpha(original_alpha)  # Preserve alpha\n    result = result.toRgb()\n\n    # Verify and fallback to black/white if HSL adjustment isn't enough\n    final_ratio = contrast_ratio(bg_lum, relative_luminance(*result.toTuple()))\n    if final_ratio &lt; min_contrast_ratio:\n        # HSL adjustment insufficient (saturated colors can't reach target)\n        # Fall back to black or white\n        white_ratio = 1.05 / (bg_lum + 0.05)\n        black_ratio = (bg_lum + 0.05) * 20.0\n        result = (\n            QColor(255, 255, 255, original_alpha)\n            if white_ratio &gt; black_ratio\n            else QColor(0, 0, 0, original_alpha)\n        )\n    # print(f\"Final ratio: {final_ratio}: {result.name()}\")\n    return result\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/color_utils.html#client.ayon_core.ui.color_utils.contrast_ratio","title":"<code>contrast_ratio(lum1, lum2)</code>","text":"<p>Calculate the contrast ratio between two relative luminance values per WCAG 2.1.</p> <p>The contrast ratio ranges from 1:1 (no contrast) to 21:1 (maximum, black vs white). WCAG recommends: - 4.5:1 minimum for normal text (AA) - 7:1 minimum for enhanced contrast (AAA) - 3:1 minimum for large text or UI components</p> Source code in <code>client/ayon_core/ui/color_utils.py</code> <pre><code>def contrast_ratio(lum1: float, lum2: float) -&gt; float:\n    \"\"\"Calculate the contrast ratio between two relative luminance values\n    per WCAG 2.1.\n\n    The contrast ratio ranges from 1:1 (no contrast) to 21:1 (maximum,\n    black vs white). WCAG recommends:\n    - 4.5:1 minimum for normal text (AA)\n    - 7:1 minimum for enhanced contrast (AAA)\n    - 3:1 minimum for large text or UI components\n    \"\"\"\n    if lum1 &gt; lum2:\n        return (lum1 + 0.05) / (lum2 + 0.05)\n    return (lum2 + 0.05) / (lum1 + 0.05)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/color_utils.html#client.ayon_core.ui.color_utils.relative_luminance","title":"<code>relative_luminance(r, g, b, _)</code>  <code>cached</code>","text":"<p>Calculate relative luminance of a color, i.e. sRGB luminance in linear space.</p> Source code in <code>client/ayon_core/ui/color_utils.py</code> <pre><code>@lru_cache(maxsize=256)\ndef relative_luminance(r: int, g: int, b: int, _) -&gt; float:\n    \"\"\"Calculate relative luminance of a color, i.e. sRGB luminance in\n    linear space.\n    \"\"\"\n    comp = [r, g, b]\n    for i in range(3):\n        comp[i] /= 255\n        if comp[i] &lt;= 0.03928:\n            comp[i] /= 12.92\n        else:\n            comp[i] = ((comp[i] + 0.055) / 1.055) ** 2.4\n    return comp[0] * 0.2126 + comp[1] * 0.7152 + comp[2] * 0.0722\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/data_models.html","title":"data_models","text":""},{"location":"autoapi/client/ayon_core/ui/data_models.html#client.ayon_core.ui.data_models.ActivityData","title":"<code>ActivityData</code>  <code>dataclass</code>","text":"<p>Simple dataclass to cache representation data.</p> Source code in <code>client/ayon_core/ui/data_models.py</code> <pre><code>@dataclass\nclass ActivityData:\n    \"\"\"Simple dataclass to cache representation data.\"\"\"\n\n    representation_id: str = \"\"\n    hash: int = field(init=False)\n    activity_list: list[\n        CommentModel | VersionPublishModel | StatusChangeModel\n    ] = field(default_factory=list)\n\n    def __post_init__(self):\n        \"\"\"Compute the hash of the activity list.\"\"\"\n        self.hash = hash(tuple(self.activity_list))\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/data_models.html#client.ayon_core.ui.data_models.ActivityData.__post_init__","title":"<code>__post_init__()</code>","text":"<p>Compute the hash of the activity list.</p> Source code in <code>client/ayon_core/ui/data_models.py</code> <pre><code>def __post_init__(self):\n    \"\"\"Compute the hash of the activity list.\"\"\"\n    self.hash = hash(tuple(self.activity_list))\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/data_models.html#client.ayon_core.ui.data_models.CommentCategory","title":"<code>CommentCategory</code>  <code>dataclass</code>","text":"<p>Model for powerpack <code>activity_categories</code> settings</p> Source code in <code>client/ayon_core/ui/data_models.py</code> <pre><code>@dataclass\nclass CommentCategory:\n    \"\"\"Model for powerpack `activity_categories` settings\"\"\"\n\n    name: str\n    color: str\n    access: dict[str, Any]\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/data_models.html#client.ayon_core.ui.data_models.CommentModel","title":"<code>CommentModel</code>  <code>dataclass</code>","text":"Source code in <code>client/ayon_core/ui/data_models.py</code> <pre><code>@dataclass(unsafe_hash=True)\nclass CommentModel:\n    activity_id: str = \"\"\n    user_full_name: str = \"\"\n    user_name: str = \"\"\n    user_src: str = \"\"\n    comment: str = \"\"  # type: ignore\n    category: str = \"\"\n    category_color: str = \"\"\n    comment_date: str = \"\"\n    short_date: str = field(init=False, hash=False)\n    type: ActivityCategory = field(\n        init=False, default=ActivityCategory.COMMENT, hash=False\n    )\n    files: list[FileModel] = field(default_factory=list, hash=False)\n    annotations: list[AnnotationModel] = field(\n        default_factory=list, hash=False\n    )\n    _comment: str = field(init=False, repr=False)\n\n    def __post_init__(self):\n        \"\"\"Set the date if not set and compute the short date.\"\"\"\n        if not self.comment_date:\n            self.comment_date = datetime.now(timezone.utc).isoformat()\n\n        self.short_date = short_date(self.comment_date)\n\n    @property\n    def comment(self):  # noqa: F811\n        return self._comment\n\n    @comment.setter\n    def comment(self, value: str):\n        \"\"\"Check if the comment contains a checklist.\"\"\"\n        if re.search(RE_HAS_CHECKLIST, str(value)):\n            self.type = ActivityCategory.COMMENT | ActivityCategory.CHECKLIST\n        self._comment = value\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/data_models.html#client.ayon_core.ui.data_models.CommentModel.__post_init__","title":"<code>__post_init__()</code>","text":"<p>Set the date if not set and compute the short date.</p> Source code in <code>client/ayon_core/ui/data_models.py</code> <pre><code>def __post_init__(self):\n    \"\"\"Set the date if not set and compute the short date.\"\"\"\n    if not self.comment_date:\n        self.comment_date = datetime.now(timezone.utc).isoformat()\n\n    self.short_date = short_date(self.comment_date)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/data_models.html#client.ayon_core.ui.data_models.FileModel","title":"<code>FileModel</code>  <code>dataclass</code>","text":"<p>Model for activity attached files.</p> <p>Attachments could be images, there could be also preview available for them.</p> Source code in <code>client/ayon_core/ui/data_models.py</code> <pre><code>@dataclass\nclass FileModel:\n    \"\"\"Model for activity attached files.\n\n    Attachments could be images, there could be also preview available for\n    them.\n    \"\"\"\n\n    id: str\n    mime: str\n    local_path: str = \"\"\n    thumb_local_path: str = \"\"\n    frame: int = 0\n    start_frame: int = 0  # for sequences, to compute the actual frame number\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/data_models.html#client.ayon_core.ui.data_models.ProjectData","title":"<code>ProjectData</code>  <code>dataclass</code>","text":"<p>Model to pass project data - anatomy, users, teams</p> Source code in <code>client/ayon_core/ui/data_models.py</code> <pre><code>@dataclass\nclass ProjectData:\n    \"\"\"Model to pass project data - anatomy, users, teams\"\"\"\n\n    project_name: str\n    users: List[User]\n    teams: List[Team]\n    anatomy: dict[str, Any]\n    comment_category: List[CommentCategory]\n    current_user: Optional[User]\n\n    @staticmethod\n    def not_set():\n        ns = \"PROJECT NOT SET\"\n        return ProjectData(\n            project_name=ns,\n            users=[],\n            teams=[],\n            anatomy={},\n            comment_category=[],\n            current_user=None,\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/data_models.html#client.ayon_core.ui.data_models.User","title":"<code>User</code>  <code>dataclass</code>","text":"<p>Data model for user</p> Source code in <code>client/ayon_core/ui/data_models.py</code> <pre><code>@dataclass\nclass User:\n    \"\"\"Data model for user\"\"\"\n\n    name: str\n    short_name: str\n    full_name: str\n    email: str\n    avatar_url: str = \"\"\n    avatar_local_path: str = \"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/data_models.html#client.ayon_core.ui.data_models.VersionData","title":"<code>VersionData</code>  <code>dataclass</code>","text":"<p>Model to pass enhanced version data</p> Source code in <code>client/ayon_core/ui/data_models.py</code> <pre><code>@dataclass\nclass VersionData:\n    \"\"\"Model to pass enhanced version data\"\"\"\n\n    id: str\n    name: str\n    author: str\n    tags: list[str]\n    status: str\n    product_name: str\n    task_name: str\n    priority: str\n    folder_path: str\n    assignees: list[str]\n    attrib: dict[str, str]\n    thumbnail_id: str = \"\"\n    thumbnail_local_path: str = \"\"\n\n    @staticmethod\n    def not_set():\n        ns = \"VERSION NOT SET\"\n        return VersionData(\n            id=ns,\n            name=ns,\n            author=ns,\n            tags=[],\n            status=ns,\n            product_name=ns,\n            task_name=ns,\n            priority=ns,\n            folder_path=ns,\n            assignees=[],\n            attrib={},\n            thumbnail_id=\"\",\n            thumbnail_local_path=\"\",\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/image_cache.html","title":"image_cache","text":""},{"location":"autoapi/client/ayon_core/ui/image_cache.html#client.ayon_core.ui.image_cache.ImageCache","title":"<code>ImageCache</code>","text":"<p>Process- and thread-safe LRU image cache backed by SQLite.</p> <p>Stores image files on disk and tracks metadata in a SQLite database. Eviction is based on least-recently-used (LRU) access time.</p> Concurrency model <ul> <li>Each thread gets its own <code>sqlite3.Connection</code> via   <code>threading.local()</code>.  SQLite WAL journal mode allows multiple   readers to proceed concurrently across both threads and OS processes;   writers are serialised by SQLite's internal file-level locking, with   a 10-second busy-timeout handling contention.</li> <li>Cache-miss work (calling file_closure and copying the file) is   serialised per cache key via a per-key <code>threading.Lock</code>.   Threads requesting different keys proceed fully in parallel; threads   requesting the same key during a miss wait for exactly one download   to complete and then read the already-populated entry.</li> </ul> Environment variables <p>AYON_IMG_CACHE_DIR: Parent directory for the AYON_IMG_CACHE folder.                     Defaults to the system temp directory. AYON_IMG_CACHE_SIZE: Override default cache size in MB. AYON_IMG_CACHE_CLEAR_ON_STARTUP: Clear all cache files on startup.</p> <p>Attributes:</p> Name Type Description <code>cache_path</code> <code>Path</code> <p>Directory where cached files are stored.</p> <code>max_size_in_MB</code> <code>int</code> <p>Maximum cache size in megabytes.</p> <code>max_size_bytes</code> <code>int</code> <p>Maximum cache size in bytes.</p> Source code in <code>client/ayon_core/ui/image_cache.py</code> <pre><code>class ImageCache:\n    \"\"\"Process- and thread-safe LRU image cache backed by SQLite.\n\n    Stores image files on disk and tracks metadata in a SQLite database.\n    Eviction is based on least-recently-used (LRU) access time.\n\n    Concurrency model:\n        - Each thread gets its own ``sqlite3.Connection`` via\n          ``threading.local()``.  SQLite WAL journal mode allows multiple\n          readers to proceed concurrently across both threads and OS processes;\n          writers are serialised by SQLite's internal file-level locking, with\n          a 10-second busy-timeout handling contention.\n        - Cache-miss work (calling *file_closure* and copying the file) is\n          serialised **per cache key** via a per-key ``threading.Lock``.\n          Threads requesting different keys proceed fully in parallel; threads\n          requesting the same key during a miss wait for exactly one download\n          to complete and then read the already-populated entry.\n\n    Environment variables:\n        AYON_IMG_CACHE_DIR: Parent directory for the AYON_IMG_CACHE folder.\n                            Defaults to the system temp directory.\n        AYON_IMG_CACHE_SIZE: Override default cache size in MB.\n        AYON_IMG_CACHE_CLEAR_ON_STARTUP: Clear all cache files on startup.\n\n    Attributes:\n        cache_path (Path): Directory where cached files are stored.\n        max_size_in_MB (int): Maximum cache size in megabytes.\n        max_size_bytes (int): Maximum cache size in bytes.\n    \"\"\"\n\n    _instance: ImageCache | None = None\n    _lock: threading.Lock = threading.Lock()\n\n    @classmethod\n    def get_instance(\n        cls,\n        cache_path: str | Path | None = None,\n        max_size_in_MB: int = 500,\n    ) -&gt; ImageCache:\n        \"\"\"Return the singleton cache instance, creating it if needed.\n\n        NOTE: `cache_path` and `max_size_in_MB` are only used on the first\n              call. Subsequent calls return the same instance regardless of\n              the arguments.\n\n        Args:\n            cache_path: Directory path for storing cached files.\n            max_size_in_MB: Maximum cache size in megabytes.\n\n        Returns:\n            The singleton ImageCache instance.\n\n        Raises:\n            ValueError: If max_size_in_MB is not positive.\n        \"\"\"\n        with cls._lock:\n            if cls._instance is not None:\n                return cls._instance\n            if max_size_in_MB &lt;= 0:\n                raise ValueError(\"max_size_in_MB must be positive\")\n            instance = object.__new__(cls)\n            instance._initialize(cache_path, max_size_in_MB)\n            cls._instance = instance\n            return cls._instance\n\n    # ------------------------------------------------------------------\n    # Initialisation\n    # ------------------------------------------------------------------\n\n    @staticmethod\n    def _check_sqlite_version() -&gt; None:\n        \"\"\"Raise RuntimeError if the runtime SQLite is too old.\n\n        WAL journal mode requires SQLite \u2265 3.7.0 (released 2010).  Any\n        reasonably modern Python 3 installation satisfies this requirement,\n        but distributions that link CPython against a very old system SQLite\n        could fail at the PRAGMA call without a clear error message.  This\n        check surfaces the problem at initialisation time.\n\n        Raises:\n            RuntimeError: If the runtime SQLite version is below 3.7.0.\n        \"\"\"\n        min_version = (3, 7, 0)\n        if sqlite3.sqlite_version_info &lt; min_version:\n            raise RuntimeError(\n                \"ImageCache requires SQLite &gt;= \"\n                f\"{'.'.join(map(str, min_version))};\"\n                f\" found {sqlite3.sqlite_version}\"\n            )\n\n    def _get_tmp_dir(self) -&gt; Path:\n        tmp = Path(os.environ.get(\"AYON_IMG_CACHE_DIR\", tempfile.gettempdir()))\n        tmp = tmp / \"AYON_IMG_CACHE\"\n        tmp.mkdir(parents=True, exist_ok=True)\n        if os.name != \"nt\":\n            os.chmod(tmp, 0o700)\n        logger.info(f\"AYON image cache: {tmp} ({self.max_size_in_MB} MB)\")\n        return tmp\n\n    def _initialize(\n        self,\n        cache_path: str | Path | None,\n        max_size_in_MB: int,\n    ) -&gt; None:\n        \"\"\"Initialise the cache instance.\n\n        Args:\n            cache_path: Directory path for storing cached files.\n            max_size_in_MB: Maximum cache size in megabytes.\n        \"\"\"\n        self._check_sqlite_version()\n\n        raw = os.environ.get(\"AYON_IMG_CACHE_SIZE\")\n        if raw is not None:\n            self.max_size_in_MB = _parse_positive_int(\n                raw, \"AYON_IMG_CACHE_SIZE\"\n            )\n        else:\n            self.max_size_in_MB = _parse_positive_int(\n                max_size_in_MB, \"max_size_in_MB\"\n            )\n        self.max_size_bytes = self.max_size_in_MB * 1024 * 1024\n\n        self.cache_path = (\n            Path(cache_path) if cache_path else self._get_tmp_dir()\n        )\n        self.cache_path.mkdir(parents=True, exist_ok=True)\n\n        self._db_path = self.cache_path / _DB_FILENAME\n\n        # Per-thread connection storage.\n        self._local = threading.local()\n\n        # Guards both _key_locks and _all_connections.\n        self._meta_lock = threading.Lock()\n\n        # Per-key locks that serialise cache-miss work for the same key.\n        # WeakValueDictionary ensures locks are garbage-collected once no\n        # thread holds a reference, preventing unbounded growth.\n        self._key_locks: weakref.WeakValueDictionary[str, threading.Lock] = (\n            weakref.WeakValueDictionary()\n        )\n\n        # Registry of every connection ever opened so they can all be\n        # closed at process exit.\n        self._all_connections: list[sqlite3.Connection] = []\n\n        # Initialise the schema using the calling thread's connection.\n        setup_conn = self._get_conn()\n        self._set_wal_mode(setup_conn)\n        setup_conn.execute(_CREATE_TABLE_SQL)\n        setup_conn.execute(_CREATE_INDEX_SQL)\n        setup_conn.commit()\n\n        if \"AYON_IMG_CACHE_CLEAR_ON_STARTUP\" in os.environ:\n            self._clear_all_files()\n\n        self._validate_cache_files()\n        self._cleanup_legacy_files()\n\n        atexit.register(self._close_all_connections)\n\n    def _clear_all_files(self) -&gt; None:\n        \"\"\"Delete every file in the cache directory and reset the DB.\"\"\"\n        conn = self._get_conn()\n        count = 0\n        for entry in os.scandir(self.cache_path):\n            if entry.is_file() and entry.name != self._db_path.name:\n                if _DB_FILENAME in entry.path:\n                    continue\n                try:\n                    os.remove(entry.path)\n                    count += 1\n                except OSError as exc:\n                    logger.warning(f\"Could not remove {entry.path}: {exc}\")\n        conn.execute(\"DELETE FROM cache\")\n        conn.commit()\n        logger.info(f\"AYON image cache: cleared ({count} files removed)\")\n\n    def _cleanup_legacy_files(self) -&gt; None:\n        \"\"\"Remove legacy JSON metadata files left from the previous version.\"\"\"\n        for name in (\"cache_metadata.json\", \"cache_metadata.json.lock\"):\n            legacy = self.cache_path / name\n            if legacy.exists():\n                try:\n                    legacy.unlink()\n                    logger.debug(f\"Removed legacy cache file: {legacy}\")\n                except OSError as exc:\n                    logger.warning(\n                        f\"Could not remove legacy file {legacy}: {exc}\"\n                    )\n\n    def _set_wal_mode(\n        self,\n        conn: sqlite3.Connection,\n        retries: int = 5,\n        base_delay: float = 0.1,\n    ) -&gt; None:\n        \"\"\"Switch the database to WAL journal mode with retries.\n\n        ``PRAGMA journal_mode=WAL`` requires an exclusive lock on the\n        database file.  Under heavy concurrent process startup the lock\n        may not be available even though ``busy_timeout`` is set, so we\n        retry with linear back-off.\n\n        Args:\n            conn: The connection to use for setting WAL mode.\n            retries: Maximum number of attempts.\n            base_delay: Seconds to wait between attempts (multiplied\n                by the attempt number).\n        \"\"\"\n        for attempt in range(retries):\n            try:\n                conn.execute(\"PRAGMA journal_mode=WAL\")\n                return\n            except sqlite3.OperationalError:\n                if attempt == retries - 1:\n                    raise\n                time.sleep(base_delay * (attempt + 1))\n\n    # ------------------------------------------------------------------\n    # Connection management\n    # ------------------------------------------------------------------\n\n    def _open_connection(self) -&gt; sqlite3.Connection:\n        \"\"\"Open and configure a new SQLite connection.\n\n        WAL journal mode is **not** set here; it is a one-time,\n        exclusive-lock operation performed during initialisation via\n        ``_set_wal_mode``.  Once set, WAL mode is stored in the database\n        file header and all subsequent connections inherit it automatically.\n\n        Returns:\n            A new ``sqlite3.Connection`` configured with busy_timeout.\n        \"\"\"\n        return sqlite3.connect(str(self._db_path), timeout=10)\n\n    def _get_conn(self) -&gt; sqlite3.Connection:\n        \"\"\"Return the per-thread SQLite connection, creating it if needed.\n\n        Each thread gets its own connection, which avoids the need for an\n        application-level lock on reads.  New connections are registered in\n        ``_all_connections`` so they can be closed at process exit.\n\n        Returns:\n            The ``sqlite3.Connection`` for the calling thread.\n        \"\"\"\n        conn = getattr(self._local, \"conn\", None)\n        if conn is None:\n            conn = self._open_connection()\n            self._local.conn = conn\n            with self._meta_lock:\n                self._all_connections.append(conn)\n        return conn\n\n    def _close_all_connections(self) -&gt; None:\n        \"\"\"Close every per-thread connection opened by this instance.\n\n        Registered with ``atexit`` during initialisation.\n        \"\"\"\n        with self._meta_lock:\n            for conn in self._all_connections:\n                try:\n                    conn.close()\n                except Exception:\n                    logger.debug(\n                        \"Exception while closing cache DB connection\",\n                        exc_info=True,\n                    )\n            self._all_connections.clear()\n\n    # ------------------------------------------------------------------\n    # Per-key locking\n    # ------------------------------------------------------------------\n\n    def _get_key_lock(self, key: str) -&gt; threading.Lock:\n        \"\"\"Return the per-key lock for *key*, creating it if needed.\n\n        Args:\n            key: The cache key.\n\n        Returns:\n            A ``threading.Lock`` dedicated to *key*.\n        \"\"\"\n        with self._meta_lock:\n            lock = self._key_locks.get(key)\n            if lock is None:\n                lock = threading.Lock()\n                self._key_locks[key] = lock\n            return lock\n\n    # ------------------------------------------------------------------\n    # Internal DB helpers\n    # ------------------------------------------------------------------\n\n    def _lookup(self, key: str) -&gt; Path | None:\n        \"\"\"Return the cached ``Path`` for *key* without updating metadata.\n\n        Purges the DB row and returns ``None`` if the file is missing on disk.\n\n        Args:\n            key: The cache key to look up.\n\n        Returns:\n            Absolute ``Path`` to the cached file, or ``None`` on a miss.\n        \"\"\"\n        conn = self._get_conn()\n        row = conn.execute(\n            \"SELECT file_path FROM cache WHERE key = ?\", (key,)\n        ).fetchone()\n        if row is None:\n            return None\n        cached_path = Path(row[0])\n        if cached_path.exists():\n            return cached_path\n        logger.debug(f\"Cached file missing for key '{key}': purging row\")\n        conn.execute(\"DELETE FROM cache WHERE key = ?\", (key,))\n        conn.commit()\n        return None\n\n    def _touch(self, key: str) -&gt; None:\n        \"\"\"Increment the access counter and refresh last_accessed for *key*.\n\n        This is a best-effort metadata update for LRU ordering.  If the row\n        has been removed between a preceding ``_lookup`` and this call (e.g.\n        by a concurrent eviction), the ``UPDATE`` matches zero rows and is\n        silently discarded \u2014 LRU accuracy is not required to be exact.\n\n        Args:\n            key: The cache key to update.\n        \"\"\"\n        conn = self._get_conn()\n        conn.execute(\n            \"UPDATE cache \"\n            \"SET access_count = access_count + 1, last_accessed = ? \"\n            \"WHERE key = ?\",\n            (time.time(), key),\n        )\n        conn.commit()\n\n    # ------------------------------------------------------------------\n    # Public API\n    # ------------------------------------------------------------------\n\n    def get(self, key: str, file_closure: Callable) -&gt; str:\n        \"\"\"Return the cached file path for *key*, caching it if necessary.\n\n        Cache hits are served via a fast path that requires no application-\n        level locking; SQLite WAL mode allows concurrent reads across threads\n        and processes.\n\n        On a cache miss, a per-key lock is acquired so that only one thread\n        calls *file_closure* for a given key at a time.  After acquiring the\n        lock the database is re-checked: if another thread already populated\n        the entry the result is returned immediately without calling the\n        closure again.\n\n        *file_closure* is called **outside** any database transaction, so a\n        slow I/O operation (e.g. a network download) does not block threads\n        that are requesting different keys.\n\n        Args:\n            key: Unique identifier for the cached file.\n            file_closure: Callable returning the path to the source file.\n                          Called only on a cache miss.\n\n        Returns:\n            Absolute path to the cached file as a string.\n\n        Raises:\n            ValueError: If *key* is empty or *file_closure* returns a path\n                        that does not exist.\n            IOError: If the file cannot be copied into the cache.\n        \"\"\"\n        if not key:\n            raise ValueError(\"Cache key cannot be empty\")\n\n        # ------------------------------------------------------------------\n        # ------------------------------------------------------------------\n        # Fast path: pure SELECT \u2014 concurrent reads across threads/processes\n        # proceed without blocking each other in WAL mode.  _touch is a\n        # separate best-effort write; LRU accuracy does not need to be exact.\n        # ------------------------------------------------------------------\n        cached_path = self._lookup(key)\n        if cached_path is not None:\n            self._touch(key)\n            logger.debug(f\"Cache hit for key '{key}'\")\n            return str(cached_path)\n\n        # ------------------------------------------------------------------\n        # Slow path: acquire the per-key lock, then double-check.\n        # Only threads requesting the same key are serialised here; threads\n        # requesting different keys proceed fully in parallel.\n        # ------------------------------------------------------------------\n        with self._get_key_lock(key):\n            # Double-check: another thread may have populated the entry\n            # while we were waiting for the key lock.\n            cached_path = self._lookup(key)\n            if cached_path is not None:\n                self._touch(key)\n                logger.debug(f\"Cache hit for key '{key}' (after key lock)\")\n                return str(cached_path)\n\n            # True miss: call the closure outside any database transaction.\n            logger.debug(f\"Cache miss for key '{key}', calling file_closure\")\n            source_path = Path(file_closure())\n\n            if not source_path.exists():\n                raise ValueError(\n                    f\"Loader returned non-existent file: {source_path}\"\n                )\n\n            cache_filename = self._generate_cache_filename(key, source_path)\n            cached_path = self.cache_path / cache_filename\n\n            self._atomic_copy(source_path, cached_path)\n\n            conn = self._get_conn()\n            file_size = cached_path.stat().st_size\n            conn.execute(\n                \"INSERT OR REPLACE INTO cache \"\n                \"(key, file_path, size_bytes, access_count, last_accessed) \"\n                \"VALUES (?, ?, ?, 1, ?)\",\n                (key, str(cached_path), file_size, time.time()),\n            )\n            conn.commit()\n\n        # Eviction runs outside the per-key lock: it can be slow (file I/O +\n        # bulk DB DELETE) and must not block other threads querying this key.\n        self._evict_if_needed()\n\n        logger.debug(f\"Cached file for key '{key}': {cached_path}\")\n        return str(cached_path)\n\n    def has(self, key: str) -&gt; bool:\n        \"\"\"Return True if *key* is cached and its file exists on disk.\n\n        Args:\n            key: Cache key to check.\n\n        Returns:\n            True if the key is present and the file exists, else False.\n        \"\"\"\n        return self._lookup(key) is not None\n\n    def get_path(self, key: str) -&gt; str | None:\n        \"\"\"Return the cached file path for *key* if it exists, else None.\n\n        Updates access metadata when the entry is found.\n\n        Args:\n            key: Cache key to look up.\n\n        Returns:\n            Absolute path string to the cached file, or None.\n        \"\"\"\n        cached_path = self._lookup(key)\n        if cached_path is None:\n            return None\n        self._touch(key)\n        return str(cached_path)\n\n    # ------------------------------------------------------------------\n    # Internal helpers\n    # ------------------------------------------------------------------\n\n    def _atomic_copy(self, src: Path, dst: Path) -&gt; None:\n        \"\"\"Copy *src* to *dst* atomically using a temp file + os.replace().\n\n        The temporary file is created in the same directory as *dst* so that\n        ``os.replace`` is an atomic rename on the same filesystem.\n\n        Args:\n            src: Source file path.\n            dst: Destination file path.\n\n        Raises:\n            IOError: If the copy or rename fails.\n        \"\"\"\n        fd, tmp_name = tempfile.mkstemp(dir=self.cache_path, suffix=\".tmp\")\n        os.close(fd)\n        tmp_path = Path(tmp_name)\n        try:\n            shutil.copy2(src, tmp_path)\n            os.replace(tmp_path, dst)\n        except OSError as exc:\n            tmp_path.unlink(missing_ok=True)\n            raise IOError(f\"Failed to cache file: {exc}\") from exc\n\n    def set_path(self, key: str, file_path: str) -&gt; Path:\n        \"\"\"Manually set a cache entry for a given key and file path.\"\"\"\n        with self._access_lock:\n            source_path = Path(file_path)\n            if not source_path.exists():\n                raise ValueError(\n                    f\"Provided file does not exist: {source_path}\"\n                )\n\n            cache_filename = self._generate_cache_filename(key, source_path)\n            cached_path = self.cache_path / cache_filename\n\n            try:\n                with open(source_path, \"rb\") as src:\n                    with open(cached_path, \"wb\") as dst:\n                        dst.write(src.read())\n            except IOError as e:\n                raise IOError(f\"Failed to set cache file: {e}\") from e\n\n            file_size = cached_path.stat().st_size\n            self._metadata[key] = {\n                \"file_path\": str(cached_path),\n                \"size_bytes\": file_size,\n                \"access_count\": 0,\n                \"last_accessed\": time.time(),\n            }\n            self._evict_if_needed()\n            return cached_path\n\n    def _generate_cache_filename(self, key: str, source_path: Path) -&gt; str:\n        \"\"\"Build a cache filename from a SHA-256 hash of *key*.\n\n        Args:\n            key: The cache key.\n            source_path: The original file (used only to preserve extension).\n\n        Returns:\n            Filename string with the original extension.\n        \"\"\"\n        key_hash = hashlib.sha256(key.encode()).hexdigest()\n        extension = source_path.suffix\n        return f\"{key_hash}{extension}\"\n\n    def _evict_if_needed(self) -&gt; None:\n        \"\"\"Evict LRU entries until cache size is at 90 % of the limit.\"\"\"\n        conn = self._get_conn()\n        current_size = self._get_cache_size()\n\n        if current_size &lt;= self.max_size_bytes:\n            return\n\n        target_size = int(self.max_size_bytes * 0.9)\n        logger.info(\n            f\"Cache size ({current_size} bytes) exceeds limit \"\n            f\"({self.max_size_bytes} bytes). Evicting to {target_size} bytes\"\n        )\n\n        rows = conn.execute(\n            \"SELECT key, file_path, size_bytes FROM cache ORDER BY \"\n            \"last_accessed ASC\"\n        ).fetchall()\n\n        keys_to_delete: list[str] = []\n        for evict_key, file_path_str, size_bytes in rows:\n            if current_size &lt;= target_size:\n                break\n            # Acquire the per-key lock non-blocking: skip keys that are\n            # currently being served by another thread so we do not delete\n            # a file that a concurrent get() is about to return.\n            key_lock = self._get_key_lock(evict_key)\n            if not key_lock.acquire(blocking=False):\n                logger.debug(\n                    f\"Skipping eviction of key '{evict_key}' \"\n                    \"(lock held by another thread)\"\n                )\n                continue\n            try:\n                file_path = Path(file_path_str)\n                if file_path.exists():\n                    file_path.unlink()\n                current_size -= size_bytes\n                keys_to_delete.append(evict_key)\n                logger.debug(f\"Evicted cache entry for key '{evict_key}'\")\n            except OSError as exc:\n                logger.warning(f\"Failed to delete cache file: {exc}\")\n            finally:\n                key_lock.release()\n\n        if keys_to_delete:\n            placeholders = \",\".join(\"?\" * len(keys_to_delete))\n            conn.execute(\n                f\"DELETE FROM cache WHERE key IN ({placeholders})\",\n                keys_to_delete,\n            )\n            conn.commit()\n\n    def _get_cache_size(self) -&gt; int:\n        \"\"\"Return the total size of all cached entries in bytes.\n\n        Returns:\n            Total bytes recorded in the database.\n        \"\"\"\n        conn = self._get_conn()\n        row = conn.execute(\n            \"SELECT COALESCE(SUM(size_bytes), 0) FROM cache\"\n        ).fetchone()\n        return int(row[0])\n\n    def _validate_cache_files(self) -&gt; None:\n        \"\"\"Remove DB entries whose files no longer exist on disk.\"\"\"\n        conn = self._get_conn()\n        rows = conn.execute(\"SELECT key, file_path FROM cache\").fetchall()\n\n        invalid_keys = [key for key, fp in rows if not Path(fp).exists()]\n\n        if not invalid_keys:\n            return\n\n        placeholders = \",\".join(\"?\" * len(invalid_keys))\n        conn.execute(\n            f\"DELETE FROM cache WHERE key IN ({placeholders})\",\n            invalid_keys,\n        )\n        conn.commit()\n        logger.debug(f\"Removed {len(invalid_keys)} invalid cache entries\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/image_cache.html#client.ayon_core.ui.image_cache.ImageCache.get","title":"<code>get(key, file_closure)</code>","text":"<p>Return the cached file path for key, caching it if necessary.</p> <p>Cache hits are served via a fast path that requires no application- level locking; SQLite WAL mode allows concurrent reads across threads and processes.</p> <p>On a cache miss, a per-key lock is acquired so that only one thread calls file_closure for a given key at a time.  After acquiring the lock the database is re-checked: if another thread already populated the entry the result is returned immediately without calling the closure again.</p> <p>file_closure is called outside any database transaction, so a slow I/O operation (e.g. a network download) does not block threads that are requesting different keys.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>Unique identifier for the cached file.</p> required <code>file_closure</code> <code>Callable</code> <p>Callable returning the path to the source file.           Called only on a cache miss.</p> required <p>Returns:</p> Type Description <code>str</code> <p>Absolute path to the cached file as a string.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If key is empty or file_closure returns a path         that does not exist.</p> <code>IOError</code> <p>If the file cannot be copied into the cache.</p> Source code in <code>client/ayon_core/ui/image_cache.py</code> <pre><code>def get(self, key: str, file_closure: Callable) -&gt; str:\n    \"\"\"Return the cached file path for *key*, caching it if necessary.\n\n    Cache hits are served via a fast path that requires no application-\n    level locking; SQLite WAL mode allows concurrent reads across threads\n    and processes.\n\n    On a cache miss, a per-key lock is acquired so that only one thread\n    calls *file_closure* for a given key at a time.  After acquiring the\n    lock the database is re-checked: if another thread already populated\n    the entry the result is returned immediately without calling the\n    closure again.\n\n    *file_closure* is called **outside** any database transaction, so a\n    slow I/O operation (e.g. a network download) does not block threads\n    that are requesting different keys.\n\n    Args:\n        key: Unique identifier for the cached file.\n        file_closure: Callable returning the path to the source file.\n                      Called only on a cache miss.\n\n    Returns:\n        Absolute path to the cached file as a string.\n\n    Raises:\n        ValueError: If *key* is empty or *file_closure* returns a path\n                    that does not exist.\n        IOError: If the file cannot be copied into the cache.\n    \"\"\"\n    if not key:\n        raise ValueError(\"Cache key cannot be empty\")\n\n    # ------------------------------------------------------------------\n    # ------------------------------------------------------------------\n    # Fast path: pure SELECT \u2014 concurrent reads across threads/processes\n    # proceed without blocking each other in WAL mode.  _touch is a\n    # separate best-effort write; LRU accuracy does not need to be exact.\n    # ------------------------------------------------------------------\n    cached_path = self._lookup(key)\n    if cached_path is not None:\n        self._touch(key)\n        logger.debug(f\"Cache hit for key '{key}'\")\n        return str(cached_path)\n\n    # ------------------------------------------------------------------\n    # Slow path: acquire the per-key lock, then double-check.\n    # Only threads requesting the same key are serialised here; threads\n    # requesting different keys proceed fully in parallel.\n    # ------------------------------------------------------------------\n    with self._get_key_lock(key):\n        # Double-check: another thread may have populated the entry\n        # while we were waiting for the key lock.\n        cached_path = self._lookup(key)\n        if cached_path is not None:\n            self._touch(key)\n            logger.debug(f\"Cache hit for key '{key}' (after key lock)\")\n            return str(cached_path)\n\n        # True miss: call the closure outside any database transaction.\n        logger.debug(f\"Cache miss for key '{key}', calling file_closure\")\n        source_path = Path(file_closure())\n\n        if not source_path.exists():\n            raise ValueError(\n                f\"Loader returned non-existent file: {source_path}\"\n            )\n\n        cache_filename = self._generate_cache_filename(key, source_path)\n        cached_path = self.cache_path / cache_filename\n\n        self._atomic_copy(source_path, cached_path)\n\n        conn = self._get_conn()\n        file_size = cached_path.stat().st_size\n        conn.execute(\n            \"INSERT OR REPLACE INTO cache \"\n            \"(key, file_path, size_bytes, access_count, last_accessed) \"\n            \"VALUES (?, ?, ?, 1, ?)\",\n            (key, str(cached_path), file_size, time.time()),\n        )\n        conn.commit()\n\n    # Eviction runs outside the per-key lock: it can be slow (file I/O +\n    # bulk DB DELETE) and must not block other threads querying this key.\n    self._evict_if_needed()\n\n    logger.debug(f\"Cached file for key '{key}': {cached_path}\")\n    return str(cached_path)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/image_cache.html#client.ayon_core.ui.image_cache.ImageCache.get_instance","title":"<code>get_instance(cache_path=None, max_size_in_MB=500)</code>  <code>classmethod</code>","text":"<p>Return the singleton cache instance, creating it if needed.</p> <code>cache_path</code> and <code>max_size_in_MB</code> are only used on the first <p>call. Subsequent calls return the same instance regardless of the arguments.</p> <p>Parameters:</p> Name Type Description Default <code>cache_path</code> <code>str | Path | None</code> <p>Directory path for storing cached files.</p> <code>None</code> <code>max_size_in_MB</code> <code>int</code> <p>Maximum cache size in megabytes.</p> <code>500</code> <p>Returns:</p> Type Description <code>ImageCache</code> <p>The singleton ImageCache instance.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If max_size_in_MB is not positive.</p> Source code in <code>client/ayon_core/ui/image_cache.py</code> <pre><code>@classmethod\ndef get_instance(\n    cls,\n    cache_path: str | Path | None = None,\n    max_size_in_MB: int = 500,\n) -&gt; ImageCache:\n    \"\"\"Return the singleton cache instance, creating it if needed.\n\n    NOTE: `cache_path` and `max_size_in_MB` are only used on the first\n          call. Subsequent calls return the same instance regardless of\n          the arguments.\n\n    Args:\n        cache_path: Directory path for storing cached files.\n        max_size_in_MB: Maximum cache size in megabytes.\n\n    Returns:\n        The singleton ImageCache instance.\n\n    Raises:\n        ValueError: If max_size_in_MB is not positive.\n    \"\"\"\n    with cls._lock:\n        if cls._instance is not None:\n            return cls._instance\n        if max_size_in_MB &lt;= 0:\n            raise ValueError(\"max_size_in_MB must be positive\")\n        instance = object.__new__(cls)\n        instance._initialize(cache_path, max_size_in_MB)\n        cls._instance = instance\n        return cls._instance\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/image_cache.html#client.ayon_core.ui.image_cache.ImageCache.get_path","title":"<code>get_path(key)</code>","text":"<p>Return the cached file path for key if it exists, else None.</p> <p>Updates access metadata when the entry is found.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>Cache key to look up.</p> required <p>Returns:</p> Type Description <code>str | None</code> <p>Absolute path string to the cached file, or None.</p> Source code in <code>client/ayon_core/ui/image_cache.py</code> <pre><code>def get_path(self, key: str) -&gt; str | None:\n    \"\"\"Return the cached file path for *key* if it exists, else None.\n\n    Updates access metadata when the entry is found.\n\n    Args:\n        key: Cache key to look up.\n\n    Returns:\n        Absolute path string to the cached file, or None.\n    \"\"\"\n    cached_path = self._lookup(key)\n    if cached_path is None:\n        return None\n    self._touch(key)\n    return str(cached_path)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/image_cache.html#client.ayon_core.ui.image_cache.ImageCache.has","title":"<code>has(key)</code>","text":"<p>Return True if key is cached and its file exists on disk.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>Cache key to check.</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if the key is present and the file exists, else False.</p> Source code in <code>client/ayon_core/ui/image_cache.py</code> <pre><code>def has(self, key: str) -&gt; bool:\n    \"\"\"Return True if *key* is cached and its file exists on disk.\n\n    Args:\n        key: Cache key to check.\n\n    Returns:\n        True if the key is present and the file exists, else False.\n    \"\"\"\n    return self._lookup(key) is not None\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/image_cache.html#client.ayon_core.ui.image_cache.ImageCache.set_path","title":"<code>set_path(key, file_path)</code>","text":"<p>Manually set a cache entry for a given key and file path.</p> Source code in <code>client/ayon_core/ui/image_cache.py</code> <pre><code>def set_path(self, key: str, file_path: str) -&gt; Path:\n    \"\"\"Manually set a cache entry for a given key and file path.\"\"\"\n    with self._access_lock:\n        source_path = Path(file_path)\n        if not source_path.exists():\n            raise ValueError(\n                f\"Provided file does not exist: {source_path}\"\n            )\n\n        cache_filename = self._generate_cache_filename(key, source_path)\n        cached_path = self.cache_path / cache_filename\n\n        try:\n            with open(source_path, \"rb\") as src:\n                with open(cached_path, \"wb\") as dst:\n                    dst.write(src.read())\n        except IOError as e:\n            raise IOError(f\"Failed to set cache file: {e}\") from e\n\n        file_size = cached_path.stat().st_size\n        self._metadata[key] = {\n            \"file_path\": str(cached_path),\n            \"size_bytes\": file_size,\n            \"access_count\": 0,\n            \"last_accessed\": time.time(),\n        }\n        self._evict_if_needed()\n        return cached_path\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/image_cache.html#client.ayon_core.ui.image_cache.make_activity_cache_key","title":"<code>make_activity_cache_key(project_name, file_id, is_thumbnail=False)</code>","text":"<p>Build the ImageCache key for an activity-attached file.</p> <p>Both the producer (download enqueue) and the consumer (UI refresh) must use this function so that they always agree on the key, and so that entries from different projects never collide.</p> <p>Parameters:</p> Name Type Description Default <code>project_name</code> <code>str</code> <p>AYON project name that owns the file.</p> required <code>file_id</code> <code>str</code> <p>The AYON file identifier.</p> required <code>is_thumbnail</code> <code>bool</code> <p>Whether the key is for the thumbnail variant.</p> <code>False</code> <p>Returns:</p> Type Description <code>str</code> <p>Cache key string in the form</p> <code>str</code> <p><code>act_&lt;project_name&gt;_&lt;file_id&gt;</code> or</p> <code>str</code> <p><code>act_thumb_&lt;project_name&gt;_&lt;file_id&gt;</code>.</p> Source code in <code>client/ayon_core/ui/image_cache.py</code> <pre><code>def make_activity_cache_key(\n    project_name: str,\n    file_id: str,\n    is_thumbnail: bool = False,\n) -&gt; str:\n    \"\"\"Build the ImageCache key for an activity-attached file.\n\n    Both the producer (download enqueue) and the consumer (UI refresh)\n    must use this function so that they always agree on the key, and so\n    that entries from different projects never collide.\n\n    Args:\n        project_name: AYON project name that owns the file.\n        file_id: The AYON file identifier.\n        is_thumbnail: Whether the key is for the thumbnail variant.\n\n    Returns:\n        Cache key string in the form\n        ``act_&lt;project_name&gt;_&lt;file_id&gt;`` or\n        ``act_thumb_&lt;project_name&gt;_&lt;file_id&gt;``.\n    \"\"\"\n    prefix = \"thumb_\" if is_thumbnail else \"\"\n    return f\"act_{prefix}{project_name}_{file_id}\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/style.html","title":"style","text":""},{"location":"autoapi/client/ayon_core/ui/style.html#client.ayon_core.ui.style.AYONStyle","title":"<code>AYONStyle</code>","text":"<p>               Bases: <code>QCommonStyle</code></p> <p>AYON QStyle implementation that replaces QSS styling with native Qt painting. Supports widget variants: surface, tonal, filled, tertiary, text, nav, etc.</p> Source code in <code>client/ayon_core/ui/style.py</code> <pre><code>class AYONStyle(QCommonStyle):\n    \"\"\"\n    AYON QStyle implementation that replaces QSS styling with native Qt\n    painting. Supports widget variants: surface, tonal, filled, tertiary,\n    text, nav, etc.\n    \"\"\"\n\n    def __init__(self) -&gt; None:\n        super().__init__()\n        self.model = StyleData()\n        self._in_widget_key = False\n        self.drawers = {}\n        self.sizers = {}\n        self.metrics = {}\n        self.base_classes = {}\n        self.drawer_objs = [\n            TooltipDrawer(self),\n            LabelDrawer(self),\n            LineEditDrawer(self),\n            ButtonDrawer(self),\n            CheckboxDrawer(self),\n            ComboBoxDrawer(self),\n            ScrollBarDrawer(self),\n            FrameDrawer(self),\n            TreeViewDrawer(self),\n            TableHeaderDrawer(self),\n            ItemViewItemDrawer(self),\n            ScrollAreaDrawer(self),\n            MenuDrawer(self),\n        ]\n        for obj in self.drawer_objs:\n            self.base_classes.update(obj.base_class)\n            if hasattr(obj, \"register_drawers\"):\n                self.drawers.update(obj.register_drawers())\n            if hasattr(obj, \"register_sizers\"):\n                self.sizers.update(obj.register_sizers())\n            if hasattr(obj, \"register_metrics\"):\n                self.metrics.update(obj.register_metrics())\n\n        # Sort base_classes: most-specific first to guarantee correct widget\n        # key resolution order.\n        def _specificity_cmp(a, b):\n            \"\"\"Return &lt;0 if `a` is more specific than `b`.\n\n            More specific means: `a` is a (strict) subclass of `b`.\n            For unrelated classes, fall back to MRO depth so deeper\n            classes still come first, then class name for stability.\n            \"\"\"\n            ca, cb = a[1], b[1]\n            if ca is cb:\n                return 0\n            if issubclass(ca, cb):\n                return -1  # a before b\n            if issubclass(cb, ca):\n                return 1  # b before a\n            # Unrelated: deeper MRO first, then name for determinism.\n            d = len(cb.__mro__) - len(ca.__mro__)\n            if d:\n                return d\n            return (ca.__name__ &gt; cb.__name__) - (ca.__name__ &lt; cb.__name__)\n\n        self.base_classes = dict(\n            sorted(self.base_classes.items(), key=cmp_to_key(_specificity_cmp))\n        )\n\n    def widget_key(self, w: QWidget | None) -&gt; str:\n        if self._in_widget_key or not w or not isValid(w):\n            return \"\"\n\n        # Handle item view widgets - check parent for delegate and exclude\n        # ComboBoxItemDelegate\n        if (\n            hasattr(w, \"itemDelegate\")\n            and not isinstance(w, (QComboBox, QHeaderView))\n        ):\n            # Calling itemDelegate() is not a simple getter - it can\n            # trigger Qt's internal operations that call back into the\n            # custom style methods (subElementRect, drawPrimitive,\n            # pixelMetric, etc.), each of which calls widget_key() again,\n            # creating infinite recursion.\n            self._in_widget_key = True\n            try:\n                delegate = w.itemDelegate()\n                if not isinstance(\n                    delegate,\n                    (\n                        TreeViewItemDelegate,\n                        TableItemDelegate,\n                        ComboBoxItemDelegate\n                    )\n                ):\n                    return \"QStyledItemDelegate\"\n            finally:\n                self._in_widget_key = False\n\n        for name, wtype in self.base_classes.items():\n            if not issubclass(type(w), wtype):\n                continue\n\n            if w.objectName() == \"qtooltip_label\":\n                return \"QToolTip\"\n\n            if isinstance(w, QLabel):\n                p = w.parent()\n                # NOTE: Qt does not use QToolTip but a QLabel (a\n                # private QLabelTip class) !!\n                # if the parent is a widget and it doesn't have a\n                # layout, it could be a tooltip.\n                # Sometimes, objectName() == \"qtooltip_label\" but the\n                # object name is set when the rect requests are made.\n                if p and isinstance(p, QWidget) and not p.layout():\n                    return \"QToolTip\"\n            return name\n        return \"\"\n\n    def style_widget(self, widget: QWidget) -&gt; None:\n        \"\"\"Apply AYON style to a widget (palette, font, hover tracking).\"\"\"\n        if not isinstance(widget, QWidget):\n            return\n\n        variant = getattr(widget, \"_variant_str\", \"default\")\n        if hasattr(widget, \"_style_data\") and not widget._style_data:\n            widget._style_data = self.model.get_style(\n                self.widget_key(widget),\n                variant,\n            )\n            widget._style_data.set_context(widget)\n\n        if hasattr(widget, \"set_palette\"):\n            widget.set_palette(\n                self.model.get_style_palette(\n                    widget, self.widget_key(widget)\n                )\n            )\n        else:\n            widget.setPalette(QtGui.QPalette(self.model.base_palette))\n\n        if hasattr(widget, \"set_font\"):\n            widget.set_font(QFont(self.model.base_font))\n        else:\n            widget.setFont(QFont(self.model.base_font))\n\n        # Enable mouse tracking for buttons to receive hover events\n        widget.setAttribute(Qt.WidgetAttribute.WA_Hover, True)\n        widget.setMouseTracking(True)\n\n        if isinstance(widget, QComboBox):\n            widget.setMinimumContentsLength(1)\n            widget.setItemDelegate(\n                ComboBoxItemDelegate(parent=widget, style_model=self.model)\n            )\n            widget.setSizeAdjustPolicy(\n                QComboBox.SizeAdjustPolicy.AdjustToContents\n            )\n        elif isinstance(widget, QLabel):\n            # rounded corner no background.\n            widget.setAttribute(\n                Qt.WidgetAttribute.WA_TranslucentBackground, True\n            )\n        elif isinstance(widget, QtWidgets.QMenu):\n            widget.setAttribute(\n                Qt.WidgetAttribute.WA_TranslucentBackground, True\n            )\n            widget.setWindowFlags(\n                widget.windowFlags() | Qt.WindowType.NoDropShadowWindowHint\n            )\n\n            # make icons visible in menus (MacOS)\n            def _setup_actions(menu):\n                for action in menu.actions():\n                    if action.isSeparator():\n                        continue\n                    action.setIconVisibleInMenu(True)\n                    action.setShortcutVisibleInContextMenu(True)\n                    if action.menu():\n                        _setup_actions(action.menu())\n\n            _setup_actions(widget)\n\n    def polish(self, widget) -&gt; None:\n        \"\"\"Polish widgets to enable hover tracking and custom palette.\"\"\"\n        if isinstance(widget, QWidget):\n            super().polish(widget)\n            self.style_widget(widget)\n\n        else:\n            super().polish(widget)\n\n    def drawControl(\n        self,\n        element: QStyle.ControlElement,\n        option: QStyleOption,\n        painter: QPainter,\n        w: QWidget | None = None,\n    ) -&gt; None:\n        \"\"\"Draw control elements (buttons, labels, etc.).\"\"\"\n        key = enum_to_str(QStyle.ControlElement, element, self.widget_key(w))\n\n        if type(w).__name__ in W_T:\n            log.info(\"  &gt;&gt;  drawControl %s %s\", type(w), key)\n\n        draw_ce_calls = self.drawers.get(key)\n        if draw_ce_calls is None:\n            super().drawControl(element, option, painter, w)\n            return\n\n        if isinstance(draw_ce_calls, list):\n            for draw_ce in draw_ce_calls:\n                draw_ce(option, painter, w)\n        elif callable(draw_ce_calls):\n            draw_ce_calls(option, painter, w)\n\n    def drawComplexControl(\n        self,\n        cc: QStyle.ComplexControl,\n        opt: QtWidgets.QStyleOptionComplex,\n        p: QPainter,\n        w: QWidget | None = None,\n    ) -&gt; None:\n        key = enum_to_str(QStyle.ComplexControl, cc, self.widget_key(w))\n        if type(w).__name__ in W_T:\n            log.info(\"  &gt;&gt;  drawComplexControl %s %s\", type(w), key)\n\n        draw_cc = self.drawers.get(key)\n        if draw_cc is None:\n            # no custom drawer fallback\n            return super().drawComplexControl(cc, opt, p, w)\n\n        draw_cc(opt, p, w)\n\n    def drawPrimitive(\n        self,\n        element: QStyle.PrimitiveElement,\n        option: QStyleOption,\n        painter: QPainter,\n        w: QWidget | None = None,\n    ) -&gt; None:\n        \"\"\"Draw primitive elements.\"\"\"\n\n        key = enum_to_str(QStyle.PrimitiveElement, element, self.widget_key(w))\n        if type(w).__name__ in W_T:\n            log.info(\"  &gt;&gt;  drawPrimitive %s %s\", type(w), key)\n\n        draw_prim = self.drawers.get(key)\n        if draw_prim is None:\n            # Fall back to parent implementation\n            super().drawPrimitive(element, option, painter, w)\n            return\n\n        draw_prim(option, painter, w)\n\n    def subElementRect(\n        self,\n        element: QStyle.SubElement,\n        option: QStyleOption,\n        widget: QWidget | None = None,\n    ) -&gt; QRect:\n        \"\"\"Calculate rectangles for sub-elements.\"\"\"\n        key = enum_to_str(QStyle.SubElement, element, self.widget_key(widget))\n\n        if isinstance(widget, QLabel):\n            log.debug(\"%s %s\", type(widget).__name__, key)\n\n        sizer = self.sizers.get(key)\n        if sizer is None:\n            # Fall back to parent implementation\n            # Catch RuntimeError in case widget's C++ object was already\n            # deleted\n            try:\n                if widget is not None:\n                    return super().subElementRect(element, option, widget)\n                return super().subElementRect(element, option)\n            except RuntimeError:\n                # Widget was deleted, call without it\n                return super().subElementRect(element, option)\n\n        return sizer(element, option, widget)\n\n    def subControlRect(\n        self,\n        cc: QStyle.ComplexControl,\n        opt: QtWidgets.QStyleOptionComplex,\n        sc: QStyle.SubControl,\n        w: QWidget | None = None,\n    ) -&gt; QRect:\n        key = enum_to_str(QStyle.ComplexControl, cc, self.widget_key(w))\n\n        if isinstance(w, QLabel):\n            log.debug(\"%s %s\", type(w).__name__, key)\n\n        sizer = self.sizers.get(key)\n        if sizer is None:\n            # Fall back to parent implementation\n            return super().subControlRect(cc, opt, sc, w)\n\n        try:\n            return sizer(cc, opt, sc, w)\n        except ValueError:\n            return super().subControlRect(cc, opt, sc, w)\n\n    def pixelMetric(\n        self,\n        metric: QStyle.PixelMetric,\n        opt: QStyleOption | None = None,\n        widget: QWidget | None = None,\n    ) -&gt; int:\n        \"\"\"Return pixel measurements for various style metrics.\"\"\"\n        key = enum_to_str(QStyle.PixelMetric, metric, self.widget_key(widget))\n\n        if isinstance(widget, QLabel):\n            log.debug(\"%s %s\", type(widget), key)\n\n        metric_func = self.metrics.get(key)\n        if metric_func is None:\n            # Fall back to parent implementation\n            return super().pixelMetric(metric, opt, widget)\n\n        return metric_func(metric, opt, widget)\n\n    def styleHint(\n        self,\n        hint: QStyle.StyleHint,\n        opt: QStyleOption | None = None,\n        w: QWidget | None = None,\n        shret: QtWidgets.QStyleHintReturn | None = None,\n    ) -&gt; int:\n        \"\"\"Return style hints for behavior configuration.\"\"\"\n\n        if hint == QStyle.StyleHint.SH_Button_FocusPolicy:\n            return Qt.FocusPolicy.StrongFocus\n        elif hint == QStyle.StyleHint.SH_RequestSoftwareInputPanel:\n            return 0\n        elif hint == QStyle.StyleHint.SH_ComboBox_PopupFrameStyle:\n            return QFrame.Shape.NoFrame\n        # Fall back to parent implementation\n        return super().styleHint(hint, opt, w, shret)\n\n    def sizeFromContents(\n        self,\n        contents_type: QStyle.ContentsType,\n        option: QStyleOption | None,\n        contents_size: QtCore.QSize,\n        widget: QWidget | None = None,\n    ) -&gt; QtCore.QSize:\n        \"\"\"Calculate minimum size requirements for widgets based on their\n        content.\"\"\"\n        key = enum_to_str(\n            QStyle.ContentsType, contents_type, self.widget_key(widget)\n        )\n\n        if isinstance(widget, QLabel):\n            log.debug(\"%s\", widget)\n\n        sizer = self.sizers.get(key)\n        if sizer is not None:\n            return sizer(contents_type, option, contents_size, widget)\n\n        if option:\n            return super().sizeFromContents(\n                contents_type, option, contents_size, widget\n            )\n\n        # Create a reasonable default size if no option is provided\n        return QtCore.QSize(100, 32)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/style.html#client.ayon_core.ui.style.AYONStyle.__init__","title":"<code>__init__()</code>","text":"Source code in <code>client/ayon_core/ui/style.py</code> <pre><code>def __init__(self) -&gt; None:\n    super().__init__()\n    self.model = StyleData()\n    self._in_widget_key = False\n    self.drawers = {}\n    self.sizers = {}\n    self.metrics = {}\n    self.base_classes = {}\n    self.drawer_objs = [\n        TooltipDrawer(self),\n        LabelDrawer(self),\n        LineEditDrawer(self),\n        ButtonDrawer(self),\n        CheckboxDrawer(self),\n        ComboBoxDrawer(self),\n        ScrollBarDrawer(self),\n        FrameDrawer(self),\n        TreeViewDrawer(self),\n        TableHeaderDrawer(self),\n        ItemViewItemDrawer(self),\n        ScrollAreaDrawer(self),\n        MenuDrawer(self),\n    ]\n    for obj in self.drawer_objs:\n        self.base_classes.update(obj.base_class)\n        if hasattr(obj, \"register_drawers\"):\n            self.drawers.update(obj.register_drawers())\n        if hasattr(obj, \"register_sizers\"):\n            self.sizers.update(obj.register_sizers())\n        if hasattr(obj, \"register_metrics\"):\n            self.metrics.update(obj.register_metrics())\n\n    # Sort base_classes: most-specific first to guarantee correct widget\n    # key resolution order.\n    def _specificity_cmp(a, b):\n        \"\"\"Return &lt;0 if `a` is more specific than `b`.\n\n        More specific means: `a` is a (strict) subclass of `b`.\n        For unrelated classes, fall back to MRO depth so deeper\n        classes still come first, then class name for stability.\n        \"\"\"\n        ca, cb = a[1], b[1]\n        if ca is cb:\n            return 0\n        if issubclass(ca, cb):\n            return -1  # a before b\n        if issubclass(cb, ca):\n            return 1  # b before a\n        # Unrelated: deeper MRO first, then name for determinism.\n        d = len(cb.__mro__) - len(ca.__mro__)\n        if d:\n            return d\n        return (ca.__name__ &gt; cb.__name__) - (ca.__name__ &lt; cb.__name__)\n\n    self.base_classes = dict(\n        sorted(self.base_classes.items(), key=cmp_to_key(_specificity_cmp))\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/style.html#client.ayon_core.ui.style.AYONStyle.drawControl","title":"<code>drawControl(element, option, painter, w=None)</code>","text":"<p>Draw control elements (buttons, labels, etc.).</p> Source code in <code>client/ayon_core/ui/style.py</code> <pre><code>def drawControl(\n    self,\n    element: QStyle.ControlElement,\n    option: QStyleOption,\n    painter: QPainter,\n    w: QWidget | None = None,\n) -&gt; None:\n    \"\"\"Draw control elements (buttons, labels, etc.).\"\"\"\n    key = enum_to_str(QStyle.ControlElement, element, self.widget_key(w))\n\n    if type(w).__name__ in W_T:\n        log.info(\"  &gt;&gt;  drawControl %s %s\", type(w), key)\n\n    draw_ce_calls = self.drawers.get(key)\n    if draw_ce_calls is None:\n        super().drawControl(element, option, painter, w)\n        return\n\n    if isinstance(draw_ce_calls, list):\n        for draw_ce in draw_ce_calls:\n            draw_ce(option, painter, w)\n    elif callable(draw_ce_calls):\n        draw_ce_calls(option, painter, w)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/style.html#client.ayon_core.ui.style.AYONStyle.drawPrimitive","title":"<code>drawPrimitive(element, option, painter, w=None)</code>","text":"<p>Draw primitive elements.</p> Source code in <code>client/ayon_core/ui/style.py</code> <pre><code>def drawPrimitive(\n    self,\n    element: QStyle.PrimitiveElement,\n    option: QStyleOption,\n    painter: QPainter,\n    w: QWidget | None = None,\n) -&gt; None:\n    \"\"\"Draw primitive elements.\"\"\"\n\n    key = enum_to_str(QStyle.PrimitiveElement, element, self.widget_key(w))\n    if type(w).__name__ in W_T:\n        log.info(\"  &gt;&gt;  drawPrimitive %s %s\", type(w), key)\n\n    draw_prim = self.drawers.get(key)\n    if draw_prim is None:\n        # Fall back to parent implementation\n        super().drawPrimitive(element, option, painter, w)\n        return\n\n    draw_prim(option, painter, w)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/style.html#client.ayon_core.ui.style.AYONStyle.pixelMetric","title":"<code>pixelMetric(metric, opt=None, widget=None)</code>","text":"<p>Return pixel measurements for various style metrics.</p> Source code in <code>client/ayon_core/ui/style.py</code> <pre><code>def pixelMetric(\n    self,\n    metric: QStyle.PixelMetric,\n    opt: QStyleOption | None = None,\n    widget: QWidget | None = None,\n) -&gt; int:\n    \"\"\"Return pixel measurements for various style metrics.\"\"\"\n    key = enum_to_str(QStyle.PixelMetric, metric, self.widget_key(widget))\n\n    if isinstance(widget, QLabel):\n        log.debug(\"%s %s\", type(widget), key)\n\n    metric_func = self.metrics.get(key)\n    if metric_func is None:\n        # Fall back to parent implementation\n        return super().pixelMetric(metric, opt, widget)\n\n    return metric_func(metric, opt, widget)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/style.html#client.ayon_core.ui.style.AYONStyle.polish","title":"<code>polish(widget)</code>","text":"<p>Polish widgets to enable hover tracking and custom palette.</p> Source code in <code>client/ayon_core/ui/style.py</code> <pre><code>def polish(self, widget) -&gt; None:\n    \"\"\"Polish widgets to enable hover tracking and custom palette.\"\"\"\n    if isinstance(widget, QWidget):\n        super().polish(widget)\n        self.style_widget(widget)\n\n    else:\n        super().polish(widget)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/style.html#client.ayon_core.ui.style.AYONStyle.sizeFromContents","title":"<code>sizeFromContents(contents_type, option, contents_size, widget=None)</code>","text":"<p>Calculate minimum size requirements for widgets based on their content.</p> Source code in <code>client/ayon_core/ui/style.py</code> <pre><code>def sizeFromContents(\n    self,\n    contents_type: QStyle.ContentsType,\n    option: QStyleOption | None,\n    contents_size: QtCore.QSize,\n    widget: QWidget | None = None,\n) -&gt; QtCore.QSize:\n    \"\"\"Calculate minimum size requirements for widgets based on their\n    content.\"\"\"\n    key = enum_to_str(\n        QStyle.ContentsType, contents_type, self.widget_key(widget)\n    )\n\n    if isinstance(widget, QLabel):\n        log.debug(\"%s\", widget)\n\n    sizer = self.sizers.get(key)\n    if sizer is not None:\n        return sizer(contents_type, option, contents_size, widget)\n\n    if option:\n        return super().sizeFromContents(\n            contents_type, option, contents_size, widget\n        )\n\n    # Create a reasonable default size if no option is provided\n    return QtCore.QSize(100, 32)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/style.html#client.ayon_core.ui.style.AYONStyle.styleHint","title":"<code>styleHint(hint, opt=None, w=None, shret=None)</code>","text":"<p>Return style hints for behavior configuration.</p> Source code in <code>client/ayon_core/ui/style.py</code> <pre><code>def styleHint(\n    self,\n    hint: QStyle.StyleHint,\n    opt: QStyleOption | None = None,\n    w: QWidget | None = None,\n    shret: QtWidgets.QStyleHintReturn | None = None,\n) -&gt; int:\n    \"\"\"Return style hints for behavior configuration.\"\"\"\n\n    if hint == QStyle.StyleHint.SH_Button_FocusPolicy:\n        return Qt.FocusPolicy.StrongFocus\n    elif hint == QStyle.StyleHint.SH_RequestSoftwareInputPanel:\n        return 0\n    elif hint == QStyle.StyleHint.SH_ComboBox_PopupFrameStyle:\n        return QFrame.Shape.NoFrame\n    # Fall back to parent implementation\n    return super().styleHint(hint, opt, w, shret)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/style.html#client.ayon_core.ui.style.AYONStyle.style_widget","title":"<code>style_widget(widget)</code>","text":"<p>Apply AYON style to a widget (palette, font, hover tracking).</p> Source code in <code>client/ayon_core/ui/style.py</code> <pre><code>def style_widget(self, widget: QWidget) -&gt; None:\n    \"\"\"Apply AYON style to a widget (palette, font, hover tracking).\"\"\"\n    if not isinstance(widget, QWidget):\n        return\n\n    variant = getattr(widget, \"_variant_str\", \"default\")\n    if hasattr(widget, \"_style_data\") and not widget._style_data:\n        widget._style_data = self.model.get_style(\n            self.widget_key(widget),\n            variant,\n        )\n        widget._style_data.set_context(widget)\n\n    if hasattr(widget, \"set_palette\"):\n        widget.set_palette(\n            self.model.get_style_palette(\n                widget, self.widget_key(widget)\n            )\n        )\n    else:\n        widget.setPalette(QtGui.QPalette(self.model.base_palette))\n\n    if hasattr(widget, \"set_font\"):\n        widget.set_font(QFont(self.model.base_font))\n    else:\n        widget.setFont(QFont(self.model.base_font))\n\n    # Enable mouse tracking for buttons to receive hover events\n    widget.setAttribute(Qt.WidgetAttribute.WA_Hover, True)\n    widget.setMouseTracking(True)\n\n    if isinstance(widget, QComboBox):\n        widget.setMinimumContentsLength(1)\n        widget.setItemDelegate(\n            ComboBoxItemDelegate(parent=widget, style_model=self.model)\n        )\n        widget.setSizeAdjustPolicy(\n            QComboBox.SizeAdjustPolicy.AdjustToContents\n        )\n    elif isinstance(widget, QLabel):\n        # rounded corner no background.\n        widget.setAttribute(\n            Qt.WidgetAttribute.WA_TranslucentBackground, True\n        )\n    elif isinstance(widget, QtWidgets.QMenu):\n        widget.setAttribute(\n            Qt.WidgetAttribute.WA_TranslucentBackground, True\n        )\n        widget.setWindowFlags(\n            widget.windowFlags() | Qt.WindowType.NoDropShadowWindowHint\n        )\n\n        # make icons visible in menus (MacOS)\n        def _setup_actions(menu):\n            for action in menu.actions():\n                if action.isSeparator():\n                    continue\n                action.setIconVisibleInMenu(True)\n                action.setShortcutVisibleInContextMenu(True)\n                if action.menu():\n                    _setup_actions(action.menu())\n\n        _setup_actions(widget)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/style.html#client.ayon_core.ui.style.AYONStyle.subElementRect","title":"<code>subElementRect(element, option, widget=None)</code>","text":"<p>Calculate rectangles for sub-elements.</p> Source code in <code>client/ayon_core/ui/style.py</code> <pre><code>def subElementRect(\n    self,\n    element: QStyle.SubElement,\n    option: QStyleOption,\n    widget: QWidget | None = None,\n) -&gt; QRect:\n    \"\"\"Calculate rectangles for sub-elements.\"\"\"\n    key = enum_to_str(QStyle.SubElement, element, self.widget_key(widget))\n\n    if isinstance(widget, QLabel):\n        log.debug(\"%s %s\", type(widget).__name__, key)\n\n    sizer = self.sizers.get(key)\n    if sizer is None:\n        # Fall back to parent implementation\n        # Catch RuntimeError in case widget's C++ object was already\n        # deleted\n        try:\n            if widget is not None:\n                return super().subElementRect(element, option, widget)\n            return super().subElementRect(element, option)\n        except RuntimeError:\n            # Widget was deleted, call without it\n            return super().subElementRect(element, option)\n\n    return sizer(element, option, widget)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/style_types.html","title":"style_types","text":"<p>Shared style types and singleton accessor for ayon_ui_qt.</p> <p>This module is intentionally a leaf \u2014 it imports only from stdlib, Qt, and the drawers utility layer.  No import from <code>.components</code> or from the top-level <code>.style</code> module is performed at module-load time, which breaks the circular dependency that would otherwise arise between <code>style.py</code> and the component modules.</p> Exports <p>StyleDict: Context-aware dict that resolves <code>@attr</code> references. StyleData: Loads and caches data from <code>ayon_style.json</code>. get_ayon_style: Returns the process-wide <code>AYONStyle</code> singleton. get_ayon_style_data: Convenience wrapper around     <code>get_ayon_style().model.get_style()</code>. hsl_to_html_color: Converts HSL CSS strings to Qt colour hex codes.</p>"},{"location":"autoapi/client/ayon_core/ui/style_types.html#client.ayon_core.ui.style_types.StyleData","title":"<code>StyleData</code>","text":"<p>Loads <code>ayon_style.json</code> and provides cached style lookups.</p> <p>Attributes:</p> Name Type Description <code>data</code> <p>The raw parsed JSON data.</p> <code>base_palette</code> <code>QPalette</code> <p>A <code>QPalette</code> built from the <code>palette</code> section.</p> Source code in <code>client/ayon_core/ui/style_types.py</code> <pre><code>class StyleData:\n    \"\"\"Loads ``ayon_style.json`` and provides cached style lookups.\n\n    Attributes:\n        data: The raw parsed JSON data.\n        base_palette: A ``QPalette`` built from the ``palette`` section.\n    \"\"\"\n\n    def __init__(self) -&gt; None:\n        fpath = Path(__file__).parent / \"ayon_style.json\"\n        with open(fpath, \"r\") as fh:\n            self.data = json.load(fh)\n        # Palette values can reference each other\n        self._palette = self.data.get(\"palette\", {})\n        for k, v in self._palette.items():\n            if v.startswith(\"hsl(\"):\n                self._palette[k] = hsl_to_html_color(v)\n        for k, v in self._palette.items():\n            self._palette[k] = self._palette.get(v, v)\n        for k, v in self._palette.items():\n            if v in self._palette:\n                raise ValueError(f\"Unresolved palette value in {k}\")\n        # cache\n        self._cache = {}\n        self.last_key = \"\"\n        # base palette\n        self.base_palette: QPalette = self._build_palette()\n        self._base_font: QFont | None = None\n\n    @property\n    def base_font(self) -&gt; QFont:\n        \"\"\"Return the base application font, loading it lazily.\n\n        Returns:\n            A copy of the configured base ``QFont``.\n        \"\"\"\n        # delayed to make sure QApplication is initialized\n        if self._base_font is None:\n            self._base_font = style_font(\n                self.data.get(\"global\", {}), QWidget()\n            )\n            _load_fonts()\n        return QFont(self._base_font)\n\n    def _build_palette(self) -&gt; QPalette:\n        bp = {\n            QPalette.ColorRole.Window: \"qt-active-window\",\n            QPalette.ColorRole.WindowText: \"qt-active-window-text\",\n            QPalette.ColorRole.Base: \"qt-active-base\",\n            QPalette.ColorRole.Text: \"qt-active-text\",\n            QPalette.ColorRole.Link: \"qt-active-link\",\n            QPalette.ColorRole.Button: \"qt-active-button\",\n            QPalette.ColorRole.ButtonText: \"qt-active-button-text\",\n            QPalette.ColorRole.PlaceholderText: \"qt-active-placeholder-text\",\n            QPalette.ColorRole.Highlight: \"qt-active-highlight\",\n            QPalette.ColorRole.HighlightedText: \"qt-active-highlight-text\",\n            QPalette.ColorRole.Light: \"qt-active-light\",\n            QPalette.ColorRole.Midlight: \"qt-active-midlight\",\n            QPalette.ColorRole.Dark: \"qt-active-dark\",\n            QPalette.ColorRole.Mid: \"qt-active-mid\",\n            QPalette.ColorRole.Shadow: \"qt-active-shadow\",\n        }\n        p = QPalette()\n        for role, color_name in bp.items():\n            p.setColor(\n                QPalette.ColorGroup.Active,\n                role,\n                QColor(self._palette.get(color_name, \"#ff0000\")),\n            )\n        return p\n\n    def get_style_palette(self, widget: QWidget, widget_key: str) -&gt; QPalette:\n        \"\"\"Get a QPalette for the widget based on the current style data.\n\n        Resolves any @ references in the style data to widget attributes.\n        Caches results for efficiency, if there are no @ references.\n\n        Args:\n            widget: The widget to build the palette for.\n            widget_key: The style key identifying the widget class.\n\n        Returns:\n            A ``QPalette`` configured for the widget.\n        \"\"\"\n        variant = getattr(widget, \"_variant_str\", \"default\")\n        # check if cached\n        cache_key = f\"{widget_key}-{variant}-palette\"\n        if cache_key in self._cache:\n            return QPalette(self._cache[cache_key])\n\n        style: StyleDict = self.get_styles(\n            widget_key,\n            variant=variant,\n        )\n        style.set_context(widget)\n\n        p = QPalette(self.base_palette)\n\n        fg_color = style.get(\"base\", {}).get(\"color\")\n        if fg_color:\n            p.setColor(\n                widget.foregroundRole(),\n                QColor(fg_color),\n            )\n        opacity = style.get(\"disabled\", {}).get(\"opacity\")\n        if opacity:\n            disabled_fg_color = QColor(fg_color)\n            disabled_fg_color.setAlphaF(opacity)\n            p.setColor(\n                QPalette.ColorGroup.Disabled,\n                widget.foregroundRole(),\n                disabled_fg_color,\n            )\n\n        bg_color = style.get(\"base\", {}).get(\"background-color\")\n        if bg_color:\n            p.setColor(\n                widget.backgroundRole(),\n                QColor(bg_color),\n            )\n\n        # cache result\n        style.set_context(None)\n        self._cache[cache_key] = p\n\n        return QPalette(p)\n\n    def dump_cache_stats(self) -&gt; None:\n        \"\"\"Print cache statistics to stdout (debug aid).\"\"\"\n        print(f\"[StyleData] cached {len(self._cache)} styles.\")\n        print(f\"[StyleData]   &gt;&gt; {list(self._cache.keys())}\")\n\n    def widget_variants(self, widget_cls: str) -&gt; list[str]:\n        \"\"\"Return all variant names registered for *widget_cls*.\n\n        Args:\n            widget_cls: The widget class key (e.g. ``\"QPushButton\"``).\n\n        Returns:\n            A list of variant name strings.\n        \"\"\"\n        return list(\n            self.data[\"widgets\"].get(widget_cls, {}).get(\"variants\", {}).keys()\n        )\n\n    def widget_states(self, widget_cls: str) -&gt; list[str]:\n        \"\"\"Return the list of states for *widget_cls*, always including\n        ``\"base\"`` as the first entry.\n\n        Args:\n            widget_cls: The widget class key.\n\n        Returns:\n            Ordered list of state name strings.\n        \"\"\"\n        states = list(\n            self.data[\"widgets\"].get(widget_cls, {}).get(\"states\", [])\n        )\n        return states if \"base\" in states else [\"base\"] + states\n\n    def widget_data(self, widget_cls: str) -&gt; dict:\n        \"\"\"Return the raw widget data block for *widget_cls*.\n\n        Args:\n            widget_cls: The widget class key.\n\n        Returns:\n            The raw data dict (empty dict if not found).\n        \"\"\"\n        return self.data[\"widgets\"].get(widget_cls, {})\n\n    def widget_list(self) -&gt; list[str]:\n        \"\"\"Return all registered widget class keys.\n\n        Returns:\n            List of widget class key strings.\n        \"\"\"\n        return list(self.data[\"widgets\"].keys())\n\n    def default_variant(self, widget_data: dict) -&gt; str:\n        \"\"\"Return the default variant name for *widget_data*.\n\n        Args:\n            widget_data: The raw widget data block.\n\n        Returns:\n            The default variant name string.\n        \"\"\"\n        variants = widget_data.get(\"variants\", {})\n        return widget_data.get(\n            \"default-variant\", next(iter(variants.keys()), \"default\")\n        )\n\n    def validate_variant(self, widget_data: dict, variant: str) -&gt; str:\n        \"\"\"Return *variant* if valid, otherwise return the default variant.\n\n        Args:\n            widget_data: The raw widget data block.\n            variant: The requested variant name.\n\n        Returns:\n            A valid variant name string.\n        \"\"\"\n        if variant not in widget_data.get(\"variants\", {}):\n            return self.default_variant(widget_data)\n        return variant\n\n    def palette(self) -&gt; dict:\n        \"\"\"Return the resolved colour palette mapping.\n\n        Returns:\n            Dict mapping palette key strings to HTML hex colours.\n        \"\"\"\n        return self._palette\n\n    def get_style(\n        self,\n        widget_cls: str,\n        variant: str | None = None,\n        state: str = \"base\",\n    ) -&gt; StyleDict:\n        \"\"\"Return a style dict for a widget, variant and state.\n\n        Args:\n            widget_cls: The widget class key.\n            variant: Optional variant name.\n            state: State to retrieve; ``\"all\"`` returns every state merged.\n\n        Returns:\n            A :class:`StyleDict` for the requested combination.\n        \"\"\"\n        try:\n            return StyleDict(self._cache[f\"{widget_cls}-{variant}-{state}\"])\n        except KeyError:\n            pass\n\n        data = self.widget_data(widget_cls)\n        vrt = self.validate_variant(data, variant)\n        dvrt = self.default_variant(data)\n        pal = self._palette\n        d = copy.copy(self.data[\"global\"])\n        d.update(copy.deepcopy(data.get(\"variants\", {}).get(dvrt, {})))\n        d.update(copy.deepcopy(data.get(\"variants\", {}).get(vrt, {})))\n\n        if state == \"all\":\n            for key, val in d.items():\n                if isinstance(val, dict):\n                    d[key] = {kk: pal.get(vv, vv) for kk, vv in val.items()}\n                elif not isinstance(val, list):\n                    d[key] = pal.get(val, val)\n        else:\n            # Override palette variables with the current state's values and\n            # remove all states. That way, we can directly use\n            # \"background-color\" without checking the widget's state.\n            state_dict = {}\n            for key, val in list(d.items()):\n                if isinstance(val, dict):\n                    if key == state:\n                        state_dict = {\n                            kk: pal.get(vv, vv) for kk, vv in val.items()\n                        }\n                    d.pop(key)\n                elif not isinstance(val, list):\n                    d[key] = pal.get(val, val)\n            # apply current state overrides last to ensure they take precedence\n            # over the base variant\n            d.update(state_dict)\n\n        # cache result\n        d = StyleDict(d)\n        self.last_key = f\"{widget_cls}-{variant}-{state}\"\n        self._cache[self.last_key] = d\n        return StyleDict(d)\n\n    def get_styles(\n        self,\n        widget_cls: str,\n        variant: str | None = None,\n        states: list[str] | None = None,\n    ) -&gt; StyleDict:\n        \"\"\"Return styles for a widget, variant and multiple states at once.\n\n        This is more efficient than calling :meth:`get_style` multiple times\n        when you need styles for several states of the same widget/variant.\n\n        Args:\n            widget_cls: The widget class name, e.g. ``\"QStyledItemDelegate\"``.\n            variant: The variant name (e.g., ``\"default\"``). Defaults to None.\n            states: List of states to retrieve (e.g., ``[\"base\", \"hover\",\n                \"checked\"]``). Defaults to all defined states.\n\n        Returns:\n            A :class:`StyleDict` mapping state names to their style dicts.\n        \"\"\"\n        if states is None:\n            states = self.widget_states(widget_cls)\n\n        cache_key = f\"all-{widget_cls}-{variant}-{'|'.join(states)}\"\n        try:\n            return StyleDict(self._cache[cache_key])\n        except KeyError:\n            pass\n\n        d = {\n            state: self.get_style(widget_cls, variant, state)\n            for state in states\n        }\n        self._cache[cache_key] = StyleDict(d)\n        return StyleDict(d)\n\n    def current_style(self) -&gt; StyleDict:\n        \"\"\"Return the most recently computed style entry from cache.\n\n        Returns:\n            The last :class:`StyleDict` placed into the cache.\n        \"\"\"\n        return self._cache[self.last_key]\n\n    def get_widget_color(\n        self,\n        color_name: str,\n        style: dict,\n        w: QWidget,\n        default: str | QColor,\n    ) -&gt; QColor:\n        \"\"\"Process color definitions referencing a widget attribute/property\n        using the ``@`` syntax.\n\n        Args:\n            color_name: The color name to retrieve.\n            style: The style dictionary.\n            w: The widget to get the color from.\n            default: The default color to use if the attribute is not found.\n\n        Returns:\n            The resolved :class:`QColor`.\n        \"\"\"\n        color = style[color_name]\n        if isinstance(color, str) and color.startswith(\"@\"):\n            color = getattr(w, color[1:], default)\n        return QColor(color)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/style_types.html#client.ayon_core.ui.style_types.StyleData.base_font","title":"<code>base_font</code>  <code>property</code>","text":"<p>Return the base application font, loading it lazily.</p> <p>Returns:</p> Type Description <code>QFont</code> <p>A copy of the configured base <code>QFont</code>.</p>"},{"location":"autoapi/client/ayon_core/ui/style_types.html#client.ayon_core.ui.style_types.StyleData.current_style","title":"<code>current_style()</code>","text":"<p>Return the most recently computed style entry from cache.</p> <p>Returns:</p> Type Description <code>StyleDict</code> <p>The last :class:<code>StyleDict</code> placed into the cache.</p> Source code in <code>client/ayon_core/ui/style_types.py</code> <pre><code>def current_style(self) -&gt; StyleDict:\n    \"\"\"Return the most recently computed style entry from cache.\n\n    Returns:\n        The last :class:`StyleDict` placed into the cache.\n    \"\"\"\n    return self._cache[self.last_key]\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/style_types.html#client.ayon_core.ui.style_types.StyleData.default_variant","title":"<code>default_variant(widget_data)</code>","text":"<p>Return the default variant name for widget_data.</p> <p>Parameters:</p> Name Type Description Default <code>widget_data</code> <code>dict</code> <p>The raw widget data block.</p> required <p>Returns:</p> Type Description <code>str</code> <p>The default variant name string.</p> Source code in <code>client/ayon_core/ui/style_types.py</code> <pre><code>def default_variant(self, widget_data: dict) -&gt; str:\n    \"\"\"Return the default variant name for *widget_data*.\n\n    Args:\n        widget_data: The raw widget data block.\n\n    Returns:\n        The default variant name string.\n    \"\"\"\n    variants = widget_data.get(\"variants\", {})\n    return widget_data.get(\n        \"default-variant\", next(iter(variants.keys()), \"default\")\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/style_types.html#client.ayon_core.ui.style_types.StyleData.dump_cache_stats","title":"<code>dump_cache_stats()</code>","text":"<p>Print cache statistics to stdout (debug aid).</p> Source code in <code>client/ayon_core/ui/style_types.py</code> <pre><code>def dump_cache_stats(self) -&gt; None:\n    \"\"\"Print cache statistics to stdout (debug aid).\"\"\"\n    print(f\"[StyleData] cached {len(self._cache)} styles.\")\n    print(f\"[StyleData]   &gt;&gt; {list(self._cache.keys())}\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/style_types.html#client.ayon_core.ui.style_types.StyleData.get_style","title":"<code>get_style(widget_cls, variant=None, state='base')</code>","text":"<p>Return a style dict for a widget, variant and state.</p> <p>Parameters:</p> Name Type Description Default <code>widget_cls</code> <code>str</code> <p>The widget class key.</p> required <code>variant</code> <code>str | None</code> <p>Optional variant name.</p> <code>None</code> <code>state</code> <code>str</code> <p>State to retrieve; <code>\"all\"</code> returns every state merged.</p> <code>'base'</code> <p>Returns:</p> Name Type Description <code>A</code> <code>StyleDict</code> <p>class:<code>StyleDict</code> for the requested combination.</p> Source code in <code>client/ayon_core/ui/style_types.py</code> <pre><code>def get_style(\n    self,\n    widget_cls: str,\n    variant: str | None = None,\n    state: str = \"base\",\n) -&gt; StyleDict:\n    \"\"\"Return a style dict for a widget, variant and state.\n\n    Args:\n        widget_cls: The widget class key.\n        variant: Optional variant name.\n        state: State to retrieve; ``\"all\"`` returns every state merged.\n\n    Returns:\n        A :class:`StyleDict` for the requested combination.\n    \"\"\"\n    try:\n        return StyleDict(self._cache[f\"{widget_cls}-{variant}-{state}\"])\n    except KeyError:\n        pass\n\n    data = self.widget_data(widget_cls)\n    vrt = self.validate_variant(data, variant)\n    dvrt = self.default_variant(data)\n    pal = self._palette\n    d = copy.copy(self.data[\"global\"])\n    d.update(copy.deepcopy(data.get(\"variants\", {}).get(dvrt, {})))\n    d.update(copy.deepcopy(data.get(\"variants\", {}).get(vrt, {})))\n\n    if state == \"all\":\n        for key, val in d.items():\n            if isinstance(val, dict):\n                d[key] = {kk: pal.get(vv, vv) for kk, vv in val.items()}\n            elif not isinstance(val, list):\n                d[key] = pal.get(val, val)\n    else:\n        # Override palette variables with the current state's values and\n        # remove all states. That way, we can directly use\n        # \"background-color\" without checking the widget's state.\n        state_dict = {}\n        for key, val in list(d.items()):\n            if isinstance(val, dict):\n                if key == state:\n                    state_dict = {\n                        kk: pal.get(vv, vv) for kk, vv in val.items()\n                    }\n                d.pop(key)\n            elif not isinstance(val, list):\n                d[key] = pal.get(val, val)\n        # apply current state overrides last to ensure they take precedence\n        # over the base variant\n        d.update(state_dict)\n\n    # cache result\n    d = StyleDict(d)\n    self.last_key = f\"{widget_cls}-{variant}-{state}\"\n    self._cache[self.last_key] = d\n    return StyleDict(d)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/style_types.html#client.ayon_core.ui.style_types.StyleData.get_style_palette","title":"<code>get_style_palette(widget, widget_key)</code>","text":"<p>Get a QPalette for the widget based on the current style data.</p> <p>Resolves any @ references in the style data to widget attributes. Caches results for efficiency, if there are no @ references.</p> <p>Parameters:</p> Name Type Description Default <code>widget</code> <code>QWidget</code> <p>The widget to build the palette for.</p> required <code>widget_key</code> <code>str</code> <p>The style key identifying the widget class.</p> required <p>Returns:</p> Type Description <code>QPalette</code> <p>A <code>QPalette</code> configured for the widget.</p> Source code in <code>client/ayon_core/ui/style_types.py</code> <pre><code>def get_style_palette(self, widget: QWidget, widget_key: str) -&gt; QPalette:\n    \"\"\"Get a QPalette for the widget based on the current style data.\n\n    Resolves any @ references in the style data to widget attributes.\n    Caches results for efficiency, if there are no @ references.\n\n    Args:\n        widget: The widget to build the palette for.\n        widget_key: The style key identifying the widget class.\n\n    Returns:\n        A ``QPalette`` configured for the widget.\n    \"\"\"\n    variant = getattr(widget, \"_variant_str\", \"default\")\n    # check if cached\n    cache_key = f\"{widget_key}-{variant}-palette\"\n    if cache_key in self._cache:\n        return QPalette(self._cache[cache_key])\n\n    style: StyleDict = self.get_styles(\n        widget_key,\n        variant=variant,\n    )\n    style.set_context(widget)\n\n    p = QPalette(self.base_palette)\n\n    fg_color = style.get(\"base\", {}).get(\"color\")\n    if fg_color:\n        p.setColor(\n            widget.foregroundRole(),\n            QColor(fg_color),\n        )\n    opacity = style.get(\"disabled\", {}).get(\"opacity\")\n    if opacity:\n        disabled_fg_color = QColor(fg_color)\n        disabled_fg_color.setAlphaF(opacity)\n        p.setColor(\n            QPalette.ColorGroup.Disabled,\n            widget.foregroundRole(),\n            disabled_fg_color,\n        )\n\n    bg_color = style.get(\"base\", {}).get(\"background-color\")\n    if bg_color:\n        p.setColor(\n            widget.backgroundRole(),\n            QColor(bg_color),\n        )\n\n    # cache result\n    style.set_context(None)\n    self._cache[cache_key] = p\n\n    return QPalette(p)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/style_types.html#client.ayon_core.ui.style_types.StyleData.get_styles","title":"<code>get_styles(widget_cls, variant=None, states=None)</code>","text":"<p>Return styles for a widget, variant and multiple states at once.</p> <p>This is more efficient than calling :meth:<code>get_style</code> multiple times when you need styles for several states of the same widget/variant.</p> <p>Parameters:</p> Name Type Description Default <code>widget_cls</code> <code>str</code> <p>The widget class name, e.g. <code>\"QStyledItemDelegate\"</code>.</p> required <code>variant</code> <code>str | None</code> <p>The variant name (e.g., <code>\"default\"</code>). Defaults to None.</p> <code>None</code> <code>states</code> <code>list[str] | None</code> <p>List of states to retrieve (e.g., <code>[\"base\", \"hover\", \"checked\"]</code>). Defaults to all defined states.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>A</code> <code>StyleDict</code> <p>class:<code>StyleDict</code> mapping state names to their style dicts.</p> Source code in <code>client/ayon_core/ui/style_types.py</code> <pre><code>def get_styles(\n    self,\n    widget_cls: str,\n    variant: str | None = None,\n    states: list[str] | None = None,\n) -&gt; StyleDict:\n    \"\"\"Return styles for a widget, variant and multiple states at once.\n\n    This is more efficient than calling :meth:`get_style` multiple times\n    when you need styles for several states of the same widget/variant.\n\n    Args:\n        widget_cls: The widget class name, e.g. ``\"QStyledItemDelegate\"``.\n        variant: The variant name (e.g., ``\"default\"``). Defaults to None.\n        states: List of states to retrieve (e.g., ``[\"base\", \"hover\",\n            \"checked\"]``). Defaults to all defined states.\n\n    Returns:\n        A :class:`StyleDict` mapping state names to their style dicts.\n    \"\"\"\n    if states is None:\n        states = self.widget_states(widget_cls)\n\n    cache_key = f\"all-{widget_cls}-{variant}-{'|'.join(states)}\"\n    try:\n        return StyleDict(self._cache[cache_key])\n    except KeyError:\n        pass\n\n    d = {\n        state: self.get_style(widget_cls, variant, state)\n        for state in states\n    }\n    self._cache[cache_key] = StyleDict(d)\n    return StyleDict(d)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/style_types.html#client.ayon_core.ui.style_types.StyleData.get_widget_color","title":"<code>get_widget_color(color_name, style, w, default)</code>","text":"<p>Process color definitions referencing a widget attribute/property using the <code>@</code> syntax.</p> <p>Parameters:</p> Name Type Description Default <code>color_name</code> <code>str</code> <p>The color name to retrieve.</p> required <code>style</code> <code>dict</code> <p>The style dictionary.</p> required <code>w</code> <code>QWidget</code> <p>The widget to get the color from.</p> required <code>default</code> <code>str | QColor</code> <p>The default color to use if the attribute is not found.</p> required <p>Returns:</p> Type Description <code>QColor</code> <p>The resolved :class:<code>QColor</code>.</p> Source code in <code>client/ayon_core/ui/style_types.py</code> <pre><code>def get_widget_color(\n    self,\n    color_name: str,\n    style: dict,\n    w: QWidget,\n    default: str | QColor,\n) -&gt; QColor:\n    \"\"\"Process color definitions referencing a widget attribute/property\n    using the ``@`` syntax.\n\n    Args:\n        color_name: The color name to retrieve.\n        style: The style dictionary.\n        w: The widget to get the color from.\n        default: The default color to use if the attribute is not found.\n\n    Returns:\n        The resolved :class:`QColor`.\n    \"\"\"\n    color = style[color_name]\n    if isinstance(color, str) and color.startswith(\"@\"):\n        color = getattr(w, color[1:], default)\n    return QColor(color)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/style_types.html#client.ayon_core.ui.style_types.StyleData.palette","title":"<code>palette()</code>","text":"<p>Return the resolved colour palette mapping.</p> <p>Returns:</p> Type Description <code>dict</code> <p>Dict mapping palette key strings to HTML hex colours.</p> Source code in <code>client/ayon_core/ui/style_types.py</code> <pre><code>def palette(self) -&gt; dict:\n    \"\"\"Return the resolved colour palette mapping.\n\n    Returns:\n        Dict mapping palette key strings to HTML hex colours.\n    \"\"\"\n    return self._palette\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/style_types.html#client.ayon_core.ui.style_types.StyleData.validate_variant","title":"<code>validate_variant(widget_data, variant)</code>","text":"<p>Return variant if valid, otherwise return the default variant.</p> <p>Parameters:</p> Name Type Description Default <code>widget_data</code> <code>dict</code> <p>The raw widget data block.</p> required <code>variant</code> <code>str</code> <p>The requested variant name.</p> required <p>Returns:</p> Type Description <code>str</code> <p>A valid variant name string.</p> Source code in <code>client/ayon_core/ui/style_types.py</code> <pre><code>def validate_variant(self, widget_data: dict, variant: str) -&gt; str:\n    \"\"\"Return *variant* if valid, otherwise return the default variant.\n\n    Args:\n        widget_data: The raw widget data block.\n        variant: The requested variant name.\n\n    Returns:\n        A valid variant name string.\n    \"\"\"\n    if variant not in widget_data.get(\"variants\", {}):\n        return self.default_variant(widget_data)\n    return variant\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/style_types.html#client.ayon_core.ui.style_types.StyleData.widget_data","title":"<code>widget_data(widget_cls)</code>","text":"<p>Return the raw widget data block for widget_cls.</p> <p>Parameters:</p> Name Type Description Default <code>widget_cls</code> <code>str</code> <p>The widget class key.</p> required <p>Returns:</p> Type Description <code>dict</code> <p>The raw data dict (empty dict if not found).</p> Source code in <code>client/ayon_core/ui/style_types.py</code> <pre><code>def widget_data(self, widget_cls: str) -&gt; dict:\n    \"\"\"Return the raw widget data block for *widget_cls*.\n\n    Args:\n        widget_cls: The widget class key.\n\n    Returns:\n        The raw data dict (empty dict if not found).\n    \"\"\"\n    return self.data[\"widgets\"].get(widget_cls, {})\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/style_types.html#client.ayon_core.ui.style_types.StyleData.widget_list","title":"<code>widget_list()</code>","text":"<p>Return all registered widget class keys.</p> <p>Returns:</p> Type Description <code>list[str]</code> <p>List of widget class key strings.</p> Source code in <code>client/ayon_core/ui/style_types.py</code> <pre><code>def widget_list(self) -&gt; list[str]:\n    \"\"\"Return all registered widget class keys.\n\n    Returns:\n        List of widget class key strings.\n    \"\"\"\n    return list(self.data[\"widgets\"].keys())\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/style_types.html#client.ayon_core.ui.style_types.StyleData.widget_states","title":"<code>widget_states(widget_cls)</code>","text":"<p>Return the list of states for widget_cls, always including <code>\"base\"</code> as the first entry.</p> <p>Parameters:</p> Name Type Description Default <code>widget_cls</code> <code>str</code> <p>The widget class key.</p> required <p>Returns:</p> Type Description <code>list[str]</code> <p>Ordered list of state name strings.</p> Source code in <code>client/ayon_core/ui/style_types.py</code> <pre><code>def widget_states(self, widget_cls: str) -&gt; list[str]:\n    \"\"\"Return the list of states for *widget_cls*, always including\n    ``\"base\"`` as the first entry.\n\n    Args:\n        widget_cls: The widget class key.\n\n    Returns:\n        Ordered list of state name strings.\n    \"\"\"\n    states = list(\n        self.data[\"widgets\"].get(widget_cls, {}).get(\"states\", [])\n    )\n    return states if \"base\" in states else [\"base\"] + states\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/style_types.html#client.ayon_core.ui.style_types.StyleData.widget_variants","title":"<code>widget_variants(widget_cls)</code>","text":"<p>Return all variant names registered for widget_cls.</p> <p>Parameters:</p> Name Type Description Default <code>widget_cls</code> <code>str</code> <p>The widget class key (e.g. <code>\"QPushButton\"</code>).</p> required <p>Returns:</p> Type Description <code>list[str]</code> <p>A list of variant name strings.</p> Source code in <code>client/ayon_core/ui/style_types.py</code> <pre><code>def widget_variants(self, widget_cls: str) -&gt; list[str]:\n    \"\"\"Return all variant names registered for *widget_cls*.\n\n    Args:\n        widget_cls: The widget class key (e.g. ``\"QPushButton\"``).\n\n    Returns:\n        A list of variant name strings.\n    \"\"\"\n    return list(\n        self.data[\"widgets\"].get(widget_cls, {}).get(\"variants\", {}).keys()\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/style_types.html#client.ayon_core.ui.style_types.StyleDict","title":"<code>StyleDict</code>","text":"<p>               Bases: <code>dict</code></p> <p>A dict where string values starting with <code>'@'</code> are resolved via :func:<code>getattr</code> on a bound context object.</p> Example <p>ctx = SomeWidget(...) d = StyleDict({\"color\": \"@fg_color\"}, _context=ctx) d[\"color\"]  # -&gt; ctx.fg_color</p> Source code in <code>client/ayon_core/ui/style_types.py</code> <pre><code>class StyleDict(dict):\n    \"\"\"A dict where string values starting with ``'@'`` are resolved\n    via :func:`getattr` on a bound context object.\n\n    Example:\n        ctx = SomeWidget(...)\n        d = StyleDict({\"color\": \"@fg_color\"}, _context=ctx)\n        d[\"color\"]  # -&gt; ctx.fg_color\n    \"\"\"\n\n    _SENTINEL = object()\n\n    def __init__(\n        self,\n        *args: Any,\n        _context: Any = None,\n        deepcopy: bool = True,\n        **kwargs: Any,\n    ) -&gt; None:\n        if deepcopy:\n            args = copy.deepcopy(args)\n            kwargs = copy.deepcopy(kwargs)\n        super().__init__(*args, **kwargs)\n        # store as attribute on the instance \u2014 not as a dict key\n        object.__setattr__(self, \"_context\", _context)\n        # convert nested dicts to StyleDicts with the same context\n        for k, v in self.items():\n            super().__setitem__(\n                k,\n                StyleDict(v, _context=_context) if isinstance(v, dict) else v,\n            )\n\n    def set_context(self, _context: Any) -&gt; None:\n        \"\"\"Bind a new context object for ``@attr`` resolution.\n\n        Args:\n            _context: The new context; pass ``None`` to unbind.\n        \"\"\"\n        object.__setattr__(self, \"_context\", _context)\n        for v in self.values():\n            if isinstance(v, StyleDict):\n                v.set_context(_context)\n\n    def __getitem__(self, key: str) -&gt; Any:\n        value = super().__getitem__(key)\n        return self._resolve(value)\n\n    def get(self, key: str, default: Any = None, raw: bool = False) -&gt; Any:\n        \"\"\"Return the value for *key*, resolving ``@attr`` references.\n\n        Args:\n            key: Dict key to look up.\n            default: Value to return when *key* is absent.\n            raw: If ``True``, skip ``@attr`` resolution.\n\n        Returns:\n            The resolved (or raw) value, or *default*.\n        \"\"\"\n        value = super().get(key, self._SENTINEL)\n        if value is self._SENTINEL:\n            return default\n        if raw:\n            return value\n        return self._resolve(value)\n\n    def _resolve(self, value: Any) -&gt; Any:\n        if (\n            isinstance(value, str)\n            and value.startswith(\"@\")\n            and self._context is not None\n        ):\n            return getattr(self._context, value[1:], value)\n        return value\n\n    def __repr__(self) -&gt; str:\n        return f\"StyleDict({super().__repr__()}, context={self._context!r})\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/style_types.html#client.ayon_core.ui.style_types.StyleDict.get","title":"<code>get(key, default=None, raw=False)</code>","text":"<p>Return the value for key, resolving <code>@attr</code> references.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>Dict key to look up.</p> required <code>default</code> <code>Any</code> <p>Value to return when key is absent.</p> <code>None</code> <code>raw</code> <code>bool</code> <p>If <code>True</code>, skip <code>@attr</code> resolution.</p> <code>False</code> <p>Returns:</p> Type Description <code>Any</code> <p>The resolved (or raw) value, or default.</p> Source code in <code>client/ayon_core/ui/style_types.py</code> <pre><code>def get(self, key: str, default: Any = None, raw: bool = False) -&gt; Any:\n    \"\"\"Return the value for *key*, resolving ``@attr`` references.\n\n    Args:\n        key: Dict key to look up.\n        default: Value to return when *key* is absent.\n        raw: If ``True``, skip ``@attr`` resolution.\n\n    Returns:\n        The resolved (or raw) value, or *default*.\n    \"\"\"\n    value = super().get(key, self._SENTINEL)\n    if value is self._SENTINEL:\n        return default\n    if raw:\n        return value\n    return self._resolve(value)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/style_types.html#client.ayon_core.ui.style_types.StyleDict.set_context","title":"<code>set_context(_context)</code>","text":"<p>Bind a new context object for <code>@attr</code> resolution.</p> <p>Parameters:</p> Name Type Description Default <code>_context</code> <code>Any</code> <p>The new context; pass <code>None</code> to unbind.</p> required Source code in <code>client/ayon_core/ui/style_types.py</code> <pre><code>def set_context(self, _context: Any) -&gt; None:\n    \"\"\"Bind a new context object for ``@attr`` resolution.\n\n    Args:\n        _context: The new context; pass ``None`` to unbind.\n    \"\"\"\n    object.__setattr__(self, \"_context\", _context)\n    for v in self.values():\n        if isinstance(v, StyleDict):\n            v.set_context(_context)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/style_types.html#client.ayon_core.ui.style_types.get_ayon_style","title":"<code>get_ayon_style()</code>","text":"<p>Return the process-wide :class:<code>~ayon_ui_qt.style.AYONStyle</code> singleton.</p> <p>The <code>AYONStyle</code> class is imported lazily on the first call to avoid a circular import at module load time.</p> <p>Returns:</p> Type Description <code>AYONStyle</code> <p>The singleton <code>AYONStyle</code> instance.</p> Source code in <code>client/ayon_core/ui/style_types.py</code> <pre><code>def get_ayon_style() -&gt; AYONStyle:\n    \"\"\"Return the process-wide :class:`~ayon_ui_qt.style.AYONStyle` singleton.\n\n    The ``AYONStyle`` class is imported lazily on the first call to avoid\n    a circular import at module load time.\n\n    Returns:\n        The singleton ``AYONStyle`` instance.\n    \"\"\"\n    if _LocalContext.ayon_style_instance is not None:\n        return _LocalContext.ayon_style_instance\n\n    from .style import AYONStyle\n\n    instance = AYONStyle()\n    _LocalContext.ayon_style_instance = instance\n    return instance\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/style_types.html#client.ayon_core.ui.style_types.get_ayon_style_data","title":"<code>get_ayon_style_data(widget_cls, variant=None)</code>","text":"<p>Return style data for widget_cls / variant (all states merged).</p> <p>Parameters:</p> Name Type Description Default <code>widget_cls</code> <code>str</code> <p>The widget class key (e.g. <code>\"QPushButton\"</code>).</p> required <code>variant</code> <code>str | None</code> <p>Optional variant name.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>A</code> <code>StyleDict</code> <p>class:<code>StyleDict</code> with all states merged.</p> Source code in <code>client/ayon_core/ui/style_types.py</code> <pre><code>def get_ayon_style_data(\n    widget_cls: str, variant: str | None = None\n) -&gt; StyleDict:\n    \"\"\"Return style data for *widget_cls* / *variant* (all states merged).\n\n    Args:\n        widget_cls: The widget class key (e.g. ``\"QPushButton\"``).\n        variant: Optional variant name.\n\n    Returns:\n        A :class:`StyleDict` with all states merged.\n    \"\"\"\n    return get_ayon_style().model.get_style(\n        widget_cls, variant=variant, state=\"all\"\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/style_types.html#client.ayon_core.ui.style_types.hsl_to_html_color","title":"<code>hsl_to_html_color(hsl)</code>","text":"<p>Convert a CSS <code>hsl(h, s%, l%)</code> string to an HTML hex colour.</p> <p>Parameters:</p> Name Type Description Default <code>hsl</code> <code>str</code> <p>A CSS colour string such as <code>\"hsl(210, 50%, 40%)\"</code>.</p> required <p>Returns:</p> Type Description <code>str</code> <p>The corresponding HTML hex colour string (e.g. <code>\"#336699\"</code>).</p> Source code in <code>client/ayon_core/ui/style_types.py</code> <pre><code>def hsl_to_html_color(hsl: str) -&gt; str:\n    \"\"\"Convert a CSS ``hsl(h, s%, l%)`` string to an HTML hex colour.\n\n    Args:\n        hsl: A CSS colour string such as ``\"hsl(210, 50%, 40%)\"``.\n\n    Returns:\n        The corresponding HTML hex colour string (e.g. ``\"#336699\"``).\n    \"\"\"\n    vals = hsl[4:-1].split(\", \")\n    hue = int(vals[0]) / 360.0\n    sat = int(vals[1][:-1]) / 100.0\n    lum = int(vals[2][:-1]) / 100.0\n    return QColor.fromHslF(hue, sat, lum).name()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/utils.html","title":"utils","text":""},{"location":"autoapi/client/ayon_core/ui/utils.html#client.ayon_core.ui.utils.clear_layout","title":"<code>clear_layout(layout)</code>","text":"<p>Recursively deletes child QWidgets in the initial QLayout and in sub-layouts.</p> <p>This function handles all types of QLayout subclasses (QVBoxLayout, QHBoxLayout, QGridLayout, etc.) and safely deletes all child widgets while maintaining the integrity of the parent layout structure.</p> <p>Parameters:</p> Name Type Description Default <code>layout</code> <p>QLayout instance to clear</p> required Source code in <code>client/ayon_core/ui/utils.py</code> <pre><code>def clear_layout(layout):\n    \"\"\"Recursively deletes child QWidgets in the initial QLayout and in\n    sub-layouts.\n\n    This function handles all types of QLayout subclasses (QVBoxLayout,\n    QHBoxLayout, QGridLayout, etc.) and safely deletes all child widgets\n    while maintaining the integrity of the parent layout structure.\n\n    Args:\n        layout: QLayout instance to clear\n    \"\"\"\n    if layout is None:\n        return\n\n    # print(f\"layout = {layout}\")\n\n    # Work backwards through the layout to avoid index issues when removing\n    # items\n    for i in reversed(range(layout.count())):\n        item = layout.takeAt(i)\n        if item is None:\n            continue\n\n        widget = item.widget()\n        sub_layout = item.layout()\n\n        if widget:\n            # Recursively clear any layouts this widget might have\n            # (in case it's a container widget with its own layouts)\n            if hasattr(widget, \"layout\") and widget.layout():\n                clear_layout(widget.layout())\n            # Delete the widget\n            widget.setParent(None)\n            widget.deleteLater()\n        elif sub_layout:\n            # Recursively clear the sub-layout\n            clear_layout(sub_layout)\n            # Delete the layout\n            sub_layout.deleteLater()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/utils.html#client.ayon_core.ui.utils.process_activity_data","title":"<code>process_activity_data(activity_data, project_data)</code>","text":"<p>Preprocesses payload data to extract and parse comment activities.</p> <p>This function processes the input data to extract activities, specifically focusing on comment activities. It parses these activities using the AYComment class and returns a list of parsed comment objects.</p> <p>Parameters:</p> Name Type Description Default <code>activity_data</code> <code>dict</code> <p>The input data containing project activities in a specific structure. Expected to have a nested structure with 'project' -&gt; 'activities'.</p> required <p>Returns:</p> Name Type Description <code>list</code> <code>list[CommentModel | VersionPublishModel | StatusChangeModel]</code> <p>A list of parsed comment objects. Returns an empty list if no activities are found or if there's an error during processing.</p> Source code in <code>client/ayon_core/ui/utils.py</code> <pre><code>def process_activity_data(\n    activity_data: dict, project_data: ProjectData\n) -&gt; list[CommentModel | VersionPublishModel | StatusChangeModel]:\n    \"\"\"Preprocesses payload data to extract and parse comment activities.\n\n    This function processes the input data to extract activities, specifically\n    focusing on comment activities. It parses these activities using the\n    AYComment class and returns a list of parsed comment objects.\n\n    Args:\n        activity_data (dict): The input data containing project activities in\n            a specific structure. Expected to have a nested structure with\n            'project' -&gt; 'activities'.\n        project_data (ProjectData)\n\n    Returns:\n        list: A list of parsed comment objects. Returns an empty list if\n            no activities are found or if there's an error during processing.\n    \"\"\"\n    try:\n        activities: list[dict] = activity_data[\"project\"][\"activities\"]\n    except KeyError as err:\n        logger.error(f\"Could not extract activities: {err}\")\n        return []\n\n    users = {d.short_name: d for d in project_data.users}\n    category_colors = {c.name: c.color for c in project_data.comment_category}\n\n    ui_data = []\n    nothing = \"n/a\"\n    for act in activities:\n        act_data = act.get(\"activityData\", {})\n        if isinstance(act_data, str):\n            act_data = json.loads(act_data)\n            act[\"activityData\"] = act_data\n\n        activity_type = act.get(\"activityType\", \"\")\n        activity_id = act[\"activityId\"]\n\n        user_name = act.get(\"author\", {}).get(\"name\", nothing)\n        user_full_name = user_name\n        user = users.get(user_name)\n        if user:\n            user_full_name = user.full_name\n\n        date = act.get(\"updatedAt\", nothing)\n\n        if activity_type == \"comment\":\n            annotation_models, ranges = _parse_annotations(act_data, nothing)\n            file_models = _parse_files(act, ranges, nothing)\n            category = act_data.get(\"category\", \"\")\n            ui_data.append(\n                CommentModel(\n                    activity_id=activity_id,\n                    user_full_name=user_full_name,\n                    user_name=user_name,\n                    comment=act.get(\"body\", nothing),\n                    comment_date=date,\n                    category=category,\n                    category_color=category_colors.get(category, \"\"),\n                    files=file_models,\n                    annotations=annotation_models,\n                )\n            )\n        elif activity_type == \"version.publish\":\n            ui_data.append(\n                VersionPublishModel(\n                    activity_id=activity_id,\n                    user_full_name=user_full_name,\n                    user_name=user_name,\n                    version=str(\n                        act_data.get(\"origin\", {}).get(\"name\", nothing)\n                    ),\n                    product=str(\n                        act_data.get(\"context\", {}).get(\"productName\", nothing)\n                    ),\n                    date=date,\n                )\n            )\n        elif activity_type == \"status.change\":\n            version = act_data.get(\"origin\", {}).get(\"name\", nothing)\n            parents: list[dict] = act_data.get(\"parents\", {})\n            product = next(\n                (p[\"name\"] for p in parents if p[\"type\"] == \"product\"),\n                nothing,\n            )\n            ui_data.append(\n                StatusChangeModel(\n                    activity_id=activity_id,\n                    user_full_name=user_full_name,\n                    user_name=user_name,\n                    product=product,\n                    version=version,\n                    old_status=str(act_data.get(\"oldValue\", nothing)),\n                    new_status=str(act_data.get(\"newValue\", nothing)),\n                    date=date,\n                )\n            )\n\n    return ui_data\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/variants.html","title":"variants","text":""},{"location":"autoapi/client/ayon_core/ui/variants.html#client.ayon_core.ui.variants.check_variants_sync","title":"<code>check_variants_sync()</code>","text":"<p>Check if the variants.py file is in sync with the style data.</p> Source code in <code>client/ayon_core/ui/variants.py</code> <pre><code>def check_variants_sync():\n    \"\"\"Check if the variants.py file is in sync with the style data.\"\"\"\n    from .style_types import get_ayon_style\n\n    # Runtime sync assertion at import\n    style_data = get_ayon_style().model\n    enum_desync = False\n    report = \"\"\n\n    for widget in style_data.widget_list():\n        json_variants = style_data.widget_variants(widget)\n        if not json_variants:\n            continue\n\n        enum_name = f\"{widget}Variants\"\n\n        # check if the enum exists for that widget class\n        if enum_name not in globals():\n            report += f\"    Missing enum for {widget}\\n\"\n            enum_desync = True\n            continue\n\n        # check if the enum values match the style data\n        enum = globals()[enum_name]\n        if set(json_variants) != {v.value for v in enum}:\n            report += (\n                f\"    Desync for {widget}: {json_variants} != {enum}\\n\"\n            )\n            enum_desync = True\n\n    # print the report\n    print(\"-\" * 80)\n    if enum_desync:\n        update_variants_file(style_data)\n        print(\"Variants are not in sync with the style data.\\n\")\n        print(f\"VARIANTS ANALYSIS {'_' * 62}\\n\" + report)\n    else:\n        print(\"Variants are in sync with the style data.\")\n    print(\"-\" * 80)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/variants.html#client.ayon_core.ui.variants.update_variants_file","title":"<code>update_variants_file(style_data)</code>","text":"<p>Update the variants.py file with the current style data.</p> Source code in <code>client/ayon_core/ui/variants.py</code> <pre><code>def update_variants_file(style_data):\n    \"\"\"Update the variants.py file with the current style data.\"\"\"\n    import os\n    import inspect\n    import re\n\n    # get the path of the current file\n    current_file = inspect.getfile(inspect.currentframe())\n    current_dir = os.path.dirname(current_file)\n\n    # load the current variants.py file\n    with open(os.path.join(current_dir, \"variants.py\"), \"r\") as f:\n        current = f.read()\n\n    # get the start and end of the variants definitions\n    start = current.find(\"# VARIANTS DEFINITIONS\")\n    end = current.find(\"# END OF VARIANTs DEFINITIONS\")\n    if start == -1 or end == -1:\n        raise RuntimeError(\n            \"Could not find variants definitions in variants.py\"\n        )\n    # adjust start to the end of the line\n    start = current.find(\"\\n\", start) + 1\n\n    # get the part before and after the variants definitions\n    before = current[:start]\n    after = current[end:]\n\n    # generate all variants enums from style data\n    defs = []\n    for widget in style_data.widget_list():\n        json_variants = style_data.widget_variants(widget)\n        if not json_variants:\n            continue\n\n        defs.append(f\"class {widget}Variants(Enum):\\n\")\n        for variant in json_variants:\n            # remove spaces and special characters\n            field_name = re.sub(r\"[^a-zA-Z0-9]\", \"_\", variant).title()\n            # avoid empty strings\n            if variant == \"\":\n                field_name = \"Default\"\n            defs.append(f'    {field_name} = \"{variant}\"\\n')\n        defs.append(\"\\n\\n\")\n\n    new = before + \"\\n\\n\" + \"\".join(defs) + after\n    # print(\"-\" * 80 + \"\\n\" + new + \"\\n\" + \"-\" * 80 + \"\\n\")\n\n    # write the new variants.py file\n    with open(os.path.join(current_dir, \"variants.py\"), \"w\") as f:\n        f.write(new)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/index.html","title":"components","text":"<p>AYON UI Qt components package.</p> <p>This package provides reusable Qt widgets styled according to the AYON design system.</p>"},{"location":"autoapi/client/ayon_core/ui/components/index.html#client.ayon_core.ui.components.AYButton","title":"<code>AYButton</code>","text":"<p>               Bases: <code>StyleMixin</code>, <code>QPushButton</code></p> Source code in <code>client/ayon_core/ui/components/buttons.py</code> <pre><code>class AYButton(StyleMixin, QtWidgets.QPushButton):\n    Variants = QPushButtonVariants\n\n    def __init__(\n        self,\n        *args,\n        variant: Variants = Variants.Surface,\n        icon: str | None = None,\n        icon_on: str | None = None,\n        icon_size: int = 16,\n        icon_color: str | None = None,\n        icon_fill: bool = False,\n        checkable: bool = False,\n        tooltip: str = \"\",\n        name_id: str = \"\",\n        contrast_color: QColor | None = None,\n        label_alignment: Qt.AlignmentFlag | None = None,\n        fixed_width: bool | None = None,\n        **kwargs,\n    ):\n        # style params\n        self._variant_str: str = variant.value\n        self._style_data = StyleDict()\n\n        # widget params\n        self._icon_size = icon_size\n        self._tooltip = tooltip\n        self._icon = icon\n        self._icon_on = icon_on or icon\n        self._icon_fill = icon_fill\n        self._contrast_color = contrast_color\n\n        super().__init__(*args, **kwargs)\n        self.setCheckable(checkable)\n\n        self._style = get_ayon_style()\n        self._style_data = get_ayon_style_data(\"QPushButton\", variant.value)\n        self._style_data.set_context(self)\n\n        # Determine the icon color\n        color_str = icon_color or self._style_data.get(\"color\", \"#ffffff\")\n        self._icon_color = QColor(color_str)\n        # Adjust the icon color to have enough contrast with the background\n        if isinstance(contrast_color, QColor) and contrast_color.isValid():\n            self._icon_color = compute_color_for_contrast(\n                contrast_color.toTuple(),\n                self._icon_color.toTuple(),\n                min_contrast_ratio=7,\n            )\n\n        # compute a readable icon hover color\n        self._icon_hover_color = self._icon_color\n        icon_hover_bg = self._style_data.get(\"hover\", {}).get(\n            \"background-color\", \"#000000\"\n        )\n        if isinstance(icon_hover_bg, str) and self._icon_color.isValid():\n            self._icon_hover_color = compute_color_for_contrast(\n                QColor(icon_hover_bg).toTuple(),\n                self._icon_color.toTuple(),\n                min_contrast_ratio=7,\n            )\n\n        # get the disabled opacity\n        self._disabled_opacity = self._style_data.get(\"disabled\", {}).get(\n            \"opacity\", 0.5\n        )\n\n        if self._icon:\n            self.set_icon(self._icon)\n\n        if self._tooltip:\n            self.setToolTip(self._tooltip)\n\n        self._label_alignment = label_alignment\n\n        self._name_id = \"\"\n        if name_id:\n            self.setObjectName(name_id)\n            self._name_id = name_id\n\n        use_fixed_width = (\n            (not bool(self.text()))  # only fixed when icon-only\n            if fixed_width is None\n            else fixed_width\n        )\n        if use_fixed_width:\n            self.setSizePolicy(\n                QtWidgets.QSizePolicy.Policy.Fixed,\n                QtWidgets.QSizePolicy.Policy.Fixed,\n            )\n        else:\n            self.setSizePolicy(\n                QtWidgets.QSizePolicy.Policy.Preferred,\n                QtWidgets.QSizePolicy.Policy.Fixed,\n            )\n\n        # self._style.style_widget(self)\n        self.setStyle(get_ayon_style())\n\n    @property\n    def contrast_color(self):\n        return self._contrast_color\n\n    def _compute_contrast_text_color(\n        self,\n        bg_color: QColor | str | None,\n        fg_color: QColor,\n    ) -&gt; QColor:\n        \"\"\"Compute text color with sufficient contrast against background.\"\"\"\n        if not bg_color:\n            return fg_color\n        qbg = QColor(bg_color) if isinstance(bg_color, str) else bg_color\n        return compute_color_for_contrast(\n            qbg.toTuple(),  # type: ignore\n            fg_color.toTuple(),\n            min_contrast_ratio=7.0,\n        )\n\n    def set_palette(self, palette: QPalette) -&gt; None:\n        self._style_palette = palette\n\n        if self._style_data.get(\"contrast-text\", False):\n            contrast_ref = self._contrast_color or self._icon_color\n            if not contrast_ref:\n                contrast_ref = self.palette().color(self.backgroundRole())\n            txt_color = self._compute_contrast_text_color(\n                contrast_ref,\n                self.palette().color(self.foregroundRole()),\n            )\n            self._style_palette.setColor(self.foregroundRole(), txt_color)\n\n    def initStyleOption(self, option: QtWidgets.QStyleOptionButton) -&gt; None:\n        super().initStyleOption(option)\n        option.iconSize = QtCore.QSize(self._icon_size, self._icon_size)\n\n    def sizeHint(self) -&gt; QtCore.QSize:\n        if self.testAttribute(QtCore.Qt.WidgetAttribute.WA_StyleSheet):\n            option = QtWidgets.QStyleOptionButton()\n            self.initStyleOption(option)\n            return get_ayon_style().sizeFromContents(\n                QtWidgets.QStyle.ContentsType.CT_PushButton,\n                option,\n                self.rect().size(),\n                self,\n            )\n        return super().sizeHint()\n\n    def paintEvent(self, arg__1: QtGui.QPaintEvent) -&gt; None:\n        p = QtGui.QPainter(self)\n        option = QtWidgets.QStyleOptionButton()\n        self.initStyleOption(option)\n        # override rect set by stylesheet\n        size = self.sizeHint()\n        if (\n            self.sizePolicy().horizontalPolicy()\n            == QtWidgets.QSizePolicy.Policy.Fixed\n        ):\n            self.setFixedSize(size)\n            option.rect = QtCore.QRect(0, 0, size.width(), size.height())\n        else:\n            self.setFixedHeight(size.height())  # draw\n        return get_ayon_style().drawControl(\n            QtWidgets.QStyle.ControlElement.CE_PushButton, option, p, self\n        )\n\n    def set_icon(self, icon_name: str):\n        self._icon = icon_name\n        # icon conventions\n        #   State.Off: checkable off\n        #   State.On: checkable on\n        #   State.Active: hover\n        disabled_color = QColor(self._icon_color)\n        disabled_color.setAlphaF(self._disabled_opacity)\n        if self.isCheckable():\n            icn = get_icon(\n                icon_name_off=self._icon,\n                color_off=self._icon_color,\n                icon_name_on=self._icon_on,\n                color_on=self._icon_color,\n                color_on_disabled=disabled_color,\n                color_off_disabled=disabled_color,\n                fill=self._icon_fill,\n            )\n        else:\n            icn = get_icon(\n                icon_name_off=self._icon,\n                color_off=self._icon_color,\n                icon_name_on=self._icon,\n                color_on=self._icon_hover_color,\n                color_on_disabled=disabled_color,\n                color_off_disabled=disabled_color,\n                fill=self._icon_fill,\n            )\n        self.setIcon(icn)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/index.html#client.ayon_core.ui.components.AYCheckBox","title":"<code>AYCheckBox</code>","text":"<p>               Bases: <code>StyleMixin</code>, <code>QCheckBox</code></p> <p>AYON styled checkbox widget.</p> <p>Overrides Qt's stylesheet painting with AYONStyle custom rendering.</p> <p>Parameters:</p> Name Type Description Default <code>*args</code> <p>Positional arguments passed to QCheckBox.</p> <code>()</code> <code>**kwargs</code> <p>Keyword arguments passed to QCheckBox.</p> <code>{}</code> Source code in <code>client/ayon_core/ui/components/check_box.py</code> <pre><code>class AYCheckBox(StyleMixin, QCheckBox):\n    \"\"\"AYON styled checkbox widget.\n\n    Overrides Qt's stylesheet painting with AYONStyle custom rendering.\n\n    Args:\n        *args: Positional arguments passed to QCheckBox.\n        **kwargs: Keyword arguments passed to QCheckBox.\n    \"\"\"\n\n    Variants = QCheckBoxVariants\n\n    def __init__(\n        self,\n        *args,\n        variant: Variants = Variants.Default,\n        **kwargs,\n    ):\n        super().__init__(*args, **kwargs)\n        self._variant_str = variant.value\n        self._style_dict = None\n        self.setStyle(get_ayon_style())\n\n        if variant == AYCheckBox.Variants.Button:\n            self.setFixedSize(self.sizeHint())\n\n    @property\n    def style_dict(self):\n        if self._style_dict is None:\n            self._style_dict = get_ayon_style().model.get_style(\n                \"QCheckBox\", variant=self._variant_str\n            )\n            self._style_dict.set_context(self)\n        return self._style_dict\n\n    def initStyleOption(self, option: QStyleOptionButton) -&gt; None:\n        \"\"\"Initialize the style option with the default implementation, then\n        override any properties needed for our custom painting.\n\n        Args:\n            option: The style option to initialize.\n        \"\"\"\n        super().initStyleOption(option)\n        option.fontMetrics = self.fontMetrics()\n\n    def paintEvent(self, arg__1: QPaintEvent) -&gt; None:\n        \"\"\"Render the checkbox using the AYON custom style.\n\n        Args:\n            arg__1: The paint event delivered by Qt.\n        \"\"\"\n        p = QPainter(self)\n        p.setFont(self.font())\n\n        option = QStyleOptionButton()\n        self.initStyleOption(option)\n        _style = get_ayon_style()\n\n        _expanding = self.sizePolicy().horizontalPolicy() in (\n            QSizePolicy.Policy.Expanding,\n            QSizePolicy.Policy.MinimumExpanding,\n        )\n        if _expanding:\n            ind_w = _style.pixelMetric(\n                QStyle.PixelMetric.PM_IndicatorWidth, option, self\n            )\n            ind_h = _style.pixelMetric(\n                QStyle.PixelMetric.PM_IndicatorHeight, option, self\n            )\n            spacing = _style.pixelMetric(\n                QStyle.PixelMetric.PM_CheckBoxLabelSpacing, option, self\n            )\n            cy = self.height() // 2\n\n            ind_opt = QStyleOptionButton(option)\n            ind_opt.rect = QRect(0, cy - ind_h // 2, ind_w, ind_h)\n            _style.drawPrimitive(\n                QStyle.PrimitiveElement.PE_IndicatorCheckBox, ind_opt, p, self\n            )\n\n            label_opt = QStyleOptionButton(option)\n            label_opt.rect = QRect(\n                ind_w + spacing,\n                0,\n                self.width() - ind_w - spacing,\n                self.height(),\n            )\n            _style.drawControl(\n                QStyle.ControlElement.CE_CheckBoxLabel, label_opt, p, self\n            )\n        else:\n            _style.drawControl(\n                QStyle.ControlElement.CE_CheckBox, option, p, self\n            )\n\n    def sizeHint(self) -&gt; QSize:\n        size = super().sizeHint()\n\n        if self._variant_str == AYCheckBox.Variants.Button.value:\n            h_pad, v_pad = self.style_dict.get(\"padding\", [6, 6])\n            size.setWidth(size.width() + h_pad * 2)\n            size.setHeight(size.height() + v_pad * 2)\n        else:\n            # Recalculate width using the custom style's actual metrics\n            option = QStyleOptionButton()\n            self.initStyleOption(option)\n            _style = get_ayon_style()\n            ind_w = _style.pixelMetric(\n                QStyle.PixelMetric.PM_IndicatorWidth, option, self\n            )\n            spacing = _style.pixelMetric(\n                QStyle.PixelMetric.PM_CheckBoxLabelSpacing, option, self\n            )\n            fm = self.fontMetrics()\n            text_w = fm.horizontalAdvance(self.text())\n            size.setWidth(ind_w + spacing + text_w)\n\n        return size\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/index.html#client.ayon_core.ui.components.AYCheckBox.initStyleOption","title":"<code>initStyleOption(option)</code>","text":"<p>Initialize the style option with the default implementation, then override any properties needed for our custom painting.</p> <p>Parameters:</p> Name Type Description Default <code>option</code> <code>QStyleOptionButton</code> <p>The style option to initialize.</p> required Source code in <code>client/ayon_core/ui/components/check_box.py</code> <pre><code>def initStyleOption(self, option: QStyleOptionButton) -&gt; None:\n    \"\"\"Initialize the style option with the default implementation, then\n    override any properties needed for our custom painting.\n\n    Args:\n        option: The style option to initialize.\n    \"\"\"\n    super().initStyleOption(option)\n    option.fontMetrics = self.fontMetrics()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/index.html#client.ayon_core.ui.components.AYCheckBox.paintEvent","title":"<code>paintEvent(arg__1)</code>","text":"<p>Render the checkbox using the AYON custom style.</p> <p>Parameters:</p> Name Type Description Default <code>arg__1</code> <code>QPaintEvent</code> <p>The paint event delivered by Qt.</p> required Source code in <code>client/ayon_core/ui/components/check_box.py</code> <pre><code>def paintEvent(self, arg__1: QPaintEvent) -&gt; None:\n    \"\"\"Render the checkbox using the AYON custom style.\n\n    Args:\n        arg__1: The paint event delivered by Qt.\n    \"\"\"\n    p = QPainter(self)\n    p.setFont(self.font())\n\n    option = QStyleOptionButton()\n    self.initStyleOption(option)\n    _style = get_ayon_style()\n\n    _expanding = self.sizePolicy().horizontalPolicy() in (\n        QSizePolicy.Policy.Expanding,\n        QSizePolicy.Policy.MinimumExpanding,\n    )\n    if _expanding:\n        ind_w = _style.pixelMetric(\n            QStyle.PixelMetric.PM_IndicatorWidth, option, self\n        )\n        ind_h = _style.pixelMetric(\n            QStyle.PixelMetric.PM_IndicatorHeight, option, self\n        )\n        spacing = _style.pixelMetric(\n            QStyle.PixelMetric.PM_CheckBoxLabelSpacing, option, self\n        )\n        cy = self.height() // 2\n\n        ind_opt = QStyleOptionButton(option)\n        ind_opt.rect = QRect(0, cy - ind_h // 2, ind_w, ind_h)\n        _style.drawPrimitive(\n            QStyle.PrimitiveElement.PE_IndicatorCheckBox, ind_opt, p, self\n        )\n\n        label_opt = QStyleOptionButton(option)\n        label_opt.rect = QRect(\n            ind_w + spacing,\n            0,\n            self.width() - ind_w - spacing,\n            self.height(),\n        )\n        _style.drawControl(\n            QStyle.ControlElement.CE_CheckBoxLabel, label_opt, p, self\n        )\n    else:\n        _style.drawControl(\n            QStyle.ControlElement.CE_CheckBox, option, p, self\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/index.html#client.ayon_core.ui.components.AYComboBox","title":"<code>AYComboBox</code>","text":"<p>               Bases: <code>StyleMixin</code>, <code>QComboBox</code></p> <p>AYON-styled combo-box with icon, short-text, and inverted-colour support.</p> <p>:class:<code>AYComboBox</code> wraps :class:<code>QComboBox</code> and adds:</p> <ul> <li> <p>Three display modes controlled by   :class:<code>~ayon_core.ui.data_models.MenuSize</code>:</p> </li> <li> <p><code>Full</code>  - shows the full item label (default).</p> </li> <li><code>Short</code> - shows the abbreviated <code>ShortTextRole</code> label.</li> <li> <p><code>Icon</code>  - hides the text and shows only the item icon.</p> </li> <li> <p>Inverted colour mode - swaps the icon foreground/background colours   so the icon appears on a coloured pill rather than a neutral background.</p> </li> <li>Placeholder text support for an empty selection state.</li> <li>Custom model support: any model that exposes <code>ShortTextRole</code> and   <code>IconNameRole</code> attributes is accepted. Models without those attributes   are flagged as incompatible; :meth:<code>add_item</code> and :meth:<code>update_items</code>   will raise :exc:<code>RuntimeError</code> when such a model is active.</li> </ul> <p>The default model is :class:<code>AYComboBoxModel</code>.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <code>Optional[QWidget]</code> <p>Optional parent widget.</p> <code>None</code> <code>items</code> <code>List[dict] | None</code> <p>Initial list of item dictionaries.  Each dict may contain:</p> <ul> <li><code>\"text\"</code> (required) - Display label.</li> <li><code>\"color\"</code>      - Hex foreground colour (default <code>\"#ffffff\"</code>).</li> <li><code>\"icon\"</code>       - Material Symbol icon name.</li> <li><code>\"short_text\"</code> - Abbreviated label for short mode   (default <code>\"&lt; UNDEFINED &gt;\"</code>).</li> </ul> <code>None</code> <code>size</code> <code>MenuSize | str</code> <p>Initial display mode.  Accepts a :class:<code>~ayon_core.ui.data_models.MenuSize</code> value or its string equivalent (<code>\"full\"</code>, <code>\"short\"</code>, <code>\"icon\"</code>).</p> <code>Full</code> <code>height</code> <code>int</code> <p>Fixed maximum height in pixels (default <code>30</code>).</p> <code>30</code> <code>placeholder</code> <code>Optional[str]</code> <p>Placeholder string shown when no item is selected.</p> <code>None</code> <code>inverted</code> <code>bool</code> <p>When <code>True</code> the icon foreground and background colours are swapped (default <code>False</code>).</p> <code>False</code> <code>icon_size</code> <code>int</code> <p>Icon size in pixels (default <code>20</code>).</p> <code>20</code> <code>show_chevron</code> <code>bool</code> <p>If False, the dropdown chevron (arrow) will not be drawn in the custom style. Default is False.</p> <code>False</code> <code>**kwargs</code> <p>Additional keyword arguments forwarded to :class:<code>QComboBox</code>.</p> <code>{}</code> Example <p>Basic usage with the built-in status list::</p> <pre><code>combo = AYComboBox(\n    parent=my_widget,\n    items=[...],\n    size=MenuSize.Short,\n    placeholder=\"Select status\u2026\",\n)\ncombo.currentTextChanged.connect(on_status_changed)\n</code></pre> <p>Switching modes at runtime::</p> <pre><code>combo.set_size(\"icon\")\ncombo.set_inverted(True)\n</code></pre> Source code in <code>client/ayon_core/ui/components/combo_box.py</code> <pre><code>class AYComboBox(StyleMixin, QtWidgets.QComboBox):\n    \"\"\"AYON-styled combo-box with icon, short-text, and inverted-colour\n    support.\n\n    :class:`AYComboBox` wraps :class:`QComboBox` and adds:\n\n    - **Three display modes** controlled by\n      :class:`~ayon_core.ui.data_models.MenuSize`:\n\n      - ``Full``  - shows the full item label (default).\n      - ``Short`` - shows the abbreviated ``ShortTextRole`` label.\n      - ``Icon``  - hides the text and shows only the item icon.\n\n    - **Inverted colour mode** - swaps the icon foreground/background colours\n      so the icon appears on a coloured pill rather than a neutral background.\n    - **Placeholder text** support for an empty selection state.\n    - **Custom model** support: any model that exposes ``ShortTextRole`` and\n      ``IconNameRole`` attributes is accepted. Models without those attributes\n      are flagged as incompatible; :meth:`add_item` and :meth:`update_items`\n      will raise :exc:`RuntimeError` when such a model is active.\n\n    The default model is :class:`AYComboBoxModel`.\n\n    Args:\n        parent: Optional parent widget.\n        items: Initial list of item dictionaries.  Each dict may contain:\n\n            - ``\"text\"``       *(required)* - Display label.\n            - ``\"color\"``      - Hex foreground colour (default ``\"#ffffff\"``).\n            - ``\"icon\"``       - Material Symbol icon name.\n            - ``\"short_text\"`` - Abbreviated label for short mode\n              (default ``\"&lt; UNDEFINED &gt;\"``).\n\n        size: Initial display mode.  Accepts a\n            :class:`~ayon_core.ui.data_models.MenuSize` value or its string\n            equivalent (``\"full\"``, ``\"short\"``, ``\"icon\"``).\n        height: Fixed maximum height in pixels (default ``30``).\n        placeholder: Placeholder string shown when no item is selected.\n        inverted: When ``True`` the icon foreground and background colours are\n            swapped (default ``False``).\n        icon_size: Icon size in pixels (default ``20``).\n        show_chevron: If False, the dropdown chevron (arrow) will not be drawn\n            in the custom style. Default is False.\n\n        **kwargs: Additional keyword arguments forwarded to\n            :class:`QComboBox`.\n\n    Example:\n        Basic usage with the built-in status list::\n\n            combo = AYComboBox(\n                parent=my_widget,\n                items=[...],\n                size=MenuSize.Short,\n                placeholder=\"Select status\u2026\",\n            )\n            combo.currentTextChanged.connect(on_status_changed)\n\n        Switching modes at runtime::\n\n            combo.set_size(\"icon\")\n            combo.set_inverted(True)\n    \"\"\"\n\n    Variants = QComboBoxVariants\n\n    def __init__(\n        self,\n        parent: Optional[QtWidgets.QWidget] = None,\n        items: List[dict] | None = None,\n        size: MenuSize | str = MenuSize.Full,\n        height: int = 30,\n        placeholder: Optional[str] = None,\n        inverted: bool = False,\n        icon_size: int = 20,\n        variant: Variants = Variants.Default,\n        show_chevron: bool = False,\n        **kwargs,\n    ) -&gt; None:\n        self._uses_incompatible_model = False\n        super().__init__(parent, **kwargs)\n        self._variant_str = variant.value\n\n        self.setStyle(get_ayon_style())\n        self.setMouseTracking(True)\n        self.setMaximumHeight(height)\n\n        # Initialize properties\n        self._size: MenuSize = (\n            size if isinstance(size, MenuSize) else MenuSize(size)\n        )\n        self._height: int = height\n        self._inverted: bool = inverted\n        self._icon_size: int = icon_size\n        self._inverted_icons: dict[str, QIcon] = {}\n        self.show_chevron: bool = show_chevron\n\n        if placeholder:\n            self.setPlaceholderText(placeholder)\n\n        # setup model\n        model = AYComboBoxModel(self)\n        self.setModel(model)\n\n        self.update_items(items)\n\n    def _assert_compatible_model(self) -&gt; None:\n        \"\"\"Raise :exc:`RuntimeError` when an incompatible model is active.\n\n        Called by :meth:`add_item` and :meth:`update_items` to guard against\n        direct item mutations when a custom model that does not expose\n        ``ShortTextRole`` / ``IconNameRole`` has been set via\n        :meth:`setModel`.\n        \"\"\"\n        if self._uses_incompatible_model:\n            raise RuntimeError(\n                \"Cannot modify items directly when a custom \"\n                \"model is in use. Modify the model instead.\"\n            )\n\n    def setModel(self, model: QtCore.QAbstractItemModel) -&gt; None:\n        \"\"\"Set the item model and detect compatibility with AYComboBox roles.\n\n        If *model* is not an :class:`AYComboBoxModel` instance **and** it\n        does not expose both ``ShortTextRole`` and ``IconNameRole``\n        attributes, it is marked as *incompatible*.  In that state:\n\n        - :meth:`add_item` and :meth:`update_items` will raise\n          :exc:`RuntimeError`.\n        - Short mode falls back to ``\"&lt; INCOMPATIBLE MODEL &gt;\"`` as the\n          displayed text.\n\n        Args:\n            model: The new item model to attach to the combo-box.\n        \"\"\"\n        mtype = type(model)\n        self._uses_incompatible_model = mtype is not AYComboBoxModel and (\n            not hasattr(model, \"IconNameRole\")\n            or not hasattr(model, \"ShortTextRole\")\n        )\n        super().setModel(model)\n\n    def _make_icon(\n        self,\n        fg_color: QColor,\n        bg_color: QColor,\n        icon_name: str | None,\n        inverted: bool = False,\n    ) -&gt; QIcon | None:\n        \"\"\"Assign a Material Symbol icon to *data_item*.\n\n        Reads the item's background and foreground colours and passes them\n        to ``get_icon()``.  When *inverted* mode is active the icon's normal\n        colour uses the **background** colour so the icon appears on a\n        coloured pill; otherwise it uses the foreground colour.\n\n        Does nothing when *icon_name* is ``None`` or an empty string.\n\n        Args:\n            fg_color: Icon color.\n            bg_color: Background color.\n            icon_name: A Material Symbol identifier (e.g. ``\"play_arrow\"``),\n                or ``None`` / ``\"\"`` to skip icon assignment.\n            inverted: When ``True``, the icon's normal colour is taken from\n                the item's **background** colour so it renders as a coloured\n                icon on a neutral background.  Defaults to ``False``.\n        \"\"\"\n        icon = None\n        if icon_name:\n            icon = get_icon(\n                icon_name,\n                color=bg_color if inverted else fg_color,\n                # TODO: add fill support to get_icon and\n                #       pass self._icon_fill here\n            )\n        return icon\n\n    def _get_inverted_icon(self, default: QIcon) -&gt; QIcon:\n        \"\"\"Return the inverted-colour icon for the currently selected item.\n\n        Looks up the ``IconNameRole`` of the current item and returns a\n        cached inverted icon.  If the icon has not been generated yet it is\n        created via :meth:`_make_icon` with ``inverted=True`` and stored in\n        :attr:`_inverted_icons` for future calls.\n\n        Args:\n            default: Fallback icon returned when the current item has no\n                ``IconNameRole`` value.\n\n        Returns:\n            A :class:`QIcon` rendered with inverted colours, or *default*\n            when no icon name is available.\n        \"\"\"\n        idx = self.currentIndex()\n        if idx &lt; 0:\n            return default\n\n        icon_name = self.currentData(self.model().IconNameRole)\n        if not icon_name:\n            return default\n\n        fg = self.currentData(QtCore.Qt.ItemDataRole.ForegroundRole).color()\n        bg = self.currentData(QtCore.Qt.ItemDataRole.BackgroundRole).color()\n        key = f\"{icon_name}:{fg.name()}:{bg.name()}\"\n\n        if key not in self._inverted_icons:\n            self._inverted_icons[key] = (\n                self._make_icon(fg, bg, icon_name, inverted=True) or default\n            )\n\n        return self._inverted_icons[key]\n\n    def add_item(self, item: dict[str, str]):\n        \"\"\"Append a single item to the combo-box model.\n\n        Constructs a :class:`QStandardItem` from *item*, sets its foreground\n        colour, background colour (from the current palette), icon, short\n        text, and icon name, then appends it to the model.\n\n        Raises:\n            RuntimeError: If a custom incompatible model is currently set\n                (see :meth:`setModel`).\n\n        Args:\n            item: A dict with the following keys:\n\n                - ``\"text\"``       *(required)* - Display label.\n                - ``\"color\"``      - Hex foreground colour\n                  (default ``\"#ffffff\"``).\n                - ``\"icon\"``       - Material Symbol icon name.\n                - ``\"short_text\"`` - Abbreviated label stored in\n                  ``ShortTextRole`` (default ``\"&lt; UNDEFINED &gt;\"``).\n        \"\"\"\n        self._assert_compatible_model()\n\n        bg_color = self.palette().color(\n            QPalette.ColorGroup.Active, QPalette.ColorRole.Window\n        )\n        fg_color = QColor(item.get(\"color\", \"#ffffff\"))\n\n        text = item.get(\"text\")\n        if not text:\n            raise ValueError(\n                f\"Item dict must contain a non-empty 'text' key; got: {item!r}\"\n            )\n\n        data_item = QStandardItem(text)\n        data_item.setData(\n            QBrush(fg_color),\n            QtCore.Qt.ItemDataRole.ForegroundRole,\n        )\n        data_item.setData(\n            QBrush(bg_color), QtCore.Qt.ItemDataRole.BackgroundRole\n        )\n        icon = self._make_icon(fg_color, bg_color, item.get(\"icon\"))\n        if icon:\n            data_item.setIcon(icon)\n        data_item.setData(\n            item.get(\"short_text\", \"&lt; UNDEFINED &gt;\"),\n            self.model().ShortTextRole,\n        )  # type: ignore\n        data_item.setData(item.get(\"icon\"), self.model().IconNameRole)  # type: ignore\n        self.model().appendRow(data_item)  # type: ignore\n\n    def update_items(self, item_list: list[dict] | None = None):\n        \"\"\"Replace all items in the model with the provided list.\n\n        Clears the model and calls :meth:`add_item` for every entry in\n        *item_list*.  If *item_list* is ``None`` or empty the model is left\n        unchanged.\n\n        Raises:\n            RuntimeError: If a custom incompatible model is currently set\n                (see :meth:`setModel`).\n\n        Args:\n            item_list: List of item dicts as accepted by :meth:`add_item`.\n                Pass ``None`` or an empty list to keep the current items.\n        \"\"\"\n        if item_list:\n            self._assert_compatible_model()\n            self.model().clear()  # type: ignore\n            for item in item_list:\n                self.add_item(item)\n\n    def set_inverted(self, state: bool):\n        \"\"\"Toggle the inverted colour mode and schedule a repaint.\n\n        In *inverted* mode the icon's normal colour is drawn using the item's\n        **background** colour instead of its foreground colour, producing a\n        coloured icon on a neutral background.\n\n        Inverted icons are generated lazily on first paint via\n        :meth:`_get_inverted_icon` and cached in :attr:`_inverted_icons`.\n        Calling this method triggers a repaint so the change is visible\n        immediately.\n\n        Args:\n            state: ``True`` to enable inverted mode, ``False`` to disable.\n        \"\"\"\n        self._inverted = state\n        self.update()\n\n    def set_size(self, size: MenuSize | str):\n        \"\"\"Change the display mode and repaint the widget.\n\n        Args:\n            size: New display mode.  Accepts a\n                :class:`~ayon_core.ui.data_models.MenuSize` value or its\n                lowercase string equivalent (``\"full\"``, ``\"short\"``,\n                ``\"icon\"``).\n        \"\"\"\n        self._size = (\n            size if isinstance(size, MenuSize) else MenuSize(size.lower())\n        )\n        self.update()\n\n    def sizeHint(self) -&gt; QtCore.QSize:\n        \"\"\"Return the preferred size for the combo-box.\n\n        Delegates to the AYON style's\n        :meth:`QStyle.sizeFromContents` so that the hint respects the\n        custom theme's metrics rather than the system default.\n\n        Returns:\n            The preferred :class:`QSize` for this widget.\n        \"\"\"\n\n        option = QtWidgets.QStyleOptionComboBox()\n        self.initStyleOption(option)\n        return get_ayon_style().sizeFromContents(\n            QtWidgets.QStyle.ContentsType.CT_ComboBox,\n            option,\n            self.rect().size(),\n            self,\n        )\n\n    def _resolve_current_text(self, idx: int) -&gt; str | None:\n        \"\"\"Return the display text for the current item.\n\n        Args:\n            idx: The current combo-box index.\n\n        Returns:\n            The text to display, or ``None`` when the option should be left\n            unchanged (e.g. no item is selected and no placeholder applies).\n        \"\"\"\n        if idx &lt; 0:\n            return None\n\n        if self._size == MenuSize.Full:\n            return self.currentData(QtCore.Qt.ItemDataRole.DisplayRole)\n        if self._size == MenuSize.Short:\n            return (\n                self.currentData(self.model().ShortTextRole)\n                if hasattr(self.model(), \"ShortTextRole\")\n                else \"&lt; INCOMPATIBLE MODEL &gt;\"\n            )\n        return \"\"  # MenuSize.Icon\n\n    def paintEvent(self, arg__1: QPaintEvent) -&gt; None:\n        \"\"\"Render the combo-box using the AYON custom style.\n\n        Overrides the default :meth:`QComboBox.paintEvent` to:\n\n        1. Draw the combo-box frame/control via the AYON style.\n        2. Substitute placeholder text (rendered in ``placeholderText``\n           palette colour) when no item is selected.\n        3. Render the label according to the current\n           :class:`~ayon_core.ui.data_models.MenuSize` mode:\n\n           - ``Full``  \u2192 item's full text.\n           - ``Short`` \u2192 item's ``ShortTextRole`` value, or\n             ``\"&lt; INCOMPATIBLE MODEL &gt;\"`` if the role is unavailable.\n           - ``Icon``  \u2192 empty string (icon only).\n\n        Args:\n            arg__1: The paint event delivered by Qt.\n        \"\"\"\n\n        p = QPainter(self)\n        option = QtWidgets.QStyleOptionComboBox()\n        self.initStyleOption(option)\n\n        p.setFont(self.font())\n        option.fontMetrics = self.fontMetrics()\n\n        if self._inverted:\n            option.currentIcon = self._get_inverted_icon(option.currentIcon)\n\n        _style = get_ayon_style()\n        _style.drawComplexControl(\n            QtWidgets.QStyle.ComplexControl.CC_ComboBox, option, p, self\n        )\n\n        idx = self.currentIndex()\n\n        if idx &lt; 0 and self.placeholderText():\n            option.palette.setBrush(  # type: ignore\n                QPalette.ColorRole.ButtonText,\n                option.palette.placeholderText(),  # type: ignore\n            )\n            option.currentText = self.placeholderText()  # type: ignore\n\n        text = self._resolve_current_text(idx)\n        if text is not None:\n            option.currentText = text  # type: ignore\n\n        _style.drawControl(\n            QtWidgets.QStyle.ControlElement.CE_ComboBoxLabel,\n            option,\n            p,\n            self,\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/index.html#client.ayon_core.ui.components.AYComboBox.add_item","title":"<code>add_item(item)</code>","text":"<p>Append a single item to the combo-box model.</p> <p>Constructs a :class:<code>QStandardItem</code> from item, sets its foreground colour, background colour (from the current palette), icon, short text, and icon name, then appends it to the model.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If a custom incompatible model is currently set (see :meth:<code>setModel</code>).</p> <p>Parameters:</p> Name Type Description Default <code>item</code> <code>dict[str, str]</code> <p>A dict with the following keys:</p> <ul> <li><code>\"text\"</code> (required) - Display label.</li> <li><code>\"color\"</code>      - Hex foreground colour   (default <code>\"#ffffff\"</code>).</li> <li><code>\"icon\"</code>       - Material Symbol icon name.</li> <li><code>\"short_text\"</code> - Abbreviated label stored in   <code>ShortTextRole</code> (default <code>\"&lt; UNDEFINED &gt;\"</code>).</li> </ul> required Source code in <code>client/ayon_core/ui/components/combo_box.py</code> <pre><code>def add_item(self, item: dict[str, str]):\n    \"\"\"Append a single item to the combo-box model.\n\n    Constructs a :class:`QStandardItem` from *item*, sets its foreground\n    colour, background colour (from the current palette), icon, short\n    text, and icon name, then appends it to the model.\n\n    Raises:\n        RuntimeError: If a custom incompatible model is currently set\n            (see :meth:`setModel`).\n\n    Args:\n        item: A dict with the following keys:\n\n            - ``\"text\"``       *(required)* - Display label.\n            - ``\"color\"``      - Hex foreground colour\n              (default ``\"#ffffff\"``).\n            - ``\"icon\"``       - Material Symbol icon name.\n            - ``\"short_text\"`` - Abbreviated label stored in\n              ``ShortTextRole`` (default ``\"&lt; UNDEFINED &gt;\"``).\n    \"\"\"\n    self._assert_compatible_model()\n\n    bg_color = self.palette().color(\n        QPalette.ColorGroup.Active, QPalette.ColorRole.Window\n    )\n    fg_color = QColor(item.get(\"color\", \"#ffffff\"))\n\n    text = item.get(\"text\")\n    if not text:\n        raise ValueError(\n            f\"Item dict must contain a non-empty 'text' key; got: {item!r}\"\n        )\n\n    data_item = QStandardItem(text)\n    data_item.setData(\n        QBrush(fg_color),\n        QtCore.Qt.ItemDataRole.ForegroundRole,\n    )\n    data_item.setData(\n        QBrush(bg_color), QtCore.Qt.ItemDataRole.BackgroundRole\n    )\n    icon = self._make_icon(fg_color, bg_color, item.get(\"icon\"))\n    if icon:\n        data_item.setIcon(icon)\n    data_item.setData(\n        item.get(\"short_text\", \"&lt; UNDEFINED &gt;\"),\n        self.model().ShortTextRole,\n    )  # type: ignore\n    data_item.setData(item.get(\"icon\"), self.model().IconNameRole)  # type: ignore\n    self.model().appendRow(data_item)  # type: ignore\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/index.html#client.ayon_core.ui.components.AYComboBox.paintEvent","title":"<code>paintEvent(arg__1)</code>","text":"<p>Render the combo-box using the AYON custom style.</p> <p>Overrides the default :meth:<code>QComboBox.paintEvent</code> to:</p> <ol> <li>Draw the combo-box frame/control via the AYON style.</li> <li>Substitute placeholder text (rendered in <code>placeholderText</code>    palette colour) when no item is selected.</li> <li> <p>Render the label according to the current    :class:<code>~ayon_core.ui.data_models.MenuSize</code> mode:</p> </li> <li> <p><code>Full</code>  \u2192 item's full text.</p> </li> <li><code>Short</code> \u2192 item's <code>ShortTextRole</code> value, or      <code>\"&lt; INCOMPATIBLE MODEL &gt;\"</code> if the role is unavailable.</li> <li><code>Icon</code>  \u2192 empty string (icon only).</li> </ol> <p>Parameters:</p> Name Type Description Default <code>arg__1</code> <code>QPaintEvent</code> <p>The paint event delivered by Qt.</p> required Source code in <code>client/ayon_core/ui/components/combo_box.py</code> <pre><code>def paintEvent(self, arg__1: QPaintEvent) -&gt; None:\n    \"\"\"Render the combo-box using the AYON custom style.\n\n    Overrides the default :meth:`QComboBox.paintEvent` to:\n\n    1. Draw the combo-box frame/control via the AYON style.\n    2. Substitute placeholder text (rendered in ``placeholderText``\n       palette colour) when no item is selected.\n    3. Render the label according to the current\n       :class:`~ayon_core.ui.data_models.MenuSize` mode:\n\n       - ``Full``  \u2192 item's full text.\n       - ``Short`` \u2192 item's ``ShortTextRole`` value, or\n         ``\"&lt; INCOMPATIBLE MODEL &gt;\"`` if the role is unavailable.\n       - ``Icon``  \u2192 empty string (icon only).\n\n    Args:\n        arg__1: The paint event delivered by Qt.\n    \"\"\"\n\n    p = QPainter(self)\n    option = QtWidgets.QStyleOptionComboBox()\n    self.initStyleOption(option)\n\n    p.setFont(self.font())\n    option.fontMetrics = self.fontMetrics()\n\n    if self._inverted:\n        option.currentIcon = self._get_inverted_icon(option.currentIcon)\n\n    _style = get_ayon_style()\n    _style.drawComplexControl(\n        QtWidgets.QStyle.ComplexControl.CC_ComboBox, option, p, self\n    )\n\n    idx = self.currentIndex()\n\n    if idx &lt; 0 and self.placeholderText():\n        option.palette.setBrush(  # type: ignore\n            QPalette.ColorRole.ButtonText,\n            option.palette.placeholderText(),  # type: ignore\n        )\n        option.currentText = self.placeholderText()  # type: ignore\n\n    text = self._resolve_current_text(idx)\n    if text is not None:\n        option.currentText = text  # type: ignore\n\n    _style.drawControl(\n        QtWidgets.QStyle.ControlElement.CE_ComboBoxLabel,\n        option,\n        p,\n        self,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/index.html#client.ayon_core.ui.components.AYComboBox.setModel","title":"<code>setModel(model)</code>","text":"<p>Set the item model and detect compatibility with AYComboBox roles.</p> <p>If model is not an :class:<code>AYComboBoxModel</code> instance and it does not expose both <code>ShortTextRole</code> and <code>IconNameRole</code> attributes, it is marked as incompatible.  In that state:</p> <ul> <li>:meth:<code>add_item</code> and :meth:<code>update_items</code> will raise   :exc:<code>RuntimeError</code>.</li> <li>Short mode falls back to <code>\"&lt; INCOMPATIBLE MODEL &gt;\"</code> as the   displayed text.</li> </ul> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>QAbstractItemModel</code> <p>The new item model to attach to the combo-box.</p> required Source code in <code>client/ayon_core/ui/components/combo_box.py</code> <pre><code>def setModel(self, model: QtCore.QAbstractItemModel) -&gt; None:\n    \"\"\"Set the item model and detect compatibility with AYComboBox roles.\n\n    If *model* is not an :class:`AYComboBoxModel` instance **and** it\n    does not expose both ``ShortTextRole`` and ``IconNameRole``\n    attributes, it is marked as *incompatible*.  In that state:\n\n    - :meth:`add_item` and :meth:`update_items` will raise\n      :exc:`RuntimeError`.\n    - Short mode falls back to ``\"&lt; INCOMPATIBLE MODEL &gt;\"`` as the\n      displayed text.\n\n    Args:\n        model: The new item model to attach to the combo-box.\n    \"\"\"\n    mtype = type(model)\n    self._uses_incompatible_model = mtype is not AYComboBoxModel and (\n        not hasattr(model, \"IconNameRole\")\n        or not hasattr(model, \"ShortTextRole\")\n    )\n    super().setModel(model)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/index.html#client.ayon_core.ui.components.AYComboBox.set_inverted","title":"<code>set_inverted(state)</code>","text":"<p>Toggle the inverted colour mode and schedule a repaint.</p> <p>In inverted mode the icon's normal colour is drawn using the item's background colour instead of its foreground colour, producing a coloured icon on a neutral background.</p> <p>Inverted icons are generated lazily on first paint via :meth:<code>_get_inverted_icon</code> and cached in :attr:<code>_inverted_icons</code>. Calling this method triggers a repaint so the change is visible immediately.</p> <p>Parameters:</p> Name Type Description Default <code>state</code> <code>bool</code> <p><code>True</code> to enable inverted mode, <code>False</code> to disable.</p> required Source code in <code>client/ayon_core/ui/components/combo_box.py</code> <pre><code>def set_inverted(self, state: bool):\n    \"\"\"Toggle the inverted colour mode and schedule a repaint.\n\n    In *inverted* mode the icon's normal colour is drawn using the item's\n    **background** colour instead of its foreground colour, producing a\n    coloured icon on a neutral background.\n\n    Inverted icons are generated lazily on first paint via\n    :meth:`_get_inverted_icon` and cached in :attr:`_inverted_icons`.\n    Calling this method triggers a repaint so the change is visible\n    immediately.\n\n    Args:\n        state: ``True`` to enable inverted mode, ``False`` to disable.\n    \"\"\"\n    self._inverted = state\n    self.update()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/index.html#client.ayon_core.ui.components.AYComboBox.set_size","title":"<code>set_size(size)</code>","text":"<p>Change the display mode and repaint the widget.</p> <p>Parameters:</p> Name Type Description Default <code>size</code> <code>MenuSize | str</code> <p>New display mode.  Accepts a :class:<code>~ayon_core.ui.data_models.MenuSize</code> value or its lowercase string equivalent (<code>\"full\"</code>, <code>\"short\"</code>, <code>\"icon\"</code>).</p> required Source code in <code>client/ayon_core/ui/components/combo_box.py</code> <pre><code>def set_size(self, size: MenuSize | str):\n    \"\"\"Change the display mode and repaint the widget.\n\n    Args:\n        size: New display mode.  Accepts a\n            :class:`~ayon_core.ui.data_models.MenuSize` value or its\n            lowercase string equivalent (``\"full\"``, ``\"short\"``,\n            ``\"icon\"``).\n    \"\"\"\n    self._size = (\n        size if isinstance(size, MenuSize) else MenuSize(size.lower())\n    )\n    self.update()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/index.html#client.ayon_core.ui.components.AYComboBox.sizeHint","title":"<code>sizeHint()</code>","text":"<p>Return the preferred size for the combo-box.</p> <p>Delegates to the AYON style's :meth:<code>QStyle.sizeFromContents</code> so that the hint respects the custom theme's metrics rather than the system default.</p> <p>Returns:</p> Type Description <code>QSize</code> <p>The preferred :class:<code>QSize</code> for this widget.</p> Source code in <code>client/ayon_core/ui/components/combo_box.py</code> <pre><code>def sizeHint(self) -&gt; QtCore.QSize:\n    \"\"\"Return the preferred size for the combo-box.\n\n    Delegates to the AYON style's\n    :meth:`QStyle.sizeFromContents` so that the hint respects the\n    custom theme's metrics rather than the system default.\n\n    Returns:\n        The preferred :class:`QSize` for this widget.\n    \"\"\"\n\n    option = QtWidgets.QStyleOptionComboBox()\n    self.initStyleOption(option)\n    return get_ayon_style().sizeFromContents(\n        QtWidgets.QStyle.ContentsType.CT_ComboBox,\n        option,\n        self.rect().size(),\n        self,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/index.html#client.ayon_core.ui.components.AYComboBox.update_items","title":"<code>update_items(item_list=None)</code>","text":"<p>Replace all items in the model with the provided list.</p> <p>Clears the model and calls :meth:<code>add_item</code> for every entry in item_list.  If item_list is <code>None</code> or empty the model is left unchanged.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If a custom incompatible model is currently set (see :meth:<code>setModel</code>).</p> <p>Parameters:</p> Name Type Description Default <code>item_list</code> <code>list[dict] | None</code> <p>List of item dicts as accepted by :meth:<code>add_item</code>. Pass <code>None</code> or an empty list to keep the current items.</p> <code>None</code> Source code in <code>client/ayon_core/ui/components/combo_box.py</code> <pre><code>def update_items(self, item_list: list[dict] | None = None):\n    \"\"\"Replace all items in the model with the provided list.\n\n    Clears the model and calls :meth:`add_item` for every entry in\n    *item_list*.  If *item_list* is ``None`` or empty the model is left\n    unchanged.\n\n    Raises:\n        RuntimeError: If a custom incompatible model is currently set\n            (see :meth:`setModel`).\n\n    Args:\n        item_list: List of item dicts as accepted by :meth:`add_item`.\n            Pass ``None`` or an empty list to keep the current items.\n    \"\"\"\n    if item_list:\n        self._assert_compatible_model()\n        self.model().clear()  # type: ignore\n        for item in item_list:\n            self.add_item(item)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/index.html#client.ayon_core.ui.components.AYLabel","title":"<code>AYLabel</code>","text":"<p>               Bases: <code>StyleMixin</code>, <code>QLabel</code></p> Source code in <code>client/ayon_core/ui/components/label.py</code> <pre><code>class AYLabel(StyleMixin, QtWidgets.QLabel):\n    Variants = QLabelVariants\n\n    def __init__(\n        self,\n        *args,\n        dim: bool = False,\n        icon: str = \"\",\n        icon_color: str = \"\",\n        icon_size: int = 20,\n        icon_text_spacing=6,\n        icon_fill=False,\n        text_color: str = \"\",\n        rel_text_size: int = 0,\n        bold: bool = False,\n        tool_tip=\"\",\n        variant: Variants = Variants.Default,\n        contrast_color: QColor | None = None,\n        elide_mode: Qt.TextElideMode = Qt.TextElideMode.ElideNone,\n        copy_text: bool = False,\n        **kwargs,\n    ):\n        # style params\n        self._variant_str: str = variant.value\n        self._style_data = StyleDict()\n\n        # widget params\n        self._dim = dim\n        self._icon = icon\n        self._icon_color = icon_color\n        self._icon_size = icon_size\n        self._icon_fill = icon_fill\n        self._icon_text_spacing = icon_text_spacing\n        self._rel_text_size = rel_text_size\n        self._text_color = text_color\n        self._bold = bold\n        self._text_setup_done = False\n        self._elide_mode = elide_mode\n        # copy the text because setting an icon will blank it, as a label is\n        # either text or pixmap.\n        self._text: str = \"\"\n        # reference bg color to compute contrast-adapted text color\n        self._contrast_color = (\n            contrast_color\n            if isinstance(contrast_color, QColor) and contrast_color.isValid()\n            else None\n        )\n        self._contrast_adapted = None\n\n        # copy-text feature\n        self._copy_text = copy_text\n        self._copy_icon_hovered = False\n        self._copy_confirmed = False\n        self._copy_done_opacity: float = 1.0\n        self._copy_pix_normal: QPixmap | None = None\n        self._copy_pix_hover: QPixmap | None = None\n        self._copy_pix_done: QPixmap | None = None\n        self._copy_confirm_timer: QTimer | None = None\n        self._copy_fade_timer: QTimer | None = None\n\n        super().__init__(*args, **kwargs)\n        self._style = get_ayon_style()\n        self._style_data = self._style.model.get_styles(\n            \"QLabel\", variant=self._variant_str\n        )\n        self._style_data.set_context(self)\n        self.setStyle(self._style)\n\n        # used to be in polish\n        self.setWindowFlag(Qt.WindowType.FramelessWindowHint, True)\n        self.setWindowFlag(Qt.WindowType.NoDropShadowWindowHint, True)\n\n        self.setSizePolicy(\n            QtWidgets.QSizePolicy.Policy.Minimum,\n            QtWidgets.QSizePolicy.Policy.Preferred,\n        )\n\n        # set alignment from style data if specified.\n        alignment = self._style_data[\"base\"].get(\"alignment\")\n        if alignment is not None:\n            if alignment == \"center\":\n                self.setAlignment(Qt.AlignmentFlag.AlignCenter)\n            elif alignment == \"left\":\n                self.setAlignment(\n                    Qt.AlignmentFlag.AlignLeft | Qt.AlignmentFlag.AlignVCenter\n                )\n            elif alignment == \"right\":\n                self.setAlignment(\n                    Qt.AlignmentFlag.AlignRight | Qt.AlignmentFlag.AlignVCenter\n                )\n\n        self._text = self.text()  # call before setting icon to preserve text\n        self.setToolTip(tool_tip)\n\n        if self._copy_text:\n            self.setMouseTracking(True)\n            self._copy_confirm_timer = QTimer(self)\n            self._copy_confirm_timer.setSingleShot(True)\n            self._copy_confirm_timer.timeout.connect(\n                self._on_copy_confirm_timeout\n            )\n            self._copy_fade_timer = QTimer(self)\n            self._copy_fade_timer.setInterval(16)\n            self._copy_fade_timer.timeout.connect(self._on_copy_fade_tick)\n\n        self.set_icon()\n\n    @property\n    def contrast_color(self) -&gt; QColor | None:\n        return self._contrast_color\n\n    def set_palette(self, palette: QPalette) -&gt; None:\n        \"\"\"Set the widget palette and trigger a repaint.\"\"\"\n        self._style_palette = palette\n        self._configure_palette()\n        if self._icon and not self._icon_color:\n            self.set_icon()  # update icon color from palette\n        self._copy_pix_normal = self._copy_pix_hover = self._copy_pix_done = (\n            None\n        )\n        self.update()\n\n    def set_font(self, font: QFont) -&gt; None:\n        \"\"\"Set the widget font and trigger a repaint.\"\"\"\n        self._style_font = self._configure_font(font)\n        self.update()\n\n    # Private methods -------------------------------------------------------\n\n    def set_icon(self, icon: str | None = None, color: str = \"\") -&gt; None:\n        if icon is not None:\n            self._icon = icon\n        if color:\n            self._icon_color = color\n\n        if self._icon:\n            same_as_bg = (\n                self._icon_color not in (None, \"\")\n                and self._style_data[\"base\"].get(\"background-color\", raw=True)\n                == \"@_icon_color\"\n            )\n            icon_color = self._icon_color\n            if not icon_color:\n                icon_color = self.palette().color(self.foregroundRole()).name()\n            elif same_as_bg:\n                icon_color = self._style_data[\"base\"].get(\"color\")\n\n            if self._icon == \"none\":\n                icn = QIcon()  # empty icon for spacing\n                pxm = QPixmap(self._icon_size, self._icon_size)\n                pxm.fill(Qt.GlobalColor.transparent)\n                icn.addPixmap(pxm)\n            else:\n                icn: QIcon = get_icon(\n                    self._icon,\n                    color=icon_color,\n                    fill=self._icon_fill,\n                )\n            self.setPixmap(icn.pixmap(QSize(self._icon_size, self._icon_size)))\n\n    def _configure_font(self, font: QFont) -&gt; QFont:\n        \"\"\"Initialize font configuration on first paint.\"\"\"\n        if self._text_setup_done:\n            return font\n\n        if self._rel_text_size != 0:\n            # _rel_text_size is in points but setting pixels is more reliable.\n            # use QFontInfo in case PixelSize() or pointSizeF() returns -1\n            pt_size = QFontInfo(font).pointSizeF()\n            new_pt_size = pt_size + self._rel_text_size\n            font.setPointSizeF(new_pt_size)\n\n        weight = QFont.Weight.Bold if self._bold else QFont.Weight.Normal\n        font.setWeight(weight)\n\n        self._text_setup_done = True\n        return font\n\n    def _display_text(self) -&gt; str:\n        \"\"\"Recompute the elided version of the stored text.\"\"\"\n        if (\n            self._elide_mode == Qt.TextElideMode.ElideNone\n            or not self.fontMetrics()\n        ):\n            return self._text\n        available_w = self.contentsRect().width()\n        if self._icon:\n            spacing = self._icon_text_spacing\n            available_w -= self._icon_size + spacing\n        text = self.fontMetrics().elidedText(\n            self._text, self._elide_mode, max(0, available_w)\n        )\n        return text\n\n    def _resolve_color(self) -&gt; QColor:\n        \"\"\"Get the effective foreground color (icon_color or palette).\"\"\"\n        if self._icon_color:\n            return QColor(self._icon_color)\n        return self.palette().color(self.foregroundRole())\n\n    def _to_qcolor(self, color: QColor | str | None) -&gt; QColor | None:\n        \"\"\"Convert a color value to QColor, handling None and strings.\"\"\"\n        if color is None:\n            return None\n        if isinstance(color, QColor):\n            return color\n        return QColor(color)\n\n    def _compute_contrast_text_color(\n        self,\n        bg_color: QColor | str | None,\n        fg_color: QColor,\n    ) -&gt; QColor:\n        \"\"\"Compute text color with sufficient contrast against background.\"\"\"\n        if not bg_color:\n            return fg_color\n        qbg = self._to_qcolor(bg_color)\n        return compute_color_for_contrast(\n            qbg.toTuple(),  # type: ignore\n            fg_color.toTuple(),\n            min_contrast_ratio=7.0,\n        )\n\n    def _configure_palette(self) -&gt; None:\n        \"\"\"Configure palette based on dim/contrast settings.\"\"\"\n        # _style_palette is guaranteed to be set in paintEvent before this call\n        assert self._style_palette is not None\n\n        if self._dim:\n            self._style_palette.setColor(\n                QPalette.ColorGroup.Active,\n                self.foregroundRole(),\n                self._style_palette.color(\n                    QPalette.ColorGroup.Active,\n                    QPalette.ColorRole.PlaceholderText,\n                ),\n            )\n            return\n\n        if self._text_color:\n            self._style_palette.setColor(\n                self.foregroundRole(), QColor(self._text_color)\n            )\n\n        if self._style_data[\"base\"].get(\"fill-from-foreground\", False):\n            # background-color is resolved from @_icon_color via style refs\n            bg_val = self._style_data[\"base\"].get(\"background-color\", \"\")\n            bg_color = (\n                QColor(bg_val)\n                if bg_val and bg_val != \"transparent\"\n                else self._style_palette.color(self.foregroundRole())\n            )\n            self._style_palette.setColor(self.backgroundRole(), bg_color)\n            if not self._contrast_color:\n                self._contrast_color = bg_color\n\n        if self._style_data[\"base\"].get(\"contrast-text\", False):\n            contrast_ref = self._contrast_color or self._icon_color\n            txt_color = self._compute_contrast_text_color(\n                contrast_ref,\n                self.palette().color(self.foregroundRole()),\n            )\n            self._style_palette.setColor(self.foregroundRole(), txt_color)\n\n        if \"disabled\" in self._style_data:\n            opacity = self._style_data[\"disabled\"].get(\"opacity\", 1.0)\n            if opacity &lt; 1.0:\n                for role in QPalette.ColorRole:\n                    color = self._style_palette.color(role)\n                    if color == Qt.GlobalColor.transparent:\n                        continue\n                    color.setAlphaF(opacity)\n                    self._style_palette.setColor(\n                        QPalette.ColorGroup.Disabled, role, color\n                    )\n\n    def _paint_filled(self, state: str) -&gt; None:\n        \"\"\"Render a filled-background label driven by style data.\"\"\"\n        assert isinstance(self.fontMetrics(), QFontMetrics)\n\n        # Auto-size from text metrics\n        if self._style_data[state].get(\"auto-size\"):\n            padding = self._style_data[state].get(\"auto-size-padding\", [0, 0])\n            t_rect = self.fontMetrics().boundingRect(self.text())\n            padx = int(self.fontMetrics().averageCharWidth() * padding[0])\n            pady = int(self.fontMetrics().height() * padding[1])\n            self.setFixedSize(\n                t_rect.width() + padx,\n                t_rect.height() + pady,\n            )\n\n        p = QPainter(self)\n        self.initPainter(p)\n        p.setFont(self.font())\n        p.setRenderHint(QPainter.RenderHint.Antialiasing)\n\n        # Fill color from foreground\n        fill_color = self._resolve_color()\n        p.setBrush(QBrush(fill_color))\n        p.setPen(Qt.PenStyle.NoPen)\n\n        # handle disabled opacity for both fill and text\n        p.setOpacity(self._style_data[state].get(\"opacity\", 1.0))\n\n        # Border radius: fraction of height or fixed\n        radius_frac = self._style_data[state].get(\"border-radius-fraction\")\n        if radius_frac is not None:\n            radius = self.rect().height() * radius_frac\n        else:\n            radius = self._style_data[state].get(\"border-radius\", 0)\n\n        p.drawRoundedRect(self.rect(), radius, radius)\n\n        # Text color with contrast computation\n        if self._style_data[state].get(\"contrast-text\"):\n            contrast_ref = self._contrast_color or self._icon_color\n            txt_color = self._compute_contrast_text_color(\n                contrast_ref,\n                self.palette().color(self.foregroundRole()),\n            )\n            p.setPen(QPen(QBrush(txt_color), 1.0))\n\n        self._style.drawItemText(\n            p,\n            self.rect(),\n            Qt.AlignmentFlag.AlignCenter,\n            self.palette(),\n            self.isEnabled(),\n            self.text(),\n            textRole=QPalette.ColorRole.NoRole,\n        )\n        p.end()\n\n    def _paint_icon_and_text(self, state: str) -&gt; None:\n        \"\"\"Render label with both icon and text.\n\n        The icon and text are treated as a single group and positioned\n        within the widget rect according to the current alignment.\n\n        The spacing between icon and text is resolved from the\n        ``icon-text-spacing`` property in *style_data*, falling back to\n        the value supplied at construction time.\n\n        Args:\n            style_data: Variant style properties resolved from the style\n                JSON for the current ``QLabel`` variant.\n        \"\"\"\n        assert isinstance(self.fontMetrics(), QFontMetrics)\n\n        p = QPainter(self)\n        p.setFont(self.font())\n        p.setRenderHint(QPainter.RenderHint.Antialiasing)\n\n        text_rect = self.fontMetrics().boundingRect(self._display_text())\n        text_rect.adjust(0, 0, 1, 0)  # +1 pixel for antialiasing\n\n        icon_w = self._icon_size\n        icon_h = self._icon_size\n        spacing = int(\n            self._style_data[state].get(\n                \"icon-text-spacing\", self._icon_text_spacing\n            )\n        )\n        group_w = icon_w + spacing + text_rect.width()\n        group_h = max(icon_h, text_rect.height())\n\n        # Position the group using the current alignment\n        widget_rect = self.contentsRect().normalized()\n        explicit_padding = self._style_data[state].get(\n            \"padding\",\n            self._style_data[\"base\"].get(\"padding\", [0, 0]),\n        )\n        pad_h = int(explicit_padding[0])\n        pad_v = int(explicit_padding[1])\n        widget_rect = widget_rect.adjusted(pad_h, pad_v, -pad_h, -pad_v)\n\n        alignment = self.alignment()\n\n        if alignment &amp; Qt.AlignmentFlag.AlignLeft:\n            group_x = widget_rect.left()\n        elif alignment &amp; Qt.AlignmentFlag.AlignRight:\n            group_x = widget_rect.right() - group_w\n        else:  # Center (default)\n            group_x = widget_rect.left() + (widget_rect.width() - group_w) // 2\n\n        if alignment &amp; Qt.AlignmentFlag.AlignTop:\n            group_y = widget_rect.top()\n        elif alignment &amp; Qt.AlignmentFlag.AlignBottom:\n            group_y = widget_rect.bottom() - group_h\n        else:  # VCenter (default)\n            group_y = widget_rect.top() + (widget_rect.height() - group_h) // 2\n\n        # handle disabled opacity for both icon and text\n        p.save()\n        p.setOpacity(self._style_data[state].get(\"opacity\", 1.0))\n\n        # Draw icon at the left of the group\n        icon_y = group_y + (group_h - icon_h) // 2\n        icn_rct = QRect(group_x, icon_y, icon_w, icon_h)\n        self._style.drawItemPixmap(\n            p,\n            icn_rct,\n            Qt.AlignmentFlag.AlignCenter,\n            self.pixmap(),\n        )\n\n        # Draw text at the right of the icon\n        pal = self.palette()\n        if not self._dim:\n            pal.setColor(QPalette.ColorRole.Text, self._resolve_color())\n\n        txt_x = group_x + icon_w + spacing\n        txt_y = group_y + (group_h - text_rect.height()) // 2\n        txt_rct = QRect(txt_x, txt_y, text_rect.width(), text_rect.height())\n        self._style.drawItemText(\n            p,\n            txt_rct,\n            Qt.AlignmentFlag.AlignVCenter | Qt.AlignmentFlag.AlignLeft,\n            pal,\n            self.isEnabled(),\n            self._display_text(),\n            textRole=self.foregroundRole(),\n        )\n        p.restore()\n\n    def _paint_text_only(self, state: str) -&gt; None:\n        \"\"\"Render text-only label.\"\"\"\n        p = QPainter(self)\n        p.setFont(self.font())\n        p.setRenderHint(QPainter.RenderHint.Antialiasing)\n        pal = self.palette()\n        if self._text_color:\n            pal.setColor(self.foregroundRole(), QColor(self._text_color))\n            if \"disabled\" in self._style_data:\n                t_color = QColor(self._text_color)\n                t_color.setAlphaF(self._style_data[state].get(\"opacity\", 1.0))\n                pal.setColor(\n                    QPalette.ColorGroup.Disabled,\n                    self.foregroundRole(),\n                    t_color,\n                )\n        flags = int(self.alignment())\n        if self.wordWrap():\n            flags |= int(Qt.TextFlag.TextWordWrap)\n        p.save()\n        p.setOpacity(self._style_data[state].get(\"opacity\", 1.0))\n        self._style.drawItemText(\n            p,\n            self.contentsRect().normalized(),\n            flags,\n            pal,\n            self.isEnabled(),\n            self._display_text(),\n            textRole=self.foregroundRole(),\n        )\n        p.restore()\n\n    _COPY_ICON_SIZE = 16\n    _COPY_ICON_PADDING = 0\n\n    def _get_copy_icon_rect(self) -&gt; QRect:\n        \"\"\"Return the bounding rect for the copy icon overlay.\n\n        Returns:\n            Rect positioned at the right edge of the content area,\n            vertically centered.\n        \"\"\"\n        sz = self._COPY_ICON_SIZE\n        rect = self.contentsRect()\n        x = rect.right() - sz - self._COPY_ICON_PADDING\n        y = rect.top() + (rect.height() - sz) // 2\n        return QRect(x, y, sz, sz)\n\n    def _make_copy_pixmap(self, icon_name: str, color_str: str) -&gt; QPixmap:\n        \"\"\"Render a single copy-feature icon onto a transparent pixmap.\n\n        Args:\n            icon_name: Material symbol name.\n            color_str: CSS colour string passed to ``get_icon``.\n\n        Returns:\n            A ``QPixmap`` of size ``_COPY_ICON_SIZE \u00d7 _COPY_ICON_SIZE``.\n        \"\"\"\n        sz = self._COPY_ICON_SIZE\n        pxm = QPixmap(sz, sz)\n        pxm.fill(QColor(0, 0, 0, 200))\n        icon_pxm: QPixmap = get_icon(icon_name, color=color_str).pixmap(\n            QSize(sz, sz)\n        )\n        p = QPainter(pxm)\n        p.drawPixmap(QPoint(0, 0), icon_pxm)\n        p.end()\n        return pxm\n\n    def _build_copy_pixmaps(self) -&gt; None:\n        \"\"\"(Re-)build cached copy-icon pixmaps for normal, hover and done\n        states.\"\"\"\n        fg = self.palette().color(self.foregroundRole())\n        normal_color = QColor(fg)\n        normal_color.setAlphaF(0.75)\n        self._copy_pix_normal = self._make_copy_pixmap(\n            \"content_copy\",\n            normal_color.name(QColor.NameFormat.HexArgb),\n        )\n        self._copy_pix_hover = self._make_copy_pixmap(\n            \"content_copy\", fg.name()\n        )\n        self._copy_pix_done = self._make_copy_pixmap(\"check\", fg.name())\n\n    _COPY_FADE_DURATION_MS: int = 500\n\n    def _on_copy_confirm_timeout(self) -&gt; None:\n        \"\"\"Begin fading out the checkmark icon over _COPY_FADE_DURATION_MS.\"\"\"\n        self._copy_done_opacity = 1.0\n        assert self._copy_fade_timer is not None\n        self._copy_fade_timer.start()\n\n    def _on_copy_fade_tick(self) -&gt; None:\n        \"\"\"Decrement the done-icon opacity and repaint each frame.\"\"\"\n        step = 16.0 / self._COPY_FADE_DURATION_MS\n        self._copy_done_opacity = max(0.0, self._copy_done_opacity - step)\n        self.update()\n        if self._copy_done_opacity &lt;= 0.0:\n            assert self._copy_fade_timer is not None\n            self._copy_fade_timer.stop()\n            self._copy_confirmed = False\n\n    def _trigger_copy_confirmed(self) -&gt; None:\n        \"\"\"Copy the label text to clipboard and start the confirmation\n        animation.\"\"\"\n        QtWidgets.QApplication.clipboard().setText(self._text)\n        assert self._copy_fade_timer is not None\n        assert self._copy_confirm_timer is not None\n        # Reset any in-progress fade and restart the hold + fade sequence.\n        self._copy_fade_timer.stop()\n        self._copy_done_opacity = 1.0\n        self._copy_confirmed = True\n        self._copy_confirm_timer.start(500)\n        self.update()\n\n    def _paint_copy_icon(self) -&gt; None:\n        \"\"\"Draw the copy-to-clipboard icon on the right side of the label.\n\n        Only called when the mouse is over the widget and ``copy_text``\n        is True. Shows a fading checkmark briefly after the user clicks.\n        \"\"\"\n        if self._copy_pix_normal is None:\n            self._build_copy_pixmaps()\n        p = QPainter(self)\n        p.setRenderHint(QPainter.RenderHint.Antialiasing)\n        if self._copy_confirmed:\n            p.setOpacity(self._copy_done_opacity)\n            pix = self._copy_pix_done\n        else:\n            pix = (\n                self._copy_pix_hover\n                if self._copy_icon_hovered\n                else self._copy_pix_normal\n            )\n        self._style.drawItemPixmap(\n            p,\n            self._get_copy_icon_rect(),\n            Qt.AlignmentFlag.AlignCenter,\n            pix,  # type: ignore[arg-type]\n        )\n        p.end()\n\n    def _paint_background(self, state: str) -&gt; None:\n        \"\"\"Draw background if specified by style.\"\"\"\n        bg_color = self._style_data[state].get(\"background-color\")\n        if bg_color and bg_color != \"transparent\":\n            p = QPainter(self)\n            p.setRenderHint(QPainter.RenderHint.Antialiasing)\n            border_radius = self._style_data[state].get(\"border-radius\", 0)\n            border_width = self._style_data[state].get(\"border-width\", 0)\n            border_color = self._style_data[state].get(\n                \"border-color\", \"#00000000\"\n            )\n            qbg_color = QColor(bg_color)\n            qbg_color.setAlphaF(self._style_data[state].get(\"opacity\", 1.0))\n            p.setBrush(QBrush(QColor(qbg_color)))\n            p.setPen(\n                QPen(QColor(border_color), border_width)\n                if border_width &gt; 0\n                else Qt.PenStyle.NoPen\n            )\n            p.drawRoundedRect(self.rect(), border_radius, border_radius)\n\n    # QLabel overrides ----------------------------------------------------\n\n    def enterEvent(self, event: QEnterEvent) -&gt; None:\n        super().enterEvent(event)\n        if self._copy_text:\n            self.update()\n\n    def leaveEvent(self, event: QEvent) -&gt; None:\n        super().leaveEvent(event)\n        if self._copy_text:\n            self._copy_icon_hovered = False\n            self.update()\n\n    def mouseMoveEvent(self, event: QMouseEvent) -&gt; None:\n        super().mouseMoveEvent(event)\n        if not self._copy_text:\n            return\n        rect = self._get_copy_icon_rect()\n        hovered = rect.contains(event.pos())\n        if hovered != self._copy_icon_hovered:\n            self._copy_icon_hovered = hovered\n            self.update()\n\n    def mousePressEvent(self, event: QMouseEvent) -&gt; None:\n        if (\n            self._copy_text\n            and event.button() == Qt.MouseButton.LeftButton\n            and self._get_copy_icon_rect().contains(event.pos())\n        ):\n            self._trigger_copy_confirmed()\n        super().mousePressEvent(event)\n\n    def paintEvent(self, arg__1: QPaintEvent) -&gt; None:\n        \"\"\"Override to support icon + text rendering.\"\"\"\n        state = \"disabled\" if not self.isEnabled() else \"base\"\n        if (\n            state != \"disabled\"\n            and \"hover\" in self._style_data\n            and self.underMouse()\n        ):\n            state = \"hover\"\n\n        # Filled-background rendering (driven by JSON properties)\n        if self._style_data[\"base\"].get(\"fill-from-foreground\"):\n            self._paint_filled(state)\n        else:\n            self._paint_background(state)\n            if self._text and self._icon:\n                self._paint_icon_and_text(state)\n            elif self._icon and not self._text:\n                super().paintEvent(arg__1)\n            else:\n                self._paint_text_only(state)\n\n        if (\n            self._copy_text\n            and self.isEnabled()\n            and (self.underMouse() or self._copy_confirmed)\n        ):\n            self._paint_copy_icon()\n\n    def sizeHint(self) -&gt; QSize:\n        \"\"\"Compute a size hint driven by QLabel style data from the style JSON.\n\n        For variants with ``auto-size`` (e.g. badge / pill), the size is\n        derived from font-metrics and the ``auto-size-padding`` factor.\n        For variants with an explicit ``padding`` list (e.g. entity-label),\n        the size is padded accordingly.\n        When an icon is present the icon dimensions are added.\n        When ``wordWrap`` is True and the widget already has a non-zero width,\n        the height is recomputed from the wrapped text bounds at that width;\n        otherwise the natural single-line size is returned and the layout\n        refines the height via :meth:`heightForWidth`.\n        In all other cases the base ``QLabel.sizeHint()`` is returned.\n\n        Returns:\n            The recommended widget size.\n        \"\"\"\n        fm = self.fontMetrics()\n\n        # --- text size --------------------------------------------------\n        if self._text:\n            t_rect = fm.boundingRect(self._text)\n            text_w = t_rect.width() + 1  # +1 pixel for antialiasing\n            text_h = t_rect.height()\n        else:\n            text_w = 0\n            text_h = fm.height()\n\n        # --- icon size --------------------------------------------------\n        icon_w = icon_h = 0\n        if self._icon:\n            icon_w = self._icon_size\n            icon_h = self._icon_size\n\n        # --- variant-specific sizing ------------------------------------\n        if self._style_data[\"base\"].get(\"auto-size\"):\n            # badge / pill: padding is expressed as a fraction of the\n            # character metrics (x-factor of avgCharWidth, y-factor of height)\n            padding = self._style_data[\"base\"].get(\n                \"auto-size-padding\", [0.0, 0.0]\n            )\n            pad_x = int(fm.averageCharWidth() * padding[0])\n            pad_y = int(fm.height() * padding[1])\n\n            content_w = max(text_w, icon_w)\n            content_h = max(text_h, icon_h)\n\n            return QSize(content_w + pad_x, content_h + pad_y)\n\n        explicit_padding = self._style_data[\"base\"].get(\"padding\", [0, 0])\n        pad_h = int(explicit_padding[0])\n        pad_v = int(explicit_padding[1])\n\n        if icon_w and text_w:\n            spacing = int(\n                self._style_data[\"base\"].get(\n                    \"icon-text-spacing\", self._icon_text_spacing\n                )\n            )\n            content_w = icon_w + spacing + text_w\n            content_h = max(text_h, icon_h)\n            # print(\n            #     f\"{self._text!r}: {content_w + 2 * pad_h} x \"\n            #     f\"{content_h + 2 * pad_v}\"\n            # )\n        elif icon_w:\n            content_w = icon_w\n            content_h = icon_h\n        else:\n            content_w = text_w\n            content_h = text_h\n\n        # --- word-wrap sizing -------------------------------------------\n        # When word-wrap is on and the widget already occupies a real width,\n        # recompute the height for that width so the hint stays accurate after\n        # the first layout pass.  Layouts will also call heightForWidth() for\n        # further refinement.\n        if self.wordWrap() and self._text and not self._icon:\n            available_w = self.width() - 2 * pad_h\n            if available_w &gt; 0:\n                flags = int(Qt.TextFlag.TextWordWrap) | int(self.alignment())\n                wrap_rect = fm.boundingRect(\n                    QRect(0, 0, available_w, 0),\n                    flags,\n                    self._text,\n                )\n                return QSize(\n                    content_w + 2 * pad_h,\n                    wrap_rect.height() + 2 * pad_v,\n                )\n\n        cm = self.contentsMargins()\n\n        return QSize(\n            content_w + 2 * pad_h + cm.left() + cm.right(),\n            content_h + 2 * pad_v + cm.top() + cm.bottom(),\n        )\n\n    def hasHeightForWidth(self) -&gt; bool:\n        \"\"\"Return True when word-wrap is active so layouts call heightForWidth.\n\n        Returns:\n            True if the label uses word-wrap and has text without an icon.\n        \"\"\"\n        if self.wordWrap() and self._text and not self._icon:\n            return True\n        return super().hasHeightForWidth()\n\n    def heightForWidth(self, width: int) -&gt; int:\n        \"\"\"Compute the height required to render the text wrapped to *width*.\n\n        Only meaningful when ``wordWrap`` is True and there is no icon;\n        delegates to the base class otherwise.\n\n        Args:\n            width: Available width in pixels.\n\n        Returns:\n            Required height in pixels.\n        \"\"\"\n        if not self.wordWrap() or not self._text or self._icon:\n            return super().heightForWidth(width)\n\n        assert isinstance(self.fontMetrics(), QFontMetrics)\n        fm = self.fontMetrics()\n\n        explicit_padding = self._style_data[\"base\"].get(\"padding\", [0, 0])\n        pad_h = int(explicit_padding[0])\n        pad_v = int(explicit_padding[1])\n\n        available_w = max(1, width - 2 * pad_h)\n        flags = int(Qt.TextFlag.TextWordWrap) | int(self.alignment())\n        wrap_rect = fm.boundingRect(\n            QRect(0, 0, available_w, 0),\n            flags,\n            self._text,\n        )\n        return wrap_rect.height() + 2 * pad_v\n\n    def setText(self, arg__1: str) -&gt; None:\n        super().setText(arg__1)\n        self._text = self.text()\n\n    def set_icon_color(self, color: str) -&gt; None:\n        \"\"\"Update the icon color and refresh the widget.\n\n        Resets contrast color cache so filled variants (e.g. Badge) and any\n        palette-based contrast logic are recalculated with the new color.\n\n        Args:\n            color: New hex color string e.g. '#44ee9f'.\n        \"\"\"\n        self._icon_color = color\n        self._contrast_color = None  # reset so contrast is recalculated\n        self._configure_palette()\n        self.set_icon(color=color)  # repaints icon pixmap with new color\n        self._copy_pix_normal = self._copy_pix_hover = self._copy_pix_done = (\n            None\n        )\n        self.update()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/index.html#client.ayon_core.ui.components.AYLabel.hasHeightForWidth","title":"<code>hasHeightForWidth()</code>","text":"<p>Return True when word-wrap is active so layouts call heightForWidth.</p> <p>Returns:</p> Type Description <code>bool</code> <p>True if the label uses word-wrap and has text without an icon.</p> Source code in <code>client/ayon_core/ui/components/label.py</code> <pre><code>def hasHeightForWidth(self) -&gt; bool:\n    \"\"\"Return True when word-wrap is active so layouts call heightForWidth.\n\n    Returns:\n        True if the label uses word-wrap and has text without an icon.\n    \"\"\"\n    if self.wordWrap() and self._text and not self._icon:\n        return True\n    return super().hasHeightForWidth()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/index.html#client.ayon_core.ui.components.AYLabel.heightForWidth","title":"<code>heightForWidth(width)</code>","text":"<p>Compute the height required to render the text wrapped to width.</p> <p>Only meaningful when <code>wordWrap</code> is True and there is no icon; delegates to the base class otherwise.</p> <p>Parameters:</p> Name Type Description Default <code>width</code> <code>int</code> <p>Available width in pixels.</p> required <p>Returns:</p> Type Description <code>int</code> <p>Required height in pixels.</p> Source code in <code>client/ayon_core/ui/components/label.py</code> <pre><code>def heightForWidth(self, width: int) -&gt; int:\n    \"\"\"Compute the height required to render the text wrapped to *width*.\n\n    Only meaningful when ``wordWrap`` is True and there is no icon;\n    delegates to the base class otherwise.\n\n    Args:\n        width: Available width in pixels.\n\n    Returns:\n        Required height in pixels.\n    \"\"\"\n    if not self.wordWrap() or not self._text or self._icon:\n        return super().heightForWidth(width)\n\n    assert isinstance(self.fontMetrics(), QFontMetrics)\n    fm = self.fontMetrics()\n\n    explicit_padding = self._style_data[\"base\"].get(\"padding\", [0, 0])\n    pad_h = int(explicit_padding[0])\n    pad_v = int(explicit_padding[1])\n\n    available_w = max(1, width - 2 * pad_h)\n    flags = int(Qt.TextFlag.TextWordWrap) | int(self.alignment())\n    wrap_rect = fm.boundingRect(\n        QRect(0, 0, available_w, 0),\n        flags,\n        self._text,\n    )\n    return wrap_rect.height() + 2 * pad_v\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/index.html#client.ayon_core.ui.components.AYLabel.paintEvent","title":"<code>paintEvent(arg__1)</code>","text":"<p>Override to support icon + text rendering.</p> Source code in <code>client/ayon_core/ui/components/label.py</code> <pre><code>def paintEvent(self, arg__1: QPaintEvent) -&gt; None:\n    \"\"\"Override to support icon + text rendering.\"\"\"\n    state = \"disabled\" if not self.isEnabled() else \"base\"\n    if (\n        state != \"disabled\"\n        and \"hover\" in self._style_data\n        and self.underMouse()\n    ):\n        state = \"hover\"\n\n    # Filled-background rendering (driven by JSON properties)\n    if self._style_data[\"base\"].get(\"fill-from-foreground\"):\n        self._paint_filled(state)\n    else:\n        self._paint_background(state)\n        if self._text and self._icon:\n            self._paint_icon_and_text(state)\n        elif self._icon and not self._text:\n            super().paintEvent(arg__1)\n        else:\n            self._paint_text_only(state)\n\n    if (\n        self._copy_text\n        and self.isEnabled()\n        and (self.underMouse() or self._copy_confirmed)\n    ):\n        self._paint_copy_icon()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/index.html#client.ayon_core.ui.components.AYLabel.set_font","title":"<code>set_font(font)</code>","text":"<p>Set the widget font and trigger a repaint.</p> Source code in <code>client/ayon_core/ui/components/label.py</code> <pre><code>def set_font(self, font: QFont) -&gt; None:\n    \"\"\"Set the widget font and trigger a repaint.\"\"\"\n    self._style_font = self._configure_font(font)\n    self.update()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/index.html#client.ayon_core.ui.components.AYLabel.set_icon_color","title":"<code>set_icon_color(color)</code>","text":"<p>Update the icon color and refresh the widget.</p> <p>Resets contrast color cache so filled variants (e.g. Badge) and any palette-based contrast logic are recalculated with the new color.</p> <p>Parameters:</p> Name Type Description Default <code>color</code> <code>str</code> <p>New hex color string e.g. '#44ee9f'.</p> required Source code in <code>client/ayon_core/ui/components/label.py</code> <pre><code>def set_icon_color(self, color: str) -&gt; None:\n    \"\"\"Update the icon color and refresh the widget.\n\n    Resets contrast color cache so filled variants (e.g. Badge) and any\n    palette-based contrast logic are recalculated with the new color.\n\n    Args:\n        color: New hex color string e.g. '#44ee9f'.\n    \"\"\"\n    self._icon_color = color\n    self._contrast_color = None  # reset so contrast is recalculated\n    self._configure_palette()\n    self.set_icon(color=color)  # repaints icon pixmap with new color\n    self._copy_pix_normal = self._copy_pix_hover = self._copy_pix_done = (\n        None\n    )\n    self.update()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/index.html#client.ayon_core.ui.components.AYLabel.set_palette","title":"<code>set_palette(palette)</code>","text":"<p>Set the widget palette and trigger a repaint.</p> Source code in <code>client/ayon_core/ui/components/label.py</code> <pre><code>def set_palette(self, palette: QPalette) -&gt; None:\n    \"\"\"Set the widget palette and trigger a repaint.\"\"\"\n    self._style_palette = palette\n    self._configure_palette()\n    if self._icon and not self._icon_color:\n        self.set_icon()  # update icon color from palette\n    self._copy_pix_normal = self._copy_pix_hover = self._copy_pix_done = (\n        None\n    )\n    self.update()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/index.html#client.ayon_core.ui.components.AYLabel.sizeHint","title":"<code>sizeHint()</code>","text":"<p>Compute a size hint driven by QLabel style data from the style JSON.</p> <p>For variants with <code>auto-size</code> (e.g. badge / pill), the size is derived from font-metrics and the <code>auto-size-padding</code> factor. For variants with an explicit <code>padding</code> list (e.g. entity-label), the size is padded accordingly. When an icon is present the icon dimensions are added. When <code>wordWrap</code> is True and the widget already has a non-zero width, the height is recomputed from the wrapped text bounds at that width; otherwise the natural single-line size is returned and the layout refines the height via :meth:<code>heightForWidth</code>. In all other cases the base <code>QLabel.sizeHint()</code> is returned.</p> <p>Returns:</p> Type Description <code>QSize</code> <p>The recommended widget size.</p> Source code in <code>client/ayon_core/ui/components/label.py</code> <pre><code>def sizeHint(self) -&gt; QSize:\n    \"\"\"Compute a size hint driven by QLabel style data from the style JSON.\n\n    For variants with ``auto-size`` (e.g. badge / pill), the size is\n    derived from font-metrics and the ``auto-size-padding`` factor.\n    For variants with an explicit ``padding`` list (e.g. entity-label),\n    the size is padded accordingly.\n    When an icon is present the icon dimensions are added.\n    When ``wordWrap`` is True and the widget already has a non-zero width,\n    the height is recomputed from the wrapped text bounds at that width;\n    otherwise the natural single-line size is returned and the layout\n    refines the height via :meth:`heightForWidth`.\n    In all other cases the base ``QLabel.sizeHint()`` is returned.\n\n    Returns:\n        The recommended widget size.\n    \"\"\"\n    fm = self.fontMetrics()\n\n    # --- text size --------------------------------------------------\n    if self._text:\n        t_rect = fm.boundingRect(self._text)\n        text_w = t_rect.width() + 1  # +1 pixel for antialiasing\n        text_h = t_rect.height()\n    else:\n        text_w = 0\n        text_h = fm.height()\n\n    # --- icon size --------------------------------------------------\n    icon_w = icon_h = 0\n    if self._icon:\n        icon_w = self._icon_size\n        icon_h = self._icon_size\n\n    # --- variant-specific sizing ------------------------------------\n    if self._style_data[\"base\"].get(\"auto-size\"):\n        # badge / pill: padding is expressed as a fraction of the\n        # character metrics (x-factor of avgCharWidth, y-factor of height)\n        padding = self._style_data[\"base\"].get(\n            \"auto-size-padding\", [0.0, 0.0]\n        )\n        pad_x = int(fm.averageCharWidth() * padding[0])\n        pad_y = int(fm.height() * padding[1])\n\n        content_w = max(text_w, icon_w)\n        content_h = max(text_h, icon_h)\n\n        return QSize(content_w + pad_x, content_h + pad_y)\n\n    explicit_padding = self._style_data[\"base\"].get(\"padding\", [0, 0])\n    pad_h = int(explicit_padding[0])\n    pad_v = int(explicit_padding[1])\n\n    if icon_w and text_w:\n        spacing = int(\n            self._style_data[\"base\"].get(\n                \"icon-text-spacing\", self._icon_text_spacing\n            )\n        )\n        content_w = icon_w + spacing + text_w\n        content_h = max(text_h, icon_h)\n        # print(\n        #     f\"{self._text!r}: {content_w + 2 * pad_h} x \"\n        #     f\"{content_h + 2 * pad_v}\"\n        # )\n    elif icon_w:\n        content_w = icon_w\n        content_h = icon_h\n    else:\n        content_w = text_w\n        content_h = text_h\n\n    # --- word-wrap sizing -------------------------------------------\n    # When word-wrap is on and the widget already occupies a real width,\n    # recompute the height for that width so the hint stays accurate after\n    # the first layout pass.  Layouts will also call heightForWidth() for\n    # further refinement.\n    if self.wordWrap() and self._text and not self._icon:\n        available_w = self.width() - 2 * pad_h\n        if available_w &gt; 0:\n            flags = int(Qt.TextFlag.TextWordWrap) | int(self.alignment())\n            wrap_rect = fm.boundingRect(\n                QRect(0, 0, available_w, 0),\n                flags,\n                self._text,\n            )\n            return QSize(\n                content_w + 2 * pad_h,\n                wrap_rect.height() + 2 * pad_v,\n            )\n\n    cm = self.contentsMargins()\n\n    return QSize(\n        content_w + 2 * pad_h + cm.left() + cm.right(),\n        content_h + 2 * pad_v + cm.top() + cm.bottom(),\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/index.html#client.ayon_core.ui.components.AYLineEdit","title":"<code>AYLineEdit</code>","text":"<p>               Bases: <code>StyleMixin</code>, <code>QLineEdit</code></p> <p>Custom styled line edit component.</p> <p>Inherits from QLineEdit and uses the AYON style system for rendering. Paints its own background, border, and focus ring using ayon_style.json data, then calls super().paintEvent() to draw text, cursor, and selection on top.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <code>QWidget | None</code> <p>Parent widget.</p> <code>None</code> <code>placeholder</code> <code>str</code> <p>Placeholder text to display when empty.</p> <code>''</code> <code>variant</code> <code>QLineEditVariants</code> <p>Visual style variant.</p> <code>Default</code> <code>name_id</code> <code>str</code> <p>Object name for identification.</p> <code>''</code> Source code in <code>client/ayon_core/ui/components/line_edit.py</code> <pre><code>class AYLineEdit(StyleMixin, QLineEdit):\n    \"\"\"Custom styled line edit component.\n\n    Inherits from QLineEdit and uses the AYON style system for rendering.\n    Paints its own background, border, and focus ring using ayon_style.json\n    data, then calls super().paintEvent() to draw text, cursor, and\n    selection on top.\n\n    Args:\n        parent: Parent widget.\n        placeholder: Placeholder text to display when empty.\n        variant: Visual style variant.\n        name_id: Object name for identification.\n    \"\"\"\n\n    Variants = QLineEditVariants\n\n    def __init__(\n        self,\n        parent: QWidget | None = None,\n        placeholder: str = \"\",\n        variant: QLineEditVariants = QLineEditVariants.Default,\n        name_id: str = \"\",\n    ) -&gt; None:\n        super().__init__(parent)\n\n        self._variant_str = variant.value\n        self._pal: QPalette | None = None\n        self._variant_styles = {}\n\n        if placeholder:\n            self.setPlaceholderText(placeholder)\n\n        if name_id:\n            self.setObjectName(name_id)\n\n        # Suppress the native Qt frame so initStyleOption reports lineWidth=0\n        self.setFrame(False)\n\n        # Neutralise any ancestor stylesheet that would intercept\n        # PE_PanelLineEdit via QStyleSheetStyle, making it visually\n        # transparent.\n        self.setStyleSheet(\n            \"AYLineEdit { background: transparent; border: none; \"\n            \"padding: 0px; selection-background-color: none; \"\n            \"selection-color: none; }\"\n        )\n\n        # Suppress macOS native focus ring (we draw our own)\n        self.setAttribute(Qt.WidgetAttribute.WA_MacShowFocusRect, False)\n\n        # Enable hover events so underMouse() is reliable during paintEvent\n        self.setAttribute(Qt.WidgetAttribute.WA_Hover, True)\n\n        self._apply_style_palette()\n\n        #  this must be called after the palette has been set for it to stick.\n        self.setStyle(get_ayon_style())\n\n    @property\n    def ayon_palette(self) -&gt; QPalette:\n        \"\"\"Return the palette used for this widget.\"\"\"\n        if self._pal is None:\n            self._apply_style_palette()\n        return self._pal\n\n    def variant_style(self, state=None) -&gt; dict:\n        \"\"\"Return the style dict for the current variant.\"\"\"\n        key = (self._variant_str, state)\n        if key not in self._variant_styles:\n            model = get_ayon_style().model\n            self._variant_styles[key] = model.get_style(\n                \"QLineEdit\", variant=self._variant_str, state=state\n            )\n            self._variant_styles[key].set_context(self)\n        return self._variant_styles[key]\n\n    def _apply_style_palette(self) -&gt; None:\n        \"\"\"Push text / placeholder colors and padding from ayon_style.json.\"\"\"\n        style = self.variant_style()\n\n        self._pal = self.palette()\n\n        text_color = QColor(style.get(\"color\", \"#ffffff\"))\n        self._pal.setColor(QPalette.ColorRole.Text, text_color)\n        self._pal.setColor(QPalette.ColorRole.BrightText, text_color)\n\n        ph_color = QColor(style.get(\"placeholder-color\", \"#888888\"))\n        self._pal.setColor(QPalette.ColorRole.PlaceholderText, ph_color)\n\n        self._pal.setColor(\n            QPalette.ColorRole.Highlight,\n            QColor(style.get(\"selection-background-color\", \"#4040dd\")),\n        )\n        self._pal.setColor(\n            QPalette.ColorRole.HighlightedText,\n            QColor(style.get(\"selection-color\", \"#ffffff\")),\n        )\n\n        # Transparent base so the background rect we draw is visible\n        self._pal.setColor(QPalette.ColorRole.Base, QColor(0, 0, 0, 0))\n\n        self.setPalette(self._pal)\n\n        # Apply padding as text margins (immune to QSS interception)\n        padding = style.get(\"padding\", [8, 4])\n        pad_h = padding[0]\n        pad_v = padding[1]\n        icon_width = 0\n        if style.get(\"icon\"):\n            icon_width = style.get(\"icon-size\", 16) + style.get(\n                \"icon-padding\", 8\n            )\n        self.setTextMargins(pad_h + icon_width, pad_v, pad_h, pad_v)\n\n    def initStyleOption(self, option: QStyleOptionFrame) -&gt; None:\n        \"\"\"Override the palette used by the style to paint the widget.\"\"\"\n        self.setPalette(self.ayon_palette)\n        super().initStyleOption(option)\n\n    def paintEvent(self, event: QPaintEvent) -&gt; None:\n        \"\"\"Paint background, border, and focus ring, then delegate text\n        rendering.\n\n        Draws the styled background rectangle and border first using QPainter\n        directly (no style or stylesheet involvement), then calls\n        super().paintEvent() which renders the text, cursor, and selection\n        highlight on top.  Qt's PE_PanelLineEdit call inside that base\n        implementation is intercepted by LineEditDrawer which returns\n        immediately for AYLineEdit instances, so our background is preserved.\n        \"\"\"\n\n        is_disabled = not self.isEnabled()\n        is_hover = self.underMouse()\n        has_focus = self.hasFocus()\n\n        if is_disabled:\n            state = \"disabled\"\n        elif is_hover and not has_focus:\n            state = \"hover\"\n        else:\n            state = \"base\"\n\n        style = self.variant_style(state)\n\n        bg_color = QColor(style.get(\"background-color\", \"#272d35\"))\n        border_color = QColor(style.get(\"border-color\", \"#41474d\"))\n        border_width = style.get(\"border-width\", 1)\n        border_radius = style.get(\"border-radius\", 2)\n        opacity = style.get(\"opacity\", 1.0)\n\n        focus_outline_width = style.get(\"focus-outline-width\", 2)\n        focus_outline_color = QColor(\n            style.get(\"focus-outline-color\", \"#8fceff\")\n        )\n\n        painter = QPainter(self)\n        painter.setRenderHint(QPainter.RenderHint.Antialiasing)\n        painter.setOpacity(opacity)\n        painter.setFont(self.font())\n\n        rect = QRectF(self.rect())\n        half_bw = border_width / 2.0\n\n        # Background\n        bg_rect = rect.adjusted(half_bw, half_bw, -half_bw, -half_bw)\n        painter.setPen(Qt.PenStyle.NoPen)\n        painter.setBrush(QBrush(bg_color))\n        painter.drawRoundedRect(bg_rect, border_radius, border_radius)\n\n        # icon\n        icon_name = style.get(\"icon\")\n        if icon_name:\n            icon_color = QColor(style.get(\"icon-color\", \"#888888\"))\n            icon_size = style.get(\"icon-size\", 16)\n            x = style.get(\"padding\", [8, 4])[0]\n            pixmap = get_icon(icon_name, color=icon_color).pixmap(\n                icon_size, icon_size\n            )\n            y = (rect.height() - icon_size) / 2.0\n            painter.drawPixmap(x, y, icon_size, icon_size, pixmap)\n\n        # Border\n        border_pen = QPen(border_color)\n        border_pen.setWidthF(border_width)\n        painter.setPen(border_pen)\n        painter.setBrush(Qt.BrushStyle.NoBrush)\n        painter.drawRoundedRect(bg_rect, border_radius, border_radius)\n\n        # Focus ring (drawn inset so it fits within the widget rect)\n        if has_focus:\n            half_fw = focus_outline_width / 2.0\n            focus_rect = rect.adjusted(half_fw, half_fw, -half_fw, -half_fw)\n            focus_pen = QPen(focus_outline_color)\n            focus_pen.setWidthF(focus_outline_width)\n            painter.setPen(focus_pen)\n            painter.setBrush(Qt.BrushStyle.NoBrush)\n            painter.drawRoundedRect(focus_rect, border_radius, border_radius)\n\n        painter.end()\n\n        # Let QLineEdit draw text, placeholder, cursor, and selection on top.\n        # LineEditDrawer intercepts PE_PanelLineEdit for AYLineEdit and is a\n        # no-op, so the background we just drew is preserved.\n        super().paintEvent(event)\n\n    def sizeHint(self) -&gt; QSize:\n        \"\"\"Override sizeHint to account for padding.\"\"\"\n        size = super().sizeHint()\n        style = self.variant_style()\n        padding = style.get(\"padding\", [8, 4])\n        size.setWidth(size.width() + padding[0] * 2)\n        size.setHeight(size.height() + padding[1] * 2)\n        return size\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/index.html#client.ayon_core.ui.components.AYLineEdit.ayon_palette","title":"<code>ayon_palette</code>  <code>property</code>","text":"<p>Return the palette used for this widget.</p>"},{"location":"autoapi/client/ayon_core/ui/components/index.html#client.ayon_core.ui.components.AYLineEdit.initStyleOption","title":"<code>initStyleOption(option)</code>","text":"<p>Override the palette used by the style to paint the widget.</p> Source code in <code>client/ayon_core/ui/components/line_edit.py</code> <pre><code>def initStyleOption(self, option: QStyleOptionFrame) -&gt; None:\n    \"\"\"Override the palette used by the style to paint the widget.\"\"\"\n    self.setPalette(self.ayon_palette)\n    super().initStyleOption(option)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/index.html#client.ayon_core.ui.components.AYLineEdit.paintEvent","title":"<code>paintEvent(event)</code>","text":"<p>Paint background, border, and focus ring, then delegate text rendering.</p> <p>Draws the styled background rectangle and border first using QPainter directly (no style or stylesheet involvement), then calls super().paintEvent() which renders the text, cursor, and selection highlight on top.  Qt's PE_PanelLineEdit call inside that base implementation is intercepted by LineEditDrawer which returns immediately for AYLineEdit instances, so our background is preserved.</p> Source code in <code>client/ayon_core/ui/components/line_edit.py</code> <pre><code>def paintEvent(self, event: QPaintEvent) -&gt; None:\n    \"\"\"Paint background, border, and focus ring, then delegate text\n    rendering.\n\n    Draws the styled background rectangle and border first using QPainter\n    directly (no style or stylesheet involvement), then calls\n    super().paintEvent() which renders the text, cursor, and selection\n    highlight on top.  Qt's PE_PanelLineEdit call inside that base\n    implementation is intercepted by LineEditDrawer which returns\n    immediately for AYLineEdit instances, so our background is preserved.\n    \"\"\"\n\n    is_disabled = not self.isEnabled()\n    is_hover = self.underMouse()\n    has_focus = self.hasFocus()\n\n    if is_disabled:\n        state = \"disabled\"\n    elif is_hover and not has_focus:\n        state = \"hover\"\n    else:\n        state = \"base\"\n\n    style = self.variant_style(state)\n\n    bg_color = QColor(style.get(\"background-color\", \"#272d35\"))\n    border_color = QColor(style.get(\"border-color\", \"#41474d\"))\n    border_width = style.get(\"border-width\", 1)\n    border_radius = style.get(\"border-radius\", 2)\n    opacity = style.get(\"opacity\", 1.0)\n\n    focus_outline_width = style.get(\"focus-outline-width\", 2)\n    focus_outline_color = QColor(\n        style.get(\"focus-outline-color\", \"#8fceff\")\n    )\n\n    painter = QPainter(self)\n    painter.setRenderHint(QPainter.RenderHint.Antialiasing)\n    painter.setOpacity(opacity)\n    painter.setFont(self.font())\n\n    rect = QRectF(self.rect())\n    half_bw = border_width / 2.0\n\n    # Background\n    bg_rect = rect.adjusted(half_bw, half_bw, -half_bw, -half_bw)\n    painter.setPen(Qt.PenStyle.NoPen)\n    painter.setBrush(QBrush(bg_color))\n    painter.drawRoundedRect(bg_rect, border_radius, border_radius)\n\n    # icon\n    icon_name = style.get(\"icon\")\n    if icon_name:\n        icon_color = QColor(style.get(\"icon-color\", \"#888888\"))\n        icon_size = style.get(\"icon-size\", 16)\n        x = style.get(\"padding\", [8, 4])[0]\n        pixmap = get_icon(icon_name, color=icon_color).pixmap(\n            icon_size, icon_size\n        )\n        y = (rect.height() - icon_size) / 2.0\n        painter.drawPixmap(x, y, icon_size, icon_size, pixmap)\n\n    # Border\n    border_pen = QPen(border_color)\n    border_pen.setWidthF(border_width)\n    painter.setPen(border_pen)\n    painter.setBrush(Qt.BrushStyle.NoBrush)\n    painter.drawRoundedRect(bg_rect, border_radius, border_radius)\n\n    # Focus ring (drawn inset so it fits within the widget rect)\n    if has_focus:\n        half_fw = focus_outline_width / 2.0\n        focus_rect = rect.adjusted(half_fw, half_fw, -half_fw, -half_fw)\n        focus_pen = QPen(focus_outline_color)\n        focus_pen.setWidthF(focus_outline_width)\n        painter.setPen(focus_pen)\n        painter.setBrush(Qt.BrushStyle.NoBrush)\n        painter.drawRoundedRect(focus_rect, border_radius, border_radius)\n\n    painter.end()\n\n    # Let QLineEdit draw text, placeholder, cursor, and selection on top.\n    # LineEditDrawer intercepts PE_PanelLineEdit for AYLineEdit and is a\n    # no-op, so the background we just drew is preserved.\n    super().paintEvent(event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/index.html#client.ayon_core.ui.components.AYLineEdit.sizeHint","title":"<code>sizeHint()</code>","text":"<p>Override sizeHint to account for padding.</p> Source code in <code>client/ayon_core/ui/components/line_edit.py</code> <pre><code>def sizeHint(self) -&gt; QSize:\n    \"\"\"Override sizeHint to account for padding.\"\"\"\n    size = super().sizeHint()\n    style = self.variant_style()\n    padding = style.get(\"padding\", [8, 4])\n    size.setWidth(size.width() + padding[0] * 2)\n    size.setHeight(size.height() + padding[1] * 2)\n    return size\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/index.html#client.ayon_core.ui.components.AYLineEdit.variant_style","title":"<code>variant_style(state=None)</code>","text":"<p>Return the style dict for the current variant.</p> Source code in <code>client/ayon_core/ui/components/line_edit.py</code> <pre><code>def variant_style(self, state=None) -&gt; dict:\n    \"\"\"Return the style dict for the current variant.\"\"\"\n    key = (self._variant_str, state)\n    if key not in self._variant_styles:\n        model = get_ayon_style().model\n        self._variant_styles[key] = model.get_style(\n            \"QLineEdit\", variant=self._variant_str, state=state\n        )\n        self._variant_styles[key].set_context(self)\n    return self._variant_styles[key]\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/index.html#client.ayon_core.ui.components.AYMenu","title":"<code>AYMenu</code>","text":"<p>               Bases: <code>QMenu</code></p> <p>QMenu that paints itself using the AYON style.</p> <p>Replicates :meth:<code>QMenu.paintEvent</code> but routes every primitive and control draw call through :func:<code>get_ayon_style</code>, so the menu is painted consistently with the rest of the AYON UI even when the application style isn't AYONStyle.</p> Source code in <code>client/ayon_core/ui/components/option_action.py</code> <pre><code>class AYMenu(QtWidgets.QMenu):\n    \"\"\"QMenu that paints itself using the AYON style.\n\n    Replicates :meth:`QMenu.paintEvent` but routes every primitive and\n    control draw call through :func:`get_ayon_style`, so the menu is\n    painted consistently with the rest of the AYON UI even when the\n    application style isn't AYONStyle.\n    \"\"\"\n\n    def __init__(self, *args, **kwargs) -&gt; None:\n        super().__init__(*args, **kwargs)\n        # Force the menu to use AYONStyle for drawing, even if the app's style\n        # is something else.\n        self.setStyle(get_ayon_style())\n\n    def paintEvent(self, arg__1: QtGui.QPaintEvent) -&gt; None:\n        \"\"\"Paint the menu using AYON's QStyle implementation.\n\n        Mirrors Qt's own ``QMenu::paintEvent`` order:\n        1. Draw the menu panel background (``PE_PanelMenu``).\n        2. Draw each visible action row (``CE_MenuItem``).\n        3. Draw the menu frame on top (``PE_FrameMenu``).\n\n        Args:\n            arg__1: The paint event delivered by Qt.\n        \"\"\"\n        style = get_ayon_style()\n        painter = QtGui.QPainter(self)\n\n        # --- Shared base option (used for panel + frame) ---\n        menu_opt = QtWidgets.QStyleOptionMenuItem()\n        menu_opt.initFrom(self)\n        menu_opt.state = QtWidgets.QStyle.StateFlag.State_None\n        menu_opt.checkType = (\n            QtWidgets.QStyleOptionMenuItem.CheckType.NotCheckable\n        )\n        menu_opt.maxIconWidth = 0\n        try:\n            menu_opt.reservedShortcutWidth = 0\n        except AttributeError:\n            # Older Qt versions expose tabWidth instead.\n            menu_opt.tabWidth = 0\n        menu_opt.rect = self.rect()\n        menu_opt.menuRect = self.rect()\n\n        # --- 1. Panel background ---\n        style.drawPrimitive(\n            QtWidgets.QStyle.PrimitiveElement.PE_PanelMenu,\n            menu_opt,\n            painter,\n            self,\n        )\n\n        # --- 2. Action rows ---\n        event_region = arg__1.region()\n        for action in self.actions():\n            action_rect = self.actionGeometry(action)\n            if not event_region.intersects(action_rect):\n                continue\n\n            opt = QtWidgets.QStyleOptionMenuItem()\n            self.initStyleOption(opt, action)\n            opt.rect = action_rect\n\n            style.drawControl(\n                QtWidgets.QStyle.ControlElement.CE_MenuItem,\n                opt,\n                painter,\n                self,\n            )\n\n        # --- 3. Frame on top ---\n        frame_width = style.pixelMetric(\n            QtWidgets.QStyle.PixelMetric.PM_MenuPanelWidth, menu_opt, self\n        )\n        if frame_width &gt; 0:\n            frame_opt = QtWidgets.QStyleOptionFrame()\n            frame_opt.initFrom(self)\n            frame_opt.rect = self.rect()\n            frame_opt.state = QtWidgets.QStyle.StateFlag.State_None\n            frame_opt.lineWidth = frame_width\n            frame_opt.midLineWidth = 0\n            style.drawPrimitive(\n                QtWidgets.QStyle.PrimitiveElement.PE_FrameMenu,\n                frame_opt,\n                painter,\n                self,\n            )\n\n        painter.end()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/index.html#client.ayon_core.ui.components.AYMenu.paintEvent","title":"<code>paintEvent(arg__1)</code>","text":"<p>Paint the menu using AYON's QStyle implementation.</p> <p>Mirrors Qt's own <code>QMenu::paintEvent</code> order: 1. Draw the menu panel background (<code>PE_PanelMenu</code>). 2. Draw each visible action row (<code>CE_MenuItem</code>). 3. Draw the menu frame on top (<code>PE_FrameMenu</code>).</p> <p>Parameters:</p> Name Type Description Default <code>arg__1</code> <code>QPaintEvent</code> <p>The paint event delivered by Qt.</p> required Source code in <code>client/ayon_core/ui/components/option_action.py</code> <pre><code>def paintEvent(self, arg__1: QtGui.QPaintEvent) -&gt; None:\n    \"\"\"Paint the menu using AYON's QStyle implementation.\n\n    Mirrors Qt's own ``QMenu::paintEvent`` order:\n    1. Draw the menu panel background (``PE_PanelMenu``).\n    2. Draw each visible action row (``CE_MenuItem``).\n    3. Draw the menu frame on top (``PE_FrameMenu``).\n\n    Args:\n        arg__1: The paint event delivered by Qt.\n    \"\"\"\n    style = get_ayon_style()\n    painter = QtGui.QPainter(self)\n\n    # --- Shared base option (used for panel + frame) ---\n    menu_opt = QtWidgets.QStyleOptionMenuItem()\n    menu_opt.initFrom(self)\n    menu_opt.state = QtWidgets.QStyle.StateFlag.State_None\n    menu_opt.checkType = (\n        QtWidgets.QStyleOptionMenuItem.CheckType.NotCheckable\n    )\n    menu_opt.maxIconWidth = 0\n    try:\n        menu_opt.reservedShortcutWidth = 0\n    except AttributeError:\n        # Older Qt versions expose tabWidth instead.\n        menu_opt.tabWidth = 0\n    menu_opt.rect = self.rect()\n    menu_opt.menuRect = self.rect()\n\n    # --- 1. Panel background ---\n    style.drawPrimitive(\n        QtWidgets.QStyle.PrimitiveElement.PE_PanelMenu,\n        menu_opt,\n        painter,\n        self,\n    )\n\n    # --- 2. Action rows ---\n    event_region = arg__1.region()\n    for action in self.actions():\n        action_rect = self.actionGeometry(action)\n        if not event_region.intersects(action_rect):\n            continue\n\n        opt = QtWidgets.QStyleOptionMenuItem()\n        self.initStyleOption(opt, action)\n        opt.rect = action_rect\n\n        style.drawControl(\n            QtWidgets.QStyle.ControlElement.CE_MenuItem,\n            opt,\n            painter,\n            self,\n        )\n\n    # --- 3. Frame on top ---\n    frame_width = style.pixelMetric(\n        QtWidgets.QStyle.PixelMetric.PM_MenuPanelWidth, menu_opt, self\n    )\n    if frame_width &gt; 0:\n        frame_opt = QtWidgets.QStyleOptionFrame()\n        frame_opt.initFrom(self)\n        frame_opt.rect = self.rect()\n        frame_opt.state = QtWidgets.QStyle.StateFlag.State_None\n        frame_opt.lineWidth = frame_width\n        frame_opt.midLineWidth = 0\n        style.drawPrimitive(\n            QtWidgets.QStyle.PrimitiveElement.PE_FrameMenu,\n            frame_opt,\n            painter,\n            self,\n        )\n\n    painter.end()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/index.html#client.ayon_core.ui.components.AYOptionBox","title":"<code>AYOptionBox</code>","text":"<p>               Bases: <code>AYButton</code></p> <p>Option box widget used as the right-hand button in an action row.</p> <p>Emits :attr:<code>clicked</code> when the user presses this button.  It is a standard :class:<code>AYButton</code> styled with the <code>Optional_Action</code> variant.</p> Source code in <code>client/ayon_core/ui/components/option_action.py</code> <pre><code>class AYOptionBox(AYButton):\n    \"\"\"Option box widget used as the right-hand button in an action row.\n\n    Emits :attr:`clicked` when the user presses this button.  It is a\n    standard :class:`AYButton` styled with the ``Optional_Action``\n    variant.\n    \"\"\"\n\n    def __init__(\n        self,\n        icon_name: str = \"check_box_outline_blank\",\n        icon_size: int = 16,\n        parent: QtWidgets.QWidget | None = None,\n    ) -&gt; None:\n        super().__init__(\n            parent,\n            variant=AYButton.Variants.Optional_Action,\n            icon=icon_name,\n            icon_size=icon_size,\n            fixed_width=False,\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/index.html#client.ayon_core.ui.components.AYOptionalAction","title":"<code>AYOptionalAction</code>","text":"<p>               Bases: <code>QWidgetAction</code></p> <p>Menu action with an optional right-hand option box button.</p> <p>Subclasses :class:<code>QtWidgets.QWidgetAction</code> to embed a custom :class:<code>AYOptionalActionWidget</code> inside a standard <code>QMenu</code>.</p> <p>Set <code>use_option=True</code> to show the option box and connect to :attr:<code>option_clicked</code> for the secondary action.</p> <p>Parameters:</p> Name Type Description Default <code>label</code> <code>str</code> <p>Display text.</p> required <code>icon_name</code> <code>str | None</code> <p>Material symbol icon name (or <code>\"none\"</code>).</p> <code>'none'</code> <code>use_option</code> <code>bool</code> <p>Whether to show the option box button.</p> <code>True</code> <code>parent</code> <code>QWidget | None</code> <p>Parent widget (typically the owning menu).</p> <code>None</code> Source code in <code>client/ayon_core/ui/components/option_action.py</code> <pre><code>class AYOptionalAction(QtWidgets.QWidgetAction):\n    \"\"\"Menu action with an optional right-hand option box button.\n\n    Subclasses :class:`QtWidgets.QWidgetAction` to embed a custom\n    :class:`AYOptionalActionWidget` inside a standard ``QMenu``.\n\n    Set ``use_option=True`` to show the option box and connect to\n    :attr:`option_clicked` for the secondary action.\n\n    Args:\n        label: Display text.\n        icon_name: Material symbol icon name (or ``\"none\"``).\n        use_option: Whether to show the option box button.\n        parent: Parent widget (typically the owning menu).\n    \"\"\"\n\n    option_clicked = QtCore.Signal()\n\n    def __init__(\n        self,\n        label: str,\n        icon_name: str | None = \"none\",\n        use_option: bool = True,\n        parent: QtWidgets.QWidget | None = None,\n    ) -&gt; None:\n        super().__init__(parent)\n        self._label = label\n        self._icon_name = icon_name or \"none\"\n        self._use_option = use_option\n        self.widget: AYOptionalActionWidget | None = None\n\n    def createWidget(self, parent: QtWidgets.QWidget) -&gt; QtWidgets.QWidget:\n        \"\"\"Instantiate and configure the custom action row widget.\n\n        Called by Qt when the action is added to a menu.\n\n        Args:\n            parent: The menu widget that will own the row widget.\n\n        Returns:\n            The newly created :class:`AYOptionalActionWidget`.\n        \"\"\"\n        widget = AYOptionalActionWidget(\n            self._label,\n            icon_name=self._icon_name,\n            parent=parent,\n        )\n        widget.setEnabled(self.isEnabled())\n        self.widget = widget\n\n        if self._use_option:\n            widget.option.clicked.connect(self.option_clicked.emit)\n            widget.option.clicked.connect(self._close_menu_chain)\n        else:\n            widget.option.setVisible(False)\n\n        return widget\n\n    def _close_menu_chain(self) -&gt; None:\n        \"\"\"Close the menu (and any parent menus) hosting this action.\"\"\"\n        w = self.widget\n        while w is not None:\n            if isinstance(w, QtWidgets.QMenu):\n                w.close()\n            w = w.parentWidget()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/index.html#client.ayon_core.ui.components.AYOptionalAction.createWidget","title":"<code>createWidget(parent)</code>","text":"<p>Instantiate and configure the custom action row widget.</p> <p>Called by Qt when the action is added to a menu.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <code>QWidget</code> <p>The menu widget that will own the row widget.</p> required <p>Returns:</p> Type Description <code>QWidget</code> <p>The newly created :class:<code>AYOptionalActionWidget</code>.</p> Source code in <code>client/ayon_core/ui/components/option_action.py</code> <pre><code>def createWidget(self, parent: QtWidgets.QWidget) -&gt; QtWidgets.QWidget:\n    \"\"\"Instantiate and configure the custom action row widget.\n\n    Called by Qt when the action is added to a menu.\n\n    Args:\n        parent: The menu widget that will own the row widget.\n\n    Returns:\n        The newly created :class:`AYOptionalActionWidget`.\n    \"\"\"\n    widget = AYOptionalActionWidget(\n        self._label,\n        icon_name=self._icon_name,\n        parent=parent,\n    )\n    widget.setEnabled(self.isEnabled())\n    self.widget = widget\n\n    if self._use_option:\n        widget.option.clicked.connect(self.option_clicked.emit)\n        widget.option.clicked.connect(self._close_menu_chain)\n    else:\n        widget.option.setVisible(False)\n\n    return widget\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/index.html#client.ayon_core.ui.components.AYOptionalActionWidget","title":"<code>AYOptionalActionWidget</code>","text":"<p>               Bases: <code>QWidget</code></p> <p>Row widget that combines a body area and an :class:<code>AYOptionBox</code>.</p> <p>The body contains an icon label and a text label.  The option box is pinned to the far right.  Both sections respond to hover state via :meth:<code>_set_row_hover</code> and :meth:<code>_sync_row_hover</code>.</p> <p>Parameters:</p> Name Type Description Default <code>label</code> <code>str</code> <p>Display text for the action.</p> required <code>icon_name</code> <code>str</code> <p>Material symbol icon name for the label.</p> <code>'none'</code> <code>parent</code> <code>QWidget | None</code> <p>Optional parent widget.</p> <code>None</code> Source code in <code>client/ayon_core/ui/components/option_action.py</code> <pre><code>class AYOptionalActionWidget(QtWidgets.QWidget):\n    \"\"\"Row widget that combines a body area and an :class:`AYOptionBox`.\n\n    The body contains an icon label and a text label.  The option box\n    is pinned to the far right.  Both sections respond to hover state\n    via :meth:`_set_row_hover` and :meth:`_sync_row_hover`.\n\n    Args:\n        label: Display text for the action.\n        icon_name: Material symbol icon name for the label.\n        parent: Optional parent widget.\n    \"\"\"\n\n    def __init__(\n        self,\n        label: str,\n        icon_name: str = \"none\",\n        parent: QtWidgets.QWidget | None = None,\n    ) -&gt; None:\n        super().__init__(parent)\n\n        _style = get_ayon_style().model.get_style(\n            \"QLabel\", variant=AYLabel.Variants.Optional_Action.value\n        )\n        icon_size = _style.get(\"icon-size\", 16)\n\n        body_widget = AYFrame(self, variant=AYFrame.Variants.Contextual_Menu)\n        body_widget.setObjectName(\"OptionalActionBody\")\n\n        label_wdgt = AYLabel(\n            label,\n            variant=AYLabel.Variants.Optional_Action,\n            icon=icon_name,\n            icon_size=icon_size,\n            icon_fill=False,\n            parent=body_widget,\n        )\n        min_h = int(\n            get_ayon_style().model.get_style(\"QMenu\").get(\"min-item-height\", 0)\n        )\n        self.setMinimumHeight(min_h)\n\n        option_box = AYOptionBox(icon_size=icon_size, parent=body_widget)\n        option_box.setObjectName(\"OptionalActionOption\")\n\n        body_layout = AYHBoxLayout(body_widget, spacing=2, margin=0)\n        body_layout.addWidget(label_wdgt, stretch=1)\n\n        layout = AYHBoxLayout(self, spacing=0, margin=0)\n        layout.addWidget(body_widget)\n        layout.addWidget(option_box)\n\n        body_widget.setMouseTracking(True)\n        self.setMouseTracking(True)\n\n        self.icon: QtGui.QIcon = QtGui.QIcon()\n        self.label: AYLabel = label_wdgt\n        self.option: AYOptionBox = option_box\n        self.body: QtWidgets.QWidget = body_widget\n\n        # Watch the children's hover transitions so we can keep them in sync\n        # while the cursor moves between them.\n        self.label.installEventFilter(self)\n        self.option.installEventFilter(self)\n\n    # -- hover propagation ------------------------------------------------\n\n    def _set_row_hover(self, hovered: bool) -&gt; None:\n        for child in (self.body, self.label, self.option):\n            child.setAttribute(QtCore.Qt.WA_UnderMouse, hovered)\n            child.update()\n\n    def _sync_row_hover(self) -&gt; None:\n        # ``underMouse()`` on the parent stays True as long as the cursor is\n        # anywhere inside this row, even while crossing child borders.\n        self._set_row_hover(self.underMouse())\n\n    def enterEvent(self, event: QtCore.QEvent) -&gt; None:\n        self._set_row_hover(True)\n        super().enterEvent(event)\n\n    def leaveEvent(self, event: QtCore.QEvent) -&gt; None:\n        self._set_row_hover(False)\n        super().leaveEvent(event)\n\n    def eventFilter(self, obj: QtCore.QObject, event: QtCore.QEvent) -&gt; bool:\n        if obj in (self.body, self.label, self.option) and event.type() in (\n            QtCore.QEvent.Type.Enter,\n            QtCore.QEvent.Type.Leave,\n        ):\n            # Qt is about to flip WA_UnderMouse on this child.  Defer to\n            # the next event-loop tick so Qt's own handling has finished,\n            # then re-assert hover state based on the parent.\n            QtCore.QTimer.singleShot(0, self._sync_row_hover)\n        return super().eventFilter(obj, event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/index.html#client.ayon_core.ui.components.AYTextEdit","title":"<code>AYTextEdit</code>","text":"<p>               Bases: <code>StyleMixin</code>, <code>QTextEdit</code></p> <p>AYON styled text edit widget.</p> <p>Overrides Qt's stylesheet painting with AYONStyle custom rendering.</p> <p>Parameters:</p> Name Type Description Default <code>*args</code> <p>Positional arguments passed to QTextEdit.</p> <code>()</code> <code>**kwargs</code> <p>Keyword arguments passed to QTextEdit.</p> <code>{}</code> Source code in <code>client/ayon_core/ui/components/text_edit.py</code> <pre><code>class AYTextEdit(StyleMixin, QTextEdit):\n    \"\"\"AYON styled text edit widget.\n\n    Overrides Qt's stylesheet painting with AYONStyle custom rendering.\n\n    Args:\n        *args: Positional arguments passed to QTextEdit.\n        **kwargs: Keyword arguments passed to QTextEdit.\n    \"\"\"\n\n    Variants = QTextEditVariants\n\n    def __init__(\n        self,\n        *args,\n        variant: Variants = Variants.Default,\n        **kwargs,\n    ):\n        \"\"\"Initialize AYTextEdit widget.\n\n        Args:\n            *args: Positional arguments passed to QTextEdit.\n            variant: Text edit variant.\n            **kwargs: Keyword arguments passed to QTextEdit.\n        \"\"\"\n        self._variant_str: str = variant.value\n        super().__init__(*args, **kwargs)\n        self.setStyle(get_ayon_style())\n        self.setAttribute(Qt.WidgetAttribute.WA_TranslucentBackground, True)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/index.html#client.ayon_core.ui.components.AYTextEdit.__init__","title":"<code>__init__(*args, variant=Variants.Default, **kwargs)</code>","text":"<p>Initialize AYTextEdit widget.</p> <p>Parameters:</p> Name Type Description Default <code>*args</code> <p>Positional arguments passed to QTextEdit.</p> <code>()</code> <code>variant</code> <code>Variants</code> <p>Text edit variant.</p> <code>Default</code> <code>**kwargs</code> <p>Keyword arguments passed to QTextEdit.</p> <code>{}</code> Source code in <code>client/ayon_core/ui/components/text_edit.py</code> <pre><code>def __init__(\n    self,\n    *args,\n    variant: Variants = Variants.Default,\n    **kwargs,\n):\n    \"\"\"Initialize AYTextEdit widget.\n\n    Args:\n        *args: Positional arguments passed to QTextEdit.\n        variant: Text edit variant.\n        **kwargs: Keyword arguments passed to QTextEdit.\n    \"\"\"\n    self._variant_str: str = variant.value\n    super().__init__(*args, **kwargs)\n    self.setStyle(get_ayon_style())\n    self.setAttribute(Qt.WidgetAttribute.WA_TranslucentBackground, True)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/index.html#client.ayon_core.ui.components.AYTreeView","title":"<code>AYTreeView</code>","text":"<p>               Bases: <code>StyleMixin</code>, <code>QTreeView</code></p> <p>AYON-styled tree view.</p> <p>Fully self-contained: uses AYONStyle for all painting, a custom item delegate that draws directly bypassing any parent QSS, and AYScrollBar instances for scrollbars.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <code>QWidget | None</code> <p>Optional parent widget.</p> <code>None</code> <code>variant</code> <code>QTreeViewVariants</code> <p>Visual style variant controlling background colour and item-state colours.</p> <code>Default</code> Source code in <code>client/ayon_core/ui/components/tree_view.py</code> <pre><code>class AYTreeView(StyleMixin, QTreeView):\n    \"\"\"AYON-styled tree view.\n\n    Fully self-contained: uses AYONStyle for all painting, a custom\n    item delegate that draws directly bypassing any parent QSS, and\n    AYScrollBar instances for scrollbars.\n\n    Args:\n        parent: Optional parent widget.\n        variant: Visual style variant controlling background colour and\n            item-state colours.\n    \"\"\"\n\n    Variants = QTreeViewVariants\n    selection_changed = Signal(QItemSelection, QItemSelection)\n    double_clicked = Signal(QMouseEvent)\n\n    def __init__(\n        self,\n        parent: QWidget | None = None,\n        variant: QTreeViewVariants = QTreeViewVariants.Default,\n    ) -&gt; None:\n        self._variant_str: str = variant.value\n\n        super().__init__(parent)\n\n        style = get_ayon_style()\n        self.setStyle(style)\n\n        # Self-contained: do not inherit parent background or stylesheet.\n        self.setAttribute(Qt.WidgetAttribute.WA_TranslucentBackground, True)\n        self.setAttribute(Qt.WidgetAttribute.WA_Hover, True)\n        self.setMouseTracking(True)\n\n        # Viewport must also be opaque with our colour.\n        self.viewport().setAttribute(\n            Qt.WidgetAttribute.WA_TranslucentBackground, False\n        )\n        self.viewport().setAttribute(Qt.WidgetAttribute.WA_Hover, True)\n        self.viewport().setMouseTracking(True)\n        self.viewport().installEventFilter(self)\n        self._hovered_row_key: tuple | None = None\n        self._sync_viewport_palette()\n\n        # Custom item delegate \u2014 paints items directly, avoids QSS.\n        delegate = TreeViewItemDelegate(\n            parent=self,\n            style_model=style.model,\n            variant=self._variant_str,\n        )\n        self.setItemDelegate(delegate)\n\n        # Styled scrollbars.\n        vsb = AYScrollBar(Qt.Orientation.Vertical, self)\n        self.setVerticalScrollBar(vsb)\n        self.setVerticalScrollBarPolicy(Qt.ScrollBarPolicy.ScrollBarAsNeeded)\n        hsb = AYScrollBar(Qt.Orientation.Horizontal, self)\n        self.setHorizontalScrollBar(hsb)\n        self.setHorizontalScrollBarPolicy(Qt.ScrollBarPolicy.ScrollBarAsNeeded)\n\n        # No header \u2014 single-column hierarchical browser.\n        self.setHeaderHidden(True)\n\n        # Indentation from style data.\n        tv_style = style.model.get_style(\"QTreeView\", self._variant_str)\n        self.setIndentation(int(tv_style.get(\"indent\", 20)))\n\n        # Selection behaviour.\n        self.setSelectionMode(QTreeView.SelectionMode.ExtendedSelection)\n        self.setSelectionBehavior(QTreeView.SelectionBehavior.SelectRows)\n\n        # No default frame \u2014 drawn manually in paintEvent.\n        self.setFrameShape(QTreeView.Shape.NoFrame)\n\n    def _sync_viewport_palette(self) -&gt; None:\n        \"\"\"Apply the variant background colour to the viewport palette.\"\"\"\n        style = get_ayon_style()\n        tv_style = style.model.get_style(\"QTreeView\", self._variant_str)\n        bg = QColor(tv_style.get(\"background-color\", \"#252a31\"))\n        p = self.viewport().palette()\n        p.setColor(QPalette.ColorRole.Base, bg)\n        p.setColor(QPalette.ColorRole.Window, bg)\n        self.viewport().setPalette(p)\n\n    def paintEvent(self, event: QPaintEvent) -&gt; None:\n        \"\"\"Draw the outer container background before the items.\n\n        Args:\n            event: The paint event.\n        \"\"\"\n        painter = QPainter(self.viewport())\n        painter.setRenderHint(QPainter.RenderHint.Antialiasing, False)\n        style = get_ayon_style()\n        tv_style = style.model.get_style(\"QTreeView\", self._variant_str)\n        bg = QColor(tv_style.get(\"background-color\", \"#252a31\"))\n        painter.fillRect(self.viewport().rect(), bg)\n        painter.end()\n\n        # Let QTreeView draw its items on top.\n        super().paintEvent(event)\n\n    def eventFilter(self, obj, event):\n        if obj is self.viewport():\n            if event.type() == QEvent.Type.MouseMove:\n                idx = self.indexAt(event.pos())\n                key = (idx.row(), idx.parent()) if idx.isValid() else None\n                if key != self._hovered_row_key:\n                    self._hovered_row_key = key\n                    self.viewport().update()\n            elif event.type() == QEvent.Type.Leave:\n                if self._hovered_row_key is not None:\n                    self._hovered_row_key = None\n                    self.viewport().update()\n        return super().eventFilter(obj, event)\n\n    def drawBranches(self, painter, rect, index):\n        \"\"\"Draw branch indicators with AYONStyle directly.\n\n        Bypasses ``self.style()`` because, when an application-level QSS is\n        active, Qt wraps the widget's style in a ``QStyleSheetStyle`` proxy\n        which would otherwise intercept ``PE_IndicatorBranch`` and apply\n        QSS ``QTreeView::branch`` rules on top of (or instead of) ours.\n        \"\"\"\n        style = get_ayon_style()  # the raw AYONStyle, never wrapped\n\n        opt = QStyleOption()\n        opt.rect = rect\n        opt.palette = self.palette()\n        state = QStyle.StateFlag.State_Item\n        if self.model() is not None and self.model().hasChildren(index):\n            state |= QStyle.StateFlag.State_Children\n        if self.isExpanded(index):\n            state |= QStyle.StateFlag.State_Open\n        if self.selectionModel().isSelected(index):\n            state |= QStyle.StateFlag.State_Selected\n        if self.isEnabled():\n            state |= QStyle.StateFlag.State_Enabled\n\n        # Row-level hover: is the cursor on the same row as `index`?\n        hovered_index = self.indexAt(\n            self.viewport().mapFromGlobal(QCursor.pos())\n        )\n        if (\n            hovered_index.isValid()\n            and hovered_index.row() == index.row()\n            and hovered_index.parent() == index.parent()\n        ):\n            state |= QStyle.StateFlag.State_MouseOver\n\n        opt.state = state\n\n        # Call our drawer directly, not through self.style().\n        style.drawers[\n            enum_to_str(\n                QStyle.PrimitiveElement,\n                QStyle.PrimitiveElement.PE_IndicatorBranch,\n                \"QTreeView\",\n            )\n        ](opt, painter, self)\n\n    def mouseDoubleClickEvent(self, event: QMouseEvent) -&gt; None:\n        \"\"\"Emit double_clicked signal on double-click.\"\"\"\n        self.double_clicked.emit(event)\n        super().mouseDoubleClickEvent(event)\n\n    def mousePressEvent(self, event) -&gt; None:\n        \"\"\"Deselect all items when clicking in an empty area.\"\"\"\n        index = self.indexAt(event.pos())\n        if not index.isValid():\n            self.clearSelection()\n            self.setCurrentIndex(self.rootIndex())\n            return\n        super().mousePressEvent(event)\n\n    def selectionChanged(\n        self,\n        selected: QItemSelection,\n        deselected: QItemSelection,\n    ) -&gt; None:\n        \"\"\"Override to emit a public signal on selection change.\n\n        Args:\n            selected: Newly selected items.\n            deselected: Newly deselected items.\n        \"\"\"\n        super().selectionChanged(selected, deselected)\n        self.selection_changed.emit(selected, deselected)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/index.html#client.ayon_core.ui.components.AYTreeView.drawBranches","title":"<code>drawBranches(painter, rect, index)</code>","text":"<p>Draw branch indicators with AYONStyle directly.</p> <p>Bypasses <code>self.style()</code> because, when an application-level QSS is active, Qt wraps the widget's style in a <code>QStyleSheetStyle</code> proxy which would otherwise intercept <code>PE_IndicatorBranch</code> and apply QSS <code>QTreeView::branch</code> rules on top of (or instead of) ours.</p> Source code in <code>client/ayon_core/ui/components/tree_view.py</code> <pre><code>def drawBranches(self, painter, rect, index):\n    \"\"\"Draw branch indicators with AYONStyle directly.\n\n    Bypasses ``self.style()`` because, when an application-level QSS is\n    active, Qt wraps the widget's style in a ``QStyleSheetStyle`` proxy\n    which would otherwise intercept ``PE_IndicatorBranch`` and apply\n    QSS ``QTreeView::branch`` rules on top of (or instead of) ours.\n    \"\"\"\n    style = get_ayon_style()  # the raw AYONStyle, never wrapped\n\n    opt = QStyleOption()\n    opt.rect = rect\n    opt.palette = self.palette()\n    state = QStyle.StateFlag.State_Item\n    if self.model() is not None and self.model().hasChildren(index):\n        state |= QStyle.StateFlag.State_Children\n    if self.isExpanded(index):\n        state |= QStyle.StateFlag.State_Open\n    if self.selectionModel().isSelected(index):\n        state |= QStyle.StateFlag.State_Selected\n    if self.isEnabled():\n        state |= QStyle.StateFlag.State_Enabled\n\n    # Row-level hover: is the cursor on the same row as `index`?\n    hovered_index = self.indexAt(\n        self.viewport().mapFromGlobal(QCursor.pos())\n    )\n    if (\n        hovered_index.isValid()\n        and hovered_index.row() == index.row()\n        and hovered_index.parent() == index.parent()\n    ):\n        state |= QStyle.StateFlag.State_MouseOver\n\n    opt.state = state\n\n    # Call our drawer directly, not through self.style().\n    style.drawers[\n        enum_to_str(\n            QStyle.PrimitiveElement,\n            QStyle.PrimitiveElement.PE_IndicatorBranch,\n            \"QTreeView\",\n        )\n    ](opt, painter, self)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/index.html#client.ayon_core.ui.components.AYTreeView.mouseDoubleClickEvent","title":"<code>mouseDoubleClickEvent(event)</code>","text":"<p>Emit double_clicked signal on double-click.</p> Source code in <code>client/ayon_core/ui/components/tree_view.py</code> <pre><code>def mouseDoubleClickEvent(self, event: QMouseEvent) -&gt; None:\n    \"\"\"Emit double_clicked signal on double-click.\"\"\"\n    self.double_clicked.emit(event)\n    super().mouseDoubleClickEvent(event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/index.html#client.ayon_core.ui.components.AYTreeView.mousePressEvent","title":"<code>mousePressEvent(event)</code>","text":"<p>Deselect all items when clicking in an empty area.</p> Source code in <code>client/ayon_core/ui/components/tree_view.py</code> <pre><code>def mousePressEvent(self, event) -&gt; None:\n    \"\"\"Deselect all items when clicking in an empty area.\"\"\"\n    index = self.indexAt(event.pos())\n    if not index.isValid():\n        self.clearSelection()\n        self.setCurrentIndex(self.rootIndex())\n        return\n    super().mousePressEvent(event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/index.html#client.ayon_core.ui.components.AYTreeView.paintEvent","title":"<code>paintEvent(event)</code>","text":"<p>Draw the outer container background before the items.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <code>QPaintEvent</code> <p>The paint event.</p> required Source code in <code>client/ayon_core/ui/components/tree_view.py</code> <pre><code>def paintEvent(self, event: QPaintEvent) -&gt; None:\n    \"\"\"Draw the outer container background before the items.\n\n    Args:\n        event: The paint event.\n    \"\"\"\n    painter = QPainter(self.viewport())\n    painter.setRenderHint(QPainter.RenderHint.Antialiasing, False)\n    style = get_ayon_style()\n    tv_style = style.model.get_style(\"QTreeView\", self._variant_str)\n    bg = QColor(tv_style.get(\"background-color\", \"#252a31\"))\n    painter.fillRect(self.viewport().rect(), bg)\n    painter.end()\n\n    # Let QTreeView draw its items on top.\n    super().paintEvent(event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/index.html#client.ayon_core.ui.components.AYTreeView.selectionChanged","title":"<code>selectionChanged(selected, deselected)</code>","text":"<p>Override to emit a public signal on selection change.</p> <p>Parameters:</p> Name Type Description Default <code>selected</code> <code>QItemSelection</code> <p>Newly selected items.</p> required <code>deselected</code> <code>QItemSelection</code> <p>Newly deselected items.</p> required Source code in <code>client/ayon_core/ui/components/tree_view.py</code> <pre><code>def selectionChanged(\n    self,\n    selected: QItemSelection,\n    deselected: QItemSelection,\n) -&gt; None:\n    \"\"\"Override to emit a public signal on selection change.\n\n    Args:\n        selected: Newly selected items.\n        deselected: Newly deselected items.\n    \"\"\"\n    super().selectionChanged(selected, deselected)\n    self.selection_changed.emit(selected, deselected)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/buttons.html","title":"buttons","text":""},{"location":"autoapi/client/ayon_core/ui/components/buttons.html#client.ayon_core.ui.components.buttons.AYButton","title":"<code>AYButton</code>","text":"<p>               Bases: <code>StyleMixin</code>, <code>QPushButton</code></p> Source code in <code>client/ayon_core/ui/components/buttons.py</code> <pre><code>class AYButton(StyleMixin, QtWidgets.QPushButton):\n    Variants = QPushButtonVariants\n\n    def __init__(\n        self,\n        *args,\n        variant: Variants = Variants.Surface,\n        icon: str | None = None,\n        icon_on: str | None = None,\n        icon_size: int = 16,\n        icon_color: str | None = None,\n        icon_fill: bool = False,\n        checkable: bool = False,\n        tooltip: str = \"\",\n        name_id: str = \"\",\n        contrast_color: QColor | None = None,\n        label_alignment: Qt.AlignmentFlag | None = None,\n        fixed_width: bool | None = None,\n        **kwargs,\n    ):\n        # style params\n        self._variant_str: str = variant.value\n        self._style_data = StyleDict()\n\n        # widget params\n        self._icon_size = icon_size\n        self._tooltip = tooltip\n        self._icon = icon\n        self._icon_on = icon_on or icon\n        self._icon_fill = icon_fill\n        self._contrast_color = contrast_color\n\n        super().__init__(*args, **kwargs)\n        self.setCheckable(checkable)\n\n        self._style = get_ayon_style()\n        self._style_data = get_ayon_style_data(\"QPushButton\", variant.value)\n        self._style_data.set_context(self)\n\n        # Determine the icon color\n        color_str = icon_color or self._style_data.get(\"color\", \"#ffffff\")\n        self._icon_color = QColor(color_str)\n        # Adjust the icon color to have enough contrast with the background\n        if isinstance(contrast_color, QColor) and contrast_color.isValid():\n            self._icon_color = compute_color_for_contrast(\n                contrast_color.toTuple(),\n                self._icon_color.toTuple(),\n                min_contrast_ratio=7,\n            )\n\n        # compute a readable icon hover color\n        self._icon_hover_color = self._icon_color\n        icon_hover_bg = self._style_data.get(\"hover\", {}).get(\n            \"background-color\", \"#000000\"\n        )\n        if isinstance(icon_hover_bg, str) and self._icon_color.isValid():\n            self._icon_hover_color = compute_color_for_contrast(\n                QColor(icon_hover_bg).toTuple(),\n                self._icon_color.toTuple(),\n                min_contrast_ratio=7,\n            )\n\n        # get the disabled opacity\n        self._disabled_opacity = self._style_data.get(\"disabled\", {}).get(\n            \"opacity\", 0.5\n        )\n\n        if self._icon:\n            self.set_icon(self._icon)\n\n        if self._tooltip:\n            self.setToolTip(self._tooltip)\n\n        self._label_alignment = label_alignment\n\n        self._name_id = \"\"\n        if name_id:\n            self.setObjectName(name_id)\n            self._name_id = name_id\n\n        use_fixed_width = (\n            (not bool(self.text()))  # only fixed when icon-only\n            if fixed_width is None\n            else fixed_width\n        )\n        if use_fixed_width:\n            self.setSizePolicy(\n                QtWidgets.QSizePolicy.Policy.Fixed,\n                QtWidgets.QSizePolicy.Policy.Fixed,\n            )\n        else:\n            self.setSizePolicy(\n                QtWidgets.QSizePolicy.Policy.Preferred,\n                QtWidgets.QSizePolicy.Policy.Fixed,\n            )\n\n        # self._style.style_widget(self)\n        self.setStyle(get_ayon_style())\n\n    @property\n    def contrast_color(self):\n        return self._contrast_color\n\n    def _compute_contrast_text_color(\n        self,\n        bg_color: QColor | str | None,\n        fg_color: QColor,\n    ) -&gt; QColor:\n        \"\"\"Compute text color with sufficient contrast against background.\"\"\"\n        if not bg_color:\n            return fg_color\n        qbg = QColor(bg_color) if isinstance(bg_color, str) else bg_color\n        return compute_color_for_contrast(\n            qbg.toTuple(),  # type: ignore\n            fg_color.toTuple(),\n            min_contrast_ratio=7.0,\n        )\n\n    def set_palette(self, palette: QPalette) -&gt; None:\n        self._style_palette = palette\n\n        if self._style_data.get(\"contrast-text\", False):\n            contrast_ref = self._contrast_color or self._icon_color\n            if not contrast_ref:\n                contrast_ref = self.palette().color(self.backgroundRole())\n            txt_color = self._compute_contrast_text_color(\n                contrast_ref,\n                self.palette().color(self.foregroundRole()),\n            )\n            self._style_palette.setColor(self.foregroundRole(), txt_color)\n\n    def initStyleOption(self, option: QtWidgets.QStyleOptionButton) -&gt; None:\n        super().initStyleOption(option)\n        option.iconSize = QtCore.QSize(self._icon_size, self._icon_size)\n\n    def sizeHint(self) -&gt; QtCore.QSize:\n        if self.testAttribute(QtCore.Qt.WidgetAttribute.WA_StyleSheet):\n            option = QtWidgets.QStyleOptionButton()\n            self.initStyleOption(option)\n            return get_ayon_style().sizeFromContents(\n                QtWidgets.QStyle.ContentsType.CT_PushButton,\n                option,\n                self.rect().size(),\n                self,\n            )\n        return super().sizeHint()\n\n    def paintEvent(self, arg__1: QtGui.QPaintEvent) -&gt; None:\n        p = QtGui.QPainter(self)\n        option = QtWidgets.QStyleOptionButton()\n        self.initStyleOption(option)\n        # override rect set by stylesheet\n        size = self.sizeHint()\n        if (\n            self.sizePolicy().horizontalPolicy()\n            == QtWidgets.QSizePolicy.Policy.Fixed\n        ):\n            self.setFixedSize(size)\n            option.rect = QtCore.QRect(0, 0, size.width(), size.height())\n        else:\n            self.setFixedHeight(size.height())  # draw\n        return get_ayon_style().drawControl(\n            QtWidgets.QStyle.ControlElement.CE_PushButton, option, p, self\n        )\n\n    def set_icon(self, icon_name: str):\n        self._icon = icon_name\n        # icon conventions\n        #   State.Off: checkable off\n        #   State.On: checkable on\n        #   State.Active: hover\n        disabled_color = QColor(self._icon_color)\n        disabled_color.setAlphaF(self._disabled_opacity)\n        if self.isCheckable():\n            icn = get_icon(\n                icon_name_off=self._icon,\n                color_off=self._icon_color,\n                icon_name_on=self._icon_on,\n                color_on=self._icon_color,\n                color_on_disabled=disabled_color,\n                color_off_disabled=disabled_color,\n                fill=self._icon_fill,\n            )\n        else:\n            icn = get_icon(\n                icon_name_off=self._icon,\n                color_off=self._icon_color,\n                icon_name_on=self._icon,\n                color_on=self._icon_hover_color,\n                color_on_disabled=disabled_color,\n                color_off_disabled=disabled_color,\n                fill=self._icon_fill,\n            )\n        self.setIcon(icn)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/buttons.html#client.ayon_core.ui.components.buttons.AYButtonMenu","title":"<code>AYButtonMenu</code>","text":"<p>               Bases: <code>AYButton</code></p> <p>A push button that shows a floating dropdown panel when clicked.</p> <p>When clicked, the button displays a <code>QFrame</code> popup positioned directly below it (or above when screen space is insufficient). The popup contents are provided by a caller-supplied <code>populate_callback</code> which receives the dropdown <code>QFrame</code> container.</p> <p>Clicking the button again or clicking outside the popup closes the dropdown.</p> <p>Example::</p> <pre><code>def populate(container: QtWidgets.QFrame) -&gt; None:\n    btn = QPushButton(\"Option A\", container)\n    container.layout().addWidget(btn)\n\nmenu_btn = AYButtonMenu(\"Options\", populate_callback=populate)\n</code></pre> Signals <p>menu_opened: Emitted when the dropdown is shown. menu_closed: Emitted when the dropdown is hidden.</p> Source code in <code>client/ayon_core/ui/components/buttons.py</code> <pre><code>class AYButtonMenu(AYButton):\n    \"\"\"A push button that shows a floating dropdown panel when clicked.\n\n    When clicked, the button displays a ``QFrame`` popup positioned\n    directly below it (or above when screen space is insufficient). The\n    popup contents are provided by a caller-supplied\n    ``populate_callback`` which receives the dropdown ``QFrame``\n    container.\n\n    Clicking the button again or clicking outside the popup closes the\n    dropdown.\n\n    Example::\n\n        def populate(container: QtWidgets.QFrame) -&gt; None:\n            btn = QPushButton(\"Option A\", container)\n            container.layout().addWidget(btn)\n\n        menu_btn = AYButtonMenu(\"Options\", populate_callback=populate)\n\n    Signals:\n        menu_opened: Emitted when the dropdown is shown.\n        menu_closed: Emitted when the dropdown is hidden.\n    \"\"\"\n\n    menu_opened = QtCore.Signal()\n    menu_closed = QtCore.Signal()\n\n    def __init__(\n        self,\n        *args,\n        populate_callback: Callable[[QtWidgets.QFrame], None],\n        **kwargs,\n    ) -&gt; None:\n        \"\"\"Initialize the AYButtonMenu.\n\n        Args:\n            *args: Positional arguments forwarded to ``AYButton``.\n            populate_callback: A callable that receives the dropdown\n                ``QFrame`` container and is responsible for adding\n                child widgets to it.\n            **kwargs: Keyword arguments forwarded to ``AYButton``.\n        \"\"\"\n        super().__init__(*args, **kwargs)\n\n        self._populate_callback = populate_callback\n        self._menu_open: bool = False\n\n        self._dropdown = ButtonMenuDropdown(self)\n        self._populate_callback(self._dropdown)\n        self._dropdown.popup_closed.connect(self._on_popup_closed)\n\n        self.clicked.connect(self._on_button_clicked)\n\n    # --- Event handlers ---\n\n    def _on_button_clicked(self) -&gt; None:\n        \"\"\"Toggle the dropdown popup visibility.\"\"\"\n        if self._menu_open:\n            self._dropdown.close()\n        else:\n            self._menu_open = True\n            self.menu_opened.emit()\n            self._dropdown.show_below(self)\n\n    def _on_popup_closed(self) -&gt; None:\n        \"\"\"Update state when the popup signals it has closed.\"\"\"\n        self._menu_open = False\n        self.menu_closed.emit()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/buttons.html#client.ayon_core.ui.components.buttons.AYButtonMenu.__init__","title":"<code>__init__(*args, populate_callback, **kwargs)</code>","text":"<p>Initialize the AYButtonMenu.</p> <p>Parameters:</p> Name Type Description Default <code>*args</code> <p>Positional arguments forwarded to <code>AYButton</code>.</p> <code>()</code> <code>populate_callback</code> <code>Callable[[QFrame], None]</code> <p>A callable that receives the dropdown <code>QFrame</code> container and is responsible for adding child widgets to it.</p> required <code>**kwargs</code> <p>Keyword arguments forwarded to <code>AYButton</code>.</p> <code>{}</code> Source code in <code>client/ayon_core/ui/components/buttons.py</code> <pre><code>def __init__(\n    self,\n    *args,\n    populate_callback: Callable[[QtWidgets.QFrame], None],\n    **kwargs,\n) -&gt; None:\n    \"\"\"Initialize the AYButtonMenu.\n\n    Args:\n        *args: Positional arguments forwarded to ``AYButton``.\n        populate_callback: A callable that receives the dropdown\n            ``QFrame`` container and is responsible for adding\n            child widgets to it.\n        **kwargs: Keyword arguments forwarded to ``AYButton``.\n    \"\"\"\n    super().__init__(*args, **kwargs)\n\n    self._populate_callback = populate_callback\n    self._menu_open: bool = False\n\n    self._dropdown = ButtonMenuDropdown(self)\n    self._populate_callback(self._dropdown)\n    self._dropdown.popup_closed.connect(self._on_popup_closed)\n\n    self.clicked.connect(self._on_button_clicked)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/buttons.html#client.ayon_core.ui.components.buttons.ButtonMenuDropdown","title":"<code>ButtonMenuDropdown</code>","text":"<p>               Bases: <code>AYDropdownPopup</code></p> <p>Floating dropdown popup for AYButtonMenu.</p> <p>A frameless popup QFrame that is shown below (or above, if not enough space) the parent button. It closes when it loses focus or the user presses Escape.</p> Signals <p>popup_closed: Inherited from <code>AYDropdownPopup</code>. Emitted when     the popup is hidden/closed.</p> Source code in <code>client/ayon_core/ui/components/buttons.py</code> <pre><code>class ButtonMenuDropdown(AYDropdownPopup):\n    \"\"\"Floating dropdown popup for AYButtonMenu.\n\n    A frameless popup QFrame that is shown below (or above, if not\n    enough space) the parent button. It closes when it loses focus or\n    the user presses Escape.\n\n    Signals:\n        popup_closed: Inherited from ``AYDropdownPopup``. Emitted when\n            the popup is hidden/closed.\n    \"\"\"\n\n    def __init__(\n        self,\n        parent: QtWidgets.QWidget | None = None,\n    ) -&gt; None:\n        \"\"\"Initialize the dropdown popup frame.\n\n        Args:\n            parent: Optional parent widget (used for style inheritance).\n        \"\"\"\n        super().__init__(\n            parent,\n            variant=AYDropdownPopup.Variants.Low_Framed_Thin,\n            translucent_bg=True,\n        )\n        self._stack = QtWidgets.QStackedLayout(self)\n        container = AYContainer(\n            layout=AYContainer.Layout.VBox,\n            variant=AYContainer.Variants.Low,\n            margin=10,\n            layout_spacing=10,\n        )\n        self._stack.addWidget(container)\n\n    def set_current_page(self, index: int) -&gt; None:\n        self._stack.setCurrentIndex(index)\n\n    def add_page(self, container: AYContainer) -&gt; None:\n        self._stack.addWidget(container)\n\n    def layout(self) -&gt; QtWidgets.QLayout | None:\n        widget = self._stack.currentWidget()\n        return widget.layout()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/buttons.html#client.ayon_core.ui.components.buttons.ButtonMenuDropdown.__init__","title":"<code>__init__(parent=None)</code>","text":"<p>Initialize the dropdown popup frame.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <code>QWidget | None</code> <p>Optional parent widget (used for style inheritance).</p> <code>None</code> Source code in <code>client/ayon_core/ui/components/buttons.py</code> <pre><code>def __init__(\n    self,\n    parent: QtWidgets.QWidget | None = None,\n) -&gt; None:\n    \"\"\"Initialize the dropdown popup frame.\n\n    Args:\n        parent: Optional parent widget (used for style inheritance).\n    \"\"\"\n    super().__init__(\n        parent,\n        variant=AYDropdownPopup.Variants.Low_Framed_Thin,\n        translucent_bg=True,\n    )\n    self._stack = QtWidgets.QStackedLayout(self)\n    container = AYContainer(\n        layout=AYContainer.Layout.VBox,\n        variant=AYContainer.Variants.Low,\n        margin=10,\n        layout_spacing=10,\n    )\n    self._stack.addWidget(container)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/card_view.html","title":"card_view","text":"<p>AYCardView component module.</p> <p>A flow-layout card view built on QAbstractItemView that displays AYEntityCard widgets using the same PaginatedTableModel as AYTableView. Supports collapsible grouping in tree mode.</p>"},{"location":"autoapi/client/ayon_core/ui/components/card_view.html#client.ayon_core.ui.components.card_view.AYCardView","title":"<code>AYCardView</code>","text":"<p>               Bases: <code>QAbstractItemView</code></p> <p>AYON-styled card view that displays AYEntityCard widgets in a flow layout.</p> <p>Uses the same PaginatedTableModel as AYTableView. In tree mode, groups are rendered as collapsible painted headers with cards flowing beneath each group.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <code>QWidget | None</code> <p>Optional parent widget.</p> <code>None</code> <code>variant</code> <code>AYCardViewVariants</code> <p>Visual style variant.</p> <code>Default</code> <code>card_width</code> <code>int</code> <p>Initial card width in pixels.</p> <code>200</code> <code>card_spacing</code> <code>int</code> <p>Horizontal and vertical spacing between cards.</p> <code>8</code> <code>card_data_mapper</code> <code>Callable[[dict[str, Any]], dict[str, Any]] | None</code> <p>Callable mapping row_data dict to AYEntityCard keyword arguments. If None, no cards are created.</p> <code>None</code> <code>group_header_height</code> <code>int</code> <p>Height of group header bars in tree mode.</p> <code>36</code> Source code in <code>client/ayon_core/ui/components/card_view.py</code> <pre><code>class AYCardView(QAbstractItemView):\n    \"\"\"AYON-styled card view that displays AYEntityCard widgets in a flow\n    layout.\n\n    Uses the same PaginatedTableModel as AYTableView. In tree mode, groups\n    are rendered as collapsible painted headers with cards flowing beneath\n    each group.\n\n    Args:\n        parent: Optional parent widget.\n        variant: Visual style variant.\n        card_width: Initial card width in pixels.\n        card_spacing: Horizontal and vertical spacing between cards.\n        card_data_mapper: Callable mapping row_data dict to AYEntityCard\n            keyword arguments. If None, no cards are created.\n        group_header_height: Height of group header bars in tree mode.\n    \"\"\"\n\n    Variants = AYCardViewVariants\n    selection_changed = Signal(QItemSelection, QItemSelection)\n    card_activated = Signal(QModelIndex)\n\n    def __init__(\n        self,\n        parent: QWidget | None = None,\n        variant: AYCardViewVariants = AYCardViewVariants.Default,\n        card_width: int = 200,\n        card_spacing: int = 8,\n        card_data_mapper: Callable[[dict[str, Any]], dict[str, Any]]\n        | None = None,\n        group_header_height: int = 36,\n    ) -&gt; None:\n        self._variant_str: str = variant.value\n        super().__init__(parent)\n\n        # FIXME: this is crashing\n        # style = get_ayon_style()\n        # self.setStyle(style)\n\n        self.setAttribute(Qt.WidgetAttribute.WA_TranslucentBackground, True)\n        self.setAttribute(Qt.WidgetAttribute.WA_Hover, True)\n        self.setMouseTracking(True)\n        self.viewport().setMouseTracking(True)\n        self.viewport().setAttribute(\n            Qt.WidgetAttribute.WA_TranslucentBackground, False\n        )\n        self._sync_viewport_palette()\n\n        self._card_width: int = card_width\n        self._card_spacing: int = card_spacing\n        self._card_data_mapper: (\n            Callable[[dict[str, Any]], dict[str, Any]] | None\n        ) = card_data_mapper\n        self._group_header_height: int = group_header_height\n        self.scroll_step = max(1, int(self._card_width / CARD_RATIO * 0.1))\n\n        vsb = AYScrollBar(Qt.Orientation.Vertical, self)\n        self.setVerticalScrollBar(vsb)\n        self.setVerticalScrollBarPolicy(Qt.ScrollBarPolicy.ScrollBarAsNeeded)\n        self.setHorizontalScrollBarPolicy(\n            Qt.ScrollBarPolicy.ScrollBarAlwaysOff\n        )\n        vsb.valueChanged.connect(self._on_scroll)\n\n        self._delegate = _CardDelegate(\n            card_width=self._card_width,\n            card_data_mapper=card_data_mapper or (lambda r: {}),\n            parent=self.viewport(),\n        )\n        self.setItemDelegate(self._delegate)\n\n        self._active_editor_pmis: set[QPersistentModelIndex] = set()\n        self._editor_sync_timer = QtCore.QTimer(self)\n        self._editor_sync_timer.setSingleShot(True)\n        self._editor_sync_timer.setInterval(0)\n        self._editor_sync_timer.timeout.connect(self._sync_viewport_editors)\n\n        self._model_connections: list[\n            tuple[Any, QtCore.QMetaObject.Connection]\n        ] = []\n\n        self._collapsed_groups: set[str] = set()\n\n        self._tree_layout: list[_GroupLayout] = []\n        self._flat_layout: list[_LayoutItem] = []\n        self._total_content_height: int = 0\n        self._is_tree_mode: bool = False\n\n        self._hovered_pmi: QPersistentModelIndex | None = None\n\n        self.setSelectionMode(\n            QAbstractItemView.SelectionMode.ExtendedSelection\n        )\n        self.setFrameShape(QAbstractItemView.Shape.NoFrame)\n\n    def set_card_width(self, width: int) -&gt; None:\n        self._card_width = width\n        self.scroll_step = max(1, int(self._card_width / CARD_RATIO * 0.1))\n        self._delegate.set_card_width(width)\n        for pmi in self._active_editor_pmis:\n            if not pmi.isValid():\n                continue\n            editor = self.indexWidget(QModelIndex(pmi))  # type: ignore\n            if isinstance(editor, AYEntityCard):\n                editor.resize_to_width(width)\n        self._calculate_layout()\n        self._sync_viewport_editors()\n\n    def _sync_viewport_palette(self) -&gt; None:\n        style = get_ayon_style()\n        tbl_style = style.model.get_style(\"AYTableView\", self._variant_str)\n        tbl_style.set_context(self)\n        bg = QColor(tbl_style.get(\"background-color\", \"#252a31\"))\n        p = self.viewport().palette()\n        p.setColor(QPalette.ColorRole.Base, bg)\n        p.setColor(QPalette.ColorRole.Window, bg)\n        self.viewport().setPalette(p)\n\n    def _source_model(self) -&gt; QtCore.QAbstractItemModel | None:\n        model = self.model()\n        if isinstance(model, QSortFilterProxyModel):\n            return model.sourceModel()\n        return model\n\n    def setModel(self, model: QtCore.QAbstractItemModel | None) -&gt; None:\n        for obj, conn in self._model_connections:\n            try:\n                obj.disconnect(conn)\n            except (RuntimeError, TypeError):\n                pass\n        self._model_connections.clear()\n\n        self._active_editor_pmis.clear()\n        self._editor_sync_timer.stop()\n\n        super().setModel(model)\n\n        if model is None:\n            return\n\n        conn = model.rowsInserted.connect(self._on_rows_inserted)\n        if conn is not None:\n            self._model_connections.append((model, conn))\n\n        conn_reset = model.modelReset.connect(self._on_model_reset)\n        if conn_reset is not None:\n            self._model_connections.append((model, conn_reset))\n\n        conn_dc = model.dataChanged.connect(self._on_data_changed)\n        if conn_dc is not None:\n            self._model_connections.append((model, conn_dc))\n\n        conn_layout = model.layoutChanged.connect(self._on_layout_changed)\n        if conn_layout is not None:\n            self._model_connections.append((model, conn_layout))\n\n        conn_removed = model.rowsRemoved.connect(self._on_rows_removed)\n        if conn_removed is not None:\n            self._model_connections.append((model, conn_removed))\n\n        source: Any = model\n        if isinstance(model, QSortFilterProxyModel):\n            source = model.sourceModel()\n        if isinstance(source, PaginatedTableModel):\n            conn2 = source.tree_mode_changed.connect(\n                self._on_tree_mode_changed\n            )\n            if conn2 is not None:\n                self._model_connections.append((source, conn2))\n            self._on_tree_mode_changed(source._tree_mode)\n\n        self._schedule_layout_update()\n\n    def _on_tree_mode_changed(self, tree_mode: bool) -&gt; None:\n        self._is_tree_mode = tree_mode\n        self._schedule_layout_update()\n\n    def _on_rows_inserted(\n        self, parent: QModelIndex, first: int, last: int\n    ) -&gt; None:\n        self._schedule_layout_update()\n\n    def _on_model_reset(self) -&gt; None:\n        self._active_editor_pmis.clear()\n        self._schedule_layout_update()\n\n    def _on_data_changed(\n        self,\n        top_left: QModelIndex,\n        bottom_right: QModelIndex,\n        roles: list[int],\n    ) -&gt; None:\n        self.viewport().update()\n\n    def _on_layout_changed(self) -&gt; None:\n        \"\"\"Handle layoutChanged emitted by the proxy on filter/sort changes.\n\n        Closes all currently-open persistent editors whose\n        ``QPersistentModelIndex`` is still valid (i.e. rows that survived\n        the filter but may have been renumbered), then hides any orphaned\n        ``AYEntityCard`` widgets whose PMIs became invalid (rows that were\n        filtered out \u2014 ``closePersistentEditor`` is a no-op for invalid\n        indices, so the widgets must be hidden manually).  Finally clears\n        the tracking set and schedules a full layout rebuild so that only\n        the rows that pass the new filter get fresh editors.\n        \"\"\"\n        for pmi in self._active_editor_pmis:\n            if pmi.isValid():\n                self.closePersistentEditor(QModelIndex(pmi))  # type: ignore\n        for child in self.viewport().children():\n            if isinstance(child, AYEntityCard):\n                child.hide()\n        self._active_editor_pmis.clear()\n        self._schedule_layout_update()\n\n    def _on_rows_removed(\n        self,\n        parent: QModelIndex,\n        first: int,\n        last: int,\n    ) -&gt; None:\n        \"\"\"Schedule a layout update when rows are removed from the model.\n\n        Args:\n            parent: Parent index of the removed rows.\n            first: First removed row index.\n            last: Last removed row index (inclusive).\n        \"\"\"\n        self._schedule_layout_update()\n\n    def _on_scroll(self) -&gt; None:\n        self._reposition_visible_editors()\n        self.viewport().update()\n        self._schedule_editor_sync()\n\n    def _schedule_layout_update(self) -&gt; None:\n        if not self._editor_sync_timer.isActive():\n            self._editor_sync_timer.start()\n\n    def _schedule_editor_sync(self) -&gt; None:\n        if not self._editor_sync_timer.isActive():\n            self._editor_sync_timer.start()\n\n    def _calculate_layout(self) -&gt; None:\n        model = self.model()\n        self._tree_layout = []\n        self._flat_layout = []\n        self._total_content_height = 0\n\n        if model is None or model.rowCount() == 0:\n            self.updateGeometries()\n            return\n\n        vp_width = self.viewport().width()\n        spacing = self._card_spacing\n        card_w = self._card_width\n        card_h = int(card_w / CARD_RATIO)\n        margin = spacing\n\n        if self._is_tree_mode:\n            self._calculate_tree_layout(\n                model, vp_width, margin, spacing, card_w, card_h\n            )\n        else:\n            self._calculate_flat_layout(\n                model, vp_width, margin, spacing, card_w, card_h\n            )\n\n        self.updateGeometries()\n\n    def _flow_items(\n        self,\n        model: QtCore.QAbstractItemModel,\n        parent_index: QModelIndex,\n        start_y: int,\n        vp_width: int,\n        margin: int,\n        spacing: int,\n        card_w: int,\n        card_h: int,\n    ) -&gt; tuple[list[_LayoutItem], int]:\n        items: list[_LayoutItem] = []\n        x = margin\n        y = start_y\n        line_height = 0\n        row_count = model.rowCount(parent_index)\n\n        for row in range(row_count):\n            idx = model.index(row, 0, parent_index)\n            if not idx.isValid():\n                continue\n            next_x = x + card_w + spacing\n            if next_x - spacing &gt; vp_width - margin and line_height &gt; 0:\n                x = margin\n                y = y + line_height + spacing\n                next_x = x + card_w + spacing\n                line_height = 0\n            rect = QRect(x, y, card_w, card_h)\n            pmi = QPersistentModelIndex(idx)\n            items.append(_LayoutItem(index=pmi, rect=rect))\n            x = next_x\n            line_height = max(line_height, card_h)\n\n        end_y = y + line_height if line_height &gt; 0 else start_y\n        return items, end_y\n\n    def _calculate_flat_layout(\n        self,\n        model: QtCore.QAbstractItemModel,\n        vp_width: int,\n        margin: int,\n        spacing: int,\n        card_w: int,\n        card_h: int,\n    ) -&gt; None:\n        items, end_y = self._flow_items(\n            model,\n            QModelIndex(),\n            margin,\n            vp_width,\n            margin,\n            spacing,\n            card_w,\n            card_h,\n        )\n        self._flat_layout = items\n        self._total_content_height = end_y + margin\n\n    def _calculate_tree_layout(\n        self,\n        model: QtCore.QAbstractItemModel,\n        vp_width: int,\n        margin: int,\n        spacing: int,\n        card_w: int,\n        card_h: int,\n    ) -&gt; None:\n        gh = self._group_header_height\n        y = margin\n        group_count = model.rowCount(QModelIndex())\n\n        for g in range(group_count):\n            group_idx = model.index(g, 0, QModelIndex())\n            if not group_idx.isValid():\n                continue\n\n            node = None\n            if isinstance(model, QSortFilterProxyModel):\n                src_idx = model.mapToSource(group_idx)\n                node = src_idx.internalPointer()\n            else:\n                node = group_idx.internalPointer()\n\n            node_id = node.node_id if node is not None else str(g)\n            label = group_idx.data(Qt.ItemDataRole.DisplayRole) or \"\"\n\n            # DecorationRole / ForegroundRole data lives on the tree-position\n            # column (e.g. \"product/version\"), NOT on column 0 (\"thumb\").\n            # Resolve the right column from the source model when possible.\n            src_model = self._source_model()\n            tree_col = (\n                src_model.tree_position\n                if isinstance(src_model, PaginatedTableModel)\n                else 0\n            )\n            meta_idx = (\n                model.index(g, tree_col, QModelIndex())\n                if tree_col != 0\n                else group_idx\n            )\n            label_color = meta_idx.data(Qt.ItemDataRole.ForegroundRole)\n            label_icon = meta_idx.data(Qt.ItemDataRole.DecorationRole)\n            child_count = model.rowCount(group_idx)\n\n            header_rect = QRect(0, y, vp_width, gh)\n            y += gh + spacing\n\n            collapsed = node_id in self._collapsed_groups\n\n            if not collapsed:\n                items, end_y = self._flow_items(\n                    model,\n                    group_idx,\n                    y,\n                    vp_width,\n                    margin,\n                    spacing,\n                    card_w,\n                    card_h,\n                )\n                y = end_y + spacing\n            else:\n                items = []\n\n            self._tree_layout.append(\n                _GroupLayout(\n                    node_id=node_id,\n                    label=label,\n                    child_count=child_count,\n                    header_rect=header_rect,\n                    collapsed=collapsed,\n                    label_color=label_color,\n                    label_icon=label_icon,\n                    items=items,\n                    parent_index=QPersistentModelIndex(group_idx),\n                )\n            )\n\n        self._total_content_height = y + margin\n\n    def _all_layout_items(self) -&gt; list[_LayoutItem]:\n        if self._is_tree_mode:\n            result = []\n            for g in self._tree_layout:\n                result.extend(g.items)\n            return result\n        return self._flat_layout\n\n    def _rect_for_index(\n        self, index: QModelIndex | QPersistentModelIndex\n    ) -&gt; QRect | None:\n        pmi = QPersistentModelIndex(index)\n        for item in self._all_layout_items():\n            if item.index == pmi:\n                return item.rect\n        return None\n\n    def _visible_rect(self, content_rect: QRect) -&gt; QRect:\n        offset = self.verticalOffset()\n        return content_rect.translated(0, -offset)\n\n    def horizontalOffset(self) -&gt; int:\n        return 0\n\n    def verticalOffset(self) -&gt; int:\n        return self.verticalScrollBar().value()\n\n    def visualRect(self, index: QModelIndex | QPersistentModelIndex) -&gt; QRect:\n        if not index.isValid():\n            return QRect()\n        content_rect = self._rect_for_index(index)\n        if content_rect is None:\n            return QRect()\n        return self._visible_rect(content_rect)\n\n    def scrollTo(\n        self,\n        index: QModelIndex | QPersistentModelIndex,\n        hint: QAbstractItemView.ScrollHint = (\n            QAbstractItemView.ScrollHint.EnsureVisible\n        ),\n    ) -&gt; None:\n        if not index.isValid():\n            return\n        content_rect = self._rect_for_index(index)\n        if content_rect is None:\n            return\n\n        vsb = self.verticalScrollBar()\n        vp_height = self.viewport().height()\n        cur = vsb.value()\n\n        if hint == QAbstractItemView.ScrollHint.EnsureVisible:\n            if content_rect.top() &lt; cur:\n                vsb.setValue(content_rect.top())\n            elif content_rect.bottom() &gt; cur + vp_height:\n                vsb.setValue(content_rect.bottom() - vp_height)\n        elif hint == QAbstractItemView.ScrollHint.PositionAtTop:\n            vsb.setValue(content_rect.top())\n        elif hint == QAbstractItemView.ScrollHint.PositionAtBottom:\n            vsb.setValue(content_rect.bottom() - vp_height)\n        elif hint == QAbstractItemView.ScrollHint.PositionAtCenter:\n            vsb.setValue(\n                content_rect.top() - (vp_height - content_rect.height()) // 2\n            )\n\n    def indexAt(self, point: QPoint) -&gt; QModelIndex:\n        content_y = point.y() + self.verticalOffset()\n        content_point = QPoint(point.x(), content_y)\n\n        for item in self._all_layout_items():\n            if item.rect.contains(content_point) and item.index.isValid():\n                return QModelIndex(item.index)  # type: ignore\n\n        return QModelIndex()\n\n    def moveCursor(\n        self,\n        action: QAbstractItemView.CursorAction,\n        modifiers: Qt.KeyboardModifier,\n    ) -&gt; QModelIndex:\n        current = self.currentIndex()\n        items = self._all_layout_items()\n        if not items:\n            return QModelIndex()\n\n        if not current.isValid():\n            first = items[0]\n            return (\n                QModelIndex(first.index)  # type: ignore\n                if first.index.isValid()\n                else QModelIndex()\n            )\n\n        cur_pmi = QPersistentModelIndex(current)\n        cur_pos = -1\n        for i, item in enumerate(items):\n            if item.index == cur_pmi:\n                cur_pos = i\n                break\n\n        if cur_pos == -1:\n            return current\n\n        if action in (\n            QAbstractItemView.CursorAction.MoveRight,\n            QAbstractItemView.CursorAction.MoveNext,\n        ):\n            new_pos = min(cur_pos + 1, len(items) - 1)\n        elif action in (\n            QAbstractItemView.CursorAction.MoveLeft,\n            QAbstractItemView.CursorAction.MovePrevious,\n        ):\n            new_pos = max(cur_pos - 1, 0)\n        elif action == QAbstractItemView.CursorAction.MoveDown:\n            cur_rect = items[cur_pos].rect\n            cur_center_x = cur_rect.center().x()\n            best = cur_pos\n            best_dist = float(\"inf\")\n            for i in range(cur_pos + 1, len(items)):\n                r = items[i].rect\n                if r.top() &gt; cur_rect.bottom():\n                    dist = abs(r.center().x() - cur_center_x)\n                    if dist &lt; best_dist:\n                        best_dist = dist\n                        best = i\n                    elif dist &gt; best_dist:\n                        break\n            new_pos = best\n        elif action == QAbstractItemView.CursorAction.MoveUp:\n            cur_rect = items[cur_pos].rect\n            cur_center_x = cur_rect.center().x()\n            best = cur_pos\n            best_dist = float(\"inf\")\n            for i in range(cur_pos - 1, -1, -1):\n                r = items[i].rect\n                if r.bottom() &lt; cur_rect.top():\n                    dist = abs(r.center().x() - cur_center_x)\n                    if dist &lt; best_dist:\n                        best_dist = dist\n                        best = i\n                    elif dist &gt; best_dist:\n                        break\n            new_pos = best\n        elif action == QAbstractItemView.CursorAction.MoveHome:\n            new_pos = 0\n        elif action == QAbstractItemView.CursorAction.MoveEnd:\n            new_pos = len(items) - 1\n        elif action == QAbstractItemView.CursorAction.MovePageUp:\n            vp_h = self.viewport().height()\n            cur_rect = items[cur_pos].rect\n            target_y = cur_rect.top() - vp_h\n            new_pos = 0\n            for i, item in enumerate(items):\n                if item.rect.top() &gt;= target_y:\n                    new_pos = i\n                    break\n        elif action == QAbstractItemView.CursorAction.MovePageDown:\n            vp_h = self.viewport().height()\n            cur_rect = items[cur_pos].rect\n            target_y = cur_rect.top() + vp_h\n            new_pos = len(items) - 1\n            for i in range(cur_pos, len(items)):\n                if items[i].rect.top() &gt;= target_y:\n                    new_pos = max(i - 1, cur_pos)\n                    break\n        else:\n            return current\n\n        target_item = items[new_pos]\n        if target_item.index.isValid():\n            return QModelIndex(target_item.index)  # type: ignore\n        return current\n\n    def isIndexHidden(\n        self, index: QModelIndex | QPersistentModelIndex\n    ) -&gt; bool:\n        if not self._is_tree_mode:\n            return False\n        parent = index.parent()\n        if not parent.isValid():\n            return False\n        node = None\n        model = self.model()\n        if isinstance(model, QSortFilterProxyModel):\n            src = model.mapToSource(parent)\n            node = src.internalPointer()\n        else:\n            node = parent.internalPointer()\n        if node is None:\n            return False\n        node_id = node.node_id\n        return node_id in self._collapsed_groups\n\n    def setSelection(\n        self, rect: QRect, flags: QItemSelectionModel.SelectionFlag\n    ) -&gt; None:\n        offset = self.verticalOffset()\n        content_rect = rect.translated(0, offset)\n        sel = QItemSelection()\n        model = self.model()\n        if model is None:\n            return\n        for item in self._all_layout_items():\n            if item.rect.intersects(content_rect) and item.index.isValid():\n                idx = QModelIndex(item.index)  # type: ignore\n                sel.select(idx, idx)\n        self.selectionModel().select(sel, flags)\n\n    def visualRegionForSelection(self, selection: QItemSelection) -&gt; QRegion:\n        region = QRegion()\n        for idx in selection.indexes():\n            r = self.visualRect(idx)\n            if not r.isNull():\n                region += QRegion(r)\n        return region\n\n    def updateGeometries(self) -&gt; None:\n        vsb = self.verticalScrollBar()\n        vp_height = self.viewport().height()\n        content_h = self._total_content_height\n        if content_h &gt; vp_height:\n            vsb.setRange(0, content_h - vp_height)\n            vsb.setPageStep(vp_height)\n            vsb.setSingleStep(self.scroll_step)\n        else:\n            vsb.setRange(0, 0)\n        super().updateGeometries()\n\n    def resizeEvent(self, event: Any) -&gt; None:\n        super().resizeEvent(event)\n        self._calculate_layout()\n        self._reposition_visible_editors()\n        self._schedule_editor_sync()\n\n    def paintEvent(self, event: QPaintEvent) -&gt; None:\n        painter = QPainter(self.viewport())\n        painter.setRenderHint(QPainter.RenderHint.Antialiasing, False)\n\n        style = get_ayon_style()\n        tbl_style = style.model.get_style(\"AYTableView\", self._variant_str)\n        tbl_style.set_context(self)\n        bg = QColor(tbl_style.get(\"background-color\", \"#252a31\"))\n        painter.fillRect(self.viewport().rect(), bg)\n\n        if self._is_tree_mode:\n            self._paint_group_headers(painter, tbl_style)\n\n        painter.end()\n\n    def _paint_group_headers(\n        self, painter: QPainter, tbl_style: dict[str, Any]\n    ) -&gt; None:\n        offset = self.verticalOffset()\n        vp_rect = self.viewport().rect()\n        header_bg = QColor(\"transparent\")\n        header_fg = QColor(tbl_style.get(\"header-color\", \"#c1c7ce\"))\n\n        expand_icon_size = 16\n        expand_icon_padding = 16\n        icon_text_padding = 8\n\n        # Set header font based on view font, but larger and bold.\n        font = painter.font()\n        header_font = QFont(font)\n        header_font.setPointSizeF(font.pointSizeF() + 3)\n        header_font.setBold(True)\n        painter.setFont(header_font)\n\n        for group in self._tree_layout:\n            visible_rect = group.header_rect.translated(0, -offset)\n            if visible_rect.bottom() &lt; 0:\n                continue\n            if visible_rect.top() &gt; vp_rect.bottom():\n                break\n\n            painter.fillRect(visible_rect, header_bg)\n\n            # Draw expand/collapse icon\n            icon_name = (\n                \"expand_more\" if not group.collapsed else \"chevron_right\"\n            )\n            try:\n                expand_icon = get_icon(icon_name, color=header_fg.name())\n                pixmap = expand_icon.pixmap(expand_icon_size, expand_icon_size)\n                icon_x = expand_icon_padding\n                icon_y = (\n                    visible_rect.top()\n                    + (visible_rect.height() - expand_icon_size) // 2\n                )\n                painter.drawPixmap(icon_x, icon_y, pixmap)\n            except Exception:\n                pass\n\n            # Resolve label color \u2014 ForegroundRole returns QBrush, not QColor\n            raw_color = group.label_color\n            if isinstance(raw_color, QBrush):\n                label_color: QColor | None = raw_color.color()\n            elif isinstance(raw_color, QColor):\n                label_color = raw_color\n            else:\n                label_color = None\n\n            # Draw label icon if present\n            text_x = expand_icon_padding + expand_icon_size + icon_text_padding\n            label_icon_size = 16\n            if isinstance(group.label_icon, QIcon):\n                try:\n                    licon_pixmap = group.label_icon.pixmap(\n                        label_icon_size, label_icon_size\n                    )\n                    if not licon_pixmap.isNull():\n                        licon_x = text_x\n                        licon_y = (\n                            visible_rect.top()\n                            + (visible_rect.height() - label_icon_size) // 2\n                        )\n                        painter.drawPixmap(licon_x, licon_y, licon_pixmap)\n                        text_x += label_icon_size + icon_text_padding\n                except Exception as err:\n                    log.debug(\n                        \"Failed to draw label icon: %s\", err, exc_info=True\n                    )\n\n            # Draw group label\n            text_rect = QRect(\n                text_x,\n                visible_rect.top(),\n                visible_rect.width() - text_x - expand_icon_padding,\n                visible_rect.height(),\n            )\n            painter.setPen(label_color or header_fg)\n\n            label = group.label\n            count_str = f\"  ({group.child_count})\"\n            painter.drawText(\n                text_rect,\n                Qt.AlignmentFlag.AlignVCenter | Qt.AlignmentFlag.AlignLeft,\n                label + count_str,\n            )\n        painter.setFont(font)\n\n    def mousePressEvent(self, event: QtGui.QMouseEvent) -&gt; None:\n        pos = event.pos()\n\n        if self._is_tree_mode and event.button() == Qt.MouseButton.LeftButton:\n            offset = self.verticalOffset()\n            content_y = pos.y() + offset\n            for group in self._tree_layout:\n                if group.header_rect.contains(QPoint(pos.x(), content_y)):\n                    self._toggle_group(group.node_id)\n                    return\n\n        idx = self.indexAt(pos)\n        if idx.isValid():\n            flags = QItemSelectionModel.SelectionFlag.ClearAndSelect\n            modifiers = event.modifiers()\n            if modifiers &amp; Qt.KeyboardModifier.ControlModifier:\n                flags = QItemSelectionModel.SelectionFlag.Toggle\n            elif modifiers &amp; Qt.KeyboardModifier.ShiftModifier:\n                flags = QItemSelectionModel.SelectionFlag.Select\n            self.selectionModel().select(QItemSelection(idx, idx), flags)\n            self.setCurrentIndex(idx)\n\n        super().mousePressEvent(event)\n\n    def mouseDoubleClickEvent(self, event: QtGui.QMouseEvent) -&gt; None:\n        idx = self.indexAt(event.pos())\n        if idx.isValid():\n            self.card_activated.emit(idx)\n        super().mouseDoubleClickEvent(event)\n\n    def mouseMoveEvent(self, event: QtGui.QMouseEvent) -&gt; None:\n        idx = self.indexAt(event.pos())\n        new_pmi = QPersistentModelIndex(idx) if idx.isValid() else None\n\n        if new_pmi != self._hovered_pmi:\n            if self._hovered_pmi is not None and self._hovered_pmi.isValid():\n                editor = self.indexWidget(QModelIndex(self._hovered_pmi))  # type: ignore\n                if isinstance(editor, AYEntityCard):\n                    editor.is_hover = False\n\n            self._hovered_pmi = new_pmi\n\n            if new_pmi is not None and new_pmi.isValid():\n                editor = self.indexWidget(QModelIndex(new_pmi))  # type: ignore\n                if isinstance(editor, AYEntityCard):\n                    editor.is_hover = True\n\n        super().mouseMoveEvent(event)\n\n    def leaveEvent(self, event: QtCore.QEvent) -&gt; None:\n        if self._hovered_pmi is not None and self._hovered_pmi.isValid():\n            editor = self.indexWidget(QModelIndex(self._hovered_pmi))  # type: ignore\n            if isinstance(editor, AYEntityCard):\n                editor.is_hover = False\n        self._hovered_pmi = None\n        super().leaveEvent(event)\n\n    def selectionChanged(\n        self,\n        selected: QItemSelection,\n        deselected: QItemSelection,\n    ) -&gt; None:\n        super().selectionChanged(selected, deselected)\n\n        for idx in deselected.indexes():\n            pmi = QPersistentModelIndex(idx)\n            if pmi in self._active_editor_pmis:\n                editor = self.indexWidget(QModelIndex(pmi))  # type: ignore\n                if isinstance(editor, AYEntityCard):\n                    editor.is_active = False\n\n        for idx in selected.indexes():\n            pmi = QPersistentModelIndex(idx)\n            if pmi in self._active_editor_pmis:\n                editor = self.indexWidget(QModelIndex(pmi))  # type: ignore\n                if isinstance(editor, AYEntityCard):\n                    editor.is_active = True\n\n        self.selection_changed.emit(selected, deselected)\n\n    def _toggle_group(self, node_id: str) -&gt; None:\n        if node_id in self._collapsed_groups:\n            self._collapsed_groups.discard(node_id)\n        else:\n            self._collapsed_groups.add(node_id)\n\n        self._active_editor_pmis.clear()\n        self._calculate_layout()\n        self._reposition_visible_editors()\n        self.viewport().update()\n        self._schedule_editor_sync()\n\n    def _reposition_visible_editors(self) -&gt; None:\n        offset = self.verticalOffset()\n        vp_rect = self.viewport().rect()\n\n        for item in self._all_layout_items():\n            if not item.index.isValid():\n                continue\n            visible_rect = item.rect.translated(0, -offset)\n            if (\n                visible_rect.bottom() &lt; 0\n                or visible_rect.top() &gt; vp_rect.bottom()\n            ):\n                continue\n            editor = self.indexWidget(QModelIndex(item.index))  # type: ignore\n            if editor is not None:\n                editor.setGeometry(visible_rect)\n\n    def _get_visible_indexes(self) -&gt; list[QModelIndex]:\n        offset = self.verticalOffset()\n        vp_rect = self.viewport().rect()\n        results = []\n        for item in self._all_layout_items():\n            if not item.index.isValid():\n                continue\n            visible_rect = item.rect.translated(0, -offset)\n            if visible_rect.bottom() &lt; 0:\n                continue\n            if visible_rect.top() &gt; vp_rect.bottom():\n                continue\n            results.append(QModelIndex(item.index))  # type: ignore\n        return results\n\n    def get_visible_indexes(self) -&gt; list[QModelIndex]:\n        \"\"\"Return model indices for cards currently visible in the viewport.\n\n        Returns:\n            List of valid ``QModelIndex`` objects whose card rects\n            intersect the viewport.\n        \"\"\"\n        self._calculate_layout()\n        return self._get_visible_indexes()\n\n    def refresh_visible_editors(self) -&gt; None:\n        \"\"\"Force ``setEditorData`` to be called for all visible card editors.\n\n        Removes the visible cards from the active-editor set so that the\n        next editor-sync pass re-opens their persistent editors and\n        updates their data.  This is useful when external state (e.g. a\n        thumbnail cache) has changed and the cards need to re-read it.\n        \"\"\"\n        for idx in self._get_visible_indexes():\n            self._active_editor_pmis.discard(QPersistentModelIndex(idx))\n        self._schedule_editor_sync()\n\n    def _maybe_fetch_more(self) -&gt; None:\n        \"\"\"Trigger model pagination when the viewport nears content bottom.\n\n        Must be called **after** ``_calculate_layout()`` has already run so\n        that ``_total_content_height`` is up-to-date.  Uses the display model\n        (``self.model()``) for all ``canFetchMore`` / ``fetchMore`` calls so\n        that proxy mapping is preserved.\n        \"\"\"\n        model = self.model()\n        if model is None:\n            return\n\n        vp_height = self.viewport().height()\n        offset = self.verticalOffset()\n        total_h = self._total_content_height\n\n        # Deduplicate parents so we never call fetchMore twice per pass.\n        fetched: set[QPersistentModelIndex] = set()\n\n        # --- flat-mode root fetch (also runs in tree mode for new top-level\n        #     groups) ---\n        root = QModelIndex()\n        near_bottom = (\n            total_h &lt;= vp_height  # content shorter than viewport\n            or offset + 2 * vp_height &gt;= total_h\n        )\n        root_pmi = QPersistentModelIndex(root)\n        if near_bottom and root_pmi not in fetched:\n            if model.canFetchMore(root):\n                model.fetchMore(root)\n            fetched.add(root_pmi)\n\n        # --- tree-mode: per-group child fetch ---\n        if not self._is_tree_mode:\n            return\n\n        for group in self._tree_layout:\n            if group.collapsed:\n                continue\n            if not group.parent_index.isValid():\n                continue\n\n            # Only fetch for groups whose content area is within the\n            # look-ahead window (2 \u00d7 viewport height ahead of current pos).\n            group_top = group.header_rect.top()\n            if group_top - offset &gt;= 2 * vp_height:\n                # Group is far below the fold \u2014 skip for now.\n                break\n\n            pmi = group.parent_index\n            if pmi in fetched:\n                continue\n            group_idx = QModelIndex(pmi)  # type: ignore\n            if model.canFetchMore(group_idx):\n                model.fetchMore(group_idx)\n            fetched.add(pmi)\n\n    def _sync_viewport_editors(self) -&gt; None:\n        self._calculate_layout()\n        offset = self.verticalOffset()\n        vp_rect = self.viewport().rect()\n\n        for item in self._all_layout_items():\n            if not item.index.isValid():\n                continue\n            visible_rect = item.rect.translated(0, -offset)\n            if visible_rect.bottom() &lt; 0:\n                continue\n            if visible_rect.top() &gt; vp_rect.bottom():\n                continue\n\n            pmi = item.index\n            if pmi not in self._active_editor_pmis:\n                self.openPersistentEditor(QModelIndex(pmi))  # type: ignore\n                self._active_editor_pmis.add(pmi)\n\n            editor = self.indexWidget(QModelIndex(pmi))  # type: ignore\n            if editor is not None:\n                editor.setGeometry(visible_rect)\n\n        sel_model = self.selectionModel()\n        if sel_model is not None:\n            for pmi in self._active_editor_pmis:\n                if not pmi.isValid():\n                    continue\n                idx = QModelIndex(pmi)  # type: ignore\n                editor = self.indexWidget(idx)\n                if isinstance(editor, AYEntityCard):\n                    editor.is_active = sel_model.isSelected(idx)\n\n        self._maybe_fetch_more()\n\n    @property\n    def card_width(self) -&gt; int:\n        return self._card_width\n\n    @card_width.setter\n    def card_width(self, value: int) -&gt; None:\n        self._card_width = value\n        self._delegate._card_width = value\n        self._active_editor_pmis.clear()\n        self._schedule_layout_update()\n\n    @property\n    def card_spacing(self) -&gt; int:\n        return self._card_spacing\n\n    @card_spacing.setter\n    def card_spacing(self, value: int) -&gt; None:\n        self._card_spacing = value\n        self._schedule_layout_update()\n\n    @property\n    def card_data_mapper(\n        self,\n    ) -&gt; Callable[[dict[str, Any]], dict[str, Any]] | None:\n        return self._card_data_mapper\n\n    @card_data_mapper.setter\n    def card_data_mapper(\n        self, value: Callable[[dict[str, Any]], dict[str, Any]] | None\n    ) -&gt; None:\n        self._card_data_mapper = value\n        self._delegate._card_data_mapper = value or (lambda r: {})\n        self._active_editor_pmis.clear()\n        self._schedule_layout_update()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/card_view.html#client.ayon_core.ui.components.card_view.AYCardView.get_visible_indexes","title":"<code>get_visible_indexes()</code>","text":"<p>Return model indices for cards currently visible in the viewport.</p> <p>Returns:</p> Type Description <code>list[QModelIndex]</code> <p>List of valid <code>QModelIndex</code> objects whose card rects</p> <code>list[QModelIndex]</code> <p>intersect the viewport.</p> Source code in <code>client/ayon_core/ui/components/card_view.py</code> <pre><code>def get_visible_indexes(self) -&gt; list[QModelIndex]:\n    \"\"\"Return model indices for cards currently visible in the viewport.\n\n    Returns:\n        List of valid ``QModelIndex`` objects whose card rects\n        intersect the viewport.\n    \"\"\"\n    self._calculate_layout()\n    return self._get_visible_indexes()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/card_view.html#client.ayon_core.ui.components.card_view.AYCardView.refresh_visible_editors","title":"<code>refresh_visible_editors()</code>","text":"<p>Force <code>setEditorData</code> to be called for all visible card editors.</p> <p>Removes the visible cards from the active-editor set so that the next editor-sync pass re-opens their persistent editors and updates their data.  This is useful when external state (e.g. a thumbnail cache) has changed and the cards need to re-read it.</p> Source code in <code>client/ayon_core/ui/components/card_view.py</code> <pre><code>def refresh_visible_editors(self) -&gt; None:\n    \"\"\"Force ``setEditorData`` to be called for all visible card editors.\n\n    Removes the visible cards from the active-editor set so that the\n    next editor-sync pass re-opens their persistent editors and\n    updates their data.  This is useful when external state (e.g. a\n    thumbnail cache) has changed and the cards need to re-read it.\n    \"\"\"\n    for idx in self._get_visible_indexes():\n        self._active_editor_pmis.discard(QPersistentModelIndex(idx))\n    self._schedule_editor_sync()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/check_box.html","title":"check_box","text":"<p>checkbox</p>"},{"location":"autoapi/client/ayon_core/ui/components/check_box.html#client.ayon_core.ui.components.check_box.AYCheckBox","title":"<code>AYCheckBox</code>","text":"<p>               Bases: <code>StyleMixin</code>, <code>QCheckBox</code></p> <p>AYON styled checkbox widget.</p> <p>Overrides Qt's stylesheet painting with AYONStyle custom rendering.</p> <p>Parameters:</p> Name Type Description Default <code>*args</code> <p>Positional arguments passed to QCheckBox.</p> <code>()</code> <code>**kwargs</code> <p>Keyword arguments passed to QCheckBox.</p> <code>{}</code> Source code in <code>client/ayon_core/ui/components/check_box.py</code> <pre><code>class AYCheckBox(StyleMixin, QCheckBox):\n    \"\"\"AYON styled checkbox widget.\n\n    Overrides Qt's stylesheet painting with AYONStyle custom rendering.\n\n    Args:\n        *args: Positional arguments passed to QCheckBox.\n        **kwargs: Keyword arguments passed to QCheckBox.\n    \"\"\"\n\n    Variants = QCheckBoxVariants\n\n    def __init__(\n        self,\n        *args,\n        variant: Variants = Variants.Default,\n        **kwargs,\n    ):\n        super().__init__(*args, **kwargs)\n        self._variant_str = variant.value\n        self._style_dict = None\n        self.setStyle(get_ayon_style())\n\n        if variant == AYCheckBox.Variants.Button:\n            self.setFixedSize(self.sizeHint())\n\n    @property\n    def style_dict(self):\n        if self._style_dict is None:\n            self._style_dict = get_ayon_style().model.get_style(\n                \"QCheckBox\", variant=self._variant_str\n            )\n            self._style_dict.set_context(self)\n        return self._style_dict\n\n    def initStyleOption(self, option: QStyleOptionButton) -&gt; None:\n        \"\"\"Initialize the style option with the default implementation, then\n        override any properties needed for our custom painting.\n\n        Args:\n            option: The style option to initialize.\n        \"\"\"\n        super().initStyleOption(option)\n        option.fontMetrics = self.fontMetrics()\n\n    def paintEvent(self, arg__1: QPaintEvent) -&gt; None:\n        \"\"\"Render the checkbox using the AYON custom style.\n\n        Args:\n            arg__1: The paint event delivered by Qt.\n        \"\"\"\n        p = QPainter(self)\n        p.setFont(self.font())\n\n        option = QStyleOptionButton()\n        self.initStyleOption(option)\n        _style = get_ayon_style()\n\n        _expanding = self.sizePolicy().horizontalPolicy() in (\n            QSizePolicy.Policy.Expanding,\n            QSizePolicy.Policy.MinimumExpanding,\n        )\n        if _expanding:\n            ind_w = _style.pixelMetric(\n                QStyle.PixelMetric.PM_IndicatorWidth, option, self\n            )\n            ind_h = _style.pixelMetric(\n                QStyle.PixelMetric.PM_IndicatorHeight, option, self\n            )\n            spacing = _style.pixelMetric(\n                QStyle.PixelMetric.PM_CheckBoxLabelSpacing, option, self\n            )\n            cy = self.height() // 2\n\n            ind_opt = QStyleOptionButton(option)\n            ind_opt.rect = QRect(0, cy - ind_h // 2, ind_w, ind_h)\n            _style.drawPrimitive(\n                QStyle.PrimitiveElement.PE_IndicatorCheckBox, ind_opt, p, self\n            )\n\n            label_opt = QStyleOptionButton(option)\n            label_opt.rect = QRect(\n                ind_w + spacing,\n                0,\n                self.width() - ind_w - spacing,\n                self.height(),\n            )\n            _style.drawControl(\n                QStyle.ControlElement.CE_CheckBoxLabel, label_opt, p, self\n            )\n        else:\n            _style.drawControl(\n                QStyle.ControlElement.CE_CheckBox, option, p, self\n            )\n\n    def sizeHint(self) -&gt; QSize:\n        size = super().sizeHint()\n\n        if self._variant_str == AYCheckBox.Variants.Button.value:\n            h_pad, v_pad = self.style_dict.get(\"padding\", [6, 6])\n            size.setWidth(size.width() + h_pad * 2)\n            size.setHeight(size.height() + v_pad * 2)\n        else:\n            # Recalculate width using the custom style's actual metrics\n            option = QStyleOptionButton()\n            self.initStyleOption(option)\n            _style = get_ayon_style()\n            ind_w = _style.pixelMetric(\n                QStyle.PixelMetric.PM_IndicatorWidth, option, self\n            )\n            spacing = _style.pixelMetric(\n                QStyle.PixelMetric.PM_CheckBoxLabelSpacing, option, self\n            )\n            fm = self.fontMetrics()\n            text_w = fm.horizontalAdvance(self.text())\n            size.setWidth(ind_w + spacing + text_w)\n\n        return size\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/check_box.html#client.ayon_core.ui.components.check_box.AYCheckBox.initStyleOption","title":"<code>initStyleOption(option)</code>","text":"<p>Initialize the style option with the default implementation, then override any properties needed for our custom painting.</p> <p>Parameters:</p> Name Type Description Default <code>option</code> <code>QStyleOptionButton</code> <p>The style option to initialize.</p> required Source code in <code>client/ayon_core/ui/components/check_box.py</code> <pre><code>def initStyleOption(self, option: QStyleOptionButton) -&gt; None:\n    \"\"\"Initialize the style option with the default implementation, then\n    override any properties needed for our custom painting.\n\n    Args:\n        option: The style option to initialize.\n    \"\"\"\n    super().initStyleOption(option)\n    option.fontMetrics = self.fontMetrics()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/check_box.html#client.ayon_core.ui.components.check_box.AYCheckBox.paintEvent","title":"<code>paintEvent(arg__1)</code>","text":"<p>Render the checkbox using the AYON custom style.</p> <p>Parameters:</p> Name Type Description Default <code>arg__1</code> <code>QPaintEvent</code> <p>The paint event delivered by Qt.</p> required Source code in <code>client/ayon_core/ui/components/check_box.py</code> <pre><code>def paintEvent(self, arg__1: QPaintEvent) -&gt; None:\n    \"\"\"Render the checkbox using the AYON custom style.\n\n    Args:\n        arg__1: The paint event delivered by Qt.\n    \"\"\"\n    p = QPainter(self)\n    p.setFont(self.font())\n\n    option = QStyleOptionButton()\n    self.initStyleOption(option)\n    _style = get_ayon_style()\n\n    _expanding = self.sizePolicy().horizontalPolicy() in (\n        QSizePolicy.Policy.Expanding,\n        QSizePolicy.Policy.MinimumExpanding,\n    )\n    if _expanding:\n        ind_w = _style.pixelMetric(\n            QStyle.PixelMetric.PM_IndicatorWidth, option, self\n        )\n        ind_h = _style.pixelMetric(\n            QStyle.PixelMetric.PM_IndicatorHeight, option, self\n        )\n        spacing = _style.pixelMetric(\n            QStyle.PixelMetric.PM_CheckBoxLabelSpacing, option, self\n        )\n        cy = self.height() // 2\n\n        ind_opt = QStyleOptionButton(option)\n        ind_opt.rect = QRect(0, cy - ind_h // 2, ind_w, ind_h)\n        _style.drawPrimitive(\n            QStyle.PrimitiveElement.PE_IndicatorCheckBox, ind_opt, p, self\n        )\n\n        label_opt = QStyleOptionButton(option)\n        label_opt.rect = QRect(\n            ind_w + spacing,\n            0,\n            self.width() - ind_w - spacing,\n            self.height(),\n        )\n        _style.drawControl(\n            QStyle.ControlElement.CE_CheckBoxLabel, label_opt, p, self\n        )\n    else:\n        _style.drawControl(\n            QStyle.ControlElement.CE_CheckBox, option, p, self\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/checkbox_handler.html","title":"checkbox_handler","text":"<p>Checkbox handler for markdown checkboxes in QTextEdit.</p>"},{"location":"autoapi/client/ayon_core/ui/components/checkbox_handler.html#client.ayon_core.ui.components.checkbox_handler.CheckboxHandler","title":"<code>CheckboxHandler</code>","text":"<p>               Bases: <code>QObject</code></p> <p>Handles checkbox parsing, rendering, and state management.</p> <p>This class manages: - Parsing markdown to extract checkbox items - Registering custom text objects for checkbox rendering - Tracking checkbox state and positions - Converting back to markdown format</p> Signals <p>checklist_changed: Emitted when any checkbox state changes.</p> Source code in <code>client/ayon_core/ui/components/checkbox_handler.py</code> <pre><code>class CheckboxHandler(QObject):\n    \"\"\"Handles checkbox parsing, rendering, and state management.\n\n    This class manages:\n    - Parsing markdown to extract checkbox items\n    - Registering custom text objects for checkbox rendering\n    - Tracking checkbox state and positions\n    - Converting back to markdown format\n\n    Signals:\n        checklist_changed: Emitted when any checkbox state changes.\n    \"\"\"\n\n    checklist_changed = Signal()\n\n    # GitHub-flavored markdown checkbox patterns\n    # Matches: - [ ] text, - [x] text, * [ ] text, * [X] text, + [ ] text\n    CHECKBOX_PATTERN = re.compile(\n        r\"^(\\s*[-*+]\\s+)\\[([xX ])\\]\\s*(.*)$\", re.MULTILINE\n    )\n\n    def __init__(self, text_edit: QTextEdit, parent: QObject | None = None):\n        \"\"\"Initialize the checkbox handler.\n\n        Args:\n            text_edit: The QTextEdit widget to manage checkboxes for.\n            parent: Optional parent QObject.\n        \"\"\"\n        super().__init__(parent)\n        self._text_edit = text_edit\n        self._checkboxes: list[CheckboxItem] = []\n        self._checkbox_object: CheckboxTextObject | None = None\n        self._original_markdown: str = \"\"\n        self._register_handler()\n\n    def _register_handler(self) -&gt; None:\n        \"\"\"Register the custom checkbox text object with the document.\"\"\"\n        self._checkbox_object = CheckboxTextObject(self)\n        doc = self._text_edit.document()\n        doc_layout = doc.documentLayout()\n        doc_layout.registerHandler(CHECKBOX_FORMAT_TYPE, self._checkbox_object)\n\n    def parse_and_render(self, markdown: str) -&gt; None:\n        \"\"\"Parse markdown and render checkboxes in the document.\n\n        Extracts checkbox items from markdown, stores their state,\n        and renders them as custom text objects with icons.\n\n        Args:\n            markdown: The markdown string containing checkboxes.\n        \"\"\"\n        self._original_markdown = markdown\n        self._checkboxes.clear()\n\n        # Find all checkbox patterns\n        lines = markdown.split(\"\\n\")\n        processed_lines: list[str] = []\n        checkbox_index = 0\n\n        for line_num, line in enumerate(lines):\n            match = self.CHECKBOX_PATTERN.match(line)\n            if match:\n                prefix = match.group(1)\n                checked_char = match.group(2)\n                text = match.group(3)\n                is_checked = checked_char.lower() == \"x\"\n\n                # Store checkbox info (doc_position filled after insertion)\n                self._checkboxes.append(\n                    CheckboxItem(\n                        index=checkbox_index,\n                        checked=is_checked,\n                        text=text,\n                        line_number=line_num,\n                        prefix=prefix,\n                    )\n                )\n\n                # Replace checkbox syntax with placeholder\n                # We'll insert the actual object after setting plain text\n                processed_lines.append(f\"{prefix}\\ufffc {text}\")\n                checkbox_index += 1\n            else:\n                processed_lines.append(line)\n\n        # Set the processed text\n        processed_text = \"\\n\".join(processed_lines)\n        self._text_edit.setMarkdown(processed_text)\n        self._remove_list_bullets()\n\n        # Now insert checkbox objects at placeholder positions\n        self._insert_checkbox_objects()\n\n    def _remove_list_bullets(self) -&gt; None:\n        \"\"\"Remove bullet markers from list blocks containing checkboxes and\n        correct the indentation to match front-end.\"\"\"\n        doc = self._text_edit.document()\n        processed_lists: set[int] = set()\n\n        block = doc.begin()\n        while block.isValid():\n            text_list = block.textList()\n            if text_list and id(text_list) not in processed_lists:\n                list_key = text_list.item(0).position()\n                if \"\\ufffc\" in block.text():\n                    fmt = text_list.format()\n                    fmt.setStyle(QTextListFormat.Style.ListStyleUndefined)\n                    fmt.setIndent(0)\n                    text_list.setFormat(fmt)\n                    processed_lists.add(list_key)\n            block = block.next()\n\n    def _insert_checkbox_objects(self) -&gt; None:\n        \"\"\"Insert checkbox custom objects at placeholder positions.\"\"\"\n        doc = self._text_edit.document()\n        doc.blockSignals(True)\n\n        cursor = QTextCursor(doc)\n        cursor.beginEditBlock()\n\n        # Find all placeholder characters and replace with checkbox objects\n        text = doc.toPlainText()\n        offset = 0\n\n        for i, cb in enumerate(self._checkboxes):\n            # Find the placeholder character (OBJECT REPLACEMENT CHARACTER)\n            pos = text.find(\"\\ufffc\", offset)\n            if pos == -1:\n                log.warning(\"Could not find placeholder for checkbox %d\", i)\n                continue\n\n            # Create format for the checkbox\n            fmt = QTextCharFormat()\n            fmt.setObjectType(CHECKBOX_FORMAT_TYPE)\n            fmt.setProperty(CHECKBOX_CHECKED_PROP, cb.checked)\n            fmt.setProperty(CHECKBOX_INDEX_PROP, cb.index)\n            fmt.setVerticalAlignment(\n                QTextCharFormat.VerticalAlignment.AlignBaseline\n            )\n\n            # Select and replace the placeholder\n            cursor.setPosition(pos)\n            cursor.setPosition(pos + 1, QTextCursor.MoveMode.KeepAnchor)\n            cursor.insertText(\"\\ufffc\", fmt)\n\n            # Store the document position for fast hit-testing\n            cb.doc_position = pos\n\n            offset = pos + 1\n\n        cursor.endEditBlock()\n        doc.blockSignals(False)\n\n    def get_checkbox_at_position(self, pos: int) -&gt; int | None:\n        \"\"\"Get the checkbox index at the given document position.\n\n        Args:\n            pos: Position in the document (one past the \\\\ufffc char).\n\n        Returns:\n            Checkbox index if found, None otherwise.\n        \"\"\"\n        doc = self._text_edit.document()\n        cursor = QTextCursor(doc)\n        cursor.setPosition(pos)\n        fmt = cursor.charFormat()\n        fmt_type = fmt.objectType()\n\n        if fmt_type == CHECKBOX_FORMAT_TYPE:\n            return fmt.property(CHECKBOX_INDEX_PROP)\n        return None\n\n    def find_checkbox_at_click(\n        self, click_pos: \"QPoint\", content_offset: \"QPoint | None\" = None\n    ) -&gt; tuple[bool, int | None]:\n        \"\"\"Find the checkbox hit by a viewport click using stored positions.\n\n        Uses the cached ``doc_position`` on each :class:`CheckboxItem`\n        instead of scanning the full document text, making hit-testing O(n)\n        in the number of checkboxes rather than O(len(document)).\n\n        Args:\n            click_pos: Click position in viewport coordinates.\n            content_offset: Optional scroll offset (x, y) as a QPoint.\n                Defaults to (0, 0) when not provided.\n\n        Returns:\n            ``(True, doc_position + 1)`` when the click lands on a checkbox,\n            ``(False, None)`` otherwise.\n        \"\"\"\n        if not self._checkboxes:\n            return False, None\n\n        doc = self._text_edit.document()\n        ox = content_offset.x() if content_offset else 0\n        oy = content_offset.y() if content_offset else 0\n\n        for cb in self._checkboxes:\n            pos = cb.doc_position\n            if pos &lt; 0:\n                continue\n\n            cursor = QTextCursor(doc)\n            cursor.setPosition(pos)\n            block = cursor.block()\n            block_layout = block.layout()\n\n            if not block_layout:\n                continue\n\n            rel_pos = pos - block.position()\n            line = block_layout.lineForTextPosition(rel_pos)\n            if not line.isValid():\n                continue\n\n            x1 = line.cursorToX(rel_pos)[0]  # type: ignore[index]\n            x2 = line.cursorToX(rel_pos + 1)[0]  # type: ignore[index]\n            y = line.y() + block_layout.position().y()\n            h = line.height()\n\n            rect = QRectF(x1 + ox, y + oy, x2 - x1, h)\n            if rect.contains(QPointF(click_pos)):\n                return True, pos + 1\n\n        return False, None\n\n    def toggle_checkbox(self, index: int) -&gt; bool:\n        \"\"\"Toggle a checkbox by its index.\n\n        Args:\n            index: The index of the checkbox to toggle.\n\n        Returns:\n            True if the checkbox was toggled, False otherwise.\n        \"\"\"\n        if not 0 &lt;= index &lt; len(self._checkboxes):\n            return False\n\n        # Toggle the state\n        self._checkboxes[index].checked = not self._checkboxes[index].checked\n\n        # Update the document\n        self._update_checkbox_in_document(index)\n\n        # Emit signal\n        self.checklist_changed.emit()\n        return True\n\n    def _update_checkbox_in_document(self, index: int) -&gt; None:\n        \"\"\"Update a single checkbox display in the document.\n\n        Args:\n            index: The index of the checkbox to update.\n        \"\"\"\n        doc = self._text_edit.document()\n        cb = self._checkboxes[index]\n\n        # Block signals to prevent triggering format_comment_on_change\n        # which would clear all formatting including checkboxes\n        doc.blockSignals(True)\n\n        try:\n            pos = cb.doc_position\n            if pos &lt; 0:\n                # Fall back to scanning if position is unknown\n                text = doc.toPlainText()\n                count = 0\n                for i, char in enumerate(text):\n                    if char == \"\\ufffc\":\n                        if count == index:\n                            pos = i\n                            break\n                        count += 1\n\n            if pos &lt; 0:\n                log.warning(\n                    \"Cannot find document position for checkbox %d\", index\n                )\n                return\n\n            cursor = QTextCursor(doc)\n            cursor.setPosition(pos)\n            cursor.setPosition(pos + 1, QTextCursor.MoveMode.KeepAnchor)\n\n            fmt = QTextCharFormat()\n            fmt.setObjectType(CHECKBOX_FORMAT_TYPE)\n            fmt.setProperty(CHECKBOX_CHECKED_PROP, cb.checked)\n            fmt.setProperty(CHECKBOX_INDEX_PROP, cb.index)\n            fmt.setVerticalAlignment(\n                QTextCharFormat.VerticalAlignment.AlignBaseline\n            )\n\n            cursor.setCharFormat(fmt)\n        finally:\n            doc.blockSignals(False)\n\n    def add_checkbox(\n        self,\n        checked: bool = False,\n        prefix: str = \"- \",\n        doc_position: int = -1,\n    ) -&gt; CheckboxItem:\n        \"\"\"Append a new checkbox to the tracked list.\n\n        This is the public API for adding a checkbox programmatically\n        (e.g. when the user presses Enter on a checkbox line or clicks\n        the checklist toolbar button).  The caller is responsible for\n        inserting the actual ``\\\\ufffc`` character into the document and\n        then updating :attr:`CheckboxItem.doc_position`.\n\n        Args:\n            checked: Initial checked state.\n            prefix: Markdown list prefix (e.g. ``\"- \"``).\n            doc_position: Document character position of the ``\\\\ufffc``\n                object, or ``-1`` if not yet known.\n\n        Returns:\n            The newly created :class:`CheckboxItem`.\n        \"\"\"\n        new_index = len(self._checkboxes)\n        item = CheckboxItem(\n            index=new_index,\n            checked=checked,\n            text=\"\",\n            line_number=-1,\n            prefix=prefix,\n            doc_position=doc_position,\n        )\n        self._checkboxes.append(item)\n        return item\n\n    def remove_checkbox(self, index: int) -&gt; None:\n        \"\"\"Remove a checkbox from the tracked list.\n\n        Re-indexes the Python list **and** patches the stale\n        ``CHECKBOX_INDEX_PROP`` values stored in the live\n        ``QTextDocument`` so that subsequent hit-tests via\n        :meth:`get_checkbox_at_position` return correct indices.\n\n        Args:\n            index: The index of the checkbox to remove.\n        \"\"\"\n        if not 0 &lt;= index &lt; len(self._checkboxes):\n            return\n\n        self._checkboxes.pop(index)\n\n        # Re-index the Python list and sync each affected document char format.\n        doc = self._text_edit.document()\n        doc.blockSignals(True)\n        cursor = QTextCursor(doc)\n        cursor.beginEditBlock()\n        try:\n            for i, cb in enumerate(self._checkboxes[index:], start=index):\n                cb.index = i\n                pos = cb.doc_position\n                if pos &lt; 0:\n                    continue\n                cursor.setPosition(pos)\n                cursor.setPosition(pos + 1, QTextCursor.MoveMode.KeepAnchor)\n                fmt = QTextCharFormat()\n                fmt.setObjectType(CHECKBOX_FORMAT_TYPE)\n                fmt.setProperty(CHECKBOX_CHECKED_PROP, cb.checked)\n                fmt.setProperty(CHECKBOX_INDEX_PROP, cb.index)\n                fmt.setVerticalAlignment(\n                    QTextCharFormat.VerticalAlignment.AlignBaseline\n                )\n                cursor.setCharFormat(fmt)\n        except Exception:\n            log.warning(\n                \"Failed to re-index checkboxes in document\", exc_info=True\n            )\n        finally:\n            cursor.endEditBlock()\n            doc.blockSignals(False)\n\n    def remove_last_checkbox(self) -&gt; bool:\n        \"\"\"Remove the last checkbox from the tracked list.\n\n        Used when the user presses Enter on an empty checkbox line to\n        terminate the list.\n\n        Returns:\n            True if a checkbox was removed, False if the list was empty.\n        \"\"\"\n        if not self._checkboxes:\n            return False\n        self._checkboxes.pop()\n        return True\n\n    def to_markdown(self) -&gt; str:\n        \"\"\"Reconstruct markdown with current checkbox states.\n\n        Returns:\n            GitHub-flavored markdown string with checkbox syntax.\n        \"\"\"\n        if not self._checkboxes:\n            return self._text_edit.toMarkdown(MD_DIALECT)\n\n        # Get current text and replace placeholders with checkbox syntax\n        # we use toMarkdown() to make sure the other formatted items have\n        # already been dealt with.\n        text = self._text_edit.toMarkdown(MD_DIALECT)\n        lines = text.split(\"\\n\")\n        result_lines: list[str] = []\n\n        checkbox_iter = iter(self._checkboxes)\n        current_cb = next(checkbox_iter, None)\n\n        for line in lines:\n            if \"\\ufffc\" in line and current_cb:\n                checked_char = \"x\" if current_cb.checked else \" \"\n                cb_idx = line.index(\"\\ufffc\")\n                raw_after = line[cb_idx + 1 :]  # everything after \\ufffc\n                raw_after = raw_after.lstrip(\" \")  # drop the space Qt inserts\n                raw_after = _PH_RE.sub(\"\", raw_after).rstrip()\n                line = f\"{current_cb.prefix}[{checked_char}] {raw_after}\"\n                current_cb = next(checkbox_iter, None)\n            result_lines.append(line)\n\n        return \"\\n\".join(result_lines)\n\n    @property\n    def checkboxes(self) -&gt; list[CheckboxItem]:\n        \"\"\"Get the list of checkbox items.\n\n        Returns:\n            List of CheckboxItem objects.\n        \"\"\"\n        return self._checkboxes.copy()\n\n    def has_checkboxes(self) -&gt; bool:\n        \"\"\"Check if the document contains any checkboxes.\n\n        Returns:\n            True if there are checkboxes, False otherwise.\n        \"\"\"\n        return bool(self._checkboxes)\n\n    @staticmethod\n    def contains_checkboxes(markdown: str) -&gt; bool:\n        \"\"\"Check if markdown text contains checkbox syntax.\n\n        Args:\n            markdown: The markdown string to check.\n\n        Returns:\n            True if checkbox syntax is found, False otherwise.\n        \"\"\"\n        return bool(CheckboxHandler.CHECKBOX_PATTERN.search(markdown))\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/checkbox_handler.html#client.ayon_core.ui.components.checkbox_handler.CheckboxHandler.checkboxes","title":"<code>checkboxes</code>  <code>property</code>","text":"<p>Get the list of checkbox items.</p> <p>Returns:</p> Type Description <code>list[CheckboxItem]</code> <p>List of CheckboxItem objects.</p>"},{"location":"autoapi/client/ayon_core/ui/components/checkbox_handler.html#client.ayon_core.ui.components.checkbox_handler.CheckboxHandler.__init__","title":"<code>__init__(text_edit, parent=None)</code>","text":"<p>Initialize the checkbox handler.</p> <p>Parameters:</p> Name Type Description Default <code>text_edit</code> <code>QTextEdit</code> <p>The QTextEdit widget to manage checkboxes for.</p> required <code>parent</code> <code>QObject | None</code> <p>Optional parent QObject.</p> <code>None</code> Source code in <code>client/ayon_core/ui/components/checkbox_handler.py</code> <pre><code>def __init__(self, text_edit: QTextEdit, parent: QObject | None = None):\n    \"\"\"Initialize the checkbox handler.\n\n    Args:\n        text_edit: The QTextEdit widget to manage checkboxes for.\n        parent: Optional parent QObject.\n    \"\"\"\n    super().__init__(parent)\n    self._text_edit = text_edit\n    self._checkboxes: list[CheckboxItem] = []\n    self._checkbox_object: CheckboxTextObject | None = None\n    self._original_markdown: str = \"\"\n    self._register_handler()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/checkbox_handler.html#client.ayon_core.ui.components.checkbox_handler.CheckboxHandler.add_checkbox","title":"<code>add_checkbox(checked=False, prefix='- ', doc_position=-1)</code>","text":"<p>Append a new checkbox to the tracked list.</p> <p>This is the public API for adding a checkbox programmatically (e.g. when the user presses Enter on a checkbox line or clicks the checklist toolbar button).  The caller is responsible for inserting the actual <code>\\ufffc</code> character into the document and then updating :attr:<code>CheckboxItem.doc_position</code>.</p> <p>Parameters:</p> Name Type Description Default <code>checked</code> <code>bool</code> <p>Initial checked state.</p> <code>False</code> <code>prefix</code> <code>str</code> <p>Markdown list prefix (e.g. <code>\"- \"</code>).</p> <code>'- '</code> <code>doc_position</code> <code>int</code> <p>Document character position of the <code>\\ufffc</code> object, or <code>-1</code> if not yet known.</p> <code>-1</code> <p>Returns:</p> Type Description <code>CheckboxItem</code> <p>The newly created :class:<code>CheckboxItem</code>.</p> Source code in <code>client/ayon_core/ui/components/checkbox_handler.py</code> <pre><code>def add_checkbox(\n    self,\n    checked: bool = False,\n    prefix: str = \"- \",\n    doc_position: int = -1,\n) -&gt; CheckboxItem:\n    \"\"\"Append a new checkbox to the tracked list.\n\n    This is the public API for adding a checkbox programmatically\n    (e.g. when the user presses Enter on a checkbox line or clicks\n    the checklist toolbar button).  The caller is responsible for\n    inserting the actual ``\\\\ufffc`` character into the document and\n    then updating :attr:`CheckboxItem.doc_position`.\n\n    Args:\n        checked: Initial checked state.\n        prefix: Markdown list prefix (e.g. ``\"- \"``).\n        doc_position: Document character position of the ``\\\\ufffc``\n            object, or ``-1`` if not yet known.\n\n    Returns:\n        The newly created :class:`CheckboxItem`.\n    \"\"\"\n    new_index = len(self._checkboxes)\n    item = CheckboxItem(\n        index=new_index,\n        checked=checked,\n        text=\"\",\n        line_number=-1,\n        prefix=prefix,\n        doc_position=doc_position,\n    )\n    self._checkboxes.append(item)\n    return item\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/checkbox_handler.html#client.ayon_core.ui.components.checkbox_handler.CheckboxHandler.contains_checkboxes","title":"<code>contains_checkboxes(markdown)</code>  <code>staticmethod</code>","text":"<p>Check if markdown text contains checkbox syntax.</p> <p>Parameters:</p> Name Type Description Default <code>markdown</code> <code>str</code> <p>The markdown string to check.</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if checkbox syntax is found, False otherwise.</p> Source code in <code>client/ayon_core/ui/components/checkbox_handler.py</code> <pre><code>@staticmethod\ndef contains_checkboxes(markdown: str) -&gt; bool:\n    \"\"\"Check if markdown text contains checkbox syntax.\n\n    Args:\n        markdown: The markdown string to check.\n\n    Returns:\n        True if checkbox syntax is found, False otherwise.\n    \"\"\"\n    return bool(CheckboxHandler.CHECKBOX_PATTERN.search(markdown))\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/checkbox_handler.html#client.ayon_core.ui.components.checkbox_handler.CheckboxHandler.find_checkbox_at_click","title":"<code>find_checkbox_at_click(click_pos, content_offset=None)</code>","text":"<p>Find the checkbox hit by a viewport click using stored positions.</p> <p>Uses the cached <code>doc_position</code> on each :class:<code>CheckboxItem</code> instead of scanning the full document text, making hit-testing O(n) in the number of checkboxes rather than O(len(document)).</p> <p>Parameters:</p> Name Type Description Default <code>click_pos</code> <code>'QPoint'</code> <p>Click position in viewport coordinates.</p> required <code>content_offset</code> <code>'QPoint | None'</code> <p>Optional scroll offset (x, y) as a QPoint. Defaults to (0, 0) when not provided.</p> <code>None</code> <p>Returns:</p> Type Description <code>bool</code> <p><code>(True, doc_position + 1)</code> when the click lands on a checkbox,</p> <code>int | None</code> <p><code>(False, None)</code> otherwise.</p> Source code in <code>client/ayon_core/ui/components/checkbox_handler.py</code> <pre><code>def find_checkbox_at_click(\n    self, click_pos: \"QPoint\", content_offset: \"QPoint | None\" = None\n) -&gt; tuple[bool, int | None]:\n    \"\"\"Find the checkbox hit by a viewport click using stored positions.\n\n    Uses the cached ``doc_position`` on each :class:`CheckboxItem`\n    instead of scanning the full document text, making hit-testing O(n)\n    in the number of checkboxes rather than O(len(document)).\n\n    Args:\n        click_pos: Click position in viewport coordinates.\n        content_offset: Optional scroll offset (x, y) as a QPoint.\n            Defaults to (0, 0) when not provided.\n\n    Returns:\n        ``(True, doc_position + 1)`` when the click lands on a checkbox,\n        ``(False, None)`` otherwise.\n    \"\"\"\n    if not self._checkboxes:\n        return False, None\n\n    doc = self._text_edit.document()\n    ox = content_offset.x() if content_offset else 0\n    oy = content_offset.y() if content_offset else 0\n\n    for cb in self._checkboxes:\n        pos = cb.doc_position\n        if pos &lt; 0:\n            continue\n\n        cursor = QTextCursor(doc)\n        cursor.setPosition(pos)\n        block = cursor.block()\n        block_layout = block.layout()\n\n        if not block_layout:\n            continue\n\n        rel_pos = pos - block.position()\n        line = block_layout.lineForTextPosition(rel_pos)\n        if not line.isValid():\n            continue\n\n        x1 = line.cursorToX(rel_pos)[0]  # type: ignore[index]\n        x2 = line.cursorToX(rel_pos + 1)[0]  # type: ignore[index]\n        y = line.y() + block_layout.position().y()\n        h = line.height()\n\n        rect = QRectF(x1 + ox, y + oy, x2 - x1, h)\n        if rect.contains(QPointF(click_pos)):\n            return True, pos + 1\n\n    return False, None\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/checkbox_handler.html#client.ayon_core.ui.components.checkbox_handler.CheckboxHandler.get_checkbox_at_position","title":"<code>get_checkbox_at_position(pos)</code>","text":"<p>Get the checkbox index at the given document position.</p> <p>Parameters:</p> Name Type Description Default <code>pos</code> <code>int</code> <p>Position in the document (one past the \\ufffc char).</p> required <p>Returns:</p> Type Description <code>int | None</code> <p>Checkbox index if found, None otherwise.</p> Source code in <code>client/ayon_core/ui/components/checkbox_handler.py</code> <pre><code>def get_checkbox_at_position(self, pos: int) -&gt; int | None:\n    \"\"\"Get the checkbox index at the given document position.\n\n    Args:\n        pos: Position in the document (one past the \\\\ufffc char).\n\n    Returns:\n        Checkbox index if found, None otherwise.\n    \"\"\"\n    doc = self._text_edit.document()\n    cursor = QTextCursor(doc)\n    cursor.setPosition(pos)\n    fmt = cursor.charFormat()\n    fmt_type = fmt.objectType()\n\n    if fmt_type == CHECKBOX_FORMAT_TYPE:\n        return fmt.property(CHECKBOX_INDEX_PROP)\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/checkbox_handler.html#client.ayon_core.ui.components.checkbox_handler.CheckboxHandler.has_checkboxes","title":"<code>has_checkboxes()</code>","text":"<p>Check if the document contains any checkboxes.</p> <p>Returns:</p> Type Description <code>bool</code> <p>True if there are checkboxes, False otherwise.</p> Source code in <code>client/ayon_core/ui/components/checkbox_handler.py</code> <pre><code>def has_checkboxes(self) -&gt; bool:\n    \"\"\"Check if the document contains any checkboxes.\n\n    Returns:\n        True if there are checkboxes, False otherwise.\n    \"\"\"\n    return bool(self._checkboxes)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/checkbox_handler.html#client.ayon_core.ui.components.checkbox_handler.CheckboxHandler.parse_and_render","title":"<code>parse_and_render(markdown)</code>","text":"<p>Parse markdown and render checkboxes in the document.</p> <p>Extracts checkbox items from markdown, stores their state, and renders them as custom text objects with icons.</p> <p>Parameters:</p> Name Type Description Default <code>markdown</code> <code>str</code> <p>The markdown string containing checkboxes.</p> required Source code in <code>client/ayon_core/ui/components/checkbox_handler.py</code> <pre><code>def parse_and_render(self, markdown: str) -&gt; None:\n    \"\"\"Parse markdown and render checkboxes in the document.\n\n    Extracts checkbox items from markdown, stores their state,\n    and renders them as custom text objects with icons.\n\n    Args:\n        markdown: The markdown string containing checkboxes.\n    \"\"\"\n    self._original_markdown = markdown\n    self._checkboxes.clear()\n\n    # Find all checkbox patterns\n    lines = markdown.split(\"\\n\")\n    processed_lines: list[str] = []\n    checkbox_index = 0\n\n    for line_num, line in enumerate(lines):\n        match = self.CHECKBOX_PATTERN.match(line)\n        if match:\n            prefix = match.group(1)\n            checked_char = match.group(2)\n            text = match.group(3)\n            is_checked = checked_char.lower() == \"x\"\n\n            # Store checkbox info (doc_position filled after insertion)\n            self._checkboxes.append(\n                CheckboxItem(\n                    index=checkbox_index,\n                    checked=is_checked,\n                    text=text,\n                    line_number=line_num,\n                    prefix=prefix,\n                )\n            )\n\n            # Replace checkbox syntax with placeholder\n            # We'll insert the actual object after setting plain text\n            processed_lines.append(f\"{prefix}\\ufffc {text}\")\n            checkbox_index += 1\n        else:\n            processed_lines.append(line)\n\n    # Set the processed text\n    processed_text = \"\\n\".join(processed_lines)\n    self._text_edit.setMarkdown(processed_text)\n    self._remove_list_bullets()\n\n    # Now insert checkbox objects at placeholder positions\n    self._insert_checkbox_objects()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/checkbox_handler.html#client.ayon_core.ui.components.checkbox_handler.CheckboxHandler.remove_checkbox","title":"<code>remove_checkbox(index)</code>","text":"<p>Remove a checkbox from the tracked list.</p> <p>Re-indexes the Python list and patches the stale <code>CHECKBOX_INDEX_PROP</code> values stored in the live <code>QTextDocument</code> so that subsequent hit-tests via :meth:<code>get_checkbox_at_position</code> return correct indices.</p> <p>Parameters:</p> Name Type Description Default <code>index</code> <code>int</code> <p>The index of the checkbox to remove.</p> required Source code in <code>client/ayon_core/ui/components/checkbox_handler.py</code> <pre><code>def remove_checkbox(self, index: int) -&gt; None:\n    \"\"\"Remove a checkbox from the tracked list.\n\n    Re-indexes the Python list **and** patches the stale\n    ``CHECKBOX_INDEX_PROP`` values stored in the live\n    ``QTextDocument`` so that subsequent hit-tests via\n    :meth:`get_checkbox_at_position` return correct indices.\n\n    Args:\n        index: The index of the checkbox to remove.\n    \"\"\"\n    if not 0 &lt;= index &lt; len(self._checkboxes):\n        return\n\n    self._checkboxes.pop(index)\n\n    # Re-index the Python list and sync each affected document char format.\n    doc = self._text_edit.document()\n    doc.blockSignals(True)\n    cursor = QTextCursor(doc)\n    cursor.beginEditBlock()\n    try:\n        for i, cb in enumerate(self._checkboxes[index:], start=index):\n            cb.index = i\n            pos = cb.doc_position\n            if pos &lt; 0:\n                continue\n            cursor.setPosition(pos)\n            cursor.setPosition(pos + 1, QTextCursor.MoveMode.KeepAnchor)\n            fmt = QTextCharFormat()\n            fmt.setObjectType(CHECKBOX_FORMAT_TYPE)\n            fmt.setProperty(CHECKBOX_CHECKED_PROP, cb.checked)\n            fmt.setProperty(CHECKBOX_INDEX_PROP, cb.index)\n            fmt.setVerticalAlignment(\n                QTextCharFormat.VerticalAlignment.AlignBaseline\n            )\n            cursor.setCharFormat(fmt)\n    except Exception:\n        log.warning(\n            \"Failed to re-index checkboxes in document\", exc_info=True\n        )\n    finally:\n        cursor.endEditBlock()\n        doc.blockSignals(False)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/checkbox_handler.html#client.ayon_core.ui.components.checkbox_handler.CheckboxHandler.remove_last_checkbox","title":"<code>remove_last_checkbox()</code>","text":"<p>Remove the last checkbox from the tracked list.</p> <p>Used when the user presses Enter on an empty checkbox line to terminate the list.</p> <p>Returns:</p> Type Description <code>bool</code> <p>True if a checkbox was removed, False if the list was empty.</p> Source code in <code>client/ayon_core/ui/components/checkbox_handler.py</code> <pre><code>def remove_last_checkbox(self) -&gt; bool:\n    \"\"\"Remove the last checkbox from the tracked list.\n\n    Used when the user presses Enter on an empty checkbox line to\n    terminate the list.\n\n    Returns:\n        True if a checkbox was removed, False if the list was empty.\n    \"\"\"\n    if not self._checkboxes:\n        return False\n    self._checkboxes.pop()\n    return True\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/checkbox_handler.html#client.ayon_core.ui.components.checkbox_handler.CheckboxHandler.to_markdown","title":"<code>to_markdown()</code>","text":"<p>Reconstruct markdown with current checkbox states.</p> <p>Returns:</p> Type Description <code>str</code> <p>GitHub-flavored markdown string with checkbox syntax.</p> Source code in <code>client/ayon_core/ui/components/checkbox_handler.py</code> <pre><code>def to_markdown(self) -&gt; str:\n    \"\"\"Reconstruct markdown with current checkbox states.\n\n    Returns:\n        GitHub-flavored markdown string with checkbox syntax.\n    \"\"\"\n    if not self._checkboxes:\n        return self._text_edit.toMarkdown(MD_DIALECT)\n\n    # Get current text and replace placeholders with checkbox syntax\n    # we use toMarkdown() to make sure the other formatted items have\n    # already been dealt with.\n    text = self._text_edit.toMarkdown(MD_DIALECT)\n    lines = text.split(\"\\n\")\n    result_lines: list[str] = []\n\n    checkbox_iter = iter(self._checkboxes)\n    current_cb = next(checkbox_iter, None)\n\n    for line in lines:\n        if \"\\ufffc\" in line and current_cb:\n            checked_char = \"x\" if current_cb.checked else \" \"\n            cb_idx = line.index(\"\\ufffc\")\n            raw_after = line[cb_idx + 1 :]  # everything after \\ufffc\n            raw_after = raw_after.lstrip(\" \")  # drop the space Qt inserts\n            raw_after = _PH_RE.sub(\"\", raw_after).rstrip()\n            line = f\"{current_cb.prefix}[{checked_char}] {raw_after}\"\n            current_cb = next(checkbox_iter, None)\n        result_lines.append(line)\n\n    return \"\\n\".join(result_lines)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/checkbox_handler.html#client.ayon_core.ui.components.checkbox_handler.CheckboxHandler.toggle_checkbox","title":"<code>toggle_checkbox(index)</code>","text":"<p>Toggle a checkbox by its index.</p> <p>Parameters:</p> Name Type Description Default <code>index</code> <code>int</code> <p>The index of the checkbox to toggle.</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if the checkbox was toggled, False otherwise.</p> Source code in <code>client/ayon_core/ui/components/checkbox_handler.py</code> <pre><code>def toggle_checkbox(self, index: int) -&gt; bool:\n    \"\"\"Toggle a checkbox by its index.\n\n    Args:\n        index: The index of the checkbox to toggle.\n\n    Returns:\n        True if the checkbox was toggled, False otherwise.\n    \"\"\"\n    if not 0 &lt;= index &lt; len(self._checkboxes):\n        return False\n\n    # Toggle the state\n    self._checkboxes[index].checked = not self._checkboxes[index].checked\n\n    # Update the document\n    self._update_checkbox_in_document(index)\n\n    # Emit signal\n    self.checklist_changed.emit()\n    return True\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/checkbox_handler.html#client.ayon_core.ui.components.checkbox_handler.CheckboxItem","title":"<code>CheckboxItem</code>  <code>dataclass</code>","text":"<p>Represents a checkbox item in the document.</p> <p>Attributes:</p> Name Type Description <code>index</code> <code>int</code> <p>Unique index of the checkbox.</p> <code>checked</code> <code>bool</code> <p>Whether the checkbox is checked.</p> <code>text</code> <code>str</code> <p>Text following the checkbox.</p> <code>line_number</code> <code>int</code> <p>Line number in the original markdown.</p> <code>prefix</code> <code>str</code> <p>Characters before the checkbox (e.g., \"- \", \"* \").</p> <code>doc_position</code> <code>int</code> <p>Character position of the \\ufffc object in the document, or -1 if not yet placed.</p> Source code in <code>client/ayon_core/ui/components/checkbox_handler.py</code> <pre><code>@dataclass\nclass CheckboxItem:\n    \"\"\"Represents a checkbox item in the document.\n\n    Attributes:\n        index: Unique index of the checkbox.\n        checked: Whether the checkbox is checked.\n        text: Text following the checkbox.\n        line_number: Line number in the original markdown.\n        prefix: Characters before the checkbox (e.g., \"- \", \"* \").\n        doc_position: Character position of the \\\\ufffc object in the\n            document, or -1 if not yet placed.\n    \"\"\"\n\n    index: int\n    checked: bool\n    text: str\n    line_number: int\n    prefix: str = \"- \"\n    doc_position: int = field(default=-1)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/checkbox_handler.html#client.ayon_core.ui.components.checkbox_handler.CheckboxTextObject","title":"<code>CheckboxTextObject</code>","text":"<p>               Bases: <code>QPyTextObject</code></p> <p>Custom text object for rendering checkbox icons in QTextDocument.</p> <p>Uses Material icons to render checkboxes: - Unchecked: white radio_button_unchecked - Checked: green check_circle</p> Source code in <code>client/ayon_core/ui/components/checkbox_handler.py</code> <pre><code>class CheckboxTextObject(QPyTextObject):\n    \"\"\"Custom text object for rendering checkbox icons in QTextDocument.\n\n    Uses Material icons to render checkboxes:\n    - Unchecked: white radio_button_unchecked\n    - Checked: green check_circle\n    \"\"\"\n\n    def __init__(self, parent: QObject | None = None):\n        super().__init__(parent)\n        self._icon_cache: dict[bool, QPixmap] = {}\n\n    def intrinsicSize(\n        self,\n        doc: QTextDocument,\n        posInDocument: int,\n        format: QTextFormat,\n    ) -&gt; QSizeF:\n        \"\"\"Return the size of the checkbox icon.\n\n        Args:\n            doc: The document containing the object.\n            posInDocument: Position in the document.\n            format: The text format of the object.\n\n        Returns:\n            Size of the checkbox icon based on font size.\n        \"\"\"\n        font = doc.defaultFont()\n        size = max(font.pointSizeF() * 1.4, 14)\n        return QSizeF(size, size)\n\n    def drawObject(\n        self,\n        painter: QPainter,\n        rect: QRectF | QRect,\n        doc: QTextDocument,\n        posInDocument: int,\n        format: QTextFormat,\n    ) -&gt; None:\n        \"\"\"Draw the checkbox icon.\n\n        Args:\n            painter: The painter to draw with.\n            rect: The rectangle to draw in.\n            doc: The document containing the object.\n            posInDocument: Position in the document.\n            format: The text format containing checkbox state.\n        \"\"\"\n        is_checked = format.property(CHECKBOX_CHECKED_PROP)\n\n        if is_checked:\n            icon = get_icon(CHECKED_ICON, color=CHECKED_COLOR)\n        else:\n            icon = get_icon(UNCHECKED_ICON, color=UNCHECKED_COLOR)\n\n        size = int(min(rect.width(), rect.height()))\n        pixmap = icon.pixmap(size, size)\n\n        # Vertically center the icon relative to text ascent\n        font_metrics = QFontMetrics(doc.defaultFont())\n        ascent = font_metrics.ascent()\n        y_offset = (ascent - size) // 3\n        draw_point = rect.topLeft().toPoint() + QPoint(0, -y_offset)\n\n        painter.drawPixmap(draw_point, pixmap)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/checkbox_handler.html#client.ayon_core.ui.components.checkbox_handler.CheckboxTextObject.drawObject","title":"<code>drawObject(painter, rect, doc, posInDocument, format)</code>","text":"<p>Draw the checkbox icon.</p> <p>Parameters:</p> Name Type Description Default <code>painter</code> <code>QPainter</code> <p>The painter to draw with.</p> required <code>rect</code> <code>QRectF | QRect</code> <p>The rectangle to draw in.</p> required <code>doc</code> <code>QTextDocument</code> <p>The document containing the object.</p> required <code>posInDocument</code> <code>int</code> <p>Position in the document.</p> required <code>format</code> <code>QTextFormat</code> <p>The text format containing checkbox state.</p> required Source code in <code>client/ayon_core/ui/components/checkbox_handler.py</code> <pre><code>def drawObject(\n    self,\n    painter: QPainter,\n    rect: QRectF | QRect,\n    doc: QTextDocument,\n    posInDocument: int,\n    format: QTextFormat,\n) -&gt; None:\n    \"\"\"Draw the checkbox icon.\n\n    Args:\n        painter: The painter to draw with.\n        rect: The rectangle to draw in.\n        doc: The document containing the object.\n        posInDocument: Position in the document.\n        format: The text format containing checkbox state.\n    \"\"\"\n    is_checked = format.property(CHECKBOX_CHECKED_PROP)\n\n    if is_checked:\n        icon = get_icon(CHECKED_ICON, color=CHECKED_COLOR)\n    else:\n        icon = get_icon(UNCHECKED_ICON, color=UNCHECKED_COLOR)\n\n    size = int(min(rect.width(), rect.height()))\n    pixmap = icon.pixmap(size, size)\n\n    # Vertically center the icon relative to text ascent\n    font_metrics = QFontMetrics(doc.defaultFont())\n    ascent = font_metrics.ascent()\n    y_offset = (ascent - size) // 3\n    draw_point = rect.topLeft().toPoint() + QPoint(0, -y_offset)\n\n    painter.drawPixmap(draw_point, pixmap)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/checkbox_handler.html#client.ayon_core.ui.components.checkbox_handler.CheckboxTextObject.intrinsicSize","title":"<code>intrinsicSize(doc, posInDocument, format)</code>","text":"<p>Return the size of the checkbox icon.</p> <p>Parameters:</p> Name Type Description Default <code>doc</code> <code>QTextDocument</code> <p>The document containing the object.</p> required <code>posInDocument</code> <code>int</code> <p>Position in the document.</p> required <code>format</code> <code>QTextFormat</code> <p>The text format of the object.</p> required <p>Returns:</p> Type Description <code>QSizeF</code> <p>Size of the checkbox icon based on font size.</p> Source code in <code>client/ayon_core/ui/components/checkbox_handler.py</code> <pre><code>def intrinsicSize(\n    self,\n    doc: QTextDocument,\n    posInDocument: int,\n    format: QTextFormat,\n) -&gt; QSizeF:\n    \"\"\"Return the size of the checkbox icon.\n\n    Args:\n        doc: The document containing the object.\n        posInDocument: Position in the document.\n        format: The text format of the object.\n\n    Returns:\n        Size of the checkbox icon based on font size.\n    \"\"\"\n    font = doc.defaultFont()\n    size = max(font.pointSizeF() * 1.4, 14)\n    return QSizeF(size, size)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/combo_box.html","title":"combo_box","text":"<p>Combo-box components for the AYON UI Qt library.</p> <p>This module provides :class:<code>AYComboBox</code>, a styled :class:<code>QComboBox</code> subclass that supports per-item coloured icons, a short-text display mode, and an icon-only display mode via :class:<code>~ayon_core.ui.data_models.MenuSize</code>. A dropdown arrow is drawn using the Material Symbol <code>arrow_drop_down</code> icon so the widget is visually recognizable as a dropdown when show_chevron is true.</p> <p>It also exposes :class:<code>AYComboBoxModel</code>, the default :class:<code>QStandardItemModel</code> subclass that adds two extra item-data roles:</p> <ul> <li><code>ShortTextRole</code> - an abbreviated label shown when the combo-box is in   :attr:<code>~ayon_core.ui.data_models.MenuSize.Short</code> mode.</li> <li><code>IconNameRole</code> - the Material Symbol icon name used to regenerate icons   when the inverted colour mode is toggled.</li> </ul> <p>Typical usage::</p> <pre><code>from ayon_core.ui.components.combo_box import AYComboBox\n\nitems = [\n    {\n        \"text\": \"Item 1\",\n        \"short_text\": \"I1\",\n        \"icon\": \"fiber_new\",\n        \"color\": \"#434a56\",\n    },\n    {\n        \"text\": \"Item 2\",\n        \"short_text\": \"I2\",\n        \"icon\": \"timer\",\n        \"color\": \"#bababa\",\n    },\n]\ncombo = AYComboBox(parent=my_widget, items=items)\ncombo.set_size(\"short\")   # or MenuSize.Short\ncombo.set_inverted(True)\n</code></pre> <p>Custom model usage (must expose <code>ShortTextRole</code> and <code>IconNameRole</code>)::</p> <pre><code>model = MyCustomModel(parent=combo)\ncombo.setModel(model)\n</code></pre> Note <p>When a custom model that does not expose <code>ShortTextRole</code> / <code>IconNameRole</code> is set via :meth:<code>AYComboBox.setModel</code>, the widget falls back to displaying <code>\"&lt; INCOMPATIBLE MODEL &gt;\"</code> in short mode and raises :exc:<code>RuntimeError</code> when :meth:<code>AYComboBox.add_item</code> or :meth:<code>AYComboBox.update_items</code> is called.</p>"},{"location":"autoapi/client/ayon_core/ui/components/combo_box.html#client.ayon_core.ui.components.combo_box.AYComboBox","title":"<code>AYComboBox</code>","text":"<p>               Bases: <code>StyleMixin</code>, <code>QComboBox</code></p> <p>AYON-styled combo-box with icon, short-text, and inverted-colour support.</p> <p>:class:<code>AYComboBox</code> wraps :class:<code>QComboBox</code> and adds:</p> <ul> <li> <p>Three display modes controlled by   :class:<code>~ayon_core.ui.data_models.MenuSize</code>:</p> </li> <li> <p><code>Full</code>  - shows the full item label (default).</p> </li> <li><code>Short</code> - shows the abbreviated <code>ShortTextRole</code> label.</li> <li> <p><code>Icon</code>  - hides the text and shows only the item icon.</p> </li> <li> <p>Inverted colour mode - swaps the icon foreground/background colours   so the icon appears on a coloured pill rather than a neutral background.</p> </li> <li>Placeholder text support for an empty selection state.</li> <li>Custom model support: any model that exposes <code>ShortTextRole</code> and   <code>IconNameRole</code> attributes is accepted. Models without those attributes   are flagged as incompatible; :meth:<code>add_item</code> and :meth:<code>update_items</code>   will raise :exc:<code>RuntimeError</code> when such a model is active.</li> </ul> <p>The default model is :class:<code>AYComboBoxModel</code>.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <code>Optional[QWidget]</code> <p>Optional parent widget.</p> <code>None</code> <code>items</code> <code>List[dict] | None</code> <p>Initial list of item dictionaries.  Each dict may contain:</p> <ul> <li><code>\"text\"</code> (required) - Display label.</li> <li><code>\"color\"</code>      - Hex foreground colour (default <code>\"#ffffff\"</code>).</li> <li><code>\"icon\"</code>       - Material Symbol icon name.</li> <li><code>\"short_text\"</code> - Abbreviated label for short mode   (default <code>\"&lt; UNDEFINED &gt;\"</code>).</li> </ul> <code>None</code> <code>size</code> <code>MenuSize | str</code> <p>Initial display mode.  Accepts a :class:<code>~ayon_core.ui.data_models.MenuSize</code> value or its string equivalent (<code>\"full\"</code>, <code>\"short\"</code>, <code>\"icon\"</code>).</p> <code>Full</code> <code>height</code> <code>int</code> <p>Fixed maximum height in pixels (default <code>30</code>).</p> <code>30</code> <code>placeholder</code> <code>Optional[str]</code> <p>Placeholder string shown when no item is selected.</p> <code>None</code> <code>inverted</code> <code>bool</code> <p>When <code>True</code> the icon foreground and background colours are swapped (default <code>False</code>).</p> <code>False</code> <code>icon_size</code> <code>int</code> <p>Icon size in pixels (default <code>20</code>).</p> <code>20</code> <code>show_chevron</code> <code>bool</code> <p>If False, the dropdown chevron (arrow) will not be drawn in the custom style. Default is False.</p> <code>False</code> <code>**kwargs</code> <p>Additional keyword arguments forwarded to :class:<code>QComboBox</code>.</p> <code>{}</code> Example <p>Basic usage with the built-in status list::</p> <pre><code>combo = AYComboBox(\n    parent=my_widget,\n    items=[...],\n    size=MenuSize.Short,\n    placeholder=\"Select status\u2026\",\n)\ncombo.currentTextChanged.connect(on_status_changed)\n</code></pre> <p>Switching modes at runtime::</p> <pre><code>combo.set_size(\"icon\")\ncombo.set_inverted(True)\n</code></pre> Source code in <code>client/ayon_core/ui/components/combo_box.py</code> <pre><code>class AYComboBox(StyleMixin, QtWidgets.QComboBox):\n    \"\"\"AYON-styled combo-box with icon, short-text, and inverted-colour\n    support.\n\n    :class:`AYComboBox` wraps :class:`QComboBox` and adds:\n\n    - **Three display modes** controlled by\n      :class:`~ayon_core.ui.data_models.MenuSize`:\n\n      - ``Full``  - shows the full item label (default).\n      - ``Short`` - shows the abbreviated ``ShortTextRole`` label.\n      - ``Icon``  - hides the text and shows only the item icon.\n\n    - **Inverted colour mode** - swaps the icon foreground/background colours\n      so the icon appears on a coloured pill rather than a neutral background.\n    - **Placeholder text** support for an empty selection state.\n    - **Custom model** support: any model that exposes ``ShortTextRole`` and\n      ``IconNameRole`` attributes is accepted. Models without those attributes\n      are flagged as incompatible; :meth:`add_item` and :meth:`update_items`\n      will raise :exc:`RuntimeError` when such a model is active.\n\n    The default model is :class:`AYComboBoxModel`.\n\n    Args:\n        parent: Optional parent widget.\n        items: Initial list of item dictionaries.  Each dict may contain:\n\n            - ``\"text\"``       *(required)* - Display label.\n            - ``\"color\"``      - Hex foreground colour (default ``\"#ffffff\"``).\n            - ``\"icon\"``       - Material Symbol icon name.\n            - ``\"short_text\"`` - Abbreviated label for short mode\n              (default ``\"&lt; UNDEFINED &gt;\"``).\n\n        size: Initial display mode.  Accepts a\n            :class:`~ayon_core.ui.data_models.MenuSize` value or its string\n            equivalent (``\"full\"``, ``\"short\"``, ``\"icon\"``).\n        height: Fixed maximum height in pixels (default ``30``).\n        placeholder: Placeholder string shown when no item is selected.\n        inverted: When ``True`` the icon foreground and background colours are\n            swapped (default ``False``).\n        icon_size: Icon size in pixels (default ``20``).\n        show_chevron: If False, the dropdown chevron (arrow) will not be drawn\n            in the custom style. Default is False.\n\n        **kwargs: Additional keyword arguments forwarded to\n            :class:`QComboBox`.\n\n    Example:\n        Basic usage with the built-in status list::\n\n            combo = AYComboBox(\n                parent=my_widget,\n                items=[...],\n                size=MenuSize.Short,\n                placeholder=\"Select status\u2026\",\n            )\n            combo.currentTextChanged.connect(on_status_changed)\n\n        Switching modes at runtime::\n\n            combo.set_size(\"icon\")\n            combo.set_inverted(True)\n    \"\"\"\n\n    Variants = QComboBoxVariants\n\n    def __init__(\n        self,\n        parent: Optional[QtWidgets.QWidget] = None,\n        items: List[dict] | None = None,\n        size: MenuSize | str = MenuSize.Full,\n        height: int = 30,\n        placeholder: Optional[str] = None,\n        inverted: bool = False,\n        icon_size: int = 20,\n        variant: Variants = Variants.Default,\n        show_chevron: bool = False,\n        **kwargs,\n    ) -&gt; None:\n        self._uses_incompatible_model = False\n        super().__init__(parent, **kwargs)\n        self._variant_str = variant.value\n\n        self.setStyle(get_ayon_style())\n        self.setMouseTracking(True)\n        self.setMaximumHeight(height)\n\n        # Initialize properties\n        self._size: MenuSize = (\n            size if isinstance(size, MenuSize) else MenuSize(size)\n        )\n        self._height: int = height\n        self._inverted: bool = inverted\n        self._icon_size: int = icon_size\n        self._inverted_icons: dict[str, QIcon] = {}\n        self.show_chevron: bool = show_chevron\n\n        if placeholder:\n            self.setPlaceholderText(placeholder)\n\n        # setup model\n        model = AYComboBoxModel(self)\n        self.setModel(model)\n\n        self.update_items(items)\n\n    def _assert_compatible_model(self) -&gt; None:\n        \"\"\"Raise :exc:`RuntimeError` when an incompatible model is active.\n\n        Called by :meth:`add_item` and :meth:`update_items` to guard against\n        direct item mutations when a custom model that does not expose\n        ``ShortTextRole`` / ``IconNameRole`` has been set via\n        :meth:`setModel`.\n        \"\"\"\n        if self._uses_incompatible_model:\n            raise RuntimeError(\n                \"Cannot modify items directly when a custom \"\n                \"model is in use. Modify the model instead.\"\n            )\n\n    def setModel(self, model: QtCore.QAbstractItemModel) -&gt; None:\n        \"\"\"Set the item model and detect compatibility with AYComboBox roles.\n\n        If *model* is not an :class:`AYComboBoxModel` instance **and** it\n        does not expose both ``ShortTextRole`` and ``IconNameRole``\n        attributes, it is marked as *incompatible*.  In that state:\n\n        - :meth:`add_item` and :meth:`update_items` will raise\n          :exc:`RuntimeError`.\n        - Short mode falls back to ``\"&lt; INCOMPATIBLE MODEL &gt;\"`` as the\n          displayed text.\n\n        Args:\n            model: The new item model to attach to the combo-box.\n        \"\"\"\n        mtype = type(model)\n        self._uses_incompatible_model = mtype is not AYComboBoxModel and (\n            not hasattr(model, \"IconNameRole\")\n            or not hasattr(model, \"ShortTextRole\")\n        )\n        super().setModel(model)\n\n    def _make_icon(\n        self,\n        fg_color: QColor,\n        bg_color: QColor,\n        icon_name: str | None,\n        inverted: bool = False,\n    ) -&gt; QIcon | None:\n        \"\"\"Assign a Material Symbol icon to *data_item*.\n\n        Reads the item's background and foreground colours and passes them\n        to ``get_icon()``.  When *inverted* mode is active the icon's normal\n        colour uses the **background** colour so the icon appears on a\n        coloured pill; otherwise it uses the foreground colour.\n\n        Does nothing when *icon_name* is ``None`` or an empty string.\n\n        Args:\n            fg_color: Icon color.\n            bg_color: Background color.\n            icon_name: A Material Symbol identifier (e.g. ``\"play_arrow\"``),\n                or ``None`` / ``\"\"`` to skip icon assignment.\n            inverted: When ``True``, the icon's normal colour is taken from\n                the item's **background** colour so it renders as a coloured\n                icon on a neutral background.  Defaults to ``False``.\n        \"\"\"\n        icon = None\n        if icon_name:\n            icon = get_icon(\n                icon_name,\n                color=bg_color if inverted else fg_color,\n                # TODO: add fill support to get_icon and\n                #       pass self._icon_fill here\n            )\n        return icon\n\n    def _get_inverted_icon(self, default: QIcon) -&gt; QIcon:\n        \"\"\"Return the inverted-colour icon for the currently selected item.\n\n        Looks up the ``IconNameRole`` of the current item and returns a\n        cached inverted icon.  If the icon has not been generated yet it is\n        created via :meth:`_make_icon` with ``inverted=True`` and stored in\n        :attr:`_inverted_icons` for future calls.\n\n        Args:\n            default: Fallback icon returned when the current item has no\n                ``IconNameRole`` value.\n\n        Returns:\n            A :class:`QIcon` rendered with inverted colours, or *default*\n            when no icon name is available.\n        \"\"\"\n        idx = self.currentIndex()\n        if idx &lt; 0:\n            return default\n\n        icon_name = self.currentData(self.model().IconNameRole)\n        if not icon_name:\n            return default\n\n        fg = self.currentData(QtCore.Qt.ItemDataRole.ForegroundRole).color()\n        bg = self.currentData(QtCore.Qt.ItemDataRole.BackgroundRole).color()\n        key = f\"{icon_name}:{fg.name()}:{bg.name()}\"\n\n        if key not in self._inverted_icons:\n            self._inverted_icons[key] = (\n                self._make_icon(fg, bg, icon_name, inverted=True) or default\n            )\n\n        return self._inverted_icons[key]\n\n    def add_item(self, item: dict[str, str]):\n        \"\"\"Append a single item to the combo-box model.\n\n        Constructs a :class:`QStandardItem` from *item*, sets its foreground\n        colour, background colour (from the current palette), icon, short\n        text, and icon name, then appends it to the model.\n\n        Raises:\n            RuntimeError: If a custom incompatible model is currently set\n                (see :meth:`setModel`).\n\n        Args:\n            item: A dict with the following keys:\n\n                - ``\"text\"``       *(required)* - Display label.\n                - ``\"color\"``      - Hex foreground colour\n                  (default ``\"#ffffff\"``).\n                - ``\"icon\"``       - Material Symbol icon name.\n                - ``\"short_text\"`` - Abbreviated label stored in\n                  ``ShortTextRole`` (default ``\"&lt; UNDEFINED &gt;\"``).\n        \"\"\"\n        self._assert_compatible_model()\n\n        bg_color = self.palette().color(\n            QPalette.ColorGroup.Active, QPalette.ColorRole.Window\n        )\n        fg_color = QColor(item.get(\"color\", \"#ffffff\"))\n\n        text = item.get(\"text\")\n        if not text:\n            raise ValueError(\n                f\"Item dict must contain a non-empty 'text' key; got: {item!r}\"\n            )\n\n        data_item = QStandardItem(text)\n        data_item.setData(\n            QBrush(fg_color),\n            QtCore.Qt.ItemDataRole.ForegroundRole,\n        )\n        data_item.setData(\n            QBrush(bg_color), QtCore.Qt.ItemDataRole.BackgroundRole\n        )\n        icon = self._make_icon(fg_color, bg_color, item.get(\"icon\"))\n        if icon:\n            data_item.setIcon(icon)\n        data_item.setData(\n            item.get(\"short_text\", \"&lt; UNDEFINED &gt;\"),\n            self.model().ShortTextRole,\n        )  # type: ignore\n        data_item.setData(item.get(\"icon\"), self.model().IconNameRole)  # type: ignore\n        self.model().appendRow(data_item)  # type: ignore\n\n    def update_items(self, item_list: list[dict] | None = None):\n        \"\"\"Replace all items in the model with the provided list.\n\n        Clears the model and calls :meth:`add_item` for every entry in\n        *item_list*.  If *item_list* is ``None`` or empty the model is left\n        unchanged.\n\n        Raises:\n            RuntimeError: If a custom incompatible model is currently set\n                (see :meth:`setModel`).\n\n        Args:\n            item_list: List of item dicts as accepted by :meth:`add_item`.\n                Pass ``None`` or an empty list to keep the current items.\n        \"\"\"\n        if item_list:\n            self._assert_compatible_model()\n            self.model().clear()  # type: ignore\n            for item in item_list:\n                self.add_item(item)\n\n    def set_inverted(self, state: bool):\n        \"\"\"Toggle the inverted colour mode and schedule a repaint.\n\n        In *inverted* mode the icon's normal colour is drawn using the item's\n        **background** colour instead of its foreground colour, producing a\n        coloured icon on a neutral background.\n\n        Inverted icons are generated lazily on first paint via\n        :meth:`_get_inverted_icon` and cached in :attr:`_inverted_icons`.\n        Calling this method triggers a repaint so the change is visible\n        immediately.\n\n        Args:\n            state: ``True`` to enable inverted mode, ``False`` to disable.\n        \"\"\"\n        self._inverted = state\n        self.update()\n\n    def set_size(self, size: MenuSize | str):\n        \"\"\"Change the display mode and repaint the widget.\n\n        Args:\n            size: New display mode.  Accepts a\n                :class:`~ayon_core.ui.data_models.MenuSize` value or its\n                lowercase string equivalent (``\"full\"``, ``\"short\"``,\n                ``\"icon\"``).\n        \"\"\"\n        self._size = (\n            size if isinstance(size, MenuSize) else MenuSize(size.lower())\n        )\n        self.update()\n\n    def sizeHint(self) -&gt; QtCore.QSize:\n        \"\"\"Return the preferred size for the combo-box.\n\n        Delegates to the AYON style's\n        :meth:`QStyle.sizeFromContents` so that the hint respects the\n        custom theme's metrics rather than the system default.\n\n        Returns:\n            The preferred :class:`QSize` for this widget.\n        \"\"\"\n\n        option = QtWidgets.QStyleOptionComboBox()\n        self.initStyleOption(option)\n        return get_ayon_style().sizeFromContents(\n            QtWidgets.QStyle.ContentsType.CT_ComboBox,\n            option,\n            self.rect().size(),\n            self,\n        )\n\n    def _resolve_current_text(self, idx: int) -&gt; str | None:\n        \"\"\"Return the display text for the current item.\n\n        Args:\n            idx: The current combo-box index.\n\n        Returns:\n            The text to display, or ``None`` when the option should be left\n            unchanged (e.g. no item is selected and no placeholder applies).\n        \"\"\"\n        if idx &lt; 0:\n            return None\n\n        if self._size == MenuSize.Full:\n            return self.currentData(QtCore.Qt.ItemDataRole.DisplayRole)\n        if self._size == MenuSize.Short:\n            return (\n                self.currentData(self.model().ShortTextRole)\n                if hasattr(self.model(), \"ShortTextRole\")\n                else \"&lt; INCOMPATIBLE MODEL &gt;\"\n            )\n        return \"\"  # MenuSize.Icon\n\n    def paintEvent(self, arg__1: QPaintEvent) -&gt; None:\n        \"\"\"Render the combo-box using the AYON custom style.\n\n        Overrides the default :meth:`QComboBox.paintEvent` to:\n\n        1. Draw the combo-box frame/control via the AYON style.\n        2. Substitute placeholder text (rendered in ``placeholderText``\n           palette colour) when no item is selected.\n        3. Render the label according to the current\n           :class:`~ayon_core.ui.data_models.MenuSize` mode:\n\n           - ``Full``  \u2192 item's full text.\n           - ``Short`` \u2192 item's ``ShortTextRole`` value, or\n             ``\"&lt; INCOMPATIBLE MODEL &gt;\"`` if the role is unavailable.\n           - ``Icon``  \u2192 empty string (icon only).\n\n        Args:\n            arg__1: The paint event delivered by Qt.\n        \"\"\"\n\n        p = QPainter(self)\n        option = QtWidgets.QStyleOptionComboBox()\n        self.initStyleOption(option)\n\n        p.setFont(self.font())\n        option.fontMetrics = self.fontMetrics()\n\n        if self._inverted:\n            option.currentIcon = self._get_inverted_icon(option.currentIcon)\n\n        _style = get_ayon_style()\n        _style.drawComplexControl(\n            QtWidgets.QStyle.ComplexControl.CC_ComboBox, option, p, self\n        )\n\n        idx = self.currentIndex()\n\n        if idx &lt; 0 and self.placeholderText():\n            option.palette.setBrush(  # type: ignore\n                QPalette.ColorRole.ButtonText,\n                option.palette.placeholderText(),  # type: ignore\n            )\n            option.currentText = self.placeholderText()  # type: ignore\n\n        text = self._resolve_current_text(idx)\n        if text is not None:\n            option.currentText = text  # type: ignore\n\n        _style.drawControl(\n            QtWidgets.QStyle.ControlElement.CE_ComboBoxLabel,\n            option,\n            p,\n            self,\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/combo_box.html#client.ayon_core.ui.components.combo_box.AYComboBox.add_item","title":"<code>add_item(item)</code>","text":"<p>Append a single item to the combo-box model.</p> <p>Constructs a :class:<code>QStandardItem</code> from item, sets its foreground colour, background colour (from the current palette), icon, short text, and icon name, then appends it to the model.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If a custom incompatible model is currently set (see :meth:<code>setModel</code>).</p> <p>Parameters:</p> Name Type Description Default <code>item</code> <code>dict[str, str]</code> <p>A dict with the following keys:</p> <ul> <li><code>\"text\"</code> (required) - Display label.</li> <li><code>\"color\"</code>      - Hex foreground colour   (default <code>\"#ffffff\"</code>).</li> <li><code>\"icon\"</code>       - Material Symbol icon name.</li> <li><code>\"short_text\"</code> - Abbreviated label stored in   <code>ShortTextRole</code> (default <code>\"&lt; UNDEFINED &gt;\"</code>).</li> </ul> required Source code in <code>client/ayon_core/ui/components/combo_box.py</code> <pre><code>def add_item(self, item: dict[str, str]):\n    \"\"\"Append a single item to the combo-box model.\n\n    Constructs a :class:`QStandardItem` from *item*, sets its foreground\n    colour, background colour (from the current palette), icon, short\n    text, and icon name, then appends it to the model.\n\n    Raises:\n        RuntimeError: If a custom incompatible model is currently set\n            (see :meth:`setModel`).\n\n    Args:\n        item: A dict with the following keys:\n\n            - ``\"text\"``       *(required)* - Display label.\n            - ``\"color\"``      - Hex foreground colour\n              (default ``\"#ffffff\"``).\n            - ``\"icon\"``       - Material Symbol icon name.\n            - ``\"short_text\"`` - Abbreviated label stored in\n              ``ShortTextRole`` (default ``\"&lt; UNDEFINED &gt;\"``).\n    \"\"\"\n    self._assert_compatible_model()\n\n    bg_color = self.palette().color(\n        QPalette.ColorGroup.Active, QPalette.ColorRole.Window\n    )\n    fg_color = QColor(item.get(\"color\", \"#ffffff\"))\n\n    text = item.get(\"text\")\n    if not text:\n        raise ValueError(\n            f\"Item dict must contain a non-empty 'text' key; got: {item!r}\"\n        )\n\n    data_item = QStandardItem(text)\n    data_item.setData(\n        QBrush(fg_color),\n        QtCore.Qt.ItemDataRole.ForegroundRole,\n    )\n    data_item.setData(\n        QBrush(bg_color), QtCore.Qt.ItemDataRole.BackgroundRole\n    )\n    icon = self._make_icon(fg_color, bg_color, item.get(\"icon\"))\n    if icon:\n        data_item.setIcon(icon)\n    data_item.setData(\n        item.get(\"short_text\", \"&lt; UNDEFINED &gt;\"),\n        self.model().ShortTextRole,\n    )  # type: ignore\n    data_item.setData(item.get(\"icon\"), self.model().IconNameRole)  # type: ignore\n    self.model().appendRow(data_item)  # type: ignore\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/combo_box.html#client.ayon_core.ui.components.combo_box.AYComboBox.paintEvent","title":"<code>paintEvent(arg__1)</code>","text":"<p>Render the combo-box using the AYON custom style.</p> <p>Overrides the default :meth:<code>QComboBox.paintEvent</code> to:</p> <ol> <li>Draw the combo-box frame/control via the AYON style.</li> <li>Substitute placeholder text (rendered in <code>placeholderText</code>    palette colour) when no item is selected.</li> <li> <p>Render the label according to the current    :class:<code>~ayon_core.ui.data_models.MenuSize</code> mode:</p> </li> <li> <p><code>Full</code>  \u2192 item's full text.</p> </li> <li><code>Short</code> \u2192 item's <code>ShortTextRole</code> value, or      <code>\"&lt; INCOMPATIBLE MODEL &gt;\"</code> if the role is unavailable.</li> <li><code>Icon</code>  \u2192 empty string (icon only).</li> </ol> <p>Parameters:</p> Name Type Description Default <code>arg__1</code> <code>QPaintEvent</code> <p>The paint event delivered by Qt.</p> required Source code in <code>client/ayon_core/ui/components/combo_box.py</code> <pre><code>def paintEvent(self, arg__1: QPaintEvent) -&gt; None:\n    \"\"\"Render the combo-box using the AYON custom style.\n\n    Overrides the default :meth:`QComboBox.paintEvent` to:\n\n    1. Draw the combo-box frame/control via the AYON style.\n    2. Substitute placeholder text (rendered in ``placeholderText``\n       palette colour) when no item is selected.\n    3. Render the label according to the current\n       :class:`~ayon_core.ui.data_models.MenuSize` mode:\n\n       - ``Full``  \u2192 item's full text.\n       - ``Short`` \u2192 item's ``ShortTextRole`` value, or\n         ``\"&lt; INCOMPATIBLE MODEL &gt;\"`` if the role is unavailable.\n       - ``Icon``  \u2192 empty string (icon only).\n\n    Args:\n        arg__1: The paint event delivered by Qt.\n    \"\"\"\n\n    p = QPainter(self)\n    option = QtWidgets.QStyleOptionComboBox()\n    self.initStyleOption(option)\n\n    p.setFont(self.font())\n    option.fontMetrics = self.fontMetrics()\n\n    if self._inverted:\n        option.currentIcon = self._get_inverted_icon(option.currentIcon)\n\n    _style = get_ayon_style()\n    _style.drawComplexControl(\n        QtWidgets.QStyle.ComplexControl.CC_ComboBox, option, p, self\n    )\n\n    idx = self.currentIndex()\n\n    if idx &lt; 0 and self.placeholderText():\n        option.palette.setBrush(  # type: ignore\n            QPalette.ColorRole.ButtonText,\n            option.palette.placeholderText(),  # type: ignore\n        )\n        option.currentText = self.placeholderText()  # type: ignore\n\n    text = self._resolve_current_text(idx)\n    if text is not None:\n        option.currentText = text  # type: ignore\n\n    _style.drawControl(\n        QtWidgets.QStyle.ControlElement.CE_ComboBoxLabel,\n        option,\n        p,\n        self,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/combo_box.html#client.ayon_core.ui.components.combo_box.AYComboBox.setModel","title":"<code>setModel(model)</code>","text":"<p>Set the item model and detect compatibility with AYComboBox roles.</p> <p>If model is not an :class:<code>AYComboBoxModel</code> instance and it does not expose both <code>ShortTextRole</code> and <code>IconNameRole</code> attributes, it is marked as incompatible.  In that state:</p> <ul> <li>:meth:<code>add_item</code> and :meth:<code>update_items</code> will raise   :exc:<code>RuntimeError</code>.</li> <li>Short mode falls back to <code>\"&lt; INCOMPATIBLE MODEL &gt;\"</code> as the   displayed text.</li> </ul> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>QAbstractItemModel</code> <p>The new item model to attach to the combo-box.</p> required Source code in <code>client/ayon_core/ui/components/combo_box.py</code> <pre><code>def setModel(self, model: QtCore.QAbstractItemModel) -&gt; None:\n    \"\"\"Set the item model and detect compatibility with AYComboBox roles.\n\n    If *model* is not an :class:`AYComboBoxModel` instance **and** it\n    does not expose both ``ShortTextRole`` and ``IconNameRole``\n    attributes, it is marked as *incompatible*.  In that state:\n\n    - :meth:`add_item` and :meth:`update_items` will raise\n      :exc:`RuntimeError`.\n    - Short mode falls back to ``\"&lt; INCOMPATIBLE MODEL &gt;\"`` as the\n      displayed text.\n\n    Args:\n        model: The new item model to attach to the combo-box.\n    \"\"\"\n    mtype = type(model)\n    self._uses_incompatible_model = mtype is not AYComboBoxModel and (\n        not hasattr(model, \"IconNameRole\")\n        or not hasattr(model, \"ShortTextRole\")\n    )\n    super().setModel(model)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/combo_box.html#client.ayon_core.ui.components.combo_box.AYComboBox.set_inverted","title":"<code>set_inverted(state)</code>","text":"<p>Toggle the inverted colour mode and schedule a repaint.</p> <p>In inverted mode the icon's normal colour is drawn using the item's background colour instead of its foreground colour, producing a coloured icon on a neutral background.</p> <p>Inverted icons are generated lazily on first paint via :meth:<code>_get_inverted_icon</code> and cached in :attr:<code>_inverted_icons</code>. Calling this method triggers a repaint so the change is visible immediately.</p> <p>Parameters:</p> Name Type Description Default <code>state</code> <code>bool</code> <p><code>True</code> to enable inverted mode, <code>False</code> to disable.</p> required Source code in <code>client/ayon_core/ui/components/combo_box.py</code> <pre><code>def set_inverted(self, state: bool):\n    \"\"\"Toggle the inverted colour mode and schedule a repaint.\n\n    In *inverted* mode the icon's normal colour is drawn using the item's\n    **background** colour instead of its foreground colour, producing a\n    coloured icon on a neutral background.\n\n    Inverted icons are generated lazily on first paint via\n    :meth:`_get_inverted_icon` and cached in :attr:`_inverted_icons`.\n    Calling this method triggers a repaint so the change is visible\n    immediately.\n\n    Args:\n        state: ``True`` to enable inverted mode, ``False`` to disable.\n    \"\"\"\n    self._inverted = state\n    self.update()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/combo_box.html#client.ayon_core.ui.components.combo_box.AYComboBox.set_size","title":"<code>set_size(size)</code>","text":"<p>Change the display mode and repaint the widget.</p> <p>Parameters:</p> Name Type Description Default <code>size</code> <code>MenuSize | str</code> <p>New display mode.  Accepts a :class:<code>~ayon_core.ui.data_models.MenuSize</code> value or its lowercase string equivalent (<code>\"full\"</code>, <code>\"short\"</code>, <code>\"icon\"</code>).</p> required Source code in <code>client/ayon_core/ui/components/combo_box.py</code> <pre><code>def set_size(self, size: MenuSize | str):\n    \"\"\"Change the display mode and repaint the widget.\n\n    Args:\n        size: New display mode.  Accepts a\n            :class:`~ayon_core.ui.data_models.MenuSize` value or its\n            lowercase string equivalent (``\"full\"``, ``\"short\"``,\n            ``\"icon\"``).\n    \"\"\"\n    self._size = (\n        size if isinstance(size, MenuSize) else MenuSize(size.lower())\n    )\n    self.update()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/combo_box.html#client.ayon_core.ui.components.combo_box.AYComboBox.sizeHint","title":"<code>sizeHint()</code>","text":"<p>Return the preferred size for the combo-box.</p> <p>Delegates to the AYON style's :meth:<code>QStyle.sizeFromContents</code> so that the hint respects the custom theme's metrics rather than the system default.</p> <p>Returns:</p> Type Description <code>QSize</code> <p>The preferred :class:<code>QSize</code> for this widget.</p> Source code in <code>client/ayon_core/ui/components/combo_box.py</code> <pre><code>def sizeHint(self) -&gt; QtCore.QSize:\n    \"\"\"Return the preferred size for the combo-box.\n\n    Delegates to the AYON style's\n    :meth:`QStyle.sizeFromContents` so that the hint respects the\n    custom theme's metrics rather than the system default.\n\n    Returns:\n        The preferred :class:`QSize` for this widget.\n    \"\"\"\n\n    option = QtWidgets.QStyleOptionComboBox()\n    self.initStyleOption(option)\n    return get_ayon_style().sizeFromContents(\n        QtWidgets.QStyle.ContentsType.CT_ComboBox,\n        option,\n        self.rect().size(),\n        self,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/combo_box.html#client.ayon_core.ui.components.combo_box.AYComboBox.update_items","title":"<code>update_items(item_list=None)</code>","text":"<p>Replace all items in the model with the provided list.</p> <p>Clears the model and calls :meth:<code>add_item</code> for every entry in item_list.  If item_list is <code>None</code> or empty the model is left unchanged.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>If a custom incompatible model is currently set (see :meth:<code>setModel</code>).</p> <p>Parameters:</p> Name Type Description Default <code>item_list</code> <code>list[dict] | None</code> <p>List of item dicts as accepted by :meth:<code>add_item</code>. Pass <code>None</code> or an empty list to keep the current items.</p> <code>None</code> Source code in <code>client/ayon_core/ui/components/combo_box.py</code> <pre><code>def update_items(self, item_list: list[dict] | None = None):\n    \"\"\"Replace all items in the model with the provided list.\n\n    Clears the model and calls :meth:`add_item` for every entry in\n    *item_list*.  If *item_list* is ``None`` or empty the model is left\n    unchanged.\n\n    Raises:\n        RuntimeError: If a custom incompatible model is currently set\n            (see :meth:`setModel`).\n\n    Args:\n        item_list: List of item dicts as accepted by :meth:`add_item`.\n            Pass ``None`` or an empty list to keep the current items.\n    \"\"\"\n    if item_list:\n        self._assert_compatible_model()\n        self.model().clear()  # type: ignore\n        for item in item_list:\n            self.add_item(item)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/combo_box.html#client.ayon_core.ui.components.combo_box.AYComboBoxModel","title":"<code>AYComboBoxModel</code>","text":"<p>               Bases: <code>QStandardItemModel</code></p> <p>Default item model for :class:<code>AYComboBox</code>.</p> <p>Extends :class:<code>QStandardItemModel</code> with two additional item-data roles required by :class:<code>AYComboBox</code> for short-text and icon-regeneration support.</p> <p>Attributes:</p> Name Type Description <code>ShortTextRole</code> <p>Custom data role (<code>UserRole + 1</code>) that stores the abbreviated label displayed in :attr:<code>~ayon_core.ui.data_models.MenuSize.Short</code> mode.</p> <code>IconNameRole</code> <p>Custom data role (<code>UserRole + 2</code>) that stores the Material Symbol icon name so icons can be regenerated when the inverted colour mode changes.</p> Source code in <code>client/ayon_core/ui/components/combo_box.py</code> <pre><code>class AYComboBoxModel(QStandardItemModel):\n    \"\"\"Default item model for :class:`AYComboBox`.\n\n    Extends :class:`QStandardItemModel` with two additional item-data roles\n    required by :class:`AYComboBox` for short-text and icon-regeneration\n    support.\n\n    Attributes:\n        ShortTextRole: Custom data role (``UserRole + 1``) that stores the\n            abbreviated label displayed in\n            :attr:`~ayon_core.ui.data_models.MenuSize.Short` mode.\n        IconNameRole: Custom data role (``UserRole + 2``) that stores the\n            Material Symbol icon name so icons can be regenerated when the\n            *inverted* colour mode changes.\n    \"\"\"\n\n    ShortTextRole = QtCore.Qt.ItemDataRole.UserRole + 1\n    IconNameRole = QtCore.Qt.ItemDataRole.UserRole + 2\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/combo_box.html#client.ayon_core.ui.components.combo_box.ComboBoxItemDelegate","title":"<code>ComboBoxItemDelegate</code>","text":"<p>               Bases: <code>StyleMixin</code>, <code>QStyledItemDelegate</code></p> Source code in <code>client/ayon_core/ui/components/combo_box.py</code> <pre><code>class ComboBoxItemDelegate(StyleMixin, QtWidgets.QStyledItemDelegate):\n    def __init__(\n        self,\n        parent=None,\n        padding: int = 4,\n        icon_size: int = 16,\n        style_model: StyleData | None = None,\n    ) -&gt; None:\n        super().__init__(parent)\n        self._padding = padding\n        self._icon_size = icon_size\n        self._icon_text_spacing = 8\n        self._style_model = style_model\n        self._icon_cache: dict[str, QIcon] = {}\n\n    def sizeHint(\n        self,\n        option: QtWidgets.QStyleOptionViewItem,\n        index: QtCore.QModelIndex | QtCore.QPersistentModelIndex,\n    ) -&gt; QtCore.QSize:\n        \"\"\"Calculate size hint including padding.\"\"\"\n\n        # Calculate text dimensions\n        font_metrics = option.fontMetrics\n        text_size = font_metrics.size(0, option.text)\n\n        # Calculate content dimensions\n        content_width = text_size.width()\n        content_height = max(text_size.height(), self._icon_size)\n\n        # Add icon space if present\n        if option.icon:\n            content_width += self._icon_size + self._icon_text_spacing\n\n        # Add padding to get total size\n        total_width = content_width + self._padding + self._padding\n        total_height = content_height + self._padding + self._padding\n\n        # Ensure minimum height\n        total_height = max(total_height, 32)\n\n        return QtCore.QSize(total_width, total_height)\n\n    def _get_icon(\n        self, fg: QColor, bg: QColor, icon_name: str, invert: bool = True\n    ) -&gt; QIcon:\n        \"\"\"Get icon from cache or create new one.\"\"\"\n        key = f\"{icon_name}-{fg.name()}-{bg.name()}-{invert}\"\n        if key not in self._icon_cache:\n            self._icon_cache[key] = get_icon(\n                icon_name,\n                bg if invert else fg,\n            )\n        return self._icon_cache[key]\n\n    def initStyleOption(\n        self,\n        option: QtWidgets.QStyleOptionViewItem,\n        index: QtCore.QModelIndex | QtCore.QPersistentModelIndex,\n    ) -&gt; None:\n        \"\"\"Initialize style option and apply any custom font from model.\"\"\"\n        super().initStyleOption(option, index)\n        option.font = self.font()\n        option.fontMetrics = self.fontMetrics()\n\n    def paint(\n        self,\n        painter: QPainter,\n        option: QtWidgets.QStyleOptionViewItem,\n        index: QtCore.QModelIndex | QtCore.QPersistentModelIndex,\n    ) -&gt; None:\n        \"\"\"Paint combo-box items directly, bypassing QStyle.\n\n        This avoids QStyleSheetStyle intercepting drawPrimitive /\n        drawControl calls when an app-level QSS is active.\n        \"\"\"\n        painter.save()\n        painter.setRenderHint(QPainter.RenderHint.Antialiasing)\n\n        # Build a copy of the option with text/palette configured\n        opt = QtWidgets.QStyleOptionViewItem(option)\n        self.initStyleOption(opt, index)\n\n        # --- resolve colours -----------------------------------\n        fg_data = index.data(Qt.ItemDataRole.ForegroundRole)\n        bg_data = index.data(Qt.ItemDataRole.BackgroundRole)\n\n        cb = self.parent()\n\n        # Menu background from the AYON style JSON\n        if self._style_model:\n            cb_style = self._style_model.get_style(\"QComboBox\")\n            cb_style.set_context(cb)\n            menu_bg = QColor(cb_style.get(\"menu-background-color\", \"#1c2026\"))\n        else:\n            menu_bg = opt.palette.color(\n                QPalette.ColorGroup.Active,\n                QPalette.ColorRole.Window,\n            )\n\n        highlight_color = opt.palette.color(\n            QPalette.ColorGroup.Active, QPalette.ColorRole.Dark\n        )\n\n        state = opt.state\n        is_selected = bool(state &amp; QStyle.StateFlag.State_Selected)\n        is_hovered = (\n            bool(state &amp; QStyle.StateFlag.State_MouseOver) and not is_selected\n        )\n\n        if fg_data and bg_data:\n            fg = fg_data.color()\n            bg = bg_data.color()\n\n            if is_hovered:\n                bg_color = highlight_color\n                text_color = fg\n            elif is_selected:\n                bg_color = fg\n                text_color = bg\n                # Regenerate icon with the swapped text_color\n                icon_name = (\n                    index.data(cb.model().IconNameRole)\n                    if hasattr(cb.model(), \"IconNameRole\")\n                    else None\n                )\n                if icon_name:\n                    opt.icon = self._get_icon(\n                        text_color, bg_color, icon_name, False\n                    )\n            else:\n                bg_color = menu_bg\n                text_color = fg\n        else:\n            # Fallback for items without FG/BG data\n            if is_hovered or is_selected:\n                bg_color = highlight_color\n                text_color = opt.palette.color(\n                    QPalette.ColorRole.HighlightedText\n                )\n            else:\n                bg_color = menu_bg\n                text_color = opt.palette.color(QPalette.ColorRole.Text)\n\n        # --- draw background -----------------------------------\n        painter.setBrush(QBrush(bg_color))\n        painter.setPen(Qt.PenStyle.NoPen)\n        painter.drawRect(opt.rect)\n\n        # --- draw icon -----------------------------------------\n        content_left = opt.rect.left() + self._padding\n        if not opt.icon.isNull():\n            icon_rect = QRect(\n                content_left,\n                opt.rect.center().y() - self._icon_size // 2,\n                self._icon_size,\n                self._icon_size,\n            )\n            mode = (\n                QIcon.Mode.Normal\n                if opt.state &amp; QStyle.StateFlag.State_Enabled\n                else QIcon.Mode.Disabled\n            )\n            icon_state = (\n                QIcon.State.On\n                if (is_hovered or is_selected)\n                else QIcon.State.Off\n            )\n            opt.icon.paint(\n                painter,\n                icon_rect,\n                Qt.AlignmentFlag.AlignCenter,\n                mode,\n                icon_state,\n            )\n            content_left = icon_rect.right() + self._icon_text_spacing\n\n        # --- draw text -----------------------------------------\n        if opt.text:\n            text_rect = QRect(opt.rect)\n            text_rect.setLeft(content_left)\n            text_rect.setRight(text_rect.right() - self._padding)\n            painter.setPen(text_color)\n            painter.setFont(opt.font)\n            painter.drawText(\n                text_rect,\n                Qt.AlignmentFlag.AlignVCenter | Qt.AlignmentFlag.AlignLeft,\n                opt.text,\n            )\n\n        painter.restore()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/combo_box.html#client.ayon_core.ui.components.combo_box.ComboBoxItemDelegate.initStyleOption","title":"<code>initStyleOption(option, index)</code>","text":"<p>Initialize style option and apply any custom font from model.</p> Source code in <code>client/ayon_core/ui/components/combo_box.py</code> <pre><code>def initStyleOption(\n    self,\n    option: QtWidgets.QStyleOptionViewItem,\n    index: QtCore.QModelIndex | QtCore.QPersistentModelIndex,\n) -&gt; None:\n    \"\"\"Initialize style option and apply any custom font from model.\"\"\"\n    super().initStyleOption(option, index)\n    option.font = self.font()\n    option.fontMetrics = self.fontMetrics()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/combo_box.html#client.ayon_core.ui.components.combo_box.ComboBoxItemDelegate.paint","title":"<code>paint(painter, option, index)</code>","text":"<p>Paint combo-box items directly, bypassing QStyle.</p> <p>This avoids QStyleSheetStyle intercepting drawPrimitive / drawControl calls when an app-level QSS is active.</p> Source code in <code>client/ayon_core/ui/components/combo_box.py</code> <pre><code>def paint(\n    self,\n    painter: QPainter,\n    option: QtWidgets.QStyleOptionViewItem,\n    index: QtCore.QModelIndex | QtCore.QPersistentModelIndex,\n) -&gt; None:\n    \"\"\"Paint combo-box items directly, bypassing QStyle.\n\n    This avoids QStyleSheetStyle intercepting drawPrimitive /\n    drawControl calls when an app-level QSS is active.\n    \"\"\"\n    painter.save()\n    painter.setRenderHint(QPainter.RenderHint.Antialiasing)\n\n    # Build a copy of the option with text/palette configured\n    opt = QtWidgets.QStyleOptionViewItem(option)\n    self.initStyleOption(opt, index)\n\n    # --- resolve colours -----------------------------------\n    fg_data = index.data(Qt.ItemDataRole.ForegroundRole)\n    bg_data = index.data(Qt.ItemDataRole.BackgroundRole)\n\n    cb = self.parent()\n\n    # Menu background from the AYON style JSON\n    if self._style_model:\n        cb_style = self._style_model.get_style(\"QComboBox\")\n        cb_style.set_context(cb)\n        menu_bg = QColor(cb_style.get(\"menu-background-color\", \"#1c2026\"))\n    else:\n        menu_bg = opt.palette.color(\n            QPalette.ColorGroup.Active,\n            QPalette.ColorRole.Window,\n        )\n\n    highlight_color = opt.palette.color(\n        QPalette.ColorGroup.Active, QPalette.ColorRole.Dark\n    )\n\n    state = opt.state\n    is_selected = bool(state &amp; QStyle.StateFlag.State_Selected)\n    is_hovered = (\n        bool(state &amp; QStyle.StateFlag.State_MouseOver) and not is_selected\n    )\n\n    if fg_data and bg_data:\n        fg = fg_data.color()\n        bg = bg_data.color()\n\n        if is_hovered:\n            bg_color = highlight_color\n            text_color = fg\n        elif is_selected:\n            bg_color = fg\n            text_color = bg\n            # Regenerate icon with the swapped text_color\n            icon_name = (\n                index.data(cb.model().IconNameRole)\n                if hasattr(cb.model(), \"IconNameRole\")\n                else None\n            )\n            if icon_name:\n                opt.icon = self._get_icon(\n                    text_color, bg_color, icon_name, False\n                )\n        else:\n            bg_color = menu_bg\n            text_color = fg\n    else:\n        # Fallback for items without FG/BG data\n        if is_hovered or is_selected:\n            bg_color = highlight_color\n            text_color = opt.palette.color(\n                QPalette.ColorRole.HighlightedText\n            )\n        else:\n            bg_color = menu_bg\n            text_color = opt.palette.color(QPalette.ColorRole.Text)\n\n    # --- draw background -----------------------------------\n    painter.setBrush(QBrush(bg_color))\n    painter.setPen(Qt.PenStyle.NoPen)\n    painter.drawRect(opt.rect)\n\n    # --- draw icon -----------------------------------------\n    content_left = opt.rect.left() + self._padding\n    if not opt.icon.isNull():\n        icon_rect = QRect(\n            content_left,\n            opt.rect.center().y() - self._icon_size // 2,\n            self._icon_size,\n            self._icon_size,\n        )\n        mode = (\n            QIcon.Mode.Normal\n            if opt.state &amp; QStyle.StateFlag.State_Enabled\n            else QIcon.Mode.Disabled\n        )\n        icon_state = (\n            QIcon.State.On\n            if (is_hovered or is_selected)\n            else QIcon.State.Off\n        )\n        opt.icon.paint(\n            painter,\n            icon_rect,\n            Qt.AlignmentFlag.AlignCenter,\n            mode,\n            icon_state,\n        )\n        content_left = icon_rect.right() + self._icon_text_spacing\n\n    # --- draw text -----------------------------------------\n    if opt.text:\n        text_rect = QRect(opt.rect)\n        text_rect.setLeft(content_left)\n        text_rect.setRight(text_rect.right() - self._padding)\n        painter.setPen(text_color)\n        painter.setFont(opt.font)\n        painter.drawText(\n            text_rect,\n            Qt.AlignmentFlag.AlignVCenter | Qt.AlignmentFlag.AlignLeft,\n            opt.text,\n        )\n\n    painter.restore()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/combo_box.html#client.ayon_core.ui.components.combo_box.ComboBoxItemDelegate.sizeHint","title":"<code>sizeHint(option, index)</code>","text":"<p>Calculate size hint including padding.</p> Source code in <code>client/ayon_core/ui/components/combo_box.py</code> <pre><code>def sizeHint(\n    self,\n    option: QtWidgets.QStyleOptionViewItem,\n    index: QtCore.QModelIndex | QtCore.QPersistentModelIndex,\n) -&gt; QtCore.QSize:\n    \"\"\"Calculate size hint including padding.\"\"\"\n\n    # Calculate text dimensions\n    font_metrics = option.fontMetrics\n    text_size = font_metrics.size(0, option.text)\n\n    # Calculate content dimensions\n    content_width = text_size.width()\n    content_height = max(text_size.height(), self._icon_size)\n\n    # Add icon space if present\n    if option.icon:\n        content_width += self._icon_size + self._icon_text_spacing\n\n    # Add padding to get total size\n    total_width = content_width + self._padding + self._padding\n    total_height = content_height + self._padding + self._padding\n\n    # Ensure minimum height\n    total_height = max(total_height, 32)\n\n    return QtCore.QSize(total_width, total_height)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/combo_box.html#client.ayon_core.ui.components.combo_box.txt_color","title":"<code>txt_color(bg_color)</code>","text":"<p>Return a legible foreground colour for the given background colour.</p> <p>Uses the HSV value (brightness) component to decide between a light and a dark foreground:</p> <ul> <li>Backgrounds with <code>value &lt; 0.9</code> are considered dark \u2192 returns   <code>QColor(\"#eee\")</code> (near-white).</li> <li>Backgrounds with <code>value &gt;= 0.9</code> are considered light \u2192 returns   <code>QColor(\"#222\")</code> (near-black).</li> </ul> <p>Parameters:</p> Name Type Description Default <code>bg_color</code> <code>str | QColor</code> <p>Background colour as a CSS hex string (e.g. <code>\"#3498db\"</code>) or a :class:<code>QColor</code> instance.</p> required <p>Returns:</p> Name Type Description <code>A</code> <code>QColor</code> <p>class:<code>QColor</code> suitable for text rendered on bg_color.</p> Source code in <code>client/ayon_core/ui/components/combo_box.py</code> <pre><code>def txt_color(bg_color: str | QColor) -&gt; QColor:\n    \"\"\"Return a legible foreground colour for the given background colour.\n\n    Uses the HSV *value* (brightness) component to decide between a light\n    and a dark foreground:\n\n    - Backgrounds with ``value &lt; 0.9`` are considered dark \u2192 returns\n      ``QColor(\"#eee\")`` (near-white).\n    - Backgrounds with ``value &gt;= 0.9`` are considered light \u2192 returns\n      ``QColor(\"#222\")`` (near-black).\n\n    Args:\n        bg_color: Background colour as a CSS hex string (e.g. ``\"#3498db\"``)\n            or a :class:`QColor` instance.\n\n    Returns:\n        A :class:`QColor` suitable for text rendered on *bg_color*.\n    \"\"\"\n    value = QColor(bg_color).valueF()\n    return QColor(\"#eee\") if value &lt; 0.9 else QColor(\"#222\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/comment.html","title":"comment","text":""},{"location":"autoapi/client/ayon_core/ui/components/comment.html#client.ayon_core.ui.components.comment.AYComment","title":"<code>AYComment</code>","text":"<p>               Bases: <code>AYContainer</code></p> <p>Enhanced comment widget that displays images from CommentModel.files.</p> Source code in <code>client/ayon_core/ui/components/comment.py</code> <pre><code>class AYComment(AYContainer):\n    \"\"\"Enhanced comment widget that displays images from CommentModel.files.\"\"\"\n\n    comment_deleted = Signal(object)\n    comment_edited = Signal(object)\n\n    def __init__(\n        self,\n        *args,\n        data: CommentModel | None = None,\n        user_list: list[User] | None = None,\n        **kwargs,\n    ):\n        self._data = data if data else CommentModel()\n        self._user_list: list[User] = user_list or []\n        self._bg_color = None\n        self._image_widgets = {}\n        self._attachments_built = False\n\n        super().__init__(\n            *args,\n            layout=AYContainer.Layout.VBox,\n            variant=AYContainer.Variants.Low,\n            bg_tint=\"\",  # keep neutral\n            margin=0,\n            layout_spacing=0,\n            layout_margin=1,\n            **kwargs,\n        )\n\n        self._build()\n\n        # configure\n        if self._data:\n            self.update_comment()\n\n        self.text_field.checklist_changed.connect(self._on_checklist_changed)\n\n    def update_comment(self, data: CommentModel | None = None):\n        prev_data = self._data\n        if data:\n            self._data = data\n        self.text_field.set_markdown(self._data.comment)\n        self.date.setText(self._data.short_date)\n        self.set_comment_category()\n        if not self._attachments_built or prev_data.files != self._data.files:\n            self.images_container.clear()\n            self._build_image_attachments()\n\n    def _build_top_bar(self):\n        self.user_icon = AYUserImage(\n            parent=self,\n            size=20,\n            src=self._data.user_src,\n            name=self._data.user_name,\n            full_name=self._data.user_full_name,\n            outline=False,\n        )\n        self.user_name = AYLabel(self._data.user_full_name, bold=True)\n        self.date = AYLabel(self._data.short_date, dim=True, rel_text_size=-2)\n        cntr = AYContainer(\n            layout=AYContainer.Layout.HBox,\n            variant=AYContainer.Variants.Low,\n            margin=0,\n            layout_spacing=8,\n        )\n        cntr.setContentsMargins(0, 0, 0, 4)\n        cntr.add_widget(self.user_icon)\n        cntr.add_widget(self.user_name)\n        cntr.addStretch()\n        cntr.add_widget(self.date)\n        return cntr\n\n    def _build_editor_toolbar(self):\n        lyt = AYHBoxLayout()\n        self.reaction = AYButton(\n            variant=AYButton.Variants.Nav_Small,\n            icon=\"add_reaction\",\n            icon_color=\"#888\",\n            tooltip=\"Not Implemented Yet !\",\n            parent=self,\n        )\n        self.cancel_edit = AYButton(\n            \"Cancel\", variant=AYButton.Variants.Nav, parent=self\n        )\n        self.save_edit = AYButton(\n            \"Save\", variant=AYButton.Variants.Filled, parent=self\n        )\n        lyt.addWidget(self.reaction)\n        lyt.addStretch(10)\n        lyt.addWidget(self.cancel_edit)\n        lyt.addWidget(self.save_edit)\n\n        self.cancel_edit.clicked.connect(self._cancel_edit)\n        self.cancel_edit.setVisible(False)\n        self.save_edit.clicked.connect(self._save_edit)\n        self.save_edit.setVisible(False)\n        return lyt\n\n    def _build_edit_buttons(self):\n        self.edit_frame = AYContainer(\n            layout=AYContainer.Layout.HBox,\n            bg_tint=self._data.category_color,\n            parent=self.top_line,\n        )\n        bsize = 22\n        self.del_button = AYButton(\n            variant=AYButton.Variants.Nav_Small, icon=\"delete\", parent=self\n        )\n        self.del_button.setFixedSize(bsize, bsize)\n        self.edit_button = AYButton(\n            variant=AYButton.Variants.Nav_Small,\n            icon=\"edit_square\",\n            parent=self,\n        )\n        self.edit_button.setFixedSize(bsize, bsize)\n        self.edit_frame.add_widget(self.del_button)\n        self.edit_frame.add_widget(self.edit_button)\n        self.top_line.addStretch(100)\n        self.top_line.add_widget(self.edit_frame)\n        self.edit_frame.setVisible(False)\n        self.del_button.clicked.connect(self._confirm_delete)\n        self.edit_button.clicked.connect(self._edit_comment)\n\n    def _build(self):\n        self.add_widget(self._build_top_bar())\n        self.text_field = AYCommentField(\n            self,\n            text=self._data.comment,\n            read_only=True,\n            user_list=self._user_list,\n            model=self._data,\n            variant=AYCommentField.Variants.High,\n        )\n\n        self.editor_lyt = AYContainer(\n            layout=AYContainer.Layout.VBox,\n            variant=AYContainer.Variants.High,\n            bg_tint=self._data.category_color,\n            layout_margin=4,\n        )\n        self.top_line = AYContainer(\n            layout=AYContainer.Layout.HBox,\n            variant=AYContainer.Variants.High,\n            bg_tint=self._data.category_color,\n        )\n        self.images_container = AYContainer(\n            layout=AYContainer.Layout.Flow,\n            variant=AYContainer.Variants.High,\n            bg_tint=self._data.category_color,\n            layout_spacing=4,\n            layout_margin=0,\n        )\n        self.images_container.setContentsMargins(0, 0, 0, 0)\n        self.top_line.setFixedHeight(20)\n\n        # Create comment category once \u2014 hidden until a category is set\n        self.comment_category = AYLabel(\n            \"\",\n            icon_color=\"\",\n            variant=AYLabel.Variants.Badge,\n            rel_text_size=-2,\n        )\n        self.comment_category.setVisible(False)\n        self.top_line.insert_widget(0, self.comment_category)\n\n        self.editor_lyt.add_widget(self.top_line, stretch=0)\n        self.editor_lyt.add_widget(self.images_container, stretch=0)\n        self.editor_lyt.add_widget(self.text_field, stretch=10)\n\n        self.editor_lyt.add_layout(self._build_editor_toolbar(), stretch=0)\n        self.add_widget(self.editor_lyt)\n        self._build_edit_buttons()\n\n    def _build_image_attachments(self):\n        \"\"\"Build and display image attachments as separate clickable widgets.\n\n        Supports gallery view: when multiple images are present, clicking\n        any thumbnail opens a GalleryDialog for navigating through all images.\n        \"\"\"\n        if (\n            not self._data\n            or not hasattr(self._data, \"files\")\n            or not self._data.files\n        ):\n            return\n\n        # First pass: collect all valid images for gallery view\n        valid_files = []\n        for file_model in self._data.files:\n            # Check if this file is marked as transparent in annotations\n            is_transparent = False\n            if hasattr(self._data, \"annotations\"):\n                for annotation in self._data.annotations:\n                    if file_model.id == annotation.transparent:\n                        is_transparent = True\n                        break\n\n            if is_transparent:\n                continue\n            # Check if file has local_path\n            if not hasattr(file_model, \"local_path\"):\n                continue\n\n            # Guard against empty path: Path(\"\") resolves to Path(\".\")\n            # which always exists (current directory), leading to errors when\n            # trying to cache it as an image.\n            local_path = file_model.local_path\n            if not local_path or not Path(local_path).exists():\n                # Register as None so refresh_image can create the widget\n                # once the background download completes.\n                self._image_widgets.setdefault(file_model.id, None)\n\n            valid_files.append(file_model)\n\n        # Build gallery images list for navigation\n        gallery_images = [\n            (\n                f.local_path,\n                f\"Frame {f.start_frame + f.frame - 1 if f.frame &gt; 0 else -1}\",\n            )\n            for f in valid_files\n        ]\n\n        # Second pass: create widgets with gallery support\n        for idx, file_model in enumerate(valid_files):\n            max_image_width = 100\n            max_image_height = 47\n\n            # coerce any invalid thumb path to None.\n            thumb_path = getattr(file_model, \"thumb_local_path\", None) or None\n\n            # frame sequences start at 1, so we need to subtract 1 to get the\n            # actual frame number.\n            # if frame is 0 or negative, we treat it as n/a. This happens when\n            # attaching a screenshot or external file.\n            frame = (\n                file_model.start_frame + file_model.frame - 1\n                if file_model.frame &gt; 0\n                else -1\n            )\n\n            # Create image widget with gallery support\n            image_widget = AYImageAttachment(\n                parent=self,\n                image_path=local_path,\n                thumb_path=thumb_path,\n                max_width=max_image_width,\n                max_height=max_image_height,\n                frame=frame,\n                gallery_images=gallery_images,\n                gallery_index=idx,\n            )\n\n            self.images_container.add_widget(image_widget)\n            self._image_widgets[file_model.id] = image_widget\n\n        # mark as built to avoid rebuilding on every update\n        self._attachments_built = True\n\n    def _edit_comment(self):\n        \"\"\"Make the field editable, hide the edit/del buttons and show\n        Save/Cancel.\"\"\"\n        self._show_edit_buttons(False)\n        self.text_field.setReadOnly(False)\n        self.cancel_edit.setVisible(True)\n        self.save_edit.setVisible(True)\n\n    def _cancel_edit(self):\n        \"\"\"Make the field read-only and restore text.\"\"\"\n        self.text_field.setReadOnly(True)\n        self.cancel_edit.setVisible(False)\n        self.save_edit.setVisible(False)\n        self.text_field.set_markdown(self._data.comment)\n        self._show_edit_buttons(True)\n\n    def _save_edit(self):\n        self.text_field.setReadOnly(True)\n        self.cancel_edit.setVisible(False)\n        self.save_edit.setVisible(False)\n        self._show_edit_buttons(True)\n        self._data.comment = self.text_field.as_markdown()\n        self.comment_edited.emit(self._data)\n\n    def _confirm_delete(self):\n        mb = QMessageBox(\n            text=\"Are you sure you want to delete this comment?\",\n            standardButtons=QMessageBox.StandardButton.Cancel\n            | QMessageBox.StandardButton.Yes,  # type: ignore\n            parent=self,\n        )\n        if mb.exec() == QMessageBox.StandardButton.Yes:\n            self.comment_deleted.emit(self._data)\n\n    def _show_edit_buttons(self, state):\n        \"\"\"show / hide edit buttons and position them.\"\"\"\n        if not self.text_field.isReadOnly():\n            return\n        self.edit_frame.setVisible(state)\n        if state:\n            fr = self.edit_frame.rect()\n            vr = self.text_field.visibleRegion().boundingRect()\n            self.edit_frame.move((vr.width() + vr.x()) - fr.width(), 0)\n\n    def set_comment_category(self):\n        \"\"\"Update the comment category and comment background tint\"\"\"\n        self._update_category_bg_tint()\n\n        if not self._data.category:\n            self.comment_category.setVisible(False)\n            return\n\n        # Update comment category\n        self.comment_category.setText(self._data.category)\n        self.comment_category.set_icon_color(self._data.category_color)\n        self.comment_category.setVisible(True)\n\n    def _update_category_bg_tint(self):\n        \"\"\"Update bg_tint on containers that use category_color.\"\"\"\n        tint = self._data.category_color or \"\"\n        for widget in (\n            self.editor_lyt,\n            self.top_line,\n            self.images_container,\n            self.edit_frame,\n        ):\n            widget._bg_tint = tint\n            widget._bg_color = None  # reset cache so it recalculates\n            widget.update()  # trigger repaint\n\n    def enterEvent(self, event: QEnterEvent) -&gt; None:\n        self._show_edit_buttons(True)\n        return super().enterEvent(event)\n\n    def leaveEvent(self, event: QEvent) -&gt; None:\n        self._show_edit_buttons(False)\n        return super().leaveEvent(event)\n\n    def update_params(self, model: CommentModel):\n        if self._data:\n            self.user_icon.update_params(\n                self._data.user_src, self._data.user_full_name\n            )\n            self.user_name.setText(self._data.user_name)\n            self.date.setText(self._data.short_date)\n\n    def refresh_image(\n        self,\n        file_id: str,\n        filepath: str | None,\n        project_name: str,\n    ) -&gt; None:\n        \"\"\"Refresh image attachment widget once a file download completes.\n\n        Called by the activity stream when a background download finishes.\n        If the widget hasn't been created yet (path was unavailable at build\n        time), creates and adds it now.\n\n        Args:\n            file_id: The AYON file identifier.\n            filepath: Unused - paths are resolved from ImageCache.\n            project_name: AYON project name that owns the file, used to\n                build the cache key via\n                :func:`ayon_core.ui.image_cache.make_activity_cache_key`.\n        \"\"\"\n        ic = ImageCache.get_instance()\n        image_attachment = self._image_widgets.get(file_id)\n\n        if image_attachment is None and file_id in self._image_widgets:\n            # Widget placeholder registered but not yet created - build it\n            # now that the download has completed.\n            image_path = (\n                ic.get_path(\n                    make_activity_cache_key(project_name, file_id),\n                )\n                or \"\"\n            )\n            thumb_path = ic.get_path(\n                make_activity_cache_key(\n                    project_name, file_id, is_thumbnail=True\n                )\n            )\n            if not image_path:\n                return  # Full-size not ready yet; thumbnail alone is enough\n            image_attachment = AYImageAttachment(\n                parent=self,\n                image_path=image_path,\n                thumb_path=thumb_path,\n                max_width=100,\n                max_height=47,\n            )\n            self.images_container.add_widget(image_attachment)\n            self._image_widgets[file_id] = image_attachment\n            return\n\n        if isinstance(image_attachment, AYImageAttachment):\n            if not image_attachment._thumb_path:\n                image_attachment._thumb_path = (\n                    ic.get_path(\n                        make_activity_cache_key(\n                            project_name, file_id, is_thumbnail=True\n                        )\n                    )\n                    or \"\"\n                )\n            if not image_attachment._image_path:\n                image_attachment._image_path = (\n                    ic.get_path(\n                        make_activity_cache_key(project_name, file_id),\n                    )\n                    or \"\"\n                )\n            if image_attachment._thumb_path or image_attachment._image_path:\n                image_attachment._load_thumbnail()\n\n    def _on_checklist_changed(self):\n        md = self.text_field.as_markdown()\n        self._data.comment = md\n        self.comment_edited.emit(self._data)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/comment.html#client.ayon_core.ui.components.comment.AYComment.refresh_image","title":"<code>refresh_image(file_id, filepath, project_name)</code>","text":"<p>Refresh image attachment widget once a file download completes.</p> <p>Called by the activity stream when a background download finishes. If the widget hasn't been created yet (path was unavailable at build time), creates and adds it now.</p> <p>Parameters:</p> Name Type Description Default <code>file_id</code> <code>str</code> <p>The AYON file identifier.</p> required <code>filepath</code> <code>str | None</code> <p>Unused - paths are resolved from ImageCache.</p> required <code>project_name</code> <code>str</code> <p>AYON project name that owns the file, used to build the cache key via :func:<code>ayon_core.ui.image_cache.make_activity_cache_key</code>.</p> required Source code in <code>client/ayon_core/ui/components/comment.py</code> <pre><code>def refresh_image(\n    self,\n    file_id: str,\n    filepath: str | None,\n    project_name: str,\n) -&gt; None:\n    \"\"\"Refresh image attachment widget once a file download completes.\n\n    Called by the activity stream when a background download finishes.\n    If the widget hasn't been created yet (path was unavailable at build\n    time), creates and adds it now.\n\n    Args:\n        file_id: The AYON file identifier.\n        filepath: Unused - paths are resolved from ImageCache.\n        project_name: AYON project name that owns the file, used to\n            build the cache key via\n            :func:`ayon_core.ui.image_cache.make_activity_cache_key`.\n    \"\"\"\n    ic = ImageCache.get_instance()\n    image_attachment = self._image_widgets.get(file_id)\n\n    if image_attachment is None and file_id in self._image_widgets:\n        # Widget placeholder registered but not yet created - build it\n        # now that the download has completed.\n        image_path = (\n            ic.get_path(\n                make_activity_cache_key(project_name, file_id),\n            )\n            or \"\"\n        )\n        thumb_path = ic.get_path(\n            make_activity_cache_key(\n                project_name, file_id, is_thumbnail=True\n            )\n        )\n        if not image_path:\n            return  # Full-size not ready yet; thumbnail alone is enough\n        image_attachment = AYImageAttachment(\n            parent=self,\n            image_path=image_path,\n            thumb_path=thumb_path,\n            max_width=100,\n            max_height=47,\n        )\n        self.images_container.add_widget(image_attachment)\n        self._image_widgets[file_id] = image_attachment\n        return\n\n    if isinstance(image_attachment, AYImageAttachment):\n        if not image_attachment._thumb_path:\n            image_attachment._thumb_path = (\n                ic.get_path(\n                    make_activity_cache_key(\n                        project_name, file_id, is_thumbnail=True\n                    )\n                )\n                or \"\"\n            )\n        if not image_attachment._image_path:\n            image_attachment._image_path = (\n                ic.get_path(\n                    make_activity_cache_key(project_name, file_id),\n                )\n                or \"\"\n            )\n        if image_attachment._thumb_path or image_attachment._image_path:\n            image_attachment._load_thumbnail()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/comment.html#client.ayon_core.ui.components.comment.AYComment.set_comment_category","title":"<code>set_comment_category()</code>","text":"<p>Update the comment category and comment background tint</p> Source code in <code>client/ayon_core/ui/components/comment.py</code> <pre><code>def set_comment_category(self):\n    \"\"\"Update the comment category and comment background tint\"\"\"\n    self._update_category_bg_tint()\n\n    if not self._data.category:\n        self.comment_category.setVisible(False)\n        return\n\n    # Update comment category\n    self.comment_category.setText(self._data.category)\n    self.comment_category.set_icon_color(self._data.category_color)\n    self.comment_category.setVisible(True)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/comment.html#client.ayon_core.ui.components.comment.AYCommentField","title":"<code>AYCommentField</code>","text":"<p>               Bases: <code>AYTextEdit</code></p> <p>Text field for comment display with markdown and checkbox support.</p> <p>Supports GitHub-flavored markdown checkboxes (- [ ] and - [x]) that render with Material icons and can be toggled even in read-only mode.</p> Signals <p>checklist_changed: Emitted when a checkbox state changes.</p> Source code in <code>client/ayon_core/ui/components/comment.py</code> <pre><code>class AYCommentField(AYTextEdit):\n    \"\"\"Text field for comment display with markdown and checkbox support.\n\n    Supports GitHub-flavored markdown checkboxes (- [ ] and - [x]) that\n    render with Material icons and can be toggled even in read-only mode.\n\n    Signals:\n        checklist_changed: Emitted when a checkbox state changes.\n    \"\"\"\n\n    Variants = QTextEditVariants\n    checklist_changed = Signal()\n\n    def __init__(\n        self,\n        *args,\n        text: str = \"\",\n        read_only: bool = False,\n        num_lines: int = 0,\n        user_list: list[User] | None = None,\n        model: CommentModel | None = None,\n        variant: Variants = Variants.Default,\n        **kwargs,\n    ) -&gt; None:\n        # remove our kwargs\n        self._num_lines = num_lines\n        self._read_only: bool = read_only\n        self._user_list: list[User] = user_list or []\n        self._user_full_name_by_username = {\n            user.name: user.full_name for user in self._user_list\n        }\n        self._data = model\n        self._bg_color = None\n        self._checkbox_handler: CheckboxHandler | None = None\n        # Guard flag: when True, format_comment_on_change is a no-op.\n        self._suppress_formatting: bool = False\n\n        super().__init__(*args, variant=variant, **kwargs)\n        self.setAutoFormatting(QTextEdit.AutoFormattingFlag.AutoAll)\n        self.setSizeAdjustPolicy(QTextEdit.SizeAdjustPolicy.AdjustToContents)\n        self.document().setIndentWidth(22)\n        # Enable mouse tracking on viewport to receive mouseMoveEvent\n        # self.setMouseTracking(True)\n        self.viewport().setMouseTracking(True)\n        self.set_markdown(text)\n\n        # configure\n        if self._read_only:\n            # Auto-size to content for read-only fields\n            self.document().contentsChanged.connect(\n                self._adjust_height_to_content\n            )\n            self._adjust_height_to_content()\n        elif num_lines:\n            height = int(self.fontMetrics().lineSpacing()) * num_lines + 8 + 8\n            self.setFixedHeight(height)\n\n        if not self._read_only:\n            self.setPlaceholderText(\n                \"Comment or mention with @user, @@version, @@@task...\"\n            )\n        self.setReadOnly(self._read_only)\n\n        # Setup user completer\n        setup_user_completer(\n            self,\n            self._on_completer_activated,\n            self._on_text_changed,\n        )\n\n        # Connect text changed signal to format mentions\n        self.document().contentsChanged.connect(self._on_contents_changed)\n\n    def _on_contents_changed(self) -&gt; None:\n        \"\"\"Forward contentsChanged to format_comment_on_change.\n\n        Skipped when ``_suppress_formatting`` is True so that checkbox\n        insertion code can mutate the document without triggering a\n        full re-format pass.\n        \"\"\"\n        if not self._suppress_formatting:\n            format_comment_on_change(self)\n            apply_code_block_backgrounds(self)\n\n    def get_bg_color(self, base_color: str):\n        if not self._bg_color:\n            self._bg_color = base_color\n            if self._data and self._data.category_color:\n                self._bg_color = color_blend(\n                    base_color, self._data.category_color, 0.1\n                )\n        return self._bg_color\n\n    def set_markdown(self, md: str) -&gt; None:\n        \"\"\"Set markdown content with checkbox and web markdown support.\n\n        Supports:\n        - GitHub-flavored markdown checkboxes (- [ ] and - [x])\n        - Web markdown syntax (text\\\\n----, **bold**, _italic_, [link](url))\n        - Standard QTextDocument markdown\n\n        Args:\n            md: Markdown text to display\n        \"\"\"\n        # Check for checkboxes first\n        if CheckboxHandler.contains_checkboxes(md):\n            self._setup_checkbox_handler()\n            # Handler is guaranteed to exist after _setup_checkbox_handler\n            assert self._checkbox_handler is not None\n            self._checkbox_handler.parse_and_render(md)\n            if self._read_only:\n                self._adjust_height_to_content()\n            return\n\n        display_md = self._inject_user_mention_display(md)\n        self.document().setMarkdown(display_md, MD_DIALECT)\n        apply_code_block_backgrounds(self)\n\n        if self._read_only:\n            self._adjust_height_to_content()\n\n    def _inject_user_mention_display(self, md: str) -&gt; str:\n        \"\"\"Convert user mention links to plain @mentions.\n\n        Args:\n            md: Markdown text with links like [label](user:id)\n\n        Returns:\n            str: Text with links replaced by @full_name (or the original link\n                if the user is not found).\n        \"\"\"\n        def repl(match) -&gt; str:\n            username = match.group(\"username\")\n            full_name = self._user_full_name_by_username.get(username)\n            if full_name:\n                return f\"@{full_name}\"\n            # Fallback: keep the original link if user lookup fails\n            return match.group(0)\n\n        return USER_MENTION_LINK_PATTERN.sub(repl, md)\n\n    def _setup_checkbox_handler(self) -&gt; None:\n        \"\"\"Initialize checkbox handler if not already done.\"\"\"\n        if self._checkbox_handler is None:\n            self._checkbox_handler = CheckboxHandler(self)\n            self._checkbox_handler.checklist_changed.connect(\n                self._on_checklist_changed\n            )\n\n    def _on_checklist_changed(self) -&gt; None:\n        \"\"\"Handle checkbox state changes.\"\"\"\n        self.checklist_changed.emit()\n        if self._read_only:\n            self._adjust_height_to_content()\n\n    def as_markdown(self) -&gt; str:\n        \"\"\"Get the content as GitHub-flavored markdown.\n\n        If the field contains checkboxes, returns the markdown with\n        checkbox syntax (- [ ] and - [x]) reflecting current state.\n\n        Returns:\n            Markdown string.\n        \"\"\"\n        if self._checkbox_handler and self._checkbox_handler.has_checkboxes():\n            return self._checkbox_handler.to_markdown()\n        rendered_md = self.document().toMarkdown(MD_DIALECT)\n        return strip_user_mention_display(rendered_md, self._user_list)\n\n    def _on_text_changed(self) -&gt; None:\n        \"\"\"Handle text changes to show/hide completer.\"\"\"\n        on_completer_text_changed(self)\n\n    def _on_completer_activated(self, text: str) -&gt; None:\n        \"\"\"Handle completer selection.\"\"\"\n        on_completer_activated(self, text)\n\n    def _adjust_height_to_content(self) -&gt; None:\n        \"\"\"Adjust widget height to fit document content\n        (read-only mode only).\"\"\"\n        if not self._read_only:\n            return\n\n        # Get document height\n        doc = self.document()\n        doc.setTextWidth(self.viewport().width())\n        doc_height = doc.size().height()\n\n        # Add frame margins (top + bottom)\n        frame_width = self.frameWidth()\n        margins = self.contentsMargins()\n        total_height = (\n            int(doc_height)\n            + frame_width * 2\n            + margins.top()\n            + margins.bottom()\n        )\n\n        self.setFixedHeight(total_height)\n\n    def resizeEvent(self, event) -&gt; None:\n        \"\"\"Recalculate height when width changes (affects text wrapping).\"\"\"\n        super().resizeEvent(event)\n        if self._read_only:\n            self._adjust_height_to_content()\n\n    def contentOffset(self) -&gt; QPointF:\n        \"\"\"Compute content offset (QPlainTextEdit compatibility).\n\n        Returns the offset from viewport coordinates to document coordinates.\n        This method provides compatibility with QPlainTextEdit for checkbox\n        hit-testing in _is_checkbox_at_cursor.\n\n        Returns:\n            QPointF offset where viewport origin corresponds to document\n            coords.\n        \"\"\"\n        return QPointF(\n            -self.horizontalScrollBar().value(),\n            -self.verticalScrollBar().value(),\n        )\n\n    def _insert_checkbox_at_cursor(self, cursor: QTextCursor) -&gt; None:\n        \"\"\"Insert a new unchecked checkbox object at the cursor position.\n\n        Inserts the custom checkbox object character (``\\\\ufffc``), and a\n        trailing space, then records the document position on the\n        :class:`CheckboxItem` for fast hit-testing.\n        The checkbox handler must already be initialised via\n        :meth:`_setup_checkbox_handler`.\n\n        Args:\n            cursor: Cursor at the insertion point; advanced past the\n                inserted characters on return.\n        \"\"\"\n        assert self._checkbox_handler is not None\n        new_item = self._checkbox_handler.add_checkbox()\n        fmt = QTextCharFormat()\n        fmt.setObjectType(CHECKBOX_FORMAT_TYPE)\n        fmt.setProperty(CHECKBOX_CHECKED_PROP, False)\n        fmt.setProperty(CHECKBOX_INDEX_PROP, new_item.index)\n        fmt.setVerticalAlignment(\n            QTextCharFormat.VerticalAlignment.AlignBaseline\n        )\n        new_item.doc_position = cursor.position()\n        cursor.insertText(\"\\ufffc\", fmt)\n        cursor.insertText(\" \")\n\n    def keyPressEvent(self, event) -&gt; None:\n        \"\"\"Handle key press events for completer.\"\"\"\n        if on_completer_key_press(self, event):\n            event.accept()\n            return\n\n        # Auto-continue checkbox on Enter\n        if (\n            event.key() in {Qt.Key.Key_Return, Qt.Key.Key_Enter}\n            and self._checkbox_handler\n            and self._checkbox_handler.has_checkboxes()\n        ):\n            cursor = self.textCursor()\n            block_text = cursor.block().text()\n\n            if \"\\ufffc\" in block_text:\n                # Extract text after the checkbox object char\n                parts = block_text.split(\"\\ufffc\", 1)\n                after_checkbox = parts[1].strip() if len(parts) &gt; 1 else \"\"\n                position_in_block = cursor.positionInBlock()\n                token_pos_in_block = block_text.index(\"\\ufffc\")\n                token_pos = (\n                    cursor.position() - position_in_block + token_pos_in_block\n                )\n                cb_index = self._checkbox_handler.get_checkbox_at_position(\n                    token_pos\n                )\n\n                if not after_checkbox:\n                    self._suppress_formatting = True\n                    try:\n                        # Empty checkbox line \u2192 end the list\n                        # Remove the checkbox content from current block\n                        cursor.movePosition(\n                            QTextCursor.MoveOperation.StartOfBlock,\n                            QTextCursor.MoveMode.MoveAnchor,\n                        )\n                        cursor.movePosition(\n                            QTextCursor.MoveOperation.EndOfBlock,\n                            QTextCursor.MoveMode.KeepAnchor,\n                        )\n                        cursor.removeSelectedText()\n                        # Remove the last checkbox from handler\n                        if cb_index is not None:\n                            self._checkbox_handler.remove_checkbox(cb_index)\n                    finally:\n                        self._suppress_formatting = False\n                    # Insert a plain newline\n                    super().keyPressEvent(event)\n                    return\n\n                # Non-empty checkbox line \u2192 insert new checkbox\n                event.accept()\n                self._suppress_formatting = True\n\n                try:\n                    cursor.beginEditBlock()\n                    cursor.movePosition(QTextCursor.MoveOperation.EndOfBlock)\n                    cursor.insertBlock()\n                    self._insert_checkbox_at_cursor(cursor)\n                    cursor.endEditBlock()\n                except Exception as err:\n                    logger.debug(\"Error inserting checkbox: %s\", err)\n                finally:\n                    self._suppress_formatting = False\n                self.setTextCursor(cursor)\n                return\n\n        super().keyPressEvent(event)\n\n    def _is_checkbox_at_cursor(\n        self, click_pos: QPoint\n    ) -&gt; tuple[bool, int | None]:\n        \"\"\"Check if a click position hits a checkbox bounding rect.\n\n        Delegates to `CheckboxHandler.find_checkbox_at_click` which\n        uses stored document positions instead of scanning the full text.\n\n        Args:\n            click_pos: QPoint from event.pos(), viewport coords.\n\n        Returns:\n            Tuple of (is_checkbox, document_position_for_lookup).\n        \"\"\"\n        if not self._checkbox_handler:\n            return False, None\n\n        result = self._checkbox_handler.find_checkbox_at_click(\n            click_pos, self.contentOffset().toPoint()\n        )\n        if result is None:\n            return False, None\n        return result\n\n    def mouseDoubleClickEvent(self, event) -&gt; None:\n        \"\"\"Prevent text selection when double-clicking checkboxes.\"\"\"\n        is_cb, cb_pos = self._is_checkbox_at_cursor(event.pos())\n        if is_cb and cb_pos is not None:\n            event.accept()\n            return\n        super().mouseDoubleClickEvent(event)\n\n    def mousePressEvent(self, event) -&gt; None:\n        \"\"\"Handle mouse press events for checkboxes and links.\n\n        Checkboxes can be toggled even in read-only mode.\n        Links are opened only in read-only mode.\n        \"\"\"\n        # Get the character at the click position\n        cursor = self.cursorForPosition(event.pos())\n        char_format = cursor.charFormat()\n\n        # Check if clicked on a checkbox (works in both modes)\n        is_cb, cb_pos = self._is_checkbox_at_cursor(event.pos())\n        if is_cb and cb_pos is not None:\n            assert self._checkbox_handler is not None  # implied by is_cb==True\n            checkbox_idx = self._checkbox_handler.get_checkbox_at_position(\n                cb_pos\n            )\n            if checkbox_idx is not None:\n                self._checkbox_handler.toggle_checkbox(checkbox_idx)\n                self.viewport().update()\n                event.accept()\n                return\n\n        # Handle link clicks only in read-only mode (display comments)\n        if self.isReadOnly():\n            # Check if the clicked text is a link (has anchor href)\n            if char_format.isAnchor() and char_format.anchorHref():\n                url = char_format.anchorHref()\n                webbrowser.open(url)\n                event.accept()\n                return\n\n        super().mousePressEvent(event)\n\n    def mouseMoveEvent(self, event) -&gt; None:\n        \"\"\"Change cursor to arrow when hovering over checkboxes, hand for\n        links.\"\"\"\n        cursor = self.cursorForPosition(event.pos())\n        char_format = cursor.charFormat()\n\n        # Show arrow cursor for checkboxes (in any mode)\n        if char_format.objectType() == CHECKBOX_FORMAT_TYPE:\n            self.viewport().setCursor(Qt.CursorShape.ArrowCursor)\n            super().mouseMoveEvent(event)\n            return\n\n        # Show hand cursor for links (only in read-only mode)\n        if self.isReadOnly():\n            if char_format.isAnchor() and char_format.anchorHref():\n                self.viewport().setCursor(Qt.CursorShape.PointingHandCursor)\n            else:\n                self.viewport().setCursor(Qt.CursorShape.ArrowCursor)\n        else:\n            self.viewport().setCursor(Qt.CursorShape.IBeamCursor)\n\n        super().mouseMoveEvent(event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/comment.html#client.ayon_core.ui.components.comment.AYCommentField.as_markdown","title":"<code>as_markdown()</code>","text":"<p>Get the content as GitHub-flavored markdown.</p> <p>If the field contains checkboxes, returns the markdown with checkbox syntax (- [ ] and - [x]) reflecting current state.</p> <p>Returns:</p> Type Description <code>str</code> <p>Markdown string.</p> Source code in <code>client/ayon_core/ui/components/comment.py</code> <pre><code>def as_markdown(self) -&gt; str:\n    \"\"\"Get the content as GitHub-flavored markdown.\n\n    If the field contains checkboxes, returns the markdown with\n    checkbox syntax (- [ ] and - [x]) reflecting current state.\n\n    Returns:\n        Markdown string.\n    \"\"\"\n    if self._checkbox_handler and self._checkbox_handler.has_checkboxes():\n        return self._checkbox_handler.to_markdown()\n    rendered_md = self.document().toMarkdown(MD_DIALECT)\n    return strip_user_mention_display(rendered_md, self._user_list)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/comment.html#client.ayon_core.ui.components.comment.AYCommentField.contentOffset","title":"<code>contentOffset()</code>","text":"<p>Compute content offset (QPlainTextEdit compatibility).</p> <p>Returns the offset from viewport coordinates to document coordinates. This method provides compatibility with QPlainTextEdit for checkbox hit-testing in _is_checkbox_at_cursor.</p> <p>Returns:</p> Type Description <code>QPointF</code> <p>QPointF offset where viewport origin corresponds to document</p> <code>QPointF</code> <p>coords.</p> Source code in <code>client/ayon_core/ui/components/comment.py</code> <pre><code>def contentOffset(self) -&gt; QPointF:\n    \"\"\"Compute content offset (QPlainTextEdit compatibility).\n\n    Returns the offset from viewport coordinates to document coordinates.\n    This method provides compatibility with QPlainTextEdit for checkbox\n    hit-testing in _is_checkbox_at_cursor.\n\n    Returns:\n        QPointF offset where viewport origin corresponds to document\n        coords.\n    \"\"\"\n    return QPointF(\n        -self.horizontalScrollBar().value(),\n        -self.verticalScrollBar().value(),\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/comment.html#client.ayon_core.ui.components.comment.AYCommentField.keyPressEvent","title":"<code>keyPressEvent(event)</code>","text":"<p>Handle key press events for completer.</p> Source code in <code>client/ayon_core/ui/components/comment.py</code> <pre><code>def keyPressEvent(self, event) -&gt; None:\n    \"\"\"Handle key press events for completer.\"\"\"\n    if on_completer_key_press(self, event):\n        event.accept()\n        return\n\n    # Auto-continue checkbox on Enter\n    if (\n        event.key() in {Qt.Key.Key_Return, Qt.Key.Key_Enter}\n        and self._checkbox_handler\n        and self._checkbox_handler.has_checkboxes()\n    ):\n        cursor = self.textCursor()\n        block_text = cursor.block().text()\n\n        if \"\\ufffc\" in block_text:\n            # Extract text after the checkbox object char\n            parts = block_text.split(\"\\ufffc\", 1)\n            after_checkbox = parts[1].strip() if len(parts) &gt; 1 else \"\"\n            position_in_block = cursor.positionInBlock()\n            token_pos_in_block = block_text.index(\"\\ufffc\")\n            token_pos = (\n                cursor.position() - position_in_block + token_pos_in_block\n            )\n            cb_index = self._checkbox_handler.get_checkbox_at_position(\n                token_pos\n            )\n\n            if not after_checkbox:\n                self._suppress_formatting = True\n                try:\n                    # Empty checkbox line \u2192 end the list\n                    # Remove the checkbox content from current block\n                    cursor.movePosition(\n                        QTextCursor.MoveOperation.StartOfBlock,\n                        QTextCursor.MoveMode.MoveAnchor,\n                    )\n                    cursor.movePosition(\n                        QTextCursor.MoveOperation.EndOfBlock,\n                        QTextCursor.MoveMode.KeepAnchor,\n                    )\n                    cursor.removeSelectedText()\n                    # Remove the last checkbox from handler\n                    if cb_index is not None:\n                        self._checkbox_handler.remove_checkbox(cb_index)\n                finally:\n                    self._suppress_formatting = False\n                # Insert a plain newline\n                super().keyPressEvent(event)\n                return\n\n            # Non-empty checkbox line \u2192 insert new checkbox\n            event.accept()\n            self._suppress_formatting = True\n\n            try:\n                cursor.beginEditBlock()\n                cursor.movePosition(QTextCursor.MoveOperation.EndOfBlock)\n                cursor.insertBlock()\n                self._insert_checkbox_at_cursor(cursor)\n                cursor.endEditBlock()\n            except Exception as err:\n                logger.debug(\"Error inserting checkbox: %s\", err)\n            finally:\n                self._suppress_formatting = False\n            self.setTextCursor(cursor)\n            return\n\n    super().keyPressEvent(event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/comment.html#client.ayon_core.ui.components.comment.AYCommentField.mouseDoubleClickEvent","title":"<code>mouseDoubleClickEvent(event)</code>","text":"<p>Prevent text selection when double-clicking checkboxes.</p> Source code in <code>client/ayon_core/ui/components/comment.py</code> <pre><code>def mouseDoubleClickEvent(self, event) -&gt; None:\n    \"\"\"Prevent text selection when double-clicking checkboxes.\"\"\"\n    is_cb, cb_pos = self._is_checkbox_at_cursor(event.pos())\n    if is_cb and cb_pos is not None:\n        event.accept()\n        return\n    super().mouseDoubleClickEvent(event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/comment.html#client.ayon_core.ui.components.comment.AYCommentField.mouseMoveEvent","title":"<code>mouseMoveEvent(event)</code>","text":"<p>Change cursor to arrow when hovering over checkboxes, hand for links.</p> Source code in <code>client/ayon_core/ui/components/comment.py</code> <pre><code>def mouseMoveEvent(self, event) -&gt; None:\n    \"\"\"Change cursor to arrow when hovering over checkboxes, hand for\n    links.\"\"\"\n    cursor = self.cursorForPosition(event.pos())\n    char_format = cursor.charFormat()\n\n    # Show arrow cursor for checkboxes (in any mode)\n    if char_format.objectType() == CHECKBOX_FORMAT_TYPE:\n        self.viewport().setCursor(Qt.CursorShape.ArrowCursor)\n        super().mouseMoveEvent(event)\n        return\n\n    # Show hand cursor for links (only in read-only mode)\n    if self.isReadOnly():\n        if char_format.isAnchor() and char_format.anchorHref():\n            self.viewport().setCursor(Qt.CursorShape.PointingHandCursor)\n        else:\n            self.viewport().setCursor(Qt.CursorShape.ArrowCursor)\n    else:\n        self.viewport().setCursor(Qt.CursorShape.IBeamCursor)\n\n    super().mouseMoveEvent(event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/comment.html#client.ayon_core.ui.components.comment.AYCommentField.mousePressEvent","title":"<code>mousePressEvent(event)</code>","text":"<p>Handle mouse press events for checkboxes and links.</p> <p>Checkboxes can be toggled even in read-only mode. Links are opened only in read-only mode.</p> Source code in <code>client/ayon_core/ui/components/comment.py</code> <pre><code>def mousePressEvent(self, event) -&gt; None:\n    \"\"\"Handle mouse press events for checkboxes and links.\n\n    Checkboxes can be toggled even in read-only mode.\n    Links are opened only in read-only mode.\n    \"\"\"\n    # Get the character at the click position\n    cursor = self.cursorForPosition(event.pos())\n    char_format = cursor.charFormat()\n\n    # Check if clicked on a checkbox (works in both modes)\n    is_cb, cb_pos = self._is_checkbox_at_cursor(event.pos())\n    if is_cb and cb_pos is not None:\n        assert self._checkbox_handler is not None  # implied by is_cb==True\n        checkbox_idx = self._checkbox_handler.get_checkbox_at_position(\n            cb_pos\n        )\n        if checkbox_idx is not None:\n            self._checkbox_handler.toggle_checkbox(checkbox_idx)\n            self.viewport().update()\n            event.accept()\n            return\n\n    # Handle link clicks only in read-only mode (display comments)\n    if self.isReadOnly():\n        # Check if the clicked text is a link (has anchor href)\n        if char_format.isAnchor() and char_format.anchorHref():\n            url = char_format.anchorHref()\n            webbrowser.open(url)\n            event.accept()\n            return\n\n    super().mousePressEvent(event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/comment.html#client.ayon_core.ui.components.comment.AYCommentField.resizeEvent","title":"<code>resizeEvent(event)</code>","text":"<p>Recalculate height when width changes (affects text wrapping).</p> Source code in <code>client/ayon_core/ui/components/comment.py</code> <pre><code>def resizeEvent(self, event) -&gt; None:\n    \"\"\"Recalculate height when width changes (affects text wrapping).\"\"\"\n    super().resizeEvent(event)\n    if self._read_only:\n        self._adjust_height_to_content()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/comment.html#client.ayon_core.ui.components.comment.AYCommentField.set_markdown","title":"<code>set_markdown(md)</code>","text":"<p>Set markdown content with checkbox and web markdown support.</p> <p>Supports: - GitHub-flavored markdown checkboxes (- [ ] and - [x]) - Web markdown syntax (text\\n----, bold, italic, link) - Standard QTextDocument markdown</p> <p>Parameters:</p> Name Type Description Default <code>md</code> <code>str</code> <p>Markdown text to display</p> required Source code in <code>client/ayon_core/ui/components/comment.py</code> <pre><code>def set_markdown(self, md: str) -&gt; None:\n    \"\"\"Set markdown content with checkbox and web markdown support.\n\n    Supports:\n    - GitHub-flavored markdown checkboxes (- [ ] and - [x])\n    - Web markdown syntax (text\\\\n----, **bold**, _italic_, [link](url))\n    - Standard QTextDocument markdown\n\n    Args:\n        md: Markdown text to display\n    \"\"\"\n    # Check for checkboxes first\n    if CheckboxHandler.contains_checkboxes(md):\n        self._setup_checkbox_handler()\n        # Handler is guaranteed to exist after _setup_checkbox_handler\n        assert self._checkbox_handler is not None\n        self._checkbox_handler.parse_and_render(md)\n        if self._read_only:\n            self._adjust_height_to_content()\n        return\n\n    display_md = self._inject_user_mention_display(md)\n    self.document().setMarkdown(display_md, MD_DIALECT)\n    apply_code_block_backgrounds(self)\n\n    if self._read_only:\n        self._adjust_height_to_content()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/comment.html#client.ayon_core.ui.components.comment.AYImageAttachment","title":"<code>AYImageAttachment</code>","text":"<p>               Bases: <code>QLabel</code></p> <p>Widget to display an image attachment with thumbnail and full-size preview.</p> <p>Supports gallery mode when multiple images are associated together. When gallery_images is set, clicking the thumbnail opens a GalleryDialog that allows navigating through all images.</p> <p>Attributes:</p> Name Type Description <code>gallery_images</code> <p>List of (image_path, filename) tuples for gallery mode.</p> <code>gallery_index</code> <p>Current image index within the gallery.</p> Source code in <code>client/ayon_core/ui/components/comment.py</code> <pre><code>class AYImageAttachment(QLabel):\n    \"\"\"Widget to display an image attachment with thumbnail and full-size\n    preview.\n\n    Supports gallery mode when multiple images are associated together.\n    When gallery_images is set, clicking the thumbnail opens a GalleryDialog\n    that allows navigating through all images.\n\n    Attributes:\n        gallery_images: List of (image_path, filename) tuples for gallery mode.\n        gallery_index: Current image index within the gallery.\n    \"\"\"\n\n    no_img = None\n    cacher_tmp_dir: Path | None = None\n\n    def __init__(\n        self,\n        parent: QWidget | None = None,\n        image_path: str | None = None,\n        thumb_path: str | None = None,\n        max_width: int = 100,\n        max_height: int = 47,\n        frame: int = 0,\n        gallery_images: list | None = None,\n        gallery_index: int = 0,\n    ):\n        super().__init__(parent)\n        self._image_path = image_path\n        self._thumb_path = thumb_path or image_path\n        if (\n            self._thumb_path is not None\n            and not Path(self._thumb_path).exists()\n        ):\n            self._thumb_path = None\n        self._max_width = max_width\n        self._max_height = max_height\n        self._frame = frame\n        self._gallery_images = gallery_images or []\n        self._gallery_index = gallery_index\n\n        self.setScaledContents(False)\n        self.setAlignment(\n            Qt.AlignmentFlag.AlignHCenter | Qt.AlignmentFlag.AlignTop\n        )\n        self.setCursor(Qt.CursorShape.PointingHandCursor)\n\n        # Set tooltip\n        self.setToolTip(\"Click to view full size\")\n\n        self._hovered = False\n        self._label_height = 16\n\n        self.setFixedSize(\n            self._max_width, self._max_height + self._label_height\n        )\n\n        # Load and display thumbnail\n        self._load_thumbnail()\n        self._draw_icon = get_icon(\"draw\", color=\"#eeeeee\")\n\n    @property\n    def image_path(self) -&gt; str:\n        \"\"\"Get the path to the full-size image.\"\"\"\n        return self._image_path\n\n    @property\n    def frame(self) -&gt; int:\n        \"\"\"Get the frame number associated with the image.\"\"\"\n        return self._frame\n\n    def _load_thumbnail(self):\n        \"\"\"Load and display the thumbnail image.\"\"\"\n        if AYImageAttachment.no_img is None:\n            AYImageAttachment.no_img = get_icon(\"panorama\", color=\"#666666\")\n\n        if not self._thumb_path or not Path(self._thumb_path).exists():\n            self.setPixmap(AYImageAttachment.no_img.pixmap(32, 32))\n            return\n\n        thumb_path = Path(self._thumb_path)\n        cache_key = f\"{thumb_path.name}_{self._max_width}_{self._max_height}\"\n\n        def _thumbnail_cacher() -&gt; Path:\n            \"\"\"Cache the scaled-down thumbnail image.\"\"\"\n            pixmap = QPixmap(self._thumb_path or \"\")\n            if pixmap.isNull():\n                raise ValueError(\n                    f\"Cannot load image from path: {self._thumb_path!r}\"\n                )\n\n            # Scale pixmap to fit within max dimensions while maintaining\n            # aspect ratio.\n            scaled_pixmap = pixmap.scaled(\n                self._max_width,\n                self._max_height,\n                Qt.AspectRatioMode.KeepAspectRatio,\n                Qt.TransformationMode.SmoothTransformation,\n            )\n            tmp_dir = AYImageAttachment.get_cacher_tmp_dir()\n            tmp_file_path = tmp_dir / f\"{cache_key}.thumb.png\"\n            scaled_pixmap.save(str(tmp_file_path), quality=75)\n            return tmp_file_path\n\n        ic = ImageCache.get_instance()\n        pxm = QPixmap(ic.get(cache_key, _thumbnail_cacher))\n        self.setPixmap(pxm)\n        self.setFixedSize(pxm.width(), pxm.height() + self._label_height)\n\n    def enterEvent(self, event):\n        \"\"\"Dim the image slightly when mouse enters.\"\"\"\n        self._hovered = True\n        super().enterEvent(event)\n\n    def leaveEvent(self, event):\n        \"\"\"Restore full opacity when mouse leaves.\"\"\"\n        self._hovered = False\n        super().leaveEvent(event)\n\n    def paintEvent(self, arg__1: QPaintEvent) -&gt; None:\n        \"\"\"Draw a semi-transparent overlay with a fullscreen icon when\n        hovered.\"\"\"\n        # draw image\n        super().paintEvent(arg__1)\n        # setup painter\n        painter = QPainter(self)\n        painter.setRenderHint(QPainter.RenderHint.Antialiasing)\n        painter.setPen(Qt.PenStyle.NoPen)\n\n        if self._hovered:\n            img_rect = self.rect().adjusted(0, 0, 0, -self._label_height)\n            painter.setBrush(QColor(0, 0, 0, 144))\n            painter.drawRect(img_rect)\n            icon = get_icon(\"open_in_full\", color=\"#eeeeee\")\n            painter.drawPixmap(\n                img_rect.center() - QPoint(12, 12), icon.pixmap(24, 24)\n            )\n\n        # draw label background\n        lrect = QRect(\n            0,\n            self.height() - self._label_height,\n            self.width(),\n            self._label_height,\n        )\n        painter.setBrush(QColor(\"#1c2026\"))\n        painter.drawRect(lrect)\n        # draw icon on the left side\n        painter.drawPixmap(\n            lrect.left() + 3, lrect.top() + 3, self._draw_icon.pixmap(10, 10)\n        )\n        # draw text on the right side\n        painter.setPen(QColor(\"#eeeeee\"))\n        font = painter.font()\n        font.setPointSizeF(10)\n        painter.setFont(font)\n        painter.drawText(\n            lrect.adjusted(16 + 4, 0, 0, 0),\n            Qt.AlignmentFlag.AlignLeft | Qt.AlignmentFlag.AlignVCenter,\n            str(self._frame if self._frame &gt; 0 else \"n/a\"),\n        )\n\n    def mousePressEvent(self, event):\n        \"\"\"Handle click to show full-size image.\"\"\"\n        if event.button() == Qt.MouseButton.LeftButton:\n            self._show_full_size()\n        super().mousePressEvent(event)\n\n    def _show_full_size(self):\n        \"\"\"Show full-size image in a dialog that respects aspect ratio.\n\n        Uses GalleryDialog for consistent UI regardless of whether there's\n        a single image or multiple images in the gallery.\n        \"\"\"\n\n        # Collect gallery images if not already done\n        if not self._gallery_images or any(\n            t[0] == \"\" for t in self._gallery_images\n        ):\n            self._gallery_images = self._image_collector()\n\n        # Build gallery images list - use gallery_images if set, otherwise\n        # just this image\n        if self._gallery_images:\n            images = self._gallery_images\n            current_index = self._gallery_index\n        else:\n            # Single image case - still use gallery dialog for consistency\n            if not self._image_path or not Path(self._image_path).exists():\n                QMessageBox.warning(\n                    self,\n                    \"Image Not Available\",\n                    \"The full-size image is not available.\",\n                )\n                return\n            images = [(self._image_path, f\"Frame {self._frame}\")]\n            current_index = 0\n\n        dialog = GalleryDialog(\n            images=images,\n            current_index=current_index,\n            parent=self,\n        )\n        dialog.exec()\n\n    def set_gallery_images(self, images: list, current_index: int = 0) -&gt; None:\n        \"\"\"Set the gallery images for navigation.\n\n        Args:\n            images: List of (image_path, filename) tuples.\n            current_index: Index of this image in the gallery.\n        \"\"\"\n        self._gallery_images = images\n        self._gallery_index = current_index\n\n    def _image_collector(self) -&gt; list[tuple[str, str]]:\n        \"\"\"Collect all images in the parent layout.\"\"\"\n        try:\n            parent_layout = self.parentWidget().layout()\n        except AttributeError:\n            logger.info(\n                \"Parent widget has no valid layout for image collector\"\n            )\n            return []  # invalid parent widget\n\n        assert isinstance(parent_layout, QLayout)\n        image_list = []\n        for i in range(parent_layout.count()):\n            try:\n                widget = parent_layout.itemAt(i).widget()\n            except AttributeError:\n                continue  # invalid layout item\n            if isinstance(widget, AYImageAttachment):\n                image_list.append((widget.image_path, f\"Frame {widget.frame}\"))\n\n        return image_list\n\n    @classmethod\n    def get_cacher_tmp_dir(cls) -&gt; Path:\n        if cls.cacher_tmp_dir is None:\n            cls.cacher_tmp_dir = Path(\n                tempfile.mkdtemp(\n                    prefix=\"ayon_review_desktop_thumbnail_cacher_\"\n                )\n            )\n        return cls.cacher_tmp_dir\n\n    @staticmethod\n    def cleanup_cacher_directory() -&gt; None:\n        if (\n            AYImageAttachment.cacher_tmp_dir\n            and AYImageAttachment.cacher_tmp_dir.exists()\n        ):\n            rmtree(AYImageAttachment.cacher_tmp_dir, ignore_errors=True)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/comment.html#client.ayon_core.ui.components.comment.AYImageAttachment.frame","title":"<code>frame</code>  <code>property</code>","text":"<p>Get the frame number associated with the image.</p>"},{"location":"autoapi/client/ayon_core/ui/components/comment.html#client.ayon_core.ui.components.comment.AYImageAttachment.image_path","title":"<code>image_path</code>  <code>property</code>","text":"<p>Get the path to the full-size image.</p>"},{"location":"autoapi/client/ayon_core/ui/components/comment.html#client.ayon_core.ui.components.comment.AYImageAttachment.enterEvent","title":"<code>enterEvent(event)</code>","text":"<p>Dim the image slightly when mouse enters.</p> Source code in <code>client/ayon_core/ui/components/comment.py</code> <pre><code>def enterEvent(self, event):\n    \"\"\"Dim the image slightly when mouse enters.\"\"\"\n    self._hovered = True\n    super().enterEvent(event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/comment.html#client.ayon_core.ui.components.comment.AYImageAttachment.leaveEvent","title":"<code>leaveEvent(event)</code>","text":"<p>Restore full opacity when mouse leaves.</p> Source code in <code>client/ayon_core/ui/components/comment.py</code> <pre><code>def leaveEvent(self, event):\n    \"\"\"Restore full opacity when mouse leaves.\"\"\"\n    self._hovered = False\n    super().leaveEvent(event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/comment.html#client.ayon_core.ui.components.comment.AYImageAttachment.mousePressEvent","title":"<code>mousePressEvent(event)</code>","text":"<p>Handle click to show full-size image.</p> Source code in <code>client/ayon_core/ui/components/comment.py</code> <pre><code>def mousePressEvent(self, event):\n    \"\"\"Handle click to show full-size image.\"\"\"\n    if event.button() == Qt.MouseButton.LeftButton:\n        self._show_full_size()\n    super().mousePressEvent(event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/comment.html#client.ayon_core.ui.components.comment.AYImageAttachment.paintEvent","title":"<code>paintEvent(arg__1)</code>","text":"<p>Draw a semi-transparent overlay with a fullscreen icon when hovered.</p> Source code in <code>client/ayon_core/ui/components/comment.py</code> <pre><code>def paintEvent(self, arg__1: QPaintEvent) -&gt; None:\n    \"\"\"Draw a semi-transparent overlay with a fullscreen icon when\n    hovered.\"\"\"\n    # draw image\n    super().paintEvent(arg__1)\n    # setup painter\n    painter = QPainter(self)\n    painter.setRenderHint(QPainter.RenderHint.Antialiasing)\n    painter.setPen(Qt.PenStyle.NoPen)\n\n    if self._hovered:\n        img_rect = self.rect().adjusted(0, 0, 0, -self._label_height)\n        painter.setBrush(QColor(0, 0, 0, 144))\n        painter.drawRect(img_rect)\n        icon = get_icon(\"open_in_full\", color=\"#eeeeee\")\n        painter.drawPixmap(\n            img_rect.center() - QPoint(12, 12), icon.pixmap(24, 24)\n        )\n\n    # draw label background\n    lrect = QRect(\n        0,\n        self.height() - self._label_height,\n        self.width(),\n        self._label_height,\n    )\n    painter.setBrush(QColor(\"#1c2026\"))\n    painter.drawRect(lrect)\n    # draw icon on the left side\n    painter.drawPixmap(\n        lrect.left() + 3, lrect.top() + 3, self._draw_icon.pixmap(10, 10)\n    )\n    # draw text on the right side\n    painter.setPen(QColor(\"#eeeeee\"))\n    font = painter.font()\n    font.setPointSizeF(10)\n    painter.setFont(font)\n    painter.drawText(\n        lrect.adjusted(16 + 4, 0, 0, 0),\n        Qt.AlignmentFlag.AlignLeft | Qt.AlignmentFlag.AlignVCenter,\n        str(self._frame if self._frame &gt; 0 else \"n/a\"),\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/comment.html#client.ayon_core.ui.components.comment.AYImageAttachment.set_gallery_images","title":"<code>set_gallery_images(images, current_index=0)</code>","text":"<p>Set the gallery images for navigation.</p> <p>Parameters:</p> Name Type Description Default <code>images</code> <code>list</code> <p>List of (image_path, filename) tuples.</p> required <code>current_index</code> <code>int</code> <p>Index of this image in the gallery.</p> <code>0</code> Source code in <code>client/ayon_core/ui/components/comment.py</code> <pre><code>def set_gallery_images(self, images: list, current_index: int = 0) -&gt; None:\n    \"\"\"Set the gallery images for navigation.\n\n    Args:\n        images: List of (image_path, filename) tuples.\n        current_index: Index of this image in the gallery.\n    \"\"\"\n    self._gallery_images = images\n    self._gallery_index = current_index\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/comment_completion.html","title":"comment_completion","text":""},{"location":"autoapi/client/ayon_core/ui/components/comment_completion.html#client.ayon_core.ui.components.comment_completion.MentionHighlighter","title":"<code>MentionHighlighter</code>","text":"<p>               Bases: <code>QSyntaxHighlighter</code></p> <p>Syntax highlighter for @mentions, raw URLs, and code spans.</p> <p>Operates on block-local plain text so positions are always correct regardless of any rich-text formatting already present in the document (bold, italic, headings, code spans, etc.).</p> <p>Patterns highlighted:</p> <ul> <li>Fenced code blocks (<code>\\`\\`\\` ... \\`\\`\\</code>) spanning multiple   lines - black background, white monospace text.  Block state <code>1</code>   tracks whether the current block is inside a fence.</li> <li>Qt-rendered code blocks (from <code>setMarkdown()</code>) \u2014 detected via   <code>nonBreakableLines</code> on the block format.</li> <li>Qt-rendered inline code spans (from <code>setMarkdown()</code>) \u2014 detected via   <code>fontFixedPitch</code> on individual text fragments.</li> <li>Inline code spans (<code>\\`code\\`</code>) in raw (un-rendered) text \u2014 same   style, detected by backtick regex.</li> <li><code>@@@word</code> \u2014 task mention</li> <li><code>@@word</code>  \u2014 version mention</li> <li><code>@word</code>   \u2014 user mention (only the first word if the full name is not   in the known user list; both words when it is)</li> <li><code>https?://\u2026</code> \u2014 raw URL</li> </ul> <p>Parameters:</p> Name Type Description Default <code>document</code> <p>The QTextDocument to attach to.</p> required <code>user_list</code> <code>list</code> <p>Live list of :class:<code>~..data_models.User</code> objects used to decide whether a two-word mention should be highlighted in full.</p> required Source code in <code>client/ayon_core/ui/components/comment_completion.py</code> <pre><code>class MentionHighlighter(QSyntaxHighlighter):\n    \"\"\"Syntax highlighter for @mentions, raw URLs, and code spans.\n\n    Operates on block-local plain text so positions are always correct\n    regardless of any rich-text formatting already present in the document\n    (bold, italic, headings, code spans, etc.).\n\n    Patterns highlighted:\n\n    - Fenced code blocks (```\\\\`\\\\`\\\\` ... \\\\`\\\\`\\\\```) spanning multiple\n      lines - black background, white monospace text.  Block state ``1``\n      tracks whether the current block is inside a fence.\n    - Qt-rendered code blocks (from ``setMarkdown()``) \u2014 detected via\n      ``nonBreakableLines`` on the block format.\n    - Qt-rendered inline code spans (from ``setMarkdown()``) \u2014 detected via\n      ``fontFixedPitch`` on individual text fragments.\n    - Inline code spans (`` \\\\`code\\\\` ``) in raw (un-rendered) text \u2014 same\n      style, detected by backtick regex.\n    - ``@@@word`` \u2014 task mention\n    - ``@@word``  \u2014 version mention\n    - ``@word``   \u2014 user mention (only the first word if the full name is not\n      in the known user list; both words when it is)\n    - ``https?://\u2026`` \u2014 raw URL\n\n    Args:\n        document: The QTextDocument to attach to.\n        user_list: Live list of :class:`~..data_models.User` objects used to\n            decide whether a two-word mention should be highlighted in full.\n    \"\"\"\n\n    # Compiled patterns \u2014 order matters: longer prefixes first so that\n    # ``@@@`` is matched before ``@@`` and ``@@`` before ``@``.\n    _P_TASK = re.compile(r\"@@@\\w+( \\w+)?\")\n    _P_VERSION = re.compile(r\"@@(?!@)\\w+( \\w+)?\")\n    _P_USER = re.compile(r\"@(?!@)\\w+( \\w+)?\")\n    _P_RAW_LINK = re.compile(r\"https?://\\S+\")\n    # Inline code: single backtick pair on the same line.\n    _P_CODE_INLINE = re.compile(r\"`[^`\\n]+`\")\n\n    def __init__(self, document, user_list: list) -&gt; None:\n        super().__init__(document)\n        self._user_list = user_list\n        pal = get_ayon_style().model.base_palette\n        self._mention_fmt = QTextCharFormat()\n        self._mention_fmt.setForeground(pal.link())\n        self._code_fmt = None\n        self._plain_fmt = self._get_plain_char_format()\n\n    def update_user_list(self, user_list: list) -&gt; None:\n        \"\"\"Replace the user list and trigger a full rehighlight.\n\n        Args:\n            user_list: Updated list of User objects.\n        \"\"\"\n        self._user_list = user_list\n        self.rehighlight()\n\n    def highlightBlock(self, text: str) -&gt; None:\n        \"\"\"Apply code, mention, and URL highlighting to a single block.\n\n        Two detection strategies are combined so that code is styled in both\n        *edit mode* (raw markdown typed by the user) and *display mode*\n        (rich text rendered via ``document().setMarkdown()``):\n\n        **Edit mode \u2014 raw fence markers:**\n        Uses block state ``1`` to track multi-line fenced code blocks. A line\n        starting with *```* opens or closes a fence; every line inside the\n        fence is styled with :attr:`_code_fmt`.  A line that both opens and\n        closes a fence on the same line (e.g. ``\\\\`\\\\`\\\\`code\\\\`\\\\`\\\\```) is\n        treated as a single-line code block with no state change.\n\n        **Display mode \u2014 Qt-rendered char formats:**\n        After ``setMarkdown()`` Qt strips the fence markers and stores rich\n        text character formats.  Fenced code blocks have\n        ``nonBreakableLines=True`` set on the block format (``fontFixedPitch``\n        stays ``False`` on the block-level char format).  Inline code spans\n        set ``fontFixedPitch=True`` on individual text *fragments* within a\n        paragraph.  Both are detected here and styled with :attr:`_code_fmt`.\n\n        Inline code spans from raw backtick syntax (`` \\\\`code\\\\` ``) are also\n        detected via :attr:`_P_CODE_INLINE` for live-typed backtick spans.\n\n        Code formatting is applied *after* mention/URL patterns so that it\n        takes precedence over any mention highlight inside a code span.\n\n        Called automatically by Qt whenever the block changes.\n\n        Args:\n            text: Plain text content of the current block.\n        \"\"\"\n        block = self.currentBlock()\n        in_fence = self.previousBlockState() == 1\n        code_fmt = self._get_code_char_format()\n\n        # \u2500\u2500 Edit mode: raw fence markers \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n        if in_fence:\n            # The entire line belongs to the open fenced block.\n            self.setFormat(0, len(text), code_fmt)\n            # A line starting with ``` closes the fence.\n            if text.startswith(\"```\"):\n                self.setCurrentBlockState(0)\n            else:\n                self.setCurrentBlockState(1)\n            return\n\n        if text.startswith(\"```\"):\n            self.setFormat(0, len(text), code_fmt)\n            rest = text[3:]\n            # Closing ``` on the same line \u2192 single-line block, no state.\n            if \"```\" in rest:\n                self.setCurrentBlockState(0)\n            else:\n                self.setCurrentBlockState(1)\n            return\n\n        self.setCurrentBlockState(0)\n\n        # \u2500\u2500 Display mode: Qt-rendered whole-block code \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n        # After setMarkdown(), Qt marks fenced code block lines with\n        # nonBreakableLines=True on the block format.  The char format\n        # carries fontFamilies=['monospace'] but fontFixedPitch stays False.\n        # Style the whole line and skip mention/URL patterns \u2014 they don't\n        # belong inside code.\n        if block.blockFormat().nonBreakableLines():\n            self.setFormat(0, len(text), code_fmt)\n            return\n\n        # \u2500\u2500 Mentions and URLs (applied before inline code) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n        users = {u.full_name for u in self._user_list}\n        mention_ranges: list[tuple[int, int]] = []\n\n        def _mark_mention(start: int, length: int) -&gt; None:\n            \"\"\"Mark mention highlight for certain pattern matches.\n\n            Args:\n                start: Start index of the mention in the block.\n                length: Length of the mention text.\n            \"\"\"\n            if length &lt;= 0:\n                return\n            self.setFormat(start, length, self._mention_fmt)\n            mention_ranges.append((start, start + length))\n\n        # Task mentions (@@@)\n        for m in self._P_TASK.finditer(text):\n            _mark_mention(m.start(), m.end() - m.start())\n\n        # Version mentions (@@)\n        for m in self._P_VERSION.finditer(text):\n            _mark_mention(m.start(), m.end() - m.start())\n\n        # User mentions (@) \u2014 highlight only the first word unless the full\n        # two-word name is in the known user list.\n        for m in self._P_USER.finditer(text):\n            full_match = m.group(0)\n            mention_name = full_match[1:]  # strip leading @\n            if mention_name in users:\n                length = len(full_match)\n            else:\n                # Highlight only up to the first word (no trailing space+word)\n                length = len(full_match.split()[0])\n            _mark_mention(m.start(), length)\n\n        # Raw URLs\n        for m in self._P_RAW_LINK.finditer(text):\n            _mark_mention(m.start(), m.end() - m.start())\n\n        # Apply plain style only outside mention/url ranges so mention\n        # formatting stays intact while the rest of the line is normalized.\n        if not mention_ranges:\n            self.setFormat(0, len(text), self._plain_fmt)\n        else:\n            mention_ranges.sort()\n            merged: list[tuple[int, int]] = []\n            for start, end in mention_ranges:\n                if not merged or start &gt; merged[-1][1]:\n                    merged.append((start, end))\n                else:\n                    prev_start, prev_end = merged[-1]\n                    merged[-1] = (prev_start, max(prev_end, end))\n\n            cursor = 0\n            for start, end in merged:\n                if start &gt; cursor:\n                    self.setFormat(cursor, start - cursor, self._plain_fmt)\n                cursor = max(cursor, end)\n\n            if cursor &lt; len(text):\n                self.setFormat(cursor, len(text) - cursor, self._plain_fmt)\n\n        # \u2500\u2500 Inline code (applied last, overrides mention formatting) \u2500\u2500\u2500\u2500\u2500\n\n        # Raw backtick syntax `code` \u2014 detected in plain text for live\n        # editing where backtick characters are still present:\n        for m in self._P_CODE_INLINE.finditer(text):\n            self.setFormat(m.start(), m.end() - m.start(), code_fmt)\n        # Qt-rendered inline code spans \u2014 after setMarkdown() the backticks\n        # are consumed and individual fragments carry fontFixedPitch=True:\n        it = block.begin()\n        while not it.atEnd():\n            fragment = it.fragment()\n            if fragment.isValid() and fragment.charFormat().fontFixedPitch():\n                frag_start = fragment.position() - block.position()\n                self.setFormat(frag_start, fragment.length(), code_fmt)\n            it += 1\n\n    def _get_code_char_format(self) -&gt; QTextCharFormat:\n        \"\"\"Return a QTextCharFormat for inline code spans.\"\"\"\n        if self._code_fmt is not None:\n            return self._code_fmt\n\n        fmt = QTextCharFormat()\n        fmt.setFontFixedPitch(True)\n        fmt.setFontFamilies([\"Noto Sans Mono\"])\n        fmt.setBackground(CODE_BG)\n        fmt.setForeground(CODE_FG)\n        self._code_fmt = fmt\n        return fmt\n\n    def _get_plain_char_format(self) -&gt; QTextCharFormat:\n        fmt = QTextCharFormat()\n        pal = get_ayon_style().model.base_palette\n        fmt.setForeground(pal.text())\n        return fmt\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/comment_completion.html#client.ayon_core.ui.components.comment_completion.MentionHighlighter.highlightBlock","title":"<code>highlightBlock(text)</code>","text":"<p>Apply code, mention, and URL highlighting to a single block.</p> <p>Two detection strategies are combined so that code is styled in both edit mode (raw markdown typed by the user) and display mode (rich text rendered via <code>document().setMarkdown()</code>):</p> <p>Edit mode \u2014 raw fence markers: Uses block state <code>1</code> to track multi-line fenced code blocks. A line starting with *<code>* opens or closes a fence; every line inside the fence is styled with :attr:`_code_fmt`.  A line that both opens and closes a fence on the same line (e.g. ``\\`\\`\\`code\\`\\`\\</code>) is treated as a single-line code block with no state change.</p> <p>Display mode \u2014 Qt-rendered char formats: After <code>setMarkdown()</code> Qt strips the fence markers and stores rich text character formats.  Fenced code blocks have <code>nonBreakableLines=True</code> set on the block format (<code>fontFixedPitch</code> stays <code>False</code> on the block-level char format).  Inline code spans set <code>fontFixedPitch=True</code> on individual text fragments within a paragraph.  Both are detected here and styled with :attr:<code>_code_fmt</code>.</p> <p>Inline code spans from raw backtick syntax (<code>\\`code\\`</code>) are also detected via :attr:<code>_P_CODE_INLINE</code> for live-typed backtick spans.</p> <p>Code formatting is applied after mention/URL patterns so that it takes precedence over any mention highlight inside a code span.</p> <p>Called automatically by Qt whenever the block changes.</p> <p>Parameters:</p> Name Type Description Default <code>text</code> <code>str</code> <p>Plain text content of the current block.</p> required Source code in <code>client/ayon_core/ui/components/comment_completion.py</code> <pre><code>def highlightBlock(self, text: str) -&gt; None:\n    \"\"\"Apply code, mention, and URL highlighting to a single block.\n\n    Two detection strategies are combined so that code is styled in both\n    *edit mode* (raw markdown typed by the user) and *display mode*\n    (rich text rendered via ``document().setMarkdown()``):\n\n    **Edit mode \u2014 raw fence markers:**\n    Uses block state ``1`` to track multi-line fenced code blocks. A line\n    starting with *```* opens or closes a fence; every line inside the\n    fence is styled with :attr:`_code_fmt`.  A line that both opens and\n    closes a fence on the same line (e.g. ``\\\\`\\\\`\\\\`code\\\\`\\\\`\\\\```) is\n    treated as a single-line code block with no state change.\n\n    **Display mode \u2014 Qt-rendered char formats:**\n    After ``setMarkdown()`` Qt strips the fence markers and stores rich\n    text character formats.  Fenced code blocks have\n    ``nonBreakableLines=True`` set on the block format (``fontFixedPitch``\n    stays ``False`` on the block-level char format).  Inline code spans\n    set ``fontFixedPitch=True`` on individual text *fragments* within a\n    paragraph.  Both are detected here and styled with :attr:`_code_fmt`.\n\n    Inline code spans from raw backtick syntax (`` \\\\`code\\\\` ``) are also\n    detected via :attr:`_P_CODE_INLINE` for live-typed backtick spans.\n\n    Code formatting is applied *after* mention/URL patterns so that it\n    takes precedence over any mention highlight inside a code span.\n\n    Called automatically by Qt whenever the block changes.\n\n    Args:\n        text: Plain text content of the current block.\n    \"\"\"\n    block = self.currentBlock()\n    in_fence = self.previousBlockState() == 1\n    code_fmt = self._get_code_char_format()\n\n    # \u2500\u2500 Edit mode: raw fence markers \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n    if in_fence:\n        # The entire line belongs to the open fenced block.\n        self.setFormat(0, len(text), code_fmt)\n        # A line starting with ``` closes the fence.\n        if text.startswith(\"```\"):\n            self.setCurrentBlockState(0)\n        else:\n            self.setCurrentBlockState(1)\n        return\n\n    if text.startswith(\"```\"):\n        self.setFormat(0, len(text), code_fmt)\n        rest = text[3:]\n        # Closing ``` on the same line \u2192 single-line block, no state.\n        if \"```\" in rest:\n            self.setCurrentBlockState(0)\n        else:\n            self.setCurrentBlockState(1)\n        return\n\n    self.setCurrentBlockState(0)\n\n    # \u2500\u2500 Display mode: Qt-rendered whole-block code \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n    # After setMarkdown(), Qt marks fenced code block lines with\n    # nonBreakableLines=True on the block format.  The char format\n    # carries fontFamilies=['monospace'] but fontFixedPitch stays False.\n    # Style the whole line and skip mention/URL patterns \u2014 they don't\n    # belong inside code.\n    if block.blockFormat().nonBreakableLines():\n        self.setFormat(0, len(text), code_fmt)\n        return\n\n    # \u2500\u2500 Mentions and URLs (applied before inline code) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n    users = {u.full_name for u in self._user_list}\n    mention_ranges: list[tuple[int, int]] = []\n\n    def _mark_mention(start: int, length: int) -&gt; None:\n        \"\"\"Mark mention highlight for certain pattern matches.\n\n        Args:\n            start: Start index of the mention in the block.\n            length: Length of the mention text.\n        \"\"\"\n        if length &lt;= 0:\n            return\n        self.setFormat(start, length, self._mention_fmt)\n        mention_ranges.append((start, start + length))\n\n    # Task mentions (@@@)\n    for m in self._P_TASK.finditer(text):\n        _mark_mention(m.start(), m.end() - m.start())\n\n    # Version mentions (@@)\n    for m in self._P_VERSION.finditer(text):\n        _mark_mention(m.start(), m.end() - m.start())\n\n    # User mentions (@) \u2014 highlight only the first word unless the full\n    # two-word name is in the known user list.\n    for m in self._P_USER.finditer(text):\n        full_match = m.group(0)\n        mention_name = full_match[1:]  # strip leading @\n        if mention_name in users:\n            length = len(full_match)\n        else:\n            # Highlight only up to the first word (no trailing space+word)\n            length = len(full_match.split()[0])\n        _mark_mention(m.start(), length)\n\n    # Raw URLs\n    for m in self._P_RAW_LINK.finditer(text):\n        _mark_mention(m.start(), m.end() - m.start())\n\n    # Apply plain style only outside mention/url ranges so mention\n    # formatting stays intact while the rest of the line is normalized.\n    if not mention_ranges:\n        self.setFormat(0, len(text), self._plain_fmt)\n    else:\n        mention_ranges.sort()\n        merged: list[tuple[int, int]] = []\n        for start, end in mention_ranges:\n            if not merged or start &gt; merged[-1][1]:\n                merged.append((start, end))\n            else:\n                prev_start, prev_end = merged[-1]\n                merged[-1] = (prev_start, max(prev_end, end))\n\n        cursor = 0\n        for start, end in merged:\n            if start &gt; cursor:\n                self.setFormat(cursor, start - cursor, self._plain_fmt)\n            cursor = max(cursor, end)\n\n        if cursor &lt; len(text):\n            self.setFormat(cursor, len(text) - cursor, self._plain_fmt)\n\n    # \u2500\u2500 Inline code (applied last, overrides mention formatting) \u2500\u2500\u2500\u2500\u2500\n\n    # Raw backtick syntax `code` \u2014 detected in plain text for live\n    # editing where backtick characters are still present:\n    for m in self._P_CODE_INLINE.finditer(text):\n        self.setFormat(m.start(), m.end() - m.start(), code_fmt)\n    # Qt-rendered inline code spans \u2014 after setMarkdown() the backticks\n    # are consumed and individual fragments carry fontFixedPitch=True:\n    it = block.begin()\n    while not it.atEnd():\n        fragment = it.fragment()\n        if fragment.isValid() and fragment.charFormat().fontFixedPitch():\n            frag_start = fragment.position() - block.position()\n            self.setFormat(frag_start, fragment.length(), code_fmt)\n        it += 1\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/comment_completion.html#client.ayon_core.ui.components.comment_completion.MentionHighlighter.update_user_list","title":"<code>update_user_list(user_list)</code>","text":"<p>Replace the user list and trigger a full rehighlight.</p> <p>Parameters:</p> Name Type Description Default <code>user_list</code> <code>list</code> <p>Updated list of User objects.</p> required Source code in <code>client/ayon_core/ui/components/comment_completion.py</code> <pre><code>def update_user_list(self, user_list: list) -&gt; None:\n    \"\"\"Replace the user list and trigger a full rehighlight.\n\n    Args:\n        user_list: Updated list of User objects.\n    \"\"\"\n    self._user_list = user_list\n    self.rehighlight()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/comment_completion.html#client.ayon_core.ui.components.comment_completion.UserCompleterDelegate","title":"<code>UserCompleterDelegate</code>","text":"<p>               Bases: <code>QStyledItemDelegate</code></p> <p>Custom delegate to display user icon and full name in completer.</p> Source code in <code>client/ayon_core/ui/components/comment_completion.py</code> <pre><code>class UserCompleterDelegate(QStyledItemDelegate):\n    \"\"\"Custom delegate to display user icon and full name in completer.\"\"\"\n\n    def __init__(self, parent=None):\n        super().__init__(parent)\n        self.icon_size = 20\n        self._user_pixmap = {}\n\n    def paint(\n        self,\n        painter: QPainter,\n        option: QStyleOptionViewItem,\n        index,\n    ) -&gt; None:\n        \"\"\"Paint user icon and full name.\"\"\"\n        user: User = index.data(Qt.ItemDataRole.UserRole)\n        if not user:\n            super().paint(painter, option, index)\n            return\n\n        # Draw background\n        text_color = get_ayon_style().model.base_palette.color(\n                QPalette.ColorGroup.Active, QPalette.ColorRole.Text\n        )\n        if option.state &amp; QStyle.StateFlag.State_Selected:\n            palette = get_ayon_style().model.base_palette.color(\n                QPalette.ColorGroup.Active, QPalette.ColorRole.Light\n            )\n            painter.fillRect(option.rect, palette)\n            painter.setPen(text_color)\n        else:\n            palette = get_ayon_style().model.base_palette.color(\n                QPalette.ColorGroup.Active, QPalette.ColorRole.Midlight)\n            painter.fillRect(option.rect, palette)\n            painter.setPen(text_color)\n\n        # Draw user icon\n        try:\n            icon_pixmap = self._user_pixmap[user.name]\n        except KeyError:\n            user_image = AYUserImage(\n                src=user.avatar_url,\n                full_name=user.full_name,\n                size=self.icon_size,\n                outline=False,\n            )\n            icon_pixmap = user_image.pixmap()\n            self._user_pixmap[user.name] = icon_pixmap\n\n        icon_x = option.rect.x() + 4\n        icon_y = option.rect.y() + (option.rect.height() - self.icon_size) // 2\n        painter.drawPixmap(icon_x, icon_y, icon_pixmap)\n\n        # Draw full name\n        text_x = icon_x + self.icon_size + 8\n        text_rect = option.rect.adjusted(text_x, 0, 0, 0)\n        painter.drawText(\n            text_rect,\n            Qt.AlignmentFlag.AlignVCenter,\n            user.full_name,\n        )\n\n    def sizeHint(\n        self,\n        option: QStyleOptionViewItem,\n        index,\n    ) -&gt; QSize:\n        \"\"\"Return size hint for completer items.\"\"\"\n        return QSize(option.rect.width(), self.icon_size + 8)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/comment_completion.html#client.ayon_core.ui.components.comment_completion.UserCompleterDelegate.paint","title":"<code>paint(painter, option, index)</code>","text":"<p>Paint user icon and full name.</p> Source code in <code>client/ayon_core/ui/components/comment_completion.py</code> <pre><code>def paint(\n    self,\n    painter: QPainter,\n    option: QStyleOptionViewItem,\n    index,\n) -&gt; None:\n    \"\"\"Paint user icon and full name.\"\"\"\n    user: User = index.data(Qt.ItemDataRole.UserRole)\n    if not user:\n        super().paint(painter, option, index)\n        return\n\n    # Draw background\n    text_color = get_ayon_style().model.base_palette.color(\n            QPalette.ColorGroup.Active, QPalette.ColorRole.Text\n    )\n    if option.state &amp; QStyle.StateFlag.State_Selected:\n        palette = get_ayon_style().model.base_palette.color(\n            QPalette.ColorGroup.Active, QPalette.ColorRole.Light\n        )\n        painter.fillRect(option.rect, palette)\n        painter.setPen(text_color)\n    else:\n        palette = get_ayon_style().model.base_palette.color(\n            QPalette.ColorGroup.Active, QPalette.ColorRole.Midlight)\n        painter.fillRect(option.rect, palette)\n        painter.setPen(text_color)\n\n    # Draw user icon\n    try:\n        icon_pixmap = self._user_pixmap[user.name]\n    except KeyError:\n        user_image = AYUserImage(\n            src=user.avatar_url,\n            full_name=user.full_name,\n            size=self.icon_size,\n            outline=False,\n        )\n        icon_pixmap = user_image.pixmap()\n        self._user_pixmap[user.name] = icon_pixmap\n\n    icon_x = option.rect.x() + 4\n    icon_y = option.rect.y() + (option.rect.height() - self.icon_size) // 2\n    painter.drawPixmap(icon_x, icon_y, icon_pixmap)\n\n    # Draw full name\n    text_x = icon_x + self.icon_size + 8\n    text_rect = option.rect.adjusted(text_x, 0, 0, 0)\n    painter.drawText(\n        text_rect,\n        Qt.AlignmentFlag.AlignVCenter,\n        user.full_name,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/comment_completion.html#client.ayon_core.ui.components.comment_completion.UserCompleterDelegate.sizeHint","title":"<code>sizeHint(option, index)</code>","text":"<p>Return size hint for completer items.</p> Source code in <code>client/ayon_core/ui/components/comment_completion.py</code> <pre><code>def sizeHint(\n    self,\n    option: QStyleOptionViewItem,\n    index,\n) -&gt; QSize:\n    \"\"\"Return size hint for completer items.\"\"\"\n    return QSize(option.rect.width(), self.icon_size + 8)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/comment_completion.html#client.ayon_core.ui.components.comment_completion.UserCompleterModel","title":"<code>UserCompleterModel</code>","text":"<p>               Bases: <code>QStandardItemModel</code></p> <p>Model for user completer.</p> Source code in <code>client/ayon_core/ui/components/comment_completion.py</code> <pre><code>class UserCompleterModel(QStandardItemModel):\n    \"\"\"Model for user completer.\"\"\"\n\n    def __init__(self, users: list[User], parent=None):\n        super().__init__(parent)\n        self.users = users\n        self._populate()\n\n    def _populate(self) -&gt; None:\n        \"\"\"Populate model with users.\"\"\"\n        self.clear()\n        for user in self.users:\n            item = QStandardItem(user.full_name)\n            item.setData(user, Qt.ItemDataRole.UserRole)\n            self.appendRow(item)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/comment_completion.html#client.ayon_core.ui.components.comment_completion.apply_code_block_backgrounds","title":"<code>apply_code_block_backgrounds(text_edit)</code>","text":"<p>Apply a full-width background colour to every fenced code block.</p> <p><code>QSyntaxHighlighter.setFormat()</code> only paints behind individual text characters, so the end of a short line keeps the regular widget background.  Setting <code>QTextBlockFormat.background</code> instead causes Qt's own layout engine to paint the background across the entire width of the block before any characters are drawn.</p> <p>This function is intentionally separate from :class:<code>MentionHighlighter</code> because Qt's documentation forbids modifying the document from inside <code>highlightBlock()</code>.</p> <p>Two detection strategies are combined so that code blocks are recognised in both scenarios:</p> <ul> <li>Display mode (after <code>document().setMarkdown()</code>): Qt marks   fenced code block lines with <code>nonBreakableLines=True</code> on the   block format.</li> <li>Edit mode (raw typing): Lines inside a <code>`\\</code>``\u2026`````` fence   are detected by tracking an open-fence flag while iterating from   the first block of the document.</li> </ul> <p>The function is guarded by the <code>_suppress_formatting</code> attribute on text_edit to prevent infinite recursion: writing block formats emits <code>contentsChanged</code>, which would otherwise re-enter this function.</p> <p>Parameters:</p> Name Type Description Default <code>text_edit</code> <code>QTextEdit</code> <p>The QTextEdit whose document should have fenced code block backgrounds applied.</p> required Source code in <code>client/ayon_core/ui/components/comment_completion.py</code> <pre><code>def apply_code_block_backgrounds(text_edit: QTextEdit) -&gt; None:\n    \"\"\"Apply a full-width background colour to every fenced code block.\n\n    ``QSyntaxHighlighter.setFormat()`` only paints behind individual\n    text characters, so the end of a short line keeps the regular widget\n    background.  Setting ``QTextBlockFormat.background`` instead causes\n    Qt's own layout engine to paint the background across the *entire*\n    width of the block before any characters are drawn.\n\n    This function is intentionally separate from\n    :class:`MentionHighlighter` because Qt's documentation forbids\n    modifying the document from inside ``highlightBlock()``.\n\n    Two detection strategies are combined so that code blocks are\n    recognised in both scenarios:\n\n    - **Display mode** (after ``document().setMarkdown()``): Qt marks\n      fenced code block lines with ``nonBreakableLines=True`` on the\n      block format.\n    - **Edit mode** (raw typing): Lines inside a ``\\\\`\\\\`\\\\`\u2026\\\\`\\\\`\\\\```` fence\n      are detected by tracking an open-fence flag while iterating from\n      the first block of the document.\n\n    The function is guarded by the ``_suppress_formatting`` attribute on\n    *text_edit* to prevent infinite recursion: writing block formats\n    emits ``contentsChanged``, which would otherwise re-enter this\n    function.\n\n    Args:\n        text_edit: The QTextEdit whose document should have fenced code\n            block backgrounds applied.\n    \"\"\"\n    if getattr(text_edit, \"_suppress_formatting\", False):\n        return\n\n    doc = text_edit.document()\n    setattr(text_edit, \"_suppress_formatting\", True)\n\n    cursor = QTextCursor(doc)\n    cursor.beginEditBlock()\n\n    try:\n        in_fence = False\n        block = doc.begin()\n        while block.isValid():\n            text = block.text()\n            is_code = False\n\n            # Display mode: Qt-rendered fenced code block.\n            if block.blockFormat().nonBreakableLines():\n                is_code = True\n            else:\n                # Edit mode: raw ``` fence markers.\n                if in_fence:\n                    is_code = True\n                    if text.startswith(\"```\"):\n                        in_fence = False  # closing fence line \u2014 still code\n                elif text.startswith(\"```\"):\n                    is_code = True\n                    rest = text[3:]\n                    if \"```\" not in rest:\n                        in_fence = True  # opening fence\n                    # else: single-line ```\u2026``` \u2014 is_code=True, no state change\n\n            bg_brush = block.blockFormat().background()\n            has_code_bg = (\n                bg_brush.style() != Qt.BrushStyle.NoBrush\n                and bg_brush.color().rgb() == CODE_BG.rgb()\n            )\n\n            if is_code and not has_code_bg:\n                cursor.setPosition(block.position())\n                new_fmt = QTextBlockFormat()\n                new_fmt.setBackground(CODE_BG)\n                cursor.mergeBlockFormat(new_fmt)\n            elif not is_code and has_code_bg:\n                # Remove the previously applied code background.\n                cursor.setPosition(block.position())\n                restored = QTextBlockFormat(block.blockFormat())\n                restored.clearBackground()\n                cursor.setBlockFormat(restored)\n\n            block = block.next()\n    except Exception as e:\n        logging.info(f\"Error in apply_code_block_backgrounds: {e}\")\n    finally:\n        # Ensure we always end the edit block and reset the flag\n        cursor.endEditBlock()\n        setattr(text_edit, \"_suppress_formatting\", False)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/comment_completion.html#client.ayon_core.ui.components.comment_completion.format_comment_on_change","title":"<code>format_comment_on_change(text_edit)</code>","text":"<p>Ensure a :class:<code>MentionHighlighter</code> is installed on text_edit.</p> <p>Idempotent: safe to call on every <code>contentsChanged</code> signal.  The highlighter is created once and attached to the document.  Subsequent calls only call :meth:<code>MentionHighlighter.update_user_list</code> when the <code>_user_list</code> reference on text_edit has been replaced (e.g. after a server refresh), which avoids triggering an unnecessary <code>rehighlight</code> \u2014 and the infinite-recursion that would follow \u2014 on every keystroke.</p> <p>Parameters:</p> Name Type Description Default <code>text_edit</code> <code>QTextEdit</code> <p>The QTextEdit whose document should have mention highlighting applied.</p> required Source code in <code>client/ayon_core/ui/components/comment_completion.py</code> <pre><code>def format_comment_on_change(text_edit: QTextEdit) -&gt; None:\n    \"\"\"Ensure a :class:`MentionHighlighter` is installed on *text_edit*.\n\n    Idempotent: safe to call on every ``contentsChanged`` signal.  The\n    highlighter is created once and attached to the document.  Subsequent\n    calls only call :meth:`MentionHighlighter.update_user_list` when the\n    ``_user_list`` reference on *text_edit* has been replaced (e.g. after a\n    server refresh), which avoids triggering an unnecessary ``rehighlight``\n    \u2014 and the infinite-recursion that would follow \u2014 on every keystroke.\n\n    Args:\n        text_edit: The QTextEdit whose document should have mention\n            highlighting applied.\n    \"\"\"\n    highlighter: MentionHighlighter | None = getattr(\n        text_edit, \"_mention_highlighter\", None\n    )\n    user_list = getattr(text_edit, \"_user_list\", [])\n\n    if highlighter is None:\n        highlighter = MentionHighlighter(text_edit.document(), user_list)\n        text_edit._mention_highlighter = highlighter  # type: ignore[attr-defined]\n    elif highlighter._user_list is not user_list:\n        # The list object was replaced (e.g. after a user-list refresh).\n        # update_user_list() calls rehighlight() which is safe here because\n        # this branch is only reached when _suppress_formatting is False and\n        # the list identity has changed \u2014 not on every keystroke.\n        highlighter.update_user_list(user_list)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/comment_completion.html#client.ayon_core.ui.components.comment_completion.on_completer_activated","title":"<code>on_completer_activated(text_edit, text)</code>","text":"<p>Handle completer selection.</p> <p>Parameters:</p> Name Type Description Default <code>text_edit</code> <code>QTextEdit</code> <p>The QTextEdit widget with completer.</p> required <code>text</code> <code>str</code> <p>The selected completion text (user full name).</p> required Source code in <code>client/ayon_core/ui/components/comment_completion.py</code> <pre><code>def on_completer_activated(\n    text_edit: QTextEdit,\n    text: str,\n) -&gt; None:\n    \"\"\"Handle completer selection.\n\n    Args:\n        text_edit: The QTextEdit widget with completer.\n        text: The selected completion text (user full name).\n    \"\"\"\n    cursor = text_edit.textCursor()\n    block = cursor.block()\n    text_in_block = block.text()\n    pos_in_block = cursor.positionInBlock()\n\n    # Find the '@' position\n    at_pos = text_in_block.rfind(\"@\", 0, pos_in_block)\n    if at_pos == -1:\n        return\n\n    # Replace from '@' to cursor with '@' + full_name\n    cursor.setPosition(block.position() + at_pos)\n    cursor.setPosition(\n        block.position() + pos_in_block,\n        QTextCursor.MoveMode.KeepAnchor,\n    )\n    cursor.insertText(f\"@{text}\")\n    text_edit.setTextCursor(cursor)\n    popup = text_edit.completer.popup()\n    if popup:\n        popup.hide()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/comment_completion.html#client.ayon_core.ui.components.comment_completion.on_completer_key_press","title":"<code>on_completer_key_press(text_edit, event)</code>","text":"<p>Handle key press events for completer.</p> <p>Parameters:</p> Name Type Description Default <code>text_edit</code> <code>QTextEdit</code> <p>The QTextEdit widget with completer.</p> required <code>event</code> <p>The key press event.</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if event was handled, False otherwise.</p> Source code in <code>client/ayon_core/ui/components/comment_completion.py</code> <pre><code>def on_completer_key_press(\n    text_edit: QTextEdit,\n    event,\n) -&gt; bool:\n    \"\"\"Handle key press events for completer.\n\n    Args:\n        text_edit: The QTextEdit widget with completer.\n        event: The key press event.\n\n    Returns:\n        True if event was handled, False otherwise.\n    \"\"\"\n    if not hasattr(text_edit, \"completer\"):\n        return False\n\n    popup = text_edit.completer.popup()\n    if popup and popup.isVisible():\n        if event.key() in (Qt.Key.Key_Return, Qt.Key.Key_Enter):\n            # Get current completion from the selected index\n            current_index = popup.currentIndex()\n            if current_index.isValid():\n                completion = current_index.data()\n                if completion:\n                    text_edit.completer.activated.emit(completion)\n                    return True\n    return False\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/comment_completion.html#client.ayon_core.ui.components.comment_completion.on_completer_text_changed","title":"<code>on_completer_text_changed(text_edit)</code>","text":"<p>Handle text changes to show/hide completer.</p> <p>Parameters:</p> Name Type Description Default <code>text_edit</code> <code>QTextEdit</code> <p>The QTextEdit widget with completer.</p> required Source code in <code>client/ayon_core/ui/components/comment_completion.py</code> <pre><code>def on_completer_text_changed(\n    text_edit: QTextEdit,\n) -&gt; None:\n    \"\"\"Handle text changes to show/hide completer.\n\n    Args:\n        text_edit: The QTextEdit widget with completer.\n    \"\"\"\n    if not hasattr(text_edit, \"completer\") or text_edit.isReadOnly():\n        return\n\n    cursor = text_edit.textCursor()\n    block = cursor.block()\n    text = block.text()\n    pos_in_block = cursor.positionInBlock()\n\n    # Find the last '@' before cursor\n    at_pos = text.rfind(\"@\", 0, pos_in_block)\n    if at_pos == -1:\n        popup = text_edit.completer.popup()\n        if popup:\n            popup.hide()\n        return\n\n    # Get text after '@'\n    prefix = text[at_pos + 1 : pos_in_block]\n\n    # Once whitespace appears after '@', the mention token has ended.\n    # Hide the popup so trailing words are not captured into the link label.\n    if any(char.isspace() for char in prefix):\n        popup = text_edit.completer.popup()\n        if popup:\n            popup.hide()\n        return\n\n    # Show completer if '@' is followed by nothing or non-space characters\n    if not prefix or (prefix and not prefix[0].isspace()):\n        text_edit.completer.setCompletionPrefix(prefix)\n        show_completer_popup(text_edit, at_pos)\n        # Auto-select if only one item\n        popup = text_edit.completer.popup()\n        if popup:\n            popup_model = popup.model()\n            row_count = popup_model.rowCount() if popup_model else 0\n            if row_count == 1:\n                popup.setCurrentIndex(popup_model.index(0, 0))\n    else:\n        popup = text_edit.completer.popup()\n        if popup:\n            popup.hide()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/comment_completion.html#client.ayon_core.ui.components.comment_completion.setup_user_completer","title":"<code>setup_user_completer(text_edit, on_completer_activated, on_text_changed)</code>","text":"<p>Setup user name completer for a QTextEdit widget.</p> <p>Parameters:</p> Name Type Description Default <code>text_edit</code> <code>QTextEdit</code> <p>The QTextEdit widget to attach completer to.</p> required <code>on_completer_activated</code> <p>Callback for completer activation.</p> required <code>on_text_changed</code> <p>Callback for text changes.</p> required Source code in <code>client/ayon_core/ui/components/comment_completion.py</code> <pre><code>def setup_user_completer(\n    text_edit: QTextEdit,\n    on_completer_activated,\n    on_text_changed,\n) -&gt; None:\n    \"\"\"Setup user name completer for a QTextEdit widget.\n\n    Args:\n        text_edit: The QTextEdit widget to attach completer to.\n        on_completer_activated: Callback for completer activation.\n        on_text_changed: Callback for text changes.\n    \"\"\"\n    users = getattr(text_edit, \"_user_list\")\n    if not users:\n        users = [\n            User(\n                name=\"not available\",\n                short_name=\"not available\",\n                full_name=\"not available\",\n                email=\"\",\n                avatar_url=\"\",\n            )\n        ]\n    model = UserCompleterModel(users, text_edit)\n    text_edit.completer = QCompleter(model, text_edit)\n    text_edit.completer.setCompletionMode(\n        QCompleter.CompletionMode.PopupCompletion\n    )\n    text_edit.completer.setFilterMode(Qt.MatchFlag.MatchContains)\n    text_edit.completer.setMaxVisibleItems(4)\n    text_edit.completer.setCaseSensitivity(Qt.CaseSensitivity.CaseInsensitive)\n    text_edit.completer.setWidget(text_edit)\n\n    # Set custom delegate\n    popup = text_edit.completer.popup()\n    if popup:\n        delegate = UserCompleterDelegate(popup)\n        popup.setItemDelegate(delegate)\n        popup.setWindowFlag(Qt.WindowType.NoDropShadowWindowHint, True)\n\n    # Connect completer signals\n    text_edit.completer.activated.connect(on_completer_activated)\n    text_edit.textChanged.connect(on_text_changed)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/comment_completion.html#client.ayon_core.ui.components.comment_completion.show_completer_popup","title":"<code>show_completer_popup(text_edit, at_pos)</code>","text":"<p>Show completer popup above the QTextEdit.</p> <p>Parameters:</p> Name Type Description Default <code>text_edit</code> <code>QTextEdit</code> <p>The QTextEdit widget with completer.</p> required <code>at_pos</code> <code>int</code> <p>Position of '@' character in the block.</p> required Source code in <code>client/ayon_core/ui/components/comment_completion.py</code> <pre><code>def show_completer_popup(text_edit: QTextEdit, at_pos: int) -&gt; None:\n    \"\"\"Show completer popup above the QTextEdit.\n\n    Args:\n        text_edit: The QTextEdit widget with completer.\n        at_pos: Position of '@' character in the block.\n    \"\"\"\n    popup = text_edit.completer.popup()\n    if not popup:\n        return\n\n    # Get editor dimensions\n    editor_rect = text_edit.rect()\n    editor_width = editor_rect.width()\n\n    # Show popup to get its height\n    popup.show()\n\n    # Calculate height based on max visible items (4)\n    max_visible = text_edit.completer.maxVisibleItems()\n    item_height = popup.sizeHintForRow(0)\n    popup_height = item_height * max_visible\n\n    # Position popup above the QTextEdit with same width as editor\n    global_pos = text_edit.mapToGlobal(editor_rect.topLeft())\n    popup_x = global_pos.x()\n    popup_y = global_pos.y() - popup_height\n\n    popup.setGeometry(popup_x, popup_y, editor_width, popup_height)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/comment_completion.html#client.ayon_core.ui.components.comment_completion.strip_user_mention_display","title":"<code>strip_user_mention_display(md, user_list)</code>","text":"<p>Convert display @mentions back to storage links.</p> <p>Existing links like label are preserved unchanged. Plain @mentions are replaced with name if the user exists.</p> <p>Parameters:</p> Name Type Description Default <code>md</code> <code>str</code> <p>Markdown text.</p> required <code>user_list</code> <code>list[User]</code> <p>Available users for mention lookup.</p> required <p>Returns:</p> Type Description <code>str</code> <p>Markdown text with @mentions replaced by links e.g. name.</p> Source code in <code>client/ayon_core/ui/components/comment_completion.py</code> <pre><code>def strip_user_mention_display(md: str, user_list: list[User]) -&gt; str:\n    \"\"\"Convert display @mentions back to storage links.\n\n    Existing links like [label](user:id) are preserved unchanged.\n    Plain @mentions are replaced with [name](user:name) if the user exists.\n\n    Args:\n        md: Markdown text.\n        user_list: Available users for mention lookup.\n\n    Returns:\n        Markdown text with @mentions replaced by links e.g. [name](user:name).\n    \"\"\"\n    if \"@\" not in md:\n        return md\n\n    for user in sorted(\n        user_list,\n        key=lambda u: len(u.full_name),\n        reverse=True\n    ):\n        mention = f\"@{user.full_name}\"\n        if mention not in md:\n            continue\n        md = md.replace(mention, f\"[{user.full_name}](user:{user.name})\")\n    return md\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/container.html","title":"container","text":""},{"location":"autoapi/client/ayon_core/ui/components/dropdown.html","title":"dropdown","text":"<p>Shared base class for floating dropdown popup widgets.</p>"},{"location":"autoapi/client/ayon_core/ui/components/dropdown.html#client.ayon_core.ui.components.dropdown.AYDropdownPopup","title":"<code>AYDropdownPopup</code>","text":"<p>               Bases: <code>AYFrame</code></p> <p>Base class for floating dropdown popups.</p> <p>Provides: - Popup window flags (frameless, no drop shadow) - <code>show_below(widget)</code> positioning with screen-edge awareness - Escape key to close - <code>popup_closed</code> signal on close</p> <p>Attributes:</p> Name Type Description <code>Variants</code> <p>Alias for <code>QFrameVariants</code> for consistent usage.</p> <code>popup_closed</code> <p>Emitted when the popup is closed or hidden.</p> Source code in <code>client/ayon_core/ui/components/dropdown.py</code> <pre><code>class AYDropdownPopup(AYFrame):\n    \"\"\"Base class for floating dropdown popups.\n\n    Provides:\n    - Popup window flags (frameless, no drop shadow)\n    - ``show_below(widget)`` positioning with screen-edge awareness\n    - Escape key to close\n    - ``popup_closed`` signal on close\n\n    Attributes:\n        Variants: Alias for ``QFrameVariants`` for consistent usage.\n        popup_closed: Emitted when the popup is closed or hidden.\n    \"\"\"\n\n    Variants = QFrameVariants\n\n    popup_closed = Signal()\n\n    def __init__(\n        self,\n        parent: QtWidgets.QWidget | None = None,\n        variant: Variants = AYFrame.Variants.Low,\n        translucent_bg: bool = True,\n    ) -&gt; None:\n        \"\"\"Initialize the dropdown popup.\n\n        Sets popup window flags and configures translucent background.\n\n        Args:\n            parent: Optional parent widget (used for style inheritance).\n            variant: Frame style variant.\n            translucent_bg: Whether to enable a translucent background.\n                Set to ``False`` when the popup must be fully opaque.\n        \"\"\"\n        super().__init__(parent, variant=variant)\n        self.setStyle(get_ayon_style())\n        self.setWindowFlags(\n            Qt.WindowType.Popup\n            | Qt.WindowType.FramelessWindowHint\n            | Qt.WindowType.NoDropShadowWindowHint\n        )\n        self.setAttribute(Qt.WidgetAttribute.WA_WindowPropagation)\n        self.setAttribute(\n            Qt.WidgetAttribute.WA_TranslucentBackground, translucent_bg\n        )\n\n    def show_below(\n        self,\n        widget: QtWidgets.QWidget,\n        y_offset: int = 2,\n    ) -&gt; None:\n        \"\"\"Position and show the popup below (or above) the widget.\n\n        The popup is left-aligned with the widget's left edge.  If there\n        is not enough horizontal space on screen the popup is shifted left.\n        If there is not enough vertical space below, the popup is shown\n        above the widget instead.\n\n        Args:\n            widget: The reference widget to position against.\n            y_offset: Extra vertical gap (pixels) between the widget's\n                bottom edge and the popup's top edge.  Defaults to ``2``.\n        \"\"\"\n        global_pos = widget.mapToGlobal(QPoint(0, widget.height() + y_offset))\n        self.adjustSize()\n\n        screen = QtWidgets.QApplication.screenAt(global_pos)\n        if screen:\n            geo = screen.availableGeometry()\n\n            # Shift left if popup would overflow right edge\n            if global_pos.x() + self.width() &gt; geo.right():\n                global_pos.setX(geo.right() - self.width())\n\n            # Show above if not enough vertical space below\n            if global_pos.y() + self.height() &gt; geo.bottom():\n                above_y = widget.mapToGlobal(QPoint(0, 0)).y() - self.height()\n                global_pos.setY(above_y)\n\n        self.move(global_pos)\n        self.show()\n\n    def keyPressEvent(self, event: QtGui.QKeyEvent) -&gt; None:\n        \"\"\"Close on Escape key; delegate all other keys to the parent.\n\n        Args:\n            event: The key event.\n        \"\"\"\n        if event.key() == Qt.Key.Key_Escape:\n            self.close()\n        else:\n            super().keyPressEvent(event)\n\n    def closeEvent(self, event: QtGui.QCloseEvent) -&gt; None:\n        \"\"\"Emit ``popup_closed`` when the popup is hidden or closed.\n\n        Args:\n            event: The close event.\n        \"\"\"\n        self.popup_closed.emit()\n        super().closeEvent(event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/dropdown.html#client.ayon_core.ui.components.dropdown.AYDropdownPopup.__init__","title":"<code>__init__(parent=None, variant=AYFrame.Variants.Low, translucent_bg=True)</code>","text":"<p>Initialize the dropdown popup.</p> <p>Sets popup window flags and configures translucent background.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <code>QWidget | None</code> <p>Optional parent widget (used for style inheritance).</p> <code>None</code> <code>variant</code> <code>Variants</code> <p>Frame style variant.</p> <code>Low</code> <code>translucent_bg</code> <code>bool</code> <p>Whether to enable a translucent background. Set to <code>False</code> when the popup must be fully opaque.</p> <code>True</code> Source code in <code>client/ayon_core/ui/components/dropdown.py</code> <pre><code>def __init__(\n    self,\n    parent: QtWidgets.QWidget | None = None,\n    variant: Variants = AYFrame.Variants.Low,\n    translucent_bg: bool = True,\n) -&gt; None:\n    \"\"\"Initialize the dropdown popup.\n\n    Sets popup window flags and configures translucent background.\n\n    Args:\n        parent: Optional parent widget (used for style inheritance).\n        variant: Frame style variant.\n        translucent_bg: Whether to enable a translucent background.\n            Set to ``False`` when the popup must be fully opaque.\n    \"\"\"\n    super().__init__(parent, variant=variant)\n    self.setStyle(get_ayon_style())\n    self.setWindowFlags(\n        Qt.WindowType.Popup\n        | Qt.WindowType.FramelessWindowHint\n        | Qt.WindowType.NoDropShadowWindowHint\n    )\n    self.setAttribute(Qt.WidgetAttribute.WA_WindowPropagation)\n    self.setAttribute(\n        Qt.WidgetAttribute.WA_TranslucentBackground, translucent_bg\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/dropdown.html#client.ayon_core.ui.components.dropdown.AYDropdownPopup.closeEvent","title":"<code>closeEvent(event)</code>","text":"<p>Emit <code>popup_closed</code> when the popup is hidden or closed.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <code>QCloseEvent</code> <p>The close event.</p> required Source code in <code>client/ayon_core/ui/components/dropdown.py</code> <pre><code>def closeEvent(self, event: QtGui.QCloseEvent) -&gt; None:\n    \"\"\"Emit ``popup_closed`` when the popup is hidden or closed.\n\n    Args:\n        event: The close event.\n    \"\"\"\n    self.popup_closed.emit()\n    super().closeEvent(event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/dropdown.html#client.ayon_core.ui.components.dropdown.AYDropdownPopup.keyPressEvent","title":"<code>keyPressEvent(event)</code>","text":"<p>Close on Escape key; delegate all other keys to the parent.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <code>QKeyEvent</code> <p>The key event.</p> required Source code in <code>client/ayon_core/ui/components/dropdown.py</code> <pre><code>def keyPressEvent(self, event: QtGui.QKeyEvent) -&gt; None:\n    \"\"\"Close on Escape key; delegate all other keys to the parent.\n\n    Args:\n        event: The key event.\n    \"\"\"\n    if event.key() == Qt.Key.Key_Escape:\n        self.close()\n    else:\n        super().keyPressEvent(event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/dropdown.html#client.ayon_core.ui.components.dropdown.AYDropdownPopup.show_below","title":"<code>show_below(widget, y_offset=2)</code>","text":"<p>Position and show the popup below (or above) the widget.</p> <p>The popup is left-aligned with the widget's left edge.  If there is not enough horizontal space on screen the popup is shifted left. If there is not enough vertical space below, the popup is shown above the widget instead.</p> <p>Parameters:</p> Name Type Description Default <code>widget</code> <code>QWidget</code> <p>The reference widget to position against.</p> required <code>y_offset</code> <code>int</code> <p>Extra vertical gap (pixels) between the widget's bottom edge and the popup's top edge.  Defaults to <code>2</code>.</p> <code>2</code> Source code in <code>client/ayon_core/ui/components/dropdown.py</code> <pre><code>def show_below(\n    self,\n    widget: QtWidgets.QWidget,\n    y_offset: int = 2,\n) -&gt; None:\n    \"\"\"Position and show the popup below (or above) the widget.\n\n    The popup is left-aligned with the widget's left edge.  If there\n    is not enough horizontal space on screen the popup is shifted left.\n    If there is not enough vertical space below, the popup is shown\n    above the widget instead.\n\n    Args:\n        widget: The reference widget to position against.\n        y_offset: Extra vertical gap (pixels) between the widget's\n            bottom edge and the popup's top edge.  Defaults to ``2``.\n    \"\"\"\n    global_pos = widget.mapToGlobal(QPoint(0, widget.height() + y_offset))\n    self.adjustSize()\n\n    screen = QtWidgets.QApplication.screenAt(global_pos)\n    if screen:\n        geo = screen.availableGeometry()\n\n        # Shift left if popup would overflow right edge\n        if global_pos.x() + self.width() &gt; geo.right():\n            global_pos.setX(geo.right() - self.width())\n\n        # Show above if not enough vertical space below\n        if global_pos.y() + self.height() &gt; geo.bottom():\n            above_y = widget.mapToGlobal(QPoint(0, 0)).y() - self.height()\n            global_pos.setY(above_y)\n\n    self.move(global_pos)\n    self.show()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/entity_card.html","title":"entity_card","text":"<p>AYEntityCard \u2014 a card widget representing an AYON entity (task, version, product, etc.).</p> <p>Mirrors the EntityCard component from https://components.ayon.dev.</p>"},{"location":"autoapi/client/ayon_core/ui/components/entity_card.html#client.ayon_core.ui.components.entity_card--layout","title":"Layout","text":"<p>\u250c\u2500\u2500\u2500 header row \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510   \u2502  [project]  [path/breadcrumb]          [entity name bold]  \u2502   \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518   \u250c\u2500\u2500\u2500 card body (thumbnail + overlay) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510   \u2502  [background image or icon placeholder]                    \u2502   \u2502                                                            \u2502   \u2502  \u2504 overlay top row \u2504\u2504\u2504\u2504\u2504\u2504\u2504\u2504\u2504\u2504\u2504\u2504\u2504\u2504\u2504\u2504\u2504\u2504\u2504\u2504\u2504\u2504\u2504\u2504\u2504\u2504\u2504\u2504\u2504\u2504\u2504\u2504\u2504\u2504\u2504\u2504    \u2502   \u2502  [title chip (icon + text)]              [play chip]       \u2502   \u2502                                                            \u2502   \u2502  \u2504 overlay bottom row \u2504\u2504\u2504\u2504\u2504\u2504\u2504\u2504\u2504\u2504\u2504\u2504\u2504\u2504\u2504\u2504\u2504\u2504\u2504\u2504\u2504\u2504\u2504\u2504\u2504\u2504\u2504\u2504\u2504\u2504\u2504\u2504\u2504    \u2502   \u2502  [users][priority] [versions]            [status icon]    \u2502   \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518</p>"},{"location":"autoapi/client/ayon_core/ui/components/entity_card.html#client.ayon_core.ui.components.entity_card.AYEntityCard","title":"<code>AYEntityCard</code>","text":"<p>               Bases: <code>AYContainer</code></p> <p>A card widget representing an AYON entity (task, version, product\u2026).</p>"},{"location":"autoapi/client/ayon_core/ui/components/entity_card.html#client.ayon_core.ui.components.entity_card.AYEntityCard--signals","title":"Signals","text":"<p>activated     Emitted when the card is clicked (or keyboard-activated). title_clicked     Emitted when the title chip inside the card is clicked.</p> Source code in <code>client/ayon_core/ui/components/entity_card.py</code> <pre><code>class AYEntityCard(AYContainer):\n    \"\"\"A card widget representing an AYON entity (task, version, product\u2026).\n\n    Signals\n    -------\n    activated\n        Emitted when the card is clicked (or keyboard-activated).\n    title_clicked\n        Emitted when the title chip inside the card is clicked.\n    \"\"\"\n\n    activated = Signal()\n    title_clicked = Signal()\n\n    def __init__(\n        self,\n        *,\n        width: int = 200,\n        header: str = \"\",\n        path: str | list[str] = \"\",\n        project: str = \"\",\n        show_path: bool = False,\n        title: str = \"\",\n        title_icon: str = \"\",\n        title_color: str = \"\",\n        is_playable: bool = False,\n        users: list[dict[str, Any]] | None = None,\n        status: dict[str, Any] | None = None,\n        priority: dict[str, Any] | None = None,\n        hide_priority: bool = False,\n        version: str = \"\",\n        image_src: str | Path = \"\",\n        image_icon: str = \"image\",\n        is_active: bool = False,\n        is_loading: bool = False,\n        is_error: bool = False,\n        is_hover: bool = False,\n        is_dragging: bool = False,\n        is_draggable: bool = False,\n        placeholder_icon: str = \"image\",\n        async_file_cacher: (\n            Callable[[str, Callable[[str], None]], None] | None\n        ) = None,\n        parent: QWidget | None = None,\n    ) -&gt; None:\n        super().__init__(\n            parent,\n            layout=AYContainer.Layout.VBox,\n            variant=QFrameVariants.Entity_Card,\n            layout_margin=0,\n            layout_spacing=0,\n        )\n        self.set_width(width)\n\n        self.setStyle(get_ayon_style())\n        self.setAttribute(Qt.WidgetAttribute.WA_TranslucentBackground)\n        self.setMouseTracking(True)\n\n        # ---- state ----\n        self._header = header\n        self._path = path\n        self._project = project\n        self._show_path = show_path\n        self._title = title\n        self._title_icon = title_icon\n        self._title_color = title_color\n        self._is_playable = is_playable\n        self._users = users\n        self._status = status\n        self._priority = priority\n        self._hide_priority = hide_priority\n        self._version = version\n        self._image_src = image_src\n        self._image_icon = image_icon\n        self._is_active = is_active\n        self._is_loading = is_loading\n        self._is_error = is_error\n        self._is_hover = is_hover\n        self._is_dragging = is_dragging\n        self._is_draggable = is_draggable\n        self._async_file_cacher: (\n            Callable[[str, Callable[[str], None]], None] | None\n        ) = async_file_cacher\n        self._drag_start_pos: QPoint | None = None\n\n        # ---- build UI ----\n        self._header_widget = _CardHeader(self)\n        self.add_widget(self._header_widget)\n\n        _body_sd = get_ayon_style_data(\"QFrame\", self._variant_str)\n        _body_sd.set_context(self)\n        border_width = _body_sd[\"border-width\"]\n        self._card_body = _CardBody(\n            parent=self,\n            width=width - (border_width * 2),\n            async_file_cacher=async_file_cacher,\n        )\n        self._card_body.set_placeholder_icon(placeholder_icon)\n        body_size = self._card_body.size()\n        self._overlay = _CardOverlay(\n            parent=self,\n            size=body_size,\n        )\n\n        # Lay body &amp; overlay in a stacked layout so overlay is on top\n        stacked = QStackedLayout()\n        stacked.setStackingMode(QStackedLayout.StackingMode.StackAll)\n        stacked.addWidget(self._card_body)\n        stacked.addWidget(self._overlay)\n        stacked.setCurrentWidget(self._overlay)\n\n        # Wrap the stacked layout in a widget to keep it centered.\n        self._body_wrapper = QWidget(self)\n        self._body_wrapper.setAttribute(\n            Qt.WidgetAttribute.WA_TranslucentBackground\n        )\n        self._body_wrapper.setFixedSize(body_size)\n        self._body_wrapper.setLayout(stacked)\n        self.add_widget(self._body_wrapper)\n        self._layout.setAlignment(\n            self._body_wrapper, Qt.AlignmentFlag.AlignHCenter\n        )\n\n        # ---- init content ----\n        self._rebuild_header()\n        self._rebuild_thumbnail()\n        self._rebuild_overlay()\n        self._apply_body_state()\n\n        # ---- drag ----\n        if is_draggable:\n            self.setCursor(QCursor(Qt.CursorShape.OpenHandCursor))\n\n    def set_width(self, w: int) -&gt; None:\n        \"\"\"Set the card width and adjust height to maintain a 1.52:1 aspect\n        ratio (default thumbnail ratio from front-end).\"\"\"\n        self.setFixedSize(w, int(w / CARD_RATIO))\n\n    def resize_to_width(self, w: int) -&gt; None:\n        \"\"\"Resize the card and all internal sub-widgets to a new width.\"\"\"\n        _body_sd = get_ayon_style_data(\"QFrame\", self._variant_str)\n        _body_sd.set_context(self)\n        border_width = _body_sd[\"border-width\"]\n        body_w = w - (border_width * 2)\n        body_h = int(body_w / IMG_RATIO)\n        body_size = QSize(body_w, body_h)\n\n        self._card_body.set_size((body_w, body_h))\n        self._overlay.set_width(body_size)\n        self._body_wrapper.setFixedSize(body_size)\n        self.set_width(w)\n\n    # ------------------------------------------------------------------\n    # Rebuild helpers\n    # ------------------------------------------------------------------\n\n    def _rebuild_header(self) -&gt; None:\n        self._header_widget.update_content(\n            self._project, self._path, self._header, self._show_path\n        )\n\n    def _rebuild_thumbnail(self) -&gt; None:\n        self._card_body.set_thumbnail(self._image_src)\n        self._card_body.set_image_icon(self._image_icon)\n\n    def _rebuild_overlay(self) -&gt; None:\n        self._overlay.set_title(\n            self._title,\n            self._title_icon,\n            self._title_color,\n            on_click=self.title_clicked,\n        )\n        self._overlay.set_playable(self._is_playable)\n        self._overlay.set_users(self._users)\n        self._overlay.set_priority(self._priority, hide=self._hide_priority)\n        self._overlay.set_version(self._version)\n        self._overlay.set_status(self._status)\n        self._overlay.set_loading(self._is_loading)\n\n    def _apply_body_state(self) -&gt; None:\n        self._card_body.set_is_active(self._is_active)\n        self._card_body.set_is_hover(self._is_hover)\n        self._card_body.set_is_dragging(self._is_dragging)\n        self._card_body.set_is_loading(self._is_loading)\n        self._card_body.set_is_error(self._is_error)\n\n    # ------------------------------------------------------------------\n    # Properties\n    # ------------------------------------------------------------------\n\n    @property\n    def header(self) -&gt; str:\n        return self._header\n\n    @header.setter\n    def header(self, value: str) -&gt; None:\n        self._header = value\n        self._rebuild_header()\n\n    @property\n    def path(self) -&gt; str | list[str]:\n        return self._path\n\n    @path.setter\n    def path(self, value: str | list[str]) -&gt; None:\n        self._path = value\n        self._rebuild_header()\n\n    @property\n    def project(self) -&gt; str:\n        return self._project\n\n    @project.setter\n    def project(self, value: str) -&gt; None:\n        self._project = value\n        self._rebuild_header()\n\n    @property\n    def show_path(self) -&gt; bool:\n        return self._show_path\n\n    @show_path.setter\n    def show_path(self, value: bool) -&gt; None:\n        self._show_path = value\n        self._rebuild_header()\n\n    @property\n    def title(self) -&gt; str:\n        return self._title\n\n    @title.setter\n    def title(self, value: str) -&gt; None:\n        self._title = value\n        self._rebuild_overlay()\n\n    @property\n    def title_icon(self) -&gt; str:\n        return self._title_icon\n\n    @title_icon.setter\n    def title_icon(self, value: str) -&gt; None:\n        self._title_icon = value\n        self._rebuild_overlay()\n\n    @property\n    def title_color(self) -&gt; str:\n        return self._title_color\n\n    @title_color.setter\n    def title_color(self, value: str) -&gt; None:\n        self._title_color = value\n        self._rebuild_overlay()\n\n    @property\n    def is_playable(self) -&gt; bool:\n        return self._is_playable\n\n    @is_playable.setter\n    def is_playable(self, value: bool) -&gt; None:\n        self._is_playable = value\n        self._rebuild_overlay()\n\n    @property\n    def users(self) -&gt; list[dict[str, Any]] | None:\n        return self._users\n\n    @users.setter\n    def users(self, value: list[dict[str, Any]] | None) -&gt; None:\n        self._users = value\n        self._rebuild_overlay()\n\n    @property\n    def status(self) -&gt; dict[str, Any] | None:\n        return self._status\n\n    @status.setter\n    def status(self, value: dict[str, Any] | None) -&gt; None:\n        self._status = value\n        self._rebuild_overlay()\n\n    @property\n    def priority(self) -&gt; dict[str, Any] | None:\n        return self._priority\n\n    @priority.setter\n    def priority(self, value: dict[str, Any] | None) -&gt; None:\n        self._priority = value\n        self._rebuild_overlay()\n\n    @property\n    def hide_priority(self) -&gt; bool:\n        return self._hide_priority\n\n    @hide_priority.setter\n    def hide_priority(self, value: bool) -&gt; None:\n        self._hide_priority = value\n        self._rebuild_overlay()\n\n    @property\n    def version(self) -&gt; str | None:\n        return self._version\n\n    @version.setter\n    def version(self, value: str | None) -&gt; None:\n        self._version = value\n        self._rebuild_overlay()\n\n    @property\n    def image_src(self) -&gt; str | Path:\n        return self._image_src\n\n    @image_src.setter\n    def image_src(self, value: str | Path) -&gt; None:\n        self._image_src = value\n        self._rebuild_thumbnail()\n\n    @property\n    def image_icon(self) -&gt; str:\n        return self._image_icon\n\n    @image_icon.setter\n    def image_icon(self, value: str) -&gt; None:\n        self._image_icon = value\n        self._rebuild_thumbnail()\n\n    @property\n    def is_active(self) -&gt; bool:\n        return self._is_active\n\n    @is_active.setter\n    def is_active(self, value: bool) -&gt; None:\n        self._is_active = value\n        self._apply_body_state()\n\n    @property\n    def is_loading(self) -&gt; bool:\n        return self._is_loading\n\n    @is_loading.setter\n    def is_loading(self, value: bool) -&gt; None:\n        self._is_loading = value\n        self._apply_body_state()\n        self._rebuild_overlay()\n\n    @property\n    def is_error(self) -&gt; bool:\n        return self._is_error\n\n    @is_error.setter\n    def is_error(self, value: bool) -&gt; None:\n        self._is_error = value\n        self._apply_body_state()\n\n    @property\n    def is_hover(self) -&gt; bool:\n        return self._is_hover\n\n    @is_hover.setter\n    def is_hover(self, value: bool) -&gt; None:\n        self._is_hover = value\n        self._apply_body_state()\n\n    @property\n    def is_dragging(self) -&gt; bool:\n        return self._is_dragging\n\n    @is_dragging.setter\n    def is_dragging(self, value: bool) -&gt; None:\n        self._is_dragging = value\n        self._apply_body_state()\n\n    @property\n    def is_draggable(self) -&gt; bool:\n        return self._is_draggable\n\n    @is_draggable.setter\n    def is_draggable(self, value: bool) -&gt; None:\n        self._is_draggable = value\n        if value:\n            self.setCursor(QCursor(Qt.CursorShape.OpenHandCursor))\n        else:\n            self.unsetCursor()\n\n    @property\n    def placeholder_icon(self) -&gt; str:\n        return self._card_body._placeholder_icon_name\n\n    @placeholder_icon.setter\n    def placeholder_icon(self, value: str) -&gt; None:\n        self._card_body.set_placeholder_icon(value)\n        self._rebuild_thumbnail()\n\n    @property\n    def async_file_cacher(\n        self,\n    ) -&gt; Callable[[str, Callable[[str], None]], None] | None:\n        \"\"\"Non-blocking thumbnail fetcher used by the card body.\n\n        When set, the card body's :class:`AYEntityThumbnail` will call\n        this callable with ``(key, on_loaded)`` whenever a thumbnail\n        cache miss is detected.  The callable should schedule the\n        download on a background thread and call ``on_loaded(file_path)``\n        on the Qt main thread when complete.\n\n        Setting this property also updates the underlying\n        :class:`_CardBody` so that any subsequent :meth:`set_thumbnail`\n        calls benefit from the new fetcher immediately.\n        \"\"\"\n        return self._async_file_cacher\n\n    @async_file_cacher.setter\n    def async_file_cacher(\n        self,\n        value: Callable[[str, Callable[[str], None]], None] | None,\n    ) -&gt; None:\n        self._async_file_cacher = value\n        # Forward to the card body so it can self-fetch on cache miss.\n        self._card_body._async_file_cacher = value\n        # Trigger a fetch only when the current image is not yet available.\n        if value and self._image_src:\n            ic = ImageCache.get_instance()\n            already_cached = ic.has(str(self._image_src))\n            on_disk = Path(self._image_src).exists()\n            if not already_cached and not on_disk:\n                self._card_body.set_thumbnail(self._image_src)\n\n    # ------------------------------------------------------------------\n    # Paint\n    # ------------------------------------------------------------------\n\n    def paintEvent(self, arg__1: QPaintEvent) -&gt; None:\n        super().paintEvent(arg__1)\n        if not self._is_active:\n            return\n        style_all = get_ayon_style_data(\"QFrame\", self._variant_str)\n        style_all.set_context(self)\n        border_width = int(style_all.get(\"border-width\", 2))\n        border_radius = float(style_all.get(\"border-radius\", 10))\n        border_color = QColor(\n            style_all.get(\"active\", {}).get(\"border-color\", \"#8fceff\")\n        )\n        p = QPainter(self)\n        p.setRenderHint(QPainter.RenderHint.Antialiasing)\n        pen = QPen(border_color, border_width)\n        p.setPen(pen)\n        p.setBrush(QBrush(Qt.BrushStyle.NoBrush))\n        half = border_width / 2.0\n        draw_rect = QRectF(self.rect()).adjusted(half, half, -half, -half)\n        p.drawRoundedRect(draw_rect, border_radius, border_radius)\n\n    # ------------------------------------------------------------------\n    # Mouse / drag events\n    # ------------------------------------------------------------------\n\n    def enterEvent(self, event: QtGui.QEnterEvent) -&gt; None:\n        if self._is_draggable:\n            self.setCursor(QCursor(Qt.CursorShape.OpenHandCursor))\n        super().enterEvent(event)\n\n    def leaveEvent(self, event: QtCore.QEvent) -&gt; None:\n        if self._is_draggable:\n            self.setCursor(QCursor(Qt.CursorShape.OpenHandCursor))\n        super().leaveEvent(event)\n\n    def mousePressEvent(self, event: QtGui.QMouseEvent) -&gt; None:\n        if event.button() == Qt.MouseButton.LeftButton:\n            self._drag_start_pos = event.pos()\n        super().mousePressEvent(event)\n\n    def mouseMoveEvent(self, event: QtGui.QMouseEvent) -&gt; None:\n        if (\n            self._is_draggable\n            and self._drag_start_pos is not None\n            and event.buttons() &amp; Qt.MouseButton.LeftButton\n        ):\n            delta = (event.pos() - self._drag_start_pos).manhattanLength()\n            if delta &gt;= QApplication.startDragDistance():\n                self._start_drag()\n                self._drag_start_pos = None\n                return\n        super().mouseMoveEvent(event)\n\n    def mouseReleaseEvent(self, event: QtGui.QMouseEvent) -&gt; None:\n        if (\n            event.button() == Qt.MouseButton.LeftButton\n            and self._drag_start_pos is not None\n        ):\n            self.activated.emit()\n            self._drag_start_pos = None\n        super().mouseReleaseEvent(event)\n\n    def _start_drag(self) -&gt; None:\n        drag = QDrag(self)\n        mime = QMimeData()\n        mime.setText(self._header)\n        drag.setMimeData(mime)\n        # Create a semi-transparent drag thumbnail from the card body\n        pxm = self._card_body.grab()\n        semi = QPixmap(pxm.size())\n        semi.fill(Qt.GlobalColor.transparent)\n        painter = QPainter(semi)\n        painter.setOpacity(0.75)\n        painter.drawPixmap(0, 0, pxm)\n        painter.end()\n        drag.setPixmap(semi)\n        drag.setHotSpot(self._card_body.rect().center())\n        self.is_dragging = True\n        drag.exec(Qt.DropAction.CopyAction | Qt.DropAction.MoveAction)\n        self.is_dragging = False\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/entity_card.html#client.ayon_core.ui.components.entity_card.AYEntityCard.async_file_cacher","title":"<code>async_file_cacher</code>  <code>property</code> <code>writable</code>","text":"<p>Non-blocking thumbnail fetcher used by the card body.</p> <p>When set, the card body's :class:<code>AYEntityThumbnail</code> will call this callable with <code>(key, on_loaded)</code> whenever a thumbnail cache miss is detected.  The callable should schedule the download on a background thread and call <code>on_loaded(file_path)</code> on the Qt main thread when complete.</p> <p>Setting this property also updates the underlying :class:<code>_CardBody</code> so that any subsequent :meth:<code>set_thumbnail</code> calls benefit from the new fetcher immediately.</p>"},{"location":"autoapi/client/ayon_core/ui/components/entity_card.html#client.ayon_core.ui.components.entity_card.AYEntityCard.resize_to_width","title":"<code>resize_to_width(w)</code>","text":"<p>Resize the card and all internal sub-widgets to a new width.</p> Source code in <code>client/ayon_core/ui/components/entity_card.py</code> <pre><code>def resize_to_width(self, w: int) -&gt; None:\n    \"\"\"Resize the card and all internal sub-widgets to a new width.\"\"\"\n    _body_sd = get_ayon_style_data(\"QFrame\", self._variant_str)\n    _body_sd.set_context(self)\n    border_width = _body_sd[\"border-width\"]\n    body_w = w - (border_width * 2)\n    body_h = int(body_w / IMG_RATIO)\n    body_size = QSize(body_w, body_h)\n\n    self._card_body.set_size((body_w, body_h))\n    self._overlay.set_width(body_size)\n    self._body_wrapper.setFixedSize(body_size)\n    self.set_width(w)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/entity_card.html#client.ayon_core.ui.components.entity_card.AYEntityCard.set_width","title":"<code>set_width(w)</code>","text":"<p>Set the card width and adjust height to maintain a 1.52:1 aspect ratio (default thumbnail ratio from front-end).</p> Source code in <code>client/ayon_core/ui/components/entity_card.py</code> <pre><code>def set_width(self, w: int) -&gt; None:\n    \"\"\"Set the card width and adjust height to maintain a 1.52:1 aspect\n    ratio (default thumbnail ratio from front-end).\"\"\"\n    self.setFixedSize(w, int(w / CARD_RATIO))\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/entity_path.html","title":"entity_path","text":""},{"location":"autoapi/client/ayon_core/ui/components/entity_thumbnail.html","title":"entity_thumbnail","text":""},{"location":"autoapi/client/ayon_core/ui/components/entity_thumbnail.html#client.ayon_core.ui.components.entity_thumbnail.AYEntityThumbnail","title":"<code>AYEntityThumbnail</code>","text":"<p>               Bases: <code>StyleMixin</code>, <code>QPushButton</code></p> <p>A push button widget that displays a thumbnail image for an entity.</p> <p>Supports single images or comma-separated composites with automatic caching (sync and async), fade-in animations, and customizable placeholder icons.</p> Source code in <code>client/ayon_core/ui/components/entity_thumbnail.py</code> <pre><code>class AYEntityThumbnail(StyleMixin, QPushButton):\n    \"\"\"A push button widget that displays a thumbnail image for an entity.\n\n    Supports single images or comma-separated composites with automatic\n    caching (sync and async), fade-in animations, and customizable\n    placeholder icons.\n    \"\"\"\n\n    class Variants(Enum):\n        \"\"\"Visual style variants for the thumbnail button.\"\"\"\n\n        Thumbnail = QPushButtonVariants.Thumbnail.value\n        Entity_Card = QPushButtonVariants.Entity_Card.value\n\n    # Minimum readable width (px) for each composite image slice.\n    _MIN_SLICE_WIDTH: int = 10\n    # 1-px inset applied to clip polygons to avoid overlapping the border.\n    _CLIP_INSET: int = 1\n\n    def __init__(\n        self,\n        src: Path | str = \"\",\n        file_cacher: Callable[[str], Path | str] | None = None,\n        async_file_cacher: (\n            Callable[[str, Callable[[str], None]], None] | None\n        ) = None,\n        placeholder_icon: str = \"image\",\n        placeholder_scale: float = 0.5,\n        placeholder_icon_fill: bool = False,\n        size: tuple[int, int] = (85, 48),\n        fade_duration: int = 0,\n        variant: Variants = Variants.Thumbnail,\n        **kwargs,\n    ):\n        \"\"\"A widget that displays a thumbnail image for an entity, with options\n        to customize the image source, caching behavior, and size.\n\n        Args:\n            src: Initial image source (path or cache key).\n            file_cacher: Synchronous callable ``(key) -&gt; file_path`` used to\n                populate ``ImageCache`` on a cache miss.  Called on the\n                calling thread \u2014 **must not** block the Qt main thread.\n            async_file_cacher: Non-blocking callable\n                ``(key, on_loaded: Callable[[str], None]) -&gt; None``.\n                When the thumbnail is not yet cached this is invoked\n                immediately and should schedule the download on a\n                background thread.  Once the file is ready it must call\n                ``on_loaded(file_path)`` on the main thread.  The widget\n                will then call :meth:`set_thumbnail` again with the same\n                key, which now resolves to the cached file.  Mutually\n                exclusive with *file_cacher*.\n            placeholder_icon: Icon name shown before the thumbnail loads.\n            placeholder_scale: Scale factor for the placeholder icon.\n            placeholder_icon_fill: Whether to fill the placeholder icon.\n            size: ``(width, height)`` in pixels.\n            fade_duration: Fade-in animation duration in milliseconds.\n            variant: Visual style variant.\n\n        Raises:\n            ValueError: If both *file_cacher* and *async_file_cacher* are set.\n        \"\"\"\n        self._file_cacher = file_cacher\n        self._async_file_cacher: (\n            Callable[[str, Callable[[str], None]], None] | None\n        ) = async_file_cacher\n        # Keys for which an async fetch is already in flight (avoid duplicates)\n        self._pending_async_keys: set[str] = set()\n        self._pending_lock = threading.Lock()\n        # In-memory cache of assembled composite pixmaps keyed by the\n        # original comma-separated src string.  Cleared on size changes.\n        self._composite_cache: dict[str, QPixmap] = {}\n        if file_cacher and async_file_cacher:\n            raise ValueError(\n                \"Only one of 'file_cacher' or 'async_file_cacher' may be \"\n                \"set, not both.\"\n            )\n        self._size = size\n        self._variant_str: str = variant.value\n        self._placeholder_icon_name = placeholder_icon\n        self._placeholder_scale = placeholder_scale\n        self._placeholder_icon_fill = placeholder_icon_fill\n        icn_size = int(size[1] * placeholder_scale)\n        self._placeholder_icon = QIcon(\n            get_icon(\n                placeholder_icon,\n                color=\"#10ffffff\",\n                fill=placeholder_icon_fill,\n            ).pixmap(\n                QSize(icn_size, icn_size),\n            )\n        )\n\n        super().__init__(QIcon(), \"\", **kwargs)\n        self.setStyle(get_ayon_style())\n\n        self._src: Path | str = \"\"\n        self._incoming_pixmap: QPixmap | None = None\n        self._opacity: float = 1.0\n        self._anim = QVariantAnimation(self)\n        self._anim.setStartValue(0.0)\n        self._anim.setEndValue(1.0)\n        self._anim.setDuration(fade_duration)\n        self._anim.setEasingCurve(QEasingCurve.Type.InOutQuad)\n        self._anim.valueChanged.connect(self._on_fade_tick)\n        self._anim.finished.connect(self._on_fade_done)\n        self._bg_color = QColor(\n            get_ayon_style()\n            .model.get_style(\"QPushButton\", variant=self._variant_str)\n            .get(\"background-color\", \"#000000\")\n        )\n\n        self.set_thumbnail(src)\n        self.setFixedSize(*self._size)\n\n    @property\n    def slant_px(self) -&gt; int:\n        \"\"\"Return the horizontal slant offset in pixels for composite slices.\n\n        This value determines how much each slice edge is skewed to create\n        a diagonal separation effect between composite images. We default to\n        10% of the widget's width.\n        \"\"\"\n        return int(self._size[0] * 0.1 * self.devicePixelRatio())\n\n    @property\n    def max_slices(self) -&gt; int:\n        \"\"\"Return the maximum number of slices that fit within the widget\n        width.\n\n        Calculated based on :attr:`_MIN_SLICE_WIDTH` to ensure each slice\n        is at least the minimum readable width.\n        \"\"\"\n        return int(self._size[0] / self._MIN_SLICE_WIDTH)\n\n    def set_fade_duration(self, duration: int) -&gt; None:\n        \"\"\"Set the duration of the fade animation when changing thumbnails.\n\n        Args:\n            duration: Animation duration in milliseconds.\n        \"\"\"\n        self._anim.setDuration(duration)\n\n    def _make_placeholder_icon(self) -&gt; QIcon:\n        \"\"\"Build a placeholder icon scaled to the current size and DPR.\n\n        Returns:\n            A QIcon sized for the current widget dimensions and pixel ratio.\n        \"\"\"\n        dpr = self.devicePixelRatio()\n        icn_size = int(self._size[1] * self._placeholder_scale * dpr)\n        return QIcon(\n            get_icon(\n                self._placeholder_icon_name,\n                color=\"#10ffffff\",\n                fill=self._placeholder_icon_fill,\n            ).pixmap(QSize(icn_size, icn_size))\n        )\n\n    def set_size(self, size: tuple[int, int]) -&gt; None:\n        \"\"\"Resize the thumbnail and update the icon size to match.\n\n        Clears the in-memory composite cache since cached composites\n        are size-specific.\n\n        Args:\n            size: New ``(width, height)`` in pixels.\n        \"\"\"\n        self._size = size\n        # Cached composites are size-specific; they must be rebuilt.\n        self._composite_cache.clear()\n        self._placeholder_icon = self._make_placeholder_icon()\n        self.setFixedSize(*self._size)\n        if self.icon() and not self.icon().isNull():\n            self.setIconSize(QSize(*self._size))\n        self.update()\n\n    def set_placeholder_icon(self, icon_name: str) -&gt; None:\n        \"\"\"Set the placeholder icon to show when no thumbnail is available.\n\n        Args:\n            icon_name: Material symbol icon name (e.g., ``\"image\"``).\n        \"\"\"\n        if not icon_name:\n            return\n        self._placeholder_icon_name = icon_name\n        self._placeholder_icon = self._make_placeholder_icon()\n        if not self.icon() or self.icon().isNull():\n            self.setIcon(self._placeholder_icon)\n            self.setIconSize(QSize(*self._size))\n            self.update()\n\n    def _resolve_src(self, src: Path | str) -&gt; Path | str:\n        \"\"\"Resolve a cache key or path to an existing file path.\n\n        Pure resolution \u2014 no side effects.\n\n        Resolution order:\n        1. If *src* is already a real path on disk, return it as-is.\n        2. If a synchronous *file_cacher* is configured, populate\n           ``ImageCache`` now (blocking) and return the cached path.\n        3. If the key is already in ``ImageCache``, return its path.\n        4. Fall through: return *src* unchanged (shows placeholder).\n\n        Args:\n            src: A file path, cache key, or empty string.\n\n        Returns:\n            Resolved file path if available, otherwise the original *src*.\n        \"\"\"\n        if Path(src).exists():\n            return src\n        ic = ImageCache.get_instance()\n        if self._file_cacher:\n            return ic.get(str(src), partial(self._file_cacher, src))\n        if ic.has(str(src)):\n            return ic.get_path(str(src)) or \"\"\n        return src\n\n    def _maybe_schedule_async_fetch(self, src: Path | str) -&gt; None:\n        \"\"\"Schedule a non-blocking background fetch for *src* if needed.\n\n        Does nothing when *async_file_cacher* is not set or a fetch for\n        *src* is already in flight.  On completion, calls\n        :meth:`_load_pixmap_from_path` directly with the resolved path.\n\n        Args:\n            src: The cache key or path to fetch asynchronously.\n        \"\"\"\n        if not self._async_file_cacher:\n            return\n        key_str = str(src)\n        if not key_str or key_str in self._pending_async_keys:\n            return\n        self._pending_async_keys.add(key_str)\n        thumbnail_ref = weakref.ref(self)\n\n        def _on_loaded(fpath: str, _k: str = key_str) -&gt; None:\n            thumbnail = thumbnail_ref()\n            if thumbnail is None:\n                return\n            thumbnail._pending_async_keys.discard(_k)\n            if fpath:\n                thumbnail._load_pixmap_from_path(fpath)\n\n        self._async_file_cacher(key_str, _on_loaded)\n\n    def _center_crop_pixmap(\n        self,\n        pixmap: QPixmap,\n        slot_w: int,\n        slot_h: int,\n    ) -&gt; QPixmap:\n        \"\"\"Scale to widget height then center-crop to slot width.\n\n        The image is first scaled so its height equals *slot_h* (preserving\n        aspect ratio).  If the resulting width exceeds *slot_w* the centre\n        strip is extracted.  If it is narrower the image is centred over the\n        widget background colour.\n\n        Args:\n            pixmap: Source pixmap.\n            slot_w: Target width in pixels.\n            slot_h: Target height in pixels.\n\n        Returns:\n            A new QPixmap of exactly (slot_w, slot_h).\n        \"\"\"\n        scaled = pixmap.scaledToHeight(\n            slot_h,\n            Qt.TransformationMode.SmoothTransformation,\n        )\n        if scaled.width() &gt;= slot_w:\n            x = (scaled.width() - slot_w) // 2\n            return scaled.copy(QRect(x, 0, slot_w, slot_h))\n        # Image narrower than slot \u2014 centre on background fill.\n        result = QPixmap(slot_w, slot_h)\n        result.fill(self._bg_color)\n        if not scaled.isNull():\n            painter = QPainter(result)\n            x = (slot_w - scaled.width()) // 2\n            painter.drawPixmap(x, 0, scaled)\n            painter.end()\n        return result\n\n    def _build_composite_pixmap(self, paths: list[str]) -&gt; QPixmap:\n        \"\"\"Assemble a horizontal composite pixmap from multiple image paths.\n\n        Each image occupies an equal-width slot and is center-cropped to\n        fill it entirely.  Slots whose path is empty or unreadable are left\n        as the widget background color.  When the number of images exceeds\n        :attr:`max_slices`, a ``more_horiz`` icon is overlaid on the last\n        slot to indicate hidden items.\n\n        Args:\n            paths: Ordered list of resolved file paths, one per slot.\n\n        Returns:\n            A composite QPixmap of ``self._size``.\n        \"\"\"\n        dpr = self.devicePixelRatio()\n        w = int(self._size[0] * dpr)\n        h = int(self._size[1] * dpr)\n        num_paths = len(paths)\n        slot_w = max(w // num_paths, int(self._MIN_SLICE_WIDTH * dpr))\n        ci = self._CLIP_INSET\n\n        result = QPixmap(w, h)\n        result.fill(self._bg_color)\n        painter = QPainter(result)\n        painter.setRenderHint(QPainter.RenderHint.Antialiasing, True)\n\n        hslpx = self.slant_px // 2\n        max_slices = self.max_slices\n        last_i = min(num_paths, max_slices) - 1\n        num_drawn = 0\n\n        for i, path in enumerate(paths):\n            if not path:\n                continue\n            raw = QPixmap(path)\n            if raw.isNull():\n                continue\n\n            cropped = self._center_crop_pixmap(raw, slot_w + hslpx * 2, h)\n            # Parallelogram clip: left edge slants right, right edge slants\n            # left, creating a diagonal separation between adjacent slices.\n            left_slant = hslpx if i &gt; 0 else 0\n            right_slant = hslpx if i &lt; last_i else 0\n            polygon = QPolygonF(\n                [\n                    QPointF(i * slot_w + left_slant + ci, ci),\n                    QPointF((i + 1) * slot_w + hslpx - ci, ci),\n                    QPointF(\n                        (i + 1) * slot_w - right_slant - ci,\n                        h - ci,\n                    ),\n                    QPointF(i * slot_w - hslpx + ci, h - ci),\n                ]\n            )\n            clip_path = QPainterPath()\n            clip_path.addPolygon(polygon)\n            painter.setClipPath(clip_path)\n            painter.drawPixmap(i * slot_w - hslpx, 0, cropped)\n\n            num_drawn += 1\n            if num_drawn &gt;= max_slices:\n                icon_size = 16\n                icon_pad = 2\n                overflow_icon = get_icon(\n                    \"more_horiz\",\n                    color=\"#f2f2f3\",\n                    fill=False,\n                ).pixmap(QSize(icon_size, icon_size))\n                painter.drawPixmap(\n                    (i + 1) * slot_w - icon_size - icon_pad,\n                    h - icon_size - icon_pad,\n                    overflow_icon,\n                )\n                break\n\n        painter.end()\n        return result\n\n    def _on_fade_tick(self, value: float) -&gt; None:\n        \"\"\"Handle each step of the fade-in animation.\n\n        Updates the current opacity value and triggers a repaint.\n\n        Args:\n            value: Current animation value between 0.0 and 1.0.\n        \"\"\"\n        self._opacity = value\n        self.update()\n\n    def _on_fade_done(self) -&gt; None:\n        \"\"\"Handle the completion of the fade-in animation.\n\n        Promotes the incoming pixmap to the button's icon and resets\n        the opacity state.\n        \"\"\"\n        pixmap = self._incoming_pixmap\n        if pixmap and not pixmap.isNull():\n            icon = QIcon()\n            icon.addPixmap(pixmap)\n            self.setIcon(icon)\n            self.setIconSize(QSize(*self._size))\n        else:\n            self.setIcon(QIcon())\n        self._incoming_pixmap = None\n        self._opacity = 1.0\n        self.update()\n\n    def _load_pixmap_from_path(self, fpath: str) -&gt; None:\n        \"\"\"Load and display a pixmap directly from a resolved file path.\n\n        Updates ``_src``, stops any running animation, scales the image,\n        and starts the fade-in.  Avoids a second :meth:`_resolve_src`\n        pass when the caller already holds the concrete path (e.g. the\n        async fetch callback).\n\n        Args:\n            fpath: Absolute path to the image file.\n        \"\"\"\n        self._src = fpath\n        self._anim.stop()\n        raw = QPixmap(fpath)\n        raw.setDevicePixelRatio(self.devicePixelRatio())\n        self._incoming_pixmap = raw.scaled(\n            QSize(*self._size) * raw.devicePixelRatio(),\n            Qt.AspectRatioMode.KeepAspectRatio,\n            Qt.TransformationMode.SmoothTransformation,\n        )\n        self._opacity = 0.0\n        self._anim.start()\n\n    def set_thumbnail(self, name: Path | str) -&gt; None:\n        \"\"\"Set the thumbnail image for the button.\n\n        *name* may be a single path/cache-key **or** a comma-separated\n        list of paths/cache-keys.  When multiple items are given the images\n        are loaded, center-cropped into equal-width slots and assembled into\n        a single composite pixmap that is cached in memory.\n\n        Args:\n            name: Image source \u2014 a file path, cache key, or comma-separated\n                combination of the two.\n        \"\"\"\n        src_str = str(name).strip()\n        parts = [p.strip() for p in src_str.split(\",\") if p.strip()]\n\n        if len(parts) &lt;= 1:\n            resolved = self._resolve_src(name)\n            if resolved and Path(resolved).exists():\n                self._load_pixmap_from_path(str(resolved))\n            else:\n                self._src = name\n                self._anim.stop()\n                self._maybe_schedule_async_fetch(name)\n                self._incoming_pixmap = None\n                self._opacity = 1.0\n                self.setIcon(self._placeholder_icon)\n            return\n\n        # --- Multi-source composite path ---\n        self._src = src_str\n\n        # Check the in-memory composite cache before resolving.\n        if src_str in self._composite_cache:\n            self._anim.stop()\n            self._incoming_pixmap = self._composite_cache[src_str]\n            self._opacity = 0.0\n            self._anim.start()\n            return\n\n        # Resolve each source synchronously where possible.\n        resolved_paths: dict[int, str | None] = {}\n        missing_indices: list[int] = []\n        for i, part in enumerate(parts):\n            r = self._resolve_src(part)\n            if r and Path(str(r)).exists():\n                resolved_paths[i] = str(r)\n            else:\n                resolved_paths[i] = None\n                missing_indices.append(i)\n\n        non_empty_paths = [p for p in resolved_paths.values() if p]\n        sparse_key = \",\".join(non_empty_paths[: self.max_slices])\n        if sparse_key in self._composite_cache:\n            self._anim.stop()\n            self._incoming_pixmap = self._composite_cache[sparse_key]\n            self._opacity = 0.0\n            self._anim.start()\n            return\n\n        if not missing_indices:\n            # All slots ready \u2014 build and cache immediately.\n            composite = self._build_composite_pixmap(non_empty_paths)\n            self._composite_cache[sparse_key] = composite\n            self._anim.stop()\n            self._incoming_pixmap = composite\n            self._opacity = 0.0\n            self._anim.start()\n            return\n\n        # Some slots still need async fetching \u2014 show placeholder meanwhile.\n        self._anim.stop()\n        self._incoming_pixmap = None\n        self._opacity = 1.0\n        self.setIcon(self._placeholder_icon)\n\n        if not self._async_file_cacher:\n            return\n\n        pending = _PendingCompositeState(\n            resolved=resolved_paths,\n            pending_count=len(missing_indices),\n            total=len(parts),\n        )\n        thumbnail_ref = weakref.ref(self)\n\n        def _make_slot_callback(\n            slot_idx: int,\n        ) -&gt; Callable[[str], None]:\n            def _on_slot_loaded(fpath: str) -&gt; None:\n                widget = thumbnail_ref()\n                if widget is None:\n                    return\n                # Discard if a newer set_thumbnail replaced this src.\n                if str(widget._src) != src_str:\n                    return\n                with widget._pending_lock:\n                    pending.resolved[slot_idx] = fpath or None\n                    pending.pending_count -= 1\n                    if pending.pending_count &gt; 0:\n                        return\n                all_paths = [\n                    pending.resolved.get(j)\n                    for j in range(pending.total)\n                    if pending.resolved.get(j)\n                ]\n                composite = widget._build_composite_pixmap(all_paths)\n                resolved_key = \",\".join(all_paths[: widget.max_slices])\n                widget._composite_cache[resolved_key] = composite\n                widget._anim.stop()\n                widget._incoming_pixmap = composite\n                widget._opacity = 0.0\n                widget._anim.start()\n\n            return _on_slot_loaded\n\n        for idx in missing_indices:\n            self._async_file_cacher(parts[idx], _make_slot_callback(idx))\n\n    def paintEvent(self, arg__1: QPaintEvent) -&gt; None:\n        \"\"\"Override the default paint event to render the thumbnail with\n        effects.\n\n        Draws the styled button base using the AYON style model, then\n        overlays the incoming pixmap with the current fade opacity for\n        smooth transition animations.  Maintains a 1-pixel inset clip\n        to prevent the image from overlapping the button border.\n\n        Args:\n            arg__1: The paint event containing the update region.\n        \"\"\"\n        p = QPainter(self)\n        option = QStyleOptionButton()\n        self.initStyleOption(option)\n        # override rect set by stylesheet\n        size = QSize(*self._size)\n        self.setFixedSize(size)\n        option.rect = QRect(0, 0, size.width(), size.height())\n        # draw base (current icon)\n        get_ayon_style().drawControl(\n            QStyle.ControlElement.CE_PushButton, option, p, self\n        )\n        # overlay incoming pixmap with fade opacity\n        if self._incoming_pixmap and not self._incoming_pixmap.isNull():\n            dpr = self.devicePixelRatio()\n            x = (size.width() - self._incoming_pixmap.width() // dpr) // 2\n            y = (size.height() - self._incoming_pixmap.height() // dpr) // 2\n            p.save()\n            p.setClipRect(QRect(1, 1, size.width() - 2, size.height() - 2))\n            p.setOpacity(self._opacity)\n            p.fillRect(option.rect, self._bg_color)\n            p.drawPixmap(x, y, self._incoming_pixmap)\n            p.restore()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/entity_thumbnail.html#client.ayon_core.ui.components.entity_thumbnail.AYEntityThumbnail.max_slices","title":"<code>max_slices</code>  <code>property</code>","text":"<p>Return the maximum number of slices that fit within the widget width.</p> <p>Calculated based on :attr:<code>_MIN_SLICE_WIDTH</code> to ensure each slice is at least the minimum readable width.</p>"},{"location":"autoapi/client/ayon_core/ui/components/entity_thumbnail.html#client.ayon_core.ui.components.entity_thumbnail.AYEntityThumbnail.slant_px","title":"<code>slant_px</code>  <code>property</code>","text":"<p>Return the horizontal slant offset in pixels for composite slices.</p> <p>This value determines how much each slice edge is skewed to create a diagonal separation effect between composite images. We default to 10% of the widget's width.</p>"},{"location":"autoapi/client/ayon_core/ui/components/entity_thumbnail.html#client.ayon_core.ui.components.entity_thumbnail.AYEntityThumbnail.Variants","title":"<code>Variants</code>","text":"<p>               Bases: <code>Enum</code></p> <p>Visual style variants for the thumbnail button.</p> Source code in <code>client/ayon_core/ui/components/entity_thumbnail.py</code> <pre><code>class Variants(Enum):\n    \"\"\"Visual style variants for the thumbnail button.\"\"\"\n\n    Thumbnail = QPushButtonVariants.Thumbnail.value\n    Entity_Card = QPushButtonVariants.Entity_Card.value\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/entity_thumbnail.html#client.ayon_core.ui.components.entity_thumbnail.AYEntityThumbnail.__init__","title":"<code>__init__(src='', file_cacher=None, async_file_cacher=None, placeholder_icon='image', placeholder_scale=0.5, placeholder_icon_fill=False, size=(85, 48), fade_duration=0, variant=Variants.Thumbnail, **kwargs)</code>","text":"<p>A widget that displays a thumbnail image for an entity, with options to customize the image source, caching behavior, and size.</p> <p>Parameters:</p> Name Type Description Default <code>src</code> <code>Path | str</code> <p>Initial image source (path or cache key).</p> <code>''</code> <code>file_cacher</code> <code>Callable[[str], Path | str] | None</code> <p>Synchronous callable <code>(key) -&gt; file_path</code> used to populate <code>ImageCache</code> on a cache miss.  Called on the calling thread \u2014 must not block the Qt main thread.</p> <code>None</code> <code>async_file_cacher</code> <code>Callable[[str, Callable[[str], None]], None] | None</code> <p>Non-blocking callable <code>(key, on_loaded: Callable[[str], None]) -&gt; None</code>. When the thumbnail is not yet cached this is invoked immediately and should schedule the download on a background thread.  Once the file is ready it must call <code>on_loaded(file_path)</code> on the main thread.  The widget will then call :meth:<code>set_thumbnail</code> again with the same key, which now resolves to the cached file.  Mutually exclusive with file_cacher.</p> <code>None</code> <code>placeholder_icon</code> <code>str</code> <p>Icon name shown before the thumbnail loads.</p> <code>'image'</code> <code>placeholder_scale</code> <code>float</code> <p>Scale factor for the placeholder icon.</p> <code>0.5</code> <code>placeholder_icon_fill</code> <code>bool</code> <p>Whether to fill the placeholder icon.</p> <code>False</code> <code>size</code> <code>tuple[int, int]</code> <p><code>(width, height)</code> in pixels.</p> <code>(85, 48)</code> <code>fade_duration</code> <code>int</code> <p>Fade-in animation duration in milliseconds.</p> <code>0</code> <code>variant</code> <code>Variants</code> <p>Visual style variant.</p> <code>Thumbnail</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If both file_cacher and async_file_cacher are set.</p> Source code in <code>client/ayon_core/ui/components/entity_thumbnail.py</code> <pre><code>def __init__(\n    self,\n    src: Path | str = \"\",\n    file_cacher: Callable[[str], Path | str] | None = None,\n    async_file_cacher: (\n        Callable[[str, Callable[[str], None]], None] | None\n    ) = None,\n    placeholder_icon: str = \"image\",\n    placeholder_scale: float = 0.5,\n    placeholder_icon_fill: bool = False,\n    size: tuple[int, int] = (85, 48),\n    fade_duration: int = 0,\n    variant: Variants = Variants.Thumbnail,\n    **kwargs,\n):\n    \"\"\"A widget that displays a thumbnail image for an entity, with options\n    to customize the image source, caching behavior, and size.\n\n    Args:\n        src: Initial image source (path or cache key).\n        file_cacher: Synchronous callable ``(key) -&gt; file_path`` used to\n            populate ``ImageCache`` on a cache miss.  Called on the\n            calling thread \u2014 **must not** block the Qt main thread.\n        async_file_cacher: Non-blocking callable\n            ``(key, on_loaded: Callable[[str], None]) -&gt; None``.\n            When the thumbnail is not yet cached this is invoked\n            immediately and should schedule the download on a\n            background thread.  Once the file is ready it must call\n            ``on_loaded(file_path)`` on the main thread.  The widget\n            will then call :meth:`set_thumbnail` again with the same\n            key, which now resolves to the cached file.  Mutually\n            exclusive with *file_cacher*.\n        placeholder_icon: Icon name shown before the thumbnail loads.\n        placeholder_scale: Scale factor for the placeholder icon.\n        placeholder_icon_fill: Whether to fill the placeholder icon.\n        size: ``(width, height)`` in pixels.\n        fade_duration: Fade-in animation duration in milliseconds.\n        variant: Visual style variant.\n\n    Raises:\n        ValueError: If both *file_cacher* and *async_file_cacher* are set.\n    \"\"\"\n    self._file_cacher = file_cacher\n    self._async_file_cacher: (\n        Callable[[str, Callable[[str], None]], None] | None\n    ) = async_file_cacher\n    # Keys for which an async fetch is already in flight (avoid duplicates)\n    self._pending_async_keys: set[str] = set()\n    self._pending_lock = threading.Lock()\n    # In-memory cache of assembled composite pixmaps keyed by the\n    # original comma-separated src string.  Cleared on size changes.\n    self._composite_cache: dict[str, QPixmap] = {}\n    if file_cacher and async_file_cacher:\n        raise ValueError(\n            \"Only one of 'file_cacher' or 'async_file_cacher' may be \"\n            \"set, not both.\"\n        )\n    self._size = size\n    self._variant_str: str = variant.value\n    self._placeholder_icon_name = placeholder_icon\n    self._placeholder_scale = placeholder_scale\n    self._placeholder_icon_fill = placeholder_icon_fill\n    icn_size = int(size[1] * placeholder_scale)\n    self._placeholder_icon = QIcon(\n        get_icon(\n            placeholder_icon,\n            color=\"#10ffffff\",\n            fill=placeholder_icon_fill,\n        ).pixmap(\n            QSize(icn_size, icn_size),\n        )\n    )\n\n    super().__init__(QIcon(), \"\", **kwargs)\n    self.setStyle(get_ayon_style())\n\n    self._src: Path | str = \"\"\n    self._incoming_pixmap: QPixmap | None = None\n    self._opacity: float = 1.0\n    self._anim = QVariantAnimation(self)\n    self._anim.setStartValue(0.0)\n    self._anim.setEndValue(1.0)\n    self._anim.setDuration(fade_duration)\n    self._anim.setEasingCurve(QEasingCurve.Type.InOutQuad)\n    self._anim.valueChanged.connect(self._on_fade_tick)\n    self._anim.finished.connect(self._on_fade_done)\n    self._bg_color = QColor(\n        get_ayon_style()\n        .model.get_style(\"QPushButton\", variant=self._variant_str)\n        .get(\"background-color\", \"#000000\")\n    )\n\n    self.set_thumbnail(src)\n    self.setFixedSize(*self._size)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/entity_thumbnail.html#client.ayon_core.ui.components.entity_thumbnail.AYEntityThumbnail.paintEvent","title":"<code>paintEvent(arg__1)</code>","text":"<p>Override the default paint event to render the thumbnail with effects.</p> <p>Draws the styled button base using the AYON style model, then overlays the incoming pixmap with the current fade opacity for smooth transition animations.  Maintains a 1-pixel inset clip to prevent the image from overlapping the button border.</p> <p>Parameters:</p> Name Type Description Default <code>arg__1</code> <code>QPaintEvent</code> <p>The paint event containing the update region.</p> required Source code in <code>client/ayon_core/ui/components/entity_thumbnail.py</code> <pre><code>def paintEvent(self, arg__1: QPaintEvent) -&gt; None:\n    \"\"\"Override the default paint event to render the thumbnail with\n    effects.\n\n    Draws the styled button base using the AYON style model, then\n    overlays the incoming pixmap with the current fade opacity for\n    smooth transition animations.  Maintains a 1-pixel inset clip\n    to prevent the image from overlapping the button border.\n\n    Args:\n        arg__1: The paint event containing the update region.\n    \"\"\"\n    p = QPainter(self)\n    option = QStyleOptionButton()\n    self.initStyleOption(option)\n    # override rect set by stylesheet\n    size = QSize(*self._size)\n    self.setFixedSize(size)\n    option.rect = QRect(0, 0, size.width(), size.height())\n    # draw base (current icon)\n    get_ayon_style().drawControl(\n        QStyle.ControlElement.CE_PushButton, option, p, self\n    )\n    # overlay incoming pixmap with fade opacity\n    if self._incoming_pixmap and not self._incoming_pixmap.isNull():\n        dpr = self.devicePixelRatio()\n        x = (size.width() - self._incoming_pixmap.width() // dpr) // 2\n        y = (size.height() - self._incoming_pixmap.height() // dpr) // 2\n        p.save()\n        p.setClipRect(QRect(1, 1, size.width() - 2, size.height() - 2))\n        p.setOpacity(self._opacity)\n        p.fillRect(option.rect, self._bg_color)\n        p.drawPixmap(x, y, self._incoming_pixmap)\n        p.restore()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/entity_thumbnail.html#client.ayon_core.ui.components.entity_thumbnail.AYEntityThumbnail.set_fade_duration","title":"<code>set_fade_duration(duration)</code>","text":"<p>Set the duration of the fade animation when changing thumbnails.</p> <p>Parameters:</p> Name Type Description Default <code>duration</code> <code>int</code> <p>Animation duration in milliseconds.</p> required Source code in <code>client/ayon_core/ui/components/entity_thumbnail.py</code> <pre><code>def set_fade_duration(self, duration: int) -&gt; None:\n    \"\"\"Set the duration of the fade animation when changing thumbnails.\n\n    Args:\n        duration: Animation duration in milliseconds.\n    \"\"\"\n    self._anim.setDuration(duration)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/entity_thumbnail.html#client.ayon_core.ui.components.entity_thumbnail.AYEntityThumbnail.set_placeholder_icon","title":"<code>set_placeholder_icon(icon_name)</code>","text":"<p>Set the placeholder icon to show when no thumbnail is available.</p> <p>Parameters:</p> Name Type Description Default <code>icon_name</code> <code>str</code> <p>Material symbol icon name (e.g., <code>\"image\"</code>).</p> required Source code in <code>client/ayon_core/ui/components/entity_thumbnail.py</code> <pre><code>def set_placeholder_icon(self, icon_name: str) -&gt; None:\n    \"\"\"Set the placeholder icon to show when no thumbnail is available.\n\n    Args:\n        icon_name: Material symbol icon name (e.g., ``\"image\"``).\n    \"\"\"\n    if not icon_name:\n        return\n    self._placeholder_icon_name = icon_name\n    self._placeholder_icon = self._make_placeholder_icon()\n    if not self.icon() or self.icon().isNull():\n        self.setIcon(self._placeholder_icon)\n        self.setIconSize(QSize(*self._size))\n        self.update()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/entity_thumbnail.html#client.ayon_core.ui.components.entity_thumbnail.AYEntityThumbnail.set_size","title":"<code>set_size(size)</code>","text":"<p>Resize the thumbnail and update the icon size to match.</p> <p>Clears the in-memory composite cache since cached composites are size-specific.</p> <p>Parameters:</p> Name Type Description Default <code>size</code> <code>tuple[int, int]</code> <p>New <code>(width, height)</code> in pixels.</p> required Source code in <code>client/ayon_core/ui/components/entity_thumbnail.py</code> <pre><code>def set_size(self, size: tuple[int, int]) -&gt; None:\n    \"\"\"Resize the thumbnail and update the icon size to match.\n\n    Clears the in-memory composite cache since cached composites\n    are size-specific.\n\n    Args:\n        size: New ``(width, height)`` in pixels.\n    \"\"\"\n    self._size = size\n    # Cached composites are size-specific; they must be rebuilt.\n    self._composite_cache.clear()\n    self._placeholder_icon = self._make_placeholder_icon()\n    self.setFixedSize(*self._size)\n    if self.icon() and not self.icon().isNull():\n        self.setIconSize(QSize(*self._size))\n    self.update()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/entity_thumbnail.html#client.ayon_core.ui.components.entity_thumbnail.AYEntityThumbnail.set_thumbnail","title":"<code>set_thumbnail(name)</code>","text":"<p>Set the thumbnail image for the button.</p> <p>name may be a single path/cache-key or a comma-separated list of paths/cache-keys.  When multiple items are given the images are loaded, center-cropped into equal-width slots and assembled into a single composite pixmap that is cached in memory.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>Path | str</code> <p>Image source \u2014 a file path, cache key, or comma-separated combination of the two.</p> required Source code in <code>client/ayon_core/ui/components/entity_thumbnail.py</code> <pre><code>def set_thumbnail(self, name: Path | str) -&gt; None:\n    \"\"\"Set the thumbnail image for the button.\n\n    *name* may be a single path/cache-key **or** a comma-separated\n    list of paths/cache-keys.  When multiple items are given the images\n    are loaded, center-cropped into equal-width slots and assembled into\n    a single composite pixmap that is cached in memory.\n\n    Args:\n        name: Image source \u2014 a file path, cache key, or comma-separated\n            combination of the two.\n    \"\"\"\n    src_str = str(name).strip()\n    parts = [p.strip() for p in src_str.split(\",\") if p.strip()]\n\n    if len(parts) &lt;= 1:\n        resolved = self._resolve_src(name)\n        if resolved and Path(resolved).exists():\n            self._load_pixmap_from_path(str(resolved))\n        else:\n            self._src = name\n            self._anim.stop()\n            self._maybe_schedule_async_fetch(name)\n            self._incoming_pixmap = None\n            self._opacity = 1.0\n            self.setIcon(self._placeholder_icon)\n        return\n\n    # --- Multi-source composite path ---\n    self._src = src_str\n\n    # Check the in-memory composite cache before resolving.\n    if src_str in self._composite_cache:\n        self._anim.stop()\n        self._incoming_pixmap = self._composite_cache[src_str]\n        self._opacity = 0.0\n        self._anim.start()\n        return\n\n    # Resolve each source synchronously where possible.\n    resolved_paths: dict[int, str | None] = {}\n    missing_indices: list[int] = []\n    for i, part in enumerate(parts):\n        r = self._resolve_src(part)\n        if r and Path(str(r)).exists():\n            resolved_paths[i] = str(r)\n        else:\n            resolved_paths[i] = None\n            missing_indices.append(i)\n\n    non_empty_paths = [p for p in resolved_paths.values() if p]\n    sparse_key = \",\".join(non_empty_paths[: self.max_slices])\n    if sparse_key in self._composite_cache:\n        self._anim.stop()\n        self._incoming_pixmap = self._composite_cache[sparse_key]\n        self._opacity = 0.0\n        self._anim.start()\n        return\n\n    if not missing_indices:\n        # All slots ready \u2014 build and cache immediately.\n        composite = self._build_composite_pixmap(non_empty_paths)\n        self._composite_cache[sparse_key] = composite\n        self._anim.stop()\n        self._incoming_pixmap = composite\n        self._opacity = 0.0\n        self._anim.start()\n        return\n\n    # Some slots still need async fetching \u2014 show placeholder meanwhile.\n    self._anim.stop()\n    self._incoming_pixmap = None\n    self._opacity = 1.0\n    self.setIcon(self._placeholder_icon)\n\n    if not self._async_file_cacher:\n        return\n\n    pending = _PendingCompositeState(\n        resolved=resolved_paths,\n        pending_count=len(missing_indices),\n        total=len(parts),\n    )\n    thumbnail_ref = weakref.ref(self)\n\n    def _make_slot_callback(\n        slot_idx: int,\n    ) -&gt; Callable[[str], None]:\n        def _on_slot_loaded(fpath: str) -&gt; None:\n            widget = thumbnail_ref()\n            if widget is None:\n                return\n            # Discard if a newer set_thumbnail replaced this src.\n            if str(widget._src) != src_str:\n                return\n            with widget._pending_lock:\n                pending.resolved[slot_idx] = fpath or None\n                pending.pending_count -= 1\n                if pending.pending_count &gt; 0:\n                    return\n            all_paths = [\n                pending.resolved.get(j)\n                for j in range(pending.total)\n                if pending.resolved.get(j)\n            ]\n            composite = widget._build_composite_pixmap(all_paths)\n            resolved_key = \",\".join(all_paths[: widget.max_slices])\n            widget._composite_cache[resolved_key] = composite\n            widget._anim.stop()\n            widget._incoming_pixmap = composite\n            widget._opacity = 0.0\n            widget._anim.start()\n\n        return _on_slot_loaded\n\n    for idx in missing_indices:\n        self._async_file_cacher(parts[idx], _make_slot_callback(idx))\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/filter.html","title":"filter","text":"<p>AYFilter component for multi-select filtering with tags and dropdown.</p> <p>This module provides: - FilterItem: Data model for filter options - FilterCheckboxDelegate: Custom delegate for checkbox-style rendering - FilterDropdownPopup: Floating popup for filter selection - AYFilter: Base filter widget with tag bar and dropdown toggle - AYFilterByCategory: Subclass managing FilterItem data and checkbox popup</p>"},{"location":"autoapi/client/ayon_core/ui/components/filter.html#client.ayon_core.ui.components.filter.AYFilter","title":"<code>AYFilter</code>","text":"<p>               Bases: <code>AYFrame</code></p> <p>Base filter widget with tag bar and dropdown toggle.</p> <p>Provides the tag bar UI, toggle button, dropdown show/hide logic, and tag management. Subclasses supply a popup widget via <code>_create_dropdown_popup()</code> and handle tag removal via <code>_handle_tag_removed()</code>.</p> Signals <p>filter_changed: Emitted when selection changes. Passes list of                 selected keys. filter_added: Emitted when a filter is selected. Passes the key. filter_removed: Emitted when a filter is deselected. Passes the key.</p> Source code in <code>client/ayon_core/ui/components/filter.py</code> <pre><code>class AYFilter(AYFrame):\n    \"\"\"Base filter widget with tag bar and dropdown toggle.\n\n    Provides the tag bar UI, toggle button, dropdown show/hide logic,\n    and tag management. Subclasses supply a popup widget via\n    ``_create_dropdown_popup()`` and handle tag removal via\n    ``_handle_tag_removed()``.\n\n    Signals:\n        filter_changed: Emitted when selection changes. Passes list of\n                        selected keys.\n        filter_added: Emitted when a filter is selected. Passes the key.\n        filter_removed: Emitted when a filter is deselected. Passes the key.\n    \"\"\"\n\n    filter_changed = Signal(list)  # List[str] of selected keys\n    filter_added = Signal(str)  # key of added filter\n    filter_removed = Signal(str)  # key of removed filter\n\n    Variants = QFrameVariants\n\n    def __init__(\n        self,\n        parent: QtWidgets.QWidget | None = None,\n        label: str = \"Sort by\",\n        default_color: str = \"#8fceff\",\n        variant: Variants = Variants.Low,\n    ) -&gt; None:\n        \"\"\"Initialize the base filter widget.\n\n        Args:\n            parent: Optional parent widget.\n            label: Text displayed before the filter tags.\n            default_color: Default tag color when item has no color set.\n            variant: Frame variant for background styling.\n        \"\"\"\n        super().__init__(parent=parent, variant=variant, margin=0)\n\n        self._label_text = label\n        self._default_color = default_color\n        self._tags: dict[str, AYTag] = {}\n        self._dropdown_visible = False\n\n        self._init_ui()\n        self._connect_signals()\n\n        self.setStyle(get_ayon_style())\n\n    def set_label(self, text: str) -&gt; None:\n        \"\"\"Set the label text.\n\n        Args:\n            text: New label text to display.\n        \"\"\"\n        self._label_text = text\n        self._label.setText(text)\n\n    # --- Hooks for subclasses ---\n\n    def _create_dropdown_popup(\n        self,\n    ) -&gt; AYDropdownPopup | None:\n        \"\"\"Create and return the dropdown popup widget.\n\n        Subclasses override this to return a concrete popup instance.\n        The base implementation returns ``None`` (no popup).\n\n        Returns:\n            An ``AYDropdownPopup`` instance or ``None``.\n        \"\"\"\n        return None\n\n    def _handle_tag_removed(self, key: str) -&gt; None:\n        \"\"\"React to a tag dismissal.\n\n        Called when the user clicks the X on a tag. Subclasses override\n        this to update their data model (e.g. call\n        ``set_filter_selected(key, False)``).\n\n        Args:\n            key: Key of the dismissed tag.\n        \"\"\"\n\n    def get_selected_keys(self) -&gt; List[str]:\n        \"\"\"Return the list of selected filter keys.\n\n        The base implementation returns an empty list. Subclasses that\n        manage selection state must override this method.\n\n        Returns:\n            List of selected keys.\n        \"\"\"\n        return []\n\n    # --- UI Setup ---\n\n    def _init_ui(self) -&gt; None:\n        \"\"\"Build the widget layout.\"\"\"\n        main_layout = AYVBoxLayout(self, margin=0, spacing=0)\n\n        # Top bar\n        self._top_bar = AYContainer(\n            parent=self,\n            layout=AYContainer.Layout.HBox,\n            variant=AYContainer.Variants.Low_Framed_Thin,\n            layout_margin=4,\n            layout_spacing=4,\n        )\n        main_layout.addWidget(self._top_bar)\n\n        # Label\n        self._label = AYLabel(self._label_text, parent=self)\n        self._label.setContentsMargins(10, 0, 5, 0)\n        self._top_bar.add_widget(self._label)\n\n        # Tags container\n        self._tags_container = AYContainer(\n            self,\n            layout=AYContainer.Layout.HBox,\n            variant=AYContainer.Variants.Default,\n            layout_margin=0,\n            layout_spacing=4,\n        )\n        lyt = self._tags_container.layout()\n        if lyt:\n            lyt.setSizeConstraint(\n                QtWidgets.QLayout.SizeConstraint.SetFixedSize\n            )\n        self._top_bar.add_widget(self._tags_container)\n\n        # Spacer\n        self._top_bar.addStretch()\n\n        # Toggle button\n        self._toggle_btn = AYButton(\n            icon=\"keyboard_arrow_down\",\n            variant=AYButton.Variants.Nav_Small,\n            tooltip=\"Toggle filter options\",\n        )\n        self._toggle_btn.setContentsMargins(4, 4, 4, 10)\n        self._top_bar.add_widget(self._toggle_btn)\n\n        # Create floating dropdown popup via hook (not added to layout)\n        self._dropdown_popup = self._create_dropdown_popup()\n\n    def _connect_signals(self) -&gt; None:\n        \"\"\"Connect internal signals.\"\"\"\n        self._toggle_btn.clicked.connect(self._on_toggle_dropdown)\n        if self._dropdown_popup is not None:\n            self._dropdown_popup.popup_closed.connect(self._on_popup_closed)\n\n    # --- Tag management ---\n\n    def _sync_tags_from_items(self, items: List[FilterItem]) -&gt; None:\n        \"\"\"Synchronize tags with a list of filter items.\n\n        Args:\n            items: The current list of filter items.\n        \"\"\"\n        for item in items:\n            if item.selected and item.key not in self._tags:\n                self._add_tag(item)\n            elif not item.selected and item.key in self._tags:\n                self._remove_tag(item.key)\n\n    def _add_tag(self, item: FilterItem) -&gt; None:\n        \"\"\"Create and add a tag widget for a filter item.\n\n        Args:\n            item: The FilterItem to create a tag for.\n        \"\"\"\n        if item.key in self._tags:\n            return\n\n        color = QColor(item.color or self._default_color)\n        tag = AYTag(item.key, color, label=item.label)\n        tag.tag_removed.connect(self._on_tag_removed)\n        tag.tag_expanded.connect(self._on_tag_expanded)\n\n        self._tags[item.key] = tag\n        self._tags_container.add_widget(tag)\n\n    def _remove_tag(self, key: str) -&gt; None:\n        \"\"\"Remove a tag widget.\n\n        Args:\n            key: Key of the tag to remove.\n        \"\"\"\n        if key not in self._tags:\n            return\n\n        tag = self._tags.pop(key)\n        tag.setParent(None)\n        tag.deleteLater()\n\n    def _emit_filter_changed(self) -&gt; None:\n        \"\"\"Emit filter_changed signal with current selection.\"\"\"\n        self.filter_changed.emit(self.get_selected_keys())\n\n    # --- Event Handlers ---\n\n    def _on_toggle_dropdown(self) -&gt; None:\n        \"\"\"Handle toggle button click.\"\"\"\n        if self._dropdown_popup is None:\n            return\n\n        if self._dropdown_visible:\n            self._dropdown_popup.close()\n        else:\n            self._dropdown_visible = True\n            self._toggle_btn.set_icon(\"keyboard_arrow_up\")\n            self._dropdown_popup.show_below(self._top_bar)\n\n    def _on_popup_closed(self) -&gt; None:\n        \"\"\"Handle popup close event.\"\"\"\n        self._dropdown_visible = False\n        self._toggle_btn.set_icon(\"keyboard_arrow_down\")\n\n    def _on_tag_removed(self, key: str) -&gt; None:\n        \"\"\"Handle tag X button click.\n\n        Args:\n            key: Key of the removed tag.\n        \"\"\"\n        self._handle_tag_removed(key)\n\n    def _on_tag_expanded(self, key: str) -&gt; None:\n        \"\"\"Handle tag expand button click - toggle dropdown.\n\n        Args:\n            key: Key of the expanded tag.\n        \"\"\"\n        self._on_toggle_dropdown()\n\n    def mousePressEvent(self, event: QMouseEvent) -&gt; None:\n        if event.button() == Qt.MouseButton.LeftButton:\n            self._on_toggle_dropdown()\n            return\n        return super().mousePressEvent(event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/filter.html#client.ayon_core.ui.components.filter.AYFilter.__init__","title":"<code>__init__(parent=None, label='Sort by', default_color='#8fceff', variant=Variants.Low)</code>","text":"<p>Initialize the base filter widget.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <code>QWidget | None</code> <p>Optional parent widget.</p> <code>None</code> <code>label</code> <code>str</code> <p>Text displayed before the filter tags.</p> <code>'Sort by'</code> <code>default_color</code> <code>str</code> <p>Default tag color when item has no color set.</p> <code>'#8fceff'</code> <code>variant</code> <code>Variants</code> <p>Frame variant for background styling.</p> <code>Low</code> Source code in <code>client/ayon_core/ui/components/filter.py</code> <pre><code>def __init__(\n    self,\n    parent: QtWidgets.QWidget | None = None,\n    label: str = \"Sort by\",\n    default_color: str = \"#8fceff\",\n    variant: Variants = Variants.Low,\n) -&gt; None:\n    \"\"\"Initialize the base filter widget.\n\n    Args:\n        parent: Optional parent widget.\n        label: Text displayed before the filter tags.\n        default_color: Default tag color when item has no color set.\n        variant: Frame variant for background styling.\n    \"\"\"\n    super().__init__(parent=parent, variant=variant, margin=0)\n\n    self._label_text = label\n    self._default_color = default_color\n    self._tags: dict[str, AYTag] = {}\n    self._dropdown_visible = False\n\n    self._init_ui()\n    self._connect_signals()\n\n    self.setStyle(get_ayon_style())\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/filter.html#client.ayon_core.ui.components.filter.AYFilter.get_selected_keys","title":"<code>get_selected_keys()</code>","text":"<p>Return the list of selected filter keys.</p> <p>The base implementation returns an empty list. Subclasses that manage selection state must override this method.</p> <p>Returns:</p> Type Description <code>List[str]</code> <p>List of selected keys.</p> Source code in <code>client/ayon_core/ui/components/filter.py</code> <pre><code>def get_selected_keys(self) -&gt; List[str]:\n    \"\"\"Return the list of selected filter keys.\n\n    The base implementation returns an empty list. Subclasses that\n    manage selection state must override this method.\n\n    Returns:\n        List of selected keys.\n    \"\"\"\n    return []\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/filter.html#client.ayon_core.ui.components.filter.AYFilter.set_label","title":"<code>set_label(text)</code>","text":"<p>Set the label text.</p> <p>Parameters:</p> Name Type Description Default <code>text</code> <code>str</code> <p>New label text to display.</p> required Source code in <code>client/ayon_core/ui/components/filter.py</code> <pre><code>def set_label(self, text: str) -&gt; None:\n    \"\"\"Set the label text.\n\n    Args:\n        text: New label text to display.\n    \"\"\"\n    self._label_text = text\n    self._label.setText(text)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/filter.html#client.ayon_core.ui.components.filter.AYFilterByCategory","title":"<code>AYFilterByCategory</code>","text":"<p>               Bases: <code>AYFilter</code></p> <p>Category-based filter using FilterItem list and checkbox dropdown.</p> <p>Manages a list of <code>FilterItem</code> objects and populates a <code>FilterDropdownPopup</code> for the user to select/deselect items.</p> Signals <p>filter_changed: Inherited. Emitted with list of selected keys. filter_added: Inherited. Emitted when a filter is selected. filter_removed: Inherited. Emitted when a filter is deselected.</p> Source code in <code>client/ayon_core/ui/components/filter.py</code> <pre><code>class AYFilterByCategory(AYFilter):\n    \"\"\"Category-based filter using FilterItem list and checkbox dropdown.\n\n    Manages a list of ``FilterItem`` objects and populates a\n    ``FilterDropdownPopup`` for the user to select/deselect items.\n\n    Signals:\n        filter_changed: Inherited. Emitted with list of selected keys.\n        filter_added: Inherited. Emitted when a filter is selected.\n        filter_removed: Inherited. Emitted when a filter is deselected.\n    \"\"\"\n\n    def __init__(\n        self,\n        parent: QtWidgets.QWidget | None = None,\n        label: str = \"Sort by\",\n        items: List[FilterItem] | None = None,\n        default_color: str = \"#8fceff\",\n        variant: AYFilter.Variants = AYFilter.Variants.Low,\n    ) -&gt; None:\n        \"\"\"Initialize the category filter widget.\n\n        Args:\n            parent: Optional parent widget.\n            label: Text displayed before the filter tags.\n            items: Initial list of filter items.\n            default_color: Default tag color when item has no color set.\n            variant: Frame variant for background styling.\n        \"\"\"\n        self._items: List[FilterItem] = list(items) if items else []\n        super().__init__(\n            parent,\n            label=label,\n            default_color=default_color,\n            variant=variant,\n        )\n        self._connect_category_signals()\n        self._sync_tags_from_items(self._items)\n\n    # --- Hooks ---\n\n    def _create_dropdown_popup(self) -&gt; FilterDropdownPopup:\n        \"\"\"Create and populate the category checkbox popup.\n\n        Returns:\n            A populated ``FilterDropdownPopup`` instance.\n        \"\"\"\n        popup = FilterDropdownPopup()\n        popup.populate(self._items)\n        return popup\n\n    def _handle_tag_removed(self, key: str) -&gt; None:\n        \"\"\"Deselect the filter item when its tag is dismissed.\n\n        Args:\n            key: Key of the dismissed tag.\n        \"\"\"\n        self.set_filter_selected(key, False)\n\n    # --- Additional signal wiring ---\n\n    def _connect_category_signals(self) -&gt; None:\n        \"\"\"Connect category-specific signals after popup is created.\"\"\"\n        popup = self._dropdown_popup\n        if isinstance(popup, FilterDropdownPopup):\n            popup.item_toggled.connect(self._on_popup_item_toggled)\n\n    # --- Public API ---\n\n    def add_filter(self, item: FilterItem) -&gt; None:\n        \"\"\"Add a new filter option.\n\n        Args:\n            item: The FilterItem to add.\n        \"\"\"\n        if item not in self._items:\n            self._items.append(item)\n            self._repopulate_popup()\n            if item.selected:\n                self._add_tag(item)\n\n    def remove_filter(self, key: str) -&gt; None:\n        \"\"\"Remove a filter option by key.\n\n        Args:\n            key: Unique identifier of the filter to remove.\n        \"\"\"\n        for i, item in enumerate(self._items):\n            if item.key == key:\n                self._items.pop(i)\n                self._remove_tag(key)\n                self._repopulate_popup()\n                break\n\n    def set_filter_selected(self, key: str, selected: bool) -&gt; None:\n        \"\"\"Set selection state of a filter.\n\n        Args:\n            key: Unique identifier of the filter.\n            selected: New selection state.\n        \"\"\"\n        for item in self._items:\n            if item.key == key:\n                if item.selected != selected:\n                    item.selected = selected\n                    self._update_popup_item_state(key, selected)\n                    if selected:\n                        self._add_tag(item)\n                        self.filter_added.emit(key)\n                    else:\n                        self._remove_tag(key)\n                        self.filter_removed.emit(key)\n                    self._emit_filter_changed()\n                break\n\n    def get_selected_keys(self) -&gt; List[str]:\n        \"\"\"Get list of selected filter keys.\n\n        Returns:\n            List of keys for currently selected filters.\n        \"\"\"\n        return [item.key for item in self._items if item.selected]\n\n    def get_selected_items(self) -&gt; List[FilterItem]:\n        \"\"\"Get list of selected filter items.\n\n        Returns:\n            List of FilterItem objects that are selected.\n        \"\"\"\n        return [item for item in self._items if item.selected]\n\n    def clear_selection(self) -&gt; None:\n        \"\"\"Deselect all filters.\"\"\"\n        for item in self._items:\n            if item.selected:\n                self.set_filter_selected(item.key, False)\n\n    def select_all(self) -&gt; None:\n        \"\"\"Select all filters.\"\"\"\n        for item in self._items:\n            if not item.selected:\n                self.set_filter_selected(item.key, True)\n\n    # --- Private Methods ---\n\n    def _repopulate_popup(self) -&gt; None:\n        \"\"\"Repopulate the popup with current filter items.\"\"\"\n        popup = self._dropdown_popup\n        if isinstance(popup, FilterDropdownPopup):\n            popup.populate(self._items)\n\n    def _update_popup_item_state(self, key: str, selected: bool) -&gt; None:\n        \"\"\"Update checkbox state of a popup item.\n\n        Args:\n            key: Key of the item to update.\n            selected: New selection state.\n        \"\"\"\n        popup = self._dropdown_popup\n        if isinstance(popup, FilterDropdownPopup):\n            popup.update_item_state(key, selected)\n\n    # --- Event Handlers ---\n\n    def _on_popup_item_toggled(self, key: str, selected: bool) -&gt; None:\n        \"\"\"Handle popup item toggle.\n\n        Args:\n            key: Key of the toggled filter.\n            selected: New selection state.\n        \"\"\"\n        self.set_filter_selected(key, selected)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/filter.html#client.ayon_core.ui.components.filter.AYFilterByCategory.__init__","title":"<code>__init__(parent=None, label='Sort by', items=None, default_color='#8fceff', variant=AYFilter.Variants.Low)</code>","text":"<p>Initialize the category filter widget.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <code>QWidget | None</code> <p>Optional parent widget.</p> <code>None</code> <code>label</code> <code>str</code> <p>Text displayed before the filter tags.</p> <code>'Sort by'</code> <code>items</code> <code>List[FilterItem] | None</code> <p>Initial list of filter items.</p> <code>None</code> <code>default_color</code> <code>str</code> <p>Default tag color when item has no color set.</p> <code>'#8fceff'</code> <code>variant</code> <code>Variants</code> <p>Frame variant for background styling.</p> <code>Low</code> Source code in <code>client/ayon_core/ui/components/filter.py</code> <pre><code>def __init__(\n    self,\n    parent: QtWidgets.QWidget | None = None,\n    label: str = \"Sort by\",\n    items: List[FilterItem] | None = None,\n    default_color: str = \"#8fceff\",\n    variant: AYFilter.Variants = AYFilter.Variants.Low,\n) -&gt; None:\n    \"\"\"Initialize the category filter widget.\n\n    Args:\n        parent: Optional parent widget.\n        label: Text displayed before the filter tags.\n        items: Initial list of filter items.\n        default_color: Default tag color when item has no color set.\n        variant: Frame variant for background styling.\n    \"\"\"\n    self._items: List[FilterItem] = list(items) if items else []\n    super().__init__(\n        parent,\n        label=label,\n        default_color=default_color,\n        variant=variant,\n    )\n    self._connect_category_signals()\n    self._sync_tags_from_items(self._items)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/filter.html#client.ayon_core.ui.components.filter.AYFilterByCategory.add_filter","title":"<code>add_filter(item)</code>","text":"<p>Add a new filter option.</p> <p>Parameters:</p> Name Type Description Default <code>item</code> <code>FilterItem</code> <p>The FilterItem to add.</p> required Source code in <code>client/ayon_core/ui/components/filter.py</code> <pre><code>def add_filter(self, item: FilterItem) -&gt; None:\n    \"\"\"Add a new filter option.\n\n    Args:\n        item: The FilterItem to add.\n    \"\"\"\n    if item not in self._items:\n        self._items.append(item)\n        self._repopulate_popup()\n        if item.selected:\n            self._add_tag(item)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/filter.html#client.ayon_core.ui.components.filter.AYFilterByCategory.clear_selection","title":"<code>clear_selection()</code>","text":"<p>Deselect all filters.</p> Source code in <code>client/ayon_core/ui/components/filter.py</code> <pre><code>def clear_selection(self) -&gt; None:\n    \"\"\"Deselect all filters.\"\"\"\n    for item in self._items:\n        if item.selected:\n            self.set_filter_selected(item.key, False)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/filter.html#client.ayon_core.ui.components.filter.AYFilterByCategory.get_selected_items","title":"<code>get_selected_items()</code>","text":"<p>Get list of selected filter items.</p> <p>Returns:</p> Type Description <code>List[FilterItem]</code> <p>List of FilterItem objects that are selected.</p> Source code in <code>client/ayon_core/ui/components/filter.py</code> <pre><code>def get_selected_items(self) -&gt; List[FilterItem]:\n    \"\"\"Get list of selected filter items.\n\n    Returns:\n        List of FilterItem objects that are selected.\n    \"\"\"\n    return [item for item in self._items if item.selected]\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/filter.html#client.ayon_core.ui.components.filter.AYFilterByCategory.get_selected_keys","title":"<code>get_selected_keys()</code>","text":"<p>Get list of selected filter keys.</p> <p>Returns:</p> Type Description <code>List[str]</code> <p>List of keys for currently selected filters.</p> Source code in <code>client/ayon_core/ui/components/filter.py</code> <pre><code>def get_selected_keys(self) -&gt; List[str]:\n    \"\"\"Get list of selected filter keys.\n\n    Returns:\n        List of keys for currently selected filters.\n    \"\"\"\n    return [item.key for item in self._items if item.selected]\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/filter.html#client.ayon_core.ui.components.filter.AYFilterByCategory.remove_filter","title":"<code>remove_filter(key)</code>","text":"<p>Remove a filter option by key.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>Unique identifier of the filter to remove.</p> required Source code in <code>client/ayon_core/ui/components/filter.py</code> <pre><code>def remove_filter(self, key: str) -&gt; None:\n    \"\"\"Remove a filter option by key.\n\n    Args:\n        key: Unique identifier of the filter to remove.\n    \"\"\"\n    for i, item in enumerate(self._items):\n        if item.key == key:\n            self._items.pop(i)\n            self._remove_tag(key)\n            self._repopulate_popup()\n            break\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/filter.html#client.ayon_core.ui.components.filter.AYFilterByCategory.select_all","title":"<code>select_all()</code>","text":"<p>Select all filters.</p> Source code in <code>client/ayon_core/ui/components/filter.py</code> <pre><code>def select_all(self) -&gt; None:\n    \"\"\"Select all filters.\"\"\"\n    for item in self._items:\n        if not item.selected:\n            self.set_filter_selected(item.key, True)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/filter.html#client.ayon_core.ui.components.filter.AYFilterByCategory.set_filter_selected","title":"<code>set_filter_selected(key, selected)</code>","text":"<p>Set selection state of a filter.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>Unique identifier of the filter.</p> required <code>selected</code> <code>bool</code> <p>New selection state.</p> required Source code in <code>client/ayon_core/ui/components/filter.py</code> <pre><code>def set_filter_selected(self, key: str, selected: bool) -&gt; None:\n    \"\"\"Set selection state of a filter.\n\n    Args:\n        key: Unique identifier of the filter.\n        selected: New selection state.\n    \"\"\"\n    for item in self._items:\n        if item.key == key:\n            if item.selected != selected:\n                item.selected = selected\n                self._update_popup_item_state(key, selected)\n                if selected:\n                    self._add_tag(item)\n                    self.filter_added.emit(key)\n                else:\n                    self._remove_tag(key)\n                    self.filter_removed.emit(key)\n                self._emit_filter_changed()\n            break\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/filter.html#client.ayon_core.ui.components.filter.FilterCheckboxDelegate","title":"<code>FilterCheckboxDelegate</code>","text":"<p>               Bases: <code>QStyledItemDelegate</code></p> <p>Delegate for rendering filter items with checkbox indicators.</p> <p>Draws a small square checkbox indicator on the right side of each item. Checked items display an X icon inside the checkbox.</p> <p>Attributes:</p> Name Type Description <code>CHECKBOX_SIZE</code> <p>Size of the checkbox indicator in pixels.</p> <code>CHECKBOX_MARGIN</code> <p>Margin around the checkbox.</p> <code>TEXT_PADDING</code> <p>Padding for the text from the left edge.</p> Source code in <code>client/ayon_core/ui/components/filter.py</code> <pre><code>class FilterCheckboxDelegate(QtWidgets.QStyledItemDelegate):\n    \"\"\"Delegate for rendering filter items with checkbox indicators.\n\n    Draws a small square checkbox indicator on the right side of each item.\n    Checked items display an X icon inside the checkbox.\n\n    Attributes:\n        CHECKBOX_SIZE: Size of the checkbox indicator in pixels.\n        CHECKBOX_MARGIN: Margin around the checkbox.\n        TEXT_PADDING: Padding for the text from the left edge.\n    \"\"\"\n\n    CHECKBOX_SIZE = 16\n    CHECKBOX_MARGIN = 8\n    TEXT_PADDING = 12\n\n    Variants = QStyledItemDelegateVariants\n\n    def __init__(\n        self,\n        parent: QtWidgets.QWidget | None = None,\n        variant: Variants = Variants.Default,\n    ) -&gt; None:\n        \"\"\"Initialize the delegate.\n\n        Args:\n            parent: Optional parent widget.\n            variant: Style variant for the delegate.\n        \"\"\"\n        super().__init__(parent)\n        self._variant_str = variant.value\n\n    def paint(\n        self,\n        painter: QPainter,\n        option: QStyleOptionViewItem,\n        index: QModelIndex | QPersistentModelIndex,\n    ) -&gt; None:\n        \"\"\"Paint the filter item with checkbox indicator.\n\n        Args:\n            painter: QPainter instance for rendering.\n            option: Style options for the item.\n            index: Model index of the item.\n        \"\"\"\n        # Update option with model data for the drawer\n        option.text = index.data(Qt.ItemDataRole.DisplayRole) or \"\"\n        check_state = index.data(Qt.ItemDataRole.CheckStateRole)\n        option.checkState = (\n            Qt.CheckState.Checked\n            if check_state == Qt.CheckState.Checked\n            else Qt.CheckState.Unchecked\n        )\n\n        # Delegate painting to AYONStyle\n        parent = self.parent()\n        widget = parent if isinstance(parent, QtWidgets.QWidget) else None\n        get_ayon_style().drawControl(\n            QStyle.ControlElement.CE_ItemViewItem,\n            option,\n            painter,\n            widget,\n        )\n\n    def sizeHint(\n        self,\n        option: QStyleOptionViewItem,\n        index: QModelIndex | QPersistentModelIndex,\n    ) -&gt; QtCore.QSize:\n        \"\"\"Return size hint for the item.\n\n        Args:\n            option: Style options for the item.\n            index: Model index of the item.\n\n        Returns:\n            Recommended size for the item.\n        \"\"\"\n        base = super().sizeHint(option, index)\n        return QtCore.QSize(base.width(), max(base.height(), 36))\n\n    def editorEvent(\n        self,\n        event: QtCore.QEvent,\n        model: QtCore.QAbstractItemModel,\n        option: QStyleOptionViewItem,\n        index: QModelIndex | QPersistentModelIndex,\n    ) -&gt; bool:\n        \"\"\"Handle click events to toggle checkbox state.\n\n        Args:\n            event: The input event.\n            model: The data model.\n            option: Style options for the item.\n            index: Model index of the item.\n\n        Returns:\n            True if the event was handled.\n        \"\"\"\n        if event.type() == QtCore.QEvent.Type.MouseButtonRelease:\n            current = index.data(Qt.ItemDataRole.CheckStateRole)\n            new_state = (\n                Qt.CheckState.Unchecked\n                if current == Qt.CheckState.Checked\n                else Qt.CheckState.Checked\n            )\n            model.setData(index, new_state, Qt.ItemDataRole.CheckStateRole)\n            return True\n        return super().editorEvent(event, model, option, index)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/filter.html#client.ayon_core.ui.components.filter.FilterCheckboxDelegate.__init__","title":"<code>__init__(parent=None, variant=Variants.Default)</code>","text":"<p>Initialize the delegate.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <code>QWidget | None</code> <p>Optional parent widget.</p> <code>None</code> <code>variant</code> <code>Variants</code> <p>Style variant for the delegate.</p> <code>Default</code> Source code in <code>client/ayon_core/ui/components/filter.py</code> <pre><code>def __init__(\n    self,\n    parent: QtWidgets.QWidget | None = None,\n    variant: Variants = Variants.Default,\n) -&gt; None:\n    \"\"\"Initialize the delegate.\n\n    Args:\n        parent: Optional parent widget.\n        variant: Style variant for the delegate.\n    \"\"\"\n    super().__init__(parent)\n    self._variant_str = variant.value\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/filter.html#client.ayon_core.ui.components.filter.FilterCheckboxDelegate.editorEvent","title":"<code>editorEvent(event, model, option, index)</code>","text":"<p>Handle click events to toggle checkbox state.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <code>QEvent</code> <p>The input event.</p> required <code>model</code> <code>QAbstractItemModel</code> <p>The data model.</p> required <code>option</code> <code>QStyleOptionViewItem</code> <p>Style options for the item.</p> required <code>index</code> <code>QModelIndex | QPersistentModelIndex</code> <p>Model index of the item.</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if the event was handled.</p> Source code in <code>client/ayon_core/ui/components/filter.py</code> <pre><code>def editorEvent(\n    self,\n    event: QtCore.QEvent,\n    model: QtCore.QAbstractItemModel,\n    option: QStyleOptionViewItem,\n    index: QModelIndex | QPersistentModelIndex,\n) -&gt; bool:\n    \"\"\"Handle click events to toggle checkbox state.\n\n    Args:\n        event: The input event.\n        model: The data model.\n        option: Style options for the item.\n        index: Model index of the item.\n\n    Returns:\n        True if the event was handled.\n    \"\"\"\n    if event.type() == QtCore.QEvent.Type.MouseButtonRelease:\n        current = index.data(Qt.ItemDataRole.CheckStateRole)\n        new_state = (\n            Qt.CheckState.Unchecked\n            if current == Qt.CheckState.Checked\n            else Qt.CheckState.Checked\n        )\n        model.setData(index, new_state, Qt.ItemDataRole.CheckStateRole)\n        return True\n    return super().editorEvent(event, model, option, index)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/filter.html#client.ayon_core.ui.components.filter.FilterCheckboxDelegate.paint","title":"<code>paint(painter, option, index)</code>","text":"<p>Paint the filter item with checkbox indicator.</p> <p>Parameters:</p> Name Type Description Default <code>painter</code> <code>QPainter</code> <p>QPainter instance for rendering.</p> required <code>option</code> <code>QStyleOptionViewItem</code> <p>Style options for the item.</p> required <code>index</code> <code>QModelIndex | QPersistentModelIndex</code> <p>Model index of the item.</p> required Source code in <code>client/ayon_core/ui/components/filter.py</code> <pre><code>def paint(\n    self,\n    painter: QPainter,\n    option: QStyleOptionViewItem,\n    index: QModelIndex | QPersistentModelIndex,\n) -&gt; None:\n    \"\"\"Paint the filter item with checkbox indicator.\n\n    Args:\n        painter: QPainter instance for rendering.\n        option: Style options for the item.\n        index: Model index of the item.\n    \"\"\"\n    # Update option with model data for the drawer\n    option.text = index.data(Qt.ItemDataRole.DisplayRole) or \"\"\n    check_state = index.data(Qt.ItemDataRole.CheckStateRole)\n    option.checkState = (\n        Qt.CheckState.Checked\n        if check_state == Qt.CheckState.Checked\n        else Qt.CheckState.Unchecked\n    )\n\n    # Delegate painting to AYONStyle\n    parent = self.parent()\n    widget = parent if isinstance(parent, QtWidgets.QWidget) else None\n    get_ayon_style().drawControl(\n        QStyle.ControlElement.CE_ItemViewItem,\n        option,\n        painter,\n        widget,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/filter.html#client.ayon_core.ui.components.filter.FilterCheckboxDelegate.sizeHint","title":"<code>sizeHint(option, index)</code>","text":"<p>Return size hint for the item.</p> <p>Parameters:</p> Name Type Description Default <code>option</code> <code>QStyleOptionViewItem</code> <p>Style options for the item.</p> required <code>index</code> <code>QModelIndex | QPersistentModelIndex</code> <p>Model index of the item.</p> required <p>Returns:</p> Type Description <code>QSize</code> <p>Recommended size for the item.</p> Source code in <code>client/ayon_core/ui/components/filter.py</code> <pre><code>def sizeHint(\n    self,\n    option: QStyleOptionViewItem,\n    index: QModelIndex | QPersistentModelIndex,\n) -&gt; QtCore.QSize:\n    \"\"\"Return size hint for the item.\n\n    Args:\n        option: Style options for the item.\n        index: Model index of the item.\n\n    Returns:\n        Recommended size for the item.\n    \"\"\"\n    base = super().sizeHint(option, index)\n    return QtCore.QSize(base.width(), max(base.height(), 36))\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/filter.html#client.ayon_core.ui.components.filter.FilterDropdownPopup","title":"<code>FilterDropdownPopup</code>","text":"<p>               Bases: <code>AYDropdownPopup</code></p> <p>Floating popup widget for filter selection.</p> <p>This popup appears below the toggle button and contains a list of filter items with checkboxes. It closes when clicking outside, pressing Escape, or when explicitly closed.</p> Signals <p>item_toggled: Emitted when a filter item is toggled.               Passes (key: str, selected: bool). popup_closed: Inherited from <code>AYDropdownPopup</code>. Emitted when     the popup is closed.</p> Source code in <code>client/ayon_core/ui/components/filter.py</code> <pre><code>class FilterDropdownPopup(AYDropdownPopup):\n    \"\"\"Floating popup widget for filter selection.\n\n    This popup appears below the toggle button and contains a list of\n    filter items with checkboxes. It closes when clicking outside,\n    pressing Escape, or when explicitly closed.\n\n    Signals:\n        item_toggled: Emitted when a filter item is toggled.\n                      Passes (key: str, selected: bool).\n        popup_closed: Inherited from ``AYDropdownPopup``. Emitted when\n            the popup is closed.\n    \"\"\"\n\n    item_toggled = Signal(str, bool)  # (key, selected)\n\n    # Styling constants\n    POPUP_MIN_WIDTH = 180\n    POPUP_MAX_HEIGHT = 300\n\n    def __init__(\n        self,\n        parent: QtWidgets.QWidget | None = None,\n    ) -&gt; None:\n        \"\"\"Initialize the dropdown popup.\n\n        Args:\n            parent: Optional parent widget.\n        \"\"\"\n        super().__init__(parent, variant=AYDropdownPopup.Variants.Low)\n\n        self._init_ui()\n\n    def _init_ui(self) -&gt; None:\n        \"\"\"Build the popup layout.\"\"\"\n        layout = AYVBoxLayout(self, margin=4, spacing=0)\n\n        # List widget for filter items\n        self._list_widget = QtWidgets.QListWidget(self)\n        self._list_widget.setSelectionMode(\n            QtWidgets.QAbstractItemView.SelectionMode.NoSelection\n        )\n        self._list_widget.setItemDelegate(\n            FilterCheckboxDelegate(self._list_widget)\n        )\n        self._list_widget.setMouseTracking(True)\n        self._list_widget.setHorizontalScrollBarPolicy(\n            Qt.ScrollBarPolicy.ScrollBarAlwaysOff\n        )\n        self._list_widget.setVerticalScrollBarPolicy(\n            Qt.ScrollBarPolicy.ScrollBarAsNeeded\n        )\n\n        self._list_widget.clicked.connect(self._on_item_clicked)\n\n        layout.addWidget(self._list_widget)\n\n        # Set size constraints\n        self.setMinimumWidth(self.POPUP_MIN_WIDTH)\n        self.setMaximumHeight(self.POPUP_MAX_HEIGHT)\n\n    def populate(self, items: List[FilterItem]) -&gt; None:\n        \"\"\"Populate the list with filter items.\n\n        Args:\n            items: List of FilterItem objects to display.\n        \"\"\"\n        self._list_widget.clear()\n        for item in items:\n            self._add_list_item(item)\n\n        # Adjust height based on content\n        self._adjust_size()\n\n    def _add_list_item(self, item: FilterItem) -&gt; None:\n        \"\"\"Add a filter item to the list widget.\n\n        Args:\n            item: The FilterItem to add to the list.\n        \"\"\"\n        list_item = QtWidgets.QListWidgetItem(item.label)\n        list_item.setData(QtCore.Qt.ItemDataRole.UserRole, item.key)\n        list_item.setData(\n            QtCore.Qt.ItemDataRole.CheckStateRole,\n            QtCore.Qt.CheckState.Checked\n            if item.selected\n            else QtCore.Qt.CheckState.Unchecked,\n        )\n        list_item.setFlags(\n            list_item.flags() | QtCore.Qt.ItemFlag.ItemIsUserCheckable\n        )\n        self._list_widget.addItem(list_item)\n\n    def _adjust_size(self) -&gt; None:\n        \"\"\"Adjust popup size based on content.\"\"\"\n        item_count = self._list_widget.count()\n        if item_count == 0:\n            return\n\n        # Calculate height based on items\n        row_height = 36  # From delegate sizeHint\n        content_height = item_count * row_height + 8  # 8 for margins\n        height = min(content_height, self.POPUP_MAX_HEIGHT)\n        self.setFixedHeight(height)\n\n    def update_item_state(self, key: str, selected: bool) -&gt; None:\n        \"\"\"Update the check state of a specific item.\n\n        Args:\n            key: Key of the item to update.\n            selected: New selection state.\n        \"\"\"\n        for i in range(self._list_widget.count()):\n            item = self._list_widget.item(i)\n            if item and item.data(QtCore.Qt.ItemDataRole.UserRole) == key:\n                item.setData(\n                    QtCore.Qt.ItemDataRole.CheckStateRole,\n                    QtCore.Qt.CheckState.Checked\n                    if selected\n                    else QtCore.Qt.CheckState.Unchecked,\n                )\n                break\n\n    def _on_item_clicked(self, index: QtCore.QModelIndex) -&gt; None:\n        \"\"\"Handle list item click - emit toggle signal.\n\n        Args:\n            index: Model index of the clicked item.\n        \"\"\"\n        item = self._list_widget.item(index.row())\n        if not item:\n            return\n\n        key = item.data(QtCore.Qt.ItemDataRole.UserRole)\n        current_state = item.data(QtCore.Qt.ItemDataRole.CheckStateRole)\n        new_selected = current_state == QtCore.Qt.CheckState.Checked\n\n        self.item_toggled.emit(key, new_selected)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/filter.html#client.ayon_core.ui.components.filter.FilterDropdownPopup.__init__","title":"<code>__init__(parent=None)</code>","text":"<p>Initialize the dropdown popup.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <code>QWidget | None</code> <p>Optional parent widget.</p> <code>None</code> Source code in <code>client/ayon_core/ui/components/filter.py</code> <pre><code>def __init__(\n    self,\n    parent: QtWidgets.QWidget | None = None,\n) -&gt; None:\n    \"\"\"Initialize the dropdown popup.\n\n    Args:\n        parent: Optional parent widget.\n    \"\"\"\n    super().__init__(parent, variant=AYDropdownPopup.Variants.Low)\n\n    self._init_ui()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/filter.html#client.ayon_core.ui.components.filter.FilterDropdownPopup.populate","title":"<code>populate(items)</code>","text":"<p>Populate the list with filter items.</p> <p>Parameters:</p> Name Type Description Default <code>items</code> <code>List[FilterItem]</code> <p>List of FilterItem objects to display.</p> required Source code in <code>client/ayon_core/ui/components/filter.py</code> <pre><code>def populate(self, items: List[FilterItem]) -&gt; None:\n    \"\"\"Populate the list with filter items.\n\n    Args:\n        items: List of FilterItem objects to display.\n    \"\"\"\n    self._list_widget.clear()\n    for item in items:\n        self._add_list_item(item)\n\n    # Adjust height based on content\n    self._adjust_size()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/filter.html#client.ayon_core.ui.components.filter.FilterDropdownPopup.update_item_state","title":"<code>update_item_state(key, selected)</code>","text":"<p>Update the check state of a specific item.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>Key of the item to update.</p> required <code>selected</code> <code>bool</code> <p>New selection state.</p> required Source code in <code>client/ayon_core/ui/components/filter.py</code> <pre><code>def update_item_state(self, key: str, selected: bool) -&gt; None:\n    \"\"\"Update the check state of a specific item.\n\n    Args:\n        key: Key of the item to update.\n        selected: New selection state.\n    \"\"\"\n    for i in range(self._list_widget.count()):\n        item = self._list_widget.item(i)\n        if item and item.data(QtCore.Qt.ItemDataRole.UserRole) == key:\n            item.setData(\n                QtCore.Qt.ItemDataRole.CheckStateRole,\n                QtCore.Qt.CheckState.Checked\n                if selected\n                else QtCore.Qt.CheckState.Unchecked,\n            )\n            break\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/filter.html#client.ayon_core.ui.components.filter.FilterItem","title":"<code>FilterItem</code>  <code>dataclass</code>","text":"<p>Data model for a single filter option.</p> <p>Attributes:</p> Name Type Description <code>key</code> <code>str</code> <p>Unique identifier for the filter.</p> <code>label</code> <code>str</code> <p>Display text shown in dropdown and tags.</p> <code>selected</code> <code>bool</code> <p>Current selection state.</p> <code>color</code> <code>str | None</code> <p>Optional background color for tag display.</p> <code>icon</code> <code>str | None</code> <p>Optional material icon name.</p> <code>enabled</code> <code>bool</code> <p>Whether the filter can be toggled.</p> Source code in <code>client/ayon_core/ui/components/filter.py</code> <pre><code>@dataclass\nclass FilterItem:\n    \"\"\"Data model for a single filter option.\n\n    Attributes:\n        key: Unique identifier for the filter.\n        label: Display text shown in dropdown and tags.\n        selected: Current selection state.\n        color: Optional background color for tag display.\n        icon: Optional material icon name.\n        enabled: Whether the filter can be toggled.\n    \"\"\"\n\n    key: str\n    label: str\n    selected: bool = False\n    color: str | None = field(default=None)\n    icon: str | None = field(default=None)\n    enabled: bool = True\n\n    def __hash__(self) -&gt; int:\n        \"\"\"Return hash based on key.\"\"\"\n        return hash(self.key)\n\n    def __eq__(self, other: object) -&gt; bool:\n        \"\"\"Compare equality based on key.\"\"\"\n        if isinstance(other, FilterItem):\n            return self.key == other.key\n        return False\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/filter.html#client.ayon_core.ui.components.filter.FilterItem.__eq__","title":"<code>__eq__(other)</code>","text":"<p>Compare equality based on key.</p> Source code in <code>client/ayon_core/ui/components/filter.py</code> <pre><code>def __eq__(self, other: object) -&gt; bool:\n    \"\"\"Compare equality based on key.\"\"\"\n    if isinstance(other, FilterItem):\n        return self.key == other.key\n    return False\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/filter.html#client.ayon_core.ui.components.filter.FilterItem.__hash__","title":"<code>__hash__()</code>","text":"<p>Return hash based on key.</p> Source code in <code>client/ayon_core/ui/components/filter.py</code> <pre><code>def __hash__(self) -&gt; int:\n    \"\"\"Return hash based on key.\"\"\"\n    return hash(self.key)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/filterable_list.html","title":"filterable_list","text":"<p>A widget with a search field and a scrollable, filterable list.</p>"},{"location":"autoapi/client/ayon_core/ui/components/filterable_list.html#client.ayon_core.ui.components.filterable_list.FilterableList","title":"<code>FilterableList</code>","text":"<p>               Bases: <code>StyleMixin</code>, <code>QWidget</code></p> <p>A widget with a search field and a scrollable, filterable list.</p> <p>Items are added as QWidget instances. Each item is matched against the search text using a caller-supplied key function.</p> <p>Attributes:</p> Name Type Description <code>search_placeholder</code> <p>Placeholder text shown in the search field.</p> Source code in <code>client/ayon_core/ui/components/filterable_list.py</code> <pre><code>class FilterableList(StyleMixin, QWidget):\n    \"\"\"A widget with a search field and a scrollable, filterable list.\n\n    Items are added as QWidget instances. Each item is matched against the\n    search text using a caller-supplied key function.\n\n    Attributes:\n        search_placeholder: Placeholder text shown in the search field.\n    \"\"\"\n\n    def __init__(\n        self,\n        placeholder: str = \"Search...\",\n        parent: QWidget | None = None,\n    ):\n        \"\"\"Initialize the filterable list.\n\n        Args:\n            placeholder: Placeholder text for the search input.\n            parent: Optional parent widget.\n        \"\"\"\n        super().__init__(parent)\n        self._items: list[tuple[QWidget, Callable[[str], bool]]] = []\n        self._placeholder = placeholder\n        self._build()\n\n    def _build(self) -&gt; None:\n        \"\"\"Build the internal UI layout.\"\"\"\n        layout = AYVBoxLayout(self, margin=0, spacing=0)\n\n        search_container = AYContainer(\n            layout=AYContainer.Layout.HBox,\n            variant=AYFrame.Variants.Low_Square,\n            layout_margin=6,\n            layout_spacing=6,\n        )\n        self.search_icon = AYLabel(\n            icon=\"search\",\n            icon_size=18,\n            icon_color=\"#ffffff\",\n            variant=AYLabel.Variants.Default,\n        )\n        search_container.add_widget(self.search_icon)\n\n        self._search_field = AYLineEdit()\n        self._search_field.setPlaceholderText(self._placeholder)\n        self._search_field.textChanged.connect(self._on_search_changed)\n        search_container.add_widget(self._search_field)\n        layout.addWidget(search_container)\n\n        scroll_area = AYScrollArea()\n        scroll_area.setWidgetResizable(True)\n        scroll_area.setHorizontalScrollBarPolicy(\n            Qt.ScrollBarPolicy.ScrollBarAlwaysOff\n        )\n        scroll_area.setVerticalScrollBarPolicy(\n            Qt.ScrollBarPolicy.ScrollBarAsNeeded\n        )\n        scroll_area.setFrameShape(QFrame.Shape.NoFrame)\n\n        self._items_container = AYFrame(variant=AYFrame.Variants.Low)\n        self._items_layout = AYVBoxLayout(margin=0, spacing=0)\n        self._items_layout.setAlignment(Qt.AlignTop)\n        self._items_container.setLayout(self._items_layout)\n        scroll_area.setWidget(self._items_container)\n        layout.addWidget(scroll_area)\n\n    def add_item(\n        self,\n        widget: QWidget,\n        match_fn: Callable[[str], bool] | None = None,\n    ) -&gt; None:\n        \"\"\"Add a widget to the filterable list.\n\n        Args:\n            widget: The widget to add.\n            match_fn: A callable that receives the current search text and\n                returns True if the widget should be visible. When omitted\n                the item is always visible.\n        \"\"\"\n        if match_fn is None:\n            match_fn = lambda _: True  # noqa: E731\n        self._items.append((widget, match_fn))\n        self._items_layout.addWidget(widget)\n\n    def add_stretch(self) -&gt; None:\n        \"\"\"Add a stretch spacer at the end of the list.\"\"\"\n        self._items_layout.addStretch()\n\n    def clear_items(self) -&gt; None:\n        \"\"\"Remove all items from the list.\"\"\"\n        self._items.clear()\n        self._items_layout.clear()\n\n    def search_field(self) -&gt; AYLineEdit:\n        \"\"\"Return the search field widget.\n\n        Returns:\n            The internal AYLineEdit used for filtering.\n        \"\"\"\n        return self._search_field\n\n    def visible_items(self) -&gt; list[QWidget]:\n        \"\"\"Return currently visible item widgets.\n\n        Returns:\n            List of visible QWidget instances.\n        \"\"\"\n        return [w for w, _ in self._items if w.isVisible()]\n\n    def _on_search_changed(self, text: str) -&gt; None:\n        \"\"\"Filter items based on search text.\n\n        Args:\n            text: Current search text.\n        \"\"\"\n        for widget, match_fn in self._items:\n            widget.setVisible(match_fn(text))\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/filterable_list.html#client.ayon_core.ui.components.filterable_list.FilterableList.__init__","title":"<code>__init__(placeholder='Search...', parent=None)</code>","text":"<p>Initialize the filterable list.</p> <p>Parameters:</p> Name Type Description Default <code>placeholder</code> <code>str</code> <p>Placeholder text for the search input.</p> <code>'Search...'</code> <code>parent</code> <code>QWidget | None</code> <p>Optional parent widget.</p> <code>None</code> Source code in <code>client/ayon_core/ui/components/filterable_list.py</code> <pre><code>def __init__(\n    self,\n    placeholder: str = \"Search...\",\n    parent: QWidget | None = None,\n):\n    \"\"\"Initialize the filterable list.\n\n    Args:\n        placeholder: Placeholder text for the search input.\n        parent: Optional parent widget.\n    \"\"\"\n    super().__init__(parent)\n    self._items: list[tuple[QWidget, Callable[[str], bool]]] = []\n    self._placeholder = placeholder\n    self._build()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/filterable_list.html#client.ayon_core.ui.components.filterable_list.FilterableList.add_item","title":"<code>add_item(widget, match_fn=None)</code>","text":"<p>Add a widget to the filterable list.</p> <p>Parameters:</p> Name Type Description Default <code>widget</code> <code>QWidget</code> <p>The widget to add.</p> required <code>match_fn</code> <code>Callable[[str], bool] | None</code> <p>A callable that receives the current search text and returns True if the widget should be visible. When omitted the item is always visible.</p> <code>None</code> Source code in <code>client/ayon_core/ui/components/filterable_list.py</code> <pre><code>def add_item(\n    self,\n    widget: QWidget,\n    match_fn: Callable[[str], bool] | None = None,\n) -&gt; None:\n    \"\"\"Add a widget to the filterable list.\n\n    Args:\n        widget: The widget to add.\n        match_fn: A callable that receives the current search text and\n            returns True if the widget should be visible. When omitted\n            the item is always visible.\n    \"\"\"\n    if match_fn is None:\n        match_fn = lambda _: True  # noqa: E731\n    self._items.append((widget, match_fn))\n    self._items_layout.addWidget(widget)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/filterable_list.html#client.ayon_core.ui.components.filterable_list.FilterableList.add_stretch","title":"<code>add_stretch()</code>","text":"<p>Add a stretch spacer at the end of the list.</p> Source code in <code>client/ayon_core/ui/components/filterable_list.py</code> <pre><code>def add_stretch(self) -&gt; None:\n    \"\"\"Add a stretch spacer at the end of the list.\"\"\"\n    self._items_layout.addStretch()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/filterable_list.html#client.ayon_core.ui.components.filterable_list.FilterableList.clear_items","title":"<code>clear_items()</code>","text":"<p>Remove all items from the list.</p> Source code in <code>client/ayon_core/ui/components/filterable_list.py</code> <pre><code>def clear_items(self) -&gt; None:\n    \"\"\"Remove all items from the list.\"\"\"\n    self._items.clear()\n    self._items_layout.clear()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/filterable_list.html#client.ayon_core.ui.components.filterable_list.FilterableList.search_field","title":"<code>search_field()</code>","text":"<p>Return the search field widget.</p> <p>Returns:</p> Type Description <code>AYLineEdit</code> <p>The internal AYLineEdit used for filtering.</p> Source code in <code>client/ayon_core/ui/components/filterable_list.py</code> <pre><code>def search_field(self) -&gt; AYLineEdit:\n    \"\"\"Return the search field widget.\n\n    Returns:\n        The internal AYLineEdit used for filtering.\n    \"\"\"\n    return self._search_field\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/filterable_list.html#client.ayon_core.ui.components.filterable_list.FilterableList.visible_items","title":"<code>visible_items()</code>","text":"<p>Return currently visible item widgets.</p> <p>Returns:</p> Type Description <code>list[QWidget]</code> <p>List of visible QWidget instances.</p> Source code in <code>client/ayon_core/ui/components/filterable_list.py</code> <pre><code>def visible_items(self) -&gt; list[QWidget]:\n    \"\"\"Return currently visible item widgets.\n\n    Returns:\n        List of visible QWidget instances.\n    \"\"\"\n    return [w for w, _ in self._items if w.isVisible()]\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/frame.html","title":"frame","text":""},{"location":"autoapi/client/ayon_core/ui/components/gallery_dialog.html","title":"gallery_dialog","text":"<p>Gallery dialog for navigating through activity thumbnails/images.</p>"},{"location":"autoapi/client/ayon_core/ui/components/gallery_dialog.html#client.ayon_core.ui.components.gallery_dialog.GalleryDialog","title":"<code>GalleryDialog</code>","text":"<p>               Bases: <code>QDialog</code></p> <p>Dialog for viewing and navigating through multiple images.</p> <p>This dialog provides a simple gallery view using standard Qt widgets, matching the official AYON style for image preview dialogs.</p> <p>Attributes:</p> Name Type Description <code>image_changed</code> <p>Signal emitted when the current image changes. Emits the current index.</p> Example <p>images = [ ...     (\"/path/to/image1.png\", \"image1.png\"), ...     (\"/path/to/image2.png\", \"image2.png\"), ... ] dialog = GalleryDialog(images, current_index=0) dialog.exec()</p> Source code in <code>client/ayon_core/ui/components/gallery_dialog.py</code> <pre><code>class GalleryDialog(QDialog):\n    \"\"\"Dialog for viewing and navigating through multiple images.\n\n    This dialog provides a simple gallery view using standard Qt widgets,\n    matching the official AYON style for image preview dialogs.\n\n    Attributes:\n        image_changed: Signal emitted when the current image changes.\n            Emits the current index.\n\n    Example:\n        &gt;&gt;&gt; images = [\n        ...     (\"/path/to/image1.png\", \"image1.png\"),\n        ...     (\"/path/to/image2.png\", \"image2.png\"),\n        ... ]\n        &gt;&gt;&gt; dialog = GalleryDialog(images, current_index=0)\n        &gt;&gt;&gt; dialog.exec()\n    \"\"\"\n\n    image_changed = Signal(int)\n\n    def __init__(\n        self,\n        images: list[tuple[str, str]],\n        current_index: int = 0,\n        parent: QWidget | None = None,\n    ) -&gt; None:\n        \"\"\"Initialize the gallery dialog.\n\n        Args:\n            images: List of tuples (image_path, filename) for each image.\n            current_index: Index of the image to show initially.\n            parent: Parent widget.\n        \"\"\"\n        super().__init__(parent)\n        self.setWindowFlags(self.windowFlags() | Qt.WindowType.Tool)\n        self.setStyle(get_ayon_style())\n        self.images = images\n        self.current_index = current_index\n        # Track if dialog size has been set based on first image\n        self._dialog_size_set = False\n\n        self.setWindowTitle(\"Image Preview\")\n\n        # Set focus policy so dialog can receive keyboard events\n        self.setFocusPolicy(Qt.FocusPolicy.StrongFocus)\n\n        # Install event filter to intercept keyboard events from child widgets\n        self.installEventFilter(self)\n\n        self._setup_ui()\n        self._show_current_image()\n\n    def showEvent(self, event: QShowEvent) -&gt; None:\n        \"\"\"Handle dialog show event to set focus properly.\n\n        Args:\n            event: Show event.\n        \"\"\"\n        super().showEvent(event)\n        # Activate window and set focus to the dialog so keyboard events work\n        # immediately\n        self.activateWindow()\n        self.setFocus(Qt.FocusReason.OtherFocusReason)\n\n    def eventFilter(self, obj, event: QEvent) -&gt; bool:\n        \"\"\"Filter events to intercept keyboard navigation from any widget.\n\n        This ensures arrow key navigation works even if a child widget\n        somehow gains focus.\n\n        Args:\n            obj: Object that received the event.\n            event: Event to filter.\n\n        Returns:\n            True if the event was handled, False otherwise.\n        \"\"\"\n        if event.type() == QEvent.Type.KeyPress:\n            key = event.key()\n            if key == Qt.Key.Key_Left:\n                self._show_previous()\n                return True\n            elif key == Qt.Key.Key_Right:\n                self._show_next()\n                return True\n            elif key == Qt.Key.Key_Escape:\n                self.accept()\n                return True\n        return super().eventFilter(obj, event)\n\n    def _setup_ui(self) -&gt; None:\n        \"\"\"Set up the dialog UI components using standard Qt widgets.\"\"\"\n        dialog_lyt = AYVBoxLayout(self, margin=0, spacing=0)\n\n        self.top_lyt = AYContainer(\n            self,\n            layout=AYContainer.Layout.VBox,\n            variant=AYContainer.Variants.Low,\n            layout_margin=10,\n            layout_spacing=10,\n        )\n        # Prevent container from accepting focus\n        self.top_lyt.setFocusPolicy(Qt.FocusPolicy.NoFocus)\n        dialog_lyt.addWidget(self.top_lyt)\n\n        # Image display area\n        self.image_label = QLabel()\n        self.image_label.setAlignment(Qt.AlignmentFlag.AlignCenter)\n        self.image_label.setScaledContents(False)\n        # Prevent label from accepting focus\n        self.image_label.setFocusPolicy(Qt.FocusPolicy.NoFocus)\n        self.top_lyt.add_widget(self.image_label, stretch=1)\n\n        # Only show navigation controls if multiple images\n        if len(self.images) &gt; 1:\n            # Navigation controls\n            nav_widget = AYContainer(\n                layout=AYContainer.Layout.HBox,\n                variant=AYContainer.Variants.High,\n                layout_margin=0,\n                layout_spacing=10,\n            )\n            # Prevent container from accepting focus\n            nav_widget.setFocusPolicy(Qt.FocusPolicy.NoFocus)\n\n            # Previous button\n            self.prev_btn = AYButton(\n                \"\u25c0 Previous\", variant=AYButton.Variants.Nav\n            )\n            self.prev_btn.clicked.connect(self._show_previous)\n            # Prevent button from stealing focus\n            self.prev_btn.setFocusPolicy(Qt.FocusPolicy.NoFocus)\n            self.prev_btn.installEventFilter(self)\n            nav_widget.add_widget(self.prev_btn)\n\n            # Info label (counter and filename)\n            self.info_label = AYLabel(variant=AYLabel.Variants.Default)\n            self.info_label.setAlignment(Qt.AlignmentFlag.AlignCenter)\n            # Prevent label from accepting focus\n            self.info_label.setFocusPolicy(Qt.FocusPolicy.NoFocus)\n            nav_widget.add_widget(self.info_label, stretch=1)\n\n            # Next button\n            self.next_btn = AYButton(\"Next \u25b6\", variant=AYButton.Variants.Nav)\n            self.next_btn.clicked.connect(self._show_next)\n            # Prevent button from stealing focus\n            self.next_btn.setFocusPolicy(Qt.FocusPolicy.NoFocus)\n            self.next_btn.installEventFilter(self)\n            nav_widget.add_widget(self.next_btn)\n\n            self.top_lyt.add_widget(nav_widget)\n\n    def _show_current_image(self) -&gt; None:\n        \"\"\"Display the current image with proper scaling.\"\"\"\n        if not self.images or self.current_index &gt;= len(self.images):\n            return\n\n        image_path, filename = self.images[self.current_index]\n\n        # Load the full-size image\n        if not Path(image_path).exists():\n            self.image_label.setText(\"Image not found\")\n            return\n\n        original_pixmap = QPixmap(image_path)\n        if original_pixmap.isNull():\n            self.image_label.setText(\"Failed to load image\")\n            return\n\n        # Get screen dimensions for sizing\n        screen = self.screen() or QApplication.primaryScreen()\n        screen_size = screen.availableGeometry()\n        max_w = int(screen_size.width() * 0.8)\n        max_h = int(screen_size.height() * 0.8)\n\n        # Scale if too large for screen while maintaining aspect ratio\n        display_pixmap = original_pixmap\n        if original_pixmap.width() &gt; max_w or original_pixmap.height() &gt; max_h:\n            display_pixmap = original_pixmap.scaled(\n                max_w,\n                max_h,\n                Qt.AspectRatioMode.KeepAspectRatio,\n                Qt.TransformationMode.SmoothTransformation,\n            )\n\n        self.image_label.setPixmap(display_pixmap)\n\n        # Set dialog size once on first image, then keep it consistent\n        if not self._dialog_size_set:\n            _layout = self.top_lyt.layout()\n            if _layout:\n                self.resize(_layout.sizeHint())\n                self._dialog_size_set = True\n            else:\n                logger.warning(\"Failed to get layout to size the dialog\")\n\n        # Update navigation controls if multiple images\n        if len(self.images) &gt; 1:\n            # Update info label\n            display_name = Path(filename).stem if filename else \"Unknown\"\n            info_text = (\n                f\"{self.current_index + 1} / {len(self.images)} - \"\n                f\"{display_name}\"\n            )\n            self.info_label.setText(info_text)\n\n            # Update button states\n            self.prev_btn.setEnabled(self.current_index &gt; 0)\n            self.next_btn.setEnabled(self.current_index &lt; len(self.images) - 1)\n\n        # Emit signal\n        self.image_changed.emit(self.current_index)\n\n    def _show_previous(self) -&gt; None:\n        \"\"\"Show the previous image.\"\"\"\n        if self.current_index &gt; 0:\n            self.current_index -= 1\n            self._show_current_image()\n\n    def _show_next(self) -&gt; None:\n        \"\"\"Show the next image.\"\"\"\n        if self.current_index &lt; len(self.images) - 1:\n            self.current_index += 1\n            self._show_current_image()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/gallery_dialog.html#client.ayon_core.ui.components.gallery_dialog.GalleryDialog.__init__","title":"<code>__init__(images, current_index=0, parent=None)</code>","text":"<p>Initialize the gallery dialog.</p> <p>Parameters:</p> Name Type Description Default <code>images</code> <code>list[tuple[str, str]]</code> <p>List of tuples (image_path, filename) for each image.</p> required <code>current_index</code> <code>int</code> <p>Index of the image to show initially.</p> <code>0</code> <code>parent</code> <code>QWidget | None</code> <p>Parent widget.</p> <code>None</code> Source code in <code>client/ayon_core/ui/components/gallery_dialog.py</code> <pre><code>def __init__(\n    self,\n    images: list[tuple[str, str]],\n    current_index: int = 0,\n    parent: QWidget | None = None,\n) -&gt; None:\n    \"\"\"Initialize the gallery dialog.\n\n    Args:\n        images: List of tuples (image_path, filename) for each image.\n        current_index: Index of the image to show initially.\n        parent: Parent widget.\n    \"\"\"\n    super().__init__(parent)\n    self.setWindowFlags(self.windowFlags() | Qt.WindowType.Tool)\n    self.setStyle(get_ayon_style())\n    self.images = images\n    self.current_index = current_index\n    # Track if dialog size has been set based on first image\n    self._dialog_size_set = False\n\n    self.setWindowTitle(\"Image Preview\")\n\n    # Set focus policy so dialog can receive keyboard events\n    self.setFocusPolicy(Qt.FocusPolicy.StrongFocus)\n\n    # Install event filter to intercept keyboard events from child widgets\n    self.installEventFilter(self)\n\n    self._setup_ui()\n    self._show_current_image()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/gallery_dialog.html#client.ayon_core.ui.components.gallery_dialog.GalleryDialog.eventFilter","title":"<code>eventFilter(obj, event)</code>","text":"<p>Filter events to intercept keyboard navigation from any widget.</p> <p>This ensures arrow key navigation works even if a child widget somehow gains focus.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <p>Object that received the event.</p> required <code>event</code> <code>QEvent</code> <p>Event to filter.</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if the event was handled, False otherwise.</p> Source code in <code>client/ayon_core/ui/components/gallery_dialog.py</code> <pre><code>def eventFilter(self, obj, event: QEvent) -&gt; bool:\n    \"\"\"Filter events to intercept keyboard navigation from any widget.\n\n    This ensures arrow key navigation works even if a child widget\n    somehow gains focus.\n\n    Args:\n        obj: Object that received the event.\n        event: Event to filter.\n\n    Returns:\n        True if the event was handled, False otherwise.\n    \"\"\"\n    if event.type() == QEvent.Type.KeyPress:\n        key = event.key()\n        if key == Qt.Key.Key_Left:\n            self._show_previous()\n            return True\n        elif key == Qt.Key.Key_Right:\n            self._show_next()\n            return True\n        elif key == Qt.Key.Key_Escape:\n            self.accept()\n            return True\n    return super().eventFilter(obj, event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/gallery_dialog.html#client.ayon_core.ui.components.gallery_dialog.GalleryDialog.showEvent","title":"<code>showEvent(event)</code>","text":"<p>Handle dialog show event to set focus properly.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <code>QShowEvent</code> <p>Show event.</p> required Source code in <code>client/ayon_core/ui/components/gallery_dialog.py</code> <pre><code>def showEvent(self, event: QShowEvent) -&gt; None:\n    \"\"\"Handle dialog show event to set focus properly.\n\n    Args:\n        event: Show event.\n    \"\"\"\n    super().showEvent(event)\n    # Activate window and set focus to the dialog so keyboard events work\n    # immediately\n    self.activateWindow()\n    self.setFocus(Qt.FocusReason.OtherFocusReason)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/label.html","title":"label","text":""},{"location":"autoapi/client/ayon_core/ui/components/label.html#client.ayon_core.ui.components.label.AYLabel","title":"<code>AYLabel</code>","text":"<p>               Bases: <code>StyleMixin</code>, <code>QLabel</code></p> Source code in <code>client/ayon_core/ui/components/label.py</code> <pre><code>class AYLabel(StyleMixin, QtWidgets.QLabel):\n    Variants = QLabelVariants\n\n    def __init__(\n        self,\n        *args,\n        dim: bool = False,\n        icon: str = \"\",\n        icon_color: str = \"\",\n        icon_size: int = 20,\n        icon_text_spacing=6,\n        icon_fill=False,\n        text_color: str = \"\",\n        rel_text_size: int = 0,\n        bold: bool = False,\n        tool_tip=\"\",\n        variant: Variants = Variants.Default,\n        contrast_color: QColor | None = None,\n        elide_mode: Qt.TextElideMode = Qt.TextElideMode.ElideNone,\n        copy_text: bool = False,\n        **kwargs,\n    ):\n        # style params\n        self._variant_str: str = variant.value\n        self._style_data = StyleDict()\n\n        # widget params\n        self._dim = dim\n        self._icon = icon\n        self._icon_color = icon_color\n        self._icon_size = icon_size\n        self._icon_fill = icon_fill\n        self._icon_text_spacing = icon_text_spacing\n        self._rel_text_size = rel_text_size\n        self._text_color = text_color\n        self._bold = bold\n        self._text_setup_done = False\n        self._elide_mode = elide_mode\n        # copy the text because setting an icon will blank it, as a label is\n        # either text or pixmap.\n        self._text: str = \"\"\n        # reference bg color to compute contrast-adapted text color\n        self._contrast_color = (\n            contrast_color\n            if isinstance(contrast_color, QColor) and contrast_color.isValid()\n            else None\n        )\n        self._contrast_adapted = None\n\n        # copy-text feature\n        self._copy_text = copy_text\n        self._copy_icon_hovered = False\n        self._copy_confirmed = False\n        self._copy_done_opacity: float = 1.0\n        self._copy_pix_normal: QPixmap | None = None\n        self._copy_pix_hover: QPixmap | None = None\n        self._copy_pix_done: QPixmap | None = None\n        self._copy_confirm_timer: QTimer | None = None\n        self._copy_fade_timer: QTimer | None = None\n\n        super().__init__(*args, **kwargs)\n        self._style = get_ayon_style()\n        self._style_data = self._style.model.get_styles(\n            \"QLabel\", variant=self._variant_str\n        )\n        self._style_data.set_context(self)\n        self.setStyle(self._style)\n\n        # used to be in polish\n        self.setWindowFlag(Qt.WindowType.FramelessWindowHint, True)\n        self.setWindowFlag(Qt.WindowType.NoDropShadowWindowHint, True)\n\n        self.setSizePolicy(\n            QtWidgets.QSizePolicy.Policy.Minimum,\n            QtWidgets.QSizePolicy.Policy.Preferred,\n        )\n\n        # set alignment from style data if specified.\n        alignment = self._style_data[\"base\"].get(\"alignment\")\n        if alignment is not None:\n            if alignment == \"center\":\n                self.setAlignment(Qt.AlignmentFlag.AlignCenter)\n            elif alignment == \"left\":\n                self.setAlignment(\n                    Qt.AlignmentFlag.AlignLeft | Qt.AlignmentFlag.AlignVCenter\n                )\n            elif alignment == \"right\":\n                self.setAlignment(\n                    Qt.AlignmentFlag.AlignRight | Qt.AlignmentFlag.AlignVCenter\n                )\n\n        self._text = self.text()  # call before setting icon to preserve text\n        self.setToolTip(tool_tip)\n\n        if self._copy_text:\n            self.setMouseTracking(True)\n            self._copy_confirm_timer = QTimer(self)\n            self._copy_confirm_timer.setSingleShot(True)\n            self._copy_confirm_timer.timeout.connect(\n                self._on_copy_confirm_timeout\n            )\n            self._copy_fade_timer = QTimer(self)\n            self._copy_fade_timer.setInterval(16)\n            self._copy_fade_timer.timeout.connect(self._on_copy_fade_tick)\n\n        self.set_icon()\n\n    @property\n    def contrast_color(self) -&gt; QColor | None:\n        return self._contrast_color\n\n    def set_palette(self, palette: QPalette) -&gt; None:\n        \"\"\"Set the widget palette and trigger a repaint.\"\"\"\n        self._style_palette = palette\n        self._configure_palette()\n        if self._icon and not self._icon_color:\n            self.set_icon()  # update icon color from palette\n        self._copy_pix_normal = self._copy_pix_hover = self._copy_pix_done = (\n            None\n        )\n        self.update()\n\n    def set_font(self, font: QFont) -&gt; None:\n        \"\"\"Set the widget font and trigger a repaint.\"\"\"\n        self._style_font = self._configure_font(font)\n        self.update()\n\n    # Private methods -------------------------------------------------------\n\n    def set_icon(self, icon: str | None = None, color: str = \"\") -&gt; None:\n        if icon is not None:\n            self._icon = icon\n        if color:\n            self._icon_color = color\n\n        if self._icon:\n            same_as_bg = (\n                self._icon_color not in (None, \"\")\n                and self._style_data[\"base\"].get(\"background-color\", raw=True)\n                == \"@_icon_color\"\n            )\n            icon_color = self._icon_color\n            if not icon_color:\n                icon_color = self.palette().color(self.foregroundRole()).name()\n            elif same_as_bg:\n                icon_color = self._style_data[\"base\"].get(\"color\")\n\n            if self._icon == \"none\":\n                icn = QIcon()  # empty icon for spacing\n                pxm = QPixmap(self._icon_size, self._icon_size)\n                pxm.fill(Qt.GlobalColor.transparent)\n                icn.addPixmap(pxm)\n            else:\n                icn: QIcon = get_icon(\n                    self._icon,\n                    color=icon_color,\n                    fill=self._icon_fill,\n                )\n            self.setPixmap(icn.pixmap(QSize(self._icon_size, self._icon_size)))\n\n    def _configure_font(self, font: QFont) -&gt; QFont:\n        \"\"\"Initialize font configuration on first paint.\"\"\"\n        if self._text_setup_done:\n            return font\n\n        if self._rel_text_size != 0:\n            # _rel_text_size is in points but setting pixels is more reliable.\n            # use QFontInfo in case PixelSize() or pointSizeF() returns -1\n            pt_size = QFontInfo(font).pointSizeF()\n            new_pt_size = pt_size + self._rel_text_size\n            font.setPointSizeF(new_pt_size)\n\n        weight = QFont.Weight.Bold if self._bold else QFont.Weight.Normal\n        font.setWeight(weight)\n\n        self._text_setup_done = True\n        return font\n\n    def _display_text(self) -&gt; str:\n        \"\"\"Recompute the elided version of the stored text.\"\"\"\n        if (\n            self._elide_mode == Qt.TextElideMode.ElideNone\n            or not self.fontMetrics()\n        ):\n            return self._text\n        available_w = self.contentsRect().width()\n        if self._icon:\n            spacing = self._icon_text_spacing\n            available_w -= self._icon_size + spacing\n        text = self.fontMetrics().elidedText(\n            self._text, self._elide_mode, max(0, available_w)\n        )\n        return text\n\n    def _resolve_color(self) -&gt; QColor:\n        \"\"\"Get the effective foreground color (icon_color or palette).\"\"\"\n        if self._icon_color:\n            return QColor(self._icon_color)\n        return self.palette().color(self.foregroundRole())\n\n    def _to_qcolor(self, color: QColor | str | None) -&gt; QColor | None:\n        \"\"\"Convert a color value to QColor, handling None and strings.\"\"\"\n        if color is None:\n            return None\n        if isinstance(color, QColor):\n            return color\n        return QColor(color)\n\n    def _compute_contrast_text_color(\n        self,\n        bg_color: QColor | str | None,\n        fg_color: QColor,\n    ) -&gt; QColor:\n        \"\"\"Compute text color with sufficient contrast against background.\"\"\"\n        if not bg_color:\n            return fg_color\n        qbg = self._to_qcolor(bg_color)\n        return compute_color_for_contrast(\n            qbg.toTuple(),  # type: ignore\n            fg_color.toTuple(),\n            min_contrast_ratio=7.0,\n        )\n\n    def _configure_palette(self) -&gt; None:\n        \"\"\"Configure palette based on dim/contrast settings.\"\"\"\n        # _style_palette is guaranteed to be set in paintEvent before this call\n        assert self._style_palette is not None\n\n        if self._dim:\n            self._style_palette.setColor(\n                QPalette.ColorGroup.Active,\n                self.foregroundRole(),\n                self._style_palette.color(\n                    QPalette.ColorGroup.Active,\n                    QPalette.ColorRole.PlaceholderText,\n                ),\n            )\n            return\n\n        if self._text_color:\n            self._style_palette.setColor(\n                self.foregroundRole(), QColor(self._text_color)\n            )\n\n        if self._style_data[\"base\"].get(\"fill-from-foreground\", False):\n            # background-color is resolved from @_icon_color via style refs\n            bg_val = self._style_data[\"base\"].get(\"background-color\", \"\")\n            bg_color = (\n                QColor(bg_val)\n                if bg_val and bg_val != \"transparent\"\n                else self._style_palette.color(self.foregroundRole())\n            )\n            self._style_palette.setColor(self.backgroundRole(), bg_color)\n            if not self._contrast_color:\n                self._contrast_color = bg_color\n\n        if self._style_data[\"base\"].get(\"contrast-text\", False):\n            contrast_ref = self._contrast_color or self._icon_color\n            txt_color = self._compute_contrast_text_color(\n                contrast_ref,\n                self.palette().color(self.foregroundRole()),\n            )\n            self._style_palette.setColor(self.foregroundRole(), txt_color)\n\n        if \"disabled\" in self._style_data:\n            opacity = self._style_data[\"disabled\"].get(\"opacity\", 1.0)\n            if opacity &lt; 1.0:\n                for role in QPalette.ColorRole:\n                    color = self._style_palette.color(role)\n                    if color == Qt.GlobalColor.transparent:\n                        continue\n                    color.setAlphaF(opacity)\n                    self._style_palette.setColor(\n                        QPalette.ColorGroup.Disabled, role, color\n                    )\n\n    def _paint_filled(self, state: str) -&gt; None:\n        \"\"\"Render a filled-background label driven by style data.\"\"\"\n        assert isinstance(self.fontMetrics(), QFontMetrics)\n\n        # Auto-size from text metrics\n        if self._style_data[state].get(\"auto-size\"):\n            padding = self._style_data[state].get(\"auto-size-padding\", [0, 0])\n            t_rect = self.fontMetrics().boundingRect(self.text())\n            padx = int(self.fontMetrics().averageCharWidth() * padding[0])\n            pady = int(self.fontMetrics().height() * padding[1])\n            self.setFixedSize(\n                t_rect.width() + padx,\n                t_rect.height() + pady,\n            )\n\n        p = QPainter(self)\n        self.initPainter(p)\n        p.setFont(self.font())\n        p.setRenderHint(QPainter.RenderHint.Antialiasing)\n\n        # Fill color from foreground\n        fill_color = self._resolve_color()\n        p.setBrush(QBrush(fill_color))\n        p.setPen(Qt.PenStyle.NoPen)\n\n        # handle disabled opacity for both fill and text\n        p.setOpacity(self._style_data[state].get(\"opacity\", 1.0))\n\n        # Border radius: fraction of height or fixed\n        radius_frac = self._style_data[state].get(\"border-radius-fraction\")\n        if radius_frac is not None:\n            radius = self.rect().height() * radius_frac\n        else:\n            radius = self._style_data[state].get(\"border-radius\", 0)\n\n        p.drawRoundedRect(self.rect(), radius, radius)\n\n        # Text color with contrast computation\n        if self._style_data[state].get(\"contrast-text\"):\n            contrast_ref = self._contrast_color or self._icon_color\n            txt_color = self._compute_contrast_text_color(\n                contrast_ref,\n                self.palette().color(self.foregroundRole()),\n            )\n            p.setPen(QPen(QBrush(txt_color), 1.0))\n\n        self._style.drawItemText(\n            p,\n            self.rect(),\n            Qt.AlignmentFlag.AlignCenter,\n            self.palette(),\n            self.isEnabled(),\n            self.text(),\n            textRole=QPalette.ColorRole.NoRole,\n        )\n        p.end()\n\n    def _paint_icon_and_text(self, state: str) -&gt; None:\n        \"\"\"Render label with both icon and text.\n\n        The icon and text are treated as a single group and positioned\n        within the widget rect according to the current alignment.\n\n        The spacing between icon and text is resolved from the\n        ``icon-text-spacing`` property in *style_data*, falling back to\n        the value supplied at construction time.\n\n        Args:\n            style_data: Variant style properties resolved from the style\n                JSON for the current ``QLabel`` variant.\n        \"\"\"\n        assert isinstance(self.fontMetrics(), QFontMetrics)\n\n        p = QPainter(self)\n        p.setFont(self.font())\n        p.setRenderHint(QPainter.RenderHint.Antialiasing)\n\n        text_rect = self.fontMetrics().boundingRect(self._display_text())\n        text_rect.adjust(0, 0, 1, 0)  # +1 pixel for antialiasing\n\n        icon_w = self._icon_size\n        icon_h = self._icon_size\n        spacing = int(\n            self._style_data[state].get(\n                \"icon-text-spacing\", self._icon_text_spacing\n            )\n        )\n        group_w = icon_w + spacing + text_rect.width()\n        group_h = max(icon_h, text_rect.height())\n\n        # Position the group using the current alignment\n        widget_rect = self.contentsRect().normalized()\n        explicit_padding = self._style_data[state].get(\n            \"padding\",\n            self._style_data[\"base\"].get(\"padding\", [0, 0]),\n        )\n        pad_h = int(explicit_padding[0])\n        pad_v = int(explicit_padding[1])\n        widget_rect = widget_rect.adjusted(pad_h, pad_v, -pad_h, -pad_v)\n\n        alignment = self.alignment()\n\n        if alignment &amp; Qt.AlignmentFlag.AlignLeft:\n            group_x = widget_rect.left()\n        elif alignment &amp; Qt.AlignmentFlag.AlignRight:\n            group_x = widget_rect.right() - group_w\n        else:  # Center (default)\n            group_x = widget_rect.left() + (widget_rect.width() - group_w) // 2\n\n        if alignment &amp; Qt.AlignmentFlag.AlignTop:\n            group_y = widget_rect.top()\n        elif alignment &amp; Qt.AlignmentFlag.AlignBottom:\n            group_y = widget_rect.bottom() - group_h\n        else:  # VCenter (default)\n            group_y = widget_rect.top() + (widget_rect.height() - group_h) // 2\n\n        # handle disabled opacity for both icon and text\n        p.save()\n        p.setOpacity(self._style_data[state].get(\"opacity\", 1.0))\n\n        # Draw icon at the left of the group\n        icon_y = group_y + (group_h - icon_h) // 2\n        icn_rct = QRect(group_x, icon_y, icon_w, icon_h)\n        self._style.drawItemPixmap(\n            p,\n            icn_rct,\n            Qt.AlignmentFlag.AlignCenter,\n            self.pixmap(),\n        )\n\n        # Draw text at the right of the icon\n        pal = self.palette()\n        if not self._dim:\n            pal.setColor(QPalette.ColorRole.Text, self._resolve_color())\n\n        txt_x = group_x + icon_w + spacing\n        txt_y = group_y + (group_h - text_rect.height()) // 2\n        txt_rct = QRect(txt_x, txt_y, text_rect.width(), text_rect.height())\n        self._style.drawItemText(\n            p,\n            txt_rct,\n            Qt.AlignmentFlag.AlignVCenter | Qt.AlignmentFlag.AlignLeft,\n            pal,\n            self.isEnabled(),\n            self._display_text(),\n            textRole=self.foregroundRole(),\n        )\n        p.restore()\n\n    def _paint_text_only(self, state: str) -&gt; None:\n        \"\"\"Render text-only label.\"\"\"\n        p = QPainter(self)\n        p.setFont(self.font())\n        p.setRenderHint(QPainter.RenderHint.Antialiasing)\n        pal = self.palette()\n        if self._text_color:\n            pal.setColor(self.foregroundRole(), QColor(self._text_color))\n            if \"disabled\" in self._style_data:\n                t_color = QColor(self._text_color)\n                t_color.setAlphaF(self._style_data[state].get(\"opacity\", 1.0))\n                pal.setColor(\n                    QPalette.ColorGroup.Disabled,\n                    self.foregroundRole(),\n                    t_color,\n                )\n        flags = int(self.alignment())\n        if self.wordWrap():\n            flags |= int(Qt.TextFlag.TextWordWrap)\n        p.save()\n        p.setOpacity(self._style_data[state].get(\"opacity\", 1.0))\n        self._style.drawItemText(\n            p,\n            self.contentsRect().normalized(),\n            flags,\n            pal,\n            self.isEnabled(),\n            self._display_text(),\n            textRole=self.foregroundRole(),\n        )\n        p.restore()\n\n    _COPY_ICON_SIZE = 16\n    _COPY_ICON_PADDING = 0\n\n    def _get_copy_icon_rect(self) -&gt; QRect:\n        \"\"\"Return the bounding rect for the copy icon overlay.\n\n        Returns:\n            Rect positioned at the right edge of the content area,\n            vertically centered.\n        \"\"\"\n        sz = self._COPY_ICON_SIZE\n        rect = self.contentsRect()\n        x = rect.right() - sz - self._COPY_ICON_PADDING\n        y = rect.top() + (rect.height() - sz) // 2\n        return QRect(x, y, sz, sz)\n\n    def _make_copy_pixmap(self, icon_name: str, color_str: str) -&gt; QPixmap:\n        \"\"\"Render a single copy-feature icon onto a transparent pixmap.\n\n        Args:\n            icon_name: Material symbol name.\n            color_str: CSS colour string passed to ``get_icon``.\n\n        Returns:\n            A ``QPixmap`` of size ``_COPY_ICON_SIZE \u00d7 _COPY_ICON_SIZE``.\n        \"\"\"\n        sz = self._COPY_ICON_SIZE\n        pxm = QPixmap(sz, sz)\n        pxm.fill(QColor(0, 0, 0, 200))\n        icon_pxm: QPixmap = get_icon(icon_name, color=color_str).pixmap(\n            QSize(sz, sz)\n        )\n        p = QPainter(pxm)\n        p.drawPixmap(QPoint(0, 0), icon_pxm)\n        p.end()\n        return pxm\n\n    def _build_copy_pixmaps(self) -&gt; None:\n        \"\"\"(Re-)build cached copy-icon pixmaps for normal, hover and done\n        states.\"\"\"\n        fg = self.palette().color(self.foregroundRole())\n        normal_color = QColor(fg)\n        normal_color.setAlphaF(0.75)\n        self._copy_pix_normal = self._make_copy_pixmap(\n            \"content_copy\",\n            normal_color.name(QColor.NameFormat.HexArgb),\n        )\n        self._copy_pix_hover = self._make_copy_pixmap(\n            \"content_copy\", fg.name()\n        )\n        self._copy_pix_done = self._make_copy_pixmap(\"check\", fg.name())\n\n    _COPY_FADE_DURATION_MS: int = 500\n\n    def _on_copy_confirm_timeout(self) -&gt; None:\n        \"\"\"Begin fading out the checkmark icon over _COPY_FADE_DURATION_MS.\"\"\"\n        self._copy_done_opacity = 1.0\n        assert self._copy_fade_timer is not None\n        self._copy_fade_timer.start()\n\n    def _on_copy_fade_tick(self) -&gt; None:\n        \"\"\"Decrement the done-icon opacity and repaint each frame.\"\"\"\n        step = 16.0 / self._COPY_FADE_DURATION_MS\n        self._copy_done_opacity = max(0.0, self._copy_done_opacity - step)\n        self.update()\n        if self._copy_done_opacity &lt;= 0.0:\n            assert self._copy_fade_timer is not None\n            self._copy_fade_timer.stop()\n            self._copy_confirmed = False\n\n    def _trigger_copy_confirmed(self) -&gt; None:\n        \"\"\"Copy the label text to clipboard and start the confirmation\n        animation.\"\"\"\n        QtWidgets.QApplication.clipboard().setText(self._text)\n        assert self._copy_fade_timer is not None\n        assert self._copy_confirm_timer is not None\n        # Reset any in-progress fade and restart the hold + fade sequence.\n        self._copy_fade_timer.stop()\n        self._copy_done_opacity = 1.0\n        self._copy_confirmed = True\n        self._copy_confirm_timer.start(500)\n        self.update()\n\n    def _paint_copy_icon(self) -&gt; None:\n        \"\"\"Draw the copy-to-clipboard icon on the right side of the label.\n\n        Only called when the mouse is over the widget and ``copy_text``\n        is True. Shows a fading checkmark briefly after the user clicks.\n        \"\"\"\n        if self._copy_pix_normal is None:\n            self._build_copy_pixmaps()\n        p = QPainter(self)\n        p.setRenderHint(QPainter.RenderHint.Antialiasing)\n        if self._copy_confirmed:\n            p.setOpacity(self._copy_done_opacity)\n            pix = self._copy_pix_done\n        else:\n            pix = (\n                self._copy_pix_hover\n                if self._copy_icon_hovered\n                else self._copy_pix_normal\n            )\n        self._style.drawItemPixmap(\n            p,\n            self._get_copy_icon_rect(),\n            Qt.AlignmentFlag.AlignCenter,\n            pix,  # type: ignore[arg-type]\n        )\n        p.end()\n\n    def _paint_background(self, state: str) -&gt; None:\n        \"\"\"Draw background if specified by style.\"\"\"\n        bg_color = self._style_data[state].get(\"background-color\")\n        if bg_color and bg_color != \"transparent\":\n            p = QPainter(self)\n            p.setRenderHint(QPainter.RenderHint.Antialiasing)\n            border_radius = self._style_data[state].get(\"border-radius\", 0)\n            border_width = self._style_data[state].get(\"border-width\", 0)\n            border_color = self._style_data[state].get(\n                \"border-color\", \"#00000000\"\n            )\n            qbg_color = QColor(bg_color)\n            qbg_color.setAlphaF(self._style_data[state].get(\"opacity\", 1.0))\n            p.setBrush(QBrush(QColor(qbg_color)))\n            p.setPen(\n                QPen(QColor(border_color), border_width)\n                if border_width &gt; 0\n                else Qt.PenStyle.NoPen\n            )\n            p.drawRoundedRect(self.rect(), border_radius, border_radius)\n\n    # QLabel overrides ----------------------------------------------------\n\n    def enterEvent(self, event: QEnterEvent) -&gt; None:\n        super().enterEvent(event)\n        if self._copy_text:\n            self.update()\n\n    def leaveEvent(self, event: QEvent) -&gt; None:\n        super().leaveEvent(event)\n        if self._copy_text:\n            self._copy_icon_hovered = False\n            self.update()\n\n    def mouseMoveEvent(self, event: QMouseEvent) -&gt; None:\n        super().mouseMoveEvent(event)\n        if not self._copy_text:\n            return\n        rect = self._get_copy_icon_rect()\n        hovered = rect.contains(event.pos())\n        if hovered != self._copy_icon_hovered:\n            self._copy_icon_hovered = hovered\n            self.update()\n\n    def mousePressEvent(self, event: QMouseEvent) -&gt; None:\n        if (\n            self._copy_text\n            and event.button() == Qt.MouseButton.LeftButton\n            and self._get_copy_icon_rect().contains(event.pos())\n        ):\n            self._trigger_copy_confirmed()\n        super().mousePressEvent(event)\n\n    def paintEvent(self, arg__1: QPaintEvent) -&gt; None:\n        \"\"\"Override to support icon + text rendering.\"\"\"\n        state = \"disabled\" if not self.isEnabled() else \"base\"\n        if (\n            state != \"disabled\"\n            and \"hover\" in self._style_data\n            and self.underMouse()\n        ):\n            state = \"hover\"\n\n        # Filled-background rendering (driven by JSON properties)\n        if self._style_data[\"base\"].get(\"fill-from-foreground\"):\n            self._paint_filled(state)\n        else:\n            self._paint_background(state)\n            if self._text and self._icon:\n                self._paint_icon_and_text(state)\n            elif self._icon and not self._text:\n                super().paintEvent(arg__1)\n            else:\n                self._paint_text_only(state)\n\n        if (\n            self._copy_text\n            and self.isEnabled()\n            and (self.underMouse() or self._copy_confirmed)\n        ):\n            self._paint_copy_icon()\n\n    def sizeHint(self) -&gt; QSize:\n        \"\"\"Compute a size hint driven by QLabel style data from the style JSON.\n\n        For variants with ``auto-size`` (e.g. badge / pill), the size is\n        derived from font-metrics and the ``auto-size-padding`` factor.\n        For variants with an explicit ``padding`` list (e.g. entity-label),\n        the size is padded accordingly.\n        When an icon is present the icon dimensions are added.\n        When ``wordWrap`` is True and the widget already has a non-zero width,\n        the height is recomputed from the wrapped text bounds at that width;\n        otherwise the natural single-line size is returned and the layout\n        refines the height via :meth:`heightForWidth`.\n        In all other cases the base ``QLabel.sizeHint()`` is returned.\n\n        Returns:\n            The recommended widget size.\n        \"\"\"\n        fm = self.fontMetrics()\n\n        # --- text size --------------------------------------------------\n        if self._text:\n            t_rect = fm.boundingRect(self._text)\n            text_w = t_rect.width() + 1  # +1 pixel for antialiasing\n            text_h = t_rect.height()\n        else:\n            text_w = 0\n            text_h = fm.height()\n\n        # --- icon size --------------------------------------------------\n        icon_w = icon_h = 0\n        if self._icon:\n            icon_w = self._icon_size\n            icon_h = self._icon_size\n\n        # --- variant-specific sizing ------------------------------------\n        if self._style_data[\"base\"].get(\"auto-size\"):\n            # badge / pill: padding is expressed as a fraction of the\n            # character metrics (x-factor of avgCharWidth, y-factor of height)\n            padding = self._style_data[\"base\"].get(\n                \"auto-size-padding\", [0.0, 0.0]\n            )\n            pad_x = int(fm.averageCharWidth() * padding[0])\n            pad_y = int(fm.height() * padding[1])\n\n            content_w = max(text_w, icon_w)\n            content_h = max(text_h, icon_h)\n\n            return QSize(content_w + pad_x, content_h + pad_y)\n\n        explicit_padding = self._style_data[\"base\"].get(\"padding\", [0, 0])\n        pad_h = int(explicit_padding[0])\n        pad_v = int(explicit_padding[1])\n\n        if icon_w and text_w:\n            spacing = int(\n                self._style_data[\"base\"].get(\n                    \"icon-text-spacing\", self._icon_text_spacing\n                )\n            )\n            content_w = icon_w + spacing + text_w\n            content_h = max(text_h, icon_h)\n            # print(\n            #     f\"{self._text!r}: {content_w + 2 * pad_h} x \"\n            #     f\"{content_h + 2 * pad_v}\"\n            # )\n        elif icon_w:\n            content_w = icon_w\n            content_h = icon_h\n        else:\n            content_w = text_w\n            content_h = text_h\n\n        # --- word-wrap sizing -------------------------------------------\n        # When word-wrap is on and the widget already occupies a real width,\n        # recompute the height for that width so the hint stays accurate after\n        # the first layout pass.  Layouts will also call heightForWidth() for\n        # further refinement.\n        if self.wordWrap() and self._text and not self._icon:\n            available_w = self.width() - 2 * pad_h\n            if available_w &gt; 0:\n                flags = int(Qt.TextFlag.TextWordWrap) | int(self.alignment())\n                wrap_rect = fm.boundingRect(\n                    QRect(0, 0, available_w, 0),\n                    flags,\n                    self._text,\n                )\n                return QSize(\n                    content_w + 2 * pad_h,\n                    wrap_rect.height() + 2 * pad_v,\n                )\n\n        cm = self.contentsMargins()\n\n        return QSize(\n            content_w + 2 * pad_h + cm.left() + cm.right(),\n            content_h + 2 * pad_v + cm.top() + cm.bottom(),\n        )\n\n    def hasHeightForWidth(self) -&gt; bool:\n        \"\"\"Return True when word-wrap is active so layouts call heightForWidth.\n\n        Returns:\n            True if the label uses word-wrap and has text without an icon.\n        \"\"\"\n        if self.wordWrap() and self._text and not self._icon:\n            return True\n        return super().hasHeightForWidth()\n\n    def heightForWidth(self, width: int) -&gt; int:\n        \"\"\"Compute the height required to render the text wrapped to *width*.\n\n        Only meaningful when ``wordWrap`` is True and there is no icon;\n        delegates to the base class otherwise.\n\n        Args:\n            width: Available width in pixels.\n\n        Returns:\n            Required height in pixels.\n        \"\"\"\n        if not self.wordWrap() or not self._text or self._icon:\n            return super().heightForWidth(width)\n\n        assert isinstance(self.fontMetrics(), QFontMetrics)\n        fm = self.fontMetrics()\n\n        explicit_padding = self._style_data[\"base\"].get(\"padding\", [0, 0])\n        pad_h = int(explicit_padding[0])\n        pad_v = int(explicit_padding[1])\n\n        available_w = max(1, width - 2 * pad_h)\n        flags = int(Qt.TextFlag.TextWordWrap) | int(self.alignment())\n        wrap_rect = fm.boundingRect(\n            QRect(0, 0, available_w, 0),\n            flags,\n            self._text,\n        )\n        return wrap_rect.height() + 2 * pad_v\n\n    def setText(self, arg__1: str) -&gt; None:\n        super().setText(arg__1)\n        self._text = self.text()\n\n    def set_icon_color(self, color: str) -&gt; None:\n        \"\"\"Update the icon color and refresh the widget.\n\n        Resets contrast color cache so filled variants (e.g. Badge) and any\n        palette-based contrast logic are recalculated with the new color.\n\n        Args:\n            color: New hex color string e.g. '#44ee9f'.\n        \"\"\"\n        self._icon_color = color\n        self._contrast_color = None  # reset so contrast is recalculated\n        self._configure_palette()\n        self.set_icon(color=color)  # repaints icon pixmap with new color\n        self._copy_pix_normal = self._copy_pix_hover = self._copy_pix_done = (\n            None\n        )\n        self.update()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/label.html#client.ayon_core.ui.components.label.AYLabel.hasHeightForWidth","title":"<code>hasHeightForWidth()</code>","text":"<p>Return True when word-wrap is active so layouts call heightForWidth.</p> <p>Returns:</p> Type Description <code>bool</code> <p>True if the label uses word-wrap and has text without an icon.</p> Source code in <code>client/ayon_core/ui/components/label.py</code> <pre><code>def hasHeightForWidth(self) -&gt; bool:\n    \"\"\"Return True when word-wrap is active so layouts call heightForWidth.\n\n    Returns:\n        True if the label uses word-wrap and has text without an icon.\n    \"\"\"\n    if self.wordWrap() and self._text and not self._icon:\n        return True\n    return super().hasHeightForWidth()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/label.html#client.ayon_core.ui.components.label.AYLabel.heightForWidth","title":"<code>heightForWidth(width)</code>","text":"<p>Compute the height required to render the text wrapped to width.</p> <p>Only meaningful when <code>wordWrap</code> is True and there is no icon; delegates to the base class otherwise.</p> <p>Parameters:</p> Name Type Description Default <code>width</code> <code>int</code> <p>Available width in pixels.</p> required <p>Returns:</p> Type Description <code>int</code> <p>Required height in pixels.</p> Source code in <code>client/ayon_core/ui/components/label.py</code> <pre><code>def heightForWidth(self, width: int) -&gt; int:\n    \"\"\"Compute the height required to render the text wrapped to *width*.\n\n    Only meaningful when ``wordWrap`` is True and there is no icon;\n    delegates to the base class otherwise.\n\n    Args:\n        width: Available width in pixels.\n\n    Returns:\n        Required height in pixels.\n    \"\"\"\n    if not self.wordWrap() or not self._text or self._icon:\n        return super().heightForWidth(width)\n\n    assert isinstance(self.fontMetrics(), QFontMetrics)\n    fm = self.fontMetrics()\n\n    explicit_padding = self._style_data[\"base\"].get(\"padding\", [0, 0])\n    pad_h = int(explicit_padding[0])\n    pad_v = int(explicit_padding[1])\n\n    available_w = max(1, width - 2 * pad_h)\n    flags = int(Qt.TextFlag.TextWordWrap) | int(self.alignment())\n    wrap_rect = fm.boundingRect(\n        QRect(0, 0, available_w, 0),\n        flags,\n        self._text,\n    )\n    return wrap_rect.height() + 2 * pad_v\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/label.html#client.ayon_core.ui.components.label.AYLabel.paintEvent","title":"<code>paintEvent(arg__1)</code>","text":"<p>Override to support icon + text rendering.</p> Source code in <code>client/ayon_core/ui/components/label.py</code> <pre><code>def paintEvent(self, arg__1: QPaintEvent) -&gt; None:\n    \"\"\"Override to support icon + text rendering.\"\"\"\n    state = \"disabled\" if not self.isEnabled() else \"base\"\n    if (\n        state != \"disabled\"\n        and \"hover\" in self._style_data\n        and self.underMouse()\n    ):\n        state = \"hover\"\n\n    # Filled-background rendering (driven by JSON properties)\n    if self._style_data[\"base\"].get(\"fill-from-foreground\"):\n        self._paint_filled(state)\n    else:\n        self._paint_background(state)\n        if self._text and self._icon:\n            self._paint_icon_and_text(state)\n        elif self._icon and not self._text:\n            super().paintEvent(arg__1)\n        else:\n            self._paint_text_only(state)\n\n    if (\n        self._copy_text\n        and self.isEnabled()\n        and (self.underMouse() or self._copy_confirmed)\n    ):\n        self._paint_copy_icon()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/label.html#client.ayon_core.ui.components.label.AYLabel.set_font","title":"<code>set_font(font)</code>","text":"<p>Set the widget font and trigger a repaint.</p> Source code in <code>client/ayon_core/ui/components/label.py</code> <pre><code>def set_font(self, font: QFont) -&gt; None:\n    \"\"\"Set the widget font and trigger a repaint.\"\"\"\n    self._style_font = self._configure_font(font)\n    self.update()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/label.html#client.ayon_core.ui.components.label.AYLabel.set_icon_color","title":"<code>set_icon_color(color)</code>","text":"<p>Update the icon color and refresh the widget.</p> <p>Resets contrast color cache so filled variants (e.g. Badge) and any palette-based contrast logic are recalculated with the new color.</p> <p>Parameters:</p> Name Type Description Default <code>color</code> <code>str</code> <p>New hex color string e.g. '#44ee9f'.</p> required Source code in <code>client/ayon_core/ui/components/label.py</code> <pre><code>def set_icon_color(self, color: str) -&gt; None:\n    \"\"\"Update the icon color and refresh the widget.\n\n    Resets contrast color cache so filled variants (e.g. Badge) and any\n    palette-based contrast logic are recalculated with the new color.\n\n    Args:\n        color: New hex color string e.g. '#44ee9f'.\n    \"\"\"\n    self._icon_color = color\n    self._contrast_color = None  # reset so contrast is recalculated\n    self._configure_palette()\n    self.set_icon(color=color)  # repaints icon pixmap with new color\n    self._copy_pix_normal = self._copy_pix_hover = self._copy_pix_done = (\n        None\n    )\n    self.update()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/label.html#client.ayon_core.ui.components.label.AYLabel.set_palette","title":"<code>set_palette(palette)</code>","text":"<p>Set the widget palette and trigger a repaint.</p> Source code in <code>client/ayon_core/ui/components/label.py</code> <pre><code>def set_palette(self, palette: QPalette) -&gt; None:\n    \"\"\"Set the widget palette and trigger a repaint.\"\"\"\n    self._style_palette = palette\n    self._configure_palette()\n    if self._icon and not self._icon_color:\n        self.set_icon()  # update icon color from palette\n    self._copy_pix_normal = self._copy_pix_hover = self._copy_pix_done = (\n        None\n    )\n    self.update()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/label.html#client.ayon_core.ui.components.label.AYLabel.sizeHint","title":"<code>sizeHint()</code>","text":"<p>Compute a size hint driven by QLabel style data from the style JSON.</p> <p>For variants with <code>auto-size</code> (e.g. badge / pill), the size is derived from font-metrics and the <code>auto-size-padding</code> factor. For variants with an explicit <code>padding</code> list (e.g. entity-label), the size is padded accordingly. When an icon is present the icon dimensions are added. When <code>wordWrap</code> is True and the widget already has a non-zero width, the height is recomputed from the wrapped text bounds at that width; otherwise the natural single-line size is returned and the layout refines the height via :meth:<code>heightForWidth</code>. In all other cases the base <code>QLabel.sizeHint()</code> is returned.</p> <p>Returns:</p> Type Description <code>QSize</code> <p>The recommended widget size.</p> Source code in <code>client/ayon_core/ui/components/label.py</code> <pre><code>def sizeHint(self) -&gt; QSize:\n    \"\"\"Compute a size hint driven by QLabel style data from the style JSON.\n\n    For variants with ``auto-size`` (e.g. badge / pill), the size is\n    derived from font-metrics and the ``auto-size-padding`` factor.\n    For variants with an explicit ``padding`` list (e.g. entity-label),\n    the size is padded accordingly.\n    When an icon is present the icon dimensions are added.\n    When ``wordWrap`` is True and the widget already has a non-zero width,\n    the height is recomputed from the wrapped text bounds at that width;\n    otherwise the natural single-line size is returned and the layout\n    refines the height via :meth:`heightForWidth`.\n    In all other cases the base ``QLabel.sizeHint()`` is returned.\n\n    Returns:\n        The recommended widget size.\n    \"\"\"\n    fm = self.fontMetrics()\n\n    # --- text size --------------------------------------------------\n    if self._text:\n        t_rect = fm.boundingRect(self._text)\n        text_w = t_rect.width() + 1  # +1 pixel for antialiasing\n        text_h = t_rect.height()\n    else:\n        text_w = 0\n        text_h = fm.height()\n\n    # --- icon size --------------------------------------------------\n    icon_w = icon_h = 0\n    if self._icon:\n        icon_w = self._icon_size\n        icon_h = self._icon_size\n\n    # --- variant-specific sizing ------------------------------------\n    if self._style_data[\"base\"].get(\"auto-size\"):\n        # badge / pill: padding is expressed as a fraction of the\n        # character metrics (x-factor of avgCharWidth, y-factor of height)\n        padding = self._style_data[\"base\"].get(\n            \"auto-size-padding\", [0.0, 0.0]\n        )\n        pad_x = int(fm.averageCharWidth() * padding[0])\n        pad_y = int(fm.height() * padding[1])\n\n        content_w = max(text_w, icon_w)\n        content_h = max(text_h, icon_h)\n\n        return QSize(content_w + pad_x, content_h + pad_y)\n\n    explicit_padding = self._style_data[\"base\"].get(\"padding\", [0, 0])\n    pad_h = int(explicit_padding[0])\n    pad_v = int(explicit_padding[1])\n\n    if icon_w and text_w:\n        spacing = int(\n            self._style_data[\"base\"].get(\n                \"icon-text-spacing\", self._icon_text_spacing\n            )\n        )\n        content_w = icon_w + spacing + text_w\n        content_h = max(text_h, icon_h)\n        # print(\n        #     f\"{self._text!r}: {content_w + 2 * pad_h} x \"\n        #     f\"{content_h + 2 * pad_v}\"\n        # )\n    elif icon_w:\n        content_w = icon_w\n        content_h = icon_h\n    else:\n        content_w = text_w\n        content_h = text_h\n\n    # --- word-wrap sizing -------------------------------------------\n    # When word-wrap is on and the widget already occupies a real width,\n    # recompute the height for that width so the hint stays accurate after\n    # the first layout pass.  Layouts will also call heightForWidth() for\n    # further refinement.\n    if self.wordWrap() and self._text and not self._icon:\n        available_w = self.width() - 2 * pad_h\n        if available_w &gt; 0:\n            flags = int(Qt.TextFlag.TextWordWrap) | int(self.alignment())\n            wrap_rect = fm.boundingRect(\n                QRect(0, 0, available_w, 0),\n                flags,\n                self._text,\n            )\n            return QSize(\n                content_w + 2 * pad_h,\n                wrap_rect.height() + 2 * pad_v,\n            )\n\n    cm = self.contentsMargins()\n\n    return QSize(\n        content_w + 2 * pad_h + cm.left() + cm.right(),\n        content_h + 2 * pad_v + cm.top() + cm.bottom(),\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/layouts.html","title":"layouts","text":""},{"location":"autoapi/client/ayon_core/ui/components/layouts.html#client.ayon_core.ui.components.layouts.AYFlowLayout","title":"<code>AYFlowLayout</code>","text":"<p>               Bases: <code>QLayout</code></p> <p>Flow layout that wraps widgets horizontally, then vertically.</p> <p>This layout arranges widgets in a left-to-right flow, wrapping to the next line when the width is exceeded, similar to how text flows in a paragraph.</p> Source code in <code>client/ayon_core/ui/components/layouts.py</code> <pre><code>class AYFlowLayout(QtWidgets.QLayout):\n    \"\"\"Flow layout that wraps widgets horizontally, then vertically.\n\n    This layout arranges widgets in a left-to-right flow, wrapping to the\n    next line when the width is exceeded, similar to how text flows in a\n    paragraph.\n    \"\"\"\n\n    def __init__(self, parent=None, margin: int = 4, spacing: int = 4):\n        super().__init__(parent)\n        self._item_list = []\n        self._h_spacing = spacing\n        self._v_spacing = spacing\n        self.setContentsMargins(margin, margin, margin, margin)\n\n    def addItem(self, item):\n        \"\"\"Add an item to the layout.\"\"\"\n        self._item_list.append(item)\n\n    def horizontalSpacing(self) -&gt; int:\n        \"\"\"Get the horizontal spacing between items.\"\"\"\n        if self._h_spacing &gt;= 0:\n            return self._h_spacing\n        return self._smart_spacing(QtWidgets.QStyle.PM_LayoutHorizontalSpacing)\n\n    def verticalSpacing(self) -&gt; int:\n        \"\"\"Get the vertical spacing between items.\"\"\"\n        if self._v_spacing &gt;= 0:\n            return self._v_spacing\n        return self._smart_spacing(QtWidgets.QStyle.PM_LayoutVerticalSpacing)\n\n    def count(self) -&gt; int:\n        \"\"\"Return the number of items in the layout.\"\"\"\n        return len(self._item_list)\n\n    def itemAt(self, index: int) -&gt; Optional[QtWidgets.QLayoutItem]:\n        \"\"\"Get the item at the specified index.\"\"\"\n        if 0 &lt;= index &lt; len(self._item_list):\n            return self._item_list[index]\n        return None\n\n    def takeAt(self, index: int) -&gt; Optional[QtWidgets.QLayoutItem]:  # type: ignore\n        \"\"\"Remove and return the item at the specified index.\"\"\"\n        if 0 &lt;= index &lt; len(self._item_list):\n            return self._item_list.pop(index)\n        return None\n\n    def expandingDirections(self) -&gt; Qt.Orientations:\n        \"\"\"Return the expanding directions for this layout.\"\"\"\n        return Qt.Orientations(0)\n\n    def hasHeightForWidth(self) -&gt; bool:\n        \"\"\"Return True to indicate height depends on width.\"\"\"\n        return True\n\n    def heightForWidth(self, width: int) -&gt; int:\n        \"\"\"Calculate the height required for the given width.\"\"\"\n        return self._do_layout(QRect(0, 0, width, 0), True)\n\n    def setGeometry(self, rect: QRect) -&gt; None:\n        \"\"\"Set the geometry of the layout.\"\"\"\n        super().setGeometry(rect)\n        self._do_layout(rect, False)\n\n    def sizeHint(self) -&gt; QSize:\n        \"\"\"Return the preferred size of the layout.\"\"\"\n        return self.minimumSize()\n\n    def minimumSize(self) -&gt; QSize:\n        \"\"\"Calculate and return the minimum size for the layout.\"\"\"\n        size = QSize()\n        for item in self._item_list:\n            size = size.expandedTo(item.minimumSize())\n\n        margins = self.contentsMargins()\n        size += QSize(\n            margins.left() + margins.right(),\n            margins.top() + margins.bottom(),\n        )\n        return size\n\n    def _do_layout(self, rect: QRect, test_only: bool) -&gt; int:\n        \"\"\"Perform the layout calculation and positioning.\n\n        Args:\n            rect: The rectangle to lay out items within.\n            test_only: If True, only calculate height without setting\n                geometry.\n\n        Returns:\n            The height required for the layout.\n        \"\"\"\n        left, top, right, bottom = (\n            self.contentsMargins().left(),\n            self.contentsMargins().top(),\n            self.contentsMargins().right(),\n            self.contentsMargins().bottom(),\n        )\n        effective_rect = rect.adjusted(left, top, -right, -bottom)\n        x = effective_rect.x()\n        y = effective_rect.y()\n        line_height = 0\n\n        for item in self._item_list:\n            widget = item.widget()\n            space_x = self.horizontalSpacing()\n            if space_x == -1:\n                space_x = widget.style().layoutSpacing(\n                    QtWidgets.QSizePolicy.PushButton,\n                    QtWidgets.QSizePolicy.PushButton,\n                    Qt.Horizontal,\n                )\n\n            space_y = self.verticalSpacing()\n            if space_y == -1:\n                space_y = widget.style().layoutSpacing(\n                    QtWidgets.QSizePolicy.PushButton,\n                    QtWidgets.QSizePolicy.PushButton,\n                    Qt.Vertical,\n                )\n\n            next_x = x + item.sizeHint().width() + space_x\n            if next_x - space_x &gt; effective_rect.right() and line_height &gt; 0:\n                x = effective_rect.x()\n                y = y + line_height + space_y\n                next_x = x + item.sizeHint().width() + space_x\n                line_height = 0\n\n            if not test_only:\n                item.setGeometry(QRect(QPoint(x, y), item.sizeHint()))\n\n            x = next_x\n            line_height = max(line_height, item.sizeHint().height())\n\n        return y + line_height - rect.y() + bottom\n\n    def _smart_spacing(self, pm: int) -&gt; int:\n        \"\"\"Get smart spacing from the parent widget's style.\n\n        Args:\n            pm: The pixel metric to query.\n\n        Returns:\n            The spacing value, or -1 if unavailable.\n        \"\"\"\n        parent = self.parent()\n        if parent is None:\n            return -1\n        if parent.isWidgetType():\n            return parent.style().pixelMetric(pm, None, parent)\n        return parent.spacing()\n\n    def clear(self):\n        clear_layout(self)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/layouts.html#client.ayon_core.ui.components.layouts.AYFlowLayout.addItem","title":"<code>addItem(item)</code>","text":"<p>Add an item to the layout.</p> Source code in <code>client/ayon_core/ui/components/layouts.py</code> <pre><code>def addItem(self, item):\n    \"\"\"Add an item to the layout.\"\"\"\n    self._item_list.append(item)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/layouts.html#client.ayon_core.ui.components.layouts.AYFlowLayout.count","title":"<code>count()</code>","text":"<p>Return the number of items in the layout.</p> Source code in <code>client/ayon_core/ui/components/layouts.py</code> <pre><code>def count(self) -&gt; int:\n    \"\"\"Return the number of items in the layout.\"\"\"\n    return len(self._item_list)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/layouts.html#client.ayon_core.ui.components.layouts.AYFlowLayout.expandingDirections","title":"<code>expandingDirections()</code>","text":"<p>Return the expanding directions for this layout.</p> Source code in <code>client/ayon_core/ui/components/layouts.py</code> <pre><code>def expandingDirections(self) -&gt; Qt.Orientations:\n    \"\"\"Return the expanding directions for this layout.\"\"\"\n    return Qt.Orientations(0)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/layouts.html#client.ayon_core.ui.components.layouts.AYFlowLayout.hasHeightForWidth","title":"<code>hasHeightForWidth()</code>","text":"<p>Return True to indicate height depends on width.</p> Source code in <code>client/ayon_core/ui/components/layouts.py</code> <pre><code>def hasHeightForWidth(self) -&gt; bool:\n    \"\"\"Return True to indicate height depends on width.\"\"\"\n    return True\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/layouts.html#client.ayon_core.ui.components.layouts.AYFlowLayout.heightForWidth","title":"<code>heightForWidth(width)</code>","text":"<p>Calculate the height required for the given width.</p> Source code in <code>client/ayon_core/ui/components/layouts.py</code> <pre><code>def heightForWidth(self, width: int) -&gt; int:\n    \"\"\"Calculate the height required for the given width.\"\"\"\n    return self._do_layout(QRect(0, 0, width, 0), True)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/layouts.html#client.ayon_core.ui.components.layouts.AYFlowLayout.horizontalSpacing","title":"<code>horizontalSpacing()</code>","text":"<p>Get the horizontal spacing between items.</p> Source code in <code>client/ayon_core/ui/components/layouts.py</code> <pre><code>def horizontalSpacing(self) -&gt; int:\n    \"\"\"Get the horizontal spacing between items.\"\"\"\n    if self._h_spacing &gt;= 0:\n        return self._h_spacing\n    return self._smart_spacing(QtWidgets.QStyle.PM_LayoutHorizontalSpacing)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/layouts.html#client.ayon_core.ui.components.layouts.AYFlowLayout.itemAt","title":"<code>itemAt(index)</code>","text":"<p>Get the item at the specified index.</p> Source code in <code>client/ayon_core/ui/components/layouts.py</code> <pre><code>def itemAt(self, index: int) -&gt; Optional[QtWidgets.QLayoutItem]:\n    \"\"\"Get the item at the specified index.\"\"\"\n    if 0 &lt;= index &lt; len(self._item_list):\n        return self._item_list[index]\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/layouts.html#client.ayon_core.ui.components.layouts.AYFlowLayout.minimumSize","title":"<code>minimumSize()</code>","text":"<p>Calculate and return the minimum size for the layout.</p> Source code in <code>client/ayon_core/ui/components/layouts.py</code> <pre><code>def minimumSize(self) -&gt; QSize:\n    \"\"\"Calculate and return the minimum size for the layout.\"\"\"\n    size = QSize()\n    for item in self._item_list:\n        size = size.expandedTo(item.minimumSize())\n\n    margins = self.contentsMargins()\n    size += QSize(\n        margins.left() + margins.right(),\n        margins.top() + margins.bottom(),\n    )\n    return size\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/layouts.html#client.ayon_core.ui.components.layouts.AYFlowLayout.setGeometry","title":"<code>setGeometry(rect)</code>","text":"<p>Set the geometry of the layout.</p> Source code in <code>client/ayon_core/ui/components/layouts.py</code> <pre><code>def setGeometry(self, rect: QRect) -&gt; None:\n    \"\"\"Set the geometry of the layout.\"\"\"\n    super().setGeometry(rect)\n    self._do_layout(rect, False)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/layouts.html#client.ayon_core.ui.components.layouts.AYFlowLayout.sizeHint","title":"<code>sizeHint()</code>","text":"<p>Return the preferred size of the layout.</p> Source code in <code>client/ayon_core/ui/components/layouts.py</code> <pre><code>def sizeHint(self) -&gt; QSize:\n    \"\"\"Return the preferred size of the layout.\"\"\"\n    return self.minimumSize()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/layouts.html#client.ayon_core.ui.components.layouts.AYFlowLayout.takeAt","title":"<code>takeAt(index)</code>","text":"<p>Remove and return the item at the specified index.</p> Source code in <code>client/ayon_core/ui/components/layouts.py</code> <pre><code>def takeAt(self, index: int) -&gt; Optional[QtWidgets.QLayoutItem]:  # type: ignore\n    \"\"\"Remove and return the item at the specified index.\"\"\"\n    if 0 &lt;= index &lt; len(self._item_list):\n        return self._item_list.pop(index)\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/layouts.html#client.ayon_core.ui.components.layouts.AYFlowLayout.verticalSpacing","title":"<code>verticalSpacing()</code>","text":"<p>Get the vertical spacing between items.</p> Source code in <code>client/ayon_core/ui/components/layouts.py</code> <pre><code>def verticalSpacing(self) -&gt; int:\n    \"\"\"Get the vertical spacing between items.\"\"\"\n    if self._v_spacing &gt;= 0:\n        return self._v_spacing\n    return self._smart_spacing(QtWidgets.QStyle.PM_LayoutVerticalSpacing)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/line_edit.html","title":"line_edit","text":"<p>AYLineEdit component module.</p>"},{"location":"autoapi/client/ayon_core/ui/components/line_edit.html#client.ayon_core.ui.components.line_edit.AYLineEdit","title":"<code>AYLineEdit</code>","text":"<p>               Bases: <code>StyleMixin</code>, <code>QLineEdit</code></p> <p>Custom styled line edit component.</p> <p>Inherits from QLineEdit and uses the AYON style system for rendering. Paints its own background, border, and focus ring using ayon_style.json data, then calls super().paintEvent() to draw text, cursor, and selection on top.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <code>QWidget | None</code> <p>Parent widget.</p> <code>None</code> <code>placeholder</code> <code>str</code> <p>Placeholder text to display when empty.</p> <code>''</code> <code>variant</code> <code>QLineEditVariants</code> <p>Visual style variant.</p> <code>Default</code> <code>name_id</code> <code>str</code> <p>Object name for identification.</p> <code>''</code> Source code in <code>client/ayon_core/ui/components/line_edit.py</code> <pre><code>class AYLineEdit(StyleMixin, QLineEdit):\n    \"\"\"Custom styled line edit component.\n\n    Inherits from QLineEdit and uses the AYON style system for rendering.\n    Paints its own background, border, and focus ring using ayon_style.json\n    data, then calls super().paintEvent() to draw text, cursor, and\n    selection on top.\n\n    Args:\n        parent: Parent widget.\n        placeholder: Placeholder text to display when empty.\n        variant: Visual style variant.\n        name_id: Object name for identification.\n    \"\"\"\n\n    Variants = QLineEditVariants\n\n    def __init__(\n        self,\n        parent: QWidget | None = None,\n        placeholder: str = \"\",\n        variant: QLineEditVariants = QLineEditVariants.Default,\n        name_id: str = \"\",\n    ) -&gt; None:\n        super().__init__(parent)\n\n        self._variant_str = variant.value\n        self._pal: QPalette | None = None\n        self._variant_styles = {}\n\n        if placeholder:\n            self.setPlaceholderText(placeholder)\n\n        if name_id:\n            self.setObjectName(name_id)\n\n        # Suppress the native Qt frame so initStyleOption reports lineWidth=0\n        self.setFrame(False)\n\n        # Neutralise any ancestor stylesheet that would intercept\n        # PE_PanelLineEdit via QStyleSheetStyle, making it visually\n        # transparent.\n        self.setStyleSheet(\n            \"AYLineEdit { background: transparent; border: none; \"\n            \"padding: 0px; selection-background-color: none; \"\n            \"selection-color: none; }\"\n        )\n\n        # Suppress macOS native focus ring (we draw our own)\n        self.setAttribute(Qt.WidgetAttribute.WA_MacShowFocusRect, False)\n\n        # Enable hover events so underMouse() is reliable during paintEvent\n        self.setAttribute(Qt.WidgetAttribute.WA_Hover, True)\n\n        self._apply_style_palette()\n\n        #  this must be called after the palette has been set for it to stick.\n        self.setStyle(get_ayon_style())\n\n    @property\n    def ayon_palette(self) -&gt; QPalette:\n        \"\"\"Return the palette used for this widget.\"\"\"\n        if self._pal is None:\n            self._apply_style_palette()\n        return self._pal\n\n    def variant_style(self, state=None) -&gt; dict:\n        \"\"\"Return the style dict for the current variant.\"\"\"\n        key = (self._variant_str, state)\n        if key not in self._variant_styles:\n            model = get_ayon_style().model\n            self._variant_styles[key] = model.get_style(\n                \"QLineEdit\", variant=self._variant_str, state=state\n            )\n            self._variant_styles[key].set_context(self)\n        return self._variant_styles[key]\n\n    def _apply_style_palette(self) -&gt; None:\n        \"\"\"Push text / placeholder colors and padding from ayon_style.json.\"\"\"\n        style = self.variant_style()\n\n        self._pal = self.palette()\n\n        text_color = QColor(style.get(\"color\", \"#ffffff\"))\n        self._pal.setColor(QPalette.ColorRole.Text, text_color)\n        self._pal.setColor(QPalette.ColorRole.BrightText, text_color)\n\n        ph_color = QColor(style.get(\"placeholder-color\", \"#888888\"))\n        self._pal.setColor(QPalette.ColorRole.PlaceholderText, ph_color)\n\n        self._pal.setColor(\n            QPalette.ColorRole.Highlight,\n            QColor(style.get(\"selection-background-color\", \"#4040dd\")),\n        )\n        self._pal.setColor(\n            QPalette.ColorRole.HighlightedText,\n            QColor(style.get(\"selection-color\", \"#ffffff\")),\n        )\n\n        # Transparent base so the background rect we draw is visible\n        self._pal.setColor(QPalette.ColorRole.Base, QColor(0, 0, 0, 0))\n\n        self.setPalette(self._pal)\n\n        # Apply padding as text margins (immune to QSS interception)\n        padding = style.get(\"padding\", [8, 4])\n        pad_h = padding[0]\n        pad_v = padding[1]\n        icon_width = 0\n        if style.get(\"icon\"):\n            icon_width = style.get(\"icon-size\", 16) + style.get(\n                \"icon-padding\", 8\n            )\n        self.setTextMargins(pad_h + icon_width, pad_v, pad_h, pad_v)\n\n    def initStyleOption(self, option: QStyleOptionFrame) -&gt; None:\n        \"\"\"Override the palette used by the style to paint the widget.\"\"\"\n        self.setPalette(self.ayon_palette)\n        super().initStyleOption(option)\n\n    def paintEvent(self, event: QPaintEvent) -&gt; None:\n        \"\"\"Paint background, border, and focus ring, then delegate text\n        rendering.\n\n        Draws the styled background rectangle and border first using QPainter\n        directly (no style or stylesheet involvement), then calls\n        super().paintEvent() which renders the text, cursor, and selection\n        highlight on top.  Qt's PE_PanelLineEdit call inside that base\n        implementation is intercepted by LineEditDrawer which returns\n        immediately for AYLineEdit instances, so our background is preserved.\n        \"\"\"\n\n        is_disabled = not self.isEnabled()\n        is_hover = self.underMouse()\n        has_focus = self.hasFocus()\n\n        if is_disabled:\n            state = \"disabled\"\n        elif is_hover and not has_focus:\n            state = \"hover\"\n        else:\n            state = \"base\"\n\n        style = self.variant_style(state)\n\n        bg_color = QColor(style.get(\"background-color\", \"#272d35\"))\n        border_color = QColor(style.get(\"border-color\", \"#41474d\"))\n        border_width = style.get(\"border-width\", 1)\n        border_radius = style.get(\"border-radius\", 2)\n        opacity = style.get(\"opacity\", 1.0)\n\n        focus_outline_width = style.get(\"focus-outline-width\", 2)\n        focus_outline_color = QColor(\n            style.get(\"focus-outline-color\", \"#8fceff\")\n        )\n\n        painter = QPainter(self)\n        painter.setRenderHint(QPainter.RenderHint.Antialiasing)\n        painter.setOpacity(opacity)\n        painter.setFont(self.font())\n\n        rect = QRectF(self.rect())\n        half_bw = border_width / 2.0\n\n        # Background\n        bg_rect = rect.adjusted(half_bw, half_bw, -half_bw, -half_bw)\n        painter.setPen(Qt.PenStyle.NoPen)\n        painter.setBrush(QBrush(bg_color))\n        painter.drawRoundedRect(bg_rect, border_radius, border_radius)\n\n        # icon\n        icon_name = style.get(\"icon\")\n        if icon_name:\n            icon_color = QColor(style.get(\"icon-color\", \"#888888\"))\n            icon_size = style.get(\"icon-size\", 16)\n            x = style.get(\"padding\", [8, 4])[0]\n            pixmap = get_icon(icon_name, color=icon_color).pixmap(\n                icon_size, icon_size\n            )\n            y = (rect.height() - icon_size) / 2.0\n            painter.drawPixmap(x, y, icon_size, icon_size, pixmap)\n\n        # Border\n        border_pen = QPen(border_color)\n        border_pen.setWidthF(border_width)\n        painter.setPen(border_pen)\n        painter.setBrush(Qt.BrushStyle.NoBrush)\n        painter.drawRoundedRect(bg_rect, border_radius, border_radius)\n\n        # Focus ring (drawn inset so it fits within the widget rect)\n        if has_focus:\n            half_fw = focus_outline_width / 2.0\n            focus_rect = rect.adjusted(half_fw, half_fw, -half_fw, -half_fw)\n            focus_pen = QPen(focus_outline_color)\n            focus_pen.setWidthF(focus_outline_width)\n            painter.setPen(focus_pen)\n            painter.setBrush(Qt.BrushStyle.NoBrush)\n            painter.drawRoundedRect(focus_rect, border_radius, border_radius)\n\n        painter.end()\n\n        # Let QLineEdit draw text, placeholder, cursor, and selection on top.\n        # LineEditDrawer intercepts PE_PanelLineEdit for AYLineEdit and is a\n        # no-op, so the background we just drew is preserved.\n        super().paintEvent(event)\n\n    def sizeHint(self) -&gt; QSize:\n        \"\"\"Override sizeHint to account for padding.\"\"\"\n        size = super().sizeHint()\n        style = self.variant_style()\n        padding = style.get(\"padding\", [8, 4])\n        size.setWidth(size.width() + padding[0] * 2)\n        size.setHeight(size.height() + padding[1] * 2)\n        return size\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/line_edit.html#client.ayon_core.ui.components.line_edit.AYLineEdit.ayon_palette","title":"<code>ayon_palette</code>  <code>property</code>","text":"<p>Return the palette used for this widget.</p>"},{"location":"autoapi/client/ayon_core/ui/components/line_edit.html#client.ayon_core.ui.components.line_edit.AYLineEdit.initStyleOption","title":"<code>initStyleOption(option)</code>","text":"<p>Override the palette used by the style to paint the widget.</p> Source code in <code>client/ayon_core/ui/components/line_edit.py</code> <pre><code>def initStyleOption(self, option: QStyleOptionFrame) -&gt; None:\n    \"\"\"Override the palette used by the style to paint the widget.\"\"\"\n    self.setPalette(self.ayon_palette)\n    super().initStyleOption(option)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/line_edit.html#client.ayon_core.ui.components.line_edit.AYLineEdit.paintEvent","title":"<code>paintEvent(event)</code>","text":"<p>Paint background, border, and focus ring, then delegate text rendering.</p> <p>Draws the styled background rectangle and border first using QPainter directly (no style or stylesheet involvement), then calls super().paintEvent() which renders the text, cursor, and selection highlight on top.  Qt's PE_PanelLineEdit call inside that base implementation is intercepted by LineEditDrawer which returns immediately for AYLineEdit instances, so our background is preserved.</p> Source code in <code>client/ayon_core/ui/components/line_edit.py</code> <pre><code>def paintEvent(self, event: QPaintEvent) -&gt; None:\n    \"\"\"Paint background, border, and focus ring, then delegate text\n    rendering.\n\n    Draws the styled background rectangle and border first using QPainter\n    directly (no style or stylesheet involvement), then calls\n    super().paintEvent() which renders the text, cursor, and selection\n    highlight on top.  Qt's PE_PanelLineEdit call inside that base\n    implementation is intercepted by LineEditDrawer which returns\n    immediately for AYLineEdit instances, so our background is preserved.\n    \"\"\"\n\n    is_disabled = not self.isEnabled()\n    is_hover = self.underMouse()\n    has_focus = self.hasFocus()\n\n    if is_disabled:\n        state = \"disabled\"\n    elif is_hover and not has_focus:\n        state = \"hover\"\n    else:\n        state = \"base\"\n\n    style = self.variant_style(state)\n\n    bg_color = QColor(style.get(\"background-color\", \"#272d35\"))\n    border_color = QColor(style.get(\"border-color\", \"#41474d\"))\n    border_width = style.get(\"border-width\", 1)\n    border_radius = style.get(\"border-radius\", 2)\n    opacity = style.get(\"opacity\", 1.0)\n\n    focus_outline_width = style.get(\"focus-outline-width\", 2)\n    focus_outline_color = QColor(\n        style.get(\"focus-outline-color\", \"#8fceff\")\n    )\n\n    painter = QPainter(self)\n    painter.setRenderHint(QPainter.RenderHint.Antialiasing)\n    painter.setOpacity(opacity)\n    painter.setFont(self.font())\n\n    rect = QRectF(self.rect())\n    half_bw = border_width / 2.0\n\n    # Background\n    bg_rect = rect.adjusted(half_bw, half_bw, -half_bw, -half_bw)\n    painter.setPen(Qt.PenStyle.NoPen)\n    painter.setBrush(QBrush(bg_color))\n    painter.drawRoundedRect(bg_rect, border_radius, border_radius)\n\n    # icon\n    icon_name = style.get(\"icon\")\n    if icon_name:\n        icon_color = QColor(style.get(\"icon-color\", \"#888888\"))\n        icon_size = style.get(\"icon-size\", 16)\n        x = style.get(\"padding\", [8, 4])[0]\n        pixmap = get_icon(icon_name, color=icon_color).pixmap(\n            icon_size, icon_size\n        )\n        y = (rect.height() - icon_size) / 2.0\n        painter.drawPixmap(x, y, icon_size, icon_size, pixmap)\n\n    # Border\n    border_pen = QPen(border_color)\n    border_pen.setWidthF(border_width)\n    painter.setPen(border_pen)\n    painter.setBrush(Qt.BrushStyle.NoBrush)\n    painter.drawRoundedRect(bg_rect, border_radius, border_radius)\n\n    # Focus ring (drawn inset so it fits within the widget rect)\n    if has_focus:\n        half_fw = focus_outline_width / 2.0\n        focus_rect = rect.adjusted(half_fw, half_fw, -half_fw, -half_fw)\n        focus_pen = QPen(focus_outline_color)\n        focus_pen.setWidthF(focus_outline_width)\n        painter.setPen(focus_pen)\n        painter.setBrush(Qt.BrushStyle.NoBrush)\n        painter.drawRoundedRect(focus_rect, border_radius, border_radius)\n\n    painter.end()\n\n    # Let QLineEdit draw text, placeholder, cursor, and selection on top.\n    # LineEditDrawer intercepts PE_PanelLineEdit for AYLineEdit and is a\n    # no-op, so the background we just drew is preserved.\n    super().paintEvent(event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/line_edit.html#client.ayon_core.ui.components.line_edit.AYLineEdit.sizeHint","title":"<code>sizeHint()</code>","text":"<p>Override sizeHint to account for padding.</p> Source code in <code>client/ayon_core/ui/components/line_edit.py</code> <pre><code>def sizeHint(self) -&gt; QSize:\n    \"\"\"Override sizeHint to account for padding.\"\"\"\n    size = super().sizeHint()\n    style = self.variant_style()\n    padding = style.get(\"padding\", [8, 4])\n    size.setWidth(size.width() + padding[0] * 2)\n    size.setHeight(size.height() + padding[1] * 2)\n    return size\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/line_edit.html#client.ayon_core.ui.components.line_edit.AYLineEdit.variant_style","title":"<code>variant_style(state=None)</code>","text":"<p>Return the style dict for the current variant.</p> Source code in <code>client/ayon_core/ui/components/line_edit.py</code> <pre><code>def variant_style(self, state=None) -&gt; dict:\n    \"\"\"Return the style dict for the current variant.\"\"\"\n    key = (self._variant_str, state)\n    if key not in self._variant_styles:\n        model = get_ayon_style().model\n        self._variant_styles[key] = model.get_style(\n            \"QLineEdit\", variant=self._variant_str, state=state\n        )\n        self._variant_styles[key].set_context(self)\n    return self._variant_styles[key]\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/option_action.html","title":"option_action","text":"<p>Menu action with an option box button (Maya-style option box pattern).</p> <p>Provides three classes that together implement a menu item composed of a standard action area (icon + label) and a small option button on the far right.  Clicking the main area fires the normal <code>triggered</code> signal; clicking the option button emits <code>OptionalAction.option_clicked</code> instead.</p> <p>Typical usage::</p> <pre><code>menu = QMenu(\"My Menu\", parent)\naction = OptionalAction(\n    label=\"Run Process\",\n    icon_name=\"play_arrow\",\n    use_option=True,\n    parent=menu,\n)\naction.triggered.connect(lambda: run_process())\naction.option_clicked.connect(lambda: open_options_dialog())\nmenu.addAction(action)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/option_action.html#client.ayon_core.ui.components.option_action.AYMenu","title":"<code>AYMenu</code>","text":"<p>               Bases: <code>QMenu</code></p> <p>QMenu that paints itself using the AYON style.</p> <p>Replicates :meth:<code>QMenu.paintEvent</code> but routes every primitive and control draw call through :func:<code>get_ayon_style</code>, so the menu is painted consistently with the rest of the AYON UI even when the application style isn't AYONStyle.</p> Source code in <code>client/ayon_core/ui/components/option_action.py</code> <pre><code>class AYMenu(QtWidgets.QMenu):\n    \"\"\"QMenu that paints itself using the AYON style.\n\n    Replicates :meth:`QMenu.paintEvent` but routes every primitive and\n    control draw call through :func:`get_ayon_style`, so the menu is\n    painted consistently with the rest of the AYON UI even when the\n    application style isn't AYONStyle.\n    \"\"\"\n\n    def __init__(self, *args, **kwargs) -&gt; None:\n        super().__init__(*args, **kwargs)\n        # Force the menu to use AYONStyle for drawing, even if the app's style\n        # is something else.\n        self.setStyle(get_ayon_style())\n\n    def paintEvent(self, arg__1: QtGui.QPaintEvent) -&gt; None:\n        \"\"\"Paint the menu using AYON's QStyle implementation.\n\n        Mirrors Qt's own ``QMenu::paintEvent`` order:\n        1. Draw the menu panel background (``PE_PanelMenu``).\n        2. Draw each visible action row (``CE_MenuItem``).\n        3. Draw the menu frame on top (``PE_FrameMenu``).\n\n        Args:\n            arg__1: The paint event delivered by Qt.\n        \"\"\"\n        style = get_ayon_style()\n        painter = QtGui.QPainter(self)\n\n        # --- Shared base option (used for panel + frame) ---\n        menu_opt = QtWidgets.QStyleOptionMenuItem()\n        menu_opt.initFrom(self)\n        menu_opt.state = QtWidgets.QStyle.StateFlag.State_None\n        menu_opt.checkType = (\n            QtWidgets.QStyleOptionMenuItem.CheckType.NotCheckable\n        )\n        menu_opt.maxIconWidth = 0\n        try:\n            menu_opt.reservedShortcutWidth = 0\n        except AttributeError:\n            # Older Qt versions expose tabWidth instead.\n            menu_opt.tabWidth = 0\n        menu_opt.rect = self.rect()\n        menu_opt.menuRect = self.rect()\n\n        # --- 1. Panel background ---\n        style.drawPrimitive(\n            QtWidgets.QStyle.PrimitiveElement.PE_PanelMenu,\n            menu_opt,\n            painter,\n            self,\n        )\n\n        # --- 2. Action rows ---\n        event_region = arg__1.region()\n        for action in self.actions():\n            action_rect = self.actionGeometry(action)\n            if not event_region.intersects(action_rect):\n                continue\n\n            opt = QtWidgets.QStyleOptionMenuItem()\n            self.initStyleOption(opt, action)\n            opt.rect = action_rect\n\n            style.drawControl(\n                QtWidgets.QStyle.ControlElement.CE_MenuItem,\n                opt,\n                painter,\n                self,\n            )\n\n        # --- 3. Frame on top ---\n        frame_width = style.pixelMetric(\n            QtWidgets.QStyle.PixelMetric.PM_MenuPanelWidth, menu_opt, self\n        )\n        if frame_width &gt; 0:\n            frame_opt = QtWidgets.QStyleOptionFrame()\n            frame_opt.initFrom(self)\n            frame_opt.rect = self.rect()\n            frame_opt.state = QtWidgets.QStyle.StateFlag.State_None\n            frame_opt.lineWidth = frame_width\n            frame_opt.midLineWidth = 0\n            style.drawPrimitive(\n                QtWidgets.QStyle.PrimitiveElement.PE_FrameMenu,\n                frame_opt,\n                painter,\n                self,\n            )\n\n        painter.end()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/option_action.html#client.ayon_core.ui.components.option_action.AYMenu.paintEvent","title":"<code>paintEvent(arg__1)</code>","text":"<p>Paint the menu using AYON's QStyle implementation.</p> <p>Mirrors Qt's own <code>QMenu::paintEvent</code> order: 1. Draw the menu panel background (<code>PE_PanelMenu</code>). 2. Draw each visible action row (<code>CE_MenuItem</code>). 3. Draw the menu frame on top (<code>PE_FrameMenu</code>).</p> <p>Parameters:</p> Name Type Description Default <code>arg__1</code> <code>QPaintEvent</code> <p>The paint event delivered by Qt.</p> required Source code in <code>client/ayon_core/ui/components/option_action.py</code> <pre><code>def paintEvent(self, arg__1: QtGui.QPaintEvent) -&gt; None:\n    \"\"\"Paint the menu using AYON's QStyle implementation.\n\n    Mirrors Qt's own ``QMenu::paintEvent`` order:\n    1. Draw the menu panel background (``PE_PanelMenu``).\n    2. Draw each visible action row (``CE_MenuItem``).\n    3. Draw the menu frame on top (``PE_FrameMenu``).\n\n    Args:\n        arg__1: The paint event delivered by Qt.\n    \"\"\"\n    style = get_ayon_style()\n    painter = QtGui.QPainter(self)\n\n    # --- Shared base option (used for panel + frame) ---\n    menu_opt = QtWidgets.QStyleOptionMenuItem()\n    menu_opt.initFrom(self)\n    menu_opt.state = QtWidgets.QStyle.StateFlag.State_None\n    menu_opt.checkType = (\n        QtWidgets.QStyleOptionMenuItem.CheckType.NotCheckable\n    )\n    menu_opt.maxIconWidth = 0\n    try:\n        menu_opt.reservedShortcutWidth = 0\n    except AttributeError:\n        # Older Qt versions expose tabWidth instead.\n        menu_opt.tabWidth = 0\n    menu_opt.rect = self.rect()\n    menu_opt.menuRect = self.rect()\n\n    # --- 1. Panel background ---\n    style.drawPrimitive(\n        QtWidgets.QStyle.PrimitiveElement.PE_PanelMenu,\n        menu_opt,\n        painter,\n        self,\n    )\n\n    # --- 2. Action rows ---\n    event_region = arg__1.region()\n    for action in self.actions():\n        action_rect = self.actionGeometry(action)\n        if not event_region.intersects(action_rect):\n            continue\n\n        opt = QtWidgets.QStyleOptionMenuItem()\n        self.initStyleOption(opt, action)\n        opt.rect = action_rect\n\n        style.drawControl(\n            QtWidgets.QStyle.ControlElement.CE_MenuItem,\n            opt,\n            painter,\n            self,\n        )\n\n    # --- 3. Frame on top ---\n    frame_width = style.pixelMetric(\n        QtWidgets.QStyle.PixelMetric.PM_MenuPanelWidth, menu_opt, self\n    )\n    if frame_width &gt; 0:\n        frame_opt = QtWidgets.QStyleOptionFrame()\n        frame_opt.initFrom(self)\n        frame_opt.rect = self.rect()\n        frame_opt.state = QtWidgets.QStyle.StateFlag.State_None\n        frame_opt.lineWidth = frame_width\n        frame_opt.midLineWidth = 0\n        style.drawPrimitive(\n            QtWidgets.QStyle.PrimitiveElement.PE_FrameMenu,\n            frame_opt,\n            painter,\n            self,\n        )\n\n    painter.end()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/option_action.html#client.ayon_core.ui.components.option_action.AYOptionBox","title":"<code>AYOptionBox</code>","text":"<p>               Bases: <code>AYButton</code></p> <p>Option box widget used as the right-hand button in an action row.</p> <p>Emits :attr:<code>clicked</code> when the user presses this button.  It is a standard :class:<code>AYButton</code> styled with the <code>Optional_Action</code> variant.</p> Source code in <code>client/ayon_core/ui/components/option_action.py</code> <pre><code>class AYOptionBox(AYButton):\n    \"\"\"Option box widget used as the right-hand button in an action row.\n\n    Emits :attr:`clicked` when the user presses this button.  It is a\n    standard :class:`AYButton` styled with the ``Optional_Action``\n    variant.\n    \"\"\"\n\n    def __init__(\n        self,\n        icon_name: str = \"check_box_outline_blank\",\n        icon_size: int = 16,\n        parent: QtWidgets.QWidget | None = None,\n    ) -&gt; None:\n        super().__init__(\n            parent,\n            variant=AYButton.Variants.Optional_Action,\n            icon=icon_name,\n            icon_size=icon_size,\n            fixed_width=False,\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/option_action.html#client.ayon_core.ui.components.option_action.AYOptionalAction","title":"<code>AYOptionalAction</code>","text":"<p>               Bases: <code>QWidgetAction</code></p> <p>Menu action with an optional right-hand option box button.</p> <p>Subclasses :class:<code>QtWidgets.QWidgetAction</code> to embed a custom :class:<code>AYOptionalActionWidget</code> inside a standard <code>QMenu</code>.</p> <p>Set <code>use_option=True</code> to show the option box and connect to :attr:<code>option_clicked</code> for the secondary action.</p> <p>Parameters:</p> Name Type Description Default <code>label</code> <code>str</code> <p>Display text.</p> required <code>icon_name</code> <code>str | None</code> <p>Material symbol icon name (or <code>\"none\"</code>).</p> <code>'none'</code> <code>use_option</code> <code>bool</code> <p>Whether to show the option box button.</p> <code>True</code> <code>parent</code> <code>QWidget | None</code> <p>Parent widget (typically the owning menu).</p> <code>None</code> Source code in <code>client/ayon_core/ui/components/option_action.py</code> <pre><code>class AYOptionalAction(QtWidgets.QWidgetAction):\n    \"\"\"Menu action with an optional right-hand option box button.\n\n    Subclasses :class:`QtWidgets.QWidgetAction` to embed a custom\n    :class:`AYOptionalActionWidget` inside a standard ``QMenu``.\n\n    Set ``use_option=True`` to show the option box and connect to\n    :attr:`option_clicked` for the secondary action.\n\n    Args:\n        label: Display text.\n        icon_name: Material symbol icon name (or ``\"none\"``).\n        use_option: Whether to show the option box button.\n        parent: Parent widget (typically the owning menu).\n    \"\"\"\n\n    option_clicked = QtCore.Signal()\n\n    def __init__(\n        self,\n        label: str,\n        icon_name: str | None = \"none\",\n        use_option: bool = True,\n        parent: QtWidgets.QWidget | None = None,\n    ) -&gt; None:\n        super().__init__(parent)\n        self._label = label\n        self._icon_name = icon_name or \"none\"\n        self._use_option = use_option\n        self.widget: AYOptionalActionWidget | None = None\n\n    def createWidget(self, parent: QtWidgets.QWidget) -&gt; QtWidgets.QWidget:\n        \"\"\"Instantiate and configure the custom action row widget.\n\n        Called by Qt when the action is added to a menu.\n\n        Args:\n            parent: The menu widget that will own the row widget.\n\n        Returns:\n            The newly created :class:`AYOptionalActionWidget`.\n        \"\"\"\n        widget = AYOptionalActionWidget(\n            self._label,\n            icon_name=self._icon_name,\n            parent=parent,\n        )\n        widget.setEnabled(self.isEnabled())\n        self.widget = widget\n\n        if self._use_option:\n            widget.option.clicked.connect(self.option_clicked.emit)\n            widget.option.clicked.connect(self._close_menu_chain)\n        else:\n            widget.option.setVisible(False)\n\n        return widget\n\n    def _close_menu_chain(self) -&gt; None:\n        \"\"\"Close the menu (and any parent menus) hosting this action.\"\"\"\n        w = self.widget\n        while w is not None:\n            if isinstance(w, QtWidgets.QMenu):\n                w.close()\n            w = w.parentWidget()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/option_action.html#client.ayon_core.ui.components.option_action.AYOptionalAction.createWidget","title":"<code>createWidget(parent)</code>","text":"<p>Instantiate and configure the custom action row widget.</p> <p>Called by Qt when the action is added to a menu.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <code>QWidget</code> <p>The menu widget that will own the row widget.</p> required <p>Returns:</p> Type Description <code>QWidget</code> <p>The newly created :class:<code>AYOptionalActionWidget</code>.</p> Source code in <code>client/ayon_core/ui/components/option_action.py</code> <pre><code>def createWidget(self, parent: QtWidgets.QWidget) -&gt; QtWidgets.QWidget:\n    \"\"\"Instantiate and configure the custom action row widget.\n\n    Called by Qt when the action is added to a menu.\n\n    Args:\n        parent: The menu widget that will own the row widget.\n\n    Returns:\n        The newly created :class:`AYOptionalActionWidget`.\n    \"\"\"\n    widget = AYOptionalActionWidget(\n        self._label,\n        icon_name=self._icon_name,\n        parent=parent,\n    )\n    widget.setEnabled(self.isEnabled())\n    self.widget = widget\n\n    if self._use_option:\n        widget.option.clicked.connect(self.option_clicked.emit)\n        widget.option.clicked.connect(self._close_menu_chain)\n    else:\n        widget.option.setVisible(False)\n\n    return widget\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/option_action.html#client.ayon_core.ui.components.option_action.AYOptionalActionWidget","title":"<code>AYOptionalActionWidget</code>","text":"<p>               Bases: <code>QWidget</code></p> <p>Row widget that combines a body area and an :class:<code>AYOptionBox</code>.</p> <p>The body contains an icon label and a text label.  The option box is pinned to the far right.  Both sections respond to hover state via :meth:<code>_set_row_hover</code> and :meth:<code>_sync_row_hover</code>.</p> <p>Parameters:</p> Name Type Description Default <code>label</code> <code>str</code> <p>Display text for the action.</p> required <code>icon_name</code> <code>str</code> <p>Material symbol icon name for the label.</p> <code>'none'</code> <code>parent</code> <code>QWidget | None</code> <p>Optional parent widget.</p> <code>None</code> Source code in <code>client/ayon_core/ui/components/option_action.py</code> <pre><code>class AYOptionalActionWidget(QtWidgets.QWidget):\n    \"\"\"Row widget that combines a body area and an :class:`AYOptionBox`.\n\n    The body contains an icon label and a text label.  The option box\n    is pinned to the far right.  Both sections respond to hover state\n    via :meth:`_set_row_hover` and :meth:`_sync_row_hover`.\n\n    Args:\n        label: Display text for the action.\n        icon_name: Material symbol icon name for the label.\n        parent: Optional parent widget.\n    \"\"\"\n\n    def __init__(\n        self,\n        label: str,\n        icon_name: str = \"none\",\n        parent: QtWidgets.QWidget | None = None,\n    ) -&gt; None:\n        super().__init__(parent)\n\n        _style = get_ayon_style().model.get_style(\n            \"QLabel\", variant=AYLabel.Variants.Optional_Action.value\n        )\n        icon_size = _style.get(\"icon-size\", 16)\n\n        body_widget = AYFrame(self, variant=AYFrame.Variants.Contextual_Menu)\n        body_widget.setObjectName(\"OptionalActionBody\")\n\n        label_wdgt = AYLabel(\n            label,\n            variant=AYLabel.Variants.Optional_Action,\n            icon=icon_name,\n            icon_size=icon_size,\n            icon_fill=False,\n            parent=body_widget,\n        )\n        min_h = int(\n            get_ayon_style().model.get_style(\"QMenu\").get(\"min-item-height\", 0)\n        )\n        self.setMinimumHeight(min_h)\n\n        option_box = AYOptionBox(icon_size=icon_size, parent=body_widget)\n        option_box.setObjectName(\"OptionalActionOption\")\n\n        body_layout = AYHBoxLayout(body_widget, spacing=2, margin=0)\n        body_layout.addWidget(label_wdgt, stretch=1)\n\n        layout = AYHBoxLayout(self, spacing=0, margin=0)\n        layout.addWidget(body_widget)\n        layout.addWidget(option_box)\n\n        body_widget.setMouseTracking(True)\n        self.setMouseTracking(True)\n\n        self.icon: QtGui.QIcon = QtGui.QIcon()\n        self.label: AYLabel = label_wdgt\n        self.option: AYOptionBox = option_box\n        self.body: QtWidgets.QWidget = body_widget\n\n        # Watch the children's hover transitions so we can keep them in sync\n        # while the cursor moves between them.\n        self.label.installEventFilter(self)\n        self.option.installEventFilter(self)\n\n    # -- hover propagation ------------------------------------------------\n\n    def _set_row_hover(self, hovered: bool) -&gt; None:\n        for child in (self.body, self.label, self.option):\n            child.setAttribute(QtCore.Qt.WA_UnderMouse, hovered)\n            child.update()\n\n    def _sync_row_hover(self) -&gt; None:\n        # ``underMouse()`` on the parent stays True as long as the cursor is\n        # anywhere inside this row, even while crossing child borders.\n        self._set_row_hover(self.underMouse())\n\n    def enterEvent(self, event: QtCore.QEvent) -&gt; None:\n        self._set_row_hover(True)\n        super().enterEvent(event)\n\n    def leaveEvent(self, event: QtCore.QEvent) -&gt; None:\n        self._set_row_hover(False)\n        super().leaveEvent(event)\n\n    def eventFilter(self, obj: QtCore.QObject, event: QtCore.QEvent) -&gt; bool:\n        if obj in (self.body, self.label, self.option) and event.type() in (\n            QtCore.QEvent.Type.Enter,\n            QtCore.QEvent.Type.Leave,\n        ):\n            # Qt is about to flip WA_UnderMouse on this child.  Defer to\n            # the next event-loop tick so Qt's own handling has finished,\n            # then re-assert hover state based on the parent.\n            QtCore.QTimer.singleShot(0, self._sync_row_hover)\n        return super().eventFilter(obj, event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/order.html","title":"order","text":"<p>AYOrder \u2013 a drag-and-drop reorderable list of labelled options.</p> <p>Drag is implemented entirely via mouse events (no QDrag / OS overlay). The ghost and drop-indicator are child widgets of the AYOrder container, so they are automatically clipped to its boundaries.</p>"},{"location":"autoapi/client/ayon_core/ui/components/order.html#client.ayon_core.ui.components.order.AYOrder","title":"<code>AYOrder</code>","text":"<p>               Bases: <code>AYContainer</code></p> <p>A vertically-stacked, mouse-drag reorderable list of options.</p> <p>Each option is rendered as a labelled row with an icon. Dragging a row shows a ghost widget constrained to the container's bounds and a drop-indicator line.  On release the :attr:<code>order_changed</code> signal is emitted with the updated label list.</p> <p>Parameters:</p> Name Type Description Default <code>options</code> <code>list[str]</code> <p>Display names for each option row.</p> required <code>icons</code> <code>Optional[list[str]]</code> <p>Material icon names, one per option.  Falls back to <code>\"drag_indicator\"</code> for every item when None.</p> <code>None</code> <code>variant</code> <code>QFrameVariants</code> <p>:class:<code>~ayon_core.ui.variants.QFrameVariants</code> applied to the surrounding container frame.</p> <code>Default</code> <code>parent</code> <code>Optional[QWidget]</code> <p>Optional parent widget.</p> <code>None</code> <code>**kwargs</code> <p>Forwarded to :class:<code>AYContainer</code>.</p> <code>{}</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>When icons is provided but its length differs from options.</p> Source code in <code>client/ayon_core/ui/components/order.py</code> <pre><code>class AYOrder(AYContainer):\n    \"\"\"A vertically-stacked, mouse-drag reorderable list of options.\n\n    Each option is rendered as a labelled row with an icon. Dragging a\n    row shows a ghost widget constrained to the container's bounds and a\n    drop-indicator line.  On release the :attr:`order_changed` signal is\n    emitted with the updated label list.\n\n    Args:\n        options: Display names for each option row.\n        icons: Material icon names, one per option.  Falls back to\n            ``\"drag_indicator\"`` for every item when *None*.\n        variant: :class:`~ayon_core.ui.variants.QFrameVariants` applied to\n            the surrounding container frame.\n        parent: Optional parent widget.\n        **kwargs: Forwarded to :class:`AYContainer`.\n\n    Raises:\n        ValueError: When *icons* is provided but its length differs from\n            *options*.\n    \"\"\"\n\n    Variant = QFrameVariants\n\n    order_changed = Signal(list)\n\n    def __init__(\n        self,\n        options: list[str],\n        icons: Optional[list[str]] = None,\n        variant: QFrameVariants = QFrameVariants.Default,\n        parent: Optional[QWidget] = None,\n        **kwargs,\n    ) -&gt; None:\n        super().__init__(\n            layout=AYContainer.Layout.VBox,\n            variant=variant,\n            parent=parent,\n            **kwargs,\n        )\n        if icons is not None and len(icons) != len(options):\n            raise ValueError(\n                f\"icons length ({len(icons)}) must match options \"\n                f\"length ({len(options)})\"\n            )\n\n        self._options: list[_AYOrderOption] = []\n\n        # Drag state\n        self._drag_active: bool = False\n        self._drag_index: int = -1\n        self._drop_index: int = -1\n        self._ghost: Optional[_AYOrderGhost] = None\n        self._anim_group: Optional[QParallelAnimationGroup] = None\n\n        # Live-preview state (set at drag start, cleared afterwards)\n        self._drag_start_slot_ys: list[int] = []\n        self._preview_group: Optional[QParallelAnimationGroup] = None\n\n        _icons = (\n            icons if icons is not None else [\"drag_indicator\"] * len(options)\n        )\n        for text, icon in zip(options, _icons):\n            opt = _AYOrderOption(text, icon, parent=self)\n            self._options.append(opt)\n            self.add_widget(opt)\n\n    # ------------------------------------------------------------------\n    # Public API\n    # ------------------------------------------------------------------\n\n    def current_order(self) -&gt; list[str]:\n        \"\"\"Return option labels in their current top-to-bottom order.\n\n        Returns:\n            List of option display strings.\n        \"\"\"\n        return [opt._text for opt in self._options]\n\n    # ------------------------------------------------------------------\n    # Drag lifecycle (called from _AYOrderOption)\n    # ------------------------------------------------------------------\n\n    def _stop_animations(self) -&gt; None:\n        \"\"\"Stop any running animations and restore managed layout.\"\"\"\n        if self._anim_group is not None:\n            self._anim_group.stop()\n            for opt in self._options:\n                self._layout.removeWidget(opt)\n            for opt in self._options:\n                self._layout.addWidget(opt)\n            self._layout.activate()\n            self.setMinimumHeight(0)\n            self.setMaximumHeight(16777215)\n            self._anim_group = None\n\n        if self._preview_group is not None:\n            self._preview_group.stop()\n            self._preview_group.deleteLater()\n            self._preview_group = None\n\n    def _begin_drag(self, option: _AYOrderOption, global_pos: QPoint) -&gt; None:\n        \"\"\"Start a visual drag for *option*.\n\n        Args:\n            option: The option widget being dragged.\n            global_pos: Initial global cursor position.\n        \"\"\"\n        self._stop_animations()\n\n        self._drag_active = True\n        self._drag_index = self._options.index(option)\n\n        # Snapshot original slot y-positions for stable insertion math\n        # and live-preview target computation throughout the drag.\n        self._drag_start_slot_ys = [opt.pos().y() for opt in self._options]\n\n        # Create the ghost from a live render of the option.\n        pixmap: QPixmap = option.grab()\n        # Make the source row invisible (opacity 0) so the layout keeps\n        # the blank space while the ghost floats above.\n        invisible = QGraphicsOpacityEffect(option)\n        invisible.setOpacity(0.0)\n        option.setGraphicsEffect(invisible)\n        self._ghost = _AYOrderGhost(pixmap, option.size(), self)\n\n        self._update_drag(global_pos)\n\n    def _update_drag(self, global_pos: QPoint) -&gt; None:\n        \"\"\"Reposition the ghost and drop indicator.\n\n        When the effective insertion index changes, the non-dragged\n        widgets are animated to reflect the new drop position.\n\n        Args:\n            global_pos: Current global cursor position.\n        \"\"\"\n        if not self._drag_active:\n            return\n\n        local = self.mapFromGlobal(global_pos)\n        opt_src = self._options[self._drag_index]\n\n        # ---- ghost position (clamped to container) -------------------\n        if self._ghost is not None:\n            gh_h = self._ghost.height()\n            x = opt_src.pos().x()\n            y = local.y() - gh_h // 2\n            y = max(0, min(y, self.height() - gh_h))\n            self._ghost.move(x, y)\n            self._ghost.raise_()\n\n        # ---- drop indicator + live preview ---------------------------\n        new_drop = self._insertion_index(local)\n        if new_drop != self._drop_index:\n            self._drop_index = new_drop\n            self._animate_preview()\n\n    def _end_drag(self) -&gt; None:\n        \"\"\"Finalise the drag: clean up visuals and apply the reorder.\"\"\"\n        if not self._drag_active:\n            return\n\n        from_idx = self._drag_index\n        to_idx = self._drop_index\n\n        # Remove ghost.\n        if self._ghost is not None:\n            self._ghost.deleteLater()\n            self._ghost = None\n\n        # Remove the invisible effect to restore the source option.\n        if 0 &lt;= from_idx &lt; len(self._options):\n            self._options[from_idx].setGraphicsEffect(None)\n\n        self._drag_active = False\n        self._drag_index = -1\n        self._drop_index = -1\n\n        # Snap preview to end values: non-dragged widgets will be at\n        # their correct final positions, so _reorder only animates the\n        # dragged widget into its new slot.\n        self._snap_preview()\n        self._reorder(from_idx, to_idx)\n\n    # ------------------------------------------------------------------\n    # Private helpers\n    # ------------------------------------------------------------------\n\n    def _insertion_index(self, pos: QPoint) -&gt; int:\n        \"\"\"Return the insertion index for a drop at *pos*.\n\n        Uses original slot positions during a drag to prevent feedback\n        loops caused by live-preview animations shifting widget geometry.\n\n        Args:\n            pos: Drop position in widget-local coordinates.\n\n        Returns:\n            An index in the range ``[0, len(options)]``.\n        \"\"\"\n        if self._drag_active and self._drag_start_slot_ys:\n            h = self._options[0].height() if self._options else 0\n            for i in range(len(self._options)):\n                if pos.y() &lt; self._drag_start_slot_ys[i] + h // 2:\n                    return i\n            return len(self._options)\n\n        for i, opt in enumerate(self._options):\n            if pos.y() &lt; opt.geometry().center().y():\n                return i\n        return len(self._options)\n\n    def _compute_preview_targets(\n        self, drag_idx: int, drop_idx: int\n    ) -&gt; dict[_AYOrderOption, int]:\n        \"\"\"Compute target y-positions for non-dragged widgets.\n\n        Uses original slot y-positions as a stable reference so preview\n        targets are independent of current animated geometry.\n\n        Args:\n            drag_idx: Index of the widget being dragged.\n            drop_idx: Current insertion index from the drop indicator.\n\n        Returns:\n            Mapping of each non-dragged option to its target y.\n        \"\"\"\n        # No-op drop \u2192 all non-dragged widgets return to start ys.\n        if drop_idx in (drag_idx, drag_idx + 1):\n            return {\n                opt: self._drag_start_slot_ys[i]\n                for i, opt in enumerate(self._options)\n                if i != drag_idx\n            }\n\n        insert_at = drop_idx - 1 if drop_idx &gt; drag_idx else drop_idx\n        virtual = [opt for i, opt in enumerate(self._options) if i != drag_idx]\n        result: dict[_AYOrderOption, int] = {}\n        slot = 0\n        for vi, opt in enumerate(virtual):\n            if vi == insert_at:\n                slot += 1  # Skip the slot reserved for the dragged item.\n            result[opt] = self._drag_start_slot_ys[slot]\n            slot += 1\n        return result\n\n    def _animate_preview(self) -&gt; None:\n        \"\"\"Slide non-dragged widgets to reflect the current drop position.\n\n        Cancels any in-flight preview and starts a fresh parallel group.\n        Widgets remain in the layout; the animation uses the ``pos``\n        property directly, which the layout will not override during the\n        short animation window.\n        \"\"\"\n        if not self._drag_active or not self._drag_start_slot_ys:\n            return\n\n        # Cancel previous preview; widgets stay at their current pos.\n        if self._preview_group is not None:\n            self._preview_group.stop()\n            self._preview_group.deleteLater()\n            self._preview_group = None\n\n        targets = self._compute_preview_targets(\n            self._drag_index, self._drop_index\n        )\n        group = QParallelAnimationGroup(self)\n        for opt, target_y in targets.items():\n            current_y = opt.pos().y()\n            if current_y == target_y:\n                continue\n            anim = QPropertyAnimation(opt, b\"pos\", group)\n            anim.setDuration(120)\n            anim.setStartValue(QPoint(opt.pos().x(), current_y))\n            anim.setEndValue(QPoint(opt.pos().x(), target_y))\n            anim.setEasingCurve(QEasingCurve.Type.OutCubic)\n            group.addAnimation(anim)\n\n        if group.animationCount() &gt; 0:\n            group.start()\n            self._preview_group = group\n        else:\n            group.deleteLater()\n\n    def _snap_preview(self) -&gt; None:\n        \"\"\"Instantly complete any in-flight preview animation.\n\n        Called just before :meth:`_reorder` so that non-dragged widgets\n        are already at their correct final positions.\n        \"\"\"\n        if self._preview_group is None:\n            return\n        for i in range(self._preview_group.animationCount()):\n            anim = self._preview_group.animationAt(i)\n            if isinstance(anim, QPropertyAnimation):\n                anim.setCurrentTime(anim.duration())\n        self._preview_group.stop()\n        self._preview_group.deleteLater()\n        self._preview_group = None\n\n    def _reorder(self, from_idx: int, to_idx: int) -&gt; None:\n        \"\"\"Move one option and emit :attr:`order_changed`.\n\n        Widgets displaced by the move are animated to their new\n        positions.  A no-op when the item would end up in its current\n        position.\n\n        When preceded by a drag gesture (live preview active), non-\n        dragged widgets are already at their final positions; only the\n        dragged widget then needs to be animated into its new slot.\n\n        Args:\n            from_idx: Current index of the item to move.\n            to_idx: Insertion index *before* removal, as returned by\n                :meth:`_insertion_index`.\n        \"\"\"\n        if to_idx in (from_idx, from_idx + 1):\n            self._drag_start_slot_ys = []\n            return\n\n        old_ys = (\n            list(self._drag_start_slot_ys)\n            if self._drag_start_slot_ys\n            else [opt.pos().y() for opt in self._options]\n        )\n        self._drag_start_slot_ys = []\n\n        old_positions = {opt: QPoint(opt.pos()) for opt in self._options}\n\n        # Logical reorder.\n        item = self._options.pop(from_idx)\n        insert_at = to_idx - 1 if to_idx &gt; from_idx else to_idx\n        self._options.insert(insert_at, item)\n\n        # Build per-widget target positions.\n        new_positions = {\n            opt: QPoint(old_positions[opt].x(), old_ys[i])\n            for i, opt in enumerate(self._options)\n        }\n\n        self._animate_reorder(old_positions, new_positions)\n        self.order_changed.emit(self.current_order())\n\n    def _animate_reorder(\n        self,\n        old_positions: dict[_AYOrderOption, QPoint],\n        new_positions: dict[_AYOrderOption, QPoint],\n    ) -&gt; None:\n        \"\"\"Animate widgets from old to new positions, then restore layout.\n\n        Args:\n            old_positions: Mapping of options to their starting positions.\n            new_positions: Mapping of options to their target positions.\n        \"\"\"\n        # Detach widgets from layout and lock height during animation.\n        for opt in self._options:\n            self._layout.removeWidget(opt)\n        self.setFixedHeight(self.height())\n\n        for opt, dst in new_positions.items():\n            if old_positions[opt] != dst:\n                opt.move(dst)\n\n        self._restore_layout()\n\n    def _restore_layout(self) -&gt; None:\n        \"\"\"Re-add widgets to layout and restore dynamic sizing.\"\"\"\n        for opt in self._options:\n            self._layout.addWidget(opt)\n        self.setMinimumHeight(0)\n        self.setMaximumHeight(16777215)\n        self._anim_group = None\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/order.html#client.ayon_core.ui.components.order.AYOrder.current_order","title":"<code>current_order()</code>","text":"<p>Return option labels in their current top-to-bottom order.</p> <p>Returns:</p> Type Description <code>list[str]</code> <p>List of option display strings.</p> Source code in <code>client/ayon_core/ui/components/order.py</code> <pre><code>def current_order(self) -&gt; list[str]:\n    \"\"\"Return option labels in their current top-to-bottom order.\n\n    Returns:\n        List of option display strings.\n    \"\"\"\n    return [opt._text for opt in self._options]\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/page_button.html","title":"page_button","text":"<p>AYPageButton component module.</p> <p>Provides a full-width navigation/settings row button with the layout::</p> <pre><code>[icon]  Label text ........................  Value text  &gt;\n</code></pre> <p>The left cluster (icon + label) is pinned left; the right cluster (value + chevron) is pinned right.  A flexible gap fills the space between them.  Default size policy is <code>Expanding \u00d7 Fixed</code>.</p>"},{"location":"autoapi/client/ayon_core/ui/components/page_button.html#client.ayon_core.ui.components.page_button.AYPageButton","title":"<code>AYPageButton</code>","text":"<p>               Bases: <code>AYButton</code></p> <p>Full-width navigation/settings page button.</p> <p>Renders a single-row entry with a left cluster (optional icon + label text) pinned to the left and a right cluster (value text + chevron) pinned to the right.  A flexible gap fills the remaining horizontal space.</p> <p>The visual style is driven by the <code>\"AYPageButton\"</code> block in <code>ayon_style.json</code>.  All painting is done in a custom <code>paintEvent</code>; the Qt button bevel is not used.</p> <p>Parameters:</p> Name Type Description Default <code>label</code> <code>str</code> <p>Text displayed in the left cluster.</p> <code>''</code> <code>value</code> <code>str</code> <p>Secondary text displayed in the right cluster (before the chevron).  Pass an empty string to hide it.</p> <code>''</code> <code>variant</code> <code>AYPageButtonVariants</code> <p>Visual variant.  Only <code>Default</code> is defined.</p> <code>Default</code> <code>icon</code> <code>str | None</code> <p>Material Symbol icon name for the left icon (optional).</p> <code>None</code> <code>icon_size</code> <code>int</code> <p>Pixel size of the left icon.</p> <code>16</code> <code>chevron_icon</code> <code>str</code> <p>Material Symbol name for the right-hand chevron.</p> <code>'chevron_right'</code> <code>chevron_size</code> <code>int</code> <p>Pixel size of the chevron icon.</p> <code>16</code> <code>tooltip</code> <code>str</code> <p>Tooltip text.</p> <code>''</code> <code>name_id</code> <code>str</code> <p><code>objectName</code> for QSS targeting.</p> <code>''</code> <code>**kwargs</code> <p>Forwarded to <code>AYButton</code> / <code>QPushButton</code>.</p> <code>{}</code> Signals <p>clicked (bool): Inherited from <code>QPushButton</code>.</p> <p>Example::</p> <pre><code>btn = AYPageButton(\n    label=\"Featured version\",\n    value=\"Done\",\n    icon=\"star\",\n)\nbtn.clicked.connect(lambda: print(\"navigating \u2026\"))\n</code></pre> Source code in <code>client/ayon_core/ui/components/page_button.py</code> <pre><code>class AYPageButton(AYButton):\n    \"\"\"Full-width navigation/settings page button.\n\n    Renders a single-row entry with a left cluster (optional icon +\n    label text) pinned to the left and a right cluster (value text +\n    chevron) pinned to the right.  A flexible gap fills the remaining\n    horizontal space.\n\n    The visual style is driven by the ``\"AYPageButton\"`` block in\n    ``ayon_style.json``.  All painting is done in a custom\n    ``paintEvent``; the Qt button bevel is not used.\n\n    Args:\n        label: Text displayed in the left cluster.\n        value: Secondary text displayed in the right cluster (before the\n            chevron).  Pass an empty string to hide it.\n        variant: Visual variant.  Only ``Default`` is defined.\n        icon: Material Symbol icon name for the left icon (optional).\n        icon_size: Pixel size of the left icon.\n        chevron_icon: Material Symbol name for the right-hand chevron.\n        chevron_size: Pixel size of the chevron icon.\n        tooltip: Tooltip text.\n        name_id: ``objectName`` for QSS targeting.\n        **kwargs: Forwarded to ``AYButton`` / ``QPushButton``.\n\n    Signals:\n        clicked (bool): Inherited from ``QPushButton``.\n\n    Example::\n\n        btn = AYPageButton(\n            label=\"Featured version\",\n            value=\"Done\",\n            icon=\"star\",\n        )\n        btn.clicked.connect(lambda: print(\"navigating \u2026\"))\n    \"\"\"\n\n    Variants = AYPageButtonVariants\n\n    def __init__(\n        self,\n        label: str = \"\",\n        value: str = \"\",\n        *,\n        variant: AYPageButtonVariants = AYPageButtonVariants.Default,\n        icon: str | None = None,\n        icon_size: int = 16,\n        chevron_icon: str = \"chevron_right\",\n        chevron_size: int = 16,\n        tooltip: str = \"\",\n        name_id: str = \"\",\n        **kwargs,\n    ) -&gt; None:\n        \"\"\"Initialize an AYPageButton.\n\n        Args:\n            label: Left-cluster label text.\n            value: Right-cluster value text (optional).\n            variant: Visual variant (only ``Default`` available).\n            icon: Material Symbol name for the left icon.\n            icon_size: Pixel size for the left icon.\n            chevron_icon: Material Symbol name for the chevron.\n            chevron_size: Pixel size for the chevron icon.\n            tooltip: Tooltip string shown on hover.\n            name_id: Widget ``objectName`` for QSS targeting.\n            **kwargs: Extra keyword arguments forwarded to\n                ``AYButton.__init__``.\n        \"\"\"\n        self._value: str = value\n        self._chevron_size: int = chevron_size\n        self._icon_qicon: QtGui.QIcon | None = None\n        self._chevron_qicon: QtGui.QIcon | None = None\n        self._force_hover: bool = False\n\n        # Call AYButton with a valid QPushButton variant but no icon so\n        # AYButton doesn't call set_icon() (which would use the wrong\n        # style data).  We manage both icons ourselves below.\n        super().__init__(\n            label,\n            variant=QPushButtonVariants.Surface,\n            icon=None,\n            icon_size=icon_size,\n            tooltip=tooltip,\n            name_id=name_id,\n            **kwargs,\n        )\n\n        # --- Override style data with AYPageButton-specific block ------\n        self._style_data = get_ayon_style().model.get_styles(\n            \"AYPageButton\", variant.value\n        )\n        self._style_data.set_context(self)\n\n        # Recompute icon colors from the AYPageButton palette\n        icon_color_str = self._style_data[\"base\"].get(\"icon-color\", \"#ffffff\")\n        self._icon_color = QColor(icon_color_str)\n\n        hover_bg_str = (\n            self._style_data[\"base\"]\n            .get(\"hover\", {})\n            .get(\"background-color\", \"#000000\")\n        )\n        self._icon_hover_color = self._icon_color\n        if isinstance(hover_bg_str, str) and self._icon_color.isValid():\n            self._icon_hover_color = compute_color_for_contrast(\n                QColor(hover_bg_str).toTuple(),\n                self._icon_color.toTuple(),\n                min_contrast_ratio=7,\n            )\n\n        # Build left icon QIcon\n        if icon:\n            self._icon = icon\n            self._icon_qicon = get_icon(\n                icon_name_off=icon,\n                color_off=self._icon_color,\n                icon_name_on=icon,\n                color_on=self._icon_hover_color,\n            )\n\n        # Build chevron QIcon\n        chevron_color_str = self._style_data[\"base\"].get(\n            \"chevron-color\", \"#aaaaaa\"\n        )\n        chevron_color = QColor(chevron_color_str)\n        chevron_hover_color = chevron_color\n        if isinstance(hover_bg_str, str) and chevron_color.isValid():\n            chevron_hover_color = compute_color_for_contrast(\n                QColor(hover_bg_str).toTuple(),\n                chevron_color.toTuple(),\n                min_contrast_ratio=4.5,\n            )\n        self._chevron_qicon = get_icon(\n            icon_name_off=chevron_icon,\n            color_off=chevron_color,\n            icon_name_on=chevron_icon,\n            color_on=chevron_hover_color,\n        )\n\n        # Force Expanding \u00d7 Fixed regardless of JSON fixed-width setting\n        self.setFixedHeight(self._style_data[\"base\"].get(\"height\", 44))\n        self.setSizePolicy(\n            QtWidgets.QSizePolicy.Policy.Expanding,\n            QtWidgets.QSizePolicy.Policy.Fixed,\n        )\n\n    # ------------------------------------------------------------------ #\n    # Public API                                                         #\n    # ------------------------------------------------------------------ #\n\n    def value(self) -&gt; str:\n        \"\"\"Return the current right-cluster value text.\n\n        Returns:\n            The value string set at construction or via ``set_value``.\n        \"\"\"\n        return self._value\n\n    def set_value(self, text: str) -&gt; None:\n        \"\"\"Update the right-cluster value text and repaint.\n\n        Args:\n            text: New value string (empty string hides the value field).\n        \"\"\"\n        self._value = text\n        self.update()\n\n    def set_label(self, text: str) -&gt; None:\n        \"\"\"Update the left-cluster label text and repaint.\n\n        Args:\n            text: New label string.\n        \"\"\"\n        self.setText(text)\n        self.update()\n\n    def set_force_hover(self, value: bool) -&gt; None:\n        \"\"\"Force (or release) the hover appearance for snapshot tests.\n\n        Args:\n            value: ``True`` to paint as if hovered; ``False`` to restore\n                normal appearance.\n        \"\"\"\n        self._force_hover = value\n        self.update()\n\n    # ------------------------------------------------------------------ #\n    # Sizing                                                             #\n    # ------------------------------------------------------------------ #\n\n    def sizeHint(self) -&gt; QtCore.QSize:\n        \"\"\"Return the preferred size of the widget.\n\n        Returns:\n            A ``QSize`` with the preferred width and height.\n        \"\"\"\n        size = super().sizeHint()\n        size.setHeight(self._style_data[\"base\"].get(\"height\", 24))\n        return size\n\n    def rect(self) -&gt; QtCore.QRect:\n        if not hasattr(super(), \"_rect_set\"):\n            # FIXME: a bit of a hack to set the height of the button\n            #        needs to be fixed in AYButton.\n            h = self._style_data[\"base\"].get(\"height\", 24)\n            self.setFixedHeight(h)\n            setattr(self, \"_rect_set\", True)\n        rect = super().rect()\n        return rect\n\n    # ------------------------------------------------------------------ #\n    # Internal helpers                                                   #\n    # ------------------------------------------------------------------ #\n\n    def _is_hovered(self) -&gt; bool:\n        \"\"\"Return ``True`` when the widget is in a hover state.\n\n        Returns:\n            ``True`` if the cursor is over the widget or\n            ``_force_hover`` is set.\n        \"\"\"\n        return self.underMouse() or self._force_hover\n\n    def _current_state_key(self) -&gt; str:\n        \"\"\"Return the active style-state key for the current widget state.\n\n        Returns:\n            One of ``\"disabled\"``, ``\"pressed\"``, ``\"hover\"``, or\n            ``\"\"`` (base/normal).\n        \"\"\"\n        if not self.isEnabled():\n            return \"disabled\"\n        if self.isDown():\n            return \"pressed\"\n        if self._is_hovered():\n            return \"hover\"\n        return \"base\"\n\n    def _get_current_style(self) -&gt; dict:\n        \"\"\"Return a merged style dict for the current widget state.\n\n        Starts with all base (non-state) values from ``_style_data``\n        and overlays the active state sub-dict on top.\n\n        Returns:\n            A plain ``dict`` mapping style keys to resolved values.\n        \"\"\"\n        # Overlay active state overrides\n        state_key = self._current_state_key()\n        return self._style_data[state_key]\n\n    # ------------------------------------------------------------------ #\n    # Painting                                                             #\n    # ------------------------------------------------------------------ #\n\n    def paintEvent(  # noqa: N802\n        self, arg__1: QtGui.QPaintEvent\n    ) -&gt; None:\n        \"\"\"Render the full-width page button.\n\n        Draws background, left cluster (icon + label), and right cluster\n        (value + chevron) using ``QPainter`` directly.  No Qt bevel or\n        ``QStyle.drawControl`` is used.\n\n        Args:\n            event: The paint event (unused \u2014 the full widget is redrawn).\n        \"\"\"\n        style = self._get_current_style()\n\n        padding = style.get(\"padding\", [12, 10])\n        if isinstance(padding, list) and len(padding) &gt;= 1:\n            pad_h = padding[0]\n        else:\n            pad_h = 12\n\n        icon_gap = style.get(\"icon-gap\", 8)\n        value_gap = style.get(\"value-gap\", 8)\n        border_radius = style.get(\"border-radius\", 4)\n        opacity = style.get(\"opacity\", 1.0)\n\n        p = QtGui.QPainter(self)\n        p.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)\n        p.setOpacity(opacity)\n\n        rect = self.rect()\n\n        # --- Background --------------------------------------------------\n        bg_raw = style.get(\"background-color\", \"transparent\")\n        if isinstance(bg_raw, str) and bg_raw != \"transparent\":\n            bg = QColor(bg_raw)\n            if bg.isValid() and bg.alpha() &gt; 0:\n                p.setPen(Qt.PenStyle.NoPen)\n                p.setBrush(QtGui.QBrush(bg))\n                p.drawRoundedRect(\n                    QtCore.QRectF(rect), border_radius, border_radius\n                )\n\n        # Content rect inset by horizontal padding\n        content_left = rect.left() + pad_h\n        content_right = rect.right() - pad_h\n        center_y = rect.top() + rect.height() // 2\n\n        # --- Right cluster: chevron then value text ----------------------\n        right_cursor = content_right\n\n        # Chevron\n        if self._chevron_qicon is not None:\n            cs = self._chevron_size\n            chevron_rect = QtCore.QRect(\n                right_cursor - cs,\n                center_y - cs // 2,\n                cs,\n                cs,\n            )\n            is_hover = self._is_hovered() and self.isEnabled()\n            icon_state = (\n                QtGui.QIcon.State.On if is_hover else QtGui.QIcon.State.Off\n            )\n            self._chevron_qicon.paint(\n                p,\n                chevron_rect,\n                Qt.AlignmentFlag.AlignCenter,\n                QtGui.QIcon.Mode.Normal,\n                icon_state,\n            )\n            right_cursor = chevron_rect.left() - value_gap\n\n        # Value text (fixed width, right-aligned)\n        if self._value:\n            value_color_raw = style.get(\"value-color\", \"#909090\")\n            p.setPen(QColor(value_color_raw))\n            p.setFont(self.font())\n            fm: QFontMetrics = self.fontMetrics()\n            value_w = fm.horizontalAdvance(self._value)\n            value_rect = QtCore.QRect(\n                right_cursor - value_w,\n                rect.top(),\n                value_w,\n                rect.height(),\n            )\n            p.drawText(\n                value_rect,\n                Qt.AlignmentFlag.AlignVCenter | Qt.AlignmentFlag.AlignRight,\n                self._value,\n            )\n            right_cursor = value_rect.left() - value_gap\n\n        # --- Left cluster: icon then label text --------------------------\n        left_cursor = content_left\n\n        # Left icon\n        if self._icon_qicon is not None:\n            sz = self._icon_size\n            icon_rect = QtCore.QRect(\n                left_cursor,\n                center_y - sz // 2,\n                sz,\n                sz,\n            )\n            is_hover = self._is_hovered() and self.isEnabled()\n            icon_state = (\n                QtGui.QIcon.State.On if is_hover else QtGui.QIcon.State.Off\n            )\n            self._icon_qicon.paint(\n                p,\n                icon_rect,\n                Qt.AlignmentFlag.AlignCenter,\n                QtGui.QIcon.Mode.Normal,\n                icon_state,\n            )\n            left_cursor = icon_rect.right() + icon_gap\n\n        # Label text (elided if too narrow)\n        label = self.text()\n        if label:\n            label_color_raw = style.get(\n                \"color\",\n                style.get(\"color\", \"#ffffff\"),\n            )\n            p.setPen(QColor(label_color_raw))\n            p.setFont(self.font())\n            fm = self.fontMetrics()\n            available_w = max(0, right_cursor - left_cursor)\n            elided = fm.elidedText(\n                label, Qt.TextElideMode.ElideRight, available_w\n            )\n            label_rect = QtCore.QRect(\n                left_cursor,\n                rect.top(),\n                available_w,\n                rect.height(),\n            )\n            p.drawText(\n                label_rect,\n                Qt.AlignmentFlag.AlignVCenter | Qt.AlignmentFlag.AlignLeft,\n                elided,\n            )\n\n        p.end()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/page_button.html#client.ayon_core.ui.components.page_button.AYPageButton.__init__","title":"<code>__init__(label='', value='', *, variant=AYPageButtonVariants.Default, icon=None, icon_size=16, chevron_icon='chevron_right', chevron_size=16, tooltip='', name_id='', **kwargs)</code>","text":"<p>Initialize an AYPageButton.</p> <p>Parameters:</p> Name Type Description Default <code>label</code> <code>str</code> <p>Left-cluster label text.</p> <code>''</code> <code>value</code> <code>str</code> <p>Right-cluster value text (optional).</p> <code>''</code> <code>variant</code> <code>AYPageButtonVariants</code> <p>Visual variant (only <code>Default</code> available).</p> <code>Default</code> <code>icon</code> <code>str | None</code> <p>Material Symbol name for the left icon.</p> <code>None</code> <code>icon_size</code> <code>int</code> <p>Pixel size for the left icon.</p> <code>16</code> <code>chevron_icon</code> <code>str</code> <p>Material Symbol name for the chevron.</p> <code>'chevron_right'</code> <code>chevron_size</code> <code>int</code> <p>Pixel size for the chevron icon.</p> <code>16</code> <code>tooltip</code> <code>str</code> <p>Tooltip string shown on hover.</p> <code>''</code> <code>name_id</code> <code>str</code> <p>Widget <code>objectName</code> for QSS targeting.</p> <code>''</code> <code>**kwargs</code> <p>Extra keyword arguments forwarded to <code>AYButton.__init__</code>.</p> <code>{}</code> Source code in <code>client/ayon_core/ui/components/page_button.py</code> <pre><code>def __init__(\n    self,\n    label: str = \"\",\n    value: str = \"\",\n    *,\n    variant: AYPageButtonVariants = AYPageButtonVariants.Default,\n    icon: str | None = None,\n    icon_size: int = 16,\n    chevron_icon: str = \"chevron_right\",\n    chevron_size: int = 16,\n    tooltip: str = \"\",\n    name_id: str = \"\",\n    **kwargs,\n) -&gt; None:\n    \"\"\"Initialize an AYPageButton.\n\n    Args:\n        label: Left-cluster label text.\n        value: Right-cluster value text (optional).\n        variant: Visual variant (only ``Default`` available).\n        icon: Material Symbol name for the left icon.\n        icon_size: Pixel size for the left icon.\n        chevron_icon: Material Symbol name for the chevron.\n        chevron_size: Pixel size for the chevron icon.\n        tooltip: Tooltip string shown on hover.\n        name_id: Widget ``objectName`` for QSS targeting.\n        **kwargs: Extra keyword arguments forwarded to\n            ``AYButton.__init__``.\n    \"\"\"\n    self._value: str = value\n    self._chevron_size: int = chevron_size\n    self._icon_qicon: QtGui.QIcon | None = None\n    self._chevron_qicon: QtGui.QIcon | None = None\n    self._force_hover: bool = False\n\n    # Call AYButton with a valid QPushButton variant but no icon so\n    # AYButton doesn't call set_icon() (which would use the wrong\n    # style data).  We manage both icons ourselves below.\n    super().__init__(\n        label,\n        variant=QPushButtonVariants.Surface,\n        icon=None,\n        icon_size=icon_size,\n        tooltip=tooltip,\n        name_id=name_id,\n        **kwargs,\n    )\n\n    # --- Override style data with AYPageButton-specific block ------\n    self._style_data = get_ayon_style().model.get_styles(\n        \"AYPageButton\", variant.value\n    )\n    self._style_data.set_context(self)\n\n    # Recompute icon colors from the AYPageButton palette\n    icon_color_str = self._style_data[\"base\"].get(\"icon-color\", \"#ffffff\")\n    self._icon_color = QColor(icon_color_str)\n\n    hover_bg_str = (\n        self._style_data[\"base\"]\n        .get(\"hover\", {})\n        .get(\"background-color\", \"#000000\")\n    )\n    self._icon_hover_color = self._icon_color\n    if isinstance(hover_bg_str, str) and self._icon_color.isValid():\n        self._icon_hover_color = compute_color_for_contrast(\n            QColor(hover_bg_str).toTuple(),\n            self._icon_color.toTuple(),\n            min_contrast_ratio=7,\n        )\n\n    # Build left icon QIcon\n    if icon:\n        self._icon = icon\n        self._icon_qicon = get_icon(\n            icon_name_off=icon,\n            color_off=self._icon_color,\n            icon_name_on=icon,\n            color_on=self._icon_hover_color,\n        )\n\n    # Build chevron QIcon\n    chevron_color_str = self._style_data[\"base\"].get(\n        \"chevron-color\", \"#aaaaaa\"\n    )\n    chevron_color = QColor(chevron_color_str)\n    chevron_hover_color = chevron_color\n    if isinstance(hover_bg_str, str) and chevron_color.isValid():\n        chevron_hover_color = compute_color_for_contrast(\n            QColor(hover_bg_str).toTuple(),\n            chevron_color.toTuple(),\n            min_contrast_ratio=4.5,\n        )\n    self._chevron_qicon = get_icon(\n        icon_name_off=chevron_icon,\n        color_off=chevron_color,\n        icon_name_on=chevron_icon,\n        color_on=chevron_hover_color,\n    )\n\n    # Force Expanding \u00d7 Fixed regardless of JSON fixed-width setting\n    self.setFixedHeight(self._style_data[\"base\"].get(\"height\", 44))\n    self.setSizePolicy(\n        QtWidgets.QSizePolicy.Policy.Expanding,\n        QtWidgets.QSizePolicy.Policy.Fixed,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/page_button.html#client.ayon_core.ui.components.page_button.AYPageButton.paintEvent","title":"<code>paintEvent(arg__1)</code>","text":"<p>Render the full-width page button.</p> <p>Draws background, left cluster (icon + label), and right cluster (value + chevron) using <code>QPainter</code> directly.  No Qt bevel or <code>QStyle.drawControl</code> is used.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <p>The paint event (unused \u2014 the full widget is redrawn).</p> required Source code in <code>client/ayon_core/ui/components/page_button.py</code> <pre><code>def paintEvent(  # noqa: N802\n    self, arg__1: QtGui.QPaintEvent\n) -&gt; None:\n    \"\"\"Render the full-width page button.\n\n    Draws background, left cluster (icon + label), and right cluster\n    (value + chevron) using ``QPainter`` directly.  No Qt bevel or\n    ``QStyle.drawControl`` is used.\n\n    Args:\n        event: The paint event (unused \u2014 the full widget is redrawn).\n    \"\"\"\n    style = self._get_current_style()\n\n    padding = style.get(\"padding\", [12, 10])\n    if isinstance(padding, list) and len(padding) &gt;= 1:\n        pad_h = padding[0]\n    else:\n        pad_h = 12\n\n    icon_gap = style.get(\"icon-gap\", 8)\n    value_gap = style.get(\"value-gap\", 8)\n    border_radius = style.get(\"border-radius\", 4)\n    opacity = style.get(\"opacity\", 1.0)\n\n    p = QtGui.QPainter(self)\n    p.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing)\n    p.setOpacity(opacity)\n\n    rect = self.rect()\n\n    # --- Background --------------------------------------------------\n    bg_raw = style.get(\"background-color\", \"transparent\")\n    if isinstance(bg_raw, str) and bg_raw != \"transparent\":\n        bg = QColor(bg_raw)\n        if bg.isValid() and bg.alpha() &gt; 0:\n            p.setPen(Qt.PenStyle.NoPen)\n            p.setBrush(QtGui.QBrush(bg))\n            p.drawRoundedRect(\n                QtCore.QRectF(rect), border_radius, border_radius\n            )\n\n    # Content rect inset by horizontal padding\n    content_left = rect.left() + pad_h\n    content_right = rect.right() - pad_h\n    center_y = rect.top() + rect.height() // 2\n\n    # --- Right cluster: chevron then value text ----------------------\n    right_cursor = content_right\n\n    # Chevron\n    if self._chevron_qicon is not None:\n        cs = self._chevron_size\n        chevron_rect = QtCore.QRect(\n            right_cursor - cs,\n            center_y - cs // 2,\n            cs,\n            cs,\n        )\n        is_hover = self._is_hovered() and self.isEnabled()\n        icon_state = (\n            QtGui.QIcon.State.On if is_hover else QtGui.QIcon.State.Off\n        )\n        self._chevron_qicon.paint(\n            p,\n            chevron_rect,\n            Qt.AlignmentFlag.AlignCenter,\n            QtGui.QIcon.Mode.Normal,\n            icon_state,\n        )\n        right_cursor = chevron_rect.left() - value_gap\n\n    # Value text (fixed width, right-aligned)\n    if self._value:\n        value_color_raw = style.get(\"value-color\", \"#909090\")\n        p.setPen(QColor(value_color_raw))\n        p.setFont(self.font())\n        fm: QFontMetrics = self.fontMetrics()\n        value_w = fm.horizontalAdvance(self._value)\n        value_rect = QtCore.QRect(\n            right_cursor - value_w,\n            rect.top(),\n            value_w,\n            rect.height(),\n        )\n        p.drawText(\n            value_rect,\n            Qt.AlignmentFlag.AlignVCenter | Qt.AlignmentFlag.AlignRight,\n            self._value,\n        )\n        right_cursor = value_rect.left() - value_gap\n\n    # --- Left cluster: icon then label text --------------------------\n    left_cursor = content_left\n\n    # Left icon\n    if self._icon_qicon is not None:\n        sz = self._icon_size\n        icon_rect = QtCore.QRect(\n            left_cursor,\n            center_y - sz // 2,\n            sz,\n            sz,\n        )\n        is_hover = self._is_hovered() and self.isEnabled()\n        icon_state = (\n            QtGui.QIcon.State.On if is_hover else QtGui.QIcon.State.Off\n        )\n        self._icon_qicon.paint(\n            p,\n            icon_rect,\n            Qt.AlignmentFlag.AlignCenter,\n            QtGui.QIcon.Mode.Normal,\n            icon_state,\n        )\n        left_cursor = icon_rect.right() + icon_gap\n\n    # Label text (elided if too narrow)\n    label = self.text()\n    if label:\n        label_color_raw = style.get(\n            \"color\",\n            style.get(\"color\", \"#ffffff\"),\n        )\n        p.setPen(QColor(label_color_raw))\n        p.setFont(self.font())\n        fm = self.fontMetrics()\n        available_w = max(0, right_cursor - left_cursor)\n        elided = fm.elidedText(\n            label, Qt.TextElideMode.ElideRight, available_w\n        )\n        label_rect = QtCore.QRect(\n            left_cursor,\n            rect.top(),\n            available_w,\n            rect.height(),\n        )\n        p.drawText(\n            label_rect,\n            Qt.AlignmentFlag.AlignVCenter | Qt.AlignmentFlag.AlignLeft,\n            elided,\n        )\n\n    p.end()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/page_button.html#client.ayon_core.ui.components.page_button.AYPageButton.set_force_hover","title":"<code>set_force_hover(value)</code>","text":"<p>Force (or release) the hover appearance for snapshot tests.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>bool</code> <p><code>True</code> to paint as if hovered; <code>False</code> to restore normal appearance.</p> required Source code in <code>client/ayon_core/ui/components/page_button.py</code> <pre><code>def set_force_hover(self, value: bool) -&gt; None:\n    \"\"\"Force (or release) the hover appearance for snapshot tests.\n\n    Args:\n        value: ``True`` to paint as if hovered; ``False`` to restore\n            normal appearance.\n    \"\"\"\n    self._force_hover = value\n    self.update()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/page_button.html#client.ayon_core.ui.components.page_button.AYPageButton.set_label","title":"<code>set_label(text)</code>","text":"<p>Update the left-cluster label text and repaint.</p> <p>Parameters:</p> Name Type Description Default <code>text</code> <code>str</code> <p>New label string.</p> required Source code in <code>client/ayon_core/ui/components/page_button.py</code> <pre><code>def set_label(self, text: str) -&gt; None:\n    \"\"\"Update the left-cluster label text and repaint.\n\n    Args:\n        text: New label string.\n    \"\"\"\n    self.setText(text)\n    self.update()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/page_button.html#client.ayon_core.ui.components.page_button.AYPageButton.set_value","title":"<code>set_value(text)</code>","text":"<p>Update the right-cluster value text and repaint.</p> <p>Parameters:</p> Name Type Description Default <code>text</code> <code>str</code> <p>New value string (empty string hides the value field).</p> required Source code in <code>client/ayon_core/ui/components/page_button.py</code> <pre><code>def set_value(self, text: str) -&gt; None:\n    \"\"\"Update the right-cluster value text and repaint.\n\n    Args:\n        text: New value string (empty string hides the value field).\n    \"\"\"\n    self._value = text\n    self.update()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/page_button.html#client.ayon_core.ui.components.page_button.AYPageButton.sizeHint","title":"<code>sizeHint()</code>","text":"<p>Return the preferred size of the widget.</p> <p>Returns:</p> Type Description <code>QSize</code> <p>A <code>QSize</code> with the preferred width and height.</p> Source code in <code>client/ayon_core/ui/components/page_button.py</code> <pre><code>def sizeHint(self) -&gt; QtCore.QSize:\n    \"\"\"Return the preferred size of the widget.\n\n    Returns:\n        A ``QSize`` with the preferred width and height.\n    \"\"\"\n    size = super().sizeHint()\n    size.setHeight(self._style_data[\"base\"].get(\"height\", 24))\n    return size\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/page_button.html#client.ayon_core.ui.components.page_button.AYPageButton.value","title":"<code>value()</code>","text":"<p>Return the current right-cluster value text.</p> <p>Returns:</p> Type Description <code>str</code> <p>The value string set at construction or via <code>set_value</code>.</p> Source code in <code>client/ayon_core/ui/components/page_button.py</code> <pre><code>def value(self) -&gt; str:\n    \"\"\"Return the current right-cluster value text.\n\n    Returns:\n        The value string set at construction or via ``set_value``.\n    \"\"\"\n    return self._value\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/qss_override.html","title":"qss_override","text":"<p>QSS override module. NOT CURRENTLY USED.</p>"},{"location":"autoapi/client/ayon_core/ui/components/screenshot_capture.html","title":"screenshot_capture","text":"<p>Screenshot capture widget and handler for text box component.</p> <p>Based on AYON core screenshot widget with proper DPI handling.</p>"},{"location":"autoapi/client/ayon_core/ui/components/screenshot_capture.html#client.ayon_core.ui.components.screenshot_capture.ScreenMarquee","title":"<code>ScreenMarquee</code>","text":"<p>               Bases: <code>QObject</code></p> <p>Screen marquee selection tool.</p> Source code in <code>client/ayon_core/ui/components/screenshot_capture.py</code> <pre><code>class ScreenMarquee(QObject):\n    \"\"\"Screen marquee selection tool.\"\"\"\n\n    def __init__(self, parent=None):\n        super().__init__(parent=parent)\n\n        screens_by_id = {}\n        for screen in QApplication.screens():\n            screen_id = uuid.uuid4().hex\n            screen_dialog = ScreenMarqueeDialog(screen, screen_id)\n            screens_by_id[screen_id] = screen_dialog\n            screen_dialog.mouse_moved.connect(self._on_mouse_move)\n            screen_dialog.mouse_pressed.connect(self._on_mouse_press)\n            screen_dialog.mouse_released.connect(self._on_mouse_release)\n            screen_dialog.close_requested.connect(self._on_close_request)\n\n        self._screens_by_id = screens_by_id\n        self._finished = False\n        self._captured = False\n        self._start_pos = None\n        self._end_pos = None\n        self._start_screen_id = None\n        self._pix = None\n\n    def get_captured_pixmap(self):\n        if self._pix is None:\n            return QPixmap()\n        return self._pix\n\n    def _close_dialogs(self):\n        for dialog in self._screens_by_id.values():\n            dialog.close()\n\n    def _on_close_request(self):\n        self._close_dialogs()\n        self._finished = True\n\n    def _on_mouse_release(self, pos):\n        start_screen_dialog = self._screens_by_id.get(self._start_screen_id)\n        if start_screen_dialog is None:\n            self._finished = True\n            self._captured = False\n            return\n\n        end_pos = start_screen_dialog.convert_end_pos(pos)\n        self._close_dialogs()\n        self._end_pos = end_pos\n        self._finished = True\n        self._captured = True\n\n    def _on_mouse_press(self, pos, screen_id):\n        self._start_pos = pos\n        self._start_screen_id = screen_id\n\n    def _on_mouse_move(self):\n        for dialog in self._screens_by_id.values():\n            dialog.repaint()\n\n    def start_capture(self):\n        for dialog in self._screens_by_id.values():\n            dialog.show()\n            dialog.setWindowState(Qt.WindowState.WindowActive)\n\n        app = QApplication.instance()\n        if app is not None:\n            while not self._finished:\n                app.processEvents()\n\n            # Give time to close dialogs\n            for _ in range(2):\n                app.processEvents()\n        else:\n            logger.error(\n                \"No QApplication instance found. \"\n                \"Capture may not work properly.\"\n            )\n            self._finished = True\n\n        if self._captured:\n            self._pix = self.get_desktop_pixmap(self._start_pos, self._end_pos)\n\n    @classmethod\n    def get_desktop_pixmap(cls, pos_start, pos_end):\n        \"\"\"Capture screen area between two points.\"\"\"\n        # Unify start and end points\n        if pos_start.y() &gt; pos_end.y():\n            pos_start, pos_end = pos_end, pos_start\n\n        if pos_start.x() &gt; pos_end.x():\n            new_start = QPoint(pos_end.x(), pos_start.y())\n            new_end = QPoint(pos_start.x(), pos_end.y())\n            pos_start = new_start\n            pos_end = new_end\n\n        # Validate rectangle\n        rect = QRect(pos_start, pos_end)\n        if rect.width() &lt; 1 or rect.height() &lt; 1:\n            return QPixmap()\n\n        screen = QApplication.screenAt(pos_start)\n        if screen is None:\n            return QPixmap()\n\n        return screen.grabWindow(\n            0,\n            pos_start.x() - screen.geometry().x(),\n            pos_start.y() - screen.geometry().y(),\n            pos_end.x() - pos_start.x(),\n            pos_end.y() - pos_start.y(),\n        )\n\n    @classmethod\n    def capture_to_pixmap(cls):\n        \"\"\"Take screenshot with marquee into pixmap.\"\"\"\n        tool = cls()\n        tool.start_capture()\n        return tool.get_captured_pixmap()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/screenshot_capture.html#client.ayon_core.ui.components.screenshot_capture.ScreenMarquee.capture_to_pixmap","title":"<code>capture_to_pixmap()</code>  <code>classmethod</code>","text":"<p>Take screenshot with marquee into pixmap.</p> Source code in <code>client/ayon_core/ui/components/screenshot_capture.py</code> <pre><code>@classmethod\ndef capture_to_pixmap(cls):\n    \"\"\"Take screenshot with marquee into pixmap.\"\"\"\n    tool = cls()\n    tool.start_capture()\n    return tool.get_captured_pixmap()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/screenshot_capture.html#client.ayon_core.ui.components.screenshot_capture.ScreenMarquee.get_desktop_pixmap","title":"<code>get_desktop_pixmap(pos_start, pos_end)</code>  <code>classmethod</code>","text":"<p>Capture screen area between two points.</p> Source code in <code>client/ayon_core/ui/components/screenshot_capture.py</code> <pre><code>@classmethod\ndef get_desktop_pixmap(cls, pos_start, pos_end):\n    \"\"\"Capture screen area between two points.\"\"\"\n    # Unify start and end points\n    if pos_start.y() &gt; pos_end.y():\n        pos_start, pos_end = pos_end, pos_start\n\n    if pos_start.x() &gt; pos_end.x():\n        new_start = QPoint(pos_end.x(), pos_start.y())\n        new_end = QPoint(pos_start.x(), pos_end.y())\n        pos_start = new_start\n        pos_end = new_end\n\n    # Validate rectangle\n    rect = QRect(pos_start, pos_end)\n    if rect.width() &lt; 1 or rect.height() &lt; 1:\n        return QPixmap()\n\n    screen = QApplication.screenAt(pos_start)\n    if screen is None:\n        return QPixmap()\n\n    return screen.grabWindow(\n        0,\n        pos_start.x() - screen.geometry().x(),\n        pos_start.y() - screen.geometry().y(),\n        pos_end.x() - pos_start.x(),\n        pos_end.y() - pos_start.y(),\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/screenshot_capture.html#client.ayon_core.ui.components.screenshot_capture.ScreenMarqueeDialog","title":"<code>ScreenMarqueeDialog</code>","text":"<p>               Bases: <code>QWidget</code></p> <p>Dialog for single screen marquee selection.</p> Source code in <code>client/ayon_core/ui/components/screenshot_capture.py</code> <pre><code>class ScreenMarqueeDialog(QWidget):\n    \"\"\"Dialog for single screen marquee selection.\"\"\"\n\n    mouse_moved = Signal()\n    mouse_pressed = Signal(QPoint, str)\n    mouse_released = Signal(QPoint)\n    close_requested = Signal()\n\n    def __init__(self, screen: QScreen, screen_id: str):\n        super().__init__()\n        self.setWindowFlags(\n            Qt.WindowType.Window\n            | Qt.WindowType.FramelessWindowHint\n            | Qt.WindowType.WindowStaysOnTopHint\n            | Qt.WindowType.CustomizeWindowHint\n        )\n        self.setAttribute(Qt.WidgetAttribute.WA_TranslucentBackground)\n        self.setCursor(Qt.CursorShape.CrossCursor)\n        self.setMouseTracking(True)\n\n        screen.geometryChanged.connect(self._fit_screen_geometry)\n\n        self._screen = screen\n        self._opacity = 100\n        self._click_pos = None\n        self._screen_id = screen_id\n\n    def set_click_pos(self, pos):\n        self._click_pos = pos\n        self.repaint()\n\n    def convert_end_pos(self, pos):\n        glob_pos = self.mapFromGlobal(pos)\n        new_pos = self._convert_pos(glob_pos)\n        return self.mapToGlobal(new_pos)\n\n    def paintEvent(self, event):\n        \"\"\"Paint event\"\"\"\n        mouse_pos = self._convert_pos(self.mapFromGlobal(QCursor.pos()))\n        rect = event.rect()\n        fill_path = QPainterPath()\n        fill_path.addRect(rect)\n\n        capture_rect = None\n        if self._click_pos is not None:\n            click_pos = self.mapFromGlobal(self._click_pos)\n            capture_rect = QRect(click_pos, mouse_pos)\n\n            # Clear the capture area\n            sub_path = QPainterPath()\n            sub_path.addRect(capture_rect)\n            fill_path = fill_path.subtracted(sub_path)\n\n        painter = QPainter(self)\n        painter.setRenderHints(\n            QPainter.RenderHint.Antialiasing\n            | QPainter.RenderHint.SmoothPixmapTransform\n        )\n\n        # Draw background\n        painter.setBrush(QColor(0, 0, 0, self._opacity))\n        painter.setPen(Qt.PenStyle.NoPen)\n        painter.drawPath(fill_path)\n\n        # Draw cropping markers at current mouse position\n        pen_color = QColor(255, 255, 255, self._opacity)\n        pen = QPen(pen_color, 1, Qt.PenStyle.DotLine)\n        painter.setPen(pen)\n        painter.drawLine(\n            rect.left(), mouse_pos.y(), rect.right(), mouse_pos.y()\n        )\n        painter.drawLine(\n            mouse_pos.x(), rect.top(), mouse_pos.x(), rect.bottom()\n        )\n\n        # Draw rectangle around selection area\n        if capture_rect is not None:\n            pen_color = QColor(92, 173, 214)\n            pen = QPen(pen_color, 2, Qt.PenStyle.SolidLine)\n            painter.setPen(pen)\n            painter.setBrush(Qt.BrushStyle.NoBrush)\n            l_x = capture_rect.left()\n            r_x = capture_rect.right()\n            if l_x &gt; r_x:\n                l_x, r_x = r_x, l_x\n            t_y = capture_rect.top()\n            b_y = capture_rect.bottom()\n            if t_y &gt; b_y:\n                t_y, b_y = b_y, t_y\n\n            r_x -= 1\n            b_y -= 1\n            sel_rect = QRect(QPoint(l_x, t_y), QPoint(r_x, b_y))\n            painter.drawRect(sel_rect)\n\n        painter.end()\n\n    def mousePressEvent(self, event):\n        \"\"\"Mouse click event\"\"\"\n        if event.button() == Qt.MouseButton.LeftButton:\n            self._click_pos = event.globalPos()\n            self.mouse_pressed.emit(self._click_pos, self._screen_id)\n\n    def mouseReleaseEvent(self, event):\n        \"\"\"Mouse release event\"\"\"\n        if event.button() == Qt.MouseButton.LeftButton:\n            self._click_pos = None\n            self.mouse_released.emit(event.globalPos())\n\n    def mouseMoveEvent(self, event):\n        \"\"\"Mouse move event\"\"\"\n        self.mouse_moved.emit()\n\n    def keyPressEvent(self, event):\n        \"\"\"Key press event\"\"\"\n        if event.key() == Qt.Key.Key_Escape:\n            self._click_pos = None\n            event.accept()\n            self.close_requested.emit()\n            return\n        return super().keyPressEvent(event)\n\n    def showEvent(self, event):\n        super().showEvent(event)\n        self._fit_screen_geometry()\n\n    def closeEvent(self, event):\n        self._click_pos = None\n        super().closeEvent(event)\n\n    def _convert_pos(self, pos):\n        geo = self.geometry()\n        if pos.x() &gt; geo.width():\n            pos.setX(geo.width() - 1)\n        elif pos.x() &lt; 0:\n            pos.setX(0)\n\n        if pos.y() &gt; geo.height():\n            pos.setY(geo.height() - 1)\n        elif pos.y() &lt; 0:\n            pos.setY(0)\n        return pos\n\n    def _fit_screen_geometry(self):\n        if hasattr(self, \"setScreen\"):\n            self.setScreen(self._screen)\n        self.setGeometry(self._screen.geometry())\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/screenshot_capture.html#client.ayon_core.ui.components.screenshot_capture.ScreenMarqueeDialog.keyPressEvent","title":"<code>keyPressEvent(event)</code>","text":"<p>Key press event</p> Source code in <code>client/ayon_core/ui/components/screenshot_capture.py</code> <pre><code>def keyPressEvent(self, event):\n    \"\"\"Key press event\"\"\"\n    if event.key() == Qt.Key.Key_Escape:\n        self._click_pos = None\n        event.accept()\n        self.close_requested.emit()\n        return\n    return super().keyPressEvent(event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/screenshot_capture.html#client.ayon_core.ui.components.screenshot_capture.ScreenMarqueeDialog.mouseMoveEvent","title":"<code>mouseMoveEvent(event)</code>","text":"<p>Mouse move event</p> Source code in <code>client/ayon_core/ui/components/screenshot_capture.py</code> <pre><code>def mouseMoveEvent(self, event):\n    \"\"\"Mouse move event\"\"\"\n    self.mouse_moved.emit()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/screenshot_capture.html#client.ayon_core.ui.components.screenshot_capture.ScreenMarqueeDialog.mousePressEvent","title":"<code>mousePressEvent(event)</code>","text":"<p>Mouse click event</p> Source code in <code>client/ayon_core/ui/components/screenshot_capture.py</code> <pre><code>def mousePressEvent(self, event):\n    \"\"\"Mouse click event\"\"\"\n    if event.button() == Qt.MouseButton.LeftButton:\n        self._click_pos = event.globalPos()\n        self.mouse_pressed.emit(self._click_pos, self._screen_id)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/screenshot_capture.html#client.ayon_core.ui.components.screenshot_capture.ScreenMarqueeDialog.mouseReleaseEvent","title":"<code>mouseReleaseEvent(event)</code>","text":"<p>Mouse release event</p> Source code in <code>client/ayon_core/ui/components/screenshot_capture.py</code> <pre><code>def mouseReleaseEvent(self, event):\n    \"\"\"Mouse release event\"\"\"\n    if event.button() == Qt.MouseButton.LeftButton:\n        self._click_pos = None\n        self.mouse_released.emit(event.globalPos())\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/screenshot_capture.html#client.ayon_core.ui.components.screenshot_capture.ScreenMarqueeDialog.paintEvent","title":"<code>paintEvent(event)</code>","text":"<p>Paint event</p> Source code in <code>client/ayon_core/ui/components/screenshot_capture.py</code> <pre><code>def paintEvent(self, event):\n    \"\"\"Paint event\"\"\"\n    mouse_pos = self._convert_pos(self.mapFromGlobal(QCursor.pos()))\n    rect = event.rect()\n    fill_path = QPainterPath()\n    fill_path.addRect(rect)\n\n    capture_rect = None\n    if self._click_pos is not None:\n        click_pos = self.mapFromGlobal(self._click_pos)\n        capture_rect = QRect(click_pos, mouse_pos)\n\n        # Clear the capture area\n        sub_path = QPainterPath()\n        sub_path.addRect(capture_rect)\n        fill_path = fill_path.subtracted(sub_path)\n\n    painter = QPainter(self)\n    painter.setRenderHints(\n        QPainter.RenderHint.Antialiasing\n        | QPainter.RenderHint.SmoothPixmapTransform\n    )\n\n    # Draw background\n    painter.setBrush(QColor(0, 0, 0, self._opacity))\n    painter.setPen(Qt.PenStyle.NoPen)\n    painter.drawPath(fill_path)\n\n    # Draw cropping markers at current mouse position\n    pen_color = QColor(255, 255, 255, self._opacity)\n    pen = QPen(pen_color, 1, Qt.PenStyle.DotLine)\n    painter.setPen(pen)\n    painter.drawLine(\n        rect.left(), mouse_pos.y(), rect.right(), mouse_pos.y()\n    )\n    painter.drawLine(\n        mouse_pos.x(), rect.top(), mouse_pos.x(), rect.bottom()\n    )\n\n    # Draw rectangle around selection area\n    if capture_rect is not None:\n        pen_color = QColor(92, 173, 214)\n        pen = QPen(pen_color, 2, Qt.PenStyle.SolidLine)\n        painter.setPen(pen)\n        painter.setBrush(Qt.BrushStyle.NoBrush)\n        l_x = capture_rect.left()\n        r_x = capture_rect.right()\n        if l_x &gt; r_x:\n            l_x, r_x = r_x, l_x\n        t_y = capture_rect.top()\n        b_y = capture_rect.bottom()\n        if t_y &gt; b_y:\n            t_y, b_y = b_y, t_y\n\n        r_x -= 1\n        b_y -= 1\n        sel_rect = QRect(QPoint(l_x, t_y), QPoint(r_x, b_y))\n        painter.drawRect(sel_rect)\n\n    painter.end()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/screenshot_capture.html#client.ayon_core.ui.components.screenshot_capture.ScreenshotHandler","title":"<code>ScreenshotHandler</code>","text":"<p>Handles screenshot capture for text box.</p> Source code in <code>client/ayon_core/ui/components/screenshot_capture.py</code> <pre><code>class ScreenshotHandler:\n    \"\"\"Handles screenshot capture for text box.\"\"\"\n\n    def __init__(self, parent_widget, screenshot_button):\n        \"\"\"Initialize screenshot handler.\n\n        Args:\n            parent_widget: Parent widget instance (typically AYTextBox)\n            screenshot_button: Screenshot button widget\n        \"\"\"\n        self._parent = parent_widget\n        self.screenshot_btn = screenshot_button\n        self._pending_screenshots = []\n        # Create a dedicated temp directory for this handler\n        self._temp_dir = tempfile.TemporaryDirectory(\n            prefix=\"ayon_screenshots_\"\n        )\n\n    def launch_capture(self):\n        \"\"\"Launch screenshot capture tool.\"\"\"\n        main_window = self._parent.window()\n        if main_window:\n            main_window.showMinimized()\n\n        QTimer.singleShot(200, self._show_snipping_widget)\n\n    def _show_snipping_widget(self):\n        \"\"\"Show snipping widget after window is minimized.\"\"\"\n        pixmap = ScreenMarquee.capture_to_pixmap()\n        self._on_screenshot_captured(pixmap)\n\n    def _on_screenshot_captured(self, pixmap):\n        \"\"\"Handle captured screenshot.\"\"\"\n        if pixmap and not pixmap.isNull():\n            screenshot_num = len(self._pending_screenshots) + 1\n            temp_path = os.path.join(\n                self._temp_dir.name, f\"ss{screenshot_num}.jpg\"\n            )\n            if pixmap.save(temp_path, \"JPEG\"):\n                self._pending_screenshots.append(temp_path)\n\n                # Add to parent's unified attachment system\n                if hasattr(self._parent, \"add_attachment\"):\n                    self._parent.add_attachment(temp_path, \"screenshot\")\n\n                logger.debug(\"Screenshot attached: %s\", temp_path)\n\n        self._restore_window()\n\n    def _restore_window(self):\n        \"\"\"Restore parent window.\"\"\"\n        main_window = self._parent.window()\n        if main_window:\n            main_window.showNormal()\n            main_window.activateWindow()\n\n    def get_screenshot_paths(self):\n        \"\"\"Get list of pending screenshot paths.\n\n        Returns:\n            list: List of screenshot file paths\n        \"\"\"\n        return self._pending_screenshots.copy()\n\n    def clear_screenshots(self):\n        \"\"\"Clear all pending screenshots and their files.\"\"\"\n        self._pending_screenshots.clear()\n        # Recreate the temp dir (deletes old files)\n        self._temp_dir.cleanup()\n        self._temp_dir = tempfile.TemporaryDirectory(\n            prefix=\"ayon_screenshots_\"\n        )\n\n    def cleanup(self):\n        \"\"\"Delete all screenshot files. Call when handler is\n        no longer needed.\"\"\"\n        self._pending_screenshots.clear()\n        self._temp_dir.cleanup()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/screenshot_capture.html#client.ayon_core.ui.components.screenshot_capture.ScreenshotHandler.__init__","title":"<code>__init__(parent_widget, screenshot_button)</code>","text":"<p>Initialize screenshot handler.</p> <p>Parameters:</p> Name Type Description Default <code>parent_widget</code> <p>Parent widget instance (typically AYTextBox)</p> required <code>screenshot_button</code> <p>Screenshot button widget</p> required Source code in <code>client/ayon_core/ui/components/screenshot_capture.py</code> <pre><code>def __init__(self, parent_widget, screenshot_button):\n    \"\"\"Initialize screenshot handler.\n\n    Args:\n        parent_widget: Parent widget instance (typically AYTextBox)\n        screenshot_button: Screenshot button widget\n    \"\"\"\n    self._parent = parent_widget\n    self.screenshot_btn = screenshot_button\n    self._pending_screenshots = []\n    # Create a dedicated temp directory for this handler\n    self._temp_dir = tempfile.TemporaryDirectory(\n        prefix=\"ayon_screenshots_\"\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/screenshot_capture.html#client.ayon_core.ui.components.screenshot_capture.ScreenshotHandler.cleanup","title":"<code>cleanup()</code>","text":"<p>Delete all screenshot files. Call when handler is no longer needed.</p> Source code in <code>client/ayon_core/ui/components/screenshot_capture.py</code> <pre><code>def cleanup(self):\n    \"\"\"Delete all screenshot files. Call when handler is\n    no longer needed.\"\"\"\n    self._pending_screenshots.clear()\n    self._temp_dir.cleanup()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/screenshot_capture.html#client.ayon_core.ui.components.screenshot_capture.ScreenshotHandler.clear_screenshots","title":"<code>clear_screenshots()</code>","text":"<p>Clear all pending screenshots and their files.</p> Source code in <code>client/ayon_core/ui/components/screenshot_capture.py</code> <pre><code>def clear_screenshots(self):\n    \"\"\"Clear all pending screenshots and their files.\"\"\"\n    self._pending_screenshots.clear()\n    # Recreate the temp dir (deletes old files)\n    self._temp_dir.cleanup()\n    self._temp_dir = tempfile.TemporaryDirectory(\n        prefix=\"ayon_screenshots_\"\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/screenshot_capture.html#client.ayon_core.ui.components.screenshot_capture.ScreenshotHandler.get_screenshot_paths","title":"<code>get_screenshot_paths()</code>","text":"<p>Get list of pending screenshot paths.</p> <p>Returns:</p> Name Type Description <code>list</code> <p>List of screenshot file paths</p> Source code in <code>client/ayon_core/ui/components/screenshot_capture.py</code> <pre><code>def get_screenshot_paths(self):\n    \"\"\"Get list of pending screenshot paths.\n\n    Returns:\n        list: List of screenshot file paths\n    \"\"\"\n    return self._pending_screenshots.copy()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/screenshot_capture.html#client.ayon_core.ui.components.screenshot_capture.ScreenshotHandler.launch_capture","title":"<code>launch_capture()</code>","text":"<p>Launch screenshot capture tool.</p> Source code in <code>client/ayon_core/ui/components/screenshot_capture.py</code> <pre><code>def launch_capture(self):\n    \"\"\"Launch screenshot capture tool.\"\"\"\n    main_window = self._parent.window()\n    if main_window:\n        main_window.showMinimized()\n\n    QTimer.singleShot(200, self._show_snipping_widget)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/scroll_area.html","title":"scroll_area","text":"<p>scroll area</p>"},{"location":"autoapi/client/ayon_core/ui/components/scroll_area.html#client.ayon_core.ui.components.scroll_area.AYScrollArea","title":"<code>AYScrollArea</code>","text":"<p>               Bases: <code>StyleMixin</code>, <code>QScrollArea</code></p> <p>AYON styled scroll area widget.</p> <p>Overrides Qt's stylesheet painting with AYONStyle custom rendering.</p> <p>Parameters:</p> Name Type Description Default <code>*args</code> <p>Positional arguments passed to QTextEdit.</p> <code>()</code> <code>**kwargs</code> <p>Keyword arguments passed to QTextEdit.</p> <code>{}</code> Source code in <code>client/ayon_core/ui/components/scroll_area.py</code> <pre><code>class AYScrollArea(StyleMixin, QScrollArea):\n    \"\"\"AYON styled scroll area widget.\n\n    Overrides Qt's stylesheet painting with AYONStyle custom rendering.\n\n    Args:\n        *args: Positional arguments passed to QTextEdit.\n        **kwargs: Keyword arguments passed to QTextEdit.\n    \"\"\"\n\n    def __init__(self, *args, **kwargs):\n        super().__init__(*args, **kwargs)\n        self.setStyle(get_ayon_style())\n        # AYONStyle's FrameDrawer would otherwise draw a 1px frame around the\n        # viewport, which shows as a thin line on the right edge (above the\n        # scrollbar) and along the bottom.\n        self.setFrameShape(QFrame.Shape.NoFrame)\n\n        self.setVerticalScrollBar(AYScrollBar(Qt.Orientation.Vertical))\n        self.setVerticalScrollBarPolicy(Qt.ScrollBarPolicy.ScrollBarAsNeeded)\n        self.setHorizontalScrollBar(AYScrollBar(Qt.Orientation.Horizontal))\n        self.setHorizontalScrollBarPolicy(Qt.ScrollBarPolicy.ScrollBarAsNeeded)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/scroll_area.html#client.ayon_core.ui.components.scroll_area.AYScrollBar","title":"<code>AYScrollBar</code>","text":"<p>               Bases: <code>StyleMixin</code>, <code>QScrollBar</code></p> <p>AYON styled scroll bar widget.</p> <p>Overrides Qt's stylesheet painting with AYONStyle custom rendering.</p> <p>Parameters:</p> Name Type Description Default <code>*args</code> <p>Positional arguments passed to QTextEdit.</p> <code>()</code> <code>**kwargs</code> <p>Keyword arguments passed to QTextEdit.</p> <code>{}</code> Source code in <code>client/ayon_core/ui/components/scroll_area.py</code> <pre><code>class AYScrollBar(StyleMixin, QScrollBar):\n    \"\"\"AYON styled scroll bar widget.\n\n    Overrides Qt's stylesheet painting with AYONStyle custom rendering.\n\n    Args:\n        *args: Positional arguments passed to QTextEdit.\n        **kwargs: Keyword arguments passed to QTextEdit.\n    \"\"\"\n\n    def __init__(self, *args, **kwargs) -&gt; None:\n        super().__init__(*args, **kwargs)\n        self.setStyle(get_ayon_style())\n\n    def initStyleOption(self, option: QStyleOptionSlider) -&gt; None:\n        super().initStyleOption(option)\n        SC = QStyle.SubControl\n        option.subControls = (\n            SC.SC_None\n            | SC.SC_ScrollBarAddPage\n            | SC.SC_ScrollBarSubPage\n            | SC.SC_ScrollBarSlider\n        )\n\n    def paintEvent(self, arg__1: QPaintEvent) -&gt; None:\n        p = QPainter(self)\n        option = QStyleOptionSlider()\n        self.initStyleOption(option)\n        get_ayon_style().drawComplexControl(\n            QStyle.ComplexControl.CC_ScrollBar, option, p, self\n        )\n        return\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/slicer.html","title":"slicer","text":""},{"location":"autoapi/client/ayon_core/ui/components/slicer.html#client.ayon_core.ui.components.slicer.AYSlicer","title":"<code>AYSlicer</code>","text":"<p>               Bases: <code>AYContainer</code></p> Source code in <code>client/ayon_core/ui/components/slicer.py</code> <pre><code>class AYSlicer(AYContainer):\n    category_changed = Signal(str)\n\n    def __init__(self, item_list=None, parent=None, initial_text=\"\"):\n        super().__init__(\n            layout=AYContainer.Layout.HBox,\n            variant=AYContainer.Variants.Low,\n            parent=parent,\n        )\n        self._combo = AYComboBox(\n            items=item_list,\n            variant=AYComboBox.Variants.Low,\n        )\n        if initial_text:\n            self._combo.setCurrentText(initial_text)\n        self._field = AYLineEdit(placeholder=\"Search\")\n        self._button = AYButton(\n            variant=AYButton.Variants.Nav,\n            icon=\"search\",\n            icon_on=\"close\",\n            checkable=True,\n        )\n\n        self.add_widget(self._combo)\n        self.add_widget(self._field, stretch=1)\n        self.add_widget(self._button)\n\n        self._field.setVisible(False)\n\n        # search filter\n        self._proxy: TreeFilterProxyModel | None = None\n\n        self._field.installEventFilter(self)\n\n        # signals\n        self._button.toggled.connect(self._on_button_toggled)\n        self._field.textChanged.connect(self._on_search_changed)\n        self._combo.currentTextChanged.connect(self._on_category_changed)\n\n    def current_category(self) -&gt; str:\n        return self._combo.currentText()\n\n    def _on_category_changed(self, text: str):\n        \"\"\"Notify other widgets of the criteria change.\"\"\"\n        self.category_changed.emit(text)\n\n    def set_model(\n        self,\n        model: QAbstractItemModel,\n        view=None,\n    ):\n        \"\"\"Insert a filter proxy between model and view.\n\n        Args:\n            model: The source model (e.g. LazyTreeModel).\n            view: The QAbstractItemView that displays the model.\n        \"\"\"\n        self._proxy = TreeFilterProxyModel(self)\n        self._proxy.setSourceModel(model)\n        if view is not None:\n            view.setModel(self._proxy)\n        self._view = view\n\n    def _on_search_changed(self, text: str):\n        \"\"\"Update the proxy filter when the user types.\"\"\"\n        if self._proxy is None:\n            return\n        self._proxy.setFilterFixedString(text)\n\n    def _on_button_toggled(self, checked):\n        self._combo.setVisible(not checked)\n        self._field.setVisible(checked)\n        if checked:\n            self._field.setFocus()\n        else:\n            # clear the filter when closing search\n            self._field.clear()\n\n    def eventFilter(self, obj, event):\n        \"\"\"Close search field on Escape key press.\"\"\"\n        if (\n            obj is self._field\n            and event.type() == QEvent.Type.KeyPress\n            and event.key() == Qt.Key.Key_Escape\n        ):\n            self._button.setChecked(False)  # triggers _on_button_toggled\n            return True\n        return super().eventFilter(obj, event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/slicer.html#client.ayon_core.ui.components.slicer.AYSlicer.eventFilter","title":"<code>eventFilter(obj, event)</code>","text":"<p>Close search field on Escape key press.</p> Source code in <code>client/ayon_core/ui/components/slicer.py</code> <pre><code>def eventFilter(self, obj, event):\n    \"\"\"Close search field on Escape key press.\"\"\"\n    if (\n        obj is self._field\n        and event.type() == QEvent.Type.KeyPress\n        and event.key() == Qt.Key.Key_Escape\n    ):\n        self._button.setChecked(False)  # triggers _on_button_toggled\n        return True\n    return super().eventFilter(obj, event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/slicer.html#client.ayon_core.ui.components.slicer.AYSlicer.set_model","title":"<code>set_model(model, view=None)</code>","text":"<p>Insert a filter proxy between model and view.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>QAbstractItemModel</code> <p>The source model (e.g. LazyTreeModel).</p> required <code>view</code> <p>The QAbstractItemView that displays the model.</p> <code>None</code> Source code in <code>client/ayon_core/ui/components/slicer.py</code> <pre><code>def set_model(\n    self,\n    model: QAbstractItemModel,\n    view=None,\n):\n    \"\"\"Insert a filter proxy between model and view.\n\n    Args:\n        model: The source model (e.g. LazyTreeModel).\n        view: The QAbstractItemView that displays the model.\n    \"\"\"\n    self._proxy = TreeFilterProxyModel(self)\n    self._proxy.setSourceModel(model)\n    if view is not None:\n        view.setModel(self._proxy)\n    self._view = view\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/slicer.html#client.ayon_core.ui.components.slicer.TreeFilterProxyModel","title":"<code>TreeFilterProxyModel</code>","text":"<p>               Bases: <code>QSortFilterProxyModel</code></p> <p>Proxy that filters tree items by label, recursively.</p> Source code in <code>client/ayon_core/ui/components/slicer.py</code> <pre><code>class TreeFilterProxyModel(QSortFilterProxyModel):\n    \"\"\"Proxy that filters tree items by label, recursively.\"\"\"\n\n    def __init__(self, parent=None):\n        super().__init__(parent)\n        self.setFilterCaseSensitivity(Qt.CaseSensitivity.CaseInsensitive)\n        self.setFilterRole(Qt.ItemDataRole.DisplayRole)\n        self.setRecursiveFilteringEnabled(True)  # Qt 5.10+\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/slider.html","title":"slider","text":"<p>AYSlider component module.</p>"},{"location":"autoapi/client/ayon_core/ui/components/slider.html#client.ayon_core.ui.components.slider.AYSlider","title":"<code>AYSlider</code>","text":"<p>               Bases: <code>AYContainer</code></p> <p>Composite slider widget: caption label + slider bar + value label.</p> <p>Layout (VBox root)::</p> <pre><code>\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n\u2502 Caption label (optional)        \u2502\n\u2502 [========\u25cb\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500] 220   \u2502\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n</code></pre> <p>The numeric value display on the right is a read-only <code>AYLabel</code> that updates live as the slider moves.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <code>QWidget | None</code> <p>Parent widget.</p> <code>None</code> <code>label</code> <code>str</code> <p>Caption text shown above the slider (hidden when empty).</p> <code>''</code> <code>value</code> <code>int</code> <p>Initial integer value.</p> <code>0</code> <code>minimum</code> <code>int</code> <p>Minimum integer value.</p> <code>0</code> <code>maximum</code> <code>int</code> <p>Maximum integer value.</p> <code>100</code> <code>step</code> <code>int</code> <p>Snapping step; values are always multiples of this.</p> <code>1</code> <code>variant</code> <code>Variants</code> <p>Visual style variant.</p> <code>Default</code> Signals <p>value_changed (int): Emitted whenever the slider value changes.</p> Source code in <code>client/ayon_core/ui/components/slider.py</code> <pre><code>class AYSlider(AYContainer):\n    \"\"\"Composite slider widget: caption label + slider bar + value label.\n\n    Layout (VBox root)::\n\n        \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n        \u2502 Caption label (optional)        \u2502\n        \u2502 [========\u25cb\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500] 220   \u2502\n        \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n\n    The numeric value display on the right is a read-only ``AYLabel`` that\n    updates live as the slider moves.\n\n    Args:\n        parent: Parent widget.\n        label: Caption text shown above the slider (hidden when empty).\n        value: Initial integer value.\n        minimum: Minimum integer value.\n        maximum: Maximum integer value.\n        step: Snapping step; values are always multiples of this.\n        variant: Visual style variant.\n\n    Signals:\n        value_changed (int): Emitted whenever the slider value changes.\n    \"\"\"\n\n    Variants = AYSliderVariants\n    value_changed = Signal(int)\n\n    def __init__(\n        self,\n        parent: QWidget | None = None,\n        label: str = \"\",\n        value: int = 0,\n        minimum: int = 0,\n        maximum: int = 100,\n        step: int = 1,\n        variant: Variants = Variants.Default,\n    ) -&gt; None:\n        super().__init__(\n            parent,\n            layout=AYContainer.Layout.VBox,\n            variant=AYContainer.Variants(variant.value),\n            layout_spacing=5,\n            layout_margin=0,\n        )\n\n        self._variant_str = variant.value\n        self._style_data = get_ayon_style().model.get_styles(\n            \"AYSlider\",\n            variant=self._variant_str,\n        )\n        self._step = max(1, step)\n\n        # --- Caption label ---------------------------------------------------\n        self._caption: AYLabel | None = None\n        if label:\n            self._caption = AYLabel(\n                label,\n                text_color=self._style_data[\"base\"].get(\n                    \"label-color\", \"#ffffff\"\n                ),\n                bold=self._style_data[\"base\"].get(\"label-font-weight\", 600)\n                &gt; 500,\n            )\n            self.add_widget(self._caption)\n\n        # --- Inner HBox row --------------------------------------------------\n        row_spacing = self._style_data[\"base\"].get(\"row-spacing\", 6)\n        self._row = AYContainer(\n            layout=AYContainer.Layout.HBox,\n            variant=AYContainer.Variants(self._variant_str),\n            layout_spacing=row_spacing,\n            layout_margin=0,\n        )\n\n        # Slider bar\n        self._bar = AYSliderBar(variant=AYSlider.Variants(self._variant_str))\n        self._bar.setMinimum(minimum)\n        self._bar.setMaximum(maximum)\n        self._bar.setSingleStep(self._step)\n        self._bar.setPageStep(self._step * 10)\n\n        # Value label \u2014 initialise with the snapped initial value.\n        snapped_initial = self._snap_unclamped(value, minimum, maximum)\n        self._value_label = AYLabel(\n            str(snapped_initial),\n            variant=AYLabel.Variants.Default,\n            text_color=self._style_data[\"base\"].get(\"value-color\", \"#ffffff\"),\n        )\n        self._value_label.setAlignment(Qt.AlignmentFlag.AlignRight)\n        value_min_w = self._style_data[\"base\"].get(\"value-min-width\", 40)\n        self._value_label.setMinimumWidth(value_min_w)\n\n        self._row.add_widget(self._bar, stretch=1)\n        self._row.add_widget(self._value_label, stretch=0)\n        self.add_widget(self._row)\n\n        # Connect before setValue so _on_bar_value_changed fires and keeps\n        # the label in sync with the snapped value from the very first set.\n        self._bar.valueChanged.connect(self._on_bar_value_changed)\n        self._bar.setValue(snapped_initial)\n\n    # -------------------------------------------------------------------------\n    # Private helpers\n    # -------------------------------------------------------------------------\n\n    def _snap_unclamped(self, v: int, minimum: int, maximum: int) -&gt; int:\n        \"\"\"Snap *v* to the nearest step and clamp to [minimum, maximum].\n\n        This version accepts explicit range arguments so it can be called\n        before ``self._bar`` has its range configured.\n\n        Args:\n            v: Raw integer value.\n            minimum: Lower bound.\n            maximum: Upper bound.\n\n        Returns:\n            Clamped and snapped integer value.\n        \"\"\"\n        v = max(minimum, min(maximum, v))\n        if self._step &gt; 1:\n            v = round(v / self._step) * self._step\n            v = max(minimum, min(maximum, v))\n        return v\n\n    def _snap(self, v: int) -&gt; int:\n        \"\"\"Snap *v* to the nearest multiple of ``_step`` within range.\n\n        Args:\n            v: Raw integer value.\n\n        Returns:\n            Clamped and snapped integer value.\n        \"\"\"\n        return self._snap_unclamped(\n            v, self._bar.minimum(), self._bar.maximum()\n        )\n\n    def _on_bar_value_changed(self, raw: int) -&gt; None:\n        \"\"\"Snap the raw slider value, sync the label, emit the signal.\n\n        Args:\n            raw: The raw integer value emitted by ``QSlider.valueChanged``.\n        \"\"\"\n        snapped = self._snap(raw)\n        if snapped != raw:\n            # Re-set without entering an infinite loop.\n            self._bar.blockSignals(True)\n            self._bar.setValue(snapped)\n            self._bar.blockSignals(False)\n\n        self._value_label.setText(str(snapped))\n        self.value_changed.emit(snapped)\n\n    # -------------------------------------------------------------------------\n    # Public API\n    # -------------------------------------------------------------------------\n\n    def value(self) -&gt; int:\n        \"\"\"Return the current slider value.\n\n        Returns:\n            Current snapped integer value.\n        \"\"\"\n        return self._snap(self._bar.value())\n\n    def setValue(self, v: int) -&gt; None:\n        \"\"\"Set the slider to *v* (clamped and snapped).\n\n        Args:\n            v: Target integer value.\n        \"\"\"\n        self._bar.setValue(self._snap(v))\n\n    def setRange(self, minimum: int, maximum: int) -&gt; None:\n        \"\"\"Update the slider range.\n\n        Args:\n            minimum: New minimum value.\n            maximum: New maximum value.\n        \"\"\"\n        self._bar.setMinimum(minimum)\n        self._bar.setMaximum(maximum)\n\n    def setStep(self, step: int) -&gt; None:\n        \"\"\"Change the snapping step.\n\n        Args:\n            step: New step size (must be &gt;= 1).\n        \"\"\"\n        self._step = max(1, step)\n        self._bar.setSingleStep(self._step)\n        self._bar.setPageStep(self._step * 10)\n        # Re-snap current value to the new step.\n        self._bar.setValue(self._snap(self._bar.value()))\n\n    def setLabel(self, text: str) -&gt; None:\n        \"\"\"Update the caption label text.\n\n        Args:\n            text: New caption string.\n        \"\"\"\n        if self._caption is not None:\n            self._caption.setText(text)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/slider.html#client.ayon_core.ui.components.slider.AYSlider.setLabel","title":"<code>setLabel(text)</code>","text":"<p>Update the caption label text.</p> <p>Parameters:</p> Name Type Description Default <code>text</code> <code>str</code> <p>New caption string.</p> required Source code in <code>client/ayon_core/ui/components/slider.py</code> <pre><code>def setLabel(self, text: str) -&gt; None:\n    \"\"\"Update the caption label text.\n\n    Args:\n        text: New caption string.\n    \"\"\"\n    if self._caption is not None:\n        self._caption.setText(text)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/slider.html#client.ayon_core.ui.components.slider.AYSlider.setRange","title":"<code>setRange(minimum, maximum)</code>","text":"<p>Update the slider range.</p> <p>Parameters:</p> Name Type Description Default <code>minimum</code> <code>int</code> <p>New minimum value.</p> required <code>maximum</code> <code>int</code> <p>New maximum value.</p> required Source code in <code>client/ayon_core/ui/components/slider.py</code> <pre><code>def setRange(self, minimum: int, maximum: int) -&gt; None:\n    \"\"\"Update the slider range.\n\n    Args:\n        minimum: New minimum value.\n        maximum: New maximum value.\n    \"\"\"\n    self._bar.setMinimum(minimum)\n    self._bar.setMaximum(maximum)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/slider.html#client.ayon_core.ui.components.slider.AYSlider.setStep","title":"<code>setStep(step)</code>","text":"<p>Change the snapping step.</p> <p>Parameters:</p> Name Type Description Default <code>step</code> <code>int</code> <p>New step size (must be &gt;= 1).</p> required Source code in <code>client/ayon_core/ui/components/slider.py</code> <pre><code>def setStep(self, step: int) -&gt; None:\n    \"\"\"Change the snapping step.\n\n    Args:\n        step: New step size (must be &gt;= 1).\n    \"\"\"\n    self._step = max(1, step)\n    self._bar.setSingleStep(self._step)\n    self._bar.setPageStep(self._step * 10)\n    # Re-snap current value to the new step.\n    self._bar.setValue(self._snap(self._bar.value()))\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/slider.html#client.ayon_core.ui.components.slider.AYSlider.setValue","title":"<code>setValue(v)</code>","text":"<p>Set the slider to v (clamped and snapped).</p> <p>Parameters:</p> Name Type Description Default <code>v</code> <code>int</code> <p>Target integer value.</p> required Source code in <code>client/ayon_core/ui/components/slider.py</code> <pre><code>def setValue(self, v: int) -&gt; None:\n    \"\"\"Set the slider to *v* (clamped and snapped).\n\n    Args:\n        v: Target integer value.\n    \"\"\"\n    self._bar.setValue(self._snap(v))\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/slider.html#client.ayon_core.ui.components.slider.AYSlider.value","title":"<code>value()</code>","text":"<p>Return the current slider value.</p> <p>Returns:</p> Type Description <code>int</code> <p>Current snapped integer value.</p> Source code in <code>client/ayon_core/ui/components/slider.py</code> <pre><code>def value(self) -&gt; int:\n    \"\"\"Return the current slider value.\n\n    Returns:\n        Current snapped integer value.\n    \"\"\"\n    return self._snap(self._bar.value())\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/slider.html#client.ayon_core.ui.components.slider.AYSliderBar","title":"<code>AYSliderBar</code>","text":"<p>               Bases: <code>StyleMixin</code>, <code>QSlider</code></p> <p>Custom-painted horizontal slider track and handle.</p> <p>Reads all visual properties (track height, handle size, colors, etc.) from <code>ayon_style.json</code> via the <code>AYSlider</code> variant block.  The native Qt style is still routed through <code>AYONStyle</code> (<code>setStyle</code>), but the visual painting is done entirely in <code>paintEvent</code> so there is no dependency on <code>drawComplexControl(CC_Slider, ...)</code>.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <code>QWidget | None</code> <p>Parent widget.</p> <code>None</code> <code>variant_str</code> <p>Variant string key used to fetch the style dict.</p> required Source code in <code>client/ayon_core/ui/components/slider.py</code> <pre><code>class AYSliderBar(StyleMixin, QSlider):\n    \"\"\"Custom-painted horizontal slider track and handle.\n\n    Reads all visual properties (track height, handle size, colors, etc.)\n    from ``ayon_style.json`` via the ``AYSlider`` variant block.  The native\n    Qt style is still routed through ``AYONStyle`` (``setStyle``), but the\n    visual painting is done entirely in ``paintEvent`` so there is no\n    dependency on ``drawComplexControl(CC_Slider, ...)``.\n\n    Args:\n        parent: Parent widget.\n        variant_str: Variant string key used to fetch the style dict.\n    \"\"\"\n\n    def __init__(\n        self,\n        parent: QWidget | None = None,\n        variant: AYSliderVariants = AYSliderVariants.Default,\n    ) -&gt; None:\n        self._variant_str = variant.value\n        self._style_data = StyleDict()\n        super().__init__(Qt.Orientation.Horizontal, parent)\n\n        # Remove all native decoration \u2014 we paint everything ourselves.\n        self.setAttribute(Qt.WidgetAttribute.WA_Hover, True)\n        self.setAttribute(Qt.WidgetAttribute.WA_StyledBackground, False)\n\n        _style = get_ayon_style()\n        self.setStyle(_style)\n        self._style_data = _style.model.get_styles(\n            \"AYSlider\",\n            variant=self._variant_str,\n        )\n        self._style_data.set_context(self)\n\n    def _draw_slider(self, painter: QPainter, style: StyleDict) -&gt; None:\n        \"\"\"Draw track, filled portion, and handle.\n\n        Args:\n            painter: QPainter instance (already configured).\n            style: Style dictionary for the current state.\n        \"\"\"\n        track_h = style.get(\"track-height\", 4)\n        track_radius = style.get(\"track-radius\", 2)\n        track_color = QColor(style.get(\"track-color\", \"#41474d\"))\n        filled_color = QColor(style.get(\"filled-color\", \"#8fceff\"))\n        handle_size = style.get(\"handle-size\", 14)\n        handle_color = QColor(style.get(\"handle-color\", \"#8fceff\"))\n        handle_border_color = QColor(\n            style.get(\"handle-border-color\", \"#8fceff\")\n        )\n        handle_border_width = style.get(\"handle-border-width\", 0)\n\n        w = self.width()\n        h = self.height()\n\n        # Horizontal padding so the handle centre can reach both ends.\n        pad = 0\n        track_y = (h - track_h) / 2.0\n        track_rect = QRectF(pad, track_y, w - pad * 2, track_h)\n\n        minimum = self.minimum()\n        maximum = self.maximum()\n        value = self.value()\n        span = maximum - minimum\n        ratio = (value - minimum) / span if span &gt; 0 else 0.0\n\n        filled_w = track_rect.width() * ratio\n        filled_rect = QRectF(track_rect.x(), track_rect.y(), filled_w, track_h)\n\n        # Empty track\n        painter.setPen(Qt.PenStyle.NoPen)\n        painter.setBrush(QBrush(track_color))\n        painter.drawRoundedRect(track_rect, track_radius, track_radius)\n\n        # Filled portion (clip to left half of track)\n        if filled_w &gt; 0:\n            painter.setBrush(QBrush(filled_color))\n            painter.drawRoundedRect(filled_rect, track_radius, track_radius)\n\n        # Handle\n        h_size = handle_size / 2.0\n        t_width = track_rect.width() - handle_size\n        handle_x = track_rect.x() + h_size + t_width * ratio\n        handle_y = h / 2.0\n        handle_r = handle_size / 2.0\n        handle_rect = QRectF(\n            handle_x - handle_r,\n            handle_y - handle_r,\n            handle_size,\n            handle_size,\n        )\n\n        if handle_border_width &gt; 0:\n            pen = QPen(handle_border_color)\n            pen.setWidthF(handle_border_width)\n            painter.setPen(pen)\n        else:\n            painter.setPen(Qt.PenStyle.NoPen)\n\n        painter.setBrush(QBrush(handle_color))\n        painter.drawEllipse(handle_rect)\n\n    def paintEvent(self, event: QPaintEvent) -&gt; None:\n        \"\"\"Paint track, filled portion, and handle using QPainter directly.\n\n        Args:\n            event: The paint event (unused \u2014 we repaint the full widget).\n        \"\"\"\n        is_disabled = not self.isEnabled()\n        is_hover = self.underMouse()\n\n        if is_disabled:\n            state = \"disabled\"\n        elif is_hover:\n            state = \"hover\"\n        else:\n            state = \"base\"\n\n        style = self._style_data[state]\n\n        opacity = style.get(\"opacity\", 1.0)\n\n        painter = QPainter(self)\n        painter.setRenderHint(QPainter.RenderHint.Antialiasing)\n\n        if opacity &lt; 1.0:\n            buffer = QPixmap(self.size() * self.devicePixelRatioF())\n            buffer.setDevicePixelRatio(self.devicePixelRatioF())\n            buffer.fill(Qt.GlobalColor.transparent)\n\n            bp = QPainter(buffer)\n            bp.setRenderHint(QPainter.RenderHint.Antialiasing)\n            self._draw_slider(bp, style)\n            bp.end()\n\n            painter.setOpacity(opacity)\n            painter.drawPixmap(0, 0, buffer)\n        else:\n            self._draw_slider(painter, style)\n\n        painter.end()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/slider.html#client.ayon_core.ui.components.slider.AYSliderBar.paintEvent","title":"<code>paintEvent(event)</code>","text":"<p>Paint track, filled portion, and handle using QPainter directly.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <code>QPaintEvent</code> <p>The paint event (unused \u2014 we repaint the full widget).</p> required Source code in <code>client/ayon_core/ui/components/slider.py</code> <pre><code>def paintEvent(self, event: QPaintEvent) -&gt; None:\n    \"\"\"Paint track, filled portion, and handle using QPainter directly.\n\n    Args:\n        event: The paint event (unused \u2014 we repaint the full widget).\n    \"\"\"\n    is_disabled = not self.isEnabled()\n    is_hover = self.underMouse()\n\n    if is_disabled:\n        state = \"disabled\"\n    elif is_hover:\n        state = \"hover\"\n    else:\n        state = \"base\"\n\n    style = self._style_data[state]\n\n    opacity = style.get(\"opacity\", 1.0)\n\n    painter = QPainter(self)\n    painter.setRenderHint(QPainter.RenderHint.Antialiasing)\n\n    if opacity &lt; 1.0:\n        buffer = QPixmap(self.size() * self.devicePixelRatioF())\n        buffer.setDevicePixelRatio(self.devicePixelRatioF())\n        buffer.fill(Qt.GlobalColor.transparent)\n\n        bp = QPainter(buffer)\n        bp.setRenderHint(QPainter.RenderHint.Antialiasing)\n        self._draw_slider(bp, style)\n        bp.end()\n\n        painter.setOpacity(opacity)\n        painter.drawPixmap(0, 0, buffer)\n    else:\n        self._draw_slider(painter, style)\n\n    painter.end()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/style_mixin.html","title":"style_mixin","text":""},{"location":"autoapi/client/ayon_core/ui/components/style_mixin.html#client.ayon_core.ui.components.style_mixin.StyleMixin","title":"<code>StyleMixin</code>","text":"<p>Mixin class for overriding QWidget's font, fontMetrics, and palette methods.</p> <p>This mixin provides reliable overrides for style-related methods that should return the custom style values instead of the default QWidget values.</p> <p>Fonts: - <code>font()</code> method returns a QFont instance based on the AYON style. - <code>setFont()</code> does not set the font but propagates our style's font. - NOTE: To actually change a widget's font, use / implement the <code>set_font()</code> method. - <code>fontMetrics()</code> method returns a QFontMetrics instance based the current style font.</p> <p>Palettes: - <code>palette()</code> method returns a QPalette instance based on the AYON style. - <code>setPalette()</code> does not set the palette but propagates our style's palette. - NOTE: To actually change a widget's palette, use / implement the <code>set_palette()</code> method.</p> Source code in <code>client/ayon_core/ui/components/style_mixin.py</code> <pre><code>class StyleMixin:\n    \"\"\"Mixin class for overriding QWidget's font, fontMetrics, and palette\n    methods.\n\n    This mixin provides reliable overrides for style-related methods that\n    should return the custom style values instead of the default QWidget\n    values.\n\n    Fonts:\n    - `font()` method returns a QFont instance based on the AYON style.\n    - `setFont()` does not set the font but propagates our style's font.\n    - NOTE: To actually change a widget's font, use / implement the\n    `set_font()` method.\n    - `fontMetrics()` method returns a QFontMetrics instance based the current\n    style font.\n\n    Palettes:\n    - `palette()` method returns a QPalette instance based on the AYON style.\n    - `setPalette()` does not set the palette but propagates our style's\n    palette.\n    - NOTE: To actually change a widget's palette, use / implement the\n    `set_palette()` method.\n    \"\"\"\n\n    def __init__(self, *args, **kwargs):\n        super().__init__(*args, **kwargs)\n\n        # Initialize with our style's default font and palette.\n        _style = get_ayon_style()\n        self._style_font: QFont = _style.model.base_font\n        self._style_palette: QPalette = _style.model.base_palette\n\n    def font(self) -&gt; QFont:\n        \"\"\"Override to return our custom style font and circumvent the\n        stylesheets.\"\"\"\n        return QFont(self._style_font)\n\n    def fontMetrics(self) -&gt; QFontMetrics:\n        \"\"\"Override to return our custom style font metrics and circumvent the\n        stylesheets.\"\"\"\n        return QFontMetrics(self._style_font)\n\n    def palette(self) -&gt; QPalette:\n        \"\"\"Override to return our custom style palette and circumvent the\n        stylesheets.\"\"\"\n        return QPalette(self._style_palette)\n\n    def setFont(self, font: QFont | str | Sequence[str]) -&gt; None:\n        \"\"\"Override to prevent the stylesheet from overriding our style's\n        font.\"\"\"\n        if hasattr(super(), \"setFont\"):\n            super().setFont(self._style_font)  # type: ignore\n\n    def setPalette(self, palette: QPalette | Qt.GlobalColor | QColor) -&gt; None:\n        \"\"\"Override to prevent the stylesheet from overriding our style's\n        palette.\"\"\"\n        if hasattr(super(), \"setPalette\"):\n            super().setPalette(self._style_palette)  # type: ignore\n\n    def set_font(self, font: QFont) -&gt; None:\n        \"\"\"Set the custom style font.\n        This is a default implementation that will be superseded by widgets\n        that implement their own `set_font()` method.\"\"\"\n        if hasattr(super(), \"set_font\"):\n            super().set_font(font)  # type: ignore\n        else:\n            self._style_font = font\n        self.setFont(self._style_font)\n\n    def set_palette(self, palette: QPalette) -&gt; None:\n        \"\"\"Set the custom style palette.\n        This is a default implementation that will be superseded by widgets\n        that implement their own `set_palette()` method.\"\"\"\n        if hasattr(super(), \"set_palette\"):\n            super().set_palette(palette)  # type: ignore\n        else:\n            self._style_palette = palette\n        self.setPalette(self._style_palette)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/style_mixin.html#client.ayon_core.ui.components.style_mixin.StyleMixin.font","title":"<code>font()</code>","text":"<p>Override to return our custom style font and circumvent the stylesheets.</p> Source code in <code>client/ayon_core/ui/components/style_mixin.py</code> <pre><code>def font(self) -&gt; QFont:\n    \"\"\"Override to return our custom style font and circumvent the\n    stylesheets.\"\"\"\n    return QFont(self._style_font)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/style_mixin.html#client.ayon_core.ui.components.style_mixin.StyleMixin.fontMetrics","title":"<code>fontMetrics()</code>","text":"<p>Override to return our custom style font metrics and circumvent the stylesheets.</p> Source code in <code>client/ayon_core/ui/components/style_mixin.py</code> <pre><code>def fontMetrics(self) -&gt; QFontMetrics:\n    \"\"\"Override to return our custom style font metrics and circumvent the\n    stylesheets.\"\"\"\n    return QFontMetrics(self._style_font)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/style_mixin.html#client.ayon_core.ui.components.style_mixin.StyleMixin.palette","title":"<code>palette()</code>","text":"<p>Override to return our custom style palette and circumvent the stylesheets.</p> Source code in <code>client/ayon_core/ui/components/style_mixin.py</code> <pre><code>def palette(self) -&gt; QPalette:\n    \"\"\"Override to return our custom style palette and circumvent the\n    stylesheets.\"\"\"\n    return QPalette(self._style_palette)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/style_mixin.html#client.ayon_core.ui.components.style_mixin.StyleMixin.setFont","title":"<code>setFont(font)</code>","text":"<p>Override to prevent the stylesheet from overriding our style's font.</p> Source code in <code>client/ayon_core/ui/components/style_mixin.py</code> <pre><code>def setFont(self, font: QFont | str | Sequence[str]) -&gt; None:\n    \"\"\"Override to prevent the stylesheet from overriding our style's\n    font.\"\"\"\n    if hasattr(super(), \"setFont\"):\n        super().setFont(self._style_font)  # type: ignore\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/style_mixin.html#client.ayon_core.ui.components.style_mixin.StyleMixin.setPalette","title":"<code>setPalette(palette)</code>","text":"<p>Override to prevent the stylesheet from overriding our style's palette.</p> Source code in <code>client/ayon_core/ui/components/style_mixin.py</code> <pre><code>def setPalette(self, palette: QPalette | Qt.GlobalColor | QColor) -&gt; None:\n    \"\"\"Override to prevent the stylesheet from overriding our style's\n    palette.\"\"\"\n    if hasattr(super(), \"setPalette\"):\n        super().setPalette(self._style_palette)  # type: ignore\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/style_mixin.html#client.ayon_core.ui.components.style_mixin.StyleMixin.set_font","title":"<code>set_font(font)</code>","text":"<p>Set the custom style font. This is a default implementation that will be superseded by widgets that implement their own <code>set_font()</code> method.</p> Source code in <code>client/ayon_core/ui/components/style_mixin.py</code> <pre><code>def set_font(self, font: QFont) -&gt; None:\n    \"\"\"Set the custom style font.\n    This is a default implementation that will be superseded by widgets\n    that implement their own `set_font()` method.\"\"\"\n    if hasattr(super(), \"set_font\"):\n        super().set_font(font)  # type: ignore\n    else:\n        self._style_font = font\n    self.setFont(self._style_font)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/style_mixin.html#client.ayon_core.ui.components.style_mixin.StyleMixin.set_palette","title":"<code>set_palette(palette)</code>","text":"<p>Set the custom style palette. This is a default implementation that will be superseded by widgets that implement their own <code>set_palette()</code> method.</p> Source code in <code>client/ayon_core/ui/components/style_mixin.py</code> <pre><code>def set_palette(self, palette: QPalette) -&gt; None:\n    \"\"\"Set the custom style palette.\n    This is a default implementation that will be superseded by widgets\n    that implement their own `set_palette()` method.\"\"\"\n    if hasattr(super(), \"set_palette\"):\n        super().set_palette(palette)  # type: ignore\n    else:\n        self._style_palette = palette\n    self.setPalette(self._style_palette)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/table_filter.html","title":"table_filter","text":""},{"location":"autoapi/client/ayon_core/ui/components/table_filter.html#client.ayon_core.ui.components.table_filter.AYTableFilter","title":"<code>AYTableFilter</code>","text":"<p>               Bases: <code>AYContainer</code></p> <p>Horizontal filter bar bound to a PaginatedTableModel.</p> <p>Displays active criteria as badges and manages a floating two-page dropdown for adding/editing criteria.  Applies filtering via an internal <code>AYTableFilterProxyModel</code>.</p> <p>Typical usage::</p> <pre><code>filter_bar = AYTableFilter(model=source_model)\ntable.setModel(filter_bar.filter_model)\nlayout.addWidget(filter_bar)\nlayout.addWidget(table)\n</code></pre> Signals <p>filters_changed: Emitted after any criteria change.                  Passes the current list of FilterCriterion.</p> Source code in <code>client/ayon_core/ui/components/table_filter.py</code> <pre><code>class AYTableFilter(AYContainer):\n    \"\"\"Horizontal filter bar bound to a PaginatedTableModel.\n\n    Displays active criteria as badges and manages a floating two-page\n    dropdown for adding/editing criteria.  Applies filtering via an\n    internal ``AYTableFilterProxyModel``.\n\n    Typical usage::\n\n        filter_bar = AYTableFilter(model=source_model)\n        table.setModel(filter_bar.filter_model)\n        layout.addWidget(filter_bar)\n        layout.addWidget(table)\n\n    Signals:\n        filters_changed: Emitted after any criteria change.\n                         Passes the current list of FilterCriterion.\n    \"\"\"\n\n    filters_changed = Signal(list)  # list[FilterCriterion]\n\n    def __init__(\n        self,\n        model: PaginatedTableModel,\n        parent: QWidget | None = None,\n    ) -&gt; None:\n        super().__init__(\n            parent=parent,\n            layout=AYContainer.Layout.HBox,\n            variant=AYContainer.Variants.Low_Framed_Thin,\n            layout_margin=4,\n        )\n        self.setObjectName(\"AYTableFilter\")\n        self.setFixedHeight(32)\n\n        self._model = model\n        self._criteria: list[FilterCriterion] = []\n        self._editing_criterion: FilterCriterion | None = None\n\n        # Proxy model wraps the source model\n        self._proxy = AYTableFilterProxyModel(self)\n        self._proxy.setSourceModel(model)\n        # Re-filter whenever new rows are loaded\n        model.rowsInserted.connect(lambda *_: self._proxy.refresh_filter())\n\n        # Shared dropdown instance (reused across open calls)\n        self._dropdown = _FilterDropdown(model, self)\n        self._dropdown.criterion_ready.connect(self._on_criterion_ready)\n\n        # search button - always visible, opens empty dropdown for new\n        # criterion\n        self._search_btn = AYButton(\n            icon=\"search\",\n            variant=AYButton.Variants.Nav_Small,\n        )\n        self._search_btn.clicked.connect(\n            lambda: self._dropdown.open_for_new(self)\n        )\n        self.add_widget(self._search_btn)\n\n        # main container for criteria badges\n        self._criteria_container = AYHBoxLayout(spacing=4, margin=0)\n        self.add_layout(self._criteria_container, stretch=1)\n\n        self._rebuild_bar()\n\n    # ------------------------------------------------------------------\n    # Public API\n    # ------------------------------------------------------------------\n\n    @property\n    def filter_model(self) -&gt; AYTableFilterProxyModel:\n        \"\"\"Return the proxy model to set on the view.\"\"\"\n        return self._proxy\n\n    # ------------------------------------------------------------------\n    # Bar management\n    # ------------------------------------------------------------------\n\n    def _rebuild_bar(self) -&gt; None:\n        \"\"\"Clear and repopulate the bar with current criteria.\"\"\"\n        self._criteria_container.clear()\n\n        if not self._criteria:\n            empty_btn = AYButton(\n                \"Filter\",\n                variant=AYButton.Variants.Table_Filter,\n                label_alignment=Qt.AlignmentFlag.AlignLeft,\n                fixed_width=False,\n            )\n            empty_btn.clicked.connect(\n                lambda: self._dropdown.open_for_new(self)\n            )\n            self._criteria_container.addWidget(empty_btn)\n            return\n\n        for i, criterion in enumerate(self._criteria):\n            if i &gt; 0:\n                sep = AYLabel(\"and\", dim=True)\n                sep.setContentsMargins(2, 0, 2, 0)\n                self._criteria_container.addWidget(sep)\n\n            badge = _CriterionBadge(criterion)\n            badge.edit_requested.connect(self._on_badge_edit)\n            badge.remove_requested.connect(self._on_badge_remove)\n            self._criteria_container.addWidget(badge)\n\n        # \"+\" button to add another criterion\n        add_btn = AYButton(\n            icon=\"add\",\n            variant=AYButton.Variants.Nav_Small,\n        )\n        add_btn.setFixedSize(24, 24)\n        add_btn.clicked.connect(lambda: self._dropdown.open_for_new(self))\n        self._criteria_container.addWidget(add_btn)\n\n        self._criteria_container.addStretch()\n\n    # ------------------------------------------------------------------\n    # Criterion handlers\n    # ------------------------------------------------------------------\n\n    def _on_criterion_ready(\n        self, key: str, values: list[str], use_substring: bool, excludes: bool\n    ) -&gt; None:\n        if not values:\n            # Empty apply \u2014 remove existing criterion for this key if any\n            self._criteria = [c for c in self._criteria if c.key != key]\n        elif self._editing_criterion is not None:\n            # Update in-place\n            self._editing_criterion.values = values\n            self._editing_criterion.use_substring = use_substring\n        else:\n            # Check if criterion for this key already exists\n            existing = next((c for c in self._criteria if c.key == key), None)\n            if existing:\n                existing.values = values\n                existing.use_substring = use_substring\n            else:\n                col = next(\n                    (c for c in self._model.columns if c.key == key), None\n                )\n                label = col.label if col else key\n                self._criteria.append(\n                    FilterCriterion(\n                        key=key,\n                        attribute_label=label,\n                        values=values,\n                        use_substring=use_substring,\n                        exclude=excludes,\n                    )\n                )\n\n        self._editing_criterion = None\n        self._rebuild_bar()\n        self._update_proxy()\n\n    def _on_badge_edit(self, criterion: FilterCriterion) -&gt; None:\n        self._editing_criterion = criterion\n        self._dropdown.open_for_edit(criterion, self)\n\n    def _on_badge_remove(self, criterion: FilterCriterion) -&gt; None:\n        self._criteria = [c for c in self._criteria if c is not criterion]\n        self._rebuild_bar()\n        self._update_proxy()\n\n    def _update_proxy(self) -&gt; None:\n        self._proxy.set_criteria(self._criteria, self._model.columns)\n        self.filters_changed.emit(list(self._criteria))\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/table_filter.html#client.ayon_core.ui.components.table_filter.AYTableFilter.filter_model","title":"<code>filter_model</code>  <code>property</code>","text":"<p>Return the proxy model to set on the view.</p>"},{"location":"autoapi/client/ayon_core/ui/components/table_filter.html#client.ayon_core.ui.components.table_filter.AYTableFilterProxyModel","title":"<code>AYTableFilterProxyModel</code>","text":"<p>               Bases: <code>QSortFilterProxyModel</code></p> <p>Proxy model that filters rows using a list of FilterCriterion.</p> <p>All criteria are combined with AND; values within a single criterion are combined with OR.  When no criteria are set every row passes.</p> Source code in <code>client/ayon_core/ui/components/table_filter.py</code> <pre><code>class AYTableFilterProxyModel(QSortFilterProxyModel):\n    \"\"\"Proxy model that filters rows using a list of FilterCriterion.\n\n    All criteria are combined with AND; values within a single criterion\n    are combined with OR.  When no criteria are set every row passes.\n    \"\"\"\n\n    def __init__(self, parent: QWidget | None = None) -&gt; None:\n        super().__init__(parent)\n        self._criteria: list[FilterCriterion] = []\n        self._columns: list[TableColumn] = []\n        self._key_to_col: dict[str, int] = {}\n        self.setFilterCaseSensitivity(Qt.CaseSensitivity.CaseInsensitive)\n\n    def set_criteria(\n        self,\n        criteria: list[FilterCriterion],\n        columns: list[TableColumn],\n    ) -&gt; None:\n        \"\"\"Replace active criteria and refresh the filter.\n\n        Args:\n            criteria: List of active filter criteria.\n            columns: Column definitions from the source model.\n        \"\"\"\n        self._criteria = [c for c in criteria if c.values]\n        self._columns = columns\n        self._key_to_col = {col.key: idx for idx, col in enumerate(columns)}\n        self.invalidateFilter()\n\n    def _direct_match(\n        self,\n        source_row: int,\n        source_parent: QModelIndex | QPersistentModelIndex,\n    ) -&gt; bool:\n        \"\"\"Return True if a single row satisfies every active criterion.\n\n        Args:\n            source_row: Row number in the source model.\n            source_parent: Parent index in the source model.\n\n        Returns:\n            ``True`` when all criteria are satisfied (AND logic).\n        \"\"\"\n        if not self._criteria:\n            return True\n\n        source_model = self.sourceModel()\n        if source_model is None:\n            return True\n\n        for criterion in self._criteria:\n            if not criterion.values:\n                continue\n            col_idx = self._key_to_col.get(criterion.key)\n            if col_idx is None:\n                continue\n            index = source_model.index(source_row, col_idx, source_parent)\n            cell_value = source_model.data(index, Qt.ItemDataRole.DisplayRole)\n            cell_str = str(cell_value).lower() if cell_value else \"\"\n\n            matched = False\n            for val in criterion.values:\n                val_lower = val.lower()\n                if criterion.use_substring:\n                    if val_lower in cell_str:\n                        matched = True\n                        break\n                else:\n                    if cell_str == val_lower:\n                        matched = True\n                        break\n            if not matched:\n                return False\n\n        return True\n\n    def filterAcceptsRow(  # noqa: N802\n        self,\n        source_row: int,\n        source_parent: QModelIndex | QPersistentModelIndex,\n    ) -&gt; bool:\n        \"\"\"Return True if the row (or any of its loaded children) matches.\n\n        In flat mode the recursive child check is a no-op because leaf\n        nodes have ``rowCount() == 0``.  In tree mode parent folders are\n        kept visible as long as at least one loaded descendant passes the\n        criteria, preserving the tree structure.\n\n        Args:\n            source_row: Row number in the source model.\n            source_parent: Parent index in the source model.\n\n        Returns:\n            ``True`` when the row or a descendant satisfies all criteria.\n        \"\"\"\n        if not self._criteria:\n            return True\n\n        # Tentatively accept tree-mode folder nodes whose children haven't\n        # been fetched yet. We can't know whether any descendant will match\n        # until the children are loaded. PaginatedTableModel emits dataChanged\n        # for the node once its children are loaded, which causes this method\n        # to be called again with real data to decide.\n        source_model = self.sourceModel()\n        if (\n            isinstance(source_model, PaginatedTableModel)\n            and source_model._tree_mode\n        ):\n            src_idx = source_model.index(source_row, 0, source_parent)\n            node = src_idx.internalPointer()\n            if (\n                node is not None\n                and node.row_has_children\n                and not node.children_loaded\n            ):\n                return True\n\n        if self._direct_match(source_row, source_parent):\n            return True\n\n        # Tree hierarchy: keep parent visible if any loaded child matches.\n        if source_model is None:\n            return False\n        source_index = source_model.index(source_row, 0, source_parent)\n        for child_row in range(source_model.rowCount(source_index)):\n            if self.filterAcceptsRow(child_row, source_index):\n                return True\n\n        return False\n\n    def refresh_filter(self) -&gt; None:\n        \"\"\"Re-apply the current filter criteria (e.g. after source data\n        changes).\"\"\"\n        self.invalidateFilter()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/table_filter.html#client.ayon_core.ui.components.table_filter.AYTableFilterProxyModel.filterAcceptsRow","title":"<code>filterAcceptsRow(source_row, source_parent)</code>","text":"<p>Return True if the row (or any of its loaded children) matches.</p> <p>In flat mode the recursive child check is a no-op because leaf nodes have <code>rowCount() == 0</code>.  In tree mode parent folders are kept visible as long as at least one loaded descendant passes the criteria, preserving the tree structure.</p> <p>Parameters:</p> Name Type Description Default <code>source_row</code> <code>int</code> <p>Row number in the source model.</p> required <code>source_parent</code> <code>QModelIndex | QPersistentModelIndex</code> <p>Parent index in the source model.</p> required <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> when the row or a descendant satisfies all criteria.</p> Source code in <code>client/ayon_core/ui/components/table_filter.py</code> <pre><code>def filterAcceptsRow(  # noqa: N802\n    self,\n    source_row: int,\n    source_parent: QModelIndex | QPersistentModelIndex,\n) -&gt; bool:\n    \"\"\"Return True if the row (or any of its loaded children) matches.\n\n    In flat mode the recursive child check is a no-op because leaf\n    nodes have ``rowCount() == 0``.  In tree mode parent folders are\n    kept visible as long as at least one loaded descendant passes the\n    criteria, preserving the tree structure.\n\n    Args:\n        source_row: Row number in the source model.\n        source_parent: Parent index in the source model.\n\n    Returns:\n        ``True`` when the row or a descendant satisfies all criteria.\n    \"\"\"\n    if not self._criteria:\n        return True\n\n    # Tentatively accept tree-mode folder nodes whose children haven't\n    # been fetched yet. We can't know whether any descendant will match\n    # until the children are loaded. PaginatedTableModel emits dataChanged\n    # for the node once its children are loaded, which causes this method\n    # to be called again with real data to decide.\n    source_model = self.sourceModel()\n    if (\n        isinstance(source_model, PaginatedTableModel)\n        and source_model._tree_mode\n    ):\n        src_idx = source_model.index(source_row, 0, source_parent)\n        node = src_idx.internalPointer()\n        if (\n            node is not None\n            and node.row_has_children\n            and not node.children_loaded\n        ):\n            return True\n\n    if self._direct_match(source_row, source_parent):\n        return True\n\n    # Tree hierarchy: keep parent visible if any loaded child matches.\n    if source_model is None:\n        return False\n    source_index = source_model.index(source_row, 0, source_parent)\n    for child_row in range(source_model.rowCount(source_index)):\n        if self.filterAcceptsRow(child_row, source_index):\n            return True\n\n    return False\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/table_filter.html#client.ayon_core.ui.components.table_filter.AYTableFilterProxyModel.refresh_filter","title":"<code>refresh_filter()</code>","text":"<p>Re-apply the current filter criteria (e.g. after source data changes).</p> Source code in <code>client/ayon_core/ui/components/table_filter.py</code> <pre><code>def refresh_filter(self) -&gt; None:\n    \"\"\"Re-apply the current filter criteria (e.g. after source data\n    changes).\"\"\"\n    self.invalidateFilter()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/table_filter.html#client.ayon_core.ui.components.table_filter.AYTableFilterProxyModel.set_criteria","title":"<code>set_criteria(criteria, columns)</code>","text":"<p>Replace active criteria and refresh the filter.</p> <p>Parameters:</p> Name Type Description Default <code>criteria</code> <code>list[FilterCriterion]</code> <p>List of active filter criteria.</p> required <code>columns</code> <code>list[TableColumn]</code> <p>Column definitions from the source model.</p> required Source code in <code>client/ayon_core/ui/components/table_filter.py</code> <pre><code>def set_criteria(\n    self,\n    criteria: list[FilterCriterion],\n    columns: list[TableColumn],\n) -&gt; None:\n    \"\"\"Replace active criteria and refresh the filter.\n\n    Args:\n        criteria: List of active filter criteria.\n        columns: Column definitions from the source model.\n    \"\"\"\n    self._criteria = [c for c in criteria if c.values]\n    self._columns = columns\n    self._key_to_col = {col.key: idx for idx, col in enumerate(columns)}\n    self.invalidateFilter()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/table_filter.html#client.ayon_core.ui.components.table_filter.FilterCriterion","title":"<code>FilterCriterion</code>  <code>dataclass</code>","text":"<p>A single active filter criterion.</p> <p>Attributes:</p> Name Type Description <code>key</code> <code>str</code> <p>Column key this criterion applies to.</p> <code>attribute_label</code> <code>str</code> <p>Human-readable label for the column.</p> <code>values</code> <code>list[str]</code> <p>List of accepted values (OR logic within criterion).</p> <code>use_substring</code> <code>bool</code> <p>When True use case-insensitive substring matching; False uses exact match (used for enum columns).</p> Source code in <code>client/ayon_core/ui/components/table_filter.py</code> <pre><code>@dataclass\nclass FilterCriterion:\n    \"\"\"A single active filter criterion.\n\n    Attributes:\n        key: Column key this criterion applies to.\n        attribute_label: Human-readable label for the column.\n        values: List of accepted values (OR logic within criterion).\n        use_substring: When True use case-insensitive substring matching;\n            False uses exact match (used for enum columns).\n    \"\"\"\n\n    key: str\n    attribute_label: str\n    values: list[str] = field(default_factory=list)\n    use_substring: bool = False\n    exclude: bool = False\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/table_model.html","title":"table_model","text":"<p>Paginated Qt table model with lazy loading support.</p>"},{"location":"autoapi/client/ayon_core/ui/components/table_model.html#client.ayon_core.ui.components.table_model--tree-mode-batch-fetching","title":"Tree-mode batch fetching","text":"<p>When a subtree is expanded, Qt calls <code>fetchMore()</code> for every child node that declares <code>has_children=True</code>.  Those calls all arrive in the same event-loop tick.  Without batching each call produces a separate async task and therefore a separate server round-trip.</p> <p>Supply the optional fetch_page_batch callback to collapse all of those calls into a single round-trip::</p> <pre><code>def fetch_batch(\n    requests: list[BatchFetchRequest],\n) -&gt; dict[str | None, list[dict]]:\n    # One HTTP call for all parent_ids in the batch.\n    ...\n</code></pre> <p>The model accumulates pending fetch requests during an event-loop tick, dispatches them as one :class:<code>AsyncTask</code> via a zero-delay <code>QTimer.singleShot(0)</code>, and fans out the results to the per-node <code>_on_page_ready</code> handler when they arrive.</p>"},{"location":"autoapi/client/ayon_core/ui/components/table_model.html#client.ayon_core.ui.components.table_model.BatchFetchRequest","title":"<code>BatchFetchRequest</code>  <code>dataclass</code>","text":"<p>Describes a single child-page request within a batch fetch call.</p> <p>A list of these is passed to the optional fetch_page_batch callback of :class:<code>PaginatedTableModel</code>.  Each entry corresponds to one node whose children need to be fetched; the callback should return a dict mapping each <code>parent_id</code> to its list of row dicts.</p> <p>Attributes:</p> Name Type Description <code>page</code> <code>int</code> <p>Page number (0-based).</p> <code>page_size</code> <code>int</code> <p>Maximum number of rows to return.</p> <code>sort_key</code> <code>str | None</code> <p>Column key for server-side sorting, or <code>None</code>.</p> <code>descending</code> <code>bool</code> <p><code>True</code> for descending sort order.</p> <code>parent_id</code> <code>str | None</code> <p>The <code>\"id\"</code> value of the parent row, or <code>None</code> for the invisible root (never used in batch - root is always fetched individually).</p> Source code in <code>client/ayon_core/ui/components/table_model.py</code> <pre><code>@dataclass\nclass BatchFetchRequest:\n    \"\"\"Describes a single child-page request within a batch fetch call.\n\n    A list of these is passed to the optional *fetch_page_batch* callback\n    of :class:`PaginatedTableModel`.  Each entry corresponds to one node\n    whose children need to be fetched; the callback should return a dict\n    mapping each ``parent_id`` to its list of row dicts.\n\n    Attributes:\n        page: Page number (0-based).\n        page_size: Maximum number of rows to return.\n        sort_key: Column key for server-side sorting, or ``None``.\n        descending: ``True`` for descending sort order.\n        parent_id: The ``\"id\"`` value of the parent row, or ``None`` for\n            the invisible root (never used in batch - root is always\n            fetched individually).\n    \"\"\"\n\n    page: int\n    page_size: int\n    sort_key: str | None\n    descending: bool\n    parent_id: str | None\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/table_model.html#client.ayon_core.ui.components.table_model.PaginatedTableModel","title":"<code>PaginatedTableModel</code>","text":"<p>               Bases: <code>QAbstractItemModel</code></p> <p>A Qt model that lazily loads rows page-by-page via a callback.</p> <p>Rows are fetched on demand using Qt's canFetchMore / fetchMore mechanism.  Each call to fetchMore retrieves one page of data from the supplied <code>fetch_page</code> callable.</p> <p>In flat mode (default) the model behaves like a plain table: no disclosure triangles, no nesting.  In tree mode rows whose dict contains <code>\"has_children\": True</code> become expandable folders; expanding them triggers a fresh <code>fetch_page</code> call with the folder's <code>\"id\"</code> value passed as <code>parent_id</code>.</p> <p>Batch fetching (tree mode only): when fetch_page_batch is supplied, all <code>fetchMore()</code> calls that arrive in the same event-loop tick (e.g. Qt calling <code>fetchMore</code> for every child of a just-expanded folder) are coalesced into a single :class:<code>BatchFetchRequest</code> list and dispatched as one async task. This reduces N sibling fetches from N server round-trips to one.</p> <p>Parameters:</p> Name Type Description Default <code>fetch_page</code> <code>Callable[[int, int, str | None, bool, str | None], list[dict[str, Any]]]</code> <p>Callable with signature <code>(page, page_size, sort_key, descending, parent_id) -&gt; list[dict]</code>.  <code>parent_id</code> is <code>None</code> for root-level items and the row's <code>\"id\"</code> value for nested items.</p> required <code>fetch_page_batch</code> <code>Callable[[list[BatchFetchRequest]], dict[str | None, list[dict[str, Any]]]] | None</code> <p>Optional callable with signature <code>(requests: list[BatchFetchRequest]) -&gt; dict[parent_id, list[dict]]</code>.  When supplied, tree-mode child fetches are batched into a single call per event-loop tick instead of one call per node.  The root-level fetch always uses fetch_page and is never batched.</p> <code>None</code> <code>columns</code> <code>list[TableColumn] | None</code> <p>Column definitions.  When <code>None</code>, columns are inferred from the keys of the first fetched row.</p> <code>None</code> <code>page_size</code> <code>int</code> <p>Number of rows per page.</p> <code>50</code> <code>no_async</code> <code>bool</code> <p>When <code>True</code>, pages are fetched synchronously on the main thread instead of via the :class:<code>AsyncTaskQueue</code>. Useful in tests to avoid worker-thread/paint-event races.</p> <code>False</code> <code>parent</code> <code>QObject | None</code> <p>Optional parent QObject.</p> <code>None</code> Source code in <code>client/ayon_core/ui/components/table_model.py</code> <pre><code>class PaginatedTableModel(QAbstractItemModel):\n    \"\"\"A Qt model that lazily loads rows page-by-page via a callback.\n\n    Rows are fetched on demand using Qt's canFetchMore / fetchMore\n    mechanism.  Each call to fetchMore retrieves one page of data from\n    the supplied ``fetch_page`` callable.\n\n    In **flat mode** (default) the model behaves like a plain table:\n    no disclosure triangles, no nesting.  In **tree mode** rows whose\n    dict contains ``\"has_children\": True`` become expandable folders;\n    expanding them triggers a fresh ``fetch_page`` call with the\n    folder's ``\"id\"`` value passed as ``parent_id``.\n\n    **Batch fetching** (tree mode only): when *fetch_page_batch* is\n    supplied, all ``fetchMore()`` calls that arrive in the same\n    event-loop tick (e.g. Qt calling ``fetchMore`` for every child of a\n    just-expanded folder) are coalesced into a single\n    :class:`BatchFetchRequest` list and dispatched as one async task.\n    This reduces N sibling fetches from N server round-trips to one.\n\n    Args:\n        fetch_page: Callable with signature\n            ``(page, page_size, sort_key, descending, parent_id) -&gt;\n            list[dict]``.  ``parent_id`` is ``None`` for root-level\n            items and the row's ``\"id\"`` value for nested items.\n        fetch_page_batch: Optional callable with signature\n            ``(requests: list[BatchFetchRequest]) -&gt;\n            dict[parent_id, list[dict]]``.  When supplied, tree-mode\n            child fetches are batched into a single call per event-loop\n            tick instead of one call per node.  The root-level fetch\n            always uses *fetch_page* and is never batched.\n        columns: Column definitions.  When ``None``, columns are inferred\n            from the keys of the first fetched row.\n        page_size: Number of rows per page.\n        no_async: When ``True``, pages are fetched synchronously on the\n            main thread instead of via the :class:`AsyncTaskQueue`.\n            Useful in tests to avoid worker-thread/paint-event races.\n        parent: Optional parent QObject.\n    \"\"\"\n\n    tree_mode_changed = Signal(bool)\n    loading_changed = Signal(bool)  # True while any fetch is in-flight\n    page_fetched = Signal(int, int)  # (page_number, total_root_rows_loaded)\n    fetch_error = Signal(str)  # error message when a fetch fails\n    pending_count_changed = Signal(int)  # number of in-flight fetch tasks\n\n    def __init__(\n        self,\n        fetch_page: Callable[\n            [int, int, str | None, bool, str | None], list[dict[str, Any]]\n        ],\n        fetch_page_batch: (\n            Callable[\n                [list[BatchFetchRequest]],\n                dict[str | None, list[dict[str, Any]]],\n            ]\n            | None\n        ) = None,\n        columns: list[TableColumn] | None = None,\n        page_size: int = 50,\n        no_async: bool = False,\n        parent: QObject | None = None,\n    ) -&gt; None:\n        \"\"\"Initialise the model and fetch the first page.\n\n        Args:\n            fetch_page: Callable\n                ``(page, page_size, sort_key, descending, parent_id)\n                -&gt; list[dict]``.\n            fetch_page_batch: Optional batch callable\n                ``(requests: list[BatchFetchRequest]) -&gt;\n                dict[parent_id, list[dict]]``.  When provided, non-root\n                child fetches are coalesced per event-loop tick.\n            columns: Explicit column definitions, or ``None`` to infer.\n            page_size: Rows per page.\n            no_async: When ``True``, fetch pages synchronously on the\n                calling thread instead of via the AsyncTaskQueue worker.\n                Useful in tests to avoid worker-thread/paint-event races.\n            parent: Parent QObject.\n        \"\"\"\n        super().__init__(parent)\n        self._fetch_page = fetch_page\n        self._fetch_page_batch: (\n            Callable[\n                [list[BatchFetchRequest]],\n                dict[str | None, list[dict[str, Any]]],\n            ]\n            | None\n        ) = fetch_page_batch\n        self._explicit_columns: list[TableColumn] | None = columns\n        self._columns: list[TableColumn] = columns or []\n        self._page_size: int = page_size\n        self._sort_column: int = -1\n        self._sort_order: Qt.SortOrder = Qt.SortOrder.AscendingOrder\n        self._tree_mode: bool = False\n        self._tree_position: int = -1\n\n        # Internal node tree.  _all_nodes prevents Python GC from collecting\n        # nodes that are held only via QModelIndex.internalPointer().\n        self._root: _TableNode = _TableNode(\n            node_id=None, row_data={}, parent=None\n        )\n        self._all_nodes: set[_TableNode] = {self._root}\n\n        # Async fetch state\n        self._no_async: bool = no_async\n        self._request_id: str = self._generate_request_id()\n        self._pending_tasks: int = 0\n\n        # Batch-fetch coalescing state (populated by _fetch_next_page when\n        # _fetch_page_batch is set, consumed by _dispatch_batch).\n        self._pending_batch_nodes: list[_TableNode] = []\n        self._batch_scheduled: bool = False\n\n        self._fetch_next_page(self._root)\n\n    # Properties --------------------------------------------------------------\n\n    @property\n    def columns(self) -&gt; list[TableColumn]:\n        \"\"\"Return the current column definitions.\n\n        Returns:\n            List of TableColumn instances.\n        \"\"\"\n        return list(self._columns)\n\n    @property\n    def page_count(self) -&gt; int:\n        \"\"\"Return the number of root pages fetched so far.\n\n        Returns:\n            Current root page index.\n        \"\"\"\n        return self._root.current_page\n\n    @property\n    def is_loading(self) -&gt; bool:\n        \"\"\"Return True while at least one fetch task is in-flight.\n\n        Returns:\n            True if any page fetch is currently pending or running.\n        \"\"\"\n        return self._pending_tasks &gt; 0\n\n    @property\n    def tree_position(self) -&gt; int:\n        \"\"\"Return the current tree column index, or 0 if tree mode is off.\"\"\"\n        if self._tree_position == -1:\n            self._tree_position = 0\n            for i, col in enumerate(self._columns):\n                if col.tree_position:\n                    self._tree_position = i\n                    break\n        return self._tree_position if self._tree_mode else 0\n\n    # QAbstractItemModel interface --------------------------------------------\n\n    def index(  # noqa: N802\n        self,\n        row: int,\n        column: int,\n        parent: QModelIndex | QPersistentModelIndex = QModelIndex(),\n    ) -&gt; QModelIndex:\n        \"\"\"Return a model index for row/column under parent.\n\n        Args:\n            row: Row number under parent.\n            column: Column number.\n            parent: Parent index (invalid = root).\n\n        Returns:\n            Valid QModelIndex, or invalid if out of range.\n        \"\"\"\n        if not self.hasIndex(row, column, parent):\n            return QModelIndex()\n        parent_node = self._node_from_index(parent)\n        if row &gt;= len(parent_node.children):\n            return QModelIndex()\n        return self.createIndex(row, column, parent_node.children[row])\n\n    def parent(  # type: ignore[override]\n        self,\n        index: QModelIndex | QPersistentModelIndex,\n    ) -&gt; QModelIndex:\n        \"\"\"Return the parent index of the given index.\n\n        Args:\n            index: Child index.\n\n        Returns:\n            Parent QModelIndex, or invalid for root-level items.\n        \"\"\"\n        if not index.isValid():\n            return QModelIndex()\n        node: _TableNode = index.internalPointer()  # type: ignore[assignment]\n        parent_node = node.parent\n        if parent_node is None or parent_node.is_root:\n            return QModelIndex()\n        grandparent = parent_node.parent\n        row = grandparent.children.index(parent_node)  # type: ignore[union-attr]\n        return self.createIndex(row, 0, parent_node)\n\n    def rowCount(  # noqa: N802\n        self,\n        parent: QModelIndex | QPersistentModelIndex = QModelIndex(),\n    ) -&gt; int:\n        \"\"\"Return the number of loaded children under parent.\n\n        Args:\n            parent: Parent index (invalid = root).\n\n        Returns:\n            Number of loaded child rows.\n        \"\"\"\n        if parent.column() &gt; 0:\n            return 0\n        return len(self._node_from_index(parent).children)\n\n    def columnCount(  # noqa: N802\n        self,\n        parent: QModelIndex | QPersistentModelIndex = QModelIndex(),\n    ) -&gt; int:\n        \"\"\"Return the number of columns.\n\n        Args:\n            parent: Unused; column count is the same for all nodes.\n\n        Returns:\n            Number of columns.\n        \"\"\"\n        return len(self._columns)\n\n    def hasChildren(  # noqa: N802\n        self,\n        parent: QModelIndex | QPersistentModelIndex = QModelIndex(),\n    ) -&gt; bool:\n        \"\"\"Return whether the node at parent has or can have children.\n\n        Controls whether Qt draws a disclosure triangle even before\n        children are loaded.\n\n        Args:\n            parent: Parent index (invalid = root).\n\n        Returns:\n            True if the node has loaded children, has more pages, or\n            (in tree mode only) the row data declares ``has_children``.\n        \"\"\"\n        node = self._node_from_index(parent)\n        if node.is_root:\n            return bool(node.children) or node.has_more\n        if not self._tree_mode:\n            return False\n        if node.children_loaded:\n            return bool(node.children)\n        return node.row_has_children\n\n    def canFetchMore(  # noqa: N802\n        self,\n        parent: QModelIndex | QPersistentModelIndex = QModelIndex(),\n    ) -&gt; bool:\n        \"\"\"Return whether more rows can be fetched for parent.\n\n        Args:\n            parent: Parent index (invalid = root).\n\n        Returns:\n            ``True`` when more pages are available for this node.\n        \"\"\"\n        node = self._node_from_index(parent)\n        if node.is_root:\n            return node.has_more\n        if not self._tree_mode:\n            return False\n        if not node.row_has_children:\n            return False\n        if not node.children_loaded:\n            return True\n        return node.has_more\n\n    def fetchMore(  # noqa: N802\n        self,\n        parent: QModelIndex | QPersistentModelIndex = QModelIndex(),\n    ) -&gt; None:\n        \"\"\"Fetch the next page of rows for parent.\n\n        Args:\n            parent: Parent index (invalid = root).\n        \"\"\"\n        self._fetch_next_page(self._node_from_index(parent))\n\n    def data(\n        self,\n        index: QModelIndex | QPersistentModelIndex,\n        role: int = Qt.ItemDataRole.DisplayRole,\n    ) -&gt; Any:\n        \"\"\"Return data for the given index and role.\n\n        Args:\n            index: Model index identifying the cell.\n            role: Qt item data role.\n\n        Returns:\n            Cell value appropriate for the requested role, or ``None``.\n        \"\"\"\n        if not index.isValid():\n            return None\n        node: _TableNode | None = index.internalPointer()  # type: ignore[assignment]\n        if node is None:\n            return None\n        col = index.column()\n        if col &lt; 0 or col &gt;= len(self._columns):\n            return None\n\n        row_dict = node.row_data\n        col_key = self._columns[col].key\n\n        if role == Qt.ItemDataRole.DisplayRole:\n            value = row_dict.get(col_key)\n            if value is None:\n                return \"\"\n            return str(value)\n\n        if role == Qt.ItemDataRole.DecorationRole:\n            icon_key = f\"{col_key}__icon\"\n            icon_name = row_dict.get(icon_key)\n            if icon_name:\n                icon_color = row_dict.get(f\"{col_key}__color\", \"#ffffff\")\n                icon_fill = row_dict.get(f\"{col_key}__icon_fill\", False)\n                return get_icon(icon_name, color=icon_color, fill=icon_fill)\n            return None\n\n        if role == Qt.ItemDataRole.ForegroundRole:\n            color_key = f\"{col_key}__color\"\n            color = row_dict.get(color_key)\n            if isinstance(color, str):\n                return QBrush(QColor(color))\n            return None\n\n        if role == Qt.ItemDataRole.UserRole:\n            return row_dict\n\n        return None\n\n    def headerData(  # noqa: N802\n        self,\n        section: int,\n        orientation: Qt.Orientation,\n        role: int = Qt.ItemDataRole.DisplayRole,\n    ) -&gt; Any:\n        \"\"\"Return header data for the given section and orientation.\n\n        Args:\n            section: Column (horizontal) or row (vertical) index.\n            orientation: Header orientation.\n            role: Qt item data role.\n\n        Returns:\n            Column label for horizontal DisplayRole, otherwise ``None``.\n        \"\"\"\n        if (\n            orientation == Qt.Orientation.Horizontal\n            and role == Qt.ItemDataRole.DisplayRole\n        ):\n            if 0 &lt;= section &lt; len(self._columns):\n                return self._columns[section].label\n        return None\n\n    def sort(\n        self, column: int, order: Qt.SortOrder = Qt.SortOrder.AscendingOrder\n    ) -&gt; None:\n        \"\"\"Set the active sort column and order, then reload data.\n\n        Sorting always resets from page 0 at all levels.\n\n        Args:\n            column: Zero-based index of the column to sort by. If out of\n                range, the call is ignored.\n            order: Sort order (ascending or descending).\n        \"\"\"\n        if column &lt; 0 or column &gt;= len(self._columns):\n            return\n        self._sort_column = column\n        self._sort_order = order\n        self.reset_data()  # refetch page 0 using new sort\n\n    # Public interface --------------------------------------------------------\n\n    def set_tree_mode(self, enabled: bool) -&gt; None:\n        \"\"\"Switch between flat table mode and hierarchical tree mode.\n\n        Emits ``tree_mode_changed`` and reloads from page 0.\n\n        Args:\n            enabled: ``True`` to enable tree mode, ``False`` for flat.\n        \"\"\"\n        if self._tree_mode == enabled:\n            return\n        self._tree_mode = enabled\n        self.tree_mode_changed.emit(enabled)\n        self.reset_data()\n\n    def set_page(self, page: int) -&gt; None:\n        \"\"\"Reset the model and begin fetching from the given page.\n\n        Args:\n            page: 0-based page number to start from.\n        \"\"\"\n        old_request_id = self._request_id\n        if not self._no_async:\n            get_task_queue().clear_context_tasks(old_request_id)\n        self._request_id = self._generate_request_id()\n        self._pending_tasks = 0\n        self._pending_batch_nodes = []\n        self._batch_scheduled = False\n        self.beginResetModel()\n        self._root = _TableNode(node_id=None, row_data={}, parent=None)\n        self._root.current_page = page\n        self._all_nodes = {self._root}\n        self._columns = self._explicit_columns or []\n        self.endResetModel()\n        # Emit loading signals *after* endResetModel so that any slot\n        # connected to loading_changed observes a consistent model state.\n        self._update_loading_state()\n        self._fetch_next_page(self._root)\n\n    def set_page_size(self, size: int) -&gt; None:\n        \"\"\"Update the page size and reset the model from page 0.\n\n        Args:\n            size: New page size (rows per page).\n        \"\"\"\n        self._page_size = size\n        self.reset_data()\n\n    def set_columns(self, columns: list[TableColumn]) -&gt; None:\n        \"\"\"Set the columns and reset the model from page 0.\n\n        Args:\n            columns: List of columns to display.\n        \"\"\"\n        self._explicit_columns = columns\n        self.reset_data()\n\n    def reset_data(self) -&gt; None:\n        \"\"\"Reset the model and re-fetch from page 0.\"\"\"\n        old_request_id = self._request_id\n        if not self._no_async:\n            get_task_queue().clear_context_tasks(old_request_id)\n        self._request_id = self._generate_request_id()\n        self._pending_tasks = 0\n        self._pending_batch_nodes = []\n        self._batch_scheduled = False\n        self.beginResetModel()\n        self._root = _TableNode(node_id=None, row_data={}, parent=None)\n        self._all_nodes = {self._root}\n        self._columns = self._explicit_columns or []\n        self.endResetModel()\n        # Emit loading signals *after* endResetModel so that any slot\n        # connected to loading_changed observes a consistent model state.\n        self._update_loading_state()\n        self._fetch_next_page(self._root)\n\n    def get_distinct_values(self, key: str) -&gt; list[str]:\n        \"\"\"Return sorted distinct non-empty string values for a column.\n\n        In flat mode scans root-level rows; in tree mode scans all\n        loaded nodes across all levels.\n\n        Args:\n            key: Column key to inspect.\n\n        Returns:\n            Sorted list of unique string values found in loaded rows.\n        \"\"\"\n        seen: set[str] = set()\n        nodes = (\n            self._all_nodes if self._tree_mode else set(self._root.children)\n        )\n        for node in nodes:\n            if node.is_root:\n                continue\n            val = node.row_data.get(key)\n            if val is not None:\n                s = str(val).strip()\n                if s:\n                    seen.add(s)\n        return sorted(seen)\n\n    # Internal helpers --------------------------------------------------------\n\n    def _node_from_index(\n        self, index: QModelIndex | QPersistentModelIndex\n    ) -&gt; _TableNode:\n        \"\"\"Return the internal node for a model index.\n\n        Args:\n            index: A valid or invalid QModelIndex.\n\n        Returns:\n            The corresponding _TableNode; returns root for invalid\n            indexes.\n        \"\"\"\n        if not index.isValid():\n            return self._root\n        return index.internalPointer()  # type: ignore[return-value]\n\n    @staticmethod\n    def _generate_request_id() -&gt; str:\n        \"\"\"Return a unique request identifier for task-queue scoping.\"\"\"\n        return str(uuid.uuid1())\n\n    def _index_for_node(self, node: _TableNode) -&gt; QModelIndex:\n        \"\"\"Return the QModelIndex that identifies node (column 0).\n\n        Args:\n            node: A non-root table node.\n\n        Returns:\n            Valid QModelIndex for non-root nodes, invalid for root.\n        \"\"\"\n        if node.is_root:\n            return QModelIndex()\n        parent = node.parent\n        row = parent.children.index(node)  # type: ignore[union-attr]\n        return self.createIndex(row, 0, node)\n\n    def _update_loading_state(self) -&gt; None:\n        \"\"\"Emit loading-progress signals based on current pending task\n        count.\"\"\"\n        self.pending_count_changed.emit(self._pending_tasks)\n        self.loading_changed.emit(self._pending_tasks &gt; 0)\n\n    def _on_page_ready(\n        self,\n        node: _TableNode,\n        request_id: str,\n        results: list[dict[str, Any]] | None,\n    ) -&gt; None:\n        \"\"\"Handle fetch results on the main thread.\n\n        Called via Qt.QueuedConnection from AsyncTaskQueue so it always\n        executes on the Qt main thread, making it safe to call Qt model\n        mutation methods.\n\n        Args:\n            node: The node whose page was fetched.\n            request_id: The request id that was active when the task was\n                enqueued.  Used to discard stale results.\n            results: Row dicts returned by _fetch_page, or None on error.\n        \"\"\"\n        # Stale callback: release the guard and discard.\n        # Do NOT decrement _pending_tasks for stale callbacks \u2014 the\n        # reset_data() call already set the counter to 0 for the new\n        # context.  Decrementing here would corrupt the new context's count.\n        if request_id != self._request_id or node not in self._all_nodes:\n            # Always release the fetching guard so the node is never stuck\n            # in-flight, even for stale callbacks.\n            node.is_fetching = False\n            log.debug(\n                \"Discarding stale fetch result for node %r (request %s vs %s)\",\n                node.node_id,\n                request_id,\n                self._request_id,\n            )\n            return\n\n        # Confirmed current-context callback: update the in-flight counter.\n        # Keep is_fetching=True until ALL model mutations below are finished.\n        # endInsertRows() emits rowsInserted synchronously, which can cause\n        # Qt's tree-view slot to call canFetchMore/fetchMore re-entrantly.\n        # The guard must still be set at that point so _fetch_next_page\n        # returns immediately and avoids a recursive re-fetch of the same page.\n        self._pending_tasks = max(0, self._pending_tasks - 1)\n\n        _was_children_loaded = node.children_loaded\n\n        if results is None:\n            # Task failed \u2014 fetch_error already logged by AsyncTaskQueue.\n            self.fetch_error.emit(\n                f\"Failed to fetch page {node.current_page} \"\n                f\"(parent_id={node.node_id!r})\"\n            )\n            node.has_more = False\n            node.children_loaded = True\n            node.is_fetching = False\n            # Emit loading state after updating node state.\n            self._update_loading_state()\n            return\n\n        if not results:\n            node.has_more = False\n            node.children_loaded = True\n            node.is_fetching = False\n            self._update_loading_state()\n            return\n\n        if not self._columns and self._explicit_columns is None:\n            self._columns = self._infer_columns(results[0])\n\n        parent_index = self._index_for_node(node)\n        first_new = len(node.children)\n        last_new = first_new + len(results) - 1\n        self.beginInsertRows(parent_index, first_new, last_new)\n        for row_data in results:\n            child = _TableNode(\n                node_id=row_data.get(\"id\"),\n                row_data=row_data,\n                parent=node,\n            )\n            node.children.append(child)\n            self._all_nodes.add(child)\n        self.endInsertRows()\n\n        node.children_loaded = True\n        if len(results) &lt; self._page_size:\n            node.has_more = False\n\n        fetched_page = node.current_page\n        node.current_page += 1\n\n        # Release the re-entrancy guard now that all model mutations are done.\n        # Any canFetchMore/fetchMore calls that arrived re-entrantly during\n        # endInsertRows were blocked by is_fetching=True; they can now proceed\n        # on the next event-loop iteration if the node still has more pages.\n        node.is_fetching = False\n\n        # Emit loading-state signals AFTER rows are in the model so that\n        # any slot reacting to is_loading=False already sees the new rows.\n        self._update_loading_state()\n        self.page_fetched.emit(fetched_page, len(self._root.children))\n\n        # On the first child-load for a non-root node, emit dataChanged so\n        # proxy models (e.g. AYTableFilterProxyModel) re-evaluate this row's\n        # filter status now that real children are available.\n        if (\n            not node.is_root\n            and not _was_children_loaded\n            and node.children_loaded\n        ):\n            idx = self._index_for_node(node)\n            self.dataChanged.emit(idx, idx)\n\n    def _get_fetch_priority(self, node: _TableNode, page: int) -&gt; int:\n        \"\"\"Return the async-task priority for a child-page fetch.\n\n        The default implementation uses priority **1** (High) for the\n        very first page of any node and **2** (Normal) for subsequent\n        pages.  Subclasses may override this method to implement dynamic\n        priority logic \u2014 for example, lowering the priority to **20**\n        (Background prefetch) when the parent node's row is not currently\n        visible in the table's viewport.\n\n        Priority scale (lower value = higher priority):\n\n        - ``0`` - Critical / visible first-page fetches\n        - ``1`` - High / visible subsequent-page fetches\n        - ``2`` - Normal / thumbnail fetches\n        - ``20`` - Low / off-screen prefetch\n\n        Args:\n            node: The parent node whose children are being fetched.\n            page: Zero-based index of the page being requested.\n\n        Returns:\n            Integer task priority passed to\n            :class:`~ayon_core.ui.components.task_queue.AsyncTask`.\n        \"\"\"\n        return 1 if page == 0 else 2\n\n    def _fetch_next_page(self, node: _TableNode) -&gt; None:\n        \"\"\"Enqueue an async task to fetch the next page of children for *node*.\n\n        Uses a per-node re-entrancy guard (``node.is_fetching``) to\n        prevent duplicate tasks.  The actual data-fetch runs in the\n        shared :class:`AsyncTaskQueue` worker thread; results are\n        delivered back on the main thread via :meth:`_on_page_ready`.\n\n        When *fetch_page_batch* is configured and *node* is not the\n        invisible root, the node is added to ``_pending_batch_nodes`` and\n        a zero-delay timer schedules :meth:`_dispatch_batch`.  This\n        coalesces all sibling fetches that arrive in the same event-loop\n        tick into a single server call.\n\n        Priority is determined by :meth:`_get_fetch_priority`, which\n        defaults to 1 (High) for the first page and 2 (Normal) for\n        subsequent pages.  Subclasses may override\n        :meth:`_get_fetch_priority` to adjust priority dynamically.\n\n        Args:\n            node: The parent node whose next page of children to fetch.\n        \"\"\"\n        if node.is_fetching:\n            return\n        node.is_fetching = True\n\n        sort_key = None\n        if 0 &lt;= self._sort_column &lt; len(self._columns):\n            sort_key = self._columns[self._sort_column].key\n        descending = self._sort_order == Qt.SortOrder.DescendingOrder\n\n        page = node.current_page\n        page_size = self._page_size\n        node_id = node.node_id\n        request_id = self._request_id\n\n        priority = self._get_fetch_priority(node, page)\n\n        self._pending_tasks += 1\n        self._update_loading_state()\n\n        # Batch path: coalesce non-root child fetches when a batch\n        # callback is available.  The root is always fetched individually\n        # because it is the first call and nothing can be batched with it.\n        if self._fetch_page_batch is not None and not node.is_root:\n            self._pending_batch_nodes.append(node)\n            if not self._batch_scheduled:\n                self._batch_scheduled = True\n                QTimer.singleShot(0, self._dispatch_batch)\n            return\n\n        if self._no_async:\n            # Synchronous path: fetch inline and deliver result immediately.\n            # Used in tests to avoid cross-thread paint-event races.\n            try:\n                result = self._fetch_page(\n                    page, page_size, sort_key, descending, node_id\n                )\n            except Exception:\n                result = None\n            self._on_page_ready(node, request_id, result)\n            return\n\n        task = AsyncTask(\n            name=f\"fetch_page_{node_id or 'root'}_{page}\",\n            function=lambda: self._fetch_page(\n                page, page_size, sort_key, descending, node_id\n            ),\n            callback=lambda result: self._on_page_ready(\n                node, request_id, result\n            ),\n            priority=priority,\n            context_id=request_id,\n            cancellable=True,\n        )\n        get_task_queue().enqueue(task)\n\n    def _dispatch_batch(self) -&gt; None:\n        \"\"\"Dispatch all accumulated pending batch-fetch requests.\n\n        Called via ``QTimer.singleShot(0)`` so it runs on the main thread\n        at the start of the next event-loop iteration, after all\n        ``fetchMore()`` calls for the current expansion wave have been\n        collected into ``_pending_batch_nodes``.\n\n        The method creates one :class:`AsyncTask` (or runs synchronously\n        when ``no_async=True``) carrying all pending requests.  Results\n        are fanned out to the per-node ``_on_page_ready`` handler via\n        :meth:`_on_batch_ready`.\n        \"\"\"\n        self._batch_scheduled = False\n        nodes = list(self._pending_batch_nodes)\n        self._pending_batch_nodes = []\n\n        if not nodes:\n            return\n\n        sort_key = None\n        if 0 &lt;= self._sort_column &lt; len(self._columns):\n            sort_key = self._columns[self._sort_column].key\n        descending = self._sort_order == Qt.SortOrder.DescendingOrder\n        request_id = self._request_id\n\n        requests = [\n            BatchFetchRequest(\n                page=node.current_page,\n                page_size=self._page_size,\n                sort_key=sort_key,\n                descending=descending,\n                parent_id=node.node_id,\n            )\n            for node in nodes\n        ]\n\n        log.debug(\n            \"Dispatching batch of %d fetch requests (request=%s)\",\n            len(requests),\n            request_id,\n        )\n\n        assert self._fetch_page_batch is not None  # guarded by caller\n        batch_fn = self._fetch_page_batch\n\n        # Use the highest-urgency (lowest numeric) priority among the\n        # batched nodes so that a batch containing at least one visible\n        # node runs at visible priority rather than the default priority=1.\n        batch_priority = min(\n            self._get_fetch_priority(n, n.current_page) for n in nodes\n        )\n\n        if self._no_async:\n            try:\n                result: dict[str | None, list[dict[str, Any]]] | None = (\n                    batch_fn(requests)\n                )\n            except Exception:\n                log.exception(\"Batch fetch raised\")\n                result = None\n            self._on_batch_ready(nodes, request_id, result)\n            return\n\n        task = AsyncTask(\n            name=f\"fetch_batch_{len(nodes)}_nodes\",\n            function=lambda: batch_fn(requests),\n            callback=lambda res: self._on_batch_ready(nodes, request_id, res),\n            priority=batch_priority,\n            context_id=request_id,\n            cancellable=True,\n        )\n        get_task_queue().enqueue(task)\n\n    def _on_batch_ready(\n        self,\n        nodes: list[_TableNode],\n        request_id: str,\n        results: dict[str | None, list[dict[str, Any]]] | None,\n    ) -&gt; None:\n        \"\"\"Fan out batch results to per-node handlers.\n\n        Delegates each node's slice of the results to :meth:`_on_page_ready`\n        so that all existing insertion, loading-state, and dataChanged\n        logic is reused without duplication.\n\n        Args:\n            nodes: The nodes whose children were requested in the batch.\n            request_id: The request id active when the batch was dispatched.\n                Used to discard stale results.\n            results: Mapping of ``parent_id -&gt; list[row_dict]`` returned by\n                the batch callback, or ``None`` on error.\n        \"\"\"\n        for node in nodes:\n            node_results: list[dict[str, Any]] | None\n            if results is None:\n                node_results = None\n            else:\n                node_results = results.get(node.node_id, [])\n            self._on_page_ready(node, request_id, node_results)\n\n    @staticmethod\n    def _infer_columns(row: dict[str, Any]) -&gt; list[TableColumn]:\n        \"\"\"Infer column definitions from a sample row dictionary.\n\n        Reserved keys (``id``, ``has_children``) and decorator suffixes\n        (``__icon``, ``__color``, ``__fill``) are excluded.\n\n        Args:\n            row: A representative row dictionary.\n\n        Returns:\n            List of inferred TableColumn instances.\n        \"\"\"\n        columns: list[TableColumn] = []\n        for key in row:\n            if key in (\"id\", \"has_children\"):\n                continue\n            if key.endswith((\"__icon\", \"__color\", \"__fill\")):\n                continue\n            label = key.replace(\"_\", \" \").title()\n            columns.append(TableColumn(key=key, label=label))\n        return columns\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/table_model.html#client.ayon_core.ui.components.table_model.PaginatedTableModel.columns","title":"<code>columns</code>  <code>property</code>","text":"<p>Return the current column definitions.</p> <p>Returns:</p> Type Description <code>list[TableColumn]</code> <p>List of TableColumn instances.</p>"},{"location":"autoapi/client/ayon_core/ui/components/table_model.html#client.ayon_core.ui.components.table_model.PaginatedTableModel.is_loading","title":"<code>is_loading</code>  <code>property</code>","text":"<p>Return True while at least one fetch task is in-flight.</p> <p>Returns:</p> Type Description <code>bool</code> <p>True if any page fetch is currently pending or running.</p>"},{"location":"autoapi/client/ayon_core/ui/components/table_model.html#client.ayon_core.ui.components.table_model.PaginatedTableModel.page_count","title":"<code>page_count</code>  <code>property</code>","text":"<p>Return the number of root pages fetched so far.</p> <p>Returns:</p> Type Description <code>int</code> <p>Current root page index.</p>"},{"location":"autoapi/client/ayon_core/ui/components/table_model.html#client.ayon_core.ui.components.table_model.PaginatedTableModel.tree_position","title":"<code>tree_position</code>  <code>property</code>","text":"<p>Return the current tree column index, or 0 if tree mode is off.</p>"},{"location":"autoapi/client/ayon_core/ui/components/table_model.html#client.ayon_core.ui.components.table_model.PaginatedTableModel.__init__","title":"<code>__init__(fetch_page, fetch_page_batch=None, columns=None, page_size=50, no_async=False, parent=None)</code>","text":"<p>Initialise the model and fetch the first page.</p> <p>Parameters:</p> Name Type Description Default <code>fetch_page</code> <code>Callable[[int, int, str | None, bool, str | None], list[dict[str, Any]]]</code> <p>Callable <code>(page, page_size, sort_key, descending, parent_id) -&gt; list[dict]</code>.</p> required <code>fetch_page_batch</code> <code>Callable[[list[BatchFetchRequest]], dict[str | None, list[dict[str, Any]]]] | None</code> <p>Optional batch callable <code>(requests: list[BatchFetchRequest]) -&gt; dict[parent_id, list[dict]]</code>.  When provided, non-root child fetches are coalesced per event-loop tick.</p> <code>None</code> <code>columns</code> <code>list[TableColumn] | None</code> <p>Explicit column definitions, or <code>None</code> to infer.</p> <code>None</code> <code>page_size</code> <code>int</code> <p>Rows per page.</p> <code>50</code> <code>no_async</code> <code>bool</code> <p>When <code>True</code>, fetch pages synchronously on the calling thread instead of via the AsyncTaskQueue worker. Useful in tests to avoid worker-thread/paint-event races.</p> <code>False</code> <code>parent</code> <code>QObject | None</code> <p>Parent QObject.</p> <code>None</code> Source code in <code>client/ayon_core/ui/components/table_model.py</code> <pre><code>def __init__(\n    self,\n    fetch_page: Callable[\n        [int, int, str | None, bool, str | None], list[dict[str, Any]]\n    ],\n    fetch_page_batch: (\n        Callable[\n            [list[BatchFetchRequest]],\n            dict[str | None, list[dict[str, Any]]],\n        ]\n        | None\n    ) = None,\n    columns: list[TableColumn] | None = None,\n    page_size: int = 50,\n    no_async: bool = False,\n    parent: QObject | None = None,\n) -&gt; None:\n    \"\"\"Initialise the model and fetch the first page.\n\n    Args:\n        fetch_page: Callable\n            ``(page, page_size, sort_key, descending, parent_id)\n            -&gt; list[dict]``.\n        fetch_page_batch: Optional batch callable\n            ``(requests: list[BatchFetchRequest]) -&gt;\n            dict[parent_id, list[dict]]``.  When provided, non-root\n            child fetches are coalesced per event-loop tick.\n        columns: Explicit column definitions, or ``None`` to infer.\n        page_size: Rows per page.\n        no_async: When ``True``, fetch pages synchronously on the\n            calling thread instead of via the AsyncTaskQueue worker.\n            Useful in tests to avoid worker-thread/paint-event races.\n        parent: Parent QObject.\n    \"\"\"\n    super().__init__(parent)\n    self._fetch_page = fetch_page\n    self._fetch_page_batch: (\n        Callable[\n            [list[BatchFetchRequest]],\n            dict[str | None, list[dict[str, Any]]],\n        ]\n        | None\n    ) = fetch_page_batch\n    self._explicit_columns: list[TableColumn] | None = columns\n    self._columns: list[TableColumn] = columns or []\n    self._page_size: int = page_size\n    self._sort_column: int = -1\n    self._sort_order: Qt.SortOrder = Qt.SortOrder.AscendingOrder\n    self._tree_mode: bool = False\n    self._tree_position: int = -1\n\n    # Internal node tree.  _all_nodes prevents Python GC from collecting\n    # nodes that are held only via QModelIndex.internalPointer().\n    self._root: _TableNode = _TableNode(\n        node_id=None, row_data={}, parent=None\n    )\n    self._all_nodes: set[_TableNode] = {self._root}\n\n    # Async fetch state\n    self._no_async: bool = no_async\n    self._request_id: str = self._generate_request_id()\n    self._pending_tasks: int = 0\n\n    # Batch-fetch coalescing state (populated by _fetch_next_page when\n    # _fetch_page_batch is set, consumed by _dispatch_batch).\n    self._pending_batch_nodes: list[_TableNode] = []\n    self._batch_scheduled: bool = False\n\n    self._fetch_next_page(self._root)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/table_model.html#client.ayon_core.ui.components.table_model.PaginatedTableModel.canFetchMore","title":"<code>canFetchMore(parent=QModelIndex())</code>","text":"<p>Return whether more rows can be fetched for parent.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <code>QModelIndex | QPersistentModelIndex</code> <p>Parent index (invalid = root).</p> <code>QModelIndex()</code> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> when more pages are available for this node.</p> Source code in <code>client/ayon_core/ui/components/table_model.py</code> <pre><code>def canFetchMore(  # noqa: N802\n    self,\n    parent: QModelIndex | QPersistentModelIndex = QModelIndex(),\n) -&gt; bool:\n    \"\"\"Return whether more rows can be fetched for parent.\n\n    Args:\n        parent: Parent index (invalid = root).\n\n    Returns:\n        ``True`` when more pages are available for this node.\n    \"\"\"\n    node = self._node_from_index(parent)\n    if node.is_root:\n        return node.has_more\n    if not self._tree_mode:\n        return False\n    if not node.row_has_children:\n        return False\n    if not node.children_loaded:\n        return True\n    return node.has_more\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/table_model.html#client.ayon_core.ui.components.table_model.PaginatedTableModel.columnCount","title":"<code>columnCount(parent=QModelIndex())</code>","text":"<p>Return the number of columns.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <code>QModelIndex | QPersistentModelIndex</code> <p>Unused; column count is the same for all nodes.</p> <code>QModelIndex()</code> <p>Returns:</p> Type Description <code>int</code> <p>Number of columns.</p> Source code in <code>client/ayon_core/ui/components/table_model.py</code> <pre><code>def columnCount(  # noqa: N802\n    self,\n    parent: QModelIndex | QPersistentModelIndex = QModelIndex(),\n) -&gt; int:\n    \"\"\"Return the number of columns.\n\n    Args:\n        parent: Unused; column count is the same for all nodes.\n\n    Returns:\n        Number of columns.\n    \"\"\"\n    return len(self._columns)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/table_model.html#client.ayon_core.ui.components.table_model.PaginatedTableModel.data","title":"<code>data(index, role=Qt.ItemDataRole.DisplayRole)</code>","text":"<p>Return data for the given index and role.</p> <p>Parameters:</p> Name Type Description Default <code>index</code> <code>QModelIndex | QPersistentModelIndex</code> <p>Model index identifying the cell.</p> required <code>role</code> <code>int</code> <p>Qt item data role.</p> <code>DisplayRole</code> <p>Returns:</p> Type Description <code>Any</code> <p>Cell value appropriate for the requested role, or <code>None</code>.</p> Source code in <code>client/ayon_core/ui/components/table_model.py</code> <pre><code>def data(\n    self,\n    index: QModelIndex | QPersistentModelIndex,\n    role: int = Qt.ItemDataRole.DisplayRole,\n) -&gt; Any:\n    \"\"\"Return data for the given index and role.\n\n    Args:\n        index: Model index identifying the cell.\n        role: Qt item data role.\n\n    Returns:\n        Cell value appropriate for the requested role, or ``None``.\n    \"\"\"\n    if not index.isValid():\n        return None\n    node: _TableNode | None = index.internalPointer()  # type: ignore[assignment]\n    if node is None:\n        return None\n    col = index.column()\n    if col &lt; 0 or col &gt;= len(self._columns):\n        return None\n\n    row_dict = node.row_data\n    col_key = self._columns[col].key\n\n    if role == Qt.ItemDataRole.DisplayRole:\n        value = row_dict.get(col_key)\n        if value is None:\n            return \"\"\n        return str(value)\n\n    if role == Qt.ItemDataRole.DecorationRole:\n        icon_key = f\"{col_key}__icon\"\n        icon_name = row_dict.get(icon_key)\n        if icon_name:\n            icon_color = row_dict.get(f\"{col_key}__color\", \"#ffffff\")\n            icon_fill = row_dict.get(f\"{col_key}__icon_fill\", False)\n            return get_icon(icon_name, color=icon_color, fill=icon_fill)\n        return None\n\n    if role == Qt.ItemDataRole.ForegroundRole:\n        color_key = f\"{col_key}__color\"\n        color = row_dict.get(color_key)\n        if isinstance(color, str):\n            return QBrush(QColor(color))\n        return None\n\n    if role == Qt.ItemDataRole.UserRole:\n        return row_dict\n\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/table_model.html#client.ayon_core.ui.components.table_model.PaginatedTableModel.fetchMore","title":"<code>fetchMore(parent=QModelIndex())</code>","text":"<p>Fetch the next page of rows for parent.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <code>QModelIndex | QPersistentModelIndex</code> <p>Parent index (invalid = root).</p> <code>QModelIndex()</code> Source code in <code>client/ayon_core/ui/components/table_model.py</code> <pre><code>def fetchMore(  # noqa: N802\n    self,\n    parent: QModelIndex | QPersistentModelIndex = QModelIndex(),\n) -&gt; None:\n    \"\"\"Fetch the next page of rows for parent.\n\n    Args:\n        parent: Parent index (invalid = root).\n    \"\"\"\n    self._fetch_next_page(self._node_from_index(parent))\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/table_model.html#client.ayon_core.ui.components.table_model.PaginatedTableModel.get_distinct_values","title":"<code>get_distinct_values(key)</code>","text":"<p>Return sorted distinct non-empty string values for a column.</p> <p>In flat mode scans root-level rows; in tree mode scans all loaded nodes across all levels.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>Column key to inspect.</p> required <p>Returns:</p> Type Description <code>list[str]</code> <p>Sorted list of unique string values found in loaded rows.</p> Source code in <code>client/ayon_core/ui/components/table_model.py</code> <pre><code>def get_distinct_values(self, key: str) -&gt; list[str]:\n    \"\"\"Return sorted distinct non-empty string values for a column.\n\n    In flat mode scans root-level rows; in tree mode scans all\n    loaded nodes across all levels.\n\n    Args:\n        key: Column key to inspect.\n\n    Returns:\n        Sorted list of unique string values found in loaded rows.\n    \"\"\"\n    seen: set[str] = set()\n    nodes = (\n        self._all_nodes if self._tree_mode else set(self._root.children)\n    )\n    for node in nodes:\n        if node.is_root:\n            continue\n        val = node.row_data.get(key)\n        if val is not None:\n            s = str(val).strip()\n            if s:\n                seen.add(s)\n    return sorted(seen)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/table_model.html#client.ayon_core.ui.components.table_model.PaginatedTableModel.hasChildren","title":"<code>hasChildren(parent=QModelIndex())</code>","text":"<p>Return whether the node at parent has or can have children.</p> <p>Controls whether Qt draws a disclosure triangle even before children are loaded.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <code>QModelIndex | QPersistentModelIndex</code> <p>Parent index (invalid = root).</p> <code>QModelIndex()</code> <p>Returns:</p> Type Description <code>bool</code> <p>True if the node has loaded children, has more pages, or</p> <code>bool</code> <p>(in tree mode only) the row data declares <code>has_children</code>.</p> Source code in <code>client/ayon_core/ui/components/table_model.py</code> <pre><code>def hasChildren(  # noqa: N802\n    self,\n    parent: QModelIndex | QPersistentModelIndex = QModelIndex(),\n) -&gt; bool:\n    \"\"\"Return whether the node at parent has or can have children.\n\n    Controls whether Qt draws a disclosure triangle even before\n    children are loaded.\n\n    Args:\n        parent: Parent index (invalid = root).\n\n    Returns:\n        True if the node has loaded children, has more pages, or\n        (in tree mode only) the row data declares ``has_children``.\n    \"\"\"\n    node = self._node_from_index(parent)\n    if node.is_root:\n        return bool(node.children) or node.has_more\n    if not self._tree_mode:\n        return False\n    if node.children_loaded:\n        return bool(node.children)\n    return node.row_has_children\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/table_model.html#client.ayon_core.ui.components.table_model.PaginatedTableModel.headerData","title":"<code>headerData(section, orientation, role=Qt.ItemDataRole.DisplayRole)</code>","text":"<p>Return header data for the given section and orientation.</p> <p>Parameters:</p> Name Type Description Default <code>section</code> <code>int</code> <p>Column (horizontal) or row (vertical) index.</p> required <code>orientation</code> <code>Orientation</code> <p>Header orientation.</p> required <code>role</code> <code>int</code> <p>Qt item data role.</p> <code>DisplayRole</code> <p>Returns:</p> Type Description <code>Any</code> <p>Column label for horizontal DisplayRole, otherwise <code>None</code>.</p> Source code in <code>client/ayon_core/ui/components/table_model.py</code> <pre><code>def headerData(  # noqa: N802\n    self,\n    section: int,\n    orientation: Qt.Orientation,\n    role: int = Qt.ItemDataRole.DisplayRole,\n) -&gt; Any:\n    \"\"\"Return header data for the given section and orientation.\n\n    Args:\n        section: Column (horizontal) or row (vertical) index.\n        orientation: Header orientation.\n        role: Qt item data role.\n\n    Returns:\n        Column label for horizontal DisplayRole, otherwise ``None``.\n    \"\"\"\n    if (\n        orientation == Qt.Orientation.Horizontal\n        and role == Qt.ItemDataRole.DisplayRole\n    ):\n        if 0 &lt;= section &lt; len(self._columns):\n            return self._columns[section].label\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/table_model.html#client.ayon_core.ui.components.table_model.PaginatedTableModel.index","title":"<code>index(row, column, parent=QModelIndex())</code>","text":"<p>Return a model index for row/column under parent.</p> <p>Parameters:</p> Name Type Description Default <code>row</code> <code>int</code> <p>Row number under parent.</p> required <code>column</code> <code>int</code> <p>Column number.</p> required <code>parent</code> <code>QModelIndex | QPersistentModelIndex</code> <p>Parent index (invalid = root).</p> <code>QModelIndex()</code> <p>Returns:</p> Type Description <code>QModelIndex</code> <p>Valid QModelIndex, or invalid if out of range.</p> Source code in <code>client/ayon_core/ui/components/table_model.py</code> <pre><code>def index(  # noqa: N802\n    self,\n    row: int,\n    column: int,\n    parent: QModelIndex | QPersistentModelIndex = QModelIndex(),\n) -&gt; QModelIndex:\n    \"\"\"Return a model index for row/column under parent.\n\n    Args:\n        row: Row number under parent.\n        column: Column number.\n        parent: Parent index (invalid = root).\n\n    Returns:\n        Valid QModelIndex, or invalid if out of range.\n    \"\"\"\n    if not self.hasIndex(row, column, parent):\n        return QModelIndex()\n    parent_node = self._node_from_index(parent)\n    if row &gt;= len(parent_node.children):\n        return QModelIndex()\n    return self.createIndex(row, column, parent_node.children[row])\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/table_model.html#client.ayon_core.ui.components.table_model.PaginatedTableModel.parent","title":"<code>parent(index)</code>","text":"<p>Return the parent index of the given index.</p> <p>Parameters:</p> Name Type Description Default <code>index</code> <code>QModelIndex | QPersistentModelIndex</code> <p>Child index.</p> required <p>Returns:</p> Type Description <code>QModelIndex</code> <p>Parent QModelIndex, or invalid for root-level items.</p> Source code in <code>client/ayon_core/ui/components/table_model.py</code> <pre><code>def parent(  # type: ignore[override]\n    self,\n    index: QModelIndex | QPersistentModelIndex,\n) -&gt; QModelIndex:\n    \"\"\"Return the parent index of the given index.\n\n    Args:\n        index: Child index.\n\n    Returns:\n        Parent QModelIndex, or invalid for root-level items.\n    \"\"\"\n    if not index.isValid():\n        return QModelIndex()\n    node: _TableNode = index.internalPointer()  # type: ignore[assignment]\n    parent_node = node.parent\n    if parent_node is None or parent_node.is_root:\n        return QModelIndex()\n    grandparent = parent_node.parent\n    row = grandparent.children.index(parent_node)  # type: ignore[union-attr]\n    return self.createIndex(row, 0, parent_node)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/table_model.html#client.ayon_core.ui.components.table_model.PaginatedTableModel.reset_data","title":"<code>reset_data()</code>","text":"<p>Reset the model and re-fetch from page 0.</p> Source code in <code>client/ayon_core/ui/components/table_model.py</code> <pre><code>def reset_data(self) -&gt; None:\n    \"\"\"Reset the model and re-fetch from page 0.\"\"\"\n    old_request_id = self._request_id\n    if not self._no_async:\n        get_task_queue().clear_context_tasks(old_request_id)\n    self._request_id = self._generate_request_id()\n    self._pending_tasks = 0\n    self._pending_batch_nodes = []\n    self._batch_scheduled = False\n    self.beginResetModel()\n    self._root = _TableNode(node_id=None, row_data={}, parent=None)\n    self._all_nodes = {self._root}\n    self._columns = self._explicit_columns or []\n    self.endResetModel()\n    # Emit loading signals *after* endResetModel so that any slot\n    # connected to loading_changed observes a consistent model state.\n    self._update_loading_state()\n    self._fetch_next_page(self._root)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/table_model.html#client.ayon_core.ui.components.table_model.PaginatedTableModel.rowCount","title":"<code>rowCount(parent=QModelIndex())</code>","text":"<p>Return the number of loaded children under parent.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <code>QModelIndex | QPersistentModelIndex</code> <p>Parent index (invalid = root).</p> <code>QModelIndex()</code> <p>Returns:</p> Type Description <code>int</code> <p>Number of loaded child rows.</p> Source code in <code>client/ayon_core/ui/components/table_model.py</code> <pre><code>def rowCount(  # noqa: N802\n    self,\n    parent: QModelIndex | QPersistentModelIndex = QModelIndex(),\n) -&gt; int:\n    \"\"\"Return the number of loaded children under parent.\n\n    Args:\n        parent: Parent index (invalid = root).\n\n    Returns:\n        Number of loaded child rows.\n    \"\"\"\n    if parent.column() &gt; 0:\n        return 0\n    return len(self._node_from_index(parent).children)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/table_model.html#client.ayon_core.ui.components.table_model.PaginatedTableModel.set_columns","title":"<code>set_columns(columns)</code>","text":"<p>Set the columns and reset the model from page 0.</p> <p>Parameters:</p> Name Type Description Default <code>columns</code> <code>list[TableColumn]</code> <p>List of columns to display.</p> required Source code in <code>client/ayon_core/ui/components/table_model.py</code> <pre><code>def set_columns(self, columns: list[TableColumn]) -&gt; None:\n    \"\"\"Set the columns and reset the model from page 0.\n\n    Args:\n        columns: List of columns to display.\n    \"\"\"\n    self._explicit_columns = columns\n    self.reset_data()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/table_model.html#client.ayon_core.ui.components.table_model.PaginatedTableModel.set_page","title":"<code>set_page(page)</code>","text":"<p>Reset the model and begin fetching from the given page.</p> <p>Parameters:</p> Name Type Description Default <code>page</code> <code>int</code> <p>0-based page number to start from.</p> required Source code in <code>client/ayon_core/ui/components/table_model.py</code> <pre><code>def set_page(self, page: int) -&gt; None:\n    \"\"\"Reset the model and begin fetching from the given page.\n\n    Args:\n        page: 0-based page number to start from.\n    \"\"\"\n    old_request_id = self._request_id\n    if not self._no_async:\n        get_task_queue().clear_context_tasks(old_request_id)\n    self._request_id = self._generate_request_id()\n    self._pending_tasks = 0\n    self._pending_batch_nodes = []\n    self._batch_scheduled = False\n    self.beginResetModel()\n    self._root = _TableNode(node_id=None, row_data={}, parent=None)\n    self._root.current_page = page\n    self._all_nodes = {self._root}\n    self._columns = self._explicit_columns or []\n    self.endResetModel()\n    # Emit loading signals *after* endResetModel so that any slot\n    # connected to loading_changed observes a consistent model state.\n    self._update_loading_state()\n    self._fetch_next_page(self._root)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/table_model.html#client.ayon_core.ui.components.table_model.PaginatedTableModel.set_page_size","title":"<code>set_page_size(size)</code>","text":"<p>Update the page size and reset the model from page 0.</p> <p>Parameters:</p> Name Type Description Default <code>size</code> <code>int</code> <p>New page size (rows per page).</p> required Source code in <code>client/ayon_core/ui/components/table_model.py</code> <pre><code>def set_page_size(self, size: int) -&gt; None:\n    \"\"\"Update the page size and reset the model from page 0.\n\n    Args:\n        size: New page size (rows per page).\n    \"\"\"\n    self._page_size = size\n    self.reset_data()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/table_model.html#client.ayon_core.ui.components.table_model.PaginatedTableModel.set_tree_mode","title":"<code>set_tree_mode(enabled)</code>","text":"<p>Switch between flat table mode and hierarchical tree mode.</p> <p>Emits <code>tree_mode_changed</code> and reloads from page 0.</p> <p>Parameters:</p> Name Type Description Default <code>enabled</code> <code>bool</code> <p><code>True</code> to enable tree mode, <code>False</code> for flat.</p> required Source code in <code>client/ayon_core/ui/components/table_model.py</code> <pre><code>def set_tree_mode(self, enabled: bool) -&gt; None:\n    \"\"\"Switch between flat table mode and hierarchical tree mode.\n\n    Emits ``tree_mode_changed`` and reloads from page 0.\n\n    Args:\n        enabled: ``True`` to enable tree mode, ``False`` for flat.\n    \"\"\"\n    if self._tree_mode == enabled:\n        return\n    self._tree_mode = enabled\n    self.tree_mode_changed.emit(enabled)\n    self.reset_data()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/table_model.html#client.ayon_core.ui.components.table_model.PaginatedTableModel.sort","title":"<code>sort(column, order=Qt.SortOrder.AscendingOrder)</code>","text":"<p>Set the active sort column and order, then reload data.</p> <p>Sorting always resets from page 0 at all levels.</p> <p>Parameters:</p> Name Type Description Default <code>column</code> <code>int</code> <p>Zero-based index of the column to sort by. If out of range, the call is ignored.</p> required <code>order</code> <code>SortOrder</code> <p>Sort order (ascending or descending).</p> <code>AscendingOrder</code> Source code in <code>client/ayon_core/ui/components/table_model.py</code> <pre><code>def sort(\n    self, column: int, order: Qt.SortOrder = Qt.SortOrder.AscendingOrder\n) -&gt; None:\n    \"\"\"Set the active sort column and order, then reload data.\n\n    Sorting always resets from page 0 at all levels.\n\n    Args:\n        column: Zero-based index of the column to sort by. If out of\n            range, the call is ignored.\n        order: Sort order (ascending or descending).\n    \"\"\"\n    if column &lt; 0 or column &gt;= len(self._columns):\n        return\n    self._sort_column = column\n    self._sort_order = order\n    self.reset_data()  # refetch page 0 using new sort\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/table_model.html#client.ayon_core.ui.components.table_model.TableColumn","title":"<code>TableColumn</code>  <code>dataclass</code>","text":"<p>Describes a single column in a PaginatedTableModel.</p> <p>Attributes:</p> Name Type Description <code>key</code> <code>str</code> <p>Dictionary key used to look up cell values in row data.</p> <code>label</code> <code>str</code> <p>Display text shown in the header.</p> <code>width</code> <code>int</code> <p>Preferred column width hint in pixels. 0 means auto.</p> <code>sortable</code> <code>bool</code> <p>Whether the column can be sorted by clicking the header.</p> <code>icon</code> <code>str | None</code> <p>Optional material icon name shown in the filter dropdown.</p> <code>tree_position</code> <code>bool</code> <p>Whether the column is used for tree indentation.</p> <code>widget_factory</code> <code>'Callable[[Any, Any], Any] | None'</code> <p>Optional callable <code>(index, parent) -&gt; QWidget</code>. When set, the delegate opens a persistent editor for every cell in this column instead of painting text/icons.  The factory receives the display-model <code>QModelIndex</code> and the viewport widget as <code>parent</code>.  <code>setEditorData</code> is called automatically by Qt whenever the model emits <code>dataChanged</code> for that index, so server-push updates reach the widget without any extra wiring.</p> Source code in <code>client/ayon_core/ui/components/table_model.py</code> <pre><code>@dataclass\nclass TableColumn:\n    \"\"\"Describes a single column in a PaginatedTableModel.\n\n    Attributes:\n        key: Dictionary key used to look up cell values in row data.\n        label: Display text shown in the header.\n        width: Preferred column width hint in pixels. 0 means auto.\n        sortable: Whether the column can be sorted by clicking the header.\n        icon: Optional material icon name shown in the filter dropdown.\n        tree_position: Whether the column is used for tree indentation.\n        widget_factory: Optional callable ``(index, parent) -&gt; QWidget``.\n            When set, the delegate opens a persistent editor for every\n            cell in this column instead of painting text/icons.  The\n            factory receives the display-model ``QModelIndex`` and the\n            viewport widget as ``parent``.  ``setEditorData`` is called\n            automatically by Qt whenever the model emits ``dataChanged``\n            for that index, so server-push updates reach the widget\n            without any extra wiring.\n    \"\"\"\n\n    key: str\n    label: str\n    width: int = 0\n    sortable: bool = True\n    filterable: bool = True\n    icon: str | None = None\n    tree_position: bool = False\n    widget_factory: \"Callable[[Any, Any], Any] | None\" = None\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/table_view.html","title":"table_view","text":"<p>AYTableView component module.</p> <p>A flat, paginated table built on QTreeView with AYON styling.</p>"},{"location":"autoapi/client/ayon_core/ui/components/table_view.html#client.ayon_core.ui.components.table_view.AYTableHeader","title":"<code>AYTableHeader</code>","text":"<p>               Bases: <code>StyleMixin</code>, <code>QHeaderView</code></p> <p>Custom QHeaderView that paints sections directly, bypassing QSS.</p> <p>Draws header sections using QPainter to avoid interference from QStyleSheetStyle when a QSS stylesheet is loaded at the app level.</p> <p>Parameters:</p> Name Type Description Default <code>orientation</code> <code>Orientation</code> <p>Header orientation (Horizontal or Vertical).</p> required <code>parent</code> <code>QWidget | None</code> <p>Optional parent widget.</p> <code>None</code> <code>style_model</code> <code>StyleData | None</code> <p>Style data model; if None falls back to super().</p> <code>None</code> <code>variant</code> <code>str</code> <p>Visual style variant name.</p> <code>'default'</code> Source code in <code>client/ayon_core/ui/components/table_view.py</code> <pre><code>class AYTableHeader(StyleMixin, QHeaderView):\n    \"\"\"Custom QHeaderView that paints sections directly, bypassing QSS.\n\n    Draws header sections using QPainter to avoid interference from\n    QStyleSheetStyle when a QSS stylesheet is loaded at the app level.\n\n    Args:\n        orientation: Header orientation (Horizontal or Vertical).\n        parent: Optional parent widget.\n        style_model: Style data model; if None falls back to super().\n        variant: Visual style variant name.\n    \"\"\"\n\n    def __init__(\n        self,\n        orientation: Qt.Orientation,\n        parent: QWidget | None = None,\n        style_model: StyleData | None = None,\n        variant: str = \"default\",\n    ) -&gt; None:\n        super().__init__(orientation, parent)\n        self._style_model = style_model\n        self._variant_str = variant\n        self._toggle_btn: QToolButton | None = None\n        # self.setSortIndicatorShown(True)\n\n    def paintSection(\n        self,\n        painter: QPainter,\n        rect: QRect,\n        logical_index: int,\n    ) -&gt; None:\n        \"\"\"Paint a single header section directly with QPainter.\n\n        Falls back to the base implementation when no style model is\n        available.\n\n        Args:\n            painter: The painter to draw with.\n            rect: The bounding rectangle for this section.\n            logical_index: The logical index of the section.\n        \"\"\"\n        if self._style_model is None:\n            super().paintSection(painter, rect, logical_index)\n            return\n\n        tbl_style = self._style_model.get_style(\n            \"AYTableView\", self._variant_str\n        )\n\n        painter.save()\n        painter.setRenderHint(QPainter.RenderHint.Antialiasing, True)\n        painter.setClipRect(rect)\n\n        # Draw cell background and border\n        painter.setBrush(\n            QBrush(QColor(tbl_style.get(\"header-background-color\", \"#272d35\")))\n        )\n        painter.setPen(\n            QPen(\n                QColor(tbl_style.get(\"header-border-color\", \"#41474d\")),\n                tbl_style.get(\"header-border-width\", 1),\n            )\n        )\n        painter.drawRect(rect)\n\n        # Label text\n        padding = tbl_style.get(\"header-padding\", [4, 8])\n        h_pad = int(padding[1]) if len(padding) &gt; 1 else 8\n        v_pad = int(padding[0])\n        text_rect = rect.adjusted(h_pad, v_pad, -h_pad, -v_pad)\n\n        painter.setPen(QColor(tbl_style.get(\"header-color\", \"#c1c7ce\")))\n        font = self.font()\n        font.setWeight(QFont.Weight.DemiBold)\n        painter.setFont(font)\n\n        model = self.model()\n        if model is not None:\n            label = model.headerData(\n                logical_index,\n                self.orientation(),\n                Qt.ItemDataRole.DisplayRole,\n            )\n            if label is not None:\n                painter.drawText(\n                    text_rect,\n                    Qt.AlignmentFlag.AlignVCenter | Qt.AlignmentFlag.AlignLeft,\n                    str(label),\n                )\n\n        # Sort indicator\n        if (\n            self.isSortIndicatorShown()\n            and self.sortIndicatorSection() == logical_index\n        ):\n            is_sortable = True\n            if isinstance(\n                model, PaginatedTableModel\n            ) and 0 &lt;= logical_index &lt; len(model.columns):\n                is_sortable = model.columns[logical_index].sortable\n            if not is_sortable:\n                painter.restore()\n                return\n\n            order = self.sortIndicatorOrder()\n            icon_name = tbl_style.get(\"header-sort-indicator-icon\")\n            if icon_name:\n                icon = get_icon(\n                    icon_name,\n                    color=tbl_style.get(\n                        \"header-sort-indicator-color\", \"#ffffff\"\n                    ),\n                )\n                size = tbl_style.get(\"header-sort-indicator-size\", 16)\n                margin = (rect.height() - size) / 2.0\n                pixmap = icon.pixmap(size, size)\n                target = rect.adjusted(\n                    max(rect.width() - (size + h_pad), 0),\n                    margin,\n                    -h_pad,\n                    -margin,\n                )\n\n                if order == Qt.SortOrder.AscendingOrder:\n                    painter.save()\n                    center = target.center()\n                    painter.translate(center)\n                    painter.rotate(180)\n                    painter.translate(-center)\n                    painter.drawPixmap(target, pixmap)\n                    painter.restore()\n                else:\n                    painter.drawPixmap(target, pixmap)\n            else:\n                arrow = \"\u25b2\" if order == Qt.SortOrder.AscendingOrder else \"\u25bc\"\n                painter.drawText(\n                    text_rect,\n                    Qt.AlignmentFlag.AlignVCenter\n                    | Qt.AlignmentFlag.AlignRight,\n                    arrow,\n                )\n\n        painter.restore()\n\n    def resizeEvent(self, event: Any) -&gt; None:  # type: ignore[override]\n        \"\"\"Reposition the toggle button after a resize.\n\n        Args:\n            event: Resize event.\n        \"\"\"\n        super().resizeEvent(event)\n        self._reposition_toggle()\n\n    def _reposition_toggle(self) -&gt; None:\n        \"\"\"Move the toggle button to the right-centre of the header.\"\"\"\n        if self._toggle_btn is None:\n            return\n        btn = self._toggle_btn\n        h = self.height()\n        margin = max((h - btn.height()) // 2, 0)\n        x = max(self.width() - btn.width() - margin, 0)\n        btn.move(x, margin)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/table_view.html#client.ayon_core.ui.components.table_view.AYTableHeader.paintSection","title":"<code>paintSection(painter, rect, logical_index)</code>","text":"<p>Paint a single header section directly with QPainter.</p> <p>Falls back to the base implementation when no style model is available.</p> <p>Parameters:</p> Name Type Description Default <code>painter</code> <code>QPainter</code> <p>The painter to draw with.</p> required <code>rect</code> <code>QRect</code> <p>The bounding rectangle for this section.</p> required <code>logical_index</code> <code>int</code> <p>The logical index of the section.</p> required Source code in <code>client/ayon_core/ui/components/table_view.py</code> <pre><code>def paintSection(\n    self,\n    painter: QPainter,\n    rect: QRect,\n    logical_index: int,\n) -&gt; None:\n    \"\"\"Paint a single header section directly with QPainter.\n\n    Falls back to the base implementation when no style model is\n    available.\n\n    Args:\n        painter: The painter to draw with.\n        rect: The bounding rectangle for this section.\n        logical_index: The logical index of the section.\n    \"\"\"\n    if self._style_model is None:\n        super().paintSection(painter, rect, logical_index)\n        return\n\n    tbl_style = self._style_model.get_style(\n        \"AYTableView\", self._variant_str\n    )\n\n    painter.save()\n    painter.setRenderHint(QPainter.RenderHint.Antialiasing, True)\n    painter.setClipRect(rect)\n\n    # Draw cell background and border\n    painter.setBrush(\n        QBrush(QColor(tbl_style.get(\"header-background-color\", \"#272d35\")))\n    )\n    painter.setPen(\n        QPen(\n            QColor(tbl_style.get(\"header-border-color\", \"#41474d\")),\n            tbl_style.get(\"header-border-width\", 1),\n        )\n    )\n    painter.drawRect(rect)\n\n    # Label text\n    padding = tbl_style.get(\"header-padding\", [4, 8])\n    h_pad = int(padding[1]) if len(padding) &gt; 1 else 8\n    v_pad = int(padding[0])\n    text_rect = rect.adjusted(h_pad, v_pad, -h_pad, -v_pad)\n\n    painter.setPen(QColor(tbl_style.get(\"header-color\", \"#c1c7ce\")))\n    font = self.font()\n    font.setWeight(QFont.Weight.DemiBold)\n    painter.setFont(font)\n\n    model = self.model()\n    if model is not None:\n        label = model.headerData(\n            logical_index,\n            self.orientation(),\n            Qt.ItemDataRole.DisplayRole,\n        )\n        if label is not None:\n            painter.drawText(\n                text_rect,\n                Qt.AlignmentFlag.AlignVCenter | Qt.AlignmentFlag.AlignLeft,\n                str(label),\n            )\n\n    # Sort indicator\n    if (\n        self.isSortIndicatorShown()\n        and self.sortIndicatorSection() == logical_index\n    ):\n        is_sortable = True\n        if isinstance(\n            model, PaginatedTableModel\n        ) and 0 &lt;= logical_index &lt; len(model.columns):\n            is_sortable = model.columns[logical_index].sortable\n        if not is_sortable:\n            painter.restore()\n            return\n\n        order = self.sortIndicatorOrder()\n        icon_name = tbl_style.get(\"header-sort-indicator-icon\")\n        if icon_name:\n            icon = get_icon(\n                icon_name,\n                color=tbl_style.get(\n                    \"header-sort-indicator-color\", \"#ffffff\"\n                ),\n            )\n            size = tbl_style.get(\"header-sort-indicator-size\", 16)\n            margin = (rect.height() - size) / 2.0\n            pixmap = icon.pixmap(size, size)\n            target = rect.adjusted(\n                max(rect.width() - (size + h_pad), 0),\n                margin,\n                -h_pad,\n                -margin,\n            )\n\n            if order == Qt.SortOrder.AscendingOrder:\n                painter.save()\n                center = target.center()\n                painter.translate(center)\n                painter.rotate(180)\n                painter.translate(-center)\n                painter.drawPixmap(target, pixmap)\n                painter.restore()\n            else:\n                painter.drawPixmap(target, pixmap)\n        else:\n            arrow = \"\u25b2\" if order == Qt.SortOrder.AscendingOrder else \"\u25bc\"\n            painter.drawText(\n                text_rect,\n                Qt.AlignmentFlag.AlignVCenter\n                | Qt.AlignmentFlag.AlignRight,\n                arrow,\n            )\n\n    painter.restore()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/table_view.html#client.ayon_core.ui.components.table_view.AYTableHeader.resizeEvent","title":"<code>resizeEvent(event)</code>","text":"<p>Reposition the toggle button after a resize.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <code>Any</code> <p>Resize event.</p> required Source code in <code>client/ayon_core/ui/components/table_view.py</code> <pre><code>def resizeEvent(self, event: Any) -&gt; None:  # type: ignore[override]\n    \"\"\"Reposition the toggle button after a resize.\n\n    Args:\n        event: Resize event.\n    \"\"\"\n    super().resizeEvent(event)\n    self._reposition_toggle()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/table_view.html#client.ayon_core.ui.components.table_view.AYTableView","title":"<code>AYTableView</code>","text":"<p>               Bases: <code>StyleMixin</code>, <code>QTreeView</code></p> <p>AYON-styled flat table view.</p> <p>Subclasses QTreeView in flat-table mode (no tree indentation or expand toggles). Uses AYONStyle for all painting, a custom item delegate that draws directly bypassing any parent QSS, and AYScrollBar instances for scrollbars.</p> <p>The header is visible and styled via TableHeaderDrawer.</p> <p>When a PaginatedTableModel is set, columns that carry a <code>widget_factory</code> on their :class:<code>TableColumn</code> definition get a persistent editor in every row via <code>openPersistentEditor</code>.  Qt calls <code>setEditorData</code> automatically whenever the model emits <code>dataChanged</code> for those cells, so server-push updates reach the widgets without any extra wiring.</p> <p>In tree mode, persistent editors are opened lazily \u2014 only for rows whose parent node is already expanded.  Rows under collapsed nodes receive their editors when the user (or code) expands that node, via the <code>expanded</code> signal handler.  This avoids creating widgets for the entire off-screen tree when Qt's layout pass eagerly triggers :meth:<code>fetchMore</code> for all visible nodes.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <code>QWidget | None</code> <p>Optional parent widget.</p> <code>None</code> <code>variant</code> <code>Variants</code> <p>Visual style variant controlling colours.</p> <code>Default</code> Source code in <code>client/ayon_core/ui/components/table_view.py</code> <pre><code>class AYTableView(StyleMixin, QTreeView):\n    \"\"\"AYON-styled flat table view.\n\n    Subclasses QTreeView in flat-table mode (no tree indentation or\n    expand toggles). Uses AYONStyle for all painting, a custom item\n    delegate that draws directly bypassing any parent QSS, and\n    AYScrollBar instances for scrollbars.\n\n    The header is visible and styled via TableHeaderDrawer.\n\n    When a PaginatedTableModel is set, columns that carry a\n    ``widget_factory`` on their :class:`TableColumn` definition get a\n    persistent editor in every row via ``openPersistentEditor``.  Qt\n    calls ``setEditorData`` automatically whenever the model emits\n    ``dataChanged`` for those cells, so server-push updates reach the\n    widgets without any extra wiring.\n\n    In tree mode, persistent editors are opened lazily \u2014 only for rows\n    whose parent node is already expanded.  Rows under collapsed nodes\n    receive their editors when the user (or code) expands that node,\n    via the ``expanded`` signal handler.  This avoids creating widgets\n    for the entire off-screen tree when Qt's layout pass eagerly\n    triggers :meth:`fetchMore` for all visible nodes.\n\n    Args:\n        parent: Optional parent widget.\n        variant: Visual style variant controlling colours.\n    \"\"\"\n\n    Variants = AYTableViewVariants\n    selection_changed = Signal(QItemSelection, QItemSelection)\n\n    def __init__(\n        self,\n        parent: QWidget | None = None,\n        variant: Variants = Variants.Default,\n    ) -&gt; None:\n        self._variant_str: str = variant.value\n\n        super().__init__(parent)\n\n        style = get_ayon_style()\n        self.setStyle(style)\n\n        # Custom header \u2014 paints sections directly, bypassing QSS.\n        header = AYTableHeader(\n            Qt.Orientation.Horizontal,\n            parent=self,\n            style_model=style.model,\n            variant=self._variant_str,\n        )\n        self.setHeader(header)\n\n        # Self-contained: do not inherit parent background.\n        self.setAttribute(Qt.WidgetAttribute.WA_TranslucentBackground, True)\n        self.setAttribute(Qt.WidgetAttribute.WA_Hover, True)\n        self.setMouseTracking(True)\n\n        self.viewport().setAttribute(\n            Qt.WidgetAttribute.WA_TranslucentBackground, False\n        )\n        self._sync_viewport_palette()\n\n        # Custom item delegate \u2014 paints cells directly.\n        delegate = TableItemDelegate(\n            parent=self,\n            style_model=style.model,\n            variant=self._variant_str,\n        )\n        delegate.setFont(self.font())\n        self.setItemDelegate(delegate)\n\n        # Styled scrollbars.\n        vsb = AYScrollBar(Qt.Orientation.Vertical, self)\n        self.setVerticalScrollBar(vsb)\n        self.setVerticalScrollBarPolicy(Qt.ScrollBarPolicy.ScrollBarAsNeeded)\n        hsb = AYScrollBar(Qt.Orientation.Horizontal, self)\n        self.setHorizontalScrollBar(hsb)\n        self.setHorizontalScrollBarPolicy(Qt.ScrollBarPolicy.ScrollBarAsNeeded)\n\n        # Viewport-aware persistent-editor management (open-only strategy).\n        # Tracks every QPersistentModelIndex for which openPersistentEditor\n        # has already been called so the same editor is never opened twice.\n        # Editors are never explicitly closed while scrolling: once built,\n        # a dormant off-screen persistent editor costs near zero (Qt skips\n        # its paintEvent), whereas re-creating it on scroll-back would cause\n        # widget churn and redundant async thumbnail re-fetches.\n        self._active_editor_pmis: set[QtCore.QPersistentModelIndex] = set()\n        # Single-shot, zero-interval timer \u2014 fires once per event-loop\n        # iteration so multiple rowsInserted bursts produce a single\n        # _sync_viewport_editors call instead of one per batch.\n        self._editor_sync_timer = QtCore.QTimer(self)\n        self._editor_sync_timer.setSingleShot(True)\n        self._editor_sync_timer.setInterval(0)\n        self._editor_sync_timer.timeout.connect(self._sync_viewport_editors)\n        vsb.valueChanged.connect(self._schedule_editor_sync)\n\n        # Flat table \u2014 no tree features.\n        self.setRootIsDecorated(False)\n        self.setItemsExpandable(False)\n        self.setIndentation(0)\n\n        # Header visible.\n        self.setHeaderHidden(False)\n\n        # Selection behaviour.\n        self.setSelectionMode(QTreeView.SelectionMode.ExtendedSelection)\n        self.setSelectionBehavior(QTreeView.SelectionBehavior.SelectRows)\n\n        # No default frame \u2014 drawn manually in paintEvent.\n        self.setFrameShape(QTreeView.Shape.NoFrame)\n\n        # Alternating row colours disabled \u2014 delegate handles it.\n        self.setAlternatingRowColors(False)\n\n        # Hovered-row tracking: stores the visual rect of the currently\n        # hovered row so we can force-repaint it when the mouse moves to a\n        # different row.  Without this, the branch-indicator area (painted\n        # by PE_IndicatorBranch, not the delegate) is never invalidated on\n        # row transitions and stays highlighted until the next full repaint.\n        self._hovered_row: int = -1\n        self._hovered_row_rect: QRect = QRect()\n\n        # Track the currently hovered index so we may pass hover state to\n        # editors.\n        self._hovered_index: QModelIndex = QModelIndex()\n\n        # Column indices that have widget_factory set (cached from model).\n        self._widget_col_indices: list[int] = []\n\n        # Track model connections for cleanup.  Each entry is (source_object,\n        # connection) so we can call the right object's .disconnect().\n        self._model_connections: list[\n            tuple[Any, QtCore.QMetaObject.Connection]\n        ] = []\n\n        # Node IDs that were expanded when the user last switched away from\n        # tree mode.  Restored row-by-row as data is (re-)loaded in tree mode.\n        self._expanded_node_ids: set[str] = set()\n\n        # Open persistent editors for already-loaded children when a tree node\n        # is expanded.  Complements _on_rows_inserted, which handles the case\n        # where data is loaded after the node is already expanded (async mode).\n        self.expanded.connect(self._schedule_editor_sync)\n\n    def _sync_viewport_palette(self) -&gt; None:\n        \"\"\"Apply the variant background colour to the viewport.\"\"\"\n        style = get_ayon_style()\n        tbl_style = style.model.get_style(\"AYTableView\", self._variant_str)\n        bg = QColor(tbl_style.get(\"background-color\", \"#252a31\"))\n        p = self.viewport().palette()\n        p.setColor(QPalette.ColorRole.Base, bg)\n        p.setColor(QPalette.ColorRole.Window, bg)\n        self.viewport().setPalette(p)\n\n    def paintEvent(self, event: QPaintEvent) -&gt; None:\n        \"\"\"Draw the outer container background before items.\n\n        Args:\n            event: The paint event.\n        \"\"\"\n        painter = QPainter(self.viewport())\n        painter.setRenderHint(QPainter.RenderHint.Antialiasing, False)\n        style = get_ayon_style()\n        tbl_style = style.model.get_style(\"AYTableView\", self._variant_str)\n        bg = QColor(tbl_style.get(\"background-color\", \"#252a31\"))\n        painter.fillRect(self.viewport().rect(), bg)\n        painter.end()\n\n        super().paintEvent(event)\n\n    def setModel(self, model: QtCore.QAbstractItemModel | None) -&gt; None:\n        \"\"\"Set the data model and configure header and widgets.\n\n        Args:\n            model: The data model to display.\n        \"\"\"\n        # Disconnect previous model signals.\n        for obj, conn in self._model_connections:\n            try:\n                obj.disconnect(conn)\n            except (RuntimeError, TypeError):\n                pass\n        self._model_connections.clear()\n\n        # Clear viewport-editor tracking \u2014 the old model's editors are\n        # invalidated by the model swap.\n        self._active_editor_pmis.clear()\n        self._editor_sync_timer.stop()\n\n        super().setModel(model)\n\n        if model is None:\n            self._widget_col_indices = []\n            return\n\n        # Cache widget-factory column indices for hover state updates.\n        source_mdl: Any = model\n        if isinstance(model, QAbstractProxyModel):\n            source_mdl = model.sourceModel()\n        if isinstance(source_mdl, PaginatedTableModel):\n            self._widget_col_indices = [\n                i\n                for i, col in enumerate(source_mdl.columns)\n                if col.widget_factory is not None\n            ]\n        else:\n            self._widget_col_indices = []\n\n        # Configure header from column width hints.\n        self._configure_header(model)\n\n        # Schedule viewport-aware editor opening for existing rows.\n        self._schedule_editor_sync()\n\n        # Connect to rowsInserted for lazy-loaded rows.\n        conn = model.rowsInserted.connect(self._on_rows_inserted)\n        if conn is not None:\n            self._model_connections.append((model, conn))\n\n        # Clear editor tracking on model reset so stale PMIs don't linger.\n        conn_reset = model.modelReset.connect(self._on_model_reset)\n        if conn_reset is not None:\n            self._model_connections.append((model, conn_reset))\n\n        # Connect to the source PaginatedTableModel for tree mode changes.\n        source: Any = model\n        if isinstance(model, QAbstractProxyModel):\n            source = model.sourceModel()\n        if isinstance(source, PaginatedTableModel):\n            conn2 = source.tree_mode_changed.connect(self._apply_tree_mode)\n            if conn2 is not None:\n                self._model_connections.append((source, conn2))\n            self._apply_tree_mode(source._tree_mode)\n\n    def _apply_tree_mode(self, tree_mode: bool) -&gt; None:\n        \"\"\"Configure the view for flat-table or tree display.\n\n        Args:\n            tree_mode: ``True`` to enable tree indentation and expand\n                controls; ``False`` for flat-table layout.\n        \"\"\"\n        if not tree_mode:\n            # Snapshot expansion state before the model resets.\n            self._save_expansion_state()\n        style = get_ayon_style()\n        tbl_style = style.model.get_style(\"AYTableView\", self._variant_str)\n        source = self._source_model()\n        tree_pos = (\n            source.tree_position\n            if isinstance(source, PaginatedTableModel)\n            else 0\n        )\n        if tree_mode:\n            indent = int(tbl_style.get(\"indent\", 16))\n            self.setRootIsDecorated(True)\n            self.setItemsExpandable(True)\n            self.setIndentation(indent)\n        else:\n            self.setRootIsDecorated(False)\n            self.setItemsExpandable(False)\n            self.setIndentation(0)\n        # Set the tree position to the column specified by the model.\n        self.setTreePosition(tree_pos)\n\n    def _source_model(self) -&gt; QtCore.QAbstractItemModel | None:\n        \"\"\"Return the underlying source model, unwrapping any proxy layer.\n\n        Returns:\n            The source model, or ``None`` if no model is set.\n        \"\"\"\n        model = self.model()\n        if isinstance(model, QAbstractProxyModel):\n            return model.sourceModel()\n        return model\n\n    def _repaint_row(self, row_rect: QRect) -&gt; None:\n        \"\"\"Repaint the full viewport width for a given row rect.\n\n        Args:\n            row_rect: Visual rect of the row to repaint.\n        \"\"\"\n        if row_rect.isNull():\n            return\n        vp = self.viewport()\n        vp.update(QRect(0, row_rect.y(), vp.width(), row_rect.height()))\n\n    def _configure_header(self, model: QtCore.QAbstractItemModel) -&gt; None:\n        \"\"\"Set up header section sizes from model column hints.\n\n        Args:\n            model: The data model (may be a proxy wrapping a\n                :class:`PaginatedTableModel`).\n        \"\"\"\n        header = self.header()\n        if header is None:\n            return\n\n        # Set header height from style.\n        style = get_ayon_style()\n        tbl_style = style.model.get_style(\"AYTableView\", self._variant_str)\n        header_height = int(tbl_style.get(\"header-height\", 36))\n        header.setFixedHeight(header_height)\n\n        # Disable header highlight on selection.\n        header.setHighlightSections(False)\n\n        col_count = model.columnCount()\n        if col_count == 0:\n            return\n\n        # Unwrap proxy to access PaginatedTableModel column definitions.\n        source = (\n            model.sourceModel()\n            if isinstance(model, QAbstractProxyModel)\n            else model\n        )\n\n        # Configure column widths\n        if isinstance(source, PaginatedTableModel) and any(\n            col_def.width &gt; 0 for col_def in source.columns\n        ):\n            # Use column definitions from the model.\n            for i, col_def in enumerate(source.columns):\n                if i &gt;= col_count:\n                    break\n                if col_def.width &gt; 0:\n                    header.resizeSection(i, col_def.width)\n                    header.setSectionResizeMode(\n                        i, QHeaderView.ResizeMode.Interactive\n                    )\n                else:\n                    header.setSectionResizeMode(\n                        i, QHeaderView.ResizeMode.Stretch\n                    )\n        else:\n            # Default: stretch all columns equally.\n            header.setSectionResizeMode(QHeaderView.ResizeMode.Stretch)\n\n        # Enable sorting.\n        # setSectionsClickable enables header click interaction (indicator\n        # toggling + sortIndicatorChanged signal) without QTreeView's internal\n        # sortIndicatorChanged \u2192 model.sort() connection that setSortingEnabled\n        # would create behind our guard.\n        header.setSectionsClickable(True)\n        header.setSortIndicatorShown(True)\n        if isinstance(source, PaginatedTableModel):\n            header.sortIndicatorChanged.connect(\n                lambda section, order: self._on_sort_indicator_changed(\n                    section, order, source\n                )\n            )\n\n    def _on_sort_indicator_changed(\n        self, section: int, order: Qt.SortOrder, model: PaginatedTableModel\n    ) -&gt; None:\n        cols = model.columns\n        if 0 &lt;= section &lt; len(cols) and not cols[section].sortable:\n            header = self.header()\n            header.blockSignals(True)\n            if 0 &lt;= model._sort_column &lt; len(cols):\n                header.setSortIndicator(model._sort_column, model._sort_order)\n            else:\n                header.setSortIndicatorShown(False)\n            header.blockSignals(False)\n            return\n        # Sortable column \u2014 delegate to the model.\n        model.sort(section, order)\n        # model.sort() triggers beginResetModel/endResetModel, which causes\n        # QHeaderView.initializeSections() to wipe the sort indicator state.\n        # Re-apply it explicitly after the reset.\n        header = self.header()\n        header.blockSignals(True)\n        header.setSortIndicatorShown(True)\n        header.setSortIndicator(section, order)\n        header.blockSignals(False)\n\n    def _save_expansion_state(self) -&gt; None:\n        \"\"\"Snapshot the node IDs of all expanded rows into\n        ``_expanded_node_ids``.\n\n        Called just before the model resets when switching from tree to flat\n        mode, while the tree is still fully populated.\n        \"\"\"\n        self._expanded_node_ids.clear()\n        display_model = self.model()\n        if display_model is None:\n            return\n        is_proxy = isinstance(display_model, QAbstractProxyModel)\n\n        def _collect(parent_view_idx: QModelIndex) -&gt; None:\n            for row in range(display_model.rowCount(parent_view_idx)):\n                child_view_idx = display_model.index(row, 0, parent_view_idx)\n                if not self.isExpanded(child_view_idx):\n                    continue\n                src_idx = (\n                    display_model.mapToSource(child_view_idx)  # type: ignore[union-attr]\n                    if is_proxy\n                    else child_view_idx\n                )\n                node = src_idx.internalPointer()\n                if node is not None and node.node_id is not None:\n                    self._expanded_node_ids.add(node.node_id)\n                _collect(child_view_idx)\n\n        _collect(QModelIndex())\n\n    def _restore_expansion_in_range(\n        self,\n        parent: QModelIndex,\n        first: int,\n        last: int,\n    ) -&gt; None:\n        \"\"\"Expand any newly-inserted rows whose node ID was previously saved.\n\n        Expanding a row triggers lazy child loading which fires more\n        ``rowsInserted`` signals, so the full expansion tree is restored\n        incrementally without extra bookkeeping.\n\n        Args:\n            parent: Parent index of the inserted rows.\n            first: First inserted row index.\n            last: Last inserted row index.\n        \"\"\"\n        if not self._expanded_node_ids:\n            return\n        display_model = self.model()\n        if display_model is None:\n            return\n        source = (\n            display_model.sourceModel()\n            if isinstance(display_model, QAbstractProxyModel)\n            else display_model\n        )\n        if not isinstance(source, PaginatedTableModel):\n            return\n        if not source._tree_mode:\n            return\n        is_proxy = isinstance(display_model, QAbstractProxyModel)\n        for row in range(first, last + 1):\n            child_view_idx = display_model.index(row, 0, parent)\n            src_idx = (\n                display_model.mapToSource(child_view_idx)\n                if is_proxy\n                else child_view_idx\n            )\n            node = src_idx.internalPointer()\n            if node is not None and node.node_id in self._expanded_node_ids:\n                self.expand(child_view_idx)\n\n    def _on_rows_inserted(\n        self,\n        parent: QModelIndex,\n        first: int,\n        last: int,\n    ) -&gt; None:\n        \"\"\"Open persistent editors and restore expansion for newly inserted\n        rows.\n\n        Args:\n            parent: Parent index; supports non-root parents in tree mode.\n            first: First inserted row index.\n            last: Last inserted row index.\n        \"\"\"\n        self._schedule_editor_sync()\n        self._restore_expansion_in_range(parent, first, last)\n\n    # ------------------------------------------------------------------\n    # Viewport-aware persistent-editor helpers\n    # ------------------------------------------------------------------\n\n    def _schedule_editor_sync(self) -&gt; None:\n        \"\"\"Schedule :meth:`_sync_viewport_editors` on the next event loop.\n\n        Multiple calls within the same event-loop iteration collapse into\n        a single sync because the timer is single-shot and we only start\n        it when it is not already active.\n        \"\"\"\n        if not self._editor_sync_timer.isActive():\n            self._editor_sync_timer.start()\n\n    def _on_model_reset(self) -&gt; None:\n        \"\"\"Clear editor tracking after a model reset.\n\n        Qt automatically closes all persistent editors on\n        ``beginResetModel``, so ``_active_editor_pmis`` would hold stale\n        entries.  Clearing it ensures :meth:`_sync_viewport_editors` starts\n        from a clean slate, then schedules a sync so visible rows get their\n        editors back.\n        \"\"\"\n        self._hovered_index = QModelIndex()\n        self._hovered_row = -1\n        self._hovered_row_rect = QRect()\n        self._active_editor_pmis.clear()\n        self._schedule_editor_sync()\n\n    def _get_visible_widget_indexes(self) -&gt; list[QModelIndex]:\n        \"\"\"Return display-model indexes for visible cells with widget\n        factories.\n\n        Uses :meth:`QTreeView.indexBelow` to walk visible rows from the\n        topmost visible item downwards, stopping as soon as the visual rect\n        passes the viewport bottom.  Works correctly in both flat and tree\n        mode.\n\n        Returns:\n            List of valid ``QModelIndex`` objects, one per (row, col) pair\n            that is both visible and belongs to a widget-factory column.\n        \"\"\"\n        model = self.model()\n        if model is None:\n            return []\n\n        source = self._source_model()\n        if not isinstance(source, PaginatedTableModel):\n            return []\n\n        widget_cols = [\n            i\n            for i, col in enumerate(source.columns)\n            if col.widget_factory is not None\n        ]\n        if not widget_cols:\n            return []\n\n        vp_rect = self.viewport().rect()\n        # indexAt() returns an invalid index when the viewport is empty.\n        top_idx = self.indexAt(\n            QtCore.QPoint(vp_rect.left(), vp_rect.top() + 1)\n        )\n        if not top_idx.isValid():\n            return []\n        # Normalise to column 0 so indexBelow() works predictably.\n        top_idx = model.index(top_idx.row(), 0, top_idx.parent())\n\n        results: list[QModelIndex] = []\n        idx = top_idx\n        while idx.isValid():\n            visual = self.visualRect(idx)\n            if visual.top() &gt; vp_rect.bottom():\n                break\n\n            if not visual.isEmpty():\n                for col in widget_cols:\n                    col_idx = model.index(idx.row(), col, idx.parent())\n                    if col_idx.isValid():\n                        results.append(col_idx)\n\n            idx = self.indexBelow(idx)\n\n        return results\n\n    def _sync_viewport_editors(self) -&gt; None:\n        \"\"\"Open persistent editors for rows that just became visible.\n\n        Called via :attr:`_editor_sync_timer` (single-shot, zero interval)\n        so that bursts of ``rowsInserted`` signals collapse into a single\n        call per event-loop iteration.  Also triggered on vertical scroll.\n\n        Uses an **open-only** strategy: opens editors for visible rows that\n        do not yet have one, but never closes editors for rows that have\n        scrolled out of view.  A dormant off-screen persistent editor costs\n        near zero (Qt skips its ``paintEvent``), while re-creating it on\n        scroll-back would cause widget churn and redundant thumbnail\n        re-fetches.\n\n        :attr:`_active_editor_pmis` grows monotonically within a model\n        lifetime and is reset by :meth:`_on_model_reset` so stale entries\n        from a previous model do not block new editors after a reset.\n        \"\"\"\n        for idx in self._get_visible_widget_indexes():\n            pmi = QtCore.QPersistentModelIndex(idx)\n            if pmi.isValid() and pmi not in self._active_editor_pmis:\n                self.openPersistentEditor(pmi)\n                self._active_editor_pmis.add(pmi)\n\n    def resizeEvent(self, event: Any) -&gt; None:  # type: ignore[override]\n        \"\"\"Schedule an editor sync when the view is resized.\n\n        A viewport resize may reveal rows that have never been visible\n        before.  Those rows need ``openPersistentEditor`` called on them\n        before their thumbnail widgets can be created and painted.  The\n        regular scroll-driven sync misses this case because the scrollbar\n        value does not change on a pure resize.\n\n        Args:\n            event: The resize event.\n        \"\"\"\n        super().resizeEvent(event)\n        self._schedule_editor_sync()\n\n    def mousePressEvent(  # type: ignore[override]\n        self, event: \"QtGui.QMouseEvent\"\n    ) -&gt; None:\n        \"\"\"Deselect all items when clicking in an empty area.\n\n        Args:\n            event: Mouse press event.\n        \"\"\"\n        index = self.indexAt(event.pos())\n        if not index.isValid():\n            self.clearSelection()\n            self.setCurrentIndex(self.rootIndex())\n            return\n        super().mousePressEvent(event)\n\n    def _set_row_state(self, row_idx: QModelIndex, hovered: bool) -&gt; None:\n        \"\"\"Set ``row_state`` property on all editor widgets in a row.\n\n        Iterates the cached widget-factory column indices and updates\n        each persistent editor's dynamic property so the style system\n        can react to hover, selected, and enabled states.\n\n        Args:\n            row_idx: Any valid index in the target row.\n            hovered: Whether the row is being hovered.\n        \"\"\"\n        state = QStyle.StateFlag.State_None\n        if hovered:\n            state |= QStyle.StateFlag.State_MouseOver\n        if self.selectionModel().isSelected(row_idx):\n            state |= QStyle.StateFlag.State_Selected\n        if not self.model().flags(row_idx) &amp; Qt.ItemFlag.ItemIsEnabled:\n            state &amp;= ~QStyle.StateFlag.State_Enabled\n\n        for col in self._widget_col_indices:\n            editor = self.indexWidget(row_idx.siblingAtColumn(col))\n            if editor:\n                editor.setProperty(\"row_state\", state)\n                editor.update()\n\n    def mouseMoveEvent(self, event: \"QtGui.QMouseEvent\") -&gt; None:  # type: ignore[override]\n        \"\"\"Track the hovered row and force-repaint it when it changes.\n\n        Qt only invalidates the cell directly under the cursor on hover\n        transitions.  The branch-indicator area (PE_IndicatorBranch) is\n        outside that cell rect, so it never receives a repaint request when\n        the cursor moves to a new row.  We force the viewport to repaint the\n        full width of the previously-hovered row so the indicator clears.\n        \"\"\"\n        new_idx = self.indexAt(event.pos())\n        new_row = new_idx.row() if new_idx.isValid() else -1\n\n        if new_row != self._hovered_row:\n            # Clear hover state on the previous row's editors.\n            if self._hovered_index.isValid():\n                self._set_row_state(self._hovered_index, False)\n\n            # Set hover state on the new row's editors.\n            if new_idx.isValid():\n                self._set_row_state(new_idx, True)\n\n            self._hovered_index = new_idx\n\n            # Repaint the old row so its indicator clears.\n            self._repaint_row(self._hovered_row_rect)\n            self._hovered_row = new_row\n            self._hovered_row_rect = (\n                self.visualRect(new_idx) if new_idx.isValid() else QRect()\n            )\n\n            # Repaint the new row so its indicator lights up immediately.\n            self._repaint_row(self._hovered_row_rect)\n\n        super().mouseMoveEvent(event)\n\n    def leaveEvent(self, event: \"QtCore.QEvent\") -&gt; None:\n        \"\"\"Clear hover tracking when the mouse exits the widget.\"\"\"\n        self._repaint_row(self._hovered_row_rect)\n        self._hovered_row = -1\n        self._hovered_row_rect = QRect()\n        super().leaveEvent(event)\n\n    def selectionChanged(\n        self,\n        selected: QItemSelection,\n        deselected: QItemSelection,\n    ) -&gt; None:\n        \"\"\"Override to emit a public signal on selection change.\n\n        Args:\n            selected: Newly selected items.\n            deselected: Newly deselected items.\n        \"\"\"\n        super().selectionChanged(selected, deselected)\n\n        # Update row_state for newly selected rows\n        for index in selected.indexes():\n            self._set_row_state(index, True)\n\n        # Update row_state for newly deselected rows\n        for index in deselected.indexes():\n            self._set_row_state(index, False)\n\n        self.selection_changed.emit(selected, deselected)\n\n    def drawBranches(self, painter, rect, index):\n        \"\"\"Draw branch indicators with AYONStyle directly.\n\n        Bypasses ``self.style()`` because, when an application-level QSS is\n        active, Qt wraps the widget's style in a ``QStyleSheetStyle`` proxy\n        which would otherwise intercept ``PE_IndicatorBranch`` and apply\n        QSS ``QTreeView::branch`` rules on top of (or instead of) ours.\n        \"\"\"\n        style = get_ayon_style()  # the raw AYONStyle, never wrapped\n\n        opt = QStyleOption()\n        opt.rect = rect\n        opt.palette = self.palette()\n        state = QStyle.StateFlag.State_Item\n        if self.model() is not None and self.model().hasChildren(index):\n            state |= QStyle.StateFlag.State_Children\n        if self.isExpanded(index):\n            state |= QStyle.StateFlag.State_Open\n        if self.selectionModel().isSelected(index):\n            state |= QStyle.StateFlag.State_Selected\n        if self.isEnabled():\n            state |= QStyle.StateFlag.State_Enabled\n\n        # Row-level hover: is the cursor on the same row as `index`?\n        hovered_index = self.indexAt(\n            self.viewport().mapFromGlobal(QCursor.pos())\n        )\n        if (\n            hovered_index.isValid()\n            and hovered_index.row() == index.row()\n            and hovered_index.parent() == index.parent()\n        ):\n            state |= QStyle.StateFlag.State_MouseOver\n\n        opt.state = state\n\n        # Call our drawer directly, not through self.style().\n        style.drawers[\n            enum_to_str(\n                QStyle.PrimitiveElement,\n                QStyle.PrimitiveElement.PE_IndicatorBranch,\n                \"QTreeView\",\n            )\n        ](opt, painter, self)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/table_view.html#client.ayon_core.ui.components.table_view.AYTableView.drawBranches","title":"<code>drawBranches(painter, rect, index)</code>","text":"<p>Draw branch indicators with AYONStyle directly.</p> <p>Bypasses <code>self.style()</code> because, when an application-level QSS is active, Qt wraps the widget's style in a <code>QStyleSheetStyle</code> proxy which would otherwise intercept <code>PE_IndicatorBranch</code> and apply QSS <code>QTreeView::branch</code> rules on top of (or instead of) ours.</p> Source code in <code>client/ayon_core/ui/components/table_view.py</code> <pre><code>def drawBranches(self, painter, rect, index):\n    \"\"\"Draw branch indicators with AYONStyle directly.\n\n    Bypasses ``self.style()`` because, when an application-level QSS is\n    active, Qt wraps the widget's style in a ``QStyleSheetStyle`` proxy\n    which would otherwise intercept ``PE_IndicatorBranch`` and apply\n    QSS ``QTreeView::branch`` rules on top of (or instead of) ours.\n    \"\"\"\n    style = get_ayon_style()  # the raw AYONStyle, never wrapped\n\n    opt = QStyleOption()\n    opt.rect = rect\n    opt.palette = self.palette()\n    state = QStyle.StateFlag.State_Item\n    if self.model() is not None and self.model().hasChildren(index):\n        state |= QStyle.StateFlag.State_Children\n    if self.isExpanded(index):\n        state |= QStyle.StateFlag.State_Open\n    if self.selectionModel().isSelected(index):\n        state |= QStyle.StateFlag.State_Selected\n    if self.isEnabled():\n        state |= QStyle.StateFlag.State_Enabled\n\n    # Row-level hover: is the cursor on the same row as `index`?\n    hovered_index = self.indexAt(\n        self.viewport().mapFromGlobal(QCursor.pos())\n    )\n    if (\n        hovered_index.isValid()\n        and hovered_index.row() == index.row()\n        and hovered_index.parent() == index.parent()\n    ):\n        state |= QStyle.StateFlag.State_MouseOver\n\n    opt.state = state\n\n    # Call our drawer directly, not through self.style().\n    style.drawers[\n        enum_to_str(\n            QStyle.PrimitiveElement,\n            QStyle.PrimitiveElement.PE_IndicatorBranch,\n            \"QTreeView\",\n        )\n    ](opt, painter, self)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/table_view.html#client.ayon_core.ui.components.table_view.AYTableView.leaveEvent","title":"<code>leaveEvent(event)</code>","text":"<p>Clear hover tracking when the mouse exits the widget.</p> Source code in <code>client/ayon_core/ui/components/table_view.py</code> <pre><code>def leaveEvent(self, event: \"QtCore.QEvent\") -&gt; None:\n    \"\"\"Clear hover tracking when the mouse exits the widget.\"\"\"\n    self._repaint_row(self._hovered_row_rect)\n    self._hovered_row = -1\n    self._hovered_row_rect = QRect()\n    super().leaveEvent(event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/table_view.html#client.ayon_core.ui.components.table_view.AYTableView.mouseMoveEvent","title":"<code>mouseMoveEvent(event)</code>","text":"<p>Track the hovered row and force-repaint it when it changes.</p> <p>Qt only invalidates the cell directly under the cursor on hover transitions.  The branch-indicator area (PE_IndicatorBranch) is outside that cell rect, so it never receives a repaint request when the cursor moves to a new row.  We force the viewport to repaint the full width of the previously-hovered row so the indicator clears.</p> Source code in <code>client/ayon_core/ui/components/table_view.py</code> <pre><code>def mouseMoveEvent(self, event: \"QtGui.QMouseEvent\") -&gt; None:  # type: ignore[override]\n    \"\"\"Track the hovered row and force-repaint it when it changes.\n\n    Qt only invalidates the cell directly under the cursor on hover\n    transitions.  The branch-indicator area (PE_IndicatorBranch) is\n    outside that cell rect, so it never receives a repaint request when\n    the cursor moves to a new row.  We force the viewport to repaint the\n    full width of the previously-hovered row so the indicator clears.\n    \"\"\"\n    new_idx = self.indexAt(event.pos())\n    new_row = new_idx.row() if new_idx.isValid() else -1\n\n    if new_row != self._hovered_row:\n        # Clear hover state on the previous row's editors.\n        if self._hovered_index.isValid():\n            self._set_row_state(self._hovered_index, False)\n\n        # Set hover state on the new row's editors.\n        if new_idx.isValid():\n            self._set_row_state(new_idx, True)\n\n        self._hovered_index = new_idx\n\n        # Repaint the old row so its indicator clears.\n        self._repaint_row(self._hovered_row_rect)\n        self._hovered_row = new_row\n        self._hovered_row_rect = (\n            self.visualRect(new_idx) if new_idx.isValid() else QRect()\n        )\n\n        # Repaint the new row so its indicator lights up immediately.\n        self._repaint_row(self._hovered_row_rect)\n\n    super().mouseMoveEvent(event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/table_view.html#client.ayon_core.ui.components.table_view.AYTableView.mousePressEvent","title":"<code>mousePressEvent(event)</code>","text":"<p>Deselect all items when clicking in an empty area.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <code>'QtGui.QMouseEvent'</code> <p>Mouse press event.</p> required Source code in <code>client/ayon_core/ui/components/table_view.py</code> <pre><code>def mousePressEvent(  # type: ignore[override]\n    self, event: \"QtGui.QMouseEvent\"\n) -&gt; None:\n    \"\"\"Deselect all items when clicking in an empty area.\n\n    Args:\n        event: Mouse press event.\n    \"\"\"\n    index = self.indexAt(event.pos())\n    if not index.isValid():\n        self.clearSelection()\n        self.setCurrentIndex(self.rootIndex())\n        return\n    super().mousePressEvent(event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/table_view.html#client.ayon_core.ui.components.table_view.AYTableView.paintEvent","title":"<code>paintEvent(event)</code>","text":"<p>Draw the outer container background before items.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <code>QPaintEvent</code> <p>The paint event.</p> required Source code in <code>client/ayon_core/ui/components/table_view.py</code> <pre><code>def paintEvent(self, event: QPaintEvent) -&gt; None:\n    \"\"\"Draw the outer container background before items.\n\n    Args:\n        event: The paint event.\n    \"\"\"\n    painter = QPainter(self.viewport())\n    painter.setRenderHint(QPainter.RenderHint.Antialiasing, False)\n    style = get_ayon_style()\n    tbl_style = style.model.get_style(\"AYTableView\", self._variant_str)\n    bg = QColor(tbl_style.get(\"background-color\", \"#252a31\"))\n    painter.fillRect(self.viewport().rect(), bg)\n    painter.end()\n\n    super().paintEvent(event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/table_view.html#client.ayon_core.ui.components.table_view.AYTableView.resizeEvent","title":"<code>resizeEvent(event)</code>","text":"<p>Schedule an editor sync when the view is resized.</p> <p>A viewport resize may reveal rows that have never been visible before.  Those rows need <code>openPersistentEditor</code> called on them before their thumbnail widgets can be created and painted.  The regular scroll-driven sync misses this case because the scrollbar value does not change on a pure resize.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <code>Any</code> <p>The resize event.</p> required Source code in <code>client/ayon_core/ui/components/table_view.py</code> <pre><code>def resizeEvent(self, event: Any) -&gt; None:  # type: ignore[override]\n    \"\"\"Schedule an editor sync when the view is resized.\n\n    A viewport resize may reveal rows that have never been visible\n    before.  Those rows need ``openPersistentEditor`` called on them\n    before their thumbnail widgets can be created and painted.  The\n    regular scroll-driven sync misses this case because the scrollbar\n    value does not change on a pure resize.\n\n    Args:\n        event: The resize event.\n    \"\"\"\n    super().resizeEvent(event)\n    self._schedule_editor_sync()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/table_view.html#client.ayon_core.ui.components.table_view.AYTableView.selectionChanged","title":"<code>selectionChanged(selected, deselected)</code>","text":"<p>Override to emit a public signal on selection change.</p> <p>Parameters:</p> Name Type Description Default <code>selected</code> <code>QItemSelection</code> <p>Newly selected items.</p> required <code>deselected</code> <code>QItemSelection</code> <p>Newly deselected items.</p> required Source code in <code>client/ayon_core/ui/components/table_view.py</code> <pre><code>def selectionChanged(\n    self,\n    selected: QItemSelection,\n    deselected: QItemSelection,\n) -&gt; None:\n    \"\"\"Override to emit a public signal on selection change.\n\n    Args:\n        selected: Newly selected items.\n        deselected: Newly deselected items.\n    \"\"\"\n    super().selectionChanged(selected, deselected)\n\n    # Update row_state for newly selected rows\n    for index in selected.indexes():\n        self._set_row_state(index, True)\n\n    # Update row_state for newly deselected rows\n    for index in deselected.indexes():\n        self._set_row_state(index, False)\n\n    self.selection_changed.emit(selected, deselected)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/table_view.html#client.ayon_core.ui.components.table_view.AYTableView.setModel","title":"<code>setModel(model)</code>","text":"<p>Set the data model and configure header and widgets.</p> <p>Parameters:</p> Name Type Description Default <code>model</code> <code>QAbstractItemModel | None</code> <p>The data model to display.</p> required Source code in <code>client/ayon_core/ui/components/table_view.py</code> <pre><code>def setModel(self, model: QtCore.QAbstractItemModel | None) -&gt; None:\n    \"\"\"Set the data model and configure header and widgets.\n\n    Args:\n        model: The data model to display.\n    \"\"\"\n    # Disconnect previous model signals.\n    for obj, conn in self._model_connections:\n        try:\n            obj.disconnect(conn)\n        except (RuntimeError, TypeError):\n            pass\n    self._model_connections.clear()\n\n    # Clear viewport-editor tracking \u2014 the old model's editors are\n    # invalidated by the model swap.\n    self._active_editor_pmis.clear()\n    self._editor_sync_timer.stop()\n\n    super().setModel(model)\n\n    if model is None:\n        self._widget_col_indices = []\n        return\n\n    # Cache widget-factory column indices for hover state updates.\n    source_mdl: Any = model\n    if isinstance(model, QAbstractProxyModel):\n        source_mdl = model.sourceModel()\n    if isinstance(source_mdl, PaginatedTableModel):\n        self._widget_col_indices = [\n            i\n            for i, col in enumerate(source_mdl.columns)\n            if col.widget_factory is not None\n        ]\n    else:\n        self._widget_col_indices = []\n\n    # Configure header from column width hints.\n    self._configure_header(model)\n\n    # Schedule viewport-aware editor opening for existing rows.\n    self._schedule_editor_sync()\n\n    # Connect to rowsInserted for lazy-loaded rows.\n    conn = model.rowsInserted.connect(self._on_rows_inserted)\n    if conn is not None:\n        self._model_connections.append((model, conn))\n\n    # Clear editor tracking on model reset so stale PMIs don't linger.\n    conn_reset = model.modelReset.connect(self._on_model_reset)\n    if conn_reset is not None:\n        self._model_connections.append((model, conn_reset))\n\n    # Connect to the source PaginatedTableModel for tree mode changes.\n    source: Any = model\n    if isinstance(model, QAbstractProxyModel):\n        source = model.sourceModel()\n    if isinstance(source, PaginatedTableModel):\n        conn2 = source.tree_mode_changed.connect(self._apply_tree_mode)\n        if conn2 is not None:\n            self._model_connections.append((source, conn2))\n        self._apply_tree_mode(source._tree_mode)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/table_view.html#client.ayon_core.ui.components.table_view.TableItemDelegate","title":"<code>TableItemDelegate</code>","text":"<p>               Bases: <code>StyleMixin</code>, <code>QStyledItemDelegate</code></p> <p>Item delegate for AYTableView that paints cells directly, bypassing QSS.</p> <p>Reads style data from the AYTableView style entry to draw cell backgrounds (hover, selected) and text/icons.</p> <p>Columns that carry a <code>widget_factory</code> on their :class:<code>TableColumn</code> definition get a persistent editor via :meth:<code>createEditor</code>.  Qt calls :meth:<code>setEditorData</code> both when the editor is first opened and automatically whenever the model emits <code>dataChanged</code> for that index, so server-push updates reach live widgets without extra wiring. User edits are written back via :meth:<code>setModelData</code>.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <code>QWidget | None</code> <p>The parent widget (expected to be an AYTableView instance).</p> <code>None</code> <code>style_model</code> <code>StyleData | None</code> <p>StyleData instance providing colour/dimension data.</p> <code>None</code> <code>variant</code> <code>str</code> <p>The variant string used to look up the correct style.</p> <code>'default'</code> Source code in <code>client/ayon_core/ui/components/table_view.py</code> <pre><code>class TableItemDelegate(StyleMixin, QtWidgets.QStyledItemDelegate):\n    \"\"\"Item delegate for AYTableView that paints cells directly,\n    bypassing QSS.\n\n    Reads style data from the AYTableView style entry to draw cell\n    backgrounds (hover, selected) and text/icons.\n\n    Columns that carry a ``widget_factory`` on their :class:`TableColumn`\n    definition get a persistent editor via :meth:`createEditor`.  Qt calls\n    :meth:`setEditorData` both when the editor is first opened and\n    automatically whenever the model emits ``dataChanged`` for that index,\n    so server-push updates reach live widgets without extra wiring.\n    User edits are written back via :meth:`setModelData`.\n\n    Args:\n        parent: The parent widget (expected to be an AYTableView instance).\n        style_model: StyleData instance providing colour/dimension data.\n        variant: The variant string used to look up the correct style.\n    \"\"\"\n\n    def __init__(\n        self,\n        parent: QWidget | None = None,\n        style_model: StyleData | None = None,\n        variant: str = \"default\",\n    ) -&gt; None:\n        super().__init__(parent)\n        self._style_model = style_model\n        self._variant_str = variant\n\n    def _table_styles(self) -&gt; dict[str, dict]:\n        \"\"\"Return base, hover and selected style dicts at once.\"\"\"\n        if self._style_model is None:\n            raise ValueError(\"TableItemDelegate requires a style model\")\n        return self._style_model.get_styles(\n            \"AYTableView\",\n            self._variant_str,\n            [\"base\", \"hover\", \"selected\"],\n        )\n\n    def sizeHint(\n        self,\n        option: QtWidgets.QStyleOptionViewItem,\n        index: QtCore.QModelIndex | QtCore.QPersistentModelIndex,\n    ) -&gt; QtCore.QSize:\n        \"\"\"Return a fixed row height from the style data.\"\"\"\n        if self._style_model:\n            style = self._style_model.get_style(\n                \"AYTableView\", self._variant_str\n            )\n            h = int(style.get(\"item-height\", 32))\n        else:\n            h = 32\n        return QtCore.QSize(option.rect.width(), h)\n\n    def createEditor(\n        self,\n        parent: QWidget,\n        option: QStyleOptionViewItem,\n        index: QtCore.QModelIndex | QtCore.QPersistentModelIndex,\n    ) -&gt; QWidget | None:\n        \"\"\"Return a widget for widget-factory columns; None otherwise.\n\n        The returned widget is kept open permanently by the view via\n        ``openPersistentEditor``.  Qt calls :meth:`setEditorData` once\n        here and again automatically on every ``dataChanged`` emission\n        for this index, so server-push updates reach the widget for free.\n\n        Args:\n            parent: Parent widget (viewport).\n            option: Style option for the cell.\n            index: Model index identifying the cell.\n\n        Returns:\n            A QWidget created by the column's ``widget_factory``, or\n            ``None`` if the column has no factory.\n        \"\"\"\n        from ..components.table_model import PaginatedTableModel\n\n        src_model = index.model()\n        if hasattr(src_model, \"sourceModel\"):\n            src_model = src_model.sourceModel()\n        if not isinstance(src_model, PaginatedTableModel):\n            return None\n        col = index.column()\n        cols = src_model.columns\n        if col &lt; 0 or col &gt;= len(cols):\n            return None\n        factory = cols[col].widget_factory\n        if factory is None:\n            return None\n        return factory(index, parent)  # type: ignore[return-value]\n\n    def setEditorData(\n        self,\n        editor: QWidget,\n        index: QtCore.QModelIndex | QtCore.QPersistentModelIndex,\n    ) -&gt; None:\n        \"\"\"Push current model data into *editor* when the model changes.\n\n        Called by Qt when the persistent editor is first opened and\n        automatically whenever the model emits ``dataChanged`` for this\n        index \u2014 server-push updates propagate to live widgets for free.\n\n        This default implementation is a no-op suited to action widgets\n        (e.g. buttons) that do not reflect model data.  Override or\n        replace this method for data-reflecting widgets: read\n        ``index.data(Qt.DisplayRole)`` (or a custom role) and push the\n        value into *editor*.\n\n        Args:\n            editor: The persistent editor widget.\n            index: Model index whose data changed.\n        \"\"\"\n\n    def setModelData(\n        self,\n        editor: QWidget,\n        model: QtCore.QAbstractItemModel,\n        index: QtCore.QModelIndex | QtCore.QPersistentModelIndex,\n    ) -&gt; None:\n        \"\"\"Write committed user input from *editor* back to *model*.\n\n        Called by Qt when the user commits an edit (e.g. presses Enter\n        or the editor loses focus).\n\n        This default implementation is a no-op suited to action widgets\n        that do not write back to the model.  For interactive widget\n        columns, read the current value from *editor* and call\n        ``model.setData(index, value, Qt.EditRole)`` to propagate the\n        change upstream (e.g. to the server).\n\n        Args:\n            editor: The persistent editor widget.\n            model: The data model.\n            index: Model index to write to.\n        \"\"\"\n\n    def paint(\n        self,\n        painter: QPainter,\n        option: QStyleOptionViewItem,\n        index: QtCore.QModelIndex | QtCore.QPersistentModelIndex,\n    ) -&gt; None:\n        \"\"\"Paint a table cell directly, bypassing QStyle.\"\"\"\n        # Skip painting for cells covered by a persistent editor widget.\n        from ..components.table_model import PaginatedTableModel\n\n        src_model = index.model()\n        if hasattr(src_model, \"sourceModel\"):\n            src_model = src_model.sourceModel()\n        if isinstance(src_model, PaginatedTableModel):\n            col = index.column()\n            cols = src_model.columns\n            if 0 &lt;= col &lt; len(cols) and cols[col].widget_factory is not None:\n                return\n\n        painter.save()\n        painter.setRenderHint(QPainter.RenderHint.Antialiasing)\n\n        opt = QStyleOptionViewItem(option)\n        self.initStyleOption(opt, index)\n\n        state = opt.state\n        is_selected = bool(state &amp; QStyle.StateFlag.State_Selected)\n        is_hovered = bool(state &amp; QStyle.StateFlag.State_MouseOver)\n        # State_MouseOver is only set for the cell directly under the cursor.\n        # When hovering the branch-indicator column, other cells in the same\n        # row don't receive it.  Fall back to a y-coordinate check so the\n        # entire row highlights consistently.\n        if not is_hovered and not is_selected:\n            _view = self.parent()\n            if type(_view).__name__ == \"AYTableView\" and hasattr(\n                _view, \"viewport\"\n            ):\n                _cursor = _view.viewport().mapFromGlobal(QtGui.QCursor.pos())\n                is_hovered = opt.rect.top() &lt;= _cursor.y() &lt; opt.rect.bottom()\n        is_item = not bool(state &amp; QStyle.StateFlag.State_Children)\n\n        styles = self._table_styles()\n        base_style = styles[\"base\"]\n        hover_style = styles[\"hover\"]\n        selected_style = styles[\"selected\"]\n\n        item_padding = base_style.get(\"item-padding\", [4, 8])\n        icon_text_spacing = int(base_style.get(\"icon-text-spacing\", 6))\n\n        # --- background ---\n        if is_selected:\n            bg_color = QColor(\n                selected_style.get(\n                    \"background-color\",\n                    base_style.get(\"background-color\", \"transparent\"),\n                )\n            )\n        elif is_hovered:\n            bg_color = QColor(\n                hover_style.get(\n                    \"background-color\",\n                    base_style.get(\"background-color\", \"transparent\"),\n                )\n            )\n        else:\n            bg_color = QColor(\n                base_style.get(\n                    \"background-color-item\" if is_item else \"background-color\",\n                    \"transparent\",\n                )\n            )\n\n        painter.setBrush(QBrush(bg_color))\n\n        pen_width = base_style.get(\"border-width\", 0)\n        if pen_width &gt; 0:\n            pen_color = QColor(base_style.get(\"border-color\", \"#000000\"))\n            pen = QPen(pen_color)\n            pen.setWidth(pen_width)\n            painter.setPen(pen)\n        else:\n            painter.setPen(Qt.PenStyle.NoPen)\n        column = index.column()\n        if column == 1:\n            painter.fillRect(opt.rect, bg_color)\n            painter.drawPolyline(\n                [\n                    opt.rect.topLeft(),\n                    opt.rect.topRight(),\n                    opt.rect.bottomRight(),\n                    opt.rect.bottomLeft(),\n                ]\n            )\n        else:\n            painter.drawRect(opt.rect)\n\n        # --- text colour ---\n        index_color = index.data(role=Qt.ItemDataRole.ForegroundRole)\n        if is_selected:\n            text_color = (\n                index_color.color()\n                if index_color\n                else QColor(base_style.get(\"color\", \"#f4f5f5\"))\n            )\n        else:\n            text_color = (\n                index_color.color()\n                if index_color\n                else QColor(base_style.get(\"color\", \"#f4f5f5\"))\n            )\n\n        # disabled dimming\n        if not (state &amp; QStyle.StateFlag.State_Enabled):\n            text_color.setAlpha(\n                int(\n                    text_color.alpha()\n                    * base_style.get(\"disabled-opacity\", 0.5)\n                )\n            )\n\n        # --- icon + text layout ---\n        content_rect = QRect(opt.rect).adjusted(\n            item_padding[1],\n            item_padding[0],\n            -item_padding[1],\n            -item_padding[0],\n        )\n        content_left = content_rect.left()\n\n        if not opt.icon.isNull():\n            icon_size = opt.decorationSize\n            icon_rect = QRect(\n                content_left,\n                opt.rect.center().y() - icon_size.height() // 2,\n                icon_size.width(),\n                icon_size.height(),\n            )\n            mode = (\n                QIcon.Mode.Normal\n                if state &amp; QStyle.StateFlag.State_Enabled\n                else QIcon.Mode.Disabled\n            )\n            opt.icon.paint(\n                painter,\n                icon_rect,\n                Qt.AlignmentFlag.AlignCenter,\n                mode,\n            )\n            content_left = icon_rect.right() + icon_text_spacing\n\n        if opt.text:\n            text_rect = QRect(opt.rect)\n            text_rect.setLeft(content_left)\n            text_rect.setRight(content_rect.right())\n            painter.setPen(text_color)\n            painter.setFont(self.font())\n            painter.drawText(\n                text_rect,\n                Qt.AlignmentFlag.AlignVCenter | Qt.AlignmentFlag.AlignLeft,\n                opt.text,\n            )\n\n        painter.restore()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/table_view.html#client.ayon_core.ui.components.table_view.TableItemDelegate.createEditor","title":"<code>createEditor(parent, option, index)</code>","text":"<p>Return a widget for widget-factory columns; None otherwise.</p> <p>The returned widget is kept open permanently by the view via <code>openPersistentEditor</code>.  Qt calls :meth:<code>setEditorData</code> once here and again automatically on every <code>dataChanged</code> emission for this index, so server-push updates reach the widget for free.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <code>QWidget</code> <p>Parent widget (viewport).</p> required <code>option</code> <code>QStyleOptionViewItem</code> <p>Style option for the cell.</p> required <code>index</code> <code>QModelIndex | QPersistentModelIndex</code> <p>Model index identifying the cell.</p> required <p>Returns:</p> Type Description <code>QWidget | None</code> <p>A QWidget created by the column's <code>widget_factory</code>, or</p> <code>QWidget | None</code> <p><code>None</code> if the column has no factory.</p> Source code in <code>client/ayon_core/ui/components/table_view.py</code> <pre><code>def createEditor(\n    self,\n    parent: QWidget,\n    option: QStyleOptionViewItem,\n    index: QtCore.QModelIndex | QtCore.QPersistentModelIndex,\n) -&gt; QWidget | None:\n    \"\"\"Return a widget for widget-factory columns; None otherwise.\n\n    The returned widget is kept open permanently by the view via\n    ``openPersistentEditor``.  Qt calls :meth:`setEditorData` once\n    here and again automatically on every ``dataChanged`` emission\n    for this index, so server-push updates reach the widget for free.\n\n    Args:\n        parent: Parent widget (viewport).\n        option: Style option for the cell.\n        index: Model index identifying the cell.\n\n    Returns:\n        A QWidget created by the column's ``widget_factory``, or\n        ``None`` if the column has no factory.\n    \"\"\"\n    from ..components.table_model import PaginatedTableModel\n\n    src_model = index.model()\n    if hasattr(src_model, \"sourceModel\"):\n        src_model = src_model.sourceModel()\n    if not isinstance(src_model, PaginatedTableModel):\n        return None\n    col = index.column()\n    cols = src_model.columns\n    if col &lt; 0 or col &gt;= len(cols):\n        return None\n    factory = cols[col].widget_factory\n    if factory is None:\n        return None\n    return factory(index, parent)  # type: ignore[return-value]\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/table_view.html#client.ayon_core.ui.components.table_view.TableItemDelegate.paint","title":"<code>paint(painter, option, index)</code>","text":"<p>Paint a table cell directly, bypassing QStyle.</p> Source code in <code>client/ayon_core/ui/components/table_view.py</code> <pre><code>def paint(\n    self,\n    painter: QPainter,\n    option: QStyleOptionViewItem,\n    index: QtCore.QModelIndex | QtCore.QPersistentModelIndex,\n) -&gt; None:\n    \"\"\"Paint a table cell directly, bypassing QStyle.\"\"\"\n    # Skip painting for cells covered by a persistent editor widget.\n    from ..components.table_model import PaginatedTableModel\n\n    src_model = index.model()\n    if hasattr(src_model, \"sourceModel\"):\n        src_model = src_model.sourceModel()\n    if isinstance(src_model, PaginatedTableModel):\n        col = index.column()\n        cols = src_model.columns\n        if 0 &lt;= col &lt; len(cols) and cols[col].widget_factory is not None:\n            return\n\n    painter.save()\n    painter.setRenderHint(QPainter.RenderHint.Antialiasing)\n\n    opt = QStyleOptionViewItem(option)\n    self.initStyleOption(opt, index)\n\n    state = opt.state\n    is_selected = bool(state &amp; QStyle.StateFlag.State_Selected)\n    is_hovered = bool(state &amp; QStyle.StateFlag.State_MouseOver)\n    # State_MouseOver is only set for the cell directly under the cursor.\n    # When hovering the branch-indicator column, other cells in the same\n    # row don't receive it.  Fall back to a y-coordinate check so the\n    # entire row highlights consistently.\n    if not is_hovered and not is_selected:\n        _view = self.parent()\n        if type(_view).__name__ == \"AYTableView\" and hasattr(\n            _view, \"viewport\"\n        ):\n            _cursor = _view.viewport().mapFromGlobal(QtGui.QCursor.pos())\n            is_hovered = opt.rect.top() &lt;= _cursor.y() &lt; opt.rect.bottom()\n    is_item = not bool(state &amp; QStyle.StateFlag.State_Children)\n\n    styles = self._table_styles()\n    base_style = styles[\"base\"]\n    hover_style = styles[\"hover\"]\n    selected_style = styles[\"selected\"]\n\n    item_padding = base_style.get(\"item-padding\", [4, 8])\n    icon_text_spacing = int(base_style.get(\"icon-text-spacing\", 6))\n\n    # --- background ---\n    if is_selected:\n        bg_color = QColor(\n            selected_style.get(\n                \"background-color\",\n                base_style.get(\"background-color\", \"transparent\"),\n            )\n        )\n    elif is_hovered:\n        bg_color = QColor(\n            hover_style.get(\n                \"background-color\",\n                base_style.get(\"background-color\", \"transparent\"),\n            )\n        )\n    else:\n        bg_color = QColor(\n            base_style.get(\n                \"background-color-item\" if is_item else \"background-color\",\n                \"transparent\",\n            )\n        )\n\n    painter.setBrush(QBrush(bg_color))\n\n    pen_width = base_style.get(\"border-width\", 0)\n    if pen_width &gt; 0:\n        pen_color = QColor(base_style.get(\"border-color\", \"#000000\"))\n        pen = QPen(pen_color)\n        pen.setWidth(pen_width)\n        painter.setPen(pen)\n    else:\n        painter.setPen(Qt.PenStyle.NoPen)\n    column = index.column()\n    if column == 1:\n        painter.fillRect(opt.rect, bg_color)\n        painter.drawPolyline(\n            [\n                opt.rect.topLeft(),\n                opt.rect.topRight(),\n                opt.rect.bottomRight(),\n                opt.rect.bottomLeft(),\n            ]\n        )\n    else:\n        painter.drawRect(opt.rect)\n\n    # --- text colour ---\n    index_color = index.data(role=Qt.ItemDataRole.ForegroundRole)\n    if is_selected:\n        text_color = (\n            index_color.color()\n            if index_color\n            else QColor(base_style.get(\"color\", \"#f4f5f5\"))\n        )\n    else:\n        text_color = (\n            index_color.color()\n            if index_color\n            else QColor(base_style.get(\"color\", \"#f4f5f5\"))\n        )\n\n    # disabled dimming\n    if not (state &amp; QStyle.StateFlag.State_Enabled):\n        text_color.setAlpha(\n            int(\n                text_color.alpha()\n                * base_style.get(\"disabled-opacity\", 0.5)\n            )\n        )\n\n    # --- icon + text layout ---\n    content_rect = QRect(opt.rect).adjusted(\n        item_padding[1],\n        item_padding[0],\n        -item_padding[1],\n        -item_padding[0],\n    )\n    content_left = content_rect.left()\n\n    if not opt.icon.isNull():\n        icon_size = opt.decorationSize\n        icon_rect = QRect(\n            content_left,\n            opt.rect.center().y() - icon_size.height() // 2,\n            icon_size.width(),\n            icon_size.height(),\n        )\n        mode = (\n            QIcon.Mode.Normal\n            if state &amp; QStyle.StateFlag.State_Enabled\n            else QIcon.Mode.Disabled\n        )\n        opt.icon.paint(\n            painter,\n            icon_rect,\n            Qt.AlignmentFlag.AlignCenter,\n            mode,\n        )\n        content_left = icon_rect.right() + icon_text_spacing\n\n    if opt.text:\n        text_rect = QRect(opt.rect)\n        text_rect.setLeft(content_left)\n        text_rect.setRight(content_rect.right())\n        painter.setPen(text_color)\n        painter.setFont(self.font())\n        painter.drawText(\n            text_rect,\n            Qt.AlignmentFlag.AlignVCenter | Qt.AlignmentFlag.AlignLeft,\n            opt.text,\n        )\n\n    painter.restore()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/table_view.html#client.ayon_core.ui.components.table_view.TableItemDelegate.setEditorData","title":"<code>setEditorData(editor, index)</code>","text":"<p>Push current model data into editor when the model changes.</p> <p>Called by Qt when the persistent editor is first opened and automatically whenever the model emits <code>dataChanged</code> for this index \u2014 server-push updates propagate to live widgets for free.</p> <p>This default implementation is a no-op suited to action widgets (e.g. buttons) that do not reflect model data.  Override or replace this method for data-reflecting widgets: read <code>index.data(Qt.DisplayRole)</code> (or a custom role) and push the value into editor.</p> <p>Parameters:</p> Name Type Description Default <code>editor</code> <code>QWidget</code> <p>The persistent editor widget.</p> required <code>index</code> <code>QModelIndex | QPersistentModelIndex</code> <p>Model index whose data changed.</p> required Source code in <code>client/ayon_core/ui/components/table_view.py</code> <pre><code>def setEditorData(\n    self,\n    editor: QWidget,\n    index: QtCore.QModelIndex | QtCore.QPersistentModelIndex,\n) -&gt; None:\n    \"\"\"Push current model data into *editor* when the model changes.\n\n    Called by Qt when the persistent editor is first opened and\n    automatically whenever the model emits ``dataChanged`` for this\n    index \u2014 server-push updates propagate to live widgets for free.\n\n    This default implementation is a no-op suited to action widgets\n    (e.g. buttons) that do not reflect model data.  Override or\n    replace this method for data-reflecting widgets: read\n    ``index.data(Qt.DisplayRole)`` (or a custom role) and push the\n    value into *editor*.\n\n    Args:\n        editor: The persistent editor widget.\n        index: Model index whose data changed.\n    \"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/table_view.html#client.ayon_core.ui.components.table_view.TableItemDelegate.setModelData","title":"<code>setModelData(editor, model, index)</code>","text":"<p>Write committed user input from editor back to model.</p> <p>Called by Qt when the user commits an edit (e.g. presses Enter or the editor loses focus).</p> <p>This default implementation is a no-op suited to action widgets that do not write back to the model.  For interactive widget columns, read the current value from editor and call <code>model.setData(index, value, Qt.EditRole)</code> to propagate the change upstream (e.g. to the server).</p> <p>Parameters:</p> Name Type Description Default <code>editor</code> <code>QWidget</code> <p>The persistent editor widget.</p> required <code>model</code> <code>QAbstractItemModel</code> <p>The data model.</p> required <code>index</code> <code>QModelIndex | QPersistentModelIndex</code> <p>Model index to write to.</p> required Source code in <code>client/ayon_core/ui/components/table_view.py</code> <pre><code>def setModelData(\n    self,\n    editor: QWidget,\n    model: QtCore.QAbstractItemModel,\n    index: QtCore.QModelIndex | QtCore.QPersistentModelIndex,\n) -&gt; None:\n    \"\"\"Write committed user input from *editor* back to *model*.\n\n    Called by Qt when the user commits an edit (e.g. presses Enter\n    or the editor loses focus).\n\n    This default implementation is a no-op suited to action widgets\n    that do not write back to the model.  For interactive widget\n    columns, read the current value from *editor* and call\n    ``model.setData(index, value, Qt.EditRole)`` to propagate the\n    change upstream (e.g. to the server).\n\n    Args:\n        editor: The persistent editor widget.\n        model: The data model.\n        index: Model index to write to.\n    \"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/table_view.html#client.ayon_core.ui.components.table_view.TableItemDelegate.sizeHint","title":"<code>sizeHint(option, index)</code>","text":"<p>Return a fixed row height from the style data.</p> Source code in <code>client/ayon_core/ui/components/table_view.py</code> <pre><code>def sizeHint(\n    self,\n    option: QtWidgets.QStyleOptionViewItem,\n    index: QtCore.QModelIndex | QtCore.QPersistentModelIndex,\n) -&gt; QtCore.QSize:\n    \"\"\"Return a fixed row height from the style data.\"\"\"\n    if self._style_model:\n        style = self._style_model.get_style(\n            \"AYTableView\", self._variant_str\n        )\n        h = int(style.get(\"item-height\", 32))\n    else:\n        h = 32\n    return QtCore.QSize(option.rect.width(), h)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/tag.html","title":"tag","text":""},{"location":"autoapi/client/ayon_core/ui/components/tag.html#client.ayon_core.ui.components.tag.AYTag","title":"<code>AYTag</code>","text":"<p>               Bases: <code>AYFrame</code></p> <p>A widget used to display a tag as a pill with a color and text, a delete and an expand button.</p> Source code in <code>client/ayon_core/ui/components/tag.py</code> <pre><code>class AYTag(AYFrame):\n    \"\"\"A widget used to display a tag as a pill with a color and text,\n    a delete and an expand button.\"\"\"\n\n    tag_removed = QtCore.Signal(str)\n    tag_expanded = QtCore.Signal(str)\n\n    def __init__(\n        self,\n        name: str,\n        color: QtGui.QColor,\n        label: str | None = None,\n        parent=None,\n    ):\n        super().__init__(parent, variant=QFrameVariants.Tag, margin=0)\n        self._tag_name = name\n        self._tag_label = label\n        self._bg_color = color\n        self._fg_color = None\n        self.setStyle(get_ayon_style())\n        self.init_ui()\n\n    @property\n    def background_color(self) -&gt; QtGui.QColor:\n        return self._bg_color\n\n    @property\n    def foreground_color(self) -&gt; QtGui.QColor:\n        if self._fg_color is None:\n            self._fg_color = compute_color_for_contrast(\n                self._bg_color.toTuple(),\n                (0, 0, 0, 255),\n            )\n        return self._fg_color\n\n    def init_ui(self):\n        self.lyt = QtWidgets.QHBoxLayout(self)\n        self.lyt.setContentsMargins(2, 2, 2, 2)\n        self.lyt.setSpacing(4)\n        self.lyt.setSizeConstraint(\n            QtWidgets.QLayout.SizeConstraint.SetMinimumSize\n        )\n\n        self.delete_button = AYButton(\n            icon=\"close\",\n            icon_color=self.foreground_color.name(),\n            icon_size=16,\n            variant=AYButton.Variants.Tag,\n            contrast_color=self.background_color,\n            tooltip=f\"Remove tag {self._tag_name!r}\",\n        )\n\n        self.label = AYLabel(\n            self._tag_label or self._tag_name,\n            variant=AYLabel.Variants.Tag,\n            text_color=self.foreground_color.name(),\n            contrast_color=self.background_color,\n        )\n        self.label.setSizePolicy(\n            QtWidgets.QSizePolicy.Policy.Minimum,\n            QtWidgets.QSizePolicy.Policy.Minimum,\n        )\n\n        self.expand_button = AYButton(\n            icon=\"arrow_drop_down\",\n            icon_color=self.foreground_color.name(),\n            icon_size=16,\n            variant=AYButton.Variants.Tag,\n            contrast_color=self.background_color,\n            tooltip=f\"Expand tag {self._tag_name!r}\",\n        )\n\n        self.lyt.addWidget(self.delete_button)\n        self.lyt.addWidget(self.label)\n        self.lyt.addWidget(self.expand_button)\n\n        self.delete_button.clicked.connect(self._delete_tag)\n        self.expand_button.clicked.connect(self._expand_tag)\n\n    def _delete_tag(self):\n        self.tag_removed.emit(self._tag_name)\n\n    def _expand_tag(self):\n        self.tag_expanded.emit(self._tag_name)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/tag_selector.html","title":"tag_selector","text":"<p>Tag selector component for displaying and managing tags.</p> <p>This module provides an AYTagSelector widget that displays a button with selected tags as badges, and opens a floating dropdown for tag selection.</p>"},{"location":"autoapi/client/ayon_core/ui/components/tag_selector.html#client.ayon_core.ui.components.tag_selector.AYTagSelector","title":"<code>AYTagSelector</code>","text":"<p>               Bases: <code>StyleMixin</code>, <code>QWidget</code></p> <p>A tag selector widget with button and floating dropdown.</p> <p>Displays a button with a tag icon. When tags are selected, they appear as badges next to the button. Clicking the button opens a floating dropdown for tag selection.</p> <p>Attributes:</p> Name Type Description <code>tags_changed</code> <p>Signal emitted when selected tags change.</p> Source code in <code>client/ayon_core/ui/components/tag_selector.py</code> <pre><code>class AYTagSelector(StyleMixin, QWidget):\n    \"\"\"A tag selector widget with button and floating dropdown.\n\n    Displays a button with a tag icon. When tags are selected, they appear\n    as badges next to the button. Clicking the button opens a floating\n    dropdown for tag selection.\n\n    Attributes:\n        tags_changed: Signal emitted when selected tags change.\n    \"\"\"\n\n    tags_changed = Signal(list)\n\n    def __init__(\n        self,\n        available_tags: list[TagData] | list[dict] | list[str] | None = None,\n        selected_tags: list[str] | None = None,\n        max_visible_tags: int = 4,\n        min_width: int = 200,\n        parent: QWidget | None = None,\n    ):\n        \"\"\"Initialize the tag selector.\n\n        Args:\n            available_tags: List of available tags. Can be TagData objects,\n                dicts with 'name' and 'color' keys, or simple strings.\n            selected_tags: List of initially selected tag names.\n            parent: Optional parent widget.\n        \"\"\"\n        super().__init__(parent)\n        self.setStyle(get_ayon_style())\n\n        self._available_tags = self._normalize_tags(available_tags or [])\n        self._selected_tags: list[str] = list(selected_tags or [])\n        self._dropdown: TagDropdown | None = None\n        self._max_visible_tags = max_visible_tags\n        self._min_width = min_width\n\n        self.setSizePolicy(\n            QSizePolicy.Policy.Preferred, QSizePolicy.Policy.Fixed\n        )\n\n        self._build()\n        self._update_badges()\n\n    @staticmethod\n    def _normalize_tags(\n        tags: list[TagData] | list[dict] | list[str],\n    ) -&gt; list[TagData]:\n        \"\"\"Normalize various tag input formats to TagData objects.\n\n        Args:\n            tags: List of tags in any supported format.\n\n        Returns:\n            List of TagData objects.\n        \"\"\"\n        normalized: list[TagData] = []\n        for tag in tags:\n            if isinstance(tag, TagData):\n                normalized.append(tag)\n            elif isinstance(tag, dict):\n                name = tag.get(\"name\") or tag.get(\"text\") or str(tag)\n                color = tag.get(\"color\", \"#8b9198\")\n                normalized.append(TagData(name=name, color=color))\n            else:\n                normalized.append(TagData(name=str(tag)))\n        return sorted(normalized, key=lambda t: t.name.lower())\n\n    def _build(self) -&gt; None:\n        \"\"\"Build the main widget UI layout.\"\"\"\n        self._main_layout = AYHBoxLayout(margin=0, spacing=4)\n        self.setLayout(self._main_layout)\n\n        # Main button with tag icon\n        self._button = AYButton(\n            icon=\"sell\",\n            variant=AYButton.Variants.Nav,\n            tooltip=\"Select tags\",\n        )\n        self._button.clicked.connect(self._show_dropdown)\n        self._main_layout.addWidget(self._button)\n\n        # Container for selected tag badges\n        self._badges_container = AYFrame(variant=AYFrame.Variants.Low)\n        self._badges_layout = AYHBoxLayout(margin=0, spacing=4)\n        self._badges_container.setLayout(self._badges_layout)\n        self._main_layout.addWidget(self._badges_container)\n\n        self._main_layout.addStretch()\n\n    def _update_badges(self) -&gt; None:\n        \"\"\"Update the display of selected tag badges.\"\"\"\n        # Clear existing badges\n        while self._badges_layout.count() &gt; 0:\n            item = self._badges_layout.takeAt(0)\n            widget = item.widget()\n            if widget is not None:\n                widget.deleteLater()\n\n        # Create badges for selected tags\n        tag_color_map = {tag.name: tag.color for tag in self._available_tags}\n        for i, tag_name in enumerate(self._selected_tags):\n            if i &gt;= self._max_visible_tags:\n                remaining_tags = (\n                    len(self._selected_tags) - self._max_visible_tags\n                )\n                badge = AYLabel(\n                    f\"+{remaining_tags}\",\n                    variant=AYLabel.Variants.Default,\n                    rel_text_size=-2,\n                )\n                self._badges_layout.addWidget(badge)\n                break\n            color = tag_color_map.get(tag_name, \"#8b9198\")\n            badge = AYLabel(\n                tag_name,\n                icon_color=color,\n                variant=AYLabel.Variants.Badge,\n                rel_text_size=-2,\n                contrast_color=QColor(color),\n            )\n            self._badges_layout.addWidget(badge)\n\n    def _show_dropdown(self) -&gt; None:\n        \"\"\"Show the floating dropdown for tag selection.\"\"\"\n        if self._dropdown is not None:\n            self._dropdown.close()\n            self._dropdown = None\n            return\n\n        self._dropdown = TagDropdown(\n            tags=self._available_tags,\n            selected_tags=self._selected_tags,\n            min_width=self._min_width,\n            parent=None,\n        )\n        self._dropdown.tag_toggled.connect(self._on_tag_toggled)\n        self._dropdown.add_new_tag.connect(self._on_add_new_tag)\n        self._dropdown.closed.connect(self._on_dropdown_closed)\n\n        # Position dropdown below the button using helper from AYDropdownPopup\n        self._dropdown.show_below(self._button)\n\n    def _on_tag_toggled(self, tag_name: str, selected: bool) -&gt; None:\n        \"\"\"Handle tag toggle from dropdown.\n\n        Args:\n            tag_name: The name of the toggled tag.\n            selected: Whether the tag is now selected.\n        \"\"\"\n        if selected:\n            if tag_name not in self._selected_tags:\n                self._selected_tags.append(tag_name)\n        else:\n            if tag_name in self._selected_tags:\n                self._selected_tags.remove(tag_name)\n\n        self._update_badges()\n        self.tags_changed.emit(self._selected_tags.copy())\n\n    def _on_add_new_tag(self, tag_name: str) -&gt; None:\n        \"\"\"Handle adding a new tag.\n\n        Args:\n            tag_name: The name of the new tag to add.\n        \"\"\"\n        # Check if tag already exists\n        existing_names = {tag.name.lower() for tag in self._available_tags}\n        if tag_name.lower() in existing_names:\n            logger.warning(\"Tag '%s' already exists\", tag_name)\n            return\n\n        # Add new tag with default color\n        new_tag = TagData(name=tag_name, color=\"#8b9198\")\n        self._available_tags.append(new_tag)\n        self._available_tags.sort(key=lambda t: t.name.lower())\n\n        # Select the new tag\n        if tag_name not in self._selected_tags:\n            self._selected_tags.append(tag_name)\n\n        self._update_badges()\n        self.tags_changed.emit(self._selected_tags.copy())\n\n        # Refresh dropdown if open\n        if self._dropdown:\n            self._dropdown.close()\n            self._show_dropdown()\n\n    def _on_dropdown_closed(self) -&gt; None:\n        \"\"\"Handle dropdown close event.\"\"\"\n        self._dropdown = None\n\n    def get_selected_tags(self) -&gt; list[str]:\n        \"\"\"Get the list of selected tag names.\n\n        Returns:\n            List of selected tag names.\n        \"\"\"\n        return self._selected_tags.copy()\n\n    def set_selected_tags(self, tags: list[str]) -&gt; None:\n        \"\"\"Set the selected tags.\n\n        Args:\n            tags: List of tag names to select.\n        \"\"\"\n        self._selected_tags = list(tags)\n        self._update_badges()\n        self.tags_changed.emit(self._selected_tags.copy())\n\n    def set_available_tags(\n        self,\n        tags: list[TagData] | list[dict] | list[str],\n    ) -&gt; None:\n        \"\"\"Set the available tags.\n\n        Args:\n            tags: List of tags in any supported format.\n        \"\"\"\n        self._available_tags = self._normalize_tags(tags)\n        # Remove selected tags that no longer exist\n        available_names = {tag.name for tag in self._available_tags}\n        self._selected_tags = [\n            t for t in self._selected_tags if t in available_names\n        ]\n        self._update_badges()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/tag_selector.html#client.ayon_core.ui.components.tag_selector.AYTagSelector.__init__","title":"<code>__init__(available_tags=None, selected_tags=None, max_visible_tags=4, min_width=200, parent=None)</code>","text":"<p>Initialize the tag selector.</p> <p>Parameters:</p> Name Type Description Default <code>available_tags</code> <code>list[TagData] | list[dict] | list[str] | None</code> <p>List of available tags. Can be TagData objects, dicts with 'name' and 'color' keys, or simple strings.</p> <code>None</code> <code>selected_tags</code> <code>list[str] | None</code> <p>List of initially selected tag names.</p> <code>None</code> <code>parent</code> <code>QWidget | None</code> <p>Optional parent widget.</p> <code>None</code> Source code in <code>client/ayon_core/ui/components/tag_selector.py</code> <pre><code>def __init__(\n    self,\n    available_tags: list[TagData] | list[dict] | list[str] | None = None,\n    selected_tags: list[str] | None = None,\n    max_visible_tags: int = 4,\n    min_width: int = 200,\n    parent: QWidget | None = None,\n):\n    \"\"\"Initialize the tag selector.\n\n    Args:\n        available_tags: List of available tags. Can be TagData objects,\n            dicts with 'name' and 'color' keys, or simple strings.\n        selected_tags: List of initially selected tag names.\n        parent: Optional parent widget.\n    \"\"\"\n    super().__init__(parent)\n    self.setStyle(get_ayon_style())\n\n    self._available_tags = self._normalize_tags(available_tags or [])\n    self._selected_tags: list[str] = list(selected_tags or [])\n    self._dropdown: TagDropdown | None = None\n    self._max_visible_tags = max_visible_tags\n    self._min_width = min_width\n\n    self.setSizePolicy(\n        QSizePolicy.Policy.Preferred, QSizePolicy.Policy.Fixed\n    )\n\n    self._build()\n    self._update_badges()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/tag_selector.html#client.ayon_core.ui.components.tag_selector.AYTagSelector.get_selected_tags","title":"<code>get_selected_tags()</code>","text":"<p>Get the list of selected tag names.</p> <p>Returns:</p> Type Description <code>list[str]</code> <p>List of selected tag names.</p> Source code in <code>client/ayon_core/ui/components/tag_selector.py</code> <pre><code>def get_selected_tags(self) -&gt; list[str]:\n    \"\"\"Get the list of selected tag names.\n\n    Returns:\n        List of selected tag names.\n    \"\"\"\n    return self._selected_tags.copy()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/tag_selector.html#client.ayon_core.ui.components.tag_selector.AYTagSelector.set_available_tags","title":"<code>set_available_tags(tags)</code>","text":"<p>Set the available tags.</p> <p>Parameters:</p> Name Type Description Default <code>tags</code> <code>list[TagData] | list[dict] | list[str]</code> <p>List of tags in any supported format.</p> required Source code in <code>client/ayon_core/ui/components/tag_selector.py</code> <pre><code>def set_available_tags(\n    self,\n    tags: list[TagData] | list[dict] | list[str],\n) -&gt; None:\n    \"\"\"Set the available tags.\n\n    Args:\n        tags: List of tags in any supported format.\n    \"\"\"\n    self._available_tags = self._normalize_tags(tags)\n    # Remove selected tags that no longer exist\n    available_names = {tag.name for tag in self._available_tags}\n    self._selected_tags = [\n        t for t in self._selected_tags if t in available_names\n    ]\n    self._update_badges()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/tag_selector.html#client.ayon_core.ui.components.tag_selector.AYTagSelector.set_selected_tags","title":"<code>set_selected_tags(tags)</code>","text":"<p>Set the selected tags.</p> <p>Parameters:</p> Name Type Description Default <code>tags</code> <code>list[str]</code> <p>List of tag names to select.</p> required Source code in <code>client/ayon_core/ui/components/tag_selector.py</code> <pre><code>def set_selected_tags(self, tags: list[str]) -&gt; None:\n    \"\"\"Set the selected tags.\n\n    Args:\n        tags: List of tag names to select.\n    \"\"\"\n    self._selected_tags = list(tags)\n    self._update_badges()\n    self.tags_changed.emit(self._selected_tags.copy())\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/tag_selector.html#client.ayon_core.ui.components.tag_selector.TagData","title":"<code>TagData</code>  <code>dataclass</code>","text":"<p>Data class representing a tag with name and color.</p> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>The display name of the tag.</p> <code>color</code> <code>str</code> <p>The hex color string for the tag (e.g., \"#4ECDC4\").</p> Source code in <code>client/ayon_core/ui/components/tag_selector.py</code> <pre><code>@dataclass\nclass TagData:\n    \"\"\"Data class representing a tag with name and color.\n\n    Attributes:\n        name: The display name of the tag.\n        color: The hex color string for the tag (e.g., \"#4ECDC4\").\n    \"\"\"\n\n    name: str\n    color: str = \"#8b9198\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/tag_selector.html#client.ayon_core.ui.components.tag_selector.TagDropdown","title":"<code>TagDropdown</code>","text":"<p>               Bases: <code>AYDropdownPopup</code></p> <p>A floating dropdown window for tag selection.</p> <p>Features a search field and a scrollable list of tags. New tags can be created by typing a non-existing tag name and pressing Enter.</p> <p>Attributes:</p> Name Type Description <code>tag_toggled</code> <p>Signal emitted when a tag is toggled (name, selected).</p> <code>add_new_tag</code> <p>Signal emitted when user wants to add a new tag (name).</p> <code>closed</code> <p>Signal emitted when the dropdown is closed.</p> Source code in <code>client/ayon_core/ui/components/tag_selector.py</code> <pre><code>class TagDropdown(AYDropdownPopup):\n    \"\"\"A floating dropdown window for tag selection.\n\n    Features a search field and a scrollable list of tags. New tags can be\n    created by typing a non-existing tag name and pressing Enter.\n\n    Attributes:\n        tag_toggled: Signal emitted when a tag is toggled (name, selected).\n        add_new_tag: Signal emitted when user wants to add a new tag (name).\n        closed: Signal emitted when the dropdown is closed.\n    \"\"\"\n\n    tag_toggled = Signal(str, bool)\n    add_new_tag = Signal(str)\n    closed = Signal()\n\n    def __init__(\n        self,\n        tags: list[TagData],\n        selected_tags: list[str],\n        min_width: int = 200,\n        parent: QWidget | None = None,\n    ):\n        \"\"\"Initialize the dropdown window.\n\n        Args:\n            tags: List of all available tags.\n            selected_tags: List of currently selected tag names.\n            parent: Optional parent widget.\n        \"\"\"\n        super().__init__(\n            parent=parent,\n            variant=AYFrame.Variants.Low_Framed,\n            translucent_bg=False,\n        )\n        # Internal container to hold the actual widgets and layout\n        self._content = AYVBoxLayout(self, margin=1)\n        self._tags = tags\n        self._selected_tags = set(selected_tags)\n        self._tag_widgets: dict[str, TagItemWidget] = {}\n\n        self.setMinimumWidth(min_width)\n        self.setMaximumHeight(400)\n\n        # Propagate popup closed signal to a local closed signal\n        self.popup_closed.connect(self.closed.emit)\n\n        self._build()\n\n    def _build(self) -&gt; None:\n        \"\"\"Build the dropdown UI layout.\"\"\"\n        self._filterable_list = FilterableList(\n            placeholder=\"Search tags...\",\n            parent=self,\n        )\n        search = self._filterable_list.search_field()\n        search.textChanged.connect(self._on_search_changed)\n        search.returnPressed.connect(self._on_enter_pressed)\n        self._content.addWidget(self._filterable_list)\n\n        # Populate tags\n        self._populate_tags()\n\n    def _populate_tags(self) -&gt; None:\n        \"\"\"Populate the tag list with tag item widgets.\"\"\"\n        if not self._tags:\n            tag = TagData(\"No available tags\", color=\"#ffffff\")\n            tag_widget = TagItemWidget(tag)\n            tag_widget.setEnabled(False)\n            self._tag_widgets[tag.name] = tag_widget\n            self._filterable_list.add_item(tag_widget)\n            return\n\n        for tag in self._tags:\n            selected = tag.name in self._selected_tags\n            tag_widget = TagItemWidget(tag, selected=selected)\n            tag_widget.tag_clicked.connect(self._on_tag_clicked)\n            self._tag_widgets[tag.name] = tag_widget\n            tag_name = tag.name\n            self._filterable_list.add_item(\n                tag_widget,\n                match_fn=lambda text, n=tag_name: (\n                    not text.lower().strip()\n                    or text.lower().strip() in n.lower()\n                ),\n            )\n\n        self._filterable_list.add_stretch()\n\n    def _on_search_changed(self, text: str) -&gt; None:\n        \"\"\"Filter tags based on search input.\n\n        Args:\n            text: The current search text.\n        \"\"\"\n        self._adjust_height()\n\n    def _on_enter_pressed(self) -&gt; None:\n        \"\"\"Handle Enter key press to create new tag if it doesn't exist.\"\"\"\n        text = self._filterable_list.search_field().text().strip()\n        if not text:\n            return\n\n        # Check if the tag already exists (case-insensitive)\n        existing_tag = None\n        for tag in self._tags:\n            if tag.name.lower() == text.lower():\n                existing_tag = tag\n                break\n\n        if existing_tag:\n            # Toggle the existing tag\n            widget = self._tag_widgets.get(existing_tag.name)\n            if widget:\n                new_state = not widget.isChecked()\n                widget.setChecked(new_state)\n                if new_state:\n                    self._selected_tags.add(existing_tag.name)\n                else:\n                    self._selected_tags.discard(existing_tag.name)\n                self.tag_toggled.emit(existing_tag.name, new_state)\n        else:\n            # Create new tag\n            self.add_new_tag.emit(text)\n\n        self._filterable_list.search_field().clear()\n\n    def _on_tag_clicked(self, tag_name: str) -&gt; None:\n        \"\"\"Handle tag item click to toggle selection.\n\n        Args:\n            tag_name: The name of the clicked tag.\n        \"\"\"\n        widget = self._tag_widgets.get(tag_name)\n        if widget:\n            new_state = widget.isChecked()\n            if new_state:\n                self._selected_tags.add(tag_name)\n            else:\n                self._selected_tags.discard(tag_name)\n            self.tag_toggled.emit(tag_name, new_state)\n\n    def focusOutEvent(self, event: QFocusEvent) -&gt; None:\n        \"\"\"Handle focus out to close dropdown.\"\"\"\n        # Check if focus is going to a child widget\n        focus_widget = QApplication.focusWidget()\n        if focus_widget and self.isAncestorOf(focus_widget):\n            return\n        self.close()\n        self.closed.emit()\n        super().focusOutEvent(event)\n\n    def get_selected_tags(self) -&gt; list[str]:\n        \"\"\"Return the list of currently selected tag names.\n\n        Returns:\n            List of selected tag names.\n        \"\"\"\n        return list(self._selected_tags)\n\n    def _calculate_content_height(self) -&gt; int:\n        \"\"\"Calculate the ideal height based on visible content.\"\"\"\n        search_height = 40  # search field + margins\n\n        tags_height = sum(\n            w.sizeHint().height()\n            for w in self._filterable_list.visible_items()\n        )\n\n        margin = 2  # layout_margin from __init__\n        return search_height + tags_height + (margin * 2)\n\n    def _adjust_height(self) -&gt; None:\n        \"\"\"Adjust dropdown height to fit content up to max height.\"\"\"\n        max_height = 400\n        content_height = self._calculate_content_height()\n        self.setFixedHeight(min(content_height, max_height))\n\n    def showEvent(self, event: QShowEvent) -&gt; None:\n        super().showEvent(event)\n        self._filterable_list.search_field().setFocus()\n        self._adjust_height()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/tag_selector.html#client.ayon_core.ui.components.tag_selector.TagDropdown.__init__","title":"<code>__init__(tags, selected_tags, min_width=200, parent=None)</code>","text":"<p>Initialize the dropdown window.</p> <p>Parameters:</p> Name Type Description Default <code>tags</code> <code>list[TagData]</code> <p>List of all available tags.</p> required <code>selected_tags</code> <code>list[str]</code> <p>List of currently selected tag names.</p> required <code>parent</code> <code>QWidget | None</code> <p>Optional parent widget.</p> <code>None</code> Source code in <code>client/ayon_core/ui/components/tag_selector.py</code> <pre><code>def __init__(\n    self,\n    tags: list[TagData],\n    selected_tags: list[str],\n    min_width: int = 200,\n    parent: QWidget | None = None,\n):\n    \"\"\"Initialize the dropdown window.\n\n    Args:\n        tags: List of all available tags.\n        selected_tags: List of currently selected tag names.\n        parent: Optional parent widget.\n    \"\"\"\n    super().__init__(\n        parent=parent,\n        variant=AYFrame.Variants.Low_Framed,\n        translucent_bg=False,\n    )\n    # Internal container to hold the actual widgets and layout\n    self._content = AYVBoxLayout(self, margin=1)\n    self._tags = tags\n    self._selected_tags = set(selected_tags)\n    self._tag_widgets: dict[str, TagItemWidget] = {}\n\n    self.setMinimumWidth(min_width)\n    self.setMaximumHeight(400)\n\n    # Propagate popup closed signal to a local closed signal\n    self.popup_closed.connect(self.closed.emit)\n\n    self._build()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/tag_selector.html#client.ayon_core.ui.components.tag_selector.TagDropdown.focusOutEvent","title":"<code>focusOutEvent(event)</code>","text":"<p>Handle focus out to close dropdown.</p> Source code in <code>client/ayon_core/ui/components/tag_selector.py</code> <pre><code>def focusOutEvent(self, event: QFocusEvent) -&gt; None:\n    \"\"\"Handle focus out to close dropdown.\"\"\"\n    # Check if focus is going to a child widget\n    focus_widget = QApplication.focusWidget()\n    if focus_widget and self.isAncestorOf(focus_widget):\n        return\n    self.close()\n    self.closed.emit()\n    super().focusOutEvent(event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/tag_selector.html#client.ayon_core.ui.components.tag_selector.TagDropdown.get_selected_tags","title":"<code>get_selected_tags()</code>","text":"<p>Return the list of currently selected tag names.</p> <p>Returns:</p> Type Description <code>list[str]</code> <p>List of selected tag names.</p> Source code in <code>client/ayon_core/ui/components/tag_selector.py</code> <pre><code>def get_selected_tags(self) -&gt; list[str]:\n    \"\"\"Return the list of currently selected tag names.\n\n    Returns:\n        List of selected tag names.\n    \"\"\"\n    return list(self._selected_tags)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/tag_selector.html#client.ayon_core.ui.components.tag_selector.TagItemWidget","title":"<code>TagItemWidget</code>","text":"<p>               Bases: <code>AYButton</code></p> <p>A clickable tag item widget used in the dropdown list.</p> <p>Displays a tag with its color and handles selection state. Inherits from AYButton to leverage built-in click handling, hover states, and checkable functionality.</p> <p>Attributes:</p> Name Type Description <code>tag_clicked</code> <p>Signal emitted when the tag item is clicked (tag name).</p> Source code in <code>client/ayon_core/ui/components/tag_selector.py</code> <pre><code>class TagItemWidget(AYButton):\n    \"\"\"A clickable tag item widget used in the dropdown list.\n\n    Displays a tag with its color and handles selection state.\n    Inherits from AYButton to leverage built-in click handling,\n    hover states, and checkable functionality.\n\n    Attributes:\n        tag_clicked: Signal emitted when the tag item is clicked (tag name).\n    \"\"\"\n\n    tag_clicked = Signal(str)\n\n    def __init__(\n        self,\n        tag: TagData,\n        selected: bool = False,\n        parent: QWidget | None = None,\n    ):\n        \"\"\"Initialize the tag item widget.\n\n        Args:\n            tag: The TagData object with name and color.\n            selected: Whether the tag is currently selected.\n            parent: Optional parent widget.\n        \"\"\"\n        self._tag = tag\n        super().__init__(\n            tag.name,\n            variant=AYButton.Variants.Tag_Menu,\n            fixed_width=False,\n            icon_color=tag.color,\n            checkable=True,\n            parent=parent,\n        )\n\n        self.setChecked(selected)\n        self.setSizePolicy(\n            QSizePolicy.Policy.Expanding, QSizePolicy.Policy.Fixed\n        )\n\n        # Connect the built-in clicked signal to emit tag name\n        self.clicked.connect(self._on_clicked)\n\n    def _on_clicked(self) -&gt; None:\n        \"\"\"Handle button click to emit tag_clicked signal with tag name.\"\"\"\n        self.tag_clicked.emit(self._tag.name)\n\n    @property\n    def tag(self) -&gt; TagData:\n        \"\"\"Return the tag data.\"\"\"\n        return self._tag\n\n    @property\n    def selected(self) -&gt; bool:\n        \"\"\"Return the selection state.\"\"\"\n        return self.isChecked()\n\n    @selected.setter\n    def selected(self, value: bool) -&gt; None:\n        \"\"\"Set the selection state.\"\"\"\n        self.setChecked(value)\n\n    @property\n    def tag_color(self) -&gt; str:\n        \"\"\"Return the tag color.\"\"\"\n        return self._tag.color\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/tag_selector.html#client.ayon_core.ui.components.tag_selector.TagItemWidget.selected","title":"<code>selected</code>  <code>property</code> <code>writable</code>","text":"<p>Return the selection state.</p>"},{"location":"autoapi/client/ayon_core/ui/components/tag_selector.html#client.ayon_core.ui.components.tag_selector.TagItemWidget.tag","title":"<code>tag</code>  <code>property</code>","text":"<p>Return the tag data.</p>"},{"location":"autoapi/client/ayon_core/ui/components/tag_selector.html#client.ayon_core.ui.components.tag_selector.TagItemWidget.tag_color","title":"<code>tag_color</code>  <code>property</code>","text":"<p>Return the tag color.</p>"},{"location":"autoapi/client/ayon_core/ui/components/tag_selector.html#client.ayon_core.ui.components.tag_selector.TagItemWidget.__init__","title":"<code>__init__(tag, selected=False, parent=None)</code>","text":"<p>Initialize the tag item widget.</p> <p>Parameters:</p> Name Type Description Default <code>tag</code> <code>TagData</code> <p>The TagData object with name and color.</p> required <code>selected</code> <code>bool</code> <p>Whether the tag is currently selected.</p> <code>False</code> <code>parent</code> <code>QWidget | None</code> <p>Optional parent widget.</p> <code>None</code> Source code in <code>client/ayon_core/ui/components/tag_selector.py</code> <pre><code>def __init__(\n    self,\n    tag: TagData,\n    selected: bool = False,\n    parent: QWidget | None = None,\n):\n    \"\"\"Initialize the tag item widget.\n\n    Args:\n        tag: The TagData object with name and color.\n        selected: Whether the tag is currently selected.\n        parent: Optional parent widget.\n    \"\"\"\n    self._tag = tag\n    super().__init__(\n        tag.name,\n        variant=AYButton.Variants.Tag_Menu,\n        fixed_width=False,\n        icon_color=tag.color,\n        checkable=True,\n        parent=parent,\n    )\n\n    self.setChecked(selected)\n    self.setSizePolicy(\n        QSizePolicy.Policy.Expanding, QSizePolicy.Policy.Fixed\n    )\n\n    # Connect the built-in clicked signal to emit tag name\n    self.clicked.connect(self._on_clicked)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/task_queue.html","title":"task_queue","text":"<p>Priority-based async task queue backed by a dispatch QThread + worker pool.</p> <p>This module provides a generic priority-based task queue system.  A single dispatch <code>QThread</code> dequeues tasks from a <code>PriorityQueue</code> and submits them to a <code>ThreadPoolExecutor</code>  (default: 4 workers) so that independent fetches \u2014 for instance all children of simultaneously-expanded tree nodes \u2014 run in parallel rather than serially.</p> <p>Key optimisations vs. the original single-worker design:</p> <ul> <li>No polling sleep - the dispatch loop blocks on a <code>threading.Event</code>   that is set by :meth:<code>AsyncTaskQueue.enqueue</code> the instant a new task   arrives, so there is zero idle wait between tree-expansion waves.</li> <li>Parallel execution - up to <code>num_workers</code> fetch tasks run at the   same time, halving the effective latency when expanding N sibling nodes   from O(N \u00d7 round-trip) to O(round-trip / num_workers \u00d7 N).</li> </ul> <p>Each task carries an optional <code>context_id</code> label. External components can call :meth:<code>AsyncTaskQueue.clear_context_tasks</code> to remove all pending tasks for a given context when the associated state becomes stale (e.g. a selection change).  Multiple distinct context IDs can coexist in the queue at the same time.</p>"},{"location":"autoapi/client/ayon_core/ui/components/task_queue.html#client.ayon_core.ui.components.task_queue.AsyncTask","title":"<code>AsyncTask</code>  <code>dataclass</code>","text":"<p>Represents a task to be executed by the async task queue.</p> <p>This class holds all the information needed to execute an asynchronous operation, including the work function, callback, priority, and context tracking for cancellation support.</p> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Descriptive name for the task (e.g., \"fetch_activity_data\").</p> <code>function</code> <code>Callable[[], Any]</code> <p>Callable that performs the work.</p> <code>callback</code> <code>Callable[[Any], None]</code> <p>Function to call with the result when task completes.</p> <code>priority</code> <code>int</code> <p>Priority level (lower numbers = higher priority). 0 = Critical (UI blocking operations) 1 = High (User-initiated operations) 5 = Normal (Background fetches) 10 = Low (Prefetching)</p> <code>context_id</code> <code>str</code> <p>Optional label grouping related tasks. Pass the same id to :meth:<code>AsyncTaskQueue.clear_context_tasks</code> to remove all pending tasks for that group at once.</p> <code>cancellable</code> <code>bool</code> <p>If True, the task can be removed by :meth:<code>AsyncTaskQueue.clear_context_tasks</code>.</p> Example <p>task = AsyncTask(     name=\"fetch_activities\",     function=lambda: fetch_data(context),     callback=on_data_ready,     priority=1,     context_id=\"project:item_id\",     cancellable=True )</p> Source code in <code>client/ayon_core/ui/components/task_queue.py</code> <pre><code>@dataclass\n@total_ordering\nclass AsyncTask:\n    \"\"\"Represents a task to be executed by the async task queue.\n\n    This class holds all the information needed to execute an asynchronous\n    operation, including the work function, callback, priority, and context\n    tracking for cancellation support.\n\n    Attributes:\n        name: Descriptive name for the task (e.g., \"fetch_activity_data\").\n        function: Callable that performs the work.\n        callback: Function to call with the result when task completes.\n        priority: Priority level (lower numbers = higher priority).\n            0 = Critical (UI blocking operations)\n            1 = High (User-initiated operations)\n            5 = Normal (Background fetches)\n            10 = Low (Prefetching)\n        context_id: Optional label grouping related tasks. Pass the same\n            id to :meth:`AsyncTaskQueue.clear_context_tasks` to remove all\n            pending tasks for that group at once.\n        cancellable: If True, the task can be removed by\n            :meth:`AsyncTaskQueue.clear_context_tasks`.\n\n    Example:\n        task = AsyncTask(\n            name=\"fetch_activities\",\n            function=lambda: fetch_data(context),\n            callback=on_data_ready,\n            priority=1,\n            context_id=\"project:item_id\",\n            cancellable=True\n        )\n    \"\"\"\n\n    name: str\n    function: Callable[[], Any]\n    callback: Callable[[Any], None]\n    priority: int = 5\n    context_id: str = \"\"\n    cancellable: bool = True\n\n    # Internal state\n    _counter: int = field(default=0, init=False, compare=True)\n    _cancelled: bool = field(default=False, init=False, repr=False)\n\n    def cancel(self) -&gt; None:\n        \"\"\"Mark this task as cancelled.\n\n        Only affects the task if cancellable is True.\n        \"\"\"\n        if self.cancellable:\n            self._cancelled = True\n\n    def is_cancelled(self) -&gt; bool:\n        \"\"\"Check if this task has been cancelled.\n\n        Returns:\n            True if the task has been cancelled, False otherwise.\n        \"\"\"\n        return self._cancelled\n\n    def __lt__(self, other: AsyncTask) -&gt; bool:\n        \"\"\"Compare tasks by priority, then by counter for FIFO ordering.\n\n        Args:\n            other: Another AsyncTask to compare with.\n\n        Returns:\n            True if this task should be processed before the other task.\n        \"\"\"\n        if self.priority != other.priority:\n            return self.priority &lt; other.priority\n        return self._counter &lt; other._counter\n\n    def __eq__(self, other: object) -&gt; bool:\n        \"\"\"Check equality based on priority and counter.\n\n        Args:\n            other: Object to compare with.\n\n        Returns:\n            True if tasks have equal priority and counter.\n        \"\"\"\n        if not isinstance(other, AsyncTask):\n            return NotImplemented\n        return (self.priority, self._counter) == (\n            other.priority,\n            other._counter,\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/task_queue.html#client.ayon_core.ui.components.task_queue.AsyncTask.__eq__","title":"<code>__eq__(other)</code>","text":"<p>Check equality based on priority and counter.</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>object</code> <p>Object to compare with.</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if tasks have equal priority and counter.</p> Source code in <code>client/ayon_core/ui/components/task_queue.py</code> <pre><code>def __eq__(self, other: object) -&gt; bool:\n    \"\"\"Check equality based on priority and counter.\n\n    Args:\n        other: Object to compare with.\n\n    Returns:\n        True if tasks have equal priority and counter.\n    \"\"\"\n    if not isinstance(other, AsyncTask):\n        return NotImplemented\n    return (self.priority, self._counter) == (\n        other.priority,\n        other._counter,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/task_queue.html#client.ayon_core.ui.components.task_queue.AsyncTask.__lt__","title":"<code>__lt__(other)</code>","text":"<p>Compare tasks by priority, then by counter for FIFO ordering.</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>AsyncTask</code> <p>Another AsyncTask to compare with.</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if this task should be processed before the other task.</p> Source code in <code>client/ayon_core/ui/components/task_queue.py</code> <pre><code>def __lt__(self, other: AsyncTask) -&gt; bool:\n    \"\"\"Compare tasks by priority, then by counter for FIFO ordering.\n\n    Args:\n        other: Another AsyncTask to compare with.\n\n    Returns:\n        True if this task should be processed before the other task.\n    \"\"\"\n    if self.priority != other.priority:\n        return self.priority &lt; other.priority\n    return self._counter &lt; other._counter\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/task_queue.html#client.ayon_core.ui.components.task_queue.AsyncTask.cancel","title":"<code>cancel()</code>","text":"<p>Mark this task as cancelled.</p> <p>Only affects the task if cancellable is True.</p> Source code in <code>client/ayon_core/ui/components/task_queue.py</code> <pre><code>def cancel(self) -&gt; None:\n    \"\"\"Mark this task as cancelled.\n\n    Only affects the task if cancellable is True.\n    \"\"\"\n    if self.cancellable:\n        self._cancelled = True\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/task_queue.html#client.ayon_core.ui.components.task_queue.AsyncTask.is_cancelled","title":"<code>is_cancelled()</code>","text":"<p>Check if this task has been cancelled.</p> <p>Returns:</p> Type Description <code>bool</code> <p>True if the task has been cancelled, False otherwise.</p> Source code in <code>client/ayon_core/ui/components/task_queue.py</code> <pre><code>def is_cancelled(self) -&gt; bool:\n    \"\"\"Check if this task has been cancelled.\n\n    Returns:\n        True if the task has been cancelled, False otherwise.\n    \"\"\"\n    return self._cancelled\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/task_queue.html#client.ayon_core.ui.components.task_queue.AsyncTaskQueue","title":"<code>AsyncTaskQueue</code>","text":"<p>               Bases: <code>QThread</code></p> <p>Dispatch thread + pool that processes async tasks from a priority queue.</p> <p>A single <code>QThread</code> (the dispatch loop) continuously dequeues the highest-priority task and submits it to an internal <code>ThreadPoolExecutor</code> so that multiple tasks can execute in parallel.</p> <p>This removes two performance bottlenecks that made multi-level tree expansion slow in the original single-worker design:</p> <ol> <li> <p>No idle polling - instead of sleeping 50 ms when the queue is    empty, the dispatch loop blocks on a <code>threading.Event</code> that    :meth:<code>enqueue</code> sets immediately, so there is zero dead time between    a tree-level's results arriving on the main thread, new    <code>fetchMore</code> calls being enqueued, and those fetches starting.</p> </li> <li> <p>Parallel execution - up to <code>num_workers</code> fetches run    concurrently, so expanding a folder with N children takes    <code>ceil(N / num_workers)</code> round-trips instead of N.</p> </li> </ol> Signals <p>task_completed: Emitted when a task finishes (task_name, result). task_failed: Emitted when a task raises exception (task_name, err). task_cancelled: Emitted when task is cancelled (task_name, ctx_id). queue_empty: Emitted when all tasks are processed.</p> <p>Multiple :attr:<code>~AsyncTask.context_id</code> values can coexist in the queue. Call :meth:<code>clear_context_tasks</code> to remove all pending tasks for a given context when external state changes make them irrelevant.</p> <p>Parameters:</p> Name Type Description Default <code>num_workers</code> <code>int</code> <p>Number of parallel pool workers (default 4).</p> <code>_DEFAULT_NUM_WORKERS</code> <code>parent</code> <code>Any</code> <p>Optional parent QObject.</p> <code>None</code> Example <p>queue = AsyncTaskQueue() queue.task_completed.connect(handle_completion) queue.start()</p> <p>task = AsyncTask(...) queue.enqueue(task)</p> Source code in <code>client/ayon_core/ui/components/task_queue.py</code> <pre><code>class AsyncTaskQueue(QThread):\n    \"\"\"Dispatch thread + pool that processes async tasks from a priority queue.\n\n    A single ``QThread`` (the *dispatch loop*) continuously dequeues the\n    highest-priority task and submits it to an internal\n    ``ThreadPoolExecutor`` so that multiple tasks can execute in parallel.\n\n    This removes two performance bottlenecks that made multi-level tree\n    expansion slow in the original single-worker design:\n\n    1. **No idle polling** - instead of sleeping 50 ms when the queue is\n       empty, the dispatch loop blocks on a ``threading.Event`` that\n       :meth:`enqueue` sets immediately, so there is zero dead time between\n       a tree-level's results arriving on the main thread, new\n       ``fetchMore`` calls being enqueued, and those fetches starting.\n\n    2. **Parallel execution** - up to ``num_workers`` fetches run\n       concurrently, so expanding a folder with N children takes\n       ``ceil(N / num_workers)`` round-trips instead of N.\n\n    Signals:\n        task_completed: Emitted when a task finishes (task_name, result).\n        task_failed: Emitted when a task raises exception (task_name, err).\n        task_cancelled: Emitted when task is cancelled (task_name, ctx_id).\n        queue_empty: Emitted when all tasks are processed.\n\n    Multiple :attr:`~AsyncTask.context_id` values can coexist in the queue.\n    Call :meth:`clear_context_tasks` to remove all pending tasks for a given\n    context when external state changes make them irrelevant.\n\n    Args:\n        num_workers: Number of parallel pool workers (default 4).\n        parent: Optional parent QObject.\n\n    Example:\n        queue = AsyncTaskQueue()\n        queue.task_completed.connect(handle_completion)\n        queue.start()\n\n        task = AsyncTask(...)\n        queue.enqueue(task)\n\n        # When the context is no longer relevant:\n        queue.clear_context_tasks(context_id)\n\n        # Later...\n        queue.stop()\n    \"\"\"\n\n    # Qt Signals\n    task_completed = Signal(str, object)  # (task_name, result)\n    task_failed = Signal(str, str)  # (task_name, error_message)\n    task_cancelled = Signal(str, str)  # (task_name, context_id)\n    task_enqueued = Signal(str, int)  # (task_name, priority)\n    task_started = Signal(str)  # (task_name)\n    queue_empty = Signal()\n    # No-arg ping signal: tells the main thread to drain _callback_queue.\n    # Never passes Python objects through Qt's cross-thread signal system.\n    invoke_callback = Signal()\n\n    def __init__(\n        self,\n        parent: Any = None,\n        num_workers: int = _DEFAULT_NUM_WORKERS,\n    ) -&gt; None:\n        \"\"\"Initialise the dispatch thread and pool.\n\n        Args:\n            num_workers: Number of parallel pool workers. Defaults to\n                :data:`_DEFAULT_NUM_WORKERS` (4).\n            parent: Optional parent QObject.\n        \"\"\"\n        super().__init__(parent)\n        self._task_queue: queue.PriorityQueue = queue.PriorityQueue()\n        # Thread-safe Python queue for ferrying (callback, result) pairs.\n        # Using Python's own ref-counting rather than Qt's marshaling avoids\n        # PySide6 issues with Python-callable arguments in QueuedConnection.\n        self._callback_queue: queue.SimpleQueue = queue.SimpleQueue()\n        self._running: bool = False\n        self._paused: threading.Event = threading.Event()\n        self._paused.set()  # Start unpaused\n        self._task_counter: int = 0\n        self._counter_lock: threading.Lock = threading.Lock()\n        # Serialises drain-and-rebuild operations in _filter_queue so that\n        # the worker thread cannot dequeue a task between two get_nowait()\n        # calls while the main thread is rebuilding the queue.\n        self._queue_lock: threading.Lock = threading.Lock()\n        # Set by enqueue() to wake the dispatch loop immediately instead of\n        # waiting for the polling timeout.\n        self._task_available: threading.Event = threading.Event()\n        self._num_workers: int = num_workers\n        self._executor: concurrent.futures.ThreadPoolExecutor | None = None\n\n        # Connect the no-arg ping to the drain slot on the main thread.\n        self.invoke_callback.connect(\n            self._drain_callback_queue,\n            Qt.ConnectionType.QueuedConnection,\n        )\n\n    @Slot()\n    def _drain_callback_queue(self) -&gt; None:\n        \"\"\"Process a small batch of pending callbacks, then yield.\n\n        Called via a QueuedConnection so it always runs on the Qt main\n        thread regardless of which thread emitted invoke_callback.\n\n        Up to ``_CALLBACK_BATCH_LIMIT`` callbacks are executed per\n        invocation, subject to a ``_CALLBACK_TIME_BUDGET_MS`` millisecond\n        wall-clock budget.  Stopping after each batch allows the event\n        loop to process paint events, keeping the UI responsive when many\n        async tasks complete simultaneously (e.g. auto-expand with\n        many folders).\n\n        If callbacks remain after the budget is exhausted, a 1 ms\n        single-shot timer re-schedules this method so that paint events\n        can be processed before the next batch.\n        \"\"\"\n        _BATCH_LIMIT = 5\n        _TIME_BUDGET_MS = 8\n\n        t0 = time.monotonic()\n        for _ in range(_BATCH_LIMIT):\n            try:\n                callback, result = self._callback_queue.get_nowait()\n            except queue.Empty:\n                return\n            try:\n                callback(result)\n            except Exception as e:\n                log.exception(\"Callback execution failed: %s\", e)\n            elapsed_ms = (time.monotonic() - t0) * 1000\n            if elapsed_ms &gt;= _TIME_BUDGET_MS:\n                break\n\n        # If more items remain, schedule the next drain after yielding\n        # to the event loop so paint events can be processed.\n        if not self._callback_queue.empty():\n            QTimer.singleShot(1, self._drain_callback_queue)\n\n    def run(self) -&gt; None:\n        \"\"\"Dispatch loop - runs in the QThread context.\n\n        Dequeues the highest-priority pending task and submits it to the\n        thread pool.  Blocks on ``_task_available`` when the queue is empty\n        so it wakes the instant :meth:`enqueue` adds a new task, eliminating\n        the 50 ms polling delay of the previous single-worker design.\n        \"\"\"\n        self._running = True\n        self._executor = concurrent.futures.ThreadPoolExecutor(\n            max_workers=self._num_workers,\n            thread_name_prefix=\"ayon_task_worker\",\n        )\n        log.debug(\n            \"Task queue dispatch loop started (num_workers=%d)\",\n            self._num_workers,\n        )\n\n        try:\n            while self._running:\n                # Wait if paused\n                self._paused.wait()\n\n                try:\n                    # Acquire the queue lock before dequeuing so that\n                    # _filter_queue drain-and-rebuild on the main thread is\n                    # atomic with respect to this get() call.\n                    with self._queue_lock:\n                        try:\n                            priority, task = self._task_queue.get_nowait()\n                        except queue.Empty:\n                            task = None\n\n                    if task is None:\n                        # Block until a new task is enqueued (enqueue() sets\n                        # _task_available) or the 0.5 s safety timeout fires.\n                        # This replaces the original time.sleep(0.05) busy-\n                        # poll, eliminating idle dead-time between expansion\n                        # waves without wasting CPU.\n                        self._task_available.wait(timeout=0.5)\n                        self._task_available.clear()\n                        continue\n\n                    if task.is_cancelled():\n                        log.debug(\"Skipping cancelled task: %s\", task.name)\n                        self.task_cancelled.emit(task.name, task.context_id)\n                        if self._task_queue.empty():\n                            self.queue_empty.emit()\n                        continue\n\n                    log.debug(\n                        \"Submitting task to pool: %s (context=%s)\",\n                        task.name,\n                        task.context_id,\n                    )\n                    self._executor.submit(self._run_task_in_pool, task)\n\n                    if self._task_queue.empty():\n                        self.queue_empty.emit()\n\n                except Exception as e:\n                    log.exception(\"Unexpected error in dispatch loop: %s\", e)\n        finally:\n            # cancel_futures=True (Python \u2265 3.9) drops queued-but-not-\n            # started futures; already-running ones are awaited.\n            self._executor.shutdown(wait=True, cancel_futures=True)\n            self._executor = None\n\n        log.debug(\"Task queue dispatch loop stopped\")\n\n    def _run_task_in_pool(self, task: AsyncTask) -&gt; None:\n        \"\"\"Execute a single task inside a pool worker thread.\n\n        Results are delivered back to the main thread via\n        :meth:`_invoke_callback_safely`.\n\n        Args:\n            task: The task to execute.\n        \"\"\"\n        # Re-check cancellation: the task may have been cancelled while\n        # waiting in the executor's internal work queue.\n        if task.is_cancelled():\n            log.debug(\"Skipping cancelled task (pool): %s\", task.name)\n            return\n\n        log.debug(\n            \"Processing task: %s (context=%s)\", task.name, task.context_id\n        )\n\n        try:\n            self.task_started.emit(task.name)\n        except Exception:\n            log.debug(\n                \"Failed to emit task_started for %s.\", task.name, exc_info=True\n            )\n\n        try:\n            result = task.function()\n\n            # Double-check cancellation after execution.\n            if task.is_cancelled():\n                log.debug(\n                    \"Task %s completed but was cancelled, discarding result\",\n                    task.name,\n                )\n                return\n\n            if task.callback:\n                self._invoke_callback_safely(task.callback, result, task.name)\n\n            try:\n                self.task_completed.emit(task.name, result)\n            except Exception:\n                log.debug(\n                    \"Failed to emit task_completed for %s.\",\n                    task.name,\n                    exc_info=True,\n                )\n\n            log.debug(\"Task completed successfully: %s\", task.name)\n\n        except Exception as e:\n            log.exception(\"Task %s failed: %s\", task.name, e)\n\n            if task.callback:\n                self._invoke_callback_safely(task.callback, None, task.name)\n\n            try:\n                self.task_failed.emit(task.name, str(e))\n            except Exception:\n                log.debug(\n                    \"Failed to emit task_failed for %s.\",\n                    task.name,\n                    exc_info=True,\n                )\n\n    def _invoke_callback_safely(\n        self, callback: Callable, result: Any, task_name: str\n    ) -&gt; None:\n        \"\"\"Invoke callback safely with unified error handling.\n\n        Pushes (callback, result) onto a Python SimpleQueue so that\n        Python callables never travel through Qt's cross-thread\n        signal marshaling (which is unreliable for arbitrary callables\n        in PySide6).  A no-arg ping signal then wakes the main thread.\n\n        Args:\n            callback: The callback function to invoke.\n            result: The result to pass to the callback.\n            task_name: Name of the task (for logging).\n        \"\"\"\n        try:\n            self._callback_queue.put_nowait((callback, result))\n            self.invoke_callback.emit()\n        except Exception as e:\n            log.exception(\n                \"Error invoking callback for %s: %s\",\n                task_name,\n                e,\n            )\n\n    def enqueue(self, task: AsyncTask) -&gt; None:\n        \"\"\"Add a task to the queue (thread-safe).\n\n        Assigns a monotonically increasing counter to the task to ensure\n        FIFO ordering within the same priority level.  Sets\n        ``_task_available`` so the dispatch loop wakes immediately instead\n        of waiting for its polling timeout.\n\n        Args:\n            task: The task to enqueue.\n        \"\"\"\n        # Assign a counter value for FIFO ordering within priority\n        with self._counter_lock:\n            task._counter = self._task_counter\n            self._task_counter += 1\n\n        # Add to priority queue\n        self._task_queue.put((task.priority, task))\n        # Notify observers that a task has been enqueued.\n        try:\n            self.task_enqueued.emit(task.name, task.priority)\n        except Exception:\n            log.debug(\n                \"Failed to emit task_enqueued for %s.\",\n                task.name,\n                exc_info=True,\n            )\n        # Wake the dispatch loop immediately \u2014 no idle wait.\n        self._task_available.set()\n        log.debug(\n            \"Enqueued task: %s (priority=%d, queue_size=%d)\",\n            task.name,\n            task.priority,\n            self._task_queue.qsize(),\n        )\n\n    def pause(self) -&gt; None:\n        \"\"\"Pause task dispatching.\n\n        The dispatch loop finishes submitting the current task and then\n        waits until resumed.  Tasks can still be enqueued while paused.\n        \"\"\"\n        self._paused.clear()\n        log.debug(\"Task queue paused\")\n\n    def resume(self) -&gt; None:\n        \"\"\"Resume task dispatching.\n\n        Dispatching resumes from where it was paused.\n        \"\"\"\n        self._paused.set()\n        log.debug(\"Task queue resumed\")\n\n    def stop(self) -&gt; None:\n        \"\"\"Stop the dispatch thread and pool workers gracefully.\n\n        Sets ``_running = False``, then wakes the dispatch loop so it can\n        exit immediately.  The loop's ``finally`` block shuts the executor\n        down (cancelling queued-but-not-started futures; already-running\n        ones complete normally).  Waits up to 5 seconds for everything to\n        finish.\n\n        Any pending (callback, result) pairs left in ``_callback_queue``\n        are discarded so that stale model callbacks from a previous test\n        (or a discarded context) cannot fire after teardown.\n        \"\"\"\n        log.debug(\"Stopping task queue worker\")\n        self._running = False\n        self._paused.set()  # Unpause if paused\n        # Wake the dispatch loop so it can observe _running=False without\n        # waiting for the 0.5 s timeout.\n        self._task_available.set()\n\n        # Wait for the QThread (dispatch loop + executor shutdown) to finish.\n        if not self.wait(5000):\n            log.warning(\"Task queue worker did not stop within timeout\")\n\n        # Discard any pending callbacks.  Any invoke_callback ping events\n        # already in the Qt event queue will call _drain_callback_queue,\n        # which will safely find an empty queue and return immediately.\n        while True:\n            try:\n                self._callback_queue.get_nowait()\n            except queue.Empty:\n                break\n\n    def is_paused(self) -&gt; bool:\n        \"\"\"Check if the worker is currently paused.\n\n        Returns:\n            True if paused, False otherwise.\n        \"\"\"\n        return not self._paused.is_set()\n\n    def queue_size(self) -&gt; int:\n        \"\"\"Get the current number of tasks in the priority queue.\n\n        Returns:\n            Number of pending tasks (does not include tasks already\n            submitted to the pool).\n        \"\"\"\n        return self._task_queue.qsize()\n\n    def _filter_queue(\n        self, predicate: Callable[[AsyncTask, str], tuple[bool, bool]]\n    ) -&gt; int:\n        \"\"\"Filter queue items based on predicate function.\n\n        Drains queue, applies predicate to each task, and rebuilds queue\n        with filtered items. This is a generic helper for both cancel\n        and clear operations.\n\n        The entire drain-and-rebuild is performed while holding\n        ``_queue_lock`` so that the dispatch thread cannot dequeue a task\n        between two ``get_nowait()`` calls, making the operation atomic\n        with respect to the dispatch loop.\n\n        Note:\n            Tasks already submitted to the pool cannot be removed here;\n            the cancellation flag on :class:`AsyncTask` is the only\n            mechanism that can stop an already-submitted task.\n\n        Args:\n            predicate: Function that takes (task, context_id) and returns\n                (keep_in_queue, emit_cancelled_signal).\n\n        Returns:\n            Number of tasks affected (cancelled or removed).\n        \"\"\"\n        affected_count = 0\n        items_to_keep = []\n\n        with self._queue_lock:\n            while True:\n                try:\n                    priority, task = self._task_queue.get_nowait()\n                    keep, emit_signal = predicate(task, task.context_id)\n\n                    if keep:\n                        items_to_keep.append((priority, task))\n                    else:\n                        affected_count += 1\n                        if emit_signal:\n                            self.task_cancelled.emit(\n                                task.name, task.context_id\n                            )\n\n                except queue.Empty:\n                    break\n\n            # Rebuild queue with remaining items\n            for item in items_to_keep:\n                self._task_queue.put(item)\n\n        return affected_count\n\n    def clear_context_tasks(self, context_id: str) -&gt; int:\n        \"\"\"Completely remove tasks for a specific context from queue.\n\n        More aggressive than cancel - actually removes from queue rather\n        than just marking as cancelled.\n\n        Note:\n            Tasks already submitted to the pool executor (i.e. currently\n            running or waiting for a free pool slot) are *not* removed\n            here.  Their results will be discarded by the model's stale-\n            context check in the callback.\n\n        Args:\n            context_id: Context identifier to clear.\n\n        Returns:\n            Number of tasks removed.\n        \"\"\"\n\n        def should_remove(task: AsyncTask, ctx: str) -&gt; tuple[bool, bool]:\n            \"\"\"Return (keep_in_queue, emit_signal).\"\"\"\n            if task.context_id == context_id and task.cancellable:\n                return (False, True)  # Remove and signal\n            return (True, False)  # Keep, no signal\n\n        removed_count = self._filter_queue(should_remove)\n        if removed_count &gt; 0:\n            log.debug(\n                \"Removed %d tasks for context: %s\",\n                removed_count,\n                context_id,\n            )\n\n        return removed_count\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/task_queue.html#client.ayon_core.ui.components.task_queue.AsyncTaskQueue--when-the-context-is-no-longer-relevant","title":"When the context is no longer relevant:","text":"<p>queue.clear_context_tasks(context_id)</p>"},{"location":"autoapi/client/ayon_core/ui/components/task_queue.html#client.ayon_core.ui.components.task_queue.AsyncTaskQueue--later","title":"Later...","text":"<p>queue.stop()</p>"},{"location":"autoapi/client/ayon_core/ui/components/task_queue.html#client.ayon_core.ui.components.task_queue.AsyncTaskQueue.__init__","title":"<code>__init__(parent=None, num_workers=_DEFAULT_NUM_WORKERS)</code>","text":"<p>Initialise the dispatch thread and pool.</p> <p>Parameters:</p> Name Type Description Default <code>num_workers</code> <code>int</code> <p>Number of parallel pool workers. Defaults to :data:<code>_DEFAULT_NUM_WORKERS</code> (4).</p> <code>_DEFAULT_NUM_WORKERS</code> <code>parent</code> <code>Any</code> <p>Optional parent QObject.</p> <code>None</code> Source code in <code>client/ayon_core/ui/components/task_queue.py</code> <pre><code>def __init__(\n    self,\n    parent: Any = None,\n    num_workers: int = _DEFAULT_NUM_WORKERS,\n) -&gt; None:\n    \"\"\"Initialise the dispatch thread and pool.\n\n    Args:\n        num_workers: Number of parallel pool workers. Defaults to\n            :data:`_DEFAULT_NUM_WORKERS` (4).\n        parent: Optional parent QObject.\n    \"\"\"\n    super().__init__(parent)\n    self._task_queue: queue.PriorityQueue = queue.PriorityQueue()\n    # Thread-safe Python queue for ferrying (callback, result) pairs.\n    # Using Python's own ref-counting rather than Qt's marshaling avoids\n    # PySide6 issues with Python-callable arguments in QueuedConnection.\n    self._callback_queue: queue.SimpleQueue = queue.SimpleQueue()\n    self._running: bool = False\n    self._paused: threading.Event = threading.Event()\n    self._paused.set()  # Start unpaused\n    self._task_counter: int = 0\n    self._counter_lock: threading.Lock = threading.Lock()\n    # Serialises drain-and-rebuild operations in _filter_queue so that\n    # the worker thread cannot dequeue a task between two get_nowait()\n    # calls while the main thread is rebuilding the queue.\n    self._queue_lock: threading.Lock = threading.Lock()\n    # Set by enqueue() to wake the dispatch loop immediately instead of\n    # waiting for the polling timeout.\n    self._task_available: threading.Event = threading.Event()\n    self._num_workers: int = num_workers\n    self._executor: concurrent.futures.ThreadPoolExecutor | None = None\n\n    # Connect the no-arg ping to the drain slot on the main thread.\n    self.invoke_callback.connect(\n        self._drain_callback_queue,\n        Qt.ConnectionType.QueuedConnection,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/task_queue.html#client.ayon_core.ui.components.task_queue.AsyncTaskQueue.clear_context_tasks","title":"<code>clear_context_tasks(context_id)</code>","text":"<p>Completely remove tasks for a specific context from queue.</p> <p>More aggressive than cancel - actually removes from queue rather than just marking as cancelled.</p> Note <p>Tasks already submitted to the pool executor (i.e. currently running or waiting for a free pool slot) are not removed here.  Their results will be discarded by the model's stale- context check in the callback.</p> <p>Parameters:</p> Name Type Description Default <code>context_id</code> <code>str</code> <p>Context identifier to clear.</p> required <p>Returns:</p> Type Description <code>int</code> <p>Number of tasks removed.</p> Source code in <code>client/ayon_core/ui/components/task_queue.py</code> <pre><code>def clear_context_tasks(self, context_id: str) -&gt; int:\n    \"\"\"Completely remove tasks for a specific context from queue.\n\n    More aggressive than cancel - actually removes from queue rather\n    than just marking as cancelled.\n\n    Note:\n        Tasks already submitted to the pool executor (i.e. currently\n        running or waiting for a free pool slot) are *not* removed\n        here.  Their results will be discarded by the model's stale-\n        context check in the callback.\n\n    Args:\n        context_id: Context identifier to clear.\n\n    Returns:\n        Number of tasks removed.\n    \"\"\"\n\n    def should_remove(task: AsyncTask, ctx: str) -&gt; tuple[bool, bool]:\n        \"\"\"Return (keep_in_queue, emit_signal).\"\"\"\n        if task.context_id == context_id and task.cancellable:\n            return (False, True)  # Remove and signal\n        return (True, False)  # Keep, no signal\n\n    removed_count = self._filter_queue(should_remove)\n    if removed_count &gt; 0:\n        log.debug(\n            \"Removed %d tasks for context: %s\",\n            removed_count,\n            context_id,\n        )\n\n    return removed_count\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/task_queue.html#client.ayon_core.ui.components.task_queue.AsyncTaskQueue.enqueue","title":"<code>enqueue(task)</code>","text":"<p>Add a task to the queue (thread-safe).</p> <p>Assigns a monotonically increasing counter to the task to ensure FIFO ordering within the same priority level.  Sets <code>_task_available</code> so the dispatch loop wakes immediately instead of waiting for its polling timeout.</p> <p>Parameters:</p> Name Type Description Default <code>task</code> <code>AsyncTask</code> <p>The task to enqueue.</p> required Source code in <code>client/ayon_core/ui/components/task_queue.py</code> <pre><code>def enqueue(self, task: AsyncTask) -&gt; None:\n    \"\"\"Add a task to the queue (thread-safe).\n\n    Assigns a monotonically increasing counter to the task to ensure\n    FIFO ordering within the same priority level.  Sets\n    ``_task_available`` so the dispatch loop wakes immediately instead\n    of waiting for its polling timeout.\n\n    Args:\n        task: The task to enqueue.\n    \"\"\"\n    # Assign a counter value for FIFO ordering within priority\n    with self._counter_lock:\n        task._counter = self._task_counter\n        self._task_counter += 1\n\n    # Add to priority queue\n    self._task_queue.put((task.priority, task))\n    # Notify observers that a task has been enqueued.\n    try:\n        self.task_enqueued.emit(task.name, task.priority)\n    except Exception:\n        log.debug(\n            \"Failed to emit task_enqueued for %s.\",\n            task.name,\n            exc_info=True,\n        )\n    # Wake the dispatch loop immediately \u2014 no idle wait.\n    self._task_available.set()\n    log.debug(\n        \"Enqueued task: %s (priority=%d, queue_size=%d)\",\n        task.name,\n        task.priority,\n        self._task_queue.qsize(),\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/task_queue.html#client.ayon_core.ui.components.task_queue.AsyncTaskQueue.is_paused","title":"<code>is_paused()</code>","text":"<p>Check if the worker is currently paused.</p> <p>Returns:</p> Type Description <code>bool</code> <p>True if paused, False otherwise.</p> Source code in <code>client/ayon_core/ui/components/task_queue.py</code> <pre><code>def is_paused(self) -&gt; bool:\n    \"\"\"Check if the worker is currently paused.\n\n    Returns:\n        True if paused, False otherwise.\n    \"\"\"\n    return not self._paused.is_set()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/task_queue.html#client.ayon_core.ui.components.task_queue.AsyncTaskQueue.pause","title":"<code>pause()</code>","text":"<p>Pause task dispatching.</p> <p>The dispatch loop finishes submitting the current task and then waits until resumed.  Tasks can still be enqueued while paused.</p> Source code in <code>client/ayon_core/ui/components/task_queue.py</code> <pre><code>def pause(self) -&gt; None:\n    \"\"\"Pause task dispatching.\n\n    The dispatch loop finishes submitting the current task and then\n    waits until resumed.  Tasks can still be enqueued while paused.\n    \"\"\"\n    self._paused.clear()\n    log.debug(\"Task queue paused\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/task_queue.html#client.ayon_core.ui.components.task_queue.AsyncTaskQueue.queue_size","title":"<code>queue_size()</code>","text":"<p>Get the current number of tasks in the priority queue.</p> <p>Returns:</p> Type Description <code>int</code> <p>Number of pending tasks (does not include tasks already</p> <code>int</code> <p>submitted to the pool).</p> Source code in <code>client/ayon_core/ui/components/task_queue.py</code> <pre><code>def queue_size(self) -&gt; int:\n    \"\"\"Get the current number of tasks in the priority queue.\n\n    Returns:\n        Number of pending tasks (does not include tasks already\n        submitted to the pool).\n    \"\"\"\n    return self._task_queue.qsize()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/task_queue.html#client.ayon_core.ui.components.task_queue.AsyncTaskQueue.resume","title":"<code>resume()</code>","text":"<p>Resume task dispatching.</p> <p>Dispatching resumes from where it was paused.</p> Source code in <code>client/ayon_core/ui/components/task_queue.py</code> <pre><code>def resume(self) -&gt; None:\n    \"\"\"Resume task dispatching.\n\n    Dispatching resumes from where it was paused.\n    \"\"\"\n    self._paused.set()\n    log.debug(\"Task queue resumed\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/task_queue.html#client.ayon_core.ui.components.task_queue.AsyncTaskQueue.run","title":"<code>run()</code>","text":"<p>Dispatch loop - runs in the QThread context.</p> <p>Dequeues the highest-priority pending task and submits it to the thread pool.  Blocks on <code>_task_available</code> when the queue is empty so it wakes the instant :meth:<code>enqueue</code> adds a new task, eliminating the 50 ms polling delay of the previous single-worker design.</p> Source code in <code>client/ayon_core/ui/components/task_queue.py</code> <pre><code>def run(self) -&gt; None:\n    \"\"\"Dispatch loop - runs in the QThread context.\n\n    Dequeues the highest-priority pending task and submits it to the\n    thread pool.  Blocks on ``_task_available`` when the queue is empty\n    so it wakes the instant :meth:`enqueue` adds a new task, eliminating\n    the 50 ms polling delay of the previous single-worker design.\n    \"\"\"\n    self._running = True\n    self._executor = concurrent.futures.ThreadPoolExecutor(\n        max_workers=self._num_workers,\n        thread_name_prefix=\"ayon_task_worker\",\n    )\n    log.debug(\n        \"Task queue dispatch loop started (num_workers=%d)\",\n        self._num_workers,\n    )\n\n    try:\n        while self._running:\n            # Wait if paused\n            self._paused.wait()\n\n            try:\n                # Acquire the queue lock before dequeuing so that\n                # _filter_queue drain-and-rebuild on the main thread is\n                # atomic with respect to this get() call.\n                with self._queue_lock:\n                    try:\n                        priority, task = self._task_queue.get_nowait()\n                    except queue.Empty:\n                        task = None\n\n                if task is None:\n                    # Block until a new task is enqueued (enqueue() sets\n                    # _task_available) or the 0.5 s safety timeout fires.\n                    # This replaces the original time.sleep(0.05) busy-\n                    # poll, eliminating idle dead-time between expansion\n                    # waves without wasting CPU.\n                    self._task_available.wait(timeout=0.5)\n                    self._task_available.clear()\n                    continue\n\n                if task.is_cancelled():\n                    log.debug(\"Skipping cancelled task: %s\", task.name)\n                    self.task_cancelled.emit(task.name, task.context_id)\n                    if self._task_queue.empty():\n                        self.queue_empty.emit()\n                    continue\n\n                log.debug(\n                    \"Submitting task to pool: %s (context=%s)\",\n                    task.name,\n                    task.context_id,\n                )\n                self._executor.submit(self._run_task_in_pool, task)\n\n                if self._task_queue.empty():\n                    self.queue_empty.emit()\n\n            except Exception as e:\n                log.exception(\"Unexpected error in dispatch loop: %s\", e)\n    finally:\n        # cancel_futures=True (Python \u2265 3.9) drops queued-but-not-\n        # started futures; already-running ones are awaited.\n        self._executor.shutdown(wait=True, cancel_futures=True)\n        self._executor = None\n\n    log.debug(\"Task queue dispatch loop stopped\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/task_queue.html#client.ayon_core.ui.components.task_queue.AsyncTaskQueue.stop","title":"<code>stop()</code>","text":"<p>Stop the dispatch thread and pool workers gracefully.</p> <p>Sets <code>_running = False</code>, then wakes the dispatch loop so it can exit immediately.  The loop's <code>finally</code> block shuts the executor down (cancelling queued-but-not-started futures; already-running ones complete normally).  Waits up to 5 seconds for everything to finish.</p> <p>Any pending (callback, result) pairs left in <code>_callback_queue</code> are discarded so that stale model callbacks from a previous test (or a discarded context) cannot fire after teardown.</p> Source code in <code>client/ayon_core/ui/components/task_queue.py</code> <pre><code>def stop(self) -&gt; None:\n    \"\"\"Stop the dispatch thread and pool workers gracefully.\n\n    Sets ``_running = False``, then wakes the dispatch loop so it can\n    exit immediately.  The loop's ``finally`` block shuts the executor\n    down (cancelling queued-but-not-started futures; already-running\n    ones complete normally).  Waits up to 5 seconds for everything to\n    finish.\n\n    Any pending (callback, result) pairs left in ``_callback_queue``\n    are discarded so that stale model callbacks from a previous test\n    (or a discarded context) cannot fire after teardown.\n    \"\"\"\n    log.debug(\"Stopping task queue worker\")\n    self._running = False\n    self._paused.set()  # Unpause if paused\n    # Wake the dispatch loop so it can observe _running=False without\n    # waiting for the 0.5 s timeout.\n    self._task_available.set()\n\n    # Wait for the QThread (dispatch loop + executor shutdown) to finish.\n    if not self.wait(5000):\n        log.warning(\"Task queue worker did not stop within timeout\")\n\n    # Discard any pending callbacks.  Any invoke_callback ping events\n    # already in the Qt event queue will call _drain_callback_queue,\n    # which will safely find an empty queue and return immediately.\n    while True:\n        try:\n            self._callback_queue.get_nowait()\n        except queue.Empty:\n            break\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/task_queue.html#client.ayon_core.ui.components.task_queue.get_task_queue","title":"<code>get_task_queue()</code>","text":"<p>Return the shared AsyncTaskQueue, creating and starting it on first use.</p> <p>The queue is a module-level singleton so all components (table models, tree models, etc.) share one dispatch thread and one pool.</p> <p>On first creation the queue is automatically connected to <code>QApplication.aboutToQuit</code> so it stops cleanly when the application exits \u2014 no manual :func:<code>shutdown_task_queue</code> call is required.</p> <p>Returns:</p> Type Description <code>AsyncTaskQueue</code> <p>The running shared :class:<code>AsyncTaskQueue</code> instance.</p> Source code in <code>client/ayon_core/ui/components/task_queue.py</code> <pre><code>def get_task_queue() -&gt; AsyncTaskQueue:\n    \"\"\"Return the shared AsyncTaskQueue, creating and starting it on first use.\n\n    The queue is a module-level singleton so all components (table models,\n    tree models, etc.) share one dispatch thread and one pool.\n\n    On first creation the queue is automatically connected to\n    ``QApplication.aboutToQuit`` so it stops cleanly when the application\n    exits \u2014 no manual :func:`shutdown_task_queue` call is required.\n\n    Returns:\n        The running shared :class:`AsyncTaskQueue` instance.\n    \"\"\"\n    if _LocalContext.shared_queue is not None:\n        if not _LocalContext.shutdown_connected:\n            _register_shutdown_hook()\n        return _LocalContext.shared_queue\n\n    shared_queue = AsyncTaskQueue()\n    _LocalContext.shared_queue = shared_queue\n    shared_queue.start()\n    log.debug(\"Shared task queue started\")\n\n    if not _register_shutdown_hook():\n        log.warning(\n            \"get_task_queue() called before QApplication exists; \"\n            \"automatic shutdown will not be registered.\"\n        )\n\n    return shared_queue\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/task_queue.html#client.ayon_core.ui.components.task_queue.shutdown_task_queue","title":"<code>shutdown_task_queue()</code>","text":"<p>Stop and discard the shared :class:<code>AsyncTaskQueue</code>.</p> <p>Called automatically when the <code>QApplication</code> emits <code>aboutToQuit</code> (wired by :func:<code>get_task_queue</code> on first use).  Safe to call manually before that if an early teardown is needed; subsequent calls are no-ops.  After this call :func:<code>get_task_queue</code> will create a fresh queue on next access.</p> Source code in <code>client/ayon_core/ui/components/task_queue.py</code> <pre><code>def shutdown_task_queue() -&gt; None:\n    \"\"\"Stop and discard the shared :class:`AsyncTaskQueue`.\n\n    Called automatically when the ``QApplication`` emits ``aboutToQuit``\n    (wired by :func:`get_task_queue` on first use).  Safe to call\n    manually before that if an early teardown is needed; subsequent calls\n    are no-ops.  After this call :func:`get_task_queue` will create a\n    fresh queue on next access.\n    \"\"\"\n    if _LocalContext.shared_queue is not None:\n        if _LocalContext.shared_queue.isRunning():\n            _LocalContext.shared_queue.stop()\n        _LocalContext.shared_queue = None\n        log.debug(\"Shared task queue stopped\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/task_queue_monitor.html","title":"task_queue_monitor","text":""},{"location":"autoapi/client/ayon_core/ui/components/task_queue_monitor.html#client.ayon_core.ui.components.task_queue_monitor.AsyncTaskQueueMonitor","title":"<code>AsyncTaskQueueMonitor</code>","text":"<p>               Bases: <code>StyleMixin</code>, <code>QWidget</code></p> <p>Compact segmented progress bar that mirrors an :class:<code>AsyncTaskQueue</code>.</p> <p>Each task observed from the queue is rendered as a color-coded segment. Segment colors map to priority: high (orange), medium (green), low (blue).  The currently running task pulses to indicate activity.</p> <p>The widget has a fixed maximum height of 24 px and expands horizontally to fill its container.</p> <p>A rich tooltip (shown on hover) reports totals by status, a percentage breakdown per priority bucket, and the name of the currently active task.</p> <p>Redraws are debounced so that bursts of rapid task completions do not cause excessive paint events.</p> <p>Parameters:</p> Name Type Description Default <code>task_queue</code> <code>AsyncTaskQueue</code> <p>The :class:<code>AsyncTaskQueue</code> instance to observe.</p> required <code>update_interval_ms</code> <code>int</code> <p>Debounce interval in milliseconds between successive redraws (default 50 ms).</p> <code>_DEBOUNCE_MS</code> <code>parent</code> <code>Any</code> <p>Optional parent :class:<code>QWidget</code>.</p> <code>None</code> Example <p>::</p> <pre><code>queue = get_task_queue()\nbar = AsyncTaskQueueMonitor(queue)\nlayout.addWidget(bar)\n</code></pre> Source code in <code>client/ayon_core/ui/components/task_queue_monitor.py</code> <pre><code>class AsyncTaskQueueMonitor(StyleMixin, QWidget):\n    \"\"\"Compact segmented progress bar that mirrors an :class:`AsyncTaskQueue`.\n\n    Each task observed from the queue is rendered as a color-coded segment.\n    Segment colors map to priority: high (orange), medium (green), low\n    (blue).  The currently running task pulses to indicate activity.\n\n    The widget has a fixed maximum height of 24 px and expands horizontally\n    to fill its container.\n\n    A rich tooltip (shown on hover) reports totals by status, a percentage\n    breakdown per priority bucket, and the name of the currently active task.\n\n    Redraws are debounced so that bursts of rapid task completions do not\n    cause excessive paint events.\n\n    Args:\n        task_queue: The :class:`AsyncTaskQueue` instance to observe.\n        update_interval_ms: Debounce interval in milliseconds between\n            successive redraws (default 50 ms).\n        parent: Optional parent :class:`QWidget`.\n\n    Example:\n        ::\n\n            queue = get_task_queue()\n            bar = AsyncTaskQueueMonitor(queue)\n            layout.addWidget(bar)\n    \"\"\"\n\n    #: Fixed maximum height in pixels.\n    MAX_HEIGHT: int = 10\n\n    def __init__(\n        self,\n        task_queue: AsyncTaskQueue,\n        update_interval_ms: int = _DEBOUNCE_MS,\n        parent: Any = None,\n    ) -&gt; None:\n        \"\"\"Initialise and connect to *task_queue* signals.\n\n        Args:\n            task_queue: Queue instance to observe.\n            update_interval_ms: Debounce redraw interval in ms.\n            parent: Optional parent widget.\n        \"\"\"\n        super().__init__(parent)\n\n        self._queue: AsyncTaskQueue = task_queue\n        self._tasks: list[_TrackedTask] = []\n        self._pulse_phase: float = 0.0\n\n        # Load style configuration from AYON style system\n        style_data = get_ayon_style_data(\"AsyncTaskQueueMonitor\") or {}\n        style_data.set_context(self)\n        if not style_data:\n            log.warning(\n                \"AsyncTaskQueueMonitor: Style data not available, \"\n                \"using defaults\"\n            )\n\n        # Colors\n        self._bg_color = QColor(style_data.get(\"background-color\", \"#1c2026\"))\n        self._bg_outline_color = QColor(\n            style_data.get(\"outline-color\", \"#3F4552\")\n        )\n        self._priority_colors = {\n            \"high\": QColor(style_data.get(\"high-priority-color\", \"#E87D0D\")),\n            \"medium\": QColor(\n                style_data.get(\"medium-priority-color\", \"#4CAF50\")\n            ),\n            \"low\": QColor(style_data.get(\"low-priority-color\", \"#2196F3\")),\n        }\n        self._failed_color = QColor(style_data.get(\"failed-color\", \"#F44336\"))\n\n        # Alphas (opacity values)\n        self._completed_alpha = style_data.get(\"completed-alpha\", 0.45)\n        self._pending_alpha = style_data.get(\"pending-alpha\", 0.70)\n\n        # Dimensions\n        self._segment_gap = style_data.get(\"segment-gap\", 2)\n        self._bar_radius = style_data.get(\"bar-radius\", 3)\n\n        # Debounce timer \u2014 single-shot, restarted on every state change.\n        self._update_timer = QTimer(self)\n        self._update_timer.setSingleShot(True)\n        self._update_timer.setInterval(update_interval_ms)\n        self._update_timer.timeout.connect(self._on_debounced_update)\n\n        # Pulse animation timer.\n        self._pulse_timer = QTimer(self)\n        self._pulse_timer.setInterval(_PULSE_INTERVAL_MS)\n        self._pulse_timer.timeout.connect(self._advance_pulse)\n\n        # Clear timer.\n        self._clear_timer = QTimer(self)\n        self._clear_timer.setSingleShot(True)\n        self._clear_timer.timeout.connect(self._clear_finished)\n\n        # Connect queue signals.\n        task_queue.task_enqueued.connect(self._on_task_enqueued)\n        task_queue.task_started.connect(self._on_task_started)\n        task_queue.task_completed.connect(self._on_task_completed)\n        task_queue.task_failed.connect(self._on_task_failed)\n        task_queue.task_cancelled.connect(self._on_task_cancelled)\n        task_queue.queue_empty.connect(self._on_queue_empty)\n\n        # Widget appearance.\n        self.setMaximumHeight(self.MAX_HEIGHT)\n        self.setMinimumHeight(self.MAX_HEIGHT)\n        self.setSizePolicy(\n            QSizePolicy.Policy.Expanding, QSizePolicy.Policy.Fixed\n        )\n        self.setAttribute(Qt.WidgetAttribute.WA_Hover, True)\n        self.setMouseTracking(True)\n\n    # ------------------------------------------------------------------\n    # Signal handlers\n    # ------------------------------------------------------------------\n\n    @Slot(str, int)\n    def _on_task_enqueued(self, name: str, priority: int) -&gt; None:\n        \"\"\"Record a newly enqueued task.\n\n        Args:\n            name: Task name.\n            priority: Numeric priority.\n        \"\"\"\n        self._tasks.append(_TrackedTask(name, priority, _TaskStatus.PENDING))\n        self._schedule_update()\n\n    @Slot(str)\n    def _on_task_started(self, name: str) -&gt; None:\n        \"\"\"Transition the first matching pending task to running.\n\n        Args:\n            name: Task name.\n        \"\"\"\n        has_running = False\n        for task in reversed(self._tasks):\n            if task.name == name and task.status == _TaskStatus.PENDING:\n                task.status = _TaskStatus.RUNNING\n                has_running = True\n                break\n        if has_running and not self._pulse_timer.isActive():\n            self._pulse_timer.start()\n        self._schedule_update()\n\n    @Slot(str, object)\n    def _on_task_completed(self, name: str, _result: object) -&gt; None:\n        \"\"\"Mark the first matching active task as completed.\n\n        Args:\n            name: Task name.\n            _result: Unused task result.\n        \"\"\"\n        self._transition(name, _TaskStatus.COMPLETED)\n\n    @Slot(str, str)\n    def _on_task_failed(self, name: str, _error: str) -&gt; None:\n        \"\"\"Mark the first matching active task as failed.\n\n        Args:\n            name: Task name.\n            _error: Unused error message.\n        \"\"\"\n        self._transition(name, _TaskStatus.FAILED)\n\n    @Slot(str, str)\n    def _on_task_cancelled(self, name: str, _ctx: str) -&gt; None:\n        \"\"\"Mark the first matching active task as cancelled.\n\n        Args:\n            name: Task name.\n            _ctx: Unused context id.\n        \"\"\"\n        self._transition(name, _TaskStatus.CANCELLED)\n\n    @Slot()\n    def _on_queue_empty(self) -&gt; None:\n        \"\"\"Schedule removal of finished segments after a short delay.\"\"\"\n        self._schedule_clear()\n\n    # ------------------------------------------------------------------\n    # Internal helpers\n    # ------------------------------------------------------------------\n\n    def _transition(self, name: str, status: _TaskStatus) -&gt; None:\n        \"\"\"Move the first active (pending/running) task with *name* to\n        *status*.\n\n        Args:\n            name: Task name to look up.\n            status: Target status.\n        \"\"\"\n        active = (_TaskStatus.PENDING, _TaskStatus.RUNNING)\n        for task in reversed(self._tasks):\n            if task.name == name and task.status in active:\n                task.status = status\n                break\n        self._schedule_update()\n\n        # If no active tasks remain, schedule cleanup so completed\n        # segments don't linger after pool tasks finish.\n        if self._tasks and not any(t.status in active for t in self._tasks):\n            self._schedule_clear()\n\n    def _schedule_update(self) -&gt; None:\n        \"\"\"Restart the debounce timer to coalesce rapid state changes.\"\"\"\n        self._update_timer.start()\n\n    def _schedule_clear(self) -&gt; None:\n        # ensure clear is never earlier than pending debounced paint cadence\n        delay = max(_CLEAR_DELAY_MS, self._update_timer.interval())\n        self._clear_timer.start(delay)\n\n    def _on_debounced_update(self) -&gt; None:\n        \"\"\"Slot called by the debounce timer; triggers a repaint and\n        tooltip.\"\"\"\n        self.update()\n\n    def _advance_pulse(self) -&gt; None:\n        \"\"\"Advance the pulse phase and repaint if any task is running.\"\"\"\n        self._pulse_phase = (self._pulse_phase + 0.06) % 1.0\n        has_running = any(t.status == _TaskStatus.RUNNING for t in self._tasks)\n        if has_running:\n            self.update()\n        else:\n            self._pulse_timer.stop()\n\n    def _clear_finished(self) -&gt; None:\n        \"\"\"Remove all terminal-state tasks, keeping only active ones.\"\"\"\n        # don't clear before pending debounced repaint runs\n        if self._update_timer.isActive():\n            self._clear_timer.start(self._update_timer.remainingTime() + 1)\n            return\n\n        terminal = (\n            _TaskStatus.COMPLETED,\n            _TaskStatus.FAILED,\n            _TaskStatus.CANCELLED,\n        )\n        self._tasks = [t for t in self._tasks if t.status not in terminal]\n        self.update()\n\n    def _active_task_name(self) -&gt; str:\n        \"\"\"Return the name of the first running task, or an empty string.\"\"\"\n        for task in self._tasks:\n            if task.status == _TaskStatus.RUNNING:\n                return task.name\n        return \"\"\n\n    def _build_tooltip(self) -&gt; str:\n        \"\"\"Compose the hover tooltip text from current task state.\n\n        Returns:\n            A multi-line string describing queue status, priority breakdown,\n            and the currently active task name.\n        \"\"\"\n        total = len(self._tasks)\n        if total == 0:\n            return \"No tasks\"\n\n        by_status: dict[_TaskStatus, int] = {s: 0 for s in _TaskStatus}\n        by_bucket: dict[str, int] = {\"high\": 0, \"medium\": 0, \"low\": 0}\n\n        for t in self._tasks:\n            by_status[t.status] += 1\n            by_bucket[_priority_bucket(t.priority)] += 1\n\n        lines: list[str] = [\n            f\"&lt;b&gt;Tasks: {total}&lt;/b&gt;\",\n            (\n                f\"Pending: {by_status[_TaskStatus.PENDING]}  \"\n                f\"Running: {by_status[_TaskStatus.RUNNING]}  \"\n                f\"Completed: {by_status[_TaskStatus.COMPLETED]}  \"\n                f\"Failed: {by_status[_TaskStatus.FAILED]}\"\n            ),\n        ]\n\n        # Priority breakdown percentages.\n        bucket_parts: list[str] = []\n        for bucket, label in (\n            (\"high\", \"High\"),\n            (\"medium\", \"Medium\"),\n            (\"low\", \"Low\"),\n        ):\n            count = by_bucket[bucket]\n            if count:\n                pct = round(100 * count / total)\n                bucket_parts.append(f\"{label}: {pct}%\")\n        if bucket_parts:\n            lines.append(\"Priority \u2014 \" + \"  \".join(bucket_parts))\n\n        active = self._active_task_name()\n        if active:\n            lines.append(f\"Active: &lt;i&gt;{active}&lt;/i&gt;\")\n\n        return \"&lt;br&gt;\".join(lines)\n\n    # ------------------------------------------------------------------\n    # Qt overrides\n    # ------------------------------------------------------------------\n\n    def event(self, event: QEvent) -&gt; bool:\n        \"\"\"Handle tooltip requests to provide dynamic hover content.\n\n        Args:\n            event: Incoming Qt event.\n\n        Returns:\n            Forwarded return value from the base implementation.\n        \"\"\"\n        if event.type() == QEvent.Type.Enter:\n            QToolTip.showText(\n                event.globalPos(),  # type: ignore\n                self._build_tooltip(),\n                self,\n                self.rect(),\n            )\n            return True\n        return super().event(event)\n\n    def paintEvent(self, _event: Any) -&gt; None:\n        \"\"\"Render the segmented progress bar.\n\n        Segments are drawn left-to-right in enqueue order.  Completed and\n        cancelled segments are dimmed; failed segments are drawn in red;\n        the running segment pulses.\n\n        Args:\n            _event: Unused paint event.\n        \"\"\"\n        tasks = self._tasks\n\n        painter = QPainter(self)\n        painter.setRenderHint(QPainter.RenderHint.Antialiasing)\n\n        painter.save()\n        painter.setBrush(self._bg_color)\n        painter.setPen(QPen(self._bg_outline_color, 1))\n        painter.drawRoundedRect(\n            self.rect(), self._bar_radius, self._bar_radius\n        )  # Background\n        painter.restore()\n\n        if not tasks:\n            return\n\n        margin = 3\n        width = self.width() - 2 * margin\n        height = self.height() - 2 * margin\n\n        n = len(tasks)\n        total_gap = self._segment_gap * (n - 1)\n        seg_w = max(1.0, (width - total_gap) / n)\n        h = float(height)\n\n        for i, task in enumerate(tasks):\n            x = i * (seg_w + self._segment_gap)\n\n            # Determine base colour.\n            if task.status == _TaskStatus.FAILED:\n                base_color = QColor(self._failed_color)\n            else:\n                base_color = QColor(\n                    self._priority_colors[_priority_bucket(task.priority)]\n                )\n\n            # Apply alpha based on status.\n            if task.status in (\n                _TaskStatus.COMPLETED,\n                _TaskStatus.CANCELLED,\n            ):\n                base_color.setAlphaF(self._completed_alpha)\n            elif task.status == _TaskStatus.RUNNING:\n                pulse = 0.65 + 0.35 * math.sin(self._pulse_phase * 2 * math.pi)\n                base_color.setAlphaF(pulse)\n            else:\n                base_color.setAlphaF(self._pending_alpha)\n\n            painter.setBrush(base_color)\n            painter.setPen(Qt.PenStyle.NoPen)\n\n            path = QPainterPath()\n            path.addRoundedRect(\n                QRectF(x + margin, margin, seg_w, h),\n                self._bar_radius,\n                self._bar_radius,\n            )\n            painter.drawPath(path)\n\n        painter.end()\n\n    def closeEvent(self, event: QCloseEvent) -&gt; None:\n        \"\"\"Clean up signal connections on widget destruction.\"\"\"\n        self._queue.task_enqueued.disconnect(self._on_task_enqueued)\n        self._queue.task_started.disconnect(self._on_task_started)\n        self._queue.task_completed.disconnect(self._on_task_completed)\n        self._queue.task_failed.disconnect(self._on_task_failed)\n        self._queue.task_cancelled.disconnect(self._on_task_cancelled)\n        self._queue.queue_empty.disconnect(self._on_queue_empty)\n        super().closeEvent(event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/task_queue_monitor.html#client.ayon_core.ui.components.task_queue_monitor.AsyncTaskQueueMonitor.__init__","title":"<code>__init__(task_queue, update_interval_ms=_DEBOUNCE_MS, parent=None)</code>","text":"<p>Initialise and connect to task_queue signals.</p> <p>Parameters:</p> Name Type Description Default <code>task_queue</code> <code>AsyncTaskQueue</code> <p>Queue instance to observe.</p> required <code>update_interval_ms</code> <code>int</code> <p>Debounce redraw interval in ms.</p> <code>_DEBOUNCE_MS</code> <code>parent</code> <code>Any</code> <p>Optional parent widget.</p> <code>None</code> Source code in <code>client/ayon_core/ui/components/task_queue_monitor.py</code> <pre><code>def __init__(\n    self,\n    task_queue: AsyncTaskQueue,\n    update_interval_ms: int = _DEBOUNCE_MS,\n    parent: Any = None,\n) -&gt; None:\n    \"\"\"Initialise and connect to *task_queue* signals.\n\n    Args:\n        task_queue: Queue instance to observe.\n        update_interval_ms: Debounce redraw interval in ms.\n        parent: Optional parent widget.\n    \"\"\"\n    super().__init__(parent)\n\n    self._queue: AsyncTaskQueue = task_queue\n    self._tasks: list[_TrackedTask] = []\n    self._pulse_phase: float = 0.0\n\n    # Load style configuration from AYON style system\n    style_data = get_ayon_style_data(\"AsyncTaskQueueMonitor\") or {}\n    style_data.set_context(self)\n    if not style_data:\n        log.warning(\n            \"AsyncTaskQueueMonitor: Style data not available, \"\n            \"using defaults\"\n        )\n\n    # Colors\n    self._bg_color = QColor(style_data.get(\"background-color\", \"#1c2026\"))\n    self._bg_outline_color = QColor(\n        style_data.get(\"outline-color\", \"#3F4552\")\n    )\n    self._priority_colors = {\n        \"high\": QColor(style_data.get(\"high-priority-color\", \"#E87D0D\")),\n        \"medium\": QColor(\n            style_data.get(\"medium-priority-color\", \"#4CAF50\")\n        ),\n        \"low\": QColor(style_data.get(\"low-priority-color\", \"#2196F3\")),\n    }\n    self._failed_color = QColor(style_data.get(\"failed-color\", \"#F44336\"))\n\n    # Alphas (opacity values)\n    self._completed_alpha = style_data.get(\"completed-alpha\", 0.45)\n    self._pending_alpha = style_data.get(\"pending-alpha\", 0.70)\n\n    # Dimensions\n    self._segment_gap = style_data.get(\"segment-gap\", 2)\n    self._bar_radius = style_data.get(\"bar-radius\", 3)\n\n    # Debounce timer \u2014 single-shot, restarted on every state change.\n    self._update_timer = QTimer(self)\n    self._update_timer.setSingleShot(True)\n    self._update_timer.setInterval(update_interval_ms)\n    self._update_timer.timeout.connect(self._on_debounced_update)\n\n    # Pulse animation timer.\n    self._pulse_timer = QTimer(self)\n    self._pulse_timer.setInterval(_PULSE_INTERVAL_MS)\n    self._pulse_timer.timeout.connect(self._advance_pulse)\n\n    # Clear timer.\n    self._clear_timer = QTimer(self)\n    self._clear_timer.setSingleShot(True)\n    self._clear_timer.timeout.connect(self._clear_finished)\n\n    # Connect queue signals.\n    task_queue.task_enqueued.connect(self._on_task_enqueued)\n    task_queue.task_started.connect(self._on_task_started)\n    task_queue.task_completed.connect(self._on_task_completed)\n    task_queue.task_failed.connect(self._on_task_failed)\n    task_queue.task_cancelled.connect(self._on_task_cancelled)\n    task_queue.queue_empty.connect(self._on_queue_empty)\n\n    # Widget appearance.\n    self.setMaximumHeight(self.MAX_HEIGHT)\n    self.setMinimumHeight(self.MAX_HEIGHT)\n    self.setSizePolicy(\n        QSizePolicy.Policy.Expanding, QSizePolicy.Policy.Fixed\n    )\n    self.setAttribute(Qt.WidgetAttribute.WA_Hover, True)\n    self.setMouseTracking(True)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/task_queue_monitor.html#client.ayon_core.ui.components.task_queue_monitor.AsyncTaskQueueMonitor.closeEvent","title":"<code>closeEvent(event)</code>","text":"<p>Clean up signal connections on widget destruction.</p> Source code in <code>client/ayon_core/ui/components/task_queue_monitor.py</code> <pre><code>def closeEvent(self, event: QCloseEvent) -&gt; None:\n    \"\"\"Clean up signal connections on widget destruction.\"\"\"\n    self._queue.task_enqueued.disconnect(self._on_task_enqueued)\n    self._queue.task_started.disconnect(self._on_task_started)\n    self._queue.task_completed.disconnect(self._on_task_completed)\n    self._queue.task_failed.disconnect(self._on_task_failed)\n    self._queue.task_cancelled.disconnect(self._on_task_cancelled)\n    self._queue.queue_empty.disconnect(self._on_queue_empty)\n    super().closeEvent(event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/task_queue_monitor.html#client.ayon_core.ui.components.task_queue_monitor.AsyncTaskQueueMonitor.event","title":"<code>event(event)</code>","text":"<p>Handle tooltip requests to provide dynamic hover content.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <code>QEvent</code> <p>Incoming Qt event.</p> required <p>Returns:</p> Type Description <code>bool</code> <p>Forwarded return value from the base implementation.</p> Source code in <code>client/ayon_core/ui/components/task_queue_monitor.py</code> <pre><code>def event(self, event: QEvent) -&gt; bool:\n    \"\"\"Handle tooltip requests to provide dynamic hover content.\n\n    Args:\n        event: Incoming Qt event.\n\n    Returns:\n        Forwarded return value from the base implementation.\n    \"\"\"\n    if event.type() == QEvent.Type.Enter:\n        QToolTip.showText(\n            event.globalPos(),  # type: ignore\n            self._build_tooltip(),\n            self,\n            self.rect(),\n        )\n        return True\n    return super().event(event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/task_queue_monitor.html#client.ayon_core.ui.components.task_queue_monitor.AsyncTaskQueueMonitor.paintEvent","title":"<code>paintEvent(_event)</code>","text":"<p>Render the segmented progress bar.</p> <p>Segments are drawn left-to-right in enqueue order.  Completed and cancelled segments are dimmed; failed segments are drawn in red; the running segment pulses.</p> <p>Parameters:</p> Name Type Description Default <code>_event</code> <code>Any</code> <p>Unused paint event.</p> required Source code in <code>client/ayon_core/ui/components/task_queue_monitor.py</code> <pre><code>def paintEvent(self, _event: Any) -&gt; None:\n    \"\"\"Render the segmented progress bar.\n\n    Segments are drawn left-to-right in enqueue order.  Completed and\n    cancelled segments are dimmed; failed segments are drawn in red;\n    the running segment pulses.\n\n    Args:\n        _event: Unused paint event.\n    \"\"\"\n    tasks = self._tasks\n\n    painter = QPainter(self)\n    painter.setRenderHint(QPainter.RenderHint.Antialiasing)\n\n    painter.save()\n    painter.setBrush(self._bg_color)\n    painter.setPen(QPen(self._bg_outline_color, 1))\n    painter.drawRoundedRect(\n        self.rect(), self._bar_radius, self._bar_radius\n    )  # Background\n    painter.restore()\n\n    if not tasks:\n        return\n\n    margin = 3\n    width = self.width() - 2 * margin\n    height = self.height() - 2 * margin\n\n    n = len(tasks)\n    total_gap = self._segment_gap * (n - 1)\n    seg_w = max(1.0, (width - total_gap) / n)\n    h = float(height)\n\n    for i, task in enumerate(tasks):\n        x = i * (seg_w + self._segment_gap)\n\n        # Determine base colour.\n        if task.status == _TaskStatus.FAILED:\n            base_color = QColor(self._failed_color)\n        else:\n            base_color = QColor(\n                self._priority_colors[_priority_bucket(task.priority)]\n            )\n\n        # Apply alpha based on status.\n        if task.status in (\n            _TaskStatus.COMPLETED,\n            _TaskStatus.CANCELLED,\n        ):\n            base_color.setAlphaF(self._completed_alpha)\n        elif task.status == _TaskStatus.RUNNING:\n            pulse = 0.65 + 0.35 * math.sin(self._pulse_phase * 2 * math.pi)\n            base_color.setAlphaF(pulse)\n        else:\n            base_color.setAlphaF(self._pending_alpha)\n\n        painter.setBrush(base_color)\n        painter.setPen(Qt.PenStyle.NoPen)\n\n        path = QPainterPath()\n        path.addRoundedRect(\n            QRectF(x + margin, margin, seg_w, h),\n            self._bar_radius,\n            self._bar_radius,\n        )\n        painter.drawPath(path)\n\n    painter.end()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/text_box.html","title":"text_box","text":""},{"location":"autoapi/client/ayon_core/ui/components/text_box.html#client.ayon_core.ui.components.text_box.AYTextBox","title":"<code>AYTextBox</code>","text":"<p>               Bases: <code>AYContainer</code></p> Source code in <code>client/ayon_core/ui/components/text_box.py</code> <pre><code>class AYTextBox(AYContainer):\n    signals = AYTextBoxSignals()\n    Variants = QFrameVariants\n    style_icons = {\n        \"stl_h1\": \"format_h1\",\n        \"stl_bold\": \"format_bold\",\n        \"stl_italic\": \"format_italic\",\n        \"stl_link\": \"link\",\n        \"stl_code\": \"code\",\n    }\n    format_icons = {\n        \"fmt_number\": \"format_list_numbered\",\n        \"fmt_bullet\": \"format_list_bulleted\",\n        \"fmt_checklist\": \"checklist\",\n    }\n    mention_map = {\n        \"person\": \"@\",\n        # TODO: Implement support for version and task mentions in completer\n        #  before enabling these\n        # \"layers\": \"@@\",\n        # \"check_circle\": \"@@@\",\n    }\n\n    def __init__(\n        self,\n        *args,\n        num_lines=0,\n        show_categories=False,\n        user_list: list[User] | None = None,\n        variant: Variants = Variants.Default,\n        **kwargs,\n    ):\n        self._variant_str: str = variant.value\n        super().__init__(\n            *args,\n            layout=AYContainer.Layout.VBox,\n            variant=variant,\n            margin=0,\n            **kwargs,\n        )\n\n        self.show_categories = show_categories\n        self.comment_categories: list[dict] = _dict_from_comment_category([])\n        self.category = self.comment_categories[0][\"text\"]\n        self._user_list: list[User] = user_list or []\n        # Store attachments - unified list for both screenshots and files\n        # List of dicts:\n        #   {'type': 'screenshot'|'file', 'path': str, 'filename': str}\n        self._attachments = []\n        self.screenshot_handler = None  # Will be initialized in _build\n        self._build(num_lines)\n\n    def _build_upper_bar(self):\n        grp_spacing = 16\n        lyt = AYHBoxLayout(spacing=0, margin=0)\n        # comment category if available\n        if self.show_categories:\n            self.com_cat = AYComboBox(\n                parent=self, items=self.comment_categories\n            )\n            self.com_cat.currentTextChanged.connect(self._on_category_changed)\n            lyt.addWidget(self.com_cat)\n        lyt.addStretch()\n        # styling buttons\n        for var, icn in self.style_icons.items():\n            setattr(\n                self,\n                var,\n                AYButton(self, variant=AYButton.Variants.Nav, icon=icn),\n            )\n            lyt.addWidget(getattr(self, var))\n        # formatting buttons\n        for var, icn in self.format_icons.items():\n            setattr(\n                self,\n                var,\n                AYButton(self, variant=AYButton.Variants.Nav, icon=icn),\n            )\n            lyt.addWidget(getattr(self, var))\n        lyt.addSpacing(grp_spacing)\n        self.screenshot_btn = AYButton(\n            self, variant=AYButton.Variants.Nav, icon=\"photo_camera\"\n        )\n        lyt.addWidget(self.screenshot_btn)\n        self.attach_file_btn = AYButton(\n            self, variant=AYButton.Variants.Nav, icon=\"attach_file\"\n        )\n        self.attach_file_btn.clicked.connect(self._on_attach_file_clicked)\n        lyt.addWidget(self.attach_file_btn)\n        return lyt\n\n    def _build_attachment_area(self):\n        \"\"\"Build the unified scrollable attachment display area for both\n        screenshots and files.\"\"\"\n        # Container for attachments\n        self.attachment_container = QtWidgets.QWidget(self)\n        self.attachment_layout = AYHBoxLayout(\n            self.attachment_container, margin=4, spacing=4\n        )\n\n        # Scroll area\n        self.attachment_scroll = QScrollArea(self)\n        self.attachment_scroll.setWidget(self.attachment_container)\n        self.attachment_scroll.setWidgetResizable(True)\n        self.attachment_scroll.setFixedHeight(100)\n        self.attachment_scroll.setHorizontalScrollBarPolicy(\n            Qt.ScrollBarPolicy.ScrollBarAsNeeded\n        )\n        self.attachment_scroll.setVerticalScrollBarPolicy(\n            Qt.ScrollBarPolicy.ScrollBarAlwaysOff\n        )\n        self.attachment_scroll.hide()  # Hidden by default\n\n        return self.attachment_scroll\n\n    def _build_edit_field(self, num_lines):\n        self.edit_field = AYTextEditor(\n            self,\n            num_lines=num_lines,\n            user_list=self._user_list,\n            variant=AYTextEditor.Variants.Default,\n        )\n        for var in self.style_icons:\n            getattr(self, var).clicked.connect(\n                partial(self.edit_field.set_style, var)\n            )\n        for var in self.format_icons:\n            getattr(self, var).clicked.connect(\n                partial(self.edit_field.set_format, var)\n            )\n\n        self.edit_field.submitted.connect(self._on_comment_clicked)\n\n        return self.edit_field\n\n    def _build_lower_bar(self):\n        lyt = AYHBoxLayout(margin=0, spacing=0)\n\n        for icn, mention in self.mention_map.items():\n            btn = AYButton(self, variant=AYButton.Variants.Nav, icon=icn)\n            btn.clicked.connect(partial(self._add_mention_to_editor, mention))\n            lyt.addWidget(btn)\n\n        lyt.addSpacerItem(\n            QtWidgets.QSpacerItem(0, 0, QSizePolicy.Policy.MinimumExpanding)\n        )\n        self.comment_button = AYButton(\n            \"Comment\", variant=AYButton.Variants.Filled\n        )\n        self.comment_button.clicked.connect(self._on_comment_clicked)\n        lyt.addWidget(self.comment_button)\n        return lyt\n\n    def _on_comment_clicked(self) -&gt; None:\n        \"\"\"Handle comment button click and emit signal with markdown\n        content.\"\"\"\n        markdown_content = self.edit_field.as_markdown()\n\n        # Get all attachment paths\n        all_attachment_paths = [att[\"path\"] for att in self._attachments]\n\n        self.signals.comment_submitted.emit(\n            markdown_content, self.category, all_attachment_paths\n        )\n        self.edit_field.clear()\n        if self.show_categories:\n            self.com_cat.setCurrentIndex(0)\n        self.clear_all_attachments()\n\n        # Clear screenshots after submission\n        if self.screenshot_handler:\n            self.screenshot_handler.clear_screenshots()\n\n    def _add_mention_to_editor(self, mention: str) -&gt; None:\n        \"\"\"Add mention text to the editor at cursor position.\"\"\"\n        cursor = self.edit_field.textCursor()\n        cursor.insertText(mention)\n        self.edit_field.setTextCursor(cursor)\n        self.edit_field.setFocus()\n\n    def _on_category_changed(self, category: str) -&gt; None:\n        self.category = category if category != NO_CATEGORY[\"text\"] else \"\"\n\n    def _on_attach_file_clicked(self) -&gt; None:\n        \"\"\"Handle attach file button click and open file dialog.\"\"\"\n        file_paths, _ = QtWidgets.QFileDialog.getOpenFileNames(\n            self,\n            \"Select files to attach\",\n            \"\",\n            \"Image Files (*.png *.jpeg *.jpg);;All Files (*)\",\n        )\n\n        if file_paths:\n            for file_path in file_paths:\n                self.add_attachment(file_path, \"file\")\n\n    def _on_attachment_removed(self, index: int, attachment_type: str) -&gt; None:\n        \"\"\"Handle removal of an attachment.\"\"\"\n        if 0 &lt;= index &lt; len(self._attachments):\n            attachment = self._attachments[index]\n            # Optionally delete temp files (screenshots)\n            if attachment[\"type\"] == \"screenshot\":\n                file_path = attachment[\"path\"]\n                if os.path.exists(file_path):\n                    try:\n                        os.remove(file_path)\n                    except Exception as e:\n                        logger.warning(\n                            f\"Failed to remove temp file {file_path}: {e}\"\n                        )\n\n            self._attachments.pop(index)\n            self._refresh_attachment_display()\n            self._update_attachment_buttons()\n\n    def _on_thumbnail_clicked(self, index: int, attachment_type: str) -&gt; None:\n        \"\"\"Handle thumbnail click to open gallery.\"\"\"\n        from .gallery_dialog import GalleryDialog\n\n        if not self._attachments:\n            return\n\n        # Prepare images list for GalleryDialog\n        images = [(att[\"path\"], att[\"filename\"]) for att in self._attachments]\n\n        dialog = GalleryDialog(images, current_index=index, parent=self)\n        dialog.setWindowTitle(\"Attachments Preview\")\n        dialog.exec_()\n\n    def _refresh_attachment_display(self) -&gt; None:\n        \"\"\"Refresh the unified attachment display area.\"\"\"\n        # Clear existing widgets\n        while self.attachment_layout.count():\n            item = self.attachment_layout.takeAt(0)\n            widget = item.widget()\n            if widget is not None:\n                widget.deleteLater()\n\n        # Add attachment widgets\n        if self._attachments:\n            for idx, attachment in enumerate(self._attachments):\n                widget = AttachmentWidget(\n                    parent=self.attachment_container,\n                    index=idx,\n                    filename=attachment[\"filename\"],\n                    file_path=attachment[\"path\"],\n                    attachment_type=attachment[\"type\"],\n                )\n                widget.remove_clicked.connect(self._on_attachment_removed)\n                widget.thumbnail_clicked.connect(self._on_thumbnail_clicked)\n                self.attachment_layout.addWidget(widget)\n\n            self.attachment_layout.addStretch()\n            self.attachment_scroll.show()\n        else:\n            self.attachment_scroll.hide()\n\n        self.attachment_container.update()\n        self.attachment_scroll.viewport().update()\n\n    def add_attachment(\n        self, file_path: str, attachment_type: str = \"file\"\n    ) -&gt; None:\n        \"\"\"Add a single attachment (screenshot or file).\n\n        Args:\n            file_path: Path to the file\n            attachment_type: 'screenshot' or 'file'\n        \"\"\"\n        if not file_path or file_path in [\n            att[\"path\"] for att in self._attachments\n        ]:\n            return\n\n        filename = os.path.basename(file_path)\n        if attachment_type == \"screenshot\":\n            # Generate screenshot number\n            screenshot_count = sum(\n                1 for att in self._attachments if att[\"type\"] == \"screenshot\"\n            )\n            filename = f\"Screenshot {screenshot_count + 1}\"\n\n        self._attachments.append(\n            {\"type\": attachment_type, \"path\": file_path, \"filename\": filename}\n        )\n\n        self._refresh_attachment_display()\n        self._update_attachment_buttons()\n\n    def _update_attachment_buttons(self) -&gt; None:\n        \"\"\"Update button badges to show counts.\"\"\"\n        screenshot_count = sum(\n            1 for att in self._attachments if att[\"type\"] == \"screenshot\"\n        )\n        file_count = sum(\n            1 for att in self._attachments if att[\"type\"] == \"file\"\n        )\n\n        # Update screenshot button\n        if screenshot_count &gt; 0:\n            self.screenshot_btn.setText(f\"{screenshot_count}\")\n            self.screenshot_btn.setStyleSheet(\n                \"background-color: rgba(92, 173, 214, .4);\"\n            )\n        else:\n            self.screenshot_btn.setText(\"\")\n            self.screenshot_btn.setStyleSheet(\"\")\n\n        # Update attach file button\n        if file_count &gt; 0:\n            self.attach_file_btn.setText(f\"{file_count}\")\n            self.attach_file_btn.setStyleSheet(\n                \"background-color: rgba(92, 173, 214, .4);\"\n            )\n        else:\n            self.attach_file_btn.setText(\"\")\n            self.attach_file_btn.setStyleSheet(\"\")\n\n    def clear_all_attachments(self) -&gt; None:\n        \"\"\"Clear all attachments.\"\"\"\n        self._attachments.clear()\n        self._refresh_attachment_display()\n        self._update_attachment_buttons()\n\n    def get_attachments(self) -&gt; list[dict]:\n        \"\"\"Get the current list of attachments.\n\n        Returns:\n            List of attachment dictionaries\n        \"\"\"\n        return self._attachments.copy()\n\n    @Slot(ProjectData)\n    def on_ctlr_project_changed(self, data: ProjectData):\n        self.comment_categories = _dict_from_comment_category(\n            data.comment_category\n        )\n        if self.show_categories:\n            self.com_cat.update_items(self.comment_categories)\n        self.edit_field._user_list = self._user_list = data.users\n        on_users_updated(self.edit_field)\n\n    def _build(self, num_lines):\n        self.add_layout(self._build_upper_bar())\n\n        # Initialize screenshot handler after screenshot_btn is created\n        from .screenshot_capture import ScreenshotHandler\n\n        self.screenshot_handler = ScreenshotHandler(self, self.screenshot_btn)\n\n        # Click to capture, but if screenshots exist, show gallery\n        self.screenshot_btn.clicked.connect(self._on_screenshot_btn_clicked)\n\n        self.add_widget(\n            self._build_attachment_area()\n        )  # Add unified attachment area\n        self.add_widget(self._build_edit_field(num_lines), stretch=10)\n        self.add_layout(self._build_lower_bar())\n\n    def set_markdown(self, md: str):\n        \"\"\"Set markdown content with checkbox support.\n\n        Args:\n            md: Markdown text to display\n        \"\"\"\n        self.edit_field.set_markdown(md)\n\n    def _on_screenshot_btn_clicked(self):\n        \"\"\"Handle screenshot button click - always capture new screenshot.\"\"\"\n        self.screenshot_handler.launch_capture()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/text_box.html#client.ayon_core.ui.components.text_box.AYTextBox.add_attachment","title":"<code>add_attachment(file_path, attachment_type='file')</code>","text":"<p>Add a single attachment (screenshot or file).</p> <p>Parameters:</p> Name Type Description Default <code>file_path</code> <code>str</code> <p>Path to the file</p> required <code>attachment_type</code> <code>str</code> <p>'screenshot' or 'file'</p> <code>'file'</code> Source code in <code>client/ayon_core/ui/components/text_box.py</code> <pre><code>def add_attachment(\n    self, file_path: str, attachment_type: str = \"file\"\n) -&gt; None:\n    \"\"\"Add a single attachment (screenshot or file).\n\n    Args:\n        file_path: Path to the file\n        attachment_type: 'screenshot' or 'file'\n    \"\"\"\n    if not file_path or file_path in [\n        att[\"path\"] for att in self._attachments\n    ]:\n        return\n\n    filename = os.path.basename(file_path)\n    if attachment_type == \"screenshot\":\n        # Generate screenshot number\n        screenshot_count = sum(\n            1 for att in self._attachments if att[\"type\"] == \"screenshot\"\n        )\n        filename = f\"Screenshot {screenshot_count + 1}\"\n\n    self._attachments.append(\n        {\"type\": attachment_type, \"path\": file_path, \"filename\": filename}\n    )\n\n    self._refresh_attachment_display()\n    self._update_attachment_buttons()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/text_box.html#client.ayon_core.ui.components.text_box.AYTextBox.clear_all_attachments","title":"<code>clear_all_attachments()</code>","text":"<p>Clear all attachments.</p> Source code in <code>client/ayon_core/ui/components/text_box.py</code> <pre><code>def clear_all_attachments(self) -&gt; None:\n    \"\"\"Clear all attachments.\"\"\"\n    self._attachments.clear()\n    self._refresh_attachment_display()\n    self._update_attachment_buttons()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/text_box.html#client.ayon_core.ui.components.text_box.AYTextBox.get_attachments","title":"<code>get_attachments()</code>","text":"<p>Get the current list of attachments.</p> <p>Returns:</p> Type Description <code>list[dict]</code> <p>List of attachment dictionaries</p> Source code in <code>client/ayon_core/ui/components/text_box.py</code> <pre><code>def get_attachments(self) -&gt; list[dict]:\n    \"\"\"Get the current list of attachments.\n\n    Returns:\n        List of attachment dictionaries\n    \"\"\"\n    return self._attachments.copy()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/text_box.html#client.ayon_core.ui.components.text_box.AYTextBox.set_markdown","title":"<code>set_markdown(md)</code>","text":"<p>Set markdown content with checkbox support.</p> <p>Parameters:</p> Name Type Description Default <code>md</code> <code>str</code> <p>Markdown text to display</p> required Source code in <code>client/ayon_core/ui/components/text_box.py</code> <pre><code>def set_markdown(self, md: str):\n    \"\"\"Set markdown content with checkbox support.\n\n    Args:\n        md: Markdown text to display\n    \"\"\"\n    self.edit_field.set_markdown(md)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/text_box.html#client.ayon_core.ui.components.text_box.AYTextEditor","title":"<code>AYTextEditor</code>","text":"<p>               Bases: <code>AYTextEdit</code></p> Source code in <code>client/ayon_core/ui/components/text_box.py</code> <pre><code>class AYTextEditor(AYTextEdit):\n    Variants = QTextEditVariants\n\n    submitted = Signal()  # Signal emitted when Ctrl+Enter is pressed\n    checklist_changed = Signal()  # Signal emitted when checkbox state changes\n\n    def __init__(\n        self,\n        *args,\n        num_lines: int = 0,\n        read_only: bool = False,\n        user_list: list[User] | None,\n        variant: Variants = Variants.Default,\n        **kwargs,\n    ):\n        # remove our kwargs\n        self.num_lines: int = num_lines\n        self._read_only: bool = read_only\n        self._user_list: list[User] = user_list or []\n        self._variant_str: str = variant.value\n        self._checkbox_handler: CheckboxHandler | None = None\n        # Guard flag: when True, format_comment_on_change is a no-op.\n        self._suppress_formatting: bool = False\n\n        super().__init__(*args, variant=variant, **kwargs)\n        self.setStyle(get_ayon_style())\n        # Enable mouse tracking on viewport to receive mouseMoveEvent\n        self.viewport().setMouseTracking(True)\n\n        self.document().setIndentWidth(22)  # pixels per indent level\n\n        if self.num_lines:\n            doc = self.document()\n            fm = self.fontMetrics()\n            frame_w = self.frameWidth()\n            cm = self.contentsMargins()\n            height = (\n                fm.lineSpacing() * self.num_lines\n                + int(doc.documentMargin()) * 2\n                + frame_w * 2\n                + cm.top()\n                + cm.bottom()\n            )\n            self.setFixedHeight(height)\n\n        self.setSizePolicy(\n            QSizePolicy.Policy.Expanding,\n            QSizePolicy.Policy.Expanding\n            if self.num_lines\n            else QSizePolicy.Policy.Fixed,\n        )\n\n        # automatic bullet lists\n        self.setAutoFormatting(QTextEdit.AutoFormattingFlag.AutoAll)\n\n        if not self._read_only:\n            self.setPlaceholderText(\n                \"Comment or mention with @user, @@version, @@@task...\"\n            )\n        self.setReadOnly(self._read_only)\n        palette = self.palette()\n        palette.setColor(QPalette.ColorRole.PlaceholderText, QColor(\"white\"))\n        self.setPalette(palette)\n        # Setup user completer\n        setup_user_completer(\n            self,\n            self._on_completer_activated,\n            self._on_text_changed,\n        )\n\n        self.document().contentsChanged.connect(self._on_contents_changed)\n\n    def _on_contents_changed(self) -&gt; None:\n        \"\"\"Forward contentsChanged to format_comment_on_change.\n\n        Skipped when ``_suppress_formatting`` is True so that checkbox\n        insertion code can mutate the document without triggering a\n        full re-format pass.\n        \"\"\"\n        if not self._suppress_formatting:\n            format_comment_on_change(self)\n            apply_code_block_backgrounds(self)\n\n    def _on_text_changed(self) -&gt; None:\n        \"\"\"Handle text changes to show/hide completer.\"\"\"\n        on_completer_text_changed(self)\n\n    def _on_completer_activated(self, text: str) -&gt; None:\n        \"\"\"Handle completer selection.\"\"\"\n        on_completer_activated(self, text)\n\n    def keyPressEvent(self, event) -&gt; None:\n        \"\"\"Handle key press events for completer.\"\"\"\n        if on_completer_key_press(self, event):\n            event.accept()\n            return\n\n        # ctrl/cmd-enter to submit\n        if (\n            event.key() in {Qt.Key.Key_Return, Qt.Key.Key_Enter}\n            and event.modifiers() &amp; Qt.KeyboardModifier.ControlModifier\n        ):\n            self.submitted.emit()\n\n        # Auto-continue checkbox on Enter\n        if (\n            event.key() in {Qt.Key.Key_Return, Qt.Key.Key_Enter}\n            and self._checkbox_handler\n            and self._checkbox_handler.has_checkboxes()\n        ):\n            cursor = self.textCursor()\n            block_text = cursor.block().text()\n\n            if \"\\ufffc\" in block_text:\n                # Extract text after the checkbox object char\n                parts = block_text.split(\"\\ufffc\", 1)\n                after_checkbox = parts[1].strip() if len(parts) &gt; 1 else \"\"\n\n                if not after_checkbox:\n                    # Empty checkbox line \u2192 end the list\n                    # Remove the checkbox content from current block\n                    cursor.movePosition(\n                        QTextCursor.MoveOperation.StartOfBlock,\n                        QTextCursor.MoveMode.MoveAnchor,\n                    )\n                    cursor.movePosition(\n                        QTextCursor.MoveOperation.EndOfBlock,\n                        QTextCursor.MoveMode.KeepAnchor,\n                    )\n                    cursor.removeSelectedText()\n                    # Remove the last checkbox from handler\n                    self._checkbox_handler.remove_last_checkbox()\n                    # Insert a plain newline\n                    super().keyPressEvent(event)\n                    return\n\n                # Non-empty checkbox line \u2192 insert new checkbox\n                event.accept()\n                self._suppress_formatting = True\n\n                cursor.beginEditBlock()\n                cursor.movePosition(QTextCursor.MoveOperation.EndOfBlock)\n                cursor.insertBlock()\n                self._insert_checkbox_at_cursor(cursor)\n                cursor.endEditBlock()\n\n                self._suppress_formatting = False\n                self.setTextCursor(cursor)\n                return\n\n        super().keyPressEvent(event)\n\n    def _setup_checkbox_handler(self) -&gt; None:\n        \"\"\"Initialize checkbox handler if not already done.\"\"\"\n        if self._checkbox_handler is None:\n            self._checkbox_handler = CheckboxHandler(self)\n            self._checkbox_handler.checklist_changed.connect(\n                self._on_checklist_changed\n            )\n\n    def _on_checklist_changed(self) -&gt; None:\n        \"\"\"Handle checkbox state changes.\"\"\"\n        self.checklist_changed.emit()\n\n    def _insert_checkbox_at_cursor(self, cursor: QTextCursor) -&gt; None:\n        \"\"\"Insert a new unchecked checkbox object at the cursor position.\n\n        Inserts a two-space prefix, the custom checkbox object character\n        (``\\\\ufffc``), and a trailing space, then records the document\n        position on the :class:`CheckboxItem` for fast hit-testing.\n        The checkbox handler must already be initialised via\n        :meth:`_setup_checkbox_handler`.\n\n        Args:\n            cursor: Cursor at the insertion point; advanced past the\n                inserted characters on return.\n        \"\"\"\n        self._setup_checkbox_handler()\n        new_item = self._checkbox_handler.add_checkbox()\n        fmt = QTextCharFormat()\n        fmt.setObjectType(CHECKBOX_FORMAT_TYPE)\n        fmt.setProperty(CHECKBOX_CHECKED_PROP, False)\n        fmt.setProperty(CHECKBOX_INDEX_PROP, new_item.index)\n        fmt.setVerticalAlignment(\n            QTextCharFormat.VerticalAlignment.AlignBaseline\n        )\n        cursor.insertText(\"  \")\n        new_item.doc_position = cursor.position()\n        cursor.insertText(\"\\ufffc\", fmt)\n        cursor.insertText(\" \")\n\n    def set_markdown(self, md: str) -&gt; None:\n        \"\"\"Set markdown content with checkbox support.\n\n        Args:\n            md: Markdown text to display\n        \"\"\"\n        if CheckboxHandler.contains_checkboxes(md):\n            self._setup_checkbox_handler()\n            self._checkbox_handler.parse_and_render(md)\n        else:\n            self.document().setMarkdown(md, MD_DIALECT)\n        apply_code_block_backgrounds(self)\n\n    def as_markdown(self) -&gt; str:\n        \"\"\"Get the content as GitHub-flavored markdown.\n\n        Returns:\n            Markdown string.\n        \"\"\"\n        if self._checkbox_handler and self._checkbox_handler.has_checkboxes():\n            return self._checkbox_handler.to_markdown()\n        rendered_md = self.document().toMarkdown(MD_DIALECT)\n        return strip_user_mention_display(rendered_md, self._user_list)\n\n    def _is_checkbox_at_cursor(\n        self, click_pos: QPoint\n    ) -&gt; tuple[bool, int | None]:\n        \"\"\"Check if a click position hits a checkbox bounding rect.\n\n        Delegates to :meth:`CheckboxHandler.find_checkbox_at_click` which\n        uses stored document positions instead of scanning the full text.\n\n        Args:\n            click_pos: QPoint from event.pos(), viewport coords.\n\n        Returns:\n            Tuple of (is_checkbox, document_position_for_lookup).\n        \"\"\"\n        if not self._checkbox_handler:\n            return False, None\n\n        # Account for scroll offset\n        scroll_offset = QPoint(\n            -self.horizontalScrollBar().value(),\n            -self.verticalScrollBar().value(),\n        )\n        result = self._checkbox_handler.find_checkbox_at_click(\n            click_pos, scroll_offset\n        )\n        if result is None:\n            return False, None\n        return result\n\n    def mousePressEvent(self, event) -&gt; None:\n        \"\"\"Handle mouse press events for checkboxes.\n\n        Checkboxes can be toggled even in read-only mode.\n        \"\"\"\n        is_cb, cb_pos = self._is_checkbox_at_cursor(event.pos())\n        if is_cb and cb_pos is not None:\n            assert self._checkbox_handler is not None  # implied by is_cb==True\n            checkbox_idx = self._checkbox_handler.get_checkbox_at_position(\n                cb_pos\n            )\n            if checkbox_idx is not None:\n                self._checkbox_handler.toggle_checkbox(checkbox_idx)\n                self.viewport().update()\n                event.accept()\n                return\n\n        super().mousePressEvent(event)\n\n    def mouseMoveEvent(self, event):\n        \"\"\"Show arrow cursor over checkboxes, hand over links, I-beam else.\"\"\"\n        pos = event.pos()\n        cursor = self.cursorForPosition(pos)\n        fmt = cursor.charFormat()\n        if fmt.objectType() == CHECKBOX_FORMAT_TYPE:\n            self.viewport().setCursor(Qt.CursorShape.ArrowCursor)\n        elif fmt.isAnchor():\n            self.viewport().setCursor(Qt.CursorShape.PointingHandCursor)\n        else:\n            self.viewport().setCursor(Qt.CursorShape.IBeamCursor)\n        super().mouseMoveEvent(event)\n\n    def set_style(self, style: str) -&gt; None:\n        \"\"\"Apply a character style to the current selection or cursor.\n\n        Styles are merged with the existing character formatting so that\n        multiple styles (e.g. bold + italic) can be combined without\n        removing previously applied styles.\n\n        Args:\n            style: Style identifier string (e.g. ``\"stl_bold\"``).\n        \"\"\"\n        cursor = self.textCursor()\n\n        # Suppress formatting during style application\n        self._suppress_formatting = True\n\n        cursor.beginEditBlock()\n\n        if style == \"stl_bold\":\n            # Toggle bold, preserving all other formatting\n            current_weight = cursor.charFormat().fontWeight()\n            new_weight = (\n                QFont.Weight.Normal\n                if current_weight == QFont.Weight.Bold\n                else QFont.Weight.Bold\n            )\n            fmt = QTextCharFormat()\n            fmt.setFontWeight(new_weight)\n\n            if cursor.hasSelection():\n                cursor.mergeCharFormat(fmt)\n            else:\n                self.mergeCurrentCharFormat(fmt)\n\n            self.setTextCursor(cursor)\n\n        elif style == \"stl_italic\":\n            # Toggle italic, preserving all other formatting\n            new_italic = not cursor.charFormat().fontItalic()\n            fmt = QTextCharFormat()\n            fmt.setFontItalic(new_italic)\n\n            if cursor.hasSelection():\n                cursor.mergeCharFormat(fmt)\n            else:\n                self.mergeCurrentCharFormat(fmt)\n\n            self.setTextCursor(cursor)\n\n        elif style == \"stl_h1\":\n            # Toggle heading size, preserving all other formatting\n            base_size = self.font().pointSizeF()\n            current_size = cursor.charFormat().fontPointSize()\n\n            fmt = QTextCharFormat()\n            if current_size &gt; base_size:  # Already a header\n                fmt.setFontPointSize(base_size)\n                fmt.setFontWeight(QFont.Weight.Normal)\n            else:  # Make it a header\n                fmt.setFontPointSize(base_size * 1.5)\n                fmt.setFontWeight(QFont.Weight.Bold)\n\n            if cursor.hasSelection():\n                cursor.mergeCharFormat(fmt)\n            else:\n                self.mergeCurrentCharFormat(fmt)\n\n            self.setTextCursor(cursor)\n\n        elif style == \"stl_link\":\n            pw = self.parentWidget()\n            if not pw:\n                return\n\n            selected_text = (\n                cursor.selectedText() if cursor.hasSelection() else \"\"\n            )\n            field = QtWidgets.QLineEdit(selected_text, parent=pw)\n\n            def _make_link():\n                link = field.text()\n                fmt = QTextCharFormat()\n                fmt.setAnchor(True)\n                fmt.setAnchorHref(link)\n                fmt.setFontUnderline(True)\n\n                if cursor.hasSelection():\n                    cursor.mergeCharFormat(fmt)\n                else:\n                    self.mergeCurrentCharFormat(fmt)\n\n                field.close()\n                field.deleteLater()\n                self.setFocus()\n                self.update()\n\n            # open link edit field\n            field.show()\n            fr = field.rect()\n            field.setGeometry(4, 0, self.rect().width(), fr.height())\n            field.selectAll()\n            field.setFocus()\n            field.returnPressed.connect(_make_link)\n\n        elif style == \"stl_code\":\n            # Detect if already in code style\n            is_code = cursor.charFormat().fontFixedPitch()\n            fmt = QTextCharFormat()\n\n            if is_code:\n                # Toggle off: revert code-specific properties to widget\n                # defaults\n                fmt.setFontFixedPitch(False)\n                fmt.setFontFamilies([self.font().family()])\n                fmt.setBackground(\n                    self.palette().color(QPalette.ColorRole.Base)\n                )\n                fmt.setForeground(\n                    self.palette().color(QPalette.ColorRole.Text)\n                )\n            else:\n                # Toggle on: apply code style\n                fmt.setFontFixedPitch(True)\n                fmt.setFontFamilies([\"Noto Sans Mono\"])\n                fmt.setBackground(CODE_BG)\n                fmt.setForeground(CODE_FG)\n\n            if cursor.hasSelection():\n                cursor.mergeCharFormat(fmt)\n            else:\n                self.mergeCurrentCharFormat(fmt)\n\n            self.setTextCursor(cursor)\n\n        cursor.endEditBlock()\n        self._suppress_formatting = False\n        self.setFocus()\n\n    def _insert_fmt_checklist(self) -&gt; None:\n        \"\"\"Insert an unchecked checklist checkbox at the current cursor.\n\n        If the cursor is on a bullet or numbered list line, the list\n        item is converted into a checkbox line, preserving its text.\n        Otherwise inserts on the current (empty) block, or on a new\n        block inserted above when the current line is non-empty.\n        \"\"\"\n        self._setup_checkbox_handler()\n\n        self._suppress_formatting = True\n        cursor = self.textCursor()\n        cursor.beginEditBlock()\n\n        block = cursor.block()\n        text_list = block.textList()\n\n        if text_list is not None:\n            # Convert existing list item to checkbox.\n            # Qt stores block text WITHOUT the list marker,\n            # so block.text() gives us the clean content.\n            line_text = block.text().strip()\n\n            # Remove this block from the QTextList\n            text_list.remove(block)\n\n            # Reset the block indent that the QTextList left behind\n            block_fmt = cursor.blockFormat()\n            block_fmt.setIndent(0)\n            cursor.setBlockFormat(block_fmt)\n\n            # Select all content in the block and delete it\n            cursor.movePosition(QTextCursor.MoveOperation.StartOfBlock)\n            cursor.movePosition(\n                QTextCursor.MoveOperation.EndOfBlock,\n                QTextCursor.MoveMode.KeepAnchor,\n            )\n            cursor.removeSelectedText()\n\n            # Insert checkbox + original text\n            self._insert_checkbox_at_cursor(cursor)\n            cursor.insertText(line_text)\n        else:\n            # Original behavior for plain text\n            cursor.movePosition(QTextCursor.MoveOperation.StartOfBlock)\n            if cursor.block().text().strip():\n                cursor.insertBlock()\n                cursor.movePosition(QTextCursor.MoveOperation.PreviousBlock)\n                cursor.movePosition(QTextCursor.MoveOperation.StartOfBlock)\n\n            self._insert_checkbox_at_cursor(cursor)\n\n        cursor.endEditBlock()\n        self._suppress_formatting = False\n        self.setTextCursor(cursor)\n        self.setFocus()\n\n    def set_format(self, format: str) -&gt; None:\n        \"\"\"Set up the bullet/numbered/checklist formatting.\"\"\"\n        if format == \"fmt_checklist\":\n            self._insert_fmt_checklist()\n            return\n\n        cursor = self.textCursor()\n        cursor.beginEditBlock()\n        cursor.movePosition(QTextCursor.MoveOperation.StartOfBlock)\n\n        if format == \"fmt_bullet\":\n            list_fmt = QTextListFormat()\n            list_fmt.setStyle(QTextListFormat.Style.ListDisc)\n            cursor.createList(list_fmt)\n        elif format == \"fmt_number\":\n            list_fmt = QTextListFormat()\n            list_fmt.setStyle(QTextListFormat.Style.ListDecimal)\n            cursor.createList(list_fmt)\n\n        cursor.endEditBlock()\n        self.setTextCursor(cursor)\n        self.setFocus()\n\n    def paintEvent(self, event) -&gt; None:\n        \"\"\"Override paintEvent to make sure the placeholder text is not\n        displayed when an empty list is added to an empty document.\"\"\"\n        if (\n            self.placeholderText()\n            and self.document().isEmpty()\n            and self._has_list_block()\n        ):\n            self._suppress_formatting = True\n            saved = self.placeholderText()\n            super().setPlaceholderText(\"\")\n            super().paintEvent(event)\n            super().setPlaceholderText(saved)\n            self._suppress_formatting = False\n            return\n        super().paintEvent(event)\n\n    def _has_list_block(self) -&gt; bool:\n        block = self.document().begin()\n        while block.isValid():\n            if block.textList() is not None:\n                return True\n            block = block.next()\n        return False\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/text_box.html#client.ayon_core.ui.components.text_box.AYTextEditor.as_markdown","title":"<code>as_markdown()</code>","text":"<p>Get the content as GitHub-flavored markdown.</p> <p>Returns:</p> Type Description <code>str</code> <p>Markdown string.</p> Source code in <code>client/ayon_core/ui/components/text_box.py</code> <pre><code>def as_markdown(self) -&gt; str:\n    \"\"\"Get the content as GitHub-flavored markdown.\n\n    Returns:\n        Markdown string.\n    \"\"\"\n    if self._checkbox_handler and self._checkbox_handler.has_checkboxes():\n        return self._checkbox_handler.to_markdown()\n    rendered_md = self.document().toMarkdown(MD_DIALECT)\n    return strip_user_mention_display(rendered_md, self._user_list)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/text_box.html#client.ayon_core.ui.components.text_box.AYTextEditor.keyPressEvent","title":"<code>keyPressEvent(event)</code>","text":"<p>Handle key press events for completer.</p> Source code in <code>client/ayon_core/ui/components/text_box.py</code> <pre><code>def keyPressEvent(self, event) -&gt; None:\n    \"\"\"Handle key press events for completer.\"\"\"\n    if on_completer_key_press(self, event):\n        event.accept()\n        return\n\n    # ctrl/cmd-enter to submit\n    if (\n        event.key() in {Qt.Key.Key_Return, Qt.Key.Key_Enter}\n        and event.modifiers() &amp; Qt.KeyboardModifier.ControlModifier\n    ):\n        self.submitted.emit()\n\n    # Auto-continue checkbox on Enter\n    if (\n        event.key() in {Qt.Key.Key_Return, Qt.Key.Key_Enter}\n        and self._checkbox_handler\n        and self._checkbox_handler.has_checkboxes()\n    ):\n        cursor = self.textCursor()\n        block_text = cursor.block().text()\n\n        if \"\\ufffc\" in block_text:\n            # Extract text after the checkbox object char\n            parts = block_text.split(\"\\ufffc\", 1)\n            after_checkbox = parts[1].strip() if len(parts) &gt; 1 else \"\"\n\n            if not after_checkbox:\n                # Empty checkbox line \u2192 end the list\n                # Remove the checkbox content from current block\n                cursor.movePosition(\n                    QTextCursor.MoveOperation.StartOfBlock,\n                    QTextCursor.MoveMode.MoveAnchor,\n                )\n                cursor.movePosition(\n                    QTextCursor.MoveOperation.EndOfBlock,\n                    QTextCursor.MoveMode.KeepAnchor,\n                )\n                cursor.removeSelectedText()\n                # Remove the last checkbox from handler\n                self._checkbox_handler.remove_last_checkbox()\n                # Insert a plain newline\n                super().keyPressEvent(event)\n                return\n\n            # Non-empty checkbox line \u2192 insert new checkbox\n            event.accept()\n            self._suppress_formatting = True\n\n            cursor.beginEditBlock()\n            cursor.movePosition(QTextCursor.MoveOperation.EndOfBlock)\n            cursor.insertBlock()\n            self._insert_checkbox_at_cursor(cursor)\n            cursor.endEditBlock()\n\n            self._suppress_formatting = False\n            self.setTextCursor(cursor)\n            return\n\n    super().keyPressEvent(event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/text_box.html#client.ayon_core.ui.components.text_box.AYTextEditor.mouseMoveEvent","title":"<code>mouseMoveEvent(event)</code>","text":"<p>Show arrow cursor over checkboxes, hand over links, I-beam else.</p> Source code in <code>client/ayon_core/ui/components/text_box.py</code> <pre><code>def mouseMoveEvent(self, event):\n    \"\"\"Show arrow cursor over checkboxes, hand over links, I-beam else.\"\"\"\n    pos = event.pos()\n    cursor = self.cursorForPosition(pos)\n    fmt = cursor.charFormat()\n    if fmt.objectType() == CHECKBOX_FORMAT_TYPE:\n        self.viewport().setCursor(Qt.CursorShape.ArrowCursor)\n    elif fmt.isAnchor():\n        self.viewport().setCursor(Qt.CursorShape.PointingHandCursor)\n    else:\n        self.viewport().setCursor(Qt.CursorShape.IBeamCursor)\n    super().mouseMoveEvent(event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/text_box.html#client.ayon_core.ui.components.text_box.AYTextEditor.mousePressEvent","title":"<code>mousePressEvent(event)</code>","text":"<p>Handle mouse press events for checkboxes.</p> <p>Checkboxes can be toggled even in read-only mode.</p> Source code in <code>client/ayon_core/ui/components/text_box.py</code> <pre><code>def mousePressEvent(self, event) -&gt; None:\n    \"\"\"Handle mouse press events for checkboxes.\n\n    Checkboxes can be toggled even in read-only mode.\n    \"\"\"\n    is_cb, cb_pos = self._is_checkbox_at_cursor(event.pos())\n    if is_cb and cb_pos is not None:\n        assert self._checkbox_handler is not None  # implied by is_cb==True\n        checkbox_idx = self._checkbox_handler.get_checkbox_at_position(\n            cb_pos\n        )\n        if checkbox_idx is not None:\n            self._checkbox_handler.toggle_checkbox(checkbox_idx)\n            self.viewport().update()\n            event.accept()\n            return\n\n    super().mousePressEvent(event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/text_box.html#client.ayon_core.ui.components.text_box.AYTextEditor.paintEvent","title":"<code>paintEvent(event)</code>","text":"<p>Override paintEvent to make sure the placeholder text is not displayed when an empty list is added to an empty document.</p> Source code in <code>client/ayon_core/ui/components/text_box.py</code> <pre><code>def paintEvent(self, event) -&gt; None:\n    \"\"\"Override paintEvent to make sure the placeholder text is not\n    displayed when an empty list is added to an empty document.\"\"\"\n    if (\n        self.placeholderText()\n        and self.document().isEmpty()\n        and self._has_list_block()\n    ):\n        self._suppress_formatting = True\n        saved = self.placeholderText()\n        super().setPlaceholderText(\"\")\n        super().paintEvent(event)\n        super().setPlaceholderText(saved)\n        self._suppress_formatting = False\n        return\n    super().paintEvent(event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/text_box.html#client.ayon_core.ui.components.text_box.AYTextEditor.set_format","title":"<code>set_format(format)</code>","text":"<p>Set up the bullet/numbered/checklist formatting.</p> Source code in <code>client/ayon_core/ui/components/text_box.py</code> <pre><code>def set_format(self, format: str) -&gt; None:\n    \"\"\"Set up the bullet/numbered/checklist formatting.\"\"\"\n    if format == \"fmt_checklist\":\n        self._insert_fmt_checklist()\n        return\n\n    cursor = self.textCursor()\n    cursor.beginEditBlock()\n    cursor.movePosition(QTextCursor.MoveOperation.StartOfBlock)\n\n    if format == \"fmt_bullet\":\n        list_fmt = QTextListFormat()\n        list_fmt.setStyle(QTextListFormat.Style.ListDisc)\n        cursor.createList(list_fmt)\n    elif format == \"fmt_number\":\n        list_fmt = QTextListFormat()\n        list_fmt.setStyle(QTextListFormat.Style.ListDecimal)\n        cursor.createList(list_fmt)\n\n    cursor.endEditBlock()\n    self.setTextCursor(cursor)\n    self.setFocus()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/text_box.html#client.ayon_core.ui.components.text_box.AYTextEditor.set_markdown","title":"<code>set_markdown(md)</code>","text":"<p>Set markdown content with checkbox support.</p> <p>Parameters:</p> Name Type Description Default <code>md</code> <code>str</code> <p>Markdown text to display</p> required Source code in <code>client/ayon_core/ui/components/text_box.py</code> <pre><code>def set_markdown(self, md: str) -&gt; None:\n    \"\"\"Set markdown content with checkbox support.\n\n    Args:\n        md: Markdown text to display\n    \"\"\"\n    if CheckboxHandler.contains_checkboxes(md):\n        self._setup_checkbox_handler()\n        self._checkbox_handler.parse_and_render(md)\n    else:\n        self.document().setMarkdown(md, MD_DIALECT)\n    apply_code_block_backgrounds(self)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/text_box.html#client.ayon_core.ui.components.text_box.AYTextEditor.set_style","title":"<code>set_style(style)</code>","text":"<p>Apply a character style to the current selection or cursor.</p> <p>Styles are merged with the existing character formatting so that multiple styles (e.g. bold + italic) can be combined without removing previously applied styles.</p> <p>Parameters:</p> Name Type Description Default <code>style</code> <code>str</code> <p>Style identifier string (e.g. <code>\"stl_bold\"</code>).</p> required Source code in <code>client/ayon_core/ui/components/text_box.py</code> <pre><code>def set_style(self, style: str) -&gt; None:\n    \"\"\"Apply a character style to the current selection or cursor.\n\n    Styles are merged with the existing character formatting so that\n    multiple styles (e.g. bold + italic) can be combined without\n    removing previously applied styles.\n\n    Args:\n        style: Style identifier string (e.g. ``\"stl_bold\"``).\n    \"\"\"\n    cursor = self.textCursor()\n\n    # Suppress formatting during style application\n    self._suppress_formatting = True\n\n    cursor.beginEditBlock()\n\n    if style == \"stl_bold\":\n        # Toggle bold, preserving all other formatting\n        current_weight = cursor.charFormat().fontWeight()\n        new_weight = (\n            QFont.Weight.Normal\n            if current_weight == QFont.Weight.Bold\n            else QFont.Weight.Bold\n        )\n        fmt = QTextCharFormat()\n        fmt.setFontWeight(new_weight)\n\n        if cursor.hasSelection():\n            cursor.mergeCharFormat(fmt)\n        else:\n            self.mergeCurrentCharFormat(fmt)\n\n        self.setTextCursor(cursor)\n\n    elif style == \"stl_italic\":\n        # Toggle italic, preserving all other formatting\n        new_italic = not cursor.charFormat().fontItalic()\n        fmt = QTextCharFormat()\n        fmt.setFontItalic(new_italic)\n\n        if cursor.hasSelection():\n            cursor.mergeCharFormat(fmt)\n        else:\n            self.mergeCurrentCharFormat(fmt)\n\n        self.setTextCursor(cursor)\n\n    elif style == \"stl_h1\":\n        # Toggle heading size, preserving all other formatting\n        base_size = self.font().pointSizeF()\n        current_size = cursor.charFormat().fontPointSize()\n\n        fmt = QTextCharFormat()\n        if current_size &gt; base_size:  # Already a header\n            fmt.setFontPointSize(base_size)\n            fmt.setFontWeight(QFont.Weight.Normal)\n        else:  # Make it a header\n            fmt.setFontPointSize(base_size * 1.5)\n            fmt.setFontWeight(QFont.Weight.Bold)\n\n        if cursor.hasSelection():\n            cursor.mergeCharFormat(fmt)\n        else:\n            self.mergeCurrentCharFormat(fmt)\n\n        self.setTextCursor(cursor)\n\n    elif style == \"stl_link\":\n        pw = self.parentWidget()\n        if not pw:\n            return\n\n        selected_text = (\n            cursor.selectedText() if cursor.hasSelection() else \"\"\n        )\n        field = QtWidgets.QLineEdit(selected_text, parent=pw)\n\n        def _make_link():\n            link = field.text()\n            fmt = QTextCharFormat()\n            fmt.setAnchor(True)\n            fmt.setAnchorHref(link)\n            fmt.setFontUnderline(True)\n\n            if cursor.hasSelection():\n                cursor.mergeCharFormat(fmt)\n            else:\n                self.mergeCurrentCharFormat(fmt)\n\n            field.close()\n            field.deleteLater()\n            self.setFocus()\n            self.update()\n\n        # open link edit field\n        field.show()\n        fr = field.rect()\n        field.setGeometry(4, 0, self.rect().width(), fr.height())\n        field.selectAll()\n        field.setFocus()\n        field.returnPressed.connect(_make_link)\n\n    elif style == \"stl_code\":\n        # Detect if already in code style\n        is_code = cursor.charFormat().fontFixedPitch()\n        fmt = QTextCharFormat()\n\n        if is_code:\n            # Toggle off: revert code-specific properties to widget\n            # defaults\n            fmt.setFontFixedPitch(False)\n            fmt.setFontFamilies([self.font().family()])\n            fmt.setBackground(\n                self.palette().color(QPalette.ColorRole.Base)\n            )\n            fmt.setForeground(\n                self.palette().color(QPalette.ColorRole.Text)\n            )\n        else:\n            # Toggle on: apply code style\n            fmt.setFontFixedPitch(True)\n            fmt.setFontFamilies([\"Noto Sans Mono\"])\n            fmt.setBackground(CODE_BG)\n            fmt.setForeground(CODE_FG)\n\n        if cursor.hasSelection():\n            cursor.mergeCharFormat(fmt)\n        else:\n            self.mergeCurrentCharFormat(fmt)\n\n        self.setTextCursor(cursor)\n\n    cursor.endEditBlock()\n    self._suppress_formatting = False\n    self.setFocus()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/text_box.html#client.ayon_core.ui.components.text_box.AttachmentWidget","title":"<code>AttachmentWidget</code>","text":"<p>               Bases: <code>QWidget</code></p> <p>Widget to display a single attachment thumbnail with remove button.</p> Source code in <code>client/ayon_core/ui/components/text_box.py</code> <pre><code>class AttachmentWidget(QtWidgets.QWidget):\n    \"\"\"Widget to display a single attachment thumbnail with remove button.\"\"\"\n\n    # Signal emits (index, type: 'screenshot' or 'file')\n    remove_clicked = Signal(int, str)\n    # Signal emits (index, type) when thumbnail clicked\n    thumbnail_clicked = Signal(int, str)\n\n    def __init__(\n        self,\n        parent=None,\n        index=0,\n        filename=\"\",\n        file_path=\"\",\n        attachment_type=\"file\",\n    ):\n        super().__init__(parent)\n        self.index = index\n        self.filename = filename\n        self.file_path = file_path\n        self.attachment_type = attachment_type  # 'screenshot' or 'file'\n        self.setup_ui()\n        self.load_image()\n\n    def setup_ui(self):\n        # Use a container for the thumbnail with overlay button\n        container = QtWidgets.QWidget(self)\n        container.setFixedSize(80, 60)\n        container.setCursor(Qt.CursorShape.PointingHandCursor)\n\n        # Thumbnail\n        self.thumbnail_label = QLabel(container)\n        self.thumbnail_label.setFixedSize(80, 60)\n        self.thumbnail_label.setScaledContents(True)\n        self.thumbnail_label.setAlignment(Qt.AlignmentFlag.AlignCenter)\n        self.thumbnail_label.setStyleSheet(\n            \"QLabel { background-color: #2b2b2b; border: 1px solid #3d3d3d; }\"\n        )\n\n        # Make thumbnail clickable\n        self.thumbnail_label.mousePressEvent = (\n            lambda e: self.thumbnail_clicked.emit(\n                self.index, self.attachment_type\n            )\n        )\n\n        # Remove button overlaid on top-right corner\n        self.remove_btn = AYButton(\n            \"\u00d7\", variant=AYButton.Variants.Nav, parent=container\n        )\n        self.remove_btn.setFixedSize(18, 18)\n        self.remove_btn.move(62, 0)  # Position at top-right corner\n        self.remove_btn.clicked.connect(\n            lambda: self.remove_clicked.emit(self.index, self.attachment_type)\n        )\n        self.remove_btn.raise_()\n\n        # Main layout\n        layout = AYVBoxLayout(margin=4, spacing=2)\n        layout.addWidget(container)\n\n        # Filename label (truncated)\n        self.filename_label = QLabel(self)\n        self.filename_label.setAlignment(Qt.AlignmentFlag.AlignCenter)\n        self.filename_label.setStyleSheet(\"font-size: 10px; color: #9aa4ad;\")\n        layout.addWidget(self.filename_label)\n\n        self.setLayout(layout)\n        self.update_display()\n\n        # Set tooltip\n        self.setToolTip(self.filename)\n\n    def load_image(self):\n        \"\"\"Load thumbnail from file_path\"\"\"\n        pixmap = QPixmap(self.file_path)\n        if not pixmap.isNull():\n            self.thumbnail_label.setPixmap(\n                pixmap.scaled(\n                    80,\n                    60,\n                    Qt.AspectRatioMode.KeepAspectRatio,\n                    Qt.TransformationMode.SmoothTransformation,\n                )\n            )\n        else:\n            self.thumbnail_label.setText(\"Image\")\n\n    def update_display(self):\n        \"\"\"Update the display with current filename and image\"\"\"\n        # Update filename label\n        display_name = (\n            self.filename[:10] + \"...\"\n            if len(self.filename) &gt; 10\n            else self.filename\n        )\n        self.filename_label.setText(display_name)\n        self.load_image()\n\n    def update_content(self, filename=\"\", file_path=\"\"):\n        \"\"\"Update the widget content\"\"\"\n        if filename:\n            self.filename = filename\n            self.setToolTip(filename)\n        if file_path:\n            self.file_path = file_path\n        self.update_display()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/text_box.html#client.ayon_core.ui.components.text_box.AttachmentWidget.load_image","title":"<code>load_image()</code>","text":"<p>Load thumbnail from file_path</p> Source code in <code>client/ayon_core/ui/components/text_box.py</code> <pre><code>def load_image(self):\n    \"\"\"Load thumbnail from file_path\"\"\"\n    pixmap = QPixmap(self.file_path)\n    if not pixmap.isNull():\n        self.thumbnail_label.setPixmap(\n            pixmap.scaled(\n                80,\n                60,\n                Qt.AspectRatioMode.KeepAspectRatio,\n                Qt.TransformationMode.SmoothTransformation,\n            )\n        )\n    else:\n        self.thumbnail_label.setText(\"Image\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/text_box.html#client.ayon_core.ui.components.text_box.AttachmentWidget.update_content","title":"<code>update_content(filename='', file_path='')</code>","text":"<p>Update the widget content</p> Source code in <code>client/ayon_core/ui/components/text_box.py</code> <pre><code>def update_content(self, filename=\"\", file_path=\"\"):\n    \"\"\"Update the widget content\"\"\"\n    if filename:\n        self.filename = filename\n        self.setToolTip(filename)\n    if file_path:\n        self.file_path = file_path\n    self.update_display()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/text_box.html#client.ayon_core.ui.components.text_box.AttachmentWidget.update_display","title":"<code>update_display()</code>","text":"<p>Update the display with current filename and image</p> Source code in <code>client/ayon_core/ui/components/text_box.py</code> <pre><code>def update_display(self):\n    \"\"\"Update the display with current filename and image\"\"\"\n    # Update filename label\n    display_name = (\n        self.filename[:10] + \"...\"\n        if len(self.filename) &gt; 10\n        else self.filename\n    )\n    self.filename_label.setText(display_name)\n    self.load_image()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/text_edit.html","title":"text_edit","text":"<p>text edit</p>"},{"location":"autoapi/client/ayon_core/ui/components/text_edit.html#client.ayon_core.ui.components.text_edit.AYTextEdit","title":"<code>AYTextEdit</code>","text":"<p>               Bases: <code>StyleMixin</code>, <code>QTextEdit</code></p> <p>AYON styled text edit widget.</p> <p>Overrides Qt's stylesheet painting with AYONStyle custom rendering.</p> <p>Parameters:</p> Name Type Description Default <code>*args</code> <p>Positional arguments passed to QTextEdit.</p> <code>()</code> <code>**kwargs</code> <p>Keyword arguments passed to QTextEdit.</p> <code>{}</code> Source code in <code>client/ayon_core/ui/components/text_edit.py</code> <pre><code>class AYTextEdit(StyleMixin, QTextEdit):\n    \"\"\"AYON styled text edit widget.\n\n    Overrides Qt's stylesheet painting with AYONStyle custom rendering.\n\n    Args:\n        *args: Positional arguments passed to QTextEdit.\n        **kwargs: Keyword arguments passed to QTextEdit.\n    \"\"\"\n\n    Variants = QTextEditVariants\n\n    def __init__(\n        self,\n        *args,\n        variant: Variants = Variants.Default,\n        **kwargs,\n    ):\n        \"\"\"Initialize AYTextEdit widget.\n\n        Args:\n            *args: Positional arguments passed to QTextEdit.\n            variant: Text edit variant.\n            **kwargs: Keyword arguments passed to QTextEdit.\n        \"\"\"\n        self._variant_str: str = variant.value\n        super().__init__(*args, **kwargs)\n        self.setStyle(get_ayon_style())\n        self.setAttribute(Qt.WidgetAttribute.WA_TranslucentBackground, True)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/text_edit.html#client.ayon_core.ui.components.text_edit.AYTextEdit.__init__","title":"<code>__init__(*args, variant=Variants.Default, **kwargs)</code>","text":"<p>Initialize AYTextEdit widget.</p> <p>Parameters:</p> Name Type Description Default <code>*args</code> <p>Positional arguments passed to QTextEdit.</p> <code>()</code> <code>variant</code> <code>Variants</code> <p>Text edit variant.</p> <code>Default</code> <code>**kwargs</code> <p>Keyword arguments passed to QTextEdit.</p> <code>{}</code> Source code in <code>client/ayon_core/ui/components/text_edit.py</code> <pre><code>def __init__(\n    self,\n    *args,\n    variant: Variants = Variants.Default,\n    **kwargs,\n):\n    \"\"\"Initialize AYTextEdit widget.\n\n    Args:\n        *args: Positional arguments passed to QTextEdit.\n        variant: Text edit variant.\n        **kwargs: Keyword arguments passed to QTextEdit.\n    \"\"\"\n    self._variant_str: str = variant.value\n    super().__init__(*args, **kwargs)\n    self.setStyle(get_ayon_style())\n    self.setAttribute(Qt.WidgetAttribute.WA_TranslucentBackground, True)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/tree_model.html","title":"tree_model","text":"<p>Lazy-loading tree model for AYON UI Qt components.</p>"},{"location":"autoapi/client/ayon_core/ui/components/tree_model.html#client.ayon_core.ui.components.tree_model.LazyTreeModel","title":"<code>LazyTreeModel</code>","text":"<p>               Bases: <code>QAbstractItemModel</code></p> <p>Qt model that lazily loads children via a callback.</p> <p>Children are fetched on demand using Qt's canFetchMore/fetchMore protocol. The root level is loaded asynchronously on construction.</p> <p>Parameters:</p> Name Type Description Default <code>fetch_children</code> <code>Callable[[str | None], list[TreeNode]]</code> <p>Callable that takes a parent node ID (<code>None</code> for root) and returns a list of :class:<code>TreeNode</code> instances.</p> required <code>no_async</code> <code>bool</code> <p>When <code>True</code>, children are fetched synchronously on the main thread instead of via the :class:<code>AsyncTaskQueue</code>. Useful in tests to avoid worker-thread/paint-event races.</p> <code>False</code> <code>parent</code> <code>QObject | None</code> <p>Optional parent QObject.</p> <code>None</code> <p>.. warning::     Async mode and <code>expandAll()</code>: Because root children are     fetched asynchronously, calling <code>view.expandAll()</code> immediately     after constructing the model will silently expand nothing \u2014     <code>hasChildren()</code> returns <code>False</code> until the first fetch     completes.  Connect to :attr:<code>loading_changed</code> and call     <code>expandAll()</code> once it emits <code>False</code>::</p> <pre><code>    model.loading_changed.connect(\n        lambda loading: view.expandAll() if not loading else None\n    )\n</code></pre> <p>Example::</p> <pre><code>def fetch(parent_id):\n    if parent_id is None:\n        return [TreeNode(\"root\", \"Root\", has_children=True)]\n    return []\n\nmodel = LazyTreeModel(fetch_children=fetch)\n</code></pre> Source code in <code>client/ayon_core/ui/components/tree_model.py</code> <pre><code>class LazyTreeModel(QAbstractItemModel):\n    \"\"\"Qt model that lazily loads children via a callback.\n\n    Children are fetched on demand using Qt's canFetchMore/fetchMore\n    protocol. The root level is loaded asynchronously on construction.\n\n    Args:\n        fetch_children: Callable that takes a parent node ID (``None``\n            for root) and returns a list of :class:`TreeNode` instances.\n        no_async: When ``True``, children are fetched synchronously on\n            the main thread instead of via the :class:`AsyncTaskQueue`.\n            Useful in tests to avoid worker-thread/paint-event races.\n        parent: Optional parent QObject.\n\n    .. warning::\n        **Async mode and ``expandAll()``**: Because root children are\n        fetched asynchronously, calling ``view.expandAll()`` immediately\n        after constructing the model will silently expand nothing \u2014\n        ``hasChildren()`` returns ``False`` until the first fetch\n        completes.  Connect to :attr:`loading_changed` and call\n        ``expandAll()`` once it emits ``False``::\n\n            model.loading_changed.connect(\n                lambda loading: view.expandAll() if not loading else None\n            )\n\n    Example::\n\n        def fetch(parent_id):\n            if parent_id is None:\n                return [TreeNode(\"root\", \"Root\", has_children=True)]\n            return []\n\n        model = LazyTreeModel(fetch_children=fetch)\n    \"\"\"\n\n    loading_changed = Signal(bool)  # True while any fetch is in-flight\n    fetch_error = Signal(str)  # error message when a fetch fails\n    pending_count_changed = Signal(int)  # number of in-flight fetch tasks\n\n    def __init__(\n        self,\n        fetch_children: Callable[[str | None], list[TreeNode]],\n        no_async: bool = False,\n        parent: QObject | None = None,\n    ) -&gt; None:\n        super().__init__(parent)\n        self._fetch_children = fetch_children\n        self._no_async: bool = no_async\n        self._reset_counter: int = 0\n        self._context_id: str = f\"ltm_{id(self)}_v0\"\n        self._pending_tasks: int = 0\n        self._root = _InternalNode(tree_node=None, parent=None)\n        # _all_nodes keeps every node reachable so Python's GC does not\n        # collect objects that are held only via QModelIndex.internalPointer().\n        self._all_nodes: set[_InternalNode] = {self._root}\n        self._fetch_children_async(self._root)\n\n    def _update_loading_state(self) -&gt; None:\n        \"\"\"Emit loading-progress signals based on current pending task\n        count.\"\"\"\n        self.pending_count_changed.emit(self._pending_tasks)\n        self.loading_changed.emit(self._pending_tasks &gt; 0)\n\n    def _index_for_node(self, node: _InternalNode) -&gt; QModelIndex:\n        \"\"\"Return the QModelIndex that identifies node (column 0).\n\n        Args:\n            node: A non-root internal node.\n\n        Returns:\n            Valid QModelIndex for non-root nodes, invalid for root.\n        \"\"\"\n        if node.is_root:\n            return QModelIndex()\n        parent = node.parent\n        row = parent.children.index(node)  # type: ignore[union-attr]\n        return self.createIndex(row, 0, node)\n\n    def _on_children_ready(\n        self,\n        node: _InternalNode,\n        context_id: str,\n        results: list[TreeNode] | None,\n    ) -&gt; None:\n        \"\"\"Handle fetch results on the main thread.\n\n        Called via Qt.QueuedConnection from AsyncTaskQueue so it always\n        executes on the Qt main thread, making it safe to call Qt model\n        mutation methods.\n\n        Args:\n            node: The node whose children were fetched.\n            context_id: The context_id active when the task was enqueued.\n                Used to discard stale results.\n            results: TreeNode list returned by fetch_children, or None on\n                error.\n        \"\"\"\n        # Always release the fetching guard so the node is never stuck\n        # in-flight, even for stale callbacks.\n        node.is_fetching = False\n\n        # Discard stale results (model was reset while this task was running).\n        if context_id != self._context_id or node not in self._all_nodes:\n            log.debug(\n                \"Discarding stale fetch result for node %r (ctx %s vs %s)\",\n                node.tree_node.id if node.tree_node else \"root\",\n                context_id,\n                self._context_id,\n            )\n            return\n\n        self._pending_tasks = max(0, self._pending_tasks - 1)\n\n        if results is None:\n            node_id = node.tree_node.id if node.tree_node else \"root\"\n            self.fetch_error.emit(\n                f\"Failed to fetch children for node {node_id!r}\"\n            )\n            # Do NOT set children_loaded=True on error so that the\n            # caller can retry by calling reset().  The node stays in\n            # a \"not loaded\" state; is_fetching was already cleared\n            # above so a subsequent fetchMore() or reset() can re-try.\n            self._update_loading_state()\n            return\n\n        new_nodes = [\n            _InternalNode(tree_node=tn, parent=node) for tn in results\n        ]\n        if new_nodes:\n            parent_index = self._index_for_node(node)\n            self.beginInsertRows(parent_index, 0, len(new_nodes) - 1)\n            node.children = new_nodes\n            for child in new_nodes:\n                self._all_nodes.add(child)\n            node.children_loaded = True\n            self.endInsertRows()\n        else:\n            node.children_loaded = True\n\n        self._update_loading_state()\n\n    def _fetch_children_async(self, node: _InternalNode) -&gt; None:\n        \"\"\"Enqueue an async task to fetch children for *node*.\n\n        Uses a per-node re-entrancy guard (``node.is_fetching``) to\n        prevent duplicate tasks.  Results are delivered back on the main\n        thread via :meth:`_on_children_ready`.\n\n        Args:\n            node: The node whose children to fetch.\n        \"\"\"\n        if node.is_fetching or node.children_loaded:\n            return\n        node.is_fetching = True\n\n        parent_id = None if node.is_root else node.tree_node.id  # type: ignore[union-attr]\n        ctx = self._context_id\n\n        self._pending_tasks += 1\n        self._update_loading_state()\n\n        if self._no_async:\n            try:\n                result: list[TreeNode] | None = self._fetch_children(parent_id)\n            except Exception:\n                log.exception(\n                    \"fetch_children raised for parent_id=%r\", parent_id\n                )\n                result = None\n            self._on_children_ready(node, ctx, result)\n            return\n\n        task = AsyncTask(\n            name=f\"fetch_children_{parent_id or 'root'}\",\n            function=lambda: self._fetch_children(parent_id),\n            callback=lambda result: self._on_children_ready(node, ctx, result),\n            priority=1,\n            context_id=ctx,\n            cancellable=True,\n        )\n        get_task_queue().enqueue(task)\n\n    def _node_from_index(\n        self, index: QModelIndex | QPersistentModelIndex\n    ) -&gt; _InternalNode:\n        \"\"\"Return the internal node for a model index.\n\n        Args:\n            index: A valid or invalid QModelIndex.\n\n        Returns:\n            The corresponding _InternalNode; returns root for invalid\n            indexes.\n        \"\"\"\n        if not index.isValid():\n            return self._root\n        return index.internalPointer()  # type: ignore[return-value]\n\n    # keep old name as alias for backward compatibility\n    def _node_for_index(self, index: QModelIndex) -&gt; _InternalNode:\n        \"\"\"Alias for _node_from_index.\"\"\"\n        return self._node_from_index(index)\n\n    def index(\n        self,\n        row: int,\n        column: int,\n        parent: QModelIndex | QPersistentModelIndex = QModelIndex(),\n    ) -&gt; QModelIndex:\n        \"\"\"Create a model index for the given row/column under parent.\n\n        Args:\n            row: Row number under the parent.\n            column: Column number (always 0).\n            parent: Parent index.\n\n        Returns:\n            A valid QModelIndex, or an invalid one if out of range.\n        \"\"\"\n        parent_node = self._node_from_index(parent)\n        if row &lt; 0 or row &gt;= len(parent_node.children):\n            return QModelIndex()\n        child_node = parent_node.children[row]\n        return self.createIndex(row, column, child_node)\n\n    def parent(self, index: QModelIndex) -&gt; QModelIndex:  # type: ignore[override]\n        \"\"\"Return the parent index of the given index.\n\n        Args:\n            index: A model index whose parent is requested.\n\n        Returns:\n            Parent QModelIndex, or an invalid index if parent is root.\n        \"\"\"\n        if not index.isValid():\n            return QModelIndex()\n\n        node: _InternalNode = index.internalPointer()  # type: ignore[assignment]\n        parent_node = node.parent\n        if parent_node is None or parent_node.is_root:\n            return QModelIndex()\n\n        # grandparent is always non-None for a non-root parent_node\n        grandparent = parent_node.parent\n        row = grandparent.children.index(parent_node)  # type: ignore[union-attr]\n        return self.createIndex(row, 0, parent_node)\n\n    def rowCount(\n        self, parent: QModelIndex | QPersistentModelIndex = QModelIndex()\n    ) -&gt; int:\n        \"\"\"Return the number of rows under the given parent.\n\n        Args:\n            parent: The parent index.\n\n        Returns:\n            Number of loaded children. Returns 0 for nodes whose\n            children have not been fetched yet; Qt uses hasChildren()\n            to decide whether to show a disclosure triangle.\n        \"\"\"\n        node = self._node_from_index(parent)\n        return len(node.children)\n\n    def columnCount(\n        self, parent: QModelIndex | QPersistentModelIndex = QModelIndex()\n    ) -&gt; int:\n        \"\"\"Return the number of columns (always 1).\n\n        Args:\n            parent: Unused parent index.\n\n        Returns:\n            Always 1.\n        \"\"\"\n        return 1\n\n    def data(\n        self,\n        index: QModelIndex | QPersistentModelIndex,\n        role: int = Qt.ItemDataRole.DisplayRole,\n    ) -&gt; object:\n        \"\"\"Return data for the given index and role.\n\n        Args:\n            index: The model index to query.\n            role: The data role.\n\n        Returns:\n            The node label for DisplayRole, None otherwise.\n        \"\"\"\n        if not index.isValid():\n            return None\n        node: _InternalNode = index.internalPointer()  # type: ignore[assignment]\n        if role == Qt.ItemDataRole.DisplayRole:\n            return node.tree_node.label if node.tree_node else None\n        elif role == Qt.ItemDataRole.DecorationRole:\n            if node.tree_node and node.tree_node.icon:\n                return get_icon(\n                    node.tree_node.icon,\n                    node.tree_node.icon_color,\n                    fill=node.tree_node.icon_fill,\n                )\n        elif role == Qt.ItemDataRole.UserRole:\n            if node.tree_node and node.tree_node.data:\n                return node.tree_node.data\n        return None\n\n    def hasChildren(\n        self, parent: QModelIndex | QPersistentModelIndex = QModelIndex()\n    ) -&gt; bool:\n        \"\"\"Return whether the node at parent has or can have children.\n\n        This is used by Qt to decide whether to draw the disclosure\n        triangle, even before children are loaded.\n\n        Args:\n            parent: The parent index.\n\n        Returns:\n            True if the node has loaded children or has_children is\n            True on the TreeNode.\n        \"\"\"\n        node = self._node_from_index(parent)\n        if node.is_root:\n            return bool(node.children)\n        # node.tree_node is non-None here (is_root is False)\n        if node.children_loaded:\n            return bool(node.children)\n        return node.tree_node.has_children  # type: ignore[union-attr]\n\n    def canFetchMore(\n        self, parent: QModelIndex | QPersistentModelIndex\n    ) -&gt; bool:\n        \"\"\"Return True if more children can be fetched for parent.\n\n        Args:\n            parent: The parent index to check.\n\n        Returns:\n            True if the node has potential children but they have not\n            been loaded yet.\n        \"\"\"\n        node = self._node_from_index(parent)\n        if node.is_root:\n            return False\n        # node.tree_node is non-None here (is_root is False)\n        return (  # type: ignore[union-attr]\n            node.tree_node.has_children and not node.children_loaded\n        )\n\n    def fetchMore(self, parent: QModelIndex | QPersistentModelIndex) -&gt; None:\n        \"\"\"Fetch and insert children for the node at parent.\n\n        Args:\n            parent: The parent index whose children should be loaded.\n        \"\"\"\n        self._fetch_children_async(self._node_from_index(parent))\n\n    def get_node_id(self, index: QModelIndex) -&gt; str | None:\n        \"\"\"Return the TreeNode.id for the given model index.\n\n        Args:\n            index: A valid QModelIndex.\n\n        Returns:\n            The node's string ID, or None if the index is invalid.\n        \"\"\"\n        if not index.isValid():\n            return None\n        node: _InternalNode = index.internalPointer()  # type: ignore[assignment]\n        if node.tree_node is None:\n            return None\n        return node.tree_node.id\n\n    @property\n    def is_loading(self) -&gt; bool:\n        \"\"\"Return True while at least one fetch task is in-flight.\"\"\"\n        return self._pending_tasks &gt; 0\n\n    def reset(self) -&gt; None:\n        \"\"\"Reset the model to its initial state and re-fetch root children.\n\n        This is the recommended recovery path after a failed root fetch\n        (i.e. when :attr:`fetch_error` was emitted for the root node).\n        \"\"\"\n        old_ctx = self._context_id\n        if not self._no_async:\n            get_task_queue().clear_context_tasks(old_ctx)\n        self._reset_counter += 1\n        self._context_id = f\"ltm_{id(self)}_v{self._reset_counter}\"\n        self._pending_tasks = 0\n        self.beginResetModel()\n        self._root = _InternalNode(tree_node=None, parent=None)\n        self._all_nodes = {self._root}\n        self.endResetModel()\n        # Emit loading signals *after* endResetModel so that any slot\n        # connected to loading_changed observes a consistent model state.\n        self._update_loading_state()\n        self._fetch_children_async(self._root)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/tree_model.html#client.ayon_core.ui.components.tree_model.LazyTreeModel.is_loading","title":"<code>is_loading</code>  <code>property</code>","text":"<p>Return True while at least one fetch task is in-flight.</p>"},{"location":"autoapi/client/ayon_core/ui/components/tree_model.html#client.ayon_core.ui.components.tree_model.LazyTreeModel.canFetchMore","title":"<code>canFetchMore(parent)</code>","text":"<p>Return True if more children can be fetched for parent.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <code>QModelIndex | QPersistentModelIndex</code> <p>The parent index to check.</p> required <p>Returns:</p> Type Description <code>bool</code> <p>True if the node has potential children but they have not</p> <code>bool</code> <p>been loaded yet.</p> Source code in <code>client/ayon_core/ui/components/tree_model.py</code> <pre><code>def canFetchMore(\n    self, parent: QModelIndex | QPersistentModelIndex\n) -&gt; bool:\n    \"\"\"Return True if more children can be fetched for parent.\n\n    Args:\n        parent: The parent index to check.\n\n    Returns:\n        True if the node has potential children but they have not\n        been loaded yet.\n    \"\"\"\n    node = self._node_from_index(parent)\n    if node.is_root:\n        return False\n    # node.tree_node is non-None here (is_root is False)\n    return (  # type: ignore[union-attr]\n        node.tree_node.has_children and not node.children_loaded\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/tree_model.html#client.ayon_core.ui.components.tree_model.LazyTreeModel.columnCount","title":"<code>columnCount(parent=QModelIndex())</code>","text":"<p>Return the number of columns (always 1).</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <code>QModelIndex | QPersistentModelIndex</code> <p>Unused parent index.</p> <code>QModelIndex()</code> <p>Returns:</p> Type Description <code>int</code> <p>Always 1.</p> Source code in <code>client/ayon_core/ui/components/tree_model.py</code> <pre><code>def columnCount(\n    self, parent: QModelIndex | QPersistentModelIndex = QModelIndex()\n) -&gt; int:\n    \"\"\"Return the number of columns (always 1).\n\n    Args:\n        parent: Unused parent index.\n\n    Returns:\n        Always 1.\n    \"\"\"\n    return 1\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/tree_model.html#client.ayon_core.ui.components.tree_model.LazyTreeModel.data","title":"<code>data(index, role=Qt.ItemDataRole.DisplayRole)</code>","text":"<p>Return data for the given index and role.</p> <p>Parameters:</p> Name Type Description Default <code>index</code> <code>QModelIndex | QPersistentModelIndex</code> <p>The model index to query.</p> required <code>role</code> <code>int</code> <p>The data role.</p> <code>DisplayRole</code> <p>Returns:</p> Type Description <code>object</code> <p>The node label for DisplayRole, None otherwise.</p> Source code in <code>client/ayon_core/ui/components/tree_model.py</code> <pre><code>def data(\n    self,\n    index: QModelIndex | QPersistentModelIndex,\n    role: int = Qt.ItemDataRole.DisplayRole,\n) -&gt; object:\n    \"\"\"Return data for the given index and role.\n\n    Args:\n        index: The model index to query.\n        role: The data role.\n\n    Returns:\n        The node label for DisplayRole, None otherwise.\n    \"\"\"\n    if not index.isValid():\n        return None\n    node: _InternalNode = index.internalPointer()  # type: ignore[assignment]\n    if role == Qt.ItemDataRole.DisplayRole:\n        return node.tree_node.label if node.tree_node else None\n    elif role == Qt.ItemDataRole.DecorationRole:\n        if node.tree_node and node.tree_node.icon:\n            return get_icon(\n                node.tree_node.icon,\n                node.tree_node.icon_color,\n                fill=node.tree_node.icon_fill,\n            )\n    elif role == Qt.ItemDataRole.UserRole:\n        if node.tree_node and node.tree_node.data:\n            return node.tree_node.data\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/tree_model.html#client.ayon_core.ui.components.tree_model.LazyTreeModel.fetchMore","title":"<code>fetchMore(parent)</code>","text":"<p>Fetch and insert children for the node at parent.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <code>QModelIndex | QPersistentModelIndex</code> <p>The parent index whose children should be loaded.</p> required Source code in <code>client/ayon_core/ui/components/tree_model.py</code> <pre><code>def fetchMore(self, parent: QModelIndex | QPersistentModelIndex) -&gt; None:\n    \"\"\"Fetch and insert children for the node at parent.\n\n    Args:\n        parent: The parent index whose children should be loaded.\n    \"\"\"\n    self._fetch_children_async(self._node_from_index(parent))\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/tree_model.html#client.ayon_core.ui.components.tree_model.LazyTreeModel.get_node_id","title":"<code>get_node_id(index)</code>","text":"<p>Return the TreeNode.id for the given model index.</p> <p>Parameters:</p> Name Type Description Default <code>index</code> <code>QModelIndex</code> <p>A valid QModelIndex.</p> required <p>Returns:</p> Type Description <code>str | None</code> <p>The node's string ID, or None if the index is invalid.</p> Source code in <code>client/ayon_core/ui/components/tree_model.py</code> <pre><code>def get_node_id(self, index: QModelIndex) -&gt; str | None:\n    \"\"\"Return the TreeNode.id for the given model index.\n\n    Args:\n        index: A valid QModelIndex.\n\n    Returns:\n        The node's string ID, or None if the index is invalid.\n    \"\"\"\n    if not index.isValid():\n        return None\n    node: _InternalNode = index.internalPointer()  # type: ignore[assignment]\n    if node.tree_node is None:\n        return None\n    return node.tree_node.id\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/tree_model.html#client.ayon_core.ui.components.tree_model.LazyTreeModel.hasChildren","title":"<code>hasChildren(parent=QModelIndex())</code>","text":"<p>Return whether the node at parent has or can have children.</p> <p>This is used by Qt to decide whether to draw the disclosure triangle, even before children are loaded.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <code>QModelIndex | QPersistentModelIndex</code> <p>The parent index.</p> <code>QModelIndex()</code> <p>Returns:</p> Type Description <code>bool</code> <p>True if the node has loaded children or has_children is</p> <code>bool</code> <p>True on the TreeNode.</p> Source code in <code>client/ayon_core/ui/components/tree_model.py</code> <pre><code>def hasChildren(\n    self, parent: QModelIndex | QPersistentModelIndex = QModelIndex()\n) -&gt; bool:\n    \"\"\"Return whether the node at parent has or can have children.\n\n    This is used by Qt to decide whether to draw the disclosure\n    triangle, even before children are loaded.\n\n    Args:\n        parent: The parent index.\n\n    Returns:\n        True if the node has loaded children or has_children is\n        True on the TreeNode.\n    \"\"\"\n    node = self._node_from_index(parent)\n    if node.is_root:\n        return bool(node.children)\n    # node.tree_node is non-None here (is_root is False)\n    if node.children_loaded:\n        return bool(node.children)\n    return node.tree_node.has_children  # type: ignore[union-attr]\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/tree_model.html#client.ayon_core.ui.components.tree_model.LazyTreeModel.index","title":"<code>index(row, column, parent=QModelIndex())</code>","text":"<p>Create a model index for the given row/column under parent.</p> <p>Parameters:</p> Name Type Description Default <code>row</code> <code>int</code> <p>Row number under the parent.</p> required <code>column</code> <code>int</code> <p>Column number (always 0).</p> required <code>parent</code> <code>QModelIndex | QPersistentModelIndex</code> <p>Parent index.</p> <code>QModelIndex()</code> <p>Returns:</p> Type Description <code>QModelIndex</code> <p>A valid QModelIndex, or an invalid one if out of range.</p> Source code in <code>client/ayon_core/ui/components/tree_model.py</code> <pre><code>def index(\n    self,\n    row: int,\n    column: int,\n    parent: QModelIndex | QPersistentModelIndex = QModelIndex(),\n) -&gt; QModelIndex:\n    \"\"\"Create a model index for the given row/column under parent.\n\n    Args:\n        row: Row number under the parent.\n        column: Column number (always 0).\n        parent: Parent index.\n\n    Returns:\n        A valid QModelIndex, or an invalid one if out of range.\n    \"\"\"\n    parent_node = self._node_from_index(parent)\n    if row &lt; 0 or row &gt;= len(parent_node.children):\n        return QModelIndex()\n    child_node = parent_node.children[row]\n    return self.createIndex(row, column, child_node)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/tree_model.html#client.ayon_core.ui.components.tree_model.LazyTreeModel.parent","title":"<code>parent(index)</code>","text":"<p>Return the parent index of the given index.</p> <p>Parameters:</p> Name Type Description Default <code>index</code> <code>QModelIndex</code> <p>A model index whose parent is requested.</p> required <p>Returns:</p> Type Description <code>QModelIndex</code> <p>Parent QModelIndex, or an invalid index if parent is root.</p> Source code in <code>client/ayon_core/ui/components/tree_model.py</code> <pre><code>def parent(self, index: QModelIndex) -&gt; QModelIndex:  # type: ignore[override]\n    \"\"\"Return the parent index of the given index.\n\n    Args:\n        index: A model index whose parent is requested.\n\n    Returns:\n        Parent QModelIndex, or an invalid index if parent is root.\n    \"\"\"\n    if not index.isValid():\n        return QModelIndex()\n\n    node: _InternalNode = index.internalPointer()  # type: ignore[assignment]\n    parent_node = node.parent\n    if parent_node is None or parent_node.is_root:\n        return QModelIndex()\n\n    # grandparent is always non-None for a non-root parent_node\n    grandparent = parent_node.parent\n    row = grandparent.children.index(parent_node)  # type: ignore[union-attr]\n    return self.createIndex(row, 0, parent_node)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/tree_model.html#client.ayon_core.ui.components.tree_model.LazyTreeModel.reset","title":"<code>reset()</code>","text":"<p>Reset the model to its initial state and re-fetch root children.</p> <p>This is the recommended recovery path after a failed root fetch (i.e. when :attr:<code>fetch_error</code> was emitted for the root node).</p> Source code in <code>client/ayon_core/ui/components/tree_model.py</code> <pre><code>def reset(self) -&gt; None:\n    \"\"\"Reset the model to its initial state and re-fetch root children.\n\n    This is the recommended recovery path after a failed root fetch\n    (i.e. when :attr:`fetch_error` was emitted for the root node).\n    \"\"\"\n    old_ctx = self._context_id\n    if not self._no_async:\n        get_task_queue().clear_context_tasks(old_ctx)\n    self._reset_counter += 1\n    self._context_id = f\"ltm_{id(self)}_v{self._reset_counter}\"\n    self._pending_tasks = 0\n    self.beginResetModel()\n    self._root = _InternalNode(tree_node=None, parent=None)\n    self._all_nodes = {self._root}\n    self.endResetModel()\n    # Emit loading signals *after* endResetModel so that any slot\n    # connected to loading_changed observes a consistent model state.\n    self._update_loading_state()\n    self._fetch_children_async(self._root)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/tree_model.html#client.ayon_core.ui.components.tree_model.LazyTreeModel.rowCount","title":"<code>rowCount(parent=QModelIndex())</code>","text":"<p>Return the number of rows under the given parent.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <code>QModelIndex | QPersistentModelIndex</code> <p>The parent index.</p> <code>QModelIndex()</code> <p>Returns:</p> Type Description <code>int</code> <p>Number of loaded children. Returns 0 for nodes whose</p> <code>int</code> <p>children have not been fetched yet; Qt uses hasChildren()</p> <code>int</code> <p>to decide whether to show a disclosure triangle.</p> Source code in <code>client/ayon_core/ui/components/tree_model.py</code> <pre><code>def rowCount(\n    self, parent: QModelIndex | QPersistentModelIndex = QModelIndex()\n) -&gt; int:\n    \"\"\"Return the number of rows under the given parent.\n\n    Args:\n        parent: The parent index.\n\n    Returns:\n        Number of loaded children. Returns 0 for nodes whose\n        children have not been fetched yet; Qt uses hasChildren()\n        to decide whether to show a disclosure triangle.\n    \"\"\"\n    node = self._node_from_index(parent)\n    return len(node.children)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/tree_model.html#client.ayon_core.ui.components.tree_model.TreeNode","title":"<code>TreeNode</code>  <code>dataclass</code>","text":"<p>Represents a single node in a lazy-loaded tree.</p> <p>Attributes:</p> Name Type Description <code>id</code> <code>str</code> <p>Unique identifier for this node.</p> <code>label</code> <code>str</code> <p>Display label shown in the view.</p> <code>has_children</code> <code>bool</code> <p>Whether this node can have children.</p> <code>icon</code> <code>str</code> <p>Optional icon name or path.</p> <code>data</code> <code>dict</code> <p>Arbitrary extra data associated with this node.</p> Source code in <code>client/ayon_core/ui/components/tree_model.py</code> <pre><code>@dataclass\nclass TreeNode:\n    \"\"\"Represents a single node in a lazy-loaded tree.\n\n    Attributes:\n        id: Unique identifier for this node.\n        label: Display label shown in the view.\n        has_children: Whether this node can have children.\n        icon: Optional icon name or path.\n        data: Arbitrary extra data associated with this node.\n    \"\"\"\n\n    id: str\n    label: str\n    has_children: bool = False\n    icon: str = \"\"\n    icon_color: str = \"#f4f5f5\"\n    icon_fill: bool = False\n    data: dict = field(default_factory=dict)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/tree_view.html","title":"tree_view","text":"<p>AYTreeView component module.</p>"},{"location":"autoapi/client/ayon_core/ui/components/tree_view.html#client.ayon_core.ui.components.tree_view.AYTreeView","title":"<code>AYTreeView</code>","text":"<p>               Bases: <code>StyleMixin</code>, <code>QTreeView</code></p> <p>AYON-styled tree view.</p> <p>Fully self-contained: uses AYONStyle for all painting, a custom item delegate that draws directly bypassing any parent QSS, and AYScrollBar instances for scrollbars.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <code>QWidget | None</code> <p>Optional parent widget.</p> <code>None</code> <code>variant</code> <code>QTreeViewVariants</code> <p>Visual style variant controlling background colour and item-state colours.</p> <code>Default</code> Source code in <code>client/ayon_core/ui/components/tree_view.py</code> <pre><code>class AYTreeView(StyleMixin, QTreeView):\n    \"\"\"AYON-styled tree view.\n\n    Fully self-contained: uses AYONStyle for all painting, a custom\n    item delegate that draws directly bypassing any parent QSS, and\n    AYScrollBar instances for scrollbars.\n\n    Args:\n        parent: Optional parent widget.\n        variant: Visual style variant controlling background colour and\n            item-state colours.\n    \"\"\"\n\n    Variants = QTreeViewVariants\n    selection_changed = Signal(QItemSelection, QItemSelection)\n    double_clicked = Signal(QMouseEvent)\n\n    def __init__(\n        self,\n        parent: QWidget | None = None,\n        variant: QTreeViewVariants = QTreeViewVariants.Default,\n    ) -&gt; None:\n        self._variant_str: str = variant.value\n\n        super().__init__(parent)\n\n        style = get_ayon_style()\n        self.setStyle(style)\n\n        # Self-contained: do not inherit parent background or stylesheet.\n        self.setAttribute(Qt.WidgetAttribute.WA_TranslucentBackground, True)\n        self.setAttribute(Qt.WidgetAttribute.WA_Hover, True)\n        self.setMouseTracking(True)\n\n        # Viewport must also be opaque with our colour.\n        self.viewport().setAttribute(\n            Qt.WidgetAttribute.WA_TranslucentBackground, False\n        )\n        self.viewport().setAttribute(Qt.WidgetAttribute.WA_Hover, True)\n        self.viewport().setMouseTracking(True)\n        self.viewport().installEventFilter(self)\n        self._hovered_row_key: tuple | None = None\n        self._sync_viewport_palette()\n\n        # Custom item delegate \u2014 paints items directly, avoids QSS.\n        delegate = TreeViewItemDelegate(\n            parent=self,\n            style_model=style.model,\n            variant=self._variant_str,\n        )\n        self.setItemDelegate(delegate)\n\n        # Styled scrollbars.\n        vsb = AYScrollBar(Qt.Orientation.Vertical, self)\n        self.setVerticalScrollBar(vsb)\n        self.setVerticalScrollBarPolicy(Qt.ScrollBarPolicy.ScrollBarAsNeeded)\n        hsb = AYScrollBar(Qt.Orientation.Horizontal, self)\n        self.setHorizontalScrollBar(hsb)\n        self.setHorizontalScrollBarPolicy(Qt.ScrollBarPolicy.ScrollBarAsNeeded)\n\n        # No header \u2014 single-column hierarchical browser.\n        self.setHeaderHidden(True)\n\n        # Indentation from style data.\n        tv_style = style.model.get_style(\"QTreeView\", self._variant_str)\n        self.setIndentation(int(tv_style.get(\"indent\", 20)))\n\n        # Selection behaviour.\n        self.setSelectionMode(QTreeView.SelectionMode.ExtendedSelection)\n        self.setSelectionBehavior(QTreeView.SelectionBehavior.SelectRows)\n\n        # No default frame \u2014 drawn manually in paintEvent.\n        self.setFrameShape(QTreeView.Shape.NoFrame)\n\n    def _sync_viewport_palette(self) -&gt; None:\n        \"\"\"Apply the variant background colour to the viewport palette.\"\"\"\n        style = get_ayon_style()\n        tv_style = style.model.get_style(\"QTreeView\", self._variant_str)\n        bg = QColor(tv_style.get(\"background-color\", \"#252a31\"))\n        p = self.viewport().palette()\n        p.setColor(QPalette.ColorRole.Base, bg)\n        p.setColor(QPalette.ColorRole.Window, bg)\n        self.viewport().setPalette(p)\n\n    def paintEvent(self, event: QPaintEvent) -&gt; None:\n        \"\"\"Draw the outer container background before the items.\n\n        Args:\n            event: The paint event.\n        \"\"\"\n        painter = QPainter(self.viewport())\n        painter.setRenderHint(QPainter.RenderHint.Antialiasing, False)\n        style = get_ayon_style()\n        tv_style = style.model.get_style(\"QTreeView\", self._variant_str)\n        bg = QColor(tv_style.get(\"background-color\", \"#252a31\"))\n        painter.fillRect(self.viewport().rect(), bg)\n        painter.end()\n\n        # Let QTreeView draw its items on top.\n        super().paintEvent(event)\n\n    def eventFilter(self, obj, event):\n        if obj is self.viewport():\n            if event.type() == QEvent.Type.MouseMove:\n                idx = self.indexAt(event.pos())\n                key = (idx.row(), idx.parent()) if idx.isValid() else None\n                if key != self._hovered_row_key:\n                    self._hovered_row_key = key\n                    self.viewport().update()\n            elif event.type() == QEvent.Type.Leave:\n                if self._hovered_row_key is not None:\n                    self._hovered_row_key = None\n                    self.viewport().update()\n        return super().eventFilter(obj, event)\n\n    def drawBranches(self, painter, rect, index):\n        \"\"\"Draw branch indicators with AYONStyle directly.\n\n        Bypasses ``self.style()`` because, when an application-level QSS is\n        active, Qt wraps the widget's style in a ``QStyleSheetStyle`` proxy\n        which would otherwise intercept ``PE_IndicatorBranch`` and apply\n        QSS ``QTreeView::branch`` rules on top of (or instead of) ours.\n        \"\"\"\n        style = get_ayon_style()  # the raw AYONStyle, never wrapped\n\n        opt = QStyleOption()\n        opt.rect = rect\n        opt.palette = self.palette()\n        state = QStyle.StateFlag.State_Item\n        if self.model() is not None and self.model().hasChildren(index):\n            state |= QStyle.StateFlag.State_Children\n        if self.isExpanded(index):\n            state |= QStyle.StateFlag.State_Open\n        if self.selectionModel().isSelected(index):\n            state |= QStyle.StateFlag.State_Selected\n        if self.isEnabled():\n            state |= QStyle.StateFlag.State_Enabled\n\n        # Row-level hover: is the cursor on the same row as `index`?\n        hovered_index = self.indexAt(\n            self.viewport().mapFromGlobal(QCursor.pos())\n        )\n        if (\n            hovered_index.isValid()\n            and hovered_index.row() == index.row()\n            and hovered_index.parent() == index.parent()\n        ):\n            state |= QStyle.StateFlag.State_MouseOver\n\n        opt.state = state\n\n        # Call our drawer directly, not through self.style().\n        style.drawers[\n            enum_to_str(\n                QStyle.PrimitiveElement,\n                QStyle.PrimitiveElement.PE_IndicatorBranch,\n                \"QTreeView\",\n            )\n        ](opt, painter, self)\n\n    def mouseDoubleClickEvent(self, event: QMouseEvent) -&gt; None:\n        \"\"\"Emit double_clicked signal on double-click.\"\"\"\n        self.double_clicked.emit(event)\n        super().mouseDoubleClickEvent(event)\n\n    def mousePressEvent(self, event) -&gt; None:\n        \"\"\"Deselect all items when clicking in an empty area.\"\"\"\n        index = self.indexAt(event.pos())\n        if not index.isValid():\n            self.clearSelection()\n            self.setCurrentIndex(self.rootIndex())\n            return\n        super().mousePressEvent(event)\n\n    def selectionChanged(\n        self,\n        selected: QItemSelection,\n        deselected: QItemSelection,\n    ) -&gt; None:\n        \"\"\"Override to emit a public signal on selection change.\n\n        Args:\n            selected: Newly selected items.\n            deselected: Newly deselected items.\n        \"\"\"\n        super().selectionChanged(selected, deselected)\n        self.selection_changed.emit(selected, deselected)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/tree_view.html#client.ayon_core.ui.components.tree_view.AYTreeView.drawBranches","title":"<code>drawBranches(painter, rect, index)</code>","text":"<p>Draw branch indicators with AYONStyle directly.</p> <p>Bypasses <code>self.style()</code> because, when an application-level QSS is active, Qt wraps the widget's style in a <code>QStyleSheetStyle</code> proxy which would otherwise intercept <code>PE_IndicatorBranch</code> and apply QSS <code>QTreeView::branch</code> rules on top of (or instead of) ours.</p> Source code in <code>client/ayon_core/ui/components/tree_view.py</code> <pre><code>def drawBranches(self, painter, rect, index):\n    \"\"\"Draw branch indicators with AYONStyle directly.\n\n    Bypasses ``self.style()`` because, when an application-level QSS is\n    active, Qt wraps the widget's style in a ``QStyleSheetStyle`` proxy\n    which would otherwise intercept ``PE_IndicatorBranch`` and apply\n    QSS ``QTreeView::branch`` rules on top of (or instead of) ours.\n    \"\"\"\n    style = get_ayon_style()  # the raw AYONStyle, never wrapped\n\n    opt = QStyleOption()\n    opt.rect = rect\n    opt.palette = self.palette()\n    state = QStyle.StateFlag.State_Item\n    if self.model() is not None and self.model().hasChildren(index):\n        state |= QStyle.StateFlag.State_Children\n    if self.isExpanded(index):\n        state |= QStyle.StateFlag.State_Open\n    if self.selectionModel().isSelected(index):\n        state |= QStyle.StateFlag.State_Selected\n    if self.isEnabled():\n        state |= QStyle.StateFlag.State_Enabled\n\n    # Row-level hover: is the cursor on the same row as `index`?\n    hovered_index = self.indexAt(\n        self.viewport().mapFromGlobal(QCursor.pos())\n    )\n    if (\n        hovered_index.isValid()\n        and hovered_index.row() == index.row()\n        and hovered_index.parent() == index.parent()\n    ):\n        state |= QStyle.StateFlag.State_MouseOver\n\n    opt.state = state\n\n    # Call our drawer directly, not through self.style().\n    style.drawers[\n        enum_to_str(\n            QStyle.PrimitiveElement,\n            QStyle.PrimitiveElement.PE_IndicatorBranch,\n            \"QTreeView\",\n        )\n    ](opt, painter, self)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/tree_view.html#client.ayon_core.ui.components.tree_view.AYTreeView.mouseDoubleClickEvent","title":"<code>mouseDoubleClickEvent(event)</code>","text":"<p>Emit double_clicked signal on double-click.</p> Source code in <code>client/ayon_core/ui/components/tree_view.py</code> <pre><code>def mouseDoubleClickEvent(self, event: QMouseEvent) -&gt; None:\n    \"\"\"Emit double_clicked signal on double-click.\"\"\"\n    self.double_clicked.emit(event)\n    super().mouseDoubleClickEvent(event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/tree_view.html#client.ayon_core.ui.components.tree_view.AYTreeView.mousePressEvent","title":"<code>mousePressEvent(event)</code>","text":"<p>Deselect all items when clicking in an empty area.</p> Source code in <code>client/ayon_core/ui/components/tree_view.py</code> <pre><code>def mousePressEvent(self, event) -&gt; None:\n    \"\"\"Deselect all items when clicking in an empty area.\"\"\"\n    index = self.indexAt(event.pos())\n    if not index.isValid():\n        self.clearSelection()\n        self.setCurrentIndex(self.rootIndex())\n        return\n    super().mousePressEvent(event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/tree_view.html#client.ayon_core.ui.components.tree_view.AYTreeView.paintEvent","title":"<code>paintEvent(event)</code>","text":"<p>Draw the outer container background before the items.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <code>QPaintEvent</code> <p>The paint event.</p> required Source code in <code>client/ayon_core/ui/components/tree_view.py</code> <pre><code>def paintEvent(self, event: QPaintEvent) -&gt; None:\n    \"\"\"Draw the outer container background before the items.\n\n    Args:\n        event: The paint event.\n    \"\"\"\n    painter = QPainter(self.viewport())\n    painter.setRenderHint(QPainter.RenderHint.Antialiasing, False)\n    style = get_ayon_style()\n    tv_style = style.model.get_style(\"QTreeView\", self._variant_str)\n    bg = QColor(tv_style.get(\"background-color\", \"#252a31\"))\n    painter.fillRect(self.viewport().rect(), bg)\n    painter.end()\n\n    # Let QTreeView draw its items on top.\n    super().paintEvent(event)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/tree_view.html#client.ayon_core.ui.components.tree_view.AYTreeView.selectionChanged","title":"<code>selectionChanged(selected, deselected)</code>","text":"<p>Override to emit a public signal on selection change.</p> <p>Parameters:</p> Name Type Description Default <code>selected</code> <code>QItemSelection</code> <p>Newly selected items.</p> required <code>deselected</code> <code>QItemSelection</code> <p>Newly deselected items.</p> required Source code in <code>client/ayon_core/ui/components/tree_view.py</code> <pre><code>def selectionChanged(\n    self,\n    selected: QItemSelection,\n    deselected: QItemSelection,\n) -&gt; None:\n    \"\"\"Override to emit a public signal on selection change.\n\n    Args:\n        selected: Newly selected items.\n        deselected: Newly deselected items.\n    \"\"\"\n    super().selectionChanged(selected, deselected)\n    self.selection_changed.emit(selected, deselected)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/tree_view.html#client.ayon_core.ui.components.tree_view.TreeViewItemDelegate","title":"<code>TreeViewItemDelegate</code>","text":"<p>               Bases: <code>StyleMixin</code>, <code>QStyledItemDelegate</code></p> <p>Item delegate for AYTreeView that paints directly, bypassing QSS.</p> <p>Reads style data from the QTreeView style entry to draw item backgrounds (hover, selected) and text/icons.  The paint() method uses raw QPainter calls so that a parent-level QStyleSheet cannot intercept and override the colours.</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <code>QWidget | None</code> <p>The parent widget (expected to be an AYTreeView instance).</p> <code>None</code> <code>style_model</code> <code>StyleData | None</code> <p>StyleData instance providing colour/dimension data.</p> <code>None</code> <code>variant</code> <code>str</code> <p>The variant string used to look up the correct style.</p> <code>'default'</code> Source code in <code>client/ayon_core/ui/components/tree_view.py</code> <pre><code>class TreeViewItemDelegate(StyleMixin, QStyledItemDelegate):\n    \"\"\"Item delegate for AYTreeView that paints directly, bypassing QSS.\n\n    Reads style data from the QTreeView style entry to draw item\n    backgrounds (hover, selected) and text/icons.  The paint() method\n    uses raw QPainter calls so that a parent-level QStyleSheet cannot\n    intercept and override the colours.\n\n    Args:\n        parent: The parent widget (expected to be an AYTreeView instance).\n        style_model: StyleData instance providing colour/dimension data.\n        variant: The variant string used to look up the correct style.\n    \"\"\"\n\n    def __init__(\n        self,\n        parent: QWidget | None = None,\n        style_model: StyleData | None = None,\n        variant: str = \"default\",\n    ) -&gt; None:\n        super().__init__(parent)\n        self._style_model = style_model\n        self._variant_str = variant\n        self._icon_cache: dict[str, QIcon] = {}\n\n    def _tv_styles(self) -&gt; dict[str, dict]:\n        \"\"\"Return *base*, *hover* and *selected* style dicts at once.\"\"\"\n        if self._style_model is None:\n            return {\"base\": {}, \"hover\": {}, \"selected\": {}}\n        return self._style_model.get_styles(\n            \"QTreeView\",\n            self._variant_str,\n            [\"base\", \"hover\", \"selected\"],\n        )\n\n    def initStyleOption(\n        self,\n        option: QStyleOptionViewItem,\n        index: QModelIndex | QPersistentModelIndex,\n    ) -&gt; None:\n        \"\"\"Initialize the style option with the default implementation, then\n        override any properties needed for our custom painting.\n\n        Args:\n            option: The style option to initialize.\n            index: The model index of the item.\n        \"\"\"\n        super().initStyleOption(option, index)\n        option.font = self.font()\n        option.fontMetrics = self.fontMetrics()\n\n    def sizeHint(\n        self,\n        option: QStyleOptionViewItem,\n        index: QModelIndex | QPersistentModelIndex,\n    ) -&gt; QSize:\n        \"\"\"Return a fixed row height from the style data.\n\n        Args:\n            option: The style option for the item.\n            index: The model index of the item.\n\n        Returns:\n            The size hint for the item.\n        \"\"\"\n        if self._style_model:\n            style = self._style_model.get_style(\"QTreeView\", self._variant_str)\n            h = int(style.get(\"item-height\", 28))\n        else:\n            h = 28\n        return QSize(option.rect.width(), h)\n\n    def paint(\n        self,\n        painter: QPainter,\n        option: QStyleOptionViewItem,\n        index: QModelIndex | QPersistentModelIndex,\n    ) -&gt; None:\n        \"\"\"Paint a tree-view item directly, bypassing QStyle completely.\n\n        Args:\n            painter: The QPainter to use.\n            option: The style option for the item.\n            index: The model index of the item.\n        \"\"\"\n        painter.save()\n        painter.setRenderHint(QPainter.RenderHint.Antialiasing)\n\n        opt = QStyleOptionViewItem(option)\n        self.initStyleOption(opt, index)\n\n        state = opt.state\n        is_selected = bool(state &amp; QStyle.StateFlag.State_Selected)\n        is_hovered = bool(state &amp; QStyle.StateFlag.State_MouseOver)\n\n        styles = self._tv_styles()\n        base_style = styles[\"base\"]\n        hover_style = styles[\"hover\"]\n        selected_style = styles[\"selected\"]\n\n        item_padding = base_style.get(\"item-padding\", [4, 8])\n        icon_text_spacing = int(base_style.get(\"icon-text-spacing\", 6))\n\n        # --- background ------------------------------------------------\n        if is_selected:\n            bg_color = QColor(\n                selected_style.get(\n                    \"background-color\",\n                    base_style.get(\"background-color\", \"transparent\"),\n                )\n            )\n        elif is_hovered:\n            bg_color = QColor(\n                hover_style.get(\n                    \"background-color\",\n                    base_style.get(\"background-color\", \"transparent\"),\n                )\n            )\n        else:\n            bg_color = QColor(\n                base_style.get(\"background-color\", \"transparent\")\n            )\n\n        painter.setBrush(QBrush(bg_color))\n        painter.setPen(Qt.PenStyle.NoPen)\n        painter.drawRect(opt.rect)\n\n        # --- text colour -----------------------------------------------\n        if is_selected:\n            text_color = QColor(\n                selected_style.get(\n                    \"color\",\n                    base_style.get(\"color\", \"#f4f5f5\"),\n                )\n            )\n        else:\n            text_color = QColor(base_style.get(\"color\", \"#f4f5f5\"))\n\n        # disabled dimming\n        if not (state &amp; QStyle.StateFlag.State_Enabled):\n            text_color.setAlpha(\n                int(\n                    text_color.alpha()\n                    * base_style.get(\"disabled-opacity\", 0.5)\n                )\n            )\n\n        # --- icon + text layout ----------------------------------------\n        content_rect = QRect(opt.rect).adjusted(\n            item_padding[1],\n            item_padding[0],\n            -item_padding[1],\n            -item_padding[0],\n        )\n        content_left = content_rect.left()\n\n        if not opt.icon.isNull():\n            icon_size = opt.decorationSize\n            icon_rect = QRect(\n                content_left,\n                opt.rect.center().y() - icon_size.height() // 2,\n                icon_size.width(),\n                icon_size.height(),\n            )\n            mode = (\n                QIcon.Mode.Normal\n                if state &amp; QStyle.StateFlag.State_Enabled\n                else QIcon.Mode.Disabled\n            )\n            opt.icon.paint(\n                painter,\n                icon_rect,\n                Qt.AlignmentFlag.AlignCenter,\n                mode,\n            )\n            content_left = icon_rect.right() + icon_text_spacing\n\n        if opt.text:\n            text_rect = QRect(opt.rect)\n            text_rect.setLeft(content_left)\n            text_rect.setRight(content_rect.right())\n            painter.setPen(text_color)\n            painter.setFont(opt.font)\n            painter.drawText(\n                text_rect,\n                Qt.AlignmentFlag.AlignVCenter | Qt.AlignmentFlag.AlignLeft,\n                opt.text,\n            )\n\n        painter.restore()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/tree_view.html#client.ayon_core.ui.components.tree_view.TreeViewItemDelegate.initStyleOption","title":"<code>initStyleOption(option, index)</code>","text":"<p>Initialize the style option with the default implementation, then override any properties needed for our custom painting.</p> <p>Parameters:</p> Name Type Description Default <code>option</code> <code>QStyleOptionViewItem</code> <p>The style option to initialize.</p> required <code>index</code> <code>QModelIndex | QPersistentModelIndex</code> <p>The model index of the item.</p> required Source code in <code>client/ayon_core/ui/components/tree_view.py</code> <pre><code>def initStyleOption(\n    self,\n    option: QStyleOptionViewItem,\n    index: QModelIndex | QPersistentModelIndex,\n) -&gt; None:\n    \"\"\"Initialize the style option with the default implementation, then\n    override any properties needed for our custom painting.\n\n    Args:\n        option: The style option to initialize.\n        index: The model index of the item.\n    \"\"\"\n    super().initStyleOption(option, index)\n    option.font = self.font()\n    option.fontMetrics = self.fontMetrics()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/tree_view.html#client.ayon_core.ui.components.tree_view.TreeViewItemDelegate.paint","title":"<code>paint(painter, option, index)</code>","text":"<p>Paint a tree-view item directly, bypassing QStyle completely.</p> <p>Parameters:</p> Name Type Description Default <code>painter</code> <code>QPainter</code> <p>The QPainter to use.</p> required <code>option</code> <code>QStyleOptionViewItem</code> <p>The style option for the item.</p> required <code>index</code> <code>QModelIndex | QPersistentModelIndex</code> <p>The model index of the item.</p> required Source code in <code>client/ayon_core/ui/components/tree_view.py</code> <pre><code>def paint(\n    self,\n    painter: QPainter,\n    option: QStyleOptionViewItem,\n    index: QModelIndex | QPersistentModelIndex,\n) -&gt; None:\n    \"\"\"Paint a tree-view item directly, bypassing QStyle completely.\n\n    Args:\n        painter: The QPainter to use.\n        option: The style option for the item.\n        index: The model index of the item.\n    \"\"\"\n    painter.save()\n    painter.setRenderHint(QPainter.RenderHint.Antialiasing)\n\n    opt = QStyleOptionViewItem(option)\n    self.initStyleOption(opt, index)\n\n    state = opt.state\n    is_selected = bool(state &amp; QStyle.StateFlag.State_Selected)\n    is_hovered = bool(state &amp; QStyle.StateFlag.State_MouseOver)\n\n    styles = self._tv_styles()\n    base_style = styles[\"base\"]\n    hover_style = styles[\"hover\"]\n    selected_style = styles[\"selected\"]\n\n    item_padding = base_style.get(\"item-padding\", [4, 8])\n    icon_text_spacing = int(base_style.get(\"icon-text-spacing\", 6))\n\n    # --- background ------------------------------------------------\n    if is_selected:\n        bg_color = QColor(\n            selected_style.get(\n                \"background-color\",\n                base_style.get(\"background-color\", \"transparent\"),\n            )\n        )\n    elif is_hovered:\n        bg_color = QColor(\n            hover_style.get(\n                \"background-color\",\n                base_style.get(\"background-color\", \"transparent\"),\n            )\n        )\n    else:\n        bg_color = QColor(\n            base_style.get(\"background-color\", \"transparent\")\n        )\n\n    painter.setBrush(QBrush(bg_color))\n    painter.setPen(Qt.PenStyle.NoPen)\n    painter.drawRect(opt.rect)\n\n    # --- text colour -----------------------------------------------\n    if is_selected:\n        text_color = QColor(\n            selected_style.get(\n                \"color\",\n                base_style.get(\"color\", \"#f4f5f5\"),\n            )\n        )\n    else:\n        text_color = QColor(base_style.get(\"color\", \"#f4f5f5\"))\n\n    # disabled dimming\n    if not (state &amp; QStyle.StateFlag.State_Enabled):\n        text_color.setAlpha(\n            int(\n                text_color.alpha()\n                * base_style.get(\"disabled-opacity\", 0.5)\n            )\n        )\n\n    # --- icon + text layout ----------------------------------------\n    content_rect = QRect(opt.rect).adjusted(\n        item_padding[1],\n        item_padding[0],\n        -item_padding[1],\n        -item_padding[0],\n    )\n    content_left = content_rect.left()\n\n    if not opt.icon.isNull():\n        icon_size = opt.decorationSize\n        icon_rect = QRect(\n            content_left,\n            opt.rect.center().y() - icon_size.height() // 2,\n            icon_size.width(),\n            icon_size.height(),\n        )\n        mode = (\n            QIcon.Mode.Normal\n            if state &amp; QStyle.StateFlag.State_Enabled\n            else QIcon.Mode.Disabled\n        )\n        opt.icon.paint(\n            painter,\n            icon_rect,\n            Qt.AlignmentFlag.AlignCenter,\n            mode,\n        )\n        content_left = icon_rect.right() + icon_text_spacing\n\n    if opt.text:\n        text_rect = QRect(opt.rect)\n        text_rect.setLeft(content_left)\n        text_rect.setRight(content_rect.right())\n        painter.setPen(text_color)\n        painter.setFont(opt.font)\n        painter.drawText(\n            text_rect,\n            Qt.AlignmentFlag.AlignVCenter | Qt.AlignmentFlag.AlignLeft,\n            opt.text,\n        )\n\n    painter.restore()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/tree_view.html#client.ayon_core.ui.components.tree_view.TreeViewItemDelegate.sizeHint","title":"<code>sizeHint(option, index)</code>","text":"<p>Return a fixed row height from the style data.</p> <p>Parameters:</p> Name Type Description Default <code>option</code> <code>QStyleOptionViewItem</code> <p>The style option for the item.</p> required <code>index</code> <code>QModelIndex | QPersistentModelIndex</code> <p>The model index of the item.</p> required <p>Returns:</p> Type Description <code>QSize</code> <p>The size hint for the item.</p> Source code in <code>client/ayon_core/ui/components/tree_view.py</code> <pre><code>def sizeHint(\n    self,\n    option: QStyleOptionViewItem,\n    index: QModelIndex | QPersistentModelIndex,\n) -&gt; QSize:\n    \"\"\"Return a fixed row height from the style data.\n\n    Args:\n        option: The style option for the item.\n        index: The model index of the item.\n\n    Returns:\n        The size hint for the item.\n    \"\"\"\n    if self._style_model:\n        style = self._style_model.get_style(\"QTreeView\", self._variant_str)\n        h = int(style.get(\"item-height\", 28))\n    else:\n        h = 28\n    return QSize(option.rect.width(), h)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/user_image.html","title":"user_image","text":""},{"location":"autoapi/client/ayon_core/ui/components/user_image.html#client.ayon_core.ui.components.user_image.AYUserImage","title":"<code>AYUserImage</code>","text":"<p>               Bases: <code>StyleMixin</code>, <code>QLabel</code></p> Source code in <code>client/ayon_core/ui/components/user_image.py</code> <pre><code>class AYUserImage(StyleMixin, QtWidgets.QLabel):\n    Variants = AYUserImageVariants\n\n    def __init__(\n        self,\n        *args,\n        src: Path | str = \"\",\n        name: str = \"\",\n        full_name: str = \"\",\n        size: int = 30,\n        highlight: bool = False,\n        outline: bool = True,\n        file_cacher: Callable | None = None,\n        variant=Variants.Default,\n        **kwargs,\n    ):\n        # file path to icon\n        self._src = src\n        # short user name\n        self._name = name\n        # full user name\n        self._full_name = full_name\n        # pixmap size\n        self._size = size\n        # green outline if true, light grey otherwise\n        self._highlight = highlight\n        # enable / disable outline\n        self._outline = outline\n        # a file loader function for the image cache: src is the cache key.\n        self._file_cacher = file_cacher\n        # Variant string for looking up style data\n        self._variant_str = variant.value\n\n        super().__init__(*args, **kwargs)\n        stl = get_ayon_style()\n        self.setStyle(stl)\n        self._style = stl.model.get_style(\n            \"AYUserImage\", variant=self._variant_str\n        )\n        self._style.set_context(self)\n\n        self.set_image()\n\n    def _get_colors(\n        self,\n    ) -&gt; tuple[QtGui.QColor, QtGui.QColor, QtGui.QColor, QtGui.QColor]:\n        \"\"\"Read image colors from the AYUserImage style data.\n\n        Returns:\n            A tuple of (initials_bg, outline_color, highlight_color).\n        \"\"\"\n        style = self._style\n        fg = QtGui.QColor(style.get(\"initials-color\", \"#ffffff\"))\n        bg = QtGui.QColor(style.get(\"initials-background-color\", \"#484875\"))\n        outline = QtGui.QColor(style.get(\"outline-color\", \"#e1e1e1\"))\n        highlight = QtGui.QColor(style.get(\"highlight-color\", \"#6be1ac\"))\n        return fg, bg, outline, highlight\n\n    def set_image(self) -&gt; None:\n        \"\"\"Render the user avatar pixmap and assign it to this label.\"\"\"\n        fg_color, bg_color, outline_color, highlight_color = self._get_colors()\n        active_outline = highlight_color if self._highlight else outline_color\n\n        dpr = self.devicePixelRatioF()\n        dpr_size = int(self._size * dpr)\n        has_outline = self._outline or self._highlight\n        line_width = 1 if has_outline else 0\n        half_line = line_width / 2\n\n        self.pxm = QtGui.QPixmap(dpr_size, dpr_size)\n        self.pxm.setDevicePixelRatio(dpr)\n        self.pxm.fill(QtCore.Qt.GlobalColor.transparent)\n        painter = QtGui.QPainter(self.pxm)\n        painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)\n        painter.setFont(self.font())\n\n        if self._src:\n            if not Path(str(self._src)).exists() and self._file_cacher:\n                ic = ImageCache.get_instance()\n                self._src = ic.get(\n                    str(self._src), partial(self._file_cacher, self._src)\n                )\n\n            # Load and draw src icon file in a circle\n            source_pixmap = QtGui.QPixmap(self._src)\n            source_pixmap.setDevicePixelRatio(dpr)\n            if not source_pixmap.isNull():\n                # Scale the source image to fit within the circle (with some\n                # margin for outline)\n\n                # Leave space for outline\n                inner_size = int(dpr_size - line_width)\n                scaled_pixmap = source_pixmap.scaled(\n                    inner_size,\n                    inner_size,\n                    QtCore.Qt.AspectRatioMode.KeepAspectRatioByExpanding,\n                    QtCore.Qt.TransformationMode.SmoothTransformation,\n                )\n\n                # Create circular clipping path\n                clip_path = QtGui.QPainterPath()\n                clip_path.addEllipse(0, 0, self._size, self._size)\n                painter.setClipPath(clip_path)\n\n                # Draw the scaled image centered\n                x = (dpr_size - scaled_pixmap.width()) // 2\n                y = (dpr_size - scaled_pixmap.height()) // 2\n                painter.drawPixmap(x, y, scaled_pixmap)\n\n                # Reset clipping\n                painter.setClipping(False)\n            else:\n                log.warning(\"Could not load src %s\", self._src)\n        else:\n            initials = \"?\"\n            if self._full_name:\n                initials = \"\".join([p[0] for p in self._full_name.split()])\n            elif self._name:\n                initials = self._name[0]\n\n            # Draw a circle with white initials over a color background\n\n            # Fill circle with background color from style\n            painter.setBrush(QtGui.QBrush(bg_color))\n            painter.setPen(QtCore.Qt.PenStyle.NoPen)\n            painter.drawEllipse(0, 0, self._size, self._size)\n\n            # Draw white initials\n            painter.setPen(QtGui.QPen(fg_color))\n            font = painter.font()\n            pt_size = max(8, self._size // 2 - 3)\n            font.setPointSizeF(pt_size)\n            painter.setFont(font)\n\n            painter.drawText(\n                QtCore.QRect(0, 0, self._size, self._size),\n                QtCore.Qt.AlignmentFlag.AlignCenter,\n                initials.upper(),\n            )\n\n        # Draw outline\n        if has_outline:\n            painter.setBrush(QtCore.Qt.BrushStyle.NoBrush)\n            painter.setPen(QtGui.QPen(active_outline, line_width))\n            painter.drawEllipse(\n                QtCore.QRectF(\n                    half_line,\n                    half_line,\n                    self._size - line_width,\n                    self._size - line_width,\n                )\n            )\n\n        painter.end()\n\n        # Set the pixmap to the label\n        self.setPixmap(self.pxm)\n\n    def update_params(self, src, full_name):\n        self._src = src\n        self._full_name = full_name\n        self.set_image()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/components/user_image.html#client.ayon_core.ui.components.user_image.AYUserImage.set_image","title":"<code>set_image()</code>","text":"<p>Render the user avatar pixmap and assign it to this label.</p> Source code in <code>client/ayon_core/ui/components/user_image.py</code> <pre><code>def set_image(self) -&gt; None:\n    \"\"\"Render the user avatar pixmap and assign it to this label.\"\"\"\n    fg_color, bg_color, outline_color, highlight_color = self._get_colors()\n    active_outline = highlight_color if self._highlight else outline_color\n\n    dpr = self.devicePixelRatioF()\n    dpr_size = int(self._size * dpr)\n    has_outline = self._outline or self._highlight\n    line_width = 1 if has_outline else 0\n    half_line = line_width / 2\n\n    self.pxm = QtGui.QPixmap(dpr_size, dpr_size)\n    self.pxm.setDevicePixelRatio(dpr)\n    self.pxm.fill(QtCore.Qt.GlobalColor.transparent)\n    painter = QtGui.QPainter(self.pxm)\n    painter.setRenderHint(QtGui.QPainter.RenderHint.Antialiasing, True)\n    painter.setFont(self.font())\n\n    if self._src:\n        if not Path(str(self._src)).exists() and self._file_cacher:\n            ic = ImageCache.get_instance()\n            self._src = ic.get(\n                str(self._src), partial(self._file_cacher, self._src)\n            )\n\n        # Load and draw src icon file in a circle\n        source_pixmap = QtGui.QPixmap(self._src)\n        source_pixmap.setDevicePixelRatio(dpr)\n        if not source_pixmap.isNull():\n            # Scale the source image to fit within the circle (with some\n            # margin for outline)\n\n            # Leave space for outline\n            inner_size = int(dpr_size - line_width)\n            scaled_pixmap = source_pixmap.scaled(\n                inner_size,\n                inner_size,\n                QtCore.Qt.AspectRatioMode.KeepAspectRatioByExpanding,\n                QtCore.Qt.TransformationMode.SmoothTransformation,\n            )\n\n            # Create circular clipping path\n            clip_path = QtGui.QPainterPath()\n            clip_path.addEllipse(0, 0, self._size, self._size)\n            painter.setClipPath(clip_path)\n\n            # Draw the scaled image centered\n            x = (dpr_size - scaled_pixmap.width()) // 2\n            y = (dpr_size - scaled_pixmap.height()) // 2\n            painter.drawPixmap(x, y, scaled_pixmap)\n\n            # Reset clipping\n            painter.setClipping(False)\n        else:\n            log.warning(\"Could not load src %s\", self._src)\n    else:\n        initials = \"?\"\n        if self._full_name:\n            initials = \"\".join([p[0] for p in self._full_name.split()])\n        elif self._name:\n            initials = self._name[0]\n\n        # Draw a circle with white initials over a color background\n\n        # Fill circle with background color from style\n        painter.setBrush(QtGui.QBrush(bg_color))\n        painter.setPen(QtCore.Qt.PenStyle.NoPen)\n        painter.drawEllipse(0, 0, self._size, self._size)\n\n        # Draw white initials\n        painter.setPen(QtGui.QPen(fg_color))\n        font = painter.font()\n        pt_size = max(8, self._size // 2 - 3)\n        font.setPointSizeF(pt_size)\n        painter.setFont(font)\n\n        painter.drawText(\n            QtCore.QRect(0, 0, self._size, self._size),\n            QtCore.Qt.AlignmentFlag.AlignCenter,\n            initials.upper(),\n        )\n\n    # Draw outline\n    if has_outline:\n        painter.setBrush(QtCore.Qt.BrushStyle.NoBrush)\n        painter.setPen(QtGui.QPen(active_outline, line_width))\n        painter.drawEllipse(\n            QtCore.QRectF(\n                half_line,\n                half_line,\n                self._size - line_width,\n                self._size - line_width,\n            )\n        )\n\n    painter.end()\n\n    # Set the pixmap to the label\n    self.setPixmap(self.pxm)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/index.html","title":"drawers","text":"<p>AYONStyle drawer modules.</p> <p>Each drawer handles custom QPainter-based painting for a specific Qt widget class, registered with the AYONStyle instance via register_drawers / register_sizers / register_metrics.</p>"},{"location":"autoapi/client/ayon_core/ui/drawers/index.html#client.ayon_core.ui.drawers.ButtonDrawer","title":"<code>ButtonDrawer</code>","text":"Source code in <code>client/ayon_core/ui/drawers/button.py</code> <pre><code>class ButtonDrawer:\n    def __init__(self, style_inst: AYONStyle) -&gt; None:\n        self.style_inst = style_inst\n        self.model = style_inst.model\n\n    @property\n    def _super(self):\n        \"\"\"Return proxy for calling QCommonStyle methods on style_inst.\"\"\"\n        from ..style import AYONStyle as _AYONStyle\n\n        return super(_AYONStyle, self.style_inst)\n\n    @property\n    def base_class(self):\n        return {\"QPushButton\": QPushButton}\n\n    def register_drawers(self):\n        return {\n            enum_to_str(\n                QStyle.ControlElement,\n                QStyle.ControlElement.CE_PushButton,\n                \"QPushButton\",\n            ): [\n                partial(\n                    self.style_inst.drawControl,\n                    QStyle.ControlElement.CE_PushButtonBevel,\n                ),\n                partial(\n                    self.style_inst.drawControl,\n                    QStyle.ControlElement.CE_PushButtonLabel,\n                ),\n            ],\n            enum_to_str(\n                QStyle.ControlElement,\n                QStyle.ControlElement.CE_PushButtonBevel,\n                \"QPushButton\",\n            ): self.draw_push_button_bevel,\n            enum_to_str(\n                QStyle.ControlElement,\n                QStyle.ControlElement.CE_PushButtonLabel,\n                \"QPushButton\",\n            ): self.draw_push_button_label,\n        }\n\n    def register_sizers(self):\n        return {\n            enum_to_str(\n                QStyle.ContentsType,\n                QStyle.ContentsType.CT_PushButton,\n                \"QPushButton\",\n            ): self.calculate_push_button_size,\n            enum_to_str(\n                QStyle.SubElement,\n                QStyle.SubElement.SE_PushButtonContents,\n                \"QPushButton\",\n            ): self.sub_element_rect,\n            enum_to_str(\n                QStyle.SubElement,\n                QStyle.SubElement.SE_PushButtonFocusRect,\n                \"QPushButton\",\n            ): self.sub_element_rect,\n        }\n\n    def register_metrics(self):\n        return {\n            enum_to_str(\n                QStyle.PixelMetric,\n                QStyle.PixelMetric.PM_ButtonMargin,\n                \"QPushButton\",\n            ): self.get_metric,\n            enum_to_str(\n                QStyle.PixelMetric,\n                QStyle.PixelMetric.PM_DefaultFrameWidth,\n                \"QPushButton\",\n            ): self.get_metric,\n            enum_to_str(\n                QStyle.PixelMetric,\n                QStyle.PixelMetric.PM_ButtonDefaultIndicator,\n                \"QPushButton\",\n            ): self.get_metric,\n            enum_to_str(\n                QStyle.PixelMetric,\n                QStyle.PixelMetric.PM_FocusFrameVMargin,\n                \"QPushButton\",\n            ): self.get_metric,\n            enum_to_str(\n                QStyle.PixelMetric,\n                QStyle.PixelMetric.PM_FocusFrameHMargin,\n                \"QPushButton\",\n            ): self.get_metric,\n        }\n\n    def get_metric(\n        self,\n        metric: QStyle.PixelMetric,\n        opt: QStyleOption | None = None,\n        widget: QWidget | None = None,\n    ):\n        if metric == QStyle.PixelMetric.PM_ButtonMargin:\n            return 6\n        elif metric == QStyle.PixelMetric.PM_DefaultFrameWidth:\n            return 0\n        elif metric == QStyle.PixelMetric.PM_ButtonDefaultIndicator:\n            return 0\n        elif metric == QStyle.PixelMetric.PM_FocusFrameVMargin:\n            return 2\n        elif metric == QStyle.PixelMetric.PM_FocusFrameHMargin:\n            return 2\n\n    def get_button_variant(self, widget: QWidget) -&gt; str:\n        \"\"\"Extract button variant from widget properties.\"\"\"\n        if widget is None:\n            return \"surface\"\n        return getattr(widget, \"_variant_str\", \"surface\")\n\n    def get_button_has_icon(self, widget: QWidget) -&gt; bool:\n        \"\"\"Check if button has an icon.\"\"\"\n        if widget is None:\n            return False\n\n        # Method 1: Try has_icon property\n        if hasattr(widget, \"has_icon\"):\n            return widget.has_icon  # type: ignore\n\n        # Method 2: Try Qt property\n        has_icon_prop = widget.property(\"has_icon\")\n        if has_icon_prop is not None:\n            return bool(has_icon_prop)\n\n        # Method 3: Check the actual icon\n        return bool(widget.icon() and not widget.icon().isNull())  # type: ignore\n\n    def get_button_style(\n        self, widget: QWidget, state: QStyle.StateFlag\n    ) -&gt; tuple[dict, str]:\n        \"\"\"Get the appropriate style dictionary for the widget's variant and\n        state.\"\"\"\n        variant = self.get_button_variant(widget)\n\n        wstate = \"base\"\n        if not (state &amp; QStyle.StateFlag.State_Enabled):\n            wstate = \"disabled\"\n        elif state &amp; QStyle.StateFlag.State_Sunken:\n            wstate = \"pressed\"\n        elif (state &amp; QStyle.StateFlag.State_MouseOver and not (\n            state &amp; QStyle.StateFlag.State_On) or widget.underMouse()\n        ):\n            wstate = \"hover\"\n        elif state &amp; QStyle.StateFlag.State_On:\n            wstate = \"checked\"\n\n        style = self.model.get_style(\"QPushButton\", variant, wstate)\n        style.set_context(widget)\n\n        return style, wstate\n\n    def draw_push_button_bevel(\n        self,\n        option: QStyleOption,\n        painter: QPainter,\n        widget: QWidget | None,\n    ) -&gt; None:\n        \"\"\"Draw the button background and frame with hover detection.\"\"\"\n        if not isinstance(option, QStyleOptionButton) or widget is None:\n            return\n\n        style, _ = self.get_button_style(widget, option.state)\n        rect = option.rect\n\n        painter.save()\n        painter.setRenderHint(QPainter.RenderHint.Antialiasing)\n\n        # Draw button background with hover awareness\n        bg_color = style[\"background-color\"]\n        painter.setOpacity(style.get(\"opacity\", 1.0))\n\n        painter.setBrush(QBrush(bg_color))\n        painter.setPen(Qt.PenStyle.NoPen)\n        border_radius = style.get(\"border-radius\", 0)\n\n        draw_icon_as_background = style.get(\"icon-as-background\", False)\n        clip_icon_to_radius = style.get(\"clip-icon-to-radius\", False)\n\n        if draw_icon_as_background:\n            # draw the icon clipped by the same rounded rect\n            painter.save()\n            if clip_icon_to_radius:\n                clip_path = QPainterPath()\n                clip_path.addRoundedRect(rect, border_radius, border_radius)\n                painter.setClipPath(clip_path)\n\n            mode = QtGui.QIcon.Mode.Normal\n            painter.drawRoundedRect(rect, border_radius, border_radius)\n            option.icon.paint(\n                painter,\n                rect,\n                Qt.AlignmentFlag.AlignCenter,\n                mode,\n            )\n\n            if clip_icon_to_radius:\n                painter.setClipping(False)\n\n            pen = QPen(QColor(style.get(\"border-color\")))\n            pen.setWidth(int(style.get(\"border-width\", 0)))\n            painter.setPen(pen)\n            painter.setBrush(Qt.BrushStyle.NoBrush)\n            painter.drawRoundedRect(rect, border_radius, border_radius)\n            painter.restore()\n        else:\n            painter.drawRoundedRect(rect, border_radius, border_radius)\n\n        # Draw focus outline if needed\n        if (\n            option.state &amp; QStyle.StateFlag.State_HasFocus\n            and option.state  # type: ignore\n            &amp; QStyle.StateFlag.State_KeyboardFocusChange\n        ):\n            focus_color = style[\"focus-outline-color\"]\n            pen = QPen(\n                QColor(focus_color), style.get(\"focus-outline-width\", 0)\n            )\n            painter.setPen(pen)\n            painter.setBrush(Qt.BrushStyle.NoBrush)\n            focus_rect = rect.adjusted(1, 1, -1, -1)\n            painter.drawRoundedRect(\n                focus_rect, border_radius + 1, border_radius + 1\n            )\n\n        painter.restore()\n\n    def draw_push_button_label(\n        self,\n        option: QStyleOption,\n        painter: QPainter,\n        widget: QWidget | None,\n    ) -&gt; None:\n        \"\"\"Draw the button text and icon.\"\"\"\n        if not isinstance(option, QStyleOptionButton) or widget is None:\n            return\n\n        style, wstate = self.get_button_style(widget, option.state)  # type: ignore\n        variant = self.get_button_variant(widget)\n\n        # Set up text color\n        text_color = self.model.get_widget_color(\n            \"color\",\n            style,\n            widget,\n            widget.palette().color(QPalette.ColorRole.ButtonText),\n        )\n        if not (option.state &amp; QStyle.StateFlag.State_Enabled):  # type: ignore\n            # Apply some opacity to disabled text\n            text_color.setAlpha(int(255 * 0.5))\n\n        painter.save()\n        painter.setPen(text_color)\n\n        # Set up font\n        painter.setFont(widget.font())\n\n        # Get content rectangle\n        content_rect = self.style_inst.subElementRect(\n            QStyle.SubElement.SE_PushButtonContents, option, widget\n        )\n\n        # Optional per-widget alignment override (None \u2192 default centered\n        # layout)\n        label_alignment = getattr(widget, \"_label_alignment\", None)\n\n        if not option.icon:  # type: ignore\n            # Text only\n            if option.text and not style.get(\"ignore-text\", False):  # type: ignore\n                _text_align = (\n                    (label_alignment &amp; Qt.AlignmentFlag.AlignHorizontal_Mask)\n                    | Qt.AlignmentFlag.AlignVCenter\n                    if label_alignment is not None\n                    else Qt.AlignmentFlag.AlignCenter\n                )\n                painter.drawText(\n                    content_rect,\n                    _text_align,\n                    option.text,  # type: ignore\n                )\n            painter.restore()\n            return\n\n        # Draw icon if present\n        if option.text and not style.get(\"ignore-text\", False):  # type: ignore\n            icon_size = option.iconSize  # type: ignore\n            icon_w = icon_size.width()\n            icon_h = icon_size.height()\n            _gap = 4\n\n            # Draw icon with text color inheritance\n            mode = QtGui.QIcon.Mode.Normal\n            if not (\n                option.state &amp; QStyle.StateFlag.State_Enabled  # type: ignore\n            ):\n                mode = QtGui.QIcon.Mode.Disabled\n            elif option.state &amp; QStyle.StateFlag.State_Sunken:  # type: ignore\n                mode = QtGui.QIcon.Mode.Active\n\n            if label_alignment is not None:\n                # Group layout: icon + text move together as a unit\n                h_align = (\n                    label_alignment &amp; Qt.AlignmentFlag.AlignHorizontal_Mask\n                )\n                text_w = painter.fontMetrics().horizontalAdvance(\n                    option.text  # type: ignore\n                )\n                group_w = icon_w + _gap + text_w\n                if h_align == Qt.AlignmentFlag.AlignLeft:\n                    group_x = content_rect.left()\n                elif h_align == Qt.AlignmentFlag.AlignRight:\n                    group_x = content_rect.right() - group_w\n                else:\n                    group_x = (\n                        content_rect.left()\n                        + (content_rect.width() - group_w) // 2\n                    )\n                icon_rect = QRect(\n                    group_x,\n                    content_rect.center().y() - icon_h // 2,\n                    icon_w,\n                    icon_h,\n                )\n                text_rect = QRect(\n                    icon_rect.right() + _gap,\n                    content_rect.top(),\n                    text_w,\n                    content_rect.height(),\n                )\n                option.icon.paint(  # type: ignore\n                    painter,\n                    icon_rect,\n                    Qt.AlignmentFlag.AlignCenter,\n                    mode,\n                )\n                painter.drawText(\n                    text_rect,\n                    Qt.AlignmentFlag.AlignLeft\n                    | Qt.AlignmentFlag.AlignVCenter,\n                    option.text,  # type: ignore\n                )\n            else:\n                # Icon + text: place icon on the left (default centered)\n                icon_rect = QRect(content_rect)\n                icon_rect.setSize(icon_size)\n                icon_rect.moveCenter(\n                    QtCore.QPoint(\n                        content_rect.left() + style[\"icon-padding\"][0],\n                        content_rect.center().y(),\n                    )\n                )\n                option.icon.paint(  # type: ignore\n                    painter,\n                    icon_rect,\n                    Qt.AlignmentFlag.AlignCenter,\n                    mode,\n                )\n                # Adjust text rectangle\n                text_rect = QRect(content_rect)\n                text_rect.setLeft(icon_rect.right() + _gap)\n                # Draw text\n                painter.drawText(\n                    text_rect,\n                    Qt.AlignmentFlag.AlignLeft\n                    | Qt.AlignmentFlag.AlignVCenter,\n                    option.text,  # type: ignore\n                )\n        elif variant not in (\"thumbnail\", \"entity-card\"):\n            # Icon only\n            mode = QtGui.QIcon.Mode.Normal\n            if not (\n                option.state &amp; QStyle.StateFlag.State_Enabled  # type: ignore\n            ):\n                mode = QtGui.QIcon.Mode.Disabled\n            elif option.state &amp; QStyle.StateFlag.State_Sunken:  # type: ignore\n                mode = QtGui.QIcon.Mode.Active\n\n            checkable = widget.isCheckable() if widget else False\n\n            icon_state = (\n                (\n                    QtGui.QIcon.State.On\n                    if wstate == \"hover\"\n                    else QtGui.QIcon.State.Off\n                )\n                if not checkable\n                else (\n                    QtGui.QIcon.State.On\n                    if option.state &amp; QStyle.StateFlag.State_On\n                    else QtGui.QIcon.State.Off\n                )\n            )\n\n            _icon_align = (\n                (label_alignment &amp; Qt.AlignmentFlag.AlignHorizontal_Mask)\n                | Qt.AlignmentFlag.AlignVCenter\n                if label_alignment is not None\n                else Qt.AlignmentFlag.AlignCenter\n            )\n            option.icon.paint(  # type: ignore\n                painter,\n                content_rect,\n                _icon_align,\n                mode,\n                icon_state,\n            )\n\n        painter.restore()\n\n    def calculate_push_button_size(\n        self,\n        contents_type: QStyle.ContentsType,\n        option: QStyleOption | None,\n        contents_size: QtCore.QSize,\n        widget: QWidget | None,\n    ) -&gt; QtCore.QSize:\n        \"\"\"Calculate minimum size for push buttons with text, icons,\n        and proper padding.\"\"\"\n\n        if not isinstance(option, QStyleOptionButton):\n            # Fallback to parent if we don't have proper option data\n            if option is not None:\n                return self._super.sizeFromContents(\n                    contents_type,\n                    option,\n                    contents_size,\n                    widget,\n                )\n            else:\n                # Return reasonable default for button if no option\n                return QtCore.QSize(100, 30)\n\n        # Set up font for text measurement\n        style, _ = self.get_button_style(widget, option.state)  # type: ignore\n        font = widget.font() if widget else style_font(style, widget)\n\n        # Create font metrics for accurate text measurement\n        font_metrics = QFontMetrics(font)\n\n        # Determine if button has icon\n        has_icon = (\n            self.get_button_has_icon(widget)\n            if widget\n            else not option.icon.isNull()  # type: ignore\n        )\n        has_icon = not option.icon.isNull()\n\n        # Determine appropriate padding\n        if has_icon and not option.text:  # type: ignore\n            # Icon-only button\n            padding = style[\"icon-padding\"]\n        else:\n            # Text button or icon+text button\n            padding = style[\"text-padding\"]\n\n        # Calculate text dimensions\n        text_width = 0\n        text_height = 0\n        if option.text and not style.get(\"ignore-text\", False):  # type: ignore\n            text_rect = font_metrics.boundingRect(option.text)  # type: ignore\n            text_width = text_rect.width()\n            text_height = text_rect.height()\n\n        # Calculate icon dimensions\n        icon_width = 0\n        icon_height = 0\n        if has_icon:\n            icon_size = option.iconSize  # type: ignore\n            icon_width = icon_size.width()\n            icon_height = icon_size.height()\n\n        # Calculate content dimensions\n        content_width = 0\n        content_height = 0\n\n        if has_icon and option.text:  # type: ignore\n            # Icon + text: icon on left, 4px spacing, then text\n            content_width = icon_width + 4 + text_width\n            content_height = max(icon_height, text_height)\n        elif has_icon:\n            # Icon only\n            content_width = icon_width\n            content_height = icon_height\n        elif option.text:  # type: ignore\n            # Text only\n            content_width = text_width\n            content_height = text_height\n\n        # Add padding (vertical, horizontal)\n        total_width = content_width + (\n            2 * padding[1]\n        )  # horizontal padding on both sides\n        total_height = content_height + (\n            2 * padding[0]\n        )  # vertical padding on top and bottom\n\n        # Ensure minimum button size (reasonable minimums)\n        min_width = 16\n        min_height = 16\n\n        total_width = max(total_width, min_width)\n        total_height = max(total_height, min_height)\n\n        return QtCore.QSize(total_width, total_height)\n\n    def sub_element_rect(\n        self,\n        element: QStyle.SubElement,\n        option: QStyleOption,\n        widget: QWidget,\n    ):\n        if element == QStyle.SubElement.SE_PushButtonContents:\n            style = self.model.get_style(\n                \"QPushButton\", self.get_button_variant(widget)\n            )\n            style.set_context(widget)\n            if option.icon:\n                padding = (\n                    style[\"icon-padding\"]\n                    if not widget.text()  # type: ignore\n                    else style[\"text-padding\"]\n                )\n            else:\n                padding = style[\"text-padding\"]\n\n            return option.rect.adjusted(  # type: ignore\n                padding[1], padding[0], -padding[1], -padding[0]\n            )\n\n        elif element == QStyle.SubElement.SE_PushButtonFocusRect:\n            return option.rect.adjusted(-2, -2, 2, 2)  # type: ignore\n\n        raise ValueError(f\"Nothing returned ! -&gt; {element}\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/index.html#client.ayon_core.ui.drawers.ButtonDrawer.calculate_push_button_size","title":"<code>calculate_push_button_size(contents_type, option, contents_size, widget)</code>","text":"<p>Calculate minimum size for push buttons with text, icons, and proper padding.</p> Source code in <code>client/ayon_core/ui/drawers/button.py</code> <pre><code>def calculate_push_button_size(\n    self,\n    contents_type: QStyle.ContentsType,\n    option: QStyleOption | None,\n    contents_size: QtCore.QSize,\n    widget: QWidget | None,\n) -&gt; QtCore.QSize:\n    \"\"\"Calculate minimum size for push buttons with text, icons,\n    and proper padding.\"\"\"\n\n    if not isinstance(option, QStyleOptionButton):\n        # Fallback to parent if we don't have proper option data\n        if option is not None:\n            return self._super.sizeFromContents(\n                contents_type,\n                option,\n                contents_size,\n                widget,\n            )\n        else:\n            # Return reasonable default for button if no option\n            return QtCore.QSize(100, 30)\n\n    # Set up font for text measurement\n    style, _ = self.get_button_style(widget, option.state)  # type: ignore\n    font = widget.font() if widget else style_font(style, widget)\n\n    # Create font metrics for accurate text measurement\n    font_metrics = QFontMetrics(font)\n\n    # Determine if button has icon\n    has_icon = (\n        self.get_button_has_icon(widget)\n        if widget\n        else not option.icon.isNull()  # type: ignore\n    )\n    has_icon = not option.icon.isNull()\n\n    # Determine appropriate padding\n    if has_icon and not option.text:  # type: ignore\n        # Icon-only button\n        padding = style[\"icon-padding\"]\n    else:\n        # Text button or icon+text button\n        padding = style[\"text-padding\"]\n\n    # Calculate text dimensions\n    text_width = 0\n    text_height = 0\n    if option.text and not style.get(\"ignore-text\", False):  # type: ignore\n        text_rect = font_metrics.boundingRect(option.text)  # type: ignore\n        text_width = text_rect.width()\n        text_height = text_rect.height()\n\n    # Calculate icon dimensions\n    icon_width = 0\n    icon_height = 0\n    if has_icon:\n        icon_size = option.iconSize  # type: ignore\n        icon_width = icon_size.width()\n        icon_height = icon_size.height()\n\n    # Calculate content dimensions\n    content_width = 0\n    content_height = 0\n\n    if has_icon and option.text:  # type: ignore\n        # Icon + text: icon on left, 4px spacing, then text\n        content_width = icon_width + 4 + text_width\n        content_height = max(icon_height, text_height)\n    elif has_icon:\n        # Icon only\n        content_width = icon_width\n        content_height = icon_height\n    elif option.text:  # type: ignore\n        # Text only\n        content_width = text_width\n        content_height = text_height\n\n    # Add padding (vertical, horizontal)\n    total_width = content_width + (\n        2 * padding[1]\n    )  # horizontal padding on both sides\n    total_height = content_height + (\n        2 * padding[0]\n    )  # vertical padding on top and bottom\n\n    # Ensure minimum button size (reasonable minimums)\n    min_width = 16\n    min_height = 16\n\n    total_width = max(total_width, min_width)\n    total_height = max(total_height, min_height)\n\n    return QtCore.QSize(total_width, total_height)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/index.html#client.ayon_core.ui.drawers.ButtonDrawer.draw_push_button_bevel","title":"<code>draw_push_button_bevel(option, painter, widget)</code>","text":"<p>Draw the button background and frame with hover detection.</p> Source code in <code>client/ayon_core/ui/drawers/button.py</code> <pre><code>def draw_push_button_bevel(\n    self,\n    option: QStyleOption,\n    painter: QPainter,\n    widget: QWidget | None,\n) -&gt; None:\n    \"\"\"Draw the button background and frame with hover detection.\"\"\"\n    if not isinstance(option, QStyleOptionButton) or widget is None:\n        return\n\n    style, _ = self.get_button_style(widget, option.state)\n    rect = option.rect\n\n    painter.save()\n    painter.setRenderHint(QPainter.RenderHint.Antialiasing)\n\n    # Draw button background with hover awareness\n    bg_color = style[\"background-color\"]\n    painter.setOpacity(style.get(\"opacity\", 1.0))\n\n    painter.setBrush(QBrush(bg_color))\n    painter.setPen(Qt.PenStyle.NoPen)\n    border_radius = style.get(\"border-radius\", 0)\n\n    draw_icon_as_background = style.get(\"icon-as-background\", False)\n    clip_icon_to_radius = style.get(\"clip-icon-to-radius\", False)\n\n    if draw_icon_as_background:\n        # draw the icon clipped by the same rounded rect\n        painter.save()\n        if clip_icon_to_radius:\n            clip_path = QPainterPath()\n            clip_path.addRoundedRect(rect, border_radius, border_radius)\n            painter.setClipPath(clip_path)\n\n        mode = QtGui.QIcon.Mode.Normal\n        painter.drawRoundedRect(rect, border_radius, border_radius)\n        option.icon.paint(\n            painter,\n            rect,\n            Qt.AlignmentFlag.AlignCenter,\n            mode,\n        )\n\n        if clip_icon_to_radius:\n            painter.setClipping(False)\n\n        pen = QPen(QColor(style.get(\"border-color\")))\n        pen.setWidth(int(style.get(\"border-width\", 0)))\n        painter.setPen(pen)\n        painter.setBrush(Qt.BrushStyle.NoBrush)\n        painter.drawRoundedRect(rect, border_radius, border_radius)\n        painter.restore()\n    else:\n        painter.drawRoundedRect(rect, border_radius, border_radius)\n\n    # Draw focus outline if needed\n    if (\n        option.state &amp; QStyle.StateFlag.State_HasFocus\n        and option.state  # type: ignore\n        &amp; QStyle.StateFlag.State_KeyboardFocusChange\n    ):\n        focus_color = style[\"focus-outline-color\"]\n        pen = QPen(\n            QColor(focus_color), style.get(\"focus-outline-width\", 0)\n        )\n        painter.setPen(pen)\n        painter.setBrush(Qt.BrushStyle.NoBrush)\n        focus_rect = rect.adjusted(1, 1, -1, -1)\n        painter.drawRoundedRect(\n            focus_rect, border_radius + 1, border_radius + 1\n        )\n\n    painter.restore()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/index.html#client.ayon_core.ui.drawers.ButtonDrawer.draw_push_button_label","title":"<code>draw_push_button_label(option, painter, widget)</code>","text":"<p>Draw the button text and icon.</p> Source code in <code>client/ayon_core/ui/drawers/button.py</code> <pre><code>def draw_push_button_label(\n    self,\n    option: QStyleOption,\n    painter: QPainter,\n    widget: QWidget | None,\n) -&gt; None:\n    \"\"\"Draw the button text and icon.\"\"\"\n    if not isinstance(option, QStyleOptionButton) or widget is None:\n        return\n\n    style, wstate = self.get_button_style(widget, option.state)  # type: ignore\n    variant = self.get_button_variant(widget)\n\n    # Set up text color\n    text_color = self.model.get_widget_color(\n        \"color\",\n        style,\n        widget,\n        widget.palette().color(QPalette.ColorRole.ButtonText),\n    )\n    if not (option.state &amp; QStyle.StateFlag.State_Enabled):  # type: ignore\n        # Apply some opacity to disabled text\n        text_color.setAlpha(int(255 * 0.5))\n\n    painter.save()\n    painter.setPen(text_color)\n\n    # Set up font\n    painter.setFont(widget.font())\n\n    # Get content rectangle\n    content_rect = self.style_inst.subElementRect(\n        QStyle.SubElement.SE_PushButtonContents, option, widget\n    )\n\n    # Optional per-widget alignment override (None \u2192 default centered\n    # layout)\n    label_alignment = getattr(widget, \"_label_alignment\", None)\n\n    if not option.icon:  # type: ignore\n        # Text only\n        if option.text and not style.get(\"ignore-text\", False):  # type: ignore\n            _text_align = (\n                (label_alignment &amp; Qt.AlignmentFlag.AlignHorizontal_Mask)\n                | Qt.AlignmentFlag.AlignVCenter\n                if label_alignment is not None\n                else Qt.AlignmentFlag.AlignCenter\n            )\n            painter.drawText(\n                content_rect,\n                _text_align,\n                option.text,  # type: ignore\n            )\n        painter.restore()\n        return\n\n    # Draw icon if present\n    if option.text and not style.get(\"ignore-text\", False):  # type: ignore\n        icon_size = option.iconSize  # type: ignore\n        icon_w = icon_size.width()\n        icon_h = icon_size.height()\n        _gap = 4\n\n        # Draw icon with text color inheritance\n        mode = QtGui.QIcon.Mode.Normal\n        if not (\n            option.state &amp; QStyle.StateFlag.State_Enabled  # type: ignore\n        ):\n            mode = QtGui.QIcon.Mode.Disabled\n        elif option.state &amp; QStyle.StateFlag.State_Sunken:  # type: ignore\n            mode = QtGui.QIcon.Mode.Active\n\n        if label_alignment is not None:\n            # Group layout: icon + text move together as a unit\n            h_align = (\n                label_alignment &amp; Qt.AlignmentFlag.AlignHorizontal_Mask\n            )\n            text_w = painter.fontMetrics().horizontalAdvance(\n                option.text  # type: ignore\n            )\n            group_w = icon_w + _gap + text_w\n            if h_align == Qt.AlignmentFlag.AlignLeft:\n                group_x = content_rect.left()\n            elif h_align == Qt.AlignmentFlag.AlignRight:\n                group_x = content_rect.right() - group_w\n            else:\n                group_x = (\n                    content_rect.left()\n                    + (content_rect.width() - group_w) // 2\n                )\n            icon_rect = QRect(\n                group_x,\n                content_rect.center().y() - icon_h // 2,\n                icon_w,\n                icon_h,\n            )\n            text_rect = QRect(\n                icon_rect.right() + _gap,\n                content_rect.top(),\n                text_w,\n                content_rect.height(),\n            )\n            option.icon.paint(  # type: ignore\n                painter,\n                icon_rect,\n                Qt.AlignmentFlag.AlignCenter,\n                mode,\n            )\n            painter.drawText(\n                text_rect,\n                Qt.AlignmentFlag.AlignLeft\n                | Qt.AlignmentFlag.AlignVCenter,\n                option.text,  # type: ignore\n            )\n        else:\n            # Icon + text: place icon on the left (default centered)\n            icon_rect = QRect(content_rect)\n            icon_rect.setSize(icon_size)\n            icon_rect.moveCenter(\n                QtCore.QPoint(\n                    content_rect.left() + style[\"icon-padding\"][0],\n                    content_rect.center().y(),\n                )\n            )\n            option.icon.paint(  # type: ignore\n                painter,\n                icon_rect,\n                Qt.AlignmentFlag.AlignCenter,\n                mode,\n            )\n            # Adjust text rectangle\n            text_rect = QRect(content_rect)\n            text_rect.setLeft(icon_rect.right() + _gap)\n            # Draw text\n            painter.drawText(\n                text_rect,\n                Qt.AlignmentFlag.AlignLeft\n                | Qt.AlignmentFlag.AlignVCenter,\n                option.text,  # type: ignore\n            )\n    elif variant not in (\"thumbnail\", \"entity-card\"):\n        # Icon only\n        mode = QtGui.QIcon.Mode.Normal\n        if not (\n            option.state &amp; QStyle.StateFlag.State_Enabled  # type: ignore\n        ):\n            mode = QtGui.QIcon.Mode.Disabled\n        elif option.state &amp; QStyle.StateFlag.State_Sunken:  # type: ignore\n            mode = QtGui.QIcon.Mode.Active\n\n        checkable = widget.isCheckable() if widget else False\n\n        icon_state = (\n            (\n                QtGui.QIcon.State.On\n                if wstate == \"hover\"\n                else QtGui.QIcon.State.Off\n            )\n            if not checkable\n            else (\n                QtGui.QIcon.State.On\n                if option.state &amp; QStyle.StateFlag.State_On\n                else QtGui.QIcon.State.Off\n            )\n        )\n\n        _icon_align = (\n            (label_alignment &amp; Qt.AlignmentFlag.AlignHorizontal_Mask)\n            | Qt.AlignmentFlag.AlignVCenter\n            if label_alignment is not None\n            else Qt.AlignmentFlag.AlignCenter\n        )\n        option.icon.paint(  # type: ignore\n            painter,\n            content_rect,\n            _icon_align,\n            mode,\n            icon_state,\n        )\n\n    painter.restore()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/index.html#client.ayon_core.ui.drawers.ButtonDrawer.get_button_has_icon","title":"<code>get_button_has_icon(widget)</code>","text":"<p>Check if button has an icon.</p> Source code in <code>client/ayon_core/ui/drawers/button.py</code> <pre><code>def get_button_has_icon(self, widget: QWidget) -&gt; bool:\n    \"\"\"Check if button has an icon.\"\"\"\n    if widget is None:\n        return False\n\n    # Method 1: Try has_icon property\n    if hasattr(widget, \"has_icon\"):\n        return widget.has_icon  # type: ignore\n\n    # Method 2: Try Qt property\n    has_icon_prop = widget.property(\"has_icon\")\n    if has_icon_prop is not None:\n        return bool(has_icon_prop)\n\n    # Method 3: Check the actual icon\n    return bool(widget.icon() and not widget.icon().isNull())  # type: ignore\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/index.html#client.ayon_core.ui.drawers.ButtonDrawer.get_button_style","title":"<code>get_button_style(widget, state)</code>","text":"<p>Get the appropriate style dictionary for the widget's variant and state.</p> Source code in <code>client/ayon_core/ui/drawers/button.py</code> <pre><code>def get_button_style(\n    self, widget: QWidget, state: QStyle.StateFlag\n) -&gt; tuple[dict, str]:\n    \"\"\"Get the appropriate style dictionary for the widget's variant and\n    state.\"\"\"\n    variant = self.get_button_variant(widget)\n\n    wstate = \"base\"\n    if not (state &amp; QStyle.StateFlag.State_Enabled):\n        wstate = \"disabled\"\n    elif state &amp; QStyle.StateFlag.State_Sunken:\n        wstate = \"pressed\"\n    elif (state &amp; QStyle.StateFlag.State_MouseOver and not (\n        state &amp; QStyle.StateFlag.State_On) or widget.underMouse()\n    ):\n        wstate = \"hover\"\n    elif state &amp; QStyle.StateFlag.State_On:\n        wstate = \"checked\"\n\n    style = self.model.get_style(\"QPushButton\", variant, wstate)\n    style.set_context(widget)\n\n    return style, wstate\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/index.html#client.ayon_core.ui.drawers.ButtonDrawer.get_button_variant","title":"<code>get_button_variant(widget)</code>","text":"<p>Extract button variant from widget properties.</p> Source code in <code>client/ayon_core/ui/drawers/button.py</code> <pre><code>def get_button_variant(self, widget: QWidget) -&gt; str:\n    \"\"\"Extract button variant from widget properties.\"\"\"\n    if widget is None:\n        return \"surface\"\n    return getattr(widget, \"_variant_str\", \"surface\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/index.html#client.ayon_core.ui.drawers.CheckboxDrawer","title":"<code>CheckboxDrawer</code>","text":"Source code in <code>client/ayon_core/ui/drawers/checkbox.py</code> <pre><code>class CheckboxDrawer:\n    def __init__(self, style_inst: AYONStyle) -&gt; None:\n        self.style_inst = style_inst\n        self.model = style_inst.model\n\n    @property\n    def _super(self):\n        \"\"\"Return proxy for calling QCommonStyle methods on style_inst.\"\"\"\n        from ..style import AYONStyle as _AYONStyle\n\n        return super(_AYONStyle, self.style_inst)\n\n    @property\n    def base_class(self):\n        return {\"QCheckBox\": QCheckBox}\n\n    def register_drawers(self):\n        return {\n            enum_to_str(\n                QStyle.ControlElement,\n                QStyle.ControlElement.CE_CheckBox,\n                \"QCheckBox\",\n            ): self.draw_indicator,\n            enum_to_str(\n                QStyle.PrimitiveElement,\n                QStyle.PrimitiveElement.PE_IndicatorCheckBox,\n                \"QCheckBox\",\n            ): self.draw_toggle,\n            enum_to_str(\n                QStyle.PrimitiveElement,\n                QStyle.PrimitiveElement.PE_FrameFocusRect,\n                \"QCheckBox\",\n            ): do_nothing,\n        }\n\n    def register_metrics(self):\n        return {\n            enum_to_str(\n                QStyle.PixelMetric,\n                QStyle.PixelMetric.PM_IndicatorWidth,\n                \"QCheckBox\",\n            ): self.get_metric,\n            enum_to_str(\n                QStyle.PixelMetric,\n                QStyle.PixelMetric.PM_IndicatorHeight,\n                \"QCheckBox\",\n            ): self.get_metric,\n            enum_to_str(\n                QStyle.PixelMetric,\n                QStyle.PixelMetric.PM_CheckBoxLabelSpacing,\n                \"QCheckBox\",\n            ): self.get_metric,\n        }\n\n    def get_metric(\n        self,\n        metric: QStyle.PixelMetric,\n        opt: QStyleOption | None = None,\n        widget: QWidget | None = None,\n    ):\n        variant = getattr(widget, \"_variant_str\", \"default\")\n        style = self.model.get_style(\n            \"QCheckBox\",\n            variant=variant,\n        )\n        style.set_context(widget)\n        metrics_h = widget.fontMetrics().height() if widget else 18\n        metrics_w = metrics_h * 2 if widget else 32\n\n        if metric == QStyle.PixelMetric.PM_IndicatorWidth:\n            # if indicator-width == 0, use 2x the font height.\n            return style.get(\"indicator-width\", metrics_w) or metrics_w\n        elif metric == QStyle.PixelMetric.PM_IndicatorHeight:\n            # if indicator-height == 0, use the font height.\n            return style.get(\"indicator-height\", metrics_h) or metrics_h\n        elif metric == QStyle.PixelMetric.PM_CheckBoxLabelSpacing:\n            return style.get(\"checkbox-label-spacing\", 8)\n        return 0\n\n    def draw_indicator(\n        self,\n        option: QStyleOption,\n        painter: QPainter,\n        widget: QWidget | None,\n    ):\n        variant = getattr(widget, \"_variant_str\", \"default\")\n        state = (\n            \"checked\" if option.state &amp; QStyle.StateFlag.State_On else \"base\"\n        )\n        style = self.model.get_style(\n            \"QCheckBox\",\n            variant=variant,\n            state=state,\n        )\n        style.set_context(widget)\n\n        if style.get(\"background-color\"):\n            painter.save()\n            painter.setBrush(QColor(style[\"background-color\"]))\n            painter.setPen(Qt.PenStyle.NoPen)\n            radius = style.get(\"border-radius\", 0)\n            painter.drawRoundedRect(option.rect, radius, radius)\n            painter.restore()\n\n        if style.get(\"indicator-position\", \"left\") == \"right\":\n            # Manually draw a centred [label  toggle] group so that padding\n            # is equal on both sides, instead of relying on Qt's layout.\n            s = self.style_inst\n            ind_w = s.pixelMetric(\n                QStyle.PixelMetric.PM_IndicatorWidth, option, widget\n            )\n            ind_h = s.pixelMetric(\n                QStyle.PixelMetric.PM_IndicatorHeight, option, widget\n            )\n            spacing = s.pixelMetric(\n                QStyle.PixelMetric.PM_CheckBoxLabelSpacing, option, widget\n            )\n\n            text = getattr(option, \"text\", \"\")\n            fm = option.fontMetrics\n            text_w = fm.horizontalAdvance(text) if text else 0\n            text_h = fm.height()\n\n            total_w = text_w + (spacing + ind_w if text_w else ind_w)\n\n            rect = option.rect\n            cx = rect.center().x()\n            cy = rect.center().y()\n            x = cx - total_w // 2\n\n            painter.save()\n            if text:\n                painter.setPen(\n                    QColor(style[\"color\"])\n                    if style.get(\"color\")\n                    else option.palette.color(QPalette.ColorRole.WindowText)\n                )\n                text_rect = QRect(x, cy - text_h // 2, text_w, text_h)\n                painter.drawText(\n                    text_rect,\n                    Qt.AlignmentFlag.AlignVCenter | Qt.AlignmentFlag.AlignLeft,\n                    text,\n                )\n\n            toggle_opt = QStyleOption(option)\n            toggle_opt.rect = QRect(\n                x + text_w + (spacing if text_w else 0),\n                cy - ind_h // 2,\n                ind_w,\n                ind_h,\n            )\n            self.draw_toggle(toggle_opt, painter, widget)\n            painter.restore()\n            return\n\n        if style.get(\"color\"):\n            option.palette.setColor(\n                QPalette.ColorRole.WindowText, QColor(style[\"color\"])\n            )\n\n        self._super.drawControl(\n            QStyle.ControlElement.CE_CheckBox, option, painter, widget\n        )\n\n    def draw_toggle(\n        self,\n        option: QStyleOption,\n        painter: QPainter,\n        w: QWidget | None = None,\n    ):\n        painter.save()\n        painter.setRenderHint(QPainter.RenderHint.Antialiasing, True)\n\n        # get style data\n        checked = bool(option.state &amp; QStyle.StateFlag.State_On)\n        variant = getattr(w, \"_variant_str\", \"default\")\n        style = self.model.get_style(\n            \"QCheckBox\",\n            variant=variant,\n            state=\"checked\" if checked else \"base\",\n        )\n        style.set_context(w)\n\n        # draw toggle background\n        painter.setBrush(QColor(style[\"indicator-background-color\"]))\n        if style.get(\"indicator-border-width\", 0):\n            pen = QPen(QColor(style[\"indicator-border-color\"]))\n            pen.setWidth(style.get(\"indicator-border-width\", 0))\n            painter.setPen(pen)\n        else:\n            painter.setPen(Qt.PenStyle.NoPen)\n        frame_rect: QRectF = QRectF(option.rect).adjusted(1, 0, -1, 0)\n        radius = frame_rect.height() / 2.0\n        painter.drawRoundedRect(frame_rect, radius, radius)\n\n        # draw toggle knob\n        painter.setBrush(QColor(style[\"indicator-color\"]))\n        offset = frame_rect.height() * 0.125\n        state_rect: QRectF = frame_rect.adjusted(\n            offset, offset, -offset, -offset\n        )\n        state_rect.setWidth(state_rect.height())\n        if checked:\n            state_rect.moveRight(frame_rect.right() - offset)\n        painter.drawEllipse(state_rect)\n\n        painter.restore()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/index.html#client.ayon_core.ui.drawers.ComboBoxDrawer","title":"<code>ComboBoxDrawer</code>","text":"Source code in <code>client/ayon_core/ui/drawers/combobox.py</code> <pre><code>class ComboBoxDrawer:\n    def __init__(self, style_inst: AYONStyle) -&gt; None:\n        self.style_inst = style_inst\n        self.model = style_inst.model\n\n    @property\n    def _super(self):\n        \"\"\"Return proxy for calling QCommonStyle methods on style_inst.\"\"\"\n        from ..style import AYONStyle as _AYONStyle\n\n        return super(_AYONStyle, self.style_inst)\n\n    @property\n    def base_class(self):\n        return {\"QComboBox\": QComboBox}\n\n    def register_drawers(self):\n        return {\n            enum_to_str(\n                QStyle.ControlElement,\n                QStyle.ControlElement.CE_ComboBoxLabel,\n                \"QComboBox\",\n            ): self.draw_label,\n            enum_to_str(\n                QStyle.ComplexControl,\n                QStyle.ComplexControl.CC_ComboBox,\n                \"QComboBox\",\n            ): self.draw_box,\n            enum_to_str(\n                QStyle.PrimitiveElement,\n                QStyle.PrimitiveElement.PE_PanelItemViewItem,\n                \"QFrame\",\n            ): self.draw_panel_item_view_item,\n            enum_to_str(\n                QStyle.PrimitiveElement,\n                QStyle.PrimitiveElement.PE_FrameFocusRect,\n                \"QFrame\",\n            ): do_nothing,\n        }\n\n    def register_sizers(self):\n        return {\n            enum_to_str(\n                QStyle.ContentsType,\n                QStyle.ContentsType.CT_ComboBox,\n                \"QComboBox\",\n            ): self.combobox_size,\n        }\n\n    def get_fg_bg_colors(\n        self,\n        opt: QtWidgets.QStyleOptionComplex,\n        w: QComboBox,\n    ) -&gt; tuple[QColor, QColor]:\n        bg_color = opt.palette.color(\n            QPalette.ColorGroup.Active, QPalette.ColorRole.Base\n        )\n        fg_color = opt.palette.color(\n            QPalette.ColorGroup.Active, QPalette.ColorRole.ButtonText\n        )\n\n        inverted = getattr(w, \"_inverted\", False)\n        current_index = w.currentIndex()\n        if current_index &gt;= 0:\n            item_color = w.itemData(\n                current_index, QtCore.Qt.ItemDataRole.ForegroundRole\n            )\n            if item_color is not None:\n                item_color = item_color.color()\n                fg_color = bg_color if inverted else item_color\n                bg_color = item_color if inverted else bg_color\n\n        return fg_color, bg_color\n\n    def draw_box(\n        self,\n        opt: QtWidgets.QStyleOptionComplex,\n        p: QPainter,\n        w: QComboBox | None = None,\n    ):\n        if not isinstance(w, QComboBox):\n            return\n\n        _style = self.model.get_style(\n            \"QComboBox\", variant=getattr(w, \"_variant_str\", None)\n        )\n        _style.set_context(w)\n        style_bg_color = _style.get(\"background-color\", None)\n        opt.palette.setBrush(\n            QPalette.ColorRole.Base,\n            QColor(style_bg_color)\n            if style_bg_color\n            else self.model.base_palette.base(),\n        )\n        _radius = _style.get(\"border-radius\", 0)\n\n        if not w.isEditable():\n            fg_color, bg_color = self.get_fg_bg_colors(opt, w)\n\n            # Paint background with status color\n            rect = opt.rect\n            p.save()\n            p.setBrush(QBrush(bg_color))\n            p.setPen(QtCore.Qt.PenStyle.NoPen)\n            p.drawRoundedRect(rect, _radius, _radius)\n            p.restore()\n\n            # Draw expand_more arrow if show_chevron is True\n            show_chevron = getattr(w, \"show_chevron\", True)\n            if show_chevron:\n                arrow_rect = self._super.subControlRect(\n                    QStyle.ComplexControl.CC_ComboBox,\n                    opt,\n                    QStyle.SubControl.SC_ComboBoxArrow,\n                    w,\n                )\n                arrow_icon = get_icon(\"expand_more\", fg_color)\n                if arrow_icon and not arrow_rect.isEmpty():\n                    arrow_size = min(arrow_rect.width(), arrow_rect.height())\n                    pixmap = arrow_icon.pixmap(arrow_size, arrow_size)\n                    px = (\n                        arrow_rect.x() + (arrow_rect.width() - arrow_size) // 2\n                    )\n                    py = (\n                        arrow_rect.y()\n                        + (arrow_rect.height() - arrow_size) // 2\n                    )\n                    popup_open = bool(opt.state &amp; QStyle.StateFlag.State_On)\n                    if popup_open:\n                        cx = px + arrow_size / 2\n                        cy = py + arrow_size / 2\n                        p.save()\n                        p.translate(cx, cy)\n                        p.rotate(180)\n                        p.translate(-cx, -cy)\n                        p.drawPixmap(px, py, pixmap)\n                        p.restore()\n                    else:\n                        p.drawPixmap(px, py, pixmap)\n\n            # set pen for text drawing\n            p.setPen(fg_color)\n        else:\n            # editable combobox - IMPLEMENT ME\n            self._super.drawComplexControl(\n                QStyle.ComplexControl.CC_ComboBox, opt, p, w\n            )\n\n    def draw_label(\n        self,\n        opt: QStyleOptionComboBox,\n        p: QPainter,\n        w: QWidget,\n    ):\n        if not isinstance(w, QComboBox):\n            return\n\n        _style = self.model.get_style(\n            \"QComboBox\", variant=getattr(w, \"_variant_str\", None)\n        )\n        _style.set_context(w)\n        icon_padding = _style.get(\"icon-padding\", [4, 4])\n        text_padding = _style.get(\"text-padding\", [1, 1])\n\n        fg_color, bg_color = self.get_fg_bg_colors(opt, w)\n\n        base_cls = self._super\n        edit_rect = base_cls.subControlRect(\n            QStyle.ComplexControl.CC_ComboBox,\n            opt,\n            QStyle.SubControl.SC_ComboBoxEditField,\n            w,\n        )\n        p.save()\n        p.setClipRect(edit_rect)\n        if opt.currentIcon:\n            mode = (\n                QIcon.Mode.Normal\n                if opt.state &amp; QStyle.StateFlag.State_Enabled\n                else QIcon.Mode.Disabled\n            )\n            pixmap = opt.currentIcon.pixmap(opt.iconSize, mode)\n            icon_rect = QRect(edit_rect)\n            icon_rect.setWidth(opt.iconSize.width() + icon_padding[0])\n            icon_rect.setHeight(opt.iconSize.height() + icon_padding[1])\n            icon_rect = QStyle.alignedRect(\n                opt.direction,\n                Qt.AlignmentFlag.AlignLeft | Qt.AlignmentFlag.AlignVCenter,\n                icon_rect.size(),\n                edit_rect,\n            )\n            if opt.editable:\n                p.fillRect(\n                    icon_rect, opt.palette.brush(QPalette.ColorRole.Base)\n                )\n            base_cls.drawItemPixmap(\n                p, icon_rect, Qt.AlignmentFlag.AlignCenter, pixmap\n            )\n            if opt.direction == Qt.LayoutDirection.RightToLeft:\n                edit_rect.translate(-icon_padding[0] - opt.iconSize.width(), 0)\n            else:\n                edit_rect.translate(opt.iconSize.width() + icon_padding[0], 0)\n\n        if opt.currentText and not opt.editable:\n            base_cls.drawItemText(\n                p,\n                edit_rect.adjusted(\n                    text_padding[0],\n                    -text_padding[1],\n                    -text_padding[0],\n                    text_padding[1],\n                ),\n                QStyle.visualAlignment(\n                    opt.direction,\n                    Qt.AlignmentFlag.AlignLeft | Qt.AlignmentFlag.AlignVCenter,\n                ),\n                opt.palette,\n                bool(opt.state &amp; QStyle.StateFlag.State_Enabled),\n                opt.currentText,\n            )\n\n        p.restore()\n\n    def draw_panel_item_view_item(\n        self, option: QStyleOption, painter: QPainter, w: QWidget\n    ):\n        cb = w.model().parent()\n        if cb and getattr(cb, \"_inverted\", False):\n            idx = option.index\n            if idx:\n                fgc = (\n                    w.model().data(idx, Qt.ItemDataRole.ForegroundRole).color()\n                )\n                option.backgroundBrush.setColor(fgc)\n        else:\n            stl = self.model.get_style(\"QComboBox\")\n            stl.set_context(w)\n            option.backgroundBrush.setColor(\n                QColor(stl[\"menu-background-color\"])\n            )\n        self._super.drawPrimitive(  # type: ignore\n            QStyle.PrimitiveElement.PE_PanelItemViewItem, option, painter, w\n        )\n\n    def combobox_size(\n        self,\n        contents_type: QStyle.ContentsType,\n        option: QStyleOption | None,\n        contents_size: QtCore.QSize,\n        widget: QWidget | None,\n    ) -&gt; QtCore.QSize:\n        from qtpy.QtCore import QSize\n\n        if not option or not isinstance(option, QStyleOptionComboBox):\n            return QSize()\n\n        style = self.model.get_style(\"QComboBox\")\n        style.set_context(widget)\n\n        text_width = cb_height = 0\n        if isinstance(widget, QComboBox):\n            for i in range(widget.count()):\n                t_rect = option.fontMetrics.boundingRect(\n                    widget.itemData(i, Qt.ItemDataRole.DisplayRole)\n                )\n                text_width = max(text_width, t_rect.width())\n                cb_height = max(cb_height, t_rect.height())\n\n        text_width += style[\"text-padding\"][0] * 2\n        cb_height += style[\"text-padding\"][1] * 2\n\n        icon_width = 0\n        if option.currentIcon:\n            icon_size = getattr(widget, \"_icon_size\", 0)\n            if icon_size == 0:\n                all_sizes = option.currentIcon.availableSizes()\n                icon_size = max(all_sizes[0].width(), all_sizes[0].height())\n            icon_width = icon_size + style[\"icon-padding\"][0] * 2\n            icon_height = icon_size + style[\"icon-padding\"][1] * 2\n            cb_height = max(cb_height, icon_height)\n            if text_width:\n                icon_width += style[\"text-padding\"][0]\n\n        final_size = QSize(\n            text_width + icon_width,\n            min(getattr(widget, \"_height\", cb_height), cb_height),\n        )\n        return final_size\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/index.html#client.ayon_core.ui.drawers.ItemViewItemDrawer","title":"<code>ItemViewItemDrawer</code>","text":"<p>Drawer for item view items using QStyledItemDelegate.</p> Source code in <code>client/ayon_core/ui/drawers/item_view.py</code> <pre><code>class ItemViewItemDrawer:\n    \"\"\"Drawer for item view items using QStyledItemDelegate.\"\"\"\n\n    def __init__(self, style_inst: AYONStyle) -&gt; None:\n        self.style_inst = style_inst\n        self.model = style_inst.model\n\n    @property\n    def base_class(self):\n        return {\"QStyledItemDelegate\": QStyledItemDelegate}\n\n    def register_drawers(self):\n        return {\n            enum_to_str(\n                QStyle.ControlElement,\n                QStyle.ControlElement.CE_ItemViewItem,\n                \"QStyledItemDelegate\",\n            ): self.draw_item_view_item,\n        }\n\n    def get_item_view_variant(self, widget: QWidget | None) -&gt; str:\n        \"\"\"Extract item view variant from widget properties.\"\"\"\n        if widget is None:\n            return \"default\"\n        if hasattr(widget, \"itemDelegate\"):\n            delegate = widget.itemDelegate()\n            if hasattr(delegate, \"_variant_str\"):\n                return delegate._variant_str\n        return \"default\"\n\n    def get_item_view_style(\n        self,\n        widget: QWidget | None,\n        option: QStyleOptionViewItem,\n    ) -&gt; tuple[dict, str]:\n        \"\"\"Get the appropriate style dictionary for the widget's variant\n        and state.\n\n        Args:\n            widget: The parent widget containing the item view.\n            option: The style option containing state flags.\n\n        Returns:\n            A tuple of (style dictionary, state string).\n        \"\"\"\n        variant = self.get_item_view_variant(widget)\n\n        wstate = \"base\"\n        is_checked = option.checkState == Qt.CheckState.Checked\n        is_hovered = bool(option.state &amp; QStyle.StateFlag.State_MouseOver)\n\n        if is_checked:\n            wstate = \"checked\"\n        elif is_hovered:\n            wstate = \"hover\"\n\n        style = self.model.get_style(\"QStyledItemDelegate\", variant, wstate)\n        style.set_context(widget)\n\n        return style, wstate\n\n    def draw_item_view_item(\n        self,\n        option: QStyleOption,\n        painter: QPainter,\n        widget: QWidget | None,\n    ) -&gt; None:\n        \"\"\"Paint a filter item with checkbox indicator.\n\n        Hover and checked states are handled independently:\n        - Background color comes from hover state when hovered\n        - Checkbox background and text color come from checked state\n        \"\"\"\n        painter.save()\n        painter.setRenderHint(QPainter.RenderHint.Antialiasing)\n\n        # For QStyleOptionViewItem, we need to check different properties\n        if not isinstance(option, QStyleOptionViewItem):\n            painter.restore()\n            return\n\n        # Determine hover and checked states independently\n        is_checked = option.checkState == Qt.CheckState.Checked\n        is_hovered = bool(option.state &amp; QStyle.StateFlag.State_MouseOver)\n        text = option.text\n\n        # Get variant for style lookups\n        variant = self.get_item_view_variant(widget)\n\n        # Get all necessary styles in a single call\n        styles = self.model.get_styles(\n            \"QStyledItemDelegate\", variant, [\"base\", \"hover\", \"checked\"]\n        )\n        base_style = styles[\"base\"]\n        hover_style = styles[\"hover\"]\n        checked_style = styles[\"checked\"]\n\n        # Constants from base style data\n        checkbox_size = base_style.get(\"checkbox-size\", 16)\n        checkbox_margin = base_style.get(\"checkbox-margin\", 8)\n        text_padding = base_style.get(\"text-padding\", 12)\n        border_radius = base_style.get(\"border-radius\", 2)\n\n        # Background: use hover style if hovered, regardless of checked state\n        if is_hovered:\n            bg_color = QColor(\n                hover_style.get(\n                    \"background-color\",\n                    base_style.get(\"background-color\", \"transparent\"),\n                )\n            )\n        else:\n            bg_color = QColor(\n                base_style.get(\"background-color\", \"transparent\")\n            )\n\n        # Text color: use checked style if checked, else base\n        if is_checked:\n            text_color = QColor(\n                checked_style.get(\"color\", base_style.get(\"color\", \"#8b9198\"))\n            )\n        else:\n            text_color = QColor(base_style.get(\"color\", \"#8b9198\"))\n\n        # Checkbox background: use checked style if checked, else base\n        if is_checked:\n            checkbox_bg_color = QColor(\n                checked_style.get(\n                    \"checkbox-background-color\",\n                    base_style.get(\"checkbox-background-color\", \"#424a57\"),\n                )\n            )\n        else:\n            checkbox_bg_color = QColor(\n                base_style.get(\"checkbox-background-color\", \"#424a57\")\n            )\n\n        # Draw background if hovered\n        if is_hovered:\n            painter.setBrush(QBrush(bg_color))\n            painter.setPen(Qt.PenStyle.NoPen)\n            painter.drawRect(option.rect)\n\n        # Calculate checkbox rect - positioned on right side\n        cb_rect = QRect(\n            option.rect.right() - checkbox_size - checkbox_margin,\n            option.rect.center().y() - checkbox_size // 2,\n            checkbox_size,\n            checkbox_size,\n        )\n\n        # Draw checkbox background\n        painter.setBrush(QBrush(checkbox_bg_color))\n        painter.setPen(Qt.PenStyle.NoPen)\n        painter.drawRoundedRect(cb_rect, border_radius, border_radius)\n\n        # Draw X mark if checked\n        if is_checked:\n            icon = get_icon(\"close\", color=\"#000000\")\n            icon_rect = cb_rect.adjusted(2, 2, -2, -2)\n            icon.paint(painter, icon_rect)\n\n        # Draw text\n        painter.setPen(QPen(text_color))\n        text_rect = option.rect.adjusted(\n            text_padding,\n            0,\n            -(checkbox_size + checkbox_margin * 2),\n            0,\n        )\n        painter.drawText(\n            text_rect,\n            Qt.AlignmentFlag.AlignVCenter | Qt.AlignmentFlag.AlignLeft,\n            text,\n        )\n\n        painter.restore()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/index.html#client.ayon_core.ui.drawers.ItemViewItemDrawer.draw_item_view_item","title":"<code>draw_item_view_item(option, painter, widget)</code>","text":"<p>Paint a filter item with checkbox indicator.</p> <p>Hover and checked states are handled independently: - Background color comes from hover state when hovered - Checkbox background and text color come from checked state</p> Source code in <code>client/ayon_core/ui/drawers/item_view.py</code> <pre><code>def draw_item_view_item(\n    self,\n    option: QStyleOption,\n    painter: QPainter,\n    widget: QWidget | None,\n) -&gt; None:\n    \"\"\"Paint a filter item with checkbox indicator.\n\n    Hover and checked states are handled independently:\n    - Background color comes from hover state when hovered\n    - Checkbox background and text color come from checked state\n    \"\"\"\n    painter.save()\n    painter.setRenderHint(QPainter.RenderHint.Antialiasing)\n\n    # For QStyleOptionViewItem, we need to check different properties\n    if not isinstance(option, QStyleOptionViewItem):\n        painter.restore()\n        return\n\n    # Determine hover and checked states independently\n    is_checked = option.checkState == Qt.CheckState.Checked\n    is_hovered = bool(option.state &amp; QStyle.StateFlag.State_MouseOver)\n    text = option.text\n\n    # Get variant for style lookups\n    variant = self.get_item_view_variant(widget)\n\n    # Get all necessary styles in a single call\n    styles = self.model.get_styles(\n        \"QStyledItemDelegate\", variant, [\"base\", \"hover\", \"checked\"]\n    )\n    base_style = styles[\"base\"]\n    hover_style = styles[\"hover\"]\n    checked_style = styles[\"checked\"]\n\n    # Constants from base style data\n    checkbox_size = base_style.get(\"checkbox-size\", 16)\n    checkbox_margin = base_style.get(\"checkbox-margin\", 8)\n    text_padding = base_style.get(\"text-padding\", 12)\n    border_radius = base_style.get(\"border-radius\", 2)\n\n    # Background: use hover style if hovered, regardless of checked state\n    if is_hovered:\n        bg_color = QColor(\n            hover_style.get(\n                \"background-color\",\n                base_style.get(\"background-color\", \"transparent\"),\n            )\n        )\n    else:\n        bg_color = QColor(\n            base_style.get(\"background-color\", \"transparent\")\n        )\n\n    # Text color: use checked style if checked, else base\n    if is_checked:\n        text_color = QColor(\n            checked_style.get(\"color\", base_style.get(\"color\", \"#8b9198\"))\n        )\n    else:\n        text_color = QColor(base_style.get(\"color\", \"#8b9198\"))\n\n    # Checkbox background: use checked style if checked, else base\n    if is_checked:\n        checkbox_bg_color = QColor(\n            checked_style.get(\n                \"checkbox-background-color\",\n                base_style.get(\"checkbox-background-color\", \"#424a57\"),\n            )\n        )\n    else:\n        checkbox_bg_color = QColor(\n            base_style.get(\"checkbox-background-color\", \"#424a57\")\n        )\n\n    # Draw background if hovered\n    if is_hovered:\n        painter.setBrush(QBrush(bg_color))\n        painter.setPen(Qt.PenStyle.NoPen)\n        painter.drawRect(option.rect)\n\n    # Calculate checkbox rect - positioned on right side\n    cb_rect = QRect(\n        option.rect.right() - checkbox_size - checkbox_margin,\n        option.rect.center().y() - checkbox_size // 2,\n        checkbox_size,\n        checkbox_size,\n    )\n\n    # Draw checkbox background\n    painter.setBrush(QBrush(checkbox_bg_color))\n    painter.setPen(Qt.PenStyle.NoPen)\n    painter.drawRoundedRect(cb_rect, border_radius, border_radius)\n\n    # Draw X mark if checked\n    if is_checked:\n        icon = get_icon(\"close\", color=\"#000000\")\n        icon_rect = cb_rect.adjusted(2, 2, -2, -2)\n        icon.paint(painter, icon_rect)\n\n    # Draw text\n    painter.setPen(QPen(text_color))\n    text_rect = option.rect.adjusted(\n        text_padding,\n        0,\n        -(checkbox_size + checkbox_margin * 2),\n        0,\n    )\n    painter.drawText(\n        text_rect,\n        Qt.AlignmentFlag.AlignVCenter | Qt.AlignmentFlag.AlignLeft,\n        text,\n    )\n\n    painter.restore()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/index.html#client.ayon_core.ui.drawers.ItemViewItemDrawer.get_item_view_style","title":"<code>get_item_view_style(widget, option)</code>","text":"<p>Get the appropriate style dictionary for the widget's variant and state.</p> <p>Parameters:</p> Name Type Description Default <code>widget</code> <code>QWidget | None</code> <p>The parent widget containing the item view.</p> required <code>option</code> <code>QStyleOptionViewItem</code> <p>The style option containing state flags.</p> required <p>Returns:</p> Type Description <code>tuple[dict, str]</code> <p>A tuple of (style dictionary, state string).</p> Source code in <code>client/ayon_core/ui/drawers/item_view.py</code> <pre><code>def get_item_view_style(\n    self,\n    widget: QWidget | None,\n    option: QStyleOptionViewItem,\n) -&gt; tuple[dict, str]:\n    \"\"\"Get the appropriate style dictionary for the widget's variant\n    and state.\n\n    Args:\n        widget: The parent widget containing the item view.\n        option: The style option containing state flags.\n\n    Returns:\n        A tuple of (style dictionary, state string).\n    \"\"\"\n    variant = self.get_item_view_variant(widget)\n\n    wstate = \"base\"\n    is_checked = option.checkState == Qt.CheckState.Checked\n    is_hovered = bool(option.state &amp; QStyle.StateFlag.State_MouseOver)\n\n    if is_checked:\n        wstate = \"checked\"\n    elif is_hovered:\n        wstate = \"hover\"\n\n    style = self.model.get_style(\"QStyledItemDelegate\", variant, wstate)\n    style.set_context(widget)\n\n    return style, wstate\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/index.html#client.ayon_core.ui.drawers.ItemViewItemDrawer.get_item_view_variant","title":"<code>get_item_view_variant(widget)</code>","text":"<p>Extract item view variant from widget properties.</p> Source code in <code>client/ayon_core/ui/drawers/item_view.py</code> <pre><code>def get_item_view_variant(self, widget: QWidget | None) -&gt; str:\n    \"\"\"Extract item view variant from widget properties.\"\"\"\n    if widget is None:\n        return \"default\"\n    if hasattr(widget, \"itemDelegate\"):\n        delegate = widget.itemDelegate()\n        if hasattr(delegate, \"_variant_str\"):\n            return delegate._variant_str\n    return \"default\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/index.html#client.ayon_core.ui.drawers.LineEditDrawer","title":"<code>LineEditDrawer</code>","text":"<p>AYONStyle drawer for QLineEdit.</p> <p>Registers a no-op for PE_PanelLineEdit when the widget is an AYLineEdit instance (which paints itself fully in its own paintEvent), and falls back to the base QCommonStyle implementation for all other QLineEdit widgets.</p> Source code in <code>client/ayon_core/ui/drawers/lineedit.py</code> <pre><code>class LineEditDrawer:\n    \"\"\"AYONStyle drawer for QLineEdit.\n\n    Registers a no-op for PE_PanelLineEdit when the widget is an AYLineEdit\n    instance (which paints itself fully in its own paintEvent), and falls back\n    to the base QCommonStyle implementation for all other QLineEdit widgets.\n    \"\"\"\n\n    def __init__(self, style_inst: AYONStyle) -&gt; None:\n        self.style_inst = style_inst\n\n    @property\n    def _super(self):\n        \"\"\"Return proxy for calling QCommonStyle methods on style_inst.\"\"\"\n        from ..style import AYONStyle as _AYONStyle\n\n        return super(_AYONStyle, self.style_inst)\n\n    @property\n    def base_class(self):\n        return {\"QLineEdit\": QLineEdit}\n\n    def register_drawers(self):\n        return {\n            enum_to_str(\n                QStyle.PrimitiveElement,\n                QStyle.PrimitiveElement.PE_PanelLineEdit,\n                \"QLineEdit\",\n            ): self.draw_panel,\n        }\n\n    def draw_panel(\n        self,\n        option: QStyleOption,\n        painter: QPainter,\n        widget: QWidget | None,\n    ) -&gt; None:\n        # AYLineEdit paints its own background \u2014 skip Qt's default frame.\n        if type(widget).__name__ == \"AYLineEdit\":\n            return\n        self._super.drawPrimitive(\n            QStyle.PrimitiveElement.PE_PanelLineEdit, option, painter, widget\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/index.html#client.ayon_core.ui.drawers.MenuDrawer","title":"<code>MenuDrawer</code>","text":"<p>Drawer for QMenu using native QPainter calls (no QSS).</p> <p>Paints the menu panel/border (PE_PanelMenu/PE_FrameMenu) and each item row (CE_MenuItem).</p> Source code in <code>client/ayon_core/ui/drawers/menu.py</code> <pre><code>class MenuDrawer:\n    \"\"\"Drawer for QMenu using native QPainter calls (no QSS).\n\n    Paints the menu panel/border (PE_PanelMenu/PE_FrameMenu) and each\n    item row (CE_MenuItem).\n    \"\"\"\n\n    _WIDGET_CLS = \"QMenu\"\n\n    def __init__(self, style_inst: AYONStyle) -&gt; None:\n        self.style_inst = style_inst\n        self.model = style_inst.model\n\n    @property\n    def base_class(self) -&gt; dict:\n        return {\"QMenu\": QMenu}\n\n    # ------------------------------------------------------------------\n    # Registration\n    # ------------------------------------------------------------------\n\n    def register_drawers(self) -&gt; dict:\n        return {\n            enum_to_str(\n                QStyle.PrimitiveElement,\n                QStyle.PrimitiveElement.PE_PanelMenu,\n                \"QMenu\",\n            ): self.draw_panel,\n            enum_to_str(\n                QStyle.PrimitiveElement,\n                QStyle.PrimitiveElement.PE_FrameMenu,\n                \"QMenu\",\n            ): self.draw_frame,\n            enum_to_str(\n                QStyle.ControlElement,\n                QStyle.ControlElement.CE_MenuItem,\n                \"QMenu\",\n            ): self.draw_menu_item,\n            enum_to_str(\n                QStyle.ControlElement,\n                QStyle.ControlElement.CE_MenuEmptyArea,\n                \"QMenu\",\n            ): do_nothing,\n        }\n\n    def register_sizers(self) -&gt; dict:\n        return {\n            enum_to_str(\n                QStyle.ContentsType,\n                QStyle.ContentsType.CT_MenuItem,\n                \"QMenu\",\n            ): self.menu_item_size,\n        }\n\n    def register_metrics(self) -&gt; dict:\n        pm = QStyle.PixelMetric\n        metrics_map = {\n            pm.PM_MenuPanelWidth: self.get_metric,\n            pm.PM_MenuHMargin: self.get_metric,\n            pm.PM_MenuVMargin: self.get_metric,\n            pm.PM_SmallIconSize: self.get_metric,\n            pm.PM_MenuButtonIndicator: self.get_metric,\n        }\n        return {enum_to_str(pm, k, \"QMenu\"): v for k, v in metrics_map.items()}\n\n    # ------------------------------------------------------------------\n    # Metrics\n    # ------------------------------------------------------------------\n\n    def _base_style(self, widget: QWidget | None = None):\n        \"\"\"Return the base style dict, context-bound to *widget*.\n\n        This is used for general operations like metrics calculation and\n        panel/frame painting, where the state/variant-specific styles are\n        not relevant.\n        \"\"\"\n        style = self.model.get_style(self._WIDGET_CLS, \"default\", \"base\")\n        style.set_context(widget)\n        return style\n\n    def get_metric(\n        self,\n        metric: QStyle.PixelMetric,\n        opt: QStyleOption | None = None,\n        widget: QWidget | None = None,\n    ) -&gt; int:\n        pm = QStyle.PixelMetric\n        style = self._base_style(widget)\n        if metric == pm.PM_MenuPanelWidth:\n            return int(style.get(\"border-width\", 1))\n        if metric in (pm.PM_MenuHMargin, pm.PM_MenuVMargin):\n            pp = style.get(\"panel-padding\", [4, 4])\n            if isinstance(pp, (list, tuple)):\n                return int(pp[0] if metric == pm.PM_MenuHMargin else pp[1])\n            return int(pp)\n        if metric == pm.PM_SmallIconSize:\n            return int(style.get(\"icon-size\", 16))\n        if metric == pm.PM_MenuButtonIndicator:\n            return int(style.get(\"icon-size\", 16))\n        return 0\n\n    # ------------------------------------------------------------------\n    # Sizing and Layout Helper\n    # ------------------------------------------------------------------\n\n    def _compute_layout(\n        self,\n        option: QStyleOptionMenuItem,\n        style: dict[str, Any],\n        contents_size: QSize | None = None,\n    ) -&gt; MenuItemLayout:\n        \"\"\"Compute the metrics, dimensions and layouts for a menu item.\n\n        Args:\n            option: The style option for the menu item.\n            style: The resolved style dict.\n            contents_size: Optional contents size passed from size query.\n\n        Returns:\n            The computed layout information for the menu item.\n        \"\"\"\n        ip = style.get(\"item-padding\", [6, 6])\n        if isinstance(ip, (list, tuple)):\n            pad_h, pad_v = int(ip[0]), int(ip[1])\n        else:\n            pad_h = pad_v = int(ip)\n\n        icon_size = int(style.get(\"icon-size\", 16))\n        item_spacing = int(style.get(\"item-spacing\", 4))\n\n        fm = option.fontMetrics\n        text_h = (\n            fm.height()\n            if fm\n            else (\n                contents_size.height()\n                if contents_size\n                else option.rect.height()\n            )\n        )\n        if text_h &lt;= 0:\n            text_h = 16\n\n        min_h = int(style.get(\"min-item-height\", 0))\n        row_h = max(text_h + pad_v * 2, icon_size + pad_v * 2, min_h)\n\n        text = option.text or \"\"\n        label, _, shortcut = text.partition(\"\\t\")\n        text_w = (\n            fm.horizontalAdvance(label)\n            if fm\n            else (contents_size.width() if contents_size else 0)\n        )\n\n        sc_w = getattr(\n            option,\n            \"reservedShortcutWidth\",\n            getattr(option, \"tabWidth\", 0),\n        )\n        if shortcut and sc_w == 0 and fm:\n            sc_w = fm.horizontalAdvance(shortcut)\n\n        icon_gutter = (option.maxIconWidth or icon_size) + item_spacing\n        is_submenu = (\n            option.menuItemType == QStyleOptionMenuItem.MenuItemType.SubMenu\n        )\n        arrow_w = icon_size if is_submenu else 0\n\n        total_w = (\n            icon_gutter\n            + pad_h\n            + text_w\n            + (pad_h + sc_w if sc_w else 0)\n            + (pad_h + arrow_w if arrow_w else 0)\n            + pad_h\n        )\n\n        return MenuItemLayout(\n            pad_h=pad_h,\n            pad_v=pad_v,\n            icon_size=icon_size,\n            item_spacing=item_spacing,\n            icon_gutter=icon_gutter,\n            label_text=label,\n            shortcut_text=shortcut,\n            text_w=text_w,\n            text_h=text_h,\n            sc_w=sc_w,\n            arrow_w=arrow_w,\n            row_h=row_h,\n            total_w=total_w,\n        )\n\n    def menu_item_size(\n        self,\n        contents_type: QStyle.ContentsType,\n        option: QStyleOption | None,\n        contents_size: QSize,\n        widget: QWidget | None = None,\n    ) -&gt; QSize:\n        \"\"\"Compute the bounding size of a single menu item row.\"\"\"\n        if not isinstance(option, QStyleOptionMenuItem):\n            return QSize(contents_size.width(), contents_size.height())\n\n        style = self._base_style(widget)\n\n        # Separator: early exit\n        sep_type = QStyleOptionMenuItem.MenuItemType.Separator\n        if option.menuItemType == sep_type:\n            sep_h = int(style.get(\"separator-height\", 1))\n            return QSize(contents_size.width(), sep_h)\n\n        layout = self._compute_layout(option, style, contents_size)\n        return QSize(\n            max(layout.total_w, contents_size.width()),\n            layout.row_h,\n        )\n\n    # ------------------------------------------------------------------\n    # Primitive painting: panel &amp; frame\n    # ------------------------------------------------------------------\n\n    def draw_panel(\n        self,\n        option: QStyleOption,\n        painter: QPainter,\n        widget: QWidget | None = None,\n    ) -&gt; None:\n        \"\"\"Fill the menu background with the panel colour.\"\"\"\n        painter.save()\n        painter.setRenderHint(QPainter.RenderHint.Antialiasing, True)\n        style = self._base_style(widget)\n        radius = int(style.get(\"border-radius\", 6))\n        painter.setBrush(QBrush(QColor(style[\"background-color\"])))\n        painter.setPen(Qt.PenStyle.NoPen)\n        painter.drawRoundedRect(option.rect, radius, radius)\n        painter.restore()\n\n    def draw_frame(\n        self,\n        option: QStyleOption,\n        painter: QPainter,\n        widget: QWidget | None = None,\n    ) -&gt; None:\n        \"\"\"Stroke the menu border.\"\"\"\n        painter.save()\n        painter.setRenderHint(QPainter.RenderHint.Antialiasing, True)\n        style = self._base_style(widget)\n        radius = int(style.get(\"border-radius\", 6))\n        bw = int(style.get(\"border-width\", 1))\n        pen = QPen(QColor(style[\"border-color\"]))\n        pen.setWidth(bw)\n        painter.setPen(pen)\n        painter.setBrush(Qt.BrushStyle.NoBrush)\n        # Inset by half the pen width so the stroke is fully inside the rect.\n        inset = bw / 2.0\n        inset_rect = QRectF(option.rect).adjusted(inset, inset, -inset, -inset)\n        painter.drawRoundedRect(inset_rect, radius, radius)\n        painter.restore()\n\n    # ------------------------------------------------------------------\n    # Control painting: individual item rows\n    # ------------------------------------------------------------------\n\n    def draw_menu_item(\n        self,\n        option: QStyleOption,\n        painter: QPainter,\n        widget: QWidget | None = None,\n    ) -&gt; None:\n        \"\"\"Paint a single QMenu row (normal, separator, submenu).\"\"\"\n        if not isinstance(option, QStyleOptionMenuItem):\n            return\n\n        painter.save()\n        painter.setRenderHint(QPainter.RenderHint.Antialiasing, True)\n\n        # Handle separators and return\n        sep_type = QStyleOptionMenuItem.MenuItemType.Separator\n        if option.menuItemType == sep_type:\n            self._draw_separator(option, painter, widget)\n            painter.restore()\n            return\n\n        # Resolve state and fetch styles\n        is_enabled = bool(option.state &amp; QStyle.StateFlag.State_Enabled)\n        is_selected = bool(option.state &amp; QStyle.StateFlag.State_Selected)\n        state = (\n            \"disabled\"\n            if not is_enabled\n            else \"hover\"\n            if is_selected and is_enabled\n            else \"base\"\n        )\n\n        # Resolve variant from action property, if available\n        # NOTE: This is a bit of a hack. I could have created a QAction section\n        # in ayon_style.json and fetched the variant from there.\n        action = None\n        if isinstance(widget, QMenu):\n            action = widget.actionAt(option.rect.center())\n        variant = (action.property(\"variant\") if action else None) or \"default\"\n\n        style = self.model.get_style(\n            self._WIDGET_CLS, variant=variant, state=state\n        )\n        style.set_context(widget)\n\n        layout = self._compute_layout(option, style)\n        item_radius = int(style.get(\"item-radius\", 4))\n        rect = option.rect\n\n        # --- Selection background ---\n        bg = QColor(style.get(\"background-color\", \"#424a57\"))\n        painter.setBrush(QBrush(bg))\n        painter.setPen(Qt.PenStyle.NoPen)\n        painter.drawRoundedRect(rect, item_radius, item_radius)\n\n        opacity = float(style.get(\"opacity\", 1.0))\n        painter.setOpacity(opacity)\n\n        # --- Left gutter (icon or check mark) ---\n        x = rect.left() + layout.pad_h\n        cy = rect.center().y()\n\n        check_type = option.checkType\n        not_checkable = QStyleOptionMenuItem.CheckType.NotCheckable\n        if check_type != not_checkable:\n            check_color = QColor(style.get(\"color\", \"#f4f5f5\"))\n            check_icon = get_icon(\n                \"check_box\" if option.checked else \"check_box_outline_blank\",\n                color=check_color,\n                fill=False,\n            )\n            check_rect = QRect(\n                x,\n                cy - layout.icon_size // 2,\n                layout.icon_size,\n                layout.icon_size,\n            )\n            check_icon.paint(painter, check_rect)\n        elif not option.icon.isNull():\n            icon_rect = QRect(\n                x,\n                cy - layout.icon_size // 2,\n                layout.icon_size,\n                layout.icon_size,\n            )\n            mode = QIcon.Mode.Disabled if not is_enabled else QIcon.Mode.Normal\n            option.icon.paint(\n                painter, icon_rect, Qt.AlignmentFlag.AlignCenter, mode\n            )\n\n        x += layout.icon_gutter\n\n        # --- Text (label + shortcut) ---\n        text_color = QColor(style.get(\"color\", \"#f4f5f5\"))\n        painter.setPen(QPen(text_color))\n\n        right_margin = layout.pad_h + (\n            layout.arrow_w + layout.pad_h if layout.arrow_w else 0\n        )\n        label_rect = QRect(\n            x,\n            rect.top(),\n            rect.right()\n            - x\n            - right_margin\n            - (layout.sc_w + layout.pad_h if layout.sc_w else 0),\n            rect.height(),\n        )\n        painter.drawText(\n            label_rect,\n            Qt.AlignmentFlag.AlignVCenter | Qt.AlignmentFlag.AlignLeft,\n            layout.label_text,\n        )\n\n        if layout.shortcut_text:\n            sc_bg_color = QColor(\n                style.get(\n                    \"shortcut-background-color\",\n                    style.get(\"background-color\", \"#2b3036\"),\n                )\n            )\n            text_rect = QFontMetrics(\n                self.style_inst.model.base_font\n            ).boundingRect(layout.shortcut_text)\n            painter.setBrush(QBrush(sc_bg_color))\n            painter.setPen(Qt.PenStyle.NoPen)\n            sc_rect = QRect(\n                rect.right() - right_margin - layout.sc_w - layout.pad_h,\n                rect.top(),\n                layout.sc_w + layout.pad_h,\n                rect.height(),\n            )\n            text_rect.moveCenter(sc_rect.center())\n            painter.drawRoundedRect(text_rect.adjusted(-4, 0, 4, 0), 4, 4)\n\n            sc_color = QColor(\n                style.get(\n                    \"shortcut-color\",\n                    style.get(\"color\", \"#8b9198\"),\n                )\n            )\n            sc_color.setAlphaF(style.get(\"shortcut-opacity\", 0.6))\n            painter.setPen(QPen(sc_color))\n            painter.drawText(\n                sc_rect,\n                Qt.AlignmentFlag.AlignVCenter | Qt.AlignmentFlag.AlignHCenter,\n                layout.shortcut_text,\n            )\n\n        # --- Submenu arrow ---\n        if layout.arrow_w &gt; 0:\n            arrow_color = QColor(style.get(\"color\", \"#f4f5f5\"))\n            arrow_icon = get_icon(\"chevron_right\", color=arrow_color)\n            arrow_rect = QRect(\n                rect.right() - layout.pad_h - layout.arrow_w,\n                cy - layout.icon_size // 2,\n                layout.icon_size,\n                layout.icon_size,\n            )\n            arrow_icon.paint(painter, arrow_rect)\n\n        painter.restore()\n\n    def _draw_separator(\n        self,\n        option: QStyleOptionMenuItem,\n        painter: QPainter,\n        widget: QWidget | None = None,\n    ) -&gt; None:\n        \"\"\"Draw a thin horizontal separator line.\"\"\"\n        style = self._base_style(widget)\n        color = QColor(\n            style.get(\"separator-color\", style.get(\"border-color\", \"#41474d\"))\n        )\n        sep_h = int(style.get(\"separator-height\", 1))\n\n        cy = option.rect.center().y()\n        y = cy - sep_h // 2\n        painter.setPen(Qt.PenStyle.NoPen)\n        painter.setBrush(QBrush(color))\n        painter.drawRect(\n            option.rect.left(),\n            y,\n            option.rect.width(),\n            sep_h,\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/index.html#client.ayon_core.ui.drawers.MenuDrawer.draw_frame","title":"<code>draw_frame(option, painter, widget=None)</code>","text":"<p>Stroke the menu border.</p> Source code in <code>client/ayon_core/ui/drawers/menu.py</code> <pre><code>def draw_frame(\n    self,\n    option: QStyleOption,\n    painter: QPainter,\n    widget: QWidget | None = None,\n) -&gt; None:\n    \"\"\"Stroke the menu border.\"\"\"\n    painter.save()\n    painter.setRenderHint(QPainter.RenderHint.Antialiasing, True)\n    style = self._base_style(widget)\n    radius = int(style.get(\"border-radius\", 6))\n    bw = int(style.get(\"border-width\", 1))\n    pen = QPen(QColor(style[\"border-color\"]))\n    pen.setWidth(bw)\n    painter.setPen(pen)\n    painter.setBrush(Qt.BrushStyle.NoBrush)\n    # Inset by half the pen width so the stroke is fully inside the rect.\n    inset = bw / 2.0\n    inset_rect = QRectF(option.rect).adjusted(inset, inset, -inset, -inset)\n    painter.drawRoundedRect(inset_rect, radius, radius)\n    painter.restore()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/index.html#client.ayon_core.ui.drawers.MenuDrawer.draw_menu_item","title":"<code>draw_menu_item(option, painter, widget=None)</code>","text":"<p>Paint a single QMenu row (normal, separator, submenu).</p> Source code in <code>client/ayon_core/ui/drawers/menu.py</code> <pre><code>def draw_menu_item(\n    self,\n    option: QStyleOption,\n    painter: QPainter,\n    widget: QWidget | None = None,\n) -&gt; None:\n    \"\"\"Paint a single QMenu row (normal, separator, submenu).\"\"\"\n    if not isinstance(option, QStyleOptionMenuItem):\n        return\n\n    painter.save()\n    painter.setRenderHint(QPainter.RenderHint.Antialiasing, True)\n\n    # Handle separators and return\n    sep_type = QStyleOptionMenuItem.MenuItemType.Separator\n    if option.menuItemType == sep_type:\n        self._draw_separator(option, painter, widget)\n        painter.restore()\n        return\n\n    # Resolve state and fetch styles\n    is_enabled = bool(option.state &amp; QStyle.StateFlag.State_Enabled)\n    is_selected = bool(option.state &amp; QStyle.StateFlag.State_Selected)\n    state = (\n        \"disabled\"\n        if not is_enabled\n        else \"hover\"\n        if is_selected and is_enabled\n        else \"base\"\n    )\n\n    # Resolve variant from action property, if available\n    # NOTE: This is a bit of a hack. I could have created a QAction section\n    # in ayon_style.json and fetched the variant from there.\n    action = None\n    if isinstance(widget, QMenu):\n        action = widget.actionAt(option.rect.center())\n    variant = (action.property(\"variant\") if action else None) or \"default\"\n\n    style = self.model.get_style(\n        self._WIDGET_CLS, variant=variant, state=state\n    )\n    style.set_context(widget)\n\n    layout = self._compute_layout(option, style)\n    item_radius = int(style.get(\"item-radius\", 4))\n    rect = option.rect\n\n    # --- Selection background ---\n    bg = QColor(style.get(\"background-color\", \"#424a57\"))\n    painter.setBrush(QBrush(bg))\n    painter.setPen(Qt.PenStyle.NoPen)\n    painter.drawRoundedRect(rect, item_radius, item_radius)\n\n    opacity = float(style.get(\"opacity\", 1.0))\n    painter.setOpacity(opacity)\n\n    # --- Left gutter (icon or check mark) ---\n    x = rect.left() + layout.pad_h\n    cy = rect.center().y()\n\n    check_type = option.checkType\n    not_checkable = QStyleOptionMenuItem.CheckType.NotCheckable\n    if check_type != not_checkable:\n        check_color = QColor(style.get(\"color\", \"#f4f5f5\"))\n        check_icon = get_icon(\n            \"check_box\" if option.checked else \"check_box_outline_blank\",\n            color=check_color,\n            fill=False,\n        )\n        check_rect = QRect(\n            x,\n            cy - layout.icon_size // 2,\n            layout.icon_size,\n            layout.icon_size,\n        )\n        check_icon.paint(painter, check_rect)\n    elif not option.icon.isNull():\n        icon_rect = QRect(\n            x,\n            cy - layout.icon_size // 2,\n            layout.icon_size,\n            layout.icon_size,\n        )\n        mode = QIcon.Mode.Disabled if not is_enabled else QIcon.Mode.Normal\n        option.icon.paint(\n            painter, icon_rect, Qt.AlignmentFlag.AlignCenter, mode\n        )\n\n    x += layout.icon_gutter\n\n    # --- Text (label + shortcut) ---\n    text_color = QColor(style.get(\"color\", \"#f4f5f5\"))\n    painter.setPen(QPen(text_color))\n\n    right_margin = layout.pad_h + (\n        layout.arrow_w + layout.pad_h if layout.arrow_w else 0\n    )\n    label_rect = QRect(\n        x,\n        rect.top(),\n        rect.right()\n        - x\n        - right_margin\n        - (layout.sc_w + layout.pad_h if layout.sc_w else 0),\n        rect.height(),\n    )\n    painter.drawText(\n        label_rect,\n        Qt.AlignmentFlag.AlignVCenter | Qt.AlignmentFlag.AlignLeft,\n        layout.label_text,\n    )\n\n    if layout.shortcut_text:\n        sc_bg_color = QColor(\n            style.get(\n                \"shortcut-background-color\",\n                style.get(\"background-color\", \"#2b3036\"),\n            )\n        )\n        text_rect = QFontMetrics(\n            self.style_inst.model.base_font\n        ).boundingRect(layout.shortcut_text)\n        painter.setBrush(QBrush(sc_bg_color))\n        painter.setPen(Qt.PenStyle.NoPen)\n        sc_rect = QRect(\n            rect.right() - right_margin - layout.sc_w - layout.pad_h,\n            rect.top(),\n            layout.sc_w + layout.pad_h,\n            rect.height(),\n        )\n        text_rect.moveCenter(sc_rect.center())\n        painter.drawRoundedRect(text_rect.adjusted(-4, 0, 4, 0), 4, 4)\n\n        sc_color = QColor(\n            style.get(\n                \"shortcut-color\",\n                style.get(\"color\", \"#8b9198\"),\n            )\n        )\n        sc_color.setAlphaF(style.get(\"shortcut-opacity\", 0.6))\n        painter.setPen(QPen(sc_color))\n        painter.drawText(\n            sc_rect,\n            Qt.AlignmentFlag.AlignVCenter | Qt.AlignmentFlag.AlignHCenter,\n            layout.shortcut_text,\n        )\n\n    # --- Submenu arrow ---\n    if layout.arrow_w &gt; 0:\n        arrow_color = QColor(style.get(\"color\", \"#f4f5f5\"))\n        arrow_icon = get_icon(\"chevron_right\", color=arrow_color)\n        arrow_rect = QRect(\n            rect.right() - layout.pad_h - layout.arrow_w,\n            cy - layout.icon_size // 2,\n            layout.icon_size,\n            layout.icon_size,\n        )\n        arrow_icon.paint(painter, arrow_rect)\n\n    painter.restore()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/index.html#client.ayon_core.ui.drawers.MenuDrawer.draw_panel","title":"<code>draw_panel(option, painter, widget=None)</code>","text":"<p>Fill the menu background with the panel colour.</p> Source code in <code>client/ayon_core/ui/drawers/menu.py</code> <pre><code>def draw_panel(\n    self,\n    option: QStyleOption,\n    painter: QPainter,\n    widget: QWidget | None = None,\n) -&gt; None:\n    \"\"\"Fill the menu background with the panel colour.\"\"\"\n    painter.save()\n    painter.setRenderHint(QPainter.RenderHint.Antialiasing, True)\n    style = self._base_style(widget)\n    radius = int(style.get(\"border-radius\", 6))\n    painter.setBrush(QBrush(QColor(style[\"background-color\"])))\n    painter.setPen(Qt.PenStyle.NoPen)\n    painter.drawRoundedRect(option.rect, radius, radius)\n    painter.restore()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/index.html#client.ayon_core.ui.drawers.MenuDrawer.menu_item_size","title":"<code>menu_item_size(contents_type, option, contents_size, widget=None)</code>","text":"<p>Compute the bounding size of a single menu item row.</p> Source code in <code>client/ayon_core/ui/drawers/menu.py</code> <pre><code>def menu_item_size(\n    self,\n    contents_type: QStyle.ContentsType,\n    option: QStyleOption | None,\n    contents_size: QSize,\n    widget: QWidget | None = None,\n) -&gt; QSize:\n    \"\"\"Compute the bounding size of a single menu item row.\"\"\"\n    if not isinstance(option, QStyleOptionMenuItem):\n        return QSize(contents_size.width(), contents_size.height())\n\n    style = self._base_style(widget)\n\n    # Separator: early exit\n    sep_type = QStyleOptionMenuItem.MenuItemType.Separator\n    if option.menuItemType == sep_type:\n        sep_h = int(style.get(\"separator-height\", 1))\n        return QSize(contents_size.width(), sep_h)\n\n    layout = self._compute_layout(option, style, contents_size)\n    return QSize(\n        max(layout.total_w, contents_size.width()),\n        layout.row_h,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/index.html#client.ayon_core.ui.drawers.ScrollBarDrawer","title":"<code>ScrollBarDrawer</code>","text":"Source code in <code>client/ayon_core/ui/drawers/scrollbar.py</code> <pre><code>class ScrollBarDrawer:\n    def __init__(self, style_inst: AYONStyle) -&gt; None:\n        self.style_inst = style_inst\n        self.model = style_inst.model\n        self._style = self.model.get_style(\"QScrollBar\")\n        self._cache = {}\n\n    @property\n    def _super(self):\n        \"\"\"Return proxy for calling QCommonStyle methods on style_inst.\"\"\"\n        from ..style import AYONStyle as _AYONStyle\n\n        return super(_AYONStyle, self.style_inst)\n\n    @property\n    def base_class(self):\n        return {\"QScrollBar\": QtWidgets.QScrollBar}\n\n    def register_drawers(self):\n        return {\n            enum_to_str(\n                QStyle.ControlElement,\n                QStyle.ControlElement.CE_ScrollBarSlider,\n                \"QScrollBar\",\n            ): self.draw_scrollbar_slider,\n            enum_to_str(\n                QStyle.ControlElement,\n                QStyle.ControlElement.CE_ScrollBarAddPage,\n                \"QScrollBar\",\n            ): self.draw_scrollbar_page,\n            enum_to_str(\n                QStyle.ControlElement,\n                QStyle.ControlElement.CE_ScrollBarSubPage,\n                \"QScrollBar\",\n            ): self.draw_scrollbar_page,\n        }\n\n    def register_sizers(self):\n        return {\n            enum_to_str(\n                QStyle.ComplexControl,\n                QStyle.ComplexControl.CC_ScrollBar,\n                \"QScrollBar\",\n            ): self.get_size,\n        }\n\n    def register_metrics(self):\n        return {\n            enum_to_str(\n                QStyle.PixelMetric,\n                QStyle.PixelMetric.PM_ScrollBarExtent,\n                \"QScrollBar\",\n            ): self.get_metric,\n            enum_to_str(\n                QStyle.PixelMetric,\n                QStyle.PixelMetric.PM_ScrollBarSliderMin,\n                \"QScrollBar\",\n            ): self.get_metric,\n        }\n\n    def get_size(\n        self,\n        cc: QStyle.ComplexControl,\n        opt: QStyleOptionComplex,\n        sc: QStyle.SubControl,\n        w: QWidget | None = None,\n    ) -&gt; QRect | None:\n        if not w:\n            raise ValueError(\n                \"Widget required to calculate scrollbar sub-control rects\"\n            )\n\n        if not isinstance(opt, (QStyleOptionSlider, QStyleOptionComplex)):\n            raise ValueError(f\"Unexpected option type: {type(opt)}\")\n\n        sup = self._super\n        try:\n            als = self._cache[\"add_line_size\"]\n        except KeyError:\n            als = self._cache[\"add_line_size\"] = sup.subControlRect(\n                cc, opt, QStyle.SubControl.SC_ScrollBarAddLine, w\n            ).size()\n        try:\n            sls = self._cache[\"sub_line_size\"]\n        except KeyError:\n            sls = self._cache[\"sub_line_size\"] = sup.subControlRect(\n                cc, opt, QStyle.SubControl.SC_ScrollBarSubLine, w\n            ).size()\n\n        orientation = w.orientation()\n\n        if sc in (\n            QStyle.SubControl.SC_ScrollBarSlider,\n            QStyle.SubControl.SC_ScrollBarGroove,\n        ):\n            rect = sup.subControlRect(cc, opt, sc, w)\n            if orientation == Qt.Orientation.Vertical:\n                rect.adjust(0, -sls.height(), 0, als.height())\n            else:\n                rect.adjust(-sls.width(), 0, als.width(), 0)\n            return rect\n\n        elif sc == QStyle.SubControl.SC_ScrollBarAddPage:\n            rect = sup.subControlRect(cc, opt, sc, w)\n            if orientation == Qt.Orientation.Vertical:\n                rect.adjust(0, 0, 0, als.height())\n            else:\n                rect.adjust(0, 0, als.width(), 0)\n            return rect\n\n        elif sc == QStyle.SubControl.SC_ScrollBarSubPage:\n            rect = sup.subControlRect(cc, opt, sc, w)\n            if orientation == Qt.Orientation.Vertical:\n                rect.adjust(0, -sls.height(), 0, 0)\n            else:\n                rect.adjust(-sls.width(), 0, 0, 0)\n            return rect\n\n        raise ValueError(\"Unexpected sub-control\")\n\n    def get_metric(\n        self,\n        metric: QStyle.PixelMetric,\n        opt: QStyleOption | None = None,\n        widget: QWidget | None = None,\n    ) -&gt; int:\n        self._style.set_context(widget)\n        if metric == QStyle.PixelMetric.PM_ScrollBarExtent:\n            # Width of a vertical scroll bar and the height of a horizontal\n            # scroll bar.\n            return int(self._style[\"width\"])\n        elif metric == QStyle.PixelMetric.PM_ScrollBarSliderMin:\n            # The minimum height of a vertical scroll bar's slider and the\n            # minimum width of a horizontal scroll bar's slider.\n            return int(self._style[\"min-length\"])\n        return 0\n\n    def draw_scrollbar_slider(\n        self,\n        option: QStyleOptionComplex,\n        painter: QPainter,\n        widget: QWidget | None = None,\n    ) -&gt; None:\n        \"\"\"Draw the scrollbar slider/thumb.\"\"\"\n        style = self.model.get_style(\"QScrollBar\")\n        style.set_context(widget)\n        painter.save()\n        painter.setRenderHint(QPainter.RenderHint.Antialiasing)\n\n        # Draw slider background\n        painter.setBrush(QBrush(QColor(style.get(\"slider-color\"))))\n        pen = QPen(QColor(style.get(\"background-color\")))\n        pen.setWidth(style.get(\"border-width\"))\n        painter.setPen(pen)\n        radius = style.get(\"border-radius\")\n        painter.drawRoundedRect(option.rect, radius, radius)\n\n        painter.restore()\n\n    def draw_scrollbar_page(\n        self,\n        option: QStyleOptionComplex,\n        painter: QPainter,\n        widget: QWidget | None = None,\n    ) -&gt; None:\n        \"\"\"Draw scrollbar page buttons.\"\"\"\n        style = self.model.get_style(\"QScrollBar\")\n        style.set_context(widget)\n        painter.save()\n        painter.setRenderHint(QPainter.RenderHint.Antialiasing)\n\n        # Draw slider background\n        painter.setBrush(QBrush(QColor(style.get(\"background-color\"))))\n        painter.setPen(Qt.PenStyle.NoPen)\n        painter.drawRect(option.rect)\n\n        painter.restore()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/index.html#client.ayon_core.ui.drawers.ScrollBarDrawer.draw_scrollbar_page","title":"<code>draw_scrollbar_page(option, painter, widget=None)</code>","text":"<p>Draw scrollbar page buttons.</p> Source code in <code>client/ayon_core/ui/drawers/scrollbar.py</code> <pre><code>def draw_scrollbar_page(\n    self,\n    option: QStyleOptionComplex,\n    painter: QPainter,\n    widget: QWidget | None = None,\n) -&gt; None:\n    \"\"\"Draw scrollbar page buttons.\"\"\"\n    style = self.model.get_style(\"QScrollBar\")\n    style.set_context(widget)\n    painter.save()\n    painter.setRenderHint(QPainter.RenderHint.Antialiasing)\n\n    # Draw slider background\n    painter.setBrush(QBrush(QColor(style.get(\"background-color\"))))\n    painter.setPen(Qt.PenStyle.NoPen)\n    painter.drawRect(option.rect)\n\n    painter.restore()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/index.html#client.ayon_core.ui.drawers.ScrollBarDrawer.draw_scrollbar_slider","title":"<code>draw_scrollbar_slider(option, painter, widget=None)</code>","text":"<p>Draw the scrollbar slider/thumb.</p> Source code in <code>client/ayon_core/ui/drawers/scrollbar.py</code> <pre><code>def draw_scrollbar_slider(\n    self,\n    option: QStyleOptionComplex,\n    painter: QPainter,\n    widget: QWidget | None = None,\n) -&gt; None:\n    \"\"\"Draw the scrollbar slider/thumb.\"\"\"\n    style = self.model.get_style(\"QScrollBar\")\n    style.set_context(widget)\n    painter.save()\n    painter.setRenderHint(QPainter.RenderHint.Antialiasing)\n\n    # Draw slider background\n    painter.setBrush(QBrush(QColor(style.get(\"slider-color\"))))\n    pen = QPen(QColor(style.get(\"background-color\")))\n    pen.setWidth(style.get(\"border-width\"))\n    painter.setPen(pen)\n    radius = style.get(\"border-radius\")\n    painter.drawRoundedRect(option.rect, radius, radius)\n\n    painter.restore()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/index.html#client.ayon_core.ui.drawers.TableHeaderDrawer","title":"<code>TableHeaderDrawer</code>","text":"<p>AYONStyle drawer for QHeaderView used by AYTableView.</p> <p>Handles painting of header sections and labels using colours from the AYTableView style data in ayon_style.json.</p> Source code in <code>client/ayon_core/ui/drawers/table_header.py</code> <pre><code>class TableHeaderDrawer:\n    \"\"\"AYONStyle drawer for QHeaderView used by AYTableView.\n\n    Handles painting of header sections and labels using colours\n    from the AYTableView style data in ayon_style.json.\n    \"\"\"\n\n    def __init__(self, style_inst: AYONStyle) -&gt; None:\n        self.style_inst = style_inst\n        self.model = style_inst.model\n        self._icon_cache: dict[str, QIcon] = {}\n\n    @property\n    def base_class(self):\n        return {\"QHeaderView\": QtWidgets.QHeaderView}\n\n    def register_drawers(self) -&gt; dict:\n        return {\n            enum_to_str(\n                QStyle.ControlElement,\n                QStyle.ControlElement.CE_Header,\n                \"QHeaderView\",\n            ): [\n                partial(\n                    self.style_inst.drawControl,\n                    QStyle.ControlElement.CE_HeaderSection,\n                ),\n                partial(\n                    self.style_inst.drawControl,\n                    QStyle.ControlElement.CE_HeaderLabel,\n                ),\n            ],\n            enum_to_str(\n                QStyle.ControlElement,\n                QStyle.ControlElement.CE_HeaderSection,\n                \"QHeaderView\",\n            ): self.draw_header_section,\n            enum_to_str(\n                QStyle.ControlElement,\n                QStyle.ControlElement.CE_HeaderLabel,\n                \"QHeaderView\",\n            ): self.draw_header_label,\n        }\n\n    def register_metrics(self) -&gt; dict:\n        \"\"\"Register pixel metrics for QHeaderView.\"\"\"\n        return {\n            enum_to_str(\n                QStyle.PixelMetric,\n                QStyle.PixelMetric.PM_HeaderMargin,\n                \"QHeaderView\",\n            ): self.get_metric,\n        }\n\n    def get_metric(\n        self,\n        metric: QStyle.PixelMetric,\n        opt: QStyleOption | None = None,\n        widget: QWidget | None = None,\n    ) -&gt; int:\n        \"\"\"Return header margin from style data.\"\"\"\n        if metric == QStyle.PixelMetric.PM_HeaderMargin:\n            return 4\n        return 0\n\n    def _get_table_style(self, widget: QWidget | None) -&gt; dict:\n        \"\"\"Resolve the AYTableView style for the header's parent table.\"\"\"\n        variant = \"default\"\n        if widget is not None:\n            # QHeaderView's parent is the QTreeView/AYTableView\n            table = widget.parent()\n            if table is not None:\n                variant = getattr(table, \"_variant_str\", \"default\")\n        return self.model.get_style(\"AYTableView\", variant)\n\n    def draw_header_section(\n        self,\n        option: QStyleOption,\n        painter: QPainter,\n        widget: QWidget | None = None,\n    ) -&gt; None:\n        \"\"\"Draw the header section background and bottom border.\"\"\"\n        style = self._get_table_style(widget)\n\n        painter.save()\n        painter.setRenderHint(QPainter.RenderHint.Antialiasing, False)\n\n        # Background\n        bg_color = QColor(style.get(\"header-background-color\", \"#272d35\"))\n        painter.setBrush(QBrush(bg_color))\n        painter.setPen(Qt.PenStyle.NoPen)\n        painter.drawRect(option.rect)\n\n        # Bottom border\n        border_color = QColor(style.get(\"header-border-color\", \"#41474d\"))\n        pen = QPen(border_color)\n        pen.setWidth(1)\n        painter.setPen(pen)\n        bottom = option.rect.bottom()\n        painter.drawLine(\n            option.rect.left(),\n            bottom,\n            option.rect.right(),\n            bottom,\n        )\n\n        painter.restore()\n\n    def draw_header_label(\n        self,\n        option: QStyleOption,\n        painter: QPainter,\n        widget: QWidget | None = None,\n    ) -&gt; None:\n        \"\"\"Draw the header label text and sort indicator.\"\"\"\n        style = self._get_table_style(widget)\n        padding = style.get(\"header-padding\", [4, 8])\n\n        painter.save()\n\n        # Text\n        text_color = QColor(style.get(\"header-color\", \"#c1c7ce\"))\n        painter.setPen(text_color)\n\n        font = painter.font()\n        font.setWeight(QFont.Weight.DemiBold)\n        painter.setFont(font)\n\n        text_rect = option.rect.adjusted(\n            padding[1], padding[0], -padding[1], -padding[0]\n        )\n\n        text = \"\"\n        if hasattr(option, \"text\"):\n            text = option.text or \"\"\n\n        # Check for sort indicator\n        sort_indicator = getattr(option, \"sortIndicator\", None)\n        indicator_space = 0\n        if sort_indicator and sort_indicator != 0:\n            indicator_space = 16\n\n        if text:\n            draw_rect = QRect(text_rect)\n            if indicator_space:\n                draw_rect.setRight(draw_rect.right() - indicator_space)\n            painter.drawText(\n                draw_rect,\n                Qt.AlignmentFlag.AlignVCenter | Qt.AlignmentFlag.AlignLeft,\n                text,\n            )\n\n        # Sort indicator arrow\n        if sort_indicator and sort_indicator != 0:\n            indicator_color = QColor(\n                style.get(\n                    \"header-sort-indicator-color\",\n                    \"#8fceff\",\n                )\n            )\n            # sortIndicator: 1 = Down, 2 = Up (in QStyleOptionHeader)\n            icon_name = (\n                \"arrow_downward\" if sort_indicator == 1 else \"arrow_upward\"\n            )\n            cache_key = f\"{icon_name}-{indicator_color.name()}\"\n            if cache_key not in self._icon_cache:\n                self._icon_cache[cache_key] = get_icon(\n                    icon_name, color=indicator_color\n                )\n            icon = self._icon_cache[cache_key]\n            icon_rect = QRect(\n                text_rect.right() - 14,\n                text_rect.center().y() - 7,\n                14,\n                14,\n            )\n            icon.paint(painter, icon_rect)\n\n        painter.restore()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/index.html#client.ayon_core.ui.drawers.TableHeaderDrawer.draw_header_label","title":"<code>draw_header_label(option, painter, widget=None)</code>","text":"<p>Draw the header label text and sort indicator.</p> Source code in <code>client/ayon_core/ui/drawers/table_header.py</code> <pre><code>def draw_header_label(\n    self,\n    option: QStyleOption,\n    painter: QPainter,\n    widget: QWidget | None = None,\n) -&gt; None:\n    \"\"\"Draw the header label text and sort indicator.\"\"\"\n    style = self._get_table_style(widget)\n    padding = style.get(\"header-padding\", [4, 8])\n\n    painter.save()\n\n    # Text\n    text_color = QColor(style.get(\"header-color\", \"#c1c7ce\"))\n    painter.setPen(text_color)\n\n    font = painter.font()\n    font.setWeight(QFont.Weight.DemiBold)\n    painter.setFont(font)\n\n    text_rect = option.rect.adjusted(\n        padding[1], padding[0], -padding[1], -padding[0]\n    )\n\n    text = \"\"\n    if hasattr(option, \"text\"):\n        text = option.text or \"\"\n\n    # Check for sort indicator\n    sort_indicator = getattr(option, \"sortIndicator\", None)\n    indicator_space = 0\n    if sort_indicator and sort_indicator != 0:\n        indicator_space = 16\n\n    if text:\n        draw_rect = QRect(text_rect)\n        if indicator_space:\n            draw_rect.setRight(draw_rect.right() - indicator_space)\n        painter.drawText(\n            draw_rect,\n            Qt.AlignmentFlag.AlignVCenter | Qt.AlignmentFlag.AlignLeft,\n            text,\n        )\n\n    # Sort indicator arrow\n    if sort_indicator and sort_indicator != 0:\n        indicator_color = QColor(\n            style.get(\n                \"header-sort-indicator-color\",\n                \"#8fceff\",\n            )\n        )\n        # sortIndicator: 1 = Down, 2 = Up (in QStyleOptionHeader)\n        icon_name = (\n            \"arrow_downward\" if sort_indicator == 1 else \"arrow_upward\"\n        )\n        cache_key = f\"{icon_name}-{indicator_color.name()}\"\n        if cache_key not in self._icon_cache:\n            self._icon_cache[cache_key] = get_icon(\n                icon_name, color=indicator_color\n            )\n        icon = self._icon_cache[cache_key]\n        icon_rect = QRect(\n            text_rect.right() - 14,\n            text_rect.center().y() - 7,\n            14,\n            14,\n        )\n        icon.paint(painter, icon_rect)\n\n    painter.restore()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/index.html#client.ayon_core.ui.drawers.TableHeaderDrawer.draw_header_section","title":"<code>draw_header_section(option, painter, widget=None)</code>","text":"<p>Draw the header section background and bottom border.</p> Source code in <code>client/ayon_core/ui/drawers/table_header.py</code> <pre><code>def draw_header_section(\n    self,\n    option: QStyleOption,\n    painter: QPainter,\n    widget: QWidget | None = None,\n) -&gt; None:\n    \"\"\"Draw the header section background and bottom border.\"\"\"\n    style = self._get_table_style(widget)\n\n    painter.save()\n    painter.setRenderHint(QPainter.RenderHint.Antialiasing, False)\n\n    # Background\n    bg_color = QColor(style.get(\"header-background-color\", \"#272d35\"))\n    painter.setBrush(QBrush(bg_color))\n    painter.setPen(Qt.PenStyle.NoPen)\n    painter.drawRect(option.rect)\n\n    # Bottom border\n    border_color = QColor(style.get(\"header-border-color\", \"#41474d\"))\n    pen = QPen(border_color)\n    pen.setWidth(1)\n    painter.setPen(pen)\n    bottom = option.rect.bottom()\n    painter.drawLine(\n        option.rect.left(),\n        bottom,\n        option.rect.right(),\n        bottom,\n    )\n\n    painter.restore()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/index.html#client.ayon_core.ui.drawers.TableHeaderDrawer.get_metric","title":"<code>get_metric(metric, opt=None, widget=None)</code>","text":"<p>Return header margin from style data.</p> Source code in <code>client/ayon_core/ui/drawers/table_header.py</code> <pre><code>def get_metric(\n    self,\n    metric: QStyle.PixelMetric,\n    opt: QStyleOption | None = None,\n    widget: QWidget | None = None,\n) -&gt; int:\n    \"\"\"Return header margin from style data.\"\"\"\n    if metric == QStyle.PixelMetric.PM_HeaderMargin:\n        return 4\n    return 0\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/index.html#client.ayon_core.ui.drawers.TableHeaderDrawer.register_metrics","title":"<code>register_metrics()</code>","text":"<p>Register pixel metrics for QHeaderView.</p> Source code in <code>client/ayon_core/ui/drawers/table_header.py</code> <pre><code>def register_metrics(self) -&gt; dict:\n    \"\"\"Register pixel metrics for QHeaderView.\"\"\"\n    return {\n        enum_to_str(\n            QStyle.PixelMetric,\n            QStyle.PixelMetric.PM_HeaderMargin,\n            \"QHeaderView\",\n        ): self.get_metric,\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/index.html#client.ayon_core.ui.drawers.TooltipDrawer","title":"<code>TooltipDrawer</code>","text":"Source code in <code>client/ayon_core/ui/drawers/tooltip.py</code> <pre><code>class TooltipDrawer:\n    def __init__(self, style_inst: AYONStyle) -&gt; None:\n        self.style_inst = style_inst\n        self.model = style_inst.model\n\n    @property\n    def _super(self):\n        \"\"\"Return proxy for calling QCommonStyle methods on style_inst.\"\"\"\n        from ..style import AYONStyle as _AYONStyle\n\n        return super(_AYONStyle, self.style_inst)\n\n    @property\n    def base_class(self):\n        return {\"QToolTip\": QToolTip}\n\n    def register_drawers(self):\n        return {\n            enum_to_str(\n                QStyle.ControlElement,\n                QStyle.ControlElement.CE_ShapedFrame,\n                \"QToolTip\",\n            ): self.draw_control,\n            enum_to_str(\n                QStyle.PrimitiveElement,\n                QStyle.PrimitiveElement.PE_PanelTipLabel,\n                \"QToolTip\",\n            ): partial(\n                self.draw_primitive, QStyle.PrimitiveElement.PE_PanelTipLabel\n            ),\n            enum_to_str(\n                QStyle.PrimitiveElement,\n                QStyle.PrimitiveElement.PE_Frame,\n                \"QToolTip\",\n            ): partial(self.draw_primitive, QStyle.PrimitiveElement.PE_Frame),\n        }\n\n    def register_sizers(self):\n        return {\n            enum_to_str(\n                QStyle.SubElement,\n                QStyle.SubElement.SE_ShapedFrameContents,\n                \"QToolTip\",\n            ): self.get_rect,\n            enum_to_str(\n                QStyle.SubElement,\n                QStyle.SubElement.SE_FrameLayoutItem,\n                \"QToolTip\",\n            ): self.get_rect,\n        }\n\n    def draw_control(\n        self,\n        option: QStyleOptionFrame,\n        painter: QPainter,\n        widget: QWidget,\n    ):\n        option.frameShadow = QFrame.Shadow.Plain\n        option.frameShape = QFrame.Shape.StyledPanel\n        self._super.drawControl(\n            QStyle.ControlElement.CE_ShapedFrame, option, painter, widget\n        )\n\n    def draw_primitive(\n        self,\n        prim: QStyle.PrimitiveElement,\n        option: QStyleOption,\n        painter: QPainter,\n        w: QWidget,\n    ) -&gt; None:\n        if prim == QStyle.PrimitiveElement.PE_Frame:\n            painter.save()\n            painter.setRenderHint(QPainter.RenderHint.Antialiasing, True)\n            style = self.model.get_style(\"QToolTip\")\n            style.set_context(w)\n            pen = QPen(style[\"border-color\"])\n            pen.setWidth(style[\"border-width\"])\n            painter.setBrush(Qt.BrushStyle.NoBrush)\n            painter.setPen(pen)\n            radius = int(style[\"border-radius\"])\n            painter.drawRoundedRect(\n                option.rect,\n                radius,\n                radius,\n            )\n            painter.restore()\n\n        elif prim == QStyle.PrimitiveElement.PE_PanelTipLabel:\n            painter.save()\n            painter.setRenderHint(QPainter.RenderHint.Antialiasing, True)\n            style = self.model.get_style(\"QToolTip\")\n            style.set_context(w)\n            brush = QBrush(style[\"background-color\"])\n            painter.setBrush(brush)\n            painter.setPen(Qt.PenStyle.NoPen)\n            radius = int(style[\"border-radius\"])\n            painter.drawRoundedRect(\n                option.rect,\n                radius,\n                radius,\n            )\n            painter.restore()\n\n    def get_rect(\n        self,\n        element: QStyle.SubElement,\n        option: QStyleOption,\n        widget: QWidget,\n    ) -&gt; QRect:\n        tt_style = self.model.get_style(\"QToolTip\")\n        tt_style.set_context(widget)\n        tt_pad_x, tt_pad_y = tt_style[\"padding\"]\n\n        if element == QStyle.SubElement.SE_ShapedFrameContents:\n            if isinstance(option, QStyleOptionFrame):\n                option.features = QStyleOptionFrame.FrameFeature.Rounded\n                option.frameShape = QFrame.Shape.StyledPanel\n                widget.setContentsMargins(\n                    tt_pad_x, tt_pad_y, tt_pad_x, tt_pad_y\n                )\n\n        elif element == QStyle.SubElement.SE_FrameLayoutItem:\n            if isinstance(option, QStyleOptionFrame):\n                option.features = QStyleOptionFrame.FrameFeature.Rounded\n                option.frameShape = QFrame.Shape.StyledPanel\n                widget.setContentsMargins(\n                    tt_pad_x, tt_pad_y, tt_pad_x, tt_pad_y\n                )\n\n        return self._super.subElementRect(element, option, widget)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/index.html#client.ayon_core.ui.drawers.TreeViewDrawer","title":"<code>TreeViewDrawer</code>","text":"<p>AYONStyle drawer for QTreeView.</p> <p>Handles branch expand/collapse indicators and the indentation metric using colours from the QTreeView style data in ayon_style.json.</p> Source code in <code>client/ayon_core/ui/drawers/tree_view.py</code> <pre><code>class TreeViewDrawer:\n    \"\"\"AYONStyle drawer for QTreeView.\n\n    Handles branch expand/collapse indicators and the indentation metric\n    using colours from the QTreeView style data in ayon_style.json.\n    \"\"\"\n\n    def __init__(self, style_inst: AYONStyle) -&gt; None:\n        self.style_inst = style_inst\n        self.model = style_inst.model\n        self._icon_cache = {}\n\n    @property\n    def base_class(self):\n        return {\"QTreeView\": QTreeView}\n\n    def register_drawers(self) -&gt; dict:\n        \"\"\"Register drawing functions for QTreeView primitives.\"\"\"\n        return {\n            enum_to_str(\n                QStyle.PrimitiveElement,\n                QStyle.PrimitiveElement.PE_IndicatorBranch,\n                \"QTreeView\",\n            ): self.draw_branch_indicator,\n            enum_to_str(\n                QStyle.PrimitiveElement,\n                QStyle.PrimitiveElement.PE_PanelScrollAreaCorner,\n                \"QTreeView\",\n            ): self.draw_scrollbar_corner,\n        }\n\n    def register_metrics(self) -&gt; dict:\n        \"\"\"Register pixel metric functions for QTreeView.\"\"\"\n        return {\n            enum_to_str(\n                QStyle.PixelMetric,\n                QStyle.PixelMetric.PM_TreeViewIndentation,\n                \"QTreeView\",\n            ): self.get_metric,\n        }\n\n    def get_metric(\n        self,\n        metric: QStyle.PixelMetric,\n        opt: QStyleOption | None = None,\n        widget: QWidget | None = None,\n    ) -&gt; int:\n        \"\"\"Return indent width from style data.\n\n        Args:\n            metric: The pixel metric being queried.\n            opt: Optional style option.\n            widget: The target widget.\n\n        Returns:\n            The indent size in pixels.\n        \"\"\"\n        if metric == QStyle.PixelMetric.PM_TreeViewIndentation:\n            variant = getattr(widget, \"_variant_str\", \"default\")\n            style = self.model.get_style(\"QTreeView\", variant)\n            return int(style.get(\"indent\", 20))\n        return 0\n\n    def _draw_cell_border(\n        self,\n        painter: QPainter,\n        rect: QRect,\n        style: dict,\n    ) -&gt; None:\n        \"\"\"Draw top and bottom border lines for an AYTableView cell.\"\"\"\n        painter.setPen(\n            QPen(\n                QColor(style.get(\"border-color\")), style.get(\"border-width\", 1)\n            )\n        )\n        painter.setBrush(Qt.BrushStyle.NoBrush)\n        painter.drawLines(\n            [\n                rect.topLeft(),\n                rect.topRight(),\n                rect.bottomLeft(),\n                rect.bottomRight(),\n            ]\n        )\n\n    def _resolve_tree_view(self, widget: QWidget | None) -&gt; QWidget | None:\n        \"\"\"Resolve widget to the actual QTreeView/AYTableView.\"\"\"\n        if widget is not None and not isinstance(widget, QTreeView):\n            return widget.parent() or widget\n        return widget\n\n    def _paint_cell_background(\n        self,\n        painter: QPainter,\n        rect: QRect,\n        style: dict,\n        is_table: bool,\n        is_base_state: bool = False,\n    ) -&gt; None:\n        \"\"\"Paint background fill and optional cell borders.\n\n        Args:\n            painter: The QPainter to draw on.\n            rect: The rectangle to fill.\n            style: The style data dictionary.\n            is_table: Whether this is an AYTableView cell.\n            is_base_state: If True and is_table, use 'background-color-item'.\n        \"\"\"\n        painter.save()\n        if is_table and is_base_state:\n            bg_key = \"background-color-item\"\n        else:\n            bg_key = \"background-color\"\n        painter.fillRect(rect, QColor(style.get(bg_key, \"transparent\")))\n        if is_table:\n            self._draw_cell_border(painter, rect, style)\n        painter.restore()\n\n    def _paint_icon(\n        self,\n        painter: QPainter,\n        rect: QRect,\n        icon,\n        icon_size: int | None,\n    ) -&gt; None:\n        \"\"\"Paint a cached icon, optionally resizing and repositioning it.\"\"\"\n        draw_rect = QRect(rect)\n        if icon_size is not None:\n            center = rect.center()\n            draw_rect.setSize(QSize(icon_size, icon_size))\n            draw_rect.moveTo(\n                rect.right() - icon_size, center.y() - icon_size // 2\n            )\n        icon.paint(painter, draw_rect)\n\n    def _paint_fallback_arrow(\n        self,\n        painter: QPainter,\n        rect: QRect,\n        color: QColor,\n        is_open: bool,\n    ) -&gt; None:\n        \"\"\"Paint a geometric triangle arrow when no icon is configured.\"\"\"\n        painter.setRenderHint(QPainter.RenderHint.Antialiasing, True)\n        painter.setPen(Qt.PenStyle.NoPen)\n        painter.setBrush(QBrush(color))\n\n        cx, cy = rect.center().x(), rect.center().y()\n        size = max(4, min(rect.width(), rect.height()) // 3)\n\n        path = QPainterPath()\n        if is_open:\n            path.moveTo(cx - size, cy - size // 2)\n            path.lineTo(cx + size, cy - size // 2)\n            path.lineTo(cx, cy + size // 2)\n        else:\n            path.moveTo(cx - size // 2, cy - size)\n            path.lineTo(cx - size // 2, cy + size)\n            path.lineTo(cx + size // 2, cy)\n        path.closeSubpath()\n        painter.drawPath(path)\n\n    def draw_branch_indicator(\n        self,\n        option: QStyleOption,\n        painter: QPainter,\n        widget: QWidget | None = None,\n    ) -&gt; None:\n        \"\"\"Draw expand / collapse arrows for tree branch items.\n\n        Args:\n            option: The primitive element style option.\n            painter: The QPainter to draw on.\n            widget: The QTreeView widget (may be the viewport).\n        \"\"\"\n        has_children = bool(option.state &amp; QStyle.StateFlag.State_Children)\n        tv = self._resolve_tree_view(widget)\n        is_table = type(tv).__name__ == \"AYTableView\"\n        is_selected = bool(option.state &amp; QStyle.StateFlag.State_Selected)\n        is_hovered = bool(option.state &amp; QStyle.StateFlag.State_MouseOver)\n        variant = getattr(tv, \"_variant_str\", \"default\")\n\n        state_name = (\n            \"selected\" if is_selected else \"hover\" if is_hovered else \"base\"\n        )\n\n        widget_class = \"AYTableView\" if is_table else \"QTreeView\"\n        t_style = self.model.get_style(\n            widget_class, variant=variant, state=state_name\n        )\n\n        # Items without children only need background/border painting\n        if not has_children:\n            self._paint_cell_background(\n                painter,\n                option.rect,\n                t_style,\n                is_table,\n                is_base_state=(state_name == \"base\"),\n            )\n            return\n\n        is_open = bool(option.state &amp; QStyle.StateFlag.State_Open)\n        color = QColor(t_style.get(\"branch-indicator-color\", \"#8b9198\"))\n        icon_name = t_style.get(\n            \"expanded-icon-name\" if is_open else \"expand-icon-name\"\n        )\n\n        # Paint background for items with children\n        self._paint_cell_background(painter, option.rect, t_style, is_table)\n\n        if icon_name:\n            key = f\"{icon_name}-{color.name()}\"\n            if key not in self._icon_cache:\n                self._icon_cache[key] = get_icon(icon_name, color=color)\n            icon_size = t_style.get(\"expand-icon-size\")\n            self._paint_icon(\n                painter, option.rect, self._icon_cache[key], icon_size\n            )\n        else:\n            self._paint_fallback_arrow(painter, option.rect, color, is_open)\n\n    def draw_scrollbar_corner(\n        self,\n        option: QStyleOption,\n        painter: QPainter,\n        widget: QWidget | None = None,\n    ) -&gt; None:\n        style = self.model.get_style(\"QScrollArea\", variant=\"default\")\n        style.set_context(widget)\n        painter.save()\n        # Draw corner background\n        bg = style.get(\"background-color\", \"transparent\")\n        painter.fillRect(option.rect, QColor(bg))\n\n        painter.restore()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/index.html#client.ayon_core.ui.drawers.TreeViewDrawer.draw_branch_indicator","title":"<code>draw_branch_indicator(option, painter, widget=None)</code>","text":"<p>Draw expand / collapse arrows for tree branch items.</p> <p>Parameters:</p> Name Type Description Default <code>option</code> <code>QStyleOption</code> <p>The primitive element style option.</p> required <code>painter</code> <code>QPainter</code> <p>The QPainter to draw on.</p> required <code>widget</code> <code>QWidget | None</code> <p>The QTreeView widget (may be the viewport).</p> <code>None</code> Source code in <code>client/ayon_core/ui/drawers/tree_view.py</code> <pre><code>def draw_branch_indicator(\n    self,\n    option: QStyleOption,\n    painter: QPainter,\n    widget: QWidget | None = None,\n) -&gt; None:\n    \"\"\"Draw expand / collapse arrows for tree branch items.\n\n    Args:\n        option: The primitive element style option.\n        painter: The QPainter to draw on.\n        widget: The QTreeView widget (may be the viewport).\n    \"\"\"\n    has_children = bool(option.state &amp; QStyle.StateFlag.State_Children)\n    tv = self._resolve_tree_view(widget)\n    is_table = type(tv).__name__ == \"AYTableView\"\n    is_selected = bool(option.state &amp; QStyle.StateFlag.State_Selected)\n    is_hovered = bool(option.state &amp; QStyle.StateFlag.State_MouseOver)\n    variant = getattr(tv, \"_variant_str\", \"default\")\n\n    state_name = (\n        \"selected\" if is_selected else \"hover\" if is_hovered else \"base\"\n    )\n\n    widget_class = \"AYTableView\" if is_table else \"QTreeView\"\n    t_style = self.model.get_style(\n        widget_class, variant=variant, state=state_name\n    )\n\n    # Items without children only need background/border painting\n    if not has_children:\n        self._paint_cell_background(\n            painter,\n            option.rect,\n            t_style,\n            is_table,\n            is_base_state=(state_name == \"base\"),\n        )\n        return\n\n    is_open = bool(option.state &amp; QStyle.StateFlag.State_Open)\n    color = QColor(t_style.get(\"branch-indicator-color\", \"#8b9198\"))\n    icon_name = t_style.get(\n        \"expanded-icon-name\" if is_open else \"expand-icon-name\"\n    )\n\n    # Paint background for items with children\n    self._paint_cell_background(painter, option.rect, t_style, is_table)\n\n    if icon_name:\n        key = f\"{icon_name}-{color.name()}\"\n        if key not in self._icon_cache:\n            self._icon_cache[key] = get_icon(icon_name, color=color)\n        icon_size = t_style.get(\"expand-icon-size\")\n        self._paint_icon(\n            painter, option.rect, self._icon_cache[key], icon_size\n        )\n    else:\n        self._paint_fallback_arrow(painter, option.rect, color, is_open)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/index.html#client.ayon_core.ui.drawers.TreeViewDrawer.get_metric","title":"<code>get_metric(metric, opt=None, widget=None)</code>","text":"<p>Return indent width from style data.</p> <p>Parameters:</p> Name Type Description Default <code>metric</code> <code>PixelMetric</code> <p>The pixel metric being queried.</p> required <code>opt</code> <code>QStyleOption | None</code> <p>Optional style option.</p> <code>None</code> <code>widget</code> <code>QWidget | None</code> <p>The target widget.</p> <code>None</code> <p>Returns:</p> Type Description <code>int</code> <p>The indent size in pixels.</p> Source code in <code>client/ayon_core/ui/drawers/tree_view.py</code> <pre><code>def get_metric(\n    self,\n    metric: QStyle.PixelMetric,\n    opt: QStyleOption | None = None,\n    widget: QWidget | None = None,\n) -&gt; int:\n    \"\"\"Return indent width from style data.\n\n    Args:\n        metric: The pixel metric being queried.\n        opt: Optional style option.\n        widget: The target widget.\n\n    Returns:\n        The indent size in pixels.\n    \"\"\"\n    if metric == QStyle.PixelMetric.PM_TreeViewIndentation:\n        variant = getattr(widget, \"_variant_str\", \"default\")\n        style = self.model.get_style(\"QTreeView\", variant)\n        return int(style.get(\"indent\", 20))\n    return 0\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/index.html#client.ayon_core.ui.drawers.TreeViewDrawer.register_drawers","title":"<code>register_drawers()</code>","text":"<p>Register drawing functions for QTreeView primitives.</p> Source code in <code>client/ayon_core/ui/drawers/tree_view.py</code> <pre><code>def register_drawers(self) -&gt; dict:\n    \"\"\"Register drawing functions for QTreeView primitives.\"\"\"\n    return {\n        enum_to_str(\n            QStyle.PrimitiveElement,\n            QStyle.PrimitiveElement.PE_IndicatorBranch,\n            \"QTreeView\",\n        ): self.draw_branch_indicator,\n        enum_to_str(\n            QStyle.PrimitiveElement,\n            QStyle.PrimitiveElement.PE_PanelScrollAreaCorner,\n            \"QTreeView\",\n        ): self.draw_scrollbar_corner,\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/index.html#client.ayon_core.ui.drawers.TreeViewDrawer.register_metrics","title":"<code>register_metrics()</code>","text":"<p>Register pixel metric functions for QTreeView.</p> Source code in <code>client/ayon_core/ui/drawers/tree_view.py</code> <pre><code>def register_metrics(self) -&gt; dict:\n    \"\"\"Register pixel metric functions for QTreeView.\"\"\"\n    return {\n        enum_to_str(\n            QStyle.PixelMetric,\n            QStyle.PixelMetric.PM_TreeViewIndentation,\n            \"QTreeView\",\n        ): self.get_metric,\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/index.html#client.ayon_core.ui.drawers.do_nothing","title":"<code>do_nothing(*args, **kwargs)</code>","text":"<p>No-op stub used to suppress default Qt drawing for certain elements.</p> Source code in <code>client/ayon_core/ui/drawers/_utils.py</code> <pre><code>def do_nothing(*args, **kwargs):\n    \"\"\"No-op stub used to suppress default Qt drawing for certain elements.\"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/index.html#client.ayon_core.ui.drawers.enum_to_str","title":"<code>enum_to_str(enum, enum_value, widget)</code>","text":"<p>Convert enum value to string representation.</p> Source code in <code>client/ayon_core/ui/drawers/_utils.py</code> <pre><code>def enum_to_str(enum, enum_value: int, widget: str) -&gt; str:\n    \"\"\"Convert enum value to string representation.\"\"\"\n    cachekey = f\"{enum.__name__}_{enum_value}_{widget}\"\n    if not hasattr(enum_to_str, \"_cache\"):\n        enum_to_str._cache = {}  # type: ignore\n    value: str | None = enum_to_str._cache.get(cachekey)\n    if value is not None:\n        return value\n\n    if hasattr(enum, \"valueToKey\"):\n        value = enum.valueToKey(enum_value)\n    else:\n        meta_object = QStyle.staticMetaObject\n        enum_index = meta_object.indexOfEnumerator(enum.__name__)\n        meta_enum = meta_object.enumerator(enum_index)\n        value = f\"{meta_enum.valueToKey(enum_value)}-{widget}\"\n\n    enum_to_str._cache[cachekey] = value\n\n    return value\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/index.html#client.ayon_core.ui.drawers.style_font","title":"<code>style_font(style, w)</code>","text":"<p>Create a QFont from a style dictionary.</p> <p>Parameters:</p> Name Type Description Default <code>style</code> <code>dict</code> <p>A dict with font-family, font-size, font-weight keys.</p> required <code>w</code> <code>QWidget | None</code> <p>Optional widget (unused, kept for API consistency).</p> required <p>Returns:</p> Type Description <code>QFont</code> <p>Configured QFont instance.</p> Source code in <code>client/ayon_core/ui/drawers/_utils.py</code> <pre><code>def style_font(style: dict, w: QWidget | None) -&gt; QFont:\n    \"\"\"Create a QFont from a style dictionary.\n\n    Args:\n        style: A dict with font-family, font-size, font-weight keys.\n        w: Optional widget (unused, kept for API consistency).\n\n    Returns:\n        Configured QFont instance.\n\n    \"\"\"\n    font = QFont()\n    font.setHintingPreference(QFont.HintingPreference.PreferNoHinting)\n    font.setFamily(style[\"font-family\"])\n    os_name = os.environ.get(\"AYON_CORE_UI_FONT_OS\")\n    if not os_name:\n        os_name = platform.system()\n    pt_size = style.get(f\"font-size-{os_name.lower()}\", style[\"font-size\"])\n    font.setPointSizeF(pt_size)\n    font.setWeight(QFont.Weight(style[\"font-weight\"]))\n    return font\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/_utils.html","title":"_utils","text":"<p>Shared utilities used by AYONStyle drawer modules.</p>"},{"location":"autoapi/client/ayon_core/ui/drawers/_utils.html#client.ayon_core.ui.drawers._utils.do_nothing","title":"<code>do_nothing(*args, **kwargs)</code>","text":"<p>No-op stub used to suppress default Qt drawing for certain elements.</p> Source code in <code>client/ayon_core/ui/drawers/_utils.py</code> <pre><code>def do_nothing(*args, **kwargs):\n    \"\"\"No-op stub used to suppress default Qt drawing for certain elements.\"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/_utils.html#client.ayon_core.ui.drawers._utils.enum_to_str","title":"<code>enum_to_str(enum, enum_value, widget)</code>","text":"<p>Convert enum value to string representation.</p> Source code in <code>client/ayon_core/ui/drawers/_utils.py</code> <pre><code>def enum_to_str(enum, enum_value: int, widget: str) -&gt; str:\n    \"\"\"Convert enum value to string representation.\"\"\"\n    cachekey = f\"{enum.__name__}_{enum_value}_{widget}\"\n    if not hasattr(enum_to_str, \"_cache\"):\n        enum_to_str._cache = {}  # type: ignore\n    value: str | None = enum_to_str._cache.get(cachekey)\n    if value is not None:\n        return value\n\n    if hasattr(enum, \"valueToKey\"):\n        value = enum.valueToKey(enum_value)\n    else:\n        meta_object = QStyle.staticMetaObject\n        enum_index = meta_object.indexOfEnumerator(enum.__name__)\n        meta_enum = meta_object.enumerator(enum_index)\n        value = f\"{meta_enum.valueToKey(enum_value)}-{widget}\"\n\n    enum_to_str._cache[cachekey] = value\n\n    return value\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/_utils.html#client.ayon_core.ui.drawers._utils.style_font","title":"<code>style_font(style, w)</code>","text":"<p>Create a QFont from a style dictionary.</p> <p>Parameters:</p> Name Type Description Default <code>style</code> <code>dict</code> <p>A dict with font-family, font-size, font-weight keys.</p> required <code>w</code> <code>QWidget | None</code> <p>Optional widget (unused, kept for API consistency).</p> required <p>Returns:</p> Type Description <code>QFont</code> <p>Configured QFont instance.</p> Source code in <code>client/ayon_core/ui/drawers/_utils.py</code> <pre><code>def style_font(style: dict, w: QWidget | None) -&gt; QFont:\n    \"\"\"Create a QFont from a style dictionary.\n\n    Args:\n        style: A dict with font-family, font-size, font-weight keys.\n        w: Optional widget (unused, kept for API consistency).\n\n    Returns:\n        Configured QFont instance.\n\n    \"\"\"\n    font = QFont()\n    font.setHintingPreference(QFont.HintingPreference.PreferNoHinting)\n    font.setFamily(style[\"font-family\"])\n    os_name = os.environ.get(\"AYON_CORE_UI_FONT_OS\")\n    if not os_name:\n        os_name = platform.system()\n    pt_size = style.get(f\"font-size-{os_name.lower()}\", style[\"font-size\"])\n    font.setPointSizeF(pt_size)\n    font.setWeight(QFont.Weight(style[\"font-weight\"]))\n    return font\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/button.html","title":"button","text":"<p>ButtonDrawer: custom painting for QPushButton.</p>"},{"location":"autoapi/client/ayon_core/ui/drawers/button.html#client.ayon_core.ui.drawers.button.ButtonDrawer","title":"<code>ButtonDrawer</code>","text":"Source code in <code>client/ayon_core/ui/drawers/button.py</code> <pre><code>class ButtonDrawer:\n    def __init__(self, style_inst: AYONStyle) -&gt; None:\n        self.style_inst = style_inst\n        self.model = style_inst.model\n\n    @property\n    def _super(self):\n        \"\"\"Return proxy for calling QCommonStyle methods on style_inst.\"\"\"\n        from ..style import AYONStyle as _AYONStyle\n\n        return super(_AYONStyle, self.style_inst)\n\n    @property\n    def base_class(self):\n        return {\"QPushButton\": QPushButton}\n\n    def register_drawers(self):\n        return {\n            enum_to_str(\n                QStyle.ControlElement,\n                QStyle.ControlElement.CE_PushButton,\n                \"QPushButton\",\n            ): [\n                partial(\n                    self.style_inst.drawControl,\n                    QStyle.ControlElement.CE_PushButtonBevel,\n                ),\n                partial(\n                    self.style_inst.drawControl,\n                    QStyle.ControlElement.CE_PushButtonLabel,\n                ),\n            ],\n            enum_to_str(\n                QStyle.ControlElement,\n                QStyle.ControlElement.CE_PushButtonBevel,\n                \"QPushButton\",\n            ): self.draw_push_button_bevel,\n            enum_to_str(\n                QStyle.ControlElement,\n                QStyle.ControlElement.CE_PushButtonLabel,\n                \"QPushButton\",\n            ): self.draw_push_button_label,\n        }\n\n    def register_sizers(self):\n        return {\n            enum_to_str(\n                QStyle.ContentsType,\n                QStyle.ContentsType.CT_PushButton,\n                \"QPushButton\",\n            ): self.calculate_push_button_size,\n            enum_to_str(\n                QStyle.SubElement,\n                QStyle.SubElement.SE_PushButtonContents,\n                \"QPushButton\",\n            ): self.sub_element_rect,\n            enum_to_str(\n                QStyle.SubElement,\n                QStyle.SubElement.SE_PushButtonFocusRect,\n                \"QPushButton\",\n            ): self.sub_element_rect,\n        }\n\n    def register_metrics(self):\n        return {\n            enum_to_str(\n                QStyle.PixelMetric,\n                QStyle.PixelMetric.PM_ButtonMargin,\n                \"QPushButton\",\n            ): self.get_metric,\n            enum_to_str(\n                QStyle.PixelMetric,\n                QStyle.PixelMetric.PM_DefaultFrameWidth,\n                \"QPushButton\",\n            ): self.get_metric,\n            enum_to_str(\n                QStyle.PixelMetric,\n                QStyle.PixelMetric.PM_ButtonDefaultIndicator,\n                \"QPushButton\",\n            ): self.get_metric,\n            enum_to_str(\n                QStyle.PixelMetric,\n                QStyle.PixelMetric.PM_FocusFrameVMargin,\n                \"QPushButton\",\n            ): self.get_metric,\n            enum_to_str(\n                QStyle.PixelMetric,\n                QStyle.PixelMetric.PM_FocusFrameHMargin,\n                \"QPushButton\",\n            ): self.get_metric,\n        }\n\n    def get_metric(\n        self,\n        metric: QStyle.PixelMetric,\n        opt: QStyleOption | None = None,\n        widget: QWidget | None = None,\n    ):\n        if metric == QStyle.PixelMetric.PM_ButtonMargin:\n            return 6\n        elif metric == QStyle.PixelMetric.PM_DefaultFrameWidth:\n            return 0\n        elif metric == QStyle.PixelMetric.PM_ButtonDefaultIndicator:\n            return 0\n        elif metric == QStyle.PixelMetric.PM_FocusFrameVMargin:\n            return 2\n        elif metric == QStyle.PixelMetric.PM_FocusFrameHMargin:\n            return 2\n\n    def get_button_variant(self, widget: QWidget) -&gt; str:\n        \"\"\"Extract button variant from widget properties.\"\"\"\n        if widget is None:\n            return \"surface\"\n        return getattr(widget, \"_variant_str\", \"surface\")\n\n    def get_button_has_icon(self, widget: QWidget) -&gt; bool:\n        \"\"\"Check if button has an icon.\"\"\"\n        if widget is None:\n            return False\n\n        # Method 1: Try has_icon property\n        if hasattr(widget, \"has_icon\"):\n            return widget.has_icon  # type: ignore\n\n        # Method 2: Try Qt property\n        has_icon_prop = widget.property(\"has_icon\")\n        if has_icon_prop is not None:\n            return bool(has_icon_prop)\n\n        # Method 3: Check the actual icon\n        return bool(widget.icon() and not widget.icon().isNull())  # type: ignore\n\n    def get_button_style(\n        self, widget: QWidget, state: QStyle.StateFlag\n    ) -&gt; tuple[dict, str]:\n        \"\"\"Get the appropriate style dictionary for the widget's variant and\n        state.\"\"\"\n        variant = self.get_button_variant(widget)\n\n        wstate = \"base\"\n        if not (state &amp; QStyle.StateFlag.State_Enabled):\n            wstate = \"disabled\"\n        elif state &amp; QStyle.StateFlag.State_Sunken:\n            wstate = \"pressed\"\n        elif (state &amp; QStyle.StateFlag.State_MouseOver and not (\n            state &amp; QStyle.StateFlag.State_On) or widget.underMouse()\n        ):\n            wstate = \"hover\"\n        elif state &amp; QStyle.StateFlag.State_On:\n            wstate = \"checked\"\n\n        style = self.model.get_style(\"QPushButton\", variant, wstate)\n        style.set_context(widget)\n\n        return style, wstate\n\n    def draw_push_button_bevel(\n        self,\n        option: QStyleOption,\n        painter: QPainter,\n        widget: QWidget | None,\n    ) -&gt; None:\n        \"\"\"Draw the button background and frame with hover detection.\"\"\"\n        if not isinstance(option, QStyleOptionButton) or widget is None:\n            return\n\n        style, _ = self.get_button_style(widget, option.state)\n        rect = option.rect\n\n        painter.save()\n        painter.setRenderHint(QPainter.RenderHint.Antialiasing)\n\n        # Draw button background with hover awareness\n        bg_color = style[\"background-color\"]\n        painter.setOpacity(style.get(\"opacity\", 1.0))\n\n        painter.setBrush(QBrush(bg_color))\n        painter.setPen(Qt.PenStyle.NoPen)\n        border_radius = style.get(\"border-radius\", 0)\n\n        draw_icon_as_background = style.get(\"icon-as-background\", False)\n        clip_icon_to_radius = style.get(\"clip-icon-to-radius\", False)\n\n        if draw_icon_as_background:\n            # draw the icon clipped by the same rounded rect\n            painter.save()\n            if clip_icon_to_radius:\n                clip_path = QPainterPath()\n                clip_path.addRoundedRect(rect, border_radius, border_radius)\n                painter.setClipPath(clip_path)\n\n            mode = QtGui.QIcon.Mode.Normal\n            painter.drawRoundedRect(rect, border_radius, border_radius)\n            option.icon.paint(\n                painter,\n                rect,\n                Qt.AlignmentFlag.AlignCenter,\n                mode,\n            )\n\n            if clip_icon_to_radius:\n                painter.setClipping(False)\n\n            pen = QPen(QColor(style.get(\"border-color\")))\n            pen.setWidth(int(style.get(\"border-width\", 0)))\n            painter.setPen(pen)\n            painter.setBrush(Qt.BrushStyle.NoBrush)\n            painter.drawRoundedRect(rect, border_radius, border_radius)\n            painter.restore()\n        else:\n            painter.drawRoundedRect(rect, border_radius, border_radius)\n\n        # Draw focus outline if needed\n        if (\n            option.state &amp; QStyle.StateFlag.State_HasFocus\n            and option.state  # type: ignore\n            &amp; QStyle.StateFlag.State_KeyboardFocusChange\n        ):\n            focus_color = style[\"focus-outline-color\"]\n            pen = QPen(\n                QColor(focus_color), style.get(\"focus-outline-width\", 0)\n            )\n            painter.setPen(pen)\n            painter.setBrush(Qt.BrushStyle.NoBrush)\n            focus_rect = rect.adjusted(1, 1, -1, -1)\n            painter.drawRoundedRect(\n                focus_rect, border_radius + 1, border_radius + 1\n            )\n\n        painter.restore()\n\n    def draw_push_button_label(\n        self,\n        option: QStyleOption,\n        painter: QPainter,\n        widget: QWidget | None,\n    ) -&gt; None:\n        \"\"\"Draw the button text and icon.\"\"\"\n        if not isinstance(option, QStyleOptionButton) or widget is None:\n            return\n\n        style, wstate = self.get_button_style(widget, option.state)  # type: ignore\n        variant = self.get_button_variant(widget)\n\n        # Set up text color\n        text_color = self.model.get_widget_color(\n            \"color\",\n            style,\n            widget,\n            widget.palette().color(QPalette.ColorRole.ButtonText),\n        )\n        if not (option.state &amp; QStyle.StateFlag.State_Enabled):  # type: ignore\n            # Apply some opacity to disabled text\n            text_color.setAlpha(int(255 * 0.5))\n\n        painter.save()\n        painter.setPen(text_color)\n\n        # Set up font\n        painter.setFont(widget.font())\n\n        # Get content rectangle\n        content_rect = self.style_inst.subElementRect(\n            QStyle.SubElement.SE_PushButtonContents, option, widget\n        )\n\n        # Optional per-widget alignment override (None \u2192 default centered\n        # layout)\n        label_alignment = getattr(widget, \"_label_alignment\", None)\n\n        if not option.icon:  # type: ignore\n            # Text only\n            if option.text and not style.get(\"ignore-text\", False):  # type: ignore\n                _text_align = (\n                    (label_alignment &amp; Qt.AlignmentFlag.AlignHorizontal_Mask)\n                    | Qt.AlignmentFlag.AlignVCenter\n                    if label_alignment is not None\n                    else Qt.AlignmentFlag.AlignCenter\n                )\n                painter.drawText(\n                    content_rect,\n                    _text_align,\n                    option.text,  # type: ignore\n                )\n            painter.restore()\n            return\n\n        # Draw icon if present\n        if option.text and not style.get(\"ignore-text\", False):  # type: ignore\n            icon_size = option.iconSize  # type: ignore\n            icon_w = icon_size.width()\n            icon_h = icon_size.height()\n            _gap = 4\n\n            # Draw icon with text color inheritance\n            mode = QtGui.QIcon.Mode.Normal\n            if not (\n                option.state &amp; QStyle.StateFlag.State_Enabled  # type: ignore\n            ):\n                mode = QtGui.QIcon.Mode.Disabled\n            elif option.state &amp; QStyle.StateFlag.State_Sunken:  # type: ignore\n                mode = QtGui.QIcon.Mode.Active\n\n            if label_alignment is not None:\n                # Group layout: icon + text move together as a unit\n                h_align = (\n                    label_alignment &amp; Qt.AlignmentFlag.AlignHorizontal_Mask\n                )\n                text_w = painter.fontMetrics().horizontalAdvance(\n                    option.text  # type: ignore\n                )\n                group_w = icon_w + _gap + text_w\n                if h_align == Qt.AlignmentFlag.AlignLeft:\n                    group_x = content_rect.left()\n                elif h_align == Qt.AlignmentFlag.AlignRight:\n                    group_x = content_rect.right() - group_w\n                else:\n                    group_x = (\n                        content_rect.left()\n                        + (content_rect.width() - group_w) // 2\n                    )\n                icon_rect = QRect(\n                    group_x,\n                    content_rect.center().y() - icon_h // 2,\n                    icon_w,\n                    icon_h,\n                )\n                text_rect = QRect(\n                    icon_rect.right() + _gap,\n                    content_rect.top(),\n                    text_w,\n                    content_rect.height(),\n                )\n                option.icon.paint(  # type: ignore\n                    painter,\n                    icon_rect,\n                    Qt.AlignmentFlag.AlignCenter,\n                    mode,\n                )\n                painter.drawText(\n                    text_rect,\n                    Qt.AlignmentFlag.AlignLeft\n                    | Qt.AlignmentFlag.AlignVCenter,\n                    option.text,  # type: ignore\n                )\n            else:\n                # Icon + text: place icon on the left (default centered)\n                icon_rect = QRect(content_rect)\n                icon_rect.setSize(icon_size)\n                icon_rect.moveCenter(\n                    QtCore.QPoint(\n                        content_rect.left() + style[\"icon-padding\"][0],\n                        content_rect.center().y(),\n                    )\n                )\n                option.icon.paint(  # type: ignore\n                    painter,\n                    icon_rect,\n                    Qt.AlignmentFlag.AlignCenter,\n                    mode,\n                )\n                # Adjust text rectangle\n                text_rect = QRect(content_rect)\n                text_rect.setLeft(icon_rect.right() + _gap)\n                # Draw text\n                painter.drawText(\n                    text_rect,\n                    Qt.AlignmentFlag.AlignLeft\n                    | Qt.AlignmentFlag.AlignVCenter,\n                    option.text,  # type: ignore\n                )\n        elif variant not in (\"thumbnail\", \"entity-card\"):\n            # Icon only\n            mode = QtGui.QIcon.Mode.Normal\n            if not (\n                option.state &amp; QStyle.StateFlag.State_Enabled  # type: ignore\n            ):\n                mode = QtGui.QIcon.Mode.Disabled\n            elif option.state &amp; QStyle.StateFlag.State_Sunken:  # type: ignore\n                mode = QtGui.QIcon.Mode.Active\n\n            checkable = widget.isCheckable() if widget else False\n\n            icon_state = (\n                (\n                    QtGui.QIcon.State.On\n                    if wstate == \"hover\"\n                    else QtGui.QIcon.State.Off\n                )\n                if not checkable\n                else (\n                    QtGui.QIcon.State.On\n                    if option.state &amp; QStyle.StateFlag.State_On\n                    else QtGui.QIcon.State.Off\n                )\n            )\n\n            _icon_align = (\n                (label_alignment &amp; Qt.AlignmentFlag.AlignHorizontal_Mask)\n                | Qt.AlignmentFlag.AlignVCenter\n                if label_alignment is not None\n                else Qt.AlignmentFlag.AlignCenter\n            )\n            option.icon.paint(  # type: ignore\n                painter,\n                content_rect,\n                _icon_align,\n                mode,\n                icon_state,\n            )\n\n        painter.restore()\n\n    def calculate_push_button_size(\n        self,\n        contents_type: QStyle.ContentsType,\n        option: QStyleOption | None,\n        contents_size: QtCore.QSize,\n        widget: QWidget | None,\n    ) -&gt; QtCore.QSize:\n        \"\"\"Calculate minimum size for push buttons with text, icons,\n        and proper padding.\"\"\"\n\n        if not isinstance(option, QStyleOptionButton):\n            # Fallback to parent if we don't have proper option data\n            if option is not None:\n                return self._super.sizeFromContents(\n                    contents_type,\n                    option,\n                    contents_size,\n                    widget,\n                )\n            else:\n                # Return reasonable default for button if no option\n                return QtCore.QSize(100, 30)\n\n        # Set up font for text measurement\n        style, _ = self.get_button_style(widget, option.state)  # type: ignore\n        font = widget.font() if widget else style_font(style, widget)\n\n        # Create font metrics for accurate text measurement\n        font_metrics = QFontMetrics(font)\n\n        # Determine if button has icon\n        has_icon = (\n            self.get_button_has_icon(widget)\n            if widget\n            else not option.icon.isNull()  # type: ignore\n        )\n        has_icon = not option.icon.isNull()\n\n        # Determine appropriate padding\n        if has_icon and not option.text:  # type: ignore\n            # Icon-only button\n            padding = style[\"icon-padding\"]\n        else:\n            # Text button or icon+text button\n            padding = style[\"text-padding\"]\n\n        # Calculate text dimensions\n        text_width = 0\n        text_height = 0\n        if option.text and not style.get(\"ignore-text\", False):  # type: ignore\n            text_rect = font_metrics.boundingRect(option.text)  # type: ignore\n            text_width = text_rect.width()\n            text_height = text_rect.height()\n\n        # Calculate icon dimensions\n        icon_width = 0\n        icon_height = 0\n        if has_icon:\n            icon_size = option.iconSize  # type: ignore\n            icon_width = icon_size.width()\n            icon_height = icon_size.height()\n\n        # Calculate content dimensions\n        content_width = 0\n        content_height = 0\n\n        if has_icon and option.text:  # type: ignore\n            # Icon + text: icon on left, 4px spacing, then text\n            content_width = icon_width + 4 + text_width\n            content_height = max(icon_height, text_height)\n        elif has_icon:\n            # Icon only\n            content_width = icon_width\n            content_height = icon_height\n        elif option.text:  # type: ignore\n            # Text only\n            content_width = text_width\n            content_height = text_height\n\n        # Add padding (vertical, horizontal)\n        total_width = content_width + (\n            2 * padding[1]\n        )  # horizontal padding on both sides\n        total_height = content_height + (\n            2 * padding[0]\n        )  # vertical padding on top and bottom\n\n        # Ensure minimum button size (reasonable minimums)\n        min_width = 16\n        min_height = 16\n\n        total_width = max(total_width, min_width)\n        total_height = max(total_height, min_height)\n\n        return QtCore.QSize(total_width, total_height)\n\n    def sub_element_rect(\n        self,\n        element: QStyle.SubElement,\n        option: QStyleOption,\n        widget: QWidget,\n    ):\n        if element == QStyle.SubElement.SE_PushButtonContents:\n            style = self.model.get_style(\n                \"QPushButton\", self.get_button_variant(widget)\n            )\n            style.set_context(widget)\n            if option.icon:\n                padding = (\n                    style[\"icon-padding\"]\n                    if not widget.text()  # type: ignore\n                    else style[\"text-padding\"]\n                )\n            else:\n                padding = style[\"text-padding\"]\n\n            return option.rect.adjusted(  # type: ignore\n                padding[1], padding[0], -padding[1], -padding[0]\n            )\n\n        elif element == QStyle.SubElement.SE_PushButtonFocusRect:\n            return option.rect.adjusted(-2, -2, 2, 2)  # type: ignore\n\n        raise ValueError(f\"Nothing returned ! -&gt; {element}\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/button.html#client.ayon_core.ui.drawers.button.ButtonDrawer.calculate_push_button_size","title":"<code>calculate_push_button_size(contents_type, option, contents_size, widget)</code>","text":"<p>Calculate minimum size for push buttons with text, icons, and proper padding.</p> Source code in <code>client/ayon_core/ui/drawers/button.py</code> <pre><code>def calculate_push_button_size(\n    self,\n    contents_type: QStyle.ContentsType,\n    option: QStyleOption | None,\n    contents_size: QtCore.QSize,\n    widget: QWidget | None,\n) -&gt; QtCore.QSize:\n    \"\"\"Calculate minimum size for push buttons with text, icons,\n    and proper padding.\"\"\"\n\n    if not isinstance(option, QStyleOptionButton):\n        # Fallback to parent if we don't have proper option data\n        if option is not None:\n            return self._super.sizeFromContents(\n                contents_type,\n                option,\n                contents_size,\n                widget,\n            )\n        else:\n            # Return reasonable default for button if no option\n            return QtCore.QSize(100, 30)\n\n    # Set up font for text measurement\n    style, _ = self.get_button_style(widget, option.state)  # type: ignore\n    font = widget.font() if widget else style_font(style, widget)\n\n    # Create font metrics for accurate text measurement\n    font_metrics = QFontMetrics(font)\n\n    # Determine if button has icon\n    has_icon = (\n        self.get_button_has_icon(widget)\n        if widget\n        else not option.icon.isNull()  # type: ignore\n    )\n    has_icon = not option.icon.isNull()\n\n    # Determine appropriate padding\n    if has_icon and not option.text:  # type: ignore\n        # Icon-only button\n        padding = style[\"icon-padding\"]\n    else:\n        # Text button or icon+text button\n        padding = style[\"text-padding\"]\n\n    # Calculate text dimensions\n    text_width = 0\n    text_height = 0\n    if option.text and not style.get(\"ignore-text\", False):  # type: ignore\n        text_rect = font_metrics.boundingRect(option.text)  # type: ignore\n        text_width = text_rect.width()\n        text_height = text_rect.height()\n\n    # Calculate icon dimensions\n    icon_width = 0\n    icon_height = 0\n    if has_icon:\n        icon_size = option.iconSize  # type: ignore\n        icon_width = icon_size.width()\n        icon_height = icon_size.height()\n\n    # Calculate content dimensions\n    content_width = 0\n    content_height = 0\n\n    if has_icon and option.text:  # type: ignore\n        # Icon + text: icon on left, 4px spacing, then text\n        content_width = icon_width + 4 + text_width\n        content_height = max(icon_height, text_height)\n    elif has_icon:\n        # Icon only\n        content_width = icon_width\n        content_height = icon_height\n    elif option.text:  # type: ignore\n        # Text only\n        content_width = text_width\n        content_height = text_height\n\n    # Add padding (vertical, horizontal)\n    total_width = content_width + (\n        2 * padding[1]\n    )  # horizontal padding on both sides\n    total_height = content_height + (\n        2 * padding[0]\n    )  # vertical padding on top and bottom\n\n    # Ensure minimum button size (reasonable minimums)\n    min_width = 16\n    min_height = 16\n\n    total_width = max(total_width, min_width)\n    total_height = max(total_height, min_height)\n\n    return QtCore.QSize(total_width, total_height)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/button.html#client.ayon_core.ui.drawers.button.ButtonDrawer.draw_push_button_bevel","title":"<code>draw_push_button_bevel(option, painter, widget)</code>","text":"<p>Draw the button background and frame with hover detection.</p> Source code in <code>client/ayon_core/ui/drawers/button.py</code> <pre><code>def draw_push_button_bevel(\n    self,\n    option: QStyleOption,\n    painter: QPainter,\n    widget: QWidget | None,\n) -&gt; None:\n    \"\"\"Draw the button background and frame with hover detection.\"\"\"\n    if not isinstance(option, QStyleOptionButton) or widget is None:\n        return\n\n    style, _ = self.get_button_style(widget, option.state)\n    rect = option.rect\n\n    painter.save()\n    painter.setRenderHint(QPainter.RenderHint.Antialiasing)\n\n    # Draw button background with hover awareness\n    bg_color = style[\"background-color\"]\n    painter.setOpacity(style.get(\"opacity\", 1.0))\n\n    painter.setBrush(QBrush(bg_color))\n    painter.setPen(Qt.PenStyle.NoPen)\n    border_radius = style.get(\"border-radius\", 0)\n\n    draw_icon_as_background = style.get(\"icon-as-background\", False)\n    clip_icon_to_radius = style.get(\"clip-icon-to-radius\", False)\n\n    if draw_icon_as_background:\n        # draw the icon clipped by the same rounded rect\n        painter.save()\n        if clip_icon_to_radius:\n            clip_path = QPainterPath()\n            clip_path.addRoundedRect(rect, border_radius, border_radius)\n            painter.setClipPath(clip_path)\n\n        mode = QtGui.QIcon.Mode.Normal\n        painter.drawRoundedRect(rect, border_radius, border_radius)\n        option.icon.paint(\n            painter,\n            rect,\n            Qt.AlignmentFlag.AlignCenter,\n            mode,\n        )\n\n        if clip_icon_to_radius:\n            painter.setClipping(False)\n\n        pen = QPen(QColor(style.get(\"border-color\")))\n        pen.setWidth(int(style.get(\"border-width\", 0)))\n        painter.setPen(pen)\n        painter.setBrush(Qt.BrushStyle.NoBrush)\n        painter.drawRoundedRect(rect, border_radius, border_radius)\n        painter.restore()\n    else:\n        painter.drawRoundedRect(rect, border_radius, border_radius)\n\n    # Draw focus outline if needed\n    if (\n        option.state &amp; QStyle.StateFlag.State_HasFocus\n        and option.state  # type: ignore\n        &amp; QStyle.StateFlag.State_KeyboardFocusChange\n    ):\n        focus_color = style[\"focus-outline-color\"]\n        pen = QPen(\n            QColor(focus_color), style.get(\"focus-outline-width\", 0)\n        )\n        painter.setPen(pen)\n        painter.setBrush(Qt.BrushStyle.NoBrush)\n        focus_rect = rect.adjusted(1, 1, -1, -1)\n        painter.drawRoundedRect(\n            focus_rect, border_radius + 1, border_radius + 1\n        )\n\n    painter.restore()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/button.html#client.ayon_core.ui.drawers.button.ButtonDrawer.draw_push_button_label","title":"<code>draw_push_button_label(option, painter, widget)</code>","text":"<p>Draw the button text and icon.</p> Source code in <code>client/ayon_core/ui/drawers/button.py</code> <pre><code>def draw_push_button_label(\n    self,\n    option: QStyleOption,\n    painter: QPainter,\n    widget: QWidget | None,\n) -&gt; None:\n    \"\"\"Draw the button text and icon.\"\"\"\n    if not isinstance(option, QStyleOptionButton) or widget is None:\n        return\n\n    style, wstate = self.get_button_style(widget, option.state)  # type: ignore\n    variant = self.get_button_variant(widget)\n\n    # Set up text color\n    text_color = self.model.get_widget_color(\n        \"color\",\n        style,\n        widget,\n        widget.palette().color(QPalette.ColorRole.ButtonText),\n    )\n    if not (option.state &amp; QStyle.StateFlag.State_Enabled):  # type: ignore\n        # Apply some opacity to disabled text\n        text_color.setAlpha(int(255 * 0.5))\n\n    painter.save()\n    painter.setPen(text_color)\n\n    # Set up font\n    painter.setFont(widget.font())\n\n    # Get content rectangle\n    content_rect = self.style_inst.subElementRect(\n        QStyle.SubElement.SE_PushButtonContents, option, widget\n    )\n\n    # Optional per-widget alignment override (None \u2192 default centered\n    # layout)\n    label_alignment = getattr(widget, \"_label_alignment\", None)\n\n    if not option.icon:  # type: ignore\n        # Text only\n        if option.text and not style.get(\"ignore-text\", False):  # type: ignore\n            _text_align = (\n                (label_alignment &amp; Qt.AlignmentFlag.AlignHorizontal_Mask)\n                | Qt.AlignmentFlag.AlignVCenter\n                if label_alignment is not None\n                else Qt.AlignmentFlag.AlignCenter\n            )\n            painter.drawText(\n                content_rect,\n                _text_align,\n                option.text,  # type: ignore\n            )\n        painter.restore()\n        return\n\n    # Draw icon if present\n    if option.text and not style.get(\"ignore-text\", False):  # type: ignore\n        icon_size = option.iconSize  # type: ignore\n        icon_w = icon_size.width()\n        icon_h = icon_size.height()\n        _gap = 4\n\n        # Draw icon with text color inheritance\n        mode = QtGui.QIcon.Mode.Normal\n        if not (\n            option.state &amp; QStyle.StateFlag.State_Enabled  # type: ignore\n        ):\n            mode = QtGui.QIcon.Mode.Disabled\n        elif option.state &amp; QStyle.StateFlag.State_Sunken:  # type: ignore\n            mode = QtGui.QIcon.Mode.Active\n\n        if label_alignment is not None:\n            # Group layout: icon + text move together as a unit\n            h_align = (\n                label_alignment &amp; Qt.AlignmentFlag.AlignHorizontal_Mask\n            )\n            text_w = painter.fontMetrics().horizontalAdvance(\n                option.text  # type: ignore\n            )\n            group_w = icon_w + _gap + text_w\n            if h_align == Qt.AlignmentFlag.AlignLeft:\n                group_x = content_rect.left()\n            elif h_align == Qt.AlignmentFlag.AlignRight:\n                group_x = content_rect.right() - group_w\n            else:\n                group_x = (\n                    content_rect.left()\n                    + (content_rect.width() - group_w) // 2\n                )\n            icon_rect = QRect(\n                group_x,\n                content_rect.center().y() - icon_h // 2,\n                icon_w,\n                icon_h,\n            )\n            text_rect = QRect(\n                icon_rect.right() + _gap,\n                content_rect.top(),\n                text_w,\n                content_rect.height(),\n            )\n            option.icon.paint(  # type: ignore\n                painter,\n                icon_rect,\n                Qt.AlignmentFlag.AlignCenter,\n                mode,\n            )\n            painter.drawText(\n                text_rect,\n                Qt.AlignmentFlag.AlignLeft\n                | Qt.AlignmentFlag.AlignVCenter,\n                option.text,  # type: ignore\n            )\n        else:\n            # Icon + text: place icon on the left (default centered)\n            icon_rect = QRect(content_rect)\n            icon_rect.setSize(icon_size)\n            icon_rect.moveCenter(\n                QtCore.QPoint(\n                    content_rect.left() + style[\"icon-padding\"][0],\n                    content_rect.center().y(),\n                )\n            )\n            option.icon.paint(  # type: ignore\n                painter,\n                icon_rect,\n                Qt.AlignmentFlag.AlignCenter,\n                mode,\n            )\n            # Adjust text rectangle\n            text_rect = QRect(content_rect)\n            text_rect.setLeft(icon_rect.right() + _gap)\n            # Draw text\n            painter.drawText(\n                text_rect,\n                Qt.AlignmentFlag.AlignLeft\n                | Qt.AlignmentFlag.AlignVCenter,\n                option.text,  # type: ignore\n            )\n    elif variant not in (\"thumbnail\", \"entity-card\"):\n        # Icon only\n        mode = QtGui.QIcon.Mode.Normal\n        if not (\n            option.state &amp; QStyle.StateFlag.State_Enabled  # type: ignore\n        ):\n            mode = QtGui.QIcon.Mode.Disabled\n        elif option.state &amp; QStyle.StateFlag.State_Sunken:  # type: ignore\n            mode = QtGui.QIcon.Mode.Active\n\n        checkable = widget.isCheckable() if widget else False\n\n        icon_state = (\n            (\n                QtGui.QIcon.State.On\n                if wstate == \"hover\"\n                else QtGui.QIcon.State.Off\n            )\n            if not checkable\n            else (\n                QtGui.QIcon.State.On\n                if option.state &amp; QStyle.StateFlag.State_On\n                else QtGui.QIcon.State.Off\n            )\n        )\n\n        _icon_align = (\n            (label_alignment &amp; Qt.AlignmentFlag.AlignHorizontal_Mask)\n            | Qt.AlignmentFlag.AlignVCenter\n            if label_alignment is not None\n            else Qt.AlignmentFlag.AlignCenter\n        )\n        option.icon.paint(  # type: ignore\n            painter,\n            content_rect,\n            _icon_align,\n            mode,\n            icon_state,\n        )\n\n    painter.restore()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/button.html#client.ayon_core.ui.drawers.button.ButtonDrawer.get_button_has_icon","title":"<code>get_button_has_icon(widget)</code>","text":"<p>Check if button has an icon.</p> Source code in <code>client/ayon_core/ui/drawers/button.py</code> <pre><code>def get_button_has_icon(self, widget: QWidget) -&gt; bool:\n    \"\"\"Check if button has an icon.\"\"\"\n    if widget is None:\n        return False\n\n    # Method 1: Try has_icon property\n    if hasattr(widget, \"has_icon\"):\n        return widget.has_icon  # type: ignore\n\n    # Method 2: Try Qt property\n    has_icon_prop = widget.property(\"has_icon\")\n    if has_icon_prop is not None:\n        return bool(has_icon_prop)\n\n    # Method 3: Check the actual icon\n    return bool(widget.icon() and not widget.icon().isNull())  # type: ignore\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/button.html#client.ayon_core.ui.drawers.button.ButtonDrawer.get_button_style","title":"<code>get_button_style(widget, state)</code>","text":"<p>Get the appropriate style dictionary for the widget's variant and state.</p> Source code in <code>client/ayon_core/ui/drawers/button.py</code> <pre><code>def get_button_style(\n    self, widget: QWidget, state: QStyle.StateFlag\n) -&gt; tuple[dict, str]:\n    \"\"\"Get the appropriate style dictionary for the widget's variant and\n    state.\"\"\"\n    variant = self.get_button_variant(widget)\n\n    wstate = \"base\"\n    if not (state &amp; QStyle.StateFlag.State_Enabled):\n        wstate = \"disabled\"\n    elif state &amp; QStyle.StateFlag.State_Sunken:\n        wstate = \"pressed\"\n    elif (state &amp; QStyle.StateFlag.State_MouseOver and not (\n        state &amp; QStyle.StateFlag.State_On) or widget.underMouse()\n    ):\n        wstate = \"hover\"\n    elif state &amp; QStyle.StateFlag.State_On:\n        wstate = \"checked\"\n\n    style = self.model.get_style(\"QPushButton\", variant, wstate)\n    style.set_context(widget)\n\n    return style, wstate\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/button.html#client.ayon_core.ui.drawers.button.ButtonDrawer.get_button_variant","title":"<code>get_button_variant(widget)</code>","text":"<p>Extract button variant from widget properties.</p> Source code in <code>client/ayon_core/ui/drawers/button.py</code> <pre><code>def get_button_variant(self, widget: QWidget) -&gt; str:\n    \"\"\"Extract button variant from widget properties.\"\"\"\n    if widget is None:\n        return \"surface\"\n    return getattr(widget, \"_variant_str\", \"surface\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/checkbox.html","title":"checkbox","text":"<p>CheckboxDrawer: custom painting for QCheckBox (toggle switch).</p>"},{"location":"autoapi/client/ayon_core/ui/drawers/checkbox.html#client.ayon_core.ui.drawers.checkbox.CheckboxDrawer","title":"<code>CheckboxDrawer</code>","text":"Source code in <code>client/ayon_core/ui/drawers/checkbox.py</code> <pre><code>class CheckboxDrawer:\n    def __init__(self, style_inst: AYONStyle) -&gt; None:\n        self.style_inst = style_inst\n        self.model = style_inst.model\n\n    @property\n    def _super(self):\n        \"\"\"Return proxy for calling QCommonStyle methods on style_inst.\"\"\"\n        from ..style import AYONStyle as _AYONStyle\n\n        return super(_AYONStyle, self.style_inst)\n\n    @property\n    def base_class(self):\n        return {\"QCheckBox\": QCheckBox}\n\n    def register_drawers(self):\n        return {\n            enum_to_str(\n                QStyle.ControlElement,\n                QStyle.ControlElement.CE_CheckBox,\n                \"QCheckBox\",\n            ): self.draw_indicator,\n            enum_to_str(\n                QStyle.PrimitiveElement,\n                QStyle.PrimitiveElement.PE_IndicatorCheckBox,\n                \"QCheckBox\",\n            ): self.draw_toggle,\n            enum_to_str(\n                QStyle.PrimitiveElement,\n                QStyle.PrimitiveElement.PE_FrameFocusRect,\n                \"QCheckBox\",\n            ): do_nothing,\n        }\n\n    def register_metrics(self):\n        return {\n            enum_to_str(\n                QStyle.PixelMetric,\n                QStyle.PixelMetric.PM_IndicatorWidth,\n                \"QCheckBox\",\n            ): self.get_metric,\n            enum_to_str(\n                QStyle.PixelMetric,\n                QStyle.PixelMetric.PM_IndicatorHeight,\n                \"QCheckBox\",\n            ): self.get_metric,\n            enum_to_str(\n                QStyle.PixelMetric,\n                QStyle.PixelMetric.PM_CheckBoxLabelSpacing,\n                \"QCheckBox\",\n            ): self.get_metric,\n        }\n\n    def get_metric(\n        self,\n        metric: QStyle.PixelMetric,\n        opt: QStyleOption | None = None,\n        widget: QWidget | None = None,\n    ):\n        variant = getattr(widget, \"_variant_str\", \"default\")\n        style = self.model.get_style(\n            \"QCheckBox\",\n            variant=variant,\n        )\n        style.set_context(widget)\n        metrics_h = widget.fontMetrics().height() if widget else 18\n        metrics_w = metrics_h * 2 if widget else 32\n\n        if metric == QStyle.PixelMetric.PM_IndicatorWidth:\n            # if indicator-width == 0, use 2x the font height.\n            return style.get(\"indicator-width\", metrics_w) or metrics_w\n        elif metric == QStyle.PixelMetric.PM_IndicatorHeight:\n            # if indicator-height == 0, use the font height.\n            return style.get(\"indicator-height\", metrics_h) or metrics_h\n        elif metric == QStyle.PixelMetric.PM_CheckBoxLabelSpacing:\n            return style.get(\"checkbox-label-spacing\", 8)\n        return 0\n\n    def draw_indicator(\n        self,\n        option: QStyleOption,\n        painter: QPainter,\n        widget: QWidget | None,\n    ):\n        variant = getattr(widget, \"_variant_str\", \"default\")\n        state = (\n            \"checked\" if option.state &amp; QStyle.StateFlag.State_On else \"base\"\n        )\n        style = self.model.get_style(\n            \"QCheckBox\",\n            variant=variant,\n            state=state,\n        )\n        style.set_context(widget)\n\n        if style.get(\"background-color\"):\n            painter.save()\n            painter.setBrush(QColor(style[\"background-color\"]))\n            painter.setPen(Qt.PenStyle.NoPen)\n            radius = style.get(\"border-radius\", 0)\n            painter.drawRoundedRect(option.rect, radius, radius)\n            painter.restore()\n\n        if style.get(\"indicator-position\", \"left\") == \"right\":\n            # Manually draw a centred [label  toggle] group so that padding\n            # is equal on both sides, instead of relying on Qt's layout.\n            s = self.style_inst\n            ind_w = s.pixelMetric(\n                QStyle.PixelMetric.PM_IndicatorWidth, option, widget\n            )\n            ind_h = s.pixelMetric(\n                QStyle.PixelMetric.PM_IndicatorHeight, option, widget\n            )\n            spacing = s.pixelMetric(\n                QStyle.PixelMetric.PM_CheckBoxLabelSpacing, option, widget\n            )\n\n            text = getattr(option, \"text\", \"\")\n            fm = option.fontMetrics\n            text_w = fm.horizontalAdvance(text) if text else 0\n            text_h = fm.height()\n\n            total_w = text_w + (spacing + ind_w if text_w else ind_w)\n\n            rect = option.rect\n            cx = rect.center().x()\n            cy = rect.center().y()\n            x = cx - total_w // 2\n\n            painter.save()\n            if text:\n                painter.setPen(\n                    QColor(style[\"color\"])\n                    if style.get(\"color\")\n                    else option.palette.color(QPalette.ColorRole.WindowText)\n                )\n                text_rect = QRect(x, cy - text_h // 2, text_w, text_h)\n                painter.drawText(\n                    text_rect,\n                    Qt.AlignmentFlag.AlignVCenter | Qt.AlignmentFlag.AlignLeft,\n                    text,\n                )\n\n            toggle_opt = QStyleOption(option)\n            toggle_opt.rect = QRect(\n                x + text_w + (spacing if text_w else 0),\n                cy - ind_h // 2,\n                ind_w,\n                ind_h,\n            )\n            self.draw_toggle(toggle_opt, painter, widget)\n            painter.restore()\n            return\n\n        if style.get(\"color\"):\n            option.palette.setColor(\n                QPalette.ColorRole.WindowText, QColor(style[\"color\"])\n            )\n\n        self._super.drawControl(\n            QStyle.ControlElement.CE_CheckBox, option, painter, widget\n        )\n\n    def draw_toggle(\n        self,\n        option: QStyleOption,\n        painter: QPainter,\n        w: QWidget | None = None,\n    ):\n        painter.save()\n        painter.setRenderHint(QPainter.RenderHint.Antialiasing, True)\n\n        # get style data\n        checked = bool(option.state &amp; QStyle.StateFlag.State_On)\n        variant = getattr(w, \"_variant_str\", \"default\")\n        style = self.model.get_style(\n            \"QCheckBox\",\n            variant=variant,\n            state=\"checked\" if checked else \"base\",\n        )\n        style.set_context(w)\n\n        # draw toggle background\n        painter.setBrush(QColor(style[\"indicator-background-color\"]))\n        if style.get(\"indicator-border-width\", 0):\n            pen = QPen(QColor(style[\"indicator-border-color\"]))\n            pen.setWidth(style.get(\"indicator-border-width\", 0))\n            painter.setPen(pen)\n        else:\n            painter.setPen(Qt.PenStyle.NoPen)\n        frame_rect: QRectF = QRectF(option.rect).adjusted(1, 0, -1, 0)\n        radius = frame_rect.height() / 2.0\n        painter.drawRoundedRect(frame_rect, radius, radius)\n\n        # draw toggle knob\n        painter.setBrush(QColor(style[\"indicator-color\"]))\n        offset = frame_rect.height() * 0.125\n        state_rect: QRectF = frame_rect.adjusted(\n            offset, offset, -offset, -offset\n        )\n        state_rect.setWidth(state_rect.height())\n        if checked:\n            state_rect.moveRight(frame_rect.right() - offset)\n        painter.drawEllipse(state_rect)\n\n        painter.restore()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/combobox.html","title":"combobox","text":"<p>ComboBox drawers: ComboBoxDrawer.</p>"},{"location":"autoapi/client/ayon_core/ui/drawers/combobox.html#client.ayon_core.ui.drawers.combobox.ComboBoxDrawer","title":"<code>ComboBoxDrawer</code>","text":"Source code in <code>client/ayon_core/ui/drawers/combobox.py</code> <pre><code>class ComboBoxDrawer:\n    def __init__(self, style_inst: AYONStyle) -&gt; None:\n        self.style_inst = style_inst\n        self.model = style_inst.model\n\n    @property\n    def _super(self):\n        \"\"\"Return proxy for calling QCommonStyle methods on style_inst.\"\"\"\n        from ..style import AYONStyle as _AYONStyle\n\n        return super(_AYONStyle, self.style_inst)\n\n    @property\n    def base_class(self):\n        return {\"QComboBox\": QComboBox}\n\n    def register_drawers(self):\n        return {\n            enum_to_str(\n                QStyle.ControlElement,\n                QStyle.ControlElement.CE_ComboBoxLabel,\n                \"QComboBox\",\n            ): self.draw_label,\n            enum_to_str(\n                QStyle.ComplexControl,\n                QStyle.ComplexControl.CC_ComboBox,\n                \"QComboBox\",\n            ): self.draw_box,\n            enum_to_str(\n                QStyle.PrimitiveElement,\n                QStyle.PrimitiveElement.PE_PanelItemViewItem,\n                \"QFrame\",\n            ): self.draw_panel_item_view_item,\n            enum_to_str(\n                QStyle.PrimitiveElement,\n                QStyle.PrimitiveElement.PE_FrameFocusRect,\n                \"QFrame\",\n            ): do_nothing,\n        }\n\n    def register_sizers(self):\n        return {\n            enum_to_str(\n                QStyle.ContentsType,\n                QStyle.ContentsType.CT_ComboBox,\n                \"QComboBox\",\n            ): self.combobox_size,\n        }\n\n    def get_fg_bg_colors(\n        self,\n        opt: QtWidgets.QStyleOptionComplex,\n        w: QComboBox,\n    ) -&gt; tuple[QColor, QColor]:\n        bg_color = opt.palette.color(\n            QPalette.ColorGroup.Active, QPalette.ColorRole.Base\n        )\n        fg_color = opt.palette.color(\n            QPalette.ColorGroup.Active, QPalette.ColorRole.ButtonText\n        )\n\n        inverted = getattr(w, \"_inverted\", False)\n        current_index = w.currentIndex()\n        if current_index &gt;= 0:\n            item_color = w.itemData(\n                current_index, QtCore.Qt.ItemDataRole.ForegroundRole\n            )\n            if item_color is not None:\n                item_color = item_color.color()\n                fg_color = bg_color if inverted else item_color\n                bg_color = item_color if inverted else bg_color\n\n        return fg_color, bg_color\n\n    def draw_box(\n        self,\n        opt: QtWidgets.QStyleOptionComplex,\n        p: QPainter,\n        w: QComboBox | None = None,\n    ):\n        if not isinstance(w, QComboBox):\n            return\n\n        _style = self.model.get_style(\n            \"QComboBox\", variant=getattr(w, \"_variant_str\", None)\n        )\n        _style.set_context(w)\n        style_bg_color = _style.get(\"background-color\", None)\n        opt.palette.setBrush(\n            QPalette.ColorRole.Base,\n            QColor(style_bg_color)\n            if style_bg_color\n            else self.model.base_palette.base(),\n        )\n        _radius = _style.get(\"border-radius\", 0)\n\n        if not w.isEditable():\n            fg_color, bg_color = self.get_fg_bg_colors(opt, w)\n\n            # Paint background with status color\n            rect = opt.rect\n            p.save()\n            p.setBrush(QBrush(bg_color))\n            p.setPen(QtCore.Qt.PenStyle.NoPen)\n            p.drawRoundedRect(rect, _radius, _radius)\n            p.restore()\n\n            # Draw expand_more arrow if show_chevron is True\n            show_chevron = getattr(w, \"show_chevron\", True)\n            if show_chevron:\n                arrow_rect = self._super.subControlRect(\n                    QStyle.ComplexControl.CC_ComboBox,\n                    opt,\n                    QStyle.SubControl.SC_ComboBoxArrow,\n                    w,\n                )\n                arrow_icon = get_icon(\"expand_more\", fg_color)\n                if arrow_icon and not arrow_rect.isEmpty():\n                    arrow_size = min(arrow_rect.width(), arrow_rect.height())\n                    pixmap = arrow_icon.pixmap(arrow_size, arrow_size)\n                    px = (\n                        arrow_rect.x() + (arrow_rect.width() - arrow_size) // 2\n                    )\n                    py = (\n                        arrow_rect.y()\n                        + (arrow_rect.height() - arrow_size) // 2\n                    )\n                    popup_open = bool(opt.state &amp; QStyle.StateFlag.State_On)\n                    if popup_open:\n                        cx = px + arrow_size / 2\n                        cy = py + arrow_size / 2\n                        p.save()\n                        p.translate(cx, cy)\n                        p.rotate(180)\n                        p.translate(-cx, -cy)\n                        p.drawPixmap(px, py, pixmap)\n                        p.restore()\n                    else:\n                        p.drawPixmap(px, py, pixmap)\n\n            # set pen for text drawing\n            p.setPen(fg_color)\n        else:\n            # editable combobox - IMPLEMENT ME\n            self._super.drawComplexControl(\n                QStyle.ComplexControl.CC_ComboBox, opt, p, w\n            )\n\n    def draw_label(\n        self,\n        opt: QStyleOptionComboBox,\n        p: QPainter,\n        w: QWidget,\n    ):\n        if not isinstance(w, QComboBox):\n            return\n\n        _style = self.model.get_style(\n            \"QComboBox\", variant=getattr(w, \"_variant_str\", None)\n        )\n        _style.set_context(w)\n        icon_padding = _style.get(\"icon-padding\", [4, 4])\n        text_padding = _style.get(\"text-padding\", [1, 1])\n\n        fg_color, bg_color = self.get_fg_bg_colors(opt, w)\n\n        base_cls = self._super\n        edit_rect = base_cls.subControlRect(\n            QStyle.ComplexControl.CC_ComboBox,\n            opt,\n            QStyle.SubControl.SC_ComboBoxEditField,\n            w,\n        )\n        p.save()\n        p.setClipRect(edit_rect)\n        if opt.currentIcon:\n            mode = (\n                QIcon.Mode.Normal\n                if opt.state &amp; QStyle.StateFlag.State_Enabled\n                else QIcon.Mode.Disabled\n            )\n            pixmap = opt.currentIcon.pixmap(opt.iconSize, mode)\n            icon_rect = QRect(edit_rect)\n            icon_rect.setWidth(opt.iconSize.width() + icon_padding[0])\n            icon_rect.setHeight(opt.iconSize.height() + icon_padding[1])\n            icon_rect = QStyle.alignedRect(\n                opt.direction,\n                Qt.AlignmentFlag.AlignLeft | Qt.AlignmentFlag.AlignVCenter,\n                icon_rect.size(),\n                edit_rect,\n            )\n            if opt.editable:\n                p.fillRect(\n                    icon_rect, opt.palette.brush(QPalette.ColorRole.Base)\n                )\n            base_cls.drawItemPixmap(\n                p, icon_rect, Qt.AlignmentFlag.AlignCenter, pixmap\n            )\n            if opt.direction == Qt.LayoutDirection.RightToLeft:\n                edit_rect.translate(-icon_padding[0] - opt.iconSize.width(), 0)\n            else:\n                edit_rect.translate(opt.iconSize.width() + icon_padding[0], 0)\n\n        if opt.currentText and not opt.editable:\n            base_cls.drawItemText(\n                p,\n                edit_rect.adjusted(\n                    text_padding[0],\n                    -text_padding[1],\n                    -text_padding[0],\n                    text_padding[1],\n                ),\n                QStyle.visualAlignment(\n                    opt.direction,\n                    Qt.AlignmentFlag.AlignLeft | Qt.AlignmentFlag.AlignVCenter,\n                ),\n                opt.palette,\n                bool(opt.state &amp; QStyle.StateFlag.State_Enabled),\n                opt.currentText,\n            )\n\n        p.restore()\n\n    def draw_panel_item_view_item(\n        self, option: QStyleOption, painter: QPainter, w: QWidget\n    ):\n        cb = w.model().parent()\n        if cb and getattr(cb, \"_inverted\", False):\n            idx = option.index\n            if idx:\n                fgc = (\n                    w.model().data(idx, Qt.ItemDataRole.ForegroundRole).color()\n                )\n                option.backgroundBrush.setColor(fgc)\n        else:\n            stl = self.model.get_style(\"QComboBox\")\n            stl.set_context(w)\n            option.backgroundBrush.setColor(\n                QColor(stl[\"menu-background-color\"])\n            )\n        self._super.drawPrimitive(  # type: ignore\n            QStyle.PrimitiveElement.PE_PanelItemViewItem, option, painter, w\n        )\n\n    def combobox_size(\n        self,\n        contents_type: QStyle.ContentsType,\n        option: QStyleOption | None,\n        contents_size: QtCore.QSize,\n        widget: QWidget | None,\n    ) -&gt; QtCore.QSize:\n        from qtpy.QtCore import QSize\n\n        if not option or not isinstance(option, QStyleOptionComboBox):\n            return QSize()\n\n        style = self.model.get_style(\"QComboBox\")\n        style.set_context(widget)\n\n        text_width = cb_height = 0\n        if isinstance(widget, QComboBox):\n            for i in range(widget.count()):\n                t_rect = option.fontMetrics.boundingRect(\n                    widget.itemData(i, Qt.ItemDataRole.DisplayRole)\n                )\n                text_width = max(text_width, t_rect.width())\n                cb_height = max(cb_height, t_rect.height())\n\n        text_width += style[\"text-padding\"][0] * 2\n        cb_height += style[\"text-padding\"][1] * 2\n\n        icon_width = 0\n        if option.currentIcon:\n            icon_size = getattr(widget, \"_icon_size\", 0)\n            if icon_size == 0:\n                all_sizes = option.currentIcon.availableSizes()\n                icon_size = max(all_sizes[0].width(), all_sizes[0].height())\n            icon_width = icon_size + style[\"icon-padding\"][0] * 2\n            icon_height = icon_size + style[\"icon-padding\"][1] * 2\n            cb_height = max(cb_height, icon_height)\n            if text_width:\n                icon_width += style[\"text-padding\"][0]\n\n        final_size = QSize(\n            text_width + icon_width,\n            min(getattr(widget, \"_height\", cb_height), cb_height),\n        )\n        return final_size\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/frame.html","title":"frame","text":"<p>FrameDrawer: custom painting for QFrame.</p>"},{"location":"autoapi/client/ayon_core/ui/drawers/item_view.html","title":"item_view","text":"<p>Item view drawers: ItemViewItemDrawer.</p>"},{"location":"autoapi/client/ayon_core/ui/drawers/item_view.html#client.ayon_core.ui.drawers.item_view.ItemViewItemDrawer","title":"<code>ItemViewItemDrawer</code>","text":"<p>Drawer for item view items using QStyledItemDelegate.</p> Source code in <code>client/ayon_core/ui/drawers/item_view.py</code> <pre><code>class ItemViewItemDrawer:\n    \"\"\"Drawer for item view items using QStyledItemDelegate.\"\"\"\n\n    def __init__(self, style_inst: AYONStyle) -&gt; None:\n        self.style_inst = style_inst\n        self.model = style_inst.model\n\n    @property\n    def base_class(self):\n        return {\"QStyledItemDelegate\": QStyledItemDelegate}\n\n    def register_drawers(self):\n        return {\n            enum_to_str(\n                QStyle.ControlElement,\n                QStyle.ControlElement.CE_ItemViewItem,\n                \"QStyledItemDelegate\",\n            ): self.draw_item_view_item,\n        }\n\n    def get_item_view_variant(self, widget: QWidget | None) -&gt; str:\n        \"\"\"Extract item view variant from widget properties.\"\"\"\n        if widget is None:\n            return \"default\"\n        if hasattr(widget, \"itemDelegate\"):\n            delegate = widget.itemDelegate()\n            if hasattr(delegate, \"_variant_str\"):\n                return delegate._variant_str\n        return \"default\"\n\n    def get_item_view_style(\n        self,\n        widget: QWidget | None,\n        option: QStyleOptionViewItem,\n    ) -&gt; tuple[dict, str]:\n        \"\"\"Get the appropriate style dictionary for the widget's variant\n        and state.\n\n        Args:\n            widget: The parent widget containing the item view.\n            option: The style option containing state flags.\n\n        Returns:\n            A tuple of (style dictionary, state string).\n        \"\"\"\n        variant = self.get_item_view_variant(widget)\n\n        wstate = \"base\"\n        is_checked = option.checkState == Qt.CheckState.Checked\n        is_hovered = bool(option.state &amp; QStyle.StateFlag.State_MouseOver)\n\n        if is_checked:\n            wstate = \"checked\"\n        elif is_hovered:\n            wstate = \"hover\"\n\n        style = self.model.get_style(\"QStyledItemDelegate\", variant, wstate)\n        style.set_context(widget)\n\n        return style, wstate\n\n    def draw_item_view_item(\n        self,\n        option: QStyleOption,\n        painter: QPainter,\n        widget: QWidget | None,\n    ) -&gt; None:\n        \"\"\"Paint a filter item with checkbox indicator.\n\n        Hover and checked states are handled independently:\n        - Background color comes from hover state when hovered\n        - Checkbox background and text color come from checked state\n        \"\"\"\n        painter.save()\n        painter.setRenderHint(QPainter.RenderHint.Antialiasing)\n\n        # For QStyleOptionViewItem, we need to check different properties\n        if not isinstance(option, QStyleOptionViewItem):\n            painter.restore()\n            return\n\n        # Determine hover and checked states independently\n        is_checked = option.checkState == Qt.CheckState.Checked\n        is_hovered = bool(option.state &amp; QStyle.StateFlag.State_MouseOver)\n        text = option.text\n\n        # Get variant for style lookups\n        variant = self.get_item_view_variant(widget)\n\n        # Get all necessary styles in a single call\n        styles = self.model.get_styles(\n            \"QStyledItemDelegate\", variant, [\"base\", \"hover\", \"checked\"]\n        )\n        base_style = styles[\"base\"]\n        hover_style = styles[\"hover\"]\n        checked_style = styles[\"checked\"]\n\n        # Constants from base style data\n        checkbox_size = base_style.get(\"checkbox-size\", 16)\n        checkbox_margin = base_style.get(\"checkbox-margin\", 8)\n        text_padding = base_style.get(\"text-padding\", 12)\n        border_radius = base_style.get(\"border-radius\", 2)\n\n        # Background: use hover style if hovered, regardless of checked state\n        if is_hovered:\n            bg_color = QColor(\n                hover_style.get(\n                    \"background-color\",\n                    base_style.get(\"background-color\", \"transparent\"),\n                )\n            )\n        else:\n            bg_color = QColor(\n                base_style.get(\"background-color\", \"transparent\")\n            )\n\n        # Text color: use checked style if checked, else base\n        if is_checked:\n            text_color = QColor(\n                checked_style.get(\"color\", base_style.get(\"color\", \"#8b9198\"))\n            )\n        else:\n            text_color = QColor(base_style.get(\"color\", \"#8b9198\"))\n\n        # Checkbox background: use checked style if checked, else base\n        if is_checked:\n            checkbox_bg_color = QColor(\n                checked_style.get(\n                    \"checkbox-background-color\",\n                    base_style.get(\"checkbox-background-color\", \"#424a57\"),\n                )\n            )\n        else:\n            checkbox_bg_color = QColor(\n                base_style.get(\"checkbox-background-color\", \"#424a57\")\n            )\n\n        # Draw background if hovered\n        if is_hovered:\n            painter.setBrush(QBrush(bg_color))\n            painter.setPen(Qt.PenStyle.NoPen)\n            painter.drawRect(option.rect)\n\n        # Calculate checkbox rect - positioned on right side\n        cb_rect = QRect(\n            option.rect.right() - checkbox_size - checkbox_margin,\n            option.rect.center().y() - checkbox_size // 2,\n            checkbox_size,\n            checkbox_size,\n        )\n\n        # Draw checkbox background\n        painter.setBrush(QBrush(checkbox_bg_color))\n        painter.setPen(Qt.PenStyle.NoPen)\n        painter.drawRoundedRect(cb_rect, border_radius, border_radius)\n\n        # Draw X mark if checked\n        if is_checked:\n            icon = get_icon(\"close\", color=\"#000000\")\n            icon_rect = cb_rect.adjusted(2, 2, -2, -2)\n            icon.paint(painter, icon_rect)\n\n        # Draw text\n        painter.setPen(QPen(text_color))\n        text_rect = option.rect.adjusted(\n            text_padding,\n            0,\n            -(checkbox_size + checkbox_margin * 2),\n            0,\n        )\n        painter.drawText(\n            text_rect,\n            Qt.AlignmentFlag.AlignVCenter | Qt.AlignmentFlag.AlignLeft,\n            text,\n        )\n\n        painter.restore()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/item_view.html#client.ayon_core.ui.drawers.item_view.ItemViewItemDrawer.draw_item_view_item","title":"<code>draw_item_view_item(option, painter, widget)</code>","text":"<p>Paint a filter item with checkbox indicator.</p> <p>Hover and checked states are handled independently: - Background color comes from hover state when hovered - Checkbox background and text color come from checked state</p> Source code in <code>client/ayon_core/ui/drawers/item_view.py</code> <pre><code>def draw_item_view_item(\n    self,\n    option: QStyleOption,\n    painter: QPainter,\n    widget: QWidget | None,\n) -&gt; None:\n    \"\"\"Paint a filter item with checkbox indicator.\n\n    Hover and checked states are handled independently:\n    - Background color comes from hover state when hovered\n    - Checkbox background and text color come from checked state\n    \"\"\"\n    painter.save()\n    painter.setRenderHint(QPainter.RenderHint.Antialiasing)\n\n    # For QStyleOptionViewItem, we need to check different properties\n    if not isinstance(option, QStyleOptionViewItem):\n        painter.restore()\n        return\n\n    # Determine hover and checked states independently\n    is_checked = option.checkState == Qt.CheckState.Checked\n    is_hovered = bool(option.state &amp; QStyle.StateFlag.State_MouseOver)\n    text = option.text\n\n    # Get variant for style lookups\n    variant = self.get_item_view_variant(widget)\n\n    # Get all necessary styles in a single call\n    styles = self.model.get_styles(\n        \"QStyledItemDelegate\", variant, [\"base\", \"hover\", \"checked\"]\n    )\n    base_style = styles[\"base\"]\n    hover_style = styles[\"hover\"]\n    checked_style = styles[\"checked\"]\n\n    # Constants from base style data\n    checkbox_size = base_style.get(\"checkbox-size\", 16)\n    checkbox_margin = base_style.get(\"checkbox-margin\", 8)\n    text_padding = base_style.get(\"text-padding\", 12)\n    border_radius = base_style.get(\"border-radius\", 2)\n\n    # Background: use hover style if hovered, regardless of checked state\n    if is_hovered:\n        bg_color = QColor(\n            hover_style.get(\n                \"background-color\",\n                base_style.get(\"background-color\", \"transparent\"),\n            )\n        )\n    else:\n        bg_color = QColor(\n            base_style.get(\"background-color\", \"transparent\")\n        )\n\n    # Text color: use checked style if checked, else base\n    if is_checked:\n        text_color = QColor(\n            checked_style.get(\"color\", base_style.get(\"color\", \"#8b9198\"))\n        )\n    else:\n        text_color = QColor(base_style.get(\"color\", \"#8b9198\"))\n\n    # Checkbox background: use checked style if checked, else base\n    if is_checked:\n        checkbox_bg_color = QColor(\n            checked_style.get(\n                \"checkbox-background-color\",\n                base_style.get(\"checkbox-background-color\", \"#424a57\"),\n            )\n        )\n    else:\n        checkbox_bg_color = QColor(\n            base_style.get(\"checkbox-background-color\", \"#424a57\")\n        )\n\n    # Draw background if hovered\n    if is_hovered:\n        painter.setBrush(QBrush(bg_color))\n        painter.setPen(Qt.PenStyle.NoPen)\n        painter.drawRect(option.rect)\n\n    # Calculate checkbox rect - positioned on right side\n    cb_rect = QRect(\n        option.rect.right() - checkbox_size - checkbox_margin,\n        option.rect.center().y() - checkbox_size // 2,\n        checkbox_size,\n        checkbox_size,\n    )\n\n    # Draw checkbox background\n    painter.setBrush(QBrush(checkbox_bg_color))\n    painter.setPen(Qt.PenStyle.NoPen)\n    painter.drawRoundedRect(cb_rect, border_radius, border_radius)\n\n    # Draw X mark if checked\n    if is_checked:\n        icon = get_icon(\"close\", color=\"#000000\")\n        icon_rect = cb_rect.adjusted(2, 2, -2, -2)\n        icon.paint(painter, icon_rect)\n\n    # Draw text\n    painter.setPen(QPen(text_color))\n    text_rect = option.rect.adjusted(\n        text_padding,\n        0,\n        -(checkbox_size + checkbox_margin * 2),\n        0,\n    )\n    painter.drawText(\n        text_rect,\n        Qt.AlignmentFlag.AlignVCenter | Qt.AlignmentFlag.AlignLeft,\n        text,\n    )\n\n    painter.restore()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/item_view.html#client.ayon_core.ui.drawers.item_view.ItemViewItemDrawer.get_item_view_style","title":"<code>get_item_view_style(widget, option)</code>","text":"<p>Get the appropriate style dictionary for the widget's variant and state.</p> <p>Parameters:</p> Name Type Description Default <code>widget</code> <code>QWidget | None</code> <p>The parent widget containing the item view.</p> required <code>option</code> <code>QStyleOptionViewItem</code> <p>The style option containing state flags.</p> required <p>Returns:</p> Type Description <code>tuple[dict, str]</code> <p>A tuple of (style dictionary, state string).</p> Source code in <code>client/ayon_core/ui/drawers/item_view.py</code> <pre><code>def get_item_view_style(\n    self,\n    widget: QWidget | None,\n    option: QStyleOptionViewItem,\n) -&gt; tuple[dict, str]:\n    \"\"\"Get the appropriate style dictionary for the widget's variant\n    and state.\n\n    Args:\n        widget: The parent widget containing the item view.\n        option: The style option containing state flags.\n\n    Returns:\n        A tuple of (style dictionary, state string).\n    \"\"\"\n    variant = self.get_item_view_variant(widget)\n\n    wstate = \"base\"\n    is_checked = option.checkState == Qt.CheckState.Checked\n    is_hovered = bool(option.state &amp; QStyle.StateFlag.State_MouseOver)\n\n    if is_checked:\n        wstate = \"checked\"\n    elif is_hovered:\n        wstate = \"hover\"\n\n    style = self.model.get_style(\"QStyledItemDelegate\", variant, wstate)\n    style.set_context(widget)\n\n    return style, wstate\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/item_view.html#client.ayon_core.ui.drawers.item_view.ItemViewItemDrawer.get_item_view_variant","title":"<code>get_item_view_variant(widget)</code>","text":"<p>Extract item view variant from widget properties.</p> Source code in <code>client/ayon_core/ui/drawers/item_view.py</code> <pre><code>def get_item_view_variant(self, widget: QWidget | None) -&gt; str:\n    \"\"\"Extract item view variant from widget properties.\"\"\"\n    if widget is None:\n        return \"default\"\n    if hasattr(widget, \"itemDelegate\"):\n        delegate = widget.itemDelegate()\n        if hasattr(delegate, \"_variant_str\"):\n            return delegate._variant_str\n    return \"default\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/label.html","title":"label","text":"<p>LabelDrawer: suppress default frame painting for QLabel.</p>"},{"location":"autoapi/client/ayon_core/ui/drawers/lineedit.html","title":"lineedit","text":"<p>LineEditDrawer: custom painting for QLineEdit.</p>"},{"location":"autoapi/client/ayon_core/ui/drawers/lineedit.html#client.ayon_core.ui.drawers.lineedit.LineEditDrawer","title":"<code>LineEditDrawer</code>","text":"<p>AYONStyle drawer for QLineEdit.</p> <p>Registers a no-op for PE_PanelLineEdit when the widget is an AYLineEdit instance (which paints itself fully in its own paintEvent), and falls back to the base QCommonStyle implementation for all other QLineEdit widgets.</p> Source code in <code>client/ayon_core/ui/drawers/lineedit.py</code> <pre><code>class LineEditDrawer:\n    \"\"\"AYONStyle drawer for QLineEdit.\n\n    Registers a no-op for PE_PanelLineEdit when the widget is an AYLineEdit\n    instance (which paints itself fully in its own paintEvent), and falls back\n    to the base QCommonStyle implementation for all other QLineEdit widgets.\n    \"\"\"\n\n    def __init__(self, style_inst: AYONStyle) -&gt; None:\n        self.style_inst = style_inst\n\n    @property\n    def _super(self):\n        \"\"\"Return proxy for calling QCommonStyle methods on style_inst.\"\"\"\n        from ..style import AYONStyle as _AYONStyle\n\n        return super(_AYONStyle, self.style_inst)\n\n    @property\n    def base_class(self):\n        return {\"QLineEdit\": QLineEdit}\n\n    def register_drawers(self):\n        return {\n            enum_to_str(\n                QStyle.PrimitiveElement,\n                QStyle.PrimitiveElement.PE_PanelLineEdit,\n                \"QLineEdit\",\n            ): self.draw_panel,\n        }\n\n    def draw_panel(\n        self,\n        option: QStyleOption,\n        painter: QPainter,\n        widget: QWidget | None,\n    ) -&gt; None:\n        # AYLineEdit paints its own background \u2014 skip Qt's default frame.\n        if type(widget).__name__ == \"AYLineEdit\":\n            return\n        self._super.drawPrimitive(\n            QStyle.PrimitiveElement.PE_PanelLineEdit, option, painter, widget\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/menu.html","title":"menu","text":"<p>MenuDrawer: custom painting for QMenu.</p>"},{"location":"autoapi/client/ayon_core/ui/drawers/menu.html#client.ayon_core.ui.drawers.menu.MenuDrawer","title":"<code>MenuDrawer</code>","text":"<p>Drawer for QMenu using native QPainter calls (no QSS).</p> <p>Paints the menu panel/border (PE_PanelMenu/PE_FrameMenu) and each item row (CE_MenuItem).</p> Source code in <code>client/ayon_core/ui/drawers/menu.py</code> <pre><code>class MenuDrawer:\n    \"\"\"Drawer for QMenu using native QPainter calls (no QSS).\n\n    Paints the menu panel/border (PE_PanelMenu/PE_FrameMenu) and each\n    item row (CE_MenuItem).\n    \"\"\"\n\n    _WIDGET_CLS = \"QMenu\"\n\n    def __init__(self, style_inst: AYONStyle) -&gt; None:\n        self.style_inst = style_inst\n        self.model = style_inst.model\n\n    @property\n    def base_class(self) -&gt; dict:\n        return {\"QMenu\": QMenu}\n\n    # ------------------------------------------------------------------\n    # Registration\n    # ------------------------------------------------------------------\n\n    def register_drawers(self) -&gt; dict:\n        return {\n            enum_to_str(\n                QStyle.PrimitiveElement,\n                QStyle.PrimitiveElement.PE_PanelMenu,\n                \"QMenu\",\n            ): self.draw_panel,\n            enum_to_str(\n                QStyle.PrimitiveElement,\n                QStyle.PrimitiveElement.PE_FrameMenu,\n                \"QMenu\",\n            ): self.draw_frame,\n            enum_to_str(\n                QStyle.ControlElement,\n                QStyle.ControlElement.CE_MenuItem,\n                \"QMenu\",\n            ): self.draw_menu_item,\n            enum_to_str(\n                QStyle.ControlElement,\n                QStyle.ControlElement.CE_MenuEmptyArea,\n                \"QMenu\",\n            ): do_nothing,\n        }\n\n    def register_sizers(self) -&gt; dict:\n        return {\n            enum_to_str(\n                QStyle.ContentsType,\n                QStyle.ContentsType.CT_MenuItem,\n                \"QMenu\",\n            ): self.menu_item_size,\n        }\n\n    def register_metrics(self) -&gt; dict:\n        pm = QStyle.PixelMetric\n        metrics_map = {\n            pm.PM_MenuPanelWidth: self.get_metric,\n            pm.PM_MenuHMargin: self.get_metric,\n            pm.PM_MenuVMargin: self.get_metric,\n            pm.PM_SmallIconSize: self.get_metric,\n            pm.PM_MenuButtonIndicator: self.get_metric,\n        }\n        return {enum_to_str(pm, k, \"QMenu\"): v for k, v in metrics_map.items()}\n\n    # ------------------------------------------------------------------\n    # Metrics\n    # ------------------------------------------------------------------\n\n    def _base_style(self, widget: QWidget | None = None):\n        \"\"\"Return the base style dict, context-bound to *widget*.\n\n        This is used for general operations like metrics calculation and\n        panel/frame painting, where the state/variant-specific styles are\n        not relevant.\n        \"\"\"\n        style = self.model.get_style(self._WIDGET_CLS, \"default\", \"base\")\n        style.set_context(widget)\n        return style\n\n    def get_metric(\n        self,\n        metric: QStyle.PixelMetric,\n        opt: QStyleOption | None = None,\n        widget: QWidget | None = None,\n    ) -&gt; int:\n        pm = QStyle.PixelMetric\n        style = self._base_style(widget)\n        if metric == pm.PM_MenuPanelWidth:\n            return int(style.get(\"border-width\", 1))\n        if metric in (pm.PM_MenuHMargin, pm.PM_MenuVMargin):\n            pp = style.get(\"panel-padding\", [4, 4])\n            if isinstance(pp, (list, tuple)):\n                return int(pp[0] if metric == pm.PM_MenuHMargin else pp[1])\n            return int(pp)\n        if metric == pm.PM_SmallIconSize:\n            return int(style.get(\"icon-size\", 16))\n        if metric == pm.PM_MenuButtonIndicator:\n            return int(style.get(\"icon-size\", 16))\n        return 0\n\n    # ------------------------------------------------------------------\n    # Sizing and Layout Helper\n    # ------------------------------------------------------------------\n\n    def _compute_layout(\n        self,\n        option: QStyleOptionMenuItem,\n        style: dict[str, Any],\n        contents_size: QSize | None = None,\n    ) -&gt; MenuItemLayout:\n        \"\"\"Compute the metrics, dimensions and layouts for a menu item.\n\n        Args:\n            option: The style option for the menu item.\n            style: The resolved style dict.\n            contents_size: Optional contents size passed from size query.\n\n        Returns:\n            The computed layout information for the menu item.\n        \"\"\"\n        ip = style.get(\"item-padding\", [6, 6])\n        if isinstance(ip, (list, tuple)):\n            pad_h, pad_v = int(ip[0]), int(ip[1])\n        else:\n            pad_h = pad_v = int(ip)\n\n        icon_size = int(style.get(\"icon-size\", 16))\n        item_spacing = int(style.get(\"item-spacing\", 4))\n\n        fm = option.fontMetrics\n        text_h = (\n            fm.height()\n            if fm\n            else (\n                contents_size.height()\n                if contents_size\n                else option.rect.height()\n            )\n        )\n        if text_h &lt;= 0:\n            text_h = 16\n\n        min_h = int(style.get(\"min-item-height\", 0))\n        row_h = max(text_h + pad_v * 2, icon_size + pad_v * 2, min_h)\n\n        text = option.text or \"\"\n        label, _, shortcut = text.partition(\"\\t\")\n        text_w = (\n            fm.horizontalAdvance(label)\n            if fm\n            else (contents_size.width() if contents_size else 0)\n        )\n\n        sc_w = getattr(\n            option,\n            \"reservedShortcutWidth\",\n            getattr(option, \"tabWidth\", 0),\n        )\n        if shortcut and sc_w == 0 and fm:\n            sc_w = fm.horizontalAdvance(shortcut)\n\n        icon_gutter = (option.maxIconWidth or icon_size) + item_spacing\n        is_submenu = (\n            option.menuItemType == QStyleOptionMenuItem.MenuItemType.SubMenu\n        )\n        arrow_w = icon_size if is_submenu else 0\n\n        total_w = (\n            icon_gutter\n            + pad_h\n            + text_w\n            + (pad_h + sc_w if sc_w else 0)\n            + (pad_h + arrow_w if arrow_w else 0)\n            + pad_h\n        )\n\n        return MenuItemLayout(\n            pad_h=pad_h,\n            pad_v=pad_v,\n            icon_size=icon_size,\n            item_spacing=item_spacing,\n            icon_gutter=icon_gutter,\n            label_text=label,\n            shortcut_text=shortcut,\n            text_w=text_w,\n            text_h=text_h,\n            sc_w=sc_w,\n            arrow_w=arrow_w,\n            row_h=row_h,\n            total_w=total_w,\n        )\n\n    def menu_item_size(\n        self,\n        contents_type: QStyle.ContentsType,\n        option: QStyleOption | None,\n        contents_size: QSize,\n        widget: QWidget | None = None,\n    ) -&gt; QSize:\n        \"\"\"Compute the bounding size of a single menu item row.\"\"\"\n        if not isinstance(option, QStyleOptionMenuItem):\n            return QSize(contents_size.width(), contents_size.height())\n\n        style = self._base_style(widget)\n\n        # Separator: early exit\n        sep_type = QStyleOptionMenuItem.MenuItemType.Separator\n        if option.menuItemType == sep_type:\n            sep_h = int(style.get(\"separator-height\", 1))\n            return QSize(contents_size.width(), sep_h)\n\n        layout = self._compute_layout(option, style, contents_size)\n        return QSize(\n            max(layout.total_w, contents_size.width()),\n            layout.row_h,\n        )\n\n    # ------------------------------------------------------------------\n    # Primitive painting: panel &amp; frame\n    # ------------------------------------------------------------------\n\n    def draw_panel(\n        self,\n        option: QStyleOption,\n        painter: QPainter,\n        widget: QWidget | None = None,\n    ) -&gt; None:\n        \"\"\"Fill the menu background with the panel colour.\"\"\"\n        painter.save()\n        painter.setRenderHint(QPainter.RenderHint.Antialiasing, True)\n        style = self._base_style(widget)\n        radius = int(style.get(\"border-radius\", 6))\n        painter.setBrush(QBrush(QColor(style[\"background-color\"])))\n        painter.setPen(Qt.PenStyle.NoPen)\n        painter.drawRoundedRect(option.rect, radius, radius)\n        painter.restore()\n\n    def draw_frame(\n        self,\n        option: QStyleOption,\n        painter: QPainter,\n        widget: QWidget | None = None,\n    ) -&gt; None:\n        \"\"\"Stroke the menu border.\"\"\"\n        painter.save()\n        painter.setRenderHint(QPainter.RenderHint.Antialiasing, True)\n        style = self._base_style(widget)\n        radius = int(style.get(\"border-radius\", 6))\n        bw = int(style.get(\"border-width\", 1))\n        pen = QPen(QColor(style[\"border-color\"]))\n        pen.setWidth(bw)\n        painter.setPen(pen)\n        painter.setBrush(Qt.BrushStyle.NoBrush)\n        # Inset by half the pen width so the stroke is fully inside the rect.\n        inset = bw / 2.0\n        inset_rect = QRectF(option.rect).adjusted(inset, inset, -inset, -inset)\n        painter.drawRoundedRect(inset_rect, radius, radius)\n        painter.restore()\n\n    # ------------------------------------------------------------------\n    # Control painting: individual item rows\n    # ------------------------------------------------------------------\n\n    def draw_menu_item(\n        self,\n        option: QStyleOption,\n        painter: QPainter,\n        widget: QWidget | None = None,\n    ) -&gt; None:\n        \"\"\"Paint a single QMenu row (normal, separator, submenu).\"\"\"\n        if not isinstance(option, QStyleOptionMenuItem):\n            return\n\n        painter.save()\n        painter.setRenderHint(QPainter.RenderHint.Antialiasing, True)\n\n        # Handle separators and return\n        sep_type = QStyleOptionMenuItem.MenuItemType.Separator\n        if option.menuItemType == sep_type:\n            self._draw_separator(option, painter, widget)\n            painter.restore()\n            return\n\n        # Resolve state and fetch styles\n        is_enabled = bool(option.state &amp; QStyle.StateFlag.State_Enabled)\n        is_selected = bool(option.state &amp; QStyle.StateFlag.State_Selected)\n        state = (\n            \"disabled\"\n            if not is_enabled\n            else \"hover\"\n            if is_selected and is_enabled\n            else \"base\"\n        )\n\n        # Resolve variant from action property, if available\n        # NOTE: This is a bit of a hack. I could have created a QAction section\n        # in ayon_style.json and fetched the variant from there.\n        action = None\n        if isinstance(widget, QMenu):\n            action = widget.actionAt(option.rect.center())\n        variant = (action.property(\"variant\") if action else None) or \"default\"\n\n        style = self.model.get_style(\n            self._WIDGET_CLS, variant=variant, state=state\n        )\n        style.set_context(widget)\n\n        layout = self._compute_layout(option, style)\n        item_radius = int(style.get(\"item-radius\", 4))\n        rect = option.rect\n\n        # --- Selection background ---\n        bg = QColor(style.get(\"background-color\", \"#424a57\"))\n        painter.setBrush(QBrush(bg))\n        painter.setPen(Qt.PenStyle.NoPen)\n        painter.drawRoundedRect(rect, item_radius, item_radius)\n\n        opacity = float(style.get(\"opacity\", 1.0))\n        painter.setOpacity(opacity)\n\n        # --- Left gutter (icon or check mark) ---\n        x = rect.left() + layout.pad_h\n        cy = rect.center().y()\n\n        check_type = option.checkType\n        not_checkable = QStyleOptionMenuItem.CheckType.NotCheckable\n        if check_type != not_checkable:\n            check_color = QColor(style.get(\"color\", \"#f4f5f5\"))\n            check_icon = get_icon(\n                \"check_box\" if option.checked else \"check_box_outline_blank\",\n                color=check_color,\n                fill=False,\n            )\n            check_rect = QRect(\n                x,\n                cy - layout.icon_size // 2,\n                layout.icon_size,\n                layout.icon_size,\n            )\n            check_icon.paint(painter, check_rect)\n        elif not option.icon.isNull():\n            icon_rect = QRect(\n                x,\n                cy - layout.icon_size // 2,\n                layout.icon_size,\n                layout.icon_size,\n            )\n            mode = QIcon.Mode.Disabled if not is_enabled else QIcon.Mode.Normal\n            option.icon.paint(\n                painter, icon_rect, Qt.AlignmentFlag.AlignCenter, mode\n            )\n\n        x += layout.icon_gutter\n\n        # --- Text (label + shortcut) ---\n        text_color = QColor(style.get(\"color\", \"#f4f5f5\"))\n        painter.setPen(QPen(text_color))\n\n        right_margin = layout.pad_h + (\n            layout.arrow_w + layout.pad_h if layout.arrow_w else 0\n        )\n        label_rect = QRect(\n            x,\n            rect.top(),\n            rect.right()\n            - x\n            - right_margin\n            - (layout.sc_w + layout.pad_h if layout.sc_w else 0),\n            rect.height(),\n        )\n        painter.drawText(\n            label_rect,\n            Qt.AlignmentFlag.AlignVCenter | Qt.AlignmentFlag.AlignLeft,\n            layout.label_text,\n        )\n\n        if layout.shortcut_text:\n            sc_bg_color = QColor(\n                style.get(\n                    \"shortcut-background-color\",\n                    style.get(\"background-color\", \"#2b3036\"),\n                )\n            )\n            text_rect = QFontMetrics(\n                self.style_inst.model.base_font\n            ).boundingRect(layout.shortcut_text)\n            painter.setBrush(QBrush(sc_bg_color))\n            painter.setPen(Qt.PenStyle.NoPen)\n            sc_rect = QRect(\n                rect.right() - right_margin - layout.sc_w - layout.pad_h,\n                rect.top(),\n                layout.sc_w + layout.pad_h,\n                rect.height(),\n            )\n            text_rect.moveCenter(sc_rect.center())\n            painter.drawRoundedRect(text_rect.adjusted(-4, 0, 4, 0), 4, 4)\n\n            sc_color = QColor(\n                style.get(\n                    \"shortcut-color\",\n                    style.get(\"color\", \"#8b9198\"),\n                )\n            )\n            sc_color.setAlphaF(style.get(\"shortcut-opacity\", 0.6))\n            painter.setPen(QPen(sc_color))\n            painter.drawText(\n                sc_rect,\n                Qt.AlignmentFlag.AlignVCenter | Qt.AlignmentFlag.AlignHCenter,\n                layout.shortcut_text,\n            )\n\n        # --- Submenu arrow ---\n        if layout.arrow_w &gt; 0:\n            arrow_color = QColor(style.get(\"color\", \"#f4f5f5\"))\n            arrow_icon = get_icon(\"chevron_right\", color=arrow_color)\n            arrow_rect = QRect(\n                rect.right() - layout.pad_h - layout.arrow_w,\n                cy - layout.icon_size // 2,\n                layout.icon_size,\n                layout.icon_size,\n            )\n            arrow_icon.paint(painter, arrow_rect)\n\n        painter.restore()\n\n    def _draw_separator(\n        self,\n        option: QStyleOptionMenuItem,\n        painter: QPainter,\n        widget: QWidget | None = None,\n    ) -&gt; None:\n        \"\"\"Draw a thin horizontal separator line.\"\"\"\n        style = self._base_style(widget)\n        color = QColor(\n            style.get(\"separator-color\", style.get(\"border-color\", \"#41474d\"))\n        )\n        sep_h = int(style.get(\"separator-height\", 1))\n\n        cy = option.rect.center().y()\n        y = cy - sep_h // 2\n        painter.setPen(Qt.PenStyle.NoPen)\n        painter.setBrush(QBrush(color))\n        painter.drawRect(\n            option.rect.left(),\n            y,\n            option.rect.width(),\n            sep_h,\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/menu.html#client.ayon_core.ui.drawers.menu.MenuDrawer.draw_frame","title":"<code>draw_frame(option, painter, widget=None)</code>","text":"<p>Stroke the menu border.</p> Source code in <code>client/ayon_core/ui/drawers/menu.py</code> <pre><code>def draw_frame(\n    self,\n    option: QStyleOption,\n    painter: QPainter,\n    widget: QWidget | None = None,\n) -&gt; None:\n    \"\"\"Stroke the menu border.\"\"\"\n    painter.save()\n    painter.setRenderHint(QPainter.RenderHint.Antialiasing, True)\n    style = self._base_style(widget)\n    radius = int(style.get(\"border-radius\", 6))\n    bw = int(style.get(\"border-width\", 1))\n    pen = QPen(QColor(style[\"border-color\"]))\n    pen.setWidth(bw)\n    painter.setPen(pen)\n    painter.setBrush(Qt.BrushStyle.NoBrush)\n    # Inset by half the pen width so the stroke is fully inside the rect.\n    inset = bw / 2.0\n    inset_rect = QRectF(option.rect).adjusted(inset, inset, -inset, -inset)\n    painter.drawRoundedRect(inset_rect, radius, radius)\n    painter.restore()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/menu.html#client.ayon_core.ui.drawers.menu.MenuDrawer.draw_menu_item","title":"<code>draw_menu_item(option, painter, widget=None)</code>","text":"<p>Paint a single QMenu row (normal, separator, submenu).</p> Source code in <code>client/ayon_core/ui/drawers/menu.py</code> <pre><code>def draw_menu_item(\n    self,\n    option: QStyleOption,\n    painter: QPainter,\n    widget: QWidget | None = None,\n) -&gt; None:\n    \"\"\"Paint a single QMenu row (normal, separator, submenu).\"\"\"\n    if not isinstance(option, QStyleOptionMenuItem):\n        return\n\n    painter.save()\n    painter.setRenderHint(QPainter.RenderHint.Antialiasing, True)\n\n    # Handle separators and return\n    sep_type = QStyleOptionMenuItem.MenuItemType.Separator\n    if option.menuItemType == sep_type:\n        self._draw_separator(option, painter, widget)\n        painter.restore()\n        return\n\n    # Resolve state and fetch styles\n    is_enabled = bool(option.state &amp; QStyle.StateFlag.State_Enabled)\n    is_selected = bool(option.state &amp; QStyle.StateFlag.State_Selected)\n    state = (\n        \"disabled\"\n        if not is_enabled\n        else \"hover\"\n        if is_selected and is_enabled\n        else \"base\"\n    )\n\n    # Resolve variant from action property, if available\n    # NOTE: This is a bit of a hack. I could have created a QAction section\n    # in ayon_style.json and fetched the variant from there.\n    action = None\n    if isinstance(widget, QMenu):\n        action = widget.actionAt(option.rect.center())\n    variant = (action.property(\"variant\") if action else None) or \"default\"\n\n    style = self.model.get_style(\n        self._WIDGET_CLS, variant=variant, state=state\n    )\n    style.set_context(widget)\n\n    layout = self._compute_layout(option, style)\n    item_radius = int(style.get(\"item-radius\", 4))\n    rect = option.rect\n\n    # --- Selection background ---\n    bg = QColor(style.get(\"background-color\", \"#424a57\"))\n    painter.setBrush(QBrush(bg))\n    painter.setPen(Qt.PenStyle.NoPen)\n    painter.drawRoundedRect(rect, item_radius, item_radius)\n\n    opacity = float(style.get(\"opacity\", 1.0))\n    painter.setOpacity(opacity)\n\n    # --- Left gutter (icon or check mark) ---\n    x = rect.left() + layout.pad_h\n    cy = rect.center().y()\n\n    check_type = option.checkType\n    not_checkable = QStyleOptionMenuItem.CheckType.NotCheckable\n    if check_type != not_checkable:\n        check_color = QColor(style.get(\"color\", \"#f4f5f5\"))\n        check_icon = get_icon(\n            \"check_box\" if option.checked else \"check_box_outline_blank\",\n            color=check_color,\n            fill=False,\n        )\n        check_rect = QRect(\n            x,\n            cy - layout.icon_size // 2,\n            layout.icon_size,\n            layout.icon_size,\n        )\n        check_icon.paint(painter, check_rect)\n    elif not option.icon.isNull():\n        icon_rect = QRect(\n            x,\n            cy - layout.icon_size // 2,\n            layout.icon_size,\n            layout.icon_size,\n        )\n        mode = QIcon.Mode.Disabled if not is_enabled else QIcon.Mode.Normal\n        option.icon.paint(\n            painter, icon_rect, Qt.AlignmentFlag.AlignCenter, mode\n        )\n\n    x += layout.icon_gutter\n\n    # --- Text (label + shortcut) ---\n    text_color = QColor(style.get(\"color\", \"#f4f5f5\"))\n    painter.setPen(QPen(text_color))\n\n    right_margin = layout.pad_h + (\n        layout.arrow_w + layout.pad_h if layout.arrow_w else 0\n    )\n    label_rect = QRect(\n        x,\n        rect.top(),\n        rect.right()\n        - x\n        - right_margin\n        - (layout.sc_w + layout.pad_h if layout.sc_w else 0),\n        rect.height(),\n    )\n    painter.drawText(\n        label_rect,\n        Qt.AlignmentFlag.AlignVCenter | Qt.AlignmentFlag.AlignLeft,\n        layout.label_text,\n    )\n\n    if layout.shortcut_text:\n        sc_bg_color = QColor(\n            style.get(\n                \"shortcut-background-color\",\n                style.get(\"background-color\", \"#2b3036\"),\n            )\n        )\n        text_rect = QFontMetrics(\n            self.style_inst.model.base_font\n        ).boundingRect(layout.shortcut_text)\n        painter.setBrush(QBrush(sc_bg_color))\n        painter.setPen(Qt.PenStyle.NoPen)\n        sc_rect = QRect(\n            rect.right() - right_margin - layout.sc_w - layout.pad_h,\n            rect.top(),\n            layout.sc_w + layout.pad_h,\n            rect.height(),\n        )\n        text_rect.moveCenter(sc_rect.center())\n        painter.drawRoundedRect(text_rect.adjusted(-4, 0, 4, 0), 4, 4)\n\n        sc_color = QColor(\n            style.get(\n                \"shortcut-color\",\n                style.get(\"color\", \"#8b9198\"),\n            )\n        )\n        sc_color.setAlphaF(style.get(\"shortcut-opacity\", 0.6))\n        painter.setPen(QPen(sc_color))\n        painter.drawText(\n            sc_rect,\n            Qt.AlignmentFlag.AlignVCenter | Qt.AlignmentFlag.AlignHCenter,\n            layout.shortcut_text,\n        )\n\n    # --- Submenu arrow ---\n    if layout.arrow_w &gt; 0:\n        arrow_color = QColor(style.get(\"color\", \"#f4f5f5\"))\n        arrow_icon = get_icon(\"chevron_right\", color=arrow_color)\n        arrow_rect = QRect(\n            rect.right() - layout.pad_h - layout.arrow_w,\n            cy - layout.icon_size // 2,\n            layout.icon_size,\n            layout.icon_size,\n        )\n        arrow_icon.paint(painter, arrow_rect)\n\n    painter.restore()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/menu.html#client.ayon_core.ui.drawers.menu.MenuDrawer.draw_panel","title":"<code>draw_panel(option, painter, widget=None)</code>","text":"<p>Fill the menu background with the panel colour.</p> Source code in <code>client/ayon_core/ui/drawers/menu.py</code> <pre><code>def draw_panel(\n    self,\n    option: QStyleOption,\n    painter: QPainter,\n    widget: QWidget | None = None,\n) -&gt; None:\n    \"\"\"Fill the menu background with the panel colour.\"\"\"\n    painter.save()\n    painter.setRenderHint(QPainter.RenderHint.Antialiasing, True)\n    style = self._base_style(widget)\n    radius = int(style.get(\"border-radius\", 6))\n    painter.setBrush(QBrush(QColor(style[\"background-color\"])))\n    painter.setPen(Qt.PenStyle.NoPen)\n    painter.drawRoundedRect(option.rect, radius, radius)\n    painter.restore()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/menu.html#client.ayon_core.ui.drawers.menu.MenuDrawer.menu_item_size","title":"<code>menu_item_size(contents_type, option, contents_size, widget=None)</code>","text":"<p>Compute the bounding size of a single menu item row.</p> Source code in <code>client/ayon_core/ui/drawers/menu.py</code> <pre><code>def menu_item_size(\n    self,\n    contents_type: QStyle.ContentsType,\n    option: QStyleOption | None,\n    contents_size: QSize,\n    widget: QWidget | None = None,\n) -&gt; QSize:\n    \"\"\"Compute the bounding size of a single menu item row.\"\"\"\n    if not isinstance(option, QStyleOptionMenuItem):\n        return QSize(contents_size.width(), contents_size.height())\n\n    style = self._base_style(widget)\n\n    # Separator: early exit\n    sep_type = QStyleOptionMenuItem.MenuItemType.Separator\n    if option.menuItemType == sep_type:\n        sep_h = int(style.get(\"separator-height\", 1))\n        return QSize(contents_size.width(), sep_h)\n\n    layout = self._compute_layout(option, style, contents_size)\n    return QSize(\n        max(layout.total_w, contents_size.width()),\n        layout.row_h,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/menu.html#client.ayon_core.ui.drawers.menu.MenuItemLayout","title":"<code>MenuItemLayout</code>  <code>dataclass</code>","text":"<p>Dataclass to hold the results of menu item layout calculations.</p> Source code in <code>client/ayon_core/ui/drawers/menu.py</code> <pre><code>@dataclass\nclass MenuItemLayout:\n    \"\"\"Dataclass to hold the results of menu item layout calculations.\"\"\"\n\n    pad_h: int\n    pad_v: int\n    icon_size: int\n    item_spacing: int\n    icon_gutter: int\n    label_text: str\n    shortcut_text: str\n    text_w: int\n    text_h: int\n    sc_w: int\n    arrow_w: int\n    row_h: int\n    total_w: int\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/scroll_area.html","title":"scroll_area","text":"<p>ScrollAreaDrawer: scrollbar corner painting for QScrollArea.</p>"},{"location":"autoapi/client/ayon_core/ui/drawers/scrollbar.html","title":"scrollbar","text":"<p>ScrollBarDrawer: custom painting for QScrollBar.</p>"},{"location":"autoapi/client/ayon_core/ui/drawers/scrollbar.html#client.ayon_core.ui.drawers.scrollbar.ScrollBarDrawer","title":"<code>ScrollBarDrawer</code>","text":"Source code in <code>client/ayon_core/ui/drawers/scrollbar.py</code> <pre><code>class ScrollBarDrawer:\n    def __init__(self, style_inst: AYONStyle) -&gt; None:\n        self.style_inst = style_inst\n        self.model = style_inst.model\n        self._style = self.model.get_style(\"QScrollBar\")\n        self._cache = {}\n\n    @property\n    def _super(self):\n        \"\"\"Return proxy for calling QCommonStyle methods on style_inst.\"\"\"\n        from ..style import AYONStyle as _AYONStyle\n\n        return super(_AYONStyle, self.style_inst)\n\n    @property\n    def base_class(self):\n        return {\"QScrollBar\": QtWidgets.QScrollBar}\n\n    def register_drawers(self):\n        return {\n            enum_to_str(\n                QStyle.ControlElement,\n                QStyle.ControlElement.CE_ScrollBarSlider,\n                \"QScrollBar\",\n            ): self.draw_scrollbar_slider,\n            enum_to_str(\n                QStyle.ControlElement,\n                QStyle.ControlElement.CE_ScrollBarAddPage,\n                \"QScrollBar\",\n            ): self.draw_scrollbar_page,\n            enum_to_str(\n                QStyle.ControlElement,\n                QStyle.ControlElement.CE_ScrollBarSubPage,\n                \"QScrollBar\",\n            ): self.draw_scrollbar_page,\n        }\n\n    def register_sizers(self):\n        return {\n            enum_to_str(\n                QStyle.ComplexControl,\n                QStyle.ComplexControl.CC_ScrollBar,\n                \"QScrollBar\",\n            ): self.get_size,\n        }\n\n    def register_metrics(self):\n        return {\n            enum_to_str(\n                QStyle.PixelMetric,\n                QStyle.PixelMetric.PM_ScrollBarExtent,\n                \"QScrollBar\",\n            ): self.get_metric,\n            enum_to_str(\n                QStyle.PixelMetric,\n                QStyle.PixelMetric.PM_ScrollBarSliderMin,\n                \"QScrollBar\",\n            ): self.get_metric,\n        }\n\n    def get_size(\n        self,\n        cc: QStyle.ComplexControl,\n        opt: QStyleOptionComplex,\n        sc: QStyle.SubControl,\n        w: QWidget | None = None,\n    ) -&gt; QRect | None:\n        if not w:\n            raise ValueError(\n                \"Widget required to calculate scrollbar sub-control rects\"\n            )\n\n        if not isinstance(opt, (QStyleOptionSlider, QStyleOptionComplex)):\n            raise ValueError(f\"Unexpected option type: {type(opt)}\")\n\n        sup = self._super\n        try:\n            als = self._cache[\"add_line_size\"]\n        except KeyError:\n            als = self._cache[\"add_line_size\"] = sup.subControlRect(\n                cc, opt, QStyle.SubControl.SC_ScrollBarAddLine, w\n            ).size()\n        try:\n            sls = self._cache[\"sub_line_size\"]\n        except KeyError:\n            sls = self._cache[\"sub_line_size\"] = sup.subControlRect(\n                cc, opt, QStyle.SubControl.SC_ScrollBarSubLine, w\n            ).size()\n\n        orientation = w.orientation()\n\n        if sc in (\n            QStyle.SubControl.SC_ScrollBarSlider,\n            QStyle.SubControl.SC_ScrollBarGroove,\n        ):\n            rect = sup.subControlRect(cc, opt, sc, w)\n            if orientation == Qt.Orientation.Vertical:\n                rect.adjust(0, -sls.height(), 0, als.height())\n            else:\n                rect.adjust(-sls.width(), 0, als.width(), 0)\n            return rect\n\n        elif sc == QStyle.SubControl.SC_ScrollBarAddPage:\n            rect = sup.subControlRect(cc, opt, sc, w)\n            if orientation == Qt.Orientation.Vertical:\n                rect.adjust(0, 0, 0, als.height())\n            else:\n                rect.adjust(0, 0, als.width(), 0)\n            return rect\n\n        elif sc == QStyle.SubControl.SC_ScrollBarSubPage:\n            rect = sup.subControlRect(cc, opt, sc, w)\n            if orientation == Qt.Orientation.Vertical:\n                rect.adjust(0, -sls.height(), 0, 0)\n            else:\n                rect.adjust(-sls.width(), 0, 0, 0)\n            return rect\n\n        raise ValueError(\"Unexpected sub-control\")\n\n    def get_metric(\n        self,\n        metric: QStyle.PixelMetric,\n        opt: QStyleOption | None = None,\n        widget: QWidget | None = None,\n    ) -&gt; int:\n        self._style.set_context(widget)\n        if metric == QStyle.PixelMetric.PM_ScrollBarExtent:\n            # Width of a vertical scroll bar and the height of a horizontal\n            # scroll bar.\n            return int(self._style[\"width\"])\n        elif metric == QStyle.PixelMetric.PM_ScrollBarSliderMin:\n            # The minimum height of a vertical scroll bar's slider and the\n            # minimum width of a horizontal scroll bar's slider.\n            return int(self._style[\"min-length\"])\n        return 0\n\n    def draw_scrollbar_slider(\n        self,\n        option: QStyleOptionComplex,\n        painter: QPainter,\n        widget: QWidget | None = None,\n    ) -&gt; None:\n        \"\"\"Draw the scrollbar slider/thumb.\"\"\"\n        style = self.model.get_style(\"QScrollBar\")\n        style.set_context(widget)\n        painter.save()\n        painter.setRenderHint(QPainter.RenderHint.Antialiasing)\n\n        # Draw slider background\n        painter.setBrush(QBrush(QColor(style.get(\"slider-color\"))))\n        pen = QPen(QColor(style.get(\"background-color\")))\n        pen.setWidth(style.get(\"border-width\"))\n        painter.setPen(pen)\n        radius = style.get(\"border-radius\")\n        painter.drawRoundedRect(option.rect, radius, radius)\n\n        painter.restore()\n\n    def draw_scrollbar_page(\n        self,\n        option: QStyleOptionComplex,\n        painter: QPainter,\n        widget: QWidget | None = None,\n    ) -&gt; None:\n        \"\"\"Draw scrollbar page buttons.\"\"\"\n        style = self.model.get_style(\"QScrollBar\")\n        style.set_context(widget)\n        painter.save()\n        painter.setRenderHint(QPainter.RenderHint.Antialiasing)\n\n        # Draw slider background\n        painter.setBrush(QBrush(QColor(style.get(\"background-color\"))))\n        painter.setPen(Qt.PenStyle.NoPen)\n        painter.drawRect(option.rect)\n\n        painter.restore()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/scrollbar.html#client.ayon_core.ui.drawers.scrollbar.ScrollBarDrawer.draw_scrollbar_page","title":"<code>draw_scrollbar_page(option, painter, widget=None)</code>","text":"<p>Draw scrollbar page buttons.</p> Source code in <code>client/ayon_core/ui/drawers/scrollbar.py</code> <pre><code>def draw_scrollbar_page(\n    self,\n    option: QStyleOptionComplex,\n    painter: QPainter,\n    widget: QWidget | None = None,\n) -&gt; None:\n    \"\"\"Draw scrollbar page buttons.\"\"\"\n    style = self.model.get_style(\"QScrollBar\")\n    style.set_context(widget)\n    painter.save()\n    painter.setRenderHint(QPainter.RenderHint.Antialiasing)\n\n    # Draw slider background\n    painter.setBrush(QBrush(QColor(style.get(\"background-color\"))))\n    painter.setPen(Qt.PenStyle.NoPen)\n    painter.drawRect(option.rect)\n\n    painter.restore()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/scrollbar.html#client.ayon_core.ui.drawers.scrollbar.ScrollBarDrawer.draw_scrollbar_slider","title":"<code>draw_scrollbar_slider(option, painter, widget=None)</code>","text":"<p>Draw the scrollbar slider/thumb.</p> Source code in <code>client/ayon_core/ui/drawers/scrollbar.py</code> <pre><code>def draw_scrollbar_slider(\n    self,\n    option: QStyleOptionComplex,\n    painter: QPainter,\n    widget: QWidget | None = None,\n) -&gt; None:\n    \"\"\"Draw the scrollbar slider/thumb.\"\"\"\n    style = self.model.get_style(\"QScrollBar\")\n    style.set_context(widget)\n    painter.save()\n    painter.setRenderHint(QPainter.RenderHint.Antialiasing)\n\n    # Draw slider background\n    painter.setBrush(QBrush(QColor(style.get(\"slider-color\"))))\n    pen = QPen(QColor(style.get(\"background-color\")))\n    pen.setWidth(style.get(\"border-width\"))\n    painter.setPen(pen)\n    radius = style.get(\"border-radius\")\n    painter.drawRoundedRect(option.rect, radius, radius)\n\n    painter.restore()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/table_header.html","title":"table_header","text":"<p>TableHeaderDrawer: custom painting for QHeaderView.</p>"},{"location":"autoapi/client/ayon_core/ui/drawers/table_header.html#client.ayon_core.ui.drawers.table_header.TableHeaderDrawer","title":"<code>TableHeaderDrawer</code>","text":"<p>AYONStyle drawer for QHeaderView used by AYTableView.</p> <p>Handles painting of header sections and labels using colours from the AYTableView style data in ayon_style.json.</p> Source code in <code>client/ayon_core/ui/drawers/table_header.py</code> <pre><code>class TableHeaderDrawer:\n    \"\"\"AYONStyle drawer for QHeaderView used by AYTableView.\n\n    Handles painting of header sections and labels using colours\n    from the AYTableView style data in ayon_style.json.\n    \"\"\"\n\n    def __init__(self, style_inst: AYONStyle) -&gt; None:\n        self.style_inst = style_inst\n        self.model = style_inst.model\n        self._icon_cache: dict[str, QIcon] = {}\n\n    @property\n    def base_class(self):\n        return {\"QHeaderView\": QtWidgets.QHeaderView}\n\n    def register_drawers(self) -&gt; dict:\n        return {\n            enum_to_str(\n                QStyle.ControlElement,\n                QStyle.ControlElement.CE_Header,\n                \"QHeaderView\",\n            ): [\n                partial(\n                    self.style_inst.drawControl,\n                    QStyle.ControlElement.CE_HeaderSection,\n                ),\n                partial(\n                    self.style_inst.drawControl,\n                    QStyle.ControlElement.CE_HeaderLabel,\n                ),\n            ],\n            enum_to_str(\n                QStyle.ControlElement,\n                QStyle.ControlElement.CE_HeaderSection,\n                \"QHeaderView\",\n            ): self.draw_header_section,\n            enum_to_str(\n                QStyle.ControlElement,\n                QStyle.ControlElement.CE_HeaderLabel,\n                \"QHeaderView\",\n            ): self.draw_header_label,\n        }\n\n    def register_metrics(self) -&gt; dict:\n        \"\"\"Register pixel metrics for QHeaderView.\"\"\"\n        return {\n            enum_to_str(\n                QStyle.PixelMetric,\n                QStyle.PixelMetric.PM_HeaderMargin,\n                \"QHeaderView\",\n            ): self.get_metric,\n        }\n\n    def get_metric(\n        self,\n        metric: QStyle.PixelMetric,\n        opt: QStyleOption | None = None,\n        widget: QWidget | None = None,\n    ) -&gt; int:\n        \"\"\"Return header margin from style data.\"\"\"\n        if metric == QStyle.PixelMetric.PM_HeaderMargin:\n            return 4\n        return 0\n\n    def _get_table_style(self, widget: QWidget | None) -&gt; dict:\n        \"\"\"Resolve the AYTableView style for the header's parent table.\"\"\"\n        variant = \"default\"\n        if widget is not None:\n            # QHeaderView's parent is the QTreeView/AYTableView\n            table = widget.parent()\n            if table is not None:\n                variant = getattr(table, \"_variant_str\", \"default\")\n        return self.model.get_style(\"AYTableView\", variant)\n\n    def draw_header_section(\n        self,\n        option: QStyleOption,\n        painter: QPainter,\n        widget: QWidget | None = None,\n    ) -&gt; None:\n        \"\"\"Draw the header section background and bottom border.\"\"\"\n        style = self._get_table_style(widget)\n\n        painter.save()\n        painter.setRenderHint(QPainter.RenderHint.Antialiasing, False)\n\n        # Background\n        bg_color = QColor(style.get(\"header-background-color\", \"#272d35\"))\n        painter.setBrush(QBrush(bg_color))\n        painter.setPen(Qt.PenStyle.NoPen)\n        painter.drawRect(option.rect)\n\n        # Bottom border\n        border_color = QColor(style.get(\"header-border-color\", \"#41474d\"))\n        pen = QPen(border_color)\n        pen.setWidth(1)\n        painter.setPen(pen)\n        bottom = option.rect.bottom()\n        painter.drawLine(\n            option.rect.left(),\n            bottom,\n            option.rect.right(),\n            bottom,\n        )\n\n        painter.restore()\n\n    def draw_header_label(\n        self,\n        option: QStyleOption,\n        painter: QPainter,\n        widget: QWidget | None = None,\n    ) -&gt; None:\n        \"\"\"Draw the header label text and sort indicator.\"\"\"\n        style = self._get_table_style(widget)\n        padding = style.get(\"header-padding\", [4, 8])\n\n        painter.save()\n\n        # Text\n        text_color = QColor(style.get(\"header-color\", \"#c1c7ce\"))\n        painter.setPen(text_color)\n\n        font = painter.font()\n        font.setWeight(QFont.Weight.DemiBold)\n        painter.setFont(font)\n\n        text_rect = option.rect.adjusted(\n            padding[1], padding[0], -padding[1], -padding[0]\n        )\n\n        text = \"\"\n        if hasattr(option, \"text\"):\n            text = option.text or \"\"\n\n        # Check for sort indicator\n        sort_indicator = getattr(option, \"sortIndicator\", None)\n        indicator_space = 0\n        if sort_indicator and sort_indicator != 0:\n            indicator_space = 16\n\n        if text:\n            draw_rect = QRect(text_rect)\n            if indicator_space:\n                draw_rect.setRight(draw_rect.right() - indicator_space)\n            painter.drawText(\n                draw_rect,\n                Qt.AlignmentFlag.AlignVCenter | Qt.AlignmentFlag.AlignLeft,\n                text,\n            )\n\n        # Sort indicator arrow\n        if sort_indicator and sort_indicator != 0:\n            indicator_color = QColor(\n                style.get(\n                    \"header-sort-indicator-color\",\n                    \"#8fceff\",\n                )\n            )\n            # sortIndicator: 1 = Down, 2 = Up (in QStyleOptionHeader)\n            icon_name = (\n                \"arrow_downward\" if sort_indicator == 1 else \"arrow_upward\"\n            )\n            cache_key = f\"{icon_name}-{indicator_color.name()}\"\n            if cache_key not in self._icon_cache:\n                self._icon_cache[cache_key] = get_icon(\n                    icon_name, color=indicator_color\n                )\n            icon = self._icon_cache[cache_key]\n            icon_rect = QRect(\n                text_rect.right() - 14,\n                text_rect.center().y() - 7,\n                14,\n                14,\n            )\n            icon.paint(painter, icon_rect)\n\n        painter.restore()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/table_header.html#client.ayon_core.ui.drawers.table_header.TableHeaderDrawer.draw_header_label","title":"<code>draw_header_label(option, painter, widget=None)</code>","text":"<p>Draw the header label text and sort indicator.</p> Source code in <code>client/ayon_core/ui/drawers/table_header.py</code> <pre><code>def draw_header_label(\n    self,\n    option: QStyleOption,\n    painter: QPainter,\n    widget: QWidget | None = None,\n) -&gt; None:\n    \"\"\"Draw the header label text and sort indicator.\"\"\"\n    style = self._get_table_style(widget)\n    padding = style.get(\"header-padding\", [4, 8])\n\n    painter.save()\n\n    # Text\n    text_color = QColor(style.get(\"header-color\", \"#c1c7ce\"))\n    painter.setPen(text_color)\n\n    font = painter.font()\n    font.setWeight(QFont.Weight.DemiBold)\n    painter.setFont(font)\n\n    text_rect = option.rect.adjusted(\n        padding[1], padding[0], -padding[1], -padding[0]\n    )\n\n    text = \"\"\n    if hasattr(option, \"text\"):\n        text = option.text or \"\"\n\n    # Check for sort indicator\n    sort_indicator = getattr(option, \"sortIndicator\", None)\n    indicator_space = 0\n    if sort_indicator and sort_indicator != 0:\n        indicator_space = 16\n\n    if text:\n        draw_rect = QRect(text_rect)\n        if indicator_space:\n            draw_rect.setRight(draw_rect.right() - indicator_space)\n        painter.drawText(\n            draw_rect,\n            Qt.AlignmentFlag.AlignVCenter | Qt.AlignmentFlag.AlignLeft,\n            text,\n        )\n\n    # Sort indicator arrow\n    if sort_indicator and sort_indicator != 0:\n        indicator_color = QColor(\n            style.get(\n                \"header-sort-indicator-color\",\n                \"#8fceff\",\n            )\n        )\n        # sortIndicator: 1 = Down, 2 = Up (in QStyleOptionHeader)\n        icon_name = (\n            \"arrow_downward\" if sort_indicator == 1 else \"arrow_upward\"\n        )\n        cache_key = f\"{icon_name}-{indicator_color.name()}\"\n        if cache_key not in self._icon_cache:\n            self._icon_cache[cache_key] = get_icon(\n                icon_name, color=indicator_color\n            )\n        icon = self._icon_cache[cache_key]\n        icon_rect = QRect(\n            text_rect.right() - 14,\n            text_rect.center().y() - 7,\n            14,\n            14,\n        )\n        icon.paint(painter, icon_rect)\n\n    painter.restore()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/table_header.html#client.ayon_core.ui.drawers.table_header.TableHeaderDrawer.draw_header_section","title":"<code>draw_header_section(option, painter, widget=None)</code>","text":"<p>Draw the header section background and bottom border.</p> Source code in <code>client/ayon_core/ui/drawers/table_header.py</code> <pre><code>def draw_header_section(\n    self,\n    option: QStyleOption,\n    painter: QPainter,\n    widget: QWidget | None = None,\n) -&gt; None:\n    \"\"\"Draw the header section background and bottom border.\"\"\"\n    style = self._get_table_style(widget)\n\n    painter.save()\n    painter.setRenderHint(QPainter.RenderHint.Antialiasing, False)\n\n    # Background\n    bg_color = QColor(style.get(\"header-background-color\", \"#272d35\"))\n    painter.setBrush(QBrush(bg_color))\n    painter.setPen(Qt.PenStyle.NoPen)\n    painter.drawRect(option.rect)\n\n    # Bottom border\n    border_color = QColor(style.get(\"header-border-color\", \"#41474d\"))\n    pen = QPen(border_color)\n    pen.setWidth(1)\n    painter.setPen(pen)\n    bottom = option.rect.bottom()\n    painter.drawLine(\n        option.rect.left(),\n        bottom,\n        option.rect.right(),\n        bottom,\n    )\n\n    painter.restore()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/table_header.html#client.ayon_core.ui.drawers.table_header.TableHeaderDrawer.get_metric","title":"<code>get_metric(metric, opt=None, widget=None)</code>","text":"<p>Return header margin from style data.</p> Source code in <code>client/ayon_core/ui/drawers/table_header.py</code> <pre><code>def get_metric(\n    self,\n    metric: QStyle.PixelMetric,\n    opt: QStyleOption | None = None,\n    widget: QWidget | None = None,\n) -&gt; int:\n    \"\"\"Return header margin from style data.\"\"\"\n    if metric == QStyle.PixelMetric.PM_HeaderMargin:\n        return 4\n    return 0\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/table_header.html#client.ayon_core.ui.drawers.table_header.TableHeaderDrawer.register_metrics","title":"<code>register_metrics()</code>","text":"<p>Register pixel metrics for QHeaderView.</p> Source code in <code>client/ayon_core/ui/drawers/table_header.py</code> <pre><code>def register_metrics(self) -&gt; dict:\n    \"\"\"Register pixel metrics for QHeaderView.\"\"\"\n    return {\n        enum_to_str(\n            QStyle.PixelMetric,\n            QStyle.PixelMetric.PM_HeaderMargin,\n            \"QHeaderView\",\n        ): self.get_metric,\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/tooltip.html","title":"tooltip","text":"<p>TooltipDrawer: custom painting for QToolTip.</p>"},{"location":"autoapi/client/ayon_core/ui/drawers/tooltip.html#client.ayon_core.ui.drawers.tooltip.TooltipDrawer","title":"<code>TooltipDrawer</code>","text":"Source code in <code>client/ayon_core/ui/drawers/tooltip.py</code> <pre><code>class TooltipDrawer:\n    def __init__(self, style_inst: AYONStyle) -&gt; None:\n        self.style_inst = style_inst\n        self.model = style_inst.model\n\n    @property\n    def _super(self):\n        \"\"\"Return proxy for calling QCommonStyle methods on style_inst.\"\"\"\n        from ..style import AYONStyle as _AYONStyle\n\n        return super(_AYONStyle, self.style_inst)\n\n    @property\n    def base_class(self):\n        return {\"QToolTip\": QToolTip}\n\n    def register_drawers(self):\n        return {\n            enum_to_str(\n                QStyle.ControlElement,\n                QStyle.ControlElement.CE_ShapedFrame,\n                \"QToolTip\",\n            ): self.draw_control,\n            enum_to_str(\n                QStyle.PrimitiveElement,\n                QStyle.PrimitiveElement.PE_PanelTipLabel,\n                \"QToolTip\",\n            ): partial(\n                self.draw_primitive, QStyle.PrimitiveElement.PE_PanelTipLabel\n            ),\n            enum_to_str(\n                QStyle.PrimitiveElement,\n                QStyle.PrimitiveElement.PE_Frame,\n                \"QToolTip\",\n            ): partial(self.draw_primitive, QStyle.PrimitiveElement.PE_Frame),\n        }\n\n    def register_sizers(self):\n        return {\n            enum_to_str(\n                QStyle.SubElement,\n                QStyle.SubElement.SE_ShapedFrameContents,\n                \"QToolTip\",\n            ): self.get_rect,\n            enum_to_str(\n                QStyle.SubElement,\n                QStyle.SubElement.SE_FrameLayoutItem,\n                \"QToolTip\",\n            ): self.get_rect,\n        }\n\n    def draw_control(\n        self,\n        option: QStyleOptionFrame,\n        painter: QPainter,\n        widget: QWidget,\n    ):\n        option.frameShadow = QFrame.Shadow.Plain\n        option.frameShape = QFrame.Shape.StyledPanel\n        self._super.drawControl(\n            QStyle.ControlElement.CE_ShapedFrame, option, painter, widget\n        )\n\n    def draw_primitive(\n        self,\n        prim: QStyle.PrimitiveElement,\n        option: QStyleOption,\n        painter: QPainter,\n        w: QWidget,\n    ) -&gt; None:\n        if prim == QStyle.PrimitiveElement.PE_Frame:\n            painter.save()\n            painter.setRenderHint(QPainter.RenderHint.Antialiasing, True)\n            style = self.model.get_style(\"QToolTip\")\n            style.set_context(w)\n            pen = QPen(style[\"border-color\"])\n            pen.setWidth(style[\"border-width\"])\n            painter.setBrush(Qt.BrushStyle.NoBrush)\n            painter.setPen(pen)\n            radius = int(style[\"border-radius\"])\n            painter.drawRoundedRect(\n                option.rect,\n                radius,\n                radius,\n            )\n            painter.restore()\n\n        elif prim == QStyle.PrimitiveElement.PE_PanelTipLabel:\n            painter.save()\n            painter.setRenderHint(QPainter.RenderHint.Antialiasing, True)\n            style = self.model.get_style(\"QToolTip\")\n            style.set_context(w)\n            brush = QBrush(style[\"background-color\"])\n            painter.setBrush(brush)\n            painter.setPen(Qt.PenStyle.NoPen)\n            radius = int(style[\"border-radius\"])\n            painter.drawRoundedRect(\n                option.rect,\n                radius,\n                radius,\n            )\n            painter.restore()\n\n    def get_rect(\n        self,\n        element: QStyle.SubElement,\n        option: QStyleOption,\n        widget: QWidget,\n    ) -&gt; QRect:\n        tt_style = self.model.get_style(\"QToolTip\")\n        tt_style.set_context(widget)\n        tt_pad_x, tt_pad_y = tt_style[\"padding\"]\n\n        if element == QStyle.SubElement.SE_ShapedFrameContents:\n            if isinstance(option, QStyleOptionFrame):\n                option.features = QStyleOptionFrame.FrameFeature.Rounded\n                option.frameShape = QFrame.Shape.StyledPanel\n                widget.setContentsMargins(\n                    tt_pad_x, tt_pad_y, tt_pad_x, tt_pad_y\n                )\n\n        elif element == QStyle.SubElement.SE_FrameLayoutItem:\n            if isinstance(option, QStyleOptionFrame):\n                option.features = QStyleOptionFrame.FrameFeature.Rounded\n                option.frameShape = QFrame.Shape.StyledPanel\n                widget.setContentsMargins(\n                    tt_pad_x, tt_pad_y, tt_pad_x, tt_pad_y\n                )\n\n        return self._super.subElementRect(element, option, widget)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/tree_view.html","title":"tree_view","text":"<p>TreeViewDrawer: branch indicators and indentation for QTreeView.</p>"},{"location":"autoapi/client/ayon_core/ui/drawers/tree_view.html#client.ayon_core.ui.drawers.tree_view.TreeViewDrawer","title":"<code>TreeViewDrawer</code>","text":"<p>AYONStyle drawer for QTreeView.</p> <p>Handles branch expand/collapse indicators and the indentation metric using colours from the QTreeView style data in ayon_style.json.</p> Source code in <code>client/ayon_core/ui/drawers/tree_view.py</code> <pre><code>class TreeViewDrawer:\n    \"\"\"AYONStyle drawer for QTreeView.\n\n    Handles branch expand/collapse indicators and the indentation metric\n    using colours from the QTreeView style data in ayon_style.json.\n    \"\"\"\n\n    def __init__(self, style_inst: AYONStyle) -&gt; None:\n        self.style_inst = style_inst\n        self.model = style_inst.model\n        self._icon_cache = {}\n\n    @property\n    def base_class(self):\n        return {\"QTreeView\": QTreeView}\n\n    def register_drawers(self) -&gt; dict:\n        \"\"\"Register drawing functions for QTreeView primitives.\"\"\"\n        return {\n            enum_to_str(\n                QStyle.PrimitiveElement,\n                QStyle.PrimitiveElement.PE_IndicatorBranch,\n                \"QTreeView\",\n            ): self.draw_branch_indicator,\n            enum_to_str(\n                QStyle.PrimitiveElement,\n                QStyle.PrimitiveElement.PE_PanelScrollAreaCorner,\n                \"QTreeView\",\n            ): self.draw_scrollbar_corner,\n        }\n\n    def register_metrics(self) -&gt; dict:\n        \"\"\"Register pixel metric functions for QTreeView.\"\"\"\n        return {\n            enum_to_str(\n                QStyle.PixelMetric,\n                QStyle.PixelMetric.PM_TreeViewIndentation,\n                \"QTreeView\",\n            ): self.get_metric,\n        }\n\n    def get_metric(\n        self,\n        metric: QStyle.PixelMetric,\n        opt: QStyleOption | None = None,\n        widget: QWidget | None = None,\n    ) -&gt; int:\n        \"\"\"Return indent width from style data.\n\n        Args:\n            metric: The pixel metric being queried.\n            opt: Optional style option.\n            widget: The target widget.\n\n        Returns:\n            The indent size in pixels.\n        \"\"\"\n        if metric == QStyle.PixelMetric.PM_TreeViewIndentation:\n            variant = getattr(widget, \"_variant_str\", \"default\")\n            style = self.model.get_style(\"QTreeView\", variant)\n            return int(style.get(\"indent\", 20))\n        return 0\n\n    def _draw_cell_border(\n        self,\n        painter: QPainter,\n        rect: QRect,\n        style: dict,\n    ) -&gt; None:\n        \"\"\"Draw top and bottom border lines for an AYTableView cell.\"\"\"\n        painter.setPen(\n            QPen(\n                QColor(style.get(\"border-color\")), style.get(\"border-width\", 1)\n            )\n        )\n        painter.setBrush(Qt.BrushStyle.NoBrush)\n        painter.drawLines(\n            [\n                rect.topLeft(),\n                rect.topRight(),\n                rect.bottomLeft(),\n                rect.bottomRight(),\n            ]\n        )\n\n    def _resolve_tree_view(self, widget: QWidget | None) -&gt; QWidget | None:\n        \"\"\"Resolve widget to the actual QTreeView/AYTableView.\"\"\"\n        if widget is not None and not isinstance(widget, QTreeView):\n            return widget.parent() or widget\n        return widget\n\n    def _paint_cell_background(\n        self,\n        painter: QPainter,\n        rect: QRect,\n        style: dict,\n        is_table: bool,\n        is_base_state: bool = False,\n    ) -&gt; None:\n        \"\"\"Paint background fill and optional cell borders.\n\n        Args:\n            painter: The QPainter to draw on.\n            rect: The rectangle to fill.\n            style: The style data dictionary.\n            is_table: Whether this is an AYTableView cell.\n            is_base_state: If True and is_table, use 'background-color-item'.\n        \"\"\"\n        painter.save()\n        if is_table and is_base_state:\n            bg_key = \"background-color-item\"\n        else:\n            bg_key = \"background-color\"\n        painter.fillRect(rect, QColor(style.get(bg_key, \"transparent\")))\n        if is_table:\n            self._draw_cell_border(painter, rect, style)\n        painter.restore()\n\n    def _paint_icon(\n        self,\n        painter: QPainter,\n        rect: QRect,\n        icon,\n        icon_size: int | None,\n    ) -&gt; None:\n        \"\"\"Paint a cached icon, optionally resizing and repositioning it.\"\"\"\n        draw_rect = QRect(rect)\n        if icon_size is not None:\n            center = rect.center()\n            draw_rect.setSize(QSize(icon_size, icon_size))\n            draw_rect.moveTo(\n                rect.right() - icon_size, center.y() - icon_size // 2\n            )\n        icon.paint(painter, draw_rect)\n\n    def _paint_fallback_arrow(\n        self,\n        painter: QPainter,\n        rect: QRect,\n        color: QColor,\n        is_open: bool,\n    ) -&gt; None:\n        \"\"\"Paint a geometric triangle arrow when no icon is configured.\"\"\"\n        painter.setRenderHint(QPainter.RenderHint.Antialiasing, True)\n        painter.setPen(Qt.PenStyle.NoPen)\n        painter.setBrush(QBrush(color))\n\n        cx, cy = rect.center().x(), rect.center().y()\n        size = max(4, min(rect.width(), rect.height()) // 3)\n\n        path = QPainterPath()\n        if is_open:\n            path.moveTo(cx - size, cy - size // 2)\n            path.lineTo(cx + size, cy - size // 2)\n            path.lineTo(cx, cy + size // 2)\n        else:\n            path.moveTo(cx - size // 2, cy - size)\n            path.lineTo(cx - size // 2, cy + size)\n            path.lineTo(cx + size // 2, cy)\n        path.closeSubpath()\n        painter.drawPath(path)\n\n    def draw_branch_indicator(\n        self,\n        option: QStyleOption,\n        painter: QPainter,\n        widget: QWidget | None = None,\n    ) -&gt; None:\n        \"\"\"Draw expand / collapse arrows for tree branch items.\n\n        Args:\n            option: The primitive element style option.\n            painter: The QPainter to draw on.\n            widget: The QTreeView widget (may be the viewport).\n        \"\"\"\n        has_children = bool(option.state &amp; QStyle.StateFlag.State_Children)\n        tv = self._resolve_tree_view(widget)\n        is_table = type(tv).__name__ == \"AYTableView\"\n        is_selected = bool(option.state &amp; QStyle.StateFlag.State_Selected)\n        is_hovered = bool(option.state &amp; QStyle.StateFlag.State_MouseOver)\n        variant = getattr(tv, \"_variant_str\", \"default\")\n\n        state_name = (\n            \"selected\" if is_selected else \"hover\" if is_hovered else \"base\"\n        )\n\n        widget_class = \"AYTableView\" if is_table else \"QTreeView\"\n        t_style = self.model.get_style(\n            widget_class, variant=variant, state=state_name\n        )\n\n        # Items without children only need background/border painting\n        if not has_children:\n            self._paint_cell_background(\n                painter,\n                option.rect,\n                t_style,\n                is_table,\n                is_base_state=(state_name == \"base\"),\n            )\n            return\n\n        is_open = bool(option.state &amp; QStyle.StateFlag.State_Open)\n        color = QColor(t_style.get(\"branch-indicator-color\", \"#8b9198\"))\n        icon_name = t_style.get(\n            \"expanded-icon-name\" if is_open else \"expand-icon-name\"\n        )\n\n        # Paint background for items with children\n        self._paint_cell_background(painter, option.rect, t_style, is_table)\n\n        if icon_name:\n            key = f\"{icon_name}-{color.name()}\"\n            if key not in self._icon_cache:\n                self._icon_cache[key] = get_icon(icon_name, color=color)\n            icon_size = t_style.get(\"expand-icon-size\")\n            self._paint_icon(\n                painter, option.rect, self._icon_cache[key], icon_size\n            )\n        else:\n            self._paint_fallback_arrow(painter, option.rect, color, is_open)\n\n    def draw_scrollbar_corner(\n        self,\n        option: QStyleOption,\n        painter: QPainter,\n        widget: QWidget | None = None,\n    ) -&gt; None:\n        style = self.model.get_style(\"QScrollArea\", variant=\"default\")\n        style.set_context(widget)\n        painter.save()\n        # Draw corner background\n        bg = style.get(\"background-color\", \"transparent\")\n        painter.fillRect(option.rect, QColor(bg))\n\n        painter.restore()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/tree_view.html#client.ayon_core.ui.drawers.tree_view.TreeViewDrawer.draw_branch_indicator","title":"<code>draw_branch_indicator(option, painter, widget=None)</code>","text":"<p>Draw expand / collapse arrows for tree branch items.</p> <p>Parameters:</p> Name Type Description Default <code>option</code> <code>QStyleOption</code> <p>The primitive element style option.</p> required <code>painter</code> <code>QPainter</code> <p>The QPainter to draw on.</p> required <code>widget</code> <code>QWidget | None</code> <p>The QTreeView widget (may be the viewport).</p> <code>None</code> Source code in <code>client/ayon_core/ui/drawers/tree_view.py</code> <pre><code>def draw_branch_indicator(\n    self,\n    option: QStyleOption,\n    painter: QPainter,\n    widget: QWidget | None = None,\n) -&gt; None:\n    \"\"\"Draw expand / collapse arrows for tree branch items.\n\n    Args:\n        option: The primitive element style option.\n        painter: The QPainter to draw on.\n        widget: The QTreeView widget (may be the viewport).\n    \"\"\"\n    has_children = bool(option.state &amp; QStyle.StateFlag.State_Children)\n    tv = self._resolve_tree_view(widget)\n    is_table = type(tv).__name__ == \"AYTableView\"\n    is_selected = bool(option.state &amp; QStyle.StateFlag.State_Selected)\n    is_hovered = bool(option.state &amp; QStyle.StateFlag.State_MouseOver)\n    variant = getattr(tv, \"_variant_str\", \"default\")\n\n    state_name = (\n        \"selected\" if is_selected else \"hover\" if is_hovered else \"base\"\n    )\n\n    widget_class = \"AYTableView\" if is_table else \"QTreeView\"\n    t_style = self.model.get_style(\n        widget_class, variant=variant, state=state_name\n    )\n\n    # Items without children only need background/border painting\n    if not has_children:\n        self._paint_cell_background(\n            painter,\n            option.rect,\n            t_style,\n            is_table,\n            is_base_state=(state_name == \"base\"),\n        )\n        return\n\n    is_open = bool(option.state &amp; QStyle.StateFlag.State_Open)\n    color = QColor(t_style.get(\"branch-indicator-color\", \"#8b9198\"))\n    icon_name = t_style.get(\n        \"expanded-icon-name\" if is_open else \"expand-icon-name\"\n    )\n\n    # Paint background for items with children\n    self._paint_cell_background(painter, option.rect, t_style, is_table)\n\n    if icon_name:\n        key = f\"{icon_name}-{color.name()}\"\n        if key not in self._icon_cache:\n            self._icon_cache[key] = get_icon(icon_name, color=color)\n        icon_size = t_style.get(\"expand-icon-size\")\n        self._paint_icon(\n            painter, option.rect, self._icon_cache[key], icon_size\n        )\n    else:\n        self._paint_fallback_arrow(painter, option.rect, color, is_open)\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/tree_view.html#client.ayon_core.ui.drawers.tree_view.TreeViewDrawer.get_metric","title":"<code>get_metric(metric, opt=None, widget=None)</code>","text":"<p>Return indent width from style data.</p> <p>Parameters:</p> Name Type Description Default <code>metric</code> <code>PixelMetric</code> <p>The pixel metric being queried.</p> required <code>opt</code> <code>QStyleOption | None</code> <p>Optional style option.</p> <code>None</code> <code>widget</code> <code>QWidget | None</code> <p>The target widget.</p> <code>None</code> <p>Returns:</p> Type Description <code>int</code> <p>The indent size in pixels.</p> Source code in <code>client/ayon_core/ui/drawers/tree_view.py</code> <pre><code>def get_metric(\n    self,\n    metric: QStyle.PixelMetric,\n    opt: QStyleOption | None = None,\n    widget: QWidget | None = None,\n) -&gt; int:\n    \"\"\"Return indent width from style data.\n\n    Args:\n        metric: The pixel metric being queried.\n        opt: Optional style option.\n        widget: The target widget.\n\n    Returns:\n        The indent size in pixels.\n    \"\"\"\n    if metric == QStyle.PixelMetric.PM_TreeViewIndentation:\n        variant = getattr(widget, \"_variant_str\", \"default\")\n        style = self.model.get_style(\"QTreeView\", variant)\n        return int(style.get(\"indent\", 20))\n    return 0\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/tree_view.html#client.ayon_core.ui.drawers.tree_view.TreeViewDrawer.register_drawers","title":"<code>register_drawers()</code>","text":"<p>Register drawing functions for QTreeView primitives.</p> Source code in <code>client/ayon_core/ui/drawers/tree_view.py</code> <pre><code>def register_drawers(self) -&gt; dict:\n    \"\"\"Register drawing functions for QTreeView primitives.\"\"\"\n    return {\n        enum_to_str(\n            QStyle.PrimitiveElement,\n            QStyle.PrimitiveElement.PE_IndicatorBranch,\n            \"QTreeView\",\n        ): self.draw_branch_indicator,\n        enum_to_str(\n            QStyle.PrimitiveElement,\n            QStyle.PrimitiveElement.PE_PanelScrollAreaCorner,\n            \"QTreeView\",\n        ): self.draw_scrollbar_corner,\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/drawers/tree_view.html#client.ayon_core.ui.drawers.tree_view.TreeViewDrawer.register_metrics","title":"<code>register_metrics()</code>","text":"<p>Register pixel metric functions for QTreeView.</p> Source code in <code>client/ayon_core/ui/drawers/tree_view.py</code> <pre><code>def register_metrics(self) -&gt; dict:\n    \"\"\"Register pixel metric functions for QTreeView.\"\"\"\n    return {\n        enum_to_str(\n            QStyle.PixelMetric,\n            QStyle.PixelMetric.PM_TreeViewIndentation,\n            \"QTreeView\",\n        ): self.get_metric,\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/preview_utils/index.html","title":"preview_utils","text":""},{"location":"autoapi/client/ayon_core/ui/preview_utils/index.html#client.ayon_core.ui.preview_utils.get_test_data_dir","title":"<code>get_test_data_dir()</code>","text":"<p>As we moved all resources from <code>tests/client/ayon_core/ui</code> to <code>tests/client/ayon_core/ui/test_data</code> we need to be able to find the test data directory.</p> Source code in <code>client/ayon_core/ui/preview_utils/utils.py</code> <pre><code>def get_test_data_dir() -&gt; Path:\n    \"\"\"\n    As we moved all resources\n    from `tests/client/ayon_core/ui` to `tests/client/ayon_core/ui/test_data`\n    we need to be able to find the test data directory.\n    \"\"\"\n\n    # Relative to repo root path tests/client/ayon_core/ui/test_data\n    return (\n        REPO_ROOT / \"tests\" / \"client\" / \"ayon_core\" / \"ui\" / \"test_data\"\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/preview_utils/index.html#client.ayon_core.ui.preview_utils.preview_widget","title":"<code>preview_widget(create_widget_func, style=Style.AYONStyleOverCSS)</code>","text":"<p>Main function to run the Qt test.</p> Source code in <code>client/ayon_core/ui/preview_utils/utils.py</code> <pre><code>def preview_widget(\n    create_widget_func, style: Style = Style.AYONStyleOverCSS\n) -&gt; None:\n    \"\"\"Main function to run the Qt test.\"\"\"\n    app = QtWidgets.QApplication(sys.argv)\n\n    if style == Style.CSS:\n        # Set old RV dark theme for the application\n        app.setStyleSheet(_load_rv_stylesheet())\n    elif style == Style.AYONStyle:\n        app.setStyle(get_ayon_style())\n    elif style == Style.AYONStyleOverCSS:\n        app.setStyleSheet(_load_rv_stylesheet())\n\n    # Create and show the test widget\n    widget = create_widget_func()\n\n    if style == Style.AYONStyleOverCSS:\n        widget.setStyle(get_ayon_style())\n\n    widget.show()\n\n    print(\"Qt widget preview started. Close the window to exit.\")\n    return app.exec()\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/preview_utils/utils.html","title":"utils","text":""},{"location":"autoapi/client/ayon_core/ui/preview_utils/utils.html#client.ayon_core.ui.preview_utils.utils.get_test_data_dir","title":"<code>get_test_data_dir()</code>","text":"<p>As we moved all resources from <code>tests/client/ayon_core/ui</code> to <code>tests/client/ayon_core/ui/test_data</code> we need to be able to find the test data directory.</p> Source code in <code>client/ayon_core/ui/preview_utils/utils.py</code> <pre><code>def get_test_data_dir() -&gt; Path:\n    \"\"\"\n    As we moved all resources\n    from `tests/client/ayon_core/ui` to `tests/client/ayon_core/ui/test_data`\n    we need to be able to find the test data directory.\n    \"\"\"\n\n    # Relative to repo root path tests/client/ayon_core/ui/test_data\n    return (\n        REPO_ROOT / \"tests\" / \"client\" / \"ayon_core\" / \"ui\" / \"test_data\"\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_core/ui/preview_utils/utils.html#client.ayon_core.ui.preview_utils.utils.preview_widget","title":"<code>preview_widget(create_widget_func, style=Style.AYONStyleOverCSS)</code>","text":"<p>Main function to run the Qt test.</p> Source code in <code>client/ayon_core/ui/preview_utils/utils.py</code> <pre><code>def preview_widget(\n    create_widget_func, style: Style = Style.AYONStyleOverCSS\n) -&gt; None:\n    \"\"\"Main function to run the Qt test.\"\"\"\n    app = QtWidgets.QApplication(sys.argv)\n\n    if style == Style.CSS:\n        # Set old RV dark theme for the application\n        app.setStyleSheet(_load_rv_stylesheet())\n    elif style == Style.AYONStyle:\n        app.setStyle(get_ayon_style())\n    elif style == Style.AYONStyleOverCSS:\n        app.setStyleSheet(_load_rv_stylesheet())\n\n    # Create and show the test widget\n    widget = create_widget_func()\n\n    if style == Style.AYONStyleOverCSS:\n        widget.setStyle(get_ayon_style())\n\n    widget.show()\n\n    print(\"Qt widget preview started. Close the window to exit.\")\n    return app.exec()\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/index.html","title":"vendor","text":""},{"location":"autoapi/client/ayon_core/vendor/python/index.html","title":"python","text":""},{"location":"autoapi/client/ayon_core/vendor/python/qargparse.html","title":"qargparse","text":"The required <code>Qt</code> module has changed to use the one that vendorized. <p>Remember to change to relative import when updating this.</p>"},{"location":"autoapi/client/ayon_core/vendor/python/qargparse.html#client.ayon_core.vendor.python.qargparse.Boolean","title":"<code>Boolean</code>","text":"<p>               Bases: <code>QArgument</code></p> <p>Boolean type user interface</p> <p>Presented by <code>QtWidgets.QCheckBox</code>.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The name of argument</p> required <code>label</code> <code>str</code> <p>Display name, convert from <code>name</code> if not given</p> required <code>help</code> <code>str</code> <p>Tool tip message of this argument</p> required <code>default</code> <code>bool</code> <p>Argument's default value, default None</p> <code>None</code> <code>enabled</code> <code>bool</code> <p>Whether to enable this widget, default True</p> required Source code in <code>client/ayon_core/vendor/python/qargparse.py</code> <pre><code>class Boolean(QArgument):\n    \"\"\"Boolean type user interface\n\n    Presented by `QtWidgets.QCheckBox`.\n\n    Arguments:\n        name (str): The name of argument\n        label (str, optional): Display name, convert from `name` if not given\n        help (str, optional): Tool tip message of this argument\n        default (bool, optional): Argument's default value, default None\n        enabled (bool, optional): Whether to enable this widget, default True\n\n    \"\"\"\n    def create(self):\n        widget = QtWidgets.QCheckBox()\n        widget.clicked.connect(self.changed.emit)\n\n        if isinstance(self, Tristate):\n            self._read = lambda: widget.checkState()\n            state = {\n                0: QtCore.Qt.Unchecked,\n                1: QtCore.Qt.PartiallyChecked,\n                2: QtCore.Qt.Checked,\n                \"1\": QtCore.Qt.PartiallyChecked,\n                \"0\": QtCore.Qt.Unchecked,\n                \"2\": QtCore.Qt.Checked,\n            }\n        else:\n            self._read = lambda: bool(widget.checkState())\n            state = {\n                None: QtCore.Qt.Unchecked,\n\n                0: QtCore.Qt.Unchecked,\n                1: QtCore.Qt.Checked,\n                2: QtCore.Qt.Checked,\n\n                \"0\": QtCore.Qt.Unchecked,\n                \"1\": QtCore.Qt.Checked,\n                \"2\": QtCore.Qt.Checked,\n\n                # May be stored as string, if used with QSettings(..IniFormat)\n                \"false\": QtCore.Qt.Unchecked,\n                \"true\": QtCore.Qt.Checked,\n            }\n\n        self._write = lambda value: widget.setCheckState(state[value])\n        widget.clicked.connect(self.changed.emit)\n\n        if self[\"default\"] is not None:\n            self._write(self[\"default\"])\n\n        return widget\n\n    def read(self):\n        return self._read()\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/qargparse.html#client.ayon_core.vendor.python.qargparse.Button","title":"<code>Button</code>","text":"<p>               Bases: <code>QArgument</code></p> <p>Button type user interface</p> <p>Presented by <code>QtWidgets.QPushButton</code>.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The name of argument</p> required <code>label</code> <code>str</code> <p>Display name, convert from <code>name</code> if not given</p> required <code>help</code> <code>str</code> <p>Tool tip message of this argument</p> required <code>default</code> <code>bool</code> <p>Argument's default value, default None</p> <code>None</code> <code>enabled</code> <code>bool</code> <p>Whether to enable this widget, default True</p> required Source code in <code>client/ayon_core/vendor/python/qargparse.py</code> <pre><code>class Button(QArgument):\n    \"\"\"Button type user interface\n\n    Presented by `QtWidgets.QPushButton`.\n\n    Arguments:\n        name (str): The name of argument\n        label (str, optional): Display name, convert from `name` if not given\n        help (str, optional): Tool tip message of this argument\n        default (bool, optional): Argument's default value, default None\n        enabled (bool, optional): Whether to enable this widget, default True\n\n    \"\"\"\n    label = False\n\n    def create(self):\n        widget = QtWidgets.QPushButton(self[\"label\"])\n        widget.clicked.connect(self.changed.emit)\n\n        state = [\n            QtCore.Qt.Unchecked,\n            QtCore.Qt.Checked,\n        ]\n\n        if isinstance(self, Toggle):\n            widget.setCheckable(True)\n            if hasattr(widget, \"isChecked\"):\n                self._read = lambda: state[int(widget.isChecked())]\n                self._write = (\n                    lambda value: widget.setChecked(value)\n                )\n            else:\n                self._read = lambda: widget.checkState()\n                self._write = (\n                    lambda value: widget.setCheckState(state[int(value)])\n                )\n        else:\n            self._read = lambda: \"clicked\"\n            self._write = lambda value: None\n\n        if self[\"default\"] is not None:\n            self._write(self[\"default\"])\n\n        return widget\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/qargparse.html#client.ayon_core.vendor.python.qargparse.Choice","title":"<code>Choice</code>","text":"<p>               Bases: <code>QArgument</code></p> <p>Argument user interface for selecting one from list</p> <p>Presented by <code>QtWidgets.QListView</code>.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The name of argument</p> required <code>label</code> <code>str</code> <p>Display name, convert from <code>name</code> if not given</p> required <code>help</code> <code>str</code> <p>Tool tip message of this argument</p> required <code>items</code> <code>list</code> <p>List of strings for select, default <code>[\"Empty\"]</code></p> required <code>default</code> <code>str</code> <p>Default item in <code>items</code>, use first of <code>items</code> if not given.</p> required <code>enabled</code> <code>bool</code> <p>Whether to enable this widget, default True</p> required Source code in <code>client/ayon_core/vendor/python/qargparse.py</code> <pre><code>class Choice(QArgument):\n    \"\"\"Argument user interface for selecting one from list\n\n    Presented by `QtWidgets.QListView`.\n\n    Arguments:\n        name (str): The name of argument\n        label (str, optional): Display name, convert from `name` if not given\n        help (str, optional): Tool tip message of this argument\n        items (list, optional): List of strings for select, default `[\"Empty\"]`\n        default (str, optional): Default item in `items`, use first of `items`\n            if not given.\n        enabled (bool, optional): Whether to enable this widget, default True\n\n    \"\"\"\n    def __init__(self, name, **kwargs):\n        kwargs[\"items\"] = kwargs.get(\"items\", [\"Empty\"])\n        kwargs[\"default\"] = kwargs.pop(\"default\", kwargs[\"items\"][0])\n        super(Choice, self).__init__(name, **kwargs)\n\n    def index(self, value):\n        \"\"\"Return numerical equivalent to self.read()\"\"\"\n        return self[\"items\"].index(value)\n\n    def create(self):\n        def on_changed(selected, deselected):\n            try:\n                selected = selected.indexes()[0]\n            except IndexError:\n                # At least one item must be selected at all times\n                selected = deselected.indexes()[0]\n\n            value = selected.data(QtCore.Qt.DisplayRole)\n            set_current(value)\n            self.changed.emit()\n\n        def set_current(current):\n            options = model.stringList()\n\n            if current == \"Empty\":\n                index = 0\n            else:\n                for index, member in enumerate(options):\n                    if member == current:\n                        break\n                else:\n                    raise ValueError(\n                        \"%s not a member of %s\" % (current, options)\n                    )\n\n            qindex = model.index(index, 0, QtCore.QModelIndex())\n            smodel.setCurrentIndex(qindex, smodel.ClearAndSelect)\n            self[\"current\"] = options[index]\n\n        def reset(items, default=None):\n            items = items or [\"Empty\"]\n            model.setStringList(items)\n            set_current(default or items[0])\n\n        model = QtCore.QStringListModel()\n        widget = QtWidgets.QListView()\n        widget.setModel(model)\n        widget.setEditTriggers(QtWidgets.QAbstractItemView.NoEditTriggers)\n        widget.setSelectionMode(QtWidgets.QAbstractItemView.SingleSelection)\n        smodel = widget.selectionModel()\n        smodel.selectionChanged.connect(on_changed)\n\n        self._read = lambda: self[\"current\"]\n        self._write = lambda value: set_current(value)\n        self.reset = reset\n\n        reset(self[\"items\"], self[\"default\"])\n\n        return widget\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/qargparse.html#client.ayon_core.vendor.python.qargparse.Choice.index","title":"<code>index(value)</code>","text":"<p>Return numerical equivalent to self.read()</p> Source code in <code>client/ayon_core/vendor/python/qargparse.py</code> <pre><code>def index(self, value):\n    \"\"\"Return numerical equivalent to self.read()\"\"\"\n    return self[\"items\"].index(value)\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/qargparse.html#client.ayon_core.vendor.python.qargparse.Color","title":"<code>Color</code>","text":"<p>               Bases: <code>String</code></p> <p>Color type user interface</p> <p>A subclass of <code>qargparse.String</code>, not production ready.</p> Source code in <code>client/ayon_core/vendor/python/qargparse.py</code> <pre><code>class Color(String):\n    \"\"\"Color type user interface\n\n    A subclass of `qargparse.String`, not production ready.\n\n    \"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/qargparse.html#client.ayon_core.vendor.python.qargparse.Double3","title":"<code>Double3</code>","text":"<p>               Bases: <code>QArgument</code></p> <p>Double3 type user interface</p> <p>Presented by three <code>QtWidgets.QLineEdit</code> widget with <code>QDoubleValidator</code> installed.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The name of argument</p> required <code>label</code> <code>str</code> <p>Display name, convert from <code>name</code> if not given</p> required <code>help</code> <code>str</code> <p>Tool tip message of this argument</p> required <code>default</code> <code>tuple or list</code> <p>Default (0, 0, 0).</p> <code>None</code> <code>enabled</code> <code>bool</code> <p>Whether to enable this widget, default True</p> required Source code in <code>client/ayon_core/vendor/python/qargparse.py</code> <pre><code>class Double3(QArgument):\n    \"\"\"Double3 type user interface\n\n    Presented by three `QtWidgets.QLineEdit` widget with `QDoubleValidator`\n    installed.\n\n    Arguments:\n        name (str): The name of argument\n        label (str, optional): Display name, convert from `name` if not given\n        help (str, optional): Tool tip message of this argument\n        default (tuple or list, optional): Default (0, 0, 0).\n        enabled (bool, optional): Whether to enable this widget, default True\n\n    \"\"\"\n    default = (0, 0, 0)\n\n    def create(self):\n        widget = QtWidgets.QWidget()\n        layout = QtWidgets.QHBoxLayout(widget)\n        layout.setContentsMargins(0, 0, 0, 0)\n        x, y, z = (self.child_arg(layout, i) for i in range(3))\n\n        self._read = lambda: (\n            float(x.text()), float(y.text()), float(z.text()))\n        self._write = lambda value: [\n            w.setText(str(float(v))) for w, v in zip([x, y, z], value)]\n\n        if self[\"default\"] != self.default:\n            self._write(self[\"default\"])\n\n        return widget\n\n    def child_arg(self, layout, index):\n        widget = QtWidgets.QLineEdit()\n        widget.setValidator(QtGui.QDoubleValidator())\n\n        default = str(float(self[\"default\"][index]))\n        widget.setText(default)\n\n        def focusOutEvent(event):\n            if not widget.text():\n                widget.setText(default)  # Ensure value exists for `_read`\n            QtWidgets.QLineEdit.focusOutEvent(widget, event)\n        widget.focusOutEvent = focusOutEvent\n\n        widget.editingFinished.connect(self.changed.emit)\n        widget.returnPressed.connect(widget.editingFinished.emit)\n\n        layout.addWidget(widget)\n\n        return widget\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/qargparse.html#client.ayon_core.vendor.python.qargparse.Enum","title":"<code>Enum</code>","text":"<p>               Bases: <code>QArgument</code></p> <p>Argument user interface for selecting one from dropdown list</p> <p>Presented by <code>QtWidgets.QComboBox</code>.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The name of argument</p> required <code>label</code> <code>str</code> <p>Display name, convert from <code>name</code> if not given</p> required <code>help</code> <code>str</code> <p>Tool tip message of this argument</p> required <code>items</code> <code>list</code> <p>List of strings for select, default <code>[]</code></p> required <code>default</code> <code>int</code> <p>Index of default item, use first of <code>items</code> if not given.</p> required <code>enabled</code> <code>bool</code> <p>Whether to enable this widget, default True</p> required Source code in <code>client/ayon_core/vendor/python/qargparse.py</code> <pre><code>class Enum(QArgument):\n    \"\"\"Argument user interface for selecting one from dropdown list\n\n    Presented by `QtWidgets.QComboBox`.\n\n    Arguments:\n        name (str): The name of argument\n        label (str, optional): Display name, convert from `name` if not given\n        help (str, optional): Tool tip message of this argument\n        items (list, optional): List of strings for select, default `[]`\n        default (int, optional): Index of default item, use first of `items`\n            if not given.\n        enabled (bool, optional): Whether to enable this widget, default True\n\n    \"\"\"\n    def __init__(self, name, **kwargs):\n        kwargs[\"default\"] = kwargs.pop(\"default\", 0)\n        kwargs[\"items\"] = kwargs.get(\"items\", [])\n\n        assert isinstance(kwargs[\"items\"], (tuple, list)), (\n            \"items must be list\"\n        )\n\n        super(Enum, self).__init__(name, **kwargs)\n\n    def create(self):\n        widget = QtWidgets.QComboBox()\n        widget.addItems(self[\"items\"])\n        widget.currentIndexChanged.connect(\n            lambda index: self.changed.emit())\n\n        self._read = lambda: widget.currentText()\n        self._write = lambda value: widget.setCurrentIndex(value)\n\n        if self[\"default\"] is not None:\n            self._write(self[\"default\"])\n\n        return widget\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/qargparse.html#client.ayon_core.vendor.python.qargparse.Float","title":"<code>Float</code>","text":"<p>               Bases: <code>Number</code></p> <p>Float type user interface</p> <p>A subclass of <code>qargparse.Number</code>, presented by <code>QtWidgets.QDoubleSpinBox</code>.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The name of argument</p> required <code>label</code> <code>str</code> <p>Display name, convert from <code>name</code> if not given</p> required <code>help</code> <code>str</code> <p>Tool tip message of this argument</p> required <code>default</code> <code>float</code> <p>Argument's default value, default 0.0</p> <code>None</code> <code>min</code> <code>float</code> <p>Argument's minimum value, default 0.0</p> required <code>max</code> <code>float</code> <p>Argument's maximum value, default 99.99</p> required <code>enabled</code> <code>bool</code> <p>Whether to enable this widget, default True</p> required Source code in <code>client/ayon_core/vendor/python/qargparse.py</code> <pre><code>class Float(Number):\n    \"\"\"Float type user interface\n\n    A subclass of `qargparse.Number`, presented by `QtWidgets.QDoubleSpinBox`.\n\n    Arguments:\n        name (str): The name of argument\n        label (str, optional): Display name, convert from `name` if not given\n        help (str, optional): Tool tip message of this argument\n        default (float, optional): Argument's default value, default 0.0\n        min (float, optional): Argument's minimum value, default 0.0\n        max (float, optional): Argument's maximum value, default 99.99\n        enabled (bool, optional): Whether to enable this widget, default True\n\n    \"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/qargparse.html#client.ayon_core.vendor.python.qargparse.Info","title":"<code>Info</code>","text":"<p>               Bases: <code>String</code></p> <p>String type user interface but read-only</p> <p>A subclass of <code>qargparse.String</code>, presented by <code>QtWidgets.QLineEdit</code>.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The name of argument</p> required <code>label</code> <code>str</code> <p>Display name, convert from <code>name</code> if not given</p> required <code>help</code> <code>str</code> <p>Tool tip message of this argument</p> required <code>default</code> <code>str</code> <p>Argument's default value, default None</p> required <code>enabled</code> <code>bool</code> <p>Whether to enable this widget, default True</p> required Source code in <code>client/ayon_core/vendor/python/qargparse.py</code> <pre><code>class Info(String):\n    \"\"\"String type user interface but read-only\n\n    A subclass of `qargparse.String`, presented by `QtWidgets.QLineEdit`.\n\n    Arguments:\n        name (str): The name of argument\n        label (str, optional): Display name, convert from `name` if not given\n        help (str, optional): Tool tip message of this argument\n        default (str, optional): Argument's default value, default None\n        enabled (bool, optional): Whether to enable this widget, default True\n\n    \"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/qargparse.html#client.ayon_core.vendor.python.qargparse.InfoList","title":"<code>InfoList</code>","text":"<p>               Bases: <code>QArgument</code></p> <p>String list type user interface</p> <p>Presented by <code>QtWidgets.QListView</code>, not production ready.</p> Source code in <code>client/ayon_core/vendor/python/qargparse.py</code> <pre><code>class InfoList(QArgument):\n    \"\"\"String list type user interface\n\n    Presented by `QtWidgets.QListView`, not production ready.\n\n    \"\"\"\n    def __init__(self, name, **kwargs):\n        kwargs[\"default\"] = kwargs.pop(\"default\", [\"Empty\"])\n        super(InfoList, self).__init__(name, **kwargs)\n\n    def create(self):\n        class Model(QtCore.QStringListModel):\n            def data(self, index, role):\n                return super(Model, self).data(index, role)\n\n        model = QtCore.QStringListModel(self[\"default\"])\n        widget = QtWidgets.QListView()\n        widget.setModel(model)\n        widget.setEditTriggers(QtWidgets.QAbstractItemView.NoEditTriggers)\n\n        self._read = lambda: model.stringList()\n        self._write = lambda value: model.setStringList(value)\n\n        return widget\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/qargparse.html#client.ayon_core.vendor.python.qargparse.Integer","title":"<code>Integer</code>","text":"<p>               Bases: <code>Number</code></p> <p>Integer type user interface</p> <p>A subclass of <code>qargparse.Number</code>, presented by <code>QtWidgets.QSpinBox</code>.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The name of argument</p> required <code>label</code> <code>str</code> <p>Display name, convert from <code>name</code> if not given</p> required <code>help</code> <code>str</code> <p>Tool tip message of this argument</p> required <code>default</code> <code>int</code> <p>Argument's default value, default 0</p> <code>None</code> <code>min</code> <code>int</code> <p>Argument's minimum value, default 0</p> required <code>max</code> <code>int</code> <p>Argument's maximum value, default 99</p> required <code>enabled</code> <code>bool</code> <p>Whether to enable this widget, default True</p> required Source code in <code>client/ayon_core/vendor/python/qargparse.py</code> <pre><code>class Integer(Number):\n    \"\"\"Integer type user interface\n\n    A subclass of `qargparse.Number`, presented by `QtWidgets.QSpinBox`.\n\n    Arguments:\n        name (str): The name of argument\n        label (str, optional): Display name, convert from `name` if not given\n        help (str, optional): Tool tip message of this argument\n        default (int, optional): Argument's default value, default 0\n        min (int, optional): Argument's minimum value, default 0\n        max (int, optional): Argument's maximum value, default 99\n        enabled (bool, optional): Whether to enable this widget, default True\n\n    \"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/qargparse.html#client.ayon_core.vendor.python.qargparse.Number","title":"<code>Number</code>","text":"<p>               Bases: <code>QArgument</code></p> <p>Base class of numeric type user interface</p> Source code in <code>client/ayon_core/vendor/python/qargparse.py</code> <pre><code>class Number(QArgument):\n    \"\"\"Base class of numeric type user interface\"\"\"\n    default = 0\n\n    def create(self):\n        if isinstance(self, Float):\n            widget = QtWidgets.QDoubleSpinBox()\n            widget.setMinimum(self._data.get(\"min\", 0.0))\n            widget.setMaximum(self._data.get(\"max\", 99.99))\n        else:\n            widget = QtWidgets.QSpinBox()\n            widget.setMinimum(self._data.get(\"min\", 0))\n            widget.setMaximum(self._data.get(\"max\", 99))\n\n        widget.editingFinished.connect(self.changed.emit)\n        self._read = lambda: widget.value()\n        self._write = lambda value: widget.setValue(value)\n\n        if self[\"default\"] != self.default:\n            self._write(self[\"default\"])\n\n        return widget\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/qargparse.html#client.ayon_core.vendor.python.qargparse.QArgument","title":"<code>QArgument</code>","text":"<p>               Bases: <code>QObject</code></p> <p>Base class of argument user interface</p> Source code in <code>client/ayon_core/vendor/python/qargparse.py</code> <pre><code>class QArgument(QtCore.QObject):\n    \"\"\"Base class of argument user interface\n    \"\"\"\n    changed = QtCore.Signal()\n\n    # Provide a left-hand side label for this argument\n    label = True\n    # For defining default value for each argument type\n    default = None\n\n    def __init__(self, name, default=None, **kwargs):\n        super(QArgument, self).__init__(kwargs.pop(\"parent\", None))\n\n        kwargs[\"name\"] = name\n        kwargs[\"label\"] = kwargs.get(\"label\", camel_to_title(name))\n        kwargs[\"default\"] = self.default if default is None else default\n        kwargs[\"help\"] = kwargs.get(\"help\", \"\")\n        kwargs[\"read\"] = kwargs.get(\"read\")\n        kwargs[\"write\"] = kwargs.get(\"write\")\n        kwargs[\"enabled\"] = bool(kwargs.get(\"enabled\", True))\n        kwargs[\"edited\"] = False\n\n        self._data = kwargs\n\n    def __str__(self):\n        return self[\"name\"]\n\n    def __repr__(self):\n        return \"%s(\\\"%s\\\")\" % (type(self).__name__, self[\"name\"])\n\n    def __getitem__(self, key):\n        return self._data[key]\n\n    def __setitem__(self, key, value):\n        self._data[key] = value\n\n    def __eq__(self, other):\n        if isinstance(other, _basestring):\n            return self[\"name\"] == other\n        return super(QArgument, self).__eq__(other)\n\n    def __ne__(self, other):\n        return not self.__eq__(other)\n\n    def create(self):\n        return QtWidgets.QWidget()\n\n    def read(self):\n        return self._read()\n\n    def write(self, value):\n        self._write(value)\n        self.changed.emit()\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/qargparse.html#client.ayon_core.vendor.python.qargparse.QArgumentParser","title":"<code>QArgumentParser</code>","text":"<p>               Bases: <code>QWidget</code></p> <p>User interface arguments</p> <p>Parameters:</p> Name Type Description Default <code>arguments</code> <code>list</code> <p>Instances of QArgument</p> <code>None</code> <code>description</code> <code>str</code> <p>Long-form text of what this parser is for</p> <code>None</code> <code>storage</code> <code>QSettings</code> <p>Persistence to disk, providing value() and setValue() methods</p> <code>None</code> Source code in <code>client/ayon_core/vendor/python/qargparse.py</code> <pre><code>class QArgumentParser(QtWidgets.QWidget):\n    \"\"\"User interface arguments\n\n    Arguments:\n        arguments (list, optional): Instances of QArgument\n        description (str, optional): Long-form text of what this parser is for\n        storage (QSettings, optional): Persistence to disk, providing\n            value() and setValue() methods\n\n    \"\"\"\n\n    changed = QtCore.Signal(QtCore.QObject)  # A QArgument\n\n    def __init__(self,\n                 arguments=None,\n                 description=None,\n                 storage=None,\n                 parent=None):\n        super(QArgumentParser, self).__init__(parent)\n        self.setAttribute(QtCore.Qt.WA_StyledBackground)\n\n        # Create internal settings\n        if storage is True:\n            storage = QtCore.QSettings(\n                QtCore.QSettings.IniFormat,\n                QtCore.QSettings.UserScope,\n                __name__, \"QArgparse\",\n            )\n\n        if storage is not None:\n            _log.info(\"Storing settings @ %s\" % storage.fileName())\n\n        arguments = arguments or []\n\n        assert hasattr(arguments, \"__iter__\"), \"arguments must be iterable\"\n        assert isinstance(storage, (type(None), QtCore.QSettings)), (\n            \"storage must be of type QSettings\"\n        )\n\n        layout = QtWidgets.QGridLayout(self)\n        layout.setRowStretch(999, 1)\n\n        if description:\n            layout.addWidget(QtWidgets.QLabel(description), 0, 0, 1, 2)\n\n        self._row = 1\n        self._storage = storage\n        self._arguments = odict()\n        self._desciption = description\n\n        for arg in arguments or []:\n            self._addArgument(arg)\n\n        self.setStyleSheet(style)\n\n    def setDescription(self, text):\n        self._desciption.setText(text)\n\n    def addArgument(self, name, type=None, default=None, **kwargs):\n        # Infer type from default\n        if type is None and default is not None:\n            type = _type(default)\n\n        # Default to string\n        type = type or str\n\n        Argument = {\n            None: String,\n            int: Integer,\n            float: Float,\n            bool: Boolean,\n            str: String,\n            list: Enum,\n            tuple: Enum,\n        }.get(type, type)\n\n        arg = Argument(name, default=default, **kwargs)\n        self._addArgument(arg)\n        return arg\n\n    def _addArgument(self, arg):\n        if arg[\"name\"] in self._arguments:\n            raise ValueError(\"Duplicate argument '%s'\" % arg[\"name\"])\n\n        if self._storage is not None:\n            default = self._storage.value(arg[\"name\"])\n\n            if default:\n                if isinstance(arg, Boolean):\n                    default = bool({\n                        None: QtCore.Qt.Unchecked,\n\n                        0: QtCore.Qt.Unchecked,\n                        1: QtCore.Qt.Checked,\n                        2: QtCore.Qt.Checked,\n\n                        \"0\": QtCore.Qt.Unchecked,\n                        \"1\": QtCore.Qt.Checked,\n                        \"2\": QtCore.Qt.Checked,\n\n                        # May be stored as string, if used with IniFormat\n                        \"false\": QtCore.Qt.Unchecked,\n                        \"true\": QtCore.Qt.Checked,\n                    }.get(default))\n\n                arg[\"default\"] = default\n\n        arg.changed.connect(lambda: self.on_changed(arg))\n\n        label = (\n            QtWidgets.QLabel(arg[\"label\"])\n            if arg.label\n            else QtWidgets.QLabel()\n        )\n        widget = arg.create()\n        icon = qtawesome.icon(\"fa.refresh\", color=\"white\")\n        reset = QtWidgets.QPushButton(icon, \"\")  # default\n        reset.setToolTip(\"Reset\")\n        reset.setProperty(\"type\", \"reset\")\n        reset.clicked.connect(lambda: self.on_reset(arg))\n\n        # Shown on edit\n        reset.hide()\n\n        for widget in (label, widget):\n            widget.setToolTip(arg[\"help\"])\n            widget.setObjectName(arg[\"name\"])  # useful in CSS\n            widget.setProperty(\"type\", type(arg).__name__)\n            widget.setAttribute(QtCore.Qt.WA_StyledBackground)\n            widget.setEnabled(arg[\"enabled\"])\n\n        # Align label on top of row if widget is over two times heiger\n        height = (lambda w: w.sizeHint().height())\n        label_on_top = height(label) * 2 &lt; height(widget)\n        alignment = (QtCore.Qt.AlignTop,) if label_on_top else ()\n\n        layout = self.layout()\n        layout.addWidget(label, self._row, 0, *alignment)\n        layout.addWidget(widget, self._row, 1)\n        layout.addWidget(reset, self._row, 2, *alignment)\n        layout.setColumnStretch(1, 1)\n\n        def on_changed(*_):\n            reset.setVisible(arg[\"edited\"])\n\n        arg.changed.connect(on_changed)\n\n        self._row += 1\n        self._arguments[arg[\"name\"]] = arg\n\n    def clear(self):\n        assert self._storage, \"Cannot clear without persistent storage\"\n        self._storage.clear()\n        _log.info(\"Clearing settings @ %s\" % self._storage.fileName())\n\n    def find(self, name):\n        return self._arguments[name]\n\n    def on_reset(self, arg):\n        arg.write(arg[\"default\"])\n\n    def on_changed(self, arg):\n        arg[\"edited\"] = arg.read() != arg[\"default\"]\n        self.changed.emit(arg)\n\n    # Optional PEP08 syntax\n    add_argument = addArgument\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/qargparse.html#client.ayon_core.vendor.python.qargparse.Range","title":"<code>Range</code>","text":"<p>               Bases: <code>Number</code></p> <p>Range type user interface</p> <p>A subclass of <code>qargparse.Number</code>, not production ready.</p> Source code in <code>client/ayon_core/vendor/python/qargparse.py</code> <pre><code>class Range(Number):\n    \"\"\"Range type user interface\n\n    A subclass of `qargparse.Number`, not production ready.\n\n    \"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/qargparse.html#client.ayon_core.vendor.python.qargparse.Separator","title":"<code>Separator</code>","text":"<p>               Bases: <code>QArgument</code></p> <p>Visual separator</p> <p>Example:</p> <pre><code>item1\nitem2\n------------\nitem3\nitem4\n</code></pre> Source code in <code>client/ayon_core/vendor/python/qargparse.py</code> <pre><code>class Separator(QArgument):\n    \"\"\"Visual separator\n\n    Example:\n\n        item1\n        item2\n        ------------\n        item3\n        item4\n\n    \"\"\"\n\n    def create(self):\n        widget = QtWidgets.QWidget()\n\n        self._read = lambda: None\n        self._write = lambda value: None\n\n        return widget\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/qargparse.html#client.ayon_core.vendor.python.qargparse.String","title":"<code>String</code>","text":"<p>               Bases: <code>QArgument</code></p> <p>String type user interface</p> <p>Presented by <code>QtWidgets.QLineEdit</code>.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The name of argument</p> required <code>label</code> <code>str</code> <p>Display name, convert from <code>name</code> if not given</p> required <code>help</code> <code>str</code> <p>Tool tip message of this argument</p> required <code>default</code> <code>str</code> <p>Argument's default value, default None</p> required <code>placeholder</code> <code>str</code> <p>Placeholder message for the widget</p> required <code>enabled</code> <code>bool</code> <p>Whether to enable this widget, default True</p> required Source code in <code>client/ayon_core/vendor/python/qargparse.py</code> <pre><code>class String(QArgument):\n    \"\"\"String type user interface\n\n    Presented by `QtWidgets.QLineEdit`.\n\n    Arguments:\n        name (str): The name of argument\n        label (str, optional): Display name, convert from `name` if not given\n        help (str, optional): Tool tip message of this argument\n        default (str, optional): Argument's default value, default None\n        placeholder (str, optional): Placeholder message for the widget\n        enabled (bool, optional): Whether to enable this widget, default True\n\n    \"\"\"\n    def __init__(self, *args, **kwargs):\n        super(String, self).__init__(*args, **kwargs)\n        self._previous = None\n\n    def create(self):\n        widget = QtWidgets.QLineEdit()\n        widget.editingFinished.connect(self.onEditingFinished)\n        widget.returnPressed.connect(widget.editingFinished.emit)\n        self._read = lambda: widget.text()\n        self._write = lambda value: widget.setText(value)\n\n        if isinstance(self, Info):\n            widget.setReadOnly(True)\n        widget.setPlaceholderText(self._data.get(\"placeholder\", \"\"))\n\n        if self[\"default\"] is not None:\n            self._write(self[\"default\"])\n            self._previous = self[\"default\"]\n\n        return widget\n\n    def onEditingFinished(self):\n        current = self._read()\n\n        if current != self._previous:\n            self.changed.emit()\n            self._previous = current\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/qargparse.html#client.ayon_core.vendor.python.qargparse.Toggle","title":"<code>Toggle</code>","text":"<p>               Bases: <code>Button</code></p> <p>Checkable <code>Button</code> type user interface</p> <p>Presented by <code>QtWidgets.QPushButton</code>.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>The name of argument</p> required <code>label</code> <code>str</code> <p>Display name, convert from <code>name</code> if not given</p> required <code>help</code> <code>str</code> <p>Tool tip message of this argument</p> required <code>default</code> <code>bool</code> <p>Argument's default value, default None</p> <code>None</code> <code>enabled</code> <code>bool</code> <p>Whether to enable this widget, default True</p> required Source code in <code>client/ayon_core/vendor/python/qargparse.py</code> <pre><code>class Toggle(Button):\n    \"\"\"Checkable `Button` type user interface\n\n    Presented by `QtWidgets.QPushButton`.\n\n    Arguments:\n        name (str): The name of argument\n        label (str, optional): Display name, convert from `name` if not given\n        help (str, optional): Tool tip message of this argument\n        default (bool, optional): Argument's default value, default None\n        enabled (bool, optional): Whether to enable this widget, default True\n\n    \"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/qargparse.html#client.ayon_core.vendor.python.qargparse.Tristate","title":"<code>Tristate</code>","text":"<p>               Bases: <code>QArgument</code></p> <p>Not implemented</p> Source code in <code>client/ayon_core/vendor/python/qargparse.py</code> <pre><code>class Tristate(QArgument):\n    \"\"\"Not implemented\"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/qargparse.html#client.ayon_core.vendor.python.qargparse.camelToTitle","title":"<code>camelToTitle(text)</code>","text":"<p>Convert camelCase <code>text</code> to Title Case</p> Example <p>camelToTitle(\"mixedCase\") \"Mixed Case\" camelToTitle(\"myName\") \"My Name\" camelToTitle(\"you\") \"You\" camelToTitle(\"You\") \"You\" camelToTitle(\"This is That\") \"This Is That\"</p> Source code in <code>client/ayon_core/vendor/python/qargparse.py</code> <pre><code>def camelToTitle(text):\n    \"\"\"Convert camelCase `text` to Title Case\n\n    Example:\n        &gt;&gt;&gt; camelToTitle(\"mixedCase\")\n        \"Mixed Case\"\n        &gt;&gt;&gt; camelToTitle(\"myName\")\n        \"My Name\"\n        &gt;&gt;&gt; camelToTitle(\"you\")\n        \"You\"\n        &gt;&gt;&gt; camelToTitle(\"You\")\n        \"You\"\n        &gt;&gt;&gt; camelToTitle(\"This is That\")\n        \"This Is That\"\n\n    \"\"\"\n\n    return re.sub(\n        r\"((?&lt;=[a-z])[A-Z]|(?&lt;!\\A)[A-Z](?=[a-z]))\",\n        r\" \\1\", text\n    ).title()\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/qtmaterialsymbols/index.html","title":"qtmaterialsymbols","text":""},{"location":"autoapi/client/ayon_core/vendor/python/qtmaterialsymbols/index.html#client.ayon_core.vendor.python.qtmaterialsymbols.IconicFont","title":"<code>IconicFont</code>","text":"<p>               Bases: <code>QObject</code></p> <p>Main class for managing icons.</p> Source code in <code>client/ayon_core/vendor/python/qtmaterialsymbols/iconic_font.py</code> <pre><code>class IconicFont(QtCore.QObject):\n    \"\"\"Main class for managing icons.\"\"\"\n\n    def __init__(self):\n        super().__init__()\n        self._painter = CharIconPainter()\n        self._icon_cache = {}\n\n    def get_charmap(self) -&gt; Dict[str, int]:\n        return get_char_mapping()\n\n    def get_font(self, size: int, filled: bool) -&gt; QtGui.QFont:\n        \"\"\"Return a QFont corresponding to the given size.\"\"\"\n        if filled:\n            font = QtGui.QFont(_get_font_name_filled())\n        else:\n            font = QtGui.QFont(_get_font_name())\n        font.setPixelSize(round(size))\n        return font\n\n    def get_icon_with_options(self, options: IconOptions) -&gt; QtGui.QIcon:\n        \"\"\"Return a QIcon object corresponding to the provided icon name.\"\"\"\n        if QtWidgets.QApplication.instance() is None:\n            warnings.warn(\n                \"You need to have a running QApplication!\"\n            )\n            return QtGui.QIcon()\n\n        cache_key = options.identifier\n        if cache_key in self._icon_cache:\n            return self._icon_cache[cache_key]\n\n        output = self._icon_by_painter(self._painter, options)\n        self._icon_cache[cache_key] = output\n        return output\n\n    def get_icon(\n        self,\n        icon_name: Optional[str] = None,\n        color: Optional[Union[QtGui.QColor, str]] = None,\n        opacity: Optional[float] = None,\n        scale_factor: Optional[float] = None,\n        offset: Optional[Position] = None,\n        hflip: bool = False,\n        vflip: bool = False,\n        rotate: int = 0,\n        fill: bool = True,\n        **kwargs\n    ) -&gt; QtGui.QIcon:\n        \"\"\"Return a QIcon object corresponding to the provided icon name.\"\"\"\n        if QtWidgets.QApplication.instance() is None:\n            warnings.warn(\n                \"You need to have a running QApplication!\"\n            )\n            return QtGui.QIcon()\n\n        options = IconOptions.from_data(\n            icon_name=icon_name,\n            color=color,\n            opacity=opacity,\n            scale_factor=scale_factor,\n            offset=offset,\n            hflip=hflip,\n            vflip=vflip,\n            rotate=rotate,\n            fill=fill,\n            **kwargs\n        )\n        cache_key = options.identifier\n        if cache_key in self._icon_cache:\n            return self._icon_cache[cache_key]\n        return self.get_icon_with_options(options)\n\n    def _icon_by_painter(self, painter, options):\n        \"\"\"Return the icon corresponding to the given painter.\"\"\"\n        engine = CharIconEngine(self, painter, options)\n        return QtGui.QIcon(engine)\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/qtmaterialsymbols/index.html#client.ayon_core.vendor.python.qtmaterialsymbols.IconicFont.get_font","title":"<code>get_font(size, filled)</code>","text":"<p>Return a QFont corresponding to the given size.</p> Source code in <code>client/ayon_core/vendor/python/qtmaterialsymbols/iconic_font.py</code> <pre><code>def get_font(self, size: int, filled: bool) -&gt; QtGui.QFont:\n    \"\"\"Return a QFont corresponding to the given size.\"\"\"\n    if filled:\n        font = QtGui.QFont(_get_font_name_filled())\n    else:\n        font = QtGui.QFont(_get_font_name())\n    font.setPixelSize(round(size))\n    return font\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/qtmaterialsymbols/index.html#client.ayon_core.vendor.python.qtmaterialsymbols.IconicFont.get_icon","title":"<code>get_icon(icon_name=None, color=None, opacity=None, scale_factor=None, offset=None, hflip=False, vflip=False, rotate=0, fill=True, **kwargs)</code>","text":"<p>Return a QIcon object corresponding to the provided icon name.</p> Source code in <code>client/ayon_core/vendor/python/qtmaterialsymbols/iconic_font.py</code> <pre><code>def get_icon(\n    self,\n    icon_name: Optional[str] = None,\n    color: Optional[Union[QtGui.QColor, str]] = None,\n    opacity: Optional[float] = None,\n    scale_factor: Optional[float] = None,\n    offset: Optional[Position] = None,\n    hflip: bool = False,\n    vflip: bool = False,\n    rotate: int = 0,\n    fill: bool = True,\n    **kwargs\n) -&gt; QtGui.QIcon:\n    \"\"\"Return a QIcon object corresponding to the provided icon name.\"\"\"\n    if QtWidgets.QApplication.instance() is None:\n        warnings.warn(\n            \"You need to have a running QApplication!\"\n        )\n        return QtGui.QIcon()\n\n    options = IconOptions.from_data(\n        icon_name=icon_name,\n        color=color,\n        opacity=opacity,\n        scale_factor=scale_factor,\n        offset=offset,\n        hflip=hflip,\n        vflip=vflip,\n        rotate=rotate,\n        fill=fill,\n        **kwargs\n    )\n    cache_key = options.identifier\n    if cache_key in self._icon_cache:\n        return self._icon_cache[cache_key]\n    return self.get_icon_with_options(options)\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/qtmaterialsymbols/index.html#client.ayon_core.vendor.python.qtmaterialsymbols.IconicFont.get_icon_with_options","title":"<code>get_icon_with_options(options)</code>","text":"<p>Return a QIcon object corresponding to the provided icon name.</p> Source code in <code>client/ayon_core/vendor/python/qtmaterialsymbols/iconic_font.py</code> <pre><code>def get_icon_with_options(self, options: IconOptions) -&gt; QtGui.QIcon:\n    \"\"\"Return a QIcon object corresponding to the provided icon name.\"\"\"\n    if QtWidgets.QApplication.instance() is None:\n        warnings.warn(\n            \"You need to have a running QApplication!\"\n        )\n        return QtGui.QIcon()\n\n    cache_key = options.identifier\n    if cache_key in self._icon_cache:\n        return self._icon_cache[cache_key]\n\n    output = self._icon_by_painter(self._painter, options)\n    self._icon_cache[cache_key] = output\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/qtmaterialsymbols/browser.html","title":"browser","text":""},{"location":"autoapi/client/ayon_core/vendor/python/qtmaterialsymbols/browser.html#client.ayon_core.vendor.python.qtmaterialsymbols.browser.IconBrowser","title":"<code>IconBrowser</code>","text":"<p>               Bases: <code>QMainWindow</code></p> <p>A small browser window that allows the user to search through all icons from the available version of QtAwesome.  You can also copy the name and python code for the currently selected icon.</p> Source code in <code>client/ayon_core/vendor/python/qtmaterialsymbols/browser.py</code> <pre><code>class IconBrowser(QtWidgets.QMainWindow):\n    \"\"\"\n    A small browser window that allows the user to search through all icons from\n    the available version of QtAwesome.  You can also copy the name and python\n    code for the currently selected icon.\n    \"\"\"\n\n    def __init__(self):\n        super().__init__()\n        self.setMinimumSize(630, 420)\n        self.setWindowTitle(\"Material Symbols Browser\")\n\n        # Prepare icons data\n        instance = get_instance()\n        font_maps = instance.get_charmap()\n\n        icon_names = list(font_maps)\n\n        center_widget = QtWidgets.QWidget(self)\n\n        # Filtering inputs\n        header_widget = QtWidgets.QWidget(center_widget)\n\n        filter_input = QtWidgets.QLineEdit(header_widget)\n        filter_input.setPlaceholderText(\"Filter by name...\")\n\n        filled_check = QtWidgets.QCheckBox(\"Fill\", header_widget)\n        filled_check.setToolTip(\"Filled icons\")\n\n        copy_btn = QtWidgets.QPushButton(\"&lt; no selection &gt;\", header_widget)\n        copy_btn.setToolTip(\"Copy name\")\n\n        header_layout = QtWidgets.QHBoxLayout(header_widget)\n        header_layout.setContentsMargins(0, 0, 0, 0)\n        header_layout.addWidget(filter_input, 4)\n        header_layout.addWidget(filled_check, 1)\n        header_layout.addWidget(copy_btn, 0)\n\n        # Icons view\n        icons_view = IconListView(center_widget)\n        icons_view.setUniformItemSizes(True)\n        icons_view.setResizeMode(QtWidgets.QListView.Adjust)\n        icons_view.setViewMode(QtWidgets.QListView.IconMode)\n        icons_view.setContextMenuPolicy(QtCore.Qt.CustomContextMenu)\n\n        model = IconModel(QtGui.QColor(\"#444746\"))\n        model.setStringList(sorted(icon_names))\n\n        proxy_model = QtCore.QSortFilterProxyModel()\n        proxy_model.setSourceModel(model)\n        proxy_model.setFilterCaseSensitivity(QtCore.Qt.CaseInsensitive)\n\n        icons_view.setModel(proxy_model)\n\n        center_layout = QtWidgets.QVBoxLayout(center_widget)\n        center_layout.setContentsMargins(15, 15, 15, 15)\n        center_layout.setSpacing(5)\n        center_layout.addWidget(header_widget, 0)\n        center_layout.addWidget(icons_view, 1)\n\n        self.setCentralWidget(center_widget)\n\n        filter_timer = QtCore.QTimer(self)\n        filter_timer.setSingleShot(True)\n        filter_timer.setInterval(AUTO_SEARCH_TIMEOUT)\n\n        filter_input.textChanged.connect(self._on_filter_change)\n        filter_input.returnPressed.connect(self._on_filter_confirm)\n        filled_check.stateChanged.connect(self._on_filled_change)\n        icons_view.doubleClicked.connect(self._copy_icon_name)\n        icons_view.selectionModel().selectionChanged.connect(\n            self._on_selection_change\n        )\n        copy_btn.clicked.connect(self._copy_icon_name)\n        filter_timer.timeout.connect(self._update_filter)\n\n        QtWidgets.QShortcut(\n            QtGui.QKeySequence(QtCore.Qt.Key_Return),\n            self,\n            self._copy_icon_name,\n        )\n        QtWidgets.QShortcut(\n            QtGui.QKeySequence(\"Ctrl+F\"),\n            self,\n            filter_input.setFocus,\n        )\n\n        self._filled_check = filled_check\n        self._copy_btn = copy_btn\n        self._filter_input = filter_input\n        self._icons_view = icons_view\n        self._model = model\n        self._proxy_model = proxy_model\n        self._filter_timer = filter_timer\n\n    def showEvent(self, event):\n        super().showEvent(event)\n        self._filter_input.setFocus()\n        self.setStyleSheet(load_stylesheet())\n\n    def _update_filter(self):\n        regex = \"\"\n        search_term = self._filter_input.text()\n        if search_term:\n            regex = f\".*{search_term}.*$\"\n\n        self._proxy_model.setFilterRegularExpression(regex)\n\n    def _on_filled_change(self):\n        self._model.set_filled(self._filled_check.isChecked())\n\n    def _on_selection_change(self, _new_selection, _old_selection):\n        indexes = self._icons_view.selectedIndexes()\n        if not indexes:\n            self._copy_btn.setText(\"&lt; nothing &gt;\")\n            return\n\n        index = indexes[0]\n        icon_name = index.data()\n        self._copy_btn.setText(icon_name)\n\n    def _on_filter_change(self):\n        self._filter_timer.start()\n\n    def _on_filter_confirm(self):\n        self._filter_timer.stop()\n        self._update_filter()\n\n    def _copy_icon_name(self):\n        indexes = self._icons_view.selectedIndexes()\n        for index in indexes:\n            clipboard = QtWidgets.QApplication.instance().clipboard()\n            clipboard.setText(index.data())\n            break\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/qtmaterialsymbols/browser.html#client.ayon_core.vendor.python.qtmaterialsymbols.browser.IconListView","title":"<code>IconListView</code>","text":"<p>               Bases: <code>QListView</code></p> <p>A QListView that scales it's grid size to ensure the same number of columns are always drawn.</p> Source code in <code>client/ayon_core/vendor/python/qtmaterialsymbols/browser.py</code> <pre><code>class IconListView(QtWidgets.QListView):\n    \"\"\"\n    A QListView that scales it's grid size to ensure the same number of\n    columns are always drawn.\n    \"\"\"\n\n    def __init__(self, parent=None):\n        super().__init__(parent)\n        self.setVerticalScrollBarPolicy(QtCore.Qt.ScrollBarAlwaysOn)\n        self.set_icon_size(54)\n\n    def set_icon_size(self, size):\n        \"\"\"\n        Set the icon size for the icons in the view.\n        \"\"\"\n        grid_size = (size / 8.0) * 12.0\n        self.setGridSize(QtCore.QSize(grid_size, grid_size))\n        self.setIconSize(QtCore.QSize(size, size))\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/qtmaterialsymbols/browser.html#client.ayon_core.vendor.python.qtmaterialsymbols.browser.IconListView.set_icon_size","title":"<code>set_icon_size(size)</code>","text":"<p>Set the icon size for the icons in the view.</p> Source code in <code>client/ayon_core/vendor/python/qtmaterialsymbols/browser.py</code> <pre><code>def set_icon_size(self, size):\n    \"\"\"\n    Set the icon size for the icons in the view.\n    \"\"\"\n    grid_size = (size / 8.0) * 12.0\n    self.setGridSize(QtCore.QSize(grid_size, grid_size))\n    self.setIconSize(QtCore.QSize(size, size))\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/qtmaterialsymbols/browser.html#client.ayon_core.vendor.python.qtmaterialsymbols.browser.run","title":"<code>run()</code>","text":"<p>Start the IconBrowser and block until the process exits.</p> Source code in <code>client/ayon_core/vendor/python/qtmaterialsymbols/browser.py</code> <pre><code>def run():\n    \"\"\"\n    Start the IconBrowser and block until the process exits.\n    \"\"\"\n    app = QtWidgets.QApplication([])\n\n    browser = IconBrowser()\n    browser.show()\n\n    sys.exit(app.exec_())\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/qtmaterialsymbols/exceptions.html","title":"exceptions","text":""},{"location":"autoapi/client/ayon_core/vendor/python/qtmaterialsymbols/exceptions.html#client.ayon_core.vendor.python.qtmaterialsymbols.exceptions.ApplicationNotRunning","title":"<code>ApplicationNotRunning</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Raised when the QApplication is not running.</p> Source code in <code>client/ayon_core/vendor/python/qtmaterialsymbols/exceptions.py</code> <pre><code>class ApplicationNotRunning(Exception):\n    \"\"\"Raised when the QApplication is not running.\"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/qtmaterialsymbols/exceptions.html#client.ayon_core.vendor.python.qtmaterialsymbols.exceptions.FontError","title":"<code>FontError</code>","text":"<p>               Bases: <code>Exception</code></p> <p>Raised when there is an issue with font.</p> Source code in <code>client/ayon_core/vendor/python/qtmaterialsymbols/exceptions.py</code> <pre><code>class FontError(Exception):\n    \"\"\"Raised when there is an issue with font.\"\"\"\n    pass\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/qtmaterialsymbols/iconic_font.html","title":"iconic_font","text":""},{"location":"autoapi/client/ayon_core/vendor/python/qtmaterialsymbols/iconic_font.html#client.ayon_core.vendor.python.qtmaterialsymbols.iconic_font--iconic-font","title":"Iconic Font","text":"<p>A lightweight module handling iconic fonts.</p> <p>It is designed to provide a simple way for creating QIcons from glyphs.</p> <p>From a user's viewpoint, the main entry point is the <code>IconicFont</code> class which contains methods for loading new iconic fonts with their character map and methods returning instances of <code>QIcon</code>.</p>"},{"location":"autoapi/client/ayon_core/vendor/python/qtmaterialsymbols/iconic_font.html#client.ayon_core.vendor.python.qtmaterialsymbols.iconic_font.CharIconEngine","title":"<code>CharIconEngine</code>","text":"<p>               Bases: <code>QIconEngine</code></p> <p>Specialization of QIconEngine used to draw font-based icons.</p> Source code in <code>client/ayon_core/vendor/python/qtmaterialsymbols/iconic_font.py</code> <pre><code>class CharIconEngine(QtGui.QIconEngine):\n    \"\"\"Specialization of QIconEngine used to draw font-based icons.\"\"\"\n\n    def __init__(\n        self,\n        iconic: \"IconicFont\",\n        painter: QtGui.QPainter,\n        options: IconOptions\n    ):\n        super().__init__()\n        self._iconic = iconic\n        self._painter = painter\n        self._options = options\n\n    def paint(self, painter, rect, mode, state):\n        self._painter.paint(\n            self._iconic,\n            painter,\n            rect,\n            mode,\n            state,\n            self._options\n        )\n\n    def pixmap(self, size, mode, state):\n        pm = QtGui.QPixmap(size)\n        pm.fill(QtCore.Qt.transparent)\n        painter = QtGui.QPainter(pm)\n        self.paint(\n            painter,\n            QtCore.QRect(QtCore.QPoint(0, 0), size),\n            mode,\n            state\n        )\n        return pm\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/qtmaterialsymbols/iconic_font.html#client.ayon_core.vendor.python.qtmaterialsymbols.iconic_font.CharIconPainter","title":"<code>CharIconPainter</code>","text":"<p>Char icon painter.</p> Source code in <code>client/ayon_core/vendor/python/qtmaterialsymbols/iconic_font.py</code> <pre><code>class CharIconPainter:\n    \"\"\"Char icon painter.\"\"\"\n    def paint(self, iconic, painter, rect, mode, state, options):\n        \"\"\"Main paint method.\"\"\"\n        self._paint_icon(iconic, painter, rect, mode, state, options)\n\n    def _paint_icon(self, iconic, painter, rect, mode, state, options):\n        \"\"\"Paint a single icon.\"\"\"\n        painter.save()\n\n        color = options.get_color_for_state(state, mode)\n        char = options.get_char_for_state(state, mode)\n\n        painter.setPen(QtGui.QColor(color))\n\n        draw_size = round(rect.height() * options.scale_factor)\n\n        font = iconic.get_font(\n            draw_size,\n            options.get_fill_for_state(state, mode)\n        )\n\n        painter.setFont(font)\n        if options.offset is not None:\n            rect = QtCore.QRect(rect)\n            rect.translate(\n                round(options.offset.x * rect.width()),\n                round(options.offset.y * rect.height())\n            )\n\n        scale_x = -1 if options.hflip else 1\n        scale_y = -1 if options.vflip else 1\n\n        if options.vflip or options.hflip or options.rotate:\n            x_center = rect.width() * 0.5\n            y_center = rect.height() * 0.5\n            painter.translate(x_center, y_center)\n\n            transfrom = QtGui.QTransform()\n            transfrom.scale(scale_x, scale_y)\n            painter.setTransform(transfrom, True)\n\n            if options.rotate:\n                painter.rotate(options.rotate)\n            painter.translate(-x_center, -y_center)\n\n        painter.setOpacity(options.opacity)\n\n        painter.drawText(rect, QtCore.Qt.AlignCenter, char)\n        painter.restore()\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/qtmaterialsymbols/iconic_font.html#client.ayon_core.vendor.python.qtmaterialsymbols.iconic_font.CharIconPainter.paint","title":"<code>paint(iconic, painter, rect, mode, state, options)</code>","text":"<p>Main paint method.</p> Source code in <code>client/ayon_core/vendor/python/qtmaterialsymbols/iconic_font.py</code> <pre><code>def paint(self, iconic, painter, rect, mode, state, options):\n    \"\"\"Main paint method.\"\"\"\n    self._paint_icon(iconic, painter, rect, mode, state, options)\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/qtmaterialsymbols/iconic_font.html#client.ayon_core.vendor.python.qtmaterialsymbols.iconic_font.IconicFont","title":"<code>IconicFont</code>","text":"<p>               Bases: <code>QObject</code></p> <p>Main class for managing icons.</p> Source code in <code>client/ayon_core/vendor/python/qtmaterialsymbols/iconic_font.py</code> <pre><code>class IconicFont(QtCore.QObject):\n    \"\"\"Main class for managing icons.\"\"\"\n\n    def __init__(self):\n        super().__init__()\n        self._painter = CharIconPainter()\n        self._icon_cache = {}\n\n    def get_charmap(self) -&gt; Dict[str, int]:\n        return get_char_mapping()\n\n    def get_font(self, size: int, filled: bool) -&gt; QtGui.QFont:\n        \"\"\"Return a QFont corresponding to the given size.\"\"\"\n        if filled:\n            font = QtGui.QFont(_get_font_name_filled())\n        else:\n            font = QtGui.QFont(_get_font_name())\n        font.setPixelSize(round(size))\n        return font\n\n    def get_icon_with_options(self, options: IconOptions) -&gt; QtGui.QIcon:\n        \"\"\"Return a QIcon object corresponding to the provided icon name.\"\"\"\n        if QtWidgets.QApplication.instance() is None:\n            warnings.warn(\n                \"You need to have a running QApplication!\"\n            )\n            return QtGui.QIcon()\n\n        cache_key = options.identifier\n        if cache_key in self._icon_cache:\n            return self._icon_cache[cache_key]\n\n        output = self._icon_by_painter(self._painter, options)\n        self._icon_cache[cache_key] = output\n        return output\n\n    def get_icon(\n        self,\n        icon_name: Optional[str] = None,\n        color: Optional[Union[QtGui.QColor, str]] = None,\n        opacity: Optional[float] = None,\n        scale_factor: Optional[float] = None,\n        offset: Optional[Position] = None,\n        hflip: bool = False,\n        vflip: bool = False,\n        rotate: int = 0,\n        fill: bool = True,\n        **kwargs\n    ) -&gt; QtGui.QIcon:\n        \"\"\"Return a QIcon object corresponding to the provided icon name.\"\"\"\n        if QtWidgets.QApplication.instance() is None:\n            warnings.warn(\n                \"You need to have a running QApplication!\"\n            )\n            return QtGui.QIcon()\n\n        options = IconOptions.from_data(\n            icon_name=icon_name,\n            color=color,\n            opacity=opacity,\n            scale_factor=scale_factor,\n            offset=offset,\n            hflip=hflip,\n            vflip=vflip,\n            rotate=rotate,\n            fill=fill,\n            **kwargs\n        )\n        cache_key = options.identifier\n        if cache_key in self._icon_cache:\n            return self._icon_cache[cache_key]\n        return self.get_icon_with_options(options)\n\n    def _icon_by_painter(self, painter, options):\n        \"\"\"Return the icon corresponding to the given painter.\"\"\"\n        engine = CharIconEngine(self, painter, options)\n        return QtGui.QIcon(engine)\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/qtmaterialsymbols/iconic_font.html#client.ayon_core.vendor.python.qtmaterialsymbols.iconic_font.IconicFont.get_font","title":"<code>get_font(size, filled)</code>","text":"<p>Return a QFont corresponding to the given size.</p> Source code in <code>client/ayon_core/vendor/python/qtmaterialsymbols/iconic_font.py</code> <pre><code>def get_font(self, size: int, filled: bool) -&gt; QtGui.QFont:\n    \"\"\"Return a QFont corresponding to the given size.\"\"\"\n    if filled:\n        font = QtGui.QFont(_get_font_name_filled())\n    else:\n        font = QtGui.QFont(_get_font_name())\n    font.setPixelSize(round(size))\n    return font\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/qtmaterialsymbols/iconic_font.html#client.ayon_core.vendor.python.qtmaterialsymbols.iconic_font.IconicFont.get_icon","title":"<code>get_icon(icon_name=None, color=None, opacity=None, scale_factor=None, offset=None, hflip=False, vflip=False, rotate=0, fill=True, **kwargs)</code>","text":"<p>Return a QIcon object corresponding to the provided icon name.</p> Source code in <code>client/ayon_core/vendor/python/qtmaterialsymbols/iconic_font.py</code> <pre><code>def get_icon(\n    self,\n    icon_name: Optional[str] = None,\n    color: Optional[Union[QtGui.QColor, str]] = None,\n    opacity: Optional[float] = None,\n    scale_factor: Optional[float] = None,\n    offset: Optional[Position] = None,\n    hflip: bool = False,\n    vflip: bool = False,\n    rotate: int = 0,\n    fill: bool = True,\n    **kwargs\n) -&gt; QtGui.QIcon:\n    \"\"\"Return a QIcon object corresponding to the provided icon name.\"\"\"\n    if QtWidgets.QApplication.instance() is None:\n        warnings.warn(\n            \"You need to have a running QApplication!\"\n        )\n        return QtGui.QIcon()\n\n    options = IconOptions.from_data(\n        icon_name=icon_name,\n        color=color,\n        opacity=opacity,\n        scale_factor=scale_factor,\n        offset=offset,\n        hflip=hflip,\n        vflip=vflip,\n        rotate=rotate,\n        fill=fill,\n        **kwargs\n    )\n    cache_key = options.identifier\n    if cache_key in self._icon_cache:\n        return self._icon_cache[cache_key]\n    return self.get_icon_with_options(options)\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/qtmaterialsymbols/iconic_font.html#client.ayon_core.vendor.python.qtmaterialsymbols.iconic_font.IconicFont.get_icon_with_options","title":"<code>get_icon_with_options(options)</code>","text":"<p>Return a QIcon object corresponding to the provided icon name.</p> Source code in <code>client/ayon_core/vendor/python/qtmaterialsymbols/iconic_font.py</code> <pre><code>def get_icon_with_options(self, options: IconOptions) -&gt; QtGui.QIcon:\n    \"\"\"Return a QIcon object corresponding to the provided icon name.\"\"\"\n    if QtWidgets.QApplication.instance() is None:\n        warnings.warn(\n            \"You need to have a running QApplication!\"\n        )\n        return QtGui.QIcon()\n\n    cache_key = options.identifier\n    if cache_key in self._icon_cache:\n        return self._icon_cache[cache_key]\n\n    output = self._icon_by_painter(self._painter, options)\n    self._icon_cache[cache_key] = output\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/qtmaterialsymbols/structures.html","title":"structures","text":""},{"location":"autoapi/client/ayon_core/vendor/python/qtmaterialsymbols/utils.html","title":"utils","text":""},{"location":"autoapi/client/ayon_core/vendor/python/qtmaterialsymbols/version.html","title":"version","text":""},{"location":"autoapi/client/ayon_core/vendor/python/qtmaterialsymbols/resources/index.html","title":"resources","text":""},{"location":"autoapi/client/ayon_core/vendor/python/scriptsmenu/index.html","title":"scriptsmenu","text":""},{"location":"autoapi/client/ayon_core/vendor/python/scriptsmenu/index.html#client.ayon_core.vendor.python.scriptsmenu.ScriptsMenu","title":"<code>ScriptsMenu</code>","text":"<p>               Bases: <code>QMenu</code></p> <p>A Qt menu that displays a list of searchable actions</p> Source code in <code>client/ayon_core/vendor/python/scriptsmenu/scriptsmenu.py</code> <pre><code>class ScriptsMenu(QtWidgets.QMenu):\n    \"\"\"A Qt menu that displays a list of searchable actions\"\"\"\n\n    updated = QtCore.Signal(QtWidgets.QMenu)\n\n    def __init__(self, *args, **kwargs):\n        \"\"\"Initialize Scripts menu\n\n        Args:\n            title (str): the name of the root menu which will be created\n\n            parent (QtWidgets.QObject) : the QObject to parent the menu to\n\n        Returns:\n            None\n\n        \"\"\"\n        QtWidgets.QMenu.__init__(self, *args, **kwargs)\n\n        self.searchbar = None\n        self.update_action = None\n\n        self._script_actions = []\n        self._callbacks = defaultdict(list)\n\n        # Automatically add it to the parent menu\n        parent = kwargs.get(\"parent\", None)\n        if parent:\n            parent.addMenu(self)\n\n        objectname = kwargs.get(\"objectName\", \"scripts\")\n        title = kwargs.get(\"title\", \"Scripts\")\n        self.setObjectName(objectname)\n        self.setTitle(title)\n\n        # add default items in the menu\n        self.create_default_items()\n\n    def on_update(self):\n        self.updated.emit(self)\n\n    @property\n    def registered_callbacks(self):\n        return self._callbacks.copy()\n\n    def create_default_items(self):\n        \"\"\"Add a search bar to the top of the menu given\"\"\"\n\n        # create widget and link function\n        searchbar = QtWidgets.QLineEdit()\n        searchbar.setFixedWidth(120)\n        searchbar.setPlaceholderText(\"Search ...\")\n        searchbar.textChanged.connect(self._update_search)\n        self.searchbar = searchbar\n\n        # create widget holder\n        searchbar_action = QtWidgets.QWidgetAction(self)\n\n        # add widget to widget holder\n        searchbar_action.setDefaultWidget(self.searchbar)\n        searchbar_action.setObjectName(\"Searchbar\")\n\n        # add update button and link function\n        update_action = QtWidgets.QAction(self)\n        update_action.setObjectName(\"Update Scripts\")\n        update_action.setText(\"Update Scripts\")\n        update_action.setVisible(False)\n        update_action.triggered.connect(self.on_update)\n        self.update_action = update_action\n\n        # add action to menu\n        self.addAction(searchbar_action)\n        self.addAction(update_action)\n\n        # add separator object\n        separator = self.addSeparator()\n        separator.setObjectName(\"separator\")\n\n    def add_menu(self, title, parent=None):\n        \"\"\"Create a sub menu for a parent widget\n\n        Args:\n            parent(QtWidgets.QWidget): the object to parent the menu to\n\n            title(str): the title of the menu\n\n        Returns:\n             QtWidget.QMenu instance\n        \"\"\"\n\n        if not parent:\n            parent = self\n\n        menu = QtWidgets.QMenu(parent, title)\n        menu.setTitle(title)\n        menu.setObjectName(title)\n        menu.setTearOffEnabled(True)\n        parent.addMenu(menu)\n\n        return menu\n\n    def add_script(self, parent, title, command, sourcetype, icon=None,\n                   tags=None, label=None, tooltip=None, shortcut=None):\n        \"\"\"Create an action item which runs a script when clicked\n\n        Args:\n            parent (QtWidget.QWidget): The widget to parent the item to\n\n            title (str): The text which will be displayed in the menu\n\n            command (str): The command which needs to be run when the item is\n                           clicked.\n\n            sourcetype (str): The type of command, the way the command is\n                              processed is based on the source type.\n\n            icon (str): The file path of an icon to display with the menu item\n\n            tags (list, tuple): Keywords which describe the action\n\n            label (str): A short description of the script which will be displayed\n                         when hovering over the menu item\n\n            tooltip (str): A tip for the user about the usage fo the tool\n\n            shortcut (str): A shortcut to run the command\n\n        Returns:\n            QtWidget.QAction instance\n\n        \"\"\"\n\n        assert tags is None or isinstance(tags, (list, tuple))\n        # Ensure tags is a list\n        tags = list() if tags is None else list(tags)\n        tags.append(title.lower())\n\n        assert icon is None or isinstance(icon, str), (\n            \"Invalid data type for icon, supported : None, string\")\n\n        # create new action\n        script_action = action.Action(parent)\n        script_action.setText(title)\n        script_action.setObjectName(title)\n        script_action.tags = tags\n\n        # link action to root for callback library\n        script_action.root = self\n\n        # Set up the command\n        script_action.sourcetype = sourcetype\n        script_action.command = command\n\n        try:\n            script_action.process_command()\n        except RuntimeError as e:\n            raise RuntimeError(\"Script action can't be \"\n                               \"processed: {}\".format(e))\n\n        if shortcut:\n            script_action.setShortcut(shortcut)\n\n        if icon:\n            iconfile = os.path.expandvars(icon)\n            script_action.iconfile = iconfile\n            script_action_icon = QtWidgets.QIcon(iconfile)\n            script_action.setIcon(script_action_icon)\n\n        if label:\n            script_action.label = label\n\n        if tooltip:\n            script_action.setStatusTip(tooltip)\n\n        script_action.triggered.connect(script_action.run_command)\n        parent.addAction(script_action)\n\n        # Add to our searchable actions\n        self._script_actions.append(script_action)\n\n        return script_action\n\n    def build_from_configuration(self, parent, configuration):\n        \"\"\"Process the configurations and store the configuration\n\n        This creates all submenus from a configuration.json file.\n\n        When the configuration holds the key `main` all scripts under `main` will\n        be added to the main menu first before adding the rest\n\n        Args:\n            parent (ScriptsMenu): script menu instance\n            configuration (list): A ScriptsMenu configuration list\n\n        Returns:\n            None\n\n        \"\"\"\n\n        for item in configuration:\n            assert isinstance(item, dict), \"Configuration is wrong!\"\n\n            # skip items which have no `type` key\n            item_type = item.get('type', None)\n            if not item_type:\n                log.warning(\"Missing 'type' from configuration item\")\n                continue\n\n            # add separator\n            # Special behavior for separators\n            if item_type == \"separator\":\n                parent.addSeparator()\n\n            # add submenu\n            # items should hold a collection of submenu items (dict)\n            elif item_type == \"menu\":\n                assert \"items\" in item, \"Menu is missing 'items' key\"\n                menu = self.add_menu(parent=parent, title=item[\"title\"])\n                self.build_from_configuration(menu, item[\"items\"])\n\n            # add script\n            elif item_type == \"action\":\n                # filter out `type` from the item dict\n                config = {key: value for key, value in\n                          item.items() if key != \"type\"}\n\n                self.add_script(parent=parent, **config)\n\n    def set_update_visible(self, state):\n        self.update_action.setVisible(state)\n\n    def clear_menu(self):\n        \"\"\"Clear all menu items which are not default\n\n        Returns:\n            None\n\n        \"\"\"\n\n        # TODO: Set up a more robust implementation for this\n        # Delete all except the first three actions\n        for _action in self.actions()[3:]:\n            self.removeAction(_action)\n\n    def register_callback(self, modifiers, callback):\n        self._callbacks[modifiers].append(callback)\n\n    def _update_search(self, search):\n        \"\"\"Hide all the samples which do not match the user's import\n\n        Returns:\n            None\n\n        \"\"\"\n\n        if not search:\n            for action in self._script_actions:\n                action.setVisible(True)\n        else:\n            for action in self._script_actions:\n                action.setVisible(action.has_tag(search.lower()))\n\n        # Set visibility for all submenus\n        for action in self.actions():\n            if not action.menu():\n                continue\n\n            menu = action.menu()\n            visible = any(action.isVisible() for action in menu.actions())\n            action.setVisible(visible)\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/scriptsmenu/index.html#client.ayon_core.vendor.python.scriptsmenu.ScriptsMenu.__init__","title":"<code>__init__(*args, **kwargs)</code>","text":"<p>Initialize Scripts menu</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <code>str</code> <p>the name of the root menu which will be created</p> required <code>parent (QtWidgets.QObject) </code> <p>the QObject to parent the menu to</p> required <p>Returns:</p> Type Description <p>None</p> Source code in <code>client/ayon_core/vendor/python/scriptsmenu/scriptsmenu.py</code> <pre><code>def __init__(self, *args, **kwargs):\n    \"\"\"Initialize Scripts menu\n\n    Args:\n        title (str): the name of the root menu which will be created\n\n        parent (QtWidgets.QObject) : the QObject to parent the menu to\n\n    Returns:\n        None\n\n    \"\"\"\n    QtWidgets.QMenu.__init__(self, *args, **kwargs)\n\n    self.searchbar = None\n    self.update_action = None\n\n    self._script_actions = []\n    self._callbacks = defaultdict(list)\n\n    # Automatically add it to the parent menu\n    parent = kwargs.get(\"parent\", None)\n    if parent:\n        parent.addMenu(self)\n\n    objectname = kwargs.get(\"objectName\", \"scripts\")\n    title = kwargs.get(\"title\", \"Scripts\")\n    self.setObjectName(objectname)\n    self.setTitle(title)\n\n    # add default items in the menu\n    self.create_default_items()\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/scriptsmenu/index.html#client.ayon_core.vendor.python.scriptsmenu.ScriptsMenu.add_menu","title":"<code>add_menu(title, parent=None)</code>","text":"<p>Create a sub menu for a parent widget</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <code>QWidget</code> <p>the object to parent the menu to</p> <code>None</code> <code>title</code> <code>str</code> <p>the title of the menu</p> required <p>Returns:</p> Type Description <p>QtWidget.QMenu instance</p> Source code in <code>client/ayon_core/vendor/python/scriptsmenu/scriptsmenu.py</code> <pre><code>def add_menu(self, title, parent=None):\n    \"\"\"Create a sub menu for a parent widget\n\n    Args:\n        parent(QtWidgets.QWidget): the object to parent the menu to\n\n        title(str): the title of the menu\n\n    Returns:\n         QtWidget.QMenu instance\n    \"\"\"\n\n    if not parent:\n        parent = self\n\n    menu = QtWidgets.QMenu(parent, title)\n    menu.setTitle(title)\n    menu.setObjectName(title)\n    menu.setTearOffEnabled(True)\n    parent.addMenu(menu)\n\n    return menu\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/scriptsmenu/index.html#client.ayon_core.vendor.python.scriptsmenu.ScriptsMenu.add_script","title":"<code>add_script(parent, title, command, sourcetype, icon=None, tags=None, label=None, tooltip=None, shortcut=None)</code>","text":"<p>Create an action item which runs a script when clicked</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <code>QWidget</code> <p>The widget to parent the item to</p> required <code>title</code> <code>str</code> <p>The text which will be displayed in the menu</p> required <code>command</code> <code>str</code> <p>The command which needs to be run when the item is            clicked.</p> required <code>sourcetype</code> <code>str</code> <p>The type of command, the way the command is               processed is based on the source type.</p> required <code>icon</code> <code>str</code> <p>The file path of an icon to display with the menu item</p> <code>None</code> <code>tags</code> <code>(list, tuple)</code> <p>Keywords which describe the action</p> <code>None</code> <code>label</code> <code>str</code> <p>A short description of the script which will be displayed          when hovering over the menu item</p> <code>None</code> <code>tooltip</code> <code>str</code> <p>A tip for the user about the usage fo the tool</p> <code>None</code> <code>shortcut</code> <code>str</code> <p>A shortcut to run the command</p> <code>None</code> <p>Returns:</p> Type Description <p>QtWidget.QAction instance</p> Source code in <code>client/ayon_core/vendor/python/scriptsmenu/scriptsmenu.py</code> <pre><code>def add_script(self, parent, title, command, sourcetype, icon=None,\n               tags=None, label=None, tooltip=None, shortcut=None):\n    \"\"\"Create an action item which runs a script when clicked\n\n    Args:\n        parent (QtWidget.QWidget): The widget to parent the item to\n\n        title (str): The text which will be displayed in the menu\n\n        command (str): The command which needs to be run when the item is\n                       clicked.\n\n        sourcetype (str): The type of command, the way the command is\n                          processed is based on the source type.\n\n        icon (str): The file path of an icon to display with the menu item\n\n        tags (list, tuple): Keywords which describe the action\n\n        label (str): A short description of the script which will be displayed\n                     when hovering over the menu item\n\n        tooltip (str): A tip for the user about the usage fo the tool\n\n        shortcut (str): A shortcut to run the command\n\n    Returns:\n        QtWidget.QAction instance\n\n    \"\"\"\n\n    assert tags is None or isinstance(tags, (list, tuple))\n    # Ensure tags is a list\n    tags = list() if tags is None else list(tags)\n    tags.append(title.lower())\n\n    assert icon is None or isinstance(icon, str), (\n        \"Invalid data type for icon, supported : None, string\")\n\n    # create new action\n    script_action = action.Action(parent)\n    script_action.setText(title)\n    script_action.setObjectName(title)\n    script_action.tags = tags\n\n    # link action to root for callback library\n    script_action.root = self\n\n    # Set up the command\n    script_action.sourcetype = sourcetype\n    script_action.command = command\n\n    try:\n        script_action.process_command()\n    except RuntimeError as e:\n        raise RuntimeError(\"Script action can't be \"\n                           \"processed: {}\".format(e))\n\n    if shortcut:\n        script_action.setShortcut(shortcut)\n\n    if icon:\n        iconfile = os.path.expandvars(icon)\n        script_action.iconfile = iconfile\n        script_action_icon = QtWidgets.QIcon(iconfile)\n        script_action.setIcon(script_action_icon)\n\n    if label:\n        script_action.label = label\n\n    if tooltip:\n        script_action.setStatusTip(tooltip)\n\n    script_action.triggered.connect(script_action.run_command)\n    parent.addAction(script_action)\n\n    # Add to our searchable actions\n    self._script_actions.append(script_action)\n\n    return script_action\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/scriptsmenu/index.html#client.ayon_core.vendor.python.scriptsmenu.ScriptsMenu.build_from_configuration","title":"<code>build_from_configuration(parent, configuration)</code>","text":"<p>Process the configurations and store the configuration</p> <p>This creates all submenus from a configuration.json file.</p> <p>When the configuration holds the key <code>main</code> all scripts under <code>main</code> will be added to the main menu first before adding the rest</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <code>ScriptsMenu</code> <p>script menu instance</p> required <code>configuration</code> <code>list</code> <p>A ScriptsMenu configuration list</p> required <p>Returns:</p> Type Description <p>None</p> Source code in <code>client/ayon_core/vendor/python/scriptsmenu/scriptsmenu.py</code> <pre><code>def build_from_configuration(self, parent, configuration):\n    \"\"\"Process the configurations and store the configuration\n\n    This creates all submenus from a configuration.json file.\n\n    When the configuration holds the key `main` all scripts under `main` will\n    be added to the main menu first before adding the rest\n\n    Args:\n        parent (ScriptsMenu): script menu instance\n        configuration (list): A ScriptsMenu configuration list\n\n    Returns:\n        None\n\n    \"\"\"\n\n    for item in configuration:\n        assert isinstance(item, dict), \"Configuration is wrong!\"\n\n        # skip items which have no `type` key\n        item_type = item.get('type', None)\n        if not item_type:\n            log.warning(\"Missing 'type' from configuration item\")\n            continue\n\n        # add separator\n        # Special behavior for separators\n        if item_type == \"separator\":\n            parent.addSeparator()\n\n        # add submenu\n        # items should hold a collection of submenu items (dict)\n        elif item_type == \"menu\":\n            assert \"items\" in item, \"Menu is missing 'items' key\"\n            menu = self.add_menu(parent=parent, title=item[\"title\"])\n            self.build_from_configuration(menu, item[\"items\"])\n\n        # add script\n        elif item_type == \"action\":\n            # filter out `type` from the item dict\n            config = {key: value for key, value in\n                      item.items() if key != \"type\"}\n\n            self.add_script(parent=parent, **config)\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/scriptsmenu/index.html#client.ayon_core.vendor.python.scriptsmenu.ScriptsMenu.clear_menu","title":"<code>clear_menu()</code>","text":"<p>Clear all menu items which are not default</p> <p>Returns:</p> Type Description <p>None</p> Source code in <code>client/ayon_core/vendor/python/scriptsmenu/scriptsmenu.py</code> <pre><code>def clear_menu(self):\n    \"\"\"Clear all menu items which are not default\n\n    Returns:\n        None\n\n    \"\"\"\n\n    # TODO: Set up a more robust implementation for this\n    # Delete all except the first three actions\n    for _action in self.actions()[3:]:\n        self.removeAction(_action)\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/scriptsmenu/index.html#client.ayon_core.vendor.python.scriptsmenu.ScriptsMenu.create_default_items","title":"<code>create_default_items()</code>","text":"<p>Add a search bar to the top of the menu given</p> Source code in <code>client/ayon_core/vendor/python/scriptsmenu/scriptsmenu.py</code> <pre><code>def create_default_items(self):\n    \"\"\"Add a search bar to the top of the menu given\"\"\"\n\n    # create widget and link function\n    searchbar = QtWidgets.QLineEdit()\n    searchbar.setFixedWidth(120)\n    searchbar.setPlaceholderText(\"Search ...\")\n    searchbar.textChanged.connect(self._update_search)\n    self.searchbar = searchbar\n\n    # create widget holder\n    searchbar_action = QtWidgets.QWidgetAction(self)\n\n    # add widget to widget holder\n    searchbar_action.setDefaultWidget(self.searchbar)\n    searchbar_action.setObjectName(\"Searchbar\")\n\n    # add update button and link function\n    update_action = QtWidgets.QAction(self)\n    update_action.setObjectName(\"Update Scripts\")\n    update_action.setText(\"Update Scripts\")\n    update_action.setVisible(False)\n    update_action.triggered.connect(self.on_update)\n    self.update_action = update_action\n\n    # add action to menu\n    self.addAction(searchbar_action)\n    self.addAction(update_action)\n\n    # add separator object\n    separator = self.addSeparator()\n    separator.setObjectName(\"separator\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/scriptsmenu/action.html","title":"action","text":""},{"location":"autoapi/client/ayon_core/vendor/python/scriptsmenu/action.html#client.ayon_core.vendor.python.scriptsmenu.action.Action","title":"<code>Action</code>","text":"<p>               Bases: <code>QAction</code></p> <p>Custom Action widget</p> Source code in <code>client/ayon_core/vendor/python/scriptsmenu/action.py</code> <pre><code>class Action(QtWidgets.QAction):\n    \"\"\"Custom Action widget\"\"\"\n\n    def __init__(self, parent=None):\n\n        QtWidgets.QAction.__init__(self, parent)\n\n        self._root = None\n        self._tags = list()\n        self._command = None\n        self._sourcetype = None\n        self._iconfile = None\n        self._label = None\n\n        self._COMMAND = \"\"\"import imp\nf, filepath, descr = imp.find_module('{module_name}', ['{dirname}'])\nmodule = imp.load_module('{module_name}', f, filepath, descr)\nmodule.{module_name}()\"\"\"\n\n    @property\n    def root(self):\n        return self._root\n\n    @root.setter\n    def root(self, value):\n        self._root = value\n\n    @property\n    def tags(self):\n        return self._tags\n\n    @tags.setter\n    def tags(self, value):\n        self._tags = value\n\n    @property\n    def command(self):\n        return self._command\n\n    @command.setter\n    def command(self, value):\n        \"\"\"\n        Store the command in the QAction \n\n        Args:\n            value (str): the full command which will be executed when clicked\n\n        Return:\n             None\n        \"\"\"\n        self._command = value\n\n    @property\n    def sourcetype(self):\n        return self._sourcetype\n\n    @sourcetype.setter\n    def sourcetype(self, value):\n        \"\"\"\n        Set the command type to get the correct execution of the command given\n\n        Args:\n            value (str): the name of the command type\n\n        Returns:\n            None\n\n        \"\"\"\n        self._sourcetype = value\n\n    @property\n    def iconfile(self):\n        return self._iconfile\n\n    @iconfile.setter\n    def iconfile(self, value):\n        \"\"\"Store the path to the image file which needs to be displayed\n\n        Args:\n            value (str): the path to the image\n\n        Returns:\n            None\n        \"\"\"\n        self._iconfile = value\n\n    @property\n    def label(self):\n        return self._label\n\n    @label.setter\n    def label(self, value):\n        \"\"\"\n        Set the abbreviation which will be used as overlay text in the shelf\n\n        Args:\n            value (str): an abbreviation of the name\n\n        Returns:\n            None\n\n        \"\"\"\n        self._label = value\n\n    def run_command(self):\n        \"\"\"\n        Run the command of the instance or copy the command to the active shelf\n        based on the current modifiers.\n\n        If callbacks have been registered with found modifier integer the\n        function will trigger all callbacks. When a callback function returns a\n        non zero integer it will not execute the action's command\n        \"\"\"\n\n        # get the current application and its linked keyboard modifiers\n        app = QtWidgets.QApplication.instance()\n        modifiers = app.keyboardModifiers()\n        if not QT6:\n            modifiers = int(modifiers)\n\n        # If the menu has a callback registered for the current modifier\n        # we run the callback instead of the action itself.\n        registered = self._root.registered_callbacks\n        callbacks = registered.get(modifiers, [])\n        for callback in callbacks:\n            signal = callback(self)\n            if signal != 0:\n                # Exit function on non-zero return code\n                return\n\n        exec(self.process_command())\n\n    def process_command(self):\n        \"\"\"\n        Check if the command is a file which needs to be launched and if it \n        has a relative path, if so return the full path by expanding \n        environment variables. Wrap any mel command in a executable string \n        for Python and return the string if the source type is  \n\n        Add your own source type and required process to ensure callback\n        is stored correctly.\n\n        An example of a process is the sourcetype is MEL \n        (Maya Embedded Language) as Python cannot run it on its own so it \n        needs to be wrapped in a string in which we explicitly import mel and \n        run it as a mel.eval. The string is then parsed to python as \n        exec(\"command\"). \n\n        Returns:\n            str: a clean command which can be used\n\n        \"\"\"\n        if self._sourcetype == \"python\":\n            return self._command\n\n        if self._sourcetype == \"mel\":\n            # Escape single quotes\n            conversion = self._command.replace(\"'\", \"\\\\'\")\n            return \"import maya; maya.mel.eval('{}')\".format(conversion)\n\n        if self._sourcetype == \"file\":\n            if os.path.isabs(self._command):\n                filepath = self._command\n            else:\n                filepath = os.path.normpath(os.path.expandvars(self._command))\n\n            return self._wrap_filepath(filepath)\n\n    def has_tag(self, tag):\n        \"\"\"Check whether the tag matches with the action's tags.\n\n        A partial match will also return True, for example tag `a` will match\n        correctly with the `ape` tag on the Action.\n\n        Args:\n            tag (str): The tag\n\n        Returns\n            bool: Whether the action is tagged with given tag\n\n        \"\"\"\n\n        for tagitem in self.tags:\n            if tag not in tagitem:\n                continue\n            return True\n\n        return False\n\n    def _wrap_filepath(self, file_path):\n        \"\"\"Create a wrapped string for the python command\n\n        Args:\n            file_path (str): the filepath of a script\n\n        Returns:\n            str: the wrapped command\n        \"\"\"\n\n        dirname = os.path.dirname(r\"{}\".format(file_path))\n        dirpath = dirname.replace(\"\\\\\", \"/\")\n        module_name = os.path.splitext(os.path.basename(file_path))[0]\n\n        return self._COMMAND.format(module_name=module_name, dirname=dirpath)\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/scriptsmenu/action.html#client.ayon_core.vendor.python.scriptsmenu.action.Action.has_tag","title":"<code>has_tag(tag)</code>","text":"<p>Check whether the tag matches with the action's tags.</p> <p>A partial match will also return True, for example tag <code>a</code> will match correctly with the <code>ape</code> tag on the Action.</p> <p>Parameters:</p> Name Type Description Default <code>tag</code> <code>str</code> <p>The tag</p> required <p>Returns     bool: Whether the action is tagged with given tag</p> Source code in <code>client/ayon_core/vendor/python/scriptsmenu/action.py</code> <pre><code>def has_tag(self, tag):\n    \"\"\"Check whether the tag matches with the action's tags.\n\n    A partial match will also return True, for example tag `a` will match\n    correctly with the `ape` tag on the Action.\n\n    Args:\n        tag (str): The tag\n\n    Returns\n        bool: Whether the action is tagged with given tag\n\n    \"\"\"\n\n    for tagitem in self.tags:\n        if tag not in tagitem:\n            continue\n        return True\n\n    return False\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/scriptsmenu/action.html#client.ayon_core.vendor.python.scriptsmenu.action.Action.process_command","title":"<code>process_command()</code>","text":"<p>Check if the command is a file which needs to be launched and if it  has a relative path, if so return the full path by expanding  environment variables. Wrap any mel command in a executable string  for Python and return the string if the source type is  </p> <p>Add your own source type and required process to ensure callback is stored correctly.</p> <p>An example of a process is the sourcetype is MEL  (Maya Embedded Language) as Python cannot run it on its own so it  needs to be wrapped in a string in which we explicitly import mel and  run it as a mel.eval. The string is then parsed to python as  exec(\"command\"). </p> <p>Returns:</p> Name Type Description <code>str</code> <p>a clean command which can be used</p> Source code in <code>client/ayon_core/vendor/python/scriptsmenu/action.py</code> <pre><code>def process_command(self):\n    \"\"\"\n    Check if the command is a file which needs to be launched and if it \n    has a relative path, if so return the full path by expanding \n    environment variables. Wrap any mel command in a executable string \n    for Python and return the string if the source type is  \n\n    Add your own source type and required process to ensure callback\n    is stored correctly.\n\n    An example of a process is the sourcetype is MEL \n    (Maya Embedded Language) as Python cannot run it on its own so it \n    needs to be wrapped in a string in which we explicitly import mel and \n    run it as a mel.eval. The string is then parsed to python as \n    exec(\"command\"). \n\n    Returns:\n        str: a clean command which can be used\n\n    \"\"\"\n    if self._sourcetype == \"python\":\n        return self._command\n\n    if self._sourcetype == \"mel\":\n        # Escape single quotes\n        conversion = self._command.replace(\"'\", \"\\\\'\")\n        return \"import maya; maya.mel.eval('{}')\".format(conversion)\n\n    if self._sourcetype == \"file\":\n        if os.path.isabs(self._command):\n            filepath = self._command\n        else:\n            filepath = os.path.normpath(os.path.expandvars(self._command))\n\n        return self._wrap_filepath(filepath)\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/scriptsmenu/action.html#client.ayon_core.vendor.python.scriptsmenu.action.Action.run_command","title":"<code>run_command()</code>","text":"<p>Run the command of the instance or copy the command to the active shelf based on the current modifiers.</p> <p>If callbacks have been registered with found modifier integer the function will trigger all callbacks. When a callback function returns a non zero integer it will not execute the action's command</p> Source code in <code>client/ayon_core/vendor/python/scriptsmenu/action.py</code> <pre><code>def run_command(self):\n    \"\"\"\n    Run the command of the instance or copy the command to the active shelf\n    based on the current modifiers.\n\n    If callbacks have been registered with found modifier integer the\n    function will trigger all callbacks. When a callback function returns a\n    non zero integer it will not execute the action's command\n    \"\"\"\n\n    # get the current application and its linked keyboard modifiers\n    app = QtWidgets.QApplication.instance()\n    modifiers = app.keyboardModifiers()\n    if not QT6:\n        modifiers = int(modifiers)\n\n    # If the menu has a callback registered for the current modifier\n    # we run the callback instead of the action itself.\n    registered = self._root.registered_callbacks\n    callbacks = registered.get(modifiers, [])\n    for callback in callbacks:\n        signal = callback(self)\n        if signal != 0:\n            # Exit function on non-zero return code\n            return\n\n    exec(self.process_command())\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/scriptsmenu/launchformari.html","title":"launchformari","text":""},{"location":"autoapi/client/ayon_core/vendor/python/scriptsmenu/launchformari.html#client.ayon_core.vendor.python.scriptsmenu.launchformari.main","title":"<code>main(title='Scripts')</code>","text":"<p>Build the main scripts menu in Mari.</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <code>str</code> <p>Name of the menu in the application.</p> <code>'Scripts'</code> <p>Returns:</p> Type Description <p>scriptsmenu.ScriptsMenu:  Instance object.</p> Source code in <code>client/ayon_core/vendor/python/scriptsmenu/launchformari.py</code> <pre><code>def main(title=\"Scripts\"):\n    \"\"\"Build the main scripts menu in Mari.\n\n    Args:\n        title (str): Name of the menu in the application.\n\n    Returns:\n        scriptsmenu.ScriptsMenu:  Instance object.\n\n    \"\"\"\n    mari_main_bar = _mari_main_menubar()\n    for mari_bar in mari_main_bar.children():\n        if isinstance(mari_bar, scriptsmenu.ScriptsMenu):\n            if mari_bar.title() == title:\n                menu = mari_bar\n                return menu\n    menu = scriptsmenu.ScriptsMenu(title=title, parent=mari_main_bar)\n    return menu\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/scriptsmenu/launchformaya.html","title":"launchformaya","text":""},{"location":"autoapi/client/ayon_core/vendor/python/scriptsmenu/launchformaya.html#client.ayon_core.vendor.python.scriptsmenu.launchformaya.find_scripts_menu","title":"<code>find_scripts_menu(title, parent)</code>","text":"<p>Check if the menu exists with the given title in the parent</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <code>str</code> <p>the title name of the scripts menu</p> required <code>parent</code> <code>QMenuBar</code> <p>the menubar to check</p> required <p>Returns:</p> Type Description <p>QtWidgets.QMenu or None</p> Source code in <code>client/ayon_core/vendor/python/scriptsmenu/launchformaya.py</code> <pre><code>def find_scripts_menu(title, parent):\n    \"\"\"\n    Check if the menu exists with the given title in the parent\n\n    Args:\n        title (str): the title name of the scripts menu\n\n        parent (QtWidgets.QMenuBar): the menubar to check\n\n    Returns:\n        QtWidgets.QMenu or None\n\n    \"\"\"\n\n    menu = None\n    search = [i for i in parent.children() if\n              isinstance(i, scriptsmenu.ScriptsMenu)\n              and i.title() == title]\n\n    if search:\n        assert len(search) &lt; 2, (\"Multiple instances of menu '{}' \"\n                                 \"in menu bar\".format(title))\n        menu = search[0]\n\n    return menu\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/scriptsmenu/launchformaya.html#client.ayon_core.vendor.python.scriptsmenu.launchformaya.main","title":"<code>main(title='Scripts', parent=None, objectName=None)</code>","text":"<p>Build the main scripts menu in Maya</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <code>str</code> <p>name of the menu in the application</p> <code>'Scripts'</code> <code>parent</code> <code>QtMenuBar</code> <p>the parent object for the menu</p> <code>None</code> <code>objectName</code> <code>str</code> <p>custom objectName for scripts menu</p> <code>None</code> <p>Returns:</p> Type Description <p>scriptsmenu.ScriptsMenu instance</p> Source code in <code>client/ayon_core/vendor/python/scriptsmenu/launchformaya.py</code> <pre><code>def main(title=\"Scripts\", parent=None, objectName=None):\n    \"\"\"Build the main scripts menu in Maya\n\n    Args:\n        title (str): name of the menu in the application\n\n        parent (QtWidgets.QtMenuBar): the parent object for the menu\n\n        objectName (str): custom objectName for scripts menu\n\n    Returns:\n        scriptsmenu.ScriptsMenu instance\n\n    \"\"\"\n\n    mayamainbar = parent or _maya_main_menubar()\n    try:\n        # check menu already exists\n        menu = find_scripts_menu(title, mayamainbar)\n        if not menu:\n            log.info(\"Attempting to build menu ...\")\n            object_name = objectName or title.lower()\n            menu = scriptsmenu.ScriptsMenu(title=title,\n                                           parent=mayamainbar,\n                                           objectName=object_name)\n    except Exception as e:\n        log.error(e)\n        return\n\n    # Register control + shift callback to add to shelf (maya behavior)\n    modifiers = QtCore.Qt.ControlModifier | QtCore.Qt.ShiftModifier\n    if not QT6:\n        modifiers = int(modifiers)\n\n    menu.register_callback(modifiers, to_shelf)\n\n    menu.register_callback(0, register_repeat_last)\n\n    return menu\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/scriptsmenu/launchformaya.html#client.ayon_core.vendor.python.scriptsmenu.launchformaya.register_repeat_last","title":"<code>register_repeat_last(action)</code>","text":"<p>Register the action in repeatLast to ensure the RepeatLast hotkey works</p> <p>Parameters:</p> Name Type Description Default <code>action</code> <code>Action</code> <p>Action wigdet instance</p> required <p>Returns:</p> Name Type Description <code>int</code> <p>0</p> Source code in <code>client/ayon_core/vendor/python/scriptsmenu/launchformaya.py</code> <pre><code>def register_repeat_last(action):\n    \"\"\"Register the action in repeatLast to ensure the RepeatLast hotkey works\n\n    Args:\n        action (action.Action): Action wigdet instance\n\n    Returns:\n        int: 0\n\n    \"\"\"\n    command = action.process_command()\n    command = command.replace(\"\\n\", \"; \")\n    # Register command to Maya (mel)\n    cmds.repeatLast(addCommand='python(\"{}\")'.format(command),\n                    addCommandLabel=action.label)\n\n    return 0\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/scriptsmenu/launchformaya.html#client.ayon_core.vendor.python.scriptsmenu.launchformaya.to_shelf","title":"<code>to_shelf(action)</code>","text":"<p>Copy clicked menu item to the currently active Maya shelf Args:     action (action.Action): the action instance which is clicked</p> <p>Returns:</p> Name Type Description <code>int</code> <p>1</p> Source code in <code>client/ayon_core/vendor/python/scriptsmenu/launchformaya.py</code> <pre><code>def to_shelf(action):\n    \"\"\"Copy clicked menu item to the currently active Maya shelf\n    Args:\n        action (action.Action): the action instance which is clicked\n\n    Returns:\n        int: 1\n\n    \"\"\"\n\n    shelftoplevel = mel.eval(\"$gShelfTopLevel = $gShelfTopLevel;\")\n    current_active_shelf = cmds.tabLayout(shelftoplevel,\n                                          query=True,\n                                          selectTab=True)\n\n    cmds.shelfButton(command=action.process_command(),\n                     sourceType=\"python\",\n                     parent=current_active_shelf,\n                     image=action.iconfile or \"pythonFamily.png\",\n                     annotation=action.statusTip(),\n                     imageOverlayLabel=action.label or \"\")\n\n    return 1\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/scriptsmenu/launchfornuke.html","title":"launchfornuke","text":""},{"location":"autoapi/client/ayon_core/vendor/python/scriptsmenu/scriptsmenu.html","title":"scriptsmenu","text":""},{"location":"autoapi/client/ayon_core/vendor/python/scriptsmenu/scriptsmenu.html#client.ayon_core.vendor.python.scriptsmenu.scriptsmenu.ScriptsMenu","title":"<code>ScriptsMenu</code>","text":"<p>               Bases: <code>QMenu</code></p> <p>A Qt menu that displays a list of searchable actions</p> Source code in <code>client/ayon_core/vendor/python/scriptsmenu/scriptsmenu.py</code> <pre><code>class ScriptsMenu(QtWidgets.QMenu):\n    \"\"\"A Qt menu that displays a list of searchable actions\"\"\"\n\n    updated = QtCore.Signal(QtWidgets.QMenu)\n\n    def __init__(self, *args, **kwargs):\n        \"\"\"Initialize Scripts menu\n\n        Args:\n            title (str): the name of the root menu which will be created\n\n            parent (QtWidgets.QObject) : the QObject to parent the menu to\n\n        Returns:\n            None\n\n        \"\"\"\n        QtWidgets.QMenu.__init__(self, *args, **kwargs)\n\n        self.searchbar = None\n        self.update_action = None\n\n        self._script_actions = []\n        self._callbacks = defaultdict(list)\n\n        # Automatically add it to the parent menu\n        parent = kwargs.get(\"parent\", None)\n        if parent:\n            parent.addMenu(self)\n\n        objectname = kwargs.get(\"objectName\", \"scripts\")\n        title = kwargs.get(\"title\", \"Scripts\")\n        self.setObjectName(objectname)\n        self.setTitle(title)\n\n        # add default items in the menu\n        self.create_default_items()\n\n    def on_update(self):\n        self.updated.emit(self)\n\n    @property\n    def registered_callbacks(self):\n        return self._callbacks.copy()\n\n    def create_default_items(self):\n        \"\"\"Add a search bar to the top of the menu given\"\"\"\n\n        # create widget and link function\n        searchbar = QtWidgets.QLineEdit()\n        searchbar.setFixedWidth(120)\n        searchbar.setPlaceholderText(\"Search ...\")\n        searchbar.textChanged.connect(self._update_search)\n        self.searchbar = searchbar\n\n        # create widget holder\n        searchbar_action = QtWidgets.QWidgetAction(self)\n\n        # add widget to widget holder\n        searchbar_action.setDefaultWidget(self.searchbar)\n        searchbar_action.setObjectName(\"Searchbar\")\n\n        # add update button and link function\n        update_action = QtWidgets.QAction(self)\n        update_action.setObjectName(\"Update Scripts\")\n        update_action.setText(\"Update Scripts\")\n        update_action.setVisible(False)\n        update_action.triggered.connect(self.on_update)\n        self.update_action = update_action\n\n        # add action to menu\n        self.addAction(searchbar_action)\n        self.addAction(update_action)\n\n        # add separator object\n        separator = self.addSeparator()\n        separator.setObjectName(\"separator\")\n\n    def add_menu(self, title, parent=None):\n        \"\"\"Create a sub menu for a parent widget\n\n        Args:\n            parent(QtWidgets.QWidget): the object to parent the menu to\n\n            title(str): the title of the menu\n\n        Returns:\n             QtWidget.QMenu instance\n        \"\"\"\n\n        if not parent:\n            parent = self\n\n        menu = QtWidgets.QMenu(parent, title)\n        menu.setTitle(title)\n        menu.setObjectName(title)\n        menu.setTearOffEnabled(True)\n        parent.addMenu(menu)\n\n        return menu\n\n    def add_script(self, parent, title, command, sourcetype, icon=None,\n                   tags=None, label=None, tooltip=None, shortcut=None):\n        \"\"\"Create an action item which runs a script when clicked\n\n        Args:\n            parent (QtWidget.QWidget): The widget to parent the item to\n\n            title (str): The text which will be displayed in the menu\n\n            command (str): The command which needs to be run when the item is\n                           clicked.\n\n            sourcetype (str): The type of command, the way the command is\n                              processed is based on the source type.\n\n            icon (str): The file path of an icon to display with the menu item\n\n            tags (list, tuple): Keywords which describe the action\n\n            label (str): A short description of the script which will be displayed\n                         when hovering over the menu item\n\n            tooltip (str): A tip for the user about the usage fo the tool\n\n            shortcut (str): A shortcut to run the command\n\n        Returns:\n            QtWidget.QAction instance\n\n        \"\"\"\n\n        assert tags is None or isinstance(tags, (list, tuple))\n        # Ensure tags is a list\n        tags = list() if tags is None else list(tags)\n        tags.append(title.lower())\n\n        assert icon is None or isinstance(icon, str), (\n            \"Invalid data type for icon, supported : None, string\")\n\n        # create new action\n        script_action = action.Action(parent)\n        script_action.setText(title)\n        script_action.setObjectName(title)\n        script_action.tags = tags\n\n        # link action to root for callback library\n        script_action.root = self\n\n        # Set up the command\n        script_action.sourcetype = sourcetype\n        script_action.command = command\n\n        try:\n            script_action.process_command()\n        except RuntimeError as e:\n            raise RuntimeError(\"Script action can't be \"\n                               \"processed: {}\".format(e))\n\n        if shortcut:\n            script_action.setShortcut(shortcut)\n\n        if icon:\n            iconfile = os.path.expandvars(icon)\n            script_action.iconfile = iconfile\n            script_action_icon = QtWidgets.QIcon(iconfile)\n            script_action.setIcon(script_action_icon)\n\n        if label:\n            script_action.label = label\n\n        if tooltip:\n            script_action.setStatusTip(tooltip)\n\n        script_action.triggered.connect(script_action.run_command)\n        parent.addAction(script_action)\n\n        # Add to our searchable actions\n        self._script_actions.append(script_action)\n\n        return script_action\n\n    def build_from_configuration(self, parent, configuration):\n        \"\"\"Process the configurations and store the configuration\n\n        This creates all submenus from a configuration.json file.\n\n        When the configuration holds the key `main` all scripts under `main` will\n        be added to the main menu first before adding the rest\n\n        Args:\n            parent (ScriptsMenu): script menu instance\n            configuration (list): A ScriptsMenu configuration list\n\n        Returns:\n            None\n\n        \"\"\"\n\n        for item in configuration:\n            assert isinstance(item, dict), \"Configuration is wrong!\"\n\n            # skip items which have no `type` key\n            item_type = item.get('type', None)\n            if not item_type:\n                log.warning(\"Missing 'type' from configuration item\")\n                continue\n\n            # add separator\n            # Special behavior for separators\n            if item_type == \"separator\":\n                parent.addSeparator()\n\n            # add submenu\n            # items should hold a collection of submenu items (dict)\n            elif item_type == \"menu\":\n                assert \"items\" in item, \"Menu is missing 'items' key\"\n                menu = self.add_menu(parent=parent, title=item[\"title\"])\n                self.build_from_configuration(menu, item[\"items\"])\n\n            # add script\n            elif item_type == \"action\":\n                # filter out `type` from the item dict\n                config = {key: value for key, value in\n                          item.items() if key != \"type\"}\n\n                self.add_script(parent=parent, **config)\n\n    def set_update_visible(self, state):\n        self.update_action.setVisible(state)\n\n    def clear_menu(self):\n        \"\"\"Clear all menu items which are not default\n\n        Returns:\n            None\n\n        \"\"\"\n\n        # TODO: Set up a more robust implementation for this\n        # Delete all except the first three actions\n        for _action in self.actions()[3:]:\n            self.removeAction(_action)\n\n    def register_callback(self, modifiers, callback):\n        self._callbacks[modifiers].append(callback)\n\n    def _update_search(self, search):\n        \"\"\"Hide all the samples which do not match the user's import\n\n        Returns:\n            None\n\n        \"\"\"\n\n        if not search:\n            for action in self._script_actions:\n                action.setVisible(True)\n        else:\n            for action in self._script_actions:\n                action.setVisible(action.has_tag(search.lower()))\n\n        # Set visibility for all submenus\n        for action in self.actions():\n            if not action.menu():\n                continue\n\n            menu = action.menu()\n            visible = any(action.isVisible() for action in menu.actions())\n            action.setVisible(visible)\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/scriptsmenu/scriptsmenu.html#client.ayon_core.vendor.python.scriptsmenu.scriptsmenu.ScriptsMenu.__init__","title":"<code>__init__(*args, **kwargs)</code>","text":"<p>Initialize Scripts menu</p> <p>Parameters:</p> Name Type Description Default <code>title</code> <code>str</code> <p>the name of the root menu which will be created</p> required <code>parent (QtWidgets.QObject) </code> <p>the QObject to parent the menu to</p> required <p>Returns:</p> Type Description <p>None</p> Source code in <code>client/ayon_core/vendor/python/scriptsmenu/scriptsmenu.py</code> <pre><code>def __init__(self, *args, **kwargs):\n    \"\"\"Initialize Scripts menu\n\n    Args:\n        title (str): the name of the root menu which will be created\n\n        parent (QtWidgets.QObject) : the QObject to parent the menu to\n\n    Returns:\n        None\n\n    \"\"\"\n    QtWidgets.QMenu.__init__(self, *args, **kwargs)\n\n    self.searchbar = None\n    self.update_action = None\n\n    self._script_actions = []\n    self._callbacks = defaultdict(list)\n\n    # Automatically add it to the parent menu\n    parent = kwargs.get(\"parent\", None)\n    if parent:\n        parent.addMenu(self)\n\n    objectname = kwargs.get(\"objectName\", \"scripts\")\n    title = kwargs.get(\"title\", \"Scripts\")\n    self.setObjectName(objectname)\n    self.setTitle(title)\n\n    # add default items in the menu\n    self.create_default_items()\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/scriptsmenu/scriptsmenu.html#client.ayon_core.vendor.python.scriptsmenu.scriptsmenu.ScriptsMenu.add_menu","title":"<code>add_menu(title, parent=None)</code>","text":"<p>Create a sub menu for a parent widget</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <code>QWidget</code> <p>the object to parent the menu to</p> <code>None</code> <code>title</code> <code>str</code> <p>the title of the menu</p> required <p>Returns:</p> Type Description <p>QtWidget.QMenu instance</p> Source code in <code>client/ayon_core/vendor/python/scriptsmenu/scriptsmenu.py</code> <pre><code>def add_menu(self, title, parent=None):\n    \"\"\"Create a sub menu for a parent widget\n\n    Args:\n        parent(QtWidgets.QWidget): the object to parent the menu to\n\n        title(str): the title of the menu\n\n    Returns:\n         QtWidget.QMenu instance\n    \"\"\"\n\n    if not parent:\n        parent = self\n\n    menu = QtWidgets.QMenu(parent, title)\n    menu.setTitle(title)\n    menu.setObjectName(title)\n    menu.setTearOffEnabled(True)\n    parent.addMenu(menu)\n\n    return menu\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/scriptsmenu/scriptsmenu.html#client.ayon_core.vendor.python.scriptsmenu.scriptsmenu.ScriptsMenu.add_script","title":"<code>add_script(parent, title, command, sourcetype, icon=None, tags=None, label=None, tooltip=None, shortcut=None)</code>","text":"<p>Create an action item which runs a script when clicked</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <code>QWidget</code> <p>The widget to parent the item to</p> required <code>title</code> <code>str</code> <p>The text which will be displayed in the menu</p> required <code>command</code> <code>str</code> <p>The command which needs to be run when the item is            clicked.</p> required <code>sourcetype</code> <code>str</code> <p>The type of command, the way the command is               processed is based on the source type.</p> required <code>icon</code> <code>str</code> <p>The file path of an icon to display with the menu item</p> <code>None</code> <code>tags</code> <code>(list, tuple)</code> <p>Keywords which describe the action</p> <code>None</code> <code>label</code> <code>str</code> <p>A short description of the script which will be displayed          when hovering over the menu item</p> <code>None</code> <code>tooltip</code> <code>str</code> <p>A tip for the user about the usage fo the tool</p> <code>None</code> <code>shortcut</code> <code>str</code> <p>A shortcut to run the command</p> <code>None</code> <p>Returns:</p> Type Description <p>QtWidget.QAction instance</p> Source code in <code>client/ayon_core/vendor/python/scriptsmenu/scriptsmenu.py</code> <pre><code>def add_script(self, parent, title, command, sourcetype, icon=None,\n               tags=None, label=None, tooltip=None, shortcut=None):\n    \"\"\"Create an action item which runs a script when clicked\n\n    Args:\n        parent (QtWidget.QWidget): The widget to parent the item to\n\n        title (str): The text which will be displayed in the menu\n\n        command (str): The command which needs to be run when the item is\n                       clicked.\n\n        sourcetype (str): The type of command, the way the command is\n                          processed is based on the source type.\n\n        icon (str): The file path of an icon to display with the menu item\n\n        tags (list, tuple): Keywords which describe the action\n\n        label (str): A short description of the script which will be displayed\n                     when hovering over the menu item\n\n        tooltip (str): A tip for the user about the usage fo the tool\n\n        shortcut (str): A shortcut to run the command\n\n    Returns:\n        QtWidget.QAction instance\n\n    \"\"\"\n\n    assert tags is None or isinstance(tags, (list, tuple))\n    # Ensure tags is a list\n    tags = list() if tags is None else list(tags)\n    tags.append(title.lower())\n\n    assert icon is None or isinstance(icon, str), (\n        \"Invalid data type for icon, supported : None, string\")\n\n    # create new action\n    script_action = action.Action(parent)\n    script_action.setText(title)\n    script_action.setObjectName(title)\n    script_action.tags = tags\n\n    # link action to root for callback library\n    script_action.root = self\n\n    # Set up the command\n    script_action.sourcetype = sourcetype\n    script_action.command = command\n\n    try:\n        script_action.process_command()\n    except RuntimeError as e:\n        raise RuntimeError(\"Script action can't be \"\n                           \"processed: {}\".format(e))\n\n    if shortcut:\n        script_action.setShortcut(shortcut)\n\n    if icon:\n        iconfile = os.path.expandvars(icon)\n        script_action.iconfile = iconfile\n        script_action_icon = QtWidgets.QIcon(iconfile)\n        script_action.setIcon(script_action_icon)\n\n    if label:\n        script_action.label = label\n\n    if tooltip:\n        script_action.setStatusTip(tooltip)\n\n    script_action.triggered.connect(script_action.run_command)\n    parent.addAction(script_action)\n\n    # Add to our searchable actions\n    self._script_actions.append(script_action)\n\n    return script_action\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/scriptsmenu/scriptsmenu.html#client.ayon_core.vendor.python.scriptsmenu.scriptsmenu.ScriptsMenu.build_from_configuration","title":"<code>build_from_configuration(parent, configuration)</code>","text":"<p>Process the configurations and store the configuration</p> <p>This creates all submenus from a configuration.json file.</p> <p>When the configuration holds the key <code>main</code> all scripts under <code>main</code> will be added to the main menu first before adding the rest</p> <p>Parameters:</p> Name Type Description Default <code>parent</code> <code>ScriptsMenu</code> <p>script menu instance</p> required <code>configuration</code> <code>list</code> <p>A ScriptsMenu configuration list</p> required <p>Returns:</p> Type Description <p>None</p> Source code in <code>client/ayon_core/vendor/python/scriptsmenu/scriptsmenu.py</code> <pre><code>def build_from_configuration(self, parent, configuration):\n    \"\"\"Process the configurations and store the configuration\n\n    This creates all submenus from a configuration.json file.\n\n    When the configuration holds the key `main` all scripts under `main` will\n    be added to the main menu first before adding the rest\n\n    Args:\n        parent (ScriptsMenu): script menu instance\n        configuration (list): A ScriptsMenu configuration list\n\n    Returns:\n        None\n\n    \"\"\"\n\n    for item in configuration:\n        assert isinstance(item, dict), \"Configuration is wrong!\"\n\n        # skip items which have no `type` key\n        item_type = item.get('type', None)\n        if not item_type:\n            log.warning(\"Missing 'type' from configuration item\")\n            continue\n\n        # add separator\n        # Special behavior for separators\n        if item_type == \"separator\":\n            parent.addSeparator()\n\n        # add submenu\n        # items should hold a collection of submenu items (dict)\n        elif item_type == \"menu\":\n            assert \"items\" in item, \"Menu is missing 'items' key\"\n            menu = self.add_menu(parent=parent, title=item[\"title\"])\n            self.build_from_configuration(menu, item[\"items\"])\n\n        # add script\n        elif item_type == \"action\":\n            # filter out `type` from the item dict\n            config = {key: value for key, value in\n                      item.items() if key != \"type\"}\n\n            self.add_script(parent=parent, **config)\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/scriptsmenu/scriptsmenu.html#client.ayon_core.vendor.python.scriptsmenu.scriptsmenu.ScriptsMenu.clear_menu","title":"<code>clear_menu()</code>","text":"<p>Clear all menu items which are not default</p> <p>Returns:</p> Type Description <p>None</p> Source code in <code>client/ayon_core/vendor/python/scriptsmenu/scriptsmenu.py</code> <pre><code>def clear_menu(self):\n    \"\"\"Clear all menu items which are not default\n\n    Returns:\n        None\n\n    \"\"\"\n\n    # TODO: Set up a more robust implementation for this\n    # Delete all except the first three actions\n    for _action in self.actions()[3:]:\n        self.removeAction(_action)\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/scriptsmenu/scriptsmenu.html#client.ayon_core.vendor.python.scriptsmenu.scriptsmenu.ScriptsMenu.create_default_items","title":"<code>create_default_items()</code>","text":"<p>Add a search bar to the top of the menu given</p> Source code in <code>client/ayon_core/vendor/python/scriptsmenu/scriptsmenu.py</code> <pre><code>def create_default_items(self):\n    \"\"\"Add a search bar to the top of the menu given\"\"\"\n\n    # create widget and link function\n    searchbar = QtWidgets.QLineEdit()\n    searchbar.setFixedWidth(120)\n    searchbar.setPlaceholderText(\"Search ...\")\n    searchbar.textChanged.connect(self._update_search)\n    self.searchbar = searchbar\n\n    # create widget holder\n    searchbar_action = QtWidgets.QWidgetAction(self)\n\n    # add widget to widget holder\n    searchbar_action.setDefaultWidget(self.searchbar)\n    searchbar_action.setObjectName(\"Searchbar\")\n\n    # add update button and link function\n    update_action = QtWidgets.QAction(self)\n    update_action.setObjectName(\"Update Scripts\")\n    update_action.setText(\"Update Scripts\")\n    update_action.setVisible(False)\n    update_action.triggered.connect(self.on_update)\n    self.update_action = update_action\n\n    # add action to menu\n    self.addAction(searchbar_action)\n    self.addAction(update_action)\n\n    # add separator object\n    separator = self.addSeparator()\n    separator.setObjectName(\"separator\")\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/scriptsmenu/scriptsmenu.html#client.ayon_core.vendor.python.scriptsmenu.scriptsmenu.load_configuration","title":"<code>load_configuration(path)</code>","text":"<p>Load the configuration from a file</p> <p>Read out the JSON file which will dictate the structure of the scripts menu</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>file path of the .JSON file</p> required <p>Returns:</p> Type Description <p>dict</p> Source code in <code>client/ayon_core/vendor/python/scriptsmenu/scriptsmenu.py</code> <pre><code>def load_configuration(path):\n    \"\"\"Load the configuration from a file\n\n    Read out the JSON file which will dictate the structure of the scripts menu\n\n    Args:\n        path (str): file path of the .JSON file\n\n    Returns:\n        dict\n\n    \"\"\"\n\n    if not os.path.isfile(path):\n        raise AttributeError(\"Given configuration is not \"\n                             \"a file!\\n'{}'\".format(path))\n\n    extension = os.path.splitext(path)[-1]\n    if extension != \".json\":\n        raise AttributeError(\"Given configuration file has unsupported \"\n                             \"file type, provide a .json file\")\n\n    # retrieve and store config\n    with open(path, \"r\") as f:\n        configuration = json.load(f)\n\n    return configuration\n</code></pre>"},{"location":"autoapi/client/ayon_core/vendor/python/scriptsmenu/version.html","title":"version","text":""},{"location":"autoapi/server/index.html","title":"server","text":""},{"location":"autoapi/server/index.html#server.CoreAddon","title":"<code>CoreAddon</code>","text":"<p>               Bases: <code>BaseServerAddon</code></p> Source code in <code>server/__init__.py</code> <pre><code>class CoreAddon(BaseServerAddon):\n    settings_model = CoreSettings\n\n    async def get_default_settings(self):\n        settings_model_cls = self.get_settings_model()\n        return settings_model_cls(**DEFAULT_VALUES)\n\n    async def convert_settings_overrides(\n        self,\n        source_version: str,\n        overrides: dict[str, Any],\n    ) -&gt; dict[str, Any]:\n        convert_settings_overrides(source_version, overrides)\n        # Use super conversion\n        return await super().convert_settings_overrides(\n            source_version, overrides\n        )\n\n    async def get_simple_actions(\n        self,\n        project_name: str | None = None,\n        variant: str = \"production\",\n    ) -&gt; list[SimpleActionManifest]:\n        \"\"\"Return a list of simple actions provided by the addon\"\"\"\n        output = []\n\n        if project_name:\n            # Add 'Create Project Folder Structure' action to folders.\n            output.append(\n                SimpleActionManifest(\n                    identifier=\"core.createprojectstructure\",\n                    label=\"Create Project Folder Structure\",\n                    icon={\n                        \"type\": \"material-symbols\",\n                        \"name\": \"create_new_folder\",\n                    },\n                    order=100,\n                    entity_type=\"project\",\n                    entity_subtypes=None,\n                    allow_multiselection=False,\n                )\n            )\n            output.append(\n                SimpleActionManifest(\n                    identifier=\"core.delivery.version\",\n                    label=\"Deliver versions (launcher action)\",\n                    icon={\n                        \"type\": \"material-symbols\",\n                        \"name\": \"create_new_folder\",\n                    },\n                    order=100,\n                    entity_type=\"version\",\n                    entity_subtypes=None,\n                    allow_multiselection=True,\n                )\n            )\n            output.append(\n                SimpleActionManifest(\n                    identifier=\"core.delivery.list\",\n                    label=\"Deliver versions (launcher action)\",\n                    icon={\n                        \"type\": \"material-symbols\",\n                        \"name\": \"create_new_folder\",\n                    },\n                    order=100,\n                    entity_type=\"list\",\n                    entity_subtypes=[\n                        \"version:review-session\",\n                        \"version:generic\",\n                    ],\n                    allow_multiselection=False,\n                )\n            )\n\n        return output\n\n    async def execute_action(\n        self,\n        executor: ActionExecutor,\n    ) -&gt; ExecuteResponseModel:\n        \"\"\"Execute webactions.\"\"\"\n\n        project_name = executor.context.project_name\n\n        if executor.identifier == \"core.createprojectstructure\":\n            if not project_name:\n                logger.error(\n                    f\"Can't execute {executor.identifier} because\"\n                    \" of missing project name.\"\n                )\n                # Works since AYON server 1.8.3\n                if hasattr(executor, \"get_simple_response\"):\n                    return await executor.get_simple_response(\n                        \"Missing project name\", success=False\n                    )\n                return\n\n            args = [\n                \"create-project-structure\", \"--project\", project_name,\n            ]\n            # Works since AYON server 1.8.3\n            if hasattr(executor, \"get_launcher_response\"):\n                return await executor.get_launcher_response(args)\n\n            return await executor.get_launcher_action_response(args)\n\n        if executor.identifier == \"core.delivery.version\":\n            selected_versions = await executor.context.get_entities()\n            if not selected_versions:\n                return await executor.get_server_action_response(\n                    message=\"No versions available in selection.\",\n                    success=False\n                )\n            version_ids = [version.id for version in selected_versions]\n            event_id = await dispatch_event(\n                topic=\"delivery.requested\",\n                project=project_name,\n                payload={\"version_ids\": version_ids},\n                finished=True,\n            )\n            args = [\n                \"deliver\", \"--project\", project_name,\n                \"--event_id\", event_id,\n            ]\n\n            return await executor.get_launcher_response(args)\n\n        if executor.identifier == \"core.delivery.list\":\n            if not executor.context.entity_ids:\n                return await executor.get_server_action_response(\n                    message=\"No list selected.\",\n                    success=False\n                )\n\n            selected_list = executor.context.entity_ids[0]\n            event_id = await dispatch_event(\n                topic=\"delivery.requested\",\n                project=project_name,\n                payload={\"list_id\": selected_list},\n                finished=True,\n            )\n            args = [\n                \"deliver\", \"--project\", project_name,\n                \"--event_id\", event_id,\n            ]\n\n            return await executor.get_launcher_response(args)\n\n        logger.debug(f\"Unknown action: {executor.identifier}\")\n\n        if hasattr(executor, \"get_simple_response\"):\n            return await executor.get_simple_response(\n                \"Unknown action\", success=False\n            )\n</code></pre>"},{"location":"autoapi/server/index.html#server.CoreAddon.execute_action","title":"<code>execute_action(executor)</code>  <code>async</code>","text":"<p>Execute webactions.</p> Source code in <code>server/__init__.py</code> <pre><code>async def execute_action(\n    self,\n    executor: ActionExecutor,\n) -&gt; ExecuteResponseModel:\n    \"\"\"Execute webactions.\"\"\"\n\n    project_name = executor.context.project_name\n\n    if executor.identifier == \"core.createprojectstructure\":\n        if not project_name:\n            logger.error(\n                f\"Can't execute {executor.identifier} because\"\n                \" of missing project name.\"\n            )\n            # Works since AYON server 1.8.3\n            if hasattr(executor, \"get_simple_response\"):\n                return await executor.get_simple_response(\n                    \"Missing project name\", success=False\n                )\n            return\n\n        args = [\n            \"create-project-structure\", \"--project\", project_name,\n        ]\n        # Works since AYON server 1.8.3\n        if hasattr(executor, \"get_launcher_response\"):\n            return await executor.get_launcher_response(args)\n\n        return await executor.get_launcher_action_response(args)\n\n    if executor.identifier == \"core.delivery.version\":\n        selected_versions = await executor.context.get_entities()\n        if not selected_versions:\n            return await executor.get_server_action_response(\n                message=\"No versions available in selection.\",\n                success=False\n            )\n        version_ids = [version.id for version in selected_versions]\n        event_id = await dispatch_event(\n            topic=\"delivery.requested\",\n            project=project_name,\n            payload={\"version_ids\": version_ids},\n            finished=True,\n        )\n        args = [\n            \"deliver\", \"--project\", project_name,\n            \"--event_id\", event_id,\n        ]\n\n        return await executor.get_launcher_response(args)\n\n    if executor.identifier == \"core.delivery.list\":\n        if not executor.context.entity_ids:\n            return await executor.get_server_action_response(\n                message=\"No list selected.\",\n                success=False\n            )\n\n        selected_list = executor.context.entity_ids[0]\n        event_id = await dispatch_event(\n            topic=\"delivery.requested\",\n            project=project_name,\n            payload={\"list_id\": selected_list},\n            finished=True,\n        )\n        args = [\n            \"deliver\", \"--project\", project_name,\n            \"--event_id\", event_id,\n        ]\n\n        return await executor.get_launcher_response(args)\n\n    logger.debug(f\"Unknown action: {executor.identifier}\")\n\n    if hasattr(executor, \"get_simple_response\"):\n        return await executor.get_simple_response(\n            \"Unknown action\", success=False\n        )\n</code></pre>"},{"location":"autoapi/server/index.html#server.CoreAddon.get_simple_actions","title":"<code>get_simple_actions(project_name=None, variant='production')</code>  <code>async</code>","text":"<p>Return a list of simple actions provided by the addon</p> Source code in <code>server/__init__.py</code> <pre><code>async def get_simple_actions(\n    self,\n    project_name: str | None = None,\n    variant: str = \"production\",\n) -&gt; list[SimpleActionManifest]:\n    \"\"\"Return a list of simple actions provided by the addon\"\"\"\n    output = []\n\n    if project_name:\n        # Add 'Create Project Folder Structure' action to folders.\n        output.append(\n            SimpleActionManifest(\n                identifier=\"core.createprojectstructure\",\n                label=\"Create Project Folder Structure\",\n                icon={\n                    \"type\": \"material-symbols\",\n                    \"name\": \"create_new_folder\",\n                },\n                order=100,\n                entity_type=\"project\",\n                entity_subtypes=None,\n                allow_multiselection=False,\n            )\n        )\n        output.append(\n            SimpleActionManifest(\n                identifier=\"core.delivery.version\",\n                label=\"Deliver versions (launcher action)\",\n                icon={\n                    \"type\": \"material-symbols\",\n                    \"name\": \"create_new_folder\",\n                },\n                order=100,\n                entity_type=\"version\",\n                entity_subtypes=None,\n                allow_multiselection=True,\n            )\n        )\n        output.append(\n            SimpleActionManifest(\n                identifier=\"core.delivery.list\",\n                label=\"Deliver versions (launcher action)\",\n                icon={\n                    \"type\": \"material-symbols\",\n                    \"name\": \"create_new_folder\",\n                },\n                order=100,\n                entity_type=\"list\",\n                entity_subtypes=[\n                    \"version:review-session\",\n                    \"version:generic\",\n                ],\n                allow_multiselection=False,\n            )\n        )\n\n    return output\n</code></pre>"},{"location":"autoapi/server/settings/index.html","title":"settings","text":""},{"location":"autoapi/server/settings/conversion.html","title":"conversion","text":""},{"location":"autoapi/server/settings/main.html","title":"main","text":""},{"location":"autoapi/server/settings/publish_plugins.html","title":"publish_plugins","text":""},{"location":"autoapi/server/settings/publish_plugins.html#server.settings.publish_plugins.CollectUSDLayerContributionsProfileModel","title":"<code>CollectUSDLayerContributionsProfileModel</code>","text":"<p>               Bases: <code>BaseSettingsModel</code></p> <p>Profiles to define instance attribute defaults for USD contribution.</p> Source code in <code>server/settings/publish_plugins.py</code> <pre><code>class CollectUSDLayerContributionsProfileModel(BaseSettingsModel):\n    \"\"\"Profiles to define instance attribute defaults for USD contribution.\"\"\"\n    _layout = \"expanded\"\n    product_base_types: list[str] = SettingsField(\n        default_factory=list,\n        title=\"Product base types\",\n        description=(\n            \"The product base types to match this profile to. When matched,\"\n            \" the settings below would apply to the instance as default\"\n            \" attributes.\"\n        ),\n        section=\"Filter\"\n    )\n    task_types: list[str] = SettingsField(\n        default_factory=list,\n        title=\"Task Types\",\n        enum_resolver=task_types_enum,\n        description=(\n            \"The current create context task type to filter against. This\"\n            \" allows to filter the profile to only be valid if currently \"\n            \" creating from within that task type.\"\n        ),\n    )\n    task_names: list[str] = SettingsField(\n        default_factory=list,\n        title=\"Task names\",\n    )\n    contribution_enabled: bool = SettingsField(\n        True,\n        title=\"Contribution Enabled (default)\",\n        description=(\n            \"The default state for USD Contribution being marked enabled or\"\n            \" disabled for this profile.\"\n        ),\n        section=\"Instance attribute defaults\",\n    )\n    contribution_layer: str = SettingsField(\n        \"\",\n        title=\"Contribution Department Layer\",\n        description=(\n            \"The default contribution layer to apply the contribution to when\"\n            \" matching this profile. The layer name should be in the\"\n            \" 'Department Layer Orders' list to get a sensible order.\"\n        ),\n    )\n    contribution_target_product: str = SettingsField(\n        \"usdAsset\",\n        title=\"Target Product\",\n        description=(\n            \"The default destination product name to apply the contribution to\"\n            \" when matching this profile.\"\n            \" Usually e.g. 'usdAsset' or 'usdShot'.\"\n        ),\n    )\n    contribution_apply_as_variant: bool = SettingsField(\n        True,\n        title=\"Apply as variant\",\n        description=(\n            \"The default 'Apply as variant' state for instances matching this\"\n            \" profile. Usually enabled for asset contributions and disabled\"\n            \" for shot contributions.\"\n        ),\n    )\n    contribution_variant_set_name: str = SettingsField(\n        \"{layer}\",\n        title=\"Variant Set name\",\n        description=(\n            \"The default variant set name for instances matching this profile.\"\n        ),\n    )\n    contribution_variant: str = SettingsField(\n        \"{variant}\",\n        title=\"Variant Name\",\n        description=(\n            \"The default variant name for instances matching this profile.\"\n        ),\n    )\n    contribution_variant_is_default: bool = SettingsField(\n        False,\n        title=\"Set as default variant selection\",\n        description=(\n            \"Whether to set this instance's variant name as the \"\n            \"default selected variant name for the variant set.\\n\"\n            \"It is always expected to be enabled for only one \"\n            \"variant name in the variant set.\\n\"\n            \"The behavior is unpredictable if multiple instances \"\n            \"for the same variant set have this enabled.\"\n        ),\n    )\n</code></pre>"},{"location":"autoapi/server/settings/publish_plugins.html#server.settings.publish_plugins.EntityListFolderModel","title":"<code>EntityListFolderModel</code>","text":"<p>               Bases: <code>BaseSettingsModel</code></p> <p>Folder must have label and can be scoped to views.</p> <p>Scope of the folder can be defined for all views or use just the view     matching list type of created list. In case the list folder already     exists the settings are not used and we just make sure the list can     be seen under the folder.</p> Source code in <code>server/settings/publish_plugins.py</code> <pre><code>class EntityListFolderModel(BaseSettingsModel):\n    \"\"\"Folder must have label and can be scoped to views.\n\n    Scope of the folder can be defined for all views or use just the view\n        matching list type of created list. In case the list folder already\n        exists the settings are not used and we just make sure the list can\n        be seen under the folder.\n\n    \"\"\"\n    _layout = \"expanded\"\n    label: str = SettingsField(\n        \"\",\n        title=\"Folder label\",\n        description=(\n            \"The label of the folder to create. \"\n            \"Anatomy formattable template for the name.\"\n        ),\n    )\n    # Don't use explicit scope enum, rather ask if the folder should be seen\n    #   everywhere or just in the list type matching created list.\n    scope_def: str = SettingsField(\n        \"all\",\n        enum_resolver=list_folder_scope_def,\n        title=\"Scope\",\n    )\n</code></pre>"},{"location":"autoapi/server/settings/publish_plugins.html#server.settings.publish_plugins.ExtractBurninDef","title":"<code>ExtractBurninDef</code>","text":"<p>               Bases: <code>BaseSettingsModel</code></p> Source code in <code>server/settings/publish_plugins.py</code> <pre><code>class ExtractBurninDef(BaseSettingsModel):\n    _isGroup = True\n    _layout = \"expanded\"\n    name: str = SettingsField(\"\")\n    TOP_LEFT: str = SettingsField(\"\", title=\"Top Left\")\n    TOP_CENTERED: str = SettingsField(\"\", title=\"Top Centered\")\n    TOP_RIGHT: str = SettingsField(\"\", title=\"Top Right\")\n    BOTTOM_LEFT: str = SettingsField(\"\", title=\"Bottom Left\")\n    BOTTOM_CENTERED: str = SettingsField(\"\", title=\"Bottom Centered\")\n    BOTTOM_RIGHT: str = SettingsField(\"\", title=\"Bottom Right\")\n    filter: ExtractBurninDefFilter = SettingsField(\n        default_factory=ExtractBurninDefFilter,\n        title=\"Additional filtering\"\n    )\n\n    @validator(\"name\")\n    def validate_name(cls, value):\n        \"\"\"Ensure name does not contain weird characters\"\"\"\n        return normalize_name(value)\n</code></pre>"},{"location":"autoapi/server/settings/publish_plugins.html#server.settings.publish_plugins.ExtractBurninDef.validate_name","title":"<code>validate_name(value)</code>","text":"<p>Ensure name does not contain weird characters</p> Source code in <code>server/settings/publish_plugins.py</code> <pre><code>@validator(\"name\")\ndef validate_name(cls, value):\n    \"\"\"Ensure name does not contain weird characters\"\"\"\n    return normalize_name(value)\n</code></pre>"},{"location":"autoapi/server/settings/publish_plugins.html#server.settings.publish_plugins.ExtractOIIOPostProcessModel","title":"<code>ExtractOIIOPostProcessModel</code>","text":"<p>               Bases: <code>BaseSettingsModel</code></p> <p>Process representation images with <code>oiiotool</code> on publish.</p> <p>This could be used to convert images to different formats, convert to scanline images or flatten deep images.</p> Source code in <code>server/settings/publish_plugins.py</code> <pre><code>class ExtractOIIOPostProcessModel(BaseSettingsModel):\n    \"\"\"Process representation images with `oiiotool` on publish.\n\n    This could be used to convert images to different formats, convert to\n    scanline images or flatten deep images.\n    \"\"\"\n    enabled: bool = SettingsField(True)\n    profiles: list[ExtractOIIOPostProcessProfileModel] = SettingsField(\n        default_factory=list, title=\"Profiles\"\n    )\n</code></pre>"},{"location":"autoapi/server/settings/publish_plugins.html#server.settings.publish_plugins.ExtractOIIOTranscodeModel","title":"<code>ExtractOIIOTranscodeModel</code>","text":"<p>               Bases: <code>BaseSettingsModel</code></p> <p>Color conversion transcoding using OIIO for images mostly aimed at transcoding for reviewables (it'll process and output only RGBA channels).</p> Source code in <code>server/settings/publish_plugins.py</code> <pre><code>class ExtractOIIOTranscodeModel(BaseSettingsModel):\n    \"\"\"Color conversion transcoding using OIIO for images mostly aimed at\n    transcoding for reviewables (it'll process and output only RGBA channels).\n    \"\"\"\n    enabled: bool = SettingsField(True)\n    profiles: list[ExtractOIIOTranscodeProfileModel] = SettingsField(\n        default_factory=list, title=\"Profiles\"\n    )\n</code></pre>"},{"location":"autoapi/server/settings/publish_plugins.html#server.settings.publish_plugins.ExtractReviewOutputDefModel","title":"<code>ExtractReviewOutputDefModel</code>","text":"<p>               Bases: <code>BaseSettingsModel</code></p> Source code in <code>server/settings/publish_plugins.py</code> <pre><code>class ExtractReviewOutputDefModel(BaseSettingsModel):\n    _layout = \"expanded\"\n    name: str = SettingsField(\"\", title=\"Name\")\n    ext: str = SettingsField(\"\", title=\"Output extension\")\n    # TODO use some different source of tags\n    tags: list[str] = SettingsField(default_factory=list, title=\"Tags\")\n    burnins: list[str] = SettingsField(\n        default_factory=list, title=\"Link to a burnin by name\"\n    )\n    ffmpeg_args: ExtractReviewFFmpegModel = SettingsField(\n        default_factory=ExtractReviewFFmpegModel,\n        title=\"FFmpeg arguments\"\n    )\n    filter: ExtractReviewFilterModel = SettingsField(\n        default_factory=ExtractReviewFilterModel,\n        title=\"Additional output filtering\"\n    )\n    overscan_crop: str = SettingsField(\n        \"\",\n        title=\"Overscan crop\",\n        description=(\n            \"Crop input overscan. See the documentation for more information.\"\n        )\n    )\n    overscan_color: ColorRGBA_uint8 = SettingsField(\n        (0, 0, 0, 0.0),\n        title=\"Overscan color\",\n        description=(\n            \"Overscan color is used when input aspect ratio is not\"\n            \" same as output aspect ratio.\"\n        )\n    )\n    # overscan_color: ColorRGB_uint8 = SettingsField(\n    #     (0, 0, 0),\n    #     title=\"Overscan color\",\n    #     description=(\n    #         \"Overscan color is used when input aspect ratio is not\"\n    #         \" same as output aspect ratio.\"\n    #     )\n    # )\n    width: int = SettingsField(\n        0,\n        ge=0,\n        le=100000,\n        title=\"Output width\",\n        description=(\n            \"Width and Height must be both set to higher\"\n            \" value than 0 else source resolution is used.\"\n        )\n    )\n    height: int = SettingsField(\n        0,\n        title=\"Output height\",\n        ge=0,\n        le=100000,\n    )\n    scale_pixel_aspect: bool = SettingsField(\n        True,\n        title=\"Scale pixel aspect\",\n        description=(\n            \"Rescale input when it's pixel aspect ratio is not 1.\"\n            \" Useful for anamorphic reviews.\"\n        )\n    )\n    bg_color: ColorRGBA_uint8 = SettingsField(\n        (0, 0, 0, 0.0),\n        description=(\n            \"Background color is used only when input have transparency\"\n            \" and Alpha is higher than 0.\"\n        ),\n        title=\"Background color\",\n    )\n    letter_box: ExtractReviewLetterBox = SettingsField(\n        default_factory=ExtractReviewLetterBox,\n        title=\"Letter Box\"\n    )\n    fill_missing_frames: str = SettingsField(\n        title=\"Handle missing frames\",\n        default=\"closest_existing\",\n        description=\"How to handle gaps in sequence frame ranges.\",\n        enum_resolver=_handle_missing_frames_enum\n    )\n\n    @validator(\"name\")\n    def validate_name(cls, value):\n        \"\"\"Ensure name does not contain weird characters\"\"\"\n        return normalize_name(value)\n</code></pre>"},{"location":"autoapi/server/settings/publish_plugins.html#server.settings.publish_plugins.ExtractReviewOutputDefModel.validate_name","title":"<code>validate_name(value)</code>","text":"<p>Ensure name does not contain weird characters</p> Source code in <code>server/settings/publish_plugins.py</code> <pre><code>@validator(\"name\")\ndef validate_name(cls, value):\n    \"\"\"Ensure name does not contain weird characters\"\"\"\n    return normalize_name(value)\n</code></pre>"},{"location":"autoapi/server/settings/publish_plugins.html#server.settings.publish_plugins.ExtractThumbnailFromSourceModel","title":"<code>ExtractThumbnailFromSourceModel</code>","text":"<p>               Bases: <code>BaseSettingsModel</code></p> <p>Thumbnail extraction from source files using ffmpeg and oiiotool.</p> Source code in <code>server/settings/publish_plugins.py</code> <pre><code>class ExtractThumbnailFromSourceModel(BaseSettingsModel):\n    \"\"\"Thumbnail extraction from source files using ffmpeg and oiiotool.\"\"\"\n    enabled: bool = SettingsField(True)\n\n    target_size: ResizeModel = SettingsField(\n        default_factory=ResizeModel, title=\"Target size\"\n    )\n    background_color: ColorRGBA_uint8 = SettingsField(\n        (0, 0, 0, 0.0), title=\"Background color\"\n    )\n</code></pre>"},{"location":"autoapi/server/settings/publish_plugins.html#server.settings.publish_plugins.IntegrateProductGroupModel","title":"<code>IntegrateProductGroupModel</code>","text":"<p>               Bases: <code>BaseSettingsModel</code></p> <p>Group published products by filtering logic.</p> <p>Set all published instances as a part of specific group named according  to 'Template'.</p> <p>Implemented all variants of placeholders '{task}', '{product[type]}', '{host}', '{product[name]}', '{renderlayer}'.</p> Source code in <code>server/settings/publish_plugins.py</code> <pre><code>class IntegrateProductGroupModel(BaseSettingsModel):\n    \"\"\"Group published products by filtering logic.\n\n    Set all published instances as a part of specific group named according\n     to 'Template'.\n\n    Implemented all variants of placeholders '{task}', '{product[type]}',\n    '{host}', '{product[name]}', '{renderlayer}'.\n    \"\"\"\n\n    _isGroup = True\n    product_grouping_profiles: list[IntegrateProductGroupProfile] = (\n        SettingsField(\n            default_factory=list,\n            title=\"Product group profiles\"\n        )\n    )\n</code></pre>"},{"location":"autoapi/server/settings/publish_plugins.html#server.settings.publish_plugins.PreIntegrateThumbnailsModel","title":"<code>PreIntegrateThumbnailsModel</code>","text":"<p>               Bases: <code>BaseSettingsModel</code></p> <p>Explicitly set if Thumbnail representation should be integrated.</p> <p>If no matching profile set, existing state from Host implementation is kept.</p> Source code in <code>server/settings/publish_plugins.py</code> <pre><code>class PreIntegrateThumbnailsModel(BaseSettingsModel):\n    \"\"\"Explicitly set if Thumbnail representation should be integrated.\n\n    If no matching profile set, existing state from Host implementation\n    is kept.\n    \"\"\"\n\n    _isGroup = True\n    enabled: bool = SettingsField(True)\n    integrate_profiles: list[PreIntegrateThumbnailsProfile] = SettingsField(\n        default_factory=list,\n        title=\"Integrate profiles\"\n    )\n</code></pre>"},{"location":"autoapi/server/settings/publish_plugins.html#server.settings.publish_plugins.ValidateIntentModel","title":"<code>ValidateIntentModel</code>","text":"<p>               Bases: <code>BaseSettingsModel</code></p> <p>Validate if Publishing intent was selected.</p> <p>It is possible to disable validation for specific publishing context with profiles.</p> Source code in <code>server/settings/publish_plugins.py</code> <pre><code>class ValidateIntentModel(BaseSettingsModel):\n    \"\"\"Validate if Publishing intent was selected.\n\n    It is possible to disable validation for specific publishing context\n    with profiles.\n    \"\"\"\n\n    _isGroup = True\n    enabled: bool = SettingsField(False)\n    profiles: list[ValidateIntentProfile] = SettingsField(default_factory=list)\n</code></pre>"},{"location":"autoapi/server/settings/publish_plugins.html#server.settings.publish_plugins.ensure_unique_resolution_option","title":"<code>ensure_unique_resolution_option(objects, field_name=None)</code>","text":"<p>Ensure a list of objects have unique option attributes.</p> <p>This function checks if the list of objects has unique 'width', 'height' and 'pixel_aspect' properties.</p> Source code in <code>server/settings/publish_plugins.py</code> <pre><code>def ensure_unique_resolution_option(\n        objects: list[Any], field_name: str | None = None) -&gt; None:  # noqa: C901\n    \"\"\"Ensure a list of objects have unique option attributes.\n\n    This function checks if the list of objects has unique 'width',\n    'height' and 'pixel_aspect' properties.\n    \"\"\"\n    options = set()\n    for obj in objects:\n        item_test_text = f\"{obj.width}x{obj.height}x{obj.pixel_aspect}\"\n        if item_test_text in options:\n            raise BadRequestException(\n                f\"Duplicate option '{item_test_text}'\")\n\n        options.add(item_test_text)\n</code></pre>"},{"location":"autoapi/server/settings/tools.html","title":"tools","text":""},{"location":"autoapi/server/settings/tools.html#server.settings.tools.DiscoverValidationModel","title":"<code>DiscoverValidationModel</code>","text":"<p>               Bases: <code>BaseSettingsModel</code></p> <p>Strictly validate publish plugins discovery.</p> <p>Artist won't be able to publish if path to publish plugin fails to be     imported.</p> Source code in <code>server/settings/tools.py</code> <pre><code>class DiscoverValidationModel(BaseSettingsModel):\n    \"\"\"Strictly validate publish plugins discovery.\n\n    Artist won't be able to publish if path to publish plugin fails to be\n        imported.\n\n    \"\"\"\n    _isGroup = True\n    enabled: bool = SettingsField(\n        False,\n        description=\"Enable strict mode of plugins discovery\",\n    )\n    ignore_paths: list[str] = SettingsField(\n        default_factory=list,\n        title=\"Ignored paths (regex)\",\n        description=(\n            \"Paths that do match regex will be skipped in validation.\"\n        ),\n    )\n</code></pre>"},{"location":"autoapi/server/settings/tools.html#server.settings.tools.FilterCreatorProfile","title":"<code>FilterCreatorProfile</code>","text":"<p>               Bases: <code>BaseSettingsModel</code></p> <p>Provide list of allowed Creator identifiers for context</p> Source code in <code>server/settings/tools.py</code> <pre><code>class FilterCreatorProfile(BaseSettingsModel):\n    \"\"\"Provide list of allowed Creator identifiers for context\"\"\"\n\n    _layout = \"expanded\"\n    host_names: list[str] = SettingsField(\n        default_factory=list, title=\"Host names\"\n    )\n    task_types: list[str] = SettingsField(\n        default_factory=list,\n        title=\"Task types\",\n        enum_resolver=task_types_enum\n    )\n    task_names: list[str] = SettingsField(\n        default_factory=list,\n        title=\"Task names\")\n    creator_labels: list[str] = SettingsField(\n        default_factory=list,\n        title=\"Allowed Creator Labels\",\n        description=\"Copy creator label from Publisher, regex supported.\"\n    )\n</code></pre>"},{"location":"autoapi/ui_preview/index.html","title":"ui_preview","text":""},{"location":"autoapi/ui_preview/buttons.html","title":"buttons","text":""},{"location":"autoapi/ui_preview/card_view.html","title":"card_view","text":""},{"location":"autoapi/ui_preview/check_box.html","title":"check_box","text":"<p>checkbox</p>"},{"location":"autoapi/ui_preview/combo_box.html","title":"combo_box","text":"<p>Combo-box components for the AYON UI Qt library.</p> <p>This module provides :class:<code>AYComboBox</code>, a styled :class:<code>QComboBox</code> subclass that supports per-item coloured icons, a short-text display mode, and an icon-only display mode via :class:<code>~ayon_core.ui.data_models.MenuSize</code>. A dropdown arrow is drawn using the Material Symbol <code>arrow_drop_down</code> icon so the widget is visually recognizable as a dropdown when show_chevron is true.</p> <p>It also exposes :class:<code>AYComboBoxModel</code>, the default :class:<code>QStandardItemModel</code> subclass that adds two extra item-data roles:</p> <ul> <li><code>ShortTextRole</code> - an abbreviated label shown when the combo-box is in   :attr:<code>~ayon_core.ui.data_models.MenuSize.Short</code> mode.</li> <li><code>IconNameRole</code> - the Material Symbol icon name used to regenerate icons   when the inverted colour mode is toggled.</li> </ul> <p>A drop-in sample dataset :data:<code>ALL_STATUSES</code> is included for quick prototyping and automated tests.</p> <p>Typical usage::</p> <pre><code>from ayon_core.ui.components.combo_box import AYComboBox, ALL_STATUSES\n\ncombo = AYComboBox(parent=my_widget, items=ALL_STATUSES)\ncombo.set_size(\"short\")   # or MenuSize.Short\ncombo.set_inverted(True)\n</code></pre> <p>Custom model usage (must expose <code>ShortTextRole</code> and <code>IconNameRole</code>)::</p> <pre><code>model = MyCustomModel(parent=combo)\ncombo.setModel(model)\n</code></pre> Note <p>When a custom model that does not expose <code>ShortTextRole</code> / <code>IconNameRole</code> is set via :meth:<code>AYComboBox.setModel</code>, the widget falls back to displaying <code>\"&lt; INCOMPATIBLE MODEL &gt;\"</code> in short mode and raises :exc:<code>RuntimeError</code> when :meth:<code>AYComboBox.add_item</code> or :meth:<code>AYComboBox.update_items</code> is called.</p>"},{"location":"autoapi/ui_preview/comment.html","title":"comment","text":""},{"location":"autoapi/ui_preview/container.html","title":"container","text":""},{"location":"autoapi/ui_preview/entity_card.html","title":"entity_card","text":""},{"location":"autoapi/ui_preview/entity_path.html","title":"entity_path","text":""},{"location":"autoapi/ui_preview/entity_thumbnail.html","title":"entity_thumbnail","text":""},{"location":"autoapi/ui_preview/filter.html","title":"filter","text":""},{"location":"autoapi/ui_preview/filter.html#ui_preview.filter.build_filter_preview_widget","title":"<code>build_filter_preview_widget()</code>","text":"<p>Build test widget.</p> Source code in <code>ui_preview/filter.py</code> <pre><code>def build_filter_preview_widget() -&gt; QtWidgets.QWidget:\n    \"\"\"Build test widget.\"\"\"\n    w = AYContainer(variant=AYContainer.Variants.High, layout_margin=10)\n    w.add_widget(\n        AYFilterByCategory(\n            label=\"Sort by\",\n            items=[\n                FilterItem(\"task\", \"Task\"),\n                FilterItem(\"folder\", \"Folder\", selected=True),\n                FilterItem(\"status\", \"Status\", selected=True),\n                FilterItem(\"priority\", \"Priority\"),\n                FilterItem(\"due_date\", \"Due Date\"),\n            ],\n        )\n    )\n    w.addStretch(10)\n    return w\n</code></pre>"},{"location":"autoapi/ui_preview/frame.html","title":"frame","text":""},{"location":"autoapi/ui_preview/frame.html#ui_preview.frame.build_frame_preview_widget","title":"<code>build_frame_preview_widget()</code>","text":"<p>All frame variants.</p> Source code in <code>ui_preview/frame.py</code> <pre><code>def build_frame_preview_widget():\n    \"\"\"All frame variants.\"\"\"\n    w = QtWidgets.QWidget()\n    w.setMinimumWidth(300)\n    lyt = AYVBoxLayout(w, margin=8, spacing=8)\n\n    for variant in AYFrame.Variants:\n        frame = AYFrame(variant=variant)\n        frame.setFixedHeight(40)\n        frame.setToolTip(variant.value)\n        lyt.addWidget(frame)\n\n    return w\n</code></pre>"},{"location":"autoapi/ui_preview/gallery_dialog.html","title":"gallery_dialog","text":""},{"location":"autoapi/ui_preview/label.html","title":"label","text":""},{"location":"autoapi/ui_preview/line_edit.html","title":"line_edit","text":""},{"location":"autoapi/ui_preview/order.html","title":"order","text":""},{"location":"autoapi/ui_preview/page_button.html","title":"page_button","text":""},{"location":"autoapi/ui_preview/slicer.html","title":"slicer","text":""},{"location":"autoapi/ui_preview/slider.html","title":"slider","text":""},{"location":"autoapi/ui_preview/style.html","title":"style","text":""},{"location":"autoapi/ui_preview/table_filter.html","title":"table_filter","text":""},{"location":"autoapi/ui_preview/table_filter.html#ui_preview.table_filter.build_table_filter_widget","title":"<code>build_table_filter_widget()</code>","text":"<p>Build test UI: AYTreeView folder navigator + AYTableFilter table.</p> Source code in <code>ui_preview/table_filter.py</code> <pre><code>def build_table_filter_widget() -&gt; QtWidgets.QWidget:\n    \"\"\"Build test UI: AYTreeView folder navigator + AYTableFilter table.\"\"\"\n\n    # --- mutable state ---\n    context = PreviewContext()\n\n    # All root-level rows (folders + leaves) for tree-mode \"show all\"\n    _all_root_rows: list[dict] = list(HIERARCHICAL_TEST_DATA.get(None, []))\n\n    # All leaf (non-folder) rows across every level for flat \"show all\"\n    _all_leaf_rows: list[dict] = [\n        row\n        for rows in HIERARCHICAL_TEST_DATA.values()\n        for row in rows\n        if not row.get(\"has_children\", False)\n    ]\n\n    # Wrap the hierarchical fetch so root-level calls respect the\n    # currently selected tree folder and current mode.\n    _hier_fetch = make_hierarchical_test_fetch(HIERARCHICAL_TEST_DATA)\n\n    def _table_fetch(\n        page: int,\n        page_size: int,\n        sort_key: str | None = None,\n        descending: bool = False,\n        parent_id: str | None = None,\n    ) -&gt; list[dict]:\n        if parent_id is not None:\n            # tree-mode child expansion \u2014 pass through to real data\n            return _hier_fetch(\n                page, page_size, sort_key, descending, parent_id\n            )\n        folder_id = context.selected_folder\n        if folder_id is None:\n            # nothing selected: flat \u2192 leaves only; tree \u2192 all root rows\n            rows = list(\n                _all_root_rows if context.tree_mode else _all_leaf_rows\n            )\n        elif context.tree_mode:\n            # folder selected, tree mode: root children of that folder\n            rows = list(HIERARCHICAL_TEST_DATA.get(folder_id, []))\n        else:\n            # folder selected, flat mode: leaf children of that folder\n            rows = [\n                r\n                for r in HIERARCHICAL_TEST_DATA.get(folder_id, [])\n                if not r.get(\"has_children\", False)\n            ]\n        if sort_key:\n            rows = sorted(\n                rows,\n                key=lambda r: (\n                    r.get(sort_key) is None,\n                    str(r.get(sort_key, \"\")),\n                ),\n                reverse=descending,\n            )\n        start = page * page_size\n        return rows[start: start + page_size]\n\n    # --- Table model &amp; filter ---\n    columns = [\n        TableColumn(\n            \"thumb\", \"Thumbnail\", width=75, sortable=False, icon=\"image\"\n        ),\n        TableColumn(\n            \"name\",\n            \"Name\",\n            width=250,\n            sortable=True,\n            icon=\"label\",\n            tree_position=True,\n        ),\n        TableColumn(\n            \"status\", \"Status\", width=100, sortable=True, icon=\"circle\"\n        ),\n        TableColumn(\n            \"type\", \"Type\", width=100, sortable=True, icon=\"category\"\n        ),\n        TableColumn(\n            \"author\", \"Author\", width=100, sortable=False, icon=\"person\"\n        ),\n        TableColumn(\n            \"version\", \"Version\", width=70, sortable=True, icon=\"history\"\n        ),\n    ]\n    table_model = PaginatedTableModel(\n        fetch_page=_table_fetch,\n        columns=columns,\n        page_size=50,\n    )\n\n    filter_bar = AYTableFilter(model=table_model)\n    filter_bar.filters_changed.connect(\n        lambda criteria: print(\n            \"[test]  filters changed: \"\n            f\"{[(c.key, c.values) for c in criteria]}\"\n        )\n    )\n\n    switch = AYCheckBox(\n        \"Show Hierarchy\", variant=AYCheckBox.Variants.Button\n    )\n\n    def _on_tree_mode_toggle(enabled: bool) -&gt; None:\n        context.tree_mode = enabled\n        table_model.set_tree_mode(enabled)\n\n    switch.toggled.connect(_on_tree_mode_toggle)\n\n    folder_tree_model = LazyTreeModel(fetch_children=_fetch_tree_folders)\n    tree_view = AYTreeView(variant=AYTreeView.Variants.Low)\n    tree_view.setModel(folder_tree_model)\n    tree_view.setFixedWidth(180)\n\n    def _on_tree_selection(selected, deselected) -&gt; None:\n        indexes = selected.indexes()\n        if indexes:\n            node = indexes[0].internalPointer()\n            folder_id = node.tree_node.id if node.tree_node else None\n            context.selected_folder = folder_id\n            print(f\"[test]  folder selected: {folder_id!r}\")\n        else:\n            context.selected_folder = None\n            print(\"[test]  no folder selected (showing all leaf rows)\")\n        table_model.reset_data()\n\n    tree_view.selection_changed.connect(_on_tree_selection)\n\n    # --- Layout ---\n    outer = AYContainer(\n        layout=AYContainer.Layout.HBox,\n        variant=AYContainer.Variants.Low,\n        layout_margin=10,\n        layout_spacing=8,\n    )\n\n    right = AYContainer(\n        layout=AYContainer.Layout.VBox,\n        layout_margin=0,\n        layout_spacing=4,\n    )\n\n    filter_row = AYContainer(\n        layout=AYContainer.Layout.HBox,\n        layout_margin=0,\n        layout_spacing=4,\n    )\n    filter_row.add_widget(filter_bar, stretch=1)\n    filter_row.add_widget(switch)\n    right.add_widget(filter_row)\n\n    table = AYTableView()\n    table.setModel(filter_bar.filter_model)\n    table.setMinimumHeight(400)\n    table.selection_changed.connect(\n        lambda sel, desel: print(\n            f\"[test]  table selection: {[i.data() for i in sel.indexes()]}\"\n        )\n    )\n    right.add_widget(table, stretch=1)\n\n    outer.add_widget(tree_view)\n    outer.add_widget(right, stretch=1)\n    outer.setMinimumWidth(900)\n    return outer\n</code></pre>"},{"location":"autoapi/ui_preview/table_model.html","title":"table_model","text":""},{"location":"autoapi/ui_preview/table_model.html#ui_preview.table_model.make_hierarchical_test_fetch","title":"<code>make_hierarchical_test_fetch(data)</code>","text":"<p>Create a fetch_page callback from hierarchical test data.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict[str | None, list[dict[str, Any]]]</code> <p>Mapping of parent_id -&gt; list[row_dict].   <code>None</code> key holds the root-level rows.</p> required <p>Returns:</p> Type Description <code>Callable[[int, int, str | None, bool, str | None], list[dict[str, Any]]]</code> <p>A callable suitable for PaginatedTableModel in tree mode.</p> Source code in <code>ui_preview/table_model.py</code> <pre><code>def make_hierarchical_test_fetch(\n    data: dict[str | None, list[dict[str, Any]]],\n) -&gt; Callable[[int, int, str | None, bool, str | None], list[dict[str, Any]]]:\n    \"\"\"Create a fetch_page callback from hierarchical test data.\n\n    Args:\n        data: Mapping of parent_id -&gt; list[row_dict].\n              ``None`` key holds the root-level rows.\n\n    Returns:\n        A callable suitable for PaginatedTableModel in tree mode.\n    \"\"\"\n\n    def _fetch(\n        page: int,\n        page_size: int,\n        sort_key: str | None,\n        descending: bool,\n        parent_id: str | None = None,\n    ) -&gt; list[dict[str, Any]]:\n        print(\n            f\"[test]  Fetching page {page} (page_size={page_size}, \"\n            f\"sort_key={sort_key!r}, descending={descending}, \"\n            f\"parent_id={parent_id!r})\"\n        )\n        rows = list(data.get(parent_id, []))\n        if sort_key:\n            rows = sorted(\n                rows,\n                key=lambda r: (\n                    r.get(sort_key) is None,\n                    str(r.get(sort_key, \"\")),\n                ),\n                reverse=descending,\n            )\n        start = page * page_size\n        end = start + page_size\n        return rows[start:end]\n\n    return _fetch\n</code></pre>"},{"location":"autoapi/ui_preview/table_model.html#ui_preview.table_model.make_test_fetch","title":"<code>make_test_fetch(data)</code>","text":"<p>Create a flat fetch_page callback from static data.</p> <p><code>parent_id</code> is accepted but ignored \u2014 all data lives at root level.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>list[dict[str, Any]]</code> <p>The full dataset to paginate.</p> required <p>Returns:</p> Type Description <code>Callable[[int, int, str | None, bool, str | None], list[dict[str, Any]]]</code> <p>A callable suitable for PaginatedTableModel.</p> Source code in <code>ui_preview/table_model.py</code> <pre><code>def make_test_fetch(\n    data: list[dict[str, Any]],\n) -&gt; Callable[[int, int, str | None, bool, str | None], list[dict[str, Any]]]:\n    \"\"\"Create a flat fetch_page callback from static data.\n\n    ``parent_id`` is accepted but ignored \u2014 all data lives at root level.\n\n    Args:\n        data: The full dataset to paginate.\n\n    Returns:\n        A callable suitable for PaginatedTableModel.\n    \"\"\"\n\n    def _fetch(\n        page: int,\n        page_size: int,\n        sort_key: str | None,\n        descending: bool,\n        parent_id: str | None = None,  # noqa: ARG001\n    ) -&gt; list[dict[str, Any]]:\n        print(\n            f\"[test]  Fetching page {page} (page_size={page_size}, \"\n            f\"sort_key={sort_key!r}, descending={descending})\"\n        )\n        rows = data\n        if sort_key:\n            rows = sorted(\n                data,\n                key=lambda r: (\n                    r.get(sort_key) is None,\n                    str(r.get(sort_key, \"\")),\n                ),\n                reverse=descending,\n            )\n        start = page * page_size\n        end = start + page_size\n        return rows[start:end]\n\n    return _fetch\n</code></pre>"},{"location":"autoapi/ui_preview/table_view.html","title":"table_view","text":""},{"location":"autoapi/ui_preview/table_view.html#ui_preview.table_view.build_table_view_preview_widget","title":"<code>build_table_view_preview_widget()</code>","text":"<p>Build test UI with one AYTableView per variant.</p> Source code in <code>ui_preview/table_view.py</code> <pre><code>def build_table_view_preview_widget() -&gt; QtWidgets.QWidget:\n    \"\"\"Build test UI with one AYTableView per variant.\"\"\"\n\n    container = AYContainer(\n        variant=AYContainer.Variants.High,\n        layout=AYContainer.Layout.VBox,\n        layout_margin=20,\n        layout_spacing=10,\n    )\n\n    # label + hierarchy switch\n    top_bar = AYContainer(\n        variant=AYContainer.Variants.High,\n        layout=AYContainer.Layout.HBox,\n    )\n    label = QtWidgets.QLabel(\"variant: tree mode (hierarchical)\")\n    switch = AYCheckBox(\n        \"Show Hierarchy\", variant=AYCheckBox.Variants.Button\n    )\n    top_bar.add_widget(label)\n    top_bar.add_widget(switch)\n    container.add_widget(top_bar)\n\n    # define model \u2014 \"actions\" column uses a widget factory\n    tree_columns = [\n        TableColumn(\"thumb\", \"Thumbnail\", width=75, sortable=False),\n        TableColumn(\n            \"name\", \"Name\", width=160, sortable=True, tree_position=True\n        ),\n        TableColumn(\"status\", \"Status\", width=100, sortable=True),\n        TableColumn(\"type\", \"Type\", width=100, sortable=True),\n        TableColumn(\"author\", \"Author\", width=100, sortable=False),\n        TableColumn(\"version\", \"Version\", width=70, sortable=True),\n        TableColumn(\n            \"actions\",\n            \"Actions\",\n            width=90,\n            sortable=False,\n            widget_factory=_make_button_factory(\"Open\"),\n        ),\n    ]\n    tree_fetch = make_hierarchical_test_fetch(HIERARCHICAL_TEST_DATA)\n    tree_model = PaginatedTableModel(\n        fetch_page=tree_fetch,\n        columns=tree_columns,\n        page_size=50,\n    )\n    tree_model.set_tree_mode(False)\n\n    # define view\n    tree_view = AYTableView(variant=AYTableView.Variants.Low)\n    tree_view.setModel(tree_model)\n    tree_view.setMinimumHeight(280)\n    container.add_widget(tree_view)\n    switch.toggled.connect(tree_model.set_tree_mode)\n\n    tree_view.selection_changed.connect(\n        lambda selected, deselected, tv=tree_view: print(\n            \"selection changed: \"\n            f\"Selected {[i.data() for i in selected.indexes()]} \"\n            f\"and deselected {[i.data() for i in deselected.indexes()]}) \"\n            f\"(full selection: {[i.data() for i in tv.selectedIndexes()]})\"\n        )\n    )\n\n    container.setMinimumWidth(700)\n    return container\n</code></pre>"},{"location":"autoapi/ui_preview/tag.html","title":"tag","text":""},{"location":"autoapi/ui_preview/tag_selector.html","title":"tag_selector","text":""},{"location":"autoapi/ui_preview/tag_selector.html#ui_preview.tag_selector.build_tag_selector_preview_widget","title":"<code>build_tag_selector_preview_widget()</code>","text":"<p>Build the test widget.</p> Source code in <code>ui_preview/tag_selector.py</code> <pre><code>def build_tag_selector_preview_widget() -&gt; QWidget:\n    \"\"\"Build the test widget.\"\"\"\n    # Sample tags with different colors matching the UX design\n    sample_tags = [\n        TagData(name=\"birds\", color=\"#4ECDC4\"),  # Cyan\n        TagData(name=\"animals\", color=\"#F4D03F\"),  # Yellow/gold\n        TagData(name=\"humans\", color=\"#E67E22\"),  # Orange/coral\n        TagData(name=\"robots\", color=\"#95A5A6\"),  # Gray\n        TagData(name=\"sets\", color=\"#27AE60\"),  # Green\n        TagData(name=\"street\", color=\"#7F8C8D\"),  # Gray\n        TagData(name=\"house\", color=\"#8E9A6D\"),  # Olive/green\n        TagData(name=\"vegetation\", color=\"#2ECC71\"),  # Green\n        TagData(name=\"vehicles\", color=\"#3498DB\"),  # Blue\n        TagData(name=\"water\", color=\"#1ABC9C\"),  # Teal\n    ]\n\n    # Create main container\n    container = AYContainer(\n        layout=AYContainer.Layout.VBox,\n        variant=AYContainer.Variants.Low,\n        layout_margin=16,\n        layout_spacing=16,\n    )\n\n    # Add a label\n    title_label = AYLabel(\n        \"Tag Selector Demo\",\n        bold=True,\n        rel_text_size=4,\n    )\n    container.add_widget(title_label)\n\n    # Create tag selector with some pre-selected tags\n    tag_selector = AYTagSelector(\n        available_tags=sample_tags,\n        selected_tags=[\"birds\", \"animals\"],\n    )\n\n    # Connect signal to log changes\n    tag_selector.tags_changed.connect(\n        lambda tags: logger.info(\"Selected tags: %s\", tags)\n    )\n\n    container.add_widget(tag_selector)\n\n    # Add another tag selector without pre-selected tags\n    container.add_widget(AYLabel(\"Another Empty Tag Selector:\", dim=True))\n    tag_selector_empty = AYTagSelector(available_tags=sample_tags)\n    container.add_widget(tag_selector_empty)\n\n    # Add another tag selector without any defined tags\n    container.add_widget(\n        AYLabel(\"Tag Selector with an empty tag list:\", dim=True)\n    )\n    tag_selector_empty = AYTagSelector(available_tags=[])\n    container.add_widget(tag_selector_empty)\n\n    container.addStretch()\n    container.setMinimumWidth(400)\n    container.setMinimumHeight(300)\n\n    return container\n</code></pre>"},{"location":"autoapi/ui_preview/text_box.html","title":"text_box","text":""},{"location":"autoapi/ui_preview/text_edit.html","title":"text_edit","text":""},{"location":"autoapi/ui_preview/tree_view.html","title":"tree_view","text":""},{"location":"autoapi/ui_preview/tree_view.html#ui_preview.tree_view.build_tree_view_preview_widget","title":"<code>build_tree_view_preview_widget()</code>","text":"<p>Show one AYTreeView per variant with lazy-loaded fake data.</p> Source code in <code>ui_preview/tree_view.py</code> <pre><code>def build_tree_view_preview_widget() -&gt; QtWidgets.QWidget:\n    \"\"\"Show one AYTreeView per variant with lazy-loaded fake data.\"\"\"\n    container = QtWidgets.QWidget()\n    root_lyt = AYVBoxLayout(container, margin=8, spacing=8)\n\n    for variant in AYTreeView.Variants:\n        label = QtWidgets.QLabel(f\"variant: {variant.value}\")\n        label.setFixedHeight(20)\n        root_lyt.addWidget(label)\n\n        tv = AYTreeView(variant=variant)\n        tv.setModel(LazyTreeModel(fetch_children=fetch_children))\n        tv.setMinimumHeight(160)\n        root_lyt.addWidget(tv)\n\n        tv.selection_changed.connect(\n            lambda selected, deselected, tv=tv: print(\n                \"selection changed: \"\n                f\"Selected {[i.data() for i in selected.indexes()]} \"\n                \"and deselected \"\n                f\"{[i.data() for i in deselected.indexes()]}) \"\n                \"(full selection: \"\n                f\"{[i.data() for i in tv.selectedIndexes()]})\"\n            )\n        )\n\n    container.setMinimumWidth(360)\n    return container\n</code></pre>"},{"location":"autoapi/ui_preview/ui_preview_setup.html","title":"ui_preview_setup","text":"<p>Setup environment for UI preview scripts.</p> <p>Makes sure that 'ayon_core' and 'qtmaterialsymbols' are in sys path.</p> <p>Also makes sure module 'ui_preview' is available. That is done by using a dummy module class if the module is not found. Python will auto-populate rest of the submodules if 'ui_preview' is available.</p> <p>It might be possible that it does not work in all cases and we might need to import each file in 'ui_preview' package manually. But for now this should be enough.</p>"},{"location":"autoapi/ui_preview/user_image.html","title":"user_image","text":""}]}